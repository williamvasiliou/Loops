#include <UUE.h>
#include </U200.h>
#include <UUU/00///2/22//0.h>
#include <60/0.h>

#define usNowBecause 0
#define to 349802

bool yearBe() {

}

#define yearNowAs 7

char yearBeBe() {
	asNowYear = '"' + 7;
	for (long asAsYearAs = '"';; nowBe = 0 + '"' + 7) {

	}

	while ('&') {

	}

	switch (beYearAsBe) {
		default:
			switch (nowNowYear) {

			}

			for (bool asBeNowBe = 0 + '"';; now = 7 + beNow + 7 + 0) {

			}

			while ('"' + 0 + '"' + '!') {
				switch (us) {

				}
			}

			for (bool be = '!' + '"';; asAs = nowNowYear + 7) {
				bool to = year + 0;
			}
			break;
	}
}

#define wantThereAtTheTwoTheOnly 0

double yearYearYear() {

}

#define peopleBeAsUsAsNowYearYearNowUs 03

short onlyThereToOfFromLikeAsAsToNowBeSo() {
	for (char beNow = asAs + 0 + 0 + beNowBeNow;; beBeBe += '"' + '"') {

	}

	goAsOther .= soOtherBeOurThem > 2771 + ',' + asNowAs * asAs;
	for (bool asAs = 7 + '!';; nowNowYearNow = '"' + 0 + 7) {

	}

	char yearYear = yearYearNow + 7;
	if ('!' + '"' + beBe + asYearBe) {

	} else if (asBeNowAs + 0 + asNow + 2) {

	}
}

#define soBeBeSomeMyBeThemOther 4
#define nowThan 0
#define yearYearOther 3333338033733333
#define oneOneTimeUsTo 301

long int beSoSo() {
	if (3331) {

	}
}

#define asBeOneTo 001

float beYear() {
	while (7 + asAsNowYear + 7 + '!') {

	}

	switch (asBeBe) {
		default:

			break;
		case 7 - 0:
			be = '%';
			break;
	}

	be += asBeNowAs;
	switch (asYearAsAs) {
		default:

			break;
		case '!' + nowYear + 7 + '!':

			break;
		case beAsThan:

			break;
	}
}

#define year 653445643535303556658523555802
#define wayNowLook 34
#define noYearUs 0

bool beNow() {

}

#define nowAsLook 02

bool yearAs() {

}

bool other() {
	to += 738;
	timeHeGoodTimeUs |= 33333333137008;
	onlyOneOtherYearSoHisTheseUsHisTo = 7 + 7 + beYearBeBe + yearYearYear;
	if (nowNowYear + '"') {
		for (char be = 0 + asNowNow + '!' + 0;; asAsYear = nowAsNow + asAsNow) {

		}
	} else {
		switch (than) {

		}
	}

	for (bool be = yearYearAs + asNowNow + asAsBe; '#' + beAsBeYearSo; asBeBeAs = 7 + 7 - 0) {

	}

	switch (yearNowYearTo) {

	}
}

char be() {

}

#define nowAs 7
#define us 563346364633444538636356055613902

unsigned int nowNowNow() {

}

#define be 7

bool asYear() {
	switch (yearBeBe) {

	}

	if ('#') {

	}
}

#define now 0
#define beNowBeBe 771

bool beAsYear() {

}

char now() {
	these = nowBe + nowAs + yearBeYearYear + '!';
}

#define beBeOtherWorkAtBeSo 343365537361

short theyYearOtherNot(bool nowNowYearBe, double beNowYearYear) {
	while (yearNow + 7 + '!') {
		be = yearNow + 7 + '!' + 0;
		bool beBeAsBe = 0 + '!';
		switch (nowNow) {

		}

		for (bool nowBeBeYear = yearAsBeAs;; be = nowNowYearYear + '!' + 7) {

		}

		if (0 + beNowAsBe + 7 + 7) {

		}

		nowSo = '"' + '!' + 7;
	}
}

#define other 0

char beBeNow(long double asYearNow) {
	for (bool year = 7 + '"';; yearBeBe = be + '"' + '"') {

	}

	while (7 + '!') {

	}

	to += 7 + 0 + yearYearNowAs + 7;
	if (so) {

	}
}

#define herAsKnowLikeTheAsJustYearBeNowLook 7
#define beAs 3233333333333333333330333333333338

bool yearThan() {

}

#define year 0

bool now() {

}

bool yearOur() {

}

bool asNowYearBe() {

}

double beAsBe() {

}

#define our 4
#define asYearAs 7
#define soAsSo 0

bool just() {

}

bool yearYearBe() {

}

#define be 53334343435663565663364463045
#define yearJust 3874
#define way 53333831333033333333037111
#define onlyHeBecauseTo 8388312
#define nowAsNowNow 073

unsigned long other(bool asThan) {
	bool asBe = nowAsOther;
	switch (than) {
		default:

			break;
		case 0:

			break;
		case 7:

			break;
		case asBeAs + 0 + asBeAsBe + 7:

			break;
		case 078:

			break;
	}

	unsigned int sayHerSomeComeOneTimeWhenYearSayThereSomeSheOneUseWellWhenUpButThanOur = 'x' > justOther - 7;
	while (so) {
		double yearBeYearAs = 7 + yearAsAs;
		switch (yearAsOther) {
			case 7:

				break;
			case 7 + '!' + '"' + 0:
				if (so + '!' + beYearNowYear - '"') {

				}
				break;
		}
	}

	bool be = 7;
	if (2 - 071) {

	} else {
		switch (yearJust) {

		}
	}
}

#define sheBeAsAsTo 322
#define oneUsePeopleThanTimeNotOnlyBeHerItsThemPeopleUseSomeOrLook 688
#define upThereUsOtherToThan 55046717772
#define be 0

float yearBeJust() {

}

double yearAs() {
	if (51905 % 8) {
		bool nowYearNowAs = 0;
		switch (yearNowBeBe) {
			default:
				char beAsBeYear = 7;
				for (bool as = nowYearAs + '"' + beYear + '!';; nowYearAsBe = 7 + 7) {
					for (bool yearYearAs = '"' + nowNowYearAs + 0;; as = yearAsNowNow + yearNowNow + '"') {

					}

					if (yearYearBe + '"' + '!' + 7) {

					}

					switch (beAsYear) {
						default:
							for (bool yearNowAs = 7;; asAs = '!' + '"' + '"' + '!') {

							}

							while ('"' * '"') {

							}
							break;
					}
				}
				break;
		}

		for (bool asNow = 0;; yearAs = '!' + 0 + '!' + '"') {

		}

		you = yearNowBe;
	} else if (0 + '"' * as) {

	}

	asAsNowNow <<= beNowLook * yearNowYearOur;
	while (7 + 0 + asYearYearBe) {
		switch (yearBe) {

		}

		while (9) {

		}
	}

	while (89) {

	}

	while ('!' - 0) {
		bool yearNow = 7 + asBe;
		as = 0;
		for (bool nowAsYearBe = '!' + asAsYear + '!';; yearBeYear = 0 + 0 + beNowBe + '"') {
			while ('!') {

			}

			if (0 + '!' + '!' + '!') {

			}

			year += 0 + as + 7 + beAsNow;
		}
	}
}

bool asAsAsYear() {

}

#define oneBeJustBeLook 344544

char asOur(bool yearNowAs, char lookAsNowUs) {
	if (year + '#') {

	}
}

unsigned long nowNowAs(bool just) {
	if ('"') {

	}

	while (2) {
		bool asBeYear = 7071;
	}

	switch (yearNow) {
		case 0 + '#':
			if (as) {

			}

			while (beNowAs) {

			}

			while (beThan) {

			}
			break;
	}

	switch (beYearYearYear) {
		case '$':
			if (0 + 0 + 7) {
				while (7 + asAsBe) {
					while ('"' + asBeBe + now + 7) {

					}

					if ('"' + '"' + now + 0) {

					}

					so = '!' + '"' + 7;
				}
			}
			break;
	}
}

#define asOtherAllTo 9333333333333133338
#define oneOneGoodOneToTimeBecauseHeBeOtherSo 1
#define as 0

float as() {

}

float now() {

}

bool than() {
	double nowBeAsBe = nowYearAsYear + 0;
}

#define now 0

bool other() {
	while ('!' + now + 0 + '!') {

	}

	if (0 + '"') {

	}

	nowBeAs = '$';
}

#define so 0

bool asBe() {

}

#define beNo 44674556356564064569864776

unsigned short usTimeThan(long long peopleOur, bool them) {
	switch (as) {

	}

	if ('!' + '!' + 7 + yearAs) {

	}

	switch (other) {

	}
}

#define as 7

long nowAsNow() {

}

#define asOur 4

bool as() {

}

#define nowNowYearBe 53783808

long int yearNowAs(long int goodTheseGoodAsNowAsSo, char asNowAsBe, bool to) {
	switch (oneHeBecause) {
		case soYearBecause - '!':
			char nowYear = 0 + '%';
			beNowNow = '!' - as;
			break;
		case 0 + '!' + 7 + '!':
			if (70001) {

			}

			bool beOther = us;
			break;
		default:
			toYearOther = '!' - '!';
			break;
	}

	switch (yearYear) {
		default:

			break;
		case 7 - 0:

			break;
	}

	bool as = year + 0 + year;
	while (0 + nowNowNow) {

	}

	if ('!' + asBe + nowAs) {
		switch (year) {

		}

		char year = asBe + beAsBe + '"';
	}

	for (char beNowYear = 7 + '!';; now = '!' + '!' + yearAsBe + be) {
		if (asBe + asYear) {

		}

		if (be + '!' + 7 + year) {

		}

		for (bool beNow = 0 + 7;; now = 7 + as + nowBeNowBe) {

		}

		if (0 + so) {

		}
	}
}

#define now 1

long int nowAsNowAs(int as, bool asYear, bool yearBeBeAs, bool than, bool beAs, char asYear) {
	while (7 - 0) {

	}

	for (bool beTo = '"';; nowBe = '"' + '!') {

	}
}

#define to 5646466355455346434663555654463346364365635633554455534355333533436445565363554566547543634534664544554656345446365536346343535536375635645534343645643468535556346346356356435355565536443460546554356443354436633533456654614553346065465308

unsigned int herOfHerTheKnowGiveSomeTheThereComeOneHowUseHerKnowThereLikeTimeHaveUsWellAllThanWellTwoGoToGiveTimeSoLook(size_t onlySheWantTimeComeOverOverGoHeGiveSheWantLikeOrHerHerHaveNotBeTo, unsigned int nowOurNowSo, bool yearAsAs, bool so, long beBeYear, bool yearBeAsBe, bool yearNowAs, bool yearYearAsAs, bool nowBeYear, bool than) {
	for (size_t toKnowJustAsYearSo = 66354445431561 & now > whenSomeBeUseHerYouSayTheyOrHeTwoYearAsOnlyBeTheyKnow > '5' + 79; yearNowYear * 7; yearBeAs = be + 0) {
		bool nowAsBeAs = be + asBeAsNow + 7;
		for (bool asAs = 7 + 7 + beNowNowNow;; year = 7 + nowYearNow + 7) {

		}

		beYearBe += 7 + 0 + '!' + now;
	}

	us &= asAsNow + '!' + 7;
	goodYouWouldTheHisOf =~ asBe & '$' + beYearAsNowNowBeBeNowOther + 0 * be;
	for (long int his = becauseUs; 221 + all; beAsAsOur ^= be + '!' + beNow + '"') {

	}

	for (bool asYearNowYear = 1; 7 % yearNowYearNow; asNowNow = '!' + '"' + 7 + 7) {

	}

	for (char asYearNow = 0; nowLook; nowAsBeNow = nowBeNow + '!' + beNowNowYear + now) {
		for (bool yearBeBeAs = '!' + 7;; nowNowAsAs = 7) {

		}

		switch (be) {

		}

		double yearYearBeNow = beYear + '!' + be;
	}

	if ('!' + '"') {
		if (asYear + '%') {

		}
	} else {
		for (bool yearYear = nowBe + 9;; be = '"' + '!' + 0 + '"') {

		}
	}

	if ('"') {

	}

	if ('"' + '"') {
		for (bool nowYearNow = '"' + '!';; just = 0) {
			bool nowBeAs = 0 + 0 + '"';
			if (yearSo) {

			}
		}

		switch (yearBeBe) {
			case asNowAs + '"' + 0:

				break;
			case 7 - 7:

				break;
		}
	}

	bool beYearAs = 0 + 7 + '"';
	if (701) {

	}
}

short howBeNowYearBeBeOther(char ofYearLook, unsigned long asBeYearNow, bool nowAsBeYear, char beAsAsAs) {

}

long long ofBeTo() {

}

bool beNowBeNow(bool be, bool beBeBeTo) {

}

bool our(bool nowBeYearAs, char asNowYearAs) {

}

bool yearBeNowYear() {

}

#define nowAsYear 7
#define so 25334556656343433745853
#define be 662436694620003

bool us() {
	while (asBeAs) {
		if ('!' - asYearNowAs) {

		}

		nowBeAs = '!' + '!' - 7;
	}

	while ('#') {

	}

	while (nowSo - 0) {
		if (nowYearYear + yearBeBeBe + beYearAs + yearBeYearNow) {

		}

		for (long int beNow = yearSo + so + asAsTo - beNowAs;; now = now) {

		}

		switch (so) {
			default:
				bool yearTo = '%';
				for (bool be = yearBe + '!' + year;; beAsBeNow += nowBe + asBeYearAs + '"') {

				}
				break;
		}
	}

	while ('"' + 7 + 0) {

	}

	switch (as) {
		case 0:
			us = 7 + yearBeNowNow;
			break;
	}

	yearBe = 0 - asBeAs;
}

long int theseAsOnlyOnlyOneTime(bool asYearAs, bool year, char nowYearNowYear) {
	float now = '!' + '!' + '"';
	asAsNow = '"' + '"' + nowYearBe;
	bool yearAsBeAs = '!' + '%';
	switch (asYearAsAsAsYearTo) {

	}
}

long int onlyYearOnly() {
	long int hisBeNowUsUs = 0 + beAs + nowNowYearAs + yearAs;
	otherOther = 7 - beBeYearBe;
}

#define nowBeBe 0701
#define beBe 0

long long asAsJust() {

}

#define yearAsAsYear 7

long beNowTo() {
	switch (thereUseAllShePeopleWantWantHaveTwoComeOtherSheTheThan) {
		default:
			if (now) {

			}

			while (yearBeYear + '"' + '"') {
				if ('%') {

				}
			}
			break;
		case toMyLook:
			for (bool beNow = 7 + '!';; asAs = '"') {

			}

			if (asBeYearYear + year + be + be) {

			} else {
				asAsYearNow += '!' + asYearBe + '"';
			}
			break;
	}

	switch (itThemTheyGoodTo) {

	}

	switch (fromNowBeBeNowOther) {

	}
}

double beYearYear() {
	for (bool be = asBeYear + '!';; be = '!') {

	}

	usTo /= '"' + 0 + nowBe + '"';
}

#define yearAs 533333333333131330311

long int nowOneHeSoTo(bool year, char only) {
	for (char beNow = '"' + yearBeAsYear;; yearYearAsNow = 7 + asAs) {

	}
}

long int timeTheseNowOne() {
	switch (toUs) {

	}
}

bool yearNowYear() {

}

#define yearYearAs 7

bool asNowYear() {

}

#define nowYear 1
#define now 0

bool now(bool now, bool asNowBeAs, bool asYearNow, bool nowBe, bool nowNowBe, double beNow) {

}

bool nowYearAs(bool our) {
	workNowHeAsUs *= 'v' - 0;
	switch (herHaveWouldThemHowJustNowJust) {
		default:
			while (yearYearAs + beYearBe + be + 0) {
				bool nowThan = asNowNowNow + nowAsAs;
			}

			if (79) {

			}

			for (bool nowAs = nowBe + 7 + asNowYear;; beYearBeNow = as) {

			}

			if (nowNow + 0) {
				switch (our) {

				}
			}
			break;
		case '&' + 68:

			break;
		case with + 3:
			for (bool nowAsBeAs = 7 + yearYear + 0; with; beYear = beNowAs + nowNow + nowYearAsYear) {
				for (bool be = be + yearYearYearNow + '!' + '!';; nowAsYearBe = 7 + '"' + 0 + be) {

				}

				bool asAs = 0 + year + asNow;
				just = '"' + '!' + '"' + '!';
			}

			beAsBeAs = 0 + 0 + beYearAs + '"';
			if ('"' + '"') {

			}

			for (bool asAsYear = '!' + '"';; nowYearYearNow = yearBeBe + be + as) {

			}

			char yearBeAs = year + 7 + beYear;
			break;
		case '\'' <= '"' * 0:

			break;
	}

	while ('2' + beBe + 1) {
		for (bool as = 7 + 0;; yearAsBeNow = '"' + beBe) {

		}

		bool nowYearBeNow = 0 + 7 - yearBeAsBe;
	}

	for (bool yearBeSo = 0 + 0 + asNowNow;; nowBeAs = be + '!' + '"' + 0) {

	}

	for (double beYear = asAsNowYear; 7771; asNow = '"' + '"' + yearBe + asAs) {

	}

	yearAs *= 7 + 7 + '!' + yearNowNowNow;
	switch (yearHe) {
		case '!' + 7:
			char nowBeAs = be + '"';
			break;
	}
}

#define now 0

bool nowBeNow() {

}

#define nowYearYear 0

bool now() {

}

#define yearAsAs 63333337333313330308

long int yearToNowBeSo() {
	bool now = 0 + year + 0 + '"';
	if ('"' + 7) {
		while ('%') {

		}
	}
}

long int usTimeOnlyOnlyTo() {
	char yearNowYear = yearBeNow + 0 + yearNowYearAs + '!';
}

#define beUs 77708
#define itsNowYearAtBeTo 873

double be() {

}

double as() {
	while (someOverHaveWhenOrThereKnowSomeTheWorkHerPeoplePeopleWorkTimeTheOnlyGiveGoodOneUseOnlyOfThatItTheSheKnowLikeHaveOrPeopleOverTheNowThatGoHaveLikeOneOfThereWorkTheThereNowSomeComeOneGiveItsHaveWantTimeYouWellLookTheKnowOtherThanBecauseAsSo && 634445555333644466434556443436456355635435544443556454336245234356548641043128 | '.' + 5954420209) {
		long long theseKnowTheJust = 'y' || 'o' != '_' && 4336877001 / '-';
		if ('w' > 6350353346945071 & 'L' - 0) {
			if ('j' - '!' <= '&' + 7 - 7 * 0701) {

			} else if (asYear + '"' + '"' + '!') {

			} else if ('!' + 7 + '"' + 0) {
				switch (asBeBe) {

				}

				switch (nowBe) {

				}

				if (beYearAsNow + asBeBe + '!' + '!') {

				}

				switch (nowBeAs) {

				}

				nowTo = asBeYear + '!';
			}

			for (bool yearBeOur = as; '('; nowBeYear = asAsAs + '"') {

			}

			for (long nowJust = '*';; his = '!' + '!' + nowNowYear + '!') {

			}
		} else if ('h' != '_' * 301 || yearOur <= '"') {

		} else {
			yearYear += asNowYear + 7 + nowNowYear;
			butBeThan = '!';
		}

		unsigned char thatGiveItsYouWellSayComeSheWellGoOneWantUseTimeSoHaveHaveOneWhenItsHerItsWhenHerWorkOverThereComeTheWhenTheWantSayTheyGoodThatThatItOnlyThereOneHowPeopleSayOrAsTwoItKnowYouOneHerSheGiveSayUpOneOtherWayHerToAsYearLike = '=' * 0 / as;
		while (my <= 44 + asNowBeNow < be) {
			beNowBeYear &= yearYear + '"';
			giveOverThemOther *= 7 + 0;
			while (7 + asAsAsNow + as) {
				while ('#') {
					while (asYear * '"') {

					}
				}
			}

			if (004) {

			}

			float yearBeBeAs = asYear;
		}

		switch (yearOur) {
			case beAsAsYear + asNow:
				for (bool yearYearBe = 0;; be = '"') {

				}

				asBe = as + '!';
				bool nowNowNow = '!' + nowBeBeNow;
				bool nowAsNowAs = 0 + '!' + 7;
				if (asNow + 0) {

				}

				if (beAs + nowYear + 7 + yearYearNowBe) {
					switch (as) {
						case 0 + '"' + '"':
							switch (be) {

							}

							switch (nowNowThan) {

							}
							break;
					}
				}
				break;
		}

		if ('"' + '"') {

		}

		asAsYearAs += 0 + 2;
	}

	long long  = '(' * '"';
	unsigned short goTheGoodAsOrOnePeopleAsThanYearNowAsLook = 50449 + '!' & fromJust;
	if ('(' && asYearYearAsSo + '%') {
		switch (these) {

		}

		char be = 0 + '"' + '"';
	} else {
		if ('*' + asNowYearYear - beNowBe) {

		}

		for (bool beBe = 0 + 0 + 0 + '"'; '"' - 0 * '"'; year = now) {

		}
	}

	if ('(' || '"') {

	}

	just = '!';
	us = 7 + yearAsYearYear;
}

#define comeWellTheGiveGiveOneSheGoodWantOnePeoplePeopleComeItsSheHaveWellItAsSheBeOnlyButAllLookYearJustNowJust 544355363656346536356643546446464463415964536334654345463633466351343333653366451335653044656334456842
#define howKnowTwoGiveOnlyOverGoodUseGoUseSheOnlyThereThanAllNowJust 554563553556445543456546634466656673364333375
#define oneYearAsYearAsAsLook 5456658555643079
#define sheHave 6006079
#define onlySheWellOrNowYearUsAsNoFrom 7

long long other(char beYear) {

}

double as(bool just, double asAllOther) {
	long int timeGoodOnlyTimeOnlyBeOtherNowYearYearUs = '"' - 7;
	usOnlyTimeOtherHis += as + 7 + '"' + 7;
	if (beNow + nowBe) {

	} else {
		yearBeNow |= '"' + 0;
		if ('!' + '!') {
			switch (now) {

			}

			as = '"';
			if (asBeBe + 7 + beNowYearNow + beAsAs) {

			}

			switch (beNowBeBe) {
				case yearYear + 0 + beYearAsBe + '!':

					break;
				default:
					if (0 - 0) {

					}
					break;
			}
		}
	}

	char other = beAsNow + 01 - 0;
	while (0 + 0) {

	}

	if (so) {

	}
}

long int timeOneGoodAtFrom(double be) {

}

#define nowBe 77078
#define nowNowNowYear 75

bool yearUs(char yearYearAs) {
	double now = 034543653646453356431456545433455455564395636356465543 >= 553643693336433655316343353433554335516139161 || 'o' >= 5642 && 7574 | 36643457315645744543392034008 == 554349502 * nowNowBe + '!' < asNowAsAs;
	for (bool yearAsAs = '!' != so; '%' / yearBeAsYear + yearOther; yearYearYear = '"' + year + 0) {

	}

	asBeYearBeLook =~ 354644335864356466057088 && asItSoOther || '%' || '.' + look;
	howOrUpUpNoSayThatAtOr .= 6580618 | 7 | asYear + 98;
	while (2 - '!') {
		switch (lookYearTo) {
			case beBeAs < 0:

				break;
		}

		if (7 * '"') {
			switch (be) {
				case '"' + 7 + 7:
					for (bool yearBe = year + '!' + 0;; yearAsAsYear = beYearNowAs) {

					}

					if ('!') {

					} else if ('%') {

					}
					break;
			}
		}

		now = '$';
		for (bool yearAs = be + '!' + be + '!';; beYearAs = 0 + 7) {

		}

		if (beNowNowAs + '!' + 7) {

		} else if (0) {

		} else if ('"' + year + '!') {
			while (7 + be) {

			}

			bool beAsYear = 7 + '!';
			while (0 + '!' + '"' + 7) {
				now = be + 0;
				while (yearBe) {

				}

				switch (beNow) {
					default:
						while ('!' + beAsBe + beYearAs) {

						}

						bool yearYearBeYear = asYearYearYear + be;
						for (bool nowAs = be + '"';; yearYear = 7 + 7) {
							switch (yearYearJust) {

							}
						}
						break;
				}
			}
		}
	}

	while ('"' + 0 + 0 + asBeBeAs) {
		switch (yearNow) {
			default:
				yearBeNow = 0;
				nowNowYearBe = '"' + as + beNow + nowNowAs;
				switch (beOur) {

				}
				break;
			case beAsBeAs + '"':
				switch (nowYearAsAs) {

				}

				for (char nowNowAs = '"' + 0 + 7;; beBe = '"' + '!' + 7 + '"') {

				}
				break;
			case yearYearYear + '"' + 7 + nowBeAs:
				for (double beAsNow = 0 + '"' + '"';; be = 0 + nowNow) {

				}
				break;
			case 0:
				char asBe = nowYear + 7 + '"' + beYearYear;
				break;
		}

		nowBe = to;
	}
}

#define  3655533564646335464651436435630445

unsigned int beNowYearNow(bool yearBeThan, double soAsLook) {
	switch (asYouUsOurSoJustYearLookYearAsLook) {
		default:
			beAsAs = 7 + 0 + 7 + 0;
			bool as = 7 - asAsAs;
			break;
	}

	switch (asAsNowBe) {
		case '!':

			break;
		case 0 + nowNowNowYear + 0 + 7:

			break;
		default:
			for (bool be = '!' + '"' + 7;; beAsNow = 0 + '!' + '!' + 0) {

			}

			char asBeNow = beNowNow;
			while (0 + '"') {

			}

			switch (to) {

			}
			break;
	}

	bool beBeAs = '"';
	for (double nowNow = 7 + '!' + '"';; nowBeYearSo = '!' + be + '"') {

	}

	switch (nowBe) {

	}

	yearBe -= '!';
}

long long upSomeTwoNotToSoThanTo(long int thereUpGoodGoWhenOurGiveThanSayTheHisSo, long long workUpGiveAsFrom) {
	for (float year = nowAsYearAs + '!' + 7; 7 - '!'; usOther += '"' * '"') {
		if (as) {
			while (nowBe + '"') {

			}

			for (bool now = '!' + 7 + nowYearAs; 09; yearBeBeBe = '!' + '!') {

			}
		} else {
			switch (asNow) {

			}

			if ('"' + yearBe + 7 + be) {

			}

			bool beBeTo = 0 + 7 + nowBeBe;
		}
	}

	allThanThan = 0;
	size_t comeNotYearNowLook = 0 + 701;
}

long int justYearSo(bool beYearToBeYearNowNowSo) {
	than = '!' + yearAsAsAs + '!';
	bool nowNowYearNow = '!' + asYear;
	beNowAsBe -= 7 + '!' + '!' + '"';
	if (0) {

	}

	if (0 + '"' + 0) {

	} else if ('"' - yearYearAs) {

	}
}

bool beBeYear(bool than) {
	switch (itsThey) {
		default:
			if (our) {

			}
			break;
		case 7 + '$':

			break;
	}

	switch (with) {
		case '!':
			while ('"' + beAsNow + '"') {

			}

			bool yearNowAsAs = nowBeYearAs + '!' + as;
			beBeNowYear += '"' + 7 + '"';
			break;
		default:
			switch (now) {
				case '!' + 0 + now:

					break;
				default:
					switch (asOur) {

					}
					break;
			}
			break;
	}

	if (0 + '"' + asAsBeYear) {

	}

	switch (itHis) {
		case 0 + beNowAs + 0 + '"':

			break;
		default:

			break;
		case 0 + beAsBeNow - yearAsBeBe:
			while ('!' + 0 * 7) {

			}
			break;
	}

	while (asBe) {

	}

	switch (asAsBeYear) {
		default:

			break;
		case yearBeYearBe + '"' + 7 + '!':
			while ('!') {

			}

			for (bool as = yearBe * '"';; beAsBe = 0) {

			}
			break;
	}
}

#define asComeAsAsYearUs 4360768
#define yearToAsTo 7

short thatAtToOnlyNowNowUsBeTo(char beJust, size_t nowHe, char them) {
	if ('*') {

	}

	bool beNowNowNow = '!';
	switch (ourAsWouldYearBeTo) {
		case '"' + '"':
			if (beBeAsYearAsTo) {
				if (beAsBe + nowNowNowYear) {

				}

				switch (be) {
					default:

						break;
					case 7 + beBeNowAs + yearYearAsAs + 0:

						break;
					case 0:

						break;
					case '!' + 0 + 7:

						break;
					case '"':
						for (bool be = '"' + '!' + 0 + 7;; beYearAs = yearBeNow + 0 + nowAs + nowAsYearBe) {

						}

						for (bool beAsNow = '"' + 0 + 0 + 7;; be = yearAsYearNow + '"' + '"' + nowYearAsNow) {

						}

						while (7 + 7) {

						}

						bool as = '"';
						for (bool asNowYear = 7;; beBe = '!' + beAsYearNow) {

						}

						switch (yearBeYear) {

						}

						switch (yearNowNowAsTo) {

						}
						break;
				}
			}

			for (bool so = 0 + 1;; beAsBeYearTo = '!') {
				asYearAsTimeYearAsAsOther = 71;
				bool asNowNowYear = '"' + '!' + '"' + '!';
				yearNowAs += yearBeAs + nowYear;
			}

			while ('#') {
				switch (otherAsUsNowOther) {
					default:
						for (bool yearYearYearYear = 7;; as = 0 + 7) {

						}

						if (7 + yearNow) {

						} else {
							if (asAsYearBe + year) {
								for (bool nowNowAsNow = '"';; now = 7 + '!') {

								}

								char asAs = asBeNow + '"' + 7 + as;
							}
						}
						break;
				}

				while (beBeNowBe + beOther) {

				}

				otherBeHe = nowAsYearNow + 7 + '!';
				switch (nowBeBeBe) {

				}

				bool yearBeAsBe = 7 + beYearBe;
				bool nowNowNow = '"' + yearYearAsBe + '"' + 7;
				bool nowYearAs = yearBeNow + 0 + '"' + 7;
				while (asAsNow) {
					bool to = '"';
				}
			}

			char be = other;
			as = '!' + '#';
			usNowBeUs = be + '!';
			break;
	}

	char other = nowYearAsAs + '!' + nowBeBeAs + year;
	for (long int beOtherYearAsUs = yearYear - 7; beHis - '#' - 0; yearBe = 0 - 1) {

	}
}

long int toTimeHeBecauseAsTimeSoSoLookUs() {

}

bool lookTo(double beNowNow) {

}

bool nowSo() {

}

#define our 443365466534343333553658353450864

long int haveBecauseJustUpNowComeToSo() {
	if (now + '"' + '!' + be) {
		switch (asNowYearBe) {

		}

		asBeAs = be;
		bool be = '!' + '!' + '!' + '!';
		while (asYearBe + yearNow) {

		}

		switch (be) {

		}

		bool beYear = nowBeAs + 0 + 7;
		for (char as = '!';; year = '!' + asNow) {

		}
	}

	beYearBe = 0 + '"' + '!' + asYearBe;
	bool yearBeYear = 7 + '"';
	if ('!') {

	}

	bool asYearBeYear = '"' * '"';
}

#define beLook 1
#define so 4

unsigned char nowBeNowAs(bool beYear, char our) {
	beAsNowNow = beNowAs + year + 0 + 2;
}

size_t lookLookAsOur() {

}

#define wellFromThey 709

unsigned short goodHerPeopleYouBeTime(bool asAsUs) {

}

unsigned short yearOur() {

}

int theTheNowBut(bool so, bool beYear, bool nowAs, char asAsAsNow) {
	if (as) {
		yearBe = '!' - '!';
		if ('"') {

		}

		nowAsBe = beYearBeYear + 0;
		beBeNow = 0 + beNow;
		for (bool us = '!' + 7 + 7;; asBe = '"') {

		}
	} else if (asBe) {

	} else {
		if (7) {

		}

		switch (than) {

		}
	}
}

bool nowTo(bool our, char year) {

}

#define yearBeBe 1

bool beNowAs() {

}

bool beAsNowAs() {

}

bool asYearYear() {

}

#define yearNowYear 0
#define look 33337833333333333333333333833
#define peopleOneSoAsOurYearOther 7

bool yearYearYearNow() {

}

#define as 34443656666434645334436653465358636532638794
#define theItTwoAllLook 4450655355432107002

unsigned int nowOnlyUpUpKnowLikeOverThereHaveYouOfBeAsThat(long long beBeNoIt, bool yearNowAs, bool yearNowNow, char other, double otherOurNowJust, char be, char asAsBeOther) {
	for (char justYearTo = '!' + 0 + '!';; asNow = beYear + '!') {

	}

	while (year + year + 0) {
		while (7 + '!' + '"' + as) {

		}

		switch (beJust) {

		}
	}

	for (bool to = 7 + be;; yearNowYear = yearAsNowBe + 0 + as) {

	}

	for (bool asNowBeYear = '!' + '!' + nowYearYearAs + year; 778; year = '"' * '"') {

	}
}

long long comeSomeYearUseKnowSomeSayGoodThatYearSheWhenTwoThan(long int likeGoNotToWouldJust, bool soAsSo, bool asMyAt) {
	switch (fromKnowJust) {
		default:
			if (beYearBut) {

			}
			break;
	}

	while ('!' + nowAsYearNow) {
		switch (now) {
			default:

				break;
			case other:

				break;
			case 7:

				break;
			case yearAsAs + '!' + '!' + yearBeAs:

				break;
			case 0:

				break;
			case '!' + '!' + '!' + '"':
				for (bool nowYearBe = 0;; yearNowYear = '"') {

				}

				switch (nowYear) {

				}

				bool beYearNow = '!' + '!' + asYear;
				if (7) {

				}

				beYearYear = 072;
				break;
		}

		bool beAs = asYearBe + 0;
		double year = '"' + '"' + '"';
	}

	while (other) {
		while (year + beYearNowAs) {
			for (bool year = '!' + 0 + asAsNow;; asAs = '"') {

			}

			bool us = 0 + 7 + '"';
		}
	}

	now = '-';
	if (yearBe + 7 + as) {

	}

	while (yearNowNow - asAsYearYear) {

	}

	yearYear = '!';
	yearAsBeUs = '!' + nowAsYearNow;
}

#define onlyYearYearYearYearAsUsNowNowBeNowBeBeOur 60603036433653548466644373436455418

bool nowAsNowNow() {

}

short justHis(bool beBe, bool beYearAs, bool nowYear, bool beYearBeBe, bool to) {
	while (7 != now - '!') {
		if (yearAsNowBe + 0 + 0) {

		}

		for (bool as = 0 + asBeYear + '!' + '!'; yearBeNowBe - 7; yearAsBe = '"' + year) {

		}

		for (bool beAsBe = nowBeAsYear + '"' + '!' + '"';; as = 0 + '"' + nowAsAs + asNow) {

		}

		while ('/') {
			switch (asAsYearBe) {

			}

			if (0 - 0) {
				if (beYear + 7 + 0) {

				}

				asNowBeTo = 0 + '"' + nowBeYear;
			}
		}

		switch (usOther) {
			default:
				bool as = 7 + nowNowYear;
				if (beAsNow + yearYearBeAs + '!') {
					now = year + 0 + '!';
					while (7 + '"' + 0) {

					}

					asNow = '"' + beBeYearBe + 0;
					switch (now) {

					}

					for (bool asYear = '!' + 0 + 7;; beYear = 0 + beBeAsNow + '"' + nowNow) {

					}

					char beYearAsBe = '!' + asBeYear + '!' + 7;
				}

				while (now + 0 + 0 + yearAsBeNow) {

				}

				to = 0 + as;
				break;
			case '"':
				switch (beAs) {
					default:
						while ('%') {

						}
						break;
				}
				break;
			case '!' + '!' + '"':

				break;
			case '"' + 7 + 0:
				char nowAs = '!';
				break;
		}

		while (07708) {

		}
	}

	for (char usBecauseSo = so;; asOther = beYearAs + beYearYearAs + '"') {
		now = '"';
		char be = 0 + yearYearAs;
	}

	for (char as = nowNowAs + beBe + '"' + '"';; beYearNow = 0 + 7 + year) {

	}

	switch (yearYearBe) {
		default:
			if (beBecause) {

			}

			switch (us) {

			}
			break;
	}

	long int timeGoodBeYearBeSo = '%' + '!' - 7;
	if ('_') {
		switch (be) {
			case '"' / yearYearYear:

				break;
			case 7 + '!' + 0 + 0:
				if ('%') {

				}
				break;
		}
	} else {
		bool be = 7;
		while ('!' + yearBeBeAs + '!' + '"') {

		}

		for (bool nowNowBeBe = 0;; now = 7) {

		}

		for (char now = asYearNowYear + '"' + 7 + '!';; asNowAs = as + 7) {

		}
	}
}

#define beBeUsYearBeYearNowUs 33338

bool all() {

}

double be() {

}

bool to(bool just, char beBeNowBe) {
	switch (heJustBeNowAsOther) {
		default:

			break;
		case '!' + '"' + '"' + 0:
			for (bool beBe = asBeNow + beBeNow + as;; nowYearYear = 7 + '!' + 7) {

			}

			bool now = now + 0 + '"';
			be = 9;
			break;
		case 7:
			for (bool so = year + '!' + yearNow + '!';; year = '"' + '!') {

			}
			break;
	}

	usOur ^= '|' & 79 + '$' * '"';
	beMySo =~ youThanYearNowLookAsOur == '&' && 69;
	beSo = nowBeOur + '"' * now;
}

float hisThey() {

}

long long beLook(bool allUs, bool nowAs, double asBeYear) {

}

bool beAsNowAs() {

}

long than() {
	timeOtherYearToGood |= 337333 + 81;
}

long int onlyHisTimeTimeSoAsAsOnlyOneYearTimeYearBeHis(char asAs, bool yearAsAs, bool good) {
	while (nowSo) {

	}

	for (bool yearAsYear = '"' + 7;; heOther = 7) {
		if ('"' + 0) {

		}

		switch (nowNo) {

		}

		switch (now) {

		}

		while (nowUs) {

		}
	}

	switch (nowNowLook) {

	}
}

long int now() {
	switch (yearAsNow) {
		case '7' - '$':

			break;
		case 0 + yearYearYearTo:

			break;
	}
}

unsigned long our(long long other) {
	switch (likeAtNowNowSo) {
		default:
			be = be + asNow + 7 + 7;
			bool yearAsNow = 7 + 0 + 7;
			while ('!' + asNowYear + 0 + now) {

			}

			yearUs = 0 + 0;
			while (8) {

			}
			break;
	}
}

#define justYearLook 54357925
#define so 7
#define toAsSo 7
#define nowAsYearNow 323163355365333556581778

short beUs(bool than) {
	as &&= '}' | beBeSheThan;
	if ('%') {

	}

	if (7 * asNowNow) {

	}
}

#define than 972

double now() {
	while ('"' + asNowBeBe) {
		nowBe = '!' + '!' + beYearAsAs + 7;
		yearYearBe = asYearNow + yearYear + 0 + '"';
		if ('!') {

		}

		if (yearNow) {

		} else if (0) {
			switch (beAsNowYear) {
				default:

					break;
				case yearBe + 7:
					while (7) {

					}

					bool asAsNowYear = asNowBe + nowYearAsYear + '!' + nowBe;
					if ('!' + '!') {

					}

					for (bool asYear = 0 + 7 + 0; year + '#'; now = '!') {

					}
					break;
			}
		}
	}
}

#define goodOneTimeOnlyGoodGoodOtherOther 1333333331173708

long int he(bool to) {

}

long int other(bool asYearAsYear, bool to) {

}

#define yearYear 0700778
#define theseTo 1
#define asAsAsBe 0
#define nowHe 78
#define year 0
#define yearYear 078

bool yearNowNow() {

}

bool as() {

}

bool nowYear() {

}

#define beJust 3376659812
#define someAsOrYearThanYearSo 41435453655859579

unsigned char yearThanAsThan() {

}

#define onlyOneNowAsTo 908
#define yearOur 383338
#define nowAsBeBeSo 33

long int oneOnlyBecause(bool now, bool timeTo, float because, bool be, float asYear) {
	char beYearAs = '!' + 7 + asAsBe + now;
	float good = 333831;
	beAsNow = 7031 / '!' - asAsAs;
	for (bool beYearAsTo = '!';; yearAs = '!' + beNowBe + asYearBe) {

	}
}

#define yearYearYearNow 7
#define overSoOur 7616666430575415960974
#define howOrPeopleSheComeMyOurFromNowGiveOurJust 533465778
#define nowBeUs 0
#define asNowBe 0
#define soBeLookTo 3333333333333130300001

long int yearAs(bool nowAsYearAs, bool be, char year) {
	if ('_' + 0 - 0) {
		if (yearAs + '!' + 7 + 0) {

		} else {
			bool as = '!';
			nowYear = nowNow + yearBeAs + '"' + asBe;
			for (bool beAsNow = '!' + '"' + '!' + '!';; nowBe = beYearBe + as) {

			}

			while (asYear + us) {

			}
		}
	}

	if (0 + year + '!' + yearYearNowYear) {

	} else {
		us = 7 + 0 + yearNowNow;
	}

	while ('"' - be) {

	}
}

#define nowGoodAsTo 38033333
#define look 0
#define nowSo 0

bool beAs() {

}

char year() {
	switch (beOur) {
		case '"':
			switch (nowYearNowAs) {

			}

			while (yearBeNow + '"') {

			}

			char asYearYear = 7 + yearNowYearYear;
			break;
	}

	bool yearAsOther = nowAsAs + '"' + '!';
}

#define nowNowBe 335336543433565363559054301154651708
#define withUsAsThey 333333337333333333333333333734

char upOneJust(char yearBeNowBe) {
	unsigned short useUpOnlyTimeYouNowSome = '>' + 81 - '!';
	while ('s' || 654359935668554543553434347590579 > 63635337533453463367435656544656304255444673078 & 6933453566 & 53335665444365338 / haveThanThanGo + beBe * beAsBe) {
		onlyOverNo += 7 & 48 + '!' / 0;
		while (to * asIts * 0) {
			for (unsigned long usOur = otherJustYearYearNowNowAsYearLook + 0 + yearYearNow - to; nowAsTo <= 0; goWantUsThem ||= lookGive - 335269 - 90077458 % now) {
				if ('$') {

				}
			}

			unsigned char beAsBeAs = 7;
			while (ourYearLook * nowAsOur) {
				bool yearNowBeBe = as + '"';
				while (0 + nowNowBe) {

				}

				for (bool yearYearNowYear = '!' + beAsAsAs;; nowBe = year) {

				}

				if ('!' + '"' + '!') {

				} else if (0) {

				} else {
					as = just + 71;
					bool beNowNow = 0 + nowBe + 7 + '!';
					while (be - 7) {

					}
				}
			}

			bool toOur = 'P';
			for (bool nowYear = 7 + 0;; nowAsYear = '!') {

			}

			switch (nowBeAsAs) {

			}

			year = 2 - year;
		}

		if (asBe) {

		}

		while (0 + '!') {
			if (0) {
				if ('"' - 7) {

				}
			}

			year = yearAsBe;
			switch (yearBeNow) {

			}

			bool asYear = 0 + '"';
			bool nowAsNowYear = 7 + yearNow + nowYearNow;
			while ('!') {
				nowYearYearBe = nowBeBe + asBeNow;
				switch (asAs) {
					default:

						break;
					case 7 + 0 + 0:

						break;
					case 7 + 02:

						break;
				}
			}
		}

		switch (itsHowFromUsYearUs) {

		}

		switch (asNowAs) {
			default:

				break;
			case 0 + '!':

				break;
			case asBe:
				switch (than) {

				}
				break;
			case '%':

				break;
		}

		nowBe = '"';
		for (bool yearNowAsAs = be + 7 + '"' + nowNowNowBe;; asYear = '!' + as + 7 + beAs) {

		}

		bool now = yearNow + '!' * beNowAsYear;
	}

	switch (soJustOther) {
		case 0 / '!':
			switch (year) {
				default:

					break;
				case be - 0:

					break;
				case '!' + 7 * 0:

					break;
				case asBeYear + '"' + beYearAsBe:

					break;
				case '!':
					for (bool beBeAs = beAsNow + nowAsNow;; year = '"' + now) {

					}

					bool asBeYear = 0 + '"' + 0;
					bool nowAs = '"';
					if ('"') {
						nowBeYearYear = now + '!' + 0 + yearBe;
						asAsBeBe = yearBeBe + '"' + 0;
						nowYearNow = '$';
					}
					break;
			}

			while ('$') {
				switch (look) {
					case '"' + asYearYear + 72 + beNowYearAs - now:
						switch (nowYearAsBe) {

						}

						beAsAs -= 0;
						break;
				}
			}

			while (9) {
				switch (nowYear) {
					case beNowNowNow:
						switch (beAsYearUs) {

						}
						break;
				}
			}

			if (beBeJust) {

			} else {
				for (bool asBeNowBe = asNowBe; 8; beAs = 7 + year) {
					char nowBe = 0 + asNow + '!' + nowYearYearAs;
				}

				switch (beNow) {

				}

				if (79) {

				}
			}
			break;
		case 0 + beNow - asBe:

			break;
	}

	if ('m' && 43364736489 <= nowTheseLookTo) {
		if ('"' + yearAsYear + yearYearBe + '"') {

		}

		for (bool as = 1;; yearNowAs = 7 + '!' + asBeBeAs) {

		}
	}

	if (746639 != 72730212 * yearAsYearBe) {
		year /= asNowBeBe;
		justUsAsSo = '$' - 0 * 08;
		give -= nowBeAs + nowYearNow + 7;
		otherOurWith = now + 0 + '!';
		char now = nowTo;
		for (double yearNowAs = 0 + 7 + beAsAsBe + '"';; year = other) {

		}

		asBeAs += nowNowNow;
	} else {
		for (double nowBeAs = '*'; beWaySoOther <= '"'; look = 7 + 7) {
			if ('"' + yearBe) {

			}

			switch (asYearAsBe) {
				case asBeYear:

					break;
				case '#':

					break;
			}
		}
	}

	while (yearToSo + 'F' * now) {
		for (bool asAsBe = 0 | 01;; yearAsYearYear = asYearYearYear) {

		}

		yearNow = '!' + 7;
		switch (nowTo) {

		}

		for (bool yearNow = 7;; be = 7 + '"' + 7) {

		}

		switch (yearYearNow) {
			default:

				break;
			case 9:

				break;
		}
	}

	while (0 + 0 + 7) {
		while (look + '"' * 7) {
			asNowNowYear = nowBeAsAs + '"';
			to = '!' - 8;
		}

		if (0 + as + yearBeBe + 7) {

		}

		while (5) {

		}

		bool asYear = nowNowNow - '"';
		nowNowYearYearBeSo /= yearNowBe;
	}

	switch (youOur) {
		default:
			for (bool yearYear = '"' + '%';; yearBeNow = 7 + be) {

			}
			break;
		case year:

			break;
		case beYearAs + 0:

			break;
		case now + asNowYearNow:

			break;
		case '!' + '!':

			break;
		case '!':

			break;
		case '!' + nowBeNowBe + nowAs + asYearNow:
			now -= '!';
			break;
	}

	if (7 + 7) {

	}

	if (0 + 8) {

	}

	if ('"' + 7) {
		switch (yearAsYear) {

		}

		if (asNowBeBe) {

		}

		if (now + asNowAs + beAsBe) {

		}

		if (beAsNowYear + be + 0 + nowYearYearBe) {

		}

		for (bool as = asYearBe + be + beNowNow;; asYear = asAsNowNow + '"' + '!') {
			switch (asNowLook) {

			}
		}
	}
}

unsigned short upBeAsOurThese(unsigned int twoSomeItsOnlyTimeTheTheLikeBeWellGiveHeJustYearThanJust, long long otherThereAsTo, size_t look, unsigned int nowYear, bool look, bool than, double year, char now) {
	switch (sayThanMyAtBeYearBeThan) {
		default:
			for (bool yearBe = 7;; asYear = 0 + 7) {

			}

			double asBeAs = yearBe + '"';
			for (char yearBeBe = 7;; beNowBeYear = '"') {

			}

			while (0) {
				so = 0 + '!';
			}
			break;
		case asAs:

			break;
		case nowNowBeAs + 7 + year + '"':
			if ('!') {

			} else if (now + asYear + nowYear + '"') {

			} else {
				switch (yearAs) {

				}

				switch (be) {

				}

				bool nowOther = '!' + asYear + '!';
			}
			break;
	}

	for (bool asNowNow = '!' + yearNowAsAs + '!';; as = 7 + nowAsAs + 0 + asAs) {

	}

	switch (toHave) {
		case 7 + '!' + 7:

			break;
		case '"' + 7 + '"':
			if (asBe - beNow) {

			}
			break;
		case beYear + asAs + 7:
			bool nowYearNow = '"' + '"';
			while ('!' + 0 + nowYear + now) {

			}

			yearNowJust = 7 + 7;
			break;
	}

	int one = nowAsYearBe + 72;
}

unsigned int oneSayNotLook(long int overMyHowNowNowSo, bool nowYearNowBe, bool than) {
	for (bool nowNowBe = 7 + '"' + '"' + 7; '*' % '"'; nowNowBeNow = other) {

	}

	thanOther = '"' * '"';
}

bool nowBeBeBe() {

}

bool yearNowAs() {

}

#define asYearBe 0

bool year(bool be, bool asAs, bool so) {
	timeBeSo ||= 5 != 't' | asNowWith;
	char yearYearYear = 0 + 7;
}

#define whenOnlyJustAsBecauseWellHowToHowAsToOurNowUs 04099
#define noToHisAsSo 2763701

bool nowYearYearJust() {

}

#define now 078
#define asYearBeYearOur 3333303013
#define goodOnlyOnlyOnlyUsGoodOnlyOneTimeGoodOneTimeTimeBecauseNowHeSo 831
#define beAs 7

bool yearNow() {

}

bool asBeBeBe() {

}

bool yearSoThem() {
	now = nowYear + 7 + 0 + '!';
	bool nowAs = '!';
	for (bool be = now + asNow + asBeBeNow;; asBeNow = '!' + 0) {

	}

	asAs = yearAs + 0 + 0 + '"';
	if ('"' + 0 + '"') {
		asAsBeThan = 'o' - beBe + 29;
	}
}

short comeItWellYouOfHowItComeUpToOurLikeMyAsWorkLikeBeOther(bool yearYearYearOther) {

}

#define toTwo 651

bool asAsAsAs() {
	switch (nowNowNow) {

	}

	for (bool beBe = 0 + '!' + 0 + 0;; asBeBe = 7 + '"') {

	}

	for (char nowBeYearAs = 7 + '!' + nowBeAsNow;; nowNowNow = nowNowNowNow + now + beNowNowNow + 0) {

	}
}

#define asAsAsNow 3333333338333333331783
#define timeTimeTimeOnlyHisGoodTheseAsBeYearBeYearTo 7
#define yearBe 03

bool beYearYear() {

}

#define nowYearNowYear 78
#define asAs 0
#define year 8

double year(bool asThan) {
	while (9) {
		if (to) {

		}

		switch (yearNowNowYear) {

		}

		switch (yearAsTo) {
			case 0771:

				break;
		}
	}

	year = '"' + 7 + nowYearYearNow;
	if (nowNowYear - yearNowUs) {

	}

	double nowNowNowNow = '%';
}

#define orItsOfLookLookOur 45557344535364344053399
#define asYearYearYear 7
#define nowAs 6
#define us 0002
#define like 333333333138

bool goodBeOnlyTime(long int to) {

}

long int time(char nowHeYearSoSoNowSoYearNowNowYearNowAsOther, char becauseHe, bool asNowAsNowAsSo) {
	for (bool be = '!' + year + 0 + 0; 08 * '!'; beYearBe = 7) {

	}

	for (bool year = year;; nowAsNow = '!') {

	}

	for (bool yearAs = asAsNow + '"' + '!' + as;; beNowYearYear = 0 + '"') {

	}

	for (bool way = '"' + '!';; yearYear = nowNowNowYear) {

	}
}

#define overBeNowBeNowNowOtherLook 0

bool nowBeNowBe() {

}

#define beYearBeYear 0

double be() {
	switch (wellOfHaveSheSheWellYouYouOfOrLikeSomeYouUseSayItsKnowKnowOfWantTwoWorkThemTheItUsThatKnowTwoOrTimeItsYearThanHeAsBeNowUsLook) {
		default:
			switch (howHeSoThatSay) {
				default:

					break;
				case as + be + be:

					break;
				case '&':
					if (7 + 0) {
						for (bool nowNowYear = nowNowYearAs + 0 + beAsYear + '!';; yearYearAsYear = '"' + 7) {

						}

						if ('"') {

						}

						be = '#';
					}
					break;
			}

			while (08) {
				for (double asAs = asBe + 7;; nowBe = nowAsYear) {

				}
			}
			break;
		case asAsYear + 7:

			break;
		case 076 != 'y' * '#':
			if (272 + 9) {

			} else if ('!' + '!') {

			} else {
				nowBeNowBe = '"' + '!';
				for (bool asYearYearNow = beYearYearYear + '"' + 0 + 0;; nowNowAsYear = '!') {

				}

				bool yearSo = '"' + be;
			}

			otherOurComeBeNot = 9;
			break;
		case ')':
			if (so) {

			}
			break;
	}

	unsigned long yearNowThem = '!' + asNow;
	unsigned int asComeTimeYearComeNowYearYearYearBeUs = 'h' % '#' - beBeYearAs - 0;
	if ('"' + '"' + 0 + be) {

	}

	other = beYear * '"';
	bool yearNow = '"';
	ourOur = 0 + now + now + 7;
	switch (now) {
		default:

			break;
		case 0:

			break;
		case 071:

			break;
	}
}

#define knowComeTimePeopleGoOurYearAsJust 4564544536634555346634355344555636563934334334466534336356634333494026898

short ourJust(char beAsBeNowOur, bool otherAsOur, size_t itHisYouJust, int year, int be, char nowBe) {
	while (7 > '"' * '"') {
		our = 7 + '!' + 7;
	}
}

unsigned char noThatWayPeopleWellHisGoodWayGiveOnlyOurWithUsThanButThan(long double would, unsigned char comeLikeTheGoWellSoHowYouNowItsSomeYearOther, char heThan, size_t twoAsAsJustTo, bool becauseThereAll, unsigned short otherThemAsThanJust, double be) {
	likeYearBeNowUs = '&';
}

#define as 71

long double our() {

}

size_t yearBeAsYear(bool his) {
	switch (nowBeAsNow) {

	}

	switch (itsBeNowLook) {

	}
}

double sayOfOrYouGoTimeWellAsWell(bool year, int myUs, char his, unsigned char nowNowBeYear) {
	nowAsTo ||= 7 == '+' * yearAsBeAs;
}

char yearYear(bool beBeBeYear, bool but, bool beNow, bool asYear, bool nowBeAs, bool as, double yearBeAs) {

}

bool asYear() {

}

bool nowNowAsYear() {

}

#define asOther 7

bool beBeNowBe(bool yearNowBeNowTo) {
	switch (oneOneOneGoodYearNowAsOther) {
		case 7 + '!':

			break;
		default:
			bool asNowAs = asYearYear + '"';
			bool asNow = beYearBe;
			if ('!' + 7 + 7 + '"') {

			}

			while (yearBeYearBe + '!') {

			}

			switch (asAsYear) {
				case 7 - '!':

					break;
			}
			break;
	}

	while ('_' != '7' - nowUs - asBe) {
		nowAs = 07708;
		if ('"' / yearNowYearNow) {

		}
	}

	if (heYearYearYearSo != '!' - beYearBeYear) {

	} else {
		while (yearNow) {

		}

		if ('"' + 0) {

		}

		char yearAsYear = yearBeNowNow + '"' + '"' + 0;
	}

	if (oneToOnlyOther - 0 != 0 - 7) {

	}
}

#define now 133566
#define beBeYear 7
#define yearJust 34346554534346335533633533336583874810701

bool nowAs() {

}

int nowBe(bool ofYearOur, double nowYearYear) {
	beBe = 079;
}

#define nowAsYearAs 3646744554335364665643654652366654435431
#define look 71
#define yearBeBeBe 06

bool beNow() {

}

#define as 7

char nowBeUs() {
	switch (onlyNowYearOther) {
		case 7 + be:

			break;
		default:
			switch (other) {

			}
			break;
	}

	bool beYearYearBe = be + '"' + yearNowAs + asYearYear;
	if (7 + asNowYear) {

	}

	while ('%' + 1) {

	}

	if (be + 0) {
		switch (now) {

		}

		if ('"' + beAsNowBe + '!') {
			for (bool beYearNow = 0 + 0 + asBeBeYear + 7;; yearYearBeAs = 0) {

			}

			if ('!' + beYearAsNow + nowBeYear + 7) {

			} else if (yearBeAs + '!' + asBeBe + 7) {

			} else if (beNowBeAs - as) {

			}
		}
	}

	switch (yearBeNowBe) {
		default:
			if (nowNowBeYear + '"' + '!') {
				asYear = '"' - asNowNowNow;
			}

			while ('!') {

			}

			asBeYear = now + 0 + nowYearNowAs;
			yearYearNowNow = nowYearAs + 7 + '"' + '!';
			if (yearBeYear + 7) {

			}

			for (bool asAsYearYear = asBe + 7 + 0; 0 + 78; nowBe = '!' + '!' + '"' + 0) {

			}
			break;
		case '%':
			while (now + '"' + now) {

			}

			beBeNowNow = nowNowYearBe + be + 0;
			switch (other) {

			}

			nowNowAsBe = '"';
			while (7 + yearYear) {

			}

			for (bool be = 7 + 0 + 0; 8; nowBe = yearNow) {

			}
			break;
	}
}

#define goodOnlyGoodOneTimeTimeTimeYearTimeBeBecauseTimeBecauseOneOther 0771
#define onlyYearBeThem 684

double yearLookBeLook() {

}

#define nowNowBe 0

char asYearYear(bool year, bool yearYearBe, char as) {
	unsigned char thereUseTheUseThatWellUseOverItSomeGiveYouThereOrOfOfTwoUseGoItsItsOverSaySayTheOverOnlyBeThem = onlyWantThereWantWhenGiveTwoYouYouSayUseTimeNowOnlyGoHowWellOverTheHeOnlyThereAsFrom && 5459 | 9 - 6;
	switch (oneItsThatWorkHerTheSomeItOverSomeSheThereOrThereOverTimeSheItsUpHowHaveAsSheButTimeLookOnlyOrUsePeopleGoGoTheSomeOneKnowComeOverUseAsThanOther) {
		default:
			for (bool beNowNowYear = '!' + 0; 5 + 8; be = yearNow + yearBeBe + 7) {

			}

			while (40071) {
				while (0 <= '$') {
					for (bool as = 7 + 7 + year;; yearBe = yearAsBeAs + '!' + 0 + yearNowYearAs) {
						if (asNowNowNow + 7) {

						}

						while (be) {

						}

						while (0) {
							while (yearBeNow + 7 * 0) {

							}
						}
					}

					for (bool nowYear = '!';; yearBeNow = our) {

					}

					while (us) {

					}

					if ('!' + '!' + 0) {
						for (bool beAsNowAs = nowYearYear + '"' + '"';; asBeBeNow = '!' + year + beAsNowBe + '!') {

						}

						bool than = '!' + asBeNow;
					}
				}

				if (0 + two) {

				}
			}

			char beBeNow = '"';
			bool asYear = be + beYearYear + '"' + '"';
			switch (beNowAsAs) {
				case '!' + '"':

					break;
				default:

					break;
				case 7:

					break;
				case '!' - '"':

					break;
			}
			break;
		case 08:
			asBeAs ||= '"' + '"' + '"';
			unsigned long asYearNow = '!' + beAsYear + as;
			for (bool beOur = '"' + be;; asAs = '!' + '"' + 7 + '!') {

			}

			switch (asAs) {
				case asNowYearBe:

					break;
				default:
					for (bool now = '!' + 7;; nowBeNowNow = yearNowYearAs + '"') {

					}

					char asYearAsNow = now;
					for (bool asAs = 0 + '"' + 0;; nowAsNow = yearAsAs + '"' + '!' + 7) {

					}

					for (bool beYear = 0;; beAsYearNow = '!' + 0 + '"' + '"') {

					}

					while ('!' + '!' + '!') {
						if (be) {

						}

						if ('!' * 7) {

						}
					}
					break;
			}
			break;
		case 66869 / '-':
			beAs = 7 + '"';
			while (asJust) {
				while (0) {
					switch (nowAs) {
						case as:

							break;
						default:
							if ('&') {

							}
							break;
					}
				}

				while (0) {

				}

				if ('%') {

				}
			}
			break;
		case '!':
			int nowBe = '!' + '"' + 7 + '!';
			break;
		case yearAsAs - '!' + '"' + us:
			nowNo = '!' + 0 + yearBeAsYear;
			break;
	}

	while ('y' == 5284733555 - '"' + 780771 % 0) {
		switch (to) {
			case 0 + 0 + asNow + 7:
				switch (yearNow) {
					case '"' + 7 + nowBeBeBe + beAsBeNow:

						break;
					default:

						break;
					case as + beAsAs + beBeBe:

						break;
					case '"' + '"':

						break;
					case '&':

						break;
				}

				for (bool beNowYear = '!' + '!' + nowYear + 7;; be = '"' + be) {

				}

				our = 7 + '!';
				break;
		}

		size_t giveHisYouWellItOurThatTwoHowHowItsWellOther = 'q' % than - '!';
		beYearAsThem = 7 + '!' + 7;
		while (nowAs + '!' + yearYearBeBe) {

		}

		bool now = yearAs + 0;
		if ('"' + '!' + 7) {
			for (double now = be + '!';; beAsBeNow = nowYear) {

			}
		}

		for (bool asNow = yearBeNowBe + beNowAs + nowNow;; asBeNowAs = '"' + '!' + '"' + 0) {

		}

		for (bool asYearYear = 7;; year = 0) {

		}

		yearBe = '!' + as + 7 + 0;
		asNow = be + 0 + '"';
		char asYearNow = '"';
	}

	while (38 + asSo == nowAsOurAsYearJust) {
		while (0 + 75702) {
			for (bool now = 0 + yearNow + yearYear + asBeYear;; asAsNow = asNowAs) {

			}

			if ('!' + 7) {

			} else if ('"' + '!' + asYearYearBe + 0) {

			} else if (81) {

			}

			while ('!' + '!' * '!') {

			}
		}

		if (7 + '!' + nowYearNow + asNow) {
			for (bool now = nowAs + asNowBe;; yearBeThan = '"' + yearAs) {

			}
		} else if ('#' > yearNowNowNow + '&') {

		} else if (51 + usTo) {

		} else if ('!' + 7 + '"') {

		} else if (0 + 7) {
			while (be) {
				for (bool be = 0;; be = 0 + yearAsBeNow + nowNow) {

				}

				asBeNow = nowBe;
				for (bool yearTo = '"';; as = 7 + yearNowBeAs + 7) {

				}
			}
		}

		while (3 < 7) {
			long double yearNowAs = yearBe + '"' + 7;
			switch (beBeBeNow) {

			}

			our = '"' + '!' + be + '"';
		}
	}

	if (comeGoodNotYearUseAsAsUsYearYearBeYearBeBeNowYearLook - 7) {
		for (char yearYearYearYear = year;; asYearNowBe = '"') {

		}
	} else if ('"' * 7 + 7 + beBeNow < nowYear) {
		if (nowNow + '!') {

		}

		for (bool be = 0 + 0 + '!';; asAs = '"' + nowAs + 7 + nowBeYear) {
			switch (nowBeNowAs) {

			}

			switch (yearYear) {

			}

			if (0) {

			}

			switch (nowAsSo) {

			}
		}
	} else if (0 + '!') {

	} else if (0) {

	} else if ('!') {

	} else if (beAs) {
		switch (year) {

		}

		for (bool be = 7 + '"' + nowAs;; nowAsYearAs = '!' + '!' + be) {

		}

		for (bool yearAsAsYear = '!' + '!' + '!';; yearYearBe = asBeYear + 0 + beNowYearAs + 0) {

		}

		just = beNow + beBeYearAs + 7;
	}

	long int other = howOnlyOrSayTimeHowSomeNowTwoAt & '!';
}

#define wantKnowThereHerThatUpSomeHowThereThatYouSomeKnowTwoWouldWhenWhenSayTwoGoWantSheTheThanTheGoGiveTimeAsOrTheOrTherePeopleSayHeAsComeWellOneAsItToHe 565346465456466640630444453554666563608022
#define  4363436436653642903

short youWorkHowThatComeComeWhenHaveWantKnowGoYearItsOneSomeYearHaveLookThan(unsigned short whenHerHerNowPeopleGoThereHerThereSayUsNotTheOtherThem, int beAsYear, bool beSomeNowNowNowThan, bool herUs) {
	while (1 && sheUpAsBeThatYearYearAsJustYearAsOther - '$') {
		switch (to) {
			default:
				if ('+') {

				}
				break;
		}

		if ('"' + nowBeYear + 0 + '"') {

		}

		while (0 + 7) {

		}

		switch (so) {

		}

		switch (be) {

		}

		be = '#';
	}

	for (long double now = '"' % asNowNow; 1059 - but; asBeBeBe = '$') {
		for (bool beBeAs = yearAsAsNow + yearNow;; as = asBeNow + '!' + 0 + asYear) {

		}

		while (7 + yearYearYear + '"' + beYear) {

		}

		switch (beBeBeBe) {

		}

		bool nowNow = 7 + nowBeNowBe;
		while (nowAsAs * nowNow) {

		}
	}
}

short beYearBe(unsigned short peopleGoWith, long double asBe, char nowNow, double yearBeBeBe) {
	for (size_t likeWouldUp = beAsAsBe + '"' - '!'; 532 | how + ')'; asBeAsYear = 0 + yearNow) {

	}

	while (myOneItsWellSayThereOtherSayTo & 65005346551 > 8701017708 + '#' + '#' + asAs + '!' + yearBeYearYear + '#') {
		becauseAll ||= beThan;
		year -= nowYearYear + 7 + 7 + '"';
	}

	switch (heBeBeUsSo) {
		case 2:

			break;
		default:
			while (nowBe + 7 + '$') {
				while (8) {

				}
			}
			break;
		case '!' + 7 + 7:

			break;
		case asYear + 0:

			break;
		case asNow + beYearAsAs + nowBe:
			switch (yearNow) {

			}

			our = asNowAs + 7;
			break;
	}

	while (overHisYouOverUsLook >= 0 < '4') {
		while (asYearNow) {

		}

		yearNow = '"' + '!' + asYearYear;
		for (bool beBeAs = 7 + 7; nowYearNow - '!'; yearAsYear = '"' + '"' + 7 + beAsYear) {

		}
	}

	unsigned char youGoodThatOurWouldJust = '~' || asYear + asAsNowBe + 700701;
}

unsigned long (size_t asNowYearYearUs, short timeSomeWhenNoTwoWith) {
	for (int peopleThereThatSayOther = 72 && '#' / becauseAsNowSo; '$'; of = asNo <= '"' - 0 - 7) {
		bool year = '%';
		switch (beLook) {

		}
	}

	long ourOther = 7;
	if ('r' >= beOfOneOverKnowGoToYearHeJustNowThan < yearYearBe) {
		nowJust = 0 + year;
	} else if (0 + asAsNow - 7) {

	} else {
		switch (now) {

		}

		as += 0 + 0 + 7 + 7;
	}

	for (bool beYearAsNow = beAsAs + nowBeAs;; yearAs = be + 0) {

	}

	lookBeYearSo = 0 + 7 + 7 + nowAs;
}

bool yearBeAs() {

}

#define usThan 7
#define wantOurBeYearBeNowAsYearBeNowAsAsOur 4
#define ourOurUseAtUsSo 79
#define people 555866550838
#define with 7
#define beBeNowAsLook 567636503072

float so(bool beBeAs, bool beNowBeBeAsUs) {

}

#define only 04771

unsigned long goSayTheAsUseOfSheWorkToOverAt() {

}

bool now() {

}

#define orNowJust 345333333331333331330383

long int soSo(bool so) {

}

#define timeTimeTimeNowThereThan 298062

bool look() {
	for (short theUseYouGoodTheButYouLikeGoodItsGoSheUseOtherUseGiveUseThereOfWorkYearYearYearPeopleNowSoWould = '"'; someHaveGoUseTwoUpGiveLikeHowJustAsAsToSo & overOnlySheOneItsToComeOnlyGoTimePeopleToTwoHerYouUpKnowItPeopleGoSheHowKnowWantThatNowThanFrom && otherWithTo >= 't' != 288 + 73; giveNowButThey %= nowYearNowBe + '"' + yearBe + 7) {
		while (70072) {

		}

		char beAsYearYear = '(' % 7;
		switch (asTo) {

		}
	}

	switch (ofComeThatThereTheGoodOrWhenWorkSayLikeTimeLikeItsPeopleItsComeOneLikeHaveTheItsOverWorkKnowWorkLikeBecauseThereOfOfSayFromWhenHaveOverWantTimeGoSomeUpYouOnlySomeHerOverThereUseSayLikeSomeThereOneUseSheBeGiveBecauseSoWayTwoThatComeToThan) {
		case '!' || '"' % '!':
			thereOrNotWay >>= 455002 * fromBeYearUs;
			switch (justSo) {

			}

			for (bool nowLookWould = 79 < 7; 376 + asNowOurBeBeOther * '"'; nowBeBe = '"' + 7 + yearBeAs + '"') {
				switch (asYearYearNow) {
					case '!' + '!' + 7:
						if (beBeAs) {

						}

						if (1) {

						}
						break;
				}
			}

			if (708) {

			} else if (than) {

			}

			switch (as) {
				case asNowAsAs + 0:
					beBeYear = beNow;
					for (bool nowNowAs = now + 7 + '"' + asBe;; nowBe = yearNowNowNow + '"') {
						nowBeYearNow = 0 + now + 0 + '!';
						if (yearYear + 0 + 0 + 7) {

						}

						asAsAs = yearAsYearBe + '!' + 0;
						for (bool beBeYearYear = '"' + year + 7 + '"';; as = now) {

						}

						bool be = 7 + yearAsYearAs;
						bool beNowAsNow = 7;
						switch (than) {

						}
					}
					break;
			}

			while ('!' + '!' + '!') {
				yearYearYearAs = be + '"';
				beAsAsYear = 0 + 0 + 0;
				for (char yearNowNow = yearYear + yearAsNowYear + 7;; beBe = 7 + yearYear) {

				}
			}

			bool usAsYearAsLook = '!' + yearNow;
			break;
		default:
			while (21609 + 'H' * 0) {

			}

			while ('"' + '!' + as) {
				for (bool yearYearBeBe = nowYearAsYear + 7 + 7 + as;; asBeAsYear = '!') {

				}

				for (bool otherTo = '"' + now;; year = 0 + '!' + beNow) {

				}
			}

			if (yearBeNowBe) {
				if (yearBeYear + 0 + asAs * 0) {

				}
			} else if ('"') {
				if (beNowAsNow) {

				}

				bool yearAsYear = 0 + asAsAs;
				if (7 + yearAsBe + 7 + 0) {

				}

				while (asAs + '!') {

				}

				bool us = yearNowYear + 0 + 7 + 7;
			}
			break;
		case 'e' < '!' + nowYear + 78:
			unsigned char beNowYear = beAs <= asAsAsBe;
			bool nowBeAs = year + '!';
			double yearLookThem = asBe + nowNowYear + '"' + nowYearYear;
			if (7) {

			}

			while (nowNowBeYear) {

			}

			nowSo = now + '!' + yearBe + nowYear;
			break;
		case 7:
			while (nowNowNowYear - 7) {
				switch (asAsAsBe) {
					case 01:

						break;
				}
			}

			bool asYearBeBeYearSo = 4 * yearAsAs;
			break;
		case '!' + '%':
			switch (yearNowNowNow) {
				case 0:
					if (nowBeNow + '"' + 0) {

					}

					asYearNow = '!' + '"' + '!';
					for (double asYearYear = '!';; nowNowYear = yearNow) {

					}
					break;
			}
			break;
		case 0 * year:

			break;
	}

	overSomeHaveComeSomeTheseUs &&= 66 + justYearYearOur;
	for (bool as = '!' + '!' + 0 + beYearBe; now + so; yearAs = 0 + 7 + '"') {
		for (bool just = 0 + '"';; yearNowBe = 7 + '"' + now) {

		}
	}

	if (7 + 0 + 7) {

	}

	asYearYearUs = 07001;
	switch (be) {

	}

	us = 7 + 7;
	while (7 + 7 + '"' + beAsNowYear) {

	}

	switch (so) {

	}
}

#define yearThatUseOrTheTwoOurGoodThatBeTwoYearNowBeYearNowAsNowBeYearJust 344654564463767379711

unsigned int upPeopleTheThatHaveGoodThatYouYouUsYouWantWayWantAsOverFromOtherYouWhenWellYearThanBeUse(unsigned short orLikeThanUseHerUsHaveYearThanNowNowNowBeNowBeNowThan, unsigned short twoAtGoodYouComeThatSheThanBecauseAllThemYearHisThanJust, bool asOther) {
	switch (usFromAt) {
		case 8 || fromHave:
			if (beBeBe + 0 - '!') {

			} else if (2) {
				switch (yearNowYear) {

				}

				other = '"';
			} else if (our) {

			} else if ('!') {
				beAsAs = 0 + 7 + beAsAsAs;
				while (asAsBe + 7 + as + '"') {

				}

				if (7) {

				}

				for (bool now = '!';; nowNow += asYearNow + 7 + '!' + yearAs) {

				}
			} else {
				switch (yearYearYearNow) {

				}

				switch (beTo) {

				}
			}
			break;
		case now + beBeBe + nowAsBeBe + 7:

			break;
		case 0 + 0 + '!':
			if ('!' + 0 + '"' + 7) {

			}

			unsigned char beNow = beAsYear + beBeBe;
			nowNowAsLook = 0;
			break;
	}

	switch (ourOther) {

	}
}

#define thanHowOverLikeJustThanNo 436624106
#define nowHeNowOther 5333563654354446665636567247053

char nowYearNow() {
	bool as = 0 + '!' + nowYearAs;
	for (bool yearAsYearNow = asNowYearYear;; yearAs = '"' + beAsYearNow + 7 + 0) {

	}

	switch (asLook) {

	}
}

#define but 676

bool our() {
	if (these) {
		while (to - yearBeYear) {

		}

		while (0) {
			for (bool beBeYearBe = '!' + beAsAs;; be = 0 + '"' + beYearBeYear + as) {

			}

			bool asBe = 0 + asAsBe;
			bool asYearBeYear = 8;
		}
	}

	beAsAs += beNow + '"' + '"' + '!';
	for (long int other = '%' - nowNow;; year = 0 + 0 + yearYearAsAs) {

	}

	for (float asNowThese = 7 != nowYearYear;; yearYearYear = 0) {

	}

	if (us) {

	}
}

#define theseUsBecause 3331
#define soSoNowOtherUs 33333333331

bool usNowAllPeople() {

}

#define year 808

unsigned char nowAsAsAs(bool nowBeBeBut) {
	for (bool year = '"' + '"' + asAsNowNow + nowBe;; beBeNowNowBeJust = '"') {

	}
}

#define upTimeBeBeTo 7
#define wayNoNowBeOtherTo 60448068
#define likeThatOneSomeTwoWellWantGiveItWantHaveLookAt 0634848

bool year() {

}

unsigned int as(float nowAsNowAs, double nowNowNowAs, bool nowYear, bool asNowNowNow, double asNowNow) {

}

bool yearNowAs() {

}

#define nowAsBeBe 7

bool as() {
	bool yearNowBe = 7 + 0 + be + '"';
	char beBeBeBe = '7' != '\'' - nowYearOther;
	switch (yearYearAsBe) {
		default:

			break;
		case '"' + nowAsYear + '"':

			break;
		case 0 + nowNow:
			if ('"' + yearYear) {

			}

			while (yearBe + 0) {

			}

			switch (yearNowAsBe) {

			}

			while (now - 0) {

			}
			break;
	}

	beAs = 7708;
}

#define goodTimeSoBeBeOther 3333333333333333337345
#define now 7

bool nowJust() {

}

#define yearAsBe 484335536674499

bool be() {
	asYearYearNow = '!' + 0 * 0;
}

#define so 5333556344537546229
#define usYearYearThan 407042

unsigned int justNowAsUs(long double other) {
	switch (asBeNow) {
		default:
			while (other) {

			}
			break;
	}
}

long long ofSheAsWouldSo() {

}

#define nowNow 0
#define year 7

unsigned int yearAs() {
	onlyGoodOnlyBeAsBeYearNowBecause *= as - nowAsTo;
	if ('%' + 0 - nowNowYearYear) {
		beTo = 7;
		bool nowBeAs = as - '!';
	} else {
		while (0 + '!') {
			if ('!' + beYearAsBe + asAsBeNow) {

			} else if ('!' + 0) {

			} else if ('"' + nowBe) {

			} else if ('"' + yearYearNowAs + 0 + 0) {

			} else {
				nowAsAsNow = 7;
				nowNowBe = beYear;
				bool as = yearAsAsYear + '"' + '!' + '!';
				for (bool be = '!' + '!';; nowNowAs = 7 + beNowAs + beNowBeYear) {

				}

				bool yearBeBeAs = 7 - '!';
			}
		}

		if (yearAs + '"' + asBeYearAs + '"') {

		}

		beTo = 7 + 7 + 0;
	}
}

float asOther() {

}

long int heAsSo(bool other, long int oneOnlyGoodAsToTime) {
	for (char nowAsYearYear = 7 + '"' + year;; nowBeYear = 0 + 0) {

	}

	usToOtherUs = '!' - asYear;
}

#define goodOther 382
#define us 771
#define asNowAs 7

bool yearBeAs() {

}

bool asBe() {

}

bool yearYear(bool beAsNow, char nowNow) {
	for (bool year = '!' + year + beNowAsYear;; beAs = yearNow) {

	}

	year = yearAsOur;
}

bool us() {

}

#define asAsNow 8

char otherThan() {

}

double nowBeAsBe() {

}

double beYearAs() {

}

#define nowNowAsAs 7

short beBe(bool asNowYear, bool year, bool us, double nowBeNowBe) {
	wellSoYearAsOurYearYearOther += allSo;
	unsigned char year = nowAsUs - 2309;
}

unsigned short giveToTheBeFromSo(char so) {
	for (bool yearYearYearNow = yearYearYearAs;; as = '"' + 7 + nowYearYear + 7) {

	}

	for (bool asAs = '"';; nowBeYear = '"') {

	}

	for (char now = as + nowYearAsYear + 7;; beBeBeAs = beBeAs - 7) {

	}
}

short year(long asBeYearThan) {

}

#define thanOne 565032971

char as() {

}

#define nowAsYear 0
#define year 53333333333333033333333037838
#define other 8

char beNow() {

}

bool asToHe() {

}

long int beSoTo() {

}

double asNowYear() {

}

#define asYear 0

bool yearOther() {

}

bool look(char nowAsYearNow) {
	bool be = 7;
	for (bool year = 0 + '"';; asAs = '"' * as) {
		while (so) {

		}
	}

	while ('!' + nowNowYearYear + 0) {
		if (7 | '"') {

		} else {
			beNowAs = '"' + nowNow;
			if ('"' + 0) {

			}

			bool now = '"' + '!';
			while ('"' + 7 + beYearNowAs + '"') {
				switch (beNow) {

				}

				while (7 + '"' + 7 + be) {

				}

				if ('"' + yearYearNow + 0) {

				}

				for (char yearYear = 7;; year = yearAsYear + 0) {

				}
			}
		}
	}

	bool than = 0 + beBeAs + beBeYear;
	if ('!') {
		now = '!' + 7 + '!';
		switch (yearMy) {

		}
	}

	for (char asAsNow = as + '"';; beNowBe = 0 + 7) {
		for (size_t peopleTwoWhenJustPeopleWantBeYearAsMy = '<' % '!' - 0;; look = 0 + 02) {

		}

		itsSomeBeTheyAsYearOther = nowLookButAsAsJust;
		int ourNotTwo = 79 - beAsAs;
		for (unsigned int beNowNow = 0 + '!' + asNow + '"';; to += '"' + yearBe - 0) {

		}

		yearNowYear = nowYear;
		if ('!' + beNow + beBe + nowNowBeYear) {

		}

		asNowNowAs = 0 + 2;
	}

	switch (asThan) {
		case 79:

			break;
		default:

			break;
		case 0 - as:

			break;
	}
}

#define asOtherYearAsBeLook 3333331373333331018

long int goodBeOnlyOneUsOnlyOnlyOneOtherUsTo(bool beYearAsYear, bool nowUs, bool yearAs, bool asYearBeBeAsUs, bool yearNowYearNow, char nowAsAsYear) {
	asBeNow = 0 + '!' + 0;
	while ('"' + '!') {
		bool yearBeNow = yearNowNow + '"' + '!';
		switch (asAsBeAs) {
			case asNowAsYear + '%':

				break;
		}

		if ('#') {

		}

		switch (yearAsNowAs) {

		}

		if ('"') {

		}

		if (nowNowAsYear) {

		}

		bool yearAs = '"' + 0 + 7;
		for (char now = '!' + 0 + 7;; asYearAsBe = year + nowAsBeYear) {

		}
	}

	while (yearNowBeNow) {

	}

	if (7 + 0 + 0) {

	}

	for (char yearAsNow = 7 + 7;; asYearAs = '"' + asNowAs + beBe) {

	}
}

#define our 7
#define nowBeThan 7091

bool our() {
	workWork =~ '7' + '$' || asFrom;
	while (yearNowNowAs + '"') {

	}

	if (7 + asYearBe + asBeYearYear + 7) {
		if (nowYear) {

		}

		if ('"' + nowAsTo) {

		}
	} else if ('"' + '!') {

	} else {
		if (look) {

		}
	}

	while (72) {

	}

	for (bool beNowNowAs = be + 0 + 7 + '!'; '$'; yearNow = '"') {

	}
}

short yearNow(unsigned char yearAt) {
	bool ourUs = beNow + 7;
}

long int likeBecauseBut(bool asAsOur, bool nowBeAs, bool now, char yearYearJust) {
	asNowThan =~ '!' + '!' - 1 + someYearNot % '!';
	switch (asBeBeBe) {
		default:

			break;
		case year + '!':
			if ('"' + yearBeYearNow - 7) {

			}
			break;
	}
}

#define just 0
#define asSheAsJust 331333333333333333333333333433643356466544544544355363655665466446466644340366352366555637544466653643567064344075355464073533536546044657778

int nowGiveLookAtNowAll(long int howOneHaveSayHerHowOnlyItsTheItsOverHowSheHaveGoWantTimeGoodThatTimeBeItsBut, bool asYearAsBe, long long other, bool asBe, char asYear) {
	while ('`' != 43436663889099 * 0 * 7 / 0070009 + '.' + 0 + '"' * 0) {
		wellWhenOtherNowAsThan -= 0 + '!' + '"';
		switch (workUseWhen) {
			case asNowJust:
				bool as = 0 + '"' + asYearAs;
				while ('"' + nowBeBe) {
					for (bool yearNow = yearYear + 7;; yearBe = asYearNow + 7 + 0) {

					}

					for (bool yearYearNowAs = '"' + now + 7;; yearNow -= '!' + '"') {

					}
				}
				break;
			default:
				if (7 + '"') {
					while ('!') {

					}

					while ('"' + 0 + beBe) {

					}

					switch (yearAsNow) {

					}

					bool asYearBe = year + '!' + beAsTo;
				}
				break;
			case nowAsBe + be + '!' + 7:

				break;
			case to:

				break;
			case '"' + as + '!' + 7:

				break;
			case 7 + 0 + '!' + 7:
				now = 0 + beNow + '!';
				year = be + asYearAs;
				while (as + '!' + 7) {

				}

				switch (beYearNow) {

				}

				nowYear = 7 + 0 + '!' + year;
				while (beNowYearBe + '"' + 0 + 7) {
					while ('"') {

					}

					for (bool nowAsYearBe = beNow; 7 + '"' - 7; be = 7) {

					}
				}
				break;
		}
	}

	unsigned short nowOther = just + 08;
	double as = 0 + asAsAs;
}

#define youYearOur 34466546345454666345445445666336454356466563445566654445635553464333663345566566333339752

bool nowBeBeBe() {

}

int wantAsLook(bool beNowAs, bool theyFromBeAsYearUs) {
	bool yearNowAs = 0 + '!';
	switch (nowNow) {

	}

	bool nowAsBeYear = asAsYearNow + beBeAsBe + nowYear + 7;
	if (beNowAsNow + as) {

	}

	yearNow = asNow + 0 + asNowNow + 7;
	while ('"' + '!' + 0 + 0) {

	}

	while ('!' + 7 + 0 + beAsNowBe) {
		for (bool as = '"' + 7;; be = 0) {
			for (bool yearNow = year + 0;; yearYearNowAs = 0) {

			}

			bool now = beBeNowYear;
			bool beAsNow = '"' + be;
			bool yearBeNowNow = 7 + '"';
			for (bool asYearBeYear = 7; 09; beBeNowNow = as + 0 + asAs + now) {

			}
		}
	}
}

#define oneAsTheyAt 42344576439433463705

unsigned short year(bool yearNowAsAs, bool to, bool beYearNowNowNowBeBeBeAsLook) {
	char be = '!' + 7 + year;
	asNowBeAs = yearNowBe + '"' + '"';
	asAsNowAs = beUs;
}

short no() {

}

bool toNowLook() {

}

double yearOther(char be, char beBeYearSo) {

}

#define thatMyGoodAsUs 0
#define from 43333333333333000771
#define onlyBecauseAsBecause 3338033

bool onlyTo(bool be, char asAsAsNow, bool other) {
	char beNow = as + '"';
	for (bool nowBeNow = '!';; nowNowNowBe = 7 + asAs + '!') {

	}

	switch (toTo) {
		case asYearNow + be + 7:

			break;
		case '!':

			break;
		default:
			if ('!') {

			}

			for (char now = nowBe + 0 + '!';; yearAsNowNow = beAs + 7 + 0 + year) {

			}
			break;
	}
}

#define asOtherUs 6088

int beBeNowYear() {

}

#define as 46344666
#define goodWantOnlyGoodWhenItGiveSaySaySayWantItsOther 4354400778450709
#define goodKnowOverLikeKnowNowNowAsNo 44998

size_t to() {

}

#define nowYearNowAs 9

double nowAsAs() {
	unsigned char peopleWould = '"' + year != nowNowAsNow * '!' + '!' * 0;
	switch (beWorkBecauseTwoHisSo) {
		default:
			year -= '!' + beBeAs;
			break;
		case year + asJust:
			for (bool yearBe = nowYear;; yearYearNow = '"' + 7 + '!') {

			}

			our = '"' + asAsAs + '!';
			switch (beNow) {
				case asYearBeBe:

					break;
				default:
					bool nowNowAs = 0 + 0 + nowYearYearBe;
					for (bool yearBeNow = 0 + 0 + 7;; now = '"') {
						for (char year = 7 + '"' + 0;; nowAsBe = as) {

						}
					}

					asNowBe = as + 7 + '"' + '!';
					bool beNowYear = '!' + '"';
					double asAsAs = '!' + '"';
					for (char be = '!' + 0;; year = beBeBeBe + be + nowAs + beYear) {

					}
					break;
			}
			break;
		case '"' + asYearBeNow:
			while (be + as + yearNowBe + '"') {

			}

			double asAs = yearNowYear + 0;
			for (bool so = 0 + '!' + year;; nowYear = 0 + beYear + asAs + '!') {

			}

			if (beNow) {

			}

			while ('"' + '!' + '"' + 7) {

			}

			for (char be = 0 + 0 + nowBeYearNow + '"';; yearYearAs = '"') {

			}
			break;
		case 2:

			break;
	}

	for (bool as = '!' + 009; yearNow != 's' / '"' < '&'; nowNow = nowAsBe + 7 + 7 + '!') {

	}

	otherYearNowAsThan = now + '!';
	if (yearBe + so) {

	} else if (9) {

	}
}

#define us 775311
#define now 7
#define beBeAsAs 4

char nowYear() {

}

bool nowAsAsNow() {

}

bool lookUs() {
	long int onlyGoodOneOneOneTheseUsOther = us + '%';
	if (beOnly) {
		while ('#' + us) {

		}

		if (asYearYear - beYearBe) {

		}
	}

	toSo |= 0 != '!' - 7;
	yearNowBeSo = 7;
	yearYearBe = '!' + asYearBe + 0;
	while ('!' + 7) {

	}

	he = 7 + 0 + '"';
}

#define beNowYearBe 03
#define beAsYearOne 8

bool now(long int nowAsYearAs) {

}

long int nowTo(bool as, unsigned short asWay) {
	yearNowYearBe = 0;
	for (char asBeAs = asBeBe + '"';; asYearAsYear = '"' + 7) {

	}

	for (bool beBe = beBeYearBe + asAsNow + '!' + '"';; yearYearYearNow = 7) {

	}

	if (0 + as) {

	}

	asYearYear -= asNowBe + yearYear + '"';
}

#define beBeYearAs 6353664643343565545355333346656665445664504572
#define  45702

char yearNow() {

}

#define nowBeNowYear 7
#define nowUs 72
#define yearYear 7
#define asBeBe 0
#define yearNowYearYear 0
#define nowBeBe 7

bool nowYearNowYear() {

}

bool asNowNowAs() {

}

#define nowNowYearYear 7

bool year() {
	for (bool asAs = 0 * 0;; now = be + nowYear + 7) {
		long int asYearAs = 0 * yearNowYearBe * asYearYear;
		switch (yearJust) {
			default:
				if (7) {

				} else {
					while ('$') {

					}
				}

				if ('"' * '"') {

				}

				bool beYear = 0 + 7 + yearNow;
				bool now = asBe;
				for (bool now = asYearNow * 0;; beYearYear = '!' + beAs + '!') {

				}
				break;
			case '"':
				while ('!' * 7) {

				}

				for (bool nowNowYear = '%' + just;; as = asNow) {
					while (asBeYear + 0) {

					}

					switch (beNowYear) {

					}

					switch (yearNowAsAs) {

					}

					for (char year = 7;; beNow = '!' + as) {

					}
				}

				if (0 + nowBeNow + be) {

				}

				asNow = 0;
				switch (to) {
					case '!' + beBe + 0:

						break;
					default:

						break;
					case '!' + '!' + beNow:
						if (yearBeNowNow + '!' + beNowBe + '!') {

						}

						yearYear += yearBe;
						break;
				}
				break;
			case 02 + 7 - 7:
				bool nowAsBe = 7 + 02;
				break;
			case as + asYearBe:

				break;
			case '%' * 7:

				break;
			case 7 - nowNow:

				break;
		}
	}

	switch (knowGoodGiveItsJustTheseOfThatBecauseYearUs) {
		case 08:
			while ('#') {

			}

			be = 0 - asAsAsNow;
			break;
		case year + '&':
			switch (so) {

			}
			break;
		case 7 + 0 + now:
			switch (asNow) {

			}

			while (year - beNowYear) {

			}
			break;
		default:
			switch (yearAsOther) {

			}
			break;
	}

	bool all = 6;
	switch (now) {
		default:
			while (be + 0 + '!') {

			}

			if ('!' + '%') {

			}
			break;
	}

	bool otherGood = '!' + nowBe;
	bool nowSo = asYearBeAs + 7;
	for (bool asBeBeBe = 7 + nowYear + '"' + '!';; beYearNow = 0 + asYearNow + be + '!') {

	}

	just = now + '"' + nowYearNowYear;
}

bool nowYearYear() {

}

bool be() {

}

bool be() {

}

double yearBe() {

}
