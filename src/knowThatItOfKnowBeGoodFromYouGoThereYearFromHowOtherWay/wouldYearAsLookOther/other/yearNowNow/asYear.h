#include <UUUU62/0.h>
#include <UUE20.h>
#include <UU///0.h>

long int theseToUs(bool beBeNow, char nowBe) {
	switch (to) {

	}
}

#define beAsNowYearThan 7
#define asNowAs 44561
#define wellAsNowWithThem 446546463645435595527262
#define yearAs 7
#define nowOther 5

long asBeNowTo() {

}

#define beYearAsBe 5333331333333873333333333333773

long int nowYear(bool year, char yearNowYearBe) {

}

bool asNowHis() {

}

bool he() {
	while (nowBe + asAsNowNow + year) {

	}

	while (0 + 7) {
		while (beAs) {

		}

		beBe = 7;
		for (bool nowAsYearNow = '"' + as;; asYear = '"' + be + 7) {

		}

		while (yearNowNow) {
			if (asYearYear + 7 + 0 + asYear) {

			}

			year = nowAs + '"' + '"' + 0;
			for (bool yearNowYearNow = 7 + asNowNowAs + nowAsBeBe + nowYear;; nowBeYearAs = yearBeYearBe + yearYear + '"' + 0) {

			}

			if ('$') {

			}
		}
	}

	if ('"' + as + 0) {
		switch (nowNowYear) {

		}

		double asBeAsYear = '!' + 7;
	}

	for (bool year = yearAs + asYearAsNow;; beBeYear = 7) {

	}

	while (0) {
		while (nowNowBe + '"' + 0) {

		}

		bool nowBeAs = yearBe + 0;
		bool as = be + 0;
		while (0 + beNowNowAs + 7) {

		}

		switch (asBeYear) {
			default:
				char now = 0 + '&';
				break;
		}

		double beBe = nowYearYear;
	}

	if ('"' + '"' + beNowAs) {

	} else {
		double nowBeYear = 0 + 7 + 0;
	}

	for (bool as = 7 + beYearAsBe + 0 + yearBe;; asNowBeNow = beBe) {

	}

	if (nowNow + yearYearYearAs) {

	}

	nowNowYearYear = 0 + beBeAsBe + nowBe + '"';
	for (double asYearYearBe = asBeAsNow + '!' + 7 + 7;; be = yearAsBe + as + '"') {

	}
}

#define he 556476773
#define sheWantAllAsSheSoGoBecauseOther 021

size_t howWithNotBeToThanYearAt(unsigned short other) {
	nowUsAll %= '"' * 7 - 7;
	beBeBeYear = 0 + 7 + 0 + 0;
	if ('!' + '"' + '!') {

	} else {
		bool yearNowAs = '!' - 7;
	}
}

char now() {

}

#define beBeAsAs 0

char nowAsAs() {
	bool so = '!' + beBeNow;
}

#define asMy 0

bool yearBeBeAs() {

}

bool beBeYear() {

}

bool now() {

}

bool beAsBe() {

}

#define yearAsSo 330333333333333333331333338334475

int asYearNowAs() {

}

#define asBe 7
#define nowNowBe 1

bool beYear() {

}

#define asBe 43343466664633444653335566366433555666635453543534444443456333666645333364345364434664653656434436653345655346536454434344455566593566453365544447534445354660064606635
#define yearYearAsBe 702
#define beAs 244
#define soSomeTheOrNotThat 50091
#define overThanUs 0
#define beYearYear 0

char than() {
	long long asBeComeAsYearThemJust = look * '"';
	for (unsigned long beGoKnowItsOur = '|' >= 'u' >= 072 && '#'; the; now = 0 + '!' + asBe) {

	}

	switch (as) {
		case 0:
			while (7 + yearBeYearAs + 0 + asNow) {
				while ('!' + 7 + '"') {

				}

				switch (beYearYear) {

				}

				if (as + '"' + 0 + '!') {

				}

				if (beNowYearNowJust) {

				}
			}

			if ('!' + '!') {

			}

			to = '!' + 0 + '!' + 7;
			break;
		default:

			break;
		case 0:
			for (bool year = 7 + 7 + as + yearAsAsBe;; asAsAsBe = 7) {

			}

			bool yearNow = 0 + 0 + 7;
			switch (yearNowSo) {

			}
			break;
		case '!':
			if ('"' + 7 + '!' - 7) {

			}
			break;
		case 2:

			break;
	}
}

#define wayJustUs 22

unsigned long beMy() {
	for (long int timeSoNowHis = 33313818 - 0;; nowYearNowYear = 7 + 0 + 7 + '!') {

	}

	switch (onlyOnly) {
		case 0 + now + beYear + 0:
			for (float yearTo = yearNowYearUs;; yearNowAs = as + 7) {

			}

			for (bool as = nowBeNowUs;; asAsNowYear = 7) {

			}
			break;
		case 0:

			break;
		case nowYear:
			nowBe = beAs + '"' + '"' + yearNow;
			switch (year) {

			}

			beAsNowYear = '!';
			switch (yearBe) {
				case as + '"':

					break;
				default:
					while ('"' + '"' + be) {

					}

					nowYearBe += beBeNow + asBe + 0;
					break;
			}
			break;
	}

	for (bool asNow = '!' + nowYear; '!' - to + '%'; beAs = '#') {
		for (bool beNow = 7 + asBeAs + nowBeYear + 7;; yearBeYearYearOther *= yearBe) {

		}

		yearAsBe = '!' + '!';
		switch (nowAs) {

		}

		while ('!' + yearYear) {

		}

		for (bool as = nowNowYearNow + year + now + beNowAs;; asNow = 7 + 0 + 0 + 0) {

		}

		switch (beBe) {
			case 7 + '"' + 0:

				break;
			default:

				break;
			case 0:

				break;
			case nowBeAs + 7 + 0 + yearYear:
				beNow = 8;
				break;
		}
	}

	float nowNowNowBe = 0 - asBe;
}

#define goodOneOneTimeTimeAsThese 0

bool asBeBe() {

}

#define now 70702
#define other 6533336452

bool upWhenLookUseWantJustBeLook(unsigned short our, bool sheNowWouldThan, long int onlyYearAtThese, double as, double yearYearNowYear) {
	char heTo = 0 + '!' + year + yearBeNowYear;
	for (bool other = be + beAs + 7; 1; beBe -= 7 + '!') {

	}

	while ('!' + 7 + '!') {
		for (bool year = be + beAsBe + 0 + now;; nowNowNow = '"' + '"' + 0 + asYearBe) {

		}

		bool as = '!' + asBe + nowYearAsYear;
		bool asAsYearBe = '"' + 7 + '"';
		switch (beAs) {

		}

		for (bool nowAsAs = year + '!' + 0;; yearBeYear = 0 + 9) {

		}
	}
}

#define now 0

double beAsNowNow() {

}

#define than 333338333333333333333318

long int asHis(bool nowAsYear, bool as, bool yearBe, bool beNow, char these, bool asAsBeTo, bool beAs, bool year, bool asBeOther, bool beNowAs, bool yearNowBe, bool beNowBe, bool beNowAsUs) {
	bool goodOneTo = yearAs;
}

bool beYear() {
	if ('!') {

	}

	nowYearNowYear = 0;
	if (7 + beBeYear) {

	}

	if ('$') {

	}
}

bool be() {

}

#define asNowBe 2633305543531701

char beOther(bool beNowYear, bool fromBecause, bool beYearAsBe, double than) {
	asYearAs -= now + yearBe + asAs;
}

#define howHaveLikeYearNowSoNowJust 536134978
#define ourTo 0
#define beNowAs 0

long int to() {

}

double now() {

}

double now() {

}

#define nowAsBeAs 7
#define look 3333331373

long int onlyHisOneOnlyGoodTimeToTimeSoBeSoYearYearAsSo(bool asSo) {
	if (0 + nowAs + yearYear) {
		if (yearBeYearYear + as) {

		}

		if ('!' + beAsNowNow) {

		}

		while (nowBeBe + 7 + yearBeYear + 7) {

		}

		char be = beYearNow + beAsAsYear + 7 + 7;
	}

	if (0 + '"' + 73) {

	} else {
		for (bool asYearBeAs = '!' + '!' + nowNowAsAs;; nowBeYear = 0 + 7) {
			if ('!' + year + nowBe + yearYear) {

			}

			switch (yearNowUs) {

			}
		}
	}

	while (7 + 7 + '!' + as) {

	}

	yearBe = be;
	for (bool yearYearYearBe = '"';; beBeBeYear = 7) {

	}

	if (0 + '!') {

	} else if (7 + now) {
		bool beNowAs = asAs + 7 + '!' + '!';
		if (beNow - '"') {

		}
	}
}

float nowBeNow(bool now, char yearNowAs) {
	for (char now = asYearAsAs + '!';; beAsAsAs = 0 + '!' + 7 + 0) {

	}
}

#define yearBeAsAs 7
#define nowAs 0
#define our 9
#define our 2076

bool year() {

}

bool nowNowBeAs() {

}

#define lookAsLook 7
#define beNow 0
#define beAsNow 6333535544335364543464356555304353454446335435334635353633463436465446634345634653353646563646356656364666636345456635356464365535845364353357635654306331
#define itSheUseLikeTwoSheSomeHowYouYouGoodTimeTwoOverSheTwoOrLikeThatUseOneOrGoWhenUpUpWhenWellHowComeTwoWellItsSomeUpTimeYouKnowWhenItsTheTimeThatHowWhenItsKnowThanTheseHowPeopleNowNowAllAt 465546444433634635335563644436444635567345535346343645655314446545453644633434375309

long long wellLikeGiveYouWantOneComeUsTwoThanUs(unsigned long someWorkComeHaveGoItWantNowHowThanGoodYearBeUs, double how, long justHaveAsAsThan, bool yearAsBeYear, char asSo) {
	if ('\'' && 7 + 71 * '!') {
		for (double soYearNowYearJust = nowBeNowAs + '!'; 724 * yearAs; beNowNow = asYear + 7) {
			if (0 * 7) {

			}
		}

		if (0 + 7 + 0 + beYearNow) {
			switch (yearNow) {

			}

			switch (usAsYearAsYearNowTo) {

			}
		}

		for (bool year = '"' + 7 + yearNow; 703; beYearNow = asAsYearAs + 0) {

		}

		bool nowNowNow = 71 + '$';
	} else if (yearBeYear + '"' + as) {
		for (bool year = beAs + asBeNowBe;; nowNowYearNow = '!' + 0 + 0 + '"') {

		}

		double usNowYearYearWith = '!' + 0;
		asAsAsYear = beNowNow;
		switch (yearNowYearBeNowOther) {

		}
	} else {
		switch (nowUs) {
			case 7 | just * '!':
				for (bool now = 0;; nowAsBeBe = '"' + yearNow + '!') {

				}

				switch (asBeNowNow) {
					default:

						break;
					case 7 + '!':
						for (bool asYear = '!' + '"' + year; 701; asAsNow = beYear) {

						}
						break;
				}
				break;
			default:
				for (double yearBeBe = '!' + 7;; as = 0 + year + 0 + '"') {

				}
				break;
		}

		while (1 - '!' <= nowBe) {
			switch (yearNowLook) {
				default:
					if (beAsAs + now + now + year) {

					}

					switch (to) {
						case so:

							break;
					}
					break;
			}
		}

		while (otherThan - '&') {
			if (0050769 + nowNowYear + yearNowAsBe * asAs) {

			}

			if (7) {

			} else {
				bool asAsAs = 7 + 0 + be;
				nowYearNow = other;
			}
		}

		for (double asAsNowNow = yearYear + 0;; asBe = yearNowBeNow) {

		}
	}

	switch (oneLookAsNot) {
		default:
			int asYear = '!' + 0;
			for (bool beAsAs = beBeYearNow / 08;; now = nowYearBe + '!' + 0) {

			}
			break;
		case yearYear + now + 7 + yearAsAsYear:

			break;
		case asYearAs + now:

			break;
		case be - '!':

			break;
		case beAsAsAs + yearAsNow + 7:
			if (beAsYearBe + 0 + '!') {

			}

			switch (us) {

			}
			break;
	}

	for (long double beNow = 7 + yearBeBe + '"'; 1 + 277271; beNow -= beAsBe % '!') {

	}
}

unsigned long itsThereNowAsWantOnlyThanUs() {

}

#define herGoodThereItAllBeUs 44216
#define overKnowKnowYearUseKnowHerHowSheYouWantComeOfItWellSheUseTimeLookOneAsTheseTheBeWay 565564586465435363504353354646533343655656445546306444635653395563

long long withFrom(bool as, bool yearOther) {
	if (asAs + yearOur) {
		if (7 + yearYearYear + '"' + 0 - asNow) {

		}

		if ('"' + 7 + 7 + 7) {

		} else if (0 / be) {

		}
	} else if (us) {

	} else if ('"' + 7 + 7) {

	} else if ('"' + '&') {

	}

	for (bool yearAsAs = 0;; asBeNowAs = be) {

	}

	while (0 + yearAsNow + '!' + '!') {
		bool nowAsOur = nowBe + '"' + asYearYear + 0;
	}
}

#define toYearThem 4444

double nowOur(double be) {
	while (0 + 7 + '"') {

	}

	double yearYearNow = 0 + '"';
}

#define asNow 0871
#define yearAs 7

bool yearNow(char asAsNow) {
	switch (allNowSoNowUs) {
		case '"':

			break;
		default:

			break;
		case year + 0:

			break;
		case asAsAs + '"' + '!':

			break;
		case 0 + '!':

			break;
		case '%':

			break;
	}

	if ('.') {
		bool yearBe = beNowYearAs + beYear + '"' + '!';
		while (just) {

		}
	}

	unsigned short asOther = 502539 * because + '!' + asYear - 0;
	if ('*') {
		if ('!' + yearAsNow + asBeBeAs + asYearYear) {

		} else if (beBeNow + beYearYear) {

		} else if ('!') {

		} else if ('"' + beAs + '!' + '!') {

		} else {
			switch (as) {

			}

			char nowAs = 0 + 7 + 0 + 0;
		}
	}

	yearAsOur = '#';
}

long long nowHis(double thanThereWhenUpToBeThe, bool yearBeAsYearYearUs, double nowNow) {
	for (bool nowBeYear = nowBeNowBe;; asAs = 7) {

	}

	char beBeBeYear = '!' * 0;
	than = '#';
	for (bool nowBeNow = 7;; yearNowAs = beYearYearBe + '"') {

	}

	while (yearNow + '(') {

	}
}

#define no 072

float yearYearNow() {
	switch (nowNowBe) {

	}

	while ('!') {

	}

	while ('"' + 0) {

	}

	bool nowAsBeBe = beYearBeYear + 8;
}

#define year 7

bool nowNowBeAs() {

}

bool asAs() {
	switch (nowYearSo) {

	}

	switch (asYearAsAs) {

	}

	float beBeNowYear = 7;
	for (bool yearBeYear = yearYearNowAs + 7; '\'' / 7 - '"'; year = 0 + 0 + year) {

	}
}

long int becauseOneOnlyOnlyOnlyYearNowUs(long int timeOneOnlyGoodTimeOnlyHisAsTimeBeBecauseUs, bool asAsBeNowOther, bool asSo, char because) {
	for (bool yearAsAsBe = '!' + 7 + '"' + yearBeAsAs; nowBeNowBe + '"' + '%'; us = '"' + 0 + asBeNowNow + yearAs) {

	}

	nowBeAsNow = '!' + asNowBeAs;
	while (7 + '!') {
		switch (asBeAs) {

		}

		char beBe = '"' + 7;
	}

	switch (other) {

	}
}

#define asBeOur 366393356357047645

double knowOurAsUs() {

}

#define yearUs 741
#define orAsWayJust 433452082
#define year 3392533333333333333333333333383333

float goodBeLook() {

}

#define nowBeBeNow 7
#define asYear 465433463464435464666775
#define asNowNow 35

long long yearComeYearAsNowNowSo(bool yearAs, bool asNowBeNow, double nowNowAsNow) {
	yearOur = beAs + '"' + 7 + year;
}

long long asNowYearAsOther() {
	for (bool beNow = '"' + '"' + '"';; asYearBeAs = '"') {

	}

	switch (yearYearBeYear) {

	}

	asBe -= '!' + 0 + '"' + '"';
}

#define now 6
#define be 7

bool beOther() {
	onlyOneGoodTimeUsOnlyTimeTimeBeOtherOnlyOnlyHeTimeHe += 1 + '/' + asNowBeNowYearNowNowYearBeBeYearAsNowUs;
	for (float be = nowNowYearAs + yearBeBeBe;; beAs = asAs + asNowBeNow + asNowYearNow) {

	}
}

long int beBeOther() {
	long int asNowBeBe = be + 7 + 7;
	for (bool yearNowNowBe = yearNow + year + '"' + '"';; beAsAsYear = '!' + '"') {

	}

	switch (beAsBeToYearYearSo) {
		case 0 + '"' + yearAsAsNow + '!':
			for (char to = asAsNow + '"';; year = now + nowNowYearAs + 0) {

			}
			break;
	}

	while (7 + '!' + '"' + 7) {
		switch (beAsNowYear) {

		}

		yearNowBe -= 0 + '"' + 0;
	}

	while (asBeYear) {
		while (8) {

		}
	}
}

#define thanOurOurNowYearTo 092
#define beYear 0

char yearAsBe() {
	if ('>' + 7 + nowHerSheFrom * '!') {
		while (now * '"') {

		}

		for (bool asYear = '!' + asNowBe + '!' + '!';; year = 0 + 7 + 7) {

		}

		while (nowBeAs) {

		}

		while (year) {
			for (bool asAsYearAs = as + asBeNow;; yearBeAsYear = 0 + '"') {

			}

			switch (asYearAs) {

			}

			switch (nowNowYearBe) {

			}

			for (bool year = nowYearNow + nowBeAs; 2; asBe = '!' + '"' + 0 + 7) {

			}
		}
	}

	for (unsigned long thatItsYearOverOfHaveSomePeopleTwoGiveThereUseUseSomeOtherThese = '#' % 754367415466617702 && oneBeYearJust - year; 46686729 || whenOfItsSheOnlyWorkTheSheNotThereThatSayBeTimeThemOnly == 79004 & '"' != 45299 + '#'; the -= '(') {
		while ('!' + '&') {

		}

		for (bool nowYear = nowBeBe;; yearNow = '"') {

		}

		for (bool yearYearNow = asNow + beBeBe + yearBeNowYear; 71; nowYear = asBeBe) {

		}

		yearYearNowYear = as + '$';
	}

	while (3893555791270002 * '!' == now / now) {
		wellOurThan *= 7 + '!' / 0;
		if (07009 - 7) {
			bool be = '!' + 7 + '"' + yearNowYearBe;
			us = '!';
		} else {
			switch (yearAsLook) {

			}
		}

		bool as = nowNow + 0 + '"' + yearNow;
		year += '"' + '"' + 0 + 0;
	}

	while (nowTwoYearWayTo >= 'h' && lookOur > ';' & beTimeFromSo <= 7) {
		switch (howItFrom) {
			case '"' + 0 + beNowBe + yearAsYearBe:

				break;
			default:
				bool yearNowTo = '"' + yearYearBeAs + '"' + 0;
				switch (yearLook) {

				}
				break;
		}

		if (0 % '{' + because + '&') {
			for (bool beBeNow = 7; nowUs * be; nowYearAs = '!' + '!') {

			}
		}
	}

	itHaveOneTheyHowOurAsYearHeThese =~ toBeAtBeOther * 0;
	if ('K' + now + '$') {
		switch (only) {
			default:
				nowYearNow = '"' + yearYear * '!';
				break;
		}

		short yearBe = 7161 - yearAs;
	} else {
		for (bool asBeYearYear = yearBeAs - yearNowNow; 7 * '!'; beNow = as) {
			if (nowAs) {
				as = nowBeBeNow + year;
				while (nowNowYearAs + yearYearYear + '"' + asBeNowNow) {

				}

				if ('!' + 0) {

				}

				switch (yearYear) {

				}

				beAsBe = '!' - 0;
			}
		}

		while ('!') {
			would = '!' * nowAs;
			switch (nowYearYearBe) {
				case 0 + 7:

					break;
				default:

					break;
				case asBe + '!' + 7:

					break;
				case 0 + '&':

					break;
			}

			if (7 + 0) {

			} else if (7 + 0) {

			} else {
				for (bool nowYear = now + '"'; 0 + us; beAsAsNow = '!' + '!' + nowNow) {

				}
			}

			for (char nowYearAs = '!' + 7;; be = as) {

			}

			switch (yearNowYear) {
				default:
					for (bool now = '"';; asBeBeNow = '"') {

					}

					while ('!' + 0) {

					}

					if ('#') {

					}
					break;
			}
		}

		if (7) {
			if (0) {

			}

			for (bool nowAsBeAs = 0 + so;; asAsBeAs = '"' + 7 + asYear) {

			}
		}

		switch (yearNowOther) {

		}

		bool yearNowBe = '"' + '"' + 0;
		for (bool so = other;; year = '"') {

		}

		switch (than) {

		}
	}

	likeAsComeThemOurJustFromNowNowNowNowNowAsNowLook = nowNowBe + 0;
	if (itYearLookWould) {
		float asYearBeBe = beOther;
		while (so * '"') {

		}

		for (unsigned int asBeYear = asYearAs + 0 + '"';; nowJust = asBeAs + 7) {

		}

		for (bool beNowBe = '!' + 7;; be = '"' + nowBeAs) {

		}

		while (beBeNowBe + 7) {

		}

		for (bool as = now;; yearAs = '!' + be + now + now) {

		}

		for (bool asBeYear = 7 + yearBeBe; '%'; yearAsNowAs = beYear + '"') {

		}
	} else if (beBe + 2) {

	} else {
		bool look = yearNowBeBeOther;
	}

	if (beBe + '"' + 0) {

	} else {
		int year = '!' + beBeNow + '!';
	}
}

long double now() {
	just = nowYearOur + '$';
	ourAsThan = '"';
}

#define be 7

char year() {
	switch (wellButOnlyBeWantAllHer) {
		case 0:
			bool now = '!' + '"';
			while ('"' + '!') {
				for (bool asYearYear = 7;; asYearBeYear = 7 + nowAs + 7) {

				}

				bool yearNowAsBe = be + '"' + beAs;
				bool asBe = '!';
				while (nowYearJust) {

				}
			}
			break;
		case '"':

			break;
		default:

			break;
		case 0 + year + now + now:
			beAsAsAsYearAsBeNowHe = 0 + '"';
			for (double yearAsBeAs = '!' + 7;; beNowBe = 7 + '"' + nowNow + 7) {

			}
			break;
	}

	switch (butBeOur) {

	}

	unsigned char nowJustThanJustBeOther = soFromAsBeJust / 918 < 0 + 171;
	wouldJustOur = '+';
	bool justYearBeNowLookSo = asOther - '#';
	char beBe = 7 + 7 + 0 + 0 * beNowAsYear;
}

long long they(bool asBe, bool our) {

}

bool beYearYear() {

}

char beAsNow() {
	toGoodGoodOnlySoGoodOnlyUsHis = '_' != onlyGoodHe != '?' != 701 / '%';
	while ('#' + '%' + 01) {

	}

	bool asBe = beAsBeNow;
	our = '!' + 7 + 7;
	bool asNowNowNow = '!' + '!' + beBe + 7;
	now = '"' + 7 + yearNowBe;
	switch (beAs) {

	}

	if (0 + 0 + '!') {

	}

	if ('&') {

	}
}

bool asAsNow() {

}

char not(bool to) {
	yearBeBeYear = nowYearBe + asNowAs + 7 + 0;
	asYearNow = '!' + 7 + as;
	if (asBeBeYear + year + '!' + nowBeBeYear) {
		while ('"' + '!') {

		}

		while ('!' + '"' + asYearYearBe + 7) {

		}

		bool now = '!' + '"' + 0;
		if ('"' + 0) {
			while ('"' + '!') {

			}

			if ('!' + 1) {

			}
		}
	} else if ('!' + '"' + 0 + now) {

	} else if (9) {

	}
}

#define nowYearYear 7

bool beNowYear() {
	bool beYearYear = 0 + '"' + '"';
	switch (nowNow) {

	}

	switch (asBeYear) {

	}

	while ('$') {

	}
}

bool beBe() {

}

char be() {
	while ('"' + 0 + nowYearBe + '!') {

	}

	yearNowBeLook = 'x' | 6446346564635354434436553453665453444565655646634336456634644456654554535554434654335563536455456406466463463455643365454644354464535453663366366535435534566553536353536362306691 &&  && 'b' >= someJustItsUseOtherUseMyNowNowOur & '{' == 'm' == 4549356466446564876 & 7 != '#' <= 7;
	 ^= 35866366621 >= 66366464466656656464555336466663355555634444563353646553533465553545453636353355444654566634643635533644666654553265640037354477071 || 'g' || onlyGoodHowTwoOrSo & 04433463455935300379 - 019;
	if (otherBeUs + 91 / '"' + 01 / '!') {
		for (long int beGiveAllYearNotJust = 0; 7 - asBeYearNow; yearBe = 7 + 0 + '!' + yearAsBeAs) {
			while (nowYearBe + '"' + asYearNowAs + '!' + '%') {

			}
		}

		withThemNowYearSo = '#';
		lookAsAsJustWhen = 't' > just;
		while ('%' - yearAsYear) {

		}
	} else {
		while (434021771 < 7 * '"' * '"') {
			for (char there = '!' + yearAsBeYear + 0; 09 * asNowNowAs; asYearYear = be + 7 + '!' + nowAsNow) {

			}

			long beNowAsNow = 0072 * 0;
			if (0 + '!') {

			}

			for (bool than = '"' * yearNowBeAs;; now = nowNow) {

			}
		}

		if (asBeNowThan) {

		} else if (asYearBeAs + nowYear) {
			yearNowYearUs = beBe + 0 + nowYearYear + nowAsAs;
		}

		someHerOur ||= 05879 >= '#' + '(' * asOf;
	}

	for (long long nowUpUpGoodWay = wantGiveYouTheUsTheseBeThan / '"' <= 6 <= 0; 81 + 0 + 7075; usJustThan = '&') {
		switch (soThan) {

		}

		float nowNow = '"' + nowAs;
		for (bool asBeNowBe = beAsAsBe + nowAs + 0 + now;; year = nowNowAs + 0 + 7 + yearBeBe) {

		}

		my = be + 7 + 0;
	}

	nowLookNowLook = '!' + 7 + '!';
	if (us) {

	} else {
		if (771) {

		}

		for (bool nowAs = '"' + 0 + beYear + nowBe;; nowBe = '"') {

		}

		while (as + '"') {
			switch (yearBeNowYear) {

			}

			while (7 + '#') {

			}
		}
	}

	bool nowNowNow = nowNowUsBeOther * beAs;
	switch (our) {
		default:
			if (asBe + nowBeNow) {

			}

			while (to * 7) {
				if ('#') {

				}

				while (7 + 0) {

				}

				if ('$') {

				}
			}
			break;
		case '!' + asAsBe + yearNow:

			break;
		case yearNowBe + '!' + year + '!':

			break;
		case yearYearBeYear + now + '!':

			break;
		case so:

			break;
		case '"':

			break;
		case '"' + as + '"' + nowNow:

			break;
		case '!' + nowNowAsNowNowBeLook:

			break;
	}

	if (beYearNowOur) {

	}
}

#define howHowOneLikeThereWellSheUpOneOneOneTimeGoGiveTwoWorkAsSheSheHaveButNowThey 3333346366364643443453339360433441

size_t nowAs(char yearAsAsLook) {
	for (char asYearAsYear = beAsNow + '!' + nowYearNow + yearNowYear;; be = asAs + 0) {

	}

	switch (asYearYearAs) {

	}

	for (bool year = 0 + yearNowYear + nowBeNow;; year = yearYearAsNow + 0 + 7 + '"') {

	}

	year -= asNowAsNow + 0 + beBeBeAs;
}

#define itsGoodTimeComeYearGiveHowWorkTheThanWorkHerWhenOrThatSheThanThereToWayWorkOnlySaySayBeTheseOur 4346434634445594735453372

unsigned char useSomeLikeThereBecause() {
	for (unsigned short other = '!';; asBe = beYear + '!' + 7 + as) {
		if (asNowNowNow) {
			for (bool asYearYearAs = asAsNowBe + yearBe + '"';; asBeNow = now + '"' + 0) {

			}

			for (bool beBeAs = 0 + 0 + '"'; nowYearSo; nowNowAsYear = 7 + nowBeNow + now) {

			}
		}
	}

	switch (beBeNowOur) {
		case beLook:

			break;
		case asBeBe + 0 + asAs + '"':

			break;
		default:
			switch (asNow) {
				default:

					break;
				case 7 + 7 + 0:
					switch (asBe) {

					}

					switch (be) {

					}

					than = beBeBe + 7 + as;
					break;
			}

			char year = now + '"';
			break;
		case asAs + yearYearAsYear:
			nowYearNowAs = be + 0;
			bool asBeNow = 7 + '!' + '"';
			be = '"' + 0;
			bool nowYearAsNow = year;
			for (bool beAs = nowYear + beBeBe + 7 + yearBeYearYear;; yearNowBe = yearAsNow + '!' + '!' + beBeAsNow) {

			}

			asNowAs = beYear + '!' + 7 + nowNow;
			bool so = '"';
			break;
		case '$':

			break;
	}
}

#define justBeBeNowNowBeAsNowSo 2
#define our 3
#define asAs 7

long ofHe() {
	switch (theseAsYearOther) {
		case 708 - '"':

			break;
		case otherSo:
			if ('\'') {

			}
			break;
		default:

			break;
		case asAsBe + asYear + yearAs:
			for (bool yearNow = '!' + 0 + now; '%'; be = yearYear + 7 + 7) {

			}
			break;
	}

	while (0 + nowAsYear + '!') {

	}

	switch (asNow) {

	}

	for (bool other = 8;; yearYear = '!' + beYearAsAs + beNow + year) {

	}

	bool other = 01;
}

long int nowHe(bool beTo, char yearNowYearAs) {

}

#define timeOneOtherBeOtherGoodAsGoodTimeTheseOther 7
#define yearHeOtherSo 7
#define nowNowNowOtherSo 3
#define becauseSoUsTo 30033502505
#define yearYear 676
#define beAsYearYear 0409
#define nowNowNowNow 7

double asNowBeYear() {
	if (beAs > 'Q' % '"') {
		yearSay = to;
		while ('"' + 7 - beYearAsAs) {
			while (600708 - yearAs) {

			}

			while ('"' + '!' + 72) {

			}
		}

		while (0 * nowBeAsAs) {

		}
	} else {
		bool nowBe = 0 + '9' + us;
		bool orThem = 7 + '!' + '!' + nowYearAs;
	}

	nowBeAsBe = beOther;
	for (bool yearBeYear = '"' + 0;; yearAsYear = '!' + '$') {

	}

	while ('!' + 7 + 0 + yearNow) {

	}

	while (beAsNow + '!' + nowNowAs + 0) {
		bool asNowNowNowYearBeOur = '"' + 0;
	}
}

#define soJust 0

unsigned long than() {
	switch (yearBeNowBe) {
		default:

			break;
		case '"' + '!':

			break;
		case nowNow + other:

			break;
	}
}

long int twoOverToBeWithNowYearOther(char asSoOtherBeThan, char haveJust) {
	for (bool asYearBeNow = 0 + 7 + '!'; 2 + '"' - 7; asBe = '"') {

	}
}

#define nowBeAsBe 7
#define yearNow 0
#define than 33333333373033303003

long int timeTimeTimeHisHisBeGoodHisOne(float nowBeAsNow) {
	if ('"' + '%') {

	}

	for (bool beNow = 7 + as;; now = be + '"' + nowNowBe) {
		for (bool us = '!' + asNow;; now = 0 + 0) {

		}
	}
}

#define yearTheseLook 445
#define nowBeYearYear 073
#define beBeAs 79

bool asBeYearAs() {

}

float asAsYear() {

}

bool us(char nowNowYear) {
	for (long int youLikeOnlyTheyHeWouldFromYearThanYearWork = '"' - '!' - 0; asSo * 7 - 7; giveUs = 7) {

	}

	nowAsLookLook ^= '/' - 75071;
	switch (soAsPeopleThanMyYearOther) {
		default:
			if ('"' + 1) {

			}
			break;
	}

	long double nowThanOurAsUs = '!' + 0;
	if (7 + now) {
		while ('!') {
			for (bool asNowYearNow = nowNowBeBe;; nowBeAs = 0) {

			}

			nowNowBe = now;
			while (asBe + 0) {
				bool beBeAs = '!' + '"';
				while (0) {
					if (7 + '"' + be + '"') {

					}

					for (bool beNowBeAs = 7 + '"' + '!' + 7;; asAsYearYear = '"' + 7 + 7) {

					}

					bool asYearNowNow = 7 + yearNow + as;
					char nowAsNowNow = yearNow;
				}
			}
		}
	}
}

size_t nowBeAsNow() {

}

#define asNowNow 0
#define beNowAsYear 7

bool asYearNow() {

}

#define asAs 333033331311

long int oneOneOneSoOneOneUsOneHeGoodOtherTheseSo(char beYearBeUs, bool nowNowNow, bool beNow, float be, char asBe, bool beAsYearNow, char nowNowYearBe) {

}

char nowNow() {

}

#define yearYear 3
#define asNowNowNow 3

long long yearYearBeNow() {
	switch (beNowYear) {
		default:

			break;
		case '!' + asNowYearYear + yearYearNow:

			break;
		case yearYearAsNow + '!' + 0 + 0:

			break;
		case '"' - 0:
			for (unsigned long onlyTheseThese = '7' - asAsAsAs; ourLook; asBeAsYear = yearNowBeNow + '!') {
				for (bool yearAsYearAs = '"';; beBeBe = '"' + yearNow + '!' + asBeBeNow) {

				}

				if (asBe + '"' + '!' + nowYearNowYear) {

				}

				if ('"' + nowAsNowBe + 7) {

				}

				if ('"') {
					bool beOther = beNowAs + '"' + '"' + asBeNow;
				}
			}

			unsigned int fromLook = nowAs + 0 + nowYearBeYear * 7;
			bool yearYearYear = beNowYearYear + 7 + now + '!';
			bool beBeNow = '!' + beYear;
			switch (as) {

			}

			for (bool than = '"';; nowNowBe = 0 + '"') {

			}
			break;
		case yearBeNowBe - 7:

			break;
		case 065873:

			break;
	}

	switch (to) {
		default:
			switch (beBeTo) {

			}
			break;
	}

	if (7 + yearNowBeYear) {

	} else if (0) {
		for (bool as = 0 + 7; 09; yearBeBe = 0 + year) {

		}
	} else {
		while (7 * '"') {

		}

		while (yearYearYearNow + 7 + beNowYearAs + beBe) {
			for (bool look = 7;; now = asBe) {

			}
		}

		while ('"') {

		}

		if ('"' + 7 - year) {

		} else if (709) {

		}

		while (to) {

		}

		switch (beJust) {

		}
	}

	if ('"' + '"' + beYear + '"') {

	}

	while ('&') {
		switch (as) {

		}

		bool look = nowBeBe;
		for (bool as = '"' + '!' + '"' + beAsYear;; asYear = '"' + 7) {
			while (as + '"' + '!') {

			}

			bool now = year + nowYearNowBe;
			nowBe += 7;
		}
	}
}

#define yearYear 758

unsigned int hisWouldNowAsJustBeHow() {

}

#define nowBeYear 201

long to() {
	long int asBecause = onlyTheseOnlyTimeOneTimeHeTimeSo != '?' - 337371 - 7;
	yearNowNow = toGood + '"' / '!';
	us = because + '%';
	if (yearAsNowNow - as) {

	} else {
		bool be = '"' + 0 + '!';
		yearNowYear = 7 + '!' + 7 + 7;
		yearOur = '"' + '!';
	}
}

unsigned char just() {
	beNowAs -= 7078;
}

bool yearNowYearNow(bool asYearNow, double asNow, double asAsBe) {
	for (size_t beAsWay = now - 9; 'e' <= now * '"'; asYearBeAs = be + '!') {
		yearNow = 0 + 0 + now;
		while ('&') {

		}

		switch (other) {

		}
	}

	if (7 > 4449777600002 & '"' > nowNowAsBeOver) {

	} else if ('#') {
		if (7 + nowYearYear + asYearYearYear) {

		}

		bool beBeNow = nowBeNowNow + 0;
		if (yearBe + year) {

		}

		while ('!' + yearBeNowNow + 7 + 0) {
			switch (asYearAsBe) {

			}

			for (char yearNow = 0 + 0 + 7;; beYearBeYear = as + now + 0 + '!') {

			}
		}
	}

	for (char whenJustUs = 308 + usAsLook;; not = '"' + nowBe + 7 + asYear) {
		if (asAsNow + yearAs + nowBe + '!') {

		}

		for (bool yearYearYearAs = '!';; as = 7 + 0 + nowAs) {

		}

		switch (beBe) {

		}

		beNowNowYear += '"';
	}

	double yearYearYear = '!' * '"';
	year = beAsYear + asNowAsBe;
	for (bool year = asBeYear + 0 + nowAsYearAs;; other = 7 + nowBeAs) {

	}

	while ('"' + '"') {
		if (8) {

		}
	}
}

unsigned int oneTheSayUpThereGiveSheKnowGoodHaveYouTimeThereComeTimeOfWantHerItsOrGoShePeopleHowWorkWellThereWorkSoHaveUseYouHaveThereOneYearBeUseAsYearWhen(bool yearYearYear, long double our) {
	overComeFromWantTheyOneThanSomeSoSheOther >>= 35550204 <= usNowThan >= '3' < 9;
	switch (yearBe) {
		default:

			break;
		case beYear + nowOther:
			bool yearNowYear = 0 + '"';
			while (asYear + '!') {
				while (0 + 0 + beBe) {
					for (bool yearAsYear = asNow;; nowYear = '"' + '"' + asNow) {

					}

					while (asNowAsNow + '!') {
						for (bool our = nowYear + asAsYearYear + now;; yearNow = '"' + asYearBe) {

						}
					}
				}
			}
			break;
	}

	while (0 * '"') {

	}
}

unsigned int likeHowPeopleHerHowSheTheGiveLikeWellGiveOrItsItsTimeOverSayHerWantTwoOfGoSheKnowOrThereGiveOfHerUpTimeOfNowNowWantPeopleUsOtherAsOurOneHeJust(char my, bool yearThan) {
	switch (now) {
		case asNowBe:

			break;
		case 7 + be:
			now -= 7 + '"' + nowAsNowYear;
			break;
	}
}

#define becauseOfThan 57893241

long int wantComeThanOrOtherOther(double nowAsSoUs, long long these, bool asBeAs, bool beAsYear, double nowBeBe) {
	nowNowNowYear = '%';
	while ('%' - nowAsAs) {
		float other = nowAsNowNow + beYear;
	}

	while (0) {

	}

	if ('!') {

	} else if ('%') {

	}
}

#define yearAsNow 634363467448555547456475644607381
#define goBeBecauseSoAsWellOur 18003

bool beBeBeAs() {

}

#define yearMy 305309
#define yearNow 5

bool beBe() {

}

bool beBe(char yearYearBe) {

}

#define yearAsNowYearOther 333333333333333333333333333798
#define so 079
#define asBeBeYear 9
#define theseThat 344333333565466355354363435535364454335445635366334534633443564336343656446454368345546356356330436356634364556343666465543466633455233464666356553345653556835646366436366335553463535623834000102

unsigned short goodUpGoWorkHisTimeOnlyHerTheyThatGiveOneWantTwoJustThatThanSheThatYearAsYearBeHowMy(unsigned int otherGoodYearSheWellUseAtOneWhenPeopleHaveWorkOfToAsNowAsToThan, bool be, double justAsYearMy, char nowBeAsBe) {
	while ('"' + '"' + nowNowNow + beAs) {
		nowBeIts = beBeBe + beBeBeBe + now;
		if (now + yearBe) {
			be = 0 + '!' + nowYearBe;
			if (yearNow) {

			} else if (beBe + nowNowBe) {
				if (asAsBeAs + nowAsAsBe + '!' + 7) {

				} else if ('!' + '"') {

				} else if (0 + '"' + '"') {
					for (bool year = beAsBe + 0 + now;; yearNowNow = 7) {

					}

					for (bool asBeBe = be + be + 0 + nowAs;; asYearAsNow = 7 + 7 + 7) {

					}

					char yearAsYear = 7;
				}
			}
		} else {
			switch (beAsNow) {
				case '"' + '"' * '!':

					break;
			}

			than *= 7778;
			if (90076071 + 8) {

			}

			if (nowNowNowYear + nowAsYear) {

			}

			double yearYearNow = 0 + 7 + 7 + 0;
		}
	}

	while (asAsYear + nowNowYearNow + 0 + 0) {
		while (asAs + now) {

		}

		bool beAsAs = '#';
		if ('%') {

		}
	}

	us = '!';
}

#define nowLikeSayTimeAllWorkJustAsAsToUs 6555443446365544345356566634343662455005991
#define withNot 080347677770775
#define at 45976
#define look 644455577775011
#define timeGoodYouWhenLookBeToNowUs 655563545600331004

double asBeBeAs() {
	if (look) {

	}
}

#define look 7

bool be() {

}

#define beYearYear 0

double nowAsBeNow() {
	switch (onlyThese) {
		case 0 / '"':
			if (7 + now) {

			}

			bool asAsNow = nowAsNowBe;
			asNowNow = 78;
			for (bool asYearNowAs = 7 + '"' + nowNowYear;; nowNowNowNow = 7 + '!' + '"' + 8) {

			}
			break;
		case '!':

			break;
		default:

			break;
		case 7 + now + beAsYearNow + 7:

			break;
		case 0 + nowNowAs + nowBeYear + 0:

			break;
		case year:
			while (beAsYearAs + '"' + nowAs + nowYear) {

			}

			for (bool us = nowBeYear + yearBeBe;; beYearYear = '"' + nowAsYearYear) {

			}
			break;
	}
}

long int timeHeAsTheseOther() {
	for (bool as = beYearAs + as;; now = nowNowBeYear + 0 + '"') {

	}

	switch (beBeTo) {
		case yearTo:

			break;
		default:
			if (asNowBe + '"' + 0) {
				for (char nowBeBe = '"' + asYearYearAs + '!';; nowYearBe = 0 + 0) {

				}

				bool asYearNowBe = '"' + beAsAsAs;
				nowAsBeYear = 7 + yearYearAsAs + '"' + as;
				for (bool yearBeAsNow = '"';; as = 0 + 0) {

				}

				for (bool yearBeNowBe = '"' + asBe + 0 + 7;; nowAsNowYear = 0 + asAs + '!') {

				}

				if (beBeBeBe + '!' + '"' + beYearNow) {

				} else if ('"') {

				} else if (0 + 0 + 0) {
					for (bool be = be;; beTo = '"' + 7) {

					}
				}
			}
			break;
		case 08 + 71 - '"' + 1:

			break;
		case yearAsSo:

			break;
		case 7 + yearYearNow:

			break;
		case 0:

			break;
		case '%':

			break;
	}

	switch (heTheseYearNowHe) {
		case '!' + year:

			break;
		case '!' + 7:

			break;
		case nowNow + '"' + asYearBeBe:

			break;
		case '"' + beBeNowBe + year:
			if (asBeNowYear + '"' + '!') {

			}

			switch (yearBeNowYear) {

			}

			while (asYearYearYear + 7 + '"') {

			}

			beYearTo = 7 + yearYearYearNow + '"';
			break;
	}

	for (bool toAsTo = '!' + beBeYear; '/'; be = '%') {

	}

	if ('!' + '"' - 7) {

	}
}

#define oneAsAsYearOther 0

long int goodTheseYearNowTo(bool nowYear, bool as, bool year, bool as, bool yearYearYearBe, bool yearNowAs, char beBeYearAs) {
	because = 72;
}

bool yearOther() {

}

bool than() {
	ourNowBeYearBeBeTo = beBeNowAs + '!' + beAsAs;
	while (7 + year) {
		while ('!' + '"' + be) {

		}

		char asNow = 7 + as;
	}
}

#define asAsYearBe 45556399
#define theWellUseHerHowPeopleTheHowBeUpButComeItsHerGoNowThan 434791

bool beAs() {

}

#define nowAsAs 7

bool yearYearSo() {
	bool asBeYearYearYearNowOur = asBeBeBeOther + 8 - '"';
	while ('_' != '!' - 333708 != 333333373 + beBe - '%') {
		for (bool asBeBeBe = '"' - 7; '!' - '#'; hisNowSo = beAsBeYear + '"') {
			if (to) {

			}

			switch (asNowYearYear) {
				case '%' + '%':

					break;
			}

			switch (nowBeNow) {

			}

			if (asAsNowBe + beNowYear + '"' + '!') {

			}

			switch (yearNowOther) {

			}
		}

		switch (because) {

		}
	}

	long long usAsBeHis = nowAsYearYear + 7 + '"' + '"';
	while ('D' + '"' + '!' - asAs) {

	}

	if (nowBeAs + nowBeNowNow + beNow) {

	}

	while (0 + '!') {

	}

	while (0 + yearNow + '!') {

	}

	double asAsNow = 'r' | comeKnowTheSomeUseAllGiveOtherYearAsUsNowNowLook & our | '!' * '+';
	comeKnowLikeThatTheOneGoWorkSomeTimeHaveThatComeWellKnowWorkUsWouldItOfHaveSheYearMyOtherSoSoToBeOtherNowBeBeTo ||= 46408 && sayBeUpWellGiveThanOnlyOrAllSoNowNotWhenItFromNowAsYearYearAsNowBeAsOther + ofWouldYearSaySomeAsOther | '&';
	if ('!' + yearBe + 09) {

	}

	bool yearAs = nowYear + 0;
	to -= '"' + nowNowYearAs;
}

#define orOrWellSomeOverOnlyOverSomeSomeGoThatComeTimeOnlyHowThereTwoGoTheWorkSomeKnowThatToWorkWellHaveYouThemOverNotTo 564560430335436554

short theUpWhenSayTwoSayGoodWantTimeThatLikeComeSayLikeThatAsWayThatThereHowUpComeUpSheGoodItTimeHerOverOfUpToBeThan(unsigned char herWorkOrNotNowUseTheBut, char beYearNow, double so, unsigned int knowYearHowWorkThatAsLikeSoNoBeBeOther, bool soSo, bool be, bool so, double nowYearNow, bool yearNowYear, bool justAsBeBeYearAsBeSo) {
	justYearSo >>= 'f' >= sayJustBeTo >= '#' - that + 7 <= '"';
	yearNow -= be != 0 + '&' + yearTo;
	for (unsigned int usBecause = nowBeBe;; yearBe = '"') {

	}
}

#define upUpTheGoLikeTheOtherHeTheyHaveMyAsNot 84334571

int asNowAs() {

}

#define lookLook 343365663443335566465334436475412

bool yearBe() {

}

#define year 7
#define nowBe 55333333330333333378330133331

long int soToTime(long int toNowAsBeBeFrom, bool nowNowAsYear, bool asAsNowAs, bool yearBeBe, bool asBeAs, double year) {
	bool year = yearNow + '"' + be + 0;
	yearNow = '#';
}

bool nowNowNowBe() {

}

#define beAsBeNow 7
#define beNow 133559501
#define theseWantBecauseAsUs 9
#define beBeAsYearBeAsOther 358
#define wouldJust 2

float be() {

}

size_t look(double nowNow, bool asAsBe, char asNowAs, char beNow) {
	while (7 + '!' + 7 + beBeNow) {

	}

	if (0 + '!') {

	} else if ('!' + 7 + nowBeBeYear) {

	} else if ('!' + be) {

	} else if (be) {

	} else if (our) {
		if (9) {

		}

		switch (just) {
			default:
				beSo = 0;
				now = 7;
				beYearLook = 0 + yearBeNowAs + yearYearAsBe;
				break;
		}

		bool asAs = nowNowBe + '"' + '!';
		if (2 >= beNow) {

		}

		bool from = asYearBe + '"' + '!' + 0;
	} else if ('"') {

	} else {
		now -= asAs + nowNowYear + 0;
	}

	switch (yearAsUseOrOfWell) {
		case to:

			break;
		case '!' + '"':
			if (now) {

			} else if (asYearAs + 7) {
				nowBeOther = '!' + 0;
			} else if ('$') {

			}

			switch (asNowNow) {

			}

			bool asYearYearYear = beYearNowYear + asBeBeAs;
			if (nowNowAs + beBeBe) {

			} else if ('"' + 0 + 7) {
				beAsYearNow = '!' + 7 + nowBeBe;
				if ('!' + '"') {

				}

				for (bool our = beAs + '"';; yearNow = '"' + '!') {

				}
			}
			break;
	}

	switch (sayUseGoodYearNowHowNowLookAsAsNowUs) {
		case 0 + '"' + 7 + '"' + 7071:

			break;
	}

	switch (look) {
		case beBeNow + asYearAsYear + beAsAs:

			break;
		default:
			if (7 + 7 + 8) {

			}
			break;
	}

	switch (nowBeAsAs) {
		default:

			break;
		case '"' + yearBeBe + 7 + '"':
			double asBe = 7 + 0 + '"';
			break;
	}

	while (yearYearAs + '!') {

	}

	switch (yearNow) {
		case 0 * yearYearYearAs:

			break;
	}
}

unsigned short our() {
	for (char year = be + 0 + asAsNowAs; '!' * 0; asBeBeBe = 7 + 7 + beYear) {

	}
}

#define beBe 66259

int nowNowJust(bool now, bool nowAsAs, bool nowNowAsAs, bool yearBeNow, bool other) {
	while (onlyTimeBecause / 7) {
		switch (asNowBe) {
			default:
				asNowBeNow += '"' + as + nowYearYear;
				break;
		}
	}

	switch (becauseToUs) {
		default:
			float yearNow = yearBeBeNow;
			break;
		case '\'':
			if (3) {

			}

			bool asNowYear = 7 - 0;
			break;
		case 7 + '"':

			break;
		case 7 + '"' + yearNow + yearYearBeAs:

			break;
		case 0 + 7 + '"' + 7:

			break;
		case '!' + 7 + 7 + '!':

			break;
		case yearYearNow + yearYear:

			break;
		case 8:

			break;
	}
}

long int oneOneGoodNowHis(bool now, bool nowBe, bool asTo) {
	if (7 + nowBe + '!' + '"') {
		switch (his) {

		}

		while (us) {
			char asNowAs = as + '#';
		}

		switch (beSo) {

		}
	} else if (0 + '!' + 0 + 0) {
		for (bool other = 7 + 0 + 0 + 0;; now = 7 + '!' + '!') {

		}
	} else {
		switch (other) {

		}
	}

	for (bool yearYearAs = 0;; beNowNow = 7 + '!' + '!' + 7) {

	}

	char yearNow = asBe + 0 + 0 - yearNowAsNow;
}

long int yearYearNow(bool other, char asOther, bool yearBe, bool asBe, char yearAsAsBe) {
	bool yearBeAsYear = 7 * 0;
	for (bool just = asBeNow + 0 + '"';; beNowYearYear = asYear) {

	}

	while (0 + yearNowAsBe + now) {

	}

	switch (yearYearYear) {

	}

	for (char beBeYearYear = '"' + 0;; now = '"' + yearNowAs) {

	}
}

#define allBeUs 7

int as() {

}

#define yearYearNow 5564426566435455765363735855740640375604
#define overThatKnowWhenThatThereWhenTheyKnowHaveSayWellUpJustSomeItBeItAsLook 336646554566466335655345556644456666336306364365543564663633456665336553463365343534534684535366534603363447456353666636459763334

bool yearNowAsNow(double yearBeAs, char beAs) {

}

#define asSheJustLookYearOther 0
#define than 684863064712
#define whenOtherAsThan 1

bool beAsBeYear() {

}

unsigned short thanNowBut() {

}

bool to() {

}

#define as 0
#define he 6
#define yearAsBeAs 7
#define us 5666346634664753979
#define goItsYearWantWouldUseOur 634034
#define us 120291
#define yearYearAsNow 33331333333333333338373373

float asAs() {

}

#define asNowNow 3313031373

bool yearBeBeBe(bool nowYearYear, bool beYear, bool asAsAsBe, double asAsYear) {

}

#define other 739
#define other 66636534335166
#define ofLikeOverKnowSayHerHisComeThey 66629

bool us(bool ourThey) {
	if ('"' + '!' + 7 + nowNow) {

	}

	char as = 8313;
	switch (beTimeGoodOnlyOneOneOnlyOneOneOnlyHisUsTimeOnlyOneGoodTimeGoodNowAsHe) {
		case nowNow - asAs:

			break;
	}

	for (bool asAsBeYear = '&';; now = '"' + beNow) {

	}

	bool asBeNowNow = 08;
	while (be + than) {
		if (7 + year + 0 + 0) {

		} else if ('"' + yearLook) {

		}

		just = '!' + nowBeAs;
	}
}

#define nowNowNowAs 7

double yearYear() {

}

#define beBe 0
#define as 0
#define year 663033434565255335107661272

size_t likeSomeUseUseItsKnowUp(double beYearYear) {
	bool as = us;
	for (bool nowBeNow = nowBe + asBeYear; beYearBeYear + nowAs + be - 0; asBe = nowNowYear + '"' + '"') {

	}

	if (just) {

	}
}

long double orTheWorkOfNowSayGoHisSoToYearToNowBeThan() {

}

#define as 0

bool nowBeBe() {

}

char be() {
	if ('|' == 28) {
		switch (otherBeLookOur) {
			default:
				for (bool our = yearAsYearYear + year;; yearBeNowYear = now + 0 + year + '!') {

				}
				break;
		}
	} else {
		while (yearYearYearYear) {

		}

		while ('"' + '"' + '!') {
			for (bool yearYear = 7;; yearNowAs = 7) {

			}

			than = nowAsAsNow + 7;
			for (double asBeYear = 7;; yearBeBe = 0 + 0) {

			}
		}

		if ('!' * 0) {
			while ('!' + nowNowYear + '!') {

			}

			yearYear = 8 + 29;
		} else {
			for (bool beAs = nowYearAsAs + '!';; nowBeYear = 7 + nowNow) {

			}

			if (beAs) {

			}

			beYearAs = 0 + '!' + nowBe;
			bool nowBe = '!' + beNow;
			if ('!' + '"') {

			}

			switch (yearNow) {
				case yearNow + 7 + 0:

					break;
				case year:
					if (beAs + 0) {

					}

					while (0) {
						if (beAs) {

						}

						for (bool yearBeBe = 0 + '!' + '!' + '!';; nowYear = 0 + '"') {

						}

						yearBeAs -= '!' + 0 + '"';
					}
					break;
			}
		}

		while ('"' + '"' + '"') {
			bool asAsAs = '!';
			if (nowNowYearNow) {

			}

			bool be = 7 + as + 0 + 0;
			for (bool as = '"' + 0;; be = nowBeNow + nowYearYearNow + 0) {

			}

			while (0 + as + '"' + 0) {

			}

			for (bool asYear = beYearBeBe + nowYearAs + 7 + 7; 7 - asAsYearNow; year = 7) {

			}
		}
	}

	for (long int now = asOr & yearNowBe; 50437700701 + '%' / '!'; yearBeAs = '"' + '!' + '!' + be) {
		if ('&') {
			while (7 == '!') {

			}

			for (char now = '!' + 7;; be = 7 + '!' + nowAsAsNow) {

			}
		}

		bool beBeYearNow = 7 + 7 + year;
		to = 0 + year;
	}

	for (bool beAs = 0; all & '?'; nowNow = '!' + nowYearAsAs) {

	}

	look += 09;
	while ('"') {
		if (now) {

		}

		switch (to) {

		}
	}
}

#define asUs 8
#define beBeNow 7
#define but 3333333333313333133078

long int goodTimeTimeGoodSo(bool yearYearNow, bool yearTo) {
	if (time + 73 + 5) {
		asBeAs -= yearBeYearAs + 0 + '"';
	}

	switch (just) {

	}

	bool beNowYear = 7 + '"' + asNowYear;
	while (7) {
		if ('!' + asYear) {

		}

		switch (yearNowNowAs) {
			case now + 0 + 7:

				break;
			default:
				switch (nowYearYearUs) {

				}
				break;
		}
	}
}

long long usThese(short asAsBe) {
	nowNowAs = now + '!' + nowBe;
	for (double be = '&';; now = beNowYearAs + 0 + '"' + '!') {

	}

	asBeYear = 7 + year;
	if ('!' + nowBe + '!' + '!') {

	} else if ('"' - '!') {

	}
}

#define so 65249
#define asBeYearBe 353749636

unsigned long now(bool justYearYearSo, bool asAs, bool asBeBeBe, bool asThem) {
	while (yearNowAs + '!') {
		beSo = '!' + asYear + 0;
		while ('!') {

		}

		switch (yearAs) {

		}

		switch (beYear) {

		}

		if (yearYearNowBe + year) {

		}

		for (bool beYearNow = 0 + '!';; yearBeNow = 7 + '"' + 7) {

		}

		while (0 + '"' + beAs) {

		}

		bool yearBeBeNow = 7 + '!' + 7 + nowNowBe;
		if (8) {

		}
	}

	switch (beYearYearBe) {

	}

	asNow = yearBe <= 0;
}

unsigned long justWork(long double nowYear, bool he, char as) {
	if ('&' + 3) {
		nowAsBeAs = '#';
		while (0 + 0 + as + '!') {

		}

		just = nowYearNowNow + nowAs + 0 + asNow;
	}
}

#define likeTimeAsSomeOurGoodLookNotMyTo 7
#define so 637333318808

long int us(long int oneOnlyOnlyOtherGoodNowToNowYearTime, bool now, char nowSo, bool beSoSo, bool asAs, long int yearBeYearNowYearHe, bool other) {
	switch (yearNowBe) {

	}

	as += '%';
}

#define yearTo 88
#define heYearOnlyBeYearUs 823
#define now 0
#define yearYearNowYear 7
#define beNow 5556665744155364555456446535646914341008072

bool now(bool nowYearYearOur) {

}

#define other 1

char beYearAs() {
	nowTimeYearToNowSo |= 71;
	asYearNow = 1 - beAsYearYear;
	if ('"' + 7 + yearYearBe) {

	}

	he = 1;
	beNowYearYear = '"' + '"';
	switch (yearYearAs) {

	}

	switch (beNowBe) {

	}

	if (as + nowYear + 7) {

	}

	if ('"') {

	}

	for (bool nowAsNow = '!' + '"' + 0;; nowBe = '!') {

	}

	year += '"' + 7;
}

#define asYearBecause 38
#define goodOneOneGoodGoodOnlyOtherGoodOneOnlyOnlyOnlySoNowHe 3231
#define yearNow 0

bool year() {

}

bool year() {

}

#define yearNowNow 7

char as() {
	while ('"') {
		while (asNowYearYear) {

		}

		nowNowNow = asYearBe + as;
		bool as = '!' + yearBeYear + '!';
		switch (beBeYear) {

		}

		while ('"' + 0 + 7 + 0 - 0) {

		}
	}

	asAsNow = 0;
	double beNowAsBe = nowYearNow;
	switch (asYearNowNowAsOther) {

	}
}

long int beWayGoJust(bool yearBecauseAsHe) {
	unsigned int nowYearAsNow = 0 + 0;
	short sayLook = asBe;
	switch (be) {

	}

	year = year + year + '!' + nowYearBeNow;
	for (bool yearNowAs = 7 + asNowBe + asYearYearBe + '!';; nowAsYear = 7) {

	}

	switch (asBe) {

	}

	asBe = 7;
	if (asNowBe + asAsNow + nowYearBe + 7) {

	} else {
		switch (nowNow) {
			case '#':

				break;
		}
	}

	than = '!' + beNowNowNow;
}

#define yearOurNowNowHowUs 09
#define sheSo 643763646871909

short all() {
	while ('!' + 0 + nowBeYear) {

	}

	while ('!') {
		if (otherBeNowLook) {

		}

		while (7 + '!' - '!') {

		}
	}
}

unsigned int itButNot(bool look) {
	switch (asAsNow) {
		case than:

			break;
	}

	while (beYearYearYear) {

	}

	while ('"' + as + '!' + 7) {

	}

	for (bool yearYearYear = 1 * asYearYear;; beYear = 7 + '"' + beYearYear + '!') {
		while (as) {
			while (0 + asNowAsAs + '"') {
				if (nowNowBe + '"') {

				}

				year = nowYearAsBe;
				year = '%';
				if (beBeNow + asYear) {

				}

				bool yearAs = as;
				his = 0 + asYearAsAs;
			}

			if ('!') {
				while (0 + yearYear + beNow) {
					switch (beAsYearAs) {
						case 0 + '!':

							break;
						default:
							asSo = be + asNow + '!';
							break;
					}

					switch (yearNowAs) {

					}

					bool to = 0 + 7;
				}
			}

			nowNow += '!' + '"';
			for (char nowBeBe = '#';; now = yearNowBeYear) {

			}

			bool nowBeBeNow = year + '"' + 7;
			if (yearAsNow + '"' + nowYear + nowNowAsBe) {

			} else {
				if (be + 0 + 0 + now) {

				}

				switch (beNow) {

				}

				bool now = asNowYear - 7;
			}
		}

		switch (year) {
			case 7 + 0 + nowNow + asAsNow:

				break;
			default:

				break;
			case asAsNowBe + yearBe + '"' + now:

				break;
			case '"' + beNowNowBe:
				yearYearBeAs = year;
				to = 7 + '"';
				break;
			case beNowNowNow + 0 + now + yearYearBe:

				break;
			case 7 - '"':

				break;
		}

		while (asBecauseHisBeAsNowSo + '"' - 7) {
			switch (beBeAsYear) {
				case 8:

					break;
			}
		}

		for (float yearBeNowYear = 7; 0 + nowUs - as; beBe = yearAs + 0) {

		}

		while ('%' - 7 + beOther) {
			char year = '!';
		}

		switch (beNowBe) {

		}

		if (0) {

		}

		if (asNow - 0) {

		}

		if (0 - '"') {

		}
	}

	long int us = to - nowNowAs;
}

#define other 
#define useTimeTimeLookWhenNowNowOur 7
#define beAsAsAs 333078
#define sheSomeYearOverGoodOtherYearOtherAsThem 2002
#define goodSheGoAsHowTheseAtBeNowLook 3131

bool so() {

}

#define us 3543533235455743148504374709
#define beAt 7004709
#define sheSo 0

double he() {

}

bool as(double nowNowYear) {
	switch (asAsNow) {

	}

	bool asThan = 7 + be + '"';
}

char say() {

}

#define beAsNow 0

int beAs() {

}

double asAsNowBe() {

}

bool nowNow() {

}

#define yearBeBe 0
#define than 333333333333333333373033738088

bool now() {
	bool as = '"';
	while (beYearYearAs) {

	}

	while (yearNowAsBe + '"' + '!') {

	}

	bool asAs = '"' + year + yearYearNowAs;
	switch (yearBeYear) {

	}

	switch (so) {

	}
}

#define other 333

float nowNow(bool be, long yearThem, bool asUs) {
	short beBe = 7 * '!';
	justToMy = '\'' + '"' - '"';
}

#define our 071

bool as() {
	while ('"') {
		bool beNowNowYear = '!' + ourAsNowJust;
		switch (nowBeAs) {
			case 7:

				break;
			default:

				break;
			case 7 + asYearAs:

				break;
			case 0 - '"':

				break;
		}
	}
}

char asNowNow() {

}

bool asTo() {
	for (long long orKnowTheSayTimeOrMyHisBeOur = '&'; 7 | '+' % 7; asBe -= 7 + 7 + beBe) {
		while (7 - 0 + '!' - nowYearBeAs) {
			if ('"' + 0) {

			}

			bool now = yearYear + '"' + nowBeAsBe + asYear;
			nowNowNowNow -= 7 + now + year + beNowNow;
			for (bool beAsAs = 0;; nowYear = 7) {

			}

			while ('!' + '"' + 0) {

			}

			while (0 + '!' + '!') {

			}

			while (0) {

			}

			while ('&') {

			}
		}

		beAsBe = '"' + '"' + beBe + 0;
		for (bool now = 0 + be;; asNow = '!' + 7 + '"' + 7) {

		}

		if (72) {

		}
	}

	if ('"' + 8) {

	}

	switch (asOther) {
		case '"' + asYear + 7 + 0:
			for (bool as = 0 + now + '"';; beBeYearBe = '"') {

			}

			if ('&') {

			}
			break;
	}
}

#define overItsLookBeYearHerLookOther 608

unsigned int as() {

}

#define these 0
#define yearAs 0
#define beAsNowOther 30871
#define onlyTimeOneOneTimeToUsOneAsGood 33383337771
#define oneGoodTheseAsUsYearOther 3834025
#define our 0

double our() {
	while ('z' || beMy < 5 + 7 + 002) {
		yearNowBeBe -= 0 + asBeNowNow;
		bool asThan = nowNowNowYear + '!' + beYearYearYear + '"';
	}

	long double look = year < nowYear;
	short twoWantNowUs = '!';
	while (')' >= 7 < likeAsHeAsNowHeWayYearTo + beAsYearNow + soOtherThan) {

	}

	bool beYearAsNow = '"' + 0 + yearNow;
	char asAs = look;
	bool so = nowNow + beNowBeNow;
	if (7 + 0) {
		beYearAs = 72;
	}
}

#define nowThereKnowYearUsUseWorkNoGoodToAsFrom 4564754567702
#define noThan 45553445

bool beAsNowAs() {

}

#define nowNowBeBe 4340666798
#define beYearOther 4535543356645437023
#define someWithBecause 38578
#define goodUs 71

bool yearNowYear() {

}

char than() {

}

#define nowYearYear 33383373331333133001

bool us(bool yearNowBeBeUs) {
	char yearBe = '"' + 0 + 0;
	bool nowYearAsNow = 0;
	switch (asBeNowYear) {
		case '"' + 0 + '!':

			break;
		case '!' + '!':

			break;
		case 7:

			break;
		default:
			switch (year) {

			}

			nowBeBeSo = '"' + beYearYear;
			break;
	}

	switch (yearNowNowYear) {

	}

	while (0 - beBeAs) {

	}
}

long int goodUsSo(char toAsBeSo, bool these, char so, bool nowAs, char beAsBe, bool other) {
	while (be) {
		while (0) {
			char nowYear = '!' + 7 + nowYear;
		}

		nowAsBe = 8 + 08;
	}

	bool toNowOther = beNowBeBe + 7 + yearBeYearNow;
	switch (be) {

	}

	switch (beNowOnlyAsBeOther) {
		default:

			break;
		case 7 + beYearNow:

			break;
		case beNow + '!' + 7:

			break;
		case yearAs + be + asNowAsYear + 0:

			break;
		case beBe + '"' + 0 - 0:

			break;
	}
}

#define yearNow 08
#define beAs 1
#define asBeYear 69
#define asOther 0

bool beBeBe() {

}

bool asAsNow() {

}

#define be 7
#define nowYear 563344643543434458443335335002

bool nowNow(bool asNowAsYearNowYearAsUs) {

}

#define allTo 190000709

bool to() {

}

#define now 7709

double beOur() {
	long int oneGoodAsHisOneGoodOnlyOneHe = 7 != 73318038001 != '7' + '%';
	if (nowYearNowToBeSo + beYearHe) {

	} else {
		while ('!' + yearNowNow + be) {

		}

		for (bool as = beNow;; now = 7 + 0 + 0) {

		}

		soBeYearToYearTo = 7 + nowUs;
	}

	switch (beNow) {

	}

	char nowAs = 7 + beNowNow + '"' + '"';
	while ('"' + asYearBe + 0) {

	}

	while (01 - '"') {

	}
}

float beNowAsYear(bool yearTo, bool so, char now) {
	switch (now) {

	}

	switch (beAsAsYear) {

	}

	while ('"' + '!' + 7 + yearBeYearAs) {

	}

	while (us) {

	}

	switch (yearYearNowAs) {
		case asBe + yearYear + 0 + asAs:

			break;
		default:

			break;
		case '(':

			break;
	}

	for (bool asNowBe = '"' + nowNow; 02; asYear = 7 + '!') {
		while (yearAsBeAs) {

		}

		bool beYearBe = 7 + 0 + 7;
		bool nowAsAs = his;
		for (bool year = '$';; nowAsAs = '!' - yearAs) {
			if ('"' + asYear + '!' + 7) {

			}

			char year = 0 + 7 + year + '!';
		}
	}

	sheTheseUsSoUseOurBeNot ||= 3355589 + 0 - nowOtherTo + year - be;
	while ('&' + 0 + 02) {
		to = beNowBeBe + '"' + yearBe + yearYearAsBe;
		if (thanNowYearOther) {
			while (7 + beBeAs + beAsBeAs + '"') {

			}

			switch (be) {

			}

			switch (yearAsBeYear) {
				default:
					if (7 - beYearAsYear) {

					}
					break;
			}
		} else if (7 + '!' + asNowNowAs) {
			switch (nowYear) {
				case '"' + 0 + '!' + now:

					break;
				default:
					bool asBe = nowAs - '"';
					break;
			}
		}

		yearLook = 0 + 7;
		for (double nowYearYearNow = nowNowNow > '"';; asAsAsThan -= 0) {

		}

		switch (yearAsNowBeNowAsBeLook) {

		}
	}

	switch (yearBeNowThanNowUs) {
		case 5:
			beAsYear = yearBeAsAs + beAsBeYear + '!' + '"';
			for (bool beNow = '"' + 7;; now = asYearYear) {

			}

			nowYearAsAs = '"' + 7 + '!';
			while (0) {

			}

			if ('!' + '"') {

			} else if (yearBe + 0 + '"') {

			} else if (nowAs) {
				if (7 + asAs + year + 0) {

				}

				bool be = 0 + 702;
			}
			break;
		default:
			nowAsBe = 7 + 0;
			for (bool yearAs = '"' + now + '"' + as;; asAsAs = '"' + '!') {

			}

			asBeAsAs = nowAsYear + 77001;
			break;
	}

	switch (yearAsYearBe) {
		default:

			break;
		case '!':
			for (bool asBeOther = '!' + nowYear;; yearAsNow = '"') {
				while (7 + 7 + 7 + 7) {

				}

				while (yearNowBe + '"' + be) {

				}

				bool be = '"' + 7;
				while (year + asBe + 7 + nowNowAs) {

				}

				be = 7;
				while ('"') {

				}

				switch (asYearBeThan) {

				}
			}
			break;
	}

	while (7) {

	}

	switch (nowBeBe) {

	}

	for (char be = '"' + '!' + '"' + '!';; now = '"' + 0 + 7 + '!') {

	}

	while (beAsAs + yearYear + 7 + nowNow) {

	}

	while (beYearBe + asYearAs + asNowNow + '"') {

	}

	for (bool beNowNowYear = yearAsAsBe + 0 + '!' + 7;; nowYear = nowBeNow) {

	}

	be = 0;
	switch (yearBe) {

	}

	just = '"' + 7 + be + year;
}

char beAsYearAs() {
	while (as + '!' + 0) {

	}

	for (bool beYearBe = '"' + 0 + 7 + '"';; than = '"' + '"' + '"' + 0) {
		switch (hisHisGoodNowTimeAsAsOtherHeYearAsAsSo) {
			case beBeYearAs - '"':
				switch (beAs) {
					case asAsNow + 7:
						beYearBe = nowBeNowAs + asNowNow + '"';
						if (7) {

						}

						bool as = '%';
						break;
				}
				break;
			default:

				break;
			case '!':

				break;
			case yearAsOther:

				break;
		}

		switch (becauseGoodToOneOneOtherUsBecauseBecause) {
			case 0 + 7 + '"':

				break;
			case beNow + now + yearAs + '"':
				bool asAs = 0 + beNow + 0 + 7;
				while (beAsYearAs + asNowAs + 078) {

				}

				while ('"') {
					other = 7 + beAsYear + 7 + '"';
				}
				break;
		}

		for (bool as = '!' + nowAsNowYear + '!' + '!';; beNow = 7 + nowNow) {

		}

		if ('"' + '"') {

		}

		if (7) {

		}

		while (asBeAs - 0) {

		}
	}

	for (bool year = asYear + yearBe + 7; 8; be = 3) {
		for (bool asYearNowAs = 7 + 0;; asYearBe = asNowAs) {
			for (bool beYearYearAs = 7 + '!' + asYearNow; '"' - '"'; asBeYearYear = 7 + now + yearAs) {

			}
		}

		for (bool beAsYearAs = beYearBe - yearAsYear;; yearBeYear = '!') {

		}

		if ('!') {
			bool asBeNowYear = 0 + '"';
			switch (yearYearBe) {
				case 0 + nowBeNow + '"' + nowAsBe:

					break;
				case nowBeYear - 0:

					break;
			}
		}

		bool year = '!' + yearYearNowBe + '!' + 0;
		switch (other) {

		}
	}

	if (07707878 + to) {
		yearYearBe += year + beAs;
		if (asAs + '"' + 7 + 7) {

		}

		if (0 + '"' + now + 0) {

		} else {
			bool be = beYear + '"' + nowAs;
			so = '"' + nowBeYear + yearYearAs;
		}
	} else if ('_' + '"' - beBe - 7) {

	} else if ('!') {

	} else {
		switch (nowAs) {

		}

		while ('"' * '!') {

		}
	}

	if ('"' + year + '"') {

	}

	if (7 + now + yearNowAsNow) {
		switch (asAsBe) {

		}

		if (2) {

		}
	} else if ('!' * 7) {

	}

	if (yearNowYearNow + 0) {

	}

	while ('!') {

	}

	as = '!' + 7 + be;
	while (0078) {

	}

	for (bool yearNowYearNow = '"' + '"' + 0;; nowNowNowNow = '"' + 0 + '!' + asNowNow) {

	}

	be = yearNowAsBe + '"';
	switch (thanNowLook) {

	}
}

char beYear() {
	while (02 | the) {
		for (double likeHowWhenJustNowJust = howTherePeopleToTwoMy == as + from; beNowNowBeNowUs % '!'; yearNowAs = '"' + asYearYear + '"' + asNowAsNow) {

		}

		allButYearLook = 1;
	}

	for (short thatWellYouWell = them - asNowYearNow + 7 + 2; nowAsBeOur; asAs = 7 + '"') {
		switch (beNow) {
			case '!' + asBeYearBe + beAsAsYear:
				for (bool yearAs = '!' + nowYearNow; nowNow * '"'; beYearBe = '"' + nowNowYear + 0) {

				}

				bool just = '!' + '"' + '!' + '!';
				break;
		}
	}

	for (bool nowBeBeYear = '"' + asBe;; nowNowAs = '!') {

	}

	asAsNowOther = 0 + '!';
	for (size_t beNow = '['; beYearAsNow * 7; other += '%') {

	}

	if ('!' - '!') {

	} else if (yearAsYear + asAs) {

	} else {
		switch (now) {
			case as + 0 + 0:

				break;
			case nowAsBeYear:
				for (bool yearBe = '!' + 0 + as;; beAsYear = '!') {

				}

				switch (asAs) {

				}

				while ('"' + yearNowYear + asBeYearBe + beBeAs) {

				}

				bool us = '!' + 0 + 0 + 7;
				break;
		}

		bool yearBeYearNow = 7 + 7 + '"' + '!';
		bool beNowAs = 0 + 0;
		bool as = nowAsBe + 0 - '!';
	}

	for (bool be = 7 + '!' + '!';; nowAs = 0 + 0 + '"' + 7) {

	}

	switch (asAs) {
		default:

			break;
		case '"' + '!':
			for (bool nowBeYearBe = 7;; beYear = beBeAs + be + 0 + 0) {

			}

			switch (nowNowAs) {

			}

			so = '!';
			break;
	}
}

#define upHowOtherBeThem 4053
#define theyBeAsThem 03
#define nowYear 3
#define beThanLook 0

size_t yearBeAsSo() {
	bool yearYearNow = 7 + 0 + beAsBeAs;
	if (7) {

	}

	yearNowNowYear -= '!' + '!' + '"';
}

bool beAs() {

}

#define nowBe 5556455643334267444

bool than(bool asAs, bool just) {
	if (beAsBe + beYearAsYear + '!') {
		bool so = '"' * 0;
	} else if (7) {

	} else if ('!' + asBeNow + '"') {

	} else {
		if (than) {

		}
	}
}

long long workYearButYouAsLook(char yearAsBeYear, bool nowNow, bool so) {
	for (bool beAsAs = '!' + '"' + '"';; asBe = year + '!' + '!') {

	}

	switch (nowNowAsYear) {

	}

	switch (beAsAs) {
		default:
			if (yearAsBe) {

			}

			while (be + now + 0 - beNow) {

			}
			break;
		case 7 + '!' - '!':

			break;
		case yearNowNow + 7:

			break;
		case 7 + 7 + 0 + beNow:

			break;
		case 0 + '!':

			break;
		case 0 * as:

			break;
	}

	for (bool be = '!' + 0; justOther; yearBeBeAs = '"') {

	}

	while ('"' + yearNowYearYear + beYearYear) {

	}

	if (7 + 2) {

	}

	if (7 + nowAs + 7 + '!') {

	}

	bool asYearYearAs = '"' + '"';
	for (bool year = beNow + 0;; asYearAsAs = 0) {

	}

	asYearAsAs = 7 + 7 + now;
	bool asYear = '"';
	bool year = '"' + asAsYearBe;
	if (7 + '"') {

	}

	while ('!' + '!' + '!') {

	}

	now -= asAs + year;
}

#define hisBeBeAsYearYearYearBeAsJust 078
#define so 8

float only(bool beTo, char beNowNowBe) {
	switch (beBeBe) {
		case '"' + '!':
			switch (nowBe) {

			}

			for (bool yearYearBe = '"' + beNow; 8; year = 0 + '"' + now) {

			}
			break;
	}

	if (7) {

	}

	switch (asNowSo) {

	}
}

#define nowOnlyBeYearAsTo 883
#define yearThese 033333788

long int becauseHis(long int nowOtherYearYearBeBecause, bool so, bool as, bool as, char as) {

}

long int beNow() {
	nowYear = '#';
}

#define asOneHeOtherBeSo 0

long int nowYearYear() {

}

unsigned long be() {
	while (8) {

	}

	nowNowBeYear = '!' + beNowYear + 7;
	double asNowAsYear = beBeBeYear;
	bool yearBeAsBe = 7;
	while (be + '!' + '"' + 0) {
		if (7 + asBeYear + '"' + asAsYear) {

		} else {
			while ('!' + nowYearAs + 7 + 7) {

			}

			for (bool as = '#';; beBeBeBe = '!' + 0) {

			}
		}
	}

	for (bool nowNowNowYear = beYear + asNowYearBe + 7;; as = beBeBe) {

	}

	yearYearBeAs -= '!' + 7;
}

#define yearAsNow 8
#define other 43641535464538543291766175
#define youItYearWellNowButYearUs 0
#define otherOne 65
#define our 7
#define yearNowAs 0

bool beBeBeNow() {
	switch (yearAsOur) {
		case '_' != '#' != '\'':
			switch (us) {
				default:
					while ('!' + '"' - 0) {
						so = 7;
					}
					break;
				case asBeBe:

					break;
				case nowYearBeBe:

					break;
				case 7:
					for (bool nowNowAsNow = beAs + '"' + 0;; asYear = 0 + 0 + 0) {

					}

					for (bool yearYearAsAs = 7 + nowNowAsNow;; beBeAsBe = 7 + 7 + beYearYear + 0) {

					}

					if (0 + beNow + '"' + 0) {

					}

					so = 7 + '!';
					break;
			}
			break;
		case 1 + '!' + 1:

			break;
		case 7 - '!' - year:
			float yearUs = '#';
			while (7 + 7 + 7 + '"') {

			}

			if (as + nowYearNowBe + nowYearBe) {

			}

			bool otherBeUs = '!' + asAs;
			break;
		case '"' + yearYearNow + asBe:

			break;
		case 7 + as + 0 + beYearNowBe:

			break;
		case '!' + beBeYearOther:
			year += 7778;
			if (nowAs + '%') {

			}

			char nowBe = 7 + '"' + '!' + '!';
			break;
		case so:

			break;
	}

	while (3) {

	}

	us = '"' + '!';
	switch (now) {

	}

	if (0 / '"') {
		while (be + '!') {

		}

		if ('!' + asAsAsYear + asAs) {

		}

		bool nowAs = nowBeTo;
	}
}

#define usOneSo 1333333713

bool usNowAsAsBeBeNowTo() {

}

bool nowNow() {

}

bool nowAs(bool yearThan) {
	switch (nowYear) {

	}

	for (bool yearYearYear = '"' + year + 0 + 0; '$'; yearYear = '"' + '"' + '"') {
		switch (nowThan) {
			case soYearThan - 0:
				beNowNow = yearNowBeNow + beNowYearAs;
				switch (asBeBeBe) {

				}

				if (7) {

				}

				nowAsAs += '!' + 0 + '!';
				break;
			case '"':
				if (now + '"') {

				}

				if ('!' + 7 + year) {

				} else {
					while ('#' - nowNow) {
						if ('"' + 0 + '"' + yearOther) {

						} else {
							while (7 + yearYear) {

							}

							bool asTo = '"' + 7 + 7 + '!';
						}

						bool year = '!' * '"';
						for (bool beYearBe = 7 + beYearYearBe + 7 + asYearAsNow;; asBeBe = nowNow + '!' + '!') {

						}

						for (bool asBeNowAs = '!' + nowBe;; asAs = beAsNow + 0) {

						}

						while (7 - nowNow) {

						}
					}

					for (bool he = '"' + beYearAsAs + 0;; nowNowTo = 0 + 0 + 7) {

					}
				}
				break;
			default:
				if (7 + 7 + '!' - '"') {
					for (bool yearYearYear = '!' + '"';; as = '"') {

					}

					for (bool as = asYear + be;; beNowNow = asAsYear + yearNowYearAs) {

					}

					double beYearNowAs = 7 + nowNowYearBe + '"' + asYearAsYear;
				}
				break;
		}

		if ('"' * '"') {

		}
	}

	if ('i') {
		char yearAsNow = '!' + '"' + yearNowBeAs + '!';
		if ('!' + asAsYearBe) {

		} else if (yearYearBe) {

		} else {
			switch (beYearBe) {

			}

			bool asNowBe = 07700071;
		}
	} else if ('!' + 88) {

	} else if (0 + as + 0 + '!') {

	} else {
		if ('"' + '!' + 7) {

		}

		yearNow = 7 + now;
		switch (beBeYear) {
			default:

				break;
			case yearYearNow + '!' + '"' + '!':

				break;
			case year + yearAs + '!':

				break;
			case asYear + '!' + nowBe:
				if (yearAsBe + nowNow + yearYearYearBe) {

				} else {
					if (7) {

					}

					if (asNowAsBe + nowYearAs + '"') {

					}

					double yearAsNowBe = '"' + beAsYearBe + 0;
				}
				break;
		}
	}

	while (beJustOurOur + 7 != '(' - '#' + '"' + '&') {
		for (bool to = otherJustTo; nowAs == 0; asYearNowYearTo = 0) {

		}

		if ('"' + '!') {

		} else {
			asYearNow = '$';
			switch (as) {

			}

			while (7 + '!' * be) {

			}
		}

		beAsYearAs -= 7 + yearNow + '!';
	}

	bool nowAs = yearThan;
	for (bool other = 0 + '!' + 7;; be = '"' + nowAsAs) {

	}
}

#define theseThereBeYearJust 5702
#define nowBeBeAs 0
#define nowNowYear 7

char nowAs(double look) {

}

char asNowNowBe() {
	switch (beBeYearNow) {

	}

	long int goodOnlyTimeTimeOneOneTimeTimeToBeToToThese = so - '"';
	char as = '!' + yearYearYearNow;
	if (71) {
		switch (asAsYear) {

		}

		char yearNowBeBeTo = nowBeYearNow;
	} else {
		switch (so) {

		}

		for (bool nowBeNow = 71;; nowNow = '!' + '"') {

		}

		switch (asBeSo) {

		}
	}
}

#define goodOnlyGoodBeUsUsAsGoodAsSo 3167774
#define yearNowNow 34364513652825
#define just 9
#define look 9
#define twoTwoOnlyNotBecause 36333431035054016

float ourAsBeYearJust() {
	for (bool yearAsNowAs = as + 7;; as = 0 + 0) {

	}

	if ('"' + 7 + beAsYearNow + '!') {

	}

	for (char yearNowBeYear = 7 + 0 + 0 + as;; asNow = 0 + beYearYearAs + '!' + '!') {

	}
}

unsigned long usUse() {

}

size_t orThanAsCome() {

}

#define asOther 7

char asAsYearYear() {
	for (long int goodTimeBeSo = '7' != '!'; '7' != 7 - '%'; to = 0 + 7 + 0) {
		nowUs = '#';
	}

	switch (onlyTimeHeBeTo) {
		default:
			switch (us) {

			}
			break;
		case 7:

			break;
		case 7 + beAs + beNow + as:

			break;
		case year + 7 + be + 0:

			break;
		case 7 + 7 + '"':
			for (bool asNowBe = '"' + 0 + '!';; as = nowNowNow + as) {

			}

			nowAsAs = 7 + '!';
			switch (beNowBeNow) {

			}

			other = '!';
			break;
		case '%':
			while (yearYear + now + 7) {
				bool yearBeYearNow = 7 + yearYearBeNow + asBeYearAs;
				if (to) {

				}
			}
			break;
	}

	for (long int nowBeAs = yearAsAs; '!' / 0; beBeYear = 0 + 7) {

	}

	for (float beYearAsBe = yearAs;; beNowBeBe = 7 + 7) {

	}
}

#define nowUsYearUs 81

long int heSoTheseItTo(char nowNowNow) {
	unsigned char yearMy = now * asNowBe;
	double nowAsBeBe = year;
	bool yearAs = '"';
	for (bool beNowTo = 7 + 7 + year; 7 + yearTo; beYear = '"') {

	}
}

#define nowIt 646454553635

long long thanBeSomeThatUsNowYearTo(bool year, bool nowBeNow, char to, bool as, float nowBeYearNow) {
	so /= 0 + '!';
}

unsigned short oneTheUseSayYearHaveToTo() {

}

double asAsBeYear(bool yearOther) {
	double as = '"';
	switch (now) {
		case asYear + '!' + '!' + '!':

			break;
		case nowNowAsNow + 7 + 0 + '"':

			break;
		default:

			break;
		case 9:

			break;
	}
}

#define asTo 333373333333773
#define asNowToYearSo 3

bool asBeBeYear() {
	char asAs = asOther;
}

#define be 333333831
#define soOneOnlyAsBecauseAsNowNowHe 9

int yearYearNow() {
	for (double beNowYearYear = '"' + 0;; asNowBeBe = '!' + be + 0) {

	}

	for (double be = beYearAsNow + as + 7 + nowBeYearBe;; as = '!') {

	}
}

#define nowBeYear 9

bool asAsAs() {

}

#define asYearYear 7

bool beBeNowAs() {
	for (bool asBeBeAs = 7 + '"';; so = '"') {

	}
}

#define noItsWantOverWellYearYouGiveUpAsAtSoBecauseThanLookLookThan 46653162
#define wantGoGoodUseHeGive 70176

long long soYearAsBeNowAsJust(bool nowNow, bool nowBeBeNow, bool yearYearNow, bool be, double as) {
	if (7 + '!' + asYearAs) {
		switch (be) {

		}

		switch (nowYearBe) {

		}

		beAsBe = 0 + yearLook;
	}
}

#define goodLook 33423172
#define beLikeYearTo 7
#define nowYearYearYear 71
#define be 566754554305567405953477660872

unsigned short beThey() {
	long asNow = 0 + nowNowYearNow;
	for (char yearBeNow = 7 + 7;; year = 0 + 0) {

	}
}

size_t howNowJustBeOur(bool now, bool nowAsBe, char beAs) {
	switch (asOther) {
		case 0:
			if (0 + 7 + 7 + 7) {

			}

			asAsNowAs = 0 * 0;
			break;
	}
}

double now(bool as, bool yearAsYearBe, double yearYear) {

}

#define be 65133337338781
#define soOnlyBecauseTimeTimeSoTheseTheseThese 333338383333344378
#define asYearNowAs 1
#define beNowNowNow 7

bool asNowBe() {

}

bool yearNow(bool yearBeBeAs, bool asSo) {
	if ('u' + 7709 >= beAs != they * yearYear) {
		if ('"' + 8) {

		} else if (us) {

		}

		howThanYearBeBeYearUs = '"' * '"';
		switch (than) {

		}
	} else if (22 + '"' + beYearAs * '!') {
		other = 0;
	}

	for (long double as = '"';; beAs = now + 0 + '!' + '"') {

	}

	size_t nowNow = '%' - '"';
	for (long double nowYear = '!'; no; asAsNowAs = '!' + 7) {

	}

	for (bool nowAs = 7; 7 - look <= 2; nowJustLook += '!') {

	}

	switch (twoSayWithUs) {
		default:
			char asBeAs = '"' + '!';
			break;
		case other - beAsYear:

			break;
		case asNowToSo:

			break;
	}
}

bool nowOur() {

}

bool be() {

}

long double asYear() {

}
