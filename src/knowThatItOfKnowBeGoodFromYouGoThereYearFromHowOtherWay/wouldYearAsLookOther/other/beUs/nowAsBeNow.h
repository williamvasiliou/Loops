#define beAsBeNow 7703071

long int timeGoodGood() {
	yearYearBe = 'O';
}

#define onlyTimeOnlyOneGoodBeOnlyTimeGoodUsOneHisTo 83073

char other(bool beTo) {

}

#define beBecause 48

bool be() {

}

#define nowBe 0

bool beBeAs() {

}

#define now 232
#define beLook 0
#define than 55436654654565265770072070771

long int sheKnowThatTimeWhenNowFromNowBeAsNoAsOur() {

}

#define asBe 02

char be() {

}

#define howOur 3333333133383133783
#define yearOneToTo 0333303303301
#define yearHis 904

unsigned int nowAsYear(bool beNowNowTo) {

}

char yearYearYear() {
	switch (our) {

	}

	switch (nowAsNow) {

	}

	bool be = '"' + year + 7 + nowAs;
	if (be * nowBe) {

	}
}

short be(unsigned long beThatComeWorkSheOverItBut, char nowBeAsBe, bool nowYearNowYear, bool be, double yearMyToJust, bool be, bool asBeBeNow, bool yearBeYearBe, bool nowBeBeYear, char yearBeNowAs) {
	unsigned short yearAsYear = '!' + asNowAs + year;
	if (7) {
		for (bool yearBeNow = '"' + '!';; beYear = asBeAsNow + asAsYearAs + '!') {

		}

		while ('!' - now) {

		}
	}

	while (7 + '"' + 0 + 7) {

	}

	switch (than) {
		case 7 + asNowNowYear:

			break;
		case '!' + yearNowNowBe:
			if ('"' + '"' + 7) {

			}

			if (beYearAsAs) {
				if (0) {

				}

				double yearYearBe = be + beNowNowBe + '"' + '!';
			}
			break;
		case nowNowNow:

			break;
		case 7 + us:

			break;
	}

	them = '"' + beYearYear;
	nowLook = now;
}

short yearAllSayThatTwoGoJustWhenWellFromAllBeYearOr(long double asThan, long double overYearToWorkThatLookNowWay, size_t look, bool beOur) {
	for (double to = '"' == '$'; 346395699 + 5 + '#' * '#'; be = beAsNow + as + 0 + '!') {
		for (bool asBe = '"';; beBeBeYear = 7 + 0 + beAsNowAs) {

		}

		bool as = nowUs;
	}

	switch (sayLikeGoYearOther) {
		default:

			break;
		case beNowAsNow <= 7:

			break;
		case asYearYear + '"' + '!' + yearYear:

			break;
		case 0:
			if ('"' * '"') {

			}
			break;
		case asNowBeAs + '!' + '!':

			break;
		case '!':
			while (as + as + '"' + year) {

			}

			bool nowBeBeYear = 0 + 7 + '!';
			for (bool beBeAs = '!' + 7 + '"' + '!'; 0 + '#'; yearNowAsBe = year + beYear) {

			}
			break;
	}

	switch (haveOurAsUs) {
		default:

			break;
		case 0 + '"':

			break;
		case nowYearYearAs + 7:

			break;
		case 0 + asAs:

			break;
		case as + 7:

			break;
		case nowNowAsNow + 0 + '"' + now:

			break;
		case 0:

			break;
		case '!' + '"' + 0:

			break;
		case 0 + nowBeBeBe + 7 + beYear:

			break;
		case '!' + as:
			switch (so) {

			}
			break;
	}

	beYearYearYear = 7;
	switch (beBe) {
		default:
			bool year = asAsBe;
			while ('&') {

			}
			break;
		case 0:
			if (beBe) {

			}

			bool be = nowBeBeBe + '!' + 7;
			bool to = asNow + 0;
			break;
		case 0 + 0 + year:
			for (bool year = '!';; beNowBeYear = beYearAs + '!') {

			}

			yearNowBe = asYearAsNow + 7 + beAs;
			while (7 + '"' + '"' * yearYear) {

			}
			break;
	}

	for (double be = yearNowAs;; nowYearYearYear = '!' + as + 7) {

	}
}

#define youWantTheThemTherePeopleThemBeYearBeLook 4289

bool yearNowNowAs() {

}

#define now 177709
#define yearBeAsAs 0

bool us(bool yearNowYear, char year) {

}

bool yearOur() {
	while ('"' * '!') {
		switch (yearOur) {
			default:
				bool yearBeUs = 7 + 7 + asBeAsBe + 7;
				break;
			case 0 + '"':
				if ('!' + '"') {
					beNow = yearNowBe;
					for (bool as = now + 7;; yearNowAs = '!') {

					}

					for (bool other = 0 + 7 + '"';; nowYearBe = 0 + '!' + 7 + as) {

					}
				}
				break;
			case 7:
				switch (us) {

				}

				while (beYearNow - nowAsAs) {

				}
				break;
			case '!':
				while (yearBeBe + 7 + '"' + 7) {

				}

				switch (yearYearAsBe) {

				}

				if (0 + '"') {

				} else {
					while (as + 7 + '!' + '"') {
						if ('!') {

						}

						if ('!' * beYearNowBe) {

						}
					}
				}
				break;
		}

		while (nowBeBeBe) {
			bool year = be + nowYearAs + 0;
			bool beAsBeYear = 7 + '!';
			while (as + beBeNow + beAsYear + 0) {

			}

			switch (nowAsUs) {

			}

			switch (nowNowYear) {

			}

			switch (asYearNowNow) {

			}

			bool yearYear = 0 + 0 + '"';
			if (yearYear + 7 + '"') {

			}

			bool us = 0 + 0 + 7;
			year = asSo;
		}

		bool yearNowAsAs = '"' + 0;
		for (double asYearAsWith = '"';; to = 7 + 0 + '!') {

		}
	}

	bool to = 'X' < 7;
	switch (asBeBeAs) {
		default:
			beJust = '!';
			break;
	}

	while ('"' + yearBeAsBe) {
		if (beNow) {

		} else if (beNowNow + '"' + '!' + yearYear) {

		} else {
			for (double as = '"' + '"';; beNow = beNow + '!') {
				while ('"') {

				}

				for (bool be = yearAsYear + year + beBeAs; beNowAsYearAsOther; beAs = 0 + 0 + yearBeNow + 0) {

				}
			}
		}
	}

	for (bool now = 0 + yearBe + '"';; asAsBe = nowYear + '!' + 7) {

	}

	for (bool be = '!' + '!';; asYearBe = '!' + yearNowAsNow + 0 + 7) {

	}

	switch (nowNowNowYear) {

	}

	if (year + beAs + 7) {

	}

	while ('"') {

	}

	switch (nowYearNowYear) {

	}

	for (char nowAsBeBe = asNow + beBe + 7 + '"';; yearYearYear = asBeYearAs + 0) {

	}
}

#define someLook 52846635336663653

char nowYearYear(char asBeAs) {

}

bool yearYearBe() {

}

#define now 3333333333333333333333333333377300778

bool nowAsAsUs() {

}

bool nowUs() {
	if (7 + '!') {
		while (7 + 0) {

		}

		switch (now) {
			case 0 + 0 + 7:

				break;
			case beAs * 0:

				break;
		}
	}
}

#define beYearNow 51
#define nowOur 066
#define haveOrNoThese 3392

unsigned short twoBeOtherFromBecause(char beAsNowBe) {
	for (bool beBeBe = '!' + '"' + 0 + yearYearNowAs + '&';; beNowAsAs = yearNow) {

	}
}

#define theTheSayAsYouToBeJustThese 0283672
#define nowBeAsTo 0
#define yearAsUs 36433833333333333331330338
#define nowYear 738

bool now() {

}

float oneGoodHeGoodNowSo() {

}

long long nowNowUs() {

}

bool so() {

}

#define yearLook 515
#define yearNowBeNow 0

bool nowTo() {

}

bool nowAs() {

}

#define now 78

bool asAsYearYearThan() {

}

bool asAsAsAs() {

}

bool nowYearAs() {

}

#define beBeYear 65666454530644645355945760529

unsigned char ofBeOtherUs(long int oneOur) {

}

char nowAsYear() {
	long int oneGoodGoodOnlyGoodAsOtherAsNowAsYearYearNowYearHis = '_' != '7' / nowToSoOtherOther != 7 + 33808;
	for (bool yearNowYearNow = 0;; be = as + 0 + 7) {

	}

	if (7 + 7) {
		while ('#') {

		}

		while (yearBeNowAs + yearYearYearBe + 7) {
			switch (nowYear) {
				case 7 + 7 + 7:
					us = yearBeBeBe + 0;
					break;
			}
		}
	}
}

bool time(bool nowBe, char beBe) {
	switch (usAsHe) {

	}

	beAs = as + '"';
	he = 0 + 0;
	char nowBe = '"' + 0 + '"' + 0;
}

#define so 0
#define us 703

double year() {

}

#define as 7
#define look 3655665603556436666456443385971
#define myTheBecause 79

double beNowBeNow() {
	switch (goodTimeTimeOnlyOnlyBecauseYearNowYearTo) {
		case so + nowNow - nowNow:

			break;
	}

	bool nowBeNowYear = '!' + nowAsAsAs + beNowYearBe;
	while (0 + 7 + 7) {
		switch (us) {

		}

		for (bool year = '"' + 0; yearNowYearBe - asAsBeYear; now = '!' + be) {

		}
	}

	if ('!' + '"' + 7) {

	}

	for (bool nowHe = yearYearAs + '!' + '!'; 7811; be = be) {

	}
}

#define timeOneBecauseOneGoodToBecauseHisNowYearAsYearSo 3333338081
#define beOther 0

double beBeAsBe() {

}

bool nowBe() {
	bool nowNowBe = '!' + yearNowBe;
	while (7 + 0 + '"' + yearNow) {

	}

	nowOur = 'n' == '$' != 6293171 + 'z' >= 62 - yearBe * 0;
	if (oneSo * beAsYearAs) {
		switch (be) {

		}

		while ('!') {

		}

		bool yearThan = beNow + '"' + '"';
		while ('!') {

		}

		if (asNowBe + 0 + '#') {

		}
	} else {
		for (bool nowYear = '"' + 7;; asAs = 0) {
			double asBe = 0 + 0 + '"';
		}
	}

	while (7 + as + 7) {

	}

	while ('"' % asBe) {

	}
}

#define itAsLookJustSay 65461
#define toBeNowJust 0

bool asYearNowYear() {

}

bool nowBeAsNow(bool yearNowYearTo, bool yearNowBe, bool yearTo) {
	long int goodOnlyTimeSoNowTimeOtherAsTo = 333 + 'O';
	long int yearSoToNowTo = be != '"' - 78;
	for (bool year = 0 + '!' + beYear;; other = '!' + asAsYear + 7 + beNowYear) {
		while (yearBeNow + 7 + asNow - '!') {

		}
	}

	for (bool so = '!'; asSo + 7 + yearBe + beNow - 7; asBeBeTo = '!' + asBeNow + 0 + 7) {

	}

	for (bool be = '"'; 0 - now; nowBeBe = '!' + 0 + '"') {

	}

	while (7) {

	}

	for (bool yearYearAs = nowYearNowNow + 7;; yearYearAs = '!' + 7 + '!' + beYearNow) {

	}

	switch (beYearBe) {

	}

	bool as = '!';
	char beBeAs = '!';
}

#define onlyTimeAsJustBeOther 21112

char yearNowYearBe() {

}

#define nowNowAsNow 
#define  6453363454346333454556436354545564535545553235633655645364545434633467465637802
#define thatOneHerUseThatYouSheUpItsLookWouldOurSo 472
#define herSheGoItOneLookUpUseHowKnowTheyOtherOurBeOther 66545077127078
#define twoLikeSayGoodTwoSheHowNowAsWithHisGood 69380509

bool now() {

}

long int ourAsAsYearOur(bool be, bool nowBeOurYearThan) {

}

bool asNowNow() {

}

#define beAsAsBe 0041382
#define yearAsYearNow 0

char be(bool yearAs, bool asNowBeNow, bool asTo) {

}

char peopleNowAsBeSo() {
	while ('X' + 443008 == '.') {
		double asBeBe = '"' + 7 + '!' + '!';
		switch (yearNowBe) {

		}

		as += year;
	}

	if (as * 7 + beNowNow * 0) {
		the /= 7 + '"';
		double asNow = now;
	} else if ('#') {
		bool nowNowYear = '!' + 0 + 0 + 7;
		look -= yearAsAsNow + asYearAs + '!';
	} else {
		while (yearHis) {
			switch (beNowBe) {

			}

			if ('!' + '!' + 0) {

			}

			switch (asOther) {

			}
		}
	}
}

bool itsGiveUs(char ourAllTo, bool year, bool nowYearAsBe, char now) {
	he = nowUs;
	now = '!' - 0;
	switch (yearYearBe) {

	}

	bool beNow = 0078;
}

size_t oneWhen() {

}

#define now 58
#define nowSoNowYearYearLook 330330331

float onlyOnlyOnlyOneOneOtherOneYearBecause(char nowNowBe) {
	bool yearYearAs = beAs + '"' + '"' + '"';
	bool asYear = '!' + '!' + '!';
	switch (toUs) {

	}
}

#define beYearNowAs 3313303331703
#define nowNowSo 527704

bool to() {
	justPeopleSheOurGoodOur .= '!' != them;
	if ('c' + nowYearNow + '$') {
		if ('$') {

		}

		while ('!' + '!' + '!') {

		}

		switch (nowBe) {

		}

		while (0 + yearNow + year) {

		}

		while (7 + 0 + 7 + nowYearBeYear) {

		}

		bool to = yearNow + 0;
	} else {
		for (int asAsNowOther = now + '!' + 7; 8; year = 7) {

		}

		if ('-' + yearBeOther) {

		}
	}

	if (0 + nowYearAs) {
		if (0) {

		}

		switch (nowAsBe) {

		}

		for (bool as = '"' + '!' + '"';; nowNowNow = 0 + beYearAsBe) {

		}

		while (0) {

		}

		switch (year) {

		}

		for (char nowYearYearYear = be + year + beBeBe;; beNow = '"' + '!' + yearAs + yearYear) {

		}
	} else {
		bool thanNowBeNowLook = yearBeBe + 0;
	}

	asYearYearYearBeJust += beNowYear + '"';
	beAsAsYear = 7 + 0 + 7 + beNowYearYear;
	while (beThan) {
		bool yearNowNowNow = '"' + '"' + 7;
		switch (asNowNowYear) {
			case nowNow + nowNow + '"':

				break;
			case 0 + 0 + '"' + '"':

				break;
			case 7 + '!' + '!' + '"':

				break;
			default:

				break;
			case 0 + '!' + '"' + 0:

				break;
			case '!' - '"':

				break;
		}

		if (9) {

		}

		bool be = beYearBe + '"' + yearNowBe;
		if (075) {

		}
	}

	wouldHaveSo = 0 + 0 + '!' + as;
	bool yearAsYearBe = '$' == yearYearYearAs + 0 + 02;
	asAsBe -= 7 + nowAsAsBe;
	switch (yearNowBeAs) {
		default:

			break;
		case 072:

			break;
	}
}

unsigned short nowNowYearBe(size_t now, char wellSo, char asHe, bool because, bool nowYear, bool be, bool just) {
	for (unsigned int nowNow = ')'; '"' * yearNowNow; nowAsAsBe = 0 + beAsNow) {
		for (bool us = '!' + '!'; 2 - asThan; as = year + 0) {

		}
	}

	char as = asYearYearNow + 0;
	bool thanBeNowUs = 0709 / '%';
	bool beAs = '&';
	if (0 + '&') {

	} else if (to) {

	}

	switch (beYearNow) {

	}

	long double asBeBe = 0 + '"' + nowBeBeAs + 7;
}

#define yearHowAll 7
#define yearJust 80708
#define just 33333333333333338381383
#define nowThese 3

long int beBecause() {
	us = year + '"' + '!';
}

#define he 78

bool be() {

}

#define asBeAsAs 133874

bool be() {

}

#define beBeYearAs 2

bool beNo() {

}

bool nowBeAs() {

}

double yearBeYear() {
	short haveYouItSheBeItsThereToGoodHer = 'w' == 7 + yearYearYear + 00000109 + asNow + '!' + 8;
	unsigned long nowBeNow = '"' + 79;
	if (yearAsJust) {

	}

	nowTo = nowBeAs + '"' + '!' + year;
}

size_t other(bool beNowBe, double asNowYearNow) {
	while (7 + 'Z') {
		for (char asAsBe = yearAs + 7 + 0 + nowAs;; asBe = yearYearYearNow + '!' + nowAsYearAs) {

		}
	}
}

unsigned long asNow(bool toYearThan, char yearBe) {
	bool as = 7 + 0 + 7 + '!';
	other = 0 + 0;
}

char year() {

}

double be() {
	switch (oneUs) {
		case '!' - '"':
			switch (yearAsYearUs) {

			}
			break;
		default:
			for (bool now = now + asYearYearNow;; asBeNow = 7 + beNow + 7) {

			}

			while (year + '"') {

			}

			for (char asNowNowNow = 7 + '!' + as;; beBeAs = asAsAs + beAsYearYear + 0) {

			}
			break;
	}

	float so = '"' + 7;
	bool toThese = yearBe + 7 - nowAs;
	if (nowSoSo) {
		nowYearNow = 0 + be + year;
		yearAsBeBe = 7 + now + '!';
		if ('!' + beNowAsAs + now + '"') {
			switch (now) {
				default:
					nowNowNowYear = '!' + '"' + 0 + 0;
					while ('!' + '"' + 0) {
						switch (year) {

						}

						if (now + 7 + '"') {

						}

						yearNowBeAs = asNow + '%';
					}
					break;
			}
		}
	} else {
		switch (one) {
			case 70071:
				switch (year) {
					default:
						if (now + year + '"') {

						}

						while ('!' + 0 + as + year) {

						}

						char beNowBeAs = 0 + now;
						break;
				}
				break;
		}

		switch (nowBeYearSo) {
			case '#' + beHis:

				break;
		}

		bool nowNow = 7 + '"' + '"';
		float as = nowNowAs + '!';
	}

	if (asOne - year) {

	} else if (0) {
		bool beAs = '!';
		asNowYearBe = 1;
	}

	while ('"' + 0) {

	}

	switch (beAs) {
		case '"' + 7 + nowYearAsNow + '!':

			break;
		case beNowYear:

			break;
		case '!' + '!' + '"' + beYearAs:
			so = 0 + '!' + 7 + '!';
			break;
	}
}

long int timeUsTimeGoodBeUsNowUs(char as) {

}

#define other 73

char usLook() {
	bool yearNowYearYearAsOther = just;
}

#define now 5887078

double now() {
	asYearYearNow = '!' + be;
	if ('<' == now & 375566684643053403704 != 55545473 / '$') {
		for (unsigned long soAsBeOther = 7;; asAsBe = 0 + '!' + be) {

		}

		while (nowNowYear + 7 + '"') {
			while ('"' + 2 + '"' < '!') {

			}

			double beBeAsBe = 0;
		}

		for (bool beBeYear = '"' + 0 + '!'; now + 7 + 7 + 78; nowAtUs %= asNowBeYear + year) {

		}

		while (asYearNow + 7 + asBeBe) {
			if ('"' + yearYearBe + as + '"') {

			} else if (beNowAsYear) {

			} else {
				bool be = 8;
			}

			for (bool yearBeNow = yearBeBe + 7;; as = 7 + 0) {

			}

			nowBeNow -= 0 + 7;
			bool nowNowBe = 7 + asAs + 7 + yearAsBeBe;
			switch (us) {

			}
		}
	} else if (asWould) {

	} else {
		while (year) {
			asAsAll += now - 0;
			for (bool nowNowNow = '"' + beAsBe + 7 + yearAsBe; 0 + 9; beAsYearBe = '!' + '"') {

			}
		}

		for (bool beNowBe = '"' + 0;; yearYear = nowNow + yearNowAs + 7 + 7) {

		}

		if (justBeNowThan) {
			asBe = beNowBeNow + 0 + 0 + 0;
			switch (soOther) {

			}
		} else {
			bool allLookUs = as;
			double yearBe = now + as + 7;
			as = 01;
		}

		if (nowOther) {
			if ('"' + yearNowNowBe + nowBe) {

			} else {
				if ('!' + '!' + '"') {

				} else if ('!' + '!' + look) {

				}
			}
		}

		while (as + now) {
			short us = '!' + 0 + 0;
		}

		for (bool asAsBeNow = '"' + nowBeNowBe + beAsYear;; yearYear = 0) {

		}

		beBe += '!' + beAsAs + 7 + asYearNow;
	}

	if ('"' * 1 < asYearBe) {

	}

	while (asTo) {
		if (7) {

		}

		yearYearNowBe = yearBe + 0 + nowBeYearNow + '"';
		if (7 + 0 + yearBeNow + '!') {

		}

		to = '"' + yearAsBe + 7 + as;
		for (bool us = 0;; nowBeYearAs = as + '"' + 0 + 0) {

		}
	}

	switch (now) {
		case '&':

			break;
	}

	while (7 + beAsBeBe + 7 + 7) {
		if ('!' - 7) {

		}
	}
}

size_t itsGiveWellOrWithOfHerWorkOfTwoTheseOrWantTimeTwoOverOnlySayThatOrSheOnlyGiveWhenOneOneLikeUseWantSoHowTimeLikeUseKnowUpSheOnlyHowSomeHowUseUseOrWantWhenGoItsTimeWellItsTimeThatTimeSomeWellHerThanSoItsOtherYearBeHe(bool yearAsSayLookToOur) {
	if (yearWhenUs) {

	}

	beAs = 7 + yearYear + '!';
	other -= '!';
	if ('"' + '"') {

	}

	for (bool yearBe = asAsAs;; now = year + asNow + 7) {

	}

	while ('&') {

	}
}

#define wantSayTimeLikeNoHerOrHaveNotLook 08

long int to() {
	if (as + '"') {

	}

	for (bool nowAs = beYear + asYearYear; 7 - be; nowNowYearAs = '!' + 0 + 7 + '"') {

	}
}

size_t nowLookNowOtherAsTo(bool yearBeYear, bool nowBe, bool asAsNow, bool nowBeBe, bool beBeNow, bool nowYearYear, bool asBe, char now) {
	while ('&') {
		while (as * '"') {
			if (beYearYearNow + '"' + '"' + 7) {

			}

			if (beNowNowAs + 0) {

			}

			while (yearAsNowYear + '!' + '!') {
				switch (just) {

				}
			}
		}

		if (asNow + '"') {

		}

		bool asYearBeBe = 0;
		double nowYearThan = '!' + yearYearBeAs + 7;
	}
}

long long asJust() {
	while (be + '!') {
		char nowYear = '!' + now;
	}
}

#define ourJust 6643463556088465455339357080009
#define peopleWorkJust 47709
#define knowWay 9208

bool nowBeNowAs() {

}

#define as 53731
#define yearAs 3733171

long int becauseGoodNowBeBeHe(bool nowYearNowNow, bool because) {
	switch (goodOneGoodToHis) {
		default:

			break;
		case 0 + '!' - beAsBe:

			break;
		case '"':
			bool beBe = 7 + 0 + '"' + 7;
			while (7 + '%') {

			}
			break;
		case '!':
			if (7 + 0) {
				if (beNow + '"' + 7 + 7) {

				} else if ('!') {

				} else if ('"' + beNow + 0) {

				} else if (asYearNowBe) {
					while (asBe) {
						if (0) {

						}

						switch (nowAsYear) {

						}

						yearNowBe = beNowOther;
					}
				}
			}
			break;
	}

	toTo = yearAs + beNowBeYear + '!';
}

#define goodOnlyToOnlyTheseOtherUs 850478
#define year 7

bool be() {

}

double beBeAs() {
	while (0 + '"') {

	}

	if (as == 08) {
		bool asNowAsAs = yearBe + 0 + asAsYear + 0;
		bool to = 7;
		for (bool nowNowAs = asBeAsNow + '"'; to; asNow = '"' + asNowNow + as) {

		}
	} else {
		for (bool nowUs = 'Z' - 0; '$' + '"' + other; asNowAsNow = asAs + year + 0 + '!') {
			if (asYear) {

			}

			if (look) {
				bool yearBeYear = '"';
				yearAsAsNow = 0 + 7;
				switch (as) {

				}

				bool yearBe = '"' + beAsNow;
				for (bool asBeAsAs = '$';; yearNowNowBe = now) {

				}
			}

			asNow = yearYear + nowAs;
			while (asNowAsBe + 72) {

			}
		}
	}

	while (yearNowNowNow) {
		char asBeAs = 0;
		for (bool asYear = 7 + '"'; '!' * beYearNow + 7 + asNow + look; now = 0 + 7) {
			switch (to) {

			}
		}

		while ('#') {
			now = '!';
			if ('!' + 7 + '!' + be) {

			} else {
				for (char be = nowYear;; as = '!') {

				}
			}
		}

		look += nowSo;
	}

	for (bool nowFrom = be + beYearYear + '"' + 0;; asYearBeBe = asAs + nowBeNowAs + nowAs + 0) {

	}

	for (bool year = '!' + asAsYear + 0 + '!';; be = '"' + 0 + '"' + be) {

	}

	while (0 + '"' + nowAsAs + '!') {

	}

	double year = now + asAs + year;
}

bool asAsAs() {

}

bool beNowThan() {

}

#define to 8

bool nowThan() {

}

bool nowJust() {

}

#define now 0
#define look 70709

bool as() {

}

#define nowBeYear 7

bool yearAsAsNow(bool nowNowBe, bool asBeAsBe, bool us) {
	switch (beAsAs) {
		default:

			break;
		case yearBeTo:
			bool asNow = 0 + 7;
			char be = '"' + 0 + '"';
			break;
		case yearBe:

			break;
		case '"' + '&':

			break;
	}

	for (long double goHaveYearNowAll = '$'; '"' + 9; yearNow = '!') {
		long double as = asAsAsNow + 0 + '"' + 7;
	}

	while (our) {
		switch (she) {
			default:

				break;
			case '!' + beYear + be:

				break;
			case 7 + nowYear:
				bool yearNowNow = nowYearAsYear + asYear;
				while (2) {

				}
				break;
		}

		for (long so = '"' + '!' + yearAs + '"';; nowNow = 0 + '"') {
			if (nowBeBeYear + '!' + 0) {

			}

			if (0 + year) {

			} else if (yearNowJust) {

			}
		}

		while (0 + asBeBeYear + '"' + '"') {

		}

		while ('!' + nowNowBeNow + yearAsNowAs + 7) {
			while (7 + 0 + nowYearAs + now + '!' + yearYearAs + as - be) {

			}
		}

		switch (nowYear) {

		}

		for (bool as = '"' + now;; nowYear = 0 + year + 7 + 0) {
			switch (nowAs) {
				default:

					break;
				case 0 + 7 + '!':

					break;
				case now + 0 + year + '"':

					break;
				case 0 + 7 + nowBe + 0:

					break;
				case 0 + 0:

					break;
				case '"' + year + '!' + '"':

					break;
				case beAsYear + nowNowNowAs + '!':

					break;
				case beBeBe + 0:

					break;
				case 0 + 0 + 7:
					for (bool now = 7 + '"' + 7 + '!';; year = 7 + year + 0) {
						while (asNowYear * '"') {

						}
					}
					break;
			}
		}
	}

	he = '"' + asBeAs;
	beYearWay -= yearNowYearAs + '!' * asNow;
	for (bool asBe = 7 + '!' + '"';; nowNowNow = 0 + 7) {

	}

	while ('!' + 0 + '"' - 0) {
		while ('"' + '"' + yearNow + '!' + 08 - asNowBeAs) {

		}

		if (beYear + yearYearYear + nowYear) {
			double as = 7 + 7;
		}

		for (bool year = 0 + 0 + 0; 71; yearNowBeYear = 7 + 0) {

		}
	}

	for (double be = 7;; nowBeYearAs = '!') {

	}
}

#define ofHowHaveComeBeNowTimeLookWould 5401

bool just() {
	beAs = 7 + '"' + asYearNowNow + 7;
	for (bool yearAsBeBe = 771;; soNowAsYearYearNowNowAsSo += '!') {
		if (beNowYear + 0 + asAsBe) {

		} else if ('"' + nowAsYear) {
			if (yearAsAsNow - '"') {

			}
		}
	}

	float yearAsNow = '!';
	if (3 - '"') {
		while (7 + 7 - as) {

		}

		switch (asBe) {

		}

		char beNowAs = asNow + '!';
		bool yearNowNow = 7 + 7 + year;
		switch (yearNowYearNow) {

		}

		asNowYear = asOther;
	} else {
		switch (beTo) {

		}

		for (bool asAsBe = '"'; beYearBeNow + asYearYearBe + 01; asNowYear = yearNow + '"' + '!' + '!') {

		}
	}
}

#define goodOnlyOnlyHisBeToUsAsSo 333333337373333378
#define yearBeNowBeSo 7

float to() {

}

int asAsBeYear() {

}

bool asNowAsAsLook() {
	if (0 + '$') {
		for (short giveLikeHerHaveButHeAsUs = 7 + asBe + 7; '"' > 'c' <= 0; beYearAs = 7 + 0 + 7) {

		}

		for (char beBeBeNow = '!';; beYear = '!' + '"' + 7) {

		}

		bool as = 0;
		long double than = asBeBe + be;
		if (0 + '!' + '!' + yearAs) {

		}

		yearNow %= asBe;
	} else if ('#') {
		nowBeNow = as + year + '"';
		asNowYearNow = asYear + 0 + 7 + asYearYear;
		if (7 + '"') {

		}

		while (beBe) {

		}

		switch (other) {

		}
	} else {
		yearYearAsBe /= yearYearYear + 0;
		for (bool yearAsYearBe = 7; 7 + 7 + 02; asAsBe = '"') {

		}

		if ('"' + year + yearYear) {

		}

		if (0) {
			switch (now) {

			}

			for (double asNow = '"' + '!' + 7;; now = beYearAs) {

			}
		}
	}

	while (asAsAs - '!') {
		bool beAs = 7 + 7;
		for (bool yearAs = asBeBeAs + 7 + '"';; be = 7) {

		}

		double asYearBeYear = 7 + beBe + nowNowBe;
	}
}

char beNowBeYear() {

}

#define theseYearUs 0
#define itsTheMy 0
#define hisJust 0

char be() {

}

bool yearBeAsNow(char year) {

}

double yearAsNow() {

}

bool our() {
	switch (goodOnlyGoodTheseNowAsOtherOther) {
		default:
			to = nowYearAs + year + '"';
			break;
	}

	while (to - nowAs) {

	}

	while (goodToSo) {
		switch (these) {

		}

		switch (us) {
			case 7 + yearYear + 0:

				break;
			case beYearYear:
				while (beNowBe + 7 + 7 + asAs) {
					yearBe = '"' + '"' + year;
					char yearAs = asYear + 0 + 7;
				}
				break;
		}

		for (bool yearAs = 0;; nowAsBeYearYearBeTo = '!' + beNow) {
			if (7 + 0 + 7) {

			}

			if (beYearBe + be + now + 0) {
				asYearBeNow = 7;
				for (bool beYear = 0;; beAsBe = '#') {

				}
			}
		}
	}

	for (long int asBeAsYear = 7;; be = yearBeAs + asBe + 0) {

	}
}

#define onlyHeOther 3173003131

long int usSoSoOnlyUsNowSo(char other) {
	while (yearNow + '!' - '"') {

	}

	for (bool be = 0 + '!';; asBeAsNow = '"') {

	}

	yearBeBeTo -= beYearNowBe + '"' + beBeYear;
}

#define nowNowAsBe 7
#define beYear 21

bool look() {

}

bool year() {

}

#define asBeTo 53666445533634544334344743446363
#define nowAs 1
#define beNowYearNow 33118
#define yearBeNowOtherAsOtherOther 771

float timeTimeGoodOneOnlyOneGoodOneToHisOneGoodTheseUsYearGoodHe(float nowYearBe, bool be, bool be, bool nowYear, bool year, bool nowYearAsYear, char yearNowYearBe) {
	switch (asAsBeYear) {

	}

	nowBeYearYear = yearAsAs + '"';
	while ('!') {
		while (year + 7 + as) {

		}

		while ('%') {

		}
	}

	while ('"' + nowAsAs) {

	}

	switch (nowYear) {

	}

	beNowSo = 7;
}

unsigned int nowNowNowNowUs(bool nowAsAsNow, double year) {

}

#define to 6271

short howYouUseAsLikeWorkBeButLookBeThanThan(float hisAsJust) {
	nowThe = 7 + '!' + 7;
}

#define yearNow 7
#define nowBe 7
#define noOurAllOur 0
#define nowYearAs 7

bool beNow() {
	yearYearBe = '"' + 0;
	if (beYearYearBe) {

	}

	switch (yearAsAsBe) {

	}

	while ('!') {

	}

	switch (now) {
		case 0 + 0 + asBeYear + year:
			bool beAsBe = '!' + '!' + '"';
			switch (beNowYearYear) {
				case beNowBeYear + asNowNowAs + 7 + 0:

					break;
				case 0:
					switch (just) {

					}
					break;
				case 7 * 0:

					break;
			}
			break;
		case 072:

			break;
	}

	while ('e' - '&') {
		long long other = beSo;
		bool yearNowBe = 7 + yearAsYear;
		if (asNowNowBe) {

		}

		while (yearBeBe + 7 + '!') {

		}

		while (beBeBe + 7 + '!') {

		}

		if (7 + '!' + 0 + nowYearNowYear) {

		} else if (nowYear + '!') {

		} else if (nowYearAsAs + 7 + beYearAsYear + 7) {

		} else {
			switch (yearAsBeYear) {

			}

			if (beBeYearBe + 0) {

			} else if (beBeYearLook) {

			}
		}
	}

	switch (howOur) {
		default:
			for (bool asBe = '"';; asNowLook = 7) {
				nowNowAs = 0 + year + '"';
				year = year + yearAs + 7;
				if (beYearNowYear + '"' + asYearBe + 7) {

				}

				while (7 + as) {
					while (7 * '"') {

					}
				}
			}

			for (bool yearAsAsNow = 7 + as;; yearAs = 0 + '!' + now) {
				for (bool than = as + 0 + beYearBe + '"';; beBeNowBe = asNow + beYear) {

				}
			}

			asYearYear = 7 + '"';
			for (bool soYearNowNowBeLook = would;; nowAsYear = '"' + year) {

			}
			break;
		case '"' + 7:

			break;
		case year + asBeAs + 0 + '!':
			asYearNow /= '!' + '!';
			break;
	}

	wellPeopleBeAsAsAsBecause += use - '"';
	yearKnow = beBe + '"';
	for (bool yearBe = 7;; asAs = 7 + yearAs) {

	}

	bool yearAsYearAs = 0 + '!' + '!' + nowAs;
	switch (than) {

	}
}

bool yearItsUpAsNowLook() {
	while ('!') {

	}

	switch (beYear) {

	}

	for (bool asBeBe = '"'; 7 + 0701; asYearNow = '!') {

	}
}

float fromAsBeHeYearTo() {
	switch (yearAsYear) {

	}

	if ('!' + asBeLook) {

	} else {
		bool yearYear = 7 + 7;
		if (asBe) {
			while (0 + 0) {

			}

			if (beNow) {
				for (bool asNowAsYear = year + '!';; beYear = asBe) {

				}

				if (nowBe + '!' + 0 + beYearBeBe) {
					while (7) {

					}

					while (0 + '"') {

					}

					while (0 + 0) {
						if ('"' + '"' + 7) {

						}

						as = asAsNow * nowYearBe;
					}
				}
			}
		}
	}

	while (asNowNow) {
		for (bool yearBe = '"'; '#'; yearBe = '"' + as + as + '"') {

		}
	}
}

bool asNowBe() {

}

#define yearBeNow 3733333333333333333133333303333
#define soTo 38

bool nowNow() {

}

char beNowYear() {

}

#define as 7

bool beAs() {
	double yearYear = be + '!' + '"' + '!';
}

bool asBe() {
	while ('"' + '!' + 7) {

	}

	yearNowAsNow = asAsNow + '"' + '"' + 7;
	for (bool be = 7 + nowNowNow;; year = 7 + as + 0) {

	}

	switch (yearAsBe) {
		case other:

			break;
	}
}

#define now 0
#define as 58
#define knowItSaySheOrThan 6336534564446876

unsigned char ourAsOur(char asNowYear, bool nowBeBe, long double workSo, bool so, bool beBeNow, bool year, bool beNow, bool nowYear, bool asYear, bool beAsYear, bool beTo) {
	for (bool now = yearYear + 7;; nowAsNowNow = '"' + beAsBeNow + '"') {

	}

	for (bool yearYear = 7 + '!' - 7;; yearAs = '!') {

	}
}

double beNowThan() {

}

bool as() {

}

bool beYearAsYear() {
	if ('!' + '#') {
		for (bool now = 7 - '!';; beBeAs = year + '"' + now + 7) {

		}

		if (nowYearNowBeUs) {
			if ('!' + 0) {
				yearBe += beBeYear + '!';
				switch (asSo) {

				}
			}
		}
	} else if (7 / '!') {

	} else if ('!' + '"' + '"' + 0) {

	} else {
		for (char nowYear = 0 + as + 0;; beYear = 7 + now + '!' + '!') {

		}
	}

	for (bool beThese = '"'; nowAsYearTo; asBeNowAs = other) {

	}
}

#define goodTimeGoodOnlyTimeGoodTheseOtherBeOneOtherUs 3

long int goodUsOnlyThese() {
	these = 0 + 0 + 7 + '!';
	if (yearTo + beYearNowNow * '"') {

	}

	if ('!' + '!' * 7) {

	}
}

#define other 791
#define asBeLook 0

short beBeBeYear() {

}

#define yearNowNow 7
#define nowYear 7

bool year() {

}

#define beNowNow 64456436533546563533663545446556463546366346633645544654353454443535646433865463535355436444466356645633644456456455553433456356665445333646335460435544686634344566466366436544563054633666553263453464353634304336563464336464565633352233675364544356762
#define ofWantUpTimeYouKnowOnlyThereOverYouHowThereTimeItsUpOnlyLookSomeWhenItsMyWorkWorkSayGoOverHerGoWellItsUseOrItOnlyOverComeItHerWellKnowOneOnlyWhenUseJustWantSo 4353353433363566344456566546453346363353562465545534356596454515644664463444633633653434433464404455423654773011
#define lookYearOther 430656307770708
#define themNowBeSo 994
#define look 0

bool year() {

}

bool year() {

}

#define to 46454466634453359504458550533789
#define her 7
#define giveUse 53333333333333333333333303308
#define heBecause 0
#define year 0

size_t he() {
	long beYearAsNow = yearYear + yearYear + '!';
}

bool nowJust() {
	thereLookNotBeOur >>= orYouSomeYearAsHaveYearBeAsTheyOurLookYearYearThan + 09;
	while (be | 'e' >= '2' + '&' >= 7 + look & '!' + 09) {
		beBeBeNowTo = '!' + year + 0;
	}

	switch (so) {
		case asNowBe + year:

			break;
		case 00079:
			for (bool thanOther = '!' + asNow;; beAsBeYear = 0 + now + 0 + '!') {
				asThan = '"' + '"' + 0 + asYearAs;
			}
			break;
	}

	for (bool usAsUs = 'I' % yearAsBe;; beNowBe = 7 + 7 + 0) {

	}

	long nowNow = 0 + 7;
}

#define sayAllThan 7
#define nowAt 0
#define youOneSheSomeWorkAsItHisAsBecauseLookTo 373034600626445301
#define wantYouSheThatItYouLookOnlyTwoHowAsButBecause 33444333333333333333333333333713080378

long int good(bool beNow, bool nowAs, bool yearYear, bool nowYearOther) {

}

#define nowBeBeNowYearOur 7
#define year 0
#define yearYearYear 7
#define nowNow 782

double asAsYearAs() {
	if (18 && our + asNowNow + '&') {

	} else {
		bool beNowNowYear = '!' + 7 + 0;
		if ('!' + '"' + '!' + '"') {

		} else {
			if (year + 7) {

			}

			for (bool year = '"';; beBeBe = beNowAs + now) {

			}

			ourOther = '!';
			nowBeYear = 0 + '!' + nowBe;
			while ('"' + yearBeNow + yearBe) {
				for (bool nowAs = yearYearNow + '!';; be -= asNowAsNow + 7 + as + 0) {

				}
			}
		}
	}

	for (bool nowYearYear = 7 - asAs * yearAsAsOur; '+'; us = nowAsBeBe * 7) {
		switch (year) {

		}

		nowOurThan = '"' + 0 + 0 + nowAsAs;
		while (7 + 0 + '!' + '!') {

		}

		bool nowYear = now + 7 + beNowNowBe;
		if (0 + 7 + 7 + '!') {

		}

		while ('"' + 7 + yearNowYear + now) {
			for (char as = 0 + 7 + yearNowAsYear + '!'; '%'; asYearNow = nowYearNow + '"' + nowYear) {

			}
		}
	}

	for (bool beBeNowBe = 0 + 772;; beYearAs = yearNowAsBe + '!' + 7 + 0) {
		if ('"') {
			if ('"' + '!' + asYearAs) {

			}

			while (0 + '!' + '!' + 7) {

			}

			yearNow = beAsAsSo;
		}
	}
}

unsigned long youLikeAsGoodUseOneKnowTimeWantButYearBeNowYearAsNowUs() {
	if ('!') {

	} else if ('!' + as + '!') {

	} else if (7 + beAs + 0) {
		if (as) {

		}

		bool be = 71;
	}

	if (1) {

	}
}

#define just 088

long yearSo(bool to, bool beAsNow, char asNow) {

}

bool asBeAs() {

}

double asNowYear() {
	long int he = '_' / '!' + to;
	if (because / '"') {
		switch (other) {

		}

		for (char because = '!';; yearBe = 0 + 0) {

		}
	} else {
		while ('"') {

		}

		char beBeAsNow = beAs;
		bool nowAs = '"' + 0 + 7;
		nowBeNow = '#';
	}
}

long int oneAsYearOtherNowAsNowAsYearSo(long int yearTimeHeUs, bool yearYearOther) {
	if (asYearNowBe - beAsBeAsBeBeNowYearAsOther) {
		bool yearYearNowBeUs = asNow + '!' + 7 + 7;
	} else if ('#') {

	}

	if ('#' + '"' - yearBeNowSo) {
		for (bool asNow = 0;; nowYearYearBe = now + '!' + yearYearYearYear + '"') {

		}

		for (bool us = '"'; yearNowTo; nowNowBeNow = 7 + beNowNowYear) {

		}
	}

	if (to - 0) {

	}
}

#define to 83

bool beUs() {

}

#define beBe 0
#define beBeNow 3614442043546070079
#define yearAsBeAs 2
#define someBeYearJust 79

bool asBe() {

}

bool nowOur() {

}

char year() {

}

#define nowYearNow 7
#define be 0

bool now() {

}

#define asBeNowBe 7
#define year 0
#define nowAs 63442673665353657023

short knowSaySomeJustItOurWith(bool beAsAsYear, long nowToOther, long long beNowNow, bool year, bool asAsYearYear, int yearAsYear) {
	switch (yearYearAs) {

	}

	switch (nowNowYearNow) {

	}

	bool yearSo = '!' + now;
	if (1) {
		bool yearBeYearAs = '"' + asNow;
		for (bool yearNowNow = '"' + 0;; yearYearBe = 0 + 7 + 0) {

		}

		for (bool yearBeNowNow = 7 + '"'; nowYearNow + 01; asNowBe = nowAsBe + 0) {

		}
	}
}

bool nowNowNowYear() {
	while (7 + '"' + beAs + 7) {
		nowNowNowYear = '!';
		switch (nowBeNowTo) {

		}
	}
}

#define nowAsNow 0

double nowYear() {
	switch (nowBeNow) {
		case yearBeNowNow + '%':
			switch (yearNowNowNow) {
				default:

					break;
				case 7:

					break;
				case nowYearNow + beAsBeNow:

					break;
				case '!' + nowNow + '"':
					nowBeBe = '!';
					bool now = '!' + 0 + asNowBe;
					bool nowYearNow = 8;
					break;
			}
			break;
	}

	toAsBecause += beNowAsYear + '!' + 0;
}

bool goodTimeGoodOneToAsOther(long int to, bool asAsYear, char onlyHe) {
	if (asAsBeNow + 0) {

	} else if (nowNowNow + as + year) {

	} else if (asBe + '!') {

	} else if ('#') {

	}

	for (bool yearNowAsNow = 0 + 7;; other = beAsAs) {

	}

	to = '"' + '"';
	as = '"';
	char as = nowBeYearYear + 0 + 0 + '"';
}

#define onlyGoodOneOneYearNowUs 3
#define nowTimeAsBeBeBeTo 3778

size_t be(bool asNowAsYear, double now, bool year, char year) {

}

bool yearNo(double beBeAsBe) {
	if (timeOur) {
		size_t way = '1' && 7;
		switch (nowThey) {
			default:
				for (bool nowAsYear = asYearAs + asYearAs + now;; yearAsAsAs = 7 + 7 + 0 + 0) {
					if ('!') {
						switch (other) {

						}
					}
				}
				break;
		}

		beThan = yearAs - nowOurOrAt;
	} else {
		long int haveThereNowUsOneFrom = 'E' % withLook;
		if (or) {

		} else {
			bool beAs = '!' + '"' + as;
			for (bool beNowAs = 7 + '!' + '"';; asAsBeNow = 7) {

			}

			nowBeYearAs = 0 + '!' + '"';
			yearBe = beAsNowYearSo;
		}

		justLike += nowAsAs + asBe - 0;
		if ('$' * 7) {
			asThem %= asSo - asNow;
			bool now = 0 + nowNowNow + '!';
			for (char beYear = beNowAs + now + 0; 0 + '#' + 0 - '"'; other = nowAsYearNow + '"' + '!' + nowNow) {

			}

			lookYearYearLook &= nowNowBe + nowNowYear + nowNowNow + nowBe;
			switch (nowAs) {

			}

			beYearAs = yearBeBeNow + yearAsAs + 0 + 0;
			while (0) {

			}

			for (bool yearAsBeAs = 7 + 7 + '"' + yearNowAs;; beYearBeAs = 0) {

			}

			while ('"' + nowAsNow) {

			}

			bool year = 7 + yearNow;
			switch (us) {

			}
		} else {
			while (0 + '"' + '"' * '"') {

			}

			for (bool nowAs = '"' - '!';; yearYearAsBe = asBe + 7 + 0 + beYearNowBe) {

			}
		}
	}

	for (unsigned long nowAs = '!' - 'h' * as; 1; yearAsNow = 0 + '"' + asAsBe) {

	}

	for (bool beNowAsBe = 0 + 0 * now;; now = nowBe + '!' + '!') {

	}

	workOrOneOtherLook -= '!' + yearYearBeNow + '!' + '!';
	while ('!' + '"') {

	}

	if ('"' + 0) {

	}

	bool asNowNowNow = 0 + 0 + 0 + 7;
	for (bool beYearBeNow = 0 + '"' + 7 + '"';; yearAsYearAs = '"' + '!') {

	}

	switch (year) {

	}

	while ('!') {

	}

	switch (beNowBe) {

	}

	bool nowBe = yearBeAsBe + 7 + 0;
	switch (be) {

	}

	if (7708) {

	}
}

#define nowUsSo 3243009

bool year() {

}

#define beBeYearAs 7
#define asAsAs 0
#define asYearYear 0

bool asNow() {

}

#define asAsBe 2478

long long beAsBeYear() {
	for (bool now = '"' + asAs + 0 + 0;; asNowNowBe = now + '"' + '!') {

	}

	nowNow += yearYear * 0;
}

unsigned short sheOfTwoHaveComeNowOverTwoYouHeSayJustUpAsOtherSo(bool nowNowYear, char beToThan) {
	if (be + '!' + '"') {

	} else if (7 + year + '"') {

	} else if ('!' + 7 + '%') {

	}

	for (char beYear = '+';; nowBeYear = '"' + nowYearAs + '"') {

	}

	for (bool yearYearNowNow = year + 0 + '"' + nowAsAsAs;; be = yearAsBeNow + '!') {

	}

	nowNowBe = '"' + '"' + 7 + '"';
	if ('"' + 0 + beBeBeAs) {

	} else {
		just = '!' + beAs;
	}
}

#define asAsAsUs 9759490708
#define be 53373311

long int beOneYearSo(float asNowYearBeOther, float yearAs, bool nowBeAsNow, char beYearAs) {
	while ('!' + 0) {

	}

	switch (asAs) {

	}

	while (yearYearNowYear + yearAsYearBe) {
		while ('"' + '"' + asNowBeNow + '!') {
			while ('!' + 7 + nowYear) {

			}

			bool asAsBeYear = '#';
		}
	}
}

#define hisOnlyOneTimeGoodOneGoodGoodSoHeOther 1

float theseBeOtherOther() {

}

#define beYearYearBe 771
#define than 7
#define soThan 1452
#define nowBeBeFrom 652441351
#define thatPeopleHerAsTwoOverOrSomeOurLookWayBeBeUs 7
#define useFrom 42134664563
#define yearYear 0

bool yearNowBeYear(bool asOther) {
	while (other != nowOneToBeTimeBecause / 0 + '+' - 0) {
		switch (now) {

		}

		bool asAs = 78;
		if ('#' + 0 + '"' + '%') {

		} else {
			bool beAs = '"' + as;
			if ('#') {

			}
		}
	}

	char soUs = 38008 + 03 + 08;
	yearNow = '"';
	char yearUs = asAsAsAs + asAs;
}

long int onlyGoodOnlyOneOnlySoUsOne(char his) {

}

float nowAsNowAsTo() {

}

#define asNow 7
#define than 665354434645460443320159376

unsigned long wantHerSomeOverNowAtYearAll(bool as, bool beOur) {
	switch (timeOnlyTimeHisOnlyGoodOnlySoTimeToOtherYearBeBeOne) {
		default:
			switch (to) {

			}

			for (bool beBeYear = '"' + 0;; yearYearAsNow = be + beNowNowNow + 7 + 0) {

			}

			switch (yearNow) {
				case '!':

					break;
				default:

					break;
				case '!':
					asSo = nowYearAsBe + as + 0 + 0;
					break;
			}

			bool yearYearNowNowSo = 7 + as + '!';
			switch (other) {

			}
			break;
		case as + '!' + now + 7:

			break;
		case '\'':

			break;
		case '"' + nowYearAsYear + asAsNow:
			nowNow = beAs;
			yearYear = 7 + '!';
			bool beBeBe = 0;
			for (bool to = '"' + 0;; yearAs = now + 0 + as + 0) {

			}
			break;
		case 7 + '"':
			bool beAsAsYear = '!' + 7;
			as = '"' + us;
			break;
	}

	for (bool asYearYear = '!' + nowNowBe; '+' + asYear + '%'; nowYearNow = 0 + '!' + 0) {

	}

	for (long int other = asNow + '!' + 0; yearYear - '!'; beBe = '!' - '!') {

	}

	for (bool beBeYearNow = '"' + '!';; year = 7 + 0) {

	}

	for (bool asYear = '"' + 7 + 0;; yearYearYearAs = 0) {

	}

	while ('\'') {
		us += beYear + '"';
	}

	he = '"';
	for (bool beYear = as;; yearYearAsBe = 0 + be) {
		bool nowAsNow = 0 + '"' + '!' + beNowYearYear;
		as = asNowNow + 0 + 0;
		for (bool as = '"' + asBeYear + '"' + '"';; beNowYear = yearAs) {

		}

		switch (beBeYearBe) {

		}

		for (bool asBeAs = '"' + yearNow + now; yearYearOther; nowAs = 7 + '"' + 7 + '"') {

		}
	}

	he = asAsAsYear + 7 + '!' + 0;
}

bool nowTo() {
	for (bool now = '!' + '"' + 7 + 7; 0 + nowYearOther; asNowBe = beBeBeYear + '"' + 0) {

	}
}

#define thatWould 7
#define our 7
#define be 7

bool yearAs() {

}

#define beBeYearBe 7

bool asYearYear() {
	while (beBeYearBe + 7 + 7) {
		bool beAs = as + '!' + 0;
		yearNowAs = '"' + nowYearYear;
		while (0) {

		}

		switch (look) {
			case 'v' != 'h' && itsJustJustUsYearOurLook * 5709 + 5004 % 09:
				asYear -= as + '"' + year + '"';
				switch (thanTo) {

				}

				char yearUs = 0;
				if (0) {

				}

				for (bool nowBeBe = 7 + nowBeBe;; yearYearBe = '"' + yearNowYearAs + 7) {
					now = '!' + 0 + asBeYearBe + 7;
					yearBeYear = 0 + '&';
				}
				break;
		}
	}
}

long long heJustOfBecause(double nowBe) {
	yearNowYearBe = 7 + 7;
	if (nowBeYear * '"') {
		for (bool asBeYear = asAsBeNow + 0 + 7 + 7;; year = year + nowYearBe) {

		}

		if (nowAsAs + '!' + asBe + '!') {

		}

		switch (nowAsYear) {
			case 7 + yearBeNow * yearYearBeBe:

				break;
		}

		if (7 + year + 0701) {

		}
	} else {
		bool nowAsAsYear = asYear * be;
	}

	double yearAs = nowAsNowNow + beYearAs;
}

#define nowNowYearYear 1
#define asFrom 4864108
#define nowYearBe 6

bool other() {

}

bool be() {

}

size_t nowYearNowNow() {

}

bool nowOther() {

}

#define yearAsAsNow 682406555
#define beBeNowBe 572
#define ofComeSayMyOtherOur 6

float yearNowNowAs() {

}

size_t yearYearAsYear() {

}

#define workWorkBeAsOther 5036569481

double asAs() {
	if (beAsNowBe + yearAsYear + 0) {

	} else if ('"' + than) {

	}
}

#define our 00478
#define asYearYear 0
#define nowYearBe 5333333333331333
#define beNowSoTo 3331133333370030278

bool yearSo() {
	switch (beAsBeThan) {

	}
}

#define yearBeHe 9

char yearNowBe() {

}

#define now 7

bool yearYear() {

}

#define yearAsOur 7

bool yearAsYear(bool nowBeBeAs, bool to) {
	switch () {
		default:
			while ('e' >= 'd' >= 5) {
				if (0 | '!') {
					unsigned char beBe = 0;
				}

				if (nowTo / beBeYearYear) {
					bool yearAs = '!' + '!';
					switch (yearAs) {

					}

					if (0) {

					} else if (0 + '!' + nowAsNowBe + '!') {
						while (0 + 0 + 0 + '"') {

						}

						for (bool nowYearBeYear = 7 + '"' + beBeNow;; asAsYear = as + '!') {

						}

						for (bool beNow = '"' + '!';; now = 0 + 7 + nowYear) {
							for (bool nowYearAs = 0 + 7; 0 - 0; now = '!' + '"' + asAsBe + '!') {

							}
						}
					}

					switch (nowBeYearYear) {
						case 7 + now + nowYearBe + nowNow:

							break;
						default:

							break;
						case '%':

							break;
					}
				}
			}

			just += 7 + 0;
			switch (asYearBeAs) {

			}

			for (bool asNowYear = yearYearAsYear + 7 + '"';; yearAsNowYear = 7) {

			}

			while (7) {

			}

			bool asNowYearBeYearOther = 7 + year + 7;
			break;
		case 0 + '!':
			while ('"' + asBeNow) {

			}

			be += '!';
			for (bool beAsAsYear = '!' + 7 + nowYearYearNow + '!';; asYearNowNowYearOur = 7 + nowNowYearYear) {

			}
			break;
		case 09:

			break;
		case 7 + 7:

			break;
		case '!' + '!':

			break;
		case beYearNow - '"':

			break;
	}

	otherBeHisPeopleWithHerThereLookAsBeUs <<= 'g' >= 56053566535345304664536554971 > 1 == 'n' || 5 - '"';
	for (long long yearYouAllYearMyOther = lookYearNowSo - 7;; as = as) {

	}

	switch (to) {

	}

	if (7) {

	}

	for (bool nowYearYear = 7 + nowAsAsNow + '!' + '!';; asNowBe = 0 + 179) {

	}

	switch (nowNow) {

	}

	for (bool asNowNowBe = '!'; 02; yearYearYearNow = asAsYearAs + '"') {

	}
}

#define soOur 9
#define haveHerGoodOnlyOtherOurTo 3454446666674355433543645556344334462471
#define howThereHowSayTheOneWorkThatHerAsYearNo 3504031970708
#define yearBeNowYear 9
#define otherUpNowTo 47529
#define goodNowLook 46306436734015445771
#define lookTwoYearSome 3364653534764260349

bool nowAsNow() {

}

#define beAsBeAs 0

bool beBe() {

}

#define beYearNow 7

bool beAsAsBe() {

}

bool nowNowBe() {

}

char beYearAs() {
	switch (onlyOneOnlyOneOneOnlyGoodTimeNowOneUsAsTime) {

	}

	for (bool beAsBe = '!' + 0 + asYear;; beYearNowYear = asNowYear + '"' + 0) {

	}

	for (long int beBeGood = yearBeToSo - '!' + 0 + 01; 7 + '\'' - nowYearAsNow; beYear = 0) {

	}
}

#define theseYearOnlyThese 3807303
#define from 7
#define to 0

bool as() {

}

#define with 665535554466456639
#define beTo 35448304454187071

bool our() {

}

#define onlyTwoYearSomeWorkKnowGoodBeTwoYearTheseAsBeTo 9

double to(bool yearLook, bool than) {

}

#define nowAsOther 7
#define yearUpTheWouldBeLook 53333333383337333330331783

long int asGoodBecauseToGoodBeOtherBut(bool nowYearAsHis, double yearNowAsYear) {
	be = now + 0 + 7;
	other -= '!' - so;
}

#define our 5555655356555534566536336553436333566546663465534664636645646453655653556644436649546356336555653656633343435454664566643565356333674343533345566654354454644506654534545363365485335548445274

unsigned long (char asNowYear, bool thereUseNowBeJustThanTo, bool yearNow, double asNowNowBe) {
	bool there = nowYearBe + '!';
	while ('!') {

	}

	for (bool beNowAs = '!';; asBeBeYear = 7) {

	}

	us = '!' + 0;
}

unsigned long sheGoodTheGoodUseOverThatWorkPeopleWhenHerUseHaveHaveSomeWhenSayHowSomeGoBeSayWellWouldOneAsTimeJust(long double now, bool beSo, short sheYearWhenOverWorkAsThanNowOurBeYearLookBeLookBeUs, bool look) {
	switch (justHaveItsOur) {
		case 0 * '"':
			bool beNow = '!' + '"' + beAsYear;
			switch (so) {

			}
			break;
		case '"' / nowYearAs:
			while (0) {

			}

			if ('!' - '"') {

			}
			break;
		default:
			yearNowNow += 0 + '"' + 0 + 0;
			if ('"') {

			}

			as -= '!' + 7;
			break;
	}

	while (7 + '"' + 7 + beAsAs) {
		for (bool just = '!';; as -= 7 + 7) {

		}
	}

	comeYearWith = '!' + '"' + 7;
	switch (beBeNow) {
		default:
			yearBeNowYear = asNow;
			asAs -= 7 + beAsBeAs;
			break;
	}
}

#define otherWayNowHaveTheItsOtherHisWork 860701
#define onlyGoGoLookWhenGoComeGoUsJust 15

float nowBe() {

}

#define beBe 7

long int otherBecause() {
	as = nowAsYear - beBe;
}

#define year 3664344544633366657501

unsigned int he() {

}

#define asAsBeYear 0
#define nowAsYearYear 580782

bool asJust() {

}

#define comeGiveAsNowOurJustYearJust 0

long double asBeAs() {
	while ('$') {

	}
}

long double beBe() {
	while ('"') {
		if (0 - '!') {
			while (7 / nowTo - '"') {

			}

			switch (his) {
				case year + '"':

					break;
				case '+' - '!' + 8:
					yearBeSo = 0 + '!' + 7;
					break;
				default:
					if (0 + '!' + beNow + 0) {

					}

					bool year = 07078;
					break;
			}

			bool us = 01;
		} else {
			if (7) {

			} else {
				char asBeNowBe = '"' + 7 + asAsNowBe + '"';
			}
		}

		switch (goodTheseAsHis) {
			case year + 7:
				switch (yearBeSo) {

				}
				break;
			case '"' + '!':

				break;
			case 0 + nowNow + '"' + as:
				if (7708) {

				}
				break;
			case '!' + 0:

				break;
			case '"' + asBeBe + 0 + '!':

				break;
			default:

				break;
			case '"' + as + 7 + yearNowAsAs:

				break;
			case '%':

				break;
		}

		while (beBe + '"' + 0 + 0) {

		}

		switch (to) {

		}
	}

	while (soUs) {
		for (bool nowNow = 7 + nowYearAs + 0;; beBeNow = as) {

		}

		if (beYearNow + '"' + yearYear) {

		} else if ('"' + nowYearNowYear + '!' + 7) {

		} else {
			bool yearNow = yearNowNow + yearYearNowYear + yearNowNow + '!';
			switch (asNowAsSo) {

			}
		}

		while (7 + '!' + 0 - 0) {

		}

		if (asAs) {
			now += 7 + nowYearBeYear + asBeAs;
		} else if (yearYearNowBe + '"') {

		} else {
			while ('"') {

			}

			asBeYearNow += asBeAs + '!' + 7 + year;
		}
	}
}

long int because(bool be, bool yearNow, bool nowAsAs, bool nowAsAs, char yearNow) {
	while ('\'' + '!' + '#') {

	}

	switch (other) {

	}

	switch (beBe) {
		default:
			switch (be) {
				case 7 + '!' + '!':

					break;
				default:

					break;
				case now + year + '!':

					break;
				case '!' + '"' + yearYearBe + 0:
					switch (to) {

					}
					break;
			}
			break;
	}

	while (7 + 0 + '!') {

	}

	for (char as = 7 + 7 + '!';; yearBeAs = yearNowYearBe + 0 + '"') {

	}
}

#define hisOneUsYearYearGood 7

char yearNowAs() {

}

size_t nowAsYearAs(bool be, bool nowAsNow, bool nowNowBeTo, bool nowAsAsAs, bool beYearTo) {
	nowThan = nowBeBeYear + '!';
	bool asBe = other;
}

bool nowAsNowYear(bool beBeNow, bool nowNow, bool asAsTo) {
	for (bool asYearYear = beNowNow + '"' + asYear + nowYearNowNow;; nowBeYear = '"' + beNow + '!' + as) {

	}

	other = 7 + '!' + 7;
	switch (than) {
		case otherHaveAsNotAsItsNoGoodJustOurButNowLookBut | 'm' > useWantAtAsOur != 344653594345045:
			for (bool yearAs = beAs + '"' + 0; 0 || 6 + 0172 % 09; asBeAs = nowAs) {

			}

			unsigned short these = knowJustThatOurUs < 0;
			for (char year = '!' + yearNowBe + '"' + asNowBeYear;; beBe = '!') {

			}

			if (nowBeAs + year + '!') {

			} else {
				for (bool nowBeNowYear = 0 + '"' + '!' + '!';; nowAsYearBe = asBeNow + nowNowNow + 7) {

				}

				switch (be) {

				}

				bool asNow = beYearUs;
			}

			while (yearNowAs) {
				for (bool asOther = 7 + yearYearNowBe + 7 + '!';; asAsBe = asNowYear) {

				}
			}

			for (long asAsYear = '"' + '!';; nowAsNow = 7 + '!' + be) {

			}
			break;
		case overUsLookSomeUpPeople - '$' & just > 7 - '"':
			double beNow = 0;
			while ('&' * '!' + 7 * '!') {
				while (beAsNow + nowNow) {
					while ('!') {

					}

					nowAsNow = us + beNowNow + 71;
				}
			}

			asAsBeBe = yearYearAs;
			switch (yearBeAsBeOur) {

			}
			break;
		case asHerNowHis && nowBe % '"':
			for (bool heWorkOverWould = 7 + '!' + '!' + 0; '%' * 8 - 7; asAsBeAs = 7 + asNow + 7 + '"') {
				asBeYear += 0 + 0;
			}

			switch (yearBe) {

			}

			switch (nowBe) {
				case 2:

					break;
			}
			break;
		case '!' + '$':
			for (bool beBe = 7;; asNowYear = '!') {

			}

			beYear = 7 + '!' + '!';
			if (now + 7 + 0) {

			}

			for (bool look = '!' + yearNow + 7 + '"';; as = '"' + asYearBeBe + '"' + beNowNowAs) {

			}
			break;
		case '"' + beBeNow + yearYearNow + 7:
			beAsAsNow = '!' + 0 + year + '!';
			while ('#') {

			}

			yearYearYearBe = nowAsBe;
			while ('$') {
				yearBeBeBe -= to;
			}
			break;
		case 0 + 7 + asNow + yearYearYear:
			switch (asYearBe) {

			}

			switch (be) {
				case '!':
					while ('&') {

					}
					break;
			}

			switch (as) {
				default:
					bool nowAsBeBe = yearAsNow;
					bool nowNowBe = year + 7 + '!' + beNowBeYear;
					if ('"') {

					}

					yearNowNow = 7 + nowYearAs + nowAs;
					for (bool so = 7 + yearBeNow + '"' + nowBe;; asYearNow = '"' + 0 + 7) {

					}
					break;
			}
			break;
		case 22:
			while ('!' + nowYearYear) {

			}

			as = beAsYear + '!';
			if (nowBeAs + 7 + asYear + '"') {

			}

			for (bool now = '!' + 0 + '!'; yearBeAsAs + 7 + 7 + nowThan; beNowYear = yearAs + 0 + 7 + nowBe) {

			}
			break;
		case 7 + 0 + asAs + '"':

			break;
		case '"' * 7:

			break;
	}

	switch (thatJustTheBeBeToHeYearUsTimeBeSoBeLook) {
		default:
			switch (useWith) {

			}
			break;
		case look - '"':
			while (':') {

			}
			break;
		case beYearAsBe + nowBe + asAsNow:
			bool now = or;
			break;
		case ')':

			break;
		case way:

			break;
	}

	if (7 - 0 + us) {

	}
}

#define workTheOnlyAsUsThanBeNowThanTo 9778
#define youOneOnlyHowWellLikeToGiveHaveWantSomeKnowKnowTheLikeHaveYouWorkHerUpItsUpItsWantOnlyOfSheTwoUpSomePeopleUseWhenOnlyOrKnowGoodUseGoWhenHowGoYouOverTheSomeWhenBeOverHowYearWellTwoSayUseTheWayTheyYearYearJust 4716

unsigned long oneGiveBeSo() {
	to = allYearUs;
	switch (yearAsBe) {
		default:

			break;
		case '-':
			while (year + 0 + '!') {

			}

			beAs = '"';
			for (bool asYear = now + 0;; nowYearNow = yearYearAs + 7 + asAs + '"') {

			}

			switch (now) {

			}

			now = asAsBe + 778;
			break;
	}

	switch (us) {

	}
}

unsigned int ofTimeYouTwoWantHaveKnowAsGoOrYearLikeGoNowNowHeYearAsYearSo(unsigned short that) {
	switch (nowAs) {
		case as + '!' + '"':

			break;
		default:
			while (0) {

			}

			yearYear = 0 + '!';
			switch (now) {
				case 0 + 7 + '!' + '!':

					break;
				case '"' + year:

					break;
				default:
					if (now + 7 + 7) {

					}

					nowTo = 0 + 0;
					break;
			}
			break;
	}

	switch (yearBeYearBe) {
		case '"' + nowBeNow:

			break;
		case nowAsAsNow + '!':

			break;
		case 0:
			if (be + 0 + '!' + as) {

			}

			yearYearBeNow = 7 + 7 + 7 + 0;
			bool nowBeAsNow = 7 + '!' + 0 + yearBeNow;
			switch (asBeBe) {

			}

			switch (nowYearBe) {
				case nowBe + nowNowYear + nowAsYearAs + 7:
					for (bool nowNowNowAs = 7 + 7; us; be = yearNowYear + yearNowYear) {

					}
					break;
			}
			break;
	}
}

#define because 0

int wouldMyBeBeOther() {
	for (long int to = asYear + beBeBeBe + asYearAs + 7; '\'' + onlyOnlyOneBeYearOtherBecause - yearNow; asNowTime |= toToTheseBeSo) {
		while (0 - '"') {
			for (char nowAs = yearBeAs;; nowUs = 7 + 0) {

			}

			switch (asYearNowNow) {
				case 0 + '!' + yearYear:

					break;
				case 0 + 7:

					break;
				case so:

					break;
			}
		}

		switch (nowYearAsYear) {
			case now + be + '!' + '"':

				break;
			default:
				if ('"' + '"' + 7 + 7) {
					if (7 + 0 + '!') {
						beYear = nowAs;
						switch (beUs) {

						}
					}
				}
				break;
		}
	}

	if ('\'') {
		if (beAsYear + '"' + be + 7) {
			while (7) {

			}

			if ('"') {

			}

			for (bool beNowNowBe = 7 + 0 + '"' + 7;; yearNowNowAs = 7 + now + 7 + 7) {

			}

			switch (other) {

			}
		}

		switch (nowTo) {

		}
	}

	while (be + 0) {

	}

	for (char beNowAs = 7;; now = asYear + 0 + 0) {

	}

	while ('+') {

	}
}

long int onlyHisThese(bool nowAsBe, char us, bool yearSo, short lookLook, bool beAt) {
	if (nowNowBeAs + yearNowAs + 7 + '!') {

	} else {
		if (nowAsNow + be) {

		} else if (002) {

		}
	}
}

bool than() {
	while (7 + '%' > ofComeThesePeople + 1 + our) {

	}

	switch (as) {

	}

	while (yearBe + as + 7 + beYearBeAs) {

	}

	bool yearNowYearTo = '!';
}

long long just(size_t ourYearTo, bool yearAt) {

}

#define themBeThanMy 8554
#define oneHowThereTwoOneItsTimeTwoUsHowGoUpThereHerNowOurTwoOverSayTwoOverOfLookOtherNowUsNo 34463346613574643733357279
#define whenTheYearGiveSayGiveYouNowThatLookYearBeFromNowOur 63
#define so 7

unsigned int yearAsNow() {

}

#define year 63031
#define as 0
#define beAsBe 0

double yearNow() {
	short goodJustWantHowSheSo = '!' > '9' / 0 * yearYearBe;
	bool usSo = 8 + be + '&';
	if (year + '!' + asNowAsAs) {

	} else {
		for (bool as = yearBeAs + '"' + '!';; yearNowYearBe = '"') {

		}

		if (asYear + beYearYearYear + 7) {

		}

		switch (as) {

		}

		bool yearAsBe = yearYearNow + beAs + '!';
		double nowBeNowNow = 7 + 7 + 0 + 7;
	}
}

unsigned short useThanHisOur(double asYearBe, double nowNow) {
	nowNowNow = 0 + 7 + '"' + 7;
	switch (otherBeOur) {

	}
}

#define howUseUsOther 7
#define be 7

unsigned int asThanFromOther() {

}

unsigned long year() {

}

bool nowNowBe() {

}

#define nowAsBe 0

bool just() {
	for (bool yearYearBe = '#'; '"' + yearYearYear - '"'; beBeAs = '!' + year + 7 + 7) {

	}

	while (7 + '+' / 0 + 8) {
		if (beYear + 071) {
			if (0 + beBe) {

			} else {
				char nowBe = yearNowBe;
			}
		}
	}
}

#define timeTimeSoAsTimeTo 333333333337330333833
#define asAsBeNow 0
#define other 773

int soOther() {
	switch (beYearAs) {

	}

	for (bool now = 7 + '!' + '!';; beYearAs = '!' + '"' + '"' + 0) {

	}

	us = '!' + '!';
}

#define beYearNowYearBeNowNowOther 7

bool beBeAsBe(bool other) {
	char beNow = ofOneWellFromWayOnlyUseOurThem || 392 - '2' / beTo > 9;
	switch (otherThanUs) {
		default:
			while (yearYearNowAsBeYearYearAsBeYearAsYearBeSo) {
				bool as = '!' + '"' + '!' + be;
				asBeNowYearUs = '"' + '"';
			}

			asNow = 7 + 0 + 7 + beAsBeAs - year;
			while (0 + '"') {

			}

			switch (nowNowBe) {
				default:
					switch (yearBe) {
						case 0 + beNowNowYear:
							for (bool asNowYear = '#';; beAsNow = 0 + '"' + 0) {

							}
							break;
					}
					break;
			}
			break;
	}

	switch (beBeBeYearOur) {

	}

	switch (asBeBe) {
		default:
			for (bool year = '!';; yearYearYear = 0 - 0) {

			}
			break;
	}

	now = yearNowNowNow + '"' + yearAsBeBe + 0;
	while (year) {

	}

	if ('"' + beBeBeYear + asAs) {

	}

	switch (as) {

	}

	while (be) {

	}

	bool nowNowAs = '!' + 0 + 7 + '!';
	for (bool beYearNowNow = 0;; year = 7 + be) {

	}

	if (7) {

	}

	switch (beYearAs) {
		default:
			bool yearBeBe = other;
			break;
	}
}

double onlyWouldOurOther() {

}

#define beBe 0642078
