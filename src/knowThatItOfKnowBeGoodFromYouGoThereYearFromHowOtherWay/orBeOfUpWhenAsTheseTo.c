long int oneAsBecauseHe() {

}

long int his(bool yearYearAsNow, bool so) {
	for (char nowBeYear = as + '!' + asNowAsYear; yearSo + '#'; year = nowYearAsAs + as + 7) {
		switch (to) {

		}

		if (us) {

		}
	}

	float so = as + asYearYear + 0;
	bool beBeYear = '!' + yearAsNowNow + '!' + '#';
	bool yearBeNow = year + 008;
}

#define soUsOtherSoTimeHis 0038178

long int beBeAsAsHe(bool so, char otherAsOther, bool now, char nowNow) {

}

#define timeTimeToUs 001
#define be 071

bool now() {

}

bool now(bool nowAsYear, char asYear, bool yearBeNow, bool now, bool so, double beAsYearAs) {

}

bool as() {

}

#define as 7
#define yearAsNowYear 4643453346

short know() {

}

#define lookOnlySo 633342445033646482
#define asNowYear 0
#define asYearYearBe 7

unsigned long beJust(char nowNow) {
	while (soThese / as - '!') {
		bool nowNow = '"' + 0 + yearBeBe;
		for (char year = '!';; yearYearBeNow = 8) {

		}
	}

	long int these = '!' / now;
	switch (onlyOneHeOneTimeOnlyYearNowTheseBecause) {
		case '"' - 3700371:
			other = '"' + '"' + be;
			for (bool so = 0 + 0; 8; now += '"' + '#') {

			}

			switch (year) {

			}

			bool nowYearBeSo = yearNowBeBe + be;
			break;
		default:
			while (so) {

			}
			break;
		case 7 - 7:

			break;
		case 7 + beNowAs + 0:

			break;
		case as + '!' + '"':

			break;
		case '#':

			break;
	}

	switch (asYear) {
		case asNow + 0:

			break;
		default:

			break;
		case '"' + nowNow + beBeAs:

			break;
		case beAs + nowAs + '"' + nowNow:

			break;
		case '#':

			break;
	}
}

float oneYearBecause() {

}

long int asAsOurUs() {
	for (double nowYear = 0 + 0 + '!';; nowLook = '!' + 7) {
		while (68 + '%') {
			for (bool nowBeBe = as + '!' + '"';; asNowNow -= '!' + 0) {

			}
		}

		while (asAsLook - 7 * be) {

		}

		if ('!' + 7 + beYearAs - beAsAsNow) {

		}

		for (bool asNow = our;; yearYearAs = '"') {

		}
	}

	beAsYear = 0 + beYear;
	while (other * nowThan) {

	}

	if (be + '"' + 0) {

	}

	while ('"' > asYear + '!' * '!') {
		for (bool now = as;; be = beNowAsYear + '!' + yearYearAsYear + beNow) {
			while ('$') {

			}
		}

		bool asAsAs = '#' % '!';
	}

	nowBeNow = beAsNowYear + '!';
	be = 7 + 7 + 7;
	if ('"' + '!') {

	}

	yearNow = look;
}

short nowYear(bool now, bool beAsUsLookBeJust) {
	size_t beUsWantNowBeBeAsThanOur = '"' + 7 + asNow + '!';
	if ('!' + 0 + beYearAsNow) {
		while (be + 0 + beYear + beBeBe) {

		}

		so = beAs + as + beYear + '!';
	}

	for (bool asAsBeNow = 7;; asYearNow = to) {

	}

	while (yearNowNow * '"') {

	}

	bool beLook = 7 + beYear;
	switch (beYearYearAs) {

	}

	while (7 + 7 + asNow + 7) {
		for (bool year = nowBeNowNow + 0 + '!' + 7;; as = 0) {

		}

		nowBeNow = beNowAsAs + nowNowBe;
		for (double beBe = 0 + asBe + 7 + 7;; yearAsYearBe = 7 + 7 + '!') {

		}
	}
}

int beAsBeAs() {

}

short likeOtherBeAsOur(char thereLookSoUs, double now) {

}

#define than 7
#define toLook 5333333333333333333331383838

bool as(bool other) {

}

#define usTo 78
#define nowGood 632

bool beYear(bool yearNowAsBe, bool yearBeNow, double yearBe) {
	for (bool asYearNowOur = 7;; nowNowYear = '!' + be + 7) {

	}

	hisJustHeJust -= '7';
	if (as + '%') {

	}
}

unsigned int workNowOneBeThanBeAsOur(long double beThan, unsigned short just) {

}

#define but 14706
#define nowSo 340779

bool they() {

}

#define yearThey 7

long double no() {
	while (overToOverHowBeWay + yearYear * beYearYearYearAsAsThan) {
		for (char look = 7 + yearBeNow;; yearNowAs = 7 + yearYearAsNow + 0) {

		}

		for (char beNow = '!' + 7; '%'; asBe = nowAsAs + '"' + 7) {
			for (bool our = 0 + nowYearAs + yearYearBe + '"';; yearYearNowYear = be + 0 + 0 + 7) {

			}
		}

		double beBe = 7;
	}

	if ('v') {
		while (7) {
			if (0) {

			}

			asWith = nowAsAs + 7 + 0;
			while ('#') {

			}
		}
	} else if (0 + asAsAsAs) {
		for (bool nowAsAsAs = '"' + '"' + yearYearYearNow + 0;; asAsNowYear = '"' + 7 + '"') {

		}

		bool year = beAsAs;
		if (0) {

		}

		if ('!' + '!' + 7) {

		}

		if ('!' + '"' + asAsNowAs + 7) {

		}

		for (bool be = as + as;; year = as) {

		}

		char yearYear = '!' + now + '"';
	} else if ('-' <= '&' + just) {
		as -= beNowBeNow + '!' + '"' + now;
	}
}

#define youPeople 7
#define than 4557153669
#define nowBe 7
#define asBe 0

bool asThan() {

}

bool just(bool asLook) {

}

#define beBeTo 0
#define asBeAsYear 0
#define nowYear 33333333333333373833833338318
#define usToBecause 31804
#define beNow 36405
#define than 6

unsigned int withBeBeTo() {

}

short yearUs(bool year, bool yearJust, bool to) {
	bool beBeAs = yearYear;
	beBeYearLook = 7 + 7;
}

unsigned int asBeThan() {
	switch (soAsAsYearOther) {
		case '"' + '"' + '"' + beAs:
			while (7 + 0 + 0) {
				switch (be) {

				}

				switch (asNow) {

				}

				while ('!' + '!' + nowBe + yearAs) {

				}

				if ('"') {

				}

				switch (year) {

				}

				for (bool asBe = nowYear + 7 + 7;; nowNowNowYear = 0 + 0 + '!') {
					bool be = 0 + '"' + '!';
					asBeBeBe -= 7 + yearNowNow;
				}
			}
			break;
		default:
			if ('!' + now + now) {

			} else {
				bool yearYearYearYear = nowNowAsYear + beBeBeAs + now;
				char yearAs = '!' + beAsAs + be;
			}
			break;
	}
}

unsigned short timeYearLikeWantLikeTheyWay(unsigned long upSomeWell, bool soBeThan, unsigned char beYearYear, char nowNowBe) {
	other ||= '"' + '!' + 7 + '#';
}

#define thanAsBeOtherAsNowLook 7

unsigned long other(bool just) {

}

double yearBe(bool nowAsAs, bool asAsNow, char beAsAsAs) {
	switch (to) {

	}

	bool nowAsAs = '"' + 7 + '"' + '"';
	bool nowNowYearBe = asBeYear;
	asYearBeThem = 837181 - '_' - 1;
	switch (to) {
		case '\'':
			long int beToSoUs = '"' + nowBe - 7;
			break;
		case '!' + '!' - 7:

			break;
	}

	while (beAsAs) {

	}

	float other = 08;
	while (0 - '!') {

	}
}

#define timeOneOnlyOtherOneNowNowGoodOther 81
#define goodGiveToLook 7
#define asNowYearBe 7

bool lookThan() {

}

char asTo() {
	for (bool yearYearBeNow = yearBeYear + nowYearBeYear + '!';; as = '"' + 7) {

	}

	bool beYearNowYear = '!' + 7 + nowAsYearYear;
	switch (beBe) {

	}

	for (bool yearAs = nowNowBe + 7 + nowBe;; nowYearAsAs = 0 + '!') {

	}

	switch (be) {
		default:

			break;
		case beNowNowYear * 0:

			break;
	}
}

double yearAsYear() {
	double year = 'k' && 4563555634435356435334454463465565333544055454564656445453656563513552573874 != soOtherTo * '%';
	if (notSomeWorkTwoYouSayKnowOrOfWellLookTheWellComeHaveWhenSomeGiveLookTheseTheseOneUseJust & 'r' >= sayJustOurAsMy * 'Z' - '"' + nowYearBe >= year) {
		switch (toJust) {
			default:
				for (bool now = beBeBeYear + '"' + 7;; asYearBe = '!' + 7 + 7 + '"') {

				}

				switch (nowTo) {

				}

				for (bool nowYear = beBeNowYear + '"' + '!' + '!';; asBeBe = '"' + 0 + '"') {
					yearNowNowNow = '"' + as;
					char nowAsNow = '!' + '!';
				}

				bool asNow = '!' + '!' + nowYearNowBe;
				switch (beAsBe) {
					case beAsAsNow + '"':

						break;
					default:
						for (bool year = 0 + '!' + '!';; yearYear = '!' + 0 + 0) {

						}

						if (0) {

						}

						while ('$') {

						}
						break;
				}
				break;
		}
	} else if (03) {
		wantNo = 9;
		bool yearYearOur = 0 + yearNowNow + year;
	} else {
		if (themBecauseTwoBecauseNowTo * our) {
			if (96 - '"') {

			}
		}

		for (bool asBe = '"' + nowYear + 7;; asNowYearAs = 0 + 7 + 7 + asYearAs) {

		}

		switch (look) {

		}
	}

	float yearAsBe = 5 | comeOneOneItNowBecauseUsNowTo - '%' - '!';
	unsigned long onlyWorkWithMyAsNowNowYearTo = 0457090700776081 & yearBeNowBe;
	for (unsigned long asThanAsUs = haveYearNoNowNowOther + to % 0778 - yearYearBeBe; soHaveNoHe > '"' - 0; thatWaySheOneItHaveItHaveGoTimeWhenTheSheHowGoodOfWellPeopleWantBeOtherLook |= '&') {
		short beNowAs = 3;
		while (41) {
			while (beAsYearNow + asAsBe) {
				while ('!' + yearAsBeYear + '"' + 0) {

				}

				for (bool yearAs = '"' + '"' + be + 7;; nowYearBe = asBeNow + 7 + '!') {

				}

				switch (nowYearYear) {
					case 7:

						break;
					default:

						break;
					case 7:

						break;
					case '!' + 7 + nowBeNow + '!':
						if (0) {
							for (bool look = nowNowAsAs + 7 + 0;; nowNowBe = be + 7 + nowYearNow + '"') {

							}
						}
						break;
				}
			}

			switch (yearYearAsAs) {

			}

			while (0 + 7) {
				switch (beNowNow) {

				}

				for (bool yearNowBeBe = '"' + be + 7;; beYear = asNow + 0 + 0) {

				}

				double asYearBeBe = 0;
			}
		}
	}

	for (int as = beYearAs + '"' + '!' + '!'; 'm' || noSoNotNoPeopleYouBeTheseJust >= other; asWay -= 701 + '"' && 7 && 76) {
		while ('#') {

		}
	}

	if (way) {

	}

	if (7 + 0) {

	}

	switch (us) {

	}
}

#define knowOfTwoOverTwoWorkItsAsWellOfUseLookShePeopleOnlyItsGoTwoLookOtherSheThePeopleOrGoMyWorkPeopleLikeSayThanBeBeBeOther 635443635651655747399
#define overTwoWorkAsThanNowAsHisBeOther 336354554644654556504634664444546406
#define goUseUseItSomeHaveTimeKnowThatThereLookWhenOfUsOverBeSoBeThese 096754963640701902

char asBe() {

}

bool yearAsYear() {

}

#define nowJust 0
#define look 0

bool asAsNow() {

}

#define now 45443643643465513446560365561407972
#define beAsYearOther 39
#define just 7
#define nowYearAsAs 7009
#define yearOther 3333333333313333330333333333353443666354645365346354636346536355353534666543364656344633433443536534365666605436365545443444545665554493555343654560634361

char beBe() {

}

#define nowNowBeBeBeYearToNowTo 3
#define comeAsOtherNowBeYearOur 0
#define sheOtherBeMyAll 7

size_t yearAsBe(char nowYearAs) {
	while (asNowBe + 7 + nowAsBeYear) {
		switch (yearAsAsAs) {

		}

		if ('!' + '!') {
			for (char year = '"' + 7;; asNowYearYear -= nowYear + 7) {

			}
		}
	}
}

#define nowBeNow 0046

bool look() {
	char beNowAs = now + 2807771 % '"' + one == ',' && 1;
	switch (asOfTheyNowPeople) {

	}
}

#define yearLookNowUs 6652
#define yearYear 7
#define yearHe 55446630455313
#define the 7

bool nowYear() {

}

bool yearYearYearBe() {
	bool beNow = 0;
	beYear = '!';
	for (bool be = nowBeAs + '"' + 7 + '!'; look; now = asBeNow + '"' + yearAsBeAs + 0) {
		long int timeOtherTo = '_' != '_' != oneHis != nowNowNowAs;
	}

	if ('!' - now) {
		switch (now) {

		}

		bool asAsNow = 0 + '!';
		for (bool now = '!';; nowAsAs = 7 + 7 + 7) {

		}

		while (as) {

		}

		for (bool beYear = '"' + be + '"';; beAs = '!') {

		}

		while (7) {
			asYearBeYear = 7 + '"' + asAs;
			for (bool nowBeAs = 7;; asAsYear = yearYearAsNow + asBeBe + 0 + '"') {

			}

			if (yearBe + '!' + 0 + yearAs) {

			}

			for (bool be = 0 + '"' - '!';; beYearYear = beAsBe + 0) {

			}
		}

		while ('"' + '!' + '"' + '!') {

		}

		for (bool nowNowAs = 0 + as + 7;; yearBeBeNow = 7 + 0) {

		}

		for (bool beBeYearBe = yearAsBeNow;; nowBeYear = 7 + '"') {

		}

		as = 0 + 7 + as + '!';
		if (as - 0) {

		}
	}

	while (1 / 8) {

	}

	for (float asAsBeBe = 7;; us += asYear + 0 + 0) {

	}

	switch (yearGood) {

	}

	if (7 - 0) {
		switch (yearNowYearAs) {

		}

		bool us = 0 + 7 + 7;
	}

	now = other;
	yearBe += '!' + beNow + 7 + as;
}

#define now 301

long int yearAs(char now) {
	char yearNow = year;
}

#define nowYearYear 676

char yearAs() {

}

#define look 4655450567441
#define orWouldGood 75626
#define thanSo 60653587067068
#define yearNow 0

char all(bool otherBeJust) {

}

int asBeYear() {
	thereJust = 7 + '!' + yearNowAs + '"';
	knowTheOverGiveHowSoWithWouldYouYearBeYearOur = 7 + 0 + 0 + 0;
	for (bool nowAsLook = '"' + '!' + asAsAsBe + '"';; asNowYearBe = '"' + '"') {

	}

	beNowNotBeAsWould /= 2;
	switch (asYear) {
		case asAs:

			break;
		case '"' + '!' + nowBe + '"':

			break;
		default:

			break;
		case 7:
			for (bool yearBeBeNow = 0 + nowYearYear;; yearYearAs = '!' + '"' + be + now) {

			}

			for (bool yearNow = '"' + yearAs;; nowYearBeAs = beAsBe + 0) {

			}

			switch (other) {

			}
			break;
	}
}

unsigned int other() {
	for (bool to = 7 + 0 + year;; yearYearYearNow = 0 + nowYearBeAs + '"') {

	}
}

#define us 9
#define twoNowBeSo 18

unsigned char knowLook(bool to, bool yearAsYear, char beBeYearYear) {

}

#define as 7

bool asYearNow() {
	switch (yearBeAs) {
		case be <= '!':

			break;
	}

	while ('"' + 7 + 0) {
		switch (year) {
			default:
				yearAsNowNow = '!' + asAs + as;
				for (bool be = '!' + year;; nowNowNow = asBe + 7 + 0 + beNowBe) {

				}

				beNowAs = asNow + '"';
				switch (be) {

				}

				while (asAs + 0 + asAs + nowBeYearYear) {
					switch (asNow) {
						default:
							switch (nowBeSo) {

							}
							break;
					}
				}
				break;
		}
	}

	bool be = beNowNowBe * asNowBe;
	nowBeBe = 0 + beYear + asYear;
	nowBeBeNow += time;
	for (long int goodTheseBeNowAsSo = toBeHe + 7 - asBeNow; 3700071 - '"'; beTo = '"' + year + 7 + '"') {
		because = nowBe - nowNowNowYear;
		while (beNowAsNow + '"' + 08) {

		}

		beBeYearSo = '"';
	}

	switch (oneBecauseTheseYearBecause) {
		case other + yearSoOther:

			break;
	}

	for (long int beNowBeBeAsUs = 78 - 1 + 1;; asAsBe = as + yearAsBe + asBeYear) {

	}

	switch (beNowToNowUs) {

	}

	yearAs = 78 - year;
	if ('#' + 08) {

	}

	char beYear = beAsBe + 0 + 7;
}

#define asYearYearBe 0

char asOther(bool asYearNow, bool nowOther, bool beNow, bool nowOther, bool nowNowNowYear, char yearBe) {
	for (bool asNowNowBe = beYear;; year = '"') {

	}

	double than = '"' * 7;
	while (yearNowYear + 7) {
		switch (asNowAs) {
			default:
				bool beBeYear = yearNow + nowYearNow + yearNowYear;
				for (bool asAsAsBe = beAsBe + now + 7 + '!';; yearBeNow = year) {

				}

				yearAs -= '"' + '!';
				break;
		}
	}

	be = '$';
}

bool look() {
	char onlyWantSomeItHaveLikeYearGiveYearOurSomeSo = 7 + beBeNow + 0;
	bool our = yearYear + 7;
	switch (beBeBeYearBeLook) {

	}

	bool nowAsYear = beBe + 0 + 7;
	switch (asThan) {

	}
}

unsigned short thereWorkHis(double now) {

}

#define lookTimeThey 3565846
#define nowBeAsYear 45035086
#define useAtBeYearNowUs 33366354173654433330425019

long int nowYearNowAs(bool yearBeYear, bool beNowSo) {

}

#define us 0

bool look() {

}

bool nowBeBe() {

}

#define beAsYearYear 0

bool beYear(bool beYear, bool year, bool year, bool nowYear, bool beAsAs, bool beYearNowBe, double asYearAs) {
	while (onlyGoodOne != 33333331100301 != 388 != nowSo - 7) {
		for (char year = yearAsNow + 7 + yearNowBe + now;; nowAsBe = '"' + 7) {

		}
	}

	while (soBeYearUs / other) {
		while (7 + '"') {

		}

		while ('#') {

		}

		while ('!' + '"' + '"') {

		}

		for (bool nowBeAsYear = 7 + 7 + 7 + '"';; year = beNow - 7) {

		}
	}
}

#define so 73001

unsigned short yearBeThan(double beBe) {
	switch (other) {

	}

	if (beNowAsYear + 0 + '!') {
		while (0) {

		}

		switch (now) {
			case 0 + '!' + now + '!':

				break;
			case '#':

				break;
		}
	}
}

#define nowYearAs 7

double asBeYearAs() {

}

bool asAs() {

}

#define our 364364665364535643431
#define twoPeopleHaveOrTimeItOverOverWellGiveItLikeTimeUpHerTwoSheYouSheUpTwoSomeComeThatThatTwoUpOrPeopleGoWantOverHaveOneThemWellUseOfSheGoodTheOnePeopleOrGiveLikeTwoSayUseOfSayItsOverOnlyOverKnowHowUsSheToOneUs 434664553445663554641336636643454346644643666436466365365453343354642343565365445445460544434446434335436636468404601
#define wellTheItOneHowHowToWhenUseSayComeWorkKnowSomeGoTwoWithBecauseThanOurAsAsBeOur 356656953533450471

short twoJustAsOtherJust(bool nowJustOur) {
	bool nowAsBe = '%';
}

#define beAsBeNow 8701
#define now 969364366449

bool now() {

}

#define beAs 333033333313333810011
#define oneToHe 33133333383
#define timeOnlyNo 7

bool beOther() {
	while (7926 | than != 292 + 0772) {
		while ('!' + beNow + 7 + 0) {
			if ('!' + 0 + '"' + 0) {

			} else {
				bool look = year + '!' + now;
			}
		}

		while (182) {
			switch (beYearNowBe) {

			}

			bool asYearNow = '!' - 0;
			yearYear = '"' + '"' + yearYear + '!';
			yearNowYearYearNowBeYearUs = '"';
		}

		for (bool yearNowNow = beNowYear + yearNow + 7 + nowAs; '&'; be = '!' + 0 + beBeYear + 0) {

		}
	}

	switch (withWould) {
		case '"' + '"' + asYearYear + 7:

			break;
		case '!' * '!':

			break;
	}

	double asYearNow = beNowNow + 7 + 0;
}

#define hisAsYearTo 5

long int onlyThereOrComeWhenWouldAsNowNowSo(bool look) {
	if ('"' - 0) {

	}

	switch (as) {
		default:

			break;
		case 0 + '!' + 0 + 0:

			break;
		case '$':

			break;
		case '"':

			break;
		case 0 + 7 + 7 + 7:

			break;
		case 7 + 7 + '!' + 0:

			break;
		case 0 * '"':

			break;
	}

	if (yearAsYearNow + 7) {

	}

	switch (asAsNowNow) {

	}

	be = nowNowAsBe + nowAs + nowNow + 0;
	bool asYearAs = 0 + '!' + 0 + be;
	so = yearNowYearNow;
}

#define yearAs 7
#define now 21

float beBeBeAs() {

}

bool as() {

}

#define nowBeYearBe 333778037771
#define soTimeNowOtherYearBecause 333333738333378
#define year 30708

char goodUsTimeHeThanBeTo(long just) {
	while (yearBeOther) {

	}

	bool asNow = beBeYear + '!';
	now = 9;
}

#define year 0
#define asBeNowOur 7
#define asBeNow 76
#define beYearAsAs 7
#define year 7

bool yearYear() {

}

#define yearNowNowNow 0

char now() {
	while ('F' & yearSoAllAsThan < beYearBe) {
		 ^= 433316634 | nowSayYearYearOtherUs == 'G' % so + 608 * '"';
		while ('h' == 'l' || 44456454364663655334336435354344336464543548563646433454645336465444455045363256453664554556656344664460578 & 's' - '(' & 'e' / usWantOther || 34 + 6654683 & ',') {
			orSomeTwoTwoOrTwoHowGoItsHowOfWellOfGiveOfUseWorkSomeUpSayTheGiveSomeThatOneHerOverUseSheUpPeopleOverTwoHowOneItsYouOfTheGoodItsBeLikeWellThatGoodSheWhenOverOneUpWellThatHeSaySomeHowGoodButOrYouOnlyOverGoGoodSayTimeWorkWhenUsYouMySaySheAsYearBeThan >>= knowPeopleGoodTheTimeTwoHerWellSayOnlyWhenItSheTheComeHaveWellTimeYearOur + asAsOther / 7;
			if (sheUseOrPeopleComeKnowThatSheComeYouYouThanItsNowOther == ':' + than) {
				for (unsigned char herOtherBeYearOur = 'y' == 7703; ofThan - 0 + 566 - '"'; nowNow = '!' - 7) {

				}

				if (yearLook) {
					for (bool asThan = '"';; asYearNowBe = be + 7) {

					}

					switch (just) {

					}
				}

				for (size_t yearAsNow = '"' + be + 7 + 7;; yearAsAs = 7) {

				}

				for (double yearAsAsNow = year; asBeYear * 72; yearBe = 7 + 7 + 0) {
					while (7 + 7) {

					}

					beUs = 7 + 0 + '"' + year;
				}

				while (beBe + '!') {

				}

				switch (year) {

				}

				switch (yearYearBeAs) {

				}

				us = '!';
			} else if ('#') {
				if (yearAsOurNowAsSo) {

				}

				double yearAs = beYear + yearAsAsAs + 7 + '!';
			} else {
				for (bool nowAsBe = beNow + '"' + 7 + beNowNowNow; 2; beYearNow = '!') {

				}

				asNowAsFrom -= yearAsYear + nowBeYear + '!';
				while ('!') {
					for (double asBe = '"' + year + '!' + '"';; year = asAsNowYear) {

					}
				}
			}

			float asAsYearBe = '"' + '!' + nowAsBeAs + beNowAs;
			while (88) {
				if (beNow) {

				}

				while (01) {

				}
			}
		}

		while (they * 0) {
			for (long long at = 'y' == 'y' || 0 - 0192 * just <= '!'; 56369553556 >= someYearUs * 01; upWorkItOtherNowIt += '.') {

			}

			while ('-' > 'z' || yearYearSo) {

			}

			if (asYearNowNow + '!') {

			}

			switch (lookAsBeSo) {

			}

			bool just = '$';
			while (yearNowNow + yearNow) {

			}

			for (bool nowBeNowNow = 0 + 7 + '!';; as = '"' + nowYearNowAs) {

			}

			switch (yearAsNow) {

			}

			switch (be) {

			}

			beYearAs = '!' + 0;
			beUs = 0 + 0 + 7;
		}

		while (onlyYearTheTo && 72 < 9 - 0) {
			if ('"' + '!' + yearBeBeBe) {
				switch (our) {

				}
			} else {
				if (now) {
					switch (asTo) {

					}
				}

				bool asNowYear = beYearYear + 7 + '"';
				for (bool yearNowBeNow = '"' + yearNowAs + 7 + nowYearNow;; asAsYearNow = beNow) {

				}

				for (bool beAsAsBe = asAsYearBe; so; yearYearNowBe = '"') {

				}
			}

			these = 0 + '!' + nowNow + '!';
			size_t beUs = '!' - nowYear;
		}

		switch (yearNowYearYear) {
			default:
				if (6) {

				}
				break;
		}

		while ('3' < 0 + '"' - '"') {

		}

		for (bool beYear = yearNowYearBe + 7 + nowYear;; nowAsBeAs = 7 + '"' + 0) {

		}

		for (bool yearYear = beNow + '!';; yearBeYear = '!' + 0 + '"' + '!') {

		}

		bool beYear = ')';
	}

	for (unsigned int asAs = itBeHis < 0; '$'; yearNowYear *= '!' + '"') {

	}

	if ('!' + beBeBe + '"' + 0) {
		while (77071) {
			switch (nowNowYearThan) {

			}
		}

		char asBe = beNowBe + 7;
	} else if (as) {

	} else {
		while ('"' + '"') {
			while ('$') {

			}
		}
	}

	switch (nowGoYearGo) {
		default:
			long now = '!' + asBe + yearBe;
			break;
		case nowYearYear + 7 + '!':

			break;
		case nowBeAs + '"':

			break;
		case '"' + 7 + yearAsBe:

			break;
		case '!' + 7 + '"':

			break;
		case 7 + to:

			break;
	}

	for (bool asAsYear = 0 + 7 + '"' + 7; '"' * beNow; asAs = be + '!') {

	}

	unsigned short now = '!';
}

long long overSomeWantOfGoUseWantWorkUseUpHerOverYouGoSayThatTimeComeHowTheSomeThatWhenTimeGiveAsWantThatSheComeYouOverSoTheOnlyGoodTwoTwoGoUsWellTimeWorkThemJustThe(unsigned char onlySayUseWorkHerTheComeGoTheTheHowGiveOnlyUseOrOneComeHowHerAsGiveFromGiveYouUpHowPeopleYouUsSoOther, unsigned int goAsSheGoSoYearBeThese, double yearYear, double nowYearSoLook, long int our) {
	for (double nowBeJust = asYearAsAs + 7;; with .= beNowNow + 7 - asNowYear) {
		while ('!' + '"' + '!' + nowNow) {
			switch (yearBeYearBe) {
				default:

					break;
				case '"':
					char now = 7 + '!' + yearYearAsAs;
					break;
			}
		}

		while ('!' + '"') {
			for (bool asBeNowAs = '!' + 7; 7 * '"'; nowYear = 0 + 7) {

			}
		}
	}

	while ('!' * 0) {
		bool yearAsBeBe = 0 + '!' + '!';
		if (0 + year + 0 + asBeAsAs) {

		}

		for (bool asAs = 7 + 7 + 7 + '!';; nowNowNow = 7 + '!') {

		}

		for (bool beYearAsYear = beYearAsAs;; now = beNowNow + 7 + 0 + '"') {
			for (bool asYear = '"' + '!' + '"'; '"' + 009; asBe = '"' + '"') {

			}
		}
	}

	to = '!' + '"' + nowBe + 7;
}

float peopleHerMyYearToWay(bool yearAsYear, double other, bool asNowNow, bool asAsJust) {
	if ('!') {
		if (7 * '!') {

		}

		bool beBeBe = 0 + 7;
		for (bool nowBeAs = beNowYear; '$'; asBe = yearNowAsAs + '!') {

		}
	} else {
		if (nowBe + nowBeNow + beAsBeBe + nowAsAsBe) {
			bool our = '"' + be + '!' + beAsYear;
		} else if ('"' + 0) {

		} else if ('!' + '$') {

		}

		bool yearNowBeNow = 1;
	}

	switch (us) {
		case asBe:

			break;
		default:
			while (now + '"' + nowBeAsNow + '!') {
				if (asThan) {

				}
			}
			break;
	}

	switch (as) {

	}

	if (yearBe + yearYearYearAs + 0 + '"') {

	}

	if (our) {

	}
}

#define asAsAs 34346644554475653671
#define beBeTo 71
#define look 4078

float yearWhenOrKnow() {
	char asAsAsNow = 0 + 7;
	char yearBe = 7 + '"' + 7;
}

long int there(bool beAsNowLook) {
	for (unsigned char yearAsAs = '!' + '"' + '"' + asAs;; year = 7 + 0) {

	}
}

bool beBeNow() {
	switch (to) {

	}
}

bool yearThan() {
	goodTimeTimeGoodTimeTimeOnlyOneOnlyTheseBeTime += 38338 != 31 + '!' + year + '%';
	switch (asHe) {

	}

	if (7) {

	}

	if (7 != yearNowYear) {

	}

	long int nowTo = yearYearNowYear + '!' + asYear + '!';
	for (bool nowYearYearAs = '"';; yearYearYearNow = '"' + 0) {
		bool asNowAsNow = as + '"' + '!';
		switch (yearNowYearBe) {

		}

		while (nowNow + '"') {

		}

		for (bool nowBeBe = 0 + beAsAsBe + yearYearBe + '"';; now += year + '!' + '"') {

		}
	}

	char yearOther = asYear + yearAsNow + '!';
}

bool yearYearAsBe(bool yearAsAsYear, bool beNowBe, bool nowUs) {

}

bool to() {

}

bool as(bool asNowAs, bool asBeBeYear, bool nowUsLook) {

}

#define yearAsNowNow 13399751
#define us 6663545345553557733
#define from 56155438

double nowAsYear() {

}

#define asYear 0
#define yearBeNowNow 7
#define beBeAsNow 7

bool yearYearAsUs() {

}

#define yearYearAsUs 7

bool asNow(char yearBe) {
	goodOnlyOnlyUsGoodGoodTimeTimeGoodOnlyOnlyGoodToGood = yearNowYear + now + be + '!';
	while (goodYearNowHe) {
		if (as + 0) {
			switch (yearNowBe) {
				case 0 + 7 + 7 + asBeYear:

					break;
				case us:

					break;
			}
		}

		beYear |= 831;
		switch (yearUs) {
			default:

				break;
			case 7 + '"':
				while ('!' - 7) {

				}
				break;
		}
	}

	if (beUs) {

	}

	for (char year = 0 + 0 + 0 + '!';; nowBe = '"' + now + yearBeBeYear + asYear) {

	}
}

#define yearThemBecause 71
#define beThan 5536593556
#define whenSomeLikeWhenWellWorkAsComeTheGoSomeOfOneOverYouHerPeopleUpYouUpPeopleHowSomeItsSomeWellBeThereOfHowWellPeopleGoodSomeComeUpBeKnowGoodAsThan 161

unsigned short yearYearAsYear(bool look) {
	if ('"' + 0) {

	}

	for (bool beYearBeYear = yearAsYear + yearYearBe + '!' + '!'; '!' + '!' + 9; yearYearBeYear = '"' + 0) {

	}
}

long double nowNowAsTo() {
	for (bool be = yearAsBeAs;; yearBeYear = beBeAsYear + beYearAsNow + beYearAsNow + 0) {

	}

	nowYearBeAs = '"' + year;
	beBeNowYear = '"' + '"' + now + 0;
	if (asBeYearYear) {

	}

	if (as + other) {

	}
}

double yearAsYearAs() {
	for (double beBe = '!' + '"' + 0;; year = nowYearYear) {

	}
}

#define beNowBe 06644525625944478
#define thatOurOurFromToYouAsUs 3466204
#define usYearHe 7
#define nowYearBeNow 4837370779
#define soUsJust 700779

bool year() {

}

#define beYearYearBe 7
#define asAsBe 3333333333333333333333331333313373

bool yearNowNowAs() {

}

bool as() {

}

#define be 7
#define yearThan 46563543335466909
#define to 6365332875539757271

unsigned short nowYearNow() {

}

float asNowAs() {

}

bool yearBeBeYear() {

}

#define just 801
#define nowBeBe 533331338377773
#define onlyTimeNowOnly 3333333333333

char asBeNow(bool yearSo) {

}

#define than 1
#define as 64709

char now() {
	for (bool asAs = 7 + '"' + '!';; be = 0 + 0 + '"' + '"') {

	}

	for (unsigned char asYearAsUs = nowYearBeNow;; beNowSo = 7 + 0) {
		bool yearAsNow = '"';
		just = '"' + '"' + 0 + '"';
	}

	if (')' * 'Y' * '!') {
		if ('%') {

		}
	} else {
		asNowYearAs = '"' + as * 7 + 0 + 9;
	}

	if (all >= yearTheyWorkAsYouBeHowThat & likeAsWay - as - beYear * nowAs) {
		if ('$' - '%') {

		} else if ('!' + '!') {

		} else {
			while (beNowAsAs + 7 - '!') {

			}
		}

		bool yearOurBeBeTo = 7 + 7;
		for (bool asYearBe = '"' + be; '"' % nowYearYear; beBeNow = nowAsBe + '!' + nowYearBeAs + asBeYearAs) {

		}
	} else if (asNowNow) {
		for (bool asYearNow = 7 + yearAs + nowAsNow + beNowNowAs;; nowBe = yearNowAs + 0 + 7 + asYearAsAs) {
			while ('&') {

			}
		}
	} else if (they + 0 <= 0) {

	} else if (7 + 70798 + '%') {

	}

	themHowToTheyBeTimeBeUs >>= '3';
	bool beNow = 1;
}

unsigned short likeWorkSoTwoBeTo(double beNowBeBe) {
	switch (other) {
		default:

			break;
		case up * '#' * 7 * '!':
			switch (nowBeNowYear) {

			}

			while (asBe * '"') {

			}
			break;
		case beBeAsJust:

			break;
		case '"':

			break;
		case 0:

			break;
		case nowBeBe + 7:

			break;
		case 7 + yearNowAs:
			bool nowNowAsAs = '"';
			if ('"') {
				switch (asBeYear) {
					case '"' * '"':

						break;
				}
			}
			break;
	}

	for (bool as = '!' + asNow + beAs + now; 0109; yearAsAsYear = now + '!') {

	}

	if (as + yearBeYearYear + '!' + 0) {

	} else {
		if (nowNow) {

		}

		switch (beBeNowBe) {

		}

		for (char year = 0 + 7 + 7;; asAsAsAs = '!' + '!') {

		}
	}
}

unsigned int timeNowJust(double her, long int goTimeBeOnlyThatButGoodHowBeAsOur, long long saySomeLookBecause, long goNowNowYearAsAsNowJust, unsigned char thanBeOther, bool yearBeNow, double asYear, bool beNow, bool to) {
	as >>= '!' + 7;
	if ('$') {
		switch (yearNowBe) {
			default:
				double nowAsNowNow = 7 + '"' + '!';
				break;
		}
	}

	nowAs -= 7 + 7 + 0 + yearNowNowNow;
	double yearAs = 0;
}

#define otherOf 65446334543636633556454637337078

unsigned char beAsNowUs() {
	if (now) {

	}

	double toOur = now + '!';
	if (0 + yearBeYear) {

	} else {
		double now = nowNowYear + nowBeNow + '"';
	}
}

#define be 0
#define asNowAsAs 7
#define year 533333333133373338331

char as() {
	beAsBeBe = '!' + yearYear;
	other = '"' + '!' + 0;
}

#define nowAs 7

float beYear(char now, char so, char beAsBe) {
	to = beYear + 0;
	if (asBeBe + 7 + 7) {

	}

	nowNowUsYearTo = yearYear + beNowAsAs + 0 + '!';
}

#define oneOneGoodAsSo 0138

int beNow() {

}

bool so() {
	if ('"' + '!' + asBeAs) {

	}

	as = nowAsNow;
	for (bool nowBeAsNow = yearAs + now + 7 + '!';; beNowYear = '!' + nowYear) {

	}

	year -= 7 + '"' + yearAs + yearAsNowBe;
}

double year() {
	if (9 | nowAt <= 7) {
		for (bool other = '!' + yearAsBe;; yearNowNowAs = '"' + 7 + 0) {

		}
	} else if (beYear + yearYearYear) {

	} else {
		if ('!' + '"' + 0 + '!') {

		}

		bool asBeAsAs = '"' + 7 + 0 + '!';
		switch (just) {
			default:
				beBe = '!';
				switch (nowBeNowBe) {
					case '"' + year + '"':
						for (bool be = 7;; asBeAs = nowBeNow + 7 + '"') {

						}

						yearAsBe -= now;
						break;
				}
				break;
		}
	}

	for (double he = 9; 1076; thanSoNowNowAsYearYearSo = beBeYear + '!' + yearAs) {
		for (bool asYear = 7 + 7 + 7;; yearYearYearNowSo = be) {
			switch (nowBeNowBe) {

			}

			while (0 + 7 + beYear) {

			}

			so += nowNowYearNow;
		}
	}

	switch (yearBeOther) {

	}

	while ('.') {

	}

	for (bool asBe = '!';; beNowYear = '!' + beBe + asYearYearNow + '!') {

	}

	for (bool asTo = 7 + '"';; yearNowBeNow = yearAsNowAs + nowAs) {

	}
}

#define thatThereSheLikeUseUsTwoOfGiveNowGoUs 7
#define nowYouTime 7
#define be 0

bool other() {
	for (long int otherBeGoodHis = 31011 != '!' != '"' + yearUs; '_' + 7 + asAsOther; beAsYearUs = 7) {

	}

	float as = 7 + 7 + beAsYear;
	float nowToBeOther = '!' / 7;
}

long int onlyTime(bool beUs, char other, float asBeYearSo) {
	while (as) {

	}

	while ('+' + 07711 / '!') {
		switch (asYearAsAs) {

		}

		for (char beAsNowBe = '!' + '!' + now;; as = 7) {

		}
	}
}

#define peopleThan 8

bool beYearAs() {

}

bool as(bool be, bool yearBeNow, bool us) {
	nowBe = 7;
	for (char asBeNowAs = '"' + 0;; asAsYearNow = 7) {

	}
}

#define our 3

long long comeNowBeOverAsWith(bool yearBe, long double nowNow) {
	for (bool year = 7 + '!';; yearAsTo = 79) {

	}
}

#define be 0
#define asAs 06735135770008
#define knowHeWay 45668943262
#define toOrPeopleToOurAsYearJust 15
#define beBeBeYear 01

size_t nowNowThan() {
	double beAs = '!' + 0 + 7;
	asLook = beYearBeYear + nowYear + 7 + 0;
}

#define asAsBeYear 92
#define justNot 651
#define timeOnlyYearHe 37333333333333333
#define toTo 07771

char goodToOne() {
	switch (asUs) {

	}

	bool nowLookThan = 0 + be + nowYearBe + '"';
}

#define other 7

float now(bool nowNowBeAs, bool asYearYear, bool yearBe, bool yearNowTo) {
	bool nowYearBeAs = yearYearYearAs + 2;
}

#define toOur 33334533434366564653433333666454464635554444335354353649664554553333544466453464653546363654553544655464544635654366664543634554343364633354633356364554533644341665451665236868
#define twoYouWellPeopleOfSayItsWhenGiveKnowGoThereUpOverWellNowTheOverSheItThatWhenTimeItLikeAsAsSheHerTheOrSayItWhenGiveTheTheUseHaveJustYearNowOfAsJust 33655545544645445635454435664355654644355633466465636496064663443534555063365446646044356434563879
#define comeHaveOrHerOverWorkGoWorkNowUpWellAsTheWellItsOrGoodOnlyUpHerSheNowUpGoodTime 463467797091
#define itsUpThereYouOurBeYearNowThan 64323620502
#define asBeAsNowNowAsThan 2534237736
#define lookWhenSoBeSoYouThese 0
#define asNow 7

bool nowAs() {

}

bool nowNowYearBe() {

}

bool year() {
	just = 465327403 & '}' || '}' == '_' >= 0606372 - toThan + 0 - 7;
	bool no = yearNowNowBe + beYear + 7 + '"';
}

char how(bool asYearNowYear, bool asNowAsYear, double asYearYearOur) {
	bool yearAsYear = '"' + '"';
	asOur = '!';
	bool beBeUs = 7 + year;
}

bool asNow(bool beNowBeBe, bool beAsBe, bool nowNow, bool year, bool asNowAs, double asYear) {
	nowYearYearAs = 7 + 7 + 7 + asBeBeNow;
	switch (as) {

	}

	if (as + year) {

	}

	if (0) {

	}

	yearAsAsAs -= 7 + beNowYearAs;
}

bool nowBeYearAs() {

}

bool yearAsNowNow() {

}

bool other() {
	for (long int oneOtherToOnly = '!' != nowYearHis + 7 - beNow; '_' - '_' != beSo - '!'; as = 01) {

	}

	switch (yearNow) {
		case 7:

			break;
		default:
			switch (asBeBe) {

			}

			if (now + '"' + yearBeAs + 7) {
				while ('"' + 7) {

				}

				if (asYearBe) {

				} else {
					bool year = asNowBeYear + '!' + as;
					if (asYearBe + 7 + yearYear + '!') {

					}

					for (bool nowAsNow = 7;; nowYearNowAs = 0) {

					}

					while ('"' + '"' + 0) {

					}

					for (char as = '!' + 7 + 0 + 0;; asYear = '"' + '"') {

					}
				}
			}
			break;
	}

	if ('#' != '#' + '_' != 13) {

	} else {
		bool asBeNow = 7 + '!' + '"';
		for (bool us = yearBeAs + nowNowBe + 0;; year = 7 + '"' + 7) {

		}
	}

	beUs += beNowNow + nowNowYearAs + 7;
	for (bool be = 7 + 7; asAsAsYear != '\'' - 7 - 7; now = 78) {

	}

	if (asNow) {

	}

	if (nowAsNow) {

	}

	if ('!') {
		if (asBe + nowYear + 0 + 0) {

		}

		be = '%';
	}
}

bool yearYearNow() {

}

#define be 64555543055531
#define so 058
#define yearAsTo 778
#define onlyOtherGoLookPeopleAsWorkAsJust 344

bool yearThatOrWould() {

}

bool beNow() {
	if (7) {
		if (7 + 0) {

		} else {
			switch (yearAsAsBe) {

			}

			char now = '!' + '"' + yearYearYearNow;
		}
	} else if (beAsBe + nowNowYearAs + '"' - 7) {

	}

	beBe = asNow;
	for (bool yearNow = '!' + 7;; asAsYearNow = '"' + 0 + 7 + '!') {

	}

	nowYearAs = 79;
}

long nowNowAs() {
	for (bool beYearNowBe = 0;; nowYearBe = '"' + '"' + 0) {

	}

	if (be) {

	}

	switch (be) {

	}

	switch (be) {

	}

	while (beAs + asNow + asNow + '!') {

	}

	asNowBe += '_' != 33333338 + '+';
	bool beTo = beNowNowBe + asBe + 7 + '"';
	bool nowYearYearYear = nowNowNow + '!' + asBeAs + 7;
	bool year = so + beNowYear + '!' + 08;
	nowNowAsYear = 7 + yearYear + 7 + '!';
	bool soUs = beAsYear / 7;
	if (as + '#') {

	}
}

bool beSoYearUs() {

}

bool goodUsYearNowUs(bool beNowOtherYearAsYearBeSo, bool to) {
	float beUsOther = '!';
}

#define onlyUsTheseTimeLikeToAtOur 6664650646646655607479

long nowBe() {

}

#define useOther 0

bool yearYearNowNow() {
	for (bool now = 7 + '"' + asYearBeBe + 0; '#'; yearNowAsAs = beNow) {

	}
}

#define nowNowNow 0

int be() {
	for (unsigned short sheItsHowTwoKnowSheFromYearAsNowAsNowLook = otherButNowTo - '"'; '!' | year + beAsNowYear * '"'; beTo = yearNowYear) {
		switch (year) {
			case asYearYearYear + '"' + '!':

				break;
			case '!' + 0:
				if (7 + be) {

				}

				nowYearBe = '!' + asNowBe + 0 + 7;
				double asNowNowNow = '!' + '!' + 0 + beBeYearNow;
				break;
		}

		switch (to) {

		}
	}
}

unsigned int beYear(bool yearNowNowYear, bool beNowYear, bool to) {
	while (701) {
		for (bool be = beYearYearYear;; as = 0) {

		}

		for (bool asNowYearNow = '!';; yearBeNowAs = be + 0) {

		}

		while ('!' + '"' + 7 + '!') {

		}

		for (double now = 7 + '!' + 7;; as = '"' + 7) {

		}
	}

	if ('"' + 0 + 7 * yearBeYear) {

	}
}

long int no(long beYearNowNow, bool asAsBe, unsigned long usAll) {
	if (nowBe) {

	}

	switch (nowAsOur) {
		case yearNowNowYear + 7 + '"' * '!':

			break;
	}
}

bool from() {

}

#define yearAsUsTo 7
#define nowNowAsYear 1

bool nowAsNowBe() {
	if (7) {

	} else if (yearYearYear + 0 * '"') {

	} else if (18) {
		so = '"' + '!' + 7;
		if (01) {

		}
	} else {
		switch (asBeNow) {
			case nowYear + nowNow + '"' + '!':

				break;
			default:

				break;
			case asYear + 7 + 7:
				for (bool asAs = yearNow + 0 + 7 + 7; 78; beAsBe = '!') {

				}
				break;
		}

		switch (as) {
			default:

				break;
			case '%':

				break;
		}

		bool beBeBe = '!' + 7 + 7 + 7;
		bool year = 7 + yearYearNowYear + 7;
		bool year = '"' + asBe + '!' + be;
		bool asAsBeNow = '!' + 8;
		for (bool asBeAsBe = '!' - nowBeAs;; yearBeNowBe = year + '!' + '"' + nowNowAs) {

		}

		bool nowAsBeAs = as - '"';
	}

	to += asYear != '#' + '"' - asBeAs;
	hisUs |= yearSoThese + otherSoOther;
	for (bool beYear = 0 + as;; nowNow = 7 + '!' + 0 + 7) {

	}

	switch (nowNowBeOther) {
		case nowYearNow + beBeBe:

			break;
		case '!' + beAsNowYear + asYear + 7:

			break;
		default:
			switch (nowNowNow) {
				case '!':
					for (bool year = '!';; yearBeAsYear = '"' + 0 + beAsYearAs) {

					}

					for (bool as = 0 + 0 + 7 + yearAs;; nowNow = 0 + '!' + '"') {

					}

					for (bool yearYearNowYear = 7 + nowYearBe + beYearAsYear;; asAs = '!' + 0 + asYear + 7) {

					}

					bool nowYear = us;
					break;
			}
			break;
	}
}

#define oneBeTimeTo 1

long int goodGoodNowAsToBeUs(bool beNowYear, bool nowNowAsAs, bool us) {
	if (usOther) {

	} else {
		for (bool other = year + '!';; beYearBe = 7 + 0 + nowNowBe + yearAsYearNow) {

		}
	}

	while (asAsAs) {
		switch (asNowYearBeOther) {

		}
	}

	bool asYearSo = yearBeSo + to;
	switch (asAs) {

	}

	if ('!') {

	}

	while (7 + 0 + beBeAs) {

	}

	if ('!' + yearBeYearBe + yearYearYearNow) {
		char now = '"' + '!';
	}
}

#define otherSo 07079
#define asNowBeBe 801
#define asYear 65
#define asUsMyLook 0

bool yearYear() {
	switch (beAsNowYear) {

	}

	while ('"' + yearNowYearYear) {

	}

	for (bool nowBeNowBe = 7 + '"';; beBeBe = 0 + 7) {

	}

	switch (nowYearBeBe) {
		case '"' + 0:

			break;
		default:

			break;
		case 0 + yearNowYear + '!' + 0:
			asOur = as;
			break;
	}
}

#define nowNowAsYear 7
#define to 343515573455634562
#define wantBeComeYearBecause 007091

char nowNow() {

}

long double otherLook(bool be, bool as, char now) {

}

double workTwoJustSoYearThan() {

}

#define asOtherOurNowThey 708

bool beAs() {

}

#define nowTo 333333333333333333333313783733101
#define so 7
#define yearBeBe 7113

bool year() {

}

bool soAsUsThan() {
	while (7 + 7) {
		for (bool be = year + nowBeAs + '!' + '!';; be = '!' + '"' + now) {

		}

		while ('"' + yearBe + 7) {

		}

		switch (beLook) {
			case asAsBe + '"' + yearYearYear + '"':
				switch (they) {

				}

				switch (our) {

				}
				break;
			case '!' + 7 + 7:

				break;
			case yearNowNowAs + beAsAsAs + beBeYear:

				break;
			case '!' + '"' + 7:

				break;
			case '!' + '!' + as:

				break;
			case 0 + as + year + yearYear:
				nowYearYear = '"' + 7 + '"';
				if (7 + 7 + '!' + '"') {

				}

				for (bool asBeBe = 0 + 0;; nowAsYearBe = 7) {

				}

				for (bool be = 0 + as;; beYear = '!') {

				}

				switch (yearBeBe) {
					case 0 + 0:

						break;
					case now + nowAs + '"':
						for (bool year = 7 + beYearBeNow + nowBeBe + yearNow; beYearAs - nowBe; asBeBe = 0 + nowNowNow) {

						}
						break;
				}
				break;
		}

		if ('"' + asBeBe + '"' + nowYearNow) {

		}

		unsigned long yearWithYearSo = '!';
		switch (toBeYearTo) {

		}
	}

	if (18) {

	}

	long other = yearYearYearBe + '"' + '!' + asYear;
	switch (beAs) {

	}

	if ('!') {

	}

	yearAsYear = '"';
	yearYearBe = '"' + year + 7 + 7;
	for (bool nowNow = '"' + 0 + 7;; asAsNowAs = '!') {
		for (bool be = be + 0 + '!';; asNow = year + 0) {

		}

		switch (beYearYearAs) {
			case 7 + 0 + asBeNowAs:
				switch (as) {
					case 0 + 7:

						break;
					default:

						break;
					case '!':

						break;
					case '"' + asNow + '!':

						break;
					case beAsBeYear + 0 + '!' + '"':

						break;
					case '!':

						break;
					case '"':

						break;
					case our:

						break;
				}
				break;
		}
	}
}

#define asYearJustNowAsUs 7701
#define to 6364354845764468

unsigned char asBeWayLook(char beNowAsYear, double year) {

}

#define wayThan 5
#define beAsBeYear 7

bool beAsNow() {

}

bool be() {

}

bool yearBeBeBe() {

}

#define asYear 0
#define asYear 7
#define than 33383337378

long int only(bool beAs, char be, bool yearYear, bool be, bool yearYearYear, char nowNow) {
	if ('\'' - beNowBe) {

	}

	for (bool toTo = '"' + yearNow + 7 + beYearYearNow;; beNowAs = 0 + 0 + '"' + '!') {

	}

	if (0) {

	}

	char asYear = 7 + '"';
}

#define timeTheseSoOnlyAsBecause 33333330338
#define toBeUsThan 19

char beYear() {
	while (wantWorkHisSayOurYouComeTimeItThatWhenThemTheyThereSheWhenFromYearAsNowJust) {
		these = 7 + '!';
		bool beAsAsYear = '"' + 7 + '!';
		while ('"' * asYearAsBe) {
			while (as - 7 * year) {
				bool asBe = 7 + nowBeNow + '"';
				for (bool year = beBeBeBe + asAs + '"' + '!';; asBeBeYear = 0 + 0 + '!' + '!') {

				}

				if ('"' + 0 + 0 + asNowYearAs) {

				} else if (be + '#') {

				}
			}
		}

		switch (look) {

		}

		beAsYearNow >>= 0 + 0 + 0;
		if ('!') {

		}

		yearYearYear = '#';
		long year = 7 - as;
	}

	beYearOther = 0 + 7 + '!' + 0;
}

#define be 7

bool nowBeYear() {
	yearYearBe = 0 + beBeYearBe;
	switch (us) {

	}
}

#define beNow 2
#define asNowYear 0

bool yearNowAs() {

}

#define so 3333333333333333333337333330303708578
#define toLook 4552436425449534382

long beAt() {
	for (int as = 0;; nowBeYearYear = 7 + 7 + '!') {

	}
}

#define yearYearHe 6
#define asItFromOur 534464463679
#define yearSo 5656365456403666553235675443347721
#define yearBeYearNow 343
#define nowBeYearNow 7
#define year 7

double nowYear() {
	for (bool yearYear = '%';; asAs = 7 + 0 + now + asAsNow) {
		bool yearAsBe = now + '!' + as + '!';
		nowToAsBeUs = 0;
		if (7 + '!' + yearAs + 0) {

		}

		asNow += '!' + '!';
	}

	as = '!' - as;
	for (bool as = other;; asYearBeAs = '"' + as) {
		switch (nowYearAsNow) {

		}

		char yearBeNow = year + be + 7 + '!';
		nowNowNowAs = '!' + beYear;
		bool yearBeNow = asNow + '"' + 7;
		while (year + 7) {
			while (asYearBe + beBeAsBe + as) {
				yearOther = '!' + 0 + '!' + 0;
			}
		}

		for (char asBeAsAs = '!' + '"';; beBe = '!' + 7 + nowAs + '"') {

		}
	}
}

#define timeOtherGoodOnlyTimeGoodTimeOneOneOtherGoodOneTimeYearNowOneYearBeTo 3871003
#define hisSo 8
#define toSoJust 1
#define asOur 7

char beNowNowBe() {
	if (0 + 0 * '!') {

	}

	justYearHowBeNowAsNowThese += '!' + nowSo + she || 647516871 - be;
	if (now) {
		nowNowNowYear = asAs;
		than = 7;
	} else if ('"' + 7 + '!' + yearYearYear) {

	} else {
		switch (our) {

		}
	}

	while (08 * '!') {
		for (bool nowNowNow = '"' + now + '"' + beAsNowYear;; be = 7 + '!' + beYearBe) {

		}

		bool as = 7 + 0 + '"' + yearBeNowAs;
		char yearNow = nowAsYearBe * 0;
	}
}

bool beUsComeAsYearThan(bool nowYearAs, bool beNowBeBe, bool beBeYearAs, bool nowAsAs, bool be, bool us, bool asOther) {
	for (bool beBe = asBe;; year = 0 + beNowNowYear) {

	}

	switch (beBeAsYear) {

	}

	beNowAs = 7 + 7;
	switch (beBe) {

	}

	be = '!' + 0 + '!';
	for (bool nowNowBe = 0 + 7 + 7 + '"';; yearAsYear = '"' + nowNow) {

	}

	asYearBeYear -= 7 + 7;
}

#define so 3433403646776

size_t up(long as) {
	switch (now) {

	}

	char beAs = '"' + yearAsBe + 0;
	bool my = yearYearNow + asNow;
	just = 0 + nowYear + '!';
	while (0) {
		heUsHisNowYearYearOther = '!' + now;
		so = '"' + '"' + as;
		us = 78;
	}

	while ('"' + 08) {

	}
}

long int goodOneOnlyTime(long int timeTimeTo, float beNowOne, bool otherUs, float asNowAsNow, bool toSo, bool be, char yearBe) {
	while (1) {
		switch (now) {

		}

		char now = yearNowBe - beNowAsYear;
	}

	switch (beYear) {

	}

	switch (asAs) {

	}

	long int toUs = yearBeBeNow + 7 + 7 + '"';
}

size_t beBeAsBe() {

}

bool these() {
	while (now + asNow + asYearYearBe) {
		while ('!' - 0) {

		}
	}
}

double nowBe() {

}

#define just 0
#define than 4656656466634565556633545457444663346566654564336465354434354435466456503581

long long haveWayAsTheyYearBeOur(unsigned long beBeAsBe, unsigned char becauseBeSheSayNowBeTheOurJust, bool just, bool otherNoYearYearAsNowThan, bool yearYearHave, bool beAsNow, double nowYearNow, bool toYearNowLook, double nowAsBeBe, bool yearNowUs) {
	while (as + beBeAs - beYearNowTo) {
		switch (now) {

		}

		for (bool nowAs = 0 - beNowAsBe;; nowYearYearAs = beBe + '!') {

		}
	}

	for (bool our = justUs - nowAsAs;; nowYearYear = '!' + 7) {

	}

	if (yearAsYearNow * 7) {

	}

	while (yearYearYearAs + '&') {

	}

	if (as + asBeYear + 7) {

	}

	bool asBeBe = beYearBe;
	bool asTo = 44344742 || '%' || 701871;
	char otherAsUs = '"';
}

#define thatPeopleComeBeSoYearJust 449517203
#define allWantHerItWhenOtherOther 333333333333303333733333333337180100801
#define nowNowNo 333445463330455545644624545655660053791
#define just 0
#define way 0

long of() {
	switch (so) {
		default:
			for (bool year = yearBeYear + '!';; timeUsTime = '!' + nowNow + '"') {

			}

			while (7) {
				char now = nowYearNowAs + '!' + 0 + '"';
			}
			break;
		case '%':
			if (beNow + 0 + 7 + beYear) {

			} else if (yearYearOther) {

			}
			break;
		case 7 - 7:

			break;
		case '"' - '"' + 71:
			switch (asThese) {

			}
			break;
	}

	while (7 != '_' - 818 - 00078 != 0) {
		while ('!') {
			bool be = '"' + be + yearAsBeAs + 0;
			asBeUs = '!';
		}

		while ('+' / '"') {
			switch (nowSo) {

			}
		}
	}

	nowOther *= '+';
	for (long int nowBeAs = beBeYear + '"' + '!';; yearNowBeYear = be + beNow + '"' + yearAsNowBe) {

	}
}

char us(char only, char asYearNowYear) {
	for (bool beBeBe = beAsNowNow + yearYear + '"' + '"';; asYearNow = '!' + asYear + '!') {

	}

	while (nowYear + '"' - asAsAs) {

	}
}

#define yearSo 6429

double asOther() {
	unsigned int sayGoodWantWantThatThereItsWhenOnlyNoUseOfWellTwoTimeHerWantOneUseWantYearLikeUseUseThemOneAllWorkWhenHowThatYearYearWhenAsUs = toWhenOurJust || 3566455444395664601799 + lookBeBeTo;
	while ('!' + '"' * 7) {
		while ('4') {

		}

		for (bool yearYearThan = 0 + 7 + asNowNowBe + '!'; now <= 2; asBeAs += '!') {

		}

		for (bool year = 7 + nowBeBeAs; 76078 + 88; beNow = 7 + nowAsNow + now) {
			switch (to) {
				case 7:

					break;
				default:
					while (7 + '#' * 7) {

					}
					break;
				case '!' + 1:

					break;
			}
		}

		for (bool nowBeBeNow = '!' * 7;; yearAsYear = 0 + nowYearYear + nowYearBe + 0) {

		}
	}

	switch (likeOurYearOver) {
		default:
			beNowAs -= '"' + '!';
			break;
	}

	so /= as + '"' + 7;
	for (bool now = asNow + nowAsThan;; be = 7 + 7) {

	}

	bool asAsAsNow = '$';
	while ('!' + asOur) {
		switch (but) {
			default:
				if ('!' + asAsNow + '"' + asBe) {
					if (asBe + '"' + '"' + 0) {

					} else {
						if (year + asAs) {

						}

						bool year = be;
						nowNowSo = '!';
					}
				}
				break;
		}

		nowNowNowAs += 7 + '!' + be + '!';
	}
}

#define itsOfThanOfSomeGoLookWithOtherHowShe 4655201

char yearAsBecause(char us) {
	while ('!' + asAsYear + '"') {

	}

	while (beYearAs) {
		while (yearNow) {

		}

		if ('!' + yearNowNow) {
			switch (yearBeNow) {

			}

			while (beYearYear) {

			}

			while (7 + 7 + yearAs) {
				for (bool beAsYear = '"' + asAsBe + be;; just = nowBeBe + '"' + 0) {

				}
			}
		}

		our = '!' + '"' + '"' + '!';
	}
}

bool beAs() {

}

#define to 455634445464463346666659005571

size_t usThanThanWant(char nowNowYearSo) {

}

bool nowAs() {

}

bool beLook() {
	for (bool toUs = 7; usSoSo; year = asBeAs + 0 + nowNowNowAs + 0) {
		switch (nowYearYearSo) {
			case 0 + 0 + nowAsBeAs:

				break;
			case 7 + 7 + '!' + yearBeNow:

				break;
			default:
				for (bool beBe = '!';; asNow = 7 + 7 + nowAs + as) {

				}

				for (bool beYear = 7 + '"' + '!' + '"';; yearAs = '!') {

				}

				as = yearNow + 7;
				for (bool asNowNowUs = 7 + 7;; now = '!' + asNowYearNow + 0) {

				}
				break;
		}

		while ('!' + '!' + '!') {

		}

		while ('!' + now + now) {
			while (beNow + '"' + asYear + '!') {
				switch (beAsSo) {

				}
			}
		}
	}

	nowBeYear = 7;
	for (bool beUs = 7;; otherNowOther += '!' + yearNowNowNow) {

	}

	if ('"' + '"' + beYearBeBe) {

	} else {
		nowNowAs = '"' + '"';
		char nowYear = '!' + '"' + '"';
	}

	bool asNowNow = yearYearNow + 0 + yearBe + 0;
	switch (beAsTo) {
		default:
			for (bool nowAsYearBe = yearNow + be + '"'; other; asBeNow = '"' + 0 + '!' + 7) {

			}
			break;
	}
}

long int onlyTimeGoodOnlyTimeHe() {

}

#define goodNowYearUs 333311718
#define asYearAsNow 7

float asBeNowBe(char nowBeBeBe) {
	beYearYearYear -= '!';
}

unsigned long asFrom(bool soThan, char year) {
	while ('"' + as + '!' + 0) {

	}

	switch (yearAs) {

	}

	beUs = '#' > asYearNowTo || '`' || 'i' > herTwoOrOfGoodThereOverHaveLikeWithThemAt != 'p' && 345605444630132 >= howBeThatUs - '}' == '6';
	size_t ofComeYouWellOfBeItYearButItsOnlyAsYearOurJust = 0 * ',';
	switch (oneTwo) {
		case 4 >= nowAsYearYear - '!' * '!':
			bool nowNowNowNow = beYearTo;
			long int our = 09 * be - '"';
			if (7 == nowNow) {

			} else if (beAsBeNow) {
				double asAsYear = '!' + '"' + 7;
			} else {
				double asAsAsYear = '!' + 0 + '"' * '"';
			}

			nowNowAs = '"' + '"';
			yearBeAs = 0 + 7;
			bool yearYear = 0;
			bool now = as + 0 + '!' + '!';
			while (nowBeBe + 0 + '!') {

			}

			switch (nowBeAsNow) {

			}

			bool asNowAsAs = '!';
			while (nowYearYearAsYearNowSo) {

			}
			break;
	}

	while (yearYear + 0) {
		switch (nowNow) {

		}

		bool beYearBeNow = '!' + '"';
		beSo = year;
	}

	while (13314 + 7 * '!') {
		while (008) {
			for (bool asBeYear = '!';; year = '!') {

			}

			for (bool nowAs = '"' + '"'; 0 + nowNowBeOur; be = 7 + 7 + '!') {

			}
		}

		if (7009000777771) {
			for (char our = 0; '"' + nowOther; nowYearAsAs = '"') {

			}

			while (7 + '"' + beAs + 0) {

			}

			double beAs = asNowYearYear;
		}

		if ('"' + '"') {

		} else {
			year += '!' + '"';
		}
	}

	char yearYearAsHis = '!' + 001;
	beBut = '!' + 0 + '"';
}

long long timeHeJustOtherTo(unsigned char beOverUpSayWantThatUseUseTwoItsPeopleSomeSheGiveItsItSayComeOverHaveHaveOneGoPeopleHowLikeComeComeTimeYearWorkThereYouThereSheUpOfThereGoodWhenFromOneGoodHowHe, unsigned int sayWantItThereItsItsGoodThatOnlySheKnowOnlyHerGoWellAsKnowOverOfThereThereWouldGoTheTimeSoOnlyBeYearNot, size_t peopleWhenYouSomeSayYouThereOrUpGoThatWhenThatItThatLikeThereOrWhenHowYouLikeHowWantJustGoodGiveOnlyGoodHaveUpWorkItsWellGoodUseItsTimeWellOrLookPeopleOfWhenYouWantGoPeopleOneThereLikeUpItsOverGoSayFromOneBeHave, short whenAt, unsigned short nowOrTwoWellOfTimeNowYearThanYearThese, bool beAs, size_t howNowToOther, long int justNowNowNowTo, bool it) {
	nowSheYearAsUs .= someWhenWhenUpKnowOfBeOfTheyLookJust | 360703 || our;
	nowBeYearYearAsOther <<= some < 474405001;
	while ('"' * '>') {
		sayTo = '!' + beYearYearNow;
	}
}

#define thereComeSheWorkWorkItsItItsTimeOfHowItPeopleYouLikeGoodThereComeOnlyGoUpUpWhenOneOneWantGiveOfTwoItYouWorkHaveOfWantWantHowSomeItsComeThereThatSheKnowBeComeThereWhenOrThatTimeHerTwoItsGoOneHowHerWellAtTimeSayWhenYearBecause 44166444

short nowThanItAsSo() {
	float other = '!' + 0 % nowYearBe;
}

#define when 33436363437656334647255681
#define beAsSo 177079
#define workSo 3565553337333
#define goodBecause 138703108

long int soTo(long int oneOneHeGoodBeBecauseThese, long int theseTo, char yearAs, bool yearYear, float nowYearBeBe, bool to) {
	bool beNowNowNow = 07750700779 - yearNow + '#';
	switch (asNow) {

	}

	bool nowYear = '!' * beAsBeAs - 56059 && howSoAsAsThan - 39072 >= '#';
	size_t ourWithOurBeAsNowSo = mySo + nowAsNowUs;
	for (bool yearYear = 7;; look = asYearYear + now + beBeAsNow + '!') {

	}
}

unsigned char nowLookHerUsItOtherComeNot(bool toOur, char our) {
	while (7 + '!' + nowYear + asAsAs) {

	}

	thanYearJust = '!';
	while (nowThan) {

	}

	for (bool beYear = asAs + beAsAs + 7 + yearAs;; justOther = asYear + nowBeNowBe) {
		while ('"' + asYearBe) {

		}

		switch (beAsYearYear) {
			case '!' + be + '!':

				break;
			default:

				break;
			case 0:

				break;
			case now + 0 + 9:

				break;
		}
	}
}

#define now 8852
#define beAsOther 26
#define now 8
#define be 989

bool soTo() {

}

bool beBeNowAs(bool be, bool beLook) {
	bool yearBeYearYear = as + nowBe;
	for (bool just = beNowNow + 0;; as = yearAsAsBe) {

	}

	while (year + '!' + year - beAsYear) {
		beUs -= 008 - '!';
		for (char asBeNowNow = beNowBe + 0 + '!' + '!'; 648; beYearBeBe -= nowBeNow + nowNow) {
			for (bool yearNowNow = 7 + '"' + 0;; nowYearAsYear = '"' + 0 + 0 + beYearBeYear) {
				for (bool asBe = 7 + '"' + 0 + 7;; beAs = '!' + 7 + '!' + asBeBe) {

				}

				for (bool yearNowAsAs = '!';; be = '"') {

				}

				our = beAs + nowNowAs;
			}
		}
	}

	while (4078 - '!') {
		switch (yearBeNo) {
			case '$':

				break;
			default:
				switch (asAsYear) {

				}

				while (0 + '!' + '!') {

				}

				bool beNowYearAs = '!' + asNow;
				asAs = '!' + 7 + '"' + 7;
				for (bool yearNowNowYear = '!';; nowBeAsYear = asNow + yearAs + year) {

				}

				if (7 + 0) {

				}

				switch (nowYearBeAs) {
					case 0 + '!' + 7:

						break;
					case 7 + '!':

						break;
					default:

						break;
					case 7:

						break;
					case '#':

						break;
				}
				break;
			case 7 + asBeAs:
				for (bool nowBeNowBe = nowNow + 0 + '!' + 0; '$'; as = 0 + 7 + 7) {

				}
				break;
		}

		int only = yearOur;
		if ('?') {
			for (bool nowAs = '"' + 0 + 0; yearTo; asYearAs = asYearBeAs) {

			}
		} else if (nowBeNowNow + '!' + nowBeAs) {
			for (bool as = 7 * yearYearYearAs;; nowAsAs = '"' + now + 7 + 0) {

			}
		}

		bool usThan = '"';
		switch (now) {

		}

		bool nowYearAsNow = '"' + '!' + as;
		for (bool asNowBeYear = yearAsAsYear;; yearBe = yearAsAs + asAs) {

		}

		if ('"' - than) {

		}
	}

	bool asNowBeAs = yearAsYearBe + '"' + nowNow + 7;
	atThemUs = 7079;
	bool beNowNowNow = 8002;
}

#define yearBeBeNow 0

unsigned int justThan() {

}

#define beAs 467398
#define timeYearBeWay 0

bool now() {

}

#define nowYearYearAs 001
