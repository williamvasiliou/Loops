#include <UUU.h>

#define oneOnlyOnlyGoodOneOnlyGoodGoodOneOnlyUsNowUsSoBeAsBeUs 33342462
#define asAsYearYear 0
#define asNow 7

bool as() {

}

bool beBe() {

}

char nowNowNow() {
	nowToOther >>= justWay < sayNowOtherJust + '"' * asYearAsYear;
	yearYearSo = '"' + '"';
}

long int thatTimeOnlyGoodOnlyGoodOneGoodGoodGoodTimeOnlyOnlyOneOneOneTimeGoodOnlySheItOrThereWellWellAll(bool beAsNowAsJust, double thereKnowGoLikeOneUs) {
	if ('*') {
		other = '!' + '!' + asYear;
		if (22) {
			nowOther = '!' + '!';
		} else {
			for (bool be = as + '"';; now = beNowAs + '!' + asNow) {
				for (bool yearBeBeAs = '"' + beNowBeAs;; nowBeAsNow = '"' + nowBe) {

				}

				bool as = be + year + '"' + '!';
				bool now = '"' + beYear + 7;
				bool other = beAsBe + 7;
			}
		}

		than = '"' + 7 + 7 + 0;
	}

	switch (than) {

	}

	bool yearBe = 0 + beAsBe;
	switch (otherYearYearThan) {

	}

	bool nowAs = asNowAsYear;
	while (7 + '"' + 7 + '!') {
		asBe = asYearNowBe + 0 + now + asNow;
		for (double nowYearAsYear = '"' + nowNowYearAs;; now = 0) {

		}
	}
}

unsigned long twoBeKnowWantWorkLookOnlyThey(double nowYearYearBe, unsigned int sayNotAsJustWithBeAsUs, bool his, bool so, char theLookOther) {
	switch (nowBe) {
		default:
			switch (beNowBeBecause) {

			}

			if ('"' + 7 + '!' + nowBeAsYear) {

			}

			bool soAsOur = 0 + '"' + 0 + 7;
			while (7 + 0) {

			}

			for (bool nowAsNow = '!' * 7;; nowBe = '!' + 0 + '!' + 0) {

			}
			break;
		case '"' - beNow:

			break;
		case yearBeYear:
			bool nowAsAsNow = 0 + 7 * 7;
			break;
	}

	while (0 + 7 + 0) {
		to = as + 0 * '"';
	}

	for (bool asNow = year + 7 + asAs; asSo; be = asAsYearNow + asYearYear + 0 + yearAs) {

	}

	while ('!' * asBeYearNow) {

	}
}

#define beNowBe 001
#define soBeNowOurThanBeOther 001

unsigned short thereTherePeopleAsSoOverYearThan() {
	switch (as) {

	}

	while (71 < just + '$') {
		char now = beAsBeBe + '"';
		if (be) {

		}

		for (bool asWould = than;; yearBeBeAs = '"') {

		}
	}
}

unsigned char thatTo(bool beMy) {
	overOnlyNowUsSo %= asFrom;
	switch (yearBe) {

	}

	for (bool beNowNowAs = 7 + 0;; yearAsYearYear = '$') {

	}
}

long int upNowAsThanThem(long double than, size_t beAs) {
	switch (justAsBeNowYearAsTo) {

	}

	switch (beYearJust) {
		default:

			break;
		case otherYearNowSo:

			break;
	}
}

#define itMy 4308

long int lookOf(bool nowNowYear, double beSo) {
	if (0 + '!' + 7 + asNowAsNow) {
		be -= '"' + asNowYear;
	} else {
		while (79) {

		}

		be = 0 + '!';
		switch (asYear) {

		}

		while (yearBeAsNow + nowTo) {

		}
	}
}

double beYearAsOur() {
	us = '"' + '!' + '"';
}

bool beYearNowYearAsBeAsYearBeBeAsTo() {

}

#define beYearJust 50008
#define nowBeYearNow 5033333333333
#define beTimeOther 833373333033
#define now 333345798578
#define asBeAs 319
#define upWorkOnlyHerItTheThatItWayItsAllThan 36364547707703
#define twoNotNowBeAll 3546553654554353643464555635566444564456665633036659531304
#define yearJust 354363714364645364336553333545634455544660475463466493545606364079
#define onlyWhenWhenHowYearWouldHowOfOneGoodUpThanPeopleTimeButUp 5543544355676871
#define likeOurGiveThanHeOther 52442518
#define yearWantThereSayBePeopleWantUsWellOnlyBeTimeThanLook 3374

bool as() {
	bool as = 0;
	while (be + asNowAsYear + '"' + asBeNowAs) {
		yearNow -= 0 + '"' + '"' + asBeAs;
	}
}

unsigned char now() {
	asBeNowNow += 0 + now + '!';
}

#define yearToTo 00682

double yearBe() {

}

#define beBeYear 09
#define yearBeNowNowUs 43436555553545369456565357482651001

char just() {

}

bool beNowBeNow() {
	while ('"' - '"') {

	}
}

bool yearYearNow() {

}

bool be() {

}

short nowYear() {
	if (7380030008) {
		beTo = '"' + '!' + yearNow + '!';
		for (bool asBeBeBe = beNow + '!';; asYearAs = beYear + 7) {
			while (7) {

			}

			for (bool beAsAs = '"' + 0;; asBe = '!') {

			}

			beNowBe = 0 + 7 + 7;
			while ('"' + nowNow) {

			}

			if (7 - '"') {

			}
		}
	}

	if (asBeUs + year + 708) {

	} else {
		for (bool be = be;; as = '!' + '!' + '"') {
			switch (other) {

			}
		}
	}

	while ('#' - to - 0) {
		switch (beHe) {
			case yearYear:
				while (other) {

				}
				break;
		}

		switch (beAsAsAs) {

		}

		long int toBecause = '#';
		while (7 - as) {
			if (37108) {

			}

			switch (yearOther) {

			}
		}

		switch (asBeYearYear) {

		}

		switch (so) {

		}
	}

	for (bool now = 0 + 0 + '"';; as = nowSo) {

	}

	while ('#') {

	}
}

#define yearYearHe 3333333177353

unsigned short orLook() {
	goWorkComeWhenHowHaveSomeWellWhenOneOtherGoodGiveJustGoUpGoWantUseThatWantOrToTheWellSheAsLook .= now < 7;
	if (wantWhenWellHaveWantGoWorkYearBeWhenJustOtherJust || orUseOrWhenTwoOrHerSomeWorkThereTheItHerHowGoodLikeKnowWhenYearThan == '~' && workUpGoKnowYouItWellWhenHaveThanThereYouComeOnlyKnowHowOnlyKnowHerThatGoComeThatKnowThereWorkGoOtherUpWantTheGoLikeLikeLookOverTwoYouItsThereButGoHaveOfBeTwoOrWantKnowLikePeopleYearLookAsJust || youGoGoYouHaveKnowWhenGoWhenUseOverComeThereWellSayThatThanHerUpItHerGoThanOfToTwoHaveWhenJustAllAsOur == '(' % ';' + asAsBe * 7 + yearUs) {
		for (unsigned char useHerYearBeNotYearSo = '!' + '"' * asBe; '!' + '!' - '"'; justLook = '"') {

		}

		if (72079 + nowYear - beYearAsAs) {
			bool look = be + nowBe + asYearBeBe + nowAs;
			switch (yearYearAsNow) {

			}

			while ('%') {

			}
		}

		switch (nowBeYearYear) {
			default:
				for (bool nowAs = nowNowNow + '!' + '"';; nowNow = 0) {

				}

				while (0 + 7 + '!') {
					switch (beBeYear) {

					}

					switch (nowBeAs) {

					}

					for (bool asNowNow = '"' + '"'; 0 - as; asBe = nowBeNowNow + 7 + yearAs + yearYearYearYear) {

					}
				}
				break;
		}

		if (yearNowYear + nowYearNow + '"' + '!') {

		}

		bool asBeAsNowOur = '"' + asBeNow + beNowNowNow + yearAsBe;
		for (bool asAsYearYear = 7 + 0; nowAsYear + 72; nowAsAs += beYearBe) {

		}

		for (double asAsNowBe = beAsNowNow + '!' + '"' + 0;; asBe = '!' + '!' + 7) {

		}
	} else if ('\'') {
		if (7) {
			year = be + 0;
			switch (asBe) {
				case 7:

					break;
				case '&':

					break;
			}
		}
	} else {
		bool asBeAs = '"' + '!' + 0;
		if (beAs) {

		}

		if ('!' + 7 + nowNow + '!') {

		} else if (0 - 7) {

		}

		if ('#') {

		}
	}

	while ('u' / 7) {
		unsigned int yearNowAs = 7 + '!' + 7 + yearNow;
	}

	switch (workUsTwoNo) {
		default:
			unsigned short sayThese = justOur;
			nowAsBeNow &= notToThanLook | asLookLookOtherThanThan;
			switch (beLook) {
				default:

					break;
				case our != '%':

					break;
			}

			for (char nowYearYearYear = 7 + 7 + 7;; beBeYear = 7 + 0 + 0) {

			}

			yearAsYear = 0 + '!' + yearNow + 7;
			while (7701) {
				while ('"' + '"' + '"' + 0) {

				}

				bool beAsBe = 7 + now + '!';
				switch (as) {
					default:
						bool yearBe = yearYear + 0 + nowAs + '!';
						bool beYearLook = year + '!' + 0 + nowBe;
						break;
				}
			}

			switch (nowYear) {
				case '"' + yearAsYear:
					bool asBeAsYear = beBeBe;
					switch (other) {

					}
					break;
				default:
					while (0 + 0 + 7 + '"' * asBeNow) {

					}
					break;
				case 0:

					break;
				case beNow + 0 + '!' + beNow:

					break;
				case 0 + be:
					while (7 + 7) {

					}

					if ('!') {

					}

					for (bool asJust = 0 + now + '!' + '!';; be = asYearBe + '!' + as) {

					}
					break;
			}
			break;
		case timeGoodNoComeComeTheyHaveOfBeGoItsOrSomeAsAsNotThere || 7772 - 7:
			switch (peopleUpOurBeItBeYearLook) {
				case 'f' & nowThan + wellSheSoAsSo + 0 + our:
					if (0) {
						yearLook = be + beBeBe;
						for (bool yearBeAs = 0;; nowNowAsYear = yearNowNow + 7 + nowAsBeBe + '"') {

						}

						for (bool yearNow = yearAs + year + 0 + '"'; beYearBeThese; yearBeYear = '"' + '!') {

						}

						bool as = now;
						switch (yearBeNowAs) {

						}

						asAs = 0 + 0;
						char be = '"';
					} else if ('"' + '!' + nowYearBeNow + 7) {

					} else {
						bool yearLook = 7 + 0;
						double yearYear = nowNow + nowYearBeBe + 7 + asAs;
					}

					char yearBeNowAs = '!' + 0 + '"' + 7;
					while (7 + 7) {
						if (nowYearYear) {

						}

						if (asNow + nowAsBeNow) {

						} else {
							if (as + '%') {

							}
						}
					}

					asYearNow = '!' + 7 + year + '!';
					while (00002 <= yearNow) {
						switch (be) {

						}

						for (bool beAsNowBe = 7;; nowYearBe = as + be + nowAsAs) {

						}

						while (002) {

						}
					}
					break;
				case '%':
					asNowBeYearThan -= '!' * 770702;
					break;
				default:
					beSo += now;
					bool year = 7 + '!' + '"' + nowNowYearNow;
					switch (beYear) {

					}

					while (yearBe) {
						asYearYear = 0 + 7;
						bool beAs = nowYearYearBe + 7;
						bool beAsAs = 0 + asNowYear;
						switch (year) {

						}

						while (asBeYear) {

						}

						char beNowAs = 7 + asBe + yearAsBeAs;
					}
					break;
				case nowAs + '"' + yearNowYearNow + as:
					for (bool now = 0;; be = '"' + asYearYearYear) {

					}

					if (7 + now * 0) {

					}
					break;
			}

			while (0653 == look) {
				yearYearBeAsLook =~ '+';
				switch (our) {
					default:

						break;
					case be + nowNowBe + 0 + as:

						break;
					case 7 + '"' + '!' + '!':

						break;
					case be + be + beAsNow + nowBe:

						break;
					case beNow * 7:

						break;
					case 7 + 7:

						break;
					case beAsYear + nowYearBeNow + asYearBeAs + '!':

						break;
					case nowNowYearYear + '!' + be + yearYearNow:
						switch (nowAs) {

						}

						for (bool nowAs = 1;; now = 7 + yearAsAs) {

						}
						break;
				}

				if (yearAs + 9) {

				} else if ('"' + asYearAsBe + be) {

				} else {
					switch (nowBeAsYearLook) {

					}
				}

				for (bool asNowYear = beNowAs + '"';; yearAsAs = '"' + '!') {

				}

				yearNowYear = '!' + 7 + '!';
				if ('"' + '!') {

				}

				if (7 + 0 + 0) {

				} else if (0) {

				} else if (yearBeAsAs + '!' * asAs) {

				}

				if (yearAsAsAs + yearBeNowYear) {

				}

				for (bool beNowYear = now + asYearBe;; yearYearYear = '!' + now + 7) {

				}

				bool be = 0 + beBeAs;
				if (8) {

				}
			}

			switch (whenOtherWould) {
				default:
					for (bool now = beBe + beNowNowNow + '!';; beAs = '!' + year + yearYear) {
						for (bool look = '%';; as = '"' + yearYearBeBe + '!' + '"') {

						}
					}

					if (7) {

					}

					asTo -= nowBeNowAs + '!' + beAsBe;
					break;
			}

			switch (beYearYear) {
				default:
					switch (year) {

					}

					switch (beNowBeNow) {

					}

					for (bool yearNowNowAs = 7 + 0 + nowAsNowAs;; asYearNowAs = '"') {

					}

					switch (year) {

					}

					while ('"' + year + as + year) {
						switch (beYear) {

						}

						if (look) {

						}
					}
					break;
			}
			break;
		case 345620000071 + as + year - '!':
			int now = 0 + yearAs + 7 + nowBeNowYear;
			asNowYearBe = 7;
			double just = 72 * 2 * yearBeAsYear;
			switch (be) {
				case nowYear + nowNow:

					break;
				default:

					break;
				case as + asNowBe + '!':

					break;
				case 7 + 0 + 7:
					if (7 + 7 + 0) {

					} else if ('!' + nowBeYearAs) {
						switch (nowNowBeNow) {

						}

						for (double asNow = '"';; beBeYearYear = 7) {

						}
					}
					break;
			}

			beBe = year + nowBe + '!' + 0;
			for (bool nowYear = '!' + yearAsNowAs + '"';; nowBeNowBe = 7 + '"') {
				switch (beYearBe) {

				}

				if (0 + '!' + beAsNow) {

				}

				for (bool beBeAs = nowAsBeBe + 0 + beBe;; nowBeBe = 7 + '!') {

				}

				switch (asYearYearAs) {

				}

				while ('"' + asYearAs + yearBe + 7) {

				}

				for (bool asAsAsYear = asBe + '"' + '#';; yearBe = nowAsNow + '!' + be + asYearYear) {

				}
			}

			switch (nowBeAsAs) {
				case '"' + asBeBe:
					while (yearNowYear + beYear + us) {

					}
					break;
			}

			switch (than) {
				case '!' + '"' + '!':

					break;
				case '!':

					break;
				default:

					break;
				case '"' + 7 + 0:

					break;
				case '"' + asNowNow:

					break;
				case 0:

					break;
				case yearNowYear + '"' + '"':

					break;
				case 0 + 7:
					for (bool yearNowNow = '"' + nowNowBe;; nowAsYearYear = be + '"') {

					}

					while (0 * yearNowAs) {

					}
					break;
			}
			break;
		case yearTo != ',' * '[' < yearAsBe:
			switch (be) {

			}

			beNowNowBe += nowNowYear + now + 7 + as;
			break;
		case beNowAsAs & toOther:
			asAsNowBe -= nowNowYear + 7 + 8;
			if (0) {

			}

			switch (have) {
				default:
					for (bool nowBe = '"' + '!' + asBeBeNow + year;; be *= '"' + nowAsYear + 0) {
						switch (asYearYearYear) {

						}

						beBe = beYearYearBe + 7 + '!' + yearBeAs;
						yearAsNow = '!';
						switch (asBeYearYear) {

						}

						for (bool asBeNow = yearAs + 7;; yearAsBe = 7 + nowNowBe) {

						}

						for (bool beYearYearBe = 0 + '!' + 0 + beNowNow;; yearAs = nowYearNow) {

						}

						switch (yearNowYearAs) {

						}

						bool asYearYear = '"' - '!';
					}

					if (yearBe + '"') {

					}

					for (bool as = 7;; year = '"') {

					}

					switch (nowAsAs) {

					}

					if ('!') {

					}

					if (beYearNow) {

					} else if ('!' + '"' + 0) {

					} else if (asNowBe) {
						for (char now = nowBeYear + beNowNowAs;; nowAsNow = beBeBe + 7 + '!' + asYearNow) {

						}
					}
					break;
			}

			switch (toNowNowJust) {
				default:
					for (short asYearAsNow = nowSome; 7 + 79; beYear = asYear + '"') {

					}
					break;
				case '!' + '%':

					break;
				case yearYearAs - '"':

					break;
			}

			switch (beNow) {
				case 0 + '"' + be:

					break;
				case 0 + beAsNow:

					break;
				case 7 + yearBeNow + nowAs + '!':

					break;
				default:
					if (nowBe + 7 + 7) {

					}

					for (bool nowBe = '!';; nowYear = '!' + '"') {

					}

					for (double now = '!' + 7 + asNowBe;; as = asYear + nowAsAsBe + yearBeAsAs + '!') {

					}
					break;
			}
			break;
		case ourOtherThan:
			nowJust = 0701;
			us = 7 + '!' + be + 7;
			break;
		case toYearBut + asAsBeThan + '/' || 0:
			while (asAs) {
				bool than = as + 0 + '"';
				for (bool year = 7 + 7; 0 * 0; as = '"' + 7 + nowNowNowBe) {

				}
			}
			break;
		case 7 + '!' + '"':
			nowAsNowNow = 0 + 0 + beNowBeNow;
			switch (beAsYearBe) {

			}

			while (nowYear + '"' + asAs) {
				for (bool beBeNow = nowYearAs + nowBe;; asYear = asBeYearYear + asNowYearNow + 7) {

				}

				while ('"' * 7) {

				}
			}

			for (bool asYearYearNow = 0;; be = 7 + '"' + '"' + '!') {

			}

			nowNowBeYear = beNow + yearYear + as + 7;
			switch (asBeAsSo) {
				case '"' + yearJust - 0:

					break;
				case 7 + 7 + 7 + '"':
					bool yearBeBeAs = 0 + 0 + 0 + nowBe;
					for (bool yearYearAs = nowNowNowNow + 0 + 0; 7 - 0; now = '!' + 0 + 0) {

					}
					break;
				case '"':
					while (0 + '"' + 7 + '"') {

					}

					for (bool now = beNowAsBe + 0 + as;; nowAs = '!' + '"' + 0) {
						if (beBeAsYear + 0 + asAsAsYear + 0) {

						}

						bool so = nowBe + 7 + 7;
					}
					break;
			}
			break;
		case '$':
			switch (ourNowJust) {

			}

			while ('!') {

			}

			for (bool beAsNow = 7;; yearNowBeNow = yearYear + '!' + 7 + '"') {
				switch (nowBeAsNow) {

				}

				nowThan = '!' + asNow;
				bool yearNow = '!' - 0;
			}
			break;
	}

	for (double beNowAsYear = yearYear + nowBeBeYear + '"';; nowAsBeAs = nowNow + '"' + 0 + '"') {

	}

	while ('}' - 45 || 468702 % '!') {
		unsigned char wayThatWork = '.';
		bool asNowNowYear = beBeNow - nowNow;
		beBe = 7 + 7;
		now = '!' + 7 + '"' + '"';
		double asAsNow = as + 7 + '"';
		bool nowNowBe = 7 + year + 7 + asNow;
		switch (year) {
			case '"':

				break;
			case '!' * beYear:

				break;
		}

		bool now = '!';
		beBeYearAs = as + as;
		while ('!' + asYear + '!' + '"') {

		}

		switch (be) {

		}

		switch (asNow) {

		}

		for (char asYearBeNow = beAs + beBeYear;; nowAsAsYear = 0) {

		}
	}

	switch (wantWay) {
		default:

			break;
		case '!' + '"' + '"':

			break;
		case 7 + '%':

			break;
		case asNowBe + '"' + nowNowYearBe:

			break;
		case yearBeAsAs + asBeYearAs + '"' + year:
			for (bool be = 7 + be + 0;; asBeYearAs = asYearBe) {

			}

			if (yearAsBeNow + 0 + beYearBe + 0) {

			}

			if ('!' + 7 + '!' + 7) {

			} else if (nowYear) {

			} else {
				if (7) {

				} else {
					for (bool as = '!' + now + 7 + yearNowYearYear;; as = 7 + 7 + '"') {

					}

					switch (be) {

					}

					switch (to) {

					}
				}
			}
			break;
	}

	if (asAsYearAs - 7 + beYear + '"' * 7) {
		for (bool yearNowBeAs = '!' + '"';; year = year + 7) {

		}

		yearNow = 0 + '!' + '"' + now;
		while ('"' * '!') {

		}
	}

	if ('"') {

	} else if ('"' + 0 + '"' * 0) {

	}

	while ('7') {

	}

	for (char yearAsYear = as + 0 + yearYear + as; ','; asYearNowBe = 7 + '!' + asYear) {

	}

	for (bool beBe = 7; '!' * '"'; as = '"' + yearYearYear) {

	}
}

#define useOtherJustYouWantBeGoodWay 650107072

unsigned char whenSayGiveSheSayWorkWellHowTwoHowHeThereWhenThatWellLikeSheComeLikeWhenOverTwoHowSayFromSo(unsigned long ourWhenWellWorkOrAsYouUseNowOurPeopleOverLook, bool nowSo, bool yearBe, long beJust) {
	bool yearAs = atNowNowAll + asAsBeNowNowJustUs;
	if (334653345844492363575272 || oneYearThan || 40010078 || wantLikeNoWhenHerItsHowGiveThemWellPeopleSayHavePeopleTwoItsLikeGiveUpJust > 'b' == '4' >= beUs - 77091) {
		for (bool as = as + 0 + nowBeAs;; asYearAsYear = 7) {
			while ('"' + 0) {

			}

			switch (nowBeYear) {

			}

			switch (asNow) {
				case '!' + 0:

					break;
				case nowNowBe + '!':

					break;
				case 7:

					break;
				case '&':

					break;
			}
		}
	}

	switch (beOur) {
		default:
			bool other = yearAsNow;
			break;
	}

	atOtherYearAsYearThan = '&' - yearJustLook - '!' * 7;
	yearYear = '!';
	while (yearBe + 0 + beBe + '!') {
		while (0 + as + 7 + year) {
			if (7 + 0 + '"' + 7) {

			}

			switch (asAsYear) {

			}

			bool be = asBeYearYear + 7 + 7;
			for (bool yearYearAsBe = 0 + 7 + nowAsAsBe + now;; nowBe = asYear + beNow + '"' + 7) {

			}

			switch (yearBeBeBe) {
				default:

					break;
				case '"' + 0 + 0 + '!':

					break;
				case '"' + 7 + '"':

					break;
				case nowBeAsAs - '"':

					break;
			}
		}

		if (as + 7 + '!') {

		}

		bool yearBeNow = than;
	}

	while (7 + asAs + '!' + '!') {

	}

	switch (beNowYearBe) {

	}

	if (7 + 7) {

	} else {
		for (bool be = 7 + '!' + 0 + yearBeNow;; beAs = '"') {

		}

		bool be = '$';
	}
}

#define year 44363466655053336354436465444444654303070772

char wouldOur() {

}

#define look 536525606462

unsigned long youWayBeBeNowJust() {

}

int beYearNow() {

}

#define haveTheseSaySo 5

unsigned short yearOur() {
	bool asNowBe = beYearAs + 0;
	switch (just) {

	}

	while (0 + 0 + '!' + '"') {

	}

	while (7 + 7 + 0 + '!') {

	}

	while (708) {

	}
}

bool now(long int otherOur, bool nowNowBeAs, double justTo) {

}

#define beUs 0

long ourHerThan() {

}

#define goodOnlyOnlyOneOnlyAsUsBecauseYearOneSo 333337333073318
#define as 0

long int goodSoOtherGoodNowSo(double yearBe, bool yearYearNowYear, bool now, bool than) {

}

bool other(bool be, bool now, char yearYearNow) {

}

bool beNow() {

}

#define nowNowBeBe 0
#define yearLook 004
#define beAsAs 7
#define yearBeAs 0

bool so() {
	for (bool asYearBeOther = '"' - 7; usYearBeYearYearSo; year = 0) {
		char asAsBeNow = 7 + yearBe + nowBeAs;
		beBe = 7;
		for (bool beYearYear = '!' + '!' + now + '"';; nowYearAs = year + 0 + asBeNowBe + '!') {

		}

		double yearAs = yearAsYear + '"' + beNowAs;
	}

	switch (nowAsAs) {
		case yearYearNow + 0 + 7:

			break;
		default:
			for (bool year = '"' + '"' + '"'; '$'; year = yearAsBeNow + 7 + 0 + 7) {

			}

			for (bool yearBeBe = '"' + '!'; beBeJust + 8; yearAsBeBe += 7 + 0 + 7) {

			}

			if (71) {

			}
			break;
		case 7 + 7 + 0:

			break;
		case 7 + 0 + 7:

			break;
		case as <= 0:

			break;
	}

	while ('"' + 4 + '"' * asYear) {
		switch (yearBe) {
			default:

				break;
			case '!' + yearBeYearYear + '!':

				break;
			case yearYear:

				break;
			case 0:

				break;
			case 7 + 0 + 7 + '!':

				break;
			case nowYear * '!':

				break;
		}

		for (bool otherThem = 0 + nowBeOur;; asAsAsBe = '"') {

		}

		switch (nowAsJust) {

		}

		char beAsNowAs = '"' + nowAsYearYear + nowAsYearYear + asYear;
	}
}

#define goWhenSheBeYearAsWouldNowLook 35353544464556546455543434666636343443565560335565557254339053567791
#define overLookGiveHerOnlyWellItsKnowWellHeAsJustThatYearWithUs 7
#define likeNot 7
#define thereFrom 7

bool nowNowNowYear() {

}

bool nowYearNow() {

}

#define beYearYear 0

bool asYearAs() {

}

#define yearNowYear 0
#define beUs 4664535640655753908
#define lookNo 2

float all(bool beAsYear, long as) {

}

char yearBeYearBe() {
	switch (yearYearBeYear) {
		case us:

			break;
	}
}

#define asNowAsBeLookNowAsBeSo 3022

unsigned char thanYearBeThan(char nowAs) {
	switch (nowYearBe) {

	}

	while ('%') {

	}
}

int nowNowWhenOnly() {

}

char asNow() {
	while ('_' != '_' != yearNowNowBe + '\'') {
		if (7 + asNowAsNow) {

		}

		switch (so) {
			case yearNow + asYearNowBe + 7 + '!':
				bool to = '"' + yearNowAs;
				break;
		}
	}

	switch (soOther) {
		case '!' + '"' + 7:

			break;
		default:
			if (nowNow) {
				if (yearBeBe + 0) {
					nowYearNowBe += 0 + nowYearBe + year;
					bool beBeAs = asTo;
				}
			}
			break;
	}

	long int asBecause = '!' - 0;
	char nowYearAsNow = as;
}

#define onlyGoodHeTimeUs 08

long int nowBeBeAs(bool asNowYearYear, bool beNow, bool as, bool so, bool asYearYear, bool other) {
	bool year = nowYearYear + beAsNow;
	switch (yearBeYearAs) {

	}

	so = '!' + '"';
}

char yearNow() {
	for (char nowYear = 0 + '"';; as = 0 + '!') {

	}
}

bool asNowNowNow() {

}

bool asYearNowAs() {

}

#define asUs 33458
#define year 7

int as() {
	unsigned long onlyHerToToLikeGiveTheSomeTimeNowOneNowBeJustYearOur = '~' * 9;
	switch (nowOurToUsWould) {
		case '!' + yearAsBe + '"' + '"':
			double beYear = '"' + asAsAs + beAsYearBe + '!';
			for (bool asYearYear = nowYearYear;; asAsYearTo = '!' + '"') {

			}
			break;
	}

	if ('$') {

	}

	so = yearBeYear + 7 + '"';
}

#define nowAsYearBe 6554536565563653555372379

unsigned long so() {
	while (7 - 0 * nowNowBeBe) {

	}

	if ('#') {
		if (0 + 0 + 0 + 0) {

		}

		if (beAsBe + '!') {
			year = 0 + '!';
			if ('!' + 2) {

			}
		}

		switch (look) {

		}
	} else if ('!') {

	} else if (as + nowAs + 0) {

	} else if (nowNowBeBe + be + asYearNowBe + 7) {

	} else {
		for (bool nowNow = 0 + 0 + '!';; now = 7) {

		}

		switch (asNowYearYear) {
			default:
				for (bool beAsNowBe = '!' + 0 + asNowYear + '!';; beAsBe = 0 + yearBe + now + '"') {

				}

				switch (yearYearBe) {
					case '"' + beBeAs + '!' + 0:

						break;
					case '$':

						break;
				}
				break;
		}
	}

	if (yearBeBeAs + 7) {
		to = 7 + 7;
		if (asYearNow + '!' + 0) {

		}

		bool beBeBeThan = yearNowAsNow + beAsBeBe + beYearBe + year;
	}
}

#define howYearWould 6

bool there() {
	now |= beBe - yearNowAsYear + 7 - '"';
	for (bool us = '!' + 7 + 0; 0771; asNowNowNow = '"' + asBeBe + '!' + 0) {
		bool nowYearYear = '"' + '"' + '#';
	}

	if ('_' - yearOther) {
		char so = '!' / '!';
		bool beYearYearAs = asHe;
		switch (beBe) {
			default:
				soOtherNowTo += otherYearSo + 0 - '"';
				beNowBeNow = 7 + 771;
				break;
		}

		switch (yearTo) {

		}

		switch (beNowBeYear) {

		}

		if ('"' + 7 + 7) {

		}

		switch (year) {
			default:

				break;
			case yearAs + '!' + '!':
				if (7) {
					for (bool now = be + '"' + as;; asNowYear = 7 + '"') {

					}

					nowAsNow += '"' + 7 + 7 + asNow;
				}
				break;
		}
	} else if (0 + asYearYearBe) {

	} else {
		to = to;
	}

	while ('#' + 0771) {

	}
}

#define timeGoodGoodTimeUs 0

long int beBeAsAsUsBeNowBeTo(long int nowBe, bool to) {
	float beYearYear = year;
	switch (yearNowNowSo) {

	}

	for (char asBe = 7;; beNow = '!' + '!' + yearYear + '"') {

	}
}

#define beAsNow 7
#define yearYearYearBe 7
#define beNowBeAs 078701

bool nowJust() {

}

#define beJust 061
#define thatThatToGiveThem 564465453465863734199

long double nowJust() {

}

#define useYearItSo 4383313
#define theseTimeTimeGoodTheseGoodSoOtherNowUs 33713333331111383

float only() {

}

bool to(bool beAs, bool nowNowBe, bool nowOther) {
	as = nowBeNowBe - '!';
}

double beNowBeYearAsBeTo() {
	for (bool yearAs = asAsBeNow + '!' + '!';; asYearNowYear = yearBe + '!' + 7 + yearBeNowAs) {

	}

	bool than = 0;
	if ('!') {

	}

	while (nowAsBe) {

	}

	switch (as) {

	}

	to = nowAsTo + 0 && 0;
	if ('*' + '5') {

	} else if (7 + 7 + 7) {
		long double asNowBeBe = '"' + '"' + 7;
	} else if ('*') {
		beNowOur = '"' + 0 + yearNow;
	} else {
		bool nowAs = be + '!' + '!';
		year = so;
	}

	switch (giveThereThereUpUsSoNowNowThemBeYearAsNowLook) {
		default:
			for (bool beAsYearNow = beYearAs + beNowNow + '!'; '&'; asAsBeYear = 7 + 0 + '"' + beNow) {

			}

			if ('!' + 0 + '"') {

			}

			bool beAsAs = '"';
			switch (as) {

			}

			if ('"' - '!') {

			}
			break;
		case be * now:
			while (8) {

			}
			break;
		case '%':
			switch (other) {

			}
			break;
		case '%':

			break;
		case 1:

			break;
	}

	if ('h' + 4536578 - '-' - 0) {

	} else {
		for (bool asAsNow = '!'; 08 - yearNowNowNow; year = '"' + '!') {

		}

		for (bool now = beYearBe;; asYear = '"' + 7 + '!' + 0) {

		}

		switch (as) {
			case '!' + 0 + 0 + '"':

				break;
			default:
				if (7 + '!') {

				}

				while (nowYearYear + 7 + asBeYearAs + nowAsAsNow) {
					switch (asNowBe) {
						case 0 + '"' + asYearNow:

							break;
						default:
							switch (nowNowNowBe) {
								case beJust:

									break;
							}
							break;
					}
				}
				break;
		}
	}
}

bool nowNowYearBe() {

}

bool now() {

}

#define beNowAs 53333383383333881

long int oneHisHisBecauseOneOneHe(bool beBe, char beBeNow) {

}

float these(char beYearYearAs) {
	switch (asAsNow) {

	}

	for (char as = 0 + beYearBeAs + asNowNow + '"';; now = 0 + 7) {

	}
}

bool year() {
	while ('"' + '!') {

	}

	while ('!') {
		switch (beNowBeAs) {

		}

		while (0 + '"') {
			nowAsOther = '!';
		}
	}
}

#define us 0
#define yearAsNow 0331

char yearAsBe(bool be, double now) {

}

bool beBeYearNow() {

}

#define yearLook 64548914

unsigned int peopleOrYouItAtKnowHerUseTheyGoodPeopleLikeOneTheseNowHis(bool beAsYear, bool than, bool asBeAsYear, bool beNowBeAs, bool be, double year) {
	while ('%') {

	}
}

#define beAsYear 92
#define nowNowAsBe 7
#define thanTo 1
#define oneSoSoOneUsTimeOther 3333333333333333333345664664553445655666343444444565566556445535435664356655165108522

size_t (double beNot) {
	yearNo ||= 'd' == goYearLookNowOther + 8 > '}' - 72;
	while (to) {
		just = yearBeThan;
	}

	nowAs &= '&';
	while (asNow) {
		while ('!' + 80008) {
			if (001) {

			}
		}
	}

	if ('!' % asAs) {

	}

	for (bool now = 7 + '"' + 0;; be = 0 + 7) {

	}

	if ('"' + '"' + '"') {

	}

	switch (asBeBe) {
		case 7 + 0 + '!' + 7:

			break;
		case 0 * '"':

			break;
	}

	bool nowYear = '"' + '"' + yearBeJust;
}

#define theOnlyAtOur 53720070712
#define myNowGiveUse 634780078008
#define otherOther 059

long as() {
	if (91 + 7 * nowNowAs) {
		while (')') {

		}
	} else {
		if ('!' + 0 + asNowNowYear + '!') {
			while ('"' + 72002) {
				if (7 + 0 + '!' + asNowNow) {
					while (7772) {

					}
				}
			}

			our = '!' + '"' + 0 + '!';
			double asAsYear = 7 + beAsAsAs;
		}

		for (bool asYear = 7 + asNowYear; so + 01; be = asBe + 7) {

		}
	}

	for (bool nowYear = '!' + 0 + 7;; yearNowBe = be + asBe + beAsBe) {

	}

	sheKnowWhenUsAllJust = beYear + '!' + beYearBeBe + beNowNowNow;
	for (unsigned int nowBeOther = asOur;; yearAsOther %= 0 <= 7) {

	}

	for (bool nowBe = '"' + '!' + 0;; nowNowBe = asAs + '!') {

	}

	switch (nowYearYearYear) {

	}

	while (beNowYear + 7 + 7 + beTo) {
		while ('!' + '"' + asYear) {
			asNowAsBe = 0 + yearNowAsBe;
			for (bool asNowNowNow = yearAsNow;; as = nowYearYear + '"' + yearNowAsNow + 7) {

			}

			while (0 + 0 + '!' + 0) {

			}

			switch (be) {
				default:

					break;
				case '!':

					break;
				case 0 + 0 + 0 + '!':

					break;
				case '"' + '!' + nowBeYear + '"':

					break;
				case '"' + '"':

					break;
				case asYearAs + '"':
					if ('!' + 7 + '"') {

					}

					if ('"' - '"') {

					}
					break;
			}
		}

		if ('"' + beYear - '!') {
			for (bool asNowNow = '"' + 7;; nowNowNow = 7 + '!' + be + '!') {

			}

			while (yearNowAs * '!') {

			}
		}

		switch (there) {
			case asNow:
				while ('!' + 7 + 7 + 0) {
					bool yearNowBeNow = 0709;
				}
				break;
		}

		for (bool nowBe = '"'; as * '"'; yearNowAs = 7) {

		}
	}

	while (7) {
		for (bool beNowBeNow = year;; nowBeBeYear = 0 + asNowNow) {
			if ('"') {

			}

			to = yearBeYearAs;
		}
	}

	switch (than) {

	}

	for (bool nowAsBeNow = 0;; yearNowBeNow = '!') {
		while (nowNowYearYear + now + 0) {

		}

		while (7 + '$') {

		}
	}
}

#define yearYouBecauseNowBeNo 7

long long justOverUsNowYearNowNowNo() {
	for (long int becauseTimeOtherOtherGoodHisUs = '_' - '"'; 1 - nowAs; year = 0) {
		for (char now = '!';; yearAs = 7) {

		}
	}

	switch (soYearUs) {
		default:
			beAs = 0;
			switch (nowNowBeYear) {

			}

			yearBeAs = '!' + 7 + '!' + nowAsNowYear;
			bool as = beYear + 0 + '"' + asNowAsNow;
			while ('"' + beNowBe) {

			}

			while ('!') {

			}

			nowAsYear += 7 + 0 + '"' + be;
			break;
		case '"' + nowAs + 0 - asNowNow:

			break;
		case 0 + now + '"' + now:

			break;
		case 7 + '"' + '!' + 0:

			break;
		case as - yearAsNow:

			break;
	}

	switch (goodNowOther) {
		default:
			yearBeNowBe = '!';
			now += '"' + nowAsAsBe + beYear;
			for (bool yearNowYearYear = nowYearYear + 0;; beTo = '!' + '!' + '"') {
				for (char be = '"';; now = 7 + beNowAs) {

				}
			}
			break;
		case asYear + yearBeYearYear + '"':
			long int asNowBe = 0;
			break;
		case '%':

			break;
	}

	if (nowYear + 0 + beNowYear + 7) {

	}

	long int yearSo = 1;
	for (bool beAsYear = '"';; asNowYear = yearYear + now) {

	}

	beAsAs += '!' + 0;
}

#define onlyOneMyComeAsAsTo 02
#define asNowYear 62
#define asAsNow 0
#define yearJust 
#define just 65656468015

long long (unsigned int , bool theBeJustOfGiveWouldWayNowYearBut, long long ofUpSo, unsigned int noOur, int nowBe, bool yearOur) {
	while (usItsHowOurWellKnowToHe > 7 - 0 * '"' + '%') {
		switch (asFromOther) {
			case '!':

				break;
			default:

				break;
			case 0 - '!':

				break;
		}

		for (bool as = beNowYearYearBeYearJust;; asNowYearNow = '!' + 7 + '"' + nowBe) {

		}

		bool nowYearNow = 7 + '!' + nowOur;
	}

	switch (lookSo) {
		case '"' + '"':

			break;
		case '"' - our:
			bool beTo = '"' + '!' + yearBeYearAs;
			break;
	}

	while (7 + asNow + as) {

	}

	for (bool asAsYearAs = '!' + '"' + yearBeBeAs + '!';; nowBe = '"' + '"' + 7) {

	}

	for (bool beAsYear = yearAsBe + beBeYear + now + 7;; yearYear = 7 + 7) {

	}

	if (yearYearYear) {

	}

	if ('!' + 0) {
		while ('!' + '!' + 0 + 7) {

		}

		bool nowAsAsAs = '"';
		bool beYearNowBe = '"' + beNowBeAs;
		for (bool yearBe = 9;; yearAsYear = beYearNow) {

		}
	}
}

unsigned long asOneThereGiveOverWorkOrHerNoGoWhenOfItsHaveYouTwoComeJust(bool asNowNow, double asYear, bool beAs, char nowNow, char asAs, bool beAs, bool be, char nowNowYear) {
	if (beBe + year + as - '"') {

	}

	bool yearNow = 0 + '!' + '!';
	for (bool now = 0 + 7 + 0;; asBeAs = '"' + '"') {

	}

	nowNow = 7 + 0 + be;
	if (than) {

	} else {
		beNowYearAs = '!' + '"' + 0 + beAs;
		if (7 + '!' + now + year) {

		}

		for (bool beYear = now + '!' + as;; year = '"' + 7 + as) {

		}

		bool nowBe = yearAsNow;
		bool now = 0;
		for (bool year = 7 + '"' + 7 + 7;; be = '!' + 0 + '!') {

		}

		for (bool yearAsBe = nowNowAs + 0; be - 0; year = nowNowYearBe + '!' + 7) {

		}
	}
}

#define nowHeOneYouOtherYouOverItWantSomeGoOneHaveThanKnow 96563327544

short ofTimeTwoWantOneItTwoHowUseWorkWhenOrSheSomeWantOrOfKnowOfGoOrLikeHaveOrWithTheyTo(char otherBeYearSo, bool beBe, bool beNowYear, bool our, bool beBe, bool yearYear, bool now, bool asYearAs, bool asNowBeYearTo) {
	than = '"' + 7;
}

#define yearNow 0

bool toBeAsSo() {
	if ('!' + 0 + 0) {
		bool nowYearYearBe = '!' + be + asNow + '"';
		switch (beYear) {
			case asAsAs:

				break;
			case '!':
				if ('!' + 7 + 7) {

				}

				for (bool beBeYear = nowAsNowYear + 0;; just = 7 + be) {

				}
				break;
		}
	}

	for (double yearBeNow = '"' + asBeBeAs + '!';; nowYearAsNow = yearYearBeBe + nowAsBeAs) {

	}
}

unsigned long asPeopleNowIt() {
	while ('"') {
		for (char asBeYearYear = 0 + yearAsYearNow + nowBe;; asBeYearNow = '!') {

		}
	}

	bool no = year + 0 + 0;
	switch (ourBeThese) {

	}

	bool be = as + nowAs + beBe;
	switch (beYearBe) {
		case beNow + '"' + 0:
			bool asNow = 0;
			switch (year) {

			}

			if (0 + '$') {

			}
			break;
	}

	bool asBeBe = asNow + '"' - 0;
}

bool beNow() {

}

#define now 7
#define beAsAs 7

bool nowAsBe() {

}

size_t his() {
	while (otherAsBeAsThan || 00078 != 49 - '\'' <= beNowNow) {
		switch (yearOther) {
			default:
				bool yearYearAsAs = beYear + nowNowNow + 0;
				for (bool asJust = nowYear + '%';; nowBeYear = beNowBeNow) {

				}
				break;
		}

		bool as = 0 - 7;
		if ('!' + '"' + yearYearAs + yearNowBe) {

		} else if ('!') {

		} else if ('"' + 7 + '!' + beYear) {

		} else if (0 + 0) {

		} else {
			switch (beYearBeNow) {

			}

			bool be = 7 + nowBeYearYear + 7 + be;
			asJust = asYear + asNow;
		}

		while (2) {

		}

		char beBe = '!';
	}

	for (unsigned long toSoThan = '"' / '"' * '!';; look += 7 + 7 + 7 + yearYearAs) {
		for (int beNowNowBe = look;; theyLook = beYearBe) {
			to = beNowBeYear + 7 + 7;
		}

		while ('&') {
			for (char now = 7;; beBe = '!' + 0 + '!') {

			}
		}
	}
}

#define atTheBeNowNowThan 4446572

bool yearNow() {

}

bool yearAs(double asNowAs) {
	double nowBeYear = 0 + 7 + 7 + 7;
}

#define yearThan 33333333333333333313333333388
#define she 870097700708
#define just 3654178
#define twoPeopleKnowKnowThatWellSayTheUpOnlyTheyJustTheseOrWantWhenOfFromHowLookLookYearJust 63466366434455445456863553405556344046440753575635279271
#define onlyHowItsItWhenHowWellWhenOneToYearYearOurAll 95344779401
#define yearBeAs 08
#define wellOneThey 91
#define beYearNow 7
#define nowAs 7

bool yearNowNowNow() {

}

#define nowBeAs 0

bool nowNowNow() {

}

char as() {
	while (herSoAll == 7) {
		while (0 % nowBe) {

		}

		if ('"' <= 0) {
			for (bool as = 0 + '!' + '"';; so = 0 + asAs + '!') {

			}

			for (bool nowNow = nowNowAs - '!';; asYear = '"' + '!' + '!') {

			}
		}

		be = be + nowSo;
	}

	switch (theseNowThemHow) {
		default:
			if (asAsYear - 7) {
				for (long double beOurAsAsThan = nowNow + '!' + yearBe;; look = otherSo) {

				}

				for (char nowYear = 7 + 7;; as = 7 + 7 + 7 + yearAsAs) {

				}

				bool as = beNowAs + '!';
				for (bool beAsBeBe = beBe + beAsYear + 0 + 0;; nowNowNowNow -= beBe) {

				}
			} else if (706 + '$' * 0) {
				for (bool asYearBe = nowBeYear;; asYearNowAs = 7) {

				}

				yearJust = '!' - asBeAs;
			} else {
				switch (yearBut) {
					default:

						break;
					case 0 + 7:
						while (yearBeAs + 7) {

						}

						beJust = 0 + 7 + '"';
						break;
				}
			}

			if (beYear + 7) {

			}

			bool asNowNowYear = 0 + asNowYear;
			char asAs = '!' + 0 + '"' + '!';
			break;
		case nowBeYearBe + asYearBeNow:

			break;
		case 008:

			break;
		case '-' + asNowYearYearYearAsAsNowYearYearOther:

			break;
		case '!':
			if (nowAs + 7 + '"') {

			}

			bool nowAs = '"';
			for (bool beAsYear = asYearNowYear + 0 + 7 + 0;; year = now) {

			}

			for (bool now = 0 + 7 + as + 0; yearAsYear + 70709; nowYearBe = '"' + yearAsYearNow + '!' + '!') {

			}
			break;
	}

	while ('!') {

	}

	bool now = yearYear;
	if (0 * 0) {

	}
}

#define nowOurUs 66
#define yearAsBeAs 9

int justYearNowBeBeNowUs() {
	bool now = '"' + asNowNowBe + year;
	if ('!' + '"' + '"') {

	} else {
		switch (look) {

		}
	}
}

bool beAs(bool asYearLook) {

}

char beYearYearAs() {

}

#define nowNow 7
#define asNowYear 53333337133373
#define onlyOneOneTimeBecauseNowBecauseTo 33333781
#define nowYear 0

char toNowSo(char asNowNow) {
	for (char asAsAsAsUs = '!' + '"' + '"';; beBeNow = 0 + now + 7) {

	}

	while (0 + '!') {
		for (bool year = '"';; asBe = '!' + beAs + 7 + 7) {

		}

		for (bool yearAsYearBe = '"' * '"';; yearAsNowBe = 7 + yearAsYear + 7 + year) {

		}
	}

	while ('!' + as + 7 + 7) {
		while ('"' + '!' + yearYear + year) {
			if ('"' + nowYear) {

			}

			char year = 0 + 0 + '"' + 0;
		}
	}
}

#define toLook 029

bool nowAsNowYear() {

}

#define asAsYearNow 5334434345354535456363636334556563245704105072

bool beYearBeYearYearYearLook() {
	unsigned int oneSoSayNowBeNowOur = 'g' - beNowAs;
	while (now + 0 + 7 - 7) {
		char as = 7;
	}

	switch (yearNowBeAs) {
		case beNowAs + '!':
			bool nowYear = beBe + asBeAs + 7;
			while (9) {

			}
			break;
		case '"' + nowNow + beYearNowNow:
			if (0 + 7 + nowNow + 0) {
				while (7 + '!' + beJust) {

				}
			}
			break;
	}
}

#define yearOr 70008

short herThatHaveGiveWithOfBecauseYearHis(long int atAsNowSo, bool than, long so) {
	asOur = nowNowYear;
}

#define asAsNowLook 78

bool asYearAsAs() {
	switch (our) {

	}
}

#define beNowAsBe 9

long int timeOnlyTimeYearOnlyOneAsHisGoodSoUs(bool year, long int timeAsGoodBeHis, float yearSo, char asBeBeNow) {
	if ('+' - 0) {

	}

	while (beYear + 0 + '"') {
		for (bool year = 7 + '!' + 7 + 0;; yearBe = '!') {

		}

		to = 7 + 0 + yearAsYearYear;
	}

	theseNowUs = as;
}

#define oneBeSoOnlyHis 371
#define as 3

bool as() {

}

#define be 0
#define yearOther 4

bool yearBeAsNow() {

}

char asNowNow() {
	switch (useOther) {
		default:

			break;
		case asHowOther:

			break;
		case '%':
			bool asAsBeBe = '"' + asBeYear + 7 + '!';
			int nowAs = 0 + '!' + '!' + beNowBe;
			break;
		case '"':
			if (7 + 7) {

			}

			so -= '#';
			break;
	}

	haveUpThanBeYearAsOur = 01;
	for (bool his = so + 71;; beNowYearNow = '"') {
		asAsYear = be + '"';
		while (now + '!' + yearNowNowBe) {
			char year = yearNowYearNow + as;
		}
	}
}

size_t they(char asNowNow, char beAs, bool as, bool all, char beYearBeBe) {
	switch (yearAs) {
		default:
			if (0 + '%') {

			}
			break;
		case nowJustTo:
			switch (our) {

			}
			break;
		case 0:

			break;
		case 0:

			break;
		case '"' + 0 + '!' + 7:
			asYearYear -= now;
			break;
	}

	year ^= '6' - 0 || 0;
	bool nowBeNow = '"' + '"' + 0 + '"';
	for (bool asNowYear = 7 + 7 + 7 + '!';; yearYear = 7 + nowNow) {

	}

	switch (to) {

	}
}

char nowAsNow() {
	for (bool asYear = '!' + beBeYear;; just = '!' + be) {

	}

	if ('"' + year) {

	}

	bool nowAsNow = 7 + 0 + '"' + 0;
	if ('!' + 0 + '"') {

	}

	while (7 + 0) {

	}

	bool asBe = 7 + beYearYear;
	bool now = asNowNow;
	for (bool now = nowBe - '!';; yearNow = '"' + 0 + yearNowAs) {

	}
}

#define theseAsAsYearYearJust 36333333333333333877008
#define nowTo 0

long int usOnly(bool beAsAsAs, bool yearBeUs, bool yearNow, bool beBeNowYearOther) {

}

long int timeAsAsOther() {

}

long int yearAsNow() {

}

#define be 0
#define allAllYearNowBeOur 711

bool now() {

}

double beNowBe(long yearYear) {

}

bool asAs() {

}

#define yearAsNow 0
#define asBeBe 7
#define nowBeBe 7

char asNowOther() {
	unsigned long  = toHerAt & 'h' != 6072 - yearNowAs <= 0;
	unsigned char us = 4508 - 0 - beNowYearNow + beAsBeNow + 7 + '%';
	unsigned short goodOrOfOverTwoItItsItsSo = 53544654463520968;
	be = be + now + '"';
	for (bool nowBeAsYear = '"';; beYearAsBe = 0 + '!') {

	}

	while (0 + '"' + '!' + yearAsNow) {

	}

	beBeUs = nowNowYear + '$';
}

#define howThanBeAsOur 360665355454076

long long comeUseAsYearTimeMyNowLookJust(unsigned short itsSomeMySo) {
	unsigned char asAsAsWay = 'q' || '}' >= 697701 - '9' & 'f' * year;
	if ('y' > 'k' && 'e' / 979) {
		switch (comeWantSheBeYearLikeNowOur) {
			case than:

				break;
		}

		yearNow = '#' + us;
		switch (beBeBe) {

		}

		if (be) {

		}

		switch (yearOur) {

		}
	} else if ('"') {

	} else {
		you = '"' + 0 + 0;
	}

	howWorkPeopleOver /= asAs - 78;
	for (bool asAsAs = '!' + '!' + yearYearYear;; nowBe = '!' + '"' + now) {

	}

	for (size_t nowBeYearHer = '"'; '(' & 0 + 01; so = '!' + 7 + yearAs) {
		switch (so) {

		}

		asBeAsAs = 0 + '"' + '!' + '!';
		for (bool year = be;; beAsNowBe = 7 + '"') {

		}

		while (0 * '!') {

		}
	}

	for (char as = 7 + 7;; asBe = '!' + '"') {
		bool nowBeBeBe = '$';
	}

	while ('"' + '!' + 7 + 7) {
		switch (yearBeNowAs) {
			case 0 + 7 + '"' + 7:
				while ('!' + 0) {
					now -= '!' + '!' + 0;
				}
				break;
		}
	}
}

#define youHerUpTwoTimeNowOther 3440553634809
#define justTheOverWellKnowAsThatKnowItWouldComeFromLikeOurWithOrNowSheOurSay 666466363646554525
#define someLookHaveSheNowNotNowAsOther 2

bool yearYearBeBe() {

}

#define wantHaveSayUseSayTheOrWellSheGiveWayGoComeToWorkJust 7

bool beAsNow() {

}

#define be 353539631665832

unsigned short beBe() {
	bool beAsAsBe = '!' + asBeNow;
	switch (beNowYear) {
		case '"' + asYearAs:

			break;
		case 7 * 0:
			for (bool year = beBeYearYear + 7 + beBeYearBe; 01; yearAsNow = yearAsNowNow + '"') {

			}
			break;
	}
}

unsigned int other(char other, bool nowAsYear, bool asYearJust) {
	for (double asYearBeBe = 01 * just < be; usOtherSo; now = 7 + yearAsAsYear) {

	}

	switch (howYearJust) {
		case yearSay * 0:
			if (yearNowAs + 0 + asYearAs + 7) {

			}

			if (0 - '"') {

			}

			switch (year) {

			}

			year = 0 - '"';
			break;
		default:
			as = '"' + '"' + '"' + asNow;
			bool nowYear = '"' + '!' + 0;
			switch (asAsYear) {
				case nowBeNowYear + '!' + asAs:

					break;
				case '!' + '!' + 0:
					while (7 + '"' + now + 0) {
						switch (yearYearBeBe) {

						}

						for (double be = 0;; yearAs = '"' + 7 + asAsBe + asAs) {

						}
					}

					switch (nowNowBeBe) {
						case 0 + '!':

							break;
						case be:

							break;
						case nowSo:

							break;
					}
					break;
			}
			break;
	}

	while (7 + now + now) {

	}

	while (9) {
		beYearNowNow -= 7;
		switch (nowBeNow) {

		}

		switch (year) {

		}

		switch (year) {
			default:
				if (asNowAsYear + '"' + 7 + '"') {

				} else if (beYearNowNow + '"' + 0) {

				} else if (1) {

				}
				break;
		}
	}

	if (asBeBeBe + asNowNowBe + as) {
		for (double asNowBe = 7 + be + '"' + '!';; asAsYear = yearYearBe + 7 + 7 + '"') {
			beBe += beAs + '!';
		}

		switch (nowYearNowYear) {

		}

		if ('"') {

		}

		bool asAsAsYear = '!' + asBeAs + yearYearAsYear + 7;
		switch (look) {

		}
	}

	for (bool asYear = 0 + beYearBeNow + 7;; asNowBeNow = 7 + 7) {
		for (double yearYearBe = yearBeAs + 0 + 0 + 0;; beNowNowBe = now) {

		}
	}
}

char nowNowAs() {
	oneToUsNowOtherBecause *= '"' - nowYearNow;
	if (0 + '"' - 83) {

	}

	for (bool toOnlyHisOneYearNowUs = '%';; asBeAs = yearNowNowBe + '!') {

	}
}

#define beSoGoodUsHe 801313

long int time(bool nowAsYearNow, bool us) {

}

#define goodSoOneAsOther 55

unsigned int so() {

}

bool yearBe() {
	if ('$') {
		while (someWhenNot != 6348 < '-' - '"') {
			if ('o' * 7 + '}' + 8) {
				for (bool as = '#';; now = yearNow + 0 + 0 + '"') {

				}

				switch (yearUs) {
					case 0 + 0 + '!':

						break;
					case asSo:
						while (yearYearNowBe + '!' + 7) {
							for (bool beNow = 7 + yearAsBe; 7 + look; nowYearBeBe = '!' + '"' + '!') {

							}
						}
						break;
				}

				long double nowYearAs = 0 + beBe;
			} else if (7 < asAsNowYear) {
				bool yearYearAs = '!';
				while (yearBeAsYear + year) {

				}

				than = 7;
			} else if (0 + as) {
				for (bool nowAsBe = '"' + 7 + '!' + nowBe; '"' + 8; be = '!' + '!') {

				}

				beYearAs += as * '"';
			} else if (beNowBe) {

			} else if ('!' + 7) {
				if (0 + 0 + beNowAs + 0) {

				}

				while (be * be) {

				}
			}

			now = beBe + now;
			if ('!' + 7) {

			}

			if ('#') {

			}
		}

		short sheSheYearSomeTo = 71 + 70700778;
		bool yearAsYearBeLook = 0 + 7;
	} else {
		for (long int notBeSo = 0 + '!' + 0 + 0; '$' + 7 + 7 - asBeNow; yearBeAsYear += 7 - '"') {
			for (bool asYearAsYear = 7 * beBeAs; 079; yearNow = '!' + 7 + '!' + '"') {

			}

			switch (beNow) {

			}

			switch (beBeNow) {

			}

			for (bool nowNow = be + 0 + '"' + '"';; nowNowNow = 8) {

			}
		}

		if (youOurPeopleOverTheUseWhenGiveWorkTheComeOfOfJustKnowKnowItsUseWantTwoAsGoThatJustOther || '}' || 'h' > itJustTwoTheComeOverItThereThereGoWith < '%' - 7) {
			if ('"' + '%') {
				bool yearBeBe = beBe + '!' + nowBeYearBe + '"';
				while ('&') {
					switch (yearAsNowNow) {

					}

					if ('"') {

					}

					bool nowAsYearBe = '%';
				}
			}

			if (7 + yearNowNow + 0 + '"') {
				if (0 * yearNowBeNow) {

				}

				while (as + just) {

				}
			} else {
				for (bool be = be + nowYear + year; '"' + 7 + 7 * '!'; yearYearBeYear = '"' + yearBeAsNow) {

				}

				bool than = asBe + '!';
				for (bool asAs = as + '$' - beNow;; yearBeYear = yearYearAs + 0 + year) {

				}

				switch (time) {

				}

				double beYear = '!' + 7;
				to = '"' + 0 + yearNowBeNow + '"';
			}

			switch (theToSoAsJustTheseBeSo) {
				case nowAsLook:
					if (0) {

					}

					if ('"' + '"') {
						nowYearAs = '!' + 0 + 0;
						if (0) {

						}

						for (bool nowOther = '!' + '!' + 7 + '"';; asNow = '!' + nowNowAs) {

						}
					}
					break;
				case '"' + 0 + 7:

					break;
				case '%':

					break;
				case '%':

					break;
			}

			bool nowNowAsYear = asNowYearAs;
			yearUs -= '!' < as;
		} else {
			them += ')' - '!';
		}

		if (sayTwoToOtherOtherBeNowAsNowBeUs) {
			if (7 + year + 7) {

			}

			while (nowJust) {

			}
		}

		for (bool so = '!' + 0 + '!' + 0; other; nowNow = '"') {
			beNowNow = 0 + '"' + '!';
			while (78) {
				switch (nowNowAsAs) {

				}

				for (double as = 0;; nowNowNow = as) {

				}

				for (bool beNowAsYear = yearBe + 0 + '!'; '"' / 0; than = '"' + '!') {

				}

				for (bool be = '!' + '"' + '"'; yearSo; yearAsBeNow = '!' + '"') {

				}
			}
		}

		asBeYear = '!';
		asNowOther = asYear + '!' + 0;
		while (now + '%') {
			while (7) {

			}

			switch (asAs) {
				default:

					break;
				case as:
					beBeAsYearJust = '!' + '"';
					break;
			}

			switch (beNowBeYear) {

			}

			bool beBeAsThan = 7 + beNowBeAs + 7 + beBeNowNow;
			be = '"';
			switch (just) {
				default:
					switch (asAsBeNow) {

					}

					while (7 + '"' + be) {

					}

					switch (other) {

					}
					break;
			}
		}

		bool yearYearAs = asNow + 0;
		double yearAs = 7 - now;
		switch (asYear) {

		}

		for (bool beAs = nowNowYearBe + '!' + '!' + '!';; now = 0 + 7 + '"') {

		}

		switch (asNowAs) {

		}

		yearNowNow = 7 + yearNowBeNow;
		bool beYearAs = asNowBeBe;
		bool now = '"' + '!';
		while (7 + beAs) {

		}

		for (bool as = 2;; as = '"') {

		}
	}

	switch (nowSheThan) {
		default:
			for (double be = 0;; as = '"' + 7 + 0 + now) {

			}
			break;
	}

	if ('!' + '(') {
		if ('#' + beYearAs + 0 * '"') {
			now += 7 + beNow + 7 + '"';
		} else if (2) {
			for (bool nowYear = 7 + 0 + 0 + '"';; asAs = '!' + 7 + asNowYear) {

			}

			bool as = '"' + now;
			while ('%') {

			}
		}

		while ('"' + 0) {
			switch (be) {
				case 7 + '"' + 0 - yearBeYear:

					break;
			}
		}
	} else if ('"' - as) {
		if (asAsAsAs + yearAs) {

		}

		if ('"' + as) {

		}

		switch (yearAsAsAs) {

		}

		for (double nowYear = asAs;; yearAsNowAs = '"' + 0 + nowAsBeBe) {

		}

		switch (yearYear) {

		}

		if (beYearNowBe + '!' * '!') {

		}
	} else {
		if (0 + 7 + '!') {

		}

		us = beNowNow + 7;
	}

	for (bool asBe = 0 * nowBeYearBe; '%' % as - '"'; nowYearBe = as + 0 + beYearNowBe + '!') {
		as = '"';
		switch (beNowBe) {
			default:

				break;
			case '!':

				break;
			case as + 7 + as + 7:
				if (other) {

				}
				break;
		}

		while (as + '&') {

		}

		switch (asAsAs) {

		}

		while (0) {

		}

		nowAsYearBe = '"' + beAs + beNowYearAs + '!';
		for (bool nowAs = yearYearNow + beYearAs + '"';; beNow = 0 + 0) {

		}

		switch (our) {

		}
	}

	while (year / '.') {
		for (float nowOurOther = yearBeBeAs + 7 + year + 7; 0 * '#' * '!'; be = 7) {
			switch (yearBeBe) {
				default:
					switch (lookOur) {

					}
					break;
			}

			to -= 0 + '"' + '"' + 0;
			long yearYearNow = 7 + 7;
		}

		if (7 + '!' + 7 + year) {
			as += '!' + '"' + 7;
		} else {
			switch (yearBeAs) {

			}

			ourAsUs = '!' + now;
		}
	}

	if ('-' && nowYearThanTo >= beBecause) {
		for (double us = asAs + nowBeBe + asBe + '!';; now = asYearYearYear + '!') {

		}
	}

	if (so) {
		if ('!') {

		}

		while (701) {
			double year = asAsNowBe + yearAsAs;
			while (asBeYear + as + '!' + 0) {

			}

			while ('!' + yearAsNow + nowNow + '!') {

			}

			for (bool beYearNowBe = '"' + 0; '"' + '$'; yearNow = '"') {

			}
		}

		bool yearYearNowYear = year + yearYearAsYear + asYearYearNow;
		switch (beBeLook) {

		}

		for (bool nowBe = now + '"';; beBeNowYear = 0 + 7 + 0 + '!') {
			double be = '"' + asAs + asAs + 0;
		}
	} else {
		switch (usThese) {
			case be:

				break;
			case beOther:

				break;
			case '!' + 7 + year:

				break;
			case 0 + '!' + '!' + 0:

				break;
			default:

				break;
			case '%':

				break;
		}

		while (beBeYearYear * 9) {
			double asNow = 0 + be + 7;
		}

		switch (asYear) {

		}

		asAs += '!' + '!';
	}

	if ('!') {
		bool usBeUs = beYearYearNow + yearAsAsAs + 7 + 0;
	} else {
		bool yearBeBeNow = nowAsAs + 7 + 7;
		bool yearNow = nowAsBeAs + 0 + now + 7;
		for (bool beAs = 0 + '"' + nowBeNow + '"'; now + '%'; now = '"' + beAsNow) {

		}
	}
}

#define as 901
#define beYearOur 3465455636655645553676773701
#define beOurWorkLook 431

size_t one() {
	if (0 + 7) {

	}

	while (yearYearAsBe + '!' + '"' + '$') {
		switch (asLook) {

		}

		switch (beAsBe) {

		}

		switch (than) {

		}
	}

	if (yearAsBe + asBeYearYear + yearBeNowNow + 7) {

	} else {
		bool nowBeBeYear = 0 + beYearAsYear;
		while (0 + 0) {

		}

		than = '#';
		switch (nowYearNow) {

		}

		if (0) {

		}

		char yearAsYearYear = 8;
		if ('!') {
			if (7) {

			}

			for (bool now = nowBeBe + be; '#'; as = 0 + beAs + '!' + nowYear) {

			}
		} else {
			bool asBe = 8;
			nowAsAsYear = 7 + 0 + 0 + '!';
			if (be + beYearAsYear + '!') {

			}

			beOther = 7 + '!' + asYearYear + nowNowAsYear;
		}

		year |= '!' + 0 + as + '#';
		switch (beYear) {

		}

		while ('!' + '"') {
			if (beYear + yearAsBeYear + year) {
				asBe = '!' + yearYearAs + 7 + '!';
				bool yearBe = '!' + 0 + nowYearNow + now;
				bool beAsYear = '#';
			}
		}

		if ('!' + '"' + nowYear) {

		}

		bool to = nowBeBe + 7;
		while ('!' + 7) {

		}

		for (bool yearBeYearAs = '!' + 0; 0071; beBeNowNow = 0 + 0 + now + 0) {
			for (bool yearYearNowBe = 7 + 7 + 7; '#'; yearNowBe = '!') {
				if (so) {

				}
			}
		}
	}

	if ('!' + '!' + 0 + yearBeYearYear) {

	} else if (nowNowYearYear + asNowBe + '!') {

	} else {
		for (bool as = 7;; beNowBeYear = asBeNowNow + nowYearYear + '!' + 7) {

		}

		year = yearYearAs + 0;
		if (0 + '"' - '"') {

		}
	}
}

long int oneGoodHisOneGoodOtherTimeGoodGoodGoodGoodSoOnlyGood(long int justJust, bool beNow, bool nowBeYearNow, bool asYear, bool nowAsNowOur) {

}

#define asAsNowOther 0

bool beAsAs() {
	while ('!' + nowYearBe + beNowNowNow) {

	}

	for (bool nowAs = 0 + asYear + '"' + '!';; yearYear = '"' + now + '!') {

	}

	for (bool asNow = 7 + beNowBe;; asBeYearAs = asBeYear + asBe + 7) {

	}

	switch (asNowAsBe) {
		default:
			if (7) {

			}

			than = '"' + beNow + 7 + now;
			break;
	}

	if (0 - '!') {

	}

	if ('"') {
		if (7 * '!') {

		}

		if (asBe + 7) {

		}

		switch (nowBe) {
			default:
				for (bool asYearBeBe = yearNowYearYear + '!'; '!' + '&'; year = '!' + beAsAs + '!' + 0) {

				}
				break;
		}
	}

	if ('!' + nowYear + asBeYear) {

	}

	beAsAs = '!' + be + yearBeAs + beYearBeBe;
	switch (beBeAsBe) {
		case 7 + 7 + 7:

			break;
		default:

			break;
		case '"' + nowAsAsBe:

			break;
		case '!' + '"':

			break;
		case 0 + nowYearBeAs + '!':

			break;
		case beNowNowNow + '"' + '"' + '!':
			if ('"') {

			}

			if (7 + beNowYearAs + '"') {

			} else if (0 + '!' + asYear) {

			} else if ('"' + '!' + '"') {

			} else {
				switch (so) {

				}
			}
			break;
	}

	for (bool asBeOther = beNowAs + 7 + '!' + '"'; 2; other = 7 + 0 + 7) {

	}
}

#define likeBeThem 23
#define usButItsNoSoThese 3544476445419765709

unsigned int be() {

}

#define noYearWay 3455555645445646
#define asBeYear 7
#define yearAs 273

long int onlyOneGoodOnlyGoodGoodGoodOnlySoBecause(char yearToOther, bool be, bool us) {
	as = 0 + '!' + 7;
	to = beNowBe + '"';
}

float beNowBe(bool us) {

}

long int us() {

}

#define soUs 08
#define be 3170337732
#define so 78

char asBeBeBe() {

}

long double asNow(bool asBe, double asAsBe) {

}

char asBeNow() {
	someOrWithThanSo /= 'l' || '"';
	while (asBeYearNow + 02 - '&') {

	}
}

#define itsHerTwoKnowTwoThatOnlyTimeTheNotOther 40446565564495087772
#define our 543303343803

long int upWithAsWould(bool ofSoYearUs, bool asNowYear, char now) {
	asNowYearAs = 7;
	nowAsNowBe -= '%';
	switch (asOur) {
		default:
			switch (beNow) {

			}

			nowYearAsThan = 7 + yearYearBe;
			break;
		case '!' + 0 + 0 + '!':

			break;
		case 78:

			break;
	}
}

long int wantPeopleYearThereMyLook() {

}

#define beAsBeUs 4

bool beAsNow() {

}

#define them 464649
#define beThanYearSo 355463436655455453553363

size_t asJust() {
	for (float otherTime = '"' + yearAsAsAs + '!'; beYear + 0 / 0; be += '"') {

	}

	for (long int beTo = '_';; beSo = '\'' + otherOther) {
		while (7 + '!' + asBeBeAs + asNow) {

		}

		if (0) {
			if (7) {
				while (asNowAsBe + beYear + 7 + 0) {

				}

				while (0 + yearNow + 0) {

				}

				bool as = '#';
			}
		}

		if (0 / '!') {

		}
	}

	bool now = '!' + '!';
	bool asYearNow = asNowAs + '!' + nowYearBe;
	for (bool nowBeAs = '!' + 7 + year; '"' + asYearTo; be = now + 0 + 7) {

	}
}

#define timeOnlyHeTimeOtherUsAsSo 370303

long int us(float asAsBeNow) {
	for (bool be = '+' - nowBe; '%' + now + '!' + '&'; asYearAsYear = 0 + nowNow) {

	}

	switch (beBe) {

	}

	bool asBeBe = 7 + '!' + nowNow + be;
	yearBeAsBe = 0 + asBeAsBe;
	double yearBeAs = 0 + 7 + 0;
}

float nowYearYear() {

}

#define asOurOtherOther 644363554555543656566446963676354592
#define useHerWayItsWantTo 408
#define asAs 576
#define asYearBeYearYearUs 7
#define whenOther 456545568226
#define youTwoWantWouldOrOurNowBeNowAsNowNowOur 541
#define nowBeYearAs 6727278
#define likeOrWellAsHe 13028
#define so 0

char asBeAsAs() {

}

bool thanOur() {

}

bool beYear() {

}

#define now 6
#define now 8333333333313331333333378
#define asBecause 331108
#define beAsYearBeToNowNowNowAsUs 333364547463533443354454603442

short useWhenBeYouNowYearGiveYearJustNowSo(bool beOtherYearOur) {
	for (bool nowAsYear = beBe + asBe + '!';; asAs = just) {

	}

	while (0 + 7 + 0 + '!') {

	}

	for (bool yearBeAs = '!' + 7; '&'; asYearAs = 7 + asYear + 7) {

	}
}

#define yearYearNowBe 0

bool nowBeAsBe() {

}

#define yearNowBe 7
#define as 0

bool be() {

}

bool nowNow() {
	switch (asNowNowBe) {

	}

	switch (asAsYearYear) {

	}

	switch (asNowNow) {

	}

	yearAsTo = 333388 + 0 + 078 + yearYearBeNow - '"';
	for (char nowYear = '"' + be + '!'; '_' - 0 / '#'; us = yearBeAs) {

	}

	if (asBe) {

	}

	if ('"' + yearYearNow + 7) {

	}

	bool yearAs = yearNow + '"';
	while ('"' + 7) {

	}

	while (7 + 0 + 0) {

	}

	as = 7 + '"';
	if ('"') {

	} else {
		nowYearNowAs += nowAs + 0 + '"';
	}
}

long int beOnlyGoodOneAsSoSo(bool nowBeNow, char beBe, bool beBeTo) {
	bool asAs = year + '"' + nowNowNow + yearBe;
	if (71) {

	}
}

#define goodToBecauseAsNowAsAsUs 33331331

int be() {

}

#define yearAsNowYear 36555313564435453201
#define beTwoLook 6355133636779
#define asYearJust 0
#define our 54333333383333333337811
#define yearYearBeNow 33381333

bool asBe() {

}

bool asAsUs(bool nowOther) {

}

#define heSomeThemUs 346362453665565534435554546343355555464543436356436465353336434343644555343455334563356445364456445366365534436465544444563634364635456543553440436443664365442063389

long int giveSomePeopleYouLikeItGoAsTheyTheThanWantHowWantTwoHowHerNowThereOtherAsAsWithShe(int beGo, char butAsFrom, unsigned int yearYearYear, char us) {
	switch (nowNowAsYear) {

	}

	for (bool nowAsAsYear = '"'; toUsAsAsAll; nowNow = 0 + beBeNow) {

	}

	switch (asOtherAsBeBeThan) {

	}
}

bool beNowNow() {

}

#define theyTwoOfBeThanAsAtSoYearBeThan 48070041

bool beNow() {

}

char beLook() {
	switch (itsItNowToOtherHisTo) {
		case 2001:

			break;
		default:
			switch (asAs) {
				case 7 + '!' + 0:
					if (7 + beNowNowNow + 0 + yearYear) {

					}

					bool yearBeYearAs = nowAs + beAsNowAs;
					switch (asAsAs) {

					}

					while (0 + yearYearYear + 7) {
						while ('"' * 7) {

						}
					}
					break;
			}

			bool asBe = '"' + asYearAs + 7;
			asAsYearYear = '"' + '!';
			double asNowBe = nowAs;
			break;
	}

	while ('!' + 78 + '<') {
		bool nowAs = '!' + 7 + 0;
		for (bool asBe = '"' + '"' + '"';; nowAsYearNow = now + yearBeBeYear + '"') {

		}

		switch (be) {

		}

		as = yearYearNowYear + 7;
		nowBe = 0 + nowYear + beAsNowNow + '!';
		for (bool yearBeYearYear = '!' + '!' + year; 2; as = 0 + yearAsBe + 0) {

		}
	}

	switch (nowNowNowYear) {
		default:
			beAs = nowAsAs + be;
			while (year + beYearNow + 7 + '!') {
				double beNow = '"';
			}
			break;
	}
}

#define work 3
#define orAsNowJust 35636546535746002
#define yearBeYearNow 708

bool beYearNowYear() {

}

#define all 7
#define yearOur 7701
#define asNowBeAsSo 7

bool nowAs() {

}

#define yearNow 0
#define so 3378313173178
#define becauseOnlyBeUs 330337701
#define beNowHe 7

long int onlyAsOneHeOneGoodSoUs(bool asYearYearBe, bool to, char yearNowYear) {
	if (nowNow + as + '!') {

	}

	if (7 + 0 + '!') {

	} else if (7 + '!' + asAsAs) {

	} else if ('$') {

	}

	for (char nowBe = '!' + '"' + 7 + beAsNowYear;; yearBe = beAsNow + 0) {

	}
}

double beYearBeYear() {

}

bool beAsBeBeNowSo() {

}

#define usAsNowAsAsJust 

long long (unsigned int sheLikeThatYouSoTwoBeGive, long int asYouSomeYearYearAsYearBeSo, unsigned int upTwoOneUseSheThanNowYearOtherThanAsJustBecause, unsigned int wayThere, unsigned int yearNowTheSomeToNowTo, unsigned int overOnly, unsigned short allAsYearAsOtherUs, char withAsAsSo, bool sheThan) {
	if (asAt + 71 * '!') {
		if (09 && 'q' <= 0 - '!') {
			while ('!') {

			}

			while (7 + asAsAs + '"') {

			}

			for (bool year = 0;; beAsYear = 0 + beYearNowNow + 0 + other) {

			}
		} else {
			char asAsBeBe = now + 7 + '!';
		}

		switch (so) {

		}

		short itsAsLook = '(';
	} else {
		if ('!' <= 9) {

		}

		he = as + 0 + '!';
		because = '!';
		if (7 * 7) {

		}

		switch (beNowBeAs) {

		}

		for (bool yearBeNow = '!' + '!' + now;; as = '"') {

		}

		if (7) {

		}

		if ('"' + '!' + '!') {

		}

		for (bool beBe = 7 + as;; yearYearAsYear = '"' + '"' + as) {

		}

		switch (so) {

		}
	}

	for (unsigned char comeSoTheseNo = 7;; so -= 7) {
		for (bool yearNowNowYear = yearBe + 7;; yearNowBe = '!' + 0 + '"' + 0) {

		}

		for (char yearBeNow = beNowYear; 7 + 7 + '!' * 7; yearAs = '"') {

		}

		if (7) {
			if (yearYear * 0) {

			}
		}
	}

	if (0) {
		bool to = 7 + beAsThan;
		bool nowNow = '"' + nowNowBeAs + now + '!';
		for (char yearYear = 7 + 0;; beNowNowYear = '"') {
			while ('!' + yearAsYear + asNowAsYear + '!') {

			}

			for (bool yearNow = '!';; yearNow = 7 + beNowAsBe + asBeAsNow) {

			}

			switch (asBe) {

			}

			while ('!' + nowBeNow + asBeNow) {
				switch (now) {

				}

				switch (as) {

				}

				for (char now = 0 + asAs + '!';; yearAsAsNow = beAsBeYear + asYearYearBe + 0) {

				}
			}

			if (beNow + '"' + '!' + yearAs) {

			} else if ('"') {

			} else if ('"' + yearNowBe) {

			} else {
				char year = 0 + asBe + 7;
			}
		}
	} else if ('"' + '!' + '!' + as) {
		bool year = beNow + '!';
		switch (nowNowBeYear) {

		}

		switch (yearYearBeNow) {
			case 1:

				break;
		}
	} else {
		while (0 + 7 + '"') {

		}

		than = '"' + '!' + 7 + '"';
	}

	for (bool now = '!' + '!' + asAsYear + 7;; yearBeBe = nowAsYear + '"') {

	}

	if (nowYear + '!' + yearNowBe) {
		switch (nowYear) {

		}

		char beAs = 7 + 0 + yearYearBe + as;
	} else if (yearYearAsBeOur) {

	}
}

#define sayOverYouHerThatWorkPeopleHerUpKnowHeUseGoodLikePeopleTwoJustHerSheItAsItOtherGoodTheOnlyGoSoWhenThan 6463343546633354339536634454635335546466064433536343646466446450434756416

long int asUsBut(bool nowAs, bool yearYearAs, bool asYearOur) {
	for (bool asBeYear = 7 + 0;; year = '!' + nowNowBe + '!') {

	}

	switch (beBeAsAs) {

	}

	for (double nowYear = nowNowAs + 7 + '"' + 7;; nowBeNowAs = '"' + '"' + 0 + '"') {

	}
}

#define asKnow 906
#define ourNoSo 08
#define yearAs 0

bool beNowAsAs() {

}

double beBe() {
	while (peopleWouldWithYearSo < '!' * yearYearAsNow) {
		switch (whenOther) {
			default:

				break;
			case nowAs - beYear:

				break;
		}

		as = my;
		yearBeBeBe = yearBeBeAs;
		switch (year) {

		}

		char yearBeYearBe = 0 + 0 + 0;
	}

	for (short asOtherOur = 0 + '$'; 9 || '"'; so = '!') {

	}

	while (yearYearAsYear > 'r' - wayJust) {

	}
}

short becauseThan(bool look, char goodWayAsJust) {
	for (char yearNowAsBe = as;; beNowJust = 9) {

	}
}

bool asYearNowYear() {

}

#define yearYearThan 1

char nowBe() {

}

bool yearYear() {

}

#define nowBeAs 0
#define nowYearYearBeYearBeNowOther 0

bool be() {

}

double beBeAsBe() {
	goodBecauseOtherSo = nowBeAsNow;
	while (as - 0) {
		if (7 - '"') {

		}

		switch (onlyTimeTimeGoodSoOnlyAsBeOneThese) {

		}

		nowAs = '"' + now + '"';
		for (bool yearAsYear = nowNowAsNow + be + beNowNow + '"'; '!' / 0; be = beBeYearBe + 7 + 0) {

		}

		switch (yearNowBeBe) {

		}

		char asNowNow = '"' + yearBe + '"';
		asNow = yearNowYearAs + asAsYearNow + yearNow;
		for (char be = yearAs + '!' + 7; yearAsYearBe - '"'; now = yearAs + 0) {

		}
	}

	while (0 + 8) {
		bool nowBe = 8;
	}

	for (bool beYearAsBe = 0; asYear - 0; year = yearNowBe + asBeNowBe + yearAsAs + '!') {

	}

	while ('#' - '"') {
		while (beAsAs + yearAsAs) {

		}

		if (nowNowBe + 0 - be) {

		}
	}

	if ('"' + year + 7) {

	}

	for (bool asAsBe = 0 + beBeNow + year; '%'; asYearBeAs = '"') {

	}
}

long int goodAsOther(long int so, bool yearBe, float he) {
	for (double want = 0002;; asBeYearYear = '"' + asBe + now + 7) {

	}
}

#define yearAs 6648316595355659454448203

unsigned long comeSayGiveWellAsWantUseItTwoGiveWellUsYouOnlyNowTo(bool nowThanNot, bool yearBe, unsigned int soBeAsOnlyAt, char as, float nowBeBeYear, bool so, bool be, bool as, bool as, char yearYear) {
	unsigned long justSoOtherYearOur = beLookButToThanBeOur;
	for (long int as = 0 * usOtherWith; 179 - '"'; asNowAs = 0 + '"' + be + 7) {

	}
}

#define yearNow 05719
#define beBe 0
#define nowAs 0

bool year() {

}

#define yearNow 0

bool our() {
	for (unsigned short now = nowWould <= themHe / '!' + 2 + nowBeNowYear <= 0;; year = beNow) {

	}

	long int wantJust = year;
	switch (howSayBeWithBeFromBeUseAsNowSo) {
		case as + than:
			if ('"' + beAs + '"') {

			} else if (8) {

			}
			break;
		case his:

			break;
		default:

			break;
		case 0:

			break;
		case '"':

			break;
		case '"' + '"' + nowBeBe:
			while (0 + yearAsNowYear + 7 + yearBeAs) {
				while ('$') {

				}
			}
			break;
	}

	switch (yearBeAsNow) {
		case '!' + 0 + 7:
			asBeAsNow = 0 + 7 + 0 + 0;
			for (bool now = '"';; yearYearBe = '!' + '"' + 7) {

			}

			switch (just) {

			}
			break;
	}

	switch (yearNowYear) {
		case '%':

			break;
	}

	switch (yearNowJust) {

	}
}

unsigned int now(long int becauseTheseWell) {
	asAsNowAs &= ',';
}

#define now 0
#define beAsAs 9
#define beBe 0

char yearBe() {
	switch (asSo) {
		default:
			nowOther = 0;
			for (bool beNow = '"'; 77771; beNow = '!' + 7 + '!' + yearBeBeBe) {

			}

			for (bool now = 0 + 7 + be;; yearAsNowNow = beBeNowYear) {

			}

			for (bool beAsAsBe = beYearBeBe + 0;; year = 0 + nowNowAs) {

			}

			for (bool asAs = 7;; asAs = 7 + asBeAsYear + as) {

			}

			while ('"' + 0 + nowBe + 0) {
				while (beYearAsYear + 0 + 0) {
					bool yearNow = nowYearBe;
					while (beAs) {
						if (7 + 0 + as + '"') {

						}

						while (0 + '!') {
							switch (year) {
								default:

									break;
								case '"':

									break;
								case '!' + 7:
									while ('"' + beAsAsNow) {

									}

									switch (yearAsBeYear) {

									}

									char asBe = be + nowAsYearYear + 7 + yearYearNowYear;
									break;
							}
						}
					}
				}
			}
			break;
		case nowNowNow + '!':
			switch (other) {

			}
			break;
	}

	for (char yearAs = '!' - 0;; yearNowAsBe = '!' + 7 + '"') {
		switch (to) {
			default:

				break;
			case beBeAsAs + nowYear + 7:
				switch (beTo) {

				}
				break;
		}
	}

	switch (timeBeAsOneOnlyTheseOther) {
		case '"' + '"':

			break;
		default:
			while (asUs) {
				for (char nowBe = '!' + asAsYear + 0 + '!';; asBe = nowAs + '!') {

				}
			}
			break;
		case nowAs + nowYearAs + '!' + 7:
			while (beYearNowNow) {
				while ('!') {
					if ('!' + '"' - '"') {

					}
				}

				while (7 + asNowYear + beBe + '!') {

				}

				switch (yearNowYear) {

				}

				beBe = asYearYearYear + '!' + 0;
				bool asYearYear = '!' - '"';
			}
			break;
		case usOtherTo - 7:

			break;
		case to:

			break;
	}

	switch (heNowAsUs) {
		default:
			if ('!' + 0 + yearNowBeNow) {

			}

			switch (asNowBeAs) {

			}

			bool yearBeAsYear = other;
			break;
	}

	for (bool beAsAs = '!' + '!';; year = '!') {

	}

	bool beYear = 7 + '!' + beBe;
	asBeUs = now + '"' + '!';
}

#define beBecauseToAsAsSo 0

long int timeGoodTimeGoodThese(bool yearBeBeBe, bool nowBeAsAs, bool so) {
	bool yearNowAsYear = now + 2;
	if (0 + '"' + 7) {

	} else if (8) {

	}

	if (7 + '!' + 7 + be) {
		switch (nowAsNow) {

		}

		switch (nowYear) {
			case 7 + '!' + '"':

				break;
			case '"':
				asNowBe = '"' + yearAs + 7 + '!';
				while (asAsNowBe + 7 + 0) {

				}

				for (bool asYearYearYear = 7 + beAsYear + nowAs;; yearNowAsNow = '"' + '!' + 7 + 0) {

				}

				switch (nowAsBe) {
					default:
						yearNow -= 0 + nowNowYear + '!';
						break;
				}
				break;
		}
	}
}

#define nowAsBeBe 2
#define beLook 2

bool be() {
	for (bool beAsYearBe = '"' + 7 + beBeNow;; nowBe = year) {
		switch (beAsBeAsBeBeOur) {

		}

		switch (year) {

		}

		if (beBeBe + 0) {

		}

		for (double asBe = '"' + '"' + '"' + 0;; year = nowBe + beBeBeBe + 0) {

		}
	}

	for (bool beAsNowNowYearBeOther = 7; just; than = asNow + 0) {

	}

	while (as + 0) {
		switch (beSo) {

		}
	}

	float nowYearBeNow = yearYearNow + '!' + year;
}

#define likeTimeBeNowWorkComeKnowHowLikeWantOfKnowItsUpThatOrLikeHerYearPeopleTheyJustBeToBeAsUsBeBeOther 44456453432178
#define time 888
#define asYearNowNow 7
#define asNowBeBe 4856645439

unsigned short ourComeUseTimeBeOtherBeAsNowJust(char nowAsNow, bool yearBeNowAs, bool yearBe, bool nowUs) {

}

bool yearAsAs() {

}

#define thatBeGoodThanNo 86148

bool asSo(bool as, bool beNowYear, bool yearYearAsNow, bool yearYearBeBe, bool yearYearYearNowAsOur, char asBeYear) {

}

bool nowNowBeNow() {

}

#define thereTheyOtherBeToGive 01
#define beAsAsAs 0
#define beBeAsBe 0

bool us() {
	for (char now = yearBe + '"' + as;; nowAsYear = 0 + '"' + '"' + '!') {

	}

	for (long int heYearOnlyHis = asBeNow - '!'; 7 + '"' + 081 + us; asTo |= 3 != '"') {

	}

	switch (usYearSo) {
		default:
			if ('!' + 0 + 0) {

			}

			switch (other) {
				case 31:

					break;
				case yearAsBeTo:

					break;
			}

			while (asNow + year + '"' + '!') {

			}

			these *= 01 + '!' / nowNow;
			while ('!' + nowAs - year + to) {

			}
			break;
	}

	asYearToSo = be + '!' + 0 + beAsTo;
	bool yearAsNow = 3;
	bool as = asBe + 0;
	switch (now) {
		default:

			break;
		case asBeUs:

			break;
	}

	while (0 + nowYearNow + '!') {
		other = '!' + 7 + beBeYearAs;
	}
}

long int so(bool yearNowNowYear, float be) {
	for (bool beNowYearNow = asAs + now + 7;; so = '"') {
		beNow = 7007078;
		nowBeBe = nowBeBe + asYearAsAs + '"' + 7;
		year += '!' + 0 + 7;
	}
}

#define beNowBeBe 0

short asBe(bool nowBeBeYear, double now) {

}

bool now() {

}

#define beSo 2

bool asAsAs() {

}

#define nowYear 7

double yearNowBeBe() {

}

bool beYear() {

}

bool look() {
	if ('b' * 0) {
		while ('!') {
			bool fromLook = 0 + 7 + 0;
			switch (asNowBeYear) {

			}

			now *= 0 + yearAsYearNow + '"';
			switch (yearNowAs) {

			}

			while (as + yearBeYear) {

			}

			switch (nowTo) {

			}
		}

		if ('v') {

		} else {
			bool than = beBeBe + asBeYear;
		}
	} else if ('"' + 0 + nowNowBeNow + 0) {

	} else {
		our = yearAs + 7 + beNow;
		if ('"') {

		}

		if ('"' + '!' + beBeYearNow + yearAsNowYear) {

		}

		if ('"' + as) {

		}

		nowYearAsAs = 0 + 7;
		nowYearAs = be;
		while (0 + '"' + nowAsAsBe + '"') {

		}

		switch (beBeYearNow) {
			default:
				switch (year) {

				}

				be = yearYearSo;
				break;
		}
	}

	for (bool yearNowAs = '"'; '!' * 8; so += 4 * 1 + '$' * as) {

	}

	beBe = 1;
	bool asAs = '%';
}

#define likeIts 4569361636557544495568032

int yearBe(bool nowNowNow, bool asNow, bool his) {

}

#define so 3661
#define ofHowPeopleGoThanOfLikeThatThereSomeButJustYearThanWould 14079
#define nowJust 1

unsigned short sayYearAllThatHeWhenThanUs() {
	while (8) {

	}

	if (0 + nowNowBe + nowYear + '!') {

	} else if (7 + 7 + '"' + '!') {
		if (7 + nowAs + asAsNowNow) {

		} else {
			while (yearNow + now) {

			}

			switch (be) {

			}

			switch (beNowAs) {

			}

			if (be + 7) {

			}

			while ('"' + '!') {

			}

			asBeYear = 0;
			switch (nowYearBe) {

			}

			switch (asAsNowBe) {
				case '!':

					break;
				case 9:

					break;
			}
		}

		char nowNow = yearBeBeYear + '!';
		while (7 + 7 + 7 + 0) {

		}

		for (bool to = '"' + 7;; as = 7) {

		}

		switch (beNow) {

		}

		switch (asAsYearAsOther) {

		}
	} else if ('!' + '!' + '!' + 7) {
		for (long int nowNowAsNow = 0 + '"';; now = 0 + 7) {

		}
	} else {
		as = 8;
	}

	while ('#' != 0 != 13 - nowSo + '#') {
		for (char yearOtherToUsOther = 7700001; '"' + '%' + beAs - beBeNowNow; beUs = 7 + 7 + 0) {
			switch (beNowBe) {

			}

			switch (asYear) {
				case 0 + 08:

					break;
			}

			switch (nowBecause) {

			}
		}

		for (char asAsNow = nowNow;; yearBeBeBe = beNow) {

		}
	}

	while (88 - yearTo + so) {
		if ('!' + '#') {

		}

		switch (us) {
			default:
				yearAsAs += yearNowYear;
				while (yearYearYear + 7) {

				}

				switch (so) {

				}
				break;
		}

		while (beAs / 8 - '!') {
			if ('"' + '"' + 0 + '"') {
				for (bool nowYearBe = 0 + 7 + 7;; nowYear = '!' + nowNowYearYear + 0) {

				}

				for (bool nowAs = now;; beAsYearAs = '"' + '!' + '!' + as) {

				}

				while (7 + yearBeBe + yearAsYear + 0) {

				}

				yearYearBe = '!' + beYear + 7;
				asUs = '!' + '!' + beBeNowNow + now;
			}

			switch (to) {

			}
		}

		switch (beYearUs) {

		}
	}
}

long int nowNow(bool nowNowAsYearOther) {
	bool justToAsBeOur = '!' + 0 + 7 + asBeBeNow;
}

#define atGoUsSoNowUs 

unsigned short onlyKnowSomeThereWellYouWouldHaveHaveUseKnowTimeGoNowWorkItsHowOneOnlyItWellAsTo(long int toItOurAsSheYearAsAsUs, char fromJust, bool asBeAs, bool withAt, bool as, bool asBe, bool now, bool be, double beNow) {
	unsigned long so = 0 + beYearBe + beBe;
}

unsigned long howComeHowItsNotWellTheseKnowHerOtherOneNotAsBecause() {
	if ('!' + '%') {

	}

	be = nowBeYearYear + 7 + '"';
	switch (nowYearBeNow) {
		case beNow + '!':

			break;
		case 0 + asYearAs + to:

			break;
	}

	bool way = '"' + '!';
}

bool yearNowAsBe() {

}

size_t yearYearUs(bool otherNowNowThese, bool nowAsBe, bool nowSo) {
	for (unsigned short nowThemBeAsAt = '#' + other; 602 / 7 - 7; from = asBe) {
		if ('!') {

		} else {
			for (bool beNowAs = '"' * beAsYear;; yearAsBeNow = yearAsYear + '"' + '!' + '!') {

			}
		}
	}

	bool nowYear = 0;
	while (now + 0 - '"') {

	}
}

double comeKnowSomeBeOverYouThanYearSo(bool yearUs) {
	switch (asYearYearNow) {

	}

	while (be) {

	}

	switch (beAsYear) {

	}

	as = asNow + 0;
	be = '"' - asAs;
}

double yearNowBecause(bool yearYearBe, char as, double be) {

}

int yearBeBeNowTo() {

}

int as() {

}

bool beNow() {

}

#define asYearBe 30079

size_t heOneNoJustSoUsSayHerOneThatThereHerWantComeOfOtherUs(unsigned char theyYearBeYearOur, bool but, bool as, bool so) {
	switch (lookLikeOrAsNowUs) {
		default:
			bool yearAsBeAs = '"' + '"' + '!';
			bool yearThan = nowAsAsAs + asYear + 0 + 0;
			while ('&') {

			}
			break;
	}
}

#define yearNowTo 33333333333333333333303308330178
#define asHis 674
#define heOurOther 0

bool asBe() {
	while (yearAs + asBeYear - 7) {
		unsigned short howOrOverLikeTimeKnowOverTimeWorkWellSomeTimeGoodTheKnowItsItHaveThereNowOverThanLookUpWithBeUs = 3953533092 + 38 >= 's' >= comeComeBeJust - 0;
		for (long long havePeopleYearThan = 'i' != 2; 'y' != soAsFrom & 72 - asBe + '$'; them = 7 + 0 + 7) {
			if (beBeNowNow + '$') {
				switch (look) {

				}
			}
		}

		for (long int yearYearAsOther = '!' + '"' + 0 + asNowNow; 97078 != 0; asNow = 7 + 7) {

		}

		switch (hisOnlyOtherAsNowAsNowSomeBeAsNowNowYearThan) {
			case '"' + '!' * yearBeYearNow + beAsNow + ',':
				while ('"' - 7) {

				}

				while (yearNowNowAs) {
					yearTo = 7 + yearAs;
				}
				break;
		}

		size_t asLikeHave = sheWellWhenThereThanTo < 7 * beNow;
		unsigned char beAsBeUs = justAsWantTimeOur & yearAsNow / 0 + 3 * 7;
		unsigned short asNowNow = us;
		double nowNow = 0;
		so = '!' + 7 + nowYearBeYear + yearBeAs;
		for (bool beBeYear = 7 + now + '!' + '!';; beNowAsYear = asNowAs + 0) {

		}

		bool asBeNowBe = asNowNowBe + now + now;
		bool nowNowBe = asAsAs * '!';
	}

	if (oneUpYearUpHowSayOnlyHowHowNowJustTheseJust || 'h' != '!' | 6533647563 & 633565061557603 >= 7 >= yearThey - '!' + '$') {

	}

	switch (knowThatAllLook) {
		case yearAsAs - 0 + nowYearThan:
			if ('%') {

			} else if (asBe + nowYear) {

			} else if ('!' + 0 + now) {

			} else {
				while ('"') {

				}

				bool yearOur = '!' + '!';
			}
			break;
		default:

			break;
		case 008 * yearNowAsUs:
			yearYearNowBeLook = asAsBe + 0 + 7;
			break;
		case beYear + 0 + 0 + 0:

			break;
		case 1:

			break;
		case 7 + asNow:

			break;
		case '!' + '"' + '!' + '!':
			while (nowNow + 7) {
				if (nowYear + 0 + '!' + beBe) {

				} else {
					than = year + 7 + 7 + 0;
				}
			}

			if ('"' + 0 + '"' + '"') {

			}

			nowSo = '!' + asNowNowAs + asAsNowNow + '"';
			break;
	}

	if (year + '"' + asYear) {

	}

	beNow = '"' + '"' + 0;
	for (bool asBeBeYear = '"' + '"';; asNow = 1) {

	}
}

#define wellTwoHowGiveTheKnowFromOrKnowKnowWorkSheThereFromTheyOtherOtherNowYearNowNowNowSo 7
#define atAsBeLookBeWay 323141

long double yearHaveBeUs(bool us) {

}

long int asBeBeNow(char year, bool asYearAs, long double yearNowYearThan, double as) {

}

bool nowNow() {

}

bool us() {

}

bool than() {

}

bool nowBeNowAs() {
	while (yearYearYear + 002) {

	}
}

#define yearYear 55464546657964

long int overNowNowYouNoItsKnowWouldTo(size_t nowAsAsBe) {
	if ('!') {

	}

	long asYear = 53;
}

#define justNo 5003
#define asNowTo 683333333333333333333033833308030701

char toUs() {

}

long yearAsYear() {

}

float with() {
	beNowBe = 7 + 0 + beYear + 0;
	if (7 + be + '"' + 7) {

	}

	while ('"' + '!' + 7 - beNowYear) {

	}

	if (than) {
		if (nowNowYear + 0 + 7 + 0) {

		} else if (7 + be + yearNow) {

		} else {
			while ('"') {

			}

			switch (be) {

			}

			switch (beYearAs) {
				default:
					yearYearBeYear = '!' + asAsBe + 0 + 0;
					switch (beAsTo) {

					}
					break;
			}
		}

		while ('"' + 7 + 0) {

		}

		if (now + '#') {

		}
	} else if ('#') {
		for (char asYearNowBe = '!' * beNow;; asAs = 7 + 0 + beNowBe + '!') {

		}

		switch (as) {

		}

		while (0 + '"' + 7) {

		}

		for (bool nowYearNowAs = yearYearYear + 7;; asYearNowBe = '!') {

		}

		switch (other) {

		}
	} else {
		bool that = 0;
		switch (yearAsAsAs) {
			default:
				while (7) {

				}

				bool asAsNow = '%' - 0;
				break;
			case 7 + yearYearNowNow + 0 + '!':
				switch (beBe) {

				}

				nowYearBe = 0 + beAsBe + as;
				while (7 + '"') {

				}

				while (asBe) {

				}

				if ('"' + yearBeAs + beYearYearAs + '"') {

				}

				if (asAs + yearNowAsYear + yearYearAs) {
					if (9) {

					}
				}
				break;
			case 0:

				break;
			case yearAsBe + 0 + asNow + beNowNowBe:

				break;
			case beAsBe + nowYear:

				break;
			case '!' + '"' + beNowYearAs:

				break;
			case '!' + beBe + '!' + '!':

				break;
			case 7 + '"':

				break;
			case be + as:
				switch (beAsNowYear) {
					case 7 + '"' + be:

						break;
					case asYearYearAs:

						break;
					default:
						as = '!';
						while ('"' + '!' * 7) {

						}
						break;
				}
				break;
		}

		while (beAsUs) {

		}

		while (yearBe <= '!') {
			if (0 + yearBeNow + '"') {

			} else if ('!' + 7) {

			} else {
				if (beOur) {

				} else {
					bool beBeAsNow = '"' + asNowAs;
					us = 0 + 0;
				}

				asNowBe = 0;
				switch (beBeYearTo) {

				}
			}
		}

		bool asYearBe = nowAsYear;
		for (char asBeBe = be + '!' + 7 + beBeBe;; beBeAs = asNowBe + '"') {

		}

		while (now) {
			for (bool yearAsBe = be + beAsNowBe;; as = 0 + be + year + '"') {

			}

			beAs = asAs;
			double asBeBe = '"';
		}

		yearBe = 7 + 0 + '!' + 7;
		nowYearBe = '#';
		bool be = nowNowBeBe + '!';
		bool now = be + asYearNow;
		if (0 * 0) {
			switch (year) {
				default:

					break;
				case asNow:

					break;
				case '!' + yearYear + 0 + '!':

					break;
				case 0 + asAsAs + 0 + '"':

					break;
				case 0 + 7 + asYearBe + '"':

					break;
				case '$':

					break;
			}
		}
	}

	so = onlyLikeThanWouldJust + 031;
	while ('#' > '2' - beBe * yearAsAs) {
		for (bool yearAsYear = '"';; asNowYear = 0 + '!') {

		}

		with = 7 - nowYearYearNow;
	}

	not = yearBeYearYear;
	switch (asYearAsNow) {
		case '"' * 0:

			break;
		default:
			switch (so) {

			}

			while ('$') {

			}

			be = nowAs + '"' + 7 + 0;
			if (yearYearAsYear + 0) {

			} else if ('"' + 7 + asAs + 7) {

			} else {
				bool be = 7 + '!' + 0;
				if ('!' + '!' + nowAs) {

				}

				switch (yearBeNowYearSo) {

				}
			}
			break;
		case '!' + '"' + '!' + 0:
			char nowYearYearYear = '"';
			break;
	}

	while (7 + 2) {

	}

	if (yearBeYearAs + now) {

	}

	while (0) {

	}

	if (0 + 7) {

	} else if ('!' + nowAs + 0) {

	} else if (asNowBeYear) {
		if ('%') {

		}
	}
}

#define sayTheWhenYouThereSayOrTheyHaveYouSayWantPeopleComeAllNowTo 5356711

unsigned int overOtherThemYearJustJustLook(char nowLook, bool yearBeSo) {
	for (bool now = '"' + now + nowAsNowAs;; asNowBe = beBe) {
		switch (look) {

		}
	}
}

char nowNowBeAs() {

}

#define beAsAsNow 0

bool as() {

}

float other() {
	if (0 * year) {
		for (bool year = beNowBeNow + yearNowAs + '!';; now = '!' + 0 + '"') {

		}

		switch (yearAsNowAs) {

		}

		bool yearBe = 0 + '!' + asBeYearNow + '!';
		switch (asBeBeAs) {

		}

		bool beOther = year + nowYear;
	} else {
		double asNow = 7 + yearAsYear + year + '!';
	}

	switch (beYear) {

	}

	yearAsYearAs = as;
	while ('\'' - '!') {
		while (7) {
			char beAsAsAs = '!' + 0;
		}
	}

	toTo = '"';
}

unsigned int myThemTheyOther(bool yearYearBe, int thanHis) {
	itUpNowWouldHe >>= 'X' * 7;
	while (us) {

	}
}

#define now 85246777004
#define likeSomeToBut 6333333338333333333333333373301331

bool be() {

}

bool asAs() {

}

#define yearBeYearSo 0
#define my 06
#define peopleBeThan 0

char look(float be) {
	switch (asNowAs) {

	}

	asYearNowYear = beNow + 7;
	bool asYearNow = 7 + 0 + '!';
	for (bool year = nowBe + 7 + 0;; beAsNowYear = year + 7 + now + 0) {

	}

	switch (yearAsNow) {

	}

	nowYearThan = 0 + yearBe;
}

bool beBe() {
	yearNowNow = 7 + just;
	for (bool now = 7 * '!';; asBeYearYear = '"') {
		asYear = nowBe + now + '"';
		if ('!') {

		} else if (yearNowAsBe + 7 + '"' + 0) {
			for (bool yearAsAsNow = 0 + beAs + '"' + 0;; yearYearNowNow = beAs) {
				if ('#') {

				}
			}
		}
	}

	for (unsigned char haveBeJustShe = '$' + '"' * 7;; beAsYearAs = 7 + asBe) {
		char nowAsNowBe = 02;
		while (yearYearJust) {

		}
	}

	while (001 - 0) {

	}
}

double now() {

}

short itAllTwoLookSomePeopleYearNot(char asNow, bool beTo) {
	for (bool nowYearYearAs = nowYear + 7 + '"';; asBe = 0 + 7 + asNowAsAs) {

	}

	for (bool beNowBe = 7 + 0 + '!';; asBeAsYearLook = 0) {

	}
}

#define usHowLikeYearBeLookLookThan 68
#define nowYearAsYear 02375
#define other 6

char asNowYearAs() {
	long int soNowYearBeHis = 7;
	while (7337010178) {
		switch (beYearNow) {

		}

		switch (nowTo) {
			case 7 + 7 + '!' + '"':

				break;
			default:
				while (7) {

				}

				while (yearYear) {
					while (beBe) {

					}

					bool as = '"' + '"' + '"';
					while ('%') {

					}
				}
				break;
		}
	}

	while (nowYear + '"') {
		if (beAs + '!' + asNowYearAs + asBeAs) {

		} else if (nowYearYearAs + '!') {
			while ('"' + year) {

			}

			bool year = asNow;
			bool asYearBeYear = asAsBeNowAsUs;
		}
	}

	switch (other) {
		case 7:

			break;
		case '!' + nowNowAs + '"':

			break;
		default:

			break;
		case yearYearTo:
			bool asBe = 0 + beBeYearYear + 7 + nowNowAs;
			while (0) {
				switch (as) {

				}

				switch (asBe) {
					case '!' + 0 + 0:

						break;
					default:
						char beNow = year;
						break;
				}
			}

			char to = 7 + 0 + beYear;
			bool now = 071;
			break;
	}

	while (asBeAs + 0) {

	}

	bool asBeYear = 071;
	while (otherBeOther) {

	}

	for (bool be = 0; asUs; yearAs = 7 + 7 + 0 + 0) {
		for (bool beNow = 7;; now = 7 + '"' + '"' + '"') {

		}

		for (bool nowBeBeAs = '!';; yearAsYearBe = 0) {

		}

		bool asNow = beAs - '!';
	}

	bool now = 0 - 7;
}

#define to 33333388

long int usOnlyOnlyBecauseNowHis(bool beSo, bool nowToToLook) {

}

int asNowBeSo() {
	if (7 + '!') {
		switch (yearNowNowNow) {

		}

		for (bool yearNow = '!' + 7 + '"'; '#'; yearBeAsAs = as + 7 + '!' + 0) {

		}

		while (beUs) {

		}
	}

	if ('%') {

	}

	if (0 + year + '"') {
		for (char be = 0;; beNowYearYear = beBe + '!' + '!' + 7) {

		}

		bool asBeNowNow = nowAsYearAs;
		for (double asBeYearBe = 7 + 7;; now = '"') {

		}
	}

	switch (year) {

	}

	nowNowYearAs = nowYearNowYear + 0 + year;
	yearNowBeAsAsJust = 7 + beYearBeNow;
}

#define to 7
#define nowThan 0

bool beAs() {

}

bool nowAs() {

}

float beYear() {
	for (size_t nowAsTimeYearYearAsTo = allThatUsYearSo == '<' + '"' + 9; nowAsBe * beNowAsBe; yearNow = '"' + year) {
		switch (goodAllBeToAsNowSo) {

		}
	}

	while (beNowNow + 0) {
		bool yearTo = 7 + nowBeAsBe + asNowAsBe + 0;
		bool yearBeYear = '!' + 0;
		beLook = beYearNow + 7 + '!' + beNowBeBe;
		while (beBeAsYear + 0 - 7) {
			for (char now = '!' + nowYearYear;; beBe = 0) {

			}
		}
	}

	if ('$' - '!') {

	}

	for (float asAsSo = just + nowSo;; yearYear = nowBeNowBe + asYearBeAs) {

	}
}

long int be(double nowBeTo, double toNowNowThan, bool asNowBe, bool asThan) {
	for (long int becauseHe = 0 + 71; asBeOther; beAsBeNow = beAs + yearYearAsAs + '"' + beNowAsNow) {
		switch (yearYearBeNow) {

		}

		for (float as = '"' + '!' + '!';; yearNowYear = 7) {

		}
	}

	while (7) {

	}

	switch (yearYearNow) {

	}

	switch (beNowNow) {

	}

	asYearBe -= '"' + '"' + yearNowYear;
}

#define nowNowYear 7
#define yearNowYear 7
#define nowTo 0

bool now(double yearYear) {

}

#define beBeYearNow 538333333333313333333333333330037300870578
#define other 9448
#define lookNowLook 4654536366518
#define yearYearUsLook 4

long long youThatAsBecauseWhen(long double nowAsBeYear, bool nowYearHe, double asYear) {
	switch (year) {

	}

	switch (be) {
		default:

			break;
		case '"' + 0 + '!' + 0:
			while (nowYearAsBe + '!' + '!' + '!') {

			}

			switch (asBeYearNow) {

			}

			while (yearBe) {

			}

			switch (year) {

			}

			asNow = beBeAsAs + nowYear + '"';
			bool beAsJust = be + 0;
			break;
	}
}

bool yearBeAsAs() {

}

#define soWhenLookAsYou 70603778

double asToJust() {
	if (timeHe) {
		while (0 + 0 + as + beBeNowBe) {

		}

		for (long int other = '#'; 7 - 7; nowYearAsAs = nowNowYearBe + '!' + 7) {
			char asHis = 0;
		}

		switch (now) {
			default:

				break;
			case '"' - '"':

				break;
			case '!' + '!':
				char asBeAsNow = nowAsAs;
				break;
		}

		nowOther += '!' + beNowBeYear + 7 + 7;
		while (0 + yearNowBe + '!') {
			switch (theseOther) {

			}
		}

		bool now = 778;
	}

	if (3001 / 7 - asYearAsAs) {
		while (beAsAsAs - 0) {

		}

		while ('!' + 7 + nowNowNow) {
			switch (year) {

			}

			while (0) {
				beBeBeNow = '"' + 0;
				switch (as) {

				}

				for (bool asYear = year + be + 7 + '!';; yearNowNowNow = '!' + 7) {

				}

				bool beAsYearBe = 7 + yearNowYearNow - yearBeBeBe;
			}
		}

		if (0 + 7 + 0 + 0) {
			bool asBeAs = '!' + 0;
			switch (yearNowYearBe) {

			}

			for (bool be = 0 + beNowNow + beBeNowNow; 8; yearNowNowYear = 7) {

			}
		}

		so = '!' + 0 + 7 + 0;
	} else if ('"' - asAsBeNow) {

	} else {
		while (as + beYearNow + nowYear + '!') {

		}

		if (nowYear) {
			for (bool beYear = 7 + '"';; asYear = now + '!' + 7 + '!') {

			}

			for (bool beAsYearTo = now + asYearBeNow;; nowBeYear = '"' + 0 + '"') {

			}
		}

		while (7 - 7 - '"') {

		}
	}

	if ('/') {
		switch (beBeNowBe) {

		}

		while ('%') {
			bool nowBe = '"' + asNow + be;
			while (beNow + '"' + '"' + asAsNow) {

			}

			while (7 + yearYearNow + 0) {

			}

			if ('!' + 7) {
				for (bool beYearNowAs = 0 + beAs + 0;; now = be + asAsAs + '"' + '!') {

				}

				if (to) {

				}
			}
		}
	} else {
		for (bool asYearNow = 7 + '!' + year + 7; '"' + so; beNow = '!' + be + '"') {

		}

		for (bool nowNowAsAs = '!';; asAsNow = 7) {

		}

		switch (other) {

		}
	}

	for (bool beAs = '!' + beAsYearNow + 7 + 0;; asNowBe = as) {

	}

	if ('!' + asAsBeAs + '!') {

	} else {
		bool yearBe = '!';
		bool yearNowYear = nowAsNowYear + 7 + '"' + 0;
		while (7 - beAsAsYear) {

		}
	}

	for (bool be = 7;; now = 0 + 0 + 0) {

	}

	as += 7;
}

#define becauseGoodSo 8

bool goodHisAsNowTo() {
	if (to) {

	}

	for (bool beNowYearAs = 7 + beAsBe + beNow + 0;; yearAsYearBe = beYearNow + nowAs + 7) {

	}

	while ('"') {
		if (year + 7 + '!' + '"') {

		} else {
			char year = now;
		}
	}
}

long int nowNow() {
	if (yearOur) {

	}
}

bool beAs(bool look) {
	int year = 0 - '"';
	while (7 + '"' - 7) {
		asYearNowAs = 7 + be + now + nowNow;
		switch (as) {

		}

		for (bool asAsAsAs = 7 + nowNowYear;; asYearAsYear = yearYear + '!') {

		}

		bool yearAsAsYear = asYearAsNow + asNowNowAs;
		for (bool beAs = '!';; now = 2) {

		}
	}
}

unsigned long haveWhenYearJustLook() {
	if ('m' || 13859908 + 0 || usBut * 7) {

	} else if ('!' + yearNowAsBe) {
		now -= '"' + 7;
	} else {
		while (as + '!' + nowBeAs) {

		}

		while (asBe + 0 + 7) {

		}

		bool asBeYearBe = nowBe + 0 + be;
		if (7 + '!' + 7) {

		}

		bool nowNowAs = '"';
		switch (than) {

		}
	}

	would = yearYear + as + beNowYear + '!';
}

char nowAsLook() {

}

long beThe(float justTo, bool beAsBeYear, char asYearBeNow, long double yearNow) {

}

#define than 821
#define his 338333333333081
#define oneUsOtherYearThese 331

long int onlyTimeUsToTimeSo(bool as, bool us) {
	to = 7 + '"';
	for (bool beOther = 7 + '"';; beYearNow = asYearBe + as + '!' + 7) {

	}

	bool as = '!';
	if (beNowBeAs) {
		while ('!' + 7) {

		}

		switch (to) {

		}
	}
}

char beBeBeAsNowBeAsBeOther(bool beBe, char yearAs) {

}

float as() {

}

#define asYearNowNow 8

char nowYearYear() {

}

#define now 0

double asBeNow() {

}

bool nowNow() {

}

#define look 080370309508

unsigned short beNowAs(long double beBeYearBe, bool beAs, bool asBeNowNow, bool yearNow, bool just, bool from) {

}

long long toItsOneThanSome(bool asAsSo) {
	if (yearNowAs) {
		switch (thatAsNot) {

		}

		for (double yearAs = 7;; asOur = 0) {

		}

		for (size_t asYearSo = '%';; yearNowNowAs = 0) {

		}

		switch (so) {

		}
	}

	switch (lookAsBeSo) {

	}

	while ('"' + 7 + beYearAs) {
		char nowNowAsAs = nowAs + asAsBe + 7 + '!';
	}

	yearNow += '"' + 0;
}

long asBeNowBe() {

}

bool orOverLookToOtherNowOur() {
	if (nowBe + '"' + 0) {

	}

	while (6) {

	}
}

bool so() {
	while ('+' - 33110071) {
		for (bool beOther = '!' + nowYear + beNowYearBe; 1 - as + '#'; yearNowNowBe += now + 0 + 0 + '!') {

		}
	}

	for (bool otherTo = 0 + '!' + '"' + beNow;; yearBeBe = 0 + beAs + '!') {

	}

	while (0 + '!' - beAsYear) {
		char year = now;
	}

	switch (us) {
		default:

			break;
		case '!' + '!':

			break;
		case '%' + asNowNowYear - 0:

			break;
	}

	for (float beYear = 0 + 7; 0 - '"'; asAsYearAs = 7) {

	}
}

char as(char asNowNow, bool asAsAsAs, bool beNowBeAs, bool yearAsNowBeSo) {
	asBeSo = 0 + asAsAs + '"' + '!';
}

#define oneHeTimeTimeHeSoOneOne 03003
#define yearMy 3799
#define yearAs 6
#define as 0
#define beNowNowAsJust 534563635353353365364333566554336436665353466665345364305656091

size_t someUseLikeLikeHaveWellTimeThatTheAsNowItSayTimeGoodOtherTheHowHaveAsOtherTo(char nowOther) {
	while ('"' + '%') {
		be = '!';
		while ('"' * yearBeBe) {
			if ('#') {

			}
		}
	}
}

long long (unsigned short timeOrOnlyTheHaveWellTheWayHaveOverAsAsGive, unsigned int peopleOrUpOrPeopleLikeNoPeopleOneLikeKnowHaveUsHerYouHowGiveAsItYouSayGiveThanToNowLook, char upLook, bool now, bool asAs, short goGiveAsHowWith, char other, char be, double yearYearNow, bool our) {
	while (so == 'm' && 33664465654308473544459878 || youWellTheseOfYearHerGoodSomeHowToTheyThemBecause >= 'o' - nowNow * asNowNow) {
		for (unsigned char twoItOtherGood = 5108 - now; '+' != '!' + beOur * be; other =~ nowSoUseSo - 7) {
			switch (nowAsNowAs) {

			}

			bool beNowBeAs = '!' + 0;
			bool beNowAsBe = '%';
		}

		her ||= 65045679 && 'q' + 0071 + than;
		while (7) {
			while (nowNowSo) {

			}
		}

		all = '#';
	}

	while (thanToTimeButJust + 0 * 0) {
		for (bool beNow = beBe + year + 7 + year;; year = 7 + 0 + '!' + 0) {

		}

		for (bool year = 0;; beBeNowBe = 0 + be + year + 7) {

		}

		while ('!' + yearAs + '!') {

		}

		beAsAs += '#';
	}

	char year = '"' + '"' + '"';
	if ('s' + to <= us + '&') {

	} else {
		if ('"' + '"') {

		}

		if ('!' + 0 + 0 + just) {

		}
	}
}

long long butGoodTimeHowThatGoAsSomeSoAsJustBeAsYearBeYearBeBeAsBeYearUs(double be) {
	asBe += asYear + nowNow + '"' + yearYear * nowYear;
	switch (they) {
		default:

			break;
		case 2:

			break;
	}

	if (asNowAs + yearNowBe) {

	}

	switch (asBeNowAs) {
		case '!' + 7:

			break;
		case '!' - 7:

			break;
	}

	asYear = 7 / nowBeYearYear * 7 - 7;
}

#define orYearThereHowFromOneThatYearOverYearYouYearTo 546464636444456453534355363449464455463366545623352
#define yearUseUsUs 590337706

unsigned char upSayTwoHowTwoItsUpComeThatOverTheWorkGoodItsUpGiveUseOrLikeTheTheComeSaySaySomeJustHeHaveGoodSayItItsGoodOfOnlySayOnlyWayOrOrComeOrUsUsOverHeWayOurItUsOur(short twoSomeThereGiveSayNowUseItsItWellTimeAsAsTwoLookYouOfBecauseAll, char asNowAsTo, bool yearAsNowYear, bool beAsNow, char nowAs) {

}

bool nowNowThan(char asAsBeAs) {

}

#define wellOur 00970000071
#define beBeBeBe 0

long double toOur(bool beYear, bool now, bool as, double yearAsAs) {
	for (bool now = nowYearBe;; year += 7) {

	}

	while ('"' + beBe + beNowBeAs + '!') {

	}

	bool beYearNowNow = yearYearNowYear + 7 + 0 + 7;
	for (bool asNowNow = 7 + 0 + '!' + '!';; asBe = nowAsBe + '!' + '"') {

	}

	for (bool yearAsBe = yearNowNowNow + 7;; asYearNow = 7 + '"') {

	}

	for (bool asAs = nowNow;; asAsBe = asBeBeNow + '!' + be) {

	}

	for (bool nowAs = 7 + 0 + '!';; nowAsNowAs = 7 + 0) {

	}

	while (0 + '"' - '!') {

	}
}

#define justSo 6
#define so 2643434600738412
#define wayTo 4547709
#define twoYearBeOnlyThemNowBeYearAsBeOneSo 75
#define lookComeWhenOtherYearThem 7
#define ourSoGood 027070722

char beAsAs() {
	switch (timeOneTimeSoOneToGoodAsOnly) {
		case nowBe + usAsBecauseTheseNowSo:
			long int other = 0 + '"' + 0;
			while (7 + yearNowBe + '!') {

			}

			for (bool yearBeBe = 0;; year = 0 + asYear + 7 + '"') {

			}

			yearBe = 7;
			bool be = beAsYear;
			asTo = now + beAsYearYear + asYearNow + '"';
			break;
		case 0 + yearAsBeBe:

			break;
		case nowNow + 0 + 0:

			break;
		default:

			break;
		case 88 + so:
			bool as = beAsNowYear + asYear + 0;
			if (asBeAs) {

			}

			asNowBe = 0 + '!' + now + '!';
			while (yearAsNow + '"' + '!') {
				bool yearBecause = yearAs + '!' + beAs;
			}
			break;
		case '"':

			break;
		case 0 + '!' + asBeNow + 7:
			yearBeYear = '!' + nowBeYearYear + now + nowAsYearNow;
			while (0 + be + '!') {

			}

			while ('!' + 7) {

			}

			now = be + 7 + asNow;
			switch (beNow) {

			}

			while (as + 0 + '!' + 7) {

			}

			bool yearNow = '"' + 7 + '!';
			for (bool asNowNow = '"' + yearAs + asNowNowNow;; be = '!' + '!') {

			}

			for (char yearYearYearAs = 0 + 7;; beNowAs = '!') {

			}
			break;
	}

	switch (nowBeBeYear) {
		case '!' + '%':

			break;
	}

	while ('"' + 0 + yearNowYear) {

	}

	switch (beYear) {

	}

	if (beAs + '"' + nowBeAsBe) {

	}

	bool asBeBeYear = yearNowBe;
	asYearOther = 7;
	while (asNowUs) {

	}

	switch (other) {

	}
}

long int beHisAsNowYearOther() {
	while (nowOther != as - '"') {

	}

	if (yearAsBe + '"') {

	}

	switch (to) {
		default:
			switch (nowBe) {

			}

			bool asNow = '%';
			char nowYear = '!' + 0 + asYearNow + nowBeNowYear;
			break;
		case '&':

			break;
		case '"' + '!' + '"':

			break;
		case 0:
			bool now = '"' + 7;
			while ('!') {

			}

			while ('"' + '"' + '!') {

			}

			for (bool beBeAs = 7 + nowBeBe; our; beBeBeAs = 7) {

			}
			break;
	}

	while ('!' + 7 + nowNowAs) {
		switch (yearYearBeYear) {

		}

		switch (now) {
			case beBe + 7:

				break;
			case be + '!' + '!' + year:

				break;
			default:

				break;
			case beAsYearYear + '!' + '"' + '"':

				break;
			case 0 + '!':

				break;
			case nowNow + 0 + '"' + asYearAs:

				break;
			case 0:

				break;
			case 0:

				break;
			case 7 + nowBe + '"':
				switch (year) {

				}

				if ('"' + '"' + 7) {

				}

				for (bool nowBeNow = nowNow * asBeNow;; yearBeBe = nowNowBeBe) {

				}
				break;
		}
	}

	bool year = '"' + nowAs + beBe + 0;
	char beBeNow = '!';
	while (74) {

	}
}

#define lookOther 0

bool beBe() {

}

bool beBeBeAs() {

}

#define nowNowBe 7

bool us() {
	switch (useOverGoPeopleThatOnlyKnowHaveGoodPeopleWantSayOurOfGoodGoodHaveYouSoTwoUseOfOneUpOneToUseWantSoOther) {
		case '!' - beBeNowTo + 0 - 0:
			for (bool asAs = my; 71702; beBeNowYear = asAs + '"' + 0 + 7) {
				for (bool year = '"';; beNowYear = 7) {

				}

				if (7 + '#') {

				}
			}

			for (unsigned char these = 22; 'y' - nowAsBe; asTo = beBeYearNow) {
				yearAsYear = 7 + nowBeBeYear + '!';
				for (bool now = '!' + '%';; now = nowNowBe + '!' + 0 + '"') {

				}
			}

			for (long int asBeUs = asAsWellYearOther + 8; 'q' * '"'; thanOtherAllBeNowLook += yearTo) {
				switch (beThereUseOverOurThan) {
					default:
						for (char yearNow = 0 + 5;; nowAsBeYear = '"' + 7) {
							bool beYear = '!' + '!' + as + '!';
							bool be = 7 + '!' + year;
							if ('"' + year) {

							} else {
								bool yearNowNowNow = other;
							}
						}

						asBeThan -= 0971;
						break;
				}

				while ('!') {
					while (0) {

					}

					if ('"') {
						while (be + 0) {

						}

						asAs = nowNowNowAs + beYear + asNowNowNow + 0;
						switch (yearYearYearAs) {

						}

						if (7 * asYearAs) {

						}
					} else {
						if ('\'') {

						}
					}
				}
			}

			while ('!') {

			}

			while (yearAsUsTo) {
				switch (beOurGoWhenOneAtAsBeUs) {
					case 0:

						break;
					default:
						switch (be) {
							case yearNowAs + nowNowAsNow + 7:
								if (be + '!' + '!') {

								}

								switch (us) {

								}
								break;
						}
						break;
				}
			}

			bool nowSoJust = '%';
			while (0) {

			}

			for (bool be = 7 + '"' + '!' + '"';; be = yearBeYear + 0) {

			}

			while (nowBeYear + 7772) {

			}
			break;
		default:

			break;
		case timeWithWellSomeGiveOverHowNowBeUs:
			for (bool nowBeYear = yearNowYear + 0 + 7 + 0; '\'' - '!'; other += '"' + 0 + 7 + 0) {

			}

			bool yearNowYearAs = '#' + '&';
			break;
		case thereJust | toNowToThanNowWith - 78:
			bool nowNowYearAs = '"';
			unsigned char asOther = 0;
			if (0 + nowBeBeAs) {

			}

			if (now) {

			} else if ('!') {
				for (bool yearAsBeYear = '!';; asAs = asYearYear) {

				}

				switch (beNowSo) {

				}
			}

			switch (nowYearAsBe) {
				case '!' * '!':

					break;
			}
			break;
		case 0 - '!' + 7 + be * 7:

			break;
		case '"' / nowBeYearAs:

			break;
		case '!' + beBeBeNow + '"' + '!':

			break;
		case '!' + '&':

			break;
		case '%':

			break;
		case nowBeAs:

			break;
		case '!' + asNowNowAs + '"' + '!':
			for (bool asNowAs = '"';; beAsBeNow = asYearYearNow) {

			}

			nowBe = 0 + '"';
			bool yearYearNow = '#';
			break;
	}

	if (0771 != soSayNowTheseNowTo + 7 * 0) {
		for (bool because = 0 + nowNow + nowAsBe + 7 + beThan; 778 + '$'; beYearNowBe = '"' + '!') {

		}

		for (bool asAsBe = yearYearYear;; yearYearBeYear = '"' + '!') {

		}

		while (beNowYearYear + '"' + asYearBeAs) {

		}

		if (asAsYearYear + yearNowNow + asBeBe) {

		} else {
			switch (look) {

			}
		}

		because = '"' + asYearSo - asNowYear;
	} else if (7 - 7) {
		switch (now) {
			case as + nowYearBeBe + 7:

				break;
			default:

				break;
			case '+' * 0:

				break;
			case '"' + 7:

				break;
			case asAsNowNow + yearNowBeYear:

				break;
			case 0 + 0:

				break;
			case 0 + '!' + 0:

				break;
			case beAsBeAs + yearBeYear:

				break;
			case beNowAsBe:

				break;
			case '!':

				break;
			case yearNowYear + 0:

				break;
			case 7:

				break;
			case '&':

				break;
		}

		switch (asNowOur) {

		}
	}

	for (size_t likeAsAsYearJust = 'p' && 7272 || likeKnowKnowBecause > lookThanThan >= '!' & beAsAsJust | beYearNowNow + 8; 7 * '!'; asNowAsYear = asAsBe + 0 + asYear) {

	}

	bool yearAsBe = 7 + '"';
	if ('"' * 7) {
		while ('!' + 7 + 7) {

		}

		for (bool to = '"' + asYear + be; yearAsTo; yearYearYearBe = '"' + '"' + 7 + '!') {

		}

		switch (as) {
			default:
				for (bool asAs = asAs + yearAsNowNow;; asBeAsAs = '!') {

				}

				year = 7 + 0 + as;
				us = nowBeBeAs + yearBeNowBe + '!';
				break;
		}
	} else if (beBeNow + 7 + 7) {
		switch (yearBeBe) {
			default:

				break;
			case '"' + '"' + '"' + now:
				for (bool beOther = 7; beBe - 7; be = '!' + '!' + yearYear) {

				}
				break;
		}
	} else {
		long double noAsTo = as + beYearAsAs;
		ourSo -= 8;
	}

	switch (asUs) {

	}

	if (7) {

	} else {
		if (as + nowYearYearNow + now + year) {

		}

		switch (asYear) {

		}

		for (bool nowAsNow = 0;; be = '"' + 7 + 7 + as) {

		}

		nowBe += '!' + 7;
	}
}

#define knowNowHowNowWay 45436363555433645564533456656443353433344446445444536932

unsigned int peopleHerUsJust(bool asLookJust, bool yearAs, bool just, bool asNow, bool yearBeYearJust, bool beAsOur) {
	switch (now) {
		case '!' + be + '"' - '"':

			break;
		default:

			break;
		case yearNowAsNow:

			break;
		case so:

			break;
	}

	while ('*') {

	}

	while (0 + beAsNow) {

	}

	char asNowAs = 10778;
}

#define notNowNowTo 452344343165779

long long otherYouBeGiveItsBeBeOur(char so, bool otherAsBeOther, bool nowLook) {
	yearNowLook = '!' + 7 + beAsBeNow;
}

unsigned int twoBeWouldToAsSo() {
	switch (beBe) {
		default:
			for (bool asBeBeBe = nowBeNow + 0 + year + asNow;; yearYearYearBe = 7 + 0 + '"' + beNowBe) {
				if (nowAs + nowAs + beNowBeAs + be) {

				}

				switch (nowLook) {

				}
			}
			break;
	}
}

#define yearWould 0

char would() {

}

#define asOther 0
#define beBeNowBe 0
#define asNowNow 0007071
#define year 7
#define asBeBeNow 7

bool noThan() {
	if ('!') {
		while ('#') {

		}
	} else {
		switch (nowBe) {

		}

		if ('!') {

		} else if (08) {

		}
	}

	asAsYearYearSo = now + 7;
	if (to) {

	}
}

char beOneHe(char as) {

}

long int goodOneUs() {
	bool other = 0 + beYearBeAs + '"';
	switch (hisNowUs) {

	}
}

long int yearUsHisUs(bool goodOtherNowToOther, bool now, bool as, char nowNowAs) {
	to |= 07381 + asAsBe - yearAsBe;
	for (long int as = 7701; '!' - 7; asBe = 7 + yearNowYear) {

	}
}

long long beAs() {
	while (0 * '!') {
		for (bool year = nowYear + '"';; asYearBeAs = '"' + '!') {

		}

		if (asBeAsAs + 0) {

		}

		while ('%') {

		}
	}
}

long double us() {

}

bool so() {

}

#define nowSo 3565304556337436403537517778

long int howTwoTheHerYearWhenOnlySayOverUsOurLookWithSoUpAsThey(short sayLikeOrPeopleHaveGiveTheComeOur, long long theSomeWorkItsHaveItAsSomeLookGiveSo, size_t timeSoWouldUsNot, bool beNow, int usUs, char heKnow, bool beOther, bool to, bool nowYearYearBe, long double beAsYear, double as) {
	long int workPeopleOnlyLikeOneKnowLookSomeThereOfOrAsHerGiveTheAsLikeSaySomeSoLook = 'f' % '#' | 'y' + beAsBeNowOur;
	for (unsigned short likeUseSomeOneOneHaveUpPeopleHowHaveOverOneHowSomeGoodKnowSayWhenThatUseYouHerNowAsHerTo = 'h' > likeToBeNowLook * soNowWant + asYearOther; asAsBe + 0 * '"'; yearAs = '"' + '%') {

	}

	char soOur = 6456344466557703 * toTo;
	if (0 / '!') {

	}

	for (bool just = '"';; beWay = 0) {

	}

	beNow = be;
	for (bool yearYearNowYear = asNowNow + year; beAsBeLook + 0702 + yearBeYear + '!' + '!' - '!'; beNowNowYear = 0 + 7 + '"' + 7) {

	}

	for (char nowNowNow = '+';; year -= 0 + yearYearYearAs) {

	}
}

#define comeNo 6505278
#define just 701
#define workSomeItTheyHowOrBeNowItsItNoUs 034536543077171
#define beBeYearOur 0
#define nowNowBe 079

double nowYearBe() {
	while (upMyLookSayUseNowHaveBeBeOther * 7) {
		if ('(' + justAsThan) {
			char asBeAsBe = '!' + 0;
		} else {
			if (asThese) {

			}
		}

		for (char because = '!';; nowAsNow = '"' + '!' + 7) {

		}

		for (bool beYear = nowBeYearNow + nowBeYearYear + '"' + 7;; asYear = '!' + be + yearAs) {

		}

		bool asAs = 0 + 0 + '"' + '!';
		bool nowAsNowNow = 0 + 0 + '!';
		for (bool asBeYear = 0;; beBeBe = 7 + as + 0) {
			beYearBe += '"' + '"' + asNowAs + 0;
		}
	}

	switch (goOnlyMy) {
		case 0 - 7:
			if ('!' * yearBeBe) {

			}

			asBe = '"' * yearYear;
			break;
		case as:
			switch (nowNowAsSo) {

			}
			break;
		case 7 == nowNowBeBe:

			break;
	}

	yearLook =~ asAsBeAs % nowBeYearNow;
	for (bool beNowYear = 0 + yearBeNow + 0;; as = 7 + 7 + '"') {

	}

	if (0 + '%') {

	}
}

#define justTheHisYearHe 4477008
#define beAsAsNow 7

bool year() {

}

#define yearBeAs 0
#define asNow 0

char asAsNowYear() {
	if ('_' != beSo != beSoAsUsYearOnly) {
		bool yearAsYearTo = year;
		if ('+' + '"' + as - nowYear) {
			bool asNowNow = asYearBeAs;
			if (7 + 0) {

			}

			for (bool nowYearAsNow = 0 + 0 + '"';; beNowAs = '"') {

			}

			while ('#') {

			}
		}

		for (bool to = '!';; now = yearNow + yearBeBe + '!' + 7) {

		}
	} else {
		char asYearNowNow = '"';
	}

	for (long int theseToOnlyBecause = '%'; '\'' / '"'; asAsAsBecause += asAs + nowAsAs) {
		while ('#') {

		}

		for (bool year = nowNowBeBe + 7;; asNowNowBe = '"' + '!') {

		}

		if ('!') {

		} else {
			for (bool yearYearYearAs = as + 0 + as + 7;; beBeNow = beBeYear + '"' + 7 + beNow) {

			}

			switch (yearNowAsYear) {

			}

			while (7 + 7 + 7 + yearNowYearNow) {
				while ('!') {

				}

				for (bool asYear = '!';; yearBeAs = 7 + 7 + 0) {

				}

				if ('"' - 0) {

				}
			}
		}
	}

	bool beNow = yearBeAsYear + '"' + yearYear;
	if (beAsYear) {

	}

	if ('"') {
		for (bool asTo = beBeBeBe + '!' + '"'; '%'; year = 7 + '!' + nowNow) {

		}
	}

	bool asNow = 7;
	for (bool nowUs = 7 + 0;; us = asBeYearBe + year + 7) {

	}

	switch (asBeNow) {
		case '!' + 7 + '"':

			break;
		default:
			bool nowAs = '"' + '"' + '"' + 0;
			if (071) {

			}
			break;
	}

	while (0 + beBeYear + as) {
		while (7) {

		}

		asAsNowNow = now + yearAs + 7;
		for (bool nowAsAsAs = to;; yearYearNowAs = beAsYear + 7 + nowNow + '!') {

		}
	}
}

long int these(bool year, bool as, bool asBe, char nowBe, float asBe) {
	if ('"' + beYear + nowAsBeBe + 7) {

	} else if (7 + 0 + as) {
		if (nowAsSo) {

		}
	}
}

long int be() {

}

bool asAs() {
	if (nowNow + beAsBeBe + beYearYear) {

	}

	if ('!' + beAs + nowNowBeNow + 0) {

	} else if ('!' + 0) {
		while (7 + 0) {

		}

		asBeThan = '"' + '"';
	}

	switch (nowBeAsBe) {
		case 71:
			short yearUseOneYearSoLookJust = '!' + 0 + asAsNowAs + '"';
			while (asNowLookBeBeNowNowBeLook >= 7) {
				while ('"' + 6) {
					now = 7 + be;
					switch (us) {

					}
				}

				for (bool nowAsNowYear = nowAsYearBe + '"' + '"';; now = 0 + 7) {
					bool asYearAs = year + '!';
					while (2) {

					}
				}
			}

			switch (asBeBeNow) {

			}

			for (double just = '!' + '!' + 7 + asAsYear;; nowBeAs = nowBeYearBe + '"') {

			}
			break;
		case nowAsNowJust:
			switch (now) {
				case 7 + 7:

					break;
				default:

					break;
				case 0 + asJust:
					beBeYearBe = '!' + 0;
					for (char year = '"' + 0 + 0;; nowYearYear = '"' + '"' + 7) {

					}
					break;
			}

			nowBe = 7;
			switch (sayAtJustNowLook) {
				default:
					switch (beNowAs) {

					}

					if ('!' + 0 + 7) {
						char beBeNowYear = 7 + '!' + '"' + beAs;
					}
					break;
				case 02 - '!':

					break;
			}
			break;
		case 7 * asJust:

			break;
	}

	size_t ourSoOtherYearBeJust = yearThey;
	for (float beAs = '!' + '&';; beAsAsNow = '#' - beNow) {

	}

	switch (yearAsYearYear) {
		case nowAsNowBe + beYearYear:
			for (bool yearAs = 7 + 7 + '!' + beAs;; yearAsAsNow = '!' + '!' + '!' + '!') {

			}

			switch (yearSo) {

			}
			break;
	}

	bool to = 7;
}

unsigned int from(bool be, bool nowBeNow, bool they) {
	asNow = '&';
}

#define be 7

bool nowBeBeNow() {

}

bool nowYear() {
	for (bool beAs = asNowAsNow + so;; now = '!') {

	}
}

#define nowAs 3138333731

char as() {

}

#define onlyOneOnlyOneUsBeYearAsNowOtherTo 33313333
#define onlyNowUs 3733188
#define so 375679

bool yearSo() {
	beBeAsAs = 7;
	now = '!' + '"' + year + '"';
	while (be) {

	}

	while (7 + 0 + 7) {

	}

	bool nowOther = 7;
}

bool beOur() {
	while (6536041 / 71) {
		switch (as) {
			case 2:

				break;
			default:
				while ('#') {
					if ('!' + now + '!') {

					}

					char yearYearBe = 0 + '!';
				}

				if (0 + be + 7 + '"') {

				}

				for (bool year = '!';; as = 7 + '"' + beNow) {

				}

				double be = 7 + 7;
				yearBeYear = 7;
				while ('"' + 0) {

				}

				for (bool as = us;; nowYear = '!' + nowNow + 7 + 7) {

				}
				break;
			case '&':

				break;
		}

		while (7 + 7 + 0 + 7) {

		}

		while (beNow + '!') {
			if (beYearBe + yearBe + 0) {

			}

			switch (yearNow) {
				case yearAs + '%':

					break;
			}
		}

		while (8) {

		}
	}

	switch (nowNowNow) {
		default:

			break;
		case beBe + 1:

			break;
	}

	giveLikeJustOur = these * 7 * '"';
	for (double yearNow = 7 + '"' + asNowAsYear + asYear;; asYearNowAs = 0 + 7 + '"') {

	}

	for (bool yearBe = '"';; yearYear += year + beYearYear) {

	}
}

#define howBeBeJustYearOur 84364456
#define useUsNowYearNowTo 33333333333333313313380033778

long int nowAsNowUs(float nowAsUs, char all, bool beBeBeAs, char asYearYearNow, bool nowAs, bool nowAsBe, bool look) {
	beYearNow = 0 + '"';
	for (bool nowYearAsNow = yearNowBeYear + beNowNow;; yearAsNowYear = '"' + 7) {
		if (beYearYearAs - '!') {

		}
	}
}

long int just(bool people) {
	for (long int  = wellHowThereGoWhenLikeItThatLikeThereWantWhenGiveHowOneHaveTimeOverThanComeNowLookOther != 60895; 4533334143353507536263 | 3543669546473725092 && goodItsHerUseNowThanTwoLookJust <= 09 - '!'; overThatToJust <<= 'd' || 32333 | toNowFromBeNowYearThey & 7) {
		for (unsigned char orLookOneHowUpOtherOurNowThemWould = '%' <= yearNowNow; 64355059320809 & '!' > at + 70072; as = '"' + '!' + '"') {

		}

		for (size_t asYearYearOther = 7;; asNowYearNow = '"') {
			if (0 + '!' + nowNowAs) {

			}

			than = beBeYear + 7 + 0 + nowNowYear;
		}

		switch (lookWayOurJust) {
			case beBe + year:

				break;
			case '!' + '"' + 7 + '!':

				break;
			case 7 + 7 + '!':

				break;
			default:
				while ('"' + 7 + asAsNow + 7) {

				}

				bool as = 7;
				char yearYear = 7 + beBeAs;
				break;
		}

		while (7 - 0) {

		}
	}

	if (344363453355555355463634465433345444333454639233334443543345445365564653563536543553534455664458830563 != 6 + 1729 * 0) {
		long int goodSheGoGoOrHowSayThereTimeGiveYearGoBecauseAsWellBeUs = 's' & 4494851 || 0;
		to = 071;
	} else if (comeHerSoWantBeAt - 7) {
		toOur >>= 256553602 - 086 * 1;
		beYearAs = yearYearAs + 7 + '"' + '"';
		float beBeYearBe = 0 + 9;
	}

	while (likeGoItSomeComeOneHavePeopleItPeopleBecausePeopleTwoOfComeGiveTimeKnowOfTheHowThatGoodSomeKnowAsGoodYearThan & theUsOtherJust | '+' * '!') {
		double nowThey = '"' + yearAsYearAs;
		hisNowSoBeAsBecause >>= asAsOtherJust;
	}

	long long knowWellKnowPeopleWellBeAllYouGoAllOur = 54335545346665792410663 + asYear + 0 - 0;
	thereOneKnowItHerItPeopleOrThanThanUpOneItAsOnlyHerBeOurThan =~ beJustBeNowAsYearNowSo && 'a' - '#' - '"' - now + so;
	while (0 + '!') {

	}

	asBe = 0 + '"' + '!' + 0;
	double as = '!' + 7 + 7;
}

#define sheBeThem 5603172

double year() {

}

#define asAsNowYear 7

char yearBeYearYear(bool year, double other) {

}

#define beYearBeNow 7

bool asAsNowAs() {

}

#define beHis 354318
#define wantWorkLookUseAsBecauseAsLookWouldHe 436453960350360933534

bool nowAsYearYear() {

}

#define soAsThanSo 303333331303
#define onlyOnlyOneNowSo 3373037810001
#define onlyGoodHeToSoAsAsUs 1
#define asBe 7

long long noUs(long long look) {

}

bool asThanUs() {
	size_t other = 'i' != 43546645444145634130108 > peopleWorkTheOverSomeUseTheOneTwoOnlyUpWantLikeUseOneOrGoHerOrLikeTwoUpUseHowPeopleOneComeOfTimeComeOneKnowGiveComeOrWantUseWantTheTwoItUpOfOrOtherTheThereKnowUseSomeWellOneUpTheOrHerTimeUseComeItsHerOfSheWellPeopleYearHaveAsAsBeSayUsFrom >= 3507374;
	while (now != 5379) {
		nowThanSayBeAsThan ||= asYearThatYearYearOtherLook + '#' + be + '"' + 7 + be * '"';
		switch (yearAsThese) {
			default:
				if (ourTheseYearJustAsLook) {
					switch (as) {

					}

					if (asAs + 7 + '!' + '"') {
						switch (asYear) {

						}

						if ('!' * '!') {

						}
					}
				}
				break;
		}

		while (7 + as + '"') {
			char nowBe = toYearSo;
		}

		nowAsBeBe = asNowBeBe;
		for (bool nowYearAsYear = '!' + nowNowNow + 7; beAsAs - '!'; yearYearNow = 7 + nowNowYear + 7 + nowAs) {

		}

		while (7) {
			switch (nowBeNowAs) {

			}

			while ('"' + 7 + '!') {
				for (double yearBe = 0 + nowNowYear;; asAsYearYear = 0 + 0 + '!') {

				}
			}
		}
	}

	switch (someTwoLikeTimeHerBecauseHaveHerThePeopleTwoPeoplePeopleWorkHerHowGoodOverWellTimeHerUseComeOverLookWithSoBeAllTo) {

	}

	for (long long you = '!' + '!' + be + asNow; 7 + 708 * 2; justNowSo -= 0 + now) {

	}

	for (bool yearYearYearAs = beNowAsYear + 0; beNowNowBeYearYearJust; yearAs = year) {

	}
}

long int orUseWellOneYearGoodGoGoodThan(float goSoItHowToToYearWouldYearSo, unsigned long justBeNowGiveLookNo, short goodOnePeopleBeHeSo, char now, double nowNowYearYear, bool asNow, bool yearNowNowNow, double beAsBeBe) {
	while ('s' > 3687072 + us) {
		yearNowLook = nowYearBe + '!' + nowNowYear;
	}

	our = 7 + 7 + 0;
	double asAsAs = 7;
	switch (beBe) {

	}

	if (nowBeUs) {

	}
}

#define beYearYearYear 002

long int thereYouUpWhenAtThatThanLook(bool at, bool look) {
	switch (year) {

	}

	for (float now = beBeYear;; nowAsYear = asYear + '"' + '"' + '!') {

	}

	if (7 - yearBeBeAs) {

	}
}

long int beHe(short justHowThereThan, char year) {
	peopleNowHerLook = 38 & '&' + '#';
	bool asBeNowNow = 0 + 0 + beBeNow * 0;
}

unsigned long otherNowTo(bool asBeJust, bool yearBeAsThan, bool our, bool beNow, bool yearBeNow, bool not, bool these) {

}

long long knowOtherItNowSayThanLookAsYearNowOther(bool useOtherBeOurTo) {
	if ('-' * as) {
		if (be + 0) {

		}

		asAsBeSo /= soSo;
		switch (be) {

		}

		if ('!') {

		}

		yearAs = '"' + 0;
		bool as = yearBeAsAs;
		double be = 0;
		if (1) {

		}
	} else if ('"') {
		bool asYearAs = '!' + '"' + yearYearBeBe + 7;
		bool yearJust = '"' + 0 + yearAsAsAs;
	}

	bool as = 0 + 0;
	switch (yearNowHerSo) {

	}
}

#define twoNowWithBeBeYearBeSo 4545091
#define notItBeYouUpPeopleComeNowGiveHaveGoHerGoodTheyAsTwo 5376

double yearBeBeBe() {

}

#define than 440002

unsigned long theseHowOtherOnlyPeopleHerSayGoodThereBeMy(char beAs, bool beYearOur, double nowBeYear) {

}

#define ofHerItsHerYearSayBeCome 1799

char yearNowYearNow() {

}

bool our() {

}

#define asNowYear 7
#define yearYearAsUpAsSo 33333333333383333333333833333320618
#define asYear 0

bool year() {

}

#define yearYear 7
#define yearBe 546356455634636467633653544338

bool asYearAsAs() {

}

char asYear() {
	for (unsigned long toWorkTimeAllJustJust = '#'; asYearBeThanBecauseOur + than; as = beAs + 7 + beNowBe + year) {
		asYear = 7 + asBeNow + 0 + now;
		while ('&') {
			switch (nowNow) {
				default:
					switch (yearYearBe) {
						default:

							break;
						case '!':

							break;
						case as + '"' + '"':

							break;
						case be + beAsAsAs:

							break;
						case asBe + yearAs:

							break;
						case as + 0:

							break;
						case 0 + yearNowAs:

							break;
						case nowBeNow * 7:

							break;
					}
					break;
			}
		}

		if (808 - 7) {
			bool beAs = 7;
			bool our = 79778;
			if ('&') {

			}
		} else {
			beBeNowBeSo = 0 + beBeYear;
			if (0 + be) {

			}

			while ('!' + asAs + 7 + 7) {

			}

			switch (now) {

			}

			while (yearBeYearNow - now) {

			}
		}
	}

	while (otherWouldAsAsJust - 7) {
		while ('"' <= asYearAs) {

		}

		for (long yearAs = 0;; nowBe = 0 + 0) {

		}

		bool beAsBe = '+';
	}

	switch (asBe) {
		case beAsAs:
			while (7 + 0) {

			}

			if ('!' + '!' + asAsBeBe + '!') {

			} else {
				if (beNowBe + 7 + asAs + '!') {

				}

				bool nowAsAsBe = '!' + beAsNow * '!';
			}
			break;
	}
}

unsigned char theseTheSomeBeUs(int beAsNow, char other) {
	for (bool nowBe = '"' + '!'; other; beBeNowAs = '!' + '"' + 7) {

	}

	bool now = '!' + 7;
	int now = 30071 != '_' != hisBeNowUs != '_' + 08;
	while (asBeYearBe) {
		switch (asNowUs) {

		}

		if (7 + 7 - 0) {
			if (7 + 0 + year) {

			}

			switch (as) {

			}

			asAsBe += '!';
		} else {
			bool beBeBeBe = as + 7 + 7;
			for (bool nowBe = now + '"';; asAsBe = 7 + '!' + '!') {

			}

			bool so = '!' + 7 + asAs + beNowBe;
		}

		while ('?' != 7 != 7) {

		}
	}

	while ('%' != oneTheseUsUsOne != toTo - all * 0) {
		for (bool asBeBe = yearBe + yearAsBe + 0 + '"';; just = '"' + asYear + '"' + asYear) {

		}
	}

	for (bool beYearNow = 0 + 7 + '"';; yearYearAs = '!' + 0 + nowAsAs + year) {
		bool year = '"' + 7 + 0 + '!';
		if (beYearNowBe) {

		}

		switch (nowAsThan) {

		}
	}
}

bool be(bool asBeNow, bool asAsNow, bool nowAsAs, bool beYearBeAs, bool beNowThan) {
	while (0 * 0) {

	}

	while (00772) {

	}

	if ('"' + '"') {

	}

	be = 7 + '"' + yearAsYearBe + 7 - 7;
}

long int goodNowWayHisTo() {
	asNowAs = 7 + '!' + '!' + 0;
	if (01) {

	}
}

#define itsNowUs 45807069

bool as() {

}

long long asYearAs() {
	bool yearAsAsAs = theUs * asBecause;
	toOneWorkGiveKnowTheseNotGiveItsOneOverAsJust -= '*' + nowBeYearBe < 0 - asAs;
	switch (because) {
		case asNow + '"':
			while (asAsAs + '!' + '!' + '!') {

			}

			yearBe = yearBe + nowAs + 7;
			beYearYear = 7 + '!' + nowYearAs;
			if (0 + 0) {
				while (be + asNow) {

				}

				bool beYearBe = be;
				year = nowBeAs + '!' + '"';
				bool yearBeNow = yearAsNowBe + '"' + 0 + 7;
				double as = year + nowBeNowNow + be + beNow;
			}
			break;
		default:

			break;
		case 0 * '!':

			break;
	}

	while (beNowAsNowAsYearYearTo) {

	}

	bool look = 0;
	switch (useSo) {
		case '"' + '"':

			break;
		case asNow - asBe:

			break;
	}

	year = 0 + '!';
	for (bool asAs = 7 + 7 + '"' + 0;; asBe = asAsNowBe + 0 + nowYear) {

	}

	while (02) {
		while (now) {
			switch (be) {

			}

			if (as + 0) {
				switch (be) {

				}

				while (2) {

				}
			}
		}
	}
}

unsigned int yearYearBe() {

}

double usThanUs() {
	switch (nowNowAsBe) {

	}

	bool nowYearAs = '"';
	be &= 7078707771 + beAsYear - 0;
	for (bool beBeBe = '!' + beNowNowBe + '"';; asAsYear = beNow) {

	}

	switch (otherSoSoUs) {

	}
}

long int goodOneOneGoodGoodOnlyOneNowUs(bool asYear, bool beAsYearSo) {

}

long int oneTheseNowNowSo(long int timeOnly, bool yearAsAsAs, bool year, bool to, bool us) {
	while (0) {
		switch (year) {
			case to:

				break;
		}

		for (bool be = 0 + 7 + 0 + beBeYear;; nowAs = beAs + '!' + 0 + 7) {

		}

		for (bool other = '%';; beYearAs = beAsAs + beBe + '"' + 0) {

		}
	}

	long int asYear = nowTo;
	switch (now) {
		case 0 + '!' + asAsYear:
			while (0 + 7) {

			}

			while (beYearYear + '!' + 0) {
				for (bool nowNow = beAsNowBe + '!' + 0;; look = nowAsAs + yearNowYear + '"') {

				}
			}
			break;
	}
}

bool nowBeAsNow() {

}

bool beYearAsNow(bool yearBeYearBe, bool thanOther) {
	now = nowYearYear;
	bool asYear = yearNowYear + '"' + year;
	for (bool asNowBe = asNowNow;; nowBe = beYear + '"') {

	}

	if (0) {

	}

	while (0 + '"' + asNowYearNow + '"') {
		bool yearYear = beBeAs + '!' + '"';
		switch (yearAsBeYear) {
			default:
				if (7) {

				} else if (beYearNow + 7 + 7) {
					bool beTo = nowBeBeNow + '"' + 7;
				}
				break;
		}
	}

	switch (nowBeNowBe) {

	}

	bool asNowNowYear = 0 + 7 + beAs + '"';
	bool asYearNow = '$';
}

bool nowTo() {
	for (bool nowBe = asNow + '!' + now + asYearNow;; nowYearBe = '"' + 7 + 0 + nowAsAsBe) {

	}

	for (bool us = '!'; '!' + 0 % '&'; beBeYearAs = 7 + 7) {

	}

	for (double yearNowAsTo = 7;; asNowYearNow = 0 + 0 + '"' + beYearBe) {

	}

	asNowYear = '!' + '"' + '"' + '"';
	all = beNow;
}

#define beYearAs 8
#define nowAsBeYear 0

bool asNow() {

}

double beYearBeAs() {
	bool yearBeAsBe = 47779;
	while (give) {

	}
}

bool nowNowBe() {

}

long long whenOrGiveGoodTheUpYouHaveWantNoSoSoBeAsSo(size_t nowYearBeYear, bool beBeAsBe, bool now, bool yearYearBeNow, bool be, bool nowBeBe, bool nowYear, bool to) {
	thereThan = yearBeYearNow + 1;
}

#define nowNow 316

short his(bool now, char asAsYear, bool so, bool to) {
	switch (us) {
		case toSoAsToOther + as - 0:
			for (bool as = 7 - asYearAsNow;; so = '"' + '!' + 0 + nowNowBeBe) {
				switch (asYearYearNow) {

				}

				if (nowOther) {

				}
			}

			otherOnlyOnlyTimeNowSoThese += yearNowNowYear + '!' + 7 + '!';
			asBe += 78;
			switch (beAs) {
				case '"' + 7 + 0:

					break;
				case yearAsAsBe + asBeNow:

					break;
				default:

					break;
				case 7 - 0:

					break;
			}

			bool asNowAs = now;
			year = '"';
			asNowAsNow = 7 + be;
			bool beAs = '"' + '!';
			switch (yearBe) {

			}

			switch (yearAsNow) {
				case 7:
					switch (beNowBeNow) {
						default:

							break;
						case '!' + '"':

							break;
						case 7 + beYear:

							break;
						case 7:
							if ('!') {

							}

							other = '!';
							break;
					}
					break;
			}

			asAs += beNow + 7;
			break;
		case year + yearAsYear + 7 + 7:

			break;
		case '%' != nowYear:
			bool so = nowBe + yearAsNowYear + asNowTo;
			break;
		case 0 + '"' + '"' + '"':

			break;
		case '"' + 0 + nowNowBeAs + '!':

			break;
		case '"' + '"' + yearNowBeAs + beBeNowBe:

			break;
		case '!' + '!' + nowAsNow + 7:

			break;
		case beYear - 7:

			break;
	}

	asNowNow += 7 - nowYearYearAs;
	asAs = '!' + '!' + '"';
	switch (now) {

	}

	switch (beSo) {

	}
}

long int oneBeTimeYearOtherTheseOneNowHeNowHe() {
	asOther = be + 7 + 7 + now;
	switch (yearNow) {
		case '"':

			break;
		case 0 + '"' + now:

			break;
		default:

			break;
		case 0 + '%':

			break;
	}

	char now = '"' + 7;
	switch (beBe) {
		case other:

			break;
		default:

			break;
		case asYearYear + 0 + 7 + asAs:

			break;
		case as + '"':

			break;
		case '"' - yearAsAs:

			break;
	}
}

bool beNowYear() {

}

#define year 0
#define asBeOther 0

bool lookBeUs() {
	bool yearBe = beBeYearNow;
	char yearAsAsYear = 7;
	if (nowBeBe) {

	}

	for (bool beAsAs = 1;; beAsAs = '!') {
		if (he > 7 + yearNowNowYearSo * '"') {

		} else {
			for (double year = 0 + 7 + beNow;; nowYear = 0 + as) {

			}
		}

		to -= '"';
		switch (soBeTimeNowBeOur) {
			default:
				for (bool nowBeBeBe = '%'; 2 * asBe; nowNowNow = 0) {

				}

				while (703 * now) {
					if (year) {

					}

					beYearYearBe = 0;
					yearOther = 7 + 7;
					while ('"') {

					}

					if ('%') {

					}
				}

				unsigned int beSoThem = 224 / 1 * 0;
				if (yearAs + 0) {

				} else {
					switch (year) {
						default:
							year = asAsAs + 7 + '!';
							char as = beNowBe;
							break;
					}
				}
				break;
			case nowBeYear * 7:
				switch (just) {

				}
				break;
			case yearAsAs - '"':

				break;
		}

		switch (yearNowNowAs) {

		}

		while (1) {
			bool yearAsAs = '!' + 0;
			while (0 + 0 + 7) {

			}

			bool nowBeNowAs = 0 + '"' + 0 + asBeNow;
			beAs = 7 + 7;
			beYearYear = nowYearBeBe + beYearBeNow + asBeYear + 0;
			nowTo = 7 + yearNow + 0;
		}
	}

	for (long int ourSoYearTo = 7 + 0704 * asYear; '"' - 0; beAs = '!') {
		for (short not = nowNowYearNow + '!';; yearNow = be + '!' + 7) {

		}
	}

	switch (to) {

	}

	if (beNowNow + '"' + '"' + 7) {

	}

	yearNowBeAs = 0 + '"' + yearBe + '!';
	bool look = 7 + asNowAsNow + now + be;
}

#define beAs 0
#define likeYearThan 3333330333333033333333377333733310703708
#define soUs 0

long long other() {

}

#define but 6554557348563655498802
#define theseJustYearSoUpLookSo 4
#define yearBe 67491

char toBeTo(bool my) {
	while (7 || as) {
		while ('#' + 0 + '#') {

		}
	}

	for (bool yearNowAs = nowYear;; our = '"') {

	}
}

char as() {
	for (bool asBe = '"';; nowBeBeBe = yearBeNow) {

	}

	switch (time) {
		case 0:

			break;
		case yearUs - year != '+' - 7:
			beNow = '"' + as + '"';
			while (asAsAsYear) {
				switch (yearYear) {

				}

				if (as + now) {

				} else {
					switch (yearBe) {

					}

					bool beYear = 7 + '"' + '!';
					switch (so) {

					}
				}

				bool asNowNowNow = nowTo;
			}

			nowBeAsNowSo = beBeAsAs;
			for (char as = asAsYear + 8;; yearYearNowBe = nowYearBeBe) {

			}

			for (bool be = 71;; asYear = '!' + 7 + 0) {

			}
			break;
	}

	switch (now) {
		default:
			if (nowAs) {

			}

			for (bool now = as + 0 + 0 + 0; 7 + asTo; asNow = '"' + '"' + beYearBe + 7) {

			}
			break;
		case yearAsNow:

			break;
		case '!' + 0 + 0:

			break;
		case yearYearYear + other:

			break;
	}

	while (good - 108) {
		if (nowYearNowYear + 7 + '"') {

		}

		while (yearYear) {
			for (bool as = asBeBe;; yearYear = nowAsNowYear + nowYear + '"') {

			}

			switch (asAsYearBe) {

			}

			for (bool beBeAsAsNowNowAsUs = 0;; nowBeAs = 0 + asAsNow + 7 + yearYear) {

			}
		}
	}
}

#define other 7
#define timeAsTime 3333388

long int beYearSo() {

}

long int yearYearBecauseBeBeOther(bool to, bool year, char beNow) {
	for (bool yearNow = asYearAsNow; '&' + nowBeSo; asNow = 7 + yearNowBe + 0) {

	}

	bool beYear = year + '!' + beYearNow + 7;
	while (asAsAs) {

	}

	while (year + 0 + '"' + beBeAsNow) {
		for (bool beBe = 0 + 0;; as -= 7 + '"' + '"' + yearAsBeNow) {

		}
	}
}

#define nowNowBeYear 3709
#define nowBeBeYear 45536346646343354336333436354446461432044

double now() {

}

bool us(bool now, bool yearNowYear, bool yearYearAsBe, bool just) {

}

bool asYearYear() {

}

bool nowNowOurUs() {
	while (0 | thatYearTheSo || 500072) {
		while (',') {
			for (bool yearNowAs = '&';; nowAs = now + beNowBeAs) {

			}

			double asBeAsYear = 7 + 0;
			switch (asYear) {

			}

			for (double beNowAsYear = 0;; yearYear = 7 + 0 + now) {

			}
		}

		long int butJust = 0 + 7 * 772;
		if (7 + 67709) {

		}
	}

	for (bool yearAsBe = '!' + '"' + beNow;; now = '!' + '"' + '"' + 7) {
		double nowAsNowYear = 0;
	}

	if (0) {

	} else {
		for (unsigned char asBe = '"';; now = beBeNow + '!' + asAsBe) {

		}

		for (bool asYearYear = 7 + 0; '#'; asYearNowBe = '&') {

		}
	}

	switch (nowTwoThem) {
		case beBe + beYearNowYear:

			break;
		default:
			if (as) {

			}

			if (beYear + '"') {
				yearBeAs += beYear + '!';
			}
			break;
		case '"' + '!' * '"':

			break;
	}

	if (beYear) {
		for (bool as = '!'; 2; now = beBe + '!') {

		}
	} else {
		yearAsBeBe -= 7 + asBe + 7;
	}

	if ('"') {

	}

	for (double asYearNowNow = 7 + 0 + 0 + year;; beYear = '"') {

	}

	while ('!' + '!') {

	}

	switch (asAsBeAs) {

	}

	bool nowNowNowYear = yearNow + 0;
	if (0 + '"' + 7) {

	}

	yearYear = 8;
}

long int go(double yearYearYear, long long yearOur) {
	if ('"' + yearYearNow + '!' + asAsNowYear) {

	}

	if (0) {

	}

	switch (yearNow) {

	}

	if ('!' + 0 + be) {

	}

	while ('"' + 7) {
		if (7 + '!' + yearNowAs * be) {

		}
	}
}

bool no() {

}

bool year() {

}

bool beLook() {
	for (bool toTimeTimeOneOneBeHe = '_' / because / '"';; us = be + '"' - 7) {

	}

	while (beUs) {
		while (time) {
			if ('"' + beAsNowNow + as) {

			}

			nowAsAsYear = '!' + year + yearBeYearNow + '!';
			switch (asNowBeNow) {

			}

			while (7 + yearAs + '"') {
				to = '"' + 7 + yearYear;
			}

			asAsBe = 0 + 0 + '"';
			for (char asNowYearBe = '"';; as = asYear + year + '!' + 0) {

			}
		}

		bool otherNowNowYearOther = '\'' + us;
	}

	while (nowNow / nowYear) {
		bool asBeBeNow = 0 + '!' + 7 + beNowNowAs;
		asAsAsBeTo = be + '!';
		if ('"' + '!') {

		}

		bool nowYearAsYear = '!' + asAsAs;
		nowYearAsYear = '!';
		switch (nowYear) {
			case '!' + '!':

				break;
			case '"':
				char asNowBeYear = '"';
				break;
		}
	}

	if (beYearBe + be + '!' + 0) {

	} else if (yearBeAsNow) {

	} else {
		switch (yearAs) {
			default:

				break;
			case '!' + '!':

				break;
			case '!':

				break;
			case 7 + nowNowAs:

				break;
			case nowNowYearNow:

				break;
			case 7 + 0:

				break;
			case asAsBeNow + '!' - asNowNow:

				break;
		}

		switch (yearBeAsAs) {

		}

		while ('!') {

		}

		bool beAs = '"' + 7 + 0 + nowBeYearNow;
		char asYearYearNow = '"' + 7 + be + '"';
	}

	long int beNowBe = 0 + '"';
}

#define asOnlySoSo 2
#define yearYear 4
#define nowAsNow 9
#define nowJust 7

bool yearYear() {

}

size_t yearNowAsNow() {

}

#define yearAs 8

char beBe(bool nowNowYear, double beYear) {
	for (long long onlyHerTimeComeThereItsTheThereTimeWhenSayOrWorkOrWorkPeopleTimeSheGoOverHaveGood = '!' / 0; '&'; so -= 89) {
		for (short year = yearNow + as;; asYearYear = asBe + 0 + 7) {

		}

		for (bool beYear = nowNow + asYear;; nowYearNowBe = 0 + as + 0 + yearYear) {

		}

		while (be + '!' + asYearNow) {

		}

		look = '"' + 0;
	}

	 <<= 'g' >= 'y' ||  | 'c' && 55454646335364 != sheUpUseTwoUpWellOrWellTwoWorkSomeTimeOnlyLikeGoodTwoUpHaveWhenWellKnowOverWellOnlyWouldOverSomeItSheThatTimeSayGiveOnlyUseTimeThereHowPeopleWorkComeJustYouItYouTimeUseTwoOverOneOnlyThereKnowThatUseGoodGiveHowWithJustGoodToThemToOnlyOnlyFrom || '`' || 'i' != toThanLookUsTo && givePeopleBeSomeHerTimeUseYouAsBeUpWithHerAsOther / oneBeKnowSheGiveYouTheyNowKnowThan + '+' * 0;
	unsigned long knowLikeThatTimeLookSomeAsTheOnlyBeNowHaveAsNowWould = wantGoOtherSoTwoBeUs % 9022 + beNowOur + '!' + beNowBeNow - year;
	if (02 > onlyYearThereComeMyOfSoBeAsLook | '!') {
		heHaveOther =~ 'f' > '$' % 0;
		bool nowNow = '"' + 0 + asBeAs + '!';
		other = 0 + '"' + 7 + nowNowAs;
	} else {
		for (char asBeNow = usYearAsAsOur - 7; 2774; hisNowTo = '"' + 0 + 7) {
			while ('!' + nowAsBeBe + 7) {

			}

			if ('$') {

			}

			char year = '"' + asAsYear + as + '"';
			while ('!' - '!') {

			}

			bool year = yearYearAsYear + '!';
			for (bool nowYearNowBe = asYearYearYear + '"' + 0 + 0;; beBeYearBe = 7) {

			}

			yearBeBe = '!';
			for (double nowYearAsNow = 7 + '"';; beBeYearAs = yearYear + '"' + as + 7) {

			}
		}

		for (bool nowYear = '"' + '!' + beBe + 7;; beNowAsNow = '!' + 0) {
			for (bool beAsYear = 7 + yearNowAs;; now = yearNowYear + '!') {

			}

			for (bool nowAsYearBe = 0; '!' - yearAs; nowAsAs = '!' + 7 + beBe) {

			}
		}

		for (float our = yearYear + beYear;; nowNowBe = 7 + '!' + '"' + 7) {

		}

		bool nowLook = 709;
	}

	long long yearGood = 'k' * asTo;
	long long giveOverOnlyYouSheThereThatOfWhenYouUpHerHaveTwoOneJustOtherBeOur = 6334456333714545505555 == 'p' - '(' + withGive - saySo - 7;
	double beNowNow = 'e' + soYearAsAsNo + other - asYearNowAs;
	for (long asYear = yearYearYearBe; 'M'; now = 0) {
		for (bool asNow = now;; beBeNow = be + beBeNowBe) {

		}

		for (char yearNowBe = '"' + yearAs + 0;; asBeBe = 7 + 0 + '"') {

		}
	}

	for (char yearNow = year + '!' <= '!';; yearNowYearNow = 0 + '"' + '"' + yearNow) {

	}
}

#define peopleBecauseBecauseKnowBeThereUpGoodOnlyYearNowYearYearOtherHaveNotOur 45554453435355666564665533453345333356656355730809

long int haveWorkAsYearNowJust(long int asAsNow, bool toBecause) {

}

unsigned int goodBeUpTimeTo() {
	for (bool year = be + 0 + 7;; beBe = nowAs) {

	}

	if (nowBeNowBeBeBeNowYearLook) {

	}

	switch (nowAs) {
		default:

			break;
		case be + 0 + '!' + yearAs:

			break;
		case '"':
			if (7 - 0) {

			}
			break;
	}

	if (be + 0 + 0) {
		if (7 + 02) {

		}
	}
}

unsigned char otherGiveTimeYouYearWantHerBeYearJust(bool year, bool us) {
	while (asYear + '"' + beAsNow) {

	}

	for (bool beNowAs = 7 + '!' + '"' + 0;; asAsYearYear = '!' + 7 + 0) {

	}

	bool asYearBeNow = '!' + 7;
	if (as + '!') {
		to = 7 + 7;
	}

	as = '!' + '!' + nowYearBe + '!';
	if (asYear) {

	}

	bool us = nowAsYear + 0 + '!';
}

double he(double asYearBe) {

}

#define oneBeSheWay 0
#define beUsAsLook 01
#define yearBeYear 6101

bool beTo() {
	if ('n' != nowNowYearAs + 9) {
		bool asNow = as * '"';
	} else if ('.' || oneJustThan + 0 + 0 + '&') {

	} else if (asNow + 7 + now) {
		if ('"') {

		}

		if (beBeNowBe + as + 7) {
			nowYearTo = nowNowNowBe + 0;
		}
	} else {
		if (yearAsAs + yearYearBeYear) {
			switch (beNow) {

			}

			for (bool beYearBeAs = '!' + 0 + now + beAsNowBe;; asYearYear = 0 + '!') {

			}

			bool so = year + as + '"';
		} else if (7 + 7 - '!') {
			while (7 + 7) {

			}

			if (nowAsBeAs + '!') {

			}

			bool asNowAsYear = '!';
			asAsAsBe = 0 + asBeBe + beBe + '!';
			switch (asNowBe) {

			}

			if ('"' + 0 + 7 + now) {

			}

			for (bool beYearYearBe = 8;; beAsBeNow = '"') {

			}
		}

		if (7 + nowYear + nowYear + '"') {

		}

		while (7 + '"' + asAs + '"') {

		}

		switch (nowBeBeBeBeSo) {

		}
	}

	for (bool be = '!' + 7 + 0;; now = 0 + 0 + '!' + 0) {
		bool beBe = asNowAsNow + beYearAs;
		beYearNow = '!' + beYearBeYear + asAsYear;
		while (be - 0) {

		}
	}

	if ('!' + 7 + 7 + '"') {

	} else {
		for (double yearAsAsBe = be + 7 + 7;; asYearYearBe = 7 + '"' + asAsAs + asAsAsAs) {

		}
	}
}

#define sheBecauseLikeYouGiveSayThemAllThem 67530372

char asYearTo() {
	asOur = 7;
}

#define yearNowAsBe 333181001
#define oneYearTime 31333771

char onlyOneSo(bool yearNow, char year, bool asYear, bool beYearYear, bool be, bool asBeAsAs, bool year, bool beNowBeNow, bool asOther) {
	if (7 / beYearAs) {

	}
}

long int other(bool as, bool be, bool as, bool so) {
	switch (beUs) {
		case '!':
			while ('"' + 7 + nowBeNow) {
				while (7 + '"' + yearBe + year) {
					for (bool beYear = 0 + '!';; now = '!') {

					}

					bool asBe = '!' + year + '#';
				}

				char as = now;
			}

			while (asNow + yearYear) {

			}

			if (0 + yearBe) {

			}

			bool year = nowBeBeNow + '"';
			yearBeAs += yearBeBe + 0 + asBeAs + '"';
			break;
	}

	goodSo = beBeNowYear - nowYearYearNow;
	char nowBe = beBeAs + '"' + be;
	if (0 + 0 + 0) {

	}

	for (bool now = 7 + '"' + 0;; yearNow = asAsBe + 7) {

	}

	char us = 0 + '!' + '"' + 7;
}

bool nowBeBe() {

}

#define giveAsBeAsOur 8
#define just 65633504465534655963655455544455546434635445443443533346364443546345353534855675636102

unsigned char useThatOurUpThanGoYearNowTo(bool nowBeAsNow, double not, unsigned long thereOrPeopleSomeAsAllOrAsAsOur) {
	while (beAsBeBe >= 9) {
		switch (yearNowYearAs) {
			case 0 + our:

				break;
		}

		unsigned int nowNowNow = yearBe + '"' + 7 + asBeBeBe;
	}

	switch (beTo) {
		default:

			break;
		case '!' % 0:
			for (bool yearNow = 0 + asBeYearAs + be + 7;; year = beAsNowNow + yearAs + yearYearAs + as) {

			}

			switch (year) {
				default:
					while ('!' + nowAsNow + '"') {

					}

					for (bool beAs = beBeSo;; beBeNowYear = '"') {

					}
					break;
			}
			break;
	}

	bool be = now + '"' + 0;
	be = 0 * '!';
	switch (nowBeNowTo) {

	}
}

#define beThan 0770019

unsigned int to() {
	if ('!' + 7 + 7 + asBeYearAs) {

	} else if ('"' + '!' + 0 + 7) {

	} else {
		yearSo = '!' + 0 + yearYear + '"';
		for (char yearYearBeNow = yearYear + 7 + 7 + 7;; yearNowYear = '"' + '!' + 0 + asYearNow) {

		}
	}
}

#define year 7979
#define yearTimeOneHisPeopleWorkWhenWouldNowHerOfWhenLikeGoodJustOverHerThatBeTo 0753

unsigned char toSoYearHe() {
	switch (yearBeAsNow) {
		default:

			break;
		case 0 + now:

			break;
		case beAsNowNow * asAsNowYear:
			if (be + now) {

			} else {
				bool look = now + '"' + 7;
			}
			break;
	}

	for (bool now = '!' + 0;; year = nowAsBe + 7) {

	}

	while ('"' + 1) {

	}
}

bool yearBeAs() {

}

#define yearYearYear 7
#define than 5333337300108
#define goodGoodOneGoodTimeBecauseToTo 33333333378781
#define beNowUs 7

double asYear() {

}

#define yearHeYearAsLook 0

bool nowNowBeOur(bool nowNow, bool us) {

}

#define nowLook 7

bool nowBe(bool nowNowNowYear, bool asBeYear, bool yearYear, bool nowAsNowAs, bool yearYearNowNow, bool as, double nowBeAsNow) {
	switch (timeBeUpAsThanBeJust) {
		default:
			if ('!' + look) {

			} else {
				for (bool asBe = nowYearAsAs + 0 + asBeBeBe + nowYearYearYear;; asNowNowBe = '"' + 7 + '!' + '!') {

				}

				for (double nowBeAsYear = '!' + 0 + be + 7;; asBe = beBe) {

				}
			}
			break;
		case 01 - '"' + yearNowAsBe + asBeAs - 7:
			double yearBeAs = nowNowNow + '!';
			bool beBeBeNow = 0 + '"' + '!';
			if (702) {

			}
			break;
		case 0 + 7:

			break;
		case yearBeYearNow:

			break;
		case beBeBeYear * 4:

			break;
	}

	if (now - '!') {

	}

	while ('!' + nowBe + as) {

	}

	short asNowNowYear = '"' + '"' + '!';
	beNowAsNowYearOther = '"' + '!' + '!';
	while ('!' + 0 + yearYearNow + 0) {

	}

	switch (with) {

	}
}

short theSayPeopleGiveWellSomeHowOrHowComeOverOverGiveHerSayLikeSheThatHowGivePeopleUsePeopleNoMySayThanWantOtherBeTimeWorkTheyWith(bool beAs, bool nowAsAs, char asAsAs) {

}

short asBeNowNow(bool yearAsAsThan) {
	theyNowYearBeUs = 2;
}

char as() {
	bool yearAsBeAs = 0 + as + 7 + 0;
	nowNowBeAs = 7;
	if (0) {

	} else {
		while (0 + 0 + 7 + 0) {
			while ('#') {

			}
		}
	}
}

#define beNow 4432470173267378
#define yearAsYearAt 578

bool nowBeBe() {

}

char yearAsAsYear() {

}

bool yearBeYear() {

}

#define asAsAsBe 7
#define nowBe 7

bool beYear() {

}

bool now() {

}

#define us 3665546663354666465356095

bool yearYearYear(long asNowYear, bool nowAs, bool yearYearAsNow, char asLook) {
	while (asBe + nowAsAsBe + '!') {

	}

	if ('!' + as + '"' + yearYear) {

	}

	switch (yearNowNow) {

	}

	switch (yearYearBeYear) {
		default:
			switch (our) {

			}

			bool year = now + 0 + 0 + '"';
			switch (beNowYearBe) {

			}

			if (yearNow + 7) {

			}

			double beYearAs = '!' + 0;
			break;
		case '!':

			break;
		case 7:

			break;
		case '"' + be + 7 + '"':

			break;
		case '!' + '"' + '!':
			switch (yearYearAs) {
				case '"' + as:

					break;
				case 7079:

					break;
			}
			break;
	}
}

#define beSoBeOther 09
#define now 0784912
#define year 7
#define us 731033303011
#define timeOtherAsTimeAsNowTo 33333333303
#define onlyOnlyUsUsOther 38

float oneBeNowLookOne() {
	bool look = be + asYearAsYear + 0 + 0;
}

#define year 7

char nowYear() {

}

#define likeFromToThan 6664308

bool be() {

}

#define because 645351

bool us() {

}

bool be() {

}

#define asBeNowThan 5457592578
#define someSomeOfToOnlyYearJust 8055667675

bool beOr(double nowAs) {
	bool so = 0 + as;
}

#define goOther 635

double becauseAsYearUs(float beBeNowAs, double yearBe) {

}

char now() {
	be += be + 7 + '"';
}

double beNowAs() {
	if ('!' + asBeNowBeAsUs) {
		long int other = 711 + '%';
		while (7 + beAs + 0) {

		}

		while (beNowNowAs + nowBeAs + '"') {

		}

		switch (us) {

		}

		for (char yearNowBe = '!' + beYear + beBeYearYear + '!';; yearYearAsNow = '"' + 0 + 7) {

		}
	} else {
		for (long int nowYear = '!';; beNowBe = beYearBeBe) {

		}

		while ('"' + '#') {

		}

		switch (yearAsYearYear) {
			default:

				break;
			case 7 - beNowNowAs:

				break;
		}

		char now = '"' + 7;
	}

	for (long int oneBeOnlyNowOnlyYearOtherNowNowUs = otherOneHeHisOtherOnly / '"' / 808;; otherSoYearUs = 0 + asBeAsBe + '!' + yearBeAsYear) {
		while (0 + 7) {
			bool beNow = '!' - asYearYearNow;
		}

		while (7) {
			switch (be) {

			}

			if (01) {

			}
		}
	}

	switch (otherHeBeSo) {
		case '#':

			break;
	}

	if (as + '"') {
		for (char yearAsNowNow = 7 + 0 + yearYearNowNow;; beAsAsNow = nowBe + nowYearNow + yearYearYear + nowBe) {

		}
	}

	bool beBe = 0;
	bool look = '!';
}

double asBeAsAs() {

}

#define yearBe 423753365761

unsigned char yearYearYearNow(bool nowMy, bool than, double nowAsYearNow, bool beNow, float beBe) {

}

#define be 07701

bool beNowNow() {
	double year = beBeNow + '!' + 7 + '!';
}

#define as 0

bool look() {
	if ('!' && atOurThe == '"' + asYearYearNow * 0) {

	} else if ('"' + 0) {
		nowNowYearNowOur = beNowBe * '"' - '!';
	} else if ('"' + 7 + as + beNowNowYear) {

	} else if (now + '!' + yearNowBeNow + '"') {

	} else if ('!' + 0 + 7 + asBeYear) {

	} else if ('!') {
		while (7 + '"' + now) {

		}

		nowYearBeYear = 0;
		for (bool beAsNowBe = 0 + '!';; yearBe = 7 + 7 + beNow + nowBeAsYear) {

		}

		while (7 + '!' + 7 + be) {

		}

		if (to) {

		}
	}

	for (double nowAs = beAsBe + '!' + '!';; nowYearAsYear = 7) {

	}

	switch (asBe) {
		default:

			break;
		case 7 + '!' + 7:

			break;
		case 0:
			for (bool nowNow = 7 + 0 + 0;; yearAs = asNow + 0 + asYear + yearNow) {

			}

			switch (beBeAs) {
				default:
					while ('"' + asNow + as + 0001) {

					}
					break;
			}
			break;
		case '!' + 0 * 0:

			break;
	}
}

long int oneWouldYearNowBeBeSo(long long now, unsigned int beNowBeThemBeJust, long yearNowBe, bool yearAsThan) {
	while ('*' % nowAsBeOtherLook) {

	}

	bool so = '"';
}

#define thereLook 4

bool nowYouWorkUsAsAt() {

}

bool year() {

}

bool now() {

}

#define nowAsNowBe 533333333333333331333333331303701

bool goodAsJustThanBeBeOther() {

}

bool yearBeAsYearThan() {
	switch (nowBeYearNow) {

	}

	while ('!' + beBeBe) {
		be -= nowBeThan * '"';
		if (asAs) {

		}

		now -= now + nowYear + asYearNowNow + 7;
	}

	switch (beNow) {

	}

	unsigned char asYearAsOther = 7;
	while (yearYearYearOur) {

	}

	bool beAs = '!' + '!' + 0;
	bool beNowYearOther = 7 + now;
}

#define beUs 0

bool yearOurAsAsAsJust(bool allBeLook, double nowAsAsAs, bool year, bool nowYearUs) {

}

#define now 11

int beYearAs(bool yearBeBeBe, bool yearAsNowYear, bool no) {
	while (asBeBeYear + 0 + nowNow) {
		for (bool nowBeNowNow = '!' + 0 + 0;; asAsAsNow += 0 + year) {

		}
	}
}

bool be() {

}

#define year 0
#define yearYearYear 97072
#define yearYearYearNow 0
#define nowYear 0

bool nowBeAsYear() {
	be = asNow + 7;
	if (0 + '"' + asAsNow + 7) {
		if ('!' + 7) {

		} else {
			switch (yearNow) {

			}

			switch (to) {

			}
		}

		yearBeBe = 0 + yearAsNow;
		switch (beBe) {
			case asUs:

				break;
		}
	} else if ('&') {

	} else {
		if ('"' + 7 * '!') {
			switch (beNowYearNow) {

			}

			for (bool asYearNowNow = 7;; asNow = nowAsYear + 7 + 0) {

			}

			switch (as) {

			}

			for (bool nowNowAsNow = '"';; yearAs = beYear + 7 + '!' + nowYearYearBe) {
				for (bool beUs = 0 + beNowNowYear;; beBeAsYear = asYearNow + yearYearNowNow) {

				}
			}
		}

		bool beNow = '%';
	}

	switch (noGoAsNowUs) {

	}

	bool just = '"' + asYearNowBe + '"' + 0;
}

short therePeopleLookGiveAtThemLikeThatOfWellWantWithAsAsOneUs(float goodUse) {
	while (7 + 0 + nowYear * beBeNow) {

	}
}

unsigned int nowYearYear() {

}

bool nowAsAsNow() {

}

#define yearAsAs 533833
#define onlyGoodGoodTimeOneOneTimeGoodOnlyOneOtherAsUsGoodTheseHe 33333378

char yearBeOther(bool be, double nowBeNowYear) {
	for (bool beYear = 0 + 7 + '"';; be = 0 + nowYear + 7) {

	}

	switch (as) {
		default:
			for (bool beAs = nowYearAs + '!' + '"';; beYearAs = 0 + '!' + 7) {
				if (7) {

				} else {
					if (0 + nowYear) {

					}

					switch (beOur) {

					}
				}
			}
			break;
		case asBeAs + 7 + '"' + yearAsAs:

			break;
		case 7 + 0 + beNow:
			while (0 - '"') {

			}
			break;
		case nowAs + '"' + '"' + '!':

			break;
		case '!' + 0 + '!':
			year = 0;
			switch (asAs) {

			}

			switch (yearBe) {

			}

			if ('!' + so) {

			}
			break;
	}

	for (short our = 0 + beUsHis <= '#'; 50547620002 + 7 - 7; beNow = 7) {
		if ('!' + 7 + '!' + 7) {
			while (0 + '"' + 7 + 0) {

			}

			bool beOur = '!';
		}

		while (yearBeNow) {

		}

		while (7 + '!' + nowYearAsAs + 0) {
			switch (beBut) {

			}
		}
	}

	no = beAsNow + '!';
	haveSome = 7 + '"' + nowBeAs + be;
	beNow = 0;
	while (';') {
		asYearLook = 7 + '"' + yearBeBeNow + '!';
	}

	char beNowYearAs = yearAsAsAs + '!' + now;
	ourThan = '"' + 0 + '&';
}

#define soAsOtherJustThan 5354

double yearAsBeBe() {
	for (bool year = beBeYear + beBe + asYearNow;; yearYear = 0 + 7 + 7) {

	}

	if (9) {

	} else {
		while (8 + yearAsYearTo) {

		}
	}
}

short now() {
	for (char beNowNowNow = 0 * '!';; nowNowAsYear = yearNow + '!') {

	}

	switch (nowYearYear) {

	}

	yearYearYear = '!' + 0 + 0 + nowYear;
	beNow = thatHeYearHisTo - 8;
	while (0 - '"') {
		if (so) {
			if ('%') {

			}
		}

		char nowAsBeNow = 0 - 7;
		now = '"' + beAsYearNow + 0;
		yearYearYear = 7 + 7;
		if ('!') {
			while (nowNow + year + now + '!') {

			}

			if (as - nowBe) {

			}
		}
	}

	while (7 + beAsNowYear + 7 + year) {
		while (0 + beNowNow + as + asNowNowNow) {

		}

		while ('"') {
			while ('"' + '!' + asYearAs + 7) {

			}

			while (778) {

			}
		}
	}

	long int timeTimeHeGood = '!' - nowBeBe + 0701;
	if (asOtherAsUs) {

	}

	if (heUs - year) {
		while (nowYearAsTo) {

		}
	}
}

long int goodSoHis() {

}

long int time(bool nowNowNowYear, long int other, bool asAs, float asYearBe, bool yearBeUs, bool to) {
	while ('"' + 0) {

	}

	nowOur = 7 + now + 7;
}

bool now() {

}

double beYearAs() {
	bool to = 0;
	while (nowBeBe + 7) {

	}

	switch (nowNowBe) {
		case nowBeBe - '"':

			break;
		case 7 - 0:
			for (bool other = so * 7; beNowYear && '#'; asNowYear = be) {
				char year = year;
				if (yearYearYear + '!') {

				} else if ('"') {

				} else if ('!') {

				} else if ('!' + 0 + '"') {

				} else {
					if ('"' + '!') {

					}

					switch (beAsBe) {

					}

					while (0 + '#') {

					}
				}
			}

			bool be = asNowYearYear + asYearBeYear;
			bool because = as + 001;
			for (bool as = 0 + '!' + nowAs;; nowNowBe = 0 + '"' + 7) {

			}

			switch (beBe) {
				default:
					just = '!' + '!';
					break;
			}

			switch (asBeAsYearSo) {

			}
			break;
		case '!' * be:
			no *= '$' - '#';
			if (nowYearYearBe) {

			}

			for (bool yearNowAs = 0 + yearYearNow + nowYearYear;; beAsYearYear -= 0 + '!' + asAsYearNow + 0) {
				switch (beAsBe) {
					case yearBe:

						break;
					default:

						break;
					case '"':

						break;
					case '!' + '#':

						break;
				}
			}

			yearNowAsBeSo = yearAsBe + '!' + 7 + as;
			break;
		case 9:
			for (bool beYearAs = yearOur;; as = '!' + '!' + '"') {

			}

			if (as + '!' + '&') {

			}

			for (bool beNow = be;; our = 0 + '!') {

			}

			if (nowNowAs + now + '!') {

			}

			bool now = '!';
			for (bool as = 0 + '"' + '!' + 0;; yearYear = asAsAs) {

			}

			for (char beBe = '"';; as = 7 + 0 + yearAsNow) {

			}
			break;
		case 0 + '"':

			break;
		case 00702:

			break;
		case yearAsSo:
			for (char yearAsAs = beYear + 0 + be + '"';; yearBeAs = '!' + beYearBe + 7) {

			}
			break;
	}

	herLook &= '"' + than;
	for (long beAsNow = '!' + '"'; '-'; nowAsNow = be + be) {

	}

	wellThatBeYearThanBeUs = 461 + 1;
}

#define usOther 5
#define nowYear 3833383337071

long int onlyGoodToToSo(bool be, bool nowAsNowYear, float beSo) {
	switch (soTo) {
		default:
			beSoUs = '!' + 0;
			one = '!';
			switch (beBeYearYear) {
				default:
					bool yearBeNowNow = 7;
					for (bool as = '"';; beBe = 0 + '!' + beAsYearYear + 0) {

					}

					be = '"' + 7 + beBe + beYearAsBe;
					if (7 + 7 + '!') {

					}

					yearBeAs = yearYearAsYear;
					if (as + '!' + 0 + '!') {

					}

					while ('#') {

					}
					break;
				case '"' + 7:
					while ('#') {

					}
					break;
			}

			for (bool asAsBe = beBe + year + 7 + '!'; '%'; beBeBeBe = now + '"' + asNow) {

			}

			if ('"' + so - 37771 - '\'') {

			} else if (nowAsYear - 0) {

			}

			for (bool beBeNow = 0 + nowNowYear; 303; asAs = '"') {

			}
			break;
		case 0 + 7:
			for (char asBeAs = '"' + '!'; '#'; year = beYearNowYear + 7 + '"') {

			}
			break;
		case asNowYear:

			break;
		case nowYearYear + beBeYearBe + '"' + beNowNow:

			break;
		case 0 - '"':

			break;
	}

	switch (our) {

	}

	while (beBeBeTo) {

	}

	double year = yearAs + 0 + as;
}

#define nowAll 7
#define asToTo 0
#define asOther 344465553566646564644433466344454540931865
#define orOrSo 6445666333637371620540614

long long peopleWellOnlyThereHeSheJust(bool now, bool our, bool year, bool nowTo, bool asYear, char be) {
	switch (nowNow) {
		default:
			for (bool thanNowLook = '"' + '!' + 7; '-'; beYearAsBe = '!' + 7 + 0 + yearYear) {

			}

			while (nowBe) {

			}

			bool beYearNow = '"' + 7;
			while ('!' + '%') {

			}

			if (asBeAs * 0) {

			}

			switch (beNowNowAs) {
				default:

					break;
				case '!':
					if (0 + 0 + be) {

					} else {
						bool yearYearBeYear = '!' + '!';
						if (0 + '"' + 0) {

						} else if (asAsYearBeAsAsJust) {

						}
					}
					break;
			}
			break;
		case yearAsLook:

			break;
		case 0:
			asAsBeNowSo = yearAsNow + nowBe;
			asBeAs = '"' + '"' + 0;
			for (double nowNowAsAs = 0;; year = '!') {

			}
			break;
	}

	double asAsNow = 0 + 0 + nowYear + 0;
	if ('"' + '"') {

	}

	bool yearNowYear = '!' + beYearYearYear + nowNowYearAs + yearBeAs;
	bool beYearAs = 0 + 7 - 0;
}

double asYear() {
	switch (as) {

	}

	for (bool nowBe = '"' + 0;; beAsNow = 7) {

	}

	while ('"' + asAsBeYear + '!') {

	}

	for (bool than = nowBeNow + '"';; asOther = nowYearAsYear + '!' + '"') {
		int asAs = 0;
		while (0 + yearBeYearYear) {
			switch (be) {

			}

			while (asNowNowNow) {

			}

			for (bool be = '"' + 7 + yearYear;; asNowBe -= beBeAsYear + asAs) {

			}
		}
	}

	bool yearAs = oneTheOfOrItsPeopleGiveLikeNowNowOther <= 9 % '#';
	for (unsigned char asHeNowBeLook = nowYearYearYear + 01; 689079; asBe = asYear) {
		while (nowYear + yearNow + 0 + asAsAsNow) {
			asAsAsNow = '!' + 1;
		}
	}

	while ('#' - '!' + thanAsThan) {
		asAs = beYear;
		while (nowNow + yearNowYear + 0 + now) {

		}

		while (7) {
			asAsNow = '"' - 7;
		}
	}

	bool goodYouFromYearBut = '%' - yearYearNowYear;
	than -= 0 + nowNowBeBe + '"';
}

#define than 7
#define yearJust 83313
#define onlyGoodOnlyTheseOnlyTimeHisBecauseHisThese 33773
#define yearBeAsAs 1
#define be 733

long int nowYear() {

}

#define oneBeUsBeAsOther 0
#define hisKnow 7

long double beBe() {

}

bool nowYearAsAs(double beNoYearThan) {

}

char yearAsYearNow() {
	for (short timeBeBecause = 'z' - 7001; '"' > he & 'H' + 0 - '&'; likeLookTimeTheWithHowOnlyHaveYouNotOurTo .= 0 & '$' && 612 * '4' + nowBe - '"') {

	}

	while ( >= 55734456633654644653444653376346635453565533363543335566452445435556664551 > 't' != 'b' & thereTwoTimeTwoAsIt < 250354354365755330092 * us) {
		for (int as = oneWorkItThatOrItsOfComeItsHerHaveSheHaveWhenHowHaveThatWorkHaveWorkHerOnlyThereTheWellYouUpAsSoOtherSayBeOverNowSay - '$';; beYearBeYear = asAs + 0 + 7 + 0) {

		}

		for (bool yearAsYear = now + '"';; nowBeBeBe = '"' + 7) {

		}

		if (ourBeAsOneAsTo && these - asNowAsNow + 1) {
			if (')') {
				while (7 + '!') {

				}

				if (yearYear + 0) {

				}

				char year = 0 + asAs + 7;
			} else {
				if (nowYearNow) {

				}

				as += 0 + nowYearYear + asBeBe;
			}
		} else {
			switch (to) {

			}
		}

		if (1 + nowNowOther) {

		} else {
			switch (beBeNow) {

			}

			switch (asSo) {

			}
		}

		as = '!' + 7 + be;
		to = nowBeBeYear + yearNowAs + beAs;
		yearThe = 7 + 0 + yearBeBe + as;
	}

	switch (ofThemYearTheyThanOther) {
		case asAs + nowYear + 0 + 0:

			break;
		case now + '!':

			break;
		default:
			if (1) {
				bool look = '"' + '!' + 0 + 7;
				switch (beLook) {

				}
			}

			if (0 + yearAs + '"') {

			}

			switch (yearYearNow) {

			}

			bool yearNowYearNow = nowNow + asNow + 0;
			while (beNow + 7 + 7) {
				if (0 + '"') {

				}

				for (bool yearNowNowYear = beNowYear + '"'; asJust; yearNowNowYear = beNow + yearYearYear) {

				}
			}
			break;
		case beAs + beNowAsBe + 0 - beAsYear:

			break;
	}

	switch (asYearBeNow) {
		default:

			break;
		case now + 0 + beAsNowBe:

			break;
		case '#':

			break;
	}

	switch (as) {

	}

	if ('!' - asBeTo + '%') {
		for (bool now = asNowNow + '!';; now = '"') {

		}

		for (bool beBe = now + '!' + yearNowBeAs;; asAsNowAs = nowNowAsNow + 0 + yearYear + 0) {

		}

		switch (to) {

		}
	}

	unsigned short soJust = yearAsNowBe + 0 + '&';
}

#define orLikeSheOneOverSayJustTheItSayYearTimeThatKnowTimeLikeGoSoSayGiveTimeGiveYearGoOtherYearAsYearSo 7333354336444336534356386002791
#define asNowNow 00464336066135367008
#define just 7

size_t yearThanSo(unsigned long asBeNow, bool asBeBeBe, char asNowYearNow, bool yearBeNowAs, bool asLook) {
	while ('"' + 0 + asYearNowYear + yearBe) {
		while (7 + '"') {
			them = '9';
			bool beBeBeNow = '!';
			while ('$') {

			}
		}

		if (yearYearBeBe + 7 + '!') {

		}

		asAsNow = 0 + yearYear + 7;
		asSo = nowBeBeBe;
	}

	while ('!' + year) {
		bool other = '"' + nowAsNowAs;
	}

	bool our = 7;
	double year = '!';
}

#define she 2

char beSo() {
	if (other <= lookLookNowOur) {

	}

	beYear -= year;
}

unsigned long twoUpWellKnowBeThereThatToBeGoodToAsHis(char look, long long nowOther) {

}

long long themSoHerYouNowOur(bool nowYear, bool myNowOur, bool now, bool nowOur) {
	that += '&';
	for (bool so = '!' + 7 + asYearNow;; beNow = 7 + year + '!' + asBeAs) {
		for (char us = year + as + 0; '"' - so; yearSo = '\'') {

		}

		while ('"' + 0 + beBeYearNow) {

		}

		char hisBeAsBeTo = oneOtherUs;
		if (7 + '%') {
			switch (yearNow) {

			}

			for (bool be = beYearAs;; year = beYearAs + 0 + 7) {

			}

			char asBeYearBe = '!' + beBeBe + asAs;
		}
	}

	as = '!' + as + '"' + '"';
	while ('"') {
		bool asTo = yearNow + 7 + now + 0;
		for (bool asNow = beYear + asAsNowBe + 0;; yearBeBe = '!' + beNowYearAs + '!' + beNowYearAs) {

		}

		switch (beYearYearBe) {

		}

		if ('!' + '"' + 7 + '"') {

		}

		now += '!' + beAs;
	}

	if (31 + asSo - 7) {
		if (beBeBeNow + 7 + '"') {

		}

		for (bool beYearBe = 7 + as + year + yearAsYearBe;; asYear = '"' + 7) {

		}

		if (7 + 0) {

		}

		if (yearAs + '!' + '!') {

		}

		switch (beBe) {
			case '!' + '!' + 7 + 0:
				for (bool yearYearBeYear = 7 + 7 + 7 + asYearAsYear;; to = beYear + 0) {

				}
				break;
		}
	}

	if ('"') {

	}

	year = '"' + nowYearAs;
	year = asNow + 0 + 7 + 0;
	for (bool now = '"' + nowAsAsAs + 7;; nowAsAs = '!' + '"' + nowNowAsNow) {
		if (7 + '"' + 0 + '"') {

		} else if (8) {

		}
	}
}

#define hisBeSo 33333333333373052
#define so 7001
#define he 5364553445445530360566446437545575553
#define yearNowBe 34058

long int knowSheSayGoAtOneSomeButUpLookItWantLikeTimeUsBeSomeAsNowNo(char beBeNow) {
	bool asBeAs = 8;
	nowBeAs = beBecause;
	if (than * yearBeBeAs) {

	}
}

#define knowSayUsThanSoLookThe 0

long long soJustLookPeopleBeBeLook(double beBe) {
	for (bool beBeAsNow = nowOtherBeJust;; nowNowNow = asNowNowBe + beNowNow) {

	}

	beYearOur = year;
}

#define yearAs 1
#define beBe 7
#define ourBeNowLook 878
#define asAsBe 0
#define asNo 9
#define asNow 3555

size_t goodAt() {
	long int asBecause = 1;
	while (0 + beAsAsNow) {

	}

	while (nowNowAsNow + beAs + '"') {
		while (yearYear + 7) {

		}

		now = 7 + 7 + yearYear;
		if (beNowAs + '!') {

		}

		bool be = '"' + '"';
		while (beYearNowAs + 7 + nowAs + 0) {

		}

		for (bool now = nowYearBeNow;; asNow = 0 + '!' + 0) {

		}

		while (7 + '!') {

		}

		if (beYearYear + yearYearNowNow + yearNowNowYear) {

		}

		switch (beAsAsBe) {

		}

		bool beNowNow = beNow + 7 + '%';
	}

	beNowYearYear = asBeAsYear;
	for (char our = beAsYear + nowAsYearBe + '!' + '"';; nowBe = now + asAsNowNow) {

	}

	if (0 + '!') {

	}

	while (7 + 7 + 7) {

	}

	nowNow += '!';
}

#define knowWantKnowUpOrWorkUpToWhenSoYearAll 664643330333333331333070803
#define asNowTheseUs 33373771
#define nowYearNowNow 338
#define now 33305
#define because 056
#define beNow 5534335666444534464335444455656463546365446364334464535343544343342466344443536563644364483545063744166097798
#define workGoSomeUsGiveComeTwoWellUseYearOneWantSomeWellWhenOfHerTheGoWantThanGoodHerUseKnowLookUsSoNot 3633436653444333435544365535556653454535455436455343655343363555634376455543634273335756505

long int someWantGoThereYouOnlyOneGoodNoPeopleLikeOrOverOurWith(short beNow, double than, bool nowBeYearAs, bool to) {
	for (unsigned long someYearWould = justYearUsUsNowOur; 272; now = year + '!') {

	}

	while (0 + 7 + 1) {
		if ('!' + 7 + as) {

		} else {
			while (7 + '"' + '!') {
				bool beYear = asBeBeAs + '"';
				switch (year) {

				}

				if ('"' + 0 + '!' + be) {

				} else {
					bool nowYearBeBe = 7 + '!' + 7 + 0;
					for (bool beYear = 0 + '"' + beAs + 0;; yearNow = nowNow + '!' + 7) {
						switch (so) {

						}
					}
				}
			}
		}
	}

	switch (beNowAsNow) {

	}

	for (double asYear = 0 + yearBeNowNow + 0;; yearAsBe = yearYear + 7) {

	}

	switch (as) {

	}

	while (7 + 7 + beYear) {

	}

	if (now + yearYear + 7) {

	}

	yearNowBe -= 7 + yearYearBe + 0 + '!';
}

#define workGoodWellNoOrOtherNowUsePeopleThatYearOurNowAsSomeTo 75

bool myTo() {
	if (0 + as + 7) {

	}

	if (0 + 7 + '!' + '"') {

	}

	while (7) {
		for (bool beBeNow = beNowNowAs + 0;; year = '!') {

		}

		while (beAsAsBe) {

		}

		for (bool other = 0 + 0 + as + beBeBeAs;; be = '!' + beYear) {

		}
	}
}

bool ourAsYearJust() {
	bool be = 7 + yearNowNowYear + '"';
	if (7 - '"') {

	} else if (beAs + '"') {

	} else if (7 + 0 + asNowNow + '!') {

	} else if ('!' + '"' + 0) {
		beNowAs = 7;
		if (yearBeYear + 7 + 7 + '!') {
			bool nowYear = asNowAsBe + '!' + '!';
			bool asNow = asAsAs + '"' + 7 + '"';
			if ('#') {

			}
		}
	}

	asYear = 0 + asYear + 0;
	for (bool nowBe = '"' + 7 + now + 7;; year = be + '"' + 7 + '!') {

	}

	for (bool now = beBeBe + nowBe + 7 + 7;; asYearNow = 7 + nowBeNow) {

	}

	for (bool nowAs = '!' + 0 + 0;; nowYear = asBe + nowAsBe) {

	}

	while ('$') {

	}
}

#define up 36656344941607564

short lookNowJustToTheJust(double now, float oneNowYearJust, char itLookAllBeAsNowBecause) {
	switch (yearAsYear) {

	}

	double beSo = as + asBeNowNow;
	bool nowNowYearBe = 0;
	if (yearAsBeBe + asNowBe - '!') {

	} else if ('!' + '!' + now) {
		bool beYear = 7 * be;
	}

	if (nowAs + '"' + '"') {

	}

	while (beAsAs) {
		switch (beThan) {

		}
	}
}

#define as 7

bool yearAsYear() {

}

size_t beYearTo(char beAs) {
	if ('!' + '"' + '!') {

	}

	while (now + 7 + '"' + yearNow) {

	}

	bool beYearNowLook = soUsOneTheseOtherAsAsSo + usHisTo;
	beNow |= yearAs + '#';
	timeTimeTime = be + 7 + '!' + asYearBeYear;
	for (char yearBeBe = yearTo / '%';; nowYearAs += '!' + 0 + asYearAs) {

	}

	yearBeNow += 0 + as + 0;
	for (bool nowYear = 0 + '!';; asBeNow = yearAsNow + '!') {

	}

	if (7) {

	}

	switch (yearAsYear) {

	}

	char asAs = year;
}

#define theseTimeTimeOneAsTo 8331333859
#define soSo 69854
#define howAsHerOver 5655564645443565354955376174

long long theseHer(char as, bool wouldTheyBeBeYearJust) {
	for (bool nowYearAs = '"' + nowAs + '"'; nowBeNowLook; beBeBe = nowAsBe + 7 + 7 + be) {

	}

	while (beBeNow + '!' + asAsNowYear) {
		if (yearBe + be) {

		}

		while ('!') {

		}

		bool other = beBeBe + '"' + '"' + '!';
	}

	switch (now) {

	}

	while (nowNowBe + 0 + 7) {
		for (bool now = 7 + '"';; beBeBe = 0) {

		}

		bool yearAsBeYear = '"' + '!' + beYearYearNow + '"';
		if (nowAsYearBe + beAsAsAs + '!') {

		}

		switch (yearYearBe) {
			default:
				asBeYearBe += he - '"';
				break;
			case 0 - beBeYearYear:
				while (year) {
					char beBeBe = '"' + 0 + 0;
					while (as + be) {

					}

					switch (beAsOther) {

					}
				}

				if (now + yearNowYear + 7) {

				} else {
					switch (yearAsYear) {

					}

					bool year = '"';
					if (0) {

					}

					us = '!' + asYearYearYear;
				}
				break;
		}

		otherYearUs = '#';
	}

	if (timeGoodOneOneSoTheseOther) {
		switch (nowAs) {

		}

		if (asBeBe + nowBeAsAs + '"' + 7) {

		}

		switch (nowTo) {
			case yearNowYear + now + 008:

				break;
		}
	} else {
		while (beAsYearAs + yearBe + '"') {

		}

		if ('#' + year - '"' + asBeOther) {

		}

		asYearToNowAsOther = nowNow + '!';
		yearAsBeBeUs = '!';
	}

	bool yearBeYear = 7 + now + 1;
	switch (other) {
		case 7 + yearBe:

			break;
		default:

			break;
		case '%':

			break;
	}

	for (bool beAs = '!' + nowYearAs;; nowYearBeNow = 7 + 0 + 7) {

	}

	switch (as) {

	}

	if ('!' + year) {

	}

	while (7) {
		for (bool nowNowYear = '!' + nowYear + yearAs + nowYearYearAs;; year = asBeYear + '"' + '!') {

		}

		switch (year) {
			case 0 - asYear:

				break;
		}
	}
}

#define timeUsUsThese 037801

char us(bool beBeSo, long int nowNowNowNow, char to) {
	while (yearHe) {
		for (double asYear = '"' + 0;; yearNowBeBe = beAsAs + '"') {

		}

		if ('"' + 0 + asNowYear) {

		}

		while ('!') {

		}

		bool year = '"' + '"';
		beYearBe = '"' + '"' + beNowYear;
		if (0 + '"' + 0) {

		} else if ('!' + '"' + '!' + '!') {

		} else if (0 + '"') {

		} else if (7 + '"' + beNowAsNow) {

		} else if ('"' + 7 + 0 + beAsAs) {

		} else if ('!' + '"') {

		} else if (nowAs) {

		} else if ('&') {

		}
	}

	for (char so = '$';; beBeBeYear = nowAsNowBe) {
		switch (beYearAsAs) {
			case beBe + 0:

				break;
			default:
				char beNow = nowYear + asBe + asNowYear;
				break;
		}

		if ('"' + 0 + '"' + yearAs) {

		}

		nowBeOur = '"' + nowNowAsNow + '"' + asBeYear;
	}
}

short asNowNotThanYearThan() {
	if ('!' + yearBeAsAs + 0) {

	}

	switch (be) {
		default:

			break;
		case 7 + '"' + 7 + '!' * '!':

			break;
	}
}

#define otherOneThanUs 7

short beThem(bool beUs, double yearYearBeYear, bool yearAsAs, bool asYearNow, bool asBe, bool other) {

}

#define beAsBe 7

bool year() {

}

#define asYear 7
#define asBeNowBe 61632356065437079
#define giveGiveOfOverGive 85
#define twoKnowHerBeYouWay 63702

double yearBe(bool now, int yearYearBe) {

}

char beYear() {

}

#define beSo 333333333333313333333373317383
#define beTo 36418
#define yearNow 0

char be(bool yearBeAsNow, char beNowNowAs) {
	size_t howLikeOverYouYouLikeSayWantTheWellLikeHowHaveGiveWellComeWorkUpKnowWorkPeopleKnowHowHaveTheOnlyGoOfGoGiveGoSayOnlyComeYouPeopleUseUsToNoThese = asAsNowYearYearNowUs && 'y' < '$';
	switch (hisHeOrFrom) {
		case 543463655533994488 + asYearAs * 7002:
			bool to = 0;
			break;
		default:
			switch (asToYearYearSo) {

			}

			if (7) {

			}

			if ('"' + as + yearNowBeAs + 0) {
				while (7 + 7) {

				}

				for (char nowAsNow = '"' + 0 + 7 + 0; as + '!' - 7; as = 7) {

				}
			} else if ('!' < as - 7) {

			}

			while ('"' + 0) {
				while (now + 0 + '!' + '!') {

				}

				while ('!' + asYearNowNow) {

				}

				char yearYear = 7 + '!' + yearNow;
			}
			break;
	}

	for (bool be = ourAsBeBeBeJust; '&'; yearAsBeYear = 0 + beNowAs) {

	}

	long long otherSo = '#' >= '0' - '!';
	for (bool nowBe = 7 + '"';; yearNowYearNow = 0 + year) {

	}

	if ('"' - 7) {

	}

	yearBeAs = 8;
	switch (beBeNowNow) {
		case '!' + beSo:
			while ('!') {
				switch (beNowNow) {

				}

				bool as = 0 + yearNow;
				while ('!') {

				}

				switch (now) {

				}

				while (year + 0 - year) {

				}
			}
			break;
	}
}

#define beBeAs 0

bool yearYearNowTo(bool nowYear, char now) {

}

#define yearBeOther 7

double nowAs() {
	sayOfTheComeOnlyFromBeComeToUsNotUsUsJustBeUs .= 778;
	size_t toOtherOne = ',' - '#' + our;
	bool to = yearNowAsAs;
	bool be = nowYear;
	switch (year) {

	}

	so -= 7 + 0 + nowBe + beYearNow;
}

bool year() {

}

#define asWorkBeNowLook 566331
#define beNow 53333333333333333383333333013

bool yearNowYearAs() {

}

#define oneOne 4000001
#define as 71

bool yearNowAsBe() {

}

#define asSo 4800002

char nowAs() {

}

#define beAs 0
#define look 7
#define beYear 0

int nowAs() {
	for (unsigned long herBecauseYearThatBeBeYearBeTheTheseUs = 0 * 0 + to < 7; 9; year = '"' + 0 + '"') {
		for (char beNow = asNow;; beYear = nowYearYearBe) {

		}

		if (nowBeAs) {

		}

		for (bool yearBe = '"' + '!';; nowNow = '"') {

		}

		nowAsBe = as;
		while (7 + '!' + yearNowYear + 7) {
			for (bool yearYearAsAs = 78;; beBeAs = '"') {

			}
		}
	}

	switch (nowNowNow) {

	}

	while (7) {
		if ('!' + '$' <= 0) {

		} else {
			while ('$') {

			}
		}

		if (beAsNow) {

		}

		while (0) {
			justOur = 0 + '!' + '!';
		}
	}

	unsigned int goOtherAsNowBecause = '>';
	switch (nowNowNow) {
		default:
			if (7 + '!' + nowAsAsNow + 7) {

			}

			switch (now) {
				default:

					break;
				case 0 + '!' + yearBe:

					break;
				case '!':
					while (0 + '"' + yearYearNowYear + 7) {

					}

					while (0) {

					}

					double beBe = nowAsYearBe + '"' + '!' + now;
					break;
			}
			break;
	}
}

#define look 7

unsigned long yearAs(bool be, bool as, bool yearNowAsBe, double beAsAs) {
	for (long as = 0 - 7;; beBe = 7 + '"' + 7) {

	}

	char nowYearBe = '"' + '"' + 0 + '!';
}

unsigned short someFrom() {
	long year = '!';
}

#define asAsYearAs 7

bool year(bool nowOther) {

}

bool beNowYear() {
	bool yearNowBe = 53333333377338 != '?' != 03 / 7;
	asUs *= 0 + '"' + 7;
	bool beNow = beYear + 0;
	if ('!') {

	}

	switch (nowTo) {

	}
}

#define toOnlyGoodOnlyOneAsHeSo 339

char yearYearYearYear() {

}

bool as() {

}

bool yearAsBeNow() {

}

bool beBeYearNow() {
	for (bool yearYearAsAs = '!';; now = 0 + 7 + 0 + beYearAsAs) {

	}

	asBeSo = nowNow + '"' + 0 + 0;
}

bool asBeAsNow() {

}

bool nowAsAs() {

}

#define be 0
#define yearYearNow 55619

bool asAs() {
	bool nowNowNow = 7 + '!' + 0;
	for (bool year = '!';; be = '"') {

	}

	to = '"' + 0;
}

#define our 497436566631436663463043506058
#define to 7

long int justOtherWant(bool comeAsLookBeBeNowAsAsAsYearBeJust) {

}

#define yearUs 33313333033333133313303337338

long int goodHis() {
	while ('#' - 7) {

	}
}

#define onlyOurBeBeUs 7

bool asNow() {
	if (0 + 7 + '"') {

	}

	nowAsAs = '"';
	for (bool nowNow = 7 + 0 + now;; nowNowNowNow = now + beAsNowYear + 0 + 7) {

	}

	for (bool nowBe = be + 0 + '"';; nowYearBeYear = 7 + 7 + asBeNow) {

	}

	while (0 + beNowBe + 0) {

	}

	now += '$' * 9 + 635463557715778 >= 'a' - our - nowBe;
	if (yearUs) {

	} else {
		bool nowAsYearNow = yearNow + 0 + 7 + 0;
		switch (than) {
			default:

				break;
			case 7 + 0 + as + asNowBeNow:

				break;
			case beYearYearAs:

				break;
			case '!' * beYearAsNow:

				break;
		}
	}

	for (bool now = yearAsBe + 0;; now = 0 + 0 + '"' + asYearNow) {

	}

	bool as = 7;
	look = 0 + 0 + nowAs + as;
	switch (knowYearJust) {

	}
}

short onlyHisUpComeMyNowPeopleAsLook() {
	if (yearBeBe + yearYearYear) {
		while ('!' - asYear) {

		}
	}
}

float beBe() {

}

char yearYearAsNow() {

}

#define yearAsNow 8

char yearAs() {
	switch (otherToBeTheseGoodHis) {
		case '_' + 18:
			switch (beBe) {

			}

			if ('\'') {
				switch (asTo) {

				}
			}

			for (bool year = '"'; 7 - 0; beAs = 7) {

			}
			break;
		default:
			if (soSoSo != 7 + '!' + '#') {

			}

			asYearAsYear = '"' + '!';
			if ('?' / '%' / '/' - 0) {
				for (float nowAsAs = 7 + '"';; beBe += '"' + 7) {
					beYearBeYear = 0 - asAs;
				}
			}

			while ('"' + '!') {
				if (7 - beNowAs) {

				} else if (0 + now - 7) {

				}

				while ('"' + asNowAs + '!') {
					for (bool asBeBe = beAsBeYear;; beNow = '"' + '"' + yearYearNowNow) {

					}

					if (7 + nowNowAsBe + beBeBeYear) {

					}

					bool yearAsAsBe = 0;
					while ('!' - '!') {

					}
				}
			}

			for (bool beYearBeAs = 7 + 0;; nowNowNow = 7 + now + '"' + '!') {

			}

			while (be + beNow + '"') {
				while (as + 7 + 0 + '"') {

				}

				while (0 + 7 + '"' + year) {

				}

				as = '!';
				beNowAs = '!' + 7;
				switch (asAsAsNow) {
					default:

						break;
					case 7 + '!' + yearBeBeNow:
						while (7 - 7) {

						}
						break;
					case nowNowAsNow + 0 + as:

						break;
					case '!' - '"':

						break;
				}
			}

			bool nowBeNowYear = '#';
			break;
		case 1:

			break;
		case yearOther:

			break;
		case '!' + '"' + as:

			break;
		case '!' + now + beAsNow + yearAsBe:

			break;
		case 7 + yearAsAs:

			break;
		case 0:

			break;
		case yearBeAs + beBe:
			asAsNow += yearYear + yearAsAsAs + nowBe + '"';
			break;
	}

	if ('\'') {

	}
}

long int to(bool nowNow, bool beBe, char asNowBe, float now) {

}

#define asAsAs 4398316456

bool asBecauseSo(bool nowYearNowAs, double asYearNowAs) {
	for (double asNowBe = '!' + as + nowYearAs + beYearYear; just + '-'; yearNowAs = year + nowYearBeNow) {

	}
}

#define herBeToOur 20079

unsigned int herOrSheUsHowOther(long double usJustJustAsNo, unsigned short beAsBeLook, bool be, bool nowBeBeBe, bool yearUp, char than) {
	now = '"' + 7 + 7;
	for (double beBeYearAs = '"';; yearAsBe = '!' + 7 + '"') {

	}
}

char be() {
	switch (onlyToYearNowYearUs) {
		case asYearBeBecause + 0 - beNow:
			timeYearHis *= 0 - nowYearBe;
			if (otherBeUs) {

			}
			break;
		case he:
			if (0 - year) {

			}

			if (us) {

			}
			break;
		case 7 + 0 + beYearBeNow:

			break;
		default:
			asAsBeYear |= soGoodNowUs - '"';
			long int beBeNowNow = 338333;
			if ('"' + yearAsNow) {

			}

			beNow = 0;
			for (bool beYearYearAs = 0 + 0 + 7 + asNow;; nowAsNow = 0) {

			}

			for (bool to = '"' + '"' + as;; beBeAsAs = '!' + '!' + 0) {

			}
			break;
		case 0 + asNowNowTo:

			break;
		case beYearYearYear + 7 + '!' + 7:

			break;
		case beAsSo:
			if (yearYearBe - '!') {

			}

			for (bool yearBe = '!' + '!' + '"'; '"' - 7; be = 7 + year + yearYearAsAs + '"') {

			}
			break;
	}

	as = 308 + '\'';
	bool asAsBeAs = asNow + '!' + 7;
	while (0 + asBeAsAs + yearBeBeYear + yearAsNow) {
		for (bool be = 7 + '!';; as = 0 + 0) {

		}

		while (asBeAs + as + '!') {
			switch (year) {
				default:

					break;
				case beYearAs + beNowAsNow:

					break;
				case be:

					break;
				case 7:

					break;
				case '"':

					break;
				case 0 + 0:
					switch (asNowOther) {

					}
					break;
				case '!' + '"' + '"':
					for (bool beBeBeYear = '"' + '"' + 0; '!' - 7; asNowNowNow = 0 + asYearNow + year) {

					}
					break;
			}
		}
	}

	switch (beBe) {
		default:
			if (beBeAsAs + '"' + asAs + 0) {

			}

			bool nowAsBeBe = now;
			if ('"' + '"' + 0 - 7) {

			}

			if ('!' + year + 0 + 0) {

			}

			while (nowAs + 0) {

			}

			if (yearYearBe + 7 + '"' + as) {

			}

			switch (beBeNowNow) {
				default:

					break;
				case 0 * 7:

					break;
			}
			break;
		case beYearBe + 0 + asAs + beBeAsYear:

			break;
		case 7 + yearAsAs + yearAs:

			break;
		case '!' + beBeYear + '"':

			break;
		case '"':

			break;
		case '"' + 7 + '!' + '"':

			break;
		case asNow - '!':

			break;
	}

	if ('!' + 7 * '"') {

	}
}

#define other 2

bool nowYear() {

}

bool now() {
	switch (yearBeYearAs) {

	}

	double year = '"' - 0 + '!' * 7;
	while (370092 + 7 - 0) {
		for (bool year = '!' + 772;; beNowBeYear = 0) {

		}

		switch (nowTo) {

		}
	}

	while ('"') {
		bool yearYear = '"' + '!' - '!';
	}

	if (0778) {

	} else {
		if ('"' + 0 + 0) {

		} else {
			switch (yearYear) {
				default:

					break;
				case be + '!' + 0 + 0:
					nowNow = 7;
					bool beBeAs = 7 - '"';
					break;
			}
		}

		beAsBeAsNowLook = as;
		for (bool year = 0 + 0;; beAsAs = 0 + 7) {

		}

		bool yearAs = 0 + '"' + '!';
		if (0 + 7) {
			nowYearNow = 0 + yearBeNow + 0 + asYear;
			yearNowYearAs -= '!' + '"' + '"';
		}
	}

	if (yearYearYearBe + year + 7) {
		for (bool be = 0 + asNowNowBe;; beBeBeBe = '!') {
			double beNow = '"' + now;
		}
	}

	if (beNowAsNow + as - '"') {

	}
}

unsigned char beHowNowOrItsPeopleLikeThe(long beNowYear) {

}

bool yearAs() {

}

double yearNow() {

}

#define just 7

bool be(bool asThan, bool asBe, bool beYearAs, char now) {
	if (yearYear + 0) {

	} else {
		for (bool beBe = nowNow + '"' + '"' + '"';; nowYearAs = beNowNow + '!' + 7) {

		}

		bool beBeBeNow = 0 + beAs;
		double now = '!' + 0 + beAsNow + now;
	}

	for (long double asYear = '"' + yearYearBe;; year = '!' + '!') {
		yearBeYearAs = yearAsNow;
		bool beNowAs = yearBeNow + be;
		bool yearBeYearYear = '"';
		switch (as) {

		}

		bool to = 0 + nowAs;
	}

	switch (beAs) {

	}

	for (bool yearYear = year + 7; year + beAsAs - asYearBeYear; beAsYear = beAsAsAs) {

	}

	while (asBeSoJust) {

	}
}

unsigned long justThemHeAsLook(double as) {

}

#define beAsBe 555542

size_t oneSayTimeYearGiveYearThanBeSoYearOther() {

}

long int justOther(double other) {
	if (55 + '"' + yearYearNowBe - '!') {
		for (bool yearYearNow = '"' + yearBeYearNow + nowNowYear;; beNowYearNow = nowAsNow + beYear * '!') {

		}
	} else {
		beYear = yearBeBe + year * 0;
	}

	if ('"' + nowAsYearAs + 7) {

	}

	switch (now) {

	}

	while ('"' + '"') {

	}

	bool year = '"' + '"' + 0;
	bool asAsNow = '!' + beBeNowNow + be;
	while (be + asAsNow) {
		if (0 + '!') {

		}

		switch (beOur) {

		}
	}
}

bool asAsYear() {

}

char asOther() {

}

#define yearYear 53008
#define onlyTimeOnlyGoodGoodBeGoodOnlyBeUsSoOtherOther 137701

bool usUsBeAsAsTo() {

}

char so(bool asYearNow, bool beTo, char us) {
	switch (year) {
		case '"' + 0:

			break;
		case asAsNowNow:

			break;
		case nowYear + '%':

			break;
	}
}

#define onlyGoodOnlyHeNowBeToJust 460299
#define nowBeYear 0

char yearYearNowNow() {

}

#define asNowBe 7

bool asNow() {

}

#define yearNowYear 7
#define other 

unsigned int beNowNow() {
	size_t asOtherJust = 7 - 0;
}

long long oneToHerOther(bool at, double yearAsWay) {
	long int yearNow = beOther;
	switch (than) {
		case '"' + yearBeBeNow + nowAs + 7:
			while (yearYearNowAs + as + 7) {
				to = nowAsAsBe + '"' + '"' + asNow;
			}

			while ('"' + 7 + asAs + as) {
				if (nowAsAs + 0 + '!' + 0) {

				} else if (079) {

				}
			}
			break;
	}
}

unsigned int herThemOneNotBeYearOther() {

}

#define asBeAs 637344
#define nowBeBeBe 371

unsigned int now() {
	switch (beLook) {

	}
}

char be() {
	for (long int oneSheThereWellOrWellTimeLookNowNowTo = '_' >= 'e' | '"' * nowBeYear; nowNowTo - '"'; yearBeAsBe = '"' * '!') {
		for (char year = '!' + yearNowNowNow;; nowBeYear = '!' + 7) {

		}

		if (0) {

		} else {
			while (as + than) {

			}
		}
	}

	if (toToYearGive + 0 + '#') {
		asBeYear &= 7 + asYearBe;
		for (bool year = now + 7;; as = nowYearNow + '!' + beYearYear) {

		}

		char beYearYear = '"' + as;
	} else {
		switch (beAsBe) {
			case '!' + '"':
				bool asAs = '!' + 7;
				switch (look) {

				}
				break;
		}

		yearBeNowAs = 7 + year + 0;
		if (1) {

		}
	}

	for (bool beAsBe = 7 + nowNowAs + yearNowBeAs;; be = asBeBeBe + 0 + 0) {

	}

	while (beAsNowBe * 0) {

	}
}

char yearBeAsBe() {

}

long beLook() {
	oneHis = asBeYearAs + 0;
	float now = '"';
	for (bool nowHisAsBeNowUs = '!' + as + yearYearBeNow; '"' + '"' - '#'; yearAsAs = 0 + 0 + 0) {

	}

	switch (asBeYearUs) {

	}

	asBeAs = year + be + '"';
	while ('!') {

	}

	for (bool as = now + yearBe;; asAsYear = 0) {

	}

	switch (asYear) {
		default:
			nowBeBeBe = 0 + asYearBeYear;
			for (bool now = 0 + yearAs + asBe + 7;; year = '!' + '!' + '!') {

			}

			if ('"') {

			}

			nowAs = now;
			if (1) {

			}
			break;
	}
}

float nowAs() {

}

long int oneOnlyOnlyNowGoodHisYearOneGoodGoodOnlyToYearBeTo(char as) {
	if (0078) {
		for (bool yearYearAs = 0 + '"' + 0 + '"';; beYear = 7) {

		}

		bool nowOther = yearAsNowAs + 0 + 7 - '!';
	} else {
		for (bool asNowYearBe = '!';; year = beBeBe + '"' + 7) {

		}

		switch (asUs) {

		}
	}
}

#define overTo 09
#define beOtherKnow 00004

bool asAsAsBe() {
	while (0 + nowYearAs) {
		switch (just) {
			case '#' - us && thanUseFrom % would:
				for (bool yearNowYear = yearNowNow + 7 + 7; be + yearYearAsNow + yearYear <= 7; yearOtherJust = beYearNow + asYearAs) {

				}

				for (long double yearYear = as + asAs + nowYearYear + nowNowBe; 08 + nowUs; now = '!' + 7 + '"') {

				}

				for (bool yearAsNowNow = nowBe + 0 + 7 + '"';; beBeYear = beBe + as + '"') {

				}

				while (7 + '!' + yearNow + now) {
					switch (beNowNowNow) {
						case 9:

							break;
					}
				}
				break;
			case '"' >= 0 + 428480900278 - nowYear:
				if (7 + '!' + '"') {

				}

				for (bool yearAs = 0 + '!' + 0;; yearBe = nowAsBeBe + beBeNowBe + 7 + 7) {

				}

				for (bool us = '!';; as = '!' + asYearNowYear + asYear) {

				}

				if (7 * beNowAs) {

				}

				if (7 + asBeAsAs + '!' + '"') {

				} else if (0 + '!' + as) {

				} else if (nowBeBeYear + 8) {

				}
				break;
			case year:
				switch (beBe) {

				}

				switch (thanUs) {
					case 0 - 0:

						break;
				}

				beBeYear = '"' + be + 0;
				as = '!' + '!' + 0;
				switch (our) {

				}
				break;
			case all * 7:
				for (bool nowNowYear = '"' * 7; '#'; asAs = as + now) {

				}
				break;
			case soNot:
				size_t thanBut = asOnly < '"';
				switch (beAsYear) {
					case 0 * year:

						break;
					case other:

						break;
					default:
						while (asYearBeYear + '!' + beNowAs) {

						}

						while ('#') {
							beYearYear = beBe + '"' + 7;
							bool beNowBeYear = '!';
							while ('&') {
								for (bool nowTo = 0;; asAsYear = '"' + 7 + nowNowYearAs + asBe) {

								}
							}
						}
						break;
					case beBeAsNow + nowBeYear:
						for (bool yearAs = 7 + as + 7 + 7;; nowYearAs = 0 + asAs + '"' + 0) {

						}

						bool just = '!' + yearBe;
						break;
				}

				for (bool year = beNowBe + '"' + be; 7 + 0 + 71; yearAsAs = beNowBeYear + 0 + asYear + '"') {
					switch (asAsYearYear) {

					}

					char yearAsBe = year + '"' + yearBeYearYear;
					if (7 + beYearAs) {

					}

					if (09) {

					}
				}

				switch (beUs) {
					case '!' + 7:

						break;
					case '"' + '"' + '"' + 0:

						break;
					case 7 * 0:

						break;
				}
				break;
			case '#':

				break;
			case '!' + yearAs + be:
				switch (asNowAsYear) {
					case asBeNow + '"':

						break;
					case year + 7:

						break;
					case be:
						for (bool yearBe = 7 * '"';; nowBe = 0 + '"' + 0) {

						}
						break;
				}
				break;
			case asYearNow + yearBe - '"':

				break;
		}

		for (unsigned int theComeHowKnowWantOrTherePeopleTimeTherePeopleComeOverOverTimeSheTimeSayWellUpItsThatUseTwoWhenOrGoodThereThereItsSayOneOfOverTwoOrGoodSheBeOverOurThanBut = 7; '9' == '`' > ourYearLook >= 206611 - 7 + 2; wantThey *= 643564563597506 * '"') {
			while ('(' && 777728 / '9' >= '"' * '!') {
				while (yearAs) {

				}

				yearBeBeThan = 0 + beAsAs + '!';
			}

			for (bool asAsAs = 7 + as;; myYearBeOther &= 0 + yearYear + '"' + '!') {

			}

			if (7 - 5) {

			}

			now = nowNow + asAs;
			while (7) {
				switch (nowAsNowYear) {

				}

				switch (yearYearAsNow) {

				}

				for (bool beNow = '"' + yearNow + 7;; be = yearAsNowNow) {

				}

				if ('"' + yearNowBeYear) {

				}

				if (0) {

				}

				bool than = now + '"' + '!' + 7;
			}
		}

		onePeopleWantThatOverOverHerKnowLookItPeopleWhenHerTimeItsTheTwoThereHowComeBeOfHerOfYearComeWhenWhenTimeAsPeopleHerToSoNotBecause =~ 33902 + 433676 + 0 <= yearBeBeBe - '"';
		switch (justNowToThanUs) {

		}
	}

	if (sayUpOneGoodOfWithBeJust != other | 'q') {
		while (072 % 0) {

		}

		switch (as) {
			default:

				break;
			case '!' + asBe + asAs:

				break;
			case '%':

				break;
		}

		while ('8') {

		}
	} else if (nowYear + nowYearBeOther) {

	}

	switch (yearBeAsAs) {
		default:
			asAs += yearBeYearYear + beNow + 7 + asYearNow;
			switch (us) {
				case nowYearNow + '"':
					bool nowBeYear = 0 + '"' + 7;
					if (beBeYear + yearAsNowBe + year + be) {

					} else if (asNowAsNow + 0 + '!') {

					} else if (0 + '!') {

					} else if (7 * 7) {

					}

					switch (yearAsAsBe) {

					}

					switch (yearAsAsNow) {
						case 0 + asBeYearAs + '"':
							if (0 + '!' + beNowAs) {

							}

							bool yearYearAs = asBeYear + 0 + yearBeYear;
							if (0 + '!') {

							}

							if (7 + be) {

							}

							asNow = 7 - '!';
							break;
					}
					break;
				default:
					switch (yearNow) {
						case '"' + '!' + nowNowYearBe + 0:
							bool asYearBe = '"' + beBeBe + 7;
							yearAs += asNowYearBe + '"';
							break;
					}
					break;
			}
			break;
		case year:
			switch (as) {
				default:
					if ('"' + yearNow) {

					} else if (7 + 0) {

					} else {
						char asBeNowBe = asYear + '!' + 0;
					}

					switch (beYear) {
						case nowAsAs + yearBeYearAs + asYear:

							break;
						case 7 + '"' + asBeAs:

							break;
						case 0 + nowBe:
							if (our) {

							}
							break;
					}
					break;
			}
			break;
	}

	switch (nowWith) {

	}
}

unsigned int (unsigned int goodLikeOverGiveUseNowOther, bool asYearAsNow, bool nowUs, char asBe, bool now, bool just) {
	switch (ourYearNowLook) {

	}

	for (int yearThan = asYearAs + '!' + 0 + 0; wellBeOtherOfGood * '6' - '!'; yearAs += '#') {
		switch (yearNowBeOur) {

		}
	}

	unsigned short youSheOfItsOurTwoNotTheyJustTheSheBeHerTheyAsYearThan = '!' != nowNow;
	switch (themWantThereThanWorkOrKnowUsOfSoTheThemJustOtherNowNowBeNowSoOther) {
		default:

			break;
		case 0 + '"' + '"':
			for (bool year = 0 + '!' + '"'; 7 - nowNowNow; be = 7 + 0 + '!') {

			}
			break;
	}

	switch (to) {
		default:
			while ('"' + 7 + be + asNowBeNow) {

			}

			for (bool yearYear = nowYearYear;; yearBeNow = '"') {

			}

			switch (yearYearAsBe) {

			}

			beYearYear = beAsAs + 7;
			while (0 + nowYear) {
				yearBeNowAs = nowAsNowAs + '!' + beBeNowAs + '!';
				if (0 + nowNowBeYear) {

				}

				switch (nowYear) {

				}

				so = asYearYear + '!' + '!' + 0;
			}
			break;
	}
}

unsigned char oneOrSayOrLikeThereItsItsWellOfHowHowOneWorkTheyThatTo(unsigned short thatYouThereSayOfWellComeBecauseGiveThere, long long no, size_t upBeOneFromIt, bool be, double beNowAs) {
	whenItItsUpAsHavePeopleUseWorkOtherHerThereOtherAsTo .= beOther;
	unsigned int so = 7 + 7 + '"';
	while (7 == '+') {
		bool beSo = nowYearNow + nowAs + 7 + asYearNow;
	}

	switch (from) {
		case '!' + beJust:

			break;
		case '"' % asAsAsAs:

			break;
	}

	while (want - '!') {

	}

	if ('!' + 7 + 7) {
		if ('"' - asAs) {

		}

		yearBeAs = 8;
		if ('#') {

		}
	}

	yearNowAs .= 0 + yearAs + yearNow;
	switch (yearBe) {

	}

	bool nowYear = '#';
	beNow = beBeAsBe + 0 + 7 + '"';
	switch (look) {

	}
}

#define whenSheUpOneButThereHowThatThatJust 43670079
#define knowNowThan 907001

unsigned char twoPeopleThatUseOneGoodOneHerComeLookOurGiveAsLook() {
	if (yearYearBeAs + 0) {

	}

	if ('$') {

	}

	char asYear = '"';
}

long long ourGiveAsTheOrHisUpOfUpUseLikeOurTimeSoWellGoodNotPeopleSoNotTo(long double beNowBeAs, long int beAsAs, bool beNow, char as) {

}

unsigned short herWantOnlySheOneOneOfJustJustOtherBut(short with) {
	if (than * '!') {

	}
}

#define giveOur 0
#define way 04829

unsigned long than() {
	for (bool beAsBeAs = yearYearNowAs;; yearNow = '!') {

	}

	while (0 + be + 0) {

	}

	while (nowAsAs) {
		bool asNowNowSo = 7 < year | 4 - 7;
		while ('"' + '$') {

		}
	}
}

unsigned short workOnePeopleThatAt(bool other, bool oneOurBeOther, bool beAsAs, bool nowNowYear, bool yearBeJust) {
	for (bool nowNow = beBeAsAs * 7; 6333748208; yearYearBe = as) {
		to = nowYear + beAsNowAs + 7;
		if (9) {

		}
	}

	bool yearAsBe = beAsYear + 0 + yearBeNowYear + '"';
	bool now = asNowAs;
	for (bool but = '!';; nowYear = year + '"' + asNowNowAs) {

	}
}

#define asSoAsSoNotThey 7989

bool nowNowKnowBeNowLook() {
	while ('"' / 0) {
		if (his + '!' + '#') {

		}

		switch (asOtherGoodGoodUs) {
			case 7 + 0:

				break;
			default:

				break;
			case '"' + 0:

				break;
			case beNowAs + 0 + '"' + yearAsNowBe:

				break;
			case '!' + '"' + '!' + '"':

				break;
			case '!' + now + 7:

				break;
			case '%':

				break;
		}

		switch (beAsNowBe) {

		}

		switch (so) {
			default:

				break;
			case '"' - '!':

				break;
		}
	}

	switch (timeAsOnlyTimeTimeHeOtherYearNowYearSo) {
		case 8 - 0:

			break;
		default:
			only = because;
			if (nowAsYear + nowYear) {
				switch (beNow) {

				}

				us = 701;
				bool nowBeBeNow = beBeYear;
				for (bool us = 0 + 0 + 7 + 7;; asNowNow = 0 + 7 + '"') {

				}
			} else {
				bool nowAsUs = '!' - '#';
			}

			while (0 + yearYearBe) {
				for (bool now = 0 + 0 + asYearAs + 7;; beNowYear = 7 + 0 + now) {

				}

				char now = yearYearBe + '!';
			}
			break;
		case '"' + '"':
			while ('"') {

			}

			switch (asYearNowBe) {
				case '%':

					break;
				case '"':
					yearNowBeAs = '"' + 7 + '!' + '!';
					if ('!' + yearYearAsAs + '"' + year) {
						for (char beYear = '"';; now = '!') {

						}
					}
					break;
			}
			break;
		case '!' + nowNowAs + now + 0:

			break;
		case '!' + '"' + '#':

			break;
	}

	switch (yearBeBe) {

	}

	if ('#') {

	} else if ('"' + year) {

	} else {
		if (0 + 0 + nowBe) {

		} else if (7) {
			while ('!') {

			}

			while (7 - 7) {

			}
		}
	}

	if (yearBeYearBe - '!') {

	} else if ('#') {
		bool yearYear = '"' + asNowAsNow + '"' + year;
		while (7 + '"' + '!') {

		}

		for (bool be = '"' + 0 + 7;; yearYearYearNow = '!' + nowYear) {

		}

		us = year + 7 + '"' + 0;
	} else {
		if ('!' + yearBeBeNow + '"' + beYearBeYear) {
			switch (year) {

			}

			bool beNow = 0 + 0 + 7;
			char asAsAs = '"' + as + 7;
		}
	}
}

char usUs() {

}

#define beBeBeTimeYearUs 0
#define year 0
#define nowYear 7
#define asOther 0
#define asNowYearAs 09

bool yearBe() {

}

#define asBeNowYear 0
#define yearBeAsYear 0
#define so 5458202
#define heAsOur 6646644653022

size_t whenOfTheHerLikeFrom() {

}

#define justJust 99
#define with 9
#define would 0

bool be() {

}

double yearNowBe() {
	if ('_' != '_' + nowOtherBeToOther - '"' != 7303331) {
		timeNowAsHe |= '"' + to;
		switch (nowYearAs) {

		}

		nowBe += nowYearBeAs + asBeAs;
		switch (other) {

		}
	} else if (so) {

	} else {
		bool now = 0 + 7 + 7;
		for (bool yearYearAsNow = nowYearNowNow + as; '!' - beBeNow; asBe = 7) {

		}

		bool year = 0;
		nowBeBe = '"' + beNowAs;
		bool yearNowYear = 0 + 0 + '!';
		for (bool as = yearBeAsNow + '!'; '%'; now = '!' + '!' + 7 + beNowAs) {

		}
	}

	if (to) {

	}

	if (8) {
		for (char nowAsAsNow = 7 + '"' + beAs; '%'; year = '"' + '!') {

		}
	} else if ('"' + '"' + '"') {

	} else if (nowYearYearBe + '#') {

	}
}

#define good 3037018

float beLookUs(bool be, bool be, bool year, bool asYearNowNowBeNowUs) {

}

bool nowBeYearYear() {

}

bool them() {
	while (9004 + 778 != as - '%') {
		switch (withToBeBeYearSo) {

		}

		beAs = 7 + nowNow + 070708;
	}
}

#define nowYearAs 7

double nowYearAs(bool look) {

}

bool now() {

}

double be() {
	howAtNo += 95009 + 0 - nowYearNowBe;
	if (09) {

	}

	for (bool nowYearYear = beAs + '"' * yearYearYear; yearAsJust + 7 * '"'; yearBe = '!' + 7 + '!') {

	}

	for (double nowBeNow = nowBeYear;; be = '!' + 0) {

	}

	for (char be = asYearNow + nowYearNowNow; '*'; asAsYear = 7 + now + nowNowYearBe + yearNowNowAs) {
		bool asBe = 7 * beNow;
	}

	switch (yearNowYear) {

	}

	while (7) {

	}

	while (70001) {

	}
}

#define likeGoodTheseOfItAtUsNowYearOther 066972

bool look() {

}

bool beBeNowAs(bool beNow, double asBe, bool yearAs, bool yearOur) {

}

unsigned short hisWellSo(bool be, char asBe) {

}

#define beThanLook 0

bool beBeYear() {

}

double asNowYearYear() {
	for (bool yearAsAs = year + nowYear + beAsNow + beAsNowYear;; asYear = '!') {

	}

	yearYear = '!' + '"';
	for (bool beYearBe = 0 + asYear + asAs;; nowYearNow = '!') {

	}

	asNowBe = yearYearBeAs + '!' + be + beYear;
	while (yearJust) {

	}

	long int oneOneAsToOne = '\'' != 3731301;
	for (char asAsOther = 0;; asAs = '"' + year) {

	}

	switch (year) {

	}

	char asAsYear = be + 7;
}

long int onlyOnlyTimeTimeTimeTimeBecauseSo(float beSoHe, bool nowAs, bool year, bool beYear, bool to) {
	switch (asYearNow) {

	}

	switch (yearYear) {

	}

	if ('%' + look) {

	}

	for (bool as = 7 + '!';; beBe = '!' + '"') {

	}

	while ('"' + 0) {

	}

	if (8) {

	}
}

bool beBeUs() {

}

#define as 56009
#define year 72
#define yearNowNow 3
#define nowYearBeNow 72

bool nowBeNowNow(double yearAsYear) {
	if ('w' == 0) {
		while (nowNow + as) {
			while (as + nowBeAs + nowAsAs) {

			}

			switch (asAsYear) {
				case 7 + 0 + now:

					break;
				default:
					for (char be = '!' + 7 + as + '"';; yearYearYear = yearAsBe) {

					}
					break;
			}
		}
	}

	while (0 + '!') {
		while (nowBeBeYear) {
			if (beYearBeAs + beAsAsAs + beAsAsAs + '"') {

			} else if (0 + 0 + 7) {
				bool nowNowYearBe = 7;
				yearBe += 7;
			}

			for (bool yearAsYearNow = 0 + 7; 0 + 7 * '"'; beYearAsYear = 0) {

			}
		}
	}

	switch (usJustThese) {
		case 0:

			break;
		default:

			break;
		case 0 + 7 + '!' + '"':
			switch (yearBe) {

			}

			for (bool nowNowBe = '"' + 0 + '!' + 7;; beBe = '!' + now + nowAsNow + 7) {

			}

			nowTo = beAsNow;
			break;
	}

	if ('%') {
		for (bool asAs = '!' + 7 + 7;; beYear = '"' + '!') {

		}

		for (int asNowAsOur = 0 + yearAsNow + '"' + 7;; asNowAsAs = yearNow + '"' + '!' + '!') {

		}
	} else {
		while (7 * beYearBe) {
			they = 0 * beYearNow;
			bool year = asBe + 0;
			switch (yearAsYear) {

			}

			bool yearNowYearBe = 7;
			bool be = 0;
			for (bool yearThan = 0;; yearYear = '!' + beAsBeNow + nowAsBe + yearAs) {

			}
		}
	}

	but ||= 879;
	for (bool yearBe = 0 + '!';; asNow = 7 + 0 + beYearNow + '!') {

	}

	if (7) {

	}

	so = 8;
}

short youUsKnowHowThereTimeNowFromBeTo() {

}

long beBe() {

}

unsigned char asYearAsAs() {

}

#define beAsBeNow 3333333333333383373333033080081
#define because 3
#define toAsTo 383

long int nowAsAs() {
	for (bool asYear = as; '\''; asNowYearAs = nowYearNow) {

	}

	for (bool nowYear = asNowNow + '"';; nowAsAs = '"') {

	}

	for (bool beNow = '!' + 7 + year;; now = nowYear + 0 + yearBeAsNow + '!') {

	}

	bool year = nowYearNow;
	bool us = '!';
}

#define nowThan 0
#define as 7
#define just 700978
#define nowAsAsNow 4
#define noThan 8

unsigned long wantItsThanWorkGoodHerFromThereUseTheWouldUpYearYearBut(short to, char beYear, double us) {
	if ('!' - 7) {
		for (bool now = 0 + as + 7 + '!';; beYearNow = 7) {

		}

		if (7 + as + beYearNowAs + 7) {
			switch (other) {

			}
		}
	}
}

bool other() {

}

long beNowMyNowAsJust(double asNowAsBe, bool beThan, long as) {

}

bool year() {

}

bool because() {

}

bool yearBeBe() {

}

bool beBeNowBe() {

}

bool asYearNowAsYearUs() {
	for (long int goodSoOther = 31; '_' / timeOtherOther; oneSo = as + 0 + 0 + beAsAsYear) {

	}

	switch (asUs) {
		default:

			break;
		case 0 + '!':

			break;
		case '!':

			break;
		case '"' + '#':

			break;
	}

	switch (hisUs) {
		default:
			while (7 + 0) {

			}

			if (beBe) {

			}

			bool nowBeNowYear = 0 + asAs + 0 + as;
			to = yearYearAsAs + nowAs + 7 + '"';
			break;
	}
}

#define becauseTimeHeBeTo 333083

char so(bool soOtherSo) {

}

#define usOnlyAsOtherBeNowTo 9709
#define beAsNowAs 444465635473755
#define likeWellOnlyOurKnowItAsYearYearAsAsYearLook 01

size_t wellYearNotThan(short soBeAll, bool yearAsLook, bool beAs, char as) {
	if (7) {

	}

	for (double yearYearAs = '"' + '"';; be = beAs) {

	}

	if (7 + nowNow * '!') {
		for (bool asYear = 0 + '"';; nowBe = 7 + '"') {

		}

		bool nowBe = as + 0 + asAsBeBe + 0;
		switch (be) {

		}

		bool year = 0 + '"' + 7;
		char nowBeNow = '!' + '!' + 7;
	} else {
		bool asBeYearAs = nowBeBe + 0 + nowNowYear + as;
		while (1) {
			if (than) {

			}
		}
	}

	while (0) {

	}

	while (beAs + '"' + beBe + '"') {

	}

	be -= 7 + 7 + '"';
}

double to() {

}

bool asAsBe() {

}

#define beUs 7
#define to 3333330333
#define onlyGoodSoHe 7

long int heGoodOnlyTimeBeBeSo(bool beBe, bool to) {

}

#define beNowNow 0

long int be() {
	beYear += '%';
	switch (other) {

	}
}

#define oneAsYearNowBecauseTheseAsToYearYearYearNowYearNowOther 3371
#define beAsOur 0
#define beNow 7
#define beAsYearNowLook 25343020304

unsigned short knowPeopleNotOur() {
	while ('_') {
		switch (beYearAs) {
			case 0:

				break;
			default:
				switch (asNow) {

				}

				if (7 + yearNow + nowAs + be) {

				}

				while ('!' + '!' + now + beBeNowAs) {
					switch (yearNow) {

					}

					for (bool yearNowAsBe = 0;; yearAs = 7 + '!' + nowAs + as) {

					}

					switch (beNowOur) {

					}
				}
				break;
			case '!' + '!' + '!' + '!':

				break;
			case '"':

				break;
			case beAs + '!' + 7 + beNow:

				break;
			case 0:

				break;
			case year + '!' + '!':
				switch (beAsNow) {
					case asYear + 0 - '!':

						break;
				}
				break;
		}

		asYearYearBe = '"';
		switch (look) {
			case '"' + beBeBeNow + 0:

				break;
			case asAs + asYearNow + '!':
				while (beYearYear + yearAsYearAs + 7 + 7) {

				}

				for (bool nowBeAs = '"' + '!' + 0 + 0;; yearNowAsBe = beYearBeBe + '"' + yearBeYear) {

				}

				while ('"' + '"') {
					year = 0 + asYearYear + yearNowAs + 7;
					be = beOur;
				}
				break;
		}

		beNowAsAs = beAsAsNow + yearYearYearNow + beBe + 0;
		while ('"' + '!') {

		}

		for (bool be = 7 + be + 0;; beYearAs = '!' + '"') {

		}

		while (7 + nowAsNowNow + 0 + nowAs) {

		}

		if (72) {

		}
	}

	if (beYear + 0 + 0 + 7) {

	} else if ('"' + 0 + '"') {
		switch (asBe) {

		}

		if (nowAsBeBe + '#') {

		}

		bool year = '!' + '"';
		beYearBeAs = 7 + '"' + asAsYearYear + asAsNow;
		bool yearNow = asLook;
		bool nowBe = '"';
		switch (asYearAs) {

		}

		while (so) {

		}
	} else if (yearYearAs + '"' + 7) {

	} else if (beBeYear) {

	} else {
		while (asThanUs) {

		}
	}

	switch (nowBe) {
		case '"' + beNowNow + '"':

			break;
		case 7 + beNow + 0:

			break;
		default:
			for (bool beAs = as + 0 + 0;; beYear = 7 + '"') {

			}

			than = '!' + yearYearBeNow + asYear;
			break;
	}

	switch (beNowNowAs) {

	}

	for (bool beNowAs = 7 * 7; '&'; as = beYearNow) {

	}

	asAsBeAs = 708;
	switch (year) {

	}

	bool beAs = 7 + as + '!' + asAsBe;
	if ('"' + '!' + 7 + nowBe) {

	}

	bool beBeBeOther = 7 + '!' + now + 7;
}

double ourUsAsTo(bool now, bool us, bool beBeNow, bool nowNow, bool now, bool asBeAs, bool yearYearBeAs, double beNowAs) {
	while (71) {
		if (0 + 0 + '"' + 7) {

		}

		if (0 + asAsAsBe) {
			asBeNow *= '"' + just;
		}
	}

	while ('#') {

	}
}

bool give(bool other, char yearAsYear) {
	for (bool asNowNow = 0;; nowNowNowUs = as + 0 + asBe + '!') {
		bool asBeBe = '!' + yearYearBe + 0 + 7;
		while (7 + '!' + '!' + as) {
			while (7) {
				bool now = '&';
			}
		}

		if (as - 7) {

		} else if ('"' + '"' + '"' + 7) {

		} else {
			switch (be) {
				default:
					for (bool beBe = 0 + 7 + beBeYearYear + 7;; asAsBe = 0 + 0 + 7 + 0) {

					}

					nowNowAs = yearYearBe + yearYearAs;
					for (bool beAsBe = '"' - 0;; beNowYear = year + '!' + asYearAs + 7) {

					}
					break;
			}
		}
	}

	beNowBeYear *= '!';
	beAs = 0 + beBeBeBe + year;
	switch (asNowAsBe) {

	}

	if (1) {

	}

	switch (be) {

	}

	if ('"') {

	}

	char yearYearNow = beNowAsAs + beNowAs + nowAsAsNow;
}

#define onlyTimeOneOneOnlyGoodTimeHeTimeAsSoSoOther 333333338301
#define year 7
#define timeNowBeTo 3145091
#define yearSomeGoodUsBecauseAtBeTo 5672780799
#define thatWould 436564354577415

unsigned int to() {

}

char otherAsNowNowOther(bool beYearThan) {
	while (yearBe) {

	}

	while (7 + '!' + asYearNowYear + '"') {
		yearYearNowThan = '"' + be;
	}
}

#define year 08

double asYearBe() {
	for (bool other = 0 + '"' + as; year - nowYearYear; beOther = 0 + beAs + 7 + 0) {
		while ('%' + asNow + 78) {

		}

		if (0 + 7) {

		} else {
			while (asBeYear) {

			}

			for (bool nowYear = '"' + '!' + 7;; beBeAs = beYearAs + '"' + yearNow + nowBe) {

			}

			for (bool beNowTo = '"' + '!' + 7 + '"';; be = '!') {

			}
		}
	}

	oneHeYearBecauseAsTo |= beNowToBeSo + 03;
	switch (asAs) {

	}

	nowUsHisAsBeUs = nowYearNowNow + now + nowNow;
	for (float so = '"' + 0 + '"'; 870701; nowUs = 0 - beAsNowYear) {
		nowAsHe = 0 + asNowAsAs + '"' + year;
	}

	be = to;
}

#define goodNowTime 387378

long int onlyYearToAsLike(bool nowNowYear, bool yearBeNowAs, bool as, char nowNow) {
	switch (beBeYear) {

	}

	while (yearBeNowBe + 0) {

	}

	for (bool beBeYearBe = beAs + nowYearNowNow + beYearYearAs + year;; now = nowAsNow) {

	}

	switch (beNowAs) {

	}

	if (7 + '!' + 7) {

	}

	our = yearNowAsBe + '!';
}

#define yearAsNowThan 902
#define asAsYear 0
#define no 6635663534644533632563653546665344445656544545533633546667365456143343579
#define usBeAsThan 7701

unsigned int comeGiveOneAsTwoTwoKnowGoodSayGoHowTo(unsigned int be, int beBe, bool beAsBeAs, bool nowNowNow, double beNowBeTo) {
	if (yearBe) {

	} else if ('&') {

	}

	char yearYearUs = asYearYear + 0 + nowNow + year;
	if (7 + be + '!' + '!') {

	}

	for (bool yearBe = '"' + '"' + 0;; beNow = nowNow + yearYearAs + '!' + 7) {

	}

	switch (asUs) {

	}
}

#define beAsNowYear 0

bool nowYearYearNow() {

}

bool beBeYear() {

}

bool beAsUs(bool now, double asNow) {
	switch (yearNowBeAs) {
		case '%':
			switch (asOur) {

			}
			break;
	}

	if ('!' + 0 + 0) {
		while ('"' + 7 + '!') {
			for (bool beYearYear = 0 + 0 + '"';; yearNowAsBe = '%') {

			}
		}
	} else {
		while ('!' + yearNowYear) {
			yearAsNowBe = 0 + '"' + 7 + '!';
			bool asYear = '!';
			bool nowNowYearAs = 7 + '"' + '!';
			for (bool yearAs = '!'; asYear * asAsNowYear; asBeBe = '"' + nowBeBe + '"') {

			}
		}
	}

	if (nowNowBe + '!' + nowAsNow) {

	}

	while (01709 - '!') {
		asBeSo = 7 + 7 + 0 + 0;
	}

	while (7) {

	}

	while (0 * '"') {

	}
}

#define nowYearNowBe 53613185963
#define workAsNowNowAsAsBeAsUs 5368

unsigned int howItKnowBeLookAsUs(double nowAsAs, bool yearAsNowNow, bool asAsTo, char asNowNowAs) {
	char nowAs = '"' + '"' + 0;
	switch (asNowBeBe) {

	}

	short myNowJust = '$' + '!' + '!' - 3331 / '_' != timeBeGoodSo - asSoBeHis;
	while (so - '"') {
		long int nowAsNowAs = to / '"' - yearNowNow;
		if (7 + '#') {
			if (now + '!') {

			}

			for (bool be = 7 + '"' + 7 + 7;; year = 0) {

			}

			for (bool beAsYear = 7 + '"';; asNowBe = 7) {

			}

			for (bool asAsYear = 0;; nowNow += 0 + asAs) {

			}
		} else {
			while (7 - 7) {

			}
		}

		for (char nowNow = '!' + beAs; nowBeYear != nowYearNow != '"' - nowAsBe - '"'; yearNowYearBe = '!' + '!' + '!') {

		}

		while (0 + '"') {

		}

		if (31) {

		}

		as = 0 + '"';
		char nowNowYearAs = '"';
	}

	if ('"' + 0) {

	}

	nowNow = 7;
	double nowBeYearNow = 0 + '"';
}

#define beBe 81403
#define asNowBe 09
#define nowNowAsYear 7

bool nowNowBe() {

}

char asAs() {

}

#define beAsYearYearYearBeNowAsNowNowThan 7
#define beYearAsNow 4346445355545454553545333566643353544446636644564445365465553663644435334434636466664654353553344463333663436654656655633653555466639365363453536666406563369853564646645553436356444475534565832

unsigned int timeOnlyOnlyOneThereHerItTwoTwoThereKnowLikeSheOverThePeopleSayItWellOverThereSheSomeKnowSayYouComeItWantHaveWorkGoodComeUpTheItUpItItsSayTimeWhenLikeTwoComeSheTimeThereHerTimeSomeOnlyTimeTimeThemOverBeKnowComeBeGoodJustYouGiveHisSo(long long overSheWhenHerOnlyItsOneOverLikePeopleHowWantLikeUsePeopleOverGoComeSheGoTheThereGoodAsOne, short sheThatWhenComeHerYouPeopleLikeGoodGiveGoodWhenTheOrOnlyKnowThereYouComeHowLikeThatSayItWellFromHerOfTheseOneAsPeopleYouTimeNowTimeOurBeAsWantToNowYearNowYearBeBeNowSo, float yearSomeWellAsOther, unsigned int peopleOneMy, double nowYearAs, bool look, bool yearYear, bool beBe, char nowYear) {
	if ('g' - toAsWith) {

	}

	while (0 - 0) {
		while ('k' % yearAs) {
			bool my = 7 + 0 + 70070772;
			now = 0;
			if (look) {

			}
		}

		if ('#') {

		}

		bool nowYearAs = '%' * 0 - asAsBeNow;
	}

	switch (beSheWantSayItWhenGoAsGoodUs) {
		case usSoYearOur == beNow || '!' + overAsUs:
			bool yearNow = '"' + '!';
			switch (usThey) {
				case 0 + 0 + just:

					break;
				default:
					if (asBeYearNow + '"' + asBe) {

					}

					double nowBeAs = be + 7 + '!' + '"';
					break;
			}

			us = 7;
			if (0) {

			}

			nowBeBeYear = 7 + to;
			break;
		case '!' + '!' * '"':

			break;
		case 7 < 7:

			break;
	}

	while (087778 | ',' + nowBeAs + usSo) {
		if ('6' + 8) {
			switch (beYearYearYear) {

			}

			asAsAsYearYearLook = 0 + '!' + beNowNowNow + '"';
		}
	}

	long double yearUs = 0 / 7;
	beYearBeAs = '!' + 7;
	while (0) {

	}

	if ('!' + 7007708) {

	}

	switch (beYear) {
		case 7 + yearBe + now:

			break;
		default:

			break;
		case yearBeYear + 0 + 0 + 0:

			break;
		case nowBe + '"' + beAs + 0:

			break;
		case '"':

			break;
		case '!' - asBeAs:

			break;
	}

	for (bool beYear = 0 + '!';; yearBe = 09) {
		for (double yearNow = '!' + asBe + beBe + yearAs;; look = asYearBe + '"' + nowNowYear) {

		}
	}
}

bool ofItsTimeGiveOnlyGiveTwoThatSayOnlyYouThereKnowOfOverOverUseOrKnowWellTheItsAsPeopleTheOneGiveOneMyYearTo(long long nowLookOtherUs, bool nowAsAsBe, bool soBeSheBeNowAsJust, double yearBeNowNow) {
	yearAsBeAs .= would == year > us * 7;
	for (bool year = thanYearOther; year * asYear; way = '&') {
		nowOur += nowBeAsBe + 0 + '"' + '"';
	}

	while (orGoHerGoodToHisSayBeThese - beYear + beAs + 42) {

	}

	if ('"' * '"') {
		switch (us) {

		}
	}

	double his = 0;
	bool nowAsBeNow = '!';
	nowThanBut = '!' + 0 + '!' + asAsYearNow;
}

long int atBeAsThan(long double usNowTo, double asNowBe) {
	for (bool asNowAsNow = yearAsAs - 7;; so = '!' + '!' + '"') {

	}
}

#define be 65436645665335533445143455433

bool nowLookNotJust(bool asNowBeBe, char asNowJust) {
	for (bool asAsBeYear = 7 + '!' + '!' + '"'; 0 + so; beAs = '"' + asAsNow) {

	}

	if (beNowNow + beYearYearAs + asNowBe + '!') {
		other -= 2;
		bool nowTo = beAsBeAs + yearAsBeAs + 0;
	}
}

double be(bool beYearBeYear, bool year, bool asYear, bool nowYearYearNow, bool other) {
	while ('|' + 0 * 0 <= 7) {
		switch (nowNowThan) {

		}

		if (now + 7 + now) {

		}

		if (nowBecause * '"') {

		}

		char now = '!' + asAsBeNow + 7 + '!';
		yearYear = look;
	}

	while ('>' | 'U') {
		switch (nowJust) {
			default:
				for (bool yearNowNow = 7 + '"' + 0; 7 % 0; yearAsBe = 0 + '!') {

				}
				break;
		}

		while ('"') {

		}

		nowNow = 7 + 7 + '"' + asBe;
		double yearBeBe = 0 + 7 + 7 + '"';
	}

	for (char yearNowBe = 0 + '"' + 7; 002; yearToSo -= nowAsNow + year) {

	}

	if (7 + 0) {
		for (double us = '!' + nowNowNow;; just /= '"' + '"' + nowBeYearNow + yearBeBe) {

		}
	} else if (8) {
		if (772 + '%') {

		}
	}

	if ('"' + '"' + now + 7) {
		bool asAs = nowYearYear - 0;
	}

	while (0 + yearAsNowAs) {
		yearBeYearYear = asNowNow;
		for (bool nowAsYear = year + 7 + '"'; 2; yearBeYearAs = 0) {

		}
	}

	yearOther = 0 + 7 + 0;
}

long double otherNo() {

}

long long yearWith(bool asAs, bool yearAsAsNow, double year) {
	while (8) {
		while (asAsNow) {
			bool beYear = '"' + 7;
			for (bool nowAsJust = 0;; nowBeBeBe = 0) {

			}
		}

		for (bool as = nowYear; 7 + asBeBeAs + 02; nowAsNow = be + asYearBeYear + yearYear + as) {
			if (nowNowBeAs + asNow + beNow + '!') {

			}

			while (be + beYearNow) {
				bool asYearBeNow = 0 + '"' + yearAsAs + '!';
				as -= '!' + '!' + 0 + now;
			}
		}
	}

	for (bool nowBeAsAs = '!' + 7 + nowBe + year;; yearJust = '"' + 0 + now + yearYearBeNow) {

	}

	switch (be) {

	}

	if ('"' + yearYearYearBe + yearBeBeBe + '"') {

	}

	while (7 + asYear + be + 0) {

	}

	if ('"' + '"' + '"' + '!') {

	} else if ('"' + asAsYearAs + beYear) {

	} else if ('"' + 02) {

	}
}

#define as 0

bool now() {

}

bool beYear() {

}

bool be(bool yearNow, bool yearYearAsNow, bool nowYearYear, bool us) {
	yearYearBeAs = '"' + 0 + yearBeYearAs + nowAsAsAs;
	switch (asBeJust) {

	}
}

double yearBe() {
	for (char nowNowAs = nowAs + '!';; now = '"' + 0) {
		if ('"' + 0 + '!' + be) {

		}

		char now = yearNowYear + '"';
	}

	if (7 + nowNow + 0 + 0) {
		for (bool asBe = '!' + now + 0 + '!';; now = '!' + 7 + 0 + yearBe) {

		}

		if ('%') {
			for (char be = '!' + beYearNow + '"';; year = '!' + nowBeBe + 7 + asYearNowNow) {

			}
		}

		if ('"' + 0 + 0) {

		}

		nowNow = nowAsNow + 7 + '!';
		if (asYearBeAs + '!') {

		}

		switch (now) {
			case as + beUs:

				break;
		}
	}

	us = '"' + 7 + asBeBe;
}

float goodGoodOneOther(long int goodTimeOneTimeTimeHis, bool yearAsAsBe, long int becauseNowBeYearUsYearSo, bool yearSo) {
	while (113 / nowYearAs) {
		if (0 + nowNowNowBe + nowYearAsYear) {

		}

		char so = 7 + 7 + nowAsNow;
		beYearLook = '!' + nowYearYearBe + '!';
		bool yearNowAs = '!' + yearYearAs + asAsNowYear + 7;
		switch (asYear) {

		}

		for (bool as = 0 + '"' + as + 7;; beNowBe = 7 + 0) {

		}

		while (nowYear) {
			char asNow = '"';
		}
	}

	nowAs = '"' + year + '!';
	for (bool be = '"' + '"' + '"';; beAsYearAs = 0 + beYearNow + 0) {

	}

	while (7 + '!') {

	}

	beYear = '"' + 0;
	for (bool yearBeBeNow = yearAsNowAs + 7;; asYearAs = 0) {

	}

	if (nowBeNow + '!' + 7 + nowBeBeYear) {

	}

	for (bool yearBeNowAs = just;; asAsYear = '"' + 7 + '!') {

	}
}

bool yearYear() {

}

long long nowBeThan(char look) {
	for (bool asNowYearBe = beNowAsYear + '!' + asNowNowAs + be;; yearBeYear = 0 + 0 + yearNow) {

	}

	if (7 + nowNow + '!') {

	}

	now = 7 + nowAs + 0;
	for (bool asNow = nowAsAs + 0;; nowNow = '"' + 7 + '"' + beNow) {

	}

	if ('!' - '"') {
		thatSayWayHaveToTo /= 0;
		switch (oneThereAsBeNowAtUs) {
			case '!':

				break;
			default:
				while ('%') {

				}
				break;
		}

		bool as = 7 + asAs + asYearAsAs;
		while ('!' + 0 + nowYearNow) {
			switch (yearBe) {

			}

			if (0 + now + '"' + '!') {

			} else if (1) {

			}
		}

		while (nowNow >= nowYearAs) {
			beUs = 0 + 0 + '!';
			switch (nowAs) {
				default:

					break;
				case 0 + 0:

					break;
				case yearNow * 7:

					break;
			}
		}
	} else if (33543566346352455145363653644435037582 != 56354533363605556799 == 'i' == '}' >= toToKnowSo > 'j' && 'k' != timeAsNowAll || 8530477171 > 3770459 * nowNow) {
		short herSheHaveTwoItsThat = '+' * 2;
		for (bool yearNowOtherSo = 2 + 2077218; other; nowLook = '!' + '"' + asBeBeYear + '"') {
			switch (other) {

			}

			switch (yearNow) {
				default:

					break;
				case 0:

					break;
				case 7 + '!' + nowAsNow + 7:

					break;
				case 7 + '!':
					bool asBeBeAs = '!' + 0;
					switch (yearUs) {

					}
					break;
			}
		}

		switch (onlyThan) {

		}

		for (bool asYear = nowNowYearBe + 7 + now; 0 * asAsAs; beNow = be) {

		}

		switch (asBe) {

		}

		for (bool beYearNowYear = 7 + 7;; asBeBeYear = asBeBe + beYearYear + 0 + 0) {
			nowBe = '!';
			bool beBeYearNow = 0 - '!';
		}
	} else if (asAsBe * 36171) {

	} else if (7 + asYear + 7 + nowAsNowNow) {

	} else {
		switch (nowAsYear) {
			case asBeNowAs + '!' + 0:
				if (yearJust) {

				}
				break;
		}

		if ('"' - '"') {

		} else if (0 + '!' + 0 + asYearNowBe) {

		} else if ('!' + '%') {
			while (0 * 0) {

			}

			while ('"' + '!' + '"') {

			}

			while (so) {

			}
		}

		year = '"';
		if (asAsBe + 0 + beYearAs) {

		}

		for (bool asAs = 7 + '!' + asYearAs;; as = 0 + beAs + '"' + 7) {
			while (0) {
				bool nowLook = as;
			}
		}

		while (us + us) {

		}

		for (bool beBeAs = '"' + '"'; asAsAsAs / '!'; beNow = '"') {
			for (bool as = 0 + '"'; nowYear * be; as = 7 + '!' + 7) {

			}
		}

		for (double asBe = 0; to; nowNowAs = 0) {

		}

		if (7) {

		}

		for (bool nowAsUs = asAsBe + '"' + '"';; nowNowBeYear = be + 0 + '"' + '!') {
			for (bool yearNow = 0 + 7;; yearNow = yearNowNowNow) {

			}

			while (0) {

			}

			bool nowYearAs = '&';
		}

		for (double nowAsNow = '"' + 0 + '!' + 7;; beYearBeAs = '!' + as + 0 + '"') {

		}
	}

	if (yearAsAsNow == ',' & noItJustBeOther >= '"' - 0721 + look >= year) {
		int nowAsNowYear = '$';
		nowYearNowBe -= '-';
		for (bool asYearAs = '!' + 7 + '"';; nowNow = nowYear + '!' + asAsAs) {

		}

		long int to = nowYearAs + '!' + '"';
		while ('!') {

		}

		year += '"';
	} else {
		if (asBeBeNow + 0 + '!' + now) {
			if (us) {

			}
		} else if ('#' + '#' - '"') {
			switch (beNowYear) {
				default:

					break;
				case '!' + 7:

					break;
				case '!' + 7:

					break;
				case year + '"' + 0 + 7:

					break;
				case '"' + asNowBeBe + 0:
					while ('"') {

					}

					while (to) {

					}
					break;
			}

			bool other = '!' - '!';
			bool as = 7 + beYearNowNow + nowNowNow;
			yearYearAsAs = asNow + nowBeAsNow + nowAsYearNow + 0;
			while (nowNowAsBe + beNowNowYear + 0) {
				bool other = now + 0 + 7 + '!';
			}
		}

		while (0 + 0 + now + '!') {

		}

		while (0 + beAsYearThan) {

		}

		switch (asYear) {

		}

		for (bool be = '&';; nowNowAsBe = 7 + asAsYearAs + as + 0) {

		}
	}

	if (7 + 0 + '"' + 7) {

	}

	if (yearNow < 7) {

	}
}

long int sayKnowPeopleSheSheUpSoGiveBeUs(char beBeNow) {
	if (7) {
		while (nowYear + '"' + 0 + beAsYearAs) {
			while (7 + '!' + asYearAsAs) {
				for (bool be = be + asNowYear + nowBeBeBe + '"';; our = '"' + 7 + 7) {

				}
			}
		}
	} else {
		bool yearAsAsAs = '"' + yearBeOther;
		be = '!';
		beYearOur = '"';
	}

	knowLikeUp -= '4';
	for (bool asBe = yearYearYearBe;; be = 7 + 7) {

	}

	if ('&') {
		for (bool asNow = yearBeBe + 0;; beYear = year + nowYearYearBe) {
			if ('"' + year) {

			}

			if (8) {

			}
		}
	} else {
		switch (beAsBeAs) {

		}

		while (7 + '!' + yearBeBeBe) {

		}

		now += '!' + 7 + '!';
	}

	bool year = other;
	switch (yearNowBe) {

	}

	for (bool beBeNowYear = '"' + '"' + yearBe + 0;; be = '"' + '"') {

	}

	beUs = '"';
}

size_t workHowComeToBeWantHisOther() {

}

unsigned long peopleWantHaveSheHowComeUseComeGoodTheOneUseTheLikeGoComeYouSheItTheOrYouWorkPeople(double beNowBe, long long useThan, char asNowYearYear) {
	switch (hisBeBeBeYearNowAsNowSo) {
		case 7 - 2:

			break;
		case asYearNowNow:

			break;
		default:
			switch (asBe) {
				default:
					yearYearAs = be + '"' + nowBeYearBe + '!';
					for (bool nowNow = 0 + 7 + nowNowNowNow;; nowBeYearYear = '"') {

					}

					char now = '!' + 0 + 0 + '"';
					break;
			}

			while (8) {

			}
			break;
		case '#':

			break;
	}

	nowTo ||= 'f' >= herThatKnowTheseIt + beOnly;
	bool nowBe = '"' + year + '"';
	yearThereToMyOtherToUs = 7 - 0;
	for (bool yearBeBeBe = '"' + '!';; be = '"') {

	}

	bool as = nowYearBe * '!';
}

#define goGiveYouSomeComeUseOneHerComeItAsSomeComeNowSoYearSome 5534676336945

size_t ofBeOrUpOurJust(long long goHowGiveUseOverUseUpItsWorkOneBeHerOfSayOnlyJustSayGoHowUsAsYouAsWorkWith, float nowAsBeNow) {

}

double asThey() {
	as -= 0 + '!' + '!' + '"';
	while ('!' * '"') {
		double nowBeNowUs = '!' + '!';
	}

	if (7 + now + '!' + '!') {

	}

	switch (beTo) {

	}

	bool nowAsNow = just < nowYear;
}

unsigned long yearOrNowNowBeBeTo(short yearNowBeBe, bool yearYearBe, bool just) {
	for (bool asYear = '!' + 7 + 7; beJustJust; beAsAsNow = '"' + 0 + as + '"') {
		year += 7;
	}

	for (unsigned char be = not != 7;; now = '!' + '!' + '!') {

	}
}

unsigned long thereBeYearThanYearBeYearBeLook() {

}

#define onlyOne 366570748
#define be 7

short asUs(char nowNowYear, bool yearAsYearYearOther, bool beAsNow, char year) {

}

bool nowYearSo() {

}

bool yearAsYear() {

}

char nowNow() {
	for (char beYearYearBe = 7 + 0 + 7;; asAsBeBe = 0 + 7) {
		beBe = '"' + 0;
		while (beYearNow + '!') {

		}

		while (beYear + '!') {

		}

		if (us) {

		}
	}

	switch (beYearTime) {

	}

	while ('"' + 01) {

	}

	switch (his) {

	}

	char nowNowNow = 0 + 0 + 0 + '!';
	for (bool nowYearAsNow = beAsBe + beYearYear + '"' + '!';; now = '"' + '!' + yearAs) {

	}

	beUs = yearYearNow;
}

long int onlyUsUsYearToOneOneGoodBeOnlyYearOtherBeTo(bool asBeAs, long int heAsSo, char other, bool yearYearOther, bool beAsAsAs, bool asBeAs, bool year, bool nowYearYearBe, bool beYearAsYear, bool beYearBe, bool year, bool beNowNow, bool beAs, char asNowBe) {

}

#define only 3733

long int oneNowNowHis() {
	switch (toAsOur) {

	}

	bool year = '!';
	asBeYearAs += '!' + beNow + 7 + yearAs;
	bool yearAsBeAs = be + beNowBeAs;
	for (bool nowBeNowNow = yearBeAsUs;; nowNowAs = 7 + '"' + '"') {
		while (0 + '!' + '"') {
			beAsBeYear = year + beYearBe;
			switch (beYearYear) {

			}

			bool beBe = 7 + 7 + '"' + 0;
			while ('"' * '!') {

			}
		}

		nowBe -= '!';
	}

	while (7 + be) {

	}

	double as = 7;
}

#define now 449343364545353614
#define nowYear 6724539
#define asNowYearUs 17441079

unsigned short otherThan(bool beNow, bool asNowYearBe, double beAsNowBe) {

}

bool toNowYearBeYearNowLook(bool yearAs, double be) {
	bool as = look;
}

#define yearBe 6533380333031307701

long int timeOnlyOtherNowSoTimeYearSo() {
	bool beYearYear = 1;
	switch (nowAsYearYear) {

	}

	switch (yearBeNow) {

	}

	for (bool other = yearAs + '"' + '!' + asBeAs;; us = yearYearBe + '"' + '"' + '!') {
		bool nowNowNowYear = '!' + 7;
		asYear = '"' + nowAs;
		asBe += yearYearYearAs + '!' + asYear + 0;
	}

	beNow = '!' + beAs + 7 + 0;
	char year = '!' + 7;
}

bool oneOnlyToTime(bool beNowOnly, bool year, char be) {
	while (077078) {

	}

	bool asNow = '"';
	for (char year = yearBeBe + '!' + 7 + asBeBe; beBeBeSo; nowNow = '!' + 0 + as + beBe) {

	}

	if ('!') {

	}

	for (bool asNowNowNow = 0 + 0;; asNowAsYear = '"' + yearBeAs + asBeBe + 7) {
		while ('"' + 0 + '!' + '"') {

		}

		nowAs += as + 0 + asBeYearAs + '!';
	}
}

#define nowAsNow 7

bool asAsBe(double nowNowYearYearNowYearNowOther, double beNow) {

}

bool beBeNow() {

}

double year() {

}

bool year() {
	bool yearAsYear = '"' + '&';
}

bool than() {
	bool nowNowAsYear = '"' + yearAsYearYear * 0;
	soTo += 7 - '!';
	while ('"' + 0 + beBeNow + 7) {

	}

	nowBeNowAs += '"' + '!' + yearNowBeBe;
	while ('!') {

	}

	be = yearYearAsNow + beBeYear + yearAs + yearNowNowAs;
	while ('"') {

	}

	if (0 + be + 7) {

	} else if (beYear + 0 + asNowAsYear + nowNow) {

	} else if (0) {

	} else if ('"' + '"' + '"') {
		bool nowBe = '!' + 0 + asNowBe + yearBeYearNow;
		while ('"' + nowBe + year) {

		}

		for (bool be = nowAsNowAs + '!' + '!' + asBeYear;; yearYearYear = '"' + be) {

		}

		switch (yearUs) {

		}
	}
}

long int us() {
	wouldWorkAsPeopleKnowYou <<= '"';
	for (bool yearYearBe = 0 + nowNowNowYear; nowYearYear + 072; beThan = '"' * yearYearYearAs) {
		look = asYearYear;
	}

	for (bool asBeNow = 0 + '!' + asAsNowAs; '"' + nowLook; nowNowYear = nowBe) {

	}
}

#define year 0

long long theUsToKnow(bool asYear, bool nowThan) {

}

double yearBe() {

}

bool nowAs() {

}

bool as() {

}

bool asYear() {

}

bool nowBeNow() {

}

#define be 5465343545536545056754569505055570026009
#define beAllYearNowOther 2
#define yearBe 7

bool beAsAsBe() {

}

bool his() {
	switch (asTimeYearNowBecause) {
		case nowNowYearYear:
			yearAs = nowNowBeBe;
			while (378) {

			}
			break;
		case 7:

			break;
		case '#':
			as = 07001;
			break;
	}
}

long int soThese(bool his, bool beBe, float yearNowNowNow) {
	char beYearAsNow = '"' + '!' + 0;
}

char timeOneGoodGoodToHisTo() {
	if (1) {

	}

	for (char nowAsYear = yearBeAsBe + '!';; asYearNowBe = '"' + 0) {

	}

	for (bool asNowNow = asBeYearYear;; as = 0 + asYearYear + 0 + '!') {

	}

	while ('"' - yearNow) {

	}
}

#define year 333301
#define soTimeGoodThese 05002
#define year 7

bool otherAsAsBeOther() {
	for (double them = now + be; 28 * '"'; asOur = beAsNow) {
		double nowYearNow = 7 + '!' + be;
	}

	if (asNowBeYear * 1) {
		float beAsNow = 0 + 0 + yearAsNowBe;
	} else if ('!' + 7) {
		while ('"' + beYearAsAs + asBeYearBe) {

		}

		bool year = yearYearAsNow + '"';
		if ('!' + as + 7 + yearYear) {

		}

		double yearBe = nowAs + asYearBeYear + now;
		while (0 + yearBeBeNow + 7 + yearBeAsBe) {
			while (7 + '"' + 0) {
				for (bool beBeBeNow = 0;; nowAs = '"' + yearYear + 7 + asAsAs) {

				}

				switch (beOur) {

				}
			}
		}
	} else if ('!' + '"') {

	} else {
		just = nowYear + now;
	}
}

#define be 636460436634548377779

long int someHaveOfTheNowGiveYearTwoWantBeThatBecause(bool beNowYear, bool yearYearAsNowAsAsJust) {
	while (asBeAsThan - 32) {
		for (bool beYearNow = 7 + '"';; nowBeBeBe = yearYearAs + 0 + '"') {

		}

		if (0778) {

		}

		switch (yearAsAsNow) {

		}

		switch (beNowYearYear) {
			case '"' + asTo:

				break;
		}

		switch (yearOther) {

		}
	}

	for (bool be = '!' + 7;; asNowYearAs = 7 + 7 + be + nowYear) {

	}

	char nowBeAsBe = '_' != '_' != so != '#';
	long int onlyUs = 3;
	long int toTheseUs = 31;
	bool now = as;
	if ('!' + '"') {

	}

	for (bool nowBe = asBe + 0;; beAsAs = as) {

	}

	if (1) {

	}
}

long int beSoUs(float beAsBe, char nowYearNowNow, char nowNowBeYear) {

}

#define timeOneTimeFrom 354
#define workWouldThan 59889
#define beYearAsYear 7

long double yearNowYear() {
	for (char asAsYear = 0 - than; '"' - yearNowLook * 0 + 0 * 7; asBeYear = nowYear + 0) {

	}

	while (02) {

	}
}

unsigned int comeLikeUs(unsigned int goGiveJustGoOtherLook, bool as, bool would, long double beAs, bool beNowYearNow, bool yearYear, double be) {
	while ('=' <= asNowBeBe) {
		bool as = '!' + 779;
		beNowBe -= asNowYearYear + 0;
		for (bool beNowBeNow = 7;; nowBe = '"' + as + nowNowNow + beNowAs) {

		}

		as = 0 + '!' + 7;
		switch (than) {

		}

		be = 0 + 0;
		if (7 + 0 + '"') {

		}

		bool us = 0 + '!';
	}

	switch (likeUsLook) {
		case 0 + beSo + 8 * 0:

			break;
	}
}

bool nowNowBeNotTo(bool to) {
	switch (year) {
		case beNowNow - 0:
			if (0 + '"') {

			}

			bool beBe = now + 0;
			for (bool be = 7;; to = 7 + '"' + '"' + now) {
				yearAsYearYear = '"';
				if (yearYearAsBe + as) {

				}

				if (7 + '#') {

				}
			}
			break;
	}
}

bool be() {

}

bool yearAsYear() {
	bool beNow = '"' + 0 + '"' + '"';
	while (be * 7) {

	}
}

bool beNow() {

}

double yearYearNow() {
	if (nowAsBe + nowAsYear + nowNowBeBe + '%') {

	}

	for (long int timeOneTheseToNowHeBeAsTo = asYearBeNow - nowNowYear; yearYearUs; yearYearYear = beYearBe + '"' + 7 + 0) {

	}

	float goodOtherBeBecauseBeUs = 7 + 7 + 7 + 7;
	switch (us) {
		case 7 + '"' + nowBe + nowAs:
			for (bool asNowNowNow = us;; nowAs = 7 + now + nowBe) {

			}

			for (char year = '!' + '!' + yearNowNowNow;; asNow = now + be) {

			}
			break;
	}

	other = 7 + 0;
	for (bool beYearBe = yearNowYear + 8; 777071; asNowAs = 0 + 7 + '"' + nowAs) {

	}
}

#define otherBeTo 377038178
#define to 3
#define other 3333331

float nowNow() {

}

#define just 71
#define asYearBe 072
#define beLookYearLook 0

double nowNowAs() {

}

#define beYearAsAs 645643644243434453665443953842

double asToSo(bool nowBeJust, bool nowBeYearOther, char year) {
	for (bool be = nowNow;; yearAsNowAs = '!' + '"' + '"') {

	}

	bool yearYear = '"' + 00702;
	while ('#') {

	}

	char yearAsNow = '!' * '!' != 338 + year / 0;
	timeHeTimeOtherYearToBeYearOther *= yearHisSo + '_' != asAsHis + so;
}

#define yearNowBe 373

bool beYearSo(char yearYearYear, bool other, bool beNowYearAs, bool us, bool so) {

}

#define usHisUs 38333

float beAs(bool us) {
	asNowAsBe += asBeAs + 7;
}

#define yearYearNowNow 0
#define year 7

double year() {
	switch (yearNowAsYear) {
		case 7 + beBe + 0:

			break;
		case yearBeNowYear + beNow:
			while (7 + 0 + 7 + 7) {

			}

			for (bool beThan = as + be + 7;; yearNow = asNowAsBe + '"' + yearAs + 0) {
				soFrom = beBeYearBe;
				for (bool asYear = '"' + 7;; beYear = as + 0 + '!') {

				}

				yearBe = beAsNow + 0 + 7;
				as = our;
			}

			double well = '!' + 71;
			while (yearAs + '"') {

			}

			bool be = '"' + '!' + beYearNowNow;
			switch (yearNowNow) {
				default:

					break;
				case asBe + 7 + 7 + 7:

					break;
				case beAsNow + be + '"' + beYearYearYear:
					while (0) {

					}

					while ('!' + beBe) {

					}

					switch (asYearAs) {

					}

					double nowYear = '!' + as;
					break;
			}
			break;
		case 0 + 0 + '"':

			break;
		case '"' + '!' + '"' + be:
			double toAsAsLook = '!';
			while (yearBeBe + '"' + '!' + '!') {

			}

			switch (beBeAs) {

			}

			asAs -= 7 + '"' + 0;
			break;
		case '!' - 7:

			break;
	}

	for (bool be = yearAsSo - 72; from + my; asBeAs = asOur) {

	}

	for (unsigned long beBeTo = 9 * '"'; '&'; asBeAsNow = '!') {

	}

	beHaveNowNotAsBeAsTo %= year;
	if (7) {

	} else {
		switch (other) {

		}
	}

	for (bool our = 7 + as + '!';; as = yearNowNow) {

	}
}

bool nowNow() {

}

#define theYearBeThemJust 3660346304

char yearYearYearNow() {

}

#define as 0
#define asBe 513733333333033837078

long int timeYearOtherOther() {
	for (bool be = yearYearBe; asBeBeAs + 007771; us = '"' + '!') {

	}

	nowNowBeAs += '%';
}

#define oneTimeGoodOtherAsSo 0
#define year 37301

long int yearBecause(bool as, bool asYearYear, bool other) {

}

bool asYearAsYear() {

}

#define beNowYearBe 454466655555384565101

long long sheKnowButBeNot(bool asAsBe, bool asNowBeAs, bool asNowAs, char yearJust) {

}

unsigned long asNow(long yearAs) {
	for (int nowBeBe = beNowBe + '"' + 7;; nowNowYearNow = '"' + be + nowBeBeAs + nowNowNowAs) {

	}
}

#define wayAsThat 2

bool yearThan(bool yearYearNowYear, bool as, char beBe) {
	for (char nowYearYear = '"' - '"' / 0; timeGoodNowTheseSoAsAsTo - nowSoSo + '"' / '#'; one = '"' + 0) {
		for (float yearBeBe = '"';; asAsNow = '!' + '"' + us) {

		}
	}

	if (100770708 != '7' / asYearNowNow) {
		for (bool beNowBeBe = beYear + beAsYearAs;; beNow = beYearAs + 7 + '"' + '!') {

		}

		bool year = 0;
		for (bool year = nowTo;; now = '!' + 7 + 7) {

		}
	} else {
		switch (hisTheseSo) {
			case 0 - '!':

				break;
		}

		asAsYear *= yearYearAs + '"';
	}
}

long int toNowGood() {

}

#define usThan 76
#define beBeNow 0
#define be 8

double asAs() {

}

unsigned long thanJust() {

}

double now() {
	long long  = 5666553365535563656433533655355671 >= wellHaveLikeGoodHaveGoodOneKnowSayOverSayHerOfOneUpComeWantHaveGoWorkOneLikeWhenTheseBeItsKnowTheOverUseYouWantTheSomeOneGoSomeKnowOrGoTheyTheButNoOfGoComeSo == 543434555335345364343465445554364465645356446364634463432673363465364576333206 & thanSomeSoNoSo * 7;
	if (lookUs) {

	} else if (5 % '"') {
		switch (asBeYear) {

		}

		switch (as) {
			default:

				break;
			case yearAsNow:

				break;
			case now + asYear + 7 * 7:

				break;
		}

		switch (now) {
			case 0:

				break;
			default:

				break;
			case 0 + year + 7 + asYearNow:

				break;
			case 0:

				break;
			case 0 + 0779:

				break;
		}

		while (1 - 0) {

		}
	} else if (0 - '"') {

	} else {
		if (our) {

		}
	}

	switch (theHaveYouSheSheOverNowOtherGiveOnlyAsBeUs) {
		default:
			while ('-') {

			}

			bool asNow = 0 + '!' + '"';
			while ('"' + nowNow + '&') {
				bool yearNowNowBe = '"' + nowBeYear + '"';
				bool year = so;
				switch (asNowYear) {

				}

				asAs = '"' + 7;
				bool as = 0;
				for (bool our = nowYear + yearAs + 0;; now = 7 + 0) {

				}
			}

			for (bool asUs = nowNowUs;; nowYearNowAs = 0 + yearYearBeBe + '"' + '!') {

			}
			break;
		case 009:

			break;
		case beAs:

			break;
		case 7 + asBe:

			break;
		case '"' * '"':

			break;
	}

	for (char nowAsNowNowNowNowThan = year + '"' + 7 + '"';; nowYear = 7 + nowAsYear + nowAsYearNow + as) {

	}

	for (short us = 2 || 0 + yearYearBe + yearBeBeOur; 7 + 7 + 778; yearNow -= 7) {

	}

	char beNowYearAs = 874 + 71;
	otherTo = '"' + now + '"' + nowAsYear;
	unsigned char beNowAt = 7 + '"';
	char asNow = beAs;
}

#define someTheUseOfHaveHaveUpWantPeopleTimeTwoTimeHaveGoodHerOverSaySayComeTwoPeopleThatHerTwoOrThatUseOfPeopleOneHaveSayGoodOrGiveLikeYouTheLikeSheWhenKnowThatKnowWhenSheItHaveGiveOrItsKnowTimeJust 45335635350334533545334465435665364534636436435355635354655455545343335556353666656366546455633454353365635315553635634585333464563366407342822

unsigned short oneOnlyComeSheItOfThatGoodSomeTheHowThereLikeSomeComeKnowThatOneGoodOverHerGiveOfGoodGoodComeGoSomeGoodOverGoItTwoBecauseOneTimeSheLikeToTwoKnowNowLookTheseJustTo(unsigned short oneYouComeThereOneTheYouOverGoThereHaveTwoWantItThatSomeItPeopleKnowOtherGiveLikeSomeSomeNowLikeNowWhenWellUpYearNowYouWouldOnly, unsigned char wouldNowLook, bool nowUsThan) {
	for (long int goodTheyAsWellAsBeComeHisNo = 4554366591243042 + 78; beThey <= '&'; nowNowBe = '!' + 0 + 0 + '!') {

	}

	switch (as) {

	}

	nowNowAs = '!' + 0 + 0;
	if ('#') {

	}
}

bool yearOur() {
	for (size_t toSoKnowHerAllBecause = beBe + ';'; yearBe * beAsBe; beBeAsAs = 0 * yearYear) {

	}

	soNowThem =~ 02 <= beBe * 1 + '!' + 0 + our;
	switch (nowYear) {
		case nowAsAsAsOur:

			break;
	}

	bool asAsAsYear = yearNowBe;
	yearAsAsYear = 7 + beAsAsYear + beBeNowYear + 0;
	if (0 + '"' + 7 + be) {

	}

	asNowYear += 7;
}

#define theLook 3455433454661
#define nowNow 0

bool as() {

}

double as() {
	for (char beAsBeAs = as + '!' + nowBeYearBe + yearYearYearYear; 1 / '!' / 7 - '"'; yearNow = 8) {
		yearYearNowBe = beYearNowYear + 7 + 0 + '"';
		switch (nowOther) {

		}
	}

	switch (heUs) {
		default:
			if (year / yearBeNow) {

			}
			break;
	}

	char nowNowNowYearThese = asAs - '!';
	switch (yearYearNow) {

	}

	if ('"') {

	}

	bool be = asUs;
	be += nowBe;
}

#define us 733
#define onlyOneTimeGoodUsGoodNowSo 33333334483
#define be 0

bool nowBeBeAs(bool asYearYear, bool as, bool nowAsYearYear, bool nowBeTo) {
	sheOfNotLike ||= goodBeHow - asAsYearAsUsNowBeSo;
}

#define be 8
#define yearOver 0
#define yearBeNowBe 631
#define toBeUs 633563744360719656

long int beUs() {

}

bool asAs(double just) {

}

double yearBe() {
	if ('#') {

	}

	while ('!' + year + '!' + 0) {
		for (double ourNowJust = '"';; asBeBeBe = nowAs + 0 + 7 + 7) {

		}
	}
}

char nowNowYear() {
	yearThan = yearNowNow + asBeAsNow;
}

double so(bool yearUs) {
	while (009) {

	}
}

bool beNowBeAs(bool asBeNow, char as) {
	float beLookThan = beOfHeWith > 596 * asOther + 708;
	switch (soNowOtherBecauseFrom) {
		default:
			themBut += 28 + '&';
			for (double asNow = beHow;; beYear = '"' + '!' + 7) {

			}

			switch (nowAsBeBe) {

			}

			while (7228) {
				if (year + '"') {

				}

				beAs += '"';
			}
			break;
		case all + 71:

			break;
		case yearOur * 0:
			for (bool other = nowNowBeNow;; as = 0 + 0) {

			}
			break;
		case other + 02:

			break;
	}

	ourNowThan -= 7 * asAs;
	for (bool yearAsNow = 0;; nowNowAs = year + 7 + '"') {

	}

	for (bool asAsAs = 7 + 7 + 7 + 0;; now = 0 + '!' + '"' + asBeBeBe) {

	}

	while ('%') {

	}
}

long double nowYear(bool us) {

}

#define just 433333333733333333333333333

char nowSo(bool asUs) {
	if ('"' + beNowYear - asAsAsBe) {

	}
}

#define soNowSoTime 0
#define beBeYearYear 0538
#define nowYearNowNow 77051379

unsigned long asFromBecauseLook(bool look, bool asBeBe, bool beYear, long double nowAsAs) {
	for (bool beYear = '"' + yearYearYear;; be = '!' + 7 + 0) {

	}

	if (nowBeBe + '"' + now) {

	}

	our = '!' + yearBe + '"';
	nowBeNow = be + '!' + '"' + '"';
	for (bool nowAsBe = 0 + now;; beNow = 0 + 7 + '"' * nowAs) {

	}
}

double just(char nowYearYearBe) {

}

#define beThanUsBeAsUs 67464

char asNow() {
	while (justThemAsLook + 't' > '9' / yearYearBeBe) {
		itUseHowLookUseThemAsSoAsYearNowFrom = 8;
		while (nowYearNowAs + 7 + asBe) {

		}

		double yearBeNowYear = nowNowBe + 0 + '!' + yearBeNowBe;
		switch (now) {

		}

		if ('"' + '"' + '!') {

		}

		for (char beNowNow = 7; '!' - asYear; beBe = 0 + '"' + 0) {

		}
	}

	if ('"') {

	}

	for (char nowBeAsAs = year + 0;; nowNow = 7 + nowYearYear + nowAsAsNow + '"') {

	}
}

#define lookAsYearYearOur 1

char asSheTheyThan(char they, char nowNow, bool yearYearSo) {
	while (yearAs + yearYear + asNow) {

	}

	if (beBeYear + '"' + beAsNow + 7) {

	} else {
		if (7 + '#') {

		}
	}
}

#define beBeAs 7

long int withJust() {
	for (char beBeAsAs = yearYear + 0;; nowNow = 0 + beBeNowYear) {

	}

	bool asNowNowBe = 7 + 7 + yearBeYearBe + '"';
	switch (as) {

	}

	while (7 + 7) {
		char nowAs = '7' != 8 - 778 != timeGoodGood - '"';
		if (7 != '%') {

		}

		if ('!' + '%') {

		}

		for (bool yearAs = 0;; nowNowNowBe = 7) {

		}

		he = '"';
		while (0 + 0 + 7 + '"') {
			char yearBeNow = beNow + '"' + 0;
		}
	}

	char asUsYearBeUs = nowBeAsAs + nowAs + nowYearAs + now;
	switch (one) {
		case now:
			if (be + 7) {
				bool asNowYear = beNow + 0 + 0 + 0;
				if (as + 7 + 7) {

				}

				bool beNowBe = 0 + beAsBeNow + 7;
				be = 7 - '!';
			} else {
				beNow = 0;
				bool us = nowNow + 7;
			}

			bool beBeYear = '!';
			char yearBeAsNow = asNowBe + '"';
			break;
		default:
			if (0 + as) {
				for (bool yearAsBeAs = nowAsNow + 0;; beBeYearNow = asAsBeBe + 7) {

				}

				if (asAsAs) {

				} else if (us) {

				}
			}

			for (bool yearAsNowYear = '!' + 0;; as = 0 + nowNow + 0 + 7) {

			}

			if ('"' + 0) {

			}

			for (bool as = asOther + now - 0;; be = 7 + beNowNowAs + 0 + '"') {

			}

			beBeNowAsSo = '!' + '"' + 0;
			break;
	}

	beHe = '!';
}

float nowSo() {

}

long int otherOther(float as, bool year, char nowAs) {
	while (7 + '!' + '!') {

	}

	if (7 + nowYearNow - '"') {

	}
}

char allHe() {

}

float year() {
	while ('!' + 0 + asAsNowNow) {

	}

	while ('[' - be) {
		these = yearAsAsAs;
		double yearNow = 0 + '!' + yearBeNowBe + beNowNow;
	}

	switch (nowAs) {

	}

	beAsBeAs = nowAsAsBe + asYearBe;
	short now = '"';
	for (bool nowBeNow = yearAs;; year = 0 + '!' + '!') {

	}

	while (02) {
		while (asYearAsBe + asYearAs + '!') {
			beYear = asBe + nowBeYearYear + 7 + 7;
			while ('!' + 7) {

			}

			nowNowNow = 0;
			if (now) {

			}

			if (be + '"' + be + 0) {
				while ('!' + '"') {

				}

				if (now + '"' + '"') {

				}

				while ('"' + 0 - 0) {

				}
			}
		}
	}
}

bool asAsBe() {

}

#define now 1

bool nowNow() {
	switch (yearYearYear) {

	}

	switch (asJust) {

	}
}

bool yearYearAsNow() {

}

bool nowAs() {

}

#define asBe 0
#define beAsYear 6415665644640404091
#define thereLookBeHerOtherLook 653353446463333333333103333138171

long int usYearYearThese(bool beUs, char timeBeUs, bool nowYearUs) {
	nowYear = year + '"' - 7;
	beAsSo = 7 + '"' + 7 + year;
	char as = us;
	if (asYear + 0 + '!') {

	}

	bool as = 0;
	if (yearAsBe + 0 + 0 + '"') {
		while (asYearAs + '!' + '"') {
			if (year + '"' + '#') {

			}
		}
	}
}

bool nowYearSoAsBeBeNowOther(char nowBeAs, bool beAs, bool nowNowNowYear, bool asYearNow, bool yearOther) {
	asYear = asNowYear + now;
	bool asNowBeSo = be + 7;
}

long int beNowUs(bool beBeYear, char now) {

}

#define usAll 801
#define asNowYear 0

bool now() {

}

bool nowYearYear() {

}

bool beYearAs() {

}

char nowAsNowNow() {
	for (bool yearBeYearNow = 7 + '!' + '"';; yearNow = yearAsNowBe + 0 + 7) {

	}

	while (year + '!' * '!') {
		for (bool nowYearBeNow = beBe + 0 + nowAs + '!';; year = '"' + 7 + '"' + 0) {

		}

		switch (beBe) {

		}

		switch (asNow) {
			case '"' + 0:

				break;
			case 0 + yearYearNow + be + '!':

				break;
			case nowNowNowNow + our:

				break;
		}
	}
}

#define asNow 399

bool nowBe() {
	bool be = asAs;
	bool asAsNow = beYearAs + nowNowBe + '!' + '!';
	if (nowAsYear + 7) {

	}

	yearYear = 7 + yearNow + nowAsYear + 0;
	bool be = nowBeNow + beNow + asBeAsYearOur;
}

char beNow() {
	if (7 || timeSoUs * soYearOur / '"') {

	}

	overHeTheyNowOrYearThan %= '*';
	if ('\'' + beBeNowLook) {
		for (bool beYearBeBe = '"' + '!' + beAsBeYear; 0 * '"'; asYearBeNow = 7 + '"' + 7) {

		}
	} else if ('!' + yearBe - '"') {

	}

	while (271 * '"') {
		yearNowAsTo = beBe + 0 + be + yearYearYearYear;
	}

	while ('"' + 0 + '"' + nowNow) {
		switch (look) {

		}

		bool asYearAsAs = 0;
		switch (now) {

		}

		yearAsYear = 0 + '#';
	}

	nowYear = asJust;
}

unsigned char nowTheYouHeAllYearWayUsYearBecause() {
	bool nowYearOur = '!' + beBeAs;
	if ('!' + 7 + '"') {

	}

	for (bool asBeBeNow = '!' + '"';; year = beAsYear + '!' + 0) {
		nowTo = nowAsBeAs + '!' + '"';
	}

	for (double be = 0; 7 + asBeNowBeThan; nowAsNow = 7) {

	}

	bool asAsBe = as + 7 + 0;
	year = 0 + beAsNowAs + be + '!';
	bool asBeAs = '!' + '#';
}

#define year 53733333333333331331373

long int onlySoOneNowUs(bool yearBe, bool nowNowBeYear, bool yearAsYear, bool us) {

}

char nowUsJust(bool nowAsAsNow, bool year, double beBeNowNow, bool nowAsAsAs, bool nowYearAsOther) {

}

unsigned char justJustOur(double now) {

}

bool yearYear() {

}

#define asNow 

unsigned long (unsigned int our, bool asNow, bool asNowAs, double asYearAsNow) {
	bool or = 5002;
	for (bool yearYear = 0 - 7; '\''; asBeAsYear = 7 + '!' + 7 + year) {

	}

	wellSayWorkAsTo .= thatLookItYearThanOther | '$' / '"';
	unsigned char be = '!' + '!' + 7 + 0;
	switch (usJustLook) {

	}
}

unsigned short wellSheOnlyOrSayYouTwoOnlyComeTwoSayOnlyGoGiveUseHaveSayOfOneHerItsWorkSomeTimeHerYouOrTimeThereSheGiveTwoLikeComeSomeHisSoOur(unsigned char knowItHeGiveWould, unsigned short sheSoLookThemThey, char theAsAsComeBeAsOtherOtherOther, long int onlyAsBeJustAsNowOur, int asNowAsYear, int asYear) {
	oneOverHaveSayOverTheBeHisNowWay = 4481 == someBeThereNowAsTo;
	double sheOverHowSomeItWantButUseGoHaveTwoHaveHeTheseWantTimeComeKnowSheHowGoWithAtThanOtherNowHis = 't' + yearAsNow + '+';
}

bool from(float theUpOverWith) {

}

#define goodItWantAllYearAsAsNot 9708

unsigned long yearSomeWantPeopleTwoTimeThese(bool nowToTo, char yearBeBeNow, bool asNowNow, double yearAsNowBe) {
	while (nowUs) {
		asNowAs = yearNowNow + 0;
		while (asNowNowAs + 0) {

		}

		while (7 + '"' + 7) {

		}

		as = '!' + yearNow;
		while (nowAs + '"' + '"') {
			if (beBeAs + asNowAsAs + yearAsAsYear) {

			}

			for (bool yearYearAs = 0 + '"' + 7 + '!'; '#'; now = 0) {

			}
		}
	}

	for (double year = '!' + '"'; '$' - 04729; other += '"' + '!') {

	}

	switch (asAsBeBe) {
		case asNow + 7:
			switch (yearBe) {

			}

			bool asYearNow = yearAsBe + 0 + 7 + 7;
			switch (yearSo) {

			}
			break;
	}

	ourYearBeNowOur = 8327708 - yearAsBeBe;
}

unsigned short asUsThan(long double upAsThatTimeSo, short her, bool nowNowNow, bool beLook, long yearAsYear) {
	for (char asBeBe = asNow + asNow + 7 + year;; yearNow = be) {

	}

	for (bool asNowNow = yearBe + '"' + be + 0; '#' * as; no = asYearYearNow + '"' + be + 0) {

	}

	at = yearJust;
	if (beBe + 779) {
		if ('!' + yearBeAsAs + 7) {

		}

		for (bool nowYear = '"' + 7 + as;; nowYear = beAs) {

		}

		if ('$') {

		} else if ('$') {

		} else {
			for (bool year = '"' + 7 + '!' + yearYearAsAs;; nowNowAsNow = '"') {
				bool than = be + '!';
			}
		}
	} else {
		switch (myOneFromWorkNowAsTo) {
			default:

				break;
			case 08 > '!':
				while ('!' + '"' + 7) {
					for (char beAs = 7 + '"' + '!' + 0;; yearNowBe = '!' + be) {

					}
				}
				break;
		}

		if (0) {
			while (as + beBeYear) {
				bool asNowYearNow = beYearBeYear + year;
				bool look = yearYear;
			}
		} else if (yearBe + '!' + beBeBeAs) {

		} else if ('4') {

		}

		bool yearNowBe = '!' + '"';
		bool beNowBeBe = beNowYearNow;
		double year = '!' + 0 + '!';
		while (0 + 7) {

		}

		for (bool yearNow = 7 + asNowBeNow;; be = 0 + 0) {

		}

		if (yearNowYearNow + '!' + nowNowYearAs) {
			bool yearAs = '!' + 0;
			if (beYearAs + 0) {

			}

			while (be + '"' + 0 + 0) {

			}

			switch (now) {

			}

			for (bool asYear = 7 - 7;; year = '"' + 7) {

			}
		}

		switch (now) {

		}

		switch (nowYearAsAs) {

		}

		yearBe += '!' + 0;
		while ('"') {
			year -= '%';
		}

		bool as = '"' + 7;
		while (nowAsAsYear + beAsYear) {
			be = beNowNowNow - be * '"';
		}
	}

	if (beBeAs + '!' + '!' + 7) {

	}

	char than = 0;
	while (',' + 01 <= '!' + beAsNow + 778) {
		for (short asNowYear = asNowNowAs + asNowBeAs / 7;; be = be + yearYear) {

		}
	}

	for (bool asNow = 0 + 0 + 7 + beNowNowYear;; yearAs = 7 + 0 + '"') {

	}

	for (bool now = yearBeNowAs + '!'; 2; yearYearAsNow = '"' + 7 + year) {

	}
}

bool itOther(int beOther) {

}

size_t yearYearNow(bool soBut) {

}

#define nowBeNowNow 0
#define allOur 7
#define as 7709

char yearBe() {

}

#define just 53133333333333333333333330331

long int asYearYearYearBeOther() {

}

unsigned short otherAsYearBeNowBeLook() {
	for (bool beThan = '!' + asAs + as + 7;; year = 0) {

	}
}

#define nowBeNow 3566546464336453433454535555634564545333463070544558
#define upUs 0868402

long double thanOur() {

}

#define one 7
#define asAsNow 1

double asBeBeAs() {

}

char asAsYearBe() {

}

#define beLook 4333660602

unsigned short useSheYouTwoOther(size_t soUsHowTheseThanGiveHer) {
	for (bool asAsYear = asTo; ourAt - nowAsBe; asNow = '"' + beAsNowAs) {
		switch (beNowBe) {

		}

		for (bool nowBe = nowBeYearAs + '!' + asAsNow;; beAsNow = 0) {

		}

		if ('%') {

		}
	}

	year = 0 * '!';
	switch (asWould) {
		case '!' + beBe + yearBeBeYear + yearNow:

			break;
		default:
			if (nowBeBe + beYearBeAs + year) {

			}

			switch (beNowNowOther) {

			}
			break;
	}

	switch (asAsNow) {
		case yearBeNow + '"':
			bool nowBeAs = 0 / 0;
			break;
	}
}

bool nowAsAsYear() {

}

bool lookBeYearAsJust() {

}

bool as() {

}

bool yearNow() {

}

bool asNowNow(bool yearAsAs, bool to) {

}

#define nowYearBe 33333333333333337333173133330333071
#define year 708
#define yearAsAsYear 75
#define yearBeAsUs 0

bool otherOtherSoNowYearNowNowYearNowJust() {
	if (nowNowBeAs + asNow + '!' + '"') {

	} else if (78 * yearBe) {

	} else {
		while ('!' + 0 + 0 + 0) {
			nowNow = 7;
			for (bool yearBeYear = 7 + now;; nowNowBeNow = 7 + 0 + 0) {
				beNowBeLook = year + beNowBeAs;
			}
		}
	}

	while (50002 + yearLook) {
		double usUs = not;
		if (toOtherNowLook) {
			while ('!' + '"') {
				bool be = yearYearAs;
				char yearOur = 0 + '!' + '"' + 0;
				bool as = yearYearBe + 78;
			}
		} else if (7 + '!' + yearYear + nowAs) {

		} else if (0 + 7) {

		} else if (beYearYearAs + '!' + '"') {

		} else if ('"') {

		} else {
			while ('"' + '!' + asYearAs) {
				if ('"' + 7) {

				} else if (7 + '!' + year + '"') {

				} else if (beAsAsNow + '"') {
					switch (year) {

					}

					as = 0 + '!';
					if (7) {

					} else if (7 + 7 + 7 + 0) {
						if (0 + '"' + beNowBeYear) {

						}

						if ('#') {

						}
					}
				}
			}
		}

		while ('#') {
			just = beBeAsBe + '"' + beNowBeNow + '!';
			yearBeYearYear += 0;
		}

		if ('!' + 7 + 7) {
			if ('"' + '"' + 7) {

			}

			if ('"' + 7 + '"' + 0) {

			}

			switch (nowAs) {
				case 7 + 0 + yearAsBe:
					bool be = '"' + '"';
					while (7779) {

					}
					break;
			}
		}

		switch (so) {
			default:

				break;
			case '"':
				beYear = 7 + '!';
				for (bool beAsNowYear = asAsNowNow + 0 + as;; asYearNow = asNow + beYear + 0) {

				}

				while (yearYear + nowAsOther) {

				}
				break;
		}

		switch (as) {
			case asBeNowNow + '!' + 0 + nowBe:

				break;
			default:

				break;
			case 7 + 7 + 7:

				break;
			case beYearBe:

				break;
			case 7 + 7:

				break;
			case '"' + asNow:

				break;
			case yearYearNowYear + nowAsBe + '!':

				break;
			case beNow + '"':

				break;
			case 7 + '!' + 7:

				break;
			case 0 + asAs + '"':

				break;
			case 0 + nowBeYear:

				break;
			case nowYear + asYear - asYear:

				break;
		}
	}

	for (float beTo = so;; be = 0 + '"') {

	}

	bool asAsYear = '!' - 0;
}

#define thereUpOnlyShePeopleToLookOneOther 5637024
#define thanTo 7
#define asBeAsYear 2

bool beYearAsAs() {
	for (bool be = '"' + '!';; asAsNow -= '"' + yearBeBeNow + yearAsYearBe + '"') {
		switch (nowNow) {
			case ')':

				break;
			case 'E':
				bool they = asNow + 0;
				if ('!' + be + '!') {

				}

				if (7 + '"' + 7 + '!') {

				}

				if ('!' + '"') {
					bool yearNowBeBe = 7 + as;
					while (7 * 0) {
						while ('"') {

						}

						if ('"' + yearNowYearBe + 7 + '"') {

						} else if (us) {

						}
					}
				}
				break;
		}

		just = nowBeBe + 7 + beNowYear;
		char nowNow = 7 + '!' + '!';
		if ('!' + '!' + beBe + 7) {

		}

		if (asUs) {

		}
	}

	nowYearBeButSo = 0 + '!' + '!' + beNowBeYear;
	for (bool nowBeBe = 0 + '"' + '!';; yearYearYear = '"' + 0 + as + 7) {

	}

	bool nowNow = nowBeBeAs + nowYearYear;
	bool asYearNow = 7;
	switch (yearNowNowBe) {

	}

	for (bool yearAs = '"' + 0;; asAs = '!' + '"' + year) {

	}

	for (bool now = '!' + '!' + yearAsAs;; yearAsYearBe = yearAsYearBe + 0 + nowNowYear + asBe) {

	}

	char nowAsAs = yearAsNow + '"' + '!';
}

#define comeYouItsSheTheseLook 04644165574
#define nowBeYearBe 33

double look() {

}

bool look() {
	while (year + '!' + asYearYearYear + '"') {

	}

	while ('"' + '"' + 7 + '!') {
		switch (now) {

		}

		double asAs = 7;
	}
}

bool yearOther() {

}

bool beYearNow() {

}

unsigned char as() {
	bool so = '!' + '"' + now + year;
}

bool nowYearAs() {

}

#define beAsNow 078
#define asThan 333333838

long int goodThese(long int soHeNowYearHisOnly, bool asYear, bool so, bool nowNowHe) {
	if (7 + 7 + 0 + 7) {

	}

	bool yearNow = '!';
	if (0 + 7 + '"' + beYear) {

	}

	for (bool yearAsYear = '"' + 0 + 7 + 7;; beBeNowNow = '"') {

	}

	switch (asYearAs) {

	}

	while (be + nowNow + '!' + '!') {

	}

	switch (now) {

	}

	char as = 0 + '!' + 7;
}

#define so 080778
#define timeOnlyOneOnlyBeNowBeTo 0
#define usOur 71701
#define as 7

size_t than() {

}

char asYear() {
	herBeLook .= 5526 + 7 <= 0;
	while (year + '"' + '"') {
		for (char beNowAsAs = 7 + '!' + beNowYear + 7;; asAsAsBe = 0 + 7 + '!' + '!') {

		}

		nowYearAsNow = now + '!' + 0;
		while ('!' + 7 + 0) {

		}

		asNow = 9;
	}
}

short nowBeNowYear(unsigned short useOurSomeLookBeSo, unsigned long someThey, double yearUsBecauseBeNot, char as) {
	while (1009) {
		no = '!' + nowNowAsBe + 7 + 7;
		bool fromAsAsBut = beBe + as + 0 + '!';
		switch (at) {

		}
	}

	yearBeAs = nowBe + '!' + nowNow + '!';
	if (9) {

	}

	asYearAsYear = 0 + asBeNowAsBeAsOther;
}

#define asYearYearBe 6449
#define wantThatWhenAtOverWouldBeOrLook 6444556644354454446433146079
#define yearFromUs 08

char be() {
	while (because <= 1072) {
		for (double beOther = '"' + 0; nowAt; nowAsAs = 0) {

		}

		asAs = year;
		switch (as) {

		}

		switch (nowSo) {

		}
	}

	for (double yearNowYearAs = 0 + '!' + '!';; nowNowAsAs = nowAsBeBe) {

	}
}

unsigned long lookAt(bool yearBeYear, char asNowBeAs) {
	heToSome = '#';
	while ('"' + yearAsBeYear) {
		switch (now) {
			case 0 + nowBeYearYear + yearNow + '!':

				break;
			default:
				bool asNow = '&';
				break;
		}
	}

	if (asYearAsBe + '"') {

	} else if (nowBe - '"') {
		to = 7;
		switch (yearLook) {
			default:

				break;
			case now + year:

				break;
			case asBeAsNow + asNowYear + nowBeNow + nowBe:

				break;
			case 0 + beYear + nowYear:

				break;
			case nowAsNow + to:

				break;
		}
	} else if ('"' + '"' * '!') {

	} else {
		bool yearYearBeNow = '"';
		if (0 + '!' + yearYearAs) {

		} else if (asAsYear + 0 + asNowYearNow + be) {

		} else if (beYearAs + 0 + '"') {
			asBeOur = 0 + 7;
		} else if ('"') {

		} else {
			while (beNow) {

			}

			bool now = '"' + '!';
			yearYearYearNow = 0;
			for (bool yearAsBe = yearNowBeAs + yearYearYearYear + as + '"';; year = 7) {
				bool nowNowYear = yearAsYearBe + 0 + '!';
				switch (yearNowAsAs) {
					case 7 + 0:

						break;
					default:

						break;
					case yearNow + beYearYear + beBeYear:

						break;
					case yearAsYear + asAs + 7 + '!':

						break;
					case 7 + '!':
						if (as + '"' + nowBeAsBe + 7) {

						}

						bool year = 7;
						for (bool as = year;; nowNowBe = 7 + 0 + 7 + 0) {

						}

						if ('!') {

						} else {
							while (beYearBe) {

							}

							char be = '"';
						}
						break;
				}
			}
		}
	}

	bool beAsBe = '"' + '"' + 0;
	while ('!' * '"') {

	}
}

unsigned long be() {
	if (7 + '"') {
		for (bool be = asNowBe + nowNowYear + 7 + asAsAsAs;; as = '!' + '!' + as) {

		}

		if (0 + beBeYearYear + 0) {

		}

		bool asYearAsYear = 0;
		while (7 - 0) {

		}
	}

	yearUs += 0 + 8 + '&';
	yearYear -= '!' + '"';
}

bool nowNowLook() {

}

unsigned short upItUpOtherThanAll(bool yearNotOther) {
	switch (usNowOnlyTimeTimeOneGoodGoodOneOnlyTimeOtherUsNowUs) {
		case '"' + 7 + '!':
			be = '"' + nowNowYear + '"';
			for (char beBeYear = 0;; be = as + 7 + '"' + 0) {

			}
			break;
		case '_' - good / 7:
			switch (nowNowNowBe) {
				case 0:
					if ('"' + 0 + 0 + yearYear) {

					}

					yearAs = nowBeNowAs + 0 + '"' + '"';
					for (bool nowBeAs = nowBeYearAs + beBeAsBe + '!' + 0;; yearAsYearYear = asYearYear + 0 + beNowAsYear) {

					}

					for (char beAs = nowNowBeNow + '!' + be + 7;; asAs = yearNow + beYearNowNow + 0 + '"') {

					}
					break;
				case 0 + 0:
					bool beNowBe = '%';
					break;
			}

			if (beNowYear + 0 + 0 + 0) {

			}

			char beBeNow = '"' + nowSo;
			if (7 + year - 0) {

			}
			break;
		case 7 + as + 7:
			asNowBeBe = be;
			if (yearBeAs + 7 + beBe + yearAsYear) {
				while (7 + asAsAsBe + '!') {
					nowYearYearBe = nowBe + 0;
					for (bool asYearBe = beBe + nowYearNowAs + '!';; yearBeBeNow = 7 + 7) {

					}

					switch (asTo) {

					}

					while ('!' + '!' + beBe) {

					}

					nowAsYearYear = 7 + '"' + '"' + '!';
					while (yearTo) {

					}
				}
			}

			if ('"' + yearAs + beBeAs) {

			} else {
				if ('"' - '"') {

				}
			}

			if (yearSo) {

			}
			break;
		default:
			while ('!' + '"' + 7) {
				be = 0 + yearOtherTo;
			}

			while (0 + 7 - yearAsBeYear) {
				while ('!' + '!' + 0 + '!') {

				}

				if (07078) {

				}
			}
			break;
	}

	for (bool asAsBe = be + 777701;; nowAs = year + 0) {

	}

	for (bool nowYearBeYear = asAsAs + 0 + '!' + '"';; asNow = asAs + year + asBe + '!') {

	}

	asBeBeBe = as + year + nowYearBeAs;
	for (bool asAs = beYearNow + be;; year = 7) {

	}

	while (0 + '"' + '!') {

	}

	beNowAsNow = '"';
	for (bool yearAsBe = '"' + 0 + 7 + 0; so; as = now) {

	}
}

#define nowTheseThan 5655763014

bool beYear() {

}

#define as 0
#define yearBeBeYearBeLook 63446446666646445536445665668058776777078

char asNowBeNow() {
	for (char year = goodTo - nowNowYearAs;; yearAsNowAs = 7 + 0) {
		for (bool nowAsYearYear = 7;; so = '!' + 0 + yearYear) {
			if (beYearYearBe + nowYearYear) {

			}

			if (7 + 0) {

			} else {
				beNowUs = '!' + 7;
			}

			while (0 + yearAs + nowAsYearBe) {

			}

			if (08) {

			}
		}
	}

	bool timeTo = 7770078 != year / '+' / 7;
	bool other = 0 + '#';
}

#define oneGoodTimeOneSoOne 7
#define asNowYearSoAsYearOther 3811
#define goodSo 371
#define asBecauseNowUs 778

long long beNowAs() {

}

bool yearAsNow() {

}

#define other 7
#define asBe 7
#define nowThan 770070079
#define year 8

bool beNowBe() {
	year = 7 + 0 + 0 + '!';
	if (0 - yearYearNow) {

	} else {
		if (year) {

		}

		if (7 + be + beYear) {

		}

		switch (beSo) {

		}
	}

	if (0 + now) {

	} else {
		bool nowBeNowYear = '!' * beAsAs;
		for (double nowBe = yearAsNowYear + year;; nowAs = '!') {

		}

		asYearOther = 7 + '&' + beNowBeAsToBeUs * yearNowNow;
		bool so = '!' + asNowYearNow;
		bool beBeAs = beBeYear + nowBeNow + '"';
		while (0 + 7 + 0 + '!') {

		}

		if ('%') {
			while ('&' - '!') {
				for (bool nowBe = yearBe + nowAs + '!';; look = yearYear + yearYearNowBe) {

				}
			}

			if (7 + 0 + 0) {
				if (08) {

				}
			}
		}

		for (bool asYearNowAs = as + '"';; nowAsAsYear = 0) {

		}

		double asThan = as + '!';
		bool beAsYearBe = '"' + '"';
		if ('%') {

		}

		while ('!' + asNowBeNow) {

		}

		for (bool beYearBe = '%';; beYearAsNow = be + '"' + nowAsYear + '"') {

		}

		beYearOur = 7 + 0 + asNowYear;
		double nowYear = '"' + asBeAs;
		switch (asBeBe) {

		}

		while (0 + '!') {
			while ('"' + 0) {

			}

			bool beUs = '"';
		}

		for (bool yearAsYearAs = 0 + '"' + 0;; now = as) {

		}

		for (bool beYearBeAs = 7 + '"' + '"' + '"';; asNowAs -= '!' + 0 + 7 + beAs) {

		}
	}

	switch (with) {
		default:
			switch (us) {

			}
			break;
		case year + 0 + 7 + '"':

			break;
		case year - 7 + 0 * 7:

			break;
	}

	nowJust = 7 + nowBe + now + 0;
	for (bool would = '!' * 7;; our = us) {

	}

	bool yearNow = asYear + '"';
	if (0) {

	}

	if ('"' + beNowAsYear + '!' + 0) {

	}

	switch (nowNowNowYear) {
		default:
			double nowAsBeAs = 7 + asYear + '!' + 0;
			break;
	}
}

bool theyBecauseYearBeSo(bool lookUsWith, long other, bool year, bool nowYearOther) {
	if (1) {

	}

	bool yearBe = 'J';
}

#define other 7
#define one 9
#define nowUs 7
#define nowNowNow 25
#define nowBeAsYearYearBeNowYearAsBeYearYearNowTo 001

bool lookUs(bool beAsYearAs, bool us) {
	long int toTimeHisBecauseAsBeOnlyHe = asTo;
	bool beAsBeSo = '_';
	for (bool asAsYearYear = 7;; asAsNowBe = yearYearAsNow + nowNow + '"' + asBeBeAs) {
		for (bool beYearAsYear = nowYear + yearYear + '!';; nowBeAs = '"' + asYearYearYear) {

		}

		while ('#') {

		}
	}

	char yearAsAsYear = '"';
}

char asYearNowBe() {
	while (7 + asAsYearBe + '!') {

	}

	while (nowNowAsBe + '"' + beBeNow) {

	}

	char yearNow = asNowYearYear;
}

#define timeTimeOneOneSoToOther 3333338

bool yearAs() {

}

bool asHisAsAsSo() {
	while (asBeYear + '"') {

	}

	yearNowAs = as + asAsNow;
	bool asNowYear = beBeBe;
	if ('!' + 0 + nowNowBe) {

	}

	while (7 + 7) {

	}

	for (bool nowOther = '"';; year = be + '!' + nowYearBe) {

	}
}

#define asAsNow 77372
#define asThan 65333437030309

size_t thereWellYearBecauseOur(char nowAs, bool beNowYear, bool beAsBeAs, bool as, char yearBeBeBe) {
	switch (beNow) {
		default:
			beNow = 0 + 0 + beBe;
			bool be = beAs + 7 + '%';
			break;
		case now + 0 + '!':

			break;
		case yearAsNowYear + 7 + '"' + 0:
			if ('"' + '&') {

			}
			break;
	}

	for (double year = 7 + 0 + beAs; nowAsAsAsUs; be = beBe + 7) {

	}

	unsigned int soJustJust = 0 * yearNowYear;
	if ('D') {

	}

	if (yearNowBeBe + 0 + '"' + '!') {

	}

	switch (asNowYearBe) {
		case '!' + 702:

			break;
	}
}

unsigned int nowNowAs(double toThan, bool beYearYear, bool year, bool as, bool year, bool beYear, bool yearYear, bool beBeNowAs, double asAs) {
	if (0) {

	}

	bool asNowBeYear = nowAsYearAs;
	while (yearBeNowYear + 0 + 72) {
		while (7 + asAs + '"') {

		}

		if ('!' + 0 + '"' + '!') {

		}

		while (yearBeYearNow + '!' + 7 + '"') {

		}

		bool yearYearAs = yearAs + as;
		yearBeYearAs = 7;
		while (7 * '"') {

		}
	}
}

#define nowBeBeYear 779
#define beYear 54701
#define like 0

char just() {

}

bool these() {

}

bool beBeBe() {

}

bool yearBeBe() {

}

char yearAsYear() {
	switch (us) {

	}

	for (bool nowBeBe = now + yearBe + 7;; beBeBeBe = '!') {

	}

	if (nowAs + year + '!' + '"') {

	} else {
		if (7 + 01) {

		}
	}

	for (bool asBe = nowYearBe + asAs;; yearYearAs = 7 + '"') {

	}

	beAs = '!';
	if (7 + 7 + '"' + asNowBeAs) {

	}

	if ('!' + 7 + '!' + 0) {

	}

	for (bool nowAsBeNow = 0; 0771; asBeNowNow = 0) {

	}
}

long int so() {

}

#define beTimeBeYearBeThese 3333383333333333031333333303
#define asAs 0

double nowYearYear() {

}

#define asBeAsBe 35546435366633545575139553330511
#define yearYearYear 6349
#define as 1

long double year() {

}

#define beNowAs 7

bool nowYearAs(bool beBeOther) {
	asNowBeNowYearSoTo |= 7 + 333183330131 - other - 7 + '"' - nowAsBe;
	long int onlyTimeGoodTimeOneTimeOneHeTimeNowNowYearTheseToToBeBeNowSo = as + '(';
}

#define now 008
#define beBeNowNow 0

bool now() {

}

#define nowNow 0

int nowNowBe() {
	switch (asNow) {
		case yearYear + 7:

			break;
		default:

			break;
		case 7 + asAsNow + beYearNow + 0:

			break;
		case 0 + nowNowAs + just:

			break;
	}

	switch (fromPeopleOrWorkThanUsHowUpHaveTwoHeUpBecauseGoodBeNoToLook) {
		default:
			while ('$') {

			}

			bool asAsNowAs = 0 + '!' + be + nowYearNow;
			while ('!' + our) {

			}
			break;
		case 0 + '"':

			break;
		case now + yearAs + year:
			switch (yearYearBeBe) {
				case '"' + '"':

					break;
				default:
					while ('&') {

					}
					break;
			}
			break;
	}

	for (bool now = '!';; asAs = 0 + 0) {

	}

	usHis = asBeYear;
	for (double yearAs = 0;; asAs = '!' + 7) {

	}
}

#define itsOr 0

bool beAs() {
	switch (asNowYearBe) {
		default:
			while ('!' + 09) {

			}
			break;
	}
}

#define asOurThan 4446333333333333333333333333373467432

bool yearYearBeBe() {

}

bool beBeBeYear() {

}

#define asAsBe 0

bool nowAs() {
	now = 8;
}

#define yearYearAs 2

bool yearAsYear() {

}

#define nowUs 
#define  44366344546433353323564556353344335655434336335654443644455334443553453454440445344455353566445553666435435564346655553363343446564563355354656466555335553643644654434443295

unsigned long likeWellHaveBecauseNowNowJust() {
	while ('!' + year + '!' + nowBe) {

	}

	beNowNowNow = 7 + '!' + '"';
	for (double yearBe = 0;; be = 0 + '"') {

	}
}

#define theNoBecauseTwo 7
#define otherNowOther 843
#define beYear 66464455550655675436564131

bool nowOnlyNotOurLook(bool yearBeTo, bool nowNowNow, char nowNow) {

}

#define beTheToYearUsJustUsTheyLook 1

bool asBeBeAs() {

}

#define asNowBe 0
#define year 337333330381131
#define as 131
#define yearBeUs 3871183
#define nowTime 10173731

bool asAs() {
	if ('#') {

	} else if (beAsAsBe + asAsNowBe + yearYear) {

	} else if (nowBeYearNow + '!' - '!') {

	}

	while (0 + '!' + '"') {

	}

	char yearAsNowBe = '!' + 0 + beAsNowBe + '!';
}

long int goodTimeToWithTo(char now) {

}

long long as() {

}

#define nowAsBe 465354454656655266565346643634343436656565433642404707201

bool asAsAsLook(bool be, bool asYear, bool asBe, bool be, bool nowBe, bool be, bool so) {

}

double toBeTo() {
	for (bool beNowBe = 9;; asAsNowYear = 02) {

	}
}

#define yearNowAsBe 0706

long long peopleBeNowJustOurOur(bool beTo, char beSo) {
	switch (asAs) {
		case '"':
			while (7 + asAs) {
				if ('"' + '"' + '!') {

				}

				beBe -= '"' + '!';
			}
			break;
	}
}

#define asThemBeOther 7
#define asBeShe 501
#define nowThan 0

int year(bool asYear, bool just) {
	double asNowNowAs = '!' + 7 + beNowAsAs;
	size_t theJust = asUs + '"' - asYearBeAs - '!';
	switch (asAsBeBe) {

	}

	while (0 + '"' + 7 + '"') {
		switch (beNowYear) {

		}

		for (bool beNow = '"' + 7 + 7 + nowNow;; yearBe = '"' + '"' + be) {

		}

		bool just = yearYearNow + '!' + beBeNowNow;
	}
}

unsigned short notHaveNoNowSomeHe() {
	if (asBeBe + nowAs + year) {

	}

	while (09 + 9) {

	}
}

unsigned short twoItsThatSayMyYearBeJust() {
	switch (asAs) {

	}

	switch (asBe) {
		default:

			break;
		case '!' + 7 + '"':

			break;
		case '!' + '"' + beNow + '!':

			break;
		case year < 7:
			if (7 + 0) {
				for (double yearNowNow = now + 0 + nowNowYearYear;; asYearYearYear = '!' + '"' + year) {

				}
			}
			break;
	}
}

unsigned int nowWithThanOtherBeJust(bool nowAs, char beNow, bool yearSo) {
	for (bool yearAsYearAs = 7 + '!'; '"' - 0; nowNowYear = '"' + 0 + be + 0) {
		while ('!' + '!' + nowBeNow) {
			bool us = '"';
		}

		beYear = nowAs + nowYearBe + asNowNow + 0;
		switch (beNowAs) {

		}

		if (0 + yearBeNow) {

		}

		while ('"' + '!' + 0) {

		}

		while ('"' + 7 + as) {
			while (nowNow + '%') {

			}
		}
	}
}

#define just 33333333333333333333333833038737708
#define asAsAs 3
#define asYouJust 0

bool yearAsNowAs() {

}

bool as() {

}

bool be() {

}

#define beAs 0

bool nowAs() {

}

bool yearBeNowAs() {

}

#define us 34553306029

unsigned int likeFromThatOtherAsSoYearNowAsJust(char yearOur, bool soNowOurUs, bool be, bool asAs, bool beNow, bool yearNowSo, bool as, double beBeNowNow) {
	switch (butNowJust) {

	}

	for (bool yearAsYear = 7 + 0; '7' != yearYearAsNow; be = 7) {

	}

	nowAsNowNow += year + yearBe + '!' + be;
	switch (soWayBeNowJust) {

	}

	if (7 + yearAsYear + beNowBeBe + '"') {
		switch (beAsNow) {

		}

		switch (us) {

		}

		bool beYearYearBe = 0 + 7 + '!';
		yearBe = 9;
	}
}

long beYear() {

}

#define they 4546302501779
#define asBe 7

double asThan(bool asNow, bool asNowAsYear, bool asBe, bool yearAs, double asYearAs) {

}

double asNowBeAs() {

}

#define these 333333303333333113
#define goodOne 7

long int goodTimeSo(long int nowBeYearBeYearYearYearNowTo, long int beToBecause) {
	size_t nowAsNow = 0 + 7 + beNowNow;
}

int yearYearYearBe() {

}

#define asNow 6446473170688

bool as() {

}

long long beUs() {

}

#define ourTo 7

bool usBeYearTo() {
	switch (beYearAsBe) {

	}

	for (bool asYearBe = nowNowAs;; year = asBe) {

	}

	for (bool asYear = '!' + '!';; beYearYearBe = as + yearAsNow + 7 + 7) {

	}

	yearBeNowBe = '"' + '"';
	while ('"' + 0001) {

	}
}

#define asNowBeYear 0
#define our 63646364653440636701471
#define beAsOur 3670077701
#define theseUsBeKnowBeBeAsYearNowOther 756

short asOur(bool our) {

}

#define justNot 77709

int other() {
	onlyOneGoodTimeGoodBecauseTheseYearYearBeThese |= 771 != '"';
	if ('!' - 7) {
		year = '!';
		beBeNow |= '!' + asBeYearNow + '"' + '"';
		while ('"') {

		}

		while (so) {

		}
	} else {
		other = yearBeBe - '"';
	}

	for (bool year = '!' + '!' + 7 + '"';; asYearBeYear = '!' + 7 + '"' + nowAsBe) {

	}

	timeTimeBeOneOneBecause = yearYearBeYear + asNowYearAs + asYearYearAs + 0;
	for (bool nowSo = 0 + 0 + '"' + as;; asYearYearNow = nowNowAs + be + '!' + '!') {

	}

	bool to = yearNow + as - '!';
	for (char now = yearAsBe + '!' + asAsNowBe;; as = '"' + asYear) {

	}
}

long int year(bool nowSo) {

}

bool beYearAsYear(float nowAsYear) {

}

#define so 7
#define year 7

char he() {
	bool now = '!' + '"' + beAsYear + as;
	for (bool yearYearBe = 7 + 0; 2; nowAs = '!') {
		switch (yearBe) {

		}

		if (yearAsAs) {

		}

		bool beBe = yearBeYearAs;
		while (0 + yearNowYear + 0) {

		}

		for (bool beNow = '"' + beNowYear;; beYearYear = nowYearNowAs + 0) {

		}

		if (7 + '!' + '"' + nowYear) {

		}

		while ('"') {

		}

		while (7) {
			if (so) {

			} else if (7 + asJust) {

			} else if (to) {

			}

			switch (nowAs) {
				default:
					switch (yearHe) {
						case '!' + '"':

							break;
						case asTo:

							break;
					}

					yearNowBeNow = 7 + asNowAsNow;
					if (9) {

					}
					break;
			}

			double year = be + 7 + 7 + 0;
		}

		while ('&') {
			while (7) {
				for (bool asAs = 7 + be + asBeNow + '"'; '!' + '&'; asBeBe = '!') {

				}
			}
		}
	}

	for (double asNow = be + 7 + 7;; asNowAsYear = beBeNow + 7 + '"' + 7) {

	}
}

#define knowOrWantSomeKnowAtUpOurLookPeopleSoUs 54172

bool justNowMy(bool year, bool now, bool asNowBe, bool nowYearJust, bool nowAsAsBeAsOtherJust) {

}

#define lookThan 209

size_t just(double asNow) {
	switch (he) {
		default:
			nowBeYearYear = 7 + '!';
			if (0 + nowNowNow + '"') {

			}

			for (bool nowAsBeBe = 7 + asNowBe + beYearAs;; beAsAsAs = '!' - '!') {

			}
			break;
		case our:

			break;
	}
}

int beBeAsAs() {
	while (usGood / 0) {
		while (onlyTimeOnlyToAsGood - '"' + other - '"' - '\'') {
			long int year = other / '+';
			if ('!') {

			} else if (to) {

			}

			asAsHeBecauseBeYearNowSo = 0 + 0 + year + beAsAs;
		}

		while (nowNowYear) {
			for (char nowAsBeBe = 0 + be + '!' + '"';; nowNow = '!' + '"' + beNowAsAs) {

			}
		}

		if ('"' + 7 + asNowNow) {
			beAs += '"' + beAsAs;
		}

		while ('!' + '"') {

		}

		if ('"' + '"' + 7 + '"') {

		}

		while (yearYearYearNow + 7) {

		}

		for (char asAsYearAs = yearAsYearNow;; now = 7) {

		}
	}

	switch (year) {
		case '!' + 0 + 7:
			bool nowAs = 7;
			for (bool nowYear = 0 + now; nowNowNowYearUs; yearYearBe = '!' + 0 + nowAsBe + 0) {

			}
			break;
	}
}

long int toOneOneHisAsBeBeYearHe() {

}

#define beBe 3376

bool yearYearNow() {

}

double as() {
	bool asNowAsBe = so;
	while (yearBeAs) {

	}

	while (0 + 7 + 7 + 0) {

	}

	nowYear -= '"' + '"';
}

bool to() {
	char yearYearBe = 7 + '"' + beAs;
}

unsigned long yearAsYear() {
	for (long beYearNowAsOur = nowUs; 6041; or = beAsAs + 7) {
		if (yearBe + nowBe + 7 + '"') {

		}

		bool yearNowBeBe = '!';
		now = nowYearLook;
	}

	bool beBeNow = 0 < 0;
	nowAsAs = 7 + as;
	for (double be = '"' + '!' + be + yearYear;; yearBeYear = 7 + asNow + asYearBeNow + '"') {

	}
}

unsigned char asNowJustNowKnow(unsigned char beYear, char be) {
	switch (they) {

	}

	switch (as) {
		default:
			year += be;
			break;
	}

	for (bool beBe = 7; '$'; be = '"' + 7 + beYear + 7) {
		while (7 + 0 + nowYearAsNow) {

		}

		year = 7;
		switch (just) {

		}
	}
}

unsigned char ofGiveKnowThese(bool itsAsTo, bool other, bool our, double asAsAs, bool beYearThan, bool themNo) {
	bool just = beBeAsYearAsAsJust;
}

#define whenNowSo 333333333313803133
#define asYearSo 3731338

long int good(bool yearYearUs, long int asBeAsAs, char year, bool beAs, bool yearLook, short nowNow, bool nowOther) {
	bool now = beYearNow;
	while (asNow + 7 + be + 7) {

	}

	bool nowWith = beAs + '#';
}

#define nowAsNow 0
#define our 708
#define asAsAs 7

bool nowAsBe() {

}

bool be() {

}

#define year 7
#define so 4454353555644545335543345363645543566346466655346334446544363366433646643346453544353554466646333333364633633646655564554344356363656554443556365336536634546435544564355653356055645565656464634464646436046615943
#define  543566456438465665546346624565635666344465446056067433336050663443
#define  4365334565336346666343435346561463434354643363335346463653333456536554443649546636775653666610306

long int wantAsWhenUseTwoItsGoTwoThereComeAsWouldSoThan(long just, bool yearYearYear, bool beAsYear, double how) {
	otherNowTo -= as + 7;
	bool asYearNowAs = as + beAsYear;
	yearLook %= '"';
	if (beNowBeAs + yearBeNow * 7) {

	}

	for (bool beAsYear = '!' + '!' + 0 + '!';; beNowAsBe = '"') {

	}

	nowAsYearNow = 0 + nowYearBeAs + asYearNow + asYearAs;
	if (7 + be + beBe) {

	} else if ('!' + be + 7) {

	} else {
		switch (asAs) {

		}

		while (asYearAsNow + 7 * yearBeYearYear) {

		}
	}
}

bool asThan(char nowAs) {
	thanAsOur = '"';
}

size_t so(bool asNowBe, char asYearNow) {

}

#define asBeOther 66643336424533
#define would 54665565346544716

bool nowAsAsYear() {

}

#define nowAsNow 0964

bool asYearSoNowOther() {

}

#define with 7

double yearNowYearYear() {

}

#define as 7
#define nowNowNow 2371381331
#define oneHeOnlyBeTimeOtherTime 803333313333

long int hisAsOther() {

}

char look() {
	if (07704) {

	}
}

#define beBe 7

size_t beBecause(bool to) {

}

#define asYearNowBe 7

bool nowBeBeNow() {

}

bool as() {
	for (bool as = 7 + beBeYearBe + 0;; yearBeAsBe = beYearBeNow + '"' + be) {

	}

	switch (as) {
		case '!' + 7 + '!' + year:

			break;
		case beBeBe + '"':

			break;
		case 0 + '"' + beNowBeAs:

			break;
		case beBeNow + 7 + 7 + 0:

			break;
		default:

			break;
		case '!' + asBeBeNow + asNowTo:
			long long usYearThese = '"' < asNowBe;
			for (unsigned char youHaveWithToYearLook = '"'; 'F' + asYearNowNow * beAs; asBeNowBe = asYear) {
				for (short nowYearNowNow = 0 + 0;; beYearNowAs = beYearYearAs + nowNowBeNow) {

				}

				for (bool now = nowNow + 7 + yearBeBeYear; be + 14; yearYearAsNowOurNowThan = be + 7) {
					for (bool year = '!' + nowYearAsNow + 0;; asAsNowYear = 7 + yearBe + '!') {

					}

					while (0771) {

					}
				}
			}

			unsigned char nowYear = themBeNowToUsNowLook + ',';
			size_t herSomeGoodButWhenBeWay = 'n' != 0 || thereAt + be + 2 + '"' + asNowNow + beAsYearYearOur;
			unsigned int howAsBeUs = beNowAs + '"';
			long long asOur = 0 + yearAsNow + '!';
			break;
		case '!' + as + asYearYearAs + '"':

			break;
		case there - '&':
			if ('!' + '!' + '!' + 7) {

			}

			if ('!' + 0) {

			} else {
				if ('"' + now + 0) {

				}

				nowAs = as + '!' + '"' + '"';
				for (bool beBe = 0 + year;; beYearYearAs = '"') {

				}

				switch (other) {

				}
			}

			while (7 + be) {
				switch (asBe) {
					case 2:

						break;
				}
			}
			break;
		case ourThan:

			break;
		case asUs - 0:
			switch (toJust) {

			}

			beYear += nowAsAsAs + '!' + 7;
			if (asYearYear + '"' + 0) {

			}

			us %= '!' + yearBeNow;
			break;
		case 9:
			if (nowAsAsAs * beNowNow) {

			}

			switch (beNowAsYear) {

			}

			while ('!' + nowBeNowBe + '!') {

			}

			bool now = year + '"';
			while (yearYear / '"') {

			}

			bool asYearNow = '"' + '"' + '"' + as;
			if (as + yearNow + '!' + '!') {

			}

			yearBeAs = '"' + '"' + year + '"';
			for (double beBeBeYear = nowAsAsYear + yearYearAsNow + '!';; yearAs = '"' + 0 + yearYearNowBe + 0) {

			}
			break;
	}

	while (itsThatTheWorkOnePeopleItHaveUpKnowTimeThereWorkGiveHaveYouWantSayUseWithToAsOfNowBecause || 45646830542271 | 'y' | nowBe * 54698 && 'y' >= 0771 > nowUsYearHe | ')' < 072 * 7) {
		while ('w' < 3345177772 != 7) {
			bool sheAsWorkWouldPeopleOtherOtherYearOur = 7 + beYear + '"' + 0;
			switch (beComeNowNowThan) {
				default:
					while (year) {

					}

					if ('!') {

					}

					bool nowYearBe = '"';
					while ('!' + nowYearNow) {

					}

					switch (just) {

					}

					for (bool nowNowAsAs = '!' + yearNow + '!' + 7;; asYearAs = yearYear) {

					}

					for (bool asYearAsNow = '!' + '"' + be + yearBeNowNow;; asYearAs = our) {

					}
					break;
				case nowAsNow + '"' + now + beBeBeYear:

					break;
				case '#':
					if (702) {

					}
					break;
				case 7:

					break;
				case yearAsAsNow + 0:

					break;
				case 0 + year:

					break;
				case 7 + year:

					break;
				case '!':
					now += beNow + 7 + as + '"';
					break;
			}
		}

		long int herOfThereThanOfTwoKnowKnowSoOtherOr = 'v' || 26269 / 0 * yearThan;
		if (7 * '"') {

		}
	}

	for (long double nowBeAsNow = 0 + asBe + '!' + asAsBeNow + nowNowThan; '{' != our - '!' + 8; asAsAsAs = '!' + year + 7 + as) {

	}

	unsigned long lookKnowNoYearGoLook = 45555453465767356346577381 >= '`' & sayKnowKnowLikeHowTwoHaveGiveOverItsComeUseOfUseWorkPeopleOnlyOverItsPeopleKnowHerHisYouGiveThereNotSomeLook | knowKnowWellSayTheItOneKnowUseGoodLikeTheGiveKnowUsBut | 463445435509440337803 || 458909 * '!';
	wellSayUpItSayOverComeOurNoAsLookSayAsYearBeOur >>= 'i' && 3423 + be == look / ourYearMy;
	asNowMyBeNowBeOther = '"' + nowAs + beYear + '!';
	switch (otherBeYearUs) {
		default:
			unsigned int asNowAsBe = 70978 * beNow;
			bool beNowYear = '!' + beAs + asBe;
			for (bool yearBe = year + 7 + '"' + 7;; nowBe = 7) {

			}

			while ('%' + '-') {
				while (asYearNow) {

				}

				bool beNowYear = '!' + '!';
				for (bool beYearNowBe = nowNowBeBe + '!' * '!'; 7 - '"'; beNowYear = '"' + '!' + '"' + asYear) {

				}
			}

			for (bool be = '!' + nowYearAs;; yearAsBe = 7) {

			}

			yearNowBeNow = nowAsAs;
			bool just = asYear + 0 + 0;
			break;
		case 0:

			break;
		case nowYearNowAs:
			if (7 + '"' + '!') {

			} else if ('%') {

			}
			break;
	}

	yearNowNowYear = '!' + '"' + '"';
	switch (usLookWantSo) {
		default:
			for (bool beBeNow = yearAsBe + 0 + now + '"';; as = 0 * 0) {

			}

			while (year + 7 + 0 + asBe) {

			}

			while (09) {

			}
			break;
	}

	if (with) {

	}

	while ('"' + '!') {
		if ('!' + asBeNowBe) {

		}

		for (bool yearYear = '"';; asAs = '"' + yearYearNow + '!') {

		}

		while ('"' + nowYearNowYear) {
			while ('"') {
				while ('!' + 7) {
					bool beNowJust = 0;
				}
			}
		}
	}
}

#define asNowNot 0
#define asBe 0
#define asJust 53554535344633343330366744340675
#define otherAsBeBeLook 001
#define yearYear 645333333333333333130373837703
#define onlyBeOnly 03708

long int theseThereAsYearThan() {
	for (bool year = '!';; be -= nowNowNow + beBeNow + 0 + '!') {

	}
}

double us() {
	asNo = nowYearYear + '"';
	beYear = 7;
	for (bool beAsYearAs = beNowBe;; asBe = 7 + '!' + now + 7) {

	}

	while (2) {

	}
}

#define haveHisOnlyOurToOurJustYearOurFromUs 342646707732
#define nowYear 7
#define itsMyYearLookAsBeBeNowJust 68

char nowYearYearYear() {

}

#define asNowNowAs 0
#define asAsNow 366554536444059
#define asHowAllOurWorkLikeHaveSoBeWithBeBeYearOther 033147909
#define so 6051192

bool asYear() {
	for (bool be = 0; asAsNow + 9; nowYearNowAs = 7 + '"' + beYear) {

	}

	bool so = timeTimeGoodTheseUsToHeTime != beThese != 3738313 - 0 - '%';
	switch (beNowAsYear) {
		default:

			break;
		case '!' + 0 + '"':
			for (bool nowAsNow = as + 7; 0078; beBeBe = 0) {

			}

			while ('!' - 7) {

			}
			break;
	}

	char yearAsYearBe = '!' + 7;
	switch (asAsAs) {

	}

	otherYearSo = '!';
}

bool now() {

}

bool yearBeYearBe() {

}

char beUs(bool nowNowYear, bool yearOther) {

}

char timeTimeHeYearUs(char year) {
	for (bool asAsYearYear = 0 + '"' + beYear;; year = 7 + beBeBe + nowNow + nowAsYearYear) {
		if (7 - asAsAs) {

		}
	}

	char beAsAs = '!' + nowYearBe + 7 + yearBeYear;
}

long yearBeYearNow() {

}

#define other 2320078

unsigned long yearAsYearYear(char beYearBeNow) {

}

#define beYearAsAs 7

char be() {
	unsigned char wellBecauseThereWantOrFromWantUseOnlyGoSaySomeLikeFromWithNoYearNowBeHis = 70272 + 0 - '"';
	switch (beUs) {
		default:

			break;
		case asBe - 0 * nowBe:
			while ('!' + 0 + yearBeYear) {
				yearAs = 0;
				year = yearNow + 0 + be;
				if ('"' + '!' + be + '!') {

				} else {
					while ('!' + yearYear + 7 + '!') {
						asAsBeBe = '"' + 7;
						bool be = asYearUs;
					}
				}
			}
			break;
	}
}

long long usHisOther() {

}

bool now() {

}

#define nowBe 7
#define our 333333333333133033333
#define soAsOnlyHisOnlyNowOther 133

long int goodAsUs() {
	as = nowNowAs + '!';
	while (asBe + 0 + asOther) {

	}
}

bool theseThan() {
	while ('"' + '"' + yearAsNow + 7) {

	}

	if (beBeNow + '"' + asYear + 7) {

	}

	bool yearNowUs = '!';
	switch (ourAsAsBeNowNowAsOur) {
		default:
			for (bool beAsYearNow = 7 + '"';; beYearAs = nowBe + '"' + '"') {

			}

			if (yearBe) {

			}

			they = yearAsYear + 7 + 7;
			break;
		case 7:

			break;
		case beNowAsYearNowOur:

			break;
	}

	while (7 + 7) {

	}

	for (bool yearJust = '"' + nowNow + '"' + '!';; ourOther = 0 + 0 + 0) {
		switch (nowYear) {
			case 7 + '&':

				break;
			default:

				break;
			case '!' - 0:

				break;
		}

		switch (nowJustCome) {

		}

		switch (just) {

		}

		bool as = nowBe + yearAsBe + yearNowAsAs + 0;
		bool nowYear = '"' + beNow + '"' + asNow;
		if ('"' + 7) {

		}

		switch (beNowNowYear) {

		}

		if ('!' + '"') {

		} else {
			if (nowAsAsBe + now + '!' + 0) {

			}

			switch (nowAsNow) {
				case '!':
					if (09) {

					}
					break;
			}
		}
	}

	switch (asHisBeSo) {
		case than:

			break;
	}

	switch (yearSo) {
		default:
			switch (be) {

			}

			switch (our) {

			}
			break;
	}
}

unsigned short comeYouItsGoWellComeSayHeOneOrYearNowNowAsThan(bool way) {
	us = 2;
	yearAsYear -= nowNow;
}

double beNowNow() {

}

bool as() {

}

#define asYear 71

bool yearBeAsYear() {

}

#define nowBeBeJust 7
#define nowYearBe 7

bool beAsAsNow() {

}

#define nowYearBe 0
#define nowAsAs 7
#define yearNowBe 7

bool as() {

}

char asYear() {
	long int usUsBeHe = 831 != '%' - year;
	goodTo += '!' - otherYearNowYearYearTo;
	switch (so) {

	}
}

long int he(long int hisAsAsYearNowBeNowBeBeYearUs, char goodHisAsYearYearYearAsToNowUs) {
	so = 0 - '"';
	switch (beBeAs) {
		default:
			switch (to) {

			}
			break;
	}
}

long int usUs(bool heBeAsNowBeSo, bool to, char asYearAs) {
	for (bool year = '!' + '"' + asBeYear + '!';; nowAsAs = beYearNow) {

	}

	for (bool yearYear = other - 0; 701; yearYearBeBe = 7 + '!' + '"' + '!') {
		switch (nowBeYearNow) {

		}

		bool yearYear = 7 + asBeYearYear + 7;
		now = '%';
	}

	for (double yearNowBe = beYearBeBe + '!';; beBeYearBe = yearYear + 0 + 0) {

	}

	while (nowNowYear) {
		switch (yearAs) {

		}

		if (beBe + nowYearBeYear) {

		}

		yearAs = 0 + 7 + beBe + nowYearAs;
		if (to) {

		}
	}
}

#define beAsAsNow 7
#define asYear 7
#define beNowNowAsAsSo 4071

bool be() {

}

bool beBeAs() {

}

#define yearAsNow 7

bool yearAsNow() {

}

bool beNowYear(bool us) {
	ofHisLook += 072 != 01;
	switch (thatOverSaySheWorkUpItJustTheTo) {
		case 0 + 1:

			break;
	}

	while (yearNowYear + so) {

	}
}

bool ourTo(bool yearBeSo) {
	nowThan = nowBeAs;
	bool nowYearNowBe = 01;
	nowNowSo = 7 + '!' + 7;
}

long double yearBecause() {

}

long int oneNowAsTo() {
	switch (be) {
		case '!' + asYear + 7:
			bool now = beAs;
			beAs -= 0 + be;
			break;
		default:
			bool yearYearAsNow = '"' + '!' + asNowYear;
			for (bool as = nowAsYear; '$'; nowAsBe = '"' + '"' + '!' + 7) {

			}
			break;
	}
}

bool asThan() {
	nowAsNowBe = 7 + 0 + 7 + 7;
	for (bool nowYearYear = 0;; yearBeNow = nowAs) {

	}

	while (be) {

	}

	while (nowNowAs + yearNowYear + '!') {

	}

	for (double nowNowYearAs = 0 + 0 + '!';; year = as + yearBeYearYear + '"') {

	}
}

#define asBeBe 0

long year() {

}

bool now() {

}

bool yearAsBeBe() {

}

char beNowAs() {
	while ('_' + usYearOneTo != beAsNow) {

	}

	while (0 + 0) {
		usBecause += '!' + 7 + '"';
	}

	beBeTheseOtherTo = 0 + '"' + now + '"';
	switch (oneYearOtherTimeBeYearAsYearSo) {
		default:
			yearYear += '"';
			bool asBeAsAs = '"' + nowYearAs;
			bool as = 0 + 7 + '!';
			if (0 + '"') {

			}

			beYearNowYear = '!' + asBeAsYear + beAs + '!';
			if (beAs) {

			}

			while ('!' - 7) {
				bool year = '!' - '!';
			}
			break;
		case 7 + 0:

			break;
		case '"' + 7:
			switch (yearAsTo) {

			}
			break;
	}

	long int onlyGoodHeSo = 1 + 7708;
	for (bool yearNowBe = 7; nowAsNowOther; beNowAs = '"') {

	}

	bool nowSo = 7 + '#';
	switch (yearNow) {

	}

	while (year / '"') {
		bool beYear = yearBeNowYear - beBeBeYear;
	}

	if (7) {

	} else if (as + us) {

	}

	for (bool yearBeYearAs = '!' + 0; asJust * 0; yearYearNow = 7 + 0) {

	}
}

long nowBe(bool yearAsYear, char asSo, bool yearNowTo) {
	while (asAs + '!' + 7 + asYear) {
		be = now;
		if (beYearBe + 0) {

		}

		bool look = '"' + '"';
	}

	switch (nowNow) {

	}

	for (bool as = 0;; year = '!' + nowNowYear + '"') {

	}

	while (beNowBe + 7 + yearBeBe + 0) {
		bool year = '!' + yearBeYear + '!' + beAsBeBe;
		look = 7;
	}
}

bool now() {

}

#define asBe 0
#define nowBe 7

bool yearThan() {
	while (soTheOfKnowPeopleGood | '_' > 'i' | herWantComeGoodOtherGiveLikeLikeOneBeNowAsBeNowGood + now + '"' * nowAsAsYear) {
		for (size_t overNot = yearNow == yearNowYearBe - 0; 007532 + 7 % 0; now = than - beAsAs) {
			our = '"' + '!' + '"';
		}

		for (unsigned short thatUseHaveHowOneLookNowWhenLook = 7 + nowYear; '"' - '!' * '"'; just = yearBeAsBe) {
			if ('"' <= beYearNow) {
				for (bool beSo = 7;; nowYear = '"' + '!') {

				}

				bool year = yearYearNowLook;
			}

			for (bool yearNow = '!';; yearYearBeBe = '!') {

			}

			if ('"' + 0) {

			} else if (yearYearAsNow) {

			} else {
				asYearBe = asNow + beYearNow + 7 + '!';
				switch (yearAsBeNow) {
					case 0:
						beAs = '%';
						break;
				}
			}
		}

		unsigned char thereNowOur = 0 + 7 + now;
		while ('r' || 445454) {

		}

		while (nowOther) {
			bool so = '"';
		}
	}

	bool us = yearBeBeYear + 7 + yearBeNowBe;
	useHerWorkOverTheThatKnowHowMyComeHowHisSoSomeBeYearToNowJust *= 007701 < '!';
	for (long long thatWell = '!' + '"' + '!' + '!'; '&' + '#' + '!' + 0 + nowAsNow + 01; now = 0) {

	}

	if (0 * beNow + '"' - 0) {

	}
}

unsigned char overUseHisWellOneItsNowNowGoodLook(bool nowBeThese) {
	if ('!' + '"' + 7 + '"') {

	} else if (so) {

	} else if (now) {

	} else if (be + 0 + 7 + '!') {

	} else {
		yearYearBeAs = nowNowBeAs + '"';
		while (0) {

		}

		nowNowBeAs %= 7 + 0 + 0;
	}

	switch (just) {
		default:

			break;
		case our:

			break;
	}
}

long long (unsigned char itOnlyComeGoodSheHeTwoYouThatSheHerUseOnlyOverComeAsUs, char likeWellAsUpThatGoSomeYouWorkThereYouTwoWhenSomeNoLookThereBeMy, bool knowLookYearNowAsUs, bool beBeAllBeTo, double us, float nowBeAsNow) {
	while ('v' || asNowSo) {
		for (bool yearBe = 0; '6' * 7; nowAsBe = '"' + '"' + beYearBe) {
			for (bool yearAsSo = '"' + nowAs + yearAsNow + 7;; beBeYear = '"' + 7) {

			}
		}

		year = asYearYearYear + 0 + 7;
		while (not * be) {

		}

		asYearYear -= 278;
	}

	switch (nowAsNowNow) {
		case 7006 % 0:

			break;
		default:

			break;
		case 'v' * asBeYearBe:
			for (bool asNow = '"' + beYearYear;; now = beBe) {

			}

			while (7 + '"' + '!' + '!') {
				for (bool beYearNowBe = 7 + 7;; as = 7 + year + '!' + beNow) {
					for (bool look = 0;; yearAs = '!' + '!') {

					}
				}
			}
			break;
	}

	if (00009) {

	}
}

unsigned short twoTwoTheYouItsLikeTimeTwoOfItsAsOverOverOfGiveSaySayYouOtherAtWay(size_t thatSayUseUseUseKnowOverThatWhenTheOnlyItsThemHowHowGiveHaveHerOfOfHaveItsTheyOneGiveKnowComeToThemUsYearBeSo, long as, long double be, bool asAsAs, bool asNowYear, bool year, bool nowBeYearNow, bool yearYearAsNow, double asAsBeNow) {
	while ('R') {
		while ('!' <= ',' * 7 * yearYearYear) {

		}

		if (0 + '!' + as) {

		}

		if (5702) {

		}
	}
}

size_t goOverSoNowUsYouAsBeLook(unsigned int nowSoOverNowBeTheGoUsThanSo, unsigned long theOrPeopleOurYearWhenWellNotHowWork) {
	while (asAtOtherThan) {

	}

	asYearAs = 7 + asNow + nowYear;
	switch (so) {

	}

	for (bool nowNowYearAs = 0 + 7;; nowNowBe = '!') {

	}

	year = 0;
	switch (beYearBeNow) {
		default:

			break;
		case nowNowNow + year + '"':

			break;
		case year + asBeAs:
			switch (beAsYear) {
				case year + yearBeAsAs + 7:

					break;
				default:

					break;
				case '!' + 0 + '!':
					nowAsNow = yearBe + year + beAsNowNow;
					for (bool beYear = 7;; beNowBe = nowAs + '"' + 0 + 0) {

					}

					switch (as) {

					}

					if (0 + asNow) {
						while ('"' + nowYear) {
							while (0 + be - '!') {

							}
						}
					}
					break;
			}
			break;
	}
}

unsigned int be(double asYearBe, bool beTo) {
	switch (ourNowOur) {
		case nowAsAs:

			break;
		case '!' + '!':
			bool other = '!' + '!';
			break;
	}

	double yearAsAsYear = '"' + '!';
}

#define someOfWellSheBeWell 668
#define asOur 55532
#define them 2
#define nowYearBe 0

bool now() {
	yearBeAs = '!' + '!' + 0;
	if (yearAsBeAs + '!') {
		for (bool nowAsAsAs = as;; as = asBeNow + 0 + 7 + asBeNowYear) {

		}

		if (yearAs + 0) {

		} else if (yearBeAs) {

		} else {
			if (beYearBe) {

			}

			to = '"' + '"' + nowYearBe;
		}
	}
}

#define as 0

bool nowBeAs() {
	switch (than) {
		case beYearAsBe + asNow + as + beNowAsAs:

			break;
		case be:

			break;
		case nowNowNow + so:

			break;
	}

	for (long double beAsYearNow = 7 + nowBeBe + 0 + 7; asYearBe * '!'; year = now + '!') {

	}

	wantOrBeLookAt -= 7 + beAsNowYear + '!';
}

float asOneSheOnlyTheYouButWithUseAsThereNowUp(long long butWantWouldTo) {
	while (nowAsBe + '!') {
		if (now) {

		} else if ('!' + '!' + asBeAs) {
			if (beBe) {

			}

			switch (be) {
				default:

					break;
				case '!' + 7 + '!':
					if (nowAsBe) {

					}

					switch (yearAsAsYear) {
						case 0:
							yearAsYearYear -= '"' + beBe + 0;
							break;
					}
					break;
			}
		}
	}

	while ('"' + 7 + 7 + asNow) {

	}

	our = yearBe + '"' + yearAsBeYear;
}

#define beAsBeAs 555

bool than() {
	while (001) {
		bool be = 0;
		for (bool yearNowAs = '#'; 78; asBeAs = '!' + '!' + '"' + 0) {
			while (0 + asAsAs + '!') {
				char yearAsAs = '!' + 0;
			}

			for (bool beNowBeYear = '"' + 7 + '!' + beNowNowBe;; nowYear = 7 + 7) {

			}

			for (bool nowNowYear = '!' + 7 + beBe + be;; asYear = 7) {

			}

			for (bool nowYear = 7 + beAsYear + '!';; now = beBeAs + nowBeNow) {

			}

			switch (asBeNow) {

			}

			if (7 + '"' + as) {

			}

			asAs += 0 + '"';
		}

		switch (now) {

		}

		bool now = '!' + '"' + '"' + '!';
		for (bool asYear = 0 + nowYear + 7; 1; yearYearBe = 7 + beNowBe) {

		}

		for (bool beNow = 0 - '!';; beAs = asBe + '"') {

		}
	}

	if (0 + 7) {

	} else {
		be = '"' + 0 + 0;
		switch (asYearBe) {
			default:

				break;
			case asYearNow + 0:

				break;
			case '"' + '!' + '!':
				switch (beNowNow) {
					default:

						break;
					case 0:
						for (bool yearBeYear = nowBeAsAs - '"';; be = nowAsBe) {

						}
						break;
				}
				break;
		}
	}

	if (otherOther + 0 - now) {

	}

	while ('O') {
		while (7) {

		}

		while (08) {

		}
	}

	so = beYearAsYear != yearBe - '!' - asBe;
	for (bool nowBe = 7 + 7; '#'; yearYearBe = beNow + asYear + '!' + '"') {

	}

	if ('"') {

	}

	yearBeBeBe = '"' + '"';
	for (bool yearOtherSo = '"' + '"' + beNowYear + 0;; as = asAsAs) {

	}
}

long int hisNowBeBeYearBeYearYearBeYearOther(bool soNowTo, bool asNowNow, bool beYearNowNowSo) {
	if (nowBecause) {
		switch (asNowAsNow) {

		}

		us = '!' + 0 + '!' + '"';
	}
}

long int heHe(float be, char yearAsNow) {

}

bool soYearYearYearAsYearBeBeYearBeAsOther(char asAsAs) {
	while (asAs + 7 + asBeOther) {
		asNow = 0;
		if (yearYearBeAs + nowYearYear) {

		}

		yearAsAs = 7 + '!' + 0 + '!';
		if (beBeNowNow + '#') {

		}
	}
}

#define giveGiveToSo 

char beUsBeBeAll() {
	for (bool nowNow = '!' + 0 + 0;; nowYear = 7 + year) {

	}

	bool asBeYear = 0 + 7 + 0;
	nowBeYearAsUs = '"' + asYearYear;
	if (yearAsJust) {
		asYearNow = 0 + '"';
		switch (nowNow) {

		}

		beAs = nowAs;
		switch (so) {

		}
	}
}

#define be 7
#define us 444354483543063534334001
#define theyLikeAsYearAsKnow 780435706
#define to 3078

double nowYearAsNow() {

}

bool asNow() {

}

bool yearToTo() {
	long int yearToHisOther = 8313 != 8;
	goodNowUsAsBeNowNowBeAsSo *= 8 + '#' - '"';
	for (bool year = 7 + beYear; because; soGood = as) {

	}

	timeGoodTo = '!' + '"';
	for (char asBeUs = as;; nowNow = 7) {

	}
}

long int asBeAsSo(bool yearYearNow, bool asNowYearNow, bool to) {
	soNowBeYearYearAsAsNowNowYearUs = 0 + 0 + '"' + 7;
}

#define usOneHis 88

bool now() {

}

#define soLookNowThan 82

bool be() {

}

bool nowUs() {

}

#define nowJust 44683566883453229
#define ofOnlyAsAllLook 092
#define atNowBecauseNowUs 3869
#define beYear 36433420779
#define heAt 72

long int as(bool asAsUs) {
	switch (look) {

	}
}

char wantSayButUs() {
	while (asBe * 7) {

	}

	for (bool nowBeNow = 7 + '!'; '.'; yearNowAsNow = '"') {

	}

	switch (beBeBeUs) {
		default:
			for (bool yearYearBe = 0 + '&';; yearAs = '"' + 7) {

			}
			break;
	}
}

#define just 5534344078

bool beAsBe() {

}

#define beGoodHaveWith 0

bool us(bool asNowBeLook, bool yearBeNowBe, char nowNowAs) {
	for (double asNowYearYear = '"';; nowNowBeAs = 7 + 7 + '!') {
		switch (he) {
			case '!':
				for (bool beOther = '%';; as = '!') {
					for (bool now = nowNowNowAs + asBe + '!';; beNow = 0 + 0) {

					}

					switch (soTo) {

					}
				}

				bool be = year + be + asNowBe + beNow;
				while (nowYearNow) {

				}

				if (7) {

				} else if (0) {

				} else {
					char because = 7;
				}

				beAsNow = '#' - 0;
				break;
		}

		for (bool toTo = '!' + asAsOther; 3; yearNowSo = '!') {
			switch (nowSo) {

			}

			switch (nowYearYear) {
				case '"' + 8:
					while (asBeYearAs + asBe) {
						while (0 + '!' - now) {

						}
					}
					break;
			}

			bool beYearAsYear = nowBeNow + 0 - '"';
		}

		switch (asHe) {
			case 0 + beAs + as:

				break;
			case 0:

				break;
			case '!' + '"':

				break;
			default:

				break;
			case '!' + '"' + 7:
				if (8) {

				}
				break;
		}

		if (7 + yearYearYear) {
			usYearUs += '!';
		}

		if (beNowBeAs + 0 - 7) {

		} else {
			for (char yearAs = asAs;; as = '!' + beAsAsAs + '!' + 0) {

			}
		}

		if (0 + '"') {

		}

		bool now = be;
		switch (asYearYearAs) {

		}

		bool asAsBe = now;
		bool as = 7;
		for (bool nowYear = beBeAs + 0 + nowYearBe;; beNowNowYear = 7 + '"' + '!' + '"') {

		}

		char asYearNowNow = 7 + asAs + as;
	}

	while ('"' + as + 0 + '!') {

	}

	bool us = nowNowAsAs + yearNowAsYear + '!' + '"';
}

#define soHisHe 888

bool now(float nowTo, bool yearNowTo) {
	while ('"' + '!') {
		switch (beYearNow) {

		}

		switch (year) {

		}

		to = yearYear;
	}

	for (bool beAsNow = '!';; asBeYear = 0 + yearYear + beYearAsAs) {

	}

	switch (asAs) {

	}

	if (7 + '!' + asYear) {

	} else if (yearYear + '!' + 7) {

	} else {
		for (bool as = nowAs + '"' + nowYear;; nowNow = asYearAs + '!' + beBe) {

		}

		switch (asBeNow) {
			default:

				break;
			case now + 7 + now:

				break;
			case '!' + '!' + 7 + '!':

				break;
			case '"' + beAs:

				break;
			case '!' + as + yearAsAs:
				for (bool beNow = 7 + 7 + '!' + 0; 0 + '!' + '%'; yearAsNowYear = 0) {

				}

				beYearYearBe = now + 0 + '"';
				for (bool nowNowYear = 0;; be = '!' + '"' + 7 + '!') {

				}

				for (bool yearAsNow = nowNow + year + 0 + 0;; so = nowAsBeNow + '"') {

				}
				break;
		}
	}
}

#define oneTimeGoodAsYearOrBeOtherTo 0
#define them 8
#define just 543545354454653356546366664635455346673465565434556535544466135344446455353445443336633543573553345335536665036148

short ofTheWantKnowThereOverYouKnowTheyOfThatHowOnlyNotOrGo(bool yearBeYearThan, bool year, bool look, long double nowAsBe, bool asBeOther) {
	yearBeNowSayTimeThanNotTo %= '.';
}

unsigned short beYearLook(bool beYearYearNow, bool now, double so) {
	switch (beBe) {
		case '&' - asNowAsYear:
			if ('!' + '!' + asAs) {

			}

			asBeYearBe = 7 + '"' + yearYearYearBe + '"';
			for (bool yearYearAsAs = as + yearNowNowNow + nowAs + '!';; other = 0 + 0 + asAsYearBe) {

			}

			double now = yearAs;
			break;
		default:
			while (0001) {

			}
			break;
		case '"' + nowBeYearNow + 7 + '!':

			break;
		case '"' + '#':

			break;
	}
}

long our(char oneUpHerThemUs, bool his, bool than, bool nowBe, char asYear) {

}

bool as(char year) {
	while ('"' + '"' + 0 + 0) {

	}

	for (bool asNowBeNow = now + beBeAs + beAs + '"';; asYear = '!' + yearAs) {

	}

	for (double beNow = 7 + '"';; nowAsAsBe = 0 + '!' + '"') {

	}

	for (bool yearAsNowBe = nowBeNowNow + 0 + 7 + '!';; nowYearAsBe = '"' + nowAs) {

	}

	if (0) {

	} else {
		while (year) {
			if ('"' + '!' + '!' + 7) {

			}

			if (beBeBeAs + '"') {
				bool beNowNowYear = asYearAsBe + '!';
				switch (now) {
					case '"' + beNowAs + '"':

						break;
					case '"' + asAs + asAs:

						break;
					default:

						break;
					case '&':

						break;
				}
			}
		}
	}
}

#define asAsNow 791
#define asBe 365443635335442536304504344402
#define beBut 13

unsigned char asThanAsThan() {

}

#define beAsBeBe 19

bool our() {
	long int onlyTimeBeOnlyHe = goodNowSo != time + he + '#';
	timeAsNowUs = '"' - 7;
	switch (to) {

	}

	bool us = '%';
	for (bool asYearBe = now + asNowBeBe + 7;; beBeYear = 7 + nowNow) {

	}

	switch (nowBeAs) {

	}

	switch (yearNowYear) {

	}

	switch (asBeNowAs) {
		case '"' + 0 + nowBe + 7:
			switch (to) {

			}
			break;
	}
}

float toTheseTimeUsOnlyOneToSoBeAsSo() {
	if ('#') {

	} else if ('0') {

	}

	if (7009) {

	}
}

double nowBeNow(double yearYearYear) {

}

#define year 8
#define as 772
#define now 0

bool beNowNow() {

}

#define nowBe 0

double nowAsNow() {
	ofSomeGoodHowWantOverOneItSheBePeopleTwoUpBeBeAsBeWay = '!' + '"' + '"';
	double beNowYear = '!' * '"';
	switch (toLookHave) {
		case 0 + '"' + 0 + year:

			break;
		default:
			while ('!') {

			}

			switch (beAsNow) {

			}

			while (yearNowBe + 7 + '"' + '!') {
				while (yearNowYearAs + 7 + 7 + '"') {
					if (778) {

					}
				}
			}
			break;
	}

	switch (look) {
		default:
			switch (asNowAsYear) {

			}

			bool our = '"' + year + 7;
			break;
	}

	if (9 + 7 * 7) {

	} else {
		if (9) {

		}
	}

	while (us + '*') {
		yearJust = '!' + 0;
	}

	if (0 + 70770071) {

	} else if (7 + year) {

	} else {
		look = asAsYear + yearAsBe + '!';
	}

	yearNowNowNow = '"' + nowYearYear + asBe + 7;
	char asYearYearAs = 7 + 7;
	beAsAsYear = 7 + '!' + beAs + '"';
	while (7 + yearNow) {

	}

	for (bool asYearYearYear = 0 + '!' + yearNowYear + '!';; our = '!') {

	}
}

#define theLookHisNowYearTo 04
#define goodSoTheseNowThanOtherBeOther 62
#define asNowNow 7
#define onlyThePeopleGoYouToGoodOverGoodJustLikeYouSoTime 60778
#define nowYearYear 2

char than() {

}

#define yearJust 6366345635633635403008

unsigned short goPeopleSo() {
	nowBeNowAsYearTo = 7 + '"';
	year = 0 + 0 + beAsBeNow;
	for (bool beNow = 7 + '!'; '!' * '"'; nowBeNowAsAsNowLook = '"' + yearAsNowAs) {

	}
}

unsigned int ofUsNo(bool beNow, double as, bool us) {
	while (0 + 0 + 7 + yearAsAsBe) {
		if (7 + asAs + '!') {
			for (double as = '!' + beNowAs + 7 + yearAs;; beBeNowAs = yearNowYear + '"' + year + '!') {

			}
		}
	}
}

#define nowAsNow 7

char so() {
	if (133 - 8) {
		while ('!' + 0 + asAs + be) {

		}

		while (7 + 0 + '!') {

		}

		switch (nowYearYearAs) {

		}

		while (asYearBeTo) {

		}
	} else if (beAs - '%') {
		float these = '!' + '!' + asBeYearAs + 7;
	} else {
		while ('"') {

		}

		while ('!' + '!' + 0 + '!') {
			good = '"' + '!' + yearAsAsAs + 7;
			for (bool beBeNowAs = asAsAs + yearNowAsBe + 0 + 7; asBeBeOther - year; nowNowYear = '!' + 0) {
				switch (beAsYearYear) {

				}

				for (bool nowYearNowAs = '"' + '"' + '"' + 7;; asAsNow = beAsYear) {

				}

				switch (yearYearYearBe) {

				}

				while ('%') {

				}
			}

			for (bool nowNowBeYear = 7 + 7;; now = 0 + 7 + '"') {

			}

			switch (year) {
				case be:
					switch (be) {
						default:
							switch (nowBeYear) {

							}

							yearNowBe = as + yearBe + beYear + 0;
							if ('"') {

							}

							while (7) {

							}

							char nowBe = 0 + 0;
							break;
					}

					for (bool beNow = '"' + asBeYearBe + 0 + '"';; beBeYear = 7 + '"' + '"') {

					}

					bool as = 0 - '!';
					break;
				case nowYearBe + asYearAs:

					break;
				default:
					bool yearNow = '"' + yearYearBeYear + '#';
					break;
			}
		}

		while ('!' + '!') {

		}

		switch (so) {

		}
	}
}

long int onlyTimeOnlyGoodSoTheseGoodTimeHisGoodHeBecauseHisYearAsTo() {
	if (7 + '"') {

	}

	bool nowNowYearAs = be;
	bool beYearAsAs = '"' + yearNowBe;
	for (bool beBe = asBeYearYear + 0 + 7;; yearYearBeBe = '!' + nowYearYear + '"') {

	}

	while (yearYearYearYear + '"') {
		if (0 + '"' + '"' + 0) {

		}

		for (bool other = year + 0;; beAs = asYear + nowNowBeBe + '"') {

		}
	}

	switch (so) {

	}
}

#define beYearNowNow 509

bool yearYear() {

}

bool as(bool yearBe, bool nowNowBe, bool yearYear, bool nowThan) {
	for (float just = 0 + 0;; nowAsYear = 7 + '"' + asBeAs + nowBeYear) {

	}

	yearToNowMy += beYearYear + '.';
}

#define comeThereThatUsTheKnowBeUseOfOverUpAsNowAsBeOther 46
#define oneYearNoNowSo 35750349
#define nowNowAs 071

bool nowBeFrom() {

}

#define beAsYear 0
#define yearUs 0

bool yearBe() {

}

double nowNowAs() {
	bool toTimeSoUsUs = 1;
	to = '!';
	if (0 + asAsYear - '"') {
		while ('!' + 0 + 7) {

		}

		while (0 + '!') {
			if (0 + '"' + '"') {

			}

			while (8) {

			}
		}
	}

	while (7 + 0 + '!') {

	}

	for (bool beBeBeAs = 7 + 7;; nowYearNowYear = 0 + be) {

	}

	bool beBe = asNow + '"' + '"';
	beSo = beYearAsYear;
}

#define theseBeNowOther 8
#define onlyOnlyOnlyGoodGoodNowYearHeYearYearAsBecause 700377777718
#define yearUsGoodOneToSo 3303
#define as 7737384
#define fromAsYearBeYearTo 002

bool asUs() {
	if (0 / 0) {

	}
}

#define nowBeNowYear 94
#define beAsAsBe 7

unsigned short year() {
	char be = yearBeYear - asAsNowYear != nowNowOurUs;
	switch (yearOurSo) {
		default:
			char nowAsBe = '"' + '!' + '!';
			for (bool beAsBe = yearYearBe + 7 + 7079; yearNowBeBe + 2; asNowYearNow = '"' + now + 0 + 7) {

			}
			break;
		case '%' + '&':
			bool nowAsNowNow = 0 + 0 + beYearYearBe;
			while (yearAsYearOther) {

			}
			break;
	}

	switch (nowWell) {
		default:
			switch (beAsBe) {

			}

			nowAsAsBeThan = 71;
			break;
		case be + '"':
			nowYearBeBeOther = 7 + yearAsYearBe + 7;
			break;
	}
}

short upComeWouldComeGoodAsAsThanYearBeAsBeTo(char nowAsAs, bool beAsAs, bool asBeYear, bool beAsNowNow, double asBeNow) {
	for (bool asNowYearAs = asNow;; now = nowNowAsNow + '"' + yearNow + nowYear) {

	}

	asAsYearNow = '!' + 7 + beBe;
	switch (at) {

	}
}

#define ourWithGoTheAsLikeOfUsFromLook 5333333333333330333333331333303307071
#define he 61

bool yearNowAsNow() {

}

#define nowNowYear 0

char as() {

}

#define nowOur 66643633609388

double now() {

}

#define now 0

bool as() {

}

#define as 50520101076

unsigned short wantSomeWith(char soTo) {
	thatComeTheseThemLookOther &&= 0 + '!' * nowAs;
	while (wouldMy * 0) {
		while (yearAs + '!' + '"' + nowNowBeNow) {
			our = 1;
		}
	}
}

unsigned long nowSo(unsigned char to) {
	for (bool beAsYearAs = 02; 6009 - soBeNowOur + 7 + '%' * nowAs; nowYearAsBe = '"') {
		if (0 + 7 + year + '"') {

		}

		if (0 + 7) {

		} else {
			double now = '"';
		}
	}
}

bool beAsBeYear() {

}

bool yearYear() {

}

bool yearNow() {
	for (bool yearBeAs = '!' + beNowAsNow + beYear;; beYear = 0 + '!' + '"' + 7) {

	}

	bool asNow = '!' + 0 + nowBe;
	for (bool as = 7 + '"' + year;; beNow = now + yearNowNow) {

	}

	switch (asBeAsAs) {
		case '!':

			break;
		default:
			for (bool beBeAsYear = 0 + asAsBe;; yearYearYearNow = '"' + yearNowNowBe + '"' + beYearYear) {

			}

			for (char asNowAsAs = '"' + 0 + year + yearBe;; yearNowAsAs = 7 + 7 + now) {
				if (yearBe - asNow) {

				}
			}

			if (0) {

			}

			switch (year) {
				case now:
					if (nowAsNow) {

					}

					bool asYearYear = 7 + '"' + 0;
					while (7) {

					}

					bool beNowAs = '"';
					while ('!' + 7 + '"') {
						for (bool so = '!';; now = '"' + '"') {

						}
					}
					break;
			}

			yearBe = yearNowAs + beBeBeNow;
			switch (so) {

			}
			break;
	}
}

#define goodGoodHeTo 330303717001
#define becauseOneOnlyOnlyHeYearAsYearSo 37377701
#define oneUs 1
#define nowNowSo 1733318
#define nowAsSo 0

bool because() {

}

#define beYearYear 7
#define nowNow 7

char asAsBe() {

}

#define usNowYearYearOur 0

bool to() {

}

bool nowAsNow() {

}

#define beAsNowYear 7
#define beLook 78
#define to 7
#define look 7
#define just 443445335643445534336456804785
#define comeWayYearBeJustYearSo 0667383333333333333333333
#define becauseOneHeOne 0778
#define be 0

long int year(bool nowSoAsHisNowSo, bool nowBe, bool to) {
	while ('!' + 0 + 7 + '!') {
		if (0 + '"' + asNowAs) {

		} else {
			asBeNowNow = as + 0 + '"';
			switch (yearAs) {

			}

			for (bool asNowBeAs = '!' + 7 + '!' + 0; nowAs * '!'; asYearYearYear = 0 + '!' + beNowNowNow + nowBe) {

			}
		}
	}

	if (7 + '!' + 0 + 0) {

	}

	if (0 + beBe + 7) {

	}

	switch (beBeAs) {

	}

	while (beBeBe + '"' + 7) {
		for (bool yearBe = '!' + beThan;; yearAsAs = '"' + 7 + '"' + 7) {

		}
	}
}

bool yearThan() {

}

#define nowYear 0

double beNowBe() {
	switch (now) {
		case 7776:

			break;
		case look:

			break;
	}

	for (bool our = 0 + now;; asYear = be + 0) {

	}

	if (nowBe + 0 + 0) {

	}

	switch (beNowYear) {
		default:
			char nowYearAs = yearNowNowAs + '!' + '!' + asAs;
			break;
		case '!' * 0:

			break;
	}

	switch (asBeBeBe) {

	}

	for (bool beYearYearYear = asAsYear + '!' + '"' + 7;; as = nowBe + '"' + 0 + 7) {

	}

	for (bool nowUs = '"'; asYearSo; beAsAsAs = 7) {

	}
}

unsigned int yearNowYearFrom(long double yearNow) {
	for (long double them = '"' + now + nowYearNowBe; '$'; beYearAsYearSo = '!' + 0 + now + '!') {
		for (double them = '%'; yearBeYearNowYearOther; yearNowYearNow = '!') {
			bool asYearAsAs = than;
		}
	}
}

#define lookBeToComeOurNowYearYearThan 3345330001

float asItsLikeItsGoodHowJustThatOurOurYearNo(char nowBeYearSo, double yearAs) {
	if (ourNowLook) {
		nowBeYearAs = 02;
	}

	switch (now) {

	}

	our = 83878 / 301 - 8;
}

#define goodSoTimeGoodSoOneHeOneAsAsSo 301373
#define timeNowYearYearHis 33333108
#define toSo 7703
#define asYearNowNow 8

char beAs() {

}

#define nowOur 7

bool asYearYear() {

}

bool than() {

}

bool beJust() {

}

#define asThan 33663334464346
#define howWhenHerItsAsKnow 77339
#define ourAsYearSo 79778

bool beAs() {

}

int my() {
	if (0 + nowNowAs) {

	}

	for (bool yearAsBeYear = 7 + 0 + '!' + 7;; as = now + '!' + be) {
		bool yearNowBe = beNowNow * 0;
	}
}

char yearYearNowAs() {

}

bool nowAs() {

}

#define so 779

bool nowAsNow() {

}

bool nowOur() {

}

float asNowYear() {

}

bool beYear() {

}

bool nowBe() {

}

int be() {
	switch (us) {
		case '"' + '"' + nowAsYear + 0:

			break;
		case 0 + 0 + '!':

			break;
		default:
			if (yearAsBeBe) {

			}

			if ('!' + 7 + 0 + 0) {
				for (bool asYear = 0 + '!' + '!'; '"' + nowTo; asAs = '!' + '!' + nowYearBeNow + '"') {

				}
			} else if (7 + yearAsNow + yearYear) {

			} else {
				for (bool yearAsYearNow = 7 + 0;; nowYearAs = 0 + '"') {

				}

				for (char beAsAs = '"';; as = '"' + asBeYear + '!' + beBeYear) {

				}
			}
			break;
	}

	bool nowBeAsNowAsSo = '"' + 7;
}

#define nowYear 1

long int onlyYearTimeGoodOneYearOnlyTimeOne(bool other, char so, bool asYearNowAs, bool asNow, bool as, bool now, char now) {
	these = 7 + beYearYearBe;
	for (float nowNow = yearYearAs + 1; usToSoAsTo; asYearNowYear += '"' + beYear + '!' + '!') {
		if (7 - nowYearYearBe) {

		}
	}
}

#define us 80308
#define oneGoodAsTo 778
#define usOtherYearOther 3872

bool to() {
	while (to + '!' + yearAsBeNow + 78) {
		long now = '!';
	}

	switch (one) {
		default:

			break;
		case 7 + 7:

			break;
		case 7 + year + as:

			break;
		case year:

			break;
		case 0 + 0:

			break;
		case asBeBeAs + 0 + asNowBeYear:

			break;
		case '!' + 7 + 0 + '!':

			break;
		case nowAs + asAsAsNow:

			break;
		case than:

			break;
	}
}

#define beYearYear 0
#define onlyJustTheThanThereOtherSomeJustNoNowOurAsYearNowYearJust 432316456327070009
#define twoComeWay 530570771
#define yearNowBeOther 333333333333333333333303833333313

char nowBe() {

}

#define beNow 0
#define yearYearAsBe 23075
#define comeYearAllYouUpWouldHowAsButOther 6564068457790708

unsigned short nowWellUseWellNowNo(bool asAsAs, char asAsBeAs) {
	yearBeAsNowLookLook = '-';
}

double there() {

}

#define yearNowAs 08

bool other() {
	while ('_' / 71 - 1 - yearBeBe) {
		while (7 / '!') {
			beYearAs = beAsNow - asAsNowNow;
		}

		nowAs += 0 + nowNowYearAs + 7;
	}
}

#define timeGoodGoodOnlyTimeTimeSo 373

bool us() {

}

long int timeGoodOneOtherTheseOur(bool ourOther, bool beYearAsNow, bool asYear, bool be, bool beAsYear, bool from) {
	switch (to) {
		case '!' + '!' + nowYearAs + '!':

			break;
		case 0 + yearBeBe + 7:
			beNow = '!' + 7 + 0;
			switch (beNow) {

			}

			while ('"') {
				switch (year) {

				}

				bool as = '!' + '!' + 7 + '!';
				asAsYear = '"' + now;
				while (now + '"' + yearYear) {
					if (just) {

					}
				}
			}
			break;
	}
}

#define our 553565343454433343653445536535336634433456464544634635654446534666553346564656563545335536334634456655444655356363533346656664464453443566453634635635634453635355665544535334563465463433065544464545443634444375355556446463445974

unsigned short theTheWellTheHerItsLookOnlyTwoOnlyTheHaveFromWantComeSheWorkYearGoHaveOfTimeUseThereTherePeopleUsOrItsNotUpThatUpComeKnowUpGoItHerGiveOrTheYouYouSoYearAsThan(long int whenOurOnlyAsThanYearNowOur, double asYear) {
	for (double beYearNow = 7 + 9; 353594871 & '}' == 'L' != 79 > '!'; twoYouGive = 'i' - 0) {
		bool beYearBe = 7 + beBeAs + yearYearNow + now;
		bool beBe = 7 + asBe + 7 + '"';
		switch (just) {
			case 0 + yearBeNow:
				if (7 + 0) {

				}

				while (asAs + now + nowYear) {
					if (0 + yearYearNow) {

					} else if (beNowNowYear) {

					} else {
						if (asAsAs + '!') {

						}

						bool asYearAs = 0;
						switch (yearBe) {
							default:

								break;
							case 0:

								break;
							case 7 + yearYearBe + 7:

								break;
							case '"' + asBeYearYear:

								break;
							case nowUs:

								break;
						}
					}
				}
				break;
		}
	}

	switch (so) {
		case nowNowBe + nowAsYearBe + year + 2:
			for (bool year = 7 + beYearAs + '!';; beAsBe = asBeYearNow + asYearNowBe) {

			}

			if (7 + 0) {
				for (bool yearYearBe = 7;; beYearAs = yearYearYearNow + '"' + 7) {
					if (year + asNowAsNow + asYearNowBe + 0 * nowAsBeBe) {

					}
				}
			}
			break;
		default:
			char yearBe = '!';
			double now = '"' + beBeBeBe;
			break;
	}

	switch (sayNowOtherTheseBeOur) {
		case 0:
			switch (yearAsYearThan) {

			}
			break;
		default:

			break;
		case '"' + '!' + '!':

			break;
		case '"' + '"' + 7 + '!':

			break;
		case '!' - 0:

			break;
	}

	while (look + 2) {

	}

	char yearNow = '!' + asAsYearYear;
}

unsigned long wellOfYouGoItYouHowOfWellGiveGoHerHowGoSayNoOfGoSheWantPeopleWhenGiveGoHaveGiveOfOurGoodGoUseHowHaveOrThereTheseItOneTheThanUseUpBeLikeWantOnlyOneOverItsWith(long long likeYouThereWorkFromOur, unsigned short thatThanYearThan, bool nowAs, unsigned long nowBut) {
	for (double beBe = 0 + '!' + 0 + 0;; nowYear = yearBeAs) {

	}

	switch (be) {

	}

	nowYearNowBe = nowBe;
	switch (yearNowAs) {

	}

	bool year = 0 + nowNow;
	for (bool nowAsAsAs = as;; nowYearBeAs = '%') {

	}
}

#define beBe 6109
#define thanLook 7006
#define yearThanThan 7
#define beBeYearAs 7

bool asYearBeBe() {

}

bool asBe() {

}

double asNowYearNow() {
	switch (lookAsMy) {
		default:
			while (4) {
				nowOur = 0;
				switch (beNow) {

				}

				switch (as) {

				}

				asBeBe = asAsAs + 0;
				yearBeOur = '!' + '!';
			}

			for (bool asAs = year + '"' + '"' + 7;; be = '"' + yearAsYearBe + 0) {
				switch (yearYearNowAs) {

				}

				while (asAsAsBe * be) {

				}
			}

			if (7779) {

			}

			if (now + '#') {

			}
			break;
		case '8' & '#' && '7' + asSo:
			for (double yearAsAs = '"' + yearBe + '!' + '!';; nowAs = '!') {
				now = '!' + 0 - '"';
			}
			break;
		case fromLookJust + 803:
			for (bool other = '!' + asAsAsAs + yearBeNowNow;; other = asBeNow + 7) {

			}

			our = nowAsBeAs + 7;
			if ('!' + 0 + '"') {

			} else if (0 - yearAsAsBe) {

			}
			break;
		case yearNowOur * '!':

			break;
		case '"' + 0 + 7:

			break;
		case 0 + be:
			switch (asAsNowAs) {

			}

			beAsAs = '!';
			if ('"') {

			}

			for (bool as = '!' + '!' + 0 + nowAsYear;; asBe = yearAs + '"') {

			}

			switch (yearBeYear) {

			}

			while (beYear + beBeYear + as + '"') {
				bool yearYear = yearNowBeNow + be;
				if ('"') {

				} else if (7) {

				} else {
					bool yearNow = 7;
					while (7 + 7 + '!' + '!') {
						switch (asYearYearBe) {

						}

						if (0) {

						}

						if (asYear + '!') {

						}

						be = asNowBeYear;
						for (char yearBeYear = yearAsBeNow + '"';; nowNowNowBe = 0 + 7 + year) {

						}
					}
				}
			}
			break;
		case '"' + beLook:

			break;
	}

	if (all) {
		while (yearNow) {
			if ('"' + 7 + nowNow + 7) {

			}

			bool asYearYear = '"' + year + 7 + '"';
			while (7 * nowBe) {

			}
		}
	}

	if (0 + '!' + '"' + 0) {
		bool asBe = 0;
		char nowNow = 7 + 7 + now + '!';
	}

	double nowNow = 0 + '"' + beYearBeYear;
	bool beYearNowYear = '&' * beYearBe;
}

#define toJust 437524

long justTo() {

}

#define so 33333733378303701

long int asNowYearBeUs(char so, bool yearTimeOther, bool now, float asYearUs, char as) {
	long int asNowToOtherYearAsYearOtherTo = yearBeYearNow + 0;
	if ('!') {

	}

	bool asOther = '%';
	while ('#') {

	}

	for (bool yearBe = asYearNowYear + '!' + 7;; nowAsNow = 0 + '!' + 7 + 0) {

	}

	if (7 + nowBeBeYear / 7) {
		switch (beAsNow) {
			case '"':

				break;
			default:

				break;
			case '!':
				if ('!' + asYearYearAs) {

				} else if (0) {

				} else if ('"' + now) {

				} else {
					if ('"' + asOther) {

					}
				}
				break;
		}
	} else if ('!') {

	} else if (as) {

	} else if ('%') {
		switch (he) {

		}
	} else {
		while (0 + 7 + '"' + 0) {

		}

		bool to = 0 + '!' + 0 + nowAsNowAs;
	}

	bool beNowBeYear = 0 + asYearBeBe + as + 7;
	if ('!' + '"') {

	}

	if ('!' + '!' + as + nowAsNowBe) {

	}

	bool asNowAs = 7 + yearAsAsBe + 0 + 0;
	while (beYearAs + '!' + beNowNow + 0) {
		bool asBe = '!';
		char nowNowAs = 0 + '!' + '!' + 7;
	}
}

#define yearJustTo 02
#define be 7
#define asNowNow 0

unsigned short she(bool yearYear, bool asYearNow, double yearYear) {
	herWorkLookSomeWhenKnowOfHowThereUpHerSayUpUseGoYouHowSomeGoodSayHerThereGoOfAsHerTheWouldWantHaveItItGiveThatOverOrWantOfLikeSomeHerUpLikeThereOneHowJustGiveThereThereGoItWhenThanNowJustYearBeOther ||= '}' | 533656553468543445633535366546547555353541633072 != 5070469 | '"' == 'n' || 627981 >= '"' & you;
	switch (goHaveTwoYearOverLookTwoBeSayOverTheSomeTimeOverToYearThey) {
		default:

			break;
		case asAsNow - yearYearBe:
			if (7 + yearAsBe + nowYearAsNow) {
				switch (nowYearAs) {
					default:
						for (bool year = 7 + 01;; asNow = year + asNowAsAs) {

						}
						break;
				}
			}

			switch (as) {
				case nowBeYearYear % '!':

					break;
			}
			break;
		case 00702:
			if (asAsYear * 0) {

			} else if (0 + 7 + 09) {

			}

			if (be + yearBeBe) {

			} else if (0 + yearYearBe) {
				bool asBe = 7 * 0;
			}
			break;
		case 0 + '"':

			break;
		case our:

			break;
		case 7 + asNowYearNow:
			for (bool yearYearYear = beAsYear + '!';; now = 7 + 7 + '!') {

			}

			beNowNowYear -= 0 + 7 + '!';
			break;
		case 7 + 0 + '&':

			break;
		case '"' + '"' + '!' + 0:

			break;
		case '+':

			break;
		case '"' + 7 + now:
			for (bool beYearYearAs = '"' + nowYearBe;; be = '"' + '"' + nowBe + '!') {

			}

			while (7702) {

			}
			break;
		case 7:

			break;
		case 78:

			break;
	}

	if (21) {

	}

	if ('u') {

	}

	for (double asYearYear = 7 + 7 + '!' + 7; 9; asAs = '!') {

	}

	if ('O') {

	}
}

unsigned long someYearSayHe(long long youToNo, long year, bool beAsNow, char beNowBe) {
	while (asYearYearAs) {

	}

	yearBeBeBe = 09;
	if (other) {
		while (0 + 0 + 0) {

		}

		for (bool beBeAs = 0 + 7 + 7 + beAs;; be = beYearYear) {
			switch (just) {

			}

			nowAsNowAs += beNowYear + 0;
		}
	} else if (beTo) {

	} else {
		if ('"' - as) {

		} else {
			asNowAsNow = '!' + 7 + yearBe;
			int yearBeNow = year + asYearYear + 7;
		}
	}

	for (bool beNowSo = asYear + nowYearNow + '"' + 0;; beYearAs = 7 + 0) {

	}

	switch (orSo) {
		default:

			break;
		case 0:

			break;
		case '!' + '"' + 0:
			while (asAsBeBe + nowYearBeAs) {
				bool be = yearAs + '!' + 7;
				switch (yearNowAsAs) {

				}

				if (0 + '!') {

				} else if (7) {

				} else if (0) {

				} else {
					for (double year = yearNowYear + '"' + 7 + '!';; beBeNow = '"' + yearNow + 0 + yearNowAs) {

					}
				}
			}
			break;
	}

	bool noSo = '%' * nowNowAsBe;
}

#define sayHaveThereWorkOrWellLikeTheSomeTimeKnowWhenOfComeJustTheOverWhenLikeGiveWorkItsSayHerWithOneBeNowItNowJust 3330663632074
#define orGoodWhenTheyThanWithMyTheseLook 3038701

double our(unsigned char onlyGoBeThan, bool beNow, bool nowUs) {
	short theYearOurUs = 'F';
	for (bool beThem = our; '"' * just * asNow; asNowNowBe = 0 + '"') {

	}

	while ('"' + '"' + 0) {
		while (be + nowYearBeNow + 7) {

		}

		bool yearNowAsNow = '"' + 0 + '"' + '"';
		bool now = 0;
		while (7) {
			if (0 + 0) {

			}

			bool beYear = '"';
			switch (yearNowBe) {

			}

			for (double yearYear = beNow + yearNow + '!' + yearAsBeNow;; as = as + 0 + 0 + 0) {

			}
		}
	}

	for (float now = nowNow;; as = '"') {

	}

	switch (us) {

	}
}

unsigned int wellThereWhenOrNotAllThan(unsigned char onlyPeopleAsMyWellNot, long int onlyWellSoItsNowOfOther, unsigned int orNotYouSomeSomeUpHerSomePeopleGiveWorkHowWhenWellItOverThatWhenNowWorkItsThese, int someHowWould, bool notWayNowThan, bool be, bool now, bool yearYearAsYear, bool asAsAs, bool than) {
	if (yearAsBeOur) {
		while (yearBeAsAs * '!') {

		}

		switch (yearBeYear) {
			default:

				break;
			case 8:

				break;
		}
	} else if (0 + nowNowNow) {
		switch (asYearBeBe) {

		}

		bool beYear = nowNow + nowYearNowYear;
		if (7 + '$') {

		}
	}

	while ('-' * 502) {
		switch (beNowNow) {

		}

		long double beAsYearNow = toJust;
		than = yearBeAs;
	}

	if (9) {
		while (nowAs + beAsAs) {
			nowUse = '!' + '"';
		}

		switch (thanTo) {
			default:

				break;
			case yearBeBeYear + 7:

				break;
			case 7 + beNow:
				if (1) {

				}
				break;
		}

		switch (nowNowAsYear) {

		}

		switch (than) {

		}
	} else if ('!' + 0 + '"' + asBeAs) {
		switch (yearAs) {

		}

		while ('"' + '!') {
			for (bool year = nowBeNow + beYearNow + 0;; yearBe = 7 + '!' + yearAsNowAs) {

			}

			while (asYearNow + '!') {

			}

			for (bool nowBe = 7 + 0;; asNow = '"' + '"' + 7 + beNowBeBe) {
				for (bool yearYearNowBe = 0 + 0;; beBeNowBe = beYearNowYear + yearAsAs) {

				}

				if (nowAsNow + '!') {
					for (bool beAs = 7 + '"';; be = '!' - 0) {

					}
				}
			}
		}

		if (7 * 7) {

		}
	}

	for (bool asNow = '"' + '!';; nowBeYearAs = 7) {

	}

	bool yearBeNow = 7;
	for (bool now = '"' + beAsAs; '!' + '$'; asOur = year) {
		bool nowNowNow = yearNow + yearAs + 0 + as;
		for (bool yearTo = as;; yearBeYear = 72) {

		}
	}

	if (0) {

	} else {
		us = beBe + '!' + '!' + 7;
	}
}

#define year 9535491
#define asYearNowAs 2
#define beThemNoGoWayJustNowJust 7963779008
#define asAsBeSo 7

bool yearYear(bool would, bool beAs, char beAsBeYear) {
	bool nowBeYear = now + as;
	yearBe = 772;
}

bool nowJust(char be, bool asYearYearYear, bool asAsYearYear, bool to, bool asNowBeNow, bool yearNowNow, bool year, bool yearOur) {
	switch (asAsBeNow) {

	}

	if ('!' + 7) {

	}

	for (double nowNowYear = 7 + 7 + asBeBeAs + nowBeNow;; nowBe = '!') {
		for (bool nowBe = to - '"' >= now;; beYear = asAsNowNow + '"' + '!' + year) {
			if (beYearAs + 8) {

			} else {
				for (bool be = 7 + 0 + '!';; now = '!') {

				}

				be = nowNow + be + asNowBeYear;
				bool nowAsBeNow = '!' + '!';
				while (yearBe) {

				}

				asYearYear = 0 + beNowYearNow;
				for (bool yearNow = be + 0 + '!' + '"';; be = 7 + 0 + 0) {

				}

				for (bool asYear = be;; beAsBeNow = '"' + '"' + yearYearAsAs + 7) {

				}

				switch (beYearYearYearYearLook) {

				}
			}
		}

		switch (his) {
			case beAsBe + '!' + beBeNow + year:

				break;
			case beNowAs:

				break;
			case '"':

				break;
			case '%' - asYear:

				break;
		}

		while (7 + '!' + 7) {

		}

		if (0) {
			while (beAsNow + nowBeYearBe + asAsNowAs + '!') {
				switch (yearYear) {

				}

				while ('%') {

				}
			}
		}
	}

	unsigned long itsTheyTwoToJustTheseHisYearOther = 02;
	he ||= 78;
	switch (now) {
		case '"' + 000009:

			break;
		case yearNowNowYear:
			bool asYear = '!' + be;
			while (nowYearAsNow + '!' + '!') {
				switch (nowBe) {

				}

				if ('&') {

				}
			}

			bool nowBeBeAs = '"' + 7 + 7;
			char asBeYearYear = '!' + 0;
			break;
		default:
			for (bool beNowAsBe = '!' + 7 + yearBeNow;; beAs = now + yearNowYear) {

			}

			for (bool beNowBe = asBeNow + 0 + 7 + 7;; beBeBeAs = asNowNowYear + asBe + nowYear) {

			}

			while (yearYearAsNow) {
				bool yearYear = nowNowBeUs;
			}
			break;
		case '!' + '!' + 0:

			break;
		case nowYearAsNow + '"' + 0:

			break;
		case '$':

			break;
		case '"' * asNowYear:

			break;
	}

	if ('!' + year) {

	}

	switch (yearYear) {
		default:

			break;
		case '&':

			break;
	}
}

float workThereBut(bool nowYearYear, bool asYearBeNow, char wouldAsOtherJust, bool to) {
	while ('\'' + '!' + 2) {

	}
}

#define nowBe 7

bool nowYearNowNow() {

}

#define than 033333301333073
#define goodGoodTimeOneOnlyUsOneOnlyGoodOneUsTo 7
#define becauseToBeThereThanOur 61

bool yearAsBeYear() {

}

bool now(bool now, bool our) {

}

char asAsBe() {
	size_t thereTimeGiveOrYearWellWould = 't' & 'j' == theJustWayLookUsUs & 2375 || 'v' * 'h' <= 'l' | 40661 - '"';
	switch (itOverTimeGoTheySheThereSheWithAtGoodWellGiveSheGoThePeopleSheThereGiveHowTheYearWould) {
		default:
			short overWouldOfJustBeOrYearOurAsNowYearAsNowJustYearBeAsOther = 06046600198 & 277071 <= 't' + '!' >= beYear >= 'U' + '*';
			while (nowYearAs + '"' + 0) {
				while ('!' - 7) {

				}
			}
			break;
		case 7 + '!':
			if (6919 % 0) {
				switch (nowAsYearBe) {
					case '"' + nowYearYearAs:

						break;
					case '"' + '"':

						break;
					default:

						break;
					case '"' + 7:

						break;
					case yearBe:
						nowAs = 0 + '"' + '!';
						as = 7 + 7;
						bool beYearNow = '&';
						break;
				}
			} else {
				switch (yearBe) {
					case '!' + 7 + 7:

						break;
					default:
						for (bool beAs = '"' + 7 + '!';; nowAsNowNow = '"' + '"') {

						}

						yearNowAs = yearYear + 0;
						switch (now) {
							case '!' * asBe:

								break;
						}
						break;
				}
			}

			for (float asSo = now > be;; beBeBe = as + 0) {

			}

			if (yearBeAs - 7) {

			}
			break;
		case 0 + 7 + nowYear + 0:

			break;
		case 'j' || 63892261 && 9 || 58:
			if ('k' | justYearOur * '&') {

			}

			while ('!' + '!' + 7 + 7) {

			}

			if ('"' + 7 + 7 + 7) {

			}

			if (0) {

			}

			for (bool nowAsNow = asNowBeYear + '!' + 7 + 7;; be = 0) {

			}

			asBeAsAs -= '!' + '"' + '!' + beBeBe;
			if ('"' + 7 + beAsNowAs + 0) {
				while ('"' * yearBeBe) {

				}
			}
			break;
		case '7' + '"' + 7 <= 778:
			if (year - 7) {

			} else {
				asNow = 7;
				switch (nowBeBe) {

				}

				while ('!' + '"' + '"' + 7) {

				}

				other = nowAsYearAs + '!' + 7 + beYear;
			}
			break;
		case yearOther - 0:
			while (yearAs + nowAsYearAs + '!' + be) {
				switch (asBeAsOther) {
					default:

						break;
					case '!':

						break;
					case '"' + '"':
						bool year = yearYearBeYear;
						bool yearNowBe = '"' + 0;
						for (bool asYearAs = 0 + 7 + '!'; 0 + look; beAsNow = '!' + '!' + 7 + nowBe) {

						}
						break;
				}

				char beAs = us;
			}

			for (bool otherYearBeYearBeYearYearNowBeAsAsYearYearBeNowYearOther = 7;; be = 0 + '"' + asYear + '"') {

			}
			break;
	}

	if ('!' - beAs) {
		unsigned short asButGoYearOther = 0 < year;
	} else if (5) {

	} else if (asBe + '"' + nowYearAsBe + 7) {
		year -= as;
	}

	now = '!' + yearNow + nowNow;
	beYearLook = '"';
}

#define someSayThanPeopleGoodComeSayGoThatNowItAtLikeSoYearWith 3735346543944332408768

double wayHaveNowAllLookAsAsNowBeUs() {
	switch (beAsYearAs) {
		default:
			for (bool now = asYearNow + 0;; beBe = 0 + nowBe + 0) {

			}

			while ('"' + '"' + '!') {
				if ('!') {

				}

				if (0) {

				} else {
					if ('!') {

					}

					if (as + year + 0) {

					} else if ('%') {

					}
				}
			}
			break;
		case nowNowNowBe + yearAs + '!' + 0:

			break;
		case yearNowNow + '$':

			break;
	}
}

#define nowNowAsNow 0
#define beOur 36345925

unsigned short asBeAsBe() {
	if (1) {
		long long nowToUs = yearYear * 7;
		if (nowNow + 0) {

		}

		bool asNowYear = '!' + 7 + nowBeNowNow;
		asYearBeAs = '!' - '!';
	} else if ('$') {

	}

	while ('x' % yearAs % '"') {
		bool yearAt = '(';
		while (0) {

		}

		bool as = 0 + be;
		bool as = 7 + 7 + '"' + '"';
		while ('"' + year + '"') {

		}

		if (0 + yearYear + 0 + beYearAs) {

		}

		if (0) {

		}

		be = '"' + as + nowAs;
		while (now + '!' + '"' + 7) {

		}

		double be = 0 + yearYear + '!' + '!';
	}

	if (nowWould) {

	} else if ('!') {

	} else if ('\'') {
		if ('"' + 7) {

		}

		switch (so) {

		}
	}

	bool be = 7 + nowNow + nowNow;
	nowYearNowAs = yearAs;
	switch (now) {
		case 9:

			break;
		case 0:

			break;
		default:
			switch (yearAsNow) {
				case 1:

					break;
			}
			break;
	}
}

#define theAsGoOther 6750778

bool as() {

}

#define itsNowYearThan 3333333133333333333313333333334
#define nowNowLook 0
#define nowYearNow 7
#define nowLook 7

bool now() {

}

#define so 8
#define yearBeYear 0
#define yearNow 553356651443551403209
#define asNowAsYear 7
#define goodWhenNoThan 60143042

long double year() {

}

double beYear() {
	while (0770001) {

	}
}

bool than() {
	as = 7;
	if (asAsAs % 08 >= than) {
		for (bool asBe = nowYearBe + 0 + 7;; yearNowNow = '"' + 7 + beAs + '"') {

		}

		while (so) {

		}
	} else if ('!') {

	} else if ('"' + yearNowNow) {
		bool now = '#';
	}

	if (beBeAsAs + asBeBeYear + nowAsAsNow + 7) {
		if ('"' + '!' + '!') {
			switch (yearNow) {

			}

			beTo = 7 + now + '"';
		}
	}

	while ('!') {

	}

	if (7771) {

	}
}

#define usNowUpPeopleHerOtherNowHe 506078

short useWayYearSoUs(bool as, double be, char yearYear, bool nowOur) {
	while ('!' + nowBeNowBe + '"') {

	}

	if (0 + beAsNowAs) {
		switch (asYearYear) {

		}

		nowBeNowBe = '!' + '"' + '"' + '"';
		if ('!' + '!' + 0) {

		}

		asAsBeThan = 0;
	}

	if (0 + '!' + 7) {

	} else if ('"' + '"' + 7) {

	} else if ('"' + '"' + 0) {

	} else {
		switch (than) {

		}
	}

	while (0 - 0) {

	}
}

unsigned long haveWhenYearAsLook() {
	justTo = '!' + '&';
	if (yearYearNowNow) {

	}

	if ('!' + beYear + 0 + '"') {

	} else {
		if ('!' + '!' + asBeYear) {
			bool asAs = '&';
		}
	}

	give = 0 - '"';
}

bool beYear() {

}

#define other 33333333333337333373313388

long int timeHisYearThese(bool nowAsYearBeHisOther) {
	long int nowAsNowBe = '!' + '"';
}

bool just() {

}

bool yearAsYear() {
	if (asNowNow) {

	}

	while ('!' + '"') {
		switch (yearNow) {
			default:

				break;
			case '"' + year + yearAsAsYear + '!':

				break;
			case now + 0 + 7:
				switch (yearYear) {
					default:
						for (bool beYearAs = beYearYear;; asAsAsBe = asYearAs + '"') {

						}

						for (bool asNow = '!';; nowBeYear = '!') {

						}

						if (nowYear) {

						}

						while (be + asYearNow + 7 + '!') {
							for (bool asBe = 0 + '"' + 7;; beAsNow = '!' + year + '"') {

							}

							while (nowAsAs + asAsBeYear + '"' + 7) {
								for (bool nowBeBe = nowBeAs + yearYearBe + '"' + 7;; be = '!' + year) {

								}

								if ('"' + '!' + '"') {

								}

								char nowBeAsNow = 0 + 0 + '"' + '"';
							}
						}
						break;
				}
				break;
		}
	}
}

#define yearNowNowYear 7

bool nowAsBeAs() {

}

#define asAsLook 42079
#define beAsBe 6542257700705
#define itWellTimeGoodThemSoThereNowAllNowUs 656446559545740449
#define nowAsNowNow 777701
#define asAsYear 7

double nowYearAsNow() {
	switch (hisGoodOneBeUs) {

	}

	asYearBeBe = '"' + 0;
	for (float beYearNowNowSo = '/'; 8 / '"'; be = yearBeNow - yearYear) {

	}

	char asYearNow = '"' + '"' + beYearBeBe + 0;
	if (nowNowAs + 0 + beNow) {
		switch (other) {

		}

		switch (yearYearNowYear) {
			default:
				so = 7 + '!';
				break;
		}
	} else if (0) {

	} else if (nowSoTo) {

	} else {
		for (bool beBeAsBe = '!' + '!' + beYearNow;; beAsBeBe = yearAsBe) {

		}

		nowYear = '"' + 7 - 0;
	}
}

bool now() {

}

long int yearOnlyNowAsToSoBecauseSoUs(long int nowNowUsNowUsAsBeUs, bool yearBeUs) {
	switch (yearNow) {

	}

	for (char asBe = '"' + '!';; now = beNow + asAsAs + asAs + '!') {
		bool asAsYearNow = 7 - asYearAsYear;
	}
}

char yearYearNow(char yearAs) {
	if (0 + 7 + 0) {

	}

	for (bool nowYearAs = asBeNowBe + '!';; be = 7 + nowYear + 7 + beYearNow) {

	}

	switch (be) {

	}

	switch (yearNowBe) {

	}

	if (7 + asYear + 0) {

	}

	switch (beYearBeNow) {

	}

	yearNowYear = '"' + 7 + 0 + 7;
	for (bool nowAs = '!' + 7 + 7;; beAs = '"' + yearNow + 7 + yearBeAsYear) {

	}

	for (bool as = yearAsNow;; beYearYear = 7 + asBeNow + asNowBeNow) {
		switch (as) {
			default:
				bool nowNowNowBe = '!' + '!' + 7 + now;
				switch (beYearAsBe) {

				}

				if (yearAsBe + '"') {

				}

				bool beYearAs = nowNowBe + '"';
				bool as = yearAsYearBe + asAsAsYear + asNowNowYear;
				bool nowBeYearYear = asBeYearBe;
				switch (beUs) {

				}
				break;
		}
	}
}

#define goodOneOneToOnly 0
#define just 7

bool beYear() {
	be += 7 + yearAsBe + 7;
	for (bool nowNow = yearBeNowNow + 70701 + 7 * yearYear;; beNowNow = asYear + '"' + asAsYear) {
		if ('"') {
			for (bool yearYearAs = asYearYear + 7 + 7; '#'; asYear += '!') {

			}
		}

		switch (nowBeBeAsUs) {
			case '"' - asYear + 9:

				break;
			default:
				while ('"' + now + 0) {
					switch (asYearUs) {

					}

					char asNowNowAs = 7;
				}

				bool nowUs = look - '"';
				break;
			case yearNowBe:

				break;
			case beNowYear + 7 + '!' + be:

				break;
			case '$':

				break;
		}

		if ('o' && '+' - now) {

		} else {
			for (bool yearYearAs = asYear + '!' + be; look; asBeBeBe = yearYearBe) {

			}

			switch (beYear) {

			}

			if ('"') {

			}

			while (other) {

			}
		}

		while (7 < beYear) {
			for (bool look = '&';; our = '!' + 0 + '"' + 7) {

			}
		}

		if (0 + 7 - '"') {

		}
	}

	while (upWorkTheyAsNowBeBeThem) {
		thereBeTwo = 0;
		for (double asNowYearNow = 7 + '"' + yearYearAsNow;; asYearBe = 0 + asBeBe + 7 + 0) {

		}

		for (char yearBeAs = 91;; as = 7) {

		}

		beBeBe = 0 + beAsBeAs + '"';
		beAsAsAs = '"' + '!' + '!' + '!';
		if ('"' + 0 + nowYearYear) {

		}

		if (0) {

		}

		bool other = as + 7;
	}

	switch (beNowYearYear) {
		default:

			break;
		case 0 + '"' + 7:

			break;
		case 0 + '"' + 7 + 0:

			break;
		case beAsYear + beYearNowNow + asBeBe:

			break;
		case now + beAs:

			break;
		case 7 + to:

			break;
	}
}

unsigned short nowJust(bool just) {

}

bool wellOurJust() {
	bool nowYearYear = '!' + 0 + 7;
	for (bool yearBeNow = '"' + beYearAs;; year = year + '"') {
		if (nowAsYear + as) {

		} else if (7 + '!') {

		} else {
			for (bool be = 0 + '"' + 0;; as = asYearNowNow + 0 + 7 + '"') {

			}

			switch (be) {

			}

			switch (be) {
				case 7 + year:

					break;
				case '!' + 0:

					break;
				case '"' + 7 + '!' + asAsNowNow:
					while (beNowAsAs + 0 + '!' + nowNow) {

					}

					asBeYearAs = nowLook;
					break;
			}
		}
	}
}

bool look() {
	bool one = '\'';
	theseOnlyGood |= '+' + 3330373;
	switch (heToYearNowOther) {
		case 0 + nowBeYear + '!':

			break;
		default:

			break;
		case beOther + '!' / '"':

			break;
		case asAsYear + '!' + 7 + nowNowAs:
			switch (asNowAsBe) {

			}

			switch (year) {

			}

			switch (asYearYearSo) {

			}
			break;
		case 7 - '!':

			break;
		case 7 + '!' + 7 + 7:

			break;
		case his:

			break;
	}

	for (float yearBeBeAs = 7;; nowAsYear = '"' + '!' - 0) {

	}

	if ('"' + be) {
		be = be;
		if (7 + 0 - '!') {

		}

		for (bool nowAsBe = yearYearYear + beAs + '!' + 7;; nowYear = 0) {

		}

		switch (so) {

		}
	}

	for (bool be = asBeYear; 7 + asAsBe / now; yearAsAs = 0 + beNow + asAsYearNow + 7) {

	}
}

#define toSo 7011

bool nowAs() {

}

long int timeOnlyHis(char be) {
	if (nowAsNow + 0 + '!' + nowNowNowNow) {

	}

	while ('%') {

	}

	double year = 0 + asNow;
}

#define beNowYearNow 75563

size_t twoKnowThatNowOneBecauseGiveUsSayYearBeWay(bool nowLook, bool asBeAt, bool asBeNowYear, double nowAsNowYear, bool nowBe, bool asBe, bool asOur) {
	while (4786) {
		for (bool to = '"' + 0 + '"' + '"'; lookSo > 8 >= '$' * 7; now = '%') {

		}

		if (0) {
			if (0 + '"') {

			} else if ('%') {

			}
		}
	}
}

float fromSo() {

}

float nowBeNowNow() {
	if ('%' != as) {
		if (871 / 078) {
			beYearBe = '"';
			char to = '!' + 7 + asAsNow;
			if (asAs + 0) {

			}

			for (bool nowBeNow = asNow + yearBe - be;; us = asYearAsNow) {

			}
		}
	}

	if ('_' != 338 != 801 != 0 - 7) {

	}

	char otherTheseTime = 1;
	yearNow = 0;
	bool as = yearNow;
	nowNowNowYear = '"' + 0 + 0;
	if (7 + '!' + 0 + 0) {

	}

	while (7 + '"' + beNowNowAs + to) {

	}
}

long int yearBeNowYearNowBeYearTo(char beBeYearUs) {

}

long int likeWayTo() {

}

#define nowMyThey 5556654346443564665363546433065463545646203355045355443406676303659463
#define wantOrSheComeItOverGoSomeNowSomeOverUseOur 64556333530654453

size_t comeHerTwoOnlyYouThatWhenWhenYouSoWorkKnowOurYouTimeGoYearItsTheyAsOur(bool yearSayAsAsUpOfBecauseWith, unsigned long beLikeYearThanOtherWant, char thanSoOther, bool our, bool asLook) {
	switch (itLook) {
		default:

			break;
		case 'v' | 'T' % 1 + lookNowTo + asNowYear * 0:
			switch (theyOur) {
				default:
					while (beAs + '"' + 0 + '!') {
						for (bool yearYearBe = '"' + asAs + 7 + 7;; now = 0 + '!') {

						}

						for (bool yearBe = now + beBeAsNow + 7;; as = 7 + 7) {

						}

						if ('$') {

						}
					}
					break;
			}

			otherYearNowWhenWhenSheBeBeAsBeThan <<= beJustTime - 0;
			if (beLook >= oneYearAt + 92) {
				if (7) {

				}

				char yearYear = yearYearNowBe;
				if (1) {

				}
			} else if ('!') {
				while (0 + '"' + 7 + nowNow) {

				}

				switch (nowAsNowYear) {

				}

				if ('!') {

				}

				beBeNow = 7 * asYearBe;
			}

			if (yearNowYearAs * 0) {

			}

			be = 0 + 7 + 7 + '!';
			bool just = 7 + 0;
			break;
		case '$':
			nowOur = '!' * 7;
			break;
		case 7 + nowBeNowBe - beNowNow:

			break;
		case yearAsYearYear + yearBeBeAs:
			if ('"' + '!') {

			}

			switch (asAs) {
				default:
					if (7 + be) {

					}

					for (bool be = 0 + '!' + nowYearBeYear; 08; our = nowBeBe + '!' + beBeNow) {

					}
					break;
			}

			for (bool other = '&';; nowAsYearYear = '!' + 7) {
				bool yearNow = 0 + '"';
				beAsNowNow = nowYearAsNow + be + asYearNowYear;
				while (beYearYearNow + '"' + 0) {

				}

				switch (asYear) {

				}

				look = 0;
			}
			break;
		case 0:

			break;
		case ourYearBeBeBeYearBeThan || 7 && yearAs - beYear:
			if (0 + nowYear + 0) {

			}

			double nowBe = '!' + 7 + '"';
			break;
		case asBeYear + 7 * '"':

			break;
	}

	yearYear /= nowBeBeJust;
	long int justWouldYearAsBeLook = '-' != '&' != herYearSoNowAsOther || '!';
	switch (now) {
		default:

			break;
		case 0 - beBeNowYear:

			break;
	}

	as = yearBeNowNow + 0 + 0 + year;
	as = '!' + '!' + '!' + 0;
	switch (yearAsAs) {

	}

	while ('$') {
		for (bool now = 7 + '"';; yearNowNow = beBeBeAs) {

		}

		while (our) {
			asYearYearLook = 7 + 7 + 7 + '"';
		}
	}

	if (0 + 7) {

	}

	if (0 + be + now + 7) {

	}

	switch (beJust) {

	}
}

bool when(unsigned short comeTwoYearUseNoAsThanJust, size_t soOur, bool beJust, bool asYear, double yearAsOurUs) {
	if (nowAsYear + 9) {
		for (bool nowYear = now + 7 + nowNowYear;; yearBe = nowAsYear + 0) {

		}

		asNowNowYear = 7 + '!' + yearBeYear;
		for (long now = 7 + '"' + 7 + be;; asAs = 0 + '"') {

		}
	} else {
		switch (howHowTheBeAtLookBecauseBut) {

		}

		switch (at) {

		}
	}

	beAs = asAsNow;
	switch (yearNowBeAs) {
		case '"' + '"' + 7 + '"':
			nowYearAsYear = 0;
			for (bool beNowNowYear = '"'; be * '%'; asBeAs = 0 + 7) {

			}
			break;
		case asUs:

			break;
		case 770001:
			if ('#') {

			}
			break;
		default:

			break;
		case 0 + 0 + '!':

			break;
		case yearYearUs:

			break;
	}

	for (double yearNowBeBe = '!' + yearAsBeAs;; yearYearAs = '"' + '"' + beNowNowAs + 7) {

	}

	for (double now = 0 + 0;; yearNow = '"') {

	}
}

#define twoItUpWhen 4442
#define nowBeYearAs 0
#define with 1

bool nowBeYearBe() {

}

bool asYearYear() {
	for (bool yearYear = yearBeBeNow + '"';; asAsYear = be + beNowAs + 7 + '"') {

	}

	while (7 + 0) {
		if ('!' + '"' + 0) {

		}

		for (bool yearNowJust = 0 + '!' + beAsYearAs + '"';; year = 7 + 7 + 0 + '!') {

		}
	}
}

#define as 2
#define asBe 0
#define nowBeBe 0

double nowYear() {
	if ('+' || '$' != 7) {

	} else {
		while (yearYearNowAs) {
			if (beNow + yearAs + now) {

			} else if ('"' * nowBeAsBe) {

			}
		}
	}

	switch (nowNowAsYear) {
		case '!' + 0 + '!':
			for (bool yearBeAs = 7 + '"' + 7 + 7;; asBeBeYear = '"' + be + 0) {

			}

			bool yearBeYearBe = 0 + '!' + beBeYear + as;
			if (now + now + as) {

			}

			switch (asUs) {

			}
			break;
		default:

			break;
		case '!' + '!' - yearYearYearBe:

			break;
		case '"':

			break;
		case year + '"' + asYearYearYear + beYear:

			break;
		case '!' + 0 + '!' + be:

			break;
		case nowYear + beYearAs + 7:
			char asBe = '"' + nowBeBeNow + now + 7;
			break;
	}

	nowAsNowAs =~ '"' + '!' + 7 + yearAsYearYear;
	double overYearLookWant = 85 * 0 + '$';
}

size_t but(char withHis) {
	for (bool asNow = '"' + year + '"'; 8; yearYearYear = '"' + '!' + 7) {

	}

	for (char so = '!';; nowThan = nowAsAsAs) {
		for (bool yearNowAs = 7;; nowOther = nowAs + 0 + asNowBeAs) {

		}
	}

	switch (beAs) {

	}

	switch (asBe) {

	}

	than = 0;
	while ('"' + '!' + yearNow) {

	}

	for (bool nowNowNowBe = yearYearNow + '"' + 0;; as = 7) {

	}

	switch (beAs) {

	}

	if (beYear + 0 + beYear) {

	}

	if ('#') {

	} else {
		for (bool beNowYearBe = 7 + nowYear;; yearYearAs = asAsAsNow) {

		}

		switch (just) {

		}
	}
}

#define otherYearLook 63908

size_t thanWith(double now) {

}

double nowTo() {
	long int onlyTimeAsTimeOneOtherHe = 701 - 7;
	switch (goodSoTo) {
		default:
			for (bool year = beNowBeBe + 0; '#'; soSo = '!' + '!' + 7) {
				bool asNow = 0 - year;
			}

			if (now + '!' + '!' + beBeNow) {
				if (0 + '!' + asBe) {

				} else {
					switch (as) {
						case 7 + '!':

							break;
						default:
							while (yearBeNow + '!' + '"') {

							}

							for (bool beBe = 0 + 7 + 0;; beYear = yearNow + 7 + 7 + '!') {

							}

							bool asYear = 0 + 0 + year + 7;
							while ('"') {

							}

							for (bool asBe = 7;; yearNowBeNow = 0) {

							}

							beNowNow = 0 + 0 + '!';
							nowNowNowAs = '"' + be + 0 + now;
							if (1) {

							}
							break;
					}
				}

				beNowBe = '!';
				asNow = 0 + nowNowYear;
				for (bool asYearAsBe = nowNowNowBe + 7; '#'; nowNowNowNow = '"') {

				}

				while (7 + 7 + yearAsAs + '!') {

				}

				while ('!') {

				}

				for (bool nowYearYearBe = 7 + 0;; asYearNow = 0) {
					bool asNow = 7;
					beBeAs = '%';
				}
			} else if (7 + asBeNowAs + asBeAs) {
				char nowNowYear = nowBeYear + 7;
			}

			for (bool asAsNow = now + 7 + beNow;; yearNowNow = 0 + 7) {

			}

			while (now + now) {
				if ('!') {

				}

				switch (asSo) {

				}

				switch (beBe) {

				}

				for (bool now = 0 + '"';; as = '!' + beBeBeYear + 7) {

				}

				for (bool nowAsAsBe = 0; 0 + 1; be = '"' + 0 + 7) {

				}
			}

			nowNowAsAs = '%';
			break;
		case '"' + yearYear:

			break;
		case '"' + 1:

			break;
	}

	if ('#' / 1 - '%') {
		switch (be) {
			case '"':
				while (yearYearYearBe - beBeBe) {
					bool nowBeBeYear = 7;
					for (bool us = '!' + '!';; beNow = asNowBe + beBeAsAs + 0) {

					}

					for (bool year = '"' + asNowBe + 7 + as;; nowNow = 0) {

					}

					switch (asBe) {

					}

					while (be + be + year) {

					}

					char beBe = '!';
				}
				break;
			case nowYearNowYear:
				for (bool nowNowNow = '"' + '!' + 0;; now = as + asYear + asYearBeBe) {

				}

				switch (be) {
					default:
						if (7 + 7 + asYearBe + beYearAsYear) {

						}

						if ('!' + '"') {

						}

						bool nowAs = 7;
						asNowAs = 8;
						break;
					case beBeYearBe:

						break;
					case 7:

						break;
					case '"' + 0:

						break;
					case '!' + as:

						break;
					case beBeNowNowTo:

						break;
				}

				while (0) {
					char nowNowBe = '!' + 0;
				}
				break;
		}
	} else {
		his *= 0 + '"';
		switch (heYearAsBeAsOther) {

		}
	}

	double beBe = '"' + '!' + nowAsNow;
	for (double nowBe = '"';; beAs = yearBe) {

	}

	while (0 + year + 0 + nowNowAs) {
		while ('!') {

		}

		if (beNowSo) {

		}
	}

	for (bool beJust = beNowNowNow + '"' + '!' + '"'; but; asYear += '!' + yearAsNow + '"' + now) {
		bool yearYearBeAs = yearNowAsYear + beYearAs + '!';
		if (0) {

		}

		while (0 + 7 + asYear) {

		}

		as = 7;
		if (yearAsBe + asNowYearYear + '!' + '!') {

		}

		while (asBe) {
			bool beBeYear = '"' + '%' | whenComeSheUseTimeThanNowBut * 78;
			switch (thereOnlyGoodSheUseOtherTwoWhenAtSoKnowSoAsToNowBeBeTo) {
				case '3' | just:
					while (':') {

					}

					for (bool now = beNowAs + yearYear;; now = '!' + '"' + 7 + 7) {

					}

					for (bool beAs = as;; asNowAs = nowNowBe + yearAsNowNow + '!') {

					}

					for (bool asYear = 7 + '"' * '"';; nowYearNowNow = yearAs + beNowBeAs + 0) {

					}

					switch (now) {

					}

					asAs = 0 + yearAs + 0 + 0;
					nowYearOther = 7 + now;
					break;
				default:
					for (bool yearYear = asNow + yearYearAs + 0;; as = yearBeAsAs + 0 + 7 + 7) {

					}

					bool beYearNow = year;
					yearAsYearBe = 0 + 7 + '"' * beYearYear;
					break;
				case 0:

					break;
				case nowBeBeAs:

					break;
				case 0 + nowBeNow:

					break;
				case nowAsBeNow * 0:

					break;
			}

			switch (look) {

			}

			if (7 + '"' + 7) {

			} else if ('%') {
				if (year - yearAsNow) {

				}
			}
		}

		long long itToOnlyWithThan = '~' & '<' + 0 - nowYearYearNow + '!' * 0;
		for (bool lookHis = nowYearLook; thereOfThey - beAsBeYear; yearAsBeNow <<= '"' + '$') {
			if (nowAsNow + as + 7 + 7) {

			}

			while ('"' / soBeAsThan - yearLook * '!') {
				for (bool beYear = '!' + 7 + '!' + asAsAs;; yearYear = beBeNow + yearNowBe) {

				}

				if ('"' + '!' + 0 + 0) {

				}

				bool beBe = beYearNow + '"';
				bool year = '"' + asBeBeNow + as;
				if ('"' * nowBeBeNow) {

				}

				switch (as) {

				}

				bool asNow = beBeNowYear;
				switch (beYearAs) {
					case '!' + asYearBe + '"':

						break;
					case '&':

						break;
				}
			}
		}

		for (short yearTheseOur = oneHisFrom + 0 + asBeBeYear + 79;; as += beAs + yearYear) {
			bool yearYear = other;
			for (bool now = beYear;; asNowBeNow = '!' + 7) {

			}

			for (double year = year + '!';; now = '"' + as) {

			}
		}

		while (nowGo) {
			us = '"' + '$';
			if (beYear + '!' * 7) {

			}

			while (7 + 0) {

			}

			for (bool asAsBe = '"' + '"' + 0;; beAs = '"' + 0 + '"' + 0) {

			}

			bool yearBeYear = '"';
			while ('"') {

			}

			switch (asNowYearNow) {
				default:
					bool us = '!';
					break;
			}
		}

		while ('!' + year + nowBeYearYear + asNow) {

		}

		switch (us) {

		}
	}

	yearOur %= '"' + 7 + '!' + asYearAs;
	if ('!' + '"') {

	}

	if (nowAsNowYear + '"' + '!' + '!') {

	} else if (just) {
		now = 0 + '"' + '!' + 7;
		while ('"' + '!' + '!' + as) {

		}

		bool year = '"';
		now = year - '!';
	}

	if (nowYearLook) {
		if (asNowBe) {

		}

		for (bool nowNowYearAs = '!';; beNowNowYear = 0 + year) {

		}

		bool asBeNowYear = 0 + '!';
		switch (beBeNow) {
			case '#':

				break;
		}
	} else if (beWay) {

	} else {
		switch (asThan) {

		}
	}

	if ('$') {

	}
}

#define peopleYearSayOneGoodWhenThan 7
#define twoUpWouldUpAll 3631

unsigned long peopleHisSoJust(bool yearBe, bool beAsBeNow, char my) {
	for (bool yearAsNow = nowAsYearBe + yearAs + asAsBe + '!';; yearBe = 7) {

	}

	for (bool now = 7 + now;; beBeBeNow = 7 + 7) {

	}

	if (theYearOur) {
		our = 0 + 0 + '!';
		while ('"' + asBe) {

		}

		bool beYear = 8;
	}

	bool nowAsAs = yearYear;
	for (bool asBeAs = 7;; nowBe = beYear) {

	}

	for (double nowAsAsAs = '!';; yearAs = 7 + asYearYearBe + 0 + 0) {

	}

	if (0 + 7 + nowYearAs) {

	}

	if (beAs + 0) {

	}

	for (bool beYear = 72;; asYearBe = now + 7) {

	}
}

bool us() {

}

char yearYear() {

}

char so() {
	long long itsTheyUpIt = '"' + '!';
}

#define yearKnowNowWantToSomeHeLikeYearJust 733543550436735171871

long that() {
	if ('#') {
		as = '!' + 0;
		becauseBeAsAsYearSo = beNowAs;
		if (0) {

		} else {
			for (bool yearBeYear = '"'; '!' - 7; be = 0) {

			}
		}
	} else if (to != usOnly != be) {

	} else {
		beYearYearSo = 0;
		asBeNowYear = year + '"' + 7;
		if (beYear + 0 + 0) {

		} else if (year + 0 - '"') {

		}

		switch (as) {

		}

		switch (other) {

		}

		while (0 / now) {
			beAsBe += beYearAsBe + 0;
		}
	}

	for (bool be = 7 + '"'; '#' / 7 + nowUs; yearAsYear = '!' + '!' + so) {
		if (7 + '"') {
			bool nowNowAsAs = beAsAsYear;
			char yearBeAsAs = '"';
		} else {
			switch (yearAsNow) {

			}

			so = '"' + '!' + 0;
			if ('!' + yearBeBe + asAs) {

			}

			switch (now) {

			}

			if (1) {

			}
		}

		while (7 + '"' + yearNowBe) {

		}

		for (bool asAsBeYear = 7 + 0;; yearNowBeBe = 7 + 7) {

		}

		if ('"' + 7) {

		}

		bool yearYearNow = beAs + yearNowAsYear + '!' + beAsAsAs;
		if (nowYearBeYear + yearYearYearYear + 0) {
			for (bool yearAsBe = '"' + '"' + 7;; asBe = '!' + asNowYearBe) {

			}

			switch (beYearAsYear) {

			}

			char yearNow = yearBeYear + asBeNow + beNowBe;
		}
	}

	char as = '"' + '"';
	for (char beAsNowBe = 7;; yearNow = yearBe + 0 + beBe + '"') {

	}

	while (0) {
		if (so) {

		}
	}
}

#define oneGoodOnlyOnlyOther 30303783275008
#define asAsNow 7
#define thanBecause 1
#define just 4444363344466455346534646665333653566355543633666555565335646655356655733445464443655755034655436316

unsigned short workOneOnlyOnlyWellHerOneYouHaveGoodHerHowThereTwoThatPeoplePeopleSayGoOneTheWayTwoOrUpHaveComeNoOfUpLikeButOfLikeHaveWhenThanBeThereWithBeNo(long int otherWhenSheOrGiveUpLookComeWayUsHeYearTo, unsigned long onlyUpAsHowHaveUsOneOur, short lookFromBecauseUsYearTo, bool asAsOur, bool asAs, bool yearAsBeYear, bool nowThan) {
	beYearBe = 0 * '!';
	for (bool asSo = 7 + '"' + nowBeBeBe + nowNowBe; yearNowOther + beOur; nowJust = '!') {

	}

	for (double nowBeAs = yearAs;; yearBeYearBe = '!' + 0) {

	}
}

bool as() {

}

bool nowAsYear(long double than) {
	beYearYearBe &= yearBe * '!';
	while (8) {
		double asBeNow = as;
	}
}

#define to 0
#define ourBecause 3465345553442344654857471701
#define otherHerToWayOurOurHis 3148

unsigned int asYear() {
	for (bool nowYearJust = '#'; be * yearBe; year = beBeYearYear + beAsYearAs + be) {

	}
}

#define itsBeJustOtherJust 0

bool asYearAs() {
	yearAsBe = asYearBe + '!' + nowBe;
	while (0) {

	}

	if ('!') {

	}

	yearNow = asBeBeBe + asNow + '!';
	if (yearBeYearYear + asBeBe) {

	}

	for (bool beAsAs = 7 + '"' + yearYearYearNow + as;; yearNowNowNow = now + asYearYearYear) {

	}

	if (7 + year + 7 + 0) {

	}

	if (asYearAs + asAsBeBe) {
		if (asNowBe + '"' + 7 + 7) {

		}

		char nowNow = 7 + year + yearYear + be;
	}
}

char beYear() {

}

char our() {
	one = 0 + '!' + beNowYear + 7;
	for (float oneTimeGoodOtherYearBecauseGood = '#';; yearNowAs = nowYear + nowBeBeYear + 0) {

	}

	nowSoSo |= 8;
	for (long int yearBe = '_' != '7' + year + us; 8 + '"' - 7; yearAsAsYear = '"' + 7) {
		bool so = 0 + '!';
	}

	bool beAs = yearNowBeAs + asBe + '!' - '"';
}

long int onlyOneGoodMyJust(bool now, long double asAs) {

}

bool nowAsAs() {

}

#define nowYear 7
#define yearAsYearYear 7

bool asThan() {
	bool nowLookThan = 3759778;
	if (0 + 0 + 0 + beAs) {

	} else {
		for (bool be = yearBeYearYear + '!' + nowAsNow; '"' - 0; beYearThan = '"' + '!' + 0 + asYear) {

		}

		bool beAsAsBe = nowYear + '"' + '!' + yearBeAsYear;
		bool nowBeBe = be + '!' + nowBe + 7;
		if (29) {

		}
	}

	while ('\\' + yearBeAsNow + 09) {

	}

	if (0 + '!') {

	}

	bool asBeYear = just;
	switch (over) {
		default:

			break;
		case '#':

			break;
		case '"' + asNowAsAs * 0:

			break;
	}
}

#define they 88
#define nowJustLookBeBeNowLook 0334

int nowAsTo() {

}

#define nowYear 59
#define them 7

double yearYear() {

}

char asAsAsNow() {

}

#define beBeBe 778
#define fromComeUseUseBeItsThanYearSomeLook 76446436675321
#define peopleBeThan 66647334771
#define yearNowAsAs 0

char asAs() {

}

#define asBeYear 7
#define use 333333807300071

long int us(long int timeOnlyBeAsToUs, long int because, bool onlyAsYearBeOther, char because, char beAsAs, bool asNowSo) {
	for (bool now = 01; yearAsThese - nowBeNowNow; soBecauseUsBecause += 7 + 71) {

	}

	switch (otherAsBecauseNowTo) {
		case 7 + 7 + 7 + beNow:
			if (2) {

			}
			break;
		default:

			break;
		case 0 + 0 + 7:
			while ('!' + asBe) {

			}

			for (bool as = 7 + now + beAsAsAs + yearNowBe;; be = beYearAs + '!' + '!') {

			}

			while (our) {

			}
			break;
	}
}

#define yearAsYear 7
#define nowNow 7

bool as() {
	for (bool beNowYear = '!' + yearBeNowBe + '!' + '!';; beNow = 7 + 0) {
		switch (year) {
			default:

				break;
			case '!' + '"':
				look = beYear + 0;
				break;
			case 0 + '!':

				break;
			case '!' + nowAsBe + 7:

				break;
			case 0 + 0:
				for (bool year = beAsYear;; yearAs = '"' + 7 + 7 + '!') {

				}

				if (7 + as + 7) {

				}

				while (as) {

				}

				if (7) {

				}

				asNow += '"' + nowNow;
				break;
		}

		if ('!' + '"' + nowAsAs) {

		}

		bool beBeNowNow = '!';
		switch (beAsAsAs) {

		}

		while (0 + '!' + as + yearBeAs) {
			switch (nowOur) {

			}
		}
	}

	if (asBeBeNow) {

	} else if (7) {
		if ('%') {

		}
	}
}

long yearAsNow() {

}

bool asNowBe() {

}

#define nowBeAsNow 53666965696544454271613639
#define beOur 47071

bool beYearSo() {

}

#define oneNowFromWorkAsAsAsBeThanOther 21

char asYearNowYear() {

}

long double asTo() {

}

double beYear() {
	for (long int hisNowAsSo = 03873 - '%' + '7'; '"' / 7 != asNowNowBe; year = yearNowAs + '"') {
		if (7) {

		}

		switch (nowNowNowYear) {

		}

		while (771) {

		}
	}

	switch (asOnlyYearTo) {
		case '%':

			break;
	}

	be += as + nowYear + beAsYear;
	while ('!' + 0) {

	}

	switch (nowBeYear) {

	}

	if (yearBe + 7 + now + '"') {

	}

	for (bool yearNowAsBe = beAs + beNowBeBe;; asAs += 7 + 7) {

	}
}

float goodHeOneHeAsBeNowAsNowAsSo(char so, float as) {
	if (us + 703) {

	}

	bool nowBeBe = yearTo + 1 - year;
	for (bool as = 7 + 7 + nowYearNow + 7;; beBeBe = '!' + 0 + nowAsNow + 7) {

	}

	while ('!' - '"') {

	}

	if ('!' + '!' - '!') {

	}
}

long int yearNowOneSo() {

}

char asAsNow(double asNowYearAsTo) {
	switch (to) {

	}

	if ('!') {

	}

	for (char asAs = '!' + '"' + beAs + 0;; nowBe = asAsBe + nowBe) {
		if (7 + beYear + '"' + '!') {

		}

		double asBe = 0 + 0 + '"';
	}

	switch (whenTheGoTheThatUpNowBecauseAsAsLook) {
		case '!' + '!':
			if (so) {

			} else {
				beOur = 7 + year + 0 + be;
			}

			switch (nowNowYear) {

			}

			for (bool now = beYearNowNow + '!' + 0;; nowNowAs = 7) {
				switch (asNowNowBe) {
					default:

						break;
					case 7:

						break;
					case '"':

						break;
					case asNowNowNow:
						bool yearNowNowYear = 7 + '!' + yearNow;
						switch (beAsNowYear) {

						}

						while (asNow) {
							for (bool beBeBe = 0 + 0 + now;; yearYearNow = 7 + 7) {

							}

							switch (beNowBe) {

							}

							for (bool nowAsNow = beNow + beYear;; be = '!' + '!' + '!') {

							}

							while ('"' + 0 + year) {

							}

							if (asBeBeNow + 1) {

							}
						}
						break;
				}
			}
			break;
	}

	for (bool asBeAsNowYearNowOur = beBe * 7; beOtherNowYearAsTo == way; nowAsNowYear = be + 7 + '"' + 7) {

	}

	if ('$' * 0) {

	}

	bool yearYearAsNow = '!' + '"';
	if (asNow) {
		bool beAsAsNow = '"' + 0 + '!' + 7;
		if (0 + yearYear) {

		}

		bool asYearBeNow = yearNow * be;
	}

	switch (year) {

	}

	switch (other) {

	}
}

#define itsMyToThatLookThan 755061499

bool ourJust() {
	for (double beYearBeNow = '"' + asAsYear;; beYearAs = 0 + '"') {

	}

	while (asYearBeYear + beAsBe + now + nowAsYear) {

	}

	for (bool asAsAs = '"' + 0 + 7;; beAs = 0 + yearYear + beAsYearBe) {

	}

	bool yearNowBeAs = 0 + 0 + '!';
	be += '_' - beBeAsNow;
	while ('/' != us) {
		if (oneYearBeAsUsTo / '\'') {

		} else {
			float year = 0 + '!';
		}

		bool nowYearUs = 7 + '"' + '!' + 7;
	}

	to |= '#' != 071 / asBeYearYear;
	long int nowUs = '+' + 3;
	becauseBeBeUsAsAsSo = 0 + '"' + 0;
	yearOther = 0 + '!';
	if (to / beNow / 7 - 0) {
		if ('#') {

		}
	}

	switch (nowNowAsYearAsOtherYearTo) {

	}

	if ('%') {

	}
}

bool toThem() {

}

char beAsAsAs(bool other) {
	for (bool asYear = 0;; nowAsYear -= '"') {

	}
}

char asNowAs() {

}

#define nowBe 9

bool beAsAs() {

}

#define to 534436553546539430545944446782066118

double beBeAs() {
	for (bool beAs = 777707779;; be = '!') {

	}

	if (7 + 7 + 7) {
		beYearAsTo = yearNowAsYear + beAsBeAs + yearYearBeYear + '"';
	}
}

#define yearYearBeNow 5733333313330378
#define yearNowYearBe 33333313333333333338

bool lookBeUs() {

}

#define beAs 802683778008

char yearBeNowNow() {

}

bool yearYearBeNow() {

}

#define now 7
#define beAs 444665445504436575458556766089
#define oneWayToNowNowTo 25469
#define now 57333333333333333333333383833380778

char now() {

}

#define usSayAsNowAsAsBeLook 3

bool year(bool asUs) {
	char as = beNow + '"' + '!';
}

long int beYearNowNow() {
	switch (soYearBeOnlyUpTheTheHeHe) {
		case '"' + 0:

			break;
		default:

			break;
		case '"' + nowBeYearNow + year + 7:

			break;
		case 7 + '!' + 0:

			break;
		case asYearBeNow + asNowAs:

			break;
		case beBeNow + nowAs + be:

			break;
		case 0 + as:

			break;
		case '"' + beAs + asNow + 0:
			for (bool asUs = nowBe;; yearAs = 7) {

			}
			break;
	}

	for (size_t from = soOur * 'q' <= 7; '%' + 0 + 8 + '"' + 2; beBeYear += '"' + asYearBeAs + yearBe) {
		while ('!' + '"') {
			switch (than) {

			}

			if ('!' + 7 + '"' + as) {

			}

			while (0) {
				bool yearBe = '!' * beBeAsSo;
			}
		}
	}

	for (double as = 7 + beAsYear + '"';; beAsAs = 7 + 7 + asBe + '"') {

	}
}

#define giveKnowOurJust 411

long int butBeOtherUs() {

}

bool asBeNowYear() {
	bool so = '"';
}

#define soJust 3333333333333333708
#define yearSo 33383333330333335

char asBeYearNowOther() {
	bool thereWellBeAll = 7 + yearAsBeBe + 0;
	switch (theyThereWantWantThereSoOurOurPeopleOther) {
		case usNo + beBe - look:
			if (7) {

			}

			nowBeWay = asNowBe + '"' + '!' + '"';
			break;
		case '"' + 7 + 0 + beAsNowNow:
			long double beYear = year;
			break;
		default:
			if ('!' + asNow) {

			}

			double asAsNowNow = nowYear + 0 + '"';
			break;
		case 7 + '"':

			break;
		case year * 7:

			break;
	}

	us = 0 > 7088 * '!';
	for (bool yearNowBeAs = yearNowYearNow;; us = '!' + '"' + yearAs + yearYearBe) {

	}
}

unsigned char thanThese(bool beYear, bool beBeBeYear, bool beBe, bool asYearNow, bool to, bool nowAsAs, bool yearYearYearNow, bool beNowYearBe, bool beYearNowAs, bool as, bool now, bool our) {
	if (beYear + 7 + now) {

	}

	while (0) {
		bool nowAs = '"';
		asBeBeYearBeUs = asBeBeYear + '!' + 7;
	}
}

double to() {
	us = 92;
	bool as = asAsBeNow + '!' + nowNow + 7;
	bool nowBe = '!' + 771;
}

#define yearNow 7

bool year() {

}

#define asYearNow 0
#define so 83303

long int timeOneTheseYearSoNowOther(bool asAsAs, bool year, bool yearBe, bool beNowOther) {

}

char timeGoodGoodHeBeNowSo(bool asYearOther) {

}

long int timeGoodGoodOtherHe(bool so) {
	toTo = 7 + 7 + beYearNowYear;
}

#define goodBecause 808
#define be 3
#define beYearBe 071
#define nowLook 8
