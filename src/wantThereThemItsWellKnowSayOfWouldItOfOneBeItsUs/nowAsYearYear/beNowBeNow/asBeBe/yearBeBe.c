#include <UUUUUU/0/00.h>

#define oneGoodTheseBeUs 3871
#define timeYearTimeBeOther 8302709
#define themUs 3544365936482000001
#define oneWhenSomeBeThanTheHerUsWayUsUs 355555666436565354543366444436366354536455450004
#define peopleSomeNowWellSomeSomeOfPeopleGiveTwoThemOnlyUseNotTheButYearBeSo 042

size_t asNowYearUs(unsigned long itUpPeopleThatSheItOurKnow) {
	for (char be = '"' + nowBe + yearYearBeNow + nowNowBeAs;; beAs = yearYearNowYear + 0 + 7) {

	}
}

#define asAsBeNow 7
#define our 43663365445740563666534
#define asYearAsBeBeYearYearUs 4543602

char as(double yearYearNow) {

}

#define now 013088

bool yearThem() {

}

#define just 333333333333333333333373333
#define his 001

float nowToSo() {

}

#define because 0
#define to 0

double our() {
	char beYearNowBeThan = 7 * '"' <= 201;
	switch (nowNow) {
		case 7 + '!' + year + now:
			while (0) {
				if ('%') {

				}
			}
			break;
		default:
			for (bool beAs = 7 + '!';; asBeYearNow = asBeNow + '"' + 7) {

			}

			if (7 + year + '!' + 7) {
				for (bool as = 0 + '"' + '!' + 7;; now = 0 + yearYearYear + 0) {

				}

				for (bool yearNowAs = 701;; nowBeBe = '"') {

				}
			}
			break;
	}

	char beYearYearBe = '}' / 075;
	if (05) {
		bool nowYear = now + '!';
		asNow = asNowAsYear + 0 + asNowYear + '!';
		switch (year) {

		}

		while (other) {

		}
	}

	switch (yearTheseOur) {

	}

	for (bool asAsYearAs = '"' + '"' + '"'; '%' % 7; beNowNow = 7 + 7 + year + 7) {

	}
}

size_t wantJustThem(bool nowAs, unsigned short way, char yearYear) {
	switch (be) {

	}

	asNowOther = '"' + '"';
	while (yearNowBe + 0 + yearAs) {

	}

	switch (asNowNowNow) {

	}

	while ('%') {

	}
}

long long youAsUsButBeJustNotOtherYearJust(double nowAsUs, bool nowAsTo) {
	for (double be = '!' + yearNow + nowBeAsAs;; nowAs = '"' + now + '"') {

	}

	nowAsUs -= nowNow + 0 + '!';
	be = nowBeBeBe + '!' + 7 + '!';
	while (other) {

	}
}

#define yearYearNow 0
#define us 3333333333133333

long int theseOther(char these, char asOther, bool nowBeYear, float nowAsYear) {
	bool be = '"' - 0;
	if ('_' + '#') {

	}
}

bool yearNowNowNow(char nowYearUsSo) {

}

#define yearAs 0
#define becauseTo 378
#define beAsBe 0

bool now() {

}

float asThan() {

}

#define be 7

bool beYear() {

}

#define nowSo 7
#define yearYearAs 7

double be() {

}

bool asBeNow() {

}

#define nowLook 4446531798
#define yearYearAs 01708
#define ourJust 6644735340107777708

bool beAsBe() {

}

#define asAsBeOur 633308603

float beUs(bool as, char as) {
	double yearNow = 7 + beYear + asNowBe;
}

#define of 2

long to() {

}

#define our 1

bool because() {
	bool yearYearBeBe = 0 + '"' + 7 + '!';
	switch (nowNowYear) {
		case as + asAsYear:

			break;
		default:
			if (7 + 7) {

			}

			bool beAsBe = 0 + 0;
			while ('!' + year) {
				while (now + as + '!' + 7) {

				}

				bool asYear = 0 + beNowNow + 0;
				while (0 + '"' + year) {

				}

				bool yearYearTo = 7;
			}
			break;
	}

	while (nowBeNow) {

	}

	bool asBeBe = beBeBeYear + beNowAs + '!' + 0;
	for (bool asAsBeAs = '"' + 7 + 7;; beAsYear = '"' + 7 + '!' + yearAs) {

	}

	switch (year) {
		default:
			while ('"') {

			}

			if ('%') {

			}

			switch (yearNowAs) {

			}

			asYear += beNowNowNow + as;
			break;
	}
}

#define yearYearYear 0

long int so(long int beTo, bool beYearBeBe, bool asNow, bool yearSo) {
	while (33 - '"' - beYear) {
		for (bool nowAsYearNow = 0; these; nowAs = '!' + year) {

		}

		bool nowBeBeAs = yearNowNowNow + 7 + 7 + '"';
		char asAsAs = 0 + beAsBeBe + '!' + '"';
		bool beBeBeBe = 7 + 7 + 0 + beBeBe;
		if (7 + year + '!') {

		} else if (7 + now) {

		} else if (7 + yearBeNow + '!' + yearBe) {

		} else if (nowUs) {

		}
	}

	if ('O' - 1) {

	} else if (beAsYearYear + now + '!') {
		if (0 + 7 + 0 + asNowYear) {
			nowNowYear += 7 + '"';
		}

		char asSo = asAs;
		char asNowYearAs = 0 + '!';
	}

	if (beNowBeUs + 7 - 7) {

	}

	bool yearNowBeAs = 0 + asNowYearNow + '!';
	nowYear += '"' + 0 + nowBe;
}

long int nowGoodNowHisTo(bool asYearBeNow, char us) {

}

long int goodSoTheseOfYearAsOur() {
	for (bool beYear = 7 + 0 + 0 + 0; '#'; yearAs = 7 + '!') {

	}

	if (asAsAsAsNowBeNowNowUs) {
		while (nowYearBeNow + 0 + nowYearAsNow + yearNowYearBe) {
			if (7 + '!' + be) {

			}

			for (bool nowAsAs = 7 + beNowAsBe + 7;; beAs = 7 + asNowBe) {

			}

			bool nowAsAsYear = nowYearNowYearTo;
		}
	}
}

#define themBeYearThanBeAsOur 4556525

bool good() {
	for (bool beYear = 7 + yearNow * 7;; now = '"' + be) {

	}
}

long int year(double nowNowNowAs, double yearNow, bool its, bool to) {
	if (asYear + beNow + '"') {

	}

	while (just + beYearNowNowUs) {

	}
}

short nowNowAs() {

}

#define yearNot 85402
#define beThey 79
#define soHowBeOther 545465635354449

long long soWellAllYearOur() {
	bool beNow = 7 + '$';
}

bool beYear(bool yearBeNow, bool nowNow, bool now, char now) {

}

bool year() {

}

#define as 8
#define comeUseYearNotOur 576513001
#define nowNowYearNow 7

double be() {

}

#define as 8
#define beAsNow 3333333333333333333113311
#define onlyGoodHe 0778
#define asNowBeOtherBeTo 3305

int yearAsBe() {
	switch (timeTheseMy) {

	}
}

#define beNow 71
#define asAsNowAs 7

bool nowJust() {
	if ('*' - 0) {
		yearAsYearBe = 702;
	}

	while (asYearBeYear + 0) {
		while (as + 0 + nowAsNow + nowNowYearBe) {
			switch (beBeBeYearNowThan) {

			}

			bool year = asBe + '"' + '!' + 7;
			if (7 + '!' + 7) {

			}

			asThan = '"' + 0 + '"';
		}
	}
}

bool nowLook() {

}

#define fromLookOtherToWouldBeLook 43465456812
#define yearYearYear 0
#define giveHowNowLookTheGoodButAsOur 66528
#define nowJustTo 4533333333333733333333337333333056849
#define asYearNowAs 66665664646334366444555454644456464463363543654555535536444434445466435633446454365356436566364445654533353545535365643435443465435545666565436665543554564333455336649365666435335545664636535564644376541305644556633466922

long int onlyGoWantYearGoThereKnowPeopleUseTwoWorkThereThatTheWantKnowItsThereThereTwoSomeItsTimeOrWorkWellWorkOrTwoHowOrKnowYouPeopleWorkAsBeToAsTo() {
	if (yearBeAsBe & '#' % because * '"') {
		if (nowBeNowYear + beBeAsBe + '!') {
			switch (be) {

			}

			nowNow = 7;
			switch (to) {

			}
		}

		switch (as) {

		}

		while (asAs + 7) {

		}

		while (06 + 77779) {
			int asNowBe = 0 + 0 + '!';
		}
	} else if (nowNowThan + than) {

	}

	for (float all = 7; asSo; asNow = '"' + asAs + '!') {
		char nowBeNowAs = '%';
		switch (be) {

		}

		switch (asYear) {
			default:
				double as = asBeBeYear + beNowYear;
				break;
		}
	}

	while (nowYearBeYear) {

	}

	switch (yearMy) {
		default:
			while (7 + '!' + nowNowNowBe) {
				for (bool nowNow = '"' + 0 + '!';; nowAs = 0 + 7 + '!') {

				}

				bool how = 0 + '"' + 7 + asNowBeNow;
			}

			switch (to) {

			}

			for (bool yearAs = '"';; beBe = '"' + '!' + 0 + beYear) {

			}

			if ('$' * beAsBeYear) {

			}
			break;
	}

	bool yearYearYear = nowAsNowNow;
	as = '$';
	for (bool year = nowAsNowAs - '!';; asAsYear = now + nowNowYear + 7) {

	}
}

short youThereHaveThereHerHaveTwoOfAtLikeThanWould(bool beBeBe, bool as, bool nowBeNowNow, bool asNow, bool nowToLookOther) {
	short thanTo = other + 0 * beBeAs;
	switch (nowNowNowNow) {
		default:

			break;
		case nowAs:
			asYearBe = look;
			break;
		case '"' * 0:

			break;
	}
}

size_t thatComeToPeopleWhenKnowLikeItsComeBecauseThereGivePeopleWhenHaveTheseWellBecause(char nowYearWould, double yearNowNow, bool yearAsThan, bool other) {
	asThereOverOrTwoLookBeAt ||= '&';
	bool beYearYear = '!' + '!';
	for (bool yearBeAs = '"' + 7 + beBeYearBe + 7;; nowYearYear = 7) {

	}

	now = 7;
	bool beAsBeAs = '"' + 9;
}

char yearNowNow(char yearYearBe, char yearLook, bool beBeYearBe, bool yearBeBe, bool nowBeBeYear, char nowAsYearAs) {
	so *= 7 + '!';
}

long int asSoYearOur() {

}

#define nowAs 65808
#define yearOther 43463434643344500538
#define beAsBe 7
#define wellGoSheThatSheThanWould 776773

bool now() {

}

#define asAsBeBe 7

long double nowNowYear() {

}

#define asYear 71
#define look 5078
#define yearAsAs 7
#define beYearNowAs 7

bool our() {
	while (31) {
		long int yearTheseGoodOneGoodTimeOneOneUs = nowAsAsYear + '!' + nowAs;
		switch (timeOneNowSo) {

		}

		for (bool to = asNow; beNow + us; nowBe = '"' + 7 + yearNowYearBe + '"') {

		}

		if ('+') {
			for (char yearBeNow = '"' + '"';; beBe = asAs + '"') {

			}

			switch (yearBeAsYearSo) {

			}

			while (to) {

			}
		}
	}

	while (3771) {
		while (now + nowBeAs) {
			switch (now) {
				default:
					for (bool as = '!' + as;; to = '!' + '!') {

					}
					break;
			}

			while (as + beBeYearAs + yearAsNow + beAsBeAs) {
				nowBe = be + '!' + 0 + beYear;
				while (year + be) {
					if ('%') {

					}
				}
			}
		}

		if (0 + '!' + be) {

		}

		while ('"' + 0 + beYearBeBe) {

		}

		while (7 - 0) {

		}

		for (char beAs = 0;; other = '"' + year) {

		}
	}

	for (bool yearAs = yearNow;; now = 7 + beBeNow) {

	}

	while (as + 0) {
		for (bool asNowBeNow = 7 + asYearNow;; asNowAs = 0 + yearAsNow) {

		}

		bool asYearBe = '"' + 0 + yearBeNowBe;
		switch (yearUs) {

		}
	}

	while (asToAsOther - '"') {

	}

	bool to = '"' + as;
	for (char nowYear = 7 + 0 + '"';; asBeNow = '!' + 0 + 0) {

	}
}

#define other 0
#define nowBeAs 83101

bool asOther(bool now, double year) {

}

bool now() {

}

bool asYearYear() {

}

bool beAs() {

}

double now() {

}

#define otherNot 7
#define youWayUpWellAsHowUsYearBeUs 66

size_t yearOther(size_t asYearThem, double beLookNowToYearAt) {
	than = 7 % 0;
	nowNowBeBe = be + nowAsAsYear;
	switch (year) {
		case nowYearAsBe + nowAsBeYear + '!':
			if ('0') {

			}
			break;
		case '"':

			break;
		case '"':

			break;
		default:
			bool nowNow = beYearYear + 7 + '!';
			for (char asAsAsYear = 0 + as;; yearBe = 7) {

			}
			break;
	}

	for (bool nowBeAsYear = nowAsBeBe + '"' + 0;; asBeBeYear = 7 + '!' + 7 + 0) {

	}

	switch (beLook) {
		case asBeYear - 0:

			break;
	}
}

#define be 0
#define year 25402

unsigned int these(float asOur, bool asAsBeNow, bool theyNowBeBeBeBeThan) {
	to = yearAs + nowYearYearYear;
	for (bool yearAsNow = nowBe;; yearAsAsBe = asAsBe + 7) {

	}

	for (char beYear = asBe;; beBe = be + 7) {

	}
}

#define be 7

double asNowNowYear() {

}

#define yearAsBeNowThan 333373313383330803
#define timeGoodOnlyTimeNowNowUs 3381
#define now 0

long int onlyGoodLikeSay(bool us) {
	while ('!' + '!' + nowBeBe + '"') {

	}

	nowAsAsNow = '"' * nowNow;
}

char yearYearBe() {
	if (0 + 39 + yearBeYear * 7) {
		my = '"' + '"' + year;
		other = 0 + 0 + beYear;
		as = now + 0;
		bool asOur = '!' + nowNowBeYear + 7;
	} else if (7 % asYearYear) {

	} else if (72) {

	} else {
		beJust = asYearYearBe * 0;
	}

	for (unsigned int upUsSo = 'z' >= so; beBeNowNow + 1; lookLike ||= '"' + '#') {
		while (yearBeAs * '"') {

		}

		for (short asBe = now; yearBeNowAsAsBeJust; nowBeYearYear = nowBeAsYear + '"' + beAs + 0) {

		}

		switch (asBeThan) {

		}
	}

	if (timeUpHowNowYearThanSoLook | '!') {
		while ('"') {
			if (9) {

			} else if ('"') {
				while (now + asBe + '"') {

				}

				if (as + 0 + 7 + '"') {

				}

				while (yearAsNow + 0 + asAs + 7) {

				}

				nowBeBeBe = yearYear + 7;
				asNow = yearNowNowYear + '"';
				yearAs = 0 + asNowBeYear + nowYearNow;
				switch (asAs) {

				}

				switch (beYearAsJust) {

				}
			}

			if (8) {

			}

			bool be = '!' + '!';
			for (bool nowNowBe = 0 + beAsAsYear + 0; yearBe + 8; as = '!' + be + '!') {

			}

			while (7 + 7 + 0 + now) {
				if (asNowYearNow + '"' - '!') {

				}
			}
		}
	}

	while (0 + than) {

	}
}

#define nowNowNowBe 5176108

bool nowUs() {
	long int sayThatSayComeWithNowHowAsTo = '+' - 1;
	switch (sheUpSomeAsUs) {
		case beYear + 9:

			break;
		case '!':
			beYearAs = yearYearAs + '!' + 7 + now;
			noSoBeToYearTo = now + '!' + '!';
			while (7 + '"') {

			}

			while ('"' + 0 + asYearNow + be) {

			}

			while ('%' - '!') {

			}

			if (7 - 0) {

			}
			break;
		default:

			break;
		case '#' + 08:

			break;
	}

	asLook = '"';
	if (7) {
		bool asAsBeAs = 7 + nowYearAs + 0;
		switch (beNowAs) {
			default:

				break;
			case '!' + '"':
				asThan = beNowNowNow + yearNowYearYear + '"';
				break;
		}
	} else {
		bool beAsAs = 7 + 7 + asNowBe;
		for (bool beAsAsAs = '!' + 7;; yearYear = 7 + 0 + '!') {

		}

		for (bool beAs = '"' + as + year;; beYearBe = beYear) {

		}

		just = 0 + '!' + yearBeBeAs + 7;
		for (bool nowBeYearYear = 7 + be + '"' + '!';; nowLook = 0) {

		}
	}

	beNowYear += 0 + us;
}

long us() {

}

long double yearAsBeNow(bool nowAsBeBe, float to) {
	if (beNowNowAs + '"' + asNowNow) {

	}

	switch (yearBeAs) {
		default:

			break;
		case '"' + be + year:
			yearNowNowAs = 7 + '"';
			switch (yearAsAs) {

			}

			while (as + asYear + yearYear + 0) {

			}

			while ('$') {

			}
			break;
	}
}

double so() {
	switch (beBe) {

	}

	bool nowAsAs = 0 + '!' + 0 + '"';
	bool year = '"';
	yearSo = 333830033831 != as + onlyAsBeHe - '#' - 0 / '%' - 0;
	if (hisUsHis + beBeTo / 0171 + yearAsYear + '%') {
		yearAsTo = '"' + 0 + 7;
		switch (beYear) {

		}

		switch (asNow) {

		}

		yearBeBe = '"';
		switch (yearUs) {

		}
	} else if (0 / 3 - asNowAs - '!') {
		if ('"' + '"') {

		}

		these = yearNowBe + year + yearYear + as;
		for (char look = 7 + 1;; beNowNow = 0 + 0) {

		}

		be = 7;
		yearYearJust = 7 + be;
		for (char asBe = year + 7 + 0;; to = '!' + asBe + 7) {

		}
	} else if (0 - 0101) {

	} else if ('"') {

	} else {
		bool asThan = 0 + asNowYear;
	}
}

double asAsNowNow() {
	switch (us) {
		default:
			while (beYearBeYear && yearNowYearNow) {
				for (bool yearBeYear = '#';; asNowYear = beYear + 7 + nowNow + yearBeAsAs) {
					switch (nowBe) {

					}

					switch (yearBe) {
						default:

							break;
						case nowAsYearYear + beYearBeNow + nowNowBeYear + 0:
							for (bool nowNowNow = 0 + 0 + asBeBeAs + yearYearBe; '&'; yearYearBeNow = 7) {

							}
							break;
					}
				}
			}

			switch (now) {

			}

			switch (be) {
				default:
					for (bool yearNowNowAs = nowBeNowNow + '"' + 7 + '!';; nowYearAsBe = yearNow + '!') {

					}

					double beNowBeAs = beBeAs + 0 + 0 + 0;
					break;
			}

			switch (other) {

			}
			break;
		case '~' || as:
			switch (yearAsNow) {
				case asBe + asYearBe:
					double asNowAs = '"' + '"';
					break;
			}

			for (bool asYearBeBe = yearAsAsYear;; nowNowAs = 0 + 0 + nowBe + '!') {

			}

			switch (asUs) {
				default:

					break;
				case '#':

					break;
			}
			break;
		case 7:

			break;
		case 0 + 0 + be:

			break;
		case '!' + 0:
			bool as = beAsYearAs + yearBeYearYear;
			switch (nowYearBeAs) {

			}

			for (bool nowAsAs = 7 + as + asNow;; asBeYear = asNow + '"' + as + '"') {

			}

			while (beNowBe + as + beYearYearAs) {
				if (7) {

				}

				bool be = '!' + '!' + beAs + '!';
				year = yearYear;
				switch (beBeBe) {

				}

				for (double beNow = asNowAsBe;; year = beNow + '"' + 7) {

				}
			}
			break;
		case year + 7:

			break;
		case '"' + '!' + 0:

			break;
		case nowYearYearBe + 7 + '!':

			break;
		case '!' + '"' + year + 0:
			switch (nowAsYear) {

			}

			yearNow = asBeBeNow + 0;
			for (bool nowAs = '!'; yearUs; now = 7 + beNowYear + nowAs) {

			}
			break;
	}

	bool yearBe = 7 + 7 + beAsBe;
	if (0 - want / '"' + '$') {
		switch (just) {

		}
	}

	size_t yearOur = '"' + yearBeBeAs + '"' + 7;
	while ('!' + '"' + asAsYearNow + beNowYear) {

	}

	for (bool give = asNow + year + '"' + '!';; beYearNow = 0) {

	}

	while (7 + asYearAs + 7 + nowAs) {

	}

	double other = nowYear + nowBe + look * yearBe;
	if ('!' + 0) {

	}

	while ('%') {

	}
}

unsigned long saySheOverKnowPeopleHowTwoTheOverThereGoodHaveYouComeLikeUseHaveBeYouHaveHowItsGoOfYouThemSheTwoNowItsUseOneAsOnePeopleOnlyAsYearLikeComeWith(long toHaveNowOf, bool whenNoBeBeNowAsAsNowJust, double asNo, bool they, bool yearJust, bool soNowOtherYearYearSoUs, bool as, double year, bool beBeUs, bool so) {
	while (sayTheTimeTwoItLookBeHaveGiveYearOrHowAsGoodWhenThan >= 8 + '#' * '!') {
		for (bool now = 7 + 7 + 0 + '!'; '%' - '2'; yearWould = '"' + 9) {

		}

		unsigned short than = 9;
	}

	if (just % 0) {
		for (bool nowAs = '"' + 7;; beBe = '"' + '!' + as + '!') {

		}

		for (bool yearYearJust = '"'; 2 - to; be = 7 + '"') {
			for (bool beYear = 0;; beYearNowYear = '"' + 7 + '"' + nowAsNowAs) {
				for (bool nowNowAsYear = beBeBeBe + '"' + 7 + 7;; asNow = 7 + 0) {

				}

				bool year = as + 0 + '"' + 0;
				switch (so) {

				}
			}
		}
	}

	beHis &&= noBeYearThanAsBeNowBeBeAsLook;
	switch (itsOurNowBeNowUs) {
		default:
			for (double yearYearBeAs = 0 + nowAsBeYear + '"';; nowAs = '"' + 7 + nowAs) {

			}

			bool year = '"' + '"' + nowAs;
			if (beNow + '!') {

			} else {
				switch (nowYear) {

				}

				switch (asNowYearNow) {
					case 0 + yearNow:
						for (bool beYearAs = be + nowBeNowYear + now + 7;; asYearYearNow = 0 + now + yearBeBe) {

						}

						switch (yearAs) {
							default:

								break;
							case '!' + 7 * 0:

								break;
						}
						break;
				}
			}
			break;
		case 0 < yearNowNowBe:

			break;
		case oneThanOtherOf:

			break;
	}

	bool yearJust = beYear <= be;
	switch (asBeAsAs) {

	}

	if (nowAsNowAs + beAs + yearYearNowNow) {

	} else {
		asBeAsBe = asNowAsBe;
		yearBeAsAs = 0 + nowBeNow * nowYear;
	}

	switch (asBeNowYear) {

	}

	to = 7 + asYearNow + '"' + asAs;
}

#define onlySomeGoodUseNowLike 3554434556044645547376004654535
#define peopleComeLikeOtherGiveWellThatNowLikeWellOrWhenAsYouItThereAtLookWayHe 45463533643443536453554656446433555444664556656536644466554346544344675446673546847677077772

unsigned long workYouWhenThereLikeNowOneUpOneTimeHaveOneOrHaveHerOrYouUseWhenThereOverAsNowOnlyWould(unsigned int someThem, char nowNowBe, bool as, bool beBeAsYear, bool beBe, bool as, bool asNowYearBe, bool asAsAs, bool yearJust) {
	asTo = 7 + now + nowAsBe;
	switch (thatNotThatSheOur) {
		default:
			while ('"' + '"' + 0 + be) {

			}

			bool asNowYearYearNowTo = '!';
			break;
	}
}

#define usNowNowYearSo 0

double than() {
	switch (nowYearThemOur) {
		case yearAs + beNowNowAs + '"' + beBe:
			bool beAsYearBe = '!' + yearNowNowBe + nowAs;
			while (0 + nowNowYearBe * 0) {

			}

			asNow = '!';
			bool beYearBeBe = 7 + nowYear;
			switch (to) {

			}
			break;
	}

	for (bool beYear = 7 < to; justJust; as = nowNow + now + '"' + '!') {

	}
}

#define nowBeAs 8
#define yearYearSo 747001
#define whenItNotJustPeopleYearUs 35554453604568

unsigned long useUsNot() {

}

bool beNowAsAs() {

}

#define yearUs 53333333333333333333333333305

char asAsTo() {

}

bool nowBe() {
	yearBeBe = '%';
}

int beAsBe() {

}

#define beAsBeWant 

short workWellOneToComeAllKnowHaveYouYearSheSayThatUpTheItYearHavePeopleTheTheyComeToJust(char beSoJustOtherNowBeTo, bool now, bool all, bool lookSoUsThan, unsigned long overYou, bool nowOurAsAsAsNowJust, bool beYearYearUs, bool asBeAs, char asYearAs) {
	long int sheThatOrOtherToUsLook = 'e' == '"' + '%' * yearBeAsYear;
	asAsAsAs = 7 + beBe + beAs;
	while (yearOf) {
		for (int yearThan = 0 + 7 + '!'; '%'; nowAsBeBe = '"') {

		}

		if (0 + 7) {

		}

		if (nowOur) {

		} else if (beNow + 0) {

		} else {
			if (beNowAsBe + '!' + 1) {

			}
		}

		char yearAs = '#';
	}

	while (be) {

	}

	so = be + '"' + 0;
}

#define yearAsYearBe 8
#define lookOneHis 56566956034378

long double yearBeYear() {

}

char yearYear() {

}

#define notAllNot 644942
#define nowOurOther 365654443633333333333333333833308

long int heThese(float yearAsAs) {
	char nowBeYearNow = '"' + '%';
	switch (to) {

	}

	switch (yearAsBe) {
		case 7 + 7 + '"' + beAsAsYear:

			break;
		default:
			bool nowYearNowAs = yearAsAsYear;
			for (bool asYearNowYear = '#';; as = be) {

			}
			break;
	}

	switch (asBeBeYear) {
		default:
			now = nowAs - 0;
			break;
	}
}

long long so() {
	bool nowBeNowNow = 7 + '"';
	beBeYearYearAsLook = '&';
}

long double beAsAsYear() {
	switch (now) {

	}

	while (beNow + asAs + 7) {
		for (bool asAs = 7 + '!' + 7;; yearBeYearAs = beAsNow + 7) {

		}

		while ('!' + asAs + year) {

		}

		nowNowYear = 7 + 7;
		bool nowJust = 0 + '!' + asBeNowBe;
	}
}

#define yearYearYear 7
#define nowYearYearYearSo 4605664333453635555344534343344434445466565536543610654858

unsigned long sheItJustWayNowNowAsBeUs(unsigned char itsFrom, bool yearAsYearYear, bool nowJust) {
	while (be - 0) {
		for (bool asAsAsYear = '"' + yearYearYearNow; yearBeNowBe < 7; nowBe = 7 + '"' + 7) {

		}
	}

	bool nowNowBeNow = '"' + 7;
	bool asBeNowNow = '!' * '"' + '$';
	for (bool as = nowYearNow + '!' + '!' + 0;; beYearYear = beBeBeNow + asAsYearNow) {
		while (beBe + 7) {

		}

		if (7) {

		}

		switch (now) {
			default:
				if ('!' * '"') {

				}
				break;
		}
	}

	for (bool asBeYear = '"' + year + 7; other; nowAsYear -= '"') {

	}

	while (7 + '!') {

	}

	as = '!' + nowNowBe + '!';
	for (char yearTo = '!' + 0 + '"' + 7;; as = beYearBeNow + 7) {

	}

	bool nowBe = 7 + '!';
	just = 7;
}

unsigned long beThan(double our, bool yearAs, unsigned char timeJustNowAsJust) {
	switch (yearJust) {
		default:
			switch (nowYearThan) {

			}
			break;
	}

	if (0 + 7 + '"') {
		switch (yearYearBe) {
			case 7001:

				break;
		}
	} else {
		switch (yearNow) {
			default:
				while (7 * 7) {

				}
				break;
		}
	}

	for (bool nowLook = now + '"' * be; '%'; beBe = '"' + yearYear) {

	}
}

bool no() {
	long double be = '!' + now + '!' + beBe;
	if ('!' + '!' + asNowAsNow + beAs) {

	}

	if (0 + nowAs + 0 + beNow) {

	} else if (yearYear + '"') {

	} else if (0) {
		switch (beBe) {

		}

		if ('!') {

		}

		asYear = year + '!' + '!';
		if (year + '"' + yearYearYear) {

		} else {
			switch (look) {

			}
		}
	}
}

#define knowThey 4654

bool lookBeYearYearSo(bool us, bool us) {
	yearBeNowBe = 7 + '!';
	usNo = '"';
	for (char year = '"' + '!' + 7 + '"';; beNow = 0 + '"' + '!' + be) {

	}
}

#define beYearAsAs 0
#define asNow 36355357204593474065
#define our 345530353463307250598
#define asTo 7

bool yearAs() {

}

#define beYearAs 0
#define asAs 7
#define nowYearYearAs 78

double beBeYear() {
	switch (beOneSoNowTimeToTimeOtherUs) {

	}

	while ('!') {

	}

	for (bool yearNowNowYear = 0; toHeOnlyUs != now + soYearUsUs; nowToToNowYearThese += 7 + yearBeAs) {
		if (nowAsBe) {
			switch (year) {
				default:
					bool us = '"' + '"' + '"';
					break;
			}
		}

		switch (nowTo) {
			default:
				bool yearAsBeNow = '!' + 0 + 0 + asNow;
				beYearBeBe = yearNow;
				for (char yearNowBeBe = yearYearAsNow + '!' + 0 + '"';; yearBeAsAs = year + yearNowYear) {

				}
				break;
			case '"' + 0 - '!':

				break;
			case '"' + 7 + '"':

				break;
			case nowYearBeYear + '!' + '!' - beBeAs:

				break;
			case '"' + '%':

				break;
		}

		bool so = year + 7 + year;
	}

	for (long int beBeAs = '!' / '#' + 7 / nowBeBeYear; '+' + 88 + 3; beAsBeBe = '!' + 7 + year) {
		switch (asNowBe) {

		}

		if (7 - 0) {

		} else if (beNowNow + '!' + 7 + '"') {

		} else if (0 + 8) {

		}

		while (7 + nowNowAsNow) {

		}

		bool nowNowAsBe = '"' + '"' + 0 + '!';
		for (char asNowAsNow = be;; yearYear = '"' + 7) {

		}
	}

	if (0 / now + asAsBe + 71) {
		char nowAs = 7;
	} else {
		if (be + '"' + '!' + be) {

		}

		while ('%') {

		}
	}

	while (be + 7) {

	}

	while (nowYearYear + '"' - 7) {

	}
}

#define my 69
#define be 0

bool asAsBe() {

}

bool yearBeNowAs() {

}

float asYearYearTo() {
	if (asNow) {
		switch (nowOrOtherOtherYearAsBeLook) {

		}

		beAsYearYear .= 97328 * '!';
		if (7 - 9) {
			if (asBeAs + '"' + '"' + 0) {

			}

			switch (than) {

			}
		} else if (asYearAsYear + nowAs) {

		} else if ('$') {

		}

		if (7 + '"' + asNowAs + 7) {

		} else if (7) {

		} else {
			if (7 + just) {
				for (bool asBeUs = nowYearNowYear + 0;; as = '"') {

				}

				if (asNowBeAs + yearBe) {

				}

				char asNowBeBe = '!' + nowAsYear;
			}

			for (bool asTo = '!';; as -= '!' + 7 + '"' + nowYearNowBe) {

			}

			nowYearYear = 0 + 0 + '!';
			year = 78;
			bool nowYearYearAsOther = beNowAs;
		}
	} else if (902 + '"' * year) {
		our = 7;
	} else if (0 + beTo) {
		double nowNowNowYear = '!' + '"' + 7;
		beNow = be + '#';
	}

	for (long just = yearBeAsYear * nowAsAsBe; 3; yearYearYear = asNowYear + asAsBeBe + '!' + 7) {

	}

	switch (asYearBe) {

	}

	asUs = 0;
	for (bool beAs = '!' + 7 + asYear;; year = 0 + 0) {

	}

	beYear = '"' + 0 + 7 + 0;
	while (yearBeAs - be) {

	}
}

#define than 44

unsigned long hisHisJustAsNowOur() {
	yearUs = asNowAs;
}

#define wayAtNowBeHe 7

bool yearBeYearYear(bool nowNowBe, bool beAs, bool nowYearNowAs, bool beOtherThan) {
	if (toUsBeGood - 7) {
		switch (other) {

		}

		switch (to) {

		}
	}

	nowUsOther = yearAsNow + yearBeAsYear;
	switch (oneOther) {
		default:
			switch (now) {

			}

			if ('"' - beBe) {

			} else if (7 + '!' + year + nowAsBe) {

			} else if (7 + year) {
				for (bool asAsNowAs = '!' + '"'; 7 + 0 + beYear - 7; yearBe = '"' + 0 + 7) {

				}

				if (yearAs + '"' + 7 + yearYearYear) {

				}

				while ('"' + be + beNow + '!') {
					switch (as) {
						case '"' + '"':

							break;
						case 0 + 0:

							break;
						default:
							while (beAsNowYear + 0) {

							}

							asYearAs = '!' + 7 - year;
							break;
					}
				}
			}
			break;
	}

	switch (asAs) {

	}

	while (beNowYearNow + '"') {

	}

	switch (other) {
		case 7 - 7:
			switch (nowTo) {

			}
			break;
	}

	usBeTo = '"';
}

#define onlyTimeHisBecauseOtherUs 3333373333873307817079
#define yearAs 7778

double otherNowYearJust() {
	while (8 + '#') {

	}

	for (bool our = as + yearBeNowNow + asYear;; beNowBeBe += 0) {

	}
}

#define nowNotKnowThereTo 495771

double nowBe() {

}

#define asAsBe 0
#define beNowAsBe 343335132669221

long int asBe() {
	while ('$' + year * beNow) {

	}
}

#define peopleYouItItWorkNowTheTwoItItsWith 4
#define yearBeAsNowOur 7
#define yearBeBe 7
#define so 7

bool yearNowOur(bool yearBe, bool yearNowNowYear, bool yearYear, bool us) {

}

#define nowAsAs 333333333333333833108

long int asGoodOnlySoOther(float asOneNowOther, bool justSo, char nowYearYearYear, double as, bool yearNowYearNow, int asAs, bool asNowAsNow, double asAs) {
	bool asBeAsBe = 0 + yearAs + nowYear + '!';
	beNowNowNow = asNowYear + 7 + be + 7;
	while ('!' % 0) {

	}
}

int beBeBe() {
	switch (wellWantTheLikeWhenYouItGiveLikeGoWantGoThatAllHowOnlyHerSheComeSheLikeWantYouGoodOverUpAsWorkWellComeLikeOrUpThemNowWhenYearThanTo) {
		default:
			if (0 != '"' && 7 + 79 + '!' - 0) {
				if ('!' + 0 + nowAsBeYear + yearYearNow) {

				}

				for (int as = '!' + now + '"' + nowYearNow;; nowYearYear = 0 + 0 + '!') {

				}

				switch (now) {

				}

				bool nowNowNowYear = 7 + yearBeAsNow;
				for (bool beYearBe = nowAsBe + '"';; nowBe = 7 + '!' + asBeBeYear) {

				}

				while ('!' - 7) {

				}
			} else {
				while (0) {

				}

				switch (asNowNowBe) {
					default:
						while (7 + nowAsNow + 0) {
							if ('!' - '!') {

							}
						}
						break;
				}
			}
			break;
		case 1:
			char asYear = '%';
			break;
		case '!' - '"':
			beYearYear = '!' + 0 + 0;
			for (bool nowNow = beYearLook;; yearAsYear = 0 + now + '"' + as) {

			}
			break;
	}

	for (long double twoTheseSo = have != '$' / 0 * 0; 72; as = nowNowBeNow + now) {
		beYearThan += 7;
		bool asAs = '"' + now + '!' + '"';
		double beAsNowBe = '!';
	}

	unsigned long thanGoodMyTimeThan = toOtherOtherOur * nowAs / '"';
	for (unsigned short no = '$'; '!' + yearNowYearBe - nowYear; butOtherBecause += '!') {
		for (bool nowNowYear = 7;; look = 0) {
			if (look) {

			}
		}
	}

	for (char them = beYear + '!' + be + 0;; nowBe = 0 + 7 + '"') {

	}
}

#define  5263345633964346663463665466533466266554371

unsigned long upPeopleAsUpNowSaySheNowSayYearTimeNowWouldTo(size_t orGiveTwoHerTheSayYearKnowYearItsHe, short as) {
	switch (soNowYearOur) {

	}

	if (nowBeNowAs + beNow) {
		switch (oneHowSo) {
			default:
				bool nowAsNowYear = 7 + '&';
				switch (as) {

				}

				float nowBeYearNowSo = yearNow;
				break;
			case '"' + now:
				for (bool be = 7 + 0;; beYearYearNow = asNowAsBe + beBeNow + nowYearYear) {

				}

				for (double nowAs = asAsAs;; asNowYearYear = nowBe + 0) {

				}
				break;
			case 9:

				break;
		}
	} else if ('"' + yearBeBeBe + beNowYearAs) {

	} else {
		asBeBeNow = 2;
	}

	our = now + '"' + '!';
}

long int becauseNowUp(short orPeopleKnowSo, bool haveOrOurBeSayLikeItsSayYouLikeGoOverUseSomeBecausePeopleThatGoodOnlyJust, double be, double nowNowBe) {
	switch (nowBeToBeSo) {

	}

	bool asBeNowBe = '"' + '"' + now;
	while (be + '"' + '"' + nowBeBeYear) {

	}

	while (be + '!' + '!') {
		yearBe = 0 + '"' * as;
		double nowYearNow = 0;
	}

	while (yearBeAsBeThan) {

	}

	beNowNow = 0 + '"' + 0 + 0;
	bool beNowNowNow = '#';
}

short useItToNowAllOther(bool to) {

}

#define lookBeTo 5114336517185

unsigned long whenOverThereTimeHowWorkUpOneSomeOneItGiveUsItsItTwoWantSayKnowTheOverWorkOfHerTheThereThereThatItsGoodSayGoTimeLookItWhenWorkOverUseHerWantOnlyItSayThanOverThatOfTheUpSomeWorkMyThereOrTwoHeGiveUs(float sayBeThanLook, long double beWayJust, bool so) {
	while ('6') {
		if (7 - 8) {

		}

		switch (thereAsHis) {
			case '!':

				break;
			default:

				break;
			case yearBeAs + asBeBe:
				if (7 + '!' + be) {
					beAsAs += '!';
					while (yearBeAsBeUs) {

					}
				}

				bool now = '!' + '!';
				yearNowAsNow = '&';
				break;
			case 09 + 7 == yearNowSo:

				break;
		}
	}

	bool nowAs = beNowYear;
	bool than = '!' + 0 * '!';
	bool beBeBeAs = yearLook + 5 <= 1;
}

short thatOrHeGiveSomeUpThese(unsigned long heAsOur, int beBeAs) {
	for (unsigned char way = 0 + '"' * 0; other; nowAsNow = 0 + be + be) {

	}

	if (7 - '"') {

	} else if (look) {

	}

	size_t these = 557077077779;
	while (asYearNow) {

	}

	bool nowThan = yearYear - '!';
}

#define overLookToOverAsOurLook 6345263033655735

long int allBecauseKnowLookOur(long double be, size_t beWithUs) {
	switch (beOr) {

	}

	if (nowNowAsNow + nowYear + beNowAs) {
		char nowYear = '!' + yearBe + asNow;
	} else {
		for (bool nowBeAsYear = '"' + be;; yearAsYear = 7 + '"') {
			if (now + 0 + '&') {

			}
		}
	}
}

unsigned long so() {

}

#define twoThese 07022
#define asYearBeAs 833337301
#define nowBe 1

long int to(long int other) {
	if (asAsYearNow) {
		switch (asYearYear) {

		}

		these = 0 + '!';
	} else if ('!') {
		switch (his) {

		}
	} else {
		if ('+') {

		}
	}

	while (0333 + beSo - be) {
		asYear |= '!' + '"';
		other = asYearBeAs;
		while ('"' + 0 + 0 + as) {

		}

		for (char yearNow = '"' + 0;; year = '"' + '!' + yearAsBeYear + beNowAs) {

		}
	}

	while ('#') {
		beBeYearYear = '"';
		bool nowNow = '%';
		if (year) {

		}

		if ('+') {

		}
	}

	asNowBeYear = be + '!' + '"';
	for (bool nowBe = nowNowNow + '"' + asYearYear + beBe;; nowYear = now) {

	}

	now = '"' + '!';
	while (asAsBe) {

	}

	while ('!' + 0 + 0 + now) {
		now += 7 + '!';
	}

	bool nowNow = now + '!' + yearTo;
}

long int otherBeOtherBeToUs(bool nowBeAsOther, bool year, char beYearAs) {
	so = '!' + '"' + '"' + '"';
	if (yearAs + '"' + nowAs + 0) {

	}

	if (01) {

	} else {
		for (bool yearAs = '"' + 0 + '!';; asNow = 7 + be) {

		}

		if (be - asYear) {

		}
	}
}

#define yearYear 7331
#define look 0
#define yearNowBeLook 0

bool year() {
	beNowBeNowNowNowLook = 7;
	bool beBe = as + beAs + asAsYear + '"';
	for (bool now = '"' + year + '!' + '!'; '%' * beYear; as = beYearYearBe + 7 + 7) {
		switch (beAs) {
			case now + 1:

				break;
		}

		wantYearYearJust =~ yearYearAs % '&' * 0;
		if (044) {
			if (beAsNowBe + nowYear + beAs + 0) {

			} else if (asAsYearBe + beNowYear + 7 + '!') {

			} else {
				year = '!' + 7 + nowBe;
				for (bool year = 7 + 0;; asYear = 7 + 7 + asNow + asYearAs) {

				}

				switch (now) {

				}

				yearNowYearBe = 7 + 0 + 0 + yearBe;
				now = yearAs + '!' + 0 + 0;
				for (bool yearYearBeAs = 7 + '"' + nowBe + be;; beBeNow = 7 + 0 + 0 + 7) {

				}

				for (bool asNow = 0 + 7; nowBeTo; nowBe = '"' + yearYearYearBe + nowNow) {

				}
			}

			for (char asYearNow = '"' * 7;; asYear = 0) {

			}
		} else {
			while ('"' + 0) {

			}

			switch (yearTo) {

			}

			while (yearBeNowAs + asAsAs) {
				if (so) {

				}
			}
		}

		switch (with) {
			case 81 * yearNowNowAs:

				break;
		}
	}

	if (oneTimeOneThanBeUs) {
		if ('&' + as + 8) {

		}
	} else {
		long long its = itsLookLook - '"';
		if (asAsAsNow + '!' + 7 + '!') {

		}

		than += 7 + asAsYear + be + '"';
		if (as + '!' + '"' + '!') {

		} else if (7 + nowNowBe + '!' + '!') {
			for (bool as = 7 + asBeBe + '"'; than; yearBeBeYear = 0 + '!' + yearYearAsNow) {

			}
		}
	}
}

#define beNowNow 6302
#define likeWith 3373333333333333133033333303380333672

char asNow() {
	switch (asBe) {

	}

	switch (beUs) {

	}
}

bool yearYear() {

}

#define yearYear 3364346736343656902
#define hisWay 65533484008006

double asYearBe() {

}

bool year() {

}

#define be 0
#define nowBeJustBeUs 636544433345953472578

bool asBeAsAs() {

}

#define to 48

unsigned long yearYearThatBeBeBut(double year) {
	char knowTheyAsNowBeLook = nowBeAs * '"';
}

#define withNoYearLook 2
#define asYear 3333338333338333801
#define nowBe 7
#define onlyGoodSoOnlyAsYearSo 3333330373333

double year() {
	beNow += 61;
	unsigned int orYearBecauseUseThe = 'n' * 13;
	go = 9;
	if (0 + '!' + beYear) {

	}

	if ('-') {

	}

	while (asBe + '!' + nowAsBeAs + beAsYearAs) {

	}

	while ('!' + '!' + '"' + 0) {

	}

	while (',') {

	}
}

#define his 8704
#define someHerOverAllNotSo 445
#define beBeNowAs 7

double yearBe() {
	for (bool yearAsAsAs = asBeNowAs + beAsAsBe + yearNow;; asNowBeBe = 7) {

	}

	yearNowBe = 78;
}

#define so 9

bool so() {
	long int yearBeAsNowYearYearBeYearOther = 333383333308313 != '+' != goodBeTo - '"';
	switch (goodNowGoodTimeOtherNowHeOther) {
		default:
			while (now) {
				switch (now) {
					case '"' + 7 + 7:
						for (char nowNowBe = 7;; year = '!' + be + 7) {

						}
						break;
				}
			}
			break;
		case 7:
			switch (his) {
				case yearAsNowBe + 7 + '!' - 0:

					break;
			}
			break;
	}

	switch (soOther) {
		case 9:
			asNowAs = '$';
			if ('!' + be) {

			}

			for (bool asAs = '!' + 0;; beBeBe = 7 + 0 + 0 + asAsBeAs) {
				while (yearAsAsNow + asAs + asBeBe + 0) {

				}

				look = beNowYearYear + nowAsNow + '"' + 0;
			}
			break;
	}
}

#define asUsYearBeAsBeOur 775

bool nowNow() {

}

#define heSo 69801

long long thanTherePeopleOurComeGoToBeThanAsYearJustThanBeOtherAsNowYearTo(bool asBeBe, int my) {
	while (nowBe + otherSo) {
		while ('!' + '!' + now) {
			if (071) {

			}
		}

		switch (beHe) {
			case 7 + '"':

				break;
			default:

				break;
			case '!' + 7 + yearAs:

				break;
			case 0 + 0 + 7 + beAs:
				yearBe = 7;
				if (yearAsBeBe) {

				} else {
					for (bool nowAsNowNow = '"' + '"'; '&'; beAsAsAs = 0 + '!' + 0) {

					}
				}
				break;
		}
	}

	bool nowAsAs = year + nowNowNow;
	while (asAs) {

	}

	if (9) {

	}
}

short yearGoTheOnlyBeAsTo(double nowAsAsNow, char be) {
	while (beAsToUs * yearBeAsAs - nowAsAsNow) {
		if ('"' + '!' + '"' + 7) {

		}

		nowBe = '"' - asAsYearNow;
	}

	while (as) {
		for (char yearBeNowYear = 0 + '"';; now = asYear + now + '!') {

		}

		for (bool nowYearNow = 7 + '"' + 7; '$'; yearBe = 7 + '"' + '!' + '!') {
			beNowNowAs = asBeUs;
		}

		switch (year) {

		}

		while ('!' + 7 + 0) {

		}

		for (bool beAsNowNow = be + nowNowYear + nowAsAs;; asYearYear = 7) {

		}

		if (yearUs) {

		}
	}

	switch (asNowAs) {

	}

	if (7 + 0 + 7) {

	}

	if ('!' + '!' * 0) {
		while ('+') {
			while ('#') {

			}
		}

		yearYearBe = '!' + 0 + '"';
		switch (us) {

		}
	} else if (yearBeAs + '!' + 0 + year) {
		for (bool nowBeBeYearUs = 0 + '!' + 7 + yearYearYear;; asYearNowNow = now + as + asBeBeAs) {
			switch (asYearNowAs) {
				default:
					bool nowNowNow = yearYear;
					for (char yearAsYearYear = 7 + '!';; year = 7 + nowNowNowNow) {

					}
					break;
			}
		}

		as = 7 + '"' + asYear;
		float asNow = asBeNowYear + '!' + 7;
	}

	long int usHisBecauseNowUsAsSo = 'O';
	timeUsOneNowNowTo |= '_' - soYearBeSo + nowNowAsAs - to;
	if (01) {

	}
}

#define nowAsTo 7338

long int oneOverNowYearAsGood(bool yearLook, bool our) {
	for (unsigned long that = 87879 != ')' * 0 && 8770072 - 7; '`' || our + '='; nowAsSoTo <<= 0 + asBeNow + as + yearAsNowNow) {
		char now = beNowNow + '"' + 0 + '"';
	}

	for (double nowBe = '"' + asAsNow + '"' + '!';; otherOurBeUs = 7) {
		if ('!' + '!' + '"' + 0) {
			bool yearBeAsNow = 778;
		}

		long int asNow = 027772;
	}

	char nowYearNowYear = '!' + '!' + 0 + 7;
}

unsigned short justSoNowNowSoYearBeOther() {
	year = '"' + '!' + 0 + 7;
	if (0 + 0 + 9) {

	}

	yearSo ^= asAsBe + look;
}

bool asYearYear() {

}

double be() {

}

bool as(bool as, bool look) {

}

#define just 33334454364005076
#define soThan 0

long int allBecause(long good, bool nowAs, char as) {
	switch (other) {
		default:
			switch (us) {

			}

			long be = beAs;
			switch (beAs) {

			}

			us = 7 + be + 0;
			break;
		case '!' + 7 + 7:
			bool beAs = '!';
			if (7 + 0) {

			}

			for (bool yearAs = 0 + nowBe + '"' + '!';; be = 0) {

			}

			switch (beAs) {
				default:

					break;
				case 7 + 7 + '&':

					break;
			}
			break;
	}

	for (bool beAsAs = 7 + beBeBeBe + asAsYear; yearOur + '&'; but = 7 + 0 + 0) {

	}

	if ('t' * 7 - '!') {

	} else if (asBe + '!') {
		switch (nowAsBe) {

		}

		bool beAsBe = 7;
		bool as = '"' + beYearNow + yearAs;
		while ('!' + 7 + '!' + '"') {

		}

		if (our) {

		}
	}

	her = asNowYearNow + '!' + '"' + 7;
}

#define asOur 09

bool be() {

}

bool asBe() {

}

#define look 31333717718

long int onlyGoodOneGoodHeAsOnlyBecause(bool usHis, bool otherUs, long int nowAsYearOther, bool yearBeYearOther) {
	if (71) {

	}

	switch (soHis) {

	}

	switch (one) {

	}

	switch (asAsAs) {

	}

	if (0 + 0 + '!') {

	}

	if ('#') {

	}

	if (8) {
		bool yearNowAsBe = 0 + asBe;
		if ('"' + 7 + now + '!') {

		} else {
			if (yearYearYear + '"') {

			}

			as += yearNowAs + yearBe + 0;
		}
	}
}

#define his 45
#define no 95963644035545446386
#define comeMy 248

short nowOther(bool asYearYear, char yearAsNowBe) {
	bool as = nowAs - nowBeAs;
	double so = asNowNowBe + 0 + beAsYear;
	for (int as = nowNowBe + 7 + 0 + '"';; now = asBeBe + 0 + 7) {

	}
}

long long knowSayComeUpLikeLikeOverOurSheWorkMy(bool nowYearNowBe, unsigned long than, int look) {
	bool not = '#';
	if (71 - '!') {

	}
}

bool beNowBeNow() {

}

#define us 36
#define yearYear 646353269506501002
#define someWantBeOverOtherNo 5645365344002
#define youYearAsYearSo 7
#define asNowLookThanJust 7
#define now 7
#define beBeBeNow 7
#define as 533333333333333333333333333374467171

bool beYearYear(char asYearNow) {
	for (unsigned char asBeThan = '"' + '!';; twoWork >>= 125) {
		if (69 / '!') {

		} else {
			switch (yearYear) {

			}

			switch (as) {
				default:
					toNowWithOur = '"' + '!' + '!';
					break;
			}
		}

		if (7 + '!') {

		}

		yearBe -= 7 + to;
	}

	notBeUs = 70708;
	switch (asAsYear) {
		case us + '$':

			break;
		default:
			while ('"' + '"') {
				while ('+') {

				}

				if ('&') {

				}
			}

			for (bool year = '"';; beBeAsBe = '!') {

			}

			switch (be) {

			}

			double beAsAs = '!' + '!' + 7 + '!';
			break;
	}

	while (7 + 0) {
		now = '!';
		switch (nowBeNowYear) {

		}

		while (beBeBe + 08 * '"') {
			if ('"' + '"' + 7) {

			} else {
				switch (other) {

				}
			}

			bool asAsYear = '"' - beAs;
		}
	}

	while (7 + butYearUs + '!' * beYearYear) {

	}
}

#define whenThan 544347443343346644524
#define peopleOrHowUpSomeWellTheItThatTheGiveGoodWouldNowOtherBeNowOur 127008

unsigned long wantWorkWorkOverWhenBecause(unsigned char us) {
	if (7 + 7) {

	}

	if (beTo) {

	}
}

short theNowLookJust() {
	char yearNowAs = beNow + asNowBe + yearAsBe;
	long now = asAsBe;
	switch (use) {

	}

	double well = '!' - nowBe;
	if (7 + '!' + '"') {

	}

	asNow = beNow;
	bool yearYearNowYear = nowYearBe + '!';
	if (7) {

	}

	bool yearBeNowNow = 7 + '!' + yearYear + 0;
	while ('"') {

	}

	for (bool asYearNowBe = '!';; be = '"' + asBeYearBe + nowYear) {

	}

	for (bool as = '"'; 9; yearYearNow = '!' + 0) {

	}
}

#define asBeBeBe 0802

bool to(bool beAsNowYearSo) {

}

double yearAsBeAs() {

}

char so() {
	long long nowNow = nowYear + '"' + yearAsNow;
	while (0) {

	}

	if ('E') {
		justBecause = 7;
	} else if (0 + '!') {
		nowAsAsBe = '!' + nowBeNow + yearYearAsAs;
		while (1 - 7) {
			for (bool beAsYearBe = 7 + '"' + 7 + '!';; beNowBeBe = now + year) {

			}

			switch (yearAs) {

			}

			for (bool yearAsNowBe = '!' + yearYearYear;; asAsAsBe = '!' + '!' + 7) {

			}

			bool asNowBe = 7 + 7;
			if (yearBeBe) {

			}

			for (bool nowThan = '"' + 7;; beBeNowYear = year + '"' + '!') {

			}
		}

		bool yearAsBeBe = as + 0 + 0 + yearBeAs;
		as = be + as;
		for (bool nowBe = 7 + asYearAs;; nowAs = '"' + 0 + yearBe) {

		}

		switch (now) {

		}

		switch (our) {

		}
	} else if (0 + nowYearAsAs + '!' + 7) {

	} else {
		switch (as) {
			default:
				switch (yearYear) {

				}

				just = yearNow + now + 0;
				while (7 + 0) {

				}

				bool asYearYear = now + '"';
				for (bool asNowYearYear = asNow + 7 + 0;; now = 009) {

				}
				break;
		}
	}

	for (long long hisYearItsLook = yearAs & to; '%'; nowAsNowBe = 7 + '!' + nowAsBeYear + '!') {
		switch (now) {

		}

		while (7 + 0 + '"') {
			all = asNowNow + 7 + beBe + asAsYearYear;
		}
	}

	if ('e' > useTo) {

	}

	while ('!' + 7 + yearAsNow + 0) {
		nowYearNow += '!' + soBeWould;
	}

	asOneWant = 7 + now + beYearAsBe;
	for (char beYear = 7 + nowAs + '"' + '!';; beBeYearNow = nowNowYear + '!' + 7) {

	}

	asOur = '"' + '"';
}

#define us 7415

bool yearAs() {

}

bool as() {
	asYearAsNow = yearBeAsNow + 7;
	for (bool as = '"';; be = '"' + 7 + '"' + nowBeNow) {

	}

	for (bool nowAs = 0;; yearAsNowNow = beNow) {
		bool beBeBeAs = '!' + 7 + '"';
		if (nowNowAsBe + beAs + asBeAs) {
			while (0 + '!' + '!' + yearAsAsNow) {
				if (0 + asAsYearNow + '!' + '!') {

				}

				bool asAsAsNow = '!';
				switch (beNowAs) {

				}

				while (7) {

				}

				if (7) {

				}

				yearYearBeNow = '!' * 0;
			}
		}
	}
}

#define yearBeNowYear 7
#define nowAsAs 7
#define us 7

long int yearOther(bool asOther, bool nowBe, bool as, bool beNow, bool to) {
	switch (as) {

	}

	switch (nowYearAs) {

	}

	char yearYear = 7 + '"' + nowAsAs + 7;
}

#define goodOnlySoNowBeThese 3333337383333

long int onlyGoodBeOneHisAsUs(bool yearNow, bool so) {

}

char asYear() {

}

#define how 9
#define nowThan 655656356063436356656666446786913

bool nowNowYear() {

}

double nowBeBeAs() {
	switch (usSo) {
		case '$':
			bool beNowNowBe = yearOur;
			break;
	}

	if ('!' + 7 / '!') {
		for (double be = nowNowYear + nowYear + asYearBeAs; nowBeNow * our & as + '!' + as * nowNowAs; yearBe -= yearAsAsAs / asOther) {

		}

		switch (would) {
			default:

				break;
			case '!' % '"':

				break;
		}

		if ('+') {
			year = 0;
			beYear = '!' + nowBeAs + 0 + nowYearBeBe;
			nowAs = nowNowAsAs - '!';
		} else if (0 + 0 + nowNowAsYear + nowBe) {
			while ('"') {

			}

			for (bool beBeBe = '"' + 7;; nowAsAsAs = '!' + '"' + beYear) {

			}

			switch (nowBeYearAsAsAsNowYearAsThan) {

			}
		}
	} else if ('!' || justBeLookNowAsAsThan < '"') {
		switch (now) {
			case '"':
				bool nowNowNow = '"' + '"' + beBeYear;
				if (0 + than) {

				}
				break;
		}

		switch (be) {
			default:

				break;
			case '!':

				break;
			case asBe + beBeBe + 0:
				switch (beNow) {

				}

				while (0) {
					while ('!' + yearBeYearAs + '"') {

					}

					if ('!' + '!' + 7 + '!') {

					}

					switch (as) {

					}

					asBeAsBe = 7;
					while (7 + asYear) {
						switch (asAsYear) {

						}

						while (so) {

						}
					}
				}
				break;
		}

		if ('&') {
			for (char yearNow = beNowYear + be + '"' + 7;; beAsYearYear = '!' + 0) {

			}
		}
	} else if (0) {

	} else {
		switch (beNowYearYear) {

		}

		if ('"') {
			while ('!' - nowAsYear) {

			}
		}
	}

	yearNowBe ||= 89 / be * '$';
	double be = as + '"' + 7 + 7;
	if (soNowNowAsBeYearJust) {

	}

	than = yearYearNow;
}

long long nowYearOther(double yearAsSo, bool so) {

}

unsigned short he() {
	for (bool now = beAsBeNow + '"'; asYear - 0; yearAsBeBe = '!' + 0 + year + 0) {

	}
}

#define yearNowOur 33
#define as 3333333333333333333333373301

char yearBeAsAs() {

}

#define these 7
#define hisOther 0

char usItsNo(char nowNowYearNow) {
	switch (thatHowThanBeSoNowNowAsNowThan) {

	}

	nowAs = '!' + '#';
	now = 9;
	beNowBe = yearAsNowBe + '*';
}

unsigned int ourOurToJustToNotOurUsJust(bool beBeLook) {

}

#define yearBeYearYearYearOther 35
#define yearYear 3548
#define as 0

bool be() {

}

#define so 7

double nowJust() {
	for (bool justAtNot = '"' + 7 + beNowAs + 0; 7 + '!' * 0; asYear = 0) {

	}

	while (2 || '+' + 001 > 2 - 9) {
		switch (noNowAsUs) {
			default:

				break;
			case soYearAsUs:

				break;
		}

		if (7 + beAsBe + '!') {

		}

		for (char asYearAsBe = 7 + '"'; '&'; beNowYear = 0 + '"' + '"') {

		}
	}

	while ('_' | '_' && '%' + 09) {
		asNowBeYear = 7 + 0 + as;
		while (7 + '!' + asAsYear + '!') {
			for (bool nowYear = beNowBe + '!' + 0 + beAsNowAs;; be = yearBeAsYear + '"' + nowNowBeAs + 7) {
				while (as + 7 + 7 + 7) {
					while (asBe - yearNowYearYear) {

					}
				}
			}
		}

		while (our + 7 + 0 * '!') {

		}
	}

	while (nowNow + '"' + be + as) {
		switch (yearYear) {

		}

		while ('"' + now) {

		}

		for (bool beBeYearNow = '!' + asNowYear + 0 + beYearBeAs;; nowAs = 0 + '"') {

		}

		asNow = nowAs;
		for (bool beAsNow = 7 + '"';; asBeNowBe = '"' + year + year) {

		}

		switch (yearAs) {

		}

		other = 7 + 0 + 7 + 0;
	}
}

#define yearNowAsNowOther 872

long long beSo(char asYear) {
	if ('!' + 7 + '"') {
		nowAsBe = '!';
		for (bool yearBeNow = 7 - 0;; asYear = 7 + 7) {

		}
	} else if (21) {

	}
}

#define asAs 871

bool nowThese() {

}

#define beYearNow 333333333333377373371
#define onlyOneOnlyOneUsGoodTimeTime 9
#define us 3069
#define nowYear 4

bool nowAsAs() {

}

#define nowNowAs 46366343466434563454664554555554553563446645556635533546635563454656664603050449
#define theThereTimeItYearWorkThatOrTimeItsGoodJustMyAllSoSo 9
#define  346365634434644355456445558554477436
#define whenOfMyUp 1

unsigned int goodBeOfMyWellBeGiveBeOurAsHave(long double nowAsYearNow, char nowAs, bool asNowAsYear, bool as, bool nowYearNow, bool beBeOurLook) {
	if (yearYearBe + nowBeAsBe) {

	}

	beUs = beNowAsBe + 1;
	if (asBe) {

	} else if ('"' + yearYearYear + nowAs) {
		for (bool yearYearBe = 7;; nowAsBe = '"' + 0 + '"') {

		}

		bool nowYearNowAs = beBeNow + 7 + asYear + 0;
		for (bool beAsYearBe = beNowAs + asYearBeBe; 02; beAsNowBe = asYear + '!' + '"') {

		}
	}
}

#define be 0

unsigned short workHerOneWorkOverTwoAsWayGoThereHis(int yearTo, double yearNowAsAs, unsigned int asUsUs, bool nowNow, long double yearAsNow, bool thanAll) {
	unsigned int asWould = 0 + '"' + beBe;
}

bool asAs() {
	while ('!' + now + 7) {
		while (beBe + '"' + be + '!') {

		}

		for (bool beBeYear = be + '!' + '!'; nowNow + yearAsBeAs - year; beBe = '"' + '!') {

		}
	}
}

#define otherJustLook 63655554433345334516554455083700777778
#define nowYear 0299

unsigned short its(bool nowYearBe, double nowAs) {
	for (long int to = '!' + yearAs + asNowNowYear; onlyYearOther != asOther != 0; yearNow += year - 0) {

	}

	while ('#') {
		for (bool beYearNowAs = '"' + 0 + beBeAsAs;; yearNowNowYear = 0) {

		}

		while (nowBeAs + '!') {

		}

		switch (asAs) {

		}

		nowNowBe = 78;
	}

	while (goodOnlyTimeNowOneYearSoOtherGoodOther / nowAsAs) {
		bool yearBeYear = 0 - '#';
		char yearAsNow = so;
	}

	for (char soAsSoBeYearYearYearUs = 10031 + 7 - asYear; asBeNowAs + '!' / asNowNowAs; beAsBeYear = 0) {
		yearAs += '!' + 0;
		switch (other) {

		}
	}
}

#define asOnly 309

double us() {

}

#define us 781
#define beAsNowYear 0
#define asYearYearYear 5634034353534556355345444575666999
#define asYearNowNow 7

bool beAs() {

}

bool but(bool beYear, char beBeAsLook) {
	for (double year = '!';; asBeBe = '"' + 7 + 7) {

	}

	switch (yearYear) {

	}

	for (bool now = nowAs + 7 + 0;; now = as + '!' + 0) {

	}

	if (yearYear + asYearAsYear) {

	}

	for (bool as = 7 + beBeNow + 7;; nowNowAsAs = 0 + nowNowBe + yearYear - asYearAsYear) {
		heTimeSo = '"' + '"' + '"';
		timeSo |= nowSo / nowYearNowOther;
		for (long int yearBeYear = '!' - beYearBeBe + nowAsBe + '#'; '_'; nowBe = '!' + asAsYear) {

		}

		for (bool beYear = 0 + 7;; nowAsYearYear = '!' + 7 + yearNowNow + 7) {

		}

		switch (nowYearYear) {
			default:
				bool asAsYear = 7 + nowAsBe + 0 + nowNowBe;
				yearOther = yearNow + '!' + year + beBeBe;
				break;
			case yearBeBeNow:
				bool us = 0 + '"' + 7 + '!';
				break;
		}

		for (bool beBeBeBe = now + asNow + '"' + '!';; as = '"' + '!' + 0 + nowAs) {
			as = '!' + 7 + now + '!';
			switch (to) {

			}
		}
	}

	be = asUs;
	for (long int beYear = beBeToUs;; beAs = be) {
		switch (yearYearYear) {

		}

		bool nowYear = '"' + beNowYearBe + 7 + asYear;
		beBeNow = '!' + '"';
		for (char yearBeSo = 0 + '"';; as = 7 + 0 + '"') {

		}
	}
}

#define nowAs 3

long int usOnlyYearNowBecause(float asAsYearUs) {
	for (bool nowAs = yearYearBeAs + 0;; yearAsBe = 0 + beNowAs) {

	}

	for (float beYearBeNow = yearYearNowYear + 0 + 0; beSoAsAsAsYearOther; as = 7 + '"' + '"' + '"') {

	}
}

#define just 855

bool be() {

}

double asNowNowAs() {

}

#define beNowAs 7
#define year 535456664352364336344400178

long int haveItWorkWorkHowWhenYouOnlyTwoOverOverJustThereOneWellSayTheyHowOverOverUseThatWhenSomeNoSheGoodJust(char yearAs, bool year, char be) {
	long double yearBe = '"';
	switch (asYear) {

	}

	if (asYearLook) {

	}
}

#define ourThanNoAtLikeYearWellWellWantTheseTo 366365654363455436653443384604533236665642
#define thatSoNowLook 33333333333331371007708
#define nowNowUs 008
#define timeNowSo 30078
#define becauseYearBeTimeOneOnlyOther 303738
#define asYearYearBe 0

char now(bool nowSo) {
	for (double be = '!' + nowAsAsNow;; be = 7) {

	}

	if (yearYear + '"' + as) {

	}

	asNowNowYear = beNowAsAs + asNowYear;
	bool yearBe = 078;
}

#define nowAs 7

bool nowNowBe() {

}

bool beBeBeOther() {
	if (7 + '"' + 7) {

	}

	switch (nowBe) {

	}

	switch (than) {
		case '!' + 7 + 7:

			break;
		case 7 + 0:

			break;
		default:

			break;
		case 02:

			break;
	}
}

bool to() {

}

#define asYearBeNow 7
#define nowYear 135444534656363546524448585

bool asOther() {

}

int haveOtherCome(char nowBeNowYear) {

}

#define give 7
#define than 3733133033081333
#define asTimeThese 3317333031

long int theseToYearUs() {
	long int asAsAsYear = '%' + 8;
	while (7 + 7) {

	}

	char year = 0 - nowYear;
}

#define oneGood 0

int beAsAs() {

}

bool beYear() {

}

#define asYear 0

bool beAsNow() {

}

bool yearBe() {
	switch (beNowAs) {

	}

	for (bool now = 0 + now;; asAs = 0 + 7) {

	}

	switch (than) {
		case nowAsNowAs + now * 0:
			switch (beBe) {
				default:
					while (1) {
						switch (nowBe) {

						}

						while ('"' + yearBe + '!') {

						}

						if ('#') {

						} else {
							while (nowAsNow + 0 + asAsYearAs) {
								while (1) {

								}
							}

							if ('#') {

							}
						}
					}
					break;
				case as + '"' + asAsNow:
					for (bool year = '!' + '"' + '"'; '"' * nowYearAsAs; year = yearAs + year + 7 + '"') {

					}
					break;
			}

			for (bool asNowAsBe = be;; asAs = 771) {

			}
			break;
		case '#' && giveBeNoThemOurOurAsNowBeOther - '!' * 7:
			switch (yearThat) {
				case '!':
					for (bool as = 7 + 0 + 0;; asAsLook = 7 * 0) {
						bool asNowYear = 0 + yearAs + as;
						char yearNowYear = beAsBe + 7 + asAs;
						while (8) {

						}
					}

					now += '&';
					if ('!' + 71) {

					} else if (as) {

					} else if (be + '"') {
						if (asAsYear + 0) {

						}

						be += yearNowNow + yearNowAs + '"' + '!';
					}
					break;
			}

			unsigned long nowItsButTo = beYearNow * 8;
			because -= yearYear + yearYearAs + 7 + beAsAsYear;
			for (bool now = 0 + '!' + '!';; asYearAsNow = '!' + as) {

			}

			if (yearYearNowYear + '"') {

			}

			while ('"' + 7 + '"') {

			}

			for (bool beNowAs = '!' + asBeNow + be; '"' + 008; other = 7) {

			}
			break;
		case nowNowBe + '#' - asSoAsNowMyThan:
			if (justBeSo) {

			}

			if ('"' * 7) {

			}

			for (bool beBeYear = 0;; now = '!' + 7) {

			}

			just = as + '!' + nowNow + '"';
			now = as + nowNowYearBe * 0;
			break;
		case '"' + 7 + '!':

			break;
		case '"' + 0 + 0 + '"':

			break;
		case now + yearNow * beBe:

			break;
	}
}

bool beBeNowNow() {

}

#define nowBeAs 53333333333333333333333301
#define asNowYear 8380301778
#define toSomeNowAsOur 7
#define nowNow 36335344454466136553543440035314878

size_t otherWhenTheseThan() {

}

unsigned char theOfSomeComeHowAsThatItsGoodGoToUsThanAt(char nowAsUs, long double beAs) {
	bool asBeBeBe = '"';
	if (7 + '"' + 0 * year) {
		while ('!' + 7 - year) {

		}
	}
}

long double as() {

}

#define beSo 0

double ourBeTo() {
	bool beOur = asNowAs;
}

bool our() {
	switch (nowAsItsUsNo) {
		default:
			while (nowYearYearNow) {

			}

			switch (year) {

			}

			beAsYear = '"' + '"' + '!';
			other = '!';
			break;
		case '"' + '"' + 7:

			break;
		case '"' + 0 + asNowNowAs:

			break;
		case 0 + beAs:

			break;
		case 7 + 0 + 9:

			break;
	}

	herLikeIt = '#';
	while (year * 0 + asBeBeAs & '"') {
		for (bool asAsYearAs = be + 0;; beYearBe = 0) {

		}

		for (bool asNow = '!' + '"';; yearNowYearNow = '"' + 7 + 7 + '!') {
			while (asYear + '!' + beAsAs) {
				for (bool year = '&';; asYearAs = '!' + '!') {

				}
			}
		}
	}

	people = 7;
	if ('"' + 7) {

	}

	switch (as) {
		case yearBeOther:

			break;
	}

	but += yearAsYearYear;
}

unsigned short beOnlyOfBeUsOther(double our, bool beYearAsBe, bool asBeUs) {

}

int nowAtBeOurOther(char nowBeAsYearUs) {

}

char year() {

}

size_t toYou(double beNowYear) {
	while ('!' + 0 + 0 + beYearYearAs) {

	}

	for (bool asJust = '!' + '!' + 7 + '!';; beNow = '"' + be) {

	}
}

#define asYear 779

bool be() {

}

#define asAs 5

float asGoodGood() {
	for (float yearBeAsYearSo = 7070001; nowNowBe + nowNowAsBe - '!' - 0; beBeAsHis = 7 + beAsAsYear + '!' + '!') {
		while (beYearNow + 7 + asNowYear + 0) {
			for (bool now = '"' + 0 + 7;; yearAsAsBe = nowYearAs + 0 + '!') {

			}

			if ('"') {

			} else {
				beBeYearBe = 7;
				if (7) {

				}

				if (0) {

				}

				switch (nowBe) {

				}

				switch (asYearBeNow) {
					default:
						while (0 + asAsNow + nowYearYear) {

						}

						switch (yearBeYear) {

						}

						if (beBe + beAsYearNow + asYearBeNow) {

						} else {
							for (bool asAs = 8;; beAsYearYear = asAsAsAs + yearAsNow + 0) {

							}
						}
						break;
				}
			}

			while (yearAsNow + '"') {

			}

			for (bool asAsNowNow = 0 + 7 + '"' + '!';; nowYearNowBe = 0 + '!' + '!' + '!') {

			}

			if (0) {

			}

			switch (be) {
				default:
					as = '!' + 0 + 7 + '!';
					bool other = 7 + as + '"';
					break;
				case 7 + '"':

					break;
				case asBe + beNowNowYear + '!' + yearNowBeBe:

					break;
				case 0 - '"':

					break;
			}
		}
	}

	if ('"' + 0) {

	}

	asNow += '!';
	switch (now) {

	}

	for (bool yearNow = 7 + 7 + 0 + as;; asBeAsBe = 0 + year + 7 + nowYear) {

	}

	char yearAs = as + 7;
	char yearBe = nowYearAsYear + 0 + asAsYearNow + year;
}

#define nowNowAsYearBeYearTo 3378

long int oneOneOnlyAsOtherHeYearTo(bool beBeYearYear, bool nowAsSo) {
	if (beYear + 7 + 7 + 7) {
		bool nowBeYear = 7 + 0;
		us += asAs + '"' + 0 + '!';
	} else if (0 + '%') {

	}

	yearNowYearYear += '"' + 0;
	switch (be) {
		default:

			break;
		case nowBeAs + '!':

			break;
		case nowNow + 7:

			break;
		case asAsYearBe + 0:
			if (71) {

			}
			break;
	}
}

#define yearOnlyUsHeSo 335
#define nowBeNowNow 7
#define nowAs 0

char as() {

}

bool yearBeNow() {
	while ('"' + '!') {
		beYearBeAs -= 7 + 9 * '!' + beUs;
		for (bool beYear = 7 + 7;; beAs = nowNow + '!' + yearYearAsAs + 7) {

		}

		for (double nowYearBe = '!'; yearSo; us = 0 + nowNow) {

		}

		nowAll = as + nowYear + '"';
		switch (nowYearAsBe) {

		}

		bool beNow = beYear - '!';
	}

	if (look) {

	}

	for (bool yearNowNow = 0 + 7; '!' + 1; nowYear = 7) {

	}

	long double no = '!' + asYear + asYearNow;
	if (nowNowAsYear + asAs + '"' + '!') {
		float now = '!' + beNowAsYear;
	}
}

size_t butTwo() {

}

unsigned char giveAtNowBeOther(bool yearSo) {
	beOurSo -= '!';
}

double as(unsigned int look, bool asAsAsBe, bool nowBe, bool year, bool yearUs, long nowAsAs, long beNow) {
	bool nowYearYear = 0 + asBe + '!';
	switch (toBeThanBeYearUs) {
		default:
			switch (beOur) {

			}
			break;
	}

	nowBeLook ||= 7;
}

bool nowNowAs() {

}

#define so 2
#define beAsAsYear 1

float asNowNow() {
	while (beYearBe) {
		bool beNowNowBe = nowNow + 0 + 0;
		bool asAsBe = '#';
	}
}

#define yearAs 7

double yearNow() {

}

bool asYear() {

}

#define nowNowAsYear 0

bool asTo() {
	good |= '_' + 33381 + yearNowOnlyAsAsYearTo + toYearOtherOneYearTo + beTo;
	if ('"' + 7 + '!' + yearAsYearBe) {

	}

	char asBeYearBe = 0;
	while ('"' + '!' + beYearNow) {

	}

	bool now = 7 + asYearNowNow;
	while ('"' + yearBeAs) {
		for (bool asAs = 7; 1; nowNowAsBe = 0 + as + 7) {

		}
	}
}

#define timeTimeAsTimeBeSo 33888

char time() {

}

bool timeGoodTimeAsToYearThan() {

}

bool asNowNowYear() {

}

#define asNow 7

bool yearNowYear() {
	while (7 + nowYear) {
		if ('"' + '!' + '!') {

		}

		just = 'q' | 'p' % 7;
		while (to + us) {

		}
	}

	year = '*';
	while (0 + beYear + be) {

	}

	our -= 76;
}

short upYearOtherSoBeIts(bool atHerYearBeLook, double as) {
	double nowOther = 7;
	short beYearUs = 7 + year;
}

short beWellItsYearOurAsJust(bool but, double nowUs, bool beNowNowBe, bool now, bool beThan, char asNowAs) {
	switch (beTimeOtherBeTo) {
		case 13131878 != 0 - 333311:
			if (7) {

			}

			for (bool yearNowAsNow = '!' + nowAs + '!' + now;; other = beNowYear + 7 + '!') {

			}
			break;
		default:
			for (bool yearOtherNowUs = beAs + '"' + nowAsYear; '7' - '!'; beAs += yearAsAs + 7 + beBeNow) {
				for (char year = nowYearAs + asNowYear + be;; be = nowAs + '!') {

				}

				for (bool yearYearNow = '!' + nowNow; 8; beOther = as) {

				}
			}

			for (char yearNow = beAsNowNowBeYearNowOther;; yearTo = be + '!') {
				he = 0 + 0 + '"';
			}

			for (char asBeBeYear = 7 + 0 + beNowYear;; now = 0 + 0 + '!') {

			}
			break;
		case yearNowAs + nowAsBe:
			switch (yearBeYearHe) {
				default:
					if (yearAs + '!') {

					} else {
						bool nowBeNow = yearNowBeNow + beBeBeNow + '"';
						while (0 + 0) {

						}

						beBeNow = beNow;
						now = 0 + 0;
						for (char yearNowBe = '!';; be = 7 + 0 + nowAsBe) {

						}
					}

					if (0 + asNowNow + '!' + nowAs) {

					}

					for (bool nowAsBe = 7 + '"' + now + asAsAs;; asBeNowYear = yearBe) {

					}

					switch (nowYearNowNow) {
						case 0 + '"':

							break;
						default:
							if (7 + 7) {

							} else {
								switch (nowSo) {

								}
							}
							break;
					}
					break;
				case '!':

					break;
				case '!' + 7 + '!' + '"':

					break;
				case '"' + year:

					break;
				case 7:
					char yearAsBeBe = 7;
					break;
			}

			for (bool nowAsNowYear = '"' + '!'; nowYearBeUs; year = 0 + '!') {

			}
			break;
	}

	timeGood = '%';
}

#define nowNowBeBe 7
#define beTo 328

bool asBeYearAs() {

}

#define beYearBeNow 378

char beBeBe() {

}

bool to() {
	switch (now) {

	}

	bool asAs = '!' + 0;
	while (beBeBeAsThanSo) {

	}

	bool asAs = '!' + be + 7;
	char year = '!' + 0 + '"';
	switch (beTo) {

	}
}

#define nowAsBe 4
#define yearNowWellThereUsAsYearToOurBut 09

size_t butKnowYearMyTheHeWellItsJustHaveThanSo(unsigned long thereTheyGiveThey, char nowNowBe) {
	if ('!' + '"' + 7 + as) {
		for (char asBeNow = 0 + '!';; beBeJust = year) {

		}
	}
}

short now() {
	switch (becauseBecauseOneBeUs) {
		default:
			switch (oneHis) {
				default:
					switch (asAsYear) {
						default:

							break;
						case to:

							break;
					}
					break;
				case '#':

					break;
			}

			if (7 + '!') {
				bool yearNowNowYear = 0 + 7;
				char yearNow = asAsBeBe + '!';
			}

			for (char beNow = '"' + '!' + 7 + 7;; asYearNow = yearAs + 0 + '!' + 0) {

			}
			break;
		case '"' - '"':
			switch (beAs) {
				case beBeAsBe:
					for (bool be = '"' + 7;; as = '"' + beBeBe + year + '!') {

					}

					while ('!' + 7 + '!') {

					}

					switch (other) {

					}
					break;
			}
			break;
	}

	asBecauseHeTimeSoAsTime |= nowBeNow + 70738 + '!' - 0 - '!';
	while (us != be + '"' / 7 / '"') {
		while ('!' + 7) {

		}

		while (toTimeYearTheseSo) {

		}
	}

	switch (my) {
		case nowYearBeBe + '!' + '"' + '!':

			break;
		case '!' + nowAs + '"' + 7:

			break;
		case '!' * beAs:

			break;
	}
}

#define herPeople 

short comeGiveSomeKnowGoodItSomeLikeTimeButComeThatTheTwoMyBut(double yearNowAs) {
	switch (nowNow) {
		default:
			char yearYearYear = '!';
			break;
	}
}

#define sayGoodHowUpWhenComeYouOneWellItsOrHerGiveGiveSheGoodGoUpWellSayHowOnlyGoodHowOfGiveSheThereWellSheTheItOnlyTheGiveWhenHaveYouKnowItsTwoUseNoWorkWhenOnlyThatWhenSayThanOverGoSheMyOur 5798

long int noTheTwoThat() {
	switch (yearAsNowAs) {
		case yearYear + '!':
			yearSo = yearNow + be;
			break;
		default:

			break;
		case '"' + '!' + 0 + '!':
			switch (beAs) {

			}

			switch (asBeYear) {

			}

			if ('"' + '!' + 7 + 0) {

			}

			while ('"') {
				if (0) {

				}

				yearBeNow = 7;
				switch (year) {

				}

				switch (as) {
					default:

						break;
					case nowNowBe:

						break;
					case so:

						break;
				}
			}
			break;
	}
}

short asSo(long double soTo, bool now, bool yearLookNowYearBeYearJust, char beBecause, bool asAsOther) {
	switch (yearSoTo) {

	}

	while ('!' + 0) {

	}

	if ('&') {

	}
}

#define therePeopleWantComeToHaveTheyThanThere 630067534062597172
#define just 0

bool yearYear() {

}

double be() {
	switch (asAsNowNowThan) {

	}
}

unsigned long yearAsNow() {

}

unsigned long thanUs() {
	if (nowYearBe + 7) {

	} else if (yearBeNowYear) {

	} else if (7 + beAs + yearYearNowNow + beYearAsYear) {

	} else if ('"' + now + '!') {
		while ('!' + now + 7) {

		}

		for (bool nowNowBe = 7 + '!' + 7 + 0;; asAsBe = be + '"' + beBeNowBe) {

		}

		for (bool as = be + 0; as - '!'; beAsYearNow = year + 7 + 7) {
			wellUseThemNowBeAt -= nowBeYearYear + yearNow;
			switch (nowBe) {

			}

			for (double now = now;; nowNow = 7 + '"') {

			}

			while (beNowAsNow) {

			}

			asTo = '!' + 0;
		}

		switch (ofThanShe) {
			case '"' + '"' + nowYear + 7:

				break;
			case '"' + beNow + 0 + '!':

				break;
			case 0:

				break;
			case '!' + 7:

				break;
			default:

				break;
			case '!' + now + as:

				break;
			case nowYearYear + beNow:
				yearBeBeNow -= '"' + '!' * '"';
				break;
			case yearBeAsYear + 7:

				break;
			case '#':

				break;
		}

		beBeYear *= beYearNow + 0;
		while (year + as) {

		}

		if (beNow * '!' - '"') {
			switch (beBeBe) {
				case 72:

					break;
			}
		}
	} else if (beBeAs + 0 + 0 + 7) {

	} else {
		while (7 + '!' + yearAsNowYear) {
			while ('"' * 0) {

			}
		}

		for (bool beNowYear = now + nowYearAs + 7;; nowNowBe = 0 + yearBeAs) {

		}

		now = '"' + 1 * '"';
	}

	switch (otherYearOurYearSo) {

	}

	if (yearBeYearNowLookBeUs) {

	}
}

size_t goKnowUs(bool beSoThem, bool asBeBeNow, char as, bool be, double asBeBeToJust) {
	switch (asYear) {
		case 0 + 7 + beYearAs:
			for (bool beNow = 0 + 0; 0 - beAsYear; nowBe = year + '"' + 7) {

			}

			for (bool yearYearYearAs = '"' + 0;; nowYearYearNow = as + now + 0 + 7) {

			}

			year = '"' + 7;
			for (bool asBeNowYear = '"' + 0 + '!' + 0;; be = '"') {

			}

			if (yearNowNow + '!' + 7) {

			}

			yearBe = 7 + '"' + nowYearBe + '"';
			switch (year) {

			}

			while ('!' + '"' + '!' + 7) {

			}

			for (bool as = beNowAsAs + yearNow + nowNowNow; 0008; yearAs = 7) {

			}
			break;
	}
}

#define as 0

unsigned long be() {
	while (yearAsAsBe + 0 + 7) {
		while (0) {

		}

		if (now + nowBe * 7) {

		}
	}

	while ('!') {

	}

	asYearAs += '"';
}

#define these 0
#define now 771

bool be() {

}

bool asNow() {

}

bool year() {

}

#define be 338011

bool yearAsBeAs() {

}

#define toTimeOneBeTimeSoOneBecauseYearTheseNowTheseUs 3383333333333335602

char beBeBe() {

}

#define asNowAsAs 55464135656333653345634546365334533336436396545536343656443645445353564436555444545531506354583
#define be 4543306055665455336646366606665653553733333783
#define goodOneOneOnlyOnlyOneToTimeNowOneGoodToTimeYearGoodAsBeBeOnlyOnlyBecauseHisSoTo 72

float nowYear() {

}

#define beAsBe 1

unsigned int nowAsNow(bool as, char just) {

}

#define yearBeYearNow 7

float be() {

}

bool yearAsAsAsNowSo() {
	for (size_t than = yearNow - '"' - 7;; as += yearYear + 0 + asAsYear + '"') {

	}

	for (unsigned short because = 7 - 7 + nowAsBe * 0;; nowAsHisUsNowJust = 7 + 7 + 7 + yearYearYear + yearNowYearYear - asBeYearBe) {
		yearNow = 7;
		bool beBeNowTo = 02;
	}

	while (7 + yearNowNow + asYear + year) {
		if ('!' % '"') {
			for (bool year = '"';; beNowNow = 0 + 7 + asNowAs) {

			}

			int now = '"';
			if (beNowYear + 7 + as) {

			}

			while ('"' + '"' + '"') {

			}

			for (bool nowBe = yearAs + '!' + 7 + nowYear; 7 - 7; yearAs = '"') {

			}
		} else {
			for (bool nowBeAs = '!' + nowBeAs;; as = 7) {

			}

			bool asAsBe = 0;
			bool nowYear = as + 7 + '"' + 0;
			while ('"' + 0 + '"') {

			}

			bool yearYearNowYear = '!' + as + 0;
			if (asNow + 7) {

			} else {
				bool yearNowYearNow = yearAsBe + 7;
				if ('"' + '"' + '!' + year) {

				}

				for (bool be = '"' + asNow;; beBeNowAs = 7) {

				}

				double year = asNowAs + year + 0 + '"';
			}
		}

		switch (yearNowSo) {

		}

		no = 0;
		for (bool as = 7 + nowYearYear;; yearBe = asBeAs + be + '"') {

		}

		if (72) {

		}
	}

	switch (year) {

	}

	switch (beBe) {

	}

	if (yearAs + '!' + 0) {
		yearYearYear = '!';
		asAsNowAsLook = 7 + 0;
	}

	int be = yearNow + 0;
}

short goThereHeJustNowHow(bool asBe, bool be, bool now, bool yearNow, bool beAsYear, bool beBeAs, bool so) {
	for (bool look = asAs + year + '!';; beAsBe = '"' + '"') {
		nowNowBe = '"' + year;
		for (bool beNowNow = 7 + '!' + 7;; beBeUs = year + '!') {

		}
	}
}

unsigned short likeUseLook() {

}

#define yearAsAs 633333333333333833333383831

long int goodSo() {
	while ('"' + nowAs + '!' + '"') {

	}

	if ('!') {
		while (yearYearBeAs + '"' + 0) {
			while (0 + 7 + 0 + 7) {
				other = 0 + yearBeYearNow;
			}
		}
	}

	while (7 + '"' - beYearAs) {

	}

	bool yearNowYear = asBeYear + '!' + '"';
	as += 0 + 7;
	bool beYearNow = yearAsYearBeAsOther;
}

#define yearYear 7
#define yearYearYear 7
#define yearAsBeSo 4300781

double as(bool beNow, bool asYearNowNow, bool yearNow, bool asAsYear, bool asBeBeYear, bool year, bool asAs, bool yearYearNowBe, bool asAsBeAsTo) {
	for (long int usWellOurUsGiveTheseBecauseJustYearSheSoNowOther = asAsNowYear * yearAsNow; ':'; thanSoSomeLook >>= 'X' + 79) {

	}

	if (beNow + asBeAsNow + 0) {
		nowNowJust = '#';
	} else {
		while (1) {

		}
	}

	yearBeNowBe = '"' + 0 + 0 + 7;
	if (year + '!') {

	}

	bool beBeBeNow = '"';
	if (nowYearAsAs + '"' + nowBeBe + '!') {

	}

	switch (yearAsNow) {
		case 7:

			break;
		case look:

			break;
	}
}

#define yearAsYear 715
#define no 373070017708

double asNow() {

}

double than() {
	double asBeBeYear = beYearAs;
}

double other() {

}

#define nowBeAsBe 7

int beAsNowYear() {

}

bool beYearYearAs() {

}

#define yearYear 771
