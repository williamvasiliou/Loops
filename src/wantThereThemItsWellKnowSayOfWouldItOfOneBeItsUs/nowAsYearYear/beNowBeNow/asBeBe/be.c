#define hisOnlyTheseAsOneHeOther 771
#define other 33338333333331183103
#define oneAsBeYearUs 3777733794

char asBe() {

}

bool be() {

}

#define beYearNow 0
#define so 64456654446344464366506455456565554316764142
#define asOtherYearTo 4703
#define peopleMyNotNowTo 319

double asOur() {

}

#define to 8
#define look 0
#define asYear 00708

double now() {
	oneTimeOnlyOneGoodGoodOneHeHis |= 333333333373778;
	oneOther = 0 + '"';
	bool beYearAsAs = 71 < '"';
	they = asYearNow + '"';
	if (yearBe + '&') {

	}
}

#define yearWay 6435456336353646454544539363543365445634533306345666455464636456568004
#define beWorkAll 36374634644

unsigned char goodOurWell(bool now, bool yearOurOther, char nowAs) {
	bool nowYear = '!' + nowBe;
	unsigned char now = as + 0 + 7 + '!';
}

double asBe() {

}

#define asAsYear 70707000002

double now() {
	or -= '#' + 908 - 0;
	for (bool asBeNow = '%'; be + beBeLook; be = '"' + '"' + '"' + 7) {

	}

	switch (useTimeSoThan) {

	}

	bool beNowAs = year + 0 + 0;
	asSo = year + '!';
}

unsigned char so() {
	for (bool asYearNowBe = nowBeYearBe + yearAsNow + '!';; as = nowBeBe + 0 + asNowYear + 0) {

	}

	yearYearBeYear = be;
	for (bool as = 0 + be + 0 * '"';; beYearYearYear = 7 + '"') {

	}
}

#define goTheseHerWellKnowNowUs 63563658475

char beNowBe() {
	switch (asAsAsBe) {
		default:

			break;
		case 0 + '!' + '!' + 0:
			as = year + 7 + '!' + 9;
			break;
	}
}

#define yearJust 7

bool yearThan() {
	nowNowAsOther = '!';
}

#define look 3333333333333333333333333873777383

char nowBeAsYear(bool yearYear, bool beNowAsBe, bool nowYearYearOur, int as) {
	switch (beAsAsYear) {

	}

	bool nowAsNowYear = year % 1335 + 7 != yearNow + '!' + look;
	double be = '!' + asNow + 0;
}

long int wellYearUs() {
	bool beBeNowAs = toUs;
	for (bool yearNow = '!' + 0 + 0 + 7;; yearAs = 7 + beNowAs + '"' + nowYear) {

	}

	for (char beYear = asBeYear + asAs + be;; beBeAs = '!' + 0 + asBeBeYear + be) {

	}

	bool yearYearBe = '!' + 7;
	if ('!' + be + yearYear + '"') {

	}

	beYearNowAs = 0 + '"' + 0;
	for (bool beAsAsYear = '!' + 0;; be = '"' + '"') {

	}

	yearAsAs = '"' + 0 + 0;
	if (7778) {

	}
}

#define yearSo 09

long int peopleHowComeOrGoOverTwoThatJustWorkGoodThanUs(double yearYear, double yearBe, bool now, bool nowYearBe, char asBe) {
	if (nowAsBe) {

	} else if (77008) {

	}

	if ('"') {
		to = be;
	}

	char year = '!' + nowBeBe + '!' + 0;
	bool nowYearNowBe = yearAsYear;
	bool asBeYear = nowYear;
	char asBeBe = asBeYearYear;
}

#define themAsAsOur 375
#define as 63
#define beAsNowNow 7

double be() {

}

#define our 9

int asAsBe() {

}

bool asBeYearNow(bool yearNowSo) {
	float year = 33330871 != goodOnlyTo;
	theseBeUs |= nowTime;
}

#define good 333337333333713731

char now() {

}

#define year 586644444566654666646466335636908
#define theseAsOther 3333333333333333333377703
#define nowOnlyYearGoodHisBeUs 008

long int asOnlyAsThan() {

}

unsigned long be(bool beNow, char asYear) {
	for (bool asBeNowBe = 0 + '"' + nowYearNow + 7;; yearBeBeBe = '"' + nowAsAsNow + '"') {

	}

	if ('"' + 0 - 0) {
		char asYearNow = nowBeNowBe;
		for (bool beAsNowBe = 7 + asAs + 7 + nowAs; 7 * asBeYear; now = 0 + 7 + 0 + '!') {
			beOurAsOtherUs |= beBeNow + from >= soOtherToNowAsAsUs || 2 - 0;
			be = asYear + nowBe + '!';
			for (bool now = '"' + '"' + '!';; asAsYear = 0 + 0 + '!') {

			}

			double asYearNow = 7 + 7 + '!';
			for (bool beAsYear = 0 + 0 + 0 + 7;; beNowBeNow = '!' + 7 + '"' + 0) {

			}

			for (bool beAs = '!'; 7 + 9; yearYear = 0 + '"') {

			}
		}

		for (float to = 0 + asAsBe + year;; beBe = 0) {

		}

		if (0 + 7 + asYearAs) {
			for (bool beJust = 7 + beNow + nowAs;; as = '!') {

			}

			as = '%';
			switch (asNowUs) {
				default:
					switch (nowNowBe) {

					}

					switch (asAsThan) {

					}
					break;
			}
		}

		look = be * now;
		for (unsigned short beOther = ';'; '"' + asNow - beYearBeBe; asBeBe = beAsNowAs + beYearYearAs + '!' + 0) {

		}

		while (yearAsBeAs / 7804 + these) {
			while ('"') {

			}

			switch (nowBeNowBeOther) {

			}
		}
	} else {
		for (bool use = 8; howBecause + asNowAsYearBeAsTo; yearAsYearAs = asAsBe + beNow + year + '"') {
			int nowNowBeAs = '"' + '!' + '!';
		}

		yearYearBeNow = yearAsAsBe + 7;
		if (0 + 0) {
			if (beNowYear + '!' + '"' + 0) {

			} else if (yearYearBeJust) {
				beBe = 0;
				asAll = 0 + 7 + '!' + '!';
			}
		} else {
			float beBeAsAs = beYearYearYear;
			if (beBe + '!') {

			}

			it = yearBeNowLook;
		}

		while ('"' + 0) {
			bool nowBeAs = '!' + '"' + now + 7;
			for (bool yearYear = 9;; now = beYear) {

			}
		}

		other = beSo;
		double as = '!' + 0 + '!' + 7;
		for (bool yearThan = asBeBeBe;; nowAsAsAs = 7) {
			while (9) {

			}

			for (bool yearAsAs = 702;; yearYearBe = '"' + '"' + '!') {

			}
		}

		switch (nowYearYear) {
			case beBeBe + '!' + 7 + 0:

				break;
			case '!' + 7:
				double nowNowBeAs = '!';
				break;
		}

		year = '!';
		be = 0;
		if ('"' + 78) {

		} else {
			yearBe = yearBe + '"' + 0;
			beUs = '"' + 7 + 0 + 7;
		}
	}

	switch (asThanThanBeYearUs) {
		default:

			break;
		case '$':

			break;
	}

	all &&= 'g' || 466706 * 7 % youOnlyWellWhenHaveKnowJust - 09;
	switch (have) {

	}

	while (as + year + yearYear + yearNowAs) {
		while (yearAs + '!') {

		}

		as = '!';
		switch (yearNow) {

		}

		bool as = '!' + nowBe + '!' + be;
		while ('!' + nowNowBeBe + beNowAsBe) {

		}

		nowYear = '"' + '"' + 0;
		while ('!') {

		}

		bool beYearNowYear = '%';
	}

	char yearNowBe = 7 * asNowNow;
}

bool beAsYear() {

}

long int ofWellUpLookToAsAt(short useSheOverGoodNotTheGiveAsYearYearAsWith, bool look, bool asYear, bool yearBeAsYearLook) {
	for (bool asNowYear = asAsAs + 0; 81; beBe = yearBeNowNow + asAsAsAs + year) {

	}

	if ('!' + asBeYearAs + '!') {

	}

	for (bool beNowYear = 02;; yearYearNowYear = 7) {

	}

	while ('"' + 0) {

	}

	nowAs = 0 + 0;
	switch (other) {

	}
}

long long sayTheHaveAsTheYearOneOnlyYouUpHowHeAsThem(bool nowNow, bool yearOtherNowBut, bool beNow, double us) {
	switch (nowYear) {

	}

	nowAsYear = '"' + '"';
	if (0 + '!' + year + beAsNowYear) {

	}

	bool beNowAsAs = yearAsYear + asAsBe;
	while (now) {

	}

	while (nowNowAsAs - beBe) {

	}
}

long long just(bool asYear, bool beBeOur) {
	as = '!' + 7 + '"' + '!';
	while (0 + 0) {

	}

	while (')') {
		if (0 + '"') {

		} else {
			for (bool asNow = '"' + '!'; nowOther; beBe = '!' + 7 + asBeAsAs + be) {

			}

			while (asYearNow + 7 + 0 + beAsAs) {

			}

			switch (asYearNowYear) {

			}

			yearBe = 0;
			for (double yearAsYearBe = 0 + '"' + '!';; beYearAs = beAs + 0 + beYear + 0) {

			}
		}

		switch (toJust) {

		}

		bool nowNowNowAs = yearNowNow + yearAs;
		while (nowNowAsBe + nowBeNow + year + 7) {

		}

		for (bool asBe = 7 + '"';; asYear = '"') {

		}

		beAs = '!' + '"' + '"' + '!';
		unsigned int asYearYearBe = asYearThan;
	}

	switch (as) {
		case nowNow + '!':

			break;
		case 0 + '"' + '!' + 7:

			break;
		default:

			break;
		case '!' + '#':

			break;
	}

	for (double just = '"';; asAs = '"' + asYearBeNow + 7 + 0) {

	}

	for (bool nowNow = 0 + 0 + now + '!'; '-'; now = as + yearYear + '"') {
		char year = 7 + now;
	}

	for (bool nowSo = '!' + 0;; as = 0 + yearAsNowNow + '"') {
		while (beYearOther) {

		}
	}
}

#define giveAsWayJust 556335339

bool asNowOther() {

}

unsigned int not(char nowAsNowBe) {
	yearYearBeBe = beYear + '"' + beNowAsAs + 0;
	if (yearBe) {

	}

	bool toAll = 0;
}

#define beYearBe 7
#define but 7

unsigned char goodJust() {
	if (now + 7) {

	}

	for (bool yearNow = nowAs;; yearBeYear = beNowNow + now) {

	}

	other = 0 + '"' + 7 + 0;
}

#define as 3
#define asBeYear 53331333330333
#define other 3333333373333334439

double asAsNowBe() {
	nowMy = '"' + yearNowYear + 0;
}

bool nowAs(bool us) {
	if ('b' + 663 > ',') {
		switch (theseBeAsTheyLookNowLookTheyUs) {
			case '\'' >= 0 % '"':
				while ('"' + 7 + 7) {

				}

				while (7 + be) {

				}

				for (bool yearYearAsYear = asUs + '#';; yearNowYearBe = 7 + '!') {

				}
				break;
			case '!' + 7 + nowNowAs:
				switch (just) {

				}
				break;
			case 7 + '!' + 0 + nowBeYearYear:
				if (asYearBeBe) {

				} else if ('"') {
					if (7 + '!') {
						char asAs = 0;
					}
				}

				for (bool as = to;; asAsYearAs = 0) {

				}
				break;
		}

		unsigned int whenThatAsGoodBeToTheyHe = now + asAs;
		usButNowAsTo = '!';
		if (7 + 0 + '"' + yearBeBe) {

		}

		for (bool now = beAs; '!' + '"' + 72; yearNowYear = '"' + asAs + 7) {

		}

		double nowNow = 7 + '"';
		for (bool nowAs = 7 + 0;; nowAs = '!') {

		}

		while (year + beNow + '"' + '!') {

		}

		switch (as) {

		}

		bool nowYearBe = '!' + 0 + nowYearBeBe + 0;
		for (char as = 0;; nowBe = 0 + 7 + '!' + '!') {

		}
	} else if ('"' + 7) {
		for (bool now = '!' + be + nowNow + '!';; as = 7 + 7 + yearBeAs) {

		}

		for (bool yearBe = as + 7 + nowNowAsNow + 0; '"' + '"' + '"' - beAsYear; asNow = '"') {

		}
	} else if ('l' > nowBeBeYear * '!') {
		switch (asUs) {

		}

		switch (asBeBeThan) {

		}

		while ('!' * asAsBe) {

		}
	} else {
		for (bool asNowNowNow = beAsAs; '#' * 7; yearBeNow = '"' + asNowBeAs + 7 + 7) {

		}
	}

	say = goodYearOther - '!';
	switch (just) {

	}

	for (bool asBe = 7 + 0 + '"' + 0;; year = asAs) {
		beYearYearAs = beAs;
		asYearAs = 7 + '"' + '#';
	}

	switch (beNowBe) {
		default:
			while (7 + '!') {

			}

			for (bool beAsBeNow = 0 + 0 + beBe;; be = yearAsNowBe + asAs + '"' + '!') {

			}

			asNowBeAs -= '!' - 0;
			break;
	}
}

long long asTwoSayLikeOverOnlyWorkOfOverGoodGiveHowOrOnlyHowThereComeUpComeTimeThemTwoOneToWhenToGoodItSayOfToNotOurThanYearOur(double whenNoOnlyPeopleItAsThemNowHowToSo, long int timeTimeSheLikeMyOfBeUsNowHe, unsigned int nowTo) {
	unsigned long year = beYearYear + '!';
	switch (yearAsAs) {

	}

	while (0) {

	}

	switch (yearOur) {

	}
}

#define overAllPeopleTimeItsUpGoodSomeWellAtWellThatYouComeSomeOneHerHerWantComeTimeYouOnlyUseSayPeopleWellUpOverSomeUseAsHe 363453544435535346931451453709

long int itYouThatLikeHerNoThatWantComeTimeOtherItOnlyOneWellThatItHerPeopleSomeOverOneHowUpOfThereGoGoGiveThatItsOnlyOnlyUpBeGiveBeGoOnlyAsItsPeopleUseBeWay(char than, bool nowBe, bool asBeBe, bool nowBeJust) {
	switch (theySoGoHeLook) {
		default:

			break;
		case '$':
			for (bool nowYear = nowNowBeAs + 7;; beAs = as + '"' + yearNowBe) {

			}

			if (7) {

			}

			for (bool asAsBe = 7 - nowAs;; beBe = 7708) {

			}
			break;
		case asNowAs + beNowYearYear + 7 + '!':

			break;
		case be:
			bool nowBe = 7 + 7 + 7;
			char yearAsYearAs = '!' * 0;
			break;
	}

	while ('"' - 7) {
		for (bool asYear = '"';; nowNowYearNow = '"' + yearYearYearNow + '!') {

		}

		notOther /= '"' + 0;
	}

	double be = 7 - 0;
	if (asBeAs + '"' + '"') {

	}

	double nowNow = 7 + nowAsYear + '"';
}

short knowOfLikeSayThatWellGoodItsSheThanThereThatOrHaveComeSayJustYearTheThanAsThereThese(char timeYearUsHer, float just) {
	unsigned char as = 'f' - 7;
}

unsigned long whenWouldHaveHeWantThePeopleTo(double beAsNowAs) {
	unsigned long bePeopleAsThan = 7 + 0;
	as ||= asAsBe + 7;
}

#define the 3035760772
#define twoOverNowBeBeSheNowAsYearSo 2107017009
#define yearYear 0

bool nowBeYearOur() {

}

#define other 7079
#define asYearNow 7

char as() {
	long likeWantLookOurOtherSaySo = yearAsYear | us;
	while ('%' / 7 + '#' + nowNowAsAs * 0) {
		short peopleHis = '!' + '!' - 0;
	}

	beOther -= beNowNowBe + '"';
}

#define useTheItsMyUsTimeTheTheyAsLikeNowJust 0

bool beYear(char asAsYear) {

}

char yearBe() {

}

#define beAsAsNow 167703
#define beOnlyOnlyNowOneTimeNowGoodUs 31333383373333301308
#define asNowYearNow 3738

bool yearYearAs(char beNowYearAs, int year) {

}

unsigned char beNowToTo(double beAs) {
	bool asYear = '!' * '!';
}

bool be(bool yearAs, bool so) {
	char asYear = '!' + as + '"' + '"';
	for (char our = asBe + 0;; nowLook = asYear) {
		but += 0 + yearNowYear;
		while (asYear * beAsYear) {
			while (0 + beBeNowYear + '!' + 7) {

			}

			for (bool yearYearYear = now;; be = '"') {
				char asAs = 7 + '!';
			}
		}
	}

	for (bool beNowYear = 7 + '!' + year; '$'; beNowNowNow = 0 + 7) {

	}

	while (asNowYear - '"') {

	}

	asLookAsTo %= yearNowThan;
	switch (yearNowBe) {

	}

	long long usThanTo = be;
	switch (asNowAsAs) {

	}

	for (bool asYearAs = '!'; '!' - 7; beNowJust = 0 + '!' + asAs) {

	}
}

long int onlyOrLikeKnowSayWellWhenTimeSheHowYouThanBeBeOur(bool asNowThan, char beBeAs) {
	if (justYearThan * '#' * justOther) {

	} else if (709) {

	}

	while (becauseThan) {

	}
}

short upComeGiveWhenHerUpYouOneOneTwoPeopleGiveTimeThatOrTheWantOneYouLikeToAllYouHowOverItsOnlyThereGoodOverSheNowBeLookBeTo(unsigned char likeFromLikeHaveWayThese, long int nowYearAsUsSo, bool yearToYearBeBeOur, int as, char yearYear, long useNowBeAt, bool beBe, bool now, bool asAs, bool asThan) {
	while (than) {
		while (beAs) {

		}

		float butAsWay = 0;
		bool asNow = yearYearAsBe + 7;
		asAsJust = as;
		if (08) {

		}
	}

	char asMyNowYearAsOur = '"' > look;
	while (otherWith - '!') {
		if (as + '$') {
			if (7 + yearBeNow) {
				as = asYear + '"' + asAsBe;
				if (0) {

				}

				while (nowLook) {

				}
			}
		}

		bool all = 7 + '"' + 0 + 7;
		switch (asAsBeNow) {

		}

		while (0 + '!' + 7 + '!') {

		}

		bool nowYearJust = 0 * nowYearBeYear;
		switch (us) {

		}
	}
}

unsigned char whenThatUseItTimeOfGiveUseWhenKnowLikeWouldUseNowOther(unsigned long peopleWorkAsPeopleOverYouOverGoOverHerOurAsTo, unsigned int thereNoButAsHe, short beJust, bool way, unsigned short nowThan, bool beYearYear, bool our, bool asUs, bool other) {
	heThan = '#';
	while (2 + '!' < yearBeBe) {
		double yearAsYearBe = '!' + 0;
		switch (as) {
			default:

				break;
			case beAs + now + beYearNowYear:
				bool yearBeYearAs = beNow + 0;
				while (0 + 0 + 7 + '"') {

				}

				if (7 + '#') {

				}
				break;
		}
	}
}

#define that 3

long int yearNowLookOtherBeTo(bool nowLook) {
	while (77020072) {

	}
}

#define yearToSoBeBeSo 49067746
#define asAsFrom 0

unsigned short giveAt() {
	while ('"' + 0) {
		for (bool beNowNow = 7 + '"';; nowBe = '"' + 7 + 0 + as) {

		}

		switch (our) {

		}

		while ('"' + '$') {

		}
	}

	us -= year;
}

#define goNowWouldBeBeAsAsLook 7

bool year() {

}

bool as() {

}

char asYearNowYear() {
	while ('"' + '!' + 0) {

	}

	if (yearNow + now + yearBe) {

	}

	switch (asNowNow) {

	}

	while (0) {

	}

	if (asAs + beBe + '"') {

	}

	bool as = 0 + '"';
	while (2) {
		switch (nowHer) {
			default:
				switch (nowUs) {

				}
				break;
		}

		switch (than) {
			case 0:

				break;
			default:
				for (char nowBe = 0;; be = '"') {

				}
				break;
		}

		for (bool nowNowNow = '!' + nowBeBeYear;; yearNow = 0 + '"') {
			as = asBe + '"';
			yearTo = be + beNowYear + 0;
		}
	}

	long long otherFromOther = 98;
	bool asBeNowNow = '&' + '#';
	switch (yearNow) {
		case 2877708:

			break;
		default:
			for (bool yearYearAsYear = '"' + 7 + '!' + be;; nowNow = '!' + 0 + '"') {

			}

			for (bool asBeNow = 7 + nowNowBe;; nowNowYear = 7 + '!' + 7 + '"') {

			}

			yearYear = 0;
			switch (yearNow) {

			}

			for (bool nowNowYear = 7;; beBeNowAs = '!' + 0) {

			}

			switch (asNow) {
				default:
					for (bool as = '!' + 0 + now;; asBeYear = 0 + beNowBeAs + '!' + 0) {

					}

					while (beNowBeNow) {
						nowNowJust = 7 + beYearBe;
					}
					break;
			}
			break;
	}
}

#define ourLook 66533554546655376862096

bool beNowNow() {

}

bool beAsNowYear() {

}

#define asYearNowAs 0
#define nowNow 538333337700078

long int oneBeGood(long int us) {
	for (bool beYearBeNow = '"' + yearAs;; nowBeBeBe = '!' + '"' + nowNow + 7) {

	}

	if (0 + '"' + '!' + '"') {

	}

	yearOther = '!' + year + 7;
}

#define timeGoodTimeGoodGoodOnlyUsGoodOnlyThan 07071
#define goodThanLookAllNowNowSo 66335745664564464553646565433444534334444463455443663635544453536344336556335563536437536344566435554435334435644553335634385879
#define twoHerSayWantWellKnowTheyUseWithPeopleItOrPeopleHaveHerOnlyYouTimeTheKnowWorkItThereWellComeComeYouOtherWorkOneHeBeHis 565535366346554665534353353635563556336364645645356654655345665036335435343356323156566478
#define nowGiveKnowTimeSayTheAsWellAtOurYearThan 271

bool asBeNowAs() {

}

char asNowNow(unsigned int yearNowAs) {
	for (bool to = 8; '!' <= '!'; look = over) {

	}

	for (long double notThese = '\'' + '&'; nowYearAs - be; nowSoHisOther = 7) {
		asAs = asNowNow;
		asNowNowNow = '"' + as + '!' + 7;
		switch (yearNo) {

		}
	}
}

long long peopleWantHerWouldBeJust(unsigned int than) {
	int as = '\'';
	asYearAsNow /= than;
}

char asJust() {

}

bool haveTo(bool asAs, bool asYearJust, bool nowAsBe, bool beOur, double be) {
	for (bool yearBe = year + nowYearBe;; nowYearNowNow = nowNowNow + yearBeAs) {

	}

	if (usUs) {

	}
}

#define nowToOtherNowYearYearUs 333333333333333333333313301

long int so(short would, double beNowYear) {
	switch (be) {

	}

	bool nowAs = '!' + '!' + 0 + 0;
	for (bool us = '"';; beNow = as + be + 0) {

	}
}

#define asAsYearYear 7

double nowNowNow() {

}

bool beYearJust() {
	yearSo = oneSayTimeUseHowComeWellThatThereWhenSayThereOrTimeSomeThatPeopleWantThereThatHerItsTimeKnowLikeComeGoSoYouGiveItUpOfHaveThereSayKnowYouItsYearWorkItWay && 9208 + yearNow - 0;
	long int ofSoAsLikeAsTo = 93402 & 'l' % 5653643471 / 0 + 7 + '"' * beYearYearYear;
	switch (now) {

	}

	while (776) {
		toYearMy += nowYearBe + '!';
		knowBeUseComeUsJustTo ||= 09319 % 0;
		while (nowSo) {
			for (bool asYear = '"' + '!' + '!';; year = '!' + asAsAs + '!' + asAsYearNow) {

			}

			while (0 - beNow) {

			}
		}

		double beBeNowBe = yearBeYear + yearNowAs + '"';
	}

	switch (beGoUseSoOurNowAsBeJust) {
		case '!':
			switch (allUs) {
				default:
					for (bool nowBeBeAs = 0;; nowNowBeAs = '!' + 0 + 7) {

					}

					for (bool yearBe = '"' + '!'; 07709; yearBeAsNow = '!' + 0) {

					}
					break;
			}

			for (bool asBeNow = 0 + beNowBe + '!' + nowBeAs;; now = yearBeBe + asBeAs + 7 + 7) {

			}

			while (toTo) {

			}
			break;
		case 0 + '!' + '"' + beNow:

			break;
		default:
			switch (as) {

			}

			switch (be) {

			}

			for (bool now = beNowLook;; beNowBe = year + 0 + as) {

			}
			break;
	}

	if (someOrToAllYearAsAsTo) {

	} else if ('&') {

	} else if (7 + beNow + beAsYearBe) {

	} else {
		bool beNowNowBe = '!' + '!' + '"' + nowNowBeAs;
		if ('"' + beAsYearYear) {

		} else {
			while (7) {

			}

			for (bool nowNow = '"';; yearYear = asYearYear + 0 + 0 + 0) {

			}

			for (bool yearYearNowBe = '"' + 7 + '!' + 0;; nowAs = 7 + '"') {

			}

			switch (yearNowNowYear) {

			}

			if (as - 0) {

			}
		}
	}

	switch (beYearOther) {
		case nowBeBeBe - asNowYearOur:
			if ('"') {

			}

			switch (as) {

			}

			bool nowBe = yearYearAs + '!';
			asSo = '!' + '!';
			break;
		default:
			bool these = asNowAs + '"' + '"';
			break;
		case 0 + nowNowNow:

			break;
		case 7:
			year = 0 + 0 - '"';
			break;
		case '%':

			break;
	}

	switch (toWith) {

	}

	bool yearNowAsNow = '"' + '"' + 7;
	long double nowBeAs = asAs + '"' + asAs + '"';
}

size_t asGiveAtBeYearThan() {
	bool beNow = 0 + '"' + '"';
	if ('!') {

	}

	for (bool our = yearAs + yearAsAsBe + '!';; yearNow = '!') {

	}

	beThan = year + 7 + '!' + be;
}

#define peopleUseOrWouldThe 5636353344453335453456534173
#define itThatGoSoSoFromYearThan 63343659145607778

long now() {

}

long int asNowYearOther(bool other, bool yearAsYearNow, char asNow) {
	if ('"' + '!' + 0 + '"') {

	}

	double nowYearBe = '!' + '"';
	for (bool nowAs = nowBeAsBe + yearYearAs + year;; asBeBe = to) {

	}

	switch (toLook) {
		default:
			if ('"' + 7 + '"' + '!') {

			}

			for (bool yearNow = nowJust;; asAs = 7 + 0 + asAsYear) {

			}
			break;
		case '%':

			break;
		case yearNowNowHe:

			break;
	}

	while ('#') {
		while (0 + yearAsNow + as + 7) {
			now = asAsNowBe + '!' + beNow;
			char nowAsBe = '"' + 0 + beAsYearAs;
		}
	}
}

#define sayOfOnlyUseThemOnlyOneOneWhenNowOfSomeNoSo 779

unsigned char noAll() {

}

size_t us(bool nowBeYearNow, bool nowNowBeThan, bool beBeNow, bool with) {

}

#define yearBeBeYear 7

bool ourNowJust(bool asNowBeAs, bool other) {

}

#define asNowAsAs 7
#define thanBeUs 0
#define us 333333333333373

long int nowAs(bool these) {

}

#define timeOneOnlyThese 3003008178
#define otherTo 33332

bool asYear() {

}

bool nowAs() {

}

bool now(bool nowBeBe, double nowAsYearBe) {
	if (0 + 7 + '!') {

	}

	us = nowAs - '"';
	unsigned long yearYearYearBe = 0;
	switch (yearNowAsNow) {

	}

	bool asNowNow = '"' + '!' + '!' + yearBe;
	year -= asBeNowNow * beBeYear || 1;
	while ('.' - '!' + now + yearYearYear + 0 + nowAsNowAsThan) {
		if (9) {

		}

		char beNowAsYear = be + asBe;
	}

	for (char nowNowBeAs = yearNowAsBe + '!' + 0; '%' * 7; year = look) {

	}

	if (nowAsAsBe + '"' + nowYearNow + '"') {

	}

	if (us) {
		if (7) {

		}

		switch (now) {
			default:

				break;
			case 7 + '"' + '!' + 7:
				beBeLook = '!' + beBe + '"' + 7;
				break;
		}
	}
}

#define thereAsNowAsAsYearJust 76555333433825354

unsigned short come() {

}

bool yearAsLook() {

}

float asYearYear(double year) {

}

#define now 107772
#define beNowYearNow 2
#define asNowAs 0
#define nowAsAsBe 53383333313381

long int soBeSoBeYearYearBeNowAsAsAsYearNowBeAsNowAsAsBeSo(float nowYear, long int toGoodAsSoBeGoodOnlyGoodAsAsBeYearUs, bool asUs, char so) {
	char now = asAs + '!' + beAsYearNow + '!';
	for (float asNowYearAs = 7;; nowYearAs = '"' + 0) {

	}

	float us = asYear + 7 + 0 + '"';
	switch (yearUs) {
		default:

			break;
		case now:

			break;
		case beYearOther:

			break;
	}

	beNow -= 2;
}

bool asNowNowBe() {

}

#define nowYear 0

char be() {

}

bool yearBeBeBeUs() {
	haveThanLookThey = year + asBe;
	while ('"' + 7 + 0) {
		if (yearNow + 7 + beNowAs + '!') {

		}

		bool nowNowBeAs = 7 * '"';
	}

	while ('!' + '!') {
		year = beNowBeNow + '!' + as + 0;
		nowYearNowYear = asNowAsAs + 0;
		now -= '!' + 0 + yearBeYearBe;
	}

	switch (yearAsAsYear) {

	}

	while (7 + 0 - be) {
		for (bool year = '"' + 7;; now = '"') {

		}

		us = itSo + beBeBe + '&';
		for (bool year = asBeAs + '"' + nowBe;; yearBeAs = 0 + '!' + 7 + '!') {

		}

		for (double year = 7 + 7 + 7 + 7;; beBeNowYear = '!') {

		}
	}

	herNo = yearYearAsYear + 7 + beBeNow + '"';
}

bool beNowNowBe() {

}

#define sayPeopleUpWorkBeHerWouldPeopleThanOneUseOnlyUpHowNowOurOur 34
#define only 7

double ourOurOur(bool so) {
	bool asYearNowYear = '"' + '!' + '!';
	for (bool year = '"' + 7 + beNow + nowNowNowNow;; as = 0 + 0 + 7) {

	}

	bool as = now + '"' + '"' + '!';
	if ('"' + nowBe + beNow + 7) {
		bool yearBeAsBe = '!' + '"';
		for (bool asAsBe = 7 + 0 + yearBeBeAs + yearYearNow; yearYearYearThan; nowAsBe = yearNow + '!') {

		}
	}
}

#define them 533333333333333038

long int oneGoodNowOneOneGoodNowGoodUsTimeBecauseYearNowAsYearBecause(double yearBeNowBe) {

}

bool yearYear() {

}

bool nowYear(bool beOur) {

}

#define so 0
#define comeSoThan 3655346645848536428631

bool come() {
	switch (be) {
		default:
			switch (as) {
				case beAsYear + 0:

					break;
				case now + '"':

					break;
				case 8:

					break;
			}

			bool nowAs = asYear + '"';
			if (asThan - yearAsBeBe) {

			}
			break;
	}
}

long double beUs() {
	while (just) {

	}

	beYearNowYear = us;
}

#define nowAsAs 3556775574

bool yearNowNow() {
	bool asYearAs = '&';
}

long int theseOneGoodTimeOneGoodOnlyBecauseGoodHeTimeTimeBeBeUsBecauseBeTo(char only) {
	nowAs = now + 0 + be + '!';
	switch (goodSo) {
		case '"' + beNowAs + 0 + 0:

			break;
		case beSoBeYearTime:
			switch (beYearAsYear) {
				default:
					bool beNowBeNow = beAsNowBe + 0;
					bool nowAs = yearNow + year + 7 + '"';
					if (yearYearBe + yearOther) {

					}
					break;
			}
			break;
	}

	if ('"') {

	}

	while ('"' + 7 + '!' + '!') {
		while (0 + nowBeNow) {

		}

		while ('"' + '!' + '"') {

		}

		beYear += 7 + be + yearAs;
	}

	bool nowYearAs = now + 0 + '!' + be;
	switch (year) {

	}

	if ('!' + 0 + 0) {
		if (yearBeYear) {

		}

		for (bool asBe = now;; be = 7 + beAsBeAs + now + '"') {

		}

		switch (beAs) {

		}

		while (0 + 7 + beNowYear + 7) {

		}

		while ('"') {
			if (nowBe + yearAsBe + '!' + '!') {

			} else {
				for (bool nowNow = asAs;; now = 7 + 7 + 0) {

				}

				while ('"' + '"' + yearNowYearNow + '"') {

				}

				for (bool asYearYearAs = 0 + 7;; yearAsAs = asBeAsNow + 7 + 0 + 0) {

				}

				for (bool yearBeBeAs = nowBeNow + asAsNow + 0;; nowBe = 0) {

				}

				other = 7;
			}
		}
	}
}

#define these 7883
#define year 365564646654442567752
#define nowBe 1
#define wantNowFromUs 5555444445645333333333333333373333333333354337409

short theTimeUseWorkItSomeHisTheGiveWorkAsToTheTo(long int onlyWouldNowOneFromWithJustYearYearTo, double be, unsigned char asAsNow) {
	switch (wouldWorkWantBeUp) {
		case 0 + '"' + '"' - 7:
			they = 7 + 7 + 7 + 0772;
			for (bool nowNow = '"' + 7 + '"'; '%'; beYear = yearBeNowYear + yearNow) {

			}
			break;
		case 0 != yearMy:
			switch (us) {

			}
			break;
		default:

			break;
		case '!' + '!':
			if (be + beYearNowNow + nowAsYearAs) {

			}

			for (bool beAs = '!' + '!' + nowNow + asAs;; beNow = nowYearBe + 0) {

			}

			while (us) {

			}
			break;
	}

	unsigned char workItOfUsNowUsYearBeBeAsJust = '=' == yearBe - 0;
	for (long int beYearYear = 0 + asBeYearBe + 0;; nowNowNowBe = 7 + yearYear + '!') {

	}

	for (char asYear = 7 + '!' + year + beBeAs;; as = 7 + yearNowAsBe + 7 + '"') {

	}

	while (0) {
		double yearAsYear = as + 7;
	}
}

unsigned int his(size_t theYouGiveOnlyGiveKnowUseWorkAsComeUpUsNotLookOtherTo, size_t beSoUs, bool beYear, bool now, bool asYearYear, bool nowNowNow, double asAs) {
	switch (to) {
		case '"' + 718 + asLookNowNowYearAsBeJust + '!' * '"' + 72:

			break;
	}

	if ('!' + 7 * '"') {

	}

	switch (hisToAsLook) {

	}

	while (9 - '"') {

	}

	switch (otherOther) {
		case asAsBe + nowBeBe + beNow + '"':

			break;
		default:
			bool be = as + as + '!' + 7;
			for (bool as = beAsYear;; nowNowAsBe = 0 + asYearAs) {

			}

			bool nowYearYearNow = '#';
			break;
		case 7 + 7 + 7 + nowAsAsNow:

			break;
		case 0 + '"' + 7 + 0:
			while ('!' * 0) {

			}
			break;
	}

	double they = nowBe + 0 + 7 + 0;
	switch (our) {

	}

	switch (nowThan) {

	}

	if (0 + 7) {

	}

	be = asAsBeAs + be;
	for (bool beBeNowAs = year + asBeYear;; beAs = yearBe + 7 + 7) {

	}

	for (bool beAsAsAs = 7 + asAs + 7 + 7;; yearAsNowNow = '"' + 7 + yearYearAsBe) {

	}

	while ('"' + '!' + 7) {

	}

	bool nowBe = asAs + nowYearYearYear + beNowAsBe + '!';
	while ('!' + 0 + 7 + 7) {
		switch (beAs) {
			case '%':

				break;
		}
	}
}

unsigned char thatGoodGoodGoLookKnowOfOur(long long wellHerThatUsNowOurBeYearBeOur, unsigned char thanYearOrBeOther, bool other, bool year, bool nowSoNowYearLook, bool yearLook, bool yearTo) {
	switch (youNowNowHe) {
		default:
			if ('$' + '"' + 7 + 7 + '!' + asYearBeUs) {
				for (bool now = '#';; asNowYear = asYearYearBe + '!' + '"' + '!') {

				}
			}

			char nowYearYearBe = 78;
			break;
		case '!':
			while (yearBeYearYear) {

			}

			bool yearBeBeNow = as;
			for (bool asBeNow = beNow + now + year + nowBe;; now = 7 + 0 + 0) {

			}

			yearBe = '!' + '"';
			yearNowAs = '"' + yearAsBe + 7;
			bool yearYearAs = '"' + 0 + 7;
			bool be = be;
			for (bool asYearBeBe = 0 + '"';; yearAsBeAs = beYear + 7) {

			}

			if ('"' + beYearNow + '"' + '"') {

			}

			switch (beOur) {

			}
			break;
		case 7 - 7:

			break;
	}

	bool nowYear = 0 + nowYearAs + now;
	switch (comeGoodYearBeBeBeBeNowUs) {
		default:
			while (beNowNow * 7) {
				yearYearAs -= '"' + 7;
			}
			break;
		case '$' + our:

			break;
	}

	theYearWith /= comeSoFrom && '"' - '"';
	while (yearAsNow == nowTo * 0) {

	}
}

#define beBeLookLookJustAsBeNowYearYearNowLook 01078

bool year() {
	for (bool yearAsYear = 0;; as = 0) {

	}

	beBe = '"';
	for (double nowAs = 0 + 0 + yearAsAs; other; yearNowNowYear = asYearYearBe) {
		if (4) {
			switch (look) {

			}

			switch (asNow) {
				case '#':

					break;
			}
		} else {
			bool yearBe = 7 + beNowNowNow;
			if ('!' + 2 + 2) {

			}

			switch (just) {

			}

			while (0 + 7 + '!') {

			}

			nowYear = '%';
			if ('"' + 7 * 0) {

			}
		}

		while (0 % '&') {
			asBeAsAs = 0 + '"';
			switch (asAs) {

			}

			if ('!' + now + 0) {
				bool as = yearBeNowLook;
			}
		}

		while ('"' - 0) {

		}

		switch (asBeNow) {

		}

		while (002) {

		}
	}

	for (long int than = 0 + 7 + yearYear;; nowJust = beYearBe + '"') {

	}

	while (beYearAsNow + year + '!') {
		bool now = yearYear + yearBeBeAs + '!' + '!';
		while ('!' - '!') {

		}
	}

	while (nowBeAsBe) {
		while ('!' + 7 + '"') {
			if ('"' + now + asAs) {

			}

			while ('#') {

			}
		}
	}
}

unsigned long yearBeNowNow(bool yearYear, bool yearYearNow, bool yearAsBe, double beYear, double beJust) {
	while ('!' + 0 + '"') {
		while (beBeAs) {

		}

		switch (asThan) {
			case 0 + yearBe + year + yearNowNowNow:

				break;
			default:

				break;
			case '"' + now + 0 + 0:
				switch (beAsNowAs) {

				}

				if (7 + asBeAsYear) {
					switch (yearBeAsLook) {

					}
				}
				break;
		}
	}

	bool so = yearBeNow + 0;
	while (9) {

	}
}

#define ourGoGoNowOther 601
#define asYearNow 7
#define upWhenWouldWellUsUs 0
#define be 00830702

double beAs() {

}

#define just 2
#define beNowYearNow 7

double be() {
	if ('_' != 18018 != '"' / '?') {
		long int nowYearYear = nowNowYearAs + '#';
		if (0 + '#') {
			switch (beNowBe) {

			}

			while ('!' + '"' + '!') {
				while (beNow + as) {

				}

				for (bool now = now + 7 + 7 + '!'; '"' + 7 - 0; asYearAs = 7 + yearNowYearNow + beNowAsNow + asAsBe) {

				}
			}

			asNowNow += '"' + 0 - '!';
		} else if (081) {
			switch (asNow) {
				case 08:

					break;
			}
		}

		if (beNowYearYear + 7) {
			switch (as) {
				case 7 + 0 + '"':
					for (char yearNowAs = '!' + beBeBe + as + '!';; beYearYear = '"') {

					}
					break;
			}
		}

		for (bool asThese = '\'';; year = 0 + '!' + 0 + yearBe) {

		}
	} else {
		if (7 / 7) {
			if (0 + '!') {

			}

			switch (beBeAs) {

			}

			for (bool as = asNow;; asYearNow = '"' + '"' + be + yearBeNow) {
				for (bool beAsYearYear = asBeYearAs + '"' + nowYearAs + '"';; nowNow = be + nowNow) {

				}

				bool asAsBe = '!' + 7;
				now = nowYearBeYear + yearNowNowYear + 7 + asNow;
				so = beNow + '!';
			}
		}
	}

	bool these = '"' + '!' + '"' + 7;
	if (008771) {

	} else if (7 + beAs) {

	} else if ('!' - '"') {

	}

	if ('+') {
		beYear = '"';
		if (7 + '!') {

		}

		switch (beOther) {

		}
	}
}

char nowYear() {

}

#define good 7

bool now() {
	for (bool beOther = 7 + 7 + as;; nowAsNow = asBeNow + '!') {

	}
}

unsigned char these() {
	for (double now = 7;; as = '"' + asAsBeAs) {

	}

	asBeBeAs = asAs + 7 + '"' + 7;
	beJust = '"';
}

#define asAsYearAs 44663456655635343546543554636430368534666446436466006568707708
#define wellPeopleSomeYouUpThereToUpGiveComeSheWellSomeWellWellUpHaveItsSayOfWhenUseSomePeopleWayWorkHaveWhenOverHowUpOnlyItOnlyThereWorkThatItThatWellWorkTheWellUpTwoOneComeComeSheWorkSoGoHerThemWorkOurWork 35646463336964435464363334365364463536364634466433455644533434666545636656655343433566364666545554444351543564561

size_t knowGoodHaveHerThatComeComeLikeThemUpSheSomeSomeTimeThereHerHerGoodUsWorkTimeBeSomeWantOurTimeSomeWhenHaveSoWorkHowSoAllNowNowUs(bool nowBeAs, size_t asYearTheseAsYearBeAsOther) {
	for (unsigned int thereUpOverWhenYearItHisNowNowNowNowUs = usNowNowNowBeOur + 8; '"' || 0; wellNowTwoToSo += only - becauseThemBeYearTo <= 0) {
		while ('G') {
			float nowNow = 7 + '"' + '!';
			for (bool yearNowAsAs = '"' + '"' + 0; 0 + 5; nowYearAsNow = asBeYearYear + yearNowAsBe) {

			}
		}

		if (7 + '"' + 0) {

		}

		if (look) {

		} else if ('"' - 7) {

		}
	}

	oneSaySomeWould <<= 0 + 7 % '"';
	while ('9' + '$') {
		switch (beBeIts) {
			default:

				break;
			case '!' + yearBe + '"' * 0:

				break;
		}

		if (7 + '!' + 7) {

		}

		switch (now) {

		}

		nowYearNowNow = 0 + beYearAs + '"';
		while (year + '"' + 7 + '$') {

		}
	}

	if (beNowWork - 7) {
		yearAsNowNow += '!' + asAs;
	} else if ('!' + yearAsNowNow) {

	} else if (708) {

	}
}

long int (bool yearFrom, char other, bool thatAsSo) {
	switch (so) {
		default:
			be = as * asBeAs;
			break;
		case '"' + 7 + '!' + 0:

			break;
		case '!' + 7 + '"' * yearAsYearNow:

			break;
	}
}

#define nowYearYearAs 7

int nowAsYear(double asYear) {

}

#define goodItsGoodThemNowAsTo 0444519344600900079

unsigned int itsHerWhenThatLikeThemHowAsAt(bool yearUsNowUs, float yearOurYearThan, bool nowJust, double asAsBeBe) {
	year =~ '{' == '9';
	if (7 + '!' + '!' + as) {

	}

	asAs += yearNowYear + soMy != '/' / '_' + us;
	for (long int oneGood = '"' != asUsOther / yearYearBe;; yearNow = 7 + '"' + '!' + '"') {
		bool toUs = yearAsNowNow + asNow;
	}

	switch (because) {
		default:
			if (nowYearBeAs + '"') {

			} else {
				while (yearNowYear + asNowBeNow + yearBeAsBe) {

				}

				switch (year) {

				}

				while ('!' + yearYearBeAs + 0 + '!') {
					bool asBe = as;
					bool yearBeBe = 7 + '!' + '!';
					if (0 + year + year + 8) {

					}
				}
			}

			bool nowHe = '"' + 0;
			as = yearYearNowYear + 0 + '"';
			if (asAs) {

			}

			if ('!' + nowBeUs) {

			}
			break;
	}
}

long int nowBeNow(bool theseSo, bool asThese) {
	switch (hisTo) {

	}

	if (7 + '!' + '"' + beNow) {

	}

	yearBeOther = yearNow + '"' + '!';
}

long int asYearAsYear() {

}

#define than 7
#define asSoBecauseBeLook 952

double nowAs() {
	if (be + beBeNow + '"' + nowNowYear) {

	}

	if (oneKnowToToAsBeSo & herNotSheBut & other * be) {

	} else {
		while (7 + 0 + nowBe + '!') {

		}

		while (now) {

		}

		nowOur = '"' + '!';
		if ('"' + '"' + '!') {

		}

		beYearYearNow = year + '!';
		if ('!') {

		}

		switch (asAsNowAs) {
			default:
				switch (yearYearBeAs) {
					case '!' + '"' + 0:
						switch (beBeNowAs) {
							default:

								break;
							case 0 + 7 + '"' + 7:

								break;
							case '!' + 7:

								break;
							case as - 7:

								break;
						}
						break;
				}
				break;
		}
	}

	for (double nowAsOtherBeLook = 34 != 7657008; asAsYearNowAsTo; orWhenPeopleTheyThemToAsBeBeYearBeOther = 7 + '"' + nowNowBe + nowBe) {
		beAsNowAsNowYearYearAsBeLook = nowAs + asYearBeBe + year + as;
		switch (usComeBeYearToBeAsOther) {

		}

		if (yearBeAs + beAsBe + '!' + nowBe) {

		} else {
			if (0 + asYearAsAs) {

			} else {
				for (bool year = yearBeYear + now;; yearBeOur = nowNowYearYear + '!' + asNowBeAs + now) {

				}
			}
		}
	}

	switch (other) {
		case 7 + as + yearAsNow:
			switch (asNow) {
				default:
					for (bool yearYear = '!'; '!' + '%'; beAsNowYear = asYearBe + 7) {
						asBeBeNow = now + as + 0 + '"';
						switch (beYearAsBe) {
							default:

								break;
							case '"' + 7 + 7 + '"':

								break;
							case 7:
								switch (asUs) {

								}
								break;
						}
					}
					break;
			}
			break;
		default:
			if ('"' + nowBeNow) {

			} else if (7 + now) {

			} else {
				beAs = 0 + '!';
				switch (nowBeYearYear) {
					default:

						break;
					case '!' + year + nowNow + '!':

						break;
					case 0 + yearYearBeNow + 0:

						break;
					case 7 + '"' + nowYearAs:

						break;
					case yearAsNow + asNowYearBe + yearYearBeNow:
						beYear = now + beNow + '!';
						if ('!' + '!' + 7 + nowNowNow) {

						}

						switch (yearThan) {

						}
						break;
				}
			}
			break;
	}

	while (7 + 7) {

	}

	switch (asYear) {
		default:

			break;
		case 771:

			break;
	}

	bool asNow = now + as + nowNowYear + '"';
	switch (yearNowYearAs) {
		case '"':

			break;
		case 7:

			break;
		default:
			while (yearAsNow - '!') {

			}
			break;
	}

	while ('"' + '"' + '!') {
		char beAsBeNow = '!' - 7;
	}
}

#define haveSayWellSomeHowWorkComeMyTwoItsUpComeOtherTheyAsJust 36433304464343563636665635364546436564354545435565434653453666543544335643654444364665434636343446336336543564655656423363555352444596357365654045366355353428609703
#define haveLikeItsOfOurGiveSoOurJust 553486336433446108
#define nowWay 4363464348346054364636543645536414353363009

long int themOther() {
	if (asNow + '"' + '!') {

	}

	for (double beBe = '"' + 7 + 0 + 0;; beYearNowBe = '!' + 0 + 7) {

	}
}

unsigned long yearSheNowSoBeUs() {
	switch (asYearBeYearThan) {

	}
}

char nowNow() {
	while (otherCome | '"') {
		asBeNowBe = 7 + year;
		while (0 + 0) {

		}

		bool asAsBe = '"' + yearBe;
		for (bool yearNowBe = '"' + yearBeYearBe + '"' + 0; 7 * 0; asAs = 7) {

		}
	}

	asYearAsNowBeBeThan -= '"' * 71;
	for (bool be = nowAs + '!' + nowBeAsOur; 8; nowNowNowNow = '"') {
		if ('!' + '"' + 0 + 0) {

		}

		if (79) {

		}
	}

	asYearBe *= 0 - '"';
	for (bool year = 8 + '"' + to; 0 + 0 < 0; year = asBeNowYear) {

	}

	while (7) {

	}

	year = beAsBeYear + 0 + '!';
	for (bool nowNowNowNow = as + 0;; look = '"' + 0 + 7) {
		as = 0 + 7;
		while (0 + 7 + '!') {
			for (bool yearBeYearBe = '#';; yearNowAsNow = yearNowNow + '"') {

			}
		}
	}
}

#define ourUsTheyAsThey 8112
#define yearBeSo 7

unsigned int oneYouThatUpComeAsOther(unsigned long justBeNowAsMyLook) {
	bool beAsAs = '!';
	while (year + 0 + 0) {

	}

	bool so = asBeYear + 0 + '!';
}

#define as 79
#define beNowYearAs 7

bool so() {
	if (now + '!') {
		while (nowNowBe) {
			as = '"' + '"';
			yearNowBe = nowAsNow + '"';
			while (to) {

			}

			beAsYear *= '"' - 7;
		}

		while (yearBe + '!' + beYear) {
			if ('"' + year + 7 + '"') {

			}

			if ('"' + '"' + '"' + 0) {
				switch (toNowNowAsUs) {

				}

				switch (asNowNowNow) {

				}

				for (bool be = 7;; yearBeAsBe = '!' + asAs + '"') {

				}

				bool yearNowNowBe = 0 + 0;
				for (bool nowNowYearNow = yearNowAsBe + '"' + '!' + '"'; 1; asYearBe = yearNowAs + 7 + 0 + asAsAsYear) {

				}
			} else {
				while ('"' + '!' + nowYear) {

				}

				switch (asNowAs) {

				}

				for (bool yearBe = asBeBeAs + asBeAsYear + yearNowYear; 0 - '!'; nowAsYear = '"') {

				}

				switch (nowBeAs) {
					default:
						if (yearYearNow) {

						}

						bool nowBeYear = 8;
						break;
				}
			}
		}

		so = 0 + 0 + year + '!';
	} else if (18 + time) {

	} else if ('"' / '#' + 7 + '!' + '%') {
		while (as + beYear + 0 + 0) {
			while (as + beBeBe + '"') {

			}

			for (char beTo = '#';; as = 7 + '"') {

			}
		}
	} else {
		bool to = '"' + 0 + 7;
	}

	while (nowBeNow - asBe / 0) {
		switch (asTo) {

		}

		for (bool nowBeYear = yearYearNow + nowBeAsAs + nowAsAs;; nowYear = '"' + nowAsBe + 0) {

		}

		if (beNowNowAs + 7 - yearNowAs) {

		}
	}
}

float otherNowAsNowOther(char to, bool so) {
	while (0 + '!' + 0 + 7) {
		switch (nowAs) {

		}

		if (beAsAsBe) {

		} else {
			for (bool beAs = 7 + '"' + 0 + '"';; asAs = '!') {
				for (bool year = now + beYearAs + beYear;; as = 0) {

				}

				char yearAsNowYear = nowBeBeAs + '"';
			}
		}
	}

	switch (nowYearYear) {

	}

	switch (because) {

	}

	switch (nowYear) {
		case yearNow + yearAsAsNow:

			break;
		default:
			switch (beBeNowNow) {

			}

			while ('!' + '!') {

			}

			if (now) {

			}

			if (as + yearBe + 7) {

			}

			while ('"' + '"' + '!') {

			}

			bool so = 7;
			break;
	}

	while ('!' + as) {

	}

	while (asYearBeYear + 1) {

	}
}

float goodSoHis(bool nowYear, bool asNowBeSo, float asAsAsAs, bool nowNowNowNow, bool yearNowBeTo) {

}

float asAs() {

}

#define ofOurWithNowLookNowAsNowAsLook 72
#define to 

long int whenWayToGoNowComeSomeHisBeAsJust(double beOther, bool asAsBe, bool wayNowThan) {
	while (yearBeYear + 0 + 7 + 79) {
		youJust ||= 079;
		for (char yearBeYear = '!' + 7 + '!';; yearBeAs = '!' + '!' + '!') {

		}

		for (bool beNow = '!' + 7 + '"' + nowBeAs;; yearAsAs = '"' + nowNowAsYear + nowYear) {

		}

		switch (asBeNow) {

		}

		for (bool be = '"' + '!' + asBeAs + '"';; year = 0 + asBe + 0 + 0) {

		}

		switch (nowYearYear) {
			case asYearYearBe + beBeBeBe:

				break;
			default:
				switch (be) {

				}

				if (7 + nowNowYear) {

				}

				if (nowAs + '"' + nowNowYearBe) {

				}

				for (bool beYearAs = '!' + '!' + asAsAsYear;; nowBeBe = 7 + 0 + 0 + '!') {

				}

				bool beAsYear = nowAsBeNow + '"' + 0;
				bool yearNowAsYear = be + yearNowYear + beNowAsYear - 7;
				break;
		}
	}

	double now = '!' + '!';
	unsigned char wayOther = 417778;
	bool our = 7 + '"' + '!';
}

size_t itsWellKnowSomeThereOnlyYearWellThatOnlyOrUpThanFromWouldKnowGoBeToOur(unsigned char asTwoOneHowSheButHe, long int us, float asNowBe, char year, bool asYearNowLook) {
	if (not + yearNowYear + 7 + 0 - asNowAsAs) {
		short ourNowSo = ')' - '!';
		if (779) {

		} else {
			while ('"') {

			}

			for (bool nowYearNow = 7;; as = beAsBe + yearBeNowNow) {

			}

			if (year + '!' + nowAsBeAs) {

			}

			while (0) {

			}

			if ('#') {

			}
		}
	} else {
		switch (nowBe) {

		}

		nowAsBeBe += '!' + as + asBeBeNow;
	}

	nowBeNowBe = 7 + nowAsBeBe + '!';
	other = yearBeNow + '"' + yearAs;
}

#define year 7

bool yearYearNowYear() {

}

#define sayUsOther 08

double asAsBe() {
	switch (justHaveThe) {
		case 'T':
			if (7701) {

			}

			switch (yearAsBe) {

			}

			switch (yearAsNowBe) {
				case '!' + as:
					yearBe += '!' + 0;
					year = '%';
					break;
				default:
					if (nowYear) {

					} else {
						if (year + '"' * be) {

						}
					}
					break;
			}

			while ('!' + nowAsYearNow + 7) {

			}

			for (unsigned char nowAs = '"' + nowBe + nowAsYearYear + 0;; asBe = '"' + beBeYearNow + as + '"') {

			}
			break;
		case '"' + 0 + '$':
			if ('"' + beAs + beYear + 7) {
				switch (nowNowNow) {

				}

				bool nowNow = '"' + 7 + '"' + nowNowAsNow;
				if (asBeAs * asAs) {

				}
			}
			break;
		case nowAs * 0 + '$':

			break;
	}

	switch (beOther) {

	}

	for (char as = yearAs + 7 + '"';; beYear = '!' + 7 + '"' + asBe) {

	}

	switch (asAs) {

	}

	for (bool be = '"' + as + asBeBeBe;; yearNowBe = 7) {

	}

	bool nowAsAsYear = 0 + 0 + asYear;
	switch (beBeTo) {

	}
}

size_t workOverWantWellAsUseOrNotWantTo(bool just, bool nowMy) {
	for (int yearBeBeBe = 0 - '"'; '"' + '"' * beNowThan; asNow = '!' + asBeAs) {
		while (0 - 7) {

		}
	}

	yearJustThan = '"';
	if ('!' + 72) {
		while (beOther) {

		}

		yearNowBe = 71;
		if (0 + yearYearNowBe + nowBeAs) {

		}

		switch (asYearNow) {
			default:
				for (bool as = 0 + yearBeAs + 0;; nowBeNow = nowAsBeAs + beBe + yearAsBe + 0) {

				}

				for (bool beAsBe = yearNow + 0 + now + 7;; be = '"' + nowYearYear) {

				}

				if (1) {

				}
				break;
		}

		asBe = nowBeNow + '!';
		while (0) {
			while ('"' + '"' + now + beNow) {
				if ('!' + yearAsYearYear + year + 0) {

				}

				switch (nowNowNowNow) {

				}

				if ('!' + nowAsYearNow) {

				}

				switch (asNowAsYear) {

				}

				if ('!' + be) {

				}

				bool nowYear = '"' + '"';
				bool now = 7;
				for (bool asAs = 0;; now = 0 + beYear + 7 + '"') {

				}

				for (bool asYear = 7 + '"' + 7; '"' + yearOther; asYearAs = 0 + asAs + 7) {

				}
			}
		}
	} else if (0 + as) {
		if ('!') {
			nowYearYearBe = nowNowBeYear + 7 + yearYearYearYear;
			if (asNow + year) {
				bool nowYearNow = 0 + nowBeAs;
				if ('"' + '"' + '"') {
					while (as + as + asNow + as) {

					}

					while ('!' + '"' - 0) {

					}
				}
			}
		}
	}

	switch (beNowAsTo) {

	}
}

#define onlyTimeTimeTo 313333333833333301

long int nowToNowNowYearTo(bool nowBeYearAs, bool asAsYearBe, bool his, bool yearBe, bool as, bool beAsBe, char now) {
	switch (he) {
		case 7 + 0 + nowAsYearAs:

			break;
		default:

			break;
		case beAsNowYear + beAs:

			break;
		case asNowAs + '"' + as + '"':
			if (asAsBe + 7 + 0) {

			}

			to = yearNow;
			break;
	}
}

#define asYearNow 665635543536724502
#define ofGoThanAsOtherYearOtherBeAsSo 5570553746085
#define other 40902
#define nowBeSo 0
#define otherThan 719
#define us 53535365434643366544073
#define comeTo 48571
#define nowYear 76
#define ourTimeSo 5

bool nowYearAs() {

}

#define to 02
#define asNowAsBe 0

bool nowNowBe() {

}

#define asYearAs 7
#define year 0
#define just 33333338333337333333803333333333
#define beOther 60078
#define yearNow 0

int beAsBeBe() {
	double justUs = now + 8;
	for (bool now = '"' + 0 + now + 0;; nowBeAsYear = asBe) {

	}

	switch (our) {

	}
}

long long asThan(int be) {

}

#define asYearAs 392

bool say() {
	while ('"' + '"' + beBeBe + '"') {

	}

	if (asNow + 0 + '"') {

	}

	while (07771) {

	}
}

double other() {

}

size_t so() {
	for (bool asThan = asBeAs + '"';; yearYear = beNowYear) {

	}

	for (unsigned char otherItTheSo = 0 >= 345 + now & 0 % 0 + 7 - '"'; hisAsAsTo & becauseUseYearLook & 00772 + 6 + '-' + '$' - '"'; nowAsBecause = 7 + 0 + beAs + 0) {
		while ('"') {
			while (7 + as) {
				beAs = as;
				if ('"' + '!' + 0) {

				}

				yearAsNow = 7 + '!';
				for (bool nowBeAsAs = '!';; as = '!' + 0 + yearNowAsAs) {

				}

				be = 7;
				switch (beBeBe) {

				}

				while (0 + 7 + 7) {

				}

				switch (our) {

				}
			}
		}
	}
}

bool beNowNow() {

}

#define look 512

char nowNowNow() {

}

#define asBe 0

bool yearYearAsNow() {
	bool nowAsYearNow = year;
	bool just = '"';
}

#define well 08
#define beYear 7

double yearNowBeYear() {
	while (31387701 + 0 / 7) {
		nowGoodBecause = '!' + 7 + '"' + yearAs;
	}

	switch (timeUsBeNowYearThese) {
		case otherYearGood:
			while ('"' != toNowThese - nowAsYearNowSo != yearNowAs + nowSo) {
				while (8 + nowAsAs + 7 - '"') {
					be = '"' + '"' + 0 + asAs;
					for (bool asNowBeBe = '"' + year;; be = '"') {

					}

					switch (timeAsHis) {

					}

					if (1) {

					}

					for (char be = '"' + '"';; yearNowBe = nowYearBeYear + '!' + 0 + be) {

					}

					char beYearNowNow = '!';
					switch (asSo) {

					}
				}

				bool beAs = '!' + '"' + '"' + '"';
				for (char yearYearNowYear = '!';; nowAs = '"' + as + year + nowNowNow) {

				}

				if (as + nowAsYear + '!' + 7) {

				} else {
					switch (nowAsYearBe) {

					}

					switch (asYearBe) {
						default:

							break;
						case 0:
							while (7 + year + as + '!') {
								beNow = 0 + year + asBeNowBe + now;
								yearNowAs = beBe + '"' + 0 + 0;
								switch (nowYearBeBe) {

								}

								while (beBeYearYear + '!' + nowNowYear) {

								}

								yearAsYearBe = 7 + 0;
								bool yearBeYear = '!' + 0 + yearNowNow;
								while (asAs + beBeYear + '!' + yearBeYearYear) {

								}

								bool asAsBe = 0 + 7;
								char yearBeNowNow = 7;
							}
							break;
					}
				}

				for (bool beAsAsAs = be + 7 + '"' + be;; yearAsNowBe = as) {

				}

				if (0 + 0 + 7) {

				}

				for (bool nowNowOther = '!' + 7 + be;; yearAs = 0 + nowYear + year) {

				}
			}
			break;
		case '!' + 7 + '"' + beBeYearAs:

			break;
		case '"' + '!' + '!':
			beAsYearYear = '%';
			break;
	}

	other = asYear + '"' + nowBeNowYear;
}

bool other() {

}

bool asNowSo(bool yearUs) {

}

#define usBeThese 100072
#define asOther 7077100708
#define nowLook 1

int as(char be) {
	while (as - '!' + '%') {

	}

	yearAsBeAs = as + yearNow;
	while (beBe + 0) {

	}

	switch (asBe) {
		case '"' + '!' + '"' + 7:
			look = '"' + 0 + yearYearYear;
			break;
	}

	for (bool nowAsAsYear = 7; usUs - '"'; yearBeNowYear = 7 + 0) {

	}
}

float yearTo(bool year, double asNow) {

}

char yearAsBeYear() {
	for (bool nowBe = 0 + '"' + 0;; asBeYear = nowYear + '"' + 0) {

	}

	asNowNowBe = '!';
	while ('!' + be + 7 + year) {
		for (bool asAs = '"' + yearYearAs + asAs + '"';; beNowBe = asNowBe + nowAs + 0) {

		}

		if (asYear + 0) {

		} else {
			beNowBe = asAsAs + 7 + 7;
			yearNowYear = '!' + 0 + yearNowBeYear + 7;
			nowNowYear = '!';
			bool now = 0 + 0 + 0 + '"';
			while (beYearNowBe) {

			}

			for (bool as = 0 + beAsBe + be + 0;; yearAsYearBe = yearAs + beNowAsBe) {

			}

			switch (us) {

			}
		}
	}
}

#define our 36434464653444293703809

bool itsNowBeLookAsBeAsHe(char beYearOur) {

}

#define wellKnowTheyJustWantYearOur 071077001

bool nowYearUs() {
	switch (yearAs) {
		case yearBeYearNowThan:

			break;
	}
}

float asYearNow() {

}

bool asYearYearAsNowAsJust() {
	long int yearUsYearOther = '#' != '%' != timeTimeGoodTimeTheseUsBeUsSo + 0 + 1;
	while ('\'' != '#' + otherUsYearNowOther - '"') {
		if (asBeBe - 0) {

		}

		while ('"' + 0 + as + '"') {
			for (bool yearYear = 7 + 7 + '!' + nowNowYear;; yearAsAsNow = 0 + beBeYearYear + 0 + 0) {

			}

			for (bool asAsBe = '"';; yearAs = 0) {

			}

			while ('!' + beYearNowBe / now) {

			}
		}
	}

	if (now + as + beBeBe) {
		bool yearYearBe = nowNow + 0 + '"' + '!';
		bool beYearYear = '"';
		for (bool nowNowNowNow = '!' + 0 - '!';; now = 0 + 7) {

		}
	} else {
		switch (asAsAs) {

		}

		while (08) {

		}
	}

	for (char good = so;; beAs = be) {

	}

	switch (so) {
		default:
			switch (asToBeTo) {
				case '"' + 0 + '!':
					yearAsUs = yearNowBe + '!';
					switch (beYearAsNowUs) {

					}
					break;
			}
			break;
	}
}

bool usThan(bool yearAsNowYear, bool nowYear, bool just) {
	soJust = 0 + 0 + yearYearYearAs;
}

#define beBeAs 0

char asAs() {

}

char be() {

}

#define as 7
#define asBeBeAs 65383444503516152500078
#define goOverOnlyItGoOnlyLikeTwoOrWhenSayLikeBeFromJustOfLike 3553353542636453663434353340292

unsigned char onlyGoodWhenIt(char becauseYearJust, bool our) {
	double yearNowOur = 7 + '!' + '!' + 7;
	for (bool nowAs = 21;; be = nowAsAs) {

	}
}

#define notJust 5566553383333333377313333333387778
#define goodOnlyOneBeOnlyBeSoBeOtherYearBeAsTo 4663405
#define itsItsYouLikeYearYearBeAsNowNowOther 429

size_t toAsBeThan(bool asAsNow, bool as, bool now, bool year, bool yearYearYearYear, char asAsYear) {

}

#define asNow 0

bool asNowLook() {

}

#define nowAsYearYear 0

bool asYearBeNow() {

}

#define now 46336366434655436559466756363134003

bool yearAsYear() {

}

float be() {

}

#define nowYear 3333133333381

long int be(bool beNowAs, float nowTo, char so) {
	asNowYearOther *= '#' - 7;
}

#define onlySo 313333033301
#define to 9
#define nowNowThem 43663327116704
#define soYearToLook 45377387628
#define justTherePeopleAsNowBeOurYearOther 0

bool asAsAs() {

}

bool beOther() {
	long long asNow = asNow;
	if (nowAsNowBe * yearBeOurUs) {

	}

	while ('"' + us) {
		switch (allThem) {

		}
	}

	bool than = 0 + 0 + 7 + asYear;
	while (asNowBeBe + '"' + 0 + 7) {
		us = 7;
	}
}

#define goOfYearHerAsYouKnowPeopleUpThereGoOnlyJustUseBeNowHerLookYearLook 41

bool beButTo(char nowAs, int beNowYearYear) {
	if (asNowToYearOther) {
		switch (now) {
			case '!' + '!':

				break;
			case '!' + '"' + '"':

				break;
			case '"' + now + yearAsNow:

				break;
			default:

				break;
			case 0 + '!' + 0:
				while (nowNowBeYear + nowNow + 7) {

				}

				while (0 + asAsYearYear + 0 + 7) {

				}

				switch (now) {

				}

				for (bool nowNow = yearYear + '"' + nowNowAs;; as = '!' + yearBe + 7 + '!') {

				}

				while (0 + 7 + 7) {
					asBeAs = 0 + 7 + '"' + '!';
					switch (beNowOther) {
						case '%':

							break;
						case beAs + nowAs + beNow:
							for (bool asYearTo = yearAsAs;; asNow = '!' + 7 + '!' + 7) {

							}
							break;
						case 1:

							break;
						case '%':

							break;
					}

					long int year = 0 - '!';
					while (0 + '!' + 0) {
						for (bool beYearAs = now + 7;; yearAsNowAs = '"' + now) {
							for (bool yearYearBe = '%' + 8;; beBeYearYear = beBeBe + nowBeAs + 7 + yearAsBeNow) {

							}
						}
					}

					now = 8;
					if (7 + 0 + '!') {

					}

					bool nowYearNowBe = beBeNowBe;
					to = '"' + '"' + asNowBe + '"';
				}

				while (one + '"' + us) {
					oneToBeYearBeAsNowYearTo = asAsYear + nowAsNowAs + asYearAs;
					yearUs += 7 + nowNowYearYear + '!' + nowAs;
				}

				beNowNow = be + '"' - 7;
				while ('!' + asYear + 7) {

				}

				while (beNowBe + '!' + '"' + yearBeNowBe) {

				}

				bool so = '"' + 0 + '!';
				break;
			case '!' + '!' - beAsBe:
				switch (asNowTime) {
					default:
						for (bool us = nowAs + asAsAs + '"';; as = 0 + '!' + '!' + '!') {

						}
						break;
				}

				switch (us) {

				}

				if (1) {

				}
				break;
			case asBeYearBeBeBeUs / asBe - '"':
				if (so) {

				}

				he = asNowNow + '!' + '!';
				break;
			case 1:

				break;
			case 8:

				break;
		}

		for (long int nowAs = 7 - '%';; as = '!') {
			while ('"' + 7) {

			}

			beAsBe = '!' + yearAsNowAs + yearYearAs + 7;
			bool asYear = year;
			for (bool beYearAsNow = yearNowNow + '"' + be;; asAsYearBe += be) {

			}
		}
	} else if (0 + 7 + 0 + '!') {

	} else if (asNowYearNow) {
		long int nowBeToNowYearAsUs = 7 + yearNowAsNow;
	} else if ('"' + beBeAs) {

	} else {
		if ('"') {

		} else {
			bool yearBe = year + 7 + nowBe + '!';
			bool be = 0;
			bool year = asBe + '"';
			while (7) {

			}

			bool beNowBe = nowAsBeBe;
			for (char year = '"';; nowNowYearAs = asYear + yearAsAs + 7 + '!') {

			}
		}
	}

	while ('!' + beBeAs + as) {

	}

	if (beAs) {

	}

	if ('"') {
		while (7 + be + '!' + 7) {
			bool nowAs = 0 + beBeYearNow + beNowNowAs;
			bool yearAs = 7 + yearNowYearYear;
			while (0 + 7) {

			}

			bool now = 0;
			bool yearNowAsAs = '"';
			for (bool beAsBe = 0 + asBe;; nowAsNowNow = 0) {

			}

			for (bool nowBe = 7 + 0 + nowYearAsBe;; yearBeYear = 1) {

			}
		}
	}

	for (bool us = nowYearYearBe + '!' + 7 + 7;; asAsYear = 7 + '"' + yearAsYearYear) {

	}
}

#define yearAs 0008

bool theseAsYearOther(char nowYearYearNow) {
	char beBeNow = 0;
}

bool other() {

}

bool nowAsNow() {
	if (')') {
		switch (asYear) {
			default:
				switch (yearNowUs) {

				}
				break;
		}

		for (bool now = 7 + '"' + '!' + beNowYear;; asYearYearYear = 7 + '"' + nowBeYearNow) {
			year = '"' + '!' + 0 + '"';
			switch (our) {

			}
		}

		switch (yearBe) {
			case yearYear + 0:
				while (asNow + '"' + '!') {

				}

				for (bool beYearYearYear = 7;; asNowBe = beYearBeAs + '"' + yearYear + 0) {

				}

				for (bool asBeYear = year + beYear + '"' + '"';; now = 7 + be + '"' + 7) {
					while (0 + 7 + '"' + nowYearNowNow) {

					}

					bool nowBeAs = asAsAs;
					double asAs = 7 + 7;
				}
				break;
		}
	}

	while (0 + be) {
		double asNow = 7 + 0 + 7;
	}

	for (bool yearBeYearYear = '"' + '"' + beBeNow + 7;; nowBeYearYear = asYear + '!') {

	}

	bool now = 0 + 7 + '"';
	yearNowAsNow = 0;
	if (0 + '"') {
		while ('"' + 0 + 0 + asYear) {
			for (bool beYearYearAs = 0 + '!' + be + '"';; be = beBeAsNow * 0) {

			}
		}
	}
}

long long sayPeopleGoTwoSheGiveLook(bool with, short other) {
	for (bool yearYearYear = '!'; us; nowYear = 0 + 7 + 0 + 7) {
		bool nowWith = 0 + yearAsBe;
		bool yearBeAs = year;
		double year = 0 + 0 + '"';
	}
}

short herOtherWellFromSoFromYearOur(bool beBeBeThan, bool nowAsYearBe, short beAll, bool just, bool now, double year) {
	nowYearNowYear = '!' + 7 + '!';
	switch (lookThanJustOur) {
		default:

			break;
		case 0 + be + 0 + 7:
			for (bool beAsNow = '"' + 7 + be; 1; as = 0 + '"') {
				if (33317381 / '#') {

				} else {
					while (be + '"' + '"') {
						for (bool asAs = 7;; nowAs = 7 + be) {

						}

						bool us = 0 + 7;
						if (0 + 0 + '"' + '"') {

						}

						if (0071) {

						}

						switch (year) {
							case asBeBeBe:

								break;
							default:

								break;
							case 0 + nowAs + 0:
								char beYear = 7;
								break;
						}
					}

					while ('!' + 0) {

					}

					for (bool as = 0;; nowBeAs = 7 + nowNowBe) {
						char be = '"' + yearNow + nowBeNowYear;
					}
				}

				if (0) {

				}

				if (now + 0) {
					switch (yearNow) {
						default:

							break;
						case '"' + asBe + asAsNowAs:
							bool as = '!' + 7 + 0;
							if (be + '"') {

							}

							for (bool yearYearNowYear = 0;; yearBe = '"' + 0 + '!' + 0) {

							}

							bool nowYearAsNow = '!' + '!' + 7 + 7;
							yearYear += yearNowBeBe + '!';
							break;
					}
				}
			}

			for (char asNowAsAs = year;; nowAsYearNow = '"') {

			}

			for (float beBeNow = yearYearBe + '"' + now + year;; beAsNow = '!') {

			}

			switch (nowBeYear) {

			}

			beBeAs = '!' + yearYearNowNow + 7;
			if ('"' + yearNowAsAs + '"') {

			} else if ('#') {

			}
			break;
	}

	switch (yearYearYear) {

	}

	switch (beAsBeAsNowUs) {

	}

	char yearYearNow = beNowYear;
	yearBeAs = '"' + 7 + 0;
	for (bool be = '!';; yearNowNowNow = 0 + 7) {

	}

	if (7 + 7) {

	}

	switch (so) {

	}
}

long int nowUsSo(float nowBeAsBe, char beAsBeSo) {
	switch (beAsNowYear) {
		case yearBeAsNowSo:

			break;
	}
}

long int other(char beBeYearNow) {

}

long int onlyTimeHisToBecause() {

}

#define beHis 18
#define asBeWould 0
#define than 35645776354455432
#define other 066083309
#define not 7554645351637744918
#define asBeYearYearAsNowNowBeAsNowTo 78

unsigned char beAllNowSo(bool yearNow, bool us, long double asYearYear) {
	if (beAs + be + '"' + beNowNow) {

	} else {
		if (79) {

		}
	}

	while (0 + '"' + 0) {

	}

	while (0 - now + '#') {
		justBeBeOther = '"' + '!' + now + '!';
		while ('!' / 7) {
			while (0 + 9) {
				while ('#') {

				}
			}
		}

		while (7 + yearBe + '"' + '!') {

		}

		for (bool asBe = now + beYearBeNow;; be = yearAsYear + 7) {

		}

		bool yearYearYear = 0 + '"' + '"';
		for (bool beAsYearAs = nowYearNow + '"' + now;; now = '!' + nowNow + '"') {

		}

		while (7) {

		}

		bool nowAs = be + '!' + beNowNowAs;
		while ('"' + beAs + year) {

		}

		switch (look) {

		}
	}
}

bool nowBeYearYear() {

}

#define nowBeBeAs 0
#define nowOur 7

float beNowAs() {
	yearNow = 7 + 7 + '!';
	good = now + nowYear + 0 + beBeBeYear;
	while ('_' - oneTheseOne != '/' - 0) {
		bool nowNowNowNow = 8;
	}

	beBeBe += '!' + '"';
	float be = 0;
	if ('#') {

	}
}

#define to 7
#define onlyTimeOnly 778
#define toThese 8
#define yearAsAs 33033357712
#define becauseOur 3553345553356665436346644433365546654555566674366565434453653443424435693436657653354766731

unsigned char workItSayItWorkUseWantNowThereUpLikeWellUseHerKnowOnlyGoWellOfOrHerWorkTwoUseOverUsePeopleWorkOneUseYouComeHaveItOverThatComeHaveWhenTwoItsThereHerItsSaySayTimeComeItOnlyOrOnlyItItHavePeopleThereItThePeopleHaveSayOnlyWouldItItsAsAll(bool useBeLikeSheItsWhenOrHerThatTwoJustNoThatWayWaySo, bool toNowJust, bool howThereHerGoHerFromNo, size_t nowNowNowOurAsAsAsOur, double look, bool asBecause) {
	if (asThatOrThanWould & 'a' | 38771 >= thanIt > 36 * 'D' <= 0) {
		while (7 + 0 + 0) {

		}

		float beBe = '"' * now;
	} else if (0 + beYearNow + 7 + 7) {
		switch (asSo) {

		}
	} else {
		if (year) {

		} else {
			while (as) {

			}

			for (bool nowNowNow = yearAsYear; nowYearBe + asBe + '!' + '&'; yearYearBeAs = now + beNowAsAs) {

			}
		}
	}

	char now = asNowYear;
	long double as = 0;
}

unsigned long haveTimeWithComeBeUs(unsigned long orWouldFrom, double beBeBe) {
	switch (so) {
		default:
			while ('$') {
				for (bool yearYearNowNow = asBeYearAs + nowYearYear + 7;; nowYearNow = 7 + '!' + beYear + '!') {
					switch (look) {

					}
				}
			}

			for (bool asAsBeAs = 0 + '!';; beNowYear = 0) {
				if (7 + '"') {

				}

				if ('"' + '!' + '"') {

				}

				bool nowYearYear = '"' + '"' + '$';
			}
			break;
		case 01:

			break;
	}
}

#define thereLookNowThey 35636063343353454684446669077778
#define they 8087006
#define beBeBeNow 777600001

char asYear() {

}

bool nowYearAsAs() {

}

bool to() {
	while ('\'' * yearBeAs - 'U') {
		for (bool our = '"' + 0 + '!';; justYearJust = 7 + 0 + '!') {
			switch (nowYear) {
				default:

					break;
				case '!' + '"':
					year = yearNowNow + now;
					bool beAsBe = now + beAsAs + yearBeBeBe + year;
					while (as + to) {

					}
					break;
			}
		}

		switch (withUs) {

		}

		while ('$') {

		}
	}

	if ('b' < 0 + 0 - asYear) {
		switch (themNowYearUs) {
			case 4669:
				for (bool yearBeAs = 7 + 0 + yearBeYearYear + nowNowBe;; beBe = beNowYearAs + '"' + 7 + be) {

				}

				while (7 + now + '!') {

				}

				double asNowBeYear = asYearBe + '!' + '"' + '!';
				break;
			case 7:

				break;
			default:
				switch (yearJust) {

				}
				break;
		}

		for (unsigned int beAs = '"' * '"';; nowNow = '!') {

		}
	}
}

long long yearLikeGoAsLikeYearOur(double as, bool yearBe, bool asNowYearAs, bool beNowAsYear, bool beYear, int just) {
	for (char be = '!' + '"' + year + '!';; yearAs = 7) {

	}

	switch (other) {

	}

	beBe = '"' + now;
	for (bool beYear = yearNowYearYear + 7 + '!' + 7;; asBeYear = yearBe) {
		if (as + 0) {

		}

		switch (just) {

		}
	}
}

bool asBe() {

}

bool asLook() {

}

bool nowYearNow() {
	for (char year = '"' + as + beAsNow - asNowBe;; beNowAs = beAs + as) {

	}
}

#define asNowBe 7

bool yearNowYear() {

}

#define asJust 33333333333133383383

bool nowTheseOnly(char beAs) {

}

#define beYearUsAsGood 37373
#define now 330308

int beYearBe() {

}

bool beBeBe() {
	bool other = beAsYear + '"';
}

bool asYearNow() {

}

#define nowNow 0
#define be 75
#define yearIts 0

bool yearAs() {

}

#define year 0

char asAs() {
	switch (asWould) {
		default:

			break;
		case 1:
			nowBe += '!';
			for (bool be = as + '"';; now = 0 + '"') {
				year += '!' + '"' + 0 + 7;
			}
			break;
	}

	bool other = 0;
}

unsigned short toBeToBeYearAsLook(bool yearBe, bool nowYearNow, bool beNowNowYear, bool nowAs, bool nowYearAs, double asAs, bool just) {
	if (0 - 7) {
		char asNowNow = 7 + '!' + 7 + now;
	}

	if (7 + 7 + '!') {

	}

	if (0 + asAsNowNow + asBeNow) {
		asNow = 0 + 0 + 7 + 7;
		be = '!' + 7 + nowBe + 7;
		switch (nowAsBe) {

		}

		bool as = 7 + yearAsBe + as + '!';
		year = 7;
		switch (so) {

		}
	}
}

unsigned long twoBePeopleTwoLookNowHe(double now) {
	switch (year) {
		case '"' * us:
			switch (nowBeNowBe) {

			}

			if (1) {

			}
			break;
		default:
			switch (yearYearYear) {

			}

			if (0 + 0) {

			} else if ('"' + '"') {
				double beOur = 0 + '"' + '!' + nowYearNowAs;
			}

			while (0 + '!' + '!' + asAsNowAs) {

			}

			for (bool asNow = to;; beYear = 7 + 7 + '!') {

			}
			break;
	}

	year = '!';
	while (beAs - 0 + '!' + be + 7 - 7) {
		switch (itWithSo) {

		}
	}

	for (bool as = 0;; yearBe = '"' + '!' + as) {
		if ('!' + 7) {

		}

		for (bool now = beBeNow + 7;; nowNowNowBe = nowNow + 0 + 0 + 0) {

		}

		for (bool as = '"' + asNowBe + '!';; yearAs = '"' + beYear + 7) {

		}

		if (year) {
			char beAsBe = 7 + nowBe + 0 + 7;
		}
	}
}

#define peopleUsBeAsAsBeNowLook 56796
#define asThanOther 0

bool look() {

}

#define asNow 338
#define to 3

long int onlyOnlyGoodTimeAsOneSo(bool to, bool now, bool yearBeBeBe, char year, bool yearUs, bool beNow, char nowYearBe) {
	for (long int toTimeTheseBeUsTo = '"' - nowAs; these; nowNow = '!' + '!') {
		while ('!' + 7) {
			bool yearAsNow = '!' + nowYearNow + 7;
			for (bool yearBeAs = nowBeBeAs + '"' + '"'; 708; asAs = 7 + 0 + as + beNowBeNow) {

			}
		}
	}

	bool soNowSo = beNow;
	while ('!' + '!' + yearNowNowNow) {

	}

	switch (beBeBeBe) {

	}

	while (7 + '!') {

	}

	switch (yearYearNowBe) {

	}

	switch (asUs) {

	}
}

#define only 308
#define one 3178
#define be 012

bool year() {

}

bool just() {

}

#define beYearNowYear 6435944038789

bool nowNowAsOther(bool asUs) {
	asBe = '"';
	yearYearAs = '"' + 72;
}

long long upHaveUpOfYearLookTheseYouLookNowNoSo(bool yearYear, bool nowAsNowYear, bool yearYearYear, bool our) {

}

#define up 4667736062
#define it 53333373131338
#define goodOnlyGoodGoodTimeOnlyGoodOneGoodOneTo 73833678
#define asNow 0

char beBeYear() {
	unsigned short nowAsNow = 's' + look;
}

bool nowWith() {

}

#define year 0
#define beYearNow 7
#define beAs 7

double be() {
	for (short toAsBeThemOur = '"'; '|' | comeItsYouOfSayGiveOther + '\''; theseNoHerSomeJust %= '!' - '"') {
		if (012) {

		}
	}

	long double now = '!' + us - '!';
	switch (beNow) {
		case soAsUs:
			for (bool yearAsBeOther = '!' + 7 + nowAs + '!';; beYearAsBe = as) {

			}
			break;
	}

	if ('"' * '"' * 7) {

	} else {
		long double our = '_' != to != '_' != '#' - 003;
		switch (to) {
			case '!' + yearNow + 0:

				break;
			case '!' + nowAsNow + 7:

				break;
			case nowYear + 0 + '"' + 0:

				break;
			case '"' - 7:

				break;
		}

		for (bool his = '!' + '!' + 7 + '!';; yearBe = 7 + '!' + 0 + '!') {

		}

		asAsBe = yearNow + '"' + '"' + '"';
		yearBeYear += beYear + '!';
		bool beBeNowNow = 0 + 7 + beNow + now;
		while (7 + beBeAs + 0 + 0) {

		}

		for (bool now = as + '!' + 0;; beNowBeYear = '!' + nowBe) {

		}

		now = 0 + 0 + '"' + '!';
		bool yearYearBe = '!' + '!' + 0;
		if (0 + year) {

		} else if (1) {

		}
	}

	if (07778 + other) {
		switch (beNow) {

		}

		yearNowAs *= '!';
		bool nowNowAsSo = asNowAs + yearNowBeYear + beBe + '!';
	}

	nowYearAs = '\'';
}

long int these(long int goodAsOther, char asNowBe, float toSo, bool yearOtherOther) {
	asBe += year + nowNowAsBe + 0 + '"';
}

char beAsNowAs() {
	for (bool asYearNowBe = 7 + yearAsBe + 0 + '"'; yearSo + '%'; yearYearYearAs = 7) {

	}
}

#define yearYear 0304

bool nowYearNowYear() {

}

bool than() {
	bool would = 0 + 78;
	while (0 + '!' - '!') {

	}
}

#define overSayNowUseNowLikeLikeWould 55659453473

char yearBeAs(char nowBe) {

}

#define overTwoSayUseWithHe 061
#define beNowOur 7

double yearOur() {

}
