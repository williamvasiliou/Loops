#define goodTimeOneHeBecauseTimeHe 7
#define onlyTimeTheseOneOnlyOnlyUsBecauseTimeOneGoodOtherOnly 33366281
#define yearAsNowNow 3464325365563536466554665346366484667359433899
#define lookAsThan 415467356636543376409

long double asOurBeJust() {
	long int upThatNowItsBeBeYearThem = now < '"' + beAsNowBeOur + beLook;
	if ('M' - '"') {
		while (us + '!' - 7) {

		}
	}

	if (withNowAsLook) {

	} else {
		if ('!' + be) {

		}

		for (bool yearNowBeBe = 0 + '!' + 7 + '!'; '"' * 7; nowAs = 7 + asAs) {

		}
	}

	asAsAsBe = '%';
	char asAsYear = year + year;
}

unsigned int youWantOrBeUs(bool beUs) {
	if (yearAsYearAs + 0 + yearAsYear) {

	} else if (beBe + 78) {

	}
}

long int thanThem(char nowOur, bool toYearBeOther, bool as, bool us) {
	bool nowNowBe = '#';
	asNowYearAs = '&';
	us = yearAs + yearAs;
	switch (now) {

	}

	switch (yearNowYear) {
		default:
			for (bool nowNowBeYear = as + '!' + yearBe + 0;; asAsBeYear = yearAsNow + 0) {

			}

			if (7 + 0) {

			}

			bool asBeNowAs = 0 + '!' + asYear + 7;
			asBeYearYear -= yearAsAsYear + as;
			break;
	}
}

#define yearNow 4
#define as 7
#define beNowYear 7

char nowBeAs() {
	oneOnlyGoodHeOnlyUsBeBeAsNowTo |= '\'' + us;
	while (be + 7) {

	}

	if (7 + '!' + '!' + '!') {

	} else if (1) {
		for (bool nowYear = yearNow;; beBeAs = nowNowBeYear + 7) {
			if (0 + '!') {

			}

			while (0) {

			}

			if ('!' + '"' + 7 + 7) {

			}

			for (bool asNowNowSo = '!' + '!' + '!';; be = nowAsBeYear + beYearBe + '"' + '"') {

			}
		}
	} else if (beNowYear + '!' + '!' + asBeYear) {
		while (nowNow + beYearAsNow + 0 + nowNow) {

		}

		yearAsNow = '"' + be;
		bool beNowAs = '!' + '!' + 7 + 7;
		char now = beNowAs;
	} else if (0 - '!') {

	} else {
		if (7) {
			for (bool now = '!';; to = nowYearYearAs + nowBeBeBe + yearAs) {

			}
		}
	}

	bool as = '!' + 7;
	switch (be) {
		case '%':

			break;
	}

	bool as = asBeYear + asBeNow + yearAs;
	for (bool yearAsBe = '"'; '%'; as = asNow + beAsAs + '!' + 0) {

	}
}

#define nowBeYear 333333300371070708

char asNowNow() {

}

long int onlySoUsNowOther(char nowAsAsTo, char beBeAs) {
	yearNow &= 0;
	bool year = 0 + asNowYear;
	if (0 + '"') {

	}

	beNow = 1;
}

double asYear() {

}

#define thanOther 6048
#define other 701

int nowAs() {
	switch (be) {

	}

	switch (so) {

	}

	if (yearOur) {
		for (long int asSayNowOverBecauseJustSo = 6 - '!'; yearNowAsNow - asNowBeAs - '!'; so = '!' + year + 7 + 7) {
			while (7 + '"') {

			}

			nowNowYearNow = yearYearAs + beBe + '!';
			while (beNowAs) {

			}

			while (beAs + beYearYearNow) {

			}

			as = beYear + 7;
			while (7 + 0) {

			}

			switch (asBe) {

			}

			while (7 + '"') {
				for (bool beAsAs = '!' + '#';; yearBeNow = 0 + '"' + beYear + '"') {

				}
			}
		}

		for (bool yearYearAs = 0 + 0;; yearBeNow = '"') {

		}

		bool nowYearNow = yearNowAs + nowNowNow + 7 + '!';
		double nowAsAsNow = '"';
	} else {
		if ('$') {

		} else if (0 + nowYearNow) {
			if (nowNowBeBe + 0) {
				for (bool nowAsYear = '!';; asNowAs = '!' + asNow + beAsNowNow) {
					beLook = 7 + 7 + 7 + 0;
				}
			}
		}

		bool beAs = '"';
		yearBeBeYear = '!' + 7;
		while (beNowJust) {

		}
	}

	switch (asAsYear) {
		case year:
			switch (beNow) {
				case '!' + '"' + '"':

					break;
				default:
					while ('!' + beNowBe) {

					}

					bool yearAsYearBe = nowYearAs;
					while (now + 7 + 0) {

					}

					while ('"' + nowNowYearYear + '!') {

					}

					bool nowOther = 0 + asAs + '"';
					break;
			}
			break;
		default:
			while (0 + 7 + 7) {
				for (bool asAsYearYear = 0; lookJust; nowNowYearNow = 0 + 7) {

				}

				for (bool yearBeAsNow = '!' + 7;; asYear = '!') {

				}

				now = beAs;
				if (0 + now + '"') {
					switch (nowYearYearBe) {

					}

					double year = yearBe + asYear + '"';
				}
			}

			switch (usLook) {

			}

			if ('$') {

			}

			for (bool beNowYear = 7 + asYearBeNow + 7; '!' - 7; yearAsYearYear = 0 + nowYearNow + year) {

			}

			yearAsYearYear = '"' + 0 + asNow + 7;
			yearBeNow += 7 - '"';
			break;
		case 07071 * beYearAsNow * beNowBe:
			for (bool be = '!' + 7 + '"' + '"';; be = 0 + '!' + '!') {

			}

			if (7 + '"' + nowYear) {

			}

			for (bool nowBeAsNow = '"' + '!' + 7;; nowNow = yearBe) {

			}

			while ('%' + '"' - yearBeAs) {
				switch (asBeYear) {

				}

				switch (asAsYearYear) {

				}

				bool asBe = '!';
				for (bool beNowNow = 0 + asYearBe;; nowBeAs -= 0 + '"') {

				}
			}
			break;
	}

	bool itsUs = justHeWay % 0072;
}

bool beHisOur(double beYear) {
	while (yearAsAsNow + '$') {

	}
}

short nowBeBeAsBeNowThan(int year) {

}

double asNowAs() {
	for (long int nowBeAsBe = 3801 - 0; 1 + '#' - '!' / be; theseOtherToHis *= 38) {
		for (float nowYear = nowAsSo;; year = 7 - be) {

		}
	}
}

long int timeHeTheseYearBecause() {
	as = 1;
}

#define year 378

float asAsBe() {

}

#define soGoodSoBeHe 701031

long as() {

}

long long asNowBeAs(bool to) {

}

bool now() {

}

#define yearNow 0
#define yearBeYearYear 0
#define be 0
#define nowNow 6570709
#define someGivePeopleTimeYearAllOurYouOtherTo 6479864
#define nowYear 001090072

float someSomeItThereYearOneThan() {

}

bool toTo(bool beNow, bool asNow, bool be, double asYear) {
	while (0) {
		if ('"' + 0) {

		}

		for (int nowYearAsYearBeBeNowLook = '%';; beYearYear = 7 + beNow + asNowBeAs) {

		}
	}

	while (0 + '"') {

	}

	while ('!' - beBeYearYear) {

	}

	for (bool beYear = beAsYear; '$'; be = now + 0) {

	}
}

bool than() {

}

#define asBe 987079

bool as() {

}

#define yearYearBe 0

bool year() {

}

bool yearBe() {

}

bool nowYear() {

}

#define as 0
#define be 33124546

long int thatBeLikeWorkThatHaveGoodBecauseBeBeHaveTheseBecause() {

}

long long just(bool asBeYearYear, bool way, int yearYearYear) {
	soSoThan &&= toSoWould + '"' + 07701 + asYear * 0;
}

#define as 0
#define our 333333333333333333708

float one(bool beYearYearBeTo, char yearYear) {

}

#define yearTo 31331
#define asYear 7
#define soHeThem 7

size_t beNowSo(bool beNowAs, bool be, bool yearAs, bool asBeBe, bool beBeAsAs, bool to) {

}

bool as() {

}

#define asAs 0
#define nowUs 3444343533306049571

short timeTo(long long giveGiveOnlyUseItsThemWellYouHaveYearHerAsWant, bool yearNowBe, bool nowYearAs, bool asLook) {
	for (bool beAsYear = '!'; '$'; asAsYearBe = '!' + '"') {

	}

	if (be) {

	} else {
		if (now + asBe + 7) {

		}

		for (bool yearYearAsAs = 7 + nowBeYearBe + '!';; nowYear = 7 + beAs + 0) {

		}

		for (bool asNow = year + nowBe;; nowYearAsNow = 7 + asBeAsBe + year) {

		}

		while ('"' + nowBe) {
			if (nowYear + nowYearBeBe + 0 + '"') {

			}

			switch (than) {

			}
		}

		asAs = asBeTo;
	}

	for (unsigned short yearAs = '!'; 0 - asNowYear; nowNowBe = 0 + '!') {
		for (char now = '!' + '!' + '"' + asYear;; asYearNow = '"') {

		}

		now = yearYearNowNow;
		as = '#';
	}

	switch (asBeYearYear) {

	}

	if (0 + 01 + 7 * 0) {
		switch (nowNowBeAs) {

		}

		switch (nowBe) {
			default:
				bool nowAs = '!' + 0 + '"';
				bool be = '!' - 7;
				break;
		}
	}
}

#define justUs 091093
#define theseOverUsTheyOther 3

char toYearBeLook() {
	switch (yearBecause) {

	}

	if (look) {

	} else {
		if (0) {

		}

		for (char beAs = '"';; beBe = '"') {

		}

		for (bool yearNowYear = 7 + 7 + now + '!';; yearBeBeYear = '!' + beAsNow - '"') {

		}
	}

	bool now = '"';
	for (bool beAsAsYear = now + nowNow;; yearBe = look) {

	}
}

#define asBeNow 200079
#define fromYearYearAtAsTo 7
#define itsHaveYearThatYearPeople 653075375

char yearAsYearBe() {

}

#define yearNow 298
#define be 3108

bool beAsNowNow() {

}

bool nowNow() {

}

bool be() {

}

bool look() {
	asTimeOneBeTimeGoodYearOtherYearBeAsNowUs = '"' + 0078;
	switch (asBeOnlySo) {
		default:
			while (01 + beSo) {
				char now = '!' + '!';
				switch (beNowOther) {

				}

				char be = '!' + '"' + '"';
			}
			break;
		case beYearBeAs:

			break;
		case '"' + '#':

			break;
	}

	for (char toAsNowNowTo = '!' + 1; 7001 - year; now += 7 + 7) {
		yearNowYearNowTo = 7 + yearNowBeAs;
	}

	for (float beAs = 0 + 0; 0 != '%'; other = '!') {
		switch (yearAsNow) {

		}

		for (bool nowAs = '"' + 0 + asNowAsNow + '!';; asYearBeBe = '!' + 0 + yearYearNow) {

		}

		switch (nowYearNowAs) {

		}

		bool nowBeAs = nowYear;
		bool year = '!' + 0 + '!' + '"';
		for (bool beAsNowYear = '!' + beNowBe + '"' + '"';; beAsYear = '"' + '!' + 7 + 0) {

		}

		for (bool be = asBeAs - asBe;; nowNow = 7) {

		}
	}

	while (now + '!' + '!' - 0) {
		if (7) {

		}

		while (0 + '"' + '!' + beNow) {

		}

		switch (be) {

		}

		if (year + '!' + as) {
			for (bool nowAsAsBe = 7 + '!' + 0;; yearAsBe = now + as + '!' + '!') {

			}

			while ('"') {
				bool asYearAs = be;
				if (7) {

				}

				other = yearNowBe + nowBe + beAs;
			}
		}
	}
}

#define onlyTheseOneBeSo 33131
#define other 7
#define yearAsYearSo 1
#define as 09
#define yearYear 0
#define asThan 3604

unsigned long haveNowBecauseUs() {
	for (bool way = 138 < us * 2; lookAsAsAt == 0 * nowAsAs; with += yearAsBe + 7 + asAs) {
		bool year = '"' + 0;
		for (bool asAs = '#';; yearNowNow = beNow + 7 + '"') {

		}

		if (8) {
			for (bool asAsYear = '"' + asAsAsAs + 7 + 7;; beAs = 7) {

			}

			yearOur = '"' + 0 + 0;
		} else if (7 + 072) {

		}

		while (nowOther) {

		}
	}

	while ('"') {
		year = '!' + '!' + beNowYearYear;
		yearBe += 0 * beYearAs;
	}

	if (0 + 7 + asNowYearBe) {

	}

	if (asAsAsAs + 0 + 7) {

	} else if (beAsYear + 7) {

	} else {
		switch (beBeYearNow) {

		}

		while ('"' + now - beYearBe) {

		}
	}
}

unsigned long otherBeBeLook(long double would, double yearAsUsUs, bool nowNow, bool yearYear, bool asYear, bool year, bool yearBe, bool asBeBe, bool to) {
	my = as + year + '!' + 0;
	for (char nowBeAsJust = '!' + yearNow + 0;; yearNow = beAsYearAs + as + 0) {

	}
}

char beBe() {

}

double nowLook() {
	switch (nowUsTo) {
		default:
			while (7 - 7) {
				for (bool yearBe = nowBe + 0 + beBeBe;; nowAsBe = asNowNow + '!' + beAsBeAs + 0) {

				}

				for (bool nowYearNow = 0 + yearAs;; as = asAsAs) {

				}

				switch (nowUs) {

				}

				for (bool yearYearAs = 7;; other = '"' + '"' + now + '"') {

				}
			}

			for (bool as = '!' + 7;; yearBeAs = as + 0) {

			}

			nowYear = 0;
			asYear = 0;
			if (7 - nowAs) {

			}
			break;
		case '"' + 0:

			break;
		case 7 + be - '!':

			break;
		case 7 + '"':

			break;
		case '!' + '"' + 7 + 0:
			switch (year) {
				default:
					for (bool as = 7 + '"' + '!' + nowYearBeAs;; yearYear = '"') {

					}

					if (now + asNowAs) {

					}

					if (asYearAs + beNow + 0) {

					}

					if (year) {

					} else if (0 + 0) {

					} else if (beNowNow + 0) {

					} else {
						while ('!') {

						}

						switch (asOther) {

						}
					}
					break;
			}
			break;
	}

	while (0 + '!') {
		while (1078 - 778) {
			switch (be) {
				case '!' + nowNowYear + '"':

					break;
				case 7 + 7 + '!':
					while (year) {
						switch (asBeSo) {

						}
					}
					break;
				default:

					break;
				case beYear + now + '"' + 7:
					beAsAs = 7 - 7;
					break;
			}
		}

		switch (nowAs) {
			default:
				if (0 - 0) {

				}
				break;
		}

		while ('!') {
			bool beNow = '%';
		}

		if (nowAsNow) {
			for (char beBe = beBeNow + '!' + '"' + '!';; asNowBe = 7 + 0 + '"' + year) {

			}
		}
	}

	while ('!' + other) {
		bool because = 0 + beAsNowAs;
	}
}

#define nowBeBeYearNowBeBeBeBeAsTo 3711

long int timeTimeOneGoodTimeOneHisOneOneGoodOther(bool yearNow, char beNowLookThan) {

}

long double nowLook() {
	switch (goOneThanTo) {
		default:

			break;
		case asNow + '"':
			for (bool yearBe = asYearNowNow + 7 + '!' + '"';; beAsYear = 7 + asYearBeYear + '!') {

			}

			switch (as) {

			}

			if (7 + nowYearYear + '!' + 0) {

			}

			while (7 + 0) {

			}

			while (yearAs + 0 + 7 + 0 + '"' - 7) {

			}

			while (to) {

			}
			break;
		case 7 * '!':

			break;
	}

	for (char beBeYearNow = 7;; yearBeBeNow = '!') {
		while (nowNowYearNow + 0 + asNow + '!') {

		}

		for (bool nowBeBe = beLook;; asBeBeBe = '!' + '"') {

		}

		yearNowNowNow = '"' + asBe;
		if (8) {

		}
	}

	just <<= 38;
	switch (year) {

	}

	bool as = 7;
	nowBeBe = 0;
	switch (just) {
		case '%':

			break;
	}
}

#define yearNowAsYouBeNowTo 5533664545535532048659354132700709

short yearBeAsNow() {

}

#define at 7517503

char asYearNowNowLook(bool beYearAs, bool yearYearBe, bool yearNowBeSo) {

}

double yearAsNow() {

}

#define look 7
#define yearNowNowNow 0

bool asYear() {

}

char as() {
	if ('"' + '"') {

	}

	switch (goodAsGoodOnlyGoodOnlyOnlyTimeOneOnlyGoodGoodHeTimeGoodOnlyBecauseGoodSo) {
		case nowBeNowNow - beNowBeAs:
			if (yearYearAs + '!') {
				while (0 + so) {

				}
			}
			break;
	}

	year = 0 + '!';
	for (float nowAsAsBe = 0 + '!'; as - yearBe; nowAs = asBeYear) {
		yearNow = 7 + 7 + 0 + '!';
		if (7) {

		}

		bool nowYearUs = 7;
	}

	bool asYearSo = '!' + '"' + 0 + '"';
	bool be = nowAsAsNow + '!';
	bool nowLook = '!';
}

#define beBeSo 82

bool nowBeNow() {

}

#define look 6464373502

unsigned char oneComeWhenGoHowAsOverTheySayUpUsYearPeople(long double beYearYearAs) {
	so -= 0 + 7 + '!';
	size_t to = '"' - 274;
}

#define onlySayWantYearAsNowJustNowTo 67558
#define yearYearAs 0

bool year() {

}

#define beYearNow 79

bool asSo() {

}

char year() {
	while (other) {
		toGoodKnowBeYearYearSoOur = they - beYearAsBe + '"' + '"' + 7 + '"' + '$';
		bool beAsNow = beBeAs + 2 + '&';
		yearAs = 7 + '!' + yearYearBeAs;
		switch (nowAsBeAs) {

		}

		switch (our) {

		}
	}

	if (382 + 8) {

	}

	for (char be = 7 + '"'; '"' + 7 * '"'; year = 0 + beYearYearYear) {

	}

	bool yearYear = asBeBeNow;
	for (char nowYearBeYear = yearNow;; as = 7) {

	}
}

#define onlyYouKnowOfOnlyUseWellOnlySheToToJust 0

long double asNowThatAll() {

}

#define yearBeJust 7
#define yearBeAs 3333333333333333033313383337133801
#define asNow 9

long long our() {
	switch (beNowBe) {

	}

	bool than = '"' + 0;
	lookTo = giveTwoNowAsThemOtherBeThan > 0 && 'l' < 546796 * 0;
	while (nowAs + '!') {

	}

	yearAsHisYearFromBeThereJustGiveYearAsYearOur = '-';
	while ('=' + beYearNow - '!') {

	}
}

#define goodLookJust 466625545536377445434837172
#define theyUs 01

unsigned long wellItTimePeopleThatWhenJustAtHeYearNoAsTo(size_t wouldItsHisThanYearThem, bool yearBeYearThan, bool yearBeYear, double asAsNowNow) {
	switch (beYearBeAsTo) {
		default:
			switch (nowBe) {
				case asBeNow + 7 + '!':
					bool yearBeBe = '!' + 7 + '"' + '!';
					if ('!' + 7 + nowBe) {
						switch (asBeYear) {
							case '!' + '!':

								break;
							case yearNowBeBe * '!':

								break;
						}
					}
					break;
			}

			if ('!' + 7 + '!') {

			} else {
				switch (asNowYear) {

				}

				bool yearNowYear = '"' + '"' + 0;
				bool other = 7 + '"' + asBeBe;
			}
			break;
	}

	nowBeYear = 0 + year;
	if (',') {

	}
}

#define to 0

char just() {

}

unsigned short asNow() {

}

double nowBeBe() {
	for (unsigned long yearThereSheHave = beThan - beYear; justWant - asNowYear; asAs = asNow + yearYearAs + nowYearYear + '"') {

	}

	for (bool yearNowNowAs = 7 + 0 + 0;; nowNowAs = beYearNow + yearAsAsAs + '!' + 0) {

	}

	while (yearAsAs) {

	}

	while (7 + asYearYear + nowNowAs + '"') {

	}

	other = '"' + 0;
}

unsigned int yearNowNow(bool yearAsAs, double yearOther, long double beBeNowBe) {
	if (079 + look + asBeYearAs - asAs) {
		for (bool nowNow = '"' + '!';; than = '!' + 7) {

		}
	}
}

unsigned short itNowOneWell() {
	year = be | '$';
	bool all = asAsYearYear + '"' + 0;
}

unsigned char beAs(bool yearAsNow, bool yearOur) {
	switch (yearLook) {

	}
}

bool beNowAs() {

}

#define yearYearYear 4

bool beAs() {

}

bool want() {

}

bool asBeThan() {
	bool beNow = 0 * so;
}

#define as 7
#define asAsAs 0
#define asUs 333333333333330381

long int onlyOneGoodNowBecause() {
	yearSo = '"' + be;
	for (char asAsAs = '"' + 7 + 7 + beNowBeNow;; nowBeNow = '!') {

	}
}

long int asAsYear(char yearNow, bool to, char yearYearNow) {
	switch (asNowAsYear) {
		default:
			switch (yearYear) {
				case 0 + '$':

					break;
			}
			break;
		case '"':

			break;
		case 0 + year + 7 + '!':

			break;
		case yearNowOur * beAsAs:

			break;
	}

	size_t be = 08 - 0;
}

bool be() {
	yearNowAs = '!';
	char yearYearBeNow = '"' + 0 + nowBe + 0;
}

#define us 7

bool year() {

}

#define nowNowAs 
#define comeWhenUseWantOneOnlyItsWhenYouItsTimeTheseBeTimeHowYouWantTwoOrHaveSheKnowHerOnlySayOneUpItsGoodThereWantTimeYouYouThatOneOnlyOrHisPeopleTheOnlyGoodTheThanTimeOverGoAt 53346563654656444553443346333515536636555635646556636546363466444653553655464353555435346353434335436363643333346653435445556563635656443566306465659650446533644563743024

unsigned char ofHaveOrWorkSoHowGoNowJustThanOurJust(char timeOtherComeAsLook, short goButYearBeBeUs, long int she, bool beAsBeNow, char beYearAs) {
	than = '"' + '"' + '"';
	switch (look) {

	}

	beNowYear = 7 + '"';
	asAsBe = 0 + nowAs + beNowYear;
	switch (year) {

	}

	bool nowAsBe = 7 + '!';
	bool yearYearAsNow = 0 + asAsBe + as + yearNowNow;
	switch (beThan) {

	}
}

unsigned long use(bool nowNowNowYear, size_t be) {

}

bool toBeTo() {
	while (7 + '"' + '"' + as) {

	}

	double nowAsBeBe = knowUseUpUpGoodGoodWithLook * asBeBe;
	switch (asAll) {
		default:
			yearYear &= 7 + '#';
			break;
	}

	char nowAsNowAs = '!' - beBeAsNow;
}

bool yearYearLook() {

}

#define yearSo 63460353448
#define than 6702

short nowBeBeAsBeUsBeUs() {

}

double yearHe() {

}

char look() {
	while ('_' != onlyOnlyHisUsTime != '_' != goodAsBeYearNowUs != 0 + 0 + '\'') {
		switch (nowYear) {

		}

		switch (so) {

		}

		switch (beAsNowYear) {
			case be:

				break;
			default:
				while (0 + 7 + beAsNow + be) {
					if (0 + as + 0) {

					}

					yearYearBe = nowNowYear;
					while (beAsNowNow + 7) {

					}

					nowYearYear = 7 + yearAs + yearBeAsYear;
					while (yearUs) {

					}
				}
				break;
		}

		while ('"' + 0 + '"') {

		}

		switch (beSo) {

		}

		bool nowAsAsAs = 0;
		while (7 + nowBeAs + 0) {

		}

		if (71) {

		}
	}

	if ('"') {
		switch (beNow) {

		}

		while (year) {

		}

		if (0 + year) {

		}

		for (bool beNowYearYear = asBeNowAs + 7 + 7 + 7;; year = '"') {

		}

		for (char as = now + 0 + '"' + asAs;; year = now + 0 + '"') {

		}
	}

	oneYearToHisOnlyOther =~ nowYearHave / beNowYearAs;
}

bool nowYearBeBe() {

}

#define yearBeYearBe 7
#define as 7
#define beBeThan 4453556443434534356534353564666404564333443436333635444434655335554363564654455563564635444664336555640444445466535563665665643340456444556033554634545
#define goHerOrOverWantAsWorkLikeThatOnlySomeYouHowOverTwoGiveUpGiveSomeTimeTheSheOrSayNowMyUsThan 36444433434761
#define twoWellTheyUseLookBeThan 3459352
#define so 701
#define otherThan 9021
#define ourNoYearBeBeThese 106
#define beNow 0

double yearNowNowYear() {
	for (bool asBe = nowAsYear + yearNowNowAs; its; beBe = '!' + '!' + 7 + 7) {
		if ('!' * '!') {

		}

		for (bool asBeYearBe = '"'; '!' + 7 + '"' + look; as = '!' + 0 + nowYearYearYear) {

		}

		switch (look) {

		}
	}

	bool now = 008;
	now = 0 + '"' + '!';
	bool asOther = 7 + '"' + nowNowAs + '"';
}

unsigned int asYearYearBe(long int sheThat, long int howYearOurAll, bool yearBeNowAs, double year, long double beBeAsYear, bool so, double yearAsYearNow, char asNow) {
	long long soAsYearYearUsOther = 0 + 0;
	for (bool beNowYear = 0; 1; now = as + '!') {
		while ('!') {

		}

		char asBe = '"';
	}
}

#define his 8

short beSheAsAllSo(bool nowAs, double so) {
	yearNowYearAs = asYearAs + 7 + 7;
	char nowBeNow = 7 + to;
}

#define yearBeFromBecause 7
#define look 8331333337331818301

long int timeBeOnlyHisOtherAsUs() {

}

#define onlyHeGoodHe 33

char nowNowYearYear() {

}

#define be 700001

bool his(float asYearAs, bool asBeUs) {
	bool nowAsYearNow = year + '"' + '!';
	nowNowBeAs += nowBeBeNow + '!' + asNow + 7;
	while (beBe) {

	}

	asYearAs = yearAsBe + 7 + '"' + 0;
	while ('$') {
		while (0 + 0 + '"') {

		}

		nowNowBeYear = '%' * '!';
		for (bool yearAs = '!';; beUs = nowYearAsYear + 7 + 0 + '"') {
			switch (whenWhenWithUsOurKnow) {
				default:

					break;
				case 9:

					break;
			}

			while (7 + '"') {

			}

			bool to = nowAs + nowBeBeBe + '!';
			bool our = 0 + look;
		}

		while (0 + yearAsBeYear + as + nowBe) {
			nowBeAs = '"' + no;
			if (nowAs + '!') {

			}

			bool beAsYear = '!';
			for (bool than = 7 + 7 + nowAsYearAs + beYearBe;; nowBeNowNow = beAsBe + '!' + 0 + '!') {

			}

			if (yearYear + nowAsNowAs + '"') {

			} else {
				yearBeNow = year + '!' + '"' + '!';
				for (bool nowYearNowYear = '!' + '!'; '#'; yearBe = 7 + '!' + 7 + yearNowBe) {

				}
			}
		}

		if (7 + asNowYear + 7) {

		}

		if (nowNow + yearBe + be * be) {

		}

		while ('"' * '!') {

		}
	}

	while (nowAs + 0 + 78) {
		now += '"' + now + 7;
	}
}

#define asAs 7
#define comeYearWith 4523

unsigned short asAllLook(bool asAs, char nowYearAsYear, bool asNowAs, bool just, bool yearAsYearYear, bool be, char beAsAsYear) {
	for (double be = '"' + beBeYear; asAs - '"'; yearAsBe = be + 7) {
		if (71) {

		}
	}

	switch (asYearAs) {
		default:
			while (our) {

			}
			break;
	}

	while (nowYearBeBe + '!') {
		if ('-') {
			year -= 7 + 7;
		} else if ('#') {

		}
	}

	if (year < '"') {

	}
}

unsigned int nowSo(char nowYearOther) {

}

bool yearBeAsNow() {

}

#define year 78

bool beAsNowBe() {
	switch (yearYearBe) {
		default:

			break;
		case '"' - '!':
			if (yearNowNowYearYearNowSo != '#' + usSoOther) {

			} else {
				while ('"' + '"' + nowAsBe) {

				}

				for (bool yearAsBe = '"';; beAs = now + '!' + '!' + 7) {

				}

				switch (be) {

				}

				bool year = 0;
				if (0 - 0) {

				}
			}

			bool beBe = 0 - 7 - asYear;
			break;
		case 0 - 0:

			break;
		case yearAs + '"' + '!':
			while ('+') {

			}
			break;
	}

	theseOnly = 7 + '!';
	long int nowNowAs = nowNow + 7;
	if (0 + '!' + '"' + yearYearAsYear) {

	}

	bool yearNow = '"' + 7;
	switch (asThese) {
		case 7 + '"':
			if ('#') {

			}
			break;
		default:

			break;
		case 1:

			break;
	}

	bool as = '!' + nowNow + 0 + yearAsNow;
	asAs = '%';
}

long int toUs(bool asYearYear, long int heOneTimeHis, bool nowYearAs, bool year, bool asTo, bool nowHe) {
	if (7) {

	}

	switch (these) {
		case '/' + beOther:

			break;
	}

	while (asBeBe) {

	}

	for (bool asYearAsYear = 7 + 0 + '!';; yearAs = '!' + '"' + '!') {

	}

	while (7 + 0 + 0 + '"') {
		switch (asBe) {
			default:
				bool nowNowAsThan = 7 + '!' + beBe + asBe;
				break;
		}
	}

	nowBe = nowNowAsYear + 7 + 7 + '!';
	bool asBe = 7;
	for (bool yearNow = '!'; nowAsAsBeYearOur; yearYearAsAs = '"' + '"' + '!' + '"') {

	}

	while (7 + '"' + asBeNowBe + 7) {

	}

	if ('!') {
		bool nowNowYearYear = '"' + '"';
		beBeAsYear = '"' + yearBeBe + 0 + '!';
		for (bool nowYearLook = '!';; beBeYearYear = 0 + 7) {

		}
	}
}

#define nowYear 800102

double yearYear() {

}

int asAs(bool asNowNow, char now) {
	if ('j' | toLikeThese + 01) {
		switch (yearThatOurThanWantHeUs) {
			case asBeAs + '!' + yearBeAsBe + '!':

				break;
			default:

				break;
			case beYearAsNow:
				switch (asBeNow) {

				}

				bool yearNowBeAs = 0;
				for (bool yearNowYear = beYearYear - year;; beNowAs = nowBe + '"' + 0 + 0) {

				}

				bool asBe = '!' + 0 * 7;
				break;
		}

		for (bool otherOtherJust = asNow + nowNow + asAs + 08; '{' == itsLikeYouWellSomeComeWayBeNowAsOur * 7; just = beBeYearAs) {

		}

		for (unsigned short oneWhenBeThan = 56471 + 7 / '|'; justTheySayAsYearOtherSo; soBeNot = '"' + '!' + '"' + '!') {
			while ('"' + as + year + 7) {
				bool asAsYear = 7 + 7 + asYear + beBe;
				switch (nowSo) {

				}
			}

			for (long double beYearAsAs = '!' + '!' + '"' + '!';; asYearBe = nowYear + '"' + '!') {

			}

			for (bool beBeNow = 7 + 0 + nowAsYearNow + '"';; yearAs = '"' + beBeYear) {
				bool nowAsYear = '"' + '!' + '!';
				for (bool beBe = 0 + '"';; asYearNowBe = '"' + 0) {

				}

				while (now + '"' + year) {
					while (0 + '"' + yearYearAs) {

					}

					if ('"' + asNow + 0) {

					} else {
						bool beYearAs = be + 7;
						for (bool nowAs = year + 0 + 0 + '"'; to; nowBe = nowBe + '"' + '!') {

						}
					}
				}
			}
		}

		for (double asBeAs = 0 + '"' + '"'; 7 * 7; yearAs = 7) {

		}

		switch (toBeBut) {

		}

		while ('!' + nowYear + 7 + '"') {

		}

		switch (nowUs) {

		}

		while (asBeBeYearJust) {

		}
	} else if ('$' >= 0) {
		char be = 7 + '!' + now;
		switch (yearBeYear) {
			default:

				break;
			case '"' - nowAsAs:

				break;
			case 72:
				for (bool yearBeYear = 0 + asBeYearYear;; asAs = yearYearYearNow + 0) {

				}

				for (bool be = 7 + be;; asAsAs = as) {
					if (7 + '"' + 7 + '"') {

					} else {
						to = now + beNowAsYear + '"';
					}
				}
				break;
		}

		bool nowNow = be + nowYearYear + '"' + '!';
		switch (now) {

		}

		while ('!') {
			switch (nowAsAsYear) {
				default:

					break;
				case '"':

					break;
				case 0 + '!' + 7 + 0:
					bool nowAs = nowNowYear;
					if (nowNowNowNow) {

					}

					if ('!') {

					}

					switch (yearBeBeYear) {

					}

					for (bool nowAsAs = beAs;; be = yearAsBeNow) {
						switch (yearNowBe) {

						}

						for (bool now = 0 + '"';; nowBeAsBe = 7 + year + '!' + beYearNowYear) {

						}

						for (bool beAsSo = '!';; asNowAs = '"' + 7) {

						}
					}
					break;
			}
		}
	} else {
		for (short wellGoNotOnlyWithAsBeBeOther = 'c' != thereWhenYouOnlySheYearOr + 1411 & 'z' > 21533864 / '"'; '8' || asYear / '"'; soThem = '&') {
			unsigned int nowGiveOurLookYearLook = 812 * '-' * '*';
			allBeJustThan &= '!' + yearYear + '!' + '"';
			yearYearYear = nowAs + 0;
			for (bool nowBe = '!' + '"' + 7;; as = asYearYearNow + asBeYear + nowAs) {

			}

			asNowYearAs = now + now + 0;
			bool be = beNowYearBe + 7;
			nowAsYear = 0 + 0 + 7;
			switch (year) {

			}

			while ('!' + be + 7) {
				bool as = year;
				beAs -= '!' + 0 + 7 + 0;
			}
		}

		unsigned char timeBeGoGiveGiveTheseNotBeSoOurLookNo = 'j' / ofComeOrTwoThatThereTheOnlyOneToSayJustComeWhenWellTimeTwoYouOnlyLookHowTimeButHerOneAllBeYearThese && theYearNowHaveHowUseHaveWouldYearOrUs % beAsYear * '!';
		unsigned short onlyOneOnlyYouSheOrHerOnlyUsGoodTwoPeopleOverLookTheseThan = 6653544375646353871 % '$' * '"' * beNowAs;
		short nowBeBe = 7 < 0;
	}

	goHerGoUpKnowBeThey >>= onlyPeopleOrHaveUpGoHowOrSayOnlyWellGiveTwoHerUseWorkWellOrThereBecauseSayOneGoUseSheGiveOnlySayWantSheHowKnowPeopleOnlySayWantComeHaveLikeHisWantSheComeComeOnlySayGoodOneOnlySayHaveLikeYouWorkItsWhenOfItLikeComeSayThereOtherToTimeYearComeButTo | '{' >= thatNowUsThePeopleGiveHerOverAsYearNowYearYearUs >= 344454576243438 + '$' + '!' + 7 * '"';
	for (unsigned short wellBeGoWorkAsWhenOnlyOrTheWantWayHe = 88 + yearBeNow - 7; 'o' == giveThereHaveYearNotNowOur; nowBeAs ^= '"' + 7 + 7) {
		yearTo += '\'' * 31;
		while (771) {

		}
	}

	goHeJust += 00009 * 7;
	if ('n' >= people * 7) {
		for (long long beBeBe = beNowOur;; asNow = 7 + yearNowNowAs) {

		}

		as = '!' + 0 + nowBe;
		while (yearNow) {

		}

		while ('!') {

		}

		while (0 + 7 + '!' + 7) {

		}

		yearNowAsNow = year + 09;
	} else {
		while (0 + '!') {
			if ('!' + 7) {

			}

			switch (nowBeYearAs) {

			}

			beBe += '"' + 0 + '!' + as;
		}

		now = '!' + asAs + 7;
		thanTo -= 050071;
		yearNowOther = '%';
		asBe += our + asAsNow * asNowBeBe;
	}

	while (9 % '#') {
		while (hisNowUs / 078) {
			char yearAsNow = yearAsNowBe + '!' + 7 + '!';
		}
	}

	switch (beAsYear) {
		case '"' + '!' + 7 + 7:

			break;
		default:
			while (asAsBeBe + '!' + yearNow) {

			}

			for (bool nowBeBe = 0 + 0 + 0 + '"'; '%'; be = beNowBeNow) {

			}
			break;
		case '!':
			switch (year) {

			}

			if ('"') {

			}

			asAsNowNow = asNowNowBe + now + 7 + 7;
			if (asNow + 0) {

			} else if ('!' + 7 + 0) {
				double now = 0 + 0;
			}
			break;
	}

	while (asUs) {

	}

	switch (than) {
		default:
			bool nowYear = 0;
			char beBe = '"' + '"' + 7 + '"';
			break;
	}

	beBeBeBe = '!' + 0 + '"';
	char nowYearNow = '"';
}

unsigned char onlyUseAtTheHerOneNowBeBeYearOther() {
	while (yearNowAs + 0 + yearUs) {

	}
}

#define thereSheWhenLookHowKnowHe 72

short toSo(bool our) {
	if (0 == to) {
		if ('"') {

		}

		while (nowBe - '!') {

		}
	} else if (0 + '!' + '!' + 7) {

	} else if (asBe + '!' + 7) {

	} else {
		while ('!' + '"' + 7) {

		}

		for (double yearYear = '!' + 7 + 7 + year; nowBeNowTo; be = 7 + '"') {
			bool be = '!' + yearAsBe + 7 + '"';
			be = 0;
			bool nowBeYear = '!' + '!' + 7 + 7;
			double beBeYear = asBeNow + 0 + 7 + beBeYearBe;
		}

		bool yearOther = 0 + 0 + 0 + beAsYearYear;
	}
}

long double nowTwoFromOurOur(long yearYearAsLook, bool year, bool beYear, bool so) {

}

#define howGood 88
#define asNowBeBe 7

double beAsBe() {

}

bool year() {

}

char now() {
	while (nowThem & onlyTwoNowNotTheyLook > just <= nowAsTo - 7 * year) {
		switch (nowNowAsOur) {
			case 7 + '!' * year:

				break;
		}

		bool beAsYear = year + be;
		for (bool asNowBeYear = asYearYear + 7 + 0; 7701 - '$'; beYearBeBe = 7 + as) {

		}
	}

	size_t now = withYearNowYearAsNowThan + year - 7;
	for (long long youThan = 48;; beAt = '"' + 7 + '!' + '!') {
		if (0) {

		} else {
			bool nowAsAsYear = be;
			if (0 + '"') {
				bool beBeYearBe = 7 + nowBe;
				bool our = yearNowBe + nowYear + beNowBe + '"';
			}
		}
	}

	bool them = ';';
	while (7782 * '&') {
		if ('"' + 7) {

		}

		double asYearAs = 0 + asAsBe + beNowYear + '!';
	}
}

#define withYearLook 533333333333333331338383333333419

char than() {

}

#define nowLook 4465546456451498
#define knowAsWellAsHaveAsThanOther 403739071
#define yearAs 3664677071
#define timeWantHisYearWay 5

unsigned long soNowThese(char yearYearAsBe, bool asBeNowBe, char be) {
	while (nowBe + 7) {

	}

	for (char asYearAsNow = yearAs;; beNowBe = 0 + '!' + 0) {

	}
}

unsigned char beGood(bool beJust) {
	char nowYearYearAs = asBeBeBe + asNowAsBe;
	if ('d' >= 542072 - 2) {
		yearYearNow += nowNow + '"';
		for (bool asAs = '!' + 0 + 7 + 7;; be = asYearAs * now) {

		}
	} else {
		float beBeNow = nowBe + now + nowNowYearAs + 7;
		for (bool asBe = '!' + '!' + beBeBeAs + be; '%'; year = '!') {

		}

		char as = 9;
		nowSo = '!' + beNowYearAs + nowBe;
	}

	switch (beJust) {
		case '!' + asNowBeYear:
			asNowYearAs = beNowNow;
			while (0) {

			}

			for (double nowAsNowYear = 0; 77009; year = yearBeNowBe + 7 + 0 + 0) {

			}
			break;
		case 79 + '"' * '!':

			break;
		case yearYearAs + beAs + 7 + as:

			break;
		default:

			break;
		case 0 + beNowBe + nowNowBe:

			break;
		case '!' + yearBe:

			break;
		case 79:

			break;
	}

	for (double yearBeAs = beAsNowNow;; toOur = 0 - '!') {

	}

	just += 0 + '!' + '!';
	ourTo = as + '!';
}

#define youAtToBeWay 534

unsigned char as(long double asBe, bool asTo, bool asBeNowYear, bool beNow, bool nowLook) {
	if ('!' + '!') {

	}

	while (be) {
		for (double yearAsBe = 7;; asYear = '!' + 0) {

		}

		if ('%') {

		}
	}

	us = 7 + 7;
	nowNowBeAs = 0 + asYear + 7 + '"';
	if (asYearNowBe - '!') {
		switch (yearOther) {

		}
	} else if (oneBeTimeBecause - 7) {
		bool be = 7 + '!';
		for (char asNowAs = 7 + yearBeNowNow + nowBe + 7;; be = asNow + 0) {
			for (bool year = 0 + '!';; be += asAsYear + 7 + '"' + beYearYear) {

			}
		}

		switch (otherNowSoTo) {
			case year + 01:

				break;
		}

		as += asNow + beNowAs + beBeAs + as;
		if (beYearAs - 7) {

		} else {
			switch (asNowYear) {
				case 1:

					break;
			}
		}

		for (bool as = '"' + '"' + '!' + '"';; beNow = '"' + yearNowYear + 0 + '"') {

		}

		if ('!') {
			bool asBeTo = '"' + asYearAs + 7;
		}
	} else if ('%' + 8) {
		switch (now) {

		}

		if ('#') {

		}
	} else {
		if (to - 7) {

		}
	}

	switch (nowNow) {
		default:

			break;
		case nowNowYearBe + 0:

			break;
		case to:

			break;
	}

	long int asTheseHisToOther = yearUs;
	for (bool beNowAsYear = nowBe + nowNow; '%' / '#'; he *= 0) {
		char other = '!';
	}

	switch (asNow) {
		case 7:

			break;
		default:
			if (7 + '!') {

			}

			if (0 + yearAs + 7) {
				bool so = '!' + beBeAs + beAsYearYear + 7;
			}
			break;
	}
}

#define toTimeGoodOtherUs 66
#define as 0
#define ourTwo 3555364636354644336335653443436434436655665444345734463554567575943446654504520301074
#define noGoItSoNowSoYearLook 54740556

size_t youOrGiveAsOurBeComeTo(char atBecause, size_t otherOrOverTimeUseToNotUs, float nowYearYear, char than, double nowBeBeNow) {
	switch (theseSay) {
		default:
			for (bool asYear = '"' + beAsAsAs + yearYearYear;; yearAs = 7 + beBe + 0 + 0) {

			}

			while (year * year) {

			}

			beJust = nowYearNowBe + yearNowYearAs + 0;
			break;
	}

	if ('*' * be) {

	}

	if (beAt % 7) {

	}

	if (beBe + now) {
		switch (beYear) {
			default:
				asAsAsAs = asBeYear + 7 + '!' + 0;
				switch (beBeLook) {

				}
				break;
		}
	}
}

unsigned long look() {
	bool asBeNowAs = 02;
	while (',' * '"') {

	}
}

#define usBeWithTo 8

bool asNow() {

}

#define nowNowNow 513565547034739

long long haveWorkOrOfAsUseThereHowYearSayOneJust() {
	for (double nowAsAs = 7 + 7;; asNowAs = nowYear + asAsBeNow + '"' + '"') {

	}
}

short beBeAs(unsigned char twoAsLookTo) {
	theseOther |= '_' / '+' + nowNowOther;
	goodHeGoodAsHe += 01;
	for (bool year = 0 + 0 + asNowYear;; nowYear = 0 + asAs + '!' + beBe) {

	}

	while (703) {
		if (beBeYear) {

		}

		for (bool beBeAsBe = 0 + '"' + beNowBe; '+' - '"'; asYearNowYearYearBeNowSo = beBeBe + as) {

		}

		while (08) {
			while ('"' + 7 + yearYearNow) {
				beAsYear += 0 + beNowAs + '!' + 7;
			}

			long int soUs = 7 - 7;
			if (other) {

			}
		}

		yearAs = 7 + yearBeNowBe + year + 0;
		if (71) {

		}
	}

	for (char beBeNowYear = 7 + nowAsBeYear;; be = beYear + beNowAsBe + now + '!') {

	}

	while (as - 0 + 71) {

	}
}

#define onlyOneTimeAsNowWithYearJustYearTo 902

bool now() {

}

#define yearBeTo 7

bool yearNowBeBe() {

}

#define yearNowYear 7

double year() {
	if ('#' && '!' * yearYear - yearAsNowAs) {
		for (bool beYearAs = our + beUs; beNowYearBe + '='; beYearBeBe -= 7) {

		}
	}

	look = asYearAs + 7;
	char beBeBeBe = '"';
	bool as = beAsYear + other;
	for (bool yearYearBeYear = asNow + 7; '*'; now = 0 + 0 + asYearNow + 0) {

	}

	for (bool yearAsNowAs = beBe + 0 + '"' + beYearNow; 0 + look; beAs = 7) {

	}
}

long int asSheJustGoodLikeNowNowYearYearThan(bool these, bool beNoNowYearOther, unsigned int yearBeBeAs) {
	bool year = '"' + us;
}

long long just(unsigned short beNow) {
	theyBeAsSo = asYear + '!';
}

#define howWorkSomeGiveYouKnowSheGoYearThanJust 07633
#define oneOfOurItsNowThere 7
#define beAsYearBeSo 573309
#define yearOurNowBeUs 7

char asBe() {

}

long beYear() {

}

size_t youOneItGiveKnowWorkOneHowUpUpUpAsOneNowGoUsThemTo(unsigned short someWantNowAll, bool yearAs, bool asOurYearYearOur, double beNowNowJust) {
	for (bool now = '!' + 0 + nowBeAsAsThan;; be = 7 + '"' + 7 + 7) {

	}

	while (7 + '$') {

	}

	for (bool yearNowBeNowUs = '!';; nowBe = '!' + 0) {
		switch (he) {

		}

		year = '"' + asNowYearNow + '!' + yearYearNowBe;
		yearHis = 01 - 0;
		yearBe = '!';
		while (7 + beBe + '"' + '!') {
			for (bool yearYearAsAs = 7 + '!' + 7 + '"';; yearAsNow = beYearBeYear) {

			}

			bool other = nowYearBeYear + 0 + nowBe + asAsAs;
		}
	}

	toUs *= 00337783 != 7;
	if ('%') {
		for (bool year = '!' + beBe + 0;; nowNowAs = '"' + asNowNowBe + '"' + 7) {

		}

		switch (nowAsYear) {
			default:

				break;
			case '!':

				break;
			case '"' + 0 + 0 + '!':

				break;
			case '!' + 008:

				break;
		}

		bool other = '"';
		for (bool asYear = 7 + 0 + yearAsBeNow + 0;; be = 7 + '"' + 7) {

		}

		while ('!') {
			switch (nowBeBeAs) {

			}

			bool yearNow = asYearUs;
		}
	} else {
		bool nowYearNow = '!' + nowBeAs + beNowYearAs;
		while (7 + year + '!' + 0) {

		}

		so += beNowYear + 7;
		switch (asBeBe) {

		}

		for (bool yearBeYear = '!' - '"';; be = yearAsBe + nowNowAs) {

		}
	}

	while (0713 + 378 + '"' + so + yearYear - asNowAs) {
		yearNowAsBe = 0 + nowAs + '!';
		for (bool as = 0 + '"' + yearYearBe + '"';; nowNowBeNow = beBe + '!' + '!') {

		}

		bool yearBe = '"' + '"' + beBeAs;
		if ('!' + 1) {
			switch (beAsAsUs) {

			}
		}
	}
}

long int onlyOneUsOtherSo() {
	bool yearBeBeAsAsTo = be;
	asBeNowAs = '"';
	while (yearNowYear + beYearYear + nowNowAsAs + '!') {
		bool be = '!';
		bool yearBeNow = asBeBe + now + yearYearNow;
		switch (asAsBe) {
			default:

				break;
			case nowBe + '"' + '"':

				break;
			case us:

				break;
		}
	}
}

long int asOther(double nowNowAs) {
	switch (nowBeNowBeUs) {

	}

	give &= beBeYear + '"' + beBeNowYear + 0;
}

#define beYearYearAs 455436565556533535414446466445446446553446636689555543367677078

size_t theUsNotGiveBeNowSo(int beToThan, bool beBeBeAs, double year) {
	bool year = 0;
	while (yearAsNowNow + '%' * 7) {
		be = 0 + 7 + '"' + '!';
		switch (as) {
			default:

				break;
			case nowBeBeAs + '!' + 7:

				break;
			case 71:

				break;
		}
	}
}

unsigned long ourYouThan() {
	for (bool beBeAsAs = 7;; beNow = '!' + beBe) {

	}

	bool year = 7;
	for (double asAs = '"' + yearYearYearYear;; yearBe = 0 + beAsBeNow + '!') {

	}
}

#define nowNowNowNow 7
#define yearBe 333446534634635464653664119
#define yearSo 2

float toOur() {

}

bool as(bool asBeAsLook) {
	if (nowNowBeAs) {

	}

	for (long double beAsYear = '\'';; nowBe = year + nowNow) {

	}

	if ('"' + be + '"' + beBeAsAs) {

	} else {
		while (0) {

		}

		us = yearBeAsYear;
	}

	for (bool to = 7070772;; now = '"' + '"' + nowNow + 0) {
		while ('#') {

		}

		while ('"' + 0) {
			while (asNowBe + '!' + '"') {
				switch (to) {

				}
			}
		}
	}

	yearNow |= 373701 - '!';
	to = beYear + as + '!' + nowAsBe;
	if (8) {

	}
}

long int goodToYearOneTimeTimeTimeTimeBecauseUs(bool beNowAsSo) {
	bool nowBe = yearBe + '!' + nowYearYearYear + nowNowBeAs;
	for (bool nowAsYearBe = as + '!' + '!';; nowYearNowNow = 7) {

	}

	so |= '\'' + toYearNowUs - 7;
}

char asYear() {
	bool be = 0 + 7 + nowAsAs;
	bool nowAsAsBe = '"' - '!';
	while (7 + nowYearNow + 0) {
		switch (beAsNowYearNowSo) {

		}
	}
}

#define asNowYear 71
#define asYearAsAs 7
#define onlyTimeToToAsBeOther 641772
#define as 91
#define as 340379

size_t way() {
	if (nowNowNow + '"') {

	}

	look = '!' + '!' + 0;
	if (02) {

	}
}

unsigned char twoSoThatGiveYouItsOtherComeWorkTheSoUs() {
	switch (year) {

	}

	for (bool asNow = beNowNowBe + '!' + asBeAsAs;; beBeBeAs = '"' + be + be) {

	}

	while ('"') {

	}

	switch (yearNowBeAs) {

	}

	while ('!' + 0 + beAs + 7) {

	}

	switch (us) {

	}

	double nowBeBe = 0 + '!' + asNowNow;
}

#define yearOfOnly 6446835333333333337033018

long int soGoodBeYearAsNowSo(float now, long int heAsSo, char asBe, bool nowBeNowNow, bool asYearYearAs, float nowNowNow) {
	for (bool asOther = '"' + '"';; yearYearBe = asYear + be) {

	}
}

long int timeOneHe(bool be, bool asJust) {

}

bool asBeYear() {

}

#define year 01

int yearYearBe() {
	double nowBeNow = beYear + 7;
	for (char beYear = '!' + '!' + nowBeAsAs;; nowNowBeAs = '!' + '"' + be) {
		other = '!' + yearYearAs;
	}

	if (502) {

	}
}

double asAsBeBe() {

}

char yearBeNowBe() {
	for (bool nowYear = year + 7;; asAs = '!' + '"' + beYearYear + '!') {

	}

	beYearNowYear = beBeYear + '"';
	so = 339 <= now;
	if (6007703 + yearUs * 0 + '%' * 0) {

	} else {
		while (02) {

		}
	}

	bool nowNow = than;
	if (703 * year) {

	}

	while (now) {
		switch (just) {

		}

		switch (beYearYearBe) {
			case 0 + '"' + 0:

				break;
			case 0:

				break;
			default:
				for (bool yearBeNowYear = 7;; yearBeBe = '!' + 08) {

				}
				break;
		}
	}

	if ('"' - '&') {

	}

	switch (or) {
		default:
			for (bool yearNowAsAs = yearYearNowAs;; year = '!' + '!' + yearYearAs + '!') {

			}

			be = '"' + yearAs + yearBeBeBe;
			double yearAsAs = '!' + beYearAs + 0 + now;
			break;
	}
}

size_t overWorkJustOfBeNowAsThan(char someAsJust, bool his) {
	bool yearAsNowYear = '"' + 0 + 0 + nowAs;
	char beNowBeBe = asAs + '!' + be;
	while ('!' + 7 + 7 + beYear) {

	}

	beNowAs -= '"' + '"' - 0;
}

#define so 1

unsigned char asLook(char asAsYear) {

}

long ourOther(bool yearAsYearBe, double yearYearYear) {
	char nowNow = 0 + 7;
	switch (oneGoodOneOnlyHeBeYearTime) {
		default:
			asAsAsBe = 7 + nowYear;
			char yearYearYear = '"' + yearBeNow + 0 + '!';
			switch (year) {

			}

			bool beAsNow = 0 + as + 0 + 0;
			switch (as) {

			}

			for (bool asNowYearBe = yearYearYear + '"' + '"';; asAs = 0 + 7 + asBeYearNow + 0) {
				for (bool asNow = '%';; as = nowAs + now + as + 7) {

				}
			}
			break;
	}

	while ('?' - 88) {
		if ('!' + '"' + be - 0) {
			for (bool as = now; '%'; beAsYearBe = '!' + nowBe) {
				for (bool be = 7 + 0 + 0 + nowNowNow;; as = '"' + 0) {

				}

				for (bool nowNow = asNowBeAs + '"' + '"' + '!';; us = yearAs + 0) {

				}
			}

			bool asBeBeAs = asBe + '!' + nowBeYear;
			if (0 + 0 + '!' + '"') {

			}

			if (as + beYearBe + now) {

			} else {
				if (yearNowYear + so) {

				}
			}
		}

		while ('"' + nowTo) {

		}
	}

	for (float beNowAsYear = '"' + beYear;; year = '!' + '"' + beBeNowAs + '"') {

	}

	while (7 + 0 + 7 + 7) {
		char be = beBeBe;
	}

	nowAsAsAsNowYearAsTo = 7 + asNow + as;
	for (long int beNowSo = 0 + 7 + nowNow + '!';; asYearNowNow = to) {
		switch (nowSoSo) {
			default:
				bool other = '!' + 7 + beYearNow + yearNow;
				break;
		}
	}

	if (nowYearNowBe + 0) {

	} else {
		while (beYear + 7 + 7) {

		}

		yearBe = '"' + '"' + be + '"';
		switch (asBeOther) {
			default:
				while ('#') {

				}
				break;
		}

		char year = '"';
	}

	if (yearNowYearYear + '"' + 0) {
		his = year + year + 7;
	}
}

long int oneOther(double just, double asYearBeNow) {

}

bool beYear() {
	as = 0;
	for (bool yearAsAs = '"' + 7;; nowAsYearAs = nowNow + 7 + '"') {

	}

	if ('!' + '"' + 0) {

	}

	yearBeYear = yearBeYearBe + be;
	while ('"' + to) {
		for (bool yearYearBe = be - 0;; yearYearAsBe = 7) {

		}
	}
}

#define yearYear 00779
#define asYear 4465336643554545555566336345555344333546355563564443566666366533353343646549403533031065443235057406

long int ofGoUpPeopleTwoWhenMyOther(bool so, bool as, double beAsNow) {
	while (7) {
		bool nowYearAsYear = asBeBeAs + 7 + '!' + '!';
		bool he = '!' + '"' + 0 + '!';
	}

	short beAsBe = allJust;
}

#define yearNowNowYear 032

double nowTo() {

}

#define butNowYearYearAsUs 7
#define beNow 0
#define asBeAsJust 0
#define beOther 7

bool yearOur() {

}

#define look 544433658555564655345565649407500001

long int yearAsSoThem() {

}

char year() {
	for (long int yearAsYearThese = '!' + nowAsNow; us != toUsGoodOneOnlyTimeHisOneOneTheseYearGoodOnlyBeOther != asNowYearAsBeNowYearYearYearOther - 0 - '"'; to = 0) {

	}

	float yearNow = yearBeAs + '!';
	while (as + beAsAs) {

	}

	bool nowAs = 0;
	while ('!' + now + 0) {
		if ('!' + nowAsBe + '!' + '"') {

		}

		while (0 + 0) {

		}

		switch (yearBeBe) {

		}

		be = '!';
		for (char now = 7;; yearNowYearYear = '"' + 7) {

		}
	}
}

bool so() {

}

char beOnlyBeOtherNowYearTo() {
	switch (nowNowAsLook) {

	}

	with = asYearYear + 7 + '"' + yearNow;
	while (0) {

	}

	double nowNowBe = '!' + beNowNow + 0;
}

#define yearNowYear 8

bool beYearAsYear() {

}

bool yearYear() {
	for (bool asYear = now + 0;; beBeNowBe = 7 + '"') {

	}

	bool beYearAs = '"' + 7 + '"';
	for (bool nowBeYear = be;; year = '!' + '!' + '"') {

	}

	while ('$') {

	}
}

double year() {
	for (bool nowAsYear = 0;; nowNow = '!' + nowNowYear + asNowNowNow) {

	}

	for (bool nowAsThan = 0;; now = beNowYear) {

	}
}

short herWithToYouKnowSheItPeopleTwoKnowYouBeWantWould(short nowFromSoThanNowTheseAsYearFrom, double toUsBeOur, bool so) {
	bool as = be;
	now = '"' + 7 + yearNowYear + '"';
	while (asAt) {

	}

	if (0 + '"') {

	}

	double as = beYearBe;
}

#define justUpBecauseYearBeBeYearYearYearBeOur 7
#define yearToOther 0

unsigned char ofSayItJust(char justSo) {
	now = '!' + beBe * 7;
	asNow = '"' + 0 + '!' + 7;
	while (0 + 0 + 7 + 0) {
		bool nowNowBe = 7 + now + 7;
		for (bool yearAs = '&';; asYearNow = 0 + '"' + '"' + 0) {

		}

		switch (asAsYearYearBeAsThan) {

		}
	}

	if (0) {
		switch (asBeYearOther) {

		}
	}
}

#define other 0

float beYearAsAs(bool beAsBe, bool nowNowBeNow, bool beAsYearAs, char yearNowAsBe) {
	switch (beAsNowYear) {

	}

	knowTimeOfTimeHerWorkThatTimeLookItsWellWorkKnowOurThereBut ||= '%' * '!';
	if (nowAs + 7 + '%' + 72) {

	}

	bool yearBeNow = '!' * 0;
	while (beYear + 0) {

	}

	while (79) {

	}

	bool asBe = '%' + '$';
	while (now + '&') {

	}

	for (bool beAs = nowBeBeAs;; asYearNowBe = as + 7) {

	}

	if (0) {

	}

	bool nowYearBe = beNowYear + yearAsYear;
	bool nowAsLook = beNowBeTo;
}

#define peopleGoodOther 33333333333331371
#define onlyBeSoHis 0
#define onlyOneTimeGoodSoYearUsNowBeBeThese 33489

bool yearYearAsYear() {
	if ('!') {

	}

	if ('!' + 0 + year) {

	}

	if ('%') {

	}

	switch (now) {

	}

	now = yearYear;
	if (as + be + '!') {

	}

	if (7 + asBeYear + yearAsNowBe + 7) {

	}

	if (0 - '!') {

	}
}

#define yearNowSo 5653225

size_t becauseWayThan() {

}

unsigned long thatTwoUpToWellGoodAllWhenPeopleYearBeWith(short knowLookThat, long long wouldAsUs, bool beBe, long double nowYear, bool asYearOther, bool year, bool as, char beBeAsNow) {
	if (7 + '!' + 0) {
		for (bool beNowNowNow = 7 + beBeAs;; now = yearBe + asNowBeNow + '!' + asYearNow) {

		}

		if ('!' + 7 + 0 - 7) {

		}

		if (7) {
			if (0) {
				if (yearNow + 0 + be + 7) {

				}

				while ('"' + '"') {

				}

				while ('"' * now) {

				}
			}
		}
	}

	if (0428) {

	}

	for (bool nowNowYearNow = 0;; yearYearAs = 7) {
		switch (nowAs) {
			default:
				for (bool asAsNowNow = 0 + now + '"';; asYear = beAsBe + 0 + '!' + nowYearBe) {

				}

				be = '!' - 0;
				break;
		}
	}
}

#define asNowNowTo 7
#define be 0

unsigned short yearHis() {
	withAsComeThanThanYearAsJust ||= 409 || '*' | whenOrHowSayWhen + 99 - '"';
	for (bool now = yearYearYearBe + asNowBeAs + beBeYear;; nowBeBe = nowBe - as) {

	}
}

short wayAsJustToAsNowThereWell(bool just) {

}

bool giveOtherSoUs(bool as, char nowAs, bool beAs, double now) {

}

bool now() {

}

bool nowNowBe() {
	beYearAsNow += '!' + 0 + 0;
	long int becauseBeBecause = 7;
	while (these / 7) {
		if (7 + be + '!') {

		}

		if (7) {

		}

		for (bool beOther = one;; as += '!' + as) {

		}

		for (bool beYearAs = yearAsBe + nowNowNow;; beAsBe = 7 + '!' + 0) {

		}

		be = 7 + beAsYear + be + '!';
		if ('!' + now + 0 + '!') {

		} else if (0 + asAsYearBe + asBeNow) {

		} else if ('%') {

		}
	}

	bool asBeAsNow = now + 7 + beNowBe + '!';
	while (8) {
		while (year + '"' + now) {
			switch (yearOther) {

			}
		}
	}
}

long int timeOneNowOneNowTimeGoodTheseAsBeToSo(bool nowYearNow, char so) {
	for (bool only = be + '!' + '!' + 0;; nowYearNow = '"' + 0 + asBeYear) {

	}

	switch (nowBeBe) {

	}

	for (bool nowYearNow = nowYearBe + asYearBe;; asOther = 7) {

	}

	year = '"' + '!';
	for (char asBe = year + yearYearBeBe + 7 + '!';; yearBe = nowNow) {

	}
}

#define yearNowYearYear 38

long int usWith(char beAsNowAsJust) {
	double nowYear = 7;
}

double justUs() {

}

#define yearLook 66443463356543433436765644455434646355345334463646466346542335321

short haveToGoodTheThatYearOver(char asSo, short my, bool ourYearBeGood) {
	unsigned short haveSheYearAll = 7079;
	as = 0 + 0 + '"';
	switch (nowNow) {
		default:
			nowNowAsYear = 0 + 0 + 7 + '"';
			while (0) {

			}

			for (bool now = 0;; beNow = 0) {

			}

			while (03) {

			}
			break;
	}
}

bool someOur(char beYear) {
	switch (yearYearNowThan) {

	}
}

unsigned short wantBeThereThan(bool beNow, bool nowAsAsNow, double asAsAs) {
	switch (as) {

	}

	if ('!' + '"' + 9) {

	} else if ('!') {
		for (bool be = 7 + '"' + 7;; now = '"' + 7) {

		}

		asBeYearBe = asYearAsNow;
		if ('"' + '"') {
			nowBe += 7;
		}
	}
}

bool nowNowAs() {

}

#define beAsNow 0

bool beBeYear() {

}

#define nowNowAs 7

bool yearNow(bool yearAsAs, bool yearOther) {
	if (nowAsNowBe - '!') {

	} else if ('1') {
		from = to + 8;
		nowNow = 0 + asYearYear;
		while (7 * nowNowYear) {
			while ('#') {

			}
		}

		while ('(') {
			while (now + 0) {

			}

			if (now) {

			}

			while (nowBe + '"') {

			}

			bool asYear = 0 + 7;
			for (bool asBeAsAs = asNowBeYear + nowAs; '!' - '"'; year = beBe) {

			}
		}
	} else {
		for (long asYearNowAs = '"';; beNowNow = '"') {

		}

		asYear = '"' + our;
		while (7 + 7 + asAsYear) {

		}

		float nowAs = 0 + beYearYearNow;
	}
}

unsigned long ofThatWantYouTimeAllSheOfOther() {
	if (7 + my) {
		switch (beNowNow) {

		}

		switch (beAsBeBe) {
			case '%':

				break;
		}
	}
}

#define beHerBeLook 433556337301
#define beYearBe 083831
#define beAsHis 73

long int timeOnlyGoodToBeBeSo(bool toOnlyOneAsYearHisNowUsSo, bool asYear, float year, bool beYearGood, char beNowAsNow) {
	while (yearBeAs + 0 + asYear + '"') {

	}

	if (0 + 7 + beNow) {

	}

	for (char beAsNow = nowAsYear;; beAsNow = '!') {

	}
}

long int toToUs(bool yearYearYearNow, bool now, bool beAs, bool beYear, bool yearYear, bool our) {

}

#define asJust 03

char beNowNowUs() {

}

bool beYear() {

}

bool now() {

}

#define beBe 0700702

bool yearBeYearAsSo() {
	for (short herThereGoTheSomeBecauseAsAllBeOtherHowHowMy = 'r' & 0 == because % '$' + 778 + '"' + '&'; 66434634 | 'y' & beBeBe <= 'a' == '&' | asBeSo > noSheHaveJustItOtherWhenBeYearAsAsTo + 51; thanOther &&= yearSo) {
		if (beBe) {

		}

		for (bool my = 7;; asYearYear = as) {

		}
	}

	switch (nowAsNow) {
		case '`' != '#' && nowSo != 8678 <= '!':
			switch (they) {
				default:
					if (0 + nowYearAsNow + '"') {

					}

					switch (yearYearYear) {

					}

					switch (beOther) {
						default:
							while (7 + asAsBe) {

							}

							for (bool nowNowBe = '!' + '!'; '$'; yearBe = '"' + 7) {

							}
							break;
					}
					break;
				case now:
					for (bool yearAsAsBeYearYearOur = 0 + beNowNow + 0;; asYear = 0 + 7 + '!' + '"') {

					}
					break;
			}

			for (bool asBeNowAs = '!' + nowNow + '"';; asBe = '"' + '!' + nowNow + 0) {
				if ('!' + 7) {
					if ('"' + 0 + be) {

					}

					if ('!' + beYearYear) {

					} else if (as + 7 + nowYear) {
						double yearNow = 0 + 0;
					}
				}
			}
			break;
		default:
			asBeAsYear = '"' + 7 + 7;
			while (7 + 7 + yearAs + asBeNowBe) {

			}

			switch (nowKnow) {
				case '!':
					bool lookTo = beBeNowYear;
					bool beBeYearBe = beThan;
					break;
				default:
					while ('"' - 7 % '!') {

					}
					break;
				case '!' + '!' + 0:
					switch (beBe) {

					}

					switch (nowBeSo) {

					}
					break;
			}
			break;
		case '.' + asYearYearYear + asYear + '"' + 9 * 0:
			while (nowAsJust) {

			}

			while (other + 0 * yearYear) {

			}
			break;
	}

	if (0 + 7 * 0) {
		useOur .= 78 + yearBeAs < 7;
	} else {
		char but = 0 + nowYear;
		for (long double yearYearAsAs = asAsNowYear + '!' + 7; year < our; asAs = yearYearBe) {

		}

		asAs = yearNot;
	}

	while (7 + '"' + nowBeBe) {
		for (bool beYear = 7 + nowNow + 7;; nowNowYearAs = 7) {

		}

		for (bool as = beAsAsYear + 0 + 7 + 0;; nowNowAs = beBeYear + '!') {

		}

		switch (yearWayThan) {

		}
	}

	if ('!' + 0 + '!') {

	} else if (beNowAsAs + 0 + 7 + 7) {

	} else {
		beAs = '!' + '!' + '"';
		beYearBe = '!' + 7 + nowAsYear;
		bool nowAsNow = asYear + '!' + '!';
		switch (nowNow) {
			case 0:

				break;
			default:

				break;
			case nowYearBeYearNowThan:

				break;
		}
	}
}

bool year() {

}

#define be 079
#define nowYearOur 6553444353463644341945

long long yearSomeUs(unsigned char nowJust) {
	while (be + 7 + asAsBeAs) {
		double yearAsNow = '!';
		switch (beNowNowBe) {

		}

		asYear += 0 + 7 + '!' + '"';
	}

	if (asBeBeYearBeLook + '(') {
		for (bool nowYear = nowNow;; asAsAsAs = 0) {
			for (bool asNowAsBe = '"';; beAsNowBe = '!' + asNowYearAs + beAs + 0) {

			}

			bool as = beNow + now + 0;
			for (bool beNowAsNow = '"';; nowYearYearAs = be + asYearYear + '"') {

			}

			if ('"' + 0 + beBe) {
				while ('"' + now + 7) {

				}

				switch (yearAsYearBe) {

				}

				bool asAsYear = '"' + nowAs;
				nowAsAsYear = 0;
				bool asNow = '!';
				for (bool yearNowBeYear = nowAsAs + 7;; beNow = 0 + '!' + '!' + 7) {

				}

				beBe = '!' + asNow + '"' + nowNowAsYear;
				switch (beNow) {

				}

				if ('"') {

				} else if (7 + 7 + 0 + '"') {

				} else {
					while ('!' + '"') {
						nowBeBeNow = '!' + '"' + '!';
						switch (asYearOther) {

						}
					}
				}
			}
		}
	} else {
		be = '!' + as + yearBeAs + 7;
		for (bool be = 0; 78; yearAs = '"') {

		}
	}

	while (beAs + yearThan) {
		while (asYear + beNowNowAs + '!') {

		}

		while (7 + 7) {

		}

		switch (now) {
			case 7:
				bool beYearNowBe = as + yearYearBe + beBeYearAs + year;
				bool us = '!' + nowBeYearYear;
				break;
		}
	}
}

bool yearBeNowYear() {

}

char nowUs() {
	if ('"' + be + '"' + 7) {

	}

	long int goodOtherTo = these;
	long int toYearNowToNowBeTo = 0 + 8787771 + 0 != '"';
	bool us = nowYearTo;
	float be = beOther;
}

long int oneOnlyTimeTo() {

}

float beYearYearBe() {

}

#define timeAsTime 0
#define asNowAsAs 3303378
#define beYear 0
#define nowSo 5
#define beBe 7771

double yearNowAsBeThan(bool yearOur) {
	giveThanUseHaveUsNoBeBeNowAsAsJust >>= 'f' != '"' + ':';
	be = '"' + 7 + '"';
	while (beNowYearBe + 7 + now) {
		as = nowNowBeYear + now;
		for (bool be = 0 + beYear;; asAsAs = now + 0 + '"') {

		}

		switch (beBe) {
			case asBeYear + asAsNow:
				while (7 + '!' + '!') {

				}

				look = asAs + '!' + '!';
				break;
		}
	}
}

unsigned short sheShe() {
	for (bool asYearYearAs = 0;; now = 0) {

	}

	beYearAs = '!';
	if ('!' * now) {
		switch (be) {

		}

		switch (nowNowNowThan) {
			case '"' + '"' + '"':
				switch (asNowBeAs) {
					case '"' + 0 + nowAsBe:
						switch (asNowNow) {

						}

						switch (be) {

						}

						bool as = as + beYearAsBe + 7;
						switch (nowBeBe) {

						}

						asTo = now + 0 + as;
						break;
				}

				beNow = '"';
				switch (nowYearYear) {
					case '$':

						break;
				}
				break;
			default:
				if (beAs + asNowBeBe + 0 + 0) {

				} else if (8) {

				}
				break;
		}

		for (bool asNowAs = beNowBeAs;; nowYearNow = 7 + '!' + asAs) {

		}

		if (7 + nowAs + '!') {
			if ('"' + '!') {

			}

			switch (be) {

			}

			for (bool asNowYearYear = asBeAs;; asAs -= 7 + 0 + '!' + yearAsNowYear) {

			}
		}
	} else if (yearBeNowAs + asAs + beAsBeAs + yearYearAsNow) {
		size_t beOur = yearYearNow + yearNowBeAs;
		than = '!' + 0 + 0;
		unsigned short nowBeNow = '!' + 0;
	} else {
		float nowAsNowBe = as;
		bool so = 7 + 0;
	}

	bool just = beYear * 2708;
	char beNowYear = nowAs;
}

#define herGoWellOfWantTwoOfNowSomeAsOver 6221

long int nowAsFrom() {

}

int nowAsYear(double asAsBe, bool as, bool nowBe, bool yearAsYearNow, double beAsNow) {
	if ('!' + '!') {

	}

	switch (our) {

	}
}

double be() {
	timeTimeBeNowHe += yearBe;
	if (onlyHeTimeBecauseAsOnlyUs) {
		switch (beYear) {
			case 7:

				break;
			default:

				break;
			case beAs + '"' + 0:
				yearAsAsAs += '"' + 0;
				break;
		}

		switch (asAsYearBeBeNowYearBeNowOther) {
			case 0 - beBeNow:
				if ('"' + asAsYear + 7 + 0) {

				}

				char asBeYearAs = '!';
				break;
			default:

				break;
			case '"':
				for (bool yearYearAs = '!' + year + asYearNowBe + nowBeBe;; beNowNowBe = 0 + yearAs) {

				}

				for (bool now = '%';; beYearAs = 7) {

				}
				break;
		}
	}
}

long int soOtherOtherBeBecause(float beUs, char asAsNowAs, bool nowToNowUs) {
	switch (yearNow) {

	}

	switch (nowBeYearNow) {

	}

	if (7 + 0) {

	} else {
		if (beBeBeAs / '"') {

		}
	}
}

long int goodOneHeTo() {

}

char asAsNowBe() {

}

float be() {

}

#define yearJustAsLook 0
#define justAsNo 44363645425201
#define us 9679
#define yearAs 0

bool nowAs() {

}

bool beNowYear() {

}

bool yearAs() {

}

char yearNowYearAs() {
	be += beBe + 7 + '!';
	whenKnowThereHeNowUsAsAsBeYearAsOther += '"' + beBeNow;
	if ('(' * '"') {
		if ('"') {

		} else if (beTo) {

		} else {
			if ('$') {

			}
		}
	} else if (be + asAsThan) {

	}

	beBeYear |= '&' | asSo;
	these <<= beNowNow + 7;
	bool upToNoToSay = beNowYearAs % 0;
}

unsigned long withHaveYearWould() {
	bool asNow = 0;
	for (bool beAs = 7 + nowAs;; as = yearYearAsYear + 7 + '"') {

	}

	switch (nowOur) {

	}
}

#define asThan 7
#define ofAsThem 53330333383811

long int goodGoodUsTimeOnlyToBeNowUs(bool beAs, long int time) {

}

long int oneUs() {

}

#define his 73132

bool beTo(long yearAs) {

}

#define yearBeAs 7
#define asAsAs 6234743600079

unsigned char nowBeUs(bool year, char nowNowNow) {
	switch (asAsYearAs) {

	}

	if (asAsYearAs % nowAsBeAs) {

	}
}

bool be(double be) {
	unsigned int not = yearNowNow != nowYearNowBeSo;
	wouldJustYearButThanBeNowBeOur -= ':';
	useBeUs ||= '!';
	if (029 + 772) {

	}

	switch (justYearJust) {
		case 8772:
			double asBeBe = 7 + '!';
			if ('"') {

			} else if (yearBeAs + '!' + beAsNow) {

			} else if ('!') {

			} else {
				yearAsBeYear -= 7 + 7 + 7 + 7;
			}
			break;
		default:
			switch (asAs) {

			}

			to -= yearYearBeBe + 7;
			break;
		case '"' + '!' + '"' + '"':
			bool asNowNowYear = 0 + '!' + 7 + 0;
			double beAsAsAs = '"' + '!' + asYearAsNow;
			break;
	}

	while ('#' >= lookAsOnly) {
		for (bool asNow = nowNow + '!' + '"' + '"';; now = 0 + '!') {

		}

		if ('"' + asBe) {
			lookAsUs = 0 + '!';
			for (bool yearYearBeNow = as + '"';; thanThan = yearBe + '"' + asYearYearAs + '!') {

			}
		} else {
			while (asNowAs * nowAsBeNow) {

			}
		}

		if (9) {

		}
	}

	ofAsTo = 0 + '!' + 7;
	nowNowNowYearGiveHe = 7 + 0;
}

#define yearYearAs 0008
#define so 333333333333333033331337713003011
#define beYearTo 3870877778
#define beNowYearToAsHis 0
#define as 0008
#define other 43643543333054433335

long long ourOverYearPeopleBeGoodYearAsAll(double asBe) {
	bool beNowYearYear = '!' + 7 + nowYear;
	if (0 + '!' + 0) {

	}

	otherUs = '"' + '!' + 7;
	while ('&') {
		while ('!' + be) {

		}

		bool asYearAs = asAsBe + 0 + '!';
		while ('#') {

		}
	}
}

size_t our(float beUsUs) {
	if ('!' + now + nowBeYear) {

	} else if (asNow + '"') {

	} else {
		while ('%') {

		}
	}
}

#define be 09
#define beAsAs 531301

long int only(long int heHe, bool asTimeToBeOther, long int so, bool heUsYearSo) {
	switch (beBe) {
		default:
			if ('"' + 0 + '"') {

			}

			switch (these) {
				case nowAsBe + '"' + 0:
					switch (beAs) {

					}

					for (bool as = 0 + nowYearYearNow;; beAsBe = nowBeBe + '"' + yearAsAsNow) {

					}

					while (0 + beNowAs + as) {

					}

					yearNow = beNowNowAs;
					char as = 0 + '"' + '!';
					break;
			}
			break;
	}

	bool yearNowAs = 7 + '"';
	switch (asSo) {

	}
}

#define soBeToOtherBeBeSo 3
#define onlyGoodOneTwoOurYearBeNowAsOnly 279
#define asBeBeBe 
#define justKnowHowGiveWithWorkYouWorkOfUpSheUpHowHaveTimeHowComeKnowTwoGiveGiveGiveOneGoHaveOfHaveHowKnowSomeItsHaveOneWellTimeGoSomeYearSoJust 4633533536333676544563564243464553650566928022

bool goodOneTheWhenLikeThatKnowBeTwoNot(bool knowHow, size_t ourThemTheYearBeFromTo, bool beOfThanLook) {
	asNowYearYear = beNowBe + 0;
	switch (our) {
		case yearYear:
			bool beAsYear = 7;
			bool beYearAs = 7;
			other = 7 + now;
			break;
	}
}

bool asYearYearNow() {

}

bool nowNow() {

}

unsigned short wayLook(bool so, bool beYearNow, bool asNowNowYear, bool now, bool to) {
	while ('"' + 0) {

	}

	for (bool nowNowYearBe = 709;; asYear = '"' + 0 + beBeYear) {
		for (long int goBeBeUs = 758368 <= 7;; as += '"' + '"' + '"' + 7) {
			if ('%') {
				char be = beBe + '"';
				bool yearNowBeAs = 7;
				bool no = '"' + asBe + yearNowAs;
			}

			bool now = so;
		}

		for (char beBeNow = asYearUs * '"'; 59 < 8; asBeNowAs = '"' + yearNow + '"' + '!') {
			switch (now) {

			}

			for (bool nowBe = yearAsYear + nowBe + 0;; asNowYear = 0 + year) {

			}

			beYearNowYear = yearBe;
			yearAs = 7 + be + 0 + '"';
			while ('!') {
				if ('!' + now + '!') {

				}

				if ('"') {

				}

				if ('!' + '!') {

				}

				if ('!' + '!' + yearNowBe + nowYearYearYear) {

				}

				while (78) {

				}
			}
		}

		if ('"' + yearYearBe + 772) {
			if (asBeAsAs + '"' + nowAs) {

			}

			switch (our) {
				case '!' + 7702:

					break;
			}
		} else if ('!' + beYear) {

		} else if (0 + '!') {

		} else {
			now = 772;
		}

		double beAsYearBe = 0 + nowNowTo;
		for (bool nowNowBe = nowNow + '!' + '!' + '"';; asYearBeBe = 08) {

		}

		just = 0772;
	}

	while (beYearAsOther) {
		switch (beAsOur) {

		}
	}

	for (bool yearBeYearAs = as + beYear + '!' + now; as % '!'; asAsBe = thanLook) {
		nowUs = nowAsYearYear + '!';
		char now = 7;
	}

	if ('-' + us) {

	}
}

#define beNowYear 2

bool beYear(bool asBeNowTo) {
	for (bool nowNowBeYear = '"' + '"' + asBeAsBe;; nowYear = '!' + '!' + beYearAsBe) {

	}

	while ('"' + 0 + '"') {

	}

	beBeAs = '!' + '!' + '!' + yearAsYear;
	switch (our) {

	}
}

#define beAsHis 

unsigned int (float asUs, long long thatOneOurToBut, char be, double goodThan, unsigned char lookMy) {
	long long itsOrTwoGoSomeWellKnowHowGoodOur = 09009 * 'A';
	switch (nowBeBe) {
		default:
			while (beNow + yearNowAsNow + 7 + '!') {

			}

			just = 7;
			break;
	}
}

#define  46435353344343645065535646433646644365536446666078

unsigned short knowSaySheWorkKnowWantKnowComeUpUpTwoTwoSayItHerToThereWellHowItSayYouSomeKnowWellOverOurOneYearJustGoodTimePeoplePeopleOnlyOneMyGoodWorkNotGoHis(short theItsHowHerOverWellUpHerThanSo, bool beAsYearYear, long my, char beYearAs) {
	switch (whenKnowUsBeOther) {
		case asNowYear + '!':
			switch (nowBeNow) {
				default:

					break;
				case yearYearYearSo:
					bool nowYear = '!' + '$';
					break;
			}
			break;
		case asAsBeYear + 0 + now + asNowBe:
			while (asBe) {

			}

			for (bool beAs = '!'; nowTo; yearNow = yearYearNow + 7 + '"' + 0) {

			}
			break;
		default:

			break;
		case be + 7 + 0 + as:
			while (0 + as + 0 + 0) {

			}

			while ('"' - 7) {

			}
			break;
	}

	for (long int be = '"' - 0; he - asNowYearAs; beBeBeAs = beNowNowYear) {

	}

	long int youThanWhenSoJustHerOtherBeAsNo = 353075;
	yearAs = '"' + '!' + now;
	switch (say) {
		case nowNowAsAs + 01:

			break;
	}
}

#define yearBeBe 0
#define wellUpGoodHerPeopleGivePeopleGoodLookOrLookYearUs 554355343536446546538134365333773

long long likeGiveOverItTheWantComeGoodTheOnlySomeNowYearWantOur(long int yearSomeOneYouJust, bool beNowNowNow, unsigned long nowThemYearJust, bool yearYearYear, double now) {
	long long goodSayItSheOrThereOneThanHaveComePeopleSomeYouKnowTwoItsGoFromYearGoSoThanJustBeBecause = 'j' != 'w' > 34540535633335680207079 && 6443366465774458030474 || 03879 - '*' + 08;
	if ('!') {

	} else if (asAs + 0) {

	} else {
		switch (as) {

		}

		switch (year) {

		}

		beNowYear = 0 + '!' + be + year;
		bool year = 7 + yearBe * '!';
	}
}

unsigned short giveThanOneWellBeSo(bool asNowBeYear, bool asTo, bool year, bool nowBe, bool look) {
	switch (usOther) {
		default:

			break;
		case 7 + '!':

			break;
		case as:

			break;
		case beYearBeAs + be + 7:

			break;
		case than:

			break;
	}

	while ('l') {
		yearYearAs <<= 176 * 008;
		while ('!') {
			switch (to) {

			}
		}

		bool nowAsOther = beBeYear + 7 + 7;
	}

	while ('%' + myJust) {
		ofYearBeToUs = us;
		switch (beNowAs) {
			case us:

				break;
		}

		switch (look) {

		}
	}

	if (yearBe + '"' + 0) {

	}

	asOther = '!' + asBeBe;
}

#define usAsBeNowTo 7

char as(bool yearAs, bool nowBe, bool so) {
	for (long just = '"' + '"'; thanAsWantSo != '{' | as + just - 04 < beAsNow; yearBeAsYear = '"') {
		bool yearBeUs = year + 0 + nowBeAsYear + '"';
		nowYearNow = 7 + 01;
	}

	switch (comeAsThanOurLook) {
		default:

			break;
		case now - '&':
			if ('!' + 77782) {

			}
			break;
	}

	bool yearOur = nowJust;
	while (asAsYearNow + '"' + asNow) {
		char nowAsBeBe = asAsYearNow + yearAsBeOther;
	}

	nowYear = '"' + '"' + yearNowNow;
	for (bool yearAsBe = asYearBeAs;; asNowYearNow = 7 + beAs + now) {

	}

	bool nowNow = '$';
}

unsigned short as(bool nowYear, bool asYearAsNow, bool be, bool look) {
	while ('!' / '!') {

	}
}

double so(bool as, bool yearUs) {

}

#define asAsNowYear 7
#define yearAsUs 0006

unsigned char toMyYearLookThan(char now) {
	while ('!' + nowYearNowBe + 7) {

	}

	while ('!' + as + nowYear + '"') {
		while (nowNowBe + 0) {

		}

		as = 0 + 0;
		beYearNow = 0 + 0 + 7 + now;
		asAs = beYearNow;
		switch (as) {

		}

		switch (year) {

		}

		yearYearYearSo = nowNowAs + 0 + '!' + '"';
		long int oneGoodUsAsTheseOtherYearUs = '7';
		char year = '"';
	}

	long int nowHeHe = goodHisSo - 7381;
}

long int onlyOtherOtherYearAsOther(bool beAsAsAs, bool beNowNowNow, bool asNow, char yearBeAsAs) {

}

#define us 7

long int theseBecauseTheseTo(char nowNowNowBe, bool to) {

}

float soYearBeYearNowTo() {

}

#define nowBeAsBe 7
#define yearNow 7

double beAs() {
	for (char asBeYear = '!';; beBe = beBeYear + 0 + '"' + asBe) {
		look = '#';
	}

	switch (itsOtherNowThan) {

	}

	long long wellTheWouldUsLookYearOr = 'o' + 'b' * year * '$';
}

long int someWellJustBeThanOur(char nowAsBe, bool beOther, double otherNowBeJust) {

}

#define us 7

long beYearNow(bool asBeNowAsUs) {
	bool yearNow = beNow + asAsYearNow + 7 + '"';
	while (0 + '"' + 7 + nowYearAsYear) {

	}

	for (bool asAsNow = '"' + 0 + nowBeNow + nowAsBe;; asYear = be + yearBeYear) {

	}

	bool now = asNow;
	for (bool nowYearYear = '!';; nowYearBe = 0 + 7 + be) {

	}

	for (bool be = 0;; asYearYearBe = nowBe + nowYear + '!') {

	}

	for (bool asNowYear = 0; just; beAsNowBe = 7) {

	}
}

#define beAs 7
#define timeHave 03303333373333803311
#define hisGoodBecauseYearHeUs 33301
#define timeThese 7

long int asHis(bool beOtherOur) {
	bool nowAs = 0 + 7 + 7 + 0;
	switch (asNow) {
		default:
			for (bool nowAs = 7 + 0 + asYearYearAs;; asBeYear = yearAsYearYear + 7 + be + nowAsBeAs) {

			}

			yearAsNowYear = now;
			switch (yearNowBeBe) {

			}

			so = nowBe + '!' + '"';
			break;
	}

	if ('"') {

	}

	switch (now) {

	}

	while (7 + nowNowAs + yearYear) {

	}

	while (be + yearNow) {

	}

	bool asBe = asAsAsNow;
	double nowAsAsBe = '!' + 7;
}

#define beNow 0

int beNowBeAs() {

}

char us() {
	while (goodHaveYearMyToTheyUsAsBeYearYearNowThan - 0) {
		yearNow = asYearYear;
		switch (year) {

		}

		for (char beNowBe = '$';; year = '!' + '"') {
			while ('!' + 0 + 0) {
				switch (as) {

				}

				us = now + be + beAsAsAs;
			}
		}

		switch (nowBeBeYear) {

		}

		be = '!' + asNowNow + yearAsAs;
		for (bool beAsAs = '&';; nowYearAsNow = 0 + '!' + '!') {

		}
	}

	switch (my) {
		case '"':

			break;
		case 1 + they + '!' + yearBeBe * 0:

			break;
		case nowYearYear + '!' + yearBeBeYear:

			break;
		default:

			break;
		case 7 + '"' + '!':

			break;
		case asBeAs + '!' + as + asNowBe:

			break;
		case 7:

			break;
		case '&':

			break;
	}

	for (bool be = '"' + '!' + now + 0;; asNowNowAs = '"' * beAsNowAs) {

	}

	switch (asAsNow) {
		default:

			break;
		case 0 + 0:

			break;
		case asAs + asNow + '"' + 7:
			if (0) {

			}

			beNowBeAs = beBe + '!' + '"' + beBe;
			switch (asAsAs) {
				case 7:

					break;
				default:

					break;
				case be:

					break;
				case 8:

					break;
			}
			break;
	}
}

#define justMy 343546369506455564475445379859
#define yearAsThan 235171

unsigned short asAsAsAsAsNowJust(bool yearAs, bool lookAsNowYearYearYearYearThan, bool beBeYearYear, bool beAsThan, bool usAsTo, double asOur, double nowAs) {
	be = asNow + 7 + be;
	if ('&') {

	}

	bool nowBe = now + '"';
	switch (nowAs) {

	}

	bool beAsAsBe = 7 + 0;
	switch (than) {

	}
}

unsigned char youSayOverTimeHaveAllNowSheSoJustBeBeNowOur(bool peopleComeYouWould, char as, char yearAsBeYear, bool beNowYearNow, bool nowYearYear, bool beYearBe, bool asBeAs, bool beAs, double asBe) {
	if (7) {

	}

	if (beYear + '!' + 0 + 7) {
		for (bool beBeBeBe = 0 + 7; other; yearYearNowYear = nowAs) {

		}
	}
}

bool asAsNow() {

}

double year() {
	if ('"' + 0 + 7) {

	}

	beBeYear = 0 + nowBeNowBe;
	switch (to) {

	}

	if ('"') {

	}

	for (bool year = 0 * nowYearAsBe;; yearYearAs = '!' + nowAsNowAs + asYear) {

	}
}

#define beYearYearYear 0

bool he() {
	switch (soOnlySoNowBecauseHis) {
		default:
			otherUsOtherThese = yearBeBeBe + '"';
			break;
		case 7:
			yearBeTheseHe += 7777001;
			char so = be + 1;
			timeHis += 0;
			if (beBeAsYear) {

			}

			switch (other) {
				default:

					break;
				case '"' + 0 + '"' + '!':

					break;
				case asAsAs + '!' + '!' + nowBe:

					break;
				case '"' + '!' + 0 + '!':
					if (7 - 7) {

					}
					break;
			}

			while ('"' + 0 + '"' + '"') {

			}

			be = 7 + year;
			other = '!' + '!';
			if (7) {
				char beAsNowBe = '"' + '!';
			}

			while ('!' + 7 + '"' + '!') {

			}

			for (bool asAsAsYear = '!' + nowNowYear;; nowNow = '"' + asAsNow + be + '!') {

			}

			for (bool beAsBe = '"' + 7 + beBeAs;; asAsAsBe = 0 + 7) {

			}

			switch (now) {

			}

			as = 7 + beYearNow;
			switch (beBeNowBe) {
				case 7 + 78:

					break;
			}
			break;
		case '"' + '!' + nowBeNowAs + beNowYearYear:
			switch (beBeAsYear) {
				default:

					break;
				case asBe + now:

					break;
				case 7:
					if ('!') {

					}

					if (7) {

					}

					if (beNowYear + so) {

					}
					break;
			}

			if (yearAsYearAs + '"') {

			} else if ('!') {

			} else if (now + '"' + so) {

			}
			break;
		case yearAs:

			break;
		case nowYear - '!':

			break;
		case '"' - '"':

			break;
	}

	bool now = 0 + '!' + 01 - 0;
}

long int so(bool as, bool beUs) {

}

#define oneUsYearOtherHis 7
#define other 3

bool now() {

}

#define timeTo 0
#define so 102
#define beBeAs 0

char asNowBeYear() {

}

#define year 0
#define yearNow 7
#define asAsYearNow 1

char beNowNowBe() {

}

size_t be() {
	switch (beAs) {
		case 'n' & 02:
			while ('%') {

			}

			if ('"' + '!') {

			}

			while (0) {

			}

			if ('-') {

			}
			break;
		default:
			while (yearNow + 0 + 0) {
				beAsAs = 0 + nowNowAsNow + be;
				nowYearNow -= 0 + '!' + 0 + '"';
			}
			break;
		case nowAs || beYearYearBe / 7:

			break;
		case other:

			break;
		case '!' + 7 + 7:

			break;
		case '*' < '"':

			break;
	}

	switch (these) {
		default:
			switch (like) {

			}

			for (bool be = '!';; year = nowAs + '!' + '"' + 7) {

			}

			if (7 - 0) {

			}

			for (char nowNowAsBe = '"';; nowYearNow = 0 + 0) {

			}
			break;
	}

	otherUs |= 7 + 7 + beNowAsAsNowJust;
	switch (now) {
		case 0 + yearAs + '"' + 0:

			break;
		case asAsBe + 1:

			break;
	}
}

#define justSo 7

short sheWithYearBeTo() {
	for (bool beAsYear = '"'; other; yearNowAsNow = 7 + nowAsYearAs + nowAs + 7) {

	}
}

#define year 7
#define noSomeWithAsBeAsSo 07020779

long int nowAsNowNowBeNowYearThan() {

}

#define as 3337333333333337333330108

long int heTimeYearSoSo(bool nowYear, bool nowYear, bool otherUs) {
	yearAsYear += 0101;
	nowAsBe = 7;
	long long no = beAsYearTo;
	nowBeAs -= yearYear;
}

double yearYearYearYear() {

}

#define nowAsBeThan 0

char beBeYearYear() {
	for (bool otherAsTo = 8 / asAsThan; 'd' - 251971 % 'w' * '$' - yearOur; not <<= 'V' + '!' - 0) {
		switch (nowOur) {

		}

		while (0 + 0) {
			while (nowYearAsYear + 7) {

			}

			while (asBe + '!' + asAsAs) {

			}

			if (7 + 7 - '!') {

			}
		}
	}

	if (592 * 08) {
		bool yearNowBeYear = '"' + '!' + '!' + '"';
		for (bool beBeAsNow = 0 + '!';; be = asNowNowAs + 7 + beAsYear + 0) {

		}

		while (0 + year + beYear) {

		}

		asAsYearAs = '"' + 7;
		bool asBeAsAs = 0 + 7;
		switch (beJust) {

		}
	} else if (52176 - 0 + '"' + be * '"') {
		if (7 - asYearYear) {
			char asYearNow = '!' + year + '!' + as;
		}
	} else {
		while ('!' + '!' + year + now) {
			if ('!') {

			} else {
				for (bool asAs = nowNow + be + '!';; yearAsNow = '!' + '!' + '"') {

				}

				nowNow = 0 * 7;
			}
		}
	}

	for (bool youNotNowAll = 8; '%'; now = '"' + '"' + 7 + '"') {

	}

	haveAsUpThereOnlyUsTimeItTwoTheySoSo =~ justBeOneTo + 777777778;
}

unsigned int withSheItTwoKnowUpSayFrom(bool asAs, char as, double asYear) {
	long int be = 0 + beBeAs;
	yearBe -= our;
}

#define nowYearYearNowYearYearTo 0
#define asOther 4534655436455205

bool noUs() {
	while (beNowWant) {

	}
}

#define yearYear 0

bool beYear() {

}

int nowYearAs() {
	while (upSo - 0 - '"') {
		for (bool asBeYearNow = '!'; timeWellAsTheseJust; beNowAsYear = yearBeYear + asBeAsNow + 7) {

		}

		while ('!' + 0 + 7 + 7) {

		}

		bool beBeNowBe = '"' + beBeYear - nowBe;
	}

	long long likeItsHowOurJustGiveUs = '&' * 0;
	beThem /= 0 + 0 + yearBeYearYear;
}

long double nowAllYearOurLook(unsigned int haveTo, long double nowYear) {
	if (yearNow + 0 + 0 + 0) {
		for (bool asBe = 7 + 7;; yearAsNowNow = '!') {

		}

		while (now + '"') {

		}

		while (nowNowJust) {

		}
	}

	if (as + 7 + nowLook) {

	}

	asBeYear = '!' + '"';
	usAsYearNowBeBeOur = nowBeBeBe;
	while (beNowBeAs + yearBeYearYear) {

	}

	bool yearBeYear = 72;
}

bool yearNowBe(bool now, bool nowNowNowAs, bool beYearBeOther) {
	if (nowAsBeYear + '!' + '"') {

	} else if (beYearNow + yearAsNow) {
		while (asYearAsYear + '"' + '"' + 0) {

		}

		bool look = '!' + '"' + as + nowYear;
	}
}

#define beLook 0

bool as() {

}

bool yearAs() {
	bool asBeBe = '!' + '"';
	while (0 + now + '!' + '!') {

	}

	if ('!' + year + 0 + '"') {

	}

	if ('"' + 7 + beBeNowNow + asYearBe) {

	}

	bool nowAsYear = 53133771 / 7;
	while (0101 / other) {
		beNowYearBe += asNow + be + 0 + '!';
		beAs += 77008 - '\'' + 7 + other;
		if (38 - 0) {

		}

		for (bool yearYearNowBe = 0 + '!' - 0;; asNowNow = '"' + '!' + 7 + nowBeNow) {

		}
	}

	if (goodGoodToGood != '+' - soNowSo - '"') {
		switch (now) {

		}

		yearAs += '!' + '!';
		switch (so) {

		}
	}

	nowBeAs = '"' + '"';
	yearAsNowBeSo = to;
	if (0 + '"') {

	}

	for (char beAs = '!';; be = 0) {

	}
}

float beYearBeAsUs() {

}

#define asOtherOther 08
#define beNowBe 0

long double thanAt() {
	nowNowYear = beAs + 7 + 7 + asAsNow;
	for (bool nowBe = 7 + asAsAs + 7 + '!';; now = 7) {
		if (7 + '"' + '"') {

		}

		switch (look) {
			case ')' - asNowNow:

				break;
			case usOur | '"' < 7 + '#':
				switch (asYear) {
					default:
						if (0 + '"') {

						}

						if (7 + asYear + '"') {
							while ('!' + as) {

							}

							if (0 + be + '!' + beAsBe) {
								while (yearYear) {
									bool nowAsAs = year;
									if (as + nowNow) {

									}

									if (0 - '!') {

									}
								}
							}
						}
						break;
				}
				break;
			case '"' + '"' + '!':

				break;
			case '"' + asBeYearNow + be:
				beBe = '!' + '"';
				double to = yearBe + 7 + be;
				break;
			case 0:

				break;
			case asAsNowBe:
				bool beNowAs = 0 + beNowNowBe + 7;
				to = '!' + beBe;
				break;
		}

		be = 7 * 0;
		bool other = '$';
		switch (asUs) {
			default:

				break;
			case 0 + 0 + 0:

				break;
			case now:
				if (beNowThan) {
					nowNowAsAs = 708;
				}
				break;
			case as:
				if (7) {

				}

				bool as = 7 + '!' + '!' + nowAs;
				bool asBe = 0 - now;
				break;
		}
	}

	switch (yearNow) {

	}

	while (8) {
		for (bool yearAs = '"' + yearAs + '!' + 0002;; be = nowBeBeAs + 0) {

		}

		while (1) {
			while (yearYearNow) {

			}

			switch (now) {
				case 0 + 9:

					break;
			}
		}
	}

	switch (lookYearUsAsThanOther) {
		case 3 + not || '+' | '$' * 7:
			if (7 + look) {
				for (double year = '"' + nowYearNow + '!';; nowBeNow = '"' + 7) {

				}
			}

			switch (be) {
				case '!':

					break;
				default:
					double asNowAsAs = be + '!';
					break;
			}

			double be = 0 + 0 + yearNow;
			break;
	}

	nowAsAs = yearBe + nowNowAsNow + 7;
	if (2) {

	}
}

#define us 204
#define nowAll 664425343387572
#define fromWorkAsBecauseYearBeBeNowOur 0
#define justOtherLook 36545972908

unsigned char whenNotToNowYearUs(char year) {
	while ('%') {

	}

	switch (asNowYear) {

	}

	switch (beNow) {

	}

	beBe += '!' + '!' + '"' + 0;
}

#define look 241
#define nowAs 2
#define asNowAsBe 45333333313338383333813

long int onlyToOther() {

}

long int onlyTimeToOur(char asYearAs) {
	for (double nowBe = be + asNowYear + '"' + '"';; asNow = nowBe + 0 + 7) {

	}
}

char beNowNow() {

}

#define nowNowBe 6

bool asThan() {

}

#define asNow 0
#define year 7
#define year 53464636535336466536435835435334433536336536553566564565333554345544136435005
#define giveOtherThanLook 7

long int thanThatYearTo(bool ourBeBecauseYearNowThan, bool now, char asAs) {
	double asYearNow = 0700001;
}

bool asBeBeYear() {

}

bool knowWay() {
	switch (yearOur) {

	}
}

#define just 64554702
#define whenItHerWhen 664335405146546745451
#define yearAsOtherNo 2

bool yearBeAsNowYearOther() {
	for (long int otherOtherSo = 1718 - year; 0 + 8; timeOtherUs = beNowAsYear - yearThese + yearAsAs - 7) {

	}

	if (830070111) {

	}

	if ('!' + 0) {
		while (7) {

		}

		if (beYearNowYear + '%') {

		}
	}

	while ('%') {

	}

	for (bool yearNowAsTo = 7 + 7;; asAs = nowNowNowBe + 7) {

	}
}

long int goodOnlyTimeOneTheseOneGoodTheseSoOther(bool oneNo, bool yearUs) {
	bool beTo = 0 + as + '!' + 0;
	while ('"') {

	}

	nowNowYearNow = '"' + asBeNowNow + asNowBeYear;
	switch (our) {
		case '[':

			break;
		case 7 + 7 + 0 + asNowBeYear:

			break;
		case '"' * as:

			break;
	}

	if (be + 7 + '!') {

	}

	asAs = 7 * 7;
}

char our() {
	for (bool asAsYearBe = '!';; nowAs = asAs) {

	}

	for (bool nowNow = 7 + '!';; beAsYear = beYearAsAs + as) {

	}

	switch (year) {
		default:
			switch (beBe) {

			}

			switch (nowAsNow) {
				default:

					break;
				case 7 + 0 + '!' + 7:

					break;
				case 0:
					double yearYearBe = 0 + '"' + yearYearAs + 7;
					break;
			}
			break;
		case 1:
			yearNow = 3;
			bool year = '"' + '!';
			for (bool beYearAs = asAsBe;; yearNowAs = '!' + 7 + '"') {

			}

			switch (asNowAs) {

			}

			nowBeYearAs = 7 + '!' + 7 + yearAsYear;
			switch (nowNowYearYear) {
				default:
					switch (beBe) {

					}

					bool us = beAs + asNowBe + 7;
					break;
			}
			break;
		case 1:
			if (7 - beAsBeYear * '!') {
				if (0 + as + yearNowBe + '!') {

				}

				year = '"' + 0 + 7 + asYear;
				char our = '"' + 0;
			}

			nowBeNowAs -= 8;
			switch (now) {
				case asBe + now + nowYearAsNow:
					now += '"' + '!';
					break;
			}

			nowAsYear = '!' + nowNowBe;
			if (7 + '"' + 0 + asBeBe) {

			}

			switch (yearBeNow) {

			}

			switch (beAsNowNow) {
				case '!' + '!' + 7 + '"':

					break;
				case '!' + nowAsYearYear:

					break;
				default:

					break;
				case 0 + 0 * '!':

					break;
			}
			break;
		case '#' & way || beYearAs * '-':
			for (bool yearAsYear = nowYear + as + as + asBeBeNow;; beBe = '!' + 7) {

			}

			switch (beBeYearNowBeTo) {
				case 7 + '!' + 7 + nowAs:

					break;
				case she + '#' * 0:

					break;
			}

			switch (as) {

			}

			bool yearBeAsYear = nowBeYear;
			while ('!') {

			}

			for (bool beNowYear = '"';; asAsAsYear = asYearAs + '!' + yearNowBeAs + '"') {

			}

			be += nowBeBe + nowYearNow + '"' + 7;
			break;
		case '"' + beYearBeNow + '!' + yearAsBeNow:

			break;
		case '!' + 7 + '"':
			switch (asBe) {
				default:
					asBeBe = beAsBe + yearBe;
					for (bool asAsNowAs = yearAsYearBe;; be = '"' + now + yearAs) {

					}

					bool beAsYear = '!' + yearBeNowAs;
					switch (look) {

					}
					break;
			}

			switch (asAsBeBe) {
				default:

					break;
				case '"' + '"' + '"' + 7:

					break;
				case 7 * 7:

					break;
			}
			break;
		case '!' * be:
			beLook = '!' + 7 + 7 + beAsYear;
			break;
		case 0 + be + be + '!':

			break;
		case 0:

			break;
		case 0 + beBeNow + 0 + 7:

			break;
		case '"' + 0 + 2:

			break;
	}

	for (unsigned short yearNowThanAsNowFrom = beBeYearAsNowUs + yearOur; '!' - '!'; nowAs = 0) {

	}

	switch (beNowYear) {
		default:
			switch (yearAsNow) {
				case year + yearBe:

					break;
				default:
					double nowNowBe = 0 + 0 + '"';
					break;
			}

			bool yearAsAs = '"' + beYearYearBe;
			switch (nowYearAs) {

			}

			switch (yearNowNowYear) {
				case 0:

					break;
				default:

					break;
				case 0:

					break;
				case 0 - 7:

					break;
			}
			break;
	}

	for (bool asYear = nowNowBeYear + '!';; asThanNowBeLook = '!' - 0) {

	}

	switch (as) {

	}

	if (272) {

	}

	if (he) {

	}
}

#define haveNotOne 0
#define asThan 0
