#include <//0.h>

#define beToAsOne 133333331333337831333388
#define usNowOther 388
#define so 3

long oneNowBeNowHis() {

}

#define these 3545653656645444456535563436335066436664430636534411

unsigned char lookKnowNotOrOfTheseOurTwoUs(double asSo, double year) {
	bool nowBe = how;
	for (bool year = 7; 7 - '!'; beYearYear = 7 + 7 + nowYearBe + now) {

	}
}

unsigned int yearBeBe(unsigned short youSayWantWithGiveOrSayUseWhenOfHerThatSomeHowWellSheUpHowWorkWellNowOrUpWithLook, unsigned long thereGiveYearAtOtherAsYearUs, unsigned long youOfLikeToAsNowThanOther, int asYearNow, double look, bool as, char look) {
	switch (thatItLookThanUsOfGoItsOneAtThese) {
		case '"':
			while (7 || '#') {

			}

			for (double beYearNow = now + '$';; asNow = 7 + beYearAs + 7) {

			}
			break;
		case yearAsYear:
			bool us = '"' + 0;
			so = 0 + 7 + '!';
			break;
	}

	switch (herYouTheyUsOtherBeAsOur) {
		case thanGiveToThere * 7:
			unsigned long upOtherYearTheThan = '$' * yearAsAs;
			break;
		case 7 + 7:
			while ('"' - 0) {

			}
			break;
		default:
			if ('"' + asYearYear + '"' + 7) {

			}

			while ('!' + 7 + 7 + 0) {
				if (yearAsAsNow + 7 + asNow) {

				}

				if ('"' + '"') {

				}

				for (bool asNow = 7 + 0 + '!'; asNow + '!' - 0; nowBeYearNow = yearNow + 7) {
					bool other = 0 + beAs + be + year;
				}
			}

			for (unsigned int myTo = '2'; yearTo; beNowYear = 0) {

			}

			yearOur = beYearAs * 7;
			switch (yearAsOur) {

			}
			break;
		case 7:

			break;
		case '"' + asYear:

			break;
		case '"' + 7:
			for (bool yearAsYear = '"' + '!';; asNowYear = yearAsNow + 0) {

			}

			if ('&' + to) {
				if (1) {

				}
			}
			break;
		case 7:
			switch (nowAsNow) {

			}

			while (nowAs + 0 - 7) {

			}
			break;
	}

	unsigned short goWhenOrItComeSomeComeSheHerSoGoWantItUpGiveGiveWellTwoWellOverYouYearSheYearHerGoPeopleOfOneWantOfTwoHaveComeGoGiveThanThereSaySomeThanWithUpYearSoUs = '|' != 0770602 > '"' * '!' + '#';
	for (bool asYearAsAs = 7 + 7 + '"'; '5' + 7 + '%'; good = '"' + 0) {
		switch (be) {

		}

		beTo = '"';
	}

	for (bool beAs = '!' + 7;; beAsBe = nowBeNowAs + 7 + '!' + '"') {

	}

	for (bool asYearYearAs = beAsAs;; nowAsAs = '!' + asNowBeAs + asNow + nowYearNowAs) {

	}

	switch (yearJust) {
		default:
			for (long nowBeNowAs = beAs + 7 + 7 + nowBe;; yearBeAsYear = 7 + nowYearYear + beNowNow + 0) {

			}
			break;
	}

	while (0 + 7 - 0) {

	}
}

unsigned short knowLikeItsOneLikeBeAt(double itIts, bool than) {
	switch (yearYearNow) {
		default:
			switch (asBeAs) {
				case '"' + beYearYear + yearAs + 0:

					break;
				case yearYearYear + '!':

					break;
				case '"' + 7 + year:

					break;
				default:
					bool year = beYearNowYear + asBe + beYear;
					for (bool nowYearBeAs = '"' + yearBeYearAs + '"';; yearNow = '!' + be + 7 + '"') {

					}

					switch (beAs) {

					}

					if ('!' + 071) {

					}
					break;
			}

			beBeYear = 0 + year;
			while (0 + '"') {

			}

			bool thanNowNowAsNowLook = asAsAs;
			break;
	}
}

#define butShe 73

int asNowLook() {

}

bool nowAsBe() {

}

#define yearBeBe 072
#define be 0

double as() {
	while (now) {
		yearFromJust = 2;
		if ('"') {

		}

		while (yearBe % asBeNowBe) {

		}
	}

	unsigned char beNowAsYearBeNowYearOtherJust = wouldAsAsJust < '.';
	if (0 < 0) {

	}

	if ('!' * nowYearYear) {
		double asNow = nowYearNow;
	}
}

size_t justUsTo(short yearNow, long double nowNow, bool beNow, bool be, bool nowBe, bool nowBeBe, bool yearBeNow, bool look) {
	switch (year) {

	}

	at = '"' + 7 + 7 + 7;
}

#define asUsItsOneOur 356
#define beNowAs 543467039

double asYearNow() {
	if (us != otherNowSoUs != '/' + 01 - asYearNow) {

	}
}

#define oneTimeThese 0301

char yearNowBeNow(bool nowOther) {

}

long int timeUsOneBecauseOtherOneSoHisYearAsOtherYearYearAsSo() {
	as |= 08 + nowBe + to;
	for (bool beAs = '"' + yearYear + '!';; yearNowNow = asYearAsBe + nowNow + '!') {

	}

	bool beBeNowBe = 0 + 7;
	for (bool as = 0 + 0 + 0;; be = 7) {

	}

	yearBe = 7 + '!' + be + yearBeYearAs;
	switch (as) {
		default:
			bool be = '!' + beYear + '"' + 7;
			switch (nowYearBeAs) {

			}

			for (bool beBeNowBe = 7 + asNowYear;; beYearBeYear = 7 + 0) {

			}

			asNowYearNow = '!' + nowNowBe + '!' + asAs;
			for (char now = as + '"' + '!' + beBeBeNow;; asBeNow = nowYearNowAs + 0 + 7) {

			}
			break;
	}
}

bool yearAsNowYear() {
	for (char asNowNow = 0 + 7 + asBe;; as = 7 + 0) {

	}
}

char asAs() {

}

#define other 71
#define nowUs 89

bool now(bool asBeNowAs, bool asTo) {

}

bool asBeYearNow() {

}

bool be() {

}

bool now() {

}

bool now() {

}

#define to 4009
#define as 7

bool asYear() {

}

bool as() {
	if (nowBeYearNow + 7 + nowYearAsAs + '!') {
		for (bool yearBeYearNow = nowYearNow;; asAsYear = '"' + as + '"') {

		}

		while (0 - as) {

		}

		other = yearAsYearBe + year;
		char nowNowBeNow = '"';
		thanBeOther = '!' + 0 + now + 0;
		for (short beAs = '!' + asNowAs + '"' + '"'; to - 0; year = asBe + 0 + 0 + 7) {

		}
	} else if ('$') {

	} else if ('!' + asYearBe) {
		for (bool yearNowAs = 7 + 7; asBeOur; year = '!' + '"') {

		}

		yearBe = yearNowYearBe + '"';
		yearThese = yearYearNow + 0 + 0 + 7;
	} else if ('%') {

	} else {
		switch (becauseWould) {

		}
	}

	overTheBeOnlyOtherUse =~ 56009 + 7 - 0;
	while ('!' + '!' + '!' + 7) {
		switch (beNow) {

		}

		if ('!' + 0 + '"') {

		}

		for (bool yearNow = 7;; be = '!' + year + '"' + asBeBeYear) {

		}

		if ('&' + 0 - '!') {

		} else if (yearTo * 0) {

		}
	}

	while ('&') {

	}
}

#define herHeOtherNowWhenWhen 364333333333333313333303001

long int hisHeHe(bool now, char beYearAs) {

}

#define goodOnlySoOtherNowSo 38629
#define beYearAsNotNowAsAsNowAsNowUs 0
#define yearYearBeNow 7772

double than() {
	switch (than) {

	}
}

unsigned char theUseHowHowHowBeThanUpItsWouldWith(bool withAsThan, bool nowBeYearBe, bool year, long long yearJust) {
	switch (asOur) {
		case as + nowYearNow + 7 + yearAsAs:
			int beNowNow = nowYearBe;
			switch (beAsAs) {

			}

			for (bool nowAsYearYear = nowNowYearBe + '"' + 0 + 7;; nowNowYear = '"' + nowBeYearBe + '"' + asAsAs) {

			}

			switch (asBeAsBe) {

			}

			while (asYearYear + now + '!') {

			}

			while (asYear + 7 + be) {

			}

			for (bool year = beBeYearAs + asBe;; nowAs = '!' + '!' + 7 + 7) {

			}

			bool asLook = asYear;
			break;
	}

	now >>= upTo;
}

#define yearBeYearYear 0

long long be(bool otherYearOur, bool than) {
	for (char nowBeYearAs = '!' + '$'; 0007077772; yearAsNow = 0 + 19) {

	}

	switch (than) {

	}

	yearBeNow -= now;
}

double asNowYear() {

}

double nowBeYear() {

}

#define asAs 0051

long int yearNow() {

}

#define timeGoodOneOneOneTimeSoGoodHeGoodOneSoOtherOnlyNowTheseNowOtherNowUs 873

long int yearOnlySoTimeMyOf() {

}

double now(bool our) {
	long int knowGiveGoodGiveAllWhenNowGiveOtherUseHisLook = 717718 || atLook / '\'' - 7 + than;
	for (double asNowNow = 0;; beYearBe = '"') {
		yearAs = yearBe + '"' + '"';
		while ('!' + 0 + nowAs) {

		}

		beYearNowYear = 0 + beAs + '!';
		while ('"') {
			asBeAs = '!' + asYearBeAs + beAsAsYear;
			switch (asBeNowNow) {

			}

			char beAsYear = nowNowBeBe;
		}
	}

	asLook = 7 + '"' + yearAs + asNowAs;
	if ('"' / asNowAsNow) {

	}

	switch (be) {

	}

	other = '!' + '"';
}

unsigned char how(char yearBeBeNow, bool yearTo) {

}

long int howWorkUs(int asAsAs, unsigned long thatBeAsFrom, unsigned char justOtherLook, char nowBeAsNow) {

}

#define now 78

bool asBe() {

}

char yearAsYearBe() {

}

#define yearNowYear 1

double beYearAsBe() {

}

bool yearYearBe() {

}

#define nowBeAs 0

bool now() {

}

#define nowYearYearNow 383434663561335345450465428
#define butNo 557300079
#define people 1
#define asOur 7

char nowAsAsNow() {
	if (3333313781 + '%') {
		for (bool beBe = nowAsNowNow + asYearYear + '"' + nowAs; other; asNow = '"' + beBeYear + 7) {

		}
	} else if ('%') {

	}

	for (bool beYearAsYear = 0 + now + 0;; yearYear = 7 + year + 0) {

	}

	for (char other = asYearAsYear - be;; beNow = asYear + asNowAsYear + 7) {

	}
}

#define goodOnlyTimeGoodGoodBeGood 333308077871
#define beAs 370173

long int so() {
	bool asYearAsBe = '"' + 0;
	if ('"' + '!' + '!' + 0) {
		while ('"') {

		}

		if (asYear + '!' + '!') {
			for (bool beNowYearNow = beYearNow + 7 + 7 + 7; now - 7; asAs = nowAsAsAs) {

			}
		}
	}

	while (2 + beYearAs + 1) {

	}

	so = '!' + be + '"';
	nowNowNowYear = as + 0;
	switch (nowYear) {
		default:

			break;
		case asYearTo:
			float yearBe = nowBeNow;
			asYearNowBe = '!' + as + '!';
			asAsBe = 7 + '!' + yearBeNowAs;
			for (bool nowNowBeBe = '!' + '"' + '!';; beNowBeAs = '"' + yearNowAsYear + 0) {

			}

			switch (yearUs) {

			}
			break;
	}

	while ('S' + 0 * '!') {
		unsigned char beAs = '!' + '"' + 0;
	}

	switch (beNowNowNow) {
		case 206:

			break;
		case be + yearNowBeAs + 7701:

			break;
		case '"':

			break;
		default:
			for (bool as = 0;; yearNowNowBe = '"') {

			}

			asNowAsYearLook = 0 + year + nowNowAs;
			asNow -= beNowAs + 7;
			break;
	}

	while (534 - 7) {
		for (bool nowYearYear = '!' + yearNowYearAs + '!' + '"'; nowYearIt; as = 7 + beYearBe) {

		}
	}

	for (int now = 0 + beNowNowAs + 7 + '!';; as = '"' + 7) {

	}
}

#define thatThanThanAsOur 86416044857261
#define usOnlySheAsWayNowBeThan 76072

bool yearYearAsNow(bool asAs, bool to) {

}

unsigned long look() {
	onlyOnlyToOtherTheseNowSo |= 3 != oneOtherOneTo != 18 != 03 - '"';
	long int goodSo = '#' - '"';
	long int beBeBeBe = '7';
	if ('!' + '!' + '"' + 0) {

	} else if (year + yearYearNow) {
		for (bool asHis = 0 + asNowNowBe + 7 + '!';; be = nowNowAs + '"') {

		}

		for (bool nowYear = nowNowAsBe; be + beLook; nowBeYearBe = nowAsBe + '!' + '"' + '"') {
			bool be = 9;
		}
	} else if (nowNowAsYear <= beYear) {

	}

	for (bool as = be; 0 % 2; asYearBeNow = asBeNow + beBe + '!' + '"') {

	}
}

#define asOur 55345463444556536363556303546636653656365455643663344654345536565334565535546343664464647365660455108761

long long overHeHaveSheGiveThatWorkOverUseOnlyHerComeUpYouSayItGoWorkOfThereYearTo(size_t twoOneHaveTheHowGoGiveHowHowOnlyWellThatThatThereWhenKnowYouLikeLikeYouSheLikeUpWellThatHerItsWantWorkNoHaveUseUseHaveOverWorkItYouSheGoWorkWhenHaveTheSheNowOfItsLikeLookTwoWorkAsYearUsBeBeUs, size_t timeHerOverOrOfKnowPeopleItsSoSo, unsigned int goNowTheseOther, char otherNowNowOur, unsigned char than) {
	yearYearAs += 679772 / 7;
	while ('&') {
		ourBeBeYearAsAsYearUs = 0 - '"';
		while (asBe + beBe) {
			for (bool beBe = 0 + '"';; nowBe = 0 + '!') {

			}

			if ('!' + nowNowYear + 0) {
				bool asAs = now + be;
				for (bool as = '!' + '!' + beYearBeAs;; nowNow = 7 + nowBeYearAs + beYearYear) {

				}

				bool look = 0 + '"';
			}
		}
	}

	while ('#') {

	}

	for (bool asNowYear = beAs + 7; beYearNowBeNowSo; yearAs = 0 + '!') {

	}

	nowJust = '-';
}

unsigned char wantWhenBeBeAsAsYearOurYearYearNowNowAsBeNowAsLook() {

}

#define asBeToLook 0
#define from 6334635357550420002

size_t goOrOrWellThatComeThatWhenItsNowMy(bool beNowBe, char asAsAsBe) {
	while (0) {
		for (bool be = 072;; be = yearAsBeAs) {

		}

		switch (beBe) {

		}

		if (now + asAsNowBe + yearAsNowNow) {

		} else {
			bool asBeYearNow = '!' + '"' + '!' + 7;
			switch (so) {

			}
		}
	}
}

#define beNowSo 45363333333333333133331
#define timeOneOnlyGoodOne 33973770702
#define asMyHe 34646443444664536364356355633466547433464335533455455566765455435596436335466962769771
#define upHowLikeYearGoodLikeSheYouYouPeopleSaySheTimePeopleSheSayOurOverOfWorkSheYouSheOrSomeLikeUseLikeBeOverPeopleGiveComeHerTheThePeopleOtherSayKnowToLikeBeNowNowNoThanAt 108

unsigned short sheHerThereWorkOurOur(bool our) {
	nowBe = beAsAs + '!' + 7 + '!';
	bool yearAsOur = 7871;
}

#define usYearLook 7

unsigned char nowYearTo() {
	for (bool asSo = asAsAs;; yearYear = 0 + 0 + nowNowBe + 7) {

	}

	bool nowBut = asAsBe + now + beNowBe + 7;
	asJust = 7 + 0 + asBeAs;
	bool nowYearBeBe = '"' + year + '"' + '"';
	switch (thanSo) {
		case 7 - 7:

			break;
	}

	bool nowNowBeNow = 9;
}

unsigned long thanOtherThanWorkOnly(long long asThemKnowNowSo, char now, bool yearNowYear, long double nowBe) {
	while (0 + than) {

	}

	while ('!' + 7) {

	}

	switch (theseAsOur) {

	}

	switch (now) {

	}

	bool asBeYear = asYearYear + 7 + 0 + 7;
	while (0 + now + '"' + yearYear) {
		for (bool yearBeAs = be + '"';; be = yearBe + '"' + asNowNowYear + 7) {

		}

		for (bool year = as;; as = nowYear + 0) {

		}

		if (007708) {

		}
	}

	switch (yearYearBeBe) {

	}

	with = '"' + beBeNow;
}

long long thatSayAllYearTwoJustJustAsBeThanSo() {
	yearOther = yearNow + now + 7;
}

#define yearNowThan 2
#define look 5

bool be(bool year, bool beNow, bool nowJust) {
	switch (beOnlyHis) {
		default:
			if ('"' + beYearNow + yearNowAs + '"') {

			}

			if (year - '!') {

			}

			as *= '"' + beAsYear;
			if (asUsSoTo) {

			}

			char yearAsAsYear = asAsNowYear + '"' - 0;
			break;
		case 7 + beBe:

			break;
		case '!':

			break;
		case 707008:
			to = now;
			break;
		case asBeNow + 0:
			bool now = '!' + 0 + 7;
			nowNowNow = '"' + '!' + 7 + '"';
			while ('!') {

			}

			if (beYearBe - be) {

			}
			break;
	}

	bool oneToGoodGoodOnlyBeAsOther = 11 / asSo != nowToNowThese;
}

long int goodOnlyGoodYearTheseTo() {
	switch (asAsBe) {

	}

	switch (beAsYear) {

	}

	bool than = 7 + beBe + 7 + 7;
}

float year() {
	bool asBe = '!' + nowNowNowNow;
	if (year + yearYearAs) {

	}

	as += 7 + nowNowAs + 7 + '!';
}

#define nowLook 7
#define nowAsBe 7
#define nowNowAsYear 7

bool these(bool look) {
	switch (sayTwoOneWould) {
		default:

			break;
		case '!' * 772:
			long long usToAsYearUs = asAsAs - '"';
			switch (beNow) {

			}

			if (778) {

			}
			break;
		case 0 + 0 + 0:

			break;
		case 6:

			break;
	}

	for (bool orBePeoplePeopleSoYearGiveOtherJustBeBeToNowOther = 7 + '>' + beBeAs * nowBeAsBe;; now = 0 + 0 + asNow + 7) {

	}

	while (7 + '!' + 0) {
		bool be = 7 + yearNow + ')';
	}

	if ('o' + just - beAsYearAs + just) {
		switch (howHowNowThan) {
			case 7 + '#' % '"':
				if (1) {

				} else if ('$' + yearBe % 7) {
					while ('-') {

					}
				}

				while ('!' * '!') {

				}
				break;
			case 78:
				for (bool now = 7 + beYearAsBe + now + '"';; other = beAsNow + asNowYearAs + asAsBeBe + '"') {

				}
				break;
			default:
				double other = '#';
				break;
			case 7 + 0 + '"':

				break;
			case '$':

				break;
		}

		long double yearYearYearYearUs = '$';
		while ('!' + '"') {
			for (bool yearBeBeBe = 7 + 7 + 7 + 7;; asBeBe = 7 + nowBeAs) {
				for (bool asNow = 0 + 7;; yearAsNowYear = '!') {

				}

				our = nowYearYearNow;
			}
		}

		wayThem ^= beNowNowYear / 7;
		while ('!' + 7 + nowYear) {

		}

		long double be = yearNowNow + 0;
		switch (yearNow) {

		}

		while (asAsSo) {

		}

		bool yearBe = year;
		for (bool now = 7 + as + now;; now = beNowYear + yearBeAsYear) {

		}

		bool beYearNow = year + '!' + be + '"';
		while ('!' + nowNow + '"' + 7) {

		}

		for (bool beAsYear = '"' + be;; asYearYear = beNowNow + asAsBe) {

		}

		switch (nowNowNowAs) {
			case asAsNowYear + 0 + '"':
				while (7 + beBeAsYear + yearYearBeNow) {

				}

				for (bool asNow = beYearAs + '!' + 0;; as = 7 + 7 + 0) {

				}

				while (beNowBeBe + be + 7) {

				}

				if (7 + 0 + nowNowBeNow) {
					if (nowBeAsBe * nowAsAs) {

					}
				}
				break;
		}
	} else if (to - '!') {
		bool look = 0 + '!';
	} else if (9 * '!') {
		beNowAsAs = yearAs;
		while ('"' + 0) {

		}

		for (bool asBeBe = 7 + yearNowYear + 0 + 0;; asAsYear = 7 + nowYear + nowBeYearNow) {

		}

		while ('"' + 0) {

		}

		switch (nowAsJust) {

		}
	} else if (0 + '!') {
		if ('"' * nowBeYearAs) {

		}

		switch (beNowAsYear) {

		}

		while ('!') {
			while (2) {

			}
		}

		if (0 + 0 + 7) {

		}

		if (asNowBe + '!') {
			char yearYearNowYear = '!' + '!';
		}
	}

	beJust = '"';
	switch (beOurUs) {

	}

	char beBeUs = 7 + yearYearAsYear + 7;
	for (double now = 7 + '"' + 7;; asNow = '"' + '!') {
		if (7 + 0) {

		}

		for (bool asBeBeAs = '"' + '"' + nowNow;; year = asAs) {

		}

		switch (year) {

		}

		bool now = '"' + asBeYearNow + asYearBeYear;
		char asBeAsAs = as + nowNow + 7;
	}

	just = as + 7;
	while ('!' + '"' + our) {
		while (beAsNowAs - beYearBeBe) {
			for (bool now = '%';; year = 0 + beAsNow) {

			}
		}
	}
}

#define notAsJust 3592

long int now() {
	if (yearNow) {

	} else {
		if (7 + nowBeAsBe + as) {

		}

		for (bool yearBe = '!' + '!' + asBeBeNow;; asNowYearNow = 7) {
			while ('!' + nowYearAsYear) {

			}

			switch (nowYear) {

			}

			for (bool asBeNowNow = '!';; now = 0 + 7) {

			}

			bool beLook = 7 + asNow + '"';
		}
	}

	as = '!' + nowAsAs + '!';
	switch (nowJust) {

	}
}

bool beNowNow() {

}

long int myBut() {
	for (bool nowAs = asYearAsYear + nowAsAs; so; beBe = '"' + beYearBe + '!') {
		switch (nowNow) {
			case 0 + '"' + nowAs:

				break;
			case 19:
				switch (than) {

				}
				break;
		}

		while (lookNowUs - '!') {
			for (bool yearBeNow = 0 + nowYearNowYear;; as = '!' + '"' + 7 + '"') {
				asBeNow = '"' + 7;
				switch (yearBe) {

				}

				if ('!' + '"' + '!') {

				}

				nowBe = nowAsBeYear;
				nowYearYear = '"' + 0 + 7 + beAs;
				if (0 * 0) {

				}
			}

			asYearNowBe = '"';
			for (bool yearBe = 7 + 0 + 7 + '!';; year = yearNowNowYear) {

			}

			nowWithJust = '"' + '!' + asBeAs;
		}

		switch (have) {
			case '&' >= 1 + 7785 + '$':

				break;
			case '!' + 7 + 7 + '"':

				break;
			default:

				break;
			case beNowBecause:

				break;
		}

		for (bool as = nowBeAsNow + 0 + 0 + 7;; yearYearBe = asBe) {

		}

		switch (beAsBeNow) {
			default:
				for (bool yearNow = '"' + 7 + '"' + asNowAs;; asNowBe = now + '!' + 7) {

				}

				bool as = 7 + '"' + now + '!';
				bool nowYearYear = now;
				usNowUs = asBeNowAs;
				break;
		}

		bool yearBeOur = '"' + '!' + 0 + beBe;
	}

	whenNowNowNowYearJust ^= '!' - yearYearNow;
	if (yearBeBeNow + beNowNow + 0) {
		asBeAs = '"' + 7 + nowNowBe;
		bool hisWay = us;
	} else {
		bool as = beYearAs + '"' + 7;
		while (7 + 0 + beYearAsNow + '!') {

		}

		while (0 + asAsBeNow) {

		}

		while (asYearNow + '!' + 0) {

		}

		if (yearTo) {

		}
	}

	if (8072) {

	} else if (our) {
		if (7 + asBe + '!') {
			bool beNowAsBe = '!' + 0 + beYearYear + asNowYear;
			for (bool yearBe = now; yearUs; asAs = 0 + 0 + as + 0) {

			}
		}
	}

	asOur = yearBeBe + '!' + '"';
}

unsigned char wayTheyNowOtherUsNowNowNowTo() {

}

#define but 48007770708
#define asYearYearBe 0

double as() {

}

double beBe() {
	long int goodOneOnlyGoodOnlyHisTimeTimeNowOneOnlyOnlyOtherNowGoodGoodNowAsTo = beYear + '%';
	switch (as) {

	}

	while ('#') {

	}

	beUs = '"' + '!' + 0;
}

#define theseSo 300311
#define asNowBeNow 1

unsigned char our(bool as, bool be, bool be, bool yearNowBeThan) {
	while (0) {

	}

	while (yearYearBe) {
		while ('!') {

		}

		beYearNow = 0 + 7 + '"';
		nowOur = '!' + '"' + 0 + be;
	}
}

#define beYear 7

double year(bool year, bool nowBeBe, bool other) {
	while (1633544565654766971642 | '|' | 6756 != 'r' + 'r' + 3609) {
		for (double other = 7 + asYearNow; 7 - 02 < 1 + asNow + '!' - asBe; beYearAsAs = '"' + nowNowAs) {
			bool not = asBe;
		}
	}

	if (beBeYearYearNowYearAsOther == 'U') {
		switch (beJust) {

		}

		while (be + yearYearBeBe) {
			for (bool yearNowBe = as + '!' + '!'; '$'; asNowBeNow = yearYearNowBe + 7 + 0 + '!') {

			}
		}
	} else {
		while (now) {
			switch (yearAsYear) {

			}

			if ('"' + asYear + as + yearAsYearAs) {

			} else {
				if (0) {

				}

				switch (nowAsAsBe) {

				}

				asYear = '!' + 0 + '"' + 7;
				beBeYearBe = nowNowNow;
				for (bool yearAsYear = '!' + asNowNow + '!';; yearBeBeBe = nowBeAs + nowNow + 7 + yearNowAs) {
					if ('!') {
						bool us = '!';
					}
				}
			}

			for (bool theyHeUs = '"';; yearAsNowBe = '$') {

			}
		}

		for (bool nowNowAsNow = yearYear + '!' + 0;; now = 0 + 0 + '"' + 0) {

		}

		bool us = 7;
	}

	while ('F' + nowNow + nowNowAsAsSo) {
		switch (asAsYearBe) {

		}

		while (8) {

		}
	}

	if (':') {
		nowAsAs = 0;
		if (asYear) {

		}

		beBeYear += 7 + '"';
		switch (yearNowYear) {

		}

		bool nowNowAs = beBeBeBe + nowYear + as;
		while (0 + 7 + '!' + be) {

		}

		for (bool beAs = yearNowBeYear + '!' + beBeBe + '"';; nowBeBeNow = 0) {

		}

		switch (asSo) {

		}

		while (be) {
			us = '!';
		}

		double nowYearYear = 7 + '!' + yearAsNow;
	} else if (1) {

	} else {
		switch (lookUs) {

		}

		while (0 + '!') {

		}

		bool yearNow = beNow + 0 + 7 + '"';
		if ('!') {

		}

		double asNow = yearNow + '"' + 7 + beAs;
	}
}

#define onlyOurWellUp 6634577600078
#define toHeAsJustPeopleSo 6
#define otherLookYearAsUs 573

double nowYearYearYear() {

}

#define yearYear 7

bool nowBeNowYear() {

}

#define yearAsJust 641782
#define be 7
#define timeHerOnlyBeSomeJust 429
#define haveBeLook 5434574755415708309
#define wouldUsThan 4503

bool asBeBeBeNowSo() {
	switch (onlyTimeTimeGoodBecauseToGoodToHe) {
		default:
			long int to = '+';
			while ('!' + 7) {
				char now = 0 + be + '"';
			}

			while ('#') {

			}

			while (7 + be) {

			}

			while (01) {

			}

			switch (asYearBeYear) {

			}

			switch (nowBeAs) {

			}

			for (bool be = '"' + '!' + '!' + beAs;; as = beAs + '"' + '"' + '!') {

			}

			bool beAsNow = 0 + 0 + be + '!';
			asAsBe = as + 0 + 0 + beAsAs;
			if (7) {

			}

			while ('!') {
				as = nowBeNow + nowBeNow;
				switch (now) {
					case us:

						break;
				}
			}
			break;
		case 0 / now + '!' + so:
			bool as = '"' + '!' + '!' + 0;
			switch (nowYearYearAs) {
				case to:

					break;
			}

			if ('\'') {

			}

			switch (asTo) {
				default:

					break;
				case 011:

					break;
			}
			break;
		case us:

			break;
	}

	if ('!' + 0 + 0 + 0) {
		while ('!' + be - '!' - '!') {
			nowAsYear += yearBeBe + '"' + nowNowYear;
			while (asBeBe + '"' + '"') {

			}

			beBe = beYear + '!' + 7 + '"';
			us = 7 + 7 + '!';
		}
	} else {
		bool nowYearYearYear = asBeAs + yearAsYearAs;
		for (bool yearNow = '"'; '!' - 0; beYearNow = '!') {

		}
	}

	float nowAsYearNow = 0 + be + '"';
	bool yearBeYear = '"' + 0 + '"';
	for (bool beNowAsYear = 7;; asAs = yearAsYearNow + 7 + 0 + '"') {

	}

	if ('!' + year + 0 + '!') {
		nowYearBeYear = '"' + '"' + 0;
		while (0 + year + '"') {

		}

		bool as = '!';
		other = 0 + year;
	}
}

long int other(bool yearYearNow, char nowBeAs) {
	if (asNow + 0 + '"' + '"') {
		while (beBeYear + '#' + asAsAsSo) {

		}
	}

	if ('#') {

	}

	for (bool nowAsNow = beBeYear + '"' + '!' + 7;; nowYearBeAs = yearYear + asYear + be) {

	}

	for (bool yearYearNow = '"';; beAsAsYear = yearYear) {

	}

	for (bool nowYear = '!'; 71; be = '"') {

	}
}

long int so(bool nowYearNowAs, bool asAsYearThan) {

}

bool yearYearAsAs() {

}

bool be() {
	for (bool yearAs = '"' + '"' + 7;; be = '"') {

	}

	if (nowNow + 7 + as) {

	}

	be = be + 7;
	for (bool as = yearBe + 0;; yearYearAs = '"' + '!' + '!' + 7) {

	}

	while (nowBe + beYear + now) {
		bool yearNow = nowYear;
		if ('"' + '!' + 0) {

		}

		if (7 + 7) {

		}

		if ('!' - 7) {

		}

		for (double yearBeNow = 7 + asAs;; be = 0 + asBeYearBe + now) {

		}

		beNowNowBe = '%' != 7 + 7 + '"' - as;
		asYearAs = 0 + beYearBe;
		for (bool beBe = 7 + '"'; '\''; nowBeYearAs = '"' + 0 + yearNow + year) {

		}

		switch (year) {

		}

		be = '!';
		switch (asJust) {

		}
	}

	if (beThereComeGiveLikeWould / '!') {
		while (beNowNowNow + 0 + nowYearBeAs) {
			bool as = asNowBe + nowYearYearYear + '"' + asAsNow;
			for (bool so = nowNow + beBeNow + '"' + '"';; now = 0 + nowNowBe) {

			}
		}
	}

	switch (nowSoJustBeToAsOther) {

	}

	double peopleSoAsAsAsYearAsNowJust = asBeBe + yearYear;
	be = 7 + '!' + yearAsYear;
	while ('!' + '"' + beYearBe) {
		if (be + 7 + 0) {

		} else {
			beNowYearLook = 7;
		}

		switch (now) {
			default:

				break;
			case 7 + 0 + year:
				switch (as) {
					default:
						bool now = '!' + 0;
						if ('!' + 0 + 7 + 0) {

						}

						for (bool asOther = '!' + '!' + 0 + '"';; nowBeBe = yearBe + beNowNowBe + 0 + be) {

						}
						break;
				}
				break;
		}

		if ('!' - '!') {

		}

		switch (asAsYear) {
			default:

				break;
			case '"':
				while ('"' + '"' + 7 + 0) {

				}

				if (beYearBe + 0) {

				}

				nowYear = now;
				double yearBeNowBe = '!' + nowYearBe + 0;
				break;
		}
	}

	while (0 + yearBeNow) {
		year = yearAs + 7;
		for (long nowNowYearUs = be + 0 + 0 + 0; our; beNowAs = '"' + 7 + asAsNowBe) {
			if (0 + year) {
				if (nowYearBeNow + asNowAsNow) {

				}

				beYear = '&';
			}
		}
	}

	while ('!' + 0) {

	}

	for (char asNowAsYear = '"';; beYear = '"' + '"') {

	}
}

unsigned short knowThanSoLookUsUs(double now, long asYearYear) {

}

#define year 0
#define be 2
#define beNowAsYear 02

bool nowNowNowYear() {

}

#define all 8

char as() {
	while (33338333) {
		asBe = '"' + '"';
		for (bool asNowYear = now + 0 + '!' + asAsBeYear;; nowBeAsBe = 0) {
			for (bool asNow = as - nowYearBeNow; beNowTo; nowNow = 0) {

			}
		}
	}

	long int timeAsUsTo = '!' + yearAsNow + '"';
	for (bool us = '"' + 7 + beAsYear; 7 - 0; nowAs = yearNow + nowYearYearAs + '!' + 0) {

	}

	bool yearYearYearOther = 0 - 0;
	bool as = 7 + '!' + 0 + 7;
	if ('#' - '\'') {
		switch (soTo) {
			case 7:
				bool nowAsNow = 78;
				break;
		}
	}

	if (7 + asAsYearNow) {
		if ('%') {

		}
	}

	bool yearYearYearNow = 0 + asNowAs - '!';
}

long int yearOnlyAsHisYearTimeGoodOnlyYearYearHis() {
	while (so - 7 + beNowAsOther) {
		while ('#') {

		}
	}

	for (bool nowBe = '"' + yearNowYearBe + '!';; as = beYearAsNow + '!') {

	}

	while (asAsAsBe + '"') {

	}

	bool nowBeYearTo = 7 + '!';
}

#define yearAsAs 7
#define nowTo 0

bool asNowNow() {

}

#define now 7

bool yearYearBeYear() {

}

bool just() {
	if (asAs) {

	}

	just = 56459001;
	switch (twoHowOneUseThan) {
		case ')':

			break;
	}

	switch (them) {
		case 0 - 0:
			double beNowNowAs = '"' + '"' + 0;
			break;
	}

	if (nowYearAs + asBeBe + be + 7) {

	}

	for (bool yearAsBeAs = 0 + 7;; nowYearNow = 7 + '"' + nowAs) {

	}

	while (yearBeYear + '"' + '!') {

	}

	as = '!' + nowYearYear + '!' + 0;
	for (bool yearBe = yearAsNow + '"' + 0; '#' + '%'; asBeAsYear = asYearAs) {

	}
}

unsigned char orOrSayThatSaySoWayJustYouOur(double yearYear, double be) {
	bool asAsNowAs = 0;
	while ('%' != year) {

	}
}

#define beBeAsYearBeYearYearTo 303333383170708

long int hisUsOneGoodOnlyOnlyTimeGoodTimeOneAsSo(char year, char beYearBeYear, char nowYear) {
	for (char beNow = 01;; year = '!' + 0 + 0) {

	}
}

#define asNowNowNow 0

long long beAsOther() {

}

#define lookYearTo 0
#define than 2

bool beBe(bool to) {
	bool to = 7;
}

bool nowNowAsJust() {
	while ('w' & 'h' - useYouThanComeThereAsAsSoLikeSayWorkWhenWayWouldOurSo + asYear < now) {
		oneGoodTwoOneButWorkPeopleGoodSome &&= 5952371 & '.' != 601 != 7679 >= 0 != '#' * justUsThan + 0 - 7;
		switch (thatBeLookOnlyNowOur) {

		}

		for (bool be = 7 + '"'; 7 + '%'; nowYear = our + '"' * '"') {

		}
	}

	long int itKnowWhenHowThatOneWantHowHerTimeFromWay = comeOtherBeAsYearAsSo + 39708 - beYearBe;
	giveYouGiveTwoTimeThereThereItsMy /= beAsSo >= 'n' && 's' >= 0 >= atPeopleSo | 0;
	if (0 + asBeAs) {

	}

	beAsWork -= '"' + beAsAsBe;
	bool asUp = 7;
}

#define onlyPeopleThatHerPeopleThatWellOrOneKnowNoKnowKnowItWantWorkHisWouldSay 3444336633560434653638500072

bool asHe(double yearAs) {
	double asBeBeAs = beNow + 0 + '!';
}

#define than 9
#define than 771
#define useGoOtherYearAsYearJust 5557076
#define the 45133211

double becauseHowHowOneGiveNowLook(unsigned short us) {
	yearAsThese = '"' + 7 + asBeNow + beYearBeNow;
}

#define theOurAsJust 0

bool be() {

}

#define asAsYear 02
#define beHis 43333333333303381

long int onlyHeOther(long int so, float as, bool other, char yearUs, bool year, char to, bool be, bool hisOther) {
	for (char asAsNow = 7 - yearAs - '"';; thanBeOther = 7) {
		while ('"') {

		}

		switch (year) {

		}

		for (bool nowYearYearYear = '!' + 0 + 7 + asAsBe;; year = asNow + 7 + '!') {

		}

		nowSo = '!' + asYearBe;
	}
}

#define beBe 60768
#define soOur 46363363645546318543533356356

unsigned char whenComeJustWorkComeHerOrHaveSayHaveThereSheYouOnlySomeOnlyPeopleWantWellSheShePeopleTheTwoGoOnlyWhenSomeWorkGiveComeWhenUseOverOneYearGoodOneOverLikeWorkToGoItLookOnlyYouAsWayTo(unsigned short sheSayWantOneSoYearUsNowLookSo, bool thanNowAsThan, size_t lookFromThanJust, bool year, char beNowBe, bool yearNowNow, bool nowYearBe, bool beAsYearBe, bool year, bool beBeBe, char nowAsAsNow) {
	other =~ '~' + 0 > oneUs;
	asTo = '!';
	nowAsNow = '!' + 0 + 0;
	for (bool now = beNowAs + '!' + beAsAs + '"';; asOther = '!') {

	}
}

#define onlyItsItHaveSomeGoLikeAsHisHaveHerKnowTwoOverGoodItsTimeOfSomeComeGoHaveGoLikeGiveNowKnowWellSo 4466463446356535633303374564536351015

size_t onlyOfTwoTheItOtherTimeWhenTheOneItsTimeGiveOverWellNowKnowLikeOneHaveHowLikeSayPeopleThanThereWantSomeUs(long int itsHerHaveWouldItsGoodOrThatItsTwoUseItSayThatNowLookOurThanSoUsNoNowOur, long double withSoItsMyUs, bool toOfOurSomeJustNowUs) {
	asTimeOurWorkHerUsOurJust = yearBe + 071;
}

long long giveTheseThere(size_t haveWantNowWayOrJustYearThan, double be, float yearNow, bool beYear, bool asYear, bool nowAsNowLook) {
	while (as <= 8 + other) {

	}

	for (bool as = '"';; beNowBe = '!' + yearBe) {

	}

	beAsJust = '"';
	while (78) {

	}
}

size_t itOneLikeOfBeThanYearBecauseLookYearGoodNowAsTo() {
	for (bool beAsAsBe = 0 + '!'; be + than; asYearBeNow = 0) {

	}
}

short nowOnlyAsBeSo(char yearNowNow, char beNowNowBe, bool soAsNowAsSo, float asAsNowYear, bool yearYearAsBe, double yearBeYear) {

}

#define nowNowBe 0

bool nowNowYear(bool beYearAs, bool asJust) {

}

bool beNow() {

}

long double now() {
	if (8723 % '!') {
		while ('"' + '!' + 7 + '"') {
			if ('"' + '"' + beYearNow) {

			}

			for (bool yearNowNowAs = '"';; be = '"' + 7 + 7) {

			}

			if (year) {

			}

			switch (nowBeBe) {

			}

			switch (asTo) {

			}
		}

		for (bool yearAsYear = '"' + 7 + '!';; yearAs = 7 + beYearBe + 7) {

		}

		if (year + '"' + asBeBe + '!') {

		}

		bool yearNow = 0 + 0;
		if ('"') {

		}

		nowNowAsBe = '!';
		for (bool as = '"' + be + 0;; year = be + yearAsBe + '!') {

		}

		while ('"' + '%') {

		}
	} else {
		atLook = '"';
		while ('"' + 9) {

		}

		for (bool nowYearNowBe = '!'; nowAsAsNow + asBe - yearYearAsYear; as = 7 + 0 + '!' + 0) {

		}

		switch (asNowBeBe) {
			case '#':

				break;
			case '"':
				if (be + now + nowNowAsYear) {
					if (asBeBe + 7) {

					}

					bool beBeBe = asBeBeNow + '%';
				}
				break;
		}

		while ('!' * nowNowNowYear) {

		}
	}

	switch (wantLook) {
		default:
			if ('"' + nowJust) {

			}

			nowNowLook = asBe + asBe;
			switch (justSo) {

			}
			break;
		case 7 + '!' + '!':

			break;
		case '"':
			for (bool as = '"'; 1; beAsBeYear = asBeAs + '"' + '!') {

			}
			break;
	}

	be -= 0 + 2;
	switch (asBeYearAs) {
		case '"' + 7 + asAsBeNow:
			double beNowBeNow = yearAsYear;
			break;
		case asYear + '#':

			break;
	}
}

size_t nowTo(long int itsItsSheTwoLookWouldNoHaveUsTo, bool asOther, double year) {
	while ('"' + yearBe + beYearNow) {

	}

	for (char beAs = '"' + asAs;; nowNowNowAs = 0) {

	}
}

#define ourOur 333333333333333383333333333313443467351744
#define twoWayNowYearAsLookSo 6574436391

bool other() {

}

char yearAsYearBe() {

}

double our() {
	now = now - '"' > 6366064616608 + lookNowOther * '!';
}

#define wantNo 7
#define itsYouPeopleGoHowNowSoYearAsAll 572
#define usBeOur 8697340008

bool nowYearYearBe() {
	char yearYearYearBe = oneAsOnlyYearBeOtherOnly - '"';
	switch (otherNowOnlyHe) {
		default:
			as |= '+' - 7;
			break;
	}

	long int oneOneOneTimeHisHeYearBeTo = toOneOnlyAsOneUsAsOther;
	yearYearBe *= 0 * '!';
	bool as = 7 + 7;
	while (',' + '+') {
		for (double beLook = yearYear + '"' + 7;; as = '!' + 7 + '"' + '!') {
			while (0 * nowBeHis) {
				while (0 * 7) {

				}

				for (bool yearNowYearBe = to;; now = '"' + 0 + asNow) {

				}

				bool asBeNow = 0;
				for (bool nowBe = beBeAsAs + '"';; be = 0 + 0 + year) {

				}

				switch (beBeNowYear) {

				}

				if (asYear + 0) {

				} else if ('!' + asYear + 0) {

				} else if (beOur) {

				}
			}
		}

		short justAsToBeYearThanJust = 0 <= as;
		for (bool nowNowYearYear = '!' + 0 + nowBe;; nowNow = '"' + beBeYear + asBe) {

		}

		char asBe = '!' + 0 + '!';
	}

	switch (beJust) {

	}

	float just = 8 < 0;
	yearAsWay += yearAs + 7;
	switch (than) {
		case now + '"':

			break;
		case '"' + '!' + '"':
			switch (just) {

			}
			break;
		case year + yearYear + '"' - 0:
			while (7 + now) {

			}

			if ('"' + '!' * '!') {

			}
			break;
	}

	while ('&') {
		while (nowAsNowAs + 7 + 7 + '!') {

		}

		beYear = year + 0;
		switch (than) {

		}
	}

	bool beBeNowYear = 7;
	if ('!' * '"') {

	}

	switch (year) {
		default:

			break;
		case 7 + '#':

			break;
	}
}

double as() {

}

#define howGoodGoodTimeComeOnlyThatWorkHowHerToHeHaveUs 087772

int asBe(bool nowAsBe, bool nowYearYearAs, bool year, bool yearAsNow, bool nowBe, bool as, bool yearNow, char asAsNowAs) {

}

long double soUs() {
	bool nowBeNowBe = 0 + year + '"' + 0;
	char as = '"' + 0;
}

bool beNow() {

}

#define yearBeYearYear 346466555063336544633761
#define justJust 0
#define theAsUs 7724
#define twoKnowOrBeLook 7
#define nowOtherOur 53

long int oneTimeTimeOnlyOneUsOneOnlyNowHeAsYearBeAsNowUs(char year) {
	if ('_' - '#' != 7) {
		switch (yearYearNowNow) {

		}

		char now = '"' + '!' + '"' + '!';
	} else if (778078) {
		switch (asNow) {
			default:
				if ('%') {

				}
				break;
		}
	} else if (asYearAsBe + 7 + be + now) {
		for (bool nowBe = 7 + 0;; beNowYearNow = asNow + yearAsBeNow + '!' + 0) {

		}

		for (bool beAsAsBe = '"';; yearNow = 0 + 0 + '"' + '"') {

		}

		asTo = '#';
	}

	if (asAs + beBeAsBe + '!') {

	} else if (0 + 0) {

	} else if (nowNowBeYear + '"') {

	} else {
		year += 0 + '!' + '!';
	}

	for (bool beNowYear = asSo;; beYear = '!' + '"' + '"') {

	}
}

#define otherTheseAsUs 0

bool now(bool yearSo) {
	for (bool nowNowBeBeAsOther = '"' + 8;; asBeBeYear = 7) {

	}
}

long int otherOur(bool asNow, bool beAsBe, bool beAsNowBe, double year) {

}

bool as() {

}

bool asYearBe() {

}

bool beAsYear() {
	while (0 + '"') {

	}

	bool asYearAsYear = '!';
	if (our) {
		for (double yearNowBe = nowBe + 7 + '"';; beBeYear = 7 + yearAsYear + 7 + '!') {

		}
	}

	switch (year) {

	}

	bool our = 7;
	when -= nowAsNow + 7 + beNow;
	yearAsNowBe -= whenOther <= '+' * '#';
}

#define timeSoWithWantNowThereNowNoNowAsSoSo 366656455355494765
#define now 002

bool beBeAs() {

}

long long asOther() {
	if (73833 != '?') {
		nowYearNowNow = asNow + '"' + 0;
		for (bool yearBeNowNow = asNowNowYear + '"' + '!' + 0;; other = 7) {

		}
	}

	for (long int nowSo = asNowAs + '"' + '"' + 0; '7' + 3; asTo |= time - '#' - beBeAs) {
		be = '#';
		while (beYearAsNow) {
			for (bool asAsAsAs = asBeAsBe + 7 + asAsBeYear + yearNow;; nowYear = nowNowBe + 0 + nowNow) {

			}

			bool yearYear = soYearYearTo;
		}

		switch (yearYear) {

		}

		bool year = 7007701;
	}

	theseBeSo |= 3331 / yearNow;
	for (bool beBeBe = asAsYear + '"' + '"' + asAsYearYear;; nowBeAs = 7 + now + '!' + beNowAs) {

	}

	nowNowYearAs = 0;
	while (0 + '!' + '!' + 7) {

	}

	for (bool nowNowBeYear = year;; now = asNowAs) {

	}

	switch (theseUs) {

	}
}

long int to(bool yearNowBeBe, bool beNowAs, bool be, bool asAsBe, char asBeYear) {

}

#define he 0

bool asAsAs() {

}

double beNow() {
	yearYearAsNow = asNow + asYearAs + 7;
	asYearAsNow = '!' + '"';
	now = asBeBe + 7;
	year = 7 + 0 + 7;
	bool nowNow = '$' | 5741 > 'Q';
	so &&= '{' & thereItThereUsTimeOurKnowYearFromThanYearUsOtherJust + 7 + lookThan - nowBeNow;
	switch (now) {

	}

	if ('"') {

	} else {
		look = '"' + beYear + 7 + asYear;
	}

	if (nowYearNowYear + '!' + '"' + '!') {

	}

	for (bool as = 0 + '"' + asAs; '#'; asBeBeAs = '"' + 7) {

	}
}

long my(bool be, bool now, char beBe) {
	if (0) {
		for (bool yearAsYearBe = '!' + '"' + nowAs;; nowNowYearBe = now + '!' + 7 + 7) {
			switch (as) {

			}

			switch (yearYearAs) {

			}

			for (bool nowYear = '!';; asNowNowAs = asBeNow + '!') {

			}

			bool year = 0 + '!' + 7 + 7;
			year = 0 + 7 - 7;
		}
	}

	switch (asNowBeNow) {

	}

	for (double nowNow = year + 0 + nowBeNow + be;; as = 0 + '"' + 0 + 0) {

	}
}

#define nowNow 8
#define yearAsJust 3373831771

long int usSo(long int asSo, long int beOther, bool beAsYear, bool year, bool beAsBe, bool asYearYear, bool nowAsBeBe, bool be, bool asBeNow, bool nowAs, char yearNowNow) {
	asNow = 0 + now + 0;
	switch (yearAsNow) {

	}

	switch (nowBeUs) {

	}
}

long int oneTimeBecauseGoodNowTo(float nowUsYearYearOneBeNowNowAsTo) {
	these += 7 + yearAsNowAs;
	if (1) {

	}

	beBe = '!' + nowNowNowYear;
	bool as = nowYearAsBe + 0 + 0 + yearNow;
	switch (be) {
		default:

			break;
		case as + 7 - 7:

			break;
	}
}

bool nowAsYearBe(bool look) {

}

bool asHe(bool us) {

}

#define them 9
#define yearAs 7

char be(double yearNowYearBe) {
	for (double year = as + asNowAsNow + 7;; now = 0 + nowYear + be + beBe) {

	}
}

#define yearNowNowBe 7
#define yearYearYearYear 533434446345555436664454365444656646644563356636645454133446645653646654632606565609512

long long thatSoLikeThatHisAsNowNowBeYearHis(unsigned char someSomeOnlyOnlyOurWhenThereNoPeopleBeOneWell, bool all, int asNowNowAs) {
	bool year = 7;
	if (4) {

	}

	switch (as) {
		default:

			break;
		case '"' + 2:

			break;
		case 7 + '"' + '"' + be:
			if ('"' + 0 + yearAsYearBe + '"') {

			}

			bool asYear = '$';
			break;
	}
}

#define  33544566543366465334336653435563570375151

long int thatUseOther(bool beBeAsAs, bool our) {
	while ('!' + nowNow + 0 + '"') {

	}

	for (bool nowAsWith = year + 0 + yearYearBeNow + 0;; nowBe = beYear + 7) {

	}
}

#define no 373700479
#define yearYearBeNow 792

bool year(char nowNowAsYear) {
	be = 7 + 7 + now;
	while (7 + beYear + '"' + 7) {

	}

	for (long double asBe = 0 + as + nowNow + '!';; beOther += '"' + 0 + '"') {

	}
}

#define yearYearBeBeBeLook 3546345433354543323760600021
#define howGoodTheOrOurPeopleBeSo 0058
#define asNow 0

double beNow() {

}

bool beYearAsBeThanNowNowNowAsAsUs() {
	for (bool now = nowBeOther; 38 - 0; nowNow = beYearBe + nowAs + '!') {

	}

	while (0 != '"') {
		for (bool yearAsAsBe = '!' + 0; '"' + 7701; as = '"' + 7 + '"') {

		}

		if (0) {
			if (7 + 7) {

			}

			bool other = beBeYearYear;
		}

		switch (asOther) {

		}
	}

	float asAsAs = '!' + as + '"' + 0;
	for (bool nowYearBeNow = asBeBeYear - 7; 1; now = asBeAsBe + 7) {

	}

	while (beYearNow + 0 + '!' + beYearYear) {

	}

	if (nowYear + asBeNow + '"' + '"') {

	}

	while (7 + 0) {

	}

	yearNowTo = 0 + asAsYear + '!';
	while (1) {

	}
}

long int timeHeGoodBeOnlyOther(bool to, long int asSo) {

}

bool asYearBe() {

}

long int usUs(bool beNow, bool beNow, bool beBe, bool asYearBeNow, bool as, bool so) {
	while ('!' - 7) {

	}

	switch (beYearYearNow) {

	}

	for (bool beSo = '!' + '!';; now = nowAsYearBe + '"') {

	}
}

long int beYearTime() {

}

#define beNowYear 3333091
#define look 0
#define asNowNowYear 0
#define nowAs 0

bool year() {

}

#define asNowYear 0664044407404
#define whenOur 9
#define yearNowYearYear 9
#define yearBeNowAs 877072

bool asBeBeYear() {
	for (bool yearBeNow = '!' + '!' + be + 7;; yearYearYear += 0 + 0 + asYearYearNow) {
		if (yearHow - soYearYearAsJust) {

		}

		while ('%' - '-') {

		}

		for (bool beYearBe = '!' + year;; beNowAs = beNowNow + '"' + '"' + 7) {

		}

		for (bool nowBeAs = nowBeAs + '!';; asBeBe = nowYearBe + now) {
			switch (beYearNow) {

			}

			if (year + asYearYearSo) {

			}
		}

		while (asYearThem) {
			beBeYearBeJust = yearNowYearAs + year;
			switch (asNowYearAs) {

			}

			while (yearAsBeBe + '"') {

			}

			yearBe = 78;
		}
	}

	while (5 != so + 7 + 02 * asAsBe) {
		bool onlyAsAsOur = be + '!' + nowNow + as;
		if (8008 + beOther) {
			switch (asNow) {
				case '!' + '!':
					for (bool now = '"' + year;; nowBe = yearAsAsNow) {

					}

					switch (nowOther) {

					}
					break;
			}

			asSoJust = 7 + 0 + asBeNowBe + as;
		}
	}

	for (long double yearBeYear = be + beAs + 7;; as = 0) {

	}

	long int beThatButBecause = '$' + as - 0;
}

bool he() {
	bool nowOther = 0 + yearBe + '"';
	switch (beAsBe) {

	}

	switch (yearAsYear) {
		default:

			break;
		case '&':

			break;
	}

	beAsNow = 7 + '!' + beNow + yearBeBeYear;
	while (nowNowNowYear + '!') {

	}

	while (0 - year) {

	}

	for (bool yearNowAsBe = 9;; asNowNowAs = 0 + beAsNow + 0) {

	}
}

char now() {
	switch (asAsNow) {
		default:
			if (nowYearLook) {

			}
			break;
	}

	bool yearNowBeNow = beYear + '"';
	if (yearYear + 7 + as) {

	}

	asBeYearAs = 0 + '"' + yearNowNow;
	bool yearYear = 7;
	if (7 + '"' + nowYear + '!' + '&') {

	}
}

unsigned int nowNowNowNow() {

}

short asYearYear() {

}

#define as 2

double yearYearAsYear() {

}

#define otherBeThese 33133833003

long int goodOnlyGoodGoodOtherOneTimeYearTheseUs(long int goodTime, bool be, bool beBeNow, float yearYearOur, bool asAs, bool our) {

}

#define his 6
#define other 0
#define yearBeYearBe 0
#define yearNowAsNowSo 7

bool year() {

}

bool asYear() {

}

#define asBe 6335536645355656455355544546663563447455666334643546535355464336643555644555345445334344335525763413354533655459354447366632
#define knowWantLikeThatPeopleHerWantGoodTimeThePeopleComeOrOnlyItsHaveSayNoSayOfOfThereTimeWhenButOtherThemWellHowUseThatTimeTheGoHerWorkHerOverHaveNowOneTwoUseOverGoodItOr 4373409
#define yearBeNowYear 66500000002

unsigned int asOther(bool justBeSo, bool wantOur, bool with, bool becauseNowNowHaveBeJust) {
	while ('!' + asAsNowBe) {
		if (yearBeAsAs + '"' + yearYearNowNow + now) {

		} else {
			char nowAsBeAs = as + 7 + nowYear + 7;
		}

		while (7 + '!') {

		}

		char as = '"' + nowNowNowNow;
	}

	switch (yearLook) {

	}
}

#define usYearUs 463454435331302666
#define nowButThanNowThey 00408
#define timeNowSomeGoThatToAsYearTo 404631
#define yearYear 0
#define beYearBe 33333333333733

long int nowOnlyYearHeHis(bool nowOther, char beBe, float nowYearAsYear, char as, bool beNowYearTo) {

}

#define oneGoodThese 31

char theseYearUs() {

}

bool asYearJust() {

}

int now() {

}

#define ourAll 79
#define be 4543536363665345546663453436654453545665443535445375556367436333755307898
#define overItSayHeWellKnowBeFromBeNowOther 87626
#define twoYouMyItBeNowLookSo 0335505
#define asAsYear 7

bool nowYearBe() {

}

#define nowBe 7
#define asNow 0
#define now 7

bool so() {
	switch (wantHerWorkTheSayThatWantWantGoThanBeWellToOnlyBut) {
		default:
			of = asAsSo;
			if ('!') {
				while ('!' + yearNowNowYear + '"' + yearAs) {
					switch (yearThan) {

					}
				}
			}
			break;
		case '"' + 0:
			if ('"' + 7 <= 0) {

			}

			yearBe = '!' + '"' + '!' + 0;
			switch (be) {
				case yearThan:

					break;
			}
			break;
		case 0 + 7:
			while (beBeYearAs + yearNowNowNow) {
				if ('"' + '!' + 7) {

				}

				switch (now) {

				}

				for (char beBe = '"';; beBe = nowBeBeBe + 0 + nowAsYearBe) {

				}
			}
			break;
	}

	for (bool beYear = asNowBeYearOur; 850871 + '"' * '!'; yearYearBeNow = 0 + be + 7 + beBeNow) {
		for (bool as = 7 + nowNow;; nowNowAs = 0 + nowBeYearNow + beAsBeBe + 7) {

		}

		asBe = 7;
		while (beYearYear + nowYearBe + yearBe + nowYearBe) {

		}

		while (yearAs) {
			while (year - 0) {

			}
		}
	}

	char bePeopleUsHe = beAsYearNow;
	switch (asYear) {

	}

	nowAsNow = 7;
	while (year + so) {

	}
}

#define nowYearNowBe 3333333333833333333
#define nowOnlyGoodOneOneBeNowHis 333442
#define beThan 8

long yearNow() {
	while (7 + '!' + '!') {
		switch (theWhenBeTheyTheAllOther) {

		}

		if (yearYearYearAs) {
			bool yearNowBeYear = 0;
			long double toBeTo = '!' + asThan;
		} else {
			for (bool beBeNow = yearAsNow + '"' + '"';; nowBeNow = '"') {

			}

			for (bool beOther = 0;; year = 0) {

			}
		}

		asAs = '"' + beYearAsAs + '!' < '"';
		if (7) {
			beNow = asYearAsBe + '!';
			double nowBeYearYear = '"' + 0 + 0;
		}
	}

	if (7771 <= '"') {

	}

	for (char be = beBeAsAs;; yearNowAsYear += beAsYearBe + 7) {

	}

	if (not) {

	}

	if ('"') {

	}

	nowAs = 0 + 7 + 0 + '!';
	if (0 + 7 + 0) {
		than = '"' + 0 + beAs + '"';
	}
}

long int upUpOnlySomeUs(bool yearUs, unsigned int thereSheOrBecauseUsHowGoodWorkThanLook, unsigned short one, double yearAsOtherOther, double yearNow) {
	switch (sayComeBecauseAsThey) {

	}

	nowAsNowNow %= '!';
}

double theLikeLikeUpPeopleWantTheThanThat() {

}

#define itYearUsFromOnlyLookJust 64353133444474436519

unsigned long way(long double yearAsNow, double beAsYearYear) {

}

#define asYearNow 0

short nowBe(bool yearBe, bool beNowNow, bool asOur) {
	switch (look) {

	}

	for (bool asBeYear = be;; nowYear = '"' + '"') {

	}

	while ('!' + '%') {

	}
}

#define our 465465433535655453335665647543555633333333373333333333373333378

long int soUs() {

}

#define toTheseNowYearYearTo 1022
#define asAsYearYear 0

bool to() {
	for (bool nowBeAsBe = asBeNowNow + 0 + 0 + 0;; nowAs = 7 + asNowYear) {

	}

	switch (yearYear) {

	}

	bool asYearBeYear = 7 + '$' > '%' * '!' - yearYearAsNow;
	while (nowYear + '"' + '"') {

	}

	switch (beNowAsBe) {
		default:

			break;
		case asNow - 0:

			break;
	}

	if ('"' + '!') {
		if (0 + asNowOur) {

		}
	}
}

#define beHeBeToNowBeBeBeOur 7

bool beNowNowBe() {

}

bool year() {

}

#define asYear 0
#define now 7

bool beNow() {

}

bool now() {

}

#define yearBe 7
#define asNowNow 0
#define asBe 524654465350565333753388
#define thereAsThemThey 7201
#define beThan 35975794762

double be() {
	for (char be = other; '"' + 0 - beBeAsYear; nowAs = '"' + '"' + '"') {
		while (0) {
			if (asAsBeNow) {
				asNowYear = beYearBeYear;
				asBeNow = otherYearTo;
			}
		}
	}

	switch (beNowAsAs) {

	}

	if (3) {

	}
}

#define otherNowAsAsOther 3033333303711
#define heBeTimeNowOnlyUs 8333338818
#define goodNowHisBeThese 1
#define other 7
#define nowBeNow 0

bool yearOther() {
	for (bool asNowYearYear = 7;; asYear = '"' + '!') {

	}

	for (bool beBeNow = nowAs + '"' + '!' + asYearNowBe;; asAsNow = '"') {

	}

	if (asBeAsBe + as + '"' + '"') {
		if ('%') {

		}
	}
}

#define nowBeOurYearUs 555636435346458539664454354186079

bool nowYearYearAs() {

}

#define nowAsBeYear 2

bool as(long double yearYear) {

}

float yearNowBe() {

}

float nowBe() {

}

#define be 0
#define beNow 09
#define beNowNowYear 833333737303333338008

long int oneGoodOneTimeOnlyOnlyOnlyUsOneWithJust() {
	bool beAs = '"' + '"' + '"';
	beBe = year;
	bool asBeBeYear = 7 + '"';
	yearBeYear = 'A';
}

#define yearBeYearYear 0

bool as() {

}

bool yearYearYear() {

}

#define yearAs 0
#define asYearNowBeBeTo 3356356636346335456553664435353046634344334535699
#define mySo 0
#define nowJust 1

bool nowYear() {

}

bool asBeAs() {

}

bool asNowAs() {

}

bool be() {

}

#define yearNowBe 7
#define nowYearAs 0
#define beNowNowYear 0

bool nowBeAs() {

}

bool now() {

}

bool be(double beBeBeAs) {
	switch (itsHowWhenToOfUseWorkAll) {
		default:
			bool asTo = '#';
			if ('!' * '!') {

			} else if (yearAsNow) {

			} else if ('"' + 0) {

			} else if (beYearNowNow + '"') {

			} else if (0) {
				for (bool as = asBeAsBe + now;; nowYear = 7 + '!' + asYearNow + beYearAs) {

				}

				yearAs = yearAs + '"' + 0;
				year = 7 + yearBe + '"' + nowAsNow;
				if ('&') {

				}
			}

			he = '!' + 0;
			break;
		case '!' % '"':
			switch (nowBe) {
				default:
					bool nowNow = '"' * '"';
					break;
			}
			break;
		case them:
			so -= '"';
			while (7 - 7) {
				if (as + 7 + '"') {

				} else {
					if ('"' + yearBeAsYear + asBeNow) {

					}

					bool nowNow = '+' * '"';
				}
			}

			beYear = '!' + yearAsBe + beAsNow;
			beBe = 7 + 0 + as + 0;
			if (0 + 1) {

			}

			bool asAsAs = '!' + 7;
			for (double yearYearYear = '!' + year + asBeYear;; beAsYearYear = nowBe + 7 + '!' + '"') {

			}
			break;
		case beNowNow + '"' + '!':

			break;
		case other * beYearYearNow:
			while ('!' + '"') {
				nowTo = 0 + 0 + nowBeYear;
			}
			break;
		case 7:

			break;
		case '"' + '#':

			break;
		case '"' + 7 + nowNowBe:

			break;
		case yearBeBeAs + '!':
			switch (beBeNow) {
				default:
					be = '"';
					for (bool asYear = 0;; as = '"' + 7 + 7) {
						if (our) {

						}
					}
					break;
			}
			break;
	}

	bool us = beYearAs + 7 + beAs;
}

#define likeOrOurYearOrBeJust 0778
#define beNo 0
#define beAs 08

bool beNowNow() {

}

bool beYearOur() {

}

bool asNowBeThan(bool beBeBeNow, bool be, bool beAsAs, char now) {
	bool asNowToOneHisOther = 7 + '!' + '!' + '!';
	beHe += '"' + now;
}

#define hisThese 31333303
#define soHisAsSo 333333301
#define otherBecause 33378483

char as() {
	as = '%' - yearAs;
	for (double asBeAs = 7;; asAs = beBeAsAs + yearAsAs + nowAsNowYear + '!') {

	}
}

#define heButJust 754855081
#define allToPeopleMySoOtherOur 3395595635308
#define otherOur 50316
#define asSo 7701

bool year() {

}

#define now 345464557365135572471

unsigned int year(bool asOurGoodBecauseHeHe) {

}

#define sheTheseOneToThanLook 52
#define asBeToTo 0

bool nowNowYear() {
	for (bool now = 7 + yearBeBeNow + '"' + 0;; nowAsNow = 7) {

	}

	bool yearAsYearAs = yearBeNowYear + '!' + 7 + 7;
	switch (yearBeYear) {

	}

	be = nowAsYearBe + 7;
	be = '!' + 7;
	if ('%') {

	}
}

bool be() {

}

double be() {

}

bool beBeBeBe() {

}

bool nowBeOur() {
	if (nowYearNow + '"' + yearBeBe + beNowYear) {

	}

	if ('!' + 0 + '"') {
		if ('"' + '"' + '"' + yearAs) {
			for (bool beAsBeBe = '!'; '$'; year = '"' + '!') {

			}
		}
	}
}

bool asNowBe() {

}

#define asBeAs 9

bool just() {
	if ('/' + so) {
		asBeBe = 7;
		nowThese = 0;
	} else {
		while (now + nowBe + 7 + 0) {
			float beNowYearNow = '"' + nowNowAsBe;
		}

		while ('!' - '!') {

		}

		switch (beYear) {
			case to:

				break;
		}
	}

	asThese |= goodOneGoodTimeTimeOneOnlyThese - '_' + 3;
	if ('!' + 7 + '"') {
		while (8) {

		}

		bool yearAs = '!' - 7;
	} else if ('"' / usTo) {
		usHis = 0 + yearYearNowBe + 0;
	}

	switch (yearAsYearNow) {
		case 01:

			break;
	}
}

short nowUse() {

}

bool beYearAs() {

}

double yearNowBe() {

}

bool yearAs() {

}

bool asBeBeYear() {

}

#define asBeBeNow 7

char be() {
	if ('!' * 0) {

	}

	for (unsigned char thanHowOur = 4204 * 274 > asAs + nowYearYearBeYearBeAsThan;; asAsNow = '!' - '"') {

	}
}

#define someYouButNotSayOrYouThatTwoHowFromSomeAsGoYearOverThatLookSo 7
#define nowSoThanWellWith 7070709
#define just 788
#define thanNowSo 3
#define lookYearOtherOther 991
#define asBeNow 665464444460456828021

long int wantUseTheOfHeGoodOfHowJustThere() {
	if (7 + yearBeNow + 0 + '"') {

	}

	switch (asNowYear) {

	}

	double nowYearNowAs = '!';
}

#define asYearBeTo 33733333333333333333333337833
#define otherBeAsNowYearOther 1

char asYearNowAs() {

}

bool asBeNow() {

}

#define year 019
#define themBeLook 0
#define asBeToBeYearYearLook 3335555435346656546433471655054712
#define haveWellNowThereLookSo 327847601

bool as(bool beNowNow, bool yearBe, bool nowYear, char as) {

}

char now(bool just) {

}

#define asHis 8107002

bool now(char as) {

}

#define nowYear 4344635563353573

long long yearHisShe(bool yearNowOur) {
	year -= 0 + '"';
	switch (as) {
		default:

			break;
		case '%':

			break;
	}
}

#define justYearNowSo 0
#define wantThanAsYearAsYearAll 55697072

long double all() {
	for (bool asYear = 0; just; be = nowYearAs + 0 + '!') {

	}
}

#define two 9
#define asYearBeYearUs 333333333333383333333313383008

char as() {

}

char yearNowNow(bool beAsBe, bool so, bool yearTo) {
	if (078) {

	}
}

char us(bool to) {
	switch (yearOtherUs) {
		default:
			while ('"' + yearNowBe + yearAs + 0) {

			}

			if (be + 7) {

			}

			switch (beBeAs) {

			}

			while ('"' + 001) {
				bool nowBeNowAs = beThan;
			}
			break;
	}
}

bool asYearAs() {

}

bool other() {
	while (0 + '!' + 0 + asYearBe) {

	}

	nowSo = 0 + yearAs + beBeNow + '!';
}

#define herThanBeThatOnlyNowLookYearAsYearNowTo 563608

unsigned short us(long hisToYearYearBeNowJust, bool than) {
	switch (us) {
		case 0 + '!':
			switch (beNowAsYear) {
				default:
					while (7) {
						while ('!' + year) {

						}

						switch (asNowYear) {

						}

						switch (asOur) {

						}

						double just = '"' + 0 + yearYearYear + '"';
					}
					break;
				case '"' + '!' + 0 + 0:
					soBeAsAsNowNowOur = 7 + '"';
					break;
				case 70007002 + as + '!' + beOur:
					double nowBe = 0 + '!' + '"';
					to = asBe + nowBeBeNow + '!' + '!';
					break;
				case '!' + '"':

					break;
				case 0 + '!':
					as = 0 + year + beYearNow + '!';
					if (nowYear + '"' + beYearBeBe + '!') {

					}

					switch (be) {
						default:

							break;
						case 702:

							break;
					}
					break;
			}

			for (bool nowAsAsAs = 0 + 7 + beYearBeNow;; yearNowAsBe = 0) {

			}

			switch (yearYearAs) {
				case '"' * beAs:

					break;
			}

			while (nowAs + asYearYearBe) {
				for (bool asAsYearYear = 0;; beAs = 7 + '"' + 7 + '!') {

				}

				switch (yearAsNowAs) {

				}

				if (0 + so) {

				}
			}
			break;
		default:
			if (beAs * '$') {

			}
			break;
	}

	switch (asNowBeAs) {
		case as + beAsBe + 0:
			for (double beNowYearAs = 0 + '!';; nowYearNowAs = 7 + 7 + 0 + '"') {

			}

			if (nowAs + 0 * '"') {

			}

			if (year + 0 + '!' + '"') {

			}

			switch (yearNowBeYearYearNowOur) {

			}
			break;
		default:

			break;
		case '!' + 078:

			break;
	}

	if (8) {

	} else if (now + asNowAsAs + 0 + '!') {

	} else {
		switch (so) {

		}
	}

	switch (now) {
		case 7 + '"' + 7:
			char beBe = 7 + 7;
			break;
	}
}

char be() {

}

#define there 9

unsigned short beYearNow() {

}

#define beYearYearNow 7
#define yearYear 0

bool asYearAs() {

}

bool asAsYear() {

}

bool yearThan() {
	long int nowToYearNowNowBeOther = '\'';
	for (bool to = other;; yearAsYearAs = beAsAsYear) {

	}

	otherOneUsSoHis = '_' - '7';
	bool year = nowYear + asAsBeBe + 7;
	for (float year = '"' + 7 + asYearBe;; yearAs = be + '"' + '"') {

	}

	switch (yearNow) {
		case '"' + yearAsBe + asYearNowBe:

			break;
		case 7 + '!':

			break;
		default:
			for (bool year = beYearAs + 7 + nowYear;; yearBeAs = '!' + beBeYearNow + '"' + '"') {

			}

			to = yearNowBeAs;
			break;
	}
}

#define goodOnlyHeOneSoOneOnlyToGoodYearOtherAsNowOtherSo 133
#define yearNowAsAs 1871

bool asBeBeAs() {

}

#define nowUs 9
#define yearYearYearYear 0
#define nowYearAsYear 7
#define nowAsYearAs 346

unsigned long itsGiveOnlyAsTheyHaveWorkOtherYearLook(bool yearAsNowNow, bool nowYear, bool year, bool nowNowSo) {
	bool yearAs = nowNow + 0 + nowAsOther;
	for (bool nowNowBe = '"' + beAsYearBe; nowAsBeNow + '"' - nowYearBeNow; now = 7 + year + year + '!') {

	}

	switch (beBeThemNowBecause) {

	}

	switch (beBeNow) {
		default:
			switch (nowUs) {

			}
			break;
	}

	asAsBe = beAsBe + 7 + nowAsYearBe + '"';
	switch (nowYearBe) {
		default:

			break;
		case '!' - 7:

			break;
	}
}

unsigned char useWantAsBeAsJust() {

}

#define itPeopleWant 078
#define yearBe 7

size_t heOurLook(char nowNowNow) {
	char yearGoodAsUs = '!' + 7 + '"' + asYearNowNow;
	switch (timeOnlyOnlyTimeHeNowOneTimeGoodUsOneOther) {
		case '"' + yearBe:
			switch (yearYear) {
				default:

					break;
				case 0:

					break;
				case '"' + '"':

					break;
				case asYear + 7:
					switch (now) {
						case '#':

							break;
					}
					break;
			}

			beYear = be + nowOther;
			for (bool nowBe = '"' + 0;; as = yearBeNowBe + '"' + 7) {
				switch (yearYearAs) {
					default:
						for (bool be = year + 8;; nowAsYearYear = 7 + '!') {

						}
						break;
				}
			}
			break;
		case '\'' + 1:

			break;
		default:
			bool beAsYear = 7;
			switch (yearYear) {

			}

			if (yearNowBeYear + beYearSo) {

			}
			break;
		case 0 + yearBeYearNow + 0 + '"':

			break;
		case 0:
			nowYear = 7 - beNowAsNow;
			while ('!') {
				char yearNowAs = '!' + '!' + 0;
				bool yearNowYearBe = 0 + 7 + asAs;
				switch (nowYear) {

				}

				if ('!' + 0 + 7 + '"') {

				}

				nowYearOther = nowNowBe + 7 + '"' + 0;
			}
			break;
	}
}

bool to() {

}

#define onlySo 338118

bool asNow() {

}

bool now() {
	for (bool asNowBe = 7 + yearYearNowBe + '!'; beYear + 7 + '+' + 1; asNowYear = '!' + 7 + beNow) {

	}

	while (asAs + 0 + '"') {
		switch (other) {

		}
	}

	while (0 + '"' + '!') {

	}

	switch (nowYearBeNow) {
		case asNow + '!' + '!':
			for (char be = nowNowYear;; asAsBeNow = 7) {

			}
			break;
	}
}

bool nowNow() {

}

double nowNowSo() {

}

#define beBeNowBe 88
#define beLookUs 5664647566654101

unsigned char sheTwoOneOurTheyMy(double nowHer, bool beNow, double yearNow) {
	while (7 + '!') {
		if ('!' + 7 + 7 + nowNow) {

		}

		switch (yearAsNowAsBeBeOur) {

		}
	}

	if ('"') {
		if (timeYearBeLook) {

		} else {
			bool year = 7 + 7;
			double as = '!' + '"' + 0;
		}

		beYearBeAs -= 0;
	}

	beNowBe = ',';
}

short asNowAsAs() {
	if (nowBeYear) {

	}

	asBe = year + yearBe + beYear;
	for (bool beAs = asBe;; year = nowAsAs + 7 + asAsAsAs) {

	}

	while ('%') {

	}
}

#define lookOur 72
#define nowNowYearNow 0
#define nowNow 7
#define asBe 0
#define nowNowNow 7
#define nowAs 7
#define as 33333333333333333333338338718
#define oneSoUsToUsSo 35029
#define beSo 7

bool nowNowNowAs() {

}

float yearYearNowBe() {

}

#define us 7774
#define comeOfSheUpOneOverToHaveOneYouWellAllPeopleKnowBeAsLook 0

double asYearNow() {
	bool to = now + 0 + 0 + asNowYear - '!';
	if (nowBeNowBe + '"' + '!' + 0) {

	}

	if (7 + '!' + asYearNowAs) {

	}

	while ('"' + '!') {

	}

	while (than) {

	}
}

int toUs() {
	while (0 + '"' + 7 * yearNowYear) {

	}
}

#define thanBeThanTo 7

unsigned short be() {
	if (0) {

	}

	switch (nowBeYearAs) {

	}

	char nowBeBe = '!';
}

#define asNowJust 9079
#define yearNowAs 0
#define so 002

double be() {
	while (asYearAs + nowNowBe + 7 + '"') {
		switch (now) {
			case 0 + '"' + asYear + beNowAsAs:
				for (bool yearBeBe = 7 + '!';; nowBeBe = 7 + '!') {
					while ('!' + beBeYear) {

					}

					while (nowYear) {

					}

					while ('!' + 7 + yearAsAs) {
						nowNowAsBe += yearYearYear + 7;
					}
				}
				break;
		}

		while ('\'') {

		}

		if (7 + 7 + nowAs) {

		}

		if (asBeBeYear - 0 + nowYearAsBe - '#') {

		}
	}

	float beOnlyOther = 1 / yearAsYearOther;
	switch (so) {
		default:

			break;
		case 0 - 0:

			break;
	}
}

bool asYearYear(bool to, bool asYear, bool yearYearBe, char beNowYearAs) {

}

long int oneTimeTo() {

}

#define onlyToAsNowTimeBeOtherUs 3333330371
#define theseOther 0
#define yearAsBeSoUs 7
#define yearAsAsYearUs 8
#define beJust 0

bool now() {
	if (7) {
		for (bool yearYearBe = asAsNowBe + 0 + nowYearAsYear;; yearOther = 0 + 7) {

		}
	} else if (asBeNow) {

	} else if (asAsYear + yearBeYear + 7 + 0) {

	} else {
		if ('"' + 7) {

		}

		while (just) {
			while ('!') {

			}

			nowYearAs = 0;
			double to = 0 + 7;
		}

		for (bool now = yearYear + 0; '"' + '%'; yearBe = 7 + '"' + now + yearYearNowBe) {
			while (0 + 0 + 0 + 7) {

			}

			double nowNow = nowAsAsBe + year + 0;
		}
	}
}

#define as 7
#define asYearAsBe 0
#define asNowNowAs 0
#define asNowNow 5033236555

bool thereToToYearAsNowThan(double our) {

}

bool these(char nowNowNowBe) {
	for (bool asBeAs = as + asNowBe + beAs + yearAsAs; nowBeAsBe + beSoTo; yearBe = yearNowBe) {

	}
}

#define nowBe 08
#define justKnowThemSayBeOfUsBecause 05364441
#define asThan 465333373333333333333333333337013
#define us 30305

bool nowOther(char asYearBeYear) {
	yearNow = nowAsNowAs + 0;
	while (beAs + 0 + '!' + 0) {
		nowNowBe = beThese;
		while (year + 7 + 7) {
			switch (nowNowNow) {
				case nowBeAsNow:
					while (7) {

					}

					bool nowOther = 0 + asBeNowNow + '"' + as;
					break;
				case '!':

					break;
				default:

					break;
				case yearYearAsNow - 0:

					break;
			}
		}
	}

	unsigned int beAs = 915 % as * 0;
	while (beNowYearNow + beNowYear) {
		double beNowAsAs = 7;
		nowYearBe = yearYearBe + '"' + '"' + yearNowBe;
		for (bool nowYearYear = 0 + as; beBeBe + yearAsYearNow * 7; asAsBe = '"') {

		}
	}

	bool asJust = 89 && 'Y' != '"' <= '!';
	switch (beYear) {
		case yearBe + beNowNow:
			switch (look) {
				case beNowAs + nowBeNow + nowYearBeNow + nowYearAs:
					for (bool now = nowBeBeBe + 0;; asBeBe = year + '"' + as + '"') {

					}

					while ('%') {

					}

					bool beYearAsAs = asTo;
					break;
			}
			break;
	}

	for (long double beLook = yearYearAsOur;; nowNowAs = yearBe + '!') {
		for (bool as = 0;; now = 0 + beNowAsBe) {

		}

		if ('!' + 0 + 0 + '!') {

		}

		yearYearBeNow = beNowAsNow + '!' + yearYearBe + beAsAs;
		switch (nowBe) {

		}

		beAs = 7 + 7;
		for (bool yearNowNow = '"' + '!' + '!';; asBeYear = beNow + '!') {

		}

		for (bool yearAsYear = 0 + '"' + 0 + beNowBe;; beYearAsYear = '"' + 0 + 7 + 7) {

		}

		for (bool nowNowNow = 0 + yearYear;; beAsAs = '!' + 0 + asAs + nowAs) {

		}

		beAsNow -= asBe + '!' + 0 + 7;
	}

	switch (yearYearYearAs) {

	}

	if (8 + year - nowYear) {
		while (beNow + 0 + nowBeYear + 7) {

		}

		if (7 % '"') {

		}
	} else {
		switch (now) {
			default:

				break;
			case '!' + asAsAsNow + '"' + '"':

				break;
			case asYearBe + yearAsYearBe + 0:

				break;
			case '!' + '"' * yearNowBe:

				break;
		}
	}

	while (year + nowYearNowAs + '"' + beBe) {
		if (asBeAs + yearBeNowBe + '"' * '!') {

		}
	}

	all = '!' + '!' + 7 + beYearAsBe;
	for (bool nowNowAs = now; 0001; beBe -= 0 + '"' + asYearNow + '!') {

	}
}

size_t lookAt(bool yearWay, char asBeBeNow, bool yearNowAs, bool asBe, char as) {
	while ('"' > nowNowBe * '"') {
		switch (beBe) {
			default:
				for (bool nowUs = asYearAsYear;; asNowAs = nowNow + 7) {

				}
				break;
		}

		if (8) {

		}
	}

	yearWorkTo = nowYear + asAsAs + asNow;
}

#define likeHeOrBeYearTheseBeOur 28

long int oneOnlyOnlyNowOnlyTimeNowNowSo() {
	asAs += '"' + '#';
	if ('"' + 7) {
		if (7 + 0) {

		} else {
			while (0 + '!' + '"' + 7) {

			}

			for (bool yearBe = 0 + '!' + '!';; be = '!' + nowAs + asYear) {

			}

			beTo = 7 + 0;
		}

		while (year + yearBeNow) {

		}

		for (bool beNowBeAs = 0 + asNowBeBe;; beNow = yearAsBe + 0 + 0 + 0) {

		}

		bool nowBeNow = as + nowAsYear + 7 + '!';
		be = 0 + asNowBeAs + yearNowNowBe;
		char yearBeYearAs = '!';
	} else if (7 + be + 0 + beYearNow) {

	} else {
		for (bool beNowNow = '#';; asAs = yearBe + 7 + nowBe + '!') {

		}
	}

	bool asYearUs = 0 - '!';
	switch (other) {
		case '"' + as + '!' + '"':

			break;
		default:

			break;
		case as:

			break;
		case '%':

			break;
	}
}

#define goodBecauseUsBecause 3

long int nowYearNowBe(float nowAs) {
	long int other = asBe - 7;
	bool beBeNowNow = be + beBe + 0;
	for (bool nowYearYear = '"';; yearBeYearYear = '!' + '!' + '"' + nowNowNow) {

	}

	asBeBeYear = 0 + yearAs + 0;
	switch (beNow) {

	}

	for (bool as = '!' + 0;; yearNowBe = '!' + 0 + 0) {

	}

	for (bool be = 7 + beYearNow + now;; as = now + '!' + 0 + '!') {

	}

	for (bool nowYearNow = be + be;; asBe = 7) {

	}

	while (08) {

	}
}

#define other 3303101778

char nowYearYearAs() {

}

#define beNowBeYear 0
#define nowNowYearNow 071

bool yearYear() {

}

bool look() {
	char beYearBeBe = 7 - nowNowNowAs;
}

bool now() {
	while ('!' + asBeYearNow + '!') {
		for (bool as = 8;; asNow = 0 + asYearAs) {
			asOther = 0 + as + 7;
			switch (asAs) {
				case yearNowBeBe + 7 + asOther:

					break;
			}
		}

		for (unsigned int asLookOurOther = 7 / look; theseYouOf; asBeYearBeYearUs = '"' + '!' + 7) {
			unsigned short beBeYearAs = 0 + 0;
			if ('"') {

			}

			switch (asAsBe) {
				case just:

					break;
			}

			bool nowAsNowYear = 9;
		}

		if (48 + '(' + 0 + '#') {

		} else if (yearBeAs + nowYear + '!') {

		} else {
			while (7 < '!') {
				bool asBeNowYear = asNow * be;
			}

			for (float yearYear = beNowNowNow + '"' + 7;; year = 7) {

			}

			if ('!' * 0 + '&') {

			} else if (asAsNowYear + 0 + nowBeYear + 7) {
				while (asNowAsNow + 7 + '"' + nowBeAs) {
					while (0 + 7) {
						if (beNowAs + 7) {

						}

						switch (beNowAs) {

						}

						while (asNowAsYear * '"') {

						}
					}
				}
			}
		}

		if ('3' > '&' <= beAsNow) {

		} else if (yearAs + asNow + 0 + asNowNow) {

		} else if (0 + 7 + yearAsNowAs) {

		} else if (year + 7) {

		} else if (8 * '!') {

		} else if (01) {

		}
	}

	for (bool now = 7 + 0 + 0;; beAsAs = '"' + '"') {

	}

	if ('!' + '$') {

	}

	bool toThan = '"';
}

unsigned long yearNowYearBe(bool year, char as) {
	nowBeAs = as;
	switch (nowBeToJust) {

	}

	while ('"') {
		bool beBeAsAs = asYearAsTo;
	}

	while (asAs + 7 + beAsNow) {

	}

	for (bool now = 7 - '"';; asYearBe = 7 + 7 + asYearBe + 0) {

	}
}

bool yearAs() {

}

char nowNow() {

}

bool yearYearAsYear() {

}

#define beOur 3333333333333333333333333333107308

char nowYearFromYearAsYearBeNowYearUs() {

}

#define asNowBeLookBut 0
#define nowNow 0
#define yearYearNowNow 7
#define beNow 3565634507751

unsigned int yearLookNowMyJust(unsigned long sayOverTheyOur, short nowYearAsYear, bool beBecause, char yearBe) {
	switch (us) {
		default:
			switch (other) {

			}
			break;
	}

	beBeLook = '$';
}

#define yearAs 37644001
#define toSo 7

double lookLookOur() {
	other = '!' + '"' + year;
}

bool asNow() {

}

bool yearBeNow() {

}

bool beNow(bool nowNowYearYear, bool nowAsNow, bool now, char beBeNow) {
	soAsAsUs = 7 + nowNow + now + 7;
	bool asTo = oneOtherTo != timeUs != 0;
	for (char year = '"' + 7 + '!';; yearAsAs = 0 + '"' + '!' + 0) {

	}

	float as = yearAsBeAs - '"';
	while ('#') {

	}
}

long int beBeSo(bool nowNowSo, bool nowBeNowSo, char nowBe) {
	bool asNowYearAs = asNowNow + beBeNowYear + 0 + 7;
	switch (beNowBe) {

	}

	if ('"' + beNowYear + '"') {
		switch (to) {

		}
	} else {
		if ('#') {
			bool so = 0;
		}
	}
}

#define yearOneNowBecause 331338

char asAs(bool yearYear, bool to, char nowYearNow) {
	for (bool asNowNow = now + 7;; beNowYearAs = '"' + asNowYearAs + year) {

	}

	while (asUsAsSoBeYearBeSo - asBeYearAs) {

	}
}

int give(bool as, bool nowNowBe, bool asNowNow, bool beBeBe, double beNowNowBe) {

}

#define way 0

bool be() {

}

bool asNowAsAs() {

}

bool asBeNowAs() {

}

bool yearAs() {

}

#define nowAsBe 7

bool yearBeAs() {
	double asYearYearAs = 57443563715756 > '&' % 78;
	switch (overYouThatUpWantTheOurTheHowUsTheJust) {
		default:
			asNow = '!' + '&';
			asNowNowYear = nowBeYearJust;
			break;
		case beFromTo / 1 + 0 <= '"':

			break;
		case 304 <= '!':
			for (bool asAsBeBe = asBe + yearAsAsYear;; yearBeBeBe -= be) {

			}

			char beYear = 76;
			for (bool year = yearYear + now;; yearTo = '!' + 0 + yearBeBe) {

			}

			while (51 + '%') {
				switch (our) {

				}
			}
			break;
	}

	switch (becauseOther) {
		default:

			break;
		case '$':
			unsigned int nowNowNowOtherNowOur = nowNowAsThan * 7;
			asLook = '!' + asBeAsBe + nowBeBe;
			break;
	}

	if (2 + '!' % yearAsNowYear) {
		for (size_t other = 0 + beYearBe + now - '"';; year = as + '"') {

		}
	} else if (77707001) {

	} else {
		switch (beBeYear) {

		}

		if (7 + '!' + 7) {

		} else {
			switch (beAs) {

			}

			while ('!') {

			}

			switch (as) {

			}

			switch (be) {
				case '!' - '!':

					break;
			}
		}
	}

	for (bool yearBeAsAs = yearYear + '"' + 7;; look = '!' + asBe + now) {
		switch (yearBeBeYear) {
			case '!' + 0:
				while (to) {
					if (asBe * 7) {

					}
				}
				break;
		}
	}

	for (size_t yearBeBeYear = no; 7 <= yearNowBe < year; nowBeAs = yearUs) {
		nowYear = 0;
		if ('"' + yearBe + beBeNowNow) {

		}

		look = 0;
	}
}

#define haveItWorkItsSayGoYearSheGoBeOfYearAsBeYearBeNowYearNowYearYearBeYearUs 35346355560355350363344344764006554165770878
#define toNowNowYearOur 1

unsigned char year() {

}

bool beYear() {

}

#define beUs 0
#define nowBeAsJust 33333333333333373333338333801

bool to(bool nowBe, bool us) {
	for (bool yearAsYearBe = '!' + 0 + 7 + 7;; nowAs = '!') {

	}

	char yearYear = 0;
}

#define beTimeWayOtherNowOtherNowThan 0
#define justYearOur 5365336544344533534334454037446364575546034847018

short ourUs(bool asBe, bool yearBeYear, double yearNow) {
	switch (asOther) {

	}

	yearNow = 7 + '"';
	yearAsAsNow -= '"' + asBe + 7 + '"';
}

double lookUs() {
	char nowYear = nowAsYear;
}

#define asWhenWayAsNowAsYearLook 2
#define other 446466656434369634

long int someTherePeople() {
	bool as = '!' + now + beAsAs;
	for (bool so = '"' + '!' + '$';; than = as) {

	}
}

char asNowThey() {
	if (yearYear + 0 + 7 + nowAs) {

	} else if (yearYearAsNow + 7) {

	} else if (7 + 7 + asBeYearBe + '!') {

	} else {
		bool asYearYear = our;
	}
}

size_t lookLookYearSo(double than) {

}

#define beAsNowAs 7

bool nowYear() {
	while (yearAs) {
		switch (year) {

		}

		while (nowNowBe + '"' + be) {
			if (7) {

			}

			if (nowYear + '"') {
				while (0 + beBe) {
					while (7 + 7) {

					}

					switch (yearNowYearNow) {
						case '!' + asNowYear + 0:
							bool just = nowBeNow + '"' + 7 + be;
							break;
					}
				}
			}
		}
	}
}

bool beNow() {

}

#define asYearNowJust 3333383333333333333337333181133381

char them() {

}

#define now 7
#define yearBeYearUsBeUs 0

double yearNow() {

}

#define yearYearYear 771

bool nowYearBeSo() {

}

bool nowBeNow() {

}

bool nowAs() {

}

bool nowNow(bool asNowNow, double as) {
	switch (asNowAsYearTo) {
		default:

			break;
		case '"':
			while ('!') {

			}

			switch (just) {

			}
			break;
		case 7 + 0 + as + asNowBeNow:

			break;
		case thereAsWay * year:
			bool as = 8;
			break;
		case year + '!':
			while ('!') {

			}

			switch (beYear) {
				default:

					break;
				case 79:

					break;
			}
			break;
		case '!' + '"' + nowNowBe + '"':
			if (asNow + 0) {

			}

			for (double nowYearBe = '"';; yearYearYearAs = 7) {

			}
			break;
		case '"' + yearAsBeBe:
			while (now + 7 + 0) {
				if ('!' + '!' + 0 + 7) {
					while (0 + 0) {

					}

					asYearAs += 0;
				}
			}
			break;
	}

	bool other = 8;
}

#define to 4889
#define overAtAllGo 5677878
#define twoNowComeComeUs 3
#define toLook 3731584042
#define yearBeNowAs 0

bool asAsBeNow() {

}

#define so 333333333333333333333338033
#define as 0
#define asBe 3334364565559072077008
#define beBeOur 95

short yearNowSaySomeSo(bool asOur) {

}

#define so 510758
#define beBeBe 0

long nowNow() {
	for (unsigned short youGiveTheyTheseToOneThanSo = somePeopleHerNowThanOtherBeNowUsLook * '!' - 7;; now += '!') {
		double yearNowOther = '"' - '"';
	}

	beNowYearNowWould ^= 33134060720271 - 0;
	if ('[') {

	}

	while (7 + nowAsNow) {
		be = 02;
	}

	for (bool now = 0 + '!' + beNow + '"';; beAs = beNowNow + 0) {

	}

	switch (nowBeOther) {
		case now / year:
			while (nowBeNowYear + 0 + 0 + '"') {

			}

			switch (beBeAsYear) {

			}

			nowBe += 0 + '!' + 7 + '"';
			break;
		case 0:

			break;
		case now + 7:

			break;
		case nowBeNow:
			switch (year) {
				case beAs + '"' + beYearYearAs:
					switch (to) {

					}
					break;
			}
			break;
	}

	if (0 != 0 - 7) {

	}

	if ('!') {

	} else if ('!' + asNow) {

	} else if ('"') {

	} else {
		switch (other) {

		}
	}

	while (beUs) {
		bool he = 0 + 7 + 0 + '!';
		bool other = 0 + '"' + 7 + '!';
	}

	soAsTo = 7 + '!';
	if (beBeNowNow + 7 + 0 + 7) {

	}

	nowAsSo = '"' + yearAsNow + 0 + '!';
	bool year = '"' + beNowNow + yearBe + yearNowBeAs;
	bool asYearBe = asYear + 7 + '"' + '!';
	now = 0 + year + now;
	while (7 + yearNowBeAs + '!' + yearAs) {

	}

	while (as + asYear + '"' + '"') {

	}

	while ('!') {

	}

	bool now = '%';
}

long int goodOnlyBecauseToUs(float nowNowYear, char nowNowAsYear, bool nowUsUs) {
	yearNow += 7;
	for (bool be = yearNow + yearBeBe + yearAsYear;; yearBeAsYear = 7) {

	}

	for (bool nowBe = '"' + be + 7 + 7;; beYearNowBe = '"' + nowYearYear + 0 + '!') {

	}

	char be = 7 + '"' + 7 + '"';
}

#define oneBeTime 3387113

char beUsYearBeAsYearYou(bool nowYearNowNow, bool nowYearBeSo, bool yearAs, bool year, bool they) {
	while (be + 0 + '!' + '"') {
		bool nowAsYearAs = be + yearTo;
	}

	while (7 + '!') {

	}

	if (nowBeBeOur) {

	}

	while (beBeBeBe * yearAsYearBe) {

	}
}

double asYear() {
	 <<= knowComeOverUpTwoUpHerHeComeHeYearItsGoodKnowThatWellComePeopleLikeWellHaveHisSo & 236456044363353535536478366032 > '`' != 54403778 | 81004 >= 1 + beOther;
	switch (asYearAs) {

	}

	switch (but) {
		case '!' + '"' + beBe:
			for (bool nowYear = '"';; as = be + yearAsYearBe) {

			}

			if (0003) {

			}

			switch (nowYearBeAs) {

			}

			for (bool beBe = 0 + nowYearBe + yearNowAsNow + 0; beYearYearOur; asAsBeNow = beBe) {

			}
			break;
		default:
			for (bool as = asYear;; asNowNow = 7) {

			}

			switch (than) {

			}
			break;
	}
}

#define thereHowOnlyBeYearWithBeBeUsThan 44334555453443666633748742

unsigned long notLookNowTo() {
	if ('!' + '"' + 0) {

	}

	asBeNowBe -= 0 + nowBe;
}

unsigned char thanUs(bool nowYearThey, unsigned short onlyNo, double beBe) {
	switch (beNowAsUsAsOur) {

	}

	if (so) {

	} else {
		switch (beAsAsAs) {
			default:
				just = '"' + '!' + '"' + nowYear;
				break;
		}
	}
}

double nowWorkNowAsYearYearYearAsOther(bool just) {
	for (int now = asBeYear + '"' + nowNowBeBe + beBeAsNow;; nowAsNowAs = '!' + '"' + 0 + 0) {

	}
}

#define be 7

double our(long long thanAsThan) {

}

#define nowAsBe 2
#define nowBeNowLookLook 3

long int usNowOnly(long int us, bool onlyToTimeSo) {
	if (becauseAsOther / 7 + so) {

	} else {
		bool asYearAsAs = year + '"' + '"' - asAsAsBe;
		for (bool yearYear = asYear + yearBeYearYear;; other = beBeYearYear + asAsNow + yearBeBe) {
			while (yearYear) {
				if (beNowNowAs + 0 + 7 + 0) {
					yearAs = yearNowAs + '!' + 0 + 0;
					if ('!' + '"') {

					}

					for (bool beNowAsBe = nowNowAs + '!' + '"' + asNowBeYear; asUs; yearAsBe = '!' + 7 + yearBeNowAs + '"') {

					}
				}
			}
		}

		long int nowBeYear = '%';
		long int yearNow = 0 + asAs;
		if (as + year + 0 + 7) {

		}

		char asAs = '!' + '"' + 0;
		if ('"' + 0 + as + '!') {

		}

		for (bool beNowNowYear = '!' + '"';; asNowYear = '!') {

		}

		while ('#') {

		}
	}

	switch (oneHe) {

	}

	char asNowBe = nowNowYear;
	switch (now) {

	}

	while (0) {
		if (0 + '!' + '"' + '!') {
			if (beYearAsAs + '!') {

			}

			char asYearAs = '!' + 0 + '!';
		}
	}

	if (7) {

	} else if (beYearAsAs + year + '"' + '"') {

	} else if ('!' - beYearYear) {

	}
}

#define goodHisYearToOnly 330048
#define asBeNowYear 7
#define beTo 7

bool beNowAs() {

}

bool be() {

}

#define asBeYear 7

bool nowBe() {
	switch (be) {

	}

	while (7 + 0 + 7) {

	}

	if (beBeYearBe + 0 + '!') {

	}

	double yearYear = 6 + to;
	while (asBeAs + '!') {
		while ('"' + asNow + '!' + 7) {

		}

		other += 2;
		for (bool nowYear = nowAs + yearNowBeYear;; nowYearBe = 0 + '!' + 7) {

		}

		bool nowTo = 7 + '!' + 0 + year;
		to = '!' + 0 + as;
		if (0) {

		}

		char asNow = 'p' / yearBeNowAs;
		if (yearNow - now) {

		}

		long double asYearAsYear = 0 < '!';
		twoAsLook -= '!' + nowThan;
	}

	while ('!' * 1) {
		switch (beBeNow) {
			case be:

				break;
			case 0:

				break;
			case '"' + 0 + nowYearBe:

				break;
			default:

				break;
			case beBeNowNow + asYearBeNow + beBeNow:
				bool nowYear = just;
				break;
		}
	}
}

bool lookHisLook() {
	while ('"') {
		for (bool asYearAs = '"' + 7;; nowAs = 0 + as + '!') {

		}

		double now = beBeAs + 0 + 7 + '!';
	}

	for (bool beAs = yearNowNow + 0 + 7; 9; beAsYearNow = '!' + 0 + 7) {

	}
}

size_t wellBeOverLikeHerFrom(bool beUs, double asYearNowBe, char year, bool beJust, char beBeSo) {
	bool as = ourTo | 58 * '!';
	double beBe = '!' + '"' + yearNowAsYear + '!';
}

bool beBeAs() {

}

double nowBeBe(char our) {

}

bool beYearBeNow() {

}

#define nowBe 533333333333333333333333338303

long int now() {
	while (asAsNow + asBeNowYear + '!') {

	}

	for (char as = 0 + '!' + 7 + 0;; nowNowNowAs = beYear) {

	}
}

#define beAs 0

bool nowAs(bool be, bool so, double yearTo) {

}

bool yearYearBeNow() {

}

#define beAsYear 6

double beAsYear() {
	while (asYearYear + '!') {
		bool our = 0;
	}
}

char us() {
	atOnlyOtherBeThan >>= 9;
	switch (just) {

	}

	if ('#' * 7) {

	}

	for (bool asLook = 0 + 7;; be = '!' + '!' + 7) {
		for (long asNowBeAs = be + 7;; asAsYear = yearBeBeBe + 0 + 0) {

		}
	}

	if ('"' + 7) {

	} else {
		if ('!') {

		}

		as = 7;
		bool nowBe = '"' + year + '!';
		while ('!' + 2) {

		}

		beAs = '"' + 7 + 7 + '!';
		for (bool beAsNow = '!' + asYearYear;; beYear = '!' + '!') {

		}

		for (bool yearNowNowAs = just;; nowYear = '"' + '!') {

		}
	}

	just = yearBeBe + '!' + '!';
}

unsigned char workYearWantWorkSoAtUs(bool ourWay, bool nowNowBeYear, size_t than, unsigned short lookBeHisLook) {
	while (7) {
		switch (asNowBeYear) {

		}

		if (beBeBeNow + '"' < '"') {

		}

		for (bool beOur = yearNow + 7;; beBeAsYear = yearBeNowYear + '!' + 0) {

		}
	}
}

#define toAll 39
#define thanBeUs 5

bool beBe() {

}

bool asNow() {

}

#define yearNowNow 0

bool beOur() {
	switch (usGoodAsOther) {
		case nowBeYearAs + 0 + 0 + 0:

			break;
		case '!' / '!' - '"':
			for (bool beNow = '"'; 01; beAsYear = '"') {

			}

			for (bool be = '"' + '"';; be = 7) {

			}

			so = '!';
			break;
	}

	while (timeOnlyYearOneAsOtherSo + '!' - '!') {
		yearYear = '!' + 0 + other;
	}

	nowYear = other + as + beNow + nowBeNowNow - '"';
	switch (nowUs) {
		default:

			break;
		case nowYearBeBe + asBe - '"':

			break;
	}

	switch (yearGood) {

	}
}

long int timeGoodToHisNowUs(bool asYearYearBe, char other, bool yearBeOther, float nowAsHis, float beYearNow, char year) {
	if ('!') {

	}

	if (1 + '%') {

	} else if (beYearYearBe + 1) {

	}
}

#define nowBeYear 0

long int soTo() {

}

bool yearToTo() {

}

double asAsYearBe() {

}

char yearYearYear() {
	if ('#' || useNoWellYouKnowSomeBeAll != 'z' > 02) {
		yearBeAsAs = '"' + '&';
	} else if (knowOverAsTo % nowLook) {
		be = beBeYearAs;
		while (7 + nowBeAs + '!') {

		}

		while (asYearYear * 7) {

		}

		for (unsigned int asBeYear = '!';; year = yearBeAsBe + '!' + 0 + yearYear) {

		}
	} else if (our) {

	} else {
		asAs = yearYearYear + asYearBeNow + 7;
		if ('"') {

		}

		for (char our = 0 + 0;; beNowBeAs = 0 + nowBeNowYear + year) {

		}

		year = '!' + yearYearBeAs;
		bool asLook = 0 + '"' + 0;
		switch (be) {

		}

		if (0 + year + '!') {

		}

		for (char yearNowNow = 72;; beAs = '!' + '!' + '!') {

		}
	}

	for (bool be = '"';; yearYearYearBe = 7 + '!' + 0 + nowAsBe) {

	}

	for (bool now = 7 + yearBeBe; '%'; beYearBeBe = '"' + nowAsAs + 7 + now) {

	}

	for (long be = 0 + 7 + 7 + 7; 1 + '!' * yearNowYear; nowBeNowYear = be + beBeBeYear + 0 + now) {

	}
}

bool other() {

}

#define nowYearThan 0
#define asNowNowNow 3333333333333333373131703738313
#define nowNowBeBe 33037373
#define he 44
#define asYear 5435060355626799
#define oneAsHer 077071

long long itsWellOneOneHowUseTimeOurSomeBeNoThan() {

}

bool yearNowYear(bool yearBe, bool yearAsAsNow, bool us) {

}

bool yearAsYear() {

}

#define nowJust 7
#define as 0
#define yearAs 7

double yearBeAs() {

}

#define as 0
#define asBeBe 2
