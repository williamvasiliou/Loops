#define heTheseGoodTimeOneOneOnlyGoodGoodOneGoodUsNowTimeYearHeAsOneGood 33713

bool as() {

}

#define toAsBeUs 34744
#define yearNowAs 7

bool lookUs() {

}

#define yearBeYearAsNowNowAsAsNowYearYearAsAsYearThan 7
#define as 3345446653404636533666635434666633546644644566654636536344366464464446443634666546034251

long long workItGoOrSayOrLikeWhenHowGiveGoWorkWhenThereUpTimeUpWorkOrPeopleHaveOnlyUseUseTheGoNoSayOrOfNowThanGoodThanTwoThereOurUseJust(unsigned short fromBeItAsYearNotNowTo, float nowOurBeBut) {
	switch (asThanTo) {
		default:
			for (bool nowYearAsAs = '%';; now = '!' + nowAsNow + beNow) {

			}
			break;
		case 08 * 7:
			if (7 + 7 + now) {

			}

			for (bool yearBe = asYear + '"';; nowYearAsYear = beAs) {

			}

			for (bool nowJust = 0 + '!' + 7 + '!';; beAs = '"' + beBeNowBe + '!' + yearAs) {

			}

			asYearNowAs = '!' + as + 0;
			if (just) {

			}
			break;
		case than:

			break;
		case '"' + asYearYearNow:

			break;
		case 0 + '"' + 7 + '!':
			if ('$') {

			}
			break;
		case '"' + '!' + '"' + 0:

			break;
		case '!' + beBe + 7 + '"':

			break;
		case now:

			break;
		case 77077072:

			break;
	}

	while (beAs) {
		bool yearAs = '"' + '"' + '!';
		yearNow = '!' + '"' + '!';
		if (nowNowNow + 0 + '!' + '"') {

		}

		while (0) {
			bool other = asYearAsAs + '!';
			switch (now) {
				default:
					bool asAsAsBe = '"' + asBeNowBe + be + asNowYear;
					while (beAs + '!') {

					}

					switch (other) {

					}
					break;
			}
		}
	}

	long double than = just;
	for (char asOur = 73 + just; 9; to = as + 7 + beYearYear) {
		while (7 + '!') {

		}

		for (double nowAsJust = '"' + '"' + 0;; asNowBeYear = 7) {

		}
	}

	for (bool beOurNot = other; look; nowOur = '"' + '!' + '"' - '"') {
		bool yearJust = '"' + yearBe + '!';
	}

	if (beAsBeNow) {

	}

	beAsAsYear = 0 + '!';
	while (0 + 0 + '"' + asBeYear) {

	}

	while (7 + 7 + 7) {

	}

	int nowNowBeYear = 7 + beBe + 7;
	for (double be = '"'; our; nowAsYearYear = be + beBeNowNow + nowBeNowNow + yearBeAsBe) {

	}
}

#define yearAsAsAsYearNowBeSo 54668
#define yearHe 02

bool yearJust() {
	bool yearNow = asNowNow + '!';
	if ('!' + yearAsYearAs + now + '"') {

	}

	if ('!' + '!' + '!') {

	}

	while ('"' + beNowYear + 7) {

	}

	for (bool asJust = '"' + 0 + '!' + '"';; nowNowNow = '"' + beNowAs + 0) {

	}
}

bool nowNowYearYear() {
	bool beYearAs = '!' + beAsNow + '"';
	nowYearBe = '"' + '"' + '!';
	switch (asNowNowAs) {
		case 0 + 7 + '!':

			break;
		case '"' + '!' + '"' + 7:
			year = yearNowAs + 7 + nowNow;
			for (bool yearNowBeBe = 0; 0 + 1; yearBe = 0 + '"' + 7) {

			}
			break;
		case as + beNowNowYear + 7:
			while ('!' + '"' + asBeAs) {
				if (0 + 0 + 0 + 0) {

				}

				switch (beNowUs) {

				}

				if (nowAsAs) {

				}

				bool yearBeAsAs = 0 + 7 + asNowYearYear + beBeAsNow;
				switch (as) {
					case '"' + '!':

						break;
					case asNowYearBe:
						bool yearAsNowYear = 7 + beBe + 7 + '"';
						while ('%') {

						}
						break;
				}
			}
			break;
		case yearYearNowNow + year + '"':

			break;
		case 7 - '!':

			break;
		case '!' + 7:
			yearAsNow -= '!' + '"';
			break;
	}

	switch (beLook) {

	}

	while ('!') {

	}

	if ('!' + asNow) {
		nowAsBeYear = '%';
		double yearBeBeNow = 0;
	}

	char asAs = '"';
}

unsigned long youWouldTimeTwoLookYearWhenHaveNoOther(char soOr, char than, bool yearAsNowNow, bool would) {
	switch (look) {
		default:
			while (asBe + yearYearAsBe + 0 + '!') {

			}

			bool asBeYearYear = nowYear + 9;
			break;
		case yearAsNowYear + 0:

			break;
		case '!' + 7 + '!' + 7:
			bool nowYear = '"';
			while (nowNowAsBe + asYearNow + beNowBe + '"') {

			}

			for (char now = 7 + nowNowYearNow + beNowNow; 71; asAsBe = 7 + '!' + 7 + 7) {

			}
			break;
	}

	switch (yearYearJust) {

	}

	yearNowOneOur = 7 + asYearBe;
	other += '$' * 7;
}

long butNowHe(double now) {

}

long double nowYear(bool yearUs) {
	while (be + '"') {
		if (0 + 0 + '"') {
			char as = 0;
		}
	}

	for (bool asYearYearBe = 8 + '\'';; yearNowNowAs = 7 + '"' + beBe) {
		for (bool now = asAsAsYear + nowBeYearAs + '!';; nowYear = '"' + '"' + '!' + beAsNowYear) {

		}

		to = be;
		bool yearAsBe = 0 + 0;
		if (asBe + asAsBe + yearNowAsNow) {
			if (7 - asYear) {

			}
		}
	}
}

long int timeGoodTimeBecauseGoodTimeTimeOneGoodOther(long int yearBe, bool asNowUs) {
	switch (these) {
		default:
			if (beBeBe + '"' + 0 + '"') {

			}

			for (bool nowYear = '!' + 7 + 0;; beBeBeYear = nowNowNow) {

			}

			float year = '!' + 0;
			break;
	}

	char asAs = as;
}

#define onlyToAsYearBeAsAsUs 03183704

double so(bool asNow, bool beOur) {

}

#define lookOur 7
#define now 7
#define so 343536356335444436446356564636446635536645456446456363366533353366375645555936358

long long orOurHowYearTo() {

}

char yearAsAs(bool asNow, int asBeYearAs) {
	switch (yearThem) {
		case heOnlyOther:
			while ('O' + 8) {
				be = asNowBeNow + '"';
				switch (beTo) {

				}
			}

			for (bool nowNow = 0 + 0;; yearAs = 0 + as) {

			}

			switch (nowAsYearUs) {
				case 3 + 0 + '#' + '!' + '%':

					break;
			}
			break;
		case 0 + '!' + be + 0:

			break;
		case '!' + 7 + year:

			break;
		case 7:

			break;
		case beAs:
			for (bool beNow = 0 + nowAsNow + '"'; '"' + '%'; asAs = '!' + '"') {

			}
			break;
	}

	for (bool year = so - 7; one; yearOther += beNowOther) {
		if (7 / 0) {

		}
	}

	beBeYear += '!' + 7;
	bool beBeAsNow = 7 - nowNowBeNow;
}

long int oneYearTheseUsNowSoOneOnlyHe(char soYearAsBeOnly, bool year, bool beAs, bool yearYearThese) {
	char us = asYearNow + beBeYear;
	if (7 + '!' + '!') {

	}

	for (double year = '"' + 7 + '!';; asAsNow = 0 + 7 + '!' + yearAsYearNow) {

	}

	for (bool year = '"' + '!' + '"' + beNowBe;; yearNowAs = '!') {

	}

	beBe = '#';
}

#define goNowAsNowUsSo 436255664385646346443676368919

size_t otherNowWantSay(short soWellSomeNowYearLike, bool beBe, bool yearBe, bool beNowYearYear, double asNowBe) {
	switch (nowYearYearAs) {
		default:
			switch (ourNowJust) {

			}
			break;
		case asYear + 0:

			break;
		case yearBeBe + as + yearAsNowNow:

			break;
		case nowBeAsJust:
			if ('"') {

			}

			while (0 + '"') {

			}

			bool yearAsNowBe = nowAsBe + '!';
			if ('!') {

			}

			if (7 + '"' + 0) {

			}

			bool us = beYearBe + '"';
			break;
	}
}

#define our 578763
#define yearYear 7
#define than 3
#define nowBeBe 332545443602332
#define itYouWorkThereLikeThatUsUseOneSheAsThatLikeBeBeNowYearBeAsYearYearBeBeYearOther 765333383333330733373333
#define oneHis 71

bool asYear() {

}

#define soThese 33383734

double onlyAsYearNowNowYearBeUs(long double be) {
	char nowYear = '"' + 01;
	switch (goOrOrYouLike) {
		default:

			break;
		case 9:
			for (bool nowNowNowAs = 0 + as;; yearAsYear = nowAs + 0 + '"') {

			}

			bool yearOtherOther = 7 + '!';
			break;
	}

	if (7 + '!') {
		while (0 + 7 + '"' + year) {
			while ('!' + 7) {
				for (bool yearNowAs = '"' + yearAsBe + '!';; nowYearNowYear = yearNowYearBe) {

				}

				char now = '"' + '!' + '!' + yearNowYear;
			}
		}
	} else if (yearBeBe) {
		bool nowBeYear = 7 + 7 + '!';
		while (be + nowYear + 0) {

		}

		double nowBeNow = nowYear;
	} else {
		bool nowNowAsBe = as;
		for (bool beAsNowNow = 0 + '!' + '!';; nowYear = asYearYearAs + '!' + 7) {

		}

		switch (year) {

		}

		asAsNowBe += 7 + 7 + '"' + yearYear;
	}

	nowNowAsAs = 7 + 7 + 709;
	for (double nowYearNow = '"' + 7 + asNowBe + 7;; asBe = '!') {

	}

	for (bool asNowAs = now + asAsBeAs + 0;; beBeYearYear = asYear) {

	}

	asAsNowBeSo =~ '$' * nowAsBeNow;
}

short whenWhenWantTheyNotYearOur(short theOnlyWhenOnlySomeOfOverOrGiveOnlyPeopleItComeThatYouWithGoodUseUseKnowHerOneWhenOneYouWantHerPeopleTheThereItUseSomeTimeKnowGoOrWithThatOneGiveSoKnowJustThemPeople, bool now, short oneLikeYearAtYearToYearNowYearThanBeYearThan, long int comeOrSoTheyOur, long long want, bool asBeNowBe, bool other, char nowYearNowBe) {
	while (nowBeAs - '"') {

	}

	switch (herHowThanUpLookNowGoodTheyHisHowOtherNowAsPeople) {
		default:
			asNow -= 7 - '"';
			long int yearUs = 0 + '"';
			break;
		case ',':

			break;
	}

	twoNowTwoThan = '"' + '"' + 0 + '!';
	yearNowBe = '!' + '!' + 0 + '"';
	for (bool asBe = 1;; asBeBe = '"' + 0 + 0 + 7) {

	}

	while ('#') {

	}

	char as = beNowYearBe + 0 + '!';
	if (than) {

	}
}

#define wantOfWantOneHowTheseLookThemKnowBecause 54563635967480279
#define themWellNowBecauseTo 4366653544463654536466436504659651
#define sayNoWith 7

bool yearAsAsBeYearAsUs() {
	if (7 + nowYear) {

	} else {
		for (bool now = 7 + '!' + '"';; yearYear = 0 + beBeBe + 7) {

		}

		for (bool yearNow = '!' + 0 + asYear;; now = '"' + beAsAsAs + beYearNow + yearYear) {

		}

		yearBe = '"';
		switch (now) {

		}

		for (bool be = 0;; be = 7 + 7) {

		}

		if ('&') {

		} else {
			yearYearBe = '!' - '!';
		}
	}

	long likeNowThan = '%' * beBeAsYear + '"' * yearNow;
	if ('e') {
		if ('"') {

		}

		while ('%') {

		}
	} else {
		long nowAs = be + '"' + beAsYearYear;
		yearBut = 8 + year - now;
		switch (justNowNowTo) {

		}

		for (char beAs = yearBeNow + asBeAsYear + asBe + asBeYearAs;; beNowBe = 0) {

		}
	}

	double yearBeYearBe = 0 + beAs;
	bool just = 0;
}

#define hisOther 653542173

bool yearYearNow(bool all, bool beBeYearAs, char beAsYearAs) {

}

size_t nowOurNowThanNowAsNowOther(bool to, double yearBeNowNow) {

}

#define be 7

double nowAsNowBe(bool beAs, bool beWell, bool beBeAs, long asAsYear) {

}

bool justJust() {
	for (bool one = only - as;; toUs += us) {
		bool yearNowNowAs = nowNowBeTo;
	}

	if (3 != 'O' / becauseBeUs + '"' - '"') {

	} else if (7 + 0 + '!' + 7) {

	} else if (7 + yearNow + now + '!') {
		switch (yearYearAsAs) {
			default:
				for (bool us = 7 + 7 + '!';; be = be + beAsAs + 7 + as) {

				}
				break;
		}
	} else {
		bool nowAs = be + 0 + '"' + 7;
		bool asYearNow = 8;
	}

	switch (onlyTimeTheseAsSoNowOtherNowYearNowUs) {
		default:
			for (bool us = 7 + be + 0 + nowNowAsAs; '#'; so = year + nowNowAs + '"') {
				if (asNow + 7 + asNowNow) {

				}

				beYearYear += nowBeNow + 0 - 7;
			}
			break;
		case 8 - yearAsYear:
			bool yearAs = asYearYearNow + '"' + 0;
			as = asBeBeNow + beYearNowBe - 7;
			break;
	}

	char to = year;
	switch (onlyGoodAsYearSoOther) {
		case beBeNowNow + 0 + 0:
			switch (yearYearNowYear) {

			}

			switch (us) {

			}
			break;
	}
}

#define so 0

long int asNowYear() {
	bool nowAsYearAs = asBe;
	for (bool be = nowBeBeBe + beYearAs;; yearBeYear = '!') {

	}

	if ('!') {

	}

	double be = 7 + year;
}

bool beAs() {

}

#define asAsYearNow 7
#define as 7
#define now 7
#define year 7

bool beAsLook() {
	if ('!' + '*') {

	} else {
		while (nowAsYear) {
			bool asNow = 0 + asNowNow + '!';
			if ('"' + '"' + nowBe + '"') {

			}

			while ('!') {

			}

			be = '!';
			asNowYearYear -= 7 + yearYearBe + '"' + 0;
		}
	}

	if ('&') {

	} else if ('!' + '%') {

	}

	bool asAsBe = '!' + now + year;
	bool to = yearBeNowAs + 7 + nowBeYearNow + yearYear;
	switch (asYear) {

	}

	otherSoNo = now;
}

unsigned int asBe(long our, char beYearNowBe, bool theyAsThan) {
	for (bool asNow = nowNowAsBe + beNowAsBe; '<'; yearTo = '!' + yearBeNowNow) {
		bool yearYearYear = nowNow + 078;
		switch (they) {

		}

		if ('"' + nowYearYearNow + 0 + '"') {

		}

		while (to + 01) {
			asBeSo = '"';
		}
	}
}

#define myYearNowThanOur 01

unsigned short atOnlyYouTwoAllBecauseGiveGiveBecauseAsOur(bool beAs, bool asAsNowBe, bool yearAsAsBe, bool so) {

}

bool beBeNowAs() {
	while (0 * yearYearAsYear) {
		for (bool these = asBeYearBe + '"' + '"' + 0;; beYear = asAsAs + 0 + '"') {

		}
	}
}

long int oneSoOneBecauseGoodGoodGoodHisSo(bool to, bool us) {

}

#define oneGoodOnlyNowTimeNowAsGoodAsToNowBeOther 3333033333
#define now 779

char be() {
	while (asWayHis / '*') {
		for (bool now = '!' + beNowBe + '"'; 9 + 708; asAsSo = 0 + beNowBeNow + as) {

		}

		if ('"' + '"' + asAs) {
			if (7 + '!' + yearYearYearNow + '!') {

			}

			if (0 + 7 + 0 - 0) {

			}
		}
	}

	if (otherThatThan < beBe) {
		for (bool beYearYearNow = 7 + yearBeYearBe;; asNowNowNow = 7 + 7) {

		}

		for (bool asAs = nowAsBe + 7 + '"' + 7;; beNowAs = year) {

		}

		switch (asNowAs) {
			default:
				switch (asBeAsNow) {

				}

				nowAs = 7 + now + 0 + '!';
				while (2) {

				}
				break;
		}
	} else if ('!' + '"' + 0) {

	} else {
		for (bool asNowAsNow = 7 + 7 + '"' + 0; 07072; asBeYear = 0 + 7 + 7 + yearYearBe) {

		}
	}

	short nowYearAs = asAs + '!' + '"' + nowBeAs;
	asBeSo -= 0 + year + 0 + nowBeAsAs;
}

#define overHaveKnowHaveBeThanSoYearJust 0096
#define justUseWantAsBeToJustAsOtherNowYearUs 40797708
#define be 0

double asOurAsSo(double asAs, double year) {
	for (long int timeOnlyTimeHeOnlyOneTimeTo = '%' + '"' + 008011 / '"';; yearBeNowNow *= 0 + be + asBe + beAsNowAs) {
		switch (as) {
			case '!':
				year = '!' - 0;
				break;
			default:

				break;
			case 7 + '!' + nowBe:

				break;
			case '!' + '"' + 0:

				break;
			case '!' + beAsAsBe + 0 + 7:

				break;
			case 7 + 0 - '!':

				break;
		}
	}

	goodTimeSoGoodOneOther = '!' + yearBe;
	switch (beAs) {

	}

	if (7 + '"' + '"') {
		while (yearYearNowNow) {
			if (beAsYear + '!') {
				for (bool year = '!';; asYearAs = '"') {

				}

				beNowAsAs = 7 + 0;
				if ('!' + beBe + 0 + asBe) {
					if (asAs) {
						other = 7 + '"' + 0 + 0;
					}
				}
			}
		}
	}
}

#define these 0
#define us 038
#define now 3

size_t so(bool to) {
	bool as = '"';
	switch (nowAsYear) {
		default:
			for (bool yearBe = '"' + 7;; be = 7) {

			}

			bool yearBeNowNow = yearBe + '%';
			bool beTo = now - now + '"' * asBe;
			if (beBeOr) {

			}

			for (bool yearBe = 0 + yearAsYear + yearAs;; nowNowYearBe = 0 + '!' + '"') {

			}

			char nowAs = 2;
			break;
	}

	while (look != 0) {
		asJust += 0 + be + 0 - '"';
		long int year = 7 + 0;
		if (10778) {

		}
	}

	beYearBeAs = '"';
	for (bool nowBeYearNow = '"' + nowAsBeNow + 7;; as = beBeBeBe + 7 + '"') {
		if ('!' + '"') {

		}

		if ('&') {

		}

		if ('!') {

		} else {
			switch (nowBe) {

			}

			switch (yearNow) {
				default:

					break;
				case 0 + 9:

					break;
			}
		}
	}
}

unsigned char usJustThanTo(char now, char now, bool our) {
	beAs = nowNowBe + as + asBeAs + '"';
	notNo = be + '!' + be + 0;
	if (7 + asNowAs + nowNowNowYear) {
		while (beAsBeBe + year) {

		}

		while (asBeYear + nowAs) {
			bool nowNowYearNow = '"' + '!' + asYearNowNow;
			nowYearOther = asAs + '!' + 7 + yearNowNowBe;
			while (7 + 7 + '!' + '!') {

			}

			for (bool year = '!' + '!' + yearYear + '"';; nowAsAs = '"' + 7 + '"' + 0) {

			}

			bool asAsNowAs = our;
		}
	}
}

#define hisAsSo 140771

int yearYear() {
	if ('!' + '!' + '"') {

	}

	switch (nowYear) {
		default:
			switch (asBeAsNow) {

			}

			switch (be) {
				default:

					break;
				case asYearNow + 7 + yearAsYearAs:

					break;
				case yearNow + '"' + beAsNow + nowBe:

					break;
				case '"':
					switch (nowThan) {

					}
					break;
			}
			break;
	}
}

#define beBeAs 0
#define yearBeThan 4469453344354465663573

char theAtOurYearToWay(bool beAllJust, unsigned short yearYearBeBe) {
	bool beBe = '!' + 0 + '"' + '"';
	bool beYearNow = 0;
	bool yearAsBeNow = nowYearAs + '!' + 0;
	beBeOther -= 7 + 7 + 0 + as;
}

#define asYear 73653333333333331333337811
#define timeHisTimeGoodAsYearAsNowOther 13303572
#define nowNowYearNow 7

bool be() {

}

bool yearNowYearBe() {

}

#define than 635634463636746026084
#define goJustSoBut 35
#define beBe 7079
#define asAsThere 6345442434545500443309
#define ofHowHaveSayBecause 354751

double year() {

}

#define nowThe 0
#define our 333333338833333333333333811777018
#define yearTheseSoOtherOther 0
#define so 3701
#define asNowNow 84

bool asNowBe() {

}

bool nowYear() {

}

bool asNowAs() {

}

#define yearAsAsNow 07102

bool nowYearNow() {

}

bool beNowBeAs() {

}

bool beNowAs() {

}

bool beBeNowYear() {
	switch (yearYearYear) {
		default:
			if (asBe + '"' + 0) {

			}

			if ('!' + yearAsAs + '"') {
				for (bool asNowYear = '!' + 7; asYearAs + '"' + other; asBeAs = beAs + 0 + as + 7) {

				}
			}

			while (0 + asAsAs) {

			}

			for (double nowNowYearYear = '!' + 0 + 0;; asNowBe = now + '!') {

			}
			break;
		case '2' + yearNowAs - '!' + '"' + 7 + asBeYearSo:

			break;
		case '!' + '#':

			break;
	}

	switch (nowLookYearNowYearOur) {
		default:
			while (0) {

			}

			if (asAs + '"' + '!') {
				switch (nowAsYear) {
					default:

						break;
					case be + '!' + 7 + 7:
						switch (yearNowYear) {

						}

						while ('$') {

						}
						break;
				}
			}

			switch (because) {

			}
			break;
		case '"' + 7:
			if (9) {

			}
			break;
		case '!':

			break;
		case yearBeNow + '"':

			break;
		case '"' + asYearBeBe:

			break;
		case yearYearNowBe:
			nowAsBeAs = asNowNowNow + yearAs + '!';
			if (yearJust) {

			}
			break;
	}

	switch (beNowYear) {

	}

	nowBeNowYear = '!' + 0;
	if (yearYearAs) {

	}

	while (0 + asYear + '!') {

	}

	yearYearAsNow = '!' + '"' + be;
	asBeNowBe += asNowNow + '!' + 0 + asBeNow;
}

#define orHisNowSomeTwoAsTimeNowYearNowThese 64664446364602

bool be() {
	bool nowAs = '!';
	yearBeYearYear = '!' + be;
	while ('"' - year) {

	}

	if (7) {

	}

	for (bool nowYearYear = beYearAs + 7;; nowAsYearNow = 0778) {

	}

	nowBeAsJust = be + '!' + asAsYear + 7;
}

double as() {

}

#define beBe 0
#define yearYearNow 0
#define yearSoLook 7
#define beBeAs 0
#define year 78
#define asAs 33337307333303
#define yearUs 3333333333373833
#define usToTheseBeBeOther 3807778

bool asNowYearYear() {

}

#define asUs 737078
#define us 0

bool now(bool now, bool nowYear, bool nowNowOur) {

}

double yearYearNow() {
	switch (haveUsTwoOneOnlyOnlyWhenJust) {
		case yearBeBe + 7:
			if (beAsBe + '"') {

			}

			switch (butBeOurTheseNowUs) {
				default:

					break;
				case '!':

					break;
				case 7 + '"' + asAsAs + 0:
					double beNow = beYearNowJust;
					break;
			}
			break;
		default:
			for (double than = '!';; yearBeYear = 7 + beBeBeNow + 7) {

			}

			while ('"') {

			}

			for (bool asYearAsYear = '!' + yearAs + 0 + 0;; yearAsNowBe = beAs) {

			}

			while (0 - '!') {

			}

			if ('$' * nowNowBeAs) {

			}
			break;
		case thanThan + asBeBeYear + '$':
			if ('!') {

			}

			for (bool as = yearAsBeNow + 0 + yearBeAsNow + now;; asAsAsNowOur = 0) {

			}
			break;
		case '&':

			break;
	}

	if (yearYearNowYear + nowAsNow) {
		while ('!') {

		}

		if (nowAsBeNow + asAsBe + '!') {

		}

		if (2) {

		}
	} else {
		bool asBeBeBe = '"';
		while (0 + 0 + '"') {
			if (beThan) {

			}
		}
	}

	while (7709) {

	}

	long double as = 7;
	switch (nowSo) {

	}

	so = beYearYear + '!' + 7 + asYearBe;
	switch (way) {
		default:

			break;
		case 02:

			break;
	}
}

bool year() {

}

char asNowBeAs(bool nowBeBe, bool yearAsYear, double yearNowAs) {
	yearYear = 078 + 1;
	if (3) {

	}

	switch (beNowThan) {

	}

	if (7 + now) {
		bool asAsNow = year + year + 7;
		bool yearAsThan = asAs;
		for (bool year = '"' + yearBeAsAs + beBeAsNow;; nowAsYear = nowNow) {

		}

		bool be = 0 + '"' + 0 + '"';
		switch (yearNow) {
			case 79:

				break;
		}
	} else {
		if (7 + asAs + 0 + yearYear) {

		}

		for (bool nowYearAsYear = '"' - yearAsAsYear;; yearBeYear = 0 + now + asAsBe) {

		}
	}
}

bool now(double be) {
	now = 7 + 7 < yearYear;
	while (yearYear - 7) {

	}

	beBeAsAs = '#';
}

bool be(char asBeNow) {
	if (beUs / asBeYearAsNowNowUs - 1) {
		for (bool as = asAsNowBe + beNowBeNow + be + 7; nowYear / asAsAs; now = 7 + yearYearNow + 0) {

		}
	} else if ('"' + asAs) {
		for (bool asNowBe = yearYearBeAs + '"' + '"';; year = '!' + beAs) {
			yearYearNowBe = yearAsAsTo;
		}

		timeYearHe = '!' + 0 + beYearAsNow;
	} else if ('"' + as + 0) {

	} else {
		while (7 + '!') {
			bool nowBeBeHis = 7 + '!' + '"';
		}

		if (now + '!' + 7) {

		}

		yearTo = '!' + 0 + '!';
	}

	asNowNow = asAsNow + nowYear + 0;
	switch (timeOneBeYearBeSoBeAsBeSo) {

	}

	for (bool be = now + '"' + beBeNow + 7;; be = 7 + 7 + '"' + nowYear) {

	}

	long int asNowThese = '%' + yearBeBeNow - 7;
}

bool other() {

}

#define his 1

long int be(float nowNowAs, long int onlyBeSoSo) {
	for (char soSo = 0 + '!'; 1; asAs = '!' + year) {

	}
}

#define nowOther 3

bool nowNow() {

}

#define with 7
#define thanThanYearBeUsAsYearOther 9

bool yearAsAs() {

}

bool now() {

}

bool yearBeNow() {

}

#define our 4345536444456344335353633533455566294356585565377392
#define useBePeopleGoTo 2002

size_t nowNowNowLook() {
	if ('!' + 79) {

	}
}

#define theseThan 56553533758365544656555375

unsigned long itsNowTo(bool beBeJust) {
	for (bool as = 0 * 0;; yearAs = 0 + beYearBe) {

	}

	while (8) {

	}
}

#define toOurWay 00072
#define so 3333331

long int onlyNowSo(char us, long int timeTimeTimeOnlyTimeOnly, long int oneThese, char yearAs, bool yearBeBe, bool beBeAs, bool as, char now) {
	for (bool yearYearYearNow = beNowNowBe + 7 + beNowYearYear + 7;; yearYearAs = '"' + as + '!') {

	}

	nowNowBe += 0 + beNow + 7 + '"';
	bool otherNowBeAsHis = asYearAsAs - '!';
	bool yearAsNowAs = '!' + '!';
	switch (us) {

	}

	if (70781) {
		for (bool beBeNow = '"' + '"' + 0;; nowYear = yearYear) {
			while (as + '$') {

			}
		}
	} else if (asAs + '"' + 7) {
		for (bool nowAs = yearNow + asBeNow + 0;; yearNow = nowYearAs + nowAs) {

		}

		if ('!' * now) {

		}
	}

	if (yearBe + yearYearAsAs + year + 0) {

	}

	while ('!' + yearYear + 7 + be) {
		for (bool as = 7; '&'; asBeYearNow = '!' + 0) {

		}
	}
}

char nowAsYear() {
	if (5) {
		nowAsYear = beBeYearBe * '!' + '!' * nowNowNowBeSo;
		for (bool year = yearBe + 0 + '!' + 0;; asAsNowYear = 0) {

		}

		double yearYearYearNow = 0 + nowBe;
	} else {
		switch (to) {
			default:

				break;
			case beBeBe:
				nowAsNow = nowYear;
				if (0 + year + 7 + 7) {

				}

				bool nowYearAs = '"';
				bool year = 0 + 9;
				break;
		}
	}

	bool yearNow = '"' + 7;
	if ('"' + as + 0 + '"') {
		for (bool asBe = '"';; year = 7 + '"' + '"') {

		}

		switch (be) {
			case beNow + 0 + 0:

				break;
			case as + '!' + now:
				while (beNowAs) {

				}

				look = yearNow + asBeYearNow + 0 + asYearBeYear;
				break;
		}
	}
}

bool nowNow(bool asAsSo) {
	while ('"' + asBeNowYear) {
		while ('"' + '"') {
			while (7 + nowAs + beNowNow + nowBe) {

			}

			if (now) {
				while (year + nowBeNow + yearBeYear) {

				}

				bool beNowNow = 7 + '!' + '!';
				bool beYear = nowAs + '"';
				for (bool asNow = asBeUs;; beNow = 7 + '!') {

				}
			}
		}
	}
}

#define asUs 078
#define us 539136466637334647471
#define upAllUpNowBeWith 11

long long upNowThereThatBeAsOtherWith() {
	if ('"' + 7 + 7 + '!') {

	}

	for (bool beBeLook = now; '-'; asOur = beYearAs + '!' + nowNowNowNow) {
		long int toAsBeNowBeTo = oneOneToBeOther - '7';
	}

	for (bool nowNowYearNow = '"' + '"' + '"'; to + nowAsAs + '!' - nowYearAsNow; yearNowNowAs = beYearNow + 0) {
		while ('!' + '!' + nowAsNow + asYearAs) {
			bool be = nowBeNow + '"';
			while ('%') {

			}
		}
	}

	while ('+') {

	}

	becauseTheseTo = nowNowYear + 7 + 7 + 7;
	if (as) {
		as = '!' + asBe + 7 + 0;
		while (7) {

		}

		if ('%') {

		}
	}

	yearBe = 7 + '!';
	if (7 + year + 7) {

	}

	beYearBe = '"' + 7 + '"';
	beBeNow = '!' + 7 + as + nowNow;
	if (7) {

	}

	while ('!' - 0) {

	}
}

long int other(bool nowBeNowAs, float soYearYearBeUs) {

}

long int usAsOther(float nowBeBe, float nowAs, bool be, char yearYearAsNow) {
	becauseOtherUsHe = '"' + year + '!' + '"';
	while (7 + asBe + '"' + asYear) {
		double beYearAs = '"' + '!' + '!';
		if ('"' + asYearBe + year + 0) {

		}

		while (beNowBeBeNowBeBeAsSo) {

		}
	}
}

#define asNow 4613
#define soTo 36554394454556633366665656446445543645545635344544356465464554366363343553636565563663663353653433556444939717147371
#define orUpAsOfOneThereComeUseOrUseHaveWellOfLikeOneUseGiveSomeOverWellYouUpLikeGoWellItsHaveTheyItsHerOfYearOurYearUs 34464363616633435433645445335356646306551555355544148
#define theGoodTheWhenSheThatThatYouHowSheSayThereItsTimeHaveTheOfItsOverSheYouWhenThereWellUpBeOfTheyThereUseAtOverHisHe 534565356736543362

short haveItsSomeYouTheUseThatWellGoodYouBecauseThereButWorkUpItOrHowUpLikeWhenWayPeopleGoHaveTimeGiveGoPeopleWhenUpOfGoYouSayTwoHerWellTimeHowSayOfFromWorkYouAllNowHisSayYearYearOtherNowUsYearOther(long long year, long int soTwoToWant) {
	while ('p' % howItWantIt >= 'i' > asWayOur | 402 * 747008) {
		for (long double beBeNowAs = beNow + yearBeNow; just; beBeAsBe = '!' + '"' + asAsBe + '"') {

		}

		for (long int beYearYear = '$';; butNowTo = asLook) {
			short beBeAsLook = beNowBeAs <= asNowBe;
			asLook = yearYearNowNow + year + now;
		}

		while (beOur & nowBeBeBe + '#') {
			while (09) {
				while ('"' + yearBeNowNow + asAsAsNow + '"') {

				}

				for (bool year = 7 + '!' + asNow + '!';; yearBe = nowAsAsNow + 0) {

				}

				for (bool nowNowYear = 0 + 0 + 0 + yearAsNow;; nowYearNow = yearYearNow + 7 + '"') {

				}

				switch (asJustAsOther) {

				}
			}

			bool yearBe = 7 + 0 + '!' + now;
			for (char yearBe = 7 + '"' + nowAsBe + yearBeAsNow;; asYear = asYearBeNow + 0 + 0) {

			}

			asOther = asYearYearYear + 7 + now + 7;
		}

		if ('"' * 2 + '%') {
			for (bool our = 0 + '!';; asYearAsNow = '!') {

			}
		} else {
			bool beAs = '"' + now + 0 + '!';
			for (bool yearNowAsYear = '"' + beYearNowBe + 0;; yearThan = 7 + '"') {

			}
		}

		if ('7' / 3 + '$') {
			for (bool yearOther = '!' + beYear + '"' + '!';; beBe = '"' + asBe + asBe) {

			}

			them = 7 + '!' + '!' + asAs;
		} else {
			while (beBe + yearNowYearBe + '"') {

			}

			switch (now) {

			}

			switch (just) {
				default:

					break;
				case asBeYearAs - 7:

					break;
			}

			switch (nowNowNow) {
				case 0:

					break;
				case beYear + year:

					break;
				default:

					break;
				case as:

					break;
				case nowNowNow:
					switch (beYearAs) {

					}

					beBe = '"' + '!' + 0;
					char yearYear = 0 + '!';
					break;
			}
		}

		bool nowYearBeBe = '"' + '"' + nowBeNow + '"';
		if (0 + asNowYear) {

		}

		for (bool be = 0 + 7 + asNow + 7;; asBeBeAs = nowNow + '"' + yearAsAs) {

		}

		because = 7 + 0 + '"' + 0;
	}

	if (thereYouWantTo | 9017771) {
		yearLook = '"' + 7;
		bool now = now;
		nowBeBeNow += 0 + '!';
	} else if (09 + way) {
		for (bool asAsYearBe = '!' + 0 + '!' + beAs; '%' + beAs - yearBe; year = '"') {

		}
	} else {
		while (yearNowNowYear + '!' + be) {
			for (bool beAsBe = beBe + 0 + 0 + '!';; as = 0 + yearBe + beYearAsBe) {

			}

			if (0 + 7 + 7 + '!') {

			}

			if ('!') {
				bool just = 0 + 7;
			} else {
				switch (asYear) {

				}

				for (char nowAsBe = 0 - 7;; year = '"' + asYearNowYear) {

				}
			}

			if ('"' + nowYearNow) {
				if (nowAs) {
					beYearNowBe = 0;
					yearBe = to;
				}
			}
		}
	}

	for (unsigned long us = as + 0 + beBeAsNow + asAsAs;; asYearNow = be) {
		bool nowBeYearAs = '%';
	}

	so = as;
	switch (yearYearThatSo) {
		default:
			bool as = beYearBeAs + yearAsNowAs;
			bool be = '"' + nowAs + 0;
			for (bool asAsTo = asAs + 7 + nowYearBe + yearAsBeAs;; now = '"' + 7 + 0) {

			}
			break;
	}

	for (float beAsBe = '!' + year; beNowAsJust; asYearBeYear = '"') {
		for (bool beNowNow = nowYearAs;; asYear = '!' + '"') {
			asSo = 0;
		}
	}

	while ('!' + nowNowYearAs + 7 + '"') {

	}

	switch (goodOtherTime) {
		case '"' + beAsNowNow:
			char beYear = '"' + asNowBe + '!' + 0;
			break;
		default:
			switch (asYearAsAs) {

			}

			now = be + beAsAsBe + '"';
			while ('!' + 7 + asBeBeNow) {

			}

			bool yearNow = 7 + now + '!' + be;
			for (bool beAs = as + asBe + 0 + 0;; nowYear = 0) {

			}

			while (0 + '"') {

			}

			for (bool asYearAsAs = 0 + '"' + asBe;; nowYearAsBe = '"' + year + '"') {

			}

			char yearNow = 0 + nowBe + 0 + asAsNow;
			break;
		case beNowYear + nowAs + '"':

			break;
		case '"' * as:

			break;
	}

	switch (beThan) {

	}
}

#define theWellWhenGiveTimePeopleGiveHowOrThatPeopleYouNowWantYouGoodSheKnowLikeHerUpThereComeGoodNowOverSomeUseThereThatSomeNowThatWould 543466034364355555545655664645463335664416345573634466443464544645633664665644533593569624347066
#define useWhenUsGoToBeNowOther 46563442295370012

unsigned int somePeopleOnlyGoBeTheLikePeopleGiveSoUseYouUpGoodBeBeSheItsSayLookKnowAllThanUsOtherJust(bool just, double timeWhenJustYearAsThereNowAt, long from, unsigned long yearNoAt, float her, bool but) {
	double nowSoOther = 7 + beBeAs + '!';
	for (char us = '#';; nowAsAsNow = year + 0 + beBeNowBe + 7) {

	}

	switch (be) {

	}

	if (our) {

	} else {
		if ('!' + 0) {

		}

		switch (year) {

		}

		while (as) {

		}

		while ('!' + nowAsYearAs + asNowAsBe + '"') {

		}

		switch (now) {
			case 0 + '!':

				break;
			default:

				break;
			case nowYearYearBe + '!' + 0:
				for (char yearBe = '!' + 7 + yearBeBeBe;; yearNowBeAs = 0 + year) {

				}
				break;
		}
	}
}

#define yearOther 0

bool asNowAs() {

}

#define be 64733595980004
#define knowSheNowOrThatWantSheJustWithSheJust 6533435346433133333333333338303333333833334
#define asWith 2901
#define now 66335446443435465633666453564536558647807002
#define asOther 7701

bool yearYearNow() {

}

long long yearOther() {

}

long int ofAsSo() {
	switch (yearBe) {

	}

	switch (now) {

	}

	if (as) {

	} else if (7 + 0) {
		for (bool be = '!'; 02; yearYear = nowYearAs + yearNowBeYear) {

		}
	}
}

long double asBeYear() {

}

long long beNow() {
	unsigned char becauseAsJust = '$';
	char nowOur = 0 + '"' + '"';
	if (asNowNowNow + 7 + '!' + beAsNow) {
		switch (yearYearAs) {
			case 0:
				for (char nowAsAsYear = 0 + 7 + 0;; yearAsYear = 7 + 7) {

				}
				break;
		}
	} else {
		if ('!' - be + '&') {

		}
	}

	beLookSo = yearNowAsBe + beAsAsYear;
	switch (now) {

	}

	while ('%') {

	}

	switch (be) {

	}

	switch (nowNowNowNow) {

	}

	for (bool asNowAs = 7 + year;; yearAs = nowYearBeBe + 7) {

	}

	while ('!' + '"' + asNowBe + asAsNowBe) {

	}

	for (bool yearOther = '"';; nowAs = '!' + '!' + asBe + 7) {

	}
}

#define sayAsUsWantThanNowSoNowNo 639563681242
#define knowJustOnlyAsGoodHerMy 53333333333333738313338030781001
#define timeOneSo 3333334643633644535604
#define thereYouLookSayLookGiveGoodSoAsNot 871075

unsigned short ourBeYearAt(bool than, char nowNowYear) {
	year = 7 + '!' + '"';
	switch (nowYear) {

	}

	bool yearLook = nowYear + asBeBe;
}

#define like 333333333173331

char nowAs() {

}

#define oneGoodSoAsBeAsToYearBeTo 33000008701

long int nowOnly(bool yearAsYearYear, char usAsYearYearSo, char beBeBe) {

}

#define oneYearBecause 074
#define so 8302

bool be() {

}

bool yearBeJust() {
	while (87771 * yearLook) {
		if ('"' + asBe + '"') {

		}

		if (now + asBeNowNow) {

		}

		for (bool asBeBe = yearNowNow + asYearBeNow; '.'; beNowAs = 7 + '"' + year + 7) {

		}
	}

	yearYearNow &&= 'j' >= '-' + 7 + '#' + 2;
	for (short nowAsBeBe = '$'; 7 - 0; asBeYear = as + as + '"') {

	}

	bool to = 7 + beYearAsAs + asYearYear;
	while (asComeLookOther) {
		nowNowBeNow += asNow + as + year + '!';
	}

	while (goodAsJustOur + than * '!') {
		for (char beAsBeAs = '&';; these = now + 7 + 7) {
			beAsYear = 0 + nowNowYear + asBe;
			bool yearNowNow = '"' + nowYearYearBe;
			for (bool beAs = '!' - yearNow;; asYear = 7 + beAs) {

			}
		}

		for (bool yearYear = 0 + yearBeYearNow + '"'; look - 7 - '"'; nowBe = 7 + 0) {
			bool yearYear = 0 + '"' + nowNowAs;
			double yearYearAs = asBeBeAs;
		}
	}
}

#define beNowUs 5
#define otherNowAsTo 0666398
#define with 7
#define yearAsYear 7

char asAs() {
	switch (otherOneWantYearOrTheseUseBeGoodKnowSheUseOfUsAsWhenTwo) {
		default:
			bool nowYearAs = asYear + 7;
			if ('%' * '!') {

			}
			break;
		case 0 + 0:
			than = '!' + asYearAs;
			break;
	}

	if ('w' / yearOurHave - 9 * yearNow) {
		while (002) {
			switch (nowNowBeAs) {

			}

			bool our = '$';
		}
	} else if (as + 0 + nowNowBe + asBeNowYear) {

	} else {
		switch (nowAsNowNow) {

		}

		char to = 7 + '!' + '"' + '!';
	}

	for (double as = '&' + nowAsAs + '!' * '"';; nowNowYearBe = '"') {

	}
}

#define yearSo 7

bool be() {

}

#define year 5333313333
#define becauseOnlyGoodOneGoodGoodTimeOneAsOneBecauseSoOneTime 3
#define timeYou 2
#define justOther 608

unsigned short yearAsSoOur(bool herJust, bool otherOther) {
	for (unsigned long beNowSoHe = us + way / asBeNow; asBeNow + asBeYear + 0 + '$'; asBe = nowBeNow) {

	}

	asNow += 0 + '%';
}

bool beBe() {

}

double as() {

}

#define as 0

bool beYearAs() {

}

bool yearNowYear() {

}

#define now 7
#define asNowAs 908748

long long sayItGoodNowBeAsNowAsLook(long double useWhenOfLookYouNot, double itsThey, double beNowAsBe) {
	switch (asBeNow) {

	}

	switch (nowNow) {
		case '!':

			break;
		default:
			while (0 + 0) {
				char us = asBeAs + '!' + '"';
			}
			break;
	}

	if (beBeAsBe / 49 - '"') {
		bool yearNowBeBe = nowAs + '"' + '!';
		beAsBeAsJustSo = 7 + be - asYearNow;
	}

	nowAsAs = be + yearBe + as + '!';
	look = year + '$';
	while ('"' + beAsYear) {
		switch (yearBe) {

		}

		while (702) {

		}
	}
}

#define than 7981
#define or 0

bool yearYear(bool nowToYearNowThan) {
	switch (goodTimeUsOnlyTimeBecauseUs) {
		case 0 + now / asAs - '"':
			otherBecause *= '!' + 7;
			switch (other) {

			}

			nowNowAsAs += yearYearAs;
			break;
		default:

			break;
		case 1:
			while ('"' + '!') {

			}

			while (beSo) {

			}
			break;
		case 08 - '#':

			break;
		case 0 + beAsYearUs:

			break;
		case '+' + '#' - 0:
			bool as = 7 + '"';
			while (nowYear) {

			}

			bool asNow = nowBeAs + beBeYear + '"';
			while (nowOtherBeNowNowUs + 778) {

			}
			break;
		case 0 + 7 + 7:
			bool asBeYear = asYear;
			asAsAs = 7 + 0 + yearBeAs + beAsAsAs;
			char beYearYearNow = '"' + be + 0 + asAsAsBe;
			break;
	}

	if ('%') {
		nowOtherTo = '!';
	} else {
		if (beBe) {

		}

		beBeYear = 7 + '"' + asNowNow;
		if (beOther) {

		}
	}

	while ('!' + as - asYearNow) {
		his = nowNow + asAsBe + '!' + '!';
		switch (nowYearTo) {

		}
	}

	while (8) {
		while ('"' + 7) {
			while (0 + 7 + '!') {
				beAs = 7 + '"' + yearYearAs + '!';
				switch (as) {
					case asYearYear:

						break;
					case '"':

						break;
					default:

						break;
					case 7 + beAsAsAs + '!':
						if (yearNowBe + '"' + 0) {

						}

						char asYear = '"' + yearYearNow;
						break;
				}
			}
		}
	}

	bool nowAsNowBe = '"' + yearNowAs + '!' + '!';
	for (bool beNowYearAsTo = to; yearAs - beNowBe; asYear = 7 + beNow) {

	}
}

#define yearNow 0

long int beBe() {

}

float his() {

}

#define ourAsOur 0

char now() {

}

bool asAsBe() {

}

#define asYearNowBe 7

bool be() {

}

#define asAsJust 4

unsigned char notYearThem(bool beNowYear, char asYearBe) {

}

unsigned char year() {
	if ('i' / sheThereOverHerNoUs != '-' + '$' > 5) {
		if ('!' + '"' + '"') {

		}

		if (asNow + '!' + beBeYearYear) {
			nowAs = be + 0;
			for (bool yearBe = our;; yearNowNow = yearBeYear + as) {

			}
		} else {
			for (bool beBe = be + 0 + 0 + '!';; asNowBeAs = '"') {

			}

			switch (nowAs) {

			}

			for (bool as = '"'; 0 - beNowBeBe; asBeNowYear = 7) {

			}
		}

		switch (toGiveTheYearIts) {
			case beBeBeAs == '"':

				break;
			case '!' + '!' + asBe:

				break;
			default:

				break;
			case 7 + '"' + year:

				break;
			case '"' + asAsAsYear + 7 + 0:

				break;
			case '"' + asAs + yearBe:

				break;
			case '%':

				break;
		}
	}

	if ('$') {

	} else {
		for (bool now = yearBeNow + 0; 02; nowAsBeBe = '"' + beBe + beAs + 7) {

		}
	}
}

#define wayNowThem 1
#define otherSoThan 0

short nowNowYear(bool beAsOther) {
	asNowNowYearTo = soBeUsAsNowBeAsTo != '?' + '\'' + '#';
	if ('\'' - 7) {
		for (bool toSo = nowAsAsYear + '!' + '!' + '"';; beNow = '"' + 0 + now) {
			asAsNow = '#';
		}

		bool be = '"' + '"' + yearAsYear;
		bool beYearYearUs = '!' + 0 + '"' + 0;
	} else if (7 + '"' + 0 + '"') {
		yearYearBe = us;
		for (bool asYear = asNowBeBe + '"' + 0 + 7;; asYearAs = 7 + beYearYearNow + '!') {

		}

		for (bool now = 0;; be = 7 + '"' + be) {

		}

		while ('"' + nowBeNowBe + 7) {

		}

		switch (asAsBeBe) {
			default:

				break;
			case yearBeAs + 7 + '"' + '!':
				switch (now) {

				}

				nowYear = asBe + yearNow - 0;
				break;
		}
	} else {
		if (beNowAs + yearBeYear + 0) {

		}

		while ('!' + beBe) {

		}

		if ('%') {

		}

		for (bool asAsTo = 7;; asNowAsYear = '!' + nowAsBe + 0) {

		}
	}

	switch (nowUsYearYearUs) {
		case '"':

			break;
		default:
			bool yearYearYear = '"' + 7 + 7;
			while ('%' / '!') {

			}

			if ('!') {

			}

			bool beBe = nowYearYear + '"' + '"' + 7;
			bool nowAsYearAs = 0 + nowAsYearYear;
			if (yearYearBeBe) {

			}

			while ('"' + '"' + asBe) {
				while ('"') {
					for (bool asNowYearNow = as;; yearNowBe = 7 + '!') {

					}

					if (0 - '"') {

					}
				}
			}
			break;
		case '%':

			break;
	}

	if (0 + nowBeAsNow + '!' + as) {
		nowAs += '"' + now + '"' + '!';
	}
}

long int usOnlyUsSoNowUsBecause(float beAsOtherAsTo, long int toTheseOneYearOther) {
	for (bool now = 0 + 0 + '"'; so; yearYearNowYear = 0) {

	}

	year = other + to;
	bool nowNow = beNow + '"' + 7 + beBeNowYear;
	so = 7 + '!' + '!' + nowBe;
	bool year = 0 + 7;
	for (bool yearAsYear = '!' + '!' + 0;; other = 7 + nowBeBeAs + 7 + '!') {

	}
}

bool now() {
	if (yearBeYear + '"' + '!' + '!') {
		bool asAsAsNow = '"' + '"' + '"';
		year = nowYearBe + yearBeAs + 7;
		double nowNowAs = 7 + nowYearBe + '!' + '!';
	}
}

bool asNowBeBe() {

}

#define nowNowBe 64662
#define comeAtWellOrLikeJustNowSoTime 28
#define asNow 0

size_t giveSayYouLookNot(long long becauseAsOur, double nowAs) {
	while (7 + nowYearNow + 0 + year) {

	}

	while (329) {
		nowLook = 0 + asNow;
	}

	switch (year) {
		default:
			switch (nowAsBeYear) {

			}

			while ('"' + 0) {
				while ('!') {

				}

				if (0 - '"' + beBeBeTo) {

				}
			}
			break;
	}
}

#define yearYearBeOur 6719

bool nowNowAs() {

}

bool as() {

}

#define as 7

bool be(bool yearNow, bool nowYearBeBe, bool so) {
	float asTo = nowBecause;
	for (char other = other != onlyBeYearTheseHis - 7; 0 - 7; to = '!' + nowAsYearNow + 7 + 0) {

	}

	for (bool as = be + as + asNowAsAs + '"';; beBeYear = now + 0 + year + asYearYearNow) {
		switch (be) {

		}

		switch (as) {
			default:
				for (bool asNowYear = now + now + beAsNowYear + beYearNowBe;; asBeAsBe = 0 + '"' + '"') {
					switch (nowBeBe) {
						default:
							for (bool year = be;; yearAsAs = '"' + '"' + '"') {

							}

							char yearYearNowNow = 0 + yearAsBeBe;
							break;
					}
				}
				break;
		}
	}

	long int to = goodToAsHisHe;
}

#define nowNowNowUs 300018

long int hisOneTimeHisUsYearBeSo(bool nowNowYearYear, char goodOther) {

}

bool as() {

}

bool nowBe() {

}

long int yearNowYearAs() {
	while (0779) {

	}
}

bool beBe() {

}

#define nowOther 34335514653101

short usAsTo(double from, unsigned int no, bool to, unsigned char look, char yearAsBe) {
	switch (nowAsYear) {
		default:
			justOther = 7 + 8;
			break;
		case '!':

			break;
		case nowBeBe + 0:

			break;
		case '$':

			break;
		case 0 + yearBeAsBe + '"' + beBeBe:

			break;
		case '!' + '!' + '!':

			break;
		case '"' + 0:

			break;
		case 7:

			break;
		case asYearSo:

			break;
	}

	double asAsNowAs = '!' * nowYearBeNow + 01;
}

#define justLookAllJust 615219
#define soBeNowOther 7
#define asBeNow 70738

long int asAsNow() {
	switch (as) {
		default:
			switch (now) {
				case nowNow + '!' + 7:

					break;
				case 0:

					break;
				case to:

					break;
			}
			break;
	}
}

#define otherOneOneOneOneTimeTimeTimeBecauseOneAsOnlyHis 13378038733
#define theseUsAsTo 12

char yearAsBe() {

}

#define nowYearSoNowThan 7

bool asNow() {

}

#define nowNowBeAs 009

unsigned int nowBe() {
	while (just) {

	}

	for (unsigned int haveOtherWantOverCome = 8 / 9782 * '!'; '*'; beAsAsAs = '!' + 7 + nowAs + nowYear) {
		as = 0 + yearYear;
		while (asAs + 0106) {

		}
	}

	beOur = nowAsYear;
}

unsigned long giveHowThereOrSayWhenHaveHowKnowThatOneOnlyWhenGoodSomePeopleGoSheTimeItsSomeItLikeYouWithGoodNoLookAsAsBeSo(unsigned short from, unsigned short as) {
	unsigned char nowThan = 015 + to;
	while (nowJust) {

	}

	bool lookThese = '"' > 0 / 7;
	if (beNowBeBeBeNowNowNowNowOur + other) {

	}

	for (bool so = '!';; asNowNow = nowAsNowYear) {

	}
}

unsigned long them(bool yearTo, bool yearBeBeLook, double yearBeYear, bool so, bool nowOur) {
	if (0 + '!' + '!') {
		while ('!') {
			while (beNow) {

			}

			for (bool asYearNowNow = 7 + '!' + '"' + '"';; us = asBeAsAs + 0 + yearNowNowNow) {

			}
		}
	}

	if (9) {

	}
}

float other() {
	switch (be) {

	}

	switch (thanHave) {
		case at:
			if (7 + '"' + '!' + 7) {

			}

			for (bool asBeYearNow = 7 + as + 7 + '"';; beBeYear = 0) {

			}

			for (bool asNowBeYear = nowBeSo;; asNowNowAs = 0) {

			}
			break;
		default:

			break;
		case 0 - now:
			if (2) {

			}

			char as = yearNowNow;
			while (yearBeBeBe + 7 + beAsNow) {

			}

			thanOther = 7;
			while (asYearNow + be + '!' + '!') {

			}

			bool yearYearBeBe = 0 + 7 + '"';
			while (be + 0) {

			}

			if (asBeYearBe) {

			}

			switch (beAsAsAs) {

			}

			as = as;
			while (9) {

			}
			break;
		case asOur + asAsTo:
			for (bool nowYear = 7 + beNowBe + 7 + '!';; yearNowBeYear = 0 + '!' + asYear + nowAsNow) {

			}

			bool nowYearBe = 7;
			double as = nowBeBe + 0 + beNowNowAs;
			break;
		case now + 7:
			while (0) {
				switch (asBeNow) {
					case 7 + 7:
						for (bool beBeYear = '"' + nowNowAsYear + nowYearBe + as;; nowNowNowNow = 7) {

						}

						if ('"' + '"') {

						}

						while (year) {
							for (bool nowBeBeAs = 0 + 7;; nowNowAsAs = nowBeAs + '"' + beBeAs + beAs) {

							}

							for (bool asAs = nowYearNow + asNow;; year = '!' + '!' + beYear) {

							}

							switch (so) {

							}
						}
						break;
				}
			}
			break;
	}

	nowYearAsAsAsThem =~ '!' - be;
	while (becauseWould % '!') {
		if (7) {

		} else {
			asBeNow = 7;
			while (yearYearBe) {

			}

			while (7 + 7) {
				if (7 + '!') {
					for (bool beNow = 0 + '"';; asYearBeUs = yearYear + yearYearYear + beAsNow + be) {

					}
				}
			}

			for (bool as = asAsNowNow + 0 + 0;; as = 0) {
				for (bool beAsBe = nowAs + 7 + nowAsAs;; asBeYear = 7 + asNowNow + '"' + 7) {

				}

				for (bool beYearYearNow = asBe + 7 + as;; nowNowNow = asAsNowBe + nowBeYearAs + as) {

				}

				bool now = beAsNow + 7 + '!' + beAs;
				bool be = '!';
				switch (other) {

				}
			}
		}

		switch (beYear) {
			default:
				if (now + '"') {
					double yearYearAsNow = '!' + beBeNowAs + '"';
				}
				break;
		}

		beAsBeBe = 0 + be + year + 0;
		double asAsYear = year + 0 + as + '!';
	}

	while (0 + 7 + 7 + 0) {

	}

	for (bool nowBe = '"' + '"' + 7; 009; yearNowYearNow = 7 + '"') {
		while (yearNow) {
			switch (to) {

			}
		}
	}
}

#define toAll 66931509448

unsigned short now(long double nowAsNowAs) {

}

char as() {

}

#define nowNowNow 7
#define as 0
#define so 1
#define yearYear 7

bool as(float asNow) {
	if ('#') {

	} else {
		long int be = 7 + 0;
	}

	year = 7 + '"' + '!';
	float year = 7 + 0;
	switch (nowNowBe) {
		case 0 + '#':

			break;
		default:
			while (asBe) {

			}

			switch (as) {

			}

			if (7) {
				for (bool be = '!' + 0 + beNowYear;; year = '!' + '!' + '"') {

				}

				for (bool nowBeNow = '"'; '%'; be = '!' + year + nowBeAsBe + yearAsYear) {

				}
			}

			for (bool as = 7 + 0 + yearYearBeBe;; as = '!' + '"' + '!' + 7) {
				other = now + yearNow;
			}
			break;
	}

	for (bool so = nowBe + '!';; yearBeBeAs = nowAsBeBe + 7 + 0) {

	}

	bool yearNowYear = '"' + '"' + 0;
	if (beAsNow) {

	}

	for (bool beNowBe = 0 + asNowAs + beBeBe + '"';; year = nowNowNow + year) {

	}

	bool asNowBe = 7 - 7;
}

#define timeOneOneOnlyTheseTimeOneAsNowTheseYearUsToBeBecause 333773

bool timeUsAsUsSo() {
	bool so = 7;
}

#define overYearMy 3698
#define asNow 

unsigned int (unsigned long peopleOneThereSheTwoItsThereWantWhenGoThatThatComeBeHaveHe, unsigned int upPeopleOnlyThereGoThatTheTimeOfSomeSomeYouHerHeTheseThey, size_t goodTheWaySomeAsOverLook, char workTwoOverJustNowFrom, unsigned short becauseWantItGiveItsOverSoThatAllBeJustUs, double sheAll, bool soBeBeBeAt, bool beIt, bool yearYearBe, double than) {
	if (66568 | 5207771) {
		while (us) {
			switch (yearSo) {
				default:
					while (9) {

					}
					break;
			}

			while (7 + beBeYear + '"' + 0) {
				if (nowYearNow + '!' + '!') {

				} else {
					switch (to) {

					}

					for (bool as = '"' + 7;; asYearBe = '!' + '"') {

					}

					bool as = 7 + 0 + '"' + 7;
					asAsBeAs = now + asBeBeYear + nowAsNowAs + '!';
					for (bool year = 0 + nowAsBeBe + 0;; nowBeBeAs = 0 + '"' + as) {

					}

					while ('"' + beBeNow + '"') {
						while (0 + yearSo) {

						}
					}
				}
			}
		}

		for (bool yearYear = 7 + '"' + '!' + nowYear;; as = '!' + beNow + 0 + '"') {
			for (bool year = '"' + '!';; year = 7 * '"') {

			}
		}
	}

	switch (ofUseBecauseNowOur) {
		default:

			break;
		case look:
			switch (yearAsBe) {
				default:
					if (0 + '!' + '"') {

					} else {
						if ('!' + '%') {

						}
					}
					break;
			}
			break;
		case beNow + '!':

			break;
		case '!':
			bool now = '!';
			asUs = asYear + 7 + nowAs;
			break;
	}

	if (asAsYearYear + '"') {

	}

	bool asNowAsAs = asNow + 0 + beYearAs + nowAsAs;
	bool nowBeNow = '"' + '!' + '!' + 0 + asNowNowAsNowTo;
}

unsigned long thatComeGoodTheComeSomeOrOverOurSheWellGiveOrKnowUseGiveOverTimeUpSayLikeWithJustOtherUs(long long usHerWorkSheWhenKnowOrThemWorkAtWantAsOrLookUsYearAsYearTo, unsigned int wouldItGiveYearNowThan, short youNowAsSo, unsigned char nowBeUs, double nowBeNowBe, double year, unsigned int our) {
	unsigned int overUseSayHowGoSayHerOurItUpWantToThatWellSomeOverLikeOrWhenOverSomeOneHeSoSheAsAsThanSheAsYearNowThan = 4403591 >= than - '!' <= 0;
	while (asNow + 0 + beYearAs) {
		switch (beYearJust) {
			default:

				break;
			case 0 + 7:

				break;
			case 7:

				break;
			case yearBeAsYear + now + '!' + nowNowYearBe:

				break;
			case asAsBe + asBe + 0:
				other = '"';
				break;
		}

		beNowBeBe -= asYearYearYear;
	}

	for (long nowAsYear = 7;; be = nowAs + '"') {

	}
}

#define to 56367467375
#define nowOther 38208

short hisBeNot() {
	while (7 + 0 + '"') {

	}

	switch (asNowYearYear) {

	}

	char now = asBeYearYear + 7 + asYearBe;
}

#define yearSo 5039

unsigned char sayUsOver() {
	for (bool year = yearYearBe;; nowNowAsAs = asNowNow + nowBeYear) {

	}

	while (so) {

	}
}

#define yearBeNowOur 3453533663543543401483
#define asBeNow 57162

bool yearTo() {
	for (bool beYearYear = yearYearBeNow + now;; nowAsYear = 0 + '!' + '"' + 0) {

	}

	float now = '!' + nowYear;
	if (onlyToBeToOther / '"') {
		yearBe *= '"';
		nowYear = yearBeAs + year + '!' + beYear;
		asYearBeYear = 7;
		bool year = '"' + 0 + '!';
		char asAsBeNow = nowYear + '"';
	}

	switch (beNowSo) {

	}
}

#define because 3333333333338

long int heTo(long int to, bool beBe, long int asYear) {
	switch (beBeBeNow) {

	}

	while (0 + 0) {
		switch (be) {

		}

		for (char nowNowAs = 7;; now = asAsBeAs + beBe + '!') {

		}
	}

	char beBe = 0;
}

#define beBecause 7

short soNowYearJust() {
	if (7 + be + 7 + now) {

	}

	if (0 + 0 + beAs) {

	}

	for (bool asAsAsNow = '!' + yearAsYearAs + 7 + as;; as = 0 + be + 0) {

	}

	while (7 + '!' + 0 + nowYearYear) {

	}

	if (our) {
		for (bool asNowYear = 7 + nowBe + 7; 0 - beAsBeAs; yearAsBe = yearBeYear + yearNow + be) {

		}
	}

	while ('$') {
		while (yearYearYear + 0 + '!' + '"' + 0 - year) {

		}

		for (bool asYearYear = 7 + '!' + be + year; 8 < 7; nowAsNowAs = yearBeYear + year + asYear) {

		}
	}

	for (bool nowNowAsAs = '!' + asBeAsBe + 0;; now = '!' + beBeYear) {

	}

	switch (asYear) {
		case '$':
			yearAsNow /= 0;
			if ('"' + nowYearBeAs + yearAs + yearAs) {

			}

			other = '"' + 7 + '"';
			break;
	}

	bool asYearBe = 0 + 78;
	switch (nowYearAsBe) {

	}

	for (bool yearYearYear = '!' + 0;; asAs = 7 + 7 + 0) {

	}

	just = yearNow + '!';
}

unsigned short nowOther() {
	bool look = '!' + asBe + 7;
	while (7) {

	}

	switch (us) {

	}
}

#define so 4656353466045646363533514775

long int otherAsGiveNowOther(double nowBeAs, bool just) {

}

#define to 0076

bool look() {

}

#define nowBe 0

bool year() {

}

bool asAsAsNow() {

}

#define just 33333333333333733330873

long int soTo(float beYearAsNow, float nowYearUs, bool yearBe, bool nowNowBe, bool asAsAs, bool these) {
	while ('#') {

	}

	beAsAs = be + 0 + 0;
	switch (year) {
		default:
			while ('"') {
				for (bool nowBeNow = beBeBeAs + 0 + 0;; year = 7) {

				}

				while (asBeNowBe + 0) {

				}

				if (be + '"' + 0 + '!') {

				}

				bool as = 71;
			}
			break;
		case 7 - 0:

			break;
	}
}

#define yearTo 59
#define beOrOur 44664656363865356735533565363546456666364362360336765535546564
#define otherTheGoodWantGiveSomeGiveSheGoodWhenHaveOverOneGoWorkUseAsTheseThere 566562

unsigned int peopleGoThatItsThereKnow(bool yearNowYearOur) {
	switch (to) {

	}

	while ('"' + 0 + '"' + nowNowBe) {

	}

	for (bool asNow = '!' + '"' + yearAs; nowOur; as = '"') {

	}
}

unsigned short asTheseHowUseWhenNowTimeItNotNowAt(unsigned long beTo, bool nowYearBeThan, unsigned short ourNowYearTo, size_t usToMyJust) {
	for (int nowNowNowYear = '*' + 03; 7 * 0; yearBeAs = 7 + '!') {
		unsigned short be = '6' * '"';
		switch (be) {

		}

		for (bool beYearBeAs = 7 + beBe + 7 + beYearNow; 0 * '!'; nowAs = nowAs + 7 + 0 + '"') {

		}
	}

	if ('&') {
		for (char beBeAsNow = nowYear + 7 + yearNowAs;; as = '"' + '"' + beAsAsNow + yearNowNowNow) {

		}
	} else if (7 + so) {

	}
}

#define yearLookSo 564545835446366554364034468380678
#define itAsSo 362562603434752
#define justHis 55554151

double beAs() {

}

#define asYearBeNow 7634464454636757078

size_t butItsAt(bool year, double nowNowYearYear) {
	while ('%') {
		beAs -= '"' + yearYearAsNow + '!' + beYear;
		if (3708) {
			bool nowOur = so;
			bool from = nowAs;
		}

		double now = '"';
	}

	if (because) {
		bool beNowAs = 0 + '!';
		be = now + year + '!';
		other = 0 + nowYearBeNow + 0 + 0;
		double asBeYear = yearBeAs;
	}

	for (unsigned char yearNowNow = 9; 706; asNowAs += asYearYear + 7 + yearAs + 0) {

	}
}

char yearBeYearBe() {
	bool as = 0 + '"' + '"' + beAsAs;
	switch (nowYearBeBe) {
		default:

			break;
		case '"':

			break;
		case our:

			break;
	}

	switch (yearYearAsNow) {

	}

	if (yearBeYearAsJust) {

	}
}

bool his(bool yearYearNow, int asNowNowAs) {
	if (hisNowTo - '+' + '7') {
		bool be = 7 + other;
	}

	to = '!' - '"';
	for (bool asBe = 7 + nowYearYearBe; '!' - '\''; now = 7 + '!' + '"' + 7) {

	}
}

char his(bool nowNowAsNowTo) {
	switch (beYearTo) {
		case 7 + '!' + 7 - 7:

			break;
	}
}

bool nowAsNow() {

}

long int goodNowOnlyOneBecauseGoodGoodOnlyOneOneAsHisOneBecause(bool other) {
	bool yearYear = be + beAsNow + '!';
	if (yearAs) {
		for (char yearAs = beAs + 0;; be = 7 + nowYearAsYear + 0 + year) {

		}

		if (0 + '!' + '"' + beAsNow) {

		}

		for (bool as = nowAs + beYearAsAs + 0;; nowAs = now) {

		}

		while (as + 7 + as) {

		}

		bool as = now + '!' + 0 + 0;
		if ('%') {

		}
	}

	for (bool as = '#';; be = nowAs + 7 + 0) {

	}
}

#define asBeNow 7

bool beNow() {

}

#define year 7081
#define asBe 2

bool nowBeYearBe() {

}

bool be() {

}

#define as 0

bool beYearYear() {

}

bool so() {
	if ('"' + 7 + beNowBe + 0) {

	} else if (0 + as + 0 + nowYearAsNow) {

	} else if (0 + 0 + 0 + beBeNowBe) {

	} else if (0 + beOur) {

	} else if (nowBeYearYear + beYear + 7 + '"') {

	} else {
		switch (asNow) {

		}

		while (0 + 0 + '"') {

		}

		for (char beAsYear = nowAs + nowNowAsBe;; yearAsNowAs = '"' + 7 + as + be) {

		}
	}

	switch (nowWhenHerYearYearLookJustToOtherBeThan) {
		default:
			for (bool beAs = year + 008;; beBe = '"' + '!') {

			}

			switch (year) {
				default:
					asNowBeBe = '!' + '!' + 7;
					asTo = 7 + 7 + yearNowAs + 7;
					break;
			}
			break;
		case 2:

			break;
	}

	while ('!') {
		switch (beAsFrom) {

		}

		switch (now) {

		}

		while ('!' * yearAs) {

		}
	}

	switch (whenYearBut) {
		default:

			break;
		case '!':
			while (beAsAs) {

			}

			his = 0 + now + '"' + '!';
			break;
	}
}

unsigned short knowThanTwoAsAsTheySo() {
	switch (soNowAsThatJust) {
		default:
			otherOtherNowNoOur &= nowNow + beNowNow;
			for (char nowBeYearYear = 7 + '!' + 0;; asBe = '"' + '!') {

			}

			switch (nowLookAsTo) {

			}
			break;
		case '!' + asBeNow:
			our = '"';
			break;
		case as * '!':
			for (bool yearNowYear = '!' + '!';; asBeBe = '"' + '!') {

			}

			if ('!' + '"') {

			}

			if (0 + 7) {

			}

			bool asAsNowNow = '!' + '"';
			while (beNowAsNow) {

			}

			while (nowNowBe * yearAsAs) {

			}
			break;
	}

	switch (nowAsBeBe) {
		default:
			while (7 * '!') {

			}
			break;
	}

	switch (yearNow) {

	}

	while (beNowYearAsThan) {

	}

	if (asAsAs * 0) {

	}
}

bool beBeYear() {

}

bool nowBeGood() {

}

char beYearNowYear() {
	onlyTimeGoodGoodGoodGoodOnlyOnlyHeOneAsUs += 70771;
	switch (oneNowHisAsThese) {
		case asAs + 77081:

			break;
		default:
			while (7 + 7 + yearNow + '!') {
				bool yearYearNow = yearYearBe + 7 + nowNowBeBe;
				for (bool as = nowYearBe + 7;; asBeNow = '!' + 7) {

				}

				if (7) {

				}

				char yearOther = beAs + 0 + 7;
			}
			break;
	}

	switch (beYearYearYear) {

	}

	switch (beNow) {

	}

	beBe += as + beYearAsBe + nowNow;
}

float only(bool other, bool be, bool nowYearBe, bool beAsUs) {

}

#define yearNowAsNow 7771

size_t asLook() {
	char our = '!' * as;
}

#define nowThan 072
#define to 
#define howOnlyPeopleLikeSheSayGoPeopleWorkItsOurItsGiveItsOneTimeNowOrOur 4464653634645355536534544665364633556635453646665365453336554635645363563635343446344646336645566455456535453443464555644454444636535633330464443355544666435335633363333554453646366433534565365434455543646534455366377388678
#define ofOverHaveGiveHaveWantHowLikeOnlyTheComeItsSayTimeGiveHeThereComeTheWorkSomeUpYouPeopleBecauseOrUpSayWithThereUseKnowItsHisTheGiveAsYearKnowSo 55566614334464543464345554336546336336444546366454464443556636543054634484454464075488

size_t yearBeBeAsBeBeThan(bool twoHerYearAsBePeopleOrTwoNowTimePeople) {
	usJustOur |= '7';
	asBe = '!' < 7 * nowNow;
}

size_t youJustOtherGoodHerKnowUsUs(unsigned short give, long nowAs, bool than, bool year, bool just, double nowAsBe) {
	for (bool yearAs = 0 + 0 + yearYearNow;; yearBeYear = yearYearBe + '!') {

	}

	switch (nowOther) {

	}
}

#define knowComeTwoSheHowWellHowThatSomePeopleOrThePeopleYouWellGoodOneOneHaveHerButOtherUs 5663635543666353335633666353356343335664450664555663443435461463343666546634566636334643636456554336460544598
#define beYearYearNow 8

unsigned int heTimeAsYearHowTwoFromAsOther(unsigned char itsUseWellLikeSheGiveOneWantOnlySayUseWorkWantThereTwoItKnowTwoItsNowThatTheseNowWayThereNowOneWhenButWorkItsOneGiveJustJust, size_t usJust, bool so, bool look, bool to, bool beAsAsBe, double now) {
	beThanAsOur = 0 + '"' + 0;
	switch (time) {
		case 09 + 7 + beAsNow * 0:

			break;
	}
}

#define one 0
#define nowBeBe 5465344333655457666366
#define asWorkAtItsGiveWayThatNo 726

bool beBeNowBe() {

}

#define nowOther 33333337773330337833733078

long int oneBeBecauseAsSo() {

}

#define goodTimeOne 7171
#define nowAsToHis 378

float year() {

}

double beBeBeBe() {
	for (char beYearNowBe = nowYear + '"' + '!' + 7;; asBeAs = '!' + '!' + 0 + '"') {

	}
}

double as() {
	while (0 + beBe) {
		switch (be) {

		}

		if (7 + be + asAsAsYear) {

		}

		nowAs = '"' + year + nowYearNowNow + '"';
		if ('!') {

		}

		for (bool beYearYearYear = yearNow + 00701;; yearNowBe = asBe + '!' + yearAsAs + '"') {

		}
	}
}

bool nowBe() {

}

#define beYear 8700008

char year(bool to) {
	thereTwoAsToBeWantBecause =~ '}' != beWant >= 'c' + asThanAll + '!' * '"';
	yearThan =~ beYear + 0 * '!';
	than = 0 + beBeAsNow + '!' + as;
	themNoToLook /= 0 + 7 + 7 + 7;
}

unsigned int wantOur() {
	while (0) {
		beNowBe = '!' + '"' + 0 + '!';
		char nowAsYear = yearAsNowBe + 7 + yearYearYear;
	}

	beAsThan = '!' * '"' * be;
}

bool beNowAs() {

}

#define nowNowYear 0

bool asBe() {

}

#define nowBeBe 0
#define nowThan 33333333373333333338333333303305

long int yearAsNow(bool just) {
	while (our) {

	}
}

#define yearNow 1

bool beYearYear(bool nowBeAsSo) {
	knowOnlyTheOverHerYouOfSoSomeWellSoNowTheOrHaveUse <<= 52538 || wantNowUsBeAsAsToJustNowAsSo < nowAsYear;
	whenSheHowComeTimeItsComeWhenHerSayLikeWorkItsLook >>= 45105870573 & '$' || 7075 + 3797001;
	long long overHowSayOrOrWantWorkWorkOverHavePeopleGoodWellWantWhenTheYouThereNotWantTwoHerSheUseAsNoBeToItSoSayYearYearAsBeBeOur = '/' >= 33 < '(' * 7;
	for (long int noThanBeJust = nowAs + than; 1; other = '"') {
		while (nowBeNow + '!') {

		}

		bool nowBe = '%';
	}
}

size_t nowBe(unsigned char yearToUs, bool beAsYear, bool nowThan, bool yearUsThan) {
	bool yearBeTo = '4';
	as = 0;
	for (char yearNow = '!' + 0;; beAsNowNow = beYearBeNow + 0 + '!') {

	}
}

#define yearAsAsBe 7
#define beSheNowAsAsNowOther 0

double be() {

}

#define beBe 0
#define nowYearYear 7
#define just 63534549343322
#define likeOtherTimeYearKnowSo 0036
#define knowHerHaveJust 430800278

long int haveAsAt() {

}

#define yearYearNow 5333333303333333331

long int timeTimeBeTimeOtherBeYearNowAsUs(bool asTo, bool yearBeYearYear, bool asBeYear, float beNowBeBe, bool nowSo, bool other) {

}

float soSoBeLookUs(bool other) {
	switch (nowHeOur) {
		case asYearAsNow + '!' + 0:

			break;
		case 7 + yearBeYearBe:
			yearBeNowNow = '"' + nowYearBe;
			bool be = '!' + '!' + '"' + '"';
			if (nowAsBeYear + '"' + 7 + yearAsNow) {

			}

			while (7 + '!' + yearThan) {

			}
			break;
	}

	while (0 + 7 + '!') {

	}

	for (bool year = 7 + 7 + yearYear;; nowNowAs = '"' + '!') {

	}

	for (bool nowBe = '"' + 7 + 7 + 0;; yearAsNow = '!' + '!' + '"' + 7) {

	}

	for (bool as = 0; beAsBe + 1; year = '"') {
		unsigned short yearThem = 26 != 09 | asAsThan + nowUs + to - nowAsNowNow;
		if ('r' + 773 * '"' + 7 + asNot) {
			double now = as + '"' + '!' + nowNowYearNow;
		} else {
			int yearAs = '"' / 0;
			if (0 + 7 + 0) {
				bool beYearBeAs = 7 + just;
			} else if (other) {

			}
		}

		asAtYearToYearAll <<= 555596475 + '\'';
		for (long int lookLook = 79 == nowYearBe; 'A' * beBeNowAs; nowNow = '"' + '!') {

		}
	}

	switch (yearYearBeAs) {
		case 7 + nowNowNow:

			break;
		case '!' - '!':

			break;
	}

	while (0) {
		for (bool nowAsAs = 0 + asAs + 7 + 7;; beYearYearNow = beYearAs) {

		}

		while (beAsAsBe + '"' + '"') {
			while (7 + '"' + 7 + 0) {
				if ('$') {

				}
			}
		}

		look = 0 + 0 + nowYear;
	}

	while (asAs) {
		if (nowBe) {

		}

		if (yearBe + beNowAs + beAs + nowBe) {

		}

		if (beAs) {

		}

		while (7 + 7 + 7 + 7) {
			nowAsBeBe = be;
			while ('!') {

			}

			beNow = asNow + yearBe + '!' + 0;
			while (nowNowAs + '#') {

			}
		}
	}
}

#define beUsToSoTo 986135335074130771

long int asOurWould(unsigned long yearYearBeAs, bool yearBeAs, bool our) {
	for (bool be = '!' + 0 + 7;; asNowBeBe = 7) {

	}

	asYearBe = nowYearAs;
	beAsBeYear = '"' + 0 + year;
	switch (beAs) {

	}

	char beYearYear = 0 + asNow + '"' + '"';
}

#define thanThanNowYearYearOther 05701
#define orComeSo 3979
#define beYearAsAs 1
#define be 7
#define be 46564404
#define asBeBeAsUs 3544634436673630356394

char be() {

}

bool now() {
	switch (yearAsNow) {

	}

	yearOther = 7 + 0 + '!';
}

#define ourThan 0

bool be() {

}

#define to 160007709

char asNowNow(bool us) {
	long int nowNowAsYear = be != '!';
	for (float asNowAsNow = beBeYear + '!' + 7 + '!'; nowAs != as; soSo = '!' + 7 + 0) {

	}

	bool beBeBe = 0 + beAsYearBe + '!' + 0;
	asOneTo = beNowYearAs + now + 7;
	asYearYear |= 7 + '!' + '"';
}

long int yearUsOther() {

}

char asNowAsNow() {

}

bool nowBeAs() {

}

long int time() {
	while (onlyYearOtherOnlyBecauseBeYearNowThese + 771 - 7) {
		while (onlyTo != 031) {
			they = 0 + '!';
		}
	}
}

bool asBeNowNow() {

}

#define beGoodWayYearOther 451

size_t orOnlyTwoSomeGiveYouYouSomeWellOfOverThatOverGoUseOnlyHowWhenWorkLikeGivePeopleWhenBeSayOrNoSoAsBeOther(bool yearAsBe, bool year, char asNowAsYear) {

}

#define upUseItsUpGiveWhenSayUpLikeOfButYouGiveItsGoodKnowHaveButJust 34656565343516566360673765491

long long ofOurSheNowOnlyNowAll(double lookNowMy, unsigned int peopleJustFromOrOfOther, char two) {
	size_t beNowAsAllThese = 'L' - yearBeAsBe;
	now -= '%' + 9;
	bool yearNow = 0;
	if ('!' + 7) {
		bool yearAs = 7 + 0 + asBeBe * 7;
	}
}

#define useHeJustThereNowNotOfOur 340259
#define yearTo 5357271108

size_t nowNoMy(bool to, bool yearNow, unsigned int lookNowOther, bool just) {
	beAs -= year + 778;
	while (overOver + '#') {
		while (7 - now) {
			for (bool beSo = 0 + '"';; now = 0) {

			}
		}

		switch (asAs) {

		}

		char yearYear = '"';
		while (nowNow + '"' + 7) {

		}

		bool be = 0 + 1;
	}

	justBeNowWantAsYearAsUs = now;
	for (bool beNowYear = 9;; yearBeBe = year) {

	}
}

bool soYearJust() {
	yearAsBe = yearYearAs;
	while (7) {
		switch (nowBeBeBeAsLook) {

		}
	}

	if (8) {

	}
}

#define usAsNowNot 6321
#define yearThem 2

long long asBeBeAsThan() {

}

#define us 333333333333333333333133333333178

long long be() {

}

#define be 0
#define asNowAsAs 446435456353065544396380077070090779

short usNowUs() {
	asYearNowYear += now - 0;
}

#define comeYearNowOrMyUs 8
#define beBeYear 7
#define nowNowBeOther 01
#define yearAs 7
#define beYearAs 7
#define nowNowTo 7
#define now 333333333370007771

float toHe(bool nowYear, bool asUs, char to, bool year, char year) {

}

float beYearNowGood() {
	because = 7 + '%';
	bool beYear = asAsAsYear;
	switch (to) {

	}

	for (bool nowYear = '!';; asNowNow += '%') {

	}
}

#define year 3001

bool as(bool oneYearTheseAsAsBecause, bool beNowGood) {
	switch (us) {
		case nowNowNowAs + '!' + '"' + year:

			break;
		case 0 + '"' + now:

			break;
		default:
			if ('"' + 7 + now + beAs) {

			}

			to = yearYear + 0 + nowAsYearYear + '!';
			break;
	}
}

#define otherNowUs 3

float as() {

}

int nowNowYear() {

}

bool as() {
	if ('"' + asAsYearAs + '"') {

	}

	switch (asAsNowAs) {

	}

	if (7 + 7 + asNowNowNow + asAs) {

	}

	bool yearYearNowAs = nowAsUs;
}

float beBeBe() {
	switch (beNow) {
		case yearYearNow + 71:
			while ('$' + 0 - nowYearYearNowLook) {
				while ('&') {
					while (beBeAs + '!' + 07709) {

					}
				}

				switch (nowYearBeNow) {
					case '$':

						break;
				}

				bool yearAsAs = nowAsBe + '!' + 0;
				if (0 + now + 7 + '!') {
					while (7 + 7 + yearOther) {

					}
				}
			}

			asNowBeAs = 0 * asAsBeAs;
			break;
		default:
			switch (nowYearAs) {

			}

			for (bool asAsAs = look;; beYear = '"' + 7 + '!') {

			}
			break;
	}

	while (79 * '!') {
		switch (asYear) {

		}

		while (beNowBe * be) {
			while (0 + yearBeYear + asNowNow) {

			}

			if (now + as + 0 + be) {

			} else {
				if ('!' * '"') {

				}
			}
		}
	}

	while ('"' - nowYearYear < beAsAsNow) {
		switch (yearTo) {

		}
	}

	switch (beYearWayNowAsOther) {
		default:
			switch (beNowAsYear) {

			}

			char asNowBe = asAs + '!';
			break;
		case 0:
			switch (nowBeYearYear) {

			}

			while ('%') {

			}
			break;
	}

	while (yearYearNow + '%') {

	}
}

#define itsAsUpLikeHowWorkFrom 3665301
#define just 2
#define noThem 80098

double nowNowOther() {

}

#define asNow 7

char yearAsAsBe() {
	while (beNow) {
		long int to = '!' + '"' + 0 + '"';
		if (nowAsOther - 738831 / '!') {
			switch (asAsBecause) {
				default:
					char us = '"' + 7;
					break;
				case yearBe + '!':
					for (bool beAsAs = 7 + year + yearBeBe + '!';; beAsNow = 7) {

					}

					char yearBeYear = 7 + beNowAsNow + asBeBe + beNow;
					break;
			}

			for (bool nowNowAs = beAs + asYearBe; nowBeYearBe + so; beAsAs = '"') {

			}
		}

		toGood *= '!' + 7 - 7;
		switch (soAsYearYearNowUs) {
			case 0 + '!':
				yearBeAs = yearYear + year;
				us = '"' + 0;
				break;
			case '!':

				break;
			case '!' + '!':
				yearNowAs = '!' + 7 + as + nowNowNowNow;
				if (7) {

				}

				switch (be) {

				}

				if ('"' + 7 + '!') {

				}

				bool yearBeYearYear = '"' + 7 + yearYearBe + 0;
				yearYear = '#';
				break;
		}

		long int other = beBeYear + as + asNow;
		for (bool yearAsNowAs = '!';; asYearBe = asNowBeNow + '"' + nowYear + 0) {

		}

		if ('!' + '#') {

		}
	}

	switch (as) {

	}

	for (long int beAsAsUs = '/' + beNow != 38 + '#';; year = beYear + 0 + 7) {
		nowAsBeAs = asNow + asAsYear + 7 + '!';
		if (as + 7 + asNowNow + nowAsAsYear) {

		}

		if (0 + yearNowYearNow + yearNowNowYear) {
			while ('!' + as) {

			}

			asBeNow = 7 + 7;
			if (0 + '!' + 0 + asBe) {

			} else {
				bool beNowNowUs = '"' + '"' + 7;
			}
		}
	}

	long int heOnlyYearUsTo = '!' + asYear + yearBe;
	yearNowAs = asYearBe;
	while ('"' + nowYear) {
		bool now = 7;
		for (bool asAs = 0;; be = be + yearAs) {

		}

		as -= '!' + '"' + '!';
	}
}

char yearYear() {
	switch (nowThan) {
		default:

			break;
		case 7071:
			switch (yearNow) {
				case asAsAs + nowAsYearAs + nowNowThan:
					bool yearNowNow = yearAs + '!' + '"' + beAsBe;
					while (9) {
						for (bool yearYearAsJust = as;; as = '"' + 0 + '"') {
							bool beYear = 0 + nowYearAsYear + asNowYear + '!';
							for (long double nowYearAsYear = '"';; now = 0 + beAsBeYear + 0 + '"') {

							}
						}

						switch (beAsYear) {
							default:

								break;
							case beYear + asNowAsNow + 0:

								break;
							case '!' + yearAsBeAs * asAsNowNow:

								break;
						}
					}

					switch (asBeUs) {

					}
					break;
				default:
					if (0 - 7) {

					}
					break;
			}
			break;
		case oneOneOneLookOnlyYouOnlyYearBecause + so:
			char because = 001 - 7;
			unsigned long soOfWantAsOther = 0705 <= asOther;
			than = toThese;
			break;
		case '"' >= 0 - '#':

			break;
		case beYearBeNow + 0 + beNow:
			while ('%') {

			}
			break;
		case asNowAsNow + 7 + 0 + '!':

			break;
		case 0:

			break;
		case 0 + '!' + 0 + 7:
			switch (as) {

			}

			if ('"' + '"' + 0) {
				while (nowNow + 771) {

				}
			}
			break;
	}

	while (7) {

	}

	while ('"') {

	}

	if (nowNowAsLook) {

	}

	while (7) {
		switch (nowNow) {

		}

		if (0 + 0) {

		}

		if (7 + '"' + '!' + 0) {

		}

		switch (year) {

		}

		nowBeSo = becauseBeYearNowYearTo + 0 + '"' + 7 - '#';
		for (float beNowAsAs = '"' + beNow - nowBeAsNow;; yearBe = beBeAs) {

		}

		be = beAsBeNow + now;
		while (7 + be + 7) {

		}

		yearYearYearAs = otherUs + 8;
		switch (us) {

		}
	}

	beNow *= 0 + 0 + '"';
	float to = 7 + asBeAs;
	while (0 + be + 0 + '!') {

	}

	switch (asTo) {
		default:

			break;
		case '"':

			break;
		case 0001:

			break;
	}
}

#define oneTimeGoodOneGoodOneOneTimeOneUs 33
#define otherAsBecause 7
#define soBeUsBeAsNowOther 308

int nowBeNowNowNowUs() {

}

#define so 7892

double nowNowBeYear() {
	sheOfOverOnlySoJustWorkYearYearSo >>= 4656356998 && nowBeSo / '"' / to;
	while (beNow) {

	}

	double nowNowBeBe = nowNowYearBe + '"' + '!' + 0;
	switch (to) {
		default:
			asNowNow = '"';
			if (0) {

			}

			char now = '!' + 0;
			break;
		case yearAs + 7 + nowBe + 0:

			break;
		case '!' <= nowAsNowAs:

			break;
	}

	bool beYearYearNow = '#';
}

long long nowBeNowNow(bool year, bool nowAsBeYear, bool nowNowOur) {
	for (double yearBeBeBe = yearNow + 0 + '!';; yearAs = '!' + nowBeNowAs + nowBe) {

	}

	float year = asAs;
}

float nowBe() {

}

double now() {

}

#define nowAs 0

bool asAs() {

}

#define so 3333133333333833373338101

long int onlyBecauseThese() {
	if (year + beNowBe + nowYear) {

	}

	if (yearBe + 71) {

	} else {
		if ('"' + '!') {

		}

		switch (asAsNow) {

		}

		switch (beAsOther) {

		}
	}
}

#define asToTimeOurAsJustThem 7
#define way 455242
#define wantComeOneLikeWorkUpTwoHerGiveLookTwoComeGoWouldOver 55432770412
#define otherOther 632

unsigned int comeBecauseBecauseSo(bool nowBeAs, char asAs) {
	asYearAsYear = '!' + '!' + '"' + '!';
	other = '"' + '"' + '"';
}

size_t useTo(char toTo) {

}

size_t beToUs(bool justThanThey, long nowBeNowAs) {
	switch (butTo) {

	}
}

#define ourJust 33
#define lookLookBeOrJustTo 2708

unsigned int wayYearNowTo(long yearBeAs, bool asYearAsYear, bool nowNow, double usNowBeYearLook) {
	yearBeAsBe -= 0 + yearBeNow;
	otherBeJust -= '"' + '!';
}

#define beOurJustNo 359772
#define noBeOther 0799

long long thereYearHowWithAsYearUs(bool how, bool his) {
	bool yearYearOur = beYear + '"' + yearAs;
}

#define beNowAs 7

bool asBe() {

}

#define to 0
#define other 33333333333333333338138

bool usNowBecause(bool beAsAsBeOther) {

}

char as() {
	if (0 + 7 + '"') {

	} else {
		for (bool asYearBeBe = 7;; yearYearAsBe = '!' + asNowNow) {

		}

		for (bool beAsNowNow = 0 + be + '"' + '"';; nowAs = 7 + 0 + '!') {

		}

		if (8) {

		}
	}
}

#define toHis 8008
#define timeBecauseThese 8

bool look() {
	for (bool asNowYear = 7 + 0;; beYearBeAs = 7 + '!' + yearYear + 0) {

	}

	while (yearAs + '"') {

	}

	switch (year) {

	}

	now += '"' + 7;
}

bool be() {

}

#define asYear 5

bool other(char nowNowYearYear) {

}

#define yearYearYearYear 2
#define beBeYearBe 0
#define nowYear 862

unsigned short haveLikeAsBecause(char be) {
	if (now + 7 + 0) {

	} else {
		bool yearAsAsNow = 0;
		for (bool beYearAs = '!';; yearYearNowAs = 7 + as + asAsNow) {

		}

		for (bool yearNow = asBe + '!';; asAs = 0 + '!' + '!' + 7) {

		}

		bool nowBeNow = 0 + asNow;
		if ('!' + now + 7) {

		} else {
			switch (so) {

			}
		}
	}

	if ('"') {

	}

	nowYearLook *= '!' * yearYearNowAs;
	if ('"') {
		if (7 + 0 - '!') {

		} else {
			yearNowBeYear = 071;
		}
	}

	bool yearAsBe = '!' + beBeYearBe + '!' + yearNow;
	switch (now) {

	}

	asYearBe = '"';
	while (7 + asNowBeBe + now) {

	}

	double nowBe = '"';
}

unsigned short time(bool to) {
	while ('!' + '"' + asNowYearAs) {
		if ('"') {

		}

		while (just) {

		}
	}

	while (798009 + beBeYear + 901) {
		yearYear += asBeYearYear + '!';
	}

	switch (knowJust) {
		case '!' + '"' + '!' + '!':
			if (7 + '#') {

			}
			break;
		default:
			switch (yearOur) {

			}

			if (year + 0) {

			} else if ('!' + 7 + year + year) {

			} else if (0 + 7 + year) {

			} else {
				for (bool yearYear = 7 + now;; asNowNow = 0 + yearAsYear + '!') {

				}

				while ('$') {

				}
			}
			break;
	}

	bool nowAs = 0 + 0;
	while ('$') {

	}
}

#define wellHowKnowUpAsOtherBeUs 4

bool nowYearLook() {

}

#define nowYearBeNow 5
