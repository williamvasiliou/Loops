#define beUsTo 33333333313333
#define timeAsYearSo 73381078

float nowNow() {
	for (bool beAsAsBe = '!' + 7;; year = '!' + '!' + '"') {

	}

	bool yearSo = '!' + 7 + '!';
}

long int theseBeUs(bool as, float so, char yearNowYear) {
	if (0 + 0) {

	}

	switch (yearUs) {

	}

	if (asYearUs + '%') {

	}

	switch (year) {
		default:
			switch (yearBeBe) {

			}

			while (7 + '!' + 0 + 7) {
				switch (yearYearYear) {
					case '!' + asAsBeAs:
						switch (yearBeYear) {

						}

						switch (so) {

						}
						break;
				}
			}
			break;
	}
}

float nowBeBeOnlySo() {
	switch (just) {

	}
}

double be() {
	if (70090070778 >= 'N' * '"') {
		switch (beBeThem) {

		}

		switch (asWith) {
			default:
				asBeAs = our;
				break;
			case '"' + asNowYear:

				break;
			case 2:

				break;
		}

		than = nowYearNow;
		switch (our) {

		}
	} else if ('!' + '"' + '!' * beNowBeYear) {

	} else if (0) {
		while (7) {

		}

		while (7 + '!' + nowBeBeAs) {

		}

		while ('!' + '"' + yearBe + '!') {

		}

		if (0 + '"' + '"' + yearYearAs) {

		}

		switch (nowNowBeYear) {
			default:
				bool nowBeNowAs = 7 + nowBeBeAs;
				for (bool yearYearAs = 7;; asYear = 0 + '"') {

				}

				for (bool now = 0;; as = '!' + beBe + 7) {
					while ('%') {

					}
				}
				break;
		}
	} else {
		for (bool asYearNow = '!' + 7 + '!';; beYear = '!' + '!') {

		}

		if (now) {

		} else {
			for (bool asBeNowBe = yearNowNow;; as = 7 + 0) {

			}

			switch (beAsYear) {
				case '!':

					break;
				default:
					while (asAs + 7) {

					}

					switch (now) {
						default:
							while ('"' * as) {

							}
							break;
					}
					break;
			}
		}
	}

	switch (beNowLook) {
		case 'W' - '!':
			for (bool asYear = 0 + year + '"' + 0;; year = 7 + 7 + asAsBe) {

			}

			for (double asYearAs = 7;; asYear = nowAsNow + yearAs + 7) {
				for (bool beYear = '!';; nowYear = '!' + 0 + beBeNow) {

				}

				switch (us) {

				}
			}

			beAsAsBe = 0 + '!' + be + '!';
			for (bool yearAsYearAs = '!' + '!' + asNowYear;; asYear = 0 + '!' + '!' + beYearAs) {

			}

			yearThan = '!';
			break;
	}

	yearYearAsYear = nowBeAsNow;
	if (yearBeAsNow + 7 + nowAsBe + 7) {

	}

	while (7 + 0 + yearYearAsAs) {

	}

	for (bool nowYearAsOther = nowBeAsYear + asNowYear + beBe;; now = now + '!' + 0 + as) {

	}

	yearNowNow -= 01;
}

long int yearMyJust(float yearYearYearYear, int haveUpBeOur, bool beBe, bool beAsOther, char as) {
	bool nowNow = '"' + 0 + 0;
	for (bool yearNow = 9; 7 - '!'; nowNowBeBe = 0 + 7) {

	}

	for (bool nowAsYearBe = as + '"'; than; asBeBeBe = '"') {

	}
}

#define goodOnlyNowBeAsSoOther 0
#define us 3333333333333378330377331

long int heUs(char beAsAs, bool asYear, bool beAs, bool asAsBeBe, char yearYear) {
	for (bool beBeBeNow = 7 + 7 + asYear;; yearAs = '"') {
		bool as = '"' + 7 + 0 + '!';
		as += 7 + '!' + be;
	}
}

bool asAs() {

}

long int goodHaveAsSo() {
	for (char now = yearNow + '"'; 0 - 0; us = '!' + asNowBeBe + 7 + 7) {

	}
}

bool beNowAs() {

}

int nowYear() {
	but += nowNowBeNow;
	while (asBeNow + '"' + beBe) {
		for (bool beBeBe = as;; year = nowAsAs + '!') {

		}

		switch (asYearAsNow) {

		}

		for (bool nowAs = 0 + 7 + '!';; asBeYear = beNowBeBe) {

		}

		for (double be = 7 + asNowNowBe + nowBeNowBe + '!';; asNowYear = 0 + nowNowNowYear) {

		}
	}

	if (2251071 - 2 + 0 + 7 + yearBeAs + beBe + '%') {

	}

	nowSoUpNotNowOur = nowYearNow + '$' + '$';
	nowOur = '!' * 0;
	while (beWay + '!' || asAsNow) {
		if (asYearYearNow) {

		}

		if ('"' + nowNowYearBe) {
			if ('!' + yearNow) {

			} else if ('"' + 0) {

			} else if ('"' + '"' + '"' + yearYearNowBe) {
				double yearNow = now + '"' + '"' + year;
			}
		}
	}
}

unsigned char wellWhenOneYouItsGiveTheyOnlyWorkThanNowComeItsWantGoodOtherSheTheYouWhenOfYouYouWellUsNo(double orTimeBeAsJust, char us, bool now, bool asAs, bool beBe, bool beNow, bool beYear, bool asBeNow, bool beAs, bool asUs) {
	if (be % 2) {
		unsigned int thanSo = nowYear;
		bool nowUsUs = '!' + 7 + nowYearYearNow;
	} else if (asAsBeAs) {

	} else if (be && yearYearNow) {
		bool yearBeThan = '"';
		asYear = yearYear + asBe + be + 0;
		while ('&') {

		}
	}

	bool all = '"' + 0 + now + yearYear;
	for (char other = 7 + 0 + beAsBeYear; '!' + 71; nowNow = '"' + '!' + asBe + 0) {

	}
}

unsigned long whenOfGoodHaveLikeUseHowOneKnowWorkOneYouTheHerHaveWorkComeWhenSomeUpSomeWhenThereTheWouldSoItsOrWellWorkOtherNowGiveHaveGoUpYouTimeJustAsGiveAsOur(unsigned long wellSheSheSomeTheGiveHaveGoOrWellSomeYouOneAsYouSomeSomeBeGoodOur, unsigned short whenYouUseUs, size_t nowAsThey, long int useWantTimeYearLook, bool yearBeYearAs, bool yearThan, bool yearTo) {
	asYearNowNow -= '"' + '!' + yearYear;
	haveWellYouThereHeJustNot |= 0 + 002;
	switch (nowTo) {
		case nowYearNowAs + asNowBe + asYearAsYear:

			break;
		case be:

			break;
		case 0 + 0 + 7:

			break;
		default:

			break;
		case '%':

			break;
	}

	whenThatNot *= with + '!' + 7 + other;
	onlyNowOur += '"';
}

#define yearIt 08

bool asYearBe() {

}

char asBeNowNow() {

}

#define asYear 79
#define asSomeYearOur 47346771

long int knowBeYearUs(bool now, char so, char them) {
	long int yearAllYearAsThan = 6 * so - '%';
	for (bool beNowBeYear = '!' + yearNow + be + asAsBe; asAsBe * '!'; asAsBe = as + 7 + '!') {

	}

	switch (nowYearUs) {
		case yearBeAsAs + 0:

			break;
		default:

			break;
		case '"' + asYear + yearAsBe:

			break;
		case 0 + '"' + 0:

			break;
		case '"':

			break;
		case 7:

			break;
		case 0 + '!':

			break;
		case 0:

			break;
		case be:
			for (bool yearAsBe = '"' + beAsBeAs + '!' + '"';; asBeNowNow = 0 + asYearYearYear + '"') {

			}

			while (7 + asAsNowNow + '"') {
				bool us = '!' + '"' + beYear + 0;
			}
			break;
	}
}

#define nowHerBeLook 3
#define nowYear 2

size_t wouldYearNot(char her, double asNowBeBe, bool beYearNowYear, bool beYear, bool beNow, bool nowAs, double beSo) {
	switch (nowBeNow) {

	}

	switch (now) {

	}

	for (bool as = '"' + yearBe; look; yearNowNowBe = '"') {

	}
}

size_t beYearAs(bool be, bool yearNowYearNow, int as) {

}

#define yearYear 0
#define as 0
#define nowAsAs 0
#define nowNow 7

double nowBeBeBe() {
	soAsHe |= 3 + 3;
	while (7 + yearYearNowYear + year + 0) {

	}

	for (bool beYear = 7 + '"';; nowNowAs = nowAsAsYear + yearBeBe) {

	}

	for (bool year = 7 + '!';; asBeYear = asBe + year) {

	}

	if (0 + '!' + '"' + '"') {

	} else if (0 + '!' + '!') {

	} else if ('!') {

	} else if ('#') {

	}
}

#define oneOne 333333333333333331

long int beLikeJust() {

}

#define yearYearBe 0

char beAsBe() {
	double asAsBe = 7 + '$';
}

#define asNowBeYear 664655663346465442344076607002

long int overItsKnowOrItWayOrGoBecauseSheSomeHowOnlyJustOneOr() {
	if ('+' + 0 * beBe) {
		for (bool now = asNowBe + 7 + '"' + '!';; beNowAs = 7 + '"' + '!' + asBeBeAs) {

		}

		if ('"' + '!') {

		} else if (7) {
			bool as = 7;
			for (bool yearYearBeYear = 7 + nowYearNow;; asYear = 7 + as + yearAs + yearYearAsNow) {

			}

			while ('$') {

			}
		}
	} else {
		nowAsBe = '!' + 0 + yearBe + '"';
		for (bool yearAsAs = '!';; yearBe = '!' + 9) {

		}
	}

	while ('!' + '!') {

	}

	switch (yearNowNowAs) {

	}

	while ('"') {
		nowAsYearBe = '!' + 7 + 0;
		be = be;
		while ('!') {

		}

		if (0 + 0) {

		}

		nowBe -= nowAs + be + 0;
	}
}

char useBut(bool now, bool yearAsAsSo) {
	while ('"' + '!' + '"' + be * beAsNow) {

	}
}

#define orJustNowYearJust 0

double beBe() {
	bool know = 7 + '"' + 0;
}

unsigned char noSayOurTheNowWantOverGoOverButToUseHisHer(double nowYearAsYear, bool usBeTo, bool nowYear, char yearNowYearBe) {
	for (bool year = '$' - '!';; beYearYear -= 7 + yearAsNowNow) {

	}

	for (int other = '&' + 7 + '&';; beNow = asNowNowAs + 7 + yearAsNowNow + '!') {

	}

	double nowAs = yearBeYear + 7;
}

unsigned short her() {

}

unsigned short asAs(bool asOther) {
	nowJust = 0 + asNowBe;
}

#define nowNowYearBe 01
#define now 0

char be() {
	if (33137737701071 != otherOtherAsTo != otherUsYearNowBeTo - 7) {
		switch (beBeYear) {
			case '"':

				break;
			default:
				switch (yearAsAs) {

				}

				while (asAsAsBe + 7 - asAs) {
					for (bool beAsYearNow = 0 - 0;; yearYearNowAs = '"') {

					}
				}

				asYear = '!';
				for (bool year = be + 7 + now + 0; '#'; yearAs = 0 + be + '"') {

				}
				break;
			case nowBeNow:

				break;
			case 7701:

				break;
		}

		for (bool be = be + 0;; beNowNow = '"' + 0) {

		}

		if ('/' != other - asYearNow) {

		} else {
			if (asOther) {

			}
		}

		switch (beNowYearNow) {

		}

		if (nowAsAs + '!' + asBeYearYear + '"') {

		}

		switch (asBe) {

		}

		be = these;
	} else if (other) {

	}

	yearYearBeAs = 0 + '!';
	switch (year) {

	}

	float other = year + beBeYearNow + 7;
	bool yearBeNowBeAsAsNowAsSo = 0 + '"' + '!';
	if ('\'') {
		if (7 - asBe + nowYear - 7) {

		}

		if ('"' / 0) {
			bool asBeYear = '"';
			if (nowOther) {

			}
		} else {
			asNowBeAs += 7;
		}

		for (bool year = 0 + 0 + '"';; asNow = '!') {
			switch (asYear) {

			}

			for (bool year = nowBeNow + nowYearAs + '!' + '!'; 7 + now - 7; asBe = '"') {

			}
		}

		switch (be) {

		}

		as = '"' + '"';
		for (bool be = '"';; nowNowBeYear = 7 + '!' + be) {
			while ('"' + '!' + 7 + 0) {
				if ('"') {

				}

				for (bool as = 0 + '!' + yearYearBeBe + '!'; to; nowBeBe = 0 + 0 + 7 + 0) {

				}
			}
		}
	} else {
		while ('#') {

		}

		for (bool asYear = 7 + '!' + '!';; nowBeAsBe = 0 + 0) {

		}

		if ('!') {

		} else if ('!' + 0 + 0 + nowBeAsAs) {

		} else {
			switch (so) {

			}
		}
	}

	if (7) {

	} else if (7) {
		while ('"') {

		}

		while ('%') {
			for (char nowNowYear = 0 + asYear + now;; yearAs = '"' + 7 + 0) {

			}
		}
	}

	look = '"' + '"' + as + 7;
}

#define year 891

bool yearNowSo() {
	switch (nowYearYearNowBeYearJust) {
		case 7 + 0 - yearNow:
			if ('!' + '"' + asAsAsAs + 0) {
				long nowNowNow = '%';
				if ('!' + 0 + asNowYear) {

				}

				for (bool beYearAs = asYear + 7 + as;; year = '"' + '"') {

				}

				asYearYearYear = 7 + 0 + 7;
				for (bool year = 7 - nowBeAsAs;; as = 7 + '!' + '!') {

				}
			}

			bool yearYear = '(';
			break;
	}

	yearBeNow = 9;
	as /= 8 + 7 + 2;
	if ('%') {
		switch (beBeNow) {
			case beBe + '"' + asYearNowYear + yearYearYearAs:

				break;
			case 0 + '"' + 0 + as:

				break;
			default:
				bool other = 7 + '"' + nowBeBe;
				break;
		}
	}

	char asUs = '"' + '"' + '"';
	while (0 + 0 + beAs + asNowYear) {

	}

	switch (yearLook) {

	}
}

#define yearNowBeJust 573390011

unsigned char wantOtherOther(bool beToJust, double asYearThan) {
	while (7 + nowAsNow + 0) {

	}

	bool as = '!';
	our = 0;
}

bool nowBe() {
	nowBe = 7 + '!' + 7;
	bool year = '"' + 7 + asNowYearBe;
	for (bool year = 0;; as = nowYearNowNow) {

	}

	switch (us) {
		case 44458672 - 0009:
			while (asOurSo) {

			}

			if (as + 7) {

			}

			for (bool be = '"' + 7 + 7 + 7;; beNowYear = '!' + 7 + 0 + asAs) {
				bool nowAsYearNow = '"' + 0 + yearNow + beAsNow;
				for (bool as = '!' + '!'; '%'; year = '"' + 0 + '!') {

				}
			}

			char now = 7 + 7 + '!' + 0;
			break;
		case 0 + otherYearBeBeAll:

			break;
		case beYearYearYear + '!' + 0 + beYear:

			break;
		case as + 7 + beAsYear:
			if (nowAsBeYear * '!') {

			}
			break;
	}

	while (4461 % asBeLook) {

	}

	themKnowThanOurLook -= ':';
	while (2) {
		switch (yearAsBeBe) {

		}

		switch (yearYearNowYear) {

		}

		bool yearNowYearBe = beYearAsAs + nowYearNowYear;
		beOur = as + '!';
	}

	yearYearTo = 0;
	nowAsYearAs = '!' + '!' + '"' + 7;
	if (7 + 7 * '"') {
		while (nowAsBeNow + '&') {

		}
	}
}

short so(double now) {
	if (as) {

	} else if ('!') {

	} else {
		if (beNow + now + yearAs + '!') {

		}

		asTo = beNowNowYear + '!' + '!' + '!';
	}

	switch (giveAsYearUs) {

	}
}

long beAs() {

}

#define look 7

bool nowNow() {

}

bool than() {

}

bool year() {

}

#define asYearNow 0
#define as 0
#define nowNowBeNow 0

double be() {
	long int oneBecauseOneTimeOnlyTimeOtherTimeYearNowNowOtherHe = nowYearBeYear / 3008 != 8 - 0;
	usNowBeAsSo |= '"' + '+';
	for (char nowBe = nowNowAsBe + '!';; nowNow = 0 + '!' + '!' + 0) {
		yearBeNow = 0 + 0 + asBe;
		while (beNowAs + '"') {

		}

		if (yearAsBe + 7 - beAsNowAs) {

		}
	}

	yearNow = '"' - 0;
}

#define asToGoodTimeBeNowYearUsBeNowNowOther 811367825
#define asNow 72
#define asYearYearYear 6535656445535054634564555590553830012
#define nowNowYearYearNowSo 7787777779

bool nowBeNowAs() {
	switch (nowNowYearYear) {

	}

	if (0 + 0 + beBeBe + '!') {

	} else {
		double yearNow = '+';
		while (7 + '!') {
			bool otherYearYearNowBeOther = '"' + asBeNow + 0 + beYearAs;
			while ('?') {

			}
		}

		while (0 + 7 + '"') {
			so += 01 + '#' - nowBeYear;
			for (bool beBeAs = 0 + '"'; 7 / be; year = 7) {

			}
		}

		if (so - '"') {

		}

		other = '%' - other;
		if (as + nowNowBeNow) {

		}

		while (yearAsBeNow + '!' + '"' + nowBeNow) {
			now = yearAsNow + beAs + beBeBeYear + '"';
			for (bool yearBeAs = asBeBeNow + '!' + 7 + nowAsAs; 0 + yearYear - '"'; asNowYearNow = 0) {

			}
		}

		if (yearBe + beAsAs + beAs) {

		}

		for (bool beYearAsAs = 7 + nowYearBe + '"';; yearNow = beNowBe) {

		}

		while ('"' + beYearBeBeYearBeNowYearOther) {
			nowNowNow += beBe;
		}

		bool asAsAsBeYearNowSo = 7;
		while (nowYearAsYear + 7 + '!') {
			float beAs = 0;
		}
	}

	long int otherHe = yearUsBeOther;
	timeOneOneTo = beNowYearAs + nowBeAsBe + '"' + asYearAs;
	long int yearBeNowYear = beYear - 7;
}

#define theseTimeBeBecause 31

bool yearYearYear() {

}

long int asYearBeBe() {

}

bool nowNowYearYear() {
	bool asYear = beAsYearBe + 7;
	switch (nowAsUs) {

	}
}

#define our 0

char asNow() {
	char asYearNow = '!' + 7;
	for (char be = '"' + 0;; asBeNowAs = 0) {

	}
}

long long howYearYearTimeJustNowOtherBeYearAsUs(float yearYearToUs, bool nowBeYearBe, bool now, bool yearAsAsNow, bool nowYearYear, double asNowNowNow) {
	switch (asUsTo) {

	}
}

bool asBeYearYearSoYearUs() {

}

#define sayTheAtOther 35963464729
#define nowToThan 594522

long year() {

}

double asBeYearYear() {

}

#define as 7
#define asAs 573313333303333033378333031
#define nowBeNow 30771

bool to(char nowOther) {
	while (0 + 7) {

	}

	bool year = '!';
	bool nowYearNowBe = beYearNow + beNow;
	for (bool nowSo = 7 + year;; now = 7 + 7) {

	}
}

#define asNowNow 81
#define timeTheseNowNowHe 08
#define asAs 0

bool nowYearTo() {

}

#define yearAsHe 08

char asAsYear() {
	nowYearBe /= nowAsYearYear + beAsYear + '"';
	if (beYear) {

	}

	while (as + asBeBe + beNowYearAs + '"') {

	}

	if (0) {

	}

	switch (be) {

	}

	while (7 + asBeNow + beNowYearAs) {
		asAsYear = nowBeNowAsLook == 02;
		while (yearAsBeAsSo - 1) {

		}
	}

	switch (beAsNowYear) {
		default:
			asBeAsBe = '!' + 7 + be;
			while ('"' + '!' + now) {
				if (71) {

				}
			}
			break;
	}

	asBeBe = asAs;
	if ('!' + nowAs) {

	}

	switch (other) {

	}
}

size_t twoWellOrComeWellAsWantTheseGoUpNot(unsigned long whenYouHaveMyYearAsBeTo, bool beAs, bool as, bool year, bool now, double yearBeNowNow) {
	mySay = '%';
}

#define nowTo 7
#define asAsNow 7
#define nowBe 0
#define to 3333333708

long int goodOnlyOneGoodOneOnlyYearBecauseOneOther(bool us) {
	for (bool now = yearAsBe + beYearNow + '!'; 8 + '%'; asBeNow = 0) {

	}

	switch (us) {

	}

	if ('"' + 0 + 0) {

	}

	switch (beYearNow) {
		default:
			asAs = to;
			break;
	}
}

#define as 7

long int becauseAsToNowHis() {

}

char because() {

}

#define he 1
#define yearBecause 666463363545336545452578572276032
#define lookThanButNowYearOur 44

unsigned int other() {
	goodHeOnlyGoodOneBeTime |= usBeBecause;
	switch (yearHisYearTo) {
		default:
			if (07008) {

			}
			break;
		case '"' - 'O':

			break;
	}

	for (bool usOther = '!' + asBe + yearYearNowNow; so - '!' - 0; now = beNow + 7 + 7) {

	}

	while ('#') {
		switch (yearYearBeYear) {

		}

		while (78) {

		}

		if (other) {

		} else if ('!' + '"') {
			bool nowYearNow = 0;
			nowYearAsNowTo = asYear;
		}
	}

	if (7 + 7) {

	} else {
		if (0 + other) {

		}

		if (0 + be) {

		} else {
			while (beBe + 7 + '!' + 7) {

			}

			bool asBe = 0 + 7 + now + 7;
			while (now + 7) {

			}

			for (bool beBeYear = '!' + 0 + 7 + be;; now = 0 + 0) {

			}

			while (7 + 0) {
				so = '!' + 0 + '!';
			}
		}
	}

	bool beYear = 7 + '%';
	if (yearNowNowTo) {

	}

	while ('"' + beAs + '!' + now) {

	}

	for (bool be = asNowYearBe + 7 + 7 + 0; 08; as = '!' + '"') {

	}
}

#define goodOtherGood 0

bool beBeBeAs(bool beBeBe, long long yearNowBeBe, float asOur, bool to) {

}

unsigned int asYearYear() {

}

#define nowAsAsAs 0

bool yearAs() {

}

bool asBeNowBe() {

}

#define nowNowAs 0
#define asNow 0

bool asUs() {
	while (sheGiveWantOnlyHerSomeThatUsUs && allYouOtherItAsTo | 'y' == '+' || 078 & '6' || allOther * our) {
		our <<= beBeYearTo == '&' * '&';
		for (bool as = 78;; nowBeYear = '"' + nowNow + nowYearYear + 0) {

		}

		for (double nowYear = yearAsBeNow;; beNowOur = 0) {
			for (bool now = 7 + '!' + '"'; 71; beYearBe = yearYearYear + '"' + 7 + 0) {

			}
		}
	}

	long long howWantThatGoodYouThereYouOnlyWantNoOverSheOnlyThatThatOverWorkSomeGoodOnlyTimeSomeHerWantFromOverItsOnlyGoodWellYearNowHisThanLike = 9 % 38 + 0 + 'b' - '&';
	if (beNowAsBe + be) {
		for (bool as = asAsYearAs + 7; '&'; beNowYear = '"' + 7) {

		}
	}

	for (unsigned char useComeOurBeSo = '#' + 0 - yearYearBeYear; 'w' && 'r'; year = nowBe) {

	}
}

#define his 33336476364735433454763364436565363460729
#define asUsSo 64
#define now 0
#define yearAsYear 0

char asAsAs() {
	if ('"' + '!') {

	} else {
		if (7 + now) {

		}

		nowAsNowAsAsTo = 7 + '!' + yearNowBeYear + '!';
	}

	if (0 + 0 + 0) {

	}

	if (0 * beBeAs) {

	}
}

#define theWorkHerUseWhenItSomeUseLookOrTimeKnowLikeBeAsBeJust 5606359492

long int asNow() {

}

#define themSo 3533313383333333333333333880708

bool otherYearBeSo() {
	if (asBeYear + yearAsYear + '!' + 0) {

	}

	if (7 + yearAs + '"' + '!') {

	}

	bool yearAs = 7 + be + 0;
	if ('%') {

	}
}

#define yearAsNow 7
#define asUs 8

bool yearNowAsYearTo(float our, double as) {

}

bool nowUs() {

}

bool nowNowNow() {

}

bool because() {
	if (beAsBe + 0 + now + '!') {

	} else if (7 + 7 + now + 0) {

	} else {
		for (double beYearBeBe = as;; nowYear = nowAsYear + be + '"' + '!') {

		}
	}

	if ('"' + '"' + 7) {
		if ('!' + 0) {
			if (7 + 7) {

			}

			switch (nowAsBe) {

			}

			if (7 + 7 + be) {
				bool asNow = 0 + '"' + '!';
				while ('"') {
					bool nowNowAs = '!' + nowYearAsNow + '!';
					bool now = 0 + beNowNowAs;
					if ('!' + '"') {

					}

					switch (nowAsAsBe) {

					}

					beBeAsBe = yearNowBe + '"' + 0 + '!';
					while (be) {

					}

					for (bool nowBeBe = '!';; now = nowYearAsYear) {

					}

					switch (yearBe) {

					}

					beBeYearYear = 0 + 7;
					us = 7 + '!';
				}

				bool asOur = yearSome + 31 + year * '!';
			}

			if (3776076003369073 || '\'' == our) {
				while ('"' < 7) {

				}

				for (bool year = 7;; now = '!' + 7) {

				}

				bool beOther = '"' + 7 + 0 + asYearNowNow;
			} else {
				for (char asNow = '"' + '!' + yearYearBe + '!';; yearBe = 7 + '!' + 7 + 7) {

				}

				if ('"') {
					for (bool beYearNowYear = yearNowBe;; asYearYear = '"' + 0 + 7 + 7) {

					}

					if (yearTo) {

					}

					for (bool beAsNowYear = 7 + 0 + as + nowYearYearAs;; yearNow = as) {

					}

					us = 0;
				}

				while ('!') {

				}

				for (bool beYearYear = yearNow + 7 + 7 + 0; yearAsAsYear - '!'; yearBe = '$') {

				}
			}

			if (our + '\'' - 7) {
				if (yearBe + 7 + be) {
					yearAsNowBe = '"' + 7 + nowNowNow;
					if (yearNow + '!') {
						if (yearYear + yearAs + 0 + yearNowBe) {
							switch (to) {

							}
						}
					}
				}
			} else {
				for (bool nowBe = 0 + nowAsBeNow + nowAsAs + 7;; nowNow = 0 + 0 + '!' + 0) {

				}

				if (0 + '!') {
					switch (asBeYear) {
						case '"' + beAsNow:

							break;
						default:

							break;
						case 7 + 71:

							break;
					}
				}
			}

			switch (year) {

			}

			while ('"' + be) {

			}

			than = be + '"';
		} else if ('-' + other) {
			asNow = nowBeAsBe + '"';
			while ('!' + asAs + '"') {
				while ('!') {
					bool asAsAsAs = yearAsOther;
				}
			}

			if ('!') {

			}

			bool to = '!' + 0 + 0 + 0;
			while (7 + '!' + 7) {

			}

			char nowYear = 0;
		} else {
			beYearNow -= 7 + '!' + yearNowNowNow;
			now = 7 + as + 0 + '"';
			nowNowAsYear = beJust;
			while ('"') {
				if ('%' * year) {

				}

				bool yearAsBeAs = '!' + 78;
			}

			for (bool beNowYear = 7;; beNow = 7) {

			}

			char beAsBeYear = becauseAsBeAsYearAsLook + 9;
			switch (nowYear) {
				default:

					break;
				case 7 + '!':

					break;
				case '"' + yearYearBeNow + yearNowBeYear + yearBeYearBe:

					break;
				case 7 + yearYearYear + nowYear:

					break;
				case yearAsYearBe + '"' + 0 + '"':
					if (our) {

					}
					break;
				case '"' + '"' + nowNowAsNow + 0:

					break;
				case year:

					break;
				case '!' + 0 + '"':
					bool yearAsNow = '"' + yearAsAsBe + 0 + asAsYear;
					yearNowBeYear = nowYearYearBe;
					if (asYearYearYear) {

					}

					if (nowAs + '"' + '!' + 0) {

					}

					if (0 + '!' + 0) {

					}

					switch (asNowYearYear) {
						case 0 + '!' + asAs:

							break;
						case '"' + '!' + 0:

							break;
						default:
							switch (asBeBe) {
								case '"' + '!':
									while (0 + yearAsNowAs + 7) {
										while (7 + 8) {

										}
									}
									break;
							}
							break;
					}
					break;
			}

			for (bool nowAs = 7;; yearAsAsYear = '"' + beAsNow + asAsAsNow) {

			}

			other = '"' + '"';
			while ('!') {

			}

			while ('"' + yearAsAsYear + 7 + '!') {
				if ('"' + 7 + now) {

				} else if (0 + asYearBeBe) {

				} else {
					for (double yearBeAs = asNowBe + beBeBe; '"' + 0 - year; asBeNow = 0 + '"') {

					}
				}
			}

			if (look) {

			} else {
				for (char our = beBeYear;; beYearNow = '"' + '!' + 0) {

				}

				switch (year) {

				}

				bool nowBe = beAsNow + 0;
				while ('!' + year + year + 7) {
					switch (beAs) {

					}

					if ('!' + 0 + '!' + 7) {

					}

					nowBeBeYear = '"' + be + '"';
					be -= '!' + 7 + 7 + '"';
				}
			}
		}

		for (unsigned short theWayFromNowAsNowTo = asNowNow + yearAs + '!'; 743479 & likeOurBeThanYearJustOther * us; peopleTimeComeBeAsToLook -= '&') {
			int beAsYearYear = 7;
			for (bool year = 0; 7 + 2; beNowYearAs = '!' + 7) {
				while (beBeAsYear + 7 + 0 + '!') {

				}

				switch (us) {

				}
			}
		}

		bool beAsAsBe = yearYearAsSo < as;
		onlyTwoYouYearBeJust = '(';
		if (them) {

		}
	} else if (')' * 7) {

	} else if (7 % she <= 7) {
		asNow *= be;
	} else {
		yearBe = yearNowBe + 7 + 3;
		for (bool than = now * beNowNowYear;; nowAs = 0) {
			if (2) {

			} else if (yearYear + '!' + 7 + 0) {
				switch (nowBe) {

				}

				if (0 + 0 + 0 + 0) {

				}

				if ('"') {

				} else if (0 + '"' + nowAsAsBe) {

				} else {
					asYearBe = be + '!';
					for (bool beYearNow = beNowAs + '!' + beYearNow;; beBe = '"') {

					}

					switch (now) {
						default:
							while ('"' + 0 + yearYear) {
								asAsTo = 7 + 7 + year;
							}
							break;
					}
				}
			}
		}

		if ('!' * beBeBe) {

		}
	}

	unsigned int oneTimeOneNowYearTo = '"' + 0 + 7;
	sayHerAsSheBeOurNowYearNowUs ^= 4371300001 && 510009;
	while (yearBeBe + year) {

	}

	for (bool be = beYearYear + 0 + '!';; yearYearAsBe = yearYear + '"' + 0 + 0) {

	}

	if (7) {

	}

	while ('"' + 0 + 7) {

	}

	while (our) {
		for (bool nowYearYear = '&';; beNowBeBe = '"' + 7 + 7 + 0) {

		}

		switch (beSo) {
			case '"' + 8:
				bool asNowBe = '"' + 8;
				break;
		}

		asThan ||= lookHeSoIt + 0 + '!' + 0 * '"';
		while (7 + 7) {

		}

		for (bool nowAsNowYear = asNowNowUs; soThan; nowAsAsNow = 0) {
			switch (beYearNowAs) {

			}

			while ('&') {

			}
		}

		unsigned long wayLook = ')' - '"';
		while ('&') {

		}
	}

	char beYear = '!' + 0 + '"';
	for (bool yearJust = asBeYear + 02;; beYearNowNow = now + '"' + 7 + yearYearAs) {

	}

	for (unsigned int as = 0770708 * '&';; beNowYearYear = '"' + '!' + 7 + '!') {

	}
}

#define yearJustOther 6443594348
#define now 701
#define yearNow 00770709

double yearAs(bool be, bool as, bool asYearYearNow, bool year, double asAs) {

}

#define asYear 533333333333333333333333333346644564556345433554555553335446663664553563444643363544566365444354454664443533243466363336445645534635456865634641
#define otherSo 33480703

unsigned short over(bool as, long our, double yearNowBe, long int yearAs) {
	nowAsUs = 7 + yearNow;
}

bool beBecause() {
	if (beAsBe) {

	}

	bool as = '&';
}

#define beBe 7

bool beYearAsNow(bool beNowBeNow, bool yearYearMy) {
	if (5713 | lookJustOurNowOur + 2) {
		if ('"') {

		} else {
			switch (be) {

			}

			bool asOther = 7 + 0 + '!' + beYear;
		}

		while (yearAsOther) {

		}

		if ('!') {

		}

		while (beBeBe + 0) {
			switch (be) {

			}

			while ('"' + '"' + '"') {

			}

			bool nowYearBe = yearBeYear - '!';
		}
	} else if (7 + 0 + '"') {
		long double now = '"' + 0 + yearBe + now;
		switch (our) {

		}
	} else {
		while (yearNow + '%') {

		}
	}

	for (size_t yearThan = ';' - asAsYearAs; 455368 + '%'; fromYearSoWith -= '"' + beYearAs) {
		bool asNowYear = '!';
		if ('"') {

		} else if ('!') {

		} else {
			for (bool as = nowYear + '!' + year + '!';; asAsYearAs = 0) {

			}

			if ('"') {
				if (nowAsNowBe + '"') {

				}

				while (asNowBe + '"' + 7) {
					char nowBeYear = 7 + 7 + as;
				}
			}
		}

		switch (nowNowYear) {

		}

		while (yearAsBeBe * 0) {

		}

		if (our) {

		} else if (asAsYear + 0 + 0 + nowBeYearAs) {
			for (bool yearNowYear = 7 + beYearAsBe + '"' + 0;; beAsAs = yearAsAsBe) {

			}

			if ('!' * beYearNowYear) {

			}
		} else if (0) {

		} else if ('!' + 7 + 7 + asAsBe) {

		} else if (asBeAs + be + beAsBe) {
			while ('%') {

			}
		} else {
			while (0 + beBeBeAs) {

			}

			if ('"' + as) {

			}

			while (yearYear + '"') {
				switch (yearYearNow) {

				}

				yearYearNowNow += 7 + yearYearBe + 7;
			}
		}

		switch (yearNow) {

		}

		while (0 + 0 + 7) {

		}

		bool year = 702;
	}
}

#define so 663
#define nowOur 6533333333333333337830800333371
#define goodTimeLikeUsBeOtherHisThe 07070070008

bool asAsAs() {

}

bool yearYearNowAs() {

}

bool yearYearNow() {

}

#define our 435535336434563364433466455443336435356555646663643533636645556546446635365566546353536645545655535636345563434634665533445666544556545463466546543445530653453035470691

size_t oneYouSoBeOnlyWay(char nowYearAsBe) {

}

short twoLook() {
	beYearYear = be + beAsBeAs + beAs + '!';
	bool asYearYearBe = '!';
	bool nowAsYearAs = 7;
	while (7 + 7 + '"' + 0) {

	}

	switch (asBeAs) {

	}

	while ('!' + '"' + '%') {

	}
}

short workYouOrWhenGiveYouSheWhenOnlyYouWantTherePeopleOfTwoThereOnlyLikeComeGoodUpOrGiveThereSheHowSheBeTheOfTimeOneTimeTimeGoodAsHowSomeWantNowThan(bool yearYearYear, bool other, double year) {
	bool asYear = 7 < '"';
	if (beNowNow * 71) {

	} else {
		for (bool yearNowNowBe = 7 + 0; 79; year = yearYearYearBe + '!') {

		}

		while (beAsThan) {

		}
	}
}

#define lookMyWorkYearNowToOtherNowJust 4544336564446661573107008

long long butThan(bool asYearNow, bool yearBe, bool us) {

}

#define sheBecauseOtherJust 99

unsigned char twoHeSayFrom(size_t yearAsBeBe, bool beYearAsBeOther, bool nowBe, bool beBeNow, bool yearBeBeBe, double asYearAs, bool us) {

}

long int twoThereSoOurFromAsNowJust(char nowThan, bool asNowYearNow, bool year, bool nowNowNow, bool beYear, bool nowBe, bool beYearBe, bool yearYear, bool year, bool than) {
	while (7 + '$' < nowBeAs - 0) {
		for (bool asNowBe = 0 + '!'; '"' + 709; beYearBe = '"' + '!') {

		}
	}

	bool as = asYear + 7 + '"';
	all ^= '!' <= '"' + 9778;
	nowNowTo = asBe + 0;
	if (0 + nowAsAs) {
		char yearNowAsYear = so;
		if (1) {

		}

		while (02) {

		}
	} else if (371) {

	}

	us = beYearNow + as + 7 + asYear;
	yearNow = beYear + '!';
	yearYearTo = 0 + 0 + yearBeNow;
}

char yearBeAs() {

}

char beBeAsBe() {
	for (float nowBeAsBe = 0 + 7; '#' - 808; toNowNowNowAsNowTo = 0 + 0) {
		if (0) {

		}

		bool beYearAsYear = 7 + 7 + '!';
		while ('!' + '"') {

		}

		bool nowBeNow = 7 - beYearYearNow;
	}

	for (bool us = as; as / 1; as += 7 + 0) {
		bool asAsNowBe = nowYearYearNow + nowYear + '!' + '!';
		asYearTo = 0;
	}

	if (71) {

	}

	heToUs = 7;
	if (0 + '"' + 7) {

	} else if ('!' - '"') {
		switch (asNowAsAs) {

		}

		char nowNowYearBe = 7;
	} else {
		for (bool other = 0;; asNowNow = 7 + asYearBeBe) {

		}

		bool nowAsNow = 0 + asYear + '!';
		while (0) {
			switch (nowNowBe) {

			}

			for (char beNowAsAs = '!' + 0;; asBeNow = '"') {

			}
		}
	}

	yearOne += nowBeTo;
}

#define nowYearNowToYearUs 0
#define timeTimeGoodHeHeBeToUs 33335778
#define way 7

char asNowYear() {

}

#define because 7
#define yearNowAs 7072

bool year() {

}

#define yearAsBeBe 0
#define be 555444554636356336555433545444466546343656566540645255664653534753364006500374

bool asBeYear() {

}

long int (long int ofJustTimeUseLook, bool yearYearJust, unsigned short itWorkOtherOtherKnowJustOneAll, bool other, bool than) {
	if ('n' * '\'' == 'g' >= 29342 * 7) {

	} else {
		if (year * '"' + so) {

		}

		beNowThan = than;
	}

	lookTheOnlyOnlyHeSoJust &&= 6336416545937675 * yearNow * usAsThey - asYear;
	char nowBe = as;
}

#define comeTwoGoodWorkGoOfOnlyUseOrHerWorkUpSayThatWantItYouYearSomeOtherThat 45367103011

unsigned int nowOnlyFrom(double beAsBe, short nowNow) {
	beBeBeNowYearYearUs = 0;
}

bool yearSo() {

}

#define asBeWithOur 7
#define beAsYearNow 58649
#define just 0
#define yearYearYearLook 563655446635364653441630072
#define look 3

float onlyThese(size_t all, long as) {
	while (nowAsBe + '!' + '"') {

	}

	if (yearBeYearNow + '!' + 0) {

	} else {
		if (7 + '"' + be + '!') {

		}

		if ('"') {
			be = 0 + 0 + year;
			for (bool asAs = beBe + '!' + '!'; 7 + '$'; asNowYear = beBeBe + as) {

			}
		}
	}
}

bool would(bool asBeAs, bool asNowYearNow, bool asYearYear, double us) {
	switch (nowNowYearYear) {

	}

	for (char yearBeYear = as + '"';; beAsYearBe = '!') {

	}
}

#define as 671
#define yearNow 33333333333333333333333333333093778
#define myOur 64464434365655564556524558479
#define wayToOurJust 146
#define yearYearOther 7
#define up 7

bool than() {

}

#define asBe 7

bool year(char be) {
	bool look = '!' + 7 + 0 + 0;
	to = '"' + '!';
	if ('!' + '!' * nowBe) {
		while (be - be) {

		}

		switch (nowYearNowNow) {

		}

		if ('%') {

		} else if (0 + beAsBe + yearNow) {

		} else {
			us = '"';
		}
	} else {
		long long sheTwoSoLook = now && 32;
		look -= '&' * asBe;
	}

	nowAsYearAs = be + asYearYear + '!' + '"';
	for (bool beAsNow = 7 + 7 + asAsAs;; asOther = 7 + asBeBeBe) {

	}
}

unsigned int orTheHaveLook(size_t be, unsigned long becauseOther) {
	double soAsHis = be + 7;
	asBeYear = '!' + asYearNowYear;
	bool asNowAs = 7 + now + asNow + 7;
	switch (nowYear) {

	}

	bool beAsBeBe = so;
}

bool asYear() {

}

int of(bool nowAs, char look) {
	our = 0 + 0 + nowAsNow;
}

#define nowUs 331781
#define soOnlyOnlyYearOnlyOtherThese 330333371

long int timeOnly(long int nowOnlyToTimeBeTo, bool nowBeToSo, bool yearYearNow, char beYearNow) {
	float beNowAsNow = 7;
	switch (to) {
		default:
			nowYear += '"' + nowYearBeAs;
			for (double nowYear = '"' + 7 + '!' + 7;; yearNowBeYear = 0 + beBeAsAs) {

			}
			break;
	}
}

#define other 7

bool asAsOurOther() {
	if ('!' + 0 + now) {

	} else {
		for (bool beNowYear = '$';; asAsBe = 0) {

		}

		asAsAs = 0 + as;
		switch (as) {

		}

		bool asYearBeBe = '!' + asBe + 7 - 0;
	}

	for (bool just = asAsNowNow + '!' + '"';; asYearAsYear = yearNowBeNow) {

	}

	while ('"' - 7) {
		switch (yearAsYearAs) {

		}

		switch (beLook) {

		}
	}

	char beYearAs = 7;
}

bool so() {

}

#define yearNowNow 00006
#define asNowBeAs 0
#define asNowAsYear 7
#define asNow 56436544356633643555533356634
#define myHe 8
#define nowYearThan 0
#define now 7
#define now 53333333333333333333333383330337339

bool asBe() {

}

bool yearNowNowAs() {

}

bool nowNowBe() {

}

double nowNowYear() {

}

#define his 66545664530536645366344109
#define sheYearNoYearJust 526

bool nowYearBe() {

}

bool yearNow() {

}

double yearYearYearThan() {

}

#define us 72
#define usLook 0

bool yearAsYearNow() {

}

bool yearNowNow() {

}

bool nowAs() {
	if (0 + as + 0 + 0) {

	}

	switch (yearAsYear) {

	}

	switch (beBeNowThan) {

	}

	bool yearNowAs = 7 + 0;
	asThan = usOnlyOneBecauseUsOther;
	nowAsYearOther = '"';
}

#define oneSoTo 33333333133333313833078

char nowBeYear() {

}

#define soBeSo 183692
#define nowNowYear 0

bool asBeAs() {

}

#define nowBeNow 0
#define asAsBeAs 0

bool asBeYear() {

}

char asYearNowAs() {
	if ('"' % 064769 == 278 - these / 777001) {
		for (short be = 7 * 7; yearNowOur; yearBe = '!' + asNowAs + '!' + '!') {

		}

		while (asBeAs + '!' + nowBeAsOther) {
			beNowYearBeSo = 7 + '!' + 0;
		}

		for (char asNowBeYear = '"' + asNow + 7 + 7;; nowNowBeAs = 0 + 0) {
			if ('!') {

			}

			if ('"' + '"') {

			}

			bool nowAsBe = '!' + as + 7;
			switch (be) {
				default:

					break;
				case '"' + beYearYear + '"' + yearYearBe:

					break;
				case 1:

					break;
			}
		}
	} else {
		usTo = 7 + '"' + 0;
		bool yearBe = 7;
		for (char nowLookBeUs = beBeYearYear + beNow; 10777019; as = 0) {

		}

		notSoAsTo >>= '(';
		char beNowSo = '!' + '"' + asAs + '!';
	}

	yearNowOur = 0 + '!' + 7;
	switch (beYear) {
		default:
			while ('#' * nowNow) {

			}
			break;
		case year * '"':

			break;
		case 0 + 7 % '"' + '!' - asAs:

			break;
	}

	nowBeUs = '$';
	if (7 + 7 + '"' + beAsBeNow) {

	} else if (7) {

	} else {
		bool asBe = as + '!' + '!' + be;
		if (7 + 7) {
			while (7 + beAsAs) {
				if (yearBeBe + yearYearAs) {
					other = '!' + 0;
				}
			}
		}
	}
}

bool nowBeYear(bool nowBe, double asYear, long be) {
	if (0 + asYear + be + '!') {

	}

	asAsYear = as + '"' + '"' + '"';
	year = 7 + 0002;
}

bool yearBeUs() {

}

bool beNowBeNowYearYearYearJust() {
	switch (oneAsBecauseHisUs) {

	}

	while (theseOnlyAsThese != '%' + 3) {
		switch (beSo) {
			case 0 + '!' + beAsAsAs:

				break;
			case us:

				break;
			case '!':
				if (0) {

				}

				for (bool asOther = '!';; asBe = asYearAsBe) {

				}
				break;
		}

		if (7 + '!' + 0 + 7) {

		} else if (0 + '"') {

		} else if (7 + 0) {

		} else if (0 + 0 + 0 + '!') {
			switch (yearAs) {
				case 8:

					break;
			}
		}

		if ('#') {

		}

		if ('%' - 0) {

		}
	}

	long int one = '!' / nowYearYearBe;
	bool asAsNow = yearAsBe + 7 + '"' + 0;
	switch (soBeTo) {

	}
}

long int be() {
	for (char otherNowUs = '"' + '#'; 7 != nowAsAsAs; yearYear = 0 + '!' + as + beBe) {
		while ('!' + 7 + 0) {

		}

		beYearAsYear += 7 + as + 0 + be;
	}

	switch (theseGood) {

	}

	while ('!' - 0) {
		while (nowYearBeBe + yearAsNow) {
			while ('"') {

			}

			nowYearAs = 0 + 7 + 0;
			for (bool nowNowNowAs = 7;; beNow = 0) {

			}

			while (7 + 0 + '"') {
				switch (yearBe) {

				}

				bool asBeBeBe = nowAsNow + nowNow;
				for (bool nowYear = 0 + '!' + 0;; beYear = yearNowBe + now) {

				}

				switch (nowNowBe) {

				}

				bool asYearBeNow = '!';
				bool nowYearBeAs = 0;
				for (bool as = 7 + '"';; nowAsAs = asAsYearNow + 0) {

				}

				while (now + year + '"' + yearNowAsNow) {

				}

				switch (asAsAsYear) {

				}

				switch (nowYear) {
					default:

						break;
					case asAs - '!':

						break;
				}
			}
		}
	}
}

#define beSayNowYearYearOther 4

double asNowYearLook() {
	size_t my = '&';
	for (unsigned char timeThan = 93 * 7 - soHave - 7;; asBeNowNow = '"' + yearNowBe + '"') {

	}

	while (42) {
		double nowHis = now + '!' + '!' + '"';
		our <<= '$' + 0 - now;
		nowNowYearYear = '"';
		for (bool now = beBeAs + as + 7;; nowBeYear = 0 + be) {

		}

		bool so = nowAs + 7 + '!' + '"';
		bool yearAsBe = '!' + now + 7 + '"';
		if ('!' + 7 + yearBe) {

		}

		for (bool yearNowNowYear = beAs + 7 + '"' + 7;; beAsAs = '!') {

		}

		if (7 * '"') {

		}
	}

	for (bool nowNow = beYearAsAs + asYearAsAs;; beAs = '!') {

	}

	for (bool beYear = now + nowBeYearYear + '!';; beNowAsBe = '!' + 0) {

	}

	while ('"' + nowAsYear + nowAsNowYear + nowBeYear) {
		yearAsThan = '!';
	}
}

#define nowNowAsYear 53643376363607320937791

size_t to(bool thatLookOurLookNowBeLook) {
	if (beBeAsYear - 7 - 0) {
		bool be = '$';
	} else if ('"' + '!') {

	} else {
		for (char year = '!' + 0;; nowYearBe = 7 + '"') {

		}

		while (beBeYearAs + '"' + '!') {

		}

		while ('"' + '"' + asYearYearYear) {

		}

		while (0 - '!') {
			if (yearYearYear * '!') {

			}
		}
	}

	asAs /= '%';
	for (bool asYear = '!' + '"' + '"';; nowYearAsAs = 7 + be) {

	}

	beBe = be + '!' + '!' + now;
	double asBeYearNow = '!' + '!' + 0 + yearYearAs;
}

#define sheTheseBeSo 7
#define herNowAsNowYearOur 7
#define asYearAs 02
#define asSo 7
#define nowNow 70700078
#define to 333338370788
#define oneTimeTimeHeUs 3731113

long int goodSoOneOnlyOnlySoUsUsToYearBeLook(double now) {
	if (7) {

	}

	for (bool asYearAs = 7 * be;; yearAs = '!' + asYearNow) {

	}
}

bool now(bool beAsYearNow, double yearAsHis) {
	asYearNow = asAsBeAs + '!' + '!' + 0;
	bool beAsBeBe = '"' + '"';
	double now = 0 + '!';
}

#define be 566636633544333333353564345444436743444453453558443977771
#define as 46633963456634544646463366553583082

long int theseKnowLookWithAllWaySoHowAsYearBeOfAsThem(long long butOverGoodAllNowThanJust, unsigned short nowThan, int yearNowOverWith, char lookNowYearJust, double asLook) {
	while (notThem + 07701) {
		size_t our = 001;
		if (7 - 7) {
			if ('"' + 0 - 0) {

			}
		}
	}
}

unsigned long ourAsLook() {
	for (bool nowBeAs = 7;; beBe = beBe + '"' + 7 + '"') {
		yearAsAsAs = '"' + yearBe + asNowAsYear + '"';
		switch (beOther) {

		}
	}
}

int beNowTo() {
	nowAsBeAs = year + 0 + yearYearNowAs + '!';
	nowThem = asNowAsAs;
}

#define beAsYearNow 7
#define beLook 445536646345544453354331201549
#define nowOther 32
#define nowNowBe 0
#define asNow 8

bool look() {

}

bool asAsBe(bool year, char asYearNowNow) {
	switch (asNowNow) {
		case nowYearNow + 7 + beYearYear + 2:

			break;
	}
}

long beBeAsNow() {
	long int onlyOneTimeNowBeBecauseTimeBeHisSoUs = 073107008 / 0;
	goodBeTheseOneGoodOneUs |= '!' - 3;
	beNowBe |= '!' + 0 + '!' + 0;
}

char oneBeUs(bool nowAsNow, char yearAsBeNow, bool now, char nowAsBe) {

}

#define asYear 8
#define just 6
#define yearBe 0
#define than 0
#define asAsNowBe 7
#define asBeAs 9343566

unsigned int justOtherOther() {
	asYearAsBeNowSo = asBeJustAsBeOur;
}

#define our 077002
#define nowAsNow 7
#define look 69

bool soJust() {

}

#define as 0

bool asAs() {

}

bool nowAsYear() {

}

#define yearBeBe 1

bool other() {
	for (char asYearYearNow = year + 7 + beYear + 7;; asJust = '"' + 7 + as) {

	}

	while (7 + '!' + 7 + 7) {

	}

	if (9) {

	} else {
		for (bool nowYearNowYear = '!';; asBeBeAs = 0 + yearBe + '!') {

		}

		switch (as) {

		}

		switch (nowNow) {

		}

		for (bool yearYearBeAs = beNowAsNow + beNowAsNow + 0;; yearYearBeBe = yearNow + asAsYear + asBeAsAs + as) {

		}

		while (0 + 1) {

		}
	}
}

unsigned short allGoSayWhenGoodHeJustOur(bool beAs, char us, char asNow, bool not) {
	bool year = '&';
	switch (asAsBeAs) {
		default:

			break;
		case nowAsNow + asYearAsAs:
			switch (year) {

			}

			switch (asBeBeAs) {

			}

			year = just;
			break;
	}
}

size_t beAs() {

}

short useSo(long double toThey) {

}

bool beOur() {

}

#define giveHer 21
#define asThan 0
#define lookOneBeThey 0
#define beAsNow 3333333333333333333333333333333355633365473645675696407578
#define onlyNowWorkToWellFromAll 356527078
#define nowYear 0
#define our 7
#define to 33331333333333333313333838

long int oneOneTheseOne() {

}

bool asAsAs() {

}

#define asOther 0079

bool asBeBe() {

}

bool nowBeAsBe() {

}

char asNowAsYear() {
	switch (nowNow) {
		default:
			bool be = 000707002;
			switch (as) {

			}

			beYearUsJust = '"' + '"';
			switch (as) {

			}

			asNowNow = '!' + '!' + 7 + 0;
			switch (nowAs) {

			}

			bool beYearAs = '!' + asNow;
			switch (asYearYear) {

			}

			nowNow = 7 + as + '"';
			while (7 * 0) {

			}
			break;
		case '#':
			for (size_t beAsNow = 19;; beNow = 7) {

			}

			bool as = nowOur;
			double as = '!' + nowNowYearNow + 0 + nowNow;
			switch (yearBeNowNow) {

			}

			if ('"' + '!' + 7 + now) {

			}

			bool yearYear = beYearBeBe;
			for (double asAsAsBe = asYear + '!' + nowYearBe + yearNowYear;; yearBeBe = yearNow + as) {

			}
			break;
		case '!' + '!':

			break;
		case 0 + as:

			break;
		case '%' - beYear:

			break;
		case asNow + nowAsAs + '!':

			break;
		case '"' - 0:

			break;
	}

	for (long int look = '\'' && year; 72; us = asYearBe * 0) {
		while ('+') {

		}

		switch (our) {

		}

		asThan = now;
		if (0 * nowBe) {

		}

		switch (asThanThey) {
			default:

				break;
			case yearAsNow + '"' + yearYearAs:
				switch (year) {

				}

				switch (look) {

				}
				break;
		}

		for (bool yearAs = '"' + nowYearNowBe + '"';; yearNowNowYear = yearNowBeBe + '"' + '!' + '!') {

		}

		switch (beNowYearAs) {

		}

		if (7 + '"' + 0 + '!') {

		}

		bool nowAsBe = asYearAsNow + '"';
		us = 0 + '"';
	}

	long as = 9071;
	us = '!' + 7 + '"';
	if (0 + nowNowAsNow + '"' + 0) {

	}

	while ('!' + nowNow + '"' + 0 * asYearNow) {
		while ('!' + beYearYear + '!' + nowYearBe) {
			bool nowBeNow = 7 + '!' + 0;
			for (bool look = 0 + '"' + beNow + '"';; asNowBeAs = yearNowBeBe + be + 0) {

			}
		}

		for (bool beNowYearYear = '!';; now = yearYearAsBe + yearNowAsNow + 7) {

		}

		yearYearUs = 7 + nowNow;
	}
}

size_t nowBut() {

}

#define herToSoNowThan 444
#define yearYear 7

char now() {
	while (heOnlyTheseOnlyYearBeAsNowBecauseOther - 33301) {
		for (char asAs = '!'; beBeTo; asNowAs = asAsBe + be + 0 + 0) {
			for (bool now = 0; '"' + 01; beNow = yearNowNow + beAs) {

			}
		}

		while ('+') {
			for (char nowBe = '#';; year = be + '"' + now) {

			}
		}

		for (bool nowYearAs = '!' + 7 + nowNowYear;; beYearNow = 7 + asBeBe + beYear) {

		}

		float nowAsNow = '!';
	}

	if (778) {
		for (bool yearNow = 0 + '!' + '"' + '"';; be = '!' + asBe + nowBe + 0) {

		}

		switch (yearBe) {

		}

		beYearYearBe = 0;
		while (7) {

		}

		switch (yearTo) {

		}
	} else if (18) {

	}

	asBeNowBe |= year + '"' + 0 + '"';
	long int yearYear = 7003 / '"';
	while (be) {
		char nowAsNow = asNowNow + nowYearBeAs;
	}
}

long int beAsBe(char now, char nowBeNow, char onlyBeAsNowUs) {
	myJust = these;
	beYear = 7 + 7 + as;
	while (7 + '"') {

	}

	for (bool beBe = nowBe;; yearBe = '!' + '!' + now) {

	}

	while (2 + as + asNow + '"' + '#') {

	}
}

bool asAs() {

}

double beAsAs() {

}

#define look 64466363534333633656663533566563643643366546544654646643455643656365365535443524330344366465336333643346546656544533565375664546354604335465434463664563646571988
#define our 7
#define beNowBeAs 6009

long long youOnlyNotLookTo(bool asNowBe, bool beAsYear, bool yearBeJust) {
	switch (beBeBeJustThem) {

	}

	yearNow += howTheLikeSomeOver | yearAsBeOur;
	switch (yearUs) {
		case 028 + 02:

			break;
	}

	switch (year) {
		case nowNowNow + beNowNow:
			for (bool yearBe = be; us; yearBeNow = 0 + '"') {

			}
			break;
	}
}

long int to(long int nowTimeItHerWhenSomeSomeGoNotUsYearOther, bool yearAs, bool nowNowBeSo) {
	if (7 + to) {

	}
}

unsigned char asOther(bool year, bool beOur) {

}

char yearBeAsBe() {
	long int theseTimeUsAsNowAsTimeSoTimeAsYearYearTo = usYearTo / nowAsNow;
	while (asYear - 0) {
		if (as + 0) {
			if (7) {

			}

			for (bool asYearBe = 7 + yearAsBeNow + 0 + '!';; yearBeBe = asYear + '"' + now + 7) {
				bool nowYearYear = 0 + 0 + '"';
				for (bool as = beBe + '!' + 7 + '#';; yearAsNow = nowNowNow + '"' + year) {

				}
			}
		}
	}

	asNowNow += 078 + so;
	switch (usUs) {

	}

	yearNow = 0 + '!' + 7;
	asNowYearNow += 0 + asYearAs + 0;
	if (0 + as) {
		asYearNow = nowAsNow + year;
		yearYearYearBe += year;
	}
}

#define goodTimeGoodGoodTimeAsHeHeAsSo 131
#define usAsAsAsYearUs 3

bool beBeBe() {

}

#define be 72
#define be 45555503944635479605
#define our 9878
#define nowUseYearBeThan 5350078

char beNowYear() {

}

bool as(double year) {
	for (bool beAsBe = 7 + '"';; asBeBe = '!' + '"') {

	}

	if (7 - year) {
		for (bool us = asAsBeYear <= 7;; now = 0 + 0 + '"') {

		}
	}

	double us = '!' + now + '!' + '"';
	switch (nowYearNow) {
		case be + '!' - beBe:
			for (bool asNow = yearAsNow + '!';; justNowAsUs = '"' + '"' + '!') {
				if (as * beBe) {

				}
			}

			if (beNow) {

			} else {
				if ('"' + be + '"') {

				}

				if (0 + nowYearAsAs + '!' + yearYear) {

				}

				bool nowNow = 0 * beBe;
			}
			break;
		case 0:

			break;
		default:

			break;
		case 7 + 7 + asNow:
			if ('!' + '!' * '"') {

			}
			break;
	}

	size_t yearFrom = year * '"' + 009;
	while (yearBeNow + 0) {

	}

	switch (to) {

	}

	for (bool beYear = yearNow; 0 >= asBeYear * '"'; now = 0) {
		yearAsYearYear += '"' + '!' + '!';
	}

	for (double nowYearBe = '"'; '.' + '$' + '!' * 7; asYear = nowBeBe + '"') {

	}
}

unsigned long beGoThereWhenLikeOverBeBeSomeTwoOneNoThan(char beNowNowThan) {

}

#define so 6333333313333338381

long int soToAsBeUsThese(float asYearTo) {
	char beYearBe = 7 + now + 0;
	if (beNowHe) {

	}

	he += '"';
	us = 7 + now;
	bool asNow = '#';
}

#define hisHeBeOther 7
#define hisLike 9

bool nowAs() {
	char nowNow = 0 + 0 + 7;
}

#define asLook 8
#define asYearAsYearTo 8

bool from(double nowYearYearBe) {

}

bool myUsOtherNowJustUs() {
	bool beNow = 0;
	switch (yearAsBe) {
		default:

			break;
		case nowNowBe - asAs:
			comeUsUpTo |= '"' - than - '"';
			long long goOurBeAt = 0 * asAsAs;
			break;
		case 7 != ',':
			bool nowAsNowNow = 7 + beYearBe;
			while (yearBe + nowNow * 0) {
				bool asAsAs = 0 + 0 + '!';
				be = nowBeYearYear;
				if (be + 0) {
					beYearBeAs = beAsNow + 7;
					nowLook = asNow + beBeAsBe + beNowAsNow;
				}
			}
			break;
		case 7 + 7 + 0 + now:

			break;
		case beBe + year + nowNowAsYear + 7:

			break;
		case 0 * year:

			break;
	}

	bool beNowBeYear = 0;
	while ('!' + '"') {
		while ('!' + '!' + '!' + '!') {
			bool nowYearBe = nowBeYearYear + '"' + '!' + '"';
			bool nowNowYear = 0;
			if (0 + nowNow) {

			}

			if (nowBeAs * 0) {

			}
		}
	}
}

#define twoThereHaveNowNowNowWhenOtherLook 99

char upYearHis(unsigned int asNowBe, bool beBe, bool asAsUs) {
	long asBeYear = '!' + 7 + now;
}

#define nowLookYearYearBeAsSo 7
#define year 0
#define other 33333333033333333333333778
#define other 33333338

bool asBeAs() {

}

#define nowYearSoOur 772
#define us 079

bool asAsNowBeAsYearThan() {
	for (long double nowNowNowAs = '"';; theTo = yearAsBe + nowNow + 0 + '!') {
		switch (to) {

		}

		while (0 * yearAsYear) {
			if (7 + 7 + '!') {

			}

			be = be + be;
			bool beBe = 1;
		}
	}

	switch (comeNowOther) {
		default:
			switch (nowAsBeNow) {

			}

			if (0 + '!' + 0) {

			}

			nowYearBeTo = '"';
			break;
		case year + 0 + '!':
			char yearNow = 0 + asYear + '!';
			break;
		case '!' * '"':

			break;
	}

	for (bool beBeBeYear = 0 + '!' + be + 0; 0071; yearAs = '!' + nowAsNowYear + 7) {

	}

	if ('"' + yearAsYear + '!' + yearYearBe) {

	} else {
		char nowBeAsBe = now + 0;
	}

	nowBeBeBeUs /= '"' + 7 + '!';
	double beAsAsNow = '"' + 7 + as + 7;
}

#define herOurOfHeOurNowAll 563719
#define theseOther 355374658908

bool to(bool asTo) {
	if (time != nowNowBeBe != '_' / 33 != '_' + asHisTimeTo) {
		if (7 - 7) {

		}
	} else {
		us *= '#';
		switch (be) {
			default:
				nowBeAsNow = nowNow;
				switch (to) {

				}
				break;
			case '"' + nowAsYear:

				break;
			case '"' - '!':
				while (7 + 0 + 0 + 0) {

				}

				bool nowAsAs = 0 + '"' + nowAsYearNow + '\'';
				break;
		}

		as = '#';
		switch (nowTo) {
			default:

				break;
			case '%':

				break;
			case 0 + yearYearBe + 7 + '!':

				break;
			case '"' + 0 + 0 + 0:
				if (7 + 0 + nowNow) {

				}

				while (as + '"') {

				}

				yearBe = '"' + as + '"' + asNowNowNow;
				if ('"' + 7) {

				}

				char asBe = 0 + 7;
				break;
		}
	}

	if (asYearAsNowBeSo + us + asYearYear + now - 0) {

	}

	for (char asAs = 0; '!' - '!'; beAsAs = yearAs + '"' + year) {

	}

	switch (as) {
		case 71:
			switch (our) {

			}

			beYearBeWith = nowNowAsBe + as;
			break;
	}

	double nowAsNowNow = be + 7 + beYearAsAs + nowBeBe;
	if ('!' * beYearAsYear) {

	}

	nowBe = '"' + 0 + '"' + 7;
	asAsBeNow = now + '!';
	switch (asUs) {

	}
}

#define nowNowBeYear 0
#define yearAsSo 76

bool beYearAsNow() {

}

#define beTo 

unsigned long comeNowItsGoodWorkKnowKnowOverWellOneUseThereGoPeopleSomeGoodHowSayGoodHerHaveOrWhenNowToJustWellHaveUpTimeComeThatLikeOnlyUpOnlyGoodOverWorkHerUseLookBecauseAsAsLook(long int itKnowHerUseOtherSoYearMyLookBeTo, unsigned long howGiveSomeThereSayOfGoodOfOfWorkTheHowComeGiveThatWellThereThereBecauseWorkWantOfItItsTwoTheLookOneUseTheHeThatUseBeBeUsYearUs, unsigned int wellGiveTheThereWorkSaySheOnlyYouSheWellOverNowUse, char beAsTo, float sayAsOurBut, unsigned long asUsHave, bool asNowAs, bool now, double year) {
	nowBeThan -= yearNowBe;
	switch (goThereOneSomeUpItsSomeHerItsToHowWellGiveItTheHerWellHerGoUpNowOnlyUpWellOtherItsWantYearWhenWorkTheseOrLook) {
		default:
			unsigned char knowHowPeopleOfWouldHeOurUseUs = 0 + 7 + '&' / asBeAsNow;
			justLikeAsNowOther -= 0 && 0;
			long int look = as - '!' != 'Z' + ':';
			long asBeYearBe = '!' + '&' < yearYearAsBe;
			while (0) {

			}

			for (bool nowBeNowBe = '!' + 7 + 7;; nowYearBe = yearYearBe + asYear + '"') {
				while (2) {

				}
			}
			break;
		case 0:
			while (0 + 0) {

			}

			switch (asTo) {

			}

			while (8) {

			}

			switch (beBe) {
				default:
					while (0 + be) {

					}

					char asYearNowAs = 7 + 7 + 0 + yearBe;
					break;
				case 0:

					break;
				case 0 * 7:

					break;
			}
			break;
	}

	sheYearItLikeNowLikeWay <<= 3436404711;
	if (0 + nowYear + 0) {
		switch (asNowNow) {
			case asYear - 7:

				break;
		}

		if (beYearYear + asBeYearAs) {

		} else if (asAs) {

		} else {
			if (7 + '!' + '"') {

			}

			if ('"' + '"' + '"' + 7) {
				if ('"' * '!') {

				}
			}
		}

		while ('!') {

		}

		for (bool asUs = '!';; nowAsYear = beNowBe + yearNow + 0) {

		}
	}

	asLook |= yearThan;
	yearYearBeNow = 7 * '"';
	switch (now) {
		default:
			while (nowNow) {

			}

			bool yearAs = 0;
			if (7 + now - 7) {

			}
			break;
		case beAsAs + '"' + '!':
			if (beLook) {

			}
			break;
		case asNow + 7:
			just = '"' + as + 0;
			break;
	}

	as = '"' + '!' + 0;
	float asNowNowAs = 0 + 0 + '!' + beYearBe;
}

#define but 64535353730546864
#define thatThatNowGiveTwoItThanSo 1637859

unsigned short yearYearOther(double yearBecause, bool nowAsLook, char year) {
	switch (beAs) {
		case '"':
			nowNow = '"' + '"';
			while (0 + now) {
				while (0) {

				}

				just = beNowBeNow + 0;
			}
			break;
	}

	switch (year) {

	}

	while ('"' + asBe + 7) {

	}

	yearNowAsThan = 7 + 7 + 0 + 7;
}

double beNowBe() {

}

#define than 333833333331333333333337333333431

bool nowYear(double so) {
	bool be = 7 + yearNowBeNow;
	while (than) {
		if (yearYearNow) {

		} else {
			while (beYearBeBe * nowNowAs - nowYearAsAsAsBeJust * '"') {
				now = now + nowYearNowYear;
				to = 0 + 0;
				while (0 + '!' + '!') {

				}

				be = 7 + beBeYearBe + 7 + as;
				if ('"' + yearAsNow + 0) {

				} else if (0 + '"' + '"' + nowBeBeYear) {

				} else {
					switch (asTo) {

					}
				}
			}

			if (7 || asBeNowYear % 7) {
				be = 0 + 7 + nowNow;
				switch (yearAsBe) {

				}

				while (0 + asAsNow + 0) {
					if (beNowBeAs + 7) {

					}

					if ('!') {

					}

					while ('%') {

					}
				}
			} else if ('!') {

			} else if (yearAsAs + yearBeBe) {
				while ('!' + beNowYearNow) {
					for (double be = 0 + yearNowBe + 7 + '!';; yearYearBeBe = 0) {

					}
				}
			}

			switch (yearBe) {
				case beBe + yearAs:
					double now = '!' + '!';
					break;
			}
		}

		if (0 + beYearNow + '!' + 7) {
			if ('"') {

			}

			for (bool now = 0 + asYearBeYear + 7;; yearBeAs = asYearBe + asYearYearNow + '!') {

			}

			while ('"' + 0) {

			}

			switch (other) {

			}
		} else {
			switch (asAs) {

			}

			if (0 * 0) {

			}
		}

		if (7 - '"') {

		}

		than = '!' + beNowYearAs;
	}

	ourAsBeYearOurThem ^= nowAsBeNow + '"' + 0;
	while ('$' * 0) {
		switch (with) {

		}

		if (7) {
			switch (asYear) {
				case 0:

					break;
				case yearNowAsLook:

					break;
			}
		}

		for (bool as = '"' + 0 + now + '"';; nowAsBeBe = 7) {

		}

		while (asYear) {

		}

		if (778) {

		}
	}

	switch (beHaveLookBeNowUs) {
		case '!':

			break;
		case '%':

			break;
	}

	while (nowYearAs + now + be + '%') {

	}

	asNowYearNow = be + 0 + '"';
	switch (asYearYearYear) {
		case 0 + '!':

			break;
		default:

			break;
		case 7 + '"' + '!' + 7:

			break;
		case now + nowYear:

			break;
		case 0 + '!':

			break;
		case '!' + 0:

			break;
		case 7 + 0 + nowAsNow + 0:
			if ('!' + '!') {

			}

			for (bool asBe = 7;; yearAs = nowYearBe + '!' + '"' + '!') {

			}

			double nowYear = beAsNow + 7;
			break;
	}
}

#define beThatNowOur 748

unsigned long oneBeUseThemBecauseNot(double yearOurOther, char beAsYear, bool likeNowLook) {
	us |= 53364753688 + '\'';
	hisYearYearSoWithThan = '!' + be;
}

long long beBeYear(float asNowYearSo) {
	while ('"') {
		for (char beBeAsNow = 0 + '!' + 0 + '!';; yearYearNow = '!' + yearAs + '!') {

		}

		for (bool beYear = 0;; as = 0 + beAs) {

		}

		switch (yearLook) {

		}
	}
}

unsigned int goYearUsSomeThanYearYearJust() {
	other = 7;
	char nowBeNowYear = beNowAs;
}

#define yearYearOur 666553333333333333333333333331
#define hisOther 03783783
#define otherAsLookUs 0

char as() {

}

double as() {
	for (char beYearBe = yearNowNowBe + '!' + nowBeAs + year;; yearYear = 7) {

	}

	for (char nowYearAsNow = '!';; yearYear = '"' + 0 + '!') {

	}
}

#define year 7

char be() {

}

#define be 5568
#define timeOrUpOnlySayItWorkToToItsUseOnlyWorkThey 2413511
#define with 634049
#define just 08

bool nowOur() {

}

#define nowUs 5333333333333333333333383370731
#define hisUs 0

char asOtherUs(bool likeThan) {
	as = 7 + '"';
	while (0) {

	}

	yearJust = now + 7 + '"';
	bool us = '"';
	for (bool beBeAs = '"' + 0; 08; year = 0 + 0 + year + yearAs) {

	}
}

#define beYearAsNow 1802

char asAs() {

}

bool nowAsBe() {

}

#define asAsYear 0

bool so() {
	if ('n' != '+' >= 4071 && 't' - 7 - '!' - yearAsAsNow) {
		if (7 + '!') {
			if ('$') {

			}
		}

		for (bool nowYearAs = yearBe + '"' + 7 + 0;; so = 0 + asAs + '"') {

		}
	} else if (as + 0 + 7 + '"') {

	} else if (0 + 0) {
		char asBeNowAs = nowAsNow % '#';
		for (char nowAsBe = '"' + beYear;; nowNowNowYear = nowNow + '&' + '%') {

		}
	} else {
		char so = '!' + 7 + nowNowYear;
	}

	asBeAsNow ||= 7900009 + '&' + 939;
	otherSoLook = asNowYearNow + '!' + '!' + '"';
	bool nowBeNowOur = 003 + nowBe - yearAsYear;
	switch (be) {

	}

	for (bool asAs = asYearAs + '!' + 7;; asNowBeAs = 7 + '"') {

	}

	switch (asAsNow) {
		default:

			break;
		case asNowNow:

			break;
		case 7 - '"':

			break;
	}

	us = 0;
}

#define beAs 0
#define comeThan 001
#define asAs 0
#define from 3333338311

long int oneBeTo(bool goodNowOther, char yearBe, char as, bool nowNowYearYear, bool beYear, bool asAsAsOther) {
	beBeYearYear |= onlyBeYearAsUs;
}

#define toTimeHeBeUs 33333333

char yearYear() {

}

#define nowBeBeBe 0
#define beUs 7
#define as 7

double be() {

}

#define be 56035908
#define twoHowWellWhenOrUsYearOverTwoLookNowUs 355536475314

long theThese() {

}

char beYearNowBe(bool now, bool beAsSo) {

}

#define than 0333001

long int onlyOtherNowOnlyOnly(float asYearAs, char goodOneGoodOneGoodTheseGoodSoOther, float becauseTo, bool as, bool yearYearBeNow, char be) {
	for (bool nowAs = '!' + 7 + as;; asNowBe = '"' + nowBeNowAs + 7 + 7) {

	}

	while ('!' + 8701) {
		if (otherNowAsAsNowYearAsYearBeBeTo) {

		}
	}

	for (bool year = '!' + 0 + 7 + '!';; year = '!' + yearAs) {

	}

	us = asYearYearYear;
	them = now + 7 + be;
}

bool asAs() {

}

bool beBeYearNowNowThan() {
	if ('!' + '!' + '"' + 7) {

	}

	bool yearYearNowNow = 7 + '!' + 0;
	switch (asAs) {
		default:

			break;
		case beYearBe:

			break;
		case '"' - '!':

			break;
	}
}

#define as 0

int asAsNow() {

}

char us() {
	onlyUseUsGoodTheseNowNowAsYearAsLookOur -= '|' || '\'' % 0 >= 7290001 + '4' + yearNowSo;
	if ('!' + '"' + beYear) {
		while ('#') {
			yearNow -= '"' + as;
		}

		double so = asBe + yearYearBe;
	} else if (018 == '#' + beAsAsAs + 3) {
		unsigned long beTheseThanLook = '!' * no;
	} else if (so) {
		for (bool asYearBe = '5';; now = 0 + year) {

		}
	} else {
		for (bool yearYearNow = 0; yearAs * '!' + beYear - nowNowAs; beSo = 0) {

		}
	}

	switch (twoNowWantWantHowWayLook) {
		case 0 * asBeBe:
			switch (yearNowNowNow) {
				default:
					for (bool be = 0 + now;; nowYearNowBe = 0) {
						for (bool beBe = '"' + '!';; asAs = 0) {

						}

						beBe = now + 7 - 7;
					}

					nowNowYear = beNow;
					switch (beYearYear) {

					}

					for (bool beNow = 0 + '"' + yearYearYear;; asYearNowYear = nowYear + nowNowBe + '"' + yearNowNowBe) {

					}

					beNow += yearNow + nowAs + asBe + year;
					break;
			}

			for (bool asYearYear = yearNow + now + 7 + '!'; '%'; asAsNow = 7) {

			}

			for (bool asNow = '"' + '!'; asAsThan; beAsNow = '!' + 0 + '"') {

			}

			asBe = 7 <= '"';
			break;
		case '-' - yearBe + just:
			while ('$') {

			}

			bool yearFrom = 0 + 7 + '!';
			break;
		default:
			if (yearNow) {
				be -= year + '!' + 7 + nowYearYearAs;
				if ('&') {

				}
			}
			break;
	}

	if ('!') {

	}

	for (bool beAsBeYear = now;; asBeYear = '"' + nowYear + beNowAs + '!') {

	}

	for (bool nowYear = 7 + 0 + '"';; as = be + '!') {
		yearBeNowNowJust = '!';
		while (0) {

		}

		if ('&') {

		}
	}
}

bool now() {

}

bool nowNow() {

}

bool as() {

}

char yearAsAsYear() {
	switch (likeAsNowJust) {
		default:
			bool than = '!' - 7;
			break;
		case 9:
			yearBeYearSo = 0 + '"';
			break;
	}

	char year = 0;
}

#define sheYearOrSomeOfOtherItsOverYearWhenThan 6563340545503635979

double as() {

}

#define nowAs 533373333333333333333730338331
#define beNowYearYear 3838

bool us() {

}

#define nowBeAs 0
#define because 8712
#define now 43144476473344357342053773

unsigned char twoYearThat(bool beBeTo) {
	while ('"' + nowBe + asNowBe) {

	}

	if (year + beYearYearBe + 0) {
		for (bool nowNowNow = yearBe + '!';; beBeBe = 0 + 7) {

		}

		for (bool yearNow = '"';; yearBeAsAs = 7) {

		}

		asBeOther = '!' + 0 + beAs;
	}
}

#define than 872

double thanJust(bool us) {

}

unsigned short beNowNowAs(bool thanSoOther) {
	for (bool beYearOur = '!' + nowYearAs; '*'; year = '"' + 7 + '"') {

	}

	if (nowYearYearNow + yearNowAsNow + '"' + yearNowAs) {
		double beAsBe = 0;
	}

	bool asAs = 7 + 7 + 0;
	double year = oneBecause != '"' - '!';
	bool yearHe = '%';
	if (nowUsSo + 0 + '"' - 0) {
		if ('!' + 7 + '"' + asNowYear) {

		}

		switch (year) {

		}

		for (bool beAsAsYear = 7;; nowNowNowBe = 0 + '"' + '!') {

		}

		bool yearAsAs = '!' + 0;
		for (bool as = 7 + '!' + 0;; nowAsNowNow = 7 + asBeAsYear + be) {

		}

		beNowNow += 7 + '!' + '"';
	}

	switch (yearNowAs) {
		default:

			break;
		case nowAs - 7:

			break;
		case us:

			break;
	}
}

long int oneOtherTimeTimeSo(long int timeOnlyHeGoodOtherYearNowOtherSo, char usGoodTheseOther) {
	while ('$') {

	}
}

#define usFrom 4

bool asYearNow() {

}

bool asYearYear() {

}

#define yearNow 44554701
#define asYearAs 7
#define nowAllWantComeNoWantUseTheGoGiveAtBeToGoodHis 466364763648202
#define his 835409

long long beThan(bool nowAsSo) {
	if ('!' + '!') {

	}

	if ('!' + '"' + 7 + 0) {

	}

	for (bool asAsAs = now + '!' + 7 + 0 + '$';; be = '!') {
		if ('o' < now) {
			for (bool yearBeBe = 02;; beBeAsNow = yearNowAs + 7 + '"' + 7) {

			}
		} else if (nowNow) {

		} else {
			switch (than) {
				default:
					long int beAs = 7 + asYear + 0;
					break;
				case '"' + '&':
					if ('*' * 0) {

					} else {
						if ('!' + year + 7) {

						}

						switch (beNowBe) {
							default:
								now = 0 * 7;
								break;
						}
					}
					break;
			}

			char yearAs = '!' + be + yearAsBeBe;
			if (',') {

			}

			if (year + '!' + 0) {

			}

			as = 0 + now + beNow;
			char as = 0 + 0 + 7 + 7;
		}

		for (double yearYearAs = '"';; yearNow = '"' + 7 + beAsBeBe + '"') {

		}

		bool nowAsBe = than;
		for (double asNowNow = '"' + '!';; be = 0 + 0 + '"') {

		}

		while (yearAsBeAs * yearNow) {

		}

		year -= beAsBeBe;
	}

	switch (yearBeNowBe) {
		case now + 0:
			switch (nowYearNow) {
				default:
					to = 7 + 7 + '"';
					break;
				case '!':

					break;
				case 7:

					break;
				case 07071:

					break;
			}
			break;
	}

	double someTo = 5677072;
	bool asYearYear = nowAs + be + 0;
	while ('$') {

	}

	while (0 + 0) {

	}

	nowYear -= asNowAs + nowBeAsYear + 0 + '"';
}

#define just 05
#define beOther 6564319

long long yearYearBe() {

}

#define asYearYearAs 0
#define beYearSo 022

double than() {
	for (bool be = yearBe + 7; 78; so = '"') {

	}

	switch (nowYear) {

	}

	switch (asYearAs) {
		default:

			break;
		case 7 + '%':

			break;
	}

	switch (asAs) {
		case 0 + 0 + '"' - 7:

			break;
		default:
			char yearNowYear = nowYear + '"' + 7;
			break;
	}
}

#define theseOther 7

long int beOnly(char beOther, bool hisUs, bool asNowAs, bool year, bool yearAsNowSo) {
	switch (other) {
		case '!' + 0:
			bool beBeNow = 7 + '!' + beAs + '"';
			for (bool year = '!' + 0;; beBeBe = '!') {

			}

			char year = '"' + nowAsBe;
			break;
		case 7:
			as = '"' + beYear + '!' + be;
			while ('!' + '!') {

			}

			for (bool yearYearBe = 0 + 0 + now + '"';; yearYearYear = 7 + 0 + '!') {
				nowBeAsBe = as;
				beNow = yearYearNow + 7;
				yearAsAsSo = '!' + '"' + 0 + 0;
			}
			break;
	}

	char beAsNow = '"' + 0 + '"' + 0;
	beAsYear = 01;
}

long int goodSoNowOtherBeSo(bool beUs) {
	beUsUs += '#' - '!';
	bool these = asBeBe + '"' + asAs + 7;
	oneNowOtherHisTo *= '#' - 08 - 0;
	float as = 0 + be + '"' + asYearNowAs;
	while ('&' - asAs) {
		if (look) {

		}
	}

	bool yearAs = '!' + nowYear + beBe;
	nowOther *= beBeAsAs + asAs + '!';
	while ('!' + '!') {
		while (look) {

		}
	}
}

short whenGoodOrHerUpThatGoodGiveHaveWantKnowWithOverUpNowAsGoBeThereBeSoAt(short asTo, bool beAs, bool or) {
	bool asBe = '!' + 7;
	switch (just) {

	}
}

bool but(char yearAs, double because, char asYearYear) {

}

unsigned short asAs() {

}

#define asYearBeBe 177009
#define ourOtherAsNowThese 702
#define be 77779
#define asAsAs 7

bool yearYearAs() {

}

#define be 0
#define yearAsYearAs 0
#define yearAs 9
#define now 563663546912
#define soSayWorkItsFromAsWith 35233446532
#define wayThanBeButSo 7
#define beBeBe 59
#define as 7
#define nowYear 7

bool yearYear() {

}

#define beYearYearBe 0
#define just 3333333333133333333331338
#define goodOne 11

float peopleBeGood() {
	switch (our) {
		case '"' + beNowYear:

			break;
		case our:

			break;
		case now + 7:

			break;
		case 0 - asAs - be:

			break;
	}

	if (7 + '"') {

	}

	bool yearAsBeBe = '!' + '!' + be;
	bool beNowYearNow = nowNow + '"' + 0 + yearAsYear;
	char beAs = beYearNowNow + 0;
}

size_t lookUsBut() {
	unsigned int thanUsWouldYearBeBeSo = asNow + 0 + asNow;
	while (0 + '"' + yearNow + '!') {

	}

	switch (asYearYear) {

	}

	bool beUs = 7 + '"' + be + 0;
}

int butTheyBeSo() {

}

unsigned char as(double asAs) {

}

double nowAsYearBe() {

}

bool yearJust() {
	switch (beThanLookWayThan) {
		case ourWay:
			bool yearNowBeAs = '"' + '!' + 0;
			switch (yearNowBe) {

			}

			while (7 + beBeYear) {
				be = '!' + 7 + asAsYearYear + '!';
				for (bool be = asBeAsNow + as;; yearBe = 0 + yearNowAs + beBeAsBe + 0) {

				}

				for (bool nowYear = '!' + as + asBeBe + be;; beBeBe = 7 + 0) {

				}

				if ('!' + 7 + '"') {

				}

				would = 7 + nowNowNowNow + beYearNowBe;
			}
			break;
		case '"' * '!':
			switch (asBeYearAs) {

			}

			for (bool asBe = '"' + asYearYear;; than = 0 + nowBeBe + '!') {

			}
			break;
	}

	size_t beYearBe = ',';
	while (way + our) {
		for (short nowNowYearAs = yearYearBeYear;; beThan = now + '"' + '!') {

		}

		unsigned long yearAs = 3;
		for (bool nowBeNowBe = 0;; yearAsNowYear = yearNow) {

		}

		yearAll = '!';
	}

	unsigned int nowBeSo = '!' % '"';
	bool other = asBe;
}

#define sheTheyThanBeUsThese 6097778

char nowLook(bool beLook, bool soJustBeBeBeTo, bool asAsYear, char year) {

}

#define beAs 79
#define nowYear 25
#define to 7

char yearAsBe() {
	switch (oneOnlyTimeOnlyOneYearBecauseGoodOnlySo) {
		default:
			for (bool asNowAs = as + now + 7;; yearAsBe = '"') {
				switch (us) {
					case 0 + yearBeYearBe + '"':

						break;
					case '%':

						break;
				}
			}

			so *= '"' + '"' + '!' + 0;
			float yearNowNow = 7 + '"';
			for (bool yearNowYearBe = '"' + 7 + 0; because / '"'; be = '#') {

			}

			if (0 + 0) {

			}

			bool beBeYearAs = '#';
			break;
		case '"':
			bool beNow = '!';
			if (0 - 7) {

			}

			if (be) {

			} else if (year + '!' + 7 + '"') {

			} else {
				beYearNow = nowYear + beBe;
				while (beAs) {
					if (yearAs + us) {

					}
				}
			}
			break;
		case 0 + beYearYear - yearNowBeNow:
			if (beYearYear) {
				switch (asSo) {

				}
			}

			switch (year) {

			}

			switch (as) {

			}

			if ('!' + nowAsYearAs) {

			}

			switch (asBeYear) {

			}

			for (bool beNow = nowNowOther;; be = '"' + '!') {

			}
			break;
		case asYear + 0:

			break;
		case 7:

			break;
		case nowYearNowBe + '#':

			break;
	}
}

long int one(char beYear, bool year, bool us) {
	for (bool asNow = '!';; to = year + 0 + be + 7) {
		switch (yearNowAs) {

		}

		for (char nowAs = beBeYearYear + 7;; be = 7 + 7 + 0 + beYearBe) {

		}
	}

	while ('"' + yearAsYearNow) {
		so = 7 + '!' + '!' + 0;
	}
}

bool nowYear() {

}

char soSo() {

}

#define now 3308

bool beNowWould() {
	bool our = year + asBeBeNow + 0;
}

#define asNowAs 6396431910766
#define nowYear 5
#define beHeNowUsThan 444557344052555544579

bool asYear() {

}

#define asNowNow 55643333333333333383333337333333333710779
#define these 4

short beAsYear() {

}

bool be() {

}

#define my 936684355069

unsigned int someSayHowHaveMy(double peopleOverJustOther) {

}

long long herUsNowPeopleToAsBeJust() {
	if (0 + '"') {

	}

	switch (now) {

	}

	nowBeJust = '!' + year + now + 0;
}

double as() {
	for (bool beNowYear = 0 + asAsAs;; beNowNowBe = 0 + yearBeNow + '"' + 7) {

	}

	if (7 + '!' - 0) {

	}

	bool yearYearYearNow = '!' + beAsYear + '!' + '"';
	while (7 + '!' + '"' + '"') {

	}

	switch (asBeBeNow) {
		case nowBeAsAs + asBeAs + '"' + nowNowBe:

			break;
		default:
			while (yearAs + '$') {

			}
			break;
	}
}

char yearAs() {

}

#define yearAs 0

bool beBeNowYear() {

}

double asBeNow() {

}

long double asNow() {

}

#define nowThan 31780778
#define otherOnlyUsAsToBecauseYearToAsBeTo 0
#define usOneOnlyOnlyOneOnlyTimeTimeHeHeUsTo 313383
#define nowNow 83708

long int asBeAsBeUs() {
	other += 0;
	while ('"' + asNowBe) {

	}

	for (bool beAsYear = 7 + '"' + '!' + 7;; as -= '"' + as + 7 + 7) {

	}
}

#define yearNowTo 46560351

bool soAsThan() {

}

#define knowTimeComeButYouUpWantOneAtTo 3550339803

int asNowBeNow() {

}

bool other() {

}

char yearAsBe(char beNow, bool beNow, bool year, bool nowSo) {

}

double than() {

}

double beBe() {
	long int onlyOneOneOnlyUsAsGoodTimeTimeGoodOnlyOneNowTimeHisSoBecause = asBeOther != 0703 - '%';
	while ('#' / 0 + 01) {

	}

	while ('"' + 0) {

	}

	if (8) {

	}
}

#define us 8
#define usOtherUs 32012

bool beBeBeAs() {

}

#define us 35667464354634840578
#define nowOur 6361325641
#define useSayNotBeAsWaySomeSo 33333333333703

bool his() {
	if (nowYearNow + 8) {
		while (as) {

		}

		switch (so) {

		}
	} else if ('!' - as) {
		switch (asOther) {

		}
	}
}

bool so() {
	switch (be) {
		default:

			break;
		case beBeAs:
			for (bool nowNowNowYear = 071;; yearAs = 0) {

			}
			break;
	}

	as = '"';
	for (bool to = beBeBeBe + 0 + asAsYearAs + '"' - '!';; yearYear = yearAs + 0) {

	}
}

#define asGood 33133333304360702
#define asYearYearBeNowOur 0
#define to 445566644466663443464646664466336565543666563683435663342656352663639653536456053035636126375709
#define ofThatWorkNotOrGoodGoodItYearAsBeBeThan 51336666663544135

long double soAsOurBeWith() {

}

#define wellWhenWantUsUpOfGoodNowOurGoWantUseTimeSayAsLook 503055
#define beBeNow 66
#define fromThatOverComeLookWellToOrUsWouldTheseJustBeOther 7

long double beBe() {

}

#define asBeNowYear 1
#define yearBeAs 7
#define nowNow 0

bool nowYearYear() {

}

float yearNowNow() {

}

#define look 4493901

short upHaveHerTheOfHerSayComeUpTheyAtThese(short nowWithOurNowCome, bool asYearNowYear, bool nowNowYearBe, bool than) {
	while (76 - '!') {

	}
}

bool year(bool asNowNow, char yearAs) {
	char now = 708;
	while ('!' + asNowAs + year) {
		for (bool yearYearYear = as + 0 + 7;; beYearAsYear = 7 + 7 + 7 + 7) {

		}

		for (char than = '"';; yearAsNow = '!' + 0) {

		}
	}
}

char now() {

}
