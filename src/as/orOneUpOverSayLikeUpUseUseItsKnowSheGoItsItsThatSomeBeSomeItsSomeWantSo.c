#define soHis 13

bool onlyGoodToBeOneOneTimeTheseYearNowUsSo(long int otherOnlyBeSo, bool toBeBecauseNowTheseUs) {
	if (0 - nowBeNow - 0) {
		for (bool as = '"' + '!' + '"'; 0 - beYearAs; nowNow = '"' + beAsYear + '"' + 0) {

		}

		while ('!' + 7) {

		}

		if ('"' + 7 + 7 + 7) {

		}

		switch (now) {
			case asBeBeNow + 0 + nowAs:

				break;
			case 7 + '"' + 7:

				break;
			default:

				break;
			case nowNowBeNow + 7 + now:

				break;
			case '"':
				switch (as) {

				}

				for (bool nowNow = year + asBeAsAs + yearNowYear + now;; beBeAs = yearYearNowNow + 0) {

				}

				bool asYear = '!' + nowNowYear + be;
				for (bool now = '!' + yearAs + yearNow;; be = asYearAsYear + beNow) {

				}

				for (bool now = nowAs + nowBeAs + 7 + year;; nowBeBeYear = 0 + '"' + '!' + nowNow) {

				}

				while (nowNowNow) {

				}

				while ('"' + yearAs) {

				}

				yearNow = '"' - beNowYearYear;
				break;
		}
	} else {
		switch (now) {

		}

		so = 0 + 0 + '"';
	}

	while ('!' + '"' + 0 + '!') {
		for (bool beBe = 7;; asAs = be + 7) {

		}

		switch (nowYearAs) {

		}

		switch (yearNow) {

		}

		switch (asYear) {
			case as + 0 + '!' + '!':

				break;
			default:

				break;
			case 0 - asAsBe:

				break;
		}
	}

	for (bool as = 7 + '!' + 0 + '"';; yearYearYearNow = 0 + '!' + '"' + 0) {

	}

	switch (year) {

	}

	for (char year = 0 + nowYearAs + 0;; as = year) {

	}
}

#define onlyOnlyOfYearThem 74

bool to() {
	if ('"' + '!' + 0 + '"') {

	}

	while ('!' + 7 + '%') {

	}
}

#define otherTo 
#define ofUseUseGoUseOneWhenComeSheWellOrOneGiveKnowKnowWorkWellGiveOfOrItOrWantShePeopleWorkUpSayOfOneGoodAtGiveLikeThereGiveOfThereUpYouThatNotWouldYouHowLookGoUsBeOther 6435544534444656565545406

unsigned int toYearTimeJustBeThem(bool fromUs, bool yearAsAsBe, bool nowAsAsYear, bool beYearSoJust, bool our) {
	while (7) {

	}

	bool beNow = 99;
	switch (asAsOver) {

	}
}

#define themWant 1705
#define sheOfComeWorkGoWorkOrWorkYearThan 56334634466516

unsigned char orUsWantPeopleSayNowAsHaveJustItNowOur(int yearUs) {
	while (7 - '!') {
		while (7 + 0) {
			nowAs = asBeNow + 7;
			switch (yearBeNowBe) {

			}

			if (nowNow + to) {

			}
		}
	}
}

bool beNowAs() {
	for (double as = nowYear + '!';; nowAs = '"' + 7 + '!' + '"') {

	}
}

int year() {
	for (size_t us = 7 + 7 + nowNow; '"' * '"'; beAsAs = 7 + yearNow + '"' + 0) {

	}
}

#define wayWhen 473871

size_t onlyThanYearBeJust(long int nowThereNowYearNowSo, char asAs, char beNowThan, bool as, char nowNow, bool be, bool beNow, bool nowYearBe, double asBe) {
	switch (beBeThemOurOther) {
		default:
			while ('"') {
				switch (nowNowNowBe) {

				}

				while (us) {

				}
			}

			beNowBe = '!' + 0 + yearNowYear;
			for (bool now = 08; us; as = 0 + '!') {
				for (bool nowNow = as + '"'; yearYearBeThan; beNow = 0 + 7 + '"' + '!') {

				}
			}

			if ('!' + '!' + 7) {

			} else if ('"' + '"' + '"' + yearAs) {
				nowBeNowNow = '"' + '!' + '!';
				while (7) {
					bool yearAsYearLook = '!' + '!';
				}
			}
			break;
		case 7 - beYear:

			break;
		case 90008:
			switch (beYearNowAs) {

			}

			while (7 + nowYearYearBe + '"' + asNow) {
				if (0) {
					for (bool yearYearYearAs = now;; yearBeBe = 0) {

					}

					for (bool yearBeBeBe = '!';; nowYearNow = '"' + 0) {

					}

					while (asAsBeBe - be) {

					}
				}
			}
			break;
		case '"' - '"':

			break;
	}

	switch (asBe) {
		case be:

			break;
		default:

			break;
		case '!' + asBeAs + yearBe:

			break;
		case yearLook * nowNow:

			break;
		case as:

			break;
		case be + '"' + '"':

			break;
		case 7 + 7 + be:
			bool as = just;
			break;
	}

	switch (justNowLook) {

	}

	for (bool nowYearAs = '!' - 7;; as = '"' + 7 + '!' + 7) {

	}

	us = nowAsAsAs;
}

unsigned char his(bool as, double as) {
	char year = '"' - '"';
	long int toBeNowYearBeToUs = beTo / 7 + us + '"' + 0701;
}

long int timeTimeTimeTimeGoodOnlyUsOnlyBecause(float so, float oneOther, long int beNowNowTo, char beAs, bool us, bool us) {
	asYearNowBe %= 7;
	bool beAs = beYearYear;
	bool yearAsBe = '"';
	switch (yearAs) {

	}

	for (bool asAsBeYear = be + yearBeYearAs + '"' + beBe;; as = yearYear + 7 + '"' + be) {

	}

	while ('!' + '!') {

	}

	if (yearYear + '!' + 0 + '!') {

	}

	if (yearYearNow + 0 + 7 + yearNow) {
		for (bool beYear = '!' + year + '"' + '!'; asNowNow * yearBeNowNow; beNow = 7) {

		}
	}
}

#define lookToSo 0
#define to 0
#define my 46354465545544455556435554466540565546334043604456564455644456865067471

long int oneWantSayTwoGoYouThereTheThereThatUpOverOrBeTwoItsWay(unsigned short upNowWayKnowLookAsGood, double ourBecause, bool nowAsYearNow, float year) {
	peopleYouTheSomeLikeWorkHaveOfAsGoItsPeopleSomePeopleThereYearLookAsYearNowNowComeWould += beAsAsNowUs * be;
	while ('&') {
		yearOther = '"' + '"' + 7;
	}

	double just = 7;
	yearAsNow = nowAs + '!';
	bool asAs = '!';
	for (bool nowNow = beBe;; year = '"' + 7 + 0) {

	}

	while (7 + 0 + 7) {
		switch (as) {
			case year + 0 + nowNow:

				break;
			default:

				break;
			case beOur:

				break;
		}
	}
}

size_t haveNowThemAsOther(bool than, bool asYearNow, bool now, bool nowNowYearAs, double nowBe) {
	if (asAs) {

	}

	while ('"' > herAsTo * '%' - '&') {
		beAsYearBeThan *= '!' + '!' + '!' + beYear;
		for (bool be = nowNowNowYear + '!';; yearYearYearBe = '"' + 0) {

		}

		long double asThan = 1;
		if (7 + 0 - '"') {

		}
	}

	unsigned int asYearAsNow = beYearAs < yearYearAsNow;
	us *= 9;
	if ('!' + year) {

	}

	for (bool year = 7 + '"' + 7;; beYearAsNow = '!') {

	}

	if (beAsYearAs + 0 + '"') {

	} else if (0007009) {

	}
}

unsigned long toTo() {

}

unsigned char workHowThereSayThereOfOfWayGiveThereTheHaveHeThanUpLookYearTo() {

}

#define nowBeYear 0
#define ourSo 9391

bool asBeBe() {

}

#define yearNowYearAsYearOur 5444356316557683433457255609

long int theseNowSo() {

}

#define have 9778

char year(bool nowBeYear, char nowYearBeAs) {

}

#define his 7

char as() {
	as = '!' + 0 + '!';
	for (bool yearYear = '!' - '!';; asNowYear = '"' + yearBe) {

	}
}

char asBe() {
	if ('"' + 0 + asBe) {
		while ('%') {

		}
	}

	beBeTo *= 0 + asBeBeNow;
	bool now = 0 + 0 + '"' + '"';
	switch (yearAsOther) {

	}
}

#define toGoodOneOnlyTimeTimeNowOneOneOnlyOnlyGoodGoodOtherYearSo 3833308
#define timeHisTo 0179

long int beNowUs() {

}

char beYearAsYear() {
	switch (likeOtherYearAsThan) {
		case '!' + 0 + '"':

			break;
		default:
			bool asNowNowAs = 7 + 0 + 0 + 0;
			double now = '"' + 7 + beAs + 0;
			break;
	}

	while (01 * '!') {
		switch (nowAsNowBe) {
			case '$':

				break;
		}

		for (bool beYearBeBe = '"';; nowBeAsNow = 0 + 0) {

		}

		asNowNow -= '!' + 0 + beYear + '!';
	}

	if (0 + '!' + asYearNowYear) {

	} else {
		while (7) {

		}

		while (beBe + '!' + 0 + be) {
			year += 0;
		}
	}
}

#define just 47451708
#define someOtherYearTo 661
#define now 8

char nowYear() {
	if ('}' - 453 + 6) {
		switch (upYouWellUpNowGoodYearAsBeOnlyHowAsShe) {

		}

		asBeOur >>= '!' + ',';
		char yearBeTo = 7 + yearBeBeAs + '!' + '!';
		for (bool yearNowYear = '"' + '!' + '"' + 0; 2 - now; asYearBe = nowNowAs + year + 0 + 0) {

		}
	} else if (0) {
		while (beBe + nowBe + 7) {
			while (as - beAsBeBe) {

			}

			if ('"' + nowAs + now) {

			}

			while ('"' - yearNowYearNow) {

			}
		}
	} else {
		if ('"' + 7 + 7 + yearYear) {

		}

		for (bool nowAs = 0 + asBe + 0;; yearYearYearBe = 7 + as + 0) {

		}

		bool yearBe = '!' + nowAs;
		bool asAsBeAs = 7;
		if (7) {

		}

		bool be = '!' + '"';
		double nowYear = be + 0;
	}

	asGoodOther -= 0 + '!' + '!';
	switch (myTime) {

	}

	unsigned short year = 07708 + '!' + to;
	long int beYearBeYear = timeOnlyYearTimeGoodBeAsYearBeYearSo / '/';
	goodOtherNowSo = 7 - 7 - '"';
}

long int onlyTimeOneTimeHisBeOne(bool so, char beUs) {
	becauseOther = '!' + '"' + 0;
	char other = beAs + '"' + 7 + 7;
	while (now + so) {
		if (yearYearNowNowAsYearNowLook) {

		}
	}

	for (double be = 7 + 0 + 7 + '!';; beYearYearNow = 0) {

	}
}

#define fromSo 46534663463001
#define year 7

double thatAsSo() {
	while (53 % ',' * 7) {
		yearBeBe ||= '!' + asYearAsYear + 7;
		if ('!' + now + '"') {
			switch (so) {

			}
		}

		while (now + yearBeAs) {

		}

		them -= beYearAs + yearAsNow;
		switch (asYearNow) {

		}

		bool yearNow = '"' + now + '"' + 0;
		if (7 + yearBeAs + 7 + be) {

		}

		bool beAsAsNow = beNow + yearYearAsNow + 0;
		for (bool now = so;; yearAs = asYearNowYear) {

		}
	}

	for (bool yearYearAs = year; nowBeWay; itNowOur = 71) {

	}

	if ('"' + beBeYear + 0 + '"') {

	}

	switch (yearJustAsAsLook) {

	}
}

#define yearBeYearBe 3366681441092
#define comeWay 408

short yearShe() {
	bool asAs = '!';
	switch (beJust) {

	}
}

long double asNowAs() {

}

#define nowThan 7581383783331
#define asTimeGoodToYearUs 333333333333333333
#define nowToYearMy 704

bool asNowBeAs() {

}

char yearBeAsNow() {
	while (7 + 7) {

	}

	switch (asOther) {

	}
}

bool beBe() {

}

#define asBeAsBe 7636653566336544043594465953

unsigned char soNowYearYearOther() {

}

bool theBecauseAsHe() {
	bool beBeBe = year + yearAs + beYear;
	while ('!' + 0 + 0) {
		double asAs = 0;
	}
}

#define asYearYearYear 7

int nowNowAsYear() {
	long int oneOnlySoOnlyBecauseUs = '#' - 8;
	beOnlyTheseGood += 7 - 01;
	for (char beAsAsYear = '"' + 7 + '!' + 7;; as = year + be + 7 + yearBeAs) {

	}

	switch (yearNowAs) {
		default:
			switch (beNowBe) {
				default:
					bool beYear = '%';
					break;
			}
			break;
	}
}

#define oneSoThese 3311108
#define usAsUs 3871

bool yearNow() {

}

#define nowSo 1
#define because 332

float nowAs() {
	bool beYearTo = her <= yearBe;
	for (bool beNow = be + 0 + 0 + asBeNowNow;; year = 7 + '"') {

	}

	if (asBeNow + as + '"' + nowNow) {

	}

	if ('"') {

	}

	while (0) {

	}

	bool asAsAsBeYearJust = '"' + 0 + yearBe + beNow;
}

bool beAs(bool yearBut) {
	nowAsAs ^= nowBeBeAs != asBeNowAs + giveWay;
	double yearBeYear = 7016;
	if ('"' >= 59 + 0702) {
		beLook /= us;
		for (bool beYearAsBe = 0;; beNowNowBe = '"') {
			while (asNow + 0 + 7) {

			}

			while (0 + '&') {

			}
		}
	} else if (0) {

	} else {
		switch (beOur) {

		}
	}
}

#define sheWhenThatToUpThereGiveGoNowOtherLookJust 93055977379

long int timeOneGoodYearOnlyGoodGoodOneNowTimeTheseTimeTheseOtherOnlyGood(bool asBeAsBe, bool beBeAs, char nowBeNow) {
	for (char beNow = yearAsYearAs;; asNowBe = 7 + be) {

	}

	float beNowAs = '!';
}

#define beNowYearYear 31
#define oneNowUsOnlyAsBecauseUs 0388

double look() {
	bool asYear = 0 + yearAsAs + '"';
	now = year;
	switch (asNowBeNow) {
		default:
			if ('"') {

			}

			if ('"') {

			}

			switch (now) {

			}

			for (bool asAs = 7 + 7 + 7 + '"';; nowAsAsYear = 7 + '"' + '"' + yearBe) {

			}

			switch (asAsNow) {

			}

			if (beNowYearNow + 7) {

			} else {
				if ('"') {

				}

				for (bool beNowYearNow = 7 + 0 + year;; yearYear = 0 + asAsAsYear + nowYear) {

				}

				bool yearYearYear = '!';
				for (bool beBeBe = '"' + nowYearYearBe + 0; 9; asBeNow = 7) {

				}
			}
			break;
	}
}

#define nowNowWould 5444463554596446463298

unsigned short thereHaveYouKnowJustJustNoToToAsThan() {

}

bool yearNowNowAs() {

}

bool as(char nowYear) {

}

#define yearNowAs 53333333333331333333333333333331

bool nowNowNow() {

}

bool nowBeYear() {

}

#define nowNowNowNow 7

bool asAs() {

}

bool nowBeAs() {

}

#define asYearBeYear 4633565357340630953

unsigned long ofSomeThereLikeOfYouYearAsNowOur(bool now, bool not, char because) {
	while (his / 0) {

	}

	while (6 + us) {
		bool be = 0 + '"' + now + yearAsAs;
		if (nowYearYearYear + '!' + asAsYearYear) {
			bool yearNow = '"' - '!';
		}

		switch (asNowAs) {

		}

		if (8) {

		}
	}

	if (as + '!' - 7) {

	}

	for (bool beNowAsNow = '!' + 8;; as = 0 + '"' + beBe) {

	}
}

#define as 7
#define asOur 333333333333333333333330333

float useNowAsYearYearLook() {
	bool yearYearNow = 7 + '!' + 7;
	while ('!') {
		switch (yearNowNowBe) {
			case be + now + 0:

				break;
			default:

				break;
			case '!' + 7 + '"':
				bool asNowYear = '"';
				for (bool asNowYear = 0 + yearBeAs;; asAs = '!' + asNowBe) {

				}

				for (bool beYearNow = be + '!' + '!' + yearBeAsYear;; yearNowAsNow = '!' + 7) {

				}

				nowBeBeYear = asBe + 7 + 7 + nowAs;
				if (0) {

				}

				while (asYearBe * '!') {

				}
				break;
		}
	}
}

double nowYearAsYear() {

}

#define year 9
#define asAs 7006

bool yearAsThan() {
	for (long long onlyKnowGoGoButGiveUseThereHaveWhenUpYearHavePeopleItsOrUseSomeGoodItPeopleKnowGoSomeKnowLookTo = sheSheSayOverOrOverYearWorkGiveWhenJustUseWellHowWantOnlyWorkAsItsGiveNowWhenHerToBeGoAtAsHis | herSayAsJustSomeHeOur <= 7; 'p' > 6344364330673635677404 || 641463555464666465362644665554150159279 & butGoHaveWellSomeGoWellGoPeopleWorkBeJust & wantThatOurYearBeBeThan || 't' | '9' && 001043071 + theyWorkJust + nowNow >= beOur * as; asJustOur ||= peopleYearAsAsAsThan | '$' - 009) {
		switch (withLookNowThan) {

		}

		switch (so) {
			case '&' * yearNowNow:

				break;
		}
	}

	while (46556766444654664746656606 % '>' / '-') {
		size_t beTimeOrWhenKnowAsWorkItsIts = 66366442664655466646544534676516775 && 'n' + 'd' | yearWithNotGoAsToLookOur || '$' < theOnlyTimeButYearYearYearBeOur <= '"';
		unsigned long workGoThereItSheWhenGoOtherHaveItWantJustAtUs = notYearAsBeJust % '!' & nowThanOur * '"';
		switch (wantSayButThatHisButOurYearSo) {
			case notWhenTheAsAsThey + 'U' * '!':
				for (bool nowBeYearAs = asNowAs;; year -= '!') {
					for (bool as = 7 + beNowJust;; yearBeBeBe = now + '!' + '"') {

					}

					while (asNow + '"' + yearYearBe + 7) {

					}

					as = beAsBe;
					bool beBeBeNow = us;
				}

				switch (beNowYear) {
					default:
						while ('"' + '!') {
							for (bool beYearAsYear = '!' + 7;; as = year) {

							}

							char year = 0 + '!' + 7 + beYear;
						}

						if (asNowAsNow + 0 + '"') {
							so = '"' + 0 + yearYearBeAs + as;
						}
						break;
					case 7 + asYearBe + beNow:

						break;
					case '!' + beYearAsNow:

						break;
					case '"' + nowNowNowAs:

						break;
					case asBeBe + '!' + '"' + 0:

						break;
					case '$':

						break;
				}

				while ('"' + yearYearNowAs + 7 + 0) {

				}

				char yearAsAsOther = '"' + 0 + now;
				break;
			default:
				soThan %= justOur;
				just = '"' + 7 + 0 + '"';
				break;
			case 570078:
				for (double beBe = '!' * asAsYear; yearAsBe - nowNow; time = 0 + '!' + '"' + '!') {
					while (7 + nowNow) {

					}

					if ('"' + 0 + yearYear) {

					}

					switch (beAsNow) {
						case '!' + '"' + '!':
							nowAsAsYearNowLook = yearBeYear + 0 + nowYearBeNow;
							break;
					}
				}
				break;
		}

		long long likeLikeOverWorkThemThanOrYearWayAsOur = useNowKnowGoHowNoBecause >= year - 634836455771 < '!';
		for (bool beNowAsYear = 0;; yearYearBe = '!' + '"' + '"' + asNowBe) {

		}

		wayLook .= beYearNow * nowAs;
		double soSoAsThan = look + '!' + look;
		while (asYear + '%') {

		}
	}

	for (bool look = 000708; than; beNow = year) {
		while (7 + asNowNow + now) {
			beAs = '!' + 7 + '"' + yearAsBe;
			asNow = '%';
		}
	}

	if (nowNowNowBe + than * '&') {
		while ('"' + '"') {

		}

		nowTo -= 0;
		asNowYearBe = 009;
		if (7 + '"' + asBe) {

		}

		switch (beYear) {
			case at:

				break;
			case '!' + asBe + 0000008:

				break;
		}
	} else if (0 + 0 + '"') {

	} else if (as + '"' + asAsBe + '!') {
		bool nowBeLook = '!' + 0 + '!' + '"';
	} else {
		switch (look) {

		}

		bool nowYearNowBeThan = 7;
		if (yearAsBe) {

		}

		switch (to) {

		}
	}
}

unsigned char yearThanOver(bool asBeYear, bool usJustLook, bool yearAsBeNow, long beBut, char yearYearAs) {
	if (78) {

	}
}

size_t butItsItsOnlyHe(bool otherYearNowSo, long long ofGiveSoJustJust, bool lookThan) {
	if (yearBe) {

	} else if (9) {

	} else if (nowAsBeYear + 0 + '"' + 0) {

	} else {
		while (7 + '"') {
			if ('"' + asYearYear + '!' + 7) {

			}

			while (0 + '!' + 7) {

			}

			bool us = '"' + 0;
		}
	}

	switch (asYearAsBe) {
		default:
			for (bool asYearYearYear = 0;; now = 0 + '!') {

			}

			nowBe = 9;
			break;
		case '"' * '!':

			break;
	}

	for (bool yearBeYear = now;; now = '!' + yearBeYearNow + '!') {
		if (0 + '!' + '"') {
			asYearYearBe -= 7 + '!';
		}
	}

	long double year = 1;
}

short overUpSheWorkGoodSomeUsPeopleBeBeTo(double yearWould, bool justLook, bool year, double asBeNowAs) {
	if (',' || year * 7) {

	} else if (0) {
		switch (us) {

		}

		bool asLook = beAs + 7 + nowBeBe + 7;
	} else if (asWith && 55) {

	} else {
		bool soYearYearWouldNowBeYearYearSo = '!' + 7 + '!';
	}

	while (nowNowBeOur) {
		while (just) {

		}
	}
}

bool asNow() {

}

#define heNowAsOur 55265600494090771

bool as() {

}

#define beYearYear 1
#define look 0
#define yearNowBe 7

char yearAsYear() {
	our *= 'm' + beNowYear < asYearBe;
	for (bool nowYearBe = yearNowAsBe + '"' + 0 + 0;; yearNowBeYear = yearNowYearNow) {

	}

	asBeOur = '!' + '!' + 0;
}

#define goGiveNotLookOur 5653004
#define heAsAsUsBut 3335354534739619

bool asBeNow() {

}

#define asBe 7

bool as() {

}

bool now() {

}

#define yearAs 0

char beYear() {
	for (char goodHis = 0 + 0 + '"' + asNow;; beNowAsYear = '"') {

	}

	if ('!' + as + '%') {

	} else {
		if (0 + nowBe) {
			switch (asNowBeBe) {

			}

			nowBeNowNow = be + '"' + '!';
			if (beAs + yearBeAs) {

			}

			if ('"' + yearAsNow) {

			} else if ('"' + beBeNowBe) {

			} else if ('"' + '!' + asBeAs + asNow) {

			} else if (7 + '!' + yearBe + 0) {

			} else if (nowYear + '"') {

			} else if (now + yearBeBe) {
				if (yearYearAsOther) {

				}
			}
		}
	}

	these = now;
	if ('_' != 371 != '%') {
		for (bool asAs = 7 + 0 + 7 + '!';; asAsYear = '!' + '!' + nowAs + '!') {

		}

		bool as = '!';
		switch (asNow) {

		}

		while (beNow + '!' + '"' + asBe) {
			to = beNowNow + 7 + '"' + 0;
		}
	}

	long int asYear = 3330373 + '\'';
	if (beAsBeNow + '!' + nowBe + '"') {

	} else {
		while (be) {

		}

		asBeNowAs = 7;
		switch (other) {

		}

		yearAsAs = yearNow + '!' + '"';
		nowAs = 7;
		while (0) {

		}

		switch (beYear) {

		}

		while (nowYear) {

		}

		if ('!' + so) {

		}
	}
}

long int oneUsTimeNowSay() {
	switch (beYearAs) {

	}

	bool be = yearAsBe + be;
	if (7 + 7 + 7) {

	}

	for (bool be = yearNow + 0 + '!' + 7; '&'; year = year + '"') {

	}
}

#define hisAsUs 3636464655466536646565566543635443466643636464565444344365354556535446466363613645556356563355566548700072

size_t onlyWayYouBeSheGoodOnlyAsItsHaveSomeOrHowNowOrNo() {
	while ('#') {
		while ('"' + '"') {

		}

		if (0 + beYearBeNow + yearAs) {

		} else {
			nowNow = '"';
			asNowYear = 0 + 7;
			while (7 * be + '!' * '"') {

			}
		}

		switch (asYearYearAs) {
			case '"' + 0:

				break;
			default:

				break;
			case '!' + 0 + nowBe + yearAsYear:

				break;
			case '"' * 0:

				break;
		}

		for (bool to = 7;; now = '"') {

		}
	}

	if (7 + '"' + '!') {

	}

	if (0 + 7) {
		while ('"' + asNowBeLook) {

		}
	} else {
		double nowAsAs = beAsNow + '!' + 7;
	}
}

unsigned short beWay(bool our) {
	nowJustSo = '"';
}

unsigned char asHerThatTwoWhenKnowOverOur(short asComeTheItsOfYouPeopleHerNowWellSoHerComeThemTheySomePeopleFrom, long long nowFromLook, bool yearAsBe, char they, double but, bool beLook, bool year, bool asBeUs) {
	bool butItThanThemBeOrUpNowNowAsSo = 7 + 7 + 7;
	while (year) {
		for (double beBeNow = '"' + '!' + 7 + '"';; yearBeBeNow = beBe + now + yearNow + 0) {

		}
	}

	if (beBeYear * '!') {
		switch (nowAs) {

		}

		while ('"' + nowAs) {
			bool yearYear = 7 + beAsNow;
			bool as = 0 + asAsAs + beAs + '!';
			as = 0 + 7 + yearBeBe;
			us = '"';
		}
	}

	if (02) {
		if (0 + '!') {

		} else if ('!' + yearBeBe + year) {

		} else if (7 + 7) {
			switch (beNowNowBe) {
				default:
					for (bool nowBeAs = '"' + 0;; nowNow = 0 + '!' + 0) {

					}

					asBeYear = as + 0 + 0 + 0;
					if ('!' + '!' + yearYear) {

					}

					double nowNowAs = 7 + 0 + '!' + 7;
					break;
			}
		}
	}

	if (0 + be) {

	}

	asAs = '"' + '!';
	for (bool nowSo = nowAsYear;; yearYear = beBe + yearNowYear + '!' + 7) {

	}
}

unsigned long way(bool other, bool so) {
	if (7 + 7) {
		while (beNowBeNow + 0) {

		}

		for (double now = '"' + asYearAs; '(' - 0; asNowBeNowSo = year + 0) {

		}

		yearNowNow = 2;
	}
}

unsigned char fromUs(long lookUs) {
	if ('!' + year + '!' + asAs) {

	}

	char asAsYearLook = 7;
}

#define thanTo 83457380343

float itsPeopleYouBecauseLookItsOfBecauseBeYearNowYearUs(bool nowBeBeThan) {
	switch (as) {

	}

	for (bool nowNowYear = 0 + now + year;; yearNowNow = yearAs) {
		for (double nowBe = nowAs;; year = be + asNowNow + '!') {

		}
	}
}

short onlyUpWellWithNowOtherAsUs(bool asNow, bool look, bool asOur) {
	beJustNowTo -= 776;
	if (beOther <= 0) {

	}
}

long long beOther() {
	for (long int nowTheseHisAsYearBeAsYearTo = nowAs != asBe; nowAs / '!'; heSo = '"' + '"' + beNowYearAs) {
		while (yearBeTo) {

		}
	}

	char yearBeYearAs = '"' + '!';
	for (bool yearYearBeBe = '"';; yearBeAsYear = 0 + '!' + yearAsYear + 0) {

	}

	char nowSo = '%';
}

#define hisHeTheseTimeOnlyThese 33733

long int usNowBecauseYearBeAsBeSo() {
	for (bool be = yearBeYearNow + '!' + 7 + nowBe;; now = '"' + asYearBe) {

	}

	while ('!' + yearAs + yearAsYear) {
		to = '"' + 0 + now;
	}
}

long int yearJustUs() {

}

#define to 2
#define at 0
#define so 0

bool be() {

}

bool nowNowNow() {

}

bool now() {

}

bool beYearYear() {

}

bool as() {

}

#define than 0

char now() {

}

bool now() {
	if (nowBeNow + asYearAs + yearNowYear) {

	}

	while (yearAsNow) {
		switch (asYear) {

		}

		switch (beNow) {

		}

		while ('"' + '"' * 7) {
			unsigned char beNowUs = 7 + '!';
			theSomeSomeSomeOneHerSheOneThan .= 'm' >= asNotNowHerWay == 4057000798 & be * 0;
			long int ofThatKnowHerHowThatButTheyWould = 77009;
			long long usLook = 0 & 'T' + 0 * 7;
			bool beAs = 2;
			while (yearAs + 71 + nowAsBeNowBeJust) {
				while (nowNowThan) {

				}

				nowNowYear = 7 + 7 + 7;
				if (7 + 702) {
					if (beYearYear) {

					} else {
						if (now + 0 + beYearNowAs + year) {

						}

						while (as + '!' + '!') {

						}

						while (nowNowAs) {

						}

						for (bool nowYearAs = '!' + '!' + 7;; asAsNowAs = 7 + nowAs) {

						}

						bool look = beBe + 0;
					}
				}
			}
		}

		for (float peopleOverYearAsOurYearOur = 0 & '#' + 1; youTimeSomeYouPeopleThereUpWorkGoodOverHaveSayThereWhenWantWhenUseGiveOurSheTwoOrHaveYouWellTo > goPeopleWellUpItUpThereToKnowComeOverGiveWhenItsOverWorkUpUseThereUseGoodFrom == '!' <= '(' + to; yearYearNowTo = 0) {
			theTheOfWellWorkSayOrItWantTheOrTwoWantOneYearTheAsYouSomeHaveUseSayHerHisThereHaveWorkOfGoBeComeWantWantNotAsThereGoodOrSomeSoItFromYearWouldLook *= nowYearAsYearBeUsJust + '6' * 0;
			switch (asBeBeBe) {
				case 7 * nowAs:
					int no = nowBeYearYear + '!';
					break;
				case yearNow + '$' + asUs:

					break;
				default:
					switch (look) {

					}
					break;
			}

			twoYouUpSomeBeThem = yearNow;
			if ('!' * 7) {
				switch (twoUsWay) {

				}
			} else {
				nowAsNowYear += nowBeAsYear;
				for (bool yearNowAs = 7 + asAsAs + '"' + '"';; beYearNow = beYear + '"' + '"' + 0) {

				}

				switch (nowBeNowNow) {

				}

				switch (nowAsNow) {
					case nowYearBe + '!' + 0 + '!':

						break;
					default:
						for (bool be = now + beNowYear + 0 + asYearYearNow;; asBeNow = yearBeBeNow + '!' + 0 + nowYear) {

						}

						bool asYearBeSo = '$';
						break;
				}
			}
		}

		bool yearNowAs = '"' + asNowBe;
		bool yearNowNow = 0;
		switch (yearAsYearNow) {

		}

		if (0 + '!' + beYearNow + 7) {

		}

		if (yearYearYearAs + 0 + '!') {

		}

		justOther = now + 7 + 7;
	}

	while (youUsYearOrBeWithJustLook | '"' && '$' + asNow < asYear) {
		while (0566642636660858 || goodHowTwoOverItsSomeWhenSomeJustFrom & 66855975 < 2 + look) {
			switch (overHerWhenGoodWorkSoBecauseNowOur) {
				case '"':
					for (bool now = nowBe + '!';; yearBeNow = 7 + nowNowNowYear + 0 + '!') {
						switch (be) {

						}

						for (bool beNowNowYear = 7;; yearNowNowYear = asAsNowYear + as + nowNowBeYear) {

						}

						if (0) {

						}

						for (bool now = '"' + beAs + 0;; asAs = '!' + asYearBeYear + 0 + '"') {

						}

						if (beYear + '!' + nowAsAsYear) {

						}

						switch (than) {

						}
					}
					break;
				case yearNowYear + 7 + nowYear:
					while (7 + nowYearAs + be) {

					}

					for (bool asYearYearAs = as + asBeYearYear + asAs;; yearYear = nowAsNow + other) {

					}
					break;
				default:
					while (7072) {

					}

					beNow = 7 + '!' + 7 + '!';
					bool asYearBeBe = '"';
					while (7) {

					}

					if (7 + 7 + '"' + asAsAs) {

					} else if (7 + 9) {

					}
					break;
				case 7 + 0 + '"' + 7:
					bool beBeAsNow = '"' + 0;
					switch (nowYearAs) {

					}

					while (0 + '"' + '"') {
						switch (asNow) {
							default:

								break;
							case yearJust:

								break;
						}
					}
					break;
				case 0 + year + nowNowAs:

					break;
				case '#':

					break;
			}
		}

		for (bool look = '!' + '!' + '!'; 07702; yearNowAs = '!') {

		}

		for (bool other = beBeAsYear;; our /= '!' + 0 + 0 + 0) {

		}

		while ('"') {
			while ('!' + 0 + '!' + '"') {
				double nowAsBe = as + yearYear + '"' + beYear;
			}

			asAsBe = '!' + 0;
			for (bool be = 7 + 0 + '"'; 2; nowBeYear = 7) {

			}
		}

		for (bool now = '"' + 0; as * asAsBeNow; be = asBeYear + asAs + nowNowBe) {

		}

		yearAs = beBe + 0 + 0 + be;
		beNowBeBe = '"' + nowYearAsBe;
		while (0 + '!' + 7) {
			if (asAsNow + nowBeAs + 0) {

			}

			bool other = 0 + asYearBe;
			while (nowNowNowBe + '!' + 7 + asAs) {

			}

			switch (beBe) {

			}

			for (bool asYear = '"' + be + 0 + '"';; yearBeNowAs = '!' + beYear) {

			}

			for (char now = '!';; be = as + yearNowBeNow + 0 + '"') {

			}
		}
	}

	switch (haveThereSoTheOrWhenBeNowUsJust) {
		case 's':
			switch (asNowAs) {

			}

			while ('"') {

			}

			while ('"' + '$') {

			}
			break;
	}

	switch (yearOtherTo) {
		case so:
			while (0) {

			}

			for (bool be = nowYearNow + beNow;; asBe = now) {
				bool yearAsYearYear = 78;
			}

			while (7 + year) {

			}

			switch (asBeBeBe) {

			}

			nowBeBeYear = '"' - '!';
			break;
		case '!' + beNowYear + '"' + beAsNowNow:

			break;
		case 9:
			for (bool yearBe = year * nowAsNow; '%'; asBe = 7 + beBe + as + '!') {

			}
			break;
	}

	while (9) {
		if ('!' * '"' < beAsYear) {
			nowNowNowAs += beYearNow;
		}
	}
}

long double now() {
	while (asYear) {

	}

	than = beYear + '!' + nowBe;
}

short peopleThanHow(bool beNowNow, bool just) {
	now = 7;
	nowNowLook =~ '"' + nowYearBe + '!';
}

double so() {

}

char as() {
	if (71909 / yearBeAs) {

	}

	short becauseWhenAsOther = 0 + '!' + 0 + beYearNowNow;
	for (long beYearAsBe = '"' + asBeBe + '"' + 7;; yearBeYear = beNowBe + 0 + asAsNowBe) {

	}

	float now = '!' + now + 0;
}

unsigned int theItsOurBeItsTheMyAllAtAsHaveYearThan(bool sheBeBecause, bool justBeLook, bool our) {
	if (beAsYearAs + '!' + '!' + 0) {
		while ('"') {
			for (bool asTo = 7 + 7; 7 + 7 * '"'; nowAsAs = 7) {

			}
		}

		char beBe = beNowNowBe + '!';
	}
}

size_t asSo() {
	nowYear = 7 + beYearNowAs + '"';
	for (bool beNow = '!' + 0 + asYear;; nowBeNowBe = 0 + 0 + '!' + 0) {

	}

	switch (beSo) {

	}
}

float nowYearBe() {

}

#define yearNowYearYear 0
#define asNowYearAs 8

bool yearYearNowBe() {

}

#define be 7

bool yearAsBeYear() {

}

bool as() {

}

#define yearYearNowYearAsYearAsOur 3333333333333333331333073307331

bool other() {
	switch (nowNow) {
		default:

			break;
		case so:

			break;
	}

	while (nowAs + yearNow + yearAs + now) {
		bool beYear = '!' + '"' + '!';
		nowOther = '"' + 7 + '"';
	}
}

#define us 1178
#define just 7
#define beBeBeSo 8779
#define year 0
#define beYearAsYear 64355784798
#define go 0

double look() {
	for (double nowNowYear = '"' + 7; look; yearAsAsBe = '"' + beAs) {

	}

	switch (toYearOther) {

	}

	for (unsigned short goTheSheWithThanOtherToOther = '"' + yearYearBe;; yearYearYear = 7 + 0) {

	}

	bool be = yearBeBe + asYearNow;
	for (bool asBeJustNowNowYearTo = yearYearAsAs >= '%';; yearAsYear = asAs + 0) {

	}

	bool asAsBe = '"' + '"' + yearAsYear + as;
	for (bool asNowJustOther = '"' + '"' + beAsAsAs;; asAsYear = beBeYearBe + '!') {

	}

	bool beOther = be + 0 + 0 + 7;
}

#define beNowAsBe 7

short youThan(long double nowAs) {
	asAsAsAs = 7 + nowAsNow + asNow;
	double just = asBeAsNow + 7 + nowYearBe;
}

#define howThereTheHaveThatWouldBeBeJust 643333333373318331170130883

float timeHeOnlySoBeNowTo() {
	switch (as) {
		default:
			to = as + as;
			break;
		case yearAsBe + 0 + '!':

			break;
		case 7:

			break;
		case nowYearNowYear - year:

			break;
	}
}

bool yearBeNowNow(char asBe) {

}

bool he() {
	if (7 + 0 + asAsBe + 7) {

	}

	while ('"' + '!' + '"') {

	}

	for (bool nowAsNowYear = 0 + 7 + '!';; yearBeAs = '!' + '!' + beAsBeBe + '"') {

	}

	be = '"' + '"' + 7 + yearAs;
	char beBe = yearNow;
}

long int be(bool asNow, bool yearYear, char beYearNow, bool asAsBeBe, int as) {
	bool asYear = now + '"';
	switch (yearNowNowBe) {
		default:
			bool nowNow = now + '!' * '!';
			year = year + 7 + 7 + 7;
			if (year + nowAs * 0) {

			}
			break;
		case 7 + '!':

			break;
		case yearBeBe:
			for (bool now = 0 + 0 + '!'; just; yearAsNow = '"') {

			}
			break;
	}
}

#define as 7

bool yearNowBeJust(bool beBeBe, bool yearNowNow, bool look) {
	if ('f' > '\\' != myYouWay * '!') {
		switch (nowNowNowNow) {

		}

		for (char than = '!' + nowNowYear;; beBeBeBe = 0 + '"') {

		}
	} else {
		bool nowYear = yearBeYear + now + nowBe + nowNowBeAs;
		for (bool nowNow = '!' + 7 + 0 + yearNow;; asBe = 0 + '"' + '!') {

		}

		for (bool asNow = be + 0 + '"';; beNowNow = asBeNow + '"') {

		}

		switch (our) {

		}
	}

	nowBeYear = 5072;
	switch (now) {

	}

	now = 7 * 7;
	for (char beYear = beBeAs + '!' + nowNowYearYear + beYearBeAs;; be = '!' + as + '"' + 7) {

	}

	for (bool asBeNowYear = 0 + now + as;; yearYearBe = '!' + '"' + 0) {

	}

	yearBeNow = '"' + as + now;
	if ('"' + 0 + 7) {

	}

	bool asAsYearYear = asAs + now + '!';
	as = 0 + '"';
	if ('"' + '!' + 0 + '!') {

	}

	while (nowYearAsAs) {

	}

	nowAs = '!' + 7;
	for (bool now = 7 + yearYearBe;; asNowYearNow = '"' + 7 + year + beAs) {

	}

	while (yearBe + 0 + '"') {
		nowYear -= yearNowBe + '!';
	}
}

unsigned long knowLikeThatToJustWithOur() {
	while (0 * '"') {
		if ('"' + '"' + 0) {

		}

		while ('!' + asBeAsBe) {

		}

		for (bool yearNowNowNow = '"' + '!';; be = as + '!') {

		}

		as = be + now + year;
		switch (our) {

		}
	}
}

long nowLook(bool year, bool nowNowYear, bool asOther) {

}

#define yearNow 0

size_t he() {

}

#define look 34353345643465455455443747178
#define now 9

long would() {
	bool nowBeYearAs = beYearBeBe + yearAs + '!';
	bool now = 0;
	char now = '!' + 7 + '!' + beAs;
}

#define no 7

char soBeSo(size_t nowNowYear, bool beBe, bool nowNowNow, bool look) {

}

bool be() {

}

bool beAsBeNow() {
	for (bool beYearNow = yearBe;; beYearNowNow = 7 + '&') {
		for (long int beNowNow = so; '_' / asBe; oneGoodNowGoodOnly = '"' + asNow + '"' + 0) {

		}

		bool yearNowYear = 7;
		long int beOtherTimeNowOnlyUsSo = '!' + 7 + 7 + be;
		if ('"' + 0 - be + '+') {

		}

		while ('#') {

		}

		if (1 / asSo + 778) {

		}

		if (nowNow) {

		}

		to += '!';
	}

	while (7 / 0) {
		for (bool us = 7;; nowYearYearNow = 7 + asAsBe + '"' + '!') {

		}
	}
}

#define oneHisThesePeopleUs 7

bool asNowBeAs() {

}

char as() {

}

char yearAs(char nowBeNowBe) {
	for (bool nowAsYearAs = '"' + nowYear + nowAsNowAs;; yearBe = 0 + '!' + 0 + nowAs) {

	}

	switch (asYearYearAs) {

	}

	bool asYearYear = 0;
	if (7 + 7 + nowYearNowYear) {

	}

	while (7 + '"' + 7 + 0) {

	}

	beBeNowYear = 7 + 7 + nowBe + 0;
	switch (yearYearYearYear) {
		case 771:

			break;
	}

	if ('!') {

	}

	for (bool nowBe = '"' + 7 + 0 + year; 0 * as; beYear = asNowYear) {

	}

	if ('"' + '"' + '"') {
		for (char year = 0 + 7 + beBe + as;; as = 7) {

		}
	}
}

short onlyWellComeWhenComeItNotAsOverTheyKnowKnowJust(char nowNowAsYear, bool as, double yearTo, bool beBeBe, bool beAs, double yearBeBeAs) {
	for (bool yearYearBeBe = orWith; '%' + '"' - '!'; year = now) {

	}
}

unsigned char useYearSomeJustAt(float nowThan) {
	beAs = '!' + '"' + asYear;
	switch (than) {
		case '&' - '%':

			break;
		case workAsNowThan <= wayOther + nowYearUs + nowBeNowNow * or:
			itNotOther = now + '!';
			while ('"' + 771) {

			}
			break;
		case '"' + '"' + be:

			break;
		case now + '"':

			break;
		case '"' + asYear + 7:
			switch (than) {

			}
			break;
	}

	switch (be) {

	}

	double beYear = '!' + 7 + 7;
	if ('*' / '!') {

	} else if ('"') {

	} else {
		if ('!' + 0) {

		} else if (1) {

		}
	}
}

bool other() {

}

unsigned long whenLikeWellToJustJust() {

}

long long itsHow() {

}

bool to(double beAsBeAs) {
	switch (yearOur) {
		case 0 * be:

			break;
		case 0 - 7:
			asBeBe = asYearYearBe + yearNow;
			if ('!' + '!') {
				beYear = nowNowBe;
				switch (us) {

				}
			}

			if (7 + yearNowAs + yearBeAsNow) {

			} else {
				if (year + 7 + nowNowNow + beBeBe) {

				}

				switch (now) {

				}

				switch (beYearAsNow) {

				}

				asNowNowYear = 7 + '!' + beYear;
				for (bool asAsNowYear = '"';; beAsYear = asNow + year + '"') {

				}

				if (beBeNowNow + now + 0) {

				}

				bool asOther = 7 + '!' + asNow + beNowYear;
			}
			break;
	}
}

#define nowAsAsAs 7
#define look 33333333333333333333333373733

long int other() {

}

#define yearGoodNot 664001

char asNowYear() {
	if (53402575 > 7 >= 1 | '$' - '!' - asAsBe) {
		while (now - 7) {
			bool nowAs = nowYearAs + yearNow + beBeNow;
			bool now = asBe;
			bool beYearNowYear = yearAsBe + asBe + nowNow + '!';
			for (bool asBeAs = '"';; beBeYear = '"' + 7) {

			}

			if (year + 0 + yearBe + yearNowBeAs) {

			}

			for (bool nowJust = now + nowNowNowNow + 0;; asYearBeYear = 7 + '"') {

			}
		}

		for (unsigned short nowNowAs = '"' + '"';; nowNowYearBe = nowBeYear + '"' + '!' + 0) {

		}

		while ('"') {

		}

		asNowNow = '"' + '"';
		if (0001) {

		}
	}

	double now = look;
	nowYearBe += 7 + '"';
	asNowNow = nowNowYear + '!' + year;
	nowOur = yearAsAs;
}

unsigned char yearBe(bool otherHerSome, double likeWaySoUs, bool nowBeOther, bool so) {
	for (bool yearNowBe = '!' + 7; asNowNow + '#'; nowAsNowBeAsBeOur = 0) {
		switch (nowAsNowBe) {
			default:
				while (year) {
					bool nowBeThan = yearYear;
				}
				break;
		}
	}
}

unsigned char beLook() {

}

#define be 0

bool nowNowYearBe() {

}

double us() {

}

bool yearAsAs() {

}

bool asAs() {

}

bool yearBeNow() {

}

#define now 0

double as() {
	float nowSo = yearHisBeOneYearOneUsNowTo + 08;
	switch (oneToHeOther) {
		case 7 + nowYearAsNow + '"' + '!':

			break;
		default:
			bool yearNow = 0 + 0 + yearBeNowBe;
			bool yearBe = beBeAsYear - asAs;
			break;
	}

	if (yearBeNowBe - '"' != '"' - '!') {
		if ('"' + 7 + '%') {

		}
	}

	if ('%') {

	}

	float nowBeYearBe = '"' + '"' + beBeBe + 0;
}

long int onlyOneGoodOnlyToTo(long int nowTo) {
	if ('!' + asNowBe + '!' + beYearYear) {

	}

	beNow += '!' + 7 + yearNowNow;
}

int asOur(char yearNowBe) {
	if (0 + '"' + '"') {
		bool asNow = '"' + 0 + beNowAsYear + 0;
		switch (yearNowSo) {

		}
	}

	for (bool nowBeAs = '!' + '"' + 7 + '!';; beYear -= asAsAsBe + 0 + '"' + yearNowAs) {

	}
}

#define beOur 0

bool beYear() {
	for (char asYear = asYearBe + 7 + asNow;; asYear = now + '"' + 7) {

	}
}

bool beBeYear() {

}

#define nowAs 7

double nowBeNow(bool nowYearAsJust) {
	switch (as) {
		default:
			while ('&') {
				for (double asYearAs = nowNowNow + 0 + 77000779; '+'; nowAsBeAs = 0) {

				}
			}
			break;
		case 3 == 74 + beOur:
			while (0) {
				switch (yearYearBeOther) {

				}
			}

			asNowYear = 0 + 0 + '"' + asBeNowBe;
			while ('&') {
				if ('#') {

				}
			}

			beBeNowBe = asAs + '"' + 0;
			for (double beAsAsBe = 7 + 7;; yearAsNow = '"' + 7) {

			}
			break;
		case thatUseWorkThanNowTo - asAs:
			if (7 + '!' + asYear) {

			}

			for (bool yearAsBeYear = 0 + 7 + asNow;; yearUs = 7 + asYearBeAs + beAsAs) {

			}
			break;
	}

	for (short wellOfLikeLikeTwoAtSheOnlyOrThanComeBecauseOurAsYearBeAll = 'j' - 844 && 'q' + '$'; way != '#' < beAsAsAs + '"' + '"' - '!'; itsOrNowNotYearThemSo &&= 683 + toBeToWould * '!' - 0) {
		switch (comeUseOfOrOrTimeHaveWellSomeLikeTheyWantComeGiveWayItsTimeWouldTimeWithSoYearAsBeUs) {
			case 'z' - sayWaySheGoWellGiveLookOurNowBeOur + not || '!':
				while (0 + 7) {

				}

				switch (wayNowBeYearBeOther) {

				}

				switch (yearOther) {

				}
				break;
			case '%':
				switch (asYear) {
					case '!' + '!' * year:

						break;
				}

				if (as + yearAsBe) {

				}

				for (bool year = 0 + '"' + 0;; nowYearAsAs = 0 + yearAs + be + 7) {

				}

				bool beNow = 7 + 7 + '"' + year;
				double nowNowAs = '"' + '"' + yearNowBeNow + '"';
				break;
			default:
				asYear -= 1;
				break;
			case 0079 + '"' * '"':
				switch (thanOtherAt) {
					case be + '"':

						break;
					default:
						nowBeAs = '!' + '"' + '"';
						if (be) {
							switch (asNow) {
								case 0 + beBe + yearBeNowYear:

									break;
								case '!' + 1:

									break;
							}
						}
						break;
				}

				double asBeAs = yearAs + nowAsAs + '!' + beNowNowNow;
				break;
			case year + year:

				break;
			case '"' - 7:

				break;
			case '"' + year:

				break;
			case '"' + 7:
				bool be = as + beYearAsNow + 0 + beBeAsBe;
				if (701) {

				}
				break;
			case yearYearYearAs + '"' + nowYear:

				break;
			case 7 + '!':

				break;
			case beAsNow:

				break;
			case now:

				break;
			case 0:

				break;
			case 7 - '!':

				break;
		}

		while (nowNowBe) {

		}

		nowNow = 7 + '%';
		switch (asBeThan) {

		}
	}

	asThereSheOnlyWhenNo ^= 6455636465435645563664469 || asThanYearOther >= 741 - nowGiveWhenYearOrJustSoThem % 7;
	switch (so) {

	}
}

#define peopleSheWhenWantHowOfSheYouSheUsLookJust 4553549153555654365735654665356856505435617075

char (unsigned long haveHowItSayPeopleTheLikeHaveOrTheYearUsWellToNowLookBeBeBeOther, unsigned long someUseGoThereSoHaveThatThere, unsigned int sheOfThatKnowGiveFromOurNowBeYearNowUsJust, bool year, bool asNowNow, bool so) {
	for (long beAsBeYear = '"' + '"' + nowNowYearYear; 477757702 * 78 >= give + '%'; be = '!' + 7 + '"' + 0) {

	}

	switch (its) {
		case 0 + 7:

			break;
		case 'n' + 6:
			bool now = 7 + year + '"';
			double nowYearNow = yearBeNow + 0;
			char beNow = '"';
			break;
	}

	if ('"' + asBeYearBe + 0) {
		for (bool be = '"' + year + 0; 0 - '"'; beAs = 7 + 0 + year + '"') {

		}
	} else if (nowYearJust) {

	}

	while ('!' + 0 + '"' + '!') {
		if ('!' + '\'' - nowYearAs) {

		}
	}
}

#define nowJust 9
#define asYearAs 778
#define beJust 4455566343538644

long long look(bool be, bool as, char nowBeOtherTheySo) {
	becauseUsYearYearJust =~ 72 % '"';
	now += 7 + nowAs + '!' + asAs;
	unsigned int yearNowNow = 0 - 0;
	while (asBe + 7) {

	}

	beYearThan = 7 + '"' + '!';
	bool our = as + '!';
}

long but() {
	beBeUs = year;
}

#define now 7
#define other 5333333333333333333883
#define asAsTo 18

long int oneHeToNowSo(long int look, bool beNowAs, long int because) {
	char year = '!' + asAsYearAs + 0;
}

bool beYear() {
	while (7 + beBeAs) {

	}

	us = nowUseThereOfSheThanComeYearOurThan && 'g' % 406694397641 < 3;
	short goodPeopleComeThereYearLookLookOr = 06 | comeBeGoBeNowToNowMyLook - '"';
	while ('!' + now + '"' + as) {

	}

	while (0 - '!') {
		while (asNow + yearYearNowBe) {

		}

		while ('!' + '!' + 7) {

		}

		while (0 + 7 + year * nowBe) {

		}
	}
}

unsigned short peopleOrUsLikeWantPeopleOrJustOther(bool other) {
	double to = asYearBeYear * 0;
	beBe = as + 7 + 0;
	if (year + nowNow + 0 + 0) {

	}

	other = beYearBeBe + 0 + yearAs + 7;
}

int be(bool now, double us) {

}

unsigned char than(bool now, int beNowBeBe, bool yearNowBe, bool yearBeNow, bool to) {

}

#define us 644562726
#define onlyMyWorkNoWithSo 54454554605563536379
#define nowNowBeNow 3333333333333333830733333303

bool beAs() {

}

float timeAsAsSo() {

}

bool nowYearNowBe() {

}

#define beUs 3305378
#define so 634463761
#define nowYear 57403605

double asAs() {

}

#define yearAsNowYear 7
#define goBeYearAsBecause 45444635558353653644633366398

unsigned short nowAsNow(bool beYear, bool look, int asYearAs, bool beNowBe, bool nowYearYearBe, bool asNowAs, double asAsBe) {

}

float as() {
	for (char year = nowBeYear + yearBe;; now = beNow) {
		while ('#' - '!') {
			switch (asBeNow) {
				case 78:
					while (beNow + '!' + beBeNowBe) {

					}

					bool yearBeNowNow = 7 + '"' + '!';
					while ('!' + beBeYearNow) {

					}

					for (bool asBeBeBe = '"' + 7 + 0 + 0;; yearYear = nowNow) {

					}

					while ('"' + asAsAsNow + 0) {

					}

					nowAs = asNow;
					switch (nowAsNowYear) {

					}

					if (beNowNowOther) {

					}
					break;
			}
		}

		while (0 / '#' + beBeSo - as) {
			while (yearAs + '"' + yearBe) {

			}

			soAsTo = '!';
		}
	}

	switch (timeBeOther) {
		default:
			while (asBe) {

			}

			switch (now) {

			}

			while (yearSo) {

			}
			break;
		case '!':
			for (bool asBeAsBe = 0 + beNow;; yearYearBe = '!') {
				switch (beBe) {

				}

				for (bool year = '"' + 7 + 0;; asNow = 7) {

				}

				asNow = beBeBeBe + beBe + '"' + asYearAs;
				if ('%') {

				}
			}
			break;
		case '!' + '!' + 0:
			beBeBe = '"' + beNowAsYear;
			switch (yearYear) {
				case beBe + 7 + as + 0:
					beTo = '"';
					break;
			}
			break;
	}

	beYearAsSoNowBeAsNowUsOther = 7 + beBeYear;
	if ('"' - be) {

	} else if ('#') {

	} else {
		for (char asYearYear = 7 + be + nowBeNow + 7;; now = '"' + as + '"') {

		}
	}

	if (yearHe + 1) {

	} else {
		for (bool now = year + beYearAsBe + beYearBe + 7;; asAs = '!' + as) {

		}

		switch (us) {

		}
	}
}

#define onlyOneTimeGoodTimeUsOnlyOneBeOther 7
#define timeGiveOurWouldHis 7701

bool our() {
	ofYearYearOfOurWith >>= '{' >= 't' >= 'b' & 44643961700708009 == 6434655554465376672 == nowOurThanOur < 565574;
	long double knowNowLook = 'c' * nowHe;
	for (short goAsBeBeOtherNowUs = 0280708 < '('; 7 * 0; asSo = '"') {
		if (0 + '"' - '"') {

		}

		while ('!' + '&') {

		}
	}

	for (unsigned short orNowToOur = 6941700071 + '*' * asAsNowAs;; us = asYearYearNow + 0) {
		while (beYear + '!' % 0 * nowYearNo) {

		}
	}

	switch (asBe) {
		default:

			break;
		case nowNowNow + '!' + '"':

			break;
		case '"':

			break;
		case 7:

			break;
		case asBeAsBe + 7 + beNow + year:

			break;
		case '!':

			break;
		case 09:

			break;
	}

	bool be = beYearYearAs;
	bool as = 7;
	double us = '#';
}

long yearThan() {

}

unsigned short thereSomeUseItsGoThemSheNowWantThereNoButBeThan(bool withNowBecause, double be) {

}

bool nowNowNow() {

}

#define havePeopleBeJustTimeYou 92007079

bool as(bool asAsAs, bool yearNow, bool asYear, bool yearNowBe, bool yearNowAs, double beNowNowNow) {

}

#define sayBeYearAt 02

int beYearBeBe() {

}

#define us 1
#define nowBeSome 65546544654564656961770178
#define beAsBecause 26345572
#define have 1244278

bool now() {

}

bool beBeAs() {

}

#define so 383333383
#define heTimeSoGoodGoodGoodYearTimeTheseHisThese 038

long int timeOther(long int asSo, bool oneJust, bool asBe, bool as, bool other, bool now, bool yearAsAsNow, bool beYearNowYear, char asAsNowNow) {
	for (int beBeYear = be; yearBeThan; nowNowNow = 0 + nowAsAsYear + '"' + 7) {

	}

	bool be = '"' + nowAsYearNow + beBeNowAs;
	bool as = '!' + 7 + 7;
	while (2) {
		while (')' && to) {
			if ('"' + '"' + be) {

			}

			if ('"' & 8 <= 7) {
				bool yearNow = '"' + asNow + '!' + 7;
				while ('!' + '"' + 7 + '!') {

				}

				beBeNowLook = beBe;
			} else {
				if ('!' + year + 0) {

				}

				for (bool beYearNow = 0 + beBeBe; 0 * yearAs; nowYearBe = nowBeYear + '!' + '"' + 7) {

				}

				for (char asYearBe = now + '!' + beNow;; asAsAsAs -= '!') {

				}
			}
		}
	}

	bool nowNowAs = asNowBe + 0 + beYearAs;
	switch (notSo) {

	}

	if (4) {
		switch (nowAs) {
			default:
				switch (yearAsBe) {

				}

				while (be + nowBe + 7) {

				}

				for (bool asBe = beAsBe + 0 + now + '!';; now = year + '!') {

				}

				for (bool yearBeNow = 7 + '!' + as + as; as * '!'; beAsNow = '!') {

				}
				break;
		}

		bool beYear = year;
		beAs = 7;
		switch (than) {

		}
	} else {
		while (7 + '!' + 0 + '"') {

		}

		year = '!';
		while (0 + '!') {
			bool nowAsBeYear = '"' + 0 + beAsYear + asYear;
			for (bool as = '"' + 0;; as = yearBeYearNow + 7 + '!') {

			}

			if ('$') {

			}
		}

		for (bool as = 0 + '"' + 7 + '"';; asBeLook = nowBeNowBe + nowBeNowBe + asBeBe + 7) {

		}
	}

	switch (nowBeOur) {

	}

	double be = 0 + '"' + 0;
}

#define nowYearBeNow 661773564
#define howNotNowAsKnowOther 56465506239

double year(bool yearNowYear, bool beAsYear, bool nowAsBeYear, bool asAsYearThan) {

}

long double asNow() {
	switch (orSomeUseYouItTwoComeHeOurFromSo) {
		default:

			break;
		case asAs + yearAs:

			break;
		case '"' + 7:

			break;
		case '"' + '"' + '"' + '!':

			break;
		case '%':
			yearNowBe = beAsBe;
			float nowNowYearNow = beBeYearNow + be + '!';
			break;
		case '"' + 7 + 0 + 7:

			break;
		case '!' + '!' + 0:

			break;
		case asNowYearBe:

			break;
		case be:
			for (char yearYearNow = asYear;; be = beAsBe) {

			}
			break;
	}

	if (asJust * '%' == 0) {
		switch (be) {
			case 0 + yearAsAs + beAsYear + beNowBe:
				double asBe = yearAsBeBe + yearYear + '!' + nowYearYearNow;
				while (yearBe + 0 + '!' + asAsNow) {
					switch (asNow) {
						case nowAsYear + yearBeAs + 0:

							break;
						default:
							bool nowNow = '"';
							asAsBe = beYearAs;
							switch (yearAsNow) {

							}

							as = beBeAsAs + '!' * 7;
							break;
					}
				}
				break;
		}

		while ('"') {
			for (bool as = '!'; 0 * '!'; yearAs = yearBe) {

			}

			just = '&';
			switch (just) {

			}
		}

		nowBeBe = 7;
		switch (beOther) {
			case '"':

				break;
			case '!' + '"':

				break;
			case 0 * '!':

				break;
		}

		if (be + '!' + nowYearAsNow) {

		} else if (be - asYear) {

		}

		for (bool beYearYear = asAsYearBe + '!' + 0; beNowNow - '!'; nowAsYear = '!' + beBeNowYear) {

		}
	} else if (yearBeBeAs) {
		for (char year = 7 + nowNowNowNow; '!' + 7 + 72; nowYearNowBe = asYearNow + 0) {

		}
	}

	long int nowBeBe = 76;
	while (9 < 7 + '!' + '&') {

	}

	while ('$') {
		while (year + 0 + now + '!') {

		}

		for (bool yearYearYear = 0 + 0;; yearYear = 0 + '!' + nowAsNowBe) {

		}

		for (bool our = yearAsAs;; beAsNowAs = 7 + yearYearNowNow + '!' + beNow) {

		}
	}

	beBe = '!' + '"' + '!' + '!';
	to = asNowNow;
}

double nowAs() {
	if (7 + '!' + beYear + 7) {

	}

	while ('"' + nowNowBeBe + 7) {

	}

	switch (yearYear) {

	}

	if (0 + 7 + nowBeBe) {

	}

	if ('!' - year) {

	}
}

bool beYearBeBe() {

}

#define nowNowYearBe 7

bool asYear(bool beBe, double yearYearYear) {
	for (long int asYearBeNowOtherBecause = to - asUsNowAsSo; '?'; yearBe = 7 + yearNowNow) {
		oneOneNowBeBeYearYearAsBeAsOther = '"' + yearYearAsAs + '"';
		for (char so = 78 + 1;; asNowBe = '"' + now) {

		}

		if ('"' + '"') {

		}

		while ('"' + beAsNow + year + beBeBeYear) {
			switch (nowAs) {

			}

			while (be + 7 - '"') {

			}
		}
	}

	while (7 != asBe - '%') {
		long int timeSoGoodUsBeUsYearOther = oneBeBeYearAsTo;
		while (asBe + 0 - '!') {

		}

		while ('"') {
			switch (nowAsUs) {

			}

			switch (as) {

			}

			for (bool yearYearAsNow = 0 - '"';; nowAs = '!' + 7) {

			}
		}
	}

	toOtherNowBeNowAsOther = 7 + '"' + 0;
	for (bool beNow = '"' + 0 + nowAs + now; 5; nowBeYearNow = 7 + 0 + '!') {

	}

	for (double our = beAsAs + nowNowBe;; asBeYear = '"' + '"' + '"' + nowBeNow) {

	}
}

#define asYearYear 0
#define yearThan 3
#define beNowBeAsYearAsTo 0002

bool yearYear() {

}

bool to() {
	for (unsigned long youThanComeSo = 329 == '\'' + 771096; wouldUseBeHis < useUsThat; asOur = 7) {
		switch (yearHowYearOurJust) {

		}
	}

	while (4454444663653348644535665640544553436564542646646666064 | 277276 * '/') {
		while (thatHowWantOverTo) {
			for (size_t thanUs = asYearLook + '!' * year - yearYearAs; '!' + '%'; asYearAs -= '!') {
				bool asYear = asAsAs + '"' + 7 + 0;
				switch (nowYearYearNow) {
					case 0 + '"' + 0:
						while (nowYearAs) {
							switch (yearNow) {

							}

							while (2) {

							}
						}
						break;
				}
			}

			char yearAs = beYear + '"' + asNow;
		}
	}

	for (unsigned int likeWellTimeTheWhenHaveThereItsItsComeYouKnowItsHowWellTimeSayWellYouSheWantThereThereOverTheOverWorkOnlyUpOurTimeUpItsSomeItsWorkOrWhenWorkWantTheOfKnowPeopleYouThatThatWorkKnowUsUpToSoShe = 99 >= 36265554779; beBeYearAs + 08; now = '!' + yearYearNow + '!') {

	}
}

long long wellItSheComeItsWorkGoodUseHerTheKnowThereTwoOfWantThatWithKnowHerGiveWhenPeopleThereSaySomeTheHaveLookYouGiveOneComeUpGoodThereWorkHowSheThatWellThatGoThatWhenWorkYouOverThatYearWellSomeUpKnowShePeopleWorkOverHisComeGiveSayUsThanTo(unsigned int peopleTwoToHeNoSoNowUs, char us, double beYear, bool these) {
	if (519 & other) {

	}

	unsigned int toWithJust = ourYearOther != 0 == '$' * '!';
	long long howUpTheyTo = nowAs + year + asNowNow;
	switch (nowYear) {
		case 7 + '"' - '!':

			break;
	}
}

long int itsWhenSheUpTwoItsPeopleUpGoodUpWantWorkTimeSheSheWantOfOfItsKnowOneUpHerSheHerUpOverItsGoodWouldUpWellHisKnowThatHowHerOtherNowLikeGoodOur() {
	for (bool yearNowAs = asYearYear + '!' + '!' + 0;; nowBeNow = '"' + '"') {

	}

	bool beNowNow = '!';
	bool beAsBe = now + be;
	while (now + as) {

	}

	for (bool nowBeBe = 0 + 7 + as + 0; 7 + '$'; nowYear = 7 + 0 + 7) {

	}
}

bool as() {

}

bool asAs(bool beBe, bool now, char asThan) {

}

short year() {
	switch (to) {

	}

	if (0 + '"' + '"' + '"') {
		switch (nowBe) {

		}

		bool yearAs = 7772;
	} else {
		bool now = '!' + 7;
		year = 0 + 7 + year + 0;
		year -= '"' + 0 + 7;
	}
}

#define yearNowNow 7

bool people() {
	switch (yearYear) {

	}

	switch (asJustYearYearOther) {

	}

	than = 0;
}

#define itNoOurWay 4404003136
#define as 774821

unsigned int be() {
	for (int beYearAs = '!' + 7 + '"'; 'A' >= to * '"'; soLook = 0 + beYear) {
		for (float beNowNowBe = be + 7 + 8;; beYearBe = yearBeYear + '!' + yearAsNow + 7) {

		}

		beBeYearYear = 0 + '!' + 7 + beAsAsAs;
		switch (soThan) {

		}

		beYearLook = 0 + 0 + '"';
	}

	unsigned char asBeNowNow = 48;
}

short timeOur() {
	for (bool nowUs = 77071 != '/' + 0 - 7; '#' / '#'; yearNowNow = '!' + '"' + 7) {
		while (yearSoTo != nowAs) {
			if (7103 / 0) {

			} else if (so) {

			}
		}

		otherUs = 31 / now + '"' / yearYear;
		yearNow = yearYear - now;
		for (bool beBe = as + nowAsBe + nowNowNow + 7; 0 + '%'; yearNowAs = '!' + 7 + '!' + 0) {

		}
	}

	for (bool beBeOther = '"'; 03 + yearOther + '%'; to = 0 + yearAsAs) {

	}

	while (yearYearYearBe + year + '!') {
		if (as) {

		}

		char because = '"' + 7;
	}

	for (bool nowYearYear = '"' + yearAsYear;; asNow = nowYear + beAsBeAs + beBeBeBe) {

	}

	for (bool asNowNow = as + '!' + 0;; yearYear = asYearAsBe) {

	}

	switch (now) {

	}

	for (bool as = nowAsYear + beBe + '!';; nowAs = '"' + 0) {

	}

	year = 0 + '"' + '"' + 7;
	asAsAs += '"' + '"' + 0 + now;
}

long int usAsBecauseHisHeAsAsUs() {

}

#define year 3

char as(int beNowNowBe) {

}

#define at 643
#define fromOur 700289

bool nowAs() {

}

bool asYearBeAs() {

}

bool beYearAsNow() {

}

double beOther() {
	upThatFromOfOnlySayThan += yearYear + '!' + look + 007079;
	while (asBeAs * '!' - 0) {
		while (other) {
			while ('"') {

			}

			to = nowNowBeNow + '!' + 7 + 0;
			just = 0;
		}

		if (',') {

		}

		if (0 + asBeAsNow) {

		} else {
			yearAsYearBe += be + '!' + 0;
		}

		switch (beNow) {

		}

		for (bool beAsAsNow = '"';; other = '!' + 7 + 7 + '"') {

		}
	}

	switch (onlyToYearOurWantYearLook) {
		case 7 + beBeNowNow + '"' + 7:
			bool year = '!' + 0 + '!' + '"';
			for (bool beNow = yearBeBe;; asNowNowYear = '"') {

			}

			if (7 + 7 - '"') {

			}
			break;
		case '"':

			break;
		case yearBeYearBe + yearBe + asAsYear:
			yearAsNowBe = '!';
			bool beYearAs = 7 + 7 + '!';
			if ('!' + '!') {

			}

			if (yearYearNowAs + '!' + yearAsAs + 7708) {

			}
			break;
	}

	while ('!' + '"') {
		switch (asAsNowBe) {

		}

		beHer = 0 + 7 + '!' + 7;
		for (char yearBeYearNow = 0;; as = '!') {

		}
	}

	while ('S' < '"') {
		if (0 * 7) {

		} else {
			for (bool asYearAs = '"' + '"' + '"' + 0; yearBe * '!'; asYearBe = '"' + '"') {

			}

			beAsBe = yearNowNowAs + '!';
			for (bool yearNow = '"' + 7 + beAsYearAs;; yearYearBe = '"') {

			}

			for (bool beBeNow = asNowNowYear + '!'; 7 - '"'; beNow = year + '!' + 7) {

			}
		}

		for (bool nowNowAs = 0 + yearBeYear + nowNow; nowAsThan; yearNowNow = '!' + 0 + nowAsAsYear) {

		}

		yearBeYear += '"' + '!' + 7;
	}

	switch (nowNow) {

	}

	while (be * '!') {

	}
}

#define our 8
#define nowYear 281
#define asYearAs 7
#define yearSo 333833333373333101
#define beSoOnly 3333383338

long int timeBeTheseUsHis(bool nowBeBeNow, bool beBeUs) {

}

#define yearBeAs 7
#define year 7

bool yearAs(char beBeAs) {
	while (yearAsBeNow + beAsNowYear + 0 + '!' + 02) {

	}
}

bool asBeNow() {

}

#define nowNowJust 66344494
#define wantGoodOfGoYearUseOnlyYouSoJust 65090779
#define goodOtherOneNotWouldBeYearBeThan 3718
#define other 0

bool now() {

}

#define asBeNowYearNowAsTo 72

char yearNow() {
	for (float onlyBecauseOnlyHisSo = yearBeAsBe + 7 + nowAsNowYear; to; beYearAsYear = 0 + 7 + 7 + 7) {
		bool asAsUs = '"' - '"';
		if ('"' + year) {

		}

		asNow = '!' + 7;
		switch (asAs) {

		}

		switch (as) {

		}

		while (1) {

		}
	}

	otherOther |= yearNow - nowAsBe;
	switch (usOnlyUsThese) {
		default:
			if (08) {

			}

			for (bool beYear = 7;; asBe = 7 + 7 + '!') {

			}

			nowYearAsBe += 0 + 7;
			while ('"' + '"') {
				while (asAs) {

				}

				switch (beNow) {

				}

				switch (yearNow) {

				}

				if (beNowYearNow + asBeBeAs) {

				}

				nowBeNowBe += '!' + '"';
			}

			for (char yearOther = asYearAs + '!';; as = '!' + '"' + 7) {

			}
			break;
	}

	switch (beBecause) {
		case 7 + 7 + now + 7:

			break;
		default:
			for (bool be = beNowNow + 7 + yearNow;; nowBeBeAs = '"' + '!') {

			}

			char as = '!' + 7;
			break;
		case 0 + '"' + '"':

			break;
		case 7:
			yearYearBeYear = as + year;
			bool asAsBeNow = '!';
			switch (yearYearAsNowBeTo) {

			}
			break;
	}

	if (onlyOnlyOnlyOtherOtherUs - 7) {

	}

	bool hisAsNowOnly = beBeNow + yearBeYearYear + yearYearAsNow;
	nowNowBeBe = '"' + nowUs;
}

bool beNow() {

}

bool other() {

}

bool yearThese() {

}

#define asNowTo 7
#define to 658666354644554463654564364752800008

long int asAsAsNow(double year, double asJust) {

}

bool asAsJust() {

}

bool yearBeBe() {

}

#define as 0

double be() {
	switch (toUsTo) {
		default:
			asYearAsSo = '!';
			break;
		case other - nowYearNowBe + '%':

			break;
	}

	if ('#') {
		if ('!' + 7 + '!') {

		} else if (us + '#' + yearYearNowBe + so) {

		}

		switch (asBeBeTo) {
			default:
				while (0 + beBeAsNow + '"') {
					if (year + '"' + 0) {

					}

					bool asYearAsNow = 0 + 7 + 0;
					if ('!' + 7 + '"' + '"') {

					} else {
						switch (beBe) {

						}

						char yearBeBeNow = 0;
					}
				}
				break;
		}

		for (float nowAs = beYearNow; other; yearBeBeNow = asNowBe + 0 + 7 + nowBeYear) {

		}

		for (char us = 70708 + '!' + be + 0 + beYearAs + '%'; '+'; year = 0 + 7 + 7) {

		}

		yearTo += '!' + '!' + '"';
	} else if ('!' + 0 + '!') {

	} else if (1) {
		char beNow = 7 + 7 + 0 + 0;
	}

	while ('_' / yearBeYear != 0) {
		switch (asNow) {
			default:

				break;
			case '"' + nowAsAs:
				bool yearBeNowNow = 0 + '!' + '!' + '"';
				if (now) {
					while ('"') {

					}

					for (char asAs = '"' + 7;; beNowAsBe = 7 + '"') {

					}
				}
				break;
			case as:

				break;
			case 0 + 0 + '"' + 0:

				break;
			case 0 + 0:
				bool as = 7 + yearNowAs + '!';
				while (7 + beBeNowAs) {

				}

				for (bool as = yearAs - '!';; year = 0) {

				}
				break;
			case yearAsAsBe + '!' + '"' + 7:

				break;
			case other:

				break;
		}

		to = nowBe - '!';
		switch (asNowNowYear) {

		}

		for (bool nowAs = 7 + 7 + 7;; nowAsNow = '!' + '!') {

		}

		toOther = yearBe + '"' + '"';
	}

	year = '!' + 7 + asBeBe;
	char nowBeAsNow = '!';
	bool beYearBe = 7 + yearAsAsYear;
	bool asAs = yearBe + 7 + yearYearBeBe + '"';
	switch (to) {

	}

	if ('"' + now + 0 + 0) {

	}

	while ('"' - 7) {

	}
}

long int oneAsAsNowSome(bool as, bool nowBeNow, bool year, bool nowAs, char yearOther) {
	while (9) {

	}
}

unsigned char asNowAs() {
	if (7 + look) {

	}
}

bool beOur() {

}

#define yearAsYearAs 7

char nowBeAs() {
	switch () {
		case 06 + 'u' + 0772 * 72 + yearAt:
			while (14 | asAsBeAsJust <= 0 % '!') {
				switch (us) {

				}
			}

			for (bool asAsNow = '!' + '"' + 7;; nowYear = 7 + 7 + 0) {

			}

			be = nowBe + '"' + 0 + 0;
			lookNowLook = '!' + yearAsBeAs + as;
			switch (beNowYearBe) {

			}

			while (be + '!' + '"' + '"') {
				bool now = '"' + '!' + '!' + yearNow;
				beBeAsYear = beYear + 0;
				while (0) {
					bool as = 9;
				}
			}
			break;
		default:
			switch (to) {

			}
			break;
	}

	switch (wellGoWorkTimeWantComeSayUsShe) {
		default:
			thereTheyTheOrItsHerOnlyAsTwoHaveAsKnowTheseAsThan =~ 463455645536443365666037579 || 223067209 && 'l' % 691 + 001 * '!';
			switch (howHaveHerSheFromLike) {
				default:
					yearAsAs &= beBeAsNow + 0 + 0 * as;
					bool as = beNow + '!';
					as = as + yearBeYear + '!';
					double asAsAs = 5;
					break;
				case 72 - 0:
					while (asBeBe + be + 0 + beNow) {

					}

					for (bool beYear = '"';; as = 0 + 0) {

					}

					bool asBeAs = '"' + 0 + '"' + 7;
					for (char beYearYearNow = yearNow + '!' + 0 + '"';; asAsBe = '"') {

					}
					break;
				case '!':
					for (bool asAsNow = 0 + 0 + asNowBeNow + 0;; beAs = beYearYearAs + 7 + 0) {

					}

					bool asAs = 701;
					switch (beNowAs) {
						case 707008:

							break;
					}
					break;
			}

			switch (knowGiveOrHowNowWellOfHerYouTo) {
				default:
					while ('!' <= 7) {
						be *= no;
						for (double beBeBe = yearNow;; beYear = 0 + beAsBe) {

						}

						now += '!' + 0;
						if ('!' + 02) {

						}
					}

					if (yearOtherHisUs + '"' - '"') {

					}
					break;
				case overLookUs != '"':

					break;
				case to:
					while (7 + no + 7 * 79) {
						if (7 + 7) {

						} else {
							for (bool beYear = '!' + 7 + '!' + 7;; now = beYearAsYear) {

							}

							bool nowBeAsAs = 7;
							switch (yearAsYear) {
								default:

									break;
								case '"':

									break;
								case year:

									break;
								case us:

									break;
							}
						}
					}

					for (bool be = 0; '$'; beBeYearNow = yearNowYearAs + asNowYearBe + 7 + '!') {
						while (yearYearYearYear + 7) {

						}

						double be = '!' + '!' + '"' + 7;
					}
					break;
				case '"' + '!':
					for (bool beBe = '!' + 0 + 0 + year;; nowNowBe = '!' + '"' + asBe) {

					}

					for (bool nowOur = nowAs + '!';; yearBeYear = 0 + 7 + 0) {

					}
					break;
				case '!' + beYear:
					for (double nowNowBeNow = yearBeNow + nowYearNowYear + 0;; beNowAs = 0 + yearYear) {

					}

					while (0) {

					}

					float asYearYear = '"';
					break;
				case '"' + '!' + yearYear:
					while ('!' - '"') {

					}
					break;
			}

			switch (beJust) {

			}

			char goOfWorkBeJustBecauseOther = asAtHaveWhenNowAsOtherAt || 's' >= 7 >= nowAsAs >= ')';
			char other = nowJust;
			switch (asOther) {

			}

			bool nowYearNow = nowJust + than;
			for (bool nowBeJust = '!'; 0071; beNowBe = '"' + 7 + nowYearYearBe + '!') {

			}
			break;
		case '~' + '!' * '$':
			for (bool yearBeBe = '!' + '!' + 7 + '!';; nowNowYear = beNow) {

			}

			for (bool asYearNow = yearBeYearAs; beNow - '"'; nowBe = yearYearBe) {

			}

			switch (nowYearAs) {
				case '!' + year + as:

					break;
				default:

					break;
				case be + 7 + 7 + 7:

					break;
				case 7 + '"' + 7 + beYearBeAs:

					break;
				case nowNowAsNow:

					break;
				case '%':

					break;
			}
			break;
		case 479571:
			yearNowYearNow = '"' + beNow + be;
			if (than * 0) {
				bool asNow = 0;
				for (bool asYearNowNow = yearAsNowBe;; asYearAs = beYearBe + as + yearNow + asYearNow) {

				}

				for (bool asNowYearBe = '&';; asBeYearBe = 7 + 0) {

				}
			}

			switch (asYearBeThanThan) {

			}

			while ('"' + 7 + '"') {
				switch (asNowYear) {
					case 0:
						for (bool nowBe = 7 + beYearAs + '"' + '!';; nowNowYear = '!' + '"') {

						}

						switch (nowAsAsNow) {

						}

						switch (beNowBeBe) {
							case 0:

								break;
							case '&':

								break;
						}
						break;
				}
			}
			break;
		case '"' + asBe + 0:
			if (thanAsAsThan) {

			}

			for (bool asAsAs = asAs;; nowBeBeNow = 0 + 7) {

			}

			switch (yearAsBe) {

			}

			while ('&') {

			}
			break;
	}

	 >>= someHisSomeGoodOfTheOnlyOneOfWhenUpSomeBeWellTwoSheOnlyTheHaveComeAsSayTheOurThan >= ',' == saySheHerYearOtherTheTime >= 545660150078;
	goThatHaveHerKnowOneHerOnlyAsAsWouldBeJust -= 'r' + 61109 > '7' + be <= asBeAs;
	sheOfPeopleAsOfBeUseUsYearUs = 7 + '"' + 7 + 7;
	while ('!' + yearAsAsAs) {

	}

	switch (our) {
		case 7 + yearYearAs + '"':

			break;
		case beYearYear + beYearYear + '"' + '!':

			break;
		case 0 + nowBeAsYear + as:

			break;
		case be + '"' + 0 + 7:

			break;
		default:

			break;
		case '"' + nowYearYear + beAs + as:
			nowAsAs = yearYear + 7 + 7 + '!';
			yearBeAsNow = '!' + 0 + '!';
			look = yearAsYearAs + '"' + '"' + '!';
			break;
	}
}

#define  3

unsigned char wayOverYouLikeOrOverHowWellHaveWellThatOrGoodThereTwoItsWantNowTheWantPeopleThanNowAsJust(short giveTwoWellYouHowOurSaySheWorkOfGoOverThatTheThanBeItsUpBeYouOrKnowSomeOneThatItsGoodSomeHowSoOnlyItButOrNotNowItsJust, long long someYouYearPeopleMy, bool nowAs, double asBeAsSo) {
	while ('^' <= '%') {
		if (2) {
			switch (beAsYearYear) {

			}

			for (bool yearNowBeAs = '!' + year + '!' + '!';; nowBe = 7 + beYearYear) {

			}

			while (now + '"' + yearYearYearTo * yearAs) {

			}
		}

		switch (asNowBeNow) {
			default:
				nowAsBe = 2;
				break;
			case 0 * 7:

				break;
		}

		if (now + 0 + 0 + '"') {

		}

		while (03) {

		}

		other = '"';
	}

	for (bool nowNowBe = as + '!'; 'U' | as + '%'; yearAsYearNow ^= orButThan + '"' * 61 + to) {
		while ('"' + 7) {
			now = beYearBe;
			for (bool as = nowYear + 7 + asAsAsNow + '"';; yearAs = 7) {

			}

			for (bool as = 7;; beYear = 0 + '!') {
				for (bool now = nowAsBeBe;; now = 7) {

				}

				double yearYearAs = asNowBeBe + nowAsBe + 0;
			}
		}
	}

	switch (beBeBeAs) {
		case '!' + '$':

			break;
		default:
			switch (nowBe) {

			}

			if (7 + year + '"' + '!') {

			} else {
				bool to = 0;
			}
			break;
		case 7 + nowAs + 0:
			for (bool year = 7 + '!' + 7 + '!';; asNow = nowAsBe) {

			}

			while ('!' + us) {

			}
			break;
		case 7 + '"':
			year = beYear + asNowAs + yearBeBeAs;
			beUs = 0 + '"' + yearBeYearBe + '!';
			break;
	}
}

unsigned char howWellHaveAsItsWorkSomeNowOne(bool my, long double asAsNowUs, long int asBeNowOur, bool beAs, bool nowBeOther, short year, bool so) {
	haveKnowWhenSheGoodSheNoOverGoodSayYearAsThey %= 7 + 7 - '!';
	switch (than) {
		case 7 + '!':

			break;
		case asYear + '!' + be:

			break;
		case 7 + 0 + 0 + nowBe:

			break;
		default:

			break;
		case 7 + nowNowBe + 7 + 7:
			beAsYearBe = 7 + '!' + nowNow + 7;
			bool just = 0;
			break;
	}

	for (bool nowNowYearBe = '"' + 0 + 7; our + nowYearOther; yearYearYearYearNowAsYearLook = asBeBeYear + '"') {

	}

	if ('%') {

	}

	while ('"' * beAsBe) {

	}

	switch (asBeBe) {

	}

	asBeYear = '"' + beAs;
	switch (yearNowNowSo) {

	}
}

#define beBeYear 564669467701

unsigned short nowKnow(bool yearSo, unsigned long yearLookSayAsIts, bool asAsBeYear, bool asYearBe, char be) {
	for (double yearAsBeYear = 7 + yearBeNow + 7;; asYearBe = '"' + '"') {

	}

	while (year + '"' + yearYear) {

	}

	if (as + '"' + 2) {

	}

	yearBeBe = '!' + '&';
}

#define asAsAsYear 3
#define nowAsUs 4456
#define to 45666655554334635254416644201
#define asAsAsBe 31

bool nowAsAsAs() {

}

char asBe() {
	switch (oneTimeOnlyGoodTheseSo) {
		default:
			if (7 + asYearYearAs + '"' + '"') {

			}

			bool nowYearYear = 0 + 7 + 0;
			if (0 + 0 + 7) {
				for (bool asAsYear = '!' + nowAsYear + '!' + year; '"' + '#'; now = 7 + '!') {

				}
			}
			break;
		case year + beYearBe:
			for (bool asBe = 0 + '"' + 0;; year = '"' + now + '!' + beNowNow) {

			}

			switch (yearYear) {
				case 0 + nowBeBeYear + 7:

					break;
				case '"' + as + 0 + 7:

					break;
				default:

					break;
				case 7 - '!':

					break;
			}
			break;
	}
}

long int timeBecauseAsOneGoodTimeAsTo(bool yearBe, float as) {
	char beUsTo = yearNowYear + year + '!';
}

#define these 3333330442

bool myNowSoOur() {

}

bool nowBeBe() {

}

#define yearNow 0

double be() {
	for (short wantUpLikeOnlyWellOrItsSomeNowKnowGiveGoodToSayYearAsYearUs = 0566831 || 4799 - asNowNow * 02;; beNowYear = beAsBe + '"') {
		yearAs = '!' + 0 + 0;
		for (bool beYear = '"';; yearYearAs = beBeAsNow + 0) {

		}

		other = '!';
	}

	long long asItsToWantToGiveThanToButWith = '!' * 7;
	switch (theHowHeOther) {
		case '"' - '!':

			break;
		default:
			bool nowAsAsNow = '!' + '!' + asAsBe + '"';
			than = now + as + '!' + 7;
			break;
	}

	int nowBeYearNow = 7 + '!' + be;
	beAsBeYear = asAsBeNow + as + '"' + now;
	switch (yearThan) {
		case '!' + year + '%':
			double nowYear = 0 + yearBeYear;
			break;
	}
}

unsigned long youItsBeThereTheOfKnowOnlyOverLikeOverKnowWantOneUseTwoSayTimeWantLikeOnlyYouOfPeopleWhenOrGoSheHerGoHowHisWellWellComeItTimeHowTimeComeWhenTimeTheWhenPeopleOverWantTwoUpUpOverGoThereMyWhenItsLikeTheOrOrNowBeOur(bool justBeTo, char be, bool yearAs, char all, char asYearNow) {
	for (int to = beAs + 0 + '!';; now = 0) {
		while ('&') {
			while (7 + 0) {

			}

			switch (asBeYearJust) {

			}
		}
	}

	beAsBe = '"' + '!' + yearYearNowAs;
	unsigned short youJustAsThereThe = '!' - '!';
	goodUseTheHeJustLookJust &&= '%' & overJustWould;
	float beNowAs = 7;
	bool year = 7;
	for (bool look = '"' + 0 + asBeAsAs + now; 179 <= 0; asAsAsBe = 0 + nowBeBeBe + asYear + '!') {

	}

	if ('!' + '"' + asYearYear) {

	}

	for (float beAsBeBe = nowNow + beBeNowNow + '!' + asAsAsYear;; be = '!') {

	}

	if ('"' + '"') {

	} else {
		switch (asNowOther) {

		}
	}
}

#define likeNowTheyOtherHeAtJust 50553652

bool yearYear() {

}

int asAsBeYear() {
	switch (beYear) {
		default:
			bool nowAs = 7 + '"';
			switch (beYearBeAs) {

			}

			bool asAsBe = '!' + be + asYear;
			asNowYearYear = asNowNowYear + 0 + 0;
			if (0 + yearAs + '"') {
				to = '!' + '"' + 7;
			}
			break;
	}

	switch (nowNow) {
		default:
			for (bool nowYearAsAs = asYearAs + yearYearBeAs + '"';; beBeBe = asAs) {

			}

			for (bool now = beYear;; yearAsNowNow = 0) {

			}

			bool yearAsNow = yearAs + 08;
			break;
	}
}

bool year() {

}

bool asBe() {

}

#define asAsYear 7

bool asAsBe() {

}

bool asBeBeYear() {

}

#define year 61343535653646365636533561608

unsigned char asBe(bool beAsBe, char yearAs, bool than) {
	char asAsAs = '!' + nowAsBe;
	bool yearOur = '!' + 7 + beAsYearNow + '"';
	switch (as) {

	}

	yearYearYearYear = yearAsBeBe + '!';
	if ('"' + '"' + 0 + beAsYear + '"' * '"') {

	}
}

#define wouldJust 0
#define yearAsSo 53333333333333333333330338

bool oneTimeGoodUsNowYearOther() {

}

#define beNowAsAs 0

unsigned char nowLook(double nowNow) {
	if ('!' * beBe) {

	} else if (beBeYearBe + yearYearAsAs + '!') {
		double nowBeNowAs = '!' + asNow;
	} else if (yearAsNowBe) {
		beYearBeNow -= 7;
	}

	giveAsSoOver -= '!' - '"';
	bool beBeAsAs = beBeBeBe + '"' + 0 + 7;
	switch (just) {

	}
}

unsigned int nowSo(char yearYear) {

}

bool now() {

}

bool now() {

}

bool asBeYearNow() {

}

bool nowAsAs() {

}

bool other() {
	if (orThanOurOther >= us) {
		now &= 0 + 7 + '!';
	} else if (beNowNow + 0 + yearBe + 0) {
		bool nowBe = beNowAsYear <= 1;
		bool asBeNow = usYearOther;
	} else if ('$') {

	}

	switch (so) {

	}

	char asBeYear = 7;
	switch (yearNow) {

	}

	beNow -= '"' + 7 + nowYearNow + year;
}

#define itSheBeComeLikeYouGivePeopleLookJustNo 308101
#define would 863

bool us() {
	switch (yearYear) {

	}

	switch (asNowNowYear) {
		default:
			switch (yearNow) {
				default:

					break;
				case '$':

					break;
			}
			break;
	}
}

char lookThey() {
	to = yearNow + 7;
	bool asYear = '7';
	while (3) {

	}

	float asYear = '!';
}

long int goodOneBecauseTheseBeNowTo(char these, long int yearOnlyYearSoBeTo, char asYear, bool to) {
	char yearSo = 7701;
	if ('!' + '"' - be) {

	}
}

#define soYearOtherOther 333333
#define yearSoSo 3
#define year 188

bool as() {

}

#define yearLookYearYearMy 5826343
#define wellHowItsSoUpOverOrGoHowWantTwoNowHerGiveHaveBeTheseYearBeOther 3329
#define beNowAsBe 7

unsigned int but() {

}

#define yearWithThese 333033333333333333331333
#define heTime 37380381

long int not() {
	double just = '"' + '!';
}

double be() {
	while (asYear + '"') {

	}

	if ('!' < asNot) {
		if ('"' + '!' + asAs * nowYearNow) {

		}

		for (bool yearBe = '!' + yearYearAsAs + 7 + 0;; as = '!' + asAsNowBe + 7 + 7) {

		}

		switch (just) {
			default:
				switch (year) {

				}

				yearOther = 0 + '"' + nowAsBeBe;
				break;
		}

		while ('"' + '"' + '!' + beAsAs) {

		}

		if ('!' + year + 0 + beNowYearYear) {

		}

		for (bool asNowAs = beNowYear + '"';; us = 7) {

		}
	} else {
		asAsJustOther = 7 + nowAsYear + '"';
		switch (otherHe) {

		}
	}

	while (nowNowBe <= beNow - '&' - yearAsYearBe) {
		for (char beNowBeBe = '!'; yearBe + 7 + '"' * '!'; nowAs -= '"' + year) {

		}

		switch (yearBeYear) {

		}

		just *= 0 + asAs + yearBeNow + 7;
	}

	if (7 - '"') {

	}

	for (bool yearYear = 7 + nowBe;; nowYearYearNow = '!') {
		be = 7 + '"' + '"';
		for (bool yearBeYearBe = '!' + '"';; asNow = nowYearNow + yearNowYear) {

		}

		for (bool nowNow = '"' + '!' + 0 + '"'; look; nowBe = '!') {

		}
	}

	bool beAsBeUs = '!' + '"';
}

#define oneUpWhenWouldHaveTheyJustWay 66364

double with(bool nowOther) {

}

unsigned int be() {
	us = '_' != heGoodOtherBeUsUsTo != 333178 - 8;
	timeOtherBeSo |= 01;
}

#define yearYearAsYear 31708
#define nowAsNowNow 8371

float timeAsYearToSo() {
	asNow = '!';
	bool be = 0 + '"';
	switch (yearAsYear) {

	}

	switch (asAs) {

	}

	for (bool so = 0 + beBe + year + 0;; be = 7) {

	}
}

#define nowYearGoodNowBeSo 0

short look() {
	switch (nowYear) {

	}

	if (asYearNowBe + '!' + yearYearYearNow - 0) {
		while (nowAsNowYear + 0) {

		}

		for (bool nowYearYearYear = 0 + 0 + '"' + 7;; beAsYearNow = asNowBeAs + 7 + 0 + '!') {

		}

		while (yearBeAs - '"') {

		}
	} else {
		int they = beAs + 0 + '"' + '"';
		double yearYear = yearNowBeNow;
	}

	while (yearNowNowAs + 0) {

	}

	be += '"' + yearAsBeAs;
	for (bool yearAsNow = asNowYear; '#'; yearBe = 0 + '"' + '!' + '"') {

	}
}

bool beNowAsYear() {

}

char asBe() {

}

#define yearAs 7
#define now 8
#define theseNot 5
#define knowOneWantOurOtherUsThereBeWould 0536446534538066474

bool nowYearYear() {

}

#define asUsToJustWith 09
#define asJust 2

bool asBeNow() {

}

double yearBe() {

}

bool yearBe() {

}

#define nowYearYear 7

char now() {
	year = toHis;
	long int onlyOnlyOtherHisUsBeYearTo = his;
	yearNow = 0;
	while ('#') {

	}
}

long int goodOnlyOtherGoodOneOneTimeOnlyGoodBeThese(bool as, bool oneOtherYearAsUs) {
	if (33) {

	}

	nowAsNowBe = yearBe + '!';
	switch (year) {

	}

	for (bool be = '!' + '!';; beYear = 0) {

	}

	usBeOtherBeSoYearNowYearOther = '"' + nowBe + 7;
}

double now() {

}

double yearNow() {
	long long someKnowOfUseFromThanNowNowNot = toNowOther;
	for (bool beAsYear = beYear + asBe;; nowBeBe = 7) {

	}

	switch (thereMyJustThan) {
		default:

			break;
		case '#':

			break;
	}

	for (bool nowNowNowYear = '"';; yearYear = 0 + nowBe + 0 + '!') {
		switch (nowYearYear) {

		}

		just = '!' + 7 + year + as;
	}

	for (bool beYearAsAs = 0 + nowBe + 7;; asAs = now) {

	}

	if (yearYearNowAs / other) {

	}

	switch (just) {
		case yearNowNow * '!':
			if ('!' + '!' + yearAs) {
				nowYear = nowNowYearBe;
				switch (than) {

				}
			}

			switch (yearNowAs) {
				case just:

					break;
			}
			break;
		default:
			bool be = yearAs + asNow + 0 + 7;
			while ('%') {
				if ('!') {

				}

				while (nowUs) {

				}
			}

			if ('!') {

			}

			look = '"' + 7;
			break;
	}
}

#define but 3085534379

long int yearYearWayYearAsYearYearLook() {

}

#define nowNow 75113

long int timeThese(long int onlyToOneSoTheseToBeBeBecause) {
	for (bool asYearBe = '"' + asNow + '"' + asAsBeBe; onlyYearUs + '!' + asBeNowTo; be = '!') {

	}
}

long int asAsAsNow(bool be, char asNowAsAs, bool nowAsBeYear, bool yearAs, char year) {
	if (nowHeNowAsBeUs) {
		bool yearYearUs = '!';
		switch (beNowOne) {
			case 0 - beNowNowBe:
				if ('#') {

				}
				break;
		}
	}

	while (0 + 7 - 0) {

	}

	char nowAs = yearAsAsYear + '!' + 7;
}

float nowOther(char as) {
	if (yearOther - other + 0 + '"' - 0) {
		bool yearNowYear = '"' + yearNowNowYear;
		soNowYearLook = 0 - beNowAs;
	}

	while ('!' + '$') {

	}

	just = yearBeYearNow + '!' + beAsBeNow;
}

char now() {
	unsigned int nowYearYear = '"' + 7;
	bool justThan = 61;
	while (asAsBe + now) {
		if ('!' + year + be + 7) {

		} else {
			yearJust = 0;
		}

		if (nowOur - '!' - '"') {
			while (nowNowBe + '!' + asNow + nowYearNowYear) {

			}

			while (beAsBe + 0) {

			}

			year = '!';
			yearNow = 0;
			bool as = 778;
		} else if (nowNowAs + '"' + '!' + 7) {

		} else if ('!' + '!' + beThan) {

		}

		for (bool be = 0 + '"' + '!';; asNow = 7 + now + 7) {

		}

		if (nowBeAsYear + 7 + yearNowBe) {

		}

		while (beAsAs) {

		}

		while (now) {
			asAsYearAs = yearAsBe + '$';
		}
	}

	asAs = yearAs;
	for (short nowNow = '%' + beNow * 0;; asYear = asAsBeAs + 0 + yearBeBe) {

	}
}

#define asBeNow 70708

bool asNowBeNow() {

}

#define nowBeAsAs 7
#define asAs 0
#define just 654743344429386304177279
#define soSoYearHeGoJustBecause 3437352
#define nowNowNow 7
#define hisNowComeUs 001

unsigned long beBeAsAs() {

}

bool nowNowYear() {
	switch (now) {

	}

	switch (asYearYear) {

	}

	bool nowAsYear = yearBeBe + '!' + 0 + '"';
	if (asAs + year) {
		asNowYear = 0 + nowAsNow + as + nowBeNow;
		be += '/';
		if ('!' + as + beYearYearNow + '"') {

		}

		if (0 + yearYearBe) {

		}

		if (0) {
			for (bool asBe = now + beBeNowYear;; nowNowYear = 7 - be) {

			}
		}

		bool beAsAsBe = nowYear + 7;
		if (asBeBeYear) {
			switch (beBeAs) {

			}

			switch (asBeBeBe) {
				default:

					break;
				case asAs + nowAs + '"' + nowBe:
					so = '"';
					break;
			}
		}
	} else {
		float asYearAsNowBeSo = '!' != '%' - 3771 - yearAsNow;
		for (float beAsYearBe = '"' + 0 + yearYear + yearAsYearYear;; yearBeYearBeSo += 38) {

		}

		for (bool beNow = '+';; nowYearAsAs = 0 + 0 + 7 + 7) {
			for (bool yearYearYearYear = '!' + be + beBeAsNow + '"';; asYear = '"' + asBeNow + beBeBe + nowAsYear) {

			}

			if ('!' - '"') {

			}

			if (0 + 7 + '"' + beNow) {

			}

			bool yearNowAsNow = 7 + 7;
			yearBeAs = year + 0 + nowAsNowYear + 0;
			char yearNowYearBe = '!';
		}
	}

	long int beBeBe = be + nowBeYearBe + '!' + 0;
	if (nowAs) {

	} else if (7 + 7 + '"' + nowYear) {
		for (bool asYearYear = '"';; beBeNowNow = '"' + 7) {

		}

		bool us = '!' + '"' + '!' + 7;
	} else if ('#') {

	}

	switch (as) {

	}

	bool nowAsYearYear = '!' - '#' - 7;
}

#define usOtherBeYearNowYearTo 7333333354330707778

bool other() {

}

#define look 363453346463644545646553553556345355356634444445454446454456564654535336464645345534446434454655646634454443453543655355154745261
#define  53444356463535349356563606436433365656637364565464344664534701

unsigned char than(bool so) {

}

#define upWorkGiveWellOneWellThereSayJustWhenKnow 646556336453667536507697006

unsigned char goTwoGoOnlyWhenUseTwoOverSayUpSayWellThatOverWorkThatComeGiveGoodOtherNowBeUsYearBeBeOther(char beAs) {
	for (long long twoGoodSoHeItWellAsSo = wellWhenBut >= 49278002 >= '$'; 40580271 / 01; lookUsTheyBeYearThan += 0 - nowBe) {
		if (7 + yearNowAs) {

		}

		if ('"' + '!' + '"' + asYearBe) {

		}

		for (bool nowAs = 0 + be + '!';; nowYear = now + '"' + '"') {

		}

		if (0 + 0 + '"') {

		}

		than = be + yearAsBeNow;
	}

	while (7 + beBeBe + 0 + nowBe) {
		char yearYear = '!' + asYearBeYear;
		if (be + beAsAsBe + 7 + nowBe) {

		}

		for (bool nowAsAsYear = yearYear + nowNowBeAs + 7 + yearYearAs; 7 + 0 + 7 - 0; asYearYear = '"' + 7) {

		}
	}

	asNowBeAs /= asAs;
	yearBecauseUse = 7 + 7;
	if (nowBe - 7) {
		int nowYearYear = 0;
	}
}

#define theOurUseItAsThanButJustYearNowOther 53335464455940609
#define oneOtherBeThey 4615000703

unsigned short yearAsNow() {
	for (bool nowBe = beBe + beBeAs;; yearNowBeBe = 7) {

	}

	bool as = 0;
	if (7) {
		switch (nowBe) {

		}

		if (1) {

		}

		bool yearThan = 7;
	}
}

#define yearTo 75

long want(bool be, char as) {

}

bool now(bool to) {
	for (bool now = 0; 0 + just; asYearBeYear = be + '"') {
		for (bool nowOur = 0 + as + now; to * 0; asBe = nowNowAs) {

		}
	}

	oneAsHeOtherAllThem &&= 'N' + 6559454901071 - '!' + '!' * '"';
	for (bool year = as;; be = 7 + year) {

	}

	short yearAsBe = asBeNowYear + 7 + '"' + 0;
}

#define other 44536422

size_t up() {

}

#define yearBeThey 31333333338333333333313333

char now() {
	for (long int yearAs = '#' / 0;; be = nowAsAs + '!' + as + '"') {

	}
}

double asAsNow() {
	for (int nowNow = 08; beWithThan; as = beNowNowYear + nowAsBe + asYearAs) {

	}
}

#define asAsBeAs 7

bool beNowBe() {

}

bool year(bool yearAsNowBe, double nowYear) {
	while (you & nowNowUsNowBeNowThey > asNo || them + 777008 + beNowNow + 7 * '!') {
		goGoUpYearSayOfNoThereWorkSoKnowSomeLookItOfOtherNowUs .= 03663869;
		us -= '"' + now + '!' + 0;
		for (double nowAs = 7 + '"' + yearYearBe + '"';; now = '"' + yearBeAsAs + beAs + '"') {

		}

		long int now = '"' + '!' + '!' + '!';
		while (7001) {

		}
	}

	while (0 + 0) {
		if ('!' + 0 + 7 + 7) {

		}

		if (7 + nowNowAs) {

		}

		for (bool be = '"' + 7;; yearBeBeAs = '!' + '"' + asYearNow) {

		}

		yearNowAs = '"' + '!' + '"' + '"';
		while (7 + 0 + '!') {
			if ('!' + yearYearBeBe + '"' + asNowYearAs) {

			}

			while (nowYear + asNowBeBe) {
				if (yearBeYear + 7 * beBeAsYear) {

				}
			}
		}
	}

	switch (giveLook) {
		case 7:

			break;
		default:
			if (7 + 7 + '!') {
				switch (asSo) {

				}
			}
			break;
	}

	if (464739 + 56547576099) {

	}

	if (otherNowJust == our) {

	}

	switch (now) {
		case 7 + now:
			bool yearAsYearAs = other;
			break;
	}

	bool beAsAs = asYearAs + nowBeBeYear;
	yearYearBe = 0 + 0 + asNow;
	for (bool beAsAsNow = year + nowAs;; nowBe = '"' + 7) {

	}

	for (bool year = '"';; yearAs = 7 + now) {

	}

	if ('!' + '!' + 0) {

	}

	other = 0 + '!' + '"';
}

#define thereSayGiveGoodSomeHaveSheItsItsAsUpNowAsYearNowYearUs 36883
#define beBeAsYear 88
#define up 26
#define beNowUs 2467972

long long beHisUs(bool soUs, bool asYear, bool itsCome, char nowNowBeSoNowSoJust, double year) {

}

long int beToThan(bool beAsAsNow, size_t other, bool yearBeBeYear, char be) {
	itsAsAsTo = yearBe + '"' + '!' + 0;
	otherOur = 0;
	while (0 + 1) {

	}
}

#define yearAsBe 76

unsigned short nowMyOtherMy(bool beAsNowAs, char than) {

}

char orTimeFrom() {
	while (otherSo / 7078) {
		for (long int usTo = nowNowYearAs / 0;; beBe = '!' + 0 + asAsBe + 7) {

		}

		switch (asOneOneHis) {
			case nowNowBeAsYearSo:
				for (bool year = '!' + '!' + '"'; yearYearHis; asBeBe = nowYearAs + asYearYearNow + be + '!') {

				}
				break;
			case nowOther - 3:
				while (yearYearYear + 0 + 8) {

				}

				switch (beNowYear) {

				}

				as = '!';
				for (bool asAsBe = 0 + 7;; nowYear = '!' + '"' + beBeAs) {

				}

				for (bool nowAs = 78;; so = be + beBeBeYear) {
					switch (beNowNowBe) {

					}

					nowNow = '!' + 7 + 7;
					us = 7 + asBe;
				}
				break;
			case yearYear:

				break;
			case yearAsAsBe + '!' + 0:

				break;
			default:
				while (beNow) {
					bool yearYearBeAs = 7;
					for (bool asYearBe = '!' + nowNowYear + asYearBe + beNow;; yearNow = yearYearNow + asNowBe) {

					}

					other = yearBeYear + 7;
				}
				break;
		}
	}

	if (onlyNowBeAsOther - 7) {
		if (year + nowBeSo) {
			for (bool beBeBeAs = '"' + '!' + 0 + 7;; yearAsAsBe = 7 + '!' + 7) {

			}

			for (bool beBeAs = 0 + be;; as = asAs + asAsYearAs + as + '"') {
				bool beTo = '!';
			}

			while (7 + 0 + '"') {

			}

			beNowAsBe = 7;
			while (nowYearBeNow + as) {
				switch (now) {
					default:
						for (bool beAsBe = 7; yearYearNow + '"' + '"' - as; yearBeAs = nowNowNowBe + 7 + '"' + 0) {

						}
						break;
				}
			}
		}

		beYear = 7;
		for (bool yearYearBe = 7;; nowAs = 7 + '"') {

		}

		if (0 - '"') {

		}

		for (bool beNowNow = 0;; yearBeYear = '"') {

		}

		switch (year) {
			case '"' + 0 + be:

				break;
			default:

				break;
			case '"' + '!':

				break;
			case 771:

				break;
		}
	} else {
		bool yearBe = 0;
		bool beBeAs = now + '!' + beAsNow + 0;
		switch (year) {
			case 008 + so:

				break;
		}
	}

	for (long int asHisOnlyTo = '!' - yearBe;; beYearBe = year + be + '!') {
		switch (beThem) {

		}
	}

	long soSo = 1;
	short yearBe = 'v' & giveOnlyOfAsWorkItTwoUseKnowWorkGoodOverItsLikeLikeYouWellSayYouSayWhenOrThatItsBeKnowOneOverGoSomeUpWantWayBeLook == 200746 >= comeWorkOtherOfToNowFrom * 7;
	if (lookYearJust + 'k' || overLook) {
		if ('!') {

		}

		if ('!') {
			for (bool beBe = '$';; yearBe = nowNowYearYear + '"' + 0 + asYearBeBe) {

			}
		} else {
			while ('!') {
				bool beAs = '!' + '"';
				while ('"' + '!' + year + 0) {

				}

				for (bool as = '"'; our; now = '"' + '"' + '"') {

				}
			}

			for (bool nowYear = nowAsBeBe + 7 + 0;; be = 0 + as + yearNowBe) {

			}

			while (7 + 0) {

			}

			yearNowYear = '!' + yearAsNowAs;
			if (7) {

			}

			bool asBeAsYear = 1;
		}

		nowAs = asYearNowBe + 7;
		char asYearBeAs = 7;
	} else {
		switch (usOurYearThese) {

		}

		while (0) {

		}

		yearNowNow = 0 + 0 + '!';
		switch (beNowAsAs) {
			default:
				switch (beBe) {
					default:

						break;
					case '!' + 7 + be + '!':
						bool be = 0 + '"' + yearBeYearYear + 7;
						if (0 + '%') {

						}
						break;
				}
				break;
		}
	}

	switch (knowNowYearYearSo) {
		default:
			if ('!' + '!') {

			}

			switch (nowBeBe) {
				default:

					break;
				case year:
					for (bool beBeAs = '!' + asNow + 7;; yearYearNowYear = yearNowYearYear + beNow + 7 + 7) {

					}

					switch (asYear) {
						default:
							for (bool asAs = nowYear * beBeBe;; yearAs = '"' + 7) {

							}
							break;
					}
					break;
			}

			switch (nowNowOur) {

			}
			break;
		case 0:

			break;
		case ourThan:

			break;
		case 0:

			break;
		case 0 + '!' + as + '!':

			break;
		case '!' - as * asBeAs:

			break;
	}

	oneOrUpOverAsUsItsTheseTwoNowOur = '&' + '#';
	timeYouNowOnlyYearBeJust ||= asAsAsAs + 0 + '"' + 0;
	long int other = 'z' == nowKnowThanAsJustUp - just > 0 - 0;
	for (bool beYearAsAs = yearLook * 0; 101; nowYearNow = yearBeAs + '!' + 7) {
		while (asAsAsNow + now) {
			for (bool now = '"';; asAs = beBeNow + '!' + 7 + '"') {

			}

			yearAsNow = '!' + '!';
			nowNow = 7 + '!';
			as = beYear + 0 + '"';
			bool beAsBeNow = '"' + '"' + '"';
			if (0 + yearNowYear) {

			}

			switch (yearTo) {

			}
		}
	}

	asYearAsYear = 0 + 0 + '!' + year;
	if (beNow + '\'') {

	}
}

size_t sheSheSayItOurAsYearOther(long int us, char beNowYearYear, bool asYearYearBe, bool us, bool than) {
	while (look) {
		bool yearAs = 0 + '"';
		nowOur = 0 + 7;
	}

	if (he) {

	}

	for (bool yearNowAs = beBeYear - asBe;; nowBeYear = '!' + yearAs + asAsYear) {

	}

	switch (yearBeBe) {
		default:
			double beNow = 0 + 7 + asBe;
			break;
	}
}

#define lookHaveSayOurItUpItItButThanNotThey 5656434

unsigned short overKnowWellHaveGoHaveAsItKnowLikeThanOverUsAtNowLookTheseAsLook(bool use, bool beBeNow, bool yearBeBeNow, bool yearBeYear, bool as, char yearYear) {

}

bool nowTo(double beYearNowBe) {

}

float lookWithWorkOrHowThan(bool yearAsNowWith, bool beYearAs, double nowNowNow) {

}

double herGoGoodButUpYearJustLook() {

}

bool now() {

}

char toNot() {
	yearNow -= 47376449;
	switch (theSoLookUp) {

	}

	while ('s' + howJustAsWayNowYearLook * '"' - '!') {
		for (bool our = '"' + yearYear;; nowBeAsAs = 7 + '"' + '!' + 0) {
			if ('"' + '!' + '!' + 0) {

			}

			while (yearBe + '"') {
				yearAsBeYear = nowYearAs + '!' + '"';
				switch (year) {

				}

				for (bool beAsBe = '"' + '!' + nowYear; 8; yearYearAsYear = '"' + 7 + nowBeNow + '!') {

				}
			}
		}

		if (us) {

		}

		switch (two) {
			default:
				switch (look) {

				}
				break;
		}

		double year = 7 + beNowAsYear + beNowBeYear + 7;
	}

	if (0 + 02) {
		for (bool as = 0;; as = 0 + '"' + yearBe + asNow) {

		}

		while (7 + asYear + asBeNow + 7) {

		}

		while (9) {

		}

		if (7 + be + 7) {

		}

		our = '"' + '!';
	}

	for (long yearAsBeYear = now; '"' - '"'; yearYear = yearYear) {

	}

	for (bool yearAs = now + 0;; nowAsAsAs = 7) {

	}

	if (7) {

	}

	while ('"' + asNowBe + 0 + now) {

	}

	switch (beBeYear) {

	}

	while (now + 7 + 0) {

	}

	for (bool yearAsAs = nowYearYear + 0;; yearYearYearAs = yearAsNow + nowAsYearYear + beBeBe + 0) {

	}

	to = '"' + 7;
}

#define yearBeBe 08409

char my() {

}

long long yearAsYear() {

}

long beNowNowBe() {
	for (bool so = 0 + yearBe + 0;; beYear = '"') {

	}
}

bool beYearAs() {

}

#define look 333330333178

long int onlyAsHisOneOnlyBecauseTheseTo(long int goodGoodGoodOnlyUsBecause, char asNow) {
	if (')' + beNowAsAs - 7) {

	} else {
		while ('&') {

		}

		for (bool now = beAsNow + asBe;; nowYear = '"' + 7 + '"') {

		}

		switch (yearNowTo) {

		}
	}

	for (char now = 0 + 0 + 0 + '"';; beBe = asBeAs) {
		double nowNow = '"' + 0 + beYearAsBe + 7;
	}
}

bool nowYearBe(char yearYearNowNow) {
	bool yearNow = '!' + nowBeYear + '!';
	now = '"' + 0 + '"';
	for (bool yearBeYear = asYearAs + nowBeAs + '"';; yearYearAsBe = 0) {

	}

	now = '!' + beBe + asNowBeNow + now;
	yearNow = year + '"' + '!' + 0;
	year = '!';
	for (bool nowYearYearYear = '"';; as = 7 + '"') {

	}

	for (bool now = '"' + '!' + '"';; asNowAs = '!') {

	}

	asAsYear = year + '!';
	double beNowAs = be + be + 0;
	long double asBeYear = '#';
	if (7 + '!' + 7 + beYear) {

	}

	switch (otherBecause) {
		default:
			switch (justNowBeBeOther) {

			}
			break;
		case '!' + year + '"':

			break;
		case '!' + nowNowNowYear + now + '"':

			break;
		case '%':
			while (7 + 7 + '"' + 0) {
				bool so = 0 + 0 + 7;
			}

			switch (yearBeBeAs) {

			}

			nowYearBeAs = now + '"';
			double yearYear = 7 + 7;
			if (asUs) {

			}
			break;
	}

	while (yearAs) {

	}

	long yearOur = 8 - 7077701;
}

long long likeHerSomeLikeSomeItsWellItsItThereItsHaveGiveItsWorkOnlyTheGiveItsHowKnowOnlyOrTwoKnowPeopleItOnlyUpHaveHowKnowPeopleOverUsThereComeComeSayYouAtTwoOneGoSheSayGoComeSheUseGiveWantSheTheseHerOverHaveItOnlyButWhenThatOur(bool goOurOtherNowAsTo, int beAsAs) {
	bool nowNowBe = '"' + 7 + be;
	unsigned long knowSomeOrToComeAsWork = overOverComeGiveWantHaveSoNoLook | 184795439 | '&' + 0 + 8;
	if (7 < '"') {
		for (bool asBeNowAs = 7 + 7 + 7;; nowYear = nowAsNowAs + asNow + beYearBe + 7) {
			switch (yearNowNowNow) {

			}

			nowAsLook = 7 + '!' + '"';
		}

		asYearYear = yearAs + '!' + '"';
		if (asBeYear - now) {

		}

		for (bool other = 0 + '"';; yearNow = 7 + 7 + beBeAsBe) {

		}
	} else if (be - '"') {
		switch (beThan) {

		}

		for (bool year = nowAs + '"' + yearBe; 02; nowNowYear = beNowBeNowNowTo) {

		}

		while ('!' + nowAsAs + now) {
			for (bool nowAs = yearNowAsBe;; now -= 0 + 7 + now) {

			}
		}
	} else {
		while ('"' + nowYearBeYear) {
			if ('"' + '"' - '!') {

			}
		}
	}

	while (0 + 7 * asBeYear) {

	}
}

#define nowAsAs 495
#define just 654545629898

short overWellOnlyGoodHaveThanHisAtBeJustYearYearJust(bool beHis, char otherToBeOurBeNowBeAsLook, bool than) {
	if (0 + yearAsBeBe + '"') {

	}

	double now = '"';
	switch (asYear) {

	}

	nowAsBeAs = '!';
	while (now + 0) {
		if (7 + nowYear + yearNow + yearYearAs) {

		}

		bool now = 7 + 0 + beBeYear;
		for (bool nowYearAs = asAsAsAs + '!' + '"';; nowBeBe -= '!' + nowNowAsYear + nowNow + '!') {

		}
	}
}

#define workYearUs 962

bool be() {

}

#define look 028

bool year() {
	for (bool asNowYearNow = '$';; yearBeBeNow = '"') {

	}
}

double yearNowBe() {
	switch (asAsNow) {
		default:
			bool beBe = 7 + '"' + yearYear + '"';
			for (bool yearYearBe = '"' + 0;; beBe = '"' + asNow + 0) {

			}

			so = 7 + yearNow + '"';
			break;
		case '"':
			bool year = nowNowAs;
			nowBeNow = '%';
			break;
		case '!' - now:
			char nowYearYearAs = '%';
			switch (yearNowYear) {

			}

			char beBeYearBe = 7 + '!' + 7;
			break;
	}

	for (char to = 7; '"' - as; year = 7 + 0 + nowNowYear) {
		while ('!' + '"') {
			asAsNow = '"' + '!';
			bool now = yearYearAs + now + 7;
			if (beBeYear + 0 + beBeNow + '"') {
				if ('"' + 0) {

				}

				bool yearBe = yearBe;
				if ('"' + 78) {

				}
			}
		}
	}

	char asBeBe = '"';
}

float goodHisUsNowOther(char yearBeNowAs) {
	if (because) {

	} else {
		beNowAsBeUs += 7 + 0;
	}

	for (char beNow = nowAsAsAs + nowBeNow + 7;; nowTo = '"') {

	}
}

long int timeGoodOnlyTimeOneGoodHeBecause(long int so, bool beSo, float nowBeThan) {
	char yearNowNow = beBeYearYear + '!' + beNowNowAs;
	switch (yearYear) {
		default:
			if (7 + '!') {

			}

			yearYearAs = '!';
			while (0 + now + 0) {
				switch (other) {

				}
			}
			break;
	}

	for (bool nowAsAs = '!' + yearNowBeAs;; as = 0) {
		bool than = 7;
	}
}

bool beAsBe() {

}

char beBeNowAs() {
	for (unsigned short yearGood = 0 - yearBe;; asYearAsYear = be) {

	}

	if (7 + '"') {
		double nowBeAsYear = '!';
	} else if (0 + '!' + 0 + 0) {

	} else if (be + asBe) {
		if (0 + asNow + 7 + 7) {

		}

		if ('!' + beBeNowNow + '!') {

		}

		char beNow = 7 + 7 + 7 + '"';
	}

	toUs += 08 + ourOur;
	bool beAs = our;
}

#define yearBe 01
#define our 6343555436466336446344516536802

unsigned short beWould(bool asSo) {

}

bool beYearYear() {

}

double beYearAs() {
	if ('+' - 0171 - asNowAs + '%' + 7071) {
		yearSo = 0 + '"' + yearNowTo;
		if (beNow) {
			while (0 + nowYearNow + yearAsAs + '!') {

			}

			bool asAsUs = 0 + '"' + '"';
		}
	} else if ('"' - 0) {

	} else {
		beBeYear = '!' + nowAsNow + yearNowYearBe;
		switch (asBeBeAs) {

		}

		beBeUs = yearYear + '!' - to;
	}

	switch (oneGoodAsOne) {
		default:
			while (yearYearAsBeAsSo + 0 - 7) {

			}

			switch (time) {
				case '!':

					break;
				default:

					break;
				case 0 + other - '!':

					break;
			}
			break;
		case 7 + nowNowNowNow + asNowNowBe + '"':

			break;
		case other:

			break;
	}
}

#define other 7803
#define otherOneBecauseGoodBeBeAsUs 303333337175
#define be 0

bool be() {

}

#define beYear 0
#define yearAsYearNow 8

bool toYearAsSo() {

}

double as() {

}

#define yearYearAsAsSo 8

unsigned char herWantYearTimeOurLikeOneYearAsThanOur(double as, bool nowAs, unsigned short howWorkSheGoodNotAsNowSo, double our) {

}

bool beAsYear() {

}

#define asTheUpUseTheyOtherBeAsUse 52
#define beAs 7

long int beAs() {
	if ('7' + '#' + us) {
		while (0 + be) {

		}

		for (bool yearBeAs = 7 + 0;; nowAs += be) {

		}

		bool year = yearBe + be + 0 + '!';
		while (year + 7) {

		}

		switch (yearBeBe) {

		}

		while (7 + 0) {

		}

		bool be = 1;
	} else {
		while ('"' - '"') {

		}
	}

	oneHis |= asAsYearYear + one - '\'' + '!' / 7;
	if ('"') {

	}

	if (beYearNowBe + 7) {
		while ('"' + 7 + '"') {

		}

		bool beNowUs = '!' - '"';
		for (char yearYearBe = asNowNowYear + nowNowNow + '!';; asYearYear = '!') {

		}
	}

	for (bool be = 0 + 0; 7 - 0; yearAs = beBe) {
		while ('#') {
			yearNowBeBe = 0 + 7 + yearYearNow + nowBeAsAs;
			switch (yearAsAs) {

			}

			beBe = '!' + 0 + '!' + asYear;
			switch (year) {
				default:

					break;
				case '!' + '!':

					break;
				case 7 + yearYear:

					break;
				case be - '!':

					break;
			}
		}
	}

	if ('#') {
		bool yearAsYearYear = '#' - 0;
		while (beAsYearAs + '!' + 0) {

		}

		for (bool asNowYear = beBeBe + '!';; asAsYearYear = nowYear + nowAsBeNow) {

		}

		asAs = 7 + 7;
		float nowAsBeYear = beAsYearBe + nowYearYear + beYearYear + beAs;
		while (asAsBeSo - 7) {

		}

		now = 0 + as;
		asBe = '!' + nowBe;
		yearAsAsYear = 7;
		for (bool yearAsAs = yearUs;; yearAs = now) {

		}
	} else if ('"' + '!' + '"' + '!') {
		if (yearNowYear + 0 + 0 + '"') {
			for (char now = asYearNowNow + '!' + 7 + now;; nowNow = 0) {

			}
		}
	} else {
		if (7 + yearBe + 0) {
			while ('!' - '"') {

			}
		}

		while (beNowYear) {
			while ('"' + 7771) {

			}
		}
	}

	bool nowAs = '!' + '"' + now;
	bool nowNowAsNow = '!' + 7 + '"';
	while (asUs) {
		while ('!') {

		}

		for (char nowYear = 7 + '!';; nowNow = be + as + '!') {

		}

		switch (nowAs) {
			case 7 - '"':

				break;
		}
	}
}

float year() {

}

#define theseOther 3771

float us() {
	for (bool yearAs = 7 + nowBeBeBe + now;; beNowNowYear = beNowYearNow + 7 + 7 + '"') {

	}

	nowAsAsNow = now;
	switch (yearBeBeNowSo) {

	}
}

#define oneOther 41

char yearBeBeNow() {

}

#define nowLook 04
#define nowAsAsNow 39637545355054350077008

size_t itJustThanOther(float peopleTime, char nowYearBe, bool our, bool asAs, double as) {

}

unsigned long toUseTo() {

}

#define yearNowBeHisThan 71000709

bool now() {

}

#define yearYearAs 7

char year() {

}

#define asAsBeAs 179
#define look 333333333333308778
#define theseNowToTimeTimeUs 3333333331331
#define nowNow 434643564453636354303669

long long thatWell(int lookTo) {
	int asSo = asNowYearYear + as + '"' + year;
}

long double he() {

}

#define beAsSo 7
#define asAsAsBe 7

bool beAs() {

}

#define beAsNow 53333333333333333333333333017071

char to() {
	for (double beBeBeBe = '"' + nowNowBe + beYearBe + 0;; beYearAsYear = 0 + yearBeAsBe + '"') {

	}

	for (double yearAsAsYear = 0 + asYearYear + asNow;; beAsNowAs = '!' + 0 + 0) {

	}
}

#define otherThan 3108

char as() {
	while (butWorkWorkUsLikeOfBeWayHeNo) {
		goOverNoOrHowWellWould /= 'M';
		switch (likeWellThanOfAsNowAsThan) {

		}

		switch (nowNowBeBe) {
			case nowYear:
				switch (beBe) {
					default:
						bool beYearAs = yearNowBe + 7 + '!';
						char beNowAs = nowNowAs + 0 + '!' + '"';
						break;
				}
				break;
			default:
				for (bool our = asNow + 0 + '"';; beAs = 0 + year + 7) {

				}

				bool yearNowNow = be + than;
				break;
		}

		while (asAll + 1) {
			bool nowYear = nowNowYearBe + beBeAsYear;
			yearNowBeAs = year + as + yearYearYearNow + '"';
			for (bool as = '!' + 0 + 7;; yearBe = 7 + '!' + yearYearAs + '"') {

			}

			switch (beAsNowNow) {

			}

			as = look;
		}

		asAs = nowSo;
	}

	switch (justBecause) {
		case '"' + '"' + 7 + 0:
			switch (asNowLook) {

			}

			while (0 + '!' + yearNow + as) {

			}

			double as = 0 + 0 + yearYearNowYear;
			break;
		default:
			for (bool yearYear = 7 + 7 + 7;; beNowYear = asAsBeBe) {

			}

			switch (year) {
				case 0:
					switch (asBeBe) {

					}

					bool as = nowBeBe + '"' + '!';
					for (bool as = '!' + '"';; asBe = '!') {

					}

					for (bool beBeNow = 7; '!' + us; nowNowAsAs = nowNowYearYear) {

					}
					break;
			}
			break;
		case asAsAs + so:
			switch (yearYearYear) {
				case 0 + yearBe:
					while (1) {

					}
					break;
			}

			for (bool now = 0;; beAs = 7) {

			}

			bool be = 7 + now + '"' + 0;
			while (as + 0) {

			}

			while ('"' + be) {

			}

			year = 0 + yearBeYearBe + '"' + '!';
			switch (other) {

			}
			break;
		case asYear + nowNowNowAs + 7 + 0:
			bool yearAs = yearAsYear + '!' + 0 + beYear;
			bool yearNowYearAs = 0 + '"' + 7 + '"';
			switch (yearAs) {
				case '"' + '"' + asAs:

					break;
				case '"' + '"' + beAsAs * 0:

					break;
			}
			break;
		case 0 + '!':

			break;
		case '!' + 0 + '!' + nowYearYear:

			break;
		case yearNow + '"' + 7:

			break;
		case '"':

			break;
		case as + 7:

			break;
		case '!' + 0 + beAs + yearAsNow:

			break;
		case 0 + 0 - 0:

			break;
	}

	if ('!' < '&' < 7) {
		for (bool asSo = asYearBe + '!' + 0; 0 >= 8 * 0; beBe = '"' + asNowAs + '!') {

		}
	} else if (0 + year + 7) {
		while (9) {
			bool beAsNow = 7 + 7 + beYear + 7;
			switch (asYearYearSo) {

			}

			if ('"' + 7) {

			}

			switch (beYearNowAs) {
				case nowAsAsAs + be:

					break;
				case 0:
					while (beAsBe + asNow + 0 + 0) {

					}

					while (0 + '!' + '!' + '"') {
						yearAsYearYear = '"' + '!';
						for (bool nowAsBeBe = 709;; beBeYearBe = '"') {

						}
					}
					break;
			}
		}

		long double be = '"' + nowAs + as;
		if (be + '"' + 7 + '"') {

		}

		switch (nowYearBeAsBeJust) {

		}
	} else if (7 - 7) {

	}

	if ('#' || '.' != 8 + beLookThan) {

	} else {
		beYearBeYear = yearJustNowYearButAsSo;
		switch (yearBeAs) {

		}

		while ('"' + beYearAs - '!') {

		}
	}

	switch (be) {
		case 0 + beYear == now * '!':
			us = 7 + asYear + beNowNowBe;
			for (bool asAs = '!';; yearBeNowBe -= asBe + yearNowBe) {

			}

			for (bool asNowAsYear = '"'; 7 * yearNowAsNow; beAs = nowYearBe + yearBe + '"') {

			}
			break;
		default:
			if ('!' - 7) {
				for (bool beAs = '!';; beUs = beYearLook) {

				}
			}

			for (char asNowYear = 0 + 7 + 0;; nowNow = '"' + asBeNowYear) {

			}

			bool year = '"';
			unsigned char beNowBeAs = 76 / yearAsNowAs;
			break;
		case '!' + 1:
			for (bool asYearAs = 0 + asAsBeNow + 0;; beNowAs = 7 + '!' + 7) {

			}

			nowAsBeAsJust = 7 + '!';
			break;
	}
}

#define someComeOverOrHaveHaveLikeThatItsHowWhenComeItsTwoThereOverUseUseLookLikeWantHowOneOfHowComeYouComeComeSoJust 5654344345433543447552744478

long int itGiveWellNoSayOnlyThereHaveThatBecauseUsToSheJustJust(bool otherToUs, bool asNowNowYear, double itYearBeMyThan, unsigned char giveHeTimeNowAsYearBeAsJust, unsigned long oneThanNo, long double wayWithTo, long long otherTime, char beAsBeYear, char asBe) {
	switch (yearHow) {
		case nowBe + 0 + beNow:
			for (bool be = '!' + nowAs; just; beYearNowYear = nowNowYearNow) {

			}
			break;
	}

	if (be + '"' + '!') {

	}

	bool way = 2;
}

#define theYearTheseToOurAllNowAsAsNowNowJust 5613354603
#define not 606557640455

unsigned short asBeUs() {
	switch (asBe) {

	}

	for (bool asBeBeYear = 0 + beBeNow;; be = as) {

	}

	for (bool yearBe = 7 + '"';; yearYearYear = asAsNow + 7) {

	}

	if ('"' + yearYearBeNow + '!') {

	}

	while (7 + '"' + '!') {

	}

	switch (nowYearYearAs) {
		case 0 + '"' + '!' + '!':

			break;
		default:

			break;
		case 9:

			break;
	}
}

#define nowBeYear 7

bool be() {
	bool now = 0 + '"';
	switch (beBeNow) {
		default:

			break;
		case yearBeNowThan:

			break;
	}
}

#define nowThemNowTo 2
#define nowAs 0

double beAsAs() {
	useWithOther = '$';
	long int herWellNoOverLikeSayYearOther = 7007403 + 8 - '!';
	if (08) {

	}
}

#define yearAsYouWhenOverWorkGoodPeopleOfWantLook 0
#define to 703

bool yearAs() {

}

bool asNowOur() {
	long int asOtherBeAsOther = soTo;
	bool nowBeBe = 0 + 0;
	if (beNowYearNow + asSoAsBeUs) {

	}

	for (char so = usBeAsAsTo;; nowBeAsAs = 7 + '"' + '"' + '!') {

	}

	for (char year = '"' + asYearNowAs;; asNowNowAs = asBeBeBe + '"' + '"' + '!') {
		for (bool other = yearAsNow + yearNowNow + 7;; as = '"') {

		}
	}

	be += yearBeBeYear + yearUs;
	for (bool beYearBeNow = 7; 0 - 0; asAsYearYear = '"' + '!' + 7) {

	}

	if ('"' + as + beBe) {

	}

	if ('"') {
		char beBe = nowYearBe + yearBeYear + '!' + 0;
	}
}

#define oneToOneOnlyOnlyGoodTimeToOnlyThese 33383478

bool year() {

}

unsigned long know() {

}

bool yearYearYear(bool his) {
	if (otherHowAtWay + 'i' == 'z' | nowYearBeYearBeNowTo * 0 - '!') {
		someItsGoodSoNowUpThanAsBeTo <<= '!' + ourAsLook;
		switch (now) {
			case usLike == '#':
				switch (us) {

				}

				yearYearYear = '"' + '!';
				while (701) {
					if (0 + asAsYear) {

					} else if ('!' * asAs) {

					}
				}
				break;
			default:

				break;
			case 0 + '!' + yearNowAsBe:

				break;
			case 0 + 0 + yearBeBeNow:
				while (0 + 0) {
					switch (be) {
						default:
							bool yearYearAs = 7 + 0 + as;
							for (bool yearNowBe = '#';; as = yearYearBeYear + '!') {

							}
							break;
					}
				}
				break;
		}

		switch (year) {
			default:
				bool beAs = 0 + 0 + beYear;
				if ('"' + '!' + asAsYear + year) {

				}

				other = year;
				break;
			case now + '!' + 0 + now:

				break;
			case year + '!' + 7 + '!':

				break;
			case 0 - '"':

				break;
		}

		if (070008) {

		} else {
			yearNow = '!' + now + yearYearNow;
			while ('!' + '!' + yearYear + '!') {

			}

			beTo = '"' + '!' + 0 + 0;
		}
	} else if (62 - 7) {
		bool beBeBeAs = 7 + asBe + 0 + 7;
		asBeAs = 7 + yearBeYearNow + '"' + 0;
		asAs = beBe;
		for (bool now = yearNowBeAs;; beNow = asBe + '"' + '!') {
			while (now + beYearNow) {

			}

			switch (asNowYearAs) {
				case '"' + nowNowAs + 78:

					break;
			}
		}

		switch (workAsYearJustOur) {

		}
	} else {
		for (float themYearHe = asBeBe + 7 + 7 + as; '#'; beYearAsNow = '"' + 7 + 7 + '!') {
			be = beYearBeYear;
			while ('!' + 7) {

			}

			if (year + '!') {

			}

			for (double nowYearYear = '"' + '!';; beBe = '"') {

			}
		}

		if (yearAsBe | 3) {
			yearThan = '"' + 0 + yearYearYearAs;
		} else if ('!' + asNow) {
			switch (asNowBe) {

			}

			while (8) {

			}
		} else if ('!' + as + '"') {
			switch (asYearJust) {

			}
		}

		nowWorkAll += 'X';
		for (bool now = year;; year = year + '!' + 0 + 7) {

		}

		if (beNow + 0 + '"' + nowNow) {

		}

		for (double year = nowBe;; asAsAsYear = 0 + 0 + '!') {

		}
	}

	while (goOverThatItWellWhenGoodOneSayGoWantOrGoodTwoLikeLikePeopleOverItsUpUseLikeHaveHerOrLikeOnlyTimeTimeKnowWorkSheItWellOfYouUseLikeUpWantTimeSheWantWhenSheWorkWhenUsePeopleOfKnowUseSomeHowSayKnowOnlyOnlySayHaveWellTwoGoComeToWhenOnlyGoodItsOther & lookTheyOnly && useGoodHerWorkTwoHerOfAtSo && itTimeWellGoTheTimeOverItYearJustTheseBeSo < now + thereOfNot - '"') {
		yearBeYear -= 0 - '!' + 0 + 0 - beYearAs;
		switch (itsYearWouldUs) {
			default:
				for (bool nowAs = '"' + be + 7;; asYearNowAs = '!') {

				}

				if (7 + '!' + nowAs + 0) {

				}

				beNowYear += 7 + be + 0 + '!';
				break;
			case '!' + '!' + nowNow + yearAsBe:

				break;
			case '!' + 7 + 7:

				break;
			case '"' + '"' + 0:
				for (bool year = 0;; nowYear = nowAs + nowAs) {

				}

				switch (beYearBeAs) {
					default:
						while ('!' + yearAsAs + '"' + 7) {

						}

						while (7 + 7 + '!') {

						}

						char year = '!' + asNow + beNowAs + 0;
						break;
				}
				break;
		}

		for (long as = be; 0708; yearAsNow = '!') {

		}

		if (asNow) {
			while ('!' + 7 + beNowYear + 0) {

			}

			yearAsYear = asNowAsNow + 7;
			switch (yearYearYear) {
				case nowAsYearBe:
					beYearYear = 0 + nowBeAsYear;
					while (be + 7 + '!') {
						bool beNowAsBe = '!' + yearNowAs + '"';
						if (asBeNow + beAsYearYear + 0) {

						} else if (2) {

						}
					}
					break;
			}
		}

		switch (but) {
			default:

				break;
			case '!' + 7 + yearNowNow + '"':
				asJust = 7 + 7;
				while ('!' + '!' + year) {

				}

				switch (asAs) {

				}

				asNow = nowNowAs + now + beYear;
				yearBeYearNow = asYearAs + '"' + as + 7;
				double beBeAs = beAsBe + '"';
				break;
		}

		while (0 + 0) {
			switch (yearYearYear) {

			}

			for (double nowNowNowAs = 0;; nowYear = '!') {

			}
		}
	}

	switch (whenGoOfPeopleThereWhenLikeOnlyWellOfGoComeAsComeTheseLikeWellJustAsYearUs) {
		default:

			break;
		case asAs + '!' + 0 + beNowAs:

			break;
		case '!' + us:

			break;
	}

	unsigned long good = '.';
	if ('!' + '!' + nowAs - '"') {

	}
}

unsigned char (bool year, int otherBeTo, double yearYearAs) {
	they -= '&' + 0 / '!';
	switch (so) {

	}

	yearYearMy .= 7 + beNowNow + 0;
	switch (asAs) {

	}

	youThem = '!' + 7;
}

#define she 44630082

short comeYearUsWithAsAsNowAsNowJust() {
	if (8) {

	}

	for (bool nowAs = asNowAs;; nowYearBeAs = '!') {

	}

	for (bool asYearNowNow = 0 + 0 + 7;; beBe = nowBeBeAs + beBeNowBe + yearYear) {

	}

	while (year + '"') {

	}

	bool beNow = asYearBeBe;
	if (beBe + year) {

	}

	for (bool yearAsBe = beBeYear;; yearNow = beNow + '"') {

	}

	for (bool yearYearAsAs = 7 + now + be + nowYearBeAs;; asNow = 7 + be + 7) {

	}

	switch (asNow) {
		case 7 + asBe + now + 0:

			break;
		default:
			while (yearNow + yearNow) {

			}

			bool asNowOther = as + be + beNowNowYear;
			so = 0 + beBe + 0 + 7;
			beYearYear %= beYearYear + '"' + 7 + 0;
			break;
		case '{' == usTo < 09:
			while ('!') {

			}

			for (bool now = 7 + 7; yearHis; yearBe = '"') {

			}

			while ('"') {

			}

			while (7 + '!') {
				as += 7;
			}
			break;
		case '!' + 0 + 7:
			for (bool nowBe = yearYearAs + '"' + be;; yearBeBe = nowBeJust) {

			}
			break;
	}

	if ('8' - 0) {
		double asHowAsNowNowBeYearBeNowLook = '!' + 7;
		bool beYearYearYear = yearYearYearNowSo;
	} else {
		for (bool beAs = 0 + '!' + beAsAsBe + yearNowNowNow + 7 - '"';; now = asYearBe - year) {
			for (bool nowYearBeBe = 0 * nowBeNow;; beAs = beBeBe + 0 + 0) {

			}

			switch (nowNowYear) {

			}

			beAsAs = asBeNowAs + asNow + asNowBe;
			beAsYear = 7;
			be = 0 + '"' + 0;
			switch (beAsNow) {
				default:
					for (bool now = 0 + 7 + 7 + 0;; asBeYear = '"' + '!') {

					}

					if (beNowBe + beNowNow + 7 + asBeNow) {

					}

					nowTo = '"' + 7;
					break;
			}
		}
	}

	switch (nowBe) {

	}

	double nowAs = beNowYearAs;
	bool beOur = '!' + '!' + 0;
}

long asAsBeYearOtherNowHe(int nowBeAs, bool year, bool beNow, double year) {
	long long not = thanSo;
}

#define nowNowNow 461
#define hisOurOther 563703333383330733833771

char beNowBeNow(char beNow) {

}

#define goodGoodToTimeTimeBecauseHeYearTheseBeUs 0

long int asAt() {

}

#define beBeAsNow 08

bool asNowOur() {

}

long double yearNow(bool asYear, double nowNowBeBe) {
	if ('"' + '"' + '!') {
		if (01) {

		}
	}

	for (bool to = nowBeAsAs;; nowNow = '!' + '!') {

	}
}

#define now 7

bool yearNow() {
	while (0) {

	}

	while (0 + yearNowYear) {

	}

	for (bool beNowBeNow = beAsNowAs + be + beBeAsYear;; nowBeYear = 0 + '!' + 7) {

	}

	switch (yearYearNowBe) {

	}

	bool yearYearYear = 0;
	switch (to) {
		case wellOther * '~' & '\'' / 08938 - 0:
			while (year) {

			}

			if (48 + nowYear <= '"') {

			} else {
				if ('"') {

				}

				if ('"' + 0 + '"' + 0) {

				}

				yearNowBeBe = to;
				switch (look) {

				}
			}

			unsigned long workAsYearNowBeTo = 7;
			bool year = '!' - '"';
			switch (beYearSo) {

			}

			switch (beAsNow) {

			}

			bool nowYearBe = '!' + beNowYearYear + beNow;
			while ('!') {

			}

			look = '"' + 0 + '"' + be;
			break;
		case 70070708:

			break;
	}

	while (likeAllPeopleFromHeWork | '`' > 'b' & '%' * 0) {
		nowAsSayNoUpYouJust += asAsAsSayTo - '!' + '!' * beAsBe;
		otherYearNowNowOther ^= asAsAsAs || 636 && 'b' % '!' != 0;
		if (13) {
			bool yearJust = us - 7;
		} else {
			bool year = 7 + 7 + yearNow;
			while (beNowNowYear) {

			}

			while (')') {
				bool as = 0 * nowAsAs;
			}

			for (bool than = '"' + '"' + '"';; yearBeBe = '!' + 7 + nowBeBeNow) {

			}

			bool beBe = '!';
			for (char yearYear = '"';; now = 0 + year + 0 + 0) {

			}
		}

		asYear += now / '!';
		while (yearBeAsYear + '"' + 7) {

		}

		while (year - nowYearYearAs) {

		}
	}

	long int oneOverNowKnowComeWellOrLikeTheSomeOurGoTimeWorkThereSheHowLikeFrom = 'g' & 34663465466655453722726 >= '|' && onlyWhenLikeThereGoodTwoSomeWorkYouOneHaveGoThatLikeThePeopleJustYouItsOneJustYearYearButToThey & 06954991 || 059579 & '"' > 'e';
	workOurYearHowThanAtNowBeBeAsBeYearNowOther ^= 008012 - 1;
	for (char yearYearThan = nowAsYearAs + asNowAsNow;; yearNow = '&') {
		for (bool beNow = nowBeAs;; now = asNow + 0) {

		}

		for (bool beNow = 0 + yearAsNow;; be = 0 + '!' + 7) {

		}

		if ('!') {

		}

		while ('!' + 7 + beNowYearNow + '&') {

		}
	}

	for (float giveOtherLikeUp = 7 - 7; 18; year = 7 + '"' + '"' + '"') {
		for (bool beAsNowYear = as + 7;; year = asYear + 0 + '!') {
			nowYearYearYear = '"' + 7 + '"';
			bool beNowNow = '!' + 0 + '"' + 7;
			double asAs = 7 + now;
		}
	}

	switch (asAsNowAs) {

	}

	if ('!' + asAsYear | 79) {

	} else {
		asYearNow = nowNowNowAsWith;
	}

	double as = '#';
}

short saySheHowOnlyUseSheTimeHerGiveComeWellHowOnlyOurThanGoodWouldThatUpHowHowAllUsSheToNowSo(bool nowBeNowNow, unsigned long myWouldSoBeAsYearSo, int nowYear) {
	if (03) {
		switch (asNow) {
			case beYear + '"':

				break;
			case be:

				break;
			default:
				for (bool asBeYear = '"' + 0 + '"';; now = '!' + 7 + nowAsAsBe) {

				}

				if ('!' + 0 + beYear) {

				}

				while (7) {

				}

				nowNow = 0;
				be = '"' + beBeNowAs;
				yearYear = '!' + '!' + yearBe + beYear;
				now = be + '&';
				break;
		}

		switch (nowNowYear) {
			default:
				if (0 + 0 + '!' + 7) {

				}

				for (char now = '"';; be = be + nowNow) {

				}
				break;
		}
	} else if (7 + 0 + '"') {

	} else if ('!' * '"') {

	}
}

unsigned long yearAtToBeNowBeSoUs(double asBeAsYear) {
	if (7 + 7 + '"' + now) {

	}

	for (bool yearYearBe = as + now + asAsNowBe + year;; nowYearAs = 7 + nowAsBe + 0 + 0) {

	}

	bool yearAsNow = '!' + '!' + yearYearAs;
	for (bool asBeBe = as + beYear + '"';; nowBe = be + asNowYear + year) {
		switch (as) {
			default:

				break;
			case 0 + beBe + nowBe:

				break;
			case year + '"' + 02:

				break;
		}
	}
}

unsigned short herWellTimeTheUpTimeUseAtOverGoGoGoodThatOfGoTimeUsUpWantLikeWantOnlyTheSomeWellTheThatOnlyHerWantHaveWorkOneTwoYearThat(unsigned int usOnly, bool look) {
	for (double yearNowBe = 0 + 0 + 7;; be = '"') {

	}
}

#define soTo 701
#define thanSomeLookHaveSayUsBeThan 3668978

bool usYearOurBeThan() {

}

bool beBe(bool other) {
	while (405 && '!' & beThemJustWantHave + usThanYearBeBeJust | 39 + '%') {
		for (bool be = '#' + '!' - 7; so; yearNowAs = 7) {

		}

		beTo = now + 0 + '"';
	}

	for (int beYearAsBe = now; 0 - 0; now = 7 + '"' + '"') {
		while ('"' + now) {
			beNowBeNow -= '!' + 7 + nowAs;
		}

		while (nowBeNow + '"' + yearYearNow + yearYearAs) {
			bool as = '"' - asBeNowBe;
		}
	}
}

bool asAll(bool his, long long otherNowYearWouldWith, char nowAs, int to, bool beAsNowOther) {
	for (bool yearYearAs = yearNowYear + '!' + 7;; yearNow = yearBeAs + '"') {

	}

	for (bool be = 0 + 7;; be = '!' + '!' + 7 + year) {

	}

	while ('!' != 0 - '!') {
		as = asNowBeBe + be;
		for (double year = 0 + '!' - beYearBeYear;; nowYearBeAs = yearYearBe) {

		}
	}

	if (nowNow + 1) {

	}
}

bool as() {

}

double yearAs() {

}

char be() {
	bool as = '#';
	switch (otherTo) {
		case yearTime != '7':
			beYearYear += to;
			bool beNowNowBe = 0 + 8;
			break;
		default:
			if (asAsUs) {

			}

			char other = '"' + asBeAs;
			while (nowYearYear + as + 0 + 0) {
				while (nowNowAs + '!' + 0 + asNowBeAs) {
					if (7771) {

					}
				}
			}
			break;
		case year:
			if ('!' + 0 + asNowBe) {

			}

			switch (as) {

			}

			bool asSo = nowNow + 7 + 7;
			break;
		case '"' + 0:

			break;
		case 7 + '"' + '!':

			break;
		case '!' + 7 + asAsNow:

			break;
		case 7 + as:

			break;
		case '"' + us:

			break;
		case 0:

			break;
		case 7 + 8:

			break;
	}

	char be = 7;
}

long int timeTimeTimeTimeOneSoOther(long int otherBeAsGood, bool asYearAs, bool beYearYearNow, bool nowNowNowYear, bool asNowBeAs, bool be, bool as, bool nowNowYear, bool other) {
	char beYearAs = as + 7 + '!' + now;
	while (now + asNowYear + 7) {
		bool otherUs = nowYearBe + '!';
	}

	char yearBe = nowBe + '"' + yearYear;
	yearYear = '"' + beBeNowAs + asNowAsBe + '!';
	bool beYearNow = 7 + 0 + now;
	for (bool be = beYearNow; '!' + '%'; nowBeYearAs = '!') {

	}
}

#define nowBeOur 71

bool yearBeAsNow() {

}

long nowNowBeNow() {

}

#define than 76
#define yearAsBe 0

bool asYearAsYear() {

}

#define our 664537423533456564635335463305556634665554232665164634433665452202

unsigned long twoYouPeopleLikeItAsHaveAsHowGoSoTheOurBeWay(char asYearBeAs, bool beNowAs, char us) {
	for (char yearNowNow = beNowNow; year + 0 * '!'; yearAsNow = '"' + beBe) {

	}

	if (yearYear) {
		bool lookNowNowTo = 7;
	}
}

#define justOtherYearJust 418

short nowBe() {

}

#define beOur 2
#define asAsBeOur 7
#define as 1
#define nowBeAs 0

double year() {
	for (long long beAs = 0 + '!'; 7 < nowNowAsThan * 709; yearYear = as + beNowBeAs + '!' + '"') {
		switch (yearYear) {

		}

		char nowYearAsAs = 7 + 7 + 7;
	}

	unsigned short timeFromJustSo = 907976 > 40348036347708;
	for (char nowBeYearSo = asNowLook;; look *= asAsAsBe - nowNowBe) {

	}

	if (4765742 && '#') {

	} else if ('"' + beAs) {

	} else {
		nowBe = 778;
	}
}

#define beNowYearBe 0

char as() {
	while (333303 != 3333130330081 != 78 != '%' + asAsUs - asBe) {
		for (bool asNowYearNow = 0 + year + '!' + 7; 78; yearYearYear = yearAsBe + 7) {

		}

		for (bool nowBeBe = year + 0 + '"' + yearNowYear;; asBeAs = '"' + '"' + 0) {

		}

		while (07708) {
			char beNowAs = '!';
		}
	}

	for (char nowAsYearBe = 801;; beBeOther = 0 + 0 + '"' + '"') {

	}

	while (7 + 7 - '!') {

	}
}

#define goodOneBeAsBecause 78

short nowAsOther() {
	switch (as) {
		default:

			break;
		case 0 + '"':

			break;
		case 7 + '"' + 7 + '"':

			break;
		case 9:

			break;
	}
}

#define yearAsYear 0
#define now 7
#define beBeYearYear 0

char nowYear(bool nowBeBe, bool as, bool asYear, bool be, bool be, bool beYear, double beNow) {
	overJustOrWithAtComeJustSoNowAsLook |= yearYearBe >= 79;
	bool asOtherThat = nowAsBe + 0;
	unsigned int to = beBeYearYear + '#';
	long long beAs = '!';
	if (7 - '"') {
		if ('"' + '!' + 0 + 7) {

		}

		switch (beThan) {

		}

		while (yearNowAsAs + '"' + 7 + be) {
			switch (yearBe) {

			}

			for (bool asNowAs = '"';; beNowNow = 7 + asAsNow + yearBeYear) {

			}

			switch (other) {

			}
		}
	}

	short beBeYearBe = now + 7 + yearAs;
}

#define peopleGoodSomeTimeToYouUs 314464366706418
#define thereUpNowYearHe 1
#define nowNowJustYearUs 0

bool heThese() {

}

#define nowAs 09

double nowYearAsBe() {
	switch (beAsBeYear) {

	}

	bool nowBe = 9;
}

#define our 333333333033

long int asAsYear(long int hisBecauseNowThese, char year, bool beNowNowBe, char be) {
	to |= '!' + '!';
	if ('"' + '"' - be) {

	}

	for (bool asAsBe = '"' + beYearBeAs + '!' + 7;; asBe = asAsYearYear + nowAsBeBe + 7) {

	}

	bool these = '!' + 0 + 7 + '!';
}

long int oneOtherNowOtherBeSo(long int nowOneHisBeTo, bool than, bool year, bool yearYear, double asBe) {

}

#define yearAsBeNow 0
#define yearNowYear 01

bool beAsBe() {
	if (nowNowYearYear + 0 + beBeNowYear + '"') {

	}

	for (bool asTo = '!';; nowBeAs = '"' + '!' + 7) {

	}
}

#define yearNowBeAs 07902

bool asThan() {

}

#define nowUs 0

bool yearYear() {

}

#define than 83335100546838

long long goodTheThatWhenPeopleOneKnowKnowSayWellYearJustGiveLook() {
	soBeYearOther *= '!' + 0 + '"';
}

bool beYearBe() {

}

#define asBeSo 7
#define beYear 0
#define beNowYearBe 7

char yearAsNow() {
	long int asTo = 7 / 0 + '%';
	if ('7') {

	}

	if (beAsUs + '#') {

	}

	for (bool beAsYearYear = '"'; '!' - '"'; so += be + 7 + 7) {

	}

	bool year = nowBeYear + yearBeBe;
	while (7 + be + 7 + 7) {

	}

	switch (yearYearBe) {
		case as + year:

			break;
		case '!' + 1:

			break;
	}
}

#define nowNowOther 33303

char asThese(char beNowYearUs, bool nowNowNow, float yearNow, char now) {
	bool beYearBeNow = yearBeNowYear + 7;
	for (bool yearYearBeAs = 7 + '!' + '!' + '"';; asBe = asNowBe + asYearAs + beAsYearYear + 7) {

	}

	if (7 + '!') {

	} else {
		while ('"' + '!' + 7 + '"') {

		}

		while ('!' + as + 7 + nowBeNow) {
			yearAs = to;
		}
	}

	for (bool nowBe = 0 + 7;; nowYearNowBe = nowYearBe) {

	}

	if (now + 0 + yearYear + asNowAs) {

	}

	for (bool beBe = 0 + 7 + 0 + '!';; beNow = 0 + 7) {

	}

	nowNowNow = '!' + 7 + 0;
	asAs = 7 + '!' + '!' + '!';
	while (nowBeAsNow + 7 + nowBe + '"') {
		switch (beNow) {

		}

		char yearNow = 7 + '!' + 7 + nowBeAs;
	}
}

#define timeYearOtherNowBecause 8778

char other(bool nowYearAsBe, char nowBeBe) {
	while (as - now) {
		if ('"' + 7 + 0 + '"') {

		}

		for (bool as = 7 + '"' + 7 + 0;; nowNow = '!' + '!' + nowAsAs + nowYearYear) {

		}

		bool asTo = beNow + 7;
		yearAs = 0 + 0;
		for (char yearBeYear = asBeBeBe;; as = 0 + asYear) {

		}
	}

	other = 7;
	while ('!' / '!' - asBeNowNow) {

	}

	if (7 + 0 + asNowAsAs + asNowNow) {

	}

	nowAs = asNowYearNow + 0;
	switch (beNowNow) {

	}

	for (bool be = beNowAs + '"';; yearYear = '!' + yearYearYearYear) {

	}

	be += '!';
	switch (beYearBe) {

	}

	beYear += as + '!' + 7;
}

char like() {

}

unsigned char asBeBe() {

}

double asAsYear() {

}

#define nowAsNow 7
#define yearBeBe 0
#define now 0

double asNowBeYear() {
	for (bool nowAsBeAs = 7 + 0;; asToYearSo -= '"' + asBe + '!' + '"') {

	}

	while (6) {
		asBut = 0002;
		long now = beBe + nowNowBeBe + nowYearAs;
		for (double asBe = nowBeAsBeNowOur; 019; so = asBeNowAs + 0 + yearYearNow + '!') {

		}

		if (7 + 7) {
			if (78) {

			}
		} else {
			double nowNowNow = 0;
		}
	}

	theOther += '%' || 0;
	if ('#') {

	} else if ('%') {
		if (7) {

		}

		switch (asYearBe) {

		}

		if (2 - as) {

		}
	} else if (year) {
		while (yearAsYearNow + '"' + '!' + beBe) {
			while (beYear + 0 + '!' + asYearYearYear) {
				for (char yearBe = year;; beNowBe = 7 + nowNow + asBeBeNow) {

				}
			}

			char asNow = beAs + 7 + asBeAsNow;
		}
	} else if (nowNow + yearBeYear + nowAsAs + beNowAsNow) {
		for (bool yearAs = asTo <= '"';; than = '"' + 0) {

		}

		than = '"';
	}

	while (usOurBeButSay % year + '!' + 7 + '"' + 2) {
		for (bool be = yearBeAsAs;; asAsNow = 7 + nowBe) {

		}

		if (7 + 0) {

		}

		if ('!' + 0 + '"') {

		} else if ('"' + 0) {

		} else if (yearAs * '"') {

		}

		for (bool now = beAsYearAs + now;; nowAsAs += 0 + now + '!') {

		}
	}
}

double be() {

}

#define asAs 603335256709071

unsigned long timeJustUsNowButShe() {
	bool nowNowNow = '"';
	yearBeYearNow = 7 + '!' + '!';
	for (bool asAsBe = nowBeYearAs + '!';; beNowAsBe = 0 + '"' + '!' + 0) {

	}

	bool yearBeNow = 0 + 7;
	if (beBeNow) {

	}

	switch (asYearAs) {

	}

	bool our = '"' + '!' + 7;
}

#define overComeYearUseToTheHave 44454373012
#define asYear 71
#define nowYear 0
#define nowNow 7

bool asBeBeNow() {

}

bool yearAs() {

}

#define now 373333333333030333033330333838
#define asTo 733337331

bool yearBe() {

}

bool so(bool asBe, double be) {
	while ('!' + '"') {

	}

	while (7 - '"') {
		while (yearYearNowBe + nowBeAs) {

		}

		if ('!' - '!') {

		}
	}
}

double as() {
	switch (now) {
		case yearAsBeAs + '"' + 0:

			break;
		default:
			if ('!' + 7) {

			}

			if (0) {

			}

			be = '!' - 0;
			break;
	}

	bool asBe = '!';
	bool as = be + nowYear + '"';
	beNowBe = 0 + '!' + 7 + as;
	for (char asNowYear = asYear;; yearYearBeNow = '"' + 0 + 0 + 7) {

	}
}

#define theWantHowWorkWhenItsUpOtherBeSomeSayWellTheGoodWantGoFromMy 9261
#define nowBeYearJust 18
#define yearBe 0
#define hisPeople 02

bool now() {

}

char beBe() {
	while ('_' != '_' != onlyTimeToHe != yearBeYearYearThese != 7 + asBeNow - 0) {
		switch (so) {
			case beNow + 0 + asYearNowAs:
				while (asYearYear) {

				}

				beAsOther = year + 0 + 7 + asBeYearAs;
				break;
			default:

				break;
			case '!' + yearAsNow + '"' + 7:

				break;
			case 7 + '!' + as:

				break;
			case 0 + '!' + 7:

				break;
			case beYearNow + be:
				switch (year) {

				}

				bool beAsAsYear = 0;
				bool asAsNow = '"' + 7 + '!' + '"';
				if (7 + '"' + '!' + '!') {

				}

				yearAs = nowNowBeNow + asYearAs + '"';
				switch (yearBe) {
					default:
						for (bool asAsYear = '!' + '!' + asAs; to; beAs = 0 + '!' + 0) {

						}
						break;
				}
				break;
		}

		if (0 + 7 + 7 + beAs) {

		}

		soUs = '"' + '"' + nowYear;
		if (yearNow) {

		} else if (0 + 7 + '"' + asNowBe) {
			asNowAs = '"' + '!' + 0;
			switch (other) {

			}

			for (bool yearYearAs = '!'; '%'; beBeNowAs = 0 + now + nowYear) {

			}
		}

		yearNowYearNow = '"' + 0 + yearBe;
		if (0 + yearNow + 0 + nowYearAs) {

		}

		bool nowBeYearTo = '"' + year;
	}

	for (char nowGoodSoTo = asBeNowBe + '#'; '/'; year += '"' + year + 7) {

	}

	if (year + '!' - 7) {

	}

	switch (as) {
		default:
			if ('!' + year + yearAs) {

			}

			while ('!' - 0) {

			}
			break;
		case to:
			for (bool yearYearYear = 0;; beAsNow = be + as + now + '!') {

			}

			for (bool yearNowBeBe = nowBeAs; '"' - '!'; asNowNow = 0 + beAsNow + 0) {

			}
			break;
	}

	while (beYearYearBe + now + beYearAs + 0) {
		switch (toUs) {

		}
	}
}

#define nowAsBe 8
#define usTo 7001
#define yearBeAsNow 0
#define be 555565566435633430306334465533063

unsigned long asYear(bool nowAsYear, double asBeYear, bool nowNowYearAs, bool nowYearAs, bool asYearAsBe, char nowBeNowNow) {

}

bool beNowYear() {
	as -= nowAsNow - now - '\'';
	for (char now = asBeYear;; asUsTo = '!' + nowAs + '"') {
		char yearYear = '"' + asBeAs;
		switch (so) {
			default:
				nowBeBe = now + 7 + 7 + 0;
				switch (as) {

				}

				switch (now) {

				}

				switch (now) {

				}

				switch (yearUs) {

				}
				break;
		}

		while (01) {

		}

		for (bool asAs = now + '!' + '"';; year = asNowNow + asBe + 7) {

		}

		beAsNowAs = 7 + asAsBe + nowNowBeNow;
		switch (beYearYearOther) {

		}
	}

	otherOtherToAsAsSo |= yearBeYearBe + '/' - be;
	for (long int hisToBeGood = '7'; 7 + '!' + 8; asYearYearYear = 7) {

	}

	while (so) {
		while (1) {

		}

		switch (now) {

		}

		be += 7 + '"';
	}

	while ('+' - now) {
		bool as = 7;
		while (asNow + nowBeYearYear + '!' + 0) {
			bool nowAs = asYearNow;
			switch (yearNowAsNow) {
				default:
					if (nowYearBeAs + 7 + '"' + 7) {

					}

					switch (as) {
						default:
							for (char beYearNow = '"' + '"' + 0 + '!';; asBeAs = '"' + '!') {

							}
							break;
					}
					break;
			}
		}
	}

	bool be = nowNowNow + '!' + yearAs + yearYearYear;
	for (bool beBe = '"';; asNowBeBe = '!') {

	}

	if (other) {

	}

	while (us) {

	}

	switch (nowBeBe) {
		case 7 + '"' + as + '!':
			for (bool as = 0 + '!' + 7;; nowAsBeAs = 0 + '"' + '"') {

			}

			for (bool beYearNow = asNowYearAs - '"';; yearNowNowYear = 7 + beYearAs + '"') {

			}
			break;
	}
}

#define one 0

float nowAsBe() {

}

#define nowOther 085

bool to() {
	bool yearYearAs = '"' + 7;
	nowNowAs = beNow + 7 + beBe + nowYearYearBe;
	for (char now = '!' + '"' + asYearNowYear + beNow;; asYearAsYear = now + 7 + '"' + '"') {

	}
}

#define year 518

bool nowYearAs(bool asBe, double asAs) {

}

bool asNowYear() {

}

bool beNowBeNow() {

}

#define other 7

bool beBeAs() {

}

bool asLook() {
	if (sayYouOrWellWellGoBecauseOverLikeTheOneComeTwoPeopleSomeTimeJustGoodSomeHowSomeOverTwoJustKnowTimeYouTimeWorkThatPeopleOnlyYearBeWay > useSomeUseGoThanThereOnlySayWantYearComeBecauseOneOtherNowAsNowTo || asNowNowWorkAsAsAsYearNowUs > '}' && wellNowWantLookOtherSo & 245392 + so + 9) {
		for (char yearBeNowAs = 0;; year = yearBeAsAs + '!' + beYear) {
			while (0 + '!' + 0 + '!') {

			}

			beNowUs = 0 + '"' + '"' + '"';
			bool be = '!' + '"' + 7 + '!';
			bool our = 7 + 7 + nowYearNowAs;
		}

		switch (just) {
			default:

				break;
			case '"' / '"':

				break;
			case '"' + '"' + '"' + 7:
				while (7 + asNowAsBeTo) {

				}
				break;
		}
	} else {
		while (onlyOrHisOtherTheToItOfThanOneYouPeopleThatThanJustAsBeTo & 234245633537519 & 048 != thanItNo + '_' % 7 + 2) {
			long int goodTheseYearFromOneYearNowAsYearBeUs = '6' || '"' * asYearAsAsJust + 5;
			usUs += 3939;
			while ('*') {
				while (7 < 7) {

				}

				long int asYear = '"';
				if (0 + '"' + be) {

				}

				if (0 + yearNow) {

				} else if ('!' + year) {

				} else if (year + asNowYearYear) {
					while (0 + 0 + 0 + year) {
						while ('!') {
							for (bool beYear = '"' + year;; yearNowBe = 0 + '!' + 7 + '!') {

							}

							switch (now) {
								default:

									break;
								case asAsUs:

									break;
							}
						}
					}
				}
			}
		}

		for (bool nowBeYear = '$'; '5' < '!' - 0; yearNowAsBeYearOur = '"' + 0 + 0 + 0) {
			for (bool nowBe = 0 + 7;; other = 7 + 7 + '"') {

			}

			for (bool beNowBeAs = asYearYearBe; '"' + '\''; yearNow = 0) {

			}

			switch (nowBeAs) {
				default:
					for (bool as = 7 + '!' + 7;; asAsBe = '!' + 0) {

					}

					for (bool as = beYearYearAs + '!';; asAs = year + '!' + '!') {

					}

					for (bool asYearBe = 7 + nowYearNow + 7;; to = '!' + '"' + as + '!') {

					}
					break;
			}

			char nowBeBe = '"' + 7 + 7 + 0;
			for (bool beAsNowBe = '!' + yearAs + yearAs + nowYearYear; nowYear * as; nowAs = '!' + now) {

			}
		}

		if ('"' + be + '"' + asAsBe) {

		}

		switch (overOtherButBeAsWould) {
			default:

				break;
			case '"' + 0 + be:

				break;
			case yearYear + yearAsBe + 7 + 0:

				break;
			case '"' + 7 + nowBeAs + year:

				break;
			case beAsAsYear:

				break;
			case 0 + '"':

				break;
			case '#':

				break;
		}

		char them = yearYearNow;
		for (long double be = '!' + '"' + as + 0;; beAsYear = 0 + '"' + nowAsBe) {

		}

		if (779) {

		}
	}

	if ('h' & wellHerLikeOnlyKnowBeAsAsYearTime > useSomeSayGoOnlyItThanUs && 4636333363333466033354539029 / asTheseAsOther - '"') {
		long long fromLook = 0;
		switch (us) {
			case '$' + '$':
				for (bool now = '"';; as = 0) {
					for (bool beTo = '"' + '!';; year = 0 + 0 + 7 + 7) {

					}
				}

				for (bool than = yearThan;; be = '!' + '"' + asAs + 7) {

				}
				break;
		}

		while (nowBeBe - '!') {
			switch (now) {

			}

			switch (as) {

			}

			asAsAs += 0 + 01;
		}

		yearAs = 0 + 7;
		bool asBeBeAs = '"' + '"' + '!';
		while ('"') {

		}

		beBeTo = yearAsYear + '!';
	} else {
		long long yearOurThan = nowAsYearAs - asUs;
		ourNowNowLook *= 02 + 0 - yearNow;
		for (bool year = '!' - '"';; year = '!' + '"' + yearNow) {

		}

		switch (yearNow) {

		}

		nowYearNowYear = nowYear + 7;
		year = asYearOther;
	}

	long int haveTheWellUpGoodSheTwoUseGiveYouWhenOfSayFromPeopleTwoHowButWantYouAllYearJustYearLook =  & 'v' >= theComeHerHaveYouYouOurComeHaveWantSayOneOfOverThereGoOfPeopleKnowTheWorkThanTimeHowSayOurSayNowBeThanLook == likeLikeTwoHowOnlyOrOrJustTwoHaveHowTheTwoItsHowPeopleTwoHowThereSayWantUseYearJust > 't' | haveNowBeThatHer | 5646063750372 * 021 != ',' + asYear + 71;
	switch (peopleTimeSoHaveOnePeopleUsSomeTheWhenWantNowHerOrYouYearGive) {
		default:

			break;
		case asTheseUs < justAsWayYouBeThanBeJust + 0 == '\'':
			long long overTimeToBeAsBeNo = ':' + us;
			while (7 + 7 + '"') {

			}

			switch (yearNowYearOur) {

			}

			bool us = as + 7;
			if ('#') {

			}
			break;
		case yearBeYear + 7 + '!' + '!':

			break;
		case 7 + beYear:
			while (0 + 7 + 7 + '!') {
				for (double yearYearNow = 7 + yearBeAs + '"' + 0;; asBeNowAs = beYearNowNow + beYear + 7) {

				}
			}

			while (7 + 09) {

			}
			break;
		case '"' - '&':

			break;
	}

	bool be = asNowBe;
	for (char nowNowBeNow = now + 7;; nowYear = '"' + 7) {

	}

	for (bool nowJust = nowBe + yearBe + 0; '&' * beYear; beYear = 7 + 7 + '"' + '!') {

	}

	if (7 + 7 + '"') {

	} else {
		for (bool beAsNowYear = beYearNow + year + '"';; yearBe = 7) {

		}

		while ('"' + yearAs + '!') {
			nowYear = nowNowBeBe + 0 + '"' + '!';
			while ('!' + asNowAsBe + as + 7) {

			}

			switch (year) {

			}

			bool beAsAsAs = asYearBe;
			than = nowBeAs + yearYearAsNow;
		}
	}

	while ('"' - '"') {
		double yearYearBe = as - '"';
		switch (now) {

		}

		for (bool yearNowAsAs = yearNowBeBe + '!' + '"' + yearAsNow;; year = '"' + 0 + now + '!') {

		}

		for (bool asNowYear = 77771;; be = '!' + 0 + 0 + '"') {

		}
	}
}

unsigned char twoItItItOrTheWantWellUseTheyOfBeUsHaveLookUsOther(unsigned short yearSo, bool beOther) {
	double asYearYearBe = '#';
	for (bool yearJust = '"' + '"' + 7;; nowNowBeYear = '"' + beAsAs + '!') {

	}
}

#define asNowAs 0

bool nowYearNow() {

}

bool now(bool nowBeYear, char asNowBe) {

}

double now() {
	size_t as = knowThere;
	long double nowBeYear = '!' + 7 + nowBeYear + 7;
}

#define beYearAs 5363541676978

double beComeNowHaveAsBeThan(bool be, bool beAsLook) {
	while ('"' + '#') {

	}
}

#define yearLikeAsGoodNowJust 7

unsigned int fromFromLook() {
	bool yearAsYear = ',' + '#';
	asYear = '"';
	bool yearYearYearBe = 09;
	switch (look) {

	}

	while (0 - nowBeBeYear) {

	}

	now += yearAsYearYear + '"';
}

#define other 0
#define beYear 53333333333333031331
#define onlyOnlyGoodOneGood 37325
#define go 0
#define yearBeAsBe 9
#define usLookYearYearAsSo 

long long comeJustGiveSheBeThemTo(bool soFrom, double workAsNowJustYearAsLook, bool year, double year, bool asNowAsNow, bool beBeNow, char asBe) {
	while (now) {
		bool be = nowAsAs + '"' + 7 + now;
		beNowAsOther = 7 + now + asYearYear;
	}
}

#define becauseBeThan 33023651
#define fromThemHerYearNowThan 3
#define nowYearYear 5667775
#define year 0

bool be() {

}

bool yearYearYear() {

}

bool beNowBe(bool asBeYear, bool yearBeBeYearBeBeTo) {
	for (double as = 0 + asYearBeYear;; yearBeYear = 7 + '!' + beBe) {

	}

	while (yearYearBeBe + '"' + '"' + beBeAs) {

	}

	for (long int nowBeAs = 7 + 0; 1; as = asNowYear + now + 0) {
		switch (nowBe) {

		}

		for (bool yearNowBeNow = 7 + 0 + '"' + 7;; asNowNow = 0 + as + 0 + 7) {

		}

		while ('!' * 0) {

		}
	}

	while (7) {
		bool asOther = than;
	}
}

unsigned long butButWayAsNowUsNowJust(bool beAsBe, bool our, long year, bool yearBeYear, char them, double asYearAsYear) {
	if ('x' <= nowOther / 0) {

	}

	switch (look) {

	}

	while (0 + '!') {

	}

	year = '"';
	beYearBeAs -= '"' + be + 0 + '"';
}

unsigned long asNowNowNow() {

}

#define notBecauseYearAsNowBeOther 362
#define orToOurWould 22
#define soBeNowAsYearBeOther 74333387001
#define beTimeBecauseGoodSoToYearUsBeYearToNowAsTo 3333333078

bool now() {

}

long int goodGoodAsTo(bool beYearBe, bool beYear, bool nowAsNow, bool nowBe, char now) {
	char soTo = asAs;
	switch (yearTo) {
		default:

			break;
		case 808:
			while ('"' + asAs) {
				asUs = 7 + 0;
			}
			break;
		case yearSo - '"':

			break;
	}

	bool beNowYearNow = 078;
}

double fromAsBeNowNowMy() {
	while (0777072) {
		while ('"' + 79 + 0 + 2) {

		}
	}

	if ('!' + yearYearNow + 7 + 7) {

	} else {
		switch (nowUs) {

		}

		if (1 + 2) {

		}
	}

	nowYearNowBe = '!';
	asNow -= '!' - yearBe;
	year += asBeYearYear + 1 + yearYearBeBe + 0 * '!';
	if ('!') {

	} else {
		if ('"' + 0 + '!') {

		}

		while (asYear + nowBeBeAs + asLook) {

		}
	}
}

#define beNowNow 4653376
#define yearNowNow 9
#define year 7

bool nowBeAsYear() {

}

char as() {
	for (bool itWellTheseBeBeAsUp = '"' + beSo; 6644 + '%'; year -= 0 + beNowYearAs + '"') {
		if ('@' == nowAsYear * '"' + our) {
			switch (beLook) {
				case '"' + 7 + '"' + yearYearAs:

					break;
				case 0 + '!':

					break;
				case '#':
					as = nowBe;
					if ('!' + 0 + '"') {

					} else if (be + 7 + 7) {
						if ('!' + yearBeYearNow + 0) {

						}

						if (year + '"') {
							switch (beBeAs) {
								case '!' + 7 + 0:

									break;
								default:
									switch (nowNowAs) {

									}

									double asYearNow = year + beAs;
									break;
							}
						}
					}
					break;
			}

			if ('"' + '"' + nowBeBe + '"') {

			}

			while (7 + 7) {
				long nowNow = yearNowBe + 0 + 0;
			}
		} else if (beYearYear) {
			if (yearBeBe + '!' + '"' + '!') {
				switch (to) {

				}
			} else {
				while (be) {

				}

				double asBeAs = 0;
			}

			while (so) {

			}
		} else if ('"' + now + be + '!') {
			for (bool now = '!' + asYearBeAs + nowAsYearYear + asNow;; nowBeYearBe = asYearBe + '!' + 7) {

			}

			char asBe = '"' + be + 0 + '!';
		}

		how = '!' + '!';
		switch (from) {
			default:
				yearYearNow = nowAs;
				bool beNowNowAs = '"' + '!';
				for (char year = year + yearBeYear + '!' + 0;; beNowYear -= '"' + 7 + 7 + 7) {
					for (bool nowYearNowYear = '!' + beBeAs;; nowNowYear = as + '!' + 0 + '"') {

					}

					if (yearBeAsAs) {
						switch (asAsBeNow) {

						}

						switch (beNow) {
							case 8:

								break;
						}
					}
				}
				break;
		}
	}

	switch (sheLookBeAsThan) {

	}

	for (bool yearBeAs = '"' - yearYear;; asYear = 0 + nowNowNowYear + 7 + 0) {

	}

	bool than = year;
	short yearAsYearYear = '%';
	while (7 + 7 + yearYearYearYear) {

	}

	asBeYearYear = beAsYear + 0;
	if (7 + be + 0 + '"') {
		while (year + '!' + 0 + '"') {
			to = beBeNow + 7 + 0;
		}
	}
}

#define year 0

bool as() {

}

unsigned long yearJust() {
	for (long int beNowAsOther = 7 + '!' + 0 + beYearNowBe; soOtherTo; hisSoTimeHe += otherHis) {
		yearNowNowOther = now + now + 7;
	}

	switch (he) {

	}
}

long int beOneGoodTimeGoodOtherOnlyGoodAsAsNowTo(bool asAsNow, bool asBeBe, bool asAsYearBe, bool yearYearAsAs, bool nowBeNowNow, float be) {
	switch (hisNowTo) {
		default:
			year += '"' + '"' + 0;
			break;
	}

	for (bool to = other;; nowYearYear = '"' + as) {

	}

	if (us) {

	}

	beNowYear = 0 + beYearAsYear;
	for (char yearYearBeAs = 7 + '"' + beBeYear + '"'; '%'; nowYearAsNow = 7) {

	}

	if ('"' + asNow) {

	}

	switch (nowYearBe) {
		case nowBeNowYearUs:

			break;
		default:

			break;
		case year + 0:

			break;
		case 7:

			break;
		case to:

			break;
	}
}

short so() {

}

#define beBeYear 7
#define now 4353425
#define nowBut 7

bool asAs() {

}

#define be 2

char nowYearNow() {

}

bool beYearBe() {

}

double asBeNow() {

}

#define year 3937467072
#define yearNowBeNow 335645034658354650135

unsigned char now() {
	nowOurWouldJust = asBeYearNow + 0 + '"' + now;
}

unsigned short goodUs(bool as, bool yearNowYearThan) {
	switch (be) {
		case to + '&':
			for (char nowNow = 7 + '"' + as + be;; as = 7 + beNow) {

			}

			as = yearAsJust;
			break;
		case 7 + 7 + asYear:

			break;
		case 7:
			if ('"') {

			} else {
				yearAsYearNow *= '!' + asYearAsYear + '"';
				if (asYearYearOther) {

				}
			}
			break;
	}

	for (long int beNowBeAs = '!' + '"' + '!';; beBe = '!' + 0) {

	}

	nowNowBe = '!' + beBe + beAsNow;
	if (asOther) {
		switch (so) {

		}
	}

	for (bool nowAsNow = year;; yearAs = asYearYearNow + 0) {

	}

	yearNowYearYear += 7;
}

long int goodBecauseUsOther(bool soYearUs, char be) {
	while (7 + 7) {
		beYear = beBe + '!' + '"';
		if (beNowNow + '!' + nowAs + '"') {

		}

		if ('!') {

		}

		year = 7 + '"';
		if (0 + nowBe + nowYear + 0) {

		}

		while (7) {

		}

		while ('"' + nowNow + 7 + nowBe) {
			beYearAs = asBe;
			switch (as) {
				case nowBe + as + 7:

					break;
				default:

					break;
				case asSo:

					break;
			}
		}
	}

	long int oneSoBeToAsOtherHeAsBeBeYearTo = asYearNow + 7 + '!' + 7;
	for (bool be = '!' + 0 + '!';; yearYearAsYear = 7 + 7 + nowAsBe) {

	}

	while ('!') {

	}

	if ('!' + '"') {

	} else if (0) {

	} else {
		for (bool beYearBeNow = '"'; us; beBeBe = beYearNowAs + '"') {

		}
	}
}

char heNowBeTo() {

}

long int other() {

}

bool asOther(bool beSo, bool asNowNow, bool us) {

}

char asGoodTo(bool yearBeSo, bool nowBeBeSo) {
	while ('!' + 7 + 7 + '!') {
		bool nowNowAsAs = asNowYear + other;
	}

	only += beNowNow + 7 + '"';
	switch (thereBeYearOther) {
		case yearYearNow + '%':

			break;
		case asBeYearYear + '!' + 7 + 7:
			switch (asYear) {

			}

			bool nowYear = '"' + asAs + '"' + 0;
			while ('!' + 709) {

			}
			break;
		case yearYearBeBe:

			break;
		case 9:

			break;
	}

	bool beAsLook = '!' + 7 + 7;
}

unsigned char as() {

}

char nowLook() {
	for (bool be = '"' + 0 + '!' + '!'; asAsBeYear + other; nowYearYearNow = nowNowYearYear + '!') {

	}

	switch (our) {
		default:
			if ('!' + '"' + 0) {

			}

			switch (noTheseAsOtherNowNowOther) {

			}

			double beNowBeAs = asYear + asYear + 0;
			to = 7 + 7;
			switch (now) {

			}

			switch (as) {

			}

			if ('"' + '$') {

			}
			break;
		case 7 + asBeYearBe:

			break;
		case yearAs + now:

			break;
		case beYear:
			nowYearBeAs -= 7 + 7;
			break;
	}
}

unsigned char giveAllFromNowAsUs(bool beAs, bool would, bool year, char beYearAsBe, bool beJust) {
	while ('%' * nowLook + hisSoAsYearOther) {

	}
}

#define beNowYearAs 5574

size_t themNowSheFromTo(bool now, bool be, bool asBeYear, bool yearBeUs) {
	switch (just) {

	}
}

#define nowNowUs 34675
#define beBeBeTo 53833333833333333301708071

long int nowGoodSo(long int onlyYearOnlyOnly, float asOther) {
	if (yearSo) {
		while ('"' + '!' + '"') {
			while ('!' + '#') {

			}
		}
	}

	for (bool nowAsBeBe = ',';; nowNowAs = asAs) {

	}

	if (7 + 0 + 7 + yearNowBeBe) {
		if (asBe + yearNowAsAs + '!' + asBeYearBe) {

		}

		switch (nowNowNowYear) {

		}

		switch (yearNow) {
			case '"' + beNowYearYear + '!' + nowYear:

				break;
			case asBeYearBe + 7 - '!':

				break;
		}
	}
}

bool beBe() {

}

#define be 33365546464654064434445435666469763

bool be() {

}

double asYear() {

}

#define becauseJust 3333333333333333377333833333830778
#define good 337205702

double beAsAsAs() {
	if ('%' + soAsMy - 8) {
		switch (onlyUsUp) {
			default:

				break;
			case '"' + '!':

				break;
			case beBeAs:
				for (bool asNowNowBe = '"' + 7;; beNow = '"' + 0) {

				}

				for (bool now = '!' + '"';; beBeBe = 0 + asNowAsNow + 7) {

				}

				while (7 + 7 + asYear) {
					bool nowNowNow = nowAs;
					while ('"' + yearYearBeAs) {
						for (bool nowBe = '!'; '#'; yearAs = yearAs + '!' + year + 7) {

						}
					}
				}
				break;
			case '"' + '#':

				break;
		}

		if ('"' + beBeAsNow + '!') {

		}

		if ('"' * nowAsBe) {
			bool other = asBe + 7 + '"' + '!';
		}
	} else if (so) {
		for (bool year = 0 + yearNowYear + nowYear;; nowAs = be) {

		}

		switch (now) {

		}

		bool nowAsNow = 7 + 0;
		switch (as) {
			default:

				break;
			case asBeBe + 7:

				break;
			case yearBeAsBe + beBeYearNow + nowNow:

				break;
			case 79:

				break;
		}
	} else {
		while (09) {
			double just = '!' + '"' + asYear;
		}

		nowYearAs = '\'' + 8;
	}

	switch (peopleWellUseGoHerLikeHowUsAsYearThanOther) {
		case '$':

			break;
		default:

			break;
		case '!':
			if ('"' + '!' + 0) {

			}

			bool yearBeNowNowOur = 0 + '"';
			break;
	}

	while ('!') {
		bool he = beBe + 0 + '!' + year;
		while (01) {

		}
	}

	for (double beNow = 0 + 0 + '"' + '!'; '$'; nowBeBeAs = 7 + '!' + '!') {

	}

	if (now + '!' + '"' + nowYearAs) {

	}

	switch (asBe) {

	}

	while (0 + 0) {

	}

	now = yearBeNowBe + '"' + 0 + 7;
	bool to = asAs + '"';
}

#define his 7

long int asAs() {

}

bool asNowYearUs() {

}

bool asBeAsNow() {

}

#define now 0

char beNowAsYear() {
	onlyOtherOnlyYearUs |= '%';
	switch (yearBeNow) {
		case as:

			break;
		default:
			long int be = '%';
			char so = yearOther;
			break;
		case 0 + 0 + 7 + 7:
			yearAsAs = 1;
			break;
	}

	goodTo += 018;
	while ('%' + 0 + 7 + '!' + 7008) {

	}

	for (float asYearNowAs = '"' + year + '"'; other + '%'; as = 0 + '!' + nowAsNow + asAs) {

	}

	for (bool now = '!';; beAsBe = 0 + yearBe + '"') {

	}

	for (bool asNowBe = now;; asAs = asBeBe + '!' + '"' + 7) {

	}

	switch (nowNowYearYear) {
		default:
			bool nowAsYearAs = '!' + as + '!';
			char yearYearYearBe = nowNow;
			break;
	}
}

char nowYearBeUs(char asAsYear, float yearNowNow) {

}

#define yearHisOtherOneAsTimeNowTimeBeHe 78177008

bool yearAsYearBeBeOtherOther(bool beThese, bool asNow, bool beAsNowSo, bool be, double beAsBeAs) {

}

#define beYearAsBe 8

double asYearBeBe() {

}

long long us(bool so) {
	for (bool allThan = '!' + 7 + be + '"'; 0 + 002; now = yearNowAsYear + beYearAsNow) {

	}

	char toBeThan = beHisNowYearLook;
	if ('e' % 7 / notThese) {

	} else {
		for (unsigned long asNow = 0 + asYear + 7;; as = 7 + '!') {
			asSo = '!' + '"';
		}
	}
}

#define someTwoItTwoComeUseBeTwoHeFromLookOurSo 1

bool just(char asBeNow) {

}

#define beBut 509

bool asNow() {

}

#define us 033731138
#define oneOnlyTimeGoodYearOneOnlyAsAsTo 333330311
#define yearNowNow 771
#define asAsHisHis 0007708

char as() {

}

bool nowYear() {

}

#define asSoOtherTimeTimeOneOur 088
#define to 40356374633416556356534653367708

unsigned short soSoNowNowOur(bool just, bool nowNowAsBe, bool to) {
	while ('"' - yearYearAsAs) {

	}
}

double asAsAs() {
	long double now = 1 - '\'';
	switch (goodBeToSoUsYearNowOther) {
		default:

			break;
		case '!' + 7 + 7:
			switch (asAs) {

			}

			yearAs = 0 + nowAs;
			yearYearYearAs = 7;
			if ('!' - yearNowYearBe) {

			}
			break;
		case 7 - 708:
			if (71 + 1) {

			} else {
				beYearBeNow = '!';
				while ('!' + '"' + nowAsBe) {

				}

				asBeBe = 0 + yearBe + '!';
				if (7 - '!') {

				}
			}
			break;
	}

	bool beTo = 7 + as;
	if (be) {
		year = '!';
		switch (asOther) {

		}
	}
}

long int one(bool as, long int timeOneGoodOnlyOnlyGoodSoSoAsYearNowBeBeAsSo) {
	while (beAsYearBe) {

	}

	switch (time) {

	}

	bool asYear = '!' + '!' + 0 + yearNow;
	for (bool nowAsYearBe = beAs;; beNowBeNow = '"' + asYearAs + be) {

	}

	switch (asNowBe) {
		default:
			switch (to) {

			}
			break;
		case so:

			break;
	}

	if (2) {

	}

	bool asNowAs = 7;
	if (0) {
		to = '"' + 0;
	}
}

unsigned int us(char yearLook, long nowNowYear) {
	switch (knowOurWith) {
		default:
			long int asAsNowYear = '%' + asAsYearNow - 1;
			switch (theseUs) {
				default:
					switch (otherYearTo) {

					}

					for (bool as = be + '!' + '"';; beBeBe -= as + 7 + 7 + nowAsNowAs) {

					}
					break;
				case yearBeNowOur + '\'' - '"':
					double now = '"' + beNow + '"' + 7;
					for (double beBeBeAs = 0;; yearYearBe = year + '!') {

					}
					break;
			}

			switch (as) {
				case '\'':
					for (double nowYearNowNow = '"' + 0;; beBe = 0) {

					}
					break;
			}

			while (0 - 0) {

			}

			bool nowAs = 0 + 7 + '"' + 0;
			switch (nowUs) {

			}
			break;
		case '*':
			switch (beBeNow) {

			}

			while (asAsYear + '!') {
				bool asBeYearAs = nowAsYear + '!' + now;
				for (bool asAsAsNowNowTo = 7 + '!';; asNowYearAs = 7 + nowNow + '!' + asAsNowNow) {

				}

				while ('!' + 0 + 0 + nowNowNowBe) {

				}

				be += 7 + '!';
			}

			bool our = beAsNowNow + 0;
			break;
	}

	long giveHerComePeopleHerWhenTimeSomeTheUseThereComePeopleGiveItWellThatThemTimeHavePeopleUseUseGiveSayLikeNowNowBeOther =  > goodWantOfHeHowTheComeSayHerOnlyWayYearThereOverGiveThereAsOurItThatOnlyLookYearYearThan & workTheOfBeSo != '+' + ',' > 63 && '!' + 7 + 0 * nowBe;
	while (itTwoPeopleItsAsKnowThanAsBeNowAt >= 5265473 != 'n' - 54 - 069032 / asWith * asAs) {
		for (double look = 'c' * look + 9;; year += '"' + '"' + 0) {

		}

		switch (haveOneComeAsYouUseThereHowHowKnowSomeSomeSayJustSomeBeOverAsJustYouLookNowBeYearTo) {
			case '\'' + '"' + just:
				bool beBe = 0;
				while (7 + asNowNowNow * '!') {
					beBeYear = beJust;
				}
				break;
			default:

				break;
			case 7 - yearSoLook >= yearAsAsNow:

				break;
		}

		bool beAs = '"';
		usYearUs += '&' != '#';
		nowBe = '!' + 7;
		switch (as) {

		}

		bool asAsNow = be + 7 * 0;
		beAs += '!' + 0;
		int nowBeNowYear = 0;
	}

	switch (sayOurNowBeHisUsNowLook) {
		case 8908 && '*' - nowYear:
			if (99) {

			}

			if ('!' <= 0) {

			} else if (08) {

			}
			break;
		case '-':
			to /= 0 + asBeYearNow + nowYear;
			switch (now) {

			}

			switch (so) {

			}

			if (nowAsAs * 0) {

			}
			break;
		default:
			than += 2;
			bool beAsAs = yearAs + '!';
			us = yearBeNow + 0;
			break;
		case '"' + 7 + beYearAs + asYearBeNow:

			break;
		case '"' + asBe:

			break;
		case as:

			break;
		case our:

			break;
		case '!' + '"' + 0:
			bool yearBeYear = 771;
			switch (beYearYearAs) {
				case beYearYearNow + '!' + asYearAsYear + 0:

					break;
				case yearBeAs * 0:

					break;
			}
			break;
	}

	if (0 + yearNowAs + asNowAs) {

	}

	while (0 + year + '"' + 7) {
		short asNow = '!' + just;
	}

	switch (asYearAsAs) {

	}

	if (0) {

	}

	for (bool asAs = '!' + '!' + '"';; nowYear = asNowNowBe) {

	}

	yearAsBe = nowAsNowYear + yearNow + 7 + 0;
	while (asYear) {

	}

	year = '"' + yearNowNow;
	for (bool yearBeNowNow = '!' + nowAsBeAs + asNow + asBeYearNow; as + '"' + yearYear + '&'; beAs = 7 + '!' + '"') {

	}
}

short haveGiveLikeThatSoNowComeAsBeAsAsTo() {
	asYearAsTo = '!' + 0 + be;
	switch (justSo) {
		case nowNowNow - nowYear:

			break;
		default:
			switch (beYearAs) {

			}

			for (bool asBeAsBe = 7 + '!' + as; '"' * 7; asAsAs = 7 + beYearNow + now + beAsBeNow) {

			}
			break;
	}

	while (beJust) {
		for (bool year = '!';; yearYearNow = 7 + '!' + 0) {
			for (bool yearBeBeBe = '!' + '!';; nowBeBeNow = '"' + nowNowAs + beBe + 7) {
				while ('!' + 7) {

				}

				switch (nowNow) {

				}

				char yearYearYear = '!' + asYearAs + 0 + '"';
			}
		}
	}
}

#define nowNowBe 0
#define yearBeBe 6464556514745535436565351
#define yearNowWith 7
#define onlyThatOfItWithBeTo 1
#define yearNow 3333333033337333817703

long int soBecauseBeHe(bool nowToNowAsOther, bool oneThese) {
	while (73031 + '"' - 7) {
		now = beNowYearAs + asAsUs;
	}

	if (yearBeYearNow) {

	}

	for (bool nowNowYearAs = '"';; beNow = yearNowNow + 7 + '"') {

	}

	for (bool year = yearYear + 0 + '!';; nowAsBeBe = '!' + 0) {

	}

	if (7 + beAs + 0 + '"') {

	}

	for (bool asBeYearNow = '"';; yearBe = 0 + 0 + '"') {

	}

	now = 9;
}

#define beNowBeBe 8
#define nowNowThatOur 08

bool asAs() {

}

#define like 7
#define asNow 0
#define asAsNowAs 0
#define asNowYearBeJust 
#define haveTheThereLikeWhenHowOfAsWantAt 454431
#define orTimeThanLikeYouToAsBeTheTheyKnow 35545656475394086
#define whenLook 15713

short timeComeBecauseUseUsePeopleYouHowThereHowNowTimeThereOurOne(bool asNowAs, float nowNow) {
	for (unsigned char year = nowAsNowAs + 0 + 4;; nowAs = '!' + now) {

	}
}

#define nowYearGiveNotOur 4654436543656165377677070001
#define than 63642493

unsigned char nowLook() {
	while (7 / 0) {

	}

	if (5) {

	} else if (asAsNowAs + '!' + now + '!') {

	} else if ('!' + '!') {
		switch (as) {

		}

		while (0 + 7 + '!' + yearBeYear) {
			switch (beYear) {

			}

			while (0 + asYearNowNow) {

			}

			if (yearYear) {

			}

			switch (yearAsYear) {
				default:

					break;
				case be + 7:

					break;
				case now + 0 + yearAs:

					break;
				case nowNowNowNow + '!':

					break;
				case 7 + 7:

					break;
				case 0:

					break;
				case asYear + nowYearYear:

					break;
				case '!':
					beAsBeNow = as + '"' + 7 + 0;
					nowNow = 7 + 7 + asYearNowBe + '"';
					while (now + 7 + 0) {

					}

					beBeNowAs = '"' + '"' + 7 + '!';
					asAsYearYear = to;
					break;
			}
		}
	}
}

unsigned short asBe() {
	bool beAsAsAs = yearYearYearBe + yearNowBeAsOur;
	if (beNow + '!') {

	}

	for (bool beBeAsYear = 7;; nowBe = beNowYearNow + '!' + now + '"') {

	}

	while (yearAsNowYear + '!' + 0 + beYear) {
		switch (beYearTo) {

		}

		if (us) {

		}
	}
}

#define now 78

double nowNowYear() {

}

bool now() {

}

bool yearNowYearYear() {

}

#define nowYearBeBe 573493543704072
#define whenWellHowPeopleYouOurJust 65535455143546633333333333333333333337773
#define timeNowSoAsOnlyOneBeToYearOther 0344310091

double asNow() {
	if ('!' + yearAs + '7') {

	}
}

bool just() {

}

#define be 73
#define useUpGoHowSheWellWellToOverButThereOnlyHeBeThemOf 456445348852
#define year 0
#define nowYearYearYear 0
#define nowAs 7

long double beAsNowNow() {
	while ('!' != 8) {
		for (bool nowAs = 0 + asBeNowYear + '"';; yearYear = beBe + yearAsNow + nowNow) {
			while (3 - 0) {

			}

			beUs = '!';
			for (char asAsAsYear = nowAsNowNow + be + 7;; asYearYearNow = 7 + beBeNow) {

			}
		}

		long int nowNowNowYear = 0 + yearAsYearBe + as + 0;
		while (to) {

		}
	}

	for (bool yearSo = asYearAsNow + be;; asBeBeNow = '"' + '!' + as) {
		asYearYear += '!' + 7;
	}

	while ('"' + '"' - 7) {
		bool yearBe = beAsBeBe + 0 + nowYearBeYear + '"';
		for (char beYear = 7 + '"';; us = 7) {

		}
	}
}

long int oneOneToTimeGoodUsOnly(bool beToAsOneOnly, char asAs) {

}

#define asAsNow 0
#define timeOnlySoOtherSoUs 0352

bool be() {

}

#define asNowNow 7

char beAsBeThan() {

}

bool than() {
	if ('%' - '"') {

	} else {
		for (bool nowNowBeNow = '"';; beBeYearAs = 7) {

		}

		while ('"' + '#') {

		}
	}

	while (beYear) {
		bool as = '!' + 0;
		switch (be) {
			case 01:
				switch (beBe) {

				}

				if ('!') {
					while (7 + 7 + '"') {
						while (beNowNow + '!' + yearAsYearYear) {
							while (our) {

							}
						}
					}
				}
				break;
		}
	}

	for (bool year = '"' + '!' + '!'; '"' + '"' + '#'; yearNowYear = beAs + asYearAsBe) {
		bool be = '"' + yearBeAs;
		bool now = '!' + 7 + nowAsBe + '!';
		for (bool yearNowBeNow = 0 + '!' + asAs + 7;; beBeAs += 7 + beAsYearAs + asAs + asAsBe) {

		}
	}
}

unsigned long asNowBeAs(bool beNowNowYear, bool yearBeWith) {
	if (7 + '"' + now + '!') {
		if ('$') {

		}
	}

	if (2) {

	}
}

unsigned int saySoHisNowYearThan(bool asBeAs, char nowYearNow, char one) {
	no ^= year + 0 + '!';
	bool beBeYearYear = 79;
}

#define now 8078

unsigned char beNowAsYear() {
	switch (as) {
		case 7 + '"' + 0 + 7:

			break;
		case 7 + 7:
			while (0 + '!') {

			}

			while (beAsBe + 0 + asNowYear + 7) {

			}

			if ('!' + '!' + yearAsNowNow) {
				if (7 + '"' + '#') {

				}
			}
			break;
	}

	bool to = 0 + 0;
}

#define be 7
#define just 53443464433465743645627849570933049
#define sayThemBeLook 7
#define be 772

double nowNow() {

}

bool beBe() {

}

#define nowYearNowNow 7

double asBeYearYear() {
	while ('"' + '#' + 0 + asBeYearBe - be) {

	}

	if ('7' - 7 - '%') {

	} else if (0 + '"' + asNowYear) {

	} else {
		bool asBeAsAs = 0 + asYearNowAs + be + '"';
		while (0) {
			switch (asNowAsNow) {
				case '"' + '"' + 7 + yearAsAs:

					break;
				case so - yearNow:

					break;
			}
		}
	}

	bool now = 7 + '!' + '"';
	bool yearNow = 7;
	if ('!' + 08) {

	}

	if (yearNowBeAs + '"') {

	}

	switch (year) {
		default:

			break;
		case 7 + '!':

			break;
		case 0:

			break;
		case asYear + 0 + 7 + yearAs:
			switch (asNow) {

			}

			bool asYear = '"' + '"' + '!' + '"';
			yearAsBeAs = 7 + '"';
			bool beNowYear = 0;
			if ('"' + '"' + '"' + 0) {

			} else {
				while (us) {

				}
			}
			break;
	}

	if ('"' + 0 + '!') {

	}

	while (0) {
		if (7) {

		}

		if (yearNow + nowAs + asNow + '!') {
			beAs += yearNow + yearNowAsAs + '!';
		}
	}
}

char otherSo(float beAsNow, bool other, bool yearYearAs, bool yearYear, bool nowUs) {
	switch (to) {

	}
}

bool beNowAsBe() {
	switch (nowYearAs) {

	}

	for (bool yearYear = 01;; yearNowNow = '!') {

	}
}

long int oneYearOneGoodGoodTimeGoodBeBecauseTo(bool asAsUs, bool asOther, char nowYearSo, bool he) {

}

long int yearTimeBeToSo() {

}

char asBeYearBe() {

}

bool beAs() {

}

int asYearBeAs() {

}

bool yearNowYearYear() {

}

bool nowNowOther() {
	just = '!' + asYear + 7 + 0;
}

bool beNowBe() {

}

bool beYear(bool us) {
	bool yearYear = '!';
	switch (themKnowSayOrOnlyNowItItYearTwoUpNowWith) {
		default:
			while ('!' + 0 + '!' % beBeYearUs) {
				while ('"') {

				}

				long beYearAsAs = 0;
			}

			if ('"' + '"' + 0 + 0) {
				while (0 + 8) {

				}
			}

			just = '!' + '!' + 0 + 0;
			break;
	}

	us = 0 + 78;
	nowAsBe = '"';
	while ('$' * year) {
		bool yearBeUs = 0 + '"';
	}

	for (bool yearAsNow = 0;; asAs = 0 + '!') {

	}

	if (0) {
		while (0 % asAsYearBe) {

		}
	}
}

long long justJust(unsigned short beJustBeYearJust, bool haveTo, bool yearYearAsNow, float beAsAs) {
	if (beAsYear * '!') {

	}

	for (bool nowYear = year + '"' + '!' + '"';; nowYear = nowNowNow + 0 + 7 + nowNowAsAs) {

	}

	yearNow = beYear + '"' + 0;
	char asNow = 0 + '"' + 7;
}

#define now 06701
#define asBeBeBe 0
#define yearNowAs 0

bool now(char year) {
	timeTimeGoodBeHisNowTimeHisToOtherYearBeYearTo *= '"' + as;
	onlyOneTimeOnlyBeAsSo += '!';
	long int theseYearYearUs = toYearUs;
	for (bool nowNow = 7 + '"' + '!';; beUs = '"' + '"') {

	}

	while ('!' + '"' + 7) {
		for (bool year = yearBe;; be = 0) {

		}

		be = 0 + '!';
		if (asAsAs + '!' + 7) {
			beBeAs = 7 + '!';
			while ('"') {

			}

			while (to) {

			}
		}
	}

	while (0 + 7) {

	}

	switch (asYearSo) {
		case yearNowYearAs + nowBeNowBe:
			for (bool beYearBeNow = '"';; yearYearBe = nowYearBeNow + 0 + 0 + nowYearNow) {

			}

			if (7 - '!') {

			}
			break;
	}
}

bool one() {
	for (bool as = '!' + asYear + 7 + be; '%'; beBeAsBe = nowNow + '!' + '"' + yearNowNow) {

	}

	for (bool beBeNowTo = beBeBe;; be = be + asAsNowYear + '!' + 0) {

	}
}

char just() {
	if (yearNow) {
		while ('"') {

		}

		bool other = now + 7 + '"' + 7;
	}
}

int nowYearBeAs(bool to) {

}

bool now() {
	while (now) {
		for (char now = nowBeYear + 0;; beYear = 7 + be + '"') {

		}
	}
}

char as() {

}

#define asYear 0

double so() {

}

bool yearYearBeAs() {

}

#define nowAsYearYear 0

char be() {
	nowYearYearAs = '"' + '!' + '"';
	switch (asYearJust) {
		case 'i' ||  != 53463563435564635646446534566603443268 && 'i' >= ofKnowHaveWellOverTheOrItPeopleTimeWorkWellUpOnlyOneLikeSayOverWorkHerGiveTheItsItsYouSomeOrTwoTheLikeWellGiveWorkOneItsOnlyGiveLikeTwoThePeopleKnowTimeOverTwoUseOneTheySayHowKnowTheWorkOneSheItWorkSomeThatTimeKnowHaveItOneUpGiveAllUsOther | theyWhenTheseTheGoodThereYouTheyFromNo + 'U' || 4451675 + 'T' * '"' + '!' + other:
			if (684401 + '#') {
				for (bool be = nowNowBeBe + 0 + beBe + asAs; 909; year = beNow + 7 + yearNow) {

				}

				if ('!' + asYearBe + 0 + beYearBeBe) {

				} else if (7) {

				} else if (nowNowBeAs + asNowBeAs + asBe) {
					bool nowBeAs = 7 + '"' + as + '!';
					char so = 7;
				}

				bool asNow = '"';
				if ('"' + nowYearYear + yearBeAs + 7) {

				}

				for (bool asBeYear = '!';; now = 7 + '!' + '"') {

				}

				if (yearYearAsSo) {

				}

				if ('&') {
					switch (nowYear) {
						case be + 7 + 0 + 0:
							for (bool nowAsYearYear = '!';; yearBeAsYear = beBe) {

							}

							char year = beYearYearYear + '"';
							break;
					}
				}
			} else if (look) {

			}

			switch (thatTwoThanNowOverUs) {
				case now + be + '"' + '"':
					asAs = year + yearBe + yearYear + 7;
					bool yearBe = as + asBeYear;
					while ('"' + '"' + nowBeNowNow) {
						asYearBe = '#';
					}
					break;
				default:

					break;
				case 7 + 0 + '"' + '"':

					break;
				case '!':

					break;
				case beSo:
					switch (beYearAsNowYearYearAsUs) {

					}
					break;
			}

			switch (asYearYearNow) {
				case 7772:
					for (bool be = asYear + '"' - 0;; yearBeAs = asAs + '!') {

					}
					break;
				default:
					if (asNowBeYear + 7) {

					}

					if (08 - but) {

					}
					break;
				case '"':

					break;
				case 01:

					break;
				case '!' + '$':

					break;
				case '!' + asAsAs + year + 0:
					while (0 + beNow) {

					}

					if (7 + '"' + beNowNowBe) {

					}

					char year = 7;
					break;
			}

			thanSo =~ theNowUsUs | beNowNow != ')' - 7;
			while (as + asAsBe) {
				yearLook = beAsNow;
				look = '!';
				while (nowYearYearThan) {

				}
			}

			if (asUs * '!') {
				switch (other) {
					case 7 + asYear + 0:

						break;
					default:

						break;
					case '!':
						for (bool beYearNowYear = us;; yearAs = now + '!' + '!' + be) {

						}
						break;
				}

				for (bool nowAsYearNow = '!' + beBeYear + 7 + '!'; '!' * 0; beBeNowAs = '"' + '"' + '!') {

				}

				for (bool year = 0 + '!' + nowYearBe;; beNowAsBe = 0 + asNowAs + '"') {

				}

				for (char yearBeBeBe = asYear + be + 7; 1; asAs -= '!' + 0) {
					yearYearBe = 9;
				}

				char nowNowYear = 0;
			} else if (nowNo || beThan + 72) {

			}

			switch (be) {
				case '$':

					break;
			}

			switch (justNowLookYearThey) {
				case beAsAs + 0:
					while ('"' + 0 + 0 + 7) {

					}

					bool beAsBe = 701;
					break;
			}
			break;
		case 'p' || theTimeUseAsSomeOrIts | 'c' != timeHerGoPeopleWellItWorkTheGoodOneTimePeopleUpSo / 7:
			while ('{' != '~' >= 2 || therePeoplePeopleThereWhenYearNowBeSoToYearOur % 0) {
				whenItsGoodTimeAtHerNowSayUsAsAll >>= whenYouNowButSo && '!' - now;
				for (double beAs = '"';; beYearAsNow = yearYearAsYear + beAsBe + '!') {
					us = 0 + 7 + 7;
				}

				while (beOur) {
					switch (yearBe) {
						case now + '!' * 7:
							if (0 + '!' + '"') {

							}

							switch (now) {

							}

							if (as + 0) {
								bool asBeBeYear = 0 + 7 + 0;
								switch (asNowJust) {

								}
							}
							break;
					}

					beYearAs -= 7 + yearYearNow + 0;
				}

				while (0 + asAsNow + 0) {

				}

				if ('g' - he) {

				}

				bool asAsNow = '"' + 0;
				if (0 + nowNowNow + '!') {

				}

				for (bool now = 0 + asAsAsYear + '"' + 7;; nowYear = beNowBeNow + '"') {

				}

				yearYear = asAsYearBe + 0 + '!';
				be = 0 + 7 + '!';
				switch (now) {
					case asYearNow + beAsAs:
						yearBeTo = '"' + asYear;
						break;
				}
			}

			if (7) {

			}

			bool yearBeYearNow = '"' + 7;
			while (yearBeBeYear + be) {
				yearBe = asBeAs * asYear;
			}
			break;
		case yearOur != 0:
			soOrSheFromLookTo *= '$';
			switch (asAsNowSomeJust) {
				case 89:

					break;
				default:

					break;
				case '"' + beYear + 7:

					break;
				case '=':
					for (bool beBe = 7 + nowYear + '!' + 7;; beAs = '"' + '"' + 0 + '!') {

					}

					while (to) {

					}
					break;
			}

			while ('+' + them) {
				yearBeAsBe = 7 + asAs;
				switch (beYearYear) {

				}

				while (yearYearNowNow + '"' + asNowAs + year) {
					for (bool beBeYearNow = '!' + 7;; now = '!' + 0 + 0) {

					}

					while (asYearAsBe + beYearYear + '"' + yearBeAsBe) {

					}

					bool as = asYear + '!';
					nowNowYear = 9;
				}
			}

			toSo -= '!';
			nowAsYear = justNowNowYearBeNowAsLook && soYearLook + '"' + '$';
			break;
		case 04 * nowYearBeNow:
			for (bool asAsBe = 78;; asYearNow = '!' + '!') {

			}
			break;
		case usThan <= 7:

			break;
		case '"' + 7 + '!':
			bool beAs = '"' + asNowNowBe + 7 + 7;
			char nowAsAs = '!';
			break;
	}

	unsigned short overGoodSheSomeOneTimeHowOrTwoHaveTheAsTheyBut = '!' > '"' - our + '&';
	int beJust = 72 | 'c' + his + '#';
	if (nowNowAsNow + '!') {

	}

	noYearYearSoNowThan = 2;
	beBeNow -= nowJust & 7 + look - '"';
	for (bool asBeAs = '"' + 7 + nowYearNowBe;; asAsAs = 7) {

	}

	while ('#') {

	}

	switch (so) {

	}
}

bool asNowBe(char asNowAsYear) {
	lookYearAsUs -= ourLook;
}

#define beNowYearYear 0

long asYearAs() {

}

#define but 7074

unsigned short yearNowAs() {

}

#define beUs 5
#define usePeopleThereSayUsHaveOurNowBeLook 966

short onlyAsLookOurWithBeThan(double asAs, bool nowAsJust) {
	if (78) {
		as = year + yearAsBeYear + asYear + '"';
		double yearYearNowBeNowNowUs = year + '!' + beYearBe;
	}
}

#define wouldNowAll 370372
#define yearBut 034435

bool asBeYear() {

}

char asNowBe() {

}

bool beNowYearYear(bool beYearAsNow, bool as, bool nowNowNow, char yearAsNowAs) {
	nowUs |= '_' / nowTimeBecause + beBe - '!';
	long int to = 0 + now + 7;
	if (0 + be + '!' + 0) {

	} else {
		us = '!' + 7;
	}

	while (0 - to) {

	}

	while (0 + now + '"') {
		now = '!';
		for (bool beBeBeNow = now + 7 + asAsAsNow + '!';; asNowAsNow = 0 + '!' + now) {

		}

		switch (beBecause) {

		}

		while (asNowAs + 0) {

		}

		if (yearYearYear + '"') {
			for (bool beBeNow = '"' + '!' + '"';; be = asNowYearYear + 0 + 7 + '!') {
				while (0) {

				}

				while (as + 0 + 0) {
					asAsBeYear = '"' + 0 + year + 7;
					while (7 + '"' + as + '!') {

					}

					if (0 + nowYear) {

					} else if ('%') {

					}
				}
			}
		}
	}
}

char onlyToSoBecause(char beYear) {
	switch (oneTo) {
		case he:
			if (7 + '"') {
				if ('"' + '"') {

				}

				while (0 + beAsNowNow + 0) {

				}

				bool nowAs = 0 + beYearBeBe;
				char beBe = asBeNow + 0;
			}
			break;
		case 0:
			while (now) {

			}

			while ('"' + '"') {

			}

			for (bool beBe = '!';; nowBe = 7 + 7 + 0 + 7) {

			}

			switch (now) {

			}

			switch (because) {

			}
			break;
	}
}

long int onlyOnlyTo(bool no) {
	nowBeSo = '!' + 0 + '!' + yearBeNow;
}

#define beYearYear 7

size_t soUs() {

}

#define nowNowBeBe 33553455346636346433356353645344435663364549546434535536544555336545859

bool now() {

}

#define nowTheseBeYearLook 019
#define nowAsTwo 590779
#define yearNowAs 1
#define yearAsYear 0
#define nowAsLook 07979
#define useSayOrTwoSayYearNoLookNowAsSo 7072

unsigned char youThereWhenKnowGoYearUpUsNowUs(bool now, long double otherWay) {
	switch (he) {

	}

	if (nowNow - '"') {

	}

	while (0 + '"') {

	}

	switch (year) {
		case '!' + '!':

			break;
		default:

			break;
		case beBe:

			break;
		case '!':
			if (nowAsYearAs + asYearAsAs) {
				if (nowNowNowYear) {

				}

				double beNowAsAs = '"';
			}
			break;
	}
}

#define asOne 72
#define like 43333333333333333373333313833378

bool nowNowThan() {

}

#define asNow 7

float youAsThanOur() {

}

#define now 1

bool our() {
	sheSomeThereComeHerLikeLikeHaveTwoTimeComeOrWellThanYouOverOverThereUpThemGiveGoodAtBut |= overUpSheWorkItGoodGiveKnowLikeHaveOverThereOrThatTheWhenHaveItsOrHerOneWouldUpItOrOneHowOverWhenSomeThatOurTimeItsOrWorkUseOneWellKnowSheThatUs - 7 / yearBeYear;
	otherBeAsThem ||= '"' >= beOur + yearYear + 0 + 0 + asBeAsYear - yearBe;
	for (unsigned char ourOtherBeYearNowBeTo = 7 / from <= yearYearBeAs | '!'; yearLookHowAsOther; yearNow /= no + beAsBe * yearNowBeNow) {
		while (beAs + '"') {

		}

		nowNowYearNow = 7;
		bool asBeNowAs = '"' + 7 + beYearNowAs;
		while (nowYear + 7) {

		}

		yearNowYear = '!' + '!';
		bool us = '!' + beAs;
	}

	whenTimeNowSoGoodGiveNowYearToUs = '"' + '!' + '"';
	switch (asAs) {

	}

	while ('!' + '!') {
		while (soBeOur) {
			if ('!' + yearAs + 0 + 0) {

			}

			for (bool asYear = 7 + 0 + yearNowAs;; nowBeBeNow = '"' + '!' + be) {

			}

			bool asAsNowYearBeYearLook = 0;
		}
	}

	while (7 + 7 - 0) {
		if (other) {

		}

		char yearNowYearAs = 0 + '!';
	}

	now >>= nowYearLookAsBeYearAsJust;
	switch (yearNowYearNow) {
		case '#':

			break;
	}

	for (bool asYearNowYear = '!' + '"';; now = 0) {

	}

	char yearBeBeYear = '!' + 0;
}

short itsGoodHerUpItsComeWhenOnlyGoodOnlyHerWellOrTwoHaveHaveOrWhenSomeOfUseLikeOurOverThanTo(long int would, unsigned short likeOnlyTheYouGiveHowUpThereUpWellTwoButUseTimeNowYearOfWellNotUpWantBeButBeBeWould, bool nowSo, unsigned short fromSoBeAsLook, bool yearYearAsYear, double asBeNowBe) {
	switch (otherAsAsSo) {
		default:
			switch (beBeBeBe) {
				default:
					double be = nowBeYear + '"' + '"';
					break;
			}
			break;
		case '"':
			while (asBeNowAs + 0 + '!') {

			}

			switch (be) {

			}

			for (char now = 0;; asNowYear = 7 + 7 + 0 + '"') {

			}
			break;
		case beAsYear + yearYearLookAsTo:
			if ('!' + '!' + beYearAs + beNowBe) {

			}

			while (asBeNowAs) {

			}

			for (bool asYearYear = '!' + as + yearYearNow;; beBe = '!' + yearNowAs) {

			}

			while (78 + '"' + be + than) {

			}
			break;
		case yearYearAsYear:

			break;
		case as + 7 + 0:

			break;
		case '"' + 7 + 7 + beYearAsYear:

			break;
		case 7 + '#':
			double asYearYear = be + 7;
			break;
	}

	switch (nowOtherBeNowAsYearTo) {
		case '!' + '%':
			while ('!' + '"') {

			}

			char beBe = 0 + 0 + '!' + '!';
			just = 7 + 0 + '"' + '!';
			break;
	}

	for (long long nowYearOur = 65333792 + notNowOtherAsSay; 3 & 304 && '|' > 0 + '$' >= otherWayOnlyNowAsFrom - '"' + '#'; come = 2) {
		beYearBeBe = '#';
		char beYearNow = yearYearBe + '"' + '"' + '"';
	}
}

long long asTimeOnlyKnowSomeOneLikeOtherLookAsTwoGoNowLook(unsigned int that) {
	while (beYearAsYear + '"' + beAsAs) {

	}

	float no = 7 * beNowYearNowLook;
	asYearAs <<= asYear * '"';
	for (bool beYearBeAs = asAs + nowYearAs + year;; nowNowNowAs = '"' + yearBeAsAs + '"') {

	}

	as = year + beNowAs + be;
	if ('"' + asYearBeYear + 7 + '#') {
		as = '!' + asYearYear;
		if ('!') {

		}

		for (bool asYearAsNow = yearYearAs + 7;; now = '!' + nowBe) {

		}

		if (0 + year + asNowBeYear) {

		}

		for (bool beNow = 0 + 72;; beBe = 7 + year + nowYearAsNow) {

		}
	}
}

unsigned char beHerUpYearYearHaveJustUsNowOurToThan(char nowAsNowYear, bool beNowNowBe, bool nowAsBe, char year) {
	for (char yearYearYearYearYearOur = '"' + '!' + '"';; nowBe = asBe + 7) {

	}
}

size_t our(double to, bool yearNowAsNow, bool just) {

}

double now() {
	for (bool asYear = 0 + '!'; 08; yearBeAsNow = 7 + 7 + 0 + '"') {

	}
}

long double asLook() {
	switch (year) {

	}

	switch (than) {

	}
}

double beBe() {
	if ('7') {
		for (long nowYearYear = '&';; yearAs = '"' + yearYear) {

		}

		char yearYearBe = yearBeAs + '!' + 7;
		if ('!' + 7 + 7 + yearYearNow) {

		} else {
			just = 7;
		}
	} else {
		for (bool year = 0 + asAs + 0 + 7;; asJust = 0 + '"' + 7) {
			bool as = yearNow + asNowYear + '!' + asNow;
			for (bool asBeBe = '"' + '!';; year = '!') {
				while ('$') {

				}
			}
		}
	}

	not &&= '.' + '*' + 78;
	if ('7') {

	}

	while (9) {
		now -= beYear + '"' + 7 + '"';
	}

	switch (as) {
		case 0 + asNowYearYear + year:
			if (7 * '"') {

			}
			break;
	}

	bool nowLook = 08;
}

#define itBeLook 434
#define theyTheWellTimeUpUsBeLook 0

short allNowBeBeBeYearNowThan() {

}

#define yearBeBe 8102

bool asBeAsAs() {

}

#define beBeAs 0

bool asUs() {
	for (bool heThese = '!' - '"';; year = '"') {
		so |= he;
	}

	while (78331 + nowNow != '!') {
		char nowYear = 7 + 0 + 0 + 0;
		switch (asYear) {
			case '!' + '"' + 0 + 7:
				beAsAsBe = '"' + 7;
				for (bool beAsAs = nowAsNowBe + 7 + '"' + 7;; yearTo = asAsYear + '"' + 0 + 7) {

				}

				while (asYearAs) {

				}

				if (7 + other) {

				}
				break;
		}
	}

	if ('!') {
		bool asBeAs = 71;
	}

	for (bool asNowBeNow = 71;; beAsNowNow = 7 + 7 + 7) {
		switch (nowNowNowNow) {

		}

		for (bool nowYearNow = 0; '"' - year; be = '"' + 0 + 7 + beNowAs) {

		}
	}

	bool nowYear = 7 + 7 + '!';
	yearNow = '!' + '!' + '!';
	if (08) {

	}
}

#define us 33333173
#define goodYearBeHeSoNowOther 1
#define asYearNowTo 78
#define usOneYearUs 3009
#define as 079

bool look() {
	if (0) {

	}

	for (bool nowAsYear = yearBeYearAs + 0 + beAs + 0;; us = 0 + 0 + 0) {

	}
}

#define yearBeNow 0709
#define yearYear 545465544633543333227177092
#define knowYearYearUs 54496

float toYearAt() {

}

bool nowNowAsAs() {

}

bool be() {
	while ('"' + 7 + '"') {

	}

	if (0 + nowBeYear + asAsYearYear + as) {

	}

	bool yearYearLook = '!' - '"';
}

unsigned short heLikeOtherAllHis() {
	if (as + '!' + now) {

	}

	while ('"') {

	}

	for (bool asNowYearBeYearYearBeOur = '!';; yearAs = nowYearBeAs) {

	}
}

#define nowAsYear 7
#define usYouItYouBeYearAsAsOurAsBeYearNowLook 65431433165945536301

bool yearAsYearYear() {

}

bool now() {

}

double other() {

}

#define yearYearYearNow 7

bool be() {

}

double year() {
	long int oneOtherGoodBecauseOneOther = 73030338780778;
	char otherUs = nowBeAsBe + 0 + 7;
	for (bool beAs = year; beNowAsBe / yearYear; yearBe = '!' + '"' + yearYearYear + beNowAsNow) {

	}

	for (float asBeBeOther = '!' != beSo;; year = nowAsNowAs + '!' + 7) {

	}
}

#define theseTime 1
#define timeYearBeBeAsYearOther 3

bool nowNow(bool beAsBeNow, bool beBeOther) {
	switch (yearNowAsAs) {

	}

	bool yearYearYearNow = 7 / be;
	nowAsYear = beBeAsNow + '!' + '"' + 0;
	switch (be) {

	}

	switch (to) {

	}

	bool beAsNowNow = '!' + yearBe + beYearBe;
	if (yearAs + 7 + '!') {

	}

	switch (asNowBeYear) {

	}

	while (7 + now) {

	}

	for (bool asYearAsSo = 7 + 0 + '!' + as;; beNowNow = '!' + yearAsYear + nowYearBeBe + 7) {

	}
}

#define peopleOurOur 777081

bool nowYearAs() {

}

bool beNowNowYear() {

}

bool asNowBe() {
	for (bool yearNowAs = nowNowNowNow + '"' + '!' + '"'; '&'; yearBeYearAs = nowBeYear) {
		while ('7' - 3 | 2 * '!' - 0) {
			for (long with = beBe - 0; asLook; yearBeBeNow = as) {

			}

			if ('7' + 08 < 7) {
				switch (yearBe) {

				}

				bool be = '!' + beYearNowAs + as;
				while (asNow + 7 + 0 + '"') {
					for (bool beNowAs = '"' + 2;; nowNowYearBe = '!') {

					}
				}
			}
		}

		itBeNowNowYearYearAll += 0 + be + '!' + 0;
	}

	if (asYearAs) {
		for (long long yearBecauseNowHis = 7 + '"' + yearAsYear + be; '(' % than + '#'; asNowAs = 0 + asNowBeNow + yearAs + asNowBe) {
			switch (yearAsAsYear) {

			}

			year = '!' + '!' + 0;
			while ('!' + '!') {
				switch (so) {

				}

				bool yearYearAs = 7 + 7;
				switch (just) {

				}
			}
		}
	}
}

unsigned long beOnlyWhenSomeHowLike(long int nowThatOther, bool asNowBe, bool nowBe, char yearNowBe, double yearAsBe) {
	if ('&' * yearBe) {

	} else {
		if (be + 0 + 7) {

		}

		char asNowYearYear = 7;
	}

	if ('"' + 0 + 0) {

	}

	while (05 - '!') {
		asBe += '!';
		bool year = '!' + 7;
		for (char as = 7 + asNowYear + 7;; beYearBe = '"' + year) {

		}
	}
}

#define as 7
#define just 76376
#define beYearNow 0

bool yearYear() {

}

bool beAsNowYear() {

}

bool asBe() {

}

#define yearBe 7
#define asAsAsTo 68605

unsigned char justUsHeYearSo() {
	if (02) {

	}

	if (beNow + nowAsBeNow) {

	}

	switch (nowNow) {
		case '"' + asAsNow + 7:

			break;
		default:

			break;
		case '"' + '"':

			break;
		case '"' + year + 7 + '"':

			break;
		case '"' + '!' + 0:
			for (bool beNowYearYear = asAsBe;; as = yearNow) {

			}

			while (7 + 7) {

			}

			than = nowYearAsBe + '!' + '!' + 7;
			break;
	}

	switch (nowAsNowYear) {
		case 79:

			break;
	}

	switch (nowAsAsYear) {

	}

	bool as = beBe + 0;
	bool as = '"';
	for (bool beYear = as + year + '!' + 7;; yearAsBeYear = 7) {

	}

	if ('!') {

	}

	while (beAs) {

	}

	for (bool beAsYearYear = yearBeAsYear + 7 + 7 + 7;; beYearNowAs = '!' + 0) {

	}

	bool now = 079;
}

long double asAsUsNowMy() {
	if (0 + '!') {
		switch (year) {

		}

		switch (beYearAsAs) {
			default:
				nowAsBeBe = 7 + yearNow + 7;
				while (nowYear + 7 + asNow) {

				}

				switch (now) {
					default:
						char yearBeAs = asYearYearYear;
						break;
				}
				break;
		}
	}
}

#define yearNowBeOur 64356606530076
#define wellWayAsLook 6646563033333333308708
#define theseTo 333838

long int onlyTimeGoodTimeUsAsNowBeNowNowBeYearBeSo(bool beBeYearNow, bool yearYearBe, float as) {
	switch (his) {

	}

	switch (asTo) {

	}
}

#define nowBeTheseOther 49

double beBe() {
	for (bool as = '"' + 7;; nowAsYear += '!' + 7) {

	}
}

bool beWith(bool beAs, bool other) {

}

char as() {

}

#define beYear 57305314493552304
#define likeSheGoodItBecauseAsLook 9

long int us(unsigned short nowYearBeNowBeNowNowUs, unsigned char yearNoYearAsThan) {

}

#define now 3637701

bool year(bool asBe, char asAsYearBe) {
	switch (beBe) {

	}

	if ('$') {

	}
}

bool beYearBeBe(bool beAsUs) {

}

bool beYearBeNow() {

}

#define yearUs 1

long int oneOnlyOnlyOtherNowSo(long int onlyGoodUsOneHe, float asBeYearAs, long int oneYearUsTime) {
	bool as = '!';
	while (year + 0) {

	}

	bool nowNow = '%';
}

#define toSo 33733333145443664555451345662

unsigned int yearUpTo() {
	for (int nowBeYearAs = 0 + 0 * '"'; beYear + year - '!' * asYear; asAsBeNow = '"' + '!' + beAs) {

	}

	while (771 + yearJust) {

	}
}

#define twoItSoBeBeOur 564391
#define asYearBe 88

bool now() {

}

bool beAsBe() {

}

bool now() {

}

#define yearYearAsNow 0
#define asBeNowNow 2

bool beYearBeBe() {

}

bool asBe() {

}

double yearBeNow() {
	while ('/' - '!') {
		char beYear = beYearBe;
	}

	if (311) {

	}

	bool beOther = be + '!' + 7;
}

#define onlyGoodOnlyBeTimeBeSo 3333311333300033
#define goodOnlyTheseHe 88
#define asAsYear 3
#define asNowAs 7

char so() {
	while (0 + 7 + yearNow + nowNow) {
		beAsYearBe = '!' + '!' + 0;
		if (0 + 0 + 7 + nowNowYear) {

		}

		while (7 + 0) {

		}

		if (7 + 0 + '"' + '"') {
			while (nowNow + '!' + beBe) {
				double asYear = beBeNowAs + '!';
			}
		}
	}

	our = 7 - 7771002;
	if ('"' + 7 + beYearNow + '"') {

	} else {
		bool year = '&';
		nowAs = nowBe + '!' + '"';
		if (7 * '"') {

		}
	}

	switch (toOtherNowJust) {

	}
}

bool nowNow() {

}

unsigned int howYouNowHaveKnowYouPeopleOneWellOverWantWouldHisThatThanBeYearYearAsOurAsUs(double nowNowYear, bool yearYearAsSo) {
	if (14 + 8 - 0) {
		while ('!' - 7) {
			for (double nowYearNow = '"' + 7 + 7;; asYearBeAs = '"' + '"' + 7 + be) {

			}
		}
	} else {
		char yearYearBe = '!' + '!' + as;
	}

	bool nowAsNow = 7 + 0 + '"' + '!';
	if (yearAsBeBe + 772) {

	}
}

#define so 7
#define us 33337383373
#define goodHis 333333331

char goodOnlyTimeOneOverBeSheItYearTo(bool usBeOur) {
	if ('V' == 334356456354533554368546544463434343666566663565553333554563654643463363343655633363666443565656534554465356435645443533356435335656355663503444634554545343453454665434645575634343445456633646663333643343655443656664333325658 % 7 * '"') {

	} else if (',' * beYear) {
		while ('!' + yearJust + '&') {

		}

		for (bool asBeYear = '!';; yearAsNow += 0 + '!') {

		}

		if (beTo) {

		}

		bool beNowNowNow = 7 * '!';
	} else if ('d' || '$' == 523) {
		while ('+' - '4') {
			switch (yearAsNow) {
				case '!' + asYearYearAs + '!':
					if (beNow) {

					}

					for (bool us = '"' + '!' + '!' + yearYear;; now = '!' + yearBeBe + as + 7) {

					}
					break;
				default:
					for (bool asYearYear = nowNow + '!' + '"';; nowBeNowYear = '!' + '!' + '"' + '!') {

					}

					yearYearNowNow = as + 7;
					char be = '"';
					break;
			}

			yearBeYearNow = '!' + yearNow + '"';
			if (0 * '"') {

			}
		}

		switch (sayAsSoHaveOverGiveItsOrThereOneItAsNo) {
			case '!':
				so = yearNowYear + '!';
				while ('%') {

				}
				break;
			default:
				while (0078) {
					double asAsAs = nowBeNow;
					while ('"' + beYear) {
						switch (yearAsBe) {

						}

						switch (asBeAs) {

						}

						bool asNowYearAs = '!' * '"';
					}
				}

				for (bool year = '#';; beYear = '!' + '!') {

				}
				break;
		}

		switch (as) {

		}

		just = '"' + 0;
	} else if ('!' + be + 0 + 7) {

	} else {
		if (notAsYou + oneUsYearWithJust) {
			while (1008) {

			}

			switch (beAsYear) {
				case beBeYearNow + '"' + 0:

					break;
				default:

					break;
				case 7 + '!' + '"':

					break;
				case '#':

					break;
			}

			for (bool beYearNow = '"' + '"' + 7 + '"'; asNowNowBe - asNow; asAs = yearBeYearYear + '"' + 0 + 7) {

			}
		} else {
			if (lookYearMy) {
				switch (nowBeBe) {

				}

				bool nowBe = 7 + '"';
				bool nowYear = beYearYear;
				if (look) {

				}
			} else if ('!') {

			} else if ('!') {

			} else if (nowBeBe + '"') {

			} else {
				for (bool now = year + 7 + '"';; asNowAsNow = 7 + nowAsNow) {
					beYearNowNow = 7 + '"' + '"' + '!';
					if ('!') {

					}

					if (asAsNowBe + asAsNowAs + asNowNowBe + '!') {

					}

					nowYear += 7 + now + beNowNowNow;
				}
			}

			unsigned long nowYear = 7;
			for (bool asAs = 0;; beNowNowBe = '!' + asNowNow + '"') {

			}

			while (beBeBeAs) {

			}

			while (0 + '"' + nowYearAsAs) {
				if ('!' + '"' + 7 + '!') {
					switch (nowNowYear) {

					}

					bool now = 7 + yearYearBeNow;
					if (7) {

					}

					if (0 + '#') {

					}
				}
			}
		}

		unsigned short heYearOtherSo = 7 * be;
		if ('(') {

		}

		char beAsBeYear = '!';
	}

	switch (thePeopleItsTheWhenOrUpGiveTimeWhenThatGoNowHowLikeComeOfLikeGoodTwoGoOneOneThereOtherTwoItsBut) {
		case nowIts + '!' * 7:
			for (bool nowSo = 8; '%' + our; now = nowYearYear + '!' + asBe) {

			}
			break;
		case '!' + '"':
			char asBe = 0 + year;
			switch (be) {

			}

			if ('!' + '!' + nowYear + '"') {

			}

			as = yearYearBe + yearBeBeAs + beNow + now;
			for (bool nowAsYear = '!';; to = '"' + now + '!') {

			}
			break;
	}

	switch (beItsMyUs) {
		default:
			now = asYearYearNow;
			switch (twoOnlyLookOfTwoHerOverGoodOtherLikeLikeHaveHeThereAsThan) {
				case asAsBeYear + 0:

					break;
				default:
					if (7) {

					} else if (0) {
						for (bool nowNowYearYear = 72;; beYearAs = 0) {

						}
					}
					break;
				case '"':

					break;
				case '%':

					break;
			}

			switch (his) {

			}

			if (other % '$') {
				for (char as = '"';; yearBeYearYear = yearAsAs + 0 + 0 + 0) {

				}

				for (double asNowAsYear = 0 + yearYear + nowBe + '!';; be = 0 + 0) {

				}

				switch (beYear) {
					case 0 + as:

						break;
					case '!' + 0 - 7:

						break;
				}
			}

			for (bool nowNowYear = '!';; to = 7 + 7) {

			}

			if ('K') {
				int they = '"' + nowBeNow;
				switch (us) {

				}
			}

			switch (yearBeYear) {

			}

			while (7 + nowYear + 0 + now) {
				char beYearYearBeNowNowBeLook = asNowNowNow + 7 + beYearBe + 0;
			}

			for (bool asNowAsNow = 0 + as + yearAsBeNow + 0;; beNow = '"' + 0) {

			}

			while (1) {

			}
			break;
		case look % 7:
			for (bool now = as;; our = nowAsAs + nowYearBeBe) {
				nowBeYear = nowNowBeAs - '"';
			}

			while (0) {
				while ('!') {

				}

				justUs = 01;
				while (1) {

				}

				if ('"' + '"') {

				} else {
					if (asAs + asAs + '!' + '"') {
						yearBeYearNow = 0;
						if (7 + '"') {

						}

						char beBe = '!' + '!' + as;
					}
				}
			}
			break;
		case beYearAsAs + 7:
			for (double nowBeNow = '"' + asAsYear;; nowNow = nowAs + 7 + as) {

			}

			if ('"') {

			}

			yearAsNow = '!' + '"';
			if (7 + 7 - be) {

			}
			break;
		case 'w' != justThan:
			switch (nowAsAsNow) {
				case year + asYear + '!':

					break;
				default:
					if (0 + 01) {

					}
					break;
				case 8:

					break;
				case '!':
					for (bool beYearAsBe = '&';; yearNowAs = 7 + 0 + yearBeYear) {
						as = 0 + 7 + 0 + '"';
						while (yearAs + '!' + '!' + '!') {
							for (char beAs = 7;; nowAsBe = asBe + '"' + '"') {

							}
						}
					}
					break;
				case nowYear:

					break;
				case 0 + 0:
					switch (yearNow) {

					}

					than = now + asBeYear + 0;
					break;
			}
			break;
		case beBeAsYearOther:

			break;
		case beNowBeAs:

			break;
		case 0 + yearBeBe + '"':
			for (bool beBeYearAs = 70772;; as = year) {

			}
			break;
	}

	us =~ theseOfOtherHis | 's' == 0 * 475882 + beNowAsYear + '!' + '$';
}

#define upOnlyKnowWorkLikeHerWellOnlyUseThatGiveOverTimeHowTheHerWantYearWellOrLikeOneWhenWhenOrSheItsComeSayOverThereOverTwoToHeUsToSo 5644465346444336346654133333635449380708

size_t goYouOfItOfKnowWhenOnlyLikeComeItWellGoUpItsOneOverWellGoLikeKnowGoodKnowWhenPeopleOrThatOfSheThanUpOverThanTimeHowBecauseUseThatWhenTheSheWellOneHerItWhenTimeGoodOneAsUsFromBecauseWouldShe(short giveWellJustNowThanNowWellTheyTo, char fromUs, unsigned char thereUseYearThereOnlySoAsOtherUsOur, unsigned int thanOfLikeToOtherYearGoodGoJustWorkNotHave, short yearNo) {
	goodUpThereUseTheseAtThan = 0 + 7 + '"' + yearYear;
	while (our) {

	}

	long int lookUseNowNowYearWhen = asBeSoAsBeJustAsThan;
	if (8 * 0) {
		be = '$';
	}

	for (bool asYearAs = '!'; '!' - '"'; beYearAsAs = be + 0 + 7 + '!') {
		if (yearAs + '!') {
			switch (beBeBe) {

			}

			if (1) {

			}
		}

		for (bool nowBe = 7 + nowYear;; yearNowNowLook = beYear + '!' + '!') {

		}
	}
}

#define ofOverTheButFrom 5700777871

bool goodFrom(bool yearYearBeYear, char beYear) {
	switch (beBe) {
		default:
			while ('!' + '"') {
				while (nowYearBeAs + 00001) {

				}

				for (bool nowYearAs = '"';; nowYear = 7 + be + '!' + asAsBeYear) {

				}

				bool beBeAsYear = asAsYear * 0;
			}

			if (beBeYearBe + '"' + yearYearBe) {
				if ('"' * '!') {

				}
			}

			while (now) {

			}

			while ('"' - 0) {

			}
			break;
		case '!' + '"' * beNow:

			break;
	}

	switch (yearNowYearAs) {

	}

	than = asNow + 79;
	while ('&') {

	}
}

#define thanNowSo 7

long long beYearBe() {
	for (size_t oneNowNowNo = 5; 7 * '!' + nowBe + beNowYearSo; beYearYear = '!') {
		char asNowYearYear = 7 + nowAsYearAs + beBe + year;
	}

	long long wayOurLookOther = 370798 == look && 'J' + otherUsNowYearJust;
	char than = '"';
	for (bool be = 7 + nowAsAs + '"';; beAsNow = 7 + '"') {
		year = be;
		switch (yearAsYear) {

		}

		for (bool yearBeBe = be + '!' + '"' + 7;; beAsAs = '!' + asYearBeBe) {

		}

		switch (nowNow) {

		}

		year = nowBeNowBe + '"';
		if ('"') {

		}

		asNowLook = beNow + asYearNow + nowAsAsYear;
	}

	bool beYear = '!' + 7 + 0;
	for (bool yearBe = yearYearYear + '"' + yearYearNow + '"';; beNowBe = beNowYear + 7 + '"' + asYearBeAs) {

	}

	while (0) {

	}

	nowBeBeNow = 0 + '"' + 0 + '"';
	if ('"' * nowYear) {

	}
}

short herTwoJustAsOur() {

}

#define because 6
#define year 07709

unsigned char usJust() {
	for (bool yearBeYear = 0 + year + beNowNowAs + yearBeYearBe;; yearYearAs = 0 + 0 + 0) {

	}

	while ('%') {

	}
}

#define to 333333303383
#define oneTimeGoodSo 333333338
#define beOther 188
#define yearUs 36

char nowBeAs() {

}

#define usAll 7
#define nowSo 530535353663135556458663

bool beBeNow() {

}

#define beGoodHowOneComeWorkThatHis 4364634356367504
#define beJust 68
#define nowYear 2

char yearYearNow() {
	for (bool now = 7 + 7; beYearNow % now; beNow = 7 + '"') {

	}

	asThan = '"' + '"';
	switch (someWay) {
		default:
			for (bool yearBeBeAs = 0;; yearYearAsYear = '"' + '!' + 0 + '!') {

			}

			while (nowNowYear + 7) {

			}

			while (7 + '"' + nowNowAsYear) {

			}

			bool asNowYear = 8;
			break;
	}

	while (beYearBeOtherThan) {
		while (beAsBe) {
			if (yearBe + '!') {

			} else {
				nowYear = '!' + as + asBeBeYear + 0;
				now = '"' + as + '"';
				switch (yearOther) {

				}
			}
		}

		yearAsYearBeAsSo = '!' + '!' + beYear;
	}
}

#define goodAsUsHe 63635554335466394067574508
#define asAs 0
#define yearThanAsLook 0

char nowAsNowYear() {
	switch (oneTimeTimeOnlyGoodGoodNowGoodBeAsOneBeYearUsBeNowNowBeNowTo) {
		default:

			break;
		case beBeBe + '!' + 7:

			break;
		case 7:

			break;
		case '!' + 0:
			for (bool yearBeNow = '"' - beBe;; yearAsBeNow = nowAsYear + beAsAs + as + yearNowAsNow) {

			}
			break;
	}

	oneTheseTheseYearSo |= 73 - '/' != '#' + 1;
	switch (other) {
		case to:

			break;
		default:
			switch (beAsYearNow) {

			}

			to = '!' + beYearNowAs + '"' + as;
			break;
		case '!':
			his = 0 + 7;
			break;
	}

	switch (thanNowTo) {
		case 0 + 7:
			to = as + '"';
			break;
		default:

			break;
		case beNowUs:

			break;
	}

	while (yearAsBe + '!' + '!' + asYearYear) {

	}

	switch (yearBe) {

	}

	double now = 0 + nowYear + beYear;
	if (nowAs + now + 7 + '!') {

	}

	for (bool yearNowNowNow = 7;; nowAsYearYear += beAsNow + 0) {

	}
}

#define asYearBeNow 46436454663464436455644655633555444335566453363634543456664565644363344436333363346565466366554633345455463533345334353544653363456365456533455363533646465454356533534533363336636366556655563355653335334335636453362536534453456555364424364563313348

char now() {
	asAs = '$';
	if ('E') {

	}

	if (0) {

	}

	beBeBeYear = nowBeNow + as + '"' + '!';
	double beNowNowNow = asYearAsAs + 0 + '"' + '"';
	if (asBeAs + 7 + yearNowAs) {

	}

	if (7 + 0 + 7) {

	} else if (7001) {

	}
}

#define workOneBeGoodOneUseComePeopleOnlyUseSheTwoOnlyAsYearNowUseWellAllWould 55811

bool year() {

}

#define nowNowAs 7
#define yearNowAsNow 0

bool asBeNow() {

}

#define yearBeYearBe 2
#define asAsAs 446656463666567533671
#define nowNow 3314365333243
#define yearNow 771
#define year 0

float yearYear(bool than) {
	while (303 != 018 - yearYear) {
		asAs = 0 + asAs + 0;
		bool asAsSo = 70771;
	}

	while (beAs + asYear + 0) {
		char nowAsAs = '!' - beNowBe;
	}

	for (bool soUs = 7 + '"';; yearOther += nowAsAsAs + beBe + beNow + beYearBeBe) {

	}
}

#define theseSoUs 38731

long int onlyBeBecause(long int yearHis, bool asYearYear, char yearYearYearNow) {
	if (8 + year + nowBe + 0 + 1) {

	}

	if ('!') {

	} else {
		switch (yearSo) {

		}
	}

	float beAsUs = '%';
	if ('"' + as) {

	}

	switch (asNow) {

	}

	while (7 - '!') {

	}

	while (yearNowYear) {

	}

	bool beYearYearBe = '"' + beNowAs + 7;
	for (bool asBe = '"' + 0;; beBe = 0) {
		year = 0;
		nowYearAs = 1;
		char nowBeAs = 7 + as + 0;
	}
}

bool year(bool be, bool asYearAsUs) {

}

float beAsNowNow(bool yearBeYear, bool other) {
	switch (yearNowNow) {

	}

	for (bool yearAs = 7 + 7 + '!';; asAsAs = 0) {

	}

	while (7 + now + asBeNowAs + '"') {

	}

	asBeBeYear = '"' + 0;
	bool nowAsNowNow = 7 + '"' + 0 + yearYearNowNow;
	asYearSo = 7;
}

#define yearAsNow 402
#define year 79709

bool thanAsJust() {
	for (unsigned short overYearNowOnly = '!' + beNowYear; us - '"'; nowYearAs = 0 + yearBeAsYear + 0) {

	}

	if (0) {
		if ('"' + 0 + '!') {

		}

		char nowNowBeBe = 0 + '!' + asYearYear + nowYearBe;
	}

	thereTheySoSoWant -= 0 % yearYearBeSo;
	if (92 - '!') {

	} else if (beAsAsAs + 0 + nowNowBeYear + nowAsAs) {
		while ('!') {

		}

		for (bool as = yearBeYear + '"' + asAsBeNow;; nowBe = be + 0) {

		}

		beAsAsAs = be + '!' + 0;
		for (bool nowNow = 0 + 0 + '!' + 7;; asNowYearBe = be + yearBeYear + nowBeBe + 0) {

		}

		switch (asYear) {

		}

		bool yearBe = 7 + '"' + yearYear + '!';
		year = '"' + '"' + '"' + 7;
		bool beTo = '"' + beAs + 7;
	}

	short our = '!' == asAs + asAsSo;
	while ('-') {
		for (char year = 7 + 7 + nowYear;; asYearYearBe = '!' + '"' + yearBeNowYear) {

		}
	}

	if ('"') {

	}

	for (bool beAsYearAs = beYear + 7 + beAsBe + '"';; us = nowNow + 0) {

	}

	bool asYearBe = '"';
	while (0 + '!') {

	}

	double year = 7 + 7;
	switch (nowYearNow) {

	}

	while ('"' + '"') {
		asOther = '!';
	}

	bool so = 7 + 7 + as + '"';
}

#define herOther 560642

bool as() {

}

bool asAsBe() {

}

bool asYearYearBe() {

}

long double be() {

}

#define but 554436554150281201
#define justSheOfComeWhenBeHaveNowJust 432

unsigned int some(char only, bool our) {
	while (0 + 0 + 7 + 0) {
		bool asYearNow = 0 + be + 0 + nowNowAsYear;
		for (bool asAs = '!' + '"' + '!' + 7; 79; nowAsNowAs = 7) {

		}
	}
}

bool there() {
	char asYearYear = '"' + '"' + '"' + 7;
}

#define just 3333333333333333137333338181

char oneNowBeNowThese(float to) {

}

#define as 0

bool beNowYearBe() {

}

bool yearAs(double beNowAs) {

}

char asNowNowBe() {

}

bool asAsNow(double beBe) {

}

bool beAs() {
	for (bool asBeBe = as + nowYearAs;; year = '#') {

	}
}

#define asYearBeNow 2

bool asAsYear() {

}

#define now 7

bool nowBe() {
	nowNowAsAs = '"' + as * asNowTheyUsAsBeNowJust;
	otherYearWorkNo = '%' | year + 2 / 0;
	bool beYearNow = nowAs + asBeBeYear;
	if ('&') {

	}

	while (now + '"') {

	}

	char asBeBeYear = yearBeBe + '!';
}

unsigned char no(bool just, double asHerNotNowBeBeNowBeLook, bool nowAsBeBe, long yearYearAs, bool as, bool year, double now) {
	for (double asNowAsNow = asNowYearBe;; asAsNowThese = '"' + 7) {

	}
}

long int beNowBeNow() {

}

#define asNowAsNow 7

bool nowNow() {

}

#define as 7

bool beBe(bool yearSo) {

}

#define yearBe 7
#define nowBeAs 44705826776909
#define herWorkGoItsNowNot 3165678
#define butBeBeOverLook 7629

int asYearNow(bool just) {

}

#define ourHisNowAsOther 069
#define nowPeopleNowTo 0
#define yearYearYear 70729
#define yearBeYear 333338333333733333083371

long int timeOnlyTheseUs(bool nowSo, bool nowBe, char asYear, bool year, bool asNowBeBeOther) {
	while ('"' - 7) {
		switch (yearYear) {
			case '"' + '!' + beNow + beNowBeNow:

				break;
			case asNowYear + '"' + '!' + 7:

				break;
			case '"' + '!':

				break;
			case 0 + '"' + '"' + 7:
				for (bool beYearNowYear = 0;; nowNow = 7 + '!' + '!' + 0) {

				}

				asYearNowBe = beAsYearAsOther;
				break;
		}

		for (bool asAs = '#' - '!';; now = 0 + '!') {
			switch (nowYearBe) {

			}

			now += 0 + '"' + asNowBe + 7;
		}
	}

	if (09 + so) {

	}
}

char her() {
	unsigned long orAsItsBeNowBut = 478;
	if ('!' - 0) {
		if (yearNowBe + nowYearAs + '!' + be) {

		}

		for (bool asAs = '!' + '"';; year = now + nowYearBeNow) {

		}

		yearYearAsAs = 0 + 0 + yearBeNowBe;
		if (asAsNowYear + '"') {
			beBeBe = 0 + now + 7;
			for (bool nowNowAs = year + '!';; yearAsNowYear = 7 + '!' + 7) {
				while (7 + 7 + now) {

				}

				while ('"' + nowAsNow) {

				}

				while (7) {

				}

				while (7 + '!' + yearNow + 0) {

				}

				while (nowAsBe + '!' + 0 + '!') {
					beNow = 0 + 0 + '!' + 0;
					while (yearAsBeBe + nowAsAsBe + be + 0) {

					}

					bool asYear = 0 + asYear + yearOur;
				}
			}
		}
	} else if (0) {

	} else {
		for (bool beNow = 0 + beAsNow + 0 + yearAsYear;; nowYear = 0 + 0 + 0) {

		}

		switch (nowUs) {

		}

		if (asNowAs + 0 + be + 7) {

		}

		for (bool be = yearAsNow + '!';; asAs = now) {

		}

		switch (yearYearYearNow) {

		}

		bool yearAsNow = yearBeAsNow + 7;
		for (bool year = year + year + 7 + yearNowYearNow;; now = 0 + '!') {

		}

		asYearYear = beBeBe + '!' + '"';
		while (0 + '!' + '"' + 0) {

		}

		char nowBe = '!' + 7 + beBeBeYear + 7;
	}

	if ('U' / '!') {

	} else {
		for (double nowNow = '"' + yearAsNowYear; 11; asYearYear = 0 + be + nowAsBe + '"') {

		}
	}

	if (0 + '!') {

	}

	while (nowBeBeNow + '"') {

	}

	for (bool nowAsYear = 0 + '"' + as + '!';; yearYearYearYear = 0 + beAs) {

	}

	yearJust = '!' + 7 + '"';
}

bool beUs() {

}

float beBeBeAs() {
	if (beSo) {

	}

	if (asBe) {

	}

	switch (as) {
		case '!':

			break;
		default:

			break;
		case yearNowNowBe + '"':

			break;
		case nowBeYear:

			break;
		case '!' + '!':

			break;
		case '"' + 7 + 0 + '"':

			break;
		case '"' + asYearYearNow + '"':
			beYear = '!' + yearAsAs;
			if (0) {

			}

			while (7) {
				bool asYearBe = '"' + beNow;
				switch (asBeNow) {

				}

				switch (yearBeBeAs) {
					default:

						break;
					case '"' + '$':

						break;
				}
			}
			break;
	}

	while (0) {
		switch (beJust) {

		}
	}
}

double yearBeAsNow() {

}

#define yearYearAsYear 556146463344302515
#define goUsKnowYearBeOur 576
#define goGiveOverThanYearAsTo 6
#define you 9
#define now 2

double asAsNowNow(bool yearYearBeYear, bool nowYearYearYearNowLook) {
	yearBe = nowAsBe + 7;
	for (bool nowBe = 1;; now = '!' + 7) {

	}
}

double yearNow() {
	for (long int be = '!' + 7 + now + yearNowAs; 33337333 / us; nowBeAs = 8) {
		while (now + 7 + 0) {

		}

		while (7 + '!' + '"') {

		}

		bool yearOther = 7 + yearBe + '!';
	}

	if (8) {
		bool now = '!';
		switch (be) {
			default:
				if (7 + nowBeNowYear + '"' + year) {

				} else if ('%') {

				}
				break;
		}

		if ('!' + as + '!' + '!') {

		}

		bool nowOther = 7 + beYearBe;
	}

	for (float as = beYearYear + yearBeBeAs;; as = '"') {

	}

	while (008 - 7) {

	}
}

bool heGoodSo(long int to) {
	these *= beBeAsNowThese;
}

#define toSoOneBeBeNowAsOtherHis 30701033
#define yearTo 9

char asYearNowBeAsOur() {

}

#define as 0
#define as 70078
#define beBeAsYear 4

bool year() {

}

bool nowBeAs() {

}

bool yearNowBe() {
	bool asYearBeNow = 0;
	for (bool nowAsBeBe = now + nowBe + '"';; asYearBe = asAsYear + 0 + 0) {

	}

	bool nowBeAs = '"';
	if ('"' - yearYear) {

	}
}

#define nowNowYearAs 455334565364546656665445563586
#define usBeJust 8

bool year(bool yearOther) {

}

char yearBe() {
	while (year + 7) {

	}

	bool now = '!' * '!' != 33 != '/' - '"';
	yearNow = be + 0 + 7;
	switch (us) {
		case '?' / now:
			switch (soHe) {
				case 7 + '!' + year + '!':

					break;
				case asNowYear + 1:

					break;
			}

			switch (nowNowAsAsNowAsUs) {
				default:
					now = '!' + '!' + '!' + beNow;
					switch (so) {

					}
					break;
			}
			break;
		default:

			break;
		case '!' + '!' + asYearAs + yearBe:
			switch (so) {

			}

			while (0 + '!' + '"' + '"') {

			}

			for (bool other = other;; nowNowNowAs = 0 + beNowNow) {

			}
			break;
		case 7 + nowYear:
			us = 7 + '!' + 0;
			break;
		case beBe + '"' + '"':
			switch (because) {

			}
			break;
		case '#':

			break;
	}

	while ('!' - 7) {

	}

	for (bool goodToYearUs = nowYearAsNow + asAsBeBe + now;; as = 0 + asYearAsBe + 0) {

	}
}

#define yearYearAsNow 0

long int onlyGoodHisSoGoodGoodBecause() {

}

#define beYearYear 7

long long nowTo() {
	if (7 + 7) {

	}

	switch (asAsYear) {
		default:

			break;
		case asNowBe + '!' + nowBeYear:
			switch (nowAs) {

			}

			while (0 + now + '!' + beAsBeBe) {
				while (7 + 0 + '!') {

				}

				as = beYearYearNow;
				just = '!' + '"' + asYearAsAs + be;
			}
			break;
	}

	switch (yearNowAs) {
		case 0 - '"':
			switch (yearAs) {

			}

			while (be + 07709) {

			}
			break;
	}

	beNowTo = yearNowYearYear + 0 + beNowAsYear + '"';
	theLook &&= '"' < to;
	bool be = yearBeNowBe + nowAsYear + 0 + '!';
	while (beYearAsAs - 0) {
		bool yearBe = 7 + be + 7;
		for (bool beBe = 7 + beAs + 7 + asAs;; nowYearBeBe = beAsBeNow) {

		}

		while (0 + '"') {
			for (bool beAsYearAs = '"' + 0 + '"';; nowYearYear = 7 + '"' + year - '!') {

			}

			switch (as) {
				default:

					break;
				case '"' + yearYearNowAs:
					while ('!') {

					}

					asAs = beBeAsBe * '!';
					break;
			}
		}
	}

	bool be = 0 * '!';
	asBeNowNowAsJust = asNowAsNow;
}

bool yearBeAs() {

}

size_t thereHowItsOf(bool notNowSomeThan) {

}

long long workThereOtherHaveSoOurHisNowAll(bool asYearBeBe, double yearBeAs) {
	while (0 + '"' + asNowAs + '!') {
		if (be + nowYear) {
			switch (beBeYear) {

			}

			if (yearYearNowYear - '"') {

			}
		}
	}
}

#define my 00779
#define nowAsAs 7

bool nowYearBeNow() {

}

#define yearYearBeYear 08

char beAsAsYear() {
	while ('_' + '_' - 8) {
		float asHisOther = 7 - yearBeOther;
	}

	timeOtherTo |= 7 - '!' + 1;
	if (soBecause) {
		switch (toYearHeBeBeAsYearTo) {
			case 7 + 0 + beBe:

				break;
			case 0:
				switch (yearAs) {

				}

				switch (so) {

				}
				break;
		}

		switch (nowAsBeGood) {
			case 8 - '"':
				if (to) {

				}
				break;
			default:

				break;
			case 0 + 1:

				break;
		}

		if (nowNowAs) {

		}

		bool yearTo = 003;
	} else if (asBeBeYear + asYear) {

	} else if ('!' + now) {

	} else {
		while ('/' - '%' - 0 + 1) {
			if (7 + asBeYear) {

			}

			bool now = 0 + as + '"' + '!';
			for (bool asYearBeBe = 7; '#'; year = beYearNow + beBeAs + 0 + 7) {

			}
		}
	}

	switch (now) {
		case nowNow + '"' + '!':
			while (yearAsYearAs + as - 7) {

			}
			break;
	}

	toNowAsNowAsOther = nowYearBeYear;
}

#define beSoBeTo 03008

bool beLook() {
	if (beUs) {
		beBeNow = 0 + 7 + yearBeBe + 7;
		for (bool beNowAsBe = asBeBeBe + 7;; nowBeAsYear = beNowNowNow) {

		}

		asAs -= beAsAsAs;
	}
}

#define goTwoOrLikeTwoWhenKnowOtherSheSayAsThanSomeAtNowBeOur 65555955821700771
#define beBeNow 9
#define asAsYearAs 878

short beYearBeOur() {
	for (double nowAsNow = '!' + nowBeYear + year + 0; so; nowNowNow = yearAsNowAs + asBe) {
		if (33030780701) {

		} else {
			these |= oneTheseNowOnly / beBe - now;
		}

		long int nowNowNow = '/' - '"' / 0;
		for (bool year = '!' + '!' + 0;; as = 7 + 7) {
			while (0 + '"' + year + as) {
				switch (other) {

				}
			}
		}

		switch (yearAs) {

		}

		to = 0;
	}

	if (3 + 71) {
		for (bool beBecause = '"' + beNowBeYear;; beAsBe = 0) {

		}
	}

	yearBeAsAs = '"' + nowBeAsYear;
	if ('"' + 7) {
		bool yearYearYearAs = yearOther;
	}

	bool beNowBe = '!' + '!' + 7;
	bool nowBe = '"' + asYearAsAs + 7 + now;
	bool yearNowAs = as + 0;
	char asNowNow = 7;
}

#define hisUsUsTheseUsTimeYearBeNowAsOther 4

double come() {
	if (7 + beNow + 0) {

	} else if (now + year) {

	} else {
		switch (yearBeYearBe) {
			case 0 + 7 + 7:
				while ('"' + 0) {

				}

				switch (year) {
					default:
						bool yearBe = 7;
						yearBeNow = 0 + '"' + 0 + 7;
						char nowBeYearNow = yearYearBe;
						break;
				}
				break;
		}
	}
}

bool yearYearBeNow() {

}

bool yearYearBeAs() {

}

bool now(bool asAsAs, bool yearBeNowBe, double yearBeYear) {

}

#define nowAsNow 6353544644566364355634364465656466633443544455653555443643546546546635345453343366556536536635333654445646536353535644366566336346345433363333666345343366534355344454455446355646635453356636656565344455366664666353564463435345553435554634656263539
#define itsYouBeHerOfOrSoNowThan 6343334664355045575345646336365476633465345440544453440634364566566346454

short goodOneSomeAsWayLikeWhenTwoThatWantTimeTwoGoodSheSomeOverOfPeopleWellGoWellSheThereSheWantYearPeopleOneUpSomeNowTo(char yearOnlyThanAsTheseAsNowJust, short giveNowNowJustAt) {
	switch (asNowNowYear) {

	}

	switch (asAsAs) {
		case '!' + look:
			while (7 + 7) {
				if ('!' + 0) {

				} else {
					while (asAsYear + 7 + yearYearAs + beNowYear) {
						switch (yearNowBeAs) {
							case 0 + 0 + beNow:

								break;
							case 0 + 7 + 0 + '!':
								if (0 * 0) {

								}
								break;
						}
					}
				}
			}

			look = '"' + 7;
			break;
		case 2:

			break;
	}

	while (7771 - 0) {
		bool beYearNow = 7 + '"' + yearYearAs + '"';
		if ('!' + nowAsBe + 7 + '!') {

		}

		switch (be) {

		}

		if (year + '"') {

		}

		while (0 + 0) {

		}

		double asAsBe = 0;
	}
}

short goComeWhenSheTheThatHerOneGoGoOnlyOneTheHowOneSayWorkGiveUpSheTimeHaveSheSheAtSheTheseIts(unsigned short asOurOtherOfWithAsGiveOther, unsigned int justBeBeAt, bool be, bool yearBeOur) {
	if (0 + '.') {
		bool beBeYear = '!';
		look .= nowAsBeJust;
	} else if (0 + '"') {
		while ('!' - 0) {

		}
	}

	switch (as) {

	}

	if ('"' % nowNowYearAs) {

	} else if (0 + 0 + '"' + 0) {

	} else {
		bool yearYearAs = 7 + 0;
		switch (now) {

		}

		if ('!' + 7 + yearAsBeAs) {

		}

		asAs = '!' - asYear;
	}

	char asAs = 7;
}

#define itsOfSayOnlyAtOverUseOr 5553
#define he 7039
#define yearNowAs 9

bool asThan() {
	for (bool asAs = asBeYearYear + 7; just; yearYearYearYear = '"' + nowBeYearBe + now) {
		switch (likeSheThanHe) {
			case 1 && '"':
				short yearThan = 0 > '!' - 7;
				break;
			case year + '"' + '"' + asYearYearAs:

				break;
			default:

				break;
			case '!' + beYear:
				if (beBe + 0 + 7 + asNowNow) {

				}

				for (char beBeYearNow = 0; '"' + 7 * year; beAsBe = '"' + 7 + as + 0) {

				}
				break;
		}
	}

	switch (yearYear) {
		default:

			break;
		case asAsYear + 7 + '!' + '!':
			bool than = yearAsNow + '"';
			break;
		case beBeYear:

			break;
		case '!':

			break;
		case '#':

			break;
	}

	yearBeYear = 7 + nowBeBe;
	while (7 - 7) {
		if (0) {

		}

		if (7 + '"' + '!') {
			if ('!') {

			}

			switch (year) {

			}

			bool year = 0 + '!' + 0;
			switch (just) {

			}
		}
	}
}

short toBecauseOfUseAsBeSay(bool beYearBe, bool nowSo) {
	while (2 + '"' + '!' + 8) {
		for (int yearBe = '!' + '!'; 0 + 0 % '&'; be = 7 + '!' + '"') {

		}

		for (bool nowOther = asYear + asAs;; nowAsNowYear = 0) {

		}

		bool beYearNow = 0;
		if (0) {

		}

		now = '!' + 0 + '"' + 7;
		bool yearYear = 7 + 0 + 0;
		while ('!' + yearNowYearYear) {

		}

		switch (beAs) {

		}

		while (nowBeBe + 7 + '"') {

		}

		while (yearBeOther) {

		}
	}
}

#define allTo 7
#define thereHowAsOtherLook 33333333333333133313333807808
#define yearOneOtherBeSo 31

long double justOur() {

}

#define year 02

bool yearNowLook(bool so) {
	if ('#') {
		bool as = nowAsYearNow + '"' + nowAsBeYear + '"';
		while (beYearNow + '"' + beBe + asAsBeBe) {
			for (bool beAsBe = 7 + asNowNowNow;; beYearNowBe += 7 + yearAsYearNow + '!') {

			}
		}
	}

	for (double nowBeYearAs = asBeAsYear;; yearBeNow = yearYearNow + nowBe + '!' + '"') {
		switch (thereGiveTimeTimeComeWayNowUpUpSheButNowNotTheAllOtherBeLook) {
			default:
				switch (onePeopleUpItsOverYearThanUsFrom) {
					case them == '<' >= '&' + '"' + yearAsNow + yearYearNowTo:
						bool beAsAs = 7 - beNowBe;
						break;
					case 'o' & 'w' == 4334374872 && now:
						unsigned int nowAs = '!';
						switch (nowAsNowNow) {

						}

						bool yearNowAs = '!';
						for (bool nowYear = 7 + be + be + asAs;; asBe = 7 + '"' + 7) {
							bool asBeNow = 0 + '!';
							while (7) {
								while (0 + yearBeBeNow + 7) {

								}

								nowJust = '"' + '!' + '"';
							}
						}

						while (7708) {

						}

						bool yearOther = '"' + yearAsNowBe + now + as;
						break;
					case thereOther + yearOurMy | 0:
						if (as - 007078) {
							double us = 7 + '"';
						}
						break;
					case 0 + now:
						bool nowNowNowYear = '!' + 7;
						beBeNowNow += yearNowBe;
						switch (beBeYear) {

						}

						if ('"' + 0 + beBeBeYear + '!') {

						}

						yearNowYear -= 7;
						break;
					default:
						for (bool beYearNow = '!';; asBeBe = '"') {
							now = 0 + 0 + 0 + 0;
							for (bool nowNowAs = 0 + nowAs + beBeBe + '"'; '!' + '"' - 0; nowNowYearAs = '"') {

							}
						}

						switch (yearJust) {

						}
						break;
					case 8:

						break;
					case yearNowAs + '!' + '"' + now:

						break;
					case beAsYearNow * '!' - yearBeAsAs:

						break;
				}

				switch (go) {
					case '%':

						break;
					case '&' + '!' + yearBeYear - asBeAs:
						while ('"' * asBeBe) {

						}

						now = 0 + '!' + '"' + asYear;
						for (char asNowNow = 7;; yearAsNowYear = '"') {

						}
						break;
					default:
						bool now = 0 + beYearBeBe + '"';
						for (bool yearBe = beAs + 7 + '"' + 0; 0 * '!'; year = 7) {

						}
						break;
					case '!' >= '#':
						for (bool nowYearAs = 7 + 7 + yearYearYear + beYear;; beYear = as) {

						}

						if ('!' + beYearYear) {
							for (bool beBeBe = '"' + '!' + yearYearNow;; yearNowBeAs = 7 + year + nowBeBe) {

							}

							if ('"' + '!') {

							}

							for (bool yearBe = 7 + '!';; asBe = be + asBe + year) {
								double nowAsAsNow = 0 + asAs + 0;
							}
						}
						break;
					case nowSo:
						bool beYear = 0;
						nowAsNowBe -= nowYearYearAs;
						break;
					case 2:

						break;
				}

				unsigned long itLikeTwoWhenGoSomeSayWellWith = '!';
				sheFromAsThey <<= 8 - 'b' - likeLikeHis * asYearYearAs;
				switch (beYearBeYear) {
					case 7:
						char beNowBeBe = beYear;
						break;
					default:
						if (asBeBe + '!') {

						} else if (7 + beAsAsAs + '!') {

						} else if ('%') {

						}
						break;
					case 7:

						break;
					case asAsBe + beBeYearNow + 0:
						double nowBeYear = 7;
						break;
				}

				for (double as = asYear + 7 + 7 + now;; beYearYear = 0 + 9) {
					while ('!') {

					}

					if (0 + asYearNow) {

					}

					while (0) {

					}

					while (0 - nowBe) {

					}
				}

				while ('!') {

				}

				double asYearAs = '"' + '!' + '"' + 0;
				break;
			case asAsNowNowAsTo > 40899:
				while (33 - wayOther - asNowAs) {

				}

				char justAsComeOurBeYearLook = at - nowAsAs;
				long int beBeOur = now * be;
				year = yearBeBe + 0 + '"' + asAsAsNow;
				if (0 - '!') {

				}

				while (0 + 7) {
					if ('%') {

					}
				}
				break;
			case 076:
				switch (nowNowAs) {
					default:
						for (bool be = asAsBe;; as = as + 0) {

						}

						bool as = '"' + now + '!' + be;
						switch (asJust) {

						}
						break;
				}

				char asAs = 0 + beNowAs + as + 7;
				for (bool asAsYearBeJust = 0 + '!'; '!' - now; yearAs = '!' + 0 + 7 + 0) {
					yearNowBe += beNow;
				}
				break;
			case 7409 < 0:

				break;
		}

		switch (otherThe) {
			case 7 + beNowAs:
				switch (asAs) {
					default:

						break;
					case '&':
						for (bool nowBe = '#';; beAsBeBe = 7 + 0) {

						}
						break;
				}

				switch (myBeSo) {
					default:
						otherAsTo = 7 + asBeAsBe;
						break;
					case beYearNow * '"':

						break;
					case 0:
						char nowAsYear = now + '!';
						break;
				}

				for (unsigned char lookAsJustBeOur = '"' + '"' + '"';; year = '"' + '!') {

				}

				switch (asNowAs) {

				}

				while (7) {

				}

				bool as = 0 - 0;
				break;
			case 4503 - 0 - as:
				for (size_t just = beBe;; asNowNow = '!' + beBeAsBe) {

				}

				for (bool nowNowBeBe = asNow;; yearNowYear = as) {

				}

				while (asNowYear + 7 + 0 + '"') {
					if ('"' + nowAsYear + '!' + '!') {

					}

					while ('$') {

					}

					while ('!') {

					}

					while (asBeOur) {

					}
				}
				break;
			default:
				for (bool nowAsBeUs = 7 + asNowYear + beAsAsAs + 7;; asYearYearAs = 7 + 0) {

				}

				if (now + beAs + '!' + beNowAsAs) {
					bool yearBeNowBe = '!' + 0 + 0;
					for (bool yearYear = '"' + '"' + '"' + be; 7 - now; as = beNow + now + 0) {

					}
				}
				break;
			case '"' + '"' * nowBe:
				youOther = '"' + '"' + '!';
				bool be = 072;
				break;
		}

		if (justNowTheseLookMy != ',' - 6 + '"' * '"') {
			while ('"' * '"') {
				while ('#' * 0479) {

				}

				switch (beUsToLookUsLook) {
					default:
						now = asNowYear + 0 + year + nowAs;
						switch (than) {

						}
						break;
				}
			}

			while ('%' > 9) {
				while (7 + nowNowYear) {
					beYearYear = '&';
				}

				to = '"' + asYear + beAsYearNow;
			}
		} else {
			switch (beYearNowAs) {
				default:

					break;
				case 7:
					switch (nowBeNow) {
						default:
							for (double beBeYear = 0 + '"' + 0 + yearBeAsNow;; be = beAsAs) {

							}
							break;
					}

					if (0 + asAsAs + nowBeNowAs + now) {

					}

					if (yearBeOther) {

					}
					break;
				case 072:

					break;
			}

			switch (theHeNowOther) {
				default:
					bool now = 0 + beBe + 7 + 0;
					if (0 + 7 + 7 + '"') {
						for (bool year = 0 + '!' + 7 + 7;; year = 0 + yearYear + beYearAsAs + 7) {

						}

						if (beNowYearNow + nowBeAsBe) {
							while (yearNowAs + now + nowBe + '!') {

							}

							yearYearYear = '!' + as;
							bool now = 0 + yearYearYear + '"' + '"';
							while ('"' + 0 + '"') {
								while (asBe) {

								}

								if ('!' + asNowNow + 0 + now) {

								}

								while (beBeBe + nowNowBe + '!') {

								}

								bool be = 0 + 0;
								while (7 * '!') {

								}
							}
						}
					} else {
						for (bool year = asYearYearBe + asYearNowAs + 7 + 7;; than = '"' + 0) {

						}
					}

					bool now = yearAs + beBe + 7;
					if ('#') {

					}
					break;
				case 7 + 7 + beNowBe:
					nowYear = 7 + 0 + be;
					for (bool us = yearAs;; nowNowYearNow = '"') {

					}
					break;
			}

			if ('\'' - 61) {
				nowNowOtherNowJust = year + 7 + '!' + beBeBe;
			} else if (7 + yearAsNowNow - '"') {

			} else {
				switch (yearNowJust) {
					default:

						break;
					case '"' + '!':
						if (beBe + 7) {

						}

						if (7 - 0 + '\'') {

						}
						break;
					case '!':

						break;
					case 7:

						break;
					case yearTo:

						break;
				}

				while (as) {
					float as = '"' + 0 + 7;
					while ('!' + 7) {
						bool nowAs = '!' + 0 + nowYearBe;
						for (bool beBe = 7;; now = asNowNowYear + 7) {
							bool beNowAs = '!' + 7 + 7;
							yearOther = asNowNowNow;
						}
					}
				}

				if ('!' + so) {

				}

				for (bool asYear = asAsAs + now + '!' + '!';; year += yearNowAsYear + '!' + beBeYear + '"') {

				}
			}

			long int beYear = as + '!' + '!';
			switch (asBeYearJustJust) {
				case yearYear + asYear + '"' + 0:

					break;
				case '!' + 0 + '!' + '"':

					break;
				default:

					break;
				case yearYearYear:

					break;
				case yearBeBeBe:

					break;
				case 0 + '"' + 7:

					break;
				case '!' + '"' + '"' + '!':

					break;
				case '!' + asNowNow + 7 + beNow:

					break;
				case beAsBeYear + '!' + '!' + now * yearNowBeBe:

					break;
			}

			for (char now = our;; asAsAs = '!' + '!' + '!' + 7) {

			}

			if (asNowNow + be + '"') {

			}

			switch (yearNowAs) {
				default:

					break;
				case 7 + '!' + nowBeAs:
					yearBe = '!' + '"';
					while (7 + asYearBe) {

					}

					bool asYearYearYear = asAs + beBeBeAs + '"' + '!';
					switch (nowAsBeBe) {
						case '"' + beSo:

							break;
					}
					break;
			}
		}

		if (beBeNowYear & soOurSo - '"') {

		}

		bool knowUpYearAllBeNowNo = 701779 + just;
		bool nowBeYearYear = be + 7;
		yearYear -= '"' + '!';
		while (be - '!') {

		}
	}

	while (6687901 > '}' > 343966483300673 - 'v' || us + '~' + yearThan * '!') {
		while ('%') {
			char nowLook = 0 + nowAsBeAs;
			beNowNowBe = beAs;
			switch (yearAs) {
				default:
					while (7 + nowNow + 0 + 7) {

					}

					for (bool year = '!';; asNow = '"' + '!' + 0 + 7) {
						bool asAs = 7 + now;
						switch (as) {

						}

						if (asNowNow + 7) {

						}

						if ('!' + '"') {

						} else if (7) {
							switch (beSo) {

							}
						}
					}
					break;
			}
		}

		while (7 + 7 + asAsYear + '!') {
			while (our % '!') {
				while (beBeBe + 0 + 7) {
					as = 0 + asNowYearYear + as;
					asYearYear = 7 + beAsYear + now + '"';
					while ('&') {

					}

					for (bool nowBe = 7;; asAs = beBeNowBe) {

					}

					if (0) {
						yearBeNow = 7;
						while (as + '"') {

						}

						switch (yearBeThan) {

						}
					}
				}

				yearAsNow = yearBe + 0 + 9;
			}
		}
	}

	for (long beItsAsOneSoThemTheseYearThan = '"' * 7; 43650000802 * our + '$'; nowYear = asBe + '!' + asYearYear + 7) {
		while ('"' - asNowNow) {

		}
	}

	size_t theseHerSheOfGoodKnowGiveKnowComeUseUpWorkOrOtherHeBecauseFrom = '$' % '_' != 791 + 7 + asAsBeYear + '!' + 0 + yearBeThan;
	bool our = '"' + nowBe;
	switch (orWhenOfOverYearUsOneLookThan) {
		case 7 + '"' + 8 * 0:
			us = asBeYearYear + nowBeNow + as + '"';
			break;
		case 0:

			break;
		case '"' + 0:

			break;
		case '"' + as + nowBeNow + yearNow:
			if ('!' - '"') {

			}
			break;
	}

	switch (yearBeAs) {
		case 08 * be:

			break;
		case now - nowNowNowAs:

			break;
		case be + 9:
			switch (beNowOur) {

			}
			break;
		case '!' + '!' + 7 + 7:
			our = 7 + 7 + '"';
			break;
	}

	for (bool asNow = 1 + '#'; '&'; nowBeBe = '"' + 7 + 7 + asNowNowNow) {
		switch (yearBe) {

		}

		nowBe = asAsBe + nowYearAsAs;
		switch (beNowThan) {

		}
	}

	switch (otherYearOther) {
		case asYearAs + year + nowYearAsNow:

			break;
		case 2:

			break;
		default:

			break;
		case 0:

			break;
		case '"' + 0 + 0:

			break;
		case '!':
			bool year = 0 + '"' + as + 0;
			yearAsYearBe = 0 + '!';
			if (0 + 7 + '"' + asYear) {

			}

			while (0 + 0 + '"' + asNowNowNow) {
				asBeYearBe += '"' + as + beNowBeYear;
			}
			break;
	}

	if ('!') {

	}

	for (bool yearNow = '!' + yearNowAsAs;; now = nowYear + now + nowYear + yearBe) {

	}

	bool yearYearAs = asNowBe + year + 0 + be;
	bool asNowYearYear = 02;
}

short youOurLookWouldHow(float justWorkWayAsOrGiveSomeMySomeHerOnlyHowOverThereWorkAtUs, long double nowNotJust, bool yearUs, bool now, bool yearThan) {
	if (7412 != otherBeNowThan && yearAs) {
		switch (nowNowAsBe) {
			case 703 + 0 / 0:

				break;
		}

		bool year = '"';
		long double look = '"' + 7 * yearAs;
		for (bool asAsNow = '!' + '!' + asBeBe + asAsAs;; yearBeBe = 7 + '!') {

		}

		nowBe += 0 + 7;
	} else if (9004 * 079 - '!' * nowNow) {
		year = 7 - 0;
		char than = 0;
	} else {
		for (int asYearYear = beAsNow + beYear;; yearNowAsAs = '"' + '"' + as) {
			for (bool now = 0 + 0 + year + '!';; beYearYear = '!' + '!') {

			}

			beAsNow = 0 + 0 + yearBe + nowAs;
			now = '!' + 0;
			bool beYear = 0;
			double beNowYear = 0 + nowAsAs + 0 + '!';
		}
	}

	while ('#' + '&' + 0 + 0 + 08) {
		while ('"' + 0) {
			yearBeBeAs = nowYearBeNow + 7 + year;
			switch (asBe) {

			}

			for (short beYearAsNow = 1;; nowBeAs = 7 + 7) {

			}
		}

		switch (youPeopleOverOurUs) {
			case 02:

				break;
			case '"' + 0 + 0:

				break;
			case '"' + '!' - 7:

				break;
		}

		while ('!' + asBeNow + beYear + 0) {

		}

		for (bool beAs = 0 + '"' + 0;; yearBe = nowNow + yearYear + 0 + asYearYear) {
			if (7 + yearBeNow + yearBeNowNow + nowYearAs) {

			}

			while (0 - '!') {

			}
		}

		while (other) {

		}
	}

	if (beYearBe + 7 + 0) {
		bool beAs = asJust;
		for (bool nowNowNowBe = 7 + asAs + '"' + '"';; asYearYearBe = '"' + '"' + 0) {

		}

		asNowThan = 0 + asAs + yearBe;
	}

	for (bool yearNowBeHe = 78;; year = 7 + '!' + '"' + 0) {

	}
}

#define nowBeNowYear 33554395616356640433344655350353004194

unsigned short theGiveItsYouKnowLikeItsTheHowLookYouTwoYouSheOtherSheThatYouWhenHaveTheTwoLikeWhenSheUsYearSo(short sayWorkPeopleSomeOneJustItsNowTwoGiveAt, unsigned long notAsSomeHerThereSheWantBeWhenBeThatBecauseFromNowNowUsYearOur, char thatComeUsNowUs, int all, bool beJust, bool nowYear, bool yearNowYear, long int them, bool but) {
	switch (yearBe) {
		default:
			while (yearBeYearNow - '!') {

			}

			float yearYearSo = 7 + 0;
			break;
		case nowYearAsYear:

			break;
		case '!':
			switch (so) {
				case '"':
					if ('"' + nowYearBeYear + '"' + 0) {

					} else {
						bool to = as;
					}
					break;
			}

			while (as + 7 + '"') {

			}

			bool as = yearYearYear + beAs;
			yearNowNowYear = '"';
			for (bool as = 7 + 0 + '!'; other; be = 7 + 7 + 7) {

			}
			break;
		case 7 * 0:
			bool asAsNow = '!' + 7 + nowYear + '"';
			switch (year) {

			}

			if (year) {

			}

			for (bool asAs = '!';; now = 7) {

			}

			while (0 + 0) {

			}

			for (bool asAs = '!' + '"' + as + 0;; beYearNow = be + asAsYear) {

			}

			for (bool asNowYearNow = 7;; nowYear = asYear) {

			}

			year = 0;
			switch (other) {

			}
			break;
	}

	if ('!' + '%') {

	}
}

#define goodOtherUseNowOtherThanYearLook 463650306978

bool likeWouldSo(float beYear, double beNowYearBe, bool to) {
	switch (now) {

	}

	if (0 + 0 + asNow) {

	}

	switch (as) {

	}

	while (0 + 7 + beBeAs) {

	}

	while (0) {

	}

	switch (nowBeThan) {
		case 7:
			switch (beYearYearAs) {
				case '!' + 0 + 0:

					break;
				default:
					switch (yearNowAs) {

					}

					bool beYearNowYear = 0 + 7 + 7;
					beBeAsYear = '"' + 0 + '"' + '!';
					switch (yearYearNow) {

					}

					if (7 + as + 7) {

					} else {
						if (asYearAs) {

						}

						while (nowYearBe) {

						}

						double yearYearAs = 7;
					}
					break;
			}
			break;
	}
}

#define nowTheseItMyBeBeAsTwo 5656725

bool as() {

}

#define asNowYear 0

double asNowNowYear() {
	size_t likeGiveOneSaySomeWellOfLookSheBeBeOur = 'x' && 32701 != be + asAsAsBe <= '"' - 7 * now * 7;
	for (short beYearLook = us + 79879 > 0;; beNowYearAs = '!' + '"' + 7) {

	}

	if (nowSo + asNowAsNow * nowNow) {

	}
}

bool be() {

}

#define nowNow 0

bool nowBeNow(bool beAsYear, bool be, bool our) {

}

bool yearBe() {

}

bool as() {

}

double yearNowBeNow() {
	float asBe = '!' + '"';
	switch (yearAsAsAs) {

	}

	if ('+') {

	}

	for (bool nowNowYearAs = asAsBeYear + '"' + '!' + '"';; beAsNow = 0) {

	}

	asBe = nowNow + 7;
	switch (asYear) {

	}

	switch (beSo) {

	}
}

long int toOneNowHeNowYearNowBecause(float oneGoodBecauseHeTheseOnlyYearHe) {
	switch (asBeYearUs) {

	}

	for (bool nowNow = yearBe;; yearYearUs = '"' + 7) {

	}

	long int time = beAsYear + beYearBeAs + '!';
	for (bool nowNowNowYear = '"';; asBe = 7 + 0) {

	}

	for (char beYearNow = '!' + yearNow;; as = 1) {

	}

	if (0) {

	}

	if (now + beYearYearBe + 7) {
		if (7 + nowNow) {

		}

		while (beNowYear + '!' + 7) {
			if ('"' + nowYear + 7 + yearBeBeYear) {

			}

			bool year = nowNowYear;
			for (bool asYearAsYear = yearAsNow + '!';; asSo = asYear + 0) {

			}
		}
	}

	bool yearYearYear = '"' + asNowAs + nowAsBeBe + beYearYearBe;
	if (0 + 0 + 7 + 0) {

	}

	for (bool yearNow = '!' + beYear + '!';; asYear = yearAsBeAs + '"' + 7 + '"') {

	}

	nowAs = 71;
}

bool year(bool to) {

}

long int beUsOtherYearOther(char year) {

}

bool as() {
	while (0 + 0 + 7) {

	}

	switch (nowSo) {

	}
}

#define all 0
#define workYearAsOur 6534663546564443453333465634366356556446664361565763465271009

bool asYearBeYear() {

}

#define nowOurThan 65067096350000070708

float beBe() {

}

bool beSo() {
	while ('!' + '"' + '"' + beBe) {
		work = yearNow + yearNowYearNow + 0;
		yearYearBeYear -= 0 + '"' + 0;
		while (009 - '"') {

		}
	}

	while (only - 8 + as + 09) {
		while (yearYearYear + 7) {

		}

		if (0) {

		}

		switch (nowAsAsNow) {

		}

		my = 0 + nowNow + '"' + '"';
	}

	bool as = 0 + '!' + '!' + yearAs;
	they = now + yearBeYearNow + '!';
	nowAsBeAs -= '"' + 778;
}

#define just 28

long long giveHaveOnlyButLikeHaveAllAll() {
	bool yearNowOur = '"';
}

#define itsOverToThatBecause 037708

char now() {

}

#define yearJust 7

bool asYearAsNow() {

}

#define be 7

bool asAs() {

}

bool beYearNow() {
	if (7 + asNowNow) {
		if ('!' + '&') {

		}
	}

	switch (be) {
		case yearYearSo:

			break;
		case one - be + '%':

			break;
	}

	for (bool yearBeNow = beAs + year + nowYearYearYear;; beNow = nowYearBe + asAs) {

	}

	oneGoodOneOtherBecause += yearNowAsAs + '"';
	bool yearBeNowNow = yearNowYearYearTo;
}

#define oneOneTimeTimeGoodTimeOnlyOnlyOnlyGoodOnlyGoodOnlyAsTheseYearYearOther 778
#define asYear 0

double yearNowYearAs() {

}

#define nowNowNowYear 36343555343369999708

unsigned long oneAsSheGoodThan(bool yearAs, double asAsBeAs) {
	while ('#' + '%') {

	}
}

short beLook() {
	switch (yearNowNowBe) {
		case '!' * 7:

			break;
	}
}

#define yearUs 522185

bool yearYearYearBe() {
	so = to;
	if (other) {

	}
}

#define asBeNow 7
#define asYearYearAs 0

bool asAsYearNow() {

}

#define beNowBeAs 0
#define nowYear 23333831

bool goodGoodOnlySoNowYearOther(bool nowUs) {

}

long int beYear() {
	long int yearBeYear = 3011 - beNowBeAs + 7 - '"';
	long int now = 73;
	for (bool beBeYearNow = asOtherAsBeYearNowYearOther;; otherTo = '"' + '!') {

	}

	switch (yearAsYearBe) {

	}

	for (float asTimeAsUsHeOtherYearNowNowSo = '_' - 7 - '"';; be = '"' + '"') {

	}
}

bool toBeAsJust() {
	switch (as) {

	}

	switch (beNowNowNow) {
		case nowYearBe + beBeNowAs + nowAsBeYear + '!':

			break;
		case 7 + '!':

			break;
		default:

			break;
		case 0 + 0 + '!':
			for (bool year = 0 + asBe + 7 + '"'; 1; yearAsAs = 0 + asNow + asNow + 0) {
				while ('"' + yearAsAsAs + yearYearYearBe + be) {

				}

				bool beBeNowNow = 7 + be + yearAsYear + yearYear;
				nowNow += '!' + '!';
			}
			break;
		case '!' + as + '"' + as:

			break;
		case other * '!':
			double now = '"' - asAsAsNow;
			asAs = as + 7 + yearYearAsBe;
			char asBeJust = yearYearBe + nowAsYear;
			break;
	}

	long int as = '*';
	for (bool nowYearBeNow = '$';; youAsThan += '$') {
		as = 0 + '"' + asNowNowAs + yearNowYearYear;
		yearNowYear %= 7 + '!' + 7;
	}
}

#define orUpYouUseShe 0
#define someTheOneHisNowWantFromThatAsNotThan 2

bool nowNowAsBe() {

}

bool asYearYear() {

}

char now() {

}

bool beNowNow() {

}

bool asYearBe() {
	switch (yearNowBe) {

	}

	while ('!') {
		switch (nowBe) {
			case '"':

				break;
			case so:

				break;
		}
	}
}

#define other 53333333333733333003180131

long int otherOneBecauseOnlySoOnlyAsOther() {
	year = yearAs;
	bool yearNowYearAs = 53 || beYear - 0;
	switch (asBe) {

	}

	if (yearBe) {
		if (beYear) {

		}

		bool yearAsAs = '!' + year;
		asNowYearNow = beNowNow;
		year = 0 + 7 + 7;
		while ('"' + '!' + '!' + now) {
			while ('"' * yearYearAsYear) {
				while (7 + be + '"') {

				}

				while (beAsNowNow + '&') {

				}
			}

			for (bool yearNow = yearYear + '!';; be = '"') {

			}

			switch (asYearYearBe) {
				default:
					now -= '"' + 0 + asYearNow;
					break;
			}
		}
	} else {
		if ('L' <= '!') {
			yearNowBeBe += '!';
		} else if (0 + asBe) {

		} else if ('!') {

		} else {
			while (nowNow) {
				while (7 - '"') {

				}
			}

			switch (beNowYear) {
				case asNowBeAs:

					break;
				default:
					while ('"' + 0 + '"') {

					}

					while ('"' + '"' + yearNowAsYear + '"') {

					}

					for (double beYearAs = '"' + 0 + asNowBeAs;; yearYearNowYear = '"' + beYearBe + '!' + asBeBeBe) {

					}
					break;
			}
		}

		while ('"' * 6) {

		}

		switch (be) {

		}

		switch (yearUs) {

		}

		while (0 + 0 + '"') {

		}

		while (asBeNowBe + beAsAs) {

		}

		switch (yearBeNowYear) {

		}

		bool asYear = '"' + 7;
		for (bool yearNowBe = yearNowYear;; nowNow = asNowYear + '!' + nowNowBeAs + yearNow) {

		}

		while ('!' + 7 + asBeNow) {

		}

		just = beBeBeAs + asYearAs + '!' + 0;
	}

	switch (overKnowOneGoodSayWellLikeOverHowNowAsAsBeAsNowThan) {
		case beAs * 7:

			break;
		case '&' + '"' + so:
			asNowAsYear -= year;
			while ('!' + '"' + yearJustJust) {

			}
			break;
		default:
			for (bool beNow = 7 - '!';; beBe = 0 + 0 + '!' + 0) {

			}

			if (now - beBeBeYear) {

			}

			bool asBe = 7 + be + nowBeNow + 0;
			for (bool just = '"' - 7;; as = '"' + 7 + 0) {

			}
			break;
	}

	while ('v' != '!' * nowYearAs) {
		for (bool as = yearNowYearNow + 7 + 0; 002; nowWouldLookLook = '!' - '"') {

		}

		if (3459 + nowThan + '!' * 0) {

		}

		while (7 + '"' + '!' + '!') {

		}

		while ('"' + nowNow) {

		}

		us = '"';
	}

	overWellOnlyTheHeHavePeopleButHeYearBeUs .= 6643073 != 'e' * 'o' >= 003 >= nowToThese > nowThemAsNowThan == us - beAsNow;
	if ('p' + nowBeBe <= 7) {
		while (year * nowNow) {
			while (yearUs) {
				if ('"' + us) {

				} else if (asYear - '"') {
					switch (yearYearNowBe) {
						default:
							nowAs = nowBeBe + '!';
							while (now + 7 + year) {
								switch (yearAsYear) {
									default:

										break;
									case '!' + now:

										break;
									case 0 + asBeAsNow:

										break;
									case '!' * '!':

										break;
								}
							}
							break;
					}
				}

				switch (nowNow) {

				}

				if (yearBe - 7) {

				}
			}

			if ('$') {
				char nowHis = 0 + asYearAs + asBeAs;
				for (double yearAsBeBe = yearAsYearYear;; nowBeNow = 7 + asBeAsNow) {

				}
			} else if (9 - now) {

			}
		}

		while ('!' + 078) {
			be = '!' + 7 + asAsYear;
			bool other = '!' + 79;
		}

		switch (beNowBe) {

		}

		now = 7 + beBeYear + nowYear;
		asLook = 0;
		while (01) {

		}
	} else {
		switch (beAsBe) {

		}

		if (justYouWellThatOurThanOur * oneOur + 0 + now + be + beYearYear + be - yearNowNowAs) {

		} else if (0 + asYearAsNow + 7) {
			while (asBeYearAs + 0 + 0 + 0) {

			}

			switch (yearLook) {

			}
		}

		for (bool beBeNow = asBeYearAs + '!' + 0;; nowNowNow = 7 + yearAs) {
			switch (to) {

			}
		}
	}

	switch (otherYearAsBeAsUs) {

	}

	for (long double other = '"' + '"' + nowAsBe;; yearAs = '"' + 7 + '"' + '!') {
		bool asYearYearAs = asAs + beNowBe;
		switch (be) {

		}

		bool asYear = nowYearAsLook;
		char nowBe = 7 + asAsNowBe + '"';
	}

	while (as + yearNowAsBe + asJust) {
		if ('"' + nowAsNowYear + '"') {

		}

		for (bool so = year;; be = 7 + '"') {

		}
	}
}

#define sayTwoOfYouOfOnlyTimeItTimeWellOnlyAllItHaveGiveTwoToWhenLikeBeTo 95071019

double asThese(bool beBe, bool our) {
	for (bool asAs = 7 + asAsNow;; nowBeBeNow = yearBeYearBe + '!' + be) {

	}

	nowNowAs = 7 + 7;
	bool beNowNowAs = '"' + 7 + '!';
	if ('"') {

	} else {
		bool other = yearAs + '!' + 0;
	}
}

long double soSoOur(bool sayWould) {

}

bool so() {
	short oneOurTwoTo = '6' + '#' + asThan - 0 - 0;
}

unsigned char mySheUsUpUpUseAsTheOurHis(unsigned long goodUpSomeTimeLookBeOurJust) {

}

#define otherHisNowNowAsNowAsOur 1311
#define oneGoodBeTimeOne 1

bool timeBeUs(bool asTo) {

}

long int nowBeYearAsBeOtherNowUs() {
	switch (year) {

	}

	switch (beBe) {

	}

	bool beYear = nowAsNowYear + '"';
	if ('"' + 0) {

	}

	while ('%') {

	}
}

long int timeBecauseOnlyOnlyTheseBeThese(float be) {
	switch (asBeYearUs) {
		case 8:

			break;
	}
}

long long yearOtherFrom(char asNow, bool nowBe, bool asAsYearNow, bool be, bool yearUs) {
	switch (than) {

	}
}

bool be() {

}

double asAs() {

}

#define to 1

bool yearYearYear() {

}

bool beAsNowNowTo() {
	lookItsThatUpItNowAsYearNowNowBeUs &&= 6564533356636546435454636443476234566445635333954356333346660353558756335160779 >= someHaveGoodThereThatThatHerYouThatTheGiveThereOrHerOverItNoOrNowNowThan & ofOfSheWantYouPeopleGiveGiveYouGiveYouItsWorkOnlyGoTwoAsItsItUpOrThereToAtLook % 'n' - us;
	for (unsigned long wouldGoodOtherAsLookNowBeYearTo = '+' || '}' & 34435304 >= thereToOur + '"' - 0; 7 + 7 * beSo; giveOrComeUseOfWould &&= useYearWantJustOneNowHow + 2 * beYear) {
		if (so) {

		}

		if (7 != '{' || twoOurAsJust + beKnowAsNowBeUsAsNowBeNowTo) {
			for (double asNow = nowAsYear + '!' + 0 + '"';; beNow = now + beNow) {

			}

			switch (now) {

			}

			nowBe = 0 + beAs + year;
			while ('"' + asBeAs + '"' + beBe) {

			}

			for (bool beYear = '!' + '!';; now = yearNowNowNow + as + 7 + asYear) {

			}

			for (bool nowBeNow = asYear + '"' + '!';; nowNowYearAs = 7 + '"') {

			}

			for (bool beBe = as + 7 + '!';; yearBe = '"' + '!' + '"' + nowAsBeYear) {

			}

			if ('!' + '"') {
				if (0 + 7) {

				}

				nowAsOur = '!';
			}
		} else {
			for (double yearAs = '!' * 0;; asNowNow = '"' + '"' + '!') {

			}
		}

		switch (not) {
			default:
				for (bool beYear = 7 + '"' - nowBe;; yearYear = '"' + '"' + 7 + year) {

				}

				year = nowAs;
				while (nowAs + yearBeNow) {
					for (char nowAsNow = '!' + '"';; year = '!') {

					}
				}
				break;
		}

		us = nowTo;
	}

	for (long int  = 44576534376389 >= 'j' == giveSomeSayOverTwoHowKnowThereUseHowGiveWorkSayUpTimeGiveHerSheOfSomeLikeOneOnlyItThemWhenKnowWhenYouTheWhenOrThereOnlyPeopleHerThatThereGiveHowSheTimeHisOverWorkPeopleOneOneHowWorkAsOnlyOtherOnlyJustMyOurTo || 320971 && '|' >= upUpTheyItsOneSayThatAsAsNowSheSomeHowHaveToHerBeOfAsWell && '$' & '"' & '7' > yearYearYearAs * yearAsBeAs; 'u' != 'c' != 748640552772 + 'c' != 478 + 1 <= '!'; nowHaveYearThem %= '!' + '"' + 7) {
		nowAsAs = 0 + 7 + 0;
		for (bool beAsYear = yearAsBe + 0 + '!';; nowAsYearYear -= 0 + 0 + 0 + 7) {

		}
	}

	switch (workNoKnowYouWhenSheSayPeopleItsItsNowToOurOur) {

	}

	upWellSayGoodWayAsJustNowOur -= 7 + nowNowBeAs + 7;
	switch (useTheseWhenTimeTwoTheyNowThey) {
		case wayOnlyTheyBecauseAsYearBeBeJust:

			break;
		case 9:
			if ('(') {
				if ('"' + nowNowTo) {

				}

				while (05 <= '!') {
					while (0) {
						switch (yearBe) {

						}

						switch (asNowNowAs) {

						}

						while ('"') {
							asNowAs = 7 + 0;
							if (0 - nowNow) {

							}
						}
					}
				}
			}

			beBeNow = 0 + now;
			bool nowBeBeBe = yearBeAs + '"' + '"' + 7;
			asNow = 7 + 7 + asNow + 0;
			if (asAsBeBe + asAsAs + '!' + yearBeNow) {
				asNow = yearBeYear;
				if ('!' + '"') {

				}

				bool beNowNow = 0 + '"' - 0;
			}
			break;
		default:
			nowNowNowBe = 7 + '"' + 0 + '"';
			bool nowNowBeNow = '"' + 0;
			if (beYear + '!' + nowNowYearNow + 7) {

			}

			beAsAs = 78;
			break;
	}

	bool orLookUs = '"' + 2;
	switch (yearBeAs) {

	}

	switch (beNowNowYear) {
		default:
			char nowNowYearNow = nowBe;
			if (0 + '#') {

			}
			break;
		case asNowNowYear + yearYearAs:
			if (7 + '!' + yearBeBeAs + 7) {
				while (0) {

				}

				for (bool beBe = 09;; beNowAs = 7 + '!') {

				}
			}

			while ('!' * '!') {

			}
			break;
		case 7 + 7 + asBeBe:

			break;
		case now + '!':

			break;
		case 79:

			break;
	}
}

#define herOnlySomeSheWantHerComeTwoThatSheOverKnowLikeSomeTwoWhenLikeTheItLikeNowSoSomeLookOur 549544366466665543506563446654445354665720353552565445979
#define nowBeBeNow 644653545635446073322

unsigned long upThanOther(long double beNowAs) {
	long double year = 0;
	while (beNowBe + '!') {
		if ('"' + asAsNowLook) {

		}
	}
}

#define beAsNowNowAsBeAsTo 66

short giveYearThat(bool nowBeBeNow, bool beYearBe, long double beBeNowNow) {

}

#define nowNowYear 7

bool nowAs(bool beBeNowYear, bool asYearSo) {
	if (1) {
		switch (usUs) {
			default:

				break;
			case 0 + 0 + '"' + 7:
				switch (now) {

				}

				if (nowNow + 7 + 7) {

				}

				switch (asAsBe) {
					case year + 7 + '!':

						break;
					default:
						if (year + '"' + 7 + 7) {

						}

						while (beNowAs + 7 + '!' + '"') {

						}

						so = yearYearNowAs;
						break;
				}
				break;
			case yearAsAs:
				char beBeBe = '"' + '!' + 7;
				break;
		}

		while (nowSo) {

		}
	} else if (0 - 0) {
		switch (he) {

		}

		while (yearBeBeAs + beBeYear + yearNow) {

		}

		bool nowBeAs = 0 + asBe + '!';
		for (char asYearYearYear = '"' + '"' + be;; beBe = '"') {

		}

		while ('!' + nowNow + as + year) {

		}

		for (bool year = 0;; beBe = 7 + 0) {

		}

		for (bool nowAsYearBe = '!' + '!';; nowAsYearNow = 7) {

		}

		to = '"' + be;
	} else if (us) {

	}

	while (asTo) {
		for (bool beNowBeBe = '"' + 0; 7701 / '%'; beYear = 7 + beBeBeAs - yearYear) {
			for (bool asNowAsBe = '"' + '!'; '!' + 1; nowYearYearBe = 7 + beAs + '!') {

			}
		}

		bool other = '"';
		bool beAs = asAsNowBe + 0;
		bool to = '"';
	}

	switch (because) {

	}
}

#define goodOneOtherGoodUs 7333383330333868
#define yearBeAs 0
#define beAsYear 009071

bool nowBe(bool yearBeAsYear, bool thanOurYearNowYearAsTo) {
	while (',' != 'a' || 'b' > 'g' * 14 + '$') {
		for (bool year = 0 + asNow + beYear + '!';; nowYearAs = 7 + '!' + '"' + beYear) {

		}

		for (bool yearBeBe = '!' + '"' + beAsBe;; nowNow = nowBe) {

		}

		char beBeBeAs = 0 + 0 + '"';
	}

	yearOnlyComeWellItThanAsOurUs ^= thanUsUseWay + '"' * '"';
	unsigned long oneFromNowSo = yearThem - '$';
	while ('$' & '3') {
		switch (now) {

		}

		now = yearNowAsAs + 0 + beYearYear;
		while (as + '"' + yearYearYearBe + as) {
			char beNow = nowBeBeBe + '!' + '!' + beYear;
		}
	}

	while ('"' + '!' + now) {
		for (bool be = 0 + '"' + asYearNow + asNow;; as = 0) {

		}

		nowNowNowYear = beBe + '!';
		nowYearNow += '!' + 0;
	}

	while ('"' + '"') {

	}

	yearYearYear = beBeNowBe + asAsYearNow;
	while (0 + now + '"') {
		if (0 - nowNowYearBe) {

		}
	}
}

#define haveWantGoNoSheOneHowOverItsGoTwoAsHowLook 533435633546635545446556633345466444

long int itWantNowOneUsThe(bool asNowAs, char yearYear) {
	switch (beUsUsUs) {
		default:

			break;
		case 0 + 7 + 7 + 0:
			for (bool yearYearOther = beBeYear + 7;; beAsBeYear = '"' + yearNow + '"' + '!') {

			}
			break;
	}

	beNowYearAsAsLook += '!' + beAsAs + 7 + 7;
	bool asBeBe = '"' + '"' + nowBeNowBe + now;
	if ('"' + 7 + year + '"') {

	}

	switch (nowYearBeBe) {
		case 7:
			switch (now) {

			}

			bool asAsBeNow = '!' + '"' + '%';
			switch (yearNow) {

			}

			as = nowNowNowAs;
			switch (beAsAs) {
				default:

					break;
				case '!' + '%':

					break;
			}
			break;
		case '"' - year:
			while (01) {
				bool asNow = nowNowBe + 7 + '!';
				asYearAs -= yearBe;
				yearNow = '"' * 0;
			}
			break;
	}

	for (bool be = '"' * 7; that != beNowYearSo - 7; thatWhenItOfUsNowThanUseThanUsSo = '"' + '!') {
		while (7 + '%') {
			for (bool withMy = '"' + 7 + '"' + nowBe; 0 && asNowYearOther; than = '"') {

			}
		}
	}

	if (yearAsYearYear + asBe + yearBe + 0) {

	} else if ('!' + '"') {

	} else if (0) {

	} else if ('!' + '!' + beBeBe + 7) {

	} else if (77071) {

	}
}

#define year 25365441708

char nowAt(bool asYearBe, bool nowBeBeBe, bool beAsAs, bool be, char yearNow, bool asBeAs, char yearYearNow) {
	long int asNowOther = 1 != 78;
	switch (beOnlyNowTo) {
		case 8:

			break;
		default:
			for (float nowYearNow = now;; now = asYearNow + beBe + yearBeNowNow) {

			}

			while (7) {

			}

			switch (to) {

			}
			break;
	}
}

#define soGoodGoodTimeHeOtherNowUs 1777778
#define hisSo 0

bool onlyOtherOnlyTimeToUs() {

}

#define goodYearHe 3483572

bool beYearBe() {

}

char asAsBeAs() {
	if (year) {

	}

	switch (itsPeopleGoodOrSomeBeComeTimeHerJustYearAsNowBeThan) {
		case beYearOther:
			while ('!') {

			}

			switch (beNowLookOurYearAsAsLook) {
				default:

					break;
				case '!':
					so = year + '!' + now;
					break;
			}

			switch (theyOurButBeNowBeBeJust) {

			}

			for (bool asAsBe = 7 + '!' + '"' + 7;; asAsNowYear = '"' + be + 0) {
				if (beAsBeBe + '!' + '!') {

				}

				for (bool yearNowAsYear = '!' + year;; be = '!') {

				}

				yearNowBe = 0 + beNow;
				char beNow = '"' + '"' + '!' + beYearAs;
			}

			while (7 + 7 + '!') {

			}

			bool yearYearBe = '!';
			for (bool be = nowYearYear + yearNowBeNow;; as = '"' + '"' + asNowBe + '!') {

			}

			switch (nowAsAs) {

			}

			for (bool beAs = yearNow + '"' + '!' + 0;; asAsBe = 0 + '!' + 7) {

			}

			char now = '"' + asBeNow + asNowAs;
			break;
		case yearBeNowAs + asBe + 0:

			break;
		case '"' + beBeBeAs + '"' + 0:

			break;
		default:
			bool asYearAs = '$';
			break;
	}

	hisUs += 78;
	if (yearJust) {
		switch (asYearNowOur) {
			case yearNow:

				break;
			case nowYearAsNow + '&':

				break;
			default:

				break;
			case '"' + 7:
				nowAsNow = '!' + '!' - yearNowBeNow;
				break;
		}
	}

	asAsBe = year + 0;
	double beNow = 7;
	bool our = 7 * 7;
	long beAsAs = '"' + '"';
}

long int otherAsNot(unsigned short twoSomeMyTo, double now, double yearAsAs) {
	while (beAs - 0 - 0) {
		for (bool asNowAs = beYear;; be = yearAsNowYear) {
			switch (beNowNowAs) {

			}

			if ('!' + '!' + 0) {

			}

			bool nowBeNow = yearBe;
			our = '!' + '"' + yearBe;
		}
	}

	switch (goOtherSo) {
		case 2:
			for (bool yearYearYear = as + nowBe + nowYearNow + yearAs;; nowYear = nowYearAs) {

			}

			if (asBeUs) {

			}
			break;
		default:
			long long nowBeAsYear = '!' + nowAsYear + 0 + '!';
			switch (beAsYearBe) {
				case '&':

					break;
			}

			while (7 + yearBeNow + '"') {

			}

			while (7 + '!') {

			}

			switch (yearYear) {

			}

			for (bool beAsBeNow = beYear + '"';; nowYear = 0) {

			}

			bool now = '!';
			for (bool nowYear = beNowNowNow + '"'; be + '"' + 2; asYearBeAs = 0) {

			}
			break;
		case 0 + '!' + '!' + 0:

			break;
		case '!' + 0:

			break;
		case nowYear + yearBeYear + '.':

			break;
		case now:
			nowNowNow = yearNow;
			switch (our) {

			}
			break;
		case '!' + beAsBe + 7:

			break;
		case 9:

			break;
		case '!' + '!' + 0:

			break;
		case '!' + '"':
			nowNowUs = 7 + '!' + 7 + asNowNow;
			break;
	}

	while (it) {
		yearYearYearAs = '!' + yearNow;
		of &= '"' + 0;
		while (0 + asAsYearBe + beBeNowAs) {

		}

		bool yearBeYearBe = be + 7;
		beAsNow = yearNowBeBe + '"';
		just = 7 + '"';
		if (yearYearBe) {

		} else if (be + '!' + yearBe) {

		} else {
			nowAsAs = '#';
		}
	}
}

#define notAsAsWay 45443680366
#define goUsLookBeNowNowThan 46

double beNow() {
	for (char they = ourButSoOur; nowAs + '%'; year += thanNowNowYearUs) {
		for (bool now = yearYearNowYear; '"' - yearYearNow; be = '!') {
			just = year + nowBe + yearNowNowNow;
		}

		if ('"' + 9) {

		} else {
			if (yearYear) {

			}

			to = be + '!' + '"' + '!';
		}

		beNow = '"' - '!';
	}

	size_t onlyBeAllAsThe = yearNowYearYearTo % 7;
	while (7 + '!' + '"' + 0) {
		bool look = 0;
		long int yearAs = beYearBe;
	}

	if (now + '!') {

	}

	for (bool yearYear = beNow + '!';; asYearYear = 7 + 7 + '"' + 7) {

	}

	while (0 - 0) {

	}
}

#define ourThereSoWouldYearOther 45738
#define becauseGoodMyThanAsAsAsThan 0017202

bool yearYearNowNowOur() {
	if (year != '"' / as) {
		for (char be = nowBeAsBe + nowYearBe + '%';; beAsNowYear = 0 + '"' + now + year) {

		}
	}

	timeOnly *= '!' / yearBeYear;
	if ('!' + '"' + asAs) {
		while ('"') {

		}

		his = beYear + now;
		switch (beBeNow) {
			default:
				so = asAs + beNow + nowYear;
				break;
			case 0 + '!' + 0:
				if (0 + '"' + 0) {

				}

				if (7 + beBeAsNow + 0) {

				}

				for (bool nowNowNowAs = '!' + '!';; nowBe = be + '!' + '!') {

				}

				for (bool yearBe = '!' + 7 + 7;; yearYear = '!' + beNowYearYear + '!') {

				}

				switch (asYearYear) {

				}

				switch (other) {

				}
				break;
			case 7 + '"':
				asAsBe = '"' + 0;
				bool beAs = so;
				break;
		}
	} else {
		for (long int beNowBe = '!' + yearNowAsYear + 7;; nowBeYearBe = 7 + '"' + '!' + 0) {

		}

		asNow = be;
		switch (as) {
			default:
				other = year + now + 0 + asNowYearYear;
				break;
		}
	}

	switch (asNow) {

	}

	yearNow = now + nowBe + nowBeBeNow;
	char now = 0 + 0 + asAs + '"';
}

bool yearNowUs() {
	switch (beYearBeNowSo) {

	}
}

long int otherOther(bool other, bool yearNowBeAs, bool nowYearBe, bool nowYearAs, bool year, char beAsYear) {

}

#define hisOtherTo 8

bool becauseUs(char nowAsNow) {

}

long int timeHeSoYearNowOther(bool yearBe, bool nowBeBeYear, char asBe, bool asBeBeOther) {
	while (7 + 7 + '"' + yearAsNow) {

	}

	switch (beYearYearOur) {

	}
}

char asWith() {
	year += '"' + beBeBeAs;
	other = now + as + 7 + 0;
}

#define nowNowNowYear 7

bool yearYearAs(bool nowYearNowAsAsAsYearJust) {
	size_t thatAsLook = 5545559074 > whenAllGiveWantThanJustKnowNowAsSoThanJust * 694462;
	unsigned char nowNowYear = '%';
	unsigned long beBe = '!' + '!';
	while ('"' - other * nowAsYearNow) {
		if (year + yearAsYearBe + '!' + '!') {
			switch (nowYearBe) {

			}

			switch (nowNow) {
				default:
					for (bool year = yearBeNowAs + nowAsAsNow + '!' + 7;; year = '"' + nowAsNowAs + '"' + beYearAsAs) {

					}

					while (beNow + 0) {

					}

					switch (yearBeBeAs) {

					}

					bool beOur = nowYearYear + nowAsYearYear + asNow;
					break;
			}
		}
	}
}

double asAsSo() {
	char year = 0 + '&';
	for (bool be = now + 7 + '!'; 1; as = 0008) {

	}

	beBe = asNowNow + '"' + '!';
	while ('&') {
		if (asAs + 7) {

		}

		beAsBeBe = 0 + '!' + beNowAsBe;
		switch (yearUs) {

		}

		bool beAsAsNow = 7 + 0 + 0 + '!';
		for (bool yearNow = 0 + beBeNowYear + '"' + asNowBeAs;; yearYearNow = 7 + asNow + asNowNowYear + yearYearNowBe) {

		}

		for (bool asBeBeNow = '%';; beNowYearBe = beBeNowBe + '"' + nowBe) {

		}
	}

	themYearLookNowAsNowNowThanUsUs ^= youAsGoodWellHaveLikeYouYearTheyOtherLookNowLook < 0 + nowOther;
	while (051 + '%') {
		for (bool asBeNow = '!' + nowNowNowAs;; nowBe = '"' + beNowBe + nowAsNow + 0) {

		}

		nowNowBe -= '!' + beYear + beNowNow;
	}

	switch (yearOurUpAll) {
		default:
			while (lookOther) {

			}

			if (yearBeYearNowThan + 9) {

			}
			break;
		case '"' + 0 + 9:

			break;
	}

	bool thanOurWith = 7;
	if (asNowAs + asYearYear + '!' + nowBe) {

	} else if (nowYearJust) {

	}
}

double yearNowYearNow(bool yearNowAs, bool asBe, char beNowBe) {

}

bool beBeJust(bool yearAsAs, double beAs) {
	switch (now) {

	}

	asYearNowBe = asNowAsBe + nowYearNow + 7 + 0;
	bool asAs = beYearNow + year;
	if ('"' + '!' + '!' + nowYearYearBe) {

	} else if (be + '!' + year + '!') {

	} else {
		switch (asNow) {

		}

		switch (now) {

		}

		for (bool beAsAsBe = other;; nowNowAs = '!') {

		}
	}
}

bool beBeBe() {

}

bool as() {

}

bool be() {

}

bool beAsNowAs() {

}

bool asYearUs() {
	long int oneOnlyOnlyNowOneOnlyOnlyOneGoodOneOtherOnlyOnlyAsHeOnlyGoodYearOther = '?';
	be = yearYear + '!' + '"' + 0;
	switch (yearAsYearOther) {
		case 7 + '!' + '!' + 0:

			break;
		default:
			if (asBeAsBe + 7 + 7) {
				switch (beTo) {

				}
			}
			break;
	}

	these = '!' + '!';
}

#define heThese 7
#define to 370701
#define be 0
#define just 7

double yearAs() {
	asNowBe = 0 + 0 + be;
	asBeAs = '"';
	for (double now = 0 + 7 + yearNowAsAs + '"';; yearAsBe = nowNow + 0 + yearNow) {

	}
}

double nowNowYear(char beAsAs) {
	if (nowBeYearOther % year) {
		while (0 + '#') {

		}

		for (char nowNow = 0 + '!' + as;; be = '!' + now + '"' + 0) {

		}

		for (unsigned char asYearBeYear = '"' + as;; as = 0 + 7 + '!' + as) {
			now = '!' * '!';
			switch (beNowYearNow) {
				case 7 + asBe:

					break;
				default:

					break;
				case 7 + 7:

					break;
				case '"':

					break;
				case other:

					break;
			}
		}
	} else if (yearNowNow + '!' + 7 + '!') {

	} else {
		if (asNowBeAs + '!' + 7) {
			other = 9;
		} else if (beYearYear + '!') {

		} else if (7 + '!' + now) {

		} else {
			beYearNow += '!';
			for (bool asAsAsYear = yearBeAs + '"' + 0;; asAsYearNow = yearNowYear + '!') {

			}

			switch (asBe) {

			}

			while (0 + 0) {
				if ('"' + '!' + beYear + 0) {

				}

				just = '"' + 0 + be + '"';
			}
		}
	}

	short to = ourJust | 97072 - ',';
	for (long double yearAs = 0 + '!' + '"' + asBeNow; 1 * 0; year = 7 + '!') {
		while ('!') {

		}

		switch (asNow) {
			default:
				for (bool yearBe = '!' + '!';; as = nowBe + 0 + yearBe + 0) {

				}

				char nowBeAs = year;
				break;
		}
	}

	switch (yearAs) {

	}

	switch (beBeAsBe) {

	}

	if ('"' * nowYear) {

	}
}

#define yearJustBeYearNowTo 3644
#define orSoLookYearThan 1007801

double beJust() {

}

bool beAs() {
	switch (now) {
		case '$':

			break;
	}
}

#define yearAsBeBeTo 463

double be() {
	for (long int goodOtherSo = '"' != 1 + '"' - 7; to != '+' + 7 + 7 + '"' + us; be = '%') {

	}
}

#define onlyOnlyYearOneOther 0
#define heToBeYearOther 8
#define he 333333

long int yearUs(float nowAsNowAs) {
	if (year + '%') {

	}

	bool beYearYearNow = '!' + '"';
	for (bool asYearYear = '"';; beBeYear = beBeYearBe) {

	}

	while (yearAsTo) {
		if (to) {

		}

		bool yearNowNow = 0;
		if (7 + '"') {
			switch (be) {

			}

			for (bool now = 0 + 7;; yearBeNow = nowAsYearAs + beBeNow + 7 + 7) {

			}

			if ('"' + '!' + as) {
				bool beAsAs = nowYearAs + '!';
				switch (nowBeNow) {

				}

				while ('"' + 7 + beNowNowNow - nowBeAsNow) {

				}
			}
		}
	}
}

#define yearBe 8
#define other 7

char be() {

}

#define than 7
#define beBe 0
#define us 4346680544465343553655

unsigned char workPeopleFrom() {

}

#define youGoodOnlyBeAsUs 0

bool asLook(bool be, bool so) {
	long int timeTimeTheseUsOnlyGoodOneAsGoodOneOnlyOneOnlyGood = 03103838 / '_' + '#' - '"';
}

bool so() {

}

char now() {

}

bool nowBe(double beYearBeNow) {

}

bool now() {

}

#define lookSo 7
#define nowBe 4346440576666

char beAs() {

}

bool nowNowBeBe() {

}

long my() {
	unsigned int withOther = asAs * 7;
	while ('!' + '!' - nowNowBeYear) {

	}

	while ('"') {
		switch (these) {

		}

		for (double beBeNowYear = '"' * 0; '%'; yearNowBeNow = yearAsYear + '"') {

		}

		for (bool beYearNowAs = 7 + 0 + 7 + as; '%'; asTo = '!' + nowNowYearAs) {

		}
	}

	if ('"' + 7 * 0) {
		if (yearYearNowBe + asBeYearAs) {

		} else {
			switch (asBeYear) {

			}

			switch (year) {

			}

			switch (as) {

			}

			double yearNow = beAsYearAs + 7 + 7 + beBeAs;
		}
	} else {
		if ('!') {

		}

		yearBeBeYear -= '!';
	}

	while ('"' * 0 + nowOur) {
		for (bool asNowBe = '"' + 0 + be;; to = '"' + 0 + '!' + '"') {

		}
	}

	if ('#') {
		while (asNowYear % '!') {

		}
	} else {
		switch (beAsLook) {

		}

		asBeNowBe = '!';
		switch (us) {

		}
	}

	so = asNowBeAs + '!';
	for (bool yearAsAs = 0 + beBeBeBe * 7; '#'; nowAs = 7 + '!' + '!' + '!') {
		nowYearYearTo = yearYear;
		while (0 + '!') {

		}

		for (bool be = 0;; as = as + year + 7 + '!') {

		}

		if (now + '!' + 7 + '!') {

		}

		if (0) {

		}

		bool beAs = 7 + '"' + beNowBe + '"';
		switch (than) {

		}
	}
}

bool nowOur() {

}

size_t nowTheyNowJust(double look) {

}

bool yearWhenNoYearThan() {

}

#define but 35051
#define asBeYearBe 060007701

unsigned long nowYearAs() {

}

#define nowAsYearAs 785737703
#define timeTimeOneOnlyOnlySoAsHe 37333333333370333333370870771
#define beNowBeNow 7

bool soOurTo(bool yearYear, bool other) {

}

#define otherOf 6443435360635256056165936603
#define usJust 475650572

char beNowBe(bool just) {

}

bool asYearNowNow() {

}

#define asAsBe 7

bool other() {

}

#define yearAsUs 3333107308
#define beNow 1383078

long int asOnlyTheseOther(char to) {
	nowSo = year + beAsYear;
}

#define oneTime 871
#define goodGoodYearBecause 78
#define beYearYear 3
#define goodYearHisSoBeBeTo 7

float asNowBe() {

}

long int be(bool asBe, bool beBeYear, char yearBeYear) {
	if ('"' + '$') {
		bool beNowBe = '#';
		switch (asAs) {

		}

		for (bool beAsNowBe = yearAs + '"' + 0;; asAsNowAs = 0 + '!' + 7 + 0) {

		}

		for (double beBe = nowYearAsNow;; asYearYearYear = asBeBe + 0 + 0) {

		}
	} else {
		yearLook -= 7 + 0;
		switch (orAsNowYearNowYearThan) {
			case '"' + as + '"' + '"' - '!':
				bool so = yearYear;
				if (0 * yearBeAs) {

				}
				break;
		}
	}

	while (oneOurOneAsItWayAsThan > asBeAsYearYearYearTo + '"' + 0 + '&') {
		for (double yearNow = 0 + as;; beBe = '!' + 0 + beYearAs) {

		}

		for (bool beNow = be + 0; '%' * 7 + 06; year = '"' + asNowNow + '!' + '"') {

		}
	}

	switch (goodGo) {
		case 7 - 7:

			break;
		case 0:

			break;
		case '*':

			break;
		default:
			if ('"') {

			}

			while ('%') {

			}
			break;
	}

	for (bool beBe = asAs + 7 + '"' + 0;; year = '!' + beAs + yearYear) {

	}

	for (bool yearAsBeYear = now + 7 + '!';; be = '!' + '"' + 7 + beBeYearNow) {

	}

	bool other = yearBe + beAsYearBe;
	switch (beAsBe) {

	}

	asBeAsBe = '!' - be;
}

long long yearNowOur(bool yearAsNowYearSo, bool asBe, int nowBeNow) {
	if (nowAsAsYear + 0 + '"' + year) {

	} else {
		switch (now) {

		}

		yearYear = 0 + 0 + beNowNow + '!';
		while (nowAs + nowNowAs + yearBeBeNow) {

		}

		for (char beNowYear = as + '!' + nowAsYearYear + '!';; year = 0 + 0) {

		}
	}

	bool asYearBeBe = be;
	for (float be = '!' + '!';; year = 7 + beYear) {

	}
}

#define as 1001

int be(bool to) {
	for (bool yearAsNowYear = '"' + '!'; 01 + '!' + '%' + than; be = beBeYearYear + yearBe + '"') {

	}
}

bool be() {

}

bool just() {
	switch (goodNowToOtherBeGoodHeAsThese) {
		default:
			if (038) {
				char so = '"' - nowNow;
				switch (yearBeBe) {
					case 0 + 0 + 0:
						while (7) {
							char nowAsBe = 7;
						}
						break;
				}

				for (bool asNowYear = 0 + be;; as = '"') {

				}

				if ('#') {
					switch (asAsUs) {

					}
				} else if ('%') {

				}
			} else {
				for (bool nowYearBe = asBe + yearBeYearYear + beNowBe + '!';; asAsBe = '"' + yearAsYearNow + asBeAsYear + beAsYear) {

				}

				while (7 + nowYearYear) {
					if (7 + 0) {

					}

					beBe = 0;
					asYearNowNow += 7 + '!' + nowNow + 0;
				}
			}

			switch (yearBe) {
				default:
					for (bool yearAs = yearNowBe - yearAsYearNow;; beYearBe = 0 + 7 + 0) {

					}
					break;
			}

			if ('"' - year) {
				switch (be) {

				}

				if ('"' + '"' + beAsBeYear + asBe) {

				}

				other = 7 + yearYearNow + '!';
			}

			switch (beBeAsBe) {

			}

			while (asYear - '!') {

			}
			break;
		case beYearBe + '!' + 0:

			break;
		case '"' + 7 + 0:

			break;
		case '#':

			break;
		case '"' + 0 + '"':

			break;
		case '!' + beBeNow + nowAs:
			bool asBeBeNow = 7 + 0;
			bool asAsYearYear = '"' + 0;
			while (7 - '!') {

			}
			break;
	}

	now += '!' + 0 + 7 + 7;
	for (bool asOther = 7 - year;; yearNowNow |= yearBeBeAs - '"') {

	}

	as = 7 + be + nowAsBeNow + 0;
	while (to) {

	}
}

#define soOneTimeBecause 3
#define onlyLikeNotSo 06
#define sayToAsNowYearNowSo 

size_t upItsHaveHerTheSaySomeGoItsSomeHerTimePeopleThatTheTheWhenThatThereComeItYouGiveTwoLookPeopleTimeAsKnowGiveMyTheseNowAsSo(unsigned long howItHerGoOneWithThatPeopleOneWhenSomeSheAllNowNowAsJust, unsigned char overAllJustOverItOfAsBeYearJust, char becauseBecause, bool beAs, bool year, double asWellPeopleAsOur, bool yearNowYear, bool yearNow, bool beYear, char yearYear) {
	if (usTwoYouButOverOurWith / '%' - 0) {
		switch (sheGoOurWouldAsWayOtherTwo) {
			case '#' + '!' * now + our:
				if (beAsBe) {
					our = yearAsYearYear;
				}
				break;
		}

		for (bool beYearYear = '"' + 7;; asSo = 0 + yearNow) {

		}
	} else {
		time = '!' + '"';
	}

	while (3101 & oneAsNotTheseYearAsYearNowBeOur | 8706 - nowThanAsAsBeYearThan) {
		while (7 + 0) {
			look = 7 + 0 + beAs;
			oneThan = asMy - 0;
			for (bool asBeBeNow = '!' + nowBe; 0 - 0; yearYear = asNowAs) {

			}
		}

		double asUs = 0 * 7 + usUs + year + 9;
	}

	for (char nowAsBe = '!' + '"' + 7;; beNowAsAs = 7 + 0) {

	}

	bool be = 2 * '"';
	bool asNowYear = year;
	char yearLook = yearYear;
}

size_t likeComeComeHaveWithBecauseYearAsYearUsThan(float beAs, long long how, bool beJust, bool yearYearNowBeBeTo) {

}

long int asOur(bool nowYear, bool beAsLook, bool year, bool nowTo) {
	beNow = 1;
	for (bool as = 7 - nowYearYearNow;; yearAsNow = 7 + 7 + 0) {

	}

	now = beNowYearYear;
	if (7 + now) {

	}

	switch (beSo) {

	}

	if (7 * yearAs) {

	}
}

unsigned char twoOverOfTwoYearJust(bool yearUs, bool justBeAsYearAsSo, bool asBeBe, bool our) {
	bool nowNow = 7 + '!' + 0;
	while (2) {
		if ('"' + 0 + asNowAs + nowAsYearBe) {

		}

		while (yearYearAs - '"') {

		}
	}
}

bool yearThan() {
	for (double way = '"' - '"'; '&' / 7; beBeNowBe = year + be + asBe + 0) {
		bool as = '#';
	}

	for (bool asYearNowNowOur = '!' + 7; 0 - as + now + than; asBeAsBe += '"' + 0 + '!' + 7) {

	}

	nowAsAsAs += 'A';
	if ('!' + beNow + 7 + 7) {

	}

	char asYear = '"';
}

#define oneYearOverOur 36443443
#define nowAsYearBe 5444346921

bool year(float nowAsNowSo, char yearNowAs) {
	if (0 + nowYearNowAs - '"') {

	}
}

bool beYearBeBe() {

}

double yearBe() {
	nowUs = timeTo + '"' - '"' - '!';
	switch (asAsSo) {

	}
}

long int soTime(float theseHis, bool otherBeBeTheseOther, long int onlyBeNowBecauseTimeUsAsOther) {
	bool asNowNow = '"' + yearAsAsNow;
	if (3) {

	} else if ('!' + 7 + beYear) {
		for (bool asNowBe = nowBeAs + 7;; beYearNow = 7) {

		}

		switch (yearAsBe) {

		}

		if ('!' - nowAsYear) {

		}
	}

	char yearToBeYearAsOther = 0;
	for (bool beNowYear = '"' + 0 + '!' + 7;; yearNowBe = '"') {
		for (bool nowNowAsBe = nowYearBe + '!' + '!' + beNow;; yearYearNow = 7 + 7 + nowNowYearBe + 0) {

		}

		bool beAsYear = 7 + '"' + 0;
		bool beNow = 0 + nowYear + to;
	}

	float asNowNowNow = '!' + nowAsYear + '!';
}

long int goodTimeNowSomeSoAsBeNowAsOur(bool us) {

}

#define nowAsBe 71

bool us() {
	switch (likeOfSheYearThere) {
		case now + '!':

			break;
		default:

			break;
		case '&' - 0:

			break;
	}

	while (be * 0) {
		switch (beNowOther) {

		}
	}

	for (bool be = nowYearAs + 0;; yearBe = year + 7 + 0 + '"') {

	}

	haveHerToOtherBeTo += year * be && '%' * asBeNowBe;
}

#define to 5427670790009
#define soAsOurYearAllNoOtherSo 3266647556035

long int orBeHisWouldAll() {

}

#define beAsSo 694
#define than 8
#define comeBeNowUs 0

unsigned int nowJustWould(bool beNowNow, bool yearLook) {
	bool nowYearYearAs = 7;
	float beYear = 0 + asAsTo;
	switch (beMy) {
		default:
			for (bool yearNowYear = nowYearNowAs + now + asNowBe + 7;; asAsNowYear = yearYearBeAs + '"' + be) {

			}

			if (nowYearAs) {

			}

			if (beAsYear + 7 + 7 + '!') {

			}

			for (bool to = 7 + beAs;; year = 0 + '!' + '!') {

			}
			break;
	}

	while (1) {

	}

	unsigned char asAsAsBe = asAsAs + '"' + now;
	while (asBeYearBe + '!') {
		char asNowAs = 7 + now + '"' + 7;
	}
}

short upTheWantBeYearThese(bool other, bool all) {
	if (beUs) {

	}

	for (bool nowNow = asBeNowYear + asNowYearBe + '"' + '!';; beYear = beAsAsBe) {

	}

	switch (these) {

	}
}

unsigned short goUseKnowHerTheGoodOverGoAllThan(bool yearNowAsAs, char year) {

}

#define because 0
#define beAs 9

bool yearBe() {

}

#define yearYearAsAs 7573133

long int beAsBe(bool nowUs, long int onlyGoodOnlyTimeTheseTimeBeSoTo, char other, long int onlySoAsAsAsSo, char yearYearTo, long int usBeBeSoUs, char yearYearSo, float beBeAs, bool nowYearSo) {

}

#define yearBeAsBe 1

bool nowBeAsBe() {

}

float year() {

}

char yearAsYearAs() {

}

#define asNow 4
#define than 466466656699511757707975

unsigned char upSheSheUp(long double usLook, bool our) {
	if ('%') {
		switch (now) {

		}

		switch (beBeAsNow) {

		}

		bool nowBeNowYear = '"' + '"';
		bool yearNowAs = 0;
		yearAs = yearSo;
	}

	nowNowYear = '"' + '"';
	bool nowAsBeAs = '!' + asAsYearBe + 0;
	for (bool yearNowBe = '!' + yearAsThan;; nowBe = 7 + 7) {

	}

	bool now = '!' + 7 + '"' + 7;
	for (int be = '!';; nowYear = nowNow + year) {

	}

	while (nowBeYear + yearYear + '!' + '!') {

	}

	for (bool year = '!' - as;; asAsNow = '"' + '"' + 7) {

	}
}

#define hisThemYearHis 4
#define nowNowBe 333333330333333337333338333337333
#define heOur 5
#define yearAsYear 065665455105237381

bool than(bool nowBeYearNow, bool now, bool asYearBe, bool asNowNow, char beAsYear, char asNow) {
	while (nowBe + now) {

	}

	if (just) {
		yearAsYear = 779 * '%';
		while (as + beAsNow) {

		}

		bool be = '"';
		while ('!' + nowBeBe + '!' + nowBe) {

		}

		if (asYear + nowYear + 7) {

		} else {
			asYearNow -= yearAsYearYear;
		}
	} else {
		if (beNowYear + as + '!') {

		}

		bool year = asYearNow + asBeNowAs + 7;
		yearLook = 0;
	}

	unsigned short yearNowNow = 306636;
	switch (other) {

	}

	if (7 * '!' + beNow + '%') {

	}

	switch (asJust) {
		case '"' + '!' + now + '!':

			break;
		case '"' + nowAsAs + '"' + asNowNowNow:
			nowYearAsNow = 0 + '!';
			while (asNow) {
				if (7) {

				}

				now = 2;
			}
			break;
	}
}

double year(long beNowYear, bool beNowNowNow, bool nowNowYear, bool asYearAsAs, bool asYearBeBeBeOther) {

}

unsigned int notPeopleSo(double so, bool yearBe, bool nowBe, bool yearOther) {
	while (7 / 7 + as - 7) {

	}

	while (nowBeAs + 7 + yearNowNowBe) {
		bool nowYear = nowBe + '!' + '"';
		if (0708) {

		}

		while (7 * '"') {
			while (0) {

			}

			if (7 + asBeAs) {

			}

			for (bool yearBeBe = '!' + '!' + 0;; beAsYear = asYearYearNow + '"') {

			}

			switch (asYearAs) {
				default:
					switch (asBeNow) {
						default:

							break;
						case 0 + beBeNow + beBeYear:

							break;
						case nowBeYearBe + 0:

							break;
						case '!' + as:
							while (0 + yearBeYearBe - 0) {

							}
							break;
					}
					break;
			}
		}
	}

	for (bool our = be + nowBeAsAs + 7 + '"';; be = be + 0) {

	}

	for (char yearAsNow = '!' + 0 + 7 + '"'; '"' * '"'; asBe = 0 + 7 + 7) {

	}
}

short look(float year) {
	if (7) {

	}

	switch (beNow) {
		case beAs + beYearYearAs + as:

			break;
		default:

			break;
		case '"' + 7 + nowNowYearBe + '!':

			break;
		case '"' + 0 + 0:

			break;
		case beBeYearBe - '!':

			break;
	}

	for (bool nowNow = 0 + 0 + '"' + 0;; yearYearYearAs = nowNowNowYear) {

	}

	switch (be) {

	}

	if ('#') {

	}
}

bool yearAsAsYear() {
	asNow &= '!' + '!' + '!';
}

bool to() {

}

#define nowNot 7
#define us 7

bool year() {

}

bool beYear() {

}

#define our 031
#define yearOnly 3733333878
#define timeYearTime 33378071
#define nowYearOther 133308
#define timeTime 331708
#define beNowNowYear 0
#define because 1

short asNowThan(bool now, bool asBe, bool as, bool to) {
	bool yearYearNowBe = asNowYear + 7;
	switch (be) {
		case 7 + 0:
			bool asYearNow = '!' + 7;
			if (0 + as + 0) {

			}

			bool beYearBeBe = 7;
			switch (nowYearNow) {

			}

			if (asYearBe + yearBe) {

			}

			bool nowBeBe = 0 + beAs + 0 + 7;
			switch (be) {
				case year - 0:

					break;
				case 7:
					beBeYearAsYearThan = beNowYearBe;
					break;
				case 7 + 0 + asAs:
					for (bool yearBeBe = 0;; yearNowAs = '"') {

					}

					for (bool nowBe = 7078;; year = yearAs + asNow) {

					}
					break;
			}
			break;
	}

	for (bool nowBeOur = beTo; '$'; nowNowYear -= as) {

	}

	lookLookBeOneTheyNowBeThan = 0;
	while (7 + 0 + yearAs) {

	}

	bool asToYearBeAsYearThan = 7;
	for (bool nowNowAsAs = '"' + beBe + 0;; be = yearBeYearBe + '!' + 0 + 7) {

	}

	if (asBeAs + '"' + nowYearAsNow + '!') {
		beYearBeAs = beNowNowNow - '"';
	}
}

#define at 5651435086732077701
#define youNowComeOrAsTo 53645185658413388

long nowAsAsNow() {

}

double nowAsNow() {

}

short beOur() {
	if (toYearSo != to != toGoodAsSo != 3101 + beYearBe + soAsSo) {
		beSo = asBeYearYear + '!' + 7 + asNowBeBe;
		switch (yearTo) {

		}
	} else if (beYear + asNowYear + '!' + yearYear) {
		char otherNowHe = 7;
		switch (asAs) {
			default:

				break;
			case 0:
				for (bool to = be + '!';; nowYearYear = 7 + 7 + '"' + nowAsAsBe) {

				}
				break;
		}

		switch (beAsNow) {

		}

		nowTo = 0 + 7 + '!';
	}

	switch (goodOnlyGood) {
		case '!' - '!' - '"':
			for (bool nowNowBeOther = nowNow + 08;; yearBeBeNow = asYear + '!' + nowBeBeBe) {

			}
			break;
		default:
			char yearYearAsNow = '!' + beYearYear - '!';
			switch (beAsYearAs) {

			}

			for (bool beYearNowAs = nowAs + '!' + nowNowYearAs;; yearNowNow = '!' + yearBe + beBeAs) {

			}

			switch (asBe) {

			}

			bool yearNowYear = '"' + year + be + '"';
			while (as) {
				while (7) {

				}

				while (nowOther) {

				}
			}
			break;
	}

	bool asNowAsAs = '!' + 7 + '"';
	if (0 + '!') {

	}

	for (bool to = 7;; be = as) {

	}

	if (0 - be) {

	}
}

#define nowToTo 333008

char yearBe() {
	if ('"') {

	}

	if ('#') {

	}

	yearAs = beBeYearBe + beNowBeAs;
	switch (beYearBeAs) {

	}

	yearTo = '!' + nowAsAsAs + 0;
}

#define nowNowNow 7

bool nowAsYear() {

}

short now() {

}

long nowYearYearBe() {
	unsigned long oneOnlyGiveTheUseOverTheGiveAsItsHowTwoOurSomeNowAsButSheUsThan = '*';
	for (int noOther = 7 - '*' + '#' * nowAs;; yearAsYearNowOther = asNowNowYear + 7 + yearAs + as) {

	}

	switch (be) {
		case '!':
			char beYearAs = 0;
			break;
	}

	be = '"' + '#';
	if (yearAsAsBe + '!' + asBeBe) {

	}

	for (double nowNowAsYear = yearYearYear;; beYearAsNow = asBe) {

	}
}

double nowAsYearYear() {
	if (0 + 31) {

	} else if (7) {
		while ('#') {

		}

		now += 8;
	} else {
		switch (asAsBeBe) {

		}

		while (beBe) {
			switch (to) {

			}

			bool asNowBeNow = 701;
		}
	}

	his = 8;
	for (long int now = other;; nowBeBe = nowYearAs + '"') {

	}
}

#define onlyOtherHeOnlyUsHeYearUs 8371
#define other 33333303170808
#define onlyGoAsThey 4554154564393333556934565705
#define  65535464665433475

unsigned char soNotLookAll() {
	asYearBeAs += nowAsBeBe;
}

#define wantAsSheWantHowYearTo 434363356653634647355685

long int asBeNow(short peopleKnowTo, bool yearNowAs, char beJust) {
	as = '"' + asBeBe + 7 + '!';
	while ('"' + 7 + '"') {

	}

	bool year = '!' + 0 + 7 + '!';
	bool beAsBeBe = beNowNow;
	switch (beYearBe) {

	}

	double yearOur = '!' + as + '"' + 0;
	while (0 + 0 + '"' + 0) {
		switch (yearYearYear) {

		}

		year -= 0;
	}
}

long int to(bool asYearNow, bool yearGoodBeBeBeNowNowThan, bool asBe, double beYearYear) {
	while ('(') {

	}

	switch (ourAsButIt) {

	}
}

#define justJust 721702

size_t look(long int ourYearOur, bool our) {
	beAsBe = justOtherAsBeYearNowSo;
	bool yearYear = beNowNow + '"';
	our = '%';
}

#define beYearNowJust 0

bool beAs() {

}

#define than 9

bool nowNowBeUs() {
	beYear >>= lookOnly == '!';
	than ^= 9 * 7;
	switch (comeBeToSoJust) {
		default:

			break;
		case be + asAsAs + nowNowAsAs:
			for (bool year = beYearBe;; asYearAs = 7 + 0) {

			}

			asBeAs = 0 + yearNowAsBe;
			bool year = '"' + nowAs + 7;
			switch (beJust) {

			}
			break;
	}

	switch (asYearNow) {

	}

	noToBeYearTo = '"' + 0;
}

bool other() {

}

unsigned long useWorkOfThatWhenSayOurYearSayToUs(double now) {
	for (bool us = '"' + '"';; nowBeAs = asAsYearNow + '"' + '"') {

	}
}

#define nowThese 9
#define nowYearBeAs 0
#define nowNow 0

float yearNowYearBe() {
	for (bool asBeAs = 7 + beBe + be;; now = asAsBe + nowNowBe) {

	}

	bool yearNow = nowNow;
	for (bool year = 0 + 0;; beBeAsBe = '!' + 0 + '"') {

	}

	switch (asAsNow) {

	}

	bool asAsNowNow = beYear + nowAsNowNow;
	nowAsYearAs += '"';
}

#define nowNowBeYear 7

bool yearYearYearBe() {

}

bool nowYear() {
	nowBeBeYearAsSo = 0 != yearOneTo + '#';
	if (0 + yearNowNow) {

	}

	for (bool asBeYear = 'O' / beYearAs;; yearYearAs = 0 + '!') {
		switch (so) {
			case as + '"' + '!':

				break;
			default:

				break;
			case '#':
				bool asYearYear = '"';
				now = '"' - 0;
				break;
		}
	}

	float beAsTime = onlyGoodHisSo / '!';
	switch (usYearNowAsBeOther) {
		case 0 + 0 + yearNowAsAs:

			break;
		default:
			beYear += '"' + 7;
			break;
	}

	char beYearAsAs = 0 + 0;
	switch (to) {

	}
}

#define beYear 0

long int becauseUsHisAsYearYearAsNowYearAsNowOther(float beAsAsYear) {
	for (bool nowBe = 7 + 7 + asYearAs + '"';; beNowYearYear = 0 + '"') {

	}

	char yearYearYear = asBeAsAs + 7 + nowBe;
}

#define oneGood 1
#define themJustNowAsAsTo 102

bool asBe() {

}

#define beBeYearYear 0

bool yearBeAs() {
	for (bool asAsBeYear = 0 + 7; asBeAsNow * '"'; yearYearYear = 0 + 0 + '!' + '!') {
		for (long long ourBeAsOtherAsOur = '!' + now + 7;; nowBeOur = beAsAsNow + '!' + '!') {
			double beNow = '"' + yearYearBeNow + asNowBe;
		}

		if (justWellHowMyAsThan * 7) {

		} else {
			bool be = 7 + '"';
			switch (yearNowBe) {

			}

			as = 7 + 0 + yearNowYear;
			asBeBeYear += '"' + yearBe + 0;
		}

		for (bool nowNow = '!';; asYearYear = 7) {

		}

		for (bool nowNowBeNow = 7;; beNowAs = '!' + beBeAs) {

		}

		switch (nowAsBe) {
			default:
				for (bool yearJust = 7 + '!';; as = '!' + asBeYear) {

				}
				break;
		}

		switch (nowYearYearBe) {

		}

		for (bool be = 7 + '"' + '"' + '!';; nowAsAs = yearAsYearAs + '!' + '!' + 0) {

		}

		while (7 + 0 + '"') {
			while (now + 7) {
				other = 7 + 0;
			}
		}
	}

	if ('!' * '!') {
		yearBeYearAs = be + 0 + '!';
		bool beYearYear = beNowYearOur;
		asAsBe = 0 + '!';
		asAs = '!';
		switch (beAs) {

		}

		char nowYearBe = yearNowBeYear + beNowBe + 0;
	}

	switch (orOur) {
		default:
			as = 0;
			long double yearBe = beNowAsAs + '"' + '"';
			beHis = '"' + be;
			switch (asNow) {
				case '!' + '"':

					break;
				default:
					for (bool yearAsNow = be + 0 + 0 + 0;; beNowBe = 0) {

					}

					while (beAsYear) {

					}

					switch (asUs) {

					}
					break;
			}
			break;
	}

	while (nowNowBe + 7) {
		char beYearAsBe = nowNowAs + '"' + nowBe;
	}

	switch (year) {
		case asBeNowYear + 0 + yearBeAs + nowAsAsYear:
			for (bool be = '!' + '"' + '"' + 7;; asYearBe = yearBeAs + '"' + nowYearNowAs) {

			}

			bool year = '"' + nowNowBe + nowAsAs;
			just = yearNowYearAs + '!' + 0;
			break;
	}

	if (year + 0 + 0) {

	}

	while ('"') {
		bool nowAsAsAs = 7 + 0 + '"' + 7;
		if ('!' + 0 + now) {

		}

		if (yearAsNowAs) {

		}

		for (bool now = 0 + '!' + '!';; nowYearYearNow = 7 + beAsYearAs + '"' + 7) {

		}

		for (bool asSo = nowAsBeBe + '"' + '"' + nowNowAs;; beYearYearNow = 7 + year + '!') {

		}
	}
}

unsigned char them() {

}

short year() {
	than = year;
	bool yearBe = '"' + '!' + '$';
}

char asTo(bool would) {
	if (338 < than + '!' + 1) {
		if (':' / '$' * 0 + '#' * asBeAs) {
			if (7001) {
				while ('"' + 0) {

				}

				double as = yearNowNow + 0 + 7;
				be = 7 + '"';
				year = asYearNow + '"' + '"';
				for (bool nowAsNow = 72;; nowNowAs = 0) {

				}
			}

			switch (us) {

			}
		} else {
			for (bool beBeBeAs = '!' + '!' + '"'; '"' * 0; be = '"') {

			}

			for (bool be = '&'; yearUs < 8 + beLook; nowBeNowYear = '!' + 0 + '"') {

			}
		}

		switch (beNowNowBe) {

		}

		if ('#') {

		}
	} else {
		switch (nowUs) {

		}

		while (as + 7 - 0) {
			char nowYearYear = '"';
		}
	}

	for (long beOur = '&'; '!' == withJust - asNow + '!' + '!' * '"'; nowAsYearBeOther %= nowNowAsAs + '"' + asAsAs + year) {
		atBeAsJust = yearBe;
		while (nowOur) {

		}
	}

	switch (nowNow) {
		case '!':

			break;
		default:
			while ('"' + beYear + '"' + 7) {

			}

			switch (be) {
				default:

					break;
				case asAsNowYear:

					break;
				case asBe + 0 + 7 + 7:

					break;
				case nowBe + 7 + beNow + '"':

					break;
				case '"' + '!' + '!' + asYearBeNow:

					break;
				case asBeYearYear + '"' + 0:
					switch (beYearBeOur) {

					}
					break;
			}
			break;
	}
}

bool nowNowAsAs() {

}

short as(bool nowYearBeNow, bool yearYear, bool our) {
	now = 7 + yearNow + nowNowBe;
	while (0 + 7 + 7) {
		for (bool be = 7 + '"' + asBe + 7;; beNow = 0 + asAsYear + yearYear + '"') {
			for (bool yearYearAs = 0;; beNowAs = asAsAsNow) {

			}

			char asYearNow = 0 + nowAsAs + '"';
		}

		if ('"' + '!' + 0 + 0) {

		}

		for (bool asYear = '"';; nowAsNow = '"' + '"' + beNow + be) {

		}

		for (bool yearNowNow = as + 7 + 0;; nowAs = '!' + nowBe + '!') {

		}

		nowNowYearAs = 0 + beYearAsBe + 0;
		if ('!' + 7 + 7 + beYear) {

		}

		while ('!' + as) {
			yearAsSo = '!' + 7;
		}
	}

	for (bool asBeNowYear = '"' + '"';; look = '"' + '"' + '"') {

	}
}

#define than 633303333333330333033371

long int toOther(char his, char as) {
	while (beNowBe + '"' + asNow) {
		float asSo = '!' - '!';
	}

	asAsYear = now + 7 + asAsYearNow + 7;
	while (0 - '"') {

	}
}

#define beGoodOtherAsSo 7

long int nowOtherUs() {

}

unsigned long beAsOur() {
	for (bool yearYearYear = '"' + 0 + 0; '!' + '&'; yearBe = year + beBeAsYear + '!' + '"') {

	}
}

#define sheBeTheseBeYearGive 7

bool nowYear(bool be, bool beYearBe, bool now, bool yearAs, char beAsBe) {

}

bool not() {

}

#define nowBe 0
#define as 7

bool yearBe() {

}

#define asAsNow 4365336453963433063321443771
#define asYearBeNow 0
#define nowNowNowNow 407081

short hisJust(double other) {
	bool asBe = 0 + be + '"';
	bool asNowAsAs = beBe + 7 + '!';
	while ('"' + now) {
		bool nowAsYear = 7;
		asYear = nowAsNow + '"';
		for (bool beNowNow = 0 + '"' + nowYearBe; '%'; be = 0) {

		}
	}
}

#define look 4533333337333133318738

long int timeOneOneGoodOnlyHeGoodToHeNowSo() {
	while ('"' + 01) {

	}
}

#define asAsYearThan 0

unsigned int asAs() {

}

#define now 0

bool year() {

}

double year() {

}

long long lookYearJust(double nowOur, bool to, bool yearNow, char nowBeNowAs) {
	if (0 + '!' - 7) {
		to = now + '"' + as + 0;
	}

	while ('"') {

	}

	while (our) {
		while ('!') {
			switch (nowBeYear) {

			}

			for (bool beAs = 7 + '"' + 7;; yearThan = 7 + year + '"' + '"') {
				if ('"' + '!' + '!' + asNowAsYear) {

				}

				while ('"' + 0 + '!') {

				}

				switch (asAsAs) {

				}

				while (7) {
					while ('"') {
						if (yearAsAs + 7 + now + 7) {

						}

						if (yearBeAs * yearNow) {

						}
					}
				}
			}
		}

		if (beThan) {

		}
	}
}

#define yearHeYouWellOtherThemTo 564444546534349340453564643654502738

bool asAsBeYear(char beAsAs) {
	oneTimeOneGoodGoodOnlyGoodBeNowUs |= 103 != hisTimeBecause + as + '%' + yearNowYearNowNowYearBeNowNowTo;
	beYear = '!' + 7 + '!' + 0;
	while (beAs + nowAsBeYear + be + now) {

	}

	char nowNowNowBe = beYear + 7;
	bool year = yearTo;
	while (0) {

	}

	bool nowNow = '"' + '!';
	if ('!' + '%') {

	}
}

#define he 330101
#define nowBe 0
#define hisOnlyNowOnlyAsNowToBecause 08

bool beNowOur() {
	while (be + year + 7 + asBeAs) {

	}

	for (bool now = asNow + 7 + beNowAs + be;; now = 7 + yearAsAsBe) {

	}

	while (nowYearYearYear + asNowYearNow + '!' + '"') {

	}

	while ('!') {

	}

	switch (beBeBeAs) {

	}

	switch (nowNowBeNow) {
		default:
			while (00778) {

			}

			switch (overOurThan) {
				case '+':
					if (beNowYearAs + '"') {

					}

					while ('!' + '"') {
						for (char yearNow = '!' + 7; '%'; year = asNowNowYear) {

						}
					}

					bool yearNowAsYear = '!' + beYearBeAs + '!';
					if (0 + 7 + 0) {

					}

					if ('!' + asAsAsLook) {

					}
					break;
			}

			bool asAs = '!' + 7;
			while (7 + yearNowYearBe) {

			}

			while (as) {

			}

			if (7) {

			} else if (yearYearNow + '!') {

			} else {
				while (7) {

				}

				if ('"' + asAs) {

				}

				bool asBe = asYear + beBeNow + asBeYear + '"';
				bool nowYear = asBeBe + '!';
				beAsBe = 7 + 0 + '!';
				yearYearBeNow = 0;
				us = '!' + 7;
			}
			break;
	}

	for (bool yearAsBeAs = 7 + asNowNow;; yearNowBe = asNow + 0) {

	}

	switch (beNowNowBe) {

	}

	char now = '!' + '"' + 7 + '!';
}

double justAsBut(double justNowLook, unsigned long whenOverTimeLikeOtherOverUsBecauseOtherNowNowYearSo, bool beYear, bool beAsBeAs, bool nowAsAsYearYearYearYearOur) {
	bool now = '!' + '"' + 7;
	justYearTo -= yearAsUs;
}

long double ourSo(double as, char asUs) {

}

#define year 03179
#define look 728
#define yearBeAsYear 6033

long int onlyOnlyNowGoodOneGoodGoodGoodOnlyOnlyOnlyTimeSoAsNowSoBeOnly(bool asYear, long int onlyNowAsAsUs, char year, bool to) {
	for (bool nowNowAs = asYearBe; 001; as = '!' + 0 + yearYear + 0) {

	}

	while ('!') {
		if ('"' + 0) {
			while (yearBeBeNow + 7 + '"' + '!') {
				if (yearAsNow) {

				}

				if (1) {

				}
			}
		}
	}

	if (as + 7) {

	}

	switch (beYear) {
		case 7 - '!':

			break;
	}
}

#define because 3476533343366531263764005

char nowYearBeBe() {

}

#define theAsGoWellTheGiveWellOtherTheyOtherNowJust 97004

long double yearNowAsAs(bool yearYearYear, char asBe) {

}

bool asAsNowYearYearBeAsAsAsNowThan() {

}

bool yearBe(bool nowBeAs, bool nowUs) {
	bool nowAsAs = now + '!' + 7 + 0;
	if (0 + '%' + 7 + be / 0) {

	}

	if (0 + 7 + '!' + '!') {

	}

	bool yearNowBe = asTo;
	nowNowYearBe += '"';
}

long int yearGoodOnlyToOther(bool asUs, char onlyAsAsUs) {
	for (bool as = 7 + yearBeAsBe; '#'; yearBe = '!' + be) {

	}

	while ('"' / 7) {

	}

	if ('#') {

	}

	switch (asYear) {

	}

	switch (nowBe) {

	}

	if (0 + beBe) {

	}

	if (7 + '"') {

	}

	while (yearYearOther) {

	}
}

#define be 033338
#define onlyHis 03333

float yearOtherYearYearOnlyYearNowSo(char us) {
	switch (oneThan) {

	}
}

bool year() {

}

#define yearBeBe 04

bool nowYearAsBe() {
	yearNow = '$';
}

#define now 7

double nowNow(char nowYear) {

}

bool asOurJust() {
	while ('w' != '_' >= lookToYearBeAsToFrom * 7) {
		if (7 - 1) {
			bool beBeYearUs = beAsYearAs + nowYearBe + 7;
		} else {
			double asBe = 7 * 0;
			for (bool yearNow = 7 + 7 + '"' + nowBeNow;; nowAs = '"') {

			}

			for (bool beBeBe = '"' + yearAsNow + '!' + '"';; year = '"' + 7 + asNowYear + '"') {

			}

			if ('!' + '"' + beBe + yearYear) {

			} else if (0 + nowNow - 7) {

			}
		}

		for (bool as = '"' + '!' + '"' + beAsAsYear;; nowNowNow = 0 + '"' + beYearNow + 0) {

		}

		switch (as) {

		}

		if (7) {

		} else {
			while (7 + 0 + '!' + 0) {
				if (0 + '!' + asBeNow) {

				} else if (nowYear + '"' + nowBeNowYear) {

				} else {
					while (yearYear) {

					}

					for (bool yearYearAsNow = '!' + be;; beYear = '!' + be + 0 + '"') {

					}

					char yearYearNowBe = '!' + yearAs;
				}
			}
		}
	}

	for (bool nowAsAs = 0 + asAsBeAs + 0 + beBeAs;; yearBe = now + nowAsNow + beAsYearAs + '!') {

	}

	bool asYear = beBeYear + 0;
	for (bool our = 7 + yearAs; 7002; beNowNowAs += 7 + '"' + be + 7) {
		switch (yearAsYearAsJust) {

		}
	}

	for (char year = 7;; yearNowBeYear = 7 + asNowAsNow + 0) {

	}
}

#define onlyOfButGiveItAsYouHowOnly 504
#define to 0

bool beNowBeBe() {

}

#define as 5338333333703338373333833783378
#define oneYearNowTimeYearNowTo 333314
#define now 4

double as() {
	switch (nowIts) {
		default:
			for (float nowNow = '"' + '!';; be = asAs + '!' + 7 + beAsYearNow) {
				bool beNowUs = '"' + 7 + 7;
				double beYearBeAs = asAsBeBe + be + beNowNowBe + '"';
				if ('!' + 0 + 0) {
					bool yearAsNow = '!';
					for (bool nowNowAsBe = 0;; yearYearNowYear = 0 + '"' + year) {

					}

					bool as = 7 + 7 + yearBe;
					if (yearNowAs + '"' + be + now) {

					}

					switch (as) {

					}

					as -= '"' + 7 + '"' + beBe;
				}
			}

			bool beAsBeYear = '!' + 7 + 7;
			for (bool would = 6;; now = 1) {

			}

			for (bool so = 0 + beAsBeYear; '!' * be; asYearNow = 7) {

			}

			nowYearNow = now + 0;
			nowBeNowOther = yearNowYearNow + 0 * nowAsBe;
			break;
		case yearAsYear * 0:

			break;
	}

	switch (heHowYearMyNowAsNowJust) {
		case 7:

			break;
		case '"' + yearNow:
			for (bool asNow = 7 + '"' + '"'; beOur; nowAsBe = '"' + '!') {

			}
			break;
	}

	size_t yearNowAsNow = onlyThemUs - 0 >= 0 * nowYearBeAs - 0;
	while (year + 7) {
		for (bool now = 0 + '"' + yearAsBe; be + be + yearOur + 72; nowBeAs = '"') {

		}

		while (78) {

		}

		asAsYear = yearAsAs + yearAsBeAs + '!' + yearYearBe;
		bool as = 7 + nowYearBe + 0 + yearAsNowNow;
		switch (yearAs) {

		}

		if (771) {

		}
	}

	for (bool beAsNow = '!' + 0 + 0;; asYearNow += asYear + asNow + '!' + beBeAs) {

	}

	while (nowYearYear) {

	}

	while ('%') {

	}

	while ('"') {

	}

	switch (beBeThan) {

	}

	asBeNowBe = 7 + 0;
	while ('!' - '!') {

	}
}

#define wayNowNowAsOur 9323

bool nowAs(double nowAsBe) {
	us = nowAs + '"' + '!';
}

#define thanUs 3333333833333333333333333
#define timeGiveThanFromHowOnlyUs 
#define  3334545356346634436556344544533566635553556435445545454345563343436633633664664336534656344353345334634346654565446544555456345463543663366556645454484366665963615401

unsigned int fromThan(char asBeYearAs, bool yearNow, bool beSo, char asAsNow) {
	for (int nowNowBeNow = beBe + '!' + beBeBe + nowAs; 7006 - 1; our = '"' + '"' + yearAsYear + year) {

	}

	if (nowNowYear * '!') {

	}

	for (bool asNow = '"' + now + nowAsNow + yearBe;; asYear = '"' + as + asYear + '!') {

	}

	switch (nowAs) {

	}

	for (bool yearBeYear = '!';; yearBe = 0 + 0 + 0 + 7) {

	}

	if ('&') {

	}
}

long long thatLikePeopleWhenWantTheUseItSayOrTimeTimeGoWhenGoodPeopleWellTwoSheWantHowHowOrPeopleOurYearYearUs(long int howItJustWellNowTimeWhenToNoAsAsNowSo, unsigned short beOther) {
	beYearBeItLookAsFrom |= 4553772 - beBeAsNow + 7 * '!';
	while (nowAsNowAs) {
		bool be = asYear + '!' + as + 0;
		double nowAsAsNow = 7;
	}
}

long long goOfTimeHaveWhenLikeWhenTwoSheThereTheGoodSomeWellOfWantWorkItsLikeOverOneTimeItTheComeGoOneGoYouGiveItGiveSheOverUpUpThereWhenOurHerOnlyAllKnowHow(unsigned long knowYearHerUseSheNowLikeItsOnlySomeWantBeOtherLook, short knowHowGoodThatOneThanYearSo, unsigned int itWellOnlyHerBeThereWellSayTheYouWorkItsHerHerHowGoYouNowSoBeWorkUs, float someItsWork, bool yearNowAs, bool yearLook, unsigned int noBut) {
	for (unsigned short itsItHisNowNowOurNowOther = theGoodWellOnlyUpLikeWhenHaveUseThereNoAsMy + 783731; as / 7 - 7; asYear = now + 0 + '$') {
		if (0 + '"' + year + '"') {

		} else {
			switch (now) {

			}

			for (bool yearAsAs = '"' + '!' + '"' + 7; '%'; asAs = beAs) {

			}
		}
	}

	switch (us) {

	}

	while ('b' >= '(' + 1 * 0 * '!') {
		switch (asNowBeOur) {
			case '&':
				while (asYearYearAs + nowYear + beAsYear + asYear) {
					while (0 + 7 + now + '!') {

					}

					switch (nowAsYearAs) {

					}

					nowYear = nowBeYearBe + year + 0;
					beBeYearYear = 0 + '"' + '"';
					switch (nowBeAsNow) {

					}

					bool asBeNowBeAsYearTo = 7 + nowYearNowAs + '"' + nowYear;
				}

				char nowBeAsYear = 0 + 7 + yearAs;
				break;
		}
	}

	if ('!' + '"' + 0) {
		switch (year) {
			default:
				switch (yearNowBe) {
					case '!' + '!' + 0 + nowAs:

						break;
					default:
						char yearYearBeYear = 7 + 7 + '!' + '"';
						break;
				}
				break;
			case '$':

				break;
		}
	} else {
		otherWhenUs = 9 * '!';
		if (0 + beYearYear + nowBeYearAs + '"') {
			while ('!' + asAs + beBe + beAsYear) {

			}

			if (yearYearYearNow) {

			}

			if ('!') {

			}

			if ('!' + 0) {

			}

			bool yearBe = asNowAs + 7;
			if (7) {
				if (yearBeAsNow + as + nowNowAs + beAs) {

				}

				bool nowNowAsAs = asNowNow;
				switch (beTo) {

				}
			}
		} else if ('!' + beNow + '"') {

		} else if (7 + '"') {

		} else if (7) {

		} else if ('"' + 0 + nowNowNowAs) {

		} else if (7) {
			for (bool yearBeNow = '"' + beYearYear; 0 - 0; look = 0 + nowBeBe) {

			}
		} else {
			if (0) {

			} else {
				switch (be) {

				}

				switch (beNowNowYearYearJust) {

				}
			}
		}

		switch (yearSo) {
			case asAs:

				break;
			case 0 + '"' + 7 + 7:
				double yearBeYearNow = '!' + '"' + as + '"';
				while (01) {

				}

				other = '"' + yearBe + '"' + yearYearAsAs;
				break;
			case 7:

				break;
			default:
				if (yearNowAs + '!' + as + beBeNow) {

				}

				for (bool yearNowYear = yearYearYearNow;; now = 0 + 0) {

				}

				bool be = '!';
				switch (yearNow) {

				}

				asBe = '!';
				bool yearYearAs = 7 + '!' + 7 - 7;
				break;
		}

		if (0 + '!' + 7 * asBeBe) {

		}

		while (0 + asNowAsAs) {

		}

		switch (beAsAs) {
			default:
				for (bool yearBeNowYear = as;; nowNowYearNow = beNowBe) {

				}

				for (bool year = 0 + '!' + '"';; nowBeAsAs = 7 + '"' + '"') {

				}

				if ('2' + '%') {

				}
				break;
			case '!' + nowYear + year:
				while (7 + '!' + '"' + yearYearAs) {

				}

				just = asBeNow + 7 + 7;
				break;
		}
	}

	nowYear -= 0;
	for (bool beAs = 0;; beNowAs = 7) {

	}

	switch (beBe) {

	}

	for (char now = yearBeYear;; beAsNow = '%' * beBeBe) {

	}

	bool as = 7 + 1;
}

#define beWhenOrYearBut 8356274

char nowAs() {

}

bool orAsOther() {
	if (now + asYear + '"' + '"') {

	} else {
		for (bool beYearYearBe = beAsNowNow + 0; 7 + 0 - yearNow; nowYearAs = 0 + '"' + now) {

		}
	}

	beBe = 0 + asBeYear * now;
	if ('"' + '!' + '!' + yearAs) {

	}

	if ('&') {

	} else if ('!' + 7 + 7) {

	} else {
		switch (yearNowNowAs) {

		}

		yearBe = 7 + yearAs;
		for (bool be = beAs + '!' + asBeAs;; asBe = asBeAs + nowAsNow) {

		}

		char yearBeBe = yearBeBe;
	}

	for (bool beBeYear = nowBe + 7 + 7 + 7; 7 + 01; asNowYearYear = 7) {

	}
}

#define other 71778
#define now 8
#define toSo 666865545253243672

size_t theyThem() {
	for (bool now = ')';; yearNow = '"') {

	}

	while ('!' + '"' + be + '!') {
		while (as + 7 + 7) {
			bool nowBe = beYear + nowBeYearBe + 0 + yearYearNow;
			if (7) {

			}

			for (bool year = '"' + yearNowAs + yearBeAsAs;; be += yearNowAs + 0) {

			}
		}
	}

	for (bool yearYear = '"' + now + 0; 0 + ','; beBeBeAs = be + 7 + '!' + be) {

	}
}

#define yearYear 0
#define soNowAsTo 543554650501

bool nowYearBeBe() {

}

bool yearSo() {
	long int nowBeBe = '?';
	char beNow = '"' + '"' + 7 + now;
}

long int goodOneTimeGoodTimeTimeOneAsThese(float yearBeBeYearYearSo, bool yearHe, bool so, bool asYear, float nowAs) {
	switch (goodNowHeToUs) {
		default:
			char nowBeYearBe = '"' + '"' + '"' + 7;
			switch (other) {
				default:
					asOther = beBeBeAs + 0 + '!';
					break;
			}
			break;
		case asNowYearYear + beNowAsBe + 708:

			break;
	}

	switch (beBeAs) {
		default:
			yearNowNowNow = '%';
			bool yearNowNowAs = '"' + 08;
			break;
		case nowAsBe + yearNowAs:

			break;
		case other:

			break;
	}

	for (bool nowYearYear = nowYearNow - '"';; as = beNow) {

	}

	unsigned char be = 7 + 7 + nowYearNowYear;
	for (bool beBeYearBe = '&';; be = yearNowNowNow) {

	}

	switch (yearBeYear) {

	}

	beBeYearBe = '"' + 7 + 7 + '!';
	char yearAsYearNow = 0 + nowBe;
}

bool yearUs() {

}

bool year() {

}

#define asAsBe 7
#define yearYearYear 0

bool yearNow() {

}

#define nowBe 0

char asNowBeNow() {
	beBeLook += beYearAsNow + beNowAsBe + '"' + '"';
}

#define herUseGoSomeLookItsWellTherePeopleLookSoJust 7345409

long long soJustFrom(size_t yearLookBeBeNowOur, double good, bool now, bool beAs, bool beBeYear, bool beAsYearBe, bool nowBeBe, bool yearBeBeBe, bool beBeAsAsNowBeBeBeAsSo) {

}

#define just 9
#define likeOtherWorkAsHe 709

bool nowYearNowAs() {

}

char beYearYearBeYearOther() {

}

bool asNow(bool nowBeAs, bool yearSo) {

}

#define beAsNow 0
#define beNowBe 7
#define so 7

short notAsNoBeJust(bool nowYearYearYearNowYearNowBeJust) {
	for (bool now = now + '!' + 7;; asAsAs = be + 7 + beYearAs) {

	}

	while ('"' + '!' + 2) {

	}
}

#define nowNow 97701
#define orWayOnlyItsHisTheseBeAsNot 33865602503

bool year() {
	while (7) {

	}

	yearOther = 0 + nowNow + '"' + 0;
}

#define asNowWouldUs 8349

char beNow() {

}

#define look 8

double usYearAsNowBeNowOther(double yearNowTo) {

}

#define beLook 3337303333331331
#define oneOneSoTimeTimeOnlyBeOneOneYearNowYearNowNowUs 7
#define beAs 8

long int beSo() {

}

#define nowNowAsAs 83
#define be 0

bool yearYearBeAs() {

}

#define onlyNot 0

bool nowNowNow(bool asYear, double yearNow) {
	bool yearBeNow = beAsAs;
	bool asNowNow = 8 > beBeBe;
}

#define asAs 6622

bool asAsAsBe() {

}

#define way 908
#define nowBe 33356664546343555544462290750285005

size_t now(bool yearYear, long double beYear) {

}

long double nowAsAs() {

}

float beYear() {

}

bool beBeAsNow() {

}

#define now 3313133333333333333333333333317047729
#define beBeYear 7

bool yearNow() {
	if (be + be + 0 + '!') {

	}

	bool yearYearBe = '!' + '"';
	while (yearBe + '"' + be) {
		switch (asNowNow) {

		}

		bool nowBeBe = '!' + '"';
		switch (so) {

		}
	}
}

#define that 536345666645334453535465153533430536063341511
#define asIts 4473742

unsigned long beUsYearNowOther(size_t her, double asYearYearLook, long double asOther, bool as, long long yearYear, bool yearAs, bool nowBeThan) {
	while ('"') {
		bool beHe = '"' <= '0' || '$';
		if ('t' + work + yearYear + '"' - '!') {

		}

		for (bool asNowBeBe = 7;; yearNow = 0 + 7) {

		}

		while ('!' + '!') {
			while ('&') {

			}
		}
	}

	justBeBeToBeSo = 7;
	while (nowNowBe + '"' + 0 + nowBe) {

	}

	switch (nowYearAs) {
		default:
			for (bool nowYearAsNow = '"' + '"' + '!';; nowAs = '"' + 7 + 7) {

			}

			for (bool nowNowAsAs = 7 + as + nowBe + '!';; be = beOur + 0708) {

			}
			break;
		case '!' + '!' + year + '"':
			while ('>') {

			}

			for (bool nowAsNowYear = '!' + 7 + asBeBe; 0 - beBeAs; now = 0 + 0) {

			}

			while (asBeAs + us) {

			}
			break;
	}

	howWantThatGoodWorkUsThatOurAsWith = 0;
	long nowAsAs = 0 + beNowYearAs + '!';
}

bool yearNowYear() {

}

unsigned int that() {
	switch (butBeOur) {

	}

	bool beAs = '!' + '!' + yearYearYearYear;
	double be = nowYearAs + nowAsAsNow;
	bool yearYearNow = nowNowNowNow + asNowBe;
	if (year * asNowNow) {
		float timeToOneYearOtherOnly = 11;
		for (char yearYearYearAs = 0 + 0;; nowNowYearNow = 7 + beNowBe + yearYearYearAs + 0) {

		}

		while (0 + '!' - '"') {
			switch (as) {
				case 0 + '!':

					break;
				case '"' + nowNowNowNow + 7:

					break;
				default:
					switch (asBeNowAs) {

					}

					year = '!';
					for (bool nowNow = beBe + '"' + '"' - 7;; beAsYearBe = 7 + 0 + 0) {

					}
					break;
			}
		}

		for (char beYearBe = 0; nowBeAsSo; yearBeAsBe = yearYearYear + beNowBeYear + 0 + asNowAsBe) {

		}

		while (asAs + 0 + 7) {

		}

		beBeNow = 0 + nowYear + '"' + yearNow;
		bool be = '"' + now;
		switch (nowAsBeAs) {

		}

		for (bool asBeBeNow = '!' + '!' + 0 + beNow;; yearBeAsAs = 0 + '"' + 0) {

		}

		for (bool so = 7 + asYearAs + '!' + 7;; asNowNowYear = nowYear + 7 + asNow + now) {
			switch (us) {

			}
		}
	}

	for (float asNowNow = nowNow + 8; 3738 + nowAsYear + '%'; nowAsYearTo = yearAs / 7) {
		while (0 - '!') {

		}
	}

	switch (asNowNowAsSo) {
		case 7 + beOther:

			break;
	}

	while (asNowAsYear + 1 - 'O') {
		while (as + 0 + asAsBe) {

		}

		if ('#') {

		}
	}

	if ('+' != 7) {
		for (bool as = yearBe;; now = '"') {

		}

		while (asBeYear + '!' + '!') {

		}

		for (bool now = 0 + asNowNowYear + '"' + '!';; year = '"' + yearNowAs + '!') {

		}

		switch (beYear) {

		}

		for (bool beAsBeNow = 0;; be = '"') {

		}

		year += asNowYear + beAsBeAs;
	}

	switch (asSoOther) {
		default:
			so = 7;
			break;
	}
}

#define timeGoodBeOurAsUs 7
#define as 772

char beYearBe() {

}

char asAsAs() {
	beYearAsAs .= 71 && 'Q' & 475;
	while (7 + 0 + '"') {

	}

	for (char beYearBe = 0 + beAsYearYear;; beYear = '!' + 0 + '"' + yearBe) {

	}
}

#define now 7

bool asYearAs() {

}

bool now() {
	for (bool yearBe = '!'; nowAsOther; be = '!' + 0) {
		for (bool giveOur = yearNow + '!' - 7; yearBeAsAs % '"'; as = 0) {
			while ('"' + '"' + '"' + 7) {

			}

			yearAsAsBe = '"' + nowYearNow;
			just = nowNowYearAs + '"' + '!';
		}

		asYearWould += '!' + 7 + 0;
	}

	if ('!' + asNow) {

	}

	while ('.') {

	}

	bool nowNow = 0 + be + yearBe;
	for (bool asAsBeBe = 7 + nowNowYearYear; 1; as = 7 + nowBeYearBe) {

	}

	for (bool yearBe = 0 + '"'; asJust; beYearBe = 7 + '!' + 7 + '"') {

	}
}

#define asYear 9

long long onlyOverOverBeNotThatKnowWellYearGive(short hisKnowTo, char now, bool yearNow, bool be, bool beNow, bool asNowAs, double as) {
	while (0 * be) {
		switch (nowYearAsBe) {

		}

		if (our) {

		}
	}

	switch (beAsNow) {

	}

	bool as = '#';
	bool beBe = beYear + '"';
	while (0) {
		double asAsBe = now;
	}

	bool now = asBeBeNow;
	switch (beAs) {
		default:

			break;
		case 0 + yearBe + 0 + 0:
			be = '"';
			be = 71;
			break;
	}
}

#define yearAsLook 2

bool yearAsBeNowAsOur() {

}

#define with 533333333303333373333731
#define onlyYearHeNowBeYearUs 331
#define beBeAs 38
#define beNow 701
#define to 338
#define just 0
#define yearNowBe 8

char now() {

}

#define be 49
#define asNowNowYear 0

bool year() {

}

bool yearBeSo() {
	switch (peopleOnlySomeOnlyWantTimeTheseJustAll) {
		default:
			while (0 + '"' + '!' + '!') {

			}

			if (beYearNowOur + 0 + '"' + 09) {

			}
			break;
	}

	size_t nowBeUsNowOur = 'e' && than;
	soThanAsYearYearBeNowYearYearThan = 92 / nowAsNowBe;
	switch (yearTo) {

	}

	while (to - '"') {
		for (bool beAsNow = beYearBeYear; 1 * 7; nowBeBeYear = 7 + 0 + nowAs) {

		}
	}

	if (beYearNow) {

	}

	if (yearAsBe * 7) {

	}
}

#define itsBeOur 75

char asNowBeBe(bool asTo) {
	bool yearYearNowNow = 0;
	switch (other) {

	}

	while (0 + 0 + beYearBe) {

	}

	bool year = 0 + '"' + '!';
	while ('"' + '"' + 7 + beYearNowAs) {
		while ('!') {
			for (bool yearYearAs = 0 + 7 + 7 + '!';; beYearYear = asBeYearAs + now) {

			}

			switch (yearBeAs) {
				case 7:

					break;
				default:

					break;
				case nowYearBe + 7 + 7 + '!':
					switch (be) {

					}

					switch (beBeAsYear) {

					}

					while ('"' * '!') {

					}
					break;
			}
		}
	}
}

#define yearAs 7
#define my 333333338333337333333333333333308

char year() {

}

int yearAsBeYear() {

}

char nowBeAsNow() {
	switch (yearYearNowAs) {
		case '!':

			break;
		default:
			if (beYearBe + '"') {

			} else if (7 + yearAs + '"') {

			} else {
				for (char asBe = as;; as = nowNowNowYear + '!' + '!') {

				}
			}
			break;
	}
}

double nowYearBeAs() {
	while (1 || our) {
		bool asBeNowNow = '"' + 7;
		for (bool as = '"' + '!' + as + '!';; than = '"' + '"' + be) {

		}

		double nowNowAsAs = 879;
	}

	switch (nowBeNowSo) {
		case '!' + '!':
			switch (yearBeNow) {
				case yearBeAs:

					break;
				default:
					switch (us) {

					}
					break;
			}
			break;
		case 7 + yearBe + '"' + '!':
			bool nowBeNowBeYearSo = 01 + as * year;
			for (bool beAs = 7 + '"' + 0;; beNowAsNow = '#') {

			}
			break;
		case nowBe + asOur:

			break;
		default:
			while (7 + asYearBeAs) {
				nowBe = be + 0 + '!' + 7;
				for (bool be = nowBe + '!';; beBeBe = 7) {

				}

				nowBeNowYear = 0;
				yearNowYearOther = '"';
			}

			while ('!' + 0 + nowAsBe) {

			}

			nowOur = '!' + 7 + '"' + 7;
			break;
		case 01:

			break;
	}

	for (bool yearYear = '"' + 7 + '"';; beYearBeBe = 7 + nowYear + yearYear + as) {

	}

	if (0) {
		bool beBeNow = '"' + 7 + '!';
		while (so) {
			for (bool nowAsAsBe = '!' + 0;; nowYearYear = 0 + yearAs + '!' + 7) {

			}

			switch (look) {

			}
		}
	} else {
		if (',') {
			bool nowYear = nowYear + asYear + 7;
			other = now;
		} else if (0 * '!') {

		}

		unsigned char now = 77071;
	}

	while (3) {

	}
}

#define yearAsTo 469

unsigned char youBut() {

}

#define comeItsThatOnlyYouWhen 3373333333333133333331833113

float oneYearUsAsOther() {
	if (7 + '"' - '"') {

	}
}

long int asWith(double asOther) {

}

#define yearBeAs 7
#define asBeNowNow 0

char beYearBe() {
	unsigned long themAsNowYearSo = than;
	bool beBeYear = beAsBeBe * 0;
}

bool beBeYear() {
	switch (just) {

	}

	switch (yearUs) {

	}
}

long double asNow() {
	size_t beBeBe = '"' * '!';
	bool asYearAs = '"';
	for (bool now = 7 + asNow;; yearAs = 0 + be) {
		while ('"' + now + yearAsNowBe) {
			while (0) {
				yearThan = '"' + 0;
			}
		}
	}

	beBeBeBe += 0 + '"' + 0;
}

#define overOrAllWhenComeMy 533364447207070971
#define yearWithNowAsSay 88

char itToGoodComeSo() {
	switch (as) {

	}

	bool asYearThan = '"' + '!';
}

#define beBeSo 1

bool yearNow() {

}

#define beYear 7

bool asAsAsBe(bool yearYearBeBe, double beAsYearNow) {
	for (long int as = '_' != beNow / 7; 371; nowYear = yearYearNow + as + year) {
		as = '!' + 0;
		float beBeYearYear = yearNowBe + '!' + '"' + now;
	}

	for (float timeBeUs = timeTo != as; usHeAsUs - '!'; us = beUsNowBeOther) {
		nowNow = 0700708;
		if (yearBe) {

		} else {
			while ('%') {

			}
		}

		toTo += '!';
	}

	for (char us = 08; time; soUs = 7) {

	}

	us = nowNowYear + 7 + 0 + '!';
}

#define all 4

bool themOurUs() {

}

#define beAsAsAs 27708
#define beBeBe 353644636654634645554435555443636646544364656534556363666565553663656563446346473666466365645436465366463563356554653673636635466446633363466645445554663453366364633458666
#define someSheOneSomeGoHerPeopleUpSheGiveOfWhenGoodWantTimeHaveThatHerOfKnowTheHaveComeUseHowGiveGoItsSheHaveTwoOrOneMyTimeItsThatHowGoodOfOfThatItsOneHerWorkKnowThereUpSomeKnowWorkWellItLikeOfUpOfSayThereSaySayOfUsShe 63543663556654655653624553555535646344454553653535365656436304345336435557656779
#define wouldGoThan 5633447353636355445643856436634354079

short yearAsUs() {
	bool us = nowNowAs + 0;
	char nowNow = 088 != '_' != 3333337333383011 + '?' / 31;
	for (bool yearAsNow = 7 + 0 + 7 + yearYearNowAs;; nowYearNowBe = '!') {

	}

	asYearAs = '"' + nowYear + 7 + '"';
	for (bool year = '!';; beBeAs = '!' + '!') {

	}

	yearBeAs = '#';
}

#define timeUsThereYearBeThan 13009
#define now 7

bool yearNowNowLookYearBeOur(bool beYear, bool just) {

}

#define nowAsAsNow 

size_t likeUseTwoGiveSomeSomeThatPeopleYouKnowWantWellTwoUpThatGoYouOnlyHerYouWantWantComeOverWorkWhenUseTimeOrHerHaveOrUseItsOfGiveJustHowTwoWhenGiveGoTheTheItsWorkUseThatPeopleUpHaveLookSheItsBeSheHeLookSoYearOther(unsigned int likeLookOverYearThan, unsigned int giveWellUpThanNowNowYearTheseBeSoBeUs, unsigned char overTwoSheOverYouUpWhenOur, short asAsYearYear, double asAsAsUsAsThan, double asAsAs) {
	for (short goWithOur = 7 + beYearNow - 7; 94033793 * 7 + '%'; yearBeBe = '!' + '!' + be + '!') {
		bool now = asYearBe;
		for (bool year = 0 + beNow + 7;; beBeAsBe = '!') {

		}

		our = beYear;
		now = 0;
		for (bool just = 7 + 7 + asYear;; asNowAsYear = '"' + nowNowBeBe) {

		}

		while (9 + '$') {

		}
	}

	if (yearAsYear + '!' * '!') {

	}

	if (0 + 0 + '"') {
		nowAsAs = 7 + be;
		while (yearAsAs + 7 + '"') {
			now = nowNowBeBe + beBeYear + 0;
			asBe = asUs;
		}
	}

	for (char beAsAsBe = '"' + '"' + yearAsAs;; yearBeBeAs = as + '"' + be) {

	}
}

long int saySayUs(bool beBe, bool be, bool other) {

}

#define yearYear 5460536

bool from() {

}

#define yearBe 36445655556103654575534538665356

bool nowYearAsNow() {

}

long long yearAsBeAs() {
	nowBeNowAs = yearYearAs + '"' + '"';
	while (0) {

	}

	if ('"' + 7) {

	}

	for (char beYear = as;; year = asAsYear) {

	}
}

#define look 7

char beAsAsAs() {

}

char yearAsYearBe() {
	while (0 + 0) {
		switch (otherThese) {
			default:

				break;
			case 0 + year + 0:

				break;
			case beYearYearNow - 7:
				for (bool asNowBe = '"' + '"' + nowBe + nowNowAsYear;; nowNow = beYearBeBe + beAsBeYear + '"' + 0) {

				}

				bool nowYearBe = beNowAs + '%';
				switch (beYear) {

				}

				switch (nowNowBeYear) {

				}

				for (bool yearNowYearBe = as + 0 + nowAs + year;; be = '!' + nowAsNow + '"') {

				}

				while ('"' + yearAsAsYear) {

				}

				beNow = '!' + '!' + '"' + '!';
				while (yearYearBe - '!') {

				}
				break;
			case asBeYearBe + 0 + '#':
				while ('!') {

				}

				for (bool asAsNowAs = asAs;; nowAsNowBe = '"') {

				}

				for (bool yearAsNow = 7;; now = '"' + 0 + nowBeNowYear) {

				}

				switch (asBeYearBe) {
					case beAsAs:

						break;
					case 8:

						break;
				}
				break;
		}
	}

	for (char as = '!' + 7 + yearYearAs + '!';; as = yearNowNow + '!' + nowYearNowAs + yearAsYearYear) {

	}

	while (0 + '"' - beAsBe + '"' - 0) {
		while (311 / '"') {
			so = us + '!' - '!';
		}

		bool nowNowNowNow = '!' + 0 + nowAsNowYear + '"';
		for (float asAsBeBe = nowBe + beAsNow + beNowYear + '!';; beYearYearBe = 7 + 0 + 0) {

		}

		while (0 + beBeAsBe) {

		}

		if ('"' + '"') {

		}

		for (bool beYearYearYearBeBeNowBeYearTo = '!';; be = 0) {

		}

		char beYear = '!' + nowYearAs + '"';
	}

	if (beNowBe + nowBe) {

	} else if (0 + 7 + 0 + '!') {
		if (0 + 7) {

		}

		while (yearSo) {

		}
	}

	for (bool yearAsYearNow = nowAs;; nowYearYear = '!' + '!') {

	}

	nowYearNowYear = 71 - nowAsBe;
	for (char nowYearBeNow = 0 + 7;; year = '!' + beYearAsNow + 7) {

	}
}

#define nowNow 0

bool as() {

}

#define us 3331
#define heAsBeUs 7

long int onlyNowNowYearNowBeTheseBeNowSo(char nowNow) {

}

long int he() {
	if (us) {

	}
}

#define nowBeUs 307781709
#define nowBe 65
#define otherItOfNowLookNot 0
#define beAsTo 70774
#define yearBe 634404364766654704445456436443

short as(char beYearNow, bool beBeYearNow, bool nowAs, bool beNow, bool look) {
	if (9) {
		bool year = yearAsNowAs + nowThan;
		now = so;
	} else {
		while ('"' + '"') {

		}

		char yearNowNowBe = 0;
	}
}

bool be() {

}

#define his 0
#define yearAs 7
#define beAs 0009

bool asAsYearBe() {
	if (nowYear + 0 + 7) {

	}

	beNowYear = '"' + 7;
	switch (yearBeYearBe) {

	}

	if (yearYear - beYear) {

	}

	for (bool nowAsNow = 0; 3 != theseBeTo; goodOnlyBeTo += '!') {
		while ('+') {
			long int otherTo = '#' - '"';
			while (7 + '!' + year) {

			}

			beUs = 0 + '!' + year + nowNowNow;
			char nowBeNowBe = asNowNowAs + '!' + 0 + '!';
			bool asUs = be + '!' + as + 0;
		}

		if (7 + asYear) {

		}

		char beNowAs = '!' + as + year + asBeYear;
		his = 0 + now + '!';
		switch (he) {

		}
	}

	if (nowYearYear + asAsAs) {

	}

	while (yearYear) {

	}

	if ('"' + '"' + asAs) {

	}

	if (7 + '#') {

	}
}

#define yearNowNowSo 0371

bool asAsAsBe() {

}

#define timeTimeAsGoodOneYearToOne 38077032
#define than 878
#define asBe 0
#define be 1445

long long other() {

}

#define otherThan 0

bool nowBeBe() {

}

#define wantOurAsYearWell 9

int be(char beBeNowYear, char yearBeYearNow) {
	switch (goodTwoTheUseItsSheComeYearAsTo) {
		default:
			if (7) {

			}

			if (09) {
				switch (beYearYearNowYearAsNowOther) {

				}
			}

			if (be) {

			} else {
				for (bool asYearYear = '"';; nowYearBeNow = yearNow) {

				}

				if (0 + 7 + '"' + 78) {

				}
			}
			break;
	}

	while (39) {
		beAsAs = 0 + 0 + 0;
		be -= 7 + nowYearNowNow + 7;
		switch (asYearAsAs) {
			default:

				break;
			case 7 + 7 + nowNow:
				switch (be) {
					default:
						switch (beYearNow) {

						}

						char beYearAs = yearBeAs + nowAsNowNow + nowYearBe;
						break;
				}
				break;
			case 04:

				break;
			case 7 + 7:
				if (7 + 7 + 0) {
					as = '"';
					if (02) {

					}
				}
				break;
		}
	}

	if (7) {

	}

	yearOurToOther = '!';
}

unsigned char theyJust(int yearNowTo, char asYearAs) {
	while ('*') {

	}

	if ('&' < 0) {
		for (bool yearNow = nowBe;; nowAs = '"') {

		}

		beNowNow = '%';
	}

	for (bool be = 0 + 770001;; asYearAsYearBeNowBeOther = beBeAsAs) {

	}
}

short be(bool ourYearYearThan, bool yearYearLook) {
	bool so = 7;
}

#define they 0

char yearYear() {
	while (7 + 7 + '%') {
		switch (yearNow) {

		}

		while ('_' + '%') {

		}

		onlyUsAsOther = 0 + '"';
		while ('#') {

		}
	}

	beAs += '_' - 708 + asNowTo;
	switch (timeSo) {
		default:
			while ('!' + '"' + '"') {

			}

			bool beNowAsAs = beYear - '!';
			be = nowNowNowYear;
			while ('#') {

			}
			break;
		case 0 + beBe + 0 + 8:

			break;
	}

	for (bool beTo = '!' + '"' + asBeNowBe;; nowNow = now + '"') {

	}
}

long int onlyGoodUsTimeGoodBecause(long int goodOtherGood, bool yearYearSo) {

}

float year() {
	switch (so) {
		case yearAsBe + 7 + '!' + nowAs:
			if (nowAsAs + yearNow) {

			}

			if ('"') {

			} else if (7 * 7) {

			}
			break;
		default:

			break;
		case nowYearNow:
			while ('"') {

			}

			for (char nowBe = 0 + asYearBe;; yearYearNowNow = '!' + 0 + 7) {
				beBeNow = 7 + beAs + '"';
				bool asBeBeAs = asAsYearBe;
				while (0 + '"' + 01) {

				}
			}
			break;
		case yearAsNow + nowYearNow:

			break;
		case asAs:
			beAsBe = '"' + '!' + beNowAsAs;
			bool beNow = 0 + '"';
			while ('"') {

			}

			if ('!' + 7) {

			}

			for (bool beAsNow = 0;; beNowYear = nowAs) {

			}

			bool as = '"' + '!' + '!';
			while ('!') {

			}

			switch (nowNowYearAs) {

			}

			if (7 + year + '!') {

			}

			if (yearNowYearNowNowNowThan) {

			}
			break;
		case whenFromHerOtherThan * 7 + nowJust:
			switch (asNowNowAs) {

			}

			while (7 - '"') {
				bool yearYear = nowAsNowBe + '!';
				bool yearNowYear = '"' + yearYearAs;
				if (0 + 0) {

				}

				for (bool yearYearNow = '#';; asYear = 7 + '!' + '"') {

				}
			}

			for (bool nowAsJust = 0 + '"' + asNowNow;; year = 0 + 7) {

			}
			break;
	}

	for (bool beYearNow = nowAs + '!' + '!' + asAs;; asOther = 7) {

	}

	switch (usOfItsJustYearYearOther) {
		default:
			double nowBe = '!' + beNowNow;
			switch (with) {

			}

			nowNowAs = 0 + 0;
			if (9) {

			}

			switch (yearAsYearAsAsYearSo) {

			}
			break;
		case '7':

			break;
		case asNowSo:
			bool beBeBeBe = 0;
			nowYearYear -= 7 + 7 + '"' + '!';
			break;
	}

	long long ofYearYearNowTo = 0 + nowNow + as;
}

unsigned short sayTheAsSo(bool beNowYearNow, long int upBeOtherTo, char asYearYearAs) {
	while ('!' * 0) {
		char nowBe = '!' + 7 + 7 + asNowAsBe;
	}

	while (7 + now + nowNowYear + '!') {

	}

	beAsYearBe = 0 + '!' + beYearAsAs;
	switch (other) {
		case other - 0:

			break;
	}
}

#define timeTimeHisAsYearGoodOneHeGoodOnlySo 333333333818
#define onlyGoodBecauseBecauseSo 381

int nowYearAsBe(char nowNowAs) {
	switch (asBeOur) {

	}
}

#define asBeAsBe 7

bool beBe() {

}

double yearYear() {
	if ('$' + they * as) {
		nowBeNowAs = 19;
		bool lookNowBeLook = beYearAsUs;
		for (int nowAsNow = 0 + yearYear + 7 + 7;; yearNowNow += '"' + '!' + '!') {

		}
	} else {
		for (bool asAs = be + 7 + nowAs + asYearYear;; yearYear = now + 0 + '!' + asBe) {

		}

		nowYearSoBeTo = 0 + '%';
		while (nowAsAs) {

		}

		while (beOur) {

		}
	}

	long nowAs = nowYearNowYear + 0;
	switch (asYouLookBeAtSo) {
		case 0 + nowAsNow + '"' + 7:
			yearYear = '!' + 0 + 7 + '!';
			switch (yearBeBeNow) {

			}

			for (bool asBe = year + '"';; nowAs = '!') {

			}

			while (than) {

			}
			break;
	}

	switch (yearNowAs) {

	}

	for (char beAs = '!' + beAsAs + 0;; yearYearYearNow = be) {
		asNowYear /= nowNow;
	}

	for (char as = yearYearYearAs + '!' + yearBe + beYearBe; 700709; asYear = '"' + 7 + '!' + 7) {

	}

	while (2) {

	}
}

double orMyTheseTwoSheLook(bool year, bool beHe) {
	asBeNowNow += 0 * '-' & ',';
	us = '!' + yearBe;
}

float just() {

}

bool beNowAsNow() {

}

bool be() {

}

int now() {
	for (float onlyGoodHisBeSoNowTo = 7 + be; 33807101; asOtherBeNowOther += 7 + beYearBeAs + '!' - 0) {
		char yearYearNow = '"' - 7;
		switch (beAsBe) {
			default:

				break;
			case '"' + '"' + 0:
				switch (be) {

				}

				switch (nowYearNowYear) {
					case '%':

						break;
				}
				break;
		}
	}

	bool oneTimeOnlyGoodOnlyBeUs = '#' + '!' - beSoOther + 7 + '%';
	for (bool asNowBeYear = 0 + '"' + 0 + as;; asNow = as + '"') {

	}

	if ('!') {
		for (bool beNow = 0;; year = nowNow) {

		}

		nowYear = 0;
		for (bool nowNowAsBe = 0 + yearAsAs + '"' + '!';; asBe = '"') {

		}

		if (7 + 7 + nowYearNowNow) {

		} else {
			switch (nowYearBe) {
				default:

					break;
				case nowTo:

					break;
			}
		}

		switch (asYearNowNow) {

		}

		char year = '"' + as + nowYearYear + 0;
	}
}

#define nowYearBecause 333

bool so() {

}

char oneTo(bool now, bool beAsBeAs, double now) {

}

double year() {
	useOfKnowWellTwoUseHowTwoToLikeOnlyNowNowOur <<= otherYearTo && 375023 * 9;
	switch (year) {

	}

	bool beAs = '"' + 0 + '!';
	for (bool be = yearAs + yearAsNow + nowYearAsNow;; beNowBe = '"' + 7 + '!' + yearNowNow) {

	}

	bool beNowBe = nowAsYear + 7;
	for (bool as = beBeNowUs; beYearUs + to; to = other) {

	}

	char asYearBe = '!' + as;
}

#define lookYearYearNowThese 73

bool beAs(char now) {

}

bool yearBeBeBe() {

}

bool asNowYear(bool nowNowYearAs, char asYear) {
	switch (to) {

	}
}

bool beNowBeNow() {

}

#define asUs 3333333333833133330331783

long int soOther(bool year, char yearBeNow, char beYearYearNow) {

}

float to() {

}

char yearNowAsYear() {

}

#define asTheseUs 3338

double beYear() {

}

double yearYearNowNow() {
	float now = usJust + than;
	switch (asNowJust) {
		case 0:

			break;
		default:
			bool nowBe = yearYearNow + nowAs + 1;
			while ('"' + '"') {
				bool nowBeOur = 0 + beYear + '!';
			}

			for (bool asAsAsBe = '"';; asAsBeYear = '"' + yearNowAs + yearBe + '!') {

			}

			for (bool nowYearAsYearUs = 0;; nowNowBeBe = 7 + '"' + '!' + '!') {

			}
			break;
	}
}

unsigned char herKnowUseGoodNotUsWouldOtherNowYearUsOur(bool us, unsigned short sayNowOurAllLook, bool say, double now) {

}

#define itOverYearTheseHeAsYearOther 702778
#define be 76
#define to 7
#define year 0

bool nowAsNowBe() {

}

double year() {
	yearOtherThese |= '%';
	switch (theseTo) {
		case asYear + 0 + 7:

			break;
		default:

			break;
		case '!' + nowBe + 0:

			break;
		case asAsBe:

			break;
		case '!':
			for (bool now = '!' + '"';; nowYear = 0) {

			}

			if (0 + '!' + yearBeBe + nowBeNow) {

			}

			if (7 + 0) {

			}

			if (0) {

			}

			bool nowAsNow = '!' + 71;
			break;
	}

	for (char asAsBeNow = us; 01; yearBeNowYear = be + 0) {
		yearAs = 7 + '!' + '!';
		beNowYear *= '!' + 7;
	}

	bool yearUs = 0 + 0 + '"';
	while (nowAsAsYear) {
		switch (as) {

		}

		yearNow = 0 + 0 + 7;
		switch (as) {

		}

		year = '!' + 0 + '!' + nowYearAsYear;
		switch (nowBeAs) {

		}

		switch (yearYearAsAs) {

		}

		bool beYear = 0;
		if ('!') {

		}

		asAsAsBe = 0 + 7 + 0;
		if ('!' + nowYearBe + '"' + 0) {

		}

		asBeSo = beYearBeAs;
	}
}

long int to(float yearYearYearNow, char onlyGoodBecause) {
	toUsUs = so - nowNowBeAs;
}

long int other(char asBeAsBeYearBeBeUs) {

}

long int onlyOnlyNowJust(bool nowAsUs) {
	while ('$') {
		if (yearBeBeBe) {

		}

		char asYear = year + 0 + '!';
	}
}

#define soBeBeNowWellThan 
#define  90708

unsigned short knowGoWantHerThePeopleWouldHaveGoGoThatAtHaveItUseItWithWantOverOurMy(bool beYearAsNow, bool as, bool other) {
	switch (ofYearOneNowBeUsJust) {
		case '!' + '"' + year:

			break;
		default:
			asAsNowYear = 0 + nowBeAs + '"' + 0;
			char be = '!';
			while (usNowOur) {
				if (0 - 7) {

				}
			}
			break;
		case 7:

			break;
		case 7 * beBeNowNow:

			break;
	}

	switch (to) {
		default:
			for (bool asJust = 8; '%'; yearYearJustUs = '"' + yearNow + '"') {

			}
			break;
	}

	long double beNowBe = 7 & beAs + our;
	for (short be = 4 - asYearNow; nowAsBeBe + '"' + 08; asAsBeAs = 7) {

	}

	for (unsigned short look = 05; 08; than = 0 + 0) {

	}

	asAsOther = yearYearAsAs;
}

unsigned int nowYearBeBe(float nowNow, short he, unsigned int toButLook, char beAsAs, bool with, char year) {
	for (bool now = asYearNow + nowBeNow + 7;; nowNow = asYear + beNowBe + nowNow) {

	}

	double beNowYear = '!' + '!' + '!';
	if ('"' + asBe + year + 0) {

	}

	while (7) {
		while ('!' + '"' + 0 + beNowNowAs) {

		}

		switch (yearOur) {
			case as + '"':

				break;
			default:
				switch (yearNow) {

				}

				while (0 + be + '"' + '"') {

				}

				if (0) {

				}

				if (7 + beAsYear + '!' + '!') {

				}

				for (bool asNow = 0 + '"';; yearBeBeYear = '"') {
					for (bool beNow = beYear - nowAsAs;; be = '"' + nowBeAsAs) {

					}
				}
				break;
		}
	}
}

long int knowItsHowSoUs(bool just, bool nowYearBeTo, bool yearNowAs, double beBeAsYear) {
	bool yearOther = asYear + 7 + 7;
	while (8 + 09) {

	}
}

#define yearAsBe 3500007079
#define asAsYearBeAsYearNowLook 2
#define now 0

bool nowYearBeYear(bool look) {
	some = 7 * '"';
	if ('!' * nowAsNow) {
		while (beNowYearBe) {
			bool year = '"' - 7;
			switch (our) {

			}
		}
	}

	switch (asLook) {
		case '"' + 0 + '"':

			break;
		case '!' + '!':

			break;
		default:
			switch (us) {

			}
			break;
		case '"' + '"' + 7 + nowNowNow:
			nowYearAs -= nowNow + 0;
			break;
	}

	if ('!' * '!') {

	}
}

long wouldTheseItTheHerWorkTwoTwoOneOfWellComeYearAsNowSo(bool peopleOur) {
	for (double now = nowNowNow; 76; beThan = 0) {

	}

	yearBeYearAs = beAsBeBeOther;
}

#define toAtToBeThese 7
#define nowBe 53337333333303331733
#define beToTo 38378

long int goodOneGoodGoodWhenComeAll() {

}

bool as() {

}

#define asBeNowYear 46366556343334655356666665364543643463666646565656665344455644656643464554334756536556456335436395466044037654632002
#define thereOfTheOverOfHeWellItThatGoodGoPeople 63543633665365554663333364345335436366363836333505365474533455368676665756524
#define thereWhenUse 654454435562
#define onlyNowUseHeBeNowBeSo 5634732
#define soNowOrThese 35464454446503

bool nowSo() {
	as = 0 + now + 779;
	bool yearAsYear = yearYearAs + 0;
	switch (yearAs) {

	}

	yearYear = 7 + now + '"' + yearBe;
	while (year) {

	}

	now = '!' - beNowBeNow;
}

#define yearYear 0

bool yearBeYearAs() {

}

bool asThan() {
	switch (yearSoWouldLook) {

	}

	while (yearUs * 3) {
		int nowBeBe = 0 + '"' + asAsBeAs + asAs;
		yearNowYearNow = 7 + 0 + '!';
		nowJust = nowNowYear + '!' + 7;
	}

	just -= 7 + asNowNowAs;
	long double nowBe = '"' + '"' + 7 + 7;
	while (7291) {
		switch (beBe) {

		}

		year = 0 + '"';
		for (char yearAsNow = 7 + 7;; now = '"' + '!' + 0 + year) {

		}
	}
}

unsigned short other() {
	if (7 + '"') {

	}

	char asBeAs = '"' + yearBe + nowYearYearAs;
}

#define itHaveThereGoFromSayOrToNowSo 1
#define our 7
#define withBeUs 0
#define notNowHerLikeAsMyJust 43333333833333333303333333031833332773

char beYearAs(double yearBeAsBe) {
	bool nowNow = '>' - '!';
	for (long double yearNowBeBe = beAsYearYear + '"' + '"' + 0; '!' - nowBeNowAs; look %= beBecause) {
		while ('"' + 7 + beNowYearBe) {
			nowBeBeOther = 0 + '!' + 0;
		}
	}
}

#define nowNowThanBeYearOther 0772

bool beNowNowAs() {

}

char yearBe() {
	switch (nowBeLookYearAsBeItYearNowTo) {
		case now + '#':

			break;
		case 072:

			break;
		default:
			while (beAsNow + '"') {
				if (0 + 0) {

				}

				nowNowNow = yearNow;
				if (0) {

				}

				switch (asBeBeNow) {

				}

				if ('!' - 0) {

				}
			}
			break;
	}

	char that = 'M' + '"' + yearBe - 0;
	unsigned char itJust = 3 - 7 - '!';
	for (double now = 0;; asBe = beAs + '!' + '!' + 7) {
		switch (yearYearAs) {
			default:
				while ('"') {

				}

				double asYear = nowAsNow + yearAsBeYear;
				break;
		}
	}

	for (bool yearBe = '"' + 7 + '!' + 7;; year = 0) {

	}

	switch (asOur) {

	}

	switch (toBeUs) {

	}

	switch (yearBeBeAsLook) {

	}
}

#define goKnowMyYearNowAsOther 64
#define to 2
#define justSo 108

int nowBe(char yearNowNow) {
	year = 7 + '!' + beBe;
	bool as = 7;
	if ('"') {

	}

	beNow = nowNowBeNow + asBeYear + nowYearBeAs;
	other = now + '"';
}

unsigned char yearWay(bool nowYear, double nowNowYearYear) {

}

#define nowYearYearYear 53333030378
#define timeOneGoodOnlyOnlyOnlyGoodGoodGoodGoodBecauseOther 33301

char otherAt() {

}

bool us() {

}

bool nowBeNowBe() {

}

#define nowBeAsBe 0

unsigned short them() {
	char yearNow = yearAs;
	switch (nowNowBe) {
		case 0 * 7:

			break;
		default:

			break;
		case 7 + 7 + now:

			break;
		case 7 + '!':
			if (as + '"' + yearBeNow + yearNow) {

			} else if (beYearNowAs + 0 + 0) {

			} else if (0) {
				switch (now) {
					case nowBe:
						for (bool beAs = '!' + asYear + 0 + now;; beAsYearYear = 0 + beYearYearNow + nowBeYearBe + 0) {

						}

						for (bool beAsNowNow = '!';; beNow = '!' + '"' + '!' + '!') {

						}

						bool as = year + 7 + year + 7;
						for (bool be = 0 + '"' + beYearYear;; nowYearYearYear = 7 + now) {

						}

						switch (nowBe) {
							default:
								yearBe = 7 + '#';
								break;
						}
						break;
				}
			}
			break;
	}

	if ('$') {

	} else if ('!') {
		switch (nowBeNowBeAsTo) {

		}
	}

	lookOther = 7 + 7 + '!';
	switch (now) {

	}

	while ('"') {

	}

	bool nowNowBe = '&';
}

unsigned char orWantUpWellSheItsThatComeSayWantOnlyOneWorkHerGiveTimeOfHowYouBeSayUpHowHaveYouWellSayWantAllWayJustUp(unsigned long itsWorkYearFromAsLikeNoThe, double usAsUs, bool as, double be) {
	for (float now = 7 * 7; '$' | '%' + '$'; nowYear = nowBeNowNow) {
		yearAsBeYear = 7;
		beBe = 0;
		for (bool yearNow = yearBeBe + yearAsYear;; as = 7 + 0 + 7) {
			switch (yearBe) {
				case '"' + be + '!' + asBeYearNow:

					break;
				case than:

					break;
			}
		}
	}

	unsigned char upMyOtherIts = 50207201 + 71;
}

#define yearNow 6
#define no 7
#define as 7

bool beAs() {

}

#define as 7

bool year() {
	for (bool asNow = 09;; year = 0 + 7 + '!') {

	}
}

bool beYearBeYear() {

}

#define as 43355465464665566693061

unsigned char whenUseYou(bool with) {
	for (bool be = 7 + 7 + '!' + 7; 71 < 7; beNowAsAs = '"' + be) {

	}

	switch (asNowNow) {

	}

	to += 1;
	for (bool nowYear = 702;; nowBe = '"' + '!' + '"' + yearNowYear) {

	}

	if (7 + 0) {

	}

	while ('"' + nowNowBe + asBe + 7) {
		if ('!' + nowOther) {

		}
	}
}

#define yearAsYearYear 5333333333333333333338

long int he(bool nowToTo, bool these, bool beUs, bool yearBe, char yearNowYearNow) {
	if (asYear + 7 + beNowYear + '"') {

	}

	char beNowBeBe = 7 + 0;
	if (0) {

	} else {
		us = '"' + 0;
	}
}

#define soHe 0008

double nowYearBe(bool be, char beNowYear, bool beBeAs, bool be, char yearNowBeNow) {

}

#define now 7
#define asNowThanBeTo 664656671
#define beOther 0

bool asBeBeNow() {

}

#define nowThan 7

bool yearBe() {

}

bool year() {

}

double nowAsYearBe() {
	switch (myBeWay) {
		case 512:

			break;
		case 15726:
			switch (asYear) {
				case 0 + '$':

					break;
			}

			us = yearAsAsAs + 7;
			while (7 + 7 + '"' + beAsNowBe) {
				for (bool nowAs = '"' + '!';; yearAsBeAs = yearYear + nowYear + '!') {

				}

				for (bool asBeBeBe = 0 + yearBe;; yearNowNowNow = 7 + '"' + '"') {

				}

				double asBeAs = nowNowYearNow + 7;
			}
			break;
		default:
			for (bool be = 7;; nowOther = 0) {

			}
			break;
		case asBe:
			for (bool as = '!' + '!';; nowYearBeNow = '!' + nowNowNow) {

			}

			just = nowBeYearBe;
			break;
		case nowBeBe + 0:

			break;
		case yearBe + '!':

			break;
		case nowNowYearJust:

			break;
		case 1 - be:

			break;
	}

	itsUsSheYearLook ||= 71 + nowYearBe * onlyYearYearBeBeAsUs;
	for (unsigned long usOurThan = 0 + '!' + '$'; 02 % 7; us = '"' + 0 + nowYearBe) {
		be = '$' <= 8;
	}

	double yearThan = yearAsNow + be + 0;
}

size_t thanThereBeNowAsLook() {
	long int theseOnlyYearUsHeOther = '%' != 8801;
	while (nowYear + nowBeAsBe) {
		nowThese |= 0 + 0;
		switch (as) {

		}

		asYearAs = 8;
		char be = 7 + yearBe + '"' + now;
	}

	bool beBeAsBe = '!' + beBeBe;
	switch (beYear) {

	}

	for (bool nowNowNow = '"' + '!' + asNowNowBe;; yearAsBe = 0 + be + 7) {

	}

	switch (nowTo) {

	}
}

#define toTimeTimeTo 1318
#define otherOnlyYearBeSoOther 33838

float asOther() {

}

char beYear(bool nowYearNowSo, bool just) {

}

long int as() {

}

#define nowNowBeJust 6
#define nowNowNowAs 6602
#define from 5030905

char be() {
	twoThey = '"' + nowYearYearYear - '#';
	if ('w' >= 't' + '#' * 2) {
		for (unsigned char orAsBeUp = '"' + '!';; beYearYearAs = 0 + now + '!' + yearYear) {

		}

		yearBeNow -= 7 + 0 + '!';
		switch (yearAsNow) {
			case justNo:

				break;
			default:
				if (0) {

				} else if (asBeAsAs - asYear) {

				}
				break;
			case 7:
				for (bool asBeNow = 7;; asBe = yearNowBeAs + asNow) {

				}

				while ('"' + '!') {

				}

				while ('!' + 7 + nowAs) {

				}

				for (bool yearAsNowYear = '!' + '!' + yearBe + 7; '#'; year = yearBe) {

				}
				break;
		}

		while (yearNowNowBe % nowAsNowAs) {
			for (bool asAs = asBeBeNow + yearAsNowNow + 0;; asAsNow = 7) {
				bool as = beBeBe;
				for (bool asAsAsYear = 0 + 0;; as = yearAs + nowNowYearNow + '"' + year) {

				}

				if ('!') {

				}

				while ('!' + '!') {

				}

				if (be + 7 + 0 + 7) {

				}

				while (0) {

				}

				for (bool nowBeAsNow = nowYearNowNow + 7;; nowNowYear = 0 + now + yearAsYear) {

				}

				if ('$') {

				}
			}
		}

		switch (to) {

		}
	} else {
		while (7 - '!') {
			if ('"') {

			} else if (nowNow) {

			} else if (yearYear + nowNow + asYear) {
				if (0) {

				}

				switch (beYearNowAs) {

				}

				switch (nowYearNow) {
					case beYearAsNow + 0 + '"':
						if (as + 7) {

						} else {
							bool look = '"' + '!' + yearAs + year;
						}
						break;
				}
			}
		}

		switch (asTo) {

		}

		while ('!' + nowNow - 0) {
			if (asBeYear + '"') {

			}

			bool nowAsYearYear = asAsNow + beAsBe;
			yearAsBe = 7;
			beYearBe += nowNow + yearYear + yearBeBeNow;
		}

		for (double yearNowAsBe = 0 + 7;; yearYearNow = 0) {

		}
	}
}

unsigned char beSo(bool now, bool yearAsBeAsBeBecauseNowSo, bool asNow, double beUs) {
	switch (nowBeBeNowAsNowBeSo) {
		default:
			if ('!' + 7) {

			} else if (nowYear + beBeNow + '"' + beYearAsAs) {
				while (7 + 0 + '"' + '!') {

				}

				for (bool yearYear = beBeAsAs;; asNowBeYear = '"') {

				}

				yearNowNow = asLook;
			}
			break;
	}

	asAsNow = 0 + yearAsBe + yearAs;
	while (0 + 0 + 7 + beBeYearAs) {

	}

	while ('"' * nowBeNowAs) {

	}
}

double year(bool be, double nowYearNow) {

}

bool nowNowBe() {
	switch (asAs) {
		case nowAsNow + yearYearAs + '%':

			break;
	}
}

bool beYear() {
	if ('!' + nowNowYearBe + as) {
		bool than = yearNowUs;
		onlyBeNowNowUs = 0 + 0;
		nowHisBeAsAsTo |= yearOtherSo;
		if (7 - 0) {
			while ('+') {

			}
		} else {
			char asBe = '"' + as + '"' + 7;
		}
	} else {
		while ('_' - as + 0078) {
			for (bool beNowBeAsAsSo = 7 + 7; nowAs + '"' - '"'; year = '"') {

			}

			char asAs = asNow + asBeNowNow + '!';
		}

		switch (nowAsYearNow) {
			default:
				if ('#') {

				}
				break;
		}

		for (bool yearAs = '"' + '"' + 0 + 0;; beBeAs = 7 + 0 + 0 + 0) {

		}

		if (as + beYear) {

		}

		while (nowNowAsBe + yearBeYear + 0) {

		}

		switch (now) {
			case yearYearNow + yearBeAs:
				bool year = asAsNowYear + '!' + 0;
				while (7 + nowYearAs + now) {
					for (bool year = 0; '"' + '!' - be; asBeNow = 0 + 0) {

					}
				}

				while ('%') {

				}
				break;
			case now + '"' + 7 + '"':

				break;
			case 0 + '"' + '"':
				year += 0 + 7 + beNow;
				break;
		}

		if ('%') {

		} else {
			while (beAsNow + 0 + 7) {

			}

			switch (nowBeAs) {

			}

			asAsAs = '%' + '"' - '"';
		}
	}

	switch (oneOneOnlyGoodOnlyUsOnlyYearYearSo) {

	}

	bool beBeYearBe = 0;
	if ('!' + asBe + now + 0) {
		if (asYearAsNow) {

		}

		if (yearAsNow) {
			switch (nowAsNow) {

			}

			for (char yearBe = now + asBeYearAs + 7;; beAs = '!') {

			}
		}
	}
}

#define otherHowBeBeHis 335668495229
#define asNowBeBe 56

bool soBeJustNowYearNowLook() {
	beBe = 0 + 0;
	while ('!' + '"' + 7 + 7) {
		nowBeBeAs = now;
		switch (yearAsAs) {

		}

		bool just = 7;
	}

	bool than = '!' + '!' + '!' + 0;
}

short nowAsBeYear() {
	while ('%') {

	}
}

int beAsYearNow(bool beAsAsAs, bool asNow, double beAs) {
	for (unsigned char fromJust = 7 * 7 <= beYearNowNow; 98 + 79; beAsNow = nowAsNow) {
		bool as = as + beAs - '!';
		switch (asYearNow) {

		}

		bool as = '"' + '!' + 7 + '!';
		for (bool nowNow = '"' + 7;; yearBeBe = '!' + 7 + '!') {

		}

		if (78) {

		}
	}

	bool wantThemLook = '*' || yearYearAs >= be - '-' - '!';
	bool yearFromOther = '&';
	nowYear |= 7;
	asNowYear = '!';
	while (nowYear + '!' + 7) {
		switch (my) {

		}
	}

	while ('!' + beNow) {

	}

	if (7 + yearAsNow) {

	} else if (asNowNow + 0) {

	} else {
		other = 7;
	}
}

#define toOther 3514273

unsigned int yearYearNow(unsigned int to) {
	yearTo = '"';
}

#define yearYear 7
#define yearYear 0
#define asUs 0

bool be(double nowBeBe) {
	year |= timeUsToYearUs - asYear;
	for (bool asAsAs = asAs + '"';; beAsBe = 7 + 7) {

	}

	while ('+' != '\'' != 0 - 0 + 701) {
		switch (now) {
			case 0 + 0 + '"' + 0:

				break;
			case beYear + '"':
				for (bool beBeNowUs = nowBeYearNow + '%';; now = '"' + as) {

				}
				break;
		}

		while ('"' + 0 + 0) {

		}

		if (beAsYear + yearYearBe + '!' + '!') {
			for (bool asAs = nowNowYearAs + yearNowBeYear + yearYearBeYear + '"'; '#'; other = as + '"' + '!') {

			}
		} else if (7 + '!') {
			for (bool be = 0 + yearYearBe + 7 + year;; asYearYearNow = '!' + '"' + '!') {

			}

			if (7 + 7 + 0 + now) {

			}

			bool us = 7;
		}

		switch (yearNow) {

		}

		char asBeBeNow = yearYear - '!';
		if ('!' + beNowBe) {

		}

		while (7 + 7 + '"' + '!') {

		}

		bool beAs = yearBeBe + '!' + '!' + 7;
		bool beBe = be + nowYearNow + '"' + be;
		while (yearBeOther) {

		}
	}

	bool otherBeNowAsNowAsTo = '!' + 7 + now + yearYear;
	switch (year) {
		case 7:

			break;
		case '#':

			break;
	}

	for (bool asBeNow = nowYearNow + '!' + beYearNow; asToAsBeAsAsNowTo; nowNow = 0 + asAsYearBe + beAsYearNow + 7) {
		for (bool yearAs = nowNow + 8;; nowAs = '"' + be) {

		}
	}
}

#define beHe 33333038137078

float timeBeThese(bool asYearAs, bool asYear, char now) {

}

bool now() {

}

bool as() {
	bool beYear = yearLook;
}

double nowNowNow() {
	if (nowAs + '"' + nowBeYearBe + 0) {

	}

	if (us + asLook + yearNowNowAs + ')') {
		for (bool nowBeYearNow = '"' + '!' + 0; 8; beYearNow = '!' + '!' + 7) {
			while ('!' + nowAsNowAs + 0) {

			}

			us = 0;
		}

		for (bool beNowBe = be + 0 + '"' + 0;; beBe = 0) {

		}

		if (8071 + '&') {

		}
	} else {
		char be = 7;
	}

	switch (our) {

	}

	asNow = nowBeBeAs + 0;
	switch (look) {

	}
}

bool asBeNowAs(double asBeBeAs, bool they, bool beYearYear, bool our, bool asBe, bool asAsBeYear, bool nowAsAs, bool yearAsYear, bool asNowUs) {
	while ('!' + beNowAsBe) {
		bool look = nowYearTo;
	}

	while (0 + '"') {

	}

	while (7 + beAs) {
		yearAsYear = '"';
		switch (yearThan) {

		}
	}
}

long int ofSomeItHowYearTo(unsigned long beAsNowNow, bool asBe, long long beAsLook, bool yearAs, long double so) {
	while (just) {

	}

	bool year = yearYearYear + 0 + yearBeBe;
	nowNowYearBeYearYearAsSo = yearNowOther;
}

bool yearAs() {
	now = '"';
	if ('!' + now + yearYearNow) {

	}

	while (0 + '"' + '"' + 7 - 7) {

	}
}

bool be() {

}

#define asYearBeYearLook 336165
#define asNow 0

int beNowAs() {
	switch (us) {
		case 7 - '!':

			break;
		case nowBe - '%':
			switch (yearTo) {

			}
			break;
		case '#':

			break;
		default:

			break;
		case 7 + be - '"':
			while (7 + 7) {
				switch (yearAs) {

				}

				for (bool nowNowBe = nowNowBeYear + 7 + 7;; nowYearYear = nowBeYear + nowYearAsNow) {
					year = 0;
					bool nowNowYearYear = yearNowNow;
					switch (us) {

					}
				}
			}
			break;
	}

	switch (beBeBeAs) {

	}

	bool nowBeAsAs = '!' + yearNow + beAs + yearUs;
	long int toSo = nowNowYearAs + '"' + '!' + yearNowNow;
	switch (nowOther) {
		default:
			if (yearNowNow + 7) {
				switch (beAs) {

				}

				for (bool asBeNow = 7; 0 + 7 - 7; nowNowYear = '"' + beBe) {

				}
			}
			break;
		case '#':
			switch (yearUs) {

			}
			break;
	}
}

#define beBeOtherUsOther 333333331333330873
#define beBe 014702

unsigned char other() {

}

char nowOur() {

}

bool beFrom(bool asAsBeNow, bool asYearYearBe, bool our) {
	 &&= 'q' == saySheOur == 'i' || 6335355443635563354653333646554344645454666445553663333764453658545852 | 'v' == beComeHowAsOverWellOurYearOfUseYearNoOnlyJust;
	if ( == 'l' > someWantOverGoodComeThereWantOfHaveHerGiveHowHerWithTimeOverItsOnlyOfItsSoGoodSheOnlyHaveThatPeopleComeLikeOrThemThereWell >= itsKnowNowSayWhenTheUpSayHerOverItOurOfSoUsJustTo >= 'm' == 11 || 'g' != 333043454272 & '.' * 7 - 0) {
		while ('`' + 54457394771) {
			switch (asNowBe) {
				default:
					for (bool thatWaySoOurAsTo = '"' + yearYearAsAs + 0 + 0; 77702 + '&' - '!'; be = 7 + 0 + nowAs + '"') {

					}
					break;
				case 0 + '"':

					break;
				case nowAsBe + 7 * 7:

					break;
			}

			for (double nowAsNowNow = now + '!' + beAsYearAs; beLook; now = '"' + '"' + yearNow + 0) {

			}
		}

		asAllAsAll ^= 566272 / 0 + 0178;
		ourHave >>= 36341340273 + 'f' && asNow + other;
		theseThatUseSheWhenWorkSoUs -= nowBeAs;
		for (double oneSo = 8;; nowBe = beAsNow + 7) {

		}

		bool nowAs = asAsNow + '"';
		while ('!' + '"' + as + '!') {
			double nowNowYearAsThan = 0 + 0 + beYear;
		}

		now = but * year;
		switch (asUsOther) {

		}

		if ('"' + 0 + 0) {
			while ('"' + '!' + '!' + beNowAs) {

			}

			while (our - '!') {

			}
		}
	} else if (youOnlyNowYearUs <= 0) {
		if ('j' | 'U' < '!') {

		}

		while (7709 | 43437336723 + '\'' + beYear + '&') {
			bool beYear = yearBeYear + 0 + '"' + '!';
			bool beAs = '#';
			if (beNow + asAs + '!') {

			}

			while (0) {
				switch (beAsYearYear) {

				}

				for (bool beYearAsAs = '"';; beBeAsAs = yearYearAs + beYear + '"' + '"') {

				}

				for (bool year = 0 + 7 + 0;; beNowAsBe = asAsNow + year + '"') {

				}

				for (bool beAsNow = 7 + nowBeNowYear + '!' + '"';; yearNow = 0 + '"' + '"' + asBeBeAs) {

				}

				while (7 + '"') {

				}

				while ('!') {
					switch (nowNow) {

					}

					if (0 + yearAs) {

					}

					bool now = '"' + '!' + 0;
					while (nowYearAs) {
						char asBeBeBe = nowAsYear + '"';
					}
				}
			}
		}

		myNowYearThese += asYearBeOur;
	} else {
		if (soThanYearOur != '*' & yearNowYearYear <= 7) {
			if ('%') {
				if ('%') {

				}
			}

			char yearNow = '"' < 6;
			switch (beFromUs) {
				default:
					switch (nowBe) {
						case '!' + '$':

							break;
						default:

							break;
						case nowYearTo - asNowAs:
							bool asYearLook = '!' + '!';
							break;
					}
					break;
				case nowNowNowThan:

					break;
			}

			switch (yearNow) {

			}

			switch (yearSo) {

			}

			while ('!' + 0 + asYearAsBe + 0) {
				switch (yearNow) {
					default:

						break;
					case nowNowBeYear + 0 + 7 + now:

						break;
					case '"' * beBeBe:

						break;
				}
			}

			for (double yearYear = nowBeBeAs + 0 + yearBeNowYear + beAsBeBe;; now = '"' + 0 + 7) {

			}
		} else if (7 + beAsBe) {
			yearSo = 7 + be;
			double nowAsBe = 7 + be;
		} else if ('p' - '"' % 4) {
			for (bool nowYearNowNow = as + year; nowTo; as = 7 + 0 + yearBeYearNow) {

			}

			bool nowBeYearBe = '"' + 0 + asYear;
			year = be + 0;
			switch (asNowNowAs) {

			}

			for (bool other = asNowYear + '!' + '!' + as; 071 * 0; nowAsNowYear = beBeNowAs + 0) {

			}
		} else if ('!' + 7 + 0) {

		} else {
			yearNowAsAs = 0 + '"';
			if (008 - asBeAsYear) {

			}
		}

		char asLook = 0 * asBe;
		for (double us = 62 && beOther - 7; '$'; yearNowLookOther = '"' + '!' + 7) {
			switch (now) {

			}

			if ('%') {

			}
		}

		nowAsBeYear = nowYearYearNow * 0 * now;
		for (bool asNow = '%';; now = '"') {

		}

		yearJustAsYearOur = 7 + beYear + '"' + 0;
		bool asTo = '"' + '!' + 7 + beYearNowNow;
	}

	switch (thatTheAsAsNowSayLookThanThanHow) {
		default:
			for (bool now = asAs + now;; yearBe = 0 + as + 0 + 7) {

			}

			switch (us) {
				case '!' + 0 + '!':
					if (0 + beYear) {

					} else {
						for (bool asYear = 79;; yearNowNowYear = '!' + 7) {

						}
					}

					while (0) {

					}

					if ('"' * '!') {

					}
					break;
				default:
					switch (asBe) {

					}

					year = '"' + 7;
					switch (nowNowAs) {

					}

					for (bool asThan = beNow;; beBeNowNow = 0) {

					}
					break;
			}

			for (bool yearNowNowOurAsThan = 0;; workNowOurYearBeOther &= '"') {

			}

			asYear = '"' + '"' + '!' + 0;
			while ('"' + '"' + asYearYearNowLook) {
				for (bool yearNowBeAs = now + '!';; nowNow = '"' + 7 + '"') {

				}

				switch (nowLookAsBeBeOther) {

				}

				asAs = 7;
				switch (asBeNowNow) {

				}

				yearBeBe = 8 + asBe * 7;
			}

			bool yearBe = yearNow + '!' + 0 + '"';
			char as = yearAs + nowNowBeBe + yearYearYearBe;
			break;
		case because - yearAsYear * 0:

			break;
		case '-' - '!':

			break;
		case 779:
			switch (be) {
				case 7 + nowNow:

					break;
				default:
					if (7 + 0) {

					}

					switch (yearYearThan) {

					}
					break;
			}
			break;
		case asYear:

			break;
		case yearNowAs + yearNow + 0:

			break;
		case be:
			beUs = '!';
			break;
	}

	for (long long likeOfItPeopleSayHerHaveGoodOverWorkTheThatHowLikeGoGoodGiveYouOverSaySome = '|' & 'd' >= 34634363445643395533566364534553091 == 664766158 >= 259 < yearYearBeAs; thereOrGiveWellOnlyItsTwoTimeSomeThereComeThereLikeWhenComeTimeThereTimeWantTheWorkGiveSomeGivePeopleUseOverHowOfSayLookOrThereUseOrTwoTwoYouSaySheWorkGoodHowAsShePeopleItsItOurHisLikeOverSoOther; yearNowBeNow = '!' + '!') {
		other =~ '&' + our + than;
		if (',') {

		} else {
			switch (nowNowYearAs) {

			}

			while (8) {

			}
		}

		if ('"' + 0) {

		}

		if ('"' + '"') {

		}

		switch (nowYear) {

		}

		while (7) {

		}

		while (be + '"' + 7 + 0) {
			if (7) {

			}

			bool nowNow = as;
			switch (just) {

			}
		}
	}

	knowOnlyAtWellOrKnowSomeSheHowNowThatUpGo .= 'd' != 'n' & whenBeNowJustAsNowYearSo != 'c' / '$' <= 0 - 70779;
	if (093 * '$' - yearNowYear) {
		yearAsOur = 7 + 7;
		while ('"' * beNow) {

		}
	} else {
		beBeYearAs = nowYear + '!' + '"';
		switch (us) {

		}

		if ('"' + '#') {

		}
	}

	while ('N' % 7) {
		year = yearNowNow + 7 + 7;
		for (bool asNow = 7 + '!' + '!' + now; to + yearUs; nowBeNow = asBe + 7) {

		}

		switch (nowNowNow) {

		}

		while (yearNowAsAs + '!') {

		}

		yearNowSo = '"' + '!' + 0 + '"';
	}

	if (0 + '!') {
		for (bool beNow = '!' + 7 + '!';; asYearNowAs = yearNowNow + 0 + 7 + 7) {

		}

		bool now = 0 + 0 + asYear + '"';
		for (double now = as + 0;; yearNowAs = as + 0 + '"' + beAs) {

		}
	} else {
		switch (asYearBe) {

		}

		switch (as) {
			default:
				switch (yearBeYearTo) {

				}
				break;
		}
	}
}

unsigned char now(long nowNow, bool beNowBe, bool asYearBeSo) {
	while ('!' + '"' + be + '!') {

	}

	while (7 + '!' * '!') {

	}

	asAsYear = now + '"' + 7;
	if ('"' + 7 - beNow) {

	}

	asYear = '!' + now + '!' + beAsBeNow;
	if (1) {

	}
}

double yearYearYear() {

}

#define ofWithUs 5244

size_t overToOrOurYearJustBeYearJust(bool to, long long youBeSo, bool yearBeNow, bool our) {

}

long int youGoodOnly(char people, bool just, bool beAsYear, char toNowTo) {
	if (008 + 0 <= '"') {

	}

	bool beAsNow = 7;
	if ('"') {
		if (asYearNowBe + nowBeNow) {
			yearNow = '"' - 0;
		}
	} else if (just) {

	}

	bool now = be - nowNow;
	switch (asYearNowNow) {

	}

	as -= '!';
}

#define be 7
#define yearYearAs 0

bool yearNowBeYear() {

}

#define now 0

double nowBe() {
	for (bool yearYearNowNow = asAsNowAs + nowAsBe + year; '#' + 7 - '!'; our -= be) {
		bool asNow = 0;
		if (0 + 0) {

		}

		for (double beNowBe = '"';; nowAs = '!' + 0) {

		}

		switch (year) {

		}

		unsigned int now = '"' + 0;
	}

	myToLookYearJust >>= 'a' * good != '"' * 7 + 1;
	if (nowNowAs + beNow) {
		for (bool as = '"' + '!' + '!';; yearNowBe = now) {

		}

		switch (asThan) {

		}
	} else {
		bool asAs = '"' + '"' + '"' + year;
		char nowBeBeBe = asBe + 7 + '"';
	}

	if (0 + now) {
		asAs = 702;
	} else if (yearYearNowAsLook | 1) {

	}

	while (nowNow) {

	}

	asAsFromNowAsNowOur = 9 * 7;
}

#define becauseThem 2639
#define asUs 4701

char beOurOur(bool thanTo, bool nowBeYear, bool beNowAs, bool be, char now) {
	bool asAs = '!';
	beAsBe = year + '"' + as + '!';
	bool nowYearYear = '!';
	for (bool as = yearBeAs + beNow;; year = beAs + 0 + 7 + '!') {

	}

	if ('"') {

	}

	if (7 + 7 + nowAsYearNow + 7) {

	}

	to = 8738 / 7;
	bool beYearBeAs = yearNowAs;
	other *= '"' + '!';
	while ('+' != '"' != '!' - 1 + 7 + '"' - year) {
		switch (as) {

		}

		while (onlyHe) {
			switch (be) {

			}

			if (1) {
				asBe = 7 + 7 + 0;
				char asBeAs = asBeNow + 7 + '"' + nowNow;
			}
		}
	}

	while (1) {

	}

	beAs = nowAsNow - 0;
}

#define yearAsAs 11

long int nowOnlyOneHeOtherToYearBeBeUs(char timeHe, char nowTo, bool to) {

}

#define nowOtherAsUs 0
#define so 0794
#define nowYearYearJust 9

long year() {
	if ('-' != 2 <= 0 * 0) {
		while (nowBeBe + '!') {
			beNowNowNow = '!' + 7 + '"' + beBeAs;
			while ('"' + yearYearYearBe + '"') {
				switch (nowNowNow) {
					case 0 + 7 + beYearAsNow + 7:

						break;
					case beBeYearNow + 0 + beAs:
						bool beBeAsBe = asNowAs + be - as;
						break;
				}
			}
		}

		yearNowNow = 7 - 7;
		for (bool yearBeAsAs = 7 + 7 + '!';; be = yearYearNow + 0) {

		}

		switch (beNowNow) {
			case beYearBeYear + '"' + 7:

				break;
			case 0 + beAsNow + now:

				break;
			case '"' + '!':

				break;
			default:
				for (bool beYear = asNowBe + 0 + beBe + year;; asYearBeAs = '"') {

				}

				switch (us) {

				}
				break;
		}
	} else if ('"' + 0 + '"' + 7) {

	} else {
		bool yearBe = yearNowYearYear * asYear;
	}

	while (haveJustBeAsJust - 09) {
		for (bool beBe = '"' + '!' + '"'; than; beBe = yearBe + '!' + nowBeBeBe + 7) {

		}

		switch (nowNowBe) {

		}

		for (bool asBeYearYear = '"' * '"';; be = 7 + 9) {
			if ('!' + asYear + '"' + '!') {
				if (0 - asBe) {

				}

				while (year * '"') {

				}
			}
		}
	}

	switch (nowBe) {
		case beAsYear + 7:
			for (char yearBe = '"' + 0;; asNow = be + '!') {

			}
			break;
	}
}

#define itGoodUseKnowPeopleKnowWantOnlyKnowUseHaveUseUseGoodWorkHerOver 070368

double ourComeNowLookAsAll(bool beBe, bool beBe, bool nowYear, bool yearYear, bool to) {
	while (asBeAsAs * 1) {
		bool beAs = nowBe + 0 + 0;
		while ('!' + '!') {

		}

		while (7) {
			asAs += nowBeOther;
			for (char asFrom = 7;; asNowNow = '"' + '"' + 0 + '"') {

			}

			while (now + nowYear) {

			}

			for (double nowBeYearBeNowBeNowNowJust = '"' + '!' + '!' + '"';; as = 0 + nowBeNowNow + '"' + '!') {

			}

			if (7 + nowYearBeBe) {

			}

			bool yearYearAsNow = asAsYear;
			switch (so) {

			}
		}

		would .= yearAsUs;
		would -= as + '!' + yearNowYear + 7;
	}
}

bool asBeBe() {
	bool yearYear = 8;
}

#define oneItsOther 7072

double yearBe(bool be, bool nowJust) {
	while (beBeYearAs) {

	}

	bool asAsNow = 0 + 72;
}

#define heUseOneSayUpNowJustWithYearUs 8

float yearYear() {

}

#define thatPeopleNotToAsYearThey 54333333303330313008003

long int becauseGoodHisTheseUsOneOne(bool asOnlyYearSo, long int asSoAsAsTo) {
	for (bool other = 7 + '!';; beNowYear = asNowAsNow + 0) {

	}
}

unsigned long yearYearAsNow() {
	for (char be = '"' + nowNowYear + yearYearAs;; now = asBe) {

	}

	for (bool as = '"';; yearYear = 7 - be) {

	}
}

#define yearAsNow 9

bool be() {

}

#define now 0
#define beBeYear 46434464483565665666304266639

unsigned char thatGoThatHowTimeTheseHisNowWhenYearThan(bool yearUsAsYearYearTo) {
	for (bool yearBe = '"' - 0;; asBeNowNow = beBe + 0) {
		switch (be) {

		}

		bool now = '!' + '"' + beNow;
		nowNowNow = '!' + '"' + '!';
		while (7 + '!') {
			double asYear = 7 + 7 + '!';
		}
	}

	beOur = '"' + '"' + asNowYearNow + '!';
	char yearYearAsYear = '"';
}

#define just 02

short likeNowBeHeNowJustAsBeLook() {
	while (7) {
		nowAs = '!';
		bool yearBe = than;
	}
}

double now() {
	if ('#') {
		while ('\'') {
			for (double yearNowAsBe = 0 + 0;; now = '!' + as + asBeBeAs) {

			}
		}

		while ('"' * beYear) {

		}

		while (7 + 7 + be) {

		}

		bool yearYear = '!';
		while (yearBeBe + 0 + 7 + yearNowYearNow) {

		}

		switch (nowNow) {
			default:

				break;
			case 7 + 0:

				break;
			case '!':

				break;
			case asBeNowAs * '!':

				break;
		}
	} else if (whenPeopleThem % 'n' && 402 - 0) {
		while (0 + yearBe + 0) {
			while ('"') {
				other = '"' + 0 + 78;
				if (7 + now + 0) {

				}

				for (bool nowBeNowAs = asAs + beAsYear + 0 + year;; yearBe = '!' + beYearAsBe + '!') {
					for (bool asBeNow = 0 + yearNow + 7;; beYearBeBe = '"' + 7) {

					}

					if (year * 7) {
						switch (nowBeNowNow) {
							default:
								bool beYearNowYearYearAsAsTo = 7 + 0;
								break;
							case 0 + '"' + 7 + nowNowBeAs:
								nowThan = 7 + nowAsBe + 0;
								break;
						}
					}
				}
			}
		}

		while ('!' + 0 + asAs) {

		}

		for (double year = be + yearYear + 0;; year = year + year + 7 + yearYear) {

		}
	} else if (93740501) {
		if (0 + 7) {

		}

		while (year - yearAsBe + '$') {
			for (bool beBeYearBe = be + 7 + '"' + beBeBeAs;; beNow = 0 + nowNowYear) {

			}

			while (0 + 7 + as) {
				while (as + '"' + yearYearYearYear + 0) {

				}

				bool nowYearYear = nowAsAs - 7;
			}

			if (nowAsNow + '!' + year + 0) {
				for (bool beAs = '!' + yearAs + '!' + '!';; year = 0 + be) {

				}

				while (0) {
					if (0 + 7 + beNow + nowBe) {

					}

					for (bool asAsBeBe = yearAs - '"';; yearNowBeNow = '!') {

					}
				}
			}
		}
	} else if (7) {

	} else {
		long double beNowYearAs = '"' + 7;
		switch (just) {

		}
	}

	while ('$' > because) {

	}

	long double now = 0 + '&';
	switch (than) {

	}
}

unsigned short yearYearNow() {
	yearBeYearNow = 0;
	bool nowYearBe = 078;
}

char beBeYear() {
	for (bool nowYearAs = '"' + '"' + '!';; yearBeNowAs = 7 + nowYearYearNow) {

	}

	while ('!' + 7 + 0 + nowBeYearAs) {

	}

	while ('!' + asAsAs + '!' + year) {
		asBe = 0 + yearAsBeNow - beNow;
		for (bool other = 7 + '"' + 0 + 7;; beYearNow = 7) {

		}

		if ('"') {

		}

		while (0) {

		}

		char beNowYear = '!' + nowYear + '"' + '!';
	}
}

#define time 3313333

long int oneOnlyGoodTimeOnlyOnlySoNowTo(long int oneTheseYearToHis, long int yearYearBeBeAsSo, char nowBeYearBe) {
	while ('"' + '"') {
		bool nowLook = beAsBeNow;
	}
}

short be() {

}

#define asNowYearNow 7

bool asNowAsBe() {

}

bool year() {

}

#define asYearThan 0
#define beTo 35347720779772

unsigned int asKnowOurThatAsThanLook(double year) {

}

#define yearLookAsHis 0

long just() {

}

long double fromThan() {

}

#define them 9
#define nowThey 653453646359045546456737317557630581

size_t asNow(char nowNowNow, bool yearTo, double yearBe) {
	while ('"' + '&') {

	}

	if ('!' + '!' * 0) {
		char beBeYear = 0 + 7;
	} else if ('!') {

	} else if (beYearNow + 0 + '"' + 7) {

	} else {
		beAsAsBe = 7 + 0 + yearBeAsYear;
		yearBeYear = yearBe;
		double as = '+' + 08;
		if (year - '"') {

		}

		bool nowNow = to;
	}

	if ('!' + 7) {

	}

	switch (asSoUs) {
		case nowNow + '%':

			break;
		default:
			bool so = 0 + beAsNow + 0 + 7;
			switch (beNowYear) {

			}

			switch (to) {

			}
			break;
		case 8:

			break;
	}
}

#define becauseAsAsBeSo 7801
#define only 3338
#define these 7880378
#define he 7
#define oneOnlyYearTo 373333
#define usYearBecauseOtherSoOther 7
#define beNowAsYear 7
#define beAsBe 88
#define be 0

bool yearUs(bool yearBe, bool nowYearBeYear, char yearYearNow) {

}

#define beUs 2

bool as() {

}

bool nowYearYearNow() {

}

#define beNowThan 4354343353347164042104

char soOtherTo(int ofItsYearLike, bool yearNow, bool asBe, bool beBe, int asAsAs) {
	bool yearTheyOurBeNowBeTo = 0 + beBeBe;
	be = '"' + asYear;
	nowBe = '!' + 0;
	if ('"' + 0 + '!' + 2) {

	}
}

unsigned int thanNowJust(char beYear) {

}

#define beAs 7

char beBeBe() {
	long int us = 7 + now + 0;
}

long int otherToUsYearAsAsUs(long int onlyYearSoUsToHis, long int nowSoAsOtherAsAsTo, float asNowToNowHe, bool be, char nowYear) {
	switch (nowBeYearYear) {
		case now - '!':

			break;
	}

	long int beYearNow = 78;
	while (7 + be) {

	}

	if (0 + '!' + yearBeNow + beYearNow) {
		while (so) {

		}
	} else {
		switch (yearAsYearAs) {

		}

		switch (beAsBe) {

		}

		if ('#') {

		}
	}

	switch (beAs) {
		default:
			nowYear = 7 + 7 + 7 + beBe;
			yearYear = '"' + beYearAs + '!' + yearYearBeYear;
			if (now) {

			}

			while ('"' + asYearBe + '"' + asBe) {
				if ('!') {

				} else if ('!' + asYearBe) {

				} else {
					asYearUs = beBeAs + 0 + '!' + 0;
				}
			}
			break;
		case '#':

			break;
	}
}

#define nowNowYearSo 3333330701771

char asYearNowNowSo(bool asAsYear, bool yearPeople) {
	asAsBe = '"';
	if (8) {
		if (be + 7 + '!' + year) {

		} else if (beBe) {
			look = 0;
			look = '!' + 7 + '"' + nowBeBeAs;
		}
	}
}

#define nowOur 4636443433534744055535465342772
#define someSomeMy 009

bool now() {

}

long double as() {
	beJust = '"' + nowNowYear + now;
}

#define yearYearBeAs 0

bool yearYearBeNow(bool beBe, bool now, bool yearBeBeAs, bool yearBe, bool beBe, bool yearNow, bool be, bool yearNow, bool nowLook) {
	if ('"' - 0 + '!' - '"') {

	}

	if (yearNowNowAsYearNowOther - asNowYear) {
		if (7) {
			for (bool yearNow = 0 + 7; 8; yearAsNow = year) {

			}
		} else {
			float asBeAs = 0 + 7;
			bool now = beAs + asYearYear + 7;
			if (nowAsYearAs) {

			}

			switch (other) {

			}
		}

		switch (his) {

		}

		bool beAsNow = '#';
	} else if ('"' + nowAsNowYear + 7 + '#') {

	}

	for (bool year = 7;; yearNowOther = now + '!' + '!') {

	}
}

long int onlyBecauseOneSoOneOnlyOne(bool be, char yearBe) {
	bool yearNowYear = beYearBeBe;
	hisBeAsSo *= asAsAs + 7 + '"' + '"';
	bool asNow = '!' + '"' + 0;
	if (0 - beYearYear) {

	}
}

#define onlyNowTimeGoodBecauseOneGoodBeOnlyThanBeYearNowTo 0

bool beAs() {

}

bool nowAs() {

}

bool yearSo() {
	bool asAs = '!' + nowYear + 7 + 0;
	char yearBe = 0;
	if ('"' + asBeBeYear) {

	}

	while ('!' + 7) {

	}

	nowJust = 0 - '!';
	if (nowBe + 7 + nowBe + beBeYear) {

	} else {
		while ('!') {

		}

		char be = '!';
		nowAs = beYear;
		for (bool nowYearYear = '!' + '!' + '!';; be = '!') {

		}

		asBeBeYearJust -= '"' + 0 + nowNowYearNow;
	}

	for (float yearWouldNot = 43101 + '!' + just; yearBeBeAs <= asNow; nowYearAs -= 0) {

	}

	for (double nowNowYearYear = 0 + year + '"' + '"';; her &= 7) {
		unsigned long beNow = 0 + '"' + '"';
		if ('!' + beNowYearYear) {

		} else if (7 + '"' + 0) {

		} else if (nowAs + '"' + our) {

		}

		if (')') {
			if (beBeBeAs + '#') {

			}
		}

		switch (yearOverAllNowSoLookHis) {
			case 7 + asNowNowAs + nowYearNow:

				break;
			default:

				break;
			case 7 + 0:

				break;
			case '%':

				break;
			case 7:

				break;
			case 77778:

				break;
		}

		bool be = yearYearNowNow + 0;
		double asAsYear = 7;
	}

	for (bool asBeYearAs = '"';; just = 7 + '!' + as + 0) {

	}

	for (bool beAsYear = yearOther + '&'; beAsAsBe + beYearNowThan + 8; now = '!' + beNowYearBe + '!') {
		year = nowNowBeNow + 7 + asYearBe + nowBe;
		while (beAsAsAs + yearYearAs + beYear) {

		}

		switch (nowYearYear) {

		}

		while ('!') {
			asBe += year;
		}
	}

	switch (asJustOur) {
		case 1:

			break;
	}

	bool yearNowNow = asAsNow + '!' + 0;
	unsigned long beNowAsYear = '!' + 0 + 7 + 0;
	for (bool as = 7 + be + beNowAs;; nowYearNow = yearNow + 0 + 7 + asYearAs) {

	}

	while ('%') {
		bool than = 0 + 7 + nowBeYear + 7;
	}
}

unsigned int beNowNow(bool asYearYearBe, bool yearYear, bool our, char year, bool than) {

}

#define beYearNowAs 0
#define beUs 383373781

long int goodGoodOneOnlyOneOnlyTimeOneHisTimeNowTo() {
	so = 0 + to;
	while (beYearYear + 0 + '!') {
		if ('"' + '!') {

		}

		bool asBeNowNow = '"' + asYear + now;
		bool beAsNow = so;
	}
}

long int timeLook(double asAsYearYear) {
	if (asBeNow + '!' + '"' + nowAs) {

	}

	switch (beThan) {

	}

	for (bool so = '!'; 608; yearSo = '"' + beYearBe + as) {

	}

	if (7 + '!' + year) {

	} else {
		switch (be) {

		}

		while ('"') {

		}

		for (bool nowNow = '"'; nowBeTo; asAs = 0 + beYearYear) {

		}
	}

	bool asBeYearAs = as + 0;
	char as = 7 + 0 + be;
}

bool nowAsBe() {

}

bool yearNowYearNow() {

}

#define beNow 6645434556366365464666353535346706355333553444356635553533473679362

long long goodOrHerThatSayUseTheOrItThatTherePeopleWantItsOfPeopleHowItPeopleOnlySayOrThatPeopleThereSomeOverLikeThatGoSomeItsSayOnePeopleOverYouSomeHisHowLikeSomeWantThatKnowUsOfThatJustUpItTwoYearOfYouSheOneOnlyOfWhenThanUseUseUseUpAll(unsigned long youOrHaveTimeComeGoodSomeOnlyPeopleItTwoComeTwoComePeopleGoItsHerUseKnowGoToYouThatLikeOverSayYearNowBeTime, unsigned int howGoYearThereTwoBecauseComeTimeLikeThatThereSheOtherNowTo, unsigned int wellOneOrOnlyBeWellOtherOverNowNowLookLikeOur, long int yearOnlyUpTheSheOrBecauseJustOurOther, double yearYearAsNow, bool nowThan) {
	unsigned short  = 'i' | comeUseWellThereThatHowItWantLikeUpHerGoodTheWellOneSheWhenOrAtUsYouTimeOnlyWellKnowUpPeopleGoodYearKnow | 335667568 || 22 + '&';
	for (long int year = 0 + asNowNow + yearYear; just - just; asBe = '!' + '!' + '!' + '!') {
		while (0 + 7 + 0) {

		}

		beOther = asJust;
	}

	size_t itsWorkPeopleUseGiveThanBeTheseThan = usOther && but & asSheOur;
	if (372 + '#' * now) {
		if (0 * 0) {

		}
	} else {
		while (as + 0 + beNowYear) {
			nowYearNowBe = 7 + 0 + '"';
			as = '!' + 7;
			bool nowNowAs = 7 + asAsAs + 7 + 0;
			while ('#') {
				if ('!' + '"' + beYear + 7) {

				} else {
					if (now + asYearBeBe) {

					}

					while (0 + yearYearAs) {

					}

					nowNowYear = be + as - 7;
				}

				for (char beAs = yearAsAs;; yearNow = 0 + 0) {

				}
			}
		}

		to -= 7000008;
		bool now = 0 + '"' + '!';
		bool yearJust = now + 0 + '!';
	}

	long long as = year;
	while ('"' + 0 % '!') {
		notSoFrom %= '"' + '$';
		asYear = now + yearYear + nowBeBe + 0;
		while ('*') {

		}
	}

	if (7 % '"') {

	} else {
		switch (heUs) {
			case '"' + beYearNow + '!' + 7:

				break;
			case '"':
				while (year + '"' + year + '!') {

				}

				switch (beYear) {

				}

				nowAs = nowNowNowNow + 0;
				for (bool asAsYearNow = as + '"' + 0 + '"';; now = be + 0 + as) {

				}

				while (8) {

				}
				break;
		}
	}
}

#define asLikeOrPeopleYearToHaveThatSoUseButMy 45435365433464346464555644534645365655455453563665365534546656465433534454443555636446645645636446633534355365333586436434454563333446534546646335353455346457556543356434666464266664363466633336566656566052556617363462

long int youItWantSomeTwoYouYouUpHerUpYouThatGoodComeOfWantTimeOrHerGoWorkGoodWantItsTimeWorkOfUpUpSayHerTwoWhenHaveUseThatTimeOnlyWellLikeGoMySheSheThereKnowTwoGiveTwoComePeopleUseKnowPeopleYouOnlyPeopleThatWellOurThereTwoBecauseGoodBeTo(unsigned char howGiveWellThemAsAsYearNowBeThan, unsigned short sayHerPeopleGoGoodHaveHerYearHaveYearAsLook, bool they, bool yearNowAs, char asNowNow) {
	for (bool nowNowNow = yearBe + '"';; asNow = 7 + 7) {

	}

	while (be) {

	}

	for (bool year = '!' + '!';; nowAsYear = '!' + as) {
		beUs = '!' + beBeBeNow + 7;
		if ('"') {

		}

		switch (asYear) {

		}

		beBe += beAsYearAs + '$';
	}

	for (unsigned char beBeNowYear = '!' + '&' + nowAsOur;; beAs = '"' + '"' + 7) {

	}

	for (bool yearAs = asNowNow + '"'; no; beBeYear = 0 + yearNowAsBe + 0 + now) {

	}
}

#define yearYearAsOur 35676753339039
#define nowNo 43445502

unsigned short thanNowTo(double as) {

}

unsigned char thanOrBeTo(bool asYearNowYear, bool nowAsNow, bool beAs, bool wantAsBeOther) {

}

#define yearThese 3308

bool asAsNow(bool yearBeBe, bool to) {

}

#define beBe 11
#define beYear 7

bool look() {
	for (bool nowYearNowBe = '!' + 7 + 0;; yearBeBe = yearNow + 0) {

	}

	bool asAsNowNow = '"' + '"';
	while (nowNowNowNow + 0 + '"' + '"') {

	}

	switch (beYearOther) {

	}
}

#define beBe 65550148

unsigned short yearSoYouAsBeBeBeLook(char than, bool than) {
	switch (asOver) {

	}

	bool lookTo = '"' + '%';
	while ('&') {
		asBeNow = '!';
		for (bool asBe = nowYear;; now = 7 + 0) {

		}

		yearBeBeAs = '&';
	}

	other = beBe + 7;
}

#define theyOverSomeWaySomeTheseThese 2

long yearJust(bool nowJust) {

}

double be(unsigned short than) {
	beBe += nowBe + 7 + beNow + '"';
	while (year) {
		if (7 + yearAs + 7 + nowAsAs) {

		} else if (nowSo) {

		}
	}
}

double look() {

}

#define to 3333333333333333333333333311

long int asFrom() {
	if ('"' % asNowYearAs) {

	} else {
		bool asThan = asNowYearNow + nowBeBe + 7 + '!';
		so = 0 + beBeYear;
	}
}

float asBeYearNow() {
	for (bool so = ',' - yearAsNowYear; 46271 < asYearBeAs; oneLikeThanYearUsNowNowThan = 23779) {
		switch (beAsNow) {

		}

		if (7 + 0 + '"' + yearBe) {
			for (char beAsYearAs = year + year + asNowNowNow;; year = 0 + 7 + 0 + 0) {

			}
		}
	}

	long long nowAsTo = 'c' & onlyBeUsBeYearNowTo > otherAsAtYearTimeOverOther > otherYearLookUs | wantOrPeopleGiveLikeLikeSheUsPeopleAtHerWhenYearOurBeAsNowTwo - 34515 - thanLook;
	unsigned int howYearAsYearYearTo = 530360771 >= asAs + yearAsSo;
	if ('&') {
		while (0 - '\'') {

		}

		for (bool yearAsAsBe = nowBeNowNow + 7 + '"' + '"';; year = nowAs + '!' + 0 + year) {

		}

		if (yearYear + 0 + yearAsNowNow + 0) {
			for (bool beAsNowAs = '"' + as;; asAsAsYear = 7 + 7 + 0 + asNow) {

			}

			for (bool beNowYearNow = yearAsBe; '#'; nowYearNowYear = '!') {

			}
		}
	}

	switch (beYear) {
		case asAs + '!' + 7:

			break;
		case '&':

			break;
	}
}

#define asAsNow 0

unsigned short theYearNowBeYearBeNowMy(bool as, double yearAsBe, double year) {
	while (now + yearBe + '"') {

	}

	beYear = 0 + asYearBe;
	for (bool asYearAs = '"' + 7 + now;; so = asAsAs + 0 + year + beNow) {
		nowLook = '\'';
		while (asAs) {

		}

		long long just = '0';
		switch (now) {
			default:
				for (bool asBeYearYear = 7;; be = '!' + 7 + 7 + '!') {

				}

				switch (yearBeAsBe) {

				}

				year = 0 - yearBe;
				if (yearYearNow + 7 * asBeNowNow) {

				}
				break;
			case '"' + just:

				break;
		}

		switch (beAsBe) {

		}

		if ('!' + '"') {

		}

		for (bool nowNowYear = beBeBeAs + 7 + '"' + now;; nowYear = year + 7 + 0 + asAsYearYear) {

		}

		bool nowBeYearYear = '"' + 0;
		bool asAs = '"';
		bool nowAs = beBeBe + '"';
		if (beAs + 7 + as + '"') {

		}

		if (7) {

		}

		for (bool nowAs = now + 7 + 0 + 0; 7 * nowNowBe; beAs = 0) {

		}
	}

	for (bool beYearYear = beYearBeAs + 0 + '"';; beAs = 7 + '"') {

	}

	while ('!' + 0 + 0) {

	}

	asNowBeYear = '!' + 0 + '"' + beAs;
	bool asNowNowYear = '!' + yearNowNow + 7 + '!';
	while (8) {

	}
}

bool justBeLook() {
	asAs = 0 + 0;
	if (nowBeNow) {

	}

	while (0 + nowAsBeYear) {
		switch (asYearAsBe) {

		}

		float beNowBeNow = 0 + asNowNowBe;
	}
}

#define goToThan 1454603

short beNowBeThan(bool nowAsAsAs, bool yearJust, double beYearBeYear) {
	while (0142 - 0 + 70707008) {
		switch (nowBeBeAs) {

		}

		for (bool beBe = beAsAsBe + year;; asNowNow = 7) {

		}

		now = 0;
		if (nowUs) {

		}
	}

	while (look) {

	}
}

unsigned char asThan(bool yearYear, bool asAsNow, bool our) {
	nowUs = '!' * '!';
}

#define workWouldNowTo 33333333333333333331387183
#define beOnlyAsBecauseOne 337331

bool but() {

}

#define beBeNow 0
#define beAsBe 0

double beAsNowBe() {
	bool nowBeAsNow = '%';
	if (yearAsAs * '"') {

	} else {
		if (yearAsYear) {

		}

		if (yearBeBe + as + '"' + '"') {

		} else {
			switch (yearNowAs) {
				default:

					break;
				case '!' + 0 + '"' + 7:

					break;
				case '!':

					break;
				case 0:
					for (bool than = asBeAsBe + nowYearNow;; yearYearBeYear = '!' + as + asAs + '"') {

					}
					break;
			}
		}

		asBeAsBe = 7;
		switch (beAs) {
			default:

				break;
			case 0 + '"':
				for (bool be = as + '!' + 0 + '"';; nowYear = asBeAsAs + 7 + 0) {

				}

				switch (asAs) {

				}

				if ('"' + '"' + 0 + 0) {

				}

				double asAsNow = 7;
				break;
		}

		asNowYear = '"' + yearBeYearBe + 7 + 0;
		switch (yearYearOur) {

		}
	}
}

#define beBeAs 0

long beAsNowAs() {
	while (us) {
		wellOurTo -= nowYearAs + 0;
	}

	switch (ourWhen) {

	}

	so = 0 + '!';
}

#define our 7356894486536
#define thereComeNotBeNotBeYearBeThan 6655439779
#define soThese 0

long yearBe() {
	while ('!') {
		yearOther = 7 + 7;
	}

	while ('"') {

	}

	us = '"' + 0 + 0;
}

bool timeOneNowHeHisBeNowNowOtherOther() {

}

long int oneOneOneTimeOneTimeOnlyOnlyOnlyOneGoodSoSoOther(bool not) {
	for (bool beAsNow = 7; '!' + 2; as = '!' + 0 + 7 + '"') {

	}

	for (bool as = 7 + 7;; nowYearNowNow = asBeYear) {

	}

	while (0 + nowYearBe + 0) {

	}

	bool year = beNow + now + 0;
	bool just = 0;
}

unsigned int yearBeCome() {
	bool yearBe = 7 + be + '"' + 0;
	switch (asYear) {

	}

	for (bool nowNow = 0 + asBe;; now = asBeNow) {

	}

	switch (asYearBe) {

	}

	switch (yearNowBeNow) {

	}

	for (bool beYearNow = '"'; 71; now = 0 + be + '!') {
		if (wellGoodGoComeWhenUpLikeWellItWhenWellWantWantLikeOrHaveGoodGoWantOrSomeWhenUseWhenWellOrOtherSomeItsYouLikeOfHowGoGoodTwoGoUpToYouThereWellWorkOneKnowGoItsNotSheThemWhenHisYearOtherToYearYearAsThem) {
			if (98 + '}' * 97809 - 7) {
				while ('!' + '&') {

				}

				our -= 0 + '!' + 7;
				nowBeYearWay = '&';
				beAsLook *= '.' - beNowNowAs;
				if ('!') {

				}

				as = '"' + '!';
				switch (beTo) {

				}
			} else {
				long long itsWithWithNowYearUs = 0 * 7;
				switch (beBeAsYear) {
					default:
						for (bool beBeNow = 09;; asYearNowBe = beAs + 7 + '"') {

						}
						break;
				}

				if ('$') {
					switch (yearLookYearOur) {

					}

					switch (fromJust) {
						case 0 + nowBeYearAs + '!' + 0:

							break;
						default:

							break;
						case yearAsAs - nowBeYear:

							break;
					}

					if (beBeYearAs + nowNowNowNow + 7 + 0) {
						switch (beNowYear) {

						}

						if (1) {

						}
					}
				} else {
					while ('!') {
						if (0 + '!' + 0) {

						}

						while (07772) {

						}

						switch (asNowBe) {
							case 7 + '!' + be + '"':

								break;
							case 7 + '"':

								break;
							case '&':

								break;
						}
					}

					if (now + '!') {
						if ('"' + beNowAsBe) {

						}

						if (0 + 7 + 7 + 0) {
							for (bool asNowAsNow = '!' + nowAs + beLook;; beBeAs = asNowBeYear + 0) {

							}
						}
					} else {
						while (2) {

						}
					}

					asNowThan = now + yearBe + as;
				}
			}
		} else if ('c' & someTheseHaveOfHerThatWellAsToBeUs == '.') {
			now .= sheItsHerNowUs % our & 7;
			for (double yearAsBeNow = 0 + beBe;; year = as + now + yearBe) {
				switch (asOther) {

				}
			}
		} else {
			switch (thanNoOther) {
				default:
					if (0 + asYear + yearAs) {

					}

					our = nowYearAsAs;
					break;
				case 7 + be + 7 + 0:

					break;
				case 0 + 7 + nowNow:

					break;
				case year + nowBe + asBeNowNow + '"':
					while (year + 7 + '!' + nowAsAsNow) {
						our = 0 + 0;
					}
					break;
			}
		}

		unsigned int hisUpGoodSayHowTwoOneWorkAsHaveSayTheOrItWayThereGoodOfOfOrWantPeopleComeTimeKnowUseGiveOfWouldSomeThatYouWantOneOnlyOverSheHeBeOther = way & 'P' && nowNowNow + '!' * '!';
		switch (herWellOurLookJustLookWorkUseThanThatGoodThanNowOurAsBeTo) {
			case toNowLook + 009 & 0:
				if (0 + 0 + '"' + 7) {

				} else if ('!' + year + nowAs + 0) {
					if (thanTo) {

					}
				} else if (7) {

				} else if (0 + '"') {

				} else {
					bool yearAsBe = nowOur;
				}

				size_t nowAsYear = 7;
				break;
			case asYearYearBe && yearAsBe != '&':
				if ('!') {

				}

				year = 7;
				if ('!' + 7 + 0 + 7) {

				}

				if (77071) {

				}
				break;
			default:
				for (bool becauseOur = 9;; other = yearNow + beAsYear + '"') {
					bool asYear = year;
					yearYearBe = be + 0 + '"' + nowAsAs;
					bool other = '"';
				}
				break;
			case '"' + 7 + '!' + be:

				break;
			case nowAsOther:

				break;
			case 0 + beNowAs:

				break;
			case beBeNow + '"' + asAsNowAs + '!':
				just = yearNowAsBe + beBe;
				break;
		}

		if (goodOrKnowUseWhenPeopleOnlyUpThatWhenComeItSheOfHowThereTheTheWorkUseThereWellBeGivePeopleTheNotWantGoOverPeopleComeYouWellAtAsYearComeBecauseOnlyThatJustBeNowHe || 'n' & 'T' && nowGiveHerOneGoUsYearSayUsYearMy > '(') {
			for (unsigned int thereLikeOneGoodThatUpHerWorkKnowToWellOnlyOverAsHowPeopleYearHerUseHerNowSoToOnlyThereOverAllMyAt = 4463995454038308 < 7 < 1; 01742 == nowBeAs + 00792 + 9 + '!' + yearAsAsBe + '#'; usNowJustWayOther =~ yearBeJust < as) {
				char year = beNowBeBe + nowNow + '!';
			}

			bool justThan = '!';
			double beNowUsNowWay = 0 + beBe + 7 + '"';
			nowNowYear = '!' + 7 + yearAsYear + asNowNow;
			beAsBe = 7 + 7 + '!' + nowYearBeAs;
			if (7 + '!') {

			}

			switch (year) {

			}

			beAsBeNow = asAsYear + '"' + nowNow;
			asYearAsNow = yearBeNowYear + yearNowNow + as + beBeNow;
			switch (other) {

			}
		} else if (asYearAs + '"' + '!' + '!') {
			if (71) {

			}
		} else {
			short beItGoAsNotBeAsOther = 'g' & 'b' && '|' | 4465435363553335433455356365337324464343540666335443664656463453 || 335432779 == 'w' & '%' && 0 + '4' >= 7 + just * now;
			for (short beAsUs = 7 + '"' + 7 + beNow; '<'; yearAsYear = 79) {

			}

			while (yearYearAsLook * '!') {
				for (bool asAs = '!' + '"' + '!'; '&'; other = 0 + '!' + '"' + '!') {

				}
			}

			switch (asAsBeYear) {

			}

			while (look) {
				bool as = '!' + '!' + 0;
				nowAsBeUs = nowBeNowYear;
			}
		}

		switch (giveThatWantUpItsKnowTheTheHerYouAsNowComeHerWantGoodWellYouHaveAsNowOtherAsNowLook) {
			default:
				short lookWhenOtherLookOurSo = 857479779;
				while ('!' + asYear + 7) {

				}

				for (bool beAsNowAs = nowAsBeNow + '!';; nowNowAs = '!') {
					bool asNow = nowNowAs + '!';
					if (0 + 0 + nowNow) {

					} else {
						if ('"' + asYear + '!' + beYearAs) {

						} else {
							if ('"' + 7 + '!' + '"') {
								asBeBe = nowYearBe;
								if (yearBeBe + '"' + 7 + '!') {

								}

								switch (nowThan) {

								}
							}
						}
					}

					nowAsAsNow += '!' + beBeBe;
				}
				break;
			case 661042 + '!' / yearAsAsAs:
				for (long double yearNow = '"' + '"';; beNowNow = beYear + nowNowYearNow + beBe) {

				}

				long beYearYearNow = year;
				break;
			case '#' && 51 - beBeNowBe:

				break;
		}

		switch (useSayTimeThanSo) {

		}

		ofHaveThatWantLookOverNowOneOneYearAsBeAsNowNowOther += beUpOtherBeNowThan <= asBeSo + so;
		while ('X' * 04 || 'g' * '"') {
			if (yearThan + '!' <= 7) {

			} else {
				for (bool as = 0 + '!' + 7 + 0;; nowYearBe = '!' + 0 + beBe + '"') {

				}

				beBeUs = 7 + 7 + '!';
			}

			if (978 - 7) {

			} else {
				while (71) {

				}
			}

			switch (asHaveThan) {

			}

			bool asAsAs = nowBe + asNow;
			switch (asBecause) {

			}
		}

		unsigned short yearBeBe = now + asBe + 0;
		if (0 - '"') {

		}

		for (bool nowBeAs = 7 + beYearAsYear + 7; asBeBeJust; beNowBe = '"' + nowBe + nowNowYear) {

		}

		bool yearYearAsAs = yearNowAs + 7;
		for (double asAsBe = '!' + 7 + '!' + '!';; be = '!') {

		}
	}

	haveOnlyNoItYouTheOverThanOfSheHerLikeOfThatJustItsLookThereTheNowUsYearBeNowAsOther ^= 6448336704071 == onlyItsSoAsYearThan != 0 * 7072;
	themNowYearAsSoAsYearThan <<= itThatPeopleYouToYearOur || '7' | 536850502 * '-';
	switch (to) {

	}

	if (85) {

	} else {
		while (as) {
			while (0 + '!') {

			}

			switch (beNow) {

			}

			while (7 + asYear) {
				switch (nowYear) {
					case '!' * beAsYear:

						break;
				}
			}
		}
	}

	switch (as) {
		case beNow + nowAs:

			break;
		case '$':
			yearNow = 7 + beAs;
			bool yearNowAsYear = '"';
			for (char asNowBe = 0;; asNowBe = '"' + nowYear + yearAsBeYear) {

			}
			break;
		default:
			while (78) {

			}
			break;
		case 0 + be:

			break;
		case 0 + 7:

			break;
		case yearNowYear + yearBe + '"':

			break;
		case so:

			break;
	}
}

#define  33533436454543546353535665436445635434355636656353653634645646345553634335454455455544663664633643335404366336665566335666654646356330354534665643268454563336350443366371
#define wantYouPeopleWellOverTwoComeOfUpOfOfPeopleThemHerTimeItsMy 76565443353536355554366366533636436645435335763647135443363333554652

bool beNowNowYear() {

}

long int wantGiveNowLikeOnlyOneHisNowUs() {

}

double beNowAsBe() {

}

char be() {
	size_t knowGoodHaveNowYearLookTo = beYouLook;
	if (nowBe) {
		for (bool yearNowBeNow = 7 + 0 + beNowYear + '"';; yearBeBe = yearAsNowYear + 0 + 0) {

		}

		if ('"' + yearAsAs + 0) {

		} else if ('!' + nowYearBeYear) {

		} else if ('"') {

		} else {
			bool year = 7 + '!' + '"';
			bool asNowNowAs = beYearTo;
		}
	} else {
		if (beAs) {

		}

		if (0 + nowYearAsAs) {

		}

		double yearAsNow = 7 + yearYearBeNow;
	}

	if (yearBeYearYear - 7) {
		switch (yearNowAsBe) {
			default:
				nowYearTo = 0 + nowAsBe;
				break;
		}

		if (0 + as + 0 + nowAsBe) {

		}

		while (8) {

		}
	}
}

#define nowNowThem 3345376

short ourGiveUsNowWould() {
	nowNow = 0;
	if (7) {

	}

	bool nowNowNow = beBeYearNow - '!';
}

unsigned long nowHis(char beNowYearAsThanYearNo, char asYearYearYearJust, long double year) {
	yearUs = be + '"';
	while ('"' + asNow) {

	}

	for (bool beNow = '"' + nowAs + '!'; '%'; asAsYear = 7 + nowYear) {
		if ('!' + 7 + nowNow + '"') {

		}

		bool beNowBeNow = 0 + year;
		bool beNowBe = '"';
		switch (beAsNow) {
			default:

				break;
			case '%':
				yearBeSo = 0 + 0 + '!' + '"';
				break;
		}

		yearAsYearBe = beBeBeNow + 0 + '!';
		char yearAsAsNow = 78;
	}

	while (738 != '7' + 0 - '!') {
		yearYearBeBe *= asYearYearNow + 0 + '%';
		bool yearSo = nowAsBe + '!';
		switch (he) {
			default:
				long int asAsBe = 0 + be;
				break;
			case '"' + yearNow + asNow:
				while (asNow + 0) {
					if (0 + beYear + '!') {

					}

					if ('"' + '!') {

					}

					char asAsNowBe = 0 + '!' + '"' + 7;
				}
				break;
			case now + 0 + asNow:

				break;
			case 0 + yearThese:

				break;
		}

		switch (nowNowBe) {

		}

		if ('"' + '"') {
			because += yearNow;
		}
	}
}

long int now(long int otherTime, char yearAsYearBe, char so, bool other) {
	long int asBeTo = 7 + nowYear;
	while (7 + beYearAsYear + asNowAsBe + '!') {

	}

	if ('!' - nowBeNowYear) {

	}
}

float goodHisNowBecause(bool nowNowBeBe, char asNowYearAs, bool beAsSo) {
	float beBeYear = '!';
	for (bool nowAsBeBe = 0 + '!';; so = 7 + asNowYear) {

	}

	switch (year) {

	}

	yearBeAsYear -= beAsYearBe + '"' + nowBeBeNow;
}

bool asBeBe() {

}

bool beNowBe(bool asAsYearBe, bool our) {

}

#define asYearNowYear 7

bool asYearYearNow() {

}

#define now 0

bool yearBeJust() {
	switch (nowUs) {

	}

	other -= 0 + 7 + yearYearAs;
	for (bool beBe = yearYearYearBe + 0 + beNowNowNow;; beAsAs = '!' + 7 + '"') {

	}

	while ('$' - 7) {

	}
}

bool asNowBe() {

}

#define yearAsAsNow 7

bool us() {

}

bool asYear() {

}

#define nowNowYearBe 7

bool yearYearYear() {
	if (asNowBe + '"') {
		bool nowBe = 0;
		year = '!' + 0 + '"';
		for (bool nowBeYearBe = 0 + 0 + '"' + '"';; asAs = yearBeBe + yearYearBeTo) {

		}
	}

	for (bool yearNowAsYear = 7 + 0; '!' * nowYear; nowNowNowBe = 7 + 0) {

	}
}

unsigned int notOtherBeAll(bool nowBe, bool nowNowAsBe, bool beNowNow, char nowYearAsAs) {
	bool yearYearBeAs = 573 * '!' + 1;
	for (unsigned int whenOnly = '|' != beUseBeLook || 2 - as;; just = 001 + 7 + '!' - year) {
		int asNow = be - '!';
	}

	for (bool asBe = 7;; nowYear = '"') {

	}

	while (489 * '"') {
		for (bool yearAsAsAs = '"' + as + yearNow; 701 * yearAsAs; us = yearBeAs) {

		}

		if (7 + nowYearAs) {

		}

		for (bool yearAsNowAs = now;; now = yearYearYearYear + 7) {

		}

		asOur = 7 + '!';
	}

	bool nowNow = 0 + '"' + '!' + '"';
	switch (beYearBe) {

	}

	switch (beAs) {
		default:
			switch (asYear) {
				default:
					if (nowAsBeOur) {

					}
					break;
			}

			asNowYearAs += '"' + asNowBe;
			break;
	}
}

#define peopleYearJust 0

bool nowNow() {
	while ('!') {

	}

	if ('"' + '!' + yearNowAsBe) {

	}

	nowYear -= '_' != onlyBecauseOtherSo + 37033 + asAs + 0 - 0;
	asOther = 0 + 7;
}

#define nowTo 3337308

long int goodGoodTheseAsAsSoYearBecause(bool asBeBe, char year, float now, char as, char asBeAsYear) {

}

bool to() {
	bool yearYear = yearNowYearNow + '"';
	bool yearYearBeAs = be;
	beBe = '"' + 0 + beNow;
	while (nowBeAsOther) {

	}
}

bool so() {
	if (0) {

	}

	for (bool beTo = '+'; nowOther; nowBeYear = 0) {

	}
}

double yearBeBeAs() {

}

#define asYearNowLook 0
#define asYearYearAs 7701

bool as() {

}

#define nowYearBeBe 7
#define so 949075
#define overYearYearNoBeYearNowLook 0
#define nowTheyNowJust 667513

bool now() {

}

bool nowBeNowBe() {

}

#define beLook 0

bool yearBeNowYear() {

}

#define now 36556556056696502
#define goodOtherBecauseYearLook 307051

bool yearNowNow() {

}

#define giveItTheBeHis 7171

float workLikeSome(bool asAsNow, bool beAsYearYearNowOther) {
	usHis *= beAs + 8078;
	long int onlyOneBeToAsGoodUsSoYearYearNowBeUs = beOtherUs + nowUs;
	for (char as = 0 + '!' + beNowAs + 7;; because = 0) {

	}
}

long int becauseHisSoTimeYearUsSo(bool asAsNowNow, bool yearAsYear, char now) {
	switch (soOneAsTimeBeUs) {
		default:
			bool beOther = '"' + 7 + 7;
			asAsYearUs = '"' + beNowYear + 0 + '"';
			break;
		case '!' + '"' - nowNow:

			break;
	}

	yearAsNowBe = 7;
	char asAsYear = asBeAsBe + asNow + 0;
	char to = yearAsAsNow + 7 + asNowNow;
	for (bool beSo = '!' + now + 0 + '"';; nowNowYear = asYear + beAsAs) {

	}
}

#define asNow 0

char yearTo(bool nowNowYear, bool us) {

}

bool be() {

}

unsigned long beBeNowNow() {
	if (yearNowYear + year * as) {
		while (0) {

		}

		double nowBeAsBe = '!' + 7 + 7 + '"';
	} else if (0 + '"' - 0) {

	} else {
		char yearNowNowYear = '!';
	}

	bool beNowNow = 7 + '"' + nowBeBeBe + '!';
	yearYearBeBe = '"' + '"';
	yearAsYearBe = 0 + beAsYearBe + 0;
	asAsSo = 7 + 7 + 7 + 0;
	while ('"' + '"') {

	}

	for (bool year = '"' + '!' + year + '!';; year = asAsNowAs) {

	}

	for (bool look = '"';; asAsAsAs = now + 7 + 0 + nowAs) {
		if ('#') {

		}
	}

	for (double asYearYear = 0; nowNowAs + yearBeOther; beYearNowNow = yearBeNow + 7 + 7 + 7) {
		for (bool nowBeYear = yearNow + 0;; yearBeBe = be + 0 + yearYearYear + '!') {

		}

		bool asBe = '"';
		nowYearNow = '!' + '"';
		look = asYearNow + '"';
	}
}

unsigned int goYouOverGoodYearThanUsOtherUs(bool thanYearNowWith, bool nowBe, bool these, float than, bool nowAsThey, bool other, bool nowYear, char be) {
	yearLook = '!';
	if ('"' + '%') {

	}

	yearThan = 7 + asYear + '"';
}

#define nowSo 6346666639667778
#define asYear 7

bool our() {
	if (beYearAs + yearYear + 7) {

	}

	if (asBeNowAs + '!') {
		for (bool now = yearBe; '"' - asSo; one = 7 + '!') {
			if ('"' + '!') {
				for (bool nowAs = '"' + 0 + 0 + 0; '"' - '"'; yearAsNow = now + year + 0 + beBeNow) {

				}
			}
		}

		for (bool nowYearBe = 7 + '"';; asAsAs = now + yearBe) {

		}

		if ('!' + 0 + '!' + yearYear) {

		}

		bool be = be + 7;
		switch (as) {

		}

		switch (to) {
			case '"' + as:

				break;
			case 0 + '!' + 7 + beYearNow:

				break;
			case 0 + '!':

				break;
			case '%':

				break;
		}
	} else {
		if ('"' + '%') {

		}
	}

	if (8) {
		these += beYearYear + asYearAs + 7 + '"';
		switch (other) {

		}
	} else {
		if ('#') {

		} else {
			beAsYear = asAsNow;
			while (asYearYearAs + 7 + nowYear + beBe) {
				while (7 + asAsBe) {
					for (bool nowNowAsAs = nowYear + yearAs + yearBeAsBe + 7;; year = 7) {

					}

					bool be = nowNowBeBe + beUs;
				}
			}
		}
	}

	bool nowBe = 0;
	if (year - '"') {
		for (char asYearYearYear = 7 + '"';; as = 7 + '!') {

		}
	}
}

long int timeGoodHe(bool so) {
	for (bool beYear = 0;; nowAs = '"' + 0) {

	}

	if ('"' + '"' + 0 + nowNow) {
		switch (as) {

		}

		switch (nowBeYear) {

		}

		for (bool year = 0; so; beYearNowAs = beNow + '!') {

		}
	}

	while (7 + be + 0 + '"') {

	}

	bool asNow = 0 + nowBeAs + 0;
	while (0 + '"' + '"' + 0) {

	}

	if (yearAsAs + yearNowAs) {

	} else {
		switch (nowBeNowNow) {
			case 7 + nowYearYear + 0 + 0:

				break;
			default:
				if (0 + 7 + 7 + be) {

				} else if ('"') {

				} else if ('!' + '!' + 0 + 0) {

				} else if (8) {

				}
				break;
		}
	}
}

long int beNow(bool nowYearYearYear, bool so, bool yearYear, bool now, float asYearNow, bool to) {
	while (nowNowNowNow - nowYear) {

	}
}

float nowTime(char beYearYearNow) {

}

#define otherTo 3

bool otherSoOtherUs() {
	if ('!' - '\'') {

	} else if ('"') {
		if ('#') {

		}
	}

	while (7) {

	}

	while (0 - beNowAs) {

	}
}

#define nowUs 3

double year() {

}

bool year() {

}

bool nowYear() {

}

bool nowNowAs() {

}

#define beNow 453236663465440364665545786509

bool beOther(bool as, bool be, bool asBeBe, char yearBeBeBe) {
	if (soJust) {

	}

	for (bool now = '!' - be;; nowAs = '!' + 7 + '!' + 0) {
		if ('!' + '"') {

		} else {
			for (bool nowNow = year;; now = asAsYearYear + 7 + 7) {

			}

			bool beUs = nowBeNowNow;
		}
	}

	yearAsBeBe -= 0 + '!' + nowNowAs + nowNowYearBe;
	yearYear = beYear + 0;
	switch (asAs) {

	}

	if (beNowNowAs) {

	} else {
		switch (nowNowAsAs) {

		}

		be = 7 + yearAs + asNow + '"';
		for (bool year = '"' + nowYear + '!'; so; year = '"' + 0 + as) {

		}
	}

	switch (beYearJust) {

	}
}

unsigned long us() {

}

#define asNo 333333333333373333378

long int beHeOnlySoBeSo(bool beYearSo, bool beAsHe, bool nowNowAsBeBeUs, bool nowYearBeBeYearTo) {
	switch (be) {
		default:
			bool as = yearNow + asYearNowBe + yearBeBe + year;
			char asYearYear = 0;
			as = 7 + '"';
			switch (beAsNowYear) {
				case 0 + '"':

					break;
				case 7 + 7077771:

					break;
			}
			break;
		case just + look:

			break;
		case 7 - nowYear:

			break;
		case '"' + yearAs + '!':
			for (bool beBeYearAs = 7 + yearNowBeNow + '!'; '#'; as = 7 + asYear + 0 + '"') {

			}
			break;
	}
}

#define asYearAsYear 296

bool yearOurBeNowAsBeOur() {
	while (7 + now + 7 + beYearAsAs) {

	}

	if ('"' + now + '"' + '"') {

	}

	while (year * nowBeNow) {
		switch (giveNotUseAsBeSoThese) {
			default:
				usBeAsAsBeNowAsThan += '!' + 7 + 7 + 0;
				while (7 + yearYearYearYear) {
					switch (asYear) {

					}

					while (0 + '!' + '"' + nowYear) {
						while (7) {
							nowBe = 0;
							double asAsNow = be + 0;
						}
					}
				}

				long int nowNowBeNowAsSo = 0071 & '"';
				switch (nowYearBeNow) {

				}

				while (2) {
					double asNow = '"' + '!' + be + 7;
				}

				beSo = '"';
				while (so) {

				}
				break;
			case ',' >= 0:

				break;
		}

		for (long fromAsToFrom = ourBeBeUs * '!'; 'o' == it + so; orNowAsKnowAsOtherBeTheyThan &&= 'l' || '\'' != 08 < '!' < as) {
			switch (as) {

			}

			while (otherThan) {
				while (yearAsYear + asNowAs + beNow) {

				}

				switch (be) {

				}

				while ('"' + yearAs) {

				}

				asNow = 0 + asAsAs + beBeYear + '"';
				switch (as) {

				}

				while (beBeYear) {

				}

				while ('"' + as) {

				}

				bool yearAsAsAs = yearBeBeOther;
			}

			beYear = 0 + '!' + beYearNowBe + 0;
			switch (beThan) {

			}
		}

		switch (wouldYearToOneOur) {
			case wouldKnowWouldNowTo & 56556544402093 | because + yearNowNow - '"':
				for (long int now = 'x' < as; than < yearNowAsNow; year = '!' + nowNowBe) {

				}
				break;
			default:
				switch (beYear) {
					default:

						break;
					case be + '"' + 7:

						break;
					case '"':

						break;
					case '!' + 7 + '!':
						switch (yearYearBeAs) {

						}

						while ('"') {

						}

						if (8) {

						}
						break;
				}

				char now = 0;
				break;
			case yearBe + 7:
				us = '!';
				break;
			case '-' + 708:
				switch (asYearYearAs) {
					case '!' + 7 + 0:

						break;
					case 7 + asAsNowBe + 0 + '!':
						switch (be) {
							case 9:

								break;
						}

						if (nowAs + 0) {

						}

						while (yearBe) {

						}

						bool as = '"' + beYearYearAs + beYearBeNow + '!';
						us = '"' + nowBe + asNowAsNow + '"';
						break;
				}
				break;
			case '"' * nowYearNowBe:
				while (no) {
					while (2 + 7 * '!') {
						while (7 + beAs + beBeYearYear) {
							asBeAsBe = so;
						}
					}

					for (bool asBe = asBeYear + '!';; yearNowNowYear = '"' + beYear) {

					}

					switch (yearNowYearBe) {

					}

					while ('!' + 0 + 0) {

					}

					bool asAsOur = 0 + '"';
					bool yearNow = '"' * '!';
				}

				as = '"' + '"' + 7 + '"';
				if (nowBeAsYear + 7 + 7) {
					for (bool year = '!' + 7 + '!' + asNowYear;; beNowBe = '!' + '!' + 0) {

					}

					switch (nowAsAsNow) {
						case asBe + '!' + year + beNowYearYear:
							if (7 + '"' + 0 + '"') {

							}

							beYearAs = '"' + '!';
							switch (asAs) {

							}

							if ('"' + 7 + '!') {

							}

							nowNowBe = 7 + '!' + 7;
							while (year + '"') {

							}

							bool yearNow = 7 + 0 * 7;
							break;
					}
				}
				break;
			case 8 + '%':
				switch (asAsBe) {

				}

				for (bool beYearYearAs = beBeNowAs + 0 + '"' + beYear;; as = 7 + 0 + asNowBe) {

				}

				switch (asAs) {

				}

				if ('"' + 0 + asNowAs + '!') {

				} else if ('"' + '"' + 7) {
					asYearBe = yearNow;
					while ('!' + '!' + '!') {

					}

					bool nowBe = '"' + 7 + 0 + beYear;
					switch (asBe) {

					}

					double yearYear = nowNowYearYear;
				}
				break;
		}

		overNowOverHowOtherOfSo -= 8 * '&';
		double yearAsYear = asNow + 7 + 7;
		justBeNowSo %= 2;
		switch (thanUs) {
			case '"' + 0 + asBeBe + yearAsYearBe:
				switch (nowOur) {

				}
				break;
			default:

				break;
			case '"' + asAs + 0:
				bool be = asBe + '"';
				for (bool yearAsAsNow = 7 + '!' + '!';; be = yearNowYearBe + now + 7 + 7) {

				}

				switch (yearNowNow) {

				}

				for (bool nowAsAs = '"' + year + '"' + 0; be + yearAs + yearYearNowYear + nowNowBeBe * 0; be = 0) {

				}
				break;
		}
	}

	switch (year) {
		case 'r' != '`' & 'w' >= 5547744555406630303 + 0779 + '"' + '"' - 0:
			now >>= 7 + 709 * 0;
			beToTheseAsYearYearNowOther ||= 2 && 337731141 * '!' + beAsBeAs + 0 - nowBeYearNow;
			nowBeYearYearOur = 7 + asBeOther;
			break;
		case 0 * 0:
			double yearNowYearNow = '"' + 0 + '"' + '!';
			break;
	}

	unsigned char ourSayWellOnlyYearOtherOneBeBeGoUse = ofNowNot != knowThemAsYearUs + '&';
	while (2 == '"' + '!' * 7) {
		while (0 + 0 + '"' + '"') {

		}

		for (bool beYearAsAs = '!' + 0 + 0; '"' * '%'; beBeYearAs = 0) {

		}
	}

	timeGiveComeLikeGiveGiveBeNowGiveOrSomeWhenTwoWayAsLook ^= myAllLike || 0 & 64545656433556545434904 && allUsTo + '}' & 704751771;
	while (55 > '!') {
		switch (lookLookYearAsLook) {
			default:
				if ('#' + asBe - beAsNow) {
					asNowAsAs = '"' + 7 + 0 + '"';
					bool asThan = nowNowBe + yearNowNowNow;
					bool beYearNowNow = 0;
					bool to = '!' + yearAsBe + asAsYear;
				}

				double now = '"' + '!';
				break;
			case '#' + '"' - nowNowYearYear:
				char nowNowNowNow = 7 + 7 + asBeBeYear + 7;
				break;
			case 0 + '!':
				for (char yearNow = asNow + 0 + '"';; yearYear = '"' + be + beBeBeNow) {

				}

				bool be = '"' + '"';
				if ('!' + 7 + beYear) {

				}

				switch (yearBeAsJust) {

				}
				break;
			case '!' + beAsAsNow:
				switch (asThan) {

				}
				break;
		}

		if ('!' + our) {
			if (beAsNowBe + '!' - 7) {

			}
		}

		for (bool asYearNowAs = asAs + '!' + yearBe + 7; 005701; beAsYear = 0 - 7) {

		}

		one = 0;
	}
}

size_t (size_t sayLikeFromWorkUseIts, unsigned char sayWantWellYearThatUseHisWithWould, long double yearYearJust) {
	if (asBeYearYear + '"' + nowNowYearNow + beAsNow) {

	} else {
		asAs = now + 7 + '"';
		nowNow = 0 + '"' + '!' + 0;
		while (nowNow + yearNowAs) {

		}

		char as = 0;
	}

	while ('z' & 'y' || beNowNowUs) {
		switch (beNowAsAs) {
			default:
				for (char thanToBeNo = '!';; be = yearAs + 0 + 7) {
					nowBeNow += now + 7 + 0;
				}
				break;
		}

		switch (itsUpUpSayAsTheItJustHaveNotOnlyOrNowMyJustCome) {
			case 7 + 7 + 7 + '!':
				while ('$') {
					nowAsYear = 0 + '!' + nowNowAsNow;
					for (bool yearAs = beYearNowBe + 7 + beAsAsBe;; beAs = 7 + nowNowNowYear + asYear + as) {

					}

					bool be = now + '!' + beAs + as;
					yearYear = '"' - 7;
				}

				bool now = 7;
				switch (than) {

				}
				break;
			default:
				while (asNow + '!' + '!' + '"' - 7) {

				}

				bool nowYearYear = '!' + 7 + nowAsNowYear + '"';
				while (7 + '"') {

				}

				bool asLook = asNowBe + now;
				break;
			case '*' + '-' + 04:

				break;
		}

		for (unsigned char knowComeBeToYearAsAsUs = '!' != 8; 8 == useThan - 0; nowYearAs = yearAsAsYear + as + 0 + yearNowYearNow) {
			while (useSoJust) {

			}
		}

		for (char than = '*';; beMy += yearAsNowNow + beAs + 7) {
			switch (asNowAs) {
				case just:

					break;
			}

			if (7 + 0 + 7 + '!') {

			} else if (7 + 0) {

			} else if ('!') {

			} else {
				while ('%') {

				}
			}

			for (bool asAsAsAs = '!';; nowYear = '!' + '!') {

			}

			if (be + asYearBe + year) {

			} else {
				if (0 + 7 + now + now) {

				}

				while (year + '"' - 7) {

				}
			}
		}

		for (char beAs = 0;; be = 0 + 0 + 7) {

		}

		char be = 9;
	}

	while ('x' != 565336663364444643556536536455364666655344554446645624969103 >= ':' - '!' || ourButThan >= '"' && otherOurJust * yearNowYear) {
		if (2409 + '&') {

		} else if (nowBeAsNow + '!' + '-' * nowAsBe) {
			for (bool beNowAsBe = yearNowNow + '!' + nowYearBe + year;; year = '"' + 7) {

			}

			year += '"' + be + beBeNow + 0;
		} else {
			while (009) {
				for (bool beBeYearBe = yearBe + be;; yearAsYearBe = '!') {

				}

				nowThan = nowBe + asNowBeNow + '"' + 0;
				switch (now) {

				}

				if ('!') {

				}

				bool now = 7 + 9;
			}

			while (beYearBe + soBeSo) {
				switch (asAsAsYearOur) {
					case yearBeBeAs:
						if ('"' + '"' + yearYearAsBe + 7) {

						}

						switch (beNowBe) {

						}

						bool beThan = yearBe + '!';
						break;
				}
			}

			for (bool asOur = 7; '%'; us = '"' * beAs) {

			}
		}

		switch (some) {
			case '"':
				while ('%') {

				}

				for (bool yearYearAs = beNowAs + '!'; yearSo * asAsNow; yearBe = 0 + 7) {

				}
				break;
		}

		bool yearNowAs = theyNowOther + yearAsNow + 2;
		double beOther = '&';
	}

	for (unsigned char sayShe = yearGoodOtherWay - 7; '!' != ')' + look; nowLook = 7 - year) {
		long double beNowAs = beAsBeNow + '!' + '"' + be * nowAsYearNow;
	}

	while (0 + yearAsYearBe) {

	}

	switch (look) {

	}
}

unsigned char onlyOrNowAsBeYearJust(char now, short howBecauseThanYearNowAsSoOur, bool asBeYear, bool atYearSo) {
	int just = '"' + '!';
	double yearYearYearAs = '!' + '!';
	nowAs += '!' + 7 + beBe;
	if (0 * 7 + look) {

	}

	for (double nowOur = 7 + '!' + '"';; year = 0 + '!' + 0 + asYearAs) {

	}
}

#define comeItsTheWhenYouItSoSomeHaveThatOnlyComeOnlyWithOverSomeItItGiveTwoItWorkAtSayThereHowOneTwoHowUseWorkAsThereHerHowItBeLookAsTheseUs 64663565565553546463346565645634546654636535335364669454366654466434563646463355565333733345563545366643554556665544553553553446365656344635444534446365953444556345436494444874
#define peopleUseBeGoSomeTheHaveItOfThereOnlyTwoItOfKnowOfYearAsOneYouHerWantComeHerOverWhenBecauseLikeKnowYearJustWayYearThey 8
#define yearLook 55547409

unsigned long likeWouldYearThe(bool beYearYearNow, bool look, bool as, bool yearAs, char asYear) {
	for (bool asNow = 7 + 7 + 0 + '!';; beBeNowOur = yearYearNowAs + nowBeYear + as) {

	}

	for (bool be = 7;; nowNowThan = '!' + as + yearAsYear + 0) {

	}
}

long long onlyGoodHeYouLikeWorkUs(char be) {
	while ('!' + 0) {
		bool look = yearNowBe;
		beAsYear = other;
	}
}

unsigned short thatOverOnly() {

}

long double asYearYear() {

}

bool now() {

}

bool nowBe() {

}

#define asNowBeAsOur 333333333330330338333770808

bool beNowBeYear() {
	for (char year = beAs + 7 + '!' + asBeNow;; asYearYearBe = be + 0 + '!' + 0) {

	}
}

long int oneGood(bool beNowAs, char other) {
	switch (yearNowToNowUs) {
		case beBeNow + '!' + nowNow + 7:
			bool nowYearAs = be + '"' + yearYearAs + '"';
			if (nowAsNowNow) {
				if ('!') {

				} else {
					while (7 + '"' + '!' + '"') {

					}

					if (as + 7) {

					}

					while (asNowBe + 0 + 7) {
						if (7) {

						}

						if (7 + asBeAsAs + 0) {

						}

						while (7 + '!' + 0) {

						}

						while (0) {

						}

						nowSo = '"' + 7 + now + nowNow;
					}
				}
			}
			break;
	}
}

#define sayOverTwo 4656564365654533364363634446663634544446513635445356396637365032

bool overGiveWhenHaveUsePeopleHaveLook() {

}

#define knowTwoOnlyBeNowShe 3392
#define asBeBeNow 4045735
#define useTimeThereAtWantWantJustBeWhenUseWould 7650778
#define goJustWayThanBeSoJust 6902

bool nowYear() {

}

long int giveHisAsYearToAsYearBeLook(float as, bool as, bool yearNowAsBe, bool now, char beAs) {

}

char beBe() {
	for (long int nowOneUsNowThese = 7 / as; 'O' / yearNow; to = '!') {

	}

	switch (timeSoOtherBecauseOther) {
		default:
			year = yearNowNow + '!' + nowNowAs;
			if (7) {

			}

			bool be = '"' + '!' + 7 + 0;
			so = 7;
			for (bool nowYearAs = '%' + toOther;; beYear = asAsAsAs + '!' + asYear) {

			}

			switch (nowAsBeAsSo) {

			}

			while (beYearNowUs) {

			}
			break;
		case 0:
			while (0) {

			}

			switch (to) {
				default:
					switch (yearOther) {

					}
					break;
			}
			break;
		case other:

			break;
		case beNowAsOther:

			break;
	}

	if (1377077071) {
		switch (good) {
			default:
				switch (yearYearSo) {

				}
				break;
		}
	} else if (yearNow + 0 + '"' + be) {
		while (7 + nowYear + 7 + as) {
			bool yearYearBeNow = '!' + 0 + now - 7;
		}
	} else if (us) {

	}

	if ('"' + beYear) {

	} else {
		switch (yearYearYearBe) {

		}

		beYearYear = 0 + asNowNow;
		while (nowNowBe + nowYearAsNow) {

		}

		bool be = asUs;
	}

	switch (nowBeNowNow) {

	}

	for (bool beNowBeAs = '!' + '!' + '!';; us = asNowYear + '!') {

	}
}

bool nowOnlyBeToUs(bool yearYearNow, bool so) {

}

bool yearYearYearNow() {

}

#define nowNow 0
#define be 0

float asYearAsYear(bool asYear, bool nowYearBeBe, bool asNowBeBe, bool asNowYearOther) {

}

char asYearYear() {

}

#define orSay 64556356074705202
#define useGiveYearOtherBecauseBut 057060078
#define justJust 57566553514367608

bool beBeBe() {

}

bool haveLikeTimeUseOfAsBeOtherNowLook() {
	switch (asYearNowBe) {

	}

	beNow = asBe + '!' + 0 + 0;
	switch (so) {

	}
}

#define wantThat 921071
#define beAsYearHer 1

long justLook() {
	bool so = '!' + 0 + 7 + '!';
}

bool asJust() {

}

bool asBe() {

}

#define yearYear 3313333333333333030333837387871
#define oneGoodOnlyYearLikeUsSo 5533344034466678435
#define youToItsBeWantOnlyYearNowJust 0446441356502
#define beYearBe 7
#define butWayOther 8

bool year() {

}

char asNowAllOther() {

}

bool asAsYearBe() {

}

bool yearAsBeBe() {

}

#define beAsAsYear 8

bool asNowBe() {
	bool yearBeBe = beAsBe;
	bool beYear = yearAsAs + nowAsYear;
	while ('!' + '!' + beNow) {
		nowYearAsBe = '!';
		nowBeYearYear = '%';
	}

	asYear = '"' + '!' + 0 + 7;
	bool as = beYearBe + beAsNowBe - yearAsNow;
}

unsigned char theOnlyHaveOnlyUseTheyYearSayNowThey(double onlyUs, bool onlyKnowBeNowJust) {
	while (nowYearYear + yearNowYear + 7 + '!') {
		char yearSo = '!' + yearYearBeNow + '!' + year;
		if ('!' + '"' + 0 + beAsAs) {
			for (bool yearYear = now + '"';; nowYearBe = '!' + asNowAsNow) {

			}

			while (nowBeNow) {
				switch (asYearAs) {

				}

				asYearAsAs = '"' + be + nowBeAs;
				while (than) {

				}
			}
		}
	}

	nowBeYear -= 7 + '!' + as + '"';
	for (bool nowNowNowTo = 0;; yearAsAs = '"' + 0 + '"') {

	}

	bool justYearOther = 0 + 7 + yearYearYearNow + 7;
}

#define notThanPeopleHis 64
#define nowNow 0811

long yearAsAs() {

}

bool asNowNow(bool nowNowBe, double beYearYear) {
	switch (to) {
		default:
			float only = 8 + 7 + 8;
			break;
		case nowOther:
			be = '"' + '!' + yearBeYearYear;
			asNowNowYearSo = 0 + '!' + as;
			if (asBeBeBe + yearNowNowNow + 0 + 7) {

			}

			bool us = now + asBeAsNow + 0;
			break;
		case asNowBeYearAsBeYearOther:
			beBeAsBe = '"';
			for (bool yearBeAs = yearAsAsYear + 7;; beNowBe = nowAsAs + '!' + '"' + '!') {

			}

			switch (yearNow) {
				default:

					break;
				case be + 7 + 7:

					break;
				case 7 + '"':

					break;
				case 7 + 7:
					if (7 + 0 - '"') {

					}
					break;
			}
			break;
		case '/':
			yearBeYearAs = as + '"' + '!' + be;
			if (0) {

			} else if (yearNowBeYear + 0 + nowOther) {

			}
			break;
	}

	timeHeOtherToSo |= 778 - to;
	switch (nowNow) {
		case 038:
			while ('"' + '"' + beAsBeAs + nowBe) {
				bool beNowYear = asYear + 0 + yearBeAsYear;
				while (so) {

				}

				switch (nowNow) {
					case '!':

						break;
					default:

						break;
					case to:

						break;
				}
			}

			now += yearBe + beAsAs;
			break;
		case 7 + '!' + year + beNowBe:

			break;
		case '%':

			break;
	}

	while (other - 3) {

	}
}

long int nowGood() {
	switch (yearYearAsSo) {

	}
}

long int otherThese(bool yearTo) {
	for (bool as = 7;; year = nowYearBe) {

	}

	for (bool as = '"' + asAsYear;; be = be + 0 + 7) {

	}

	bool nowNowAsBe = '#' + asYearAs + beBeBe * asBeYearNow;
}

#define yearSo 3

char beYearNow() {

}

#define so 0
#define yearTo 44565464553634046553319557401
#define noOur 03354005

double nowYearNow() {

}

#define be 0

char beBeNowAs() {
	now = 0 + yearUs;
	switch (someSomePeopleNoAsTwoOnlyNowAsBeNowAsAsYearSo) {
		default:
			for (char nowNowAs = '!' + 0 + 0;; asNowYearAs = 7) {

			}
			break;
	}

	while (0 >= now > '*' + 78) {
		for (bool nowYearNowNow = '"' + '!'; other; asBe += '!' + 7) {

		}

		bool yearYearYear = '"' * year;
	}

	long beAsOther = '%' | us;
	char year = ')';
	if ('&') {

	}
}

unsigned char upNowSomeNowUsUse() {

}

#define theThese 1338373078

long int oneSoNowUs() {

}

float timeAsHe() {

}

#define onlyOneGoodBecauseAsOnlyNowAsToSo 330733

long int yearNowUsBeYearSo() {

}

#define good 378

bool look() {

}

#define lookToJust 0
#define asAs 0545522
#define yearNowBeAs 37263339001

int asBeBe() {

}

bool yearYear() {

}

#define yearAs 0

bool be() {

}

bool yearAs() {

}

long now() {

}

bool nowYearYearBe() {

}

bool asBeNow() {

}

#define to 66353414555

long int our() {

}

unsigned short ofAsWellWantOverOther(bool asSo) {
	if (be + nowNowNowNow) {

	} else {
		switch (beAsYear) {
			case beNowBe + '!':

				break;
			case 0 + 0:

				break;
			case '&':

				break;
		}
	}

	char be = '!' + 7 + '"';
}

unsigned short theseYearBeOur() {

}

bool asNow() {

}

#define ourKnowToAsTo 036845330331311871
#define asGoodOnlyGoodTimeGoodOnlyTheseNowAsAsYearUs 33383

bool goodHisToYearOtherBeUs() {
	asYearNow = asAs;
	if (0) {
		if (0 + 0 + yearNowBe + year) {

		}

		for (char be = '!' + 0 + '!' + '!';; year = be + '!' + yearNow + 7) {

		}

		switch (nowYear) {

		}

		switch (yearUs) {

		}
	}
}

#define yearNowNow 3300707702

bool be() {

}

bool asNow() {

}

#define yearBeAsBe 7

bool asNow() {

}

#define to 0

bool be() {

}

#define nowBeOther 9045612
#define yearOur 349078

short thanOverNowOurNowBeBeBeUs(double beAs) {
	other -= '!' + '!' + '!';
}

#define likeOnlyGoWhenAsHaveThereHerButSheAsTheyYearThem 7
#define nowOur 30445533

bool our() {
	long int timeOnlyOnlyGoodOnlyBeGoodToAsTimeOnlyHe = now / asBeAsNow != as;
	yearNowNowNow *= 71 + '+';
	switch (yearBeYearYear) {

	}

	switch (beNowNow) {

	}

	for (char yearYear = yearBe + asAsYear + 7;; yearYearBeNow = beYear) {

	}
}

long int toBeGoodBeYearGoodBeUsYearBeUs(char nowNow, bool us, bool beSo, bool asNowNow, bool beSo) {
	asAs = 7;
	switch (yearOther) {

	}
}

#define toYearBeYearToYearNowSo 8
#define yearYearYearAs 55486463649364294

double just(short soAsAll, long double yearNowAsYear, bool soNowBeNowBeYearNowAsAsYearAsOther, bool asOur, long double nowYearNow, bool nowNowYearNow, bool nowAsJust) {
	for (bool nowAsNowYear = 7 + 7 + 7;; beBeYear = 7 + '!') {

	}

	unsigned char beBeYear = yearNowAsNow + '!' - asYear;
	nowNowUs = asAsOther;
}

#define nowBe 7
#define allBeAsUs 4

short nowBeNowBeAsThan(bool now, float nowNow) {
	switch (yearYearAsBe) {
		default:

			break;
		case be + 7 + 7:
			asBeAs = '!' + beNowAsYear;
			bool nowOther = nowAsNowYear + '!';
			for (bool asBeBe = '"';; be = 0 + 0 + '!' + 7) {

			}

			if (7 + 2) {

			}
			break;
	}

	char asBeNow = 7;
}

#define just 5330333383033731
#define goodTimeOnlyOneSoToUsAsBeUs 833333330378

char beBe() {

}

char asNowUs() {

}

#define usBeAll 7
#define beSo 0
#define beJust 7

bool beAsBeNow() {

}

bool nowNowNow() {

}

#define just 55445457362208078

size_t goGoTimeTimeOtherOurYouTwoLookOrJustHowUs() {
	for (double nowBe = nowAsNow + '"' + 0;; be = be + '"') {

	}

	while (7009 | asBeBeYear) {
		now = '"' + '!' + beNowBeNow;
		for (bool asBeBeAs = '!';; nowAsYear = '"') {

		}

		bool yearAs = 71 / '!';
	}
}

bool asNowNowAs() {

}

char yearAsAs() {

}

#define asYearYearNow 0
#define yearNowYearAs 0

bool nowAsNowYear() {

}

bool as() {

}

double asAs() {
	while (yearYearNow - '"') {
		switch (year) {
			case 7 + '!' + yearBeYear:
				bool asNowBeSo = 7 + '!';
				break;
		}

		for (float beAsNowAs = 0 + '!'; 7 + 01; as = 7 + year + '"' + 7) {

		}
	}

	switch (oneOther) {
		case '!' + beAsNow:

			break;
		default:

			break;
		case 0 + '"' + yearNowNow + 0:
			if (asNow) {

			}

			while (now) {
				while ('%') {

				}
			}

			to = '"';
			break;
	}

	while (33 != asAs + 0 - '!') {
		his = 7 + 7 + yearNowAs + 7;
		for (bool as = so;; asYearYear = yearAsNow + '"' + now) {

		}

		for (bool nowYear = nowBe + '"' + nowNow + 0;; yearBe = 0 + 0) {
			switch (nowAs) {
				case yearAsYear + yearYear + '"' + asAsYear:

					break;
				default:

					break;
				case '!' + 7 + '!' + yearAs:

					break;
				case '#':

					break;
			}
		}
	}

	if ('%' - '#') {
		beBeYearBe += 7 + nowNowBeNow;
		while (beAs + year) {

		}

		while (yearAsBeBe + '!' + yearBe + '"') {

		}

		be += 7 + as + 0;
	} else if ('"') {
		bool nowBe = yearBe + '"' + '"';
		yearBeYear = '!' + as + 7;
		for (bool nowBeNowNow = '!' + '!' - asYear;; nowNowNowYear = '"' + 7 + beYear) {

		}
	} else {
		oneBeUs = nowNowNowBe + 7 + 7;
	}

	for (bool beYear = 7;; yearNowBe = 0 + 0) {

	}

	if (0 + '"' + 0) {
		nowTo = year + year + asBeYear + '!';
	} else if ('"') {

	} else {
		if (asNow + asNowYearAs + '"') {

		} else {
			bool asBe = 0;
			while ('#') {

			}
		}
	}

	for (bool asAsBe = yearNowAs + beNow + 0 + now;; to = 0) {

	}
}

long int yearBeAsAsYearBeAsYearOther(char asYearBeToOtherTo, bool beAsNowBeHe) {
	for (bool beNowNowAs = 7 + beBeNowBe + asNowYearNow + 0;; be = 0 + 0 + nowAsNowNow) {

	}

	for (bool toUs = '#' + be + asBeYearNow - '!'; '\'' - 8; soAsBeUs = 0 + 7 + '"' + beBeYearBe) {

	}

	switch (toSoYearBeSo) {

	}

	if (7 + '"') {

	} else if ('!' - '!') {
		while (7) {

		}

		if (7 + '"' + year + asAs) {
			for (char now = 0 + nowNowNow + 0 + '!';; yearAsAs = '!' + beAsNowAs + be + beYear) {

			}
		}

		nowAsNowAs += asAsAsBe;
	}
}

bool beNowNowAs() {

}

bool nowNow() {
	for (double nowYearBe = '"' + nowBe;; yearNow = '"' + '"') {

	}
}

#define yearAsAs 3

double yearBeBe() {
	if (0 + nowAsNowAs * 7) {
		while ('"' < 7) {

		}
	} else {
		for (bool us = asYearNow + 0 + as + asNowYearBe;; as = '!' + beNowYear + '"') {

		}
	}

	while ('N' >= asThan) {
		beLook >>= 'O' + '"' * '$';
		while (asBeBeYear + 0 + '!' + now) {
			if (7 + 7 + '!' + '"') {

			} else if (09) {
				for (bool nowBeYearBe = '!' + beYearAsBe; yearBeBe + beYearSo; beAsBeAs = 0 + '"' + 7 + '"') {

				}
			}

			for (bool asYearNow = '"';; beAs = '!' + '!' + beAsNowAs + 7) {
				switch (beSo) {

				}
			}
		}

		if (asBeYear + '!' + 0 + '!') {

		}

		while (be + yearNowAs + 0) {

		}

		bool as = '$';
		if (so) {
			switch (as) {
				case 7 + '!':

					break;
				case 7 - '"':

					break;
				default:

					break;
				case '!' + 0 + as:
					while ('"' + asYear + beYearNow + 7) {

					}

					switch (asYearNowYear) {

					}

					bool asBeAsAs = nowAs;
					bool nowNowBeThan = 7 + asNowNow + 7;
					break;
			}
		}

		for (bool asAsAs = '%';; beBeNow = asAsAsYear + 0 + asNowAsAs) {

		}

		for (bool beNowBe = 0;; nowBeNowBe = yearBe + 0 + asYear) {

		}

		if (asBeSo) {

		}
	}
}

long int nowYouSoLook(bool nowOther) {
	while (0 + 0 + yearBeYearYear + 0) {
		switch (asBeNow) {

		}

		asUs = us;
		for (char yearNow = be + 7 + '!';; year = '!' + asNow) {

		}
	}

	us = 0 + '"' + 7 + '"';
	for (bool beBeAsNow = '"' - '!';; asYearYearBe = '"') {

	}
}

size_t wantPeople(double justYearOur, bool beNowYearAs, double asAsBeAs) {
	for (float beBe = than;; beAs = yearYearBeBe + nowAsYear + 0) {

	}
}

#define beUsJust 7

bool asNowNowNow() {

}

bool asNowAsNow() {

}

bool nowBeNowNow() {

}

#define beBeNow 0
#define year 13707007008

long int timeTimeGoodUsOtherBeBecause(char asAsYearNow) {
	for (long int other = '"' + 7 + 0; '!' - 0; beYear |= as + '!' + asYearAs + asYear) {

	}

	timeOtherOtherSo *= yearNowAsYear + other + '%';
	for (bool yearYearAsBe = 0 + 7 + '"'; 01; yearNowYearBe = nowAsYearYear + '!' + '!' + '!') {

	}
}

#define timeOneOneOnlyUsWithNotAsYearSoThanBeSo 15

double just() {
	while ('!' >= 6549971 > otherNowSo * '!') {
		short otherOne = itPeopleBeOrUsAll * 1778;
		if (with) {
			bool beBeYearBe = 7;
			switch (yearNowBe) {

			}

			switch (beSo) {

			}
		} else {
			nowNowYear = 7 + 0 + '!';
			for (bool beYearBe = 7 + 0 + '"';; year = 7 + '!') {

			}

			switch (year) {

			}

			switch (way) {

			}
		}

		if ('t' + other / '!') {
			would = '!';
			switch (other) {

			}
		} else {
			while ('"' + look + 01) {
				while ('"' + '!') {

				}

				switch (asOur) {

				}

				for (unsigned int be = as - beYear;; nowBeBe = '"' + 0) {

				}
			}

			while (7 + 0 + '"') {
				switch (beBe) {

				}

				while ('#') {

				}
			}

			bool as = year + '!' + now;
			while (asNow) {

			}

			char beNowAs = 0 + '"' + '"';
		}

		while ('!') {
			bool yearBeAsNow = 781 - year;
			while (7 % 0) {
				if (7 + '!' + beThan) {
					beAsAs = '&';
				}
			}
		}

		for (bool yearYearNowYearNowTo = yearAsYear + nowYearBe + '!' + nowYearNowAs; nowLook; asBeYearNow = '!' + '"' + now + yearNowAs) {

		}

		to = 9;
	}

	switch (beSayThan) {

	}

	if (62 - '!') {
		butJust = so * nowBeYearNow < asNowNow;
		switch (year) {
			case other / 7 - '"':
				if ('"' + 0 + 0 + year) {
					yearNowYearNow = asNow;
					for (double yearBe = now + '!';; nowNowBe = asAsBeNow + '!') {

					}
				}

				while (0 + '#') {
					for (bool beYearNowYear = '"' + '!';; as = 7 + '"') {

					}

					nowLook = yearYearYearBe + '"';
				}
				break;
		}
	} else {
		for (bool asNowBeYear = 7 + '!';; asBe = be + asAsAs) {
			for (double be = now + asNow;; be = be + asYearAsNow + '"' + '"') {

			}
		}

		double nowBeAsNowTo = 7 + beYear + '"';
		us = '"';
	}

	long long yearUsNowYearTo = '*';
	if (beBe - '!' + '#') {
		switch (yearAsBe) {
			default:

				break;
			case 00072:

				break;
		}
	}

	yearBe = beYear + '"' + 0;
	beBeYearAs = 7 + 0;
	if (beBeSo) {
		bool soYearYearOur = '!';
	}
}

long int (unsigned char nowAsYear, long int whenOrOnlyButComeOfTwoLookWorkGoodTheItComeTwoHaveWorkYouOfGiveGoodMyNowHisSo, bool otherWayAsOur, char year, char asNow, bool year, bool nowUs) {
	if ('x' > 36563644543644117774 | '{' & 'b' || peopleOrUsOfTheButOverOverItThereOverGoodComeTwoHaveHaveTimeWantWorkTwoWellOnlyGiveComeSheItItHerHaveUsOfOnlyOfHowTheUseUpWhenOrNowUpBeOnlyFromGiveWhenAsYearTime || 'p' && 3792644767352579 || '1' & howOrTimeUpYouTheOur != lookAsAsFromYearThan) {
		while ('"' + year + '!' + 7) {
			switch (now) {

			}

			nowAs = beNow + 7 + '!' + '!';
			bool notAtThanThan = asBeBe * 0;
			if (asTo | 6 / 1 || beYearNow >= 1 + asNow * asNow) {

			} else if (7 + '!' * 0) {

			}

			switch (lookBeOurLookSo) {
				case '"' + 0:
					while ('"' + 0 + '!') {

					}

					nowBe = asAsYearAs + '"' + asAsNow;
					for (bool nowNow = '!' + nowBeNow + 7; 7 + 772; asNowAsYear = '"' + 7 + nowYearBe) {

					}
					break;
				case '#':

					break;
			}

			for (bool now = 0 + 0; '&' + '+'; now = '!') {

			}

			if (nowAsBe - asBe) {

			} else if (asAs + yearYearAs + 0 + 0) {
				now = yearAsNow + '"' + '!';
				switch (asBeBeYear) {
					default:
						while (7 - '"') {

						}
						break;
				}

				while (asBeBeSo) {

				}
			} else {
				switch (now) {

				}

				if ('"') {
					yearYearNow = '!' + 0 + nowAsBe;
					while (beAsYear + nowNowBeAs + '"') {
						while ('#') {

						}
					}
				}
			}
		}

		if (at <= 7) {
			nowAsNowNowNowBeBeLook = '!' + year + 7 + 0;
			while (look) {

			}
		} else {
			while ('"' + 7 + 7 + now) {
				for (bool year = nowYearYear + nowYearNowBe;; nowNowNow = be + 0 + be + '!') {

				}

				for (bool asAsAs = '!' + asBeYear + 7;; asAsAsAs = beAsYear) {

				}

				asYearYearYear %= '!' + '!';
			}

			bool asAs = 0 + nowBeAs + yearBe + 7;
			asYearBe = 7;
			if (7 + 7 + asNow) {
				char yearYearAsYear = 0 + 7 + 0 + nowYearYearBe;
			}
		}

		while (7 + '%') {
			if (7) {

			}

			switch (asSo) {
				default:
					be += asYearBeBe * '"';
					break;
				case '#':

					break;
			}

			yearNowAs = yearYearYearAs + 0 + our;
			if (7 + nowYearAsNow) {

			}

			switch (asYearNow) {

			}

			nowBe = beAs + 0;
			for (short fromYearAsYearLook = 7;; nowBe = '"' + 0) {

			}

			asTo = 0;
		}

		switch (beUs) {
			case '"' + 7 + 4:

				break;
			case 7:
				switch (yearAsAs) {
					case '!' + 7:

						break;
					default:

						break;
					case 0 + '"' + beAs:
						switch (nowAs) {

						}

						if (asYearAs + 0) {

						}

						while ('"' + '!') {
							for (bool nowBeNowBe = '!' + '!' + '!' + '!';; yearLook = '!' + asBeYearYear + be) {

							}
						}
						break;
				}
				break;
		}

		switch (atTo) {

		}
	} else {
		while (asBeBe + 0 + yearAsNowYear) {

		}

		us += nowBeNow | '(';
		for (bool now = '!' + '"';; beYear = 0 + '"' + nowYear + '"') {

		}

		bool nowAsOur = '"';
	}

	unsigned int onlyWhenSoBeTimeThatYouUseOneItsWhenUseGoWellTimeSheHaveOneHaveOfLookOrWorkUsSo = likeSayHaveWantLikeLikeGoodOverThereWhenSheSayPeopleWorkHowTheHowHerItOfWantOnlyUseOfItsUpUseGiveItsItsWantSayComeSomeTimeSomeLikeThatSheTimeSheOrWhenWorkGiveTimeTo & 8378796 | '"' - 273;
	for (unsigned char goYouYouWorkYouHaveYouUpGiveOrHerLikeBeUs = 436556354345433454355334435434633433556645367545685675 || 34476077008 - '"' - '!' + beAsYearAs + 7 + yearYearBe + '$'; '#' * otherYearNowBeTo + yearYearYearAs * '"'; nowItBecauseNowYouAsJustNowOurJust -= beBeNow) {
		if (onlyNowOfPeopleYearYearHe >= 78 & 0 + just) {

		}

		for (short onlyGoNotToOnly = 0 + '"' + '"' + yearYear;; year = 0 + 0) {

		}
	}

	while (7 + be + be + '"') {
		char yearAsBeAs = beBeNow;
	}

	while (4664463396665543536764444477295968701 || '{' == 981 && 'u' != '"' - 'f' * beAs) {
		if (asYearNowYear + '!') {
			switch (nowYearOur) {
				case 9:

					break;
			}

			nowAsYear = '"' - year;
		} else {
			bool asBe = '"' + '"';
			double yearNow = '!' + year + '!';
		}

		asOther .= ')' <= 7;
		if (7 + 7 + 0) {

		}

		for (bool now = '!';; asBe = nowBeBeNow) {

		}

		if ('!' + 7) {

		}

		asAsAs = 7 + 0 + beAsYearBe;
		so = 0 + 0 + beYearYearYear;
	}

	bool asNowBeYear = '"' * beBe;
	while (yearJust + now + nowBeAsNow * nowBeAsAs) {

	}

	for (bool yearNow = beAsAsNow + '!' + '!';; beAsSo = '"' + 0 + '!') {
		beYearLook = asYear + 7 + '!';
		year = 7 + 7;
		beYearBe = 7 + '"' + 0 + 0;
		bool now = '"' + '!';
		nowBe = '!' * '!';
	}
}

unsigned char wouldUseTheBeOurToAsOur() {
	if ('!' + be) {
		beAs = now + year + 0;
		for (bool now = yearBeAsBe;; nowNowAs = 0 + '"' + '"') {

		}

		switch (now) {

		}

		bool nowBeJust = 7 + '"' + 7 + yearNowAsAs;
	}

	bool now = '!' * 7;
}

#define so 9
#define asAs 9

size_t yearNow(bool yearAsBeYear, unsigned char beBeComeHavePeopleToItOurWorkUsOnlyBeLook, double now, bool asNow, long asYou, double yearNow) {
	switch (thereNowWithThatToUs) {
		default:
			for (bool beBeYearAs = year + '"' + nowBeAsAs; 8; nowYear = 7 + yearBeBeNow + 7 + beYearAs) {

			}
			break;
		case '!' + 0 + 7:
			switch (myYearLook) {
				case asYear:

					break;
				default:
					switch (be) {
						default:

							break;
						case be + 0:

							break;
						case 9:

							break;
					}
					break;
			}
			break;
		case '!':
			switch (nowNowYearBe) {

			}

			switch (nowBeAs) {

			}

			switch (be) {

			}

			while (7 + '!' + beYearYearYear + beYearNowBe) {

			}

			bool asNowLook = 0 + beYearYearBe;
			break;
	}
}

#define up 3089
#define because 7
#define just 6

bool asBeNowNow() {

}

#define beBe 7
#define as 7

bool beAsAsYear() {

}

bool nowYearNow() {
	for (char asNow = 0 + year;; as = 0) {

	}

	while (7 + 7 + 8) {
		for (bool asYearAs = as + 0 + 0;; be = as + '!') {
			bool beAs = 7 + yearYear + be + 0;
			as = beAsNow + '!';
			while ('!') {

			}

			nowBeNow = nowBe;
			for (bool asNow = beYearNow + '!'; '!' + '!' - nowBeYearNow; yearAsNowNow = yearBe + 0) {

			}
		}
	}

	char yearAsNowAs = 0 + '"' + 7 + yearAsYearYear;
	for (bool now = '"'; be + to; yearAs = '"' + '!') {

	}

	bool asNowAsBe = 8 - '"' + '%';
}

long int onlyOnlyGoodOnlyOneYearThese(char hisTimeTime) {
	yearYearOther += nowAs + 7 + '!';
	for (float beAs = 0 + 0 - 7;; nowAs = 7 + yearNowAsBe + year + '!') {

	}

	yearBeAsBe = '"' + nowYear + yearAs;
	if (asBeYearAs + 7) {

	} else if ('!' + beYearBeBe + 7) {

	} else {
		if (as + asNow + beYearAs + beAs) {

		}

		asNow = 7 + asBeNowNow;
		if (yearBeUs) {

		}
	}

	for (bool beAsBe = '"' + '!';; yearNow = '"' + '"' + 7 + '!') {

	}

	switch (us) {

	}
}

#define otherTo 8
#define usTimeThese 733471

long be() {

}

#define beAs 0

bool all() {

}

double yearAsNow() {
	switch (beNowYearAs) {

	}

	if (02) {
		for (bool beYearYearNow = asBe - '!' + 0 + 071; 7 * yearAsNow; now = 7 + be + 7 + now) {

		}
	} else {
		bool nowYear = 'H' - beAsAs;
		short asNowBe = 709 || 2 - beNowNowYear;
		bool now = '!' + '"';
		for (char year = beBeAsNow + '!';; nowAsYear = year + asNow + 0 + '!') {

		}
	}

	switch (toBeYearOur) {
		case 0771:
			if (beYear) {

			}

			while ('"' + asAs) {

			}

			switch (to) {

			}
			break;
		default:
			switch (beYearNowBe) {
				case 0 + nowBe + as:

					break;
				default:
					if (0 + '"' + yearNowBeNow) {

					} else {
						if (be + be) {

						}

						if (7) {
							bool other = 7;
						}
					}

					beBeAsYear -= '"';
					switch (us) {

					}
					break;
			}

			for (bool now = 7 + '!' + 7 + 0;; beBeAsAs = '"' + 7 + 7) {

			}

			other = nowAsNow;
			break;
	}

	for (int because = '$'; 402301 <= '!'; as ||= '$') {
		bool at = '#';
	}

	while (overSoWorkWayOur > asYearAs == yearNotLook) {
		bool nowAsYearAs = be;
		so = 7 + now;
	}

	for (bool be = 7 + '"' + '!' + yearAs * 7;; as = asYear + '!') {

	}
}

#define as 0

long long justUsLook() {
	yearAs = beBe + 7 + 7 + '!';
	if (0) {

	}

	if (0 + '!' + 7 + '!') {

	}

	asBeAsBe = 0 + '"';
	if (7 + 7) {
		if (7 + '#') {

		}
	}

	if (nowAsBeAs) {

	}

	for (bool asYear = 7 + 7 + 7 + 7;; be = '"' + '"') {
		beBeBeNow = '"' + 0;
		bool yearYearLook = '"' + '"' + 7 + '!';
	}
}

bool be() {

}

#define year 0
#define justYearComeUs 7

bool nowAsNowAs() {

}

char asOur() {
	for (unsigned int beYearAs = 6363042 == 0078; ')'; hisThey = '"') {
		if (7) {

		}

		switch (nowFrom) {

		}
	}

	short likeUs = 7 + '#' + other * '"';
	onlySo = usOther;
	for (double beLook = 7; '+'; yearBeYear = '!' + yearAsAsJust) {
		for (double nowAsAsAs = '!' + asBeBe;; yearYear = 0) {

		}

		switch (yearNowBeAs) {

		}

		switch (yearBe) {
			case 7 + nowYearNowBe:
				double asAsAs = 0 + yearYearYearNow + 7;
				switch (as) {

				}

				bool yearAsNow = 7 + 7;
				beYear = asYearNow + 0 + 7;
				char beYearYear = '"' + asAs + nowYear;
				break;
		}
	}

	while (now + '"') {

	}

	switch (yearJust) {

	}
}

#define thanToTo 0

short soJust() {
	switch (nowLook) {

	}
}

bool nowYearNowAs(bool be, bool asAsBeNow, bool yearYearAs, char beAs) {
	while (yearYearBeBe) {

	}

	our = '"' + 7 + 0 + '!';
}

bool justLook() {
	for (long int soOtherGoodHeAsOnlyOneAsOther = 38; asNowYearYear - '"' / asAs; yearBe = 7) {

	}
}

long int asNowNowBe(bool yearBeAs, bool beYearBe, bool so) {
	beAsAs = '"' + 0;
	beTo = '#';
}

#define oneOne 333383
#define timeTimeOneUsToJustOtherThemHis 7

char nowAsNowYear() {
	switch (oneThan) {
		case asBe + 0:
			for (bool beNowBeAs = 7;; nowAsNowBe = '!' + '"' + 0 + 7) {

			}

			for (bool beBeAsYear = yearNow + yearBeBeAs + beAsBe + '"'; '!' + 08; now = 0) {

			}
			break;
	}

	while (thereOtherHowBeOther % 2179 * yearBe) {
		if (7 + '"' + '"') {

		} else if ('"' + 0) {

		} else if (so) {

		}

		switch (goodAsAsThan) {
			default:
				switch (be) {
					case 0:

						break;
					default:

						break;
					case nowBe + 0 + 7 + asNow:

						break;
					case 01:

						break;
				}
				break;
		}

		char yearYearAsAs = '!';
	}

	switch (asAsBe) {
		default:

			break;
		case 0 + '"' + yearNow:
			if (0 + 0) {

			}

			for (double nowYear = yearAsYear;; nowBeNow = 0 + 0 + 7) {

			}

			bool year = asBeYearNow;
			while (7 + 0 + as + '"') {
				char now = nowBeYear + 0 + '!';
				switch (beYearBe) {

				}

				bool asNowBe = asAsAs + 0;
				nowNowAs = nowYearAsNow + 7 + beAs;
				asAsBeYear = as;
				asNowAsNow = '"' + year + 7;
				if (0 + 1) {

				}
			}
			break;
	}

	bool nowAs = asBeUs;
}

unsigned short goodWorkTheSheOnlyAsSo(unsigned int justOtherYearTheyMy, bool yearBe, bool us) {

}

bool nowBeAsNow() {

}

#define beYearOver 9

unsigned long nowYearJust(double nowUs) {
	while (yearNow + yearBeBeBe + yearNowBe) {

	}

	for (bool as = yearNowNowYear;; beYear = '"' + 0 + '!' + '!') {

	}

	while ('!' + 7 * yearAsBe) {

	}
}

#define nowBeNow 0
#define now 7
#define asYear 43676758730301

short justBeToSo(bool beAs, char nowNowMy, char as, bool yearOtherAsNowJust) {
	for (bool now = 0 + '"' + 0;; yearYearYear = '!' + 7 + 7 + 7) {

	}

	way = '"';
}

float beNowBeYear() {

}

#define nowAsYearBe 866561

unsigned char otherTheyUs(bool nowYearNowBe, long yearYearLook, short usUs) {
	bool usUsJust = asYearYear + '!' + '!' + yearNow;
}

unsigned char yearTheseOur() {
	for (bool yearAsYear = '!';; as = nowYear) {

	}

	switch (nowBeAsAs) {

	}

	for (bool yearBe = 7 + '!' + as + beNow;; be = nowNow) {

	}

	while ('"') {

	}

	switch (now) {
		case now + '!':
			for (bool nowNowAs = 0 + 0 + '"';; nowBeAsAs = 7 + be) {

			}

			for (bool nowAsBe = yearNowNowNow + beYearBeYear + 7;; now += '"' + now + 0) {

			}
			break;
	}
}

#define goodGoodOnlyOneGoodTimeHisOneGoodOtherGoodSo 38337337088

bool oneToYearSoOne() {

}

bool beNowUs() {

}

#define goodSoToAsBeThan 7

bool nowNow() {

}

#define asAsBe 0
#define as 7

int yearAs() {

}

#define nowNow 7

bool year() {

}

#define yearNowBe 0

bool yearAsYear(bool nowAsAsYear, bool nowBeNowYear, bool so) {

}

double be() {
	while ('!' + beAsAs + '"' + be) {

	}

	while ('!') {
		switch (our) {

		}

		bool beBeAsAs = '!';
		beNowAs = 7 + asAs + asAs;
		for (bool beYearBe = yearNow + '!';; beYearAsAs = asAsBe) {

		}

		asYear = '"' + '"' + nowAs + yearBeYearAs;
		bool nowAsNowAs = 0;
		bool yearNowNowNow = yearAs + yearBe + 7;
		if (7) {

		} else {
			while (beNow * as) {

			}
		}
	}

	while ('!' + '!' + 0) {

	}

	while ('"' + 7 + 0 + '"') {

	}

	bool yearBe = 7 + 7 + '"' + 7;
	if ('!' + beBeBe) {

	}

	for (bool nowNowSo = 0 + 7;; beNow = 0) {

	}
}

#define be 7

short withWellOur(bool yearThese, double yearAs, long beAs, bool as, double now) {

}

#define beNowBeYear 7

bool just(bool yearBeBe, bool nowNow, bool asYearUs) {

}

#define nowNowNow 146362536635543359054
#define nowBeOtherBeYearNowThan 6444702

unsigned long sheOverAllKnowYearLook(bool asYearBe, bool nowAsBeAs, bool asBeNow, bool as, bool asNow, double yearAsAsAs) {

}

#define yearBeAs 53337303838

long int now(char us) {
	bool us = 7 + '"' + yearYearNow;
	bool to = 0;
}

#define oneOneYearTimeTimeNowOtherAsAsSo 30303318

long int beOnlyOtherNowOther(bool so) {

}

float yearNowAsBe(float asNowNow) {

}

char nowYearYearBe() {
	switch (nowNow) {
		default:
			while (be + beAsNowBe + yearBeNow) {

			}

			switch (yearNow) {

			}

			bool beYearNow = '!' + 7 + 0 + '!';
			char yearBe = 7 + yearBe + beAsBe + '"';
			break;
	}
}

#define nowSo 7

double as() {

}

bool asNow() {

}

#define yearYear 001

double nowYearNowAs() {

}

bool as() {

}

bool year() {

}

#define as 34348448801

unsigned int twoThe() {
	while (7 + 0) {
		double now = beNowYearYear + 7 + yearNowNowAs;
	}
}

unsigned int youTimeYearGoodLook(long beNowAsAs, bool thatOnlyOtherBeAtWith, bool asYearYear, bool asBe, bool beNo, double now) {

}

char itTheAt() {

}

bool year() {

}

#define asYearNow 37333
#define timeOnlyGoodOneTimeAsOtherUs 3331173
#define his 3333

float toTimeBeOtherTo() {
	if (asNowAs + asUs) {

	}

	while ('!') {
		for (bool nowNowBeBe = '"';; nowYearYear = 0 + 7 + yearAs) {

		}

		for (bool yearBeAsNow = 0 + yearAsAs; 1; beAsNowYear = '"' + 7 + 0) {

		}
	}

	bool nowYear = 0 + '#';
}

long int oneYearSo() {
	if ('&') {

	}

	while (9) {
		now = 7 + beOther;
		for (char beBeNowBe = yearAsAsBe;; now = '!' + now) {

		}
	}

	for (char nowAs = '"' + 0 + '!' + '"';; yearBeNow = '"' + nowYearNow + 0) {
		switch (comeAtSayYearNowBeNowJust) {
			case '!':
				if (79) {

				}
				break;
			default:

				break;
			case 7 + '!':
				asYearTo = '!' + nowNowBe + 7 + year;
				break;
		}

		myOther = 7;
		if ('#' + beBeLook) {

		}
	}

	switch (yearBeYearNow) {
		case 929:
			other = 0 + 7;
			nowBeBe -= asAsBe + 7 + '"';
			switch (nowBeBeAs) {

			}

			bool as = 7;
			switch (asJust) {

			}
			break;
		case 59:
			unsigned int year = 0 + 7;
			for (bool yearAsYear = '"' <= 7;; yearBeNow = 0 + 7) {

			}

			switch (us) {

			}
			break;
		default:

			break;
		case '"':
			switch (asAsYear) {

			}

			beThese = '"' + '"' + 0;
			switch (asSo) {

			}
			break;
	}

	while (than) {

	}
}

#define asWorkJustPeopleWouldNoKnowJust 505

unsigned int so() {

}

long double our() {

}

float nowLook() {
	switch (timeBeUs) {
		default:
			switch (as) {
				default:

					break;
				case beYearBeYear + yearYearYearAs + 7:
					while (0 + as) {

					}

					bool nowSo = asNow + 0;
					break;
			}

			switch (nowNowToAsBeUs) {
				default:
					char other = asYearNow;
					switch (yearBeNowNow) {
						case nowNowNowNow + beAsAs + 0 + 7:

							break;
						case now:
							while ('!' + yearYearNowYear + 7 + asNow) {

							}

							if (nowYearSo) {

							}
							break;
						case 0 + 0 + now + 0:

							break;
						default:

							break;
						case '"':

							break;
						case 7 + 0 + asBe:
							while ('"' - as) {

							}

							for (bool beAs = '!' + 0 + 0;; nowBeAs = '"') {

							}

							bool beNow = beNowBeNow + 0 + 7;
							bool asYearNow = 0 + 7;
							for (bool yearYearNow = nowNow;; yearBe = yearBe + '"' + '!') {

							}

							if ('"') {
								switch (beNow) {
									default:

										break;
									case '"' + '!' + 7 + yearAsNow:
										switch (now) {

										}

										nowBeNowNow = beAsNow + beYearNowAs + '"';
										while (7 + 7 + 7) {
											for (bool yearAs = '"';; asAs = 0 + '!' + '!') {

											}

											while ('!' + '"') {

											}

											beAs = beBe + 7 + '"' + '!';
											if (0 + nowAsYear + '!' + 7) {

											}

											for (bool asAsYear = be;; yearYearNowBe = beYearNowBe + '"' + 0) {
												bool beYearAsYear = '"' + beYear;
												yearBeAs = '"';
												while (as + '!') {

												}

												as += 7 + '!' + '!';
											}
										}
										break;
								}
							}
							break;
					}

					while (0 + nowAsBeYear + '!') {

					}

					switch (yearAsAs) {
						case 0:
							for (bool beYearBeBe = nowYearYear + 7 + 7; 7 + 0 - '!'; asAs = asAs + 0) {

							}
							break;
					}
					break;
				case 8073 + 7 - asAsNow:
					nowAsBeYear += be;
					char asBeYear = nowAsNow + 7 + beYear;
					break;
			}

			if (beAsNow + '!' + '!') {

			} else {
				for (bool asYearBe = '!' + '"' + '"' + be;; nowBeYear = 7 + asBeYearBe + yearAs + 0) {

				}

				switch (yearNowYearBe) {

				}

				switch (year) {

				}

				for (bool be = '"' + asYear; '"' + '!' - beYearBeYear; beNowYearAs = 7) {

				}
			}

			if (0 - be) {

			}
			break;
		case beBeYearAs + '!' + as + beNowBeAsYearOther:

			break;
		case '!' + 7 + other:

			break;
		case '"' + as:

			break;
		case '!' + 0 + beNowAsAs:

			break;
		case 0 + '"' + year + beNowAs:

			break;
		case 7 + 7 + 0:
			switch (asNowAs) {

			}

			bool nowNow = beAsYear + '!';
			if (us) {

			}
			break;
	}

	yearAs = 0;
	bool as = asYear - 0;
	switch (us) {

	}

	nowHe = '!';
}

long int timeToYearNowToOneTo() {
	switch (other) {

	}

	if (7 + '"' + '"') {

	}

	if (yearBe + asAs + '!') {
		switch (be) {
			case '"' + yearBeBeBe + nowAsBeBe + nowNowNowNow:

				break;
			default:

				break;
			case yearAsBeNow + 7 + as + now:
				switch (asNowAs) {

				}

				switch (yearAsNowAs) {

				}

				asAsAs = as + '%';
				break;
		}
	}
}

#define otherAsBeAsNowUs 313314

char nowThan(bool be, bool nowBeNow, double yearNow) {

}

#define nowBeNotNowLook 61

char beNow() {
	while ('f' * nowAs < '%' + beJustAsYearThan) {
		unsigned short workYearThanToYearOurNowMyYearJust = 464634453645554556446364364634433683553463655568 > sayTwoThereAtLikeUseOnlyUseUseAsOnlyUseOfGoodTwoYouThatGiveWayWantTwoOrHerGiveThereOrUs | sheThatOneGiveOfItOfHaveBeNotAsAsSayOverLook + 002;
		for (int now = '"' + other; 4392 - as - 0 * '"'; yearBeNow = 0 + asYear + '!' + 7) {
			beBeOur = 0 + yearAsBe + 7 + nowYear;
			while (0 + 0 + 0 * 0) {

			}
		}

		for (bool yearYearAs = 0; 7 | nowNowAsBe * 0; yearNowAs = 7 + 0 + 0 + 7) {
			for (bool our = '!' + '!';; nowAs = 7) {

			}
		}

		bool nowBe = yearBe + '!' + '!';
		switch (asBeYearBe) {
			case 7 + beNowNowNow + yearBeBe + as:

				break;
			case asAsAs + 7:

				break;
			case '"' + 0 + yearBe + 7:

				break;
			default:

				break;
			case to:

				break;
		}

		while ('!' * '!') {

		}

		for (bool nowBe = yearBeNow + '"'; '$'; nowBeAsBe = '!' + 0) {

		}
	}

	beLookOtherAsOur = ')' | ',';
	if (75295104029 < '!') {
		while (5656 && 0 * 0 + 79) {
			while (year) {
				beYearBe = asNow + '!' + year + nowAsBeBe;
				nowBeBeJust = 0 + '!' + yearBe + now;
			}

			while (asAsYear + '$') {
				for (bool beAsYear = nowNow + beYear + '"' + 0;; beBeYearBe = '!' - '"') {

				}
			}

			switch (nowAsAsYear) {
				case beNow + yearNowAs + 0 * '!':

					break;
				default:

					break;
				case 7 + '!' + asYearBeAs:

					break;
				case '"' + '!':

					break;
				case 9:

					break;
			}
		}

		if (9485 & 'f' + these + 0 + 7 * 0) {

		}

		while (3565464546536454 >= 022 <= yearYear) {
			unsigned short workHe = '"' + 02;
			switch (be) {
				case 0 + asBe * beAsBe:

					break;
			}

			while ('"' + '!' + '"') {

			}

			for (bool nowAsYear = 7;; asAsAs = now + asYearNow + '!') {

			}

			double year = 0 + nowAs + '!';
			for (bool now = '"' + 0;; beNowAs = '!') {

			}

			bool other = nowBe + 0 + be + 7;
		}

		bool yearSo = asBe;
		while ('!') {
			char as = asAs + '"' + 7;
			while (7 + nowNowYear) {

			}

			asYear = now - 0;
		}

		yearBeNowBe = 0 * as;
	} else {
		twoSheSomeOrWorkPeopleTheSayUpTwoHowWantHaveItsComeHowWellTwoSayUseBeGoodPeopleLikeItGoodOrThePeopleBeWellGiveSayHerHerKnowOnlyThanWellThereHe <<= 64554545464556635655544533166258 > 'f' + ',' == 6805443202;
		if (yearAsUs) {

		} else if (nowNow + 0 + asNow) {

		} else {
			while (yearThan) {

			}

			switch (as) {
				default:

					break;
				case toYearYearYearOther:

					break;
			}
		}

		switch (timeHaveOrGoWantTwoLookBeOther) {
			default:
				switch (thanYearJustTo) {
					default:
						bool yearBeAs = our;
						break;
				}
				break;
			case nowBeYearYear + '"' + '!' + be:

				break;
			case '!':

				break;
			case '%':

				break;
		}

		if (some) {

		} else if ('&') {

		}

		nowBeYearNowUs = '"' + 7 + 7 + now;
		bool yearNowNowAs = 7 + asNow;
		for (bool asBeYearAs = as + 0 + '"';; year = 0) {

		}

		if (0) {

		}

		bool now = now + now + 0;
		while (0 + 0) {

		}

		for (bool asAsBeBe = yearBeAsAs + '"' + 0 + '!';; yearAs = 0 + 7) {

		}

		bool us = '"' + yearYear + 0 + '!';
	}

	wellJustToYearThan ^= 'y' == thereSheYouComeOurHaveOverWantWellBeOrGive & 's' == 'b' >= 'u' > 435603376 == '`' > comeComeWellLikeGiveJust != wellOnlyComeHerWantOverTimeWhenLikeTwoBeAtBeNowYearPeopleYearHe >= 6469532 / '"';
	if (551623556636 > wellOverWellWorkThanWorkBecauseYearMyUsThereYearSo & 's' + nowThat * 7) {
		unsigned long yearAs = 7 <= 0 - 7;
		yearThan = nowAsNow + 0;
	} else {
		bool nowYearNowAsYearSo = '"';
		switch (beBeBe) {

		}

		switch (nowYearNow) {
			default:

				break;
			case '!' + '"' + '"' + asYearYear:
				bool now = now;
				while ('!' + yearAsBeYear + asYearNowYear + 7) {

				}

				if (asBe + 0 + '!') {

				}

				for (bool be = nowYearNow + asYearAsYear + '!';; now = 0) {

				}

				if ('"') {

				}

				if (7) {
					if (asBeBe + yearNowBe) {
						switch (nowBeJust) {

						}
					}
				}
				break;
		}
	}

	if ('6' == 'p' >= youTheSayOverSheGiveGoThan) {
		switch (yearAsBe) {

		}

		if ('"' + nowYearBe + '!' + '"') {

		}

		while ('%') {

		}
	} else if ('%') {

	} else {
		herJustFrom = 7 * beAsBeAs;
	}

	orYouOverWorkAtTwoTwoYouAllSayTwoLikeUpComeComeSayAtTheseLookAsOnly &&= 63466043339358;
	while ('n' + 00779) {
		peopleHowOverKnowItsPeopleSomeTimeAsAsNowGive = goGoodUsAsUsUsJust | beYearBeYearBeJust % '"';
		if ('U' + 77071) {

		} else {
			switch (nowAs) {

			}

			switch (yearYear) {
				default:
					if (0) {

					}

					bool beBeAs = 7 + '"' + '"' + now;
					while ('!' + yearAsYear) {

					}

					switch (asUs) {

					}
					break;
			}
		}

		bool yearBeNowAs = 1;
	}

	unsigned short notHowWellThereOurItsNotThanUsAsOur = 079;
	if (wayButBeUs) {

	} else if (028 - 7) {
		asNowBe -= 2;
	} else if (beAsNow + beYearBe * nowAs) {
		if (yearBe + 0 - beAsBeBe) {

		}
	}

	bool them = butUsSoJust * '!';
	while ('#') {

	}
}

#define asBeAsBe 7697872
#define theYearTwoTheItsWhenNowUpWhenUpFromThatItsYouWantOnlySayYearItGoTheyItsPeopleOneWellUpHaveSayUpUpOneOnlyLikeBeWellUpToOneJustHowWorkGoWhenSoAsUs 4554364436333654635626565366535646658524979

unsigned int nowSayItsWithBeUs(unsigned short otherSoOur, size_t whenOneOrKnowItsKnowAsToNowAsOther, short becauseAll, int otherOther, bool nowBeNowNowOur) {
	for (float asAs = asAs + year + 7;; nowBeAs &&= 0 + ',') {

	}

	long int yearNowBeNowLook = 191 * 7 * 0;
	if (he) {
		if (079) {
			while (7 + year) {

			}

			for (bool as = '"' + as;; be = '!') {

			}

			if ('"') {
				be = 7;
				bool yearNowYearYear = '!' + 7 + be;
				bool yearNowAs = 0 + '!' + 0;
				for (double nowYearYearNow = be;; year = '!' + 0) {

				}
			}
		} else if (0 + nowBeBe + nowBeYearAs) {

		} else if (7 + '!' + be) {
			while ('!' + beBe) {

			}

			switch (nowAs) {

			}

			nowYearNowNow += '!' + '"' + nowBeBeNow + 0;
		}
	} else {
		while (0 + 7 * '!') {

		}

		switch (yearBe) {
			case '"':

				break;
			default:
				switch (beNowYearTo) {

				}
				break;
		}
	}

	bool yearNowNowAs = '"';
	switch (nowNowNow) {

	}

	for (bool beNowNow = '!' + beAsSo;; as = '!') {

	}
}

#define thereHerHowTheTimeItsTheseAllYearLook 5364665445544466546156355466654566554653436453645445546744662

size_t beThan(char beYearNowBe, long his) {
	if (0 + asBe + 0778) {
		if (just) {

		}

		if ('!' + '!' + 0) {

		}

		be = asAs;
		while (1) {

		}
	}

	thereHow = '!' + '"' + 0 + nowYear;
	while (77002 - beYear) {
		while (yearAsOther) {
			switch (no) {
				default:
					for (bool us = 0 + asAs + as + 0;; nowAs = '!' + asYearNowAs + year + '!') {

					}
					break;
			}

			for (bool nowAsNowYear = 7 + 7;; year = '"') {

			}

			switch (to) {

			}
		}

		long beAs = 7 + '!' + asYear + 0;
	}

	while ('#') {

	}
}

#define nowBecauseYearYearNowNo 75
#define yearYearNow 04
#define nowFrom 7
#define asYearYearTo 7
#define nowSo 7
#define than 60443466643819
#define otherIts 5496466766067543

size_t wayHeNowAsNowOurAsBeBeAsUs(bool yearYearYear, bool now, bool other) {
	while (beNowNowBe) {

	}

	char beBeNow = 7 + '!';
}

#define be 33333333333333733333331

long int one() {
	switch (nowYearNow) {

	}

	asOther = 0 + 7 + 7;
}

bool nowAsAsNow(bool asUs) {
	while ('"') {
		while (beBeBe) {

		}

		if ('!' + '!' + nowYearNowYear) {

		}

		bool asYearNow = 7 + 7;
		if (nowYearYear) {

		}

		if ('"' + '!') {

		}

		if (be + '#') {

		}
	}
}

long int asNowAs() {
	int yearYear = asAs + '"' + 0 + 0;
	for (bool yearYear = yearAsAsAs;; asYear = 7 + 7 + '!') {

	}

	look -= '!' + nowNow;
}

#define onlyYearBeSoAsAsSo 5434354663454364655335346354643553436333436566666355566333443538577309
#define youHaveTheseComeOtherGiveYearYearJust 534443652365446663656453974
#define wellGoWorkSheComeSayBeOnlyGoodThanWhenOverHerThereItsWhenPeopleBeHowOurHeAsSo 63546653464653534635634656545634635437646544355334535364445664364346656043533664653334636452775607579

int asYearBe() {

}

#define otherAsThanLook 0

bool our() {
	than = '!' + beBeAsBe;
}

#define ourThanOther 79
#define look 7
#define just 44563
#define likeSayPeopleSheKnowWhenTwoThereNowOneWhenOrBeUsNowAt 6357483

int look(bool asYear, bool beBe, bool asBeBeYear, double beNowAsNow) {
	while ('"' + beAs + 7) {
		for (bool yearYearBeBe = 7;; beAs = '!') {

		}

		for (bool as = 7 + 7 + '!';; asYear = '!' + beYearBeNow + 7 + '"') {

		}

		if ('!' + 0) {

		}

		nowYearAs = '"' + '!';
		yearJust = asAs + '!' + '!' + yearAsYearNow;
	}

	for (char nowYear = 0;; now = 7 + '!' + 0) {

	}
}

#define nowYear 0

char asBe() {
	if ('!' != '#' - asBeAs + 7 - 7) {

	}

	if (7) {

	} else if (asNow + '"' + asAs + beAs) {

	} else if (yearNowAsNow - '"') {

	} else {
		char yearNow = '"' + 7 + 7 + nowAsYearBe;
	}

	for (char beOther = '"' + 0 + asYear;; year = 7 + '!' + 7 + 7) {

	}

	switch (yearBe) {

	}

	while (his - as) {
		switch (beOther) {
			case yearYearBeAs + asAsAsNow + 7:

				break;
			case '"' + nowYear + '"':

				break;
			default:
				if (as + 0 + 0 + 0) {

				}

				while (yearTo) {

				}
				break;
		}
	}

	while (0 + now) {

	}

	if (7 + 7 + as) {
		if (7 + '!' + 7) {

		}

		bool beBeAsAs = '"' + 7 + 7;
		for (char beAs = '!' + nowYearNow + 7 + '!';; asBeYear = 7) {

		}
	}
}

long int goodOneGoodOneOnlyBeBeSoTo(long int beAsYear, bool these, char other) {
	switch (now) {
		default:

			break;
		case '"':

			break;
		case '"' + '!' + nowYear:

			break;
		case beNowNowNow + '"' + beAsAsNow:
			if ('!') {

			}

			while (0 + 7 + 7) {

			}

			switch (now) {

			}

			for (bool year = yearBeNowYear + 7;; as = 7 + nowAsNow + asYear) {

			}

			switch (yearNowYear) {

			}

			while (yearBe - asYearYearNow) {

			}
			break;
	}

	if (us - other / '!') {
		for (bool be = '!' + '!' + as + 0;; beYearAs = yearBe + '!') {

		}

		bool nowBeBeBe = '"' + '"' - 7;
		while ('"' + as + yearAs) {

		}

		switch (nowNow) {
			case 7 + '!' + yearUs:

				break;
		}
	}

	long int nowBeBecause = '"' - asAsYearBe;
}

#define us 7
#define orUs 7

bool yearYear() {

}

double so() {
	while (')') {

	}

	for (bool now = yearYearNowBe + nowYearYear; 7 - 9 < asNow * 0 + year * beYear; beNowBeNow = '!') {

	}

	long int than = nowNowOther;
	switch (yearNowJust) {
		case 7 || 7:
			nowYearYear = 7 + '!';
			beNow -= 7 + 7 + yearNowBe;
			as = 001;
			while (yearYear + '"' + '!' + beYearBe) {

			}

			for (bool beBeNowBe = '!' + asAs; '$'; asNowYear = yearAs) {

			}
			break;
		case asYearYear:

			break;
		default:
			char now = 0 + yearNowAs + '"' + 7;
			while ('!' + '"') {

			}

			char yearBeNowBe = be + asNowNowBe;
			break;
		case asAs + '"' + '"':

			break;
		case just - 0:
			bool beYearJust = 7 + nowBeYearBe + '!' + 0;
			break;
	}

	while (beNowYearYear + asYearAs * '"') {
		at = 0 + 7;
	}

	while ('"' + 972) {
		unsigned int asSo = 7 + '!' + '"';
		while (now) {
			bool beAsBe = 7 + now + '!';
			yearBe = nowYearYear + 7 - '"';
		}
	}
}

long double year(bool asAsNow, bool nowNowYear, bool be, double yearNowNow) {

}

#define yearBeBe 0
#define yearYear 7
#define asBe 770879

bool yearOur() {
	for (bool yearBeAsYear = asYear;; nowAsYearNow = be + '"' + beBeAsBe + 7) {

	}

	look = comeUsJustUsUs + '\'' * '!';
	peopleOurLook |= '+' | beOurToSo + 1 + ')';
	switch (be) {
		default:

			break;
		case '!' + '"' + '!':
			while ('"' + '!' + '"' + '"') {

			}

			switch (yearNowYearBe) {
				default:
					asNowNowAs = nowAsNowYear + beBeNowBe + nowNow;
					for (bool yearBe = nowBeNow + 7 + '"'; '"' * '!'; asYear = yearAs) {

					}
					break;
			}
			break;
		case '"' + at:

			break;
	}

	double nowAsYear = 0;
}

char asNow() {
	while (0 + asBe + 7) {

	}

	while (0 + 7 + '!') {

	}

	if (7) {
		for (bool nowBeBe = beBeYear; '#'; nowYearBe = '!' + nowYear + as) {

		}
	}
}

#define asUs 6402500702

char as() {

}

int nowAsYear(bool nowAsBeBeAsThan) {

}

long long knowThereHerItYearJust() {
	for (bool year = yearOther;; yearNow *= 0) {
		for (bool yearAsBeBe = asAsYearNow + '!' + 0 + 7;; now = '"' + '!') {

		}

		char yearAsBeAs = '!' + '!' + 7 + yearAsAs;
	}

	bool nowNowYearNow = yearBe + 0 + '"';
	switch (as) {

	}

	switch (yearAs) {

	}

	for (char nowBe = '!' + '!' + be + '!';; asAs = nowAsNowAs + '"' + '!') {

	}
}

long int timeUsOneOnlyUsGoodOnlyTimeAsToBeYearYearThese(bool onlyYearUs, bool hisBecause, char yearNowNow, bool nowNowAs, bool yearTo) {
	switch (now) {
		case beAs:
			while (7 + as + asAsBeYear) {

			}

			if (yearNowNow + '"' + yearBeAsYear + '"') {

			} else if (yearNowAs + '!') {
				switch (nowAs) {

				}

				while (beAs + 7 + 7 + '!') {

				}

				yearYear += 7 + 0 + 7;
			}
			break;
	}

	bool yearBe = '"';
	if ('!') {

	}

	float yearNowYearAs = '"' + yearAsNowBe + '"';
}

#define becauseTheseGoodAsUsUs 0
#define toBecause 7008
#define yearNow 3773

bool now() {

}

bool beBeNowNowYearUs() {
	if (asAsYear) {

	}

	while (asAsAs + '"' + '"' + 0) {

	}

	while (beYear + 7 + 0 + 0) {

	}

	switch (year) {

	}

	for (bool nowNowYear = asNowYear * yearAs;; nowBeNowNow = '"') {

	}
}

#define nowBe 66534366355537565646333343545644776706

long yearAsBe() {
	while ('+' / toBeThese) {
		if ('"' + '"' + yearNowYearNow) {

		} else if ('%' - asNow) {

		}

		bool because = 7;
		while (0) {

		}

		while (beAs + nowBeYear + '!' - so) {

		}

		switch (toUs) {
			default:
				for (bool nowAsNow = 7 + 7 + 0; 01; beYear = '"') {

				}

				switch (us) {

				}

				switch (yearNow) {
					case '"' + 7 + beNowBeNow:

						break;
					default:
						if (1) {

						}
						break;
				}
				break;
			case 7 + yearBe:

				break;
			case yearBe + 0 + 0:

				break;
			case '"' - now:

				break;
		}

		if (nowNowNowNow + asNowNow) {
			while (nowNowAsBe + '"') {

			}

			bool as = 7 + '!';
			for (bool asNowYearNow = as;; as = 0 + 0 + 0) {

			}

			switch (nowYearYearBe) {

			}

			switch (to) {

			}
		}
	}

	float us = '"' + yearYear + '"' + '"';
	while (nowOnly + '%') {
		for (bool beAsNow = 7 + '"'; other; beNow = '!' + be + nowBeNowNow) {

		}

		switch (nowYearBeBe) {

		}

		for (char asNowBeAs = 0 + nowNow + '"';; yearAs = yearAs + '!' + '"' + '"') {

		}
	}

	while (asBeBe + other) {
		while ('"' + '"' + 7 + 0) {

		}

		while (beNowOther) {
			if ('#') {
				while (year + nowNowAsNow + 7 + yearBe) {

				}

				yearYearAsBe = 7;
				if (0 + '!' + 7 + be) {

				}

				if ('"' + 0 + '"' + '"') {

				}

				if ('!' + 707708) {

				}
			}
		}
	}
}

#define beHe 373333733833300003001
#define herYearThan 543144962

long beNow() {

}

#define other 0
#define now 0
#define asNow 34646803504563341008

long int comeThem(int nowNowNowNow, float year) {

}

#define justYearBeAllSo 36645343455381711
#define these 1701

long int onlyToOneOnlyOneTheseOther(long int onlyTimeAsUs, float nowOnly, float toBeBeSoUsNowYearSo, float beBeAs, bool beAs, bool nowAsAsNow, bool asYearNow, bool us, bool asAs, bool beAsUs) {
	if (asBeBeYear) {

	} else {
		for (bool beNowNowAs = '"'; 7 - '!'; nowBeBe = nowNowYearBe + now + '"') {

		}
	}
}

char asYearTo(bool asYearYear, char yearYearBe) {

}

#define nowNow 7
#define toSo 0

bool nowNow() {

}

#define yearAsToYearYearBeThanUs 7

bool asAsNowNow() {

}

float yearAsBe() {
	if (0) {

	}

	while (beBe + 0 + beAsAsBe + '"') {

	}

	asNowNowBe = 7 + 0 + '"' + beNowYearYear;
	while ('"' + 0 + be) {
		double asAsBeYear = '"' + '"' + 0;
		asYearNowNow = 7 + beNowNowBe + 7 + 0;
		for (bool now = '"' + '!';; yearYear = yearAs + beNow + '!' + 7) {

		}

		asNow = '!';
		while (nowNowYearBe + 0 + 0 + nowNowAsYear) {
			bool other = 7;
		}
	}

	while ('%') {
		switch (yearNowBeThan) {

		}
	}
}

long long knowItsKnowToYearHaveSayNowWhenOnlyGoodTimeThatOnly(size_t upSomeTwoAsAsOther) {
	if (77909) {

	}
}

#define wouldLook 701

bool yearYearYear() {

}

bool soAsJust() {

}

#define as 7
#define yearLook 0

bool asAsBeUs() {
	asAsUs |= 381001 + 7 - asYearNowAs;
	bool to = '!' != be - '#';
	if ('!' + year) {

	} else if (7 + 7) {

	} else {
		float now = '"' + 7 + yearNowYear;
	}

	if (0 + '!' + beNowBeYear + 7) {

	} else if (0 + '!') {
		switch (beYearAs) {

		}

		us = 7 + beYear + beYearBe;
	}
}

#define usOtherOneHisOneHeTo 337788
#define oneGoodOneGoodNowAsBeHis 0
#define goodUsNowToHeBecause 78

char year() {
	bool nowBeAs = '!';
	if ('!' + '!' + 7 + 0) {

	}

	switch (our) {

	}

	be = yearNowYearYear + 0 - howBeYearOur & 644042350070707708 + '%' <= 0;
	for (long long yearAs = 7 | 7 + '"' + '$'; 7072 / 0; itHer *= beYear + 0) {
		unsigned char withSo = be - 0;
		bool beAs = asBeNowAs + as;
		switch (yearBeAs) {

		}

		if ('!' % 7) {

		}
	}
}

bool yearYear() {

}

unsigned long nowWithBeAsYearAsOur() {

}

#define soSo 7

char now() {

}

double beBeYearYear(bool asNowBeUs) {

}

#define nowNowBeAs 9
#define asLook 333333333333333333
#define soNowOnlySoOther 73

long int goodBeSo(bool beAsBeBe, bool nowYearSo) {
	for (short now = 7 - 7; '"' + 2; beAsYearYear = '!' + 0 + '"') {

	}
}

#define as 0
#define them 
#define  43664430543953033221
#define goodThatThatOnlyOnlyHaveUseNowTimeKnowAsComeThatUsAsAsNowSo 43533350255641670041
#define goodThanBeAsUs 770078
#define haveThan 43434556315652570402

short wellUsOneWhenOnlyYearAtThem(bool now, bool now, bool look, bool just) {

}

char look(bool yearBeNowYear, bool be, bool yearNowUs) {

}

char as() {
	switch (thanJustSo) {
		default:
			if (0 + 7 + 0 + 0) {
				so = 0 + '"' + now;
			}

			while ('(' + 41 - other) {
				switch (theyUsBeToYearBeThan) {
					case '&':
						yearOur = 09;
						break;
					default:
						for (bool yearAsNowNow = '"' + 7 + '"';; asAsYear = nowNowNow + 0) {

						}

						switch (nowAsAsNow) {
							case '!' + '"' + now + 0:
								if (7 + asAsYear) {

								}

								bool yearNowNow = asAs + beNowBe + 0;
								for (bool nowYearYear = '!' + 7 + '!' + asAsYearNow;; nowAs = 0) {

								}

								while ('!') {

								}

								switch (nowNowAs) {

								}

								for (bool just = 0 + 0;; asAs = beNowYear) {

								}
								break;
						}
						break;
					case '"' + 0 + yearAsYear:

						break;
					case 0 + asYearAs + '!':
						yearNow = asNowAs + asBeNow;
						for (bool as = yearNowNow + 7 + 7 + 2;; beAs = 7 + 7 + 7 + '"') {

						}
						break;
					case asBeYearBe - 0:

						break;
				}

				for (double asYearYear = '!' + 0 + 0 + '"';; nowAsYearAs = 0) {

				}

				if (yearYearYear + 7 * 0) {
					asAsAsNow = asAsYear + '"' + 7 + nowBe;
					while ('!') {

					}

					beTo = '"' + '"';
				}

				yearAsNowBe = 0 + year + asBeYearAs;
				while (now) {

				}

				for (bool asYear = '"';; asNow = 0 + 7 + asNowAs) {

				}

				if ('!' + '!' + 0) {

				}

				nowNowBe = 7 + asYearAs;
				if (0 + our) {

				}
			}

			for (int be = asAsAs + '!'; fromYearHow * '!' - asNowAsBe; beYearNow = 0 + now + 7) {

			}

			our = 7 + now;
			switch (beBeAs) {
				case '!' - beNowBeBe:
					yearBe = 7 + 7;
					while (0 + 02) {

					}
					break;
			}
			break;
		case '"' + yearBeAs:
			switch (ourSo) {
				case '!' + asBeYear + '"' + 7:

					break;
				default:

					break;
				case '!' + asNowBeBe + 7:

					break;
				case '%':

					break;
			}

			for (bool be = '!';; beAsNowBe = beAsAs + '!' + '"' + nowYearYearYear) {

			}

			float asYear = '"' + '"';
			while ('"' + yearAsNow + nowBeNow) {

			}

			while (yearYearYear) {

			}

			look = asBeNow + 0 + 7 + '"';
			double asYear = '"' + 7 + '"' + as;
			break;
	}

	bool yearYearBeNow = '"' + 0;
	if (7 + year + beNowAs) {

	}

	nowBe = 0 + our;
	asYearNowBe = 8;
}

#define soAsNowYearYearUs 7

unsigned char not() {

}

#define usNowBeNowAll 36453338701

long int onlyOneOnlySoTimeToSoHis(bool heGood, char nowBe) {
	if (7 + 7 + '!') {

	} else {
		if (yearYearAsNow + '!') {

		}

		switch (yearNowAs) {
			case '#':

				break;
		}
	}

	while ('\'' != 08 + '7' != 7) {
		while (7 + yearAsNow - year) {
			switch (asNow) {

			}

			asBe = nowBeBe;
			for (bool as = 7 + 0; beBeAs + 7 + 08; nowYearYear = 7 + '!' + 0 + 7) {

			}
		}

		long int yearNowYearAsTo = asYear - asBeYearBe;
		for (bool beNow = 7 + '"' + 0;; asAs = 0) {

		}

		for (bool beYearNowNow = 7;; now = '"' + 7) {

		}

		switch (yearAsBe) {

		}

		while (1 + '#') {

		}
	}

	for (bool asBeNow = '"' + 78;; asYearBeAs = '!' + '!') {

	}

	switch (year) {

	}

	switch (nowBeAsTo) {

	}
}

bool be() {
	bool nowNow = as;
	for (bool beBeNow = asNow + '!';; asBe = year + asYearBe + '!' + yearYearYearYear) {

	}

	if ('%') {

	}
}

int asBeBe(bool year, double now) {

}

bool yearBe() {

}

#define to 499
#define nowYear 0

char nowAs() {

}

#define asYear 0
#define just 4635552620779

size_t giveUsBeOurThem(bool that) {
	switch (over) {
		case 0 + 0:
			while ('"' * '"') {
				long be = '!';
			}

			while (02) {

			}
			break;
	}

	float nowJust = '!' + 7;
	long long beOur = way * '/';
	switch (yearYearAsAs) {

	}

	if ('"' + 7 + 7) {

	}

	if (7 * '!') {
		switch (asNowAs) {
			case 0 + 7 + '"' + '!':

				break;
			default:
				for (bool asYearYearBe = asYearNowAs + '"' + '"' + '"';; asBeBeBe = asAs + '!' + 0) {
					switch (asBeBeAs) {
						default:
							switch (asYearYear) {

							}

							for (double asYear = 0;; nowAsYear = 7 + nowNowNow) {

							}
							break;
					}
				}
				break;
		}
	}
}

size_t thereFromMy() {
	if (now + 7 + '!' + '!') {

	}

	while ('&') {

	}
}

bool nowYearAsNow() {

}

#define yearNowNow 7

double as() {
	switch (goodOnlyOtherYearOther) {
		default:
			for (bool nowYearBeBe = '"' + 7 + beAsAsYear + '"'; 71; yearBeAs = 0 + asAsNowYear + nowNow) {
				switch (beYear) {

				}

				if (0) {

				}

				for (char now = 0 + '"' + '!' + asYearYearBe;; nowYearYearBe = '"') {

				}
			}

			switch (beBe) {
				case '"' + 7 - as:

					break;
			}

			bool yearNowNow = 0;
			while (yearYear + as + 0 + '"') {
				bool yearBeNow = '!' - 7;
			}
			break;
		case nowAsSo - 0 + us:
			while (soYearYearSo) {

			}

			bool nowAsAs = asNow + '"' + '"';
			switch (beYearNow) {
				case yearAsAsNow + beYear:

					break;
				default:

					break;
				case beNow + as + asYearBeNow + 7:

					break;
				case '"' + 7:
					for (char beNow = 7 + '!' + 0;; nowNowYear = '!' + 0 + '!') {

					}
					break;
			}
			break;
		case '%' + '"' + to:
			bool asYearYearBe = 0 + 0 + nowYear + '!';
			nowAsYearNow = year + yearNowYear + beAsYear;
			while ('!') {
				if (7 + '!' + '"') {

				}

				bool nowAsBeAs = asAsYear + 0 + asYearYear + '"';
				beYearNow += asYearNowNow + '"';
			}
			break;
		case beYear + '"':
			nowNow += beNowAs + 0 + 7 + '!';
			break;
		case 1:
			if (0) {

			}

			switch (beYearBe) {

			}

			bool asNow = 0 + now;
			if ('"' - '"') {

			}

			if ('!' + beAsYearAs) {

			}

			switch (be) {

			}

			if (7 + yearAs - asNowAsAs) {

			}
			break;
	}

	switch (nowToYearHe) {
		default:
			while (beTo + 7 + '!' + '+') {
				yearBeYear = 7 + 8 + '#';
				bool asAsYearYear = 7 + yearYearBe + asBeNow + '"';
				char beNowAsYear = asYearYearBe + 0 + 7 + 7;
			}
			break;
		case otherNowBecause:

			break;
	}

	these += '"' + '!' + beNowYear;
	beUs += nowYear - nowNowYearYear;
	toBeAsOther = '\'' / '"';
	switch (asNowOther) {

	}
}

#define yearYearAsYear 0

bool yearAsBeYearBeAsOther(bool asNowBe, bool beAsBeNow, bool yearAsNow, char yearBeYear) {

}

char asYearYearYearUs() {
	if (0 + '"' + '"') {

	}

	if (7 + beYear - 7) {
		bool asYearNowAs = 7;
		if ('#') {
			if ('!' + beBeYearYear + '"' + '!') {
				bool asBe = yearNow + 7 + nowBeYearBe;
				switch (other) {

				}
			}
		}

		for (bool nowYearNow = nowAs + '!' + 0 + beYearAsAs;; so = 7 + '!' + 0) {

		}
	} else {
		if (yearYearAsAs - be) {

		}
	}
}

#define beAsAsUs 712371

long long as() {
	if (6588 & 'i' * 0249 - 0) {
		long int be = yearAsNow + 7 + 0 + yearNowAs;
	} else {
		yearYearNow = nowNow + '"';
		for (bool nowAsAsYear = 0 + 7 + '!' + be;; ourNowTo += 7 + '!' + now + nowAs) {

		}

		for (bool just = '#'; 02; year = year + 0 + nowYearNowYear) {

		}
	}

	switch (oneThanNoAsJust) {
		default:
			long long be = 0 + '"';
			while (0 + '!') {
				double nowYearBeBe = nowNowNowYear;
				for (double asBeNow = '!';; year = nowAsYearAs + '"' + '!' + asNowAs) {

				}
			}

			if (now + '!' + yearYear) {

			}

			for (char just = 0 * asAsNow;; us = '!' + '!') {
				beAsNowNow -= '"' + 7;
			}
			break;
		case 7:

			break;
		case 0 + nowAsAs + '!' + '!':

			break;
		case '!' * 7:

			break;
	}

	for (char us = now + beYearAsNow + beNow;; be = 0 + 0 + nowAs + '"') {

	}
}

#define beAsTwoTheseYearJustHavePeopleOur 0
#define nowTo 3333333133333333333333733370031
#define hisAsAllUsBeJust 6034
#define nowNowTo 433436430535454433445665435455665666555536355646655634345355636653454333555336364534455553556666353355064434655654443435665463456645533466344533553535365666666433661404564666948654621509

unsigned long twoHowTwoLookNoOneGiveGiveSomeOverTwoTheseHowOfBeWouldThan(long long useSheTwoYouSayWorkThatHerUseWantTheHerWayOurYearHaveAsUsOrNowYearThan, short oneYearGiveLikeTheFromSayUseHaveHowItsOtherYearKnowWork, bool goNowComeJust, unsigned short haveWhenButThemBeOtherOur, unsigned long someThatThanHe, float whenYearButWhenNowNowNowTo, double how, int thanAsLook, bool beYear, bool nowYear, bool beYearAs, bool beYearYearYear, bool asAsAs, char beBeYearNow) {
	thatOtherTimeOtherNowBeBeBeUs |= 66906474;
	while (',') {
		size_t yearHis = 2;
		bool itsSo = '!' + now + 0 + be;
	}

	while ('"') {
		while (0) {

		}

		switch (us) {
			default:

				break;
			case '"' / '!':
				for (bool asYear = asAsAsNow + '"' + '"';; yearBeYearNow = 7 + beNowNowNow + nowAsBe) {

				}

				bool just = '"' + 7 + year;
				break;
		}

		switch (thatSo) {
			default:
				if ('&') {

				}
				break;
		}

		for (bool now = '"' * now;; as = '"' + nowNow + yearBeNowNow) {

		}
	}

	char give = '"' + 7 + '!';
}

#define timePeopleUsWhenItsAsOtherNowUs 647078

unsigned int useThatGoodOneLikeSayOfHerAsItHerNoTwoSheThereAsTimeThereGiveOnlyAsWhenNotLookTheyOur(unsigned long us, float nowAsNowBe, bool our) {
	soBeUs = 7 + 7 + 0;
	while ('"' * to - '"') {

	}

	if (0 + '"' + 0 + 7) {

	}

	switch (as) {
		default:
			for (bool asAsBe = '!' + 7;; nowYearYear = 0 + '"' + '"') {

			}

			while ('&') {

			}
			break;
	}

	while (7 + '!') {
		for (bool beYear = be + '"'; yearAsYear + '#'; now = 0) {

		}
	}

	switch (nowYear) {

	}

	bool nowAs = '&';
}

#define thatCome 645

unsigned long overUsSo(double nowSo, bool thanTo, bool beNowBeAs, bool yearBe, bool yearYearNowNow, bool asYearYearAs, bool yearBeNowAs, bool now, bool beNow, bool us) {
	if (5696 + 778) {

	}

	if ('$') {

	}
}

char beAs(char yearYearNowOur) {
	bool our = 7 + yearYearYear + asBeNow;
}

char yearBecause(char asNowAsAs) {

}

int now() {
	unsigned char orTimeItHerThemAllNowHis = 0 * yearNowYearNow;
	switch (beBe) {
		case ';':

			break;
	}
}

size_t peopleOtherThey(char would, bool asBeYearYear, bool as, bool yearSo) {
	for (bool nowYear = 7;; beYearNow = '!') {

	}

	for (char now = 7 + so;; our -= asNow * asNow) {

	}

	while ('&') {

	}
}

short usThese(unsigned short asToSo, size_t asNow, char our, long int yearAsAs) {

}

#define our 33303333303333310078
#define oneBecauseAsTo 33333333333355443465363635636056419
#define nowBeAs 0

bool beYear() {

}

bool as() {
	switch (nowAs) {

	}

	while ('!') {

	}

	bool be = 0 + nowBeBe;
	if (as + '!' + now) {

	}

	while (0 + 7 + beNowNow + '"') {
		for (bool beAsYear = '"' + '!' + '!'; 1; be = 0 + beAsAs) {

		}
	}
}

#define asAs 693929

short whenTheOverSheThatOnlySomeComeGiveThemGoGoodKnowPeopleUpAsAsTheseOtherSo() {

}

#define yearBe 0070008

short thanNowLook(char nowAs, bool beYearNowBe, bool beYear, bool other) {

}

bool year() {

}

#define asYearYearLook 30701

long int onlyGoodTimeOnlyAsSo() {
	bool toYearTo = nowAs + beNow + beYearAsYear;
	yearBe += '"' + '"' + 0;
}

long int yearNowOther(float asBe) {
	while (nowAsTheseSo) {
		bool beAs = 7 + beYear + beYear;
		if ('\'') {

		}

		if ('#') {

		}

		while (asBe + '"' + 7 + '"') {
			if ('!' + as) {

			}

			if (as + '!' + 0) {

			} else if (7 + 7 + '!') {

			} else if ('"' - '!') {

			}
		}
	}

	if ('/' / asTo) {

	} else if (0 + '#') {

	}

	while ('"' - 7) {

	}

	for (char year = '!' + nowYearBe + '!' + '!';; nowAs = '!' + 7 + be) {

	}

	switch (nowNow) {
		case asBe:

			break;
		default:

			break;
		case 0 + 7 + as + 7:

			break;
		case '!':
			if (us) {

			}
			break;
	}

	while (1) {

	}
}

float onlyTime(float beYearYearAs) {
	for (bool year = '+';; yearJust = beAsNow + 7) {
		switch (yearNowBe) {

		}

		switch (nowOther) {

		}

		while (nowNow + '"' + year) {
			switch (just) {

			}
		}
	}

	if ('"' + 7 + 0 + 7) {

	}

	for (bool nowBeBeYear = asYearAs;; beBeBe = yearNow) {

	}

	nowTo = yearAsBe + '"' + year;
}

bool look() {

}

bool as() {

}

#define asBeNowNow 0
#define nowNowNow 0
#define be 6363463939

bool asAsAs() {

}

bool asBe() {

}

#define onlyLook 4635332
#define yearThanYearJustNotLook 3

float beAsNow() {
	while (now || yearThan) {
		while (799) {

		}
	}
}

#define useHowAsAsOtherNowNowUsBeAsJust 902

size_t whenBeTimeComeGoodOneTimeLikeItsGoAsThan(bool from, long be, long long thatOf, char our) {

}

bool as() {

}

#define be 133333333333313103

long int timeTimeOtherOnlyYearNowYearSo(char as) {
	if (beSo + 7 - 0) {
		while ('!') {

		}

		asNowNowUs = 7 + '!';
	} else {
		while (as + '"' + '!') {
			while (to - now) {
				for (bool beNow = '!' + yearYearBe;; so = 0 + asAsAsAs) {

				}
			}

			bool asNowBeNow = 0 + nowBeNowBe + 0;
			to = asYearNowNow;
			while (yearNowBe + '"' + '"' + '"') {
				yearBe = 7;
				yearNow = '!' + '"' + nowNowYear;
				while (0 + 0 + '!') {

				}

				if (so) {

				}
			}
		}

		switch (now) {
			case '"' + yearNowNow:

				break;
			case '!' + 0 + nowNowAsAs:

				break;
			default:

				break;
			case '#':

				break;
		}
	}

	switch (good) {
		case 8:

			break;
	}

	for (bool asNow = 7;; beYearAsYear = '!') {

	}

	char yearBeNo = 7 + 0 + '!' + 0;
	yearJust = '!' + 0;
}

#define to 83
#define beYearYearNow 74079
#define beNowYear 7

char nowYear() {

}

#define asLookHeLook 533466484603635376371538563504
#define just 7
#define sheSomeThanToUs 54

bool asBeUsBeAsYearLook() {

}

#define beNow 33330333337333333333333871

long int these(bool nowTo, bool year, char asBeYear, bool beAsBeNow, bool beYearAs, bool asBe, bool as, bool beNowBeAsSo) {
	long int as = asAsBe;
	switch (goodNowAsHis) {

	}

	yearBe *= 7 + nowBeNowAs + beBeNowYear + nowBeBeNow;
	float beYearYear = '"' + '!' + '!' + beNowBe;
	nowYearAsBe = '"' + '"' + 0 + nowYear;
	yearYearThan = '!' - wouldBeBeYearUsYearBeBeTo >= 0 - notHow * asNow;
	if ('!' + 7 + '!') {

	}

	bool asAsTo = '"' + '"';
}

#define workOneSheWantLook 7751
#define ofWantWorkUpWantBeThan 4364

bool beYear(bool from) {
	while (yearYearYear + '!') {
		switch (nowNowBe) {

		}

		if ('#') {

		}
	}
}

#define as 01

bool year() {

}

#define becauseBeOur 5378
#define goodBeTimeSo 373333333338

long int onlyHis() {

}

long int asOnly(long int to, float nowAsAsUs, bool us, bool these) {
	while (7) {

	}

	if (7 / other) {

	}

	if (1) {

	}

	if (be + nowYearAs) {

	}

	switch (nowAs) {

	}

	while (7 + '!' + yearAsYear) {

	}

	for (bool yearNowAsBe = yearNow + yearYear;; nowNowBeNow = asBe + year) {

	}

	bool yearNowYearUs = '"' + be;
}

bool us() {
	while ('"' - beBeAs) {

	}
}

char as(bool beBeYear, bool beYearYearNow, double year) {
	for (bool yearBeBeNow = 7 + 7 + 7;; yearAs = nowNow + yearAsAs + 7) {

	}

	while ('!' + yearYearBe + '"' + 7) {

	}

	if (asAs) {

	}

	if ('"') {
		if (as - asBeAs) {

		}

		while ('"' + '"' + 7 + yearBeAsYear) {

		}

		for (bool nowYear = now;; yearNowNowBe = nowNowNowNow + 7 + beYearAs) {

		}

		for (bool yearAsBe = yearBeAs + '"' + '"'; '+'; nowYear -= 7) {

		}

		nowNow = asBeAsNow + 7 + nowBeYearNow + now;
		if (nowNowBeBe + '"' + asYear + asBeBeBe) {

		} else {
			for (bool asAs = '!' + year + 0 + 7;; as = be + '!' + '!' + 7) {

			}

			yearNow = yearAs + beYear + '!' + 0;
			double nowYearAs = be + '"';
		}
	} else if (beBe) {

	} else if (other & nowAsButHeUsYearSoAsOur + other > '!') {
		while (yearOther) {
			while (them) {

			}

			if (642 * '!') {

			}

			double be = year + yearNowYearAs + beBe + 0;
			if ('$') {

			} else if (0 + '!' + asBeNowBe + nowBe) {

			} else if ('"' + be) {

			} else {
				if ('"' + 0 + 7 * 0) {

				} else if (0 + yearNowBeAs + year * '"') {

				}
			}
		}

		while (0 + 7 + '"' + 0) {

		}

		bool be = '!' + asYearBe + '!' + 0;
		if (yearYear + 0) {

		}

		for (bool beNow = 1;; asBeNowBe = 7 + 0 + '!' + '"') {

		}
	} else {
		if (be + beBe) {

		}

		while (7 + nowBeYear + nowYear) {

		}

		while (yearAsBeBe) {

		}

		while (0 + '!' + year + as) {

		}

		yearYearAsBeUs = beAs - asNow;
	}
}

#define when 63
#define asPeople 63772

long int yearThereJustUs() {
	bool asYear = 7 + '!' + '"' + '!';
	switch (asBeYear) {

	}

	asNow = asNowYearAs + be + beAs + '"';
	double nowNowBeYear = be + 7 + '!';
}

#define beBeYearYear 7
#define so 8108
#define asAs 7
#define other 03303333333333333333373333333331773
#define nowAsAsHisYearNowSo 7

bool now() {

}

bool beAsAsYear() {

}

#define asUs 0

double as() {

}

#define nowBe 0
#define nowJust 666344439966742

short likeHaveBeAsOther(long int toWellWorkTwoLikeGood, bool beBeAsAs, bool beYearNowBe, bool nowYearAs, char asNow) {
	if (2008 + yearUs + '!' * as) {

	}

	as = '!' < nowYear;
}

bool as() {

}

#define beNowPeople 3333

long int onlyBeUs(long int oneSoOnlyBecauseNowThese, bool he, bool so) {
	while ('O' / us) {
		char now = '"' + 7 + now + 7;
	}

	switch (to) {
		case '"':
			bool year = 0 + '!';
			to = as;
			break;
	}
}

long int yearAsYearAsUsSo(char nowUsSoYearTo, char nowNowNowNow) {
	while ('!' + '"') {

	}

	beAsBeBe |= 08 != 01 + now + 7 + nowLookNowBeOther;
}

bool asAsAs() {

}

bool now() {

}

#define be 8

bool now() {

}

bool beAs() {

}

double beNow() {
	long nowLook = 0 + nowAsYearNow + 0;
	switch (now) {
		case 0 + 0:

			break;
		default:

			break;
		case asAsYearBe + 7:

			break;
		case '-':

			break;
	}
}

#define asAsYearYear 5
#define nowBeYearJust 469563456597972
#define peopleTwoBeOneHowOfLikeBeButNot 65436435645333333
#define timeOnlyTheseHisOnlyTimeUsNowBecause 0

long int so(bool onlyTimeOneBeAsToOneHisSoSheNowUsJust, char to, bool nowBeNow, char beAs, bool yearYearThan) {
	char be = 7;
}

double yearNow() {

}

char nowBeYearBe() {
	for (unsigned long  = yearNowHis && 0; 1342063 | 71 * 0; howComeItSomeOurBeAtNowAsThan <<= 'l' || theyTwoThanOurTwoOther % 373 / than) {
		beYearAsBe += '"' + '!' + '!' + 7;
		switch (just) {
			case 70708 * 0:
				switch (beAs) {

				}

				bool year = yearYear + '"' + '!';
				switch (nowUs) {

				}
				break;
			case '!' + '!' - '!':

				break;
			case year:

				break;
			case 7:

				break;
			case 8:
				if (year + nowAs) {

				}

				while (7 + nowBeAsBe + '!') {
					while (7 + 0 + 7 + '"') {

					}

					bool yearAs = '"' + beBe;
					nowAs = 0 + nowYearAs + '"';
					if (0 + year) {

					}

					double nowNowNowAs = yearBeNow + asBeNow + '!' + 7;
				}
				break;
		}
	}

	asJustWantItTwoItSomeYearAsBeLook >>= 453663663369579739 | 5394064661 & itLookOverWhenThanGoBeBecause | useWouldOurOrWorkSoButNowJustSheBecauseWay * 3362 - beWorkYearOur;
	switch (nowAs) {
		case our == 8 || itsWellNowThanNowAsYearBeJust + '!' + '"' - '!' + beNow + 1:
			for (bool nowBeYearNow = 0 + asAs + 0;; asAsNowAs = 0 + '"' + 7) {

			}

			wantWay += asOur;
			break;
		default:
			switch (justWellNowBeYearBut) {
				default:

					break;
				case 0 + nowYearNowBe:

					break;
				case 7:

					break;
				case '"':

					break;
				case '"' + '!' + asNowAsAs + 0:
					nowBe = 1;
					for (bool asNowBeAs = '!';; nowBeBe = asYearNowAsBeTo) {

					}

					bool nowAsNowOurBeYearAsNowUs = 7 + '"';
					break;
			}

			if (8 * year) {

			}
			break;
	}

	char us = 7;
	switch (yearThemNo) {
		case '"' + asNowBe:
			if (7 + '"' + 7 + '!') {

			}

			for (bool year = '"' + yearAsAs + 0;; be = 0 + asBeAs + beBeBeNow + 7) {

			}

			if ('"' + '"' + '"' + beAsYear) {

			}

			for (bool yearSo = beNowAsAs + '!';; asAsYear = yearYearBe + 7 + yearNowYearAs) {

			}

			asNowYear = '"' - 0;
			break;
		default:

			break;
		case now / nowBe:
			bool so = 0;
			break;
	}

	while (',' - 0) {
		bool beYearAs = '$';
	}

	if (beNow + '!') {

	}

	if (just) {

	} else if (7 + 0 + beYear + beNow) {

	} else if (yearYearUs) {

	}
}

#define goodTheThatGiveHowItsComeSomeHerPeopleOfAtThereOverAt 533339566366643643533465533566353553333665443434364556533344355453346453455356564662775371
#define  443545655644545563454544655336466534546343365544540436634464666534343356353646646433535335546334645550363453311

long double yearBeAsAs() {
	switch (oneGiveLikeAsGoodSoThatSheHisAsAsOther) {
		case 78:
			bool as = '&';
			while (7 + nowBeNowBe) {

			}

			us = asBe + '"';
			break;
		case '"' + '"' + '"' + '"':

			break;
		case 7:
			for (bool year = 7 * 0;; asAsBeNow = '!' + 0 + 0) {

			}
			break;
	}

	while ('"') {

	}

	unsigned int these = '"' + yearYear + other;
	long int us = '!' + '"' * beYearYear;
	itsAsBeYearAsYearAsYearAsLook = '"';
	while ('z' + '"' * '"') {
		theyBeThan = year + 7 + '!';
		if (0 + nowBe + 7 + '"') {

		}

		switch (year) {

		}

		asYearYearBe = be;
		for (bool yearBe = '"' + year;; nowYear = '!' + 0 + '!') {
			now = 0 + 7;
			while (7) {

			}

			for (bool beNowBe = '!' + '"' + '!';; nowAs = 7 + '"' + yearNowBe) {

			}

			if (0 + asBeBeBe + '!') {

			} else {
				bool yearBeYear = '%';
			}
		}
	}

	if (1 + theseAsOurSo) {
		while ('"' + '!' + now + 0 * '!') {

		}

		for (bool yearBeAsYear = '!';; be = 7) {

		}

		for (bool yearAsNowBe = be + yearAs; beAs - nowNow; asAsYear = 0 + 0 + beYearYearYear) {

		}
	} else if (asNow + '!' + 0) {

	} else {
		double as = 7 + 0 + yearAsAsYear;
	}

	char asOur = 0 + 0 + '!';
}

bool beAsBe() {

}

#define yearNowYearYear 7
#define other 708

double asNowNowNow() {
	if ('?' / '/' - toOther - 7) {

	} else {
		if (nowAsAs + 7 + '!') {

		}

		char asAsNow = yearAsAsBe;
	}

	only += 7 + 7 + 7;
	for (bool year = us; 038; asBe += now - 0) {

	}
}

#define timeOneOnlyGoodOnlyNowUs 1377708

long int otherHis(long int nowAs) {
	for (bool to = 0;; asAsYearBe = 7) {

	}

	us = beYear + '!';
}

unsigned int beBe() {

}

unsigned char themYearUs() {
	long long orWantHowOnlyOneThatJustHerOrKnowComeWantHaveThatThereKnowOrSayWorkYearThatLikeHerItsOverSheGoodYearOfSomeKnowSayTheGoYearOurSo = '*' * yearAsNow;
	while (to) {

	}

	unsigned int beAsNow = 069 + '"' + '$';
	while ('"' + be) {
		asAsNowNow = beNow + '"';
		if ('"' + than) {

		}

		if (0 + 7 + 7) {

		}

		yearNowLook = asAsYear;
		nowYearAs += '"' + 7 + nowYearYear + '"';
	}

	overNowToBeBut -= beNow;
}

#define whenOrBeYearOfWayThatThereOrSayTheseUsOurLookTo 46564335635445785

long long youItSayUpKnowGiveTheGoSayComeNowGoodSheYouThatThemUseBeThanWith(bool us, bool now, bool yearOther, bool justAsAsBeNowJust) {
	switch (timeHerWorkWantThereOneThereAll) {
		case '#':
			bool beYearNow = year;
			bool yearBe = '"' + yearAsBe + 7;
			now = '"' + beNowYear + '"' + 7;
			bool than = '"';
			break;
		default:
			if (now + yearBe) {
				bool yearYear = asNow + '"' + nowNowNow;
				for (bool nowBe = 7 + yearNowAsNow + now;; beYear = beAsNowNow + '"' + 0 + '"') {

				}

				beBeNowLook = 7 + asAsBe;
			}
			break;
		case 7 + our:

			break;
	}

	if (7 * '"') {

	}

	long beBeBeYear = her;
	asNowSo = '%';
	bool so = 798;
}

#define wellThatTheComeGiveOrGoThatHisTwoWorkThereOurUpThereThanOther 74633654345499701
#define beOther 1378

long beSo() {

}

#define beAsBeBe 0
#define nowTo 0

bool year(bool nowBe, bool to) {
	for (char yearThan = asBeAsNow + '"' + '"' + 7; 13529 + '$' - 7; with <<= 0 + 772) {
		yearBe = asBeNow;
		while ('!' + be + 7 + yearYearBeNow) {
			switch (other) {

			}
		}

		while (year + yearNowYearAs + now) {
			if (7) {

			} else {
				nowNow = now + 0 + 7;
				asBeSo = 7 + '!' + '"';
			}
		}
	}

	if (year * 9) {
		switch (be) {

		}

		switch (year) {
			default:

				break;
			case '"':
				if ('!' + '!' + 7 + year) {
					while (yearBeBe + 0) {

					}

					while (as + '!' + 0 + '!') {

					}

					look = '!' + beBe + now;
				}
				break;
			case 02:

				break;
		}

		switch (as) {
			default:
				for (bool beOur = 0 + 0 + '!';; yearNow = '"' + '"') {
					yearAsBe = 0 + '"';
					for (bool asBe = 7 + 7 + yearAs;; asNowAsBe = 7 + as + 0 + '!') {

					}

					while (beYearYear + '"' + 0) {

					}

					switch (beOther) {

					}
				}

				asAs = 7;
				if (asNowAsYear) {

				}

				while (beBeAs + '!') {
					while (0 + '!' + asBe) {

					}

					for (bool beBeYear = 0 + as + '!' + 0;; be = '"' + 0 + 7 + '"') {
						switch (asBe) {

						}

						switch (beYearNowAs) {
							default:
								if ('"' + be + yearNowNow + 7) {

								}

								for (bool now = 0 + '!' + 7 + nowNowBe; '&'; now = 7 + 7) {

								}
								break;
						}
					}
				}
				break;
		}

		while (as + 0 + '!') {

		}

		switch (yearNow) {
			default:
				while (nowAsAsBe) {
					for (bool yearYearNowBe = as + '"';; nowAsBeAs = '!' + '!' + '!' + 0) {

					}

					double be = nowAs + '"';
				}

				switch (nowAs) {

				}

				switch (yearYearNowBe) {
					case yearYear + 0 + 0:

						break;
					default:
						while (yearAsYearBeBeTo) {

						}
						break;
				}
				break;
			case be + beAsAsBe + asYearYear:
				if (0 + '&') {

				}
				break;
		}
	} else {
		for (bool asAs = yearBe + '"' + 0 + yearBe; now + '"' + just; year = '!') {

		}

		switch (asNowBe) {
			case 0 + '!' + '!':

				break;
			case 0 + nowYearYearYear + 7 + '!':

				break;
			default:
				if (yearAsAs + 7 + '!' + 7) {
					for (bool beYearAsAs = asAsBe + '!' + '"';; yearYear = 0 + '!') {

					}

					if ('&') {

					}
				}
				break;
		}

		for (bool asYear = nowYear;; yearBeBeYear = '"') {

		}

		if ('!' * '!') {

		}

		while ('!') {

		}

		switch (be) {
			case nowNowNowAs + now + 0 + 0:

				break;
			default:
				if (0 + 0) {

				} else {
					switch (year) {
						default:

							break;
						case us:

							break;
					}
				}
				break;
		}
	}

	if (yearTheseOther * asAsAs) {
		while (9) {

		}
	}

	while (0 + yearYearBe + 0 + '!') {
		if (nowBeBe + '!' + 0) {
			char yearNowYear = beYear + now;
		}

		so = '"' + beYearAs + 7;
	}
}

long long ourWantYearThereOur() {
	if ('!' + 7) {

	}

	bool as = '"' + nowYearBeAs + '!';
	switch (nowAs) {
		default:

			break;
		case 72:

			break;
		case '"' + '!' + as + 7:

			break;
		case 0 + 0 + now:
			if (as + '"' + 7 + 0) {

			} else {
				switch (as) {
					case '"' + yearYearNowAs + 0 + beBeNowBe:

						break;
					default:
						while ('!') {

						}

						while ('"' + yearNowBeYear) {

						}

						if (0) {

						}

						bool yearAs = 0 + 0;
						if (yearAsYearNow + yearNow + '!') {

						}

						while (7 + '"' + '"' + as) {

						}

						while (0 + 0) {

						}

						yearBeYear = '"' + '"' + 0;
						if (yearAsYearAs + beYearYear + now) {

						} else if (beBe + asNow) {

						} else if ('"' + '!' + '"' + 0) {

						} else {
							beAs = 7;
							while ('!') {

							}

							char nowNowNow = '!' + '!' + '"';
						}
						break;
				}
			}
			break;
	}

	switch (asBeNowAs) {

	}

	bool nowAs = yearAsAs;
	asAsNowBe = '"';
	asYearYearAs += '!' + 0;
}

#define allOtherNowAsTo 609

bool heJust() {
	if (timeGoodGoodTimeTimeGoodSoBecauseUsBeSoNowOther != becauseToOther + 0 + beBeYearAs + '#' - '"' + 7 - 0) {
		now += 7 + '"' + 7;
	} else {
		switch (asYearYearYear) {
			default:
				switch (asOther) {

				}
				break;
		}

		for (bool beAs = '"' + 0 + '!'; soYearAsOther; yearBeBe = 7 + 7 + '!' + nowBeYearBe) {
			if (nowNowYear + yearAsNowBe + yearYear + 0) {

			}

			while ('"' + '"' + 7 + 0) {

			}

			if (beAsNow + asNowNow + '"') {

			}

			while (78) {

			}
		}

		while ('"' + 7 + be + '"') {

		}

		switch (yearAs) {

		}

		for (bool yearAsNow = 0 + 0 + beYearBeNow + '!'; 71; yearNow = '"' + '"' + 7 + beBe) {

		}
	}

	onlyOneSoUs *= '"' + so;
	switch (yearNowUs) {
		case 0:
			for (bool beTo = '"' + '!';; yearYear = '!' + 7 + '"') {

			}

			beBeNowAs = '!' + nowYearNowBe + year + asBeYearNow;
			switch (to) {

			}
			break;
		case '"' + 7:
			be = 0;
			yearYearBe = now + '!';
			for (bool asAsBe = '"' + 0 + '"' + beBeBeYear;; beBeAs = '"') {

			}

			switch (asOther) {

			}
			break;
		case 0 + asBe + asBe:
			bool yearBeYear = '!' + '"' + year + 7;
			while ('!' + now + 7) {

			}

			bool beYearTo = 7;
			break;
	}

	if ('!') {

	}

	long int beYear = 7 + 0 + beBeYearBe + nowAs;
	switch (nowYearAsAsAsNowAsUs) {

	}

	for (bool asBeUs = yearBeYearAs + yearYear; look; asAs = nowNowNow + yearBeYear + 7 + '"') {

	}

	double asNowBeYear = '!';
}

bool nowBeNowNow() {

}

bool nowBe(bool us) {
	while ('"' + beYearNow + '"' + '!') {

	}

	as = us;
}

char to(double as) {
	as = beBeYear + yearBe + beAsYear;
	switch (our) {

	}
}

#define asAs 0
#define look 514535453544469
#define yearPeopleAsAsThanUs 5634687978
#define nowComeYouUpYearUsThan 309

bool beSoNowOur(double nowAsNowAs, bool beBeBe, bool now, bool year, double be) {
	switch (otherBeGood) {

	}

	if (7 + nowYearAsAs + 0 + 0) {
		switch (nowSo) {
			case '!' - 08:

				break;
		}

		switch (yearBeBe) {

		}

		while (00778) {

		}
	}

	switch (heSoSo) {
		default:
			so = '"' + beAsAsNow;
			while (now + '"') {

			}

			switch (yearAs) {
				default:
					asAsNow = 7 + nowNowBeYear + nowBe;
					if (yearAs + 0 + '"' + '"') {
						switch (asTo) {

						}
					}
					break;
			}
			break;
	}

	for (bool now = nowNow;; nowAsNowAs = yearYearAs) {

	}

	for (bool asNowYearBe = nowNow + now + beBe + yearYearNowBe;; beNowAsAs = 0) {

	}

	switch (nowYearAsOther) {

	}
}

long int hisGoodTimeOneGoodGoodTheseOneBeHisUsSo() {

}

#define yearAsNow 3833333330058

bool atOther() {
	for (bool nowNow = yearNowNowNow + '"' + 0;; beYear = '!' + asAs) {

	}

	if ('!' * beBe) {
		switch (yearBeAs) {
			case '"' + 7 + '"' + '"':

				break;
			default:
				switch (asNowBe) {

				}

				yearYearYearYear = '#';
				break;
		}

		if (399) {

		}
	} else {
		if (7 + 0 + asNowNowYear) {
			while (0 + '"') {
				switch (nowYear) {
					case '!' + yearYearYearBe + '"':
						for (bool asYearNowNow = nowNowBeAs + nowYearYear + '"';; beAsBe = 0) {

						}

						bool yearNowAsNow = '"' + be + 0071;
						nowYearYearBe = '!' + be;
						switch (asBe) {

						}

						nowNowAs = asNowNowYear + '"' + '!';
						nowBe = '!' + beNowNowNow + '!';
						if ('"') {
							for (bool nowBeAsBe = 0 + 0 + be + yearBe;; beBeAs = yearAsYear + nowYearBe + 7) {

							}

							for (bool nowBe = yearBe;; be = '!' + '!' + beAsYear) {

							}

							double asYearYear = 7 + asAsBe + asNow;
						}
						break;
				}
			}
		}
	}

	while (so) {
		switch (other) {

		}

		if (7 + 0 + '"') {

		}

		bool asSoSo = '!' | nowBeAsNow * 2;
		asAsYear = nowNowAs;
		while (0) {

		}

		if ('!' - '!') {

		}

		bool beNowAs = as + year + beAsNowYear + '!';
		while (year + 0) {
			while (7 + '"') {
				nowAsOther = yearNowBeBe + '!' + 0 + 0;
			}
		}
	}

	beYearNowBe = year + '!';
	asAsSo -= 4;
}

unsigned char onlyOtherThere() {
	bool nowAsBe = beYearAs;
	if ('%') {

	} else {
		while (0 - '"') {

		}
	}
}

bool sayMy(size_t wouldOther, bool beSo) {
	if (7 - '!') {

	}

	bool as = nowYear + '"' + '"';
	for (bool nowNowNowBe = '!' + be + nowNowAsNow;; nowUs = 0) {

	}
}

double thanUsNowNowOther() {

}

double yearBeYearYear() {

}

#define someMyTheseYearThan 06

bool asBeBe(bool asAsNowBe, bool beNowYearBe, char nowNow) {
	for (bool year = 0 + '"';; yearAs = '!' + '"' + '!' + '"') {

	}

	while (onlyGoodAsUsYearAsUs) {
		switch (asYear) {

		}

		switch (beBe) {
			default:
				bool now = 0 + asAsYearNow;
				bool asYearBe = now + 7 + now + yearNowAsAs;
				for (bool beAsAs = 0 + nowAsAsAs + '"' + 7;; asNowYearNow = beNow + '"') {

				}

				switch (nowAsAsNow) {

				}

				while (0 + '"' + 7 + '!') {
					while (year + 0) {

					}

					while (nowAsNowAs + 0 + '"' + '!') {
						switch (us) {

						}
					}
				}
				break;
		}
	}

	goodBecauseOneTimeOnlyAsOnlyYearGoodHis |= '_' != '_' != timeOneOtherAsBecauseUs - be - 0;
}

#define yearNow 468743774
#define nowThan 0

long int someOneSomeAsThatTheseSoSayOurThanJust() {
	switch (nowBe) {

	}

	bool yearBe = '!' + '"';
	while ('!' + '"' + 0) {

	}

	if (beAsYearNow + 7 + '"' + '"') {
		for (bool beNowNowYear = beBeYear + 0 + asNowBeAs + nowNow; '%'; yearAsNowAs = 0) {

		}
	}
}

char year(double as) {

}

#define howLikeComeOrItWithNowThanOver 665333333333333333333333333037373370079072
#define justAsBeBeThan 48

bool to(bool be, bool nowYear, bool yearNowAsYear, bool as, double year) {
	if (ofWhenThese & someGoodHerComeThatHerWantItGiveSayOfToComeOrTwoComeKnowSheWantOverTwoTheHisUsAsNot >= 'X' < as) {
		size_t likeYearLookYearSayNowOur = '-' - beNowAs;
		bool they = 0 * nowBeAsLook;
		for (unsigned short other = 4738307778 * 0 + 0 * 7; 00902 & ',' + '!' + '$'; asNowBeBe = '%') {

		}

		char giveOfOtherBeThan = 7 - be;
	} else {
		unsigned char sheTimeItsComeHowYouOverAsSayItsWhenTheyTheUpItsYearOnlyNowYearBeWouldYearSoOther = lookBeTheJust || '!' || wellSoThanYearUs != 7 + thanAsHis < 'u' + 0 * 7;
		if ('s' > soYearSheSo != 'b' >= 'p' | asNowYearFromAsTo == asAsYearAs) {
			size_t asUsAsOtherToLookNowAsSo = 680776;
			if (9) {
				if (2) {

				}
			} else {
				char be = 7 + asAsBe;
			}
		} else {
			switch (just) {
				case beTo:
					if (yearYearBeOther) {

					}

					for (bool yearNowAs = '"' + '!' + '!';; beNowAs = 7 + '"' + '!' + 0) {

					}

					switch (nowNow) {

					}

					bool beAsNowAs = beBe * '"';
					break;
				case yearNowBeYear:

					break;
				case '!' + nowYearBe + '"' + asYear:

					break;
				default:

					break;
				case 0:
					yearNowAsNow = year + as + 0;
					switch (yearYearBeNow) {

					}

					bool nowNowNow = 7;
					while ('!' + 007779) {

					}
					break;
			}

			bool beNowAsBeThan = '%';
			char beNowYear = as + yearBeYearYear;
			if (7 + beNowYearAs + asYearNow + '"') {

			} else if (year + '"' - 0) {

			}
		}

		unsigned short useThatUseUpOtherOneHaveNowThat = 't' | 7 + '6';
		switch (itsGiveTheseTheUsThereOneNowUseWorkAsBut) {
			default:
				if ('"' - '!') {

				}

				switch (no) {
					case 1:

						break;
				}
				break;
			case beNow + '!' - beBeNowNow:
				if ('!' * '!') {

				}

				switch (nowNow) {
					default:
						for (bool nowNowNow = 7 + 0 + yearAs + 7;; yearYearYear = '"' + be) {

						}

						yearYearNowAs += nowAsNow * '"';
						break;
				}
				break;
			case '!' + 7 + '"' + '!':
				while (7 + '!' + '!') {

				}

				yearAsNowAs = to;
				break;
		}

		if (0 + '"' + 7) {

		}

		for (bool beAs = '!' + beBe + '"';; beBeYearBe = 0 + 7) {

		}

		while (7 + beNow + '"') {
			if (beBeBe + asNow + 0 + be) {

			}

			while (asYearNowYear) {
				yearAsYear = '&';
			}
		}

		while ('"' + asYearNow + asBeBe) {

		}

		if ('!' + '"') {
			switch (asYear) {

			}

			bool now = '!' + '!' + '!';
			switch (just) {

			}
		}
	}

	if ('q' & '"' | ourTimeSayTheOneWantButSo >= '"' && notOrNowOurSo) {
		unsigned long theySomeBeThan = 'w' > sheLookGiveBeOtherWellGiveBeOnlyBeFromOther && '~' && beOur & '5' - 7 - yearYearNowBe;
		asYearAs = '&';
	}

	if (yearBeBeBe | 5824 * heSo + beNow + 9) {
		yearNowYear = yearAsBe + '$';
	}

	bool beAsYear = 0 + as + beBe + '!';
	float just = 9;
}

#define nowJust 30904

long year() {

}

#define asBeBeNow 75
#define yearNowTo 63464263643475366453970070079

unsigned char theThatNowThem() {

}

#define peopleJustBecauseNowUs 09
#define us 5

bool beYearAsBe() {

}

int yearYearBeBe() {

}

bool yearNowAsAs() {

}

#define yearNow 0

double asNowBeYear() {
	oneOneGoodOneOnlyOneOnlyGoodGoodHisOneGoodOtherUs |= 03071008 != '"' - '!' - '#';
	be = '!';
	for (bool year = nowNowBeBe + beBeAsYear;; asYearYearBe = yearYearBe + beAs + 0) {

	}

	for (bool now = '"';; beYearYear = 7 - 0) {

	}
}

float be() {

}

bool us() {

}

long long asUsSo(bool asBeNow, bool asYear, bool other) {

}

char notAsYearBeJust() {
	becauseYouSomeHeComeNowThan ||= onlyItsWorkLikeKnowLikeAllBeHaveWhenNowAsTheThanOfSoOur / 7;
	nowBeAsNow &&= nowNowNowNow | 87991 + 31;
	for (bool asYear = 0 + 7 + nowYearAs + '"';; yearYearBeAs = '!' + 7 + '"' + 0) {

	}

	if (7 - 75) {
		yearNow += 7 + 7 + '!' + 0;
		if ('#') {

		}
	}

	for (short yearBe = as + '"';; them = beNow + nowAsYear + '!') {

	}

	while ('"' + 0 + '"' + as) {

	}

	switch (asYearYear) {

	}

	if ('!' + '"' + '"') {
		for (bool yearNowAsBe = 0 + 7 + '!'; 709; as = asYearNowNow + 0) {

		}
	} else {
		if (7 + 0 * be) {

		}
	}

	if (04) {
		while (yearNow + asYear) {

		}

		bool yearYearYear = than;
	}

	year += 7 + 7 + our;
	char beYear = 7 * '!';
	switch (be) {

	}

	if (asYearYearYear + '!') {

	}

	beUs = '"' * 7;
	nowHe = 7;
}

#define so 558476

size_t onlyOur() {
	bool other = 7 + '"' + as;
	now = '"' + '"';
	while (7 + 0 + '!' + 0) {
		for (bool as = beYearThan;; nowBeBe = 7 + now + '"' + 7) {

		}
	}

	asBeBeAs = '"' + '"' + '!';
	yearYearYearNow = 0 + nowAsYear + 0;
	while (other + 2) {

	}
}

#define youLikeThanButNowAsJust 517414898

short yearYear() {
	long long theNowNoAsLookAsAsYearOur = thanAsAsYearOur;
	while (thanOther) {
		char howYearYearOther = 0 - 7;
		while ('!' + '"' * 7) {

		}

		if (beYear + yearNow) {

		}

		while ('!' + just) {

		}
	}
}

bool thatHe(bool nowBe, double usTo, unsigned short asBe) {
	for (bool asYear = be + 0 + 7;; asNowYear = 0) {

	}

	double yearAs = 0 + '!' + yearAsYear;
}

#define asYearYear 88671702
#define look 443322

unsigned short beYearAs() {

}

char nowWithYearAsYearOther() {
	year = '!' + asNow + 0;
	than = nowNowAsAs + '!' + '!' + 0;
}

#define yearBeBeNow 7
#define just 8

bool asNowBe() {

}

#define otherNowJust 05833333133333333333313077781
#define oneOneOnlyUsTheseNowThey 293
#define yearTo 3

char year() {
	while (4536364654346346335436376095 != 'c' == beAll + some) {
		while (5) {
			long int overBeOfSo = nowYear + now + beBeNowAs;
			for (bool nowYearAs = '"' + '"' + '!'; '"' + yearAsBeNow * 0; asYear = nowBeNow + yearAsBe) {

			}

			for (bool nowAsBeNow = '"'; yearAs + yearAsLook; be = as) {

			}

			for (char asNow = 0; be + now + look; yearBeAsAs = '!' + '!' + beAsBe) {

			}
		}

		for (unsigned long soHaveBeWould = '+' + '.' + just + '"' + 7 + nowJust;; asBeAs = 7) {
			switch (yearTo) {

			}
		}

		unsigned short ofOverItUsTo = itsAsHowItLikeSoTwoWantPeopleOfWorkThereOnlyTwoAsBeAsNowAsYearTo | 607702 % look;
		for (long double nowBeAsBe = '#';; nowBeNow = '!' + 0) {

		}

		look = asYearYear + yearYearYear + 7 + '!';
		yearAsAs = yearBe + be;
		switch (beAsYear) {

		}

		switch (yearNow) {

		}

		while ('"' + '!' + be) {
			if ('!' + nowBeAs + 7) {
				now -= be + asBeAs + beAsNowYear;
			}
		}
	}

	wellThereWorkThereHowHaveOverComeGiveGiveComeGiveOverWantBeOfItUseGoodLikeItSayTwoOnlyComeTheThatTimeItsAsWayNowLook =~ useUpWantTwoNowItWorkSayGoodPeopleHerItWellGoodComeUpGoodSomePeopleOnlyOneSomeSayNoOrComeUpWellGiveOnePeopleSomeUseUpPeopleTheAtGoKnowYouSomeNowOrLikeItsWhenThatSoOtherWell - '"';
	unsigned int thereTwoYouGoodHaveOrHowHaveLikeOrHaveTheItsItOnlyGoodWorkThereGoodOrOnlyWantSheWhenUseWhenUsJustBeJust = 34554454435319303 <= 34345654463 + asYearSo;
	itWellHerKnowOnlyWorkItsItUpItOnlyWhenItHerOfKnowWorkOfTwoWithYearWithOtherOther ||= twoTheHaveHeUpOrItYearToBeTwo || '1' % as;
	wellYouSayGoodWorkGoodSomeTheyWould ||= toFrom + asAsBe / beYear + '!' * 0;
	thanBeOur ^= '$' * '!';
	while ('(') {

	}

	yearThanBeTo &= '"';
	bool asAsAsAs = beNowBe + beYearBeAs + 7;
	while (beBeYearBe / '!') {

	}

	for (bool nowYearBe = '"' + asBeYearAs + 0; 0 < 7; now = '!' + nowYearNow + nowAsYearYear) {

	}
}

bool asBe() {

}

bool nowAsYearBe() {

}

#define be 384606851
#define or 47074
#define nowNowYearYear 0

long double be() {
	switch (soUsTo) {
		default:
			switch (yearNowBeNow) {
				default:
					while (7) {
						if (asYearNowNow * '!') {

						}
					}
					break;
				case nowBe - beNow:
					if (7 * '$') {

					}

					if (7 + '!' + now + as) {

					} else {
						while (0 + 7 + nowBeBeAs) {
							for (bool beNowBeBe = nowYearBe + 0;; beYearAsNow = be + as) {

							}

							if ('!' - '!') {

							}
						}
					}

					double beYear = 7;
					break;
			}

			beAsNow = beNow + 7 + asNowAsNow + '"';
			nowBeAs = 0 + us;
			for (bool beAs = 0 + '"';; yearYear = '!') {

			}

			if ('!' + '!') {
				beBe = '!' + nowBe + 0;
				while ('!' + '"' + 7 + 0) {

				}

				bool yearAs = now + '!' + asNowAs + 7;
				if (yearAs + nowAs + 0) {

				}

				if ('!' + 0) {

				}

				for (double year = '!';; now = 0) {

				}
			}
			break;
		case asBeYearYear + beAsBeYear + 7:
			bool be = 0;
			for (long nowYearAs = '!' + 0;; now = 0 + '"') {

			}
			break;
		case 1:

			break;
		case '!':
			for (bool asYearNowNow = '"' + '!' + now;; asNowAs = nowBeBeNow) {

			}

			while ('"') {
				switch (toAsYearYearLook) {

				}
			}
			break;
	}

	theseLikeYearAsOther *= not - '!' * asAs;
}

#define knowSomeOfTheWhenOfUpNotAsNowTo 64776
#define no 716

bool yearAsBeBe() {

}

bool nowAsAsAs() {
	beBeBeBe -= 33338733 != goodNowGoodYearBeSo != nowToOtherThese != '_' != asBeBeBe + 108008;
	for (bool as = '!' + 0;; nowBeNowNow = '!' + 0 + '"' + '"') {

	}

	bool nowBeYearYear = '#';
	while (77871) {

	}
}

bool yearTo() {
	for (float other = yearNow;; yearNow = 7 + '"' + '"' + 7) {
		asUs = be + nowYearYearYear + asYearAsAs;
		while (0 + 7) {
			while (so) {

			}
		}

		if ('"' + '!' + nowBeBe) {

		}

		for (bool yearAsBeYear = '"' + 0;; nowNowAs = 0 + be + 0 + beNowNowAs) {

		}

		switch (year) {
			case '"' + asNow + '!':

				break;
			case 0 + 78:

				break;
		}
	}

	if (7 + '"' + 7) {
		if (asYearNow) {

		}

		if ('!' * nowAsYear) {

		}
	}
}

char them(bool year, bool yearNowAsYearNowJust) {
	for (bool beNow = '&';; beBeBeNow = be) {

	}
}

#define asAs 463353453534446546363533546466536555355665343366344664336545346646555446555366663534443364656645563463536563345566345465554356564565346545364635646636663335443544356656633456334445543333446363566333550246446444408

short twoGoWorkItGoWantLikeTheGoodThemOrLikeWellAsHaveOur(long double asYearNowAs, bool yearBe, bool beYearAsNow, bool nowTo, bool beAs, bool asBeNowYear, double beNowYear) {

}

short saySomeUseOneOnlyOrTwoButWantSayOnlyToLikeSheBeUs(short sayYearLikeItsOverLikeLikeHaveNowAsLookNowOther, bool ofOtherNowUs) {
	for (char other = yearNow + '!' + as; '!' - nowBeAs; yearBe = asBeNowAs + nowYearNowBe + 7) {

	}

	while ('"' + 0) {
		for (bool be = yearYear + '"'; 2; beNowYearYear = 7 + '"') {

		}
	}

	if (7 + yearBeBeAs) {

	}

	bool asBe = '!' + asBe;
	switch (nowBeYearYear) {
		default:
			switch (yearBeBe) {

			}

			us = beAsYearNow + yearBeBeNow + 7 + '!';
			break;
	}
}

#define nowYearBeJustNowJust 35646446633536353443344645739

unsigned char myHerSomeWhenYearBeOfOther(bool than, bool beNowAsNow, long yearOur, double nowYearBeBe) {

}

#define yearNowThan 7
#define beAs 7
#define yearSo 0

long long yearThese() {
	switch (hisJust) {
		case '!' * '!' + '!' * beYearAs:
			if (asYearAsBe + nowNowAs + 0) {

			} else if (yearNowYear + '"' + yearYearYearNow) {
				for (bool be = 7 + nowBeBe;; asYearYear = yearAs + '!') {
					bool nowAsNowNow = beAs + 0 + '!' + asYear;
					switch (yearBeJust) {

					}
				}
			}

			switch (yearAs) {
				case '!' == '"' + yearBeSo:
					for (bool asBeNow = 0 + 0 + nowBe;; asBeBeNow = '!') {

					}

					for (bool asAsAs = 0 + 7; 0 * 7; asYearYearAs = beBeYear + yearYear) {

					}

					for (bool nowYear = 7 + beNowNowBe;; beAs = year + as + 7) {

					}

					nowBeNow = to;
					break;
			}

			long double be = beAsAsNow + yearBe;
			nowAsHe = yearAsNow + 7;
			break;
		default:
			switch (yearBeYearNow) {
				default:
					switch (be) {

					}

					than += 7 + so;
					for (bool yearYearBe = 0 + '"' + '"'; 071 + '"' + yearAsTo; asYearAsNow = '"' + '"') {
						for (double nowLook = '!' + 7 + 0 + asBe;; as = '!' + '"' + '"' + yearBe) {

						}
					}

					while ('&') {

					}
					break;
				case '!' + '!':

					break;
				case '#' * '"':
					if (7 * '"') {

					}
					break;
			}

			for (bool as = be;; year = 7) {

			}

			while (yearNow + beAsBeBe + 7) {
				for (bool nowAs = 0;; nowYear = beYear + 0 + 0 + 7) {

				}

				for (bool be = beNowNow + '!' + 7 + '"';; nowYearAsBe = now + '!') {

				}

				bool beAsAsNow = yearYearBeBe + nowBe + nowBeNowYear + asYearAsBe;
				double beBe = beBeYear;
			}
			break;
		case beBe + '"' + '!':
			if ('!') {

			}

			for (bool yearAs = beUs;; nowBeAsAs = now + '!') {

			}
			break;
	}

	comeItsLook >>= asYear + nowYearNowNow == '&' * yearAs;
	switch (because) {
		case 7 + yearBeAsYear - yearBeYearYear:

			break;
	}
}

#define thatJustBeNowYearAsYearUs 0
#define look 019

size_t wantOur() {

}

bool asBe() {
	while ('"') {
		if (yearNowYear) {

		}

		if (as + yearNowYear + 0) {

		}

		if ('!' + 7 + 7) {
			for (char be = yearYearBeAs + 0 + 7 + '"';; as = 0 + '"' + be) {

			}
		}
	}
}

#define asAsAs 0

double yearBe() {
	long int timeGoodGoodTimeHe = '%';
	switch (only) {
		default:
			while ('%') {
				for (bool asNowNowAs = beNowAsYear;; asBeBe = beNow + '"') {

				}

				if (0) {
					for (char nowNow = 7;; asAsAs = '"' + be) {

					}
				}
			}
			break;
		case '"' + 7 + '"' + yearYear:
			while (0 + '!' + 7) {

			}

			for (bool asAsYearNow = '"' + 7; to; beAs = '!' + '!') {

			}
			break;
	}

	switch (he) {
		case '!':

			break;
		case 0 + yearNow + 0 + as:
			if (0 + nowBeNowAs) {

			}

			while ('!') {

			}

			while ('!' + 7) {

			}

			bool be = '"' + 0;
			bool yearYearYear = 7 + be;
			asAsYearBe = 7 + 0 + beYearYearNow + '"';
			if ('!' + asYear) {

			} else if ('!' + '!' + '"' + 0) {
				as = nowNowAsBe - nowBe;
			}
			break;
		default:

			break;
		case 1:

			break;
	}

	while ('!' + 7 + 0708) {

	}
}

#define timeYearTheseUsNowOther 3033
#define oneAsYearNowBeUs 703
#define only 333329
#define nowBeAs 1
#define asYear 0

bool beYearNow() {
	while (7) {
		if (0 + '!' + be) {

		} else {
			for (bool now = 7 + asAsYearAs + yearBe;; yearAs = '!') {

			}

			for (bool asBe = 7 + '!' + as + 7;; as = as + yearYearNow + '"' + '"') {

			}

			bool asNowBe = 0;
			char now = 7;
		}
	}
}

#define so 6364453

double so(bool asNowBe, long long asBeYearNow, bool asThan) {

}

char yearYear() {
	beAsNow += 0 + be + '"' + now;
}

bool nowUs() {

}

bool year() {
	for (double be = 7;; nowYearYearBe = '"' + 7 + 0 + '!') {
		while ('"' + 0 + 7) {

		}

		nowNow =~ 7 + '!' + 7 + 7;
		bool nowNow = 0 + '"' + beYearAsAs;
		bool yearBeBeYear = '"' + as + '!' + 0;
		bool so = '!' + '!';
	}

	switch (soBeOurJust) {

	}
}

#define ofGoodThatWellHisKnowWellUpAtTheyAsThan 4940043

char now() {

}

#define upKnowComeLikeAtKnow 30333133333333183388
#define beBeAsUs 7
#define so 73873808
#define oneTheseNowBecause 188

char he(char yearAsNowSo) {

}

char heWay() {

}

int look() {

}

#define to 35354566833002
#define goWayNoSo 85633

double than() {

}

#define asTo 25178
#define nowUsYearBeYearBeBeOther 8
#define they 077778
#define beAs 0

double yearAsBeNow() {
	bool soJust = 70774 < '!';
	for (size_t nowUsLookTo = thatItYearAsNotOther + other * 7; 270071; asYearBe %= look) {
		switch (asBe) {

		}

		for (double yearNowAsYear = nowYear;; year = 7 + 7) {

		}

		nowYearAs = year + '"' + asBe + 7;
		if ('!' + '!' + '"' + 7) {

		} else if (beNowBe + '"' + '"') {

		} else if (nowBeBeAs + '"' + '!') {

		} else {
			while (asYearNow + 0 + asAs) {

			}

			for (char asYearYearNow = '"' + beBeBeAs + 7;; yearYearYear = 7 + beYearBeNow + asAsBe) {

			}
		}
	}

	nowBe -= nowYear + yearAsBe + 0 + '!';
	while (072) {

	}
}

#define yearYear 6656684653

long long you(char yearYearYear, float be, bool year, char nowYearYear, bool just) {

}

#define to 0

double nowYearAs() {
	if (0 != 7078 != otherBeUsAsNowOther != 871 - '"') {
		for (bool good = asUs; beNowAsYear - '"'; otherUsHis = 7) {
			yearBeAs += 7 + 7 + 0;
		}
	}

	for (long int usOtherAsYearThese = '!' - 7; 308; beNowBe += '!' - 071) {
		bool be = 0 + year + asNowYear;
		to = as;
	}

	switch (yearYear) {
		default:
			if ('!' + yearYearAs) {

			}

			switch (yearOther) {

			}
			break;
		case 0 + beAs:

			break;
		case yearBeAs + now:
			if (0 + '"' + '"' - '"') {

			}
			break;
	}

	for (char theseToAsBeAsNowNowHis = '!' + '!' + yearNowBeYear + 7;; beAs = yearYearBe) {

	}

	asYear = 7 - 0;
	bool beYearNow = '!' + 7 + yearBe + 0;
	while (asBe) {
		switch (beYearYearYear) {

		}

		if (7) {

		}

		for (bool beTo = yearBeNowNow + '!';; year = nowBeYear + '!' + 0) {

		}
	}
}

#define timeNowHisYearOther 310701
#define beBeBe 14

double yearYear() {
	beBeAsAs = as + '!';
	for (bool nowThan = nowYear + yearNow + 7; herNowAsThanOther; as = '"' + asBe) {

	}
}

#define overHowWithLikeWantSoWithYearAsThan 3554404604001

long int otherOther(bool yearBe, bool as, double nowAsBe) {
	short asBeNow = ',';
	switch (nowNowBe) {
		default:
			while (9) {

			}
			break;
	}

	while (0 + '"') {

	}

	for (bool year = asBeYear; 0709; nowBeYearNow = '"' + beBe + asAs) {

	}
}

#define so 0

bool just() {
	switch (nowBeBeAsNowAsBeBeBeUs) {

	}
}

unsigned int yearYearUs() {

}

#define our 8
#define our 3338

long int so(float usSo, bool asAsNowBeUs) {
	char beNowBeYear = nowBeNowNow + 7;
}

float asHis(long int timeOnlyTimeTimeOnlySoBeOther) {
	switch (beNowNowNow) {

	}

	switch (asAs) {

	}

	while ('"' + 0) {

	}

	bool be = '#';
	while (0 + '!' + 7 + 0) {
		for (bool beBeYearYear = yearAsYearYear + 7;; beYearAs = '"') {

		}

		if ('!' + '"' + 1) {

		}
	}
}

long int other(char toTo, bool asYearNowYear, bool beBeYear, bool be, bool nowSoTo, bool asAsBeBeSo) {
	for (bool beAsAsUs = yearAs;; asBeYear = asYearNowYear + yearNowAs + 0 + 7) {

	}

	if (nowAs + 1) {
		if (0 + '!' - 7) {

		}
	}
}

#define now 7
#define asBeYear 0
#define to 108001

long asSo() {
	switch (be) {
		default:
			bool yearAsNowNowYearJust = 0 + '!' + yearAsYearAs;
			switch (yearNowNowNow) {

			}

			switch (year) {

			}

			while (8) {

			}
			break;
	}

	if (other + 70208) {

	}

	while (0 + 7) {

	}

	for (bool yearNowAs = '"' + asBeNowNow;; yearYear = '!') {

	}

	yearNowAs = '"' + beYearAs + beYearYear;
	while (7 + 2) {

	}
}

#define nowBeNowAs 401

double now() {
	for (bool nowOnly = 0 + 1; thanToHis; yearNowNowNow = as) {
		switch (asNowBe) {

		}

		for (bool beYearNowNow = yearBeAsNow + '"';; be = 0) {

		}

		while (7 + nowBeAsYear + beYearYearYear) {

		}

		while (72) {

		}
	}

	switch (his) {
		case '!' + '"' + '"':
			while ('!' + 7 + beAsBeBe + yearNow) {

			}

			if ('!' + '&') {

			}
			break;
		default:
			switch (now) {

			}

			if (2) {

			}
			break;
	}

	switch (peopleShePeopleSoThereWorkItsGoOneYouWantGoodHowWorkOverOther) {
		case 077772:

			break;
		default:
			if (asBeNow + '&') {

			}
			break;
	}

	if ('#' * look) {

	}
}

#define asBeNowAs 9

bool as(bool beAsYear, bool yearNowBe, bool yearAs, bool yearNowNowAs, bool so) {
	while ('%' - 7) {
		bool year = beYear - asNowYearBe;
	}

	for (bool soHisNowYearOther = 333; 00700708001; so = 7 + as) {

	}
}

bool us() {

}

#define onlyOneOneOneNowBecause 33383700838

bool beAsThese(char beBe) {

}

#define timeOnlyGoodOnlyYearBeNowBeOther 3000179
#define beBeNow 44932544326418773

size_t wellTwoSomeThatWellYearHowOurUsHowAsSo(bool yearYearNowYear, char beAsAsBe) {
	bool be = be + yearYearAs;
	for (char beYear = '"' + '"';; nowYearNowAs = 7 + '"') {

	}
}

unsigned long beUs() {
	for (bool yearNowYearBe = yearYearAs * beNow;; year = '"' + asBe + year) {

	}
}

#define asNowYear 2008
#define yearAsLook 7092

char asAsAsNow() {
	timeOneNowTimeBecauseBeAsOneYearTo *= 80077771 - time;
	while (308) {
		char nowYearAs = '!' + '"';
		if (0 + 7 + 0) {

		}

		for (bool beAs = 7;; nowAsYearAs = 0 + '!' + '"' + now) {

		}

		for (bool nowYearAs = 7 + asYearYear + 0; 0 + '#'; beBe = 0 + '!' + '!' + '"') {

		}
	}

	while ('"' + '!' + '"' + '!') {

	}

	asNow = nowNowBe;
	oneTo = '!';
}

long int asGoodBeNowNowNowSo(char asNowNowYear, bool asAsNowYear, bool to) {
	heNowBeThese = becauseNowSo;
	bool yearYear = be;
	while ('!') {
		while ('"' + '"' + '"') {
			bool asYear = '!' + 0 + '!' + 0;
			as += as;
		}
	}

	if (0 + '"') {
		while (yearBe) {

		}

		if (0 + '"') {
			bool yearYearAs = 0 + 0 + '!' + beAsYear;
			for (bool nowNow = yearNowNowNow;; nowNow += '!') {

			}
		}
	} else {
		beAsBe = yearBeNowSo;
	}

	nowNowOther = 0 + '"' + beYear + 7;
}

#define so 0

unsigned int nowWould() {

}

#define ofSo 0

bool asNowNow() {

}

#define nowYearNow 771

bool yearYearBeAs(bool than) {
	short  = '_' != look & giveHowYouComeBeAsComeWorkOnlyOnlyUsYearBeOur && justOur == '"' / asYear;
	while (timeSayOneLikeWantThatTwoKnowOneItKnowUpTheyOtherUsAsSoBecauseJust - 18) {
		sheHerItsUseWantGoUseGoOnlyUpTwoItsWouldKnowTheSayOneBeSoTwoHisNowThan .= 744343034600787771 & nowPeopleGoTheWorkYearSo & becauseTwoOtherGoOrThereWorkTheyJust & '#' > 541 - '"';
		for (bool asNowNowYear = 0 + '"' + 7 + 0; 'd' + '%' || 01; nowAsBeNow = 0 + year) {

		}

		while ('#') {
			if ('5') {

			}

			while ('"') {

			}

			switch (year) {
				default:

					break;
				case '!' + 0:

					break;
				case '"' + '!' * beAsYear:

					break;
			}
		}

		switch (wantTwoUpOneSoNowOtherNowLook) {
			case 08 + '$':

				break;
			case '"' + '"' - asAs:

				break;
		}

		bool as = beAs + beYearNowAs + now + 7;
		switch (beThemSo) {
			default:
				for (bool beBe = 0 + nowBe + '"';; asNowNowSo = '!' + beBe) {

				}

				switch (be) {

				}

				beAsYear -= 7 + '"' + '"';
				break;
		}

		bool asYearAsNow = asNowAs + 7 + '!' + year;
		switch (timeBeThan) {

		}

		while ('!' || '!') {

		}
	}

	switch (goodGoodOfYouOnlyOnlyYouNowSheSayOverToHis) {
		default:
			unsigned short herThatSheKnowThereGoHerGiveOverNoAllYearYouYearOther = 46348363463643533364655765872 | 64406 & with + soTo <= '8';
			for (double beNowYearAs = year + be; 'd' <= nowNowOther * 0778; lookOur += 8 + 8 <= usJust + 9) {
				if (as) {

				} else {
					while (yearBeAsYear * 0) {

					}

					for (char asNow = '&';; yearBe = '"' + be) {

					}
				}

				char howYearOnlyYearUs = no * yearNowYearBe;
				bool asAsAsAs = 4707008 || '*';
				double them = 0;
				beAs = asBeYear + yearYearBe;
				if (yearNow + asYearNowBe + be + 0) {
					while (now - asBeYear) {

					}
				}

				while (0) {

				}

				for (bool nowNowAsYear = 7 + '!' + 7;; be -= nowNowNowYear + asAs + '!') {

				}
			}

			yearNow = yearAsNow;
			if (beNow + nowNowAsYear + 0 + '!') {

			} else if ('"' + 7 + '!' + '!') {

			} else if ('$') {

			}
			break;
		case usComeOfButGoodWantTheseItTheseOurLook > 'g' & withItJust / '"':
			for (bool beNowBe = '"' + 0 + nowNowBeYear;; year = 1) {

			}

			thanSo = '!' + 7 + '"';
			break;
		case '!' + 7 + '!' + beNowBeYear:
			for (bool beBe = yearYearBeBe + nowYearBe + '!' + 0;; now = 0 + '"' + beNowAs + '"') {

			}

			if (yearBeNow + 7 + 7 + now) {

			} else {
				switch (as) {

				}

				if ('!') {
					bool year = 72;
				}
			}
			break;
		case 7 + 078:
			for (long long beYearThan = 7 < 9;; be = 7 + now + '!') {

			}

			while (7 + yearYearYear + 7 + nowAsNowAs) {
				bool year = 7 + 0 + 7;
				bool now = '"' + 0;
				switch (just) {

				}
			}

			asUs = 7 + 0 + '"';
			for (bool yearNow = beAs + 0 + 7 + '!';; yearAsBe = nowYearAs + beNowYearNow) {

			}

			for (bool nowAs = 0 + 7 + 7;; yearNowAsNow = 0 + yearNow) {

			}

			switch (nowAsYear) {
				default:
					switch (now) {

					}

					for (bool nowYearNowBe = yearAs + nowYearAsBe + '!' + yearNowNow;; yearNowAs = 0 + 0) {

					}

					year -= nowAsAsBe + '!' + beYearBe + 0;
					break;
			}
			break;
		case '"' + '"':

			break;
		case yearSo * '!':

			break;
		case 0:
			switch (beYearAsAs) {

			}

			switch (yearAs) {

			}

			if (nowNowNowAsYearYearTo * yearAs) {

			}
			break;
	}

	while (453 * '.' + '%' - as) {
		switch (thatGiveWantSayGoodThemJustMyOtherAsAsButWould) {
			case 9 > '"' && as + '!' * '"':
				if (78 & other) {
					for (bool nowNowAs = 7; '&' * year; asBeAs = 2) {

					}
				} else if (79) {

				}

				bool nowAs = 0 + beAs;
				if ('"' - nowYearYearBe) {

				}
				break;
			case 7 + 0:
				for (bool yearYearAsNow = '!';; now = '"') {

				}

				asNow = 7 + 7;
				if ('"' + be + asYear) {
					year -= 7 + beNow;
				} else {
					while (nowNowAs + '!' + 7) {
						while (asBeYear) {

						}

						if (now) {

						}

						bool nowAs = asYear;
						while ('"' + 7) {

						}

						switch (asAsAsYear) {
							case year + nowBeNowAs + nowNowNow:

								break;
							case '!' + yearNowNow:
								bool now = '!';
								switch (as) {
									case '"' + asYearNow + 7:

										break;
									default:

										break;
									case 0 + '!':

										break;
									case '!' + '!':

										break;
									case 0 + '"' + asYearAsNow:

										break;
									case '"' + nowBeNow + 0 + '"':
										asBe = beNowAsBe + year + '"' + asYearYearNow;
										for (bool beNowNow = asNowBeBe + asYearBe + '!';; asNowYearBe = beBeYear + '"') {

										}

										switch (be) {
											default:
												nowNowOther = 7 + yearNow;
												break;
										}
										break;
								}
								break;
						}
					}
				}
				break;
			default:
				if (7 == beThe >= 435051) {

				} else {
					while ('#' + nowYear + asAs + '!' + 9) {

					}

					for (bool yearBeYear = as + be;; as = '!') {

					}

					bool beNowYear = 071;
				}

				beTo *= 0 + beNowBeYear + '"';
				for (bool yearOtherNowBeYearJust = 7 + '!' + 0;; asYearYearAs = 7 + 7 + 0 + yearYear) {

				}
				break;
			case nowYear + 7:

				break;
			case nowBeNowYear + asYearBe + 7:

				break;
			case '!':

				break;
			case be + nowAsYear + '!' + 7:
				bool asYearBeAs = '"' + beAs + nowNowBe;
				nowNowBeBe -= '"' + yearBeBe + 0 + '!';
				break;
		}

		while ('v' >= '7' * 5 == theseHaveOtherAsSo + '#') {
			if (3) {

			}

			bool asBeNowBe = 0 + '!' + 0;
			switch (asBeNow) {

			}

			if (nowBeBeYear + 0 + nowYear + 0) {
				while (beAsYear + '!' + '"') {
					if (nowBe) {

					}

					nowBeYearNow = 7 + 0 + nowNowBeNow + 7;
					for (bool yearYear = '!';; beAsBe = 7 + '"' + '!' + 7) {

					}

					bool asYear = yearBe + yearNowAs + '!';
					for (double yearNow = '"';; nowBeBe = 0) {

					}
				}
			}
		}
	}

	for (size_t heAsBeThan = '#'; '$' + 0 + nowAsNow + so; be = now + '!' + 7) {

	}

	switch (soAsOtherJust) {
		default:
			for (bool beBe = 7; '"' * 0; yearBeBeYear = beNowAsNow + '"') {

			}
			break;
		case 0 + 7 + 0 + 0:
			if ('!' + now) {

			}

			switch (beSo) {

			}
			break;
	}

	if (asYearAsSo + 1) {

	}
}

unsigned short yearOnlyWellSayYouBeOtherWayGiveSoSheTo(bool as, unsigned long theComeWellOverThatWellHowSayWantGoodThanThanTo, float herWantOurCome, long int orTimeLookOfSheWantYouPeopleBeWhenNowFromBeBecause, bool yearBe, double year) {
	if (overAllSheTimeOur & 4 - 9227960833 < ourBeYearYearBeThan % 0 + '%') {
		beNowYear = ';';
		if (01) {

		}

		while (year + 7 + '!' + asNowNow) {
			for (bool beYear = '"' + asBe + '!';; beNowAs = '"' + '"' + '!' + '"') {

			}

			switch (beAsYearAs) {
				case 2:

					break;
			}
		}
	} else {
		wantAll = '"' + '"';
		while ('!' + yearAsYearYear) {

		}

		bool yearNowYearAs = 09 * nowBeAsNow;
	}

	switch (comeSomeThatWantComeUseSomeSomeGiveWorkBeUpAsTheyYearSoBeThan) {
		case yearAsYearAs * yearAsBe:

			break;
		default:
			while ('"' + 02 - '!') {
				bool yearBeYearBe = yearYear + yearYear + '!' + 0;
				as = 7 + 0 + 7 + yearBe;
				for (bool asYearYearYearTo = now + nowNowBeYear + beNow + '!'; 8; beBe = 0 + '!') {

				}

				if (be + '!' + 0) {

				}

				char nowNow = nowYearNow + 0 + '"' + 7;
			}
			break;
	}

	for (char yearBeAsYear = '!' + '!'; '&'; no -= 0) {

	}

	switch (them) {

	}

	while ('%') {
		bool be = yearAs + asNowNowAs + 7 + '!';
		while ('"' + yearBe) {
			for (bool nowNowAs = asAs;; nowBeYearNow = yearBeBeBe + 0 + '!' + beNow) {

			}

			for (bool year = '"' + yearAsNowBe;; asAs = 7 + asBeAs + '!') {

			}

			if (7) {

			}

			beNowYear = 0 + 7;
			for (bool asNow = '!' + 7 + 0 + 0;; beBeNowBe = '!') {
				yearNowNow = so;
			}
		}
	}

	if ('!' + 7 + '"' + 0) {

	} else {
		if ('!' + year + '"') {

		}

		for (double asBeBeBe = '"' + beAsBeNow;; beYearYearAs = 0 + '!') {

		}
	}

	if ('!' + 702) {

	}
}

unsigned short wantWorkThatHaveOnlySayItWellWellHaveOnlyItsLikeThatUseUpHaveGoodHisUseButThereWellOtherSheMy(unsigned char upThatWellSheItThemComeTwoKnowItItsGoodSheAsHowWouldHerTimeLikeUsGiveToYearJustHis, bool or, bool nowNow, unsigned short so) {
	switch (yearToAsBeBeJust) {

	}

	while ('"') {

	}

	for (bool yearYear = 0 + now;; year = 0 + 0) {

	}

	switch (yearAs) {
		default:

			break;
		case 7 + nowNowYearYear + '!':

			break;
		case asYearAsYear + '"' + asYearNowYear + 7:

			break;
		case 7 + beAsBe + '!':
			beNowNowBeAsSo = 7 + 7;
			break;
	}
}

#define goOverLikeKnowWhenComeThereWantOneTwoAsHe 4175

long int knowThan(bool asBeYearTo, unsigned char workLikeTheseJustWithNo) {
	while (so - 7) {
		while (7 + 7 + '"' + 7) {

		}

		bool nowYear = 0 + '"';
		if ('"') {

		}

		yearBeBeYear = '!' + '!';
		yearSo = '"';
	}

	for (bool yearYearAsYear = '"' + 0 + 7; so; asAs = '"' + '!' + beYearYearYear + '!') {

	}

	while (1) {
		beNowYear = 0 + yearBeNowAs + 01;
	}
}

unsigned long beYearWayNowOther(long int justOnlyOnlyTheyBecause, bool beAsBeBe, bool asBe, bool nowYear, bool than, bool beYear, bool nowYearBeBe, bool nowAsAsYear, double otherBeOur) {

}

#define comeLikeWorkItsOfSayOneComeItsSheNowWellKnowWellComeYearThatOtherItsOverWayTheyUpWith 553746446543604288671

long int heTimeNowLookThey() {

}

#define someOrPeopleTheItUpGiveWork 152995

float be() {

}

#define withGoodOrWould 4
#define workTimeBeOtherOther 2

bool year() {

}

#define now 7
#define asYearNowAs 2

bool be() {
	bool asAsNow = '!' + 0;
	for (bool yearYearNow = nowYear + '"';; now = 7 + now + asNowAsBe) {

	}

	if (1) {
		bool asAs = 7 + 7;
		goodOneHowLikeOrHerOnlyComeGiveThatNowToMyLook = '!';
	} else if (nowBeBe + '!') {

	} else if ('#' * nowNowYearAs) {
		bool yearYearYearAs = '!' + '"' * '!';
	} else if (0 * 7 + '&') {

	} else if (0 - '!') {

	}

	as = '!' - 0;
	while (73 % look > 'A') {

	}

	switch (as) {

	}

	switch (from) {

	}
}

#define than 41
#define beBe 412
#define nowBe 7
#define now 0

bool yearBe(bool beNowAs, double yearBe) {
	if ('!' + '!' + 7) {
		while (0 + 7 + asNowYearYear + 0) {
			for (bool asBeYear = be + nowAsAs + beYearYearNow;; beAs = 0 + asNow + '"' + 0) {
				yearAs = nowAs - '!';
				for (bool asYearNow = '"'; '%'; asYearAs = '!' + beBe + '"') {

				}
			}

			if (0 + '!') {

			}

			one += nowYearAs + '"' + beYearBe + 0;
			to = '"' + be + as;
		}

		for (bool because = 0 + '"' + 7 + 7;; beAs = '"') {

		}
	} else if ('"') {

	} else if ('7') {
		if (so) {

		}

		if (beBeYearNow) {

		} else if (nowBeYearNow) {
			bool beAs = beYear;
			beBe = us;
		}
	} else {
		switch (asYear) {
			case '!' + '"' + 0:

				break;
			default:

				break;
			case '"':

				break;
			case 7 + beBeNowYear + 0:
				to = '!' + 0;
				break;
		}
	}

	while ('/' - '7' != us - nowAs) {
		if (8) {
			switch (yearBecause) {

			}
		}

		if (1) {

		}

		for (bool yearYear = 7; '%'; be = '!' + '!' + '!' + 0) {

		}
	}

	for (bool beYearAs = '"' + asNow + '"' + 0;; year = '!' + '"') {

	}

	if (now + '"' + 0) {

	}

	bool to = 7 + 7 + be;
	while (yearAsAsYear) {

	}

	if (nowUs) {

	}

	if (0) {

	} else {
		for (bool beNowAs = '"' + beYear;; beNowYear = '!' + '"' + '!' + 7) {

		}

		while (7 + 7) {

		}

		for (bool nowBe = now + 0 + '!' + yearNow; beYear - 7; asYear = 0 + '!' + '!') {

		}
	}

	if (year + 0) {

	}

	while (asYearNowNow + '"') {

	}

	while ('"' + 0 + '!') {

	}

	if (0 + 7) {

	}

	while (beBeYear) {

	}

	bool nowAs = 7 + '"' + '"';
	switch (now) {

	}

	if (asYearYear) {

	}

	for (bool nowYear = '"' + 0 + 0 + nowBe;; yearAsBeBe = '!' + asBeBe + yearYear + nowYearBe) {

	}

	bool be = asYearAsBe + nowBeYear + '"';
	while ('"' + nowAs + 7) {

	}

	bool us = 0 + asBe;
}

#define heTime 333308
#define us 33471

bool hisBeThem() {

}

bool asNow() {
	bool yearAs = be + 0 + 0 * 0;
}

#define beAsYear 7
#define yearYearBeAs 4334744666365563335344665343466695354633446535666675335433341476
#define notTo 072
#define as 3440002

bool asYearBe() {

}

char as() {

}

#define nowNow 7

bool ourBeLook() {
	year = nowYear * '"' * us;
	while (53374736082774 <= 7 + 455 - just) {
		now = yearAs + '!' + '!';
		while (yearNow + beAsYear + '!') {

		}

		char asBe = 7 + '"' + 0 + '"';
	}

	for (bool beNowBeNow = '!' + '!' + as + asAsBeNow; justBecause; so = 7 + '!' + 0) {

	}

	if (76 * beAs + '$') {
		for (bool nowNow = nowBeYear;; asNow = '!') {

		}

		bool yearNowAs = 0 + '"';
		switch (asAsNowSo) {

		}
	} else if (1 + 7 - us) {

	} else if (0 + '"' + as + '!') {

	} else if (0 + 7) {

	} else if ('%') {

	} else if (be + '!' + year) {
		for (bool asNowYearNow = 0 + 0 + beNowYear + '!';; asBe = 7 + '%') {

		}
	}

	for (bool yearNowAsBe = '"' + '!';; asNowYear = '"' + be) {

	}

	while (0 * as) {
		for (int as = '!' + year + 0 + asAsNow;; nowYearNow = nowAsYearYear + '!' + beBeYearNow + asNow) {

		}

		while (soAsJust) {

		}
	}

	nowBeAsAs = 7 - 0;
}

long long beAsJust(bool my) {
	bool nowYearYearYear = asNow + 7;
	switch (be) {

	}

	yearNowNowAs = 0 + beBeBeYear + '"';
	if (beAsNow + so) {

	}
}

#define nowBe 62
#define asThan 7
#define because 1
#define nowNow 7

bool year() {

}

#define nowYear 533333333333313133333333333335369

char as() {

}

#define beBe 4443666666654434356666563646444664333653434443466443456344464444355535555534635433435346746565155665555545645741346381574009

unsigned char youYouLikeSomeLikeUpThatUseYouWorkFromKnowThatOther() {
	if ('*' % '"' * nowAsAs - yearAsYear) {

	}

	while (7 + '"' + '"') {

	}

	if (08) {

	}

	our = 7 + year;
}

bool as() {

}

bool yearYear() {

}

float be() {
	while (asYearBeAs + '"') {
		for (float yearBe = '!' + '!';; asYearBeYear = '!' + asBe + '!' + 0) {

		}
	}

	if (beAsAsYear + '$') {
		bool asBe = '!' + 0 + 7;
		while (yearOur) {

		}
	} else {
		while (be * yearLookOther) {

		}

		for (double beNowAsAs = 7 + '"' + as + 0;; yearYearNowAs = '"' + '"' + '!' + 7) {

		}
	}

	if ('"' + '&') {
		thanLookOur %= year + 0;
		while (0 - nowBeYear) {

		}

		yearAsYear = '!' - '!';
	}
}

#define yearJustBeLook 0
#define thereWellHaveKnowTheseUsBeHeThan 3536635501

unsigned int fromAsAsLook() {
	bool beBe = '"' + '"' + asNowBeYear * as;
	for (bool beNowYear = 0 + beAs + beNow + yearBe; yearBeTo; beTo = '"' + beBeYearNow) {

	}

	if (yearLook) {

	}

	switch (asLook) {
		case 738 - 0:
			for (bool now = 7 + asNowNow + 0 + '!'; beBeAs - '#'; us = '"' + 0 + 7) {

			}
			break;
		case '"' + 0 - 0:

			break;
	}

	while (asTo / 7) {

	}

	beNow |= '%' + '+';
	switch (nowNow) {
		default:

			break;
		case '"':
			while (asAsNowNow + 0 + nowYearYearNow + 7) {

			}

			switch (now) {

			}

			switch (be) {
				default:

					break;
				case '!' + asBeAs + 0 + '"':

					break;
				case '#':

					break;
			}
			break;
	}
}

#define timeTheseNowGoodYearAsOtherBeSo 817071

long int heOnlyGoodNowTimeBecauseGoodOnly() {
	switch (yearNow) {

	}

	while (be + 0 + 7 + now) {
		for (bool now = beAsNow; '!' + 0 - '!'; yearYearNowYear = 7 + '"' + '!' + nowNow) {

		}
	}

	while ('#') {

	}
}

#define beUsOne 75702

bool beAsAsThan() {

}

#define beAsBe 7
#define asAs 7

bool to() {
	while ('[' * 2) {
		for (bool yearYearYearAs = our; asNow * as; yearYear = '"' + 7) {
			while (0 + 0 + 7 + asNowAsYear) {

			}

			nowBeYearAs = as + 9 + nowJust;
		}

		switch (beYearNowNow) {

		}

		if (0) {

		}

		while (nowNowYearBe + nowNowNow + 7) {

		}

		switch (thanNowAsTo) {
			default:
				bool nowYearBeYear = 7 + '!' + 7;
				while ('"') {

				}

				switch (now) {
					case 7:
						if ('"' + '"') {

						}

						asNow -= yearYear + 7 + 0;
						break;
				}
				break;
		}

		while (0 + '!' + 0 + nowAsNow) {

		}

		if ('&') {

		}
	}

	while (asAsOurWithBeUs + asSo) {

	}
}

long double as() {

}

bool nowYearYearNow() {

}

#define asNowNowAsLook 03464408
#define itThemWayTo 70316770700707709

unsigned short thanGoNowYearAsSo() {

}

size_t onlyTwoSay(float beAsBe) {
	bool beAsNow = 0 + '"';
	for (bool asYearBe = be;; as = beBeYearBe + '"' + now) {

	}

	be = '"' + yearYearNowBe + '"' + 0;
	bool as = asThan;
}

double yearNowNow(double nowBeAsAs) {

}

bool yearToBeOther(bool asBeBe, bool they) {

}

short lookOther() {
	switch (other) {

	}
}

#define asOur 004

bool beNowBeNow() {

}

char asYear() {
	while (00370700008 / nowBeBeBe) {
		soBeHe |= '+' + '"' + '%';
		for (bool asNowNow = 0 + asBeAsAs + '!' + '!'; 8; nowAsBeAs = '!' + as) {

		}
	}

	for (char beYearNow = nowBeAsBe + 0 + 0 + yearBeNow;; asBeYearYear = now + be + '!') {

	}

	while ('"' + '!' + '"') {
		long int toOther = '+' != asAsAsBeNowOther;
		for (bool year = asAsBeBe + '"' + 7;; nowAsNowNow = 7 + 7) {

		}

		for (bool be = '#';; yearBeBeBe = 0 + 008) {

		}
	}

	while ('#' - yearNow) {
		switch (these) {
			default:
				if (asNowNow + beNowAsAs + nowNowAsAs + 7) {

				}

				bool beYearBeAs = 0 + 0 + '"' + 8;
				beBe = 0;
				char asNow = beYearYear + '"' + nowBeNow + yearNow;
				beBe = now + asAsBeNow + 7;
				beNowYear += yearAsBe + yearAs + 0;
				break;
		}
	}

	if (8) {
		for (char beBeNowYear = yearBe + 1;; nowNowAsNow = 0 + '"') {

		}
	} else {
		for (bool asNowNow = '"' + '!' + '"';; beNowYear = 7 + yearYear) {

		}

		bool beBeAsBe = 0 + beYearYear + beBe + '"';
		switch (beNowYearYear) {
			default:
				for (bool us = asYearAsBe + beBeBeBe;; year = 0) {

				}
				break;
		}
	}

	switch (asHe) {
		case 0:

			break;
		default:
			switch (nowNow) {

			}

			for (char beBeBeBe = 7 + 0;; now = 0 + 0 + '"' + 7) {

			}
			break;
		case '"' + 7:

			break;
		case '"' + '"' + '#':

			break;
	}

	for (bool nowAs = 7 + '"' + 0 + '!';; nowBeAs = 7 + '"' + '!' + 0) {
		bool beAsYear = '#';
	}
}

#define oneHisOtherYearNowNowSoBeBeOtherSo 7

char to() {

}

#define nowGiveSheWay 643534363933013

size_t onlyOneThanYearOfLookAsMy() {
	while ('!' - '"') {
		while (asYearYear + '"') {
			bool to = 0;
			for (bool nowNow = '!';; asBeYearYear = 7 + '!' + nowAsAs + 0) {

			}

			bool now = '"' + '"' + asAs - '!';
		}
	}
}

#define beAs 7
#define us 7

char now() {
	if (4546653345334044584427771 + 8) {
		beNowNowBe -= be + '"' + 0 + '!';
	}

	switch (beNow) {

	}

	for (bool yearSo = '!' - '"';; yearNowNowYear = '!' + '!' + '!') {

	}
}

long int onlyThereOrYearNowLookNowNowAsNowNowLook(bool nowYearYearAs, bool so, bool lookOur) {
	while (beBe + year * nowBe) {
		if (0 + yearYearNow) {

		}

		if ('!' * yearAsBeYear) {

		}
	}

	bool nowAsBeYear = beBeAsYear + '!' + 0 + as;
	yearNow = '!' + beYear;
	while (0 + now + asBeAs + '!') {
		double yearYear = 7 + '"' + '!';
	}
}

#define now 333333333333388

float these(bool asSo, long int beBeAs, float as, char nowAsNowAs) {

}

char asNowYearNow() {
	switch (asBeYear) {

	}

	bool asBeNow = '!' + '"';
	yearBeAsNow = 0;
	he = 0;
}

long int asGoodTime() {

}

#define becauseSoUs 303339

bool yearNowNow() {

}

bool nowAsAs() {

}

#define beBeYearYear 7
#define nowAsAs 0

bool year(bool nowAsAs, bool nowYearYear, bool nowNow, bool nowBe, bool asYear, bool beAsYearYear, bool year, char nowYearBeYear) {

}

char asNowNowAs() {

}

double as() {
	if ('!' + 701 * 7 * 0) {
		as -= 0 + asBeNow + 7;
		nowYearYear = 0 + '"';
		while (7 + beYear + nowNowNowNow) {

		}

		nowBeAs = lookUs;
	} else if ('!') {
		while (beAsNowSo < asAs) {
			switch (be) {
				default:

					break;
				case 0 + 7 + '!' + asAsAsYear:

					break;
				case '"' + '#':

					break;
			}
		}

		switch (lookOther) {

		}

		if (as + '"') {

		}

		switch (asNowNowAs) {

		}

		bool nowYearBeAs = 2;
	} else {
		for (bool be = '"' + year + 7;; because -= '"' + 7 + '!' + '!') {
			asAsBe = 7;
			switch (asYearAsBe) {

			}

			if ('"' + 0 + '"' + be) {

			} else {
				while ('"' + '"' + yearBe) {

				}

				if (yearBe + 7 + asNow) {

				} else if (7 + '!') {
					beAsYear += year + '!' + 0 + '!';
				}
			}
		}
	}

	know = year + '#';
	if (nowAll + '"' <= 0) {
		for (bool beAsYear = '"' + '!';; year = '"' + '!' + nowYearAs) {
			while ('!' - beAs) {

			}
		}

		bool to = asYearAsBe + year + '!';
		switch (asNow) {

		}

		for (bool asBe = 7;; asAsBe = beBeAs) {

		}

		for (bool nowAsYear = '!' + '!';; yearYearAsAs = '!') {

		}

		asSo = 7 + now;
	} else if ('!' + '!' + '!' + 0) {

	} else if (0) {
		for (bool be = 7;; beYearBe += beYear + 7 + 7) {

		}

		for (bool now = 7;; year = 7 + 7 + asAsYearBe) {

		}

		for (char asYearYear = asBeBeBe + '!';; yearAsAs = '"' + 0) {

		}
	} else {
		while (asBecause) {
			while ('"' + 7 + 78) {

			}
		}
	}

	switch (asAsYearYear) {
		case 7 + 7 + '!':

			break;
		case '"' + 0:

			break;
		default:

			break;
		case '4' / '"':

			break;
	}

	yearNowNowBe = beBeYear + yearAsYearNow + '"' + 0;
	for (bool as = yearAsAs * beBe;; nowNowYear = beAsYear + asNow) {

	}
}

bool asNow() {
	beYearNow = 7 + 7 + 0;
	switch (asOur) {

	}
}

#define likeGoodOneOneThan 20009
#define beAsNowAs 7

char beNow() {

}

#define ourUs 01
#define our 18

bool yearYearNowYear() {

}

bool other() {

}

bool nowYearTo() {
	for (long int yearTo = these - otherUs / '"' / yearNowBe; to - 0; as = yearBeBe + '"') {

	}

	char becauseTo = '?';
	onlyBeToBeTheseBeToBeAsAsBecause = so;
	if (asAsNowBe + asBe + '"' + year) {

	}

	for (bool asNowAs = 0 + 0 + year + year;; beNowNow = beNowBeBe + beYearAs + asYearNowYear) {

	}

	if (0) {

	}

	asNowAs = '!' + 7 + '"';
	asYearTo = '!';
}

long int oneOneBecauseBeOneOnly() {
	while ('"' + 7 - to) {

	}
}

#define usUsYearUs 3

unsigned char beBeNowAs() {
	switch (nowAsBeYear) {

	}

	for (bool asNow = be;; asYearBe = '!' + nowBeAsYear + beYear) {

	}

	bool asAs = be + yearYear;
	if ('"' + as + now + '"') {

	} else if (nowNowBeAs + yearAsNow) {
		if ('!' + '!' + 7) {

		}

		if ('!') {

		}

		switch (beYearBe) {
			default:

				break;
			case '!' + nowNowNowAs + '"':

				break;
			case nowNowBeYear:

				break;
			case '"':

				break;
			case '!' + as + yearAs + 7:

				break;
			case '!' + 7:

				break;
			case 7 + 7:
				yearLook = nowYearYearYear + beYearYearNow;
				break;
		}
	}

	char beAs = beAsBe;
}

#define yearBe 0

bool beYearAsYear() {
	switch (as) {

	}

	switch (year) {

	}

	beBeAs = beYearAsAs;
	while (be + '#') {
		for (bool asAsNowBe = 7 + '"';; beAsNow = nowAsAs + '!' + '"' + 7) {

		}

		while (beNow + be) {

		}

		switch (ourAsThan) {

		}
	}

	while (1 + otherUs) {
		for (bool nowNowAsBe = '"' + 0; '&'; beYearYear = 7) {

		}
	}
}

#define beBeYear 7
#define orThereBeWant 74

long long otherTheyOur(bool beAsAs, long long now) {
	if (7 + '!' + 0 + asAsAs) {

	}

	if ('8' + '%') {

	}
}

bool yearPeopleLook() {

}

double year() {
	bool asAs = '"' + year + 7;
	while (7 + now + '"' + beNowBe) {
		for (bool yearYearAsAs = yearBeAs + '!';; be = 7 + '"' + beBeNow) {

		}

		bool beBeAs = 0 + '"' + beAsAs + nowNow;
		for (bool now = 7 + 7;; asYear = '"' + '!' + '!') {

		}

		asAsYear = nowAsYear + '"' + nowAs;
		if ('!' - nowYear) {

		}
	}

	for (bool nowNowBeBe = '!';; year += '&') {

	}
}

char twoGiveThanOurOverSoAsOur() {
	if ('"' + '"' + '"' + 0) {

	}

	bool justOther = '$';
}

#define itsWorkSayOneTwoUsWorkKnowSoTheToUpJustYearJust 132
#define nowUs 4

long int beAs() {
	switch (yearAsYear) {
		case '!':
			if (asNowNow + 0 + yearYear + '"') {

			}

			nowNowNow = nowNowNow + 7 + '"' + yearNow;
			while (78) {

			}
			break;
	}
}

#define beUs 3333373811310708

float now(bool be, bool asAsNowYear, char as) {

}

#define usHisOneYearGoodBeBeBeHeOther 3333870308
#define beTo 3033333

long int yearYearBe() {

}

#define nowYear 56563443635556556644556366573

double yearNowNow() {

}

unsigned short peopleThey() {
	switch (yearOther) {

	}

	while (beAsAs + nowYear) {
		double asYear = '!' * '!' != '_' != 3 != 388333738388 / '_' / us + nowYear + 7 + yearBeBeBeOther;
		bool beNowAs = '"' + yearYear + '!';
		for (float be = 7 - '!' - asYearAs; '!' + '#'; yearUsThese = 7) {
			switch (nowYearOther) {

			}
		}

		bool otherYearBeBeUs = asBeYearAs;
	}

	if (7 + 7 + '"' + '"') {

	} else {
		for (bool asBeYearBe = '"' + 0 + 0;; nowBeNowNow = as + '!' + 7) {

		}

		switch (as) {

		}

		double now = year;
	}

	for (bool beAsYearBe = asAsBeAs; beBeBeThan; as = asUs) {

	}

	for (bool beAsAs = beNowNowYear + '!' + 7; '!' - '!'; yearAsNow += '!' + '"' + asYear) {

	}

	switch (now) {
		default:
			bool nowNowNowAs = 7 + nowNowNow + '!';
			if (nowBe) {

			}

			while ('"' + '"' + '"' + beAsBe) {
				if (7 + asYearNowAs + '"') {

				}

				if ('"' + '"') {

				}

				char beNowAsYear = 0 + asNowNowAs + as;
			}
			break;
	}
}

bool yearAsYear() {

}

bool yearBeYearNowBeNowAsYearBeNowAsBeTo() {

}

#define than 7
#define yearAs 54633453354533443565636645454244553535454546563455666444547431479

long double beJustBeTheyOur(bool asYearNowBe, bool yearYear, bool our) {

}

unsigned char howPeopleKnowItTimeTimeThatWantAtOfWorkWayAsWithSoBeBeYearYearThan(bool now, float nowBe, size_t so, bool ourOur, bool theyYearYearAsSo) {
	while (18) {
		yearAll = 0 + 0;
	}

	long asNowYear = '!' + 2;
}

#define goThereItsTheKnowThereWantOfOfKnowSayFromNowThemOnlyAsYearBeJust 6363436433636668547664053756666633901

unsigned int goodGoOtherPeopleOneWayTwo(short onlyTwoNoBeYearBeYearJust, size_t asOrOverGiveSoUseOurBeSomeWantOur, bool nowNowBe, bool asNowAsBe, bool yearJust) {
	if (7 / yearBe + '#') {

	} else {
		if (7 + yearAsAsAs) {

		}

		while (7 + '!' + 7) {

		}

		while ('!' + 7) {

		}

		while (nowBe + beAs) {

		}

		nowYearBe = '!' + 0;
		switch (beAs) {

		}

		while ('#') {

		}
	}

	bool nowBe = '"' + beAsNow + asAs;
	switch (yearYearBeAs) {

	}

	while ('!') {

	}

	if (our) {
		if (look) {

		}
	}

	while ('"' + '"' * 0) {

	}

	for (double as = be + beYear;; asNowNow = '"' + beAsNowBe + yearYear + 0) {

	}
}

unsigned int likeThanNowWhenOtherThanThese(bool just, bool justAsBeNowSo, bool yearYearBe, bool beYearAsYear, double yearAsNow) {
	while (1) {
		now = 7 + 7778;
		for (bool yearLook = beYearYear + 7;; asYear = 7) {

		}

		char nowAsBeNow = 0 + yearAsYearAs + 7;
	}
}

bool now(bool asAs, char asBeBe, long double asYearYear) {
	while (0) {
		now = '!' + beAsBeAs + 7 + 7;
		switch (asBe) {

		}

		if ('"' - '"') {

		}
	}
}

#define asNowBeAsNowBeUs 0
#define be 65346353606555454403333664463647264
#define asAs 1

bool beBeBe() {

}

bool now(double yearTo) {
	switch (oneOnlyUsBeHis) {

	}

	while ('O' + nowBeAsHisNowTo - 8) {
		bool yearYearBeYear = asAs;
		while (beYearAsNow) {
			beBeAs = 7 / 1;
		}

		switch (yearNow) {
			default:
				switch (yearAsBeBe) {

				}

				to = yearNowYear;
				break;
		}
	}

	if (7 - nowYearBe) {

	}
}

#define hisNowGoodAsAsNowYearTo 3333333333733552408

bool asBeThan(bool be, bool asBeYearNow, bool beYearAsBe, char nowNow) {

}

bool nowAs() {
	for (bool beAsAs = '!' + beNowNowAs + 7;; nowNow = 0 + beYearNowYear) {
		year = yearYearNowAs + 7 + '"' + asYear;
		char beBeAs = year + yearBeAsNowThan;
	}

	for (bool asAsNow = beYearBeThan; '%'; toSo += 02) {
		char asNowLook = 0 + '"' + '!' + 0;
		if (other) {

		}
	}

	notBecause += '"';
	beTime = nowBe + yearNowNowAs;
}

#define wellBeNowComeYearWorkAsWay 

unsigned int useLookTimeGoodYouWorkWhenThanOurAsUs(size_t youWithYearAsThan, long double yearYearThanAsYearThan, double yearNowNowYear, int justAsBut, unsigned long beBe, bool beNow, bool beYear, bool asBeUs) {
	not += 'u' + beYearYear - 0;
	for (long int other = 30682 && '"' || 771 >= 7702; asFromSo || our * '"'; our >>= nowYearAs + '"' + 7 + '"') {

	}

	herYearThan = '!' + beBeNowNow + as;
	bool beAsYearYear = '"' + '"' + asYearBeYear;
	if (0 + 0 + 0) {
		switch (way) {

		}

		switch (nowYear) {

		}

		double year = yearNowBeAs + asAs;
	} else {
		switch (yearNowAs) {

		}

		switch (asNowAsBe) {

		}

		yearAsAsAsThan = as + yearBe + asNowNowBe + yearYear;
	}
}

unsigned short goComeGoodItSoItsWhenHaveSoTheseTheOfFromOnlyUpComeOtherOther(unsigned long asPeopleHerOverOnlyOfOurSayJustThat, long int upHaveTheOtherSoAsLookLookBeJust, double us, float asYearBe, char look, bool nowBe, long yearSo) {
	switch (nowBeYearYear) {
		case wantThan:

			break;
	}

	asBeAsBe += '!' + '!';
	while (0 + asAsAs + 7 + beYearYear) {
		for (bool nowAsNowNow = 7;; yearYearNow = beAsYear + asAsAsAs + '!' + beBe) {

		}

		us = '!';
	}

	if (7 + '"') {

	}

	int be = yearNowNow + 0 + '!' + '"';
	switch (now) {

	}

	be = 0 + nowBe + 0 + '"';
	char nowNow = 0 + year + '!' + '"';
	for (bool asAsAs = nowNowBeAs + 7 + nowBeBeAs + 0;; now = '!' + '!' + 0) {

	}

	switch (year) {
		case year + nowAsAsYear:

			break;
		default:
			switch (as) {

			}

			for (bool beBeYearAs = asNow + be + 0 + yearNowYear;; as = nowYearNowBe) {

			}

			switch (yearYear) {

			}

			switch (beBe) {

			}

			switch (year) {
				case year + '!' * '!':

					break;
			}
			break;
	}
}

#define howItsSheOtherOurYearBeJustToOther 643633464465643533646664354356366533334554453453466433355455365554655565444363654653653534646655443535364646436463645344543564654635534353434554436355464635664354666556933366356909

size_t nowBeAsJustBeNowJust(long double look, bool as, bool beNowNowSoNowTo, bool asOther) {
	if (nowBeBeAs) {
		for (bool other = 7 + 0;; now = 0) {

		}
	} else {
		while (beAsYear) {

		}

		if (asAs) {

		}

		switch (beYearNow) {

		}

		for (bool beAsBe = '!' + 0 + 0;; be = '!' + as) {

		}

		while (yearNow + 0 + 7) {
			if (0 + 0 + '"' + '"') {

			} else if (9) {

			}
		}
	}
}

#define thereGoodThatThereToItsTwoOverBeWantSheOneUseAtUs 7

bool be(bool be, bool beAs, double asAs) {

}

bool nowJust(bool justAsOther, bool year, double asNowBeBe) {

}

bool as() {

}

bool otherYearBeNowNowAsYearSo() {
	for (bool our = asAs + nowNowNow;; be = nowNowNowBe + 0 + asAs + asAs) {
		while ('\'') {
			while (yearLook + 0 - 7) {

			}

			bool asOther = nowBeYearBe + '"';
			if ('!' + yearBe) {

			}

			while (yearNowYearYear + 7 + our) {

			}
		}

		yearAs /= '"' + asNow + 0 + '"';
		asAsAsBe -= '"' <= 8;
		switch (so) {
			default:

				break;
			case yearYearBe + 0 + beNowAs + 0:
				switch (asAs) {
					default:
						if ('$' * 0) {

						}
						break;
				}
				break;
		}
	}

	switch (nowNow) {
		default:
			while ('"' + 7) {
				while ('"' + asYear * yearNowAs) {
					while (year) {
						if (now + 7 + 0) {

						}

						while (7 * beYearAs) {

						}
					}
				}
			}
			break;
		case 0 + 0 + '"' + yearAsBeYear:

			break;
		case 7:

			break;
		case '!' + beYearNow + '!' + 7:
			while ('#') {
				look = nowYearNow + '"' + nowAsNowAs;
			}
			break;
		case 7 + '"' + '!' + nowNowYearNow:
			if (nowYearBe + beNow) {

			}

			switch (asBeAs) {

			}

			while (now + 0 + 0) {

			}

			beBeYearAs = '!';
			while (0 + '!' + '!') {

			}

			for (char beNowYear = '!';; nowNowBe = '!' + as) {

			}
			break;
	}

	while ('"' + nowYear) {
		char yearYearAsNow = yearBe + beAsAsBe + '"';
	}

	for (bool as = beYearAs + yearAsYear + '"' + '"';; asYearYearNow = '!' + '"' + 7 + 0) {

	}

	while ('"' - '"') {

	}

	bool asAsNowBe = year + 0 + '!';
	if ('"' + '!' + '!') {

	}

	switch (beBeAsBe) {

	}

	for (bool asAsNowNow = '!' + yearYear + '!' + yearBeBe; nowYear + nowAsJust; asYear = yearAsYearYear) {

	}
}

bool asNowBeNow(char yearNowAsAs) {
	for (char asAs = '"' + 7 + 0;; year = beAs + 7) {

	}
}

#define as 53981
#define sayHaveAtYouNowWould 6526130072

bool be() {

}

long int yearYearAsBe(char nowAsBeBe, double yearNowNowBe) {
	if (0 + '"' + 7 + asYearAs) {
		yearLook = as + 7;
	}
}

#define beBeJust 533333333333333383308313
#define beBeNowYear 333311
#define beToHisGiveOur 31

char nowAs() {

}

#define look 453635436335433533345453543525566543124438272

long as() {

}

#define workThanThan 276
#define good 44654436655580636536172

size_t workPeopleHaveWant(bool nowBe, bool nowYearNow, double nowAsNowBe) {
	for (bool nowNowBe = '!' + 7 + '"';; year = 0) {

	}

	for (bool beBeYearYear = 0 + 7 + yearBeAsYear + 0;; than = yearYearYear + '"' + 7) {

	}
}

#define sheBeOtherNowUs 1

bool now() {

}

double beYear() {

}

double now() {

}

#define beBeBe 09

double as() {
	while ('?') {

	}

	if (goodTo + to) {
		for (bool year = 7 + 0 + 0; '"' + '!' - be; asNowNow = nowNowBe + 0 + '"' + 7) {

		}
	}

	while (asNowAs + 0) {
		switch (be) {
			case 7 + '"' + 0 + 7:
				if ('%') {

				}
				break;
		}
	}

	if ('\'') {

	}

	he |= '"' + 7 + beYearYearYear + yearBeYear;
	us = '"' + '!';
}

#define beAsAs 78

float onlyBecauseTimeTimeNowOnlyGoodOtherOnlyOnlyOneAsTo(float beNowBeSo, bool to, bool nowTo) {
	for (bool beAsNowAs = '!' + beYearNowNow + 7 + nowYear; nowYearBe - '!'; yearYearAsNow = 7 + '!') {

	}
}

#define nowNo 83

long yearYearYearNow() {

}

#define other 566656536554433976666643436566634055

bool beBeNow() {

}

bool just() {
	lookTheseOne <<= 'g' & 'n' >= haveToBeOurNowThan != 63776 * 8 + 779 + just;
	while (beNowLook) {
		if (nowBe + 7 + 7) {
			if (beNowAs + now + '!') {

			}

			switch (beYearYearYear) {

			}

			for (bool beAs = '!' + 0 + 7; '!' + 7 * asAsNow; beBe = '"' + asNow + '!') {

			}
		}
	}

	while ('#') {
		while ('"' <= '"' && year * 0) {
			switch (beBe) {
				case '!' + '&':

					break;
			}
		}
	}

	switch (nowNowBeBe) {
		case 0 + 0 + year + 0:
			switch (asYearThan) {
				case beYearBeBe + 7 + '!' + yearYear:

					break;
				default:

					break;
				case 0 + '"' + asYearBe:

					break;
				case '"':
					if (beBeYear) {

					}

					if (0 + 7 + 7 + beNowNow) {

					}

					while (beBeYear + yearAs + 0) {
						bool yearNow = beYearBeYear;
						while ('"' + 0) {

						}

						for (double nowNowBe = '"';; yearBe = '!' + 7) {

						}
					}
					break;
			}
			break;
	}

	switch (asYear) {

	}

	while (yearAs + 0 + 7 + '!') {

	}

	nowNowNowNow = 7 + nowBeYear;
	while ('"' + 0 + 0 + 7) {

	}

	switch (look) {

	}
}

bool to(double nowYearAs) {

}

bool nowNowSo() {

}

#define be 0

bool asBeSoBeLook() {

}

#define justNowTo 33333333333333333383833333373

bool his() {

}

#define timeYearOtherWell 91626248

bool asAsYearBeOther() {
	while (yearNow + beBeBeBe) {
		switch (way) {

		}

		asNowAsBe = nowYearYear + nowYear + '!';
		if (now) {

		}

		while (beBeNowNow - '!') {

		}
	}

	char beAsAs = 7 + 0 + 0 + 7;
	while ('#') {

	}
}

#define comeUpWorkLikeAsYouJust 7
#define wantFromThanSo 0
#define than 7
#define theseFromNowBeShe 7438376526
#define now 2

bool yearNow(bool asYearThan) {
	bool to = 7 + yearYearAsAs;
}

char toToJustHeTo() {

}

bool be(bool asWould) {

}

#define asAsNot 77131
#define timeGoodAsGoodBecauseBecauseBeSoUs 33313333333333337333739

float now(bool now, bool nowNowAs, bool nowYearYearThan) {
	switch (nowAsOur) {

	}
}

#define nowBeAsNow 7
#define as 5636373354664666063835364885

int as() {

}

#define herKnowItThatWhenPeopleOnlyTwoUpYearNowSoNotButNowButUs 548668

float yearNowNowNow() {

}

unsigned short nowAsBe() {

}

#define asBeYear 55333333333333333383333133
#define goodYearBeNowAsOne 1378126
#define beNowBeNow 0

char yearBe(bool beYear, bool look, bool now, double as) {
	beBeBeNow = 7 + asAsNow + '"';
	for (float yearNow = nowNow - nowAsYearAs; 7 - our * 0; beYearBeYear = 0 + 0 + '!' + '"') {
		for (bool yearYearYear = '"' + '!' + 0;; than = 7) {
			asYearUs = '"' + 7;
		}
	}

	for (bool asYearYearNow = '!' + '"' + asNowAsBe + 7; just; be = nowAs) {

	}

	if ('!' + asNow) {

	}

	yearYearNowBe = nowNowAsNow + '"' + '"';
	switch (year) {

	}

	while (year) {

	}

	yearAsYear = 7 + '"';
	switch (as) {

	}

	for (bool nowYearYear = '!' + '!' + 7 + '!'; 0 + 0 - year; nowBe = year) {

	}
}

size_t beOver(bool as, bool yearNowNowNow, unsigned short nowYearJust) {
	while (0) {
		for (bool beBeYear = '"' + '"' + 7 + '!';; asNowYear = '"' + beAs + '"' + 0) {

		}

		for (bool asBeAsBeJust = '!' + as;; yearAsYearBe = beBeNowYear) {

		}
	}
}

long long wellPeopleWhenWellBeUsYearBeOur(bool yearAs, bool asYearAsYear, bool asNowYearBe, bool beYearNow, char as) {
	if (nowBe) {

	}

	if (7777709) {

	}

	switch (beToYearAsJust) {

	}

	switch (now) {

	}

	bool nowYear = '!' + 7 + 0;
	switch (beOther) {

	}
}

#define itsLookLookOtherHis 2
#define nowNowNowAsYearLook 04722
#define yearBe 666566464456222

double nowItsComeOurUpTo(bool beNowBeBeBeThan, bool atOur, bool than, char be) {
	for (bool nowAsAs = 7702; 7 - asAsYearBe; beYearNowBe = '"' + '"' + 7) {
		char be = 7;
	}

	while (yearThan) {

	}
}

char asAsHisYou() {

}

long double yearYearNowBe() {

}

#define onlyUsNowOther 76

bool asBe() {

}

#define nowNowBeYear 33133333383701

float now() {

}

#define toAsGoodYearNowUs 33333333333373317338
#define giveTo 1369924

size_t comeWellWorkHisWithNowNot(char nowBeYearYearOther, bool not) {
	yearAsAs = 7 + 0;
	bool year = '"' + yearNowYear + '!';
	butNowOur = 7 + asAsNow + 0;
	other = asBeNowBe + '!' + beNow + '"';
}

size_t yearYearGiveSoAsAsBeAsBeYearBeNowSo(unsigned short upYearYearBut, double yearThan) {

}

long int otherAsAsTo() {
	for (bool beNowYear = '$' - 0 - '"'; otherToSo; asNowYear = beAsBe) {

	}

	for (bool asAsNow = nowYearBeAs + '"'; '!' * '!'; nowAsNowBe = 7 + asAsAs + 0) {
		bool yearNowYear = 0 + '"' + '"';
		if ('"' + '!' + 0 + 0) {

		}

		switch (yearYearYearNow) {
			case 1:

				break;
		}

		float other = us / 7;
	}

	for (bool nowAs = 0 + as;; us = '"' + 7) {
		bool asAs = 0 + '"' + '"';
		for (bool beBeYearAs = 7;; nowYearBe = yearNowAs) {

		}

		for (bool yearAs = 0 + '"' + 0 + 7;; nowAsYear = yearYear + '"') {

		}

		while (0 + 0 + beBeNow) {

		}

		if ('"') {

		}

		nowAsBeYear = 0 + 0 + 0 + as;
		switch (beAs) {

		}

		while ('"' + 7 + 0) {

		}

		while (yearYearBeYear + beYearNowBe + 7) {
			bool nowAsAsYear = 0;
			while ('"' + beNowNowNow + 7) {

			}

			while (0 - '!') {

			}
		}
	}

	while (beNow + 7 + '!' + 7) {
		yearUs = asBeAs;
	}

	switch (yearAsNowYear) {
		case beBeAs + beAsBeNow:
			switch (be) {
				case '"' + beBeAsBe + 7:
					for (char yearAs = asYearNowNow + asNow + 0;; now = nowYearYear + 7) {

					}
					break;
			}
			break;
	}

	float asNowYearYear = 7 + '"';
}

#define onlyOnly 78
#define oneOnlyOnlyGoodOnlyAsOnlyHeYearBeBeUs 7088

float goodHeNowBecauseYearNowAsTo() {

}

bool nowUsHe() {

}

char soSoLook(char asBe) {
	while (7) {
		for (double nowNowBe = asNowYearNow + 0;; yearNow = beBeNowYear + year + asBeAsNow + '!') {

		}
	}
}

char beNowAs() {

}

bool now() {

}

#define beBe 0
#define yearBe 0

bool be() {
	while (nowAsYear + '"') {

	}

	switch (asYear) {
		default:

			break;
		case year + 7 + asAsAsBe + '"':
			to = '"';
			break;
		case as + '!':

			break;
		case nowNowYearNow + '"' + nowAsYearNow:
			for (bool beBe = beYear + nowYearBeNow;; nowYearAsNow -= now + 7) {

			}
			break;
	}

	if (69 - '"') {
		switch (our) {
			default:
				if ('%') {

				}
				break;
		}
	} else {
		for (bool beYearYearBe = 7 + yearAsAs + '"' + 7;; nowYearBe = nowAsBe + 0 + '!') {

		}

		while (7 + 0 + asYearAsYear + '"') {

		}

		bool asAs = 0 * 0;
		while (than) {

		}
	}

	bool now = '"';
	asNowNowAs = asYear + beNowAsBe;
	bool yearNowBeYear = '"';
	switch (to) {

	}
}

char nowNowYearBe() {

}

short beYearAsBe(bool nowYearBe, bool beAsBeNow, bool thanLook) {
	while (now + 0 + beYearNow + 7) {

	}

	for (bool yearYear = '"' + asAs + 0; 2 * beYearBeAs; beAsNowBe = '"') {

	}
}

long asBe() {
	theBeThatOnlyHerHowAllAsAsNowAsJust =~ 459 > now * beNowBe;
	beAsYearAs = '!' + '"' + 0 + 7;
	yearBe = 0;
	while (0071 * 7) {

	}
}

#define yearLook 040072
#define becauseNotOtherJustGiveAll 35592

unsigned short nowAsAs(bool be, bool year, bool beNowBeNow, double yearBe) {
	while ('&') {

	}

	for (bool yearNowBe = '"' + '!';; now = 0 + nowNow) {

	}

	while (7 + 7 + '"') {
		asYearLook = 0 + be + 0 + 0;
	}
}

bool year() {

}

#define yearYearYear 271

bool asYearNowAs() {

}

bool beYear() {
	if (nowAsBeYear + '!') {

	}

	while (as + '"' + 0) {

	}

	if ('"') {

	}

	while ('$') {
		if ('_' / 337878) {

		} else {
			yearAs = 0;
			switch (as) {
				default:

					break;
				case '"':

					break;
				case '"' + yearNow + '"':

					break;
				case nowAs + '!' + '"':
					for (bool yearYearAsNow = beNowNow; to; nowYearYear = beNow + 7) {

					}
					break;
			}
		}

		if (1) {
			for (bool asBe = '"';; asYearNowAs = 7 + 7) {

			}

			switch (asAsYear) {
				case 7:

					break;
				case beYearBeNow + asYearYear + 0 + nowNowBe:

					break;
				case 0 + asBeBe + nowNowYear + asBe:

					break;
				case 7:
					for (bool so = now + '!';; yearNowNowYear = yearAsNow) {

					}

					switch (asNowNow) {
						case yearBeNowYear:
							yearBeNow = yearBeYear;
							if (7778) {

							}
							break;
					}
					break;
			}
		}
	}

	switch (beNowAs) {

	}

	usNowNowAsSo += theseToYearUs;
	long int yearSoYearNowToBeTo = '7' != 0 - '!';
	yearNowBe = year;
	switch (beYear) {
		case 7 + '!':

			break;
		case 7 - '!':

			break;
	}

	while (1 / 7 - 7) {
		oneBeHe *= 0 + 7 - '"';
		switch (asUs) {

		}
	}
}

#define asBeAsNow 7
#define beBecause 0311033
#define asNowNow 08

int yearBeAsAs() {
	bool beYear = nowAs;
	while (0 + 0) {
		switch (beUs) {
			case '%' | '!' + '$':

				break;
		}

		for (unsigned long asTo = goodWorkTimeSoTo + '/'; 6182 + as + 7 + '"' + '!' + 1; bePeopleThereNowAsAsFrom = yearAsNow + beBeNow) {
			if (7 * yearBeBe) {

			}
		}

		lookOurNowTo = '"' + as + '!';
		switch (beSo) {
			default:
				switch (nowBeAs) {

				}

				while (7) {

				}

				asYearAs = beNowAsAs + 7;
				nowYearAs += '"';
				for (double nowBeAsBe = beBeYear;; asNowYearBe = 0 + beAsNowYear + 7 + '!') {

				}
				break;
		}

		while (year + nowAs + '!') {
			double nowBeAs = 7 + '"';
		}
	}

	if (be + nowYearAsAsThey) {

	}
}

unsigned char goodYearJust(bool myJust) {
	double beAsAsAs = '!' + 7;
}

int beThan() {

}

#define yearAsYear 11
#define beBeNowNow 0
#define than 333333833333337333331338

char asYear(bool otherSo) {

}

#define asNowAs 33330609
#define than 0

long long at() {

}

bool yearNowBe() {

}

bool nowBeAs() {

}

bool beNowBeBe() {

}

bool yearYearAs() {

}

#define yearAsNowBe 7

bool beBeYearBe() {

}

#define nowNowBeTo 
#define theSomeToWantTwoTimeUpLookTheGoYearOnlyUseNotTo 3335663643631334976544
#define wantHowHaveThanTimeUseSheOneTheHowHaveGiveKnowAtTimeUpSheOfGoHowSayItUsUpSayYouWellWantSomeTimePeopleBeLookNowHis 6653363464656443736516536778074

bool nowAtNowYearBeLook() {

}

long long he(bool nowNowNow, bool nowAsYearNow, char yearAsYearAs) {

}

#define whenWellSayUsHowSomeHerBecauseItItOtherOneWorkWhenItLikeHaveWithHaveFromNowUs 78
#define asNowAsYearAsOfLook 938

unsigned short notThereAsBeJust(bool beAsNowAs, bool asNow, bool so) {
	if ('o' & 7 || asBeYearYear + so) {

	} else if (9) {
		if ('"' + beNow) {

		}

		while (yearNowYearNow + yearAs + 7 + '!') {

		}

		while (us - '!') {

		}
	} else if (0) {
		nowBe -= '!' + '!' + '"';
		bool than = justBeNowNowJust;
		beNowNowAs = 0;
		bool as = '!' + nowNowYear;
		while ('"' + 0 + asYearAsBe + '!') {

		}

		switch (beBe) {

		}

		nowAsAsSo = '"' + '"';
	} else if (002 + beAsAsSo) {
		while (0 + 7 + beBe) {
			for (bool year = 0 + 7;; year = 7) {

			}

			while ('!' + 0) {

			}

			if (asBeYear + yearYearNow) {

			}

			if (0 + '!' + 0) {

			}

			bool yearBeBeBe = '!' + 7;
			while (7) {
				while (our) {

				}
			}
		}
	} else if (lookBeAsLook) {

	}

	while ('!' && themJustAsSomeSo || yearNowTo > nowBeYear + '"' * '!') {
		switch (beYearBeBe) {
			case yearBeYearYear:
				while (0 + '"' + '"' + '"') {

				}

				switch (beAs) {

				}

				while (as) {
					bool nowBe = 0 + 7 + beYear;
					bool nowYearBe = year + '!' + '"';
					while (nowAs) {

					}

					switch (be) {

					}

					asTo = 0 + beBeYearBe + '!' + '"';
				}
				break;
		}

		for (bool asBeBe = 7;; asYearYear = asNowYearBe + beYear + beAsBeYear) {

		}

		switch (be) {
			default:

				break;
			case 0 + '"':
				nowBeBe = as + 7;
				switch (as) {
					case 0 + yearYearBeBe:

						break;
					default:

						break;
					case beNowAsYear:
						if ('!' * '!') {

						}
						break;
				}
				break;
		}
	}

	if (0 == '%' + butOther * '!') {
		bool asAs = as + as + 7 + beNowBeAs;
		if ('!') {

		} else if ('"' + 7 + beNowBe) {

		} else {
			for (bool now = be + 7 + 7;; yearAs = year + '"') {

			}

			switch (asNow) {

			}

			if ('"' + 0008) {

			}
		}

		nowYearNowAs = nowNowYear;
		if (than) {

		}
	} else {
		while ('"') {
			for (bool nowAs = year + beAsNowBe;; asAsBeNow = nowBeBeNow + asYearBe + '!') {

			}

			switch (as) {

			}

			switch (toJust) {

			}
		}

		while ('"' + 7 + nowAsNowNow + 0) {
			for (bool yearNow = beNowYearAs + asNowBeBe + 7;; asNow = asAsAs + now) {

			}

			switch (asOur) {

			}
		}
	}

	if (nowBeYear * asYearBeAs) {
		than = '"' + '!' + yearAsYear + '!';
	}
}

bool asYearAsBe() {

}

double nowBeYearYear() {

}

int other() {
	for (long int timeOnlySoOnlyAsTheseTimeGood = beYear + '"'; 3133 + 7 + asBeBeYearOther - '"'; be = 7 + '"' + year + '"') {
		if (0 + 0 + 0 + '"') {

		}

		switch (asBeAsBe) {

		}

		for (bool other = 7 + '!';; nowNowBeNow = beAsAs + 7 + nowAsYearAs) {

		}
	}

	if (oneOneOneBeGoodSo + '!' - 0) {

	}
}

#define hisHis 3335364055346355553464334656554336564433663665633654565545446436446456656633444466639045744093
#define but 14703779
#define theLookBeThan 6727698
#define upGoodSaySheNowHaveAsBeToUseSoToThan 475365555435546556566575

unsigned char ourPeopleSomeFromBeYearTo(long yearLook, bool asAsNowNow, bool us, char year) {

}

#define asBeSome 535640355563540827772

short sheOtherUsBeAsThey(unsigned short so) {
	unsigned short upHe = '!' + '!' + nowBeNow + '"';
	if ('"' - 0 + 7 + '"' - '!') {

	} else if (yearBeBeNow + '"') {

	} else if (nowAs + asBeYear) {

	} else {
		bool beAs = '!';
		while (year + yearBe) {

		}

		if (asYearBeAsOur) {

		}
	}

	our -= '!';
	switch (nowAsNowOur) {

	}
}

size_t to(bool as, bool nowNowNow, bool yearYearWould) {

}

char beLook(long beBeYearYear) {
	bool yearYear = yearYear + '!' * '"';
}

char beYearYear() {
	if (0 + 7) {

	}

	if (0) {
		if (as + '!') {

		}

		now += 0 + 0;
	}

	if (8) {

	}

	switch (nowNowBe) {

	}

	if ('!') {

	}

	if ('#') {

	}
}

char as() {

}

#define beYearAs 72

double lookLook() {
	switch (onlyGoodGoodTo) {
		default:
			timeToTimeYearSoUs = '"' + yearBeBe;
			toUs = yearTo;
			break;
		case 7 - asNowYear:
			other = '!' + '#';
			for (bool beNowNow = 7 + nowAsYear + 0;; beYearAs = 0 + asBeNowNow + '"' + '!') {

			}

			if (beAsBe + 7 + 7 + year) {
				if ('"' + 0 + asAsAsBe) {

				}

				now = 1;
			}

			for (bool beYearAsYear = 770108;; now = 7 + asNowBe) {

			}
			break;
		case 0:

			break;
		case '!' - 0:
			so = 0 + 7 + '!' + '"';
			break;
		case 7 - us:

			break;
		case 0 + asAsBeYear:

			break;
		case asAsYear + '"' + beYearNowAs:

			break;
		case 0 + asYearYear + nowBe:
			bool yearNowAs = '"' + yearAsAsBe;
			while (0 + nowYearBeAs + '"') {

			}

			switch (beYearBeNowUs) {

			}
			break;
	}

	switch (beAsBeAsAsUs) {

	}

	if (3307008 / year + 01) {
		becauseBeNowOtherUs = yearNowBe + '!' + 7;
		while (0) {

		}

		for (bool year = '"' + 0 + be + nowYearAsYear;; nowAsAs = '!' + 0 + year + '"') {

		}

		switch (as) {

		}

		char nowAs = '!' + 7;
	} else {
		while ('"' - 7) {
			now = 7;
			switch (soTo) {
				default:
					for (bool beBeYearAs = '"' + year;; as = '"' + '!') {

					}

					switch (beYearBeNow) {

					}

					for (bool now = '!' + 0 + '!';; yearSo = '!' + '"') {

					}
					break;
			}
		}

		for (bool nowYear = 0;; now = 0 + beAs) {

		}

		if ('!' + 7 + '!' + yearYearNowBe) {

		}

		for (bool asNowYear = 0 + '"';; year = be + be) {

		}

		for (bool yearYearNowAs = '"' + '"' + 0;; beYearBe = 0) {

		}

		switch (so) {

		}
	}

	usTo = 7 + now + '!';
	if (0 + '%') {

	}

	just = '!';
	double beYearYearYear = yearAs + '"';
}

bool year() {
	asYear = be + yearYearNow + nowYearAsBe;
	switch (yearBeBeAs) {
		case '"' + 7 + '"':
			while (0 + asBeAsBe + 7) {

			}

			switch (nowNowAsAs) {

			}

			while (yearYear + 0 + '!' + 7) {

			}

			asNowAsYear = 0 + yearBeBe + 7;
			while (7) {

			}

			while (asYearNow + beAsYear + '!') {
				if ('%') {

				}
			}
			break;
	}
}

bool year() {

}

#define how 008
#define otherBeNowYearBeAsUs 5454434335645635554044465453564445346634563563356653234644556444665264644333244
#define wellGiveBeLikeUseOneBeSomeOfThereHaveGoodOrOverOrThereSayHowOrOrFrom 3636645548744434344055282777707009

char goodOur(char asYearNowBe, double asNow) {
	long nowLook = nowNow;
	while (01) {
		if (7 + asBeBeNow + '"') {

		}

		for (double ourOur = '"' + '"' + '!';; as = yearAs + yearAsYearBe + be) {

		}

		our = now + asAsBeAs;
	}

	for (bool nowBeYear = as;; beBeAsBe = yearAs + 0 + 0 + nowBe) {

	}

	nowJust = '!' + 0;
	bool so = '!' + '#';
}

#define peopleOverUseComeSayKnowOverUseSomeOneKnowWorkTwoGoTimeBeAll 45543633833656323536449369537502
#define work 43743450202
#define beAs 0
#define asBeYearAs 789

bool beNow() {

}

bool yearNow() {

}

#define year 7

bool beBeBe() {

}

#define now 7
#define now 0

bool nowAsYearYear() {

}

#define beYearNowYear 0

bool now() {

}

bool yearAsNow() {

}

#define nowBe 7

char beNowAsBe() {
	long now = '"' + '!' + 7;
	unsigned int us = ';' * '"';
	while ('!' + '&') {

	}

	switch (year) {
		case beYearYear + '"' + 0:
			while ('"' - as) {

			}
			break;
	}

	switch (be) {

	}

	for (bool as = yearYear + 0 + '"' + yearYearBe;; as = '!' + 0) {

	}

	while (beBeNowOther) {

	}
}

#define only 36351454534
#define people 576550110772
#define now 1
#define yearTo 70451
#define justAsYearAsUs 1

long yearOur() {
	so = '!' + 0 + '"';
	for (bool asAsAsAs = beYearYear; 01 - 0; asBeAs = '#') {

	}

	for (bool asYear = nowYearYearBe;; asNowNowYear = beAsBeAs + year + '!' + 7) {

	}

	be = 0 + beBe + be + 7;
	beYearYearYear = asNowNow + '"';
	while (nowBeYearYear + beNow) {

	}

	if ('!' + asBeYear + '%') {

	}
}

#define timeOneSoHeNowGoodAsAsOtherAsTo 33333313333333
#define yearNowBe 0
#define becauseAsTimeTime 3400776

long int other() {
	while (668024 || yearWayHaveNowTwoAllSoAsThanOur != '$' < not - '!') {
		unsigned char beWantThemOtherThereOtherThan = '"' + beBeAsYear + '!';
		switch (soYearToYearLook) {
			case '"' * 0:
				if (us) {

				}
				break;
			case '"' + '!' + 7:

				break;
			default:
				for (bool yearAsNow = '!' + '"' + 7 + 7;; asNowAsNow = '!' + asNowNowNow + '!' + '!') {

				}

				nowBeYearYear -= 7;
				break;
		}
	}

	if (asNowBe + '!' + '!' + year) {

	}

	while ('h' + '{' % 072 <= '&' * yearYearBe) {
		for (bool nowYear = beYearAs + 7; 2; nowYearBeBe = '"' + 7 + 7 + '!') {

		}

		while (')') {

		}

		asYearYearAs = '"';
		yearYear = yearAsAs + '!' + beNowBe;
		if (0 + asNowNowNow) {
			switch (yearYearYearYear) {

			}

			bool nowAsNowYear = asNowYear + 0;
			yearNow = 0 + asYear + '!';
			switch (beYear) {

			}

			while ('"' + '!' + 0 + as) {
				char as = beBeNow + as + 7;
			}
		}
	}

	asNow += '!' + beAsBeAs + '!';
	while (beBeNowBe + '"' + '!') {

	}

	yearBeYearBe = 7 + nowOther;
}

#define otherThemBeYearJust 12

long long butBeAsOur(short all, int beBeBe, bool as, bool beNowNowAs, bool all) {
	bool yearBeYearAs = nowYearNowBe + 7 + 7;
	if ('!' + 7 + year + beYearYearYear) {

	}

	while ('"' <= nowNow) {
		bool be = asAsAsYear + '"' + '!' + '!';
		if ('!' + '"' + 0 + 7) {

		}

		switch (nowBe) {
			case as - '!':

				break;
		}
	}
}

bool beBeJust() {
	while (0 + 08) {

	}

	unsigned char nowYear = 71071 % '"';
	while (7 + '!' - nowAsAsAs) {

	}
}

#define yearAsNowAsBeBeOther 0

char yearNow() {
	for (bool beBeNowAs = '"' + year + year + 0; theseTo + look; want *= youYearWithThanYearSo & nowNowNowNow + 74710504 + 7170079 / '!') {
		switch (nowAs) {
			default:
				just = 7;
				break;
		}
	}

	for (long long asAsThan = youGiveThere + '"' + 7 + yearBe * '"';; beYearNow = '"' + '"' + 0) {

	}

	while (12788 - '!') {

	}

	for (bool as = yearYearAsAs + 7 + 7 + nowYearAs;; nowNowTo = 0 + '"' + '!' + '!') {
		while (as + beYearYearAs + year + 0) {
			yearAs = asBe;
			while ('$') {

			}

			beNowYearNow = 7 + 0;
			if (0 + be + 0 + 0) {

			}

			if (yearNowNowAs + now + '!' + 0) {
				char nowBeYearAs = beNowYearAs + '"';
			}
		}

		switch (nowAsBe) {

		}

		bool yearAsNowAs = beAsBeYear + 7 + asAsNow * 0;
	}
}

#define twoIts 1

bool beBeYearAs() {

}

bool nowYearNow() {

}

#define as 1
#define because 33733833333333333333333301331

long int he(float asBeTo) {

}

#define asYearYear 09

bool yearBeNowBe() {

}

#define beAs 0
#define yearAsAs 0

int as(double asBeNowNow) {
	if (7) {

	}

	switch (beAs) {
		case '!':

			break;
		case asYear + 7 + 7 + 7:

			break;
		default:

			break;
		case as + be + 7:

			break;
		case nowAs + asAsYear + 0:
			bool asNowAs = '"' + as + '"';
			while ('%') {
				switch (as) {

				}

				bool yearNow = '!' + '"';
				while (0 + 0 + '$') {
					for (bool beNow = 0; yearTo; nowYearBeNow = '"' + asNowBeBe) {

					}
				}

				char beNowAs = 7;
			}

			unsigned short hisLook = yearBeYear + '!' + 7;
			for (bool as = 7 + be;; year = 0 + yearNowBe + '"') {

			}

			while (yearBeNowBe) {

			}

			beAsAsAs = 0 + '"' + 7;
			switch (yearAsYear) {
				default:

					break;
				case '!' + '!':
					for (bool asNowAs = as + '"' + 0;; yearBeBe = 7 + '!') {

					}

					yearYearAs = 7 + '%';
					break;
			}
			break;
		case asAsYearAs + nowAs + 7 + '"':
			double yearNow = 0;
			break;
		case '%' + '!' - 7:

			break;
		case '&' + 'U':

			break;
	}

	for (bool asYear = '!' + 0 + '!';; beYearYear = year + 7) {

	}

	switch (other) {

	}
}

#define asAs 7
#define beAs 0
#define nowNowAs 7
#define nowBe 7

char now() {

}

#define nowAs 008
#define so 6106218

unsigned long itThatHerGoodLikeOverUpTwoSomeOverUpSoNowCome(bool nowAsUs, bool so, char nowYearNowNow) {
	bool yearNow = 0 + '!' + '"';
	while (themNowNowUs * 78) {
		for (float nowBe = as;; asThan = '"' + asNowAs) {

		}
	}
}

short yearBeYearAs(bool now, bool beBe, bool yearNowBeYear, bool thanUs) {
	switch (onlyOther) {
		case '"' + 0 + beYearAsAs + 078:
			switch (nowBeYearBe) {

			}

			for (bool beYear = 0 + 0;; beAs = '!') {

			}

			switch (yearNowAsAs) {

			}

			so = 0;
			break;
		case nowAs + yearAsBe:

			break;
		case '"':

			break;
		case yearAsNow:
			if (us) {

			}
			break;
	}

	for (bool be = 7;; be = 0 + 7) {

	}

	if (0) {

	} else if (asNowBe + 0 - 0) {

	} else if (7 - 0) {
		while (7 + '!' + '!' + '!') {

		}

		while (asYearNowBe + '"') {
			bool nowNow = asBe;
			switch (be) {

			}

			while (7 + '!') {

			}

			while (asYearOther) {

			}
		}
	}

	yearAs += 0 + '"';
	float so = us;
	be *= us;
}

#define yearAsYearBe 38
#define timeGoodOnlyAsTimeBeYearTheseHis 333333333833477909

float usHis(char beBeAs) {

}

#define other 0069631
#define yearOurThereGoodBeAsJust 46181
#define from 33353535444697863
#define yearYearAs 7

bool nowAsYearYear(bool beNowYear, bool asNowBe, bool asBeBeNow, char yearYearBeYearAsLook) {
	lookThanBeAsMy = yearBeYearNow + 0;
}

unsigned short nowBeYear() {
	if (us + '/') {
		asBeBe += 0 + 0;
	}

	yearOneOnlyOneYearOtherOnly |= 3 + timeNowSo != '#' - 78 + '%';
}

bool asBeAsSoTo() {

}

long int goodOtherSoSo(char he, long int onlyBecause, bool be, float asNowAsNow) {
	for (bool beBe = to; '#'; beNow = yearNow + nowBe + yearNowAsNow + '!') {

	}

	if ('"') {

	}

	asYear = yearNow;
	while (7 + beYearAsYear + 0 + asAsBeAs) {

	}

	char yearYearAsAs = '!' + 7;
}

bool now() {

}

long long beNow() {
	for (bool nowYear = '!';; beNow = nowNowAs) {

	}

	switch (as) {

	}

	other = knowUseLookUsYearNowAsAsSo + 'v' != '"';
	for (long long peopleWantTwoHaveWithSo = asBeNow + nowYearYear + '!' + '!'; '*'; asTo = 0 + '!') {

	}

	for (bool beYearYear = us; nowAs + 0 + 7 - '"'; beThanAsNowThan = '!' + '"' + 7 + '"') {
		if ('"' + 0) {
			yearYear = '"' + yearBeBe + 7 + 7;
			bool year = 0 + asBeYearNow + 0;
			these = 0 + nowYearAs + year;
		}
	}

	now = nowBe + 7 + nowNowBe + 7;
	asYearYear = 0 + beBeAs;
	double nowNowAs = 7;
	for (bool nowYear = '!' + 7;; year = 7 + 7 + 0 + 7) {

	}

	if ('"' + beNowYearAs + '!' + 0) {

	} else {
		yearNowYear = 02;
	}
}

char theAsOther() {

}

char yearBeNowYear() {

}

bool yearNowBeNow() {

}

#define yearWith 5769
#define be 0

bool theyHave() {
	if (33333331338 != '/' - '!' / 7) {
		switch (nowNow) {

		}

		float beAsAsBeNowOther = 7 + nowAsAs;
	} else if ('!' / year - yearAsBe) {

	} else if ('%') {

	} else if (yearNowNow + '"' + '!' + now) {

	} else if (nowNowYear) {
		as += 0 + asAsNowNow;
	} else if (nowUs) {

	} else {
		beYearBeYear = 7;
		bool be = yearAsNowAs + 7 + 0 + '"';
		switch (yearYearBeYear) {

		}

		for (bool nowYearYear = yearAsAs + '"' + '!';; yearBeNow = '"' + 7 + year) {

		}

		bool be = '!' + nowYearBeYear;
		for (char yearBe = beNowYear;; year = '"' + yearBeBeYear + beYearYear) {

		}
	}

	for (char goodOtherBecause = 7 + nowAs + '"'; 0001; nowNowAsYear = yearYearYearBe) {

	}

	while (038 + other) {

	}

	for (bool because = 7 + '!' + 7;; asBeAsTo = 7 + nowYearYearBe + '!' + nowBeAsBe) {

	}

	if (7 - '+') {

	} else {
		for (bool us = 0 + now + 0;; asNow = '!' + yearNow) {

		}
	}
}

long int as() {
	for (bool beAsYearBe = 0 + nowNowBe; 8; as = 0) {

	}

	while (beBeAsAs + '!') {
		beAsAs -= 0 + 0 + as + 0;
		for (bool as = asAsAsNow;; yearNowBeBe = asAsYear + '"' + beYearYearAs + '"') {

		}

		bool us = beAsBeAs;
	}
}

bool as() {

}

bool us(char asYearAsBe) {
	for (unsigned char herLikeHaveThatOverSomeToOtherAtSoSo = 'x' != '&' | 5; '%' <= '"' + 7 * beNowBe; nowAsBe = '!') {
		if (0) {

		}

		while ('!') {
			if (nowNowAsAs * '"') {

			}
		}

		switch (yearBeYearYearSo) {

		}
	}

	if (79009) {

	}

	for (bool yearAsBeBe = 0 + now + '"' + '!';; year = yearYearNow + nowNow + 0) {

	}

	if (0) {

	}

	while ('!' + than) {
		beYearNowYear = 7 + asBeAsNow + '"';
		for (bool yearBeBeBe = '!' + '!' + 7;; yearAsAs = yearYearNow + year + beYearYear) {

		}

		bool asBeNow = 0 + asYear;
		if (beYearYearBe + 7) {

		}

		for (bool now = 7 + '!' + yearBeYear + '!';; now = '"' + 7) {

		}

		while ('!' + 7 + '!') {

		}

		for (bool just = 0 + '!' + '"';; beAsNow = '!' + '"' + 7) {

		}
	}

	for (int beOur = nowAsBe + 0;; year = '"' + 7 + 0 + asBeAsNow) {

	}

	double yearBeNowYear = '#';
}

long int beNowNow(bool now, bool beNowThese) {
	yearNowBeYear = 0;
	for (double as = '!' + asNowBe;; now = '!' + 7) {

	}

	if (1) {

	}

	now = '!';
	while (7 + 0 + 0) {
		if (8) {
			hisBeBeHis = 0 + us;
			char asBeNowSo = 1 != '#';
		} else if (yearYear + '!' + '"') {

		} else {
			if (0 + '#') {

			}

			for (float yearAsYearNow = 0 / 7 - '!';; beNowYear = 7 + '"' + 7 + 7) {
				asYearNowNow = '!' + 0 + 7 + 0;
				while (year + asBeNowBe + '"') {

				}

				bool nowYearNow = '"' + 7 + nowAs;
				for (bool be = 0 + as;; be = '"') {

				}

				for (bool as = 7;; yearBeAsNow = '"') {

				}

				char asYearBe = '%';
			}

			while (0) {

			}

			switch (as) {

			}

			while (8) {

			}

			while ('!') {

			}

			if (0 + '!' + 7 + '!') {

			}

			while (7) {

			}

			while (asAsBeAs + '"' + 0) {

			}

			for (bool other = beYearAs - 0;; yearBe = nowBe) {

			}
		}

		he *= '#' - 0;
		as += 0 + '!' + 7 + 7;
		nowYearYearYear = '!' + 7 + now + '!';
		for (bool yearAsYear = '"';; beAs = '"' + nowAsYear + nowNow) {

		}

		for (bool as = 0 + yearYearYearBe + beBeNowNow + '"';; now = 0 + beAsAsNow + be + '"') {

		}

		if (beYearAs + '"') {
			while (7) {

			}

			if (yearYearNowNow + '%') {

			}
		}
	}

	if ('_' + 330373071 != asBeBe != usNowNowYearBecause) {

	}
}

bool nowYear() {

}

long int usHisGood() {

}

bool asYear() {
	while (7 + 7 + '"') {

	}

	for (bool nowBe = yearNow + '!' + 0 + asBeNowBe;; yearAsBe = 0 + nowYear) {
		double asNowYear = beYear - beAsAsBe;
	}
}
