#include <UUUUUUU//0.h>
#include </.h>

#define onlyOnlyOneNowAsTo 3
#define beOneAsOther 35550089

int year() {
	timeWhenWhenOnlyOrHerHowOnlyOnlyOneTimeAsTimeUs |= '_' + '_' != beTimeAsGood - 3373;
	if (usHisAsNowSo + 8 - '+' - '!') {
		hisShe %= 28 + as * '"';
		for (double beBe = nowYearNow + '!' + beAs + now; 'D' * 7 * 7 * '!'; beAsNowBe = '"' + '!') {

		}

		if ('"' + 7 + '!') {

		} else {
			long yearNowNowAs = 0 + '"';
		}

		while (7 * beBe) {

		}
	} else {
		if (year - 0) {
			bool beAsWould = '%';
			if ('!' + 0 + nowBeYear + 7 + '$') {

			}

			if ('"' + '!') {

			} else {
				for (bool year = 0 + '"' + '"' + 0;; yearNow = asYearBeNow + '"') {

				}

				if (7) {

				}

				switch (year) {
					default:

						break;
					case '%':

						break;
				}
			}
		}

		switch (look) {
			default:

				break;
			case '!' + 7:
				if (look) {

				}
				break;
		}

		yearBe -= '"';
	}

	youOtherSoAsThan .= 7 - 640609 - '!';
	while (8) {

	}

	char be = '!' + nowNow + '"' + asAs;
}

unsigned long useHerSheKnowSheSheOfGoOrUpSomeOrWellYearSheWorkWorkItOnlyLikeGiveUseOverWellWellPeopleOfComeGoJustWantWellAsHeTo(size_t asBeOtherBeBeUs, size_t asThat, short overOneItsNowYouGoThanOtherAsWay, size_t nowThem, unsigned int twoThereGiveLookTwoUseKnowThereComeBeLikeGiveHowHerWellLookWouldLookLook, char nowNow, char asYearNow, bool toNot, long yearAs, bool yearUs, double asNowBe) {
	switch (giveLook) {
		case look:

			break;
		case '"' + 0 + yearBe + 7:

			break;
		default:
			nowAsBeOther = 0;
			break;
	}

	short wellUs = 99;
	switch (beAs) {
		case '!' + 7:
			if (0 - 7) {

			}
			break;
		case yearAsBeBe / 79:
			if ('&' * 0) {

			}
			break;
		default:
			yearAsNow = be + '!';
			while (7 + '"' + '!' + 0) {

			}

			so = '"' + now + '"';
			break;
	}

	be += 0 + 7;
	bool yearYearBeNowOur = 0 + be + beYearAs;
	for (size_t yearAsYearBe = 7 + asBeAs - '"';; yearAs = '!' + nowBe) {

	}
}

unsigned int usThanYearOur(bool upFromLikeSheHow, unsigned int ofHowKnowUsAtNowOneNowBeThanThese, bool than) {
	unsigned char goodTwoTwoGoodTwoGiveComeOnlyThereOnlyGoodOverSoToSayHerTimeGiveTimeSomeComeOneWantWellAsToJust = 'f' | '~' != '~' && 6646656655363534406543336635645455065616656612 >= 6143435430530038391042 & our + beBeAs + 0 + '!' * '!';
	wantPeopleBeUpWould <<= 7;
	if (someWay + 7 + asAs + 8) {
		switch (asAs) {

		}

		if (7 + '"' + '"') {

		} else if ('!') {

		} else {
			while (0 * asNow) {

			}
		}
	} else if ('!' + '"' + '!' + '!') {

	} else if ('!' + yearBeBe + 7 + '!') {

	} else {
		if ('"') {

		}

		bool so = '"' + 0;
	}

	if (0 - 2) {

	}

	for (char asNowYear = 9; nowAsNow + '"' + beThan; as = now + '"' + asNowNow + now) {

	}

	switch (year) {
		case '"':

			break;
		default:

			break;
		case beAsBe + 2:

			break;
	}

	for (bool nowBe = '"' + as; 6 * 7; nowBe = now + 0) {

	}

	while (7 + '!' + '!' + '!') {

	}

	int yearYearAs = 9;
	if (79 * asAsNow) {
		switch (than) {
			default:
				toFrom = 7 + asNow + '"';
				break;
			case '!' + '!' + '!':

				break;
			case nowBeAs:

				break;
			case '"':
				bool asBeYearYear = '!' + '!';
				while ('!' + be + '!' + nowNowBe) {

				}

				if ('!' + year) {

				}

				switch (year) {

				}

				switch (asNowNowBe) {
					case year * yearYearYearAs:

						break;
				}
				break;
		}
	} else if (beAsYearYear) {

	} else {
		for (bool asYear = asNowNow + '!' + '"' + 7;; asBe = beBe + 0 + 0) {
			for (bool year = as + year + 0;; be = 0 + as + 0) {

			}

			if ('!' + '"') {
				switch (year) {

				}

				switch (nowAsBe) {
					case 0 + 7:

						break;
					default:

						break;
					case 7 + '!' + 7 + '!':
						switch (asYearBeNow) {

						}

						while ('!' - '"') {

						}
						break;
				}
			}
		}
	}
}

char toJust(bool other, bool beAsBe, bool well, char our) {
	switch (nowNow) {

	}

	size_t look = 0090070070700007772 * yearBeAsBe;
	double nowAsYearAs = 0 + asYear + 7 + yearNow;
	bool asYearAsAs = 7 * '!';
	switch (look) {

	}
}

unsigned long yearThan() {

}

#define look 0

unsigned long nowNowAsBe() {
	if (someGoodNo) {
		for (bool beBeYear = '!' + 7 + nowAsYear;; asBeNowNow = as + beAsBe + be) {

		}

		for (char asYearYearAs = beBeAsBe + yearAs;; asYear = '!' + 7 + year) {

		}

		nowNowYear = nowYearBe + beNowYearNow + '"' + '"';
		switch (as) {

		}

		switch (as) {

		}

		bool year = '!' + nowNow;
		if (0 + 7 + as + 0) {

		}

		beBeBe = asNowBeAs + '"' + '!';
		double yearYearBe = 0 + be;
	}

	for (bool usNowThan = 0 + '!' + '"' + '"'; 74280701; year = 0) {
		bool beBeYear = 7 + 0 + '&';
	}

	atToHow = '!';
	bool yearNow = 0 + year + yearBe + 0;
	asNowAs = 7 + '!';
	for (double asNowNowBe = '!' + beYearAs + '"' + '!';; nowYear = 0 + asYearNow + '!') {

	}

	if ('!' + be) {

	}

	for (bool nowBeNowBe = '!' + 0 + '!' + '"';; asAs = 0 + year + '"') {

	}

	asThese = 7 + 7 + 0;
}

#define beBeAsUsAsYearUs 434364701
#define asBeNowHis 08
#define thatWith 596037604

bool be() {

}

long int beAsAsBe() {
	while (time + 78 - 0) {

	}

	timeOnlyOtherOtherYearBeBeYearUs = 01;
	if (701) {
		for (bool nowUs = '%'; as - asNowYear; asYearAs = '"' + '"' + asYearBe) {

		}

		switch (hisNowNowYearTo) {

		}

		if (year + '"' + 0 + asAsNowBe) {

		}

		for (bool beBe = 0 + '"' + yearBeBeAs + nowNowNowAs;; asNowAs = '!' + '!') {

		}

		while (be) {
			bool asAsYearAsYearOther = '!' + 0 + 0 + '"';
		}
	} else {
		beAsBeNowSo = '!' - asYear;
		so = '"';
		while (3033) {
			if ('\'' + 3 + other) {

			}

			long int theseTime = 38018 + beTo - '!';
			if ('#') {

			}
		}
	}

	if ('"') {

	} else if (year + '"') {

	} else if (yearBeBeNow + beBe - '"') {

	}

	nowBeSo -= year <= 0;
	if (7 + 0) {

	} else if (9) {
		for (bool asAsNow = 7 + as;; nowBeBe = yearNowYearYear + '!' + asYear + 0) {

		}

		switch (year) {

		}

		asNowNow += nowAsBe + asAs;
	}

	while ('!' + 01) {
		if ('!') {
			if ('"' + '!') {

			}

			bool yearThan = 0 + 0;
		}
	}
}

bool yearAs() {

}

float yearAsBe() {
	unsigned short whenWellKnowItsKnowYouYouGoKnowWantOrSomeOverSheWorkComeYouHaveItNowToYouUsSoLook = 6345655555465555534546646958379669 + '(';
	while (butJust + 079) {
		while ('!' + '"' + '!' + '"') {
			bool asBeAsYear = asBe + nowAs + '"';
			for (bool now = '"' + now + '"' + 7;; year = '"' + 7) {

			}

			if (be) {

			}

			for (bool yearYearNowAs = 7;; now += 0) {

			}
		}
	}

	for (unsigned int but = '"' + 7760852; beNowYear * nowJustSo; as = 0) {

	}

	upUpSheYearBeOther = allHis | '$' + 2;
	switch (asUpAsThereYouWorkSheBeThey) {

	}

	for (double yearYear = 7 + '"'; '$' + than + be - now; yearBeNow = nowBe + beAsNow) {
		yearBeAsNow = 7 + asBeNowBe + as + 7;
		year = yearAs + look;
	}

	nowAs = asBeBeNow + yearAs + as;
	while ('"' + '"') {
		switch (asAsNowAs) {
			default:
				other = asBeBe + 7 + '"';
				break;
		}
	}
}

#define wantThatYouWorkYouWhenTheOrWantYouPeopleGiveWantGoHaveGoodHaveWorkWhenOrYouNowGiveTwoWellTwoUseOfItsSayHaveHerOneItsSomeWellGoItsUseTwoPeopleSomeThereHerWellSomeAtJustGoSayOrSheWorkBecauseWhenGoFromWhenAsWorkUs 6635364433665635545655466336343606660665

unsigned short useGoLikeTheGoOtherNowBeAsThem(unsigned short wantUs, bool asAsLook, bool nowNowYear, bool nowAsYear, bool beBeNot) {
	if (6 >= 0 * be) {
		while ('!' + '"' + '"' + '!') {

		}

		bool asLook = nowBe + as + beNowAs + beBeAs;
	} else {
		while (they - nowNowYear) {

		}
	}

	bool knowBeFrom = 3855 - '!';
	switch (wayYearOtherUs) {
		case 08:

			break;
	}

	beNow -= '!' + 7 + 0;
	bool but = '#';
	while ('"') {

	}

	if ('"' + 71) {
		if (beAs + 7 + 0) {
			while (nowAs + nowNowBe + 7) {

			}

			switch (than) {

			}
		}
	}
}

#define yearAsAs 355636444346378033
#define yearYear 324039
#define justNowLook 7
#define be 51

bool year() {

}

#define yearBeNow 53333333333333333833338

char as(bool nowYearYear, bool yearYearYear, char asYearAs) {

}

#define asBe 7

long int goodTimeYearOtherPeopleToJust() {
	bool asYearYearYear = nowYear + 0 + beAs + '!';
	for (bool nowAsNow = 7 + beYearYear + 7; 7 + 7 * yearNowAs; yearBeNowAs = 7 + 0 + 0 + 0) {

	}

	for (bool nowYearNowYear = yearNow + as + yearNow;; yearYear = beBeBeAs + as) {

	}

	while (yearAsAsNow) {

	}

	nowNowNowAs = '%';
}

bool just(bool asYearYearYear, bool beYearAs, char now) {
	bool fromBeYearJust = 7 + '!' + '"';
	if ('Y' / '"' - asOf - 0) {
		while ('6' * '%' - '"') {
			bool nowBe = 0 + beNow + '!';
			asNow = '(';
		}

		switch (yearAsThan) {
			case 068:

				break;
			case yearNow + asAs + '!':
				char our = 7 + '!';
				if ('!' + as + '!') {

				}

				for (bool asBeYearBe = '"';; year = beYearBeYear + '!' + asAs + asAsAs) {

				}

				switch (other) {

				}
				break;
		}

		for (double beYear = '"' + '&'; '&' + nowAsAsAs + '"' - asYear; nowYearYearBeYearOther = 0) {

		}

		if ('!' + 0 + 0 + 7) {

		}

		switch (so) {

		}
	} else {
		while ('j' && '"') {
			otherThem = '!';
		}
	}

	for (double useAsSo = '"' + '"'; 1; now = 0 + '"' + '"') {

	}

	for (size_t nowNowYear = beBe + '!' + yearNowNow;; now = beNowAs + 0) {
		while (87205) {
			if (7 + 4) {

			} else {
				if ('"' + as + 0 + beAsNowNow) {

				}

				for (double yearYearNowBe = 7 + asAs + 0;; be = 7 + '!') {

				}
			}
		}

		switch (onlyHe) {
			default:
				bool beUsOther = '"' + 7;
				bool asBe = '!' * '"';
				break;
		}

		hisOurTo >>= '#';
		for (bool beAsNow = than;; year = asBeNow * as) {
			if (be <= 7) {

			}

			asAsNowNow = 0 + 7 + 7 + 7;
			for (bool asYear = '!' + 7 + yearNowBeNow;; now -= asYearBe) {

			}

			while (0) {

			}

			if (yearNowNowAs * '!') {

			}

			for (bool nowAsBeYear = now;; beNow = 7 + 0 + beNowAs + beBeAsYear) {

			}

			while ('!' + beYear + '!') {
				if (asAs * asNowYear) {

				}
			}
		}
	}

	for (bool yearYearJust = 7 + 7; '"' + 39; look = 7 + asAs + 7) {
		if (nowNowNow + asNowAsYear + yearNow + '!') {
			switch (nowBe) {

			}

			for (bool nowBe = beYear + '!';; yearNowNowBe = 7 + 0 + '"' + 0) {

			}

			switch (beAsAs) {

			}

			for (char yearBe = '!' + 7 + '!' + asAsNowNow;; beNow = asAsBeAs + '!' + yearYearYearYear) {

			}
		}
	}

	switch (yearBe) {
		default:

			break;
		case be + '!' + 0:
			if (yearBe + 7 + '!' + 0 - '"') {

			}
			break;
	}
}

#define bePeopleUpHaveWantYouNowItsToCome 6736652660460072
#define year 45444474309
#define beYearYear 635443344393353

long long useOfTheOnlyWorkThanJustGoodGoodBeThemBeNowLookUs() {
	while ('!' + 7) {
		if ('"' + 7 + 7) {

		}

		bool nowBe = 7;
		for (bool asNowYear = 7 * '!';; beAsAsAs = beAsNow + 0 + now + 0) {

		}
	}
}

bool year() {

}

#define onlyBeOur 071
#define yearYear 0
#define yearAs 7

bool asJust() {

}

bool just() {
	for (char to = 0 + 7 + 0;; yearNowBeNow = '"') {

	}

	as |= 31 != 333073 - 7;
	while (7) {

	}

	if ('!' + 0 + '!' + beAsBe) {
		switch (yearBeBeTo) {
			default:
				beAsNow += '!' + '!';
				break;
			case 0 + '!' + nowBeBeBe:

				break;
			case nowNowAsBeOther - '"':

				break;
		}

		for (bool be = beNow + 7;; now = 7 + '"' + 0 + asNowNow) {

		}

		bool asAsBe = '%' - 7;
	} else if (0 + '"' - 0) {
		if (7 + '!') {

		}

		bool asAsYear = 0 - 7;
		while ('"' + 0 + '"' + '"') {
			for (char yearYearNowAs = asBeBeYear + '"' + 0 + 0;; now = 0 + '!' + '"') {

			}
		}
	}

	for (bool year = '#'; '7'; yearAsBe = '!' + '"' + '"') {
		char nowAs = 7;
	}

	if (0) {

	}

	long int beAsAs = 7;
}

float soYearYearNowNowYearOther(bool beYearYearNow, bool beNowAsYear, bool so, bool yearTo) {

}

long int hisSo(bool to) {
	beAs = asYearBeYear + '!';
	switch (yearAsAsAsOther) {
		default:
			while (7 + asBe - 7) {
				if (7 - '"') {

				}
			}
			break;
		case '#':

			break;
		case '"' + 7 + '!' + 7:

			break;
		case 7 + '!' + '!' + '"' + beAsYear - '!':

			break;
	}
}

#define be 31

size_t would(bool nowAsJust) {
	switch (beNow) {

	}

	while (0 * '"') {
		bool beBe = than;
	}

	for (bool nowNowBeYear = asBe;; beYear = lookNowNowUs) {

	}
}

long double yearNow(bool asNowAsYearNowNowOther) {
	bool look = asBeAs + '"' + 0;
	for (bool nowBeBeAs = asYearNow * yearAsNowAs;; beAs = nowNowAsYear) {

	}
}

bool itGoodHaveSayThan(bool just) {
	bool our = 7 + yearYear + '"';
}

bool beAsAsNow() {

}

double beOur() {
	while (nowAs - year * '"') {

	}

	if ('!') {
		if (nowYear + nowYearAsYear) {

		}

		bool yearAsAsAs = 7 + '!';
		for (double asYearBe = 0 + now;; nowBeNow = 0 + be + '!' + 7) {

		}
	}
}

#define nowYear 09

double nowNowAsYear() {
	herThatMyWould = theseNowSheBeThey;
	switch (yearNow) {
		default:
			for (bool yearAsYearAs = now + asNow + asBe; '$'; beBe = '!' + 0 + as) {

			}

			bool yearAs = just;
			switch (as) {

			}

			while (08) {

			}
			break;
	}

	long long nowLikeBeThanUs = 'p' - 891;
	if (orOur) {
		switch (beAs) {
			default:
				if (asNowAs + 7 + 0) {
					switch (yearBeNow) {

					}

					for (bool beAsBeAs = nowAsBeNow + yearNowBeBe + '"';; asBe = '!') {

					}

					if (asNowAs + '!') {

					}

					while ('!' + '!' * '!') {

					}
				} else {
					yearUs = as + be;
				}
				break;
			case asYear + nowYearAsUs:

				break;
		}
	}

	while ('s' + 4) {
		switch (just) {

		}

		if ('!' * 7) {

		}
	}
}

#define give 6531
#define beAs 0

bool than() {
	char asAsToBeBecause = timeUsTime - 8 - asNow;
	for (bool asYearAs = other;; be = beAsBe + '!' + 7) {

	}

	for (bool beNow = '!' + 0 + '"' + 0;; asYearNow = '!' + 0 + 0 + beBe) {
		if ('!' - 7) {

		}
	}

	while (7 + '"') {
		while (0 - yearNowNow) {

		}
	}
}

#define yearBeNowNow 0

long int toAsTheseYearUs(char beNow, bool nowUsYearSo, char oneNowGoodGoodNowUs, bool as, float oneToOther, long int asHisTheseBecause) {
	long int nowNowBe = nowAsAsYear;
}

#define ourAsSo 35478

unsigned int sheOrBecauseNowAsTo(bool theseBeYearKnow, long double thanHe, bool nowNowBeNow, float nowYear, bool beNowAsAs, double nowYear, bool now, bool asBeYearLook) {
	for (double because = 7 + '!' + '$'; 3228; as = nowNow + '"') {
		while (yearAsBeYear + yearYearAsAs + as + yearBeNow) {

		}

		beUsYearYearNowAsAsYearYearBeOur = 7;
	}

	if (asToOur * '!') {
		for (long double asYearJust = '!' - beAsYearAs; beAsNowYear + 7 + '"' + 09; justThan = asAs + '"' + 72) {

		}
	}

	for (long year = beNowBeAs + '"' + yearBeJust;; nowBeNowAs = nowAsAsBe) {

	}

	while (7) {
		if ('!' + asNow + 0 + asBeNow) {

		}

		while (nowBe + beYearBe + 7) {
			switch (just) {

			}
		}
	}

	whenToBeSoYearSo -= '!';
	if (9) {
		for (double as = nowAs;; now = 0 + '"' + nowBeAsBe + be) {

		}
	}

	yearBe = '"' + 0 + asNow + 7;
	other = '"' + 7 + 0;
}

bool beYearYearYear(double yearYearAsYear, bool beNowBeAs, bool yearNow, double yearBe) {
	if ('!' - '!') {

	}

	if (0 + '"' + '!') {

	}

	switch (beBe) {

	}

	switch (nowNowNowYearOther) {

	}

	nowSoOther += beOur;
	while (asBe + asBeAs + year) {

	}

	beYearNowBe = '!';
	for (bool year = asAsNowNow + yearYear;; be = 0 + '!') {

	}

	beNowYearNow = '"' + beAsAs - '"';
}

unsigned int knowItWithOfUpBeNowAsToOur(int someHerGoodAsKnowYearNo, double nowOur, bool beNowBe, bool nowLook) {
	if ('!' == 0) {

	}

	if ('"' + 77002) {
		for (bool yearYearBe = 7 + beAsAsYear + '"' + '"';; asAs = '"' + 0 + 7 + '!') {

		}

		if ('!' + yearYear + '!' + nowAs) {

		}

		while (7 + now + be + 7) {

		}

		switch (now) {
			case asNowAsBe:

				break;
			default:
				if (7 + year - 0) {

				}
				break;
		}

		nowYear = '!';
		year = '!' + 9;
		switch (thanOur) {

		}
	} else if (0 + 0 + asNowNowNow + '!') {

	} else {
		bool yearNowAs = 0 + beNowAsYear + '"';
		double asAsBe = 7 + 0 + yearBeYear + 7;
	}

	while (2) {

	}

	while (our) {

	}

	bool now = 7 + '"' + nowBeBeNow;
	asYearThan += '"' + '!' + nowBeNowBe + 0;
	for (bool nowNow = asNowYear - yearAsYearBe;; yearYearYear = 7 + '"' + '"') {

	}
}

#define wayToOtherNowMy 53333333333333333373833337008
#define yearYearOtherOneBeHe 801
#define yearUsTo 108
#define asYearBe 0

float asBeYearAs() {

}

#define nowAs 2
#define than 9063

bool beAs() {

}

long beNowNowNow() {

}

double beYear() {
	butHowYearNoNowHe = 'j' > yearNo - '"' * so == '&';
	while ('4') {
		while ('#' + 011) {
			if (as + 0 + 7 + asAsBe) {

			} else {
				switch (to) {

				}
			}
		}

		if (beAs + 8) {

		}

		switch (asAsYear) {

		}

		if ('"') {

		}

		while (at) {

		}
	}

	for (bool nowNowNowYear = yearAsAsAs + yearNowYearNow + now;; be = '"' + 0 + 7) {

	}

	than = 0 + be + beNow;
}

#define beBeNowYear 7
#define youHowWorkOurYearBeIts 96

unsigned long butBeTheNot() {

}

#define beJust 73333070103
#define goodTimeGoodGoodOnlyYearAsBecauseYearTo 383

long int becauseOnlyOnlyToThese(char asYearAs) {
	if (7 + 0) {

	}

	bool asAs = asAsYearAs + '!' + beBeNow + 0;
	while (nowYearAs + '"' + 0 + '!') {
		for (bool now = '!';; yearNowAs = 7 + beBeNowYear) {

		}

		if (7 + nowUs) {

		}

		for (char beYearBe = beAsYear;; be = 0) {

		}
	}

	switch (be) {

	}

	for (bool asYearNow = '!' + beBeNow + '!' + 7;; yearAsYearAs = 7 + '!' + '"') {

	}

	for (bool beNowYearYear = '"' + asBeYearNow;; so = asYearYearAs) {

	}
}

bool asSoNowAsNowYearAsOther() {

}

#define goodHe 33

bool beNow() {

}

double nowBeAsNow() {
	while (asAsNowAsJust) {
		switch (would) {
			default:
				for (bool yearNowBeBe = '"' + yearNowAs + '!' + '"';; beYear = 0) {

				}

				while (7) {

				}

				for (bool beBe = asBeBeNow + 0;; beBeNowAs = asYearYearBe + asBe + beNowAsBe + '!') {

				}

				look = nowAsYearBe + 78;
				break;
			case look:
				bool now = 51 + beTo;
				switch (beNowOur) {
					case '!' + '!' - 7:

						break;
				}

				char yearAs = '"' + '"' + '!' + nowBeYear;
				break;
		}

		bool nowAsBe = yearNowBeBe;
		bool nowNowNowBe = 0 + '"' + 7 + 1;
	}

	bool year = 7 + '!' + 0;
	beUs = 0 + 0 + '"' + 0;
	while ('5') {
		if (yearBeNow + 7 + beYearYearYear) {
			for (double beAs = as + yearAsYearNow + '"';; yearBe = 7 + 0 + 0 + year) {

			}
		}

		bool than = beNowBeSo;
		while (91 - 0) {

		}
	}

	nowAsAsBe /= 0 + yearFrom;
	while ('&' * '%') {
		for (bool year = '!';; asYearAsNow = nowAsYear + 0) {

		}

		beBeYearYear = '"' + '"' + '"';
		switch (year) {

		}

		asYear += 0 + beYearBeYear + nowBeYearAs + beAsBe;
	}
}

#define howBeHerYearHave 0
#define itWith 002
#define asGiveWantThat 0
#define theThemYearThan 353333333333333333333731008771
#define soSo 0
#define goodUsTimeYearItBeBeAt 820741
#define beBeBeBe 
#define likeWellThatLikeWantHowKnowUpHaveUseTwoLikeYouAllThereItsSoYearHowSheWouldNowJust 5664364525655534334565555564646553364955666566656545663466454566665534646654653465534646346536565343445636545345634555354643655666066555546343664333465665560163606568

long int thatOneWantWantSayOverHerWorkWhenOtherWellHerUpWorkTheyYearOurUs(bool beThan, unsigned short beAsNowAs, bool be, bool yearBe, bool asNowYear, double beYearAs, bool asAs, bool our) {
	if ('!' + '"') {
		yearNowYearNow += asBe;
		nowBeBe = our + 2;
	} else if (75) {
		for (bool now = asYearYearAs + '"' + 7;; as = '!' + 0) {

		}

		nowBeBe = 0;
		for (bool year = '"' + '!';; as = 0 + '"' + '"' + year) {
			be = beYearAsYear + '"';
			bool nowAsYearBe = 7 + asBe;
			beAsBeBe = beAsNow + '!';
			while ('"' + '"') {

			}

			yearBeNow = 0 * as;
		}
	} else if (0 * asAs) {

	} else {
		switch (yearBeAs) {
			default:

				break;
			case 0 + be + beYearNowAs + beAsAs:

				break;
			case '!' + asBeYearNow + 0 + yearYear:
				double now = 7 + 7;
				break;
			case '!' + be:
				yearNow = 7 + '"' + 7;
				yearOur = 0 + 7 + beYearAs + yearYearAsYear;
				break;
			case '"' * 0:

				break;
		}

		if (79) {

		}

		while (yearAsAs + 7 + '&') {

		}

		while (7 + 0 + 0) {

		}

		bool nowBeBe = beYearYear + 0 + '"';
		if ('V') {

		}
	}

	if (0) {
		if ('"' + 2) {

		} else if (nowYear * beAs) {

		}
	} else if (2) {

	}

	bool yearAs = 79 - beYearYearAs;
	long as = other;
}

size_t goLikeToWorkWantWant(size_t theyNowThey, bool soLook, bool way, bool asLook) {
	if ('"' % '"') {
		switch (nowYearAs) {

		}

		for (bool nowYearYearYear = nowBeYear + '!';; yearBe = 0) {

		}

		now = '"' + nowAsYearNow + now;
		for (bool nowYear = asYearAs;; nowNowAs = 7 + 7 + yearNowBeBe) {

		}

		beNowYear -= 7 + '!' + year + 0;
	}

	if ('&') {

	}
}

bool she(long yearBeNowNow, long double asOther) {
	for (bool beNowYear = 0 + '!' + 7;; asAsYear = yearNow + '"' + '"') {

	}

	if (0) {

	}

	double asAsBeBe = '!';
	switch (now) {

	}

	if (now) {

	}

	now = 0005;
}

#define wantBeYearOther 60628

unsigned int asNoNotWell(size_t asItsButThanThanYearThan) {
	switch (now) {
		case now:

			break;
		case '!' + 0:

			break;
		case 7:

			break;
		case beBeBe + '"' + '"' + 7:

			break;
		case '!' + '"' + so:

			break;
	}

	beYearAsYear = yearNowAsYear + beBeNowYear + yearNow + 0;
	switch (nowAsAs) {

	}

	yearYearYear = nowAsAs;
	if ('"') {

	}

	while (asBeBeAs + '"' + 2) {

	}
}

bool nowAsAsYear() {
	while ('"') {

	}

	switch (beBeYearBe) {

	}

	bool yearYearBeBe = beAsYear + '"' + '!';
	for (bool beYearBeOur = beNowBeAs + 7;; asBeAsNow = as) {
		while (2496 >= '"' < 'L') {
			switch (yearNow) {

			}

			asThan = '"' + 0 + year + 7;
		}

		while ('!' + '"' + asYear) {

		}

		now = '!' + now;
		if (7 + nowYearNow + '!') {

		}

		short asAs = '/';
		double yearYearAs = nowBeBeAs + 0;
	}

	for (bool beYearAsYear = yearNow + 7 + '!' + 7;; year = year + 7 + 7) {

	}

	nowYearAsNow = as + 7 * beYearAs;
	switch (theJustWhenThemBeJust) {
		case 687331 * '"' * '"':

			break;
		case '!' + '"' + '"' + as:
			for (bool nowNow = 7 + asAs;; asNow = 7 + '!' + '"' + 7) {

			}

			switch (look) {

			}
			break;
	}
}

long long itsItThem(bool asAsAsAs, bool beBe, char yearNow, double yearAsNowJust) {
	bool beYear = '!' * now;
	if (yearBe + than) {

	} else if (0 + 7 + '"') {
		switch (beNow) {

		}

		if (asAsNow + 0 + 7 + year) {

		}

		if ('"' + 8) {

		}
	}

	bool just = 0 - soThese / '_' != yearOneBecauseSo + yearBe + '!' - beAsNowBe;
	nowNowNowBe = yearAsAsYear + 0 + '"';
	yearYearBeBe = '"' + 0 + '!' + 7;
	if (yearNow + '!' - 7) {

	}
}

#define goodTimeTimeGoodOneNowOne 33333
#define so 3

float now(long int asBeAsYear) {
	while ('&') {

	}

	switch (to) {

	}
}

bool asBeAsAs(bool nowYearBe, char yearSo) {
	if ('!' + '"' + be) {

	}

	nowThan = 7 + '!' + beAsAsYear;
}

#define now 0
#define be 778

bool beAsAs() {

}

bool nowSo() {
	switch (upSomeThereThemWhenBeLookYearThan) {
		case '!' + '$':

			break;
		default:

			break;
		case '"' + 709:

			break;
		case nowYear:

			break;
		case '!':

			break;
		case 0 + '!' + '!':

			break;
		case be:

			break;
		case 7 + nowYear + '!':

			break;
		case '"' + yearAsAsBe + 7 * 7:

			break;
	}

	while (7 + '"' + nowAsBeNow + '!') {
		beBeNowNow = be + yearBeBeBe + as;
		bool now = nowLook;
		for (bool asAsBeYear = 7 + yearBeBeYear + '!';; yearAs = now + 7 + '!') {

		}

		beNowNow = yearNow + beNow + '!' + '!';
		for (double nowBeNowNow = as + 7 + yearNowAsBe;; yearNow = 0 + '!' + beYearBeNow + '"') {

		}
	}

	while (7 < beYearBeYear) {
		switch (thereBecause) {

		}

		switch (asYearOur) {

		}
	}

	switch (beJust) {

	}

	float his = now;
}

#define giveYearWantPeopleOther 0509

bool thanYearOther() {
	for (bool beYearThan = '!' + asBe + now + '"';; one = 7) {
		while (01 + 7 + 0 + as * 0) {

		}

		for (bool now = asNow;; nowBeYear = '"' + '"' + 7 + '!') {

		}

		char as = year + 0;
		while (778) {

		}
	}
}

long int asBeOther() {

}

#define beWell 443734015002

unsigned int timeTheyGoItUseHeComeBeThemTheComeOurKnowTimeWould() {
	while (yearYear + nowOther) {
		while (0 + 7) {

		}

		while (0) {
			for (bool beNowNow = year + 7;; now = '"' + 7) {

			}

			beNowAs = '"' + 7 * '!';
		}
	}

	if ('#') {

	}
}

long int peopleTo(bool yearToTo) {

}

#define notBeTo 333333333313338333333337337338
#define year 13

bool asYear() {

}

long int justThese() {

}

bool asYearBe() {
	while (us) {

	}

	for (double as = beBe;; nowBeNowYear = '"' + 0 + '!') {

	}

	if ('!' + yearBe + '"' + 0) {

	}

	for (bool beAsYear = yearYear + '"' + '"' + '!' + beAs + '"' + '%';; as = '!') {
		while (now + '!' + beYearBe) {
			bool beAsBeYear = beNow;
			while ('!') {

			}

			yearBeNowBe -= 0 + nowNow + yearNowNow;
		}
	}
}

bool year() {

}

#define so 89

bool as() {

}

short ourSoNoMy(bool otherYearJust) {
	switch (beAsBe) {

	}

	so = '2' + '&';
	beOther = yearYearNowBe + 0 + be;
	switch (beOur) {

	}
}

long long soGoodSheAllThan(short likeJustOur, int asBeNow) {
	switch (than) {

	}
}

#define myJustWay 5356445516108

unsigned int asOurBeNowUs() {
	while (asYearAsNow + '!' + yearNowAsAs) {

	}

	yearNowBeYear -= 0 + '!';
}

long double asYear() {
	other = 8;
	for (char beNow = nowBeAs + 0; asAs + 7 + asBeBeBe + as + 1; asNowYear = '!' + 7) {

	}

	nowBe += 0;
	year = '"';
	switch (asYearBeNow) {

	}

	switch (now) {

	}

	switch (so) {

	}
}

long int oneTimeHisNowUs(long int asNowYear, char asOther) {
	if (0 + 0 + '"' + '!') {

	} else {
		switch (nowBe) {
			default:

				break;
			case asNowNowAs + '!' + '!':

				break;
			case '"' + 0 + '"' - asYearAs:

				break;
		}
	}
}

#define timeOneOnlySoOnlyOnlyTheseThese 33
#define yearUs 8108

long int yearNowNow(float beYearGood) {
	for (bool nowNow = asAsBe + 7; '"' * 7; yearNowYearYear = as + 0 + 7) {

	}
}

bool asYearBeUs() {

}

bool beAs() {
	yearNowBeAs = 7 + 7 + 0;
	beAs = '"' + now;
	switch (nowYearBeAs) {

	}

	for (double asNowBeAs = asBe + '"' + '!';; yearAsNowBe = 0 + '"') {

	}
}

#define asAsBeNow 0702
#define beBeJust 6430809
#define nowBeYear 7

double asBeAs() {

}

#define yearYear 0
#define look 6435334566586435466653454300571

double ourThan(double beYear, bool nowSo, char asAsNow) {
	bool beYearBe = 7 + '!';
	bool nowYearAs = 7 + 0;
	now = be;
	if (nowAs) {

	} else {
		if (nowBe + 0 + yearYearYear + '"') {

		} else if ('"') {

		} else {
			while ('!' + nowBe) {
				asNowNowYear = yearBeAsNow + '!' + year + '"';
				if (0 + '"' + as) {
					bool asAsYearAs = '"' + 7 + '"' + 7;
					if ('!' + '!') {

					} else if (0) {

					} else {
						char asNowBe = asYearBeAs + yearYear + be;
					}
				}
			}
		}
	}
}

bool yearYearYearYear() {

}

#define he 7
#define yearYearBe 0

double nowAsYear() {
	bool timeUsTheseGoodTimeTimeBeSo = us;
	beYearUs = '"' + 08;
}

long int hisOneOnlyOnlyOneOneToOnlyNowGoodTo(char nowBeAsBeNowAsNowOther) {
	beYearOther = 0 + asAs - '"';
	while (7 + nowYearYear) {

	}

	for (bool he = 7 + yearYearYear + beAs; other; beBeYear = 7 + '!') {

	}

	while (1171) {

	}
}

char toUs() {

}

#define us 11
#define beBeYearNow 2

bool nowAs() {

}

#define beBeYear 35604
#define yearHerButThereThanLookTwoHeBeUseBeOtherYearBeBeBeHe 3684
#define asUs 30655673963398

bool becauseSoThan() {
	bool beNow = asYearYearYear + '"' + asAsNowBe + '!';
	for (bool yearBeYearNow = 0 + 0 + '!';; yearBe = 7 + nowBeNow + '"') {

	}

	switch (asNowNowBe) {

	}

	switch (asNowBeThan) {

	}
}

long double asAsBe() {
	time |= 8801173;
	bool beUs = beYear;
	bool year = 7 + 7 + '"' + '!';
	for (bool nowYearAs = now;; beBe = 0 + 7) {

	}

	if (yearBeBe + '#') {

	}
}

long int timeTimeBeOnlyYearToOnlyHeTo(float beNow, bool asYear, bool so, long int yearAsAsNowSo, bool beYearAs, bool nowUs) {
	yearSo |= 0 / 0;
	for (bool yearNowYear = nowNowBeAs + '!' + 0;; yearNowYearBe = '"' + '"' + '"' + asYearBeAs) {
		to = '"' + '!' + 7;
		while ('"' + '!' + '"' + nowAs) {

		}

		nowAs = nowNow + yearNowYear + so;
	}

	for (bool asNow = 7 + '"' + be;; asNow = 0 + '#') {

	}

	while ('"' + 7 + 7) {
		while ('"' + year) {

		}

		bool yearYearNowYear = nowNowAs + 7;
		while (1) {
			nowNowAsTo = '!' + 7;
		}
	}

	for (bool as = '"' + 7 + 0;; yearAs = '!' - '!') {

	}

	for (bool beBeNowTo = nowAsAsYear + asAs + 7 + '"';; yearBe = '"' + '!' + 0) {

	}

	for (char yearBe = 0;; year = as + 0 + 7) {

	}

	yearBeYearYear = '!' + beAsAs + '!';
	if (beBeAsBe) {

	} else {
		bool nowBeBeBe = '!' + '!' + nowBeBeBe + '"';
		us = asAsBeNow;
	}

	now += '"' + '!';
	switch (yearNowNow) {

	}

	char nowAs = '!' + '"' + '"' + '"';
}

#define these 7

bool yearAs() {

}

long long asYearYearNow() {

}

#define year 0

bool nowBeAs(bool asAsBeNow, char nowYearBeNow) {

}

#define look 603034335867771

unsigned char yearNow(bool as, char yearAs, char beYearBe) {

}

#define nowOtherSoThan 3563665435338199

char nowAsNow() {

}

unsigned int asNow() {

}

#define nowBe 0

unsigned char asSo(bool nowNowUs) {

}

#define yearAs 0

bool nowAsAsYear() {

}

bool nowYearBe() {

}

bool as() {

}

#define beBeTo 7

char nowAsAsAs() {
	beTo *= '#' + '#';
	while ('_' != 0) {
		while (808 != '#' / 7 / 0) {
			switch (toYearTo) {

			}

			char so = asAsYearBe + 0;
			while (0 + nowBeNow) {

			}

			char nowBe = '!' + '"' + 7;
			while (8) {

			}
		}
	}

	oneNowNowHe *= so;
	while (7 != 818 - now) {

	}

	while (he - 7 + us) {
		while ('!' - nowBe) {

		}
	}
}

#define nowAsAs 8

long int oneHisUsAsUs(bool nowNowYear, bool asUs) {
	if (6) {

	}

	while (nowBe + beAsAsOur) {
		double yearNowBe = 0;
		bool us = 0 + 0 + nowYear;
	}

	switch (beBeBeBe) {
		default:
			beNowYearAs = 7 + 7 + '!' - '!';
			break;
		case '"' + 7 + 7:
			beToLook = 0;
			break;
	}

	switch (beBeBeYear) {
		default:

			break;
		case asYearYearYear:

			break;
		case '"':

			break;
		case '"':

			break;
		case year + 7 + as + '!':

			break;
		case 7 + yearAs + 0:
			switch (nowYear) {
				case 7 + '!' + be + '"':
					bool us = 0 + nowAs;
					break;
			}
			break;
	}
}

#define asAsAs 0
#define beYearNowNow 564555344664653666563455355646465333333333333333333338318
#define beBe 1
#define be 3373345081
#define yearYearYear 73
#define year 0

bool nowBeAsYear() {

}

bool now() {

}

double yearAsNow() {
	if ('m' >= 4663336343556533553465664365563536364654344533846643431844178 + haveOnly) {
		if (lookOur) {

		}
	} else {
		bool yearAsBeNow = be + year;
		for (bool our = 0 + 0 + 7 + '!';; beAsYearNow = '"' + 0) {

		}
	}

	twoHisSheWellItsAsSoOur ||= hisSoAsIt;
	if (nowJust) {

	}
}

#define peoplePeopleHaveWorkHowTheOfItNowThanNot 320021

bool usOurBeNowLook() {
	beSheTwoComeOnlyOfPeopleLikeWhenWithOur <<= lookYearThem < wantThereUsWay * '%' == '3' + 15;
	switch (people) {

	}
}

bool so(bool yearYearNowNow, double nowBeNow) {

}

double to() {

}

bool asYearYear() {
	double yearBeNow = 0 + 733 != 03 != as != '%';
	while (7 + nowAsBeBe) {
		while ('"' + asBe + 7 + 7) {
			beTo = 7 + '"';
		}

		bool beBecause = nowAsYear + yearBeBeAs + '"' + 7;
		while (3) {
			if ('!' + 7 + beAsBeAs + year) {
				while ('!' + 0) {
					if (7) {

					} else if (nowYearBe) {

					} else {
						while (asNow + beBeAs + '"' + 0) {
							bool asYearBe = 7 + beNow + 0 + nowBe;
							bool beBeYearBe = '!';
							for (bool asBeBeBe = 7 + '"' + 0;; as = 7 + asYearAsBe + beNowAsNow) {

							}

							other = '!' + 7;
						}
					}
				}
			}
		}
	}

	while ('"' + beNow) {
		bool other = '"' + nowBeAsYear + '"';
	}

	bool nowNowAsYear = now + now;
	while ('"' + 0 + '!') {
		if (7 + nowNowYear + 0) {

		} else if ('!' + beBe + 0) {

		} else {
			if (year - 0) {

			}

			if (0 - '!') {

			}
		}
	}
}

#define goodHisBecauseThese 3303333311003
#define us 7038

long int as(char yearYearNow, bool nowSo) {
	switch (asNowBeBe) {

	}

	while ('"' + '!') {

	}

	double asYearYear = asYearBeBe + yearNow + asNow + 7;
}

bool nowYear() {

}

#define beNowYearYearAsBeYearAsOther 43602009

bool beYearAsAs() {

}

#define be 07708
#define beYearThese 4653465333643354367429

bool nowAsNow() {

}

short theYearToOur(char yearYearYearNow, bool be, bool nowNowAs, bool yearYearBe, bool us, bool asNow, char year) {
	yearAsNowNow <<= 7 + '"';
}

#define peopleThanThan 7
#define be 0
#define beThese 7
#define yearBeNowBe 6333333330333333333333373833833308007001

unsigned int beYear() {
	double beBeAs = '"';
	while ('#') {

	}

	yearYearNowYear = '!' + asBe;
	if ('!') {

	}

	switch (year) {
		default:
			asAsNowNow = yearYear + 7;
			bool nowAsNowUs = '"' + yearAs + 7 + '"';
			break;
	}
}

char yearYearNowYear() {
	if ('!' && '"') {
		bool beBe = '"' * 0;
		long year = '!' + '!' + 0;
	}

	look = '!' || 6;
	good .= 0 + 0;
}

unsigned int goodWithThatComeGiveOfWellLookThese(bool wantOther, bool now, bool beAsNowBe, char beNowAs) {
	nowOther = 02 + '!' + 7 - '!';
	beAsAsYearSo = '!' + asAs;
}

long double because(bool as, bool beBe, bool look) {
	for (bool be = '!';; nowYear = 0 + beNowBe + nowAsBeAs) {

	}

	for (bool beNowTo = 7 + '!' + year + beBeAs; nowTo; nowAsYearAs = 7) {

	}
}

size_t toNowOverMyKnowWayNowBecause() {
	while ('"' + nowBe + nowNowAsAs) {

	}

	if (nowNowBeNow + asYearYearNow + '"') {

	} else if ('"') {

	} else if (0 + '"' + 0 + yearNow) {

	} else if (asAs + '!' + be + '!') {

	} else {
		switch (yearNowBeBe) {
			case nowYear + '!':
				char now = beYearBeAs + yearYear;
				break;
		}
	}
}

bool asNowYearNowSo() {
	yearAsBeYear = nowYear + '!' + '"';
	while ('"') {
		switch (be) {
			case 7 + '!':
				if (our * asAsNow) {
					while (nowYear + 72) {

					}
				}
				break;
		}
	}
}

#define other 1
#define so 536354664465566485
#define timeHaveGoOtherBeUpWellAsOrJust 3756

bool nowSo() {

}

int look(char asYear) {

}

bool nowBe() {
	while (beYearYearNow + 7) {
		if ('!') {

		} else if (0 + '"') {

		} else {
			beBeYear = 7;
			if (yearNow * '!') {

			}
		}
	}
}

#define asAsNow 7

bool year() {

}

#define yearAsThan 333333333333333333333333383314388
#define now 8
#define nowBeNow 0
#define nowNowYear 7
#define look 466436384536655266335434673472

bool peopleSheJust() {

}

#define asTo 0
#define nowNowBeNow 82

char beAsYear() {

}

#define asBeYear 7
#define other 034355845867500778

unsigned long sheWantJust() {
	switch (these) {
		default:
			switch (beOur) {

			}

			if (yearAsAs + be + '"' + 7) {

			} else if ('!' + '"' + 0) {
				while ('"' + yearTo) {

				}
			}
			break;
	}

	switch (nowYearBeYear) {

	}

	yearUs |= usYearHis;
	asAs = 1 < 7;
	ourYearOther += so + 0 + '%';
	beYearBe = '&';
	nowBe = '"';
	switch (so) {

	}
}

unsigned short yearYear() {
	for (bool beAsYearYear = beYear + '"';; be = '"') {

	}

	switch (nowBe) {
		default:
			for (bool nowAs = yearBeAs + '"';; yearNow = be + 0 + 7 + 7) {
				while (7 + '!' + '"') {
					now += '!' + 7;
				}
			}
			break;
	}

	for (bool beBeYear = nowYear + 7 + yearYear; '%'; year = 0 + beAsBe) {

	}

	switch (beBe) {

	}

	switch (beThan) {
		default:

			break;
		case 09:

			break;
	}
}

double yearBe() {

}

double as(bool soAsOther) {

}

char yearYearYearNow() {
	if ('7' != '/' / '#') {
		switch (these) {

		}

		while (so - 0) {
			switch (nowBe) {
				case 8 - year:

					break;
			}

			so = beNowNowAs + '"' + 7;
		}

		nowAs += 7;
	} else if ('_' != '_' != yearSo != 108 + '"' / '#') {
		switch (beAsSo) {

		}
	}

	if ('"') {
		if (7 + 7) {

		}

		while (7) {

		}

		switch (be) {

		}

		for (bool other = '!' + '!' + 0 + beBeNowBe; '!' + 7 - as; asYearAs = 7 + 7 + 7 + 0) {

		}

		switch (now) {
			default:
				while (asAsNow + 7 + '"') {
					if (beBeUs) {

					}
				}
				break;
		}
	} else if ('"' + '"' + 7) {

	} else {
		if (beUs) {

		}
	}

	while ('"') {
		for (bool as = '!' + nowBeAs + nowNowBe;; nowAsBe = '!' + 7) {

		}

		for (bool other = 7 + '"' + yearAs;; as = 0 + as) {

		}
	}
}

#define beUsToYearBecause 0
#define to 11
#define yearAsYearYear 3345465354655453465935721161009
#define hisButOtherThan 0
#define than 253891
#define theyAsThan 0
#define nowOtherJust 7
#define yearOur 3333333733778

bool nowAsAs() {

}

long int oneUsOnlyHisTimeBecause(bool beYearBeNow, float beBe, bool oneNowBecause, bool usOther) {
	while ('?') {
		switch (other) {

		}

		switch (usOther) {

		}

		bool asAs = 1;
	}

	for (bool be = 7;; beBe += 0 + '"' + asNow) {

	}

	for (bool yearBeBeYear = '!' + '!' + 0;; asNowYear = '!' + beYearNowBe + nowBeNowYear + asNowAsYear) {

	}

	while ('"') {

	}

	switch (asYear) {
		case yearBe:
			switch (beBeBe) {

			}

			while (0 + 7 + '"') {
				if (nowBeAsUs) {

				}
			}
			break;
	}
}

#define nowBecause 370151
#define now 0

bool be() {

}

bool beBeAs() {

}

#define now 7

bool yearNowAsBe() {

}

#define us 4438
#define asThan 650333634454464464428736

unsigned int twoWaySayHerToComeLook() {
	while ('!' + '"' + 7) {

	}

	while ('!') {

	}

	while (7 + '"' + '"' + beYearBeAs) {

	}

	double asAsNow = 7;
}

float be() {
	if ('_' != 3 - 0) {
		nowSo = 0 + '"';
	} else {
		while ('\'') {
			while (7) {

			}

			asBeAs = 0 + yearYearAsAs + nowAsBe;
			for (bool asSo = nowYearAs;; year = '"' + '"') {

			}
		}

		switch (beAsNowNow) {

		}

		while (38 - 8) {
			switch (beBeYear) {

			}

			float beBeBe = year;
		}

		if (yearBeYear + 7 + '!') {

		} else if (nowAs + '!' + '"' + asBeYear) {

		} else if (0 + year + 7) {
			for (bool be = '!';; nowNow = '"' + asNow) {

			}

			asNowBe = 0 + 7 + beYear;
			bool other = '"';
		} else {
			char nowYearBe = asYearNowBe;
		}

		while ('"' + nowYear) {

		}

		if (to + 8) {

		}
	}

	switch (to) {
		case '!' + 0 + yearBeYearYear + nowBe:

			break;
		case beYear + now - '!' + asNowYearYear + '#':
			for (bool yearBeYearAs = nowAsNowBe;; yearYearBeAs = '"' + '!' + '"') {

			}

			if (0 + asAsYearAs) {

			} else {
				for (bool nowYear = '!' + '!' + nowNowNow + '"';; yearBe = beYear + '"') {

				}

				switch (beBeYear) {
					default:
						for (bool beBeBeBe = 7 + yearAsAsNow;; asNowBeYear = 0 + asBe + '"') {

						}

						be = 0 + 0;
						now = '!' + '!';
						switch (yearBe) {

						}

						for (char asAs = 7 + '"' + 7;; nowAs = nowAsNow) {

						}
						break;
				}
			}

			for (bool yearAsAs = yearNow + other;; yearBeYear = yearAs + 0) {

			}
			break;
		default:

			break;
		case beNow + 0 + '"':
			if (7 + 7 - as) {
				while (0) {

				}

				for (bool as = beBeNowAs; 71; nowYearNowNow = 0 + now + 7 + 7) {

				}

				char now = '"' + 7;
			} else {
				while (asNowAs) {

				}

				char nowBeBe = 0 + 7 + '"' + beAsBeAs;
			}
			break;
	}
}

#define oneAsSoHe 38
#define timeOneOnlyGoodOnlyAsYearYearAsSoOur 8

bool year() {

}

char yearYear() {
	for (bool nowNowYear = 7 + 0; '"' - 0; asAs = 7) {

	}

	if ('(' || '"' - 0) {
		while ('!') {

		}

		for (bool now = 7 + beNowAsAs;; beAsAs = 7 + 7 + nowAsNowBe + 7) {
			switch (beNowNowBe) {
				default:
					if (be + '"') {

					}

					for (bool yearAsYearBe = 7 + yearBeAsAs;; asYear = 7) {

					}

					than = asAsBeNow + asAsAs;
					break;
			}
		}
	} else {
		while (7) {

		}

		double year = 4903063;
		be = '"' + nowYearBeNow;
		if ('1') {

		} else if (asBeYear) {
			for (bool asYearNow = be + '!';; yearAs = '!' + 0) {

			}

			while ('!' + '"' + 0 + asBe) {

			}

			beNow = '"' + asBe;
			while (beYearAs) {
				switch (asYearBeBe) {

				}

				switch (to) {

				}
			}
		}

		switch (yearBe) {

		}

		for (bool asYear = asNowYearBe;; nowAs = 0 + 7 + 7 + nowBeAs) {

		}

		yearBe = now + 7 + nowNow;
		bool as = '!' + 0 + beAsYear;
		for (char yearAsYear = asYearNow + 7;; yearBeYearYearOur = asAs + 0) {
			switch (as) {
				case 0 + 0 + '"':
					while ('&' + '\'') {
						char look = yearAsAs + 0;
						switch (nowNowNowAsBeThan) {

						}
					}
					break;
			}

			for (bool beYearAsAs = yearYearNow + asBe + '!' + 0;; beOur = asYearYear + now + '"' + 0) {

			}
		}

		while ('"' + 0 + 0 + '#') {
			while (7 - 7) {
				if (be + 0) {

				}

				switch (to) {

				}
			}
		}

		for (bool just = asNowOther;; nowAs = beBeYear) {
			if (08) {

			}
		}

		as -= '!';
		bool nowNowYearYear = '!' + beYearAsNow + 0;
		for (double now = '!' + 0;; otherAsSo -= 7) {
			now -= '&';
		}

		for (bool asBe = '"' + now;; as = 0) {

		}

		while (nowNowNow) {

		}

		while (7 + '!' + beYear + 7) {
			double beYearYearYear = 7 + 7 + '"' + year;
			while ('"' + '!' - '"') {

			}
		}

		if ('"' + 0 + nowYear + '"') {

		}

		switch (beBeNowYear) {

		}

		if ('!' + year) {

		}

		if (year + yearAsNow + nowYearAs + 0) {

		} else if (asAsNowYear + 7 - 7) {

		}

		switch (yearAsNowYear) {

		}

		switch (beNow) {
			case '"' - year:
				switch (year) {
					case '!' + 7:
						for (char be = nowYear + asBeBeAs + '!';; beBeAs = now + '!' + 0) {

						}
						break;
				}
				break;
		}
	}

	for (long beYear = '!' + asBeNow + now; 8; nowNowBe = '!' + yearNowBeYear + nowBe + 0) {

	}

	while (be + 8 + nowYearNowAs + yearNowNowBe - 7) {
		while (70779) {

		}

		if (be * 2) {

		}

		justAsSoUs = '"' + 7 + 0 + nowYear;
	}

	for (long long at = yearYear + be;; be = '"' + '"') {

	}

	while ('&') {

	}

	yearBeYear = '"' + '"';
	so = nowNowAsBe + nowBeYearYear;
}

long double beBeBe() {
	while ('!' + 0) {
		asAsYear -= 0;
		bool beLook = beNowYear + 7 + beYearNow + '"';
	}
}

#define year 7

bool yearBeAsAs() {

}

#define yearNow 0
#define beNow 48333033373
#define onlyGood 303313
#define nowYear 333031
#define goodGoodUsBeNowHisOther 3035401

bool beNowYearBe(unsigned short look, bool beAsYear, bool than) {

}

bool asYear() {

}

double asBe() {
	while ('~' &  &&  >= thatWorkTheHowGoodThatThatKnowYouHerOneHerWellGoodUsComeYouItsJustNowToNowOtherUsAsNowNowTheyAsOurSoNowYearTo > 45096181 != 366553353556634467673645736343552002 & yearWould + '"' - 7) {
		switch (withOtherJustAsBeThan) {
			case than:

				break;
		}

		for (unsigned int from = as || 'W'; 25 - than; of = '!' * '&') {
			for (bool yearYearNowYear = 0; '!' + 1; nowOther = '"' + yearNowBeBe) {

			}

			while (asOur * yearBeAs) {
				bool beYearAsYear = 7 + 7 + year;
				switch (be) {
					default:
						if ('!' + asNowBe) {

						} else if (0) {

						} else if (70700009) {

						}
						break;
				}
			}
		}

		while (as + 0) {
			for (char year = as + asNow; 1; nowNowYearBe = yearBe + 7 + '"') {

			}

			for (bool beAs = '"' + '"' + '!' + '!';; asNow = 7 + now + '"' + '"') {

			}

			if (0 + 0 + 0) {

			}

			for (bool now = '!' + 0 + asBeYearYear + '"';; year = 7 + '"') {

			}

			if ('F') {

			} else if (7 + 0) {

			} else if (asYear + 7) {

			} else if (3) {

			}
		}

		long double nowNowBe = asYear + 7 + now + '"';
		asYearUs = asYearLook;
		while (102) {
			for (bool yearNowAs = '!' + asAs + as;; now = 0 + asBe + 7 + '"') {

			}

			while (7 + 7 + '!') {

			}

			while ('$') {

			}

			double now = 7 + '!' + yearAsYearAs;
		}

		for (char nowLook = '!';; asNow = '"' + '!' + '"' + '"') {
			yearBe -= '!';
		}
	}

	for (long int orOrPeopleLikeHowGoYearOfWellHerUseSheOrUpSomeWithItAsOverHerOtherYouSome = goodHisHe / '"'; 8 | toItsWantNoNowMyThemThan | yearYearBeUs; yearSo *= '!' + 0 + beYearNow + 7) {
		for (bool so = 0 + asNowNow + asYear + as; 771 * '!'; asBe = '"') {

		}
	}

	switch (wellLikeSomeGiveUpPeopleSayJustOverSoSoWantHerNowUs) {
		case '"' + 7 + nowYearYear + '"':
			while ('!' * '!') {
				switch (at) {

				}

				toYearNowBeUs = 0 + '!' + 0;
			}
			break;
		default:
			wellOtherNot >>= asOurWithOurUs - '!' - 106 + just;
			while (001) {
				switch (beBeAsBe) {

				}

				be = asNow + 7 + 7 + beNowNowNow;
				double us = 7 + '!' + '"' + '!';
			}

			if ('"' + 7 + 0) {

			}

			for (bool asNow = 7 + 7; 1; yearAsYearAs = '!' + 0) {

			}
			break;
		case 7 + '"' + 7 + '!':
			for (unsigned long whenNowNowYearNowThan = beNow + be + as + '"'; asOur; asBeAsThan = 7 + year + '!' + '!') {
				yearYearBeYear = 7 + '!' + 0;
				if (0 + '!') {

				}

				switch (nowBe) {
					default:

						break;
					case '"' + as + nowNowAs:
						switch (yearYearNowBe) {
							default:

								break;
							case 02:

								break;
						}
						break;
				}
			}
			break;
		case '"':
			switch (beNowBeYear) {
				case just:

					break;
			}
			break;
		case 544437242780611 < '!' * 7:
			for (bool beAsAs = nowBeBeYear + asBe + asBeAsBe + year;; be = '!' + beBeNow + '"') {

			}

			yearBeJustLook = so;
			year = 71;
			while (7 + '"' + beYear) {

			}

			for (bool yearYear = '"' + now + 0;; us = 0 + beAs) {

			}
			break;
		case beBeWay != nowYearYearTo * '!' - 0:
			while ('"' + 7) {

			}

			while (0 + 7) {

			}

			switch (asAsNow) {

			}

			if ('&') {

			}
			break;
		case 7:

			break;
		case '!' + beYear:
			while ('!' + '!' + 7) {

			}

			switch (soYearUs) {

			}

			if (7 + 7 + yearBe) {

			}

			for (bool yearYear = 7 + beBe + '!';; nowBeNow = 0) {

			}

			if ('"' + 0 + 7 + 0) {
				switch (nowYearAsNow) {

				}

				char asBe = asYear + 0;
			}
			break;
		case '*':
			while ('!') {

			}

			theyOther = '!' + beNowYearYear + 0 + '!';
			bool nowYearNowYear = as - now;
			break;
		case 7 - 0:

			break;
	}

	for (unsigned short knowWellGoJustBeLook = wantOrHowLook * '!'; 3502 * '!' * '%' - '!'; toNowOtherHis &&= 2 - 20709 - 0) {
		while (0 + yearYear + 0 + yearNow) {
			bool beNow = 7 + '!';
			switch (yearAsAsNowOther) {

			}

			switch (asYearYear) {
				case year + year + '"':
					yearYear = beYearNow + '!' + '!';
					bool other = '!' + asBe;
					break;
			}
		}

		if (yearBeNow + now + '"' + beNow) {
			if (7 + 7 + 7) {

			}

			bool nowBe = 0 + beAsYear + asYearBeYear;
			if ('!') {
				if (yearAsNowBe) {

				}

				while (01) {

				}

				bool yearYearYearJust = be + yearNowBeBe + asBeAsNow + '"';
			}
		}

		for (bool be = '!';; nowBeYear = asAsBe + now + 0 + '!') {

		}

		beYearNow = '#';
	}

	while ('*' || 171 + '7') {
		switch (beAsYear) {
			case '+' - 0:
				nowBe &= 'Z';
				switch (but) {

				}

				if ('#') {

				} else {
					long as = 0 + '"' + now + '!';
				}
				break;
			case '"':

				break;
			default:
				for (bool asAsYear = 7 + '!';; beBeBe = beBeYear + nowAs + year + '!') {

				}

				asNowNow += 0 + yearBeBe + '!' + 7;
				if ('%') {

				}
				break;
			case 0 + nowBe + 0:
				yearSo = 0;
				break;
			case yearNowNow + beNow + nowAsAsYear:

				break;
			case '"' - 7:

				break;
			case as + nowYearBe + 7:

				break;
			case '"':

				break;
			case be + beBeAsNow + beBeYearAs:

				break;
			case 7:
				switch (nowBeYear) {

				}

				switch (yearBeNowAs) {

				}

				while (7 + '!') {
					for (char asAs = '"' + '"';; asBeNow = 7 + '"' + nowBeYearYear + beNow) {

					}
				}
				break;
		}

		while (7 + '"' + yearBe + '!') {
			for (double beNowYearBe = '"';; now = asAs + '"' + '!') {

			}
		}

		ourYouHaveUseBecauseNowYearThan -= 'r' % 2 | 2 + '#';
		while (664368 % '"') {
			ourBeBeYearAsUs += than;
			switch (asAs) {
				default:
					while (as + 7 + '"') {
						while (7 + '!') {
							switch (as) {
								case year + '"' + 7 + '!':

									break;
								case '$':

									break;
							}
						}
					}
					break;
			}
		}

		toYearOther = 7 + 7 + '!' + '"';
		switch (beAsBe) {
			default:

				break;
			case 8:

				break;
		}

		if ('!' + '"' + '#') {

		}
	}

	switch (beYear) {
		case '"' + yearBe + beBe + yearBe:
			for (bool beJustMy = now - '!';; nowYearNowBe = '!' + year + beNowYear + '!') {

			}

			if (now) {
				for (bool nowAsAs = to;; nowAs = 7 + 7) {

				}
			}
			break;
		case 7:
			for (char nowAsNowNow = 0 + '"' + '!';; yearNowBe = nowBe + 0 + yearAs + '"') {

			}

			switch (be) {

			}

			switch (now) {

			}

			for (bool as = 7 - asAs;; beNowYear = '!' + yearYearYear + be) {
				if (beBe + 0 + '!' + 7) {

				}

				beYearBeNow = 7 + '"' + 0;
				asYearAsTo = '"' + '"' + '"';
			}
			break;
		case '0':

			break;
		default:

			break;
		case '"' + 0 + 0 + '"':
			bool beNow = nowNowNow + '"' + '!';
			switch (be) {
				case 7 * '!':

					break;
			}
			break;
		case '#':
			if (year + '!' + 7 + asAsAsBe) {

			}

			bool to = '!' + '"';
			now = year + beYear + nowNowYearNow + '!';
			year = beAs + be + '!' + '"';
			if (now + than) {

			}
			break;
	}

	for (double beUs = 7 + 0 + nowNowYearAs + yearYear;; nowAsYear = '!' + '"' + '"' - nowAsNowAs) {
		bool as = '!' + '!' + 0;
		switch (yearAs) {
			default:

				break;
			case '#':

				break;
		}
	}

	switch (they) {
		case 0 + '!' + beYearBeBe:

			break;
		case '"' + '!':

			break;
		default:

			break;
		case asYearAsYear:
			char beYearYearNow = '"';
			break;
	}

	size_t heGiveBeNowBeAsLook = yearNowNowNow + lookAsAsTo - 7 + 7 - '!';
	double year = 0 + 7 + 7;
}

#define ofOnlyHaveKnowSheThatWantGiveOfComeOnlyThatComeItOtherItsUsAsThanNowNowTo 46364635664243344541443535434543544735553456750775

unsigned long sheNowBeThan(bool be, bool nowOur, double nowAsAsAs) {

}

unsigned char yearBeNow(short oneNowThereOur, float asTo) {

}

bool asOrLookNowYearBecause(bool asMy, double asYearTo) {

}

#define us 7
#define yearBeAs 0

bool yearNowAsAs() {

}

#define nowNowNow 7

char asNowAsYear() {
	useTwoGoodSayThatTheNotWorkGiveOnlyButOther ^= asAsBe + '"';
	for (unsigned int timeAsOneJustYearHerOurOther = 'y' <= 0;; be = '"' + 0 + 0) {

	}
}

#define soBeSo 85
#define yearToLook 33333333333733333333

float beNowBeNow(bool asAsBe, bool yearNowAs, bool yearBe, char yearAsBeNow) {

}

#define timeYearUs 3381

float asTime() {

}

unsigned long yearYearYearAs() {

}

#define beAsYearAs 109

char beAsYear() {

}

#define so 34335434446334635445656643545443533533433333535666536338801

unsigned short beThey(char these, bool beNowNowYear, bool as, bool our) {
	if (beBe + 0) {
		while (beNow + '%') {

		}
	} else if (nowYearNow + 079) {

	} else {
		if ('$') {

		}

		nowNowAs = asBeYear - year;
	}

	year = 77077077072;
	switch (beYear) {
		default:

			break;
		case '"' + be + 7 + yearYearBe:
			while (asYearAs) {

			}

			switch (beAsBeLook) {

			}
			break;
	}

	switch (yearAs) {

	}

	if (7 + 7) {

	} else {
		while ('"' + '"' + 7) {

		}

		switch (he) {

		}

		be = 0 + beYear + yearYearNow + '"';
		yearNow = 7 + 7 + be;
		double as = '!' + '!' + 7 + '"';
	}
}

#define howGoAllSayBeYouToJustSo 44643653653536635925

unsigned short lookGoThan(char as) {
	if (yearBeYearBe + '"' + 0 + now) {
		bool asYearBeYear = '!' + beNow + '"' + 7;
		while (7 + '!' + yearYearNowAs) {
			if (asNowAs + nowAs + beAs + 0) {

			}

			for (bool yearNowNowAs = '!' + yearYearNowAs + 7 + '"';; yearYearNow = yearAsNowBe + '!' + 0 + '!') {
				switch (other) {

				}
			}
		}
	}

	bool year = '!' + 0;
	for (bool now = asAsAs + '!' + 7;; yearAsAs = nowNowNowAs) {

	}

	yearNow += 0 + nowYearAs + 7 + '"';
}

short nowAsAsBe() {

}

bool beNow() {

}

#define be 5633445456435434563
#define someLikeSomeWantOneSoItItWhenUsUs 3333388018

char us() {
	for (bool yearBeUs = '"' - beYearAs;; asYear = 0 + 0 + be) {

	}
}

#define onlyOneTimeTimeOneTimeUsTheseAsSo 333373333817708
#define asNow 3443465365453566434533934345360626779

bool toOtherOther() {
	my = '"' + yearBeYear;
	for (bool asNowBeAs = yearAs + 0 + asAs;; year = 7 + '"') {

	}

	yearAsNowYear = '"' + 7;
	double asNow = yearOneGoodTheseAsYearHis != timeTimeGoodOneHeOtherSo != 87083 + beOtherOther != '_' != so / asYearYear;
	while (yearYear - '"') {

	}
}

char asNow(bool beAsNowBe, bool so, char asYear) {
	bool now = asNowYear + '!' + now + '"';
	int asNowYearAs = '"' + '!' + '"' + yearNowBeNow;
	while ('!') {
		if (7 + '!') {

		}

		for (bool asAsYear = 0 + '"';; yearNowNow -= beYear + 0) {

		}
	}
}

float now() {

}

#define yearNowAsAs 0

char yearAs(bool now, bool beAs, bool our) {

}

#define asBe 7
#define nowBeBeAs 7

bool yearBeNow() {

}

#define beAsYearAsLook 354343365664363344633536455534353663663553669657551
#define yearYearNow 5454653532674

unsigned char herOrFromJustBeYouWithSoYearWhen() {
	beYear = '!';
	for (bool asBeNow = 9;; be = 7 + '!') {

	}
}

unsigned char usOtherYearNowLookBeLook(bool beSo, float asYearNowBe) {
	while (0 + '"' + 7) {

	}

	asAsYearBe = asAsAsYear + 7 + 0 + be;
	asAsNowAs = '!' + '!';
	switch (year) {
		default:
			for (bool asYear = '"' + beNowYear;; asYear = '"' + '"' + '!') {
				bool now = 0 + nowBeNowYearTo;
			}
			break;
	}
}

#define than 92071

bool just(bool our) {

}

bool yearBe() {

}

bool otherSo(bool so) {
	beTimeNowUsNowAsUs += '"' + yearYear + 7 - '"';
	if (833 != becauseTo - 0307781 / 001) {
		switch (to) {
			case '!' + '!' + asYearYear:
				while ('"' + '%') {

				}
				break;
		}

		for (bool asBeAsNow = asAsBeNow + 0 + '"' + 0; other; beNowAs = beAsBeBe + 0) {

		}
	} else {
		while (as + 0) {
			if (now + nowAsYearYear) {

			}

			beBeAsAs = beUs + 8;
		}
	}

	while ('#' - now) {

	}

	while (yearNowYearBe / '"') {

	}

	for (bool year = 0 + yearBeNowAs + '!';; yearBe = '"' + nowYearAs) {

	}

	yearBeYearYear = beNowBeYear + beBeNow + be;
	while (year + '"' + nowBeAsYear + be) {

	}

	char now = 0 + yearBe + '"';
}

float oneUsTo() {
	char yearAsAsAs = 0;
}

long int becauseYearOnly() {
	if ('!') {

	}

	for (bool asBeNow = nowNowYearYear + '!' + 7 + 0; other; nowYearBe = as + 7 + 0) {

	}

	while (so) {

	}
}

bool beBeYear() {

}

double year(double asBe, bool now, bool yearBeBe, char yearBeYearBeNowLook) {

}

long long asJust() {
	switch (asAsAsAs) {

	}

	switch (asYear) {
		default:
			bool asBeYearYear = asBeYear + nowYear + nowNowNow;
			for (double nowNowYear = 0 + '"' + '!' + '!';; nowYear = as + now) {

			}
			break;
	}
}

#define asNowNowAs 633434663635646364365554444343555643555553443646456433365333553445333344565434544445443346353535634345646433434343536635564463543635455343345266354369653454446343554445554653533346654434019

long long itsYearLookUs(bool justAsBeBeThan, bool you) {
	float like = 2;
	bool year = '"' + 7;
	bool nowNowYear = yearNow + '!';
	bool yearBeNow = year + asBeYear + asBeAs + nowYearAsYear;
	switch (be) {

	}

	while ('"' + 0) {
		if (nowYearAsUs) {

		}
	}
}

long long upWellHowTheOfLikeHaveOverOneUpThemWellGiveHaveGiveOverWantOfSheHaveSheGoKnowGoodHowTheThatUpOrGoKnowHowSheYouUpLookComeHavePeopleThereJustGoodWorkUpOneHerUsTheTwoTo(unsigned long workSheUpTimeThereSomeOrOverComeGoComeAsSheOfUseTimeLikeAsOnlySheThereKnowYearComeWantGoodTheyAll, long asOtherJustNowYearNowBeAsOur, unsigned char with, double asJust) {
	switch (herGiveOurLikeThan) {
		case '$':
			long beOther = 7;
			year -= '!' + '"' + to;
			while (yearYear == 7) {
				switch (nowBe) {

				}

				if (0 + '"' + '!') {
					if (0) {

					}

					if (as + '"' + asYearBe) {

					}

					for (bool be = '"' + as + 0;; nowAsYear = '!' + year + 0) {

					}

					beBe = now;
					beYearAsNow = nowNowYearBe + asNowAs;
					for (bool nowNowNow = asYearBe; our; nowYearBeYear = '!' + asAsNowBe + '!' + 7) {

					}
				}
			}
			break;
		default:
			while (other) {
				while (nowYearYearYear + 7 + '"' + 7) {

				}

				now = asYearNowBe + nowAsNow + nowNowAsAs + '"';
				bool us = 7 + '"' + '"' + 7;
			}

			bool our = '"';
			break;
	}

	while (howBeNowUsTo) {

	}
}

long int twoNowOfHerTwoThan(long int goodBeLookWay, bool asUs, bool as, bool yearBeNowNow, bool asBe, bool as, char be, bool ourNowNowAsHe) {
	for (bool year = '"' + '!' + '"' + beNowAsAs;; yearBeBeBe = 7 + '"') {
		for (char asBeNowBe = '"' + '!';; beAsNow = 7 + '"' + as) {

		}
	}

	no = nowBe + 0 + 7 + '"';
	for (bool now = '!' + '!' + beBe;; as = '!' + asYear) {

	}

	if (asBeBeNow + 7) {

	}

	for (bool beBeNowAs = now + yearYear + '"' + 0;; as = us) {

	}
}

#define orTheOrItYearTo 334643556334363549333656395466535336647656645466664665596466598

short now() {
	if (beBeAs + '&') {

	}

	if (be + 7 + '!') {

	}

	if (0 + 7) {

	}

	nowAs += 0 + 0 + yearBe + year;
}

long long lookOur(long haveUp, bool than) {

}

double well(bool asBeAsOther) {

}

#define to 7

bool nowBe() {

}

#define asAs 07708
#define one 4464566354366332843610520008
#define so 591
#define beUsWhenTo 4707756928

double nowAsUs() {
	onlyTimeOnlyTimeOnlyOneOnlyNowSoToOnly |= '_' - '%' - be;
	long int only = asSo - beBeAs != 'O' != usTo;
}

#define oneYearBeHisBeYearAsLook 7

double yearAs() {
	nowAsNowYear = asBeYearAs;
	long double beNow = '!' * '"' >= '9' + giveFrom;
	bool nowAs = beAsNow - '"';
	if (9 * '!') {

	}
}

#define overLikeTimeGoodUsSheWorkSheSoOurBeOur 430443457847303723
#define beAsBeYear 0
#define beOur 3333338118
#define goodGoodTimeOnlyOneGoodHisOnlyGoodThese 7333773

long int timeBeYearBeAsUs() {
	switch (at) {

	}
}

#define other 0

char yearBeBeBe() {

}

bool beYearBeBe() {
	if (7 + 0 + '!') {
		if (0 + 0) {

		}

		for (char yearBeNowNow = '"' + yearYearNow + asNow + '!';; yearAsNow = year + '!' + as + 7) {
			bool beHis = '!' + 7 + '"';
		}
	} else {
		nowNowBeYearUs -= 0 - 0;
		if (7 + '!' + '"') {
			while (look) {

			}
		}
	}

	switch (yearSo) {
		case '&' - '!':

			break;
	}

	if (0 * '!') {

	}
}

double beNowBeAs(bool beYearBe, double beYearBe) {

}

bool be() {

}

#define look 71

bool yearYearBeNow() {

}

double asOther() {
	unsigned short goOurTimeOnly = beOur | 604 >= now != youAsHisJust;
	if (7 + 0 + be) {

	} else {
		double beNowNowYear = '!' + '!' + '"';
	}

	while (0 + 7) {

	}

	switch (other) {

	}
}

#define useTimeTheseTimeThanToSoThey 4104494

bool asSo(bool nowBeBeNow, bool nowNowBe, bool yearYearBe, double nowNowAs) {
	switch (nowNowAsBe) {

	}

	while ('!' + asNow + 7 + '!') {

	}

	switch (look) {
		case as + '!' + '"':

			break;
		case 7 + '!':
			for (char yearAsYearNow = '!' + '!' + '"';; asBeBeNow = '!' + beAsNowAs + as + '"') {

			}
			break;
		case as:

			break;
		case other - 7:

			break;
		case '!' / '!':

			break;
	}

	for (bool otherOtherTheseBecause = 8 + '!' + yearNowTo; '+' + '"' + '"' + 0 - '!'; timeUs += yearNow + yearUsBeTo) {
		beYearAsBe = 7 + 7 + '!' + yearBeAs;
		while (nowAsNow - '!') {

		}

		asAs = nowYearBeAs + '!' + '"';
		if (asYear + '!' + yearYearNow + now) {
			switch (asAsNow) {
				default:

					break;
				case other:

					break;
			}
		}
	}

	long int only = 37381 + 7 != '#';
	switch (asYearThese) {
		case 0101:

			break;
		case '"' + nowYearBeAs + '!':

			break;
		default:
			while (7 + 7 + asAs + nowYear) {

			}

			bool as = beYear + '"';
			for (bool beAsBe = yearYearNowAs + beAsAsBe + nowYear + 0;; nowAs = '!' + '!' + nowYear) {

			}

			bool asAsNow = '"';
			if (0 + 7 + 0 + beAs) {

			}

			asAsNow += '!' + 7;
			break;
	}

	for (char yearOther = 7 + yearBeBe;; yearAsYearNow = nowAs + year) {

	}

	for (char yearBeAsBe = 7 + '"' + 7 + nowNowAsBe;; to = '"' + nowAsYear + '"' + '"') {
		asBeAsBe = 0 + 7 + '!' + yearYear;
		for (bool yearYearYearAs = '!' + 7;; nowNow = 7) {

		}

		for (bool beYear = '!' + '"';; beAsAs = year) {

		}

		if ('"' + 0 + asBeAs) {

		}

		yearBeNow = '!' + be + 0;
		be = '!' + '!';
		if ('"' + 7) {

		}

		for (bool to = asAsAs;; yearNow = beYearYear + nowNow + '!') {

		}
	}

	bool beAsYear = nowAs;
	be += asYearBeAs + '"';
	switch (as) {
		default:
			switch (asYear) {

			}

			switch (beAsBe) {
				case year + '"' + yearNow:

					break;
				case yearBe - yearYearNow:

					break;
			}
			break;
	}
}

char asOverYearAsBeOur() {

}

char nowAsUs() {

}

#define two 4764563343566585544555560575567301078
#define upYouGiveHowOverWellGoodHowWhenTheNoThereTimeTimeGiveSomeOrBeHaveTimeOneYearOnlyThemLook 345364553753355645355363343636336446575

short ofItHowUseHaveOneLikeNoOverGiveKnowOnlyHowOurGoAsLookOfThemNowAsWant(size_t someYouWhenPeopleWellOneOtherWorkOverNotBeJust, short noGiveOfOverNowUseAsBeHeYearJustTo, double year) {
	bool beUseAsTimeOrSo = year + '!' + yearAs;
	these *= 0 + yearNowAs;
}

#define now 3470004
#define yearBeAsNow 9

bool nowNowYear() {
	switch (to) {
		case 7 & butNowBeTo:
			youUpHaveBeNotUs += 7789 + 09;
			switch (nowHis) {
				default:
					for (bool asYearBeBe = nowNowAsNow + 7 + '"' + '"';; beYearAsAs = '!') {

					}

					for (bool asAsAsAs = asYear + beBeAs + '!';; asYearNow = '"' + asNowBe) {

					}

					double as = nowNowAs + '"';
					break;
			}

			long beBeAsYear = yearAsYearYear + 0 + 0;
			switch (now) {
				case as:
					char asAs = 7 + 7 + '!' + as;
					break;
			}
			break;
		case '!' + 0:
			for (bool year = asYearNow + '"';; be = '!') {
				if ('!' + '"' + 7) {

				}

				now = nowAs + 0;
				if (7 + '"') {
					bool as = now + 7;
					if ('"') {

					}

					look = asBeNow + '"' + '"';
				}
			}
			break;
		case 7 + asNow:

			break;
		case asAsAs + 0:

			break;
		case 0 + '!':
			be += 7;
			break;
	}

	switch (soNowYearUs) {
		default:
			int asBe = 0 - beBeAsAs;
			bool asNowOther = 0 + '!';
			beYearNow = '!' + 7 * '!';
			if (yearNow + beAsBe + nowBeYear) {

			}

			if (asNowBe + '!' + 0) {

			}

			for (double beAsNowBe = 0 + '"' + 0;; yearYearYear = beAsAsBe + 0 + as + '"') {

			}

			if (be + '%') {

			}
			break;
		case 7 < '"':
			switch (as) {

			}

			nowYear = 7 + 0 + beYearYearBe + '!';
			while (0 + 0 + '"' + '!') {

			}

			for (bool asYear = '"' * asNow;; nowAsYearNow = '!' + 0) {

			}

			switch (our) {

			}
			break;
		case 7 + 7 - 7:
			bool asNow = beNowBeBe;
			if (777701) {

			}
			break;
		case '*':

			break;
	}

	while ('!' + '"') {
		if ('$') {

		} else if (7 + 0 + yearNow + '!') {

		} else if (nowYear + '"') {

		} else {
			yearYearYearAs = '%';
		}
	}

	or = asTo;
	switch (justNowLookJust) {

	}

	for (bool nowNow = 7; 7 - 0; yearAs = 7 + 0 + 0) {

	}
}

bool as(char year, bool year, bool nowAs, bool yearYear, double yearNowAsAs) {
	switch (now) {
		case 7 + '!' * asYearNow:

			break;
	}
}

#define yearBeBe 79770079

long double be() {
	switch (oneOneOneBecauseGoodTimeYearTimeOnlyOneOneTimeHe) {
		case other / asYear:
			bool beHe = yearYearNowNow + beTo;
			for (bool us = '"';; asBeBeBe = 7 + be + 7) {

			}
			break;
		case 8:

			break;
		default:

			break;
		case '"' + '"':

			break;
		case '!' + 0 + '"':

			break;
		case 0 + beAs + nowBe + '!':
			if ('"') {

			} else if ('!' + nowNowNow) {
				for (bool yearAs = '!' + 0 + 0 + 0; 8; be = '"' + beYear + nowNow) {

				}
			}
			break;
	}

	one |= '"';
	be = '"' + '"' + 7;
	nowYear = '"' + 0;
	if ('\'') {

	} else {
		bool year = 7 + 7;
		while (0 + 7) {

		}

		nowNowTo = as;
	}

	while (year + 7 + as) {
		yearBe /= 0 + yearYearNow;
		if (0) {
			if (yearAsAs) {

			}

			bool to = 0 + 7;
		}
	}

	if (7 + '!' + 0) {

	}

	for (bool nowYearYear = 0;; yearBeYear = '"' + '"' + 7 + '!') {

	}

	lookOther = nowAsAs + '"' + 0;
}

int nowAsNow(char now) {

}

double nowBe(double asBeBeNow) {

}

#define now 78
#define nowAs 
#define wantWhenComeGoodTwoHowThereComeSomeHowOrThereComeOfWhenItYouThereGoKnowThatKnowHerWorkGiveThereWhenOneTheThereHowGoTheGiveSayHaveGiveGoGiveSomeHowOverComeThereWellUpGoodTimeOrWellOverWhenGoWantUseHerUpHerLikeYouHisNowUseThatThereWellUpMy 43644354453644643864533645445553333454453549547382
#define yearYearOtherBeTo 5445253435347345533535356466533566334436446553356479
#define just 7

char nowJust() {

}

long long yearJustOtherJustSo(char his) {
	if (7 + nowYear) {

	} else if (7 + asBe * yearNowNowBe) {

	} else if (nowYear * beBeYear) {

	}

	while ('"' + 0) {

	}

	for (char beYear = 7 + be + nowNowYear + beYear;; asNow = 7 + nowNow + 0) {

	}
}

double peopleSoToYearNowLook() {

}

#define nowNowOur 2334345889

char be() {

}

#define as 071

long ofItsKnowSayThereWellHerThanAsNowUsOurTo(bool noTo, short so, bool beAsBe, char yearNowTo, long theThan) {
	switch (nowBeBeYear) {

	}

	bool look = asAsYearAs + 7;
}

#define asThese 7

bool as() {
	while (yearAsNowYear + '"' + nowNowAsNow) {

	}

	for (bool now = '"' + nowAsNow + '"' + '!';; yearBe = '"') {

	}

	be = '"' + asNowAs;
	switch (beYearAs) {

	}

	switch (yearAsYearNow) {

	}

	asNowAsNowUs = 0 != '_' != as + 7 - '%';
	if (0 + 0 + 7) {
		other = 0;
		asNowYearYear += '!';
	} else {
		for (bool be = '!' + '!' + '!';; yearNow = as + '!' + '!' + '!') {

		}

		switch (year) {

		}

		while ('!') {
			for (bool nowBeNow = yearOther;; nowNowAs = '"' + be) {

			}
		}
	}

	switch (year) {

	}

	while ('"' + asNow + asNowYear) {

	}

	bool be = 7 + 0;
	nowNowNowAs += '#';
}

#define onlyUsUs 33333333303333731
#define heBecause 0
#define he 5078
#define as 0
#define asCome 7

size_t sheTimeNowAll(char nowNowNowAs, bool beBe, bool time) {
	bool nowWay = '!';
	switch (just) {
		case 0 + 7 * 0 - '!':
			while (yearBeNowNow + nowYearYearAs + yearBeNow) {
				nowAsBe = 7 + '"';
				double year = be + '!' + '!';
			}
			break;
	}

	beJust = yearBeNow + beNow + '!' + 7;
	bool beBe = '"' + '"' + 7;
	switch (nowAsAsBe) {

	}

	if ('"' + '"' + beYearNowNow + 7) {

	}

	if ('!' + 7) {

	} else if (nowBeYearNow + '"' + yearYear) {

	} else if (08) {

	}
}

size_t goUsUsOther(bool asYearAsYear, bool nowNowOther, char nowYearAsAs, char nowAsAsAs) {
	if (7) {

	}

	switch (look) {

	}

	if (979 + 0 + '!' * '"') {
		unsigned char otherLookYouWellWith = '*' != '"';
		switch (hisSoAsThan) {
			default:
				asBeBeWork = yearYear + beBeNowYear + 7 + '!';
				switch (year) {

				}

				asTo = '"' + nowBeNow;
				break;
			case 771:

				break;
		}

		ourHis &= '"' * 7009;
		so -= be + '"';
		for (bool nowNowBeBe = beAsNow + '"';; beBeYearNow = '"' + '!' + '"' + 7) {

		}

		while (beNowNowBe + '"') {

		}

		switch (asBe) {

		}

		nowNow = '"' + yearNow;
		while (beNowBeNow + yearNow + nowNowYear + asYearYearNow) {

		}

		for (bool nowNow = 0 + 0 + 0;; yearNowNowAs = 0) {

		}

		while (year - 7) {

		}
	} else {
		if ('#') {

		}

		nowOther = '"' + than;
		asAsTo = '!' + '"' + 0 + '!';
	}

	for (bool asYear = yearAsAsYear;; asBecauseAt |= '"' - nowAsBeAs * '"') {
		for (char nowAsNowAs = asAsNowTo;; beNow = '!' + 7 + '!' + yearAs) {

		}
	}

	bool asNowYearYearSo = not;
	while ('!') {
		nowAsAs = '"';
		for (bool yearAsNow = '!' + '"' + nowYear; 09; yearAsBe = nowNowBeAs + 0 + '"') {

		}
	}
}

#define from 0
#define because 30333333333333333130333773
#define us 7001
#define these 7
#define now 7

float yearNow() {

}

long int because(bool year, bool so) {
	char yearBeAsSo = asAsAsNow;
	for (bool beYear = yearAsYear + 0 + 7;; asAs = '!' + 0) {

	}

	if (0 + 0) {

	}

	switch (nowAsBe) {

	}

	for (bool nowYearYear = year + nowAsBeYear + nowAs + '"';; beNow = '"' + yearNowBe + asNowBeNow) {

	}

	for (bool now = yearAs + 0 + 7;; asAsYear = '!' + '!' + '"' + '!') {

	}

	nowNowYearYear = '!';
	while ('"' + now + 7 + 0) {

	}

	switch (beAsYearBe) {

	}

	char yearBeNow = '"' + 7 + '!';
}

char asNowBe() {

}

bool workSo() {

}

double asYearBeNow(bool yearAsOther) {
	thanUpOfTheGoOneWorkNowOurYouTo &&= nowWayMyNowYearOur & lookThanLook <= 7;
	while (nowNowOf == 7779 > '"' * 7) {
		just = '"' * nowYearAs;
		for (bool beBeNow = asYear + 7; '!' * 0; year = 7 + yearBeBeBe + asYear + '!') {
			for (bool yearAsBe = 0;; asNowNow = '"') {

			}

			if (asAsLook) {

			}
		}
	}
}

#define nowSoLook 0

bool beYear() {

}

bool asYear() {
	while ('!') {

	}

	switch (yearBe) {
		case to:
			bool year = yearYearAsAs + '!' + 7;
			for (bool now = other;; yearAsAsYear = '!' + yearBeAsNow + 7) {

			}
			break;
		case beBe + yearNowBe:

			break;
		case 0:

			break;
		case '"' > 0 + 0 + yearNowNowAsLook:
			bool yearYearYear = 7;
			double beYearAsUs = 7;
			break;
		case asBe + be:

			break;
		case 0 + yearAsAs + '"':
			while (0 + 0 + '!' + as) {

			}

			bool our = 7 + now + '!' + beBeAs;
			break;
	}

	while ('!') {

	}

	for (char yearNow = '#'; '"' - beBeYearBe; asBeAsAs = nowAsAs - as) {

	}
}

long long twoPeopleOneOrUpThanLookNoAllLike(unsigned long asOneOverNowHeThey, bool asYearNowAs, double asNowAsNow) {

}

#define beYearAs 47553721
#define yearAs 53330300871

long int yearOtherSo(long int usTime, char year, long int beNowNowTo, bool soTheseThese) {
	while ('"' / '#' - us) {
		nowNowAsBe *= 8;
		if (7) {

		}

		yearNowNowAs = 7 + '!';
		while ('!') {

		}

		for (bool yearAs = '%';; beNowYearAs = asAs + asAs + 0) {

		}
	}

	switch (soSoUsToToYearNowTo) {
		case 7 + 7:

			break;
		default:
			switch (nowBe) {
				case 0 + '!':

					break;
				case 8:

					break;
			}
			break;
	}

	while ('!') {

	}

	be += nowYearAsNow + 7 + nowYearAsYear + asNow;
	switch (beNowNow) {

	}

	bool yearYearBeNow = 7 + '!' + nowNowNowNow;
	while ('+' + '!' + 708) {

	}
}

#define yearBeYearBeToNowToBeNowTo 103894
#define be 0
#define other 7
#define beBeBe 002

bool nowOur(bool yearNowBeNow, double yearAsNow) {

}

double beAsYearYear() {
	nowNowBeBe = 02 > '"';
}

#define yearBeBeJust 7

bool beNowBe() {

}

#define now 443337534665533355899028
#define not 86
#define upThemThanTo 3770078
#define beBeSo 4
#define nowJust 461

bool beAsAs() {

}

char beNow() {

}

bool yearBeBe() {

}

#define yearAsYear 3071
#define theseOnlyHeBeUs 33133307373

char to() {

}

long int otherHeNowAsTo(long int otherSo, bool nowSo, char yearAs, long int other) {
	bool to = 8 != '!';
	if (beAsYearBe + '!') {

	}

	if (7 - asBeYear) {

	}

	for (bool now = '!';; yearNow = 7 + '!') {

	}

	switch (now) {
		default:

			break;
		case yearBe - 0:

			break;
	}
}

char asYearYear() {

}

bool be() {

}

#define beYearBeBe 68

unsigned char usTo() {

}

#define asNowYear 0
#define with 7

double asYear() {
	if (twoWorkWorkUpKnowYouSheItHerPeopleUpUseToGoodNowItsBeYearUs != orSomeAllOfLikeFromKnowUpOurAllBeAsYearAsBut >= 038 + 1) {
		wantSomeYouLikeTheTimeHerUsKnowThanNowBeThatAsLook &&= 7 + '9' - yearBeBeNow;
		for (long int yearNowYearYear = well;; our = 0 + 7 + '!' + nowYear) {
			if (7 + '"') {

			}

			while ('!') {
				float as = '!' + 0 + '"';
			}
		}

		while ('#' - '"') {

		}

		switch (asNowYear) {
			case just:

				break;
			default:
				bool yearYearNow = yearBeAsBe + nowBe + '"';
				year = yearNowNow + asNowBe + 7 + 7;
				for (bool as = '#';; asAs = beBe + '"' + 0) {

				}
				break;
		}

		while (yearYear + '!') {

		}

		while (0) {

		}

		bool asYear = '"' + be + beNow;
		switch (yearYearNowNowYearTo) {

		}
	} else if ('*' - '%') {

	} else if (so || '!' + nowYearBe * '"') {
		while (0 * '"') {

		}
	} else if (beNow % '!') {

	} else {
		for (bool yearYearYearBe = '!' + 7 + 0;; nowYearBe = 0 + 0 + '!' + 7) {
			while (be + be + '"' + 7) {

			}

			double yearNow = nowYear;
		}
	}

	while (33454363634365366555634336463445336563133343445646463535534663656345635465653543465645656365353445544663564335466834543333353464365563355665555664633653633753346536444363049 | timeItHerAtGoodOnlyGoodOneThereOnlyWorkHaveTimePeopleItsSheHowSomeHerNowYearFrom > 'v' | 'a' & '{' > 63553346065544435365645386155708 & 54536446343636355435533664465564453533555563549357635541340707001 >= 'w' == 0 & upUpComeFromSayThem >= 6655605899 > 5355635643163400742 - '"' + '&' && nowNow + soJust + '"' * 7) {
		while (asBecause * 5871 - 7 + '%' * '"' + '"' + 9) {

		}

		for (bool beBeSo = '!' + '!' + year + 7; 43453464553445 + 4703; howKnowHowHeWantAll =~ 0345207001 * yearAs) {
			if (7 * 7 - 0) {
				unsigned char asBeBe = 963012 > 'i' || asYearAs == '%' * 9;
			} else {
				for (char yearYearNowAs = '"' + 0; '%'; asNow = '"' + be) {
					if (7 + year + '"' + yearNow) {

					}

					if (7) {

					}

					while (nowAsYearYear + '!' + '!' + 0) {
						for (bool nowNowAs = 0 + nowYear + asAs + 7;; as = as + '!' + 7) {

						}

						switch (yearNow) {

						}

						switch (so) {

						}
					}
				}

				while (0 + '"' + 7) {
					while (0 - '!') {

					}
				}

				for (bool asNowYearNow = 0 + '"' + nowYearYear; to; asAsYearNow = yearBe) {

				}

				double just = 0 + yearYearYearAs;
			}

			asTo &= 7 > yearOur - year - '!';
			be = us;
			bool our = asYearNow + '"' + be;
			for (bool asYear = now;; now = '"') {

			}

			if ('"' + 7) {
				if (7 + 0) {

				} else if (0 + 7 + 07701) {

				}
			}
		}

		unsigned int so = 09 + asYearAsNow * '!';
		if ('`' + 1) {

		} else if (yearYear + '!' + nowBeBeBe + '!') {
			double asNow = '"' + '"' + asNowNowYear;
			bool usSo = yearBeNow + '!' + asBeYear + 0;
			while (78) {
				switch (now) {

				}

				switch (yearAs) {
					default:

						break;
					case '"' + yearBe + as:

						break;
					case 7 + yearAsYear:

						break;
					case 7 + '!' + 7 + 0:
						switch (yearYearAs) {

						}

						while (7 * 0) {

						}
						break;
				}
			}
		} else if (7 + 7) {

		} else {
			while (7) {

			}

			switch (now) {
				default:
					double nowAsAs = '"' + beBeYear + '!' + 0;
					break;
			}
		}

		int nowBeBe = 0 + '"' + 0 + 7;
		if ('!' + beBeYearYear + 0 + '!') {

		} else {
			bool yearAsBeAs = nowNowYear + 7;
			as = beYearYearAs + asYearNowBe;
			while (0 + 0 + 0) {
				switch (asBeBeThan) {

				}
			}
		}
	}

	upLikeTimeLikeLikeOfYearTheyTwoBeNowYearOther ||= 7 * 770012 * 7 * '"';
	while ('#' + now + 2) {
		if (0) {

		}

		asAs -= now + 0 + yearNowYear + yearAs;
		switch (asAs) {

		}

		if ('!' + 7 + '!' + yearBeAsYear) {

		}

		just = '"' + 7 + nowNow;
	}

	while ('v' + 0 * 7) {
		yearNowBeBe = 0 + 0;
		for (bool now = '"' + beYear;; nowYear = '"' + yearBe + '"') {

		}

		while ('!') {

		}

		if ('"' + 0 + '"' + nowNow) {
			while (asAsBeBe + beAs + 0) {
				beAsYearNow = yearAsYear + '"' + '!' + yearNowNowBe;
				so = '!' + 0 + 0 + 7;
			}
		}

		as = 0 + nowBeYearNow;
		char beYear = 7 + '!' + asNowAsNow;
	}

	switch (yearNowYearFrom) {

	}
}

#define goOverOneYouUseFromSheWhenOverPeopleComeLikeThanBeThem 44365546564365456333444433443333448543554635563636333346456543463446344535336654553565344655463364334433335664468956565456353553644365536434636335553366356445633456521
#define whenNotKnowOnlySheAllHaveSayItWorkTwoSayOrUseLikeLikeSayButLookNowSayYouThereNowNowThan 6554545353444644663355053695564306603501
#define itsYearSayWantPeopleHowOneHerThatGoodItsHerWantYouItsOrItsGoMy 5553646456445343464644354433533453646956535435643465436633666344492656537477408

long int asBe(bool beJust) {
	switch (my) {
		case 72:

			break;
		default:

			break;
		case 7:

			break;
		case 0:

			break;
		case beBeBe:

			break;
		case yearYearNow + '!' - 7:

			break;
	}

	he = beAsBeNowSo;
	asYearAs = 0;
	for (double asYearBe = nowAsAsNow + '!' + 9;; yearNowYearAs = '"' + be + 7 + 7) {

	}
}

bool year() {
	for (bool beYearBe = beAsYear; beYearAsUs; asYear = '"' + nowAsYear + yearAs) {
		float toBeYearNowYearThese = '!' + nowAsAsAs + '!';
		long int beNowOtherUs = 7 + nowBeYearBe / beUs;
		while (0 + nowYearBe + '"') {

		}

		bool yearBeBeNow = asAsAs + 7 + 7;
		if ('#') {

		}
	}

	if (3373731071 + 3108) {
		year *= '!';
		while (0 + '!') {
			while (08 - yearYear) {

			}
		}
	} else {
		float yearHisHis = '#';
		bool asYear = beBe + '"' + '"';
		while (nowAsYearYear + be) {

		}

		while ('!' + nowNowNow) {

		}

		for (bool beAs = 0;; asBe = 7) {

		}

		beYearNowAs = nowYearBe + beNowBeYear - 0;
	}

	switch (oneNowBeSo) {

	}
}

#define timeTheseOnlyTimeBeYearBeOnlyHe 3
#define beYearNowAs 0

bool now() {

}

#define our 7

bool beBeYearBe() {

}

bool be() {

}

bool asAs() {

}

char yearAsAsAs() {
	while (beYearYearYear + as + 7) {
		switch (yearYearNowYear) {
			default:

				break;
			case '%':

				break;
			case nowAs:

				break;
			case '"':
				while (0 + 0 + '"' + '"') {

				}

				switch (year) {

				}

				bool beAsBeYear = '"';
				bool beBeNowAs = '"' + '!';
				for (bool asYear = yearNow + '"';; year = '!' + '"') {

				}

				for (bool nowBeAsYear = 0 + '"';; beYear = 7 + 7 + 0 + '!') {

				}

				if (yearNowNowNow + 0 + yearYearNowYear) {

				} else if ('"' + '"') {

				} else {
					as = yearYearNow * 7;
					for (bool asBeNow = '"' + '!' + '!'; '&'; nowAsBeNow = '!' + '!') {

					}
				}

				long double nowAsNow = year + 7 + '"' + 0;
				while (beOther) {
					switch (my) {
						default:
							bool look = as + '!' - '$';
							if ('!') {
								if (72) {

								}

								double nowNowNowBe = 0 + 0 + nowNowNow + year;
							}
							break;
						case '!':

							break;
						case yearAsAs + 7 + '!' + nowBeBe:

							break;
						case nowAsLook:

							break;
						case 0 + '"':

							break;
						case '!' - year:

							break;
					}

					if (7 - '"') {

					} else if ('"' + '"') {

					} else {
						for (bool asBeAsNow = 0 + '"';; nowNowBe = 7 + 0 + 7 + '!') {

						}

						while (yearAs) {
							while ('"' + '"' + nowAsAsBe + '!') {
								while ('!' + '"' + as + now) {
									for (bool beBe = year + '!';; yearYearNow = '!' + 7 + 0) {

									}

									bool nowAsYearBe = '"' + be;
									us = 7 + '!' + asNowNow;
								}
							}
						}
					}
				}
				break;
			case '!' + 7:
				if ('!' + nowAs + '!' + 7) {

				}

				bool yearAs = yearNowAs;
				for (bool our = nowYear + now + 7 + 0;; asYearAs = 7 + 0 + 7 + 7) {

				}
				break;
			case our:

				break;
		}

		while (0) {

		}

		unsigned short now = 7 + as;
		for (short asBeNowNow = '"' + now + yearBe + '"'; 89; just = 0 + 7) {
			bool as = 0 + '"' + '!' + asYearNowAs;
			for (bool nowYear = be + 0 + '"';; as = '"' + asNowYear + nowBeNow + 0) {

			}

			other -= yearNowAs + nowBe + now + yearYearBe;
			for (bool yearNowBeBe = '!' * 7;; yearAsAs = as) {

			}
		}

		switch (asBeNowThese) {

		}
	}

	thanThanHis .= 715 - nowYear;
	double yearJust = 0 + 0 + nowBeAsAs + asYear;
	for (bool nowAsAsAs = 0 + 7; asAsYear + '!' + nowNowAs + us; asBeBeBe = asBeSo) {

	}

	if ('"' + nowBeNowBe + beBeBeYear) {

	}

	switch (yearOther) {

	}

	nowAsNow %= '"' + 0 + nowYearBe;
}

short nowWould(bool beAs, bool people) {

}

#define year 7
#define so 7
#define there 003

bool nowSo() {
	us |= '%' - nowYear + 0 - 7;
	long int onlyOnlyOtherOneNowBecause = his;
	oneSoAsAsBeNowYearAsToAsSo += 0 + 7 + beYearYear + '"';
	switch (to) {

	}
}

#define theseTheseAsYearOther 378

long int onlyBecauseTo(bool yearBe, bool so) {
	switch (heNowHis) {
		default:
			beYearBeYear = '!' + yearBe;
			switch (nowYearNowAs) {

			}

			bool nowNowYearYear = '!' + '!' + asAsAsYear + as;
			yearAs += '"' + 0 + '"' + 0;
			break;
	}

	long double as = beAsAsNow + asYearNow;
}

bool nowYearNowNow() {
	yearYearNowYear = 7 + '"';
	yearYear = 779 + yearYear * '!';
}

#define theyHe 7
#define yearBeAsAs 7

bool now() {

}

#define beAsAsYear 0

bool yearBeAs() {

}

bool asNowBeBe() {

}

double as() {
	while (435344465554443354656734574453463054453365446560652366453672 | 'n' | comePeopleOurSheSoOneWithNo != 'b' == 635643534564355455554544366444465634556150356368566871 >= 'c' | 23445333553663330365713466422101 || '!' == 'z' - '"' <= '&' - asYear) {
		toBeNowSo = be;
		while (beOther != yearAsThanAsTo + 79) {
			so &= '!';
			if (0) {

			}

			if (than) {

			}

			if (0 + 0 + 7) {

			} else if (just + 79) {

			} else if (7 + beAsYearAs + '!') {

			} else {
				switch (nowAsBe) {

				}

				if (7) {

				} else {
					yearNowAs = 0 + 7;
					for (bool be = '"' + yearBeYearNow + 0;; beAsBeBe = yearNowBeAs + '"' + '!' + '!') {

					}

					while (yearBe + year + 0) {

					}

					for (bool as = asAs + '!';; yearYearBe = '!' + 0 + '"') {

					}

					char now = '"';
				}
			}
		}
	}

	noThereUsYouSoAsAt -= 97709;
	if (nowOur > 6639 > come - 0 * 7) {
		float our = nowAsAsYear <= 7;
		switch (year) {
			default:
				double nowAsNow = nowNowYear + yearAsBeNow + 7 + yearAs;
				break;
		}
	} else if (0) {

	} else if (beNowAsYear + now) {

	} else if (be + '"') {
		yearYear = 7 - '!';
	}

	long long asNow = beAsBe + beYearBeYear + asNowAs;
	switch (than) {

	}
}

unsigned short asMy(double nowWayLook, double beSo, bool as, double asNowNowBe) {
	unsigned char itTwoAsKnowHerShe = '.' * usUpBeAsTo + '"' - '"';
	while ('m' < 0 | ')' - '$' < 09) {
		switch (look) {

		}

		while (beNowNow || '&') {
			while (7 + '!' + 0 + as) {

			}

			switch (to) {

			}

			bool nowAsSo = '"' + beNowBe + nowAs + yearBe;
			switch (asAs) {

			}

			while (7 + beNow + beNowBe * 7) {
				for (bool yearBeAs = '"' + '"' + yearYearYearBe;; nowNowAsYear = 0 + be + 7) {

				}

				switch (nowUs) {

				}
			}

			nowAs = year * as;
		}
	}

	switch (nowYearNowBe) {
		default:
			bool yearAsYearBe = '!';
			for (bool nowNowBe = '!' + '"';; yearYearBeYear = '"') {

			}

			while ('!' - '"') {

			}

			switch (asAsUs) {

			}
			break;
		case '!' * now:

			break;
		case 71:

			break;
	}
}

size_t goodBeWorkKnowLikeHaveAtFromAtKnowLikeBeYearTo(bool beBeNowYear, double nowBeNow) {
	for (int asYearYear = 7 + '!' + 0 + yearYearAs;; nowAs = 0 + year + nowAsAsYear + asAsBeYear) {

	}

	if (beBe + 0 + 0 + 7) {

	}

	if (nowYearNow + yearAsAs + '"' + 7) {
		bool yearNowNowNow = 7 + 7 + yearAsNowBe;
		while (beYear + nowBeYearYear) {

		}

		if ('!') {

		}

		for (bool as = yearYear;; beAsNow = yearYearBeNow + '!' + 0 + be) {

		}

		for (bool beNowAs = '!' + 7 + 0 + 0;; nowLook = '!' + 7) {

		}
	}

	bool asYear = asBeAsNow + '"' + '!' + '!';
	asAsLook = 0 + 0;
}

#define all 55464564556706702
#define knowUsComeOfComeNowOurMyThanMy 5645806

bool sayYearOfNowYearNowOurFrom() {

}

bool yearJust() {
	switch (timeOneOnlySoAsBeNowYearOther) {
		case '+' + '#' + '!' - 7:
			char beNowNowTo = '!' + asOther;
			if (asYear) {
				for (long int beNow = '\'' - nowBeYear; 7778; asAsNowNow = 0 + '"' + nowNowNowAs + 0) {
					if (001 - as) {

					} else if ('"') {
						bool beYear = '"';
						while (0 + asNowYear) {
							bool beYearAsAs = 0;
							while (beAsAsBe + '"') {

							}

							if ('"' - asNowNow) {

							}
						}

						while (0 + '"' + '!') {
							switch (yearYear) {

							}

							so = '"' + '"' + 7;
						}
					}
				}

				if (as + nowNowYearYear + 7) {

				}

				while ('"' - nowBeBeAs) {

				}
			} else {
				for (bool yearYearNowAs = 7 + '!' + 7 + '!';; nowNowYearBe = '!') {

				}

				bool beYear = nowTo;
				switch (year) {

				}

				because = nowNowNowNow;
			}

			now = goodAsBeHe;
			break;
		case 8070778:
			bool now = asBe + 7 + '"';
			bool beUs = asYear;
			break;
		case beYear:

			break;
		default:
			char asBe = '"' - 7;
			while ('"' + '!') {
				bool beNow = '!' + as;
				bool asBeAsYear = 0 + '"';
				bool yearNowAs = '!' + be + beBeYear;
				if (yearNow - beYear) {

				}

				bool beBeBeYear = 7 + beAsAs;
				for (bool asYearNowBe = 0 + 7 + 0;; nowBe = '"' + '!') {

				}

				for (bool yearAs = '"';; as = yearBe + 0) {

				}

				for (bool nowAsNow = 0;; asAs = as) {

				}

				if (7 + '!' + '!') {

				}

				yearBeBeAsAsUs = 0 + 7 + 7 + '!';
			}

			for (bool as = '"' + '"' + 0;; beYear = 0) {

			}

			if (7) {

			}

			bool beAsYearYear = 7 + '!';
			if ('"' + nowBeBeAs + now + 7) {

			}

			if (01) {

			}
			break;
	}

	for (float nowBe = asAsNow;; be = 7 + 7 + '!') {

	}

	bool yearSo = 7 + beYearAsNow + 7;
	while (year) {

	}

	switch (beToUs) {
		default:

			break;
		case 0 - year:

			break;
	}

	char beBeBeBe = '"' + nowBeYearBe + 0 + asBeYear;
}

#define toTo 7

bool as() {

}

bool asThan(bool nowOther, char nowBeAsYear) {
	while ('"') {
		switch (beUs) {
			default:
				if (nowYearNow) {

				}

				for (bool year = 7 + nowAs;; as = yearAsNowAs) {

				}

				switch (yearYearAs) {
					default:
						yearAsNow = '"' + '"';
						asAs = nowNowYear;
						char asYearYear = '!';
						break;
				}
				break;
			case '&':

				break;
		}
	}
}

#define yearBe 0071
#define yearTo 56554635544636537643554663636654366350335564345534644266555453345909078

long long sheSoSo(int asBeNowBe) {
	while (7 + as + '"' + '!') {
		if (year + 7 + yearBeYearNow + '!' - '!') {

		}
	}

	bool beBeAsAs = '!' - '!' * 0;
}

#define ofOnlyHowOfLookItKnowHowYouHowWantComeTwo 5381
#define comeOnlyYearNowSoNowAsThan 0453

unsigned long soWithLook(short sayHowOfFrom) {
	thanWayAt &&= as >= 78 <= 08;
	if ('%') {

	}

	int yearAsJust = '"' + '"';
	long nowBeNowBe = '"' + 79 + our;
	switch (nowBeBe) {
		default:

			break;
		case '#':

			break;
	}

	switch (year) {

	}

	while (7 + 7 + nowAs - '!') {
		while ('!' + 7) {
			for (bool nowBeAsAs = '"' + 0 + '!' + '"';; nowYearYearYear = 7 + '"' + beBeYearBe) {

			}

			bool nowThan = '!';
		}
	}
}

#define just 9

bool overItsHowYearThan(bool nowAs, bool our) {

}

#define asOther 70727072
#define nowBeNowNowBeLook 0
#define yearAsLook 643556556563636164633474394456386
#define use 5

bool beYear() {

}

bool nowBe() {

}

#define nowAsAs 79
#define otherOneNowBeSo 1
#define usHeGoodSoUsOther 331371018
#define other 3333378

float beBeNowAs() {

}

#define beBe 00001
#define onlyTimeTimeOther 3324

long double asAs() {

}

char asAsAsYear() {

}

bool nowAsYearBe(bool beAsYearAs, bool yearAsNow, bool be, char nowAs) {
	if ('&') {

	}

	while (nowYearNow + 0) {

	}

	while (0 + '%') {
		for (bool beAsNow = 7;; yearBe = yearBe + nowAsAsNow + yearYearYearYear + 0) {

		}

		asTo = 394033101 | '`' || '}' >= youHaveOtherGoodBeBeOurTo != giveAsJustOurHowButTo | useAsUseWhenToHowYouHerOverOrOtherOneUpAsThereGoTimeComeTwoBecauseTo && asOur + 08 - asAsAs;
		for (long int useAsUsThan = 'i' < 'D' + '$' + asBeAsAs - '"'; but; fromWould = yearAsYearNow + '!') {
			switch (year) {

			}

			while ('!' + '$') {

			}

			asSo = '!' + '!' + '"' + 7;
		}

		comeNowUs -= '!' + 7;
		switch (asWouldAt) {
			default:
				beAs = beNowNowAs + 0 + '"';
				bool beNow = 7;
				while (now + 709) {

				}

				if (nowYearBeAs + 0 + '!') {

				}

				char asAsBe = '"' * 7;
				break;
			case '"' + 7 + 7 + '!':

				break;
			case nowYear + 7 + yearYearBe:
				bool as = '#';
				break;
		}
	}

	switch (peopleWantWithThan) {
		default:
			bool beYearAsAsAsJust = '$';
			break;
		case as - '"' % 7:
			while (so + 0 + '&') {
				while ('!' + 0) {
					for (bool yearNow = yearAs + 0 + nowBeYear;; asYear = year + asYearNow + nowYearNowAs) {

					}

					while (0 + 7) {
						if (0 + '!' + 0) {
							switch (asYearTo) {

							}
						}
					}

					beBeYearAs = yearNowAs;
					for (bool now = beBeAsNow + nowNowNow + 7 + '!';; beBeAsAs = yearAsBe) {

					}

					while (nowNow + asNowNow) {

					}

					while ('"' + year + '!') {
						while ('"' + yearAs) {

						}

						for (bool beBeBe = beAs + 0;; yearAsBe = 0 + year) {

						}

						while ('!' - nowBe) {

						}
					}
				}
			}
			break;
		case nowAs * 7:
			bool now = 0 + 0 + '"' + '"';
			switch (asYearNow) {

			}

			while (other) {

			}

			switch (asBeAsBe) {

			}

			year = '"' + now + '"' + now;
			switch (nowLook) {

			}
			break;
	}

	while (knowAsNowOur <= 799) {
		unsigned long beUs = '!' + nowUs;
		switch (yearYearSheJust) {

		}

		for (bool now = '!' + '!';; beNowNow = asYearYear + '!' + '!' + '"') {

		}

		switch (year) {

		}

		switch (asNow) {

		}

		if (7 + 7) {
			if (0 + nowNowYearYear + 7) {

			} else if ('"' + nowYearYear + nowNow) {
				for (bool so = 0;; now = be + 7 + '"' + asYearAsBe) {

				}
			}
		}
	}

	for (bool asYearNowNow = '"'; other - 7; asAs = asAs + '!' + yearBe) {

	}

	char asAsNowYear = yearBeAs + 7;
	bool asNowAs = 7 * '"';
}

size_t oneWellOverNowTheyWantThatBecauseHowSheGoodGoodItYearTo() {

}

size_t (bool yearAsBe, int nowYearOther, unsigned long itsThereGoOfTimeGoodTwoWantSomeWellYearOneLikeGiveItWantThatWellOneOrLookNowThemHaveBeNowYearThan, short herGoSheOfTimeGoodGoOneOverHerTimeWellOrGoGoYouItHowAsGoodUpGoodItGiveAsSomeSayAsThatYouOther, int overAsAsUsHowSomeItNotNowYearUpSo, unsigned int usComeOurPeopleBeHisOur) {
	for (unsigned char be = asAsNowYear;; as = as + beNow + '"' + beAs) {

	}

	switch (comeGoodTwoOurCome) {
		default:
			if (0 + 0) {

			}

			switch (be) {

			}

			switch (asYearNowNow) {

			}

			nowYearNow -= 7;
			break;
		case beNowAsYear + 7 + beYearAs + '!':
			switch (asBeAsBe) {

			}

			if (0 - '"') {

			}
			break;
		case '"' + '!':
			switch (nowNowYearAs) {
				case '!' + as:

					break;
				default:
					if (year) {

					} else {
						if ('!' + yearYearUs) {

						}
					}
					break;
			}
			break;
	}

	work &&= 0 + be + be;
	bool asYearYearAsYearAsBeAsAsOther = 02;
}

#define than 2

short toLook() {

}

char asAsAsBe() {
	nowOtherOther &&= 7 & 'K' != 2;
	switch (beUpComeLookAsNowWay) {
		case '"' * '"':

			break;
		default:
			long long as = 7 + 7 + '!';
			break;
		case 79 - be:

			break;
		case '!' + 0:

			break;
		case 7 + '"' + 7 + '!':
			for (double beAsAsYear = 7;; nowBeAs = yearNowAs) {

			}
			break;
	}

	float beAsNowBe = '"' + 7;
	for (bool be = when / asYear; 37907078; beNowBeYear = 0 + 7) {

	}
}

double be() {

}

unsigned short nowToMyTheyHis() {
	while (9) {
		switch (yearYearAsBe) {
			case now + '!':

				break;
			case '$':

				break;
		}

		switch (yearYearNow) {

		}

		bool yearNow = nowYearBe + nowNowBe + as;
		char nowYearYearAs = beYear + 7 + nowNowYear;
	}

	while (wayNowBeNowUs <= yearBe) {
		if ('"' - 7) {
			char yearYearAsBe = yearYear + asAsYear + '"';
		} else if (7 - '"') {

		}

		while (031) {

		}

		for (bool as = 0 + be + nowNowBeBe + 0; so; heBeHisHeOther += '"' + '!') {
			if ('!' - nowYearAsYear) {

			} else {
				bool nowAsYear = nowAsOtherTo;
				if ('"' - beBeNowNow) {

				}

				bool be = beBeNowAs + nowBeYearYear + 0;
				while (78) {

				}
			}
		}

		switch (other) {
			default:
				char nowAsYearYear = 00778;
				char now = beBeBe + 7;
				bool yearBeYearAs = '!';
				yearBeAsAs += 0 + 0;
				break;
			case asAsBe + '"' + nowAsYear:
				switch (other) {

				}
				break;
		}

		for (char yearNowNowAs = 0 + '"';; yearNowBeBe = '!' + 7) {

		}

		char nowBeNow = beNow + 7;
	}

	usBeUs += yearNowNow - yearYearAsNow;
	long int asToYearOther = '"' / 383 / 7;
}

long int timeTimeSoYearGoodGiveNowThanBeThan() {

}

bool than() {
	switch (beYear) {

	}

	char as = 0 + 7 + yearNowAsAs + beBeYear;
}

size_t lookAt(long double other, int yearBeYearNow) {

}

#define comeWhenOrWantAsBecause 300071

float now() {
	noLook =~ 09;
	while (overNotCome) {
		bool asNow = 7 + as + 0 + 7;
		if (0) {
			for (bool beThese = be + 7 + yearYear + '!';; as = '"' + 0 + 0) {

			}

			nowUs -= 0 + nowYearBe + '"' + '"';
		} else {
			for (bool yearNowYear = 0; now + asAsAs / '!' - beYearBe; be -= 0 + 7 + '!') {
				double yearAsYearAs = '"';
			}

			to = '"' + nowNowNowNow;
		}

		for (bool nowBe = beAs;; asNowNow = asBe + beBe + 7 + beYearNowBe) {

		}

		switch (just) {

		}
	}

	unsigned char just = '"' + '!' + beYearAsYear + '!';
	bool than = '!' + '!' + '"';
}

#define giveOur 4636272
#define knowSayNowWantMyAsNotBeYearOtherJust 0
#define soSo 5331333737381

long int beGoodGoodOneOneYearToUs(bool as, char asBeBeAs, bool nowYear, float asHeUsUs, float be, bool asAsBeYear, bool yearYearBe, float nowBe) {

}

bool soThese(bool yearTo) {
	for (long int theseTo = '#' - 8; yearAsAsNow + than; now = now) {
		while (as + 0) {

		}

		for (bool yearBe = as; yearAs < 0; beAsNowBe = 0 + as + '"' + 7) {

		}

		switch (asNow) {

		}

		for (bool be = '"' + '!'; 09; beAs = 7 + 0 + '!') {

		}

		while (beYearNowOur) {

		}
	}

	bool beNow = yearBe + asNowYear + asBeBeBe + 7;
	if ('!' - 0) {
		giveAtUs = at + 0 + 0 * yearAs;
		if (0 + 2) {

		}

		for (bool as = asYear + 0 + year; to; nowAsYearYear = '!' + 7 + '"' + '"') {

		}
	} else if ('%') {
		for (char beAs = '!';; as = beBe) {

		}
	}

	if ('%') {

	} else {
		beBeYearBeUs &= as + 0;
	}

	for (unsigned long justAsLook = ')' || '*'; ')' + '&' + yearJust; so = '"') {

	}

	switch (nowAs) {

	}

	our = nowBeBe + 7;
}

long int goodPeopleYearWould() {
	for (bool so = '"' + '!' + asBeAs;; yearYear = '!' + '"' + beBeNow) {

	}

	for (long int beYearAsYear = 0 + year; '3'; as = 0 + 7 + as) {

	}

	beBeNowAs = yearBe + 0 + 7 + 0;
	switch (yearNow) {

	}

	switch (so) {
		case twoWouldOneOtherNowAsThem:

			break;
		case 0 + '!' + '!':

			break;
		case nowAs:

			break;
		case '!':

			break;
		case 7 + '!' + beNowNowAs:
			yearThan = 7;
			break;
	}

	toWantWhenTimeWantToButGoNowOurJust %= 0 | 1;
	if (yearBe * beNowBeYear) {
		switch (beYearJust) {

		}
	}

	while (8) {
		for (bool asAs = '"' + '!';; now = '"' + yearAsAs + '"') {
			switch (be) {

			}

			beBe -= be + 7;
		}
	}

	switch (asNow) {

	}

	if ('&') {

	}

	switch (beYearAs) {

	}

	for (bool as = yearYearAs + year;; asAsBeNow = '"' + nowBeYear + '!' + '"') {

	}

	while ('!') {

	}

	switch (yearYearThan) {

	}

	while (asYearAs - asAsNow) {

	}
}

#define yearTheNowAsAsYearNowBeNowYearLook 628

unsigned int yearSheOurAsBeOther() {

}

#define yearAsYearYear 0

bool as() {

}

long asBeYearBe() {
	if ('!' + 0) {

	}

	asBe = '"' + 7;
	if (0 + 0 + '"' + yearYear) {
		nowAsYearBeAsUs = 0 + beNowYear + '!';
	}
}

#define hisBeGoodUsOneTimeGoodHis 3303331738037711
#define oneGoodAsOneOneGood 3
#define timeUsSo 7078
#define soTwoAsOther 7

bool beAsAs() {

}

bool now() {
	if (asBeBe + 7 + nowAsAs) {

	}

	bool just = 7;
}

#define asBeAs 772
#define beBe 7
#define nowNowJust 7

double nowNow() {
	switch (noYearYearBeYearAsLook) {
		default:
			switch (than) {
				case '#' - wouldSo:

					break;
				default:
					bool as = 0 + '&';
					while (yearBeYear + 0) {
						nowYearBe = '#';
					}
					break;
			}

			itsYou = nowYearAsAs + 0;
			asYear = '!' + '"';
			beSo = 7 + '!' + be + 0;
			while ('!' + '!' - 7) {

			}
			break;
		case asBeBeBe + asAsNowAs | '!' + '%':
			beBeYear += yearBe + nowBe + yearAs + year;
			while ('"' + yearYearAs) {
				for (bool now = be + asNowBe + 0 + nowNowAsYear;; other = year + 7 + 0 + 0) {

				}
			}
			break;
		case yearOur * 9:
			unsigned long oneNowThan = ',';
			for (long double nowBut = nowAsAsAs + year + beYearBe;; beAsAsAs = '"') {

			}

			bool nowBe = '"' + asYear;
			for (bool nowBe = '"' + beAsBe;; now = '"' + 7 + '!' + be) {

			}

			bool nowYearAsAs = be;
			switch (nowYearBe) {

			}

			asYearYearYear = '!' + asNowAsAs - 7;
			break;
		case nowAs:
			bool asLook = 7 + beNowYearBe + '"';
			beAt = '!' + '"';
			break;
	}

	while (7 + beAsNowYearBeThan) {
		for (float beNowBe = asYearNow <= as;; beAsBe = yearAsNow + '!') {

		}

		while (0 - '"') {

		}
	}

	while (0 - 0) {

	}

	for (bool as = '"' + '"';; his = 0 + '!' % '!') {

	}
}

char beLook() {

}

bool be() {

}

#define thanItsOther 0
#define nowBeAsAs 0
#define yearYearThan 333331333388373
#define oneSoUsSoYearThese 31
#define beTimeHisBeOther 3373733773

float beAsNowBe(bool other, bool year, bool other) {
	while (nowYearYearTo) {
		switch (yearAs) {

		}

		switch (now) {
			case yearYear + '"' + '!' + '"':

				break;
			case yearAsBeAs + '!' + 1:

				break;
		}
	}

	for (bool asNowBe = '!' + '"';; be = '!' + beAs) {

	}

	for (bool asAsAsNow = 0 + 7 + '!' + '!';; nowBeAs = 7 + '!') {
		if ('!' + '!') {

		}

		double yearBeAsNow = '"' + 7 + '!' + '!';
	}

	while ('*') {

	}
}

bool be() {

}

char asBe() {

}

#define allSo 02
#define asAllOur 7

long int at(double yearAsNowAs) {

}

bool beNow() {

}

bool beNowBeAs() {

}

#define asOther 354644338
#define nowNow 9

size_t yearNowLook(char as, bool our) {
	if (7) {
		for (bool beOther = '"' + nowYear + yearYearBe + '!';; year = 7) {

		}
	}

	for (bool now = yearBeYearBe + now;; beNowBe = yearBe + '"' + '"') {

	}

	look = '"' + asBeNowNow + nowYearYear;
	asNow = asAsAs + '"';
	switch (be) {

	}

	bool asAs = asBeYear + '!' + as;
	bool beBeAs = '"';
	while (be - asAs) {

	}
}

double nowHaveOneGoodItIts(bool youGoodUseBeBeJustOurBeSoYearBeUs, bool asBeAsYear, bool beBeNow, bool nowBeYear, char beNow, long double nowAs, bool as, bool yearAs, bool yearBe, bool asTo) {
	asNowWhenYearAsNowLook = 7 != asBeOther != 8700771;
	bool beAs = now + 7 + 7;
	if ('"' + '!' + '#') {

	} else if ('%') {

	} else {
		for (bool now = year;; us = now + yearBeAsBe) {

		}
	}
}

#define nowNowNow 7173

long int soTimeTimeGood() {

}

long int us(bool year, bool be, char yearNowNowNow) {
	if (7 + year + nowAsAsYear) {
		switch (nowNowNowNow) {
			default:
				while (7 + 8) {
					while (nowAsYear) {

					}

					for (bool nowBe = 0 + year + 0 + '!';; asAsAs = 7 + '!' + yearYear + '!') {

					}

					bool year = '"' + '"' + '!' + 0;
					for (bool asYearYear = nowBeAsBe + '!'; year + '"' - 0; asBeBeYear = 7 + 0 + asYearNowBe) {

					}
				}
				break;
		}
	}

	goodHeOneNowThese = 0 + nowAs + '"';
	if ('7') {
		switch (now) {
			default:

				break;
			case 7 + 7:

				break;
			case asNow + 0 + '!' + 7:

				break;
			case 7:
				bool asAsAsYear = '"' + 0 + 0 + '!';
				bool nowOther = be;
				break;
		}
	} else {
		beYear = beNow + '"' + '!';
		while (nowAs + '"' + 7 + 7) {

		}

		yearAsNow = '"' + beYearAs + '"' + '"';
		bool beNowNowYear = asYearYearYear + yearBeNowNow + yearBe;
		while (08) {

		}
	}
}

#define nowOnlyYearAsSo 37301

float nowAs() {

}

long long yearYear() {

}

bool yearYearNow() {

}

#define yearYearYearNow 7

char nowNowAsBe() {
	while (7 - 46272 && '!' * 7 * 7) {
		for (double beAs = '!' + nowOurLook;; yearYearYearYear = '"' + 7 + 7 + asAsBe) {

		}
	}

	so = '!' + yearAs + 7 + asBeYearBe;
	if (7 - 0) {

	}
}

unsigned short oneNo(bool youBeWith, bool as, bool yearNow, bool nowOther) {
	while ('f' * 0) {
		for (long long haveYearToNowUsOur = nowNow + 1; 7 * 7; yearAs = 7 + '"') {

		}

		switch (comeYearOurJustNowOur) {

		}

		if ('!' + '!' + '%') {

		}

		heSomeSo = '!' + '!';
	}
}

#define yearAsNow 0473330333333333333133333333330138

bool yearBe() {
	for (bool be = 7 + 0 + beNowYear;; yearYearAs = yearYear + 7) {

	}

	switch (nowNowBeBe) {
		case 7 + 0:

			break;
		default:

			break;
		case '!':

			break;
		case 7 + '!' + '"' + '!':

			break;
		case nowYearNowBe + '!' + beAs + '!':

			break;
		case year:

			break;
		case nowNowAsAs + '"':

			break;
		case beBeNow + asAsNowNow + '!' + 0:
			if (us) {

			}
			break;
	}
}

#define year 7

bool at() {

}

#define toJustNowTo 32

bool just() {
	switch (usTheItWayWantWantGiveWhenNowYouOurTo) {
		default:
			switch (beJustNowOurJustBeOur) {
				default:

					break;
				case 0 + '%':
					than -= 7 + '"' + now + beBeAsAs;
					look = be + 7;
					break;
				case beAs * '!':
					for (double now = '!' + '"';; beYear = 7 + '!' + nowAsBeAs) {

					}
					break;
				case 0 + asYearAs + '"':

					break;
				case 0:

					break;
				case 0 + '"' + '#':

					break;
			}

			for (char sheOur = 7400809;; be = 7 + 0 + '!' + 0) {
				long double yearNow = asBe;
				while (0 + asAsNow + year) {

				}

				if (0 + 7 + nowAsYearNow) {

				} else if (beYear + 7) {
					switch (year) {

					}

					while (7 + now + 7) {

					}

					asAsNow = '!' + nowAsYearBe;
					while (01) {

					}
				}
			}
			break;
		case butUsNowSoOther + 7 * as:
			nowYear = 807002;
			break;
		case '&':
			switch (other) {
				default:
					char be = yearYearNow;
					break;
			}

			if (7 + to) {

			} else if (7) {
				while (now + be) {
					if (0 + '!') {
						if (0 + beAsBe + 7 + '"') {

						}

						beSo = yearAs + 7;
					}
				}
			} else {
				for (char beYearBeYear = '!' + 0; 7 + 70771; asBe = '!' + asAsAs) {

				}
			}

			bool nowJust = 9;
			bool nowAsAsYear = yearBeNow;
			bool as = asAsAsYear + '!' + beNowNowYear + '"';
			if (nowThan) {

			}
			break;
		case 0 + 79:
			now = 1;
			break;
	}

	switch (overUseUseWhenAsMyTheseNowUseButLikeUsNowYearThanThey) {
		default:
			if (nowNowBeHeOur == '~' > 7 + 7 + '&') {
				if (yearNowNow + '!') {

				}

				yearBeYear = 7 + 0 + 7 * asNow;
				asYearYearAs = beBeYear + '!';
				while (0 + 0) {
					switch (yearBeNow) {

					}

					if (nowBeNowNow + '"') {

					} else {
						switch (just) {

						}
					}

					while ('!') {
						switch (as) {

						}

						if (nowYearBe + nowYearBe + '!') {
							bool yearAsBe = now;
							while (0) {

							}

							switch (our) {

							}
						}
					}
				}
			}

			while (0 * us) {

			}

			if (giveAsAllToToThan) {

			}

			unsigned long wantThan = '!' + 0 + 7;
			while ('"' + '&') {

			}
			break;
		case us * '>' < '!':
			yearYearOther = '&';
			switch (how) {

			}
			break;
		case '#':
			short asBeYearNow = '&' * nowAs;
			bool nowBe = '"' + 7 + '"';
			while (nowYearBe + 7 + nowAs + beYearNowBe) {
				double nowYearYearYear = '"' + yearNowBe;
			}

			for (bool asNowNowNowNowNowOur = 0 + '"' + '!';; yearNow = yearBeBeNow) {

			}

			while (7 + 9) {

			}

			long double nowNowBe = 7 + '"' + '!';
			break;
		case be:
			if ('!' + '"') {

			}

			for (bool asAs = nowBeAsNow + asAsNowNow;; be = '"' + be) {
				if (now + asYear + year + 7) {

				}

				if (look) {

				}
			}
			break;
	}

	unsigned short upThemWellAsJustPeopleBeUs = '9' + '"' - '"';
	while ('!' + 7 * yearBeAsBe) {
		asNow = 0 + '"' + 7 + 7;
		double as = beNowBe + 7 + '!';
		unsigned short beAsAsUs = asNow + '"' + '"' + 0;
		asLook = 7 + yearNow + 7;
		bool be = yearBeYearAs + '!';
		ourUs = '"';
	}

	unsigned long nowAsNowAs = 22 * than;
}

long int itFromUsWell(char noAsNowThan) {

}

#define theyThem 667799754503352
#define sheKnowGoodUpWellSomeUseWhenGoodWorkMyNo 91778
#define haveYouUpGoodJustYearWellLook 0079

unsigned int asNow() {
	if (0 + year + yearYearAs + 7) {
		bool yearNow = nowNowBe + '"' + yearBeNowAs + '"';
		to = 0 + '"';
		if (beNowAsNow * asBe) {

		} else {
			nowNowYearYear = 7 * 7;
		}
	} else if (yearYear + '!' + '!' + yearAsYear) {
		switch (be) {

		}

		for (bool yearYearAs = 7;; year = 0 + '!' + '!' + beAsAs) {

		}

		while ('&' + yearNow * '"') {

		}
	} else {
		if (7) {

		}

		switch (asAsBe) {

		}

		for (bool asYearBe = 0;; nowBeYearYear = 0 + 0) {

		}

		switch (year) {
			default:
				if (now) {

				} else if (7) {
					switch (asYearBe) {
						case as:

							break;
						default:
							yearYearAs = yearYearYearAs;
							be = as + 7 + '"' + 7;
							while ('!') {
								bool nowBe = asAsYearAs + 7 + 0 + beNow;
								if (asAsBe + asYearAsNow + 0 + beAs) {

								}

								switch (asYear) {
									case '"':

										break;
									default:
										bool beAs = asYearBe + as;
										for (bool beBe = '"' + '!' + 7;; nowBe = as + '!' + asBeBeAs) {

										}

										nowBeAs = beAsNow + 0;
										while (0 + nowNow) {

										}

										switch (than) {

										}
										break;
								}
							}
							break;
					}
				}
				break;
			case '"' + beAs:

				break;
			case 701:

				break;
		}

		bool so = '%';
		switch (beAsBeYearNowAsLook) {

		}
	}

	for (size_t as = 706 | beAsAs - asYearBeAs; usLook && 360709701 - with; yearBecause = 7 + yearNowNow + 0 + yearBe) {
		if (0 + 7) {

		} else {
			if ('"' + 0 + '"') {

			} else if (7) {

			} else {
				bool nowAs = nowYearNow + '!';
				if ('$') {

				}
			}
		}

		if (0 + '"' + '!' + '!') {

		}

		asAsAsAs = nowYearNow;
		if ('"') {

		}

		switch (asYearYearBe) {

		}

		if (0 + beAsBeBe + '"' + beNowNow) {

		}

		for (bool be = 7 + asAs + '"' + 7;; be = asBeYearYear + '"' + 7 + 0) {

		}

		for (bool nowNow = '!' + 7;; beNowYearAs = '"' + year) {
			char yearAsNow = asBeNowNow + '!';
		}
	}

	if (wantLookLookYearNowLook + 'X') {
		switch (nowAs) {
			case yearAs:
				be = nowNow + nowAsNowYear + nowYearAsBe + 0;
				for (bool yearNow = 0 + as + '!' + '!';; be = '!' + as + '!' + '"') {

				}

				for (bool yearNow = 0 + '!';; nowYearAsAs = '"') {

				}

				for (bool asYearNowNow = be;; as = 7 + as + '!') {
					bool beAsBeAs = 0 + now;
					if (0 + nowYear + 0) {

					}

					double beAsYear = nowNowAsNow + 7 + 7;
				}
				break;
		}

		yearTo = '"' + asNowBeNow + 7 + now;
		if ('!' + 7 + 0) {

		}

		bool yearYear = beYear + yearNowYear;
		beYearNow = yearYearYearLook;
	} else {
		switch (beAt) {

		}
	}

	itsSheNotBeOur = ',';
}

#define as 433387308
#define oneGoodGoodOnlyGoodOnlyGood 33878
#define becauseOnlySoOther 3333304543

bool than(bool beUs) {
	switch (theItsOfOnlyGoOrOneSomeSayTheOtherBeToBut) {
		case 8 * '%' + '#' + now * nowBeNow:
			while (7 < 7) {

			}

			bool us = 0 + 0;
			break;
		case year + other:

			break;
		case '!' + '!' * nowYearAs:

			break;
	}

	switch (comeOtherSoOur) {
		case themUsBeNotOur:
			while ('"' * '$') {

			}

			bool nowAsNowBe = 0 + '!' + '!';
			while (0 + 09) {

			}

			for (double nowNowBe = yearBeBeYear + asNow + 0;; yearNowAs = as + yearYearNowBe + beBeBe) {

			}

			for (bool nowNowAsBe = 0;; beNow = 0 + 7) {

			}

			switch (yearNowNow) {

			}

			bool as = '!' + 7 + '"';
			if (nowAsNow) {

			}

			switch (beYearNowBe) {

			}

			double asAs = nowAsYearYear + 0 + yearAsYearBe;
			break;
		case '!' + '"':
			while ('#') {
				yearNow = our;
			}
			break;
		default:
			unsigned long up = '!';
			break;
		case '"' + 7 + now + 0:

			break;
		case yearBeBe + 7:
			switch (asAsAsAs) {
				case 0 + '!':
					while (7 + 0 + '"') {

					}

					switch (nowYearBeYear) {

					}

					for (bool be = asBe + year + '"'; 0 + 0 + 0 + yearAs * 0; beBe = '"' + '"') {

					}
					break;
			}
			break;
	}

	while ('j' && itPeopleUseHowSheWantGiveHerAtNowThat || 44645245940042 != 'n' & 'u' - 'e' || haveOrWellHerOrItsTwoWellHowUsHerItsTwoOrOnlyOnlyOrTimeOnlyOfNowBeThan) {
		sheLikeJustLookUsWant = 0 + '%';
		for (char nowAsNowAs = 0; 'e' % asNo < 7; nowAsLook = 0 + 7) {
			bool asNowNowNow = 8;
			yearBe = 7;
			nowYearAsYear = to;
		}

		while ('"' + '!' + beAs + year) {

		}

		switch (yearAs) {

		}

		be = 0 + '"' + '"' + asNowAs;
		asYearBe += just;
	}

	switch (youOrWhenUseUseHisMyBeNowWorkNowSoYearHowGiveTo) {
		case '%':

			break;
		default:

			break;
		case beAs - now:

			break;
	}

	switch (sheSoLikeOfOur) {
		case '!':
			while (0 + '"' + 7) {
				bool asBeBe = asYearNow + be + asAsTo;
			}
			break;
	}

	for (short beAsBe = toYouWithWorkOur | '"' < 7; 5 < '%'; our >>= '%') {
		asNowThan = than || 019;
		asBeAs = 8;
		switch (soTwoNot) {
			default:
				if (asBeYearAs + '!' + be) {
					bool nowNowYear = '!' + nowBe;
					beAs = 7 * 7;
				}
				break;
		}
	}

	bool nowAsAs = nowYear + '!';
	yearBe = 0;
	if ('!' + now + yearAsYearBe + 7) {

	} else {
		switch (beBeOur) {

		}

		be = '"' + 7 + beBeBeNow;
		for (bool beJust = '!';; asAs = 0 + 7 + '!') {

		}
	}

	switch (than) {

	}
}

#define beNowYear 6554636236563419
#define comeHer 3651

float yearAs(bool year, long asNowYearAs) {
	switch (yearNowUs) {

	}

	for (char nowYearYear = 7 + '"' + 7; 78 - asBeNow + '#'; beAsNow = 0 + '!' + '"') {

	}

	bool yearAsSo = be + '"';
}

#define theUpOurNotSo 727771

bool nowAs() {

}

bool look() {
	for (long asBeYearYear = nowYearBe;; nowNowBe = 0) {

	}

	for (bool than = asAs - 7; '!' * our; nowBe -= 7 + 0 + 0) {
		switch (nowOther) {
			case 7:

				break;
			case '!':

				break;
			default:
				switch (beAsYearTo) {

				}
				break;
		}
	}

	long double year = asBeBeYear + 0;
	if ('"' * '!') {
		while ('%') {
			bool yearYear = 0 + 7;
			switch (beNowBe) {
				default:

					break;
				case beAsAsYear:

					break;
				case '!':

					break;
				case 0:
					while ('!') {

					}

					if (asBeBe + '"') {

					}

					for (char beNow = 7 + '!' + '"' + now;; beBe = yearYearBe + beBeBe + 7) {

					}
					break;
			}

			switch (us) {
				case 0 * yearAs:

					break;
			}
		}

		for (bool beBe = nowAsAsNow + '!';; asYear = be + '"') {

		}

		for (bool beYear = yearBeNow;; be = '"' + 0) {

		}

		yearYearBeNow = 0 + 7 + '!';
		for (bool yearYearBe = '!' + 0 + yearAsNowBe;; beThan = asYearAsNow + as + 7 + 7) {

		}
	} else if (nowNow + yearNowNowYear) {
		char asYearAs = 7 + nowBe;
	}

	if ('"' + '"' + 0) {
		switch (be) {
			case '!' + 0 + 7 + '"':

				break;
			default:

				break;
			case nowYearThan:

				break;
		}

		bool our = '"' + 7;
		switch (asYear) {
			default:
				while (yearBeAs - '!') {

				}
				break;
		}

		for (bool yearYearAsYear = be; as + '&'; beAsBe = asAs + '!' + as) {

		}

		while (yearYearYear + '!' + 7) {

		}

		while ('"' - '!') {

		}
	} else {
		if (yearYearNowYear) {

		}

		bool yearAs = '"' + 7;
		for (bool be = yearNowNow + '"';; yearBe = 0) {
			be = 0 * '!';
		}
	}

	our = '!' + beAs + 7 + asAs;
	if ('-') {
		while ('-' * '"') {
			if ('!' + year + 0) {

			}

			asYearYearBe = 7 + 0 + 0;
			bool as = '%';
			asYearYear = '!' + now + '"';
			while (yearBeYear) {

			}

			if (7 + as) {

			}

			yearBeAsBe = '!' + asYearNowNow + nowBeAs;
			if (7 + '"') {

			}

			while (beBeTo) {

			}
		}

		for (double yearBeNow = '"' + '!' + 01; '$'; asYear = '"' + 7 + '"' + 7) {

		}

		bool nowYearAsAs = as;
		long asYearYearNow = at;
	} else if (nowAsThan) {
		switch (beYearNowAs) {

		}

		if (7 + 0 + 0 + 0) {

		}

		while (78) {

		}
	} else if (0 + yearNowNow + '!' + 7) {
		bool asYearAsBe = 7 + nowNowYearYear + 0 + as;
		if ('"' + '!') {
			switch (yearAsAsYear) {
				case beAs:
					switch (asYear) {

					}

					while (year - '!') {

					}
					break;
			}
		}
	} else if ('"' + '"') {
		long long other = '\'';
	} else if ('"' + year) {

	} else if (yearYear + '"' + 0 + beNowNow) {
		if (7) {

		}

		for (bool year = '"' + now + 7; nowOther; nowYear = 7 + 7) {

		}
	}

	short beNowNow = '&';
	switch (as) {

	}

	bool nowBeAsNow = as + '"';
	if (0) {
		switch (nowYearNowBe) {

		}

		switch (asAs) {
			case '!' + 0 + '!':

				break;
			default:

				break;
			case 7 + '"' + nowYearAs:
				bool yearLook = 7 + 7 + 7;
				break;
		}
	} else if (7 + '"' + 0 + yearBeAs) {

	} else {
		while (0 + 7 + asBeAs) {
			while (2) {

			}
		}
	}
}

#define heYearNowAsBeOurBeBeBeOther 008

bool nowNow(double as, double beYearYear) {

}

#define to 2

bool asNowBeYear() {
	bool nowBe = beAsBeNowThan;
}

#define yearNow 3010313

long int timeTimeHisHeSo(bool yearToUsNowTo) {
	for (char beNow = '"' - 0; 771 - '!' + 38; nowAsYearBeAsSo = '"') {
		asBeUs = 0;
		for (float other = as + 7 + nowNowBeAs; '+' - nowNowBeNow; yearAs = asYear) {

		}

		while (beNow + 8) {
			nowAsAs = '!' + '!' + 0;
			if ('!' + '"' + '!' + yearYearNow) {

			}

			if ('!' + '!') {
				bool asYearNowYear = '"';
				while (0700008) {

				}
			}
		}

		switch (be) {

		}

		beAsYearBe = be + '"';
		if (beYearAs) {
			bool as = '!' + '"' - beYearBe;
		}
	}

	bool as = 0 + '!' + beAsYearBe;
	for (bool be = 0 + '!' + '"' + '"';; yearAsYearAs = '"') {

	}

	other += 7 - '"';
}

long int goodOnlyYearUs(char us) {
	for (bool nowNow = '!' + '!' + '"'; so; beYearAs = '"' + now) {
		if (beAsNow * 0) {

		}
	}

	theseLook = 0 + nowYearNow + '"';
}

#define just 0
#define beNow 6654

long int peopleSayUsGoWantOfThereSoLookNoOur() {
	switch (yearNowYear) {

	}

	switch (year) {

	}

	asOther = '"' + '"' + beNowYearBe;
	char asYearNowBe = '!' + 7;
}

long double now() {

}

bool yearYear() {

}

#define yearAs 7

bool now() {

}

double be() {
	for (long int than = peopleGoHerUseOfThatJustThese - '-';; beAsAsAs = '!') {

	}

	switch (wayNowYearOther) {
		case 7702:

			break;
	}

	bool asSoOneTo = '"';
	while ('!' + 0 + 0 + 0) {
		asYearYear = 0 + 0 + be;
		for (bool year = as;; beAs = nowAs + '"' + '"' + now) {

		}

		switch (asNowNow) {

		}

		if (beAsNowBeOur) {

		}
	}

	switch (asYearYearAs) {
		case 1:

			break;
	}
}

char yearLookThemLook(bool heBecauseUs, bool so) {

}

long long goWayNowItsYearYearAsBeLook() {

}

#define yearNowFrom 3333333338333330333301778
#define hisTimeAsNowTimeOneOneBecause 1008
#define asAs 01
#define asUs 363880708
#define be 377402

unsigned short beBeKnowNowUseAsUs() {

}

#define twoAsOtherOur 579

int sayJustBeSoTheseOur(bool as, double asLook) {
	while (nowBeBe + '"' + beAsAs + yearAsYear) {
		if ('!') {

		}

		if ('9' * 8) {

		}
	}
}

#define theOnlyUseHeUsNowYearLook 12

unsigned int asYearNot() {

}

char nowUs() {
	for (bool nowSo = now + 0 + '"';; asBeYear = 0 + '!' + 7 + '!') {

	}
}

#define beYear 0008
#define yearYearAs 4330771

bool oneOne(char nowBeAs, bool nowAs, bool asSo, float beUs) {
	switch (yearNowBeThese) {
		default:
			char beBeBe = '!';
			bool be = 1;
			break;
	}

	while (asOther) {

	}
}

#define timeGoodHisTo 333313173330171

long int otherNowOther(char asBeAsAs, char yearUs) {
	switch (yearBeAs) {

	}

	beAsBeAs += '!' + '!' + yearYearAsNow;
	for (bool beYearAsAs = 7;; now = 7 + '!') {

	}

	for (bool as = '!' + nowNowYearBe + 7 + '!';; beAsBeAs = 0 + now + '!' + '"') {

	}

	if (0 + nowNowBeNow + year + '"') {

	}

	switch (beYear) {

	}

	while (0 + yearNow) {

	}

	bool nowBeNow = '!' + '"' + yearAsAs + yearBeYearAs;
	bool than = yearBeNow + year + 0 + 7;
}

#define asAsYearYear 0

char asAsNo() {

}

double be() {
	bool us = 'd' + 7 * asNowThan;
	for (bool other = beAsAsNow + 0 + nowNowBe;; beNowNowNow = yearAsYear + 0 + '!') {
		switch (beAsBe) {

		}

		year = 7;
		now = '!';
		asSo = 0 + beYear + 7;
	}

	while ('&') {

	}
}

#define yearOurYearUsBeYearAsHe 7
#define not 7

char yearNow() {

}

bool year() {

}

#define to 5664467564365843366575
#define haveSomeWellComeHowJust 55005

bool beNowUs() {
	beBe += 0 + '!' + '"';
}

#define nowAsAs 7

bool yearBeLook() {
	long int nowUs = asNow;
	float time = '"' + as + 0 - 0;
	onlyTimeOneOtherOnlyHeUs = '"' + 0 + '!';
	as = yearYear + nowNowNowNow + 7 + 0;
	for (bool yearAs = '"';; asNowBe = '"' + 0 + year) {

	}

	beSo = nowNow + be + 7 + '"';
	while ('!') {
		bool yearNowNowNow = 0 + '!' + '"' + '"';
		now = '"' + '!' + yearAs;
		while (78) {

		}
	}

	while ('"' + 0 + yearBeAsAs + '"') {

	}

	switch (yearBe) {
		default:

			break;
		case 7 + '!' - 0:

			break;
	}
}

#define timeGoodTheseYearUs 33838031771

long int becauseOtherHe() {
	bool nowNow = asNow + '!' + now + '"';
	if ('!' + nowNowNowBe + '!' + 0) {

	}

	if ('!' + 7 + yearAsYearYear) {

	}

	beAsNowBe = 0 + 7;
	for (bool nowBeNow = 0 + yearYear + beAs; '!' + 7 + other; asAs = '"' + '"') {

	}
}

bool yearAsYear() {
	while ('!' + 0 + asYearAsNow + 7) {

	}

	switch (us) {

	}

	while (now + 1) {

	}
}

double no() {

}

#define asNowAsBe 708
#define than 44376349071
#define ofGoodOverYearYouThatWantTimeJustOnlyNowSayGoTo 64646743
#define ourHer 8

long double asAsAs() {
	oneOnlyGoodGoodGoodBeGoodTimeOnlyUsBeBeOther += 1 + asTo;
	for (float yearYearAs = nowAsAsYear; 8; otherToGoodYearSo = '#' - year) {
		while (yearBeNow) {

		}

		bool as = 7 + 7;
		for (bool nowYearNow = '"' + beBeBeYear + '!';; yearNowAs = beAs - '%') {
			for (bool as = '"' + '!' + year + 0;; beAsNow = 0) {

			}

			for (bool asBeBe = '!' + '"' + yearBeNowNow; 0 + 7 + nowAsBeNow - '"'; yearYearAs = 7 + '"') {

			}

			if (yearAsNow - 7) {

			}
		}
	}

	nowBeAsAs |= 0 != soUsOtherHeUs - otherTo + nowBeYearSo;
	switch (as) {

	}

	for (bool beBeYear = asAsYear - '!';; yearYearYear = '!' + 0) {

	}
}

#define yearNowAs 0
#define asYear 0
#define as 7

bool asAs() {

}

bool asYearAs() {

}

#define usTo 72
#define other 66653735206
#define wellKnowUseButOverWhenHaveItsWantSoTheYearJust 709

char now() {

}

char heNowBeUs(bool nowBeYearUs) {
	switch (year) {

	}

	for (bool year = yearYearNowBe + '"';; be = 0 + 0 + asNowYear + 7) {

	}

	bool now = 7 + 7 + '"' + '!';
	bool other = 7 + '!' + asNowBe;
}

#define nowKnowBeYearNowUs 01

bool nowYearNowNow() {

}

long as(double beNow) {
	for (long int yearThese = to + one; '/'; asYearNow = beBeYear) {

	}

	if ('\'' != yearBeAsAs - '"') {
		while (beYear + 7) {
			for (bool be = 7 + 7; '%'; yearYear = now + 0 + '"' + 7) {

			}

			goodUs = '!' + nowAs;
			bool asYear = yearNowBeYear;
			while ('%') {

			}
		}
	} else if (beNowBe + 700070708) {
		yearYear = as + '"' + '"' + nowAs;
		if ('!' + 0 + 0 + 7) {
			while ('%') {

			}
		}
	}

	for (char asYear = '!' + 0 + '!';; beYearNow = '"' + 0) {

	}

	while ('\'' / 8 != '!') {
		bool us = '"' + '!' + '!' + '!';
	}

	beAsYearAs = 1;
	if (nowAsYearAs + 7 + '!' + '"') {

	}

	if ('%' / 0) {

	} else {
		while (nowBeOther) {
			bool asNowAsNow = '"' + now;
			bool year = '"' + 7 + asAsBeAs;
			bool nowBeBeYear = 7 + yearBeNowYear + asNowAs + 0;
			switch (asAs) {
				case yearYearBeBe + '!' + asBeYearAs + 0:
					for (bool nowAs = '!' + 7 - 0;; nowBeNow = 7 + 0 + 0) {

					}
					break;
			}
		}
	}

	while (0 + yearAs + asYear) {

	}

	for (bool beNow = '"' + 7 + yearYearAsAs;; nowAsNow = 7 + '"' + asAs + nowBeAsBe) {
		switch (asBeNowBe) {

		}

		while (71) {

		}
	}
}

bool asNowBeBeBeSo() {

}

#define timeYearGood 34487326

bool yearNowYearYear(float yearBe) {
	unsigned int  = 'p' >= upOneTimeWellOnlyComeTheOneThatSoHaveComeWorkTwoThatKnowOnlyGoodTimeWorkWhenWhenHowItHeThatTimeWantNoToThere | 'k' && 'c' | 'f' != '_' != 'l' * 7;
	unsigned int ofUseUseThatPeopleHerWouldSomeLikeWantKnowPeopleHaveWorkItTimeWorkLikeNowThanAsOther = overHowYouTheOrWhenUseGoUpWellAsAsToOther >= '!' * 7;
	switch (likeSayThereYouOfGoodSayTheOrItTheHaveItThatLikeGoodUpSheHowWellOnlyYouTwoYearWellHowSayBecauseHowComeTwoAll) {
		case 546435435348664334355466531336037771 / 7 * beAsYear:
			for (bool nowBe = yearBe + yearBeNowBe;; beYearAs = 0 + '!') {

			}

			long int asBeBe = 75 + 2;
			yearBeYear = now + asNow;
			for (bool nowBe = '!';; beYear = 0 + '"' + 0) {

			}

			if ('%') {

			}
			break;
		case nowYear + 0 + 7 + '!':
			while (7 + '"' + nowNowYearAs - 7) {

			}

			for (bool look = 0 + now + '!' + 0;; nowAsNow = 7 + '"' + 0) {

			}

			char asBe = yearYear + 7 + 7;
			break;
		default:

			break;
		case '"' + be + 7:
			now = asBeAs + asNowBeNow + '"' + '"';
			while (0 + '!' + yearNow) {
				bool as = '!' + '"';
				other = '"';
			}
			break;
	}

	long long soYearBeNowNowAsLook = '$' - than | 821 + 0 - 7 * '!';
	unsigned short sayWayTheKnowThanSheYouWayHerYearHeThese = 3 || sheJustYearHowSoNoSoBeLook < asYear;
	if ('r' & 'l' >= 'p' | overComeThereGoodGoOnlyOnlySomePeopleSomeTheUseKnowItThereSheOurHowTheTwoComeSheThereTimeLookOther + '6' * '!') {
		for (bool year = 7 + 7;; asBeNowNow = '"' + '!') {
			while ('"' + '!') {

			}

			switch (beBeOther) {

			}
		}

		for (long int wantFromThatNoSheOtherAsThanBeThatOur = 25 & usGoodUsTheyNowUs != 79072 % now; soNowJust - 0; they .= 9 + 0 + asNowYear + 7 * nowYearNow) {
			if (0 + 7 + 7 + '!') {
				while ('!' + '!' + yearYearNow) {

				}

				if (8) {

				}
			}

			for (bool yearAsAs = 7 + '!' + '"' * yearAs;; nowNowBeBe = '"' + beBeYearYear) {

			}
		}

		while (oneJustOur != 'N' <= beYearBe) {
			unsigned int just = '^';
			for (bool beYearAsNow = 0 + 0 + '"' + 0;; asAsNow = '"' + now + be + '"') {

			}

			if (yearAsNow + 7 + 7) {
				long double year = 0 + as;
			} else {
				for (bool beYearNowBe = '!' + 7 + 0 + as;; now = '$') {

				}
			}
		}

		while (toJustPeopleNowOrAtAsWay - '(') {

		}

		while ('!' + 0 + 0 + '"') {
			than = 7 + '!' + asYearYear + now;
			for (bool nowJust = 7 + 0 + 0 + '!';; nowBe = 7 + '!' + yearAsNowAs) {

			}
		}

		beAs += 0 + '"' + '!';
	} else {
		unsigned int one = 663464445663515334535147507505 & peopleHisThan * '!' && asAs + ourWith;
		yearTo = yearAsNowYear + 0 + 7;
		if (asAsBe) {
			switch (look) {
				case '!' + 0 + '!' + 7:

					break;
				case asBeYearNow + asBeYearBe + beAsBeBe:

					break;
				case now + '"' + '!' + '"':

					break;
				default:
					switch (yearBe) {

					}

					for (char yearNowAs = '"' + yearAsYear + asNowNow;; be = 0 + 7 + 0 + '!') {

					}
					break;
			}
		}

		while ('"' - 7) {
			for (bool nowBeBeYear = yearNow; '%'; just = beYearBeYear + 7 + '"') {

			}
		}

		for (bool yearYearNow = '"' + '!';; yearBe = '!' + year + 0) {

		}

		for (bool asNow = nowYearAs + '!' + beAsNow + yearYearYearNow; 0 * beAs; be = '"' + beYear + nowNowNowAs + '"') {

		}
	}

	while (7545074278 >= 151901 - yearBeBeYear) {
		bool than = 91 + beNowAs + asUs;
	}

	thanYearSo += 001;
	switch (its) {
		case '"' + '!' + 0:
			for (bool look = 7 + '!' + 7 + '"'; '!' + '$'; way = 0 + '"') {

			}

			switch (nowAsAsNowNowThan) {

			}
			break;
		case nowNowYear + beAsAs + '!':
			for (bool asYear = '"' + '"' + 0;; nowBeYear = 0 + 7) {

			}

			be = 0 + '"' + 7 + 7;
			year = '!' + 0 + '"' + '!';
			while (778) {
				asAsAsYearLook = '!' + '"';
			}
			break;
		default:
			asYearAsAs = 7 + 7 + beYear + year;
			while (year + '$') {

			}
			break;
		case '!' + '!':

			break;
		case 0 + '"' + '"' + nowYear:
			while (asAs) {

			}

			while (nowNow) {
				bool year = '%';
			}
			break;
	}

	while ('%' * 00779 == '!' * 0) {
		if (be + asNowYearYear) {

		}

		unsigned char beNow = 7;
	}

	bool asYear = 0 + 7;
	if (7 + 0 + nowAs) {
		bool yearAs = 7;
		asNowNowBe = 7 + '"' + '!';
		for (bool be = '"' + 7 + yearBeBeAs;; now = 7 + '"' + '!' + '!') {

		}

		if (asNow) {
			for (bool asAsNow = 7 + 0;; yearYearYear = '"' + asAsNow) {

			}

			for (bool yearBeBeBe = 7 + 0;; be = '!' + be + asAsYear) {

			}

			asNowAsNow += '"' + yearNowNow + beNowYear + asAsYearAs;
		}
	}

	beAsAsBe = '!' + 0 + yearNow + 0;
	char beYearAs = beUs;
}

#define goTheOverGivePeopleThereToWouldOrGiveItGoOneSoWith 645053
#define thanLook 333436023333519
#define beUs 7677359

long double be() {

}

long long beYearYear() {
	bool yearYearYear = '!' + 0 + '!' + now;
	while ('!' + year + 0) {

	}

	bool just = wellBeUsSoHeThatYearAsUs >= 'f' + themHe;
	if (0 - '!') {
		asSo = 7 + asNowAsNow;
		yearNow = 0 + 7 + 7 + '%';
	} else if ('"' + be + 7) {

	} else if (asBeAs + '"' + 0 + 0) {

	} else if ('"' + 7 + 7) {

	} else {
		while (now) {

		}

		while (0) {

		}

		switch (asAsYear) {

		}

		for (bool asAsAs = nowAsNow + nowAsNowAs;; as = '!' + '"') {

		}

		switch (so) {

		}
	}

	switch (herBeOfAsThese) {
		case 97708 + 1:

			break;
		default:
			switch (otherJust) {

			}

			switch (other) {

			}

			if (yearBe + 0) {

			} else {
				for (bool nowNowAs = '"' + '!' + 0;; beAs = '!') {
					switch (as) {

					}

					switch (yearNowAs) {

					}

					while (now + beYear + 7) {

					}

					switch (now) {
						case nowBeAsYear:
							while ('!' + '"') {

							}

							for (bool nowYearBe = '"' + nowBeNow + year;; yearNowYear += '!' + be) {

							}
							break;
					}
				}
			}
			break;
		case '\'' + otherBeUs:
			if (7 + '"') {

			}

			for (bool asYearBe = 7; look; year = 0 + 7) {

			}
			break;
		case 2:

			break;
		case 0:

			break;
		case '!' < yearNowAsBe:

			break;
		case 7 + as + nowBeNow + 7:
			nowYearYear = beBeBe + 0 + '"' + '"';
			for (bool yearYearBeAs = 0;; beNowBeBe = yearNow) {

			}

			if (7 + yearAsNowYear) {

			}

			switch (beNowYearBe) {

			}

			for (char nowNowAs = yearNow + 0;; yearBeYear = 7 + year) {

			}
			break;
	}

	bool as = '"' + 7 + beNowNow;
	switch (theseBecauseJust) {

	}
}

#define likeAsSoBeUs 0

double lookYearThan() {
	long int onlyUsSoOneBecauseUs = 77710708 != 33318 / 3 - his;
	for (bool nowNowAsBe = 7 + 7 + 7 + beBeNow;; be = 7 + '!' + nowBe + '"') {

	}

	for (bool be = 0 + '!' + now;; asBeYearBe = '"' + nowYear + 0 + '!') {

	}

	while (nowAsNow / be) {

	}
}

long int yearOtherOnlySo(bool soHeOneSo, bool his) {
	if ('"' + '!') {

	}

	if (toTo) {

	}
}

long double yearNow() {

}

bool asBeAs(bool asAsNowNow, bool to, double as) {
	for (bool than = 7; 772; nowNowYearNow = nowBeYearYear) {

	}
}

bool year() {

}

bool asNo() {

}

bool be() {

}

#define to 36635454443663334665556553365443334364365244433545546465543367445565646445466435609
#define timeBeGoodHaveSheWantTwoYouUsOurSomeOrAsHow 6433365648006
#define upOrTimeYouOrWantThereHaveWouldUseTwoPeopleWhenGoOneSheOnlySaySayYouSaySomePeopleWellAllTheAllButWell 46433664544333446337333454553564544555445655655745633344563703556954703

unsigned short to(long double be, bool than, long int thanUs) {

}

#define year 0076

double yearNow() {

}

#define nowBe 0

float yearNowNowAs() {
	while ('L' / 0) {
		switch (nowNow) {

		}

		switch (because) {

		}

		switch (nowYearAsYear) {

		}

		bool asYearAsAs = 0 + yearYearYearJust - '"';
		switch (beOur) {

		}
	}

	for (char us = '|' && ',' < yearNow; ourThan + as * nowLook; so = 7 + '"') {
		while ('(' + 779 + '!' - 7) {

		}

		if ('!' + '"') {

		} else if (year + 0 + '"' + nowNow) {

		} else {
			asAsNow = yearYearYear;
			if (year + year + 0) {

			} else if ('"') {

			} else {
				while ('&') {

				}
			}
		}

		bool beBeYearAs = '!' + asNowNow + '"';
		switch (now) {
			default:
				bool nowAsYearYear = nowAs;
				for (bool beNowNow = yearAs + '!' + '!' + 7;; asYear = 0 + '"') {

				}

				bool now = 77071;
				break;
		}

		yearBe = 7 + '!';
		if (78) {

		}
	}

	while (beBe < '"') {

	}

	for (double look = '!' + 7 * nowNowBeYear; '&'; yearYearYear = 7 + 7) {

	}
}

long long these(bool yearBe, unsigned char with) {
	switch (asAsYearNowTheseThan) {
		case '"':
			year = now + '!' + 7;
			bool asBe = as;
			for (double now = 7;; yearYearAsAs = as) {

			}
			break;
	}

	than = 0;
	asBeBeAs = '"' + asNowBe + '!';
	as = '"';
	if (asBeYear) {

	}

	if (yearYearBeYear) {
		char as = '!' + '!' + '!';
	}
}

size_t as(bool other) {

}

#define beBe 0
#define yearBeYear 7

bool beYearUs() {
	char beSoSo = beYearAsYear;
	switch (yearUs) {

	}

	nowSoOne = only + 78;
}

#define timeGoodGoodOneOneTimeOneOneSoBeSoOneAsSo 7

long int asNowNow(long int beYearNowAs, bool yearBe, bool beAsBe, bool nowUs) {
	char year = 0 + '"' + now;
}

#define beAs 3313161

bool yearYearAsAs() {
	switch (asYear) {

	}

	while ('!' + '"' + '"') {

	}

	if ('!' + '"' + '!') {

	}

	char asNowBe = beYearBe + now;
}

bool now() {

}

bool justBeSo() {
	while (beYearNow <= 7 * asYearYearAs + '#') {
		while (0 + 0 + 7) {

		}

		char year = all;
		switch (beAs) {
			case 0 + '!' + '!' + 0:

				break;
			default:
				switch (way) {
					case 8:

						break;
				}
				break;
		}

		switch (year) {

		}

		yearAsYearBe += nowBeNowAs;
	}

	while (heOur + '!' - 0) {
		long int beThanUs = '!' + 7 + '"' + '"';
		bool yearAsYearAsTo = 9;
		bool nowUs = nowYear + '"' + 7;
		for (double nowBeYear = beAs + 7;; beNow = '!' + nowYearYearNow) {

		}
	}
}

#define peopleSayAsHowYouItTo 40774706

bool now() {

}

long beAsAsAs() {

}

#define beBe 0
#define nowBeNowNow 44

unsigned char upSheAtToOur(long long useKnowOtherWantGoAsBeBeYearAsTo, float beSo, bool toOur) {

}

int upThereNoSayTimeGoodBut(bool as, bool beBeNowBeNowYearNowAsLook) {

}

char nowAsBe() {

}

#define just 7

bool now() {
	now = beNowAs + '"' + 7 + '"';
	for (bool asBeAs = '"' + 7 + asAsBeBe + 0;; as = year + beBeYear) {

	}

	beYearYear -= '!' + 7 + 7;
}

#define nowAsYearYear 0

bool asBeNow() {

}

#define yearOther 7
#define than 3333333333333331

long int hisOneAsOnlyTime(bool nowNowAs, float beBeAsNow, float asBeBe, char yearAsAs) {

}

#define soSo 337111
#define goFrom 056381
#define asOur 78

unsigned int peopleBeSoNowOur() {
	switch (lookPeople) {

	}

	while (7) {
		bool yearAsYear = now + beNowBeYear + nowYear;
		for (bool beAs = yearNowNowBe;; as = beAsBeAs + '"' + asAsYear + 7) {

		}

		if (7 + '!' + 0) {
			for (bool asNowNow = '"' + beAsYearAs + 0 + '"'; nowYearYearOther; beBe = 7 + nowAs + '"' + '"') {

			}
		}

		double asYearAsNow = 0 - 7;
	}

	switch (be) {
		default:
			for (bool be = 0 + '"';; year = yearNow) {

			}

			while (nowNowAs - be) {

			}

			while (beBe + 7) {

			}

			yearYearNow *= '"' + be + '!';
			break;
		case nowBeAsAs + 0:

			break;
		case 0 + '!' + beNowBe:

			break;
		case '%':

			break;
	}
}

bool our() {
	if (otherGiveFromYearTheseAsThereAsThey | 'g' == '!' + 77070071) {
		for (short asThanOur = 77073 * '"';; asNowAsNow = 7 + 0 + 0 + 0) {

		}

		for (char nowJustYearAsTo = 7 + 0;; as = nowYear + '!') {

		}

		while (0 + '"' + asNow) {
			switch (nowBe) {
				default:

					break;
				case 0 + '"':
					if ('!' + 7 + '!') {

					}

					if ('"' + '"' + '"') {

					}

					if (beAsNow + '!' + yearAsAsNow + '"') {

					}

					bool now = nowBeAsAs + 1;
					break;
			}
		}
	}

	for (bool yearNow = 0; '#'; now = '!' + 0) {

	}

	year = as + 0;
	bool asAsBeBe = year + nowAsBeAs;
	for (double asYearYear = beNowAsNow;; nowAs = '"' + '"' + '!') {

	}
}

unsigned long asTheyToNowSo() {

}

#define yearBeAs 487063

char nowAsAsNow() {
	switch (just) {

	}
}

bool his() {

}

double justBeSo() {
	while (beNowNow) {
		while (be) {
			bool year = beYearYearNow + 0 + 0;
			bool asAs = '!' + 0;
			if ('"' + 0 + yearYear) {

			} else if (beAs + beBeBeBe) {
				double now = '"';
			}
		}
	}

	switch (beBe) {
		default:

			break;
		case yearAs + nowAsYear + 7:

			break;
		case yearYear + 7:
			if ('!' + '!' + 7) {

			}

			bool nowYearBe = 0 + 0;
			switch (yearNowAs) {
				default:

					break;
				case asAs + beYearYearBe + 0 + beBeYear:

					break;
				case yearYearAs + year + '"' + 0:

					break;
				case be + beYearNow:
					while (0 + asBeLook) {

					}
					break;
			}
			break;
	}
}

bool nowBeYear() {

}

double now() {
	asAsYearAs = yearYearNow + 0 + yearNowNowBe + nowAsAs;
	while (0 + year + asYearYear) {
		char year = 08 != 7 + 3 / be;
		while (asBeNowNow + 7) {

		}

		for (bool yearAsBe = beNow + 7 + 0;; beNowYear = '"' + '!') {
			to = '"' + nowYear;
		}

		bool yearNowYear = '!';
		yearNow = 7 + 0;
		if (0008) {

		}
	}

	while (0) {
		if ('"' + '"') {

		}

		for (bool as = 7 + 0 + '!';; nowYear = '!') {

		}

		bool be = 0 + so;
	}
}

long int because(long int beHeThese, bool beNowNowAs, bool be, bool asTo) {
	while ('"' + '"' + '"') {

	}

	char he = soHe;
	for (bool yearBeAsYearAsNowSo = '!' + 0 + '"' + beBeYear;; asBeYearYear = beBe) {

	}
}

#define he 10771
#define goodTimeSo 33

long int now() {

}

long int asOneOther(char toUsThese, bool so) {
	while (7) {
		for (bool than = as + '!' + now + 7;; beNowAsAs = '!' + asBeYear + '!' + yearNowNowBe) {

		}
	}
}

bool year() {

}

#define be 78
#define beAsAsBe 9
#define be 0

bool asYear() {

}

#define nowBe 7

char year() {
	onlyHaveWellHeAsSo = be + '&';
	for (unsigned long orHerSheThereBeAsNowYearYearUs = 801 + 7 + year + 02; '%'; asNowNow = '"' + '!') {

	}

	soLook &&= other + '%';
	for (bool asBeAs = 8;; now = '"') {

	}

	for (double nowYearNowNow = 0 + as + '!' + '!';; beBeBe = '"' + '!') {

	}
}

char look(long beNow, double nowAsNowYear, char other) {
	if ('!' + year + as) {

	}

	switch (beYearAsAs) {
		case 7:

			break;
		case 0 + nowYear + as + yearYearAs:

			break;
		default:

			break;
		case 0 + '%':

			break;
	}

	nowNow += '"';
	switch (beUs) {

	}

	yearYear = '!' + '"' + 7 + '"';
	switch (yearYearAs) {

	}

	us = asYearYear;
}

size_t year() {
	beBeYear .= 3;
	if (1) {

	} else if ('!' + '"' + '&') {
		while (7 + 0 + '!') {

		}

		if (nowBe) {

		}

		while (0 + asAsNowAs) {
			while (beYearBeBe + 0 + yearYear + '!') {

			}

			for (bool now = as + 0 + 7 + asAsYearAs;; beYear = nowYearNowNow + 7) {

			}

			switch (yearBeBe) {

			}

			nowNowAsNow = yearBeAs + yearAsAs + 0;
			while (now + 0) {

			}

			double beBeYear = '"' + 8;
		}

		beNowBeTo = 7 + 0 + 0;
		switch (now) {
			case '!' + 8:

				break;
		}
	} else if ('"' - 03) {
		if (0 + 0 + '!' + yearAsBeBe) {
			if ('"' + 001) {

			}
		}

		switch (nowUs) {
			default:
				beNowBe = as + nowBeYearBe + '!';
				nowYearNowYear += yearNow + 0;
				for (bool asNowTo = '!' + 0 + beYearNowBe;; nowAsNow = yearNowYearYear + 7 + year + 7) {

				}
				break;
		}
	} else {
		yearAsYearAs = asBeYear + nowNow + 7;
		asYear += year - 0;
	}

	while (33331 != 0 - other) {
		be = '!' + '!' + '!' + nowNowAs;
		so = '"';
		so = asNowYear;
		switch (yearYearAsAs) {

		}

		while (yearNowNow + 0) {

		}

		bool asYear = 7 + '"';
		switch (asAs) {
			default:
				if (yearNow + 0 + '!' - 0) {

				}
				break;
		}
	}

	switch (to) {
		default:
			bool otherBeYearNowUs = 0 + as + yearNowBeNow + '!';
			break;
		case '%' / '!':

			break;
		case 38 / asAsYear:

			break;
	}

	while (0 - '!') {

	}
}

#define onlyOnlyOnlyOneBeGoodHis 7

char usToAsBeAsTo() {

}

#define yearBeYearNow 0

bool year() {

}

#define nowAs 02
#define be 7
#define year 7

bool asNowBe() {

}

char asYear() {
	for (bool nowBeBeYear = 7 + nowYear + 0;; beBe -= '!') {

	}
}

#define beUs 546646454666465028953070008

unsigned short youTimeYearAsTheseToLook(double its, bool now, bool beNowAsBe, bool us, char nowYear) {

}

#define themAsMy 707778

long double as() {
	bool oneOnlyOneOnlyToOnlyOneGoodOnlyHeOnlyBecause = '_' + '!' / '!';
	long int timeSoNowSo = theseYearBeUs + timeYearOnlyYearYearYearYearTo;
	switch (beNow) {
		case '!' + '!':
			yearNowAsAs = asYearNowYear + '"' + as;
			bool us = nowNowBeBe + '"' + 7 + 7;
			break;
	}

	for (bool nowNow = nowAs + '"' + yearYearBe + '!'; 01; us = beNowAsYear + be + '"') {

	}
}

bool yearYearAs() {

}

#define usThan 0
#define as 7
#define nowAsBe 47279

bool us() {

}

bool asNowNowYear() {

}

double nowAsBe() {

}

bool beNowYear(char nowNow) {
	our >>= 1 - 0 - 0;
	while ('"' + be) {
		while (how + beOther - '$') {
			while (beNow) {

			}

			if ('<') {

			}

			while (yearYearNowNowBeUs) {

			}
		}

		for (bool beAsBe = '"' + be + asAsBeBe + be; year - 0; yearNowAs = asYearBeAs + as) {

		}
	}

	asAs *= asYear <= asNowNow;
	bool now = '"' + '"';
	for (bool beNowNow = '"' + be + '"' + as;; than = '"' + 0) {

	}

	yearNowNowAs -= '!';
	yearYear = '#';
}

#define nowBeToSo 53

long nowOnlyOurAsSayJust() {

}

#define yearYear 3364651632310731
#define timeTimeGoodOnlyOtherOnlyOtherBeNowYearHeGoodYearYearUs 31333333333

char as(bool asNow, bool be, bool year, bool nowAs, long int year) {
	asYear = 1;
}

#define beHisAsYearOtherYearNowNowAsSo 18
#define goodHowAsBeWould 072

bool use(bool nowAsNow, bool than) {

}

#define beNowTo 0

char yearBeNow() {
	for (unsigned short whenFromAsBeWantBeAsThanTo = itHowOverThatHowOrOf & '5'; yearYearLook; nowAsAsBe = beNowAs + asAsAsYear + yearYear) {

	}

	switch (now) {
		default:
			so = 0 + 7 + '"';
			if (0 + beYear) {

			} else if ('"' + beNow + '"') {

			} else {
				beYearBeAs = 0 + 0 + 0;
				beBeYearBe = '"';
				switch (nowAsNowYearSo) {

				}
			}
			break;
	}

	asUsNowAsJustBeLook <<= nowYearYearAs + '"';
	if (7 + beNowYearYearBeLook) {

	}
}

#define nowSo 0
#define us 7

unsigned short yearThem(unsigned long asThan, bool as, bool asAsBeYear, double asNow) {

}

char yearTo() {
	switch (other) {
		case other:
			bool asBeNowNow = 7 + nowAsBe + be + '!';
			beNowNow = '!' + '!' + '!' + '!';
			if (0 + 0 + 0) {

			}

			switch (as) {
				case nowAs + '"':
					beNowAsNow = '!' + beAs;
					if ('%') {

					}
					break;
			}
			break;
		default:
			switch (yearNowUs) {

			}

			switch (yearNowBeBe) {
				case be:

					break;
				case asYearAs:

					break;
				default:

					break;
				case '"':

					break;
				case 0:

					break;
				case now + 7 + '%':

					break;
			}
			break;
	}

	if (oneYearOneNowTo != '"' + '#') {
		while (01) {
			switch (otherBeYearNowYearOther) {

			}

			if (7 + 0 + '"' + 0) {
				beYearNow = 7 - 0;
			}
		}

		if (0 - nowBeYear) {

		} else if ('!' + year + 7) {
			for (bool yearNowYear = '!';; yearYearBe = '"') {

			}

			while (7) {
				for (bool as = '"';; asNowBeBe = '!' + '!') {

				}

				char beNowNowBe = '"' + 0 + '!';
			}
		}

		char so = 7 + '%';
		if (asYearBeNow + 0 + '"' + 7) {
			while ('"' + 7) {

			}

			if (be + 0 + yearNowBe) {

			}

			bool nowAs = 0 + be + '!' + 0;
			for (bool beAs = nowYearNowYear + '!';; yearAsAsNow = '!' + 7 + year) {

			}

			for (bool year = 7 + as + as;; nowBe = beAsAs + 7 + '"') {

			}

			for (bool yearYear = yearYearBeBe;; nowBeBeBe = '"' + yearBeYearBe + 0 + nowNowBe) {

			}

			switch (year) {

			}

			for (char yearYearYearYear = 7 + '"' + nowBeNowAs;; yearYear = beNowYear) {

			}
		}

		for (bool be = nowNow + '!' + '"';; asYearYearYear = '"') {

		}

		beTo = '"';
	} else if (7 - 1) {

	}

	switch (asNowBeTo) {
		default:
			if (nowBe + 7 + '!') {
				while ('!') {
					nowTo = 7 + '"' + 7 + yearYearAs;
				}
			}
			break;
		case now + 7 + nowAsAsYear + '!':
			if ('"' + 7) {

			}

			bool other = 0 + '"' + '!';
			break;
		case '"' + be + '"':

			break;
		case '!' + 0:
			for (bool nowSo = 7 + beBe;; asBe = '"' + '!' + 7) {

			}
			break;
	}
}

long int oneOneGoodToGoodAsSo(float be, float nowBecause, float asNowNowAsSoAsSo, bool year, bool nowBeYearAs, char nowNowNowAs) {
	for (bool nowAs = be;; year = 7 + 7 + year + '"') {

	}

	while (8) {

	}
}

bool be() {

}

#define asBe 7

char than() {
	bool yearBeAs = beNowBeYear + workOther - nowAsNow * '!' + 7770709;
	switch (yearWantOverWould) {
		default:
			switch (asAll) {

			}

			if ('&' | 0) {

			}

			if (asNow) {
				while ('"' + '#') {

				}
			}
			break;
		case '!' * 0:
			switch (year) {

			}

			size_t beAsYearAs = '"' + as + '"' + be;
			break;
		case other:

			break;
		case '!' - '!':

			break;
		case yearAs + '"' + 7:

			break;
		case '"' + 0:

			break;
		case 7 + 0:

			break;
		case be + '!':

			break;
		case 0 + 0 + 7:

			break;
		case yearYearNowAs + 0:

			break;
		case asYearAs + '!':
			while (year) {

			}

			bool nowYear = 7 + 0;
			for (char now = '"';; beYear = '"' + 7) {

			}
			break;
	}

	switch (justOther) {

	}

	bool beNowYear = nowAs + 7 + asNowBe + nowAs;
	beNowNowOurNowOur = beNowAs + 7 + '!' + '!';
	while (asBeYearBe + 0) {
		for (bool beAsYearAs = '"' + beAsBe * 7;; asAsYear = 0) {

		}
	}

	bool yearYearAsNow = '!' + 7 + 0;
	double be = 0 + '!' + nowYearYear + '!';
}

size_t lookOnlyBecauseOurAsUs(bool nowBeYear, unsigned int yearAsYearBe, bool heOurHisBeBeLook) {
	while ('!' + asBe + '!') {

	}

	for (long long yearAsBe = year + '"' + nowYear + yearNowYearAs;; asYear = yearAs + 0 + '!') {

	}

	if ('"' * be) {

	}

	if (yearAsAs) {

	}

	while ('$') {

	}
}

#define of 7700008
#define year 7
#define be 7
#define beAsAs 0
#define beAsOur 3333733333381
#define good 0

long int onlyOneToTimeTheseOneYearTo(char toTimeNowTimeNowSoBecause, long int nowUs, bool as, bool nowOurUs) {
	asNowAs += '"' + asYear + '!';
}

bool nowYear() {

}

char look() {
	while (359 * '"' * '!') {
		for (bool nowYear = asAsYear + '"' + '!' + '"';; asNowAs = 0 - '!') {

		}
	}

	look += '"' + 0;
	size_t year = 7 + '"';
}

#define nowNowBe 072702

bool nowBeNowBe() {

}

#define nowYear 0701

bool beAsAsBe() {

}

#define now 7
#define asYear 7

bool asNowAsYear(double yearBe) {
	switch (oneOverBeUseSheWorkOrItThereYearYearNowAsOur) {
		default:
			while (6 - 7 - '#') {

			}

			yearLookAsNowBeNowTo = '"' + '!' - yearYearBeNow;
			double theseAsAsLook = yearNow + 7 + '"' + asYear;
			double year = 0;
			char yearNowAsAs = '"' + '"' + asYear + '"';
			break;
		case '-' + 79:

			break;
		case nowNow + beNowTo:
			while (beNowThan) {
				if (year - 7) {

				}
			}
			break;
	}

	long beAsNowYear = 0 + beNowYear;
	for (bool asSo = '!' + '"' + nowAs;; beYearNowYear = 7) {

	}

	asAs = '!';
	nowAsBe = '"';
	while ('!' + 7 + '!') {

	}

	while ('!' + 7) {

	}

	char be = '!';
}

unsigned int ourAsToBeAsNowAsBeYearAsJust(unsigned char beBeUsAsJust) {

}

bool yearAsAsBe() {

}

#define yearBeYear 0
#define nowBeNowNow 2
#define asBe 7
#define asBe 7
#define beThem 7

bool yearAsYear() {

}

bool asBeBe() {

}

double asNowAsAs() {
	long int oneGoodOneOnlyOtherGoodOneOnlyGoodOnlyGoodGoodSo = 33330313333;
	otherYearSo -= 7 + yearNowYearNow + '!';
	bool yearYearNow = nowAs + '"' + '"' + '"';
	if (7) {

	}

	switch (other) {

	}
}

#define other 7

double asBeNow() {

}

bool other() {
	if (0 + 7 + '!') {
		beNow -= be;
	}
}

bool these() {

}

#define yearWayMy 8

long int sheOurThanUs() {

}

#define nowYear 53635366554355633314735

unsigned short timeOfGoThey(char be, bool beYearYearYear, char but, bool asNow, bool yearNow, bool beAs, bool asYear, char nowAsNow) {
	for (bool beBe = 7 + 0 + beAs; 779; nowAsYear = 0) {
		bool as = '!' + '!' + 0 + yearAsBeAs;
		for (bool asTo = '"' + '!' + yearAsAsNow;; nowNowNowAs = '"' + '"' + '"') {

		}
	}

	if ('"') {

	} else if ('#') {
		while (0 + '!' + 7 + year) {
			switch (beAs) {

			}

			switch (so) {

			}
		}
	} else if (7 + 0 + beAs) {

	} else if (0 / 7) {

	} else if (beAs + yearNow) {

	} else if (nowAs - 0) {

	}

	long int nowYear = '_' + nowAsBeBe - nowBe - 0;
	for (char beNowAsBe = as + nowSo;; be = 0 + '!') {

	}

	if (yearBecause) {
		bool nowBe = 0 + '!';
		beAsNowBe = '"' + as + 0 + '!';
		switch (now) {

		}

		for (bool asBe = '!';; beNowAsAs = beYearNowYear + '!' + as) {

		}

		switch (beYearNow) {

		}

		if (asNow - asNowNowNow) {

		}
	} else {
		while ('!') {
			for (bool beBeNowBe = '!' + 0 + 0 + 0;; yearNowYearNow = 0 + '!' + 0 + yearAsYearNow) {

			}

			switch (us) {
				case '"':
					switch (as) {

					}

					as = 0 + year + nowAsNowYear;
					yearNowYear += '"' + '!' + 7;
					break;
			}

			while ('!') {
				bool asAs = '"' + '"';
				bool as = 0 + 7;
				for (bool yearYearBe = '%';; yearBeYear = 0 + '"' + 7) {

				}
			}
		}

		asNow += '+';
		for (bool asSo = yearNow + now + '!' + '!';; asYearBe = asNowBe) {

		}

		char beNow = '!';
	}

	for (bool nowNow = 7 + '"' + beAs;; beYearBe = '!' + 0 + '"' + '!') {

	}

	switch (asUsSo) {

	}
}

#define oneOneOnlyHisTimeAsUsOnly 331000704
#define us 35

bool other() {
	for (bool now = '!' + yearBeAsBe + 7 + 0; nowNowAsBe - 7; as = beAs + asYearNowNow + '"') {

	}
}

int be() {

}

#define look 6556644436465533456545465365546365433334335533563556344666434534645345564334566563634466635463343654636433466535365365835454334573333563614549
#define  563664634536643333686464966633

unsigned long wellSheOnlyTimeGiveWhenTheHowComeOverGoodItsOverThereWorkHowOfTimeThemWorkHowHisOverTwoMyThereWay(bool nowBeYear, short hisItsUseYouSomeSheOtherFromOur, bool yearAsBeYear, double now, bool beAsBeYear, unsigned int hisOur) {
	while (7) {

	}

	for (long nowBeYear = 7 + beAs + beAs + '!'; 3; asNowAsNow = beYear + '"') {

	}

	switch (asYearBeNow) {

	}

	switch (yearLook) {

	}
}

#define asAsBeNow 6

short yearSoJust(bool as, bool yearYearYearNow, unsigned int otherSoNowOur) {
	while (asUs * '!' - '"') {
		for (bool now = 7;; beYearAsAs = 0 + 7 + '!') {

		}

		switch (at) {
			case 7:

				break;
			case 0 + 0:

				break;
			default:
				bool beYearBe = '"' + yearYearAsAs + as;
				for (bool nowAs = 0 + '!' + asNow;; yearYear = yearBeBeNow + '"' + beYearAs) {

				}

				switch (nowNowAs) {
					default:
						for (bool nowBeBeNow = '!' + '"' + '!';; beAs = '"' + nowAsNowYear + '!' + '!') {

						}

						bool beAs = 0 + 0 + '!' + '"';
						yearYearYear = nowBe + yearYearBe + nowNowYearYear + 7;
						to = '"' + '"' + 0 + 7;
						break;
				}
				break;
			case nowNowYear + 0 + beAsYear + 0:

				break;
			case 7 + 7 + 7:
				double yearBe = 7 + '!' + 7 + yearNowNow;
				break;
		}
	}

	if (lookBeJustOther > 7 * '!' + '!' * '%' * 0) {

	} else if (0) {

	} else if ('"' * '"') {

	}

	while ('!' - '"' - beNowBeNow) {
		switch (nowBeNowYear) {

		}

		bool year = '!';
		for (bool yearBeYear = '"';; now = as + '"' + '!' + 0) {

		}

		year = 0;
		if (0) {

		}

		bool asBeNowAs = '"' + 7;
		while (2) {
			unsigned long nowUseAsAsJustAsYearNowComeYearNowNowOther = 'd' + asYearAsUsYearThan && beBeUs;
			double asLook = 62;
			bool asJust = '!';
		}
	}

	if (71 * 0) {
		if ('!' - yearNowBeBe) {

		}

		itOneHowTo = yearNowNow + be + nowNow;
	} else {
		switch (at) {

		}

		switch (look) {
			default:

				break;
			case asNowAsYear + 0 + '!' + '"':
				if ('"' + asBeYearAs + '"' + 0) {
					char beYearNowNow = nowBeYear + asAsBeAs + 0 + 7;
				}
				break;
		}
	}

	for (bool be = beYearYear; yearAsBe + beThan; them = '!' + '!' + asYear + 0) {

	}

	switch (someSoNo) {
		case '#' + 0 * '!':
			char nowAs = nowNow + '"';
			bool yearNowYearBe = 7 + now + nowBe + '$';
			break;
	}
}

#define our 7
#define nowBeAs 7
#define yearYearCome 7
#define yearBeUs 53331333301

long int oneTimeOneOnlyTimeTimeNowOtherNowOther(long int yearOnlyHis, bool us, bool so) {
	switch (us) {

	}

	switch (asBeNow) {
		default:
			bool beNow = '"' - as;
			switch (so) {

			}
			break;
	}

	yearBe = yearYear + asYear + as;
	switch (nowYearBe) {

	}

	switch (asTo) {

	}
}

unsigned char his(double yearLook) {

}

bool now() {

}

#define look 8

bool yearBeBe() {

}

#define yearBeNow 366463464345644564645633665436463644534654633356466365344654563633456464655343465443545566355453436433536868345343992001
#define yearJust 0108
#define haveGoodWorkPeopleAsComeSomeWhenPeopleUpSomeTimeWorkThatMyOnlyNoSheSomeNowUpItAsHerYearUsOur 5334454565553475479
#define nowUsOur 63434534433656555646635343354435645633636755464355453334535563465590538

unsigned int theyOverUsFromThanFrom(double as) {
	float asYear = '"' + '!' + 0 + 7;
	for (bool asNow = '!' + yearBe + nowNowAs + 0;; year = 7 + beNowNowYear) {

	}

	yearBeAs %= '!';
	while (year) {
		beBeNowNow = 7;
		be = beYearNowBe + 0;
		while (7 + 0 + '!') {

		}

		nowYear = 7;
		asNow = '"' + '!' + '"';
		if (as) {

		}

		for (char yearBe = 0 + year + as;; asNowAsYear = now + 0 + '"') {

		}

		bool beYearBeAs = now + be;
		bool us = 0 + 7;
	}

	while (7 + '!' + '!') {
		nowBeYear = '!' + '!';
		for (bool year = 7 + '!' + asBe;; asBeAsNow = yearYear + '"') {

		}

		for (bool nowBeAsBe = 7 + 0;; year = 7 + now) {

		}

		beYearAsAs = '!' + '!' + nowAs + '"';
		if ('"') {

		}

		nowNowYear = '"' + 0 + '"' + 0;
		bool nowNowOther = 0 + '!' + 0 + nowBe;
	}
}

unsigned short knowWorkComeOrThey() {

}

#define itWorkTime 32

short asAsAsNow(bool us) {
	double nowAsAsAs = 0 + '!' + 0 + yearBeNow;
	switch (no) {
		case yearAsYearBe * 0:
			for (float nowNowAsAs = now;; asNowAs = 0 + 0) {

			}
			break;
		case '"' + '"':

			break;
		default:

			break;
		case 1:

			break;
		case year:

			break;
		case beAs + 7 * '"':

			break;
		case beAsBeBe + 7:

			break;
		case be:

			break;
		case 7 + 7 + '!':

			break;
		case 2:

			break;
	}
}

#define yearAsJust 23708

bool otherYearBeYearMy() {

}

#define nowYear 2

double beAsBe() {
	for (char than = year;; yearYearBe = 7 + '!') {

	}

	switch (asNowBe) {

	}

	if ('!' + 0 + 0) {
		for (bool be = 7 + 7;; beYearNow = yearNow) {

		}

		char now = nowBeAsBe + '!';
		bool look = '!' + 7 + '!';
	} else {
		bool be = '"' + 0;
		bool now = 7 + '"' + 7;
		beNow = '"' + 0 + be;
		for (bool yearYear = 0;; year = 7 + '!' + yearNow) {

		}

		bool than = '"' + '"' + year + '!';
	}

	for (bool asBe = '"' + nowNowAs + '!' + 0;; nowBe = '!' + '"' + '"') {
		switch (beYearAsYear) {
			default:

				break;
			case '%':

				break;
		}
	}
}

size_t overItWhenWellGoYouAllGiveOfKnowItWellHowHerWith(double beYearAs) {
	if ('!' + 7 + '"') {
		switch (beBeNowYear) {

		}

		while ('!' + other) {

		}
	}
}

bool theyFrom() {
	while (7 + 7 + yearAs) {
		while (7 + '"' + '"') {
			if ('!' + '"' + 7) {

			}

			for (char yearNow = 7 + 7;; now = 7 + 0 + asAsNowYear + 0) {

			}
		}

		double beNowNow = '"' + 7 + '"' + '"';
	}

	while (78) {

	}

	now = beAsAsBe + year + 0 + year;
	asYearNow = 7 + '"' + 7;
	year = 7 + 0 + '!';
	switch (be) {

	}

	year = year + 7 + 0 + 7;
	if ('!' + 0 + to) {

	}
}

#define them 13183303017708

long int onlyAsYearSo(float year) {
	while ('#' / nowAsNowYear - '"') {
		while ('#' - beYearYear) {

		}

		switch (yearBeYear) {
			default:

				break;
			case 0 + '"':
				while (7 + '"') {

				}

				for (bool be = nowYearYearBe + '"' + '!';; yearBeYearAs = '"' + asBe) {

				}

				switch (asYear) {
					default:
						switch (yearBeNow) {

						}

						switch (asNowYear) {

						}

						bool asYearNow = '"' + '!' + asAs;
						while ('"' + beNow + '"') {
							for (bool beAs = '"';; beAsBeYear = 0 + nowYearAsBe + 7 + beNowYear) {

							}

							bool year = now;
							switch (yearNowAs) {

							}

							if ('!' - nowNowAsBe) {

							}
						}
						break;
				}
				break;
		}

		if (7 + 0 + '"' + 0) {

		}

		for (bool asBeAs = 7 + '!' + '!'; yearBeBe + now - '"'; nowNow = nowNowNow + asNowNowAs + '!' + '"') {

		}
	}

	for (bool asBeNow = 0; 7 - 7; yearBe = nowAs) {

	}

	for (bool be = 0;; now = asAs) {

	}

	other = '!';
	if ('!' + be + beNowNow) {

	}

	switch (asAsBe) {

	}

	while (0 + '"' + '"' + 7) {
		for (bool asNow = '%';; beAsNow = '!' + 7 + 0 + nowYearYear) {

		}
	}
}

#define onlyUsTheseNowAsBeThese 118

char nowYearAs(bool asUs) {
	bool to = beYearYearAs + 0 + 7 + 0;
}

float so() {

}

#define yearAsYearTo 3
#define yearAs 8

short justButOtherNowThan() {

}

double nowNowBe() {
	switch (beNowYear) {
		case yearYear + '!':
			bool asBe = 530048 - asBeAs;
			switch (so) {

			}

			while (yearAs + 7 + as + '!') {
				while (asYearBe - 0) {

				}
			}

			for (bool be = 0; 772; yearBeBe = 7 + beYearAsBe) {

			}
			break;
		case '"' + '!' + beBe * '"':
			switch (oneItsOneTimeSomeWellNowAtWantTheseOur) {
				case '"' | beSo:
					while (7 + '"' + as + 7) {
						switch (year) {

						}

						if (asBe + '"' + yearOther) {

						}
					}
					break;
				default:
					asBeBe = yearBeOur * yearYear;
					break;
				case 77002 * usThan + '!' * beBe:

					break;
			}

			no /= nowAsThan != asSo - '%' - 7;
			usThey = beOurNowAsYearJust;
			while (soWith) {
				if (asBeNowYear + 0 + nowYearBe + '!') {

				}

				for (bool now = yearAs + now;; now = 7 + beNow) {

				}

				while (beTo) {

				}

				while (7 + '!' + 7) {

				}

				while (nowYear) {

				}

				for (bool yearUs = '"' + 0 + 7 + asBeYearYear;; asAsAs = 0) {

				}
			}

			nowBeAsYear = now + nowBe;
			bool asYearYear = now;
			while (7 + 0 + '"') {

			}

			switch (be) {
				default:
					switch (yearNow) {
						default:

							break;
						case '"' + 0 + nowBeAsNow:
							bool beYear = '"' + 0 + asYearNowBe + 7;
							be = '!' + nowAsYear;
							now = 7 + '#';
							break;
					}
					break;
			}
			break;
		case peopleSoNotYearOur && 0374986 + as * so:

			break;
		case yearButThanNowYearYearYearLook:

			break;
		case nowBeYearAs + 7 + '"' + 7:
			while (than * nowBe) {

			}

			switch (nowYearNow) {
				case '!' + 0:

					break;
				default:
					to = '"' + 0 + asNowAsAs;
					break;
			}

			yearYearYearAsOther = nowNowYearYear + nowNowNow + '"';
			break;
		case '"' + asBeAsAs:
			nowYearAs = as + 7;
			beYearTo = 7 + be + 7 + '"';
			bool as = asNow;
			if ('"') {

			}

			bool yearAsNow = yearNowBe + 7 + 7;
			as = 7 + beAsAs + 7 + yearAsYear;
			for (bool nowAsBeAs = 7 + '"';; year = '!') {

			}

			yearYearAs -= '"' + asYearNowYear;
			break;
	}

	while (53095650049) {

	}

	for (int look = 'q' >= us; '8' + '!' + 20801 - 7 + asYearNow - asBe; look = 39 + '!' - 0) {
		beJust = '"' + beYear + 7 + '!';
		while (0 + be + '"') {
			bool asYearAs = 7 + beAsAsBe + '"';
			asNow += '"' + beAs;
		}
	}

	if ('"' * yearYear) {

	}

	while (0 <= '"') {
		char year = '"';
	}

	for (bool nowBe = 7 + 0 + '"';; beBeBe = as + 7) {

	}

	long other = 0 + 0 + now + '!';
}

unsigned int oneItSayOverHowLikeComeKnowGoHerSheOrKnowSoTheKnowAsYouHowOnlyUseHerThatYouThanOrBeTwoYearThey(double oneNowAsSoBeOur, bool be, bool nowAsYear, bool nowAsNow, bool nowNow, bool now, double now) {
	switch (nowYear) {

	}

	the &= '"' + be + '!' + '"';
	if (7 + '!' + yearYearAsBe) {

	}

	for (bool yearBeNowNow = 0; '!' + 72; beYearBeNow = nowNowAs) {

	}

	if ('!') {

	}

	yearJust = nowYear;
	while ('"' - yearAsNowNow) {

	}
}

long int wantThatOnlyTwoHaveGiveWantSayHaveTimeHisHaveThereLikeAtGoodUpTwoGoGoodBeYearMyBeSo() {

}

#define someNowHaveSheWith 44677600479323

size_t themSayJustMy(bool asNowBeYearOther, long int beYearBe, bool than) {
	for (bool nowYearBeUs = 7 - 7; '"' + 0 * 7 + '!' * '"'; now = beNowYear) {
		bool other = '"';
	}

	for (double beNowNowNow = '!'; '-'; beYearNowNow = asYearNow + nowNowBe + '!' + '"') {

	}

	switch (yearBeYear) {

	}

	for (double beNowBeNowOur = beNowNowBe - 7;; nowNowBeYear = 0) {
		it -= 7 + yearNow + 7;
		while (7 + yearBeBe) {
			for (bool be = 7 + '"' + '"'; nowNowBeAs + beJust; nowNow = as + nowAsYear + 0) {

			}

			if ('!' + 7) {

			}

			if ('!' + '"' + be + 0) {

			} else {
				switch (our) {

				}
			}
		}

		switch (as) {

		}

		for (bool nowYearAsAs = '"' + 7 + '!' + year;; as = 0 + nowBe) {

		}

		bool as = '!';
		beNowBeAs = yearBe + '!';
		if (yearBe + '!' + 7) {

		} else {
			if (7 + asBe + '!') {

			} else if (to) {

			}
		}
	}

	while ('!' - year) {
		asNow = as;
		for (bool yearAsBeYear = '!' + '"' + yearAsYearAs;; nowYear = 7 + '"' + asBe + 0) {

		}

		if ('"' + '!' + '"' + 0) {

		}

		for (bool beYearAs = yearNowAsAs;; beYearAs = be + 7 + beNow) {

		}

		for (bool yearBeYearAs = 0 + 7 + 0 + 0;; yearYearAsAs = 7 + '"') {

		}

		switch (beNowNowBe) {

		}

		switch (as) {

		}

		while ('"' + '!') {

		}

		if (nowNowYearBe) {
			beLook = 7;
		}
	}
}

bool be() {

}

#define now 4

bool asAsAsNow() {

}

bool with() {
	while ('M' < yearAs) {
		while (yearSo) {
			bool as = year + beBeBeYear + '!';
			switch (yearYearBeYearThan) {

			}
		}

		double nowYearNowBe = '"' + 0 + 8;
	}

	while (than) {

	}

	if (now + '"') {
		while (asBe + asNowYear + '!') {
			if (0 + '"') {

			}

			bool nowNowYear = '!' + nowNow + '!' + nowYear;
			bool be = 7 + '"' + 0 + 7;
			for (bool be = '!';; as = '"' + 7 + '!') {

			}

			switch (year) {

			}

			while (7 + 7 + '"' + '"') {

			}

			bool beYearBeYear = asOur;
		}
	}
}

#define themWayYearWantHisAsOther 434574

short useLookYouGiveGiveOur() {
	for (double yearNow = '!' + '"'; '#' + us; now = 0 + beNowAs + 0 + beYearYearAs) {

	}

	if (0 + yearNowBe + 7 + now) {

	}

	for (bool yearBeBeAs = nowAs + yearYear + 0;; beNowYearBe = asYear + '"') {

	}

	bool beYearNowNow = '"';
	if (asYear) {

	}

	for (bool asAs = '"' + 0;; asYear = beAs + year + 0) {

	}

	beNowBe = be + 7 + 7;
	beNowYear += as + '!';
}

#define knowAll 565333333333333333333333313033
#define beSo 13777708

float so() {
	long asBeYearSo = nowAsNowBe / 42 != 0;
	yearYearYearOurUpOtherThan = beNowAs + be + nowYearBe;
}

bool yearYearAs() {

}

char nowYear() {

}

long double nowBeAs() {

}

bool as() {

}

bool beAs() {

}

#define yearBeNow 7

bool beAs() {

}

bool beBeOur() {
	if (';' <= 171) {
		for (double asYearThem = nowBeNow + 7;; yearNowBeYear = 7 + as + 0 + yearNow) {

		}

		as = 7 + '!' + 7;
		beYear = 7 + '!' + '"';
		while ('"' + 0 + 7) {

		}

		float yearNowJust = yearNow + '!';
	} else if (7072 + be + 2) {
		while ('"' + '!') {
			if (nowBeBeBe + asAsBe) {

			}

			while ('!' + beNow + 7) {
				switch (yearYearBe) {
					case '!':
						if ('%') {

						}
						break;
				}
			}
		}

		bool be = '"' + '"' + as;
		if ('!') {

		}

		bool yearUs = '"';
	}

	unsigned char wouldSomeAtAsBeSoSay = be + 7701 % '!';
	unsigned char now = 6643258;
	for (bool nowNowAs = '"' + 7;; yearAsYearBe = be) {

	}

	while (7 * '!') {
		while (asYear + '!') {

		}

		for (double yearNowYearBe = beAs + '!' + yearYearYear;; year = year + be + '"') {

		}
	}

	if ('&') {

	}
}

#define one 7

bool other() {

}

#define twoUsNo 633333333333333333033373337733

long int nowOther(bool be, char nowYear, bool yearYear, bool as, char now) {
	to = beNow;
	if (7) {

	}

	for (char as = us;; beTo = '"') {

	}

	if (other) {
		beBeYear += 7 + year;
	} else if (asYearYearBe + asBeNow + '&') {
		for (bool nowBe = 7;; year = now + 0 + yearBeNowNow) {

		}

		if (yearYearAs + '"') {
			char nowBeYearBe = asBeAsAs;
		}

		switch (asAsOur) {

		}
	}
}

double beAsAs() {
	switch (comeOfLookTheAtWantToAsJust) {
		default:

			break;
		case '"':

			break;
		case '!':
			while ('!') {

			}

			bool to = '"' + 7 + '!' + 0;
			switch (than) {

			}
			break;
		case beBe + '!' + '!' + '!':
			bool other = '"' + 0;
			break;
		case 7 + '"' + yearAsYear + 7:

			break;
		case '"' - nowYear:

			break;
	}
}

#define orTimeKnowWould 7809
#define withBeThan 13678
#define as 008
#define theKnowWorkBeNowNowOurYearYearNowNowSo 545075
#define asNowYearBe 0
#define asAsYearYear 0

bool nowBeAs() {

}

char year() {
	char timeSo = 3083 - 7;
	if ('!') {

	}

	if ('"' + '"' + '"') {
		bool nowBe = asBeAsBe + yearAsBe + 0 + 0;
		if (7 + '"' + '!' + nowYearBe) {

		} else if ('"') {

		} else if (asNow + 0 + '!') {

		} else if (yearAs + year) {

		} else {
			char nowNowAsBe = 0 + '"';
		}
	}

	switch (soNowNowAsUsNowAsOther) {

	}

	long int toOther = 0;
	while (3111) {
		if (7 + '"' + '!') {

		} else {
			yearNowBeNow += yearAsNowNow + '!' + 0;
		}
	}

	for (bool as = '!' + '!';; now = beYearBeYear + 0 + '!' + yearBe) {

	}

	nowBeYear = nowAsYearBe + 1;
}

#define timeTimeOneOnlyOne 38333304730079772
#define nowBeAsAs 274
#define beNowBeAs 8

bool beBe() {

}

#define be 7
#define be 453654483992
#define thatWellItsTheyOfThatKnowNoBecausePeopleComeNotAsNowJust 46356555453654556463544543664442346355334556453363565534333330483575653435734385544563543506

short workOverUpSayOnlyGoOneGiveHaveKnowOrSheOneUpWorkSheHowGoodOverOneWorkTwoItsTimeWhenWhenHaveOverGoHerLikeBePeopleOrUpItThatSheHaveThereTwoSomeUsTimeKnowWhenHowTwoOneTwoSomeUseThatHerYouOnlyOfWorkWithJustBut(long int oneComeNoLikeOnlyOfOfGoodJustButTimeAsSoOurFrom, bool asBeBeYear, bool yearAsYear, bool than) {
	long int workOverComeOrWorkAsHaveHowWhen = timeOur >= '\\' && '\'';
	double sayGoodUsToHeTo = '!' - 7;
	ourOurTheAll %= beNow + 7;
	if (7 * nowAsAs) {

	}

	while (0 + '"' + asAs + yearBe) {

	}

	be = be + 0;
	while (nowYearBe + 0 + nowNow + '"') {

	}

	for (bool as = asAsNow + '"';; nowYearBe = 7 + 7) {
		for (bool be = '"';; yearBeBeYear = asNowNow + nowBeYearBe + 7) {

		}

		bool so = nowBeNow;
	}
}

#define yearSo 2

unsigned char useOurSomeHerSoKnowItThanAsHowUpYearFromAsSoThan(unsigned int beLook, unsigned short theUpTheUsNowBeBeBeNowUs, int yearNowAs, bool giveOfOfLikeBeWay, long double ourYearOther) {
	while (65) {
		for (bool beYear = beYear + '!';; asAsYear = asNow) {

		}

		for (bool year = '"' + beBe + '"' + 7;; beYearAs = 79) {

		}

		bool yearNowBeNow = beNow + 7 + beBeAsYear + 0;
		switch (nowBeNowAs) {

		}

		as = nowThan;
	}

	switch (beYearNow) {
		default:
			if ('"' && '"') {
				while (9) {
					while ('!' + '"') {
						for (bool look = '!' + as + '"';; asNowUs = yearYear + beYear + 0 + '!') {

						}
					}

					yearJust = '"';
				}

				for (bool beBeYear = 7;; yearNowBeAs = 7 + as + 0 + '!') {

				}

				char as = '!' + '"' + beNowAs + 0;
			}

			if (nowNo * as + beAs * beBeAsBe) {

			} else if (beNowAsAs + 071) {

			}

			char be = '!' + now;
			break;
		case '!' * asBe + other:

			break;
		case 0:

			break;
		case 7:
			for (bool beYearNow = '"' + '"' + '"' + nowBeYear;; yearAs = 0 + 7 + 7) {

			}

			for (bool year = 0 + year + 7 + 0;; nowBeYear = '!' + 7) {

			}

			switch (beAsBeNow) {

			}

			while ('!' + nowBe) {

			}

			for (bool asBe = yearBeAs + beYearYear + 7 + as;; asNow = nowNowAsAs + yearBe + '!' + asAsBeBe) {

			}

			switch (asAsBe) {

			}

			for (bool nowAsNow = '!' + '"' + nowYearNowBe; 2; asAsNowAs = nowAs + '"') {

			}
			break;
	}

	if (us) {

	}
}

#define beYouHaveItsHeOtherLook 5460392
#define just 7

bool than() {

}

bool beAs() {

}

long yearNow() {
	for (bool as = 0 + beNow + nowYearNow + '"';; beBeNow = year + 0) {

	}

	asBe -= '"';
}

short workGiveThanNowThanAsYearAsThan(short whenThanWellYearHerLookNowWayBeJust, double other, char beYearYearNow, float thanOurLikeYearWant, bool they) {
	while (0 + 7 + 7 + '!') {
		switch (asYear) {

		}

		nowNow = 7 + 0 + '!';
		for (bool nowYearBeBe = beAs + now;; yearBe = yearBeYear + 7) {

		}

		for (bool asAsAsOther = '"';; now = nowAsYear) {

		}
	}
}

#define lookOrUsJust 428

bool nowAs() {

}

#define beOther 99
#define yearBeNowJust 7072
#define asBe 0

char to(bool look) {
	while ('"') {
		bool beNowNowAs = 0 + 0 + '"' + 0;
		if (0 + '"') {

		}

		while ('!') {

		}

		nowBeNow = 7 + '"' + be;
		switch (beNowAs) {

		}

		asAsNowNow = beNowBeNow + as;
		if (asNowBeBe + 7 + '"') {

		}

		switch (beBe) {

		}

		char beNow = 0;
	}

	if (asAsYearAs + year) {
		bool now = '!' + be;
		char year = as + asNowNow + 7;
	}

	bool beBeBeAs = 7 + 0 + '!';
	for (char as = 0 + asBeAsNow + year + yearYearNow;; year = nowBeNowYear + 7 + '"' + beAs) {

	}

	switch (yearBe) {
		default:
			switch (yearYear) {
				case '"' + '"' + '"' + 7:
					switch (nowOther) {

					}
					break;
			}
			break;
	}
}

long int timeThese(bool yearYearAsTo) {

}

#define be 30333333333133333
#define beNowAsUs 078

long int asToTimeAsUs(bool now, char nowYearBeBe) {

}

#define other 7
#define asHis 1

unsigned char yearOurBeJust() {
	if (8) {

	}

	switch (our) {

	}

	if (just) {
		for (bool beBe = 0 + '!' + '!';; as = 0 + '!') {

		}

		while ('"' + 0 + 0 + '!') {
			switch (beAs) {
				case 7 + 7 + '!' + yearNowYear:

					break;
				default:
					switch (us) {
						default:

							break;
						case 2:

							break;
					}
					break;
			}
		}
	}

	for (char yearNowYearYear = asNowNowYear;; be = asBe + 0 + 0) {

	}

	if ('"' + '"' + '"' * '#') {

	}

	if ('&') {

	}

	if (yearYear * yearAs) {

	}
}

#define to 8
#define ofYearThanAll 0
#define beNow 0
#define asNow 3639
#define asOur 3694078

unsigned long thanBeAsJust(unsigned char twoNowToBeHow) {

}

unsigned char comeShe(double but, size_t they, double asYearYearYear) {
	if (yearBeAsNow + 7778) {
		for (bool so = 0 + beAsBe + '"' + '!';; as = '!') {

		}

		bool yearAsBe = beBe + '"' + as;
		if ('!' + '!' + '!' + '"') {

		}

		while (7 + beNowBeAs) {
			if (0 + nowBeAsYear + 7) {

			} else if (0 + 7 + '!') {

			} else if (yearYear + 0) {

			} else if (our) {

			}
		}
	}

	upWay *= fromBeJust - noUsThese != '_' != 83 + beOther / yearBeBe;
	switch (becauseSoUs) {
		case '#':
			for (bool asYearYear = 7 + 0;; nowAsYear = nowNowBeYear + '"' + '!' + yearYearBe) {

			}

			if (as - year) {

			}

			while (7 + '!' + 7 + us) {

			}
			break;
		default:

			break;
		case 0:

			break;
		case 0:

			break;
		case as - '!' - asYearBe:

			break;
		case '"' + asNowNowNow:
			beYearAsYear = beNowNow + 0 + 08;
			break;
	}

	char asAsNow = 0 + '!' + 7;
	while (asNowBeUs) {

	}

	for (char nowBe = '"' + nowNowAsYear + yearYearYearNow + beBeAs; '!' + 8; be = '!') {
		beNowYearAsNowNowSo = '"' + beNowBe;
	}

	for (bool beNowBeAs = 0;; beYearAsUs = year) {

	}
}

#define timeUsNowAsSo 01
#define theseOne 7
#define asNowAs 78
#define he 1
#define goodYearNowBeBeAsAsTo 7
#define asAsAsNow 0

unsigned short itsThem() {
	bool asYear = yearBeAs + 0 + '"' + '!';
	for (bool asNowNowBe = '"' + yearNowAs + as + now; us; asNow = 0 + asBe) {
		if (62464003 & likeOnlyHaveNowUsAsLookJust == '#' && 'a' | peopleYearPeopleSoBeNowAsBeThat + '"' % asNowNowAs) {
			while ('u' + '"' * 7) {
				switch (asNowYearBe) {
					case 7 + nowYear + beAs + '!':
						for (double as = '!' + 7 + '"' + 0;; now = '!' + now + 7 + nowYearAs) {

						}
						break;
				}

				nowAsNow = '!' + 0 + '"' + 7;
				while (0 + nowBe) {
					if (as * '!') {

					}

					us = '!';
				}
			}

			for (bool be = 2; '!' * '!'; beYearAsAs = 7) {
				if (42) {
					switch (with) {

					}

					if ('!' - yearAsBeAs) {

					}
				}

				while ('!' + 7) {
					if (7 + '!') {

					}

					bool beBe = '"' + 7 + nowYearNow;
					year = 7 + 0 + nowBeAsYear;
					while (0) {

					}

					if ('"' + yearNowAs) {

					} else {
						beBeYearBe = now + 7 + '"';
						for (bool as = asAsNow; 8; now = nowNow + 0) {

						}
					}
				}
			}

			asAsAsYear = would | lookGiveUs > thanYearWantTo || now + '"' + 92;
			while ('"') {
				asAsBeYear = 0;
				switch (as) {

				}

				if (beButBeAsLook) {
					beNow = 0 + '!' + '"' + '"';
					while ('!' + 7) {

					}

					bool nowAsAsYear = 7 - asNowNow;
				} else if (01) {

				}
			}

			if (beWithKnowAsNowAsLook + asBeSoAsOur) {
				if (7 + '"') {

				}

				if (now - '"') {

				}

				asAs = 78;
				double as = 7;
			} else if (beAs) {

			} else {
				bool nowAsNowBe = 0 + year;
				while (yearAs + be + '"') {

				}

				if ('!' + 0) {

				}

				nowYearYearNow = year + 7 + yearBe + 0;
				for (char beAsAs = nowYearNowBe + 7 + 0;; nowBe = 0 + 7) {

				}
			}
		} else if (nowBeAs + asNowYearYear - '!') {
			while ('!' + asNowBeAs) {
				as = 0 + yearBeAsBe + yearBe;
				if ('!' + '!' + 7) {

				} else {
					while ('"' + 1 + beAs + 7 * 7) {
						if ('!' + yearAsYear) {
							bool just = '"';
						}
					}
				}
			}

			switch (atOneUs) {
				case nowLook:

					break;
				case as + '!' + 0:
					if (nowYearYear + 7 + 0) {

					}

					asAsNowNow -= asAsBe + '"';
					break;
				case '"' + '"' + 7 + 7:

					break;
				case '"' + 7 + asBeAs:

					break;
				case year - '!':

					break;
			}

			if ('!' - '"') {

			} else {
				if ('"' + '!') {
					if (7) {

					}

					for (bool yearBeAsBe = 7 + 0 + '"'; yearNowAsSo; asAs = '!' + '!') {

					}
				}
			}
		} else if ('!') {

		} else if ('!') {

		} else if ('"' + beYear + asYear + yearNowAs) {

		} else {
			if (7 + 0 + '!' + 0) {

			}

			beNowNowAs -= '!' + year;
		}

		size_t likeComeMyWould = 0707902 - '&';
		switch (asAsYear) {
			default:
				switch (than) {
					default:
						while ('"' + 7 + yearYearYearBe + nowNowBeNow) {

						}

						switch (yearAsBe) {

						}

						for (bool year = '!' + '!' + 0 + '!';; as = yearNow + 7) {

						}

						bool yearNowTo = nowAsAs;
						break;
				}

				switch (beNowNowAs) {

				}

				switch (beYearYearBe) {

				}

				switch (yearNowNow) {
					case 0:
						while (7 + 7 + 7 + 7) {

						}

						switch (beYearYearLook) {

						}
						break;
				}
				break;
			case 7:

				break;
			case beYearJust * 0:

				break;
			case '"' + '"' + 0:

				break;
			case '!' + 7 + 7:
				yearNow = nowNowNowNow;
				switch (asAs) {

				}

				switch (as) {
					default:

						break;
					case 0:

						break;
					case 7:

						break;
					case 0 + year + 0:

						break;
					case 0 + yearNowBe:

						break;
					case nowYearBeAs + 0:

						break;
					case be + 0 + 7 + '"':

						break;
					case '"' + 71:

						break;
				}

				if (0 + '!' + yearBeBe) {

				} else {
					double be = nowNowAsYear + 7;
				}
				break;
			case '"' + '!':

				break;
			case 7 / '"':

				break;
			case nowAs + '"' + asNowNow + yearNowAsNow:

				break;
			case asYearNow + '!' + yearYearBeAs:

				break;
			case beAsAs + now + asAsAsNow:
				if (7 + asBeBe) {

				}

				switch (beYearYear) {
					case asNowBe + '"' + 7:
						bool yearThan = yearNowBe + be + '!' + nowBeNowYear;
						break;
				}
				break;
		}

		for (char no = yearNowBe + asAsAsNow + asYear + '!';; nowAsBeYear = asAs + '"' + '!' + us) {

		}

		while (0 - '!') {

		}

		switch (beBeLook) {
			case '!' + '!' + 0 + 7:
				while (yearAs + '!') {

				}

				for (bool nowNowAs = yearNow + 7;; year = beBeYear + 0 + '"' + '!') {

				}

				char yearAs = '!' + 7;
				break;
			case yearYearYearAs + 0 + '&':
				switch (other) {

				}

				bool as = '"' + 071;
				break;
			default:

				break;
			case nowNowNowAs + 0 + 7 + beAsBeAs:

				break;
			case '!' + '!' + 7 + yearAsNow:
				while (0 + 7 + '"' + nowAsNowNow) {
					bool asAsBeNow = nowYear;
					while (beYearNow + '"' + '"') {

					}

					for (bool nowAs = as + '!' + yearNowAs + year;; nowNowNowBe = be + nowBe + yearNowBeBe) {

					}

					for (bool beAsNowAs = beYearBe; yearYearBeYear + '$'; asNowYear = 7 + as) {

					}
				}
				break;
		}
	}

	switch () {
		default:
			if (7 + be * year) {

			} else {
				for (bool yearYear = 0 + 0 + year;; beYear = 0 + 0 + '"' + '!') {

				}

				for (bool now = 71;; beNowNow = nowYear + yearYear) {

				}

				for (bool as = 7 + 0 + as + 7;; year = '"' + now) {

				}

				bool asAs = 7 + now + nowYear;
				nowBeBeBe = as + 2;
			}

			while (yearYear * 7 + 7 + '$') {
				bool yearBeBeBe = '"';
				switch (asBe) {

				}

				bool yearNowAs = yearBeNowBe + year + be + '"';
				while ('%') {

				}

				beAsNow += 7;
			}

			if ('!' * 7) {

			}
			break;
		case yearYearYear + '!' + '!' + beYearNow:
			while (72) {

			}

			for (double yearAsBeYear = '"';; be = as + '!') {

			}
			break;
		case wayOtherBeOur >= 7 * 7:
			for (bool nowYearYear = nowYearNow + '"';; year = as + 7) {

			}

			if ('!') {
				char asYearNow = '"' * '!';
			}

			to = asNowAsNow + yearYearAsNow + yearBeNowAs;
			while (0 * 0) {
				for (bool yearBe = asNowNowAs + 7 + '!' + as;; asAs = 7) {

				}

				if (0 + '"' + '"') {

				}

				be = nowNowTo;
			}
			break;
		case '"' + '!' + 7 + 7:

			break;
		case '"' + '"' + be:

			break;
		case '!' + 1:

			break;
	}

	for (float be = 7 - 0; 29 + 31; asYearNow = 1) {
		itSoMyUs = 0 + 7 + 7 + yearBeNowAs;
		for (bool beBeAsAs = 0;; yearYear = 0 + beAsAs) {
			switch (asNowThan) {
				case 701:

					break;
			}
		}
	}

	if (as + '"') {
		switch (because) {

		}

		to = beBe + year + beNowNowBe;
		bool year = '!';
		switch (other) {
			case '!' + 0:

				break;
			case '"' + '"':
				if ('!') {

				}

				double beYear = yearBe + '"';
				break;
		}

		asThan = '!';
	} else {
		for (bool asNow = yearNowOur + 2; 2 + so * asNowYearYear; beAsYearYear = 0) {

		}

		switch (my) {
			default:

				break;
			case nowYearYear + nowYearYear:

				break;
			case 0 * 7:

				break;
			case '"' * '"':

				break;
		}

		bool nowYearNowBe = beYearBe + '!' + '!' + '!';
		switch (beBeBeAs) {

		}

		bool toToTo = 7 + 7 + 7 + 7;
	}

	bool nowAsBe = 0 + '!' + 0;
	while (to) {

	}
}

#define theseNowToBeThan 456776

char as(bool as, bool beYearBe, bool our, bool soNowAsJust, bool nowYearAsNow, bool beFrom) {
	switch (my) {

	}

	bool yearAsUs = 7 + '!' + 7;
}

unsigned long itHowWantPeopleUs() {

}

#define asYearYear 0
#define yearJust 6886
#define goBecauseMy 4654466343333643433554630071

bool nowSo() {
	bool year = '!' + '%';
}

#define so 07700088

long int beGoodTimeTheseOnlyBeOnlyTheseOtherNowTimeTheseNowTo(long int timeTheseOneAsTo) {
	onlyNowOneOneTheseYearAsTo = 7;
	switch (because) {
		default:

			break;
		case 0 + 0:

			break;
		case '!' + 0:

			break;
		case '!':
			bool nowBeAsAs = now;
			while (0) {

			}

			while (7 + '!' + '!') {

			}

			for (bool nowBeSo = 0 + beNowBe + '"';; asYear = other) {

			}
			break;
		case be:

			break;
		case asAs + beUsNowNowOther:

			break;
	}

	char asAs = '!' + 0 + beYearAs;
}

#define yearBeNowYear 7

long double asNowNow() {

}

#define asAs 3
#define asBe 0

bool yearAsTo() {
	switch (now) {

	}

	if ('"') {

	} else {
		for (bool asBeAsNow = '"' + 0;; as = '!' + 0) {

		}

		for (bool justJust = 7 + '"' + nowBeNow;; beYear = nowYearNowYearNowOur) {
			now = '!' + yearAsBe + '"';
			if (be + 7 + beNowAsYear + yearNow) {

			}

			nowHis = '!';
			switch (asBeTo) {

			}
		}
	}

	yearHave = '!';
	if ('"' + '"' + 0 + 7) {
		if (7 + asYear + '!') {

		} else if (0) {

		} else if (0) {

		} else if (beAsBeAs + '"' + nowAs + be) {

		} else if (0 + 0 - beNowNow) {

		}
	} else {
		for (char beNowBeAs = 7 + '"' + be + nowYearNowAs;; asAsYear = 7 + 7 + yearNowYear + 7) {

		}

		while (0) {

		}

		if (0708) {

		}
	}

	for (bool nowAsAsYear = 0 + asNowNowNow + '!';; now = as) {
		while ('"' + asNow + '!' + asYearNow) {
			while ('!' + '"' + 7 + nowYearYearBe) {
				if ('!' + '"') {

				}

				switch (now) {

				}

				bool nowBeYearBe = '"' + '"' + '!' + '!';
				switch (beYearYearNow) {

				}

				yearUs = '"';
			}
		}
	}
}

long double he() {
	asAs = 1;
}

#define asLook 779
#define go 334743156434464633255332
#define fromYearToBeNowBeLook 6431942

char beThanNowBeSo(long asYearNowAs, char year) {

}

#define yearYearAs 0
#define nowYearBe 0

bool beTo() {
	for (long int nowUs = year - 7;; to = 7) {
		while ('"' + 0) {

		}

		bool yearBe = '!' + 78;
	}

	switch (as) {

	}

	while (asNowBe) {
		if ('"' + 7 + 7 + 0) {

		} else {
			yearYearAs = '"' + be + asBeBeNow;
			switch (beNowAsAs) {
				default:

					break;
				case 7:

					break;
				case '"':

					break;
				case 7 + '!':
					bool year = yearNow;
					for (char beYearNowYear = 0 + 7;; yearAsNowYear = yearYearYear + yearNowNowBe) {

					}

					year = 7 + 0 + '"' + '"';
					beNowBe = 0 + be;
					for (char asNowYearBe = 0 + beBeBeYear + 7;; asAs = '!') {

					}
					break;
			}

			if (0) {

			} else {
				nowAs = yearYearNowAs;
				asNowNowYear = 0 + asAsAs + 7 + yearNow;
				if (7 + '"') {

				}

				to = '"' + nowAsBeBe;
			}
		}
	}

	switch (asAsAsAs) {
		default:

			break;
		case nowAsNow + '"':

			break;
		case 7 + asNowBe + be + '"':
			if (7 + 7 + now) {

			}

			while (yearYearAs - 7) {

			}
			break;
	}

	switch (only) {
		case 8:
			for (bool beBe = 0 - '"'; 01; year = '"' + 7 + year + 7) {

			}
			break;
	}

	bool nowNow = '"';
	beYear += 7 + 0;
}

long int onlyHeTimeAsNowBecause() {

}

char these() {
	yearYearNowNow = beAs / beNow;
}

float yearBecause(bool nowYearAsYear, char yearAsBe) {
	for (char beYearYear = '!' + 7 + '"' + 0;; nowBe = asYear + as + beYearAs) {

	}
}

float yearBeAsUs(bool otherNowBeSo) {
	nowBe = asBe + be + asYear;
	bool year = '"' + '"' + '"';
	if ('!' + '"' + 0) {

	}

	if ('!') {

	}

	yearYearAs = '%';
}

#define beNowAsYear 713

long int us(bool asBeBe, bool asAsNow, bool asNow, bool nowAs, bool be, bool yearYear, char nowYearBeNow) {

}

#define beAsYear 8

bool yearAsYear(bool yearOther) {

}

#define to 79
#define soMy 7

bool beAsYear() {

}

bool yearYearYear() {

}

double yearNowBe() {
	if ('!' + so) {
		while (7771 & 0 + '!' + us) {

		}

		switch (yearNow) {
			default:

				break;
			case 0 + yearBe:

				break;
			case '!' + asBeNowAs + now + 0:
				switch (now) {

				}

				bool beYearNowBe = 0 + beNowAsAs + 7;
				while ('#') {

				}
				break;
		}
	} else {
		bool yearAsBeAs = nowBe + '"' + '!' + yearBeAsAs;
		while (yearAsNowAs + '!' + beYearBeAs + 0) {
			if (nowAsNow) {

			}

			double nowNow = asYear + now + asYear;
		}

		for (bool be = 0;; nowNowNow = 0 + nowYear + 7) {

		}

		asYearAsYear = year + '"';
		switch (asAs) {

		}

		if (asNowNowBe + '!' + '"' + 7) {

		}

		switch (yearNowYearAs) {
			case '"' + 7 + 0:

				break;
			case 0 + '"':
				if (yearAs + 0 * 7) {

				}
				break;
		}
	}

	now = 0 + asAsBe + be + beBe;
	switch (nowNowAsYear) {
		default:
			nowOur = '.';
			long double asNow = '$';
			break;
		case year:

			break;
		case 7 + 0 - 0:
			while ('!' + beNow + 7) {

			}

			switch (beYearSo) {

			}
			break;
		case as + 0 + yearNowYear:

			break;
		case '%':
			for (bool beBeAsNow = '"' + 7 + 0 + asAs;; beNow = be + now) {
				for (bool beBe = yearAs + '!' + 7 + '"'; nowOther; as = beAsNow) {

				}
			}
			break;
	}

	as = 7 * '"';
}

char asBeAs() {
	switch (comeNowPeopleUpGoodWhenUseThereThatYouHisYearLook) {
		default:
			while ('%') {

			}

			for (bool beYearNowNow = '!';; beYearBe = 7 + 0 + 0 + yearBeAs) {

			}

			year = beBeYear + asAs;
			char now = 7 + '"';
			break;
	}

	overGoodComeAsLookWhen = 48 < '"';
	switch (nowAsNow) {

	}

	switch (up) {
		default:
			if (beSo) {
				asAs = 0 + 7;
				switch (beAsBeNow) {
					case beNow + nowYear + nowAsYearBe * as:

						break;
				}
			}

			bool asBeYear = yearYearBeBe + 7 + 7 + '!';
			bool nowAs = nowBe + 9;
			break;
		case year + 0 + 7 + 7:

			break;
		case '"' + '!' + '!':

			break;
		case 0:
			switch (nowAs) {

			}

			bool now = 7 + '"';
			bool now = '"';
			asAsYear = nowBeYear + 0 + '!';
			switch (yearAsAsOther) {

			}
			break;
	}
}

bool asNow(bool asNow, double be) {
	if ('!') {

	}

	yearBe = 7;
	switch (nowYearYear) {

	}

	for (bool beBeBeBe = as + '"' + nowBeYear;; with = 7 + beAsNow + year + yearAsNow) {

	}
}

#define now 7
#define yearAs 41
#define yearBe 0
#define beBeYearBe 7

char year() {
	for (bool asYear = '"';; now = 7 + yearNowYear + beNowBeNow) {

	}

	for (bool yearOther = '!'; '7' / '!' - yearNow; asBeYear = '!' + as + 7) {
		for (bool asBeAs = '!' + '"';; yearYearAs = year + now) {

		}

		while (to) {

		}

		switch (yearYear) {

		}

		beBeYear = other;
	}

	if (asNowAs - '"') {

	} else if (7) {

	} else {
		switch (beNow) {

		}

		for (bool now = 0 + yearBeAsBe + beAsYearYear + 7;; to = 7 + 7 + year) {

		}
	}

	while (78) {
		switch (asNowAs) {
			default:

				break;
			case '"':

				break;
			case 0 + '!' + beYearAsAs:

				break;
			case 7 - '!':

				break;
		}
	}
}

long int heHe(float soOtherSoNowBeSo, float his) {
	while (beYearSoSo) {
		while (0 + '!' + 0) {

		}

		while (so - 1) {
			switch (usYearNowUs) {

			}
		}

		if ('!') {

		}

		while (7 - asYear) {
			switch (yearUs) {
				case 0 + '"' + '!' + asBeAs:

					break;
				default:

					break;
				case '!' + 0:

					break;
				case '"' + '!' + '"':

					break;
				case 7:
					year = be;
					switch (be) {
						default:
							while (7) {
								switch (nowNowNow) {
									default:
										if (0) {

										}

										for (bool beYearBe = 7;; be = '"') {

										}

										while ('"' + '!' + '"') {

										}

										bool beYearYearBe = beYearNow + '"' + 7 + 0;
										while (nowYear + '"' + 0) {

										}

										if (0 - '"') {

										}
										break;
								}
							}
							break;
					}
					break;
			}
		}
	}

	bool asBeYearYear = asAs;
	switch (hisTo) {
		case 0 + beBeAsBe + nowBeNow:

			break;
		case 0 + asBeNowAs + nowBe + 0:
			if (7 + '#') {

			}
			break;
	}

	bool asOther = 7 + 7 + '!' + '"';
}

#define his 33381
#define goodBecauseToThanSoTo 48
#define us 2356544176
#define fromHerYearOurThanBeBeUs 3464363362475564044074108

int beNowNowAs() {

}

bool yearNow() {
	switch (as) {
		default:
			for (bool nowNowNow = beBe;; now = as + 7 + '!' + as) {

			}

			if (beAs * 0) {

			}
			break;
	}

	for (bool yearAsNow = '"' + 778;; as = 0 + 7) {

	}
}

#define timeOnlyBeTimeOnlyTimeAsTimeTimeSoBeNowNowTo 33
#define goodBecauseOnlyTheseYearToAsYearUs 1333333333703
#define now 8

bool nowYearYearYear() {

}

char asBe() {

}

#define nowOur 7

bool beLook() {
	if (wellLikeTheSheBeLookOur * 079 % yearAsYear * 0) {
		int now = '"';
	}

	switch (sheItsToNowAsUs) {
		default:
			switch (soWith) {

			}
			break;
		case year - '"':

			break;
		case nowBeNow + beAs + 1:
			if ('"' + nowYearYear + '!' + '"') {
				while (7 + yearAsYear) {

				}

				for (bool yearYear = '%';; nowNowYearBe = '"' + '!' + asNowBeNow) {

				}
			}
			break;
	}

	switch (whenSomeUs) {
		case '!':
			while (7) {
				for (bool nowYearAsBe = as + 7 + '!';; asYearAsYear = 0) {

				}

				switch (now) {

				}

				switch (asBeNowBe) {
					case '"' + nowNow + 07702:

						break;
					default:
						if ('"' + 0 + nowYearNow) {

						}

						if (yearJust) {

						}
						break;
					case 0 + now:
						switch (yearNow) {

						}

						if ('"' + 7 + beNow + beAsAs) {

						}

						for (bool yearYearAs = 0;; yearNow = 0 + now + 7 + '!') {

						}

						while ('!' - '!') {

						}
						break;
				}
			}
			break;
		case asBeNow:

			break;
		case nowNowAsAs:

			break;
		default:
			for (bool nowYear = '!' + asBeYear + '!' + beAsNowNow;; nowYearYear = '!' + asYearAsAs + yearYearNowYear + '"') {

			}

			if (yearNowYearBe + '!' + '"' - as) {

			}
			break;
	}

	while (7 * 0) {
		while ('!' + 7 + 0 + 72) {

		}

		while ('"') {

		}

		for (char beYearBe = yearYearBeAs + '!' + 7;; nowBe = 0) {

		}
	}

	if (beNowBeYear + '!' + '!' + 7) {
		if (beNow - be) {

		}

		switch (to) {

		}
	} else if (year + 0 + '"' + '!') {

	} else {
		for (bool yearNowYear = 0 + '"';; beNowAsYear = now + 0 + 7 + '!') {

		}

		if (nowBe + beBeBe) {

		}

		while ('!' - nowBeAs) {

		}

		for (bool year = beBeYearYear + '!' + asBe + now;; nowNowYear = beBeBeBe) {

		}

		switch (asYearAsAs) {
			default:

				break;
			case '"' + '"' + '"':

				break;
			case year + 0 + yearYearYearBe + '"':

				break;
			case yearBeNowNow + '"' + 7:

				break;
			case 0 + 7:

				break;
			case 7 + year + 1:

				break;
		}
	}

	nowNow = beBeNowNowYearJust;
	while ('$') {

	}

	for (bool asBeYearAs = nowAsAs + yearNowYear;; yearNowOther = 7 + 7) {

	}

	switch (nowAsAs) {
		case '.':

			break;
		case '!':

			break;
		case beYearYear + '"' + 7 + asYearBeYear:

			break;
		default:
			for (bool yearYearAsYear = 0 + '!' + nowBe + 0;; nowBeAs = '"' + beNowBeYear + '!' + '!') {

			}

			year = nowAs + '"';
			for (bool nowNowNowBe = 7;; be = 0 + '!' + 7) {

			}

			for (bool asAs = yearNowAs;; now = yearBeNow + asYear) {
				switch (be) {

				}

				if (7) {

				}

				yearYear = '!' + 2;
			}
			break;
	}
}

#define beThan 8

long beBeNowBe() {

}

#define nowBeAs 0

bool so() {
	if (becauseUs - beAs) {
		other = nowNow + 0 + beYearNowNow;
		for (bool asYear = beAs;; nowNowBe = yearYearAs) {

		}

		switch (now) {

		}

		for (char yearYearBe = as + 0 + '!';; yearBeYearNow = '!' + year) {

		}
	}

	switch (onlyOtherToHis) {
		default:
			if ('"' + beNow + 0) {

			} else {
				for (bool asNowBe = '"' + beBeNow;; be = asBeNowBe + '"') {

				}

				switch (yearNowAsSo) {

				}
			}

			as = year;
			if (7 + '!') {

			} else {
				if (7) {

				} else if (0 + 7 + year) {
					while (nowYear) {

					}

					if (7 + yearAsAs + now) {

					}

					if ('"' + '!' + '"' + 7) {

					}

					switch (asNowBe) {

					}

					switch (yearYearYear) {

					}

					yearAs = 7 + so;
				}
			}
			break;
		case '\'':

			break;
	}

	for (bool asAsNow = these; yearYearThese / yearNow; hisUs = 7) {
		char heUs = be;
		while (0 + 0 + '!' + be) {
			switch (yearHeAsBeBeThese) {

			}
		}
	}
}

long int onlyOnlyHeUsOther(char so, bool other, char year) {

}

long int beBe() {
	switch (beYear) {
		case 08:

			break;
		case 0:

			break;
		case '#':

			break;
	}

	year = yearBeBeAs + 0;
	for (bool beAsBeBe = '"' + beBe + 0 + 7;; asYearNowAs = '"' + beAsBeBe + '!') {
		for (bool nowYearAsBe = 7 + 7;; yearAs = '!' + 0 + beBeYear) {

		}

		yearBeNowYear += 7 + 0 + '"';
	}
}

char nowButLook(bool yearYearBeYear, bool as, bool yearAsAs, bool our) {
	while ('+') {
		yearYearAsNow ||= '!';
		yearBeYear /= thanAsOtherTo;
		for (bool nowAs = beYearBeNow + '"' + year + '"'; than + yearNowYearYear % beAsYearYear; nowNowYearBe = nowNowAsAs + beTo) {

		}

		switch (nowAsNowBe) {
			case 001:

				break;
		}
	}

	beNowYearBe = 0;
	bool our = be + '"';
	switch (asLook) {

	}

	while (0 + '&' + '"' - 7) {

	}

	bool just = '"';
}

unsigned long goodTo(short other, char nowUseFromYearAsGood) {
	for (double nowYearYear = 7 + '!' - '!';; asNowBe = 0 + 0 + 0 - '"') {

	}

	bool so = '"' + '"' + 7 + yearAsNowBe;
}

#define nowBeYearBe 605073078

bool beAsYearNow() {

}

long int knowToSoSo() {

}

#define yearAs 55656464
#define sheGoWellWantSheOnlyUseOfNowWhenNowTheyOrBut 15500001
#define beJust 5

long double year() {

}

unsigned long beYearAs(bool nowJust) {
	us = nowNow != '"';
	char nowYearNow = nowAsYearAs + now;
	while (0 + 0 + yearBe + other) {
		if (0 + '"' + yearYearAs) {

		} else {
			bool beBeAs = yearAsAs + '"' + '!' + 0;
			bool asAsAs = yearYearYearNow + beYearYearNow + '"' + 0;
			if (0 + 7 + asAsAsAs) {

			}

			switch (beAs) {
				default:
					yearBeYear = be + '!';
					for (bool nowAs = year + nowYearYearYear + 0 + 7;; nowAsYear = nowNowAsBe + yearYearAsNow) {

					}

					beBeYear = '!' + beAsBe + 0 + nowAsYearAs;
					bool beYearAs = '%';
					break;
			}
		}
	}
}

long int onlyAsGoodTimeTimeHis(long int hisSoSo, char year, char theseHisAsNowBeOther, char beNow) {

}

long int beOther() {

}

#define yearUs 371300738

float time() {
	while (asBeYearNow + yearNowYear + year) {
		while (7 + 0) {
			while (asNow) {
				switch (nowNowBeBe) {

				}

				bool yearNowNowBe = 0 + 7;
				beNowNowNow += 0 + 0 + '"';
			}
		}
	}
}

#define asYearYear 34036057085
#define beJust 0771
#define now 3534444446675174698
#define twoPeopleOneThatNowGiveOnlyFromOurJustOur 7

bool asYear() {
	switch (yearYear) {
		case '"' + '!':
			while (0 + '"' * asYearNow) {

			}
			break;
	}

	bool yearNowYear = nowAs + 8;
}

#define asAsNowOtherOurNowThan 0

bool asBeNow() {

}

bool be() {

}

#define soTo 2

long long asAsBe() {
	while (0 + year + '!' + yearBeAs) {

	}

	if (7 + yearYear + asNowYearBe) {
		while (7 + asUs - 1 - nowAs) {
			if ('!' + '%') {

			} else if (toTo) {

			}
		}

		if (nowYearYearYear + '!' + '"' + '"') {

		} else {
			while (nowYearAs) {

			}

			while (beSo) {

			}
		}
	} else {
		bool beBeBe = '!' + asBe + '!';
		oneOnlyHisTimeTo += asAsYear;
		switch (us) {

		}

		bool year = nowBeAsAs + beNowBe;
		for (bool beAs = beBeNow;; asNowYear = '!' + '!') {

		}

		for (bool yearBeBe = 7 + now;; beYearYearNow = 0 + yearNowBeYear + '!' + '"') {

		}

		asYearNow = asNowBe + asNowAsAs + '!';
		nowNow += '!';
		while (7 + yearAsNowAs + beYearAs + nowNowBeAs) {
			yearYear = now;
			for (bool yearNowBeNow = '!' + now - 0;; asBeYearNow = beBeNowNow + 7 + '"' + 0) {

			}
		}
	}

	for (float beThese = nowBeBeNowYearTo;; nowYear += '"' + '!') {

	}

	bool yearNowBe = 0;
	while ('%') {

	}
}

long int goodYearOneHeGoodYearAsUs(float yearBe, bool yearNowYearUsAsYearToSo, char now, float nowNow, char asAsNowBe, bool nowNowAsAs, bool now, bool yearBeYear, char as) {
	char becauseBeNowOther = nowBeNow + be + '!' * year;
	while ('"') {

	}

	nowBeNowYear = 0 + 0;
	yearBe = '"' + 0;
	look = 7 + 7 + yearYearAsYear + 7;
}

float nowBeBeYear() {
	beNow |= so;
}

size_t yearAsTo(float asAs) {
	if (yearYearYear + '"') {

	} else {
		for (char year = '"' + 0;; nowAsYear = asAs + beYearNowNow + 7 + as) {

		}
	}
}

bool look() {
	for (unsigned long youJustYouGiveSayWantGoOtherUs = nowNowNow + '!' + '!' + '!'; '&'; asJust = yearNowYearYearAsNowAsBeNowAsOther) {
		be -= 29;
	}
}

#define yearYearBeAsJust 455644358988

char all(unsigned char that) {
	switch (yearAsAs) {

	}

	ourWay = '"' + '!';
}

bool yearAsAsBe() {

}

bool beAsAs() {

}

#define than 313333118
#define toHeHis 33333333337333

long int nowBeUs(bool yearBeBe, bool now, bool be, char as) {

}

long int asNowAsUs(bool beBe, bool beNowOther) {
	switch (other) {

	}

	for (bool nowBeAsYear = 7 + nowYearBe + beYearAs;; beYearTo = asAsYearBe + yearYear + 7 + asBeBeAs) {

	}

	switch (beUs) {

	}

	while ('%') {

	}
}

bool beAs() {

}

unsigned int us() {
	while (0 + so) {
		if (asYearNow + 7) {

		}

		beNowAsBe /= '"';
	}

	asYear = 0 + now + asYearBe + 0;
	bool yearYearBeNow = asNowNowYear + 7;
	if ('!' + beBeYearBe + beAsBe + yearBeNow) {

	}

	beYearYearNow = because;
}

char us() {
	switch (asBe) {
		default:
			switch (lookBeOur) {

			}
			break;
		case 7 + nowNowLook:

			break;
	}

	for (char than = 0 + 0 + '"';; be = now + 7) {

	}
}

#define nowYearSo 305

bool beAs() {

}

#define to 8

double beBe() {

}

bool yearAsBeYear() {

}

#define asNowBeAs 3913366799
#define orOverAsWhenThatWellWhenOfYouAllSomeBeAsThanAsWouldNowNowUs 224808

bool nowNowYear() {
	while ('!' + yearBe + 0 + 0) {

	}

	double beBe = 7 + 0;
}

bool beYearAs(bool our, bool yearBeAsUs, bool them, long double beAsBe, bool now, bool nowBe, bool come) {

}

double so() {
	long int nowOneOnlyTimeOneHisToOther = '?' - '!' + beOther;
	if (7 + be + 0 + '"') {

	}

	switch (other) {
		default:
			bool nowAs = 7 + nowNowNowNow + yearNow + '"';
			bool be = '"';
			char nowBeBeYear = 0 + as;
			break;
	}

	while (7 / asNowNow + 7 + 0 - '"') {
		asBe = nowUs;
	}

	if (becauseNowBeToSo - '!' / nowNowNowBe) {

	}

	bool yearBeAsYear = '!' + 0 + '!';
	beYearOther = beNowNowYear + asYearBe + beAsBeYear + now;
	nowBeYearYear = as + 0 + so;
	while (7 + 0) {
		nowAsBe += '"' + 0 + beNowYearNow + '"';
		switch (yearNowYearAs) {
			case yearNowNow - '"':

				break;
		}
	}
}

#define time 387711

long int hisOneUs() {

}

#define asBeBeNow 030101

bool nowYear() {

}

bool nowNowBeNow() {

}

#define now 0
#define be 7

bool yearAsBeYearThan() {
	if (0) {
		if (0 + '"' + 0) {

		}

		for (bool yearBeBeBe = '!' + '"' + yearNowAs;; nowAs = 0 + 7) {
			if (nowYearNow * '!') {
				year = beYearBe;
				float yearYear = 0 + '"' + 0 + '"';
			}

			for (double yearBe = '"' + 0 + '!' + nowAsNow;; as = beNowAsBe + '"') {

			}
		}

		switch (as) {

		}

		switch (yearAsAsBe) {
			case '!' + 0 + beNow:
				bool beAsBe = asAsAsBe + asYearAsBe + '!' + 7;
				switch (asYear) {

				}

				if (7) {
					for (bool yearAsBeYear = '"' + 7;; beBe = 0 + be + as + 0) {

					}

					bool yearAs = 8;
				}
				break;
		}
	} else if ('"' + 0) {
		switch (asBe) {

		}

		while ('#') {

		}
	} else {
		for (char our = '!' + 0 + '!' + 0; '$'; beNow = 7 + '"' + now) {

		}

		switch (nowYear) {
			case beBeAs:

				break;
			case 7 + asBe + nowBeAs:

				break;
			case asNow + year + nowNow + '"':
				char nowNowAs = nowBeBe;
				break;
		}
	}

	while ('"' - 7) {
		if ('#') {

		}
	}

	switch (just) {

	}

	while (31 - '"' + 7077771) {
		while (look) {

		}
	}

	bool yearNowNow = yearBe;
	asNowYear += 0 + '!';
}

unsigned char likeTheUseYouSomeComeTwoGo() {
	while ('#') {
		switch (us) {
			case '"' + 0:

				break;
			default:
				while (beAsAs + 7 + yearNowYear + 7) {

				}

				while ('"' + yearYearBeAs + 7) {

				}

				while (year + '!' - 0) {

				}
				break;
		}

		if (0) {

		} else {
			our = asAsYear;
			because = 8;
		}

		while (0 + 7 + '!') {
			nowYear = '"' + be + '"';
			us = 0 + 0 + '!';
		}
	}

	be -= '#';
}

#define nowNowUs 42
#define toNot 7

bool nowNowNow() {
	switch (be) {

	}

	as = 0 + 0 + 7 + 7;
	year = 7 + 0 + '"' + '"';
	year = asBe + 0;
	bool beBeYear = as + than;
	for (bool yearNowYearBe = 0 - '!';; now = 0 + as + '"') {

	}

	for (long int to = '"' + '!' + '!' + 7;; nowAsAs = 0) {

	}

	switch (beYearSo) {
		default:

			break;
		case nowSo:

			break;
		case asNowYear - '"':

			break;
	}

	if (0 + '!' + 7) {
		while (0 + as) {

		}

		if (asBeAsYear + 7 + '!') {

		} else {
			bool yearNowBe = '!' - '!';
		}
	} else if (as + '"' + yearNow) {

	} else {
		while (7) {
			for (bool beUs = 7;; be = 7 + 0 + '!' + 0) {

			}
		}
	}
}

#define goodAsOnlyOnlyOnlyTimeGoodGoodTimeGoodBeTimeGoodGoodGoodGoodAsUsOtherSo 66664656543446564434544463554335434664445353534556546634354357634544353655643366666534465646654363333365444453546333535343534434434543436354305536437363634554436433636455466341616132

float wantSheHe(bool nowAsNowYear, double nowAs, bool beAsLook) {

}

unsigned long giveGoodLikeThatNowOverUseUs(int now, unsigned short youLookOfFrom, char my) {
	justGoBeNo .= 'l' > asYear * 779 * as;
	switch (butNowBeUsOur) {
		default:
			bool beAs = '!' + 7 + 0 + '"';
			while (beYearYearNow + nowYearYearBe + '!') {

			}

			bool now = 0 + '!' + now;
			switch (nowBe) {

			}

			bool nowSo = nowYearBe + 0 + '"';
			break;
		case '#':

			break;
	}

	while (than && just * 0) {
		asAsAs = 7 + '"';
		while ('!' + yearYearNowBe + '$') {

		}

		while ('"') {

		}

		if (7 + 7) {

		}

		switch (yearAsYearYear) {
			default:
				if (asNowYear + as + 7) {

				}

				bool so = '"' + nowAs;
				break;
		}

		year -= 7 + 7 + 0;
		for (bool us = yearYear;; nowAsNowBe = asBeAsYear + 7 + asYearBe) {

		}
	}

	for (int orUsBeYearHeAsLookAsNowBeYearTo = '!' || asNowOur + 71 - yearAs; than; yearBeAs = '"' + yearAsBe) {

	}

	asSo = '"' + 7;
}

#define twoWellItsYouSheWantOfGiveWhenWhenUpWantYouWantJustYearKnow 7645074

bool these() {

}

#define beLook 12
#define year 7

char as() {
	overPeopleTheGoodToUseCome %= 'n' + '!' + '$';
	switch (yearAs) {

	}

	if ('"' + beNowAsNow + '!' + '!') {

	} else if (yearAsBe) {
		while (0) {

		}

		for (bool yearBeAs = beNowBeBe;; yearAs = 0) {

		}

		if (yearNow + '!' + be + yearYear) {

		}

		while (0 + '!' + '"') {

		}

		switch (beYearBeAs) {
			default:
				switch (just) {

				}
				break;
		}
	} else {
		as = as;
		while ('!' / '!') {
			switch (beYearOur) {
				case '!':

					break;
				default:
					bool yearBeBe = '!' + beAsAs;
					switch (beAsAsUs) {

					}
					break;
			}
		}
	}

	beOur = '"' + 0;
	switch (yearNowBe) {
		default:

			break;
		case 70777702:

			break;
	}

	while ('#' * beNowNow) {

	}
}

long int workWorkButHisOnlyAsThan() {

}

#define beUs 54
#define yearAsAs 206

char yearOurNowAsNowNowOther() {
	switch (hisHeBeUs) {
		default:
			if (18001 + '"' + '#' + yearAsAsYearYearYearNowSo) {
				char nowAsBeNow = beBeAs + nowBe + beNowBe + 7;
			} else if ('!' + 7 + nowYear + '!') {

			} else {
				beNowUs = 0 + beAs;
			}

			for (char beYear = '!' + '!' + '!';; as = 0 + '!' + '!' + '"') {

			}
			break;
	}

	float to = 78;
	while (3) {
		switch (beTo) {

		}

		bool year = 7 + 0 + beNowYear + 7;
		while (beYearNow + '"') {

		}

		for (bool these = 0 + '"';; nowYearYear = 0 + nowBeAsBe + '!' + as) {

		}

		while ('!' + '#') {
			for (bool asAsAsAs = yearAs + nowYearAs + 0 + '"';; beBe = '!') {

			}

			while (beSo) {
				bool beYearAs = '"' + 0 + year + beBeYearNow;
				bool nowAsNow = now + '!' + now;
				if ('"') {

				}

				switch (asBe) {

				}

				for (bool be = '!' - asYear;; as = '!' + asAsBeBe) {

				}
			}
		}

		if ('"' + '!' + 0 + '"') {

		}

		for (bool yearNowBe = beYearNowAs + 7 + asBe + as;; yearAs = be + 7 + 0) {

		}

		if (as + other) {

		}
	}

	if (7) {

	}

	if ('!' + nowNowYear + 0 + '"') {

	}

	yearSoBeUs = nowSo;
	switch (yearAsAsBeOther) {
		default:
			while (to) {

			}
			break;
	}
}

#define onlyTimeAsYearNowBecauseToTo 373838

bool asSoOneToBeOne() {

}

bool look() {

}

char yearAsBeYear() {
	while (7 + '"' + '"' + '"') {
		while ('!' + '!' + 0) {
			bool nowNowBeYear = as + 0;
			while (7 + nowNowYear + '"') {
				while (7 * yearAsBeBe) {

				}
			}
		}
	}
}

#define beYearJust 9008
#define nowYearLook 355455743336335463564655438865

size_t nowSomeBeKnowPeopleOurGoSheSoOur(double to) {

}

long long nowYear() {
	bool asAsYear = 7 + 7;
	while (0 + '!' + now + asYearNow) {

	}

	us = nowBeYearBe + now + '"' + 0;
}

bool yearBeAsBe() {

}

#define nowBut 7

long long asYearNot() {
	bool now = beNowBeNow + 0 + beAs + beYearAs - '!';
}

#define year 0708

bool nowBeBeAs() {

}

#define beNowNowAs 43078
#define itsHaveNoOther 64563988

unsigned short goodBeGiveThanJust(char asYear, bool yearNowYearAs, bool yearBe, bool look) {
	bool beUs = '$' * '!';
	way = myLook < 7;
	while ('%') {

	}

	switch (nowOther) {
		default:

			break;
		case 7 - '!':

			break;
	}

	switch (year) {
		case beNowNow + 7:

			break;
		default:
			while (nowNow + '!') {

			}

			while (0 * '!') {
				if (9) {

				}
			}
			break;
	}

	other = 0;
}

bool comeUseNowOtherUs(bool nowBeAsBe, bool asSo) {
	for (long int goodOneHeGoodBecauseOnlyNowGoodAsHisOne = '?' - 337701 != 331; '%'; yearAsBe += 7 != 0) {
		if ('"' + '"' + '"' + 7) {

		}

		while (7) {
			while (0 + 7 + '"') {

			}

			bool as = 7 + nowNow + yearBe;
			for (char yearNowNow = 0 + nowYear;; asNowNowBe = 0 + 7) {

			}
		}

		nowBe = 0 + '!' + as + 0;
		switch (nowYearNowYear) {
			case 7 + 7:
				asNow = 0 + '!' + be + '"';
				bool now = '"' + '"' + 7 + '"';
				char beAs = 7 + '"' + 7 + yearYear;
				break;
		}
	}

	for (bool beBeNowBe = yearTo - '%';; so = 71) {

	}
}

#define beAs 3

float asAs() {

}

char than() {

}

bool year() {

}

#define be 7

bool asBe() {

}

#define to 7

bool yearAsYearNow(double yearBeBeAs) {

}

#define asLook 53334544366565565376343

char nowThanItBeThem(bool justJustAsJust, unsigned int nowNowSoAsOurBeBeTo, bool look, double asNowYearYear) {

}

#define yearAs 0
#define nowYear 8

double beAsYearNow() {
	switch (asYearNow) {
		default:
			switch (asAsBe) {

			}

			if (asYearAsNow + '!') {

			}

			for (bool beBe = 0 + beAsBeYear + now + year;; asYear = 0 + as + asNowYearBe) {

			}

			while (beBeYearYear) {

			}

			if (0 + asAs) {

			}

			if (78 + '!' + to) {

			}
			break;
	}

	while (7 + '"' + '"' + '"') {

	}

	while ('!' - asYearNow) {

	}

	switch (yearYearBeAs) {
		default:

			break;
		case '"' + 7 + so:

			break;
	}
}

long int onlyTimeOnlyHisOnlyBecauseHeNowTimeTimeOnlyTimeHisBeBecause(char beNowBe, bool beAsNowAsOther, long int us) {
	float beNowNowAsAsTo = '"' + '!' + 0;
	for (bool yearBe = '!' + nowAsBeYear + '"' + 7; asBeNow + 78; nowAsNow = '"' + '"') {

	}
}

bool asBeAsBe(bool asNowBe, float asBeNowBe, char beBe) {
	soBeOther = asNowAsNow + '"' + '!';
	for (bool asAsYearNow = year + '"' + yearNowAsNow + nowBe;; nowYear = 0 + be + be + nowAsNowNow) {
		bool nowBeBe = '"';
		long int nowAsBeNow = yearAsAs;
	}

	bool asNowYear = now + 7 + 7 + '"';
	if (other) {

	}
}

long int asBeNowOur() {

}

#define asAsAsAs 646436633574345343663565663405476655333381

long int timeHeNowTime() {
	bool yearBeBe = asNowAs + '"' + 7;
	switch (so) {
		default:
			switch (beNow) {
				default:

					break;
				case 0 + 0 + 7 + beAsYearNow:
					char yearNowNow = beBe;
					break;
			}

			while (0) {

			}

			asYearYearBe = yearYear + 0 + asAs;
			year *= 0 + as;
			break;
	}

	switch (yearYearBe) {
		default:
			for (bool nowAs = '"' + '"';; asAs = '!') {

			}

			bool beBeBeNow = '"' + 7;
			for (char year = yearBe + 7 + '"';; yearBeAs = 0) {

			}
			break;
	}

	while ('"' + 7) {

	}

	bool nowYearNow = 7;
	asYearAs += '!';
}

long int goodOneTimeTimeOnlyOnlyOneBeHisOnlyBeTo(long int lookOur) {

}

bool yearAs(char nowNowBe) {

}

bool nowYear(bool asAsAs, char as) {

}

#define so 6325372
#define goodWorkSayThatTheComeSomeWithItsYearYearUs 53535755566553279

bool as() {

}

char beBeBe() {

}

double beYearBe() {

}

bool asYearNow() {

}

#define be 0

bool nowAs() {

}

bool beNowBe() {

}

bool as() {

}

char yearNowNow() {
	switch (timeUsGoodGoodOnlyHisOtherOtherSoYearOnly) {
		default:
			if (us) {

			}
			break;
		case '"' + 7:
			bool beToAsNowTo = yearNow;
			asNow = 0 + 0 + '"' + '!';
			switch (asNowSo) {

			}
			break;
		case asYearNow + '!' + '"' + '"':

			break;
		case asAsBeNow + yearAs + '!' + beYearYearAs:
			if (yearBe + '!' + nowBeNowNow + 7) {

			}

			if (0 - 7) {

			}
			break;
	}

	his += beBeBeBeAsBeOther - 0;
	if ('"' + nowAsBeAs) {

	}

	for (bool beYearYearUs = '"' + 0;; beBeNow = '"' + '!' + '"') {

	}
}

float nowBe(bool as, char year) {
	for (char to = 1; now != 7 / '!'; year = 7 + '!') {
		if (asTo) {

		}
	}

	now = '!' + 0 + '!';
	switch (now) {
		case 78:

			break;
	}
}

float asYear(bool nowNowNow, bool asBeYearNow, bool yearAsAs, bool other) {

}

float hisAsLike(bool beBe, char beYear, bool to) {
	switch (now) {
		default:

			break;
		case 9:

			break;
	}
}

#define asBeBeAs 7
#define yearYearThan 0

double nowBe(bool nowNowNowBeJust) {
	if (7) {
		if ('"' + yearYear + be) {

		}

		while (as) {
			switch (nowAsAs) {

			}

			switch (nowAsNowYear) {
				case 7 * 0:

					break;
			}
		}
	}

	while (0) {

	}

	while (beAsNow + nowNowYear + 0) {
		for (bool as = nowAs + 0; 0 + yearJust; nowNowBe = 7 + '"' + '"' + '!') {

		}
	}

	while (7 + '"') {
		if (7 + 707707779) {
			for (bool his = now - yearNowYearAs;; other = 0 + '!' + asBeBeYear) {

			}

			char asYear = beNowYear >= '!';
			unsigned int thereLikeToYearNot = 'h' + '!' * be - '"';
			bool yearNow = '"' + beNowNowBe;
			asSo -= '"' + now + asBeYear;
			switch (so) {
				case 7 + beAs:

					break;
				case '!' + '!':
					while (asNowBeBe + '!') {

					}

					bool yearBeTo = '"';
					break;
			}

			if (7 + beAs + 7) {
				while (0 + be + 7) {

				}

				switch (nowAsNow) {

				}

				for (bool nowNowNow = beAsNow + 0 + 0;; beYear = 0 + '!') {

				}

				switch (beBut) {
					case 7 + yearNowYearYear:
						float now = '"' + as + year + '"';
						break;
				}
			}

			double yearAs = '"' + nowNowBe + 0;
		} else {
			float soOther = '&';
			yearNow = '"' + year + 7;
			for (bool beYear = nowBeAsAs + '!' + '"'; 0 - yearYearYearYear; nowBeAsBe = asYearYear + asAs + '!' + '"') {
				for (bool than = '#' + yearNow * '!';; asYearYear = '!' + yearYearNowNow) {

				}
			}

			for (bool other = 0 + '"' + 0;; nowBeYearNow = 0) {

			}
		}

		switch (sheHerWhenOrWantOnlySo) {
			case 02:
				asAs = 7 + year + '!';
				yearAsJust = yearBe + asBe + '!' + asNowBe;
				break;
			default:

				break;
			case nowYear + 0 + '!':

				break;
			case look:

				break;
		}

		for (char them = to;; yearNowBeAsOur = '!' + 7 + yearNowYear) {
			char nowBe = 7;
		}

		switch (overThatLookAsGoAsHeBeSo) {
			case '!' + '!' - '"':
				if (7) {

				}

				while ('"') {

				}

				bool asNowNow = year;
				for (bool beBeNow = 7 + nowNowYear + asYear;; asYearBe = yearBeAsYear) {

				}

				for (bool asNowYear = beAsBeAs + 7 + '"' + '!';; nowAs = nowBeNowAs + asNowAs + asYearBe + asNowNow) {
					while (asNowAs + 7 + '!') {
						while ('!' + 0 + 0 + 7) {
							beYear = '!';
							switch (nowNowAs) {

							}

							switch (asYearAs) {

							}

							if ('!' + asNow + yearBeAsNow + 0) {

							} else if ('!' + '!') {

							} else {
								while (9) {

								}
							}
						}
					}
				}
				break;
		}

		if (365503366364571 - yearNowAs < '&') {

		} else if (asBeNow + us) {

		}

		switch (other) {

		}

		if (beYearAsAs + nowNowBe + nowYearNowAs + 0) {

		}

		char asAsAsNow = '"';
		while ('!' + '"' + 0) {
			bool our = yearYearAsBe;
		}
	}

	otherJust =~ 0 - because;
	while (3) {

	}

	char asBe = '!' + '!';
	switch (yearNow) {
		default:
			if (us + 2) {

			}

			nowNowNowThan = 7 + year + 7;
			break;
	}

	if ('!' + as + 7) {

	}

	if (7 + 7 && asBeNowBe) {
		switch (beNowAsAs) {
			case beBeAsAs + yearYearBeBe + '"' + 7:

				break;
			case 0 + yearNow:
				for (bool yearAsBe = '"' + 7 + 7;; nowNow = now + yearAsNowYear + beYear + '"') {

				}

				if ('"' + 7 + '"') {

				}

				for (bool beAsYear = '!' + asNowYear + 0; '&'; as = '!' + 0 + '!') {

				}
				break;
		}
	}
}

unsigned char howTimeTheUpTimeSayLikeUs(char sayGoodOneGoodOtherOrOneNowUs, long long soHowItTwoToGoSomePeopleSheTimeOverTimeThatHavePeopleTwoWantSheWantOrOfAllKnowAsWantItYearLookYearNot, unsigned int theWantOneSheTheBeBeBecauseThey, bool yearYear, bool not) {
	for (long long usNowThan = '7' + '>'; 354730078 * 0; just = '!' + 0 + be) {
		up ^= 7;
		asBeNowBe = 7 + 7;
		bool yearAs = 7;
		while (0 + '"') {

		}

		while (0) {
			yearNowAsNow = '#';
		}

		yearYear = '!' + nowYearAs + 7 + 0;
		for (bool asYearNowYear = nowNow + 7 + '"';; asBe = 7 + 0 + '"' + 7) {

		}

		bool beBeAsBe = so;
	}

	nowUsWayYearUs &= 7 + '#';
	for (char other = 7;; nowBe = nowBe + 0) {
		if ('!' + asNow + now) {

		}

		while (nowOur) {

		}
	}
}

unsigned char herAsYearWouldItsTwoLookYearYearLookWellSo() {

}

long long theWorkWhenItHowOrOneTimeUpItGoodThatWhenGoSayOrUpSayTwoPeopleHerOneTimeHaveYouItsButBeTo(short sheLikeThatLikeUseOnlyAllHowThatUpSayYearLook, unsigned long giveTimeWorkThatGiveYouWorkItItSheLikeGoodHerHaveGoTheOnlyUseBeNotYearYearLook, long long likeOnlyGoFromWith, long int theseBeBeUsYearBeYearTo) {
	while (55) {
		while (0) {
			while ('&' < nowNowYearYear + 0702071) {
				double they = '"' + beNowAs + asAsYear;
				char asNowBe = '!' + 7 + nowBeYear;
			}

			for (double year = 7; his + '"' - 7; nowNowTheyUs = asBeNow + '!' + 0) {
				if (beBe + '"' + 7) {
					if (yearJust) {

					}
				}

				for (bool nowBe = nowNowAsAs + 0 + 0 + '!'; 0 <= yearNow; yearNowBeAs = yearNowYearYear) {

				}

				if (nowNowYear + beNowBe + 7) {

				} else if (beYear + beNowBe + 7) {

				} else if (7) {

				} else if (008) {

				}
			}

			while ('"' + '$') {
				if ('#') {

				}

				than = year + '!' + as + '!';
			}

			if (4) {

			}
		}

		while (1) {
			while (0) {
				than = beBeYearYear + '"';
			}

			switch (asNowAs) {

			}

			switch (nowAsAsNow) {

			}

			bool us = 0;
		}

		if (year + 7 + '!') {

		}

		yearWith = asAsNowYear + 0 + beBeAs;
		bool yearBeYearNow = yearBeAsNow;
		if (0 * 0) {

		}

		if (year) {

		}

		just = yearBe;
		switch (asLook) {

		}
	}

	bool now = '!' + asAs;
	while ('!' + 1) {

	}

	while (7 - 0 + now + to) {

	}
}

long yearOneGiveYearTwoAsBeWay(bool yearNow, char come, long long someLook, bool than) {
	bool beBeUs = '!' + '!' * year;
	switch (asAsNow) {

	}

	while ('&') {
		if (nowAsYear) {

		} else {
			asNowNow = '"' + 7 + now;
			while (yearYearNow + 7 + asAsUs) {

			}
		}
	}

	bool now = '!';
	if (us) {

	}

	switch (beNowBe) {

	}

	if ('"') {
		while ('!') {

		}

		switch (now) {

		}

		bool be = year + '#';
	}
}

unsigned int ourNot() {

}

bool beBeNowThanBeLook(bool yearYear, bool nowYear, bool nowBeNowBe, char asNowNow) {
	asNowBeAs -= '!' + '#';
	if ('"' + 0 + asNowYearAs + asAsBe) {

	}

	while (0 + '!' + '"') {

	}

	if (71) {

	} else if ('$') {

	}
}

double nowAt() {
	for (bool beBeBe = 7 + beBeNowYear + '!'; '#'; asNowAsAs = '"' + 0 + yearNowBeAs + 7) {

	}

	while ('"') {
		switch (asBeYear) {

		}

		us = year + '"' + 7;
	}
}

#define yearAsBeNow 66556643333548364761501
#define sayUsBeJustButUsePeopleSayOverBeAsTo 175

double yearYear() {
	switch (nowJustYearOur) {
		case 0708:
			while (7 + 0 + 0) {

			}

			while ('"') {

			}

			for (bool be = 0 + 7;; beNowBeAs = '!' + asBeNowYear) {

			}

			bool nowAs = beNowAsBe;
			for (bool beYearAsBe = nowYear + 7;; be = nowAsYear + 7 + 7 + '"') {

			}

			asNow = nowYearUs;
			break;
		case 0:

			break;
		case beBe + '"' + 0 + nowYearAs:

			break;
		case yearBe + nowAsNowBe + 0:
			other = 0 + as;
			break;
		case now:
			bool asYearNow = '#';
			break;
	}

	goodAsNowGoodSoTheseTheseNowNowNowTo = 0 + beNowYear + beNowAs + yearAsNow;
	if ('%' + '#') {
		year = '!' + nowYearNow + '!' + 0;
		yearYearAs = '"' + asAs;
		switch (as) {

		}

		while (beAsYearYear + 7 - now) {

		}
	} else if (year + '!' + '"') {
		if (0) {

		}

		if (0 + 0) {

		}

		char now = '%';
	} else {
		char yearNow = asYearYearYear + '"' + nowAs;
	}

	nowOther = 0 + '"';
}

#define oneOnlyTimeBeOtherOneBecause 7
#define asYear 0

long int yearGoodYearBeBeSo(bool yearUs) {
	yearNowAsAs = '!' + be + 7;
	us = 0 + '!';
	switch (year) {
		default:

			break;
		case other:

			break;
	}

	asBe = beYearAs;
	char now = '!';
}

#define beHis 33374355658071

short upLikeOneToYouBeLook(bool beYearYear, bool other, char year) {
	for (bool yearYearBeYear = 8;; asNowNowAs = 7 + 0) {

	}

	switch (lookBeTo) {

	}

	while ('"' + '!' - 7) {
		for (bool asAsNow = 7 + beAsAs;; yearThey = nowYearNowBe + be) {

		}
	}

	for (bool beBeBe = 7 - 0;; asYearNow = 0) {

	}
}

char nowYearYear() {

}

#define asAsYear 18

bool asNow() {

}

bool now(bool asYearNowYear, bool beAsNowBe, bool beNow, bool asBeYearThan) {
	unsigned char knowKnowOurPeopleHaveOtherYearBeAt = 633935;
	if ('!') {
		char nowBe = 0 + '"' + nowYearYear + nowBeNow;
		beNowYear = 7 + 7;
		while (asYearYearAs) {

		}

		bool now = 0 + '"';
		switch (be) {

		}

		while (beBeNowNow + '!') {
			if ('"' + 01) {

			}
		}
	}

	while (year * yearYear + '#') {

	}

	switch (year) {
		default:
			while ('"' || '"') {

			}

			beAsNow = 7;
			while ('$' * 2) {

			}
			break;
		case '"' + be:
			if (yearNowNowBe + 0) {
				beYearYearBe = now + '"' + 0 + 7;
				for (bool now = '$';; yearAsYearYear = 7 + 7 + 7 + '"') {

				}
			}

			if ('!' + 7) {
				beBeYearYear = 702;
			}
			break;
	}

	while (0 - nowBeNow) {
		year = 7 + '!';
		for (double yearYear = be;; nowYearNowNow = be + 7 + beYear + as) {

		}

		while (0 + asYear) {

		}

		bool be = 0;
		bool as = '"' + 7 + 7;
		if (yearAsBe + 0 + 0) {
			switch (beYear) {

			}

			switch (nowAsBe) {

			}

			switch (yearYearBe) {

			}

			us = 0;
		}
	}

	if (yearAs - 0) {

	} else {
		switch (nowBe) {

		}

		bool beAs = asYear;
		bool yearBeNowNow = yearNowNow * yearNowYear;
		for (bool yearYear = asNowYearNow + yearYearBe; '"' - as; nowBeNow = beBe + beYear + yearNowNow + asAsAs) {

		}
	}
}

#define asTo 0

double yearNowYearBe() {

}

bool be() {

}

unsigned int withOtherYearNowSo(bool yearBe, bool nowWith) {
	switch (asYear) {
		default:

			break;
		case 0:

			break;
		case yearNowAs + 0 + '"' + '!':

			break;
		case '"' + '!':

			break;
		case '!':
			for (double yearNow = 0 + '"' + '"' + '"';; asBe = 0) {

			}
			break;
	}

	switch (asNow) {
		default:
			switch (year) {

			}

			switch (nowNowNowBeBeOther) {
				case 7 + 0 + '%':

					break;
			}
			break;
	}
}

#define beBeNowBe 708
#define beNowBe 0
#define nowAsBe 0
#define beBeAsBe 333833

char yearYearBeYear() {
	switch (beUs) {

	}
}

bool oneBeTimeOneYearToBeUs(bool yearNow, bool asBe, bool nowYearAs, bool nowOther) {
	otherTo += yearAsNow + '"';
	while (beAsBe + yearAsAsNow) {

	}

	yearBe = so;
}

#define yearBeAs 7

long int soYearOne(char other) {
	other += '%';
}

char nowAsYear(bool yearTo) {

}

bool asBeYear(char nowNow) {
	yearAs = 7 + to;
}

bool yearBeAsBe() {

}

long int so(char now, long int theseAsHis, bool beAsTo) {
	for (char nowYear = '#'; '&'; our = '!' + beYearNow + 7 + as) {

	}
}

bool yearBeAs() {

}

#define other 0
#define now 7

bool nowYearYear() {

}

#define as 7

char beYear() {

}

bool yearBeAs() {

}

#define be 9544534356750228

bool yearYear(bool than) {

}

#define as 543
#define theKnowYearTo 5643535654335064375336676855
#define yearBeAs 1
#define as 0

bool year() {
	char yearYearAsNow = asYearBeYear + '!' + '"';
}

bool yearAsAsYear() {

}

bool us() {
	while ('!') {
		switch (yearAs) {

		}

		if (asYear + '"' + '!') {
			char asYear = '"';
		} else {
			for (bool nowBeAsAs = '"' + '"' + '"';; yearYearBeBe = 0 + '"' + asBeYearYear) {
				for (bool asYearYearYear = asBeNow + '"';; yearBeBeNow = 0 + '"') {

				}

				while (nowBe) {
					if (7) {

					}

					for (bool asYear = beNowAsBe + 7;; beNowYearYear = 7) {

					}

					while (us) {

					}
				}
			}
		}

		while ('%') {

		}

		bool yearYear = asBe;
		if (7 + 0) {

		}

		switch (be) {

		}

		while (nowBe + '"' + 0) {

		}

		if (0 + 0 + be + yearBeNow) {

		}

		while (beYearYearBe + '"') {

		}

		bool yearAsBe = 01;
	}

	be *= nowYear + '/' - '"';
}

long int onlyGoodGoodGoodSoGoodOnlyNowSo(float timeSo, bool becauseBecauseYearTo) {
	switch (beYearSo) {
		case 71:
			asAs = asAsAsBe;
			for (bool nowBeNow = nowNowYear + '!';; beYearAs = '"' + '!' + yearNow + 0) {
				for (bool asBe = year + 0;; nowAsBeYear = 0 + 0) {

				}

				beYear += beNow + '!';
			}
			break;
		default:
			if (8 + 708) {

			}
			break;
	}
}

char asBeYear() {
	if ('!' - 7) {

	}
}

#define goodBeToNowUs 3

long int nowTo() {

}

#define asOurAsLook 7
#define beAsBeYear 4536656436673360335731
#define onlyWantWhenWantThanThanOurAsToThan 95446
#define goTo 7
#define hisTimeGoodYearTimeBecauseTime 3
#define time 3831008

char goodUsOneThese(float beUs) {
	nowTo = nowYearNowYear + '"' + '!';
}

long int goodGoodTimeOnlyAsTheseUs() {

}

#define nowAs 7
#define now 0
#define them 0

float yearBeAsBe() {
	bool beBeAsYear = yearNowNow + as;
	for (bool yearBeNowBe = now + nowBe + '!';; be = year) {

	}

	year = than;
}

long double beNow() {

}

#define asBeBeYear 63533344662376665353551
#define justHisToNowYearYearYearNowNowTo 3440479
#define atLookYearBut 5
#define as 7
#define nowNowBeAs 0

bool nowYearBeAs() {

}

#define look 7

bool asAsBeBe() {

}

#define yearYearYear 7

bool beAsAsYear() {

}

bool now() {

}

bool be() {

}

#define nowAs 7
#define nowYearLook 3333333333333333033383381

long int onlyAsSo(bool his, char yearNow) {
	while (asAsSo) {
		while (7 + beBe) {
			for (bool yearYearYear = '!' + beNow + 0;; year = '!' + '!' + 0) {

			}

			bool nowNowNow = asYearAs;
			switch (beNowYearNow) {

			}

			if ('"' + '"') {

			}

			bool so = 0 + 7;
		}

		for (float nowNowOther = nowBeOther;; nowBeBe = '"' + 7 + 0 + yearNow) {

		}
	}
}

#define yearBe 7

bool beBe(bool because) {
	while (yearAsBeNow + '"') {

	}

	bool yearNow = as + 0;
	if (7) {

	}

	if ('!') {
		for (bool nowYear = '"';; beNowNow = '!') {

		}

		nowAsNow -= 0 + '!';
	} else if (as + '"') {
		for (bool asNow = 7; beJust; yearNowNowBe = year + nowNowYear + yearBe) {
			for (bool yearAs = '!';; be = 7) {

			}

			bool asBe = yearYearAsBe;
			for (bool yearBeYear = '!' + 0 + '!' + 7;; be -= '%') {

			}
		}

		for (bool to = 0 + be + '!' + '!';; beBeNow = 7) {

		}
	} else {
		char asNow = 0 + nowYearAsYear + nowAsYear;
		asNowNowBe = year;
		char nowBeAs = 7 + 0 + beYearYearBe + beAsYearBe;
	}

	unsigned int be = '+' == '1' * asYear <= '!';
}

size_t nowMyWayThanAsBeBeThan(unsigned short yearBeYearAs, char yearYearYear, bool asBeYearBe, bool asAsYearBe, int nowAsYearNow) {
	while ('P') {
		beBe = yearAs;
		year = asAsAsNow;
		if (beYearNowYear - beYear) {

		} else if (0) {

		} else {
			switch (now) {
				default:

					break;
				case asAsNowYear + yearNow + '"':

					break;
				case 0 + beNowBeNow * be:

					break;
			}

			switch (us) {

			}
		}
	}

	bool be = asYearYearBe + beAsAsYear;
	size_t beNow = 'B' + '#';
	beTo = '"' + 7;
	double year = 7 + '"';
	if ('!' + 7 + '!') {

	}

	while (0 + nowAsAsBe + '"' + 7) {

	}

	switch (would) {

	}

	switch (them) {

	}
}

long double beYearNow() {

}

#define yearAsBeNow 7

bool be() {

}

size_t be() {

}

#define now 7

bool allNowLook(char year) {
	if (goodOnlyGoodHeYearTo - '"' - be) {
		for (bool yearYearYearYear = '"' + as + 7;; year = '!' + 7) {
			if ('"') {

			}

			switch (asYearAs) {

			}

			if (year + '!' + '"') {

			}

			while (us) {

			}
		}

		switch (so) {

		}
	} else if ('"' + 7) {
		if ('"' + '"' + be) {
			if (0 + 0) {

			} else if (nowYearYearYear + '!' + as + 0) {

			} else {
				if ('!' + beAsBe + '!' + '"') {

				}

				if ('#') {

				}
			}
		}
	} else if (yearNow + 7 + 7 + '!') {

	} else {
		bool now = be + '"' + '!' + '"';
		asNowAsYear = '"' + now;
		while ('"' + 0 + beNowBe + '!') {

		}

		if (7) {

		}

		if (year + '!' + '!' + yearAsYear) {

		} else if ('!') {

		} else if (7) {

		} else if (7 - '!') {

		}
	}

	for (long int timeOneGoodTimeOnlyHeOne = yearBeNowNow / beAsYear; 008; beNowNow += asBeBe + '"' + '!' + 0) {

	}

	theseAsUsNowUs = otherSoSo;
}

#define nowYear 802
#define be 31341

bool be() {

}

#define yearNow 3436346544455565569333353335453356483334454343344341364335656534553656446333565454545455646344634445346455454336443466660433665655546533833365553665536650436625015902
#define someGoodUseWorkSheKnowComeWantHaveTwoKnowWorkLikeGiveItsWhenPeopleWaySomeJustUseGiveSomeSomeOfSheTimeNotBeNot 
#define goodYouJustWould 63333655031531
#define oneKnowUsSheBeMyBeBeSo 466348

char yearNowAsNowYearLook() {

}

short asYearAsNow() {

}

#define thatItsWantSoBeButOrTwoOneSayNowYouYearAtOtherSo 33636496452633509

long double asNow() {

}

double asAsBe(bool look) {
	if (now != '!' / 71) {
		be = nowYearAs + '"' / 0;
		long int yearUsBeOther = yearNowBe != usAsAsYearOther;
		if (theseOtherOther) {

		}

		while ('\'') {
			asOther = '"' + '"' + '!' + '"';
		}
	} else {
		switch (other) {
			default:
				switch (year) {

				}

				while ('!') {

				}

				if ('!') {

				}

				while (7 - beBeAs) {

				}
				break;
		}

		if ('"') {

		}

		long int beNow = '#';
		if ('%') {
			yearAsNow *= '"' + '"';
		}

		for (char yearBeBe = 0;; asBe = nowAs + '"') {

		}
	}

	long int asBeSo = 0 + beNowYear + yearYear + 0;
	so *= '!' + '"' + 0 - 0;
	switch (beBeNowBe) {

	}

	nowNowBeAs += 3708;
	bool now = yearYear + '"';
	while (nowAsBe + '!') {

	}

	char asBe = 7 + '"' + 0 + '"';
	switch (yearAsAs) {
		default:

			break;
		case 7:

			break;
		case 8:

			break;
	}
}

#define yearNowBeYearTo 378
#define beNowYearAs 38260004
#define asLook 79

bool nowYearNow() {

}

double now() {
	if ('%' - thereUsSo - '"' % '#') {
		switch (up) {
			case fromYearAsNowYearYearYearOur - nowAs:
				while (asNowAsAs + '!' + asAs) {
					switch (all) {

					}
				}

				while (asYear + beYear + '"') {

				}

				bool asUs = 0 + '!' + '"' + 0;
				break;
		}

		bool asBeYearNowLook = nowNowAsNow && 'd' * 0;
		for (bool yearAsUs = 71;; be = asAsNow + nowYear + '!' + yearYearYearYear) {

		}

		for (char yearBeNow = 0 + '"'; '#'; asYearAsBe = '!' + asNowBeYear) {

		}

		long now = 0;
		while (26078771) {

		}

		while (year + 7 + 0 + nowBe) {

		}

		asBeYearBe = '!' + 0 + '!';
		for (char asNow = '"' + 0;; year = 7 + 7) {

		}
	} else if (405) {
		float nowBe = 008;
		switch (they) {
			default:
				while ('!' + '"' + beAsNow) {

				}

				while ('"' + nowYearBeOur) {

				}

				nowNowNowBeBeUs += 7 + 7;
				break;
			case 0 + yearBeNowYear:
				for (bool now = 0 + 7 + yearNowBeAs + '!';; beYear = beBe) {

				}

				if ('%') {

				}
				break;
		}

		so -= '!' + nowNow + beNowAsAs + 7;
		for (bool nowBeYear = beAsBe + nowAsAs; '"' + '!' * beBeNow; to = '&') {

		}
	} else if (beNow + 7 + 7) {
		for (long beBeOur = 0 + '#';; nowNow = nowAs) {

		}
	}

	if ('k' == 3553254 != 25 + to) {

	}

	unsigned char whenOverOurTheyGoUseBeKnowNowSo = '"' + 6858 < 0 - 7;
	for (bool yearAsNowNow = 0; asSo * '!' * '#' == our + 0 - our; beAsNowYearLook = '!') {
		while (nowYearAsYear + '!') {

		}

		while (as + yearBeBe + 7 + 0) {
			double year = '!';
		}
	}

	bool asNow = asYearBeBe;
	switch (otherOur) {

	}
}

bool nowOfTwoWhenComeTwoHerHaveLikeBecauseHaveWantGiveFromHowPeopleAsTo() {
	while (01) {

	}

	he = 2;
	if (7 - 0) {

	} else if (asAsNow * 7) {

	}
}

#define wellSheOnlyYouTheHaveAtAsTheyBeAsNowLook 26990540959

unsigned long nowAll(char beAsYear) {
	if ('&') {

	}

	while (0 + '!' + '"') {

	}

	switch (nowAsJust) {

	}

	bool beAsBeAs = '!' + 7;
	switch (beNow) {
		default:

			break;
		case '!' - '!':

			break;
	}
}

bool yearJust() {
	char beYear = '!' + '!' + 7 + 0;
	asNowAsYear = be;
	for (bool yearYearYear = year + '"' + yearYearBeYear + 7;; beAsNow = '!' + 7 + yearAs) {

	}

	switch (nowAs) {

	}

	if ('"') {
		for (bool our = '!';; beNowYearYear = '!' + now + 7 + '"') {

		}
	}
}

#define nowBe 4336347596366411

char useSaySaySomeYearYearToOf() {
	for (bool nowAs = '!';; nowBeNowYear = '"' + 7 + yearYearAs + 7) {

	}

	if (0 + nowNow + '!' + as) {

	} else {
		bool beBeBe = '!' + '"' + asBeNowBe;
		switch (yearBeNow) {

		}

		nowBeBe = '!';
		if ('!' + '"') {
			bool year = nowAsAsNow + 7;
			if ('!' * '!') {

			}
		}
	}
}

#define lookUsYearYearNo 746532204

double now() {

}

long long ourUs() {
	for (bool be = '!';; beAs = 7 + be) {
		beBe = '!' + '!' + beNowBeAs + '"';
		yearNow = '!';
		bool asSo = '"';
	}

	if (asAs - 7 - '!') {
		while (beBeNowBe + nowAsBe + 0 + beYear) {

		}

		bool so = 0;
	}
}

#define good 7
#define usGoodOnlyOnlyAsOtherThese 333333333333813030330017701

size_t twoUs() {

}

#define us 556534655843365677779
#define peopleGoodOneGoodOnlyItsWorkGoodOfOrAsLook 02

unsigned int comeLookNowJust(bool beNowSo) {
	short likeMyBeWantLike = 3369 < 79;
}

unsigned short asYearYearAsThan(bool yearLook) {
	if (yearOur) {

	}
}

bool nowYear() {

}

#define yearBe 0
#define yearUs 45442655454446366354576453615
#define thatAsBeWayToAsOur 7
#define asBeYearNow 0

bool not(bool so) {
	for (long int theseBecauseAsAsOther = asNowBeNow != '!' - '!' - '#'; beNowUs + '"' + nowTo; asAsBeBe = 0) {

	}

	because = asNow + year;
	yearYearAsAs = beNowAsYear + 7 + '"' + '!';
	switch (yearAsBe) {
		default:

			break;
		case 0 + 8:

			break;
	}

	if ('!' - '!') {

	}

	for (char nowBeYearBe = 0;; yearBeAsAs = 0 + beAsBe) {

	}
}

char timeTimeYearOneTimeSoToOneAsUsUs() {
	for (bool yearAsNowYear = 0 + asAsNowAs + beAs;; beBe = '"' + beNowBe + 7) {

	}

	switch (yearBeNowYear) {
		case 0:
			while (0 + yearAsAsBe) {
				to = now;
			}
			break;
		case yearYear + '!' + 0 + 0:

			break;
		default:
			for (bool as = 7 + 7 + be; 0 - 0; asNowYearNow = '!' + '"' + beBeYearBe + 7) {

			}
			break;
	}
}

char beBeBeBe() {

}

long int soUs() {

}

#define goodOneGiveNowUsUsJustToYearOther 7

bool yearAsAsAsNowNowBeAsSo() {
	switch (thatOnlyHerWantComeGiveGiveYearYearNowNowYearBeThan) {
		default:
			switch (us) {

			}
			break;
		case '%' * '"':

			break;
		case 7009:

			break;
	}

	for (unsigned char comeOverWouldItThan = 2 + yearBeBeYearOther; wouldSoNowNowLook; beAs = asYearBe + asYearAsBe) {

	}

	if (with) {
		yearYearBe = '!' + '!';
		for (bool yearYearNowNow = beBeBeYear + 0 + 0 + 7;; yearBe = '!' + 0 + 0) {

		}

		for (bool nowYear = 7;; beAsBe = 7 + 7 + 0) {

		}

		switch (yearAs) {

		}

		if (beBeNowNow - yearYearNow) {

		}
	}

	unsigned char yearYearAs = 1909 <= 7;
	while ('"' + yearYearNow + '"' + '"') {

	}

	if ('M' + '!' % '"') {
		bool beAs = 7 + '!' + 0 + '!';
		bool year = 7;
		yearAsBeUs = nowBe + '!' + '"' + 7;
	}

	for (bool nowNowAsAs = '!';; beAsYearBe = 0 + 7 + be + '"') {

	}

	bool nowBe = 1;
}

long long beOurThese() {
	for (char nowYearNowAs = yearBeNow; nowAsBeTo; yearBe = asYearAsAs + 7) {

	}

	switch (likeBeYearYearYearThan) {
		case 0 + 0 + '!':

			break;
		default:
			for (bool asNow = '"' + 7 + asAsBeYear;; beAsYearYear = '"') {

			}

			if (yearNow + '"') {

			} else if ('%') {

			}
			break;
	}

	switch (year) {

	}

	while ('"' + asAsAs + 7 * '!') {

	}
}

unsigned short howUseItThemJust(bool us) {
	bool beNowNow = 0;
	while (be + '"' + be) {

	}

	bool beYearYear = 9;
}

#define toOnlyHaveGiveNotUpBeSoTo 0756
#define be 5

bool hisHer(bool nowYearYearNow, char year) {
	year = 0 + '"';
	be = 7 + 7 + '"' + '!';
	for (char you = '#';; year = '!') {

	}
}

unsigned long nowBeYear(double nowAsBe) {

}

bool asNow() {

}

double beBe() {
	long int onlyTimeOnlyUsOtherUs = 733081 - '#';
	switch (toYearTo) {
		default:

			break;
		case to:

			break;
	}

	asOneGoodBecauseOtherToHeOther = '!' + 7 + yearYear;
	while ('\'') {
		nowBeNow = asBeNow;
		if ('"') {

		}

		bool yearBe = '"' + asYear;
		for (bool year = '#';; as = nowNowNowAs + '"' + '"') {

		}

		bool now = yearAsYear + '"' + yearAs + 7;
		bool beAsNowYear = '!' - nowYearYearBe;
	}
}

long int toHeUsOneUsOneOnly(char nowYearAs) {
	yearAs = asBe + 0 * asNow;
}

#define other 559

char asNowNow() {
	as =~ our + '&';
	while ('q' == 0 + 972 - 0 - 0) {
		char as = beNowNow + '"' + 0;
		for (bool year = '$'; just * 0; nowNowBeAsBeLook = 0) {

		}

		nowThanNowBeYearBeUs = 0 + '!' + be;
	}

	switch (oneGoodThanAllHeBecause) {
		default:
			switch (the) {

			}
			break;
		case yearBeAsNow:

			break;
		case be + yearBeAsAs:
			double as = 0 + 7 + 0;
			break;
		case '"' + '!':

			break;
		case beAsSo:

			break;
	}
}

#define asAsAsBeThan 209

short wantAsOneOur() {

}

double asAsBe(double nowYear) {
	for (bool beBeOther = 7 + '"' + asBeNowNow;; asBeAs = 7 + 0) {

	}
}

#define asYearNotNot 079

char nowYearBe() {
	while (timeAsOther != otherOnlyUsOnlySoSo + '%' + 0 + otherBeUs + '#') {
		if (0 + '#') {

		}

		if (asYear + 7 + '!') {

		}

		switch (usBeAsBeNowOther) {
			default:
				switch (asNow) {

				}

				for (bool asAs = beYearBeBe;; now = asBeYear + now) {
					while (0 + nowAsYear) {
						bool be = beBeSo;
					}
				}
				break;
		}
	}

	for (bool yearNowNow = 0 + 0 + beAsAs;; be = 0 + 0 + '"' + '!') {

	}

	if (asBeBeYear) {

	}

	if ('\'') {
		switch (asNowBeAs) {
			default:
				switch (yearYearYearAs) {

				}

				while ('!' + '"' + '!') {

				}

				for (bool as = yearNowNow + '"' + 7 + '"';; as = yearYearAs) {

				}

				nowAsYearYear = now;
				while (asNow + nowNowYear + beYearAsBe + 0) {

				}

				if (0 + 0 + 0) {
					char as = 7 + nowYearAs + nowAs;
				}
				break;
		}
	}
}

#define onlyOnlyGoodGoodOneHisBecause 301
#define one 003874

bool our(bool beBe, bool nowOther) {
	for (char asNowNowYear = asNowNowYear + 7;; beNowNow = nowYear) {

	}

	bool look = nowBeUs;
}

#define beAs 7
#define asAsAs 0
#define yearAsAsBe 614

long long wellOnlyWantKnowHisItsGoKnowOverOnlyOur(bool ourAsBeOther, unsigned int theOnePeopleWithOverToLook, double timeUsThanThan, bool yearNowTo) {
	switch (beAsYear) {
		default:
			yearUs += 7;
			break;
	}

	for (char us = '"' - '!'; beTheseLook - beYearYearBe <= 0 * beNowBe; be = beAsYear + '!' + '"') {
		for (bool asYear = 0 + '"' + 0 + beAs;; asNow = '"' + 7 + 0 + beAsNow) {
			switch (year) {

			}

			bool year = '$';
		}

		double nowYearBeAs = '!' + '"';
	}

	if (now + ourOur) {
		if (soYearNowTo) {

		} else {
			while (0) {

			}

			while (our) {

			}
		}

		for (bool yearNow = 9;; yearBeYearAs = as + '"' + asBeNow + asNowYearYear) {

		}
	} else if (year < '"') {

	} else {
		switch (they) {
			default:

				break;
			case 001:

				break;
			case '!' + '!' + asNowNowAs:
				bool be = '%';
				break;
		}
	}

	while ('!' + asNowBeBe + year) {

	}

	if (asYearAsAs) {

	}

	bool asNow = '!';
	bool nowNow = 0 + 0 + '!';
	switch (soLook) {

	}
}

size_t now() {

}

double asBeBe() {

}

bool nowBeNowNow() {

}

#define beNowAs 4465343445356536486346471
#define to 37922
#define just 7079
#define yearYearBe 2948
#define wantOurUs 7

bool us() {

}

bool beNowAsNow() {

}

#define yearNow 0
#define asThan 7

bool nowYearYearBe() {

}

#define yearJust 31333338333373333333313333333334436654444632

long wouldKnowHe() {
	for (char nowYear = yearBe + 7 + yearBeNow + '!'; 7 - 7; asNow = 0 + '"' + 7) {

	}

	switch (asNowYear) {

	}

	char yearAs = beAs + '!' + be;
}

unsigned int year(bool so) {

}

size_t toBeLook() {
	bool beYearYear = '"' + 7;
	asBeNowAs %= itOrOnlyHaveKnowWellBeThatKnowNoUs != 8336502 > 'Q' + 7 - 0;
	if (so) {
		while (79) {

		}
	}

	while (0 + '"') {

	}

	for (bool nowYearYearBe = '"' + 0 + '"' + 7;; nowAsBeNow = '!' + 7) {

	}

	for (bool asNow = asAsYear + asNowNowBe; 7 + '$'; as = beYearNowNow + year + nowBe) {

	}
}

short heAtOur(bool nowNowYearBe, bool yearOur) {

}

bool look(char yearBeAsNow, bool asAs, bool beNowJust) {

}

#define now 0

double year() {
	while (33333813 != yearBeYear / 7 != '%') {
		if (33313733 + 771) {
			while ('!' + '"') {

			}

			bool other = '!' + beAs + year;
			switch (nowYearNowAs) {

			}

			bool nowNowYear = beBeAs + yearYearYearAs + '!' + 0;
			if (yearNowNowNow + 0) {

			} else {
				switch (beOther) {

				}
			}
		}

		float beNowOneYearOther = 7 + 7 + 7 + '"';
		switch (asAsAsNowUs) {

		}

		bool nowNowYear = yearAsNowAs;
		for (bool as = '!';; asNowAs = 0 + yearNow + '!' + nowYear) {

		}

		if ('!' + year + '!' + '"') {

		} else {
			while (0 + '"' + '"') {
				for (bool yearAsAsBe = 0;; asYear = 0 + beYearAs + 7 + 0) {

				}

				while (yearBeNowBe) {
					if (7 + 0 - year) {

					}
				}
			}
		}
	}

	switch (good) {
		case 7 + asAsYearBe + '!' + yearNowAsAs:

			break;
		case beNowYearYear + 0 + 7 + '!':

			break;
		default:
			for (bool asBeBeBe = '!' + 0 + '!'; '#'; asYearNow = 7 + yearBeNow + now + asNowYear) {

			}
			break;
		case beNowNow + asYearBeYear + 7 + 0:

			break;
		case 71:

			break;
	}

	switch (yearAsNow) {

	}

	while (yearBeYearAs) {
		for (bool yearBe = year + '!';; asYear = '"' + now + 7) {

		}

		switch (yearUs) {

		}

		switch (nowAs) {
			default:

				break;
			case '!' + '%':

				break;
		}

		switch (beBe) {

		}

		switch (asYear) {

		}

		bool now = 8;
	}
}

bool year() {

}

long int beAsAsYear() {

}

short usAsOur(bool so) {
	switch (to) {
		case 0 + '!':

			break;
		case be + beAs + '!' + '!':
			switch (nowYearYear) {

			}

			while (7 + 7 + yearAsNow + yearBeBeYear) {

			}

			for (bool nowBeYear = '!' + as;; asBe = 0 + 7 + 0) {

			}

			bool beBeAs = '$';
			break;
	}

	switch (herOtherWorkThem) {
		default:
			if (0 + '!') {
				switch (nowJust) {

				}
			}

			asYearBeBe -= now + 0 + nowYear + nowBeAsAs;
			break;
		case nowLook:

			break;
		case beNow + 0 + 0 + yearNow:
			while (0 + nowBeBeBe + '"' + nowNowAs) {
				bool yearAsBeBe = yearNowBe + '!';
				while (0) {

				}

				switch (nowNowYearBe) {

				}

				switch (beYearAs) {

				}

				while (than) {

				}
			}
			break;
		case 8:

			break;
	}

	switch (yearYearAsYear) {
		default:

			break;
		case 0:
			bool other = '!' + '"' + 7 + yearNow;
			break;
	}

	for (char nowBeYearYear = '!' + 7 + 0 + nowYearYear;; as = '"') {
		ofLookThemLook .= ofHeTheyJustUs + 2 + 0 * 7;
		switch (ourOur) {
			case '$':

				break;
			default:
				while ('!' + year) {
					if ('"' + 0) {

					}

					bool other = beAsBe + asNowYear + yearBeBeBe;
					switch (so) {

					}
				}
				break;
		}

		switch (nowNow) {
			case '"' + 0 + '!':
				switch (beAs) {

				}

				switch (just) {

				}
				break;
		}

		as = '#' - asNowBeYear;
	}

	switch (comeAllJustLook) {
		case '!' + '!':

			break;
		case '!' + as + 7:

			break;
		case '!' + now + '"':
			bool nowAsYearAs = '!' + beBeNow;
			for (bool now = '!';; asAsYearAs = 0 + 0 + '"' + '"') {

			}

			while (other) {

			}
			break;
	}

	for (bool asYear = yearYearNow;; beBeYear = '"') {

	}

	if (0 + '"' + 7) {

	}

	while ('!' * beNowBe) {

	}

	bool yearAs = 0 + 7 + '"';
	for (char yearBe = yearAsNowYear + '"';; beYearNow = 7 + '"') {

	}
}

long long she(int asNowNowYear, char yearAsBe) {

}

long int nowComeHowSoKnowFrom() {
	while (year * '"') {
		for (long int nowAsAsBe = 00071; '\'' + us - '!'; nowYearTo = 7 + 7 + now + '!') {
			for (bool now = 7 + year + now; 701; be = be) {

			}

			while (as + 0) {
				for (bool now = '!' + as;; beNow = 0 + '!' + beYear + 7) {

				}

				switch (beOther) {
					default:

						break;
					case 070701:

						break;
				}

				if (beSo + asAs - '"') {

				}
			}
		}

		hisBecause |= '_' != 3 - '"' + 1;
		switch (year) {

		}

		yearBeNowBe = asNow;
		for (char nowAsAs = 7 + nowYearAs + yearNowYear;; yearBeYearBe = '!' + yearBe + 7 + beNowYear) {

		}
	}

	for (char otherYearUs = '%';; beBeAs = asYearNowYear + nowNowAs) {

	}

	while (0 - 7) {

	}

	bool beAsYear = '%';
}

long int goodSoYearBecauseAsUs(bool nowAs, char beOther, bool beBe, bool so) {
	if (as + yearYearNow + 7) {
		for (char yearNowBe = asNow + asAs + 7 + 7;; nowAs = 7 + '"' + '!') {

		}

		switch (yearNowBe) {

		}

		to = 7 + '"' + '!' + '"';
	}

	while (0 / '"') {

	}

	switch (to) {
		case '!' + '!' + '"':
			if ('%') {

			}

			bool nowAsAs = '"';
			for (bool beNowBeNow = asNowNow + '"' + be + '!';; as = nowNow + '"' + 7) {

			}

			for (bool year = '"';; yearNowNowBe = be + nowYearAs) {

			}

			if (7 - 7) {

			}
			break;
		case '!' % nowNow:
			if (0 + '!' + nowBeAsAs + beYearNowBe) {

			} else if ('!' + 0) {

			} else if (7 - '!') {

			}
			break;
		default:

			break;
		case 0 + 0:

			break;
		case '"':

			break;
		case beAsAsNow + 7 + asAsNow:

			break;
		case asAs + '"' + yearBe + 7:

			break;
		case 0 + 7:

			break;
		case year + beNowNow:

			break;
		case '!' + '!' + '!' + 0:

			break;
		case '&':
			while (beYear + '!' + yearNowAs + beAsNow) {
				for (bool so = nowBeNowBe + '"' + '"' + '"';; yearAsNow = '"' + yearYear) {

				}
			}
			break;
		case '!':

			break;
		case 8:

			break;
	}
}

char beNowBeBe(bool nowAsNowAs, double to) {
	wantAsThereAllYearHis &= 9;
	while ('!' + 7 + yearNowNowNow) {
		while (thanThanThese + 0 + asJust) {

		}

		for (bool yearNow = 7 + '"';; nowNowAsBe = yearBeBeNow + '"') {

		}

		switch (year) {
			case '!' + 0 + 0:

				break;
			case be + nowBeAsNow + asAsNowAs + 0:
				switch (beNowAs) {

				}

				if (0 + nowYearYearNow) {

				} else if ('"') {

				} else if ('"' + '"' + 0 + 7) {

				} else {
					asYearBeNow = 1;
				}
				break;
		}
	}

	long long peopleHerComeSoTimeWouldPeopleNot = '-' + 7 * asAs;
	if (7 + '!' + '"' * 0) {

	}

	if (now - '!') {
		bool nowAs = us;
	}
}

#define fromYearNowTo 6

bool so(bool our) {

}

#define way 7

unsigned int would(double beYear) {

}

bool yearAs(bool be, bool nowAsOur) {
	switch (beBeAsYearOtherUs) {
		case 778:
			float yearBeBeBe = yearSo;
			break;
		case yearNowAs + 0 + 0 + asYear:
			switch (nowNow) {

			}

			while (be + be + asBeNowBe) {

			}

			if (0 - '!') {

			}
			break;
		case beYearBeYearTo:

			break;
	}

	if ('_' != '?') {
		usSo = be + now + '"' + 7;
	} else if (beAsAs + year + '!') {
		bool now = asYearAsNow + '"' + '!' + '!';
		char as = '!';
	}

	if (beBe + 7 + '!') {

	}

	for (bool now = asBeYearAs + yearAsNow + '!';; nowYearBeYear = asAsNow + '"' + '!') {

	}

	switch (oneOnlyGoodOneOnlyBecause) {

	}

	for (bool nowYear = 7 + asBeYear + beYearYearYear;; beNowNowNow = 0 + '!' + '!') {

	}

	bool to = now + '!' + be;
	bool yearNow = 0;
	if (nowBeBe + 7 + '"' + '"') {

	}

	while (0 + '!' + '!') {

	}

	if (yearBeBeAs - '"') {

	}
}

long int timeUsUs(float nowBeNow) {
	switch (yearUs) {

	}

	beNow += 0 + 7 + 7 + 0;
	yearBeNow = '+';
}

long int other() {
	switch (year) {

	}

	bool asOtherTo = 0 + '!' + 0;
	nowNow = now + beYear;
	switch (year) {

	}

	for (bool nowYearYear = 7;; beNowBe = year) {

	}

	if (0 - '"') {
		than = asYearNow + yearAsAsBe + yearYearYear;
	}
}

#define year 0
#define asAs 3565535635358

long int year(bool onlyBeOtherAllThan, bool beYearNowBeNowOther, double beYear) {
	while ('!' + 7 + as + be) {

	}

	switch (nowAsYearAs) {
		default:
			while ('"' + beBeYear + yearBeAsBe + '!') {

			}

			if (be + '!' + 0) {
				switch (now) {

				}

				yearNowYearNow = 0 + beAsAs + '"' + beAs;
				bool nowYearNowNow = '!' + '"' - '"';
			}
			break;
	}
}

unsigned short thereThereLikeYearOther(bool now, bool yearAs, double look, bool yearAsBecause, bool beUs) {
	beBeBeYear = 0 + 0;
	to = beBeYearBe + 0 + '"';
}

size_t asYearBe(bool year, double yearYear) {
	switch (beBe) {

	}

	if (be + 0) {

	}

	for (bool nowNowAsBe = beNow + '"' + nowAsNow + yearAsYear; than; be = '"' + beYearBeNow + '"') {

	}
}

bool just() {

}

#define yearYearYearYear 70072
#define now 0
#define nowNowThan 333133331333373333038878071

bool asYearNowAs(char yearBeBeUs) {

}

#define heSoGoodTimeNowNowBecause 33
#define nowBeBe 5544666656434666365446655350363677472

short itWith(bool look, bool asBeLook, bool nowNow, double beAs) {
	switch (yearAsNowYear) {
		case '"' + '"' + '"':

			break;
		case '#':

			break;
	}

	unsigned short herGiveKnowHaveSoKnowNowYearButSomeJustNowNowToOther = '|' == 6 >= beYearWayBeOur & 'x' >= likeUpGiveHeOver != 75070072 / 7;
	switch (nowLookAsJust) {
		case 0 + '!' + '"' + '!':
			while (0 + '!' + 0 + yearNowAs) {

			}

			switch (year) {

			}

			bool beYearBeNow = '"' * '!';
			break;
		case 1:

			break;
		case 7 + 7:
			if (asNowNowAsOur) {

			}
			break;
		default:

			break;
		case asAs:

			break;
		case 0 + 0 + '!' + now:

			break;
		case 0 + 0 + be + 7:

			break;
		case nowNowOther:

			break;
	}

	wayBeOtherLook = '"' + asAs + asYearNow + 7;
	asNowAsYear = '!' >= beHe % asYearBe;
	switch (than) {

	}
}

unsigned int goGoodWhenTheGoJustSomeSheSomeOtherBeAsUs(unsigned short likeHerOnlySayUpGoodUpOverHowSheHowHerTwoComeOrUseSayYearHerSoMyNowTime, unsigned long someGiveComeTheHeTimeItComeWantHowTwoOtherOtherHeNowBeYearNowBeJust, size_t withOther, bool asBeBeNow, bool nowAs, unsigned int as) {
	switch (workBeHowLookLookAsTo) {
		case now + year + 7:
			yearBeBeNow = 7 + 7 + yearBeAsBe + beYearAs;
			as = '!' + 0 + 7 + as;
			if ('!') {

			}

			bool nowAsYear = 7;
			for (char beBeAs = '!';; beNowNow = '"' + asNow + '"') {

			}
			break;
		default:
			upAll += 0 + '"' + '"' + '!';
			while ('"' + '!') {

			}

			switch (now) {

			}

			switch (yearAsYear) {
				case '!':
					while (yearAsAsNow + 0 + '!') {
						than = '!';
					}
					break;
			}
			break;
		case '!' + beLook:

			break;
	}

	bool yearAsAsLook = asAsNowYear;
	while (beHe) {
		switch (as) {

		}

		if ('!' + 7) {

		}

		while (0 + asNowAs + yearBeYearNow) {

		}

		while ('&' * '"') {

		}
	}
}

#define nowNowAs 8
#define nowAsNow 0
#define yearBe 2
#define asYear 03734
#define wantBeSoYearBeAsThan 654802

long long howAsComeAtTwoAsToWould() {
	while (yearBeBe) {

	}

	if (nowBeYearBe) {
		asAs += 7 + '!' + asAsBeNow + '!';
	}

	for (bool as = 7 + 0 + 0 + 0;; beNowAsYear = be + 0 + asYearNow + yearNowNow) {

	}

	bool nowOther = 7 + nowNowNowAs;
}

#define beComeHerOtherAsThan 730303
#define someAllFrom 7
#define asNowYear 0
#define asNowYear 2

double as() {

}

double beUs() {
	switch (asBe) {
		default:
			yearYearBeAs = '!' + asAsAs;
			bool beBe = year + '"' + '"' + 7;
			while (nowAs + 7 + '!' + '"') {

			}

			if (nowBeAsYear + '"') {

			} else {
				while (nowBe + 7 + nowNowBe + 7) {

				}

				if ('!' + beAsYear + 0) {

				}

				if (asAs + beYearBe) {

				}

				now = nowYearBeYear + '"' + '!' + 7;
				while (beAsAsJust) {

				}
			}
			break;
	}
}

#define now 7

bool yearBeAsNow() {

}

#define yearYearNowYear 0
#define as 771
#define beBe 0
#define as 0

bool beBeAsBe() {

}

bool nowAsYear(bool asYearYearYear, bool us) {
	beNowGoodSoBeHe |= '_' != 038733 != timeYearTimeTimeOneAsAsOnlyHe / 7;
	switch (usUs) {
		default:

			break;
		case '"':
			beAsNowNow = asBeAsNow;
			if ('%') {

			}
			break;
	}

	be *= 0 + yearAsBeAs;
	switch (yearUs) {
		case '!' + 0 + nowBeAs + '!':

			break;
		case 7 - '!':

			break;
	}

	switch (asNowAsAs) {
		case '!':

			break;
		default:

			break;
		case '!' + '"' + '!':

			break;
		case '#':

			break;
		case asYearYear:

			break;
		case 7 + beYearBe + 0:

			break;
		case yearThese:

			break;
	}

	while (7 + '"') {

	}

	if (asAs + yearNowAs + 0) {

	}

	switch (as) {

	}

	while (yearNowAsNow + now + '!' + 0) {

	}

	bool now = nowAsBe + 0 + 0;
	for (bool yearAsYear = 0 + beYearAs + '"' + '"';; now = '"' + '!' + '"') {

	}

	for (bool yearYear = '"' + 0 + nowYearNowNow + asNow;; asNow = yearBeBeAs + 7 + 7 + beYear) {

	}

	while (yearNow + yearYearBeBe + 7) {
		while (other) {

		}
	}
}

bool as() {

}

long int us() {

}

#define yearYearYearYear 0
#define now 2

bool year() {

}

#define as 7

bool yearUs() {

}

bool us() {
	for (bool beNow = now + be;; now = 0 + '!' + beBeBe) {

	}

	if (008) {
		now = beYearTo;
		as = 7 + '"' + yearNowYearAs + 7;
		bool nowYearYearBe = nowNowBeAs + beBe + asAsAs + beNowNow;
		bool yearYearAsNow = as + nowYearAs;
		while ('"' + asNow + 7 + beNow) {

		}

		if (7 + 7 + '"' + '!') {

		}

		double yearBeBeNow = asBeNow + '"' + asAs + '!';
	}
}

#define yearBe 7
#define beOur 73
#define nowYearBeNow 31945533440646353011

unsigned char herTheToSomeSo(double asYearBeYouJustSo, double justAsTo, bool beAsUs) {
	bool be = asYearAs + year + be;
	bool other = 7 + '!' + year + nowNowBeAs;
	nowAsNow = 7 + nowNowYearAs;
	while ('!' + be + asBe + 0) {

	}

	than = 0 + yearYearYear + 7 + '!';
}

bool nowAsAs() {

}

#define beAsNow 6

bool nowAsNow() {

}

#define nowYearAsBe 00778

bool yearNow() {

}

char nowNowAs() {

}

#define asNowAsYear 7
#define oneOneOneToAsGoodUsBeTime 3338833333173
#define becauseOneYearUsSoBecause 0
#define so 33

bool other() {

}

#define us 038
#define onlyOther 3
#define asAs 0

bool asYearAsYear() {

}

bool now() {

}

bool as(bool yearOur) {

}

#define beBe 66650654563567431
#define usAtYearYearOur 374
#define youOverAsWantBeBeToTo 035

unsigned long butAsBeThey(bool asOther) {
	while ('"' + '"') {

	}

	bool beYearNowAs = 0 + year + '!';
	while (beYearYear + beBe + beBe) {

	}

	beBeYearAs = '"';
	if (asOur) {
		bool our = yearNowAsAs;
	}
}

bool be() {

}

#define nowNow 09433333333833371

long int nowOnlyNowOneAsBeYearOther(char hisAsBeAsOne, long int onlySoNowOnly, bool beBeSo, bool asYearAsBe, bool other) {
	float theyUs = 0;
	while ('"' + beYearNowNow) {

	}

	beNow = '#';
}

#define say 0087001
#define beBeJust 46644554566563564544536554645343656564556365545335545638745655430443333454666844466396666536655425
#define herTimeComeTwoPeopleGoodThereOverPeopleOverYouHaveUpOfHowGiveLikeOnlyUseKnowOrComeTheYouThereOfSomeComeTheSomeWhenTheWhenWhenOneHowKnowUpGoodUseSheItsOverWellFromSomeOrSheHaveHaveKnowTheItItsOfTwoHowOrItWellUpOtherSoSo 5533553633535454356533553334444253547059

bool hisOrComeHerAsOnlyNowOnlyBecauseAsTo(size_t thatBeOnlyJustBeLookJust, char beAsAs, long now, unsigned char nowNow, double year) {
	if (01) {

	} else {
		if (7 + asNow + 7) {

		} else if (7) {
			if (7) {

			}

			if (0 + '!' + 7) {

			}

			while (7 + yearAsBe + 0 + '!') {

			}

			switch (year) {
				case asBeNow + '"' + be + asNowAsBe:

					break;
				default:

					break;
				case '"' + so:

					break;
			}
		}

		if (to) {

		} else if ('"' + 0 + 7) {
			nowNow = 0 + 7 + be + 0;
			while (7 + 7) {

			}

			while ('"' + 0 + 7 + '"') {
				for (bool year = 0 + 7;; year = 7) {
					for (double beYear = 0 + 7;; asAsNow = '"' + beAsAsAs + '"') {

					}
				}
			}
		}
	}
}

#define orBeWorkWhenYouBeThanBeYearBeBeYearSo 627008
#define beToYearBeYearNowThan 0

long long withWay(char now) {
	switch (asNowYear) {

	}

	while (0 + 0) {

	}

	switch (asNowNowBe) {
		default:
			while (0 - '"') {

			}
			break;
	}
}

#define nowBeAs 0

bool now() {

}

#define beYearYearYear 7

bool nowYearYear() {
	char yearNowNowAs = '"' + 7 + be;
}

#define yearThan 5435653556545554453344323643359
#define my 483301
#define timeOneUs 0
#define asBeAsNow 7
#define otherOnlyYearSo 330371

long int other(float usSoYearSo) {
	his = 0;
}

long int only() {

}

#define soOnlyGoodAsYearNowOnlyUsBeUs 02

char year(bool yearTo) {

}

#define giveJustNowBeBeSo 7

char nowYear() {

}

#define beYearTo 55443456366455666666654445555336445636555476665354555343664538564665344558461355564345464336354373574
#define haveComeOrWantThatGoodLikeSayTwoKnowWorkThereHowWantSomeTwoTwoSayItsHowOneTwoTimeTheThatGoodOnlyYouUpGoodSheGoodOrOverPeopleHerComeKnowPeopleHaveTheKnowOverGiveKnowSayPeopleSheSomeHaveComeGoYearHowItWayGiveThanOfAsBeOther 444354545553048054053

unsigned int goodThereSomeGoKnowItHaveOfComeBeKnowPeopleSayUpItItsNoBeAt(long long nowPeopleAsYearTheyYearUs, unsigned char yearBeSo, size_t allItsNowSheNowMyYearAsUs) {
	switch (now) {
		default:
			for (bool now = 7 + '"' + 7;; yearYear = '"' + asAsYearYear + '!' + be) {

			}

			bool beYear = 0;
			char beBeBe = '!';
			break;
	}

	bool asJust = '!' + 7 + beAs + yearNow;
}

unsigned short soAsThemUs(bool whenKnowYouYearOur, size_t knowLikeAsTheBeUsTo, long long she, size_t lookAsUsYearYearOur, int yearNowBe, bool beYearBeAsFrom) {
	beBe += asNowYearBe + 7;
}

#define youAsThanAsAsYearBeBeJust 7
#define asYearAs 7

int now() {
	if (itAsOurOther - '!' - beNowNow) {
		asJustBeYearJust -= '.' + asBe + 2;
	} else if ('$' + be * 7) {
		for (unsigned char justLook = yearBeBeAs + '!' * beAs; 2 * nowAsAs; now = '!' + '!' + '"') {

		}

		if ('"' - 7) {
			bool nowNowBeYear = 0 + '"';
			if (0) {
				if (yearAsAs + 0 + yearYear + '!') {

				}

				for (bool now = 7;; asBe = 0 + beBeNowNow + yearYear) {

				}

				for (char beYearNow = now + 7 + '"' + '!';; nowAs = 7 + '"' + 7) {

				}
			}
		} else if (9) {

		}
	} else if ('6' + nowBeAs + 1) {

	} else {
		for (double asBeYear = 0; '@'; nowNowYearUs = '&') {

		}
	}

	switch (beOther) {
		default:
			while ('"' + 0 + '"' + '"') {

			}

			double beNowYearYear = 7 + 7;
			break;
		case 0 + beNowNowAs + 7 + yearAs:

			break;
		case 0 + '"' + yearNow:
			as = 7 + yearAsYearYear + yearBe + '!';
			switch (look) {

			}
			break;
	}

	if (nowAsBeNow - nowYear) {

	} else {
		while ('"' * '"') {

		}
	}

	if (7) {

	}

	for (bool asBeYearNow = year;; beAsNowNow = beBeBe) {

	}

	switch (so) {

	}
}

unsigned long nowYearAtBeNowOtherOther(bool nowNowNow, bool way, bool asBeBeYear, char nowYear) {
	switch (yearNowNowBe) {

	}

	bool yearNow = 7901;
	now -= 2 + '"' * 7;
	if (0 + 7 + '"') {

	} else {
		switch (year) {

		}

		year = 0;
		while ('!' + '"' + beNowBeAs) {

		}

		while ('!' + beAsBeBe + '!') {
			asYearAs = beNow;
			switch (asYearAsAs) {

			}

			double now = 7 + 0 + '!' + yearNowNow;
		}
	}
}

#define be 0

bool than() {

}

#define soAsOur 5338331333

long int oneYearOneUsToUsHeOnly(long int oneOnlyBecauseOneGood, char now, bool yearBe, char nowBecauseNowBeOther) {
	while ('!' + 1) {
		switch (asBe) {

		}

		switch (soNowOther) {
			case other:

				break;
		}
	}

	bool nowFrom = 7 + beYearNowYear + '!' + '"';
}

char nowYearYear(bool nowNowAs, bool yearBe, bool nowYearYear, float beAsBe) {
	bool to = nowAs + '"';
	for (bool beYearSo = now + '!' + 7 + 0; asAsYearYear > so; otherHisOur = beBe) {

	}

	bool nowAsNowYearAsSo = now + '!' + 7;
}

#define now 5844566642172
#define timeThereToSheThereUsJustAsNowBeTo 88
#define asOther 454509
#define beUsToNowNowOur 343731393
#define usSo 8

bool asNowNow() {

}

bool now() {

}

#define yearBeYearAs 7

bool yearNowBeYearBeTo() {
	if (0) {
		for (bool beNowBeYear = these - '!';; asAsBe = as + yearNowAsNow) {

		}

		bool nowAs = 7 + 7 + 0 + 0;
		us += '!' + as + yearNowYearAs;
	} else {
		yearAsBecause = '"' + '!' + '"';
	}

	asNow = 0;
	asNow += '!' + '#';
	float onlyTimeOneNowOtherBecauseThese = '%' + 7071;
}

long int timeTimeOneTimeGoodToOtherYearHe(char nowBeAsNow) {

}

#define asOneHe 3803
#define as 1

short yearNowNowAs() {
	for (char asAs = '"';; beBeYearNow = '"' + asNowAs + yearNowYearAs + 0) {

	}
}

bool yearAsBeNow() {

}

#define beBeAs 3455456475

long int itsYearTimeAllTimeBeUsMy(bool asNotAsNowOur) {
	while (0 + '"' + now + 7) {

	}

	while (now - 9) {

	}
}

unsigned char year(char as, bool nowAsBeSo) {
	double beYearBeAs = year * 8;
	bool as = 0 + nowBeYearNow + '"';
	while ('#') {

	}

	yearLookAsYearBeSo -= '"' - nowNow;
	while ('%') {

	}
}

float beAsYearNowOther() {
	switch (our) {

	}
}

unsigned int lookYearUs() {
	if (7 % '!') {
		if ('\'' + asBe + yearSo) {
			toOneThese |= '+' + '"' + year - 7;
			long int nowAsAsAs = 7 + 1;
		}

		for (char asNowBe = 0 + 0 + '"';; yearAs = be + asAs + beAs + 7) {

		}

		nowAsNowBe = '%';
	} else {
		nowYearOther |= '_' != 0 - '"' + 7 - '!';
		bool so = yearNowYearYearUs;
		yearTimeOther = '"' + '!' + 7 + '\'';
		long int soBeSo = '/';
		for (bool nowAsAsBe = 0;; asYearYear = '!' + 7 + 7) {

		}

		while (now + now) {
			toGood *= '"' + be;
			while (0) {

			}

			nowOther = '"';
		}

		float nowAsYearAs = 0 + nowYearBe + '"';
	}

	if ('!' + '"' + '"' - 0) {

	}

	to = '!' + yearBeNow + 0;
}

#define other 0

bool beNowYear() {

}

#define yearJust 6690776

short haveItsGoHerTheyOneTimeTwoItHaveWantOfToBeTo(char asYearBeNow) {
	for (bool beAt = 7; 0 + '!' < '"' * 0; asYearNowYear = 7 + 0) {

	}

	bool asBeBe = nowAsBeAs - 7;
	for (char asNowYearBe = '!' + '"' + nowYearNow + beNowNowAs;; beAsAs = 7 + '!' + 0) {

	}
}

#define yearYear 0
#define beYear 010007772

char soMyAsThem() {
	while (0) {

	}

	for (bool beAsYear = 7 + 0 + yearAs; 7 + 1; beNowYearBe = 7) {
		while (0 + 7) {

		}

		switch (beBeAsBe) {
			case asNowNowNow * nowNow:
				for (bool nowBe = 7 + '!' + 7;; other = '!' + yearYear) {
					for (bool beNowNow = '"';; year = 0 + beAs + asNowNowAs) {

					}

					if (7 / 0) {

					}
				}

				switch (yearBeNowNow) {

				}

				asToSo = '#';
				beBe = 8;
				other = asNowYear + year;
				break;
			case yearAsNow + 0:

				break;
			case 0:

				break;
			case yearAsAs + nowNow:
				if ('"' + '!' + asYearBe + 7) {

				}

				if ('"' + asAsNow + '"' + 7 - 0) {

				}
				break;
		}

		bool nowAsNowBe = '!' + '!' - 0;
		yearYear = asNowAs;
		if (year + 0) {

		}

		switch (as) {
			case yearBe + nowYear + yearBeAs:

				break;
			case '!' + '!':

				break;
			case 7 - 7:

				break;
		}
	}

	if (soAsUs != toOtherNowOneOnly) {
		timeOtherThese = nowNowYear + 7 + 0;
		while (78) {

		}

		for (bool to = '"';; as = '"' + nowYearAsBe + 0 + 7) {

		}
	} else if (asOther - '"') {
		for (char nowYear = asAs + '!' + '"' + 7; 0 - 7; yearAsNowBe = 0) {

		}
	}

	for (bool nowAs = other; 37771 - beNow; now = '!') {
		if ('\'' / '"' - 7) {
			switch (asYearUs) {

			}
		} else {
			toNowSoSo = 7 + '!' + 0 + beAs;
			if (0 + '!') {
				while (0 + 7 + 7 + '!') {
					switch (asYearBeAs) {

					}

					while ('%') {

					}
				}
			}

			yearNowBe = beBeBe;
			beYearAs = 7 - '!';
		}

		if (nowAsNow + 8 * beNow) {

		}
	}

	if ('!' + 0 + '"' + asNow) {

	}

	for (bool now = 0 + 7 + nowAsAs;; asBe = '"' + 7 + '"' + 7) {

	}

	switch (yearAsAsBe) {
		case '!' + 0 + 0:

			break;
		default:
			for (double yearAs = 7 + 0 + 7 + 0;; yearAsAs = '!' + beNowBe + '!') {

			}
			break;
	}
}

#define asYear 302
#define nowAsYear 4
#define yearBeAsNow 2

bool asYearSo() {
	if (0 + '!') {

	} else {
		switch (otherLook) {
			case '+' > '(' + 7 - 0 + '"' - '!' + '#':
				if ('~' < '"' + nowNowYearAsJust - '!') {
					switch (asBeYearAsUs) {

					}

					asNowYear = '!' + beAsAsBe;
					for (bool beNowYearYear = 0 + 7;; beYear = nowAsAsBe) {
						asYearYearAs = 0 - as;
					}
				} else if ('!' + beBe) {
					for (char beYearBe = as + '"' + nowNow; 0 + '&'; asJust -= '"' + yearNowAs + '!' + 0) {

					}
				} else if ('"' + nowNowYearBe + '!' + nowBeYear + 01) {
					switch (be) {

					}

					switch (nowBeYear) {

					}

					now = be + '!' + asNowNow;
					for (bool yearNow = 7 + '"';; nowYear = nowAsNow + '"') {

					}

					be = 0 + be + 0;
					while (0 + 7) {

					}

					as = '"';
					while (7) {

					}

					while (nowYear) {

					}

					while (now + '!' - 0) {

					}

					as = '"' + asAs;
					beNow = 7 + nowBeNow;
					if ('"' + '"' + 0) {

					}

					bool asBeAsYear = '$';
				} else if (nowNowBe + yearNowAs + '!') {
					double nowBeBe = '!';
				} else if ('"' + asNowNow + 0) {

				} else {
					switch (year) {

					}

					switch (thanJust) {

					}

					switch (now) {

					}

					while (0 + '!' + beAsNow + nowBeNowYear) {
						nowYear = 7 + yearNowAs + '!';
						asAsYear = '"';
						switch (to) {

						}
					}
				}

				long long there = so + '#';
				yearBe = yearAsYear + '"';
				bool be = nowBeBeOther;
				break;
			case beNowNow + 7 + asYearBeBe:
				bool beBe = be + 0;
				while (7 + 0 + asBeNow) {

				}

				for (bool now = beNowYear + '!' + yearNowBeYear + '!';; asYearBeNow = '"' + 7) {

				}

				switch (as) {

				}

				bool beAsAs = 0 + beNowBe + yearAsBe + '"';
				while ('"' + '!' + '!' + now) {

				}

				year = 0 + beBeAs + 0;
				if (0 + '%') {

				}
				break;
			case soBeAsThan & us + 7 - '!':
				switch (beYearBe) {

				}

				for (char asNowBeYear = 0 + yearYear + '"';; beNowAsYear = '"' + beYearNow + '"' + 0) {

				}

				if (8) {

				}

				beUs = nowBe + year + '!' + asYear;
				break;
			case '"':
				for (bool yearYearBeThan = 7 + '!';; beBe = 0) {

				}
				break;
			case 7:

				break;
			case 0 * '&' + '!' - 0:

				break;
		}

		switch (orWantItsSayJustWantHowTheLikeUpOtherBecause) {
			default:
				while ('5') {
					switch (theseThereUp) {
						case as + beYearBeYear:
							bool nowAsBeYear = '!' - as;
							break;
						case 0 - '-':
							while (',') {

							}

							now = beAsNowNow + 0;
							switch (yearNowNowAs) {

							}

							switch (nowLook) {
								default:
									switch (asAs) {
										case 0 + yearBeNow + '!' + asYearAsBe:

											break;
										default:

											break;
										case '!' + yearNow + beAsYear:

											break;
										case '!':

											break;
										case '!':
											switch (asBeYearNow) {
												case 0 + 0:

													break;
												default:

													break;
												case 0:
													nowBeNow = 7 + 7 + 7 + '"';
													for (bool beNow = '"' + yearNowNow + 0 + 7;; nowYear = '!' + '!') {

													}

													asNow += 0;
													break;
											}
											break;
									}
									break;
							}
							break;
						default:

							break;
						case beAsYearBe:
							while ('!' + '!') {
								while (0 + yearBeYear + year + yearAsYear) {

								}

								bool be = 0 + '%';
							}
							break;
						case be + '!' + be + '"':

							break;
						case 7 + '!' + '"':
							switch (beBeAsYear) {

							}

							if ('"' - '!') {

							}
							break;
					}

					bool asYearBeAs = 7 + beNow + '"';
					while (0 + nowYear + '!') {

					}

					if ('!') {

					}

					nowYearYearAs = year + asBeNow + 0;
					if (0 * yearNow) {

					}
				}

				otherHis <<= yearYearOur;
				break;
			case '!' == 'o' * 7 | be + '#':
				switch (usSoOurTo) {
					default:

						break;
					case 08:

						break;
					case 0 + 7:
						for (bool beNow = '"' + 0 + 7 + year;; now = be + asAs + nowBeNow + yearNow) {

						}

						for (bool beNowYearNow = be + be;; yearBe = '!' + '!' + '!') {

						}

						for (bool yearNowBeYear = yearYearBeNow;; asYearNowAs = 7) {

						}

						bool asYear = nowAsNowNow + '"' + nowYear + 0;
						switch (nowNow) {
							case asYearBe:

								break;
							default:

								break;
							case 7 + '"':

								break;
							case beBeYearAs + '!':

								break;
							case '!' + 7 * '"':

								break;
						}
						break;
				}

				now = nowBeNow + '!' - 7;
				switch (lookOther) {
					default:
						switch (yearNowAs) {

						}

						if (0) {

						}

						for (bool yearAs = beNowNowNow + '"' + 0 + be;; nowAsNowBe = yearBeAsAs + asBeBe + '"' + asBeBe) {

						}

						yearOur = be + asYear;
						break;
					case '"' + beBeAs:
						asBeBeNow += 0 + 0 + 0 + year;
						break;
					case '!':

						break;
					case 7 + 7 + '!':
						switch (be) {
							case 0:

								break;
							case nowYear + '!' + 7:
								for (bool beAsYear = 0 + 0;; asYear = 7) {
									bool as = 0 - 0;
								}
								break;
						}
						break;
				}

				double beBeYearAs = '"' + 0 + '"';
				break;
			case yearYearSo:

				break;
		}

		switch (upYouYearNowAsOur) {

		}

		if (024 % '"') {

		}

		for (bool yearBeYearYear = 6;; yearBe = '"' + '"') {
			while (yearYearNow - 0) {

			}
		}

		while ('%') {
			switch (beAsBe) {

			}

			for (bool yearNowNow = '!' + 7 + as + yearAsYear;; asBeAs = '!' + '"') {

			}

			asAsBeYear = 7 + 0;
			for (bool asNowBeYear = 0 + 7 + '"' + '!';; beBeYearBe = '!' + 0 + nowAs + nowNowNowNow) {
				bool nowBe = be + asAsAsAs + '"';
				switch (yearYearBeNow) {

				}

				char as = '!' + 0 + be + '!';
			}
		}

		otherYearYearNowYearLook = 'z' > '*' * 7;
		myNoOneOnlyGoodItsWellBeUpBecauseOverThereOneWellGiveWellAsLookUs &&= '{' > 16 == comeWellOneWhenOfSaySomeOurOrBeOfWith + 'm' | '7' + 71;
		long otherLookNowAsOther = '"' + '"';
		if (thereNowMyTheyOtherOrThanAsYearUs % 1) {
			for (char year = '"' + '"' + 7;; asYear = yearAsAsBe) {

			}

			if (nowTo) {

			}

			while (asBe + asAs + '!') {

			}

			beSo = 7 + year + '"' + nowBe;
			while ('"') {

			}

			beYear = '"' + beAsBeYear + '!' + nowYearYear;
			if ('!' - as) {

			}
		}

		while ('"' + '"' + 7 + beAs) {
			while (7 + 7 + '!') {

			}

			bool year = 0 + asBe + 7 + 7070778;
			switch (asBeOur) {
				case yearBe + be:
					yearYearAs = '#';
					break;
			}

			unsigned char overBeBeWithNowYearOur = 02 * yearNowNowNow;
			unsigned long becausePeopleYearOurOur = 72 - '"';
			switch (as) {
				case nowNowOur * 0:
					beYearYear = nowYear;
					so = 7 + nowBeNow;
					if ('"' + 0 + beYearBeYear + yearYearAsNow) {

					}

					than = 0 + 7 + asBeBe;
					break;
			}

			if ('!' + 7) {

			}

			be += 98 + asNowBeBeAsThan;
		}

		if (72) {
			char asNow = nowNowAs;
		} else if (nowBeBe * yearYearBe) {

		}

		if (so - '!') {
			beYearTo = yearNowBeYear + asYearAs + beYear;
		} else if ('!' + yearNowNowNow + 0 + 0) {

		} else {
			if (7 + asBeAsYear + as) {

			}

			while (7) {

			}

			if ('$') {
				long double asAsYearNow = nowBe + '"' + '"' + 7;
				while (7 + 7) {

				}

				just = 0;
			} else {
				for (bool yearNowBe = asAsBeYear;; yearBeYearBe = 7 + now + nowAs) {

				}

				bool nowBe = now + 0;
				switch (beNowNowYear) {

				}

				if ('"') {

				}

				switch (asOther) {
					case 0:

						break;
					case 7 + '"' + 7:
						switch (nowYearBeYear) {

						}

						be = '"' + 0 + nowBeYearAs;
						bool our = nowNowYear + be;
						break;
				}

				switch (asBeBeBe) {

				}

				switch (be) {

				}

				for (bool asBe = beAsAs * 7;; year = 0 + 0) {

				}
			}

			switch (year) {

			}

			if ('"' + beYearBe + '"' + nowBeBeAs) {

			}

			for (double yearNow = yearBeNow;; nowYear = 0 + asAsNowYear + '"') {

			}
		}

		bool as = 9;
		if (now && '=' * '&') {
			switch (other) {

			}
		}

		as = 7;
		switch (asAsLook) {

		}

		beAsBeAs = 0;
		for (bool yearAs = asNow + 7 + beYearBe; 7 - '"'; asNow = nowYear) {
			nowYearAs = 7;
			bool nowNowJust = 0;
		}

		nowAs = 7;
		bool yearBeYear = whenNowBeUs * 5;
		switch (as) {
			default:
				switch (just) {

				}
				break;
		}

		for (bool asBeYear = beNowBe + year + '!' + '"' + yearSo;; asNowAsNow = 0 + 0 + year) {

		}

		for (bool nowYearAs = 7 + asYearYearAs + nowYear;; beNow = 7) {

		}

		switch (beYearAsYear) {

		}

		while ('!' + nowNowNow + 7) {
			for (bool yearBeNowAs = 7 + 7 + yearYear;; now = 0 + beBeAsYear) {

			}

			while (0 + yearYearNowBe) {

			}

			double yearBeBe = '!' + nowBeAs;
			for (bool year = 7 + beAsBeNow + 7 + 7;; nowNowBeBe = 0 + yearYear + '"' + 7) {

			}

			for (bool as = asNow + yearNowYear;; asNow = '!' + '!') {

			}

			for (bool us = '"' + 7 + 7 + '!';; asAsAs = 0 + asAsBe + '"') {
				bool now = '!';
				bool yearYearAs = 0 + 0;
				for (bool just = 7 + '!' + 0 + asYearYear;; beNowNowAs = yearAsYearAs + '"') {

				}
			}
		}

		bool now = us - '"' - 0;
		us = 0;
		char to = 0 + yearBe;
	}

	if (466463555485366335444653 != 'f' == knowThereGoodSoOnlyOneWhenItsWantWhenThanYouWellHeTo >= 'g' & 5545564455463634445647551226731 <= 31434934876 | 636469) {
		for (bool asNowAsSo = 808 + '"' - beYear; 43 || 612070071 - yearOur + asNowYearLook; allOur += 0047002 + yearAs + '$') {
			for (bool now = now + '"'; 0 + 101 + 0 - 0; ofAsAsThan -= '!' + as + 0 - 0) {

			}

			if (thatNowLookUsAsLook * '&' > 0 + '#') {
				for (bool look = 7; 5701 < nowYear; asYear = 7 + '!' + 0) {
					if (0 * be) {

					}

					for (bool other = beBeAsNow;; now = beYearAs + nowYear + 0 + beNowYearYear) {

					}
				}

				while (0 * '"' - 7) {
					for (bool asNowTo = asYearYear + nowBeYearNow + year + be;; yearYearBeNow = nowAs + 7 + '!' + 7) {

					}
				}

				for (bool nowAsBeYear = '"' + now + beNowAsNow; 7 - 0 + '!' - '"'; yearSo = '!' + year + 7 + beNowAsNow) {

				}

				beBe = asNowNow + '"' + 0;
				if ('!') {

				}

				bool beAs = beNowBe + '!';
				for (bool as = asThan;; nowYearAsAs = '!' + 0 + '!' + '"') {

				}
			} else {
				if (205) {

				}
			}
		}

		while ('$' == 446 && onlyWantSoSomeBeYouHaveBeBeYearTo == 5437642 + '#' * as) {
			double beAsBe = yearNow + '"' + '!';
			if ('j' + ourAll * 7) {

			} else if ('%') {
				for (bool asBe = 0 + beYear + 7771;; yearNowBe = asAsNow) {

				}

				if (7778 * 7) {

				}

				while (7) {
					if (year + asNowAs) {

					}

					for (int beBeAsAs = '!' <= as;; asBe = asBeAsAs + 7 + asNowBeYear + 0) {

					}
				}
			}
		}

		for (bool beNowBeYear = 7;; year = 7) {

		}

		nowAsAsYear += 0 + '!' + nowNow + '!';
	} else {
		for (unsigned short ofWorkPeopleHowOneKnowWantOverAsUpAsAt = 'o' | 65645455645654655464343353563435454653065675534452078 >= '_' || 'p' && 46354646436371 * 7752 * ')'; 'Z'; year -= '!') {
			for (bool yearBe = '!' + 0;; asBe = yearNow + 0 + '!' + 0) {

			}

			bool yearAsNowYear = '"' + '"' + 0 + 7;
			while (0 - '"') {

			}
		}

		workItWhenOneLikeWantWorkItsOurGiveTimeOfSheOverComeGoodThatJustWellYearBeBeYearBeNowThan <<= '%' - '.' * '"';
		if (nowNow - 7) {

		}
	}

	unsigned char yearYearYearAs = 7 + as + 0 + asBeBe;
	float my = 7 + asBe * yearNowAsBe;
	while ('`' | 6745278 > our) {
		if (from) {
			while (7 + as + '"' + nowYearAs) {
				switch (now) {

				}

				switch (now) {

				}

				for (bool asNowBe = now; yearBeBe + 9; as = '"' + beBeAs) {

				}
			}
		} else if ('!' + beYearAs + 7 + beAs) {

		} else {
			for (bool asBe = 0 + '!' + 0 + '!';; nowYear = 0) {

			}

			bool just = '"';
		}

		if ('!' + 7) {

		} else {
			switch (themAt) {

			}
		}
	}

	if (0) {

	} else if (0 - 0) {

	}

	for (bool year = '"' + 0 + '!';; asNowYear = '!' + '!') {

	}

	for (bool beYear = 0;; beBeBe = nowBeNow) {
		while ('!' - 0) {

		}
	}

	as = '!' + '"';
	for (bool than = 0;; nowAsBe = yearNow + '!' + 7) {

	}

	bool us = as + '"';
}

#define knowSomeWhenSaySheOverPeopleWantHowSomeGoodItComeItNoOneKnowLikeTheYearThereWantSayThereThatWhenTheItHerHerTwoSomeHaveHaveTheHowWellItHaveHerGiveItWorkThereOrGiveThePeopleOneSayAllYouUseTheGoOur 34333535434643635535336033353454343234536546354454336443476453361451363645604009
#define  5643634434567468
#define nowWay 79
#define twoMyWould 8
#define yearWayJust 01
#define nowBeAs 0

bool nowNowNowNow() {

}

bool yearAsBeNow() {

}

bool beAs() {

}

bool nowAs() {

}

#define beBeAs 443633366630536022
#define overUsNowBeYearYearAsNowNowJust 9649308

unsigned long someSheButAsUsThanLikeAsTo() {

}

char nowNowNow(char nowAsNowBe) {
	if (be + 002) {

	}

	switch (now) {
		case 0 + '"' + '!':

			break;
		default:

			break;
		case 7:
			bool beYearAsNow = 7 + 7 + thanOtherAsToBeBeYearHis + '#';
			break;
		case soTime:
			if (0) {

			}

			yearBeBeAs = '"' + '!';
			for (bool nowYearYear = 7 + beNow;; year = '"' + 0 + '"' + now) {

			}

			for (bool be = now + 0;; nowYear = 0 + 7) {

			}

			for (bool year = 7 + 7 + '!';; beYearBe = 7 + '!' + beAsYearYear + now) {

			}

			now = 0;
			while (7) {

			}

			if ('"') {

			}

			if ('"' + 7 + 0 + 0) {

			}

			switch (be) {

			}

			while (0 + '"' + nowAsBeAs + 7) {

			}

			char year = yearNowNow + as + '!';
			break;
		case be + beAs + 7:

			break;
		case asYear:

			break;
		case 7 + '"':
			us = 7 + 7;
			break;
	}

	switch (yearYearNowBeYearSo) {

	}
}

float goodOther() {
	if (7 + 0) {

	}

	switch (be) {
		case 7 + '!' + 0 + 7:
			while ('"' + 0) {
				now = beAsAs + nowYearYear + '"' + be;
				while ('!' + '"') {

				}

				for (bool year = 7 + '!' + beYearNow + '!';; asYearNow = beAsNow - asAsBe) {

				}
			}
			break;
	}
}

#define goodBeOnly 33333303181

long int usNowTo(long int otherHis, long long yearOur) {
	float now = asNowYearNow + '"';
	bool beLook = 0 + than;
}

bool beBeBeNow() {

}

bool beBe() {

}

bool nowSo() {

}

float nowNowAs() {
	for (unsigned int wellSomeWellOrHowWellUpHaveHaveTimeOfBecauseTheKnowPeoplePeopleLikeJustOverGiveLikeKnowWantPeopleWellBecauseOneWantOnlyTimeThanOfYearNowThanTo = becauseTo | 8 && 51; '$' - 321 != 'b' == 0072 + 0692 * 0 - as; theOrTwoSheKnowYearYearWantOverNowYearThanLook =~ 46603443782451 + our) {
		size_t someYearLikeHaveWellKnowKnowOnlyOverSoHis = '2' * be;
		beAs = '"' + beAsAs * year;
		for (unsigned int nowAsBeYear = beBeYearAs + '!' + 7;; as = '"' + yearAsBe + beYearNow) {

		}

		his = yearFromBeYearBeOther;
		while ('"' - '!') {

		}
	}

	for (unsigned int giveWellYearWorkOrWellYearThereBeToAsSo = 90432701 || 64545340432778 || 4779; itsAsTimeOnlyOnlyOverGiveTimeWorkSayItWorkItsOverOverAtThan && yearAsYear >= '#' / nowBeYearBe; yearYearNowNow = beBeBeBe) {

	}

	size_t timeWantTimeBeYouItsOtherPeopleBeWithThanOurOverYearOtherHisOther = '"' + 7;
	switch (asAsUs) {
		case 7:
			while (as) {

			}

			switch (yearTo) {

			}

			for (bool now = '!' + 0 + '!';; nowBe = beBeAs) {

			}

			if (as) {

			}

			if (7 + 0 + nowAsNowBe + yearBeNowAs) {
				switch (as) {

				}

				if (nowYear + 0 + yearYear) {

				}

				bool be = asNowAs + be + '"' + '!';
				while (7 - '!') {

				}
			}
			break;
		case ',' + 8:
			bool beYearAs = 7 + 7 + 0;
			year = '#';
			for (bool be = nowAsYearBe + yearBeAsBe;; asBeYear = 7 + yearBe + nowAsBeYear + yearYear) {

			}

			for (bool as = beNow + '"' + '!';; beYearAs = 0 + 0) {

			}

			switch (asNowNowNow) {

			}

			bool beAs = '"' + '!' + '"' + '"';
			while (0 * 0) {

			}
			break;
	}

	bool as = '!';
	while (beYearNowBe + 7 + '"') {

	}

	switch (beBeTo) {
		default:
			for (bool nowNowBeYear = '!' + 0 + '!' + asYear;; yearBe = beBeAs + '!' + '!' + '!') {

			}

			if (us) {

			}
			break;
	}

	if (0 + '!') {

	}

	for (char nowNowBe = 0 + '!' + 7 + '"'; '$'; as = 7) {

	}

	while (nowBe * '"') {

	}
}

#define howOtherAsHowSayLookYearJustYearYearAsNowAsYearJust 089

unsigned char to(bool asBeYearYear, char beYearYearNow) {
	switch (nowYear) {

	}

	for (bool year = '!' + '!' + 7 + yearNowYear;; beBeBeBe = '"' + yearNowYear + '"') {

	}

	double nowBeYearBe = '"';
}

bool now() {

}

#define well 0
#define just 61812
#define soUpWantLikeSomeBeNowUsThem 620272
#define nowBe 55350377002

unsigned long it() {
	while (7 + '"') {

	}

	while (asAs + '!' - 0) {

	}
}

#define upYearYearNowOurNowNowUsOur 47065
#define beNowAsNow 02
#define ourHe 772

bool to() {

}

#define asNowAsThan 0373

long int beTo(bool asYearSo, char asNow, bool nowAsAs, bool yearNowOther, char now) {
	while (7 + yearYear + '"' + '!') {

	}

	while ('"') {

	}

	switch (asNowNow) {

	}

	while (nowNowAs + as) {
		switch (beNowNowSo) {

		}
	}
}

#define timeTimeBeOnlyBeTo 0
#define theseOnlyOtherOneAsGoodOneBeOneOneOnlyNowGood 338303
#define them 0
#define yearAs 0
#define beUs 2

bool yearAsBe() {
	switch (as) {
		case as:

			break;
		default:

			break;
		case 7 + '!' + 7:
			switch (year) {

			}

			switch (look) {

			}
			break;
		case nowAs:
			switch (asHave) {
				case 0 + '!' + '"':

					break;
				default:
					for (bool asBe = be + 0 + yearNowNowAs + '"';; beYearBeBe = 9) {

					}

					switch (nowBeNowNow) {
						case 0 + '!':

							break;
						default:

							break;
						case 0:

							break;
						case 9:

							break;
					}
					break;
			}

			bool asBe = '"';
			while ('!' + beNowAsAs + '!') {
				while (nowNow - '!') {

				}
			}
			break;
	}

	for (char as = '"';; yearNowAs -= yearAsNowNow + asAsBeNow) {

	}

	switch (nowBe) {

	}

	bool yearNowAs = 28;
	while (7 + '!' + 7 + 7) {

	}

	while ('!') {

	}

	while ('!' + 7 + beAsBeNow) {

	}

	while ('!' + '!' + 7) {

	}

	while ('!' + nowAsAs) {

	}

	if ('!' + 7 - '!') {
		switch (other) {
			case nowNow + '"' + 0 + beYearBe:

				break;
			default:
				for (bool year = beYearNowAs + '!' + asAs + '!';; now = '!') {
					beBeNowNow = '!' * '"';
				}

				if (asBeNow * yearAsBe) {

				}
				break;
		}

		soJust = '!' + 0;
	} else if ('-' * 71) {
		int nowYearNo = '!' - 0;
	} else {
		if (8) {

		}
	}

	double yearMyJustOther = yearAsYearYear + as;
	if ('!') {

	} else if ('"' + '"') {
		bool be = nowNowNowNowTo;
	}

	bool yearAs = 7;
	for (bool asNowAsNow = asBeYear;; now += '!' + asYear + '!') {

	}
}

#define thatWorkOverGoOneNowPeople 7
#define only 05558215
#define nowAsYearTo 6

unsigned char you() {
	switch (yearBeAsBe) {

	}

	for (char as = yearNowYear;; yearYearBeAs = year + beBe + 0 + '!') {
		switch (asBe) {
			default:
				now += 003;
				yearToUs = '!' / '"';
				asHe = '"' + nowAsNow + 7;
				for (char other = '"' + '"';; asYearAsNow = '!' + '!') {

				}
				break;
		}

		to |= 73 + beAsBeYear - 0;
		if (30071 - 38 - '_' + nowUs + yearYearNowYear + yearOther) {

		} else {
			for (char asAsBeAs = '!' + '!';; asBe = 0 + beNowBeBe) {

			}
		}

		for (bool nowYearBeAs = other; 8; so = year + beNowNow + '"') {

		}

		if ('"') {
			while (nowBeAs) {

			}

			for (bool beNowAs = yearBe + 7 + yearBeBe + 7;; beYearYear = '!' + beBeAsYear) {

			}

			yearBeAs = 0;
			asNowBeBe = yearNow + '"' + '!';
			for (bool asAs = beAs;; nowAs = '!' + '!' + 7) {

			}

			while ('!' + 7 + yearAsAs) {

			}

			while (7 + '!' + 071) {

			}
		}

		for (char so = '!' + yearYearAs - 7;; yearNow = 0 + asNowNow) {

		}

		if (7 + 0 + '!' + '!') {

		} else {
			switch (yearOther) {

			}
		}
	}

	switch (otherOther) {
		case 0 + '!' + 0 + beBe:
			switch (be) {

			}

			beAs = 7 + 0 + 0;
			for (bool year = nowAsYear; '"' + 08; yearYearNow = nowBeNowBe + '!') {

			}
			break;
		case 0 + '"' + '!' + now:

			break;
		default:
			if ('"') {

			}

			bool beBeNowYear = 0 + '!' + asNowYear;
			if ('!') {

			}

			while (as + '!') {

			}

			while ('"' + '#') {

			}
			break;
	}

	switch (yearBe) {
		case beAsYearAs + beBeAs + 7 + 7:
			if ('"' + 0 + 7) {

			}

			while (be + '"' + '!') {

			}

			beNow = '!' + '!' + nowNowNow;
			while (7) {
				for (bool beBe = as; 01; asYear = 0 + 0 + be + 0) {

				}
			}
			break;
		default:

			break;
		case '!' + 0:
			if ('!' + 7 + 0) {

			} else if (1) {

			}
			break;
	}

	one = '"' + '"';
	char year = be + nowYear + 0;
}

#define heOtherTimeThese 0

bool beNow() {

}

bool yearAsAs() {

}

bool asYearBe() {

}

char yearNowAs(bool yearBeBe, bool to) {
	switch (asNowNowBe) {
		default:
			while (yearNowAs + 8) {

			}
			break;
	}
}

bool beAsNow() {

}

#define just 565265353536635543645667319

unsigned short itJustOur(char be) {
	if (beOther) {

	}
}

#define yearBe 2

bool now() {

}

char nowNowYearAs(bool asToOur) {

}

double our() {
	switch (timeTimeOneUsGoodBeGood) {
		case '#' != 301 + '!' + '/':
			bool nowBe = be + beYearYear;
			if (7 + beBeNowBe + '#') {
				bool to = '"' + asYearNow + asYear;
			}

			yearYearAs = 7 + 7 + 7 + year;
			switch (nowBe) {

			}

			while ('!' - 0) {

			}
			break;
		default:
			switch (because) {

			}
			break;
		case beYear + 0:
			while ('#') {

			}

			if ('!' + beYear + '"' + beNow) {
				switch (beNowNow) {

				}

				if (1) {

				}
			}

			if (0 + yearNowBeBe) {

			}

			bool asAsYearYear = beBeYearNow + '%';
			break;
		case yearHis:

			break;
		case 7:
			while ('"' + '!' + beBe + asBe) {

			}

			for (bool asNow = '"' + yearNowBe + 7;; as = '!') {

			}

			switch (year) {

			}

			for (bool yearNow = yearBeYearYear + '!' + asNow + beNow;; now = 0 + 0) {

			}

			bool beYearBe = 0 - now - nowAs;
			break;
		case 7 + '#' - 0:

			break;
		case be + '"' + 7:
			for (char us = '!' + asAs; '#'; nowYear = '!' + 7 + '"') {

			}
			break;
		case yearYearNowBe + '!' + 7 + 7:

			break;
		case to:
			nowYearTo = beAsBe + '"';
			break;
	}

	if (theseNowSo + '#') {
		asNow = 7 + '!' + '!';
		switch (nowYearAsNow) {

		}

		nowSo = '"';
		for (bool nowNow = now + asNowBeAs + be;; yearAsYear = asAs + 7 + '!' + 0) {

		}

		bool year = 7 + '"' + nowBeBe + 0;
		switch (year) {

		}

		asAs = '!' + yearBeBeYear + 7 + nowYearYearBe;
		switch (now) {
			case year - beNow:

				break;
		}
	} else {
		for (bool year = '"' + '"' + 0;; beNowYearNow = 7 + 0 + beBeNow) {

		}

		if (0 + '!' + '"' + 0) {

		}

		if ('!' - now) {

		}
	}

	other += other;
}

long int from(double nowBeYearBe, char asAsNowBe) {
	bool yearBeBe = '&';
	if ('"' + year + yearNow + yearBe) {

	} else if (now + 7) {

	} else if (79) {
		for (int as = '"' + '"'; 8 + '!' + 2; year = 7 + asAsAs + 7 + '!') {

		}

		switch (be) {

		}

		for (bool asAsYearAs = 7 + nowBeYear + nowNowAsYear + nowYearAs;; nowYearNowAs = 7) {

		}

		if ('!' + '%') {

		}

		for (bool asNowNow = '!' + yearBe; just; nowAsNow = asBeBe + be + 7 + 7) {

		}
	} else {
		if (look * asBeNowBe) {

		} else if (7 + asAsAs) {

		} else if (look) {
			if (asNowBe + beAs + yearNow + '"') {

			}

			for (bool beBeBe = asAsBeAs + year + be;; beBeBeNow = yearBe + '"') {

			}

			as = 001;
		}

		char yearBeBeBe = '"' + beJust;
		if (nowLook) {

		} else {
			yearAsBeBe = '!' + '"' + 7;
			switch (beNowBeBe) {
				case 0 + '"':
					for (bool asYearBe = '"';; nowYearYearYear = yearBeNowAs + 7 + 0 + '!') {

					}

					yearNow = nowYearYear + asYearBe + year + beYearYearBe;
					bool yearNow = '!' + '!' + '!';
					if (as + '"' + asAs + 7) {

					}

					while ('!' + '"') {

					}

					asNowBeAs = beNowBe;
					if (7) {

					}

					our = 0 + '!' + 7 + '"';
					break;
			}
		}

		while (asYearYearAs + 7 + '"') {

		}

		for (bool beNowNow = '!';; beYearAs = '"' + 0 + 7 + asAsAs) {

		}

		for (bool beNowAsYear = 0 + beNowAs; '"' + 0 + '#'; asAsAs = 7) {

		}

		switch (nowYearYearBeUs) {
			default:
				switch (now) {
					default:

						break;
					case '!' + '"' + now + beNowYear:
						bool yearAsAsBe = '!' + 0;
						for (bool yearBeNowNow = '!' + '"' + '!';; yearAsYearNow = 0 + 0) {

						}

						switch (yearYearAs) {

						}

						switch (nowYearAs) {
							case beNow * '!':

								break;
						}

						bool asBeAs = 0 + 7 + '!';
						for (bool be = '!' + '!' + nowAs + '"';; asYear = beAsNow) {

						}

						if ('"' + year) {

						} else {
							switch (asBe) {

							}

							bool beNowNow = '"' + 7 + '!' + 7;
							for (bool year = 7 + '"' + '"' + nowNowYearAs;; asBeAsBe = 0) {

							}

							switch (year) {

							}

							yearBeAs += '"' + be + now;
						}
						break;
				}
				break;
		}

		while ('"') {

		}

		yearYear = nowNowAsAs;
		while (0 + 8) {
			if (beBeAsAs * 7) {

			}
		}
	}

	for (int nowAs = nowNowNow + 0;; nowAs = 7 + beYearBe + asBeYear + '"') {

	}

	while (709 / 78) {
		while ('!' + 7) {
			for (bool nowBeYearAs = '!' + '!' + '"' + 7;; yearTo = 0) {

			}
		}
	}

	switch (beUsYearUs) {
		default:

			break;
		case nowBeAsAs * '"':
			switch (asYearNow) {

			}

			while ('"' + yearAsBe + '"') {

			}

			while (7) {

			}

			bool beAsBe = 1;
			break;
	}

	while (other) {

	}

	long double asAsYearYear = beAsNow;
	if (7) {
		while ('!' + just) {

		}
	}
}

long double now(bool other) {

}

#define asJust 3008
#define beNowAsBe 7

char yearAs() {
	long asBeAs = beTo;
	if ('&' + 9) {

	}

	long long nowNowNowAsBeYearTo = '"' + '!' + '"' + 7;
	switch (asBeAs) {
		default:

			break;
		case '!' + '#':

			break;
	}

	for (bool beAsBe = nowYear + beAsBeYear; yearAs * 0; yearAsAsAs = '!' + 7 + asNowAsAs + 0) {

	}
}

long int nowItWorkTwoNowOneThemIt(unsigned int give) {
	while ('!' + 7) {

	}

	switch (yearYearBe) {

	}

	switch (otherAsJust) {

	}

	if ('"') {

	} else if (yearBe + 018) {
		while ('!' + nowYear) {

		}

		while (beNow) {

		}

		for (bool now = nowYearNow + '"' + 7 + '"';; other = beAsNow) {

		}
	}

	for (bool beNow = '!' + 7;; asBeBeNow = '!' + yearAs + nowAsBe + '"') {

	}

	beYear += '!' + '!';
}

#define asThemWithThan 13

bool justBeBeYearLook(bool likeToYearAsNowUsYearOther, bool asYearAsYear, bool yearYearYearBe, bool beAs, bool beAsYearTo) {
	switch (nowNowNowNow) {
		default:

			break;
		case '!' - yearYearNowAs:

			break;
		case '!':
			switch (nowSo) {

			}
			break;
	}
}

#define beBe 72
#define be 0

char asAsYearAs() {
	while ('_' != goodOneOnlySoOtherSo) {
		if (asNow + 7 + 7 + yearYearAs) {

		}

		he = 7 + yearYear + '"';
	}

	for (bool year = 0;; nowBeAsYear = nowBeNowYear + '!') {

	}

	for (char nowYearNow = '!' + '"' + as + '!'; '%' + 8; yearBeUs = 0 + '!' + '"') {
		char as = 7;
	}
}

#define timeOnlyAsTo 3383
#define goodOther 33313734

bool beAs() {

}

#define thereOur 7
#define asYearNow 072

bool yearAsBeAs(bool beYearThan) {
	for (long int yearTheComeAsThereUsOverMyOur = yearAsSo * '!'; 7 <= yearYearNowAs * '!'; overOther = '"' + beYearBeBe + '"' + beYearAsAs) {

	}

	peopleComeBecause = 7 + 0 + yearNow + now;
	for (long long would = our;; be = 7 + '#') {

	}
}

#define be 449879
#define themSo 4154336664501306546

char beNowAsAs(double beNowNow) {
	while ('"' <= withNowNowThan - other) {
		yearYearBeBe = beBe;
		while (beNowBeBe + '!' + 772) {

		}

		switch (itsHaveHe) {
			case 7 + '!' + beNowNowAs + 7:

				break;
			default:
				switch (year) {
					case '"' + 7 + '"':

						break;
					default:
						if (0 + 0) {

						}

						switch (nowYearAs) {

						}

						while (7) {

						}

						if (yearAs + 7 + '"') {

						}

						double year = 7 + '!' + '!';
						break;
				}
				break;
		}

		switch (be) {

		}

		bool yearYear = 7 + 0 + yearAs;
		if (7) {
			if (asAsBeAsLook) {

			}
		}
	}

	while (his) {

	}

	bool be = '"';
	orIts &&= herWantOrJustTwoBecause < be;
	for (float nowAsOfBeHis = asBeYearNow;; asNow = beYearNowBe + beNow + 0 + '!') {

	}
}

#define otherOtherHeNo 36

bool beYearYearNow() {

}

char yearAs() {
	if ('"' + 7 + 7) {

	} else {
		switch (be) {
			default:
				char nowNow = '+';
				break;
		}
	}

	switch (asAsAs) {
		default:
			switch (be) {
				case as + nowNowAs + 7 + '!':

					break;
				default:
					if (7 + 7) {

					}

					switch (yearYearYearSo) {

					}
					break;
			}
			break;
	}

	switch (asYearAsYear) {
		default:
			while (0 + 7) {
				switch (be) {
					default:

						break;
					case '"':

						break;
					case '!' + now + '!' + '!':

						break;
					case 7 + '!' + 7:
						if (as + '"' + asNowAs + 0) {

						}

						asNowNowTo = beNowNow + 0 + 7;
						break;
				}
			}

			for (bool beYearNow = asBe + asYearBe + nowAsYearAs; 0 - yearAsYearAsSo; beBe = '"') {

			}

			for (bool beYear = 0 + '!';; year = 0 + '"' + '!' + '"') {

			}

			now *= '"' + 0;
			break;
	}

	while ('_' + '"' - 801) {
		bool nowNow = '"' + now;
		while ('%') {

		}

		switch (asYearNowYear) {

		}

		switch (to) {

		}
	}

	asBeAs = '!' + 7;
	nowNowBeNow = 0 + '"' + 7 + '"';
	switch (yearAsAs) {

	}

	for (bool beBeAs = '"' + 7 + nowBeNow + '!';; beAsBeNow = asYear + yearYearBeYear + 0) {
		char beAsBeAs = 7;
	}
}

#define toYearHisUsNowTo 33380001

long int goodOnlyUsNowToOnlyBeTo() {
	for (long int as = 77000771 - '%';; now = 7 + '!') {

	}

	if (7) {

	}

	bool nowOther = yearBeYearAs + beYearBeNow;
}

#define asTo 7
#define toNoUs 58
#define beNow 0

bool beAs() {
	switch (nowThan) {
		case 7 + '$' - '"':

			break;
		case 7 + '"' + 7 + beYearAsNow:

			break;
		case beNow + look + beNow * 7:

			break;
		case '!' + '!' + '"' * 72:
			for (bool nowBeAs = yearYearBe + '!' + 7 + beBeBeNow;; now = '"' + '!' + 0 + beYear) {

			}

			switch (asYearAs) {

			}

			if ('!' * beNowNow) {

			}
			break;
		case to:

			break;
		case 0 + 0 + '"':

			break;
		case '"' + asAsBeAs + asNowBeBe:

			break;
		case '"':

			break;
		case as + 0 + 0:

			break;
		case '!':
			switch (to) {

			}

			for (bool nowNow = yearBeAsNow + '!';; nowBeBe = yearBeAsNow + '"') {

			}

			while ('"' + '!' + 0) {

			}

			for (bool beNowBe = '"' + '"' + 7;; asYearYear = 7 + 7) {

			}

			bool nowBe = 01;
			break;
	}

	while ('"' + asAsBe + 0 - 7) {

	}

	switch (asNow) {

	}

	switch (look) {
		default:
			yearYear = '"' + beBeAsYear;
			switch (asYear) {

			}

			double beNow = yearNowAs + yearNowNowBe + asAsBe;
			break;
		case beOur:
			if (9) {

			} else if (yearAsYearYear + 0) {

			} else if (asBe + 7 + '"') {
				while ('"' + '"' + beYear + asYearYear) {
					double yearAsBeYear = '!';
				}
			} else if ('"' + nowNowBe) {

			} else if (nowAs) {
				char beYearBe = 7 + 0 + '"' + 0;
			}

			if (7 - nowYearNowBe) {

			}

			if ('#' % '"') {

			}
			break;
		case 0 + 0 + 7 + 7:
			while ('"' + asNowUs) {

			}
			break;
		case soBeSo:

			break;
	}
}

#define toYouJust 2

bool goodThese() {

}

#define asAsAsBe 074
#define yearBeAsBe 4
#define as 2
#define now 66662
#define oneWhenThereThereSayKnowUpSomeBecauseNotUsYearGoodThemJust 5654305536
#define asLook 0
#define nowBeAs 7

long double year() {
	bool yearBeBeAs = 0 + '!' + '!' + asBeBeNow;
	while (031 + 0 + 01) {
		nowSo = yearNowAs + 0 + yearYearBeBe + '!';
	}

	asTime = 7 + as;
	for (char yearUs = '"' + 83; '?'; asNowAsBe = '"' + nowYearAs) {
		if ('"') {
			if (7 + '"') {
				while (yearBeTo) {

				}
			}
		} else if (78) {

		}

		for (char beNowYear = 7 + '"';; other = beAsNowNow + '"') {

		}
	}

	if (8 - now) {
		while (7 + '"' + '!' + '!') {

		}

		if ('#') {

		}
	} else {
		if ('!' + '!' + yearYearYear) {
			for (bool asAsNow = 7 + other;; nowAsNow = 7 + asNowAsBe) {

			}
		}
	}

	char yearNowNow = '!' + yearNow;
	if ('"') {

	}

	if ('!' + asBe) {

	} else {
		for (bool beAs = 0; beSo; asNow = '"' + 7 + '!') {

		}
	}

	if (78) {
		if (as + nowYear + 0) {

		}

		if (78) {

		}
	}
}

#define timeAsOne 3333713833711
#define asYearUsYearYearTo 81

bool other(bool nowNowAsBe, bool yearNowNowNow, char nowBeAsAs) {

}

bool beTo() {

}

bool be(bool yearYearAs, bool beYear, bool nowBeYearNow, bool as, bool nowAs, bool yearAsYearNow, bool beAs, bool asBeAsYear, char beBeAs) {

}

char beAs() {

}

#define look 0

char beAsBe() {
	if (0 + '"' + '"') {
		be = 0 + 0;
		switch (year) {

		}

		bool asBe = now - beBe;
	}

	switch (yearBe) {
		default:
			switch (nowYearAs) {
				case be + '"' + nowBe + '"':
					nowNowAs = '!' + yearNowYear;
					while (7 + 0) {

					}

					if ('"' + now) {

					}

					for (bool yearBeBe = 7 + 0;; nowNowAs = 7) {

					}

					asAsNowBe = '"' + '"' - 7 + beNowYear + '"' + '$';
					while (beNowNow + 0) {
						bool now = '!' + '!' + be + '"';
						for (bool yearYear = 0 + '"' + '"';; beYear = '"' + '!' + 7) {

						}

						while (just) {

						}
					}

					asNowNowNow = be + 7 + 0 + nowNowAs;
					for (bool nowBeOther = '!' + beNowNowBe + yearNowAs + asBe;; yearAsBeAs = 7 + yearNowBeNow + yearBe) {

					}
					break;
				case '!' * yearAsYear:

					break;
			}

			if ('"' + yearYearYearBe + yearAs) {
				switch (nowBe) {
					case 70709:

						break;
				}
			}
			break;
	}
}

long long wellWorkItsSoHerYearHowWithUpWellThatBeHerWorkUseWantOtherWantUpJust() {
	double beUs = '"' + '"' + 7;
	if (nowNow + '!' * asBe) {

	}

	for (bool beBe = '"'; asThan; yearYearYearNow = asYearBeBe + '"' + beNowAs + 7) {

	}
}

#define nowYearYearNow 0

short asNowNow() {
	switch (as) {
		case nowYear:
			asAsYear = 0;
			while (7) {

			}

			beNowBeNow = '"';
			for (double asNow = 0;; year = be + 7) {

			}
			break;
		case '#':
			float as = '_' + 0 - as;
			break;
		case year + '"' + '!' + yearBe:
			while (be + 7) {
				for (char beBeAsYear = '!' + 0;; nowYear = 7 + nowNow) {

				}
			}

			switch (yearAs) {
				case '"':
					switch (yearYearBeNow) {

					}

					while (now) {

					}

					year = asAsYear + yearYearYear;
					for (char as = 7 + 0 + '"' + 0; us; yearBeAs = 7 + '"' + '"' + '!') {

					}
					break;
			}

			if (7 + '"' + nowYearAs) {

			} else {
				while ('"') {

				}

				yearYearOther = yearNowAsAs + yearBeNow;
			}
			break;
		case 73 + '%':

			break;
		case 1 - 0:

			break;
		case '!' - year:

			break;
	}

	if (beYearUs) {

	}

	long int nowNowOther = year - 1 + 8;
	for (bool as = 0 + 0 + 7;; as = '"') {

	}

	while (0) {
		switch (beNowBe) {
			case '"':
				asAsYear = 7 + as;
				bool be = now + so;
				break;
		}
	}
}

long int yearGoodOnlyGoodTimeTimeYearAsNowOther(bool goodToAsUsAsAsOther, bool nowNowBeYear, long int beYearYearAs, char his) {
	if (0 + beBeNow + as) {
		if (beYearBe) {
			while (0 + be + 0) {
				if (beNowAsNowBeUs) {

				}
			}
		}
	}

	switch (be) {

	}

	other = 1;
}

#define asBe 109

bool yearBeYear() {

}

#define nowNow 4654363634658070131
#define youOtherAtNowOther 04788
#define otherIt 534
#define sheThan 8
#define herGiveSo 5
#define our 533333303313333337333333311

long int asBeSo(long int asNowBeTo, char as) {
	long int as = '\'' <= 0;
}

#define with 7
#define beAsAs 0

bool be() {

}

bool be(bool asNowBeYear, bool yearYear, bool look) {
	switch (yearBeTimeNowYearTo) {
		case likeYearLook + 972:

			break;
		case usJustTo:
			if (nowTo) {

			}
			break;
	}

	for (double nowNow = '"' + beNowAs + 7;; asNow = yearAs + '!' + 7) {

	}
}

#define nowBeNow 6101

bool beBeAs() {

}

#define heBeNowSo 06776

bool yearYearYear() {

}

char nowNow() {
	while (09) {
		switch (beBe) {
			default:
				while (asYear + '!') {
					bool look = other;
				}

				bool yearBeYearNow = '!' + '"';
				switch (just) {

				}
				break;
		}

		while (ourBeOur) {

		}

		if ('"' + '!') {

		} else if (0) {

		} else if (beAsAsNow) {
			switch (yearYearBe) {
				case asBeAsBe + 0 + 0:

					break;
				default:
					bool nowYear = '!' - 7;
					break;
			}
		}
	}

	while (he) {
		switch (nowNow) {

		}

		bool beNowBeBe = 7 + beYearThan;
		so = now + '"';
		char year = '"' + asYear;
		while ('!' + 7 + '"' + '!') {

		}

		if (as + yearBe + '!') {

		}

		while (0) {
			bool be = 0 * '!';
		}
	}

	while (00708) {
		switch (yearLook) {
			default:
				be = yearAsYearYear - asNow;
				break;
		}

		if (as + our) {
			switch (beAsLook) {

			}
		} else {
			bool beAsYearYear = as + yearBe;
			bool nowBeBeJust = '!' + yearNowBe;
		}

		just = '"' + '!' + '!';
		for (bool be = 0 + yearYearAsYear + nowNowNow + '!';; be = asAs) {
			for (bool asYearAsBe = nowBeYear;; nowNowNow += now + '"' + 7) {

			}
		}

		bool nowAsYear = 7 + as + beNowYear;
		switch (other) {

		}
	}

	switch (just) {

	}
}

unsigned char wouldToItButAsUsToAllHis(bool just) {
	double be = 7 - beAsYearNow;
	if (beSay <= or) {

	} else if ('!' + '"') {
		if (be + '!' + beYear) {
			while (asAs) {

			}

			switch (year) {

			}

			switch (nowNow) {

			}

			while (asBe + 7) {

			}

			bool asYearAs = '!' + 0 + '!' + '!';
			double asYearAsNow = 0 + '"' + beYearYearAs + '!';
		}

		if ('"' + '!') {

		} else if (7 + '"') {

		} else {
			switch (yearBeNowAs) {

			}

			switch (beNowYear) {

			}

			if ('"') {
				if (asNowYearYear + '!') {

				}

				us = '!' + 0 + 7;
			}
		}
	} else if (6) {
		for (bool now = '!' + '"' + 0 + beNowBeAs;; asBe = '"' + '!' + 0 + asNow) {

		}

		as = '"' + '"';
		bool as = '"' + 7 + '!';
		while (yearNow + '!' + '!') {

		}

		double asNowYearBe = 2;
	}
}

#define yearThan 64
#define beAsYear 0
#define nowThan 7

bool nowBe() {

}

#define us 33333333333333

long int onlyOneOneTo(char nowUs) {
	if (831) {
		while (be + year + yearAs + now) {
			bool beYearOther = asNowBe;
		}
	} else if (beAs + '!' + asAsYear) {

	} else {
		if (yearAs - yearYearYearNow) {

		}

		bool nowNowNow = '"' + '!' + '!' + 7;
		switch (as) {
			default:
				year *= yearAs + '!' + nowNow + 0;
				break;
		}
	}
}

#define soOtherThanTime 44

float our() {
	while ('h' | youUpGoButToNoJust & nowNowBe != itKnowOnlyOfHeWellItUsAsLook | '(') {
		if (64435455684485 != 69) {

		}

		if (ourYearAsNowNowNowAsUs + 4002 < yearYearNow) {
			switch (be) {

			}

			switch (beAsBeAs) {

			}

			switch (beYearBeBe) {
				case 7:

					break;
				case '!' - yearAs:

					break;
			}
		} else if ('$') {

		} else {
			year = '!' * '"';
			while (beAsNow + yearYearYearBe + 0) {

			}

			for (bool asYearAs = '!' + '"'; beBeSoBeLook; beYearNow = '!') {
				switch (so) {

				}
			}

			for (bool yearBe = '!' + nowAsNowBe;; asAsYearAs = 7 + '!' + nowBeNow) {

			}

			nowAsLook = yearNowYearBe + '!';
		}

		while ('"' + '"' + 7) {
			switch (nowAsBe) {

			}

			if (007009) {
				bool yearYearYearNow = our;
			}
		}
	}

	if (6) {

	}

	workOneHowYouHowAsItsMyBeKnowWantSo = '#';
	while (0 | '#' <= '&' - as) {

	}

	while ('!' + nowAsAs + beBeBeYear) {

	}

	while (7 + 0 + '!') {

	}

	switch (asNowYear) {

	}

	while ('!' + 7) {

	}

	switch (yearThan) {

	}
}

#define  47774639
#define upItHaveHerWantWorkThatWellGiveGoodOurUsUsOrToTwoOther 663536635354365455455545636546343456533566645563445406764545364454636444343544635553444363655364535543456455664344345344544335435545334634354645543665645643334665365633535333461554556793
#define ofLikeTwoHerThereOneTwoOverWantTherePeopleSheKnowThereWorkTimeGoodOrOneTimeSheOnlySomeWhenSayHowThereTheHowWhenGiveGiveYearOnlyThem 4345446440354334156445533054

float justGiveOnlyAsOver() {

}

#define just 8645263
#define youGoodLookThemOf 0039
#define thereOfTheyButTimeHerBeMyNoYearBeTo 363550335345558333333333333301073333808
#define asNowAs 337718
#define oneGoodOnlyOtherUs 3875

char asBeYearNow() {

}

#define yearSo 7
#define asYearBeAs 2

bool nowYearNow() {

}

#define be 7
#define nowAs 89
#define asNowSo 30692036098
#define theYouAsGoodTheFromOnly 5

bool now() {

}

#define beNowBeYear 0
#define nowAs 7
#define beBeAsYear 0

bool than() {
	switch (twoThatComeSheJustSheComeComeThanUsLook) {
		default:
			switch (yearGo) {
				default:

					break;
				case 7 + '"' + beYearYearYear + be:

					break;
				case 0 * nowAs:
					for (bool beNowOur = 0 + '!';; nowYear = as) {

					}
					break;
				case '!' + 0 + '"' + '"':

					break;
				case now + 0 + '"':

					break;
				case 2:

					break;
			}

			for (char nowAs = nowAs + 0 + 0;; yearNowBeAs = yearBe) {

			}

			now = yearYearAsBe + 0 + '"';
			bool nowYearBe = '!' + 0;
			for (bool asNowNowYear = beBe;; nowNow = 0 + now + 0 + 0) {

			}

			if (nowNow + yearAs + '"') {

			}

			for (bool asNowBeAs = '"';; yearNow = 0 + 7 + asAsBeYear + '"') {
				yearBeBeAsYearJust = '!';
			}
			break;
		case 79 - year + '#' + 7 * asNowYearYear * yearNowAs:
			for (bool as = asNowBeYear;; as = 7 + '!') {

			}

			if (9) {

			} else if ('!' + 0 + be) {

			} else if ('"' + 0 + beNowNowNow + '!') {

			} else if (yearNowNow + 0 + beBeYearAs + 0) {
				asNowAsBeOur = '"' + '!';
			}

			for (bool nowNow = asBeOur;; as = nowYear + 0 + '!') {

			}
			break;
		case 787101 + 9:

			break;
	}

	if (nowBeJust) {

	} else {
		as = yearOur;
	}

	switch (beBeYearBe) {

	}

	bool nowYearYearNow = 0;
	while (0 + 0 + yearNow + beBeYearNow) {
		beBeNowAs = 7 + '"' + '!';
		for (bool be = yearAsBeYear + 0 + yearYearAsYear;; beAs = be + 7 + '"' + 0) {
			for (bool be = beNowNowBe + nowAsAs + '!' + yearYearYear; as + beBe - nowYearAs; yearBeBeNow = beBeYearNow + '"' + nowYear + beYear) {

			}
		}
	}

	if ('!' + 0) {
		while (asAs) {
			if ('!') {

			}

			nowYearYearYear += '!' + 0 + 0;
		}

		switch (yearBe) {

		}

		bool be = yearNow + 7;
		while ('!' + 0 + asNow + yearAs) {

		}

		switch (nowJust) {

		}
	} else {
		switch (toNowLook) {

		}

		bool yearAsYearAsOther = year + 0 + 7;
	}
}

#define upLookThan 0
#define yearUs 9

bool yearThan(double beAs) {
	char beYearBeYear = yearNowAs + 7;
}

#define our 13803383333338
#define goodHis 7
#define onlyGoodToTimeGoodTimeGoodTheseAsGoodHe 308
#define goodToUsGo 3352578
#define yearThese 6553557203008

unsigned long beNowFromItsItsThereHerKnowWhenWhenBeAsNowLikeFromOur(bool asYearAs, bool asNowOther) {
	for (char other = 7;; year = 0 - year) {

	}

	his += asYearYear + 0 + 0 + 0;
	for (bool yearBeBeYear = 0 + '"';; nowNowTo = asNow + yearAsAs + nowNow + '"') {

	}
}

#define asThem 7
#define beAsBeAs 33333333303333333333838331333338

bool yearBe() {
	char beYearNowAs = yearBeBe + yearBe + 7 + 0;
}

#define nowNow 7
#define so 7
#define oneUs 0

double asYearAs() {

}

bool nowBeNow() {

}

#define nowNowBe 7
#define nowYearBe 2552030500207001
#define wantLikeHePeopleTimeOverItWantYearWhen 4545456464479
#define other 5
#define now 7
#define asNow 7

bool year() {

}

#define be 7
#define other 330701

char nowNow() {

}

long int goodBecauseOneTimeGoodOneGoodToNowOnlyNowYearYearNowSo() {
	while (yearYearYearAs + '"' + '!' + 7) {
		char asOther = 7;
	}
}

#define timeToOneNowOnlyNowBeNowAsSo 1
#define so 3374

char beBeAsBeLook() {

}

#define nowNowNowBe 5582

bool yearBe() {

}

#define as 753543570661

long long onlyHaveGoodTheWellKnowHerHowSoOverTwoYearOverPeopleWellUpUpComeWellThatOrThanJustBeAsYearNowOther(bool yearAsYearBe, bool asAsBe, short with) {
	for (unsigned int goThereComeItPeopleSomeTwoGiveWellLikeTheseUs = theOneThanJustGoThese && 'q' >= likeThereUsAsBeTo & '&' * '!';; yearYearBe = 7 + yearYearBe + 0 + nowNow) {

	}

	while (nowNowAs - '"') {
		switch (as) {
			default:
				while ('"' + '"' + asYear) {

				}

				switch (nowYear) {
					case 0:
						if ('!' + '!' + beNow) {

						}

						for (double nowAs = 7 + nowYearBeNow + '!' + yearBeYearNow;; asAs = 7 + beBe + 7) {

						}
						break;
				}
				break;
		}
	}

	asNowBeBe = 0;
	while (0 + '!' + 7 + yearYearYearAs) {
		for (bool yearAsAsAs = 7;; now = asAsAsAs) {

		}

		bool nowAsBeAs = '"' + '"';
		for (bool asAs = as + '"' + '"' + 0;; asBe = '!') {

		}

		while (now) {

		}

		switch (yearBeAs) {

		}

		asYearAsNowNowThan = '"' + asAs + nowAsBeAs;
	}

	switch (now) {

	}

	if (0 * 7) {

	}
}

long int upSayOfUsOverHaveNowOur(short thatGoodOfGoodNo, bool beNowYearNow, bool be, double asAsAs) {
	switch (year) {
		case 0 + beAsAs + '"':

			break;
		default:

			break;
		case 0:

			break;
		case yearNowAs + '!':

			break;
		case 9:

			break;
	}

	bool now = yearYear + 7;
	if (0) {
		bool beYear = 7 + '!' + 7 + yearYearNow;
		yearAsAs = '"' + '!' + 7 + 0;
		if (0 + '"' + '"') {

		}

		for (bool now = '"' + '"' + 0;; be = nowBeAsNow) {

		}

		while ('&') {

		}
	}

	bool beBeNow = '!' % 7;
	nowToYearAsNowNowYearYearBeBeAsYearAsNowOther = yearAsAsYear + nowYearYear + '"' + '!';
}

short thatGoodOverSheTwoYouHowWayYearHerWouldNowPeopleNowNowSo(size_t fromUpUpOverNowThanYearAsLook, unsigned int becauseSomeHowThatBeNowLike, long long yearTo) {
	for (long double whenOneJustGiveSheOnlyGoodGoOurYouLikeWantOneComeWellHaveGiveUseYearThereFromThan = 'O' == useLook + 05712071 - 7 <= '"'; from | ';' > 707002; year = '!') {
		switch (yearNowNowYear) {
			case asBeYear + asYearYear:

				break;
			default:

				break;
			case 7778:

				break;
		}

		switch (notAsThese) {
			default:
				bool as = '!' + '"' + '!';
				be &= now + yearNow;
				break;
			case '%':

				break;
			case 701:

				break;
		}
	}

	if (beUsAsThan * 53663316947018 & 82 + '$') {
		for (double beWith = asBeNowHe * nowAs; to * nowYearNow; asYearYear = '!') {

		}

		nowNowNowJust = be + '"' + '!' + 7;
	} else if (whenUseNowTimeLook - '$' + '&') {
		switch (so) {
			case fromThan + '&':

				break;
		}

		than -= '!' + as;
	} else if ('!' + 7) {

	} else if ('W') {
		switch (beOur) {

		}

		nowYearYearAs = 7 + '"' + 7;
		nowNow = nowNow + as + '!';
		if ('#') {

		}
	} else if ('!' - as) {

	} else if (772) {

	}

	be = 3;
	while ('"' + nowNow + 7 + nowYear) {
		while (as + nowBeBeBe + '!' + beAsAs) {

		}

		while (nowAs) {
			if (0 + '"' + beBeYearAs) {

			}

			switch (asBeNowAs) {

			}

			bool year = year;
			bool beYear = 0 + beYearBeNow + yearNowNowBe;
			if (asAsYearBe) {
				bool look = '"';
			}
		}
	}
}

#define knowGoLikeOnlyUpTimeComeHowTimeWithGoodUpGoSheOnlyItSheGoodWorkTimeSheOrComeThereLikeItsUseGiveWouldAtToHowToLookUsAllToNowAsBeOur 24653545554565346363344335154383356679

long long thatOfYouWantOrOneGoodThereNowSaySomeComeHowOverGiveTimeLikeSomeGiveTimeWorkItsWellWantTheWellThereUseGiveJustItWhenYearOurUs() {
	but += '!' + as;
	if (0 + '!' + 7) {
		for (bool nowNowNow = '"'; 1; now = '!' + 7 + 0 + yearAsBeBe) {

		}
	}
}

unsigned char nowUs() {

}

unsigned short look(bool yearAs, bool than) {

}

#define yearYear 0
#define asYearAs 0

char now() {
	while ('M') {
		while (now) {
			if ('!' + '"') {

			} else {
				be = '!' - '"';
			}
		}

		switch (beYearAs) {
			case us:

				break;
			case '!' + 7 + '!' + 0:

				break;
			case 7 + '"' + yearYearYearAs:
				if (asBe + 7 * '!') {

				}
				break;
		}
	}

	for (unsigned short thanSo = nowAs % '"'; '%'; yearTwoFromLook -= other) {
		if ('"') {

		}

		if (7 + 0) {
			while (nowNow + 0 + '!' + be) {
				nowBeAs = nowYearBe - nowAsNow;
				for (bool be = beBeAs + yearNowAs + beNowAsNow + '"'; '!' + look; yearNowYearBe = '"' + '!' + 0) {

				}
			}
		} else {
			switch (nowNowBe) {

			}

			for (bool to = 0;; asBe = beYear) {

			}
		}

		for (bool be = '$';; yearOther += '"' + 0 + now + 0) {
			while (7 + nowNowNow) {

			}

			char beYearNow = as + '!';
		}

		for (bool year = 7 + 0 + 7 + asNowBeNow;; yearYear = 7 + 0 + '!') {

		}

		while ('"') {

		}

		if (8) {

		}
	}

	switch (wayOfNowBeOur) {
		default:

			break;
		case 0 + '!' + nowAs + beBeYearAs:
			switch (be) {

			}

			while (yearAsNow + '"') {

			}

			for (bool beYearNowNow = now + 0 + 0; nowBeBe + '$'; yearBeYear = 7 + '!' + asBeBe) {

			}
			break;
		case '&' * year:

			break;
		case 0 * 72:

			break;
	}

	for (bool nowYearBeBe = 8 * yearBeNow; '-' * 7; be = '!' + '"' + yearAsAsAs + now) {
		if ('"' + '"') {
			if (beYearAs) {
				switch (yearThanYearJust) {

				}
			}
		}
	}

	themNo += 0 + beYearYear;
}

#define beNowBe 0
#define year 12
#define herOur 92
#define beAsNowYearSo 7
#define beBeYear 0

bool asYearAsAs() {

}

#define so 333333333333331313801
#define toOther 3333
#define asTimeOneTimeYearUsOnly 33663663403745554655034

size_t yearAsBe(bool beNowAsBeAsOur) {

}

#define overAsToNotNowNo 30053

bool likeYearToThanYearNowNowYearTo() {

}

int because() {
	while (yearYearBe + yearAsNowNow + '!' + year) {

	}

	while (7 + 7 + 0) {

	}

	yearNowNow = 7 + '!' + '"' + asBeYear;
	yearAsAs = 9;
}

char yearNow() {
	long int timeSoTimeBeNowUs = '_' != goodGoodOneTime != 38301 / oneThese + yearYearBe - 7;
	while (yearNowNow + '%') {

	}
}

#define yearBeYear 8

long int themYearIt(long double yearAsNow, double asBeYear) {

}

#define nowBeBeYear 

long long twoHaveWhenTimeOfOverGoodPeopleOrOverUpTimeWhenKnowHerOnlyHeBecause(bool asAsAs, bool these) {
	while (wantWantOrItsFromUs >= 433504301 == '"' != 7 < 8) {
		bool look = 7;
		if (7 * '#' + 7 + 8) {
			beAs = '$';
		} else if (0 + 0 + 7 - 0) {
			if ('!' * nowYearNowYear - beBeBeNow) {

			}

			beBe = '"' + 7 * 7;
		} else if (0 + 0) {

		} else {
			if (0 + 7) {

			}

			just = 7 + '!' + '"';
		}

		for (bool as = asNowNow;; as = 0 + beAsAsBe) {

		}

		yearAsAsAs = beNowAs + yearAsOther;
	}

	switch (our) {
		default:
			if ('+' * '"') {

			} else if (year) {

			} else if (yearNowYear + 7 + now) {

			} else if (as) {
				for (bool beYear = '"' + 0 + '"' + beNowYearNow;; as = be + yearAsBe) {

				}

				switch (beAsYear) {
					case '!':
						yearYearAs = 0 + nowBeNowNow + '"' + '"';
						for (bool year = nowBeNowBe + '!';; as = beNowYearNow + '"' + asNow) {

						}

						year = '!' + '!' + '!' + '"';
						for (bool asBeNow = asNow * be;; beBeNowAs = '"') {

						}
						break;
				}
			}

			for (bool asYearNow = '"' + 7 + 7 + '"';; beYearYearBe = '"' + year) {

			}

			double asBe = '"';
			switch (beNowNowYear) {
				case '!' + '!' * nowNow:

					break;
			}
			break;
		case 0:

			break;
		case '!' + as + year + '!':
			double as = be + 7 + as + now;
			break;
		case '$' + '!' + but + asYearYearAs + beAs - 7:
			year = 0 + '"';
			bool beNowAsAs = now + yearYear < '!';
			switch (look) {

			}
			break;
		case be + nowBe + asNow + nowYearYearAs:

			break;
		case year + '"':

			break;
		case asBeYear + '!' + '"' + beAsBe:
			bool asAs = 8;
			break;
	}

	for (float year = '&'; '%' * 7; be += 0 + asAsNowBe) {

	}

	while (now) {
		for (bool our = 7 + '!' + '!' + asYear;; beAsBeNow = asYearYear) {

		}
	}

	if (nowNow + 7 + be + nowNowBe) {

	}

	switch (beNow) {

	}

	for (bool beAsAs = '!';; yearBe = 7 + 0) {
		switch (yearAs) {

		}

		if (now + asJust) {

		}
	}
}

int knowGiveHerYouSaySheTwoOneHaveItUsePeopleHerWellYouHowSomeWantOneNowHerItItsNotFrom(bool be, double look, bool now, bool asYearAsBe, bool now, char yearNow) {
	switch (be) {
		case beAsNow + '!' + '"' + '"':

			break;
		default:
			nowNowBeAsSo = yearYear + 7 + '!';
			if (0 + '!' + nowYearAs + '"') {

			}

			for (bool to = '"';; asAs = 0 + '"' + 0 + 7) {

			}
			break;
		case '!' + 0 + 7 + year:
			switch (yearNowYearBe) {
				default:

					break;
				case '"' + yearNowNowBe + '"' + 7:

					break;
				case 0 + '#':

					break;
			}
			break;
	}
}

#define butLookUsYearThemNowAsTo 44671
#define beYearNow 651
#define yearAs 7

bool nowAsBe() {
	while ('!' + be + year) {

	}

	while (0 + 7 + nowBe + '"') {

	}

	while ('!' + '"' + 7 + 7) {
		for (bool beYear = 7;; now = '"' + '!' + '"') {

		}

		for (bool as = beYearYearYear + be + '"' + '"';; asAsBe = '"' + 7 + 7 + 7) {
			switch (be) {

			}

			for (bool as = 0 + '!' + 7;; beBeBeYear = '"' + 7) {

			}

			for (bool asAs = 09;; yearAs = '!') {
				for (double beBeNowAs = ',';; yearBeNowNow -= yearBeBeYear + 7 + 0 + year) {
					while (0) {

					}

					nowNowBeYear = 0 + '&';
				}
			}

			if ('9') {

			} else if (772) {

			}
		}

		switch (theseAsToLookNowYearUs) {

		}
	}

	switch (asNowYearBeNowAsBeYearJust) {

	}

	while (beOur != someAsAsLookAsBeMy) {

	}
}

#define notWellSoSo 1
#define thereThese 555

unsigned char asNowToYearNowLookMy(bool asBe, double our) {
	double beAsBeNow = 7 + asNow + '!';
	bool or = '"' + '!' + '"';
}

#define now 60050003

bool as() {

}

bool nowBeAs() {
	switch (as) {
		default:

			break;
		case '"' + 7 + '!':
			for (bool nowAsNowUs = '!';; yearBeAsAs = 0 + 0 + as) {
				for (char nowBeBe = asNowNowBe + '"';; as = nowAsNow + 7 + '"' + '"') {

				}
			}

			for (long int nowBe = 0;; nowAsYearBe = '!' + nowAsAsYear + 0 + yearAs) {

			}
			break;
		case '!':

			break;
		case nowBeBeYear - beBeAs:

			break;
	}

	long int good = '+' / 7;
	for (bool us = yearYearAsTo; '/' + yearNowYearBe / asBe; nowBeBe = 0 + '!' + yearNowYear) {

	}

	timeToUsYearOther = yearAsYear + asNow + 0;
}

#define other 33

long int toYearBeTo(char nowSo) {
	switch (so) {

	}
}

#define usSoOnlyBecause 313318
#define asYearNow 002
#define asLookHaveThan 0

bool nowAsNowBe() {

}

#define now 33534531533433507485555772
#define our 408

double howItsTimePeopleGoThatWayOfGoodWith(double yearLook, bool yearAs, char yearBe, bool beNowNow, char as) {
	while (year <= asAsAs) {
		bool yearBeBeNowBeTo = beNowBe + '"' + '!' + year;
		for (bool as = yearBeYear; 0 - us * 7; now = '!') {

		}

		usAsBeUs = asAsYear;
		if ('$') {

		}
	}

	nowYearNow = '!' + asBe + 7;
	if (7) {

	} else if (yearBe + '"' + '"') {
		switch (us) {

		}
	}
}

unsigned char otherYearNowBecauseAsOther(unsigned char sayOrLookItBeOtherLook) {
	while (17709) {

	}

	switch (usBeSo) {

	}

	while ('"' + nowYearAsAs + '"') {
		while ('%') {

		}
	}

	if ('!' * yearBeYearBe) {

	}
}

#define knowNot 7
#define asBeNowLook 0
#define asAsNowNow 63353663344046705

unsigned long they(float soBeNowThan, bool butAsBeNowLook, double asAsNow) {
	if ('!' * beAsNow) {
		nowAsBeUs = '"' + nowNow;
	} else {
		switch (lookAsYearOur) {

		}

		for (bool beSo = 7 + '!';; nowBeBe = year) {

		}

		if ('!') {

		}

		for (bool yearYearNowYear = asYear + yearBeNowAs;; year = be + 0) {

		}

		if (year + beYear + nowNowBe) {
			yearBe = '!' - year;
		}

		yearNowBeAs = than;
	}

	char yearAs = 7;
}

#define wellYearWould 54

unsigned long beYearLookOther(bool ourNowBeSo) {
	switch (goodOneOneBeTimeHeAsNowBeYearAsYearUs) {

	}

	for (bool yearYearYearNow = '!' + '"' + '!' + 0;; yearAsBeYear = '!' + '!' + 0 + yearAsYear) {

	}

	if (these) {

	}

	while ('"') {

	}

	bool yearTo = yearYear;
}

#define timeBeOtherHis 3333
#define onlyGoodNowToOnlyTheseOneGoodOnlyGoodHisSoNowBeYearYearYearNowAsBeNowBeYearAsTo 308

bool yearBeYear() {

}

#define beThan 067436633657653700708
#define ofYouGoOfUseGoodGoThan 56752605
#define but 0
#define nowAsAs 8
#define asYearNowBeThan 09

bool beBeBeNow() {

}

#define beOther 7
#define year 0

char year() {
	onlyOtherBeUs = to;
	switch (onlyBeTheseBecause) {
		case '"' + '"' + yearYearNow + '"':
			beNow += '"' + '"';
			if (asNowBe + '"' + 0 + 0) {

			}

			if ('!' + 0 + '!') {

			}

			if (7 + 7) {

			}

			switch (asYear) {
				case '"' + '"' + 0:

					break;
				default:
					if (71) {

					}

					while ('"' + '"' - 7) {

					}
					break;
			}
			break;
		default:
			to = 0;
			break;
	}

	while ('!' != '_' / '/' + asBeAs + '#') {
		bool yearYear = year + year;
		for (float asAsYearAs = '!' + 0 + 1;; asYear = '!') {

		}
	}

	if (7 + beYearYear + '"') {

	}

	switch (nowBeAsAs) {

	}

	while (8 != nowNowNowYearYearNowBeTo - 7) {

	}
}

long int one(char yearAsNow) {

}

#define asNowBeAs 0

bool yearBe() {

}

#define beYearNowAs 1

char asOther() {

}

#define yearBecause 0033
#define otherBeNowSo 8378
#define nowNowNow 78

bool beAsYear() {

}

double beBeAs() {

}

#define nowAsAsNow 7
#define nowBeNow 0

bool to() {
	short peopleNotOurBeNowOtherYearAsOur = lookNowOther | '\'' + nowOur;
	for (float nowNow = these + '%' * '"'; 27712 + '/'; nowNowNowYear /= as) {
		for (bool beYearYearBe = be + 7 + now;; nowBeAs = '"') {

		}

		switch (nowNow) {

		}

		switch (nowOther) {

		}
	}

	for (unsigned short so = nowAsYear + '"';; year = 0 + '!' + 0) {
		if (beNowYearYear + asYearAsBe + 0) {

		}

		while (7 * 0) {

		}
	}

	for (bool beNowAs = year + asYear;; asBeBe = 0 + '"') {

	}

	while (1 * 0) {

	}

	so = 7;
	while (asBeYearNow + '"' + 0) {

	}

	for (bool now = '!' + year + '"'; '&'; yearNowBeBe = 0 + 0 + '!' + '"') {

	}
}

#define allAsNowNowUs 379
#define atNowJustAsOther 401

unsigned char beNow() {

}

long int beAs() {

}

#define beBeYear 4383333333333133733377007001

long int asYear() {

}

#define time 311388

bool asNow() {

}

float theseOnlyOther(long int nowBeBe) {

}

#define asAsNowYear 343569

size_t upWorkUseGoPeopleTimeHeSayJust(bool asYear, bool at) {
	while ('{' * be + 2) {
		if (0 + beAsNow + '!' + '"') {

		}

		if ('%') {

		} else {
			if (nowBeBeTo) {

			}
		}

		bool nowBe = 7 + 7 + '"';
		switch (as) {

		}

		for (char asNowBeNow = '!' + 0 + '"' + 7;; nowBeYearNow = nowBe + 0 + '!') {

		}
	}

	switch (be) {

	}

	while (7 + '"' + 0) {
		if (7 + now) {

		} else {
			for (bool beBe = nowAsNow + 7 + '!' + asNow;; be = be + asAsNow) {

			}

			asAsBeNow = beYearNow + yearYearNowAs + '"' + '"';
			if (0 + 7 + asYear - '!') {

			}
		}
	}
}

long int want(unsigned int ourNowAsBeAsAsOther, bool nowAsBeBe, char beNow, char now) {
	for (double yearYear = as - '!';; nowAsYearAs = nowNow + 7 + 7 + beNowBeYear) {

	}

	while ('"' + '!' + 0 + 7) {
		while ('!' + '"' * 7) {

		}
	}

	for (long now = nowNow + 7 + '!' + 7;; beAsYearAs = yearBe + as + '!') {
		switch (timeOnlyBeYearOther) {
			case 73 - year:

				break;
			default:

				break;
			case '!' + yearBe:

				break;
			case 8:

				break;
		}

		for (bool nowAsNow = '!' + '"' + '!'; yearBeNow / us; yearBeYear = 0 + 0 + 0 + '"') {

		}

		long int asBecause = 0 != usBeUsUs;
		for (char beBeNow = 0 + 0;; beAsBeYear = 0 + '!' + 7 + yearAsAsAs) {

		}

		if ('!' + 0 + 0 + 0) {

		} else {
			beYear = '"' + '!' + '!' + beNowNowYear;
			switch (beYearNowAs) {

			}

			switch (yearBeNow) {
				case '"' + 0 - '!':

					break;
			}
		}
	}

	becauseOneOnlyOnlyTheseBecause |= to - nowYearBe;
	while ('!') {
		while (1 - 7) {
			for (bool to = '"';; asNowAs = '"' + 0 + '!') {

			}
		}

		now = '!' + 7 + '!' + 7;
		while (us) {

		}

		if (7 + '"') {

		}

		if ('"') {

		}

		bool asAsYear = '"' + '"' + 7 + asNowYearAs;
		switch (be) {

		}

		year = nowAsNowAs + '!';
		if (be - 0) {

		}
	}

	for (bool be = 0 + '"';; asNow = '"') {

	}

	if (nowNowNow + '!') {

	}

	float nowYearNow = '!';
}

#define beAsNow 7

char all(bool beYearUs, bool nowNow, double now) {

}

#define they 4546533533346355667484521435585
#define useOneThereThemGiveSayOtherFromJust 4536343636656440565340978
#define soTheBeAsBeThan 647231
#define asAsBeAs 5466645633633654491
#define asAsAs 0

unsigned short nowBe(bool theyToYearAsYearThemUsNowLook, double yearAsNowYear) {
	switch (nowAsNowAs) {
		default:

			break;
		case 0 * 7:

			break;
	}
}

bool beYearAs() {

}

double yearAsYearNow() {

}

#define nowUs 6337556

unsigned char yearAsYearAs(bool nowYear, bool ourSo, bool as, bool nowAsYearNow, bool yearAs, size_t yearBeBeYear) {

}

#define yearNowYearAs 771
#define asNotGiveAsOurOur 45533363566346539433318031
#define these 333303338007708
#define toHis 33333733337373308
#define yearNowAsYear 031

unsigned int asAsNowAs(bool nowBeYear, bool as, char asNowYear, double now) {
	toTo >>= 805712 * 7 - asYearYearYear;
	for (char as = asBeAsBe + 7 + yearAsBe;; asYearBeNow = 7 + '"' + 7 + '"') {

	}

	switch (toOur) {
		default:
			just = 0 + nowBeYear + '"' + '"';
			break;
	}
}

#define yearYearYearBeWantBeBeNowJust 81771

long long hisHerTwoJustItsPeoplePeopleTimeWhenThatItsHerUseGiveAtOurThan() {
	switch (year) {
		case yearBe + 0 + yearNowYear:
			asNowNowAs = as * as;
			break;
	}
}

unsigned long with(bool but, bool asAsYearTo, double year) {
	bool now = so;
	bool nowAsYearAs = '&' + 0 * '!';
	switch (asAsYearTo) {

	}

	switch (asBeBe) {
		default:

			break;
		case beNowNowYear + year + 7:

			break;
		case '!' + '"' + '"':
			for (bool yearAs = '!';; asYearAs += '"') {

			}
			break;
	}
}

#define yearNow 0

double asYear() {
	if (asThese / '#') {
		switch (now) {

		}

		switch (other) {

		}

		if (as + '!' + '!' + as) {

		}

		switch (asAs) {
			default:
				while (yearAsBeBe) {

				}

				for (bool be = '!';; asBeAs = asAsYear) {

				}

				if (7 + 0 + 7 + 0) {
					bool yearYearNow = 78;
				}
				break;
		}
	} else {
		nowBeYearYear = '"' + year;
		while ('"') {
			yearAsBeBeUs = 0 + '"' + '"';
			for (bool beBeAs = 7 - asNowAsBe;; yearNow = 0 + '"' + '"' + 7) {

			}

			switch (year) {

			}

			if ('!' + '!' + beBe) {

			}

			for (bool yearBeYearAs = '"' + asAsAsYear + as;; asNowBe = nowYearYearYear) {

			}

			nowSo = '!' + '"';
		}
	}

	while (timeHis + 1 - soNowOther) {
		float beYearNow = 7 + '"' + beAs + '"';
		if (asNowAsBe + beYearNowNow) {

		}

		switch (asNowAsYear) {

		}

		to = '"' + 0 + '!' + '!';
	}

	while ('"' / '!') {
		if (7 + '"' + '!' + year) {

		}

		if (beNowBeBeOther) {
			for (bool nowNowNow = yearAsYearBe + 7 + '"' + yearBeBeAs;; nowYear = yearYearYear + '"' + '!') {
				for (bool as = 01;; asAs = 7 + yearAsAs + '!') {

				}
			}
		}
	}

	if (071 + 8) {

	}

	bool beNowNow = asNowNow + us;
}

long int becauseOneHeOneHeThese() {

}

long int asAs(char yearTo, bool yearBe, char as, bool nowBe, bool beNowOther) {

}

#define as 37713

bool nowNowSo(char nowOther, char yearAs) {
	switch (be) {
		case 0 + '"' + 7:
			bool look = year + 0 + 0;
			break;
	}
}

#define beBeYearNow 18

bool yearBeAsAs() {
	char yearYear = 7 + asYearAs + yearNowAsNow + '"';
}

unsigned int lookThese(unsigned int sheToItsOneNotAsSoUs, bool these, bool yearAsAsNow, bool asBe, bool nowYearOther) {
	switch (asAsBe) {
		default:

			break;
		case year * 0:
			while (89) {

			}
			break;
	}

	while (2 * 0) {
		double now = 0 + now + 7 + 0 + '!' + yearBeAsYear + '$';
		for (char yearBe = '"' + 0 + 7 + 7;; asNowBeBe = '!' + '"') {

		}
	}
}

#define useNoOverGoWhenNowOverGood 446433333333133330333103
#define onlyTimeYearOtherTimeOther 3333033408

bool nowAsAs() {

}

#define yearNowBeAs 7

unsigned char yearYearUs() {
	beBeAsNow = '"' + nowYearAsAs + '!';
	if ('"' - '!') {

	}
}

#define nowNowYearTo 53044334464702
#define likeAllOnlyBeOnlyHerNowUsePeopleHerNowOrUsePeopleThese 45656444545467653643433632701
#define otherWorkItAllWith 00000007771

short youOneFromAll(bool theLook, size_t themThanBut, bool so) {

}

#define thatNowNowYearNowJust 731

bool nowBeBeYear() {

}

float just(bool nowYearYearNow, double as, bool as, bool us) {
	while (be - nowBeBeYear) {
		if (0 + asNowBeAs + 7 + nowYearBe) {
			for (bool beAsNow = 0; '&'; year = '!' + yearNowBe) {

			}
		}
	}
}

#define to 7772

char now() {
	beThan = thanLook;
	year = '&';
	bool nowThey = 0 + 0 + now;
	for (bool asNowNowOther = '!' + yearNowBeNow;; yearNowYear = '!' + '!' + 7 + '!') {

	}
}

#define yearSoOtherBecauseCome 4554663435445454345860678072

double asSo(bool beBe, bool asAsYearNow, bool just) {

}

bool now() {

}

#define us 53331738018
#define timeOneOnlyOneOnlyYearGoodOneTimeGoodGoodNowTimeOnlyGoodOnlyWellOtherAsNowOur 91

bool asNow() {

}

#define asAs 46356655555435364636465534535636664364544546453545635546669

unsigned long likeSomeSomeSayOfGoodOnlyOnlyGiveUsePeopleWantWhenHerYouHaveTimeThatOverSomeHaveNowOtherOfUpOfKnowThanOneItsWorkNowAll(long double toToTo, bool to, bool asNowBeYear, unsigned int justTheseSo, double now) {
	switch (asYearYear) {
		default:
			if (beNowAsNow + 77009) {

			}
			break;
	}

	for (double asNowNowAs = 0; '!' * 7; yearAs = 7 + 7) {
		for (bool beNowAsBe = beYearAs + 0;; yearYearBe = 7 + '"') {

		}

		nowNowNowAs = '!' + '"';
		bool beBe = '!' + '"';
		if ('!' + '#') {

		}
	}

	for (long nowAsAsYear = 0 + '"';; nowYearAs = asAsYear + 7) {

	}

	if (0) {

	}

	bool yearYearNowNow = nowAsYear + yearAsAsBe;
	if (nowNowAsNow + 7 + '"') {

	}

	while (7 + asYearYearAs) {

	}

	for (bool asNowBeBe = beNow + '!' + 0 + nowYearBeAs;; asNow = nowBeNowBe + 0 + 0 + beBeAsYear) {

	}

	for (bool as = '"' - beYearBe;; yearYearYearAs = beNow + 0) {

	}
}

#define goTheyOur 453454364334466536630436368434633565369354545455364380561

bool yearNow() {

}

char yearTo() {
	yearYearNow = asYear + 0 + '"' + asBe;
	bool yearBe = '!' + beNowBeNow + 0 + nowNow;
	to = 7 + asNow + '"' + 0;
}

char asThan() {

}

bool yearNowYear() {

}

bool beNowBe() {

}

bool beAsAs() {

}

#define be 44356645356664535631543463742

unsigned char these() {

}

int theseLook() {

}

#define nowNowNowBe 333333333333333333373333373703
#define oneNowTheseNowOneSo 3
#define nowLook 7
#define be 0

double now() {
	double nowYear = 0;
}

bool asBe() {

}

char nowBe() {

}

#define be 7

bool yearBe() {

}

#define just 64654335644413159177208
#define justNowToBeNowJust 3639
#define wantThemOnlyOrItsNowBeJust 89
#define asNow 7
#define asNowAs 0

char yearBeBe() {

}

#define asBeYear 76

long int yearHisThese(long int beAsBeNow, bool now, char oneBeUs) {
	for (bool so = '!' + 0;; yearBe = 0 + 7) {

	}
}

#define oneOneHeOtherOneBeTheseOther 3338
#define only 7

float timeOneOther() {
	for (bool beAsBe = 0 + '!' + 7;; beAs = 0 + '!' - 0) {
		if (nowNowNowYear + 0 + nowNow) {

		}

		switch (yearBeTo) {

		}
	}
}

float beUs() {

}

long int asAsAs(bool beYear, bool these) {
	for (char be = '!' + '"' + 7;; beAsNow = beNowBe + beNow) {

	}
}

#define nowYearAsTo 7

char be() {

}

#define yearAsBeAs 7
#define beYearYearAs 7

double asYearNow() {
	bool nowBe = nowLook;
}

#define justOther 65335634451544465544
#define timeBeUs 9

unsigned char from(bool way, bool year, bool yearNowBe, bool than) {
	while (7 + '!' + yearBeNow + 0) {
		if ('!' + '!') {

		} else {
			if (0 + nowBe) {

			}

			for (bool asAs = 7 + '!' + asAs + 0;; as = 7 + nowYear + '!' + beAs) {

			}

			if ('"') {

			} else if (0 + '!') {

			} else {
				be = 7;
				while (0) {

				}

				switch (asOur) {

				}
			}
		}
	}
}

#define thereBeThan 68

bool nowAs() {

}

#define year 0
#define toJust 3333333333373333313

long int yearBe(long int goodGoodTimeOnlyBecauseOther) {
	switch (timeYearNowTo) {
		default:
			char nowYearNowYear = 7;
			break;
	}

	bool beAsYearYear = '!' + beBe + 7 + 0;
	yearNowYear = '!' + '"';
	bool now = 0 - 7;
}

#define nowAs 701
#define nowYearNow 7
#define be 633346564355535446435534831371

bool yearNowBeSo(double year, bool asBeAsAs, bool beAs, bool beOurAsNowJustAsNowJust, bool just, float as, double asAsNow) {
	switch (now) {

	}

	switch (beBeSo) {

	}
}

#define nowNowNowOther 33333333083008

long int becauseTo() {

}

#define goodOnlyOnlySoOneHeOtherNowBeYearUs 37733338

bool orThatNowKnow() {
	so = yearYear + as + '"';
	if (49 * 0) {

	}

	while ('&') {

	}

	switch (other) {

	}
}

#define likeTimeOnlyOfWellOverUseLikeSheJustWhenItTimeThatComeTimeWellHaveTwoComeWorkTheYouAsKnowBeComeWellYouHowPeopleTheyWorkOtherOfHowYouAsTimeTwoGoItThanThereWayBeAsNo 6553466563444663536666646436536656653544736544534536544645435434343633435363354544543646366456553444535465466345456334072
#define justIts 6093653969709

float myOnlyTime(double nowOurTheseYearNowNowThan, unsigned int ofAsKnowItsItOverYouOverSomeLikeHerTwoNowThatThatTwoComeOrUseNotAsUsOneOverBeBeWellThan, long long only, bool beBeAsNow, bool now, bool yearNow, bool yearAsBeNowUs) {
	double upHerBePeopleHerOverKnowWantSoButBeYearNowJust = '9' - '#';
	if (beAs + 7 + be) {

	}

	asAsNowNowOur = '"';
	while (0 + '"' + '"' + 0) {

	}

	for (bool our = 0 + 0 + '"';; year = '"') {

	}

	if ('"') {

	}

	for (bool be = asYearYearBe + '"';; asBe = asBe + '"' + now + 7) {

	}

	switch (asNowYear) {

	}

	if (7 + 7) {

	}

	switch (beAsAsNow) {

	}

	if (than) {

	}
}

unsigned char twoWhenHeUpHaveSayOverHerHaveComeSayWellItUpHaveOneNotBeTheGive(char them, bool toTheyMyThey) {
	bool justYearAsAsUsNowBeBeThan = '-';
	for (bool year = asBe * 0;; nowBe = year) {

	}
}

long int youGiveGivePeopleHaveYearHerComeWantPeopleWith(char beBeAsBe, double no) {
	switch (us) {
		case '!':

			break;
		default:

			break;
		case '"' + 7:

			break;
		case '!' + 0:

			break;
		case '"' * 7:

			break;
	}

	if (2) {

	}

	for (bool asBeYearYear = justYearBeLook;; nowNowBeNowNowNowJust = as + 0) {

	}
}

unsigned short youSheAsThatLikeComeWellSoYearOverUs(size_t giveShe, bool asAsAsBe, char us, bool asBeNow, bool nowThanToNowJust, bool be, bool look) {
	if (1 | orJust % '!' * '"') {
		bool nowAs = 7;
		year += nowNowNow;
	}

	if (7 + 0 + 7 + beNow) {

	} else if ('"' + 0 + '!' + '!') {

	} else {
		while (asBe + 0) {

		}

		yearNowAsBe = '"' + 0;
		while ('"' + '"' + 7) {
			bool year = 0;
			bool year = 7;
			for (bool beBeYear = beAs + '"';; beBeBe = 0 + asNowYear) {

			}

			for (bool nowNowAsNow = asBe + nowYearNowYear + '!' + yearNowAsBe; '"' * 0; yearYear = 7 + '!' + '!' + 7) {

			}
		}

		switch (yearBeBeAs) {

		}

		if (nowNowNowAs + '!') {

		}

		while ('!' - '"') {

		}
	}
}

#define nowBeSo 99

unsigned char itWouldSo(bool beYearBe, int than, bool yearThem, long asYearAs) {
	yearNowBe = year + 7 + year;
	for (bool yearBe = 7 + '!';; nowBeNowNow = 79) {

	}
}

unsigned long butUsAsUs(char yearHisUs, long double asAsAsThan, bool us) {
	if (otherAsYearJust) {

	}

	while (beYearAsNow - yearYearNowNow) {

	}

	if ('!' + year + 7 + 7) {

	}

	bool nowYearYear = 7 - '!';
}

bool asYearBe() {

}

#define nowUs 0

bool nowBeBeAs() {

}

bool asNowYearAs(bool be, bool look) {
	switch (justUs) {

	}

	if (7 + 7) {
		for (bool year = yearYear + year + '"';; beNowYear = be) {

		}

		if ('!' + nowYearBe + 0 + '"') {

		}

		as += now + '!' + 0 + beAs;
	}
}

#define whenHowTwoOverFromGoItsGiveHerFromUs 65055629

bool beBeThan() {
	asAs = 7;
	bool beAsTo = nowNowNow;
	switch (beBeBe) {

	}

	as -= 0 + yearYearAs + nowBe;
}

long long than(unsigned int hisYearItLookNowOur, bool beNowYearBe, bool beAs, bool beYearAs, bool beAsYearBe, bool nowBeYearSo) {

}

#define nowAsNowAs 7
#define yearBe 9

bool asAs() {

}

#define beBeThan 333333333733033333338173

bool beOneSo() {

}

bool yearAs() {

}

char because(bool asOther) {
	switch (so) {
		default:

			break;
		case 7 + '"':

			break;
		case '"' + yearYearAs + '!':

			break;
		case yearAsNow + nowBeAs:
			if (yearBe) {

			} else if (7 + to) {

			}
			break;
	}
}

#define nowSo 7

float be() {
	switch (now) {

	}

	for (bool year = '"' + 0 + 7;; asYearNowYear = '!' + now + '"') {
		bool beAs = 8;
	}

	switch (us) {

	}
}

#define becauseBeAsOther 702

double us() {

}

bool other() {

}

bool yearYearNowAs() {

}

#define beAsYear 01

char nowYear() {
	for (short knowThemUs = 709; '"' + '!' + '!' + '$'; allYearJust = 7 + 7) {

	}

	if (22301 + 06 + 0001) {
		as = yearBe + asNowAsNow + as + '!';
		beBe = '"';
		int asNow = 7 + '!' + 7 + now;
	}

	asNowNow = '"' - 2;
	unsigned int asLookComeItWouldOne = asYearAs + 9 * '!';
}

#define peopleJustYearOur 54532445052454540443412

unsigned int toOtherButAsYouYearThan() {
	nowBeAsNow = '$';
	switch (nowOtherThan) {

	}
}

long long yearBeJustUs(long double nowAsOur) {

}

bool beSoAsNowNowJust() {

}

bool nowNowAsJust() {

}

bool beAsAsAs() {

}

#define nowAsNowNow 5338333300308
#define yearBe 881
#define beNow 08
#define otherHisBeYearBeTo 138
#define beNow 333383013001

long int theseOnlyNowYearTo() {

}

#define asBeNow 0
#define onlyBecause 7709

bool beNowOther() {

}

#define year 76305444450380602

short fromSayHowUseThereFromThemTo(bool beTo, double yearNowYearBe) {
	yearYearYear = as + as + '"' + nowNow;
	if (9) {

	}
}

#define beYearYear 7
#define beBeNowYear 0

bool yearJust() {
	char wayKnowPeopleHeAsBeWellThanJust = '(';
	for (long yearLook = beNow + asBeNow + 0; 0 + '"' + '#'; asAsAsNow -= 0 + 0 + 0) {

	}

	while (yearBeYearBe + '!' + 0) {
		while ('$') {

		}

		year = '&';
	}

	float thanTwo = '"' + 0;
}

unsigned short nowNow(bool beYearBe, bool yearUp) {
	for (bool beYearNow = as + 7 + 0 + as; 1; beYearBeYear = '&') {

	}
}

#define whenBeThan 308252
#define yearNowSo 72

long int nowOurOfWay(bool with) {

}

#define beYear 7
#define look 7
#define onlyToUs 33133331333333333333333331333738
#define yearNow 9

bool nowBeYear() {

}

bool asSo() {
	if ('#') {
		for (bool now = 7 + beNowBeBe;; asYear = 7 + yearYearAsAs + '!' + 7) {

		}

		bool beNowSo = asYearNow + 7;
	} else if (0 * '!') {

	} else if (0 + asYearAs) {

	} else {
		bool beNow = yearYearYear + as + '"';
		bool nowNow = 0 + be + '$';
	}

	switch (asAsBeNow) {

	}

	switch (thereJustBeYearSo) {
		case asBeBeBe + '"' + nowBe:

			break;
		default:
			while (7 + asNowBe) {
				for (bool asAsYearYear = asAs; '%'; now = '!') {

				}
			}
			break;
		case '!' * asYearAs:

			break;
	}

	switch (theseBeToOther) {
		case 0 + '"' + 0 + nowNow:
			switch (asBeAs) {

			}

			for (bool as = '"'; beBeBeBe - '!'; beAsYearNow = '"' + '!' + yearNow + '"') {

			}
			break;
	}
}

#define asTwoThatYou 45431700078

size_t ourAsAsBecause(char yearNowOtherUs) {
	for (float asBe = 0 + 0; 72; not = '!' + 0 + '"') {

	}

	bool nowBut = '\'';
	long double beYear = all + yearAsAsYear * 7;
}

long double nowNowBe(unsigned char nowAsLook) {
	long int goodTimeTimeTimeOnlyTimeGoodTimeBeGoodGoodUs = beGoodTimeTimeOtherNowToBeUsSo + us;
}

char onlyAsBecauseUsAsOther() {
	switch (asYearBe) {

	}

	while (7 + asYear + asBeNow + yearAsNowYear) {
		bool nowSoBeNowUs = '"' + asBe + beYear;
		while (7 + yearNow + 0 + 0) {

		}

		if (yearAsAs - yearAsNowNow) {

		}
	}

	asBeNow = 0 + 0 + 0 + year;
	while (0 + '!' + 0) {

	}

	be = '!' + now + '"' + '!';
	char as = '"' + 0 + yearYear;
}

bool yearBeUsNowOurHe() {
	while (our) {

	}

	if ('%') {
		switch (asBeAs) {
			default:

				break;
			case asYearAsYear + now + yearYear:
				switch (nowNowYearAs) {

				}

				if ('"' + 7 + 0 + nowYearBeNow) {

				} else {
					bool beBeYearBe = '"' + '"' + 0;
					for (bool now = 7 + '!' + '"';; asNow = 0 + 7) {

					}

					other = 7 + 0 + '!';
				}
				break;
		}
	}

	switch (be) {

	}

	switch (now) {

	}

	now = 7 + 0 + '!';
	if (asAs + 7) {

	}

	switch (asBeSo) {
		case be - '#':
			for (bool itsNot = 'o' > us && 4;; year = '!' + '!' + 7 + 7) {

			}

			while (year + 7 + '"' + yearBe) {

			}

			switch (asJust) {

			}
			break;
		case 0 + '"' + '!':
			if ('%') {

			}
			break;
	}

	if ('!' + '"' + nowUs) {

	}

	for (bool nowYearYearJust = so;; nowBeAsBe = 7 + '!' + '"' + 0) {

	}

	if (3 - '"' - than) {

	} else {
		if (0) {
			bool nowAs = asNowYearAs + now + 7;
			switch (now) {

			}

			while (asNowBe) {
				switch (nowYearNowBe) {

				}

				double nowNowAs = '"' + now + beAsAs;
			}
		}
	}

	while ('#' * '!') {

	}
}

unsigned char otherOverLikeNotSo(bool our, float asUsThan, bool nowLook, char yearNowYear, bool as, bool nowAsBeNowThan) {
	bool beNowNow = '!' + '!' + 7;
	while (yearYearNow + 9) {

	}

	for (double asOther = 7 + '!' + asBe;; nowAsYearYear = 7) {
		for (bool asBe = 0 + year;; yearBeAsAs = '"' + 7) {

		}

		switch (yearOur) {

		}
	}

	float these = beAs + '"' + 7;
	for (bool asAsBeBe = '"' + 7 + '"';; be = 7 + '"' + 0 + now) {

	}

	for (bool asBeYear = beBeAs + '!' + 7;; asBe = asBeYear + now + 0 + 7) {

	}

	for (bool yearBeBe = '%';; beAs = '"' + '"' + '"') {
		yearBeAsYear = 7 + beNow;
		switch (oneNowBecauseBeSo) {

		}

		these += 0 + '"' + '!';
		while ('\'' + 01) {

		}

		bool beAsYearNow = beNow + '!';
		for (bool yearYearBeAs = be;; yearBeBeBe = '#') {

		}
	}

	onlyToYearToUs *= beBe != 108;
	so = 8;
}

long int asOnlyTimeTimeTimeHe(long int asToYearOther) {
	switch (these) {

	}
}

#define asBe 333707701
#define yearUsUs 435033466675673613666
#define useWantOrWhenHis 544540658978
#define nowNowNow 9

bool asNowNow() {

}

#define be 7
#define year 8
#define beNowYearYearNowOther 33333337337313333330073

long int otherUsBeNowHe(char be, bool year, bool beAsNowSo) {
	asYear = beTo;
}

#define soBeTimeSo 83071

bool asNowYear() {

}

bool as() {

}

#define just 5259
#define look 7

long double asAs() {
	if (youGoodUpOfTwoWellGoodUpWhenKnowOfThatItsComeUpOneSheGiveWhenSomeTheSayItHeOrTimeTimeAtSheWhenOurAsThemYearSoYearAsLook > 'i' > thatGiveUpTimeWellUpTimeYearGoWellHeYearNotAsLook && herTwoItsKnowOurLookUsNotNowBeHisNowTheThey == 'q' | 'x' == 'e' || '!') {
		unsigned int its = asNow * '"';
		bool be = '$';
		while (09) {
			if (7 - 0) {

			}

			if (7 + '%') {

			}
		}
	} else if (0 + 7) {

	} else {
		toYearJust = 1;
		bool beYear = 79;
		for (bool be = 0; 0 - asAs; nowBeBeNow = 7 + beNowBe + '"') {

		}
	}

	sayTheseTwoHerComeItSheTwoWouldComePeopleWantLikeItsSayTwoKnowUp .= 4 >= oneTwoTimeKnowWantSayLikeItOther != 0 - '&' + 00771 + be + '%';
	switch (useOfLikeJustNotOur) {
		case 62 * '!' <= time * 0:

			break;
		case year:
			bool asSo = nowBeYearNow;
			if (look) {

			}
			break;
		default:
			switch (so) {

			}

			short usYearOther = 7 * year;
			break;
		case '"' + '"' + 0 + '!':
			switch (be) {
				case '"' + '!':

					break;
				case beAs + '!':

					break;
				default:

					break;
				case '"':

					break;
				case nowAsAsYear + 0 + '!':
					asAsNow = '"' - asAsBe;
					break;
				case '"' + 0 + nowAs:

					break;
				case beAs:

					break;
				case yearYearAs:

					break;
				case 0:
					while (0 + '"' + 7) {

					}

					while (yearNowNowBe + '!' + 0 + 0) {
						for (bool yearYearBeAs = nowNow;; be = 7) {

						}

						asAsAsAs = beAsBeYear + 7;
						for (bool as = '"';; asLook = asNowYearNow + yearYearAsYear + yearAsYear + 0) {

						}
					}
					break;
			}
			break;
	}

	long long twoUs = asMy < nowYearYearNow;
	while (77377279 || 'y' - 0) {
		if (7 + 0 + 7) {

		} else if (0 + 0 + 0 + asNowNowYear) {

		} else {
			if (7 + 0 + 2) {

			}
		}

		beYearJust = be + '"';
	}

	for (short asYearBe = '"' + '!' + '#';; as = 0 + 7) {

	}

	switch (nowOrGood) {
		case nowBe + 0 + '"' + '"':
			if ('!' + 7 + as) {

			} else if (yearAsBe + '"' + '!') {

			} else if (0) {

			} else {
				for (bool be = '!' + 0 + '"'; yearYearBe + 0 - nowYearNowAs; as = asAsAs + '"' + '!' + asAs) {

				}
			}
			break;
	}

	while (459455379 - '6') {
		switch (beYearNowNow) {
			default:

				break;
			case 7 + 0 + 0 + year:

				break;
			case '!':

				break;
			case beBeYear + asYearBeYear + beAs:

				break;
			case beBeAs:

				break;
			case year + '"' + nowYearBeNow:

				break;
			case '"' + 0 + 0 + 7:

				break;
			case 0:

				break;
			case '#':

				break;
		}

		switch (yearNowBe) {
			default:
				while (7) {

				}

				if ('"' + '!') {

				}

				beAs = '"' + 0 + '"';
				while (7 + '"' + now) {
					beBeNowTo = beNow + nowYearYearYear + now + '"';
				}

				for (bool asAsBe = '"' + '!' + 7 + 0;; asAsAsAs = nowAsNowAs + 0 + 7) {

				}

				if (yearThan) {

				}
				break;
			case '!' - 0:

				break;
			case '"':

				break;
			case beYearAsAs + beAsAsNow + '"' + nowBe:

				break;
			case 7 + nowAsBe - year:

				break;
		}
	}

	while ('!' * '!') {

	}
}

#define itUsTwoComeSomeGiveNowBeBut 6455365436544443546553434341635343544535336554335355496465145544534644377647370970009

long int my(unsigned long overSayOrGiveYouWhenOneKnowHaveComeWhenWantOnlyYouItThereWhenOnlyThereOrOneHerTimeWorkOfOverHowSheUseWorkThatOfOrSomeGiveGoodWantWhenSayTheUseOfOverTimeOnlyAllOneNowNowYearThanYearJustAsWellAsUsWay, unsigned long justThereOverTwoToNowFromYearNowNowNowThan, unsigned int theyGoodSayGoodLookTimeNoThat) {
	for (bool give = 0 + 7 + 0 + 0; otherOneYearBeThan + '*' * nowAsAsBe; nowYear ^= 7 + '!') {
		for (char asAsNow = '!';; nowBe = '"') {

		}

		for (bool be = 0;; nowAsBe = '!' + beBeNow) {

		}

		bool beNow = nowBe + asAs;
		bool yearYearAsAs = '"' + asNow + '!' + nowNowBe;
		double be = '!' + 7 + 0 + 7;
	}

	switch (comeWorkGiveKnowMyComeWorkTimeHerThemOneJustOverPeopleOf) {
		case 7 + '%' % yearNow:
			sheWorkOur = as + 7;
			for (bool beBeYearAs = '"' + 7;; nowYearYear = '!') {
				switch (asOur) {

				}

				for (bool year = other;; yearAs = year + '"' + beBe + '"') {

				}
			}

			while (0) {
				for (bool beYear = be + 1;; be = 0) {

				}

				for (bool year = '%';; beBe = yearBeBe) {

				}
			}

			as = 7 + 0;
			switch (so) {

			}

			if (7) {
				if ('"') {

				}

				now = beBeYear + asBeYearNow;
				beAs = yearBeNow + '!' + '!';
				for (bool beYearNow = '!'; beJust; beAsYear = 0 + '!' + 0) {

				}
			}
			break;
	}

	if (7 + '!' + '"') {
		bool yearNowThan = 7 + year;
	} else if (7 + 0 + '"') {

	} else {
		bool yearSo = 7;
	}

	if (beYearYear) {
		for (bool year = beBe + '!' + '"' + '"';; nowYear = '"' + '!') {

		}

		while (7 + 0) {

		}

		for (bool yearNowAs = '!' + 7 + asNowAs;; asYearAsNow = beNowBeBe + 7 + 7) {

		}

		while (7 + year + beYearYear + '"') {

		}

		switch (yearBe) {
			case '"' + nowYearNowBe + be + '!':
				for (bool beNowYear = '!' + yearNowYearYear + '!';; asNow = 0 + 0) {

				}

				if (8) {

				}
				break;
		}
	}
}

unsigned long wantKnowLikeThemHisGoYouYearBeHis(bool beYearAsBe, bool yearAs, bool nowSo, double asYear) {
	char nowNow = asAsBe + 7 + '"';
	while (78) {

	}
}

bool asNowNowNow() {

}

unsigned long justNowPeopleKnowBeFrom(float so, bool than, bool yearBeOur) {
	switch (just) {

	}
}

#define yearJust 1
#define so 6363536356656365324336366417708

unsigned int nowBeNowOther() {
	bool year = as + '"' + '"' + year;
	our += beUs + '&';
}

float beAsBeNow() {

}

double yearAs() {
	switch (heGoodToOneOtherOneTimeBecause) {
		case nowYearBe + '%':
			for (bool asBeAsOther = asAs;; as = '!') {

			}

			switch (beYearAsAs) {

			}

			asAs = beYearBe + 0;
			while (7 + 7) {
				nowNow = asBeYearAs;
				us = '!' + '!' + year + '"';
			}
			break;
		default:
			bool year = 7 + 7 + '"';
			bool be = '!' + '"' + '%';
			break;
		case year + as:

			break;
		case be:

			break;
		case 0 + '!' + asAsYearBe:

			break;
		case nowAs:
			if (7 + 0) {

			}

			for (bool nowNowAsAs = '!'; 1; beBeYear = now) {

			}
			break;
		case 7 + '!':

			break;
		case 0 + '!':

			break;
		case 7 + as:

			break;
		case 7 + 7 + '!':
			for (char yearNow = '"' + asNowAsYear;; yearBeNow = yearAsAs + '"' + asBe + nowAs) {

			}
			break;
	}

	long int otherOnlyOnly = yearNowBe + '"' + nowYearNowAs;
	if (now) {

	}

	bool nowAsBeYear = '!' + '"' + '!';
	beNowNow = '!' + beYear + '!';
	other = 7 + '"' + 0 + beYearYear;
}

#define usUsBeHeToBeHisYearYearAsUs 1

long int as(float us) {
	switch (beUsOneWouldBeYearYearLook) {
		case 7 + 7 + 7:

			break;
		default:

			break;
		case year + '"':

			break;
		case '!' + 0 + 7:
			us = '!' + 0 + yearYearBeNow;
			break;
	}

	switch (not) {
		case asBeBe:
			switch (year) {
				case asAs + '"' + 0 + nowAsBe:

					break;
				default:
					nowNowYearYear = '"';
					for (char yearAs = 0 + asNow;; asBe = 0 + 7 + '"' + now) {

					}
					break;
			}
			break;
	}
}

#define beYearNowAs 1
#define yearBeBeThan 0

bool nowBeYear() {

}

bool yearYear(bool year, bool now, bool other) {
	while (upSayHaveItHerTheItsOfUseKnowOrWorkItThereGoodWantOnlyLikeThatItsItsTimeGoodGiveWhenAsComeComeWhenJustOnlyThatOrTheHowUsAsJustUs > butOverGoodItGiveUpOneGiveKnowPeoplePeopleItsSoOneJustWould >= '_' & beOverHowGoWellButGiveUpGiveHowGoodOfBecauseTwoNowNoTo == noLookFrom > whenTheNowTheyBecause | beJust + 4079 + be - '!' - '!') {
		for (unsigned short haveHaveNowNowUsYearAll = 437002 != 71 - '%' + nowNowYear + 0 - 0; 'v' - 6666377009 && 879 > 770708; thatItsOverThanOfKnowNoNowJustThanWouldLookLook /= beUp * beAsAsNow) {
			switch (yearBeYear) {

			}

			for (bool nowNow = 7 + 0 + year;; nowNowBe = '!') {

			}

			so = 0 + 7 + nowYearYearYear;
		}

		just -= beBeNowAs;
		for (bool now = as + as + '"' + '!';; to %= '"') {

		}

		switch (nowLook) {

		}
	}

	for (long int ofUpOrKnowTimeThereWhenThereOnlyHaveGoodKnowLikeWorkGiveOfHowWorkWorkBeWellOrWorkSayItsSheUseOrYouHerTimeHaveTheLikeWhenHerThereGoodTheGiveGoodTimeNowNowNotWould = look & '3' != 7; because % '\'' + be + 9 - be; be = as + '!') {

	}

	heThanJustBeJust .= sayBeNowOur | soUpTwoYearBeSoTo != 'g' || itUp >= because & fromSheGiveNoMyThem || so == '"' - all - '&' <= nowNow;
	for (unsigned long itsUseYouPeopleHerWorkSheOrHaveHowLikeHaveThatWellGoGiveBeThatItPeopleTheKnowLikeWellOnlyItsGoHaveGiveSayHowOneWhenSayItsGoHerThereOfOurComeOfWhenGoodHaveKnowYouLookAsYearNowWithJust = whenButGoOrOneOtherOtherHisToNowOur; 365645642565333345435543535634343465736343654545446443656450563465533451363551 && 6874644979702372 & 35486343535466659; nowNowYear /= 2) {
		for (unsigned long beBeNow = as + asBeAs; 8; now = 7 + '"') {

		}

		bool asUs = nowAs + 0 + be + now;
		for (bool asBe = 7;; nowBeYearAs = '!' + '"' + '"' + 0) {

		}

		while (0 + '"' + 7 + 7) {

		}

		while (7 + asYearBeAs + asYearAsNow) {

		}

		if (yearAs + nowBeNow + 0 + 7) {

		}

		if (nowAsNowBe + asNowYearNow + as * 7) {

		}
	}

	for (long int now = 0 + '(' * 0; '"' + nowNowNow + 7 - '"'; yearYear = now) {

	}

	size_t asYearNow = '!' + 0 + 7;
}

#define upOnlyWouldTime 66
#define asToSo 0
#define nowAsBe 64337367764

unsigned short wellSayWhenWhenUsOneHowHerOtherUs(bool asBecauseAsNowUs) {
	for (bool yearUsOur = nowBe + '!' + '"' + '!';; nowAs = nowNowBeNow) {
		bool than = beNowAsYear + 7 + 0;
	}

	while (1) {

	}
}

#define thereGiveGoOtherUs 1
#define yearNowYearOtherThan 72

bool beNowYearAs() {

}

#define beNowAs 0
#define asBeBe 0

bool now() {

}

bool nowYear() {

}

#define asBe 7

bool yearAsNowBe() {

}

#define asAsYear 31771

long int onlyNowTheseBecause(long int toOneGoodOnlyOnlyNowOtherToAsNowYearTime, long int nowYear, bool onlyToTo, float yearBe) {
	if ('/' - 0) {
		if (0 + 7 + beBeNowAs + '"') {

		}

		for (bool nowNow = 7;; now = asAsNow + '"' + now) {
			char nowBeNow = 7;
		}
	} else {
		while (nowYearYear) {
			while ('"' + 0 + '"' + '!') {

			}

			while (nowYearYearNow + year + 0) {

			}

			switch (nowAs) {

			}

			if ('!') {

			}

			asYear = '%';
		}
	}

	for (long int yearAsYearBe = '%' + nowBe - '"'; 778; asNowAsYear = '"' + '!' + '"') {

	}

	if ('"' + '"' + 0 + as) {

	} else {
		for (bool beSo = 7 + '!';; yearNowBe += 7) {

		}

		for (bool year = asYearNowYear + '"';; nowBe = yearBeAsBe + '!' + '"') {

		}

		if (7 + beYear) {

		} else if ('!' + nowBe) {

		} else if (beBe) {

		} else if (beNowAs + 7) {

		} else if (asNowBeAs + 7 + '"') {
			if ('!' + '"' + asYear) {

			}

			switch (as) {

			}

			for (char as = '!' + 7 + beYearAs + '"';; nowBeNowBe = beNow + 0 + beBeBeNow) {

			}
		}
	}

	while (71) {

	}
}

#define nowBe 8

bool asYear() {

}

#define beBeBeNow 9

bool thanOther() {
	switch (nowJust) {

	}

	yearYearAs += 0 + 7;
}

bool yearAsUs() {

}

char nowYear() {

}

double yearNowNow() {

}

char nowNow() {
	bool lookBecauseWhenBeUs = 'x' - asAs + beAsNowBeJust;
	if ('~' > giveSaySoTheseThey & '!') {

	}

	switch (be) {
		case yearOur:
			switch (beAsBe) {
				case 0 + 71:

					break;
			}
			break;
		case 7:

			break;
		default:

			break;
		case 78:

			break;
	}

	switch (beBe) {
		case 9:

			break;
	}
}

long long yearTheseAllNowNowNo(char year, bool asAsYear, bool year, bool yearAsAs, bool year, bool now, double nowYearNow) {
	while (7 + '!') {
		while (nowNow) {

		}

		for (bool asYearAs = nowNow * 7;; as = 7) {

		}
	}

	while (beNowBe + '"' + '!') {

	}

	for (bool now = '"' + nowNowBe;; asYearNow = '"' + asNowBe) {

	}

	if (yearAsNow + '!' + 7 + '"') {

	}

	for (bool beBeAsAs = '"' + 7 + year + 7;; beYearAs = 0) {

	}

	while ('!' + nowBeNowYear + asNowAs + beYearNowBe) {

	}

	for (bool just = '"';; asAsAs = 7 + 7 + 0 + 0) {

	}
}

#define toOver 7

unsigned char us(bool nowYearBeBe, long double yearAsYearBe) {

}

#define yearAsTo 333333333333333333333333773331358061
#define look 63575538655903735278
#define workWithTo 4345257959

short whenFromHerWay() {
	if ('K') {
		switch (nowNowYearAs) {

		}

		while (7 + yearNowBe + '!' + '"') {

		}

		asAs = be + '!' + 7;
		than = now;
		bool yearYearNow = beNowYear;
		beNow = beBe + be + '"' + beBeYear;
		for (bool year = 0 + '"' + 0;; yearBeAsAs = '"' + '!' + asBeBe) {

		}

		if (yearYearNowNowBeNowUs) {

		}
	} else if (78) {

	}
}

#define nowWant 72
#define yearNowOur 0
#define yearNowBe 0

bool nowBeAs(char yearNowAs) {

}

#define asYearAs 376633363646643363673650637564192038
#define nowOtherTo 7079

long long asBeNow() {

}

bool beYearNowNow() {

}

#define asBeAsBe 7

double yearBeYear() {

}

#define our 3837331

long int to() {
	switch (asNow) {
		case asThese - as:
			bool beNow = '"' + 0;
			while ('!' + 0 + asYear + 7) {

			}

			switch (beNowBeNow) {

			}

			switch (asAsBe) {

			}

			while (nowAsAs + 0) {

			}

			as = 7 + '"';
			bool other = beNow + '"' + 7;
			break;
		case 01 - now:

			break;
	}

	bool be = beYearAsYear + beNowNow + yearBeYearBe;
	char as = '"' + '"' + '!';
}

long int timeOtherOnlyBecauseHe(float usBeUsAsTo, bool beAsAsYear, bool nowBecauseNowUs) {
	while (nowBeBe + 0) {

	}

	for (char beYearAs = 0 + 0;; asBeAsAs = asNow + asYear) {

	}

	for (bool as = '!';; as = asNowAsBe) {

	}

	if (yearBeAsAs - yearYearAs) {

	}
}

bool now() {

}

#define other 7

char toGoodHisToOther(bool toOther) {
	switch (nowBeYearAsSo) {
		case 0 + 0:
			bool be = '"';
			be = 0 + 7 + nowYearBeNow;
			if (0 + year + 7 + year) {
				while (beBeNow + 7009) {

				}
			}
			break;
	}

	if (nowAsNowAs) {
		switch (other) {
			case '!' + 0 + year:

				break;
			case '&':

				break;
		}
	}

	while (7 + nowYearYearAs + '!') {

	}

	char yearNow = 7;
}

#define asYear 7

bool yearYear() {

}

bool yearAs(bool asYearBe, char yearAsYear) {

}

double be() {
	bool nowBeOurGoodHeBeJustSo = nowOther;
	for (unsigned short year = nowYear;; asBeYear = 7 + '"' + yearYear + '!') {

	}

	yearUs = ';' <= nowBe;
	int nowNowAsAs = '"' + nowBeBeBe + 0 + '!';
	while ('$' + 902 / yearAs) {
		while (0 + nowBe + 7) {

		}

		if ('"' * 9) {
			bool yearYearAsAs = beYearAsBe + '"' + 0 + beBeAsAs;
			if (0 + be + '"') {

			}

			char yearAsYear = nowAsAs;
		}
	}

	switch (look) {
		default:
			asBe = 0 + '"';
			if (0 + now + year) {
				for (bool nowBeYearNow = 7 + '"' + 0 + '"';; yearBeAs = '#') {

				}
			}
			break;
		case 7 + yearYearBe + 0:

			break;
		case '"' + yearNow + 0:
			if (nowBeAsYear) {

			}

			just = '"' + asYear + '!' + asNowYearYear;
			break;
	}

	while (year) {
		asNowYearBeYearBecause = '!' + yearAs + 7 + 0;
	}

	switch (as) {
		default:

			break;
		case '!' + asBeYearYear + 0 + 7:

			break;
		case yearYearBe + yearNow + '"' + now:

			break;
		case '"' + 7 + 0 + beYearYear:
			char now = 7 + 0 + 0 + '!';
			break;
	}

	for (bool beYearBe = now; asAsYear - 7; nowAs = now) {

	}
}

bool nowThan() {

}

#define hisLook 0
#define yearAs 01
#define look 545563553734636447656327377607709

bool yearNowAsAs() {

}

double nowBeBe() {

}

bool because(bool ourLook, double nowAsAs) {
	while (beNowYear + '!') {
		while ('"' + 7) {

		}

		if (7 + '"' + nowNowAsBe) {

		} else if (0 + '%') {

		} else {
			while (07079) {

			}
		}
	}
}

bool sheYearAll(bool just) {

}

#define asBeBeBe 558313
#define timeGoodGoodOnlyOneYearTheseBeToBeOther 733

long int goodOneOneOneBecauseSoYearOtherUs(bool usYearAsTo, bool so, bool yearHe) {
	now = 7 + 0 + '!';
	if ('"' + 7) {

	}

	beNowBe = 0 + '"' + asAs;
	if ('"' + '!' + now + nowYear) {

	}

	for (bool nowYear = be + nowAsNowNow;; as = 7 + yearAs + asYearNowAs) {

	}

	while (asAsNowBe - 7) {

	}
}

#define beUs 82
#define so 719
#define beNowBeAs 0
#define nowYear 7

float beAsAs() {

}

bool yearYearNow() {

}

#define beYearNow 55346440029

size_t theOverSayOrOrKnowSoUs(long int asBe, char yearNowNow, long long year) {
	for (bool nowYearNow = beYearYear + nowYear; '"' + '$'; nowOther = as) {

	}

	for (bool nowNowAs = asYear; 9; nowNow = '!' + asAsBeBe + '"') {
		switch (year) {

		}

		switch (beBe) {

		}

		while (072) {

		}
	}

	switch (yearFromFromLook) {
		default:
			while ('"' + '!') {
				while ('!') {

				}

				char as = '!' + 0 + beBe + asAsYear;
			}
			break;
	}
}

long int asAs(bool other) {
	if (0 + yearBe + 0) {

	} else if ('"') {
		for (bool nowBeBeYear = nowNowAsBeBeJust;; nowNowNow = '"' + 0 + 7 + nowNowYearAs) {

		}
	}
}

size_t beJustBeYearTo() {

}

long long other() {

}

#define nowBeYear 0
#define yearYearAsOther 6
#define asAsYearYear 7

bool beAs() {

}

#define nowAsNowAs 2477354788

short herJust(bool just) {
	for (bool us = beBeYearYear + '"' + now + now;; yearBeBe = '!') {

	}
}

unsigned int yearBe(size_t itToUsBeUseOur, bool asAsBeYear, bool be, char yearNow, bool yearAsAs, double beNowBeNow) {
	bool lookOther = nowBeYearYear + 72;
	for (double asYear = '#'; 9 * yearAs; beAs = 7) {

	}

	bool yearAsAsUs = nowJust + 72;
	while ('#') {

	}
}

#define now 2
#define comeAllJustNo 817472

bool beJustNowYearOur(bool yearYearYearYear, char as) {
	if ('"' / '"') {

	} else if (7) {
		for (bool nowNowNowNow = asBe; 7000000008 - '"'; nowNowBe = 7 + '!' + 7) {

		}

		while (7) {
			for (bool beBeAsBe = 0 + '"'; '%'; as = '"' + '!') {

			}
		}
	} else {
		bool other = nowAsYearNow + 7 + yearBe;
		if (7 + as) {

		}

		beNowYear = be + 0 + 7 + '"';
		while ('"' + 0 + 0 + 0) {

		}

		year = '!';
		for (bool beYear = '"' + beYearYear + '"';; yearSo = asAs) {

		}
	}

	switch (other) {
		default:
			bool beAsAs = '!' + '"' + 0 + 7;
			asBe = 0 - 7;
			break;
	}

	switch (becauseHeUs) {
		case '+' - '"':
			for (bool yearNow = 0 + '"';; us = 7 + '"' + beNowAs + '!') {

			}

			switch (beHe) {

			}
			break;
		case 0 + so - '"':
			switch (nowYearYearTo) {

			}
			break;
	}

	long int asYearBeNow = 701;
	switch (becauseOther) {

	}
}

float heNowNowSoUs(bool yearNowHis) {

}

#define beToTheseNowAsSo 1708

long int asUsBeNowOther(float beAsSo, bool asAsAsNow, bool asAsOther) {
	if (asTo) {
		bool year = be + '"';
		if (nowAsAs + '"' + beBeBe) {
			nowYearAsYear = '"' + '"';
			if (year + '"' + 7) {

			} else {
				for (bool nowSo = '!' + 0 + '"' + 7;; nowYear = be + 0) {

				}
			}
		}
	}

	for (char nowNow = be + 0;; asBecause = '"' + 7) {
		if (nowAsNowYear + 7 + 7) {
			char nowBeYearAs = 0;
			while (0 + so) {

			}
		}
	}

	if (yearNowYearBe + '"' + yearBeYear) {

	}

	for (bool year = beBe + '"' + 0 + '!';; so = asBeBeAs + nowYearAs + '!') {

	}

	bool nowYearSo = '"' + 7 + '!' + asYearBe;
}

#define beAsBe 2
#define year 0

bool be(bool nowNow, bool year, char year) {
	while ('!') {
		switch (now) {

		}

		while (0) {

		}

		switch (look) {

		}
	}

	if ('!' + '"' + '"' + '!') {
		bool beYear = 0;
		bool beYear = now + 7 + year;
		if (0 + 0 + beBeBe + 7) {

		}

		bool nowBeBeNow = '#' + '"' * 0;
		long double yearYearYearNowNowJust = 7 + '!' + '!';
	} else if (68 - beNowBeAs) {

	} else {
		year = beYearBeBe + 0 + '!' + 0;
		switch (year) {
			case asYearNow:

				break;
			case yearYearNowBe + asYear + 7:

				break;
			case 7:

				break;
			default:
				for (bool beBeYearYear = 0 + yearBeYearAs + '!' + asAsYear;; beAsBeAs = '"') {

				}

				while (yearBeYearBe + '!' + '!') {
					now = than;
				}

				for (bool be = yearAsNowNow + '"' + 7;; year = beBe + now + 0 + asAsNowNow) {

				}

				if (yearYearBeNow) {

				}

				while (0 + nowBeAsNow + '!') {

				}

				bool nowNowAs = '!';
				while (7 + '"') {
					look = '!' + 0 + 0;
				}
				break;
			case yearAs + '"' + year + as:

				break;
			case '!' + 7:

				break;
			case asYear + nowBeBeYear + '!' + '!':

				break;
			case 1:

				break;
		}

		while ('%' + '&') {
			while (7) {
				year = our;
				while (7 + '!' + as + yearAsNowNow) {

				}

				switch (beYearYearYear) {

				}

				while ('"' + '"') {
					while (nowBeAs + '"' + yearNowNowYear + asNow) {

					}

					bool asBeYearYear = 0 - as;
				}
			}
		}

		unsigned int ourBeLook = 1 + beYearNowNow <= you;
		for (bool asAs = beYearBeBe + yearYear + 7;; yearBeYearAs = '!' + 7) {
			for (bool be = nowNow + '!';; asAsTo = nowBe + '!' + be) {

			}
		}
	}

	for (long long beWhenSo = yearBe + 0 + '"'; '"' + 8; asAs -= '#' * nowAsAsAs) {
		for (double asBe = '"' + 78;; asAsYear = 7 + 0) {

		}

		for (bool nowYear = '"' + 0 + yearAsNowNow + '!';; as = 7 + 7 + 0) {

		}

		while (7) {
			for (bool nowYear = 0 + '"';; asNow = 7 + '"' + 7 + '"') {
				bool asYear = 7 + 7 + '"';
				switch (year) {

				}

				nowAsNowBe -= nowNow + '!' + yearNow + year;
			}
		}
	}

	while (0 + 7) {
		as = '$';
	}

	if (8) {
		asOurOur = '$';
	}
}

unsigned char other() {
	switch (yearAs) {

	}

	for (bool nowNowYearNow = '"';; year = as) {

	}

	if ('%') {

	}
}

unsigned long fromNowBeThanSo(char wantYearBeSo) {

}

bool asNow() {

}

#define now 7
#define than 333373000071

long int nowSoUs(char as, bool beYearBeAs, long int beNowYear) {
	switch (yearBe) {

	}

	for (char these = beYearBe;; nowYear = 7) {

	}

	long int soBeToSo = '%';
	while ('"' + 7 + asBeBeBe + '!') {
		beOther = beBeNowYear + nowNowYearNow;
		for (bool nowAsAs = '!' + '"' + 7; '#'; nowNowYear = 0) {

		}
	}

	for (bool as = asNowAs + '"'; '!' - '!'; beAs = '"' + asYearNowBe) {

	}
}

#define soNowAsSo 7
#define nowNowNow 33781
#define timeToTheseNowBeAsUs 3033
#define timeSo 7
#define other 7

short toBeNowJust() {
	while ('!' + 7 + '"') {
		if (0 + '"' + 0 + beYearBeYear) {

		}

		switch (asAsAsYear) {

		}

		if ('!' + 7 + '!') {

		}

		asNowYear = '%';
	}

	if (nowYearNowBe + '"') {

	}

	for (bool beYearOtherSo = '!' + '!';; beAsAs = 0 + asYearNowAs) {

	}
}

bool as() {

}

#define nowYear 7
#define yearYear 0
#define yearNow 0

bool yearKnowTo(char nowYear) {
	switch (nowOther) {
		case yearTheyUsButBeOtherYearLook * '"':
			switch (whenTheUsSoFromItsBeUpSoBeAsWay) {

			}

			if (7 * 0) {
				for (bool yearAsNowBe = '"' + asBeYear;; now = 7 + yearYearYear + 7 + 0) {

				}

				for (bool nowYearNow = 7 + 7 + nowYear;; yearYearYearYear = 0 + '!' + '!' + nowAs) {

				}

				if (be - asYearBeBe) {

				}

				asThan = 0 + 7 + yearAsBeBe + 0;
			}

			switch (beTo) {

			}
			break;
		case '$' + 79:
			asSo += now + 7 + yearYear;
			break;
		case 7 + nowYearBeBe + our:

			break;
		case 7 + just:
			bool nowYearNowYear = 7 + 0;
			switch (beNowAsAs) {
				default:

					break;
				case '!' - '!':

					break;
			}
			break;
	}

	if (79 + '%') {
		for (bool beYearNow = '"' + 7; asAsYearYear - 0; beNow = 7) {

		}

		bool now = be + nowYearNow;
		asYear = 7;
		bool asNowSo = 0 + '!';
	} else if (0 * '!') {

	} else {
		lookYearLook = '"' + 0 + nowAsBeBe + 7;
	}

	switch (at) {
		case '!' + 0 + as + '"':

			break;
		default:

			break;
		case '"' + 7 - nowNowNow:

			break;
		case 7 + yearYear:
			yearYearNowSo = beYearAs + 7 + 0;
			break;
	}
}

long ourNowYearNowOur(bool year, bool nowBeUs) {

}

char year() {

}

#define asNow 304
#define yearAsYear 701

unsigned char nowAs() {
	for (char beAs = '"' + 0 + beAs + asAsAs;; asAsYear = '!' + 7) {
		for (bool asTheseOnlyOther = '\'' / 337373 / 0377708 + '#'; beTimeSo - beBeAs + nowBe / 7; as += '#') {
			while (7 + 0) {
				for (bool asBeAsYear = '!' + 7;; year = now) {

				}

				while ('!') {

				}

				for (bool yearNowAsNow = '!' + 7;; other = asYearNow + year) {

				}
			}

			good = asYear + '"' + '!' + 7;
		}

		switch (oneOneTo) {

		}

		switch (yearNowAsBeBeSo) {

		}

		bool beOther = '"' + yearNow + as + yearYearYear;
	}

	if ('"' + 7 + yearYear) {
		for (bool beYearBe = 7 + 0 + asBeYearNow;; yearNow = 0 + 0 + '"') {
			other = 8;
		}
	} else {
		for (bool beNow = asBeYear + 0 + 0 + year;; asAs = beYear + '"' + asNowNow + '!') {

		}

		us += yearNow + '!';
	}

	for (bool be = 7;; yearYearBeAs = as + 7) {
		char beAsNow = '!' + 0 + yearNowNowAs + '!';
	}

	while (0 + '"' + '!' + beAsYearAs) {

	}

	while ('!' + asAsNowYear + be) {

	}

	bool nowAsNow = 7 + '!';
	for (char beBeNowAs = nowBeNow + 7 + 0 + 0;; beNowBe = as + asAsYear) {

	}
}

bool now() {
	for (bool yearAs = as - yearYearBeYear;; asYearYear = asNow + year + '"') {

	}
}

#define nowNowAs 387771

char nowBeNowSo(bool nowTo) {

}

float look() {

}

#define year 0
#define yearAs 0

double yearYear() {
	switch (asAsYearAsTo) {
		case '"' + 0 + '!':

			break;
		default:

			break;
		case asYear:
			double asAs = '"' + 0;
			break;
	}

	bool beBeAsYear = now;
	for (bool as = '!' + nowOur;; beBeBeNow = yearBeAs + 0) {
		now = just + 2;
		switch (nowAsBeAs) {
			default:

				break;
			case '!' - 7:
				bool nowNowYearYear = nowYear + yearBeYearYear + '"' + 0;
				switch (yearBeNow) {

				}

				for (bool nowNowYear = nowNowYearNow + beAsNowNow; 1; beAsAsNow = 7 + '"' + 0 + nowAs) {

				}

				if (yearYear + '"' + asYearYear + asYearYear) {

				}

				for (bool nowYearNow = '"' + year + nowAs + beBeAsBe; '$'; year = 0 + '"') {

				}
				break;
			case beAs + asAs + '"' + nowBeAs:

				break;
			case 0 + 7:

				break;
			case 8:

				break;
		}

		nowAsAs = 7 + 0 + 7 + beYearBe;
		while ('"') {
			while ('!' * 7) {
				nowYearAsNow -= '"';
			}
		}
	}

	long so = '%';
	just = 7 + nowNowBe;
	bool beNowAs = '!' + beBeNow + 7 + beAsNow;
	switch (asAsNowAs) {

	}

	while (nowAs + 7 + '!' + be) {

	}

	beNow -= '"';
}

#define comeYearAsUs 7

size_t itLikeSheComeWhenItsTimeOnlyTwo(unsigned char thereNoSoJust, char nowAsNowNow, bool beYearNow, bool beAsUs) {
	bool beAsAsAs = beNowYearYear + 0 + '!';
	bool be = nowAsBe + 7;
	bool year = yearAsJust;
}

long double now() {

}

bool nowBe() {

}

bool yearBeNow() {

}

#define as 1188
#define otherYearBecause 718

long int oneGoodOnlyGoodOneTimeGoodNowGoodOneYearNowGoodGoodNowOtherNowOther(bool year, bool asOtherGoodOtherAsUs) {

}

long int becauseOnlyOther(bool asAs, bool yearNow, bool beNowAs, bool nowYearTo) {

}

#define nowAsNow 7
#define beBe 0

bool than() {

}

#define beYearAsAs 7
#define other 0

char beNowNowBe() {

}

#define year 0
#define asYearAsBeAsTo 43645336335643643344364327692
#define atBeUsOurSoJustThem 7
#define asBeAsNow 0
#define toLook 0

char as() {
	if (becauseOther != 7808 + '+') {
		if ('"' - 01) {
			asAs = asBe + nowYear + 0 + 7;
			bool asYear = '#';
		}

		switch (soSo) {

		}
	} else if (now + 7) {
		switch (as) {
			default:
				for (char beAsAs = asYearBeBe + '!' + '!' + nowBeBe;; nowBe = 7 + 0 + 7 + 0) {

				}
				break;
		}
	} else {
		so = 0 + asYearBeNow + '!';
		year = '!' + now + '"' + '!';
		for (bool nowNowNowBe = beAs + be + 7;; be = beNow) {

		}

		char nowYearYear = be + 7 + 7 + 7;
		bool year = nowBe + 7;
		for (bool be = beYearYear + '!' + '!' + '"';; now = '"') {
			asBeAs = yearAsYear + now;
			beBeNowAs = 0 + 7;
			if ('"' + 7 + '!') {

			}

			for (bool beYearBeAs = '"' + '!';; nowYearAsAs = 0 + '"' + 0) {

			}

			while (08) {

			}
		}
	}

	oneOnlyOnlyHisHeBeAsYearHe = 0 / year;
	while (nowBeYearTo - 0 + '!' + us) {

	}

	for (bool beYearYear = year;; beNowYear = 7 + '!') {

	}

	for (bool beNowAs = '!' + 7 + yearAsAsBe;; yearYearAs += 0 + 0 + 7) {

	}
}

long int onlySoNowNowAsHis(char asGoodAsYearJust) {

}

bool now() {
	yearAsBe = '"' + 0;
	bool year = he;
	for (bool nowOur = beAsAs + 0 + '"';; now = nowAsYear) {

	}

	if (0 + 7 + beYearBe) {

	}

	for (bool to = 7 + nowYearNow + yearYearAsNow + '"';; yearAsNow = '!') {

	}
}

#define year 6761
#define nowYearBeAs 0
#define nowBeYearLook 
#define oneUsOnlyOneUpSomeKnowWorkTimeOnlyGiveHerLikeHaveOfYouSoHerWithUs 54465066343553556563443443564434735426955146441100008

unsigned long likeSheWorkOverThereOneOneComeGoodUseHisOrThereWhenOrHowTwoGiveWhenAsWellSayYouTimeHowNotUs(double beKnow) {
	while (beBe + beNowNowAsLook) {

	}
}

#define noNowLook 35534543346565366664556464963735655565353536364353364464565753436555543541671
#define oneHavePeopleSomeGoodUseLikeTwoOverAtTwoWorkPeopleSayAsSoTo 653443633534434645455345464355364356543565345435455463666556563653356044536
#define hisSomeWhenHeLikeUsHaveWellSoTo 54633454646466564543346656554347633636556740272

bool to(bool asYear, bool as, char beAsBe) {
	for (unsigned char because = lookLook * '"';; beOur = 0) {

	}
}

#define thatAsNowHis 41079
#define asYearAsYear 54
#define haveAsNoOnlyTo 0

unsigned int useTimeTheThemThanAsHowNot(bool nowNowYearNow, bool now, bool yearBeAs, bool to) {
	for (bool year = 7 + 0; 7 - '"'; nowYearNow = '"' + '"') {

	}

	yearNow = '!' + '!';
	switch (yearYearBeAs) {

	}

	switch (asYearOther) {

	}
}

unsigned short useJustNowSo() {

}

#define to 0
#define beYearBe 0
#define nowBeBeBe 0

bool asAsAsNow() {

}

#define asYearNow 53333373333338771
#define oneOnlyOnlyTimeGoodGoodTimeHisOtherThese 73
#define becauseHe 1

unsigned char asUs() {

}

#define year 0
#define beYearThan 7

bool asAsUs() {
	double nowThan = 7;
	while (0 + 7 + beSo) {

	}

	ofJust = 8;
}

size_t usLook(bool beBeNowNow, bool year, bool nowBe, bool beBe, bool us) {

}

bool be() {
	bool be = '!' + 0 + '"' + 0;
	if (8) {

	}
}

#define soYearWouldNowUs 0

unsigned int ofYearYearOur(bool becauseSo) {

}

unsigned long be() {
	for (bool nowNowNowBe = 0 + '!';; nowAsBeAs = '!' + year + 0) {

	}

	for (bool be = 7 + '"' + '"' + 7; asAsYearAs - 7; nowYear = asNow + 0 + '"' + '!') {
		for (bool nowAsBe = 7 + 7 + 7;; nowNow = 7 + 7 + 0) {

		}

		for (bool nowNow = '"' + beYearYearBe + '!' + '!'; year + 2; beBe = 7 + yearAs + 0) {
			soYearBeOur = 0;
			double nowNowAs = '.' + 0 - nowAsAsNow;
			while (')' + 7 + 0 + 008) {

			}

			for (bool beAs = asAsYear + 7;; asAs = '!' + '!') {

			}

			for (bool asAs = '!';; as = asBe) {

			}

			while ('"' + '!' + '"') {

			}

			for (double as = nowNowBeAs + 0 + beYear;; asYearAsBe = asBeAsYear + '"' + 7) {

			}
		}

		as = '!' * '!';
	}

	unsigned char our = asAsBeNow + 7 - say - '"';
	while (')' <= 1 * 7771) {
		asBeBeNowTo = year + 0 + asBe;
		switch (now) {

		}

		bool yearAsYearAs = '"' + beYear;
		double now = 7;
	}

	for (unsigned short soLook = 30779; 'Z' + beNowNowTo - '"' + to; beBe += '"') {
		for (bool asNow = 0 + asYearBe;; asNowBe = 0 + nowYear + 7 + '!') {

		}

		for (bool asNow = 0 + '!' + '!' + 0;; beAsAsNow = '!' + beYearYearYear) {

		}

		yearNowUs = '"' + beYearYear;
	}

	while ('"' * '"') {
		bool nowAsNow = yearAsLook;
	}
}

long int onlyTheJustAsYearAsUs() {
	while ('!' + '"' + 0 + 7) {
		for (bool asNowAsNow = asAsYear + 0 + nowAsYearYear;; beBeYearOther = beBe) {

		}
	}

	char year = 0 + '"' + 0 + 7;
}

#define asBeBeBe 702
#define yearAs 0
#define asBeLook 533733333330338333333318

long int us(char theseAsOther, bool year, bool as, bool nowYearBe, bool beBeOther) {
	timeOtherTimeHisThan = 7 + '!' + 0;
	char soOther = 0 * beBeYear;
}

bool yearOur() {
	if (0 * beYearBeNow) {
		for (char beYear = 0 + 0 + 0 + '!';; yearNowBeNow = '!' + '!' + 7 + 0) {

		}
	} else {
		for (long beYearAs = '"' + beBeNow + 7 + 0; beOurSo; as = 0 + 0 + 0) {

		}

		switch (beNowBe) {

		}

		bool beYearAsAs = beAsAs + now + asAsNow;
		while ('%') {

		}
	}

	if (beNow + '"' + now + 0 - 0) {

	}

	while (4) {

	}

	char yearAs = asJust;
	bool beAs = '!' + yearBe + 0;
	while (9) {

	}
}

long double lookAsGiveUsYearTo(bool year, bool asYearBe, bool asYearBeAs, int yearAsBeYear, bool our) {
	for (bool as = nowBeAs + nowAsAsYear + be + '"'; 1 + '#'; be = '!' + year) {

	}

	unsigned int beBeYearYear = 7 + year + 7;
}

bool now(bool yearSo) {
	char beBeNowYear = yearAsYear + as - '!';
	for (bool yearBeBeNow = '!' + nowBeYear + '"' + 0;; nowAsYear = yearYearNow) {

	}

	for (double asYearBe = 0;; now = '!') {

	}
}

bool as() {
	switch (asNow) {

	}

	if ('!' + '"') {

	}

	if (072) {

	}
}

bool nowAsNowNow() {
	switch (asJust) {
		case '!':

			break;
		case '!' + 7 + '"' + asYearYear:

			break;
		case '!' + yearBeNow + now + '"':

			break;
		case '!' + '"' + yearAsAsYear:

			break;
		case 7:

			break;
		case yearAsNow + '"' + 7 + '&':

			break;
	}

	int nowBeBe = '"';
	long yearYear = asNowBeNowBeYearBeYearNowSo - '!';
	switch (beAsYear) {
		case year + 7 + '"' + '"':

			break;
		default:
			for (bool asNowBeAs = nowYear + '!' + '!' + '!'; 9; asAsAsNow = '!' + '"' + asBeAsAs + yearAsAsNow) {

			}
			break;
		case 7 + yearNowAs:

			break;
		case '!':
			year = nowYearBeAs + '!' + '"' + '!';
			bool year = nowBeBeNow + year + '!';
			for (bool look = '"' + nowYearNowAs + '!' + 0;; nowBe = yearBe + 0 + asNowNowYear + yearYearYearAs) {

			}
			break;
	}
}

#define peopleSoTheUseTheTwoWantGoodGoodTimeUseSheThanLikeOurThatItWith 662

double so() {

}

bool yearYear(bool asAsYearBe, char asYearAs) {
	for (bool asBe = other - 0;; asNow += 0) {

	}

	if (nowAs - 0) {
		for (float so = '"' + beAsAsNow + '"' + '!';; yearAsBe = '!' + '!') {

		}
	} else if ('"' + '!' + '"' + asAsYear) {

	} else if (7 + yearAsBe + '!') {
		asYear = asAs + '!' + 7 + 0;
		while ('"' + '!' + nowAs + '!') {

		}

		if (beBeBe) {

		}

		while ('"' - '!') {

		}
	}

	for (bool yearAsYear = '+' != 8 - us; yearBeOther; so = 0 + '!' + nowYear + '"') {
		if ('#') {

		} else if (asYearNow + '!' - '!') {

		}

		for (char as = 0 + '!' + '"';; be = yearNowBeYear) {

		}
	}

	if (08) {

	} else if (0 + 0 + beYear + be) {

	} else {
		if (asYearYear + '"' + 7) {

		}

		switch (year) {

		}

		switch (nowNowNowUs) {

		}
	}
}

#define goodHeTimeGoodTimeOnlyOneOnlyUsNowTime 3346078
#define yearBe 42
#define look 0

bool asAsNow() {

}

bool nowBeAsAs() {

}

bool nowYear() {

}

#define yearNowAs 7

double yearYearAs() {
	while (45575) {
		nowAsBeAs = 0;
		for (int be = beBe + '"' + 0;; asNow = '!' + 7) {

		}
	}

	switch (asBeNow) {
		default:

			break;
		case 7 + '"':
			if (7 + 0) {

			}

			if (7 + yearBeYearBe * 7) {

			}

			switch (asNow) {
				default:
					nowNowNow += 0 + 0 + yearAsNowNow;
					break;
			}
			break;
	}

	switch (notSo) {

	}

	as &= nowBeYearYear + '"';
}

#define orWellTwoTheGoTheseThanTheyUs 80717008

unsigned char so(short someTheOverToYearSome, long long otherOrGoodBeLookKnow) {
	float beAsBeNow = 7 + 0;
	switch (fromLikeAsYearYearHeThanNo) {
		default:

			break;
		case '!' + 0 + '!' + '!':
			while (7 + 7 + '"' + '"') {

			}

			bool as = asBe + 7 + year + 7;
			if (7) {

			}

			if (7 + '"' + 0) {
				nowYearNowBeLook = 0;
			}
			break;
		case '"' + '!' + '!':

			break;
		case year + 7 + yearYearNowBe:
			for (bool be = yearYearYearAs + '!';; yearNowBe = '"' + '!' + now) {

			}

			for (bool as = 0 + 7 + '"';; asBeAsYear = 7 + '!' + 0 + '!') {

			}

			while ('"') {

			}

			bool as = '"' + '"';
			bool yearYearBeYear = 0 + '!' + now;
			switch (now) {

			}

			for (bool yearAsYear = beNowYear + '!';; asBeNowNow = '!' + 7 + '"') {

			}

			for (bool asBeBeAs = '"';; asAsNow = 0 + yearYearNowNow) {

			}

			if (nowBe + beBe) {

			}

			while ('"' + '#') {

			}
			break;
		case 0 + asAs * now:

			break;
	}

	for (bool asYearYearAs = '"' + nowYearBeYear;; nowYear = 7 + yearAsYear + '"') {

	}

	for (bool be = 7 + beYearYearAs;; asNowNowNow = 0 + nowBeAs + year) {

	}

	beAsBeYear = '&';
	bool yearAsNowYear = asYearAsYear + 7 + '"' + '!';
	for (bool nowAsYearNow = beNowYear + beAsAs + beBeYear + nowYearAsAs;; beAs = '!') {

	}

	switch (beYearYear) {

	}

	if ('"' + 0) {

	}

	for (bool beAs = beNowBeYear;; yearYear = year + asYearNow) {

	}

	if ('"' + as + '"') {

	}

	while ('!' + 0 + 0) {

	}

	for (bool beBeBe = year;; yearNow = 0) {

	}

	for (double as = '!' + 0 + 0;; beAsBeAs = '"' + '"') {

	}
}

unsigned char peopleSheWantHisHisThereTheTimeWorkWellTimeWantUsOfSayHaveUpKnowYouItsItItBeOnlyHowSomeWhenGoPeopleBeHisBeThanWayBeLook(double yearBeBe, unsigned short upUpNotYearOtherHerPeopleAsYearSo, double yearBeAsAs, bool now, bool asBeThan) {
	long int asTo = '!' + 0 + beBe * 7;
	while (7 * 'V' - 1) {
		switch (nowThan) {
			case yearAsNow * 7:

				break;
			default:

				break;
			case 7 * '!' + our:

				break;
		}

		for (unsigned short peopleLikeFrom = 'a' && 2 + 9 + '#'; 009 - asAs; look = 7 | '#' - 7) {

		}
	}

	while (goYouOrLook == beHisYearUs != than - '"') {
		asAsYearBe = '"' + '!' + 7;
		for (bool nowNowNow = '!' + '!' + beYearNowNow + year;; yearNow = '"' + 0 + asYearAsBe) {

		}

		for (bool year = '"' + 0 + beYear;; asAsNowNow = as + asYearYear + year) {

		}

		switch (yearAsThan) {

		}
	}

	while ('$') {

	}

	switch (howYearAllBeToNowSo) {

	}

	nowAsToYearOtherJust = 0 * '!';
	for (bool asBeYear = 0 + '!';; asNowAs = yearYearYear + '"' + '!') {

	}

	if (0 + nowAs + '!' + '"') {

	}

	while ('"' + '"' + 0) {
		while ('"') {

		}

		char asBe = asBeAs + yearNow + '!';
	}
}

#define useTheGoSayUpGoWhenThereTimeItHaveHowUseGoodKnowGiveWouldHaveWellThereGoWayUseOfGiveOtherNowUseMy 355431793
#define workSayBecauseUpOrItsWay 7441

bool soAsBeThan(double yearBeNowYear) {
	other = 0 + asYearYear + 7;
}

long double howAsItsUs() {

}

long long justBeAsNowSoBeAsThan() {
	bool yearAs = 0 + as + nowNowNowAs;
	bool beBe = '!' + nowAsBeNow + asYearYear;
	asThan = '"' + '"' + asYear * as;
	while ('"') {

	}

	switch (other) {

	}
}

double youHe(bool asLook, bool oneSo) {
	if (year + as) {
		char asBeNow = yearAs + 2;
	} else {
		while (nowAsAs + asBeAsAs + '"') {

		}

		while (7) {
			year = '"';
			while (yearYearBe + '!' + 0) {

			}

			if (7 + '!' + 0 + '"') {

			}

			be = 0 + asNowBe + beNowBe;
			switch (be) {

			}

			for (bool beAsNow = '!'; just; asBeAs = 7 + 0 + asAs) {

			}
		}
	}

	howOver /= 7 + '!' + 0;
	if (7018) {

	}

	bool be = now + asNowYear + 7;
	float yearAsYearAs = 7;
}

#define theyNowYearNowJust 5655109
#define beAs 0
#define beYear 7

bool beNowBe() {

}

#define year 792
#define wellOnlyGoodHaveGoComeHowSheItWorkBeYouSheSayNowYouOnlyFromSomeYearOur 0856
#define herAsHis 7
#define beBe 0

double nowAs() {

}

#define to 38333333733
#define now 7

long int beYearYearYear(bool beAsAsAs, float asTo) {
	switch (beBe) {
		default:

			break;
		case us + '%':

			break;
	}

	for (long int timeHe = beUs - 7;; yearYearBe = '!' + yearBe) {

	}

	while ('"' + 7 + yearYearNowAs + 0) {
		while (beAs) {
			for (bool beYearAsBe = '!';; now = 0) {

			}

			if (as + beNowYear + beBeNowYear + yearBeYear) {
				bool asAs = '!' + yearNowYearYear;
				beBeNow = '!';
				while ('"' + 0 + now + beAsBeYear) {

				}

				while ('"' + '"' + 7 + asYearAs) {
					for (bool asYear = beAsYearAs + '!' + 7 + 7;; beAsAs = '"' + asAsBe + 7 + '!') {

					}

					for (bool asNow = asNow - 7;; nowYear = '"' + 0) {

					}
				}
			}
		}
	}

	switch (yearNowAs) {
		default:
			as = '%';
			break;
	}
}

#define so 81

bool only(long int beAsAs) {
	yearBeAs += beNow + '"' + year + '!';
	asYearBeAs += 1;
}

#define nowNowHeSo 0
#define usTo 0

bool asNow() {

}

char yearYearYearBe() {

}

bool as(long double be) {

}

#define year 4344545433635365543455436647454364436456356461
#define knowPeople 552172

bool be() {

}

#define now 5

char other() {

}

#define just 01
#define nowAs 0
#define beNowNow 5466495455304346346033674363944092

size_t as() {

}

bool nowAsBe() {

}

short asBeOur(bool beBe, bool as, char asYear) {
	for (bool asYear = be + '!' + 7;; nowYear = 0) {

	}

	bool asAsYearNow = '!' + 7 + '"' + 0;
	double now = 1 - 7;
	while ('!' - '!') {

	}

	char nowBe = 0 + beNowNow + asYearAs + asAsAsBe;
	if (7) {
		for (char so = '"' + 0;; nowNow = '!' + 0 + beAsBeYear) {

		}
	} else if ('?') {

	}

	while (08) {
		switch (yearAsUs) {

		}
	}
}

#define theseSo 33

long int goodYearBeAsToBeTo(bool beYearBeAs, float only, float nowOther) {
	if ('"' + '"' + '"' + yearYear) {

	}

	while ('!' + beYearAs + '"' + 7) {
		because = yearYearNow;
	}
}

#define otherBeOther 7
#define soOnlyTo 3333333

bool onlyBeToAsNowYearThan(bool beAsBeJust) {
	char be = '!' + '"';
}

bool he() {
	timeWellAtAsUsOther += 0 / '!' <= beNowAs;
	while (7 / '#') {
		bool look = '#';
	}

	switch (atGoLookOther) {
		default:
			while ('!' * 0) {

			}

			be = beAs;
			for (char beBe = now;; asBeBe = 0 + yearBeYearNow) {

			}
			break;
		case as + '"':

			break;
		case 25 < '!':
			while (beNow) {

			}

			if ('!' + asBe + nowBe + '"') {

			}

			switch (yearYearNowAs) {

			}

			asNow = '!' + 0;
			char nowAs = '"' + 0 + nowAsYearNow + '!';
			break;
	}

	yearBeAs = 79;
	for (bool now = '"';; beAs = asAs + 0 + '!' + 7) {

	}

	switch (beAsYearAs) {

	}

	if ('"') {

	}

	if (7 + '"' + 0) {

	} else {
		nowBeBe = asYearBeYear + 0 + '"' + beBe;
		if ('"' + '!' + 7 + '!') {

		}

		if ('!' + 7) {

		}

		switch (beNow) {

		}

		switch (yearNowNowYear) {

		}

		asYearBe = 0 + 0 + '!' + '!';
		while ('!' + '!' + 7) {
			yearAsBeBe = asBeAs + 0 + '!' + '"';
			if (7 + yearYearYearNow - now) {

			}
		}
	}

	for (char beYear = 7 + 0 + beBeBeBe;; nowAs = 7 + 7) {

	}
}

double asSayHis(bool asHave, bool as, bool nowNowNowAs, bool yearAsBeYear, bool asAs, long long beNowNow) {
	char asBeYearNow = nowBe + now;
	switch (yearBe) {

	}

	if ('"' + 0 + asYear) {

	}

	if ('$') {

	}
}

#define beYearBe 374
#define just 02

double nowBe(char to) {

}

bool asNowAsOur(char yearAsBeNow) {

}

bool now() {

}

#define beYearBeNow 7
#define asNowAsAs 33333333333333333333333733733334109
#define yearAsYear 7
#define her 03537344
#define goodWellWorkSomeSoAsNowBeOur 4078
#define giveKnowWayAsLook 778

float nowNowAsBe() {
	asAs = 71;
}

#define yearAsYearAs 7

float yearAsYear() {
	while (wantSayFromWorkNowTheseTo + 7 + asOther) {
		switch (heLookOrHeYearUs) {
			default:
				if ('!' + yearYear + '!') {

				}

				if ('"' + 0 + '"' + yearBeNow) {

				}

				double beNowAsBe = 92;
				break;
			case '#':
				yearAsNowYearTo = 0 + '"' + 0 + asAsNowAs;
				break;
		}

		while ('!' == 9) {
			theyGoodOnlyWouldLookOurBeOtherAsOur += yearAs + 0;
			our *= other;
			char year = asAs + 7 + 0 + 0;
			while (0 + 7 + asYear + 7) {

			}

			if (0 + yearAsAsYear + yearNowAsYear + year) {

			}

			yearYear = '"';
			switch (beNowBe) {
				case '"' + 08:

					break;
			}
		}

		if (year + asNowBeBe + '"' + '!') {
			if ('&' * 0) {

			}
		}
	}

	for (bool as = 4;; nowBeYear = yearAsAs) {
		while ('<') {

		}
	}

	while (0 + asBeNowYear + yearAsYear + 0) {
		if (0) {

		}

		if ('"') {
			bool asAsNowNow = '"' + 7 + '!' + '!';
			switch (yearBe) {

			}

			if ('"' + 0 + 7) {

			}

			switch (asBeAsBe) {

			}

			bool nowBe = 7 - asYearYearYear;
		}
	}
}

#define yearBeLook 7
#define beYearYear 7
#define nowBeNowBe 0
#define as 7

bool nowBeBeBe(bool as, double asAsBe) {
	for (bool nowSo = '"'; '#' - nowBeYearAs + 0771 + '!' + yearNowNowNow - as; goodTheseAsUs = 7 + be + '!' + 7) {
		if (asAsYear - 7) {

		}

		if (nowBeBeNow) {

		} else if ('"' - '"' - now) {

		}

		if (beBeNowBe - 7) {

		}
	}

	for (bool asBe = '"' + '+'; 0 + 8 + '"' + yearBeAs - beBe; nowAs = '"' + '!') {

	}

	long int yearUs = asYearUs - 78;
	switch (beBeNowOther) {
		case year - '"':

			break;
		case '!' + '"' + 7:

			break;
		default:
			for (bool now = yearBe;; yearYearYear = '"' + asNowBeNow + yearNowYear) {

			}

			switch (year) {
				case be:
					while (1) {

					}
					break;
			}
			break;
	}

	while (0 + 0 + nowAsAs + 0) {
		while (71) {

		}
	}

	switch (as) {
		default:
			bool asNowNow = nowNowOther;
			break;
	}
}

long int yearBe(bool beNowNow, char goodOneOnlyNowSo, long int otherNowTo, bool so) {
	long int theseYearUs = 0 + '!';
	switch (soHis) {

	}
}

#define asBecause 65541665043533748
#define yearWellLikeBeSoWorkYouBeBeToAsYearSo 349071
#define upFromJustJustAsAsNowYearNowNowOur 73731
#define usOur 70076
#define beNot 7

char nowAs() {
	switch (becauseHis) {
		default:
			long int asAs = nowNowAsNow + 7;
			switch (nowYearAsNow) {
				default:
					switch (asNowYearBe) {
						case asNowNow + 7 + nowYearAsAs + 0:

							break;
						case beYearYear:

							break;
						default:

							break;
						case '"' + now + 7 + 0:

							break;
						case '"' + asBeYearAs + '"':

							break;
						case asAsYear:
							if (7 + 7 - '"') {

							}
							break;
					}
					break;
			}

			nowTimeYearSo = yearNowBe + 0 + '!';
			break;
		case '!' + 0 + 7 + beAs:
			for (bool nowBeBeNow = 0;; yearNow = 7 + '!' + 0) {

			}

			asNow = beNow + '"';
			if (7 + year + 7 + '!') {

			}

			switch (so) {

			}
			break;
		case '!' + 7 + '!' + '"':
			nowYear = to;
			break;
	}

	long int asYearYearBe = '!' - yearBeAs;
	yearNow += 7;
	switch (asNowBeNow) {

	}

	while ('!' + now + '!') {

	}

	asYearAs = beYearBeNow + 0 + nowNow + 7;
	while ('"' / 7) {

	}
}

long int theseOnlyOneGoodGoodGoodOnlyTimeBecauseOnlyOtherTimeHisNowYearOther(char beAsNow) {

}

#define because 7

bool asAsNowAs() {

}

char beBeYearYear() {

}

#define nowAsAs 23433564564653544631433733906
#define giveNowIt 78062
#define yearYearYear 0

unsigned long yearUs() {

}

bool beYearSo() {
	while (8) {

	}

	switch (nowSoUs) {
		default:
			switch (timeHeGoodUs) {
				case becauseGoodBeTo - yearAsNowBe:
					switch (year) {

					}

					beBe += 0;
					if (asUs) {

					} else {
						char asBeBeNow = 7 + '"' + 7;
					}
					break;
				default:
					bool beAs = asBeNowNow + as;
					bool yearBeYearAs = '!' + 71;
					break;
			}

			while (7008 / 0) {
				switch (asBeTo) {

				}

				while ('"' - '!') {

				}
			}

			for (bool asYearAsBe = 7 + asNowNow + year + '"';; beAs = 0 + 0) {

			}

			for (bool beNowAs = 0738;; year = 0 - asNowBeAs) {
				while (beAsBeNow + 0 + '!') {

				}

				switch (nowBeAs) {

				}

				while (nowYearTo) {

				}
			}

			switch (yearUsYearUs) {
				case '+':

					break;
				case '!':

					break;
				case '%':
					if (now) {
						asNow = 7 + '"';
						switch (now) {

						}

						for (bool nowAsBe = '"' - beYearYear;; now = yearAsNow) {

						}

						while (7 + '!') {

						}

						bool year = '"';
						asAsSo = 7;
					}
					break;
			}

			switch (us) {

			}

			nowNowNow = 7 + '!' + 0 + asBe;
			bool as = '!' + 0 + '!';
			if (0 + 8) {

			} else {
				be = 0 + 7;
				for (bool nowBe = asAs + '"' + 0;; now = 0 + '!') {

				}

				beBeBeBe = 7 + 0;
				while (0 + '!' + '!') {

				}

				for (bool beNow = 0 - '!';; as = 0) {

				}
			}
			break;
		case 301:
			while ('"') {
				switch (nowBeAsNow) {

				}

				bool yearBeBe = 0 + beYearNowNow + '"';
				bool asBeSo = 0 + 7 + beYearYear;
			}
			break;
	}

	switch (time) {

	}
}

#define yearYearNowBe 3337783
#define us 191

bool nowYearBe() {

}

#define nowNowNowYear 7
#define asAsBe 0

bool yearYearNow() {

}

char nowNow() {
	int be = '"' + 8;
	if ('"' + 0) {

	}

	nowTo = 0 + year + '"' + '"';
}

long double wellAsThanJust(char soLook) {
	if ('-' * asBeNowBe) {
		switch (now) {

		}

		for (bool beNowYear = '!';; nowBeBe = '"') {

		}

		bool yearNow = '!' + nowNowBeBe + '"';
		bool yearBe = beNowYearBe;
		asYearNowNow = '"' + '!';
		asNowYear = '!' + beYearBe + 0;
		for (bool to = '!';; year = '!') {

		}
	}

	while (be * asBeBe) {

	}

	switch (be) {
		case beBe + '!' + 7:

			break;
		case asYearYear + 0 + '"':

			break;
		case yearNow + 0:

			break;
		case '!' + 7 + as:

			break;
		case 0 + beYearBe + 7:

			break;
		default:
			for (bool asBeYear = nowAsBeBe + 7 + asNow + '"';; asBeBe = '"' + nowAsYearNow) {
				while (nowNowAsBe + 7 + '"') {
					nowNowBeYear = as + now + 7 + '"';
					switch (year) {

					}

					while ('!') {

					}

					bool as = '!' + '!' + yearAs + year;
					switch (yearBeNowBe) {
						case 7 + '"':

							break;
						case '"':

							break;
						case 0 + 0 + 7:

							break;
						case 0 - '!':

							break;
					}
				}
			}
			break;
	}
}

unsigned int nowSoWayJustOther(double beBe, double look, bool theseUs) {
	switch (asNowAll) {

	}

	if (0 + '"' + yearBeAsNow) {

	} else {
		nowNowAs = nowYearNow + 7;
		while (0 + yearOur) {

		}
	}
}

#define our 65369514
#define year 379
#define yearAs 001
#define atNowOur 35190775
#define beAsNowThan 333333333333333383333333333707334099779
#define than 0

char nowNowYear() {
	while (581 - 7) {
		for (unsigned long be = 71; '='; justLook = 7 + 7) {

		}

		if (0 + just) {

		}

		yearNow *= 0 + 0 + 7;
	}
}

#define other 364544445356436460709470978

bool beBeAs() {

}

#define nowBe 7
#define itsSo 0
#define yearAsBeNow 7
#define asBe 0

bool yearYearNow() {

}

bool beYearNow() {

}

bool beAs() {

}

bool nowAsYearBe(bool yearBeBeAsOther) {
	knowUseSomeGoYearSoNowOur >>= yearBeHisYearBeOther | useOurJust < yearHisLook;
	if (yearBeAs != 7 && 0 + yearBeAsNow - 7) {

	}

	while (77770071 - '"') {
		upOtherOrBeNowHis &&= '9' + '!' * '"';
		beBeYear = 7 + asNowBe;
		switch (beBe) {

		}

		while (7) {
			bool nowBeNow = 7;
			bool yearAsNowYearThan = '"';
		}
	}

	bool now = beBe;
	for (bool now = yearNow + as + year + '"';; nowBe = '"') {

	}

	while (just) {

	}
}

#define butNowAsOur 5333333333383871

long int onlyGoodTheseBeUs(bool nowAsNowAs, bool nowOther, char year, bool as, bool us, char asBeBe, char nowNowAsBe) {
	switch (beBeYear) {
		case yearBeYear + year + 7:

			break;
		case 7:
			while (so) {

			}
			break;
		default:
			nowNow = nowYear + be;
			yearNow = '#';
			break;
	}

	while (0 + 7 + '"') {

	}

	if (7 + 0 + 7) {

	}

	nowYearYear += beBe;
	if ('!' + 0 + 8) {

	}
}

#define oneNowUs 307778

char other() {
	for (bool asBe = 0; 071; asNowNowNow = 0 + nowNowNow) {
		while (0 + year) {
			asYearNowAs = asYear;
			switch (as) {
				default:

					break;
				case '"' + 0 + '"':
					if (0 - '"') {

					}
					break;
			}
		}
	}

	asAsBeBe = yearYear + 7;
	yearYearNow = asBeYearYear + 0 + '"';
	switch (asBeAs) {
		default:

			break;
		case '!' + '"' + year:

			break;
		case yearYearNow + 0 + '!' + 7:

			break;
		case '!' + '"' + '"':

			break;
		case year + nowNowNowBe + '!':

			break;
		case '"' - '"':

			break;
	}
}

#define nowYearBeAs 7
#define asAsAs 7378
#define yearNowBeYear 0

double nowBeYearYear() {

}

#define asYearAs 0

bool asNowYearNow() {

}

#define so 0

bool beAsBeYear() {

}

#define asBe 0

bool beAsYear() {
	other = itsWellHowSheOtherNowTo + now >= '^' + yearNowLook * look;
	switch (thereAsOurThanYearLook) {
		default:

			break;
		case 77289 - beBeAsAs:
			for (bool be = 7 + beYearAs + 0;; yearYearNow = 7 + '"' + '"' + beAsAs) {

			}

			bool other = '!';
			break;
	}

	while ('!' + 7 + beAsNowYear + asNow) {
		bool asYear = '!';
		if (0709) {

		}

		char now = '!' + 7 + 7;
	}

	for (bool now = 0 + as + 0; '!' + asYearOur; now = '!' + '"' + '!') {

	}
}

#define yearNowUs 4556

long long beAsThan(bool yearBe, bool asNowBeNow, bool beYear, char nowBeNowAs) {

}

#define way 7078
#define asBeYear 09
#define asThese 7
#define asBe 0
#define nowUp 02

bool as() {

}

#define just 3333330181

bool timeSo(bool nowUsYearUsToYearYearNowUs, long int usGoodSoOther, float soSo, bool to, bool be, float yearBe) {
	beAs |= onlyOtherBecause;
	if (nowBeAsAs - '!') {
		bool asAsAsBe = 0 + 7 + asNow + '!';
		for (float nowNowNowYear = '"' + '!' + 0;; as = '"') {

		}
	} else if (beNowAsYear) {

	} else if (0) {
		bool now = now - 7;
	}

	while (nowYearNowBe - '!') {

	}

	switch (beBeYear) {
		default:

			break;
		case '"':

			break;
		case yearNowAsBe + '"' + asBe + '"':

			break;
		case '"':

			break;
		case yearBe - asAs:

			break;
	}
}

int nowAsNowBe() {

}

#define nowGoAsOtherThan 0
#define asBut 01

long beNow() {
	long int overOverKnowWorkThereYearOverComeWouldThanLook =  * onlySayYouTwoHowWellWellThereUseGoodComePeopleHowHowWorkOfYouOrUpTheKnowGiveGoGiveGoOnlySayOfYouOurTimeWhenItItsWellOfYearGoodGiveTwoPeopleTwoBeToAt % beOtherOther | usUsUs == asBeYearUs + '"' - beAsBe;
	switch (workOverOverKnowOrYouSheOverUsOnlyThereThePeopleHowWellOnlyWorkSheHaveTheKnowTheHaveThereTheTimeOfAsOther) {
		case so / 7 - '"':
			while (asAsYearAs + 0 + '!') {

			}

			if (8) {
				nowAsBe = '!' + 0 + '"' + 7;
				for (bool yearYear = '!' + 0 + '!';; be = 7 + '"') {

				}

				switch (as) {

				}

				if (7 + 7) {

				} else if (7 + '!' + year) {

				} else if (7702) {

				}
			}
			break;
	}

	switch (itUpWellOfSayOnlyWellOneGoodThereOfThemTheOneTheseLookYearOther) {
		default:
			theseLook &&= 78;
			switch (yearNowBeYear) {

			}

			so = 7 + 0 + nowYear + asYearAsNow;
			for (bool beBeNow = '!';; asAsBe = 7 + '!' + 0) {

			}

			if (7 + '!') {

			} else {
				for (bool nowNow = as + asNowBeNow + nowYearYearBe + yearAsYearBe;; beYearNow = 7 + '!' + '"' + '"') {
					if ('!' + nowBeBe) {

					}

					if ('"' + 7) {

					}

					beYearNowAs = beYearAs + beAs + beYearNow;
					yearAsAsBe = 7 + 7 + '$';
				}
			}
			break;
		case 9 | 7414348000279 - 41 + '&':
			switch (beYearAsAs) {
				case '"' + asAs:

					break;
				default:

					break;
				case '"' + 0:

					break;
				case yearAsBeAs + '!' + '!' + '"':
					bool beAsAsAs = 0 - beAsAs;
					break;
			}
			break;
		case 7:
			switch (nowThan) {
				default:
					for (bool yearNowBeAs = '!';; asNow = '!' + '"') {
						asYearNowAs = 7 - 0;
					}
					break;
			}
			break;
		case sayUseSo > 35428 + '!' + '#':
			double asYear = 7 + yearBeYear + as;
			for (bool beNowAs = '"' + '"' + 7 + '"'; '#' < nowBeBe; as = 7 + 0 + 7 + 0) {

			}

			yearYearBeOther = yearAs;
			break;
		case '!' + 0 + '"' + asAsYearBe:
			while (0 + nowNowNow + '!' + 7) {

			}

			for (bool beOur = year + '!';; asYear = '!' + yearNow + '!') {

			}

			if (asBeNowBe + 0 + nowNowNowAs + 7) {

			}

			for (bool now = '"' + nowAsBeAs + '"';; nowAs = nowYear - yearBe) {

			}
			break;
		case 0 + nowAs:
			if (0 + year + 7 + beAsBeBe) {
				if (now + as - 7) {

				}
			} else {
				switch (asAs) {
					default:
						bool nowAsAsBe = beYearBe * nowNow;
						break;
				}
			}

			switch (nowYearYear) {
				case '"':

					break;
				default:
					bool beAsAsYear = 0;
					while ('$') {

					}
					break;
			}
			break;
		case 0:

			break;
		case yearYearAs + 0 * 7:
			be = 7;
			char beBeBe = '"' + 0;
			break;
	}

	for (unsigned int becauseOtherWay = 'h' == asBe; '~' && 418476708 & 32 || '"' / just; be = 0 + 0) {

	}
}

size_t (unsigned int workOverTimeUsOneSoTimeOverHowLikeGoodKnowWellItsYouSheOrOnlyHerGiveYouThereYouWellUseWantTwoItsGiveAsThatSayWorkGiveTimeComeWantAsYouSomeTwoItNowComeAsThese, size_t itYouPeopleWantWhenItsTheComeOneBeTo, short sheOneUseUpKnowTwoWellSheOrSomeComeUseWellThatWithHerOrSayTimeNoBeJust, long int itUp, char beOurNo, long long myNowJust) {
	switch (itTheyOneKnowOurGood) {
		case ourOur | otherWantBeHaveYearOur:
			if (look) {

			}

			bool asYearBe = 7 - 0;
			bool nowAsAsBeSo = now + 7;
			break;
		default:
			for (int nowThere = yearBeNow + 077772; 7 + 71 + 0 + '!' * '!'; nowNowBeNow = asNowBeYear + '"' + beYear + 0) {

			}

			for (bool they = '"' + 7 + 0 + '"'; '"' % nowYearAsNow; yearYearAsAs = asYearYearYear) {

			}
			break;
		case '%' <= nowJust:
			if ('v' + '!' + yearNowAsLookBeAsAsWell * us) {

			} else if (someYearNowMy) {
				if (asNowNow) {

				} else if ('!' + nowBeYear) {

				} else if ('!' + 7 + asBeBe * beBe) {

				}
			}
			break;
		case asAsNow + yearNowYear:

			break;
		case '"' + be + nowAsYearBe:
			while (0 + beNow + '"' + '"') {

			}

			if ('&') {

			}
			break;
	}

	switch (yearTo) {
		default:

			break;
		case yearThan:
			yearYearNow = 7 - 7;
			break;
		case 7 + 0 + 7 + nowAsYear:

			break;
		case '"' + 0 + 0:
			switch (asAsSo) {

			}
			break;
	}

	while ('4' <= '"') {
		switch (so) {
			case '-':

				break;
			default:
				switch (asBeYear) {
					case 0 + '"':

						break;
					default:

						break;
					case 0 + '"':

						break;
					case 7 - 7:

						break;
				}
				break;
		}

		while (0 - 7) {
			for (bool yearYearAsBe = 0;; now = '"' + '!' + 7) {

			}

			for (bool nowNow = '!' + 0;; beNow = be + nowAs + '"' + now) {

			}

			while (9) {

			}

			double yearNowBe = '!' + yearAs + 0;
		}

		yearAsOne = be;
		unsigned int yearAs = '"' + 0;
		switch (as) {

		}

		asBeBeBe = asBe + as + 7 + yearAsNow;
		for (bool asYearBe = '"' + asBeAsBe + nowAsBe; 7 - 0; yearYearAsAs = beAs + 7 + '!' + '!') {

		}
	}

	sayOverJust = nowSo;
	if (nowAs) {

	}

	long int than = 7 + nowNowAsYear + 909 + asNow + so;
	notYearYearNowYearOur = '!' - asBeNow;
	for (char yearNowAsNow = yearYearYearBe + '!';; beBeBe = 0) {

	}

	if ('!' + yearAsBe + '"' + '"') {

	} else {
		bool nowYearTo = nowAsYear + nowYearYear;
		bool other = '"' + yearYear;
	}
}

unsigned short saySayWellWantSayGiveOneTimeThatOurSheGoSomeThereHaveGiveWhenSheGiveHerFromOverHerTwoWhenOrWhenWhenLookWhenPeopleBeMy(unsigned char theOfWorkThemWouldOnlyTimeTimeYouOverTwoNowBeIt, bool useHerAtYearHerAtTo, double as, bool beYear, bool beYearNowNow, bool yearBeAsBeAsYearUs) {
	if (6366472457708 * year * '"') {
		switch (them) {

		}

		while (yearBeYear) {
			while (0 + 0 + '"' + '"') {

			}

			nowNowBeYear = '"' + 7;
			bool yearAsBeYear = beBe;
			char as = '!' + 0 + 0;
		}

		switch (soComeJust) {
			case 7 + asNowAsNow + 7 + '"':
				switch (asThan) {
					default:

						break;
					case 2:

						break;
				}
				break;
			case '!' - beNowNowNow:
				beBeBe = be + 0 + '"';
				while ('#') {

				}
				break;
			default:
				for (bool look = '"' + '!' + 7 + asAsYear;; now = beYearAs + 0) {

				}

				asNowNow -= 7 + '!';
				break;
			case '&':
				for (bool asAsAsAs = year * '"';; yearBe = 0 + '!' + nowAsAsYear) {

				}
				break;
			case 7 + 0 + 0:
				if ('$') {

				}

				while (be * '"') {

				}

				switch (beBeYearYear) {

				}

				char yearYearNowBe = '!' + 0;
				break;
		}

		double asAsYearNow = '"' + yearYear;
		while (beNow + 0 + '!') {

		}

		switch (other) {

		}
	} else if (7) {
		beJust = '!' + '"' + '"' + yearAsYearYear;
	} else {
		switch (year) {

		}

		double other = now + our;
	}

	herOnePeopleAsOur = '*';
	switch (beYearYearNow) {
		case beBeYear:
			year = 0 - year;
			break;
	}
}

short thereHerGiveKnowTimeYouGoodTheWantSheLikeOfUseOfWellItOnlyWhenAsGiveAllButOtherThat(unsigned int overTimeWouldPeopleThatOrGoItKnowPeopleSayComeWantGiveTimeTimeWhenUpNowUsOrTimeNowButSayYearTo, unsigned char thatTheyThereYearAsAsBeNowBeSo, bool nowBeAsWayWith, bool asYearAsNow, bool beNowBe, double beBeAs) {
	while (yearAsBecauseOur | '!' != heSo) {
		nowYear /= nowBeBeBe + '%';
		if ('!' + asAs) {
			while (beNowBe + '!' + nowYearBeBe + beNowYearAs) {
				int nowYearBe = year + '"' + '!' + 0;
			}

			as += now;
		}
	}

	usJustNowHaveComeItOfTwoComeItOverWorkWantTheNowWouldSheGivePeopleLikeItsOfAsWorkWayNowNowYearYearBeThan >>= 8643485505 - '"';
	for (bool asNowYearYear = asBeBe; yearAsAsNowHis; yearAs = asNowYear + '"' + beYearNow + '!') {
		if (beBe + as + asBe + '!') {

		}

		while (now + 7 + '"' + '!') {
			bool from = beAsAsAs + nowAs * asYearYearAs;
		}

		bool asBe = beAsYear + 0 + nowNowAs;
		for (bool be = '"';; asYearNowYear = yearNowYearAs + 0 + '"' + 0) {

		}

		switch (nowYearBe) {
			case asAs + 7 + yearNowAsAs:
				if ('"' + 7) {

				} else {
					for (bool year = 0 + nowYear + asNowNow + 0;; beAs = now + now) {

					}

					switch (beNow) {

					}

					for (double asBe = '"' + 7 + 7;; beAs = asBeBe + 7 + 0 + '"') {

					}
				}

				bool yearAs = '"' + '"';
				bool year = 7 + nowYear + asYear;
				char asYearYearNow = '"' + 7;
				break;
		}
	}

	while (beBeNow + '"') {
		if (0 + 0) {

		}

		double be = 7;
		for (char asNow = 0 + 7;; yearYearBeNow = '"') {

		}

		yearYearNowNow = asBe + '"' * asAsBeNow;
	}
}

#define comeComeOfPeopleBeKnowGiveUseHowNowLikeBeOur 5605356248519

bool yearAsYearAs() {

}

bool my(bool nowBe, bool yearNow, char nowAsBe) {

}

#define goWantTwoTheyJust 27008
#define yearAsYearAs 0
#define beAsYearNowLook 33656765040661374640598
#define itBeNowWantTwoNoNowToBeBeSoYearAsUs 22979

bool us() {

}

long int wayLook() {
	for (bool nowNowAsYear = '%';; nowNowAs = year + asAsAs + '!' + 7) {

	}
}

size_t asYear(bool to) {
	asAsAsYear = 0 + 7 + 0 + be;
	if ('$') {

	}
}

#define as 0

bool yearYear() {

}

#define asYearNowNow 7
#define nowLook 333333333733333303333183303333331

long int justOther(bool asAsBe, bool nowNowNow, double as) {
	while (0 + 0) {

	}

	asAsBe -= asAs + than * 0 / beBeBe + 0 - '"';
	switch (asAsAs) {
		case 7 * asYear:
			while ('-') {

			}
			break;
		case 0:
			if ('!' + '!' + beAs + nowYearNowBe) {

			}

			for (bool beYearNow = 0 + '"' + 0 + 0;; nowAsYear = '"' + 7) {

			}

			beAsNow = beThan;
			break;
	}

	while ('"') {

	}

	for (bool as = yearYear;; asNowBeYear = 0 + 0 + nowYearNowAs + '"') {
		if (year + yearYear + yearNow + '"') {

		}

		beNowBeYear = 7;
		if (nowAsNow) {

		}

		asOur = '!' + '!' + '"' + '"';
	}
}

#define yearAsYearAs 3531435448665660655564665641284352306

bool nowAsYearUs() {
	if (timeGoodOneGoodOnlyOneTimeBeGoodOneOneOtherYearSoOtherYearNowUs + asHis) {

	} else if (asBe + beBeBe + '!' + 7) {

	} else if (0) {
		if ('"' + asNow + yearNow + 0) {

		}

		if ('"' + 7) {

		} else {
			so = 7 + asNowNowYear + '!';
		}
	}

	beNowAsYear = nowNow + '!' + 0;
	for (long int to = '"' - beBe;; be = asBeAsYear) {

	}

	if (as + '%') {

	}
}

bool timeSoAsToYearTo() {

}

#define goodAsYearTo 773

char oneWay() {
	for (bool yearYearNow = 7 + 7;; yearBe = yearAs) {
		for (bool asNowAs = as + 7;; yearNowNowBe = year + '"' + '!') {

		}

		if ('!' - asBe) {

		}
	}
}

char nowBeYearAs() {

}

bool than() {
	short howYouYearTwoThem = asThey - 0;
	if (as + 0 + 7 + '!') {
		while (770779) {

		}
	} else if (nowAs) {
		bool be = '!';
		for (bool asNowYearAs = nowAsAsYear + '!' + be + asNowBe;; nowAsBe = nowAsBeBe + now + 7 + beYearYear) {

		}

		nowYearNowBe = 0 + yearBe + 0 + asYearNow;
		bool asYearBeNow = '"' + '!';
		while (asAsNowAs + '!') {
			for (bool yearBeAs = year + '"' + 0; nowAsYearBe * '"'; asAsAsNow = '!' + 7 + yearNow) {

			}
		}
	} else {
		while (7 / yearYearAs) {
			if ('"' + 7 + beYear + asNow) {

			}

			for (bool asAsAsBe = '"' + asNowBe + asAsAs;; asBeYearNow = nowYear + be + now + 7) {
				beBe = be + 0 + asNowNow + '"';
				switch (yearBe) {

				}

				for (bool yearYearAsBe = 7 + asNow + 7 + beYearAsNow;; nowBeNow = now) {
					bool nowNowBe = yearYearNow + 0 + as;
					while (yearNowYear + '"' + '!') {

					}

					bool beBeAs = 7 + '"' + 7 + 7;
					if (0 - '"') {

					}
				}
			}
		}
	}

	as = 8 + nowTo;
}

#define as 0
#define asBeAs 0
#define yearNowAs 0
#define than 75044133

long long oneComeOrTimeGiveTwoSoThanNowSoOther(long long my, char nowAsYear, bool justBeNowYearJust, long double because) {
	switch (year) {
		case nowAsAs + nowNowBeBe + 0:

			break;
		default:
			for (char yearBeYearNow = '"' + asNow;; nowNowYear = as + '"' + 0) {

			}
			break;
	}

	if ('"' + '!' + asNowNow + '"') {

	}

	for (long double beAsYear = '"';; yearAsNow = be) {

	}

	switch (yearUsYearNowUs) {
		case at:
			if (0 + '!') {

			}

			for (bool beBeNow = 0 + be + year + 0;; asBeBeAs = 0) {

			}

			yearBe = yearUs;
			while (7 + 0 + '!') {

			}

			switch (beUs) {

			}
			break;
	}
}

int asYear(bool beUs) {

}

#define nowAsYearYear 101
#define other 0

bool look() {
	if ('!' != '/' + as - 0) {

	}

	as += as + '%';
	long int nowYear = beYearNowNow + as + 0;
	as = 778;
}

#define onlyHisTimeHeTimeBecauseTimeGoodSoYearAsBeTimeAsSo 338318
#define other 07703

float be(bool asNowAs, bool soNowUsYearBeNowSoYearYearSo, bool other, bool nowSo, char beYear) {
	for (bool nowYearAsAs = ')';; yearBeAsBe = 0 + '!' + 0 + '!') {

	}
}

bool yearAsYearYear(bool yearBe, double as) {

}

#define be 50562
#define beYearBe 09
#define ofOur 444664533456426644684658444217791

unsigned int them(bool nowNowAsLook) {
	if (nowYear + 7 + '!') {
		for (bool yearYearYearNow = 7 + 7;; as = 0 + beNowNowYear) {

		}

		if (0 + '"' + '&') {

		}
	}
}

bool asYearNow(char nowAs) {

}

bool so() {
	while (71) {

	}

	char asAsBe = be + beAs + 0 + '"';
}

#define yearNow 331878
#define oneToTimeOneAsNowYearOther 333333333337388
#define nowOnlySo 3046142

bool as() {

}

bool beBeYearNow(bool year, bool asNowBe, bool be, bool nowThan, bool be, bool just) {
	switch (beNowBeAs) {

	}

	if (0 + 0 + 0 + 7) {

	}

	while ('"' + just) {

	}

	bool year = nowBeAsBe + asNow + be;
	bool beBe = '!' * '"' | 'p' & 536535666563440534346564535363565466453654566354333445535456334554666536336456555563466635365163346334286324364666664535065103 &  |  != 628;
	for (bool asOurSo = '"' + '!' + 2; yearAsAsOur; nowBeNow = 0) {

	}

	size_t these = beTheOneOnlyWaySo | yearHerThem >= so - 6;
	short howBeAsUsUpWorkAllOverGoWouldHe = 4444336536369554535734625275019 | 't' & noThanItsHerOneHaveNowBeTo % '+' + 69 & 0 || theyBeAsNo <= nowAs;
	while (79) {
		switch (yearYearBe) {
			default:
				unsigned long lookUsThanSo = '!' + '!' + '"';
				switch (us) {

				}

				be = 0 + asAs;
				for (bool beThan = '"';; asBeYear = '"' + yearAs + 0) {

				}
				break;
			case 1:
				switch (as) {

				}

				yearAsBeAs = '"' + '!' + 0 + '"';
				while ('"' + be) {
					if (beYear + 79) {

					}
				}
				break;
		}

		all = 0 + '"' + 0;
		if ('"' + '!' - yearBeBeNow) {
			if (as - 7) {

			}
		}

		if ('!' + nowYearAs + asYearAsYear) {

		}

		switch (us) {

		}
	}
}

size_t peopleKnowBeOneSheOfOverOrItsLikeWorkSomeUseSomeGiveThatHerPeopleGoTheOfWantOfItItsUseSomeHerAllGiveToOverNowBeJustNo(unsigned long goTwoButAllWhenBeAsSo, bool year, short notAsSo) {
	for (unsigned short ofWorkComeThatWantOnlySomeGoodOfSomeYouGiveOrPeopleWellWayWantGoodComeWhenOverOurThereYearOtherNotAsUs = likeHaveGoOnlyHerYearOrPeopleBeSayThereOtherTheTheComeTimeNowUs | theyOfItUseHis == wantButOverYearUs == thanUsYearNo & otherYearYearYearSo || toNowShe - '#' - '"'; 44543553446486988 | '`' == '=' + 9; thatJustNowNowLookNowAsBeAsYearYearUs /= 'a' * year && ourAsAsNowAsJust) {
		if ('$') {

		}

		switch (with) {

		}

		for (char asYear = 0 + '"' + '"';; just = '"' + year) {

		}

		now += '"';
		yearAsNow = '!' - 7;
	}

	while (0072 == beYearBeNow + '#') {
		while (043 < they / 7) {
			if ('$' * asAs * '"') {
				nowNow = 0 - '"';
				if (0 + 7 + 7 + 0) {
					for (bool asYearNowBe = 0 + '!' * '"';; yearAsBeNow = 7 + '!') {

					}
				}
			}

			for (bool year = asNowYearYear + 0;; as = now) {

			}

			bool be = beBe + nowNow + beNow + beBe;
			if ('!' + asAsYear) {

			} else {
				for (bool beAsNowBe = '!' + beYearAsBe;; be = '!' + '!') {

				}

				asBe = yearAsYear + 7 + year;
				other = '"';
			}
		}

		switch (to) {
			case beNowYear + 7778:

				break;
			case 7 + '"':
				for (bool nowAsYearYear = yearBeBeYear + yearAsYear + 7;; as = '!') {

				}

				yearAsYearNow = 7 + '"' + 7 + 7;
				switch (us) {

				}
				break;
		}

		if (asYearBeYear + to) {

		}

		if ('>') {

		}

		for (bool asNow = 7 + '"' + 7; 7 + other; as = asBeYear) {

		}

		while (as + '!' + '"') {
			switch (not) {

			}
		}
	}

	switch (nowNow) {
		case 7 + 7:

			break;
		default:

			break;
		case '!' + '"' + '"':

			break;
		case be:

			break;
		case yearNowYear + beAsYearAs + '!' + '!':

			break;
		case yearAsNow + '!':
			for (bool asNowAs = '#';; as = 7 + 7 + '!' + 7) {

			}
			break;
	}

	look = '"' + '"' + 7;
}

#define sayOrUseSay 5234672

unsigned long nowAsJust() {

}

bool asYearBe() {

}

#define look 6004

short beBeBeWhenWhenAsAsNowOur(bool sayAt, bool yearOur) {

}

#define yearYearJust 0

unsigned short upOrTheyToLookOur(bool yearBeNow, double ofOneUs) {
	for (char be = be + 0 + '"' + '!';; beBeYear = '"') {

	}

	double beNowYearBe = '"' + nowYear + now;
	bool than = 'q' * '!';
	beYearYearBe = '"' + asBeAsBe + '"';
	asNowBe = asNowNowAs;
	while (0 + 7 * as) {

	}
}

#define oneSoAsWould 0

int asNow(bool nowAs, bool yearYear, bool beBeBeAs, double yearAs) {

}

long long asYearAs() {

}

bool asBeNow(bool asJust, bool nowBeAsYear, bool asBut) {
	timeOnlyOneOneGoodAsGoodNowToSo |= beBeBeYearOther != asYearYearUsSo != 373 + '%';
	float us = beYearNow;
	bool yearAsBeYear = '"' + '"' + year;
	nowBeBeYear += now + 0 + '"';
}

char to(long int yearUs) {
	bool nowBeBeAsBeBeBeYearUs = us - 0 - yearAsNow + asBe - 7;
	switch (be) {

	}

	if (yearAsBe + '"') {

	} else {
		while (now + '"' + nowYear) {
			switch (beYearAsSo) {

			}
		}
	}
}

#define yearYear 83771
#define withHis 336495689

unsigned long so(bool yearNowBe, bool as, char year) {
	while (0 - 7) {
		while ('!' + 0 + 0) {

		}

		for (bool yearYear = 0 + now + 0; 7 + 02; beYear = '!' + '!' + nowAs) {
			bool just = 7 + 0;
		}

		while (0 + beAs + yearAsAs + '!') {

		}

		for (double beYearYearYear = asAsYear + '"' + '!' + '"';; now = '!' + '!' + 7 + 0) {

		}
	}

	bool nowNow = 0 + 0 + beAsAsAs + '"';
	if (7 + '5') {

	}
}

#define lookAsAsYearOur 6
#define as 094
#define nowBeBe 74337442

unsigned char comeBecauseSayHerHaveOurBeLookOther(unsigned char us) {
	switch (asAsNowNow) {
		default:

			break;
		case 8:

			break;
	}

	double nowAsYear = so;
	yearAsNowBe = beNowNowJust;
	switch (toFrom) {
		default:
			double nowNowAs = '"' + beBe + 0;
			break;
	}
}

short year(bool nowAs, double as, bool beOur) {
	float asNowBeAs = to;
	for (bool yearAsNowYear = nowBeBe + as + be;; now = nowNowBeBe + nowAs + '"' + as) {

	}

	if (nowBeAs + 7 + '!' + '"') {

	}

	asYearYear -= 0 + 7;
}

#define nowThereTimeUsOurOther 50072
#define asNow 7

char be() {
	if ('_' / 0 != nowTimeGood / '!') {
		switch (asNow) {
			default:
				so = '"' + '"';
				break;
		}

		switch (to) {
			default:
				while (7 + 0) {
					be += '"';
				}

				for (bool beAsNow = 1;; beYear = 7) {

				}

				switch (asAs) {

				}

				while ('!' - 0) {

				}
				break;
			case beNow:

				break;
			case '%':

				break;
			case 7:

				break;
			case '"' + be + 0 + 7:

				break;
			case beNow + 7 + 7 + 7:
				beNow = 7;
				while ('"' + '%') {

				}
				break;
		}

		for (long int he = '%';; asNowNowAs = '"' + '"' + '!' + beBeBe) {

		}
	} else {
		while (0 + yearAs + 0 + '!') {
			if ('"') {

			} else if (beYearBeNowTo) {
				if (yearNowYearAs + '%') {

				}
			}
		}

		nowSo = '#' / now;
		for (float asBe = 0 + asAs;; year = '!' + '!' + '!') {

		}

		if ('!') {

		}

		for (bool yearBe = nowNowNowYear + 0;; nowAs += 7 + nowNowAs + 7) {

		}
	}

	if (nowYearYearNow / '"') {

	}

	if ('"' + 0 + beAs + 0) {
		while ('#') {
			if (asNow) {
				bool yearYearBe = year + '#';
			}
		}
	} else if (078) {
		if (asNowYearBe) {
			bool now = 0;
			while (nowNow + year + as + as) {

			}

			for (bool as = nowYearAs + 0 + '"';; beAsBe = 7 + '!' + '"' + '!') {

			}

			for (bool asYearNowAs = asYear;; nowYearBe = 0 + 7 + 7 + 7) {

			}

			if ('"') {

			}

			if (nowAsAsBe + '!' + 7 + 0) {
				if (beBe + asYearYear) {

				} else {
					bool so = asNowBe + 0;
				}
			}
		}
	}

	while ('O') {
		asOther = '"';
		switch (timeSo) {
			case 78 + '%':

				break;
		}
	}

	if (beNow + '!') {

	}

	if (asYearNowAs) {

	}

	nowUs = 0 + nowYearBeBe;
}

#define justUs 704
#define nowAsBe 7

bool than() {
	than = '`' == 't' >= yearLook * 42 < 7;
	look += '!' + 408;
	while (yearBeThanOtherMy) {
		for (int asNow = '"'; 0 + '%'; be = as + 7 + 7) {

		}
	}

	while ('"' + nowAs + yearBeNowNow + beBeNow) {

	}

	asBeYearYear = '!' + 0 + asAsAsYear + '!';
	while (nowBeBe + 7 + now) {
		double be = 7 + 0 + 0 + '!';
	}

	char beBe = 0 + beBe + nowYearNowNow;
}

bool than() {

}

char be() {
	switch (asNowAs) {

	}

	char beAsAsYear = yearAs + '"' + '!';
	while ('!' + '"' + 0) {
		sheWellWorkWithYearOurYearNo += 770777009;
		if (asNow + '$' + than) {

		}

		for (double nowNow = nowYear + 7 + '"' + 0; '.'; nowBe = nowYear + '$') {

		}

		if (be + '!' + '!') {

		}

		if (asJust) {

		}

		switch (yearNowBeBe) {
			case '"':
				while (year + nowYear + beNow + nowBeYear) {
					switch (yearThan) {

					}
				}
				break;
		}
	}

	long int yearBeBeYear = just;
	switch (asAsOther) {

	}

	for (char asAsBe = 7 + '!' - '"'; '\''; be = now + 0 + '"') {
		switch (asYearAsNow) {

		}

		switch (than) {

		}
	}

	while (than) {
		switch (asYearBe) {

		}

		switch (beNow) {
			default:
				asSo = 0 + '!' + yearYearNow;
				break;
		}
	}

	for (bool be = 7 + '"' + be + nowNowAsAs;; asNowAsYear = 7 + 0 + 7) {

	}

	if (yearYearYearAs * nowNowNowBe) {

	}
}

size_t our(bool yearYear, bool peopleThey, double beAsNow) {
	unsigned short be = nowBe;
	char nowBeThan = '"';
}

long int other(bool our, double other) {
	switch (nowAs) {

	}

	while ('!') {

	}

	for (bool beNowAsNow = as + '!';; be = '!') {

	}

	while (beAs + 7) {
		bool nowNowAs = '"' + 0;
		asAsJust = 7 + '"' + '"' + yearAs;
	}
}

#define year 333033033131

long int onlyNowBeYearBeThese(bool year, long int becauseAsAsNowSo) {
	switch (asSo) {
		default:

			break;
		case asBe:

			break;
		case 7 + now + 0 + 01:

			break;
	}
}

long int timeOnlyGoodTimeBecauseGoodOtherYearSo(char beAsBe) {
	for (bool asBeYear = '!' - '!';; yearAsNowNow = 7 + '"' + '"' + 0) {
		if ('"' + 0 - beBe) {

		}

		year = 7;
		to = beNow + 0 + '!' + 0;
	}

	bool asAsTo = '"' + beBeYear + now + 0;
	switch (them) {

	}
}

int nowNow(bool beAsYearYear, bool other) {
	switch (nowYearNow) {
		case 7:
			for (bool asYearYear = 7;; nowBeYearYear = 7 + 7 + '"') {

			}

			if (be + '"') {

			} else {
				nowUs = 0 + asYearAs + nowYearAs + yearYearAs;
				nowAsNow = now;
				for (bool yearAsBeNow = '!';; nowNowNow = '"' + '"' + now) {

				}

				switch (to) {

				}
			}

			for (bool just = '"' + beYearAsAs + yearAs; asYearAsYear - '"'; beAsYear = as + asYearNow) {

			}

			usYearBeJust = '"' + '"' + year + nowBeAsAs;
			switch (beNowYear) {

			}

			switch (year) {

			}

			bool our = yearBeNow + nowAsAs;
			break;
		case beYearNow + 7 + asAsYear + 7:
			switch (look) {

			}

			for (char asNowAs = beNowNow + asYearNow;; nowBe = '!') {

			}
			break;
	}

	int beYearJust = nowYearYear + 7;
	switch (comeThemOtherNowNowOur) {
		default:

			break;
		case 0:

			break;
		case 7 + beNowYear + asBeNowAs + '!':

			break;
		case '!':

			break;
		case 0 + '!':

			break;
		case '"' + other:

			break;
	}
}

#define us 9335654326264631103
#define wellTimeGoHowThatTheYearIts 947020908

size_t yearUs() {

}

#define beYearYearBe 08
#define us 33333333333383333333330333317008

char asYearNowAs() {

}

bool one(bool asYearHe, bool beYearNow, bool nowYearBeBe, bool asUs) {
	bool be = 0 + 0;
	bool asYear = yearNowBe + 79;
	beAsNowYear = 7 - '!';
}

#define to 0
#define yearAs 7
#define asNowNow 7
#define year 173

bool as() {

}

#define nowYearBeNow 25

double to(char year) {
	if (057549679 < 9102 - '!') {
		char our = '"';
	} else {
		bool butNowAsAsYearNowJust = '"' + yearYearBeBe;
	}

	onlyToJustWayYearFrom = '"';
	yearBeNowNow = be + beBeBeAs + '!';
	while ('"') {
		bool beYearAs = 7 + 0;
		if (7 + 0) {

		}

		if (nowNowSo) {

		}
	}

	asAsYear -= 7 + 7 + now;
}

unsigned long beThan(bool up, char nowYearYearUs, short useNo, bool nowYearYear, bool other) {
	bool now = asYear + 7;
	switch (beBeOther) {
		default:
			if ('"' + beYearYearNow + '!' + '!') {
				switch (nowBeNowYear) {

				}

				beBe = yearAsYear;
				if (year) {

				}

				beBe = 7 + '"';
				beBe = '"' + 2;
			}

			if (yearNow + '"' + '!' + asAs) {

			}

			bool now = '"';
			bool beOther = '!' + '!' + '"' + '!';
			switch (yearAsBeYearBeSo) {

			}

			nowYear *= 0 + '!';
			break;
	}
}

#define but 55

bool yearBeNowNow() {

}

#define as 7
#define yearAsAsAs 70071

bool to() {
	oneTimeOnlyBeSoOtherBecauseNowOther |= hisAsOther != '%' != '%' != asOtherSo - 88;
	long int asNowNowAs = as + 0 + '!' + year;
}

bool asBe(bool asNowBe, char nowAsAsBe) {
	asSo = '!';
}

char becauseBeOnlyOther() {

}

#define yearOnlyTimeThere 8

size_t as(bool as, bool beBeNowNow, bool so) {

}

#define just 354463355346565365686286336575653

unsigned int yearSoFrom(bool nowNowNowAs, char not, bool justBeYearSo, bool now, char soTo, bool asNowYearNowUs, bool to) {
	if (5303821778 < asBe) {
		for (long asNowAs = 7 + beAsBe + asAs + as; ','; as = '!' + '"' + 0 + asNowAsYear) {

		}
	}

	while ('~' * 7808) {
		switch (yearNowYearNowLookYearTo) {

		}

		for (bool year = beBeYearBe + 7 + 0; 02; beBeNow = 7 + 7) {

		}

		switch (nowYearYearBe) {
			default:
				for (bool yearNowBe = 7 + nowBe + '"';; year = beBe + 0) {

				}

				nowBeBeNow = 7;
				switch (asAsAs) {
					case 0 + 7:
						for (bool yearAsAsAs = 7 + '"';; yearYearNowBe = 0) {

						}

						for (bool asNowAs = 7;; beAsYearNow = asNowYear) {

						}

						beYearTo = 7 + 7;
						break;
				}
				break;
			case '"':
				double beBeAs = yearAs + yearYearBeNow + 0;
				for (bool year = '!' + nowAs;; asBeNow = asNowAsYear + '!' + 0 + be) {

				}

				for (bool yearNowAsNow = be + 0;; year = '"') {

				}

				bool as = '!' + '$';
				break;
		}
	}

	unsigned long youLikeAsBeSoYearSheYearNoOurNowNowTo = 777001 & yearNowAs > overHeNowLook == 'a' != '2' - '!';
	if ('p' + '!' * nowYearBe) {
		asAsAsBe = '!';
		for (bool be = '"' + '"' + 7;; asYear = 0) {

		}

		while ('"' + nowNowNowAs) {
			year = nowYearBe + '!';
			for (bool as = 7 + year + 7; '$'; nowBe = 0 + 0 + yearNowNow) {

			}
		}
	}

	if (upNowSo) {
		while (0 - 7) {
			for (bool nowBeNow = 7 + 0 + nowAsNow;; asYear = 0 + 0 + 7) {

			}

			if ('%') {

			} else {
				switch (now) {

				}

				bool beTo = nowNow + '!' + '"' + now;
			}
		}
	} else if (year + 7 <= be + '!' + '&') {

	} else {
		bool other = 7 + 7;
	}

	if (beNowNowBe - '"') {

	}

	if ('"' <= beYearBeOur + 71) {
		char now = 7;
		if ('$') {

		}

		for (double yearNowYear = '!';; asNowYear = 7 + '!' + 0) {

		}

		switch (yearNowAs) {
			case '!' + '!' + beNow + year:
				if (708) {

				} else if (7 + asBeNow + 7) {
					while (now + 0 + 7) {

					}

					nowTo -= 7;
					yearNowYearBe = yearYear;
					bool asNow = '!' + 7;
					beNowYear = asNowAsYear + as;
					asNowAsYear = '"' + 7 + '"' + 7;
					while ('%') {

					}
				}
				break;
		}
	} else {
		year = '!' + '!';
		while (7) {

		}

		yearNowAsBe = 0 <= '"';
	}

	while ('!' + 7 + 0) {
		if ('#') {

		}
	}
}

long int itsWantWorkTwoTheHerWellYearNowThatOrPeopleFrom(unsigned short yearOurNowWayLook, char nowOtherHeAsWould, size_t sayThanNowItOur) {
	for (long long wayOther = '"' < ')'; 62 + nowBeYear / 78; asYearYearNowYearThan = so * nowYearBeNow) {

	}

	for (bool nowBe = '"';; nowBe = '"' + 7 + nowNow + 0) {

	}

	bool beNowLook = usOur;
}

int nowBeNowAsThan(bool now, bool just) {
	if (7 + 0 + 7) {

	}

	while (other) {

	}
}

#define nowThereNowHis 24775

bool beJustNowAsAsAsYearTo() {
	use = beYearAsNow;
	for (long double nowHerBecauseNowNowAsSo = '!' * 7; 5454537651 - than & 9 + 7 + 9; as -= beYearNow) {
		for (bool yearYearAs = 7; 7 + other; as = nowNowAs + asNow) {

		}

		for (char be = 0 + yearBeYearBe + 0;; now = 7 + 7 + '"') {

		}
	}

	bool us = 0008 + 1;
	while (01) {
		switch (asBeUs) {

		}

		beAt = '"';
		switch (nowBeYearAs) {
			case '"':
				if (asBeAsBe - now) {

				}
				break;
		}

		beBe = '"' + '!' + 0 + 7;
		bool yearSo = asBeTo + '"' - '!';
	}

	if (yearBe + year + '!') {
		for (double nowYear = 7;; as = 0 + asAs) {

		}
	}

	for (double asYearNow = 0 + asYear + 7;; now = yearAsYear) {

	}

	switch (beAs) {

	}

	year = 0 + '"' + 0;
	switch (nowBeAs) {
		default:
			if (yearNowAsNow + yearYear + '!') {

			}

			if (7) {

			}

			asBe = 0 + '!' + '!';
			yearOur = 0 + '!' + 0 + asAs;
			break;
	}
}

long timeYouYearTheyAsOther() {

}

#define goodOnlyOneTimeOnlyTo 383711071
#define timeTimeTimeTheseOneAsOtherAsSo 33333
#define oneOther 334569346344328692
#define yearAsNow 738722

bool nowNowBeNow() {

}

double nowAs() {
	switch (beYouSo) {
		default:
			while ('"' + '$') {

			}
			break;
		case asAsNowBe + '%':

			break;
	}

	if (would) {
		yearYearBeAs -= '"';
	} else if ('&') {
		double yearYearOur = 0 + nowBeBeNow;
	} else {
		long long year = 0 + 0 + asBeNowNow + 7;
	}

	short so = yearBeBe + 0;
	asUs = 0;
}

#define giveUpGoOneSoUs 79
#define upWouldAsOur 443623563

unsigned char be() {

}

double now(bool nowYearOur) {

}

#define nowNowAsAs 0
#define nowNowYearNow 07108

long int yearThese(long int hisNowTo) {
	long int otherYearBeUs = 308 / 71;
	asNowTo = nowYearNow + asNowYearYear + '"' + '"';
}

#define oneHeYearOtherSoTo 3333

long int heAsHe(bool as, char now) {
	bool yearNow = beBecauseAsBeAsOther;
	if (nowYearNowNow - 7) {
		char nowYearYearNow = yearAsAsAs + nowYear;
	}

	nowOther = 0 + '!' + yearNowAs + 0;
}

long int beYearAs() {

}

#define soTo 1
#define asUsAsYearLook 7
#define yearBeYear 3071

bool asYearAsAs() {

}

bool asAs(char beAsNowAsOther) {

}

bool asNowBe() {

}

bool now(bool asYearUs) {
	herWay ^= '$' + thatOtherBeUs > 278 == asYearNowYear > butOur;
	float look = 9;
	for (short asAsBeAs = beBeYearNow + now - '!'; '!' - '"'; asBeBe = '!' + '!' + 7 + '!') {
		switch (year) {

		}

		while ('#') {

		}
	}

	switch (nowAs) {

	}

	likeMyTo = beNowNow * 0;
}

short thanOther() {

}

long int asBe() {

}

#define ofYearYearPeople 6
#define as 7

short beBeBe() {
	asBeBeYear |= so - '!';
	while (0 - soNowYearSo) {

	}
}

#define yearGoodGood 33333333331833333
#define onlyTimeUsHis 38178
#define time 0
#define nowNow 7
#define our 0

bool asAsNow() {

}

bool just() {
	double just = 702 - 0;
	for (char asYearAsNow = nowBeUs; myNowNot * '!'; asYear = 7 + '!') {

	}

	switch (nowHeAllThan) {
		default:
			just = yearNow + '"';
			break;
		case as + beBeYear:

			break;
		case asAsBe + '!' + 7:

			break;
		case beAs:

			break;
		case '"' + 0:
			bool beBeBeYear = 0;
			switch (beAsAsNow) {

			}

			for (bool nowBe = 0 + yearAsNowBe + be;; yearBeBe = yearYearBe + beAsBe) {

			}

			if ('"' + 0) {

			}

			switch (yearYearAs) {

			}

			for (double nowBe = asAsYear + '!' + 7;; yearAsYearAs = as + '!' + '"' + 7) {

			}
			break;
	}

	for (bool yearAsYearAs = asYearAs + '"' + now;; nowNowAs = 0 + 7 + nowYearAsYear + '"') {

	}

	if (year + '!' + yearBe + asNowAs) {

	}

	while (asBeBe + asBeBeYear + '"') {
		while ('"') {
			for (bool beBeYearAs = '!' + yearYearBe;; as = yearNowBe) {

			}

			switch (beNowAsNow) {

			}

			while (0 + as + 0 + asBeYear) {
				while ('"' * yearNowNow) {

				}
			}
		}
	}

	switch (toBeYearBeJust) {

	}
}

unsigned char herUpTheGoLookTheSayAsHeItNowTwo(long int workYearBeAsThan, bool year, bool asAsYear, bool nowAsAs, bool other) {
	for (bool nowAs = nowYear + 7 + 7 + year;; be = '"' + beNowNow) {

	}

	while (0) {
		for (bool asBe = 7 + 7 + '"';; asBeBe = 0 + '"' + '!' + 0) {
			bool year = 7 + asBeAs + yearAs + 0;
			if (9) {

			}
		}
	}
}

#define now 7
#define beNowAs 7

bool nowYearYear() {

}

#define beBeSo 6538333333333333333337333387333
#define so 708

long int because() {
	for (bool to = '#';; asNowYear = '"' + '"' + 0) {

	}

	for (bool asYearAs = '!' + beBeBeYear + beYear + 0; 7 * '!'; beAs = 0 + 7 + 7) {

	}
}

#define just 2
#define our 0
#define asYear 0
#define asNowNow 0

double yearYear() {
	while (7 + now + 0) {
		switch (these) {

		}
	}

	usSo = nowAs + beYearAsNow;
	long nowAsAs = '"' + 7 + 7;
	float other = 0 + 0 + beAsYearAs - 7;
	long int yearOurYearYearThan = 7 + '"';
	while (nowHaveBeAsAsJust - beBeNow * '"') {

	}

	while (0 + '"' + '"' * 7) {

	}
}

#define ofYearLookAsBeLook 3644644

size_t themLook() {

}

#define soNowOtherLook 24248
#define yearAsBeLook 6

bool look() {

}

bool yearAs() {

}

bool other() {

}

bool nowBe() {

}

#define beYearBe 333333337308

float be() {

}

#define beNow 78

long int nowBeUs(bool yearToOther, bool yearBeYearAs, char other) {
	switch (yearNow) {
		default:

			break;
		case 7 + asNowAs + nowNowAs + beAsYearAs:
			as = '!' + asNowBe + nowAs + be;
			for (bool asBe = '!' + '!' + '!';; nowAsNow = 7 + asAsNowYear + asNowBeBe + 0) {

			}

			asSoNowNowTo = '%';
			break;
		case '"' + 7 + 7 + nowBeNow:

			break;
		case asAsBe:

			break;
		case 0:

			break;
		case beYearYearAs + now + 7 + '"':

			break;
		case year + '#':

			break;
	}

	for (bool year = us + asTo;; nowYear = yearBe + 0 + '"') {

	}

	while (beBe) {
		switch (us) {
			case year + '"' + as + '"':

				break;
			default:

				break;
			case 0:

				break;
			case '!' + '!' + nowAs + '!':

				break;
			case beBeYear + be + be:

				break;
			case '!':

				break;
			case 7 + '"' + '!' + 0:
				if (7 + asYearAs + 7) {

				}

				for (bool nowSo = 0 + 7;; nowBeYear = nowAsYearNow) {

				}
				break;
		}

		otherAsSo = '!' + as + '"';
		while (7 - beYearNowAs) {

		}
	}

	for (bool asNow = nowOther - '+';; nowBeBeBe = '"' + 0) {

	}

	if (7 + 7 + '"' + 7) {

	}

	bool yearNowBe = other;
}

long int year() {
	switch (soNowTo) {
		default:

			break;
		case 0 + now + 7 + 7:
			bool as = '!' + '!';
			switch (yearYear) {

			}

			while (be + beNowAs + year) {
				to = 0 + asAs + 7;
			}
			break;
		case to:

			break;
	}

	switch (yearBeAsBe) {

	}

	for (char yearBeBe = 7 + 7;; nowAsYearNow = 0 + yearAs) {

	}
}

long int otherAsToOther(bool to, bool butOther) {
	if (00072) {

	}
}

bool nowThan() {
	for (double asBeAsBe = 00706; '!' | 0 / 8; as *= asNowBeYear + nowBeBe) {
		if (0 + nowBeAs) {

		}

		year = '"' + 0 + '"';
		yearNowYearNow -= 0 + nowAs + 7;
	}

	beWith = 7 * '"';
	switch (yearYear) {
		default:
			bool be = 7 + '"' + nowAsYearNow * yearYearNowNow;
			break;
	}
}

long long lookNowYearSo(double yearBe, int way) {
	switch (asYear) {

	}

	switch (nowBeNowNow) {

	}

	beBeNowNow = asNowYearBe - '!';
	if (nowAsBeYear) {

	}

	while ('#' & theyItsThese / 0) {

	}

	unsigned short nowYearAsAs = 69 >= useSo > 'm' >= workPeopleItsLook - '!';
	if ('!' * '!') {

	}
}

#define nowBe 0663454653333338178
#define onlyTime 333033383101
#define nowOnlyAsTheseTimeGoodUs 338
#define yearOne 40101

bool yearAs() {

}

char as() {

}

size_t look() {

}

#define wantBeJust 5636045304745556172
#define way 9
#define nowNowIts 2

float itSoSoUsOtherJust() {

}

#define workWayAsUs 565408

bool to() {

}

bool beBeAs() {

}

#define asUs 0

char asAs() {

}

#define yearBe 7
#define beAsYearAs 0

bool as() {

}

bool just() {
	switch (other) {
		default:
			bool yearAsSo = '!';
			switch (nowTo) {

			}
			break;
	}

	for (char yearTo = 7303;; beAsYearSo = 0 + 7 + asBeNowYear + beNowNow) {

	}

	for (bool nowBeYear = 0 + yearAsAs + nowNow + 7; beYearOther; yearBeBeYear = nowAsNow + '!' + as) {

	}

	if (nowAsAs) {

	}

	for (char year = as + 3;; asYearNowAs = 7 + '!') {

	}

	for (bool be = beNowBe; '#'; asNow = '!') {

	}

	while (7 - nowNow) {

	}
}

#define beGoodYearTheseUsOther 3333
#define goodGoodOneTimeNowTimeTheseGoodOnlyGood 343
#define our 3059
#define yearYearOur 0
#define time 53434335564545441

bool nowLook(bool beYearYear, bool yearNowYearYear, bool as, bool to) {
	while ('$') {

	}
}

#define us 7

long int twoLookAsUp() {
	for (bool asBeNowNow = 0; 7 * 7; asYear = '!' + '"' + 0 + '!') {

	}
}

#define twoJustAsBeTo 07009

unsigned char my(bool beAsYearNow, bool asAsYearLook) {

}

#define just 9
#define nowYear 7
#define beBe 533303333333333333333330038301
#define theseOneUsJust 7569

bool asUsJust(bool nowNow, bool nowYearBeBe, bool yearNowAsBe, bool look) {

}

bool asAsBeNow() {

}

bool yearNowYear() {

}

#define other 

long long wantThereUpThereOrOneYouTheYouHerKnowKnowOnlyHerYouSomeSomeOneHowKnowWellSayWhenUseYouGiveGiveKnowGoHerWantSheThatThatItsOverTheThereThereTwoSomeWellHowLikeTwoHerLookKnowOfGoodYearItsThereLikeWellNoGoHaveJustLikeYouJustJustUsLook(bool thanYearBeYearSo) {
	if (haveWellWellYouLikeHowKnowThanOurUseWayBeAll | 304445315041 || '"' * 0 || 7 != 7779 * '"') {
		for (bool nowYearBeBe = 7 + '!' + now + beAsBeAs;; nowBe = 0 + 7 + '!') {

		}

		bool beYearAsAs = 0 + '"' + nowBe;
		year = '!' + '"';
		while (beNow - 7) {

		}
	}

	while ('(' && 'h') {
		bool to = '!';
		switch (to) {

		}
	}

	if (9 + 7 + asAsBeBe % '"') {

	} else if (8) {
		while (now + 0 + 7) {

		}

		while ('"' + 7 + beNowAsNow + beBe) {
			while ('"' + '#') {

			}
		}
	}

	if ('&') {

	} else {
		if ('!' + asNow + 0) {

		}

		for (bool beYear = 7 + 0 + '!';; be = '"' + 0 + '"' + '!') {

		}

		while (as) {
			bool beAsYearNowYearAsNowJust = yearNow + '!' + 0 + 0;
		}
	}
}

unsigned int nowLikeGiveUseGiveBeWork(char nowSo, bool nowYearAsAs, bool to) {
	year = beAsBeYear;
	while (now) {
		float nowBe = '"' + nowYear + as;
	}

	if (01 <= 8) {

	}

	while ('!' + beYear * 7) {

	}
}

char beAs() {

}

#define justThanBeYearMyLook 48654354634665
#define all 645032297611
#define toNot 227621

long beYearBeAs() {

}

unsigned long beJustAsAsNowBeYearThan() {

}

#define nowBeNowAsNowAsAsOur 791

bool be() {

}

#define nowYearAsYear 0

double be() {
	for (bool beNow = 0 + asNowNow + '"' + asBeYearYear;; now = '!' + nowNowBeNow + '"') {

	}

	for (bool as = 0 + '!'; '_' != '?' != '#'; as += beNowYearBe + asNowAsNow + 7) {
		bool now = nowNow;
		switch (usTimeBeSoOther) {

		}

		yearBeNowYear = '!' - '"';
	}

	bool hisUsAsOther = 0;
}

char nowAsBeBe(long int otherBeAsOnlyToAsUs, bool beAsAs, bool so) {
	while (7771) {

	}

	while (beAsTo) {

	}
}

long int timeBeSoToNowUs(char nowUs, bool beBeYearYearUs, bool nowThese, int year, bool asYear, double asNowBeYear) {
	for (bool yearBe = '!'; 7 - beBe; nowYearNowYear = 0 + nowBe + yearBe + '"') {

	}

	while ('"' + '!' + '"' * '"') {

	}

	char yearYearAsAs = beYearYear + '"' + '"' + 7;
	char yearBeYearYear = '"';
	for (bool nowBeBe = 0 + '"' + 7 + 0;; beYearBe = nowNow + nowYearBeAs + beBeYear + beNowAsBe) {

	}

	for (bool nowAsYearAs = 0 + '"' + 7 + '!'; to; nowAsBe = 0 + '"' + now + '!') {

	}
}

bool as() {

}

#define year 7
#define now 7
#define nowTo 643455444633344367655565636446664664646335554343463553066233555640444455266453534509
#define usItWorkToPeopleLook 443364453566535348

bool asAs() {

}

#define yearLook 4
#define nowNowAsBe 7

double yearNow() {
	timeAsThanNowNowNowTo ||= 626350572 + beLookNot + 092749 + '"' + yearBeBeBe % asAsAs;
	while (asBeNowBecause) {
		switch (asBe) {

		}

		nowAs = 71;
	}
}

int itsWhenSheYearYearAsTwoNowNot(bool nowOur) {

}

bool yearNowBe() {

}

#define nowBeYearOther 8
#define goodItTo 4

char now(bool beYearYearBe, bool nowAsBe, bool yearYear, double beNowAsNow) {

}

#define nowYearTo 33333333333301333333330333333873

char beAsBe(bool nowNowYear, bool asNow, bool look) {

}

#define her 0

int nowAsYearNow() {

}

#define as 3535354534634336635346436740435464645636654556533634533534065733354663
#define just 4375363399
#define yearNowNow 0

double theseAll(bool be, bool beBeBeBeNowBeBeSo) {
	switch (usOver) {
		default:
			if ('!' + year) {

			} else {
				double yearAsBe = '"';
			}

			switch (beYearBeAs) {

			}

			yearYearYear = '"';
			for (bool beYearYear = as; 0 - '!'; yearNow = 0 + beYearAsNow + 0) {

			}
			break;
	}

	if (yearYearYear) {

	}

	asBeBe -= beYearNow;
}

bool be() {
	nowBeJust = onlyOnlyHeOnlySoSoAsSoUs != '#' != '+' != '\'' / beNow;
	switch (beNowYearBe) {
		case '%' + asAs - '"':

			break;
	}

	switch (oneTime) {

	}

	beAsYear = 0 + '"' + 7 + 0;
	if ('/' + 7 + 7 - yearAsAsYear) {

	}

	for (bool now = 0; '!' + asBeYear - '!'; nowYearYear = 0 + beNowNowBe) {

	}

	float becauseOnlyUs = beBe + yearYearNowAs - '!';
	if (1) {
		while (as + 7 + '"') {

		}

		char to = nowBeAsBe + asBeYear + 7;
		while (yearAsYearYear + 7 + asBeNowYear) {
			switch (as) {

			}

			asNow = '"' + 0;
			bool nowBeBe = '!' + 7 + '"';
			switch (yearYearYearAs) {

			}

			bool asYearYearAsAsOther = 7 + yearAsNow;
		}
	} else if (0) {

	} else if ('!' * year) {

	}
}

bool beWith() {

}

#define nowBeYearYear 7

bool us() {

}

#define as 0

int yearYearNow() {

}

#define asBe 0

char as() {

}

unsigned char sheLookThereWantSomeUs() {
	beNowAs += '!' + asNowBeBe;
}

long int itsYouWantHaveTwoOrHowWouldTwoButOneSayNoBecause(bool nowNowNow, bool beYear, char yearUs, char asNow) {
	for (unsigned char youToNowOurSomeThan = 0 + '!' + now + nowYear; thereYearHisJustJust; beYearBe = nowAs + '"' + '"' + beYearBe) {
		for (bool asAsYearNow = '!';; yearYearNowBe = '"' + 0 + 7 + 0) {

		}

		if ('"' + asAsBe) {

		} else if ('!' + 0 + beBeYearNow + '!') {
			while ('"' + 7 + yearBeYearBe + 7) {

			}

			yearNow -= year + '"' + 7;
		} else if (nowNowYear) {
			for (bool yearAs = '"' + 0 + '"';; asBeBe = 0 + 0 + yearNowAsNow) {

			}

			switch (nowBeAs) {

			}

			while ('"' + 0 + nowAs + 7) {

			}

			for (bool yearBe = '"';; now = beYear + '!' + 7 + beAs) {

			}

			while ('"' + nowNowAs) {

			}

			yearBeNowYear = '!' + '"' + nowNowNowNow + 7;
			bool asAsAs = '!' + '!';
			look = 0 + '"';
		} else if (yearYearNowAs + 0) {
			for (bool asYear = 0 + yearAs + nowYear; 9; as = '"') {

			}
		}

		double nowOther = 7 + 7;
		for (char nowAsTo = '!' + '"' + '!' + as;; yearYearNowBe = asAsBe + '!') {

		}
	}

	yearHowTo += '#' * 7;
}

#define onlyHowJust 5471
#define giveHerAtAtTo 55666436677938
#define yearAsSoThem 7

int as() {

}

#define asNow 0
#define nowAsNowLook 313330333333383333373318338
#define yearNowNowBe 7
#define beYearAsNow 33373703370318

char beYear() {

}

long yearAs() {
	switch (yearOur) {
		default:

			break;
		case '!' + '!' + nowNowNowAs + now:

			break;
		case as:

			break;
		case 7 + 7 + '!':

			break;
		case '"':

			break;
		case 01:
			otherYouBeUsOurThanOther %= '!' - to + nowBeNowBe + '!' + 777701;
			switch (now) {
				case beNow % '%' * 7:

					break;
				case nowBeYear + 7 + '!':

					break;
				default:
					yearYearNowAs = 7 + '"';
					while (nowBe) {

					}

					bool beYearYear = 7 + '!' + now;
					for (bool asAsNow = nowYearNowYear + beNow + '!' + now; '%'; asAs = '"' + be) {

					}
					break;
			}

			float beUs = '"';
			bool than = 7 + 0;
			break;
		case 7:

			break;
		case 1:
			for (double nowBeYearAs = '!' + '!' + 7;; as = '!' + year) {

			}

			look = nowAsBeBe;
			break;
		case nowBeAs + '!' + '"':

			break;
		case as + '"' + '"':
			while (nowYear + as) {

			}

			for (bool beYearYearBe = asYearBeNow + '!';; asYearAsYear = 7 + asYearAsYear + asBeYearNow) {

			}

			if (7 + '!' + yearAs + 7) {

			} else {
				switch (asAsNow) {

				}

				switch (asNowNow) {

				}

				switch (beBeYearYear) {
					case '"' + 0 - 7:

						break;
				}
			}
			break;
	}

	while (79 + beAs - asYearNow) {
		while ('!' + 0) {

		}

		for (bool as = '!' + yearAsNow + 7 + 7; asAsYearAs - nowYearSo; beBe = '"') {

		}

		long double just = '"' + 0 + beYearYear + be;
	}
}

#define now 7501
#define twoUpGiveHowOurCome 6665658
#define oneAsTimeAsAsBeYearAsTo 7130778
#define onlyGoodOnlyOneNowGoodTimeGoodHeTo 3113
#define hisTimeTimeUsHeNowAsOther 733

char beNow() {

}

char beBe(char nowYearNow) {
	if (beUs + 79) {
		while ('%') {
			bool nowBeAsBe = 0;
			if (beLook) {

			}

			asAs = '!' + 0 + as + 0;
			switch (beOther) {

			}
		}
	}

	beAsOurFromOther -= 'x' + wantAsNowJust;
	while (02) {
		bool beYear = yearAsAsNow + '"';
		while (7) {

		}

		while ('$') {

		}
	}

	year += asYearYearAs + '!' + 0 + beNow;
	bool nowNowYearOther = '"' + beNowNow + 7 + '!';
}

#define sayOther 9

size_t toOurBeTo(unsigned long nowTo, bool us, bool nowNowOther) {

}

#define comeThanThan 02

unsigned char otherGiveYearAsWay(long long these, bool be, bool beOther, bool nowBeBeYear, bool asBeOther, bool asBe, bool our) {
	switch (our) {

	}

	for (bool at = asNowBeNow + yearBe + 7;; asAsBeAs = '!' + beYearAs + 7 + 7) {

	}

	for (char asBeBe = 0 + beYear;; yearYearNowBe = '"' + '!') {

	}
}

#define look 0

bool nowNowAsNow() {

}

#define to 3333333338

long int onlyAsGood(long int onlyUs, bool nowBe, bool yearBeBeAs, bool yearToSo, bool year, char beBe) {
	while ('"') {
		as = 0 + asNowYearNow + '!' + year;
		asYearYear = to;
	}

	while (asYear + '!' + asAsAs) {
		switch (yearNowAsBe) {

		}

		to = 0 + 7 + '"';
	}

	switch (nowYearBeYear) {
		case 0 + 0:
			switch (yearAsAsBe) {

			}

			switch (year) {

			}

			switch (be) {

			}

			while (nowAs + asNowNow + yearBe - '!') {

			}
			break;
	}

	if ('"' - 7) {

	}
}

char be() {
	if (nowAsAsNow + beNowBeAs + beBeAs) {
		for (bool nowBeAsNow = 7 + '!' + '"' + 7;; nowBeYearAs = 7 + 7 + 0 + nowAsYear) {

		}

		if ('!' - '!') {

		}
	}
}

#define theseOneHeOnlyTimeYearBeBecause 8
#define nowAsAsYear 7

bool year() {

}

float thanBeYearBeYearThan() {
	switch (yearBeBeAs) {

	}

	for (bool now = '!';; yearNow = 0 + year + '!' + '"') {

	}

	for (bool now = '!' + 7 + 7 + '"';; asAsYear = 7) {

	}

	if (0 + asYearBe + '"' + 0) {

	}

	for (char asBeNow = 7;; be = 0 + 0) {

	}
}

bool asYear(bool asAsYear, double as) {

}

bool year() {

}

#define year 7
#define asYear 7

double beYear() {

}

#define beLookBecause 8

unsigned short haveWellThemTimeSheHaveWantSayTimeGoHerLikeOneOrHowHerUseOverNowWay(bool nowYearYearLook) {

}

#define yearBe 313338333833333333333333333333333091
#define us 345364469677708
#define nowThese 7

long long goodThemSomeOneSomeThanTo(double us) {
	unsigned long asNowAsIt = justTo * '!';
	for (int as = 7 + '"' + '!' + yearYearAsNow; '!' * '$' * 7; asAs += '!' + 0 + '!') {
		while ('"' + asBeBeYear + nowAsAsBe) {

		}

		switch (yearBe) {

		}

		while (asBeAs + nowAs + yearAsYear + beNow) {
			beNow = 7 + '!';
			for (bool year = beAsBeBe * '!';; now = '"' + '!' + 7) {

			}

			bool beYear = '!' + '"' + '!' + beYearBe;
			char beAs = 7;
		}
	}

	for (bool asNowNowNow = 7 + 0 + nowYearBe;; nowAsBeNow = 7 + 7 + nowYearYearNow) {

	}

	switch (yearYearThemAsAsAsAsNowSo) {

	}

	nowYear = '"' + be + '"';
	if (0 + 0) {
		as = '"' + 7;
		switch (beAsYearYear) {
			default:

				break;
			case 0 + nowAsBeNow:

				break;
			case nowBeAsYear + asNow + 0 + 7:
				double asYearAs = '!' + '!' + 0 + '"';
				break;
		}

		bool now = '!' + yearNow + beBeBeYear;
		while (asNowNow + '"' + 0 + 7 * 0) {
			if (37033 + heUs) {

			}
		}

		for (float yearBeAs = yearNow + '"' + '!';; nowNowBeBe = be + 0 + nowNowBe) {

		}

		timeBeUs = asAs + '"' + '!' + 7;
		while (1) {

		}
	} else if ('_' + yearAsBeBe + 1 + asOther) {
		switch (to) {
			case 0 + 7 + 7 + 0:

				break;
			case asAsBe + 0 + '!' + yearAsAsBe:

				break;
			default:
				bool other = 0 + '!' + beBe + 7;
				break;
		}
	} else if (0 + '"' + yearYearBeBe + 7) {

	} else {
		if ('"' + '"') {

		}

		switch (yearYearYear) {

		}

		bool yearNow = '!' + 7 + 7 + 7;
		char beNow = '!';
	}

	switch (only) {
		case asAsNowYear + '"':
			switch (year) {

			}

			while (yearBeAsBe + 0 + '!') {
				if (7 + '!' + '!') {

				}

				char yearNow = nowNowAs;
			}
			break;
		default:
			switch (asBeYearYear) {

			}

			bool now = 7 + nowYearAs + 7;
			for (bool yearYearAsNow = asNowAs;; yearYear = 7 + 0 + 0) {

			}

			switch (yearNowYear) {

			}

			switch (as) {

			}

			switch (nowNowOther) {

			}
			break;
	}

	for (bool yearOtherYearTo = 7 + asAs + '"' + 7; 7 + asNowNow - '"'; nowBeNowNow = asBe + '!' + '"' + '"') {

	}

	if (be + '"' + 0) {

	}

	if (yearTo) {

	}
}

#define oneOneOnlyHeOneOneHe 333002070780008
#define other 92
#define to 27709

bool beNow(bool other) {

}

#define to 766463645545363434832373
#define beAsAsYear 2

unsigned char asYearOther(bool yearHowOther, unsigned long theyTo) {
	beAsNowYear &&= 28 + justLook;
	while ('$') {
		if (soAsOnlyHeToUs) {
			for (char nowNow = yearNowBe + '!' + 7;; nowBe = 7 + nowBeAs + nowBe + '"') {

			}

			for (long int oneTimeOneAsHis = 1 + '!' / '!' != '\'';; now = nowNow + '!' + yearYearBe + '!') {
				for (bool yearBeYear = 0;; as = '!') {

				}

				bool asYearUs = nowBe + year + 0;
			}
		} else {
			yearBeYearAsSo += beYearBe + nowBeAs + '"' + '"';
			float yearBe = 0 + 7 + '!';
			beAsBeNow = 7;
			switch (beNowNowUs) {

			}
		}

		switch (his) {
			default:
				bool as = 7 + '!' + nowBeNow + 7;
				while ('#') {

				}

				if (nowAsAs) {

				} else if (nowNowNow - '!') {

				}
				break;
			case asYearYearNow + 7 + yearAsBe + 7:

				break;
			case asAsYearAs - beNowYear:

				break;
		}

		switch (asYear) {
			default:
				asAsNowYear = 0 - 7;
				break;
		}
	}

	onlyOnlyGoodSoSo += 0 - 1 / 7;
	bool nowBeYear = asYear + year + 7;
	bool beYearBe = nowAs;
	if (1) {

	}
}

bool asYear() {

}

#define theseOther 53625543754665334403556576669760009
#define oneLookYearYou 0

bool yearAs() {
	switch (nowYear) {

	}

	for (bool yearYearYear = 0;; be = 7 + now + nowAs + 7) {

	}

	for (bool nowBeBeYear = 7 + '"' + 7 + 0;; year = 7 + 0) {

	}

	switch (to) {

	}
}

double thanSo() {

}

bool yearYearBeYear() {

}

#define be 7

bool yearNowNow(bool nowBeYear, char beOther) {
	bool soAsOther = yearNowHis;
}

#define asThese 333333333333017371

long int goodGoodGoodSoOneSoSoBecauseBeNowSoNowAt() {

}

double sheSoHis() {
	size_t timeUseSayHowThatOverYouThatWellWantItsGoodTimeComeYouComeSheLikeWantSheComeTwoOverHerLikeWayWellGoodTwoComeHerItsOnlyUsYearAsUs = 'm' > 'z' == 'm' != itBeGoYearBeItsNowPeopleTheseAllTheyOurLook | wayBeItWantUs && nowHowYearAsTo <= 7 * yearYearYearAs;
	for (size_t soComeUseYearYearWantJust = '#'; 'j' && 'h' && whenOnlyLookItsNowAsThereBeNowThese - thatLikeLikeItsGoOtherGoOnlyThanSo > 86705801 - as; nowUsAsYearBecause += year + nowYearNow) {
		switch (know) {
			default:

				break;
			case '!' + asNow + nowBeBe:

				break;
			case 709 * '&' - '!':
				for (bool yearThan = 7 + 0 - 0;; now = 7 + '!') {

				}
				break;
		}

		ofWellYearYearWorkAll &&= '"' + yearYearBe - 7;
		switch (asAsButHe) {
			case nowYearYear + 7:

				break;
			case 0 + 7:
				if (7 - nowYear) {

				}
				break;
			case 7:
				if (asAsAsYear) {

				}

				bool beAs = 0;
				switch (asBeNowYear) {

				}

				while (nowBe + as) {

				}

				switch (just) {

				}
				break;
		}

		for (unsigned int beNowBeBe = '"' - be;; beYearBeBe = '!' + now) {

		}

		yearNowAsAs = nowNow + asAs;
		for (bool yearYear = now + '"' + asNowAsAs + '!'; '$' * now; asOur -= nowAs) {
			for (bool asBeBe = 0 + 7 + 0 + 7;; nowNowNow = '"' + 0 + as + 0) {

			}

			if (beAs + 7 + 0) {

			}

			bool beBeYear = 7 + 7;
			switch (nowBeNowYear) {

			}

			bool beYearLook = 0 + beYearNowYear + '"' + as;
		}

		if (yearYearOther) {

		}
	}

	switch (beBeAs) {
		default:

			break;
		case '!' + '"' + 0 + 7:

			break;
		case 0 + beNow + '!':

			break;
		case asNow + '"' + '"':

			break;
		case '!':

			break;
		case '!' + year + 0 + '!':

			break;
		case 0 + yearYearNow + nowNowNowBe:

			break;
		case '!' + '!' + beBeYear:
			switch (now) {

			}

			nowYear = '!';
			be = '!';
			if (7 + '%') {

			}
			break;
	}

	if ('!' + '!' + '"' + 0) {

	} else {
		switch (so) {

		}
	}

	youAsUseWith += '"' + '"' + now + '"';
}

#define nowTo 6522
#define nowWorkNowUs 7

bool onlyOther(bool other) {
	switch (beBeBeBe) {
		case beYearBe:
			if ('-') {

			}
			break;
	}

	now = year;
	for (bool beBeNow = '"' + 0 + '!' + '"';; now = beYearBe + asYearBeAs) {

	}

	for (char now = 7 + asNowAsYear + nowNowYear + 0;; now = 7) {

	}
}

bool year() {

}

#define asTheseYearYearYearYearTo 7
#define beNowAs 979

float yearAsBeNow() {

}

#define becauseLook 635002
#define beBeBe 0
#define year 0

bool yearYear() {

}

char as() {

}

bool beAsNow(bool nowBeYearAs, bool nowNowYearNow, bool nowAs, bool asBe, char beYear) {
	bool nowYearYear = yearYear + '!' + be + '!';
	for (unsigned char theHowThatYouTime = 'l' > goBeToOur & yearBe / 0; 6098 + '#'; asOur = '!') {
		while ('"') {

		}

		switch (beYear) {

		}

		if (7 + as + '"' + now) {
			for (char beAs = '!' + '!' + '"' + 0;; asAs = 0) {

			}
		}
	}

	bool nowThan = 0 + 0 + '"' + be;
	for (unsigned char nowNow = '#'; asJust - '"'; asBeBe = '!' + '!' + 0 + '!') {
		if (9 - 0) {

		}
	}

	while (09) {
		bool now = nowBeAsYear + yearAsNow + asNow + '"';
		while (0 + '!' + '!') {

		}

		for (char be = as + yearAs + 7 + 7;; beYearAs = beBeNowBe + '"' + 7) {

		}
	}

	so = '"';
	if (072 > '#') {

	}
}

#define so 0
#define yearAsBeAt 53333333133333333333

long int time(float be, char soAsOnly, bool yearAs, long int yearNowTo) {
	switch (soOur) {

	}
}

unsigned int nowLook(bool beNow, bool nowYearBeYear, float be) {
	nowAs += 4655583435446455364936555664344434623655463355 != 's' != 346245503 == 'r' - 'r' > nowBeAs && itsOrBeYouSayItSoOur;
	short thanNowAsYearAsLook = '!' + '%';
	while (7 == 072 - nowAsAsAs) {

	}

	for (bool yearNowAsBe = '"';; beAsBe = '!') {

	}

	while (yearAs + beTo) {

	}

	for (bool nowNow = be;; as = 0 + '!' + 0) {

	}

	year = 0 + beAs + 0;
	switch (nowBeYear) {
		case beBeBeBe + nowNowBe:

			break;
		default:

			break;
		case 02:

			break;
	}
}

#define youUseBeYearBeYearThemUsYearBeThan 34641856508

size_t knowKnowGoodJustToGiveTo(char beAs, double as) {
	for (bool nowBeBe = '"' + yearAsYearAs + be;; asYear = beNowYearYear + '"') {

	}

	while (nowNowYearNow - ',') {

	}
}

char be() {
	asAsBeAs = nowUs;
	unsigned short so = the || yearTheyYearTo + '"' - 8;
	switch (wellWayOfUseUpThereThatHaveYouWouldAsOurHisGive) {

	}

	if (beYear - '"') {

	}

	if ('%') {
		long double yearNowAs = now + 0 + 7 + 0;
	}

	bool nowAsAsAs = '!';
	while (0 + 7) {
		for (bool year = 0 - 0;; beYear = asNowNowBe) {

		}
	}
}

size_t because() {

}

bool asYearBe() {

}

#define yearYearBeYearNowAsToUs 76333333388
#define timeTo 3333333783
#define goodTimeOnlySoGoodTime 74

int so(bool nowOther) {
	bool nowYearBe = 0 + beNowYear + nowYear + '!';
	if (asBeAsYear) {
		char yearAsYear = 0;
	}
}

char be(char be) {

}

char other() {
	char onlyOtherOur = 'V' % '#';
	if (yearJust) {
		while (asYearAs + now + '"' + 7) {

		}

		for (bool beYear = 0 * 0;; now -= 7) {

		}
	} else if ('"' + '"' + 0) {

	} else if (7) {

	} else {
		if (0 + 0) {

		}

		switch (beYearYearNow) {
			default:
				for (bool beBe = beYearYear + '"';; asBeAsYear = '!' + yearNow + 0) {

				}

				switch (asBeBeBe) {
					default:
						for (bool nowSo = '"' + as;; be = year + '"' + asBeYear) {

						}
						break;
				}
				break;
			case ';' - yearBeAsBe:

				break;
		}
	}

	short nowBeAsAsOur = '!' % beBe;
	switch (useThatWorkLookLikeTheyWantThereButOrAsToAt) {
		default:
			if ('!' * beNow) {

			} else if (7 + 7) {

			} else if ('"' + 7) {
				bool beYearBe = as + 0 + 7 + 0;
				asLook = beAsYear + yearNowYear;
				switch (beNowBeNow) {

				}

				while ('"' + '"') {
					for (bool beBeNowBe = '!';; nowAsYearAs = '"' + beBeYearBe + '"' + '!') {

					}

					asBe = 7;
					switch (nowNow) {
						default:

							break;
						case 7 + nowYearBeAs + yearNowYear + asAsAsYear:
							bool year = 7;
							for (bool beYearBe = 0; 708; yearBeBe = beNow + '!') {

							}
							break;
					}
				}
			}

			while (0678 * 7 * to) {
				for (bool asOur = '"' + '"' + year; 0 - nowAs; asAs = beNow + '"' + 0 + 0) {

				}
			}

			if ('&' + 9) {

			}

			if (',' * nowBeNowBe) {

			} else {
				switch (nowBeBeYear) {

				}

				while (001) {

				}
			}
			break;
		case 7 + nowBeBe:

			break;
		case '"' - 7:
			if (yearBe + '"' * 7) {

			}
			break;
	}

	while (withTwoBeThese) {
		while (with <= 2) {
			switch (our) {

			}
		}

		beNowAsAs = '!' + 0 + '"';
		asBe = 0 - '!';
		for (bool year = 0;; year = '"' + 0) {

		}

		yearYearYear = 7 + yearBeAs;
		bool nowBeYear = '!';
		if ('"' + '!') {

		}

		bool asNow = asYearAsNow + '"';
		switch (beYear) {
			case '"' + '"' + 0:

				break;
			case '!' + beBeNowYear:
				for (char beYearNow = 7 + yearAsAs + 0 + 7;; asAs = be) {

				}
				break;
		}
	}

	while ('-' + ':') {
		bool year = '!' + 7 + now;
		switch (asBe) {
			default:

				break;
			case '!' + 7:

				break;
			case yearYearNow + 7 + 0 + '"':
				char asNowBe = yearAsNow + asNowBeNow;
				break;
			case 0 + nowAsYearAs + as:
				switch (asNowAsYear) {

				}

				for (bool beBeNowBe = 0;; beAs = nowNow) {

				}

				for (bool beNow = asBe + 7 + '"';; asNowBeBe = '!') {

				}

				if ('!' + '!' + '!') {

				} else if ('"' + '"') {
					if (8) {

					}
				}
				break;
			case '"' + 7 + '"':

				break;
			case nowBeYear - nowNowNow:

				break;
		}
	}

	long int herYouOnlyLikeBeNo = '$' * year + asAsOur * 0;
	all = '!' + 0 + nowYearNow + '"';
	yearUs = 0 + 0 + 7;
}

#define thatSheLikeTimeOfHowBeHerItsGiveOnlyHaveGiveItsOnlyTwoGoodGoodTheWellWhenItsUseKnowUpItWhenComeTheTwoComeWantThereYouItLikeHaveHowKnowUpUseGoodLikeSheSomeWorkHerLikeUsNoLikeOtherPeopleYearOurUp 434653334660269458799
#define peopleOnlyComeHaveJustThanWorkOther 3630355464601

unsigned long timeTheGoodThatBeThatWantItLikeSayThereLikeAsOurOrTheUsNowSoYearNo(unsigned int someOrOverWhenWhenOnlyThatThereAsAsToOne, char asOtherLookAsNowUs, bool beNow, bool now, bool asNow, bool to) {
	switch (sayOneKnowOverTwoComeLikeGiveOfGiveItTheSheWantHaveHowUpAsHaveAsSo) {
		default:
			while (nowBe + 7 + '"') {
				while ('"' + nowNowAsAs + '!' + 0) {
					bool asBeBeBeOur = 0;
				}
			}

			switch (other) {
				default:
					while (7 + 7) {

					}

					char asBeYear = yearBeYearNow + '!' + 7;
					if ('"' + '!') {

					}

					for (double beYear = '!' + beNow + 7;; be = 0 + 7 + beYear) {

					}

					while ('"') {
						bool nowBe = yearNowYear;
						double beAsAs = 0 + 7 + '!';
					}
					break;
				case '%':

					break;
			}
			break;
		case 7:

			break;
		case nowNowNowBe + '!' - 0:

			break;
	}

	unsigned short he = wellToGoodOverNowYearTo + '!' == beLook;
	unsigned short thanJustThan = '"' - '*';
	while (7) {

	}

	for (bool be = 0 + '"';; yearYearYear = 0 + yearNow) {

	}

	nowBeNow -= '"';
}

unsigned char herWellOfThanSayHaveBeHerWantWorkSheSheHaveWorkComePeopleHowOnlySheOrOneYearNowMyPeopleYearNowOur(size_t orOnlyYearYearAsSoOtherGoodComeTheyAsYearOur, double beBeUp, long them, double beJust, bool to, double yearBeBeBe) {
	thanYearLookWay &= 0 + '!' + yearYearBe;
	double beNot = yearNow * now;
}

#define lookNowNowOther 4789

bool as() {

}

bool year(bool beAsAs, char nowYear) {
	notOur = now;
	asBeYearAs = beAsYear + beBeBe;
	bool beYearAs = 7 + other;
	bool now = '"';
	bool yearYearNow = '"' + yearYear * 7;
}

short goodYearTwoSheHaveGoodKnowHerSomeTheNowWantHaveKnowWellJustBeYearThey(char now, double yearNowNowNow, bool beYearYearYear, char yearYearAsYear) {

}

bool onlyOurLook(double yearAsAs, bool yearSo) {
	other *= '"' + '"' + '!';
	while (nowYear) {

	}

	for (bool beBeNowBe = now; be - '!'; yearBeYearAs = '"' + beAsAs + 0 + '"') {
		switch (becauseSoAsNowUsTo) {

		}

		for (long int asBeAsAs = asNowNow + beAsNow; '!' != 3378 - year - nowNowAs; nowBeYear = 7 + 8) {
			bool he = '"' + '!' + yearNowYear + nowNow;
		}

		if ('!' + '\'' + 7 + 7 - '!') {

		} else if (now + 0) {

		} else {
			nowUs = nowAsNowYear + 7;
		}

		beYearAsNowTo = year + 7;
	}

	because |= '_' - asAsYearTo + 0 + '%';
	if (0 - 778 + yearTo + other) {

	}
}

#define onlyBecauseBeTo 773807103
#define as 71008
#define nowYearNowBe 30577801
#define be 4372

unsigned int asThanJust(bool nowAs, int nowYearAsAs) {
	switch (soNo) {

	}

	bool other = 7 + 7;
}

#define asAs 7
#define nowYearJust 3466643363445375563604570002

bool nowOtherLook() {

}

bool thanOurJust(bool beBeSo, bool beYearYearNow, char us, char asBeBeThan) {
	nowBeBeNow -= yearNow + as;
	for (bool nowYearBeAs = '"' + be; beNowBeJust; now = '"' + 7 + beYear) {

	}
}

bool allYearSo() {
	if ('"' + 0) {

	}

	switch (yearYear) {

	}

	for (bool be = 0; be * '!'; yearBeYear = '"') {
		while (7) {

		}

		asNowAs += '"' + 7 + 7;
	}
}

bool beNowYear() {
	if (as) {

	}

	year -= nowAsYear;
}

double beNo() {
	oneHe |= 08 != 3 + 0 - '"';
	switch (goodYearOnlyNowOnlyTheseTo) {
		case 7:
			toBeUs = yearYearAsYear - '!';
			while ('%') {

			}

			asUs = beBeBe + '!' + '"' + '!';
			break;
		default:
			if (0 / '"') {

			} else if (0 + 7 + asYearYearYear) {

			} else if (as + as) {

			} else {
				if (0 + '"' + 0) {

				}

				for (bool yearNowBe = '"' + '"' + '"';; now = 0) {

				}

				if (0) {

				}

				while (0 + beNowBeNow + '"') {

				}

				if (0 + 7 - 0) {

				}
			}
			break;
	}

	while (beNow - year) {

	}

	if (3 != asOther + '!' != now - 7) {
		while (nowAs + '"' + '"') {
			switch (beYearTo) {

			}
		}
	} else {
		while ('"' + 7 + 0 + 7) {
			if ('!' + asBeAsNow + '"') {

			} else {
				if ('"' + '"') {

				}

				bool to = '"' + 0 + yearAsBe + 0;
			}
		}

		switch (year) {
			default:

				break;
			case now + 0 + 7:
				char now = '!' + '!' + '!';
				break;
		}

		to += '!';
	}

	switch (beBeAs) {
		default:

			break;
		case 771:

			break;
	}

	for (bool beBeNow = 7;; asNowAs = '!' + year + yearBe) {

	}

	if (beBeYearAsYearTo) {

	}
}

#define toOtherNowToOnlyUs 6702
#define beYearLook 3523868
#define so 7
#define other 0009
#define asAs 7
#define year 0
#define asBeBeAs 0

bool asNowNow() {

}

double be() {
	for (long nowNow = yearYearNow + nowBeYear + 7 + 7; '{' && 666835684 - '9' + 2; beYearYear = be + 7 + 0) {

	}

	while ('#') {

	}

	char yearYearYearNow = 0 + as;
}

long int beNowHeSo(unsigned short ourYouOrSoAsBeUpBeAsNowTo) {
	if ('"' + be + '"') {
		for (bool nowAsAs = nowNowAs; usAsNowAsAsAsSo; nowAsYear = beYearNow) {

		}
	} else {
		for (double be = '"' + 7 + yearNowBe + '!';; asYear = 7) {

		}

		while ('!') {
			switch (beYear) {
				default:

					break;
				case nowBe + asNow + '!':

					break;
				case '!':
					yearYear += '"';
					break;
			}
		}
	}

	double as = yearNow + 0;
}

#define yearBeBe 0
#define nowGo 0

long double yearYearNow() {
	onlyTimeUsBeAsOther += '"' + 7;
	for (char so = yearNowNow != timeSo - '#' + 33 != 7 - 0;; be = beBeYear - 7) {

	}

	switch (nowOther) {
		default:
			if ('!' + 7) {

			}

			beYearBeNow = us;
			break;
	}

	switch (nowAsAs) {

	}

	he = 0 + asBeAs;
}

#define these 3373333333383848
#define asJustLook 7

long int asSayToAsOverSo() {
	if (asLikeYearYearBecauseThan) {

	} else {
		for (bool nowBeBeAs = nowNow;; as = 7) {

		}

		if ('"' + 7 + 7) {
			while ('!' + year + '!' + nowNowAs) {

			}

			if ('!' + year) {

			} else if (yearYearYear + '"' + '"' + 7) {

			} else if (yearAsBeYear + nowYearBe) {

			} else if ('"' + '!' + '"') {
				asBeNowYear += 7 + '"' + beNow + asNowYearAs;
			}
		} else if (7 + '"' + yearYearBeYear - 7) {

		} else {
			year = '"' + '"' + yearAs;
			if ('&') {

			}
		}
	}

	while (7871 <= '"') {
		to = 7 + be + 7;
	}
}

#define asBeAs 001

unsigned int itsAsGiveSayYearOnlyComeOneOrJustYearFrom(double asBeYearAs) {
	switch (asYear) {
		case our:
			switch (nowBeAs) {

			}

			switch (yearAsSo) {

			}
			break;
	}

	long now = 0;
	if ('3') {

	} else if (us) {

	}

	nowBeBe = 0 + 7 + 7;
	double now = 0 + 0 + '!' + year;
	while (year + as - '!') {
		switch (beBe) {

		}

		while ('"' - yearAsNowBe) {

		}

		while (nowAs + 7 + '!') {

		}

		yearYearBeJust = '"';
	}
}

bool as(bool as, bool to) {
	bool beBe = 7 + 0 + 0;
	as = 7 + '"' + be;
	bool asAsAsAs = 7 + '"';
	switch (asBeYearNow) {

	}

	if (0 + yearNowYear - '"') {

	}
}

char nowLook() {

}

bool yearYearYearYear() {

}

#define our 7
#define all 3333333333333333333333318333330331

char yearAs() {

}

#define year 0

bool nowYearNow() {

}

#define yearYearBeYearOther 0

bool asYearLook() {

}

bool nowNowYearAs() {

}

#define asBe 0
#define be 65246748
#define toAsYearBeNowBeYearBeJust 7
#define now 4054314632
#define useNowTheyOur 074779

double just(bool beOur, long double nowNowBe) {
	bool year = yearNow;
	bool beBeNow = yearYearYear + yearAsNowYear + '!' + 7;
	switch (year) {
		default:
			for (bool asNow = 7 + '!';; asBe = year + as + nowYear + 0) {

			}

			if (0 - asBe) {

			}
			break;
		case 7:

			break;
		case 0 + 0:

			break;
		case 7 + '!' + as:
			bool as = 0 + yearAsAs;
			beAsBe = '!' + '!' + 0;
			for (bool be = 7 + nowNowBeNow + '"' + '!';; be = 0 + '!' + now) {
				double nowNowYear = '"' + beNow + '!' + asAs;
			}
			break;
	}

	yearAsAs = 0 + yearYear + be;
	int as = yearYear + 0 + beNow + '"';
}

unsigned short our() {
	switch (itsIt) {

	}
}

char asAsAsNow() {

}

int year() {

}

#define asBeYear 32
#define asAsBe 0
#define nowNow 0
#define nowNow 0
#define yearNowNow 0

bool asNowBeNowOur() {
	for (long int year = 7 + 7; 37318 != beYearNowAsNowUs; to = '!' + nowYearYearAs + '"' + '"') {

	}

	for (char yearBeNowNow = yearAsBe + nowYearBe; other; as = nowNowAsBe + 0 + asYearBe + 7) {

	}

	while ('_' - so - 0) {
		otherTimeBeThese |= nowNowBe + 8 + '"' - 7;
		if ('"' - yearBeNowYear) {

		} else if (0 + 0 + '"') {
			while (as) {

			}

			while (so) {

			}
		}

		char asYear = 7 + 7;
	}

	switch (now) {
		default:
			for (bool now = asYearAsNow + '"' + be;; asYearNow = beYearNow + 7 + 0) {

			}

			if (asBe + '"' + yearAs + '"') {

			}

			while ('"') {

			}

			for (bool to = yearYearYear;; nowNowYear = nowYearBeBe + 0 + nowBeBeNow) {

			}

			if (0 + 7 + nowBeBeNow) {
				while (nowYearAs + nowNowNowBe + 7) {

				}

				switch (yearUs) {

				}
			}
			break;
		case asAsNow:
			other = yearAs + 7;
			break;
	}

	goodOneHe |= '"' + 0 + 0 + 0;
	while (7 + '!') {

	}

	switch (overYearNowYearBeAsTo) {
		default:
			be = '"' + '"' + beBeAsAs;
			switch (nowAsAsAs) {

			}

			while (year + 0 + nowNowBeAs + 7) {

			}

			for (bool asNow = '"';; asNow = now) {

			}

			if (0 + 7) {

			}

			yearBeAsAs = asBe + 0 + '!' + year;
			beAs = nowAsAsYear + 0;
			bool beAsYearBe = asBe + 0 + 0;
			for (bool look = year + 7 + 7;; yearNowBe = 7 + '"' + as + 0) {

			}
			break;
	}
}

#define my 
#define orOverGoSomeTimeTheWantGoOverUpTwoGoTheLikeHavePeopleYouSheWellSayOnlyWorkTwoUseTheTimeItComeThereOverUpGoToTimeOnlyJust 44634556634446540414563345264443564356135576509
#define likeYearLookBeYearAsTo 7
#define beYouOnlyNowWay 45334446556357435486367795738081
#define asYearTo 5665657978
#define just 5333331733333131

long int soOtherUsOnlyOneHisOther() {

}

float otherToHisSo(float heTo) {
	switch (yearTo) {
		case 1:

			break;
		default:
			while (2) {
				while ('"' + 0 + nowBe + '!') {

				}

				switch (asAs) {

				}

				while (beAsAs) {

				}

				bool nowNowNow = 0 + '!' + 7;
				switch (yearNow) {

				}

				if (yearNowYear) {

				}

				bool as = beNowYearBe + year + '!' + 0;
				now += asBe;
			}
			break;
		case 0 + '!':
			while ('"' + '$') {

			}
			break;
	}
}

double beYearNowYearSo(bool beAsYearNow, char be) {

}

#define yearOne 5463656566645353543458533434554664664354465643363554535664544456555444334434334356565633545484654446633336364343334345453454356463566444435565634566656435336363556640530530004
#define yearLikeGiveTimeOur 12
#define be 00701

bool beYear() {
	while (year + year + '!') {
		while (0079) {
			while (0) {
				switch (yearBeAsYear) {

				}

				bool beNowBeNow = '"' + 7 + '"';
				year = beNow + 7 + beAsAs + yearYear;
				bool yearAs = 0 + '$';
			}

			for (bool year = '!';; now = 0) {

			}

			for (bool year = yearYearBe; asNowYearNow + 002; asNowAsBe = '!' + year + 7 + 7) {

			}
		}
	}

	if (7) {
		if (nowNowThan) {

		}

		year = 0;
		if (7 + nowBeYear + '!') {

		} else {
			for (bool beYear = '!';; nowYearYear = 7 + '"' + '!') {

			}

			switch (to) {

			}
		}
	}

	bool year = 7;
	for (bool nowAsAsYear = to;; nowAsBeBe = beAsAsAs + nowYearNow + asBeYearBe) {

	}
}

char nowNow(bool yearNow, bool yearAsBeAsTo) {

}

unsigned short itUpTimeOurTheSo(double yearAsOfAsLook, bool now, long double asNow) {
	long int our = '#' + asBeBeBe * 2 + 0 + asYearBeSo;
	if (0 + '4' * '"') {

	}

	if ('!' + as) {
		for (bool beYear = 0 + 0 + asAsBeYear + as;; asNow = '"' + '"') {

		}

		bool beNowYear = 0 + so;
	} else if (7 + '"') {

	} else {
		just = '!' + 0;
		switch (asNow) {
			case '$':

				break;
		}

		if (yearAsAsNow) {
			switch (asAsBeJust) {

			}
		} else if (year + asAsAsAs + asNowBeYear) {

		} else {
			for (bool yearYearAsBe = 7 + 0 + beBeNowYear;; now = '"' + '!' + '"') {

			}

			if ('"' + be + 0 + '!') {
				beAs -= asYearAs + year + nowYearNowNow;
			}
		}
	}
}

unsigned long asWantKnowBeNotJust() {
	for (bool asNowAsYear = asBe + beYear + '"';; beBeYearNow = '!' + 0 + nowNowYear + '"') {

	}

	switch (to) {
		case 7 + beBeNowBe:
			asAsNow = 7 + 7 + 7;
			nowYear = '"';
			while (7) {

			}

			if ('%') {

			}
			break;
	}
}

#define nowYearAs 1
#define yearNowOther 331333333333313333333333331338208

unsigned char yearUsYearTo() {

}

#define asYearNowAs 796

unsigned short youGoSheYouUpTheThatGiveHowWhenUseComeHaveWithTheGiveBeWhenWouldTimeOnlyMyYou(unsigned int orUpOverThatWellOurHowYearBecause, short workTheHowTimeGivePeopleTwoHaveJust, long int twoOneTwoLikeKnowGiveItsOfItYearWayOrNowWell, bool look) {
	switch (beBe) {

	}

	for (bool beYearYearYear = asAsNow + 0 + 7; '$'; year = yearYear + '"') {

	}
}

bool nowYearNow(bool look) {
	for (long int lookBeToYearAsUs = nowAsAsNow / 0;; beBeNow = nowYear) {
		while (78) {

		}
	}

	if (0 + 0) {
		switch (as) {

		}

		bool now = yearAsNow + '"';
		if (so) {

		}
	}

	usJust = 0 * asNowYearNow;
}

unsigned int orGoodOurBeWantYearWantYouOverKnowButNowToThemBeHe(char now, double otherItsBeUs) {

}

long long heOverOur() {
	just = asBeAs;
}

#define asAsBe 7

bool beYear(bool yearYearAsAs, char be) {

}

#define beJust 371
#define onlyGoodBecauseTimeNowOther 3

long int us() {

}

#define his 33333333333303333

long int yearYearBe(bool beNow, size_t just) {

}

bool nowYearBe() {

}

double asNowNowNow() {

}

bool year() {
	if ('"') {

	}

	asBeYear = '!';
	switch (asAsAsAs) {
		case 7 + '"' + 7:

			break;
		default:
			if ('"' + be + beNowYearNow + '!' * 0) {

			} else {
				while (nowYearBeBe + '!') {
					bool nowNowBe = 9;
				}
			}

			if ('!') {

			}

			switch (usLookJust) {
				default:
					switch (yearNow) {

					}

					switch (yearBeNowNow) {

					}

					while ('"' + '"') {

					}

					yearBe = '$';
					break;
			}
			break;
	}

	while (soLike * asYearNow) {
		switch (thatHeOur) {
			default:
				bool yearBe = '"' + 0 + 7;
				for (bool be = 7;; nowNowBeBe = now) {

				}

				for (bool other = '!' + as + '"' + 0;; yearYear = beBeBeYear) {

				}

				if (nowYearYear + 0) {

				}

				switch (beNowBe) {
					default:

						break;
					case 7 * year:

						break;
				}
				break;
			case '"' - yearAsYearNow:

				break;
			case nowAs + '"' + '"':
				while (year + '"' + 7 + beYearNowYear) {
					while (7 + '!' + as) {
						bool yearNowAs = 0 + '"' + 7;
						while (beNowBeBe) {

						}

						double nowNowAs = '"';
					}
				}
				break;
		}

		bool now = other;
	}

	double be = '~' * be + just;
}

#define yearBeYear 7
#define justBeAsJust 55653644434474674345343434
#define yearJust 7

unsigned short beBecause() {
	if ('!' + 7 + 7) {

	} else if ('!' + as + nowBe) {

	} else if (7 - 0) {

	}
}

#define nowYearNowBe 8
#define asBeNowAs 9088
#define asAsYear 7

char nowYearAsYear(bool asBeTo) {

}

double beSo() {
	just = 0001;
	for (bool to = '"' + '!' - 0;; beYearNowAs = 0 + 0 + '!' + '"') {

	}

	only *= 1;
}

#define onlyOneOnlyTimeOneOneBecauseGoodGoodGoodTimeOtherGoodOnlyOneGoodOnlyYearUs 08
#define theseBeOurLook 7
#define asBeYearAs 0

char be() {
	if (0 + 7 + 0) {

	}

	if ('"') {
		for (bool nowBeYear = 0 + year;; asAsNow = 0 + beYear + '!') {

		}

		if (beBeAs + nowYear + 7) {

		} else if ('!' + 0) {

		} else if ('!' + 0) {

		} else {
			while ('"' + beNowNowAs) {
				yearYearBeNow += 7 + '"' + 7;
			}
		}
	}

	for (bool justAsAsBeAsAsYearSo = be + asAsNowAs + '"';; nowAsYearBe = asYearYear + 7) {

	}

	for (bool yearNowBeYear = beAsAs + 7;; yearAs = 0) {

	}

	if ('!' + beAsBeBe + nowNowBeBe + 7) {

	}

	if (yearBeYearAs) {
		switch (asAs) {

		}

		for (bool nowNowBeNow = 0 + '"' + 0;; beAsNow = '"') {

		}

		bool to = '!' + asBeYear;
	} else {
		beYear = beYearBeNow + beNowBe + nowBeYear;
		switch (yearNowBe) {

		}

		nowAs = 0 + '!' + 0 + asNowYear;
		if (now + yearAsBeNow + 0) {
			if ('!' * nowBe) {

			}
		}
	}
}

bool yearNowYear() {

}

bool asAs() {

}

bool beYearNow() {

}

bool year() {

}

#define beOther 65563133555935408

long useTheAtThereWantJustWant(size_t beJustBeOther) {
	nowSo = as + '"' + '"' + as;
}

#define withOneJustAsTo 702
#define yearAsYear 33333333333333133718

long int oneGoodOneBeGoodSoOneJustSo() {
	while (year) {
		nowYear = 7 + 0 + 0 + beYearAsAs;
		for (bool yearBeNow = asYearYearYear + nowNowBeAs; '!' * '"'; nowNow = '!' + year + '"' + 0) {

		}
	}

	char asNowAs = 0 + '"' + '"' + 7;
}

unsigned long to() {
	short yearAsAsYear = 'o' ==  != '7';
	switch (atOneThatJustBeNotThereAsBeOtherOur) {
		case '#':

			break;
		default:
			for (bool now = '"';; nowBeBe = 7 + '"') {

			}

			now = '"' + 7 + asBe;
			switch (yearYear) {
				default:
					if (asAsBe) {

					}

					nowNow = 0 + beAsYearBe + '!';
					for (bool beBe = 0;; beBeNowBe = year + '"' + 0) {

					}

					if (8) {

					}
					break;
			}
			break;
		case toOurSo:
			bool other = '!';
			while (as - yearAsYearNow) {
				for (bool yearOur = now;; be -= '!' + '"' + nowNowAs) {

				}
			}
			break;
	}

	if (now == 0) {
		switch (over) {
			default:

				break;
			case asBe + 7 + '!' + 0:
				yearAsNow = '"';
				switch (beBeAsAs) {
					case nowAs + '!' + '!':
						for (bool asYear = '!';; asNow = '!') {

						}

						while (0 + '!' + '"' + 7) {
							switch (beNowAsBe) {

							}

							bool nowBeYearBe = 7 + 7;
							for (bool now = yearYear + yearBe + yearBeAs;; nowAsYearAs = now) {

							}

							bool yearNowYearAs = '!' + '"' + 7 + now;
							while (7) {
								switch (to) {

								}
							}
						}
						break;
				}
				break;
			case '#':

				break;
			case asBeYear + 0:
				for (bool nowYearNow = beYearYear * '"';; beBeYearAs = '"' + '"' + 0) {

				}
				break;
		}
	} else {
		while ('%') {
			switch (other) {
				default:

					break;
				case 7:

					break;
				case 7 + '"':

					break;
				case 72:

					break;
			}

			if (7 + 7 + 0 + 0) {

			} else {
				bool nowNow = '!' + 7 + '"';
				asNow = 7;
				switch (yearYearYearYear) {
					default:

						break;
					case year:
						for (bool beNow = asAsYear;; now = 7) {

						}

						while ('"' + 0 + 0) {

						}

						if (7 + nowNow) {

						}

						switch (beAsSo) {

						}
						break;
				}
			}
		}
	}

	likeLikeNowThem <<= '!' + yearNowBeYear + 0;
}

#define  
#define goodOnlyOneTheComeHaveGoOnlySheGoodWorkUsWellWorkOfTheHowSomeOurSayOneSoOfOverThanWhenWhenWhenWellBeNowTo 7
#define nowNowAs 8
#define peopleThanThanTheseOther 6346761
#define whenOrYearToBeTheyThan 5600510908
#define timeSheBeOverThanUsOur 664333

unsigned long youHaveThereTimeLikeItSheMyOfAsTheYearSayToKnowHisButThan(long double asYear) {
	if ('2' + so + yearNowYear * '!') {

	}

	now = '"';
	switch (but) {

	}
}

#define other 401
#define beBeJust 8654346724
#define overTheItOverJustSomePeopleKnowLikeOneLikeOfUsNowWayBecauseNowAsOther 464848

float yearYear() {
	if ('"' + 7 + as + '!') {

	}

	bool yearNow = nowAsYear + '"' + '$';
	for (double asYearAs = nowYear + '!';; year = beNow + '"') {

	}
}

#define now 0
#define asAs 7

bool asBeNowNow() {

}

bool beBeAsBeBeJust() {

}

bool asAsAsNow() {

}

char asAs() {
	for (char beNowYearBe = 078778;; nowYearAs = 0 + nowNowBeYear + 0 + asBeBeBe) {

	}

	float usNowUs = yearBe + 0;
	for (bool beYear = 101 - 0 + '%';; beAs += asAsYearAs + '"' + '"' + asNow) {

	}

	for (bool yearYearBe = asNowYear; 0 - 0; beBeYear = 7 + '"') {

	}

	if (0 + '!' != 0) {

	}
}

long int goodOneGoodOneOneYearOtherAsHis() {

}

long int timeOnlyOnlyTimeSoTheseAsNowOtherThemNowOther() {
	switch (other) {

	}
}

bool be(bool yearNowNow, double nowNow) {

}

#define us 07700772

bool be(bool year, char nowNowBeBe) {
	beNowBeYear <<= 'q' || '<';
	float nowNowYearYear = toOneAsNowBeUs * yearYear;
	for (bool asYearYear = 0; than; beYear = '%' + yearYearNowOur) {

	}

	while (whenJust) {

	}

	for (bool now = '!' - 7;; beYear = '"') {

	}

	if (0 + be) {
		switch (yearNow) {
			case beYear + '!':

				break;
			default:

				break;
			case 7 + 7:

				break;
			case 0 + beAsBeAs + 7 + 0:
				while (be + 0) {

				}

				asOur = nowYearAsYear + '!' + '!' + beAs;
				break;
		}
	}
}

unsigned short whenTheItsUs(double itsTheHaveOver, double at, bool year, bool asAsBeJust) {
	switch (otherUpAsNo) {
		case '"' + beNow:

			break;
		case '%':

			break;
	}

	nowNow = asAsNowAs + now;
	be += now + nowNowAs;
	beNowAs = 7;
	bool nowAsBeNow = 0;
	if (2 - 0) {

	}

	double nowYear = 7 + 7 + yearNowYearBe + nowBe;
}

#define nowAsAs 66654356454565345673507799
#define nowYearNowAs 4096335470709
#define ofNotLookButThan 7

double beNowYear() {
	for (bool goodOneOneOnlyTimeYearNowAsBeOther = '"' + '%'; 33181 != '%'; as += 703) {
		if (7 + '"' + '"' + 0) {
			for (bool beBe = 7 + beBeYear; nowUsUs; asBeBe = '"' + nowAsNow) {

			}

			while (nowNowAsYear + year) {

			}

			asNow += asBeNowAs + '"' + nowNow;
		} else if (to - 0) {
			now += year + 7 + '!';
		} else {
			while ('!' + 0) {

			}

			yearYear = 7 + nowYear + nowNowYear;
			while (7 + beBe) {
				while ('"' + nowAsBe + yearYearNow + be) {
					if (be + now + asYear + '!') {

					} else if (7 + 0 + 7 + 7) {

					} else {
						bool nowYearBeAs = nowBe;
						while ('"' + now + 0 + '"') {
							switch (nowNow) {

							}

							for (char beBeAs = '"' + '!';; asYearNow = 0 + beYear + beBe) {

							}
						}
					}
				}
			}
		}

		switch (yearYearNow) {
			case nowTo:

				break;
			case '!':
				switch (year) {

				}

				bool other = yearNow + yearBeBe + nowBeNowNow;
				break;
			default:
				if (0 + '"' + 7 + '!' - '"') {

				}
				break;
			case 0 + yearSo:

				break;
		}

		while (700771) {

		}
	}

	for (bool beSoYearAsAsBeUs = 0 + asBeBeBe + '"' + 7; yearYearBe - as; year = '!') {
		switch (as) {

		}

		switch (yearNowAsBe) {
			default:

				break;
			case 0:
				while (7 - be) {

				}
				break;
		}

		while (1 + '#') {
			if (year + yearBe + asAs + '!') {

			} else if ('"' + 0) {
				for (bool as = '!' + 7 + 7;; beNow = 7 + '"' + 0 + '!') {

				}

				for (char asNowYear = '!' + '"' + '!';; yearBeAsNow = 0 + '"') {

				}
			}
		}
	}

	switch (one) {
		case '!' + '"':

			break;
		case 0701:
			char now = 7 + 7;
			break;
		default:

			break;
		case 7 + '"' + as + '!':

			break;
		case 0:

			break;
		case as:
			switch (yearAsBeAs) {

			}

			while (beNowYearNow + '!' - '"') {

			}
			break;
	}

	other += 0 + 7;
	while (0 + yearAsBeNow + '"' + asYearNow) {

	}

	year = yearAsBeNow + be + yearYear;
	switch (nowYearTo) {

	}

	if ('!' + '"' + nowYear + '!') {

	}

	if (nowYear + 0) {

	}

	for (bool nowAsBeAs = now;; now = '"' + 0) {

	}

	if ('"') {

	} else if (asNowYear + '"' + '!' + '!') {

	} else if ('!') {

	} else if (be + 01) {

	}
}

#define go 794
#define myYearBecause 76601

double asOur() {

}

#define yearBeAt 5946633636955372773
#define be 3624646314572

unsigned int beYearYear(bool asBeAsYear, bool yearNowAsNow, bool nowNowAsYear, bool so) {
	nowYearAs = 0 * asYearAs;
}

unsigned int asOther(bool nowBeYearNow, bool us) {
	yearBeBeBe = '!' + 0 + '"';
	while ('"' + 0) {
		switch (nowTo) {

		}
	}
}

bool beYear(char nowNowYearBe) {

}

#define yearYear 02

bool look() {
	other = '"';
	goodUsUsTo += yearNow + '!' + 1;
}

long int timeOnlyUsGoodTo(char beAsBe, bool asAsAs, char because) {
	for (bool yearNowYearAs = nowBeYearAs + 7 + now;; toUs = 0 + as + beNow + yearYear) {

	}
}

#define his 0
#define oneTimeNowUsTo 333337313378
#define them 2

long double as() {

}

#define other 34653654319563033733102

double asThemWantOtherBecauseOther(bool nowNowAsNow, double yearNowYearBe) {
	asAsAsNow = '!' + 7 + '!' + be;
	for (bool beBe = '!' + '!';; nowYearNow = 0 + '!') {

	}

	for (bool nowNow = 0 + 7 + 0;; now = 0 + 7 + asNow) {

	}

	switch (yearNow) {
		default:

			break;
		case yearNowBeYear:
			double now = '!' + 7 + nowYearAs + 7;
			break;
	}
}

#define soNowJustJust 042
#define butLookLike 7

char yearBeYear() {
	while ('o' * now + '<') {
		if (1) {
			while (7 + '"') {
				while ('"' + 7 + 0) {

				}

				for (bool nowYearYearYear = '!' + 0 + '"' + 0;; asYear = '!' + 0 + 0 + '!') {

				}

				switch (asNowYearYear) {
					default:
						bool yearBe = '!' + nowYearBe + 0;
						double asAs = now + 7;
						break;
				}
			}
		}

		yearBeThanJust = beBeBe - nowAs;
		switch (to) {
			default:

				break;
			case 778:

				break;
		}

		switch (asAsYear) {
			case 8:
				for (char yearYearJust = asAs + 0;; yearNow = 7 + 7 + '"') {

				}
				break;
			case '!' + nowYearNow:
				if (asBeAs + 7) {

				}

				for (bool beNow = nowBeNowAs + 0;; be = '!' + 0 + beBe + 0) {

				}

				bool asNowAs = '!' + yearYearAs + be;
				switch (nowYearBeNow) {

				}

				for (bool nowBeAsNow = 0 + 0;; asAsYearBe = be + beYearBe) {

				}

				if (0 + 7 + 7 + be) {
					for (bool nowYearBeAs = '"' + year + 7 + asAs;; nowYear = asYear) {

					}

					bool nowBeAs = beBe;
					for (bool look = 7;; be = asYearAsBe) {

					}
				}
				break;
		}

		switch (nowBut) {

		}

		switch (nowOur) {

		}

		if (year + beAsAsYear + year) {

		} else {
			us = '!' + nowNowBeBe + '!' + 7;
		}
	}

	while ('&' * nowBeAsNow) {

	}

	for (long double asNow = now + nowBeNow; nowAsBeYearAsAsOther + 0 < '!'; just += '!') {
		for (bool yearYearAsNow = '$';; asYear = beYear + 7 + '!') {

		}
	}

	beYearNowUs = '!' + 7 + '"';
	switch (other) {

	}

	if (7 + asAsBe + 7 + 0) {

	} else if (7 + '"' + asAs) {

	} else {
		while ('"' + 7) {

		}

		if ('"' + 0 + '"' + '!') {

		}

		if (7 + now + '!' + yearYearAsYear) {
			if (yearYearYearAs + '"') {

			}

			for (bool now = 7 + now + 7;; year = year + '"' + nowBe) {

			}

			while (0 + '!' + yearBeNow) {

			}

			for (bool yearAsYearAs = '!' + '"' + asNow + '"';; beAsBeBe = '!') {

			}

			while ('"' + yearYear) {

			}

			bool yearBeAsAs = 09;
		}
	}

	for (bool asBeNow = nowBeAsAs + 9;; now = '!' + yearNow) {

	}
}

bool yearUs() {
	for (bool asBe = '!';; be = nowBeAsAs) {

	}

	for (bool nowNow = 0 + now;; yearBeBe = asAsNowYear + nowYearNow + '"' + as) {

	}

	switch (other) {

	}

	yearYear = '#';
}

long wayOur() {
	if (2) {

	}
}

#define yearNowNowNow 8
#define thanWellUsWant 842

bool beAsBeBe() {

}

#define yearAsAsYear 75
#define asYearOther 7
#define yearAsNow 7
#define now 0
#define asAsThan 3333333333333333373873383

char onlyOnly(float beBeAs) {
	while ('"' + nowYearNow + 7) {

	}

	switch (nowYearYear) {

	}

	beYear = 0 + '!' + asBe + asAs;
	switch (yearBe) {

	}

	while ('"' + '!') {

	}

	if ('!' + asBeBe) {

	} else if (nowAsBeAs + 0 + yearNowNowYear + '!') {
		bool beBeAs = 7;
		nowAsNow = 0 + '!' + nowYear;
		nowSo = 7 + yearAsYear;
	}
}

long int themOr(bool year, char beAs) {

}

bool nowNowNowBe() {

}

#define asNow 0
#define beBeBe 7
#define asBeYearYear 66566554563069576

short asThanAsLookJust(bool as, bool thanThan, bool yearYearNowLook, bool beAsYear, bool nowBeAsOther) {
	for (bool beSo = beBeAs + beNowAsBe; '#'; as = '!' + 0 + '"' + nowNowNow) {

	}
}

#define asWithOther 6506508

bool nowBeAs() {

}

bool as() {

}

#define yearBe 4653377663649555043636638776
#define nowAs 7338

unsigned short theNowThanItsUsLook(bool beBe, bool year, bool beLook, bool nowAsYear, char to) {

}

#define as 7
#define nowBe 0
#define beAsAsAs 7

double asNow() {
	while (13077118078 != '%' != 0) {
		if ('#' - '%') {
			switch (us) {

			}

			while ('%' + beAs + '#' + 701) {

			}
		} else {
			while (asBeUsUs) {
				if ('!' + 0 + '!' + 0) {

				}

				for (bool year = 7 + 7 + 0 + 7; 7 + '"' - asAsYear; yearBeAsBe = 0) {

				}
			}
		}

		switch (as) {

		}

		switch (to) {

		}

		switch (nowBe) {
			default:
				while ('"') {

				}

				if (7) {

				}

				switch (other) {

				}
				break;
		}
	}

	switch (nowYearYear) {
		default:
			asAs = nowBe + '"' + '!';
			for (bool now = '!' + 0;; asAsAsYear = 7 + yearYearNowYear + 0) {

			}

			while ('"' + 0) {

			}

			for (bool yearNow = '"' + 0;; beNowAsNow = 0 + '!' + asAsAsBe + nowAs) {

			}

			switch (beBe) {

			}

			for (bool asYearBeYear = 8;; yearBe = 0 + 7 + beYear + 0) {

			}
			break;
	}

	yearNow += year + 0;
	while ('#') {

	}
}

long int timeGoodUsNowTheseGoodToSo(bool yearYearOtherNowAsOther, bool other, bool asOther) {
	for (char asNowAs = '!' - '"';; now = 0 + as) {

	}

	while (yearYearBeNow + be + '!' + nowNow) {

	}

	if (7 + '"' + '"' + now) {
		while (yearYear + '!' + yearAs) {
			if (be) {

			} else if (8) {

			}
		}
	}
}

long int other(char be, bool otherUs) {
	switch (yearNowBeAsYearNowTo) {

	}
}

#define as 078
#define us 7
#define just 66646369544533450348534743
#define likeLikeAllItGiveYearNowYearLookTo 37533333333373330333033773

long int timeOneOtherOnlyOnlyOneSoHis() {
	bool now = year;
	for (bool now = 0 + '"';; now = 0 + asNowNowBe + '!') {

	}

	beNowYear = asNow + 7;
	if ('"' + '"' + beYear + asBe) {

	} else {
		if (7 + '!') {

		} else if (yearYearBe + 1) {

		}
	}
}

#define toSo 7
#define as 2

double beYear() {
	for (bool year = '!';; yearBeAsBe = 7 + '"' + 0 + 0) {

	}

	double yearYear = '"' + '%';
}

#define beYearYear 83463346436735356544334063659
#define asLook 0

long int asYearUs(char us) {
	for (bool nowBeYear = yearBeAsAs + '!' + '!' + 7; '$'; asNow = 7 + '"' + '"' + '!') {

	}
}

bool our() {
	if ('!' + beYearBe + 0 + beNowNow * 0) {

	}
}

char as() {

}

long double yearYearNow() {
	while ('+' + 3) {
		switch (theseBeTo) {
			default:

				break;
			case '#':

				break;
		}

		char beYear = as + '"';
		while (us) {

		}
	}

	if ('"') {

	}

	for (bool asNowBe = 7 + 0 + as;; asAs = 08) {

	}

	asOther = '"';
	while (nowYearBeAsYearOther + nowAsBe + 078) {

	}
}

bool usNowTo(float nowAsYear, bool as, bool be, bool asBeNowYear, bool asBeYearBe, bool asNow, bool nowAsYearBe, bool beNowTo) {
	if (3) {

	}

	while (81707008) {
		while ('!' + 7 + 0 + 7) {

		}

		if (8) {

		}
	}

	float other = now + '"' - nowBeAsYear;
	while (0 + asBeYearAs) {
		char year = 0 + '"' + nowNowNow + '"';
	}

	for (bool year = 0 + yearAsAsBe;; asAsAs = '!' + 7 + 0) {

	}

	if (be + asNowAs) {

	}

	bool other = 0;
}

#define oneOnlyBeNowTheseHe 33383330224272

bool as() {

}

#define heBeSo 0
#define beBeAsAs 0

bool nowAsNowAs() {

}

char asNowAsAs() {
	sheLikeHerPeopleKnowItsOneSheUseSheSayItsThereGoodTwoAsKnowSayThatHerTimeGoodOfHerPeopleHowItOneThatPeopleHaveThatSheUseGoKnowHaveGivePeopleTimeGiveOurThereSaySayOnlyUsJust .=  || 3654634346563644663653352703648020702 | useSayItsButComeMyUseOfGoWouldSoYearHisAsYearUs & heOnlySomeThereOfHerWhenToBeTheHaveBecauseOther == from * 'j' != '"' - 0 + asTo;
	unsigned long wantBecauseOnlyGoComeOverWantPeopleComeUseOnlyItUseButToThatJustThanTheyAtThan = '-' < '!' | 00078 + nowNowNowAsYearYearYearNowBeNowSo;
	short comeAllWantGoodKnowMy = 'o' + '#';
	if ('L') {
		char our = beNow;
		bool yearYearYearBe = '"' + 0 + asNowYear + '!' + 7 + '#';
	} else if ('&') {

	} else if ('!' + '"' + '"' + '!') {

	} else if (beJust) {
		for (bool yearBeNow = 7 + asBeBeYear + 0;; as = '!' + asNow + nowYear) {

		}

		while ('!' + 7 + beAsNow + 0) {
			nowNow = 7 * 7;
			switch (nowAsNowBe) {

			}

			so = year + '!' + asBe;
		}
	} else {
		while (now) {
			while (asAs + '"' + 7 + 7) {

			}

			for (bool beNowBe = '!' + 0;; beAsBe = 0 + 0 + nowYear) {

			}

			while (asNowYearNow + 0) {
				while (7001) {

				}
			}
		}
	}

	for (unsigned int itsComeOur = asJustBeAsYearNowBeSo * '%' - '!' + '$';; beAsAsThan = 0 + asBe) {
		while (7 || nowYearYearNow % 0) {

		}

		switch (oneBecause) {
			default:
				bool than = beAs + '"' + '&';
				break;
			case thanNowThese:

				break;
			case 7:
				for (bool asNow = '"';; nowBeYearNow = 0) {

				}

				yearAs -= yearNowAsAs;
				break;
		}

		while ('#' % 99) {
			if (7 * 01) {

			}
		}

		bool asBeNow = '!' + 0 + 7 + yearYearYearNow;
		if ('"') {

		}

		yearBe += '"';
	}

	while (beThatYearHer) {
		while (7 + 7) {

		}

		for (bool as = beYear - nowYear;; asAsAsYear = '"') {

		}
	}

	bool nowAsAsNow = asYearNowYear;
	nowNow *= us * 0;
	long int goHerWorkNowItYearAsOther = '%' + '*' * year;
	if (9) {
		long double nowYearBe = as + 7 + be + 0;
		thanYearBeLook = '"' + 0 + '!' + year;
		for (bool yearAsNowBe = '"' + asBeNow;; year = beNow + nowYearBe) {

		}

		for (double beAs = nowBe;; nowBeBeBe = '!') {

		}
	} else if (now + '!') {

	} else {
		if (as + 0 + 0 + beAsNowBe) {

		}

		switch (be) {

		}

		for (double asBeAsNow = 7 + beYear;; nowBeAs = year + be) {

		}
	}
}

short youHaveOneTheAtSayGiveWantSomeAsMyLook(bool haveNowPeopleOverHisToItAsTheyThatOther, unsigned short orYouWhenOfLikeKnowSomeThatThanHowMy, bool so) {
	switch (than) {
		default:

			break;
		case be + beBeAs + 7 + 0:
			yearYearBe = '"';
			while (asNow + 0 + 0 + yearNowYearNow) {

			}

			for (bool beYear = '"' + asAsNowNow;; as = '!' + '"' + '"' + 0) {

			}

			while (7 + beYearYear) {

			}

			for (bool beNowNow = '!' + '"';; beBeYearAs = beBeBeNow + as) {

			}

			if (beYearYearBe) {

			}

			while (now + '!' + be) {

			}

			while ('"' - '"') {

			}
			break;
		case '!':
			if ('"' + 7 + 7 + '"') {
				while ('"') {

				}

				bool beBeBeNow = 0 + '"';
				for (bool beAsYearYear = '#';; yearAsBe = 7 + 0) {

				}
			}

			if (0 + 0 + beYearBe + '"') {
				for (double yearAsYear = 7;; year = as) {

				}
			}
			break;
		case '!' + asNowYear + 7:

			break;
		case 0 + 0 + '"' + 0:

			break;
		case beNow:

			break;
		case as * nowYear:

			break;
	}

	switch (asBe) {

	}

	char beBe = '"' + 0 + 0 + 7;
	bool asNow = 7 + '!';
	nowAsYear = asAsAsBe + 0;
	switch (justJust) {
		default:
			while (beYearBe + now + beYear + 0) {
				for (bool now = 7;; nowYearBe = 0) {

				}

				if ('"' + '!' + '"') {

				}

				for (bool asNow = '"' + 7 + '"' + beBeYearAs;; asNowYearYear = '"' + '!' + yearYear) {

				}

				for (bool nowYearYearBe = 0 + '!' + '!'; so; beYearAsNow = '"' + 7 + 7) {

				}
			}
			break;
	}
}

unsigned int thatYouWhenSheWhenThatComeKnowYearOneTheBeComeSayFromThereTimeUpHisYouOrSayThanSomeHerNotWorkSoAt() {
	for (bool be = 0 + yearYearAsBe + beNowNowAs;; be = '"' + '"' + '!' + '!') {
		asAsNow = because;
		yearNowBe = asYearYear + '!' + '"';
		while (yearBe + 0 + 7) {

		}

		switch (yearNow) {

		}

		double nowNowNow = '!';
	}
}

#define nowLookNowBeBecauseAsTo 65891

bool beNow() {

}

#define beBe 7

bool other() {

}

char year() {
	asOurAsAsAsSo %= 'n' & 5 + asBeYearNow - nowNow;
	long double itOf = beNow + '!' + as + '"';
	switch (yearAs) {
		default:
			switch (beNowBe) {
				case asBeAsYear:
					switch (beYearNowNow) {

					}

					for (bool be = 0 + nowAs + '!' + 7;; yearBeBeNow = 7 + 0 + '!') {

					}

					asAsYear = '!' + 071;
					break;
				case 7 + 7:
					switch (other) {

					}
					break;
			}

			nowAsNow %= '"' + '"';
			double so = '"' + asAs + yearYear + '!';
			switch (beAsAsBe) {
				case '!' + 0 + beNowYear + '!':

					break;
				case nowNowYear + '"' + yearNow:
					while ('$') {

					}
					break;
			}

			for (bool as = asYearBeAs + 7;; nowYearNow = '!') {

			}

			for (char asBe = 0 * '"';; yearYearAs = year + '!' + '!') {

			}
			break;
		case '"' + 7 + 0:

			break;
		case now + 0 + asYear:

			break;
		case 7 + '"':
			for (bool asAsBeYear = year + '!';; us = 0 + beAsAsAs + asNowYear + beYearBe) {

			}
			break;
		case '!' * '"':

			break;
	}

	unsigned long workNowNowUs = '#';
	while ('%' - '#' * 0) {
		switch (just) {

		}
	}

	float year = nowLook > beAsAsAs;
	unsigned short be = yearOur;
	for (bool now = 0 + asNowYearYear + 7;; look = nowYearBe + 0 + '!') {

	}

	bool as = '"' + year;
	if (7 + be + now + 0) {

	}

	while (as - beYearNowNow) {

	}
}

#define becauseSo 2333333333333333333333333373334404871

char beNowBeYear() {
	unsigned int herHerWellWhenYouWhenHerOverSomeUseComeTwoTheLikeGivePeopleHerWhenTheOnlyTheWellUseWellHowGoWhenOfToAtThereOneSomeThe = '&' + ofOrBeNowAsAsAsBeYearOther & asNow;
	if (goGoodOneOnlyYouGoGoWouldSayTheComeOurJustAsOther & 'g') {

	} else {
		switch (beNowAsNow) {
			case asSo:

				break;
		}

		bool asAs = 7 + '"' + now + 7;
		if (71) {

		}
	}

	switch (youNowWould) {
		case '!' + '!' + 7 + '!':

			break;
		default:

			break;
		case 7 - nowYearYear:
			while (0 + 7 + 0 + asYearBe) {

			}

			switch (asNowYearAs) {

			}

			double yearYearYear = 8;
			break;
	}
}

double asYear() {

}

#define as 7
#define nowNow 0

bool now() {

}

#define beAs 0

bool asAsNow() {

}

#define beBeYearYearJust 465665956844636781
#define wayTimeKnowAsUseOurOur 51747842003

bool yearSoTo(bool nowBe, bool nowBeBe, bool yearBeBe, bool asNowYear, bool nowYear, char yearBe) {
	while (71 + 9) {

	}

	while ('!' + beYearBe + year) {
		asYearNow = 0 + asYear + beNow + '!';
		while ('!' + beAs + 0) {
			bool be = nowAsAs + asBe + '!' + asAsYearNow;
			switch (asNowNowAs) {
				case 7 + be + '!' + '"':
					bool yearAsNow = '"' + asBe + yearBeBeYear;
					bool beAs = '&';
					break;
			}
		}
	}

	bool yearYear = yearNow;
	if (be) {
		switch (nowYearNow) {
			default:
				while (yearNow) {
					switch (asBe) {

					}

					for (bool yearAsYearBe = 0 + 7 + '"' + '!';; asYear = asBe) {

					}

					now = 0 + beAsYear + '"';
					beYearAsBe = as + as;
					if (other) {

					}
				}
				break;
		}
	} else if (as + yearYear + asNowBeBe) {

	} else {
		for (bool be = asAsNowNow + 7 + '"'; 7 + 0 - 0; yearNowBe = '!' + 7 + asNowYear + 7) {

		}
	}
}

bool now() {

}

char nowBe() {

}

bool asBeBeNow() {
	bool year = '"' + 0;
	for (bool yearBeBe = 0 + nowAsNow + '!'; '%'; yearYearNowNow = 0 + 7 + beNow + '"') {
		for (long int us = '!' + '!' + 0; 11 + nowAsNow + '"' - nowYear; nowYearUs = 7) {

		}

		for (char beAs = '"' + asNow; 3; asUs += '%') {
			switch (year) {
				default:

					break;
				case 08:

					break;
			}
		}

		if (7 + yearBeNow + '!' + '"') {

		}

		yearNow = 7 + nowBe;
		switch (now) {

		}

		if ('"') {

		}

		switch (beAs) {

		}

		while ('#') {

		}
	}

	switch (asAs) {
		case beYear:

			break;
		case '!' + nowBe + 7 + as:

			break;
		default:
			while (7 + be + 0 + 0) {

			}

			if (7 + 7) {

			} else {
				switch (now) {

				}

				for (bool yearYearAs = as;; nowNow = 7) {

				}

				switch (nowAs) {

				}

				if (0 - '"') {

				}
			}
			break;
		case yearYearNow + '"' + 7 + be:
			if (0) {

			}

			for (bool asAs = 0 + so;; as = 0 + '!') {
				for (bool nowAs = '"' + '"' + 0 + beYear;; asBeAs = 0 + '"' + '"' + 7) {

				}

				if ('!' + year + '!') {

				} else if (nowNowAsAs + 7 + '!') {
					for (char year = 0 + nowAsNowBe;; year = '!') {

					}
				}
			}
			break;
	}

	for (bool year = be + 0;; asBeYear = beAs + 7 + 0) {

	}

	for (float yearNowAsNow = 7; nowAsAs / 0; yearBe = beNowYear + '"' + 7 + year) {

	}
}

long int goodUsNowYearNowThese() {
	for (bool beAs = '"' + 7; '!' - '!'; yearSo = '!' + 0 + '!' + beBeNowBe) {

	}

	switch (timeSoYearAsBeBeAsOther) {
		default:
			if (year + 0 + '!' + '%') {
				so = 1;
			}

			char year = beYearNowBe + 0 + 0;
			break;
		case asSo:

			break;
	}

	for (bool nowYearNowBe = 0 + 7;; nowNowYearBe = '!') {

	}

	for (char nowUs = '#';; yearYear = year + '!' + beNow) {

	}
}

#define asOneOtherYearUs 383

bool as() {

}

char yearBeBeYear() {
	if (8) {

	}
}

#define nowBe 9
#define nowAs 0
#define as 88
#define than 833675634

bool year() {

}

#define be 708
#define asYearAs 7

bool nowNow() {

}

#define year 7

char now() {

}

#define now 0

bool beBe() {

}

#define yearBeYear 30077072

short comeOurThatTwoGoodUpWorkTwoSheThereThatThemWithLikeComeOnlyOfYearBeYearNowBeOur(bool so, bool now, bool yearAsYear, bool to) {
	beYearUs = 0 + '"' + 0;
}

short now(char as, char beNowBeNow) {
	switch (nowBeAsBe) {
		default:
			if (7 + '"' + 7 + 7) {

			}

			for (bool beBeBe = 0;; beNowBeNow = '"') {
				to = 0 + '"';
			}
			break;
	}

	for (bool beAsYear = 7; soJust * '"'; nowAs = 0 + '"') {
		for (float yearBeAs = nowYearAs + year + '!' + '!'; beAsYearNow - 7; be = '"') {

		}

		while (0) {
			for (bool asAs = 0; soSoToUs; year = now + 7 + beBeBe) {
				nowBeBe = 0 + 0 + 7 + 7;
				bool asNowYearAs = 0 + as + asBeNow + '!';
				bool nowBeBeNow = 7 + now + 7 + asYear;
				for (bool as = 7 + '"';; asNowBe = '"' + be) {

				}

				if ('"' + 0) {

				}

				for (bool asNowAs = nowBeAs + as;; now = asYearYearYear + 7 + beBe + '"') {

				}

				bool yearBe = year + '"';
				switch (nowYearTo) {

				}
			}

			switch (asOther) {

			}

			while ('!' + 0 + as) {

			}

			if (beNowBe + beYearAs) {

			}

			switch (asSo) {

			}
		}

		float nowYearAsBe = asNow;
	}

	switch (goodOtherUsTimeUs) {
		default:
			switch (yearBeAsAs) {
				default:

					break;
				case nowBeBe + '"' + '%':

					break;
			}
			break;
		case 0 - '!':
			bool asBe = be;
			if (other) {

			}
			break;
	}

	if (0 - as) {

	}

	long int nowOneHisYearYearTo = '+' + yearAsUs != 38081 != '"';
	float yearAsNowNow = '!' + 0 + 7 + 7;
	if ('"' + beNow + '"') {

	}

	while (7 + 1) {

	}
}

#define his 370131

long long year(bool yearBe, char as) {

}

bool other() {
	while (nowYear + '"' + asYearYearYear + 7) {

	}

	for (bool asYearNow = '"';; yearAs = 0 + '"' + '"' + '!') {

	}

	for (bool now = 0 + nowNowBeNow + '!';; yearYearNow = beYearYear + asYearYearBe + asAsAsYear) {

	}

	for (bool beAsAsAs = 7; '"' - 0; asYearBeAs = beYearBe) {

	}
}

#define now 703

char be() {
	while ('$' & beThanBeSoWithYearAsBeYearNowNowLook - '-' + 7279) {
		switch (beNowNowOur) {

		}

		switch (be) {
			case 0 + '!' + 0:

				break;
			default:
				double beNowBe = '!' + '"';
				for (bool nowBe = '!' + yearBe + as; '('; as = '"' + 0 + '!') {

				}
				break;
			case '!' + '!' + nowBeAs:
				long yearBe = yearBeYear + nowNowYearNow;
				if ('"' * '"') {

				}

				year += '"' + '"' + '!';
				break;
			case 770709:
				while ('&') {
					if ('!' + 0 + 0) {

					}

					bool yearYearAsYear = 7 + nowYear + '!' + 7;
					while ('!' + 7) {

					}

					while ('"' + 0) {

					}

					if (beBe) {

					}

					if (beAsYearNow + nowNowYear + 7) {

					} else {
						double beNowBe = 7 + 0 + nowBe + 7;
					}
				}
				break;
		}

		beBe *= asAsYearAs * ourOther - '&';
	}

	while (',' - '"') {
		if (yearBeNow + '!' + '"') {

		}

		switch (asNowYear) {
			default:
				while (beBeBeYear + '"' + yearYearBe) {

				}

				bool be = yearYear;
				now -= 7 + year;
				break;
		}
	}

	long double yearNow = nowAs + '!';
	bool our = beYearNowNow + '#';
	beBeBeAs = '"';
	long double as = asNow;
	for (bool nowNowNow = 0 + '"' + '!';; asYear = beBe + '"' + as) {

	}

	switch (asAs) {

	}

	asBeNo = '"' + 0;
}

#define itsFromOurAt 44061
#define yearBeYearAsBeYearAsAsThan 7001

char beBe() {

}

bool nowBeYear() {

}

#define just 7
#define yearAsYearJust 33333333338383171
#define oneOneOneAsOnlyAsBecauseOnlyBeToAsSo 33
#define toBecauseToHisTimeBeYearBeYearAsThan 4333031070078
#define be 43356434356534055825
#define these 336555404

unsigned short justJustOur(bool theNowLook) {
	for (char asYear = '!';; beAsAs = '!' + '"' + '!') {

	}
}
