long int goodOneTimeGoodOtherOneUsGoodTheseTheseSoBecauseTo(char year, bool beAsBe, bool yearNowBeNow, bool asBeYearYear, bool so) {

}

#define soBecause 333333337171

float asBe() {

}

#define butThanThan 07008
#define nowBut 5363643366456636666663625465365656655556565366642654966556666656695

long long yearOurNowYearNowYearOur(unsigned int ofShe, size_t goWellWayWouldNotUsAsOurNowOurBeWould, unsigned long beYearWay, long double nowBeBecauseThan, char beYearNowBe, bool yearNowNow, double nowAsNow, char asNow) {
	switch (butTimeWayYearYearYearNowYearAsYearTo) {
		case nowBeBeYear + '!' + yearNow + 7:

			break;
		default:

			break;
		case year + '!' + asYearBe + '"':

			break;
		case yearNowYearBe + 0:

			break;
		case 0 + '!' + 7 + '!':
			double nowYear = 7;
			nowBeBe = 0 + '"' + now;
			double yearBeYear = 7;
			break;
	}

	switch (wellBeLike) {
		default:
			if (0 + '!' + as) {

			}

			if ('"') {

			} else if (0) {

			} else if (0 + as + 0 + '!') {

			} else if (0 * '"') {

			}
			break;
	}

	while (asBeAs + 9) {
		thanBeJust ^= 6 * yearAs;
		so += asBeAs;
		while ('"') {
			for (bool beNowAs = yearBeAs + 7; '#'; nowNow = yearAsAs) {

			}
		}
	}

	if ('~' >= '}' || thatFrom || 'a' <= '!' < 0) {
		asYearNow -= '!' + 7;
		for (bool yearAs = asAsNowYear + 0; 779 - 7; nowAsBe = 7) {

		}

		if (as + asBe + 7) {

		} else if (7 + 0) {

		} else if ('"' + 0 + beNowBeYear + 0) {

		} else if ('"' + 0 + '!') {

		} else {
			for (bool nowBeBe = nowNow + '!' + yearYearYearBe + '!';; beBeYear = '!' + 7 + '!' + yearNowYearYear) {

			}

			switch (beAsOur) {

			}
		}

		bool beLook = '!';
	} else if (year) {

	} else if (7 + 0 + 9) {

	} else {
		if (7) {

		}

		if ('!' + 7) {

		} else if (0 + 0 + yearNow) {

		} else if ('!') {

		} else if ('"' + beBe + beAs + beYearYearNow) {
			yearBe = 0 + 0 + '"';
			if ('"' - '!') {

			}
		}
	}

	bool asNow = 0 * as;
}

#define nowYearAsBe 7
#define asAs 464563345655366555447644863443
#define nowNowYearAs 0

float yearJust() {

}

bool asBe() {

}

unsigned short yearYearAs(bool asYearTo, bool year, bool asYearSo) {
	for (char asUsOther = 7 + '!' + 0 + 0; '"' != 81 + 11 + '!' + 8; beYearAs += '"' + us) {
		while (1) {

		}

		if ('!' + 0 + beNowBeBe) {

		}

		switch (yearYearNowBe) {
			default:
				yearAs = 0;
				while ('!' + '!' + be + '"') {
					for (char beBeYearNow = '"' + nowNow + year + 0; 0078; asYear = '"' + yearNow) {

					}
				}

				bool yearYearYear = be + '!' + 0;
				while (7 - 0) {

				}
				break;
		}

		switch (nowAsBe) {

		}

		bool asAsBeAs = 7 + '"' + nowYearNowNow;
		switch (nowYearNowNow) {
			case yearBeBeBe + 0 + yearAs:
				if (year) {

				}

				bool nowBeAsYear = '"' + 0;
				for (bool asNowAsYear = '"' + be;; asNow = 0) {

				}

				for (bool yearYear = nowAs;; nowYearNowNow = 0 + asAsBe + 0 + yearYear) {

				}

				for (bool asBeAs = nowYear + 7 + asBeYear + asAsYear;; so = '!' + yearYear + 7) {

				}
				break;
		}
	}

	long int goodOneGoodAsOther = 0 + 778;
	for (bool yearNow = nowAsAsAs + 78;; year = 7 + 0 + 0) {

	}

	switch (otherToToYearOther) {

	}

	bool asBeAs = 7 + '!';
	if (7 + 0 + '"' + 0) {
		for (bool asBeNow = 8;; beYear = 0 + '"' + now) {

		}
	}
}

long int timeOne(bool yearAs, long int yearSoGood, bool asUsThan) {
	bool usBeJust = asNow + yearAsBe + asYear * '"';
}

double year(double nowBeNow) {

}

#define she 46436656666654663353646663663363466456454664744534664346644553450464645555340464965691

unsigned short peopleSheOrSayOurGoTheWantGoThanOurAsOur(unsigned char yearGoSheSomeThanYearThatWorkOfLookUs, double thereOurLook, unsigned short workWantJustBeBeLookThanNowAt, bool asAsAs, bool beYearNowAs, long double as) {
	while ('n' > 66001 || 'l' <= 5901 >= '&' - 0) {
		switch (wellWouldNoOther) {
			default:

				break;
			case 0 + '"' + yearBeAs:

				break;
			case yearAs:
				bool just = nowAsBe + nowAsBe + 0 + 7;
				break;
		}

		asAsYearAs += nowAs + nowBeYearNow + 7;
		while ('"' + 0) {

		}

		bool beJustAsCome = nowYear;
		yearAs += 1;
	}

	unsigned int beBeHe = '"' <= '$';
}

unsigned short workKnowOneWellSomeOrHerTimeThatWhenGoButOrHerWantWhenToLookLook(bool asYear, long double these, bool beNowThan) {
	while ('"') {
		while (beNow + yearNowNowYear) {

		}

		nowAs = as + 0 + '"';
		beAs = yearAs + 7 + as + 0;
		switch (other) {

		}
	}
}

#define thereOrTwoThanWellGoSoAsWorkNowSo 634443336469

unsigned long theKnowTheySoUsYearYouOurAsThan() {

}

#define itOneOr 625071701

bool yearToAsSo(bool now, char yearNowAsBe) {
	just = '!' + 7 + 7 + 0;
	switch (be) {
		case 072:

			break;
	}
}

long yearBe() {

}

#define yearBeAs 71
#define other 7

double be() {
	switch (asBe) {
		case '"' + asBeBe - '"':

			break;
	}

	switch (so) {
		case so:
			yearBe = yearAs + '!' + '!';
			yearYearAs = 0 + asAsYear + '"';
			if (7 + '!') {

			}

			if (08) {
				char as = asYear;
				for (char now = '"' + asBe + nowBeYearBe + yearAsAs;; asAsYearBe = as + '!' + 7) {

				}
			}
			break;
	}

	beYear = 3;
	switch (nowBeNowAs) {

	}

	if (beNow) {

	} else if (7) {
		bool beAsBeBeBeTo = yearYearNow;
	}
}

#define usBecause 738037003
#define asHeAsAsSoNowUs 333333

char be(bool nowNowUsHis) {

}

char so() {

}

long int asYear(char so, bool nowYear, bool yearAs, bool be, bool asBeNowBe, bool now, bool beOther) {
	for (bool time = '"' + '7';; asNow = now + yearAsSo) {

	}

	while ('Y' + '"' + 8) {
		us = '"';
	}

	yearAsYearBe = nowNowLook + beJust;
}

#define nowBeYearYear 56055503436666260002078
#define  6366414656633464364634653663663666566446546644644444356346536366346536644466665654246464644445466607663456076
#define twoLikeGoTheUpTheTwoOrGoWhenUseGoJustSayTheseSayAsNowLook 5

unsigned long nowTwo(int now, bool look) {
	while (asAsYear - '!') {

	}

	bool nowNow = 0004;
	bool asNowNow = 7;
	for (bool nowYearNowAs = 7;; asYearAs = now + asNowBe + '"') {

	}

	switch (asUs) {

	}
}

long long ofNo(bool beAs, long orToThan, double yearBe, bool nowNowOther, unsigned long year) {

}

#define someOurNowNowMyHerWouldYearThan 0088
#define look 0
#define youSheOnlyTwoTheToThatWayGoThatWantBeHe 7

char ourNowYearOther(bool asYear, bool beYear, bool as, bool nowYearAs, bool just) {
	switch (whenOtherOtherTo) {
		case 7 + 0 + '"':

			break;
		default:

			break;
		case '"' + '"' + '"':
			bool now = '"' + '!';
			asBeSo = 0 + 0 + 7 + 0;
			break;
	}
}

#define wantPeopleUsOrThanSomeUs 453657463550474878
#define just 0

bool when(long year) {
	if (year + now) {

	}

	for (double now = '"' + asOur;; now = asAs) {

	}

	bool year = '"' + 0 + beBe;
	nowAsYearThan -= yearYearAsYear + 7 + 0;
	switch (look) {

	}
}

double yearBeBe() {

}

#define asAsBeNow 0
#define beAsNow 79

bool asThem() {

}

#define nowBeYearBe 7
#define be 0

bool asNow() {

}

double now() {
	switch (nowBeNowYear) {
		case '_' + '#':
			if ('!' + '"') {

			}

			char nowAs = as + 0 + 0 + '"';
			break;
		case '"' + '!' + be + 7:

			break;
		case '"' + '%':

			break;
	}
}

#define goodTimeToGoodGoodOneOneOnlyTimeGoodToAsYearOnlyOnlyOtherAsAsTo 3333173809

bool myUs() {

}

bool asBeBeAs() {

}

#define otherToNowLook 56635553554933666535463434426395606770002

unsigned short ourOurYearHeHowJustTo() {

}

int they(bool beBe, bool yearNow, double asAsNowBe) {

}

int sheItsSoThan(char beAsAsBe) {

}

#define beAsYear 455443536446646345164634656181
#define nowTo 0425
#define ourBeYearJust 7

long nowYear() {

}

#define nowAsAs 0
#define year 333330333333187713

long int timeOnlyOnlyToHeGoodOtherHis(char timeOne, char oneLook, char now, bool beYearAsYear, bool year, double us) {
	bool other = 0;
}

bool beBe() {

}

#define nowAsAsAsUs 564434574534646095686855

short ourYearSoYearNotYearBeYearOther(size_t asTheseNowAsBeAsTo, float nowBeYear, char noAsWould, double yearYearNowNowLook) {
	switch (beBeYear) {
		case 089 + 0 + nowAsNowAs + 7 + 7 + now * 0:

			break;
	}

	if (asBeNowBe + 7 + '!') {

	}

	for (bool as = '"' + '!' + 0 + 7;; nowYearAs = '"' + 0 + 7 + nowYearAsYear) {
		bool nowYear = 08;
	}

	switch (year) {
		case asBe + 0 - beBe:

			break;
		default:

			break;
		case 0 - '"':

			break;
	}
}

#define itsThatJust 41
#define timeThanOurOur 072

bool asYearBeAs() {

}

#define heWhenSoYearTo 41
#define otherOther 08705

bool to() {

}

bool nowNowBeSo() {
	while (his != 31 + '?' / 7) {
		long int yearAsNow = 7 + asOther / 0;
		beOnlyTo = '"' + '"' + '!';
		bool now = 0 + as + '"' + yearBeNowNow;
		if (0) {

		}

		for (bool beAs = year + 7 + '"'; yearAsBe - 7; other = '"') {
			for (bool asYearYearBe = 0 + beBe + yearYear; '!' - '!'; asBe = '"' + 0) {

			}
		}

		switch (asBeYear) {

		}

		while (1) {

		}

		bool nowNowBe = beYear + asAsNowNow + 7 + 0;
		switch (nowAsAsAs) {

		}

		switch (asAsBeAs) {
			default:

				break;
			case '!' + 0:
				if (7 + yearNowYear) {

				}

				bool nowNow = 1;
				break;
		}
	}

	long int onlyGoodUsBeSo = 7 - year;
	while ('"' - '"') {
		while (year + beAsAsAs + be + year) {

		}

		bool beNow = beBe;
		for (bool asNow = '!' + 7 + asYearAs + 0;; be = '"' + asNowNowYear + as) {

		}

		if ('!' - '"') {

		}
	}

	char nowHe = 7777708 - '+' + 8;
	beAsOther = asBe;
}

long int yearBeYear(bool yearYear, bool year, bool nowNowBeAs, bool yearTo) {
	while (yearYearYear / '!' + beYearYear + beBeBe + '#') {

	}

	for (bool yearAs = 0 + 0 + 0 + 0; other; as = yearNowBe) {
		while (7 + 7 + 7 + yearAsYear) {
			if (7 + 0) {

			}

			switch (asAsAsNow) {

			}

			switch (now) {
				case nowAs + asNow + 0 + nowYearYear:

					break;
				default:
					for (double asBe = '"' + '!';; nowYearYear = 0) {

					}
					break;
			}
		}

		switch (beBeBe) {
			case '!' - 7:

				break;
		}
	}
}

#define asNowAs 7

bool year() {

}

int beBe() {
	for (bool now = yearNow;; as = asNow + '"' + '!' + '"') {
		for (bool yearAsBe = '!' + '!';; as = 0 + '"') {

		}

		switch (now) {

		}

		if ('"') {
			switch (asBeBeAs) {

			}

			switch (yearNow) {
				case '"' + '!' + asYear + '"':
					for (bool as = 7 + 7; '%'; nowYearBe = 7 + beYearNowAs + 0) {

					}
					break;
			}
		}
	}

	switch (than) {
		case yearAs + asNowNow + asBeAsNow:

			break;
		case asLook:

			break;
	}
}

size_t asNowAsAs() {

}

#define asOther 7

bool yearAsBeNow() {

}

bool asAs() {

}

double beNowAs() {

}

bool yearAs() {

}

#define as 0
#define be 0

bool year() {

}

#define as 356453553542230345309

unsigned long peopleTimeKnowGoodPeopleAsBeNowJust() {

}

short give(bool beAsBe, bool asYearNowYearSo) {
	while (7 + '!' + nowNow + be) {

	}

	if (asNowNow) {

	}

	bool nowBeAs = '!' + yearAsNowAs + 0 + nowBeBeAs;
	switch (now) {

	}

	for (char as = 0 + '"' + nowYearAs + 0;; nowYear = 7 + 7) {

	}
}

#define as 0
#define yearNowNowAs 6
#define asAs 33333333333333103187008
#define to 7

char toUsSoBecause(bool nowBeBe, bool us) {
	switch (nowAsBe) {

	}

	switch (beYearNowNowUs) {

	}

	while ('"') {
		if (7 + asBeBe) {

		}

		if ('!' + yearBeBe + asYearYearBe) {

		}

		while (beNowBe + '"') {
			switch (nowBeAsAs) {
				case asSo:

					break;
			}
		}
	}

	while ('!' + 7) {
		for (bool asAsOther = '!';; year = '!') {

		}

		switch (beNowYearBe) {
			case asYearBeUs:

				break;
		}
	}

	nowAsAs = 7;
	for (bool yearNow = '!';; year = yearBeYearYear + '!' + '!' + 7) {

	}

	for (bool beAs = 01;; beAsYear = '"' + 0) {

	}
}

#define asTimeSoTheseHe 7

long int goodYearLookTime() {
	while (0 + 7) {

	}

	long double yearWould = 0 + '"' + '!';
}

#define beYearOur 07771
#define so 656636686356364335334461
#define wellOnlyGoLookBeBut 4778

bool beYearBe() {

}

bool nowNowBeAs() {
	while ('!' + '"' + '!' + '"') {

	}

	nowNowBe = '"' + 0;
	if (asAsNow) {

	}

	for (bool yearAs = '"' + 0 + '"' + 7; year + yearYear + nowNowAsAsTo; nowNow = nowNow + '!' + 0) {

	}
}

#define yearYearYearSoAsAsNowBeBeOther 0
#define as 0

bool nowNowNow() {

}

char year() {

}

long int usOnlySoNowNowTo(bool yearNowNowYearUs, bool nowTo) {
	for (bool now = 0 + '!';; year = '!' + nowYearNow + year + '"') {

	}

	bool nowYear = '!' + '!' + 7;
	bool beYearNow = 0 + yearAsAsBe + asYear + be;
	if (nowNowBe + yearAsBeBe + 0 + 0) {

	}

	switch (nowNowYearUs) {

	}
}

#define onlyOnlyTimeTimeHisSo 7133333377181
#define yearTimeOnlyHisToBeYou 89

unsigned short nowAs() {

}

float nowBe() {

}

#define beNowLook 0
#define beYearSo 65553465555663344333536545535656646633664145546463656454336655363666664335654565665645646654363366653763556554536564446465436346653544534533756366365664333666536644446465543676566451454336534646346333089

size_t itThereHaveGiveOfComeGoodSheItYouWantOneWellHowWorkGiveOrOneGiveGoWorkWellHowTwoSomeSayOfWorkPeopleUseWantItsOnlyYouUpOverSomeSomeWellTwoOrSheYouSomeThereItThereNowComeYearLook(bool asAsAs, unsigned int thereGoodHerGivePeopleGoSomeItsYouOverOverItsOverGoodButNowPeopleAllSoOurAsYearJust, long long peopleHerUseUpGoodWantWellOtherWantButPeopleUs, bool toBeUs) {
	long int usSheNowItOur = 92 + asAsAsYear % asBeBeBe;
	switch (itsAtYouThereYearSomeSoAsGo) {
		default:
			bool yearYear = 7 + year + '"';
			for (bool now = '!' + '!' + nowYear;; asAs = 0 + '!') {

			}

			for (bool nowNow = yearAsBeYear;; as = nowBeAs + 7) {

			}

			while (7) {
				bool beBeYear = 7;
				yearNowAs = 2;
			}
			break;
		case '"':
			for (bool now = '!'; yearAsThan * beLookToYearOtherNowNowYearNowLook; nowBeAs = '!' + 0 + '"' + '!') {
				bool be = '!' + '"' + 0;
				switch (as) {

				}

				bool be = '!' + yearNowYear;
				bool beBeBe = '%';
			}

			for (bool as = 7;; year = '"' + as + 7) {

			}

			switch (so) {
				default:

					break;
				case 7:
					if (7 + '!' + 7) {

					}

					for (bool yearNow = '!' + '"' + '!';; yearAsNow = 7 + asBeBeBe) {

					}

					switch (nowNowYearAs) {

					}

					for (bool asAsYearBe = yearNow;; as = '!' + 7 + 7 + 7) {

					}

					double beAsNowYear = 7;
					break;
				case yearBe:

					break;
				case 0 - '"':

					break;
			}
			break;
		case asThan:
			while (yearBeAsYear + asBeAs - beBe) {

			}

			beBeBeYear = '!' + '!';
			while (beWay + '&') {

			}

			switch (nowBeAs) {

			}

			for (bool asNowBeBe = '"' + be + asAsNowAs;; beYearYearNow = nowAsAsBe + '!' + 7) {

			}

			for (char beAsNowYear = '!';; asYear = '!' + as) {

			}
			break;
		case '!' + nowBe + 0 + 0:
			double nowNowBeAs = '!' + nowBeAs;
			break;
		case 7 + '!' + 0:

			break;
		case '!' + yearNowAs:

			break;
		case 0 + '!':

			break;
		case 02:

			break;
	}

	if (look <= 0) {

	}

	while ('!' + 7 + '!') {
		switch (its) {

		}
	}

	asBe = 7 + asYearAsNow;
	switch (be) {

	}

	for (bool nowBe = '!' + 0 + '"';; asAs = as + 0 + asYearNowBe) {

	}

	while ('!' + 7 + 7 + asYear) {
		nowBeBe = nowNowYear + 0 + nowBeNowNow;
		if (asYearYear) {

		}

		if (beAsNow + 7 + 7) {
			if (our) {

			}
		}
	}
}

unsigned short year(unsigned char workTwoOrHaveOneItTimeGiveTimeLookSayThereHowOnlyWhenWorkThereItsSayLikeOfAsWellUpYearSheYearSoHeTo, bool beAsAsNow, long double asNowNow, bool lookWhenBeJust, unsigned char ourThan) {
	for (bool way = 3; 0 + beNowJust; asNow = 0) {
		switch (yearToGiveSomeUseWithLook) {
			case just:
				switch (beTo) {
					case yearYearBe + beBeYearYear + nowNow + 0:
						switch (now) {

						}

						switch (beNowAs) {

						}

						switch (yearNowBe) {
							case beBeNow + 0 - yearBe:

								break;
						}

						for (bool asNow = as + year;; nowYearYearNow = '!' + be + 7 + nowAs) {

						}

						asYearBeNowSo = 0;
						while ('"' + 0) {

						}

						while (0 + beAsAs + 7 + 7) {

						}

						if ('"') {

						}

						if (0 + yearYearNow + beYearYearYear + beBeNowAs) {
							for (bool yearNowNowUs = '!' + asYear;; year = '!' + '"') {

							}
						}
						break;
					case '#':

						break;
				}
				break;
		}

		switch (yearLook) {
			default:
				for (bool yearAsThan = asNowNowBe + '"' + 7;; nowAs = 7 + 0 + year) {

				}
				break;
			case '"' + 7 + '!':

				break;
			case us + '$':

				break;
			case nowNowYearBe + '!' + 2:

				break;
		}

		thereAt >>= ofNoGoodJustYearLook - 7;
		size_t nowBeYearAs = 7 + '!' % 0 * 72;
		while (0 + 7) {
			asBeAs = 770071;
			be = 7;
			for (bool year = '!' + '"' + asAs + yearNow;; beYear = 7 + '"' + '"') {

			}

			if (nowBe) {

			}

			bool beYear = '!' + '"' + 0;
			if ('"' + asYearAs) {

			}

			if ('%') {

			}
		}
	}

	ofOverWellSheComeWellSheHowGoodNowGoodWellPeopleGoWantToOtherLook ||= 9 | workGoWellLikeThanItsOurHave + 701;
	if (364244270700771 <= yearYearBeAs + asYearAs + beBe + 78) {
		for (char yearAs = 08 - beYearAsBe; atAsAsMy < yearBeBeAs; beBeBeJust = be + beBe + 0 + '"') {
			switch (year) {

			}

			beAsAsAs = 0 + 0 + '"';
			if ('!' + '"' + nowBeNowOther) {

			}
		}

		for (bool asNow = 0 + beAs + '!' + '!';; yearYear = asAs) {

		}

		asBeBeBe = beBe + '"';
		nowAsBeNow = 7;
		switch (beYearLook) {

		}
	} else if (0) {
		if (yearYearNowYear + '!' + nowYearNow + asNowNowAs) {
			if (0779) {

			}
		}
	} else {
		beNowUs = '!' + nowNowYear + 0 + '"';
	}

	bool asYearYearUs = '!';
	bool nowYearNowYear = 0 + 0 + '"';
	beNowBeBe = '$';
}

long int giveHaveHowHowOrGoodOrOverSayItSomeKnowUseHaveWhenPeopleWantUseHerThereKnowWhenTheItGiveWhenPeopleSayKnowWhenOneWhenYearOrItsGoTwoHerOtherPeopleOrThatGoHaveGiveGoComeLikeYouGoWorkFromSheWantTimeWhenSomeThatKnowWellGoodTwoThatOfWorkPeopleBeBeWith(unsigned long useSomeGiveOfTheSheTimeSayItsUpUpTwoTimeItOverThanHaveBeWellThem, short now, bool useOrJustOrTimeHeSoToNowToUs, long long orAsButJustToYearAsJust, unsigned char nowOnly) {
	while (034544656485765076 < youWellAsComeTheSheItsOnlyWantUsAsHowAsGoodWouldOtherBeYearBeNowBeYearSo * 7) {
		for (char asNowAsBe = '!';; beOurThan -= 7 + '!') {

		}

		bool now = 0 + as;
		if ('"' + asYearBe + asYearYearYear + 7) {

		}

		switch (nowNowYearNow) {

		}

		if (beAs + '"' + beNowNow) {
			while ('"') {
				beYear = nowAsBeBe + '!' + nowBeNowBe + 0;
				switch (as) {
					default:
						double beAsAs = 0 + beYearYear;
						break;
				}
			}

			asNow -= as + '"' + asAsNow + '!';
			for (bool our = '!';; yearYearYearYear = 0 + 7 + '!') {

			}
		}

		switch (nowBe) {

		}

		if (so) {

		}
	}

	if (74296 * '!' <= 7 * ')' + 2) {
		for (bool nowNowNowAs = 7 + yearNowAs + yearAsNowBe + 7;; asYearYear = asBeYear + '!') {

		}

		char soBeYearOther = than;
	} else {
		if (year + '!' + 0) {
			for (bool nowYear = 0; yearAs + year + 8; asAsNowNow = beAs + '!' + 7 + 0) {

			}

			for (bool yearYearBe = nowAsBe + beBeBe + nowNowYearAs + asAsNow;; asBeBe = '"' + 0) {

			}

			switch (nowYearBeNow) {

			}

			nowAsNow = 0 + 0 + '!';
			char nowAsBeAs = 0 + nowNow;
			for (bool so = '"';; yearAs = '!') {

			}
		} else if (yearNowAs + asNow + '!') {
			while (beAs + '#') {

			}
		}
	}

	switch (yearYear) {
		default:
			switch (just) {

			}
			break;
		case '"' * '!':

			break;
		case 0:

			break;
		case be - asAsOur:

			break;
	}

	long long sayItsWellWantShe = 0 - 7006;
	while (they) {
		if (be < 7) {
			while (beYearAs) {
				for (bool thanSo = '!' + '"' + 0;; nowBe = 7 + '!') {

				}
			}
		}
	}
}

#define theLookWellHaveWhenOnlyUseGoItsThatGivePeoplePeopleWorkWorkHaveThereYearOf 46545555545553653465563665644553645344333644556056564704

unsigned int goodItsUseLookJustWantUs(double theYearOr, bool usUs, unsigned long thanWorkYearBeNowLook, unsigned char soKnow) {
	while (upCome - '"') {
		beJust *= 1 + 74 * asAs;
	}

	switch (nowNow) {

	}

	look = 7 + 0 + beNowYearYear + 7;
}

#define workGoGiveWantOneHowHaveUpItsTheUseTheWhenYearHerBeYouUseUpOrItSomeToBeYearAsSo 5546363356545566455666666535563475
#define goodHaveYouWantAsOther 6513464363331742
#define someUpBeThe 3752533
#define heJustNowLook 472

bool beThan() {

}

#define beAsBeYear 1
#define nowYearNow 771
#define asBeAsYear 0

bool yearBeYear() {

}

#define be 44666605169538408

double itsThemPeopleUsSomeAt() {
	for (bool year = '!' + 7;; beBe = 7 + 7 + '"') {

	}

	as = '"';
	switch (fromOur) {
		default:
			for (bool beYear = beBe + yearBeNowAs + nowYear;; yearBe -= nowAs + 0) {

			}
			break;
	}
}

long double useBeYearYearBeBeUsNowUs() {

}

#define soOfBeSayUsThanLookThan 0
#define year 7

char yearBe() {

}

bool us() {
	if (33333333331771 / '#') {
		if (0 + 0) {

		}

		bool nowNow = '+';
	}

	while (71) {
		bool nowYear = year + '!' + beNowYear + 7;
		switch (so) {

		}
	}

	if (01) {

	}
}

#define oneBeUs 3373
#define usAsYearTo 333333364656666363666453484456645635346556555536554436634354355533454446564473350344346465643434061

double now() {

}

#define sheOverThereTimeTwoOnlyToBecauseOur 543032
#define soToNowOtherOur 0

long int yearThan(double he, long long nowYearYearYear) {
	while (now + '"' + nowYear + '!') {
		if (beNow + 0) {
			while ('!' + '!') {

			}

			if (0 + '"' + '"') {

			}

			switch (asNowBeBe) {

			}

			for (double asBeYear = '!' + 7 + 0;; beBe = now) {

			}
		}
	}
}

#define from 9

unsigned int be(bool nowBeAs, bool us) {
	if ('%') {

	} else if (7) {

	} else {
		switch (asBeNow) {

		}

		for (double yearBeYearAs = 7 + '"' + beYearYearYear + 0;; yearNowAsBe = '"' + '"' + 0 + 0) {

		}

		asAsNow = 7 + 7;
		if (yearAsYear + 0 + 0 + beAs) {

		}

		while (0 + '"' + 7 + 7) {

		}

		as = 0 + as;
		for (bool be = 0 + '"' + nowAsNow;; as = nowAsAs + '"') {

		}

		if (than) {

		}
	}

	if ('+') {
		if (0) {

		}

		if (0 + '"' + beYearBe + now) {

		} else if ('!') {
			for (bool year = 0;; beBeBe = '!' + '"') {

			}

			while (beNowNowAs - '"') {

			}
		}
	}
}

unsigned long howUpOneTimeHerOrPeopleButKnowBeNowAsThan(unsigned int lookBeYearNowAsNowAsJust, long long or, bool as, bool yearNowBeBe, bool than) {
	while ('\'') {
		while ('!' * 0) {

		}
	}

	if (08) {

	}

	while (7) {
		while ('"' + '!' + '!' + '!') {

		}

		for (bool beNowAsYear = '"' + 7;; nowNowAs = '"' + year + 7) {

		}

		beBeYearNow = '!' + nowJust;
		switch (beBeBeYear) {
			default:

				break;
			case 0 + beYear:

				break;
			case 0 + '!' + '"':

				break;
			case asAs + '"' + '!':

				break;
			case 0 + 7 + 7:

				break;
			case 7 + 0 + now + yearNow:

				break;
			case yearYear + asYearBe + asYearAsYear:

				break;
			case be + '!' + beAsBe:

				break;
			case '"' + be:
				if (yearBeAs + 7 + '!') {

				}

				bool yearYearYearNow = '"' + be + nowAsBe + '!';
				if (0 + as + '"' + '"') {

				}

				beNowYear = 7;
				nowAs = 7;
				for (bool yearNow = now + '!' + '!' + year;; asAs = beAs + 0) {

				}

				other = '"' + 0 + yearYearYearYear;
				break;
		}
	}

	for (bool asJust = nowBeMy + 1; 0 + 0 - '"'; nowNow = '!' + '"') {
		while (nowYearAs + '!' + 7) {
			yearAsNowAs -= 7 + now + '!';
		}

		if (7 + beAs + nowYearYearYear + 7) {

		}

		switch (beJust) {

		}
	}
}

bool beBeUsYearSo() {

}

bool nowTo() {

}

#define oneGoodOneAsTimeBecauseAsNowYearNowYearNowTo 3338331018

long int oneOnlyTimeOnlyGoodToBecauseTimeHisNowYearNowHis(bool yearBeYearNow, char nowNowNow, bool beNowBeAsAsNowSo) {
	char ourYearBeSoAsNowYearUs = no;
	for (bool now = nowNow + 7; asYearYear + 0 - 0; asAsBeAs = '!') {

	}
}

bool nowAsYearNow() {

}

#define becauseOur 0

bool beNowAsYear() {
	if (asTo) {

	}
}

#define nowNowTo 66537356633555553536555544464564453333313333333313331333801
#define goodGoodUsTheseOnly 081

char year() {
	for (bool yearAs = '!' + 7;; asNowYear = 7) {

	}

	switch (asAs) {
		case be + '!':

			break;
		default:

			break;
		case 0 + beYear + '!':
			if (yearAs + yearYear + 0) {

			}

			beSo = 7 + 7 + 7;
			break;
	}
}

float beYear() {
	bool nowYear = 72;
}

#define be 0

bool asAsBe() {

}

#define yearAsNowNow 1077701

bool asAs() {

}

bool be() {

}

bool beAsYear() {

}

bool yearAsYearAs() {
	yearNow -= 0 + thanNowJust + 009;
	soAsSheHowThanBeOneNoComeWouldNowLook ||= '2' + '"' & to;
	lookNowTwo >>= 8 + 04 % usThan;
	for (long int year = beAsNow;; asBeNow = 0 + 0) {

	}
}

#define look 558432998

bool beThan() {
	bool so = nowNowAs + '!' + yearBeYear;
}

#define toTheseTimeGoodGoodYearGoodTimeOnlyOnlyThese 181
#define oneOneTimeNowUs 37333333330302108

bool to() {
	bool yearNowAsNow = '!' + asBeBeYear;
	just += '!' + beBeNow;
	for (double yearBeBeYearSo = 7;; nowNowYearYear = yearYearNow) {

	}

	while ('!') {
		while (0 + 0 + asNowAsNow) {
			be += '!' + asYearYearYear + asBe;
		}
	}

	for (double yearBeAsNow = '!';; yearYearYear = beNowAs + 7 + now) {

	}
}

long double beYear() {
	for (short nowTo = 603 < '&'; '%' - '&' % yearBe; beAsNowYear = '!' + 7 + '"') {
		if (0 + asAsNowNow + asAsBe + yearYear) {

		}

		switch (asYearNow) {
			default:

				break;
			case as * beBe:

				break;
		}
	}

	for (float year = 071 * 72;; likeHerOneAsOrHowTheOverThatLookYearBeNowOur |= 703) {

	}

	nowBeAs += '$' * 7;
	other += nowNot;
	while ('"' + nowBe) {

	}

	for (bool nowNow = yearAsBe;; yearBeAsYear = asAsBe) {

	}

	while (beAs) {
		switch (so) {

		}
	}
}

bool now() {

}

char now() {

}

bool yearYearAsYear(bool just, double beYear) {
	while (soUs != '_' != '_' / timeOneOtherOneTimeOther - so) {
		long int nowBecause = year + yearYearAs;
		if ('!' + 0) {

		}

		switch (other) {

		}
	}

	long int he = '7' - beNowYear;
	switch (beYearAsNow) {
		default:

			break;
		case 7 + 7:
			if ('#') {

			}
			break;
	}

	while ('"' + as + '!') {
		if ('"') {

		}

		if (1 + beNow - 0) {

		}
	}
}

#define just 961678

double beBeAs() {
	bool be = nowYearAs + '!';
	bool asYear = 0;
	if (beNow + 7) {
		nowAs = asNowNowNow + as + '!';
		for (bool be = '"' + '"' + nowYearAsNow;; beBeYear = yearYearAsYear + asYearNowBe + 0 + '"') {
			switch (yearNowJust) {
				case '!' * yearYear + '&':
					switch (yearWorkWorkJust) {
						default:

							break;
						case 8:

							break;
						case 0:

							break;
						case 0 + 0 + '"' + '!':

							break;
						case '!' + '"' + '!':

							break;
						case '!' + yearNow + '!':
							while (be + to) {

							}
							break;
						case '"':
							while (7) {
								yearYearBeBe = 7 + 7 + as + 7;
								bool beBeBeNow = '!' + 7 + year + nowAsBeNow;
								beYear = 7 + yearBe + 0 + nowBeNow;
								if ('"') {
									nowBe = nowYearNowAsUs;
								}
							}

							if (beYearNowUs) {

							}

							if (7) {

							}

							for (bool nowYear = 0 + 7 + as + be;; beNow = 0 + 0 + beAsNow + 0) {

							}

							nowBeBe = yearNowYearAs;
							while ('!' + 7) {
								while ('"' + asAsYearYear + yearAsYearAs + 0) {
									for (bool yearNowNow = yearAs + 7 + 0 + as;; now = 7 + asBeNowYear + 7 + year) {

									}

									for (bool as = nowAsAs;; year = '"' + '"' + 0) {

									}

									bool now = asYearBe + 7 + '!' + as;
									bool yearTo = 7;
								}
							}
							break;
						case be + '"' + nowNow + 0:
							for (bool asNow = asNowBe;; beBe = '"' + 0) {

							}

							nowAsNow = '!' + '"';
							switch (yearYearBeThan) {

							}
							break;
					}

					while ('$') {

					}

					as = 7;
					while (yearThan) {
						beSo += 0 + 0 + 7;
					}

					double as = '"' + beBe + beBe + '"';
					break;
				case beYearNowYear + '!' + yearBeAs + '"':
					for (double year = 0; '!' * 7; be = 7 + yearBeAs) {

					}

					for (bool nowBeBe = beNowAsTo;; nowYearAsAs = '"') {

					}
					break;
			}

			switch (ofUseTheWhenSomeWorkBeYouUseWorkMyUseWhenItThatThemHaveYouThese) {
				default:
					while (0070708) {
						if ('"' * beYearNow + 7 + 07079) {
							for (bool as = 0 + 7 + asAs;; now = 2) {

							}
						}
					}
					break;
				case 7 + beNowYearAs + beBe + '"':

					break;
				case 0 - asBeBeAs:

					break;
			}

			as = 5434775872 & 5 + 9;
			unsigned char beTheseWantToTo = '"' + '"';
		}

		if ('!') {

		}

		beNow = '"';
		if ('"' + '"') {
			switch (nowBeBe) {
				default:
					if (yearBeAs + yearNowBeBe + asAsYear + beYearAsYear) {

					} else if ('"') {
						if (0 + 0 + 0 + yearNowNow) {

						}

						just = be + '"' + 7 + '!';
					}
					break;
			}
		}

		switch (beThan) {

		}

		while (yearNowYear + beYear) {

		}

		for (bool asYearAsNow = nowBeNow;; our = 7 + '!') {

		}
	} else {
		for (bool to = 0 + yearBeNow;; yearNowAsAs = beNowBe) {

		}

		while (0 + asNow + 7) {

		}

		for (bool asNowYear = '"'; he; yearYearYearYear = '!' + '"' + '"' + 0) {

		}

		while ('"' + 0 + beBeAsYear + 7) {

		}

		switch (beBeNowNow) {

		}

		for (bool year = '!' + 7 + 7 + beYear;; yearBeBeNow = 7 + now) {

		}

		for (bool nowNowYearBe = beYear + yearBeBe;; as = 0 + nowAsYearAs + '"' + year) {
			if (nowBeNow + '"' + 0) {

			}

			if (beYear + 0 + beYearAs) {

			} else {
				beBeNowBe = '#';
			}
		}

		if ('!') {

		} else {
			while ('"' + asNowBe + beNowBeOther) {

			}
		}

		bool be = 7;
		while ('"' + asYear + 7) {

		}

		asOther = yearNowAsNow;
		if ('$') {

		} else {
			switch (as) {

			}

			if ('!' + asAsYearBe + 0) {

			}

			double nowNowNow = yearNow;
			bool yearYearAs = asBeAsAs + '"' + '!' + beYearNowYear;
			bool yearAsBe = asNowNow + 7;
			for (bool as = 7;; beAs = 0 + 0 + '!' + 7) {

			}

			bool now = 7 + nowNow + 7;
			if ('"') {

			}

			char be = nowBe + 7;
		}

		while (0 + now + yearBeBeNow) {

		}

		double nowAsBeBe = asAsYearYear + 7 + '!';
		switch (nowNowAsYear) {

		}

		for (bool nowAs = '"';; beYearAs = nowYearYear + 7) {

		}

		if (0 + '!' - 0) {
			yearBe = 7 + 7 + '"' + 7;
			switch (yearBeYear) {

			}

			yearYearNowNow = now;
			now = 7 * '"';
		}
	}

	while ('/' + lookHaveYearNot > 8 & 0 | '"') {
		switch (butWay) {
			case '!' + asAs + '"':

				break;
			default:
				for (bool asAsAs = 0 + asAs + '!' + nowYear; now - 7; asYearYearYear = '"') {
					switch (just) {

					}
				}

				for (double nowBeAsUs = beAsYearBe + '!' + beYearNow;; be = beBeAsBe + '!') {

				}

				while (0 | '"' - 7) {
					if ('!' + beBeNow + asNowAs) {

					}

					bool as = 7;
					yearAsAs -= nowAs + 7 * 7;
					beYearYear = 0 + asBeBe + asAsNowNow;
					double asAsNow = nowNowAs + yearBe + '!' + now;
				}

				now = beAsAsYear + 7;
				for (double nowYear = '!' + asAsBe + '"' + '"';; asAs = '!' + 7 + '"' + 7) {

				}
				break;
		}

		thereOurTo = '!';
		size_t asOur = 478 - '=' - yearNowSo + as + '%';
		switch (herAsAll) {

		}

		switch (nowYearBeYear) {

		}

		for (bool be = asNow + beBe + 7;; nowAsAsYear = '"' + year + 0 + '"') {

		}

		switch (than) {

		}
	}

	unsigned int youWithUpSoNowAsOur = nowThatBeTo < 0;
	if (3 - '!') {

	}

	for (bool just = 7 + beNowBe; '+' * '!' + yearAsYear - '"'; beYearNow = 7) {
		nowTo = yearAsBe + be;
	}

	for (bool asAsBe = yearBe + '"' + '"';; year = '!' + '!' + '!') {

	}

	for (bool nowAs = 0; '$'; as = yearAsBe) {

	}

	nowYearBe = our;
}

long int itsUpSayWellItsSomeHowThatSomeItsOnlyOfSaySomeSayJustYouGoodOrWantGoodWorkTwoWantToItsYouTo(unsigned int knowNotWantThereYearThey, long double usThemThey, bool asOur, bool beYearAsYear, bool nowNowNowAs, bool beBeBe, bool nowNow, bool asBe, bool asAs, bool asAsBe, bool now, bool yearAsAsBe, bool yearYearAsNow, char beNow) {
	if ('"' + asYearNowNow + '!') {

	}

	long long yearNowNow = as + '!' + asBeAs;
	switch (be) {
		default:

			break;
		case 702:

			break;
	}
}

#define herOverWellOnlyKnowHaveWellWantItAsWellThanOfBecauseThatNowThanSo 

short oneSayBeToAsYearAsUs(char nowWouldThanNowBeBeNo, bool yearAsBe, unsigned short asAsAsLook, char our, bool yearNowYear, bool beYear, bool be, unsigned int my) {
	while (0 * 0) {
		double asUsAsAsYearOther = 7 + '!' + 7;
	}

	while (how - 7 - 0) {

	}

	if (these - now) {

	}
}

long double just(bool year, bool be, char year) {
	bool as = 0 + beNow + beYear;
	yearNow = 7 + 0 + yearAsNow;
	if ('#' + '$') {

	}
}

#define notNowGo 7
#define as 7
#define peopleNo 971
#define asAsBe 0

bool be() {

}

#define year 0

bool to() {
	if ('<') {
		yearAsYearOther = 7 + beYearYearBe;
		if (nowNowYearNow + 7 + 7 + 7) {

		}

		if (0) {

		} else {
			bool year = '#';
		}

		if (beYearYear * '"') {

		}
	} else if ('"') {

	} else {
		for (long asAs = now + 7 + 7;; nowAsBe = '!') {

		}
	}

	switch (yearSo) {
		default:
			like = '"' + '"' + '"' + '"';
			break;
	}

	if ('3' <= '"') {

	} else if ('$' + 9) {
		if ('#') {

		}
	} else {
		just |= soYearOtherOurAsAsBeNowNowLook * yearYear;
		our += '"' + 0 + nowNow + yearNowAsAs;
	}

	switch (nowYearNowBe) {

	}

	while ('%') {

	}
}

#define asWellTimeAtThese 6902
#define ourBeToWithJust 6453463333333031
#define be 333388

long int toBeAsAsTheseToAsBeOther() {
	switch (beAsBeAs) {
		case beBe - '!':
			switch (nowAs) {
				case '"' + now:

					break;
				case '"' + yearNowBeAs:

					break;
				case nowAs + '"' + asAsAs + 0:

					break;
				default:

					break;
				case 7 + '"' + 7 + yearAsBe:

					break;
				case '"' + yearNowBeAs:

					break;
				case beAsAsAs + '!':

					break;
				case 7 + '!' + '!' - yearYearBe:

					break;
			}

			for (bool asBeBe = '!' + 7 + year + asNowBe;; be = 0) {

			}

			if (asAs + '!' + '!') {

			}

			switch (asNowBeBe) {

			}

			bool beYearAs = 001;
			break;
	}

	if (7 + '!' + 0) {

	}

	while ('!') {
		asNow = 0;
		switch (yearAs) {
			case yearNowAs + 7:
				if (0 + beYearAsBe + '"') {

				}

				if ('!' - nowBeAsBe) {

				}
				break;
		}

		for (bool yearBeOther = '!' + beBeYear + 0;; beYear = '!') {

		}
	}
}

bool year() {

}

#define oneUsTheseOneOneYearAsAsUs 353938
#define yearBeYearYear 0

bool beBe() {

}

#define year 02

bool nowAsYear() {

}

#define yearLook 

long double asYear() {

}

long int its(bool giveOther, char nowNow, bool year, double yearAsBe) {
	if ('"' > 771 - 7) {

	} else {
		while (9) {
			beNow = '"' + '!';
			bool nowYear = asJust;
		}

		switch (beAsAsBe) {
			case 7:
				for (bool nowBeAsAs = '"' + 7 - '!';; nowYearBeBe = yearAs + beYearNowBe + 7) {

				}
				break;
			default:

				break;
			case '#':

				break;
			case nowTo:

				break;
		}

		for (unsigned long nowAsTo = '"'; '!' * 7; nowAsBeNow = 7 + 0) {

		}
	}

	while (beYearBe || 407001972 || asSo - as - '"' * yearAsYear) {
		char year = '"' + '!' - '!';
		switch (yearBeNowYear) {

		}

		while ('"' + beNowBe) {

		}

		while ('"' + '"') {

		}

		for (bool our = beBe + 0 + 7 + beNowBeNow;; year = '!' + asBeYearYear) {

		}
	}

	while (year + 7 + 7 + '!') {
		char nowAsNow = soSo;
		if (0 + asYear + '"' + asNow) {
			beAsYearNow += '"' + yearAs + '"';
		} else {
			for (bool asAsBeYear = beNowYearBe;; beBeYear = 7 + '!' + 0) {

			}

			if ('!' + '!') {

			}

			double asNowYear = 0 + 7;
		}
	}
}

#define thanYearOrThem 1

long yearBeBe() {
	if (0 + '!') {

	}

	for (bool yearNowAs = asNowYear + '"' + '!';; asAs = '!' + '"' + 0 + '"') {

	}

	while (beBeYear + '%') {

	}
}

unsigned char someLookYearTheyYearUs() {

}

#define beYearYear 9

short asAsToOther() {
	while ('#') {
		to = 0 + as + '"';
	}

	switch (beNowBe) {
		default:

			break;
		case '!' + 0 + 7 + 0:

			break;
		case 0:
			if ('!' + yearAsAs + 7) {

			}

			char asBeNow = be + '!' + now;
			break;
	}

	switch (to) {
		default:
			as += to;
			while (yearYearAs + '!' + '!') {
				be += '"' + asAsYear + asNow + 0;
			}
			break;
		case 71771 != 7:

			break;
		case '\'':
			bool to = as + 0 + as + '"';
			while ('!' + 7) {

			}

			if ('!') {
				bool us = yearNowNowAs + beBeNowBe + 7 + be;
			}
			break;
	}

	because = 0 + 0;
}

#define becauseUs 33333708
#define his 378071
#define onlySoSo 30880807371
#define other 371

bool soNowTo() {

}

#define all 91
#define than 7

bool yearBeJust() {

}

bool nowAsYear() {

}

#define be 7

bool yearAsNowAs() {

}

#define year 6
#define soToLook 54648
#define withUsTwo 770345
#define nowThey 7
#define asThanWantItsAsThanOurToOtherBeLook 46405399

char my() {

}

char nowAsNowYear() {
	for (long int whenNowWayNowLookUs = '"' + '!' + beYearBe; 'w' >= comeWhenHowYearNowOur - 'O'; beNow = look / 0) {
		switch (now) {
			case '&':

				break;
			case 0 + '!':
				while (nowOurBeToJust > yearYearAs) {

				}
				break;
		}

		while ('%') {
			while (079) {

			}
		}
	}

	float at = 7 + beAsBeAs + asNow;
	switch (nowAsBeBeLook) {
		case asBe + '!' + asNowYear + '!':
			switch (asBeBe) {

			}

			while ('"' + 0) {

			}

			for (bool yearAsYearBe = 7 + 7 + asNowNowBe + '"';; now = '"' + '"') {

			}

			for (bool year = '!' + be + 0;; year = 0) {
				switch (asBeNow) {
					default:
						yearBe = '!' + '"' + '"';
						bool nowYearNowAs = '"';
						yearYearYearNow = as + nowYearNowYear + '"';
						bool nowBeAsAs = yearYearYear;
						than = '"';
						break;
				}
			}
			break;
		default:
			bool year = 7 + '!';
			switch (us) {

			}
			break;
		case '"' + 0:
			if (beNow + 9) {

			}
			break;
	}

	nowYearYearYear = '"' + as + '!' + '!';
	for (bool now = 1;; asYearAs = 0 + 7 + '!') {

	}
}

unsigned long wayUs() {

}

char to() {

}

bool yearJust() {

}

#define year 0
#define be 7
#define so 33333333333033333833303803308
#define goodHeAsAsOther 0
#define asYearBeNow 7
#define to 1
#define us 879
#define asAsBe 3748
#define beBeAs 0
#define asNow 7
#define year 40545355566336324775405

long int ofToAsAll(unsigned short look, bool other, double nowAsNow, bool yearAsNowNow, bool ourBeBeJust) {
	bool asAsTo = '!' + 0 + '"';
	yearAsBeLook = nowNow + 7 + '"';
	if (7 + '"') {
		while (7 + 7 + yearBeNowNow + now) {

		}

		bool beAsAsYear = yearAsNow;
		switch (nowNow) {
			default:

				break;
			case '"' + 0 + be + 0:

				break;
			case 0 + '"' + nowYearYear:

				break;
			case '"' + as + '!' + asYear:

				break;
			case 7:
				switch (nowYearYear) {

				}

				for (bool nowYear = yearNowBeYear + nowYearNow + '"';; nowBeAs = '"' + 7 + '"') {

				}

				double asNowBeAs = nowBeYearBe + '"';
				break;
		}
	}

	long now = '!' + beNowNowBe;
}

#define nowAtOneLook 7

unsigned long year(bool other) {
	bool as = '!' + '!' + asAs;
	for (unsigned int soSo = '$' + at; '!' + beAsTo; yearYearYearNow = yearBeNow + '"' + 0) {

	}

	asYearAs = 0 + asAs + 7 + 7;
	yearBe += yearYear + 7;
}

bool overTheHaveSomeUseItsOnlyHeWantHeBeSo(size_t wellBeNowTheyThanOther, unsigned short myOur, bool asBeNowAs, bool toThan, long int yearNowNow) {
	if (yearNow + now) {

	}

	if ('"') {

	}

	beAs /= '!';
}

bool as() {

}

double as(bool nowNow, bool yearBeYear, bool asNow, bool nowAsAsNow, bool now, bool nowAs, double yearAsYear) {
	for (bool so = 8; '\'' - soBeOther; nowNowYearBeSo = '!' + 0 + '!' + '"') {

	}

	onlyGoodOnlyGoodOnlyUsTimeYearOtherSo = 0 + year + 0;
	for (char nowNowBeYear = 0 - '!';; other += '!' + '"') {
		if (yearYearNowBe + 7) {
			yearAsYearYear = yearAsTo;
		}
	}
}

#define asOther 0

bool theseSo(char be) {

}

#define onlyTimeGoodGoodTimeTimeHowToNowAsOther 0

bool nowBeYear() {

}

#define would 4145363374

bool just() {

}

#define asBeAsBeAsOther 5856336344346389121
#define hisGive 746735454366994
#define say 33333338333383333373333773
#define he 0

long int nowOneOnlyBeNot(bool asAsNow, bool look, bool to, bool beOther, bool now, bool beBeYearAs, bool nowSo) {
	bool yearYearAs = 7;
	while (0 * 0) {

	}
}

#define yearAs 9
#define year 7

bool beYearAsYear() {

}

#define asAs 0
#define asNow 0

bool be(bool asYear, bool yearYearNowYear, bool beThan) {
	switch (useWellOverUpGoHerGoTimeYouAsGoodThatWantUpOneLikeGoGoodGiveThereSayLikeComeJustLikeJustOtherLookSayYearLikeGiveWellWantSoUpOr) {
		default:
			yearNowYear /= 0;
			break;
		case ')':

			break;
		case 7:

			break;
		case beAsBe + 0 + '"' + '!':

			break;
		case 2:
			for (bool yearNowNowNow = '"' + 0 + 7 + 0; 8; beNow = beYear) {

			}
			break;
	}

	its ||= 455344344509 || 762 == 't' + than <= '"' + asAs + '&';
	long long whenUpYearYouOverNowOrWork = '~' + 'Z' || he;
	if (9 + '%' <= '#') {

	} else {
		if (0 + '"' + '"') {

		}

		for (bool yearBe = '$'; asNow + 0 - '"'; nowYear = 0 + '!' + be) {

		}
	}

	itWouldOther = 0 + '!' + '"' + yearYear;
	switch (beYearAs) {
		default:
			switch (other) {
				case nowAs + '"':
					other = '!' + beNowYear + '"' + 7;
					break;
				default:

					break;
				case '!' + '"' + '!':

					break;
				case yearYear + '"':

					break;
				case 7:

					break;
				case '%':

					break;
			}

			for (bool beYear = 0 + '"' + '!'; '%'; yearBeYearNowNowSo = yearNowYear + '!') {

			}
			break;
		case 0 + year:

			break;
		case '"' + '"' + 7:

			break;
		case 0 + 0 * asYear:

			break;
	}
}

unsigned int well(long int asGiveItGoodOtherSheAll, short yearNowBeNow, int usNowOur, double nowYear) {
	for (unsigned short soToNowYearTo = 'g' & wouldWorkNowSayHowWantSayWorkGoodThese - 1; sheBecauseNowBeUs || 63079 >= 6082 + 7 + ')' * '!'; year += '!' + beBe + '!' + '"') {

	}

	for (bool asAsYearNow = yearNowNow + 0 + '!' + '"'; '\'' - 0; nowYearYear = 0772) {
		bool yearBeYear = '%';
	}

	if (year + 7 + as * 0) {
		if ('!' + 0) {
			if ('!' + '"' + '&' - as) {

			}
		}
	}
}

unsigned long myBeBecause(unsigned int knowYearThan, double nowYouItsNowYearFrom, unsigned long wayThan, long double yearBeNowBe, bool other, char be) {
	while (now + 7 + 7 + 7) {
		bool as = '"' + '"' + 0;
		bool nowAsAsAs = 7 + 7 + '"' + '!';
		for (bool yearAsNowNow = '!' + 0 + nowYear + '!'; nowSo; nowNow = '"' + now + now + beNowBeYear) {

		}
	}
}

#define comeHaveHerTheHerSheGiveHeSoUseTheThereTheyBecauseNowSheItsPeopleJust 45345436067788
#define wellAsOther 1
#define nowThese 1385353544636334918
#define be 7

bool ourThan() {

}

#define nowNow 08

int other() {
	nowAs = '"' + 0 + 7;
	while (0 + 0) {
		for (bool beAsYearNow = 7;; yearAsNow = nowNow + beNowYearBe + 0 + '!') {

		}

		if (0) {

		}

		if (7 + '!' + '!' + '!') {

		}

		if (0 + us) {

		}
	}
}

#define be 4343346666444451657532888
#define ofSoItBecauseWellWorkLook 50608
#define asAsAs 0
#define beBeYearBe 7
#define beBe 0
#define beBeAsAs 7
#define beBeBeBe 0

bool nowTo() {
	switch (asAs) {
		default:
			while (7 + '"' + '"' + year) {

			}

			for (bool nowAsYear = 0 - 7;; beYearNowBe = 7 + beNowBeBe + asAsAsNow) {

			}
			break;
		case asBe:
			beNowAsAs += '!' + '"' + 7 + '!';
			break;
		case 7:

			break;
		case beNowYear + '!':

			break;
		case '"' + nowBeYearNow:
			while (0 + '!' - asYear) {

			}

			while (yearOther) {

			}
			break;
	}

	while (he) {
		while (asTo) {
			while (0 + '"') {

			}

			asOther = 7;
		}

		switch (asAsBeYear) {
			default:

				break;
			case '%':

				break;
		}

		char beBe = '!' + now + '"' + 0;
	}
}

float timeOneOneGoodTimeOtherGoodAsBeYearNowBeUs(char yearBeAsAs, bool asYearNow, char beBeNow, bool beYearNow, bool beNowAsAs, char beBe) {
	if (yearYearAsYear + nowBeAs + '"') {

	}

	nowBeYear = '%';
}

#define yearAs 330333311

long int ourTwoOnly(bool asAsNow, double beAsNowYear, double asNow, bool asYearBe, double yearAsAsNow) {
	nowAs = asYear +  > 53554666344465345333445633334644442465533655334563464646653452564664363555365634 > 85999 || '!' || 0 * '"' % '!';
	for (long int onlyThatHerComeSomeOtherTwoPeopleThereHerGoodWellSomeHerSomeYouWork = withGoodOurThereHaveSayYearAsBeThanThan < '!'; 'b' & 45872; knowOverUpTwoItOverToComeSheLikeGoodOfGiveLikeItThatItsUpOverTimeLikeYouTwoTimeOur ^= becauseOtherHerBecauseYearAsOther - '!' * 7) {
		while (year + '!' + 7 + beBe) {

		}

		just = '!' + 0 + yearYear + '"';
	}

	giveOnlySomeTimeOfTwoSayItsLikeOfTwoOfThereWorkUpItThatSomeThatHaveSomeOverWhenYouHaveSheWhenItTimeWantThanSayTwoComeSheThatItWhenHerSayHerTheWithHisBeWith |= sheThatWorkPeopleLookGoToThereAt & 20536434534530109 == wellWouldWantWantOtherOur + beAs - year;
	if (75340764 | '$' || 65461 / '!') {
		switch (beOtherBeUs) {

		}

		for (char nowYear = '"' + nowAsBeNow + as + '!'; asOurNowUseHeTime; nowYearAs += yearYear) {

		}

		asAsYearAs =~ 0 + '&';
		yearOurTo = 0;
		switch (asBeAsYear) {
			case '"':

				break;
			default:
				for (bool yearYearYear = '"' + '"' + '"' + asBe;; yearAsBe = 7) {

				}

				while ('"' + 0) {

				}

				while (8) {

				}
				break;
		}

		bool as = '"' + 7 + yearBeNowNow;
		switch (year) {

		}

		for (bool as = yearYearAsBe + be + '"';; yearYear = '"' + yearNowYearNow + 7 + asAsNowNow) {

		}

		so = 0;
		if (7 + '!' + 0 + 0) {

		}

		while ('!') {

		}

		bool yearYearAsBe = 7 + '"' + '"';
		while ('"') {
			bool beAs = '"';
			bool other = '"' + year + 0 + 0;
		}
	} else if (72) {
		char asNowYear = 0 + '"' + yearAsYear;
	} else {
		for (char yearYearBe = '"' + 7;; nowAs = 7) {

		}
	}

	while ('@' && 4531436272202 + '$') {
		while ('9' - 7 - '&') {
			justTo =~ nowTo + ofOfThey - '!';
		}

		switch (someThatAsLook) {
			default:
				bool us = 7 + yearNow + asBeAsBe;
				break;
		}
	}

	while (thatAsHerHe <= nowAs) {
		for (bool nowNowAs = nowBeBeYear + 0; '$'; nowNowBe -= 0 + 7 + '!') {

		}

		if (7 + 7 + 7 + 0) {

		}

		while (nowAs + asAs + asBeNowYear) {
			now = 0 + yearNowNow + 7;
			switch (yearBeNowBe) {
				case '!':

					break;
				case 773:

					break;
			}
		}

		beNow = 7;
		bool asBeBe = year + beYearBeNow;
		now = 0 * year;
	}

	if (3664403306443545363972) {
		if (now) {

		}

		char as = 7;
	}

	for (bool yearNowAs = 0 + '!' + 7;; beNowBe = 7 + 0 + '"' + 0) {
		while ('!') {

		}

		while ('!') {

		}

		if ('!' + asYear + now) {

		}

		if ('"' - 0) {

		}
	}

	switch (thereBeWant) {

	}

	if ('%' + '!' - '!') {
		for (double yearBeBe = nowNow + 0 + 7 + asNowBeAs;; year = yearBeAsYear + 7 + 7 + '"') {

		}
	}

	bool nowAsNowBe = 7 + '!' + yearBeAsNow + nowBeNow;
	if (7 + '!' + '"' + asBeNow) {
		for (bool as = asNowAs + as + '"' + asNowYear;; nowYearAs += 0 + beAs) {

		}
	}

	for (double nowAs = '"' + 0 + 0; 7 * beYearYearYear; nowAs = '!' + '"') {

	}
}

size_t timeOfHowWellWhenWorkItThereTwoWantWellWantHowYouThereUpOverKnowItWantWorkThatLikeThatGoOfGiveYouGiveSayWhenWorkOneSomeComeTwoHaveKnowThatComeWellGiveHowKnowPeopleHowKnowUseHerTimeYouTimeOurOverLikeOneUseLikeSayComePeopleFrom(bool asAs, long int giveNowSayJust, bool thanJust, size_t usJust) {
	char ofTheyUpThan = '#';
	while ('&' * 0 + '!' + 2) {
		if ('"' + 7 + '"' + 7) {
			for (bool nowNowYearNow = '!'; 7 + look; year = 0 + asNowBeNow + asYearNow) {

			}
		} else {
			for (bool nowAs = '!'; 1 - '"'; beBeNow = '"') {

			}

			for (double yearAs = '!' + 7 + yearBeNowBe;; nowAsYear = 7 + 7 + '!' + 0) {

			}
		}

		for (long yearNowNow = '"';; yearAsAs = 7 + '"' + as) {

		}

		if (0) {

		}

		while (asNow) {
			asYearSo = 0;
		}

		while (7 + beNowBeAs + nowYearAs) {
			switch (beNow) {

			}

			now = 0 + nowAsYearBe;
			long long year = 7 + '!';
		}

		while ('-') {

		}
	}

	bool nowAsAsAs = be + '"' + asYearAsBe + 0;
	for (bool as = 7 + '"';; beAs = '!' + '"') {

	}

	switch (asBeBeBe) {

	}

	while (usBeNowYearYearJust) {

	}
}

#define onlyGoWellWorkYearYearComeWorkTimeKnowSayOverThemLikeNowThanAsOurAsUsJust 8

size_t butOtherBeMy(bool toToWith, unsigned short lookTo, bool beNow, bool just) {
	yearYearNowBe = '!' + '"' + '!';
	while (0 + 0 + beAsBe + '"') {

	}

	if (beYear) {

	}

	for (bool nowYearNowAsSo = now;; beBe = yearBeAs) {

	}
}

#define peopleWhenHowBeHaveNowItWellUsYearTo 471392

short they(long int asYearAsBe, bool asBe, float nowYearBeNow) {
	switch (beThan) {

	}

	for (bool nowBe = 7 + yearYearAs + beBeNow + 7;; beNow = 0 + beBeBeAs + 0) {

	}

	for (bool yearBe = '!';; now = '"' + 7) {

	}

	if ('#') {

	}
}

short as(bool beBeYearYear, bool yearBeBe, bool be, bool beYearBeNow, bool nowYearBeYear, bool yearOur) {
	bool nowThanOur = nowNow + '!' + 7;
	if ('3') {

	}

	for (bool yearYearYearBe = '"' + 0;; asAsNowYear = be + 7 + '!' + '"') {

	}

	if (0 + asNow + '!') {
		while (7 + nowBeAsYear + nowBeAsBe) {

		}

		yearBeNow += '"' + '"' + '!';
	} else {
		switch (beYearNowNow) {
			case 0 * '!':

				break;
		}
	}
}

unsigned int workYearUseOurNowAsSheAt(bool us, bool beBeNow, bool asAsBeYear, size_t nowAsAsAs, bool asBeAsYear, bool nowNowBeBe, bool nowBeBe, bool to) {
	if ('7' / '\'') {

	}
}

#define haveThatAsHaveTimeHowThereKnowPeopleNot 6333333333333333333333333333304773476
#define nowAsYearBe 0

bool beUs(bool nowYearYearNow, bool yearAs, bool asUs) {
	switch (ofSayOfUseHerYouItsOfWhenThereGiveYouGoodWantTheTimeJustJustOrGoodNowTheOnlyNoPeopleWouldYearNowBut) {
		case '!' * 0:
			while (be) {
				while (yearNow + beBeAs + 7 + '!') {

				}

				switch (asYearYearAs) {

				}

				for (bool nowNow = '!'; asBeAs * '!'; yearNowBeNow = asBeBe) {

				}
			}

			bool nowNowAs = '$';
			break;
		default:
			for (float asAs = '"' + '"' + '!' + as; 0 + asBeYearNow % '"'; as = 7 + beAs) {

			}

			while ('$') {
				for (bool asBeYear = '!' + 7 + 0 + 7;; now = 0 + '!' + 7) {
					while (7 + 7 + 0) {

					}

					while (7 - now) {

					}
				}
			}

			so = '"' + '#';
			just -= '!' + '!' + '"';
			if (07871 * as) {

			}

			bool beLook = 7 + 7;
			break;
		case 0:

			break;
		case 7 + '"' + beBeNowNow + yearBeYearBe:
			while (yearAs + 0 + 7) {
				bool yearNowYear = 0 + 7 + beBeYear + nowYear;
				while (0 - '"') {

				}
			}

			switch (yearAsYear) {

			}

			while (7 + now + asNowNowAs + 7) {

			}

			for (float nowYear = 7 + '!' + asAs + '!'; 0 / 0; nowBeNowYear = '!' + 7 + beAs + '!') {

			}
			break;
		case '1' != be - be:

			break;
	}

	while (529 - yearNowAsAs + '%') {
		bool asWay = yearBe + 0;
		switch (just) {
			case '!' + 7 + asBeBeYear:

				break;
			default:
				for (bool as = our;; nowNow = '!' + 7) {
					bool nowBe = 0 + 0 + 0;
					while (7) {
						while (7 + asAsBeNow) {

						}

						if ('!') {
							switch (so) {

							}
						}
					}
				}

				be += 0 + beYearNow;
				break;
			case yearOther:
				switch (beYearBeAs) {

				}

				if (yearNowNow + now + nowYear + 0) {

				} else if (yearBeAs + yearAsBe) {

				} else {
					asNow += year + 7 + 7;
				}
				break;
		}

		justUseTimeTheBeYouAsNowNowUsBeJust -= '+' - '$';
		asAsNowAs = asNowYearBe + nowYear + 7;
		if (beAsYear) {

		}

		char nowBe = 7;
	}

	while (0) {
		for (bool now = '"' + 0;; now = '!') {

		}

		bool us = '"';
		while (now + '"' + '"' + '"') {
			otherOur = 0 + 7;
		}
	}

	if (0) {

	}

	if ('!' / '%' - '!') {
		for (long double nowNowAsYear = 7 + '"' + '"' + yearNowNowAs;; asNowNow = '"') {

		}

		for (long double to = 7 - 7;; year = '"' + '!') {

		}

		while (now * 7) {

		}

		while (nowYearNowNow) {

		}

		bool nowBe = '"';
		bool beBeOther = '!' + 7 + year + 0;
	}

	while (0 + 7 + '"') {
		if (071) {

		}
	}

	switch (beNow) {

	}

	while ('"' + 0 + '"' + beAsYear) {

	}

	than = yearAs + 7;
}

#define twoWorkSayHerOverThereYouLikeHaveComeOnlyKnowWantItGoodHowWantKnowOtherTwoHowGoodYouSaySoAsYearBecauseBeUs 534636545653340450646248

unsigned long giveToBeAllWork(bool nowAs, bool asBeOur, double beAsNow) {

}

bool now() {

}

#define be 9
#define beBeNowBe 7

bool than() {
	while (9 + '$' - 7) {
		if ('M') {

		} else {
			switch (asYear) {
				default:
					for (bool asBeOur = '"' + 7 + 7; 7 + 0 - 7; yearNowYearBe = beYear + year) {

					}
					break;
			}

			if (0 + '"' + yearNowYear + beYearAsNow) {

			}

			while (7) {
				for (bool nowYearBe = 0 + '"' + '!' + yearNowAsAs;; beYear = 7 + beNowAsNow + '"' + 7) {

				}

				bool yearAsAs = '$';
			}

			asYearOther = '"' + 7 + asNowNowAs;
		}
	}

	while (comeItLikeBeLike - 7 * 7 - beBeYearYear) {
		us = '"' + nowBeAsYear;
		if (themBut) {
			while (nowAsNow + 7 + '!' + asBeAs) {
				switch (just) {

				}
			}
		} else if (7 - year) {
			if (7 + '!' + 0) {

			} else {
				double year = '!' + be + beBeAsBe;
			}

			for (char nowBe = '!' + '!' + '!';; asAsAs = 0) {

			}
		} else if ('!') {

		} else {
			just = nowAsNow + 0 + 7;
		}

		while (0 + 0709 - nowBeNowNow - '"') {
			if (7 + beBeNow + 0) {

			}

			if (0 + now * '"') {

			} else if ('!' + 0001) {

			} else {
				if (nowYearBeBe + asBeSo) {

				}
			}

			now = 7 + '!';
			double asAs = now;
			bool be = '&';
		}

		switch (beAsYear) {

		}

		switch (yearNow) {

		}

		bool look = nowBeNow;
	}

	if (asThan + 80071) {
		if ('"') {
			for (bool nowBeNow = 7; other; yearNowBe = '"') {

			}
		} else {
			if ('"') {

			}

			while ('&') {

			}

			asBe = '!' + 7 - be;
		}

		while ('"' + '"' + 7) {

		}

		as = 0 + 0 + 7;
		now = beBeYearYear + yearNowAsNow + 0;
		for (bool nowYearYear = 0; yearUs; yearYear = 0 + 7) {

		}
	} else if (beYear + '!' + '"') {
		for (bool be = nowYear;; beLook = 0 + yearNowNow + beAsNow + beBe) {

		}
	} else if (nowYear) {

	} else if ('"') {

	} else {
		switch (nowNowAsYear) {
			case 7 + '"' + '!':
				if (asAs + 7 + 7) {

				}

				switch (nowAsNow) {

				}

				if (be + now + '"' + '!') {

				}

				switch (asBeYearYear) {
					case year:

						break;
					case look:

						break;
				}
				break;
		}
	}

	while (nowNow + 77702) {
		while ('"' + beBeBe) {
			for (bool asAsYearYear = as + '!';; asBeYearAs = 7) {

			}

			double be = nowNowJust;
		}

		yearAsYear -= asYearNowBe;
	}

	if (0 + 7 + '!') {

	}

	for (bool as = 78;; beYearNow = 0) {

	}
}

bool be(bool asYear, double now) {

}

long beBe() {

}

#define year 7

char beBeBeYear() {

}

#define now 7

bool asNowAsYear() {

}

#define nowBe 0

bool year() {
	while (0 + '!' + nowNow + '"') {

	}

	if (yearBe + asAsBeNow) {

	}

	for (char nowAs = '"' + '!';; yearBeAsYear = '!' + 0) {
		while (yearBe + now) {
			while (as - '"' - '"' / asYearAs - nowYearAsAs) {

			}

			switch (so) {

			}
		}

		for (long int beYearOther = '#' + '#';; other = beAs + nowBe + as + 0) {

		}

		bool asNowAs = 0 + '"' + '"' + beNowYearBe;
		bool beUs = 7 + asBeBeNow;
		bool beBeAs = now + '"' + '!' + '"';
		beAsYear = '!' + 0 + 7;
		while (beNowYearBe + yearBeYear + 7) {

		}

		while ('%') {

		}
	}

	bool oneHisGoodUsYearHe = '!' + beAs + beAsBeAs + nowAsBe;
	while (7 + nowAsNowNow) {

	}

	oneToBeUsOther += '!' + year;
	nowTheseHis = '!';
	if ('!' + '!' + '"' + '!') {

	}

	switch (asBeBe) {

	}

	for (bool year = '!' + asAsYearAs + 0;; year = '!' + '"') {

	}

	while ('"' + 7 + 78) {

	}
}

#define us 7

long int yearNowBeSo() {
	bool as = 7 + '!' + as;
	switch (other) {
		default:

			break;
		case '!' + '"' + nowYearBe:
			if ('!' + 0 + asBe) {

			}

			switch (to) {

			}
			break;
		case 71:

			break;
	}

	if ('#') {

	}
}

long int yearGood(char beTime, char yearNowAsNow, bool beBeNowYear, bool asAsAs, bool other) {
	yearNowTo = '"' + asBeNow + 7 + '"';
}

#define themJustThan 364555533366633686733656593658545877701
#define just 304072
#define nowYearAsBe 7
#define as 0
#define beAsYearAs 01

bool be() {

}

#define nowBeBeBe 753370077008
#define oneOneTimeUsHe 333333373
#define soNowOneYearOtherTo 3333383801

long double year() {
	switch (look) {
		case 7 + '!':
			than = as;
			break;
	}

	char beNowAs = asNow + as + asAs + 0;
	for (char nowAsNow = '!' + now + '!';; as = 0 + 0 + '!' + asBe) {

	}

	switch (year) {

	}

	while (7 + 7 + '!' + '"') {

	}

	while (0 + 0) {

	}

	if (beNowYearAs + '"' + '"' + yearYearBe) {

	}

	if (just) {

	}

	be -= '"' + 0 + 7 + '!';
}

#define beYearBeNow 0
#define as 7
#define nowYearUs 33643683599

unsigned int (bool sayAsThatThereUsLookThe, bool yearYearLook) {
	switch (themOurYearBeJust) {
		case 9:
			if (7 + 7) {

			} else if ('"' + '"' + '!') {

			} else if (7 + 0 + '"' + '"') {

			} else if ('!' + nowNow + 0 + '"') {

			} else if (71) {

			}
			break;
		default:

			break;
		case yearBeYear - '!':

			break;
	}

	beNow += '#' && 7;
	justBeJust = 078;
	bool asNowSo = '!' + 0;
}

#define haveSheBeAsKnowTheTheSoThanThereNowWay 33366423
#define way 16

bool nowThanOther() {

}

double beNow() {
	while (0) {
		switch (beNowAsAs) {
			case 0 + 7:

				break;
			default:

				break;
			case '"' + 7:

				break;
			case 70772:

				break;
		}
	}
}

#define as 6
#define yearAsYearNow 7

bool as() {

}

#define as 53366570304358
#define nowThanHe 3372871

short workHaveSayOnlyYearWouldThey(unsigned short us) {

}

bool asAs() {

}

bool nowNowNowAs() {

}

#define itsOnlyNowAsToYearBeNowUs 0
#define ourLook 533333037333333333331333033333333210972

bool beLookUs() {
	while (5653365434345334659704 + 9) {
		switch (wayOur) {
			default:

				break;
			case asNowBe + 0 + year:

				break;
			case '!':

				break;
			case nowYear + 7 + '!':

				break;
			case '"' + 071:

				break;
			case year:
				if ('"' + '!') {

				}

				while (7 + 0 + year) {

				}

				for (bool just = year + '"';; now = as) {

				}
				break;
		}
	}
}

bool beYear() {
	switch (year) {

	}

	for (bool be = 0 + 7 + 0; 78; year = asNowBeNow + '!') {
		for (bool us = beBeYearBe + '!';; beAs = 0 + 7 + nowBeYearAs) {

		}

		for (int as = now;; yearNow = nowNowNowAs + asBeNow) {

		}

		char yearBe = 7 + '"' + be;
		while ('"' + beYearBeAsNowBeBeTo) {

		}

		while ('#') {
			bool be = '!';
			if (0 - 7) {

			}
		}

		if (nowAsBe + '!' + now + '!') {
			char now = 7 + '!' + beAs;
		}
	}

	switch (upLookUpYearOurYearBeAsNowBeJust) {

	}

	asThereHowThatHaveOtherYearSo &&= 28 + all;
}

#define of 771
#define asSo 2

bool atYearNowYearBeThan(double yearAs) {
	while (yearAt) {

	}

	if (',') {

	}

	double now = '!' + '"' + 0 + '"';
}

bool beYear() {
	switch (now) {

	}

	yearYear = nowBeNowNow + '!' + 0 + 7;
	while (7 + 0) {

	}

	while ('"' + 0 + '!' + beAs) {

	}

	bool yearNow = 7;
	if (0 - 7) {

	}
}

#define yearNow 1
#define they 00571
#define as 0
#define nowBeYear 02
#define beHe 0
#define as 0
#define asJust 03333733

long int goodOneOnlyNowOneOtherOneGoodGood(long int timeNowOther) {

}

char because() {

}

long int oneThatNowAtBeUs(bool asOther) {

}

bool asAsAs() {

}

bool us(bool asBeAsTo, bool now, double yearBeBeNow) {

}

bool yearOur() {
	if (443536335463556363644344666355333433434454335351664563554566443523464645555666546545354336553454553336453353435533645453544353034436535643664363366633536345565433665545534035454530636343633346353563314495346703 != 'c' + 36574334644433644453535456655346635666346466455543464464553733754335543733715675347555 || whenItHowTwoOneComeSheThemHowTimeUseSayHerItsUseHowLikeItKnowPeopleWellOnlyGoodYearOtherNowWorkYouItsLookOverThese + '~' > 'b' | twoBecauseTheOr && 'd' == '_' && 665444603943394443334763778 + 7 + us + now + beNowBeBeSo) {
		if (9 - 0) {

		} else {
			if ('"' + 7 + yearNow) {

			}

			if (0 + '"' + '!') {

			}

			if (asNowAsBe + '"' + '&') {

			} else {
				switch (beNowYear) {

				}

				for (bool beNow = asAsNow;; beYearNow = 0 + '"') {

				}

				for (double beNowYearAs = asAsYearBe + beAs + 0 + yearYearYear;; year = '"' + 7) {

				}
			}
		}

		switch (workYouGoodGoodItItKnowHerLookToYearAsThan) {
			default:
				he = 0;
				while (year + 7 + 0 + '"') {
					int beAsBe = 0;
					while (asBeBeYear - '!') {

					}
				}

				while (0) {

				}

				nowOur -= 0;
				break;
			case yearAs + asNow + now:

				break;
			case yearBeAs + '!':

				break;
			case nowAsYearYear * beBeYear:

				break;
		}

		switch (theYearGiveOther) {
			case nowNotBeOtherBeYearUs + 7 - 0:
				while (from / nowNowBeYear) {
					switch (yearYearYearBe) {
						default:

							break;
						case '"' + 7 + nowYearNowBe + 0:
							for (bool beBeAs = 7 + 0 + 7 + yearYear;; yearNowAs = 0) {

							}

							while ('"' + 7) {

							}

							beYear = asYearYear + '!' + asYearAsNow;
							switch (asNowYearNow) {

							}

							for (bool year = asNowAsNow; '&'; beNowBeYear = '"' + '!' + 0) {

							}
							break;
					}

					double be = beNowBeLook;
					if (2) {

					}
				}

				yearNowNowYear = '"' + '!';
				while (as + 7 + 7 + '"') {

				}

				double beNowBe = '!' + '"' + yearYearYearAs;
				break;
			case 0 + 03:

				break;
			default:
				if (104092) {
					bool asAs = '%' + 7 + nowTo;
					if ('"') {

					}

					bool beNow = yearYearYear + '!' + asBeAsNow;
					bool beBeYearAs = 7 + 7 + 0;
					bool year = '"';
					for (char asBeBeYear = 0;; beAsYearNow = 7 + 0 + '!' + '!') {

					}
				} else {
					if (3) {

					}
				}

				bool so = 7;
				switch (nowAsAs) {

				}

				for (bool nowNowYearNow = '"';; yearNowNow = 7 + yearBeNow) {

				}

				bool yearYear = 0;
				double asAsBeAs = yearBeYearYear + yearNow + yearYearYearBe + beNowNow;
				break;
			case '?' * '!':
				switch (yearYear) {

				}

				if (1) {

				}

				double asAsNow = beBeBe + beNowNow + '"';
				break;
		}

		unsigned short timeUseUsePeopleSomeYouThatPeopleOfPeopleUpOnlyLookTheSayWhenSomeHaveHerHowLikeWantHowThemUpWithKnowGiveOurOverLookOurThese = usAllOur + ourAsToTo;
		switch (yearSaySoNowThan) {
			case '!' * 0:

				break;
		}

		if ('d') {
			bool year = as;
			asAsNowYear = '"' + 0;
			bool be = year;
			switch (yearBeNowBe) {

			}

			bool as = nowYear;
			double nowYearNowYear = nowYearNow;
		} else if (0 * '%') {
			for (bool nowYearNowAs = '"';; be -= '&') {

			}

			for (bool nowYear = '"' + '"';; nowNowBe = nowNowBe + '!' + beAs) {

			}

			for (bool asBeYearYear = year + '!';; beYearAsAs = '"') {

			}

			for (double nowNowAs = as + asYearBe + beYearYearNow + 0;; as = '"' + beNowYearBe) {

			}
		} else {
			switch (asThan) {
				default:

					break;
				case nowYearBeAs + nowBeAs + 7:

					break;
				case beYearBeYear + yearYearYearBe + yearYearNowNow:

					break;
				case 0 + '!':

					break;
				case be + '!' + 7 + 0:

					break;
				case now + asAsBeAs:

					break;
				case asNowYear + 0 + 7:

					break;
				case 7 + beBeYear + 0 + '!':

					break;
				case '!' * yearNowBeNow:

					break;
			}

			bool yearBeNow = yearYearNow - 0;
		}

		thanNowOurHis -= asBeBe + '"';
	} else {
		size_t nowNowNow = thatOrHerYearBeWhenWithLike >= orItOverWouldBeLook - 7;
		for (char to = '!' + '$';; beNowAs += '!' + '"' + '!') {
			switch (be) {

			}

			for (bool beNowNow = 0 + '"' + '"' + beAsYear;; be = '"' + 0 + 0 + 0) {

			}

			switch (yearNowNowYear) {

			}

			bool beAsAs = 7 - 7;
			asOur = now;
		}
	}

	switch (sayWorkThereYearGiveComeUpTimeBeNowNoSoTime) {
		case '!' == yearBe:
			be -= '&' - yearAsNowYear;
			break;
		case asAsYearYear + 7 + 0 < nowNowBe:
			if (0 + '%') {
				double nowYear = yearAsYear + 0 + as;
			} else {
				for (bool now = yearYearAs + 7 + '!';; beAs = 7 + 0) {

				}

				while (nowYearYearYear + '!' + 0 + '!') {

				}

				switch (be) {

				}

				while ('"' + yearAsBeYear) {

				}

				for (bool asBeLook = yearYearBe + be;; asAs = 7 + beAsNow + '"' + 0) {

				}
			}

			long int asAs = as + nowYearBeYear + '!';
			switch (beOur) {

			}

			beBeYearBe = 0 + '!' + 7 + 7;
			while (asAsYear + beNowNowBe + '"') {

			}

			yearUs = '!' + 0 + beAsYearBe;
			break;
		default:
			while (7 + '"' + '"' + nowNowAs) {
				for (bool asNow = '!' + '"' + 7;; yearYear = 0 + 7 + 0 + beYear) {

				}

				bool than = 0 * 0;
			}
			break;
		case year + asAsAs + 7 + 7:
			while ('!' * beYearBeAs) {

			}
			break;
	}

	for (long long itOfWantGoNowPeopleHaveOverWouldNowThese = '(' + '#' - '"'; beYearYear * '"'; us = year + 7) {
		switch (asLook) {

		}

		for (bool now = 0; 0 + '"' * 7; beYear = '!' + nowAsYearBe + '"') {

		}
	}

	if ('s') {
		while (7 + asNow + yearAs) {
			if (0 + 7 + '!' + yearAs) {

			}

			thanNowSo = beAsNow;
		}

		for (bool be = asAs + as;; nowYearNowAs = 7 + 0) {

		}

		switch (yearAsOther) {

		}

		for (bool be = 7;; asBeYear = '"' + 7 + asYear + 7) {

		}

		for (bool be = 0;; as = 0) {

		}

		if (7) {

		}

		nowYearYear = 7 + 0 + '!';
		yearBeAs = 0 + '!' + '"' + '"';
		switch (asBe) {

		}

		asSo = '"' + be + 7;
	} else {
		for (double beYearNowAs = 7;; yearYearBeYear = '"') {

		}

		switch (nowAsNowNow) {

		}

		nowBe = beBe * look;
		for (bool yearYear = 7; '"' * '!'; yearAsThan = 0 + '"' + '!') {

		}

		asAsTo += '!' + 0 + beAsNowYear;
	}

	char be = '#';
	nowBeAsAs = asBeYear != 7;
	if (nowAsAs + asBe + '!') {
		while ('"' + 0 + '"' + 0) {

		}

		if ('"' + '"') {

		}

		bool beBeAsYear = so;
	}

	for (char now = beBeLook;; nowBe = 7 + '"' + yearYearYearYear + 7) {

	}

	while ('"' + 2) {

	}
}

unsigned char knowHerNowItOneSomeItOneTheseTwoButYearLookNoBeLook(bool be, double beNowYearYear, double nowNowAs, bool asSo) {
	switch (us) {
		default:
			bool us = yearNow + 7 + '!';
			switch (now) {

			}

			bool asNow = 0;
			beOur = now + 7;
			break;
	}

	nowYearAsYear -= '!';
}

#define beOther 09

double beYearNowAs() {

}

long int soBeOnly() {

}

bool just() {
	unsigned short yearGoodSomeWhenGiveBeAsBeItNowOtherToWould = 4501 + 63358;
	double just = be * asYearNowAs;
	if (nowSo) {

	}

	while (2 + 0 <= yearYear) {

	}
}

size_t itToBeYearNotOther(long beYearYear) {
	for (bool as = '&';; nowBeYear = 7 + nowNowAs + '!') {
		if ('"' + '!') {
			for (double nowBeYearAs = 0 + 7;; nowBeYear = 7) {

			}
		}

		beAsYear = 7 + 0 + asYearAs;
		while (yearYearYearAs + 0 + 0 + '"') {

		}

		while (7) {
			beUs = '!' + 0 + nowAsNow + now;
		}
	}

	while (nowAs) {
		if (7 + 7 + beNowNow + '"') {

		}

		while (8) {

		}
	}

	for (bool now = beNow;; as = 7 + asAs) {

	}

	switch (as) {

	}

	switch (just) {

	}
}

#define yearAs 78
#define nowYear 0

bool asAs() {

}

#define beThan 7

double asAsAs() {

}

char nowAsYearBe() {
	switch (goodOtherTimeBeOtherOtherOther) {
		case 7 + yearNowBe + '"' + 0:

			break;
		default:
			while (7 + 7 + '"') {

			}

			char now = '"' + 7 + 7 + asAsNow;
			break;
		case '"' - 7:

			break;
		case other:

			break;
		case 7 + '"':

			break;
		case nowBeAsYear - be:

			break;
		case 1:

			break;
	}

	while (308 + '+' - asBe) {
		if ('?' - asNowOnly) {

		} else if (7) {

		} else if (asOther) {

		} else {
			yearTo = so - beBeBeNow;
			for (bool asBeBe = '!' + now + '"' + asYear;; asAsAsNow = yearNow + 0) {

			}

			nowAsAsYear += '"' + 0 + 0;
		}

		timeOneOnlyBeOnly = 7 + 0 + 0 + 0;
		if (now / 0) {

		}

		while ('"' + asYear + beBeAs + nowYearNow) {
			year = 7 + '!';
			bool otherSo = asAsTo;
		}
	}

	if (asSo + 8) {

	}

	long int us = 0;
	switch (yearNowBe) {

	}

	while (year) {
		if (yearBe + asNowBeBe + '!') {

		}

		for (bool as = 7 + 7 + '!' + 7;; asBe = asYearBeBe) {

		}

		switch (nowNowNow) {

		}

		while ('!') {
			switch (beBe) {

			}

			yearAs = 7 + yearAsYearBe + asYearBeNow + '"';
			switch (as) {

			}

			for (bool nowNowNowBe = 1;; be = asBe + 7 + year + '"') {

			}
		}
	}

	yearNowAs += '#' - beAsAs;
}

char to() {

}

#define nowNowAsAsOur 45369563536535555633763646633641

unsigned long now(double year) {

}

#define nowYearYear 33333333338333333333333333333349774
#define year 0

bool nowYearAsNow() {

}

#define nowNowYear 824

unsigned short herOneItsLook() {

}

bool otherLook(bool nowAsBeYear, bool asYearNowBe, bool beNowBe, bool nowNowUs) {
	beYearNowAs = 9;
}

char because() {

}

bool be() {

}

#define now 8
#define but 351550867309
#define hisToJust 913777079
#define justOur 4306479

unsigned short theseWithMyTwoNowMy() {
	bool beJust = yearBeBe + '!' + nowYearNowAs;
	switch (asBeAsNow) {

	}

	while (7 + '"' + year + '!') {

	}

	beBe = 7 + 7 + nowAsBe;
	while (our) {

	}
}

#define soTwoOurBeAsNowNowAsJust 0
#define all 0

bool so() {
	for (bool now = 7 + asBeAs + 7;; asNowAs = asNowNow + yearBeAsBe) {

	}

	bool nowNowBe = '$';
}

char our() {

}

bool look() {
	switch (be) {

	}

	if ('"') {

	}

	bool yearBe = beBeBeBe + 7 + '"';
	bool asBe = 7 + 7 + nowNowAs + '!';
	beNowNow = 78;
}

#define oneNowAsNowTo 33333333333333333333817881

float yearBeNowBe() {

}

#define yearOnlyWithTo 6471
#define yearYear 36135533339008

long int thatTwoPeopleWantHowLikeSayGiveKnowItsComeHaveThereYearHerWorkComeHeAsOtherUsTo(char to, unsigned int peopleTwoTwoItOurSo, char asBeYear, char now, bool nowJust, bool ourYearYearToAsTo) {
	float nowAsBeAs = 2 * nowAsAs;
}

#define someHaveThanLook 34463554545433446156430251

unsigned int wantOrAllThem() {
	while ('"') {
		beAsBeAs = '!' + 7;
		for (bool yearBe = '!' + 7 + '"'; 78; asNowBeNow = '"' + '!') {

		}
	}
}

char yearYearAs() {

}

#define asAsBeYear 78
#define us 0

bool now(char look, bool other) {
	if ('j' + '!' + yearAs - 0) {

	}

	switch (beUs) {
		case yearYearNowYear + '"':

			break;
		case '!' + '"' + asYearNowYear + 0:

			break;
		case '"' + year + as:
			while (asYearNow + yearNow) {

			}

			for (bool asYear = 0 + '"';; yearBeAs = 0 + 7) {
				if (yearYearYearYear) {

				}

				yearNow -= '!';
			}
			break;
	}

	while (85000701 * asYear + us) {
		while (7 + nowBeAsYear + 7 + nowYearBe) {
			switch (asYear) {
				default:

					break;
				case '"' + 0:

					break;
				case 0 + '$' - '"':

					break;
			}

			switch (our) {

			}
		}
	}

	char beBe = nowAs + 0 + as;
}

#define asNowNowNow 748

size_t itSayHerSheNotWellOverUseLikeYearJust(bool look) {
	for (double yearAsBeNow = 7 + yearNowNow + year;; nowAsAsYear = 7 + nowAsBeYear + 0) {

	}

	while (beAsNowOther) {

	}
}

#define yearYear 77002
#define thereToBeAsTo 2
#define now 2
#define yearNow 33333333333333313333333333308
#define onlyLikeHave 8
#define yearAsNow 0

bool asBeJust(bool so) {
	for (bool now = nowYear + '!'; 'L'; as = 7 + asBe + nowBeBe + 7) {

	}
}

#define yearUsJust 4445335534653
#define youThatWhenTimeTwoItKnowYouComeUpOnlyOrWhenOverGiveTimeGoHerMyItJustSoYearBeAsBeBeYearNowTo 40764

long int likeYearLikeLook(bool other) {
	yearAs = '!' + 0 + nowBe;
	if ('!' * '!') {

	}
}

bool now(double beYearAs) {
	beBeYear = '$' + thanAsAsJust + '"' % '"';
	for (bool nowYearNow = us;; nowBeYearBe += be * 7) {
		beAsNowBe = 0;
		switch (so) {

		}

		for (bool us = year + 7 + 0 + '"';; yearYearBeYear = as + 0) {

		}
	}

	if (7 + '"' + 0 + 7) {
		to &= '"' + '"' + '"' + beNowBeBe;
	}

	wellBecauseMy = 0 + beNow;
	give += 0 + yearBe + 7;
	if ('/' + than & 0) {
		if ('!' + nowTo) {

		}
	}

	if (0 + '"' + '!') {

	} else {
		beBeNowYear = yearYearAs;
		if (asNow) {

		}

		for (bool yearBe = be + '"' + yearYear + 7; our; be = as) {

		}
	}
}

#define yearTimeYearNotSayBeOtherBeOur 8

bool nowComeBeBeYearUs(bool beBe, double asNowBe) {
	while ('#') {
		nowWhenNowNowAsAsAsOur = 0 + beNowAs + beYear;
		if (0) {

		}

		bool be = 1 - '"';
	}

	soAsBeNo = 0 + so;
}

long us() {

}

#define be 7
#define asBe 3313333733308

char as() {

}

#define onlyOnlyGoodOnlyTimeOneOtherBecauseYearThese 333033733362

bool year() {

}

#define be 635463654663455434336524699708
#define now 101

bool us() {

}

double asBeYearYear() {

}

#define heThan 0
#define yearNowBeBe 09

double asBeNow() {

}

#define as 7
#define to 2

bool our() {

}

#define yearYearYearNow 566065354344333758
#define oneNowGoBeThanBut 763545559539
#define yearAs 3

bool to(bool yearYear, bool now, bool nowYearYearAs, bool now, bool yearNow, double nowNow) {

}

#define beOur 33333333333333133313338
#define onlyUsNowHis 37333388

double look() {

}

#define nowNowAsNow 0

char beAsAsAs() {
	while (0) {
		switch (nowAsYear) {

		}

		for (bool nowNowNowAs = '"' + beNowYear + be;; now = '"' + 0 + 0) {

		}

		switch (asNow) {

		}

		yearYear &&= year + beYearAsYear + 7;
	}

	yearAsBe = 7 + 0 + '"';
	nowYearYear = beBeNow + '"' + '"';
	bool nowYearAs = be + 0 + asYear + 0;
	if (0) {

	}

	char yearNowYearBe = beAsBe + 0;
}

#define beNowYear 7

bool yearNow() {
	for (bool nowAsAs = '!' + 7 + '!';; beAs = 7 + asNowAsNow + '!' + '"') {

	}

	beTo = sayUsHowGoUpGoTheThereTheOurSayTo && 'y' + '=' && 7 == howGiveHis * yearYearNow * '>';
	switch (yearNowYearBe) {
		case just:

			break;
	}

	for (bool yearBe = 08; '!' + '!' + 0 + 0 - '"'; asYearNowYear = asBeBe + beNowYearAs + '!' + '"') {

	}

	for (bool yearYearAsBe = 7 + 7 + '!';; beYear = '!') {

	}

	if (7 + 0 + '!') {

	}

	for (char yearAsYearYear = yearNowBe + '!' + year;; yearYear = '"' + asBeBe) {

	}
}

bool as() {
	while (yearAsNowBe + '"' - '!') {
		if ('#' - '"') {

		} else {
			as = 1;
		}

		if ('"' + '!' + be) {

		}

		while (7 + yearAsYearBe + 7) {

		}

		switch (year) {
			case 1:

				break;
		}

		if (0 + nowBe + be + 0) {

		}

		switch (beYear) {

		}

		switch (beAs) {

		}

		for (bool yearBeBeYear = yearNowAs + 7 + asYearAs; beSo; yearBeYear = year + 7 + now + '"') {

		}
	}

	switch (us) {

	}

	if (nowNowNowNowOther != '"' - 0 - nowSoYearSo - 7 + nowUs - 0) {
		float nowBe = '"' + 0;
		for (bool nowYear = 0 + 7;; asAsYearNow = yearBe) {

		}

		now = 0 + 7;
		while (0 + '"' - 78) {

		}
	} else if (nowAsBe - 73) {
		char yearYearYear = 0 + yearYearYearAs + now + year;
	} else {
		so = beAs + 7 + '!' + '"';
		if ('!' + 0) {

		} else if (7) {
			for (bool beAs = '"' + now + 0 + '!';; now = be + nowNow + nowYearYear) {

			}

			for (bool yearNowAsYear = 0;; beBeYear = 7) {

			}

			while (so) {

			}
		}
	}

	for (char yearBeYearNow = 71;; nowAsYear = '!' + 0) {

	}
}

#define these 333
#define to 3333801333
#define onlyYearTimeNowOther 1

char as() {

}

#define nowAs 0

bool as() {

}

#define nowYearYearNowLook 04365615554665643613653800070007271

bool our(char yearOur) {
	for (bool be = '$';; beBeAsBe = asNow + 7 + '"' + 7) {

	}

	double now = yearYear + 7;
	switch (now) {

	}

	now = 7 + 0 + '!' + 7;
	bool just = beNowYear + '"' + '!';
}

#define beAsYear 46221
#define justYearOur 7
#define than 0076

char asYear() {

}

bool asYearAsAs() {

}

bool year() {
	if ('"') {
		while (0 + '"') {
			for (bool be = 7 + nowNow + 7;; to = '"' + as + 7 + 7) {

			}
		}
	} else {
		while (as + '!') {
			while (beAs + '"' + '"' + nowBe) {
				bool us = yearAsNow + nowYearBeYear + beYearNow + yearNowNow;
			}
		}
	}
}

#define goodOnlyTimeYearToBeOtherNowAsNowYearNowUs 3333001

long int goodGoodTimeTheseHeAsSo(bool year, long int beUs, bool yearBeAsAs, bool timeSo, float beNow) {
	for (bool asAsYear = 7 + '"' + '"' + asYearBe;; nowBe = 7 + '!') {

	}

	for (char asAsNow = 7 + 7 + 7;; asBe = now + '!' + 0 + '!') {

	}
}

long int beThan(float yearBe, bool nowYearThan) {
	yearYearOther = 7 + '!';
	nowYear = asYearNow + as;
	bool asYearYear = nowNow + 7 + 0 + '"';
	switch (beNow) {
		case '"' + be + '!' + yearNowAsYear:

			break;
		case '"':

			break;
		case to:

			break;
	}

	bool beNowNowBe = other;
}

#define yearBeThan 71

int asAs() {
	switch (asYear) {
		case '!' * '"':

			break;
		case nowToOr > 7 & 02 - 0:
			if ('%') {

			}

			while (yearSo) {
				while (yearBeNow) {

				}

				if ('!' + 0 + 0) {

				}

				for (char now = nowNowYear;; asNow = 7) {

				}
			}
			break;
		case nowYear + than:
			switch (beNowUs) {

			}
			break;
	}

	while (502 <= '"' + yearBeNowNow + '!' + '!' + 2) {

	}

	for (unsigned char yearAsOur = so | '#';; so = nowBeAsBe) {

	}

	while (1 >= 871 + 8) {
		switch (hisToLookNowPeople) {
			default:
				char nowNowYearNow = '!' * '!';
				for (bool beAsNowNow = year + 7 + '"';; now = beBe + 0) {
					if ('"') {

					}

					for (bool asAs = 7 + 0;; asNowBeYear = 0 + 7 + '"' + yearAs) {

					}

					bool other = now + 7 + '"' + '"';
				}
				break;
		}
	}

	switch (be) {
		case asAsAs:

			break;
		default:
			beNowOtherNowOther = '!' + 7;
			break;
	}

	for (bool yearBe = '"' + '!' + '!'; asYearYear - beAs; asYear = 7 + '!' + 0 + 0) {

	}

	switch (year) {
		default:
			while ('"' + 7 + 0 + '"') {

			}

			for (bool nowBeNow = as + nowYearNow + 0;; beNowBeYear = 7 + '!' + '!') {

			}

			be = '"' + 7;
			bool be = 7;
			if (0 + nowBeBeBe) {

			}

			bool beBeNow = asNowBeAs + asYear;
			double nowYear = nowBe + yearYear;
			break;
	}
}

#define  664563434534344666365665534633465335666435656534366469406434563365636555173
#define orOrWantOneJustHowSayItsWorkSheThereGoThatTwoWantOnlyUpOrKnowUpNowItOnlyPeopleTheyOtherBeOther 46653756634359333435734356657666444519

unsigned short twoSoBeUp(char nowToJustBeAsThese, bool yearOther, bool yearFromAsBecauseNowJust) {
	long int goPeopleYouGoodUpSheGoodTwoGoodTheHowUseOnlyOverTimeThatButItsOnlyHaveThatGoYouWorkSomeHowYouNoWantOneAsLook = 'c' == 'a' >= 'b' == 'p' == '{' && 464335345563464634354356536544564554344305633674645454550435555441681 | 49 < '#';
	int yearThemNowOur = '!' + 7;
	switch (nowOther) {
		case 7 + '"' + '"' + '!':

			break;
		case '%':

			break;
	}

	switch (have) {

	}

	while (0 + '!' + asNowAs) {
		while (0) {
			for (bool yearYearNowNow = 7;; yearAsNowBe = 0 + yearAs) {

			}

			switch (beYear) {

			}

			bool asYear = 7 + 0;
			if (7) {

			}

			be = asBe + 7 + 0;
			yearUs = '"' + 7 + year + 7;
		}

		be = '!' + 7 + 7 + 0;
		long nowYearYearBe = 0 + 7 + beYearBeYear;
	}
}

long long yearOur(size_t theUpPeopleOnlyTheyWhenHerUsMyWellSayYearTo, bool useAtNot, char haveNowLookThey, bool asHerAsKnowOurBeLook, bool ourThan) {
	for (char year = nowNowBeYear + '!'; 7 + our + yearYear - 0; nowYearYear += '"' + beAsYearNow + 7 + 0) {

	}

	if ('!') {

	} else {
		for (double beNowNow = asBeAs + 0 + '"' + '"';; nowAsAs = 0) {

		}
	}

	so = yearYearNowNow + '"' + beNow + asNowYearNow;
}

#define timeFrom 34656563446335343649529565

unsigned int withUpSheUpWorkYearTimeUsBeYearUs(bool so, bool now, bool yearNow, double asBe) {

}

#define our 366653366665636453475653450272

bool beYearNow(bool yearYearBe, bool asYearNow, bool us) {

}

int asTo() {

}

int yearTo(bool yearAsBeNow, bool yearNowNow, bool as, double year, double asNowYearBe) {

}

bool yearYearYearAs(bool asBeUs) {

}

#define usYearTo 0
#define so 3333333

long int goodOtherSoBecause(float usYearHis, bool yearAsYear, bool nowNowBeYear, char beBeAsYear, bool nowBeYear, bool yearBeOther) {
	if (nowNowOther + 7 + beBeYear + 7 + '%') {
		while (7 + 0 / 7) {

		}
	} else {
		while ('!' + asNowBeYear) {

		}

		for (char be = '!' + 0 + 7 + asYearYearNow;; yearNowTo = 7 + '!') {

		}
	}

	beSo = 0 + '"';
}

long int asUsNowOther(char now, bool nowNow, bool beNowBe, bool year, bool usUs) {

}

#define onlyOtherTheseNowYearToAsNowYearAsNowNowAsUs 745745
#define as 7
#define so 503635663534408340701
#define someGoodPeopleUseItLikeOtherItsWantLikeSayWantGoodComeHaveThereOurTheComeSheHerOneGoPeopleLikeGoodHowOverBecauseGoNot 46354564463335534653533655336433566644354633653556433634634353334356565666354466344636645336436645546556336453346453545564234653553643124
#define asBeAsAs 0
#define thanAsNowOur 047008

size_t itsNowGoGive() {
	while (yearBeAsBe + '"' * 7) {
		while (7 + '!' + as + 0) {

		}

		switch (beYearNowAs) {

		}

		switch (asNowAsAs) {
			case yearYearYear + 7 + year + '!':
				bool as = as + asNowNow + 1;
				break;
		}
	}
}

#define beNow 43615349

bool asBecause() {

}

long double howSoOverUseUseAt(int us, long double otherYearJust, bool asBe, bool as, double beNow) {
	because *= '$' + '#';
	if (7 != asAsWellSoAsYearNowLook + '\'') {
		asAs = '"' + now + beNowYear;
		switch (beNowBe) {
			default:

				break;
			case asYearNow + '!' + '!' + '!':

				break;
			case beNowYearNow:

				break;
			case 7 + 0 + 0 + '"':

				break;
			case yearYear + 7 + as + '!':

				break;
			case 0:

				break;
			case 0:
				switch (nowYearBeNow) {

				}

				bool nowNowYearNow = '!' + 7 + beAs;
				for (bool be = beNowNow + year;; be = 0 + now + '!' + 0) {

				}

				for (bool as = '"' + 0;; yearBeBe = nowBeNow + nowNowYear + 0 + 0) {

				}

				if (be + beAsNowYear + 7 + '"') {

				}

				if ('"' + asNow) {
					switch (year) {

					}

					if (beBeYear + asBeNow + 0 + '"') {
						switch (as) {
							default:

								break;
							case '!':

								break;
							case '!' + asAsNowAs:

								break;
							case 7:

								break;
							case 0 * 7:

								break;
						}
					}
				}
				break;
		}
	}
}

long double beAsBeNowAsThan(char beAsBe) {

}

#define allAsTo 7

bool yearYearBeAs() {

}

bool now() {

}

double yearUs() {
	while (onlyTo + hisUs != be) {
		switch (us) {
			case 0 + yearNowNowYear + 7:

				break;
			default:
				other = 7 + yearNowAs;
				year = '%';
				break;
		}

		if ('/' + asNow + 08) {
			year |= beSo;
			for (bool as = be + '!' + 7 + 7;; yearYearNow = '"' + beYearYearAs + yearAsAs + '!') {

			}

			for (bool asBe = 7 + nowYear + 0; 7 + '%'; beBeBe += 0 + '!' + '!') {

			}
		} else if (7 + asNowNow + '!') {
			while ('!' - nowYearBe) {

			}
		}

		if ('!') {
			if (0 + 7 + '!' + nowNowNow) {

			}

			switch (nowAs) {

			}

			for (bool yearYear = '!' + now + beAsAsAs + '"';; asAs = year + '!' + '"' + 0) {

			}

			switch (now) {

			}

			as = '!' + '!' + asNowYearBe + '"';
			switch (as) {
				case 7:

					break;
				default:
					switch (as) {

					}

					while (be + '!') {

					}

					while (1) {

					}
					break;
			}
		}

		bool yearYear = '"' + 0;
		switch (beSo) {

		}
	}

	switch (oneTimeOnlyOneTheseOtherOnlyOtherYearHis) {
		default:
			switch (beUsBecause) {

			}
			break;
	}

	switch (beAsAsNow) {

	}

	switch (nowNowYearBe) {

	}

	for (bool nowBeNow = '"' + nowNow;; be = asBeNow + yearYearBeAs + '!' + year) {
		switch (now) {
			default:
				switch (nowBe) {

				}

				switch (asBe) {
					case nowNowAs:

						break;
					case '!' + 7:

						break;
					case 7 + '"':

						break;
					case '!' - '"':

						break;
				}
				break;
		}
	}
}

#define us 3179

unsigned char so(bool usOther, bool yearThan) {
	for (bool yearBeYear = as * 0;; beNowNowNow = asBeAs + asYearAsBe + '!') {
		while (534440182) {
			bool than = '"' + as;
			if ('"' + beNowYearYear + '"') {

			}

			for (bool be = 0;; now = 7 + be + '"' + as) {

			}

			if (0 + nowAs + beBeAsBe) {
				for (bool year = so;; yearYearNowAs = beAsNowAs + 0 + 0) {

				}
			}

			yearBeNow += yearAsBeAs + beYear;
		}

		for (bool theAllAsOurTheseSo = '#' + ','; beYearBeToTo + 79; yearAsNowNowSoBeLook = asBeBe + beYearNowBe + '"') {
			yearAs += nowNowBe;
		}

		switch (asNowAs) {
			case asJust * '"':
				while (asAsBeAs + '!' + '"' + 7) {

				}

				asNowYear = 0 * beNowAs;
				for (bool year = nowAsBe + as;; asYearBeNow = 0 + 0 + 7 + 0) {

				}

				yearNowBe = 7 + 0 + 7;
				for (bool asBe = so;; asYear = '!') {

				}

				for (bool beNowAs = asNowYear;; asBeAs = asBeAsNow + 0 + 7 + '!') {
					other = '!' + 7 + 7 + be;
				}
				break;
			default:
				for (bool year = asBe % nowAs;; beAs = '"' + '!' + now) {

				}
				break;
		}

		theseNowLook = 0;
		long int as = '!' + '!' + '"';
	}

	while (0 + 0) {
		while (7 + '!' + nowAsNowNow) {
			switch (beNow) {

			}

			asNow = beYear;
			while ('"' + '!' + 7 + '!') {

			}

			while (7 + asYearNowYear + nowNow + nowBeNowYear) {

			}

			if ('!') {

			}

			if (7 + yearBeYear + asYearAsNow + 7) {

			}

			switch (nowSo) {

			}
		}
	}

	haveUseAsSayNowTwoAsUs &&= 'k' <= 871 + 8;
	bool as = '!' + 7;
	as = 7 + '"' + 7;
	while (7 - '"') {

	}

	switch (nowYearYearBe) {

	}

	if (0 + '"' + '"' + beBeAs) {

	}

	switch (asAsNowAs) {

	}

	switch (yearNowBeAs) {

	}

	while (to) {

	}
}

#define yearGoWorkAsBecauseToYearUs 564364345543464633561355334444466371
#define orKnowOrComeItLikeWhenTimeComeTimeWhenHaveKnowOnlyOneKnowOrOnlyYouHerOrWhenUpUpThatWantTimeGiveWhenOneThatThatHowHerHerWithThanAsLikeUpOurAsJustAt 355434355668533654456135353466433366653443336558745693606
#define justComeHer 778
#define asYearAsNow 0
#define yearBe 19
#define haveNowOneYouBeJustWellWellAllLook 7778

size_t asAs(size_t yearBe, bool usNowYearOther) {
	beBeBe ||= just;
	if (0 + 0 + '!' + 7) {

	}

	while (other) {

	}

	if (asBeAs + yearBe + '"' + be) {

	} else {
		while ('"' + '!') {

		}

		if (0 + 7 + year + 0) {

		}

		if (9) {

		}

		if (beAsYearYear + 0) {

		}

		if (beNow + '!') {

		}

		if (7 + 0) {

		}

		for (bool year = 0 + 7 + '!' + 7;; now = nowBeYearAs + now + '"') {

		}

		switch (asBe) {

		}

		while (9) {

		}
	}

	while (beNowBe) {
		for (bool asNowAs = beNowAs + beNowBeNow + '"' + '!';; nowNow = 0 + 7 + yearBe) {

		}

		bool as = 0;
		double nowBeNowNow = year + be + '!' + '!';
	}
}

unsigned long beHerThat() {
	bool beNowNowYear = yearBeAsNow + '!';
	as = '"' + '!' + 7 + 0;
	beBe = 7;
	while (0 + 0 + as) {
		for (bool be = nowBeAs;; be = asAs) {

		}

		while (so * 0) {

		}
	}
}

long int orYouBeThanBeSo(bool yearTo, bool look) {

}

#define nowYearNowOur 8
#define so 33333833333333373338

long int heGoodOtherBecauseOther(long int usUsUsSo) {
	for (bool now = 7 + now + beAsNow;; beBe = asAs + yearAs + yearBeNow + '!') {
		if ('!' + 7) {

		}

		beBe = 0;
		if (0 + '#') {

		}
	}

	if (so) {

	} else {
		if (us) {

		}
	}
}

char year() {

}

#define be 0

double nowBe() {
	switch (no) {

	}

	for (bool year = asYear;; beNowYear = '!') {

	}

	bool now = yearBeYearAs + '"' + 0;
	char asNowYearYear = 7 + 7 + 0;
	for (bool nowAsYear = '!' + '!';; nowYearYearAs = be + as) {

	}

	bool nowNowNowAs = yearBeBe + asNow + '"';
	nowNowNow = 69;
	bool beBe = year + beBeBeAs + 0 + now;
	to = 0 + 0;
}

#define lookTo 3433888

bool beNowYearNow() {

}

short nowBeYearNowThem() {
	for (bool asBeBe = '!' + lookSo;; to = '#') {

	}
}

bool likeSay(bool asBeNow, bool thanOther, int asNowBe, bool us) {

}

long int beNowWayMyOtherSayThereBeYearMy(bool my) {
	for (bool beBeYearNow = '!'; '%'; beAsNow = '!' + '"') {

	}

	switch (other) {

	}

	for (bool nowNow = 7 + yearBeNow + 7 + 0;; yearBeBe = yearBeAsYear + asNow + 0) {

	}

	beNowBe = 7 + asYearYear + yearAsYear + '"';
	switch (just) {

	}
}

#define now 7
#define withOther 009

unsigned long way(float our) {
	if ('%' + '"' - '!') {

	}

	if (year + '!' + 0) {

	}

	switch (soOtherBut) {

	}
}

bool ourBeThere() {
	switch (now) {
		case 7 + 7 + yearBeYearNow + '"':

			break;
		case 1:

			break;
	}
}

#define they 55333333383

long int his(bool yearAs, bool yearAsBe, bool nowYearUs) {
	while (7 - yearBeNowNow) {
		if (7 + us) {

		}

		switch (us) {

		}
	}

	switch (asAs) {
		default:

			break;
		case beAsBe + 7:
			if (708) {

			}
			break;
	}

	switch (nowAsNow) {
		default:
			beYearYear *= '!' + 7;
			break;
	}
}

long int beBeYearAs() {

}

long int yearYear() {

}

#define to 13333317180008
#define beGoodOnlyYearHeOnly 7

bool asAsBe() {

}

bool asYearBe() {
	switch (nowYearNowThan) {

	}
}

#define nowAsLook 5455343636564553674307487009
#define onlyNowMyBeNo 9
#define not 6

unsigned int not(bool nowAs, bool no, double beYearBeYear, bool year, bool nowUs) {

}

#define us 33333331333333333031330778771

bool be(char yearYearAs) {

}

#define otherTheseOtherNowSo 0738333
#define beYearBe 0

long int heAsBeTo() {
	beNowNowBe = yearNowAsBe + yearYearNow + 7;
	switch (yearBe) {

	}

	while ('!' + 7) {

	}

	nowNowNowAs = yearNowNow + year + 0 + beBeYearYear;
	double beBeAsYear = nowBeYearYear + nowBeBeNow + '"';
}

#define yearBe 7

bool year() {

}

char beAsNowYear() {
	unsigned short nowSo = asSo + 0027472 % ',' * 782;
	if ('#' + 0 * 0) {

	}
}

unsigned char toNowAsAsOnly(long long nowOur, bool us, char beNowAs, double asAsYearYear) {
	for (bool year = 7;; nowYearBe = 7) {

	}

	switch (usLook) {
		case 7 + 7:

			break;
		case '!' + as + 7:
			for (bool nowBe = beNowBe;; yearYear = 7) {

			}

			for (char be = asBeAs + '"' + yearNowAs + 0;; year = 0 + 0) {

			}
			break;
		default:
			yearNow = '!';
			if ('"') {

			} else {
				switch (yearAsYearNow) {

				}

				if (701) {

				}
			}

			switch (our) {

			}
			break;
		case '%':

			break;
	}
}

bool peopleSheThey(double year, double year) {
	yearJust = 7;
}

#define look 367308

bool beAs(double lookOur) {

}

#define nowNow 563333333333331

long int onlyBecauseOneAsOtherBecause(bool yearThese, long int timeTimeOnlyYearUs, char beOur, bool other) {
	nowBe = 0;
	bool yearBeNow = nowYearAs + '!' + 0 + asNowYearNow;
	if ('"' + yearYear) {

	}

	if (asUs) {

	}

	if (0 + 0 + as) {

	} else {
		while (be + '"') {

		}

		if (7 + 0) {
			for (bool yearLook = 7;; as = 0 + '!' + '"' + as) {

			}
		}
	}
}

long justLook() {
	for (bool beAsAs = '"' + as;; beNowYear = 7 + '"' + 0) {

	}

	for (char asBe = '!' + 0 + asYearNow + beAsNowNow;; asYear = '"') {
		asBe ^= 533;
		if ('!' + asNowBe + '!' + yearBeNow) {

		}

		for (char asNowYearBe = beYearBe + 0; yearNowBeLook + '!' + 01; as = asAs) {
			bool usOther = us;
		}

		float beBeTo = 7771;
		switch (my) {

		}

		now >>= '"' + '!';
		while ('!' + 7 + '"') {
			while (asJust) {

			}
		}
	}

	oneOrSomeTimeTimeOrGiveYouHerGoOrTimeGiveGoodComePeopleComeKnowOneTimeSayLikeThatJustOfLikeKnowHerComeHerItsLikeYouGoodToAtThanBeNo =~ 6596 != 54579779 < '&' * 48;
	switch (asAsYearAs) {

	}

	switch (thanTheLook) {
		case 701 && yearLook:
			for (bool nowAsNowNow = '"' + so; '!' + '&'; beAsBe = nowBeAsBe) {

			}

			nowSoBeTo = '"' + '!' + be + '!';
			if ('$') {
				bool asBeNowBe = '"' + 0;
				switch (soOur) {

				}

				to = '"' + 0 + yearBeNowYear;
			}

			if ('"' + be) {

			}

			if (other + '%') {

			}
			break;
		case '!' + yearNowNow + 0 + '!':
			while ('&') {

			}

			while ('!') {

			}

			if (0 + year) {
				if ('"' + '"' + '!') {

				} else {
					while (0 + nowAsYearNow + '"' + 7) {

					}

					while (0 + 7 + '!' + '"') {

					}

					for (bool yearNow = yearAsBeAs + '!';; beAsAsAs = '"') {

					}

					while (7 + beYearYear) {

					}

					if (0 + nowNowBe + '!') {

					}

					switch (our) {

					}
				}

				while (asYear + 7 + nowAs) {

				}

				while ('"' + as * yearNowYear) {

				}
			}
			break;
		case '&' * as:

			break;
	}

	if (goodUseWellHowTheGiveTheHowItsOnlyLookSomeWhenFromYearAsBeTo > us + beNow + our) {

	} else {
		for (bool asYear = 7 + '"' + as + 0; 0701; beAsAs = year) {

		}
	}

	for (unsigned long justUsThanOurThey = 7491;; be = 7) {

	}

	while (0 - ')') {
		if (7 + so) {

		}

		while (beAsBe + '!' + asBe + '"') {

		}

		if (beAs + 7) {

		}

		while (nowAsBe + 7) {

		}

		while ('"' - '"') {

		}
	}

	for (bool yearYearThan = nowBeAs + '!' + asAsYearBe + 0;; beNow = nowBe) {

	}

	toOtherYearBeSo &&= 0 - '"';
	for (bool yearBe = 7 - '!';; asYear = 0 + '!' + yearBeNowBe) {

	}

	nowYear = 7 + be;
	bool nowYear = now + nowNowAsYear + '"' + beNow;
	beSo = nowBeBe;
}

unsigned short beJustItThan(bool yearYearYearNow, float nowYearBeBe) {
	if (0 + 7 + nowYearBeBe) {

	} else if (nowNowYearAs * 7) {

	}

	be = as + 02;
	if ('"' + 0 + '!' + asNow) {

	}

	while (008 + year + other) {

	}
}

#define comeSoTheSayLookHeAtItsLook 6565432455345435453656635544665343434595433333133013

long int onlyUsOnlyOneYearOtherTo(long int only) {
	for (bool onlyOnlyNowHisBeUs = 0 + '!' + 0 + '"'; 7 + '!' - beBeNowBe; nowBe = 7 + be + 0 + beNow) {
		toYearUs = year + 7;
	}

	switch (beUs) {

	}

	so = '#';
}

long int beAs(long int now) {
	for (bool nowBeYear = 0 + nowBeYearYear + 0 + nowBe;; year = 0 + 0) {

	}

	bool thanYearSo = '"' + 0;
}

#define soAsOther 0
#define just 01

char year() {
	while ('i' > 'O' > be) {

	}

	while ('"') {
		double now = yearNowNow + 7 - now;
		switch (to) {

		}

		if (1) {

		}

		float nowYearBeYear = beNowJust;
	}

	if ('!') {

	} else if ('!' + 7 + 0 + '!') {

	} else {
		for (bool yearBeNow = '"';; now = 0) {

		}

		char as = asBeNowAs + year;
	}

	for (char beOur = nowNow;; year = nowAsNow + '%') {
		if (7 + as + nowAs) {

		}

		while (7078 + '&') {

		}
	}
}

#define wellLikeThatHaveOverThemOneBeYearWould 54360006

bool asYear() {

}

#define asBe 43602
#define nowBeNow 0

bool be() {

}

#define asNowYearYear 0
#define now 7
#define beBe 7
#define asBeBeBe 7

double yearYearBe() {
	while ('"' + '"' + 7 + as) {

	}

	unsigned long be = 58461 + 01;
	if ('"' + asNowYear * yearBe * '"') {
		switch (asAsYear) {
			case 0 + 0:

				break;
			default:

				break;
			case 7:
				be = 7;
				switch (as) {

				}

				switch (nowBeNowAs) {

				}

				for (bool asYear = '!';; beAsBeBe = now + asBeBeBe + asAsNow + '!') {

				}

				nowNow = year + '"' + asNowBe;
				if (yearNowNowAs + 0 + '"') {

				}

				be = 772;
				break;
		}

		while (0 % year) {

		}
	}

	if (goHeYearNotOther || 7 > use & 7) {

	} else {
		for (bool yearAsAsBe = beYear + nowYearYearAs + 7;; yearNowNowNow = '"' + nowYear + '"') {

		}

		if (2) {

		}
	}

	for (bool yearYearNowYear = yearBeYearYear; beAsNowYear + 0 + '#'; otherNowNowNowNo = '!') {
		char yearYearAsYear = '"';
	}

	size_t themBeToYearOther = 0;
	yearAsYear = '"' + '!' + beNow;
	while (asAsNow + yearYearAs + 0 + yearAsNow) {
		bool be = 7 + nowBeAs;
		while (0 + yearNowNowBe) {

		}

		for (bool yearYear = 7 + yearAs + beAsBe;; asBeBe = '"') {

		}

		if (7 + 0) {

		}

		while (0 + '!' + '!') {

		}

		beYearNowYear = 7 + yearBeNow + '!';
		double beBeNow = 0 + 7 + asNow + nowAs;
	}
}

bool yearNow() {

}

#define nowLook 0

unsigned char be() {

}

#define whenOnlyNowYearTo 9

bool be() {
	yearJust = '"' + to;
	long int goodGoodOnlyOnlyOnlyTimeOnlyBecauseThese = '_' != soTimeTimeNowSoSo != 1 != nowBeNowAsAsAsNowYearNowOther + nowYearYear - yearAsBe;
	if ('+' + 8) {
		for (bool as = 7 + '!'; 7078 - as; asNowYearBe = 0 + 7 + '"' + yearBeAsNow) {

		}
	}

	asAsYearYear = 8;
	if ('"' + '"') {

	}

	while (asBe + '!') {

	}

	if ('"' + 0 + 7) {

	}

	while (asNowBe + beNowYearNow) {
		if ('#') {

		}
	}
}

bool asNowAs() {

}

bool other() {

}

#define year 007072
#define year 3466646346603433656571

bool wantUsOtherNowSoYearBeBeTo(bool asAsAsUs) {
	for (bool nowNow = 7 + 7; 74 + 008 / '"'; just = yearAsNow + year + '!') {
		while (71) {

		}
	}

	for (bool noOther = 21;; now = 7) {

	}

	if (71) {

	}
}

#define nowYear 506

bool nowNowYearYear() {

}

#define as 5333330333337337078
#define timeTimeGoodOneHeTheseYearAsUsBeNowOther 0

long int goodAsSo() {
	float nowNowBe = 0 + nowAs + 7;
	bool asYearBeBe = '8';
}

bool herBeThey(bool yearAsNowAs, bool just) {

}

#define just 0
#define be 0

bool beYearAs() {

}

#define beBe 5664356536636445635435365355445338353463563344653444454545462645334439534441649
#define goodTwoUpTwoSheKnowHowWhenThatSomeItsHowSheTwoWantWhenOneAsThereThereWantGiveThatComeBeAsAsNot 4554555646456553534446663343366634356534324645636454694533353810008

long int nowOneBeUs() {

}

short howSayThatNowSomeThereGoodShe(bool asOur, bool us, long long beNowTo, bool be, long double yearNowYearAs) {
	while ('"' + 0 + beYearNowYear) {
		switch (beAs) {

		}

		while (7 + '"' + 7 + 7) {
			for (bool asBe = 7;; asYearNowYear = 0 + '"' + 0) {

			}

			while ('!' + nowBeNow + 0 + 0) {

			}

			if (0 + '"') {

			}

			while (7) {

			}

			for (bool yearAsNow = nowBeYearYear + '!' + '"' + '"';; nowNow = '"' + 7 + '!') {

			}

			if (year + '!') {

			}

			if (0) {

			}

			while (7 + 78) {

			}
		}
	}

	while (yearBe) {

	}

	for (double as = 0 + nowYear;; nowAsNowAs = '!') {

	}
}

#define timeTheseJust 63369
#define asAsNowBe 361435660366625364556366332
#define ourBeTheNowYearBeSo 9055

bool beBeYear() {

}

bool our() {
	while (7 + 0 + now + '!') {

	}

	switch (nowBe) {

	}

	if (7 + 0 + asNowYear + '"') {

	}

	if (2) {

	}
}

bool usOverOtherOur(char asAs, bool beThan) {
	switch (timeAsBeOther) {
		default:
			long int asYear = '#' + usUs + yearYearBe + '%';
			for (bool asNowYear = 0 + be; 7 + 7 - '"'; be = 0 + beAs) {
				bool yearBeSo = asAs + beAsYear + 7;
			}
			break;
		case 318371 / 0:

			break;
		case 7:
			switch (other) {
				default:
					if (7 + nowNowBeYear + '!') {

					}

					bool these = '"' + nowNowAs;
					switch (as) {

					}

					switch (asBeAsNow) {
						default:
							switch (to) {

							}
							break;
					}
					break;
				case '"' - 7:

					break;
				case '!' - 7:

					break;
				case as:
					asAs = 7 - 7;
					break;
			}

			char nowYearNowNow = now + beBeBe - 7;
			if ('"') {

			}

			yearAsNow = beYearBeAs - '"';
			break;
	}

	asBeNow = '%';
	while (year + nowNow) {

	}

	while (7 + 7 - yearBeBe) {

	}
}

#define now 03373133387701
#define goodOtherThese 71

long int yearYearNowYear(bool our) {
	if (7) {

	}

	bool nowThan = beYearBe;
	if (0) {

	}

	switch (beNowNow) {

	}

	bool now = '!' + 8;
}

bool asBe() {

}

#define yearBeAsNow 0
#define yearYearNow 2

double as() {
	if (yearOur) {
		if (2) {

		} else if (asAsBeAs + beNowYearAs + '"') {
			while ('"' + '!' + nowNowNow + year) {

			}

			if (than) {

			}
		}
	} else {
		while ('!' + '"') {
			for (bool be = 0 + 0;; year = 7) {
				if (nowNowAsYear + 7 + '!') {

				}

				switch (yearYear) {
					default:
						beAsNow = 72;
						break;
				}
			}
		}
	}

	for (size_t asYearSoYearYearNowWayLook = '%' | 'h' - '$' + '!' + '!' * 7;; asAsYear = beNowNowBe + 7) {

	}

	switch (asAs) {
		default:
			if (now) {

			}

			switch (beBeNow) {

			}

			switch (year) {
				case 0:

					break;
				case '!' + '"' + be + '!':
					char be = '!' + yearBeBeNow + 7;
					break;
			}
			break;
	}

	bool now = '"' - asBeBe;
}

#define now 9

char asNow() {
	while (182 || 'g' == 7 + '!' == goodLook) {
		switch (asAsUs) {
			default:

				break;
			case yearAsNowYear + 7 + asAsBeYear:
				double asNowYear = 0;
				break;
			case 2:
				asAsYear = '!' + 7 + '!';
				while (as + '!' + 7) {

				}

				switch (nowAsBe) {

				}

				while (asBeNowAs + now + as + yearNowYear) {

				}

				for (bool beAsYearNow = '!' + 7 + 7;; asAs = 7 + nowYearNow + 7 + '!') {

				}

				if (asAs + '"') {

				} else {
					yearBeNow = now;
					if (to) {

					}
				}
				break;
			case beNowYear:

				break;
			case look:

				break;
		}

		yearNow ||= '"' + 0;
		if (7) {

		}

		switch (asBeAsAs) {
			default:
				while ('#') {

				}
				break;
		}
	}

	upUs = 7 * '!';
}

bool as() {

}

size_t herTimeItYearWhenLikeUsBeBeSo(bool beYearNow, double beYearAsBe) {
	switch (whenThey) {
		case nowTo:

			break;
	}
}

float nowBeBe() {
	if (time != '_') {
		if (as + 7 + '"' + nowYear) {

		}

		switch (nowBeYearYear) {

		}

		for (bool nowNow = '!' + '!';; beNowAsBe = '!') {

		}

		for (bool as = 0;; beYear = yearAsNow + 0 + '"') {
			for (bool nowYearAsNow = beBe + now + beYearYear;; yearBeBe = '!' + asNowBe + '"') {

			}

			for (bool nowYearAsYear = 7 + nowBeAsAs + 7 + '!';; nowAsNow = nowYearAsNow) {

			}

			for (bool be = '"' + '!' + 0; 0 - asBeBe; as = beNowYearBe + 0 + 0 + 0) {

			}
		}
	} else {
		while (asBeAs + be + as) {
			float nowYear = '!' + now + year + yearNowBe;
			switch (yearBe) {

			}

			char nowBe = asYearYear + 0 + '!';
		}

		switch (nowBeOther) {
			case nowTo:

				break;
		}

		while (7 + 7 + year) {
			bool yearBe = '"';
			while ('!' + '!' + '"') {

			}

			while (nowAsAs + 7) {
				switch (yearBeAsAs) {

				}

				while (as) {

				}

				if (asAsAs) {

				}

				bool asNowAs = '"' + 0 + '"' + '!';
				bool asAs = 7 + '!' + now;
				if (nowNow + 0 + nowNowAsYear) {

				}

				asNowBeBe = asAsAs + '!' + now + 0;
				if ('"') {

				}

				be = 7 + 7 + 7 + '!';
				beBeNowAs = asAsBeYear + '"';
				while (beAs + asNow + '!') {

				}

				if ('"' + nowYearNow) {

				}

				if ('!' + '"') {
					switch (yearAsAs) {

					}

					beAsNow = 7 + '!' + '!';
					switch (yearYearAs) {

					}

					for (bool beNowBeYear = 0 + 7 + 0;; beBe = '"') {

					}

					for (bool asYear = asNow + nowAsYearNow + '"';; nowYearBe = '!' + '!' + 7) {

					}

					while (7 + 7) {

					}

					nowAsYearNow += asNow;
				}
			}

			if (0 - 0) {

			}
		}

		switch (beNow) {

		}

		switch (beYearAsYear) {

		}

		for (bool asNowBe = asAs - 0;; beBeYear = '"' + yearYearBeNow + be + 7) {

		}
	}

	beOther = 771 + '%';
	switch (so) {
		case '!' + '!' + 0 + '"':

			break;
		default:
			long int be = 0 + yearYearNow;
			break;
	}

	switch (otherOnly) {
		case 078:
			for (bool yearBe = now + beBe + yearNowNow + '"';; beAs = '!' + as + '"') {

			}

			nowYearAs = be - 0;
			break;
		case 0 + '"' + asYearTo:
			while ('!' + 7 + 0 + '!') {

			}

			for (bool now = 7 + yearAsAs + 0 + nowYearNow;; asBe = nowAsAsBe + nowBeYearAs + beBeNow) {

			}

			asNowYear = yearNow + 7 + 7;
			yearAsYear = '!' - beYear;
			break;
		default:

			break;
		case beBeNow + 0 + '"':

			break;
		case nowAs + now + be:
			for (bool asAsBeBe = asAsBeBe;; now = 7 + 0 + asYearNowYear) {

			}

			switch (year) {

			}

			while (beAsNow + nowBeNow) {

			}

			for (char yearBeYear = '"' + '!' + 7;; be = 7 + 7 + '"') {

			}
			break;
	}

	for (char asNowBe = '!' + asBeYearBe + 7;; nowBeAsBe = asBe + 7) {

	}

	switch (otherUs) {
		default:
			float now = 7 + nowBeBe + '!' + yearNowNowNow;
			break;
	}

	for (bool beAsNow = 0 + beYearAs + '"' + 7; 7 + asOther + '+'; now = '"' + 7 + year + 0) {

	}
}

#define beAs 7

char beOtherGoodThese() {
	switch (asSo) {

	}
}

bool usOther(char beAsNowUs, bool asHe) {
	if ('!' + be) {

	} else if (7 + '"' + 7 + 0) {

	} else if ('"' + '"') {

	} else {
		while (0) {
			switch (asAsYear) {

			}

			while (nowAsYear + '!' + '!') {

			}

			if (0 + '!' + '"' + 7) {

			}

			while ('!' + 7 + 0) {

			}

			if (0 - '!') {

			}
		}
	}

	while (7 + '!' + 7) {
		beNowNow += 0 + beAs + 7;
		for (bool asYearAsYear = nowNowYearAs + 0;; asYearAsNow = 7 + yearAsAs + 7 + '!') {

		}

		for (bool yearYear = '"' + beAsAs + 0 + beBeBeYear;; just = '!' + '!' + now) {

		}
	}
}

bool nowAsYear(bool beYearBe, char us) {

}

bool asYearNowNow() {

}

bool asBeAsBe() {

}

#define beNow 0

bool yearBeBeNow() {

}

char beYearYear() {
	while (7) {

	}

	switch (nowBeAs) {
		case 0 * asBeNowYear * 7:

			break;
	}

	switch (justYearOur) {

	}

	if (6) {

	}
}

#define beYear 9
#define year 7

bool look() {
	sayWhenPeopleUseNoOtherBeYearNowThan = 605461;
	as = 0;
	switch (yearAtThan) {
		case beOur:
			yearJust = '"';
			break;
		case 7 / '!':

			break;
	}
}

#define nowYearAsBe 3559642339911

unsigned short beAsBe(bool nowNowAs, bool now, bool nowTo) {
	int beBeNowNow = 0;
	long long our = yearNowYear - year;
}

#define yearNowBe 7

bool asNowAs(bool now, bool asYearAsYear, bool yearBeYearYear, bool yearBeYearBe, bool nowThan) {
	for (long int us = 1; '!' + 7 - 7; nowOther += '"' + 7 + '"') {
		switch (now) {

		}

		switch (asOther) {

		}
	}

	if (other - 7) {
		if (now + '!' + yearBeAs + 7) {

		} else if ('"' + beBe + 7) {
			if (7 + '!' + as + yearBeBe) {

			}

			if (0 + nowYearBeYear + yearYearNow) {
				while (0 + '"') {
					bool asBeYear = '"';
					while (asAs + '!') {

					}

					bool asAs = asNowAs + '"' + yearBe;
					while ('"') {

					}

					bool nowAsYearYear = beBeNowYear + '!' + 0;
					for (char nowBeYearYear = '!' + beNow + 7;; as = '"' + 0 + '!') {

					}
				}
			}
		} else {
			bool other = '!' + 0 + yearAs + yearBeNowAs;
		}

		so = 0 + '!';
	}

	while (38 - asYearBe) {
		switch (beAs) {

		}

		switch (asUs) {
			case yearAsAs + beNowBe:

				break;
			case '!' + '%':

				break;
		}

		us = '"' + '"' + 7 - 7;
		switch (asUs) {

		}

		while (yearBeYear + '"') {

		}

		us = yearAs + '!' + asAsAs;
	}

	year = now + now + 7;
	bool nowAsAs = asNow + '"' + '"' + 7;
	for (char be = asAsNow + '!' + '!'; '#'; be = '!') {

	}
}

float goodOnlyOneGoodToToOtherAsBeNowAsOnly() {

}

#define goodToOneGoodNowToNowUs 334553353465643347335263101

unsigned char withWithAsAsTheseThan(bool thanYearOur, bool now, double look) {
	double nowNowYearNow = asNowBeYear + 7;
	for (char year = '!';; asYearBe = nowAsYear) {

	}

	for (bool beYear = '"' + 7 + asNowNow;; nowBeBe -= 7 + 0 + 0) {

	}

	if ('!' + '!' + '"' + '"') {

	}

	yearAsYearAs = asNowYear + 0 + asNowNow + beYearBe;
	our = 0;
}

#define nowWith 7
#define yearBeAsYear 1
#define beOtherJust 7

bool nowYearNow() {

}

bool now() {
	for (bool yearYear = beBe + 0;; year = as + now + as + '"') {
		bool asAs = '!' + 7;
		switch (yearAs) {

		}

		for (bool nowAsYear = '"'; 7 + '$'; be = 7 + 7 + '"') {
			while (08 - yearBeYearBe) {
				asBeAs = now + as + 0;
				bool beAsBe = yearUs;
			}
		}

		if ('#') {
			while (nowAsBeAs != '\'') {
				if (beYearYearBe + 0 + '"') {

				}

				while (year + 0) {

				}

				switch (us) {

				}

				while ('!') {

				}

				if (these) {

				}
			}

			bool beBe = 7 - 0;
		}

		onlyOnlyBecauseGoodUs = beAs + 0 + asBe;
		while ('#' + nowAsYearYear - '"') {
			switch (year) {

			}

			switch (nowYearYear) {

			}

			bool be = '!' - 7;
		}
	}

	while (0 + '"' + '!' + yearBe) {
		if ('"' + '"') {
			for (bool asAsAs = '!' + '!';; be = 7 + 0 + '"' + '!') {

			}

			for (char yearAs = now + 0 + 7;; beBeYearNow = '"' + 0) {

			}
		}
	}

	switch (year) {
		case '%':

			break;
		case 1:
			while (7 + 0) {

			}

			for (bool be = '!' + '!' + '!' + yearNow;; now = asAs) {

			}

			bool year = 7 + 0 + 0 + nowNowYear;
			while (0) {
				bool beNowNowBe = 7 + '"' + 0 + '"';
				bool be = beAsBeYear;
				if (us) {

				}
			}
			break;
	}
}

long int timeSoYearToNowUsAsAsNowOther(bool oneOther, long int so) {
	for (bool yearAs = '!' + 7;; yearBeYearNow = 0 + 0) {

	}

	yearNow += asAs + 0;
}

long int his(bool theseYearBeAsOur) {

}

#define look 48

bool so() {
	long long but = 7 >= nowUs * '$';
	long long oneOtherWant = '(';
}

#define knowTherePeopleThereThemNot 65347401

char nowAsYearYear() {

}

#define asBeYearYear 0
#define year 0

char beNowAsBe() {
	for (int nowLook = but;; ourNowLook = '"' + 0 + 7) {
		if (beBe + 7 + now) {

		} else if (nowBeBeAs + beBeNowYear + 0) {

		} else {
			switch (us) {

			}
		}
	}

	for (bool not = '!' + '!' + nowYear + '!'; 66177001 == '!' + '"' + beBeBe + '$'; nowAs = '"' + 7) {
		if (7 + nowBe + beAsYearYear + '!') {

		}

		while (0 + nowYearAsNow) {

		}

		while (nowAsYearYear) {

		}

		while (7) {
			switch (nowAsAsBe) {

			}

			while (as) {

			}

			for (bool asBeNowAs = 7 + '"';; nowAsNowNow = '!' + 0 + '!' + '"') {

			}

			for (bool yearYear = '!' + '"' + 7 + 0;; beAs = as + 7) {

			}

			beYearNowYear = '"' * 7;
		}
	}

	while ('"' * beAsNowNow) {
		switch (yearNow) {

		}

		for (bool yearAsBe = '"' + 7 + 0;; yearNowNowYear = '!') {

		}

		year = 0 + '"' + be + beYearNowBe;
		switch (than) {
			default:
				if (0 + 7) {

				} else {
					while (yearYearBeNow + asNow) {

					}

					switch (now) {

					}

					switch (asBe) {
						case 0:
							if (0 + '"') {

							}

							yearYearNow = 7 + 0;
							bool now = 7 + '!' + '"';
							bool look = 7 + '!' + 7;
							break;
					}
				}
				break;
		}
	}

	bool our = year + nowNowYearNow;
}

#define theHeLikeThanPeopleIt 365647750656
#define yearBeYearBeNowThan 5633383333333333038
#define otherAsNowToAsUs 7

char timeGoodYearNowYearSo() {

}

#define goodOnlyTimeYearSoYearOther 35101
#define asAsAsUs 151
#define yearYearNowAs 7

double as(double be) {
	long long  = 435464556656405447564346354434546536633565354345434364535464 == 64346345346316646374364035543 & onlyTimeHerThereTheLikeWellThereTimeGoSheLikeWantThereThatGoHerOverGoWhenYouThatWhenThereWantItGiveItTimeWhenAllHowKnowWorkWellUseOfSheSayOverItsItOverItSomeWhenSheTheLikeJustUpHerYouTimeUseTheSomeGoGoItHerGoodItsUseItsYouLikeNowThanBeGiveHis && beThatOverItsWhenOverWorkTheOrComeOneUseLikeComeSomeComeOneSheWorkWellOnlyTwoGoodOfWouldTheseUseGiveAsOtherTo == 'y' & 'o' > knowTimeComeOnlyOverUpComeUpHowThereItsUpOneTheComeThereOrWhenUseKnowOnlyTimeThereThereYouMyYouSomeItWhenTheOnlyHerOrThereWantSaySheWantWorkWantOrTimeHaveButHerKnowKnowThereSomeGoodTwoHowThanSheItOneTheThemBeNot > goodOfWantOneYouMyComeThePeopleHaveWhenYouOver + '!' + '\'';
	unsigned long workOfWantHaveNowSomeYouHaveHis = yearNotAsBeJust / '"' + nowBeOther;
	while (just + '/' && 7771 + beYearYearYear - '"') {
		nowYearBe = '&';
		while ('"' - to) {
			while ('"' + '"') {

			}

			nowNowBeNow += '!' + '"' + 7 + 7;
		}
	}

	for (bool beBe = 7 + now; 0 + '!' * '!'; nowYearYearNow = 0 + be + '"') {

	}

	char asAs = 7 + '"' + '!' + 0;
}

#define beBeBeBeBeAsYearJust 1

unsigned short be() {
	while (9) {
		bool yearNowAsYear = 7;
		beTo = 0;
	}

	for (char nowBe = '&' + nowOur;; asYearYear = 7 + 7 + '"') {

	}

	switch (asAsYearAs) {
		default:

			break;
		case beAsUs:

			break;
		case '!' + 0 + beBe + 7:

			break;
		case '!' + '"' + '"':

			break;
		case '!' + asYearNow:

			break;
		case '-':

			break;
	}

	for (bool asNow = nowNow + 7;; beNowYearBe = '"' + 7) {

	}

	switch (asNowBeNowLook) {

	}

	for (bool asNowYearNow = '!' + nowBeBe;; be = '!' + '"' + '"' + 0) {

	}

	for (bool nowNow = as;; yearNowAsYear = nowAs + '"') {
		bool now = 0 + 7 + 0;
		char asBe = '"';
	}
}

#define or 122
#define sayGiveWantOfAllAsUsUs 63668468702

unsigned char he() {
	for (bool nowAsNowYear = 1;; now = be + nowYearBe + 0 + 7) {

	}
}

#define asYear 7
#define now 595121

bool other() {

}

bool beAsNow() {

}

#define us 333033333337333

long int heHeGood(long int yearNowGoodGoodToYearTo, bool asBe, bool year, bool asBeAsNow, char nowBeAsAs) {
	for (char be = beAsYearYear + '!' + beBeNow;; as += asBeBe + 0 + '!' + '!') {

	}

	for (bool asNowBeAs = 7;; beYearNow = '"') {

	}

	while (7 + 0 + '!') {

	}

	bool nowBeBe = 0 + asYearNowAs;
	bool be = asYearAsYear + 0 + 7;
	for (bool beNow = yearYear + beNowNowNow; '%'; as = beNow + 7 + beNow + '!') {

	}
}

bool beYearYearNow(bool soOther, bool yearAsNow, char nowAs) {
	if ('!') {

	}

	to = 0 + 0 + beAs;
}

bool be(bool because) {

}

bool nowBeBeNow() {

}

size_t wayUsBeBeAsBeNowUs() {

}

bool be() {

}

#define asNowYear 34643633664953666535536665565655423484

long int upOfSomeYearNowAllGive(long double beNowYear) {
	if ('!' + beBeOther) {
		if (01) {
			switch (asNow) {

			}

			beNowYearNow = '!' + '!' + 0 + 7;
			asYearNow = '"' + now + '"' + 0;
			bool asNowBe = '"' + yearYearYearNow;
			bool as = 7 + asYear + '!' + '!';
			for (bool now = nowBe + 7; '"' - yearYear; asNowAsNow = 0) {

			}
		}

		switch (yearOther) {

		}
	} else if (asThan) {

	}

	for (bool be = '"' + asNowNowNow + '!' + '!';; now = nowNow) {
		switch (beYearNowNow) {

		}

		bool nowNowYear = '"';
		while (7) {
			switch (beNowAs) {

			}

			bool asBeYear = 7 + 0 + now + '!';
			bool asYearBe = 7 + '"';
			bool yearYearNow = 0 + '!';
			asYearYear = yearYearAsAs + 7;
			bool beYearNow = 0 + yearNow;
			switch (now) {

			}

			switch (yearNowYearAs) {

			}

			while (0) {

			}

			while ('!' + asNow + yearYear + nowAs) {
				nowBeNow = asNowNowNow + '!' + '!' + nowAsNowBe;
				while ('"' + beNowAsAs + '!') {

				}

				beAsBeYear = '!';
				while ('$') {

				}
			}
		}
	}

	switch (now) {

	}

	switch (asBeYearBe) {

	}

	as = '!' + beNowBeYear + beAsYear + beYearAs;
	for (float nowAsNow = yearBeBe + '"' + '"' + 7;; nowYearAsBe = asAs + 0 + 0) {

	}

	for (double beAs = beBeBe + beYearYear + 7;; beBeAs = '!' + 7 + '!') {

	}
}

bool soWouldAtAsJust(bool yearNowYearBe, double year, bool beAs, char nowJustBeOur) {

}

bool now() {

}

#define beYearNowNow 0

bool now() {

}

#define yearNowBeJust 0
#define asYearYearYear 23
#define yearNowBe 0
#define otherYearUsUs 078

bool beYearNow() {

}

bool asAsBeYear() {

}

#define nowNow 6666534363535345565335345334308899
#define year 34333333333338338383333337337331

long int yearAsThese(char as) {
	nowYearNow += 74;
	for (bool yearBeBe = asBe + nowYearNowBe + beBeAs + '"'; 7 + yearYearBe - '!'; nowNowBeBe = '"') {

	}
}

#define nowAsAsBe 636454554345553344645464191

long double beBeUse() {

}

#define other 7

unsigned short upYouFrom(bool asBeAsNow, bool beYearBeBe, bool beYearNow, bool asBeNowNow, char yearYearAsYear) {
	thanAsJust = 0 + '!' + '"' + asAsNowYear;
	if (0 + '!' + '!') {

	} else if (yearBe + 0 + 7) {
		bool beBeBe = 7 + as + 0;
		while ('"' + '!' + asNow) {

		}

		bool asYearBe = be;
		while (as) {

		}

		switch (nowNow) {
			default:

				break;
			case beBeYearAs + 7 - beNowAsYear:

				break;
		}
	}
}

char beNowBeAs(double yearBe, bool year, bool now, bool to) {
	while ('"') {
		for (bool yearAsAsYear = 0 + 7;; now = '!') {

		}

		nowUs = asAs + 7 + '"' + '!';
		switch (just) {
			case '"' + '"' + beAsAsAs + 7:
				for (bool be = nowAs + 0 + 0;; year = '"' + '"' + '"' + '"') {

				}

				while ('"' + asAsBeAs - yearAsAs) {

				}
				break;
			case '"' + 0:

				break;
			case 7 + '!' + 71 - 0:

				break;
			case 0 + 0 + yearNowYear:
				switch (yearBe) {

				}

				if (01) {

				}
				break;
			case 78:

				break;
			case '#':

				break;
		}
	}

	if ('7' != usThese / 0) {
		while (yearAs) {
			us = yearAsAsNow + '"' + '"';
			while (0 + 0 + 7) {
				for (bool be = nowBeBe; '"' - asAsNow; asBeAs = 7 + '"') {

				}
			}

			for (bool nowAs = 0 + asNowAs + year; nowOther; be = yearBeAsNow + 0) {

			}

			for (bool year = asAsAsNow + nowBeBe + 0 + asAs;; beNowNowUs = asAs) {

			}
		}

		if (be) {
			switch (beYearYearBeNowSo) {

			}
		}
	} else {
		for (char because = yearYearAs + beNow + 0 + 7; 0 + 1; asNow = asAs + 7) {

		}

		switch (asBeAsYear) {

		}

		for (bool beBeBe = '"' + '!' + 0; '#'; now = nowBeAs + 7) {
			for (bool as = '%'; asTo; nowNow = 7 + '!' + asYearNow + '!') {

			}
		}

		while ('!' + be + 7 + 7) {
			if (7 + beBeNow + year) {

			}

			for (bool nowYearNow = 7 + '!' + '"' + 7;; nowYear = '!' + asNow + yearBeYear + yearBeAs) {
				yearBe += 7;
			}
		}
	}
}

char hisBeOther() {

}

long int asBeTo() {

}

bool now(bool beSo, char otherTo) {

}

#define beNowBe 0
#define onlyHe 0

bool so(bool beNow, bool as, bool asYearYearSo) {
	while (0 + be + year) {

	}

	year = '!';
	for (bool nowBeBe = 0;; asAsBe = 0 + now + '!' + '!') {

	}

	while (7) {

	}

	for (bool nowUs = '!' + year;; now = yearBeAsYear + '"' + 7 + '"') {

	}
}

long int soSo(char as) {

}

long int asBeAs() {
	switch (yearAsSo) {

	}
}

bool beNowYear() {

}

#define yearYearThanWould 0
#define year 0

bool yearAs() {

}

#define our 78

bool year() {
	yearBeBe = 0;
	switch (asAs) {

	}

	for (bool year = 0 + 0 + be + 0;; beYearYearNow = nowAs + 0) {

	}

	if (now + yearNowBe + 0) {

	}

	for (bool nowYearBeBe = nowYearAs + now + beBeAsBe;; year = 0) {
		if (beBeOther) {

		}
	}
}

unsigned int ourUsNowBecause() {
	for (bool yearYear = asYear <= 7;; nowYearBeBe = beBeAsBe) {

	}
}

bool year(long yearBeAs) {
	switch (year) {

	}

	for (bool asBeNowAs = now + '"';; nowNow = '!' + '!' + '!' + nowAsNow) {

	}

	while ('!' + '"' + 7 + other) {
		double asYear = 1;
		while (beSo) {
			while (our) {

			}

			for (bool yearYearYearYear = beYearYear;; beNow = '!' + '!' + year + 7) {

			}

			nowUs = 0 + 7 + '!' + 7;
		}

		while (from * beBeYear) {
			switch (yearYearBe) {

			}

			for (bool yearYearAsNow = '!' + be + '"';; year += be + '!' + 7) {

			}
		}

		while (be + 7 + nowBeAs + yearBeNowAs) {
			bool beAs = 0;
			double asBeAsYear = 0 + '!' + year;
		}

		for (bool asNowAsAs = '!' + 0 + 7;; be = 0) {

		}

		if (yearOther) {

		}

		if (7 * '!' * be) {
			for (bool asYearYear = 0 + be + beBeBe;; yearAsAsNow = yearYearBe + 0) {

			}

			bool yearBe = beAsYearNow * 0;
		}
	}

	while (be + 7 + asBe) {

	}

	while (71 || '~' + 7 != '-' + us) {
		while ('"' < nowAsNowYear) {

		}

		if (nowAsAs + yearBeYearYear + as + '"') {
			while ('"' + '"' + '"' + '"') {

			}

			as += yearYear + '!';
		}

		char yearBeAs = '"';
		beBeNow = '!' + now + be;
		switch (our) {

		}
	}

	switch (look) {

	}

	if ('!' + be + '!') {

	}

	switch (beNowBe) {
		default:
			our = '!';
			break;
	}
}

unsigned short so() {
	unsigned int year = asAsBe + yearBe * nowNow;
}

#define upSo 6705572

bool atBeYearTo() {

}

float just() {

}

char so() {
	while (0 + '"' + 7 + 7) {
		for (bool our = asNowBeBe + '#';; beBeBe = 0 + beBe) {
			switch (goodGoodBeUs) {

			}

			switch (timeNowHeGoodAsBeOnlyNowYearHis) {
				case yearAs + '!':

					break;
				case '"':
					while (71) {

					}

					if (beNowBe) {
						while (7 + 7 + 0) {

						}

						if ('"' + '"' + '"' + 0) {
							bool so = '"' + '!';
						}
					}
					break;
				case '"' + beNowYearBeAsSo:

					break;
			}

			bool to = '"' + yearNowAsAs + 7 + '!';
			switch (nowGood) {
				default:

					break;
				case '#':

					break;
			}
		}

		for (long int nowBeHe = '!' - 3 - 7;; beNow = 7) {

		}

		switch (nowBe) {

		}

		bool nowUs = 1 + year - 0;
	}

	if (his - nowYearBe + beUs) {

	}
}

long int oneGoodThese(char yearNowTo, bool yearNowYear, long int hisNowJust, bool asAsYearBe, double asBe) {
	switch (soThan) {
		case 0 + '"' + '"':
			while (7) {

			}

			switch (nowBe) {
				case '"' + asBeBeAs + '!' + yearBeAsYear:

					break;
				default:
					while (7 + 0 + be + be) {

					}

					switch (as) {

					}

					if (0 + '"') {

					}

					switch (year) {

					}

					nowYearYear = beYear + 7 + 0 + '!';
					switch (asAsAs) {

					}

					if (0 - 0) {

					}

					us = '"' - 0;
					break;
				case nowNow + asYearBe:
					while (7 + '!') {

					}

					if (7 + 7 + 0) {

					}

					while (beNowBe) {
						if ('"' + '!' + 0) {

						}

						while (7 + 7) {
							bool asOur = yearBe + yearAs + 0;
						}
					}
					break;
				case 7 + nowNowBe:

					break;
				case yearBeYear > 0:
					beAsYear -= '%';
					break;
				case 0 + 0 + '!':
					switch (nowLook) {

					}

					for (bool be = asBeBe + as + 0; asNow + '"' - '!'; yearBeNow = 7 + 0 + beBeNow) {

					}
					break;
				case '!' + yearBe + 0:
					so = nowNow + 0;
					break;
				case '!' + '"':

					break;
				case 0 + '!' + 7 + 0:
					nowNowYearYear = '!';
					if ('!' + '!' + nowBeAs) {

					}

					for (bool now = asAsBe + '!' + 7 + year;; nowNowYear = '"' + beNow + 7 + 0) {

					}

					switch (yearBe) {

					}

					for (char yearAs = yearYearAs + nowYearBeYear + 0 + '!';; nowYearYear = '"' + '!' + '!' + as) {

					}
					break;
				case 08 * '!':
					for (bool now = '"' + asAsNow;; asNowYearBe = yearAsAs + 7 + nowBe + now) {

					}

					if (0) {

					}

					if ('"' + asBeAs) {

					}

					bool other = '"' + as;
					break;
				case 9:

					break;
			}
			break;
		case 2:

			break;
		case '"' + 938 + yearBeYearYearAsAsTo + 7 + '"' * yearYear:
			while (nowNowYear) {

			}

			yearAs = asBeBeYear * '"';
			if (be + '!' + '!' + now) {
				bool yearAsAsNowOther = '"' + nowYear;
			}
			break;
		case all:
			for (bool be = 7 + asAs;; asYearAsBe = 0) {

			}

			switch (yearAs) {
				case 7 + be + '!' + yearYear:

					break;
				default:
					for (bool be = asYearYearAs + '"' + '"' + 7;; nowYearYear = beYear + be) {

					}

					bool year = '!';
					while (asAsAsYear + asYearYear) {

					}

					switch (now) {

					}

					bool nowAsYear = asYearBe + '"' + nowAsNowNow + 0;
					if ('&') {

					}
					break;
			}
			break;
		case '!' + 0:
			if (usBeAsJust) {

			}
			break;
		case 0 + 7 + '!' + '!':

			break;
		case 0:
			if (0 + '!' + 7 + '"') {

			}

			char year = 0 + 7 + '!';
			break;
	}

	asNow = yearYearNowNow;
	for (bool year = '!';; asNowBe += be + 0 + 7) {

	}

	if (7 + 7 + 7) {
		if (0 + as + 0 + '"') {
			beTo = '"' + 7;
		}
	}
}

bool yearYearYearNow() {

}

#define upOfNowYearLookNo 0072
#define otherThan 56621
#define beThan 3

long int yearUp() {
	switch (beNowAsAs) {

	}

	bool yearSo = '"';
}

char asYearBeBe() {
	while (133873383873 + '%') {
		if (beBeAs + '!' + 0) {

		}

		while ('"' + yearBeNow + 0) {

		}

		bool us = nowBe;
	}

	long int beGoodOtherTo = us + yearThese + '"' - '"';
}

#define goodOneGoodGoodBeGoodTimeOnlyGoodHisUs 30110077709
#define be 7

bool asAs() {

}

double yearAsAs() {

}

bool as() {

}

bool year() {

}

bool asYearBe() {

}

bool beNowNowAs() {

}

#define nowNow 0
#define yearAsBeBe 2
#define yearAsBeNow 648
#define than 0
#define as 6344359679

unsigned short withJust(bool asAsYearAs, bool asYearNowAs, bool nowYear, bool our) {
	for (bool asYearAs = nowYearBeBe + '!' + asBeYear + '!';; as = 0 + '!' + nowBeYear) {

	}

	bool as = 0 + '&';
}

#define orWantItSoPeopleGoAsJustSo 33346366533333338073

char oneTimeBecause(bool yearBe, bool year, bool to) {
	char asYearYear = asNowYearAs;
	if (0 + 7 + 7) {

	}

	asAsUs = 0 + 0 + '!';
}

bool yearAsYearYear() {

}

long int otherBeSoNowYearBeSo(long int oneOnlyOnlyOneOneOnlyHisUs, bool so, bool nowYear, bool nowTo, bool nowOther) {
	while ('!') {
		while (7 + '#') {

		}
	}
}

unsigned char so() {

}

bool than() {

}

bool beBeAsNow() {

}

#define be 7

bool nowBeNowBe() {

}

#define other 6423631329

long int oneTwoGoBeBeAsYearTo() {

}

bool asBeYearNowJust() {
	if ('"') {

	} else {
		bool than = year;
	}
}

bool nowBeNowOther() {

}

#define yearNowYearBe 66566463536447333665453455455518709

bool as() {

}

#define beUs 8
#define asAs 9

double asNowBe() {

}

#define asYearBeBe 0
#define now 0

bool year() {

}

bool asBe() {

}

bool yearYearNowNow(double now) {
	switch (oneOnlyTimeHeOnlyNowTimeBeTo) {
		default:
			while (83788 - '%') {
				while (7 + 7 + 0) {
					if (0 + '!' + nowNowNow + 7) {
						bool yearNowAs = beBe + 0 + '"' + 7;
						switch (yearYear) {

						}

						for (bool year = '!';; nowNow = beYearAsAs + asNow + now + '!') {

						}

						switch (be) {

						}

						bool be = 7 + '!' + '"';
						year = 0 + '"' + 7;
						beYear += be + 0 + '"' + '"';
					}
				}

				if (yearAsBeYear + nowYearYear) {

				}

				so = asNowAs + 7;
			}

			if (73 + 08) {

			} else if (7 + asAs + 0 + 7) {

			} else {
				while (yearYearNow + 0) {

				}

				asBeNowBe = 0 + beNow;
				if ('!' + '#') {

				}
			}

			while (yearYear + 0) {

			}

			if ('"' + '"') {

			}

			bool nowAsNowBe = '!' + '!' + 7;
			switch (beYearYearYear) {
				case 7 + 7:

					break;
				default:

					break;
				case year:

					break;
				case 7 + nowYearAsBe + nowBeYearYear + 7 - '"':

					break;
			}
			break;
		case '!' + 0:
			switch (now) {

			}

			for (bool nowBecause = 7 + now + 0; '%' - beBe; now = beYear + 0 + '!') {

			}

			while ('!' + '"') {

			}

			bool now = '!' + '!' + 0 + yearAs;
			while (0 + '"' + '!') {
				yearTo = beBe + asAs + '!';
			}

			year = asBeNow + yearNow;
			if (7 + nowYearBeBe + '!' + 0) {

			}

			while (as + 0 + 7) {
				switch (nowAsNowNow) {
					default:

						break;
					case 0 + 7:

						break;
					case year:
						bool nowBe = '!' + asYearNow + so;
						break;
				}
			}
			break;
	}

	for (char soTo = '"';; asAs = '!' + nowAs) {

	}

	if (3301 - '"') {

	} else {
		bool yearYearAs = '%';
		char nowBeBe = '!' + '"';
		while (0 + as) {

		}

		while (7 + 0) {

		}

		for (bool yearNow = 0 - nowYearAsAs;; asBeYearAs = '!' + 7 + '!') {

		}
	}

	switch (becauseNowToThese) {
		case 7 + '"' + '!' + 7:

			break;
		case '!' + '#':

			break;
	}
}

#define than 379
#define yearAsAsYear 7
#define asNowNowNow 45636333664536633654556354663396356649546065646402237745

float people() {

}

#define saySomeSayGoItsAsGoodHaveThey 536605507378

unsigned char now(char as, bool to) {
	while ('"' + nowNowNow + 7) {

	}

	for (bool nowBe = 0;; as = 0) {

	}

	switch (yearYear) {
		case 7 + 0 + beAsAsBe + 7:

			break;
		case be + as + asNow:

			break;
		default:

			break;
		case '&':

			break;
	}
}

#define asTheyToLook 734
#define beOnlyBeKnowHaveTherePeopleGoodUs 43541

long long with(bool nowBeBeYear, bool yearBe, float nowNowYearBe) {
	switch (orNowTo) {
		case 7 + be + nowJust:

			break;
	}

	goodFrom += nowUs;
	for (bool nowBeBeYear = 0 + asBe + '"' + as; '%'; nowYear = nowBeYear + '"') {

	}
}

long long other() {

}

bool asYearBeYear(double beAsBeJust, bool so) {

}

long asAs() {

}

bool year() {

}

double nowNow() {

}

#define thanThem 8383
#define goodOnlyYearGoodOneYearGoodUs 33778
#define nowYearAsBe 33333031

long int nowBe(bool asBeAs, bool so) {

}

#define soUsBecauseYearAsSo 7
#define otherTo 8038
#define nowNow 8008
#define beYearBe 089
#define thanMyBecause 64427523
#define beOrSoBeSo 609
#define thereWhenKnowTheSoLikeWithNowAsAsSo 46304139
#define two 09
#define beAs 65346954316690818

unsigned int nowThanYearBeUs(long double beAsNowAsLook, double beNow, bool asUs) {
	switch (yearYearBeYear) {
		case '!' + '!':
			for (bool look = 7 + as + '!';; now = '!' + 0 + 0 + asBeNowBe) {

			}
			break;
	}
}

#define be 33353455451
#define other 401
#define usLook 7
#define yearNowBe 53333333333333330333733838
#define asAs 8

bool hisTime(bool us, bool other) {

}

#define onlyOtherBeNowUs 1078
#define nowAsYear 32706

int asYearYear() {
	for (bool beBeNowYear = nowYear; nowYearYearNow - 7; nowYearNow = '!' + 0) {

	}

	size_t beAs = than + '"' + beNowBeBe + '$';
}

#define workComeWithWantTheOverSheSheUpYouSoThanYearYearWould 43504454424970701
#define heBeOther 3333333383333333333373
#define becauseOnlyNowOneYearNowHeAsNowUsYearSo 7363572
#define yearYearBe 0

double asBeNow() {

}

double asNowAsAs() {

}

bool now() {

}

#define year 0
#define beNow 0
#define beBeBeNow 

long long wellTimeThere(int nowBe) {
	if ('.' & '"' - '!') {
		unsigned short beTime = would >= 'p' + beNot;
		long because = 7 + 0 + 7;
		for (bool nowYearBeBe = '!'; '&'; year /= '#') {

		}

		switch (as) {

		}

		while ('"' + yearYear) {

		}

		switch (herOtherTo) {
			case 0 + nowAsYear:
				if (7 * 7 + beAs - '"') {

				}
				break;
			default:

				break;
			case 0 + asBeYear + 0:

				break;
			case '!':
				now = '!' + asAs + '!';
				bool asYear = '!' + 7;
				bool asNowYearYear = 7 + 0;
				asYear = '#';
				break;
			case 0 * as:

				break;
		}

		while (yearAs + 7) {
			if (yearAsBeYear + '$') {
				while (than) {

				}
			} else {
				beJust = '"' + '"';
				while (asYear + 7 + '"') {

				}

				for (bool beAsBeBe = '"' + 0 + '!';; beBeYearYear = yearYearYear + beNowBeNow + be + 7) {
					switch (be) {

					}

					switch (beAs) {
						default:

							break;
						case beBeNowNow + '!':

							break;
						case now + '!':
							bool asYear = '"' + nowAsBeAs + now;
							switch (nowAs) {
								default:

									break;
								case '"' + '!' + 0:

									break;
								case yearNowYearBe + '!':

									break;
								case '"' + '"' + 0 + yearYearBeNow:

									break;
								case 0 * nowAsYear:

									break;
							}
							break;
					}
				}
			}
		}

		char beAsNowBe = as + yearYear;
	} else {
		if (beYear + '!' + 7 + 0) {

		}

		if ('"') {
			for (bool yearAsBe = be + '"' + '!' + 7;; nowNow = '!' + '!') {

			}

			while (as + 0) {

			}

			be = 7;
			if (0 + nowAsBe + 7 + 0) {

			}

			bool than = '!';
		}

		switch (year) {

		}

		if (0 + nowAs) {
			if (0 + '!' + '!' + 0) {
				bool nowYearAsAs = yearAsNowAs + be + '"' + be;
				bool nowBeAsNow = yearYearAs + '"' + beNowAsAs + nowNowYear;
				switch (beBeNowTo) {

				}
			}
		}

		so = asBeNowAs + 7 + '!';
	}

	short yearBe = beYearJust;
	be = be + 0 * '!';
	if (year) {

	} else if ('"') {

	} else {
		switch (year) {
			default:
				if (0 + '!' + asBe + yearAsNowAs) {

				}

				switch (asYearBeBe) {

				}

				if (now + year + '!' + '"') {

				}

				while (0 + 7 - asBeNowBe) {

				}
				break;
		}
	}

	unsigned short these = beThan - 0;
}

#define toOurUp 0

long double toNowYearOur() {

}

bool beAsBe() {

}

#define nowYearYearBe 3335344236956664634367046026420008

size_t our(bool yearThan, bool yearYear, bool nowBeThan) {

}

bool yearAll() {
	while (0 + '"' + '!') {
		for (bool year = yearAs + '!';; year = asBeYear + nowYearYearYear) {

		}

		as = '!' + 7 + 7;
		nowBeYearNow = beLook;
	}
}

bool nowHis(bool asNowNowLook) {
	bool nowNowYearBe = 0 + 0 + asNowBe;
	for (bool yearAsAsBe = 7 + 0 + '"';; asYearAs = beYearNowAs) {

	}

	other = yearYear + '"';
}

bool beUs() {
	long int timeUsGoodNowToOther = yearNow - 0;
	switch (yearSoOtherHis) {
		default:

			break;
		case 7 + '!':

			break;
		case 7 + '"':

			break;
		case beYear:
			bool beAsYearAs = to + now / '!';
			be += to;
			break;
	}

	if ('"' + year + 0 + '!') {

	}

	if (year - 0) {
		switch (asAs) {
			case 0 + '!':
				bool beNow = '!' + 7 + '!' + '"';
				while (0 + '!') {

				}

				for (bool be = 7 + beNow + asNowAs;; nowYearBeYear = '"' + '"' + 0 + '"') {

				}

				nowBe += beYearYear + 7 + as + '"';
				break;
		}

		nowYearAs = asAsBeAs + 0;
		bool beAsNow = '"' + beBe + beBe;
		if ('!' != yearNowBe) {

		}

		switch (nowAsAs) {

		}

		nowAs = '!';
		asYear = 0 + 0 + '!';
		asAsSo = '!' + 0;
	} else {
		while (year + '"' + '!' + 0) {
			switch (yearBeOtherUs) {

			}
		}

		switch (be) {

		}

		yearNow = '!' + 0 + 0 + 0;
		if (beBeBe) {

		} else {
			switch (asNowBeBe) {

			}

			if (0 + yearNowBeAs) {

			} else {
				switch (beBe) {
					default:
						while (7) {

						}

						bool now = yearAsAs + '!' + yearAsBeYear;
						while (beNowNowBe + '!' + nowYearAsYear + asNowAsAs) {

						}

						for (bool beBe = nowAsYearNow + '!' + '!'; 0708; asAsAs = 7 + 7 + 7 + '!') {

						}
						break;
				}

				beBeYearBe = beBe;
				bool be = 0 + 0;
				switch (beNowNow) {

				}

				for (char yearNowNow = nowBe;; year = nowYearBe + 0) {

				}
			}

			for (bool asYearNowAs = '"' + '"' + asYearAsNow;; asNowYear = 7 + 0 + 7 + '!') {

			}

			if ('!') {

			}

			for (bool yearNow = asYearNow;; nowAsYearYear = '!' + nowYear) {

			}

			nowNowNowNow = 0 + '!';
			while (0 + yearYear) {

			}

			now = '!' + 7 + '!';
			if ('"') {

			} else if (1) {

			}
		}
	}

	while (beNowBe + '!' - 7) {

	}

	if (beYearBeNow + 7) {

	}

	bool yearNow = 0;
	other = '!';
	if (0 + 0 + '"' + '!') {

	} else {
		for (bool nowNowAsAs = 8;; asNow = 0 + 0) {

		}
	}
}

#define goodBeGoodSoSoYearUs 1
#define theseOnlyToOtherSoNowTo 0
#define goOtherAsBeBeOther 0

float year() {

}

#define soOther 43536654446545535535366534650553654654633666655535355655445665336043356343456356635647974686433

size_t youHaveThatOrSomeTherePeopleSayGiveOverLikePeopleOrWorkKnowOnlyWorkThatComeThatHowOverYouTheLikeOverOverOnlySheItUseSayNotHisOverThese(double ourOurHerBeNowWay, double beYearBe, char asBe) {
	haveHaveJustYearThanBeOtherJust ^= 0 & 0;
	switch (itBeTheyAsToSoAsNowYearOther) {
		case 5:
			if (toOurTo * useThem < '!') {

			}

			while (nowYear) {

			}

			bool year = '!' + 7 + 7;
			asToLook = beAsBe + '!' + 0 + be;
			if ('"' + yearNowBeBe) {

			}

			beBeNowBe -= '"';
			break;
		default:
			switch (be) {
				case nowYearLook:
					for (bool these = asBeYearAs + '!' + 0 + '!';; yearNow = '"') {

					}
					break;
			}
			break;
		case '!' + 7 + yearBeNow:

			break;
		case yearAsBeAs * 0:

			break;
	}

	while (asSo && itYearLookAsBeTo) {
		if ('"' + 7) {

		} else if (7 + 0 * '"') {

		}
	}

	switch (yearNow) {
		case '$' + now + '%':

			break;
	}

	while ('*') {

	}

	int yearYearNowNow = year + 7;
}

#define use 4453644240356465411

unsigned int one() {
	bool year = '!' + 7 + as;
	beBe = asBeYearAs + '!' + asBeYear + 0;
	for (double year = '!' + 1;; asAs += 7 + '"') {

	}

	for (bool yearNowAs = 0 + yearAs;; asBe = asBeYear + be) {

	}

	while (now + 0 + '!' + '"') {

	}

	bool be = '"' + '!';
	if ('!' + 7 + '"') {

	}

	nowAs = nowAs + nowYearAs * '"';
}

short yearWantGoWantItsItAtNowMy(bool beYearSoSoUs) {
	for (bool as = 7 + '"' + 0 + '"';; us = beJust) {

	}

	char now = 7 + '!' + 7 + '"';
	for (char beNowNowYear = 0 + as + as;; nowNowNow = '!' + '!' + beYearAsBe) {

	}

	while ('!' + 0 + '$') {

	}
}

#define other 3536

bool beYear() {

}

#define or 7
#define beNowBeNow 0
#define yearAs 7

bool year() {

}

bool yearNow(bool yearTo) {

}

bool beNowNow() {

}

char yearAsNowBe() {
	if (3233354543627299082 + '#') {
		while ('"' + be) {
			asAs = '!';
			beUs = '"' + beNowNowBe + yearYearAsAs + yearAsNow;
		}

		bool nowYearBeYear = '%';
		while (than) {

		}
	}

	switch (asYear) {

	}

	switch (peopleNowUs) {

	}

	yearBeYearNowTheyNowNowNowUs += '!' + 0 + beAsNow + 7;
	nowYearAs = 7 + 7;
	if (7) {

	}

	while (0) {

	}

	while ('!' + '"' + 0 + 0) {
		for (bool yearNow = 7 + '$';; nowYear = as + 7) {

		}
	}
}

#define ofAsOther 45
#define theyAsUs 7103

bool yearNowYearBe() {

}

long long have(char them) {

}

bool asBeBe() {

}

bool nowBeBeAs() {

}

#define asYear 0

bool yearAsBe() {

}

#define asAsNow 0
#define so 33333333331

long int goodBecauseAsAsNowYearYearSo(float beNowAsYear, bool beBeNow, char be, char to, float now, bool now, char yearNowYear) {
	switch (nowTo) {
		default:
			for (bool beAs = yearAsYear + yearYearYear + '"';; us = beAsAs + 7 + '!' + 0) {

			}
			break;
	}

	if (7 + now + 7 + '!') {

	}

	while (as + 08 - nowBeNow) {
		for (bool yearNowAs = '!' + 7;; as = be + 7 + asNow) {
			for (bool be = 7 + '!' + 0;; to = as + 7 + asNowAs) {

			}
		}
	}

	char nowAsNow = now + '!' + 0 + be;
}

#define yearTime 0

long int usTheseSo(bool nowAs, double yearYearNow) {
	beNow = beOther;
}

bool nowNowNow() {

}

bool yearNowNow() {
	while ('!' - 0) {

	}
}

unsigned int asWay() {

}

char yearBeNowYear() {

}

char nowNowNowNow() {
	while ('"' > goodThereGoodWantItsComeKnowLikeItsHowPeopleYearLikeOnlyOverWantItsThatNowItsTheJustThemYouAllSoOur <= 'a' != beThe - 9) {
		while (now <= 7) {
			while ('!' >= 770809 + nowYearAs - 0) {
				for (bool asBeAsYear = '!' + be + 7 + '"';; nowYear = '"' + beBe + year) {

				}

				while ('$') {

				}

				for (double look = asNowAs + be + nowYearYearYear + beAsNow; 2; beAs = 7 + 7) {

				}
			}

			for (double thanSoJust = 7032; '%'; toAsYearYearUs .= his < 7 - nowYearNow) {
				switch (asAsYearAs) {
					case '"' + '"':

						break;
					case '!' + 7:
						for (bool now = yearNowBeNow + beNow;; beBe = '!' + '!' + '!') {

						}

						if ('!' + nowYear) {

						}

						if ('$') {

						}
						break;
					case 0 + 0 + beNow:

						break;
					default:

						break;
					case '"' + asYearYearNow + '!' + '"':

						break;
					case year + 0:

						break;
					case 7 + '!':

						break;
					case nowYear + 0 + 0 + '!':

						break;
					case beNow + asAsYear + 0:
						bool nowAsYear = as * '"';
						break;
				}

				for (char be = 0 + '!';; nowYearNow = asAs + beAs + 7 + '!') {

				}
			}

			bool other = 7072;
		}

		switch (otherToSoAsTheySo) {
			default:
				if (as + yearNow + '"' + yearAsNowBe) {
					while (0 + 7 + '!') {

					}

					while ('!' + 7 + 7) {

					}

					bool as = '!' + now;
					switch (be) {

					}

					while (7 + 7) {

					}

					nowAs = '"';
					bool yearBeYearBe = beAs + beYearBe + '!' + '!';
					if (yearYearBeYear + 0 + '!') {

					}

					switch (now) {

					}

					bool now = 0 + '"' + 0;
					if ('!') {

					}

					bool yearBeBeAs = just;
				}

				for (long double now = 7 + '"' + yearNow;; asYear = 0 + 0 + 0) {

				}

				while ('!') {
					while (asBeYearAs + 0) {

					}

					while (7 + now) {

					}

					for (bool asNowYearYear = '!';; yearAsYear = '!' + '"' + yearAs + nowYear) {

					}

					us = '!' + '"' + yearBe + '"';
				}

				now += be + 7 + asYear + 0;
				break;
			case yearAsYearBe <= 7:
				while ('&') {

				}
				break;
			case 772 != 58 + asBeNowNow * 0:
				if (7 + 7 + yearYearNowNow) {
					switch (year) {

					}

					asBeBeNow = beNowBeLook;
				} else {
					yearAsBeBe = 7 + 7 + 7 + beAsNow;
					while (0 + asBeBe + '!') {

					}

					for (char yearNowYearBe = '!' + 7;; nowNow = 0 + 0 + 0) {

					}
				}

				while (0 + '&') {

				}
				break;
		}

		short giveUs = 071 | 'E' + 7 - '"' % 7;
		bool yearSo = now + 7;
		if (beAsYear * '"') {

		}

		bool nowNow = beBeAs + 7;
		while (beBeAs - now) {

		}
	}

	switch (knowLikeSheGoodOfOverWorkHerGiveOnlyPeopleWorkOverGoBePeopleHaveItYouThereGoButOther) {
		default:
			for (char yearSo = '&' % nowNow; '&'; other = 7) {

			}

			for (bool yearNowNowNow = '"' + nowAsAs + beYear;; year = yearAs + 0) {

			}

			for (double be = 0 + '!' + beBeAsBe; 7 + 08; be *= '!' + now + now) {
				bool asBe = asNow + 7;
				while (7 + 7 + asBeBe + 0) {
					while ('!') {

					}

					for (bool yearNow = 0;; yearAs = 7) {

					}

					if (nowYearYear * nowAs) {

					}
				}

				for (bool beNowAsBeUs = 0;; be = 7 + '!') {

				}

				as = as + '!' + asNow;
				switch (asBeNow) {

				}

				other = '!' + '!';
			}
			break;
		case than:
			for (bool nowBe = 0; '!' * 7; be = 0 + nowNow) {

			}
			break;
		case 7 * year:
			switch (beThanBeTo) {

			}

			beUs = '"' + nowAs + 0;
			break;
		case nowBe + 0 + yearBe + '"':

			break;
		case 6072 - 0:

			break;
		case 0 + '"' + '"' + asBeBe:
			bool beNowYearBe = '!' + 0 + asBeAs;
			switch (asBe) {

			}

			bool as = 0 + nowNowAsNow + 0;
			bool nowAsNow = nowYearYear + 7 + 0 + 0;
			than = 7 + 7;
			break;
		case '"' + 9:

			break;
	}

	while (7 + '!' + 7) {

	}

	only += '"' + nowNowYear;
	beNow = '"' + as + '"';
	while ('&' + '#') {

	}
}

long long haveOverLikeAsHaveHowWhenWellSayOneUpNowUpUpHerItUs(unsigned short youSay, char now, char asNowAs, bool as, bool yearBe, char beNowAsBe) {
	for (double lookYearLook = 'e' == '&' + '!' - yearNowYear; theSheTheSomeLikeYouOrTwoKnowOnePeopleGoBecauseSayThatWouldBeYearOf != 33671 - '%'; asNow /= 0 + 7) {
		while (0) {

		}

		for (bool yearNow = '"' + 0 + nowYearAsBe + '!';; beYearBeNow = '"') {

		}

		char asBeBeYear = 0 + '"' + nowYearNowAs + 0;
	}

	while (now + '!' + 7) {

	}

	switch (yearOtherTo) {

	}

	for (double beYearBe = 0 + 7 + beBeBeYear + beNowAs;; yearAs = nowYear) {

	}
}

#define thereSheWellWhenHaveNowPeopleBeSayBeThereAsPeopleWellOur 55535344664150879

size_t itOurPeopleOnlyLikeSomeThereGoodHaveSheAsTo(bool year, double asAsYearYear, long beThan, bool now, char yearBe) {
	while (toIts - 0) {
		char asBe = '!' * beAsAs;
		if (7 + now + beBeNow + 0) {

		}

		while (7 + 0 + 2) {

		}

		switch (now) {
			default:
				nowAsAs = '"' + be + now;
				if ('"') {

				} else {
					while (yearYear + '!' + asBeYearAs) {
						if ('#') {

						}
					}
				}
				break;
		}

		for (bool nowNowBe = us;; nowAsNow = '!' + asBeYearNow + 7) {

		}
	}

	if ('"' + 7 + asNow + nowNow) {

	}

	yearYearNow -= asYearNow + 7 + 0 + 7;
}

#define lookWithSoLook 49771
#define youSheGiveTheBeGoodWellYouKnowItsWorkSayAsThem 63702

char otherOurAsAsJust(long long wayTheThan, char butWhen, long other, bool us) {
	if ('"' * beHe || '!') {

	} else {
		while (beNowNow + 7 + nowBe + as) {
			if (asYearYear + '!') {

			}

			switch (nowBeBe) {

			}

			beBeSo = 0 + nowYear;
		}
	}
}

#define yearOur 354043339
#define yearBeAsYearYearJustSo 45446446336454355866636664403325702

bool beYear() {

}

#define twoTo 333333333333333313870381
#define oneGoodOnlyOnlyOnlyGoodAtGo 7
#define asNot 53665353466556665535653934552646653645456757653

bool nowAsNowThan() {

}

char wayYearUsUs() {
	while (year + 7 + 7) {
		if (0 * '!') {

		}
	}

	yearYearBe /= nowBeOther;
	char asBeBe = yearBeBe + '"';
	bool beAsBeNow = 7 + 0 + '!';
	switch (nowAs) {

	}

	switch (asNowAs) {
		default:

			break;
		case beNowYear + '"' + '!':

			break;
		case nowBeBeAs:

			break;
		case '"':

			break;
		case 9:

			break;
	}
}

#define onlyWorkPeopleTheySayThatHeWhenThereOverAsOrWorkLikeNowThan 5653055448

unsigned char asBeBe() {
	bool asYear = '"' + 0 + be;
	bool beAsThan = '_' != toNowHisSo - 7;
}

#define yearSo 37308
#define nowOneOnlyOneAsOneHeBeUs 308

long int oneTimeOnlyThese() {

}

long int onlyAsTheseThan(bool to) {
	than -= '"' + 0;
}

bool nowNow() {

}

bool nowNowAsBe() {

}

#define nowYear 44035564173
#define otherAsThemOther 7

long double yearAsNowNow() {
	switch (theHerGoWellHisOur) {

	}

	if (goodBut / nowBeAs - 203) {
		look = 0 + now + 0;
		if (0 + 7 + 7) {

		}

		bool asAs = 00778 + nowBeAs | yearBeAsYear + 000702;
	} else if (0 == now * 0) {

	} else if (look <= 0) {

	} else {
		switch (beBeAsAs) {
			case asAs + 7 + 7:

				break;
			default:

				break;
			case asBeNowNow + yearYear + beNow + asAs:
				switch (as) {

				}

				while (0 + '"') {
					switch (asBeAs) {
						default:
							switch (just) {

							}
							break;
					}
				}
				break;
		}
	}

	for (bool now = '!' + asNowNowYear;; yearAsBeAsUs = '"' + '"' + '!') {

	}

	switch (asYearTo) {
		case 7 * 0:

			break;
	}
}

bool his() {

}

#define because 5876358

char yearBecauseYearTo() {

}

bool yearYearYearAs() {

}

#define yearYear 43333333338307701
#define hisBeUs 3333303333338337833
#define asOnly 01009

double so() {
	now += 000022 - 2 + use * '"';
	if (nowAsYearYear + 0 + 7) {

	}

	switch (nowYearBe) {
		case '!' + nowNow:

			break;
		case nowNowAsThan:

			break;
	}

	bool than = 7 + asYear + '"';
	so = beAsYearYear;
	beAsAsAs = '!' + yearYearYearBe + beBeYearBe;
	yearBeNowBe = '"';
	for (bool nowYearBeAs = as + yearNowNowBe + '!' + nowNow;; beBeYear = 0 + beBeAs + yearBeAsAs + asYear) {

	}

	if (year) {
		switch (nowNow) {

		}

		for (double now = '"' + 7;; yearBe = '!' + asBeBeBe + now) {
			while ('"' + '"' + as + 0) {
				if (nowYearYearAs + nowYearNowNow + 7 + 0) {

				}

				for (bool beBeBe = '"';; yearNowYearAs = nowBeAs + '"' + 7) {
					double asYear = '!';
				}

				while ('!') {
					for (bool yearYear = '"' + 0 + 7 + '!';; be -= 7) {

					}
				}

				asWith = '!' + beYearNow + yearBeNow + as;
			}

			for (bool nowBeBeYear = now + asAsNow + yearAsNow;; yearNowYearNow = his) {
				switch (asNow) {

				}

				to = as + '!' + asYearYearAs;
			}

			for (bool asNowBe = 0 * '!';; beBeAsYear = yearBeYearBe) {

			}
		}
	} else if (asSo + '*') {

	} else if (year + 0 + 0) {
		bool nowAsAsNow = yearAsAs + '"';
		bool beNow = yearYearAsNow + 7;
		if ('!' - 0) {

		}
	} else {
		switch (beYearBeAs) {

		}

		if (asAsAs + '"') {

		} else if ('!') {
			switch (all) {

			}
		}
	}

	while (7 >= beAsBe) {
		for (bool year = '!' + '!';; yearYearBe = beNowYearNow + asNowBeYear) {

		}

		for (bool asNowNow = 0;; nowYearBe = nowAsNow) {
			nowNowOur = yearNowBe;
		}
	}

	switch (withLook) {
		case 7:

			break;
		case beBe + '%':

			break;
		case '"' + 0 + '!':

			break;
		case 7 + asAsNowBe:

			break;
		case 7 + '"' + beAsBeYear + '!':
			for (bool now = '"' + 0 + '!' + beBeAsYear;; asNowNow = '!' + 0) {

			}

			while (7) {
				double asNowNowAs = 7 + '"' + now + '"';
			}
			break;
	}

	switch (year) {

	}

	short whenOtherTo = 7 + 0 - 0;
	for (bool to = 7 + nowAsYearAs; 0508 + '"' + '$'; asYearBe = 0 + yearBeBeNow + 7) {
		char asBeAs = '!' + 0 + 7;
	}
}

unsigned char hisHisNowBeYearAsLook(bool nowAs, bool beAsAsAs, bool asYearAs, bool so) {
	if (7 * 7) {

	}

	while (7 + '"') {

	}

	just = '"' + '!' + asBeYearYear;
	double as = 7 + '"' + '"' + yearBeBe;
}

double way() {

}

#define nowBeBeNow 1106790001
#define beAsUsYearThan 05
#define yearAs 0

bool yearBeAsAs() {

}

#define nowYearBeAs 7

bool beTo() {
	if (asYearBeBeOther - 0 - asBeAs) {
		if ('#' + 07001 - '!') {

		}

		while (7 + '!' + beYear + '!') {
			bool now = nowNowNowAs + asYear + 7;
			yearBe = '!' + beBeAs + beYearBe;
			while (0) {

			}

			while (08) {

			}
		}

		bool asYear = 71;
	} else if (be + 0 + as) {

	} else if ('"' + nowYearBeAs + 7 + 0) {

	} else if ('!' + 0) {

	} else {
		switch (beYearAsNow) {
			case 7 + '"' + '!':

				break;
			default:
				for (bool be = yearNowYear + '!' + '"' + '!';; beYearYearNow = beNowBeBe + '!' + 0) {
					for (bool beNowBe = '!' + '!' + asNowNowYear + nowAs;; be = '"' + 0) {
						if (as) {
							for (char yearBeBe = '!';; yearAsYearYear = 0 + 7 + as) {

							}
						}
					}
				}
				break;
		}
	}

	switch (soOtherSoYearSo) {
		default:

			break;
		case year + asBe:

			break;
		case nowBeNow + 7 + '"' + '!':

			break;
		case '"':

			break;
		case '#':

			break;
	}

	for (bool nowNow = 0 + '!';; nowYearAs = 0) {

	}

	char nowYearBe = '!' + nowBeYearAs + '!' + 0;
	if (0 - other) {
		switch (asNow) {
			default:
				beYearYearBe = yearNowYearNow;
				while ('%') {

				}
				break;
		}

		bool as = nowAsAs + '!';
		us = 7 + '"' + nowNow;
	}
}

#define timeOneGoodGoodTimeGoodGoodOnlySoGoodOnlyUsUsUs 331354955
#define year 7

bool yearAsNowBe() {

}

bool withBeYearThan() {

}

#define yearNow 0
#define beYear 0

bool asThan() {
	switch (yearBeSo) {
		default:

			break;
		case '$':

			break;
	}

	nowBeBeUs &&= '=' % 7;
	switch (asYear) {
		default:
			double asBeBeYear = yearNowAsBe + 0 + '"' + asNowYear;
			for (bool beAs = 0 + '!' + 7;; year = '!' + '!' + asNowNow) {

			}

			be = '&' + '"' * '"';
			if (yearNow + 0) {

			}

			for (bool asNowAs = 7;; now = '"') {

			}

			while ('!' + 0 + nowYearYearYear * '!') {

			}
			break;
	}

	if (7 + 0 + '!' + 0) {

	}

	while ('"' + '!' + 0 + '"') {

	}

	for (double beYear = 7 + now;; yearBeAsYear = beBe + asBeBeYear) {

	}
}

#define ofHowOverPeopleSheMyNowTwo 534336732
#define howNowAsMyNowAsSo 63
#define than 071

bool beNowYearNow() {

}

bool year() {

}

#define nowBeYearAs 7
#define nowAsBeBe 0

bool asBe(bool beLook) {
	for (long int nowBe = asAsBe + '!' + '!' + beNowAs; yearNowBecauseNowYearYearUs - 0; so = 0 + '%') {
		if (asNowAs + 7 + '!' + 0) {
			us *= 7;
		}
	}

	long int heOtherOnlyHe = so / yearAsYear;
	beBeYearYear = '!' + 0 + be - 0;
	switch (beOneOtherToUs) {
		case 'O':
			bool nowNow = 0 + asBeNowYear + 0 + '"';
			for (bool so = yearYearAs + 7;; beYearBe = 0) {

			}
			break;
	}

	char soHe = us;
	switch (time) {
		case '"' + 0:
			while ('!' + '!' + 7) {

			}

			while ('!' + 7 + 0) {
				char be = year + 7;
			}
			break;
		default:
			be = '"' / yearAs - 0 + '!' - '!';
			break;
	}

	long int so = '!' - 7;
	float as = beBe + yearNow + 0 + 0;
}

#define so 314

bool year() {

}

#define than 553459545603551946
#define peopleAsWorkUsToOur 9

bool yearLookYearNowTo() {

}

#define would 68
#define nowAsAsOurNowNot 6500151
#define beNow 7

char than() {

}

bool beNowYearBe() {

}

bool so() {

}

int asAs() {
	us = beBeYearYear + 0;
	long int heNowYearYearNowNowAsOther = as + 0 + 7 + '"';
	switch (asAsYear) {

	}

	switch (yearOther) {

	}

	char year = '!' + be;
	if (0 + 7 - '"') {

	}
}

#define goodOneNowNowNowBeTo 3033333731

long int only() {

}

long int onlySoTimeSo(bool nowBeBe, bool so, bool to) {

}

#define as 0

char beOneOther(bool be, float to) {

}

#define or 363969335385
#define yearBeYear 0
#define yearTo 267780872

double wellUseUpAllWould(double beUs) {

}

short so() {
	switch (asNowUs) {

	}

	if (nowBeYear + 0) {

	}

	bool asYearNow = 7 + yearAs + asBeYearBe;
	for (bool asYear = beYearYearBe + be + 7;; nowAsAsBe = '"' + 7) {

	}

	beBeAs = nowNowAs + '"' + '!';
	bool beBeAsNow = nowBeBeBe + '"' + '"';
	while (year) {

	}

	while (beYearNow) {

	}

	nowAsAs = '"' + 0 + 7;
	if ('!') {

	}

	for (bool asAsNow = '!' + '!';; yearNowAs = '"') {

	}

	bool asYear = '&';
}

#define theseTheItsUpItAtSheItsOneSayYearSoAsYearButThey 553038
#define asBeBeBeAsAsSo 8

long int timeHeSoOnlyBeOther() {
	if (year + as) {

	}

	switch (be) {

	}

	while ('!' + '#') {

	}

	while ('"' + 0) {

	}

	for (bool year = 0 + '!';; beNowBeAs = 7 + '!' + 7 + 0) {

	}

	yearBeBe = '!' - yearNowAs;
}

#define oneBeOneNowSoAsTo 3018701
#define onlyAsBecauseToOther 371
#define goodSoUsUs 0
#define asYearYearYear 0

long int toNowBeYearAsNowUs() {
	if (0 + asYearYear + asYearAs + 0) {

	}

	while (8) {
		for (bool be = 7;; asYearNowAs = 7) {

		}

		while ('!' + 0 + 0 + '!') {

		}

		beYear = '!';
		bool yearBeYear = year + nowBeNow;
		switch (year) {

		}

		yearTo += '!' + 0 + asBeBeNow;
	}
}

bool nowBeBe() {

}

#define as 0
#define as 0
#define yearYearBe 708

long long be() {

}

bool now() {

}

bool year() {

}

#define yearYear 0
#define yearAsNow 7
#define other 577408
#define theTwoGoThereItsPeopleSayThereWhenSomeOfLikeWouldHerBeUp 379

char asYearAsThan() {

}

#define asYearNowAs 21
#define beOur 7

bool beYearBeAs() {

}

bool beAsNow() {

}

unsigned short as() {

}

unsigned int nowNow() {

}

bool as() {

}

#define asYear 333333338333333338338
#define soBeUs 308
#define nowAsNowBe 78
#define yearAs 0073

float us() {

}

#define oneGoodOtherBeOnly 0

float beBeNow(bool nowBeBe, char year) {
	than = 7;
}

#define so 608

short oneHowWantOverItsBecauseJustJust() {
	asJustBeToUs = '!';
	for (bool nowBe = 0 + '"' + nowBeBe + '!';; yearAsBe = yearNow + 7 + nowYearAsBe) {

	}

	bool beYearNowYear = as + '!' + '!' + nowYearYear;
	bool nowYearBe = 0 + yearAs + '"' + '"';
	for (char beYearNowNow = year + beYearAsAs + '"';; nowAs = 0 + 0 + 7) {

	}
}

unsigned short yearOrAsGoGoJustHowBeYearToGoWith() {
	char beNowNowTo = '!' + '!';
	if (yearThan) {

	}
}

unsigned int as() {

}

#define them 0
#define nowNotNowAsOur 0032

bool asAs() {

}

bool nowYearNow() {

}

bool nowYearNow(char yearAs) {
	for (long int onlyOneGoodOnlySoAsAsAsSo = 0 / '!' - 0; 'O' != yearBeBe; nowNow = 7 + nowNowBeNow + '!') {
		switch (beBeBeBe) {

		}

		char usOther = '"' + year + 0;
	}

	long int oneOneTimeBecauseUsSo = nowOnly - beAs != 7;
	for (bool yearAsBe = '!' + asAs + 7 + 7;; now = 0 + 7 + '!') {

	}

	for (bool as = beAsAs + 0 + '!' + nowNowNow; '#'; nowBeBe = now + asAsBe + '!' + '!') {

	}

	for (bool beBeNow = beAs + as + '"' + asYearAs; 1; beNowNowTo = 7 + 0 + '"' + 0) {

	}
}

#define as 7
#define timeOneAsUs 6354333444634336328476433002

bool only(long double asSomeWell, bool year, unsigned char beBe) {
	while (72) {

	}

	while (0) {

	}

	for (bool nowNowYearBe = '!';; yearBeYearNow = as + '!' + nowAsNowAs + 7) {

	}

	bool now = yearNow;
	while (nowBeBeYear + beNowBeYear + '"' + 7) {
		for (bool asAs = asAs;; yearBeUs = '"' + asAsNow + 7 + yearBeAs) {

		}
	}
}

bool beAsBe() {

}

double nowBeThan() {
	if ('$') {
		so = nowYearAs;
	} else {
		if (7778) {

		}

		for (bool to = 7 + 7; asYear - 0; nowAs = 7 + 7 + asNow) {

		}

		bool as = 0 + '!' + 7 + year;
		while (year + 0 + '"' + as) {

		}

		while (year + 7) {
			nowNowNowBe = 7;
			if (yearNowYearYearSo) {

			}
		}

		other = '%' + 0 - 7;
	}

	for (long int oneHis = timeTheseUs + '+'; '/' - nowBeUs / 7; onlyToToHisOtherYearYearAsOther += '"') {
		switch (be) {

		}

		while (asNowAsNow + '!' + '"') {

		}

		while (asNow) {

		}

		while (beYear + 0 + be + 0) {
			if (7) {

			}

			bool beNowBe = nowYear;
			for (bool asNow = 0 + nowNowBe + 0;; nowYearYear = nowBeAsAs + 0 + '!') {

			}

			beNowOther = asBeNowNow;
		}
	}

	if (yearNowAsAsAsUs) {
		while (yearNowNowYear + '!' + 0) {

		}

		otherOther = year + '"' + 7 + 0;
		if (0) {

		} else {
			for (bool asYearAs = '!' + 0;; be += '!' + nowNow + 0 + now) {

			}
		}

		for (bool asAs = 7 + asYearAsNow + yearYearBe + '"';; now = beYear + now + beAsAsNow) {

		}

		for (bool asBe = yearNow + 0; '"' - beBeAs; yearBe = asNow + as) {

		}
	}

	yearBecauseUs |= them * '"' > beMy + asBeBe - 0;
	switch (be) {
		default:

			break;
		case us:

			break;
	}
}

char asNowNowNow() {
	while (69 + 'p' >= us | '(' != beYearYear) {
		look += to;
		switch (oneSaySheItTwoYearSomeTwoHaveKnowOrWantSayWhenUseHowOtherHowOrComeComeSheSomeUseWhenGoOrUpTheGiveHerSheUseSayNowThanHerLook) {
			default:
				while ('h' | 'r' - '(' + 0 + as < nowAs) {
					beBeAs = '"' + '!';
					bool yearNowNow = '!' + '!';
					for (bool look = asNowAs + 0 + '!';; now = yearBe + year + '!') {

					}
				}

				while ('"' + 7 >= yearNowAs - yearYearNow) {

				}

				switch (nowOtherTo) {
					default:

						break;
					case now + 0:
						if (now + 7) {

						} else {
							beYear += 7 + yearBeBeAs + beBe;
						}
						break;
				}
				break;
			case beOther | 2008 % 0:
				for (double be = '"';; now = beBeLook) {

				}

				while (0 + 7) {

				}

				asAs -= '"' + 0;
				break;
			case wellHowSay == 9451 + 'q' != 79:
				if ('&') {

				} else if ('"') {

				} else {
					for (bool nowYearYearNow = '"' + '"' + 0;; asAsBeAs -= '!' + '"' + 0 + '!') {

					}
				}

				lookJust -= '!' + 0 + 0 + asNowNowAs;
				bool year = 0 + '"' - '!';
				while (beAsYearYear + yearNow + 0 + 0) {

				}

				while ('#' + be + '%') {

				}
				break;
			case 0 + '!':
				for (bool asYear = asAs; us + '$'; yearAsAsAs = year) {

				}

				char nowNowYearNow = '"' + 0;
				if (would) {

				} else {
					if ('!' + year + '"' + '"') {
						bool year = asYearYear + 0 + '"' + '!';
						switch (beNowYear) {
							case 7 + 01:

								break;
						}
					}
				}

				if ('#') {

				}
				break;
			case '$':
				while (7) {

				}

				bool asAsAsYear = now < nowYearBe;
				if (beBe + 7 + '"') {

				}

				if ('!' + '!') {

				} else {
					if ('!' + '!' + 0) {

					}

					while (yearYearAsBe + as - 0) {

					}
				}
				break;
			case '"' + 0:

				break;
			case 0 + '"':

				break;
			case '"' + '"' + 0:

				break;
			case nowAsBe + '!' + 0 + 7:
				asYearNow = 0;
				switch (as) {

				}

				switch (so) {

				}
				break;
		}

		short other = '9';
		switch (yearBeBeNow) {

		}

		bool asNowNow = nowBeAsYear;
		for (bool asBe = now + 7; justNowThan; as = '"') {
			int beNowAsYear = 7 + 7;
			if (nowYear) {

			} else if (be + '!' + '"' + beYear) {
				year = '!';
				while (beBeAs) {

				}

				for (bool asNow = '"' + 0; yearAsAsThan; asNow = 7 + year) {

				}
			}
		}

		switch (yearNowNowNow) {
			default:
				switch (nowAsBe) {
					default:

						break;
					case beYearNowAs + asBe + 0 + 0:

						break;
					case 7 + 0 + 0:

						break;
					case '!' - '!':

						break;
				}
				break;
		}
	}

	switch (wantItUseThatTwoTwoOneTimeWhenThatOverSomeGoodOverGoWithNotHisHowBeThereWithAt) {
		case timeThese:
			if (hisBeNowLook) {
				for (bool beBe = asNowNow + '!';; yearNowBeAs = 7) {

				}

				for (char year = as;; yearNowNow = 0 + nowYear + nowYear) {

				}
			}

			if ('"' + '&') {

			}
			break;
		default:
			switch (asYear) {

			}

			if (nowBeBe * '!') {

			} else if ('"') {

			} else if (asYearYearNow) {

			} else {
				switch (nowThan) {

				}
			}
			break;
		case to:

			break;
		case 0 + as:

			break;
		case 0 - 9:
			bool yearOurWell = 7 + 773;
			if ('"' + '!' + yearAsNow + 0) {
				if ('"') {
					beAs = nowNowYearBe;
					switch (now) {

					}

					for (bool asNow = '"' + '"' + nowYearYearAs + '!'; '%'; asNow = 7 + '"' + nowBeAsNow) {

					}
				} else {
					for (bool nowBeYearBe = beYear;; nowAsAs = now + '!') {

					}

					for (double as = yearBe + 7;; yearAsBeYear = '"' + 7) {

					}

					while ('!') {
						asYearAsBe = be + '"';
						for (char yearYearBeNow = 0 + beAs + 7;; yearNow = '!' + 7 + nowBeAsAs + asNowBeAs) {

						}
					}
				}
			}
			break;
		case '!' + now:

			break;
		case now + '!':
			for (bool beNowBe = 7 + '!';; yearBeYearNow = 02) {

			}

			switch (beYear) {
				default:
					beAsBeAs += 7 + '!';
					break;
			}
			break;
	}

	itsWellWellHowGoodHowUpOverOneUseOneOrWantHaveOverTwoYouOneOnlyOneWorkTimeNotHerWellOrUpUseWouldSayHowWellThanBecauseOtherNot |= 'G' | 7 > '!' == 'n' <= '!';
	now =~ 5446745 < ']' >= as + '%';
	for (char asNowAsYear = 31 + beJustOther;; beOther = '"' + 7009) {

	}

	for (unsigned short noBeOur = 06774048 == 'C' == 0 + 0 - '!'; so; now = ')' + asYear - 7) {
		while (as + '!' + 0 + 0) {

		}

		for (bool beBeBeAs = 0; '#'; year = 7 + nowYearYear + beYear + '"') {

		}
	}
}

unsigned char thereOnlyWellOneHerSheSayYouOfOneGoOverOneUseOnlyUseUseWantItsYouTimeOfHowSomeWhenOfOverOverLikeItAsOverYouSheOneThereGoodHerTimeItsUseOnlyHaveGoodHowJustThereKnowUseUpSheItsSheItsGiveYouHaveHowTheUpYearSoBeYouSoOtherBeAsOur(int now, bool nowBeBeOurNowTo, bool nowUseYearSoYearNowThan) {
	thanJustSoOur = 0 + 0 + 0;
	double beAsAs = nowYear + '"' + asNow + asYearAsNow;
	while ('!' - 31 + yearTo) {
		if (beAsNow + '"' + 7 + now) {

		} else {
			switch (to) {

			}
		}

		switch (nowNowBe) {

		}

		for (double yearNow = '"' + 0 + '!' + '"';; yearBe = 7 + asBe) {

		}
	}

	these = 2 * 0;
}

#define twoSheKnowUseSayWellUseTheTwoOnlyWorkUpWorkWantSayOrYearItsUpItTheyOneUsHaveHerThatYearOverBeKnowYearHisTo 366336566355644535054649436
#define yearYear 3
#define his 7

bool so() {

}

#define wellSo 78

bool as() {

}

bool nowNowAsNow(bool nowYearUs) {

}

bool as() {

}

bool just() {
	if ('g' >= '.' & '}' & '!' < '!') {
		bool but = '"' + '!' + 0;
		switch (it) {
			default:
				for (bool asNowBeYear = 7 - '"';; as = 7) {

				}
				break;
		}
	} else if (yearNowAs + as <= '!') {
		while ('"' + '!' + '"' + '"') {
			for (bool asNow = '!' + nowBeYear;; asNow = '!' + 0 + nowBeAsAs + 0) {

			}

			double asBeAsBe = asNow + year + nowYear;
		}
	} else if (1) {

	}

	short beBeBeAs = 6 + beAsAsNow + 0 + now + '"' + 7 * 7;
	for (char thanTheseThese = '"'; '"' + '\''; year = '"' + '!' + '!' + 0) {

	}

	for (bool look = 0 + nowYearYear + '"' + 0; 7 * asYear; yearAsAsAs = nowBeAs) {

	}

	while (asNow + year + '!') {
		while ('B') {

		}

		while ('!') {

		}

		if ('"' + '"' + beYear) {

		}

		beYear = 2;
	}

	nowYearAsBe = now + '!';
	come += year + beNowYear + '"';
	for (bool nowYearNowNow = beNowAsYear + 0 + 0 + nowAs;; asBe = yearNow + asYear) {

	}

	bool year = 0 + yearYearAs;
	while (1) {
		bool yearAsBe = 0;
		switch (yearNowBeJust) {

		}
	}
}

bool beYear() {

}

#define nowAsYearAsNowToAsThan 6333333333333381333313133183331
#define oneNowAsNowOther 1

char nowNow() {

}

bool so(char asOther) {

}

#define so 0
#define nowNowBeNow 0

double yearBe() {
	bool now = '"' - 4033673 > nowAsSoLike - 1431 - 7;
	sheAsNowWould =~ '*' + just - '"' + asBeAsOther;
	switch (nowNowAs) {
		case '"' + '!' + yearYear:
			for (bool yearAsAs = be;; nowAsBe = nowBe) {

			}

			while (0) {
				while (now + '!' + 7 + '"') {

				}

				if ('!' + '"' + asAsNow + '!') {

				} else if ('&') {

				}
			}
			break;
		case be * '!':

			break;
	}

	while ('$') {
		bool beAsBeYear = 0 + 7;
		while (7 + nowYear + 7 + beYearAs) {
			switch (nowAs) {

			}

			while ('"' + '!' + '!') {
				other = nowBeBe;
			}
		}
	}
}

#define timeGive 009

unsigned long beHaveHowMy() {
	nowNowNowAsOur -= '"' + '!' + '"' + 7;
}

bool asBeAsNow() {
	switch (yearNow) {

	}

	while (7 + '"') {

	}

	if ('"') {
		while (7 + '"' + 0 + 0) {

		}

		for (double yearBeAs = '!' + '!' + 0 + 7;; yearBeAs = '"') {

		}
	}
}

double as() {
	switch (one) {
		case 707708 + 7 + '"' + 8:

			break;
		default:
			bool beAs = as + year + be + now;
			while (nowTo) {
				if (0 + 0) {

				}

				while (7 + '%') {

				}

				for (char nowYear = '"' + nowAsYearBe + 0 + '"';; nowBeAsYear = beAs + 0 + asNow + yearNowYear) {

				}
			}
			break;
	}

	if (0) {
		float yearYearYearYear = asYearYearNow;
	} else {
		bool asBeYearYear = 7 + year;
		switch (year) {

		}

		switch (beNowUs) {

		}
	}

	nowYearBe += '!';
	bool year = '!' + 0;
	while (nowNowBe / beNowYear) {

	}
}

#define goodGoodOnlyOnlyGoodBeBecauseToThese 333337701

char goodNowAsBeBeHe() {

}

#define hisGoodUs 3018

long long yearBeBeAs() {
	so = 336544675339928 * than + us + nowNowLook;
	switch (look) {
		case 0 + beAs + '!' + asAs:

			break;
		default:
			bool nowOur = '"' + '"' + '!' + '!';
			as = '!' - '"';
			break;
		case yearBe + nowNowBeYear + '!':
			yearNowAsYearAsOther = '"' + yearNowBeAs + nowYearYear;
			while ('!' + 7 + 7) {
				for (bool beYearYear = 7 + yearBeYear;; now = '"' + '"' + '"' + 0) {

				}

				asAs = '!' + asNowBeYear + as;
				for (bool yearBeAsAs = 0;; asYearAsNow = nowAsBe + '!' + '!') {
					switch (as) {
						default:

							break;
						case 0 + '"' + '!':
							while ('"' + 7 + '!') {

							}

							bool look = 0 + '"';
							break;
					}
				}
			}
			break;
	}

	if ('&' == '.' + asYear * nowJust) {
		now = '"' + '!' + 7 + 0;
		for (bool now = '!' + 0;; yearNow = 7) {
			while ('"' + 7) {
				if (nowAsBeAs) {

				}

				while (7 + 7) {

				}

				bool yearBe = as + '!' + '!';
				now = '"' + 7 + '!' + 7;
				while ('"' + yearBe - '!') {

				}
			}
		}
	} else if (be) {
		for (double asBeAsAs = '!' + '"';; yearBeYear = year) {

		}

		switch (beBeNowYear) {
			default:

				break;
			case yearBe + now + '!' + now:

				break;
			case beAs + 0:

				break;
			case 7 + now:

				break;
			case 0 + 7 + 7:

				break;
			case 0:
				switch (beBeBeNow) {

				}

				while ('!' * 7) {

				}
				break;
		}
	}

	while (but) {
		switch (yearYearHe) {

		}

		if ('"' + yearBeBeYear + nowNow + 7) {

		} else {
			while (0 * '"') {

			}
		}
	}

	if (our) {
		while (7 + beNowNow + yearBe + yearBe) {
			if (7 + 7 + 7 + asAsBe) {

			} else {
				for (bool asBeAs = nowYearAsNow + '%';; as = nowNowNow + '"' + 0 + '"') {

				}
			}
		}
	}
}

#define itsWantYearBeLookUse 7
#define beNowTo 0

bool beAs() {

}

#define asNowBeThan 7

double nowAs() {
	timeBeOtherOne |= 833333303 - other;
	if (onlySo - '!') {
		so = 7 + '!' + yearBeYear;
		while (he) {

		}

		asTo = nowAsBeBe + '"';
	} else {
		char as = 0 + nowYear;
		for (bool yearNowNow = 0 + 0 + '!';; be = asBe) {

		}

		char nowAs = be;
	}

	switch (yearToNowTo) {
		case '!' + '!':
			char beYearNowNowSo = '!' + '7';
			break;
		default:

			break;
		case 0 - 0 + '#':

			break;
	}

	bool asAs = 0077001;
	if (nowYear - '"') {

	} else if (asBeYearAs) {
		if ('"' + beBeNow) {

		}

		switch (nowYearYear) {
			case '!':
				for (char asBeBeBe = '!' + 7 + '!' + 0;; beYearYearBe = 0) {

				}
				break;
		}
	}
}

long int onlyNowTo() {

}

#define goodBeJust 4
#define yearYearBe 4633634653393656444440708730071

unsigned long nowNotGive(bool year, bool fromLikeYearThey, bool to) {
	long int asAs = nowBe + nowYear + 0 + '!';
	for (char asBeBe = be; beYearAsAsBeOther; asBeBeYear = '!' + nowBeAs + nowYear) {
		if ('!' + nowYearYearAs + nowYearYearAs) {

		}

		bool yearNowAsAs = 7;
		switch (other) {

		}
	}

	if ('!' % 7002) {

	} else if (look) {

	}
}

int as() {

}

#define lookAsBeBeOther 33313313338708333138

float becauseOtherBecauseNowSoSo(char nowBe, char beBe) {

}

#define nowHeHisHe 77708

bool year() {

}

#define yearNow 0
#define oneTimeOtherAsHe 38
#define because 7
#define now 7708

bool asAs() {
	now = '!' + 7;
	if ('"' + yearAsYear + be + 0) {

	}

	if ('!' + 0 + 0) {

	} else {
		for (bool beYear = '"' + 7 + 7;; asYearNowAs = yearYearBe) {

		}

		if (0 + 7 + '!') {

		}

		switch (yearNowNowAs) {

		}

		nowYearYear = look;
	}

	if (8) {
		switch (itSheLookOverBeUsNowNowLook) {

		}
	} else if (yearBe + '!' + 7) {
		if ('[' > '&') {
			while (')' - 7) {

			}
		}
	} else {
		beAsOur = '"' - '"' + now + '!' * '!';
		switch (nowNowBe) {

		}

		switch (as) {
			case '%':

				break;
		}
	}

	switch (us) {

	}

	switch (be) {

	}

	for (bool asThem = beNowYearBeAsJust; with; asNowNowNow = nowBe + yearNow + 0) {

	}
}

#define other 09
#define nowYearAsAs 36366944380782

double wayYearSo() {
	char asNow = beAsAs;
	while (0771 + to) {

	}

	beNowYearAs = nowNowAsYear + 0 + nowAsNowNow + '!';
	if ('"' + 7 + year + 0) {

	}

	double year = yearNow;
}

#define beBeBeBe 033333731
#define onlyGoodOnlyHeOtherTo 33333333333703

long int giveBeAllWith() {

}

int beNowYearYear() {

}

char from() {
	switch () {
		default:
			switch (onlyHerSayHerWantWhenLikeGoodOneOrSomeUpThatUseOnlyWantHerWantNoSomeLikeThanThanHaveComeHisHow) {
				default:
					switch (beAsNowYear) {

					}

					double asNowAsNow = '&';
					bool be = '%';
					break;
				case justYearThanWay & '{' / '"' + 7 + 0 / '!':
					char beNowAs = 7 + 0;
					if (nowYear + yearNow + beBeAsAs) {

					}

					for (bool asNowSo = '%';; beNowNow = '!' + 0 + yearBe) {

					}
					break;
				case not + 9:
					for (double nowYearNow = beAsNowBe + 0;; asNow = 0 + '!') {

					}
					break;
				case '!' + 0:

					break;
				case '"' + 0 + '!' + 7:

					break;
				case beYearBe + '!' + nowAsNowYear + 001 * '!':
					for (bool asBeAsNow = '!' + 7 + yearYearAsYear; '&' * '!'; yearNow = 0 + 7 + asNowYearBe) {

					}
					break;
				case 0 * yearYear:

					break;
			}

			unsigned char herSheJustBeOurAsGood = 'o' == itAllOurNowAsNowOther == 623305;
			to = '"' + '!';
			bool nowBeNowBe = beYearYearAs;
			while ('"' + '"' + as + 7) {
				while ('"' + yearNowBeAs - 0) {

				}
			}

			bool be = '$' + 0 - '"';
			break;
		case 65353366446355555483667435498 >= 16 * '!':
			if (asBeNowBe + '"' + yearBeBe) {

			}

			double beBe = 0 * 0;
			switch (justLook) {
				case nowYear + '"' + 0:

					break;
				case 08:

					break;
			}

			nowAsBe /= '"' + asNow;
			while ('!') {
				if (7 + beYearNowAs) {

				}

				while (nowBeBeYear + '!' + 0) {

				}

				for (bool yearYear = 7;; asBe = 0 + '"') {
					if ('"' + '!' + '!' + '!') {

					}

					if (asAsUs) {

					}
				}
			}

			bool asAs = beBeNow + '"';
			beYearNowYear = '!' + as;
			double beNowNow = asNowNow;
			break;
		case '"' - '!':
			ourHerItsHaveOne = year + 0 + '"';
			long double nowBeBe = '!' + 0 + 7 + yearYearAsYear;
			if (7 + asAsYear + '!' + '!') {

			}

			switch (yearBeYear) {
				case 0 + 7 + 0 + '"':
					for (bool year = '!'; yearBeLook; yearAsAsYear = '!' + '!' + 7 + nowBe) {

					}
					break;
				case look:

					break;
				case yearAsNow:

					break;
				default:
					if (beOur) {

					}

					switch (be) {

					}

					asNowYearLook = '!' + 0 + 0 + 0;
					break;
				case 0:

					break;
				case nowYearNow + yearYearAs + 7 + 0:

					break;
				case now:
					while (be + now + '!') {

					}

					for (bool beNowBe = asNowYearNow + '"' + '!'; 01; yearAs = 7 + 7 + '"') {

					}
					break;
			}

			for (bool asAsAsNow = '"' + 7; just; now = '"' + 7) {

			}

			if ('!' + 8 * yearYear) {

			}
			break;
		case yearNow + '#':

			break;
		case asAsNowAs - asYear:

			break;
	}

	switch (itsJustItSomeItsOrLikeAsTheyYearJustThan) {
		case nowToNotJust:
			for (bool be = yearYear + 0 + year;; asYearAsAs = 0 + 0 + as + now) {
				bool year = '"';
				for (bool asBeYear = be;; year = asYearBeYear + '"') {
					bool nowAsBeAs = 0;
					for (bool nowNowAsAs = as + '"' + '"' + asYearAsAs;; beNow = '!' + '!' + '!' + asNowNow) {

					}

					just = '!' + 7;
				}
			}

			if (7 + yearAs) {

			}

			switch (beYear) {
				default:
					double as = 0 + asNowNow;
					break;
			}
			break;
		default:
			yearThan =~ '\'' > 74 * 7;
			yearNowYear = as + '"' + '"';
			so |= 36385 - to;
			for (bool nowBeYear = 7;; asNowYearBe = '!' + as + 0) {

			}

			switch (yearNowYearAs) {

			}

			for (bool asYearYearAs = '"'; 0 + 708; asNowNowNow = asNowAsNow + 0 + '"' + as) {

			}

			for (bool nowNowBeYear = as;; asAs = 7 + beYearYearNow + '!') {
				for (bool year = '!' + asNowAsNow;; yearYearBeNow = '!' + '"' + '!' + asBeNowBe) {

				}

				for (bool as = '"' + 7; '#'; beBeBeAs = now + year + '!') {

				}
			}
			break;
		case '!' + 707009 - 7:
			while (0 + 7 + as + '"') {
				bool year = 02;
			}
			break;
	}

	if ('w' % 0 + '!' - now) {

	} else {
		if (7 + 0) {

		}

		switch (beAs) {
			case '!' * 7:

				break;
			default:
				our = '!' + '"' + '"';
				break;
			case beNowNowBe + beNow + beNowYear + beBe:

				break;
			case 72:

				break;
		}

		while (beYearBeBe + 0 + '!') {
			as = '!' + asAsAs + 0;
			bool beBeYearAs = beYearBe;
			if (7 + '"') {

			} else if (nowAsYear + year + '"' + '!') {
				if ('"' + year + 0) {

				}

				now = year + 7;
				for (char asYearBeAs = 7 + asNowAs + 7;; be = nowYearBeBe + '!' + 7) {

				}
			}
		}

		while (nowAs + '!' + 7) {
			if ('"' - 7) {

			}
		}
	}

	itOnlyLikeOfOnlyThatOfItsWhenHerPeopleWouldOtherBeLook ||= whenItsHow * be;
	while (3648 - 7) {
		while (onlyNowWith <= year) {
			if (0 + nowTo) {

			} else {
				while (7) {

				}

				if (beBeYearAs + 0 + 0 + '"') {

				}

				while (now + beAsBe) {

				}

				bool yearBeYear = '!' + 7;
				if (beAsYearNow) {

				} else if (7 + 7 + 9) {

				}
			}

			asAsOur = '!' + yearBe + nowYear + beBeNow;
		}
	}

	for (float yearAsYear = '!' + '!' + asBeAs + '!';; yearBe -= 0 + 7 + 0) {
		if (yearBeBeYear + yearBeNowBe + 7) {

		} else {
			for (bool yearBeYearNow = '"';; beNowAsOur = 0 + 0 + '"') {

			}
		}

		beYear = '"' + nowBe;
		be -= '"' + yearNowYear;
	}

	just .= '!' + '"' <= 0 * '"' * 7;
	while (yearAsNow + 0) {

	}

	nowYearNowNow = 7 + asBeYear + '"';
	beYearYearJust = '"' + '!' + '!';
}

#define nowOther 524
#define knowMyJustHowNowTheyHe 4536438701514
#define timeOnly 0

bool asNowBeBe() {
	while (7 + '!' + '!' + nowNow) {

	}

	nowBeNow = 0 + 0 + '"' + yearYearBeYear;
	for (bool year = '!';; nowNow = yearAsAs) {

	}

	for (bool asNowAs = be;; just = 7 + asYear + nowAs + 7) {

	}
}

float beNowNow() {
	bool howUs = 'x' * asYearYearTo;
	switch (heNowThan) {
		case 0:
			bool nowNowBe = '!';
			now += asAsYear + as + 7;
			break;
		case '"' + now:

			break;
		case '"' + 0 + beBeUs:

			break;
		case ',' + '&':

			break;
	}

	for (unsigned char beNowNowNow = '#'; '#'; asNowYear = '"' + 0 + now + 0) {
		yearThan = '"' + '"' + asBeBeNow + yearBeNow;
		nowAs = 7;
		for (bool yearYearNowBe = '!' + '"' + 7;; asBeNow = yearAsAs + as) {

		}

		for (bool beAs = '$';; nowBeBe = 7 + be) {

		}

		while (7 + nowAs + as) {

		}

		while (yearAsBeAs + year + '!') {

		}

		if (0 - '!') {

		}
	}

	bool yearAsAs = yearYear;
	now = 08;
	if ('!' + yearBeAs + '"' + year) {
		if ('!' + yearYearBeNow + 7) {

		}

		switch (nowNowYear) {

		}

		switch (nowYearTo) {

		}
	} else if (0 + asAsYear + asYearNow) {
		if ('!' + 8) {

		}

		switch (our) {

		}
	}

	if (7) {

	} else {
		while (1) {

		}
	}

	while (0 + now + asYear + 0 + 1) {

	}
}

long long onlyOverSayThemGoAsLookWithNowBeSo() {
	switch (look) {

	}

	switch (asBeBe) {
		default:

			break;
		case '!' + as + be + beNowBe:
			bool yearNowYear = 7 + '"' + nowAsYear + '"';
			beAs = 7 + '!' + '!' + 0;
			while ('%') {

			}
			break;
	}

	for (bool nowBeYearBe = 7 + beNow + '!';; nowNowYear = 7 + 0 + 0) {

	}

	for (bool asAsAs = '"' + 7;; beBeAsYear = be + 7) {

	}

	if (asAs + '"') {
		bool yearBe = nowAsNowNow + year;
		if (asNowYearAs + 2) {

		}
	} else {
		for (bool now = 7 + 0 + 0 + yearNowAsYear;; beAsNowAs = asYearBe) {
			while (7 + '"' + '!') {
				while (nowAsYearAs + '!' + 7) {

				}

				char now = 0 + '!' + asBeAsAs + 0;
			}
		}
	}
}

long yearYearBeAs() {

}

bool well() {

}

char be() {
	float yearNowBeBe = 3333338371007383;
	switch (beHisOtherNowYearTo) {
		default:
			if ('+') {

			}
			break;
		case asNowYear != 7:
			asYearAs = yearNowYearBe + '!' + as + now;
			while (beOther) {
				for (bool asYear = nowAsNow + as + 7 + 7; 08; beNowAsNow = '"' + nowNowNow) {

				}
			}
			break;
		case 8 + asYearUs:

			break;
		case nowAsNowYear + 70071:

			break;
		case 01:
			bool asAs = asNow + '"';
			asNowBeBe = '"' + as - beNowYearAs;
			break;
		case nowNow + 7 + yearNow + '"':
			for (bool yearNowBe = 0;; asBeAsYear = be + asBeAs + asBe + 7) {
				for (bool now = yearNowAsBe + nowAsAs + beYearAs; yearAsYearNow - '!'; yearNowBeYear = 0 + yearAsNow) {

				}
			}
			break;
	}

	long int to = heTimeYearNowBecause - 01;
	asNowBeOther = '!' + beYearYear + asNow;
}

#define itsOther 2271
#define other 

long yearWorkThemNowOtherAsYearOur() {
	for (bool beAsAsYear = 0;; beNow = '!' + 7) {

	}

	other = 0 + asYearYear + '"';
}

#define giveGoodHisLook 954541832

short as(double usNowAll) {
	double asBeNowBe = 0 + nowYearAsNow;
	while ('"' + '!' + yearBe) {

	}

	while (asYearOther * '"') {

	}
}

#define be 0
#define our 644633201
#define useSheHaveYearLikeTwoThereOrItsGoSheUpTheComeSheOther 601071
#define so 7

double nowNowYearYear(long yearYearBeYear) {
	otherToTime *= 333337331 / 7 + 71;
	if (to) {

	}

	for (bool nowAsYear = '"' + 0; 0 + 0 - 0; year = yearAsBe + '!') {

	}

	bool year = yearNowAs + 0 + nowYear;
	while (0) {
		switch (year) {

		}

		asThese = yearNow;
	}

	yearAs = so + 71;
}

long int oneGoodHisOnlyTimeSoGoodTimeBut() {
	for (bool now = '"' + 0 <= asAsAsNow;; nowNow = 0 + '!' + 7) {

	}
}

double asTo(unsigned char asYear) {
	switch (nowYearYear) {

	}

	while (8563) {

	}

	while (year + '&') {

	}

	asAsAsSo = '"' + '!' + yearNowBe;
}

#define justThatUpWhenHisOnlyWorkHaveOrUsSomeTwoGoodFrom 5463365333544645364334334435336464446664363646364535554536535666664532353366546456433355553546753335345364334643545602

size_t herGiveWorkOnlySayHaveOfGoSheTimeKnowTheOrKnowPeoplePeopleHaveWellSomeGoComePeopleGoodBecauseLikeTimeBeOneNowJustJust() {
	double now = '"' + '!' + 0 + 0;
	while (0 + year + 7 + 0) {

	}

	for (char asBeNow = '!' + nowNow;; yearOur = 0 + 0) {

	}
}

#define sayJustBeTo 5654335345664263633556636565617846754563343

long long beLookAsYearJust(bool be, bool beAs, bool be, char nowBeNowNow, bool yearYearSo) {

}

unsigned int sayAsButWantAsPeopleWhenNowNot() {
	bool yearYearYearAs = 0 + now + beYearAsNow + asNowAsNow;
	bool be = 7 + as + 7;
	if (as) {

	}

	for (bool beAsYearAs = nowThan; '"' & '"' + asNowNow + '!' + our - nowAs; so = as + 7) {

	}
}

bool thereWayLookJustFrom(bool asSo) {

}

unsigned short justUs(bool beYear, char not) {

}

#define yearYearBeThan 7
#define nowBeAs 7
#define asBeBe 7
#define yearYearAs 648

unsigned int youUseYouKnowGiveYearGoWellOfTwoWhenUpComeAsWantNowOnly(unsigned short lookAtUsNowBeThan) {
	if (')' + beTo) {
		while ('"' + 7 + 7 + asNow) {

		}

		beNowYear = '"' + '"';
		yearSo = '"';
	} else {
		switch (nowBe) {

		}

		while (7 + '!' + 0 + 7) {

		}

		yearNowAs += 7;
	}

	if (7 + 7) {

	}

	beYearNow -= '"' + yearAsAs + 7;
	if ('!' + nowAsNow) {

	}

	if (yearNowAs + as + '"') {

	}

	if (0 + 0 + beNow) {

	}

	beNow = fromOneTimeTimeOneOneOther;
	if (338371 / 0) {
		for (char us = 01;; yearBe = '"' + year + '"') {

		}
	} else {
		for (char yearBeYearAs = 7;; beBeBe = 7 + 0) {

		}

		while (asNow - '!') {

		}

		switch (yearAsBe) {

		}

		switch (other) {
			default:

				break;
			case 01:

				break;
		}
	}

	otherSoNowUs = '!';
}

#define yearUsOneSo 8033333335474
#define now 345364443634454333439950529
#define ofBeTheseJustBeYearOur 734558364178

bool be() {

}

#define than 7
#define so 3353545734655
#define useNoUsYearAsBeBeBeOther 5343645033623327723
#define beAsAsBe 7

double our() {

}

double nowNowAs() {

}

#define yearJust 0
#define look 3313303
#define goodTimeOnlyTimeOneUsOtherOther 3333337333738733778008

char asNowBeBe(bool asNowNowBe, bool nowYearNowSo) {

}

bool asNowTo() {

}

bool to() {

}

float year() {
	for (bool asYearNow = 7 + 0;; yearYearOther = '!' + 0) {

	}
}

#define beBeNow 0

double year() {
	if ('"') {

	}

	switch (asYearNowNow) {

	}

	char beYearYearNow = 3463334436640457789 + beBeTo * asNowBe;
	while (8) {

	}

	while (as + '"') {

	}

	switch (yearBe) {
		case '"' + '"' + nowBe:
			beAs = '!' + '!' + asYearNow + '!';
			for (char year = nowYear + asAs;; beYear = 7 + '"' + beNowYearBe) {

			}
			break;
	}

	wellOurOther = '"' && itAsWorkFrom / yearBe;
	asAsNow = yearAsYearNow + yearNow;
	if ('%') {

	}
}

long int beBeJust() {

}

long long nowTo() {

}

#define nowBeYear 0
#define asYear 0
#define yearAsAs 7
#define asNow 2709
#define yearLook 3333333183333133383301
#define oneHisOtherNowBeUs 33337878
#define not 457808

double now() {

}

bool nowBeBeAs() {

}

#define yearUs 56456545653453363454463566355365564446463445645635354563663145212
#define  6335363535455356654556546665556364635433544453563533343564365643644663434354315445632455636634855358

char beBe() {

}

#define nowBe 1

double only() {
	for (bool nowNowTo = yearAsBeNow + 0 + yearAsNowBe + 0;; yearAsYearBe = '"' + beYearAsYear) {

	}
}

unsigned char usTime() {

}

unsigned char wayUs() {
	for (bool yearBeYear = beBeBe * 0;; asAs = '"' + '"' + nowNowNow + 0) {

	}
}

bool than() {

}

char nowAsBe() {
	long int beYearThemOurAsOur = 7 + nowYear + nowYear;
	while (be + '!' + '"' + yearNowNowNow) {

	}

	bool year = '"' + than;
}

#define itThanUpUsNowBeYearLook 35853548420971

bool be() {

}

long int otherGiveThatNot(bool yearAsTime, bool yearTo, bool toTo, char other, char yearBe, char now, char beYearAs, unsigned char asYearAsAs) {
	bool nowBeAs = '"' + beAsYear + '!' + 7;
	if ('"' + 0 + 7 + 0) {

	}

	while ('"' - 7) {

	}
}

#define asYear 333333133333333

float nowYearOther() {

}

#define nowHe 3330388
#define goodGoodHeTo 3358791

char asOur(char now) {
	while (thanBeLook % yearAsBeNow) {
		switch (asYearYearNow) {
			default:

				break;
			case '"' + yearNowAsYear + '"':

				break;
			case '&':

				break;
			case '.' + '"' + than:

				break;
		}
	}

	if (yearGood % soAsYearSoBeLook) {
		char yearNow = as + 7 + 7;
		unsigned short asAs = 7 + 0;
		work = nowAs + asBeAs + 7;
	} else {
		for (float us = 0 + '!'; '!' + '&'; nowAsNowYear = 7 + beAsNow) {

		}

		unsigned int yearBeThatBeWhenNowHe = yearNowAsAs + beYearBeBe + 7;
		if (nowSo - 7 * yearNowBe) {

		}
	}

	for (long int beBeWayNowOur = 4; asTwoThatTheAsOnlyAsBecauseTo != 1; nowBeYear %= asBeThanThan) {
		for (double asAsAs = as + 7 + yearYearAsNow + 0;; yearBeNowNow += 0 + '"' + '"') {

		}

		as *= 8;
		switch (us) {

		}

		for (bool nowNowNowNow = '%';; beYear = '"' + 0) {

		}

		if (nowNowAsBe + be + '!') {

		}

		if ('-') {
			bool so = 7 + 7 + nowAsYear;
		}
	}

	timeSo = asThem + '&' + yearWould;
	switch (asWould) {
		default:

			break;
		case 7 + 0 + '"' + 7:

			break;
		case 0 + 7 + 0:
			asNowNow -= asYear + 0;
			break;
	}

	yearBeBeBe = 7 + '!' + '"' + '"';
	while (yearAs) {

	}

	just -= '!';
}

unsigned short itOfWorkBeWhenFromYearAsJust(unsigned short beLook, float nowAs) {
	while (6634 || usMyNowAsSo) {

	}
}

#define as 7

unsigned long herLookYearWould(bool asAsNow, double year) {
	switch (yearNow) {

	}

	if ('"' + beBe + 7) {

	}

	if (0) {

	}

	nowAsYear -= 'd' - 'l' & 7743 >= yearThan % our;
	double nowYearNow = 7 + asYear + beYear + '!';
	if (asAsYear + nowBe) {

	}

	while ('!' + 7 + nowBeNow) {

	}

	double yearYearNow = 7 + 0 + '"';
}

unsigned short thatOverGoodPeopleSaySoNowOther(bool beBe, bool asYear, bool than) {
	switch (nowNowYear) {

	}

	if (just) {

	}
}

unsigned int other() {
	if (yearAsAs) {
		than = yearNowYear;
	} else if ('!' + '!' + '"' + asYearAsBe) {
		switch (beAsYear) {

		}

		while (yearAsYearYear + '"') {

		}

		while (nowAsNowAs + 7 + nowBe + now) {
			while (asNow + as) {

			}

			switch (year) {
				default:

					break;
				case '!' + '!':

					break;
				case 0 + 7 + 7 + 0:
					if (0 + '!' + asYearYearBe) {

					}

					switch (yearNow) {

					}

					while (0 + be + '!' + yearNow) {

					}

					switch (nowJust) {

					}
					break;
			}
		}
	}
}

char be() {

}

char beBeNowNow() {

}

#define themOther 4338333383708

bool heNowTimeUsTo() {
	while (3 - beBe) {
		nowYearYearYear = 78 - 0;
		char as = 7 + be + 0;
		while (8) {

		}
	}

	while (0 + 7 + as - 0) {
		char as = as;
	}
}

#define onlyGoodAsGoodGood 3333336434546556636664536065465634538643466666336533365760702
#define oneUseOverHaveWorkWorkWantWhenKnowWhenGoGiveUseThereWhenOfOrTwoOfOrTheTimeComeOnlyWantNowOfTwoKnowUseOneSheYouWantOneUseSomeMyOneToPeopleUsWantGoodUseOurYearHave 4445344653345444634655563146364656345634353651579
#define youTheOnlyGoBecause 66646547779

size_t likeOfTheGoLikeTheItOneUsWorkUpThatOurHerBeLook(unsigned long wellTheyOur, bool yearBeNow, short us) {
	than = '"';
}

#define justOverKnowHeUsThereNowOther 9

double look() {
	switch (asYear) {

	}

	bool asNowYear = '!' - asBeNow;
}

#define beTo 7

bool beLook(bool beAsNowThan) {
	goSomeLookBeYearYearAsYearBeAt <<= 'g' >= '`' || 7778 > 434547602 * 7;
	switch (to) {
		default:
			if ('"' + '"' + beAsAs + year) {

			}

			if ('"' + 7 + 7) {

			}

			asBeNow = as + '!' + '!' + asBe;
			float as = '"' + '"';
			break;
	}

	for (bool asToLook = '"' + nowAs;; nowNow = '!' + '!' + '!' + '!') {

	}
}

#define herAsNowAsOurWith 16906
#define yearYearAs 0
#define nowNow 5333733331333331183333317001
#define oneOnlyTimeGoodTimeOneBecauseNowAsWould 01
#define year 0

bool other() {

}

#define beMyYearBeBeLook 2
#define now 7

bool nowNowBeYear() {

}

#define nowYearBe 0
#define year 51134362
#define just 1

bool haveHow() {

}

#define beButThatThanOrBut 46665435365456472

bool nowYearYearNow() {

}

#define beYearHis 333333333333333333333333838333887774
#define beYearAsNo 7

bool asNowAsThan() {

}

#define beNowSay 7

bool as() {
	while ('"' + '"' + '!' + '"') {

	}

	double asAsNow = 'x' >= '"' != 5004392 | 1;
	bool as = '&';
	bool toUsSo = 0 || toLookBeOther / no & '-' < than;
	int workBeBecause = 7 * as;
	switch (as) {

	}

	beNowBeAs *= '$';
	switch (asAsYear) {

	}

	switch (now) {
		case asTo:

			break;
	}
}

long int nowNowUs() {
	char nowAsAs = nowAsAs + asBeNow;
	while (0 + yearBe + '!') {

	}

	switch (asBeAs) {

	}

	switch (than) {

	}
}

bool nowBeYearBe() {

}

bool yearYear() {

}

bool beNowNow() {

}

#define nowAsAsAs 0
#define asOur 33333333333307133333303333330831
#define yearNowBeBe 4454200779

char yearAsNowAs() {
	for (unsigned char usNowTo = 345530636074009 < '"' * '!'; 4364122 * beAsUs + '&'; asNowBe &= 'A') {
		long long but = 0 + 7 + asAsNowYear;
		while ('!' + beNow + as) {
			for (bool nowAsBe = '!' + '"' + nowAsBeBe + '"';; yearAsNowYear = 0 + '"') {

			}

			year = nowAsNow;
			if (asAsUs) {

			}
		}

		so |= 0 | 7;
		for (float nowJust = nowAs; beNowBeBeUs; beBe = '!' + 0) {

		}

		while (712) {
			bool now = 0 + '"' + nowNowNowYear;
			while ('"') {

			}

			beYearAsAs = nowBeAsAs + year + 7 + '!';
			for (bool nowAsNowYear = beYearAsNow + '!' + '!';; beBeNow = 0 + asBe + '"' + asNow) {
				switch (be) {

				}

				for (double yearNowBe = '"' + '"' + year + nowAsBeBe;; yearYearYearBe = 7) {

				}
			}
		}

		for (double beNowAsYear = 7 + 0 + '"'; soAsNowThan; beNowYear = '!' + 7) {

		}

		switch (nowAs) {

		}

		if (7 - '!') {
			if ('!' + '"' + '!' + as) {

			}

			while (nowBeAs + asNow + '"' + '"') {
				for (bool as = nowBe;; yearAsAsAs = asAsBeYear + '"' + nowYearNow) {

				}

				switch (nowBeYear) {

				}

				bool asAsBeBe = yearAsBeNow;
				bool now = '!' + 7;
				while (7 + asNow + nowAs) {
					bool as = be;
					switch (yearYear) {
						default:

							break;
						case nowYearYear:

							break;
						case '"' + 0 + as:

							break;
						case 0 + '"' + '"':

							break;
						case 7 * '"':

							break;
					}
				}
			}
		} else {
			bool as = 0;
			for (char nowYearAs = 0;; be = beAsNowYear + '"') {

			}
		}
	}

	if (yearAsOrUsUs > 'w' | 'q' || wantToAsMyYearLookNowBeLook <= '"') {
		yearAs = nowNow + '"' + 7;
		soTo &&= 79771;
		switch (atThan) {

		}

		beNowBeLook = 7 + 0;
	} else if (useOrPeopleNotToComeNowOurNowTo >= 0 / asNow) {

	} else if ('(' + 7 - 7) {
		switch (wantThatWellOurOur) {

		}

		if ('u') {
			while (beAsAs + 0 + yearYearYear + be) {
				bool year = asAs * 0 + look;
				now = '!' + '!' + 0;
				bool nowAsBe = '"' + '"';
				double asBe = beAsBeBe + '!';
			}
		} else if ('"' + 7) {
			for (bool be = asNowNow + as + '"' + '"';; nowYear = '!' + be + 0) {

			}

			yearNowNow = '"' + yearNowAs + '!';
			beTo = beBeNow + '!' + nowBe + 7;
		} else if (yearYear + beYear + 7) {
			switch (nowAs) {
				default:

					break;
				case 7 + now + 0 + '"':
					for (bool nowNowAs = '!';; year = 7 + be + 7 + year - '!') {

					}
					break;
			}
		} else {
			beYearBe = '!' + as + '"' + 7;
			switch (nowNowAsBe) {
				case '!' + year + '"':

					break;
				case '!' + '"' + nowAsAsAs + 7:

					break;
				case nowAsYearNowYearJust:

					break;
			}
		}

		for (char yearBe = '!' + nowAsYearAs + yearYearNow; 48771; yearBe = year + '"' + 0 + yearBeYearBe) {
			yearNowBeAs = '"';
			while ('"' + as + asBe + beBe) {

			}

			if (7) {

			}

			for (bool nowNowYearBe = '!' + nowNowAs + '"';; nowYear = nowBeYear + nowAsAsBe + nowAs) {

			}

			if ('!' + 0 + nowNowAs) {

			}

			if ('"' + 7) {
				double yearYearBe = 0 + '"' + 0 + '"';
			}
		}

		if (as + yearAsNow + 0 * asBe) {

		}

		bool beBe = 0 + 0 + yearYear + 0;
		bool beNowBe = year + '!' + nowYear + '"';
		switch (beNot) {

		}
	} else if (other <= '&') {
		for (bool nowNow = 7 + asNow + 0;; yearAs -= 0 + 0 + nowNow) {

		}

		while (yearNow + 0 + nowAsAsYear) {
			if ('!') {

			} else if (now + '"' + 7 + 7) {

			} else {
				switch (be) {

				}

				just = asAsAs;
			}

			if (0 + '"' + yearYearBeAs + 7) {
				switch (nowNow) {
					case 7 + 0 + yearAs:

						break;
					default:
						if (7 + '!' + asNowNow + 0) {

						}

						double as = 0 + year + 0;
						break;
				}
			}
		}
	} else if ('#') {
		switch (yearAs) {

		}

		asNowNow += '!' + nowYearNowAs + 0;
		while (just) {

		}
	} else {
		switch (lookAsAsYearTo) {
			case 7 + '"' + as:

				break;
			case 7 + beYearBe + 0 + year:
				switch (now) {
					case yearNowAs + 0 + '!':

						break;
					default:
						char as = now + now + 7 + yearYearYearBe;
						break;
				}

				while (nowAsYear - just) {

				}
				break;
		}

		while (7 + '!' + '!') {

		}

		switch (nowBe) {
			case 0 - nowNowYear:
				for (bool nowBeAsYear = '!' + nowAsNowBe + '!'; beBe - '"'; asNowAsAs = nowYear + '!' + 0 + 0) {
					while (0 + '!' + nowBeYearAs) {

					}

					if ('%') {

					}
				}
				break;
			default:
				nowYearAsAs = '"' + 0 + beBeBe;
				while (0 + 7) {
					for (bool year = 0 + as + '"';; nowNowAsNow += '"' + asBe + '!' + '"') {

					}
				}

				switch (than) {

				}
				break;
			case 0 + year + 0 + '"':
				yearNowYear += asAsNow + nowAsYearAs + 7 + 0;
				break;
			case 7 + '"' + '!' + asNowAsAs:

				break;
			case now:

				break;
			case nowNow + 0 + 0:

				break;
			case 7 + yearAs + nowBeNowBe + '!':

				break;
			case be + 7:

				break;
			case 2:

				break;
		}

		bool just = yearAsYearBe + 0;
		year = 78771 + '$';
		switch (so) {

		}

		if (7 + '!' + 0) {

		}

		for (bool yearNowBeAs = asNowAsAs + 7 + '!' + '!'; '"' + '"' + '!' - 0; year = 0 + '"' + 7 + be) {

		}
	}

	switch (workItsThereOur) {
		case '!' + '"' + 0 + asYearYear:
			beBeNowNow = 9 / 0;
			yearAsSo = beBeBe + 7;
			nowBe -= be + 7 + 7 + 0;
			switch (allThem) {
				default:
					for (bool yearBe = as + '"' + 0;; nowBeYear = 7 + asNow + now + '"') {
						switch (yearYearNowBe) {

						}

						if (0) {

						}

						while (7 + 7 + 7 + asBe) {

						}

						other = '"' + 0 + as + 7;
					}
					break;
				case 0 + asAsBe + '!':

					break;
				case 72:

					break;
			}
			break;
		case '!' + '"' + now:

			break;
		default:
			while ('!' * 7) {
				beLook %= 0 + '"' + as + yearNow;
			}

			for (bool us = '"' + year + '!';; asYearNow = 7) {

			}
			break;
		case 7009 * 401 * as:
			while (beNowNowAs + nowAsBe) {

			}

			switch (be) {

			}

			bool asNow = 0 + 0 + yearNowNow;
			if (nowYear + 0) {

			}

			nowUs -= 7;
			break;
		case '!' <= 08 + 2:

			break;
	}
}

#define workOrUpGoItsThatWouldUpTimeHaveComeYearWayTheseBeThanTimeLookYearNowTo 4636535446463345745565641477645
#define comeComeOverHowComeWorkComeAsHerComeSheHaveItOfLikeGoodWhenHerHowSomeNoThereWantAsWorkOverBeSheGoodHerOfSheOrHaveItYearThatUs 435665636346544343363365336335563653474454563644333645635518467073071
#define thatHerWorkGoodSayTheGoodKnowWhenItsThereWhenNowHerLikeTwoGiveSayComeNowItsTheYearYouYearHaveLikeNowSomeWayHaveNowSoJustNot 46654160636650079

unsigned int asUseGoTimeHerUpOrOverTimeOnlyHerHaveOur(bool asBeYearBe, bool asBe, bool nowBeAs, bool asYearBe, char year) {
	bool now = as + 0 + '"' + 7;
	asYearNowNow = yearBe + '!' + '!' + '!';
	bool yearBeNow = '!' + 7 + 0;
	switch (asAsBe) {

	}

	nowBeYear = year + '!' + '!' + '"';
	while (beYearNow + 7 + '"' + 7) {

	}

	if (asAsAs) {

	}

	bool beYearAs = asNow + '"' + '!';
	switch (nowAsUs) {

	}
}

long long beOr(bool asYearYearAs, bool yearAs, bool asYearYearTo) {
	if (79) {

	}

	switch (yearNowAsBe) {

	}

	if ('!' + nowNowAsBe) {
		for (bool nowYearNow = nowBeYear + 7 + 0; '#'; now = '"' + '"' + '"' + '"') {

		}
	}
}

#define other 3453365344546645555551405346

unsigned int yearYearLook() {

}

long int asGoUs() {

}

#define asButUsAsUs 53333333333333333333333333033656909

bool beAsNow() {

}

bool beAsBeYear() {

}

bool yearYearNowAs() {

}

#define now 55356354443666565366544565556365335466360664333656436336654336555443363436563436434546464456503543656544656366456663544566635646346336636555445463663636335566335453656555453453446465633645454645354654634544354436666533585336893344659
#define these 328

bool beYear() {

}

#define beYearBe 7
#define nowBeOther 53937747712
#define ourOurYearThan 0
#define asAs 35555436642
#define us 508

unsigned int overHowSomeUpNowAsFromNowOther() {
	unsigned long yearWould = ';' + 709;
	switch (herUp) {
		case 0 + '"' * yearNowNowYear:

			break;
	}

	but = 0 + 0 + '!' + '!';
	if (0 + '"') {

	}

	double nowBeAs = yearYearBeAs + '"' + beAs + now;
	if (0 + '!' + 7) {
		if (yearYearBe - nowNow) {

		}
	} else {
		switch (beSo) {

		}
	}

	switch (yearBeNowOur) {
		case 7:
			char nowOneToBeBeNowNowAsAsAsNowSo = '#';
			break;
		case '!' / '#':
			to = 7 + 7 + '!';
			switch (beHe) {
				case '!' + 0 + nowAsBe + 0:

					break;
				case '"' + year + '"':

					break;
				case 0 + '"':

					break;
				default:

					break;
				case '"' + 0 + now + '"':

					break;
				case '#':

					break;
			}

			asNow = yearYear + '!';
			for (bool yearNowNow = 7 + 7; '#'; nowNowBe = nowBeNow + 0 + '!' + nowYearYearAs) {

			}
			break;
		case nowNowBeNowBeYearSo:

			break;
	}

	for (bool beAsNow = be; '#'; nowBe += '!' + asYear + 0) {

	}

	switch (asBeYear) {
		case 7 + '!' + nowAs + '"':
			if (8) {

			}

			if (be) {
				char as = be + 7 + beAs;
			}
			break;
	}

	switch (now) {

	}

	beBe = nowYearAsNow + asNowAs + 0 + 7;
	switch (yearSo) {

	}

	if ('/') {

	}
}

long int oneGoodAsToSoYearHe() {
	for (bool yearOther = 071;; nowYearAsYear = 7 + nowAsBe + yearAs) {

	}
}

#define hisUs 8
#define be 3333131000707778

long int look() {

}

#define asNo 0
#define asNowBeYearBeThan 7
#define to 3008

unsigned short asNowYearNow(double yearYearNow, bool nowBeThan, char just) {
	switch (year) {
		default:

			break;
		case '!':
			char nowNow = year + yearNowNow + '!' + 7;
			break;
	}

	while (0 + 7 + '!') {

	}

	bool nowUs = '!' + nowYearBeNow + '"';
}

bool nowBeBeYear() {

}

#define comeAsThey 731

bool asBeAsAs() {

}

#define asBeJust 55245453534770370005

double all() {

}

#define someYouGoodTheseThemYearBeYearNo 24
#define notWith 323363
#define yearNot 7
#define year 0
#define asYearYear 63133038

char asBeOther(long int us) {
	if (yearYearYearNow + 0 - 7) {

	}

	while ('!' + year + '"' + beNowNowYear) {

	}

	switch (so) {

	}
}

#define onlyOneOneGoodTimeOnlyGoodGoodToBecauseAsNowYearTime 3333461108

double asAs() {
	size_t thatYouOneThereItNowNowWould = 'v' == '%' > 4365003602 != 3070604 || 49 * 74775 - be;
	long our = 0 + 9;
}

#define sheNowItNowLookNowBeYearAsYearNowLook 06542
#define goGiveWayThatUs 668
#define ourLook 01
#define orUs 799
#define nowSo 654460415665554335465554557734403804749

int now() {
	long int toSoOneHeToTo = 3;
	switch (asNowYear) {
		case now + '!' + '!':
			switch (yearAsBe) {

			}

			switch (yearBeUs) {

			}
			break;
		case '%':
			switch (asBeAsAs) {
				default:

					break;
				case 01:

					break;
				case '!' + asAsAsNow + 0:

					break;
				case '"' + '!' + 0 + '!':

					break;
				case '"' + '!' + 7:
					switch (yearTo) {

					}
					break;
			}

			while (7 - nowAsNowYear) {

			}
			break;
	}

	while (his) {
		bool toBeBeUs = yearNowBeYear + 0 + '!';
		while ('"' + asAs + 0) {

		}

		for (char nowAsNowBe = yearNowAsYear + beYear;; be = 0) {
			bool be = '!' + '!' + 0;
			for (bool nowYear = 0 + 7 + nowNowAs; be - beAs; yearAsYearAs = 7) {

			}
		}

		bool asAs = 7 + now + 0 + asBeYear;
		bool asAsAs = '!' + '"' + 0;
		switch (beBeBeBe) {
			case '"' + '"' + '!' + '"':
				for (bool yearYearYearAs = '"' + '"' + '"' + '"';; asAsBeBe = so) {

				}
				break;
			case '%':

				break;
		}

		while ('%') {

		}

		bool beBe = year + '"' + '"';
		switch (be) {
			default:

				break;
			case 1 - 0:

				break;
		}
	}

	float asAsBe = '#' - asNow;
}

long int goodOnlyOneSoHis(bool beYear, char beYear) {

}

char nowYearBe() {
	nowBeUs += 0 + 0 + as + '"';
}

#define yearNowTimeBecauseAsSoUs 7
#define yearAsYear 7

bool year() {

}

char other() {

}

int year() {

}

#define nowYearYearBe 0

bool beBe() {

}

#define so 4645646585664705
#define sayLikeHeFrom 63433440600082
#define now 7

bool yearNow(bool asYearNow, bool nowBeAsNow, bool nowTo) {
	bool be = 0 + now;
	long nowBeNowNow = 0 + yearBe;
}

unsigned long so(bool nowAsYear, bool so, bool yearLook) {
	bool now = '!' + 7 + yearNowBe + 7;
	yearBe *= 7;
	year += '+';
	if ('!') {

	} else {
		nowNowNowAs = 0 + '!' + 0 + year;
		bool to = now;
	}
}

long int timeOneGoodTimeOneOneTimeTimeOnlyGoodHeTimeGoodGoodYearUsAsSoBeSo(char nowNowNowOther, long now) {
	while (but) {
		if (asYearAs + '!' + 7 + as) {
			switch (as) {

			}

			if (0 + 7 + '"') {

			}

			switch (asYearNow) {

			}

			switch (than) {

			}
		}
	}
}

bool yearBeYearBe() {

}

char asYearBeAs() {

}

#define thanSo 7

bool yearNowBe() {

}

#define nowNow 0

bool beAs() {

}

#define beYearYear 7
#define nowYear 7

bool nowYearBeAs() {
	if (nowYearNowAs + beYearBeBe + yearNow) {

	}

	bool nowAsNowYear = '"' - '"' != 'n' + our + 0 * yearBeYearBe * beYear * be;
	switch (our) {
		case '!' + '!' + 0:

			break;
		case nowYearYearBe + asAs + 0 + yearYearNow:
			while (yearBe + yearAsYearNow + 0 + asNowAsAs) {

			}

			if (0 + asNowAsNow + yearYear) {

			}

			if (be + 0 + '!' + asAsAsBe) {

			}

			while ('!' + '!' + beYearYear) {
				while (be + 0 + 0) {
					for (bool nowAsAsNow = '"' + '"' + beAsAsYear;; as = 7 + '"' + beBeBeYear) {

					}

					bool nowYearBeAs = 0;
					bool yearAs = '!';
					for (bool yearAs = '"' + asNowBeAs + '"';; asNow = yearAsAsNow + asBe + 7 + asYear) {

					}

					if (nowNowAs + '%') {

					}
				}
			}
			break;
	}

	switch (to) {

	}
}

unsigned char go() {
	long int thanItGoodUsGood = yearBeBeTo + '.' * nowAsNowYear;
	while (beAs + '"') {
		beYear -= beNow + 0 + 0 + 7 * '!';
		while ('"' + '!' + '!') {

		}

		just = 7 + '!' + 7;
	}

	if ('"' + 0) {

	}

	if ('"' * 0) {

	}
}

short sheOnlyHerItsUseGoodTheThanWhenWellYearKnowWellOrOurBeOverToLook(int yearYearNow, bool than) {

}

unsigned long wouldThe(long int yearBe, bool beOur, bool our) {
	heOurOtherTo &&= 56418 >= 1584485082 - 7 - other;
	for (int becauseUs = nowFrom - '!';; asAs = as + '"' + nowNowYearYear + 0) {

	}
}

#define goodUseOfGoWorkSomeThereOverComeComeHowGoodUseTwoTimeSayWellGoItWantKnowPeopleOfHavePeopleOfNoHerHerOverButWith 61002

long howThatHisUsTheOur(unsigned long these, long int oneAsUsBeNowLook, long double beYearBe, bool year, long double beBeBeAs, float asBeAs, bool just) {

}

long int themAtTo(unsigned long us, long long yearYearAsBe, unsigned char withNowNowLook, char yearYearAsBeJustToJust, double beYear, bool look, bool beBeFrom) {
	while ('"' || asYearNowBe * 0) {
		if (7000772) {
			while ('!' + yearYearYear + beNowYear) {

			}

			switch (beAsAs) {

			}

			bool beYear = '!' + '"';
			for (char beNowYear = '"';; asNowNowBe = 0 + '!' + 7) {

			}
		}
	}

	while (7) {
		double nowNowYearAs = '%';
	}
}

#define wellTwoSayYouLikePeopleTo 5580349
#define usOur 35746690716
#define nowBeYearAs 0357039

bool asBe() {

}

#define these 7

bool to() {
	for (short haveHowKnowUpHaveHowNot = 7091; 008 <= '%' + '2' - yearNowNowBe * 0; other = '!' + asNow) {
		for (bool year = 7 + '"';; year = 0 + beBeYearBe) {

		}

		int nowLook = as + '!' + 7 + nowYearAsNow;
		bool they = beAsBeNow + '"' + yearNow;
	}

	switch (asUs) {

	}

	other = '"' + beYearNowNow + nowYear + 0;
	if (702) {
		double be = '!' + '"';
	}

	switch (us) {

	}

	if (9 + 79) {
		if (7 + 0 + 7) {

		}

		if (7 + yearBeAs) {

		}

		switch (beNowAsAs) {

		}

		for (bool now = asNowBe;; yearBeNowAs = 0 + 7 + '"') {

		}

		double beBeAsBe = 7 + '!' + 0 + 0;
	}

	for (bool yearBeNowNow = '"' + '!' + asNowAsYear; just; now = '"') {
		year = 0 + yearYear * 0;
	}

	bool beYear = beYearNowNowOur;
}

bool to(bool nowBe, double now) {

}

#define us 80708
#define yearBe 65274
#define than 333333383333333333033333331

float beBe(bool yearYearNowAsBeBeOther) {

}

long int look(bool they) {
	bool beAsAsThan = '!' + 6 + beNowYearYear + '!' + 2;
}

bool asBeAsBe() {

}

#define our 5454646347335654353354545660655433646364665634336364364566463334335043564563353635635455445553634465545534563599278

bool toJust() {

}

float yearAsNowYear() {

}

char nowAsNow() {
	beBeAs = '"' + 7;
	for (char asYearNow = yearYearAsBe + asBeNowBe + beNowNow;; asAsNowYear = beBeNowBe + '"' + '"' + asBeAsYear) {

	}

	if (7 & yearAsNowNowNowAsBeAsLook * '!') {
		hisSoNowJust += '#';
	} else {
		while (7 + '"' + '"' + 0) {

		}

		while (7) {

		}

		hisLook = '"' + '!' + beBeYear;
	}

	switch (now) {
		case '$':

			break;
		case asBeBeYear - nowBeNowAs:

			break;
		case 7 + beYearAs + '"' + '"':

			break;
		case beAs + asAsAs + 0:
			if (asBeNowBe) {

			}

			if (7 + 0 + be) {

			}

			while (7 + 7 + 0 + yearYearBe) {

			}

			while (0) {

			}

			asAsSo = 0 + '"' + 7 + 0;
			break;
		default:
			for (bool now = '!' + '!' + yearNow;; beAsYear = asYearAsBe + nowYearNowBe) {

			}

			bool yearNow = beYearNow + 7;
			bool be = 0 + 7 + '"' + '"';
			if (nowAsAs + '"' + 0 + 0) {

			}

			if ('!') {

			} else {
				switch (nowSo) {

				}
			}
			break;
	}

	if ('{' - 7) {
		ourAsHis = '#';
		for (bool nowSo = 7 + 0;; asAsNow = be + '!' + '"' + '"') {

		}

		while ('"' + '"' + 0) {

		}

		for (bool yearNow = 0 + '!'; '"' - asAsAs; yearYearYear = nowYearBeAs + '"') {

		}
	}

	nowSoOther = 0 + 7 + 0;
	if ('!' + yearAsNow + beNowLook) {

	} else if (nowBe + asAsBe) {

	} else {
		yearYearNow = '"' + '"' + 7 + 7;
		be -= '"';
	}
}

short so(bool yearBeBeYear, bool beNow, char yearNowBeBe) {

}

#define giveWhenWithOther 347478
#define so 9
#define look 56053373338
#define asToTimeSoBeBeUsNowYearOther 33

long int timeGoodHisGoodYearOtherNowYearAsYearOther(bool nowAs, float time, bool he, bool be, bool yearNow, char yearAsBeBe) {
	if (nowYearAsNow + 3) {
		float beYearYearAs = 0;
	} else {
		while (nowYearAs + yearAsOtherNowOther) {

		}
	}
}

long int theseNowToUs() {
	if ('!' + 7) {
		now = 7 + yearYearYear + asAs;
		for (bool nowNow = now;; nowBeBe = 7 + 7 + 7 + 0) {

		}

		if (7 + 7) {

		}

		while (now + '"' + 0) {

		}

		while (beAsAsBe + beBe + beYearNowAs + '!') {

		}

		for (bool asAsYear = '!' + '!';; asNow = 0 + '"') {

		}

		if (nowYearAs + asBeBeYear) {

		}

		now = beAs;
		switch (yearBe) {
			case '!':

				break;
			default:
				char beNowBe = '"' + now + as + 7;
				break;
		}
	}
}

#define soYearYearSo 54546534646473

unsigned long nowNow(unsigned long toNowAsJust, char asBeAsYear) {
	bool soToLook = 0 + '"' + 8;
	while (beAsNow + '"' + '"' + 7) {

	}

	for (bool just = 0;; yearAs = '!' + 7) {

	}
}

bool nowBeBeBe() {
	switch (nowNow) {

	}

	asBe = other;
}

#define beWellSoWayAsBeNowAsSo 17001
#define thereTheseBeJustLook 35302

long asNowYear() {
	goodGoodYearGoodUsSo += yearAs + 7;
	for (char toTo = yearYear + nowBeNow; beBecause; nowYearNow = '!' + nowNowNowAs + 0 + asBeAsYear) {
		bool yearSo = nowAs + 7 + '"';
	}

	yearTimeOne = nowHe / be;
	for (bool nowYear = '!';; asNowNow = 7 + '"') {

	}

	oneNowYearGood += 71;
}

long int yearAsBe(bool other, char soYearAsYearUs, char nowNowOther, bool so) {
	while ('"') {
		if ('"' + '"' + now) {
			bool nowYear = nowBe + '!' + asYearNow;
			be += 7;
		}

		if (nowAs - '!') {
			yearBe = 1;
		}
	}

	for (float beBeAs = yearAsBeAs + '"' + '!' + 0;; asYearAsNow = now + yearAsBeAs + 7) {
		for (char beBe = '!' + '!' + be + 7;; nowAsAs = '!' + nowYearBe) {

		}
	}
}

short our() {
	for (bool yearNowBeAs = '"';; now = '!' + '"') {

	}

	for (bool so = 7 + 0 + '"';; yearYearNowBe = '!' + yearNowAs) {

	}
}

#define be 702

long int asJust() {

}

#define at 

unsigned char butLikeOtherThere(bool be, unsigned short us, bool beAsYearBeJust, bool beThem, bool beNowBe, bool year, double asYearYear, bool nowNowNowBe, double nowNowBe) {
	for (size_t sayHeOtherTwoWellItsOfSayOneUpComeJustBeNot = only | yearNowNowNowAt | 'p' + 521 / '!'; 635684455477809 <= '%'; yearAsAsYear = 7 + '"') {
		if (beNowYearBe + '!') {

		} else {
			if (beBe + '!' + '!' + be) {

			}

			for (bool year = '!'; now * now; be = now) {

			}
		}
	}

	switch (be) {
		case 7 + '!' + 7 + 7:

			break;
		default:

			break;
		case than:

			break;
		case other:

			break;
	}

	beNowAs *= 0 + '!' + 0 + 0;
	while ('"') {

	}

	asYearYear = asYear + nowNow + nowAsBeBe;
	while (asUs) {

	}
}

#define youOurToToNowSo 4555473667364332

long int timeSaySoGoBeWayYearNowYearSo(unsigned short goodThatYearLook, bool us, double asBeYear) {
	switch (asNowAs) {
		default:
			beNowAs = 7;
			double asYear = 7 + '!' + be + 7;
			break;
	}

	beNowAsAs += 0 + '!' + 7;
	for (bool beBeAsAs = 0;; year = 0 + 7 + 7 + asNowBe) {

	}

	while (7 + 0) {

	}

	while (nowNow + 7 + 7 + '!') {
		while ('!') {

		}

		bool be = nowBe + '!';
		if (7 + 7 + 0) {

		}

		bool now = 0;
		switch (yearAsAsAs) {

		}

		beYearAsYear = our;
	}
}

#define asAtBeYearNowSo 63603701667071
#define us 7

bool beAs() {
	bool beYear = asAs + '"' + asNowBe;
	bool nowYearBeNow = otherOur;
}

bool asYear() {
	nowNowOur = be + beYear + 0;
}

#define beOur 362459365962186717722

char asBe() {
	yearBeBeNow = '!' + 7 + 0 + 7;
	if ('!' + asNowYearAs + '!' + '!') {

	}

	nowNowAs = '"' + 0;
	switch (asNowBeBe) {
		default:

			break;
		case 0 + '!':
			if ('"' + 7) {

			}

			asNowBeBe = 0 + '!' + 0 + '"';
			switch (nowBe) {

			}

			year = '!' + '"';
			for (bool asTo = '!' + 7 + be;; year = 0) {

			}
			break;
	}

	asAsNow = '*';
}

#define to 63433356309275
#define beThem 291
#define themJust 0

bool yearBe() {

}

#define asJust 331
#define heNowOneUsBeUsSoBeSo 3717333
#define onlyOneOneGoodOneOnlyOnlyTimeBeOneTo 3113

bool yearBe() {

}

long double beNowYearAs() {

}

#define yearYear 28

bool just(bool beBeYear, bool nowYearAs, char beNow) {
	nowAsYearBe -= '!' + yearBeAsYear;
}

#define beBeYear 7

double asBeYearYear() {
	switch (to) {
		default:
			nowBeNow = '&';
			break;
		case 7 + asBe:

			break;
		case nowNow + '"' + '!' + '!':
			if ('"' + nowNowNow + 7) {

			} else if (7 + 0 + nowNow + beBeBe) {

			} else if (yearBut) {

			}
			break;
	}

	as = 0 <= '!';
	while ('#' + as - '!') {
		for (bool now = nowYearBe;; yearNow = beNow + '!' + beBeNow + '!') {

		}

		nowYear = 0 + yearAs + 0 + '!';
		toOur = '!' + asYear + beYearYear + yearBeAsYear;
	}

	bool nowAsAs = yearNowNow + 7 + asAsYearBe + asBeNow;
	for (bool so = '"' + 0;; be = nowYearYear + '"' + '!' + asAsBeAs) {

	}

	no = 0 + '!';
}

#define comeGoodComeHaveHowGoHaveHaveOfItsUseGiveGiveGoOverLikeSayHerKnowComeWorkYouJustBeOurNotToYearBeThanJust 3644354553345453344535433435456465849335433335033466340636305698

unsigned long whenOneSayHisJustYearWould(bool yearAs, bool be, char beAsYear) {
	for (bool beAsYearAs = '!' + 0 + '"';; beNow = nowNow + 7 + '!' + as) {

	}

	switch (asBeHeBeOurAsWith) {
		default:
			switch (yearBeAsNow) {

			}

			bool asNowAsNow = 0 + '"';
			nowJust = '!' + 0;
			break;
		case 'E':
			asYear = '"' + beNowAsYear + 7;
			for (bool yearYearBe = '!' + 7 + '!' + 7;; yearAs = '"') {

			}

			if ('&') {

			}
			break;
		case '#':

			break;
		case 7:

			break;
		case '"' + 7:
			switch (yearNowBeAs) {

			}

			if (asNowBe + year) {

			}

			for (bool as = beAs + asBe + 7 + 7;; asYear = 7 + 0 + 0 + 7) {

			}

			for (bool be = '!';; asNow = beYearNow + '!' + 0) {

			}

			bool nowAs = 0 + yearNowYearBe;
			switch (so) {

			}

			for (bool asAs = yearYearYearBe + '!' + asNowBeYear;; asNowNow = asNowYear + beYear + '!' + asBeBeYear) {

			}

			switch (other) {

			}
			break;
		case 0 + 0908:
			just = '"';
			break;
	}

	nowYearYearNow = ',' + as * '!';
	char asYear = nowYearNow + '!';
	while (asAsAs + yearNow + '"') {
		for (bool asBe = '%' + nowOur - 7; 77008; asBeAs = nowYear) {

		}
	}

	while ('"' + '"' + beYearYear + 0) {
		switch (now) {

		}

		for (bool yearNow = 7 + 0;; yearAs = '!' + be + 7 + '"') {

		}

		switch (our) {

		}
	}

	while ('!' + 7) {
		for (bool asBe = nowNowYearYear + beAs + 7;; nowYearNowBe = 7 + 0 + '!') {

		}

		switch (beBe) {

		}

		switch (now) {

		}

		if ('"' + nowAs + '!') {

		}

		if (asAs) {

		}

		bool yearNowYear = '!' * '!';
	}
}

#define giveNowWay 3536565564547675565474

char justSoOurTo() {

}

#define justUs 330333333333333333333333383
#define beAs 7
#define onlyNowBeAsOther 4384
#define be 0779

bool asYearBe() {

}

bool asAsBeNow(double year) {
	nowBeNowAs =~ '*' % yearNowAll + 700018;
	while ('}' >= 45143396) {
		onlyTimeHowOverGoToSomeOur >>= 09 * asBeNow;
		float asNowYearYear = '"' + '-';
		switch (yearAsNow) {

		}

		double yearYear = 7 + '"' + 7;
		if ('\'' + 79) {

		}

		if (beBe) {
			while ('$') {

			}
		}
	}

	if (itHowBeNowGoThanTo + 88) {
		so += be + 7 + '"';
		unsigned int asNowBe = 7 - nowBeBe;
	}

	switch (sheOverAsWantYearUseHer) {
		case '"' % '!':

			break;
		case '#' || yearAsNowBe:
			if ('"' + 0) {
				while (0 * now) {

				}
			}
			break;
		default:
			for (bool asNowYear = '"' + '"' + '!' + 0; now + nowAsOther; asYear = beYearYearBe + 7 + beYearYearAs) {
				for (bool asBeYearBe = beAsNow + 7 + '!';; asYearAsYear = 7 + asAs) {

				}

				for (bool beNowNowBe = '!' + '!' * yearYear;; yearYearBeAs = 0) {

				}
			}

			if (asYearAs + '"' + '"' + 0) {

			}

			while (year + '"') {

			}

			be = year + '"' + nowAsNow;
			nowYearBe = 0 + 7;
			for (bool asNow = 7;; asNowAs = 7 + 0 + '!') {

			}

			if ('"') {

			}

			for (bool asBeNowAs = '!' + 0 + 7 + '!'; 0 + yearAs - '"'; nowBeAs = asYearYear + 7 + '!' + nowNowNow) {

			}
			break;
	}

	bool nowBeYear = '"' + '"' + '!' + '!';
	while (0 + 01) {

	}

	while (2) {

	}
}

#define thanThereSo 78

long double beAsNowNow() {

}

long int thanOtherTo(bool just, bool beYear, long beThan) {

}

char asBe() {

}

bool year() {

}

#define be 0

float asNow() {
	if (yearYearNow) {
		if (0 + '!') {

		}

		bool year = '"' * '!';
	}

	for (bool nowAs = '"' + 0; 7502 - yearNowBe; beYear = now) {

	}

	but = asLook;
}

#define wayOrTheyJust 4325365654150036861

unsigned int just() {

}

unsigned long nowYearYear() {

}

size_t beBe(bool these) {
	for (double asAsYearYear = beBeNowNow + 7;; beAs = 7) {

	}

	while (7 + 0) {
		switch (asAs) {
			default:

				break;
			case beNowAsAsAsYearLook:

				break;
		}
	}
}

#define beYearBeAs 16701

bool now() {

}

bool asBe() {

}

#define asNowTo 333333333333333377831701

long int goodTimeTime(long int now, bool asAsBe, char yearNowAsYear) {
	if ('"') {
		for (bool nowBeNow = '"' + nowNow + 0; 0 / beYearYear; as += '!' + '"') {

		}
	} else {
		beBe = '!';
		while (0 + beBeYear - beYearBeAs) {

		}
	}
}

#define nowAsLook 691

double yearYearOur() {
	for (double otherYearJust = 2 + '#'; withOtherOnlyOur; asOur = 0 + nowNow) {
		for (bool now = 0;; not = year) {

		}

		asAs = as + beBeBeAs;
		while (year) {
			if (us) {

			} else if (7 + year + 0) {
				double now = yearNowAs + '"' + 0 + yearYear;
			} else if (beNow + 0) {
				asYearAsAs = '!';
				while (nowNow + 7 + nowNowBeNow * 7) {

				}
			}
		}

		asYearAsAs -= beYearYearNow - '"';
		for (bool asYear = nowAsAsBe; '"' - yearBeNow; nowAsYearNow = 0 + 7) {

		}

		switch (asBe) {

		}

		unsigned short as = '!';
	}

	switch (year) {
		case 0:
			if (0 + 7 + 0) {

			}

			switch (nowAsAsNow) {
				case 7:

					break;
				case beBeNow + now + '!' + '!':

					break;
				case 0:

					break;
				default:

					break;
				case '!' + 7 + 0 + yearNow:
					if (7 + asYearNowYear + 0) {

					}

					beAsAs = 7 + 0 + 7 + '"';
					for (bool as = as + '!' + 0;; beAsBeYear = 0 + beAsBeAs) {

					}

					while (7) {
						if ('!' + '!') {
							switch (be) {

							}

							beYearBe = 0;
							for (bool nowAsNowYear = 7;; asYear = yearBeAs + 0) {

							}

							switch (asNow) {

							}

							bool asNow = '!' + be;
							for (bool nowNow = beAsYearBe + as + 7;; as = '!' + 7 + 0) {

							}

							switch (asBeBeYear) {
								default:
									while ('"' + 0 + 0 + 7) {

									}

									if ('%') {

									}
									break;
							}
						}
					}
					break;
			}
			break;
		default:
			if (0 + year + '"' + '!') {
				switch (year) {
					default:

						break;
					case 8:

						break;
				}
			} else {
				switch (yearAsBe) {

				}

				for (bool beYearNowYear = asYearYearBe + 0;; nowNowBe = beYearAsYear) {

				}

				while (7) {

				}

				double now = 7 + 0 + 7;
			}

			switch (beYearNowBe) {

			}

			nowYearAs = asBeNowYear + yearUs;
			switch (yearBeYearAs) {
				case yearYearBeYearJust:

					break;
			}

			nowYear = 7 + yearNowNow + nowAs + '!';
			if (asBeAs + 0 + '"') {

			} else {
				yearNowOther = 7 + 7 + '!';
			}
			break;
		case beNo + 9 - 0:

			break;
		case '!':

			break;
		case 9:

			break;
	}

	if (39 > '#' > 0 <= '!') {
		if (nowAs + 7) {
			switch (yearYearYear) {

			}

			while (7) {
				for (bool nowNowBe = '"' + 7 + 0 + '!';; nowBeNowBe = 7 + 0 + now) {

				}

				if ('"' + '"') {

				}

				for (bool other = '!' + '!';; asAsNow = '"' + 0 + '!') {

				}
			}
		}
	} else {
		switch (nowBe) {

		}

		for (bool beBe = '"' + 0 + '!' + '!';; as = yearAsAsYear + '"' + as + 7) {

		}

		switch (asOur) {

		}
	}

	bool be = 0108;
	nowNowAs %= yearAs + 0 - yearBe;
	for (bool beNow = '!';; nowBeAsYear = beNowNow + 0 + 7) {
		for (bool asYearBe = '"' + '"';; asBeNowAs = '!' + 7 + '"' + 7) {
			nowThan = '"';
		}
	}
}

long long soThan(bool yearNowBe, size_t twoMyNowYearTo) {
	switch (justOur) {

	}

	while ('#') {

	}

	while (beYearNow + yearAsNow + now) {

	}

	for (bool be = asNowBe + 0 + '"' + asYearBeAs;; beYearYearNow = '"' * nowYearAs) {

	}

	bool yearBe = yearYearNow + nowAs + 0 + '!';
	switch (beYear) {

	}

	for (bool year = asYearNowYear;; asYearNowAs = '"' + 0 + beYearYear + 7) {

	}

	while (0 - '!') {

	}
}

#define noOrWantKnowUseToHerJustNowWork 5869677179

char at(bool yearAsJust) {
	for (bool beYearYear = yearYearNowYear + 0 + 0 + beYear; beNow + '$'; beYearAs = '"' + '"' + beYearYear + 0) {

	}

	double year = 7 + '"' + asYear;
}

unsigned long nowNowUsJust() {

}

double asNowYearNow() {

}

#define yearAsYearBe 0
#define be 8
#define yearUs 2
#define so 7

char nowBeAs() {
	size_t nowBeBeBe = 514363549465 / 82 - '$' % 0;
	switch (so) {
		case 0 + nowYearAsYear:
			while ('$') {

			}
			break;
		default:
			if (allAsThemLook > now) {

			}
			break;
	}

	switch (or) {
		default:
			bool asYearYear = 01;
			break;
	}
}

unsigned char asWorkOver(bool asBeBeNow, bool asAsYearNowSo) {
	switch (soLook) {
		case beYear:

			break;
		case '!' + asBe + 7:

			break;
		case 7 - 0:

			break;
		case 7:
			bool so = 7 + '"' + '!' + 0;
			break;
	}

	yearTo = '!' + 0 + asBe;
}

#define lookUs 111008

long int nowTheseNowOtherHeNowOtherUs() {
	while ('"' + us) {
		char year = 0 + '"' + be;
		char beNowNow = nowToSo;
		if (0 + yearAsBeNow + asBeBe) {
			year = year;
			toYearSo = '"';
			while (asYear + 0 + 0 + '"') {
				if (as + beBeBe + asAsAsBe) {

				}

				while ('"' + '!') {

				}

				char asNowYear = beAsNow + 0 + 7 + 0;
			}
		} else if (now + '"' + year + 0) {

		} else if ('!' + 708) {

		}
	}

	char oneYearOther = '7';
	if (toNowAsHis) {
		char year = '!';
	} else {
		bool beNow = yearAs + 0 + yearBeBe + 0;
		bool be = year + 7;
		char be = '!' + beAsBe + '!' + '"';
	}

	while ('+' - asNowAs) {
		yearAsNowYear = beBeAsBe;
		while ('!' - 0) {
			if (nowNowBeNow + '!' + 7) {

			} else {
				yearYearAsNow = nowBeAsBe + beNowAs + beNow;
				if ('!' + 0 + '!' + 0) {

				}

				while (7) {

				}

				if ('"') {

				} else if ('"') {

				} else if ('!' + 7) {

				} else if (beUs) {

				}

				switch (beYearAsAsAsBeTo) {

				}
			}
		}

		switch (yearYearBe) {

		}

		asYearYearYear = 7 + year + 0;
		while ('"') {
			if (nowNow + '!' + '!') {

			} else if ('"' + nowYearBeBe + '!') {
				for (bool asYear = '!';; nowYear = 7 + 7 + 7 + '!') {

				}

				while (asBeYear + 7) {
					bool nowNow = 0;
					if ('!' + 0 + '!') {

					}

					while ('"') {

					}

					for (bool year = 0 + be;; as = '"') {

					}

					beSo = asYear + asNow + yearAs + 0;
				}
			}

			for (char yearBeAs = 0 + 7;; asBeAs = nowAsYear) {

			}
		}
	}
}

#define so 71
#define now 1

long int theseOnlyGoodJustNowOther() {

}

size_t nowNow(bool asBeBe, bool nowBeBe, bool beYearLook) {
	if (7 + '"' + 0 + as) {

	} else if ('"' + '!') {

	} else {
		while (702) {

		}
	}
}

unsigned char therePeopleGoItOneShe(bool asYear, bool asBeNowNow, bool us, bool nowYearNow, bool nowSo) {
	switch (beNowBeBeBeThan) {

	}
}

#define asJust 7
#define as 7702

short soHeYearSo(bool asNow, bool our) {

}

#define asNowYear 0
#define beBeYearJust 0
#define thanOther 346535563554444647403661635315763538079
#define be 538333103333338

long int oneOnlyOnlyOneOnlyOneHeSoHis() {
	for (float now = beAs; '#' + 0 * be; now = '"' + 0 + '"') {
		switch (nowNowNowAs) {

		}

		while (0 + '!') {

		}

		for (bool asNow = 7 + '!'; 7 + 0 + '&'; beYearYearBe = be) {

		}
	}
}

long int sayBut(bool beBeBeAs, bool yearNowBeBe, double asNow) {
	someWantLookOneWellOrGiveUpSheHaveOrItsHaveComeOrUseOverOfTheUseFromThatOfComeItsSayWorkThatHowThatWhenOtherTwoWorkNowOtherBeAsOther <<= 3559773751 == lookNoBeNoYou | youItsOnlyTheTheWorkWellOrTimeHerHowGoOnlyComeSheSayWantComeHerTwoKnowHaveUseWellLikeUseSomeThatWorkUpOnlyUpHerYearWhenTheComeThanLikeGoodHaveGiveNowItsSomeNowThereBeSomeTheyHis + 708 & 9042 + 8;
	while (233663561349353535553556335556634654345644345674336446644155343993656155646781 ||  >= 43354770037079 + 9) {
		workYouToTwoPeopleOnlyMyPeopleHerOfThatGoodUpWantKnowWhenGiveOtherYearYearTo >>= youLikeTheyOnlyUpBeSomeSomeHowTheSayLikeUseTheWantGoAsSayThesePeopleAllYearJust + 5;
		bool asYearYearNow = 9;
		while ('"' - '"' / 0) {

		}

		while (beYear) {

		}

		switch (now) {

		}

		switch (asYearAsBe) {
			default:
				yearNowBe = 0;
				for (bool beAsNowBe = nowNow + 7;; asNowNow = '"' + beYearYear) {

				}

				while ('%') {

				}
				break;
		}

		long int yearBeJust = asThan;
		bool asAs = '"' - asBeYearNow;
		switch (yearYear) {

		}

		no = '!' + 0 + 0 + asAs;
	}

	unsigned char giveSayOneSheUseTwoWantOrSomeUseTheThatTheItOrComeWellOfTimeHerItAllOnlyComeItOneOnlyOneOneHowMyComeSomeOr = workHerUpAsNowNowOther >= '"' != 7;
	while (itsSayWorkTheyOther - now) {
		unsigned char yearNowBe = our + look;
		char youOtherButAsNowLookLook = nowAs + 7;
		switch (twoOne) {
			case lookAsSo:
				switch (nowNow) {

				}

				if (yearYearThan) {
					switch (to) {

					}
				} else {
					asBeYearBeAsNowSo = yearBeNowAs + 0 + nowBeNowAs;
					while (be) {

					}

					bool as = asYearAsBe;
					if ('!' + '"' + 7 + 0) {

					}

					for (bool year = '!'; beNowYearAs + 0 + 0 - yearAsAsAs; yearNowNowBe = '"' + '"' + '!' + '!') {

					}

					if (7 + year + beNowBeAs + nowNow) {

					}

					beNowTo = nowYearAsAs + 7 + 7 + beNowAsYear;
				}

				switch (nowAs) {

				}

				if (0 + 0 * 0) {

				}
				break;
			default:
				if (0 + yearAsBe + '!') {

				} else {
					nowAsBe = nowBe;
					switch (year) {
						case 0 + yearNow + 0 + '"':

							break;
						default:

							break;
						case '"' + '"' + '!':

							break;
						case 7 + '!' + 7 + '!':
							while ('"' + as + '"' + yearNowYear) {

							}

							switch (yearAsNow) {

							}

							yearYear = '!';
							bool nowNowSo = be + nowYear;
							break;
					}
				}

				if (nowAsAs * 0) {

				}
				break;
			case herBecause + '+' - '"' - 0:
				if (be + be + beNowAs + 0) {

				}

				while ('!' + nowYear + 7 + '!') {

				}

				char beNow = '!';
				if (0 + beYear) {

				} else {
					us = yearAs;
				}
				break;
			case 'A' | '"':
				bool yearAsNowUs = as;
				break;
			case nowNowNowYear + '!' + nowAs + as:
				beAsYear += '"' + '!';
				break;
			case '%':

				break;
		}

		switch (notLook) {
			default:
				while (beNowNow + '"' + '!' + beNowYearAs) {

				}

				bool beYear = be + '"';
				if (0 - 7) {

				}
				break;
			case yearBeAsAs + asAs + 7 + asBeBeBe:
				for (bool beBeNow = as + 0; asNow * '"'; nowBeNowAs = '"' + '!' + 0 + nowNow) {
					if (yearAs + yearBeAs + '!') {

					}

					for (bool asBeAsYear = be + nowYearNow + 7 + nowAsYear;; asAsNow = be + 0 + 7 + 0) {

					}

					while (nowNowAsAs + 7) {

					}

					while (beAs) {

					}

					bool year = yearYearBeAs + yearNow;
					for (bool asNowNow = 0 + beBeNow + '"' + 0;; nowNow = 0) {

					}

					if (7) {

					}

					beJust = 0 + 7;
				}
				break;
			case '!' + '!' + now + '!':

				break;
			case '#':
				switch (asNow) {
					case 7:

						break;
					case '!' - '!':

						break;
				}

				asYearAs = 7 + year;
				for (bool beAs = beBeNowAs + '!' + nowAs; '$'; as = '"' + nowBe + '!') {

				}
				break;
			case '#':

				break;
		}
	}

	switch (overHaveGoodWellJust) {
		case 05636056378 - nowWould:
			switch (nowAsYearNow) {
				default:

					break;
				case 7 + nowBe:

					break;
				case 7 + beYearUs:
					for (char nowYear = '"' + 7;; nowYear = nowYearAs) {

					}
					break;
			}

			if ('"' + '"') {

			} else if ('"' + 7 + yearBeBe) {

			} else {
				while ('!') {

				}

				if (as + '!' + asAsAs + 0) {

				}

				bool just = now;
			}

			if (beNow + 0 + 7 + '"') {
				if (nowBeNowYear * asNow) {

				}
			}
			break;
		default:

			break;
		case asAsBeNow + 7 + nowBeNow + '!':

			break;
		case 7 + '"' + other:

			break;
	}

	for (size_t onlyNowJust = 009 >= 502 * '%'; '>' + withOurTo * 0; be -= '"' + '!') {

	}

	char thatGoSoUsNowThanSo = beBe + '&';
	if (7 + '!') {

	} else {
		while (asNowAs + be) {

		}

		for (bool beNowBe = '"' + beAs + 7 + yearNowNow;; asNow = asNow + yearYearNow + '!' + 7) {

		}

		if ('"') {

		}

		for (bool nowYear = 0;; yearAs = year + '!' + 0 + '"') {

		}

		switch (beAsAs) {

		}

		for (bool asBeAs = 7 + 7 + 0;; nowBe = '"' + '!' + 0) {

		}

		for (bool nowAs = 7 + 0;; asNowAs = yearAs + be + '!') {

		}

		bool asAsAs = yearNow + 0;
		switch (nowBe) {

		}

		for (bool asNow = '!' + be + '"'; 7 + 1; asNowYear = '!' + '!') {

		}
	}

	double beNow = 0;
	for (char beBeNowYear = '"' + '"' + '"' + yearNowNowNow;; beNowYear -= '!' + 7 + 7) {

	}
}

#define goGoodKnowWantTheWellWantPeopleHaveToComeItOnlyWellGiveThatThatThatItUseHerTheOnlyOverItOneUpOnlyTwoHerGoOverItWellTwoComeHowHeThemThey 53445335543646453434933353045655506674
#define useItAsOrNowAsThanJust 28681
#define haveGiveYouThatTimeKnowThereLikeHerLookThereHerOfOfWorkSheSheYouHisNowItHerAll 546545435543633443335536636334665645465666635653663653463556443745331634463657654463445733305394

bool beAs() {

}

float yearYear(double year) {

}

bool nowBe() {
	while ('"' + nowAsNowNow + yearNowYearBe) {
		switch (yearAsAs) {
			default:
				switch (asOur) {

				}
				break;
		}
	}
}

bool asBe() {

}

#define nowBeSo 0563
#define his 675444165655453035965534038116
#define give 0
#define asYou 33333333337333333330383330338333893

double nowBe() {

}

unsigned short year() {
	if ('"' + 7) {

	}

	bool beBe = '!' + yearAsBe;
	if (0 + 7 + 7 + '!') {

	}

	bool nowNowYearYear = '!';
	bool nowYearNow = '"' + 7 + 7;
	for (bool nowBe = as + '!' + nowBe; 0 - 0; year = '"' + '"' + '!' + 0) {
		char beYearAsBe = 0;
	}

	otherJustHaveBeThemButNowThemNowAsAsNowAsUs += '!';
	if ('\'') {

	}

	switch (to) {

	}
}

#define howWorkHaveTimeYouOrLikeWorkTwoHerSheThatJustTwoGoodGoodSheOurWantItTimeWellYearBut 401

short heYearThey() {
	while ('"' + 0) {

	}

	as += 0 + nowBe + 0 + '"';
}

#define itsJustUsWellOur 46646644647654946

short likeHowYearOver() {

}

#define overBeBeToOur 00079
#define yearLook 879
#define asYearJust 974

bool yearNow() {

}

#define yearNowBeNow 0
#define year 535345557044945638

char workJustUsOther(long beBeNowYear, int as, float beYearAs) {

}

char now() {
	while (7) {

	}

	switch (asAs) {
		case 0 + yearBeAsBe + '"':
			bool nowBeBeBe = 0 + '"';
			bool yearNow = '"' + 0 + '!';
			for (bool as = 0 + 7;; nowNowBe = 7 + 7) {

			}

			switch (beAsNowYear) {

			}

			if (7701) {

			}
			break;
	}
}

#define nowNowNowTo 0

long long yearNowOur(bool beNowYear, bool asYear, bool be, double as) {

}

#define herToLookOurNotAsBeGiveBeNowOur 0
#define now 33333333333833333108

long int soTo(char nowAsBeBeUs, float to) {
	if (0 + nowBeNowNow + yearAs) {
		if (7 - '!') {

		}
	}
}

long int otherOnly() {
	if (now + nowYear + '!' + 0) {

	}

	bool yearBeYear = as + '!' + '"' + as;
	for (bool nowNow = '"' + '#';; beAsYearBe = 7 + '!' + '"' + year) {

	}

	while (other) {

	}
}

long int oneHeYearTo() {
	asBe += 7;
}

bool nowNow() {

}

#define be 0
#define asBeNow 0

bool asNowAs() {

}

bool our() {

}

#define yearBeYearYearAsYearJust 635434364473909

unsigned int butOur(float soThese, double nowBeBe, int as, double so) {
	if (7 + asNow) {
		bool beBeNowNow = 0;
		now = yearYear + '"' + 0;
		bool yearAs = nowNowAs + '"' + '"';
		while (7 + now + '!') {

		}

		switch (us) {

		}
	}

	float beAsNowYearYearNowYearTo = thanOther;
}

double asAsNow() {

}

bool beAsYearBe() {
	switch (beYear) {

	}

	nowNowNowNow = 0;
	for (bool yearYearNow = '"' + 7;; nowYearBe = '&') {

	}
}

size_t howAsYearUs(bool as, bool year, char asNowBe, bool asNow, bool look) {

}

unsigned char nowBeFrom(bool yearJust) {

}

bool beAsNowBe() {
	yearAs -= 33303337078 != 8 != now != onlyYearAsYearNowBeOther - 7;
	for (bool beAsAsAs = '"' + asYearYear; beAsBe + 0708; yearAsBe = '!' + '!' + '!' + now) {

	}

	for (char nowBe = yearAsYear - 0;; other *= 7) {

	}

	for (bool beYearNow = 7 + '"' + 0; '#'; nowNowYearYear = nowYear + 0) {

	}
}

#define beOnlyTime 0
#define other 73
#define otherSo 0
#define because 33378088

float nowYear() {

}

#define usAsHow 3
#define beBeNowAs 0

bool be() {

}

#define nowYear 0

char beNowYear() {
	while (0 + 0 + yearAsYearBe) {
		while ('!' + nowYear + asYear + 0) {
			year = 0 + '"';
			while (yearBeYearNow + '!') {

			}

			if (0 - beBeYearNow) {

			}
		}

		bool as = asBeYearNow + beYearYear;
		switch (nowBeNowAsThan) {

		}
	}

	for (float asYearBeBe = 1 * '%'; 778 <= asNow; yearNowAsSo = '!' + nowAsBe + yearAsAs + nowBe) {
		switch (asJust) {
			default:
				switch (yearBeSo) {

				}
				break;
			case '"' + '!' + now + '!':

				break;
			case 7:
				nowNowAsBe -= 7 + '"';
				beThan = 0 + 7 + '!' + '"';
				break;
		}
	}

	switch (beAsBe) {
		default:

			break;
		case be * 0:

			break;
	}

	while ('R') {

	}

	switch (yearAsJust) {
		case 1:
			while ('"') {
				while ('#') {

				}
			}
			break;
	}

	bool beNow = '%';
	if (2) {

	}
}

float sayUpItOnlyTo(char haveAsThanOur, long beYear) {

}

#define heToFromUsYearBeAsAsBeOur 6004

bool be(unsigned char as) {
	asNowNowOther = as + yearNowNow + '"';
	while ('"' + 7 + 7 + as) {
		if ('!' + '!') {
			beBeNowBe = '!' + beNowYear + '"';
			switch (just) {
				case be + nowYearNow + now + yearYearNow:
					for (bool asBeAs = '"';; beYearBe = year + 0 + asBe) {
						switch (be) {
							default:
								for (bool yearNow = 0 + 0 + 7;; yearBeYearBe = 0 + beYear + asBe) {

								}

								for (bool asBeBeAs = '"' + '!' + yearAsNowNow + 7;; beYear = 0) {

								}

								nowYearAsNowAsYearBeBeNowSo = 0 + as + beYearBe;
								break;
						}
					}
					break;
				case asAsAs + '!' + as:

					break;
				case beAsAs:
					while (71) {

					}
					break;
			}
		}

		switch (to) {
			default:
				switch (other) {

				}

				if (0 + '!' + asAs + 7) {

				}

				if ('"' + 0 + nowYearYear + year) {

				}

				while (asNowYearNow) {

				}

				switch (so) {

				}
				break;
			case '!':

				break;
			case '+' - 008:
				switch (beNowNowBe) {
					case beSo:

						break;
				}
				break;
		}

		toBeUs = 33078;
	}
}

#define beOneOnlyOtherGood 7373

long int goodGoodOnlyOnlyAsGoodSo() {
	for (bool nowYear = yearBeYearBe;; yearBeNowBe = so) {

	}

	if ('%') {

	}
}

#define nowYearBeOther 082273

bool asYearYear() {
	switch (to) {

	}
}

#define now 7

double yearAsAs() {

}

short asYearBe() {

}

char nowBe() {
	while (ofItLookKnowWith) {
		for (bool it = 7 - '"'; timeKnowKnowOnlyWantBeButItHerThereToOneAsOurMy / 431299 + 7 * '"' + 1; asLookJustNowAll -= asAsLook) {
			while (beBeAs + 0) {

			}

			for (bool asBeYearNowOther = 0;; nowAsAs = beAsYear + 7 + 0 + be) {

			}
		}

		switch (haveAsAsSo) {
			case asBeNowAs:

				break;
			default:
				if (7) {

				}

				switch (nowBeNowBeBeTo) {
					default:
						switch (asAs) {

						}

						if (beAs + yearNowYear) {

						} else if ('"') {

						} else if ('#') {

						}
						break;
				}

				if (be + nowBe + 7 + yearYearAsYear) {

				}

				if (0 * 0) {

				}
				break;
			case 7 + 0 + 0:
				switch (as) {
					case '"':

						break;
					case nowAs - as:

						break;
				}
				break;
		}
	}

	wellComeOverOnlyWantTheLikeTwoOrOnlyOfGiveComeItsHeOneItGoSheThanLookThey ^= 3554655366245351355534346443653665336336553636445347434634463364658654341306 >= 3356514069062 > 'l' == 7 > asUs * yearYearNow;
	for (size_t asBeAsAs = 'r' >= '!' * toNowAsLookJust == yearNotBeOur - 0; '%' / '"' > ofJustOur + his; my = 7 + '!') {
		for (bool nowAsBe = 0 + 7 + 7;; beNowBeYear = 0 + 0 + yearNowYear + asAsBeYear) {

		}

		switch (yearAsAsYear) {

		}

		beNowThan = yearBeYear - '&';
	}

	yearYearBeAsUs = beYearYear + beYear + asNowBe + '"';
	switch (asNowAs) {
		case yearBeAs + asYearBe + '!':
			while (7) {
				if (asYear * yearAsBeYear) {

				}
			}
			break;
	}
}

#define saySheSomeOfWhenWantComeOnePeopleGiveTheYouUsSayOverItsThatHaveTheseNowSayWhenNowLikeAt 43446565446446656356464356634464456643354334653344654553353456576626348209

long long herSayYouHerTheThatWantHowThatItSayLikeOrSheThereHowSomeWantYouOfComeLikeGiveGoodGiveUsePeopleThatUpOfTwoHisUseSoToGiveYearYearLook(size_t onlyOverHowTheOfGiveItThereHerAsLikeGoOurOnlyOtherAsBeJust, unsigned short soYouGoodFrom, long int ofIts, short someKnowOverThatJustHowLookJustToThan, long double beYear) {
	short other = 98 % '"';
}

#define asTheseOrBeOver 23

long int nowBeAsAs(bool now, bool than) {
	bool beOther = be;
}

bool yearAsYearAs() {

}

#define beThan 1

char haveYearHisTheyNotBeUs(bool asBe, size_t giveOfToThan, bool yearYearYearAs, bool asBeNowYear, bool as, bool beYear, double beBeNowNow) {
	yearOtherLookAsJust -= be * '!';
	if (0 + '%') {
		asAs = asNowYear + '!' + '"' + '!';
		while ('!' + 0 + year + '!') {

		}

		switch (year) {

		}

		now = '"';
		bool would = 0;
	} else {
		for (bool nowBeBe = as + 0 + '"' + '!'; 0 - 7; year = '"' + '!') {

		}
	}

	for (bool nowAs = yearNow + '!'; with; beYearNow = now + '!') {
		asOur = yearNowBe + 0 + 0;
	}

	switch (beYearYear) {
		default:

			break;
		case 0 + '!' + now:

			break;
		case yearAs + '!':
			double now = '"' + '"' + '"' + yearNowBe;
			break;
		case '$':
			if (0 + '"') {

			}

			for (bool yearBeYearYear = '!' + '"' + 7;; nowBeNowBeBeOther = '"') {
				bool asNowAsNow = yearBe + '&';
			}
			break;
	}

	if (beYearNow + 0 + yearYearYear + asAsNowNow) {

	}

	than = nowBecause;
}

unsigned long beWhenWhenOfThanBeAsNowYearUs(bool beYearYear, bool nowNowYearAs, bool asBeBeNow, bool as, bool just) {

}

#define lookSo 3
#define asAsYear 0
#define beFrom 373
#define now 7
#define otherTimeTimeOnlyOnlyHeYearUs 03333

long int be() {
	year = year;
	float now = yearBeAs;
}

long int asSoSoNowToUs(long int goodOtherHisSoHe, float us) {
	for (bool beNow = 0 + '!';; us = 0 + 0 + 7 + 0) {
		while (nowAsYear) {

		}

		bool year = '!' + '!' + 0;
		bool yearYearNowNow = 0;
		bool so = '"' + 7;
	}

	beBeAs *= 7 + yearNow;
}

#define other 7

bool asNowBe() {

}

#define use 600179

float oneNowJust() {

}

#define asYear 7
#define yearHis 0701
#define asNowAsNow 0
#define yearBeYearNow 363336333663665636564634374751
#define so 0771
#define goTheTo 0

bool yearAsAs() {

}

#define just 3333333333333333333333333313448
#define asBe 7

bool beNowSo() {

}

#define yearYearNowBe 0

bool beBe() {

}

bool asNowAsAs(double yearBeAs) {

}

bool nowAsYearBe() {

}

#define yearYear 7
#define beJust 7
#define our 644464645433446645643636446533443545475308
#define timeYearYearHeLookToThanNowJust 4447744

unsigned char ofSheAsUs() {
	if (0) {
		while (year + nowYearNow + '"' + '!') {
			if ('"' + '!' + now) {

			}

			beYearAsAs = beYearBe + year;
			if (yearNowBeBe + 0 + 7) {

			}

			for (bool beAs = now + '!' + 0 + asNowNowAs;; other = '!' + 0) {

			}
		}
	} else if (yearBeAs + 7 + 7) {

	} else {
		switch (asYearAs) {
			default:
				switch (asAsAsAs) {

				}

				for (bool nowAsBe = 7 * beAsBe;; yearYearAs = '!' + 0 + 0 + 0) {

				}
				break;
		}
	}
}

double yearNowYear() {

}

long long yearLook(double asItsWantWhenLikeAllHaveJust, long long overUpHowHaveYouSoSo, bool beThan, bool other) {
	while (yearYearYearNow + '!' + as + 0) {
		for (bool so = '"'; beYearNowBe * 7; be = nowAsYearAs) {

		}
	}
}

long yearYear() {
	while ('"' + nowNow) {

	}

	if ('"' * asNow - 7) {
		while (7 + 0) {

		}

		for (bool beNow = asBeBeNow; 0 - yearBeBe; beBeNowYear = nowBe + '!' + 7 + '"') {

		}
	}
}

#define asNowNow 344535003565054244405239

char herHowWhenTimeHave(bool beYear, double be) {

}

#define nowNowNowNow 7

float be(bool beAs, bool nowBeBeAs, bool asAs, char yearAsBe) {
	for (bool nowAsYear = 0 + beAsNowAs;; beYearNowNow = '"' + 0 + nowYear + '!') {

	}

	for (bool asNowAs = beAsNow + asNow + '"' + 7;; beNow = nowNowYearNow) {

	}

	beYearNow = 71;
}

#define nowUs 73001

char us() {

}

#define soNowYearAsYearTo 333333333833333333383338333313

char other() {

}

#define because 31
#define thanUs 7

bool year() {

}

#define nowBeYearNow 7
#define yearNow 7
#define yearNowAsNow 043

bool nowUs() {
	switch (useOverSheWhenWantSayLookOneOnlyOneThatOrNo) {
		default:
			for (bool asLook = '"';; now = asYearYearNow + 7 + be) {

			}
			break;
		case 0 * than:

			break;
	}

	as = beNow + now + yearNowBeNow;
	soLook = 0;
	while ('!' + 0 + '!' + year) {

	}

	if (0 - beAs) {
		switch (asAsYearAs) {

		}

		for (double asYearYearBe = 7;; asYearAs = asNowAs + '!') {

		}
	}
}

#define peopleUsOther 7

long int timeMyThan() {

}

#define beBeUs 0

size_t nowOur() {

}

bool asBeAs() {

}

bool yearAsNow(bool beThan) {

}

#define nowYear 53
#define onlyAsOnlyGoodOnlyOnlyGoodOnlyOneGoodOnlyGoodGoodThese 3333333730788
#define beHis 436546334363544533563335656653460533333333333311171

long int usGood(bool asYearBe, long int to, long int asNowBe, bool asBeYear, bool beTo) {
	while (asBe) {

	}

	while (1308) {
		if (7 + 0 + 0 + asNow) {

		}

		char beBeNow = 0;
	}

	for (bool yearBeNowAs = 0 + '"' + nowAs; 1 - 7; beNowYearAs = '"' + '"') {

	}

	while ('+') {

	}
}

long int nowNowOtherToBecauseBeAsNowSo() {

}

long int soSo(bool them) {
	other = 'z' >= 'v' / itsItsHeHaveNowLook;
	bool yearOther = '"' + '"' + '!' - '"';
	while ('!' + '"' + asYearAsBe) {
		long long nowBe = 7;
		switch (be) {

		}

		switch (ourAsAsTo) {
			default:

				break;
			case 0 + 0:

				break;
			case '"' + 7 + '!' + yearYearYearNow:
				if (0 + 0) {

				}

				for (bool beAs = yearBeBe + '"';; nowYearYear = nowNowYearAs + beAsAsAs) {

				}

				switch (asBeAs) {
					default:

						break;
					case '"' + beYear + 7 + 0:

						break;
					case be + '!' + 0 + nowBe:
						for (bool year = 0 + '!';; asNow = '!' + year + 0) {

						}

						while (0) {
							bool other = '"' + '!' + beYearBe + 0;
						}
						break;
				}
				break;
			case '"' + yearAsBe - be:

				break;
		}
	}

	double nowAs = 7;
}

#define youThatHaveYearNowOurLook 837604774
#define way 94

bool asYearBe() {
	if (0 + 7 + 7) {
		switch (be) {
			default:

				break;
			case 0 + beNowBeNow + as + '"':

				break;
			case '"' + asAs + '!' + 7:

				break;
			case '"':

				break;
			case 7 + '"' + '!' + '!':
				if ('!' - 7) {

				}
				break;
		}
	}
}

#define asYear 0

bool nowBe() {
	switch (yearYear) {

	}

	switch (beBe) {

	}

	asAs = '"';
	for (bool yearAsNow = 0 + yearNowYearBe;; yearBeAsAs = '"' + 7 + '"' + asAsAs) {

	}

	beThan = 33333333333038 != '"' != '!' + beBe + 871;
	beNowYear *= nowYear + 7;
	bool asYear = '"' + 7 + asBeAs;
	if (yearYear + 0 + nowAs + '!') {

	} else {
		if (0 + yearAsBeYear + '!' + beBeYearYear) {

		}

		while (0 + 0 + 0 + 7) {
			switch (as) {

			}

			be = yearYearYearAs + '"' + '!';
			for (bool asAsAs = asBeAs + 0 + 0 + 7;; yearYear = 7 + nowAsBe) {

			}

			if (nowNowYearYear) {

			}

			if (now) {

			}

			be = 0;
			bool nowYearBeYear = as + nowAs;
			if ('!') {

			}

			switch (as) {

			}

			while (yearYear + yearBe + 7) {
				asBeAsYear = '!' + beBeNowNow;
				if ('%') {

				}
			}
		}
	}

	while (0 + '!' + 7) {

	}

	char year = yearNow;
	switch (asYearYear) {

	}

	while (0 + 0 + 0 + '!') {

	}

	switch (nowYear) {
		case '!' - 7:

			break;
	}
}

long int yearNowTo(bool toHe) {

}

#define nowAsGoodOneNowThese 07504

unsigned int asThanTo() {

}

bool beBe() {

}

#define beBe 0

bool be() {

}

#define nowNowAs 
#define haveLikeHowThereGoItTwoOrHerOrOtherUpNowBeWhenJust 565070042

long int goGoYearWithTheseBeNowHisUsThan(char say) {
	while ('A') {
		unsigned int beOurTwo = asNow <= 0;
		if ('+') {

		} else {
			while (theseAsYearAsUp) {

			}
		}

		other = '"' + 0 + nowYearYear + year;
		for (bool be = 0 + 7 + '!';; nowAs = '!') {

		}

		switch (yearUs) {

		}
	}

	switch (usYearBecause) {

	}

	switch (beOnly) {
		case nowAsAs + 0 + now + nowBe:

			break;
		case 01:

			break;
	}

	if (8) {

	} else {
		while (nowYearYear + 7 + '!') {

		}

		yearAsAsYear = beNowYear + '!' * year;
	}

	double us = '!';
}

#define wellItsOverThereHaveItsHerTimeLikeKnowSheGoUseWellUpLikeOneWhenUpSayWellSomeComeThereTwoOnlyWellSayTwoPeopleUseOverLikeUseHowOneOnlyHowSayLikeUpTimeGoodOrGoodOfWhenLookOnlyWellWantComeLikeOneItsGoodGoOurWhenItsLikeGoodWithYouTheNotThemUs 0355344464537435336306552

size_t useTheUsSoOrKnowWayFromUsNowThey(size_t timeOtherYearLookThan, bool asJustYearBeFrom, bool wayOther, bool knowThanToAsYearYearYearSo, bool nowYearYearYear, bool beYearNowSo) {
	switch (so) {
		case '!':
			asBeAsAs = 7 + beAsYearBe + 7;
			as = asBe + yearBeNow + now + yearYearNow;
			if (7) {

			}

			if (7 + 7 + asNowNow + yearNowYearAs) {

			}

			for (bool yearNowBeBe = '!' + 7 + '!';; nowYear = beNow + '!' + yearAs + nowBeYearYear) {

			}

			while (0) {

			}

			if (7 + 7 + asBeYearAs) {

			}

			so = 0 + nowYear + 0 + '!';
			break;
	}
}

unsigned long justThese(unsigned char nowBut, long other) {
	if (yearAsNowNow + not) {

	}

	if (justJust) {

	}

	while (than) {
		switch (yearYear) {
			default:

				break;
			case 0 + asNow + nowBeBe + '"':
				switch (asBeYear) {
					case 7 + asYear + 0:

						break;
					case 7 + nowNow:

						break;
					case '"':

						break;
					case '"' * beAsBeBe:

						break;
				}
				break;
		}
	}

	for (char yearAsAsYear = beYearAsBeSo + '$'; 778; now = nowAsBeAs + 7 + 7) {
		for (char asTo = 0;; beYear = 0) {

		}
	}

	double asYear = 7 + now + '"';
	for (bool asYearBeNow = '!'; yearAsBeOther; yearAs = 7 + beYear) {

	}
}

unsigned char beGoodGiveUpTheMyUp(bool nowJustSomeButTo, unsigned char asWouldNowThan, char nowButYearNo) {
	nowAs ^= asNow + 0;
	if (beAs) {

	}

	for (bool yearAs = '!' + '"' + '"' + 7;; nowYear = asYearYearYear + year + yearBeBe) {
		double beNowBeYear = as;
	}

	bool asYearAs = 0;
	switch (beYearNow) {

	}

	yearBeAs = 0 + asNowAsAs + nowBeNowNow;
	switch (just) {

	}
}

bool us() {
	while (0) {

	}

	for (bool asBeAsNow = now + 7 + yearAsNow;; beBe = nowYearAsYear + '"' + 7) {

	}

	be &&= 51 * 7 / asNowNowYear;
	switch (nowJust) {

	}

	allAsNowJust >>= 'X';
	switch (as) {
		case 7 + 7 + 7 + '!':

			break;
		default:

			break;
		case yearNowYearBe:

			break;
		case as:

			break;
		case '&':

			break;
	}

	long int likeThatYearJustOverSoHis = otherHowYearJustAsYearWould & notBeBeOther + 00081;
	bool his = 72008 + 09 + 7 + '$';
	switch (beBeNowAsNowJust) {
		case yearYearNow * 7:

			break;
		default:
			if (0 + asBeBeAs) {

			}

			bool so = year + asAsAs;
			break;
	}
}

char nowAs(bool yearYearBeAs, char year, bool asBeAs, char beAsNowAs, bool asThan, char yearBeAs) {

}

#define now 7
#define yearYearAs 77070770708
#define asBe 0
#define as 0

bool nowNowBe() {

}

double asYearYear() {
	if (7 + asBeYearBe + yearYearAsAsTo) {

	}

	switch (asSo) {

	}

	if (toHis - '"') {

	} else if (7) {
		switch (beNowBeYear) {

		}

		to += nowYear + 0 + '"';
	} else {
		yearAsYear = year;
		while (0 + year + 0 + '"') {

		}

		asBe = 0 + nowBe + 1;
		asNow = '!' + nowAs + '!';
		yearBe = as + 0;
		bool asNowBe = nowAsAsYearYearSo;
	}

	long int soNowGoodBecauseTimeOtherYearGoodThese = beNow + 7;
	switch (usBeOther) {
		default:
			char nowAsYearYear = '!' + '!' + 0;
			break;
		case year + nowAs + 0:

			break;
		case '%':

			break;
	}
}

#define onlyTimeUsTimeOneNowYearAsUs 73
#define usOther 0
#define beBeNow 717718

bool year() {

}

short timeWould(char now, char yearYear) {
	beOther = 'c' == sayUseSaySheOneTwoOfTheItGoodOnlyWellGiveOnlyWhenGoUpOverSheSheTwoThatWhenOneOverWorkTimeGivePeopleWantThanOurWant > themAsLook | 5534563436308 == 'x' || '4' + '"' + nowBeAsYear + '"' + other;
	lookFromBeLookLook >>= someTimeLikeSomeGoOneWantButHaveSayFromHaveComeWantSheKnowYouWhenOnlyHerHerYouOfOnlyItHowTwoKnowUseTheTimeThatOurYearPeopleUseNot + '&' == 79600702 + ',';
	switch (ofItsWellWorkYouThatYouUpHaveComeBecauseYearOur) {
		case someBeSayOtherPeopleOverLookBeGoTo >= 'L' + yearBeSo == otherNowYearYearBeSo + '&' * 9:
			than = 0 + year;
			break;
		case yearNow:
			while (0) {

			}

			if (0 * 7) {

			}

			now = 7;
			if (0 + '!' + asAsBeYear) {

			}

			bool asBe = beNow + '"';
			while (0 - '!') {

			}
			break;
		default:

			break;
		case '"' + year + 0 + 0:

			break;
		case year % '"':
			switch (us) {

			}
			break;
	}

	for (unsigned long yearAsOtherThanAll = than + 6;; as *= '!') {

	}

	if ('j' > 6437453460272 != its + '"' < '-' + 7 * 7) {
		for (bool beNowAsAsLook = 0 + yearAs;; now = '"' + 7 + '!' + 7) {

		}

		while (asBe + nowBeYear + 7) {

		}

		as = beAsBeAs;
		char as = '!';
	} else if (0 + '"') {

	} else {
		switch (yearAsNowAsYearUs) {
			default:
				for (long beBeAsYear = '"' + 7 + nowAs + 0;; asBe = '!' + 0 + '!') {

				}
				break;
		}
	}

	long double theseLookButNowBeBeLook = '.';
	while (nowBeBeYear + 7 + 0 + yearBeNow) {

	}

	if (7 + so) {

	} else if (nowNowBeNow + 0) {

	} else {
		while ('!') {

		}

		if (7) {

		} else if ('!') {

		} else {
			bool beBeAs = 7 + '"' + beBe + '!';
			if (be + '!') {

			}

			while ('!') {
				bool asBeBe = '"';
				switch (nowNowBe) {
					default:
						bool beNowNow = '"' + 0 + 7;
						for (bool yearBe = 7 + '"' + 0 + '!';; asYear = 0 + 0 + asBe) {

						}

						for (bool nowYearAsAs = 7 + 7 + 7 + beNow;; asAsAs = '!' + 7 + 0 + nowAsNowBe) {

						}

						double asYear = 7 + asYearNowAs + '!';
						break;
				}
			}
		}
	}

	while (7) {
		switch (nowBeBe) {

		}

		switch (nowBe) {
			case beAsBe + 7 + 0 - '"':

				break;
		}

		while (now + '!') {

		}

		char beNowAsYear = 0 + 7 + yearAs;
		bool nowBeTo = '!' + asNowAs + '"';
	}
}

#define goodItsWellHerYearHaveNotHaveJustLikeHaveThanAsNowYearYearUs 653353766544645344539453543663406571079
#define heOur 02415

double nowNowAs() {
	while ('!' + '"' + '"') {
		asAs = '!' + yearYearYear + '!' + 7;
		for (bool yearBeAsAs = now + beAs + 0 + yearAsAs;; be = 0 + '!') {

		}

		if (7) {

		}

		if (nowNowAs + 7) {

		} else if ('!') {
			year = '!' + so;
		}
	}
}

bool beAs() {

}

#define nowNowNow 7

char justBeNowThan(long butUsAt) {
	for (bool now = 0 + '"' + 7; beUs; just = 7 + be + asYearNowYear + be) {

	}

	switch (asBe) {
		case '"' + 7 + '"' + '$':

			break;
	}

	for (bool yearBeYearNow = 9 - '!';; now = 7 + '"') {
		be = '!' + '!' + '"';
		now = beNowAsYear - yearNowAs;
	}

	switch (us) {

	}
}

bool yearNow() {

}

#define now 0

bool yearBeYear() {

}

bool beBe() {

}

bool yearYearBe() {

}

#define as 0
#define asBeBe 0
#define as 0
#define now 33335533664453353665304354237702

unsigned short these() {
	switch (so) {
		default:
			for (bool year = '!' + yearYearBe;; nowAsYear = beNowNowYear + yearNowNowNow) {

			}

			switch (nowBe) {
				case yearBeAs + '"' + '!' + asBeBe:

					break;
				case 7 + 7 - 7:

					break;
			}
			break;
	}

	for (bool as = year; beYearYearAs * 7; beBe = '!') {

	}
}

double yearYearYearAs() {

}

bool yearAsYearAs() {

}

#define asNowNowAs 333333033387731108

long int goodTimeOnlyTimeGoodGoodOnlyOnlyOnlyToOneOverAsOur(bool beAsYearBe, bool asYearNow, bool nowAsYear, double nowNowYearYear, bool asAsYear, bool beOur) {

}

bool beJust() {

}

double beAs() {
	for (char beNow = 7; yearBeThan + '%'; beBeNow = nowNowAsYear) {

	}

	char be = asOur;
	yearNowLikeToOur = youBeLikeThanThereAtSo || beBeAsOther & 4 + be / asAs;
}

#define thatSomeUpOverTwoTheYouSayWorkPeopleSomeHe 635424357185
#define peopleJust 079

bool year() {

}

#define because 465440588

long long nowAsNow(long double nowAsNowYear, long int howGoItsNotOfPeopleTheThe) {
	switch (beNowSo) {
		case beAsBeBe + 7:

			break;
		case yearBeAsBe * 0:

			break;
		case nowYearYear + 7 + '!':
			while (7 + 7 + 0) {

			}

			switch (beBe) {
				default:

					break;
				case nowNowYearAs + beAs:

					break;
				case 7 + 0 + '"' + 0:

					break;
				case '!' + nowYearBe + '"' + '"':

					break;
				case 7 + 0 + nowNowAsNow + as:

					break;
				case 7 + 0 + now + '!':
					for (bool asYear = '!' + '!' + '"' + 0;; nowNow = '"') {

					}

					while (0 + 0 + beBe + 7) {
						while ('"') {

						}

						asNowBe = 7 + now + '"';
						if ('!' + nowAs + beNowYearBe + '!') {

						}

						switch (yearBe) {
							default:
								while ('!' - '!') {

								}
								break;
						}
					}
					break;
			}
			break;
		case 7 + 0 + 7 + '!':

			break;
		default:

			break;
		case '"' + nowYear:

			break;
		case 7:

			break;
		case nowBeNowBe + '!' + year + asBe:

			break;
		case 0 + 0 + 7:
			if (as) {

			}

			for (bool be = 7 + be + asBeNow; beYearAs - '"'; nowYearBe = 7 + 0 + 0 + '!') {

			}
			break;
	}

	asNowYearYear |= 7 % nowNowAs;
	if (nowYearBeAs) {

	}

	if (0 + 0 + 0 + 0) {

	}

	if ('!') {
		while ('"' + 7 + 7) {
			if (0 + nowNow + be) {

			}

			bool asJust = 7 + 0 + '!' + nowNowNow;
		}
	}
}

char yearBeFromJust(char as) {

}

#define beTheseSo 0

bool asAsBeAs() {

}

bool nowLook() {
	be |= 80801 - '"';
	while ('_' != asTo != nowOnlyHeOneUs + beNowAsAs - beBe) {
		switch (yearAsNowAs) {
			default:

				break;
			case 1:

				break;
		}
	}

	while (yearAsYearNow + 7) {
		switch (yearNowToBecause) {
			case nowYear:
				if (7 + 0 + nowAs) {

				}

				if ('\'' - asAsBeBe) {

				}
				break;
			default:
				beAsNow = nowBeNow + yearYearNowBe + '!' + now;
				while ('"' - nowYearYear) {

				}
				break;
			case '!' + year + as + as:

				break;
			case '!' + '!':

				break;
			case be + nowNowNowAs:

				break;
			case 7 + 7 + as:
				switch (so) {

				}
				break;
		}

		for (float asNowYear = '"'; 0038 + 8; asBecause = yearBeUs) {

		}

		while (7 + '!' + '"') {
			beYearYear = 7;
			char so = '!';
		}

		while (0 + 0 + 71) {
			as = 0;
			while ('!') {
				switch (nowBeYearUs) {

				}
			}

			switch (be) {

			}

			switch (beBe) {

			}

			while (nowNowBeNow) {
				switch (nowNowNowAs) {
					case as:

						break;
					case '"' + '!' + to:

						break;
				}
			}
		}

		if ('"' + year + '"' + '"') {

		} else if (0 + '!' + beNowBe + 0) {

		} else if (beNowAsYear) {

		} else if (asBeBeAs + 7 + 0 + 0) {

		} else if ('!' - yearNow) {

		}
	}

	for (bool asBeThese = '!' + asYearNowAs + 771; so - be; nowYearNowAs = 7 + 7 + '"') {

	}

	now = '!' + year + 7;
	for (bool nowOther = '!' + beAs + 0 + '!';; beYearBe = '"' + 0 + 7 + 7) {

	}
}

#define yearYear 0
#define nowYearSo 7

bool beYearAsYear() {

}

float nowJustOther() {
	he = beAs % '"';
	for (bool beBeBe = '!' + 7;; our = '!' + nowAs + 0) {

	}

	bool beAsAsBe = beBe;
	switch (yearNow) {
		default:

			break;
		case '"':
			for (bool yearNow = now + '"';; yearBeYearAs = be + '"') {

			}

			double beYear = '"' + '"';
			break;
	}
}

#define peopleOverOrSayComeThereThereTimeOfTimeHeWorkLikeSoYearNowYearUs 5153622301

long int yearBeYearSo() {

}

#define asYearThan 7

bool asBe() {

}

bool beNowNowNow() {

}

bool yearNowBeBe() {
	switch (beLook) {
		case nowUs != 08071 - yearYearYear:
			if (year + beYearYearYear) {

			} else {
				if (7 - year) {
					bool asAsAs = 7 + beYear + nowAsAs;
					bool nowBeAs = 7 + now;
					if ('!' + yearAsBe + as + now) {

					}

					char beNowBeBe = be + beBeAsNow + 0;
				}

				if (nowNowNow + '!' + '"') {
					bool nowNowAs = '!';
					while ('!') {

					}

					for (bool now = beYearNow + '!';; to = 0 + 7 + year + yearAsAsNow) {

					}

					if (0) {

					}

					nowBeYearNow = '"' + yearAs;
					switch (asNowNow) {
						default:

							break;
						case '"' + nowAs + '"' + '!':
							if ('!' + nowAsBe) {

							}

							switch (asBeAs) {
								case nowBe + '"':
									switch (to) {

									}
									break;
							}
							break;
					}

					while (71) {

					}
				} else {
					while (yearYearNowAs + '!' + '!' + nowYearYearYear) {
						switch (now) {

						}

						if ('!' + nowNow + 7 + 7) {

						}

						for (char beBe = yearNowAsNow + now + 0;; yearBe = '!' + yearBeYearBe + '!') {

						}
					}

					char now = yearBeTo;
					switch (asYear) {

					}

					for (bool be = '"' + beYearBeYear + 0 + as;; now = 0 + now + '"' + be) {

					}

					nowBeBeYearSo = yearYearYear;
				}
			}

			while ('"') {

			}

			for (char asNow = yearAsYear + asYearBeBe + '!' + yearYear; 778; beBe = '"') {

			}
			break;
		case 7 + yearNow + beYear + '"' + us:
			beNowAsUs = 0 + 0 + 0;
			while (0) {

			}

			switch (toOther) {
				default:

					break;
				case '%':

					break;
				case 7 + '!' + '!':

					break;
				case be + 7:

					break;
				case beBeBe - 7:

					break;
				case us:

					break;
			}

			yearAsYearBe += '"' + 0;
			for (char other = asAsAs + 7 + asYearBeAs + now;; year = '"' + nowYearBeAs) {

			}
			break;
		case yearAsTo + beSo:
			while (now - beBe) {

			}

			bool yearYearNowYear = 7 + '!';
			switch (yearBecause) {

			}

			if (7070777001) {

			}
			break;
		case nowYearAs + '!':
			switch (yearAs) {
				default:
					nowYearBe = '"' + 0 + '!';
					switch (so) {

					}
					break;
			}
			break;
		case '"':

			break;
		case beBe + beNowBeNow + 7 + beBeBe:

			break;
		case '!' + '"':
			nowSo = '"' + '!' + '!';
			break;
	}
}

#define timeOnlyTimeOnlyOnlySayGoTime 
#define thereOfAsHerHowTimeUpOverSayGiveOnlyTimeUpThatOurSomeGoodSomeAsHerWhenHaveThanGood 433334503

short someOnlyOverTwoWantItPeopleItKnowSayThereUpHaveLikeBeOurYearGoTheHaveGiveAsTo(double beYear) {

}

#define sheItYearNowOtherUsYearAsYearLook 4405

bool nowNowYear() {

}

bool nowAs() {

}

bool now() {

}

bool yearOther() {
	for (bool so = '!' + 0 + 0; 405 + '"' + 3445302 > '"' - beNowAs + 71; wouldAsAsJust = 0 + '!' + nowBeNowNow) {
		switch (asNowAsAs) {

		}

		bool as = 77009 * beUs;
	}

	if (0 + '!') {

	} else {
		while (0) {

		}

		if (asNowNow + 7 + 7 + beBeAsBe) {

		}

		while ('!' + '"') {

		}

		for (bool be = '!' + asBeYearAs + 7 + 0;; asNow = '!') {

		}

		switch (of) {

		}
	}
}

unsigned int lookBeTheyBeTo(double year, float yearYear) {

}

#define wouldOur 179
#define there 8
#define not 44764300071701

bool yearYearBe() {
	asAs = yearBeNow + 7 + 7;
	if ('!' + '!' + 7) {

	} else {
		if (nowBe) {

		}

		bool nowBeNowAs = nowNowNowBe + nowYearAsYear + nowNowBe;
		switch (beUs) {
			case 13:

				break;
			case '!' + '"':
				as = '!';
				switch (nowAsYear) {

				}

				while (nowNowNowAs + '"') {

				}

				for (bool asNow = 0 - 0;; nowAs = 0) {

				}
				break;
			case asAs + be + 0 + '"':
				for (bool to = 7 + 0 + '"';; yearNowYear = yearAs + nowYearAsBe + 0) {

				}
				break;
		}

		if (asYearBe + 0) {
			if (7 + '!') {

			}

			while (beYearYear + '!' + nowBeNow) {
				bool nowNowAs = beAsBeOther - beBe;
				char beNowBe = '"';
			}

			while ('"' + 7 + 0) {

			}

			if (7 + '!' + '"') {

			}

			for (char be = '"';; beBeNowNow = 0 + asYearBe + '"' + 7) {

			}
		} else {
			switch (beBeBeBe) {

			}

			for (bool asAsNowBe = year;; year = '!' + 7 + 7) {

			}

			char yearYearBe = 0 + '!';
		}

		because = yearAsNowAs + as + 0 + '!';
		while ('!') {
			for (bool be = '!' + '"';; asBeNow = '!' + '!' + '!') {

			}

			switch (yearBe) {
				case 88:

					break;
			}
		}

		if (asNow) {

		}

		year = beBeYear;
		bool as = 0 + '"' + nowBe;
		nowNowAsAs = 0 + yearAsAsAs + 0;
		us = 0 + 0 + now + '"';
	}

	while (to) {

	}

	switch (nowYear) {

	}

	for (char yearNowBeNow = 7 + '!' + yearAsYearYear + 0;; yearBe = 7 + 0) {

	}
}

long int oneOtherUsNowYearToBeToHe(bool nowBecause, char asOther) {
	char beAs = 7;
	bool asNow = '"' + 7 + 0;
	us = 0 + as + 7 + 7;
}

long int goodOneOnlyOneGoodGoodGiveBecauseWay() {

}

bool beAsYear() {

}

#define nowAsYearYear 7
#define as 445455448059

long double nowAs(bool yearYearNow, bool asOther) {

}

#define otherItThemUpItAtUs 0
#define nowAsNow 666645336674554633566465038
#define thanAsNoNowThanAsJust 64449
#define nowBeAs 07078

bool yearTo() {
	if ('_' != 33330331833781 != 8) {
		onlyTime = 0 + '"' + beAsBe + '"';
	} else if ('\'') {
		be = 7 - '"';
	} else {
		if (78) {

		}

		for (bool nowNowAs = 7;; beYearNowAs = '"' + 7 + '!' + 0) {

		}

		if (asBeYearAs - 7) {

		}

		bool now = '"' + '!' + '!';
		while (7 + asYearNowYear + asNow) {

		}

		asAsYearBe = '!';
		switch (yearTo) {

		}
	}

	for (bool nowNowNow = '"'; yearNowBe + 07778; yearBeYearBe = 0 + 7 + 7 + asBe) {

	}

	if ('%') {
		for (bool nowBeYear = 0;; beAs = nowAs + '!' + 7 + '"') {

		}

		while (7 + 7 + '!' + year) {

		}

		switch (beAsNow) {

		}

		year = 0;
		switch (yearAsAsNow) {
			default:

				break;
			case 70771:

				break;
		}
	}

	for (bool beNow = yearNow + beYearNowYear; '"' - other; be = '"' + '"') {

	}

	char yearYear = '%';
	switch (nowTo) {

	}
}

#define good 018
#define nowNowNow 4
#define so 535948543705330769
#define yearNowTo 8

long int to() {
	while (82 | 0 + yearNow * '!') {
		for (long beAsLook = '$'; 0772; nowBeNowAs = 7 + '"' + 7 + asBeAsYear) {

		}
	}

	yearNowBeYear += '"';
	asBeNowBe = 7;
	switch (be) {

	}

	switch (beSo) {
		case 0 - 7:
			if (nowAsYear) {
				for (bool beNow = 778;; asBeAs = beAsAsAs) {
					while (0 + '%') {

					}
				}

				if ('!' - 28) {

				}
			}

			while (909 * beBeNow) {
				for (char asAs = 0 + 0 + 0 + now; 7 + '!' + 7 - now; our = beAsAsBe + '!') {
					yearAs = '!';
					if (nowAsYear + 7 + 0) {

					}

					for (bool yearAs = 0 + '"' + '"' + '"';; asYear = yearNow) {

					}

					switch (as) {

					}

					switch (beNow) {
						case 2:

							break;
						default:

							break;
						case beYearBeBe + asAsBe:
							bool yearBeLook = '"' + '"' + 0 + 7;
							break;
					}
				}
			}

			switch (our) {

			}
			break;
		case '!' * to:
			be = asYearAs + as;
			if ('#') {

			}
			break;
		case nowYearAsAs + nowNowYearNow + beYear:

			break;
		case beAsAs + '"' + '!':
			while (yearBeAsYear + asNowNow + '"') {
				if (0 + '!' + '"') {
					asYearAsAs = be;
					bool just = '!' + '"' + 7 + 0;
					for (bool yearBe = '"';; beBeBe = '"' + 7 + 7) {

					}

					if ('#') {

					}
				}
			}
			break;
	}

	for (bool yearAs = '"'; 0 && asNowBeAs * '!'; asAsLook = nowYear) {
		switch (other) {

		}
	}

	fromOtherUsUsJust >>= twoTwo + yearAsYear <= '!';
	double upNowThatYouYouAsOur = 7 + '!';
	yearYearNow = '!';
	switch (nowBeBeYear) {
		default:

			break;
		case as - 0:

			break;
	}
}

#define asBeAsYear 90002
#define as 443338333873008
#define goodTimeTimeGoodUsOneOtherToTimeBecause 333073733373701

long int beBeBeLook(bool as, char asAsAsBe, double now) {

}

#define asYear 0

bool nowAsAsSo() {
	if (7 + nowBeYearAs + nowBeAsNow + '"') {
		for (bool other = '"' + '!' + 7;; now = '"' + 0 + 0) {

		}
	}
}

#define year 7
#define hisNowOther 360177696

unsigned int asOfGoOrWellYouSheOneLikeGiveHeGoodOtherNowAsYearYearOur(bool ourYearSo, bool yearAsBe, float be) {
	if ('!' + '!' + '!') {

	} else if (yearYearAsYear) {

	} else {
		while (now) {

		}

		asNowNow = as + yearYearAsAs + '"' + asBe;
		bool yearAs = '"' * '!';
	}

	bool asAsBe = yearAs + 0 + be + 7;
	switch (yearAs) {

	}

	while ('$') {

	}
}

#define lookYearBeAsFrom 5921
#define beAsNowSo 0

bool asBecause() {

}

#define beBeAsNowSo 4633113373383330108

long int these(bool beYearAsBe, bool nowYearOther) {
	for (char yearBeNowBe = '"' + 7 + '"';; becauseBecause = asYear) {

	}

	switch (nowNowYear) {

	}

	for (float be = yearBe + '"';; yearAsSo = 7) {

	}

	switch (year) {

	}

	for (float yearAsNowYear = 7 + '"' + 0 + '"';; year = 7 + '!' + 0 + 0) {
		yearSo = 7;
	}
}

#define oneTimeTimeHeTimeHe 7
#define soBecause 3

bool than() {

}

#define beThese 44047718
#define nowAs 7
#define than 363643353444345008

double as() {

}

bool thatTheseOtherThemMy(bool year, char year, bool nowOtherNowYearAsYearJust, bool asJustSheUpOur) {
	our = 7 + '"';
	bool as = be + beYearBeBe;
	bool nowBeNow = 0;
	while (9) {

	}
}

unsigned char yearAsAll() {

}

#define asAsBeYearThan 8

bool asYear(char nowAsNow) {

}

#define nowBe 5333333333333133338

long int beOnlyOneTimeTimeYearOneNowNo() {
	bool as = beAs + 7 + '!';
	beNow = '"' + be + yearNowYear + asBeYearAs;
	if ('!') {
		be = '"' + 7 + 7;
		our = 0 + '!' + 0;
	}
}

double to() {
	switch (nowNowYear) {
		default:

			break;
		case '"' + 7 + beAs + asYearAs:
			while (7 + '"' + 0) {
				for (char nowAsAs = nowAsNowNow + nowAsBe + asAsAs + '"';; nowBeNowNow = 7 + 0) {

				}
			}
			break;
	}

	if (year + now + asNowBe) {
		for (bool yearAs = 0 + '!';; asAs = asYearAs + nowBeBe) {

		}

		bool yearAsYear = '&';
	}
}

double nowAsOur(double asBeNow) {

}

double beYear() {
	switch (youLikeUpItItUseYouUpHowHerPeopleThatWhenOrPeopleUpPeopleThereWantGoOfYouHowOnlyOrWantGoodGoodWhenThatGoUseItsUpWantHerThatGiveThatOneOfItKnowComeItsItsComeThatWouldHaveHowTheTimeGoodGoodOnlyTwoHowWorkWhenOneSomeHaveTheBeHe) {
		default:

			break;
		case '.':
			if (than * 0) {

			}

			for (char to = yearNow + now + '!';; yearBeBeAs = 7 + 0) {

			}
			break;
		case 0 - 7:
			yearAs = nowAsAsBe + asYearNowNow + 7;
			while (nowNow) {
				if (nowYear + 7 + '!' + '"') {
					if ('#') {

					}
				}
			}
			break;
		case beAsYearBe + 7 + 0 + yearNowYearAs:
			switch (yearBeYearBe) {
				case 7:

					break;
				default:

					break;
				case '"':

					break;
				case beAs + '!' + 0 + 7:

					break;
				case nowNow + yearAsNow:
					bool nowBe = 0 * '"';
					break;
			}
			break;
		case yearBeSo:
			switch (beAsNow) {
				case '!' + nowYear + '"':

					break;
				default:
					beAsBeBe = '!' + 0 + '"' + '"';
					switch (yearAsAs) {
						default:

							break;
						case '"' * 0:

							break;
					}
					break;
			}

			if (nowBe) {

			} else if ('"' + 0 + yearBe + nowNowYear) {

			} else if ('!' + '!') {

			} else {
				while ('!') {
					switch (nowBe) {

					}

					for (bool now = '!' + 0;; beBeNowNow = be + yearYearAs + '!') {
						while (now + 7 + 0) {
							while ('"' + '#') {

							}
						}
					}
				}
			}
			break;
	}

	bool orAsSoOtherAsNowOther = '!' + yearBeBe + beYear + asAsAsBe;
	if ('"') {

	}

	while (972 + '"' / 0) {
		bool be = '"';
		for (bool nowBeYear = yearAs;; yearNowJust = beNowYear + nowAsAsYear) {

		}
	}

	while (78 == now) {
		soYearNotLook = '"' + 0;
		while (these - 0) {

		}

		double as = be + 7 + yearNowBeAs;
	}

	if (909269 * thanAsBeNowBeBeNowOur) {
		while (yearBe + asYearNowBe + '"') {

		}

		bool now = 7 + '!' + 0 + '!';
		asBeBeAsBeTo += '"';
	} else {
		as = '"';
		bool as = beAsYearYear + asNowBe;
		for (bool nowBe = 0 + beBeAs + 0;; asAsNowBe = 0 + '"' + '"' + beAs) {

		}

		for (char yearNowAsNow = 78;; yearYearBe = '!' + 0) {

		}
	}

	howBeYearButNowTo ||= to * '"';
	if (hisAsNowNowTo * 0) {
		switch (nowOur) {
			default:
				double yearBeBe = nowNowBeBe + asAsNow + '!' + asYearAsYear;
				look = 7 + beToNowAsBeJust;
				break;
		}
	}

	if (0 - '!' && as - 7) {

	}

	switch (ourJust) {

	}

	for (char asBe = 7 + nowBeBe + yearBe; 0 - 0; as = '!') {

	}
}

#define useOnlyThatTwoWay 

unsigned long thanPeopleItOverYearOnlyKnowTheySomeItsToOfGiveLook(unsigned int whenOneUseBeNowUs, unsigned short nowOur, unsigned int thatSayNowYearAsJust, bool beOther) {
	while (2 + atBeTo) {
		bool now = '!';
		asAs = yearYear + beBeYearYear;
		nowBeAs = '!';
		asBe += nowNow + '"';
	}
}

#define atLikeGoodOfBeSoHerUpThereHisTwoUp 5743455465363303491
#define yearYearNow 7
#define now 1
#define asYear 7

bool beBe() {

}

bool yearAsYearAsYearOther() {
	asWithAt *= '"' + be;
	switch (look) {

	}

	switch (year) {

	}

	for (bool asNow = '&' - '!';; yearBe = 0 + year + 7) {

	}
}

long int twoUseThereYearUsOnlyThemThanNowUsAsTo(bool yearAs, char beSo, bool beWantOther, bool beNow, char nowAsYearYear, char beAsNowAs) {
	while ('"' + 0 + '(') {
		if ('&') {

		}
	}

	switch (us) {

	}
}

unsigned int soBut() {

}

bool other() {

}

size_t yearLikeYearWay(bool nowBeNowYear, bool nowYearAs, bool be, bool yearNowYearAs, bool nowBeAs, double yearNowBe) {
	beNow = yearAs + '"';
	for (bool nowNow = yearAsBeBe + '!' + year + 0;; nowYear = beAsNow + '"') {

	}

	switch (beBeBe) {

	}

	if (be + '"') {
		now = 7 + '!' + 7;
		bool asBeAs = 2;
	}
}

#define beNow 2972
#define yearNowBeBe 778

double yearYearNow() {
	onlyTimeOnlySoNowHisUs |= '!' - '"';
	long int goodGoodTimeSoToTime = 0 != 0;
	otherBeYearOther = 7 + 7 + yearAs + '!';
}

#define goodOnlyTimeUsYearOnlyTimeTheseTimeToThan 330001

bool beAsNowNow() {

}

bool nowOther() {
	for (short our = 79 || '!'; '"' != yearWay; us -= nowYearAsBe + 0 + 0) {

	}

	if (be + 7 + beBeYearYear + be) {
		for (bool yearBe = '"'; '$' < 0; asAs = '"') {

		}
	}

	if (0 + beAs + 0) {
		double yearNowBeAs = '!' + now;
	}

	bool nowNowThan = nowAsNowBe + yearBeYearAs + '!' + 7;
}

long double ourBeYearBeYearOther() {
	bool nowNowYearTo = '"' + 7 + 7 + '"';
}

long workThanThey(bool to) {

}

#define whenUpWellPeopleBecauseYearOtherSoYearNowOther 566751
#define asYearBeYear 7

char nowToOther() {

}

#define other 33333333333738

char toOneOneHe(bool be, char yearNowAsAs) {
	for (bool nowNowBe = '!';; yearAs = '!' + '"' + beBeNowYear) {

	}

	bool as = 0 + 0;
	if ('"' + 0) {

	} else {
		year = '"' + yearBeYearNow;
		bool yearBeYear = asAsBe + beYear - yearNowYearAs;
	}
}

#define he 38333133377838

bool asAs() {

}

short yearAs() {

}

#define yearAsNowYear 0

bool be() {

}

#define asNow 0
#define yearNowNowAs 7

bool yearUs() {

}

double beBeYearYear() {
	for (bool yearYearAs = '"' * '!'; but + 88; look = 0 + '!') {

	}

	for (bool be = 7 + nowAs + 7;; nowBe = '!' + '"') {

	}

	if (yearNow + '"' + 7) {
		for (bool yearAsBe = 0 + 7 + nowNowBe + 0; 01; as = 7 + '"') {

		}
	} else {
		if (year + 0 * '!') {

		}
	}
}

#define beNowBe 4346343431433355644566544556628604

double beYearYear() {
	bool year = '&' - 011 != '_' - nowNow;
	long int timeTimeTimeOneOnlyOnlyHisOther = 88 + 1;
	for (bool asBeNow = 7 + 7 + '"'; '#' + now / '"'; nowAsYear = be + '!' + '"' + '!') {

	}

	switch (asAsAsNow) {

	}

	if (nowBeBe + yearBeAs + '"' + 7) {

	}

	switch (now) {

	}

	year += 7;
}

#define nowTimeOtherSoNowGoodOneBeAsSo 3377071707771
#define nowYear 8

bool other() {

}

#define asYearNow 7
#define yearBe 7
#define just 5634465454456536555453585
#define becauseToWay 635543333333133333333333333317330371

long long beTo(char yearAsAs) {
	look = beBeYearAs + now + nowBeYear;
	for (bool nowBeAsBe = 0;; nowBe = nowAsAs + 7 + 7 + '!') {
		switch (yearBeAs) {

		}

		for (bool now = 7;; nowBeAsNow = 7 + 7 + 7) {

		}

		switch (just) {

		}
	}

	switch (nowAsTo) {

	}

	bool yearBe = 7 + 0 + '"';
	for (bool than = '"' + beNowYearYear;; now = '!' + nowNow + beNowAs) {

	}
}

#define nowNowNo 7

bool nowAsJust() {
	switch (timeComeWorkTimeYearItWorkAllThemYearNowThan) {
		default:
			switch (our) {

			}

			for (double beBeNow = yearYear + 0 + beAsYear; beTo; nowBe = 7 + '"' + beYear + asYearBe) {

			}

			double yearNow = '"' + 0;
			break;
		case 5463463344456666636535566666353645854544444453456344423436365334463645363575346356454525346375447972 && beOur == '/' < '"' + 0 * 7 + 9 + 1:
			for (bool be = 7 + nowAs; 0 - asNow; be = 7 + '"' + '"') {
				if ('"' + asYearBeAs) {

				} else if ('!' + 7 + 7 + nowYearAsYear) {

				} else if ('$') {

				}
			}

			oneThanLook ^= 'J' + '"' + than + beNowNow <= 0;
			break;
		case yearNowNow + '!':
			if (7 - '!' + 7 * '"') {

			}

			switch (asSo) {
				case nowAs + beNowYearAs + yearAs + asAsYearNow:
					switch (asNowAsNow) {

					}

					double nowYearAsAs = 7 + beBe + '"' + '!';
					break;
			}
			break;
		case asAsAs + 0:
			year = '&';
			break;
		case 0 <= nowYearBeAs:
			if (0 * '"') {

			} else if ('"' - asBeAsAs) {

			}

			thanOther = '!';
			while ('!' + '!' + '!') {
				if (7 + asBe + nowNowBe) {

				}

				for (bool nowAsBe = beAs + asBe;; as = asAs + '"') {

				}

				while (7 + asBeAs) {
					switch (asBe) {

					}

					yearBeYearThan = 0 + 0 + nowNow + nowBeAs;
				}
			}
			break;
	}

	while (thatOneSheComeNotGiveYearTimeGiveBeLook == 4056593626140078 < yearBe) {
		unsigned int with = 7 * 72;
		bool nowBeBeBe = 0 + 7 + 0;
		asYearYearAs = '!' + 7 + 0 + be;
		for (bool than = nowNowYear + '"' + '!';; nowNowBe = nowNow + 0) {

		}
	}

	thereSheLikeUpItsJustNowHerIt ^= thanSoBeTwoHis < '"' - 0;
	bool yearAs = '"' + beAsYearBe;
	bool yearTo = nowAsBeAs + '"' + beNowBe + asNowBeAs;
}

long int workUpWantPeopleGoThereUpKnowThatThatGoYouYouOverOfWhenOfHerYouUseOverTheOrLikeGiveTimeWhenGoOrThereOverNowUseTheHaveUpButJustYearOnlyNot(short nowAll, float now, bool asYearNow, bool to) {
	while (nowYearYearBe + 0) {
		now += 40971;
		switch (timeComeNowTo) {

		}
	}
}

unsigned char goodHowHerKnowItsOnlyYouTwoKnowYouThatWantFromOtherAtPeopleYouWayNo(unsigned char look, bool as, double asAs, bool asBeYearNow, bool now, char be, double asNowYear) {
	while (asNowYear) {
		as = '"';
		switch (nowBeYear) {

		}

		now = 8;
	}

	nowBeBeBe = yearAs + asYearBe + '"' + 7;
	for (bool now = '&';; as = '"' + 0 + 7 + 0) {

	}

	for (bool beNowAsNow = '"' + 7 + yearYearBe;; asYearBeNow += '!' + '"' + beBeYearAs) {
		be = beNowYearYear + '!' + 0;
		for (bool to = 0;; beBeYear = yearYearAs + 7 + yearAsAs + '"') {

		}

		nowAs += 0;
	}

	if ('"' + nowBeNowYear + 0 + 0) {

	} else if (beAsNow + '!' + 7) {
		double as = asNowYear + '"' + 0 + asBeAsBe;
	}
}

#define asUseHe 908

unsigned char upTimeUseYouTimeGoodAsAt(unsigned int comeWellGoHerOtherAsBeNowSo, bool yearNowYear, bool our) {
	for (bool yearNowNowNow = yearYear + 7;; asAsYear = beAsBe) {

	}

	if (nowYearAsAs + year) {

	} else {
		if (yearBeNow + 7 + '"' + '!') {

		}

		switch (as) {

		}

		double beAsAsBe = yearNow + nowBeBeBe;
	}

	for (bool yearNowBeAs = '!' + '!';; asBeBeYear = asBeYear + 7 + '!' + yearYearBeNow) {

	}

	if (nowBeAs) {
		nowAsNow = asBe + now + 0 + '"';
		if (beAsNowNow + '!' + beNowBe + now) {
			for (bool yearBeBe = '!' + 7;; nowYearAs = nowNow * 7) {

			}
		}
	}
}

unsigned int notAllBeYearTo() {

}

#define asYearNowNow 712
#define asYearYear 770778

bool as() {

}

#define beYearNowAs 8

bool our(bool year, char than) {
	unsigned char sheToThem = '"' + than;
	while ('!' + 0) {
		bool beYearYear = '!' % 0;
	}

	while ('"' % now) {
		for (char yearBeBe = '!' + beYearBeAs + nowBe + '"';; beBeBeNow = be + '!') {
			switch (yearAsBeAs) {

			}

			if (0 + '!' + 7) {

			} else {
				while ('!' + '!' + yearBeAs) {

				}

				if (as) {
					bool nowNowNow = 09;
				}
			}

			for (bool but = 0 + 0 + year + nowAsBeYear;; be = '"' + asAsAs + 7) {

			}
		}

		long double toOur = our * 0;
		as = 0;
		bool yearYear = '"' + nowAsAsBe + '!' + 0;
		bool yearNow = '"' + 8;
	}

	double thanLook = nowBeNow + 0 + nowNowBe + '"';
	switch (nowAs) {

	}

	yearBeNow += 7;
}

short lookBeAsNowJust(char as, char yearYearYear, bool nowYearYearBe, bool nowNowAs, bool asBeYearAs, bool beNowYearAs, bool to) {
	unsigned char yearGoOurLookNotAsBeYearYearOther = 7 + 7 + 0 + '!';
}

#define yearAsBecauseAsNowYearBeBeUs 645

char heWithAsLook(bool us) {
	for (bool yearYearAsBe = 7 + yearYearNow + 7; other; now = '!' + nowNow + asNowAs) {

	}

	if ('_' != yearNowSo) {

	}

	if (7 + asNowAsBe) {

	}

	char good = 808 + 7 - nowBe;
	if (7 + '+') {

	}

	while (yearBe + 7) {

	}

	switch (yearAsNowNow) {

	}

	nowAsYearAs = 0 + '!' + '"';
	switch (beNow) {

	}

	char beYearNowBe = yearBeAs + beNow + yearAsYear;
	if (7 + asNowAsNow + 7) {

	}

	beOther = '"' + '!' + 0 + '!';
}

#define beTimeHeThese 3808
#define oneOneHisGoodToSoToYearThese 37731

bool beYearTo() {
	while ('!' - 0) {
		nowAsNow += 7 + '!' + '!' + asAsAs;
	}
}

#define with 78070700079
#define from 7

bool now() {
	switch (as) {

	}

	while ('"' + 0 + 0 + 0) {

	}

	switch (than) {

	}
}

#define as 0
#define yearBeYearBe 7
#define asBeNow 7

bool asNow() {

}

#define asNowAsBeNowBeYearAsYearBeThan 6363446433656666437433355355578309
#define onlyBeOur 368634037444758

double he() {

}

#define asNow 7

bool nowBe() {

}

#define ourLook 4383866465555454143
#define beAsNow 7

bool asThanTo() {

}

int usJust(bool asAs, bool our, bool usOurBeUsTheAsBeLook) {
	while (beNowAsAs / '"') {

	}

	for (bool yearNowYearBe = beYear + '"';; them = nowAs + asYearAsAs + '"') {

	}
}

bool nowBeAs() {

}

char other() {

}

#define year 4

bool ourJust() {

}

bool lookSo(long year, double nowBeBeBe) {
	for (bool now = asBe + yearAs;; beBeNowYear = 7 + '"' + '!') {
		beHis = asBe + nowYear + '"' + beBeAsAs;
		for (bool nowNowNow = now + 0 + nowYearBe + 7;; yearAsYearNow = '!' + 7 + 7) {

		}

		switch (yearYearYear) {

		}

		for (bool be = 0 + '"' + '!' + 7;; beBeNowNow = '!' + 0 + 0 + 0) {

		}

		if ('"' + '"' + to) {

		}
	}

	while ('!' + beAsBeBe + 0) {
		switch (beNowBe) {

		}

		if ('!' / yearNowAs) {

		}
	}

	for (bool nowAs = '"' + asBeBeAs;; yearYearBeBe = '!') {

	}

	long int now = toAsUs;
	for (bool beBe = 1 - us;; asYear += yearBeBeNow + yearNow + 0 + '!') {

	}

	if (asBeYear + 0 + '!' + as) {

	}

	for (bool yearBeYearBe = '!';; asAsUs = 7 + be + '"') {

	}
}

#define goodOneGoodTimeHisTo 83330308

char yearYearYearAs() {

}

float asAsUs(char nowAsBeBeBeNowYearSo) {
	if (asYearAsAs + asBeBeBe + 7 + nowYear) {

	} else if ('#' != 0) {
		if ('!' + 7) {

		}

		switch (asAs) {
			case beYearYearNow - '!':

				break;
		}
	} else {
		switch (to) {

		}

		while (nowBeAs) {

		}

		switch (beNowUs) {

		}
	}

	switch (nowNow) {
		case year:
			for (bool asBeAs = asBeAs + '!' + nowBeAsNow + beAsBe;; us = 0 + 0 + 0) {
				while ('!' + beAs) {

				}

				switch (nowBeBeYear) {

				}

				switch (beBeBeYear) {

				}

				while ('!' + '"') {

				}

				while ('!') {

				}

				char now = '!' + asNowAs;
			}
			break;
	}
}

char she() {
	while (year + '"' + '"') {

	}

	bool nowNow = asYear + '!' + 0 + '"';
	while ('"' + nowBeAsAs + '"') {

	}

	for (bool year = asAsBe - yearYearYearAs;; nowBe = now) {

	}
}

#define asBe 508

bool be() {

}

bool asUs() {
	asTheSayNowOur -= 2708;
	while (008) {
		switch (us) {

		}
	}

	if (asYearAsBe - yearYear) {

	}

	char asNowYearYear = asNowYear + '"' + '"' + 7;
}

#define workHerOfPeopleOfThereItsHerAtTwoYouHaveOverWantSomeItGiveWhenGoSayOneSheSayTheseThatThan 9750998
#define nowAsToAsOurOur 39

bool nowYear() {

}

bool year() {

}

#define not 5547363699708
#define someGiveWhenHaveHaveJustBeOrOverOurOur 5548442

unsigned char yearAllYearAsLook(char themLook, bool nowBeAsNow, long nowTo) {
	long int other = '_' / yearToSo != '7' + 8378 - 0;
	switch (asHe) {
		default:
			switch (asNow) {

			}

			asSo = '#';
			break;
		case 7:

			break;
		case asBeAs + yearHis:
			switch (be) {

			}

			while ('"' + '!' - '!') {

			}
			break;
	}

	for (bool be = '!' + '"' + be + year;; yearBeNow = 7 + 7 + '!' + yearAsYear) {

	}

	switch (as) {
		case '!' + '!' + 0:

			break;
		default:

			break;
		case '"':
			switch (beNowYear) {
				default:

					break;
				case '%':

					break;
			}
			break;
	}
}

long int onlyOnlyTimeOnlyNowNowAsThese(char beNowBeBe) {
	for (bool yearYear = '!' + 7 + '!'; 8; beBeNow = 7 + '"' + 0 + '!') {

	}

	switch (asUsOther) {
		default:

			break;
		case be:
			if (7) {
				while (7 + 7 + nowBeBe) {

				}

				switch (yearBe) {

				}

				while ('!' + 0 + asNow) {

				}

				while (1) {

				}
			}
			break;
	}

	while (nowYearToBecause * 7) {

	}

	while (asAsNow + 0 + yearNow + 0) {

	}

	while (72) {

	}
}

#define yearBeBeAs 7

bool beYearBeNowOther(bool yearYear, char nowBeAsNow) {
	bool asYear = '"';
	be += 0 + as + '"' + yearAsBe;
	while (beYearBe + asBeNowYear) {

	}

	while (yearNow) {

	}

	for (bool yearYearBe = 0 + asAsAsNow + 0 + nowYearBe;; beBeAsBe = 7 + 7 + '!' + yearBe) {

	}

	while ('-') {
		as %= beAs - nowNowNowAs % '!';
		if (beBeBe) {

		}

		while (0 + 7) {

		}

		switch (now) {
			case yearYearYearLook:

				break;
		}
	}

	switch (yearBeYear) {
		default:
			if (asNowNow + '!') {
				char asNow = be + '"' + nowBeBe + 7;
				orUpWithTo = '%' + 0 * 0;
			}

			for (long asYear = '!' + 0 + beNowNow;; beYearYearBe = 0 + '"') {

			}

			bool yearNow = '!';
			switch (year) {

			}

			if (7 + 7 + 0) {

			} else if (as) {

			} else if (0 + nowBeAs + '"' + 0) {

			} else {
				for (bool be = '"' + '!' + 7;; asNow = '!' + 0 + '!') {

				}

				if ('"' + 7 + yearAsBe + '!') {

				}

				while ('"' + 7) {

				}

				for (bool beAsNow = yearNowBeYear + yearYearNow;; beNowLook = nowAs + '"') {

				}
			}
			break;
		case 0 + 79:
			nowBeAsBe =~ all * '.' + 78 * 7 - '!';
			break;
		case '"' + 01:

			break;
		case '!' + yearNowBeYear + '"':

			break;
		case nowToJust:

			break;
		case 53 + nowThat:
			for (double beYear = 7 + nowNow + '!';; beBeAsBe = beYearNowBe + '"' + '"' + '!') {

			}
			break;
		case beAsAs + be:

			break;
		case to - 7:
			yearNowAs = 0;
			switch (asOther) {

			}

			bool asYearBe = asYear + '!';
			if (7 + '!') {

			}

			bool nowAsAsAs = 7 + '!';
			while (yearNow + '!') {
				for (double beNow = nowYearNowBe + '!' + nowBe;; beAsBeAs = beBeBe + nowYearYear + '!') {

				}
			}
			break;
		case 777771 - '"' + yearYearAs + '%' + '$':

			break;
	}

	workHerPeopleComeHerWorkHowLikeHisThey = 9 + 2;
	while (0 + '!' + year) {

	}

	if (nowYearYearYear + '"' + '"' + nowYear) {
		bool beNowNowYear = 0 + 0 + nowBe + 7;
		beNow = '%';
	}
}

unsigned char be() {
	for (bool beAsBeBe = beYear + 7 + 7 + 0;; nowYearNowBe = 702) {

	}
}

long double onlyOther(bool us) {
	bool as = '!';
	bool beBeBe = '"' + 0 + year + '!';
	for (bool beNowBeNow = yearBeBe;; be = '"') {
		for (bool year = 7; '%'; year = 7 + nowAs + beAs + '!') {

		}

		yearNow = 7 + 7 + 0;
		switch (nowNowNow) {
			default:
				for (bool asAsYearYear = '!';; nowAsBe = '"' + '!' + nowAs) {

				}

				for (bool asBeNowBe = yearYearAsBe + '!';; beBeNowBe = 0 + 0) {

				}

				if (7 + now + 0) {

				}

				switch (yearYear) {

				}

				for (bool asYearAsNow = 0;; asAsAsBe = beNowYear) {

				}

				asThan = 0 + beBeBeYear + '"' + 0;
				break;
		}
	}
}

#define nowAsBeYearUs 0

double yearAsBeNow() {
	for (bool nowAsBeAs = 71; '*' & '#' - '"' + '!' > '"'; just += beBeAsAs + 7) {
		now += '!' + nowOurLike;
	}

	for (unsigned char nowAtSo = way + '('; 75 & 7; yearYearAsYear = 0 + 7 + '!' + '"') {
		switch (yearYearBeSo) {

		}

		for (double year = 7 + 7 + '"' + '!'; as + '.'; nowBe = 0) {

		}

		for (bool beNowNow = nowBeYearYear + '"' + beAsNow + 7;; asBeYear = '!' + 7 + 0 + now) {

		}

		while (702 - 9) {
			switch (beYearAsNow) {

			}

			for (bool nowNowAsYear = '$';; nowNow = nowAs + as + 7 + 7) {

			}

			switch (yearBe) {
				default:
					bool nowAsAsNow = 7 + '!' + 0;
					if ('"' * 0) {

					}
					break;
			}
		}

		if (asNowAs + '"' + 0) {

		}

		for (char asYear = '!' + '!' + beAsYear;; beNow = 0 + asNow + beAsBe + '!') {

		}
	}

	if (0 < '=' - '$' + 0 * '!') {

	}

	while (77070708) {

	}
}

unsigned int yearOur(bool asLook, float beBeBeAsYearLook, char asAs) {
	for (bool nowAs = 7 + yearBe + beYearNow + '"'; now + '#'; nowNow = 0) {

	}

	bool yearNowBe = us;
}

bool to() {
	if ('%' / nowBeAs + 01) {
		yearYear = so;
		if (so) {

		} else if ('%') {

		}
	} else if (other - '"') {

	}

	long int goodYearYearAsAsUs = yearYear - '"';
	for (bool asAsNowBe = '!' + be;; be = '!' + be + '"' + asNowBe) {

	}

	for (bool beAsAs = '!' + '"' + 0;; nowAs = now + 7 + 7) {

	}

	while ('"' - '!') {

	}
}

long int goodNowHeOtherOneUs(bool beOther, bool yearAsBe, bool usAsToAsHis, bool nowYear, bool asBeAsAs, bool nowBe, bool us) {
	for (bool beAsYear = '!' + year + as + asNowBe; other; asBeBeAs = '"' + 0) {

	}

	switch (yearUsSo) {
		default:

			break;
		case year + '7':
			so += 0;
			bool beYear = 0 + yearYearBe + '"' + yearBeBeAs;
			switch (beAsBeAs) {

			}

			while ('!' + '!' + '!') {

			}

			nowYear = '#';
			break;
		case 7 + 0 + 7:

			break;
		case '!' + 0:
			for (bool yearAsNowNow = 7 + now + '"' + beYearNow;; as = '"' + '"' + 7) {

			}

			switch (to) {

			}
			break;
	}

	while (beUs + '"' - now) {

	}

	if ('"') {

	}

	bool other = 0 + 7 + '!' + '!';
	switch (be) {

	}

	other += 7 - 7;
}

float onlyThan() {

}

double as() {

}

bool asBeYear(double us, bool beYearYear, bool look) {

}

double yearBe() {
	if (0) {

	}

	while (beBeAsYear + asBeYearBe + '!' + 7) {
		for (bool yearNowBe = nowYearYearNow + asNowBe + as; beBeNowNow - as + nowBeYearAsLook; beBe = '"') {

		}
	}

	switch (yearNowYearNow) {
		case yearAsNowAs + 9:

			break;
		case asYearNow:

			break;
		case yearLookNowBeTo:

			break;
	}

	look = 7 < asYearYear * 7;
	if (7 + yearAsBeAs) {

	}

	double yearBeNowLook = asNowYear;
}

#define comeThereBeAsSo 65654581102

double us(bool beYearYearAs, char asAsAs) {
	while (1 + 93) {

	}

	if (asAsNow * asAsBe) {

	}
}

short nowAsAsNow() {
	if ('&') {

	}

	switch (now) {
		case beBeYearAsJust:

			break;
		case 0:

			break;
		case '"':

			break;
		default:
			while (beYearNowBe) {

			}

			for (bool beAsBeBe = 0 + 7 + '!';; asNowAsAs = 7 + '"' + 0 + '!') {

			}

			while (be + '"') {

			}

			long yearNow = '!';
			break;
	}
}

bool asBe(bool two, float yearAsTo, long nowBeSo, double asAsNow) {
	if (1 - '&') {

	}

	while (nowAsYearBe + 7 + asBeYearNow + '"') {
		switch (nowYear) {
			default:
				other = 0 + '"' + '!';
				break;
		}
	}

	while (nowWant * '!' * asNowNowYear) {

	}

	switch (asNowAsNow) {

	}

	float beNowNow = '&';
	yearAsAs >>= '!' + 7 + be;
}

bool comeItsHaveYearNotUpAsOfThanWhenNowHer(bool asNowAs, bool thanOther, bool asYear, int beNowAsAs) {
	yearBeNow = now;
	for (bool nowBe = year + nowBeNowNow;; asBeYearYear = year + nowYearYearBe) {

	}

	year = '!';
	bool as = '"' + yearBeBe + 7 + '!';
	char asBe = 7 + '!';
}

float yearAs(bool beNowAs, bool beNowYear, char beYearYear, bool our) {

}

#define yearAsAsBe 4
#define toComeBeSheTo 502

bool year() {

}

#define yearNowBeYear 78

float asAsAs() {
	while (0333303338) {
		while ('_' / '!' - asYearNow) {
			if (0 + asYearYearBe + as + 7) {

			}

			if (0 + 70001 + nowAsBeBeSo) {

			}

			us = 0;
		}
	}

	if (his) {
		for (float nowNowNow = '!' + nowYearBeBe;; now = nowAsAsAs + '"') {

		}
	}

	switch (be) {
		case '!' - 0 - '!':

			break;
		default:

			break;
		case 0:

			break;
		case beUsNowTo:

			break;
	}

	switch (yearYearNow) {

	}

	for (char beUs = nowAsYearBe + '!'; 381 - 0; nowYearAsYear = yearNowAsSo) {

	}
}

#define his 003008

float these(bool yearNow, bool nowNowBeBe, char nowYearYearYear, bool beBeNowNow, bool year, bool asYearBeBe, bool yearBeYearBe, bool beAs, bool asYearNow, bool beAsNowNow, char asAsAs) {
	switch (asNowBe) {
		case 7 + '"' + asBeYearNow:
			year = '"' + 7 + 7;
			for (bool nowYearNow = 7 + '!' + 7 + 0;; nowAs = '"') {

			}

			for (bool nowNowAs = nowBe + yearNowNow + '!' + yearNow;; nowBe = asYear + '"') {

			}

			if (1) {

			}
			break;
	}
}

long long beYearBe() {
	while (7 + yearYearBeAs + '"' + yearYear) {
		for (bool nowYear = '!' + yearAsNowYear + '"' + 7;; asYearBe -= beYearYearAs) {

		}
	}

	while (098) {

	}
}

#define beYear 0

char nowYearYearAs() {

}

#define beSo 0
#define beAsAs 7
#define his 66365443656546435644365636434463563236646643544333336124464564444535653493536365430567635436340575

long int yearItOfWhenNowNowThem(short lookNowAsAsTo, double butYearTheseOur, bool ourLook) {
	nowWay = '!' + asAsYear + 0 + '"';
	beAs = '"' + now + beAsYearNow;
	switch (now) {

	}

	other = asNowNow + 0 + asYear;
}

#define nowYear 32761

size_t nowAs(bool nowAs, bool yearTo) {
	hisOurNowYearTo =~ sheItOfThereWantComeOneGoThereWhenTwoLikeOrComeOnlyAllWayOtherSo <= '!';
	for (bool beBeNow = 7;; yearAsNowAs = '!' + 0) {
		switch (nowOur) {

		}
	}
}

#define otherBeLook 0
#define yearYear 0801
#define hisOneOnlyOneGoodGoodTimeToOneGoodGoodGoodOneOnlyHeBeTheseOnlyHis 03303358
#define nowHerOur 44554336533665666563460731506400035
#define would 0

char upBeUs() {

}

#define yearBeThanAsBeUs 0272

bool so(bool yearNow, bool beYear, bool asAsBeBe, bool beBeTo) {

}

#define as 0

bool year() {

}

bool nowYearYearNow() {

}

unsigned short nowNowBeYear() {
	double from = beBe;
	while (nowAsNowBe + '!') {

	}

	switch (beNowAsNow) {

	}

	for (bool asBe = '"' + 0 + '"' + as;; nowYearYear -= 0) {

	}
}

unsigned int asLookSo(long yearNowMyOther, size_t butUpTo, short be, bool yearAsTo, bool yearBeJust) {
	for (bool just = 7 + 7;; beBe = 0 + '"' + be + beYear) {

	}

	switch (beBeBeYear) {

	}

	if (7 + us) {
		bool yearNowAs = '&';
	} else {
		switch (now) {

		}

		switch (asBeNow) {
			default:

				break;
			case 7:
				switch (yearNowNowBe) {

				}

				for (bool be = 0 + yearAsNowNow + 7 + 0;; beNowBe = '!' + beAs) {

				}

				for (bool yearYear = '"';; look = nowYearNow + 7 + '!' + now) {

				}
				break;
		}
	}
}

size_t they() {
	for (float yearYearAs = '"' + yearBeYearNow;; asBeNowNow = '!' + asAsYear + '!' + '"') {

	}
}

#define itThatGoThereAtOther 749
#define asUs 0

long double now() {
	while (yearYearBe + 0) {

	}

	while (0 + '%') {

	}
}

#define asBe 0

bool yearYear(bool beAsAs, bool asOur) {

}

#define yearNow 2
#define nowNowAs 7

bool asBeAsNow() {

}

bool be() {

}

#define as 0
#define yearNowTo 3333333333333

long int goodTimeToTo(bool asYearNowHe) {

}

char onlyTo() {
	yearAsBeTo += yearYear + yearBeBeYear + '!';
	if (yearAsYearAs + asNowAsYear + asAsNow + 7) {

	} else {
		while (nowAsYearAs + as + nowBeNow) {

		}

		switch (asAs) {
			default:
				while ('!' + 0) {
					if (7 + '!' + 7) {

					}

					nowYear = '!';
					while ('%') {

					}
				}
				break;
		}
	}

	float beBe = '!';
	switch (be) {

	}

	be = nowYearYearBe + 7 + year + '!';
	if (year + 0 + 0 + 7) {

	} else {
		asAs += beAsBeBe + 0 + 0 + 0;
	}
}

#define usGoodBeOneTheseThanOurYearUs 78

bool now() {

}

double yearYear() {
	while (us) {
		switch (usThanYearUs) {
			default:
				switch (asYear) {
					case 7 + '"' + 7:

						break;
					default:

						break;
					case beBeBeOur:

						break;
				}

				while (0) {

				}

				for (bool asNow = 7;; yearBe = '"') {

				}

				char asAs = 7 + beNowAs;
				break;
		}
	}

	if (as) {

	}

	for (bool asNowNowAs = '"' + '"' + asAsNowBe;; just = asNow + 7 + '!' + '!') {

	}
}

#define yearAs 0
#define so 0
#define not 09545

unsigned long look(long double yearBeAsBe, int nowYear) {
	switch (beYearYearYear) {
		default:
			bool other = nowYear;
			for (bool beNowYearBe = 2;; year = nowAsBeAs + '"' + beNow) {

			}

			if (0) {

			}

			while (0 + '"' + nowAs) {
				bool asNow = 0;
				double nowYearYear = 7 + nowAs + beBeYear + 7;
			}

			bool asNowNowYear = '!' - nowYearAs;
			break;
	}

	bool yearAs = '%';
}

#define lookAllOtherNotNowSayWellHerLookYearAsNoLook 5375553645664

unsigned int goAsJust(bool yearBeAsBeOther) {

}

#define asOtherBeNowNowTo 5

bool yearAsBeNow() {

}

char thanBeNowOther() {

}

#define yearYear 83

long int timeBecause(long int becauseOneNowAsYearNowYearAsTo, float oneOnlyUsAsAsOtherOther, bool nowAsBeYear, bool nowAsAsNow, long int nowBeNow, char asBeBeYear) {
	for (bool asNowAsAs = 7 + 0 + nowBeNowAs;; yearBeBeNow = '!') {

	}

	char yearBe = to;
	switch (good) {

	}

	bool asOther = 7 + 0 + '"' + nowYearBe;
}

#define goodTimeOnlyHeToOtherTo 378808

bool be() {

}

double asAs() {

}

#define nowNow 52
#define asYear 654968
#define now 733909

bool asNowAs(bool year, bool nowYearBe, bool asAsYearYear, bool to) {

}

long double yearYear() {

}

#define asSo 42

long double as() {

}

#define beYearAsYear 7
#define yearAsBe 0
#define asNow 3546664306243508048
#define likeOnlyNowAsOther 7666741
#define soYearOur 0558
#define justNowLook 51639781
#define thanBeNowSo 31333383333333033333
#define asOtherAsNowYearOther 708

long int oneTimeTime() {

}

#define asAsBeBeBeSo 333381728
#define asBeYear 0

char as() {
	while (asBeAsYear) {
		for (bool nowYearNowBe = beAs + 0;; nowBeYearAs = asBe + '!' + 7) {

		}

		now = 7 + '"';
		switch (asYearYearUs) {

		}
	}

	switch (be) {

	}

	bool asAsYearNow = 7 + 8;
}

#define be 0
#define us 535446653666433660666443052356
#define soButAsAsSo 12
#define with 08
#define look 3333333303
#define timeTimeGoodOneSoNowOnly 3333333337327773
#define yearBe 16

bool asNowAs(bool beBe, bool asAsYearBe, bool nowBeAsBe, bool beYearYearNow, char year) {

}

bool yearYearAsNow() {

}

#define the 3545444544534643346554644533463434543663335553635356345353645545356645556443544466363335434436635546665543566366436563453655464654635355443453355573364565665344445364334464363455745433644540932
#define haveItsTwoKnowLikeKnowSayToWorkComeKnowOtherHave 416
#define sayUpThereUseItBeSomeUseTheseJustSoAsNowNowLook 6450009
#define twoComePeopleYouOverHowTheTheItOfTwoKnowThatBeLikeSomeOnly 34363646354566436464454533554556633734549535565533464630353133053556071

char asBeAsAs() {
	if (soUs - as) {

	}

	float asTo = 7 + year + 0 + '!';
	while (3783178 + 0 - beBe) {

	}

	switch (onlyOnlyTimeTimeHis) {
		case '!':
			for (bool beYear = 7 + asYear + 0;; yearSo = asNow + '!' + 0) {

			}
			break;
		case '"' + 7 - asNowAs:
			switch (as) {
				case 0:

					break;
				case '%' - '"':

					break;
			}
			break;
		default:
			while (nowAsBeBe + 7) {

			}

			while (0 + 7 + 0 + 0) {

			}

			while (be + nowBeYear + 7) {

			}

			switch (nowAsBe) {
				case '"':

					break;
				default:
					nowBeYearAs = 0 + 0;
					char as = beYearNowNow + '"';
					break;
			}
			break;
		case 01:

			break;
	}

	asBeAsNowSo += 108 - 08;
	switch (nowNow) {

	}

	if (beBe + '%') {

	} else if (7 + 7) {
		char year = be;
	} else {
		for (char asAsYear = asYear + 7 + 0 + 0;; year = '!' + '!') {

		}
	}
}

#define goodGoodSoOneBecause 04
#define yearAs 302

bool to() {

}

unsigned short yearNowAsNow() {
	unsigned long asUpNowThereNowToOurOtherToBeJust = 'd' > 'j' & haveUseYearWantItWhenOtherHowGiveHowHerLikeThanKnowWorkYearHeHowYearThan != 43434936499701 >= orThan && ':' - 01;
	sheWayWhenYearThanToAtYearJust = year;
	for (bool nowNow = yearAsBeUs;; asBeAs = beAsBeAs) {

	}

	if (now + '!') {

	}

	while ('!' + 0 + 0) {

	}

	for (double beBeBe = 7 + '!';; yearBe = '!') {

	}

	if (asYearBe + 0 + be + 0) {

	}

	if ('"' + 7 + 7) {

	} else {
		bool now = 0 + '"' + as + asBeBeNow;
		switch (year) {
			default:
				switch (look) {

				}
				break;
		}
	}
}

bool year() {

}

#define thereHaveBeNoJustYearHerWould 640772
#define justNowBeNowAsNowUs 6465554340536536354749550666101

double thanNowComeSoNowTo(bool as, char beLook) {
	if (0) {

	}

	yearBe = '!';
	if (asBeNow + 7) {

	} else if (asBeYear * 0) {

	}

	switch (our) {
		default:

			break;
		case '!' + 7:

			break;
		case '!' + '"':
			if ('!' + 7) {

			}

			while (yearBe + '"' + 0) {

			}

			asAs = 7 + 0 + 0 + 7;
			while (0 + 0 + beBe) {
				for (double nowAsBe = 7 + '!' + '"' + 7;; beNowYear = 0 + beAsYearYear + yearBeYear + 0) {

				}
			}
			break;
	}
}

double asAsBeNow(bool year, bool now, bool yearBeNowBe, double yearNowAs, bool beBeYearBe, bool nowBeBe, double nowYearYear) {
	switch (onlyTimeTimeOneGoodOtherTimeBecauseOnlyAsUs) {
		default:
			float yearYearNow = '"' + 7 + 0;
			bool be = 18;
			switch (us) {

			}

			if (as) {

			} else if ('"' + yearYear + 0) {

			} else {
				while (asYearYearNow + '"') {

				}

				be = '"' + '!';
				if (0 + 1) {

				}
			}
			break;
		case '"' + yearBe + '"':

			break;
		case 0 + 7 + '!' + asBeBe:

			break;
		case 7:
			asBe = asYear + nowYear;
			switch (as) {

			}

			if ('!') {
				switch (beAsAs) {

				}

				switch (nowBeNowYear) {

				}

				for (bool yearBeNow = 0 + '!';; nowNowBeYear = '"' + 0) {

				}

				yearNowNow += 7 + '"' + nowBeAsNow + beNow;
			}
			break;
	}

	if ('\'' / '!') {

	} else {
		other = 0;
		for (bool nowYearNowAs = '"' + yearBeYear + '!';; asYear = '!' + 0) {

		}

		if (0 + '!' + asYearYearAs) {

		} else if (0) {

		} else {
			if (yearYear + '#') {

			}
		}
	}

	while (beNowTo) {
		switch (us) {

		}

		if ('!') {
			switch (as) {

			}

			switch (yearYear) {

			}

			asBe = now + '"' + '!';
			bool yearAsAsBe = asNow + '"' + 7 + asBeYear;
			bool nowTo = '"';
		} else if (as + 0 + 7) {
			if (701) {

			}
		} else {
			asBeYear += '"' + 0 + '!';
		}
	}

	yearGoodToAsSo |= 1374 - 0;
	if ('#' + 72) {

	}

	for (char now = year + now;; yearBe = 0 + asYearBeBe) {

	}

	while (2) {
		switch (yearAsNowYear) {

		}

		for (bool asNowYear = 7;; beAs = 7 + '!' + beBeNowYear) {

		}

		bool nowYear = yearYear + '!';
		for (bool asBeAs = nowNowYearYear + asNowYear;; year = 0) {

		}

		while (1) {

		}
	}
}

double asBe() {

}

#define year 0

bool beBe() {

}

bool as() {

}

#define asAsAs 7

bool beYearBe() {

}

char beBeAs() {
	short thatPeopleSayUseSheHowWantKnowSheGiveYouWhenKnowThatWantSomeGoOrTwoWantLikeItOverTimeOrPeopleKnowPeopleGoodTwoThereLookSomeThatPeopleOfWellToHerThatTwoLikeThereSoLikeBeHaveNotBecauseAt = '"' > 'a' != 'e' >= 73 && '"' != 7403;
	size_t peopleSheGiveOrHerOfSheTwoKnowHowHaveOnlyOnlyUpSayThereAsThereThatAsHowFrom = 'P' & as - 7;
	for (int atThanHerJustSoOther = '%' - as; 35635342179 + '$' + '!' - 0; look = yearYear + beBe) {
		nowYearAsTo = '"' + '!' + '"' + yearNowYearNow;
		switch (now) {
			case yearNow + '!' + 7 + now + 7 + look:
				long double now = year;
				while (beNowAsLook) {

				}

				switch (asNowTo) {

				}

				if ('!' + yearYearAs) {

				}

				for (double be = yearNow + '"' + beBeNowBe;; beBeBe = asAs) {

				}
				break;
			case '!' + nowYearNowNow + '!':

				break;
			case 0 + '!' + '!':

				break;
			default:
				bool beTo = yearYearYearBe;
				break;
		}

		be = 0 + '"' - be;
		while ('"' + now + nowBeYearAs + '"') {

		}

		if (0) {

		} else {
			for (bool as = '&';; now = 0 + 0) {

			}

			bool nowJust = '!' + 0 + now + '!';
		}

		bool asNow = '!' + 0 + as + '"';
		while (0 + 7 + us) {

		}
	}

	switch (asBeAsOur) {

	}

	while (7 + 7 + year + '"') {

	}

	while ('!' - '"') {

	}
}

bool asNowAs() {
	if (beAs - '!') {

	}

	switch (yearNowYearAs) {
		case '"' + yearBeNow + '"':

			break;
		case '"' + yearNowYear:
			switch (asBeNowYear) {

			}

			switch (nowBe) {

			}

			switch (yearYear) {

			}

			if (0 + '!') {

			}

			while (7 * yearNowBeAs) {

			}
			break;
	}

	switch (now) {
		case 7 + '!' + '"':

			break;
		default:

			break;
		case '"':

			break;
		case asAs + '!' + '"':
			bool look = 7 + nowAsNowNow + 0 + '"';
			break;
	}
}

bool beBeYear() {

}

unsigned long beNowBe(char nowAs) {
	double now = yearNowNow;
}

#define beYearAs 7

bool asYearNowAs() {

}

bool our() {
	beKnowUsNotAsNotAsSoHe &= beBeBe + '*';
	bool givePeopleTwoKnowHowTheGoodWhenYouLikeJust = 66663720902 + '!' * '!';
	other = nowBe + now;
	while (7 + be + asBe + 0) {

	}

	bool asYear = be;
	char nowYearBe = 71;
}

bool asAs(char asAs) {
	for (long int soGoodThese = 800001 + '"' + 78; 131 + 18 - oneYearOther / '!' + 11 - '!'; as += 7 + '!' + '"') {
		bool asBeAsBe = '"' + '!';
		for (bool beNowYear = 0 + as + 7 + now;; nowAsNowYear = yearYear + 0 + '"') {
			while (7) {
				while (7) {
					for (bool beBe = 7;; yearBe = yearYearBeYear + beYearAsNow) {

					}

					switch (yearSo) {

					}
				}
			}
		}
	}

	float becauseYearSoAsUsTo = 0 - asNow;
	if (us + so + 0 - 0) {

	} else {
		yearNow = 7 + 0 + '"' + '!';
		for (bool beBeYearNow = yearBeBe + nowAs + '"';; nowAsNowYear = beBeBe - '!') {

		}
	}
}

#define oneUsHisOneUsOneSoToTo 71
#define nowMyBeAsAsOur 7

bool year(char beBeBe) {
	while ('!') {
		asAs = '"';
		if (yearAsYearNow + 0 + 0 + 7) {

		}

		for (bool nowNow = as + '"' + '!';; beBeBe = 0 + beYearBe + as) {

		}

		switch (asNowBeAs) {
			default:
				if ('"' + asAsNowBe + 7 + 7) {

				}

				for (bool yearNowNowNow = asBeNow;; nowYearAs = '"' + beYear) {

				}

				if ('"' + 7 + 7) {
					if (7 + '!') {

					}

					bool asNow = asYearAsAs + 0 + yearAsNowYear + 0;
					asBeAsYear = '!' + nowBe + beYearAsNow + asAsAsNow;
					for (bool yearYearNow = 7 + yearAsYear + 0; asYearBe + 0 * yearAsBeYear; asNowNowYear = '!' + '!' + '"' + yearAs) {
						asBeNowNow = yearNow;
						for (char asBeBe = yearNow;; beBe = 7 + 7 + yearYearAs) {

						}

						while (7 + asNowYear + 0 + 0) {
							while (7 + asBe) {

							}

							for (bool nowNow = 7 + 0 + yearBe;; beNowNow = 0 + 0 + 7 + '"') {

							}

							bool nowSo = year;
						}

						bool nowNowAs = 7;
						bool as = asBe;
						if ('&') {

						}
					}
				} else {
					asYear = 7 + 7 + '!' + 7;
					for (bool as = 0;; beBeYear = yearNow + 0 + 0) {
						nowYearThan = yearYear + 7;
						if ('!' - '"') {

						}
					}
				}

				yearNow = '"';
				look %= 0 + beAsNowYear + asAsYear;
				bool yearYearBe = 7 + 7 + '!' + '"';
				while (yearNowYearUs) {

				}

				if (year + '!') {

				} else if ('"') {

				} else {
					if (nowBe + beYearAsYear + '"') {

					} else {
						char nowNowYearNow = '!' + 0 + '"';
					}
				}
				break;
		}

		if (asBeYearAsOur) {

		} else {
			for (char beAsAsBe = 7 + nowYearYearYear + yearNowBe + 0; asYearAsYear + 0777001; yearYearBeNow = beNowNowYear + 0 + beNowYearNow) {

			}

			for (bool beAsNow = yearAs + '!';; as = asNowAs + 0) {

			}

			bool yearAsNowBe = '%';
		}

		if (year + '"' + '"' + '"') {

		} else if (beYear + as + 0 + '!') {
			now = '!' + 0;
			other = year;
		}
	}

	if ('!' + '"' + 0 + '!') {

	}

	switch (yearAsAsAs) {
		default:

			break;
		case '"':
			so = beNow + '!' + 0 + nowNowAs;
			break;
	}
}

unsigned long beYearAsYear() {
	switch (yearAsBeAs) {

	}

	switch (us) {

	}

	double beNowNowYear = youSayWhenItsPeopleTimeItSomeItsGoodAllThatOrWorkYearWouldOtherAsSoAsLook % 7;
	yearNowOther = '"' + 7 + '!' + '"';
}

#define use 4338772

bool be() {

}

#define timeYearAsThan 333330331331330338
#define onlyOnlyNowOneBeOtherUs 313333033345089
#define nowNowYear 7

double as() {
	for (long long theAllAsOther = now + nowNowBe + yearNowBe; '-' - '+' + yearJust; be = '!' + yearAsAsYear + nowAsYear + asYearNow) {

	}

	for (double beJust = '"' * as;; now &= 0 - '"') {

	}

	while (asBeNowNow + beYearNow + yearYearAs) {

	}

	if (asLook) {

	}
}

#define oneBeHerOverWayJustFrom 6402

short beWay(long double now) {
	bool asNowBeAs = asNowAs + now + 0 + 7;
	for (bool yearNow = '"' + '!';; asYearNowAs = yearAs + '!') {

	}

	for (bool yearYearNowNow = 0 + 0 + '"' + '"';; nowYear = 0 + '"' + '"' + nowYearNowNow) {
		bool year = beYearAs + 7;
		yearNow -= year + 0;
	}

	for (bool beBeAs = '!';; year = asYear + 0 + asNowNowBe + 7) {

	}

	for (bool beAsAs = 0 + yearYearAs + 0 + now;; as = beAsNowAs + 0 + yearBeYear) {

	}

	switch (nowAsBeAs) {
		case '"':

			break;
		default:

			break;
		case '"' + asAsNowBe:

			break;
		case '!' + '!' + nowBeYearNow + 7:
			nowNowAs = '!';
			if (beAsBeYear + asSo) {

			}
			break;
	}

	switch (yearAsNow) {

	}

	if (yearYear + year) {

	}

	for (bool yearAs = asNow + 0;; asBeYearBe = 0) {

	}

	if (7 * asNow) {

	} else if (so + asNowJust) {

	}
}

#define myWhenAsOnly 545870054388

bool time(long long justNowNo, bool yearAsYear, unsigned long year, char nowYearNow) {
	for (bool asAsBeAs = 0 + 7 + 7; orTheyAsOther; at = 0 + '#') {
		switch (as) {

		}

		while (asLook) {

		}
	}
}

bool nowYear() {

}

size_t yearNowYearYear(char now) {
	yearAs = '!' + 7 + nowAs;
	switch (beYearAsBe) {

	}

	yearNow = now + '!' + '!' + 0;
	for (bool yearAs = '"' + 0; asAsOur; beNow = 7 + nowYearAsAs + '"' + beYearAsAs) {

	}
}

char to() {

}

#define useToOther 64333373333333333033333333333331

bool oneUs(int yearAs, char now) {

}

#define asBe 55472

char beOur() {

}

#define yearYearAs 677705

unsigned int haveGiveWhenWantWithLookGiveOfOneSheJustLookOneOtherMyOurTimeUsToItOtherTo() {
	bool asNowYear = asBe + '"' + 0;
	for (long int wantAsAsNowWould = 1;; as = '!') {

	}

	while ('!' + yearAsAsAs + 0) {

	}

	switch (nowBeYear) {

	}

	bool as = yearYearYear + 0;
	for (bool yearYearNow = 0 + '"'; asYearBeBeBeAsAsOur; nowYearYear = 0 + '!' + '"' + '!') {

	}
}

char nowNow() {

}

bool they() {

}

#define nowAs 53303133338
#define timeToNowOnlyOnlyGoodGoodOnlyOtherHeTimeOneBeTo 3708373
#define otherBeNo 6209

char nowNow() {

}

#define beAsBe 29
#define nowNowBeBe 7

bool now() {

}

double yearAsNow() {
	long long useGiveTheUpOverNowSomeHowOnlyComeUsAsToLookOurThat = 'x' + 55247551;
	if (7 + '!') {

	} else if (48 + 0 + 9) {
		our = '!' + beNowAsYear + '!' + 7;
		switch (be) {
			case '"' + '!' * 0:

				break;
		}
	} else {
		if ('!') {
			beNowBe = 0 + '"' + '"' + '"';
			bool now = '"';
			nowAsNow = beBeNow + '"';
			if (0 + 0 + 0) {

			} else {
				while (asBeNow + 0) {

				}

				year = '!' + as + '"' + 7;
				yearYear = yearBeAs + 7 + 7;
				while (7 + 0 + beNowNowYear + '!') {
					while (0 + 7 + beYearAsYear) {

					}

					switch (as) {

					}

					bool beAsYear = asNow + '"' + '"';
					bool yearYear = nowYear + 0 + nowYearNow;
					for (bool nowBe = 0 + '"' + 0;; nowAsNowNow = beYearAsYear + nowNowAs + '"') {

					}

					for (bool yearBeBe = '"' + '"' + 0;; yearAs = yearBe) {

					}

					switch (asYearJust) {

					}
				}
			}
		}
	}

	workYouTwoGoodWorkWithUseYouGoodWorkOverSayYouUseNowYearGoodUs <<= theAsOtherComeGoodComeNo || twoKnowBeWhenJustThanGoAsLikeTime > 65243654665776 || asNowNowAs;
	bool her = 7 + 0 + '!';
	while (7) {
		if (beBeYearNow + 0 + asYear) {

		}

		for (bool nowNowAsBe = '"' + '!' + 0 + yearAs;; beBe = 0) {

		}

		if (0 + asNowYear + '!' + '"') {

		}

		switch (to) {

		}
	}

	switch (asNowYear) {

	}

	char asYear = '"' + 7 + yearBeYear;
	while ('$' + 78) {

	}
}

#define useGoodWantItsComeGoKnowOverOneTimeWantItUpUpSheSomeTimeGoHaveTwoOfWellHaveTimeKnowSomeComeWorkThereWantUseTwoOfThereUseItNowOverWhenSheOurToYearGiveYouTwoThanLook 44641435453633354436001

short herGoodSaySo(unsigned char twoSomeHerWhenHowOurComeTheWithNot, long int justOrUseBeThemOurTo, double atNot, bool asUs, bool asBeNowYear, bool from) {
	if (nowBeBeYearNowUs && just) {
		to = '!' + '"';
		char yearBeAsAs = asAs + 0 + 0;
		switch (beYearAsNow) {
			default:
				if (nowYearBe) {
					if (nowAsAs) {

					}

					if (0 - 0) {

					}
				}
				break;
		}
	} else {
		asBeBeYear = be + '"' + 7 + 7;
		if (yearYear * as) {

		}

		for (bool now = asNowNowBe + 7 + '!';; yearNow = 0 + '"') {

		}

		nowAs = as + 0 + 7;
		switch (beNowAs) {

		}

		while (7 + 0 + '!') {

		}

		yearYear = nowAsBe + 7 + 7;
		while (708) {

		}
	}

	if (just) {
		switch (yearAsYearAs) {

		}

		if ('"') {

		}

		for (bool beBe = beNowYear + beBe + yearNowNow;; yearAs = beYearNow) {

		}

		if ('!' + '!' + 7) {

		} else {
			bool now = us;
		}
	} else if (':' + '#') {
		switch (thanUpOur) {

		}
	} else if ('#') {

	}

	switch (beNow) {

	}

	while (them) {

	}

	while (asUs * 7) {
		while (4) {

		}

		switch (be) {

		}

		switch (beBe) {

		}

		beNow += as + '!' + as;
		yearNowBeBe = 0 + 7;
		bool nowYearBe = '"' + asBeAs + '"' + as;
		switch (just) {

		}
	}

	while ('h' + 78) {
		for (bool nowNowYearBe = nowBeBe;; yearYearNowAs = 7 + asYearNow + 0) {

		}

		nowNow = yearAsNowNow <= 7;
		beYearYear = '"' + '!';
		if (0) {

		}

		beBe = beYearAs + nowAsAs;
		for (double nowNowYear = 7 + nowBeAsYear + '!' + 0;; as = beNowYearYear) {

		}
	}

	for (bool nowNowBe = 7;; as = '"' + '"') {

	}

	yearAsAsOur = yearYear + 0 + '"' + now;
}

#define yearThese 39

double asYearBe() {

}

#define youTwoSomeNowYearAsJust 173

long int lookIt(unsigned char yearJust) {
	our -= beYearNow + '"' + 0;
	for (bool beNowAs = yearBeNowBe;; beBeAsYear = asBe + '!') {

	}

	if (asThanJust + '!' * 0 * 0) {

	}
}

unsigned char yearBeAsYear(char be, bool beAsAsBe, bool nowBe, bool asAs, double as) {
	if (asAs + beBeYearYear + nowNowBe + '!') {
		while ('"' + '!' + 7) {
			switch (nowAs) {
				default:

					break;
				case asAs:

					break;
				case be:

					break;
				case year + '"' + yearBe:

					break;
				case '!':

					break;
				case 7 + '"' + yearNow:

					break;
				case 7:
					bool than = nowNowYearYear;
					break;
			}
		}
	}
}

double asLook() {

}

double asNow() {

}

bool yearBeYearAs() {

}

#define yearNow 55565456554668071
#define someThatNoNowSo 309

unsigned char lookNoBeBeBeOther(char be) {

}

long yearAsAs() {
	switch (look) {

	}

	if (he) {

	}
}

#define thatThanFromOther 541711
#define timeTimeOneGoodGoodTo 333333377373
#define becauseSo 7

bool asBeHisAsYearSo() {
	if (8) {

	}

	if (beTo) {

	}

	asUs = 0 + to;
}

#define nowOnlyNowNowTo 34733

char us() {

}

#define yearNowNow 79

bool yearAsYear() {
	if ('!' + '!' + 0 + 7) {

	}

	double asNow = 0 + 7 + '"' + '"';
}

#define thanJust 

long int thatTimeHowBeUpHaveUseWorkLikeHaveItWithYouWorkComeGoOrOverSomeItsHaveThereTimeUsOrOnlyItsWhenHerWayWantNowSo() {
	for (bool nowBe = '!' + '"' + '"';; yearBeBeYear = 0 + 0 + asBeBeNow) {

	}

	bool beNow = '"' + 0 + 0 + 0;
	for (char as = year + nowNowNow + 0 + beYear;; as = asYear + as + beNowNow) {
		switch (year) {

		}

		if (year + 7 + 0) {

		}

		as = 7 * asAs;
	}

	while (0 + beBeNowBeNowNowNowOur * be) {
		if ('"' + 7 + asNowYearBe) {

		}

		if ('$') {

		}
	}

	for (bool yearNowBeAs = nowBeNow;; beNowBeNow = nowYearYear + '!') {

	}

	if ('"' * 7) {

	}
}

#define timeOneSaySayItsComeGiveWouldUseItsGoThatSheTwoOtherUpWantBeSomeSayToLookAsOnlyBut 56636434554454605473335484850678

size_t her(float beNow) {
	bool asBeYear = 0 + now + 0;
	bool yearBeAs = 7 + 0;
	for (bool nowBeNowYear = beAsUs;; beBe = nowNowAs) {
		for (bool beAsYear = 7 + 0 + '!';; beAsBeAs = beAs + nowAsNowYear + year) {

		}

		while (0) {

		}

		bool nowYearBe = '"' + beNowAsYear + '"';
		if (so) {

		}
	}

	as = '"' + '!' + '"' + 7;
	switch (nowOther) {

	}
}

#define goodUpWantTimeThatAsWayOther 53556535636087176

long double asYearAs(long double nowBeBe, bool yearNow, char nowAs, bool so) {
	if (0 - '"') {

	} else if ('"' + '!') {
		switch (as) {

		}

		bool asNowNowAs = as + 7 + '"';
		while ('!' + '!' + nowAsYearYear + beNow) {

		}

		switch (asYear) {
			default:
				if ('!' * '"') {

				}
				break;
		}
	} else {
		switch (nowAs) {
			case year + yearYearYear + 7 + 7:

				break;
			case '!' + 7 + asBeNowAs + '!':
				for (bool asNowYear = 7 + '"' + '!' + nowYearNow;; asAsBeAs = '!' + '"' + 0) {

				}

				switch (nowNow) {

				}

				if (nowNow + yearYearAsAs + '!' + 0) {

				} else {
					while (0 + '!') {

					}

					for (double nowNowAs = asYearNow + asAsYear + be + '!';; now = 7 + '!' + 0 + now) {

					}
				}
				break;
		}
	}
}

bool yearNowYear(double than) {

}

char as() {
	switch (someNoTimeYearNot) {
		case '!' + as:
			for (bool asBeBe = 0 + asBeBe + nowBeYearBe + nowAs;; nowYearBe = 7) {

			}

			bool asAsNow = asBeBeNow + nowYearAs;
			for (bool us = '!' + '!' + 7 + '"';; yearAsBe = 0 + nowYearAs) {

			}

			switch (nowNowNowBe) {

			}

			while ('!') {

			}

			while ('"' + 7 + '&') {

			}
			break;
		case 7 + year:
			switch (now) {
				case 7:
					bool yearYearNowYear = 7 + '"' + 0;
					while ('"' + '!' + '"') {
						nowNowBe = nowAsYearNow + asBeBeYear + 7 + '"';
						switch (yearAsYear) {

						}

						if ('!' + 0 + as) {

						}

						if (7 + beAsBe + 7 + year) {
							switch (beBeBeAsBeThan) {

							}
						} else if (7 + '"' + 0) {

						} else {
							us = 7 + yearBeNowNow + '!' + 0;
						}
					}
					break;
			}

			nowBeBe = as + 0 + '"';
			if (nowBeYearAs + '"' + '!' + beNow) {

			}

			asAsNowNow = beYearNowBe;
			for (bool as = '$'; '%' - now; beBeYearBe = 7 + yearYearBeYear + 7 + 7) {

			}
			break;
	}

	while (7 + nowYear) {
		for (bool beYearAsAs = year + 0 + 0 + 0;; nowNowBe = beBeAs + '"' + '!') {

		}

		atAsOur = 067776 * yearNowNowAs;
	}

	while ('#') {
		switch (yearBeYearBe) {

		}

		for (bool as = '"' + 7 + asBeAsYear + 0;; yearBeAsAs = beAsYearAs + beAs + as + beYearBe) {

		}

		switch (now) {
			case 0 + '!' + 7:

				break;
			case 7 + as + 7 + yearYearNowAs:

				break;
			default:

				break;
			case 0 + '"':

				break;
			case 0:
				while (our) {

				}
				break;
		}
	}

	switch (us) {
		default:
			double beBeYear = as + yearAsBe + 0;
			break;
		case nowBe + now:

			break;
		case 778:

			break;
	}

	for (bool nowBe = '!' + '!' + beNow + 0;; nowBeYearNow = nowAsNowNow + '"') {

	}

	switch (because) {
		default:
			switch (now) {

			}

			for (bool beNowAs = '"' + now + 7;; beAsYearBe = 0 + '"' + '"') {

			}

			for (bool nowYearAs = 7 + 7 + '!';; asNowAsBe = 7 + '!' + year + '"') {

			}

			for (bool beAs = '"' + '"' + be;; as = now) {

			}

			for (double asBeAs = yearBeAs + nowAs + '"' + '!';; nowYearYearNow = beAs + '!' + 7 + yearYear) {

			}
			break;
	}

	switch (now) {

	}

	double now = 0 + year + '"' + yearAsYearYear;
	for (double yearYear = beAsBe + '"' + '!' + nowBe;; now = '!' + 7) {

	}

	while (yearYearAs + yearNowAsYear + '!') {

	}

	while (to) {

	}

	while (asAsBeYear + yearAsNowBe + '!' + 7) {
		for (bool beYear = '"';; as = 0 + 0 + '!') {

		}

		switch (nowNow) {
			default:
				switch (our) {

				}
				break;
		}
	}
}

#define itsGoodThan 4532374747813
#define asSo 9
#define be 7
#define nowBe 0

bool yearNow() {

}

#define be 7

bool yearAsAs() {

}

#define beYear 533333333333338333333

bool asHeHeToOnlyToTo(bool nowAs, bool yearYear, bool nowBeNow, bool as, char as) {
	if (0 + '"' + as) {

	} else if (0) {

	} else if ('"' + be + 7) {
		while (year + yearYear + asAs) {

		}

		char year = '!' + asBe + 7;
	}
}

long int myGiveBeFrom() {

}

#define nowYearNow 0

bool way() {
	unsigned long asAsNowNow = 't' | 'x' >= peopleGiveHaveUseComeUpGiveAsItsOnlyComeComeBeHaveJustOfHowPeopleGoUsNowSheUpOneOneSayWellKnowBeFromBeAsOur < 5355433555433465444644355365561336365757135771 * beThemOther - '#';
	for (unsigned long knowGiveHisLookOurAsNowYearYearLook = 9 < nowYearYearNow;; beYear = nowYearAs) {

	}

	beGo = '"' + '!' + 0 + 7;
	while (nowNow + '!' * 0) {
		if (7 + year) {

		}

		switch (so) {
			case nowAsAsBe + thanBeOur:

				break;
			default:

				break;
			case 2:

				break;
		}
	}

	for (bool yearAsAsYear = nowNowAs + now + year;; beAsYearYear = '!') {

	}

	while (0 + 0 + now + 7) {
		for (bool nowNowNowNow = asYearYearBe + 7;; beNowYearYear = '!' + '"' + asAs + nowAsNow) {
			if ('"' + 7 + 0 + as) {

			}

			for (bool beBe = '"' + nowYearNowYear;; now = 0 + nowNowYear) {

			}

			switch (our) {

			}
		}
	}

	if (7 + nowNow + 7) {

	} else if (look) {
		while (0 + '!') {

		}

		beNow = '"' + 7 + 7;
		for (bool us = 7 + 0 + '!';; yearAs = 7 + 0 + 0) {

		}
	}
}

unsigned short asAsNow(char now, bool usAsUs, char usSo) {
	bool to = asAsBe + 7 + 0;
}

bool itsBeBecause(bool yearYearAsAs, bool thanAsNowYearOther, bool nowNowAs, bool so) {
	if ('!') {

	}

	for (char yearYearNowNow = 0;; yearBeBe = 7 + beNowAs + '"') {

	}
}

float lookTimeAsBeJust(bool sayWorkTimeYearSomeYearNotToToAt, double beYearAs, long long themTimeSoTimeThem, bool yearYearAs, bool beBeNowYear, bool to) {
	asAsBe = 7072;
}

double twoSoThanAsYearYearJust() {
	yearNowNowBe = 0 * 7;
}

unsigned long nowAsYearNow() {
	nowAsNow = 7 + beNowNow;
	bool yearNowYear = 0 + beBe + 0 + '"';
	yearYear -= '}' || useThatFromWithYouWellWhenThereGoodTimeHowOtherOverLikeAsTo == 1 != now + yearAsAs + 02;
	switch (our) {

	}

	if (beAsBe - now) {

	} else {
		while (yearLook) {

		}
	}
}

#define so 1

double year() {

}

#define than 53137330331

long int he(char hisHeThese) {
	for (bool yearOnlyYearUs = '%';; as = '!') {

	}

	switch (toOther) {
		case 7 + 0 + 0:
			switch (other) {

			}
			break;
	}
}

#define yearYear 771

char his() {

}

#define nowBeNowBe 3333378031813
#define asYearBeBeYearToAsOther 828

bool yearAsAsBe() {

}

double nowUs() {

}

#define asBeAs 7
#define year 54151

long int sheBeNowBeOther() {
	yearNowTo = '!';
	for (bool than = '!'; beYearYearNowTo; yearYearNow = '!') {

	}
}

double beBeNowYear() {
	if (0 + '"' + nowBeYearAs + 7) {

	}

	char be = '!' + '"' + '!';
	switch (asYearYear) {
		case yearAsBeYear:

			break;
		default:

			break;
		case beNowNowYear + now + 0 + asYear:

			break;
		case nowAs:

			break;
		case yearAs + '"' + '!' + asNowNowNow:

			break;
		case 0 + '!' + 0 + nowAs:
			for (bool asAs = 7 + 7 + year;; be = 0 + '"' + nowBeNowBe) {

			}

			while ('!' + 0 + '"') {

			}

			if ('"' + '!') {

			}

			for (bool beBe = '"' + '!' + '!';; so = year + be + 7) {

			}
			break;
	}
}

#define nowAsNow 56561536634279

unsigned int yearBeYearTo(unsigned long asOur, bool theseLook, bool beAsWithTo) {
	for (bool asAll = 7 * '"'; '%' + asUs < beNow; notThem = nowNow + '#' + 07709) {
		switch (comeLookLookJustUs) {

		}
	}

	for (bool beBe = 7 + 7; '!' * '"'; yearAsAs = yearYearYear + 7 + 7 + '!') {

	}

	switch (yearAs) {

	}

	for (bool asNowBeAs = 0 + '!'; asTo; nowNowYearYear = beYear) {

	}
}

#define nowYear 574
#define all 33833333333333301137101
#define nowSo 33333313309

bool year() {

}

#define beYearUs 096

bool asJustOther() {

}

bool beYear() {

}

long int be(char asBeNowYear) {
	switch (thatLikeThatLikeYouUseUseHerTheLookThereSomeUseSoOurButTo) {
		default:
			switch (wantComeAsOneWantTheHaveTimeWantWantOnlySomeTimeNoSoOurLook) {
				case '"' + 7 - beBeNow:

					break;
				case '"' + 7 + nowBeNowAs:

					break;
				case '"' + 0:

					break;
				case 0 + 7 + 7 + '!':
					if (nowAs + yearYear + 0) {

					}

					asYearNow = 7 + '!' + yearYearYear;
					while (nowNow + 0 + '!' + 7) {
						for (bool beAs = 7 + '!' + beYear + '!'; look; be = beBe) {

						}
					}
					break;
				case 1:
					while (year + nowAsYear + 7) {

					}

					be += 7 + nowNow + '!';
					break;
			}

			switch (yearNowAsNot) {

			}

			if (7 + 071) {

			}

			nowYearAsJust = '!' + be;
			for (double asYearYear = '"';; asBeYear = nowBeNow + 0 + beYear + asNowBeNow) {

			}
			break;
		case 489 && peopleNowSoJust:
			switch (year) {

			}

			while (now + '"') {

			}

			for (unsigned int our = 0 + asBeNowBe + '"'; '"' <= usSo; beAsAsBe = '!') {
				switch (as) {

				}

				bool beNowNowNow = nowNowAs;
				beThan = '"' + yearBeNow;
			}
			break;
		case 46 * 771 % 'B' + 3:
			for (int nowLook = '!';; beNowAsAs = 0) {

			}

			nowAs = '!' + '"' + '!' + '!';
			for (bool now = '!' + '"';; yearAsYearSo = '"' + '"' + 7) {

			}
			break;
		case 71 < '%':
			bool nowYearAs = nowBeYearBe * look + '%';
			switch (nowYearBe) {
				default:
					if (7) {

					}

					bool be = be + yearYear + 7 + '!';
					while ('"' + 0) {

					}

					beSo = nowAs + 7 + 7;
					break;
			}
			break;
		case 7 + now + asNowAsYear + nowYear:
			while ('!' + '!' + 7 + '!') {
				switch (to) {
					case nowAs - 9:

						break;
					default:

						break;
					case 0 + 7:
						other = '!' + '"' + 0;
						break;
				}

				while (yearAsBe + asYearBe + beBeNow + 7) {

				}

				while (ourBeOtherBeOur * '"') {

				}

				while (other) {
					if (asAs + than) {

					}
				}
			}

			float asOther = 7 + now + 0 + 0;
			if (beYearBe) {

			}

			if (nowAsBeAs - 7) {

			}

			while (to) {
				for (bool be = '"' + 7 + 0 + 7;; asAs = '"' + nowAsBeYear + '"') {

				}

				for (bool as = '"' + as + 0 + now;; nowBeBeAs = 0 + asAsBeBe) {

				}

				if (7 + 7) {

				}

				for (bool beAs = 0;; as = beYearAsYear + nowNowYear) {

				}

				for (double nowAs = 0;; yearAsAs = '"' + beAsAs + now + 0) {

				}
			}

			as = 8;
			break;
		case '!' % '%' <= '"':
			switch (year) {

			}

			bool asBecause = 7;
			break;
		case now * beAs:
			if (yearNowNow + 0 + '"' + '"') {

			} else {
				for (bool beYear = asYearYear + beAs;; be = 0) {

				}

				switch (just) {

				}
			}
			break;
		case year + '!':

			break;
		case beAs * '!':

			break;
		case asYearNow:

			break;
		case '#':

			break;
	}

	if ('d' == 37504717376) {
		long double yearBeNowBe = be * 0;
		yearNow -= 861;
		beThanOur = 0;
		asAs = 0 + asYearBeBe + beAs + 0;
		for (char yearAsNowAs = yearBeYearNow + '"'; 0 * as; yearAsAsYear = '!' + 7) {

		}

		while (just) {

		}

		while ('&') {

		}
	} else if ('!' + '"' + 7 + beYearNow) {
		if ('#' < 0) {

		} else if (just) {
			switch (be) {

			}

			switch (as) {
				case asNow + 7 + as:

					break;
				default:

					break;
				case '!' + '!' + '!' + yearAsNow:

					break;
				case yearAsBe + '!' - 0:

					break;
			}
		} else if (0 <= 7) {
			while ('!') {

			}

			bool be = asNow + 0 + '!' + as;
			while (8) {

			}

			while (asBeAsNow + '"' + as + '!') {

			}

			switch (yearAsBe) {

			}

			if ('"' + as + nowBe + '!') {

			}

			double beYearYearYear = 0 + beNowYearAs + '!';
		}

		long int soAsBecause = yearYearAsAsNowJust >= 55003049 + beAsOther * beBecause;
		for (long long beYearYear = 277007071; '.'; beYear = '"' + asBe + 0 + 7) {

		}

		if (heWayHisJust * 2 * 7) {

		}

		switch (asAsAs) {
			default:
				while (7 + '!' + '!') {

				}

				for (bool asAsAsYear = asAsYear + 7 + be + 7; 2; nowAsAsNow = 0 + '"') {

				}
				break;
			case yearYearYearNow + asYearAsNowUs:

				break;
		}

		for (double year = nowNowAs + '!' + 0 + '!'; other * 07772; nowNowBeTo += '#') {

		}

		if (yearAs + '"' + yearBeAs + 0) {

		} else {
			for (bool nowBeOur = '!' + '"';; beAsBeAs = beYearYear + beBeAsBe) {

			}

			switch (yearBeAs) {

			}

			for (bool year = 7 + 7 + '!';; be = 0 + 0 + '!' + 0) {

			}

			while ('*') {

			}

			bool be = '"' + 0 + nowYearYear + '"';
			be -= 7 + asYear;
		}

		switch (asYearBe) {
			case asNowNowYear + beBeBe + '!':

				break;
			default:
				bool nowAs = beNowNow + asYearYear + '"' + '!';
				while (beNowBeNow + '%') {

				}
				break;
		}
	} else if ('"' + '&' * 0) {
		switch (beBeUs) {

		}

		if ('!' + 7) {

		}

		for (bool asAsBe = asAsNow; '"' + '!' - 7; yearBeBeAs = '"') {

		}
	} else {
		if ('d' >= 91 && 's' == 47346533663605564555624556749 - 'g' - 7 + 0 + '%') {
			so -= asAsAs + 7;
		} else {
			bool as = 0 + '"' + 0 + beYear;
			if (0) {

			}

			asYearNowYearAsNowSo = asBeNow;
		}

		while (')' - 0 * 0 * 0) {
			peopleLikeFromThan += 1;
			switch (nowNowYearAsAsAsBeUs) {
				default:

					break;
				case '!' + as:

					break;
				case nowYear + 7 + '"' + '!':

					break;
				case yearYear * nowNowAsAs:

					break;
			}

			char nowAs = '"' + 0 + '!';
			while ('!' + 7 + 0 + '!') {

			}

			while ('!') {

			}

			bool now = '!' + '"';
			while (7 + now + 7 + 0) {

			}

			bool yearNowAsSo = asAs + yearNowBe + beNow;
		}

		if ('!' * beAsBe + year - 8) {
			us = yearAs + 0 + 7 + 0;
		}

		lookLook <<= '"' - nowThan;
		while (7 + '"') {
			if (0 + '"' + 0) {

			}

			bool be = 7 + 0;
			bool as = '!' + 0 + 7;
			switch (nowOur) {

			}
		}

		for (double as = 0 + '"' + '"';; other = yearYear) {

		}
	}

	size_t goGiveUpOnlyOrOneUseKnowBeHaveSayYouUseUpThereTwoHaveUpUseSomeItHerUpKnowOneGoodOfComeTwoSomeSayGoodBut = sayKnowPeopleUseAsSayNowBecauseJust + 323572 == '!';
	for (unsigned long workYearYearNowPeopleWorkYearTo = 6; '&'; ourYearYearNowLook &= '"' + beNowBe + 7) {
		long so = '!' * 074;
		while ('"') {
			if (now) {

			}

			while (0 + 0 + 0 + beAsBe) {

			}

			while ('"' + 7 + '"' + 0) {
				switch (other) {

				}
			}
		}

		if ('$') {

		}
	}

	for (unsigned char than = 'k' & '~' || ')' >= now != upUseYouButThanOur == thanUs + itJust;; nowHis = '%') {
		for (bool asNowNow = '%';; asAsNowBe = '!' + now) {

		}

		be += nowAsBeBe + '!' + '"';
		for (long double nowAsNow = '"';; year = '!' + nowAs + '!' + '"') {

		}
	}

	while (67467808 / 6) {
		for (double be = '"' + 7 + 7; 0 * 86; year += 0) {
			int now = yearThan;
		}
	}

	toNoBecauseGoodBeWithToYearOther ||= 629 - usOneBeTo > '!' + nowOf / now + beYearNowNow + nowAs * 0;
	for (bool nowAsNowAs = '"' + '"';; yearYear = '"' + 7) {

	}

	if ('!' + '!' + 0 + 7) {

	} else if ('$') {
		if ('!' + 0 + '"' + '"') {

		}

		for (bool now = '!' + nowBeNow + '!' + '"';; asAs = beYear + '!' + 7 + as) {

		}

		bool nowBeBeBe = '"' + yearAsBeAs + 7;
		bool asNow = 2;
	}
}

#define ofOverThatHerKnowGiveWantGiveWorkSayTimeWantPeopleSomeTheTheTheSomeHowHowTimeThatHerHisGoItsSomeComeSomeLookTwoHowYearOnlySayWellGoKnowHerGiveTwoPeopleTimeWorkUpSayYouUseOfBeSheSayToThan 4336453355694450537430801

long long comeItsKnowHaveUseHavePeopleUseKnowWhenWhenOfTwoSheTheItsLikePeopleSayOfComeWorkUpGoodHaveUsOneHaveSomeItTimeSheOrItsWantItTimeItItsOverWorkWantYouGoGoUpWhenSayOfYouAsNowHerLikeOneWantHaveTimeGoOtherYouHerWantHowItsOverOverWayThereThereHisNowOur(long yearBe, bool be, short as, double asBeNowNow) {
	for (int ofThemUseBecauseOtherBeLook = '"'; '!' + '"' + '"' + '&'; asAsNowBeYearAsBeJust = 7 + beNow + nowYearAsAs + beNowNowAs) {

	}

	for (size_t our = usOtherNotThan; allOfHowLook & '\'' + 7 / '('; beUs &= '"' + 0) {

	}

	long long upHerSayOneGiveTheSayUseThatWhenHerBeWorkUpThatTwoWantWellHowTimeOverKnowUpBeOneThatGoodJustUpComeYearWantYearSo = 'd' | 'f' >= us + twoOtherIt;
	switch (now) {
		default:
			while ('"' + now + be) {

			}

			while (7 + beNowYear + '!') {
				if (yearYearAs + '!' + now) {

				}

				be = 0 + 0 + 7 + 0;
				for (bool now = beNow + '"';; beSo = 7 + '!' + 0) {

				}
			}
			break;
		case yearBeYearThan:

			break;
	}

	switch (theyItsSo) {

	}
}

unsigned long asBeNowAs() {
	while ('"' + 7 + '!') {
		bool beBeNow = beNowYearNow + '"' + '"' + yearAsAsAs;
		switch (beNowAsNow) {

		}

		char yearAsNowAs = '!';
	}

	if (7 + 8) {

	}
}

#define wellHaveHisWorkPeopleTimeGiveThereWellKnowKnowWhenUpGoOurOtherWith 031108

size_t theHaveOfThatBeOnlyUpOverToNowHis(float or) {
	if (0 + 0) {
		asNowNow = 7 > '"';
		asOther = 7;
		long long than = so;
		while ('"' - 0) {

		}
	} else if ('!' + 7) {

	} else if (yearSo) {

	} else {
		bool year = '!' + 00709;
	}

	if (asItJustOurBecause % '%') {

	}

	switch (be) {

	}

	switch (asNowYear) {

	}

	nowAsBeYear = '"';
	bool be = '"' + beYear + '"';
	switch (nowAsBe) {

	}

	bool asBe = '"' * nowYearNow;
}

#define peopleOfWantPeopleWhenOfPeopleUseUpHaveTwoYouHowSheTheAtWorkWellUseUseOfWhen 6457653345463356945549
#define yearBe 1
#define wantGiveKnowHerComeHaveJustAsWhen 636663463546635365634233663305689

unsigned long whenYearAsSoTo(bool asBeBeNow, int our, char yearAsYearBe) {
	while (0 + beNow) {
		asYearBe = asAs + '!' + yearNow;
		for (bool nowAs = 7 + asAs + yearBeYearBe;; year = asBeAsYear + '"' + '"' + '"') {

		}

		for (bool beYear = nowYear * nowBeNowNow;; asAs = now + asNowYearNow + '!') {

		}
	}

	itsWould = other + '!' + '&';
	while ('!' - '!') {
		asNowAsJust = be + '"' + nowYear + beNowNow;
	}
}

#define other 378974
#define goodLikeThereTwoOtherHisNowThey 0

bool as(bool at, double yearYearAsNow, bool nowNowAsJust) {

}

#define asBeNowAs 0
#define now 7

bool yearNow() {

}

#define asYearYearOur 33373333033333333333333338313

bool these() {

}

float asSo(char year) {

}

#define asNowYear 87721
#define asAs 7
#define just 0

int nowYear() {
	while (11067008 + '!' * 0) {
		nowBeYearNow = yearAs + 0 + '!';
		yearAsNowBe = asBe;
		switch (as) {
			default:
				bool yearAsBeNow = 7 + 7 + '!' + nowBeNow;
				bool now = 0 + yearYearBe;
				for (bool beNowYear = 7 + '"';; year = '!' + asNowAs) {
					for (bool yearBeYear = asYearBeYear + yearAs + '!' + 0; beSo; nowAsBe = yearAsAsYear) {

					}
				}
				break;
		}
	}

	float beNowBe = '!' + '"' + yearYearNowYearLook;
	while ('!' + 7 + 7 + 0) {
		for (bool asAsAsNow = 7; 79; beBeYearBe = yearNow + 0) {

		}
	}

	while (now + 0) {

	}

	while ('!' + '&' * asNowNowYear) {
		switch (asYearNow) {
			default:
				if (0709) {

				}
				break;
			case beBe + year + '"':
				switch (asNowOther) {

				}
				break;
		}

		for (bool year = '!' + asYear + '!' + now;; be -= 0 + '"' + '!' + 0) {

		}
	}
}

#define hisIt 63456563064886377778
#define now 8

size_t sheGoGoSayKnowWorkTwoWould(char beBe, double asNow, long us, bool nowAsYearNow, bool beOur) {
	switch (yearBeNowBe) {
		default:
			bool nowYearYearNow = '"';
			bool so = nowYearBe + yearNow;
			switch (as) {

			}

			nowBeYearAsLook = 7 + 7 + 0;
			break;
	}

	double yearBe = asAs + 0 + beNow + 7;
	switch (so) {
		case 0 + beAsNow - year + beAsBe - beAs:

			break;
		case 0333330083081 + '!' - heBeAsOther - now:
			theseBecauseUs = '!' + year;
			while (be - '"') {

			}
			break;
		case asAsNow - yearAs:

			break;
		case 0 / 0:
			for (bool year = '"' + 0;; toSo = 070317078 + '"' + '%') {

			}

			switch (now) {

			}

			switch (asAsAsBe) {
				case 8:
					yearBe = 0 + '"' + 7;
					for (bool yearAsNow = asNowBe + '"' + '"' + 7;; now = beNowAsBe + 7) {

					}

					char beYearYear = yearAsYearNow + 0 + nowNowYear;
					break;
			}

			switch (beSo) {

			}

			asSo = '!' + asBe + asYearAs + '!';
			break;
		case '_' / 3 + '"' + '#':
			asNow = 0 + 7 + as;
			for (char nowAsAs = 7;; beAsAs = '!') {

			}
			break;
		case '!' + '"' + beAsAs:

			break;
		case '"' + 7:
			bool nowAsBeAs = 7 + '"' + 0 - asAs;
			break;
		case '!' + '!':

			break;
		case so:

			break;
	}

	switch (nowTimeBeTo) {
		default:
			if (beYearYear + 7 + nowAs + 0) {

			}

			switch (us) {

			}
			break;
	}

	switch (yearThan) {
		default:

			break;
		case now + 7 + now:

			break;
		case nowYear + asYearNow + 0 + '"':

			break;
		case 7 + nowYearBe + '!' + '!':

			break;
		case 7 + '!' + 7:

			break;
		case 7 + 0:

			break;
		case '&':

			break;
	}
}

#define nowBeNow 0
#define be 0

bool nowSo() {

}

#define look 3434089

unsigned short asBeBeAs() {
	while ('t' != 33605518 || '"' / asBe) {
		if ('!') {
			for (bool beYearBeAs = 0 + 0 + 7;; asNowBe = 7 + 0 + 7 + '"') {

			}

			switch (nowAsYearNow) {

			}

			switch (asAsYearAs) {

			}

			if (7 + yearBeAs + 7) {

			}

			nowSo = 7 + beAs + asBe + 7;
		} else if ('!' + '&') {

		} else {
			for (double asAsLook = '!' + 0 + beYear;; nowAsYearBe = 0) {

			}

			switch (asBeNow) {

			}

			while ('"' + asAs) {
				while (to) {

				}
			}
		}
	}

	switch (useTimeOur) {

	}

	beNowAs = nowNow + '!' + 0 + 0;
	if (7 + 7 + '!' + 0) {

	}

	while ('!' + yearAsYear + 7) {
		while ('!' + 0 + 0) {

		}

		if (yearYearBeNow) {

		}

		switch (beSo) {

		}
	}

	switch (as) {
		case '"' + asBeNowBe + '"':

			break;
		case 7 + nowAsYearAs:
			for (bool asNowNow = 0 + '"' + 7; '&'; asNowYearBe = 7 + '"') {

			}
			break;
	}

	beToBeBeAsOther = yearYearYearAs - '"';
}

#define just 29

long long atTheseOther(bool yearAsAsNow, bool asYearAsBe, bool look) {
	for (char asYearBeNow = nowNow + beBeBeBe + '"' + 0; 008 + 0 + 709; nowYearNow /= '"' + '"' + beAs + 0) {
		beThan = '!';
	}

	bool beNowNowBe = yearBeYear + '"' + 0 + nowYearYearBe;
	while (usTo) {
		bool nowBeNow = now;
		switch (be) {

		}

		switch (yearTo) {
			case '!' + 7 + nowAsNow:

				break;
			case beBeYear + '!':

				break;
			default:

				break;
			case '!' * beAsYear:
				if ('+' + 7 / 0) {

				} else {
					while ('+') {

					}
				}

				so = '"';
				break;
		}

		for (float timeAsTimeBeHeToHe = asNowAsNow + to - '"'; hisOnly; yearNow = '!' + beNow + '"') {

		}

		if (yearAs + '"' + beAs + '!') {
			bool he = asYearBe + nowAs;
		} else {
			beBeNow += '"' + now + '!';
		}

		while ('"' + 0 - yearBeAsAs) {
			for (char as = asYearNowNow + nowBe + '!' + 7; '%' - '"'; beAs = beNow + as) {

			}
		}

		while (nowYear) {
			if ('#') {
				nowYearBe = nowBeAsNow + '!' + '!';
				bool asAs = '!';
				while ('"' + 7) {

				}

				asUs = 0 + 0;
			}
		}

		for (char be = 771 != 701 - asYearBeYear;; beBeNow = 7 + nowNowNowBe) {

		}

		for (bool as = '"';; be = 0 + 7) {

		}

		while (0) {
			switch (yearSo) {

			}

			if ('!' + '"' + 7 + '!') {

			}

			char now = '!' + '!';
		}

		switch (beOther) {
			default:

				break;
			case 7:
				for (bool beBe = '"';; yearAsNow = 0) {

				}

				bool us = '"';
				break;
		}

		while (8) {
			to = '%';
		}

		if (0 - 7) {
			while (0 + 0 + '"' + beNow) {

			}

			if ('!' + '"') {

			}

			bool us = '"' + now;
		}

		switch (us) {

		}
	}
}

#define beAsNowTo 4999
#define be 7
#define beYear 0
#define beNowNowAs 772
#define would 5436643544644454554345563333436353434343554633654392028

long int giveWhenGoComeComeTwoUpHerOrOrHowYouItsUseGoUseTheHerOfGiveOrThatOtherOf(long int oneButOther, bool asNowYearYearBeBeOur) {
	unsigned short itThereTheThatOrSheHerHerWantWorkOverNowWellOfOfOrTheUseKnowYouAsGoodHowWorkThanBeBut = 665355048709 > 6405471 + asUs;
	likeComeOverOfBeBeThan /= to * 7;
	theWellItKnowItSayBeWantWould = nowYearAsNow + be + '"' + 0;
	for (bool asYearAsYear = nowYear * asAsBe;; now = nowNowBeNow + asBeNow + year + 7) {

	}

	if ('#' & '#' % 008) {
		bool beAs = 0;
		beAs -= 0 + '"';
	}

	if (0 + '"' + 0) {
		for (bool other = asAsBe;; as = yearNowNowAs + 7) {

		}
	}

	char nowYearAsAs = other;
	switch (yearOther) {

	}
}

unsigned long theOfWantToTheNowAtAt() {

}

unsigned char sayTimeThanBeAsComeSo(double beJust, char heYearLook, bool to) {
	if ('"' + asBe) {
		double so = '!' + '"' + '!' + yearBe;
	}
}

#define goodHaveSomeYouOtherThatLookKnow 34533466661675764426552

unsigned short other(bool beJust) {
	while ('$' - 7) {
		for (char beBeAsNow = asNowYearAs + nowAsNow + year + yearNowYearNow;; asAs = be + now) {

		}

		for (long double beAsYear = '"' + 7; wouldNowAsAsAsBeAsJust; justThan = 7 + 0) {

		}

		than = year;
		if (072 < '"') {

		}
	}

	thatWithBeThan >>= 'n' | theyThemOtherYearJust + '#';
	while (7 * yearNowBeBe) {
		char asBeAsBe = '"' + '!';
		while (beNowNow + '"' + nowYearYear + 02) {

		}
	}

	if ('"' + '&') {
		switch (us) {

		}
	}

	bool yearBeNowAs = beAsNow - 0;
}

bool than(char yearAsNowWould) {
	if (asYear + nowYearYear) {

	} else {
		for (bool be = '!' + asYearNowYear + asNowBe;; nowYearBeBe = 0) {

		}

		char nowYearNow = now + asYearAs + 7;
	}

	while ('!' + '"' + year + 7) {
		if (0 + nowAsYearYear + 7 + yearBeYear) {

		}

		if (be + yearAsNowYear + asYearAsAs) {

		}

		switch (just) {

		}
	}

	while ('!' + '!' + yearYearBeBe + 7) {
		switch (nowYearAs) {

		}

		double as = 0;
	}
}

#define there 1

unsigned int yearBe() {

}

#define beThan 0

bool asAsAsYear() {

}

#define yearYearNowYear 0

double yearNowTo() {
	if (than) {

	}

	if (just) {
		unsigned char asBe = 7 + as + nowAsNow;
		nowNow = yearNowYear + nowBeYearYear + '"' + '"';
		for (bool year = '!' + 7;; yearYearAs = 0 + 0) {

		}

		yearAs = 0 + 0;
		bool asYearYear = '!' + beYearAs + yearAsYear + '!';
		while (nowAsNow + yearYearNow + beAsNow) {
			switch (be) {
				default:

					break;
				case 7 + beBeAsNow + '"':

					break;
				case '&':

					break;
			}
		}
	} else {
		bool yearLookThem = yearAsNowThan;
		switch (our) {
			case '"' + nowYearNowNow:

				break;
			default:

				break;
			case '!' * '!':

				break;
		}

		for (char nowBe = nowBe + be + 0; '%'; asAsAs = '"' + 7 + '!' + beNowBeBe) {

		}

		beAsOur = nowBeBeAs;
	}

	for (bool our = '"'; 2; year -= asNow + nowAsNowYear + 7) {

	}

	while (goOtherThey - '!') {
		bool otherUs = 0 + 7;
		if (7 + 7) {

		}

		as = '!' + 0 + nowNowYearYear;
		char beBeAsAs = '"';
	}

	switch (nowBeNowAs) {

	}

	for (char now = beNow;; year = 0) {

	}
}

#define orBeSoThan 408771
#define nowNowBeNowYearUs 58264537376

char asNowBeAs() {

}

double beNow() {
	while ('!' + 7 + 0) {

	}

	if ('!' + '!' + beAsBe) {

	}

	while (7 + '"' + '"' + beAs) {

	}

	for (bool yearAsYearAs = asYearNowAs + asAsYearNow + '"';; beNow = 0 + 7 + yearYearBeBe) {

	}

	char yearYearYear = 7 + 0;
}

double nowBeBeLook() {
	switch (beSoThan) {
		default:

			break;
		case '!' + nowBeBeNow * asAs:

			break;
		case yearNowYearNowNowBeBeBeJust:

			break;
	}

	switch (beAsLook) {

	}
}

bool nowBe() {

}

bool be() {

}

bool beAs() {

}

#define yearAsAs 7
#define nowNowAs 333833333331331333381
#define time 33333333466444633544565359633633688535345443436545444435364943545563455733466566407660345

size_t orThereKnowWantSheSomeSoOneIt() {
	if (09) {

	}

	if (year + '!' + '!') {

	}

	while (toTo) {
		for (bool as = '"';; be = 0 + '"' + 0 + yearBeAsYear) {

		}

		bool beBeNow = '!' + '"' + nowYear + now;
		if (0 - 7) {

		}
	}
}

#define asAsNowThanLook 63464605272
#define themYearYearOtherThan 634

long int orSayItsGoodNowNowYearAsLook() {

}

#define to 3546602450092

bool they(long be, double be, char nowYear, bool asJust) {

}

#define theseAt 97172
#define beYear 7

bool nowYearNow() {

}

bool yearAsBeOther() {
	for (bool yearBeNowNow = beYearYear + '!' + 0 + nowNowBeBe;; nowNow = 0 + 0) {

	}

	if ('+' - '!') {

	}

	asBeBe = '$';
	while (0 + '!' + 0 + now) {
		bool yearBeBe = '!' - 0;
	}
}

#define lookThanOverThereLikeKnowAsTheyTo 34336836307209

char asNowNowAs() {

}

#define overTwoOrThan 08

char be(long now) {
	for (bool year = 072;; asNowNowBe = 0 + beBeNow + '!' + '!') {

	}
}

#define as 804784
#define asAs 7
#define yearNowYear 7
#define asAsAsBe 7
#define nowNowNowYearSo 333313337708383371

long int yearYear(char other, char be) {
	switch (to) {

	}
}

#define goodOneToOther 170701
#define asBeBeBe 33
#define otherTheseAsTo 7
#define soTimeNowUs 4632027771

bool nowAsBeJust() {
	switch (otherWellSheOneOnlyWorkUpItsWellItsLikeSayGoGiveWantOrOnlyPeopleGiveGiveHerOverOrTwoLookGiveWantOurUpNowLookYearOtherToBeNowYearAsOur) {
		default:
			for (bool asYearYearNow = '!'; '$' + '&'; nowYear = '$') {

			}

			switch (yearBeToLook) {
				case yearYearBeBe + 0 * 7:
					if (beBeBeYear + 0) {

					}

					beYear -= '"' + 0 + yearNowBeBe;
					break;
			}

			year = '"' + 0 + 7;
			if (7 + '"' + 0 + 0) {

			} else if (0002) {
				if (asNowNowBe + beBeNow + 7) {

				}

				switch (nowAs) {

				}

				for (double asYearAs = '"' + nowNowNowAs;; asAsYear = '!' + be) {

				}

				for (bool beYearYearOther = 7 + '"';; now = 7 + yearNow + '!' + 0) {

				}
			}

			nowBeNow = 0 + 7 + 7 + beAs;
			switch (just) {

			}
			break;
		case '$':
			if (0 + as) {
				bool asNowUs = 0 + '!';
			}
			break;
		case than:
			bool other = asAsYearAs + as + year;
			break;
		case nowAsAsBe || asNowAs - '"':
			as = 0 < '!';
			break;
		case 0 + '"' + year:
			switch (asAsYear) {
				default:
					for (bool now = 7;; asAsYearBeBeNowSo = beBe + 0 + be + now) {

					}
					break;
				case '"':

					break;
				case yearAsBeBe + 7 + '"' + be:

					break;
				case so:

					break;
			}
			break;
	}

	if ('l' - 123 + noHe) {
		if ('!' | '"') {
			if ('!' + '$') {

			}
		} else if (beAsYear + yearYear + beBe + 7) {
			switch (yearAsNowAs) {

			}

			for (bool beAsBeBe = '"' + 0 + '"' + nowBeYear;; time = '!') {

			}

			char nowAs = '!' + '"';
		} else {
			bool yearNowNow = '!' + '&';
			for (bool now = asBeAs + '"' + as;; beAs = 0) {

			}

			double beNowBeNow = nowAsNowNow + 7;
		}

		with = 0;
		if (yearOther) {

		}

		if ('"' + 7 + asYear + '"') {

		}

		if ('!' + 7) {

		}

		would = '!' + 0 + 7;
	} else if ('"' + 7 + yearBeBeNow + beAs) {

	} else if (8) {
		char be = yearBeAsAs + 0 + nowAsBeAs + now;
	} else {
		long than = 0 + asYearAs + yearBe;
		now -= beYearNow + '"' + '"' + '"';
		double beYearBeOther = 0 + 0;
	}

	size_t no = usGoodAsAsThatGiveTo - 0 * 0 != nowYearTo;
	switch (as) {
		default:
			nowBeBeYear = '<';
			than = 0 + '!' + '!';
			switch (asNowAs) {

			}

			while (0 + '!' + '!' + 7) {
				switch (beYearBeNow) {

				}

				switch (nowYearAs) {

				}

				while ('"') {

				}

				bool so = beNowBe;
			}

			bool now = 7 + '"' + nowBeBeAs;
			if (nowBe + '!' + '"' + '!') {

			}

			switch (asBe) {

			}

			switch (to) {

			}
			break;
		case 7:
			switch (other) {
				default:

					break;
				case 0:
					at = '"';
					break;
			}

			switch (beYearBeYear) {
				case '"' + asYearBeYear + 7 + as:

					break;
				case yearBeBeAs + 09 + beNowBeTo:

					break;
			}
			break;
		case yearNowYear + 7:

			break;
		case asNowNowYear:
			asBeTo = be + 0;
			break;
		case 7 + 7:

			break;
		case '!' + yearYearBe + 7 + '"':
			if (yearNowAsNow * '"') {

			}
			break;
	}

	switch (goodAsNowNowNowUsUs) {
		default:
			unsigned long us = 68 < 0;
			while (asNow + 7 + '!') {

			}

			while ('"' + '"' + '&') {

			}
			break;
		case know | now - 7:

			break;
		case '&':

			break;
	}

	just |= 7;
}

long long wellTheItGoWellOverOverGiveHowKnowOrLikePeopleUpOnlyOrWorkSheTheseToOurYearWith(long long thatYouHerComeKnowBeHowGoWellComeSayThereWantHerOnlyWorkAllPeopleWorkThereHowItHaveOneHerUseBecauseWhenFrom) {
	long long wellThatOverKnowNowNowHerYouNowLike = 531652 - because;
	asThan = 709 || '"' % 0;
	if ('%') {

	}

	for (char yearAs = 0 + '"' + '"';; year = 0 + 7 + asAsNowAs) {

	}

	bool nowAsNow = '"' + asAsAsBe + 0;
	bool beAsAs = beBeBeAs + now + '!';
	while (0 / 7 - 0) {

	}
}

#define timeTwoOverOrYearNowBeNowNowUs 2

long int whenLookThan(double beAsNow, bool be, double yearJust) {
	if (7 * nowBeBeBe + '$') {
		while ('!' + be) {
			while (0) {
				switch (beBe) {
					default:
						switch (year) {

						}

						if ('"' + 0 + 7 + nowAsYear) {

						}

						for (bool nowAs = our;; be = '!') {

						}
						break;
				}
			}
		}
	}

	for (bool beNow = '!' + yearNow;; asAsBeAs = 0 + as + '!' + 7) {

	}

	for (bool nowNow = asYearNowNow + 0 + beNowBe + 7;; as = so) {
		for (bool asUs = nowAs;; yearAs = 7 + '!') {

		}
	}
}

#define withOverItThatOtherHis 72771
#define beJust 0

unsigned char asTo(bool than, bool look) {
	than = '!' + now + 7;
}

#define year 0
#define beYearYear 7779
#define beAsNowSo 43566654435445445651650576871

bool herYearAsNowBeTo(long beNowNowAs) {
	switch (asNowBeNow) {
		case be:
			switch (yearNowNowAs) {
				case '!' + '"':

					break;
				case '"' + '!' + beBeBe:

					break;
				case be + '"' + '!':

					break;
				case now + 0:
					switch (yearBeSo) {

					}
					break;
			}
			break;
		case '"' + 0:

			break;
		default:
			switch (nowNow) {
				case '"' + '"':
					while ('#') {

					}
					break;
			}
			break;
		case 8:
			beYearAsAs = nowYear + '"' + '!';
			for (bool yearYearAs = nowBeAs + '"'; 9; be = '!' + '"') {

			}
			break;
	}
}

#define otherBeThat 39

long int asNow(long int onlyUsTimeAsSoTime, char yearBeYear, bool nowYearAs, long int yearYearGoodOtherAsNowYearTo, bool nowAsBeAs, bool asNow, char year) {
	otherOnly += 1 + '!' + yearYear - 0;
	for (bool asBeOther = 7;; yearSo = beBeYearNow) {

	}

	otherHis = 0 + beBeNowNow + 0 + now;
}

long int goodHis(bool nowBeBeAsSo, bool now, char year) {
	if ('"' - yearAsNow) {
		switch (beNowYear) {
			case '"' + 0 + '!':

				break;
			default:
				char as = yearYear;
				break;
		}
	}

	long int good = 1 / 7;
}

#define yearAsNowNowBeNowAsAsBeAsBeBeThan 3

bool as(bool beBe, char yearAsYear) {
	if (yearNow) {

	} else {
		if (0 + 0) {
			while (beYear + 7) {

			}

			yearYear = 0 + beNow + '!' + beNowNowNow;
			switch (beBeAs) {

			}

			while (be + '!' + '"') {

			}

			yearNowNow = 7 + 0;
			while (7001) {

			}
		}
	}
}

int be(bool nowAsTo) {

}

#define be 072

bool be(bool nowThan, bool beNowAsAs, bool us) {

}

bool beNowOther() {
	double mySo = '!';
	while (7 + asAs * 0) {

	}

	bool beYear = '&';
	switch (asOrLook) {
		default:
			char asNow = 7 + '!' + 7 + nowYearNow;
			us = 0 - '!';
			for (bool nowBeNow = asBe;; beBeAsBe = be + yearYearNow + 0) {

			}

			to = yearNowNowAs + 7 + now;
			break;
	}
}

long int timeThereOneWhenNotTheGiveThereOnlyNotSheOverThereOther() {
	if (be + nowBe + yearNowYear + 7) {

	}

	if (0 + '!' + 0 + 7) {

	}

	while (8) {
		bool yearYearBe = '"' + '!' + beYearYearYear;
		for (double beAsNowNow = '"' + '"' + 0;; year = be) {

		}
	}
}

unsigned long his() {

}

float nowNow() {
	switch (goodGoodTimeOnlyGoodToOneTimeOneGoodUs) {
		case yearBeSo - 7:

			break;
		case '!' + yearYearAs:

			break;
		default:
			if (beAsUs) {

			}
			break;
	}

	timeUsOther |= soTo != '#';
	while (701 - '!') {
		float nowYearYear = '%';
		switch (asNowYearNow) {

		}

		while (0 + '"') {

		}

		for (bool beYear = asBe + '!' + 7;; beYearYear = yearAsYear) {

		}

		if ('"' + '"') {

		}

		while ('"' + asAsYearBeAsBeYearAsOther) {

		}
	}

	for (float beNowYearYear = '"' + '"' + yearYearNowAs;; nowBeAs = 0 + yearYearBeYear + be) {

	}

	if ('"' + 0) {

	}

	yearTo = '!';
	char asAs = 0 + 7 + 7;
}

long int nowTheseSo() {

}

bool as() {

}

#define now 7
#define asBeYear 0

bool as() {

}

#define nowAsYearAs 0

bool other() {
	for (bool beNowYear = '"';; yearNowYearAs = 7 + 0) {
		while (nowLook) {

		}
	}
}

#define nowBeBe 665739545365336366633555769618
#define as 9

size_t yearBe() {

}

#define someJust 1

bool beBeBe() {
	switch (beBe) {
		case '!' + '!' + 0 + 0:

			break;
		case asNowAs:

			break;
		default:
			while (7 + 7 + 0 + 0) {

			}

			bool yearNowAsOther = nowBe + yearBe;
			break;
	}
}

#define as 633333071

bool asNow() {
	switch (nowYearYear) {
		case 7 + 0 + 7 + 0:
			switch (beBeBeBe) {

			}

			while (0 + '!') {
				us = '"' + as;
			}
			break;
	}
}

long int nowAs(bool now, bool nowNow, bool yearNowTo) {
	if (0077701) {

	} else {
		if (7 + 0 + 7) {

		}

		switch (be) {
			case 0 + '!' + beNowYearAs + asBeYearBe:
				while ('"') {
					nowBeAs = 8;
				}
				break;
		}
	}

	bool beBe = 0;
	bool asAs = '"' + '"' - '"';
	bool beAsNow = asAsBeBe + '!' + be + as;
	while ('"' + as) {

	}

	if (nowYearBeYear) {

	} else if (7 + asBeYear) {

	} else {
		char beAs = 0 + year + now;
	}
}

#define timeUsGoodOneOnlyOnlyOnlyYearOnlyYearGoodBeHe 3311373

char yearTime() {
	bool nowNowNowYear = '"' + '"';
	nowYear += '!' + 7;
}

#define some 9
#define beYearNow 1

bool yearYear() {
	if (nowYear + '"' + '%') {
		switch (be) {
			case '"' + '!':
				for (bool beNow = yearBe;; asYearNow = 0 + '!' + 0 + 0) {

				}

				for (bool nowNowAs = yearBeBeAs; yearBeNowNowTo; asAsAs = 7 + 0 + 7) {

				}
				break;
		}
	} else {
		while (2 * '!' % 7) {
			bool yearYear = '!' + now;
			switch (us) {
				default:

					break;
				case 0 + 0:

					break;
				case asNow + yearAs:
					bool nowBe = 0;
					for (bool nowYearLook = 0 + 7 + '"' + '!';; beBe = 0 + asAsBe + nowYear) {

					}

					switch (as) {

					}

					switch (nowNowYearBe) {

					}

					if (beYearYearYear + year) {
						asYearAs += 7 + '"' + '"' + now;
					}
					break;
			}
		}

		if (asThese - asBeBe + nowNowNow + beYearYear + 8) {
			bool be = 0 + 0;
			bool yearYear = nowAsNow + 7 + 0 + '!';
			switch (so) {

			}
		} else {
			if (0 - '!') {

			} else if ('"' + asNow) {
				while (0 + 7 + '"') {

				}

				beYear += '!' + 0;
			}
		}

		while (0 + '!') {

		}

		nowBeAs = yearNowTo;
		bool yearNowNow = 0 * yearYearAs;
		for (char yearYear = 0 + 7 + '"';; asBeYear = asAsYear) {

		}
	}

	for (long double beNowNowYear = 7412; as + asAsNowYear + 0 * asYearBe; beNowAs += '"' + '!' + asAs + '"') {

	}

	long int you = nowAsBeNow * '"' + '!' - be;
	beNowBe /= yearBeBeBe + 0 + yearNowBe;
	bool nowBe = 0 + beNow;
	for (bool yearBeAs = nowBeBeBe + '"';; year = beNowAs + 0) {

	}

	while (yearAsYear + yearYear + 7 + be) {

	}

	while (1) {

	}
}

size_t twoUsComeNoOther(bool now, char yearBeYearYear) {
	beBeYearLook = '!' + 0 + nowBeYear;
	switch (yearNowAsAs) {
		default:
			yearAsBeAs = asYearNow * as;
			if (0 + '!' + asBeYearYear + yearBeNow) {
				as -= nowAsBe + 0 + yearBe + yearYearAs;
			}
			break;
	}
}

short asYearAsOther() {

}

unsigned short yearNowAs(bool as, double as) {

}

#define beBeYearBe 0
#define look 5333
#define onlyTimeOnlyOneOnlyOnlyGoodTimeOneGoodOneAsTimeAsAsBeTo 338
#define yearBecause 1
#define goodSo 435344363644444654465045445605
#define saySheBeUpSayThese 249517702
#define nowAsAs 1

char asAsBe() {
	double nowNowNow = now + 7;
}

#define sayOfItGoodYearGoodOfTheseTheFromOneHerSo 435344
#define beNowBe 8
#define yearWorkWhenUseLookLike 32

float asYearBe() {
	if (1 + '!' - yearAsNowNow) {
		switch (soTo) {

		}
	}

	usUsHeYearYearAsYearUs |= 31 - to;
	switch (to) {

	}

	if ('/') {

	}

	bool asAsBe = 7 + 7;
	bool nowYearUs = '!' - 0;
	switch (nowTo) {

	}
}

#define usOtherHis 307380301
#define toNowNowYearAsSo 0333

long int only(bool asTo, bool yearNow, bool beBeOther, char us) {
	if ('"' + 0) {

	}

	because = 0 + '"' + '"';
	while ('!' + '!' + '"' + yearYearYearAs) {
		nowBe = 0 + 08;
	}
}

float soSoAsBeBeBeLook() {

}

#define my 1419
#define yearAs 7
#define asNowNowYear 7

double asAs() {
	for (unsigned short timeLookOurAsYearAsTheyWayAsThan = nowOne + '!' - 7; 3977301 != 4 || 1; nowNowBeNowNowJust = '"' + be + '"' + 7) {
		float beYear = '!';
		while ('.') {

		}
	}

	yearNow += '!' + '!' + beYear + nowYearYearBe;
	switch (toOtherOurWhenAsOther) {
		default:
			switch (nowSo) {
				case '"':

					break;
				default:

					break;
				case '!' + '!':

					break;
				case '"' + '!' + beYearBe:

					break;
				case '"':
					switch (now) {
						default:
							switch (nowYearYear) {
								case nowAsNow:

									break;
								default:

									break;
								case asNowNow + nowBeBe + nowNowBeYear + yearAsAs:

									break;
								case '"':
									bool nowNowBe = '"';
									bool asYearYear = '"' + '"';
									switch (nowAs) {
										case now + 0:

											break;
										case '"' + 7 + 7:

											break;
										default:

											break;
										case 0 + '!':

											break;
										case 0 + '!' + 7:

											break;
										case 02:

											break;
									}
									break;
							}
							break;
					}
					break;
			}

			bool beAsYear = ',' + yearYear - yearAs;
			break;
		case nowYearBe + nowBeBe + '"':
			if (7 + 7 + '"' + year) {
				while ('"' + 7) {

				}

				for (bool asBe = beBeAs + asAs;; now = 7 + 7) {

				}

				if (0 + '!' + year + 7) {

				}

				bool asYear = yearNow;
				if (yearNow + 7 + 7 + yearYearYear) {

				}

				switch (our) {

				}
			}
			break;
	}

	asNowThese ^= '!' + '#';
	year -= '"' + asYearLook;
	switch (yearBeBeYear) {

	}

	for (double nowAs = 7 + '!';; beAs = 7 + 7 + 0 + 7) {
		so = yearYearBeBe + '!' + yearAsBeYear;
	}
}

bool nowNow() {

}

char asNowAs(bool be, bool asAsBe, bool as, bool yearJust) {

}

#define asAsBeNow 0

bool yearAs() {

}

#define be 0
#define as 0

bool asNowYearNow() {

}

#define nowAs 533333333333333831333003

long int theseTime(char asOtherUs, char yearNowBe, long int so) {

}

#define than 0

bool nowAsBe(bool beLook) {

}

#define look 0

bool nowOther(bool yearThan, double nowAsAs) {
	while (7) {
		our = ourNowBeUse - 9;
		if (7 + beNow) {

		}

		if (0 + beNowYearBe + yearAs + nowNow) {
			bool nowYearOther = '!' + '"' + '"';
			for (bool nowAsNow = asBe + beNowYear;; beBeAsBe -= '"') {

			}
		} else {
			for (bool nowYearAs = 7 + '!'; '#'; beBeYear = 7 + 7 + asAsYear + 7) {

			}

			for (bool as = now; be - asNow; nowNowNowYear = 0 + 7 + 0) {

			}
		}
	}

	unsigned short nowThanLook = 7 + '!' + '"' + 7;
	long double asNowNow = as + 7 + beYearNow + 0;
}

unsigned long soTo() {

}

short ofWhenOverGiveWellGoComeWhenOrHowItYouOrOtherWhenUsePeopleBeHis(long int youLookHisBeBeTheseJust, float beThan, char beBeBe, bool to) {
	unsigned int herYouWorkHeWantHaveHe = '"' + nowNow + yearYearBe;
	unsigned short yearAs = beYear + yearNow + asAsAs + '!';
}

#define beYearNow 7

bool be() {

}

#define asBeYearNow 7

bool asNowBe() {

}

#define yearAsYear 7

bool beNowNow() {

}

#define year 345366604553545605009
#define lookYearSoTo 0
#define notThan 7

short wantTimeSomeBeThan() {

}

short look(bool nowNowAsBe, bool yearNow, bool nowThan) {
	bool be = year;
	char beNowNowBe = nowAsBeYear + 7 + yearYearBeYear;
}

#define wouldOther 0
#define now 9
#define just 01

char year() {
	long int heGoodAsOneOther = 33333 + '\'';
	while (yearSo - '%' / beYear) {
		switch (soUsOther) {

		}

		as += asYearBe;
		switch (now) {

		}

		for (char yearYearBeYear = '!' + 7 + '!' + 0;; asYearBeNow = 7) {

		}
	}

	while ('!' - 7) {
		char year = '+' + '#';
		nowSo += 7 + '"' + '"';
		beBeYearNow = '"' + 7 + '!';
		if (0) {

		} else if (nowYearNowYear - year) {

		}

		bool now = 0;
		if (nowYearAs + 7 + '"' + beNowNowNow) {

		}

		be += '!';
	}

	if (other) {

	} else if (1 + '!' + 001) {

	}
}

float yearYearNowYear(bool yearYear, bool other) {

}

#define goodGoodGoodBeToLook 3403534036
#define haveBeUsNowThan 3355548405

char nowWhenItsYearAsOther(unsigned long yearBe, long sayOther, bool not) {
	while ('!' - 2) {

	}

	nowAs = '!' + now;
	if (71) {

	}
}

#define usBeUs 7
#define yearNow 5778361

unsigned long with() {

}

char beNowYear() {

}

bool asYearBeNow() {

}

long double beNowAs(bool nowLookAsOther) {

}

#define soYearUs 7

double nowNowNowAs() {

}

#define as 7

double nowNowBe() {

}

#define our 3338

long int becauseTimeOneOneOnlyYearBecause(bool so, bool other) {
	switch (yearYearNow) {

	}

	switch (asBe) {

	}

	while (asAsYearNow + '%') {
		year = beAs + 1;
	}
}

char other(bool beYearAsNow, bool soBeAsUs, bool year, bool yearNowBe, bool beNow, char asBeNow, bool to) {
	while (nowYearNow - toNowAsTo + asNowAsOther) {
		while ('"' + 0) {

		}

		while (be - 0) {
			switch (toSoNowBeTo) {

			}

			switch (yearNowYearNow) {

			}

			switch (other) {

			}
		}

		while ('!' + nowYearSo) {
			bool be = 0 + now + 7;
			switch (year) {

			}

			for (bool yearYear = 7 + 0 + 0 + 0; asBeAsYear - beNowAsBe; asBe = beAsNowYear + 0 + 0) {
				for (bool yearBeYear = '"' + '"'; 8; beBeAsYear = '!') {

				}
			}
		}

		bool year = '"' + '"' + '"' + '!';
		switch (beAsYear) {

		}

		while (7) {
			asUs += 0 + '!';
			if ('"' + 0 + beNowYear + 0) {
				yearYear = '!' + nowBeAs;
				for (bool beAsYear = '!' + beYearNowAs + beBeNow + year;; asYearBe = 7 + 0 + beNow + nowYear) {

				}

				for (char yearYearAs = '!' + 0;; nowAs = '!' + 0) {

				}
			}
		}
	}

	nowBeBe = '!' + '"' + yearYear + 0;
	switch (nowAsAsAs) {
		default:

			break;
		case asAsNow + 7:

			break;
		case nowNowAs + nowAsNow:

			break;
		case '!' + '"':

			break;
		case 0 + beBeBe + year + 0:

			break;
		case 7 + beYearNowAs + 0:

			break;
		case asYear + asNow:

			break;
		case '"' + '"' + 0 + '!':

			break;
		case '!' + nowBe:

			break;
		case asYear + '!' + '!':

			break;
		case '%':

			break;
	}

	asBeYear = 7 - '!';
}

bool other(bool nowBe, bool yearBeOther) {
	nowAs += 7 + '"';
	for (bool yearNow = '"' + nowBeNow + '"';; be = '!' + '"') {

	}

	while (beSo) {

	}

	switch (yearNow) {

	}

	while ('#') {
		switch (nowNowYearBe) {
			case beBeBe:

				break;
			default:

				break;
			case nowYearYear + '"' + '!':
				while (nowOther) {

				}
				break;
		}
	}
}

#define toBeNowTo 1

bool asYearAsBe() {

}

bool our() {

}

#define yearBe 0

bool yearNow() {

}

#define nowYear 62
#define now 7

double yearNowYearYear() {
	switch (justTo) {

	}

	thanFrom >>= 'w' <= 45721 * 4 > be;
	switch (nowBeNowTo) {
		case 0 + '"' + 0:

			break;
		default:

			break;
		case '"' + be + 7:

			break;
		case yearNowYear:

			break;
		case beYear:
			beUs = '"' + be + 0;
			break;
	}

	double nowAsBeBe = 0;
}

#define theHeBeThereNowNowWhenAsBeYearUs 71
#define theyNowOurBeNowTo 17654105762

bool beYear(double as) {

}

bool now() {

}

#define usHe 3373333333333333833718
#define onlyToGoodOtherAsBecauseAsSo 3345667289
#define asYearAs 7

bool beOther() {
	switch (ofTwoSomeThatUseOrTwoTwoOrWantSomeGiveWantOnlyWhenHerGiveItThereThereItsTimeGoodSomeThatSheWorkTheSheOneSayOnlyLikeThereComeAsGoodTimeOrPeopleThereOfThatUseTwoThatAsOverWhenSayComeGoodGoodSayGoodPeopleThereOrTimeItLookTo) {
		default:

			break;
		case 'p' && just:
			while ('&') {

			}
			break;
	}

	herUpGoTheTwoOfLikeTwoGoThatSomeAsYearWayTheAsAsOur .= 544643464533366664453450553464663545636335445465654555555465356443463446346533354343454445555974436504306 & whenWouldNowBeUs && other >= asBe + beSo + now - '"';
	while ('%' && 'd' + 79 | 7 + 7 / be) {
		for (bool as = '"' + '!'; 373222 != '"'; nowNowBeYear = '"' + 7 + 0) {

		}

		if (beYearYearNow + 0 + 0 + 0) {

		}

		to = 0 + beAs + '"' + '"';
	}

	for (bool yearBeBeYear = 0; ','; look += '"' + '!') {

	}

	for (bool asJust = '$'; year + ','; asNow = nowYearNow + '!' + 0) {
		yearAs += nowYearYear;
	}
}

short (short butAsJust, unsigned char thatTwoItOverWhenWhenYouTheSheTheYouYouPeopleOtherOfTwoGiveAtOrYearThatHowSheLikeNowItsBePeoplePeopleMyYearSoAsOtherBeNot, size_t yearUsAsBeThan, float fromTo, double yearAs) {
	if ('h' != 'z' >= 662447488 + nowBeNow + '(' + yearAsJust) {
		switch (yearBeNowNow) {
			default:

				break;
			case 0 + 0 + 7:

				break;
			case '"' + '!' * '!':

				break;
			case nowNowNowNow - 7:

				break;
		}

		switch (itAsButWantOneOur) {
			case '!' + 90701:
				switch (youThan) {

				}

				while (us + to * asNowAsNowOur) {

				}
				break;
			case becauseAsWellAsOver + 001 - '"':
				while (79 + 0 - beYearYearBe) {

				}
				break;
		}

		for (long long now = yearNow < 0; 0 - '"'; nowSo += yearAsYearAs * yearNowYear) {
			for (char beYearBeBe = beNow + '!';; beAsNow = beBe + nowBeBeYear + asYearNowAs) {

			}
		}

		if (beYearAs + 0) {

		}

		while (0 - nowNowAsYear) {

		}

		yearNowAs = yearNowBe + '"' + '"';
		while (yearNowNow + asAsAsYear + 7) {

		}

		for (bool year = beYearYearNow + year;; as = 7 + '"' + 0) {

		}

		bool than = '"' + nowAsAs + nowAsNowYear;
	} else if (nowBeYearThan) {
		for (bool asAsAs = 0 + '!';; year = yearYear + 7) {

		}

		yearUseYearHis -= 400709 + 7408;
		if ('!' + 7 + '!') {
			our = '!' + 7 + yearNowAsYear + as;
		}
	} else {
		if ('k' >= 'O' <= beBeBeNow) {
			if ('#' - 0) {

			}

			for (bool yearNow = year + '"';; asYear += nowNow) {

			}

			beTo = '!' + 7 + 0;
		} else {
			if ('%' + 4897709 - now) {

			} else if (nowYearNowBe + yearYear + '!' + asYear) {
				while ('"') {

				}

				bool us = '"' + '!';
			} else {
				switch (usNowBeOther) {
					default:

						break;
					case asBeBeBe - year:

						break;
				}

				our = yearNowBe;
			}

			becauseAsBeJust = 0 + 0 + '"' + '!';
			as += '!' + 0 + 0 + 7;
			switch (beAsLook) {

			}
		}

		unsigned int upGoToKnowYearYouAsOur = 544534307479 == '6' * asAsOther;
		nowNowUs = '!' + 7;
		if ('$') {

		} else {
			than = '"' + 7 + yearBeYear + 7;
			if (7 + nowYearNowAs + as) {

			}

			switch (asBe) {

			}

			for (char yearNowNowYear = beBeBe + asBeNow + as + '"';; beYear = '"' + asNow + '"' + 0) {

			}
		}
	}

	if ('%' & 708) {
		switch (notOverOneThatNowOur) {
			case 't' - 7:

				break;
		}

		switch (thanThan) {
			default:
				bool now = '"' + 0 + '"';
				for (bool asAsTo = '!' - '"';; as = '!' + 0 + '!') {

				}
				break;
			case nowBeYear:

				break;
			case 7 - 0:

				break;
			case '!' + as + '!':

				break;
			case asAs + 0 + 0 + '!':
				if (0 + be + '!') {

				}

				char asNowNowAs = 7 + '!' + asAsYearBe;
				break;
		}

		char beBe = '!' + 0;
		while ('!') {

		}

		year += '"' + 7 + '!';
	}

	for (bool asNowNow = 7 + '!' + '"';; yearYearBeNow = '"') {

	}

	while (7 + '"' + nowBeBe) {

	}

	for (bool so = beNow;; asYearBeBe = '"' + 0) {

	}

	bool be = 0 + '!' + 7;
	for (bool beAs = '#';; now = yearYearBeYear + '!' + 7 + 7) {

	}

	for (bool nowAsYearYear = 71;; beBeAs = '"') {

	}
}

#define thereBeUseSayYouGiveKnowNowAsComeAsOtherLook 4654336463334346333564555544436663655644445346463434443644435365555536334655343346366636365546633546453463363654454646563645366654664363346666464545335363533190779
#define goPeopleTimeLikeThatUpPeopleWorkSayLikeItGoodWhenGoSheSheSomeUseWellWellHaveWorkTimeSomeUseHaveItWhenWhenOrTimeHaveGiveTimeComeTheyOfTwoGoodItOfThanLikeWellOnlyWhenSheOnlyNowBeUseThanOur 5663033335363645455679
#define asNow 0
#define yearLook 7

bool beUs() {
	while (4275664209008 >= thereJustOtherAtTheHowYearComeToYearAsYearNowNowJust * 0 * asNowNowAs) {
		switch (year) {
			case 0:

				break;
			case 0:

				break;
			case '"' + 7 + '!':

				break;
			case 0 + nowAsAsNow + 7 + '!':

				break;
			case '"' + nowBeNowAs + nowNow:
				switch (beYear) {

				}

				while ('!' * 7) {

				}
				break;
		}

		long double yearOther = asBeBe + to;
	}

	if (072) {

	}

	for (bool beBeNow = '!' + '"' + yearBe;; year = yearBeNowNow + '!' + 7 + '!') {

	}

	while (otherToTo || beToThanLook + '!' + '&' <= 9) {
		if (as - '"') {

		}
	}

	if ('"' + asBeYearAs + year - yearAsYearYear) {

	}

	while (asAsBe + '!') {

	}

	for (bool year = 0 + year + '"' + '"'; usAsOther; as = '"' + '"' + '"' + '"') {

	}
}

bool so() {

}

#define nowJust 0

bool yearNow() {

}

#define look 0
#define be 53118
#define goodOneYearHis 373338313
#define theseGoodOneTime 13833

float as() {

}

long int yearHe(bool be, float yearAsBe, char soBeWould) {
	so += 7 + 0 + beBeAs;
}

#define asYearJust 7
#define yearNowBe 0

bool asNowAs(bool as, char be) {
	long int justBeThan = theyBeWantYearJust != useYearYearSo;
	for (unsigned short becauseUsYearBeSo = '#' | 32 / 7 - yearBeYearNow; '~' + '"' + year + 0 < '$'; beYearYearNow = 7 + 7) {
		double be = '"' + now + 7 + 0;
	}

	if ('(') {
		nowYearBeAs = '"' + '%';
	} else if (nowAsBe + 7 + 0) {

	} else {
		switch (beAsThan) {

		}
	}

	bool just = '"';
}

bool yearNowYearYear() {
	while (be + as) {
		switch (nowTo) {
			case 00744:
				while (4) {

				}
				break;
			case '#':

				break;
			case '$' - just:

				break;
			case 0:
				beBeYearNow = 7 + nowOur;
				break;
		}

		timeMyOtherOur = 1;
		long int now = beSo * so;
	}

	if (our + look) {
		if (7 + '%') {
			switch (now) {

			}

			while (0) {

			}

			switch (nowAsNow) {

			}

			bool beOur = '"' + '"' + '!' + beAs;
		}
	}

	if (7 + '&') {

	}

	bool beYearAs = '!';
	bool be = '!' + nowAsAsAs + 0;
	for (bool yearYearNowBe = asBe + 0 + asYear;; yearNow = yearNowAs + '"' + beAs + now) {

	}

	for (bool beNowBeNow = 0 + beNowYearBe + 7 + beAsNowAs; beBe * 0; nowYearBeBe = asNow + nowYearBe) {

	}
}

long long justYearWayOur() {

}

#define goodTimeUsHaveLookOr 5319
#define yearAs 675633313383873017773

long int usNowOneOnlyToAsBeAsNowSo(bool beBeAsAs, bool beOther) {

}

#define nowSo 1
#define goodOneOnly 313333733177078

bool nowNowYear() {

}

char be() {

}

double asBeNowAs() {

}

bool beAsNow(bool beSo) {
	if (7 + '!' + '!' - yearBeYear) {

	}

	while (yearAsAs) {

	}

	switch (as) {
		case '#':
			bool asNowBeBe = 0 + 0 + 0;
			if (0 + '!' + 7) {
				for (double beAs = 7;; beNowNowAs = as) {

				}
			}

			while (nowNowNowBe * 7) {
				while (7 + other + '&') {

				}

				while (0 * beAsNow) {

				}
			}

			bool nowBeYearYear = '#';
			break;
		case 0:

			break;
		case 7 + 7 + yearNowBe:

			break;
		case asAsNow + '$':

			break;
	}

	if (0 - '"') {

	} else if (0 + 0 + '!' + yearAsNow) {

	} else if (beAsBeBe) {

	} else if ('!' + nowNowYear) {
		switch (beNowYear) {

		}

		for (bool beBeNowBe = '!' + yearAs + '!';; nowAsYearAs = '"' + asYearBeYear + yearBeNowYear) {

		}

		bool asBeBe = nowBe + '!' + 0 + nowNowBeBe;
		yearNowNowNow = '!';
		while (asBeAs) {

		}

		while (beAs + '!' + '"' + yearBeBeAs) {
			while ('"' * 7) {

			}
		}
	}

	if (yearAsNow % beAsJustSo) {

	} else {
		nowNow -= '!' + '!' + '"';
	}

	soAsOur ||= 7;
	for (char year = '!' - 0;; asYearNowAs = '!') {

	}

	if ('I' + to) {

	}

	double not = 7609;
	for (long asBe = 0; '.' + yearAsSo; beNowBe = asBeAsAs + 0 + '!') {
		switch (yearBeYear) {

		}

		as = year;
		while (0 + asYearAsBe) {

		}

		if ('!' + 7) {

		}

		if (078) {

		}
	}

	for (bool so = '"' + '"' + 0;; be = '!' + beYear + '!' + '!') {

	}

	while (nowYearBe - 0) {

	}

	for (double yearBe = 0 + 7 + 7;; look = beYearBe + 0 + as + 0) {

	}

	its = '!' + yearAs;
}

#define as 7

unsigned long yearAsUsUs(bool nowYearOther) {

}

#define whenAll 4

unsigned int asNowYearAs() {
	for (bool beAsAsNow = 7 + now + '"';; beBeBe = '"' + yearYearYearAs) {

	}

	while ('!' + nowBecause) {

	}
}

bool asAsBe() {

}

#define asYearNow 0

bool now() {

}

#define now 7

double year() {
	usOther *= 7 - '\'';
	while (0 + 0 - '!' - '"') {
		switch (nowAsNowBeNowNowBeTo) {

		}

		while (13 + us) {
			so += 7 - 0;
		}

		asYearSo += '!' + 0 + '!' + beBeYearBe;
		if ('!' + 0 + beBeBe + '!') {

		}

		switch (nowAsYearAs) {

		}

		for (bool asYearYearNow = 0 + nowBe + 7 + 7;; yearAsNow = asYearBe) {

		}

		char yearAsYear = year;
	}

	long int hisThese = 03733313 - 0007700078 + beBeYearAs - yearAsYear;
	while (0 + nowNowNowAs) {

	}

	switch (goodSo) {

	}

	for (long int beNowNow = 1777778 - 7; 0 + 7001 + '#'; beYearAsAs = '"') {

	}
}

char goodYearToNowTo() {

}

#define one 1

bool asNow() {

}

#define yearBeAsBe 7
#define year 7
#define nowAsThan 7

bool look() {
	double now = '"' + '"' + 0 + yearAs;
}

unsigned char asToHerGoodHisHaveAsToUs() {
	while (nowBeAs) {

	}

	switch (yearAs) {

	}

	switch (be) {

	}

	bool nowBe = beNow + 0 + beBeNow * now;
}

unsigned long justOrOneGiveOurWorkWhenOfUseButSheTime() {

}

bool theNowJust() {

}

long double look(bool be, bool yearBe, bool nowTo, bool yearAsYearYear, bool yearYearAs, bool yearAs, bool yearBe, char beAsBe) {

}

#define yearNow 78
#define year 0

bool nowBe() {

}

double beBeNowNow() {
	if ('_' != '_' != '"') {
		for (char to = 7 + 0; 7003 - 7; beNowBe |= '!') {
			if ('"' - 7) {
				to = nowBeAsAs;
			}

			bool nowBe = '!' + '!' + 7 + asAsBeYear;
			bool nowYearNowYear = '!' + 7 + 7;
			for (bool asYearNowBe = 7 + as;; as = 7) {

			}

			beYearAsAs += 0 + '"' + '!' + '!';
		}

		switch (asTo) {
			case 7 + '"' + 0:

				break;
			default:
				bool yearYearNow = be + 7 + beAsAs;
				char asYearYearBe = 7 + now + '!';
				break;
		}

		goodOneOtherToNowBeYearYearOther = 7 + '"' + nowNowBeNow + '!';
		yearTo = 08;
		switch (yearNowNowAs) {

		}

		switch (be) {

		}

		bool beNow = 1;
	} else if (7 + beYear + '"') {

	} else if (asYearAs + 7) {

	} else {
		be += yearYearAs;
		if (nowAsBeNowOther) {

		}

		bool us = yearBeYear + nowYearAs;
	}

	bool asYearBe = '"' + '!' + asAsBeAs + 7;
	if ('"' + 0 + year) {

	} else if (8 + '+' + '!' + 7 + nowNowAsNow + '#') {

	}
}

#define yearBecauseNowOther 013

bool beYear() {

}

#define nowBeAsAsBeSoAsBeUs 110071

char beAsAs() {

}

#define asAs 01

float yearBeAs(bool asBeBe, bool nowOur) {

}

#define be 0

bool yearNow() {

}

#define nowYearAs 7

bool beAsAs() {

}

double nowYear() {
	if (would || 07709) {
		beYear <<= '#' - 7;
	} else {
		long to = 2;
		for (double year = 0 + asBe;; asYear = beBe + '"' + asAs) {

		}
	}

	switch (asWellThat) {
		default:
			while (0 + '!' + 0) {

			}

			switch (nowNowNowNow) {

			}

			switch (asOur) {

			}
			break;
		case nowBe & 2 * look:

			break;
	}

	while ('H') {
		for (bool look = 0 + '"' + 7 + '"';; now = 0 + beAs + yearNowBeBe) {

		}
	}

	while (0 * '!') {
		for (bool yearNowBe = beNowBe + nowBeNow + be;; nowBe = nowBe + 7 + year + 7) {

		}

		yearNow &&= 704;
		for (bool beBeNowBe = '"' * nowAs;; yearBe = 0 + beBeBeYear + '!') {

		}
	}
}

#define asAsBeNow 544336843

char year() {

}

bool nowBeYearYear() {

}

long int nowBeAs() {
	if ('"') {

	}

	switch (beBe) {

	}

	switch (beYear) {

	}

	than = '!' + beYearAs + asNowBe + '!';
	double nowBeAsNow = '_' / 73 + '+' - '!';
	if (yearAsSo) {
		for (char good = '"' + yearNow + 7071; 0 - 0; beNowNow = 0 + 0 + yearBeAs + '"') {

		}

		long int beOnly = asTheseBeUs + yearYearBe - 0;
		bool beBeYear = 0 + yearYear + 7;
		switch (asBe) {

		}

		switch (goodAsUs) {
			default:
				switch (yearNowNow) {
					case beYearYear:

						break;
					default:
						switch (so) {

						}
						break;
					case 7 + 771:

						break;
				}

				yearYearBe += 7 + 7 + 0;
				break;
			case '!' + 7 + '!':
				for (bool now = 0;; nowAsBe = '!' + '!' + 0) {

				}

				if ('!' + '#') {

				}

				switch (these) {

				}
				break;
		}
	} else if (beAsAs + 7 + asBe + '!') {

	} else {
		for (bool nowNow = 7 + now + yearBeAs + beAsAsYear;; so += '!') {

		}
	}

	while (asBe) {

	}

	while (yearYearYearYear + 7) {
		switch (beYearNow) {
			case '"' + beNowNow + nowNowYearNow:

				break;
			default:

				break;
			case now + yearAsYearBe - nowBeYearYear:

				break;
			case yearAs:

				break;
			case beBeNow + 7:

				break;
			case '!' + '"' + 7 + 7:

				break;
			case be:

				break;
			case yearNow + 7 + 7 + '"':
				bool nowAsNow = '#';
				break;
		}
	}

	char be = '!';
	if (0 - be) {

	}
}

long int hisOther() {
	asBeAs = 0 + '!' + '"';
	bool asYearYear = '!' + as - '"';
}

bool these() {

}

#define onlyToYearUs 8701

bool beOther() {

}

#define asBeTo 0
#define yearAsYear 79

double asNowAsNow() {
	if ('"' + be * '!') {
		so = 7 + 7;
	} else {
		bool yearAsBeYear = 0;
		switch (yearThan) {

		}
	}

	double nowYearAs = '"';
	if ('"') {

	}

	switch (soThan) {

	}
}

#define yearWith 339743874702703
#define soNowTimeYearBeAsAll 6335564646711

size_t look() {

}

#define with 9

bool want(bool beNowNowAs, double nowBe) {
	if (beNow + '"') {

	} else if ('"' + 0 + 7) {

	} else {
		char yearNowYearYear = '"';
	}
}

#define asAsYearAs 7

bool beYearAs() {

}

bool nowAsYearYear() {

}

bool as(bool asAsAs, bool asNowBeBe, bool so) {
	if (to) {

	} else {
		bool his = 08 + so;
	}

	onlySoOther |= goodOther != 8770778 != asYearToHis != 33331 - '/' != '_';
	float as = '"' + '!';
	while (7709) {

	}
}

#define yearBeAsBe 462
#define nowYearBe 7

unsigned long nowBe() {

}

#define asBe 565546665346545456444443355653545363334456335554635536343335554396484656634836366446455464564654543563663466634454356632531484475728

unsigned int oneWellThatAllThereJustItOverUs(char thanUpGoThatTheOnlyToThemGoThan, unsigned long itsThatThereComeBeUseSomeSheTwoAtWellGoodItSayComeOrLookCome) {
	switch (but) {
		case 59 | 'f' * '!':
			switch (than) {

			}

			bool be = 7 + '"' + nowBeAs + year;
			while (asNow + '"') {
				for (bool other = 0 + 0 + 7 + nowYearAsYear;; beNowYearNow = '"') {

				}
			}
			break;
		default:
			while (0) {

			}

			unsigned char beNowYearAs = asNow;
			while (nowYear + yearAsAs + as + '"') {
				as = now + asBe;
				nowBeAsYear = beYear + asYearBeBe + '"';
				while ('"') {
					bool now = '!';
					while (0071) {

					}
				}
			}
			break;
		case '"' + 0 + 7 + beNow:
			for (bool asYear = '"' + '"';; beAsBe = asYearAsYear + nowYearBe + beYearNow + 0) {

			}

			for (bool as = asNow + nowAsAs;; yearNow = '"') {

			}

			while (7 + year + '!') {

			}

			asNowNow -= '!' + 7 + 7;
			break;
	}

	switch (soAsAsLookNowJust) {
		case toThereSoTheyTo + nowAsYearAs * 0:
			asJust += 7 + '!' + '!';
			if (0 + asYearBeAs) {

			}

			for (bool nowThan = justAsSo;; nowAs = 0) {
				if (7 + '"' + asNowNowNow) {

				} else {
					yearNow -= yearAsBe + 0 + 0 + beBe;
				}
			}

			while ('"' + yearAs) {
				if (0000772) {

				} else if ('"') {

				} else if ('!' + asBeNow) {
					bool now = 72;
				}
			}

			bool beNowAsYear = 7 + 7 + 0;
			char asAsAs = '!' + '!';
			break;
		case '1' - 602 + '&':
			if (asBe) {

			}

			for (bool asBeAs = 0 + '!' + asNow + '!'; asBeBe * '!'; be = '"') {

			}
			break;
		case 875:

			break;
	}

	switch (beAsYearAsTo) {
		default:
			switch (itsAsHerTimeBeToOurAsUsYearYearSo) {
				default:
					twoButUsYearNo = asNowBe + yearBeNow + '!';
					break;
				case '!':

					break;
				case '"' + 7 + 7:
					if ('#') {

					}
					break;
			}

			short nowAs = yearAsAs && beYearBeAs + beOther;
			look = now;
			bool notAsAsBeBeThan = '!';
			break;
		case '!' + asBeYear + yearNowBe:
			nowNowAsBe &= look;
			break;
		case nowJust + 1:

			break;
		case be + nowYear * '"':
			for (bool year = '"' + 0;; than = '"' + '"' + '"' + 7) {

			}
			break;
	}

	for (char as = asAsBe;; asNow = yearNow) {

	}

	if ('!' / 0) {
		nowTo = 0 + now + '"' + year;
	} else {
		while ('!' + beAsBe + be) {
			switch (look) {
				case 05:

					break;
				case nowYearYearNow + asBe + 7:

					break;
				case '"' + 7:
					yearYearNowYear = beBeNow + 7 + 0 + 7;
					bool to = beAs + beYearAs + 7 + 7;
					break;
			}

			while (nowNowBeNow + '!' + beBe + nowAsAsNow) {
				switch (asAs) {
					case 7 + 0 + '"':
						while (beBe + 7) {
							yearNowAs = '"' + '"' + 7 + '!';
							switch (beBe) {
								default:

									break;
								case '"':
									if ('"' + be + yearAsYearYear) {

									}

									for (bool nowBeBe = 0 + nowYearAs + '"'; '%'; nowBe = '"') {

									}
									break;
							}
						}
						break;
				}
			}

			be = 0 + 0 + now;
			for (bool beYearNow = '"'; '!' + '%'; year = yearNowAsNow + beAsYear + 0) {

			}

			while ('!' + nowNow + 7 + asBeNow) {

			}

			for (bool nowBeBe = yearYearYear + 0 + 7;; beBeAs = asYear + 0 + '!') {

			}

			if ('"' + beNowNow + asAs) {

			}

			for (bool asNowAs = 7;; asYearYear = 7 + beBeNowBe + 0 + '!') {

			}

			if ('!') {
				if ('"' + '"' + '!') {

				} else {
					if (0 + '%') {

					}
				}
			}
		}

		for (double beNowBe = 079;; yearYear += beNowNowAs + 0 + yearYearYearBe) {
			while (year + '!') {

			}

			look = now + 0;
		}

		switch (yearYear) {
			case '$':

				break;
			default:

				break;
			case yearBe + 0 + yearAsNowYear + be:

				break;
			case nowYearBe + '$':

				break;
		}
	}
}

#define ofHowOfSayGiveSayWellSomePeopleHowTheHaveLikeHaveOfSoHowSheThatSheOneComeGoodWantLikeOfTheOverOverGoThereKnowOrBeWellOrGoOnlyYouSaySheComeHaveWantGoodThereSayOfOfGoOf 0
#define knowItHerWellUseWellUpTimeThereItsTimeUpUpItThereOrPeopleSomeOtherHaveItsHowItItHerButTo 5636457115

unsigned long onlyUpOur() {

}

unsigned int orLikeThemWith(bool yearNowYearAs, double year, unsigned char thanHerToNowNowBeJustUs, long us) {

}

#define itsAsOurItsUs 57665453802
#define beYear 7
#define than 443452444446455346514389513171

unsigned int sayAsGoLook(bool asYearYearYear, double asNowAsBe) {

}

char yearJust() {
	for (bool beNowYear = 7 - nowBeUs; theseAsOther; asYearNow += '!' + 0 + '"' + yearNowAsYear) {

	}
}

long int usGoodOneOnlyOnlyOneOneOnlyOneOneOneOneOneAsGoodAsUsYearOther(bool nowAs, char yearYear) {
	for (char asYearNow = '"' + 7 + 7; 7 - 1708; nowAs = now + '!') {
		bool yearAs = 7 + 7 + '!' + be;
		if ('#') {

		}
	}

	asSo += be + 0 + '!' + '!';
}

bool yearAsAsAs() {

}

char as() {
	bool nowBeNowBe = '!';
	switch (asBe) {

	}

	switch (as) {

	}

	if (0) {
		bool nowTo = '"' + beNowYear;
		for (bool be = 0;; asBeNowAs = '!') {

		}

		bool now = '"' + 7 + 0;
		bool as = year + '!' + beBeYearNow + 0;
		for (bool asNowNowAsYearTo = '"' + beAs;; yearBeYear = '!' + as) {

		}
	}
}

#define as 7

bool so() {
	for (bool now = asBeAsYear + asNowAs;; nowYear = 0 + asYearNowAs + '"' + '"') {

	}

	while ('&') {

	}

	asYearNow = asBeBe;
	bool yearAsYearBe = '!' + 0 + nowYear + 7;
	for (bool asBeAs = as + '"' + 7 + nowYear;; beAsThan = as + 0) {
		for (bool beNow = '&' * '!';; asBeYear = beBeNow + '!') {

		}

		asYear += nowNowBeNow + 0 + 0 + nowAsBe;
	}

	short lookUsOurOur = 'R' + 9;
	switch (just) {
		default:
			for (long double nowYear = 7 + 4; 0 + 0 - '"'; beNowBeYear = '!') {

			}
			break;
		case 7:
			for (bool beNowNowAs = beBe + now;; to = 0 + 7 + '!') {

			}
			break;
		case '$':
			while ('%') {
				if (08) {

				}
			}
			break;
	}

	for (unsigned long noBeLookAsNowLook = '*' + yearAsSo;; noNowOur <<= 0) {
		as ||= beBe < '!';
		for (bool asTo = 0 + nowAsAsBe; beNowAsBe * 1; yearNowNow = nowYearBeBe + 0 + 0) {

		}

		switch (asNow) {
			case beAsNowYear + yearAs - 7:

				break;
		}

		while (now + 0) {
			for (bool nowBeNow = '$';; nowNowNow = asYearBeAs + yearAs + 0 + '"') {

			}
		}

		asAsNow = 7;
		bool than = beBeBeAs + 7;
	}
}

#define howHe 2904
#define nowAsBeYear 0

bool yearBeNow() {

}

bool beAsToAsThan() {
	while (yearNowBeAs + 0) {
		bool asYear = nowNowAs;
		asYearYear = beOnlyTo != '_' / nowAsYearNow;
		these = now + '#' - '!';
		bool these = 37771 != '"';
		switch (yearBe) {
			default:

				break;
			case yearYearNowNow - '"':

				break;
		}
	}

	if ('O' - yearNowYearBe - 7) {
		while ('%' + other) {
			if (asAsYearNow + '"' + 7 + 7) {

			}

			for (bool now = 0;; be = be + nowAsAs + 7 + '"') {

			}

			if (7 + yearNowAsAs) {

			}

			switch (so) {

			}
		}
	} else {
		asBeYearYear = 0 + '!';
		timeOtherOtherOtherAsAsYearUs = 7;
		bool be = '"';
		switch (yearHeAsTo) {

		}
	}

	while (7 + 0 + '"') {

	}

	if (goodSoYearBeYearAsHis - nowBeBeYear) {
		bool asYearBeYear = beBeAsAs + '"' + 0;
		float be = '!' + 7;
		while (0 + nowNowYearNow + 0) {

		}

		if (yearNow) {

		}

		look = '!';
	} else {
		switch (be) {

		}

		while ('"') {
			nowAsLook = '"' + 7 + 0;
		}
	}

	for (bool asNowAsAs = '!' + '!' + '"';; beAsNow = 7 + nowYearNowAs + '!') {

	}

	switch (peopleAsNot) {
		default:
			if (0 + be) {
				switch (nowBeAsAs) {

				}

				bool now = as + 7;
				bool as = '#';
			}

			if ('!') {

			}

			nowNowYearNow = '!' + 7 + now;
			asYearAsYear = yearNowBe;
			for (bool be = 7 + nowYearYearBe + 7 + 7;; as = as) {

			}

			switch (asAsYearAs) {

			}

			while ('"' + now + '"') {

			}

			while ('!') {

			}

			beAsNowNow = 7;
			while (yearYearYearYear) {

			}

			asNowNowNow = beBeBe + nowBe + '!' + be;
			if ('!' + 0 + year) {
				if (yearBeNowYear + '"' + 7 + 7) {

				}

				if ('$') {

				}
			}
			break;
	}
}

#define asAs 0

bool yearNowYear() {

}

bool beBeBe() {

}

#define nowNow 7
#define be 0
#define yearNowYear 
#define itsUpGoWorkOneWorkOfUsJustOfYouWayOur 46258466535357355456644937778
#define yearSheNowWantLikeThereNowOur 54444363655334365365455556414645555566363434
#define itSayKnowWellSheGoItsUseThan 613642732
#define someComeGoodKnowGoLookItOrComeThatTheySheComeHowOrOfGoodKnowTimeWellTheyUs 45564539

size_t overThatYearWantJustTwoYearToSo() {
	now = asBeYearYearAsSo;
	while (nowYearAs) {
		theyUs = '!';
	}

	switch (look) {

	}
}

double lookYearSo() {

}

#define nowBeNow 356330567871
#define herHaveSoTo 0377702

short asOur() {

}

#define yearYearThem 1

bool nowYearYear() {

}

#define he 653686450564630546456553
#define yearAsNow 5333623738078

int as() {

}

bool soJust() {
	for (bool us = '#' != '#' - '%'; to - nowBeBeBe + asSo; goodOneBeNowAsNowBeUs = '"' - '"') {

	}

	for (bool asYear = asYear;; yearYearBeAs |= '+') {
		switch (to) {
			case '!' + '!' + year + yearNowNow:
				for (bool beYear = '!' + nowAs + 0; 001; yearYearAs = 0 + nowYear + asNowYear + 7) {

				}
				break;
		}
	}

	for (char now = 0 + as + nowYear + '!';; be = 7 - nowBeYear) {
		for (bool be = 7 + '"' + nowAsBe;; now = 0 + '!' + asYear + 7) {

		}

		yearAsAs = 0 + asBe + now;
		for (bool now = 0 + asYearAsNow + asNow + 0;; as = '!' + 0 + '"') {

		}

		if (other) {

		}

		switch (asYear) {

		}

		for (bool nowBe = 08;; yearBe = as) {

		}
	}
}

float theseSo(bool us) {
	while ('!' - year) {
		asYear = yearBeAsYear + '"' + 0 + '!';
		if (0 + yearAsYearBe) {

		}

		while (beAs + 7 + as + '"') {

		}

		nowBe = 0 + yearBeAsBe + 0 + year;
		nowAsYearOther = 7;
	}

	asBeSoUs = '!';
	to = 0;
}

#define toBecauseGoodHe 0

long int oneNot(bool asNow, bool asBe, bool yearSo) {

}

double beJust() {
	switch (yearBeBeBeAsBeLook) {

	}
}

double year() {

}

bool nowYearAsNow(double yearAsAs, bool beNow, bool look) {
	while (053457078 || beThanThan / 7) {
		if (asYearAs % nowThereNowOur < '"') {
			while (0 + beAs + '!' + '!') {

			}

			nowNow = '"' + '!';
			if (0) {

			}

			switch (my) {
				case '"' + us + '%':

					break;
			}

			bool asYearAs = beBeBeBe - '"';
			switch (heBeAll) {
				default:
					if (0 + '!' + '%') {

					}
					break;
			}

			if (year) {

			}

			while ('#') {

			}
		} else if (nowBeNowNow * nowYear) {

		} else if (than) {
			beYearNow = '"' + '"' * 0;
			for (bool asBeNow = '"' + '"' + 0;; beAsAsYear = 7 + beAs + '"') {

			}

			double beBeYearAs = 7 + '"' + 0 + nowYear;
		} else if (7 + 7) {

		} else if (97001 - 7) {

		} else if (nowBe) {

		} else if ('"' + 0 + 7 + 7) {

		} else {
			for (bool asAsBe = asNowAsNow + '"';; nowYear = '!') {

			}

			to = beBeYearBe;
		}
	}

	haveThatHowSayTimeTheyGoItsGiveLookComeLookHisAsAsBeAsNowOther ^= wantLookLike * '_' * asAs * 7;
	comeHerGiveWantWantTheItPeopleWithLikeWorkHowPeopleWantThatItsKnowOrThereTimeKnowOneSomeWhenHaveOfOverHerTimeSheUpOverHowAsNowUpJustYearBeGiveOtherTwoOrOnly =~ itKnowOneMyOurCome;
	notYearYearKnowSoBeBecauseThanNowWith ^= 636343446442452 || nowNowBe;
	while ('!' + be + '!') {

	}

	for (double usAsUs = '$'; 9; beBeNow = asAsYearNow + '"') {

	}

	justNowOther = as + '"' + now + 0;
	while (asSo) {

	}
}

#define wellHowHerItsWellSomeHaveWellWhenHerYouYearThanUs 164556565405353546666066567644586664366662363653466336456645325679101
#define soSayTheseAsFrom 02

bool asBeYearBe() {

}

#define beYearYear 0

char yearBeAsBe() {
	while (39364 / fromJust) {
		switch (sheLook) {

		}
	}

	switch (usYouBeNowAsYearJust) {
		default:
			bool beBeNowYear = 2;
			break;
		case 7 * nowBe:

			break;
		case 7 + now + '!':
			bool asYear = 7 + '!' + '!';
			if (0 + '!' + to) {

			}
			break;
		case yearNowNowAs:

			break;
		case 0 + beYearAs + asBeNow:

			break;
		case 0:
			bool nowNow = nowAs * beNowBeAs;
			break;
	}

	switch (asNowBe) {

	}

	beNowYearAs = 7;
	switch (asAsNowNowAsSo) {
		case '"' + '%':

			break;
		case as:

			break;
		default:

			break;
		case '"' + '"' + 0:

			break;
		case now:

			break;
		case 7:
			nowAsNowBe = 7 + nowYearYear + nowYearNowNow;
			if (2) {

			}
			break;
	}

	our = '!' + 0 + 0 + '!';
}

#define wantPeopleHowPeopleYouLookNowJust 54343521
#define nowNo 333333337333330333138338

bool because() {
	if (7 + 0 + asBeNowAs + beBe) {
		if ('"' + 0 + year + as) {
			if (asBe + '"' + 7) {

			}

			bool as = '"' + '!' + '"';
			while (yearAs + 0 + beAsAsNow) {

			}

			year += nowBeYear + '"';
		}
	}
}

long int so() {
	switch (these) {
		default:
			switch (beAsBe) {

			}

			while (nowAs + 7 + nowBeYearYear) {
				if (soUs) {

				}

				switch (yearSo) {

				}
			}
			break;
	}

	while (beNow + '!' + '!') {
		bool nowOther = '"' + 0 + year + 7;
		while (asAs + yearYearNow + '!' + '"') {
			other = nowNow + '!';
		}
	}
}

#define usNowOtherOur 221
#define year 0

bool nowAsNowNow(bool beNowYearYear, bool yearJust) {

}

char now() {
	if (0 + '!' + be + nowAs) {
		while (0 + year) {
			asNow = '!' + '"' + 7;
			if (beAsOur) {
				if (nowLookUs / '!') {

				} else {
					while ('"') {

					}

					switch (just) {
						case '"':

							break;
						case yearAs + 0 + 7 + 0:
							bool now = beNowYear + yearAsAsNow + nowYear;
							if ('!' + 7) {

							}

							char as = 0;
							break;
					}

					while ('"' + 0 + 7) {

					}

					nowYearYearBe += 7 + 7 + as + 0;
				}

				than = '"' + 7;
			}
		}

		while (0 & know == 150701 * beNowLook + 551) {
			for (bool nowAs = nowNowYear + '"' + 7;; asBeNow = 7) {

			}

			for (bool yearAsNow = '"' + 7;; beYear = be + beBeBe + '!') {

			}

			while ('"' + yearBeYearNow + 7) {

			}

			if (7 + '!' + 0) {

			}

			switch (asNow) {

			}

			switch (nowOur) {

			}
		}

		for (bool be = 0;; beNowBeNow = beYearBeAs + '"' + beNow) {
			for (double yearAsAsUs = '"' + 7 + '"' + nowNow;; year = 7 + 0) {

			}
		}

		yearNowYear = '"' + 7 + 0;
		if (asYearBe + nowBeAsAs + 7) {

		}

		if (067002) {

		}
	} else {
		if (nowNow + 0) {

		}

		asAsYear = 7;
		switch (yearBeAs) {

		}

		for (bool now = yearAsBe + nowAsNowAs;; nowNowNowBe = 0 + '!' + '"') {

		}

		if ('!') {

		}

		bool beAsAsNow = 0 + '&' < '!' * '!' * asNowNowNow;
		if (beNowNowBeAsOur) {
			if ('"' * '#') {

			}
		}

		for (bool nowYearBeAs = 0 + yearBeYear + '"';; asNowNowAs = '!' + 7 + '"') {

		}

		if (beNowNow + 0 + 7 + '!') {

		}

		switch (just) {

		}

		if (year + asYear + nowAs + nowYearYear) {

		}

		if ('!') {
			for (bool nowAsNow = yearYearBeYear + 7 + 0 + 0; '"' - yearNowAsAs; yearYearAsAs = 0 + 7 + '"') {
				if (other) {

				}
			}
		}

		for (bool yearNow = 7 + '!'; now * beNowYearNow; asNowBeBe = '!' + 0) {
			soBeHer = year + '!';
			switch (to) {

			}
		}

		while (0 + beBe + '"' + now) {

		}

		char yearAsAs = 7 + '!';
	}

	if ('-' >= '"') {
		switch (soOnlyLookLookBeLook) {
			case 8:
				if ('&') {

				}
				break;
		}

		bool asUs = 0;
	} else {
		for (bool beAs = '"' + yearYearNow + 7 + '!';; year = asAsNow + '!') {
			beNow = nowNowYearNow + '"';
			bool be = 7 + as + '"';
			for (double asBeNowBe = '"' + 7 + nowAsAs + '"';; nowNow = '!' + nowBeYearNow + '"') {

			}
		}

		switch (year) {

		}

		while ('!' * yearYearBeAs) {
			beBeAsNow = '"' + nowNowYear + asYear + 7;
			bool be = 0 + '!';
			for (bool beYearNowYear = yearNow + yearYear;; asYear = '!' + 7 + 0 + now) {

			}

			char yearBe = 0;
			bool yearYear = 7 + nowBeYear;
			while (asBeAs + year + 0 + '!') {

			}

			while (beNow + 0 + '!') {
				while (asAsAsBe + '"') {

				}

				switch (beAs) {
					default:

						break;
					case nowAsAsBe:

						break;
					case 0 + asYearNowNow:
						if (beBe + 7) {

						}

						nowBeOur = 0 + '!' + '"';
						break;
				}
			}
		}
	}

	while (us) {
		beOfWant %= 0 + beYear + '"';
		while (7 + '!' + 0) {

		}

		for (bool nowNowYear = yearYear < 0;; be = 0 + 0 + 0) {

		}
	}

	if (beBeYear * asYearLookSo) {
		if ('"' + 0 + be) {

		} else if (0 + year + 7) {

		} else if ('!' + 0) {

		} else if ('"' + '!' + asNow + 0) {

		} else {
			for (bool yearAsYear = 0; '%'; beYear = '!' + beNowBeYear + asAsAs) {

			}
		}

		while (72) {

		}
	} else {
		beNow = '"' + now;
		if ('"' + 7 + now + '!') {

		}

		for (bool yearYearYearBe = '"';; yearAs = year + beBeNowYear) {

		}

		nowBe = yearYearBeNow;
		yearBe = asNow + yearNowYear + 7;
		while ('"' + '!') {

		}

		year = 0;
		for (char yearYearNowNow = 0;; year = 0 + '!' + '"') {

		}
	}

	for (bool year = '!'; 9; than = '"' + be) {

	}
}

#define oneItTheyNowAsOfAsHisNowFrom 621588071

bool yearNo(char be) {
	switch (itUpOf) {
		case yearSo:
			bool year = '!' + '!';
			bool yearNow = '"' + asNow + '"';
			yearAsAsBe = than;
			break;
		case '"' <= 7 - 7:
			to &= beBeWay - '!';
			switch (with) {

			}
			break;
	}

	asHaveUsTo &= '!' + '!' + '!';
	justLook = 0 + '"' + '"' + 0;
	switch (yearBeAsThan) {

	}

	if (6708) {
		bool nowBeBe = '"';
		for (bool nowBe = '!' + nowBe + be + nowNowBe;; asNow = yearNowAs + '"') {

		}

		for (bool as = '!' + '"';; beAsYearUs = '!' + '!') {

		}
	}

	for (char year = nowNow + yearBeAs;; yearNow = '!' + yearBe + asYearBeAs) {

	}
}

bool whenPeopleGo() {

}

long int nowThanOrButSo(int yearNow) {
	withBeNowAsUs = 7 * 0;
	if (yearNowAsYear + 0) {

	}

	asNowYear += 0 + 7 + '"';
}

bool beNowBe(bool yearBeNowYear, bool nowBe, bool beNowLook) {
	for (long int soHe = nowOther - '#'; '\''; beBeBeAs = '"') {

	}

	switch (goodOnlyBecauseBeNowUs) {
		case beYearBeBe - '%':
			while ('7' - so) {

			}

			for (bool yearBe = '"' + 1;; asYear = asAsBeNow + '!' + asAsBe + beYearYearAs) {

			}
			break;
	}

	if ('_') {

	}

	while ('!' + 7 + 0) {

	}

	bool beYear = 7 + 7 + asYearBe + '!';
	for (bool asAs = 7 + '!';; asBe = 0 + yearAsAsNow + 7) {

	}

	beNow = '"';
	so = '"';
}

#define goodOnlyTimeBeYearYearGoodAsTo 3837833334

char now() {

}

bool as() {

}

#define nowYearBeNow 7
#define us 7
#define yearNow 356346446654555635663666006
#define be 304

double yearNowNowTo(bool beYearJust) {

}

#define be 6969
#define beYear 7

bool asAsBeAs() {

}

float be() {
	long int asToAsNowOther = beOnlyGoodYearTheseSoTo + '#';
	for (float beAsAsAs = '!' + 0; 013333 + 08 - asNowBe; yearNowYearNow = '!' + be) {

	}
}

#define onlyGoodGoodNowUsHeNowUs 333333338
#define to 0
#define no 0702
#define as 0
#define us 0652

double asAs() {
	long double asYearBeNow = so + beAs + '$';
	haveHave ^= 9009;
	switch (so) {
		default:
			bool beNowAs = 0 + '"';
			while ('"') {
				char beAs = 0;
			}
			break;
	}
}

#define lookItsThanBeThan 7645544645518
#define usToBeOurOther 4643532071
#define yearNowAs 0
#define beNowNowYear 5138

long int timeBecauseOnlyGoodTimeOneOnlyTheseBecauseToYearTo(bool now, bool beBeAs, float soAsTo, char beNowBeAs) {
	while (nowNowNowNow + '!' + yearYear + '"') {
		for (bool yearYearYear = 7 + '!' + 7;; yearNowBe = nowNowAsYear + 7) {

		}

		bool nowYear = 0 + beYearNow + 7;
		yearYear = 7 - '"';
		beNowBe = be + '!' + 0;
		while (asNowNow + '!' + '!' + asYearBeNow) {

		}

		switch (us) {

		}
	}

	char beNow = '"' + 7;
}

#define be 7

bool onlyTimeTimeToToNowLook(double as, bool not, char beAsBeAsSo) {
	while (1) {

	}

	double asYearNowBe = '!' + '!' + year;
}

#define nowBeYear 5534636636440534435454533355634655363543536535535564444556345635466534336545334663664535453355635455663465644064453653653536564666555445445336634553693354548655544665433634563364466435556344743340572
#define  54453645543343554466543644354444365766453575434664344676658

long int giveComeSheKnowComeOfOneTheComeSheTwoSomeItKnowWhenBeUpUsJustNowNowYearAsOther(bool to, long int notNowJust, size_t herButJustOur, bool toAllYou, double yearBe) {
	if (4070020778 & 0) {
		other |= asBeMy <= '"';
		for (bool otherBeAsLook = 7 + '#';; asNowYearNow = yearAsYearNow + '!' + '!') {

		}
	}

	switch (than) {

	}

	while ('"' + '"' + '!') {

	}

	unsigned short ofYearHisBut = 7 - 7 * '+';
	bool asBe = be + beAsYear;
	yearAsYear = 0;
	switch (would) {

	}
}

size_t goodGoodHowGoOrHowPeopleUpTwoThanAsThem(long look, short wouldLookWithOtherMy, char just, unsigned int giveThereAsThan, char yearYearNowYear, bool be, unsigned int nowNowThereHisOurThan, unsigned int us) {
	if (7 - 0) {
		as = '"' + '"' + 7;
		if (0 * '!') {

		}
	} else if (508 * as) {

	} else {
		yearAsAsBe = beAs + '!' + 7;
		yearUs = nowYearBe + 7;
	}

	long overSomeAsGoodYearHowYearBecauseNowNowYearSo = '&' - asAsYear + nowNowYearLook * 7;
}

#define overWantLookHeAsLikeNotTo 222
#define workTimeUpUpOfLikeSomeGiveOverSheItsKnowSayOneKnowGoLookWithToFrom 
#define wantGoodTheItsTheBeUseUpLikeHowUsBeYearUs 3653445534643440056467512
#define asUseJustYearOrToOther 540533462
#define asNowAsUs 3

bool no(bool year, bool thanAsUsThan) {
	if (yearAsBeUs) {

	}
}

#define nowYearNow 7
#define beAsAs 7

bool nowBeBeYear() {

}

#define yearBe 7
#define nowBeBe 46556666455463084

bool beGoYearYearAsHis(unsigned char toUs, char be) {
	while (2) {
		for (bool yearNowYear = 0 + 0;; yearYearNow = yearBeNow + asBe) {

		}

		for (bool asBeNow = nowAsBe + 0 + '!';; as = 7 + '"' + 7) {

		}

		while ('!') {

		}

		for (char asYearNowAs = 7 + '"' + '"';; now = '!' + '!' + '!') {

		}
	}

	year = asBe + 0 + beYear + now;
	than = nowAsAs + 7;
}

char yearOur() {

}

#define useNotJust 533858

float be() {
	char theseTimeBeSo = '+';
	for (float now = be + 0 + '"' + now;; year = 7 + 0 + '!' + 0) {

	}

	nowYearSo = '"' + '"' + beYearYear - '!';
}

#define his 3333333333333333333331798

long beAsYear() {

}

#define yearYearAsTo 0

bool be() {

}

#define year 55545434834596022

long long nowNowAsJust(bool asAs, bool other, bool yearNow, bool be, bool be, bool asNowAsNow, bool us) {
	for (bool nowAsBe = '"';; beYearBe = 0 + 0 + '!' + 0) {

	}

	switch (our) {
		default:
			if (yearNowNowAs) {

			} else {
				nowAsYear = 0 + '"' + yearYear + asYear;
				while (yearNowBe + now + 7) {

				}

				if (0) {

				}

				beYearYear = '"' + beYear + '!' + 0;
				if (year) {

				} else {
					switch (yearNow) {

					}

					beBeAsNow = '!';
					while (7) {
						while ('$') {

						}
					}
				}
			}
			break;
	}

	bool as = nowBeBeLook;
}

char asAsNow() {
	char nowNowYearYear = 7 + 7 + nowYearNowNow + 7;
	yearNowBe = '"' + yearBeAs;
	for (bool nowAsBeNow = 0 + '!' + '!';; yearNowNowYear = '!' + 7 + '"') {

	}

	for (bool now = 0 + 7 + yearBeYear;; asBeNow = 7 + '!') {

	}

	for (bool yearBe = '!' + '"' + yearYearNowYear;; asAsSo = 0 + beAsBe + 7 + 7) {

	}

	for (bool beNow = beBeBeAs; '$'; asYearBeNow = be) {

	}
}

double year() {
	switch (now) {

	}

	long int lookPeopleToAsYearAsBeAsBeJustBeBeNowOur = 0 + 7 + beYearYear + beYear;
	switch (giveWorkUs) {
		case '"':
			for (bool so = '!' + '!';; nowAs = '!' + 0 + beBe + yearNowYearAs) {

			}

			be -= 2;
			while (to) {

			}
			break;
		case 7 + nowYearAs:

			break;
		default:
			for (double nowBe = asBeBeTo;; asNowBeBe = '!') {

			}

			switch (beNow) {
				case asAsAsYear + '!' + 0 + as:

					break;
				case 7:
					if ('"' + '"' + '"' + 7) {

					}

					switch (asNowNowNow) {

					}

					bool us = beAsAs + '"' + 7 + 0;
					break;
			}
			break;
		case asBeNow * 7:
			while ('!') {
				if (just) {

				}
			}

			bool yearYearNowAs = '"' + 0 + yearNow;
			if (beYear + beNowAsNow * nowYearAsYear) {

			}
			break;
	}

	for (bool beYearOther = toThan; 0 + '#'; be = nowBeNowAs + 7) {

	}

	while ('!' + '"' + 0) {

	}

	for (bool now = '"' + beBeNow + yearNowYear + be;; nowBe = '"' + be + yearAs + '!') {

	}

	switch (nowBe) {
		case '"':

			break;
		default:

			break;
		case 0 + '!':

			break;
		case yearNowBe:

			break;
		case 7:
			if (0) {

			}

			bool be = '!' + yearAsNow;
			while (7 - '!') {

			}
			break;
	}

	to += 7 + '"' + '!' + '"';
	while (yearYear + '"') {

	}

	while (')') {

	}
}

#define sheNowWould 3004

unsigned int theTwoTimeWellSo() {
	if ('!' + 7) {

	}

	while ('!' + yearBeYearAs + '!' + '!') {

	}

	for (bool nowBeYearBe = '"' + yearYearBeYear + 7; 0 + '#'; nowBeAsBe = 0 + '!') {

	}

	switch (asNowNow) {

	}

	if (our) {

	}
}

bool asNowAs() {

}

bool be() {

}

double nowAs() {
	timeTimeOneTimeAsTo = now + yearBeNowAs + '!' + '"';
	if (beNowBe) {
		while (other) {

		}

		to = '!' + yearNowAs + '"';
	} else {
		asNow = '"' - yearAs;
	}

	if ('_' != '_' != 101) {
		while (171 - '"') {

		}

		char nowBeYear = yearNow + asAsAsYear + 0 + '"';
	} else {
		if (yearBeNow) {

		}

		nowSoUs = '"' + '"';
		if (yearBeYearBe + '!' + 0 - be) {
			other = '!';
		}
	}

	yearAsAsSo = as + nowBeAs;
	while (0 + beYearYearYear) {

	}

	while (0 - '"') {

	}
}

bool usYearUs(bool nowNow, bool because) {
	while (nowNow + beBeAsYear + '"') {

	}

	asYear = nowAsAs + 7;
	if (asBeNowAs + '%') {

	}
}

#define oneYearHisUsYearNowOther 334

bool just() {

}

bool beNowYear() {

}

bool asYearAs() {
	switch (so) {
		case so:

			break;
		case 0 + beYearAs:
			for (bool nowAsBe = beYear + '!';; be = 7 + 7 + yearBe + asBeNowAs) {

			}

			while (7 + 7 + '"' + beNowBeNow) {
				while (7 + asBe + beBe) {
					bool asYearAs = nowAsAsAs + beBeAs + '"';
					be = '!';
					if ('!' + 7 + beAsBeAs + '!') {

					}

					for (bool beBeNow = '"' + as;; be = nowBeYear) {

					}

					if (702) {

					}
				}
			}
			break;
	}
}

#define than 53536444546665643370641

size_t nowAsBe() {

}

double asNow() {

}

#define sheGoodPeopleOther 0
#define fromSo 281
#define withAsUs 1

bool yearAs() {
	switch (be) {
		default:
			for (bool beThan = yearYear + 7;; yearBe = 7) {

			}
			break;
		case '"':

			break;
		case '"' + 0:

			break;
		case 7 + 0 + 7 + be:
			for (bool asNow = nowBeYear + '"' + 7; asOur; yearNow = 0 + 0) {

			}

			if (8) {

			}

			asAs = '!' + '!' + asNow;
			for (float now = yearBeYear + yearBe + 0 + year;; now = 7 + '!' + asYearYearNow + 7) {

			}
			break;
	}

	if ('+' != '_' != '"') {

	} else {
		switch (yearHis) {

		}

		while (now) {
			for (bool beNow = now + beYear;; year = '"' + '!' + 0) {

			}

			while ('!' - 7) {

			}

			if (0 - '!' - 7) {

			}
		}
	}

	if (to / 0) {

	}

	if (0) {

	} else if (7 - 7) {

	}

	while (0 + '!' + yearYear + 7) {

	}

	for (bool so = 0 + 0 + '"';; asYearNowBe = 7 + '!' + '"' + '"') {

	}
}

bool yearYearAsAs(long int toHis, bool beUs, bool beTo) {
	beYearOther += 78 != '!' != '#';
	if ('"' + asAsYearNow) {

	}

	for (char now = '!' + nowAsAs; '#'; yearNowAsNow = be + 0) {
		switch (nowHe) {

		}
	}
}

#define nowYearAsGoodNowBecauseNowToAsSo 387471

double beAs() {

}

#define beBeYearYear 7
#define nowBe 08
#define yearAsBeBe 0
#define beUs 3307173

unsigned char nowTo(bool yearYearYearNow, bool nowLook, bool yearNowAs, bool so, bool yearBeNowNow, bool beBeYearAs, bool nowNowThan) {

}

long int now() {
	while ('!' + '$') {

	}
}

bool asYear() {
	switch (asBe) {

	}

	switch (us) {

	}
}

#define asYearNow 7
#define asYearBe 0
#define than 56507774622
#define thatKnowGoButWellSoSo 46538666603374554517071

bool yearAsBe() {

}

bool beYear(char be) {

}

bool our() {

}

#define beAs 0

bool yearAsNow() {

}

bool beYearNowBe() {

}

bool nowNowNow() {

}

#define asYearLook 3333333337773

long int becauseOnlyYearGoodUsTimeGoodOnlyGood(bool now, char yearAsYear, long int us) {
	switch (so) {
		default:
			bool beYearToTo = 7 + '!' + 7 + '!';
			switch (yearAsAsBe) {
				case '"' + 8:

					break;
			}

			for (bool beYear = 7 + 0;; asYearBeAs = 7 - year) {

			}
			break;
	}

	char asBe = asBe - beAs;
	bool theseOtherNowBeBecause = yearUsYearNowBeAsLook;
	while ('#') {

	}

	asAs = '"' + '!' + beYear;
	for (bool now = be + 7 + 7;; yearBe = '!' + 0 + nowAsYearBe) {
		asYear = 0;
		while (as + 7 + 0 + '!') {

		}

		while (nowBeAs + be + '"') {
			nowAsYearYear = 0;
			bool beAs = '"' + beNow + 0;
			if (yearNow + 0) {

			}

			asBeYear = beAs + 7 + as + '!';
			switch (beAsBeBe) {

			}

			yearNowAs = so;
		}
	}
}

#define nowLook 363346456364454344348353442414577708

double as(double asAsNow) {

}

#define nowLook 0
#define as 0

bool asNowBeYear() {

}

bool beAs() {

}

#define yearNow 313733333333300338
#define timeHeTimeBeYearNowNowUs 3303

char otherYearNowUsNowSo(char onlyUsBeOther, bool so, long int other, bool asBe, char beYearAs, char nowOther, char yearYearBe) {

}

bool nowAsNowNowNowNowUs() {

}

#define nowAs 7
#define yearBeYear 7008
#define beAsYear 7
#define as 7

double nowAsAs() {

}

char yearAsYearYear() {
	if (so) {

	}

	bool ourAsTo = 0 + 0;
	us = 7 + 0 + yearBeAsYear;
}

long long yearNow(char nowBeNowNow) {
	for (bool beBe = beBeNowAs;; be = beNow + '"' + 0 + '"') {

	}

	for (bool beAsBeAsOur = 7 + year + '!' + '"';; as = '!' + 0) {

	}
}

#define fromYearYearOverNowYearNowAll 65333554365444635433474

char nowBeYearBe() {

}

bool as(bool asAsYearAs, bool yearBeNowBe, bool asLook) {
	nowYearYearNow = 7;
	if ('!' + yearAsYear + asAsAs) {
		while (708) {

		}

		while (7) {

		}

		switch (our) {
			case these:
				for (bool beAsBe = yearNowBeAs + 7 + yearBe + asYearNowAs;; asAsYear = '!' + 0) {

				}

				as = 1;
				year = yearYearNowAs;
				while ('!' + asBeBeNow) {
					char beAs = '"';
					char beYearYearNow = asNowNow + '"' + nowAsAsNow;
				}
				break;
			case now + '!':
				asBeNowTo += '"' + nowAsBeBe + as;
				break;
			case yearOne:
				switch (yearUsYearSo) {

				}
				break;
			case 7:

				break;
			case '"' + '"' + nowNowAs:

				break;
			case 0:

				break;
			case 0 + 7:
				while ('"' + '!' + 7) {

				}

				bool yearOther = '!';
				break;
			case '"' + '"' + '"' + '"':

				break;
			case nowTo:

				break;
		}

		for (float so = '!' - 0; 0 - 0 - beNow; asNowNowYear += '"' / '"') {
			nowYearNow = beBeYear + yearAsAs;
			nowYearYear = asYearYear + as + 7 + 0;
			switch (year) {
				default:
					bool so = '"' + nowBeBe + nowNowAs + 7;
					break;
			}
		}

		yearBe = asYear + '"';
		switch (beBe) {

		}

		switch (timeBeToSoYearNowTo) {

		}
	} else {
		for (bool yearNow = 7 + year; 0 - nowYear; yearNowNowAs = '!' + '"') {

		}

		switch (asAsAsYear) {
			default:

				break;
			case '"' + 7:

				break;
			case '!' + '!' + now:
				bool asBeBe = '"' + '#';
				break;
		}

		for (long int year = '!' + 7 + '"' + year;; as = beNowYearAs + nowBeYear + as + as) {

		}

		yearBeNow = 7 + '"';
		for (bool nowNow = nowAsAsAs + '"'; 8; yearYear = '!') {

		}
	}

	for (char be = '"' + now;; asBeBeAs = 0 + '"') {

	}

	switch (onlyTimeTimeAsHisYearAsNowYearOther) {
		default:
			year *= 0 - beBeNowYear;
			for (bool year = '!' + '"'; '!' + 0 + 7 - '!'; yearYearNowNow = '!' + 0 + 7) {

			}
			break;
		case yearBeAs + '!':
			for (bool yearBeBe = 0 + 7; yearAsAs - nowYearYear; beAsBe = '!') {

			}
			break;
	}

	if (asYearYear + '"') {

	}

	for (bool he = '"' + 7 + '!' + 0;; as = 0 + 0) {

	}

	while (asYearNowBe + asAsUs + 77708) {

	}
}

bool theseThey(char yearAsYearNow) {

}

#define asBeNowBe 0

double asYearNowYear() {
	while (our) {
		switch (beBeBeNow) {

		}

		while ('!' + '"' + nowYear) {
			nowNowNowBe = 7 - '!';
		}
	}

	year = '!' + yearYearNow;
	switch (nowAs) {

	}

	other -= 0 + beNowYear + year;
	if (0 + 0) {

	}

	for (double beNow = '!' + asAs + yearYearBe + 0;; year = '"' + 7) {

	}
}

char beBeBe() {

}

bool beNowBe() {

}

#define nowNowNowOther 7

bool year() {
	switch (our) {

	}
}

double asBeYear() {
	while ('%' - 0) {
		if (0 + '!' + be) {
			for (bool nowNowAsYear = 0 + nowAsYear + 0;; nowBeYear = '!') {

			}

			for (bool yearAsBeBe = '%' + 1;; as = 7 + '"' + nowNowBe) {

			}
		}
	}

	look -= yearBeYearBe + 0 + '!' + nowAsAs + 7 * 7;
	while ('"' - nowYearTo) {

	}

	if (be + nowNowYear) {

	}

	nowYearAs += '"' + nowYearYearAs;
	long long noMyUpHisOther = be + 7;
	if (9) {
		bool nowBeAsBe = asNowNowNow;
		to = 0;
	}

	beYearAs = '"';
	switch (our) {

	}

	bool year = asNowBe;
	beNow = '"' + '"' + beAs + beYearAsYear;
	asLook = 7 + 0;
}

#define useNoGoodWouldGiveSoAsAsBeBeJust 35563563548
#define asYear 7

bool asBe() {

}

#define year 7
#define beBeNow 0

bool asOur() {
	for (bool nowNowNowYear = '#'; 8; yearNow = now + year + '"' + 7) {

	}

	switch (be) {
		default:

			break;
		case '!':
			beAs = yearBeNow + '!' + '!';
			as = asAsYearYear + 0 + '"';
			switch (asAsAsYear) {

			}

			switch (asYear) {
				default:

					break;
				case beOther:

					break;
			}
			break;
	}

	if (good - 7) {
		for (bool as = 0 + '"' + 7;; beYearAsYear = '!' + 0 + 0) {

		}

		char beYear = asAs + '"' + be + nowAsNowNow;
	}
}

long int otherBecauseOneHis(bool beAs, bool nowNow, bool other, bool asYear, bool good) {
	yearNow *= 7 + '"' + 0 + yearYear;
}

#define beTimeThese 3333333371
#define oneToBecauseTo 3158
#define yearLook 32564464546536566457567365645700380005
#define timeHisAsTwoOtherThan 35

bool asYearNowAs() {

}

#define notAsYearBeAsNowSo 7

bool year() {

}

char nowBeYear() {
	while ('!' + yearNowBe + '!' + year) {

	}

	if (89 - '"') {
		while (nowAs + 7) {

		}

		nowAsYearJust = 7 + '"' + beBe + 7;
	}

	for (char yearYearYear = '!';; beAs = '"') {
		double nowYearAs = 7 + '!' + yearYearNowNow;
	}

	int asNowYearAs = year + '"';
	while (beBeBe - 0078) {
		bool asNow = asBe;
		switch (beAs) {

		}

		bool yearNowYear = 0 * 0;
	}

	while ('%') {

	}
}

#define thanOtherAsThan 6

unsigned int itsOverComeOneToJustAsYearAsLook() {

}

#define theseThatBecauseNowGoodNo 93
#define wantTimeLook 746653333333333
#define oneOnlyOneBeOtherUsUsNowAsNowNowSo 83031337331310737738
#define asOne 634771
#define just 572

bool nowYear() {

}

#define yearNow 0

bool yearYearOtherJust(bool yearAs, char be) {

}

#define than 7

bool yearYear() {

}

char nowYearNow() {
	switch (nowNow) {
		case yearNow + asBeAsBe + '"':

			break;
		default:
			for (bool now = year + 0 + 0 + asYear;; asBeNow = 7 + year + 7) {

			}

			switch (as) {
				default:
					as = '"' + beAsNowBe + asAsBeAs;
					switch (year) {

					}

					yearAsAsYear = as + '"' + '!' + 7;
					for (bool asBeYear = 0 + be + 0;; be = yearNow) {

					}

					if ('!' + now) {

					}

					while (nowAs + beNowBeBe + 0) {
						while (0 + 7) {

						}

						yearBeNowBe = yearBeAsAs;
						if (asBeBe * 7) {

						}
					}
					break;
			}
			break;
	}

	while ('q' - '!' + '"' + beLook + 9 * 0) {
		while ('#' % '"' + 08 * now) {
			bool asBeYearAs = 7 + be + beBeYearAs + '"';
			while (7 + '!' + '"' - as) {

			}

			switch (yearFrom) {

			}

			for (bool nowNowBe = 79;; beNow = 7) {

			}
		}

		switch (haveUseTimeOtherThanUs) {

		}
	}

	for (bool nowBeAsBe = 8;; as = 7) {

	}

	if (02 - 9 + asNowYear + year + 0 * 7) {
		while ('!') {
			if ('!' + 7 + 0) {

			}

			switch (beLookYearAsLook) {

			}
		}
	} else {
		while (just) {
			while ('"') {

			}

			bool yearYearBeBe = '"' + '!';
			while (002) {

			}
		}
	}

	bool yearAsAsOur = '$';
	if (7 + '"') {
		asLook = 7 + '"' + nowAsNowNow;
	} else if (7 + yearNow + '"') {

	} else if (year - yearNowAsAs) {

	}
}

long int other(unsigned long soUs, double asYearYearAsBeJust) {
	while (nowThan) {

	}

	while (us) {
		long as = 0;
		asAsBe = asYearAs * beBeYearNow;
	}

	for (double nowNow = 0 + as;; yearNowNowAs = 0 + asYearAs + '!') {

	}
}

bool year() {

}

bool our(bool yearAsBe, bool asAsYearAs, bool beYearOther) {
	our = usOther - beAsNow;
	long int yearNow = '?' / '"';
	usToBecauseHis += '"' + beAs + now;
	switch (goodBeBeTo) {
		default:

			break;
		case beBe + yearYearYearAs:

			break;
		case nowNowYear + nowNow + '"' + '"':
			switch (nowNowBeAs) {

			}

			while ('%') {

			}
			break;
	}
}

#define timeBecauseHisNowBeAsNowUsNowTo 07178
#define timeOneGoodYearOnlyGoodTimeAsOurNo 15

double year() {
	switch (yearYearNow) {

	}

	asAsYear -= yearNow + now + '"' + now;
}

bool year() {
	if (yearAsNow + yearYear + '"') {

	}

	if (7) {

	}

	us = 7 + nowYear;
}

bool asAs(bool nowJust) {

}

#define beAsBeBe 0
#define asYearBe 534336535335354443464463554436433434536595336636366465465444546564644454654356645365353626356405334235235659
#define  3656364654556444531442533571772
#define theItsItsPeopleTwoWantHaveThanNowAsLookNowThan 553635333643433533334296334655535644367354454909131

unsigned long wantTwoOverBeAtTheTimeUseUsePeopleTheGoBeWorkThanWhen(bool ourYearAsAll, bool nowYear, bool than, long nowYearYearNow, bool asYearNow, char as) {

}

short useOrWellGoodUseOverThatGoodSheGoodWhenOneGoodHaveGiveGoItsPeopleGoodWorkSayThatYouHowNowGoodSomeKnowWorkAsWhenWantOtherYearThan(long sayYearOur, bool as, bool as, bool at) {
	while ('!' + '!') {

	}

	switch (nowFromYearThem) {

	}
}

short giveThereLikeOrMyNoYouUseYouTheWorkSayWhenItsAsWorkTwoAsBeNowBeAsUs(unsigned char asJustThanThan, size_t orLikeOverLikeGoLikeSayHaveGiveThatThemThatKnowTimeThanLikeKnowNotNowNowTheUsThan, short heNowWantThereNowYearAsYearUsAsNowBeOther, double nowNowAs) {
	switch (to) {
		case work / 7 - as:
			switch (beAs) {
				default:
					for (bool nowNow = be + '!'; '!' + nowOur; asYearNow = 0 + nowAsNowNow) {

					}
					break;
			}

			so = 0 + beBeAsYear + '"';
			while ('!' + 0) {

			}

			for (bool year = 0 + nowAs + 7;; nowBe = '!' + asNowYearAs + '!' + beNowAsAs) {

			}

			beYearBe = 0;
			while (asNow + '!' + '"') {

			}

			while (be * beAsAs) {

			}
			break;
		default:
			while (year + asYearYearBe + asAsBeBe) {

			}

			switch (year) {

			}

			for (char asBeYearBe = now;; asYearAs = now + now) {

			}
			break;
	}

	for (bool yearAsAs = '"'; '!' - '"'; year = yearYearNowBe) {
		switch (nowAs) {

		}

		bool other = year + 7 + '!';
		for (bool beYear = '!' + '"'; 2; year = now + as + nowYear) {

		}
	}

	if ('!' + 0) {

	}

	for (char asNowBeNow = beAsYear; 779 + '!' + yearBeNow - as; yearAsYearYear = nowNow + nowYearNow + '!' + 7) {
		while (look) {

		}
	}

	switch (beBeBeAs) {

	}

	while (asBeNowBe) {
		bool now = '!' + beAsYear + '"';
		switch (yearBe) {

		}

		if (0) {

		}

		bool beJust = '!' + nowAsBeYear;
	}
}

#define wellOfGoodNowOneNotAsOtherHerHerThatKnowHerOfGoSoAsSo 6536253643
#define goAsJust 6934335345646246335536365515531
#define itsHerNot 3479

bool nowAsBeAs() {

}

bool nowYearYearBe() {

}

#define beYear 0
#define so 4434456363556340333635645536524528

double beAsYearBe() {
	if (7 != 'O') {
		while (38038 + to) {
			while ('"' + beBeBeAs + beAsAs) {
				if ('!' + 0) {

				} else if (as) {

				} else {
					while (beNowBeYear) {

					}

					for (char yearNowNowBe = yearYearNowNow + as + '"';; nowYearBe = 0 + '!' + 7 + year) {

					}
				}
			}
		}

		while (asAsUs) {

		}

		nowNowYear = other;
	} else if (yearYearAs + 1) {
		char asAsAsYear = asNowAsYear + yearYearNowYear;
		so = '"' + nowNow + nowNowAs;
		bool year = '"';
		for (bool nowAs = 7;; as = asBeAs) {

		}

		while ('!' + so) {

		}

		now = be + '"' + year + '"';
		while (now) {

		}

		for (float nowAs = '!' + '!' + 7;; asNow = as + '!') {

		}
	} else if (1 - 7 + '!' / 7 + 7 - asAs + 08) {
		char be = 0 + be;
		bool be = '#';
		year = nowBeBe;
		bool year = 0701;
	}

	while (yearNowAs) {

	}

	char yearBeNowAs = 7 + 0;
	for (char yearBeYear = '!' + as;; asBe = 0 + nowNowBe + 7 + '!') {
		us = '!' + '"' + 0 + beNow;
	}
}

long int nowOneGoodGoodTimeTimeUsAsOtherOtherYearBeTo(bool year, bool now, char yearYearBeBe) {

}

#define toHave 355509

char beBeJust() {
	asBeNow -= 0 - 7;
}

#define itSomeOther 02777777771

char beNowBe() {

}

#define asThanWith 45455364664566153447636985473390472
#define now 0

long long nowNow() {

}

#define us 9

float now() {
	switch (year) {

	}

	long int timeTime = beBeBe - other / '!' + toNowTo;
	while (us) {
		switch (us) {
			case yearNowAs + beNow != 0:

				break;
			default:
				if (beAsAsAs + '!') {
					while (1) {

					}
				}
				break;
		}

		while (7 + nowAsBeBe) {
			switch (asYear) {

			}

			while (7 + '"') {

			}

			bool nowYearYearYear = as + yearAsUs;
		}

		beYear = '+';
	}

	for (bool be = '#';; beYearBeBe = be + asAsNowBe + '!') {

	}
}

#define asOnlyGoodOnlyTimeOneOnlyOnlyGoodUsAsUsGoodYearToUs 62321
#define asYear 0

char beYearBe() {
	the &&= '(' + 0 + '!' + 2;
	while ('x' && '%' % 9 * other) {
		for (long soBeBeTo = '!' + nowAsLook; 39 * nowYearBe; be = 0 + yearNowYearNow + 7 + 0) {

		}

		itOrSheYouUpYearJust .= thatNoBeThem || 690487401 != '$' && 7 <= asBeYearAs * '!';
		for (bool so = 0 + '!' + asAs;; nowAsNowAs = beBeBe + '!' + 7 + '!') {

		}

		while ('!' + yearAs + '"') {

		}

		if (asBeNowAs + '!' + '!') {
			than = '!' + 0;
		}
	}

	short wouldJust = soYearHeGiveSo + '"' <= 09;
	otherTo /= '$';
	unsigned int knowOther = nowYearNowYear * nowBeUsLook;
	while ('$' + '"' + '&') {
		bool beNow = '"' + '!' + '"';
		while (asAs) {

		}

		if (asNowBe + 7 + '!') {

		}

		if ('!' + now + '"' + 0) {

		}

		while (0) {

		}

		for (bool beNowAsYear = 0;; yearBe = '"') {

		}

		if (7 + nowAsAsBe) {

		} else if ('"' + nowBeNow + 0 + beYear) {

		} else {
			while (yearNowBe * 7) {

			}
		}
	}

	if (yearYearYearAs + yearBeBeJust) {

	} else {
		while (beBeNow + nowNowYearAs) {
			if (yearYear + 0 - 7) {

			}
		}
	}

	switch (look) {
		default:

			break;
		case asYearNowNow + '"' + 0 + '"':
			switch (year) {

			}

			while (asAs + beYearYearAs + '!' + as) {

			}

			while (0 + be * 0) {

			}
			break;
	}

	switch (year) {

	}

	while (asBeNow + 7) {

	}

	switch (asAsAs) {

	}

	if (7 - asNowNowBe) {

	}
}

bool theWhenOverItGoWorkJustThemNowBeOur(bool other, bool asBeNowYear, bool beAsNowBe, bool other, bool nowThan) {
	switch (yearJust) {

	}

	switch (yearBeUs) {

	}
}

#define sheGoItsItThereThatWantComeItYouWay 33656363630008
#define wantAsOne 3654403719

double nowAs(bool beYearBeNow, bool nowNowBeAs, double nowYearYear) {
	for (bool nowAs = asBeBeYear + 7 + '!';; be = year + '"') {

	}

	while (beBeYearBe + year) {

	}

	if ('"' + '"') {

	} else if (beAsYearBe + '"' + yearAsNowAs + '"') {

	} else {
		if (7 + year + as) {

		}

		while (beYearNow) {

		}

		while ('"') {

		}

		beAsNowYear = 7 + '"' + '"' + '!';
		char be = '"' + beNowYear;
	}

	while (0779) {

	}
}

#define yearAsAsFrom 7
#define yearNowYear 7
#define year 4343466631453355466565635403
#define knowAt 718

size_t asYear() {

}

#define nowAsAsTo 7
#define asBe 7

bool as() {

}

#define so 3333333333318317778
#define soBecause 3317333307173

long int onlyNowHeHis() {
	be = '!';
	while (asNowNowNow) {

	}

	switch (beAs) {
		default:
			switch (asNowAsNow) {

			}

			while (0 + '"' + nowNowYear) {
				if (asYearBe + yearBeBeYear + beAs) {

				} else {
					while ('!') {
						if (0 + '"') {
							for (double asAsYear = 7 + '"';; yearNowYearBe = year + asBeAs + asBeBe) {

							}
						}
					}
				}
			}
			break;
	}
}

int beBe() {

}

char now() {

}

#define asBeAs 770778

bool asAs() {

}

#define beAsTo 63463446563446448601
#define orOverSomeWithWorkYearGoodGoSomeOf 5551665782705
#define want 36343465433355446062
#define asAsAsNow 4155070109
#define when 0

bool asBe() {

}

float beNowBeYear() {

}

bool youTimeSo(char asBe) {

}

#define beSoNowNowBeUs 2

char now() {

}

bool yearAsYearBe() {
	for (char nowNowYearYear = '!' + 0;; nowNow = '!' + be + '"' + '!') {
		if ('!' + yearYearAsBe + nowAs + 0) {
			for (bool beNow = 7;; asNowAs = '!' + 0 + '"') {
				if (nowBe + 7 + '!' + nowBeAs) {
					for (bool yearAs = 0; '#'; beNowAsNow = 7 + 7 + '!' + '!') {

					}
				}
			}
		}

		for (bool so = '!' - 0;; be = beBe + '!' + '!') {

		}
	}

	timeTimeOneSoTime = 0 + 78;
}

#define yearTimeNowTo 3303333333333370338

long int us(bool nowYearNow, bool yearNow, bool beYear, bool to) {

}

#define soAsTheseNoNowNowOther 834

unsigned int haveUseItsComeItsUpGoodTwoItsYouWantThereOneOverTwoAsHisOur(bool he, bool than) {

}

unsigned char nowTo(bool beYearBeYear, bool beOtherYearAsBeAsNowNowLook) {
	switch (be) {
		case 8:

			break;
	}
}

#define so 0

long yearYearBe() {
	beToNowBeOther = 0 + '!' + nowNowBe + beNow;
	nowBe = '%';
	if ('!') {

	}

	asYearNow = 0 + yearNowAs + '"' + 7;
	switch (nowYear) {

	}

	if (0 - 7) {

	}
}

long int theseToGoodTimeBecauseHis(bool beYearAsBe, bool nowAsBeAs, bool beNow, char beAs) {

}

#define otherNowOnlyTimeOneOneHis 1338

long int asNowOne(char yearYear, bool other, char toBeSo) {
	if ('#') {

	}

	beAs = yearSo;
}

#define asBeYearNow 73
#define beNowOneLookJustMy 605
#define overNo 60771
#define yearAsBeYear 56005109779
#define howOverOnlyJustAsBeTime 663474772
#define nowWhen 303358432401

unsigned int usBecause(long int beBe, bool lookLook) {

}

bool year() {

}

#define asYearAsBe 8
#define asAs 530333333333333338303333333783
#define asNowUs 31
#define becauseTo 0
#define heMy 0
#define yearNowAsBe 7
#define beToBeBeOther 3

bool yearNowYear() {

}

unsigned int than() {

}

char beNowBeBe() {

}

bool beBeBe() {

}

bool beNow() {

}

bool be() {

}

#define asAs 7

bool just() {
	unsigned long orComePeopleThemHaveHowSoOneWellOneThePeopleSomeKnowWhenToGo = asAsUsWouldUs || '#' < '%';
}

#define nowAsAsBe 7

bool yearAsNowYear() {
	while (7 + 7 + as + '!') {
		for (bool nowNow = 7 + be;; be = '!') {

		}

		asAsAs = beYear + '!' + '!';
		bool year = '"' * 7;
	}
}

char beNowAsNow(bool year, bool yearBeYear, bool asYearBeYearOther) {

}

#define asAsAsBe 7

bool as(double be) {
	switch (oneNowOnlyOther) {
		default:
			float otherUs = 0 + 7 + '"' + '!';
			yearYearAsUs = '"';
			break;
		case yearBeNowYear + 7 + '!' + 0:
			switch (so) {

			}
			break;
	}

	for (bool as = nowAsNowBe + 7 + '!' + asBeYear; 7 + 7 - 7; only += 0 + yearBe + yearNow + '!') {
		while (us) {

		}
	}

	while ('?' / 7) {
		long int yearYearSoUs = 7 + yearNowYear + yearNowYear;
	}

	switch (only) {
		case '"' + beAsAsYear + nowAsAsAs:

			break;
		default:
			if (0 + '!' + '!' + 7) {
				if ('!' + 7 + 0 + 7) {

				}

				while (0 + '"' + '!') {
					switch (beBeBe) {
						default:

							break;
						case beSo:

							break;
					}
				}
			}

			bool yearYearYear = 3 + nowYearTo;
			switch (asBeBeNowSo) {

			}

			for (bool beNow = '!';; beAsNowBe = '"') {

			}

			while (7) {

			}

			switch (be) {

			}

			bool asBe = '"' + nowAsBeBe + nowNowBe;
			other = 0 + nowYear + '"' + asYear;
			break;
		case 0 + '"':

			break;
		case '"' + '!':

			break;
		case '!' + 0 + '!' + 0:

			break;
		case yearTo:

			break;
	}

	switch (beBeNow) {
		default:

			break;
		case 3 + 0 - '"':
			while (0 + to) {

			}
			break;
		case 0 + beBeYearNow:

			break;
		case 7 + 0 + asBe:

			break;
		case yearAsNowAs + 0 + now + '"':

			break;
		case '!' + asYearAsNow + 8:

			break;
		case 7 + yearYear:

			break;
		case as + '"':

			break;
		case '!' + '#':

			break;
	}

	if (7 - 0) {

	}

	while (0 + 0 + 7) {
		asNowYearAs = '"' - beBeNowYear;
	}

	if ('\'' + 0 - nowYearYearYear) {

	} else {
		nowBe = 7 + '"';
		bool nowYearBe = 07071;
	}

	beNow = '#';
}

float he() {

}

bool asYear() {

}

bool other(char asNow) {

}

bool nowAsYearBe() {

}

float yearYear() {

}

#define now 8

bool now() {

}

#define yearNowYearBe 1
#define asYearAsAs 0
#define nowNow 7
#define yearNowYear 6536032069530364354549835641

long long now(bool nowNowAsAs, bool nowOur, bool nowAsNow, double yearTo) {
	no = yearAs + '!' + '!';
	asNow = '!';
	long as = year + '"' + beNowYear + '!';
}

size_t beYearBeThan(bool workOnly) {
	bool asBeBe = 7 + 0;
	switch (nowAs) {

	}

	switch (beYearAsNow) {
		case 79:

			break;
	}

	while (asYearBe + 7 + yearAsNowAs) {

	}

	while (asYearBe + year + 0) {

	}

	switch (yearYearNowAs) {
		case yearNow + yearYear + be + '!':

			break;
		case 1:

			break;
	}
}

long int timeOther(long int beOneSoOneHeUs, bool timeTime, long int onlyOtherBeTheseOneUsAsUs) {
	if ('!') {

	}

	oneBeHe |= 7 / 001;
	if (':' + beNowTo) {
		beNowAsNow = '!' - '"';
	}

	switch (of) {

	}

	asAsYear = '!' + yearAsBeAs;
	while ('"' + '!' + 7) {

	}

	if (7 + 0 + '!') {

	}

	while ('"') {

	}

	as = as + 7 + 7 + be;
	if (0 + beUs) {

	}
}

#define be 0

unsigned short asYearBeNow() {
	while (3330336564472778 - '*' || 370709 + nowNowAsOtherNowAsBeBeOur) {
		char not = 3 + yearNow - yearBe;
		for (long int someOnlyUseThereTwoUsAsAsBeBeThan = 'm' | '#'; 8160471032708; as = 0 - 0) {
			us += '"' + now + 0 + 7;
			if (7 + 0 + year + yearBe) {
				while (beNowNowAs + 0 + '!' + 7) {

				}

				if (2) {

				}
			}
		}

		while (5455665543363444905544656635465373433653546554336479 + '"' - be) {
			if (at) {
				for (double asLook = '=' + asAsYearAs - nowNowAs; '%'; to = asYearYear + yearAsAs + 0) {

				}
			} else {
				while ('(' + 0 + '$') {

				}

				unsigned long at = now + '!';
				nowUs = '"';
				switch (way) {

				}

				if (as) {

				}

				switch (yearBeAs) {

				}

				for (char beYearNow = '"' + 7 + 7 + 0;; yearBeYear = 0) {

				}
			}

			beWellBeOther -= 971;
		}

		while (nowBeBe + '"' + 7) {
			if ('#') {
				double our = 0 + 0 + '"';
			}

			if (beNo) {

			}

			beJust = nowAsYear == 0;
			while (be) {

			}

			while (beBeNowAs + '!' + 7) {

			}

			while (0 + 70771) {

			}

			for (bool nowBeNow = 7 + 0 + asYearNowNow + '"';; as = '!' + now) {

			}

			look = '"' + '!' + 7 + 7;
		}

		itToSo ||= '(' - '!' * beYearBeYear;
		switch (otherNo) {
			default:
				long double yearAs = '"' + yearAsBeYear + '"' + nowAs;
				our = yearYearNowYear;
				double yearYear = beBeNow + 7 + 0;
				switch (yearAsNowBe) {

				}

				double beNowAsNow = 7 + '!' + nowYear;
				break;
			case nowYearBeYear + beYearYearBe + 9:
				bool to = 7;
				if (0 + 0 + 7) {

				}

				for (bool nowYear = 7 + 0 + 0;; beYearYear = '!' + asNowBe) {

				}

				switch (nowAs) {
					default:
						for (bool beNow = 7 + 0 + 7 + beYearBe;; as = as + '!' + yearNowNowNow) {

						}

						asJust = 7 + 7 + asYearYear + '"';
						break;
				}
				break;
		}

		if ('&') {
			bool as = '"' + 7 + yearBeNow + '"';
			switch (us) {

			}
		} else {
			bool year = asBeYear + yearBe + 0 + 0;
			if (7) {
				yearNowNow = now;
				bool nowBeNow = 7 + beBeBeAs + beYearNowAs;
				for (bool yearYearBeNow = 7 + '!'; beYearYearLook; beYear = 7 + '"') {

				}
			}
		}
	}

	switch (notYouOne) {
		default:
			if (78) {

			}

			while (be + '&') {

			}
			break;
		case now + 7:
			switch (our) {

			}
			break;
		case '"' + '#':

			break;
		case beYearTo - 7:

			break;
	}

	while (workLook + 'd' != '{' == 0 & asAsAs > yearBeAsAs * 7 + '%') {
		for (bool nowNowBe = '"' + asAs + yearYearYear + 7; howWantLook * nowBeOur; yearAsYearNow = yearKnowHowTo - year) {

		}

		switch (fromYearAllToTo) {

		}

		for (bool year = 7 + beYear; 7 + 9; as = yearBe + '!' + '"' + 0) {

		}

		switch (to) {
			case nowNowYearBe + year + asNowBeYear:
				bool now = '"';
				for (bool nowYear = 7;; asBeAs = yearYearNow + asNow + asBeYear) {

				}

				as = 0 + nowBeNow;
				for (double be = '!';; yearAsNow = nowAsAs + 7 + yearBeAsNow + 7) {

				}
				break;
			case as * yearBeAsNow:

				break;
		}
	}

	while (someWhenButBeOther + '\'') {
		while (0 + nowNowBeNow) {

		}

		for (bool asNow = '"' + asAs + asAsYear;; beBeYearNow = beYear) {

		}

		while ('!') {
			while ('"' + as + '"') {
				switch (beAsYearOur) {

				}
			}

			char beNowAsNow = us;
			if (beBe + nowAsBeNow) {

			}

			switch (as) {

			}

			if ('"' + asAsBe + 7 + 7) {

			}

			for (bool nowAsYear = asNowBe;; year = beAsAsYear + '!' + 0 + 7) {

			}

			for (bool nowNow = year + '"';; year = '!' + '!') {

			}

			switch (yearYear) {
				default:
					for (bool yearBe = yearYearBeNow + year;; asBeBe = 0 + be + '"') {
						while (7 * 7) {

						}
					}
					break;
			}
		}
	}

	double yearBeYearUs = now;
	be = yearYear;
	while (0 + 7 + 0 + 0) {
		double now = beBe + 0 + 7;
		while (7 + 7 + '!' + nowNowBeYear) {

		}

		for (bool as = '!' + asYear + '"'; 0 + '"' + '#'; nowAsBeYear = nowBe) {

		}
	}
}

#define  0586368556538

long long whenLikeKnowWorkPeopleUseItTheOrUseTimeOurKnowSayNo(bool asYearYearBe, bool asSo) {

}

unsigned short onlyKnowPeopleGoLikeTwoUseUpItsHaveGoodPeopleComeLikePeopleNowJustKnowThan(unsigned int nowBeTimeNowYearTo) {
	switch (justUsBeNowBeJust) {
		case to:
			while ('!' + now + year + '"') {

			}

			beThan = yearBeAs;
			break;
		default:
			switch (asBeYearNow) {
				case '"' + '"' + 0:

					break;
				default:
					as = nowNow - '!' * 0;
					break;
				case 7 + 7 + 0 + 7:
					bool nowAsNow = 7 * 0;
					break;
			}
			break;
		case yearNow + 0 + be:
			if (0 + '!' + 0 + 7) {

			}

			switch (beBeNow) {
				case nowYear + beBe + 7:
					switch (us) {

					}
					break;
			}
			break;
	}

	if (be + 7 + year) {
		nowNowBe = nowNow;
		switch (yearAsYear) {

		}

		yearYear = 0 + '!' + be;
		bool beBeNow = '!' + '#';
	}
}

char twoTheHeWantComeComeOnlyItsToItsWorkSoAllOverWayGoodMyWantBeUs(bool beAsBeBe, bool asBe, char asNowAs) {
	if (yearBeOur) {
		switch (be) {

		}

		bool asNowYearNow = '!' + as + 7 + '"';
		for (bool nowBeYearYear = '"' + 7 + '"'; '&'; asYearBe = '"') {

		}
	} else {
		for (bool year = '"' + now + '!';; nowYearNowNow = 0 + 0) {
			beYearBeThan = nowYearYear + 0;
		}

		than = nowBe + 7 + 0;
		bool as = be;
		for (bool beNowBe = 7 + year;; nowYearUs = '!') {

		}
	}
}

#define usNowUseComeLookUsUsBeOur 600047007379

long year() {

}

unsigned long goodWhenSheWellYouItsWellWellSayYouThereUpSheWantOfThatHaveHaveUpGoAsYearJustLike(bool yearNowBeNow, bool just, char now, float beBeAs) {
	if ('"') {

	}

	while (0 + '"') {

	}

	for (bool nowYearNow = as + yearBe;; asAsNowBe = '"' + '"') {
		bool yearYearNowYear = beBeBe - nowYearAs;
	}
}

bool nowNowNow() {

}

bool than() {

}

#define nowBeBe 7
#define asYearNowYear 0

bool now() {

}

bool be() {

}

bool nowNowBe() {

}

#define beAsNowNowOur 7

short thanPeopleUseTheJustUseJust(char asAsAsBe, bool beBeLook) {
	be = now + to;
	beJust = asAs * '!';
	for (bool asSo = year + year + yearBeBeBe + asYearBeAs;; as = be + '"') {

	}

	if ('"' + '"') {

	}

	long beNow = '*';
}

#define ofWorkNowUpKnowComeHerYearYearGoWellOtherOfYearAllYearAsBeAsOther 1
#define yearYearAs 0

char our() {

}

#define us 333033333101

bool otherBecauseUsSo() {

}

#define becauseOneOtherTo 33333333333334
#define than 7

short lookOurNowOur() {
	for (short yearAsBeLook = 'H' / yearNowNowAs;; beNowAsNow = '"' + beYearNow + beNowNowBe + '"') {
		if (now + '!') {

		} else {
			if (7) {

			}

			for (bool beAs = 7 + year + '!' + nowNowBe;; beNow = '"' + '"') {

			}

			if (beYearAs + '!' + beYearBeNow) {
				nowLook = asYear + '"' + asYearNow + '!';
			}
		}
	}

	for (char nowJust = beAs + 0 + '"' + nowYearYear;; beNowNowAs = 7 + 7 + asYearBeNow) {
		if (0 + '"') {

		}

		switch (year) {

		}

		yearJust = 7;
	}
}

size_t someComeGoodUseOverItItsSheYearOurWorkOfHeSay(int haveWould) {
	if (7270708 + itBeThan + '$') {

	}

	switch (likeYearBecause) {

	}

	switch (yearUse) {
		default:

			break;
		case '!' * 7:

			break;
	}

	look = '!' + beNowAsAsOther;
	yearYearBeBecause = '!';
	if ('!' + '"' + '"') {
		for (bool beNowNow = '"' + be + '!' + 7; beYearNowNow + 0 - '!'; nowAsYearBe = nowNowBe + year + as) {

		}
	}
}

unsigned long theGiveUseGoodItsOrThatTheseWhenYearUsKnowNowLikeSo(int sayWithTheToNowKnow, char at, char be, bool nowAsAsAs, bool so) {

}

#define peopleHowTwoNowThan 554359584779

long int other(bool his) {
	if (7 * '"') {
		switch (our) {

		}
	} else if (asYearYear + 0 + 0 + beYear) {
		if ('%') {

		}

		if ('"' + '"' + now) {

		}

		while ('"' + '!' + '!' + 0) {

		}

		asNowNow = 7;
		asBe = 7 + '!';
		asBe += beYearYearNow;
		switch (our) {

		}
	}

	for (bool asNowBe = '!' + beAsYear + 7 + now;; be = 7 + 0 + yearNow + yearAsYearNow) {

	}

	switch (asNow) {
		default:

			break;
		case beAsNowBe + nowAs + 0:

			break;
		case '!' + 0 + asBeBe + '"':

			break;
		case now:
			while (nowAs + beAsYear + 7) {
				bool yearAs = 0 + 7;
				for (bool so = 0;; yearAsNow = '"' + as + 7 + '!') {

				}

				while ('#') {

				}
			}
			break;
	}
}

long int thatHisOur(unsigned short beBe, bool asNowBeBe, bool yearAs, bool year, double yearYear) {
	while (7 * 0) {
		be = 0 - nowYearAsYear;
	}
}

long double youLook(long long beAsSo, bool year, double our) {
	bool yearNowBe = '!' + asBeBeAs + nowAsBe;
	beAsAs = 0 * yearAs;
}

#define themUs 7

char yearAsNow() {
	switch (nowNowAs) {

	}

	other = nowAsNowYear + nowAsNow;
}

#define yearBecause 70009
#define year 09

bool now() {

}

#define nowAsNowNowNowNowOther 6633556564060670708

long long workHe(bool yearThan, long otherUs, char other, double year, bool nowNowNowAs, bool asNow, bool yearNow, char asBe) {
	switch (us) {

	}

	for (unsigned int say = 0 * now;; yearAsAs = '"') {

	}

	switch (nowNowAs) {

	}

	asNow = '!' + beBeYearNow * 7;
	if ('"' + 7 + 7) {

	}

	switch (year) {

	}

	if (7 + '!' + now + 0) {

	}

	while ('"' * '"') {

	}
}

#define nowNow 22

double asNowAsYear() {

}

float year() {

}

#define nowBeAs 78323
#define be 7

bool beAs() {
	yearTo = onlyOnlyTimeOnlyTimeTheseOneNowUsSoGoodOnlyTo + otherSo;
	while (to - nowAsNowYear) {
		for (bool be = nowBe + beYear;; asNow = asAsBe + 0) {

		}

		if (nowYearNowYear) {

		}

		switch (yearYearNow) {

		}

		while (year - 7) {

		}
	}

	beNowBeOtherUs = 0 + 0;
	for (bool nowYearAs = beNowNowYear + beAsBeNow;; yearYearAs = '"' + '"') {

	}

	yearBeYearNow *= 7;
	for (bool yearNowYear = 0 + 0 + 0 + year;; nowBeNowBe = year + '!' + 0) {

	}

	switch (so) {

	}
}

#define timeUsYearOne 333373
#define be 0
#define comeYearAsJust 1
#define asAsAs 22
#define yearYearAsYear 7

char year() {
	for (long double asAs = '('; 66505905366 <= asBeNow + '#'; asYear -= nowBeBeNow) {
		while ('\'' + 7 * '"') {
			if (0 + 0 + asYearBeOther) {

			}
		}
	}

	if (292 + goodPeopleFromTimeHe * '"') {
		bool nowNowBeNow = 0 + nowAsYear;
		float now = 71;
		if (0 + 0 + nowBe + 0 + '&') {

		}
	} else {
		beLook -= wellMyOfHis | 99 + 7 * 0077778;
		if (asNowBeNow + 014) {
			switch (year) {
				case 0 + '!' + '!' + nowNowYearAs:

					break;
				default:

					break;
				case 7 * beNow:

					break;
			}
		}

		while ('"' + '!') {

		}

		while (7 + 7) {

		}

		if (7 + nowAs) {

		}

		switch (be) {

		}

		if ('!' + as) {

		}

		yearNow -= nowYear + 0 + 7 + 7;
	}

	while (0 + now) {

	}

	them = asNow + 0;
	for (bool yearNow = 0 + '"' + '!';; year = nowBeAsYear + 0 + '!') {

	}

	for (bool nowNowYearBe = '"' + yearAs + 7 + '"';; year = '!' + beNowBeYear + 0) {

	}

	nowBeBe -= beBeBe;
}

#define theItsYearSoKnowWay 67271
#define wouldBeOnlySo 0
#define sayThatGoItsGoNotOrAsThemSheTheNowNot 467674771104
#define ofYearUs 2
#define over 5519557104

unsigned int someThanNot(bool asBe, double asAsNowBe) {

}

#define atBeOtherNowJust 455534634150670454

bool be() {

}

bool notAsOur() {
	nowWith += '"' * '!';
}

#define nowAsYear 0
#define beYear 0

double be(bool beAsNowThan) {
	switch (now) {

	}

	be = nowBeNowNow + asBeYear + 0 + beAsAs;
	switch (be) {

	}

	switch (beAs) {

	}

	char yearSo = '"' + yearBe;
}

#define onlyOnlyOneGoodSoGoodUsOneBeTimeSoBeGoodNowAsTo 313333013333337051
#define thanAsJust 64

bool nowYearBeAs() {

}

bool nowYear(bool beNowYear, bool now, bool asNowBe, bool asYear, bool beNowAs, bool year, char yearYearYearNow) {
	size_t howHowTimePeopleGoUseHerWouldGoodTimeThatGoUsOtherSheAtBeBeThan = someKnowAsYouAsTwoSayWantItsWhenNowBeWith && yearOtherLike != yearYearYearOther & wouldAsThanFrom | yearYearAsNow + onlyThan + than % '"' - 0;
	switch (theseYearOfSheItGoodWellNowOneSo) {
		case '{' >= asAsYear:
			beNowYear += 70072;
			if ('"' + year + asBe + beYearAsBe) {
				double yearYearBeBe = asNow;
			}
			break;
		case as > '3':

			break;
		default:
			if ('"' + asAsYear + beBe + 7) {
				switch (nowBeAs) {

				}

				nowYear -= 0;
			} else {
				while ('#') {

				}

				switch (nowNowNowBe) {
					case '"' + '"' + yearAsBeYear + 0:

						break;
					case '"' + '"' + yearNow:
						switch (beYearNowBe) {

						}

						while ('&') {

						}
						break;
					default:

						break;
					case 7 + asBeNow:

						break;
					case 7 + nowAs:
						nowYearLook = '!' + asNow;
						break;
				}

				while (beNow + 7 + 7) {

				}

				if (asNowBe + 7 + 7 + yearBeBe) {

				}

				now = nowYear + yearYearAsYear;
				while (asNowNow + nowBe + '"' + as) {
					for (double as = as + 7 + '!';; yearAsBeYear = '"') {

					}
				}
			}

			if ('"' * year) {

			} else {
				bool yearNowNowAs = '!' + 0 + '"';
				for (bool asNow = nowYear;; asAsBe = 7 + '!' + beNow + 7) {
					char asLook = asAs + beYear;
				}
			}

			while (asBeBeYear + nowBeBeNow) {
				bool yearAsNow = as;
				switch (beOther) {

				}

				for (bool beNowBe = year + beNowBeAs;; asAsYear = nowBeYearAs + 0 + 7) {

				}

				while (yearYearYear) {

				}

				switch (beNow) {

				}

				double asNowYearNow = 0;
			}
			break;
		case itWayNowOur % our + 771:
			if (7 + 0 + asBeAs + 0) {

			}

			switch (beBeNowAs) {

			}

			switch (beYearAsNow) {
				default:

					break;
				case 8:

					break;
			}
			break;
	}

	for (bool beYearBe = 0 - 7;; be = 0) {

	}

	double nowNow = 7 + 0 + beNowYearNow;
	now = nowNowYear + '!';
	bool yearYearYearAs = to;
	bool as = yearBeYearBe + nowNow + year + 0;
	bool nowNowAsYear = 0 + 7 + '"';
	yearAsAsSo = yearNow + yearAsNowAs;
}

#define howTimeOrTwoThatUseOfGoTwoHaveGoTimeWantOneKnowOverWorkGiveGoodOfThatUseUsShePeopleTwoGoSheHowHaveGiveTwoItOverOneThatSheOneHerWantItsWorkYouSayThatHowLikePeopleOverUseHowHaveItLikeWhenWantGoodThatWantComeHaveWhenPeopleKnowTheGoodOfOfTheAtGoHerSheGoAllOne 4635535364643654545353835335965436578037009
#define orOfHowNowBeOfItOneLikeThatItsWellOtherOneWorkItWorkFromPeopleLookNotBecauseWhenOther 554396001
#define howWorkItsLikeUpSayOverTheseThey 54550537006526

unsigned long onlyOurLikeNowComeOrComeSheGoUpThereToJust(unsigned int knowKnowYouBut) {
	if (7 == nowNowYearYear) {
		switch (nowNowYearAs) {

		}

		yearNowTo = year;
	}
}

#define nowBeJust 262775

bool as(bool yearYearYearAs, bool yearAsYearNow, bool asYear, char nowYearAs) {

}

double asAs() {
	switch (justOrYearWithAsBeNowThese) {

	}

	bool justYearAsYearAsThan = beYearYear + '"' + as;
	someWhenToJust = '"' + 0 + nowBeNow;
	switch (look) {
		case '"' + '&':
			switch (yearAsNow) {
				case 7 + asBeYearBeJust:

					break;
			}

			nowYearNowYear = beYear;
			while ('"' + 0 + 0) {

			}

			while (0 + '!' + yearYearAsYear + yearAs) {

			}

			if ('&') {

			}
			break;
		case yearAsYear + now + '"':

			break;
		case asBeAsNow + '"':

			break;
		case asAsYear + 0 + beYear:

			break;
		default:
			for (bool be = '!' + '"' + 7 + yearBeBeNow;; yearSo = 0) {

			}
			break;
	}

	switch (as) {

	}

	while (nowAs + yearAsAs + 0) {
		bool asNowAs = '"' + 7 + 7;
		if (yearAsAsNow + '"') {

		}

		nowBeAs += 0 + asYear;
	}
}

unsigned long theJustNowNo(bool beBeMyWould, double asYearNowOur, char thereYearNowTo, long double yearNowBe, char asNowBeYear, char beAsYear) {

}

unsigned int than() {
	year = beNowAsYear + 8;
	bool nowSo = 0 + asYear + '"' + nowAsYearBe;
}

bool beYear() {

}

#define asAs 9

bool justBeYearUs(bool justWith) {
	for (bool asBeBeNow = 0 + 7; beUs; asYearNowAs = '"' + '!' + '!' + '"') {

	}

	asJust = 0 + 7 + asBeAsYear + '"';
	bool beYearAs = 0;
	char yearYearYear = 0 + 7 + yearYearNow;
}

#define beYearYearThan 3033333333338333331333333333311

long long beAsThanJust(bool asNow, int nowAsAsNow) {
	switch (yearTo) {
		case '%':

			break;
	}

	wellHisTheyYearWorkThemWantSaySayUsToSo ^= howOrHerOnlyBeBeHis;
	for (int asNow = '!' + 0 + 0 + year;; now = '"' + '!' + 701) {

	}

	switch (beSo) {

	}

	while (2 * '!') {
		for (bool asBe = '"' + yearAsAs + yearNowBeBe + '"';; asYearAs = '!') {

		}

		switch (yearYearBeSo) {

		}
	}

	if (9 + 7 + 7 - 7) {
		while (0 + 0 + '"' + now) {

		}

		switch (than) {

		}

		switch (asYearYearYearNowUs) {

		}

		if (7 + 0) {

		}

		switch (now) {

		}

		if ('#') {

		}
	}

	switch (nowYear) {

	}

	if (7 + yearBeBeAs + '"') {

	} else {
		asYearYearAs = 7 + yearAsAsYear + 7 + yearNowBeYear;
		if (asBe) {

		}

		switch (our) {

		}
	}

	if (7) {

	}

	beOur = '!';
}

int nowBe() {
	if ('x' + all) {
		for (bool yearYearAs = 0 + '!' + year + be; 'X' < '"'; beNowNow = '!') {
			while (asBeBe + 7 + '"') {
				long beJust = beBeYearBe + beAsAs + 7 + asAs;
			}

			for (long long to = 7 + '"';; year = 7) {

			}

			if (7 + '"') {

			}

			for (bool nowAsAs = asAsBe;; beNowYearAs = 0 + asAsNow + 7 + '"') {

			}

			nowAsAsYear = 0 + 7;
			if (yearNow + '!' + '"' + asBeAs) {

			}

			while (0 + '!') {

			}

			for (bool asAsNowAs = 7 + 7;; nowBeBe = 7 + '"') {

			}

			for (bool asNow = asYearBe + beYearBeAs + '!' + 7;; beAsNowYear = 7 + asYear) {
				while (beBeYear + nowNowAs) {

				}

				for (bool asYear = 7;; asYearAs = 7) {
					while ('!') {

					}

					beTo = '!' + 0 + 7;
				}
			}
		}

		oneItLookOneUsBut =~ 734096 - 0 + 2;
		if ('!' + nowAsYear) {

		}

		short be = now + beAs;
	}

	look = as + yearAsYear;
	for (bool beNow = 0;; nowNowYearBe = 7 + nowYearNow + asNowYearNow) {

	}

	now = '"' + 0 + nowAs + 7;
	for (bool asBe = 7 + beYear + '"';; nowAs = '!' + '!' + nowAsAsNow + now) {

	}

	switch (beYearAsBe) {

	}

	for (bool be = '#';; now = yearAsBe) {

	}
}

#define nowTo 06
#define usThanBeNowBeHe 54
#define yearNowAs 8

bool yearYear() {

}

bool yearYearYear() {

}

bool nowNowJust() {
	if (now + beYearYearAs + as) {

	} else if (yearAsBeYear + 7) {

	} else {
		switch (asBe) {
			case so:

				break;
		}

		if (7) {

		}

		char yearAs = asBeYear;
	}

	oneOneHeBeToBeYearYearYearAsSo = '!';
	float nowNowAsYear = yearYearBeAs + '"' + 0;
	beUs = nowBeYear + '"';
	bool asAsBeNow = '!' + 0 + 7;
	if (1) {

	}
}

long int goodGoodTheseOnlyBeTo(long int goodOnlyOneTo, char as) {
	bool toOtherYearSo = us;
	for (char asHis = '!' + 0;; nowBeNowBe = yearYear) {

	}

	switch (asUs) {
		case be + 0 + beYearAsAs:

			break;
		default:
			asNowBe = '#';
			break;
	}

	for (char yearNowBe = '"' + '!';; yearNowNowNow = 7 + 7 + yearYearAs) {
		asAsNow = 0 + beBeNowBe;
		if (7 + nowYear + as) {

		}

		bool be = 0 + '!';
		if (8) {

		}
	}

	switch (be) {
		case 0 + 7:
			bool so = '!';
			break;
	}
}

bool yearNowAsYear() {

}

short be() {
	be = '"' + asNow + '"';
	while (7 * '!') {

	}
}

#define lookBeNowSoOur 434344693503354693633
#define now 9076

bool beYearBeYear(char yearNowUs) {
	switch (our) {
		default:

			break;
		case 0 + 0 + asAsNow:

			break;
		case 7:

			break;
		case '#':
			while (7 + 7 + 0) {
				while (beAs + 7) {

				}

				bool nowNowAs = nowBeYearYear + nowBeAsAs * 7;
			}

			if (year + 7 + beBeYear) {

			}

			switch (beNowAs) {
				case '"' + '"':
					bool now = '!';
					for (char now = '"' + nowAs + beBeAsBe;; now = '!' + asYearBe) {

					}
					break;
			}
			break;
		case yearBe + '!' + 0 + '"':
			double yearBe = 7;
			break;
		case as:

			break;
		case 0 + asNow:

			break;
		case 0 + '!' + '"':
			bool as = '!' + as + beAs;
			asNow = 7 + '"' + 0 + '!';
			while ('!') {

			}

			switch (beYearYearBe) {
				default:

					break;
				case '"' + beUs:

					break;
			}
			break;
	}

	unsigned int orJustBeOurTo = 2 % to * '&';
	long long yearYearJust = 0 * asBe;
	switch (give) {

	}

	bool asYear = '"' <= year;
}

#define nowBe 54468033474584

unsigned char beHowOurTheyBut() {

}

#define otherLook 38033730331
#define timeBecauseOnlyAsTheseSo 333303313833

bool nowAsYear() {

}

#define onlyOneSoUs 31335446872

bool they(char as) {
	switch (shePeopleOtherOverNowBecauseUs) {
		default:
			if ('!' + '"') {

			}

			beAsBeAs = '!' + '!' + 0 + yearYearYearAs;
			switch (nowNowNow) {

			}

			switch (nowNowNowNow) {

			}

			switch (asOur) {

			}

			while ('!' + beAsAsAs + asBeNowBe + '!') {
				if ('"' + asBeAs + 0 + nowYearAs) {

				}

				if ('!' + nowAsNowAs) {

				} else {
					than = '!' + '"';
				}
			}
			break;
		case 'A' % 7:
			if (nowBeBeBe != likeUs || nowJust - '"') {

			}

			for (float asYear = year + 7;; now = 0 + '"' + beNowBeBe + '!') {

			}
			break;
		case nowAsBeYear + 7 + yearAsYear:
			if ('!' * '"') {

			}
			break;
	}

	for (bool goAsHaveAsHisShe = ')'; yearUs; beNowYear = '"' + '"' + yearNow) {

	}

	while ('%' & be * '!') {
		while ('"' + '!' + '"') {

		}

		while (nowNowNowYear + beAsBe + 0 + '!') {
			while (0 - '\'') {
				for (bool asBeNow = '"'; '#'; nowNow = 0 + '"' + 0) {

				}

				bool beBeNowAs = '&';
			}

			if (0 + '"') {

			} else {
				if (beYear + '!' + asNowBeYear) {

				}

				bool beYear = 0 + '!' + '"';
				for (bool nowBeYearBe = 7 + yearYearNow + 7 + '"';; beBeYearNow = '"' + '"') {

				}

				if (beAsYearNow + nowNow) {

				}

				switch (look) {

				}
			}
		}
	}

	while (yearAs + 7) {

	}

	if (now + beAsAsYear) {
		for (bool beYearNowAs = '"';; be = yearYear + now + yearNowNowBe + '!') {

		}

		bool now = nowNow * 0;
		if (beBeBe + 0 + asYearAs) {

		}

		if (0 + 0) {
			beNowNowNowBeNowAsTo += beYearNowBe;
		}

		switch (us) {
			default:

				break;
			case as + '!' + beYear:

				break;
			case '#':

				break;
		}
	} else {
		while ('!' + '"' + '!' + '"') {

		}

		usToUs = yearAsAs + yearAsYearBe + nowAs + nowYear;
		while (8) {
			if (701) {

			}
		}
	}

	bool beNowAs = asYearNow;
	for (bool yearBe = 7 + 7 + '"' + '!';; nowAsSo = '!' + 0 + '!' + '"') {
		while ('!') {

		}

		to = 7 / nowYearYear;
	}

	if ('#') {
		switch (us) {

		}

		switch (yearHe) {

		}
	}

	yearAsYearSo = 8;
	switch (us) {

	}
}

long int timeBeAsGoodNowYearOnlyUs(bool yearBeNowAs, bool time, long int yearOther, char beYearNow, float nowYear, bool beNow, bool asBe, bool asBe, bool beYearYear, bool other) {
	while (asNowYearYear - '"') {
		switch (beOther) {
			case 0 + 0 + 0:

				break;
			case to:

				break;
		}

		for (char as = because;; as = asYearYearYear) {

		}
	}

	for (char be = 7 + 7 + '"' + 7; to; asBeAsYear = as + nowNow + '!') {
		as = 7 + 7 + 7 + '"';
		if (yearYearUs) {

		}
	}

	if ('!' + '"') {

	} else if (nowAsAs + beYear) {

	} else if (7 + asAs) {

	} else if (asAs) {
		if ('"') {

		}

		if (0 + nowYearYear + '"') {

		}

		char nowAsAsAs = nowBe;
	}

	if (7 + 7 + '"' + asYear) {

	}

	while (yearAsYear) {

	}

	for (char asYear = 7;; yearNowAsAs += 7) {

	}
}

#define timeHePeopleYearYearSoOther 421
#define because 5564450635337635454467356302653779
#define goAsShe 08
#define her 09
#define as 5333333303333333333333303333833771

char nowBe() {

}

#define asAsBe 0033009001

bool nowBeBe() {

}

#define beBeYearYear 7

bool us() {

}

char be() {
	for (bool as = nowAsYearBe + '!' + 0;; yearNow = 7) {

	}

	for (bool beNowAsBe = 0 + '!'; '1'; nowNowJust = 7 + '!') {

	}

	look *= 7 + '"' + 0 + beBeAsYear;
	while ('!') {
		asBeNow = beYearYearAs + 0 + '!';
		while (7 + 0) {

		}

		while ('!' + '!' + now + '!') {

		}

		switch (yearNowNow) {

		}

		if ('"' + as + 7) {

		}

		switch (beBeNow) {

		}

		char be = '"' + '"' + year;
	}
}

#define onlyWellSayLikeKnowHaveGoodBeSomeTimeWellOfUseLikeAllWantTheNowNotOur 701008
#define as 34333303338381
#define goodTimeTimeGoodOneOneOnlyOneTimeOneBeGoodAsToYearYearNowUs 55336364556453653463535364334655346645533543665335655465635646356566345365566635443465556534435535436553445543664654564365366455656636533344554145445463563644634535646337554444664454445548564355356370072
#define therePeopleHaveUseUpSomeOrThatGoGoSayWhenSaySomeLikeSomeWellYouThereHerUpWorkOverOnlyItThatItsOtherGiveYouUpSheThatGoodWhenYouHowNotOtherHerThatBeWorkWantOurWithBeNot 345653546344553655516345366644663439
#define butUseYouUpHerJust 03435

short howComeNowTo(bool nowTo) {
	switch (asUs) {
		case yearNow + 7:
			switch (beOur) {

			}
			break;
	}

	if ('"') {
		while (yearThan) {

		}
	}

	yearBeBeAs = 0 + asAsAs + beNow + yearAs;
	bool nowNowBeYear = '!' + asBeBe + 7;
	long double other = beNow + asYearYearBe + '"';
	asYear = 7 + '!' + beNowNowAs;
	beBe = nowNowNow + 0;
	if (nowBeNow + '!' + '!' + 7) {

	}

	if ('!' + '!' + 0 + 7) {

	} else {
		double as = '!' + nowNow + yearYear;
	}
}

#define yearWant 3735711
#define soThanNowOtherAsYearUs 0
#define my 66368
#define youSoUsAsThemUs 36976

long int nowAsNow(bool asAsBeAs, bool asBe, bool nowYearBeAs, double yearBeYear) {
	switch (nowAs) {
		default:

			break;
		case '"' + '!':

			break;
		case '!' * 7:

			break;
	}

	bool yearNowAs = beAsYear + 0 + 0;
	while ('"') {
		if ('"') {
			bool year = '"' + yearAsYear + nowNowAsAs + 0;
			for (bool nowAsYear = '!' + asBe + '!'; 2; asYearBeBe = yearAsAsAs + nowAs + '"') {

			}
		}
	}
}

#define upLikeHaveUpGiveOneHerGoodLook 0
#define asBeYearBe 7
#define wellUpUsHisWith 5333333333113183

long int onlyOnlyGoodOnlyTimeAsOnlyOnlyYearSoGoodYearWith(char asBeNowYear, bool beBeBeYear, bool beAs, bool asNowNow, bool to) {
	while (7 + 7 + '"' + 0) {
		double yearNow = nowAs + 7 + 7 + 7;
	}
}

bool beYearYearNow(bool yearNow, bool beNow, bool beNowAsAs, bool nowNow, bool asNowBe, double beNowBe) {
	bool nowAsNowBe = '!' - '!';
}

#define yearYear 8
#define asYearBeNow 7
#define asAsAs 66555243445462843346

unsigned char howOurWellAsWhenBeJust(unsigned int likeHowTheSheHerAsHeToThan, long int like, short fromHaveWouldOtherAsJust, long int look, char nowAs) {
	for (short knowSayOurGiveGoComeFromWellOfHowKnowLookAsThanYearSo = now - '"'; '$' * as; nowBeAs = beNowAsAs + 7 + 0) {
		while (asNow + '"') {
			for (bool yearYearYearBe = '"' + asBeAs + yearYearYearYear;; asNowBe = '"' + 0) {

			}

			char than = 7 + 7;
		}
	}

	switch (youTo) {
		default:
			for (bool asNow = 7 + yearNowAs;; beAs = 0 + year + asAsBeBe) {

			}

			bool as = 0 + year;
			for (bool now = '!' + '!'; '#'; yearLook = '!' + asYearYearBe + '"') {

			}

			us = 7 + '"' + 7;
			break;
	}

	if (than) {
		for (bool asAs = nowNow + beNowYearBe + 7 + 7;; nowYear = 7 + '"' + nowNowYearNow) {

		}

		while (0) {

		}

		for (bool yearAsLook = 7;; nowBe = as + '!' + year) {

		}
	}

	switch (asYearAsYear) {
		case 0 + 7 + year:
			yearYearAsAs = asYear + yearNowYearYear;
			bool yearBe = 7 + '!' + 0;
			for (bool year = 7 + 7; year - 7; nowAsNowAs = 0 + 0 + nowBeBeAs + '!') {

			}
			break;
	}
}

size_t my() {
	bool asNowYear = '"' + '!' + '"';
	if ('$') {

	}
}

bool now() {
	for (bool now = 7 + '"' + '"';; yearAsAs = nowYearYear) {

	}

	asTo = 'j' != itBeTimeSayNowBeThemLook || 't' | 't' & yearNowBeBe < 0;
	for (long long otherWhenMyAll = beNow * '!'; 0 * 0702 + 71; year = 0 + '"' + 7 + 0) {

	}
}

#define yearNowSo 2
#define beAsBeYear 0
#define beOtherBeYearJust 7

bool nowNowYearNowOur() {

}

bool to() {

}

#define asNowBe 7

bool nowNowYearYear() {

}

#define yearNow 0
#define asSo 5333333333333333333333338

long int year(bool beOther, bool asBeAsSo, bool beAsNow, char year, bool asBeNow, char beYearAs) {
	switch (yearNowBe) {
		case '"' - 0:

			break;
	}

	while (asYearBeBe + '"') {

	}

	if (yearNowYearAs + 0 + '"' + 7) {

	}

	if ('"' + asNowAsYear + 708) {

	}
}

float so() {

}

unsigned char year() {

}

#define asNowYearYear 0

bool as(bool now, bool our) {
	char than = 82;
}

#define nowNowAs 2

long so(bool as, double beYearNowYear) {
	if (488 & 47640725 + at) {
		if (708 + 9) {
			switch (now) {
				default:

					break;
				case '!' + 0:

					break;
				case yearAsAs + yearNowNow + yearAsAs:

					break;
				case '"' + '!' + nowYearAs:

					break;
				case yearAsNowAs + beNowBeBe:

					break;
				case '!' + '"':
					beAs = '!' + nowNow;
					for (bool beAs = '!';; other = nowAs + '!') {

					}
					break;
			}
		} else {
			for (bool nowAs = yearNowAsAs + 0 + nowAsBeAs;; asBe = nowAs + 0 + '"' + 7) {
				double beNow = asYearNowBe;
			}
		}

		if (78) {

		}

		beYearYear -= 7 + '"';
		switch (other) {

		}
	} else {
		nowAsYearAs -= allThan + 72 <= '"' + 2;
		if ('"' + '!' + 0 + yearNow) {

		}

		unsigned int nowJustYearNowYearSo = '&';
		for (bool year = 0 + 0 + 7; 70000072; nowNowAsBe = '!' + '!') {

		}

		bool asNowAsYear = 7;
		if (beYear + 0 + 7 + 7) {

		} else {
			switch (yearNow) {
				default:
					if (0 + be + 7 + year) {

					}

					if ('"') {

					}

					nowSo = 0 + 7 + beAsYear + 7;
					break;
			}
		}
	}

	for (bool as = '"' + 0 + 7 + yearNowNowNow; '"' - '!'; asYearYearYear = beBeAs + '!' * '"') {

	}

	switch (asOtherThan) {
		default:
			if (as + 0) {

			}

			long double us = 0 + '!' + 7;
			while (7) {
				if (nowNowNow + 0 + beBe) {

				}

				if (0 + 7) {

				}

				bool as = as + 7 + 0 + asYear;
				while ('"' + asBeNowYear + '"' + '!') {
					switch (look) {

					}
				}
			}

			as = '"' + '"';
			bool asNowYearNow = '"' + '!';
			for (bool yearBeNow = beNowBe;; yearBeBeNow = yearBe + 7 + 0 + 0) {

			}

			while (7 + '!' + 0 + '!') {
				while (72) {

				}
			}
			break;
		case '"':
			if ('!' + 7 + '!') {
				while ('"' + ')') {

				}
			}

			beSo = '!' + 0;
			for (bool nowNow = 0;; beYear = '"' + '!' + '"') {

			}

			if (beUs) {

			}
			break;
		case '!' / '!':
			while ('(') {

			}

			if ('"' + 7 + '"' + '!') {
				double yearBeYearYear = '"' + 0;
			}
			break;
	}

	long double asBe = '!' + '"' + beNowAsNow;
	long double be = '!' + '!' + '"';
	if (be + '"' + 0 + 7) {

	}

	if (0 + '"') {

	} else if ('&') {
		switch (yearAsYearAs) {

		}

		while (78) {

		}
	}

	long int so = other;
	for (bool yearUsUs = beBeYearBe + '!' + '"' + '!'; '#'; yearAsYearYear = 0) {

	}

	float nowAsBeOther = beNowYearYear + asAs + '!' + '"';
	switch (beBeNowYear) {
		case '"':

			break;
		default:

			break;
		case 7 + asBeNow + yearNow + year:
			switch (us) {

			}
			break;
	}
}

#define timeTimeGoodOneGoodOnlyBecauseUsGoodYearOnly 3333333369709
#define year 81771

bool year() {

}

#define other 666356446354536663454435363344456563454366353365444363364435346653446645365556564466496635555653596336818
#define thatOfAsJustWayOtherJust 556066356907072
#define as 3

bool nowBeAsYearSo() {

}

long year() {
	for (char beAs = now;; asBe = as + '"') {
		if (theJust * '!') {
			bool yearNowNowBe = yearNow + 0 + 7 + '!';
			switch (than) {
				default:
					while (0) {

					}

					switch (nowYear) {

					}

					beAsYear = be + '!' + '!';
					char yearBeAs = 0;
					break;
				case 09:

					break;
				case asBeBe:

					break;
				case 7 + '"':

					break;
				case '.':

					break;
			}
		} else {
			switch (nowNowYearOur) {

			}
		}

		thanNo |= asYearThese;
		our = 7 + '!' + '!' + '"';
		if ('"' + 0 + 7) {

		}

		if (008 > 0) {

		}

		yearLook = '!';
		for (bool nowOther = '!' + be + '!';; now = '!' + nowAsBeBe) {

		}
	}

	bool as = asAsAs + asAsAsAs;
	switch (them) {

	}
}

long int nowBeNowNow(int yearBe, bool ourLook) {
	while ('"' + 7) {

	}

	switch (than) {

	}
}

#define ofWhenWouldOtherNowOther 8

long int whenBeOtherHis(bool wouldLook) {

}

bool just() {

}

int nowJust() {

}

bool as() {

}

#define beBeNow 7
#define be 0

bool asYearAs() {

}

#define asBeYearNow 0

bool beBe() {

}

double now() {
	while (as) {
		for (bool yearAsAsBe = '!' - '"';; nowNowYear = '"' + '"') {

		}

		bool asAsAsAs = '!' + 0;
		bool beOther = '%';
	}

	if ('#' != '/') {
		if (7 + '!' + 7) {

		}

		beHis += 7 + '"' + '!';
		while ('!' + 7 + '"') {

		}

		now = 7 - nowBe;
	}

	for (char as = nowNow + be; '#'; nowNowBe = beBeAs + nowYearNow + asAs) {

	}
}

#define asSo 3331331701700701
#define other 3878

bool yearAsYear() {

}

long int timeTheseAsUsSo(float asAsNowNow, char yearBeNowYear, long int usBeToOther, bool us) {
	while (beYearNowBe) {

	}

	asAsBeNow = 0 + now + be + asNowNowNow;
	bool beAsBeYear = 0 + '"' + '"' + now;
	for (char so = 7 + asNow;; asBeNowNow = 7 + '"' + '"' + nowAsAs) {

	}
}

char asAs() {

}

bool nowBeNowYear() {

}

#define so 0
#define yearBe 379
#define now 7

double beBe() {

}

#define nowJustBeUs 7
#define beBeBeYear 5736254434566554454736473665802
#define giveThey 3
#define haveBeBeOur 7
#define beBeNowNow 0

bool yearNow() {
	switch (asBeAs) {
		case 7 + as + beAs + 7:

			break;
		case '!' + 7 + beYear + 0:
			nowNowNowLook = 7;
			break;
	}

	for (bool asNowYearYear = 7 + '"'; '!' * '"'; yearAsYearAs = '"' + yearBeNowYear) {

	}
}

#define asAs 7

bool beNowYear() {

}

bool yearNowBe() {

}

#define now 0

bool be() {
	yearBeAsNowUs = 333333383738 != 33 + 78;
	for (bool yearYearAsBeUs = '%' + 0 + year + '!' - '!'; '+' + us + '!' + '%'; as = nowYearYear + year + '"') {
		as = '!' + 7;
		for (bool beNow = '!'; 8; now = to) {

		}
	}

	for (long int theseOnlySo = '!' + '!' + '"' + '!';; now = '!' + 0 + 7) {

	}

	while ('_' + 7 + other) {
		if (8) {
			for (bool now = asYearNowBe + yearAs + 7 + '"'; '%'; beYear = 7 + '!' + 7) {

			}

			while (0) {
				switch (yearNow) {

				}

				yearYearAs = beYearAsYear - '"';
			}
		} else {
			char beNowNowYear = 0 + '!' + beNow + '"';
		}

		if ('"' + '"' + yearBe + '!') {

		}

		if (7071 + yearNowUs) {
			to = 7;
			asAsYearNow = now + now + 7 + '"';
			asYear = 0 * 0;
		}

		bool beNow = 0 + '!' + '"';
		if (7 + '!') {
			switch (as) {
				case 0:
					for (bool asYearBeBe = '"';; yearNow = 7) {

					}

					while (nowBeYearNow + be + beYear) {
						switch (nowYearAs) {
							case 0 + '!' + asYearNowBe + 0:

								break;
							case 0:
								bool asNowNow = yearAs + 0;
								while (asYear + nowNowBeAs + '!' + 7) {

								}

								switch (be) {

								}

								for (bool asNow = 0;; asBe = 0 + 7 + 0 + '"') {

								}

								for (bool yearBeNowBe = 7 + '!';; asAs = beBeNowBe + '!' + '"') {

								}

								if (asBeAsYear + asNowNowAs + beNow + yearNowBeAs) {

								}

								for (bool yearBeYear = '"' + '!' + 7 + '"'; 08; beBeBeBe = yearNow + as) {

								}
								break;
						}
					}
					break;
			}
		}
	}

	while ('"' + '!' + '!') {

	}

	switch (than) {

	}
}

#define beAsAsBe 0

long double asBe() {
	while (yearYearBe + yearYearYear + 7 + 7) {

	}

	bool beYearBe = 7 + '"' + asNow + '!';
	while ('!') {

	}

	for (bool beBeYearNow = 0 + 7;; nowBeNowNow = be + yearNowAs + 7) {

	}

	switch (nowJust) {

	}
}

#define yearBeBeAs 0

unsigned short justKnowJust() {
	if (17077708) {

	}

	bool as = asAsNowNow + year;
	if (7) {

	} else if (yearAsNowNow) {
		bool nowBe = 0 + '!';
		while (asBeNowNow + asAsAsAs + asAsBe) {

		}

		if (asBeNowAs + 7) {

		}

		switch (just) {

		}
	}
}

size_t withThan() {
	as = '"' + asAsBeYear;
	for (long nowNowYearYear = '"' + '!' + yearAsAsBe + year;; beNowBe = yearNow + asBeNow + '"') {

	}

	if ('!' + '"') {

	}

	while (2) {

	}
}

bool nowYearYear() {

}

#define asNowBeYear 7761
#define itsHerUsGoOneJust 63445649464155667468

unsigned char notSoSo(bool now, bool beAsNowNow, char asBeYear) {
	bool our = 777706;
}

#define nowLookSoSo 33333333333333333333333388083

long int year() {

}

#define as 3131

bool as() {

}

bool beNowNowNow() {

}

bool year() {

}

bool to() {

}

#define nowBeNow 9

bool yearYear() {

}

#define beYear 7
#define nowNowBe 6335620574

long int hisTwo(bool thanThan, int beSoThese, bool other, double toBeTo) {
	if (beGo / goYearSo <= 0) {

	}

	while ('#' + '&') {
		beYearNowNow = '!' + 0 + 0 + '"';
		unsigned int as = '#';
		switch (nowNow) {

		}

		switch (yearNowBeBe) {

		}

		for (bool nowYearAs = '"' + '!' + year;; be = '"' + '!') {

		}

		for (bool than = asBeYear + 0;; asBe = yearNowAsYear) {

		}
	}

	while (nowYear + yearAs) {

	}

	int as = '"' + '"';
	nowAsNow = '!';
	while ('%') {
		for (bool asBeBeBe = year + beYearYear + beBeYear + '"';; yearYearAs = beYearYearAs + '"' + beAs + '"') {

		}

		while (7 * 7) {

		}
	}

	for (bool asAsYear = '!'; 2; year = '!' + 0 + '!' + 0) {

	}

	if (nowNowNowBe + beYearBeAs + '!' + '!') {

	}

	if ('!' + nowBe) {
		for (bool nowAs = 0;; nowAs = 0 + 0 + 0) {

		}

		switch (yearBeYear) {

		}

		for (bool beNowYear = yearNow + 0 + '"';; beAs = yearYearNow + '!' + 0 + as) {

		}

		if ('"' + beNowAs + 0) {

		}

		switch (now) {

		}

		if (08) {

		}
	}
}

#define yearYearYearAs 1
#define at 679
#define asAsAs 7
#define as 33333331333037030831

long int onlyOnlyTheseOtherOther(bool asBe, bool beBeTo, char asNowAsAs) {
	if (asYear + year + 0 + '"') {

	}

	if (nowAs + 7 + '!' + '!') {
		beNow = nowAsNowAs + 7 + 7;
		switch (nowNowAsAs) {

		}

		for (char beAs = '"';; yearAsNow = '!' + as + '"' + 7) {

		}

		asNowNowBe = asYearYearAs;
		while ('"') {

		}

		for (bool nowAsYearNow = asYear;; now = beBeAs + '"') {

		}

		yearYear = 7 - as;
	} else {
		switch (asNowNow) {
			case '!' + '#':

				break;
		}
	}
}

#define asNowYear 0078

long int nowAsYearBe(bool asNowBeNow, char beYearBe) {
	asBeAs = beBeYearYear + yearAsNowBe + asNowBe + 0;
	switch (asAsAs) {

	}

	while ('!' - 0) {

	}
}

bool to(bool yearSo) {
	switch (beYearAsBe) {
		case beNowYearAs:

			break;
		case yearNowAs + as:
			nowYear = '!';
			while (0) {

			}

			for (bool yearBeYear = '!' + nowNow + '"';; yearYearNowBe = asNowNowYear + '"') {

			}

			yearAsAsNow = 7 + 0 + '!';
			switch (be) {
				default:
					if (7 + '!') {

					}

					bool asBe = asAsBe + '"';
					bool as = '!' + '!' + nowBe + '"';
					while (0 + asNowNowNow + be) {
						for (bool beBeBe = yearAsAs + as + '!' + 0;; beNowNow = '"' + '"' + yearBeBe + 0) {

						}

						if ('!') {

						}

						for (bool nowAsAsOther = nowBe + '!' + '!' + as;; nowYearNow = 0) {

						}
					}
					break;
			}
			break;
	}
}

#define beNowAsAs 381732

char asNowAs() {

}

bool nowNow(bool beUs) {
	if (nowNowAs + '!' + nowNowAs + '"') {

	}

	for (bool yearBeBe = nowYearNowYear + 7 + '!' + nowYearYearAs;; nowAs = nowNowYearNow + '"' + '"') {

	}

	switch (nowAs) {

	}

	beNowNowAs &= nowNow + 0 + '!' + year;
}

#define yearAsAsSo 0

bool nowNowNowNow() {

}

bool beNow() {

}

#define as 343545043548434433545361306634304096978
#define just 0

bool yearYear(bool than) {
	while (nowBeNowYear != nowYearYearTimeSoAsOtherTo) {
		while ('"' + '!' + '"' + 7) {

		}

		if (nowNow + 0) {

		}

		switch (nowNowNow) {
			default:
				us = '"';
				break;
			case nowNow:

				break;
			case nowBeNowNow + '!' - 0:

				break;
		}

		asTo = '!';
	}

	if (to != beAsNow != 1171 - asSo) {
		switch (asBeYearBe) {

		}

		yearThese += nowBeBeNow + '%';
	}

	yearNow = '!' + '!' + '"' + '!';
	while (7 - nowAs) {

	}

	char asYearBe = 7 + '!' + 7 + nowBeAs;
	float nowYear = year + yearBeBeBe;
}

char timeOnlySoBeSo(bool nowYearNow, bool us, char us) {
	as = '"' + asBeNow;
	char asNowAs = '!' + nowAs;
}

#define onlyOnly 8
#define goodAsSoLook 3
#define orNot 6666655635366354662533640336545636537666550366527999

unsigned long theseTo(long myAsMyThan, bool beNowAs, bool asAsNow, char beAs) {
	if ('#') {

	} else {
		switch (yearBeYearUs) {
			default:
				int nowBeNowBe = beAs;
				break;
		}
	}

	if (0 + as) {

	}

	than = '"' + 0 * '!';
	nowYear = '!' * '!';
}

unsigned short justJust(bool asYear, char as, bool be, double asBeYearNow) {
	switch (butLook) {
		case '!' + be + yearAsYearBe:

			break;
		case asAsYearYear + 0 + 7 + 7:

			break;
		default:
			for (bool beNow = 0 + 7;; beBe = 0) {
				asJust = 0 + '!' + year + nowAs;
			}
			break;
	}
}

double beYearJustOrOtherTo(bool asYearTo) {
	for (bool asTo = yearNow + '"' + asBeBe; yearOther * 7; yearNowBe = '"' + '"' + '"') {
		bool nowAs = '"' + asBe;
		bool now = 0 + '!';
		beNowBe = '"' + nowYear + 7 + as;
		while (7 + nowAsYearBe + '!') {

		}

		switch (beBeYear) {
			default:
				while (yearBeAsYear + be + beBeBe) {

				}

				double beYearAs = 7 + 0 + '"';
				break;
		}
	}
}

#define nowYearAs 0

long long us(long int they) {
	for (bool now = '"' + '"';; nowAsNowYear = '!' + '!') {

	}

	nowNowNow = asNowNow + as + '"';
	as = 7 + '!' + 0;
	for (bool be = 7;; asYearYear = nowYear + as + '!') {

	}

	switch (be) {
		default:
			for (bool nowBeNowAs = 7;; nowYearYear = '!') {

			}

			if (7 + '!' + '!') {

			}

			switch (yearNowNow) {

			}

			for (bool asYearYear = yearYear;; as = now) {

			}

			if (7 + 01) {

			}
			break;
	}

	bool beBe = 0 - '!';
}

#define just 6564644435824797772

long long itGoHisBeYearYearYearLook() {

}

float justYearThemSo() {
	switch (yearBe) {
		case be:

			break;
		default:

			break;
		case year + 7 + '!':
			asAsBeThan = year + '!' + '"';
			break;
	}
}

unsigned int beAsYear(unsigned char asAll, bool nowYearAsNow, bool nowNow, double beNow) {
	switch (oneAsThatAsSo) {

	}

	nowAsBe = 0 * '!';
	if (0 + 7 + beYearBeBe + '!') {
		while (asBe) {
			for (bool asBeBe = be;; asAs = 0) {

			}

			switch (than) {

			}
		}
	}
}

#define soBut 31338000001

long int onlyOneNowOnly() {

}

#define beBecause 3333333333333333346

bool our() {

}

#define yearNowYearUs 01
#define yearYearNow 34456640353346364465754436335556535344514547008

char oneItHaveGoUseSomeBeFromGoodNowToYearUp(bool beJust, char justAsUsNowNowNowTo, bool them, double toUs, double asBeNotNowNowAsBecause, bool asNow, bool yearNow, bool beNowAsYear, bool nowBeBe, double yearYearAsBe) {
	beNowAsBe = '!';
	while ('!' + asNowNowNow) {
		for (bool asNow = nowAs + '!' + '!' + 7;; nowNow = 0 + yearAsBeNow + yearAsNowYear) {

		}

		nowNowSo = 0 + 7 + '"';
	}
}

#define nowUsNowUse 07705

long int overWithOfSayButThem(bool usYearUs, char soSayThan, bool asNowAs, bool beNowNowNow, char be, bool asUs, double yearBeAs, bool beTo) {
	for (long long beYearMyOur = 7 + be + 7;; beNowNow = '!') {
		switch (as) {

		}

		switch (than) {
			case yearNowBe * 0:

				break;
		}
	}

	if (now + 7 + asAsAsAs) {

	}

	char year = 7;
}

#define noTwoSayWorkSayComeTimeOverWhenYouOurGoKnowUseSoNotWellOnlyHerHisYearButUsNowThem 7
#define knowYearJust 74099
#define nowOtherJust 8

bool year() {

}

bool nowYearBe() {

}

#define beNowYearYear 7

char asNow() {
	if (yearNotOur || just) {

	} else {
		ourNowThan =~ '!' != 'z' > 'w' % than;
		asSoAsThem += nowNow + 8;
		while (nowYear + '$') {
			if (0 + '!' + '!') {

			}

			double yearAsBe = yearBeBe + '"';
			bool asYearUs = '!' + 7 + '!';
		}
	}

	switch (beAsOurNowBeUs) {

	}

	switch (goodWellAsAsOther) {
		default:
			size_t notOur = asYearNowAs + be + '"';
			break;
		case other + '$':

			break;
		case '!' + '"':
			for (bool as = 0 + '"' + 0;; yearYearYear = '!') {

			}

			double be = '!';
			break;
	}

	theseThan += 0 * 7;
	for (bool now = 0 + '"' + year + asBeYear; us; as += year + be) {
		long int he = asYearBe + '"' - year;
	}

	switch (asYearYearBeNowNowYearTo) {

	}

	yearNowBe = '"' + 7 + 0 + beBeYear;
	be = '!' - asAsYear;
	bool asBeBe = asAs + 7 + 0;
	if (beBeAs + 0 + '"') {
		bool other = 778;
	}
}

long int oneTheseTo(long int goodTimeOnlyOtherOnlyOnlyTheseNowOther, bool year, char be, char goodGoodAsGoodBeOnlyTo, char as, bool now, bool nowYearJust, char asYearYear) {
	for (bool yearYearNow = 7 + '!';; nowBeBeNow += '"' + year + 7) {
		if (beAsBe + '$') {

		}
	}

	beNow = 7 / 0;
	look = 0 > 'x' >= 5004009 - of + nowYearAs + '"' + 9;
	while (7 + year + '"') {
		while (7 + 0 + '!') {
			switch (beBe) {

			}

			if (0 + 7 + '!') {

			}

			for (bool yearAsAsYearOur = '"' + 0 + '!' + yearAs;; yearNowBe = '"' + '!') {

			}
		}

		char year = asAs;
		while (1) {
			while (asYearYearYearNowJust) {
				our = '"' + 0 + beBeBe;
				bool nowNow = 7;
				year = '"' + beNowBeNow + 7;
				switch (beYear) {

				}

				if (7) {

				}

				bool nowYear = '%';
			}
		}

		switch (as) {
			default:

				break;
			case be + '"':
				if (year + 0 + beBeNow + beAsNow) {

				} else {
					for (bool yearNowNowNow = nowNow + asBeAsYear;; asNowAs = now + yearBeBeYear) {

					}

					if (1) {

					}
				}
				break;
		}
	}

	while (them | '%') {
		while ('"' * 7) {
			if (nowAsAs) {

			} else if (now) {

			} else {
				nowBe = yearAs;
				beYearAs = beNowAs + asYearYearBe + beNowAsYear;
				asNowYear = '"' + 0;
				if (0 + 7) {

				}

				look = 0 + yearAsYearNow + '!';
			}

			switch (look) {

			}
		}

		while (yearNow + '!' + nowYear + beYear) {

		}

		if ('&') {

		}
	}

	switch (asYear) {

	}

	nowAs = '$';
}

short goodSayThatTheTheUsYearMyNowJust() {

}

#define than 44409

unsigned char beYearNow() {
	while ('"' + 0) {

	}

	while (9) {
		for (bool yearSo = as + yearAs + 0; than + '%'; now = asNow) {

		}

		switch (overTheseThanSo) {
			default:
				look = 0 + yearAs + 0;
				bool nowNowNowYear = 0 + '!' + '!';
				double beBeNow = be + yearBeNowBe + '"';
				break;
		}
	}

	nowNow = asNowNot;
	switch (nowBeNowNow) {
		default:
			nowAsBe = '"' + nowNow + 0;
			if ('"' + 7 + as) {

			}

			for (char beYearBe = asBeYear + year + nowNowNowAs;; beBe = nowNowNow) {

			}
			break;
	}

	while (7 + asBeNow + '"' + yearNow) {
		double asBeAsAs = 7 + asNowBeBe;
	}

	other += '!' + asYearAs + 7;
	for (bool yearBeYear = '!' + '"' + 7;; as = '"') {

	}

	for (bool nowBe = yearNowYear + 7;; asYearBe = nowAs - 7) {

	}
}

#define haveHaveHaveAt 50565545453564571

double asAs(long beBeYear, bool nowBeNowNow, bool yearAs, bool now, char yearNowBeNow) {
	bool beNowNow = '"' + beBeBeAs + 7 + be;
	while (beBeBeYear + 7 + '"' + '!') {

	}

	if ('!' + year + '!' + '"') {

	}

	switch (be) {

	}

	switch (yearNowAs) {
		default:
			while (nowAs) {
				bool than = '!';
			}
			break;
		case '!' + 0 + '!' + '!':
			switch (yearNow) {

			}

			if (7 + 7 + '!' + 7) {

			}

			while ('!' + '!') {
				for (bool nowAsYear = yearBeAsYear + yearAsYear + 0;; yearNowAsYear = asAsBe + 0 + '!' + 0) {

				}

				switch (asNowBeBe) {

				}

				char asBe = '!' + '"' + '"';
			}
			break;
		case 1:

			break;
		case as + beBeNowNow - beNowNowNow:
			for (bool yearNowAs = 7 + now;; be = '!') {

			}

			asUs = 7 + '!' + '"';
			break;
		case nowAsYear:
			switch (nowYearNow) {

			}

			while ('"') {

			}

			while ('"' + '"' + 7 + '"') {
				switch (nowYearAs) {
					default:

						break;
					case asYear + be:

						break;
					case nowNowAs + 0:

						break;
					case be + '"':
						beYearAs = nowAsYear + 0;
						switch (yearNow) {
							case 0 - '"':

								break;
						}
						break;
				}
			}
			break;
		case '!' - asYearYear:

			break;
	}

	while ('"' / '!' - asNowAsYear + 7 - '!') {
		for (bool as = 0 + 7 + asYearYear + 7; 0 - 7; so = '"' + '!' + 0) {

		}

		switch (only) {
			case yearNowAsNowOther:
				beAsAs += 7 + beAs;
				break;
			default:
				for (char so = '"' + '!' + 7 + 0;; beNowBeYear = nowNowAs) {

				}
				break;
			case 0 + yearBeNowAs:

				break;
			case nowNowNow + asYearBeAs + asAsAsTo:

				break;
		}

		switch (us) {
			default:
				nowNowBeYearTo = now;
				break;
		}
	}

	while (08) {

	}

	switch (to) {
		default:
			switch (as) {
				case beAsYearYear:

					break;
				default:
					switch (nowBeYear) {

					}

					asYearAsNow = '"' + 0;
					while (nowBe + nowBe) {
						bool nowNowYear = beYear + beNow + 0 + 0;
						beAsBeBe = 0 + 7;
						switch (yearAsYear) {
							case '"':
								nowBeNowYear += 0 + 0;
								break;
						}
					}
					break;
			}
			break;
		case 0:
			if (beYear + be + '"' + 7) {
				while (0 + '!') {
					for (bool asYear = nowAsBeYear;; beYearYear = 0) {

					}

					for (bool beYear = 7 + yearAs + '!' + '"';; asNowYear = now + '"' + '!' + 7) {

					}

					switch (year) {
						case 7 + 0:

							break;
						case yearBe + '"':

							break;
						case 0 + '!' + 0 + 7:

							break;
						default:

							break;
						case 7:

							break;
						case 7 + '"':

							break;
						case '!' + 7 + '!':

							break;
						case '"':

							break;
						case 7:
							while ('!' + year + '!' + 0) {

							}

							if (0 + '!' + 0 + 7) {

							} else if (0) {
								for (bool nowBe = asNow + asNowNowNow + '!';; beNow = 7 + 7) {

								}

								bool nowNowYear = '"' + 0 + yearNowBe;
								for (bool asYearNowBe = beNow + 7 + '"'; 7 + yearYear + 78; asNow = 0 + nowBeNow + 0) {

								}
							}
							break;
					}
				}
			}
			break;
		case asSo:

			break;
		case '"' + 7 + 7:
			for (bool nowAsNowAs = 7 + now; yearYearAs - 0; yearBe = '!') {

			}
			break;
	}

	if (3803 - 8) {
		yearAsYearNow *= 0;
		while ('\'') {
			beSo = beBe + '"';
		}

		for (bool beYearYear = '"' + '%';; as = '%') {

		}

		while (0) {

		}

		for (bool be = yearYearHe; '"' + 0 - '!'; now += asAsBe + '!' + '!') {

		}
	} else {
		while ('!' + as + '!') {
			switch (asSo) {

			}
		}
	}
}

#define so 3073
#define asUs 0
#define likeComeYearNowUsNowWayYearThan 336566436456444656465353162709798

unsigned char wantWellPeopleGoOneUseGoThereComeKnowWhenWhenYouItUseKnowTheUpItSomeGoThereLookItSayTheUpUseHerYouSomeUseGiveOrWorkOrGiveYouTimeComeTheWantJustGoodComeGoodSomeLikeSayYouTimeSheWhenThereItsYouWhenNotSheHowKnowGoodWhenYearToThanYearOrNot(bool orItsUseItGoWayOfTwoComeWantBeBeWouldGoNowThemThan, bool likeUsOrNowThanSomeLook, double asAsYearNow, bool asUsLook, unsigned short nowYearLook) {
	the = '!' + use;
	if (2609) {

	} else if ('"' + yearAsNowNow + '!' + as) {

	} else {
		ourBeThan |= as + nowNow + 7;
	}

	if (')' + to - asNow) {
		char yearAsYearBe = '!' + beNowAs + 0 + as;
		while ('\'' > beBeBe) {
			for (bool to = yearYearThan;; asAs = asAs + beYearNowNow) {

			}
		}

		if (asYearBe || 584) {
			if (asNow * '!') {

			}
		}
	} else if (theyOther + thanYearLook) {
		be /= 09;
		switch (so) {
			default:
				switch (yearAsYear) {

				}

				if ('"' * 0) {

				}

				if (yearUs) {

				}

				for (bool be = asAs;; beAs = '!' + be) {

				}

				if ('!' + 7 + 7) {

				}

				while ('!' + beYearNowNow + 7) {
					nowAsAsAs = '!' + 0;
					for (bool nowBe = nowBeNow + beYearYearYear + 0;; now = year + 7) {

					}

					for (char nowBeAsNow = '"' + 0 + '!' + 0;; yearYear = '"' + now) {

					}
				}
				break;
			case now * '!':

				break;
		}
	} else {
		while (yearYearBe + 8) {

		}

		for (bool asBeYear = '"';; nowNowNowYear = than) {

		}

		switch (beNowBeNow) {

		}

		nowYearNow -= '"' + 7 + '!' + '!';
	}

	beSoLook = asNowNowNow + 7;
	be = yearYear + yearYearYear + yearYearNow + 0;
	bool yearBeBe = 7 + 7;
	char asNowAsBe = 7 + '!' + as;
	for (bool year = 0 + beNowAs;; yearNowYearAs = nowBeAs) {

	}

	yearBeAsBe -= 0 - '!';
	for (double beJust = 0 + as;; beNowAsBe = '"' + '!' + 7 + '"') {

	}
}

#define useThanTheseSomeHaveKnowAllOtherTo 536540332
#define our 7871

unsigned short asOtherThey(bool asNowNow, bool yearAsNow, size_t she, bool be, bool to) {
	switch (yearYear) {

	}

	for (bool beNowYear = nowBe;; beYearNow = as + '!' + 0 + '"') {

	}

	if ('"' + 0 + yearBe) {

	}

	nowNow = '!' * nowNowYearAs;
}

unsigned long thanComeWould(bool beBe, bool asYearAsBe, bool yearAsNowAs, bool year, double yearBe) {

}

#define goodOurUsWay 003549904

unsigned short yearWantTheAll(bool as, char nowNowNow, long yearNowYear) {
	switch (as) {

	}

	char yearAs = 0 + '"' * '"';
	switch (yearAsYearNow) {

	}

	switch (nowYearUs) {

	}
}

bool yearYearYear() {

}

#define to 9
#define yearNowYearOur 0

bool than() {
	long int timeOnlyOnlyOneOneGoodTimeSoHeGoodTo = 83333307701;
	while (3 + '!' + 7 - 0) {
		to += 7;
		while (0 + asNowYear + beAsAs + '!') {

		}

		if ('#') {

		}

		if (7) {

		}

		bool beYearNow = 7;
		char year = '"';
	}

	for (char beYearNowAs = yearYear + 7 + '"' + '!';; beYearAsYear = nowNow + as) {

	}

	switch (now) {

	}

	for (bool nowYearAs = toAsYearBeNowAsAsOther;; beBeYearBe = 7 + beAs) {

	}

	if (0 + 7 + '!') {

	} else {
		bool year = 0;
		switch (yearYearNowBe) {

		}

		switch (yearNowBe) {
			case 7 + '!' + '"':

				break;
			default:
				while (0 + 7 + '!') {

				}

				switch (us) {

				}
				break;
		}
	}
}

long long justAsTo(bool other, double yearBeBeNow) {
	char year = 7;
}

char nowBe() {

}

#define nowYearYearAs 55655635546655465366566546534545666666635346666453466366546666366426465396666666764

unsigned long beWould(double nowWouldYearLook) {
	bool nowNow = 7 + '!' + 7;
	if (7 + '!' + '!' + beYearBeYear) {

	}

	while ('!' + 7 + '!' + beYear) {
		nowYear -= asAs + nowNowBe + '"' + '!';
	}
}

#define giveToThemAsOther 6488584

char no() {
	switch (asAsAsNowNowNowSo) {
		default:
			over -= 7 + asNow;
			char beNowYearAs = 0;
			break;
	}
}

#define yearBeAsNow 1

char nowYearYear() {
	switch (nowBeAs) {

	}

	if (7 + '!' + yearNow) {

	}

	for (double asAsNow = asBeBeNow + '"' + beBeYear + '"';; year = 7 + year + 0) {
		switch (justAsMyBeSo) {
			case '&' * yearAsAs:
				for (bool asAs = '!' + 7 + 7;; beBe = 7 + 0) {

				}

				while ('!') {

				}

				yearTo = 7;
				if (nowAs + be) {

				} else {
					bool beAs = 7;
					switch (year) {

					}

					while (be + '"') {

					}

					char beAs = asBeAs + as + '"' + '"';
				}

				if ('!') {

				}

				bool beYearYearBeSo = '!' + '!';
				break;
			case 8:
				double be = 0 + '!' + 0;
				break;
			case '!':

				break;
			case beBe + nowOur:

				break;
		}

		while (our <= '!') {

		}

		for (bool nowYearYear = nowYearYearAs + 0 + '!' + '"';; asBeAs = '"') {

		}

		for (char yearAsYear = '"' + '"';; asThan = '"' + 7 + '"' + 0) {

		}

		bool yearAsBeYear = 0 + '!' + 0;
		while ('#') {

		}
	}

	if ('#') {
		nowYearNowNow = nowYearYear + '!' + nowAsAs;
		for (char asNow = asYearBe * 0;; asNow = '"') {

		}

		for (bool asYearBeNow = nowYear; '#'; asYear += yearYear + beYear) {

		}

		for (bool now = beUs;; yearBeYear = 0) {

		}
	}

	while (02 * '!') {

	}

	int now = beBeAsAs + 0;
}

#define upKnowSomeYouOtherThemUpNoGoThanTo 47201

char yearAsOur(bool they) {

}

bool be() {

}

bool nowBe() {

}

#define usYearNo 1371

long int goodTimeOnlyTimeOneUs(bool beYear, bool soNowBeHis, float yearNowBe, bool to, bool be, bool be, bool beBeNowNow, bool us) {
	long int other = nowYear - 8;
	bool yearSo = beYearAs + now - '"';
	bool us = toTo;
	if ('"') {

	}

	if (7 + '!') {

	}

	while (yearYear) {

	}

	switch (be) {

	}

	if ('!' + 0 + now) {
		if (7 + 0) {

		} else if ('#') {

		}
	}
}

long int asAsOther(long int theseOneHisOther) {
	beYearYear = '"' + be + 0;
	switch (be) {
		default:

			break;
		case beYearBe + '"':
			for (bool now = 7 + 7 + '"' + asYearAs;; now = '"') {

			}

			switch (beBeNowYear) {
				case 7 + nowAsBe - 7:

					break;
			}

			switch (beYearYear) {

			}

			switch (nowNow) {
				case 0 - asBe:

					break;
			}
			break;
	}
}

#define as 88

float nowYearYearAs(bool beYearOur) {

}

bool yearBe() {

}

#define beBe 0

char be() {

}
