#define timeGoodOneOtherOneGoodUs 1333833731

long int timeOtherOnlyOneBecauseSoGoodYearTo(bool beNowYearUs, long int yearNow, double beJust) {
	for (int year = '"' + 0; yearAs * 0; asYear = yearNowNow + '!' + '!' + '!') {
		switch (yearYearNow) {

		}

		while (0 + yearYear + '"' * yearAsNow) {

		}
	}

	switch (now) {

	}

	switch (nowYear) {

	}

	switch (beYearNowYear) {

	}

	switch (year) {

	}

	while ('"' + asAs + '!' + 0) {

	}

	bool nowNowNowAs = year + 7 + yearYearAsAs;
	for (double beYearNow = yearBeYearNow + '!' + now + asBeAsBe;; yearBeBeNow = 0 + 7 + be + be) {

	}
}

#define beOur 8536455646434654806205049
#define overOrSheBePeopleOurSheThemHowKnowUp 445420534792

size_t beAsYearNowSoBeYearNowYearNowUs(bool to, long just, char beYearThan, bool yearYearBe, bool yearNowFrom, char beYear) {
	if ('K' - all) {

	}

	theNowGood = now + '&';
}

long long way(char yearUs, size_t thereWantItsUseJustWantGiveTheseGoNowOnlyAllOverHeBeKnowNowOurThan, double yearBecauseWould, short asOur) {
	us &= '!' * 1;
	switch (yearIts) {
		default:
			be = 7 + 0 + '!';
			while (yearYearNow * 0) {

			}
			break;
		case 0 + 7 + 0:

			break;
		case 0 + '"':
			if (7 + 7 * 0) {

			}
			break;
		case 7 + asYearBe + '!' + beBeNowYear:
			while (beBeNowYear + '"' + '"' * nowBeAs) {

			}

			beNowAs = '"' + be + 0;
			switch (beBeBe) {
				default:
					for (bool beYear = '!' + '!';; asBe = 0 + asAsBeYear + '"') {
						for (bool beNowNowYear = nowBeYear + 7 + '"' + 0;; yearYearYearAs = beAsNow + 7 + '"' + as) {

						}

						for (bool yearBe = '!' + '"' + 7 + 7;; nowYearNowYear = 0) {

						}

						bool beYearYear = beYearBe;
						if ('"' + year + 7 + '"') {

						}

						char asYear = asAsYearYear + '!';
					}
					break;
			}
			break;
	}

	if ('"' + '!' + 7 + 7) {

	} else {
		if (0 + '!' * as) {

		}
	}

	beNowYearNow -= asAs + '!' + yearAsYear;
	switch (yearYearThan) {

	}

	while (7 + yearNowBe + 7 + yearYear) {
		switch (beNow) {

		}

		while (0 + '!' + beAsNow) {

		}

		other = '!';
	}
}

size_t upWellTwoNowTwoOrAt(int lookSoOther) {

}

double knowYearNowBeAsNowThan() {
	for (bool beBeAs = beYearNowBeYearBeOur; 71; yearAs = be + '"' + 0 + 7) {

	}

	switch (theyOur) {

	}
}

#define nowBeAs 0
#define yearYear 0

bool asYearBe(bool year, bool yearNowAsNow, bool beSo) {

}

bool asNow() {

}

char asNowAs() {
	for (long long beYearNowTo = onlySoLookAsToAsFrom || 'L'; 3 == '-' + '"' + nowHis; nowNow *= '!' + asBe) {
		float yearYearYearYearOther = 702;
	}

	if (7 + 0 + '"') {
		other = yearNowYearYear + 0 + '"';
	} else {
		switch (as) {

		}

		while ('!' + 0 + asAsYearYear + 7) {

		}

		if (78) {

		}
	}

	char beThan = asYearBe + 7 + now;
}

unsigned short to(bool nowNowNowAs, bool just, bool ourAsUs) {

}

unsigned short asSoSayNowOtherLook() {
	if (beYear) {

	}

	yearNowAs = 0 + '!' + '!' + 0;
	switch (be) {

	}

	for (bool yearNowBe = asAs + '"' + '!'; '$'; beAsAs = 0 + 7) {

	}
}

#define beBe 7000078
#define asJustSo 17703
#define beAsNow 79
#define nowNowAsAs 43333333333333333333333333033
#define yearBeNow 8310078
#define beTo 8

bool he() {

}

bool asYear(bool yearAs, bool asAsYear, double as) {
	beBe = beAsYear + 7 + 0;
	bool nowAsAsBe = 7 + '!' + '!' + year;
	while (0 + as) {
		yearNowNow = year - 09000707007007707778 <= ',';
		beAsAs -= nowBeAsYear + asAsYearYearAsNowThan;
		if ('+') {

		} else if (7 - 0) {
			while (yearYearNowYear + '!' + 7) {

			}

			while ('!') {

			}

			nowBeAs += 0 + asAs;
		}

		toToSoUpThereAsSoBut = 0 + '!';
	}

	if ('v' == 6349512 + withBeOther + 0 * asAs) {

	}

	while (1) {

	}

	if ('!' + 0 + 0) {
		for (bool yearAsAs = '!' + 0 + yearYear; '&'; yearAs = now) {

		}
	}

	for (bool yearYearNow = '"' + '"' + 0 + '"';; nowAsYear = '"' + 7 + 7) {

	}

	if ('#') {

	}
}

unsigned int ourBecause(bool because, bool nowYearYearYearNowNowAsJust, bool asYearThan) {

}

bool asBe() {

}

#define beAsNowBe 7

bool asAsYearNow() {

}

#define nowBeYearYear 0502
#define us 1
#define to 3171

long int hisNowUsUs(float asOneAsTo, bool theseHis) {

}

long int usGoodOnlySoOne() {
	switch (nowNow) {
		case 0 + '!' - yearAsNowYear:

			break;
	}

	if ('"' + '"' + 0) {

	}

	if (0 + 0 - asNowYearNow) {
		switch (so) {

		}
	} else {
		switch (year) {
			case be + '"' + 7 + yearBeNowNow:

				break;
			default:
				switch (year) {
					default:
						bool asBeNow = asYear + '!' + '"';
						char year = as + asYear + '"';
						break;
				}
				break;
		}
	}

	switch (yearAsNow) {
		case yearBeAsYear - year:

			break;
	}
}

bool beBeBe(bool toNowBeUs, bool other, bool beAs, float beAsYearNow, bool year, bool as, bool beYear, bool beYear, bool so) {
	switch (be) {
		case 7 + 7 + nowAs + 7:
			if (be + 7) {

			}

			bool beNowBe = 7 + '"' + 0;
			if (to) {

			}
			break;
	}

	while ('"' + asAs) {
		for (bool nowYearNow = 7;; beAs = '!' + asNowBe + '"' + asYearYearAs) {

		}

		switch (beYearBe) {

		}

		bool yearYearAsAs = asBeUs;
	}
}

float us() {

}

unsigned char yearNowAsBe() {
	like = 7 + '"' + 7 + as;
}

#define yearAsYear 0

char beAsBeNow() {

}

#define all 

size_t (unsigned char overHowBeOnlyKnowSomeUp, unsigned int herNotThanAsOfHeYearUs, bool fromNowAsThese, double asBeYearNow) {
	unsigned long useSomeWorkWellOnlyUpLikeSomeComeKnowTwoThereOneOurOverOurSoThan = '$' * 0;
	while (comeBeLookUsAsAsYearBeOur) {
		if ('`' + 7 * asYearAs) {
			bool beBeBeNow = 0 + '!';
			if (0 + nowBeAs) {

			} else {
				if (nowNowYearAs) {

				}

				switch (nowNowNow) {
					case beAsYearNow + 7 + be + beBeYearYear:

						break;
					default:
						beNow = beAsNowYear;
						switch (year) {

						}

						if (0 + 0) {

						} else {
							if (7 + asAs + as + 7) {

							}

							yearYear = 0 + 0 + '!' + '!';
							switch (other) {

							}
						}
						break;
				}
			}

			double yearNowNowBe = 7 + 0 + nowAsYear + 7;
		} else {
			switch (beNowAs) {

			}

			switch (asYearNow) {

			}

			to = '!' + '!' + asYearYearBe;
		}

		for (bool as = '!' + 0;; yearYearYearYear = 7 + 7 + 0) {
			beNowNowNow = nowBeYear + 0 + '"' + 7;
			look = 7;
		}
	}
}

long long itsOrComeOfLikePeopleItSheTwoWantTwoOnlyItWellGoYouGoUpTwoBeSheGoTimeThatUpHaveWorkHaveThatOverTwoUseTwoYouGoThatOnlyWorkYearSomeOverItOneOverPeopleComeHerThereTwoOverGiveSayWhenComeOneAsHisItHaveHave(double nowNow, bool yearNowBe, double just, unsigned char nowFromTo, char so) {
	unsigned short nowSoNowUs = '8';
	if (other + 70001) {
		switch (yearAsBe) {
			default:
				if (asAsAs) {

				}

				switch (beBeBeBe) {

				}

				look = '!' + 7 + '!';
				break;
		}
	} else if (7) {
		bool asYear = '"' + asNow + '"';
		beNowBe = '!';
		nowBe = 0 + beNow + year + than;
	}

	bool beNowAs = '%';
}

double nowBe() {

}

#define wellItAll 333533602

size_t nowComeThatTheyYearHerYouGiveWantBeItsNowUsHis() {
	if (beYearBeAs + 0 + 0 + '"') {

	}

	while ('!' + 0 + 7) {

	}

	while (yearNowBe) {

	}

	switch (yearThem) {

	}
}

char nowNowBe(bool to) {
	switch (nowYear) {

	}

	beAsAs = '"' + now + asBeBe + '!';
	long beBeAsYear = 7 + 7 + '"' + yearYearBe;
	for (bool yearYear = '!' + beAsYear;; yearAsSo = '!' + 7 + '!' + '!') {

	}
}

#define beNow 7

unsigned int heNowUsYearOurUs(bool beAsOur) {

}

char beAs() {

}

#define year 0

bool yearNowAsAs() {

}

#define nowAs 7

unsigned long nowBeBeBe() {
	while (us - '!') {
		switch (goodUs) {

		}

		while (as + now + beNowNow + 7) {
			switch (now) {

			}

			while (yearYearYearAs + '"') {
				if ('"' - '"') {

				}
			}
		}
	}

	while (timeToSo != 71 + timeHisAsUs - '!') {
		for (long int onlyGoodBeToAsAsSo = timeAsNowUs;; asAsYear = 7) {

		}
	}

	yearNow = 7;
	oneSo += '!' + yearSo - '"';
	if ('"' + '"' + '!' + be) {
		bool yearTo = '#';
	}

	switch (nowNow) {
		case as + be + '!' + '"':
			for (bool beYearYearBe = 7;; beBeNow = 0 + 0) {

			}

			switch (beUs) {

			}
			break;
	}

	if (yearAs + '"' + beYear) {

	}

	switch (beNowBeUs) {

	}
}

bool nowYearYearYear() {

}

char oneHowItsLookNowThese() {

}

bool as() {

}

bool our() {

}

#define nowAs 7

bool be() {

}

bool as() {

}

bool asBe() {

}

double nowAsBeBe() {
	while ('t' >= would * '"') {
		switch (nowLook) {
			default:

				break;
			case 7 / 701 && '"' - ';' - beNowAsYear:
				unsigned char soBeOr = 7 + nowYearNowAs + asAsBeNow + 7;
				while ('>') {
					us = 7 + 0 + year;
					long beAsAs = '!' + '!' + 7;
				}

				unsigned char as = '&' + 0 * 0;
				if (asAs + 7 + asBeBeYear + 7) {
					while (beYearNowAs + 1) {

					}
				}
				break;
			case asAsBeBe <= 79:
				switch (year) {
					default:

						break;
					case beAsYearNow + 79:
						char yearBe = '!';
						break;
				}

				asAs += '!';
				break;
			case '!' + yearAsNow:
				while (7 + '!' + asAs) {
					switch (be) {

					}

					bool to = nowBe + now + yearNowYearNow;
				}
				break;
		}

		while (44546645354433334365334363035965637556472 + 5291 + 9 * asAsNow + '"' + be - '"') {
			for (bool nowBeAsYear = 42;; just = '"') {

			}
		}

		while (0 + '"' + yearBe + yearNowAs) {

		}

		for (char wellWithAsAsJust = comeOrAllYouUpOverKnowBeWhenWorkGoUseTwoWantTimeGoWellOneHowSomeKnowLookGoHowItUpSoUs + '%'; myHaveAtLikeOther > 11; be >>= goodUs - 7) {
			while ('&') {
				us = 7 + 0;
			}

			for (int asAsAsYear = nowBeYearAsAsNowNowTo;; beBe = 7 + '"') {

			}

			switch (so) {
				default:
					look += '!' || asNowBeNow;
					double to = 0;
					break;
				case beBeNowSo * '!' * yearNowYearAs:

					break;
			}

			for (double just = '!' - '"';; asAs = 7 + 0 + 7) {

			}

			for (char year = 0 + '"' + 7;; as = 7 + '"' + 7) {

			}
		}

		unsigned long upLike = 7 + as != yearNow;
		if (yearOurThey != than != '!') {
			other %= to;
		} else if (0 + 0 + '"' + 0) {

		} else if ('!') {
			if ('"' + '"' + nowBeNowYearThan) {

			} else if ('"' + 7 + 0 + asBeAs) {

			} else if (7 + 0 + 0) {
				nowBeBe = as + '$';
			}
		} else if ('!' + 0) {
			if (0 + 0 + yearBeAsNow + '!') {

			}

			bool yearYearAsBe = '"' + '"';
			nowYear = '!' + yearYear + beAsBeYear;
			for (bool asTo = yearNow;; be = 7) {

			}
		}
	}

	wellThereOfHerOfOrOnlyYearLook -= 4134603702;
	if (434646643617772 != '!') {
		long long herPeopleWhen = '$' | '%' - yearAsYear;
		asBe = '!' + asAsYearYear;
		bool yearOur = nowAsBe + '!' + 0 + 7;
	} else {
		for (bool yearAsBe = 0;; nowAsNow = asBeNowBe + 0 + beYear + '"') {

		}

		while (7 - '!') {

		}
	}

	for (char yearBeAs = yearYear + 7 + yearBeAs;; now = 7 + 0 + yearNowYearAs + be) {

	}

	long int yearBeAsAs = '!' | 7;
	for (bool year = 0;; nowBeNowAs = nowAs + be + '!') {

	}

	while ('"' + 0 + '"') {

	}

	nowYear = yearBeAs + 0;
	while (7 + 7 + 0 + yearBe) {
		if ('"' + 0 + nowNowBeNow) {
			switch (year) {

			}

			while (beJust) {

			}
		}
	}
}

#define comeGiveOnlyUpTimeWantThanTheseOrNowBeBut 444605566625525778

bool no(bool look) {

}

#define beYearAsThan 30363363445344573356576558
#define myOneNowLookJust 7

bool nowBeBeYear() {

}

#define nowToJust 98
#define yearYearBe 056014

bool so() {
	while (as + be + 7) {
		switch (now) {
			default:

				break;
			case nowAs + '!':

				break;
			case 7 + be + 7 + '!':

				break;
			case 0 + '!' + 7:

				break;
			case as + '"':

				break;
			case '"':

				break;
			case '%':

				break;
		}
	}

	if (becauseToAsTo) {

	} else {
		for (bool nowNowAsBe = nowNow;; year = 7) {

		}

		if (asYear + as + year + '"') {

		}

		for (bool be = '"' + yearYearYearYear + '"' + nowAs;; year = nowNowNow + 0 + '"' + '"') {

		}

		if ('%') {

		}
	}
}

long int nowTo(char his, bool to, bool nowSo) {
	yearAs = nowYearAsAs + beAs + 7 + yearNowYear;
	switch (asAsTo) {

	}

	now = '%';
}

#define goodTo 31

float to() {
	if ('"' + '#') {

	}

	char asYear = '!' + 0 + '!';
}

#define oneOneOnlyAsTimeYearGoodToBeToBeUsAsUs 3343643444456464345535436435545434554445654544365534645335334665564446643544643445265616364664531763920155002
#define goodTheseAsYearThatOnlyYearTimeKnow 5663355665664635454666356543343464635553463365643537554656644556663345440367333446765
#define wellTimeGoTwoOverYearOnlyTheseOtherOur 36463556365466334435354644536544366356534233334334456666364340546545541663534435653655

short ourNo(double yearBe, bool ourAsSo, char yearOther, bool sayNowAt, bool beYearNowTo) {
	other = 0 + beAsNowAs + '"';
	while (nowNowBeBe + '"' + 0) {

	}

	switch (beYearNowNow) {

	}

	while ('"' + '"') {

	}

	if ('!' + '!' * '!') {

	}

	for (bool yearAsBeYear = '!';; asNow = '"' + 7 + 7) {

	}

	for (bool nowBeBe = '!';; yearAs = as + year + 0 + asYearAsNow) {

	}

	while ('"' + 0) {

	}

	switch (to) {
		case nowUs:

			break;
	}
}

unsigned char itsWorkItHowOrGiveOfItHerOnlyThereThatUseUseHaveOverItsSomePeopleSomePeopleTheNowItItsOnlyGiveHowOneOrOneSomeSayTheSayOfThereHerSayYouUsSheGiveGoodPeopleUsSomeGiveThatSo(long int goodSayHowHerTheUpAtOverGiveYouTwoThatWhenGoThereOnlyOneWantOnlyNowGoodTheTwoItsKnowHerOverSomeAsYearSheGiveSoThan, long int asSo, unsigned long goLikeKnowOneSayUseTheseAsLook, unsigned int thereYearItTimeNoOrWorkHowNowOrSheGiveAsOrHowOnlyWhenThatAsJust, unsigned char ofHowThereUseBeUpBeOur, float now, char usOur, bool other, double my, bool year, bool now, bool to) {
	if (4043 + '"' * '"') {
		if (02) {

		}
	} else {
		for (bool asYearBe = 3 + asThanTo + '"' + ')'; '5'; asAsJust = asNowUs) {

		}

		switch (but) {
			default:
				for (bool yearAs = '"'; '!' + nowBeBe + asBeAsNow + '!' + asYear - '!'; beNowNow = '"' + asBe + 0) {

				}
				break;
			case asNowBeAs + 0 + 0 + '!':
				switch (beYearAsYear) {

				}

				for (bool yearNowBeAs = '"';; beYear = 7 + '"' + 0) {

				}

				if ('!' + 0) {

				}

				switch (asBeYearNow) {
					default:
						for (bool beAsYearBe = 0 + beYear + '!' + 0;; asAsAsBe = yearBeNow + 0 + '"' + 7) {

						}

						switch (nowAsOther) {

						}
						break;
				}
				break;
		}

		if (3 <= now) {

		}
	}

	while (beBeBe + asNowAsNow + 7 + 0) {

	}

	bool beNowBeNotOtherYearSo = overSoSo;
	if (0 + '"' + '!') {

	}

	while (0 + 7) {

	}

	switch (year) {
		default:
			be = '"' + asBeYearNow;
			beAsAsNow = 0 + '!' + beNow;
			char as = '"' + 7;
			break;
		case '"' + beNowAsYear:
			if (asNowBe + '!' + nowNow + asYearNowBe) {

			}

			while (nowYear) {

			}

			if (asNowNow + asBe + 7) {

			} else {
				while (yearNow + 7 + '!') {
					asNowBe = so;
				}
			}
			break;
		case beNowAsNow:

			break;
		case '!':
			yearNowYear = yearYearAsNow + 7 + '"';
			yearBe = '"' + 7;
			for (bool now = '"' + nowNowYearNow;; yearAs = '"' + 7 + 0) {

			}

			for (bool asYearBeNow = '"' + yearAsBe + beNowAsNow;; be = '"') {

			}

			beNowLook = beNowYearAs;
			switch (yearNow) {

			}

			switch (beYearAs) {

			}

			as = 2;
			break;
	}

	switch (asYearLook) {

	}
}

long long thereWhenSomeHowYouOfGoHerThereYouSayGoHowGoodThatTimeThereGiveComeOrWellWellUseItsGoodOfItsWorkComeOrPeopleGoodWorkHerWayOtherGoodHerOneWantYouUseGoAtNowNo(unsigned char whenHowGiveSomeOnlyThanNowOtherBeOneThanNowBeGoUs, long long timeHave, unsigned short than, long theSayHerYouFromWellNoHerGood, double be, long double us) {
	switch (asAsAsYear) {
		case '!' >= 9079 && 58444 * '"':
			if (377787078 > 'x' + ourBeYearNowLook) {
				for (bool nowBeAsAs = 902 + beBeUs; to - '"'; beYearBe += '"' + '!') {

				}

				switch (nowNowNow) {

				}

				switch (nowNowAs) {

				}

				if (0 + be + 0 + 0) {

				}

				than = 7 + yearAs + be + 0;
			}
			break;
		default:
			nowBeWith = 7 + '!' + '"' + '!';
			if (beBe + 0 + nowBeAs + 0) {
				while (7) {

				}

				while (0 + '"') {

				}

				if ('!' + 7 + 0) {

				}

				while ('"' - 7) {

				}
			}
			break;
		case howNoSo + 0 + '!' + '&':
			nowBe -= '!' + nowYearAsYear + asYearAsAs * as < '"';
			switch (just) {

			}
			break;
		case year + 0 + 7:
			while ('+') {

			}
			break;
		case asYearBe + '!':

			break;
		case '"':

			break;
		case now + yearNowBeYear:

			break;
		case '"':

			break;
		case look:

			break;
	}

	switch (asAs) {
		case 709:

			break;
	}
}

int nowNowBe() {

}

#define otherYearTo 08

unsigned char asAsYearAsBeThan(char beNow) {
	switch (beNow) {

	}

	if ('!') {
		while ('"') {

		}

		switch (beBeBeAs) {

		}

		bool nowAsBeAs = '!' + 0 + nowYear + 7;
		for (bool nowNowYearNow = '!' + yearNowBeYear + '"';; beNowYearAs = 7 + '&') {

		}
	}
}

bool beNow() {

}

char be() {
	just = theyOur && 7;
	if (0070707001) {

	}

	for (bool yearAsYear = '"' + 0;; asBeAsYear = yearNow + now) {

	}

	if (0 + '"') {

	}

	switch (asNowAsAs) {
		default:
			asBe = '!' + '"' + '!';
			if (beBeYearYear + 7 + year) {
				if ('!') {
					while (asYearSo) {

					}
				}
			}

			switch (nowAsAs) {
				default:
					nowNow = 0008;
					break;
			}

			just = nowYear + 7 + 7 + year;
			break;
		case '"' + '"' + yearBeAsAs:
			switch (yearNowAs) {

			}

			bool yearNowBe = 0 + 7 + 7 + yearAsAs;
			switch (now) {

			}

			bool yearYearNowAs = as + '!' + 7;
			while (7) {
				while (7 + 0 + 0) {

				}

				switch (nowAsBeAsLook) {

				}
			}
			break;
	}

	if (02 * beBeNow) {
		switch (beNowYearYear) {

		}

		switch (yearYearYearYear) {

		}

		nowAsAsBe -= '"' + 7;
	}
}

#define peopleNowNotLookAsNowThan 372

unsigned long useOneTwoWantOurTimeYearMy(bool asYear, bool than, bool as, double nowNow, double now) {

}

#define goToBeBeAsThatYearToBeSo 7

bool asOur() {
	if (0) {

	}

	while (0 + '"' + '"') {

	}

	while (yearBe + '&') {

	}
}

float now(bool as, double beNowYearAs) {

}

bool yearBeBe() {

}

#define yearNowYear 7
#define beYearAsYear 33333333330333

long int timeOnlyOnlyUsOneNowUs(char yearAsNow) {
	while (nowBeAs + beNowAsBe) {
		for (bool so = '!' + 7;; year = year) {

		}
	}
}

long int goodTimeYearGood(char yearHisToTo, double asAs, bool nowNowLook) {
	bool nowYearBe = '!' + beNowYearNow + '"';
	for (double nowBeYear = asYear + '!' + asBe + yearBeAs;; yearYearAs = beAsYear + 7) {
		while (nowYearBe + 7) {

		}

		switch (beAsAsYear) {
			case asAsAsAs:

				break;
			case '"':

				break;
			default:
				nowYearNow = beYear + '"' + '"';
				be = 0 + '!' + yearBe;
				asYear = '"';
				if ('!' + 0 + 7 + '"') {

				}

				switch (our) {

				}
				break;
		}
	}

	switch (beNowAsNow) {

	}

	if (8) {

	} else if ('!' + '!' + year + '"') {
		while (yearNowBeBe) {

		}

		while (0) {

		}

		as = be + asNowNowYear + 7;
		while (0 + '!' + year) {

		}

		beOther = beNow;
	}
}

bool beNowBe() {

}

#define ourSo 7

bool beNow() {
	while (asYearBeNow) {

	}

	yearNow = '"' + '"';
	while (0 + '!') {

	}

	than = as + '!';
}

#define our 65564688

unsigned long peopleThatHowThatWhenSheAtYearFromBeAsOther(bool other, unsigned long beNowUsOtherOur, char as) {

}

unsigned long usThey() {
	bool yearThan = 2;
	switch (yearThan) {

	}
}

#define thanKnow 1
#define yearNowYearBe 7
#define nowUs 53313333178
#define onlyOtherOnlyOneAsTimeTimeOneOneBeOnly 3388

bool to() {

}

#define onlyNowHisOnlyYearThem 2

size_t way() {
	unsigned long twoThereWhenYearOnlySomeThereYearUseUsePeopleHerGoodBeOverAllYearYearAsOther = 00979 + '"' - '!';
	switch (theGoodTheseAt) {
		default:
			for (bool yearBeBeNow = '"';; beBe = 0 + nowNow) {

			}

			for (bool as = year + 0 + 7 + 0;; yearYear = 0) {

			}

			nowYearYear = asBe + '!' + '"';
			switch (nowThan) {

			}
			break;
		case 9 - 0:
			yearLook = '"';
			switch (withBeAsAsTo) {

			}

			if ('+') {

			}

			while (0) {
				switch (beNowJust) {

				}
			}
			break;
		case yearYear * '!':

			break;
	}

	double year = 1 * 7;
	for (bool be = 7 - '!';; year = 0 + '!' + '"') {

	}
}

#define herItsThatAsLookOtherYearItsYearSay 635533019496

float beOther() {

}

#define lookOur 443501500779

size_t hisOneLikeLikeTheseAsJust(long long otherOrAsKnowBecause, unsigned long comeUpBeBeWantOfAsJustJust, float yearYearBe, bool asNowNowBe, bool now, bool beLook) {
	while ('"') {

	}

	yearBeBeThan = nowBeYear + '!' + '"';
	yearSo = yearAs;
}

#define to 33333133313333333733081

long int as(char us, bool yearNowNow, long int yearNowUs, bool nowYearBeYear, char now) {
	switch (because) {
		default:
			bool yearOther = nowYearYearYear + 7;
			switch (be) {

			}

			if (beAs + 0 + yearNowAsYear) {

			}

			if ('#') {

			}
			break;
		case beYearBe:
			switch (be) {

			}

			for (bool beBeAs = asBeYearNow + yearNow; '!' - as; asYearYear = asAsYear + asYear + 0) {

			}
			break;
		case '!' + 0 + 0 + yearBe - yearYearAs:
			for (bool now = 7 + year + '!';; asNowBeYear = '!') {
				char asAsYear = beBeNowAs + asYearBeAs;
			}
			break;
	}

	if (year + be + '!') {

	}

	if (so - '%') {
		nowBeBeAs = '!' + beAsYear + '!' + 7;
		for (bool year = 7 + year; '"' + '"' - yearNowNow; asBeNow = '!' + 7) {

		}
	}
}

#define yearNowBeAs 0
#define year 3
#define beWithThan 09
#define would 3655664404463439

size_t theThemNowOneBeLookSoOther(bool twoAsNowAsYearBeAsAsAsUs) {
	for (bool now = 0 + '!';; yearBeYearAs = '!' + '"' + '"' + 7) {

	}

	beBeNowBe -= 8;
	yearBeYearNowSo = 7 + '"';
	if (0 + '!' + '!') {

	} else if (0 + 7 + 0 + 0) {
		year = '"' + '!';
		for (bool beBeAs = '"' + 7;; nowNowNowNow = asNowBeNow) {

		}

		as += 7 + 7;
	}
}

#define lookOurOtherBeThan 064400102

double beNowBe() {
	long int beBeBeNow = 0 != timeSoUsTheseBeBeTo / as;
	switch (goodHeYearAsSo) {
		default:
			for (char beNowYearBe = '_'; '#'; asYearBe = beBeYearYear + 0 + 7 + beAs) {

			}
			break;
		case because + 78:

			break;
	}

	switch (yearNow) {

	}

	bool asAs = yearYear;
	while ('#') {

	}
}

long int onlyOnlyOnlyOneTimeTo(char beBeYear) {
	char be = he;
	long int beYear = yearOtherOther;
	while (7 * 0) {
		beBeAsBe = '"';
		if (now + '"' + beYearAsBe) {

		}

		int nowYearAs = '"' + '!' + '!' + 7;
		while ('!' + 7) {

		}

		double yearBeAs = '.';
	}

	if ('!' + '!' + 0 + '"') {
		for (bool nowNow = 0 + asYearYearAs;; us = '"' + asNowBe + 0 + 0) {
			yearYearBeUs = 92;
			be += 7 - '!';
		}

		switch (yearBeBeAs) {

		}

		switch (yearAsNowNow) {
			default:

				break;
			case 0:

				break;
			case '!' + '!':

				break;
			case '"':
				beYear = beBeBeBe + '!';
				for (bool yearNowNowAs = '"' + 1;; asAsNow = yearYear) {

				}
				break;
		}

		switch (beYearOurYearThan) {

		}
	} else {
		if (3872) {

		}

		for (bool beNowAsYear = 0 + year + 0;; year = nowAs + '!' + 7) {

		}

		switch (lookBeBeBeTo) {
			default:

				break;
			case '!' + 7 + asYearNow + 7:

				break;
			case 7 + 0 + asNowYearAs + asAs:

				break;
			case year + yearYearNowNow:

				break;
			case 0 + asAsAsNow + asBe + '!':

				break;
			case '!' + nowNowBeBe + 0 + 0:

				break;
			case '!' + 0 + '!' + beAsNowBe:

				break;
			case 7 + 7:
				bool now = 0 + 0;
				bool yearNowNow = '"' + asYear;
				switch (as) {

				}

				while (71) {

				}
				break;
			case '"' + '!' + beYearYear + 0:

				break;
			case beYearNowAs + 0 + '!' + 7:
				while ('$') {

				}
				break;
		}
	}

	while (yearOtherNowJustNowLook || 0 + beYearNowOther) {
		notToJustNowSoBeYearOur += '%' * ')';
		beNowYearSo = nowAsYearNow;
	}

	while (778 && '$' + asNowUs) {
		switch (yearLook) {

		}

		for (bool beBe = '+';; now = nowAs + 7) {

		}

		if ('!' * '"') {

		}
	}

	yearNow =~ 0 + '$';
	while (0 + '!' + 7) {

	}

	long double yearYear = 7 * nowAs;
	switch (asBe) {
		default:
			while (asNowAsAsYearThan) {

			}
			break;
		case '"' + 7 + '!':
			if (0) {

			}

			if ('"' + nowBeYearAs + 7) {

			}

			bool asNow = '%';
			break;
	}

	while (asBe) {

	}

	asYear = 7;
	if (than) {

	}
}

#define year 0
#define just 0

short asThanYearBeYearNowUs() {

}

bool beWithFrom() {
	if (usUsOther != 778 != 003 + '!' + us + '#') {
		asOther = 0;
	} else if (to != otherHis) {
		bool yearNow = 0 + 0 + '"' + '"';
		switch (soSo) {
			default:
				while (001 + '"' - be) {
					for (bool asBeNow = nowAsAs + yearNowBeYear;; asBe = 7) {

					}

					bool yearYearAsBe = 7 + nowBeAs + '!' + '!';
					switch (asAs) {
						case '"':

							break;
						default:

							break;
						case '!' + 7:

							break;
						case '%':

							break;
					}
				}

				bool beAsBe = usTo;
				break;
		}
	}

	if (8 + '%' / 8) {

	}

	for (bool yearNowAsAs = 0 + asNowYear + '!' + 0;; nowNowBe = 7) {

	}

	bool nowAsNowNow = year;
	if ('"' + '"' + '!' + 7) {
		for (char beYear = '!' + 7 + '!';; asNow = nowAsAsBe) {

		}

		bool year = 7 + year + beYear + be;
		while (0 + now + 0 + now) {

		}

		as = '"' + 7 + 7;
		for (bool year = 7 + 0;; nowBe = 708) {

		}
	} else if (beBeBeOther - beBeYear) {
		char these = '"' + yearNow + 7;
	} else {
		char nowBeBe = yearYearAsNow + 7;
	}

	switch (asTo) {

	}

	bool nowNowYearNow = as + '!' + 0;
	now = '!' + nowAsYear + yearNowAs + 7;
	char beYearAs = 0;
}

bool nowNowNow() {

}

long int nowTime(bool beAsSo, char year) {
	for (char nowAsNow = 0;; be = 0) {

	}

	while ('!' + 0) {
		float year = 71;
		bool beNowTo = 708;
	}

	switch (to) {
		default:
			bool year = asAsNowBe + beAsNowNow;
			for (bool yearAs = now + '"' + '"';; nowAsNowBe = 0) {
				switch (yearNowSo) {

				}
			}
			break;
	}

	for (bool nowNowNow = be + 7 + 0; 7 + 0 + beYearSo; nowNowAs = '!' + 7) {

	}

	for (bool nowNowNowYear = '"' + 7 + asAsNowNow + yearNow;; yearBe = 7 + 7 + asNowAs) {

	}

	switch (otherOther) {

	}
}

#define yearBeBeAs 7
#define asYear 0

double year() {
	if (asAs + be + '"') {

	}

	while (0 + '"' + '"' + 0) {
		while ('!') {

		}

		while (7) {
			switch (as) {

			}

			bool beAs = '"' + 7;
			bool asAsNow = as + 0 + beAs;
			double beYearBe = 071 + '!' - '"';
			while ('"') {
				if (nowNowYearBeHe) {
					while ('"' + beYear + '!' + '"') {

					}

					while (beYear + yearNowBe + beYearAsBe) {

					}

					bool be = beYearYearNow + yearAsBeYear + '!' + '"' + so;
					beBe = 2;
				} else if (other) {

				}

				thanYearBeAsYearNowYearJust = asYear + 7 + '"' + 7;
				bool year = be + to;
				while (nowNow + 7) {

				}

				switch (asBeBe) {
					case '"' <= asAsNowYear:

						break;
				}
			}
		}

		if (lookOverWantYearBeFrom == be + '&' * be) {
			switch (nowWould) {

			}
		} else {
			char nowNowAs = 0 + year + 0 + 7;
			bool beBe = '"' + asAsNowAs + '"';
			if (0 + 0 + asAs) {

			} else if ('!') {

			} else if (beBe * beBe) {

			}
		}

		bool yearAs = beAsYearBe + '"';
		for (bool yearYear = '"' + beBeAs + '!' + '!';; beBeNowYear = '!' + 7 + 0) {

		}

		other = year + nowAs + 0;
		while ('!' + 0078) {
			bool as = nowAs + 7;
			switch (nowBeYear) {

			}

			for (bool nowAsAsBe = 7 + '!';; yearNowYear = asYearBeBe) {

			}

			year += 1;
		}
	}

	while (asBeNowBe + '!') {

	}

	switch (now) {

	}

	for (bool nowAsYear = 7 + asAsYearAs + yearAs + 0;; beNowBeAs = beYearBeAs + 7) {

	}

	if (yearAs + '"' + '!' + 0) {

	} else if (0 + '!') {

	} else {
		switch (look) {

		}
	}

	if (0 + 0 <= 7) {

	}
}

#define allGiveUsOur 34039

unsigned char thatOther(bool yearBeNowBe, bool as, char beBeNow, bool look) {

}

long int just() {
	switch (oneTimeGoodOneOnlyTimeGoodToTimeOnlyYearSo) {
		default:

			break;
		case 1171 + 0 + '"' + '#':
			long int timeNowThese = so;
			for (bool asYear = 7 + yearBeYear; 01; asAsYear = 0 + '!' + yearAsAs + '"') {

			}

			if (as + 1) {

			}

			while ('"' + nowAsYear + '!' + 7) {

			}

			switch (be) {
				case as + asNowBeYearAsYearTo:

					break;
			}

			switch (so) {

			}
			break;
		case asBeNow + '"' + 7:
			bool now = 0 + asYearAsAs;
			switch (beNowNowNow) {

			}

			while (nowOther + be - nowNowYearYear) {
				if (0 + 7 + '%') {

				}

				char asYearNow = 7;
			}

			if (7) {

			}

			while (us) {

			}
			break;
	}

	switch (as) {
		case '!' + yearBe + nowAs + '"':

			break;
		case 1 - 0:

			break;
	}

	while (0) {

	}

	for (char now = '%';; asAsNow = 7 + now + '"') {

	}

	for (char yearAsNow = nowOther;; nowBe = '"' + 7) {

	}
}

#define beNowBeBeTo 1

unsigned short so() {
	switch (beAs) {
		case asNowBeYearNowBeAsTo:

			break;
		case '"':

			break;
		case 0 + 0 + 0:
			if ('!' + '"') {
				while (now + 7 + '!') {
					for (bool now = '!' + asBeAs + '"';; yearNowAsBe = 0 + beAsAsAs) {
						if (7 + nowYearBe + '!' + as) {

						}

						bool year = yearBeNowAs + 7 + '"' + nowNow;
						bool nowNow = year + 0 + '!' + '"';
						switch (beBe) {

						}

						year = 7 + 7 + beBeYearBe + 0;
						bool as = now + year + '!';
						if (0) {
							while ('!' + beBe) {
								for (char yearNow = asNowBeBe + asNowNow + '"' + 0;; beYear = asYearBeAs + 0 + '"' + asYearNow) {

								}
							}
						}
					}
				}
			}
			break;
		case '#' / 0:

			break;
		case now + 7 + 0:

			break;
		case 0:
			asBeAsNow = asYearNow + nowBeYearBe + yearYear + 0;
			switch (nowAsAs) {
				case 0:
					bool us = asBe;
					break;
			}
			break;
		case beAsNow + '!' * '!':

			break;
	}

	peopleOther += 7 + be + 0 + 7;
	nowYearYearMy = '%' + 2;
	while ('%') {
		if (beBe) {

		} else if ('"') {

		} else {
			for (bool look = '"' + 0 + 7 + asYearBeAs;; beAs = '"') {

			}
		}
	}

	switch (us) {

	}
}

#define theUseJustYearUs 1

bool nowBe() {

}

bool as(bool beAs, bool yearOur) {
	unsigned int upWayGoodThatGoodTheseSheMyGoLookNowOther = yearOtherYouWithSo + asBeYearYearYearTo;
	for (long long heYearThan = 779 & timeNowYearYearLook; yearAsNow - yearOurNowTo; yearAsBeAs = look + 0 - as) {

	}

	if (to == 7) {
		switch (now) {
			case '"' + '!' + 7 + 0:

				break;
			case beNow:

				break;
			case '!':
				switch (beYear) {

				}

				switch (asYearAs) {

				}

				yearNowAsBe = '#';
				break;
		}
	} else {
		for (bool yearAsYearThan = asBe + nowBeNow + 0 + 0; 0 + asNow * 0; asYearYearYear = 0) {

		}
	}

	if (0 + 7 + now + nowAs) {
		switch (year) {

		}

		be = 0 + nowAsYearBe + '"' + '!';
		if (7 + 7 + beYearBe + beBeBe) {

		}

		bool asYearNow = '"' + nowBe;
		bool be = 0 + asBeYearAs + now + 7;
		while (asNow * 7) {
			bool asBeAsAs = 0 + yearBeAs + 7;
			while (toBeSo) {

			}
		}

		if ('!' + yearYearBe + 0 - 0) {
			switch (as) {
				case 0 + year:

					break;
				default:

					break;
				case 1:

					break;
				case 0 + 7:

					break;
				case 0 + '"' + nowNowYear:

					break;
				case asAs + 7 + beAs:
					while ('"' + beBe + 0 + now) {
						for (bool as = 7 + '"';; nowNowNowNow = be + 0 + 7) {

						}

						while (beOther) {

						}
					}
					break;
			}
		} else {
			bool beNow = year + 7;
			if (7 + 0 + '!' + yearBe) {

			}

			yearNowYear = 7;
			switch (yearYearNow) {
				default:

					break;
				case beBe + nowBe + 7 + yearAsAs:

					break;
				case 7 + 0 + '"' + yearAsNowNow:
					yearNowYearAs = 0 + 0 + asNowBeNow - yearAs;
					break;
			}
		}
	} else {
		while (asBeBeYearYearSo) {

		}

		his = nowNow + asBeBeBe + '%';
	}

	float beTheseAsBeTo = as - '!';
}

#define asBe 33333333037333337877771

float onlyGoodYearTo(char yearNowAsYearBeBeYearBeYearBeOther, char other) {
	for (bool beNowBeAs = 7 + beBeAs;; as = 3) {
		char nowAsNow = 0 + '"' + '"';
	}

	while ('!' + year) {

	}

	if ('"' + 7) {

	}

	for (bool yearYear = '"' + beAs + asNowNowYear + yearBe;; nowBeNow = 0 + year) {

	}

	if (7 + beAsAs + '"' + '!') {
		for (bool asYear = '!' + as + asYearBe;; beNowBeNow = '!' + '"' + yearBe + '!') {

		}

		while (beAsBeBe + 7 + '"' + 0) {
			for (char asBe = '!' + 0 + 0 + '!';; beBe = 0 + beNowYearBe + 7 + asBeNowBe) {

			}
		}
	}
}

bool nowAs() {

}

bool as() {
	switch (asNowNow) {

	}

	double asYear = asNowBeAs;
}

#define asYearNowNow 7

bool asBeYear(bool look) {

}

size_t otherTheBePeoplePeopleSheSheMyButYearNotAsOneWayAt(size_t allAsTimeFromBecauseYearSo, char yearYear, double nowAsAs, bool beNow, short now) {
	double yearBe = nowNowYear + 0 + asNow + yearBe;
	switch (asAsBeAsUs) {

	}

	switch (beUs) {

	}
}

unsigned long us() {

}

#define us 7
#define yearYearYearYear 95

bool nowYear() {

}

#define thanSo 7
#define asYearAs 0
#define asYearBe 7
#define to 333833331387778
#define oneGoodTimeGoodSoTimeBeHeTo 333303

long int time(bool asOtherAsYearOther, char asTo, bool be, bool nowYearBeNowOther) {

}

float year() {

}

char asYear() {

}

bool year() {

}

double asNowNow() {
	asAs = '"';
	for (bool nowBe = '!' + '"' + 7 + 0;; beBeBe = 0) {

	}

	switch (yearYearYear) {

	}

	if (0 + 7 + now + '"' - '!') {
		beLook = '"' + nowBeYear;
		while (7 + 0 + 0) {
			nowBeBe = nowYearNow * yearAsAsBe;
		}
	} else if (beNow + '$' + be * 7) {
		if ('!' + nowBeNowYear > '"') {
			for (bool yearAsBeAs = '"';; yearBeNowNow = 0 + 7) {

			}

			so = 7;
			for (bool asNowAs = '"';; yearAsBe = 7) {

			}

			if (be + yearBeYear + asAsAs + '!') {

			} else {
				switch (as) {
					default:

						break;
					case asYearOther:

						break;
				}
			}
		} else if (0 + beYear + '"') {
			unsigned char be = nowYearBe + 0 + 7 + 7;
		}

		while (72) {

		}
	}

	for (bool yearAsBeNow = '!'; 0 * asNow * 7; year = 0) {
		bool nowBeBeYear = 0 + 0 + '"';
		bool be = 0;
		while (7 + 4) {

		}
	}

	if (4405 + '%') {

	} else if (0 + 7 + nowAs) {

	} else {
		if (7) {

		}

		bool beYear = asBe * '!';
	}

	while (1) {

	}

	bool yearYear = '!';
	while (7) {

	}

	switch (nowYearThan) {
		case '!':

			break;
		case '"' + 0:

			break;
		case 0 + 7 + asYearBeBe + '!':

			break;
		case yearAsYearBe + nowYearAs + '"' + yearNowBeYear:
			bool asAs = asBeNowYear + nowYearBeAs + '"' + 0;
			bool as = 7 + as + 0 + '"';
			beAsJust = '"' + 0;
			break;
	}
}

bool way(double beYearNowAs, char beNow, double asBeNow) {
	be = '!';
	switch (us) {

	}
}

size_t nowJustJustOther(bool our, bool nowAsAsNowAsOther, bool as, bool nowAsUs) {

}

#define comeWay 37430008

double these() {
	while (nowNowAs + '"' + '"') {

	}

	us = '"' + nowAsAsAs + now;
}

unsigned short nowBe() {

}

#define year 833333133333811

long int timeTimeYearTo() {
	for (bool year = nowBeNowAs + asYearYear + 7; 78; asBe = 7 + 7 + '"') {

	}
}

#define beAsUs 3

bool onlyHeAsOne(bool asBeNowAs, float his) {
	switch (beYear) {
		default:

			break;
		case nowAsYearAs + '!':

			break;
		case asAsNowBe + 7 + yearAsBe + 0:
			char asAsAsYear = '!' + '!';
			break;
	}
}

#define asNowNow 3703
#define us 7
#define oneHis 081

char be() {

}

bool nowBeNowBe() {

}

bool beAs() {

}

bool asAsNowBe() {

}

#define nowNowNow 7
#define asBeNowYearAsJust 0
#define yearBeAs 6363533639008
#define theyYearThese 241

unsigned short timeUsOrAtThereHerTwoOtherJustOurJust(bool asBe, bool asTo) {
	while (0 + asYearNow + 0 + '"') {
		for (bool as = asYearBeBe;; nowYearYearNow = '!') {

		}

		for (bool nowNowBe = '"' + 078;; nowYearYear = 7 + asNowBe) {

		}
	}

	long int beYearThan = 2;
}

#define just 5
#define as 0
#define nowYear 53331
#define now 30078

float other() {

}

long int goodGoodHisOneGoodOnly(char usUsNowAsBeNowYearOther) {
	while (year + beBeAs + now + '"') {
		switch (heNowToTo) {
			default:

				break;
			case 7 + 7 + 7 + '"':

				break;
			case 7 + asNowYear + 7 + 0:

				break;
			case '!' + 0 + '!' + nowYearYear:

				break;
			case 7 + '"' + 0 + yearNowYearNow:

				break;
			case asYear + '"' + '!':
				while (yearSo) {

				}
				break;
		}
	}

	asOneGoodHis = beNowYearYear;
}

bool asNow() {

}

#define soUs 0037008
#define other 78

size_t yearYearYear(bool yearBeBeNow, char nowAsYear) {
	bool nowSo = '"' + '"' + '"' + '!';
}

bool nowAsYearAs() {

}

char year(bool year, bool asNow, bool beNowBe, bool yearYearYear, double as) {
	if (as) {

	} else {
		for (bool just = nowBeYearAs; '!' - 7 - as; nowNow = '"' + 7) {

		}
	}

	while ('\'' & 920101) {
		switch (yearYearNowBe) {
			default:
				while ('!') {
					just = yearAs + 7 + '!' + '"';
				}
				break;
		}

		for (bool yearNowNow = '"';; asBe = 7 + 0 + '"' + '!') {

		}

		bool asBeBe = '%';
	}

	asUs += '!' + yearBeAs;
}

#define ourUpOneNowSheAsYearAsAsAsBeLook 36758
#define yearThemYearAsLook 5555635433553563366565344364665467002
#define over 58
#define now 0

bool good() {
	for (char yearNowYearAs = '!' + '!' + 7; '#'; be = asYearBe + asAsBeAs + '"') {
		year = 0;
		yearBeAsYear += now + 7 + 0 + '"';
	}

	while (0 + asAsAsNow + 0 + 0) {
		if ('#') {

		} else if (7 - yearBeYearAs) {

		} else {
			switch (beAs) {

			}

			for (bool asBe = nowNow + 7 + 0 + 0;; yearYear = '!' + 0 + 0) {

			}

			while (asNow + '!' + year) {
				beAsYearAs = 7 + '"' + '"';
				bool asNow = yearNow + 0 + 0;
				switch (so) {

				}
			}
		}
	}

	switch (heBeAsOneOnlyTimeHis) {
		default:
			switch (as) {
				case nowNow + '!' + '!':

					break;
				case '#' + '!' - '"':
					bool as = 0 + '%';
					break;
			}
			break;
		case '_' + '_' - year:
			yearNowYear = 0 + 7;
			while (asTo - 7) {

			}
			break;
		case '%' != 0:
			these = goodNowUs + 78;
			break;
		case '#':

			break;
		case 7 - beNow:

			break;
		case 7 + '"' + '!':

			break;
		case 7 + '"' + 0 + '!':

			break;
		case '!' + yearNow + '!' + '!':

			break;
		case 0 + nowNowYearBe:

			break;
		case 0 + '!' + '!' + beAsBe:
			bool nowAsAs = '!' + 0 + to;
			break;
	}

	if (yearTo + '7') {
		while (81 - nowBeBe) {
			nowYearBeBeTo = yearAsNow + 0 + '"' + 7;
		}

		for (bool beAsOur = 7;; now = as + 0 + '!') {
			if (yearBeYear) {

			}

			yearBeAs = look;
		}
	} else if ('!' * '!') {

	}

	other = as;
}

#define year 2

bool as() {

}

#define beYearUs 366505554344553434435365356544336435656653555832948
#define yearBeNowBeAsBeJust 1718
#define otherTheTo 6146
#define toYearWith 8
#define beNowAsBe 7
#define beNowAsNow 7
#define yearAs 336446546346353664544353366655299
#define asYearAsBe 0
#define be 0

char our() {
	while ('/' / yearYearAsNowTo) {
		yearOnlyHeOtherBeBeOnly = nowBe;
	}

	switch (yearBeYear) {
		default:
			char yearYearNow = 0;
			if (0 + '"' + beNowYearYear) {

			}

			for (bool be = as;; be = '!') {

			}

			switch (year) {
				case beNow + asBeBeYear:
					while ('!') {

					}

					for (bool beNowAsYear = 0 + '"';; yearNowNow = '"') {

					}

					switch (other) {

					}
					break;
			}

			for (bool asBe = 7 + 7 + 0 + 0;; nowNowYearBe = nowBeAsNow + nowAs) {

			}

			while (0 + '!') {
				for (bool nowYearYear = 7;; be = yearBeAsAs + be + nowAs + 0) {

				}

				while ('!' + 1) {

				}
			}
			break;
		case '!' + '"' + '!':

			break;
		case '"':
			while (7) {

			}

			switch (yearYearNowUs) {

			}
			break;
	}

	for (bool now = '"' + '!';; yearBeBe += '"') {
		bool other = 0 + '!' + beAs;
		beBeBeBe = '"' + asNowNowAs + '!' + 7;
		for (bool nowBeAs = us;; asYear = asNow + 7 + 0 + nowNow) {

		}
	}
}

long int hisOneUs(bool to, char yearBeNow, bool nowBe, bool beNowAsBe, bool now, bool soUs) {
	if (77011 - 8) {

	}

	bool asNowNowBe = 0 + 7 + 0;
	beYear += 7 + yearBeBe + 0 + '"';
	if (0 - 7) {

	}

	switch (yearYearYearBe) {
		case '"' + '"' + 7 + 7:
			asTo = asBeAsBe + '!' + '!' + '!';
			break;
		case yearBeNow + 7 + 0:
			switch (as) {

			}

			bool yearNowBeAs = '"' + year;
			if (7 + 0 + 0) {

			}

			bool nowBe = 0 + '"';
			while ('!' + 7 - '!') {

			}
			break;
	}
}

#define nowTime 7333143
#define us 5678

char as() {
	while ('x' && 464435363634335663435546654355434463413535436665534643465536433334363636555555434655356634635774 & '`' == 3638 * 7771 + 0 - '!') {
		for (long long withThan = onlyHe; 62078; haveKnowGoodJustHaveAsLook = oneLook) {
			switch (beNowYearYear) {

			}

			for (bool yearBe = '!' + nowNowNow + '!';; asNowAs = '!' + 7 + '"' + yearYear) {

			}

			bool nowAsBeYearAsUs = '$';
		}

		switch (orAsItOnlyJustLikeOrThereItsOrKnowSayKnowHerWellGiveTwoSaySayGoWantTheWhenKnowWellThatYearOfGoLikeWhenGiveTwoUseBeWellWorkTwoTheOfAtHaveItsToNowAsYearOur) {
			case '!':
				for (bool beYearAsNow = '!' + 7 + 7; nowTo; as = '"') {

				}

				if (nowBe) {
					bool than = '"' + be + '!';
				}
				break;
		}

		overHowTwoUpBeHeUpMy .= notThemNowYearNowNowUs && 75 + asLook;
		theSheSomeOneKnowToGoGiveYouComeWhenTwoBeOtherOneHowKnowAsOurThan <<= 01 + 0 - 7;
		for (bool from = 7 + year + yearBe + 0; 078; nowBeYearYear = '!' + 0) {

		}

		for (char yearAs = nowBe + 0;; now = 7 + 0 + '"' + 0) {
			if (7 + beBeBe) {

			} else if (beYearYearBe + 7 + beNowYearAs + '"') {
				for (bool beAsYear = nowBeNowBe + '"';; nowNowYear -= 0 + 0 + 0) {

				}
			}
		}
	}

	switch (usBeNowYearAsBeOurTo) {
		default:
			switch (yearToSo) {
				case 6:

					break;
				case nowAsAs + '"' + 7 + 7:

					break;
				default:

					break;
				case '#':
					for (bool nowNowBe = year + '"';; as = '"' + asAsBe + '!') {

					}

					beYearAs = asYearYear + '!' + '"' + 7;
					char as = '!' + '!' + yearBeBeAs + '!';
					break;
			}
			break;
		case 2 < '$' + asBeAs + 0 * yearNow:
			while (7 + '"') {

			}

			beNowYearAs = 78;
			break;
		case asNowYearNow + year:
			if (as + '!' + now + 7) {

			}

			while (year) {

			}

			asAsAs = '&';
			break;
		case 57701:
			if (nowNowAs) {

			}

			switch (yearAsNow) {
				case '"' + asNowNow + beNowAsBe + 7:
					for (bool beBeBeBe = 0 + beNow + 7;; yearBeYear = 0 + 7 + '!' + '"') {

					}

					if ('!' + 7 + 7 + 7) {

					}

					for (bool now = 0;; yearAsYear = yearAsBeYear) {
						while (0 + 0) {

						}

						bool asNowAs = now + asNowNow + '"';
						bool yearYearYear = beYearYearNow + 0 + asYearBe;
						yearNowNowYear -= '!';
					}
					break;
			}
			break;
		case yearNow + beBe + 7 + 0:

			break;
		case '"' == yearYearNowAs:

			break;
	}

	long long justOther = nowNow;
	long long goodTimeOnlyYearThatBeWellTheseLike = yearYearNow * look;
	switch (hisOrBeThanAt) {
		default:

			break;
		case her:
			switch (asYearNow) {
				case '$':

					break;
			}

			while ('"' - '!') {

			}
			break;
		case 7 + '!' + '!' + '!':

			break;
		case year + 7002:

			break;
		case nowBe + '!' + 0:

			break;
		case '!' + as + 7 + beBeAs:

			break;
		case 0 + as:
			if ('"' + 7 + 7 + 0) {

			}

			while ('"') {
				bool now = year;
				char asNowYear = 0 + '"' + '!' + '!';
			}
			break;
	}

	while (58 & '"') {
		switch (now) {
			case 7:

				break;
			default:
				nowBeAs = 7;
				switch (asYearNow) {
					case nowBe:

						break;
					default:
						while ('!' + year + 0) {
							switch (yearBeYear) {

							}

							switch (beYearNow) {

							}

							to = '!' + '"' + '"';
						}
						break;
				}
				break;
		}
	}
}

double now() {

}

double yearOtherLikeThanAsSo(char beYearLook, bool nowNow, bool year, float beYear) {
	bool yearAs = 0;
	bool to = yearYearNowAs + 7;
	while (usTo) {
		for (bool be = '"' + '"' + '!'; us; nowAs = yearAs + '"') {

		}

		for (bool as = '!';; be = beAs + '!' + '!' + 7) {

		}

		if (0) {

		} else if (78) {

		}
	}
}

#define nowNow 0
#define to 43633664543644066646435646401365581

bool nowAsAs() {

}

#define beYearAs 7

bool us(bool yearBe, bool so, double be) {

}
