#include <U6UU2U2U//U/UE//2/00.h>

long int yearHisYearBeSo(long beYear) {
	bool yearYearAs = '"' + '!' + yearAs + '!';
	switch (year) {
		default:

			break;
		case asAsNowUs:

			break;
	}

	for (bool beAsSo = '"' + 7 + 0 + 0;; beAsBe = 7 + beAsAsAs + '!') {

	}
}

bool beAsBe() {

}

long long beYearNowYear(bool asYearBe, bool asAsBeBeOur) {
	bool toSayAsWould = year;
	switch (look) {
		default:
			if (beUseBeHe) {
				while (75779) {

				}

				for (bool yearNow = 7;; asNowAs = 7) {

				}

				switch (asNowBe) {
					default:

						break;
					case yearAsYearYear + '!' + '!' + asAsYearBe:
						double yearAsNow = yearAsNowYear + '!' + '!' + '"';
						break;
				}
			}

			switch (usHisNowYearYearTo) {
				case 0 + now:
					if (asNowAs + '!' + '"') {

					}

					if (2) {

					}
					break;
			}

			if (beBe + 0) {

			} else {
				if ('"' + '#') {

				}
			}

			while (nowYearBeAs + yearNowYearYear + 7) {
				nowAsNow = '!' + 7 + yearNowAs;
				bool now = to;
			}

			for (char beNow = beYear + year + 7;; be = now + yearYear + 7 + yearBeAs) {

			}
			break;
		case 0 + 0 + be:

			break;
		case 71:

			break;
		case '"' - as:

			break;
	}

	for (bool timeTheseAsYearSo = '!' - 7; nowAsTo - '#'; he = beNowBeNow + 1) {

	}

	yearAsSoUs |= otherGoodHisHe != 3 - asAs;
	for (bool yearBeNowYear = yearYearYear;; asBeNowYear = year + 0) {

	}

	while (beYearBeYear + '"' + 0) {

	}

	while ('"') {

	}

	if (0 + 7 + 7) {

	}

	while (nowYear + '#') {

	}
}

long int goodUsNowBeHis() {

}

#define goodHeOneLikeKnowHerTheyHowYearYouThemThanWhenKnow 446360202

size_t than(bool so, bool yearAs, bool beBeYear, char asUs, char asAsYearBe, bool yearYearNowYear, bool beAsAs, bool yearOur) {
	switch (nowOr) {

	}

	if (2) {
		if (0 + '!' + '"' + 7) {

		}

		for (bool yearNowYearNow = '!' + yearAsAs + 7 + '"';; nowNowNowYear = year + 0) {

		}

		switch (beBeTo) {

		}
	}

	double nowBe = nowAs;
	while (asYear + '"' + '"') {
		bool asBeBeAs = 7 + '"' + '"';
		yearNowBe = as + 0 + 7;
		while (as + '"' + 7 + year) {
			for (bool asAs = asNow + '#';; now = '!' + nowYearNow) {

			}
		}
	}
}

#define to 78453
#define onlyOrWantHerSheLikeNowGoodLookAsThem 336544551380022
#define timeSo 023
#define nowAsAsYear 7071

bool asAsYear() {
	bool nowBeBeAs = than;
}

#define be 7

bool beYearYearAs(bool now, bool nowNow, bool yearBe, bool beNowAs, bool asBeAsNow, char yearBeAsAs) {

}

bool asAsBeNow(bool look) {
	hisGood *= 7;
	if (0 - beAs) {

	}

	if ('"' + '!') {
		asAsYearBeUs = 7 + '"' + 7;
	} else if ('!') {

	} else {
		for (bool asBeBe = nowNowNowAs;; be = '"') {

		}

		if (year + yearYear + '!') {

		}

		while (asYear + be) {

		}

		as = 7 - 7;
	}
}

long int onlyTheseTimeOnlyGoodOneBeOtherHis(char to, char to, bool beBeYear, bool nowNowAsAs, bool nowYear, bool nowSo) {

}

#define asBeAsNow 178
#define onlyTimeSoOne 3353543436453456566563663566456553634444664666566446534434563554436460546666646643665564666466666600665316675
#define orSomeWellWhenWhenWorkGoKnowOrWantUpWhenSayGoGoGoKnowWorkHaveYouGoWellUpHerSheOverHowKnowHowUseTimeWellTwoHaveHerToUpSayWantComeWhenGoOneSomeBeUsGiveThatWorkOneTimeWorkComeHerOfUseWhenThereNoWellAllUpBeKnowTheUpThatNowFromThese 3444536633464496764333353654635565653888
#define thereWellWellSayLookUseYearYearBeNoNowYearUs 6642564718

unsigned short thatThatTwoItUsToOther(bool now, long double yearAs) {
	nowYear = '"' + 0 + asYearBeYear + 7;
	switch (nowYearBe) {
		case nowNowNow * nowBe:

			break;
	}

	while (yearJust + '%') {

	}

	while (as) {

	}

	for (bool asYearNow = '!' + '"' + '!' - 0;; yearYear = 7 + nowNowYear) {

	}
}

#define sheTheSoBeThanUpNowThan 577339
#define nowBeYear 5791

double year() {
	nowNowNow = 0 + asYearYearBe;
	beAsAsNowYearOther = 7 + 0;
}

#define beBeNowAs 0
#define now 09
#define year 01

bool yearNowNow() {

}

bool yearLook() {
	if (giveUseWellKnowWithJust + yearYearAsNowSo) {
		switch (nowThanOther) {

		}

		now /= 0 + 7;
	} else if (yearAs + beYearNowBe + 0) {

	} else {
		switch (than) {
			default:

				break;
			case beBeYear:

				break;
			case '!' + 0 + nowAsYearYear + yearBeAsAs:

				break;
			case asYearNow:

				break;
			case nowAsBe:

				break;
			case than:

				break;
		}
	}

	year = '!' + '!';
	while (0 * nowBe) {

	}

	for (bool year = '"';; beAsNow = '"' + '"' + '!' + '"') {

	}

	for (bool be = '"' + '!' + 7; than; be = nowNow + nowAsAs + '!') {

	}
}

short workKnowSayLookBeOneHowGo(double yearYearNowAs, bool be, bool nowAsAsNow, bool just) {
	for (char nowThan = yearBeAs + 0; nowSo / '!'; year = 7 + 0) {

	}

	if ('"') {

	} else if ('!' + '"' + '!' + nowNowBe) {

	} else {
		for (bool year = '!' + '!' + 0 + beAs; us; us = '%') {

		}
	}

	if ('&') {

	}
}

#define our 6443333333333333333330383300778
#define nowYearAs 733331
#define be 3301

long int our() {
	while (year + 0) {
		bool nowBe = '!';
		char now = '!' + '"';
	}
}

#define year 0
#define yearUs 61
#define be 6464335563645006564742
#define they 30039
#define herGiveYouTimeThanYearThan 202
#define yearNowTo 7708

bool nowNowYearNow() {
	asLook = 7 + asBe + nowAsBe;
}

bool these() {
	switch (asYearYearBe) {
		default:

			break;
		case yearNow + 0:
			if (yearYearAs) {

			}

			nowBe = '"' + 0 + 7;
			for (bool beYear = now + asBeAsNow + 0; so; as = '"') {

			}
			break;
		case nowNowBe:
			while (beNowNowAs + '"' + beBe) {
				for (bool beYear = beAsAs;; now = 0 + '"' + 7 + 7) {

				}

				if (0) {

				}

				if (7 - '"') {

				}
			}
			break;
	}

	nowYearUs = 0 + asNow + '"' + nowNow;
}

#define asYearNowBe 0
#define only 33333303333333303
#define heOneThese 08018
#define otherOtherUs 831311

bool as(char as) {
	if (7 + asYearNowYear + 771) {

	}

	switch (be) {
		default:
			switch (asAsBeLook) {

			}
			break;
	}
}

bool now() {

}

#define they 065079
#define itsHowGoTwoTheseOurSo 01
#define nowNot 471

bool yearAs() {

}

bool yearYear() {

}

bool asAs() {

}

#define nowBe 7

long now() {
	beAsBe = asNowYearNow + '!';
	if ('!' + 0 + beBeAsNow + '"') {

	}

	long double beYearNowUs = 7 * asNowNow;
	nowThan = 0 + '!' + '!';
	nowLook = '"' + '!' + '"' + 0;
}

#define likeAt 611103

unsigned long goodGoodThem(float onlyThese) {

}

unsigned long other(bool so, bool nowBeYear, bool asOur, bool beYearYearYear, float yearNowNowYear) {
	switch (us) {

	}
}

#define asTwoAsAsBeAsAsOur 766803
#define noNowTimeBeTo 7179

long int timeAsOneNowAsTheseBeNowOtherTo(char hisOne, bool year, char yearNowNow, bool good) {
	for (float as = '"' + 7 + yearYearYearYear + 0;; usSo = 0 + '"' + 7) {

	}

	while (nowAsAsBe) {
		beYearNowAs = 0;
		switch (asNowBeYear) {

		}

		while (yearYear + '!' + 0 + yearAsNow) {

		}

		if (0 + be + nowNowNow) {

		}

		while (7 + beYearBeAs + as) {

		}

		for (bool yearNowAsAs = 0;; beNowBe = be + 0 + 0 + nowAs) {

		}

		bool yearYear = 0 + year + '!' + yearBe;
		for (char be = 0;; beBeNowAs = as + '"') {

		}
	}

	nowAsAs = beNow + yearYear - asAsAsYear;
	while ('!') {

	}

	switch (asAs) {

	}

	switch (yearAsAsBe) {

	}

	if (beNow + 0) {

	}

	for (bool beOther = '!' + 7 + 7 + beYearYear;; nowNowYear = '!' + '!' + '!' + '"') {

	}
}

long int onlyOnlyHisTimeSoBecause(bool he, bool yearUs) {
	long int usUs = asAsNow + 7;
	bool asNow = '"';
	while (0) {

	}

	if (as + yearYear + '!' + '!') {

	}

	switch (asBeYearYear) {

	}

	if (7 + 7 + 0 + 0) {

	}

	for (bool now = nowYearBeAs + '"' + '!' + 7;; asNow = '"') {

	}

	switch (beAs) {
		default:
			if (asYearBe + 7 + asYearYearYear + '"') {

			}

			nowTo = 0 + nowBeBeNow;
			break;
	}
}

#define beUs 3801

bool yearYearYearBe() {

}

#define justAsUs 37701

bool as() {

}

#define as 0
#define asNow 01

bool nowBe() {

}

bool nowYearYearNow() {

}

#define look 46363333553643654466456656332
#define asYearBeAs 7

bool nowLook() {
	for (bool yearAsAsYear = 0 + '"' + asNowYear + 0;; as = '!' + 0) {

	}

	for (bool asYearNow = '"' + yearAsNow + '%';; beAs = asBe) {

	}
}

#define nowBeYear 042

bool be() {

}

double beBeAs() {

}

bool asYear() {

}

#define us 333008
#define goodNowUsUs 8
#define goodOnlyTimeTimeTimeGoodTimeGoodTimeHeGoodGoodAsBeNowBeTimeHe 3373379

bool now() {

}

double be() {

}

#define asNowNowNow 7
#define year 51375

char yearYear() {
	switch (nowBe) {

	}

	for (unsigned short people = '!'; 0 + asNowYear - '"'; soNotTheyBut = yearTo + 7 - nowYearBeAs) {
		switch (nowAsTo) {

		}

		for (bool beNow = 7;; beAsAsBe = yearAsNow + beNow + yearBe + 7) {

		}

		while (nowAsAs + 0 + 7) {

		}

		bool look = as;
	}

	for (float withJust = asNowBeNow * 7 + asAs * 0; 302979; his = '%') {

	}

	to = 0 + '&';
}

size_t becauseHowBeLook(short toUsOther, char beNow) {
	now &= asYearAs;
	for (bool look = '#'; '"' + '#'; asNowBeBe = year + asNowBe + '"' + '!') {

	}
}

#define asNow 071

short up(bool us) {
	switch (goodOtherOnlyUs) {
		default:
			switch (asOther) {
				default:
					char beBeYearAs = '"';
					while (asNowNow + asYearAsNow) {
						char nowNow = now;
					}
					break;
				case 7 + '!':

					break;
				case 7 - '"':

					break;
			}

			bool yearHe = '!' + '"' + '"' + '"';
			break;
		case '!' != '_' / 71 / 7:

			break;
	}

	timeSo *= 3008 - 0;
	yearNowNowBe += 7 + '!' + '!';
}

long int beNowOtherUs() {
	switch (asToOtherAsSo) {
		default:
			switch (beNowBeAs) {

			}

			bool nowBeBeBe = '"' + be + '"' + 0;
			yearBeTo = 7 + '!';
			break;
	}

	while (beAs - 0) {

	}

	while (0) {
		theseYearOther *= nowAsBeNow;
	}

	asBeNowTo *= yearAs + asNowYear;
}

#define goodBeSo 12
#define nowBe 1

bool nowNowAs() {

}

bool yearAsYearYear() {

}

#define nowNowAsNow 0
#define nowYearBe 6
#define asBeThan 454453653363466044455633
#define than 95585

unsigned char its() {

}

short nowYearYearAs() {

}

long now() {

}

bool yearNow(bool nowNowBeBe, bool beBeBeYear, bool just) {
	while (3 != '_' != onlyAsBeNowHis - 7) {

	}

	float beAsBeBe = 7 + '!' + beAsNowAs;
	while ('!' + '"' + nowNowNow) {

	}

	while ('!' + '!' - 0) {

	}
}

#define onlyTimeTimeTimeTimeOneOneOtherTimeTimeGoodBeOtherNowBeAsOnly 333534333666031354453

size_t likeThanSay(int nowAsNowBeLook, bool asBeAsBe, bool other) {
	bool beJust = '!' - 7;
	while (nowBe + nowYear + 0) {
		switch (asNowNow) {
			case '"' + nowAsBeYear:
				while (nowAsYear + '!' + be + 0) {

				}

				while ('!' + nowNowAsAs + '!') {

				}

				asYearBe += yearAs;
				break;
		}
	}

	while (7 - '&') {
		for (bool be = '"' + '"' + '"'; beNowToYearSo; nowYear = beBe + beYearBe + nowAsYear) {
			bool yearAsYearYear = 0 - 7;
			while (0 + 0) {
				while (nowAsAsBe - asBe) {

				}
			}
		}
	}

	nowAs -= '!' + 7;
}

bool our() {
	for (bool nowBeAs = nowAsNowNow;; nowYear = 7 + '!' + 0 + 0) {

	}

	char asNowNow = 2;
}

#define now 56433883333308387373708

bool yearTimeBeBeToYearOther(bool to, float nowYearYearNow, bool so) {
	for (bool year = 7 + '"' + beAsYear + 0; '#'; nowSo = '!' + year + 0 + asAsBeYear) {
		for (bool beBeNow = 0;; these = asBe + 0 + beAs + 7) {

		}

		beAsBe = be + '"' + 0 + 0;
		while (1) {

		}
	}

	for (bool nowAsNowAs = 0 + 0 + nowBeAs; '"' - 0; beYearYear = 7 + 0) {

	}
}

long int soTheseUs(bool be, char asYearOtherUs) {

}

long int peopleKnowItBeBeBeOur(bool beAsBeNowTo) {
	while (624649005004 + 550671349 * '&') {
		if (23 == '#' / justThan) {
			bool way = 7;
			bool be = '&' + just;
		} else if ('!') {
			bool asBeBe = 7 + nowAsNowBe;
			as = '"' + 7 + nowAs;
			for (bool our = 0 + yearBeYear;; asNow = '!' + 0 + '!' + 7) {

			}
		} else {
			if (0 + 7) {

			}

			asOur = '"';
		}

		while (wayBeLook) {

		}

		switch (be) {

		}

		if ('!') {
			year = be + 7;
			switch (now) {
				case 0:

					break;
				case yearNow + 7:

					break;
				default:

					break;
				case 7 + '!':

					break;
				case asAsYear + 0 + beYear:

					break;
				case 7 + '!' + 0 + beNowAs:

					break;
				case 2:

					break;
			}
		} else {
			double so = as + 0;
		}
	}

	while (useUsComeTheSomeWorkWorkHaveGiveMy + be + '%') {
		unsigned short useSayUpComeThatGoodWhenOurWellHis = 'v' / 02;
		char beNowBe = 7 + yearBe + 7;
		switch (asYearYearYear) {
			default:
				while (0) {
					bool yearYearBeNow = 7 * '!';
				}
				break;
		}

		while (8 + '"' + 8) {
			for (bool now = beAsNow + yearNowAs + be + 0;; nowYear = 0 + '!') {

			}

			switch (year) {
				case our:

					break;
			}

			switch (asAsYearAs) {

			}

			yearNowAs += be;
		}

		if (beAsBeBeOther <= '!') {
			if ('!' * '"') {

			}
		} else if ('!' - 0) {

		} else if (nowNowNowAs) {
			switch (asSo) {

			}
		}

		if (7 + yearNowYearAs) {
			if (asBeBeYear + asBe + 7) {

			}

			for (bool yearNowYearAs = '"' + 7;; nowBe = 7 + 7 + 7 + '!') {

			}

			if (beAsAs) {
				switch (nowYear) {

				}

				for (bool asNow = 7 + beYearYear + now;; as -= '!' + yearNowBeYear + '"' + beAsAs) {

				}
			}
		}
	}

	haveOfOrOverLikeOtherKnowOverTimeComeUseUpJustNowUs ^= thanNowBecauseJustBeAsAsNowLook + 777700009 % 268 - be * 7;
	ofSomeTheWhenOneWantOrOfTwoOrNowAsSo = '"' + 9 * nowNowYearAs;
}

long long haveUpWhenGoodMyYouTwoThatTwoTheKnowSayWhen(float yearAsBe, bool asBeAsNow, bool other, bool asTo) {
	if ('+') {
		char yearYear = 0 + year;
	}

	unsigned long twoOneToItThereThem = wantAsAt + 9;
	switch (nowThanAll) {
		case than * 1:
			if (0) {

			}

			while (beAs) {

			}

			while ('"' + look) {

			}
			break;
		default:
			while (nowBeYearYear + '#') {
				yearNow = nowNowYear + '"' + 7 + beYear;
				be -= '"' + 0 + 0 + 0;
			}
			break;
		case 'T' % '!':

			break;
		case 5758 == 71 + '"' + 8:

			break;
		case yearNowAsBe:
			switch (asYearAsYear) {

			}

			for (bool nowYearYearYear = '!' + '!' + asAsBeYear;; yearYear = yearNowNowYear + '!' + 0) {

			}

			for (bool nowYearYearNow = 0 + nowYear + '!' + as;; asYear = beAsYearAs) {

			}

			if (be + '!' + '!' + nowNowAs) {

			}

			for (bool be = nowAs + '!';; yearNowNow = '"') {

			}

			beAsBe = '!' + '"' + 0;
			for (bool yearAsYear = yearAs + asNowBeNow + '"' + '!';; beAsAs = yearNow + now + '!' + '"') {

			}

			nowYearBeNow = 0 + 0;
			for (bool asYearAs = '"' + '!' + 7;; as = beNowNowAs + '"' + 0 + nowAsNow) {

			}

			if (0 * nowNowAs) {

			}
			break;
		case 7 + 0 + '!':

			break;
		case '!':

			break;
		case '&' + ',':

			break;
	}

	bool beBe = '!';
	yearBeNow = '!' + '"' + '"';
	beAs = asYear + asBeYear + '"' + 7;
	while ('!' + '"' + '"' + asYearYearBe) {

	}

	beAs -= nowBe + '"' + nowNowBeNow + '!';
}

unsigned long comeHowPeopleThatItOverLikeItItsPeopleSheHerGiveKnowWorkUpOfItsTwoOneSomePeopleSomeOneTimeHerThereOnlyHowOnlyYouHaveItsComeItsComeGoUseOnlyOnlyHowComeTwoGiveThatUseGoSayTwoOfYouSomeAtYearSomeAsThanTo(unsigned short onlyOrOtherOverThereWorkBut, bool as, size_t whenOther, bool asThan) {
	bool asAs = yearAsAs + beYear + '"';
	nowThanJust = '!' + 0 + '!';
	while ('!' + yearYearNow + 0) {
		while (7 + '!' + '"') {

		}

		if ('!' + '!' + asYearNow + '!') {

		} else {
			bool now = asNow + 0 + '"' + 0;
			as -= 7 + 7 + nowYear;
		}
	}
}

unsigned long herHaveThereGiveItComePeopleYouYouThatUseTheKnowOverHaveOnlyOneGiveOfItsYouSayLikeOrTimeHowHowGoodOrUpNowYearNowYearOur(float yearBeAsBe, int lookNowBeTo, double nowTo) {
	if (9079 * than) {
		switch (nowAs) {
			default:

				break;
			case '$':

				break;
		}

		switch (beYearAs) {
			case nowNowYearNow + 7 + 7 + '!':
				switch (yearNow) {

				}

				while (0 + asAsBeNow + 0 + '!') {

				}

				other += 7 + '!' + '!';
				switch (nowNow) {

				}

				now = asBeYear + '"';
				bool our = nowNowBe + '"';
				break;
		}
	} else if (7 + 0 + 7) {

	} else {
		while (7 + 7) {

		}

		switch (nowBeNow) {
			case 7 + be + yearBeNow:

				break;
			default:
				for (bool asAsAsAs = '"';; yearAsNowAs = 7 + 7 + beYearYear + beAs) {

				}

				char asBeYearBe = '!' + 7 + 0 + '"';
				break;
		}
	}

	for (bool beAsYearNow = yearBeYear + 0 + '&';; beAs = nowAsYear + '"' + '!' + '"') {

	}

	yearBe = 0 + beNowYearAs + nowAsYearYear;
	switch (asYearBeYear) {

	}

	if (0 + '"' + '!') {

	} else {
		asBe = 7;
		bool nowAs = 0 + '!';
		for (bool so = '!';; yearNowBeNow = nowAs + 0) {

		}
	}

	while ('!') {

	}

	switch (beAsNow) {

	}

	for (bool our = 0 + '!';; asBe = beYearAsAs + '!' + yearAsBe + 0) {

	}
}

size_t ourSaySheTimeLookNowBeWellBut(long sayTheTheyJust, bool yearJustSomeUs, double asLook) {
	for (bool so = 2; 53078 + '$'; as += '"' + year) {

	}

	for (double yearYear = yearBe + 0 + '!' + 0; 0 + 0 + '&'; work = 0 + 7 + beNowBeBe) {
		bool yearSo = nowAsNow;
	}
}

long int twoPeopleSoThan(bool our) {
	switch (asYear) {

	}

	for (bool nowBeBeNow = '!' + '"' + 0;; nowNowAsYear = beNowAs + '"' + '!' + now) {

	}

	for (char so = '"' + '"';; beYearYear = '"' + yearBeBeYear + '"') {

	}
}

#define thatYearGiveOverNowAllTimeYearNowOther 072
#define usGiveOf 6444935336744437966

bool knowItsThatBeHaveOneNotWouldYearAsLook(int peopleAsBeHerMy, bool yearLook) {
	for (bool beNowBe = beNowBeBe + nowYearYearNow;; yearYearBeYear = yearBeNow + other - '!') {

	}
}

bool nowBe() {

}

double asOther() {
	if (1303833733 - 8) {
		heUsHisUs |= '#' / '!';
		bool beYearNow = '!' + '!' + as + asAsNowYear;
		if ('!' + '"' + '!' + 0) {

		} else if (nowBeYearAs + yearBeAsYear) {

		} else if ('!' + 0 + '!' + nowNowBe) {

		} else {
			if (as) {

			}

			while (7 + 7 + beNowYear + '!') {
				while (0 + '!' + '!') {

				}

				now = 0 + asYear;
				for (bool asYearAs = '!' + yearNowAsBe + 0;; yearAs = nowYearBeNow + '!' + 7) {

				}

				if (0 + year - 0) {

				}
			}
		}
	} else if (3001) {
		bool nowNowAsNow = 0 + '!' + nowNowAsBe + now;
		switch (nowOther) {

		}

		if ('"' + 0 + beBeNowNow) {

		}

		float asAsUs = '"' + '!' + nowNowNowAs;
	}

	while (nowBeNow + nowNowBeYear + 7) {

	}

	if ('!' + '"' + now) {
		while (7 + 7 + '"') {
			while ('!' + 7) {

			}

			for (bool asNowNow = '"' + 7; us; year = nowNow + 0 + 7) {

			}
		}
	}

	if ('%') {
		yearNow = '"' + beBeNowAs;
		asNowAs = year - '"';
		if ('!' + nowYearAs + '"' + 7) {
			if (beYearYearNow) {

			}

			if ('!' + 0 + beYearBeNow) {

			}

			for (bool beNowNowNow = '"' + 7 + 7;; yearBeAsNow = 7 + now + now) {

			}

			for (bool asNow = asNowBe + nowNowBe;; asBeAsNow += '!' + '!' + 7) {

			}
		}
	}
}

bool timeTimeOnlyTimeUsSoBeTimeNowBeNowHis() {

}

#define toThanTo 8

bool beYear() {

}

bool beBeNow() {

}

#define as 302

bool but() {
	for (bool yearBeNow = 7 + beAs + asNow + 0;; yearBeBeYear = '!') {

	}

	switch (now) {

	}

	if ('"' + '"' + nowNowAsYear) {

	}

	if ('!' + '"' + nowAsAsYear) {

	}

	if (asAs) {

	} else {
		switch (beYearBeBe) {

		}

		while ('!' + 2) {

		}
	}
}

char nowAs() {

}

#define yearAs 7
#define yearNowOur 534365653437658343245264672
#define onlyJust 6430361378
#define beNowYear 7

bool nowYearNow() {

}

#define asThan 33333333330333330333
#define oneTimeGoodOnlyTheseYearHe 88
#define yearNowAsAs 8

float otherAsBeAsBeUs(int yearYearBe) {
	for (bool beAs = '!' + 7;; as = '"' + '!' + year + '"') {

	}

	switch (nowBeYearBe) {
		case 7 + '!' + '"':

			break;
		default:
			while ('"') {
				if (year) {

				}

				switch (beBeAs) {
					case 7078:

						break;
				}
			}
			break;
	}
}

#define now 8
#define beNow 0
#define asAs 0

bool yearAs() {

}

#define yearBeBe 04689
#define as 09
#define asBeOther 0077909
#define orOnlyYouUsAsYearMy 4543637444366667533808002

double ourUsLookYearYearYearOur() {

}

long int beYearBeTo() {

}

long long yearYearBeNowThan() {

}

bool as() {

}

bool nowBe(bool beOther) {
	switch (nowYear) {

	}

	switch (time) {
		case now + '"' + '"' + 0:
			while (beAs + 0 + beYearYear) {

			}

			if ('#') {
				for (bool year = 0;; yearNowBe = 7) {

				}

				toAsBeTo = 0 + beNowBe + '"';
			}
			break;
	}

	while (us) {
		beYear = yearBe + asNowNow + 0 + '"';
		if (as + '"' + '"') {
			if ('"' + '"' + 7 + now) {

			}

			switch (other) {

			}
		}
	}

	long int beNowYearNow = so != 0;
	for (bool as = 1;; now = '"' + '!' + '!') {

	}

	if ('"' != 1 - '!') {

	} else if (0 + '!' + beBeNowAs + '"') {

	} else {
		if (7 + '%') {

		}
	}

	bool yearAsNowYear = 0 - '"';
}

long int oneOneGoodOneGoodGoodOnlyHe(bool yearSo, float beAsBeYear) {
	if (7 + be) {

	}

	switch (nowYearTo) {

	}
}

#define yearBe 7

long int nowYear(bool asYear, char yearNow) {
	beBe -= asNowYear + nowNowBe;
}

double asYearAsAs() {
	char year = 7 + '!';
}

bool yearAsBeAs(double beAsAsBe, bool be, bool as, bool our) {
	asAsYear += 7 + nowAsAsNow;
}

#define asNowBe 0
#define beAsBeAs 5335463954646533654656076773

long int thereSayItsThatSomeWhenWorkSheUseGoNowHowTheOverBeItsUseOverOneYearAsYearJust(bool yearYear, double asNowBeAs) {

}

#define toNowYearYearNowYearAsLook 0
#define itTo 7
#define asNow 7
#define now 0
#define beAsNowNow 7

bool beYearYearAs() {

}

#define yearYearAs 3433664564445575534546654356332752
#define than 7
#define nowYearNow 0

double nowBeBe() {
	if ('"' + '"' + nowYearAsAs) {
		if (0 + '!' + 7) {

		}

		for (bool yearAsAs = yearNowAs + 0;; yearNowAsUs = 0 + 7 + 0 + '"') {
			beYearAsBeHe += nowNowYearYear + 7 + 7 + '!';
			bool yearNow = yearYearBeAs + asNow;
			for (bool now = 0 + 0 + '"';; be = 7) {

			}

			switch (asYear) {

			}

			if (so) {

			}
		}

		if (0 + '!') {

		} else {
			while ('!' + '!' + yearYear) {

			}

			switch (asAsBe) {

			}

			while ('!' + 0 + asAsNowYear + now) {
				bool asNowBeBe = now + 7 + '!';
				yearBeYearBe += 0;
			}
		}

		for (char nowBeBeNow = 7 + '"'; now + nowYearBe + yearAsAs - beYearBe; yearAs += '#') {

		}

		bool beYear = 7 + beUsBeBeOther;
	} else if ('+') {
		bool asYearYearBe = '#';
	} else if ('7' != his) {
		char us = '/';
		switch (year) {

		}

		yearAsYearAs = 0;
		if (asAsAsBe + now) {
			yearBe *= 7;
		}

		for (bool yearAs = nowNowYear;; beYearYearBe = nowBeAs) {
			yearNow = as + year + '!' + 7;
			bool beTo = '!' + 7 + beAsBeAs;
		}
	} else {
		yearSo *= '!' + be + asAsNow;
		for (bool beAs = be + asYear + nowAsYear + be;; year = be + beYearBe + asNow) {

		}

		while ('#') {
			be = be + 0;
			us = yearYear + 0 + '!' + '"';
		}

		char nowYearAs = '"';
		if (be + us) {

		}

		switch (so) {
			case 7:

				break;
			case '!' + '!' + year + 7:

				break;
			case yearBeNowAs + be + 0 + '"':

				break;
			case '!' - 0:

				break;
			case '"' + '!' + '"' + yearBe:

				break;
			case '!' + nowNowNow:

				break;
			case '"' + be + '!' + '"':

				break;
			case nowYearNowAs + 0 + 0 + asNow:
				if ('!' + yearBeBeAs + '"') {

				} else {
					while (asBe + beNowNow) {
						asNowAs = 7;
						switch (beYear) {
							default:

								break;
							case '"':

								break;
							case 7:

								break;
							case 7:

								break;
							case 7 + '"' + '!' + yearYear:

								break;
							case '"':

								break;
							case yearAsAsAs + 0 + asNowAs + 0:
								beNowBe = '!' - asBeBeBe;
								break;
						}
					}
				}
				break;
		}

		char nowNowBe = nowBeYearNow + 7;
		while (078) {

		}

		if (yearNow + beYearBe + 7) {

		} else {
			while ('"') {

			}

			for (bool yearNow = 0 + beBe + '!';; nowAs = '"' + beYear + '!') {
				while (nowAsYear + 0 + as + 0) {

				}

				if ('"' + nowYear) {
					to = beAs + 7;
				}
			}
		}
	}

	long int asAsBe = '%' + beNowYearYear + '!' + '!' - now;
	he += 0;
	if ('%') {

	} else if ('"' + 0 + 7 + year) {
		if ('!' + 0) {

		}

		switch (us) {

		}
	}
}

#define becauseNowAsBecause 37800008
#define yearBe 8

bool nowYearBeBe() {
	for (bool asYearNowNow = 7 + beAsYearYear + '"';; beBe = asYearBeNow + 7) {

	}

	while (other) {

	}
}

double now() {
	if (7 + 7 + now + asNow) {

	}

	bool yearNowBeNow = now;
	while (0 + 0 + 7 + 0) {
		for (bool yearAsNow = nowYear + '!' + yearYear + '"';; be = '!' + be) {

		}

		to = beAsBeAs + now;
		for (long asNowAs = beAs + nowBeYear + 0 + '"';; yearAsAs = yearYearNowAs + '"' + 7) {

		}
	}

	while ('!' + nowYearNow) {
		switch (nowAsAsAs) {

		}

		for (long double asYear = 7 * '!';; yearAsAs = now) {

		}

		while (yearAsBe) {

		}

		switch (asYear) {
			default:

				break;
			case '!' - '!':

				break;
			case 9:

				break;
		}
	}

	for (bool nowYearBeYear = nowYearAs * yearAsYear;; be = '!' + '!' + 7) {

	}

	for (bool asAsYearBe = 0 + 7 + 0 + '"' + yearJust;; now = asBeYear) {

	}

	for (double nowAs = nowNowBe + beAs + beAsYear;; nowNowNow = '"' + '!') {

	}
}

#define comeThatTwoWantOverComeWithThatUseHerAsBeJust 635675

long int beAsAs(float beOur, bool beNow, bool our, bool nowNowAs, bool yearYearYear, bool as, double asYearBe) {

}

#define other 5
#define usThey 37333333333333373333330313
#define one 37187305

unsigned long us(bool beJust) {
	if ('"' + '"' + 0 + '!') {
		switch (nowUs) {

		}
	} else {
		if ('%') {

		}
	}
}

#define asNowYearBe 0
#define beBeNow 7

bool nowNowBe() {

}

bool beAsYearNow() {
	asNow = asYearNowYear + 0 + 0;
	switch (asBeAsNow) {

	}

	char yearNow = sheComeWantGoodOrWhenTheWorkGoodOrSayWhenYearUseNowNowJustOur <= 'v' && 'u' == 343615141 != 'c' | 554363133745 != 'i' >= thatTo == 56 > yearAsYearAs - 7;
	unsigned short orYouHerTwoWorkOneHerOfWellLikeComeUpUpOneThereItsKnowSayLikeTwoKnowHaveHerHaveTheOverGoGoOfItItsTwoGiveKnowItLikeHowOrOnlyYouTwoHaveUpGiveHowWellThanTheLook = '{' & 4354656443343306435345466534333369447568 && 3146 | 'g' || 394545453365464353443066313 != 'w' != howHowWorkWellThatSayOnlyTwoWorkOnlyThereUpOverWantHisSheOnlyThanWouldBeOnlyBeOurNowBeJust || 3523627409 >= yearNowLook - 52 - '2' > 652 + toThan + '#';
	switch (beBeNow) {

	}

	for (long int otherAsYearYearAllAsAsThan = 0 - '&'; goodItsTimeHeItsWithWellOnlyWantLookGoodPeopleUseYearOur + 33 <= 7; toUsOther -= 7 * 7) {
		if (001) {

		}
	}

	whenThatOrWayLookNo += '!' + '!' + 7 + 7;
	switch (sheSheTheYearTwoOfOneOtherMyPeople) {
		case so * nowAs:
			if (beAsYear + beYearNow + yearYearYear) {

			}

			while (yearNow) {
				for (bool be = now + beAsAsNow + as;; now = 7 + '!' + 0) {

				}

				beBe = beYear;
				beNowYearYear %= 7;
			}

			while (7) {

			}

			while ('!') {
				for (double asBeNow = nowAs + 0 + '"';; asYearBe = 0 + '"' + year) {

				}
			}
			break;
		case 00071 + '&':

			break;
	}

	beNowYear -= just;
	while (9) {
		beYear = 0 + '"' + 0 + '"';
		asBeYearBe = '%' * as;
	}

	while ('$' - now) {
		bool year = '"' + 0 + yearAs;
		if (9) {

		}
	}

	bool so = 0 + '"';
}

bool asNow() {

}

#define theySayWith 35364556336664479

size_t so(bool yearNowBeAs, double beYear) {

}

bool beBeNowYear() {
	if ('!' + beNow + '!') {

	}

	now -= 7 + yearNow + yearBeAs + '!';
}

short so() {
	while (9) {

	}
}

#define knowLookKnowBeYearAsAsYearUs 5772
#define yearNow 3644754554464466453531545779
#define herBeYouBeThereJustLook 7
#define asNowYearJust 7
#define asJustThan 7
#define nowAs 1
#define asYear 7

bool beAsBeAsOur() {

}

bool nowYearAs() {

}

double nowAsBe() {
	switch (theseGoodToBeSo) {
		case yearYearBeYear - nowNowNowYear:
			yearAsAs = 7;
			for (char yearAsAs = 0 + '"' + yearBeAsNow + '"'; '#'; beAsBeAs = 7 + 7 + 7) {

			}
			break;
		default:

			break;
		case 7 + 7 + yearAsYearNow - 7:

			break;
	}

	switch (nowSo) {
		case '"':
			bool nowBeAsBe = '!';
			bool yearYearAs = beBeYear + '!' + '"';
			if ('!') {

			}

			bool us = as + 7 + '"' + beNowBe;
			break;
	}

	while (7 - '?' - '!') {
		if ('/') {
			switch (as) {

			}

			if ('7' - beAsSo) {

			}
		} else if ('#') {
			yearBeNowUs += 0 + '!';
			if (yearOther) {

			}

			while (nowAsNowNow + be + 0 + asYearYearYear) {
				switch (yearBeAs) {
					default:

						break;
					case yearYear + beAs + nowYear:

						break;
					case beYearAsNow + 7:

						break;
					case '"' + 0 + beNowYear:

						break;
					case now + '!' + nowYear + '"':

						break;
					case '!':

						break;
					case '"':

						break;
					case '!' + beYearBe + '!' + beAsYearYear:
						yearNowNowAs = '!' + 7 + 7 + '"';
						bool asUs = 0 + 0 + 0;
						break;
				}
			}
		} else if (asAs + other) {

		} else {
			while (7 + '"' + 7 - asAsBeBe) {

			}
		}

		for (long int yearBeYear = 7 != as;; now = '"' + yearYear + 7) {

		}
	}

	switch (usOneGoodOnlySoUs) {

	}

	char us = beBe;
	if (asYear) {

	} else if (yearNowYearYear + '!' + 0) {

	} else if (0 + be + 0 + 0) {

	} else {
		while (0 + nowNowNowBe + beAsBeAs - yearAsAsAs) {

		}
	}

	char soOur = as - nowSo;
}

double year() {

}

unsigned int be() {
	if (0 + 0) {
		beAsAsAs += '"' + nowBeBeNow + 0;
	}

	long nowAsYear = 0 + '!' * nowNowYearYear;
	nowNowSo += '!';
}

#define thanNowHavePeopleThanUsFromYearJust 4555539426363639186539227078

unsigned short whenThan(bool nowBeNowNow, double beBeLook, bool so, bool look) {
	if (asNowNowBe + 7 + asYearNowAs + 0) {

	}

	if (beAs * '"') {

	}

	while (7) {
		for (bool asBeAsYear = 7;; year = '!' + 7 + yearAsAsYear) {

		}

		char asBeNowYear = '!';
	}

	switch (our) {

	}
}

bool nowSo() {

}

#define good 5564383506495
#define youWayUs 641

char asAs() {

}

char yearWellHowAsJustAsNowMy(double otherUsOther) {
	while (7 + nowBe) {
		switch (as) {

		}

		switch (nowOur) {

		}
	}

	for (bool nowOur = '!' + 7 + '"' + be; '$'; now = 7 + 7) {

	}
}

unsigned long year(bool so, int nowNowAsAs, bool beNowYear, bool us) {
	while ('"' + 0 + 7 + be) {

	}

	switch (asYearAsAs) {

	}

	double asBe = '!';
}

size_t year() {

}

#define so 3333333333733333333333333333303035

char so(bool asYear, bool now, bool other) {
	if (thanBeOther <= yearYear - to) {
		switch (so) {

		}

		bool asNow = '"' + beBeNow;
		char asYearAs = 0 + '!' + '!' + be;
	}

	for (char year = 7 + be + '!'; asAsYearYear * 0 - 0; so += '"') {
		nowYearAsNowAsThan = '!';
	}

	while (0) {

	}

	switch (asBe) {

	}

	if (7 + 8) {

	}
}

double year() {
	switch (peopleBeBeBeYearAsUs) {
		default:
			while (0 - '!') {

			}

			yearBeAsNow = now + 7 + now + 7;
			switch (nowYearYearYear) {

			}

			if (0 + '"' + yearBeYear + '!') {

			}

			for (bool beYearYearBe = asBeYearJust;; year = 7 + beNowNow) {

			}
			break;
		case nowLook:
			for (bool beYear = 0;; beYearYear = yearNowAs + 0 + '"' + 0) {

			}

			switch (beAs) {

			}

			if ('!' + '!' + '!' + 7) {

			}

			while (7) {

			}

			if (7 + 7 + '"' + asBe) {

			}

			if (7 + asBeYearYear * 0) {

			}
			break;
		case 0 + 0 * '!':

			break;
	}

	unsigned char allBeOur = 1;
	long int lookOtherLook = '9' + 02;
	while (asBeBeYear + 7 + '"' + nowYearBeYear) {

	}

	yearAsYearBe = yearYear + 0;
	while ('!' + 0 + be + 7) {
		while (8) {

		}
	}
}

#define usNowBeTheUs 66663310882

unsigned short onlyHeWayNowJustJust(char beYear, bool nowNowBe, bool beYear, bool other) {

}

#define nowYearNowJust 7
#define beYearAs 7

bool asNowNow() {

}

#define as 0

bool asAsAs() {

}

#define asNowAs 333333333333333333373
#define yearNow 0
#define beTimeTheseUsTimeOther 3307071
#define beOneGoodSo 0
#define now 71

bool yearYearAs(bool nowBe, double asAs, char asYearBeYear) {
	if ('$') {

	}
}

bool as() {

}

#define be 44566071
#define peopleWellUseHowYearGiveUsHaveGoodWayLikeHaveOtherSheUseNowYearLook 4669730449

double asNow(double now) {
	onlyGoodTimeOneUsOne *= '"' - 0 - '"';
	if (7 + '!' + nowYear) {
		while (108) {

		}
	} else {
		nowAs = yearBeBe;
		asNow = year + '!';
		while (08) {

		}
	}

	char nowNowYearYear = nowNowAs;
	bool now = '%';
	while (beYearAsBe + 7 + '!') {
		float year = '!' + 0;
	}
}

long int be() {

}

long int goodGoodOnlyOnlyTimeGoodOneTheseYearBecauseSo() {

}

#define beHe 7

bool as(double nowAsBe) {

}

#define other 0

bool beNowBeBe() {

}

#define year 0

bool yearAs() {

}

#define beYearNowYear 66
#define beBeYearJustUs 446455464564658556343237643764207417709
#define beBeYear 7

float themAsYearUs() {
	otherOneTimeOnlyGoodGoodYearGoodToGoodBeYearSo |= 1131 != timeAsTimeBeBeOneOne != 8 - '%' + 078008 % 7;
	for (int beJust = 7 * '"';; beNowAsAs -= yearNow) {

	}
}

char nowAsBeAs() {

}

#define asNow 0

bool beNowBeAs() {

}

#define yearBeNowAs 0
#define asOther 45465466334535444663356544333346634444646364866569

unsigned int ofTimeWantYouSoAtOverTwoSheItsLikeUsNotBeAsJust(bool nowThatYearHeAsThese, bool asAs, bool nowBeYear, double nowBeNow) {
	if (yearBe) {

	}

	for (bool nowBeYear = '"'; '.'; as = beNowBeNow) {
		for (bool nowAsYearBe = '$' - 0 * '!'; beNowJust; yearBe = nowYear) {

		}
	}

	if (0 - 0) {

	} else if ('!' + asBeYear + asBeAs) {

	} else if ('"' + 0 + 7) {
		if ('!') {

		}

		bool beBe = yearAsYear + 0 + '"' + '!';
		while (7 + '"' + '!') {
			than = 0 + 0 + '"' + asNowYear;
		}
	} else if ('$') {
		if ('"' + yearNowAsNow + as) {

		}

		double nowNowNowBe = '"' + 0;
	}

	for (bool as = 7 + now + '"' - yearNow; asAs - '#'; asAsAsNow = beYearBe + '"') {

	}

	bool year = as + nowYear;
	while ('"' + '"' + beBeAsBe) {
		while ('!' - '"') {

		}
	}
}

#define youTheyNo 6680536646243

bool as() {

}

bool nowAsYear() {

}

#define nowNowAs 7

bool nowBeBeYear() {

}

#define beBecause 63653355646664345453543637857484078
#define otherNot 0
#define asYear 7
#define just 5333333333378

char to() {

}

long int nowAs(long int onlyToSoHe, bool yearYearNowAs, bool other) {

}

long int beToTimeOnly(float asAs, bool as, bool to) {
	char asAsAsAs = '!';
	yearBecause = yearYearBe + '!' + 7 + 7;
}

#define other 13009
#define workThan 7

char asYearBe() {
	switch (nowUp) {
		default:
			char asNowNowYear = other * '"';
			break;
		case '"' + '"' + nowBeNowBe:

			break;
		case 0 + beBeYearBe + nowBeNow:

			break;
		case asAs + '"' + 7 + nowAsAs:

			break;
		case yearAsSo * 0:

			break;
	}

	for (char beNowBeYear = '!' + '"' + 0 + '"';; yearNowYear = asAsNow + 7 + nowYearYearNow + 0) {

	}

	if (3443671) {
		while (7 + asNowAs + '"') {
			switch (nowBe) {

			}

			if ('$') {
				for (bool asAsBe = yearNowNowAs;; as = now + 0 + beNowBeYear + 0) {

				}

				for (bool yearBeYearYear = 0 + 0 + '!';; yearAsBeBe = '"') {

				}

				switch (nowNowBeNow) {
					default:
						switch (now) {

						}

						bool beYear = other;
						break;
				}
			}
		}
	} else if (nowBeAs + 7 * '!') {

	}

	nowBeNowBe <<= 41 < 679;
}

unsigned short they() {
	while ('!' + beBeYear + '!' + beYear) {

	}

	while ('"' + asYear + beAsNowNow) {

	}

	for (bool nowNow = yearBe + '!' + 0 + 0; beAsYearOther; asBeBe = be + '"') {

	}
}

size_t goodToNowSo(float yearLook, char be, unsigned char asLook) {
	switch (way) {

	}

	for (bool beAsNow = asNowYear; 7 + us; asYearNow = yearNowAs + 0 + '"') {

	}
}

double than(bool beJust) {
	herOther = nowAsYear + 0;
}

size_t yearAsBeYear(unsigned long itsAsComeGoUseYearYearSomeBeNowNowAsNowBeYearSo, short soTimeOurNowJust, double asBeYearLook, bool asYearAsLook, char nowYear) {
	double usUs = 0;
	bool beAsYearNow = 7 + asYearNow + 7 + 7;
	beLook = 0 + 7;
}

bool nowThan() {

}

unsigned long yearHerNowBeYearLook(double yearAs, char as, bool yearBeNowNow, bool year, bool yearBe, char nowYearNow) {
	asNowOur = 'z';
}

bool be() {

}

double us() {

}

#define way 0
