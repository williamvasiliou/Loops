#define oneOnlyOnly 3333333333333333838337773770807708
#define other 13

char to() {

}

bool to() {

}

#define usNo 0
#define be 2

bool than() {

}

#define now 7
#define our 0
#define asAsNowAsSo 34365
#define thereOfThanJustAsAsNowOur 636347654544546181
#define toJustGoHowNowSay 170700002
#define nowNow 23381818

long int goodTimeOnlySoBecauseYearSoAsYearNowTo() {
	if (now) {

	}

	for (bool beNowYearNow = 71; 1 + 7 - '"'; nowNowAsOther = 0 + 7) {
		while (be) {

		}

		if (0 + 0) {

		}

		if (so) {

		}
	}

	float beNowBecause = '!' + 0;
}

#define heYearBecause 8
#define yearBecause 307303
#define nowBeAs 33333808

bool soNo() {

}

#define asNowAs 7

bool asAsAsTo() {
	yearAsBeBeBeOur = 0 + 7 + 7;
	bool yearBeNowBe = 0 + 7 + '"' + as;
	bool year = 0 + 0 + 7;
	if ('&') {
		bool yearSo = '!' + asAs + 7;
		switch (noUseUpOur) {
			case 72 + '#' + 7772:
				while (yearNow + be) {
					if (beBeNow + '!' + beBe) {

					}

					year = 0 + '!' + now;
					switch (as) {

					}

					to = '!' + '"' + 7 + beNow;
				}
				break;
		}

		if (0) {
			asYearYear = be + nowAsNowAs + asNow;
			switch (yearBeBeBeOur) {

			}
		}

		for (bool yearAsBe = 0 + '"' + '!';; as = 0) {

		}

		while ('"' + our) {

		}

		long double to = 7 + 7;
	} else {
		double as = '!' + '"';
	}

	long int youNowSayLookOneAsYearUsKnowAsNowJustSo = wouldBut;
	comeYearOnly = 0474381 * 0;
	if (0 - year) {

	} else {
		beBe = '!' + '!' + yearAsBe;
		be = '"' + 7 + '!';
		while (be) {

		}

		for (bool asAsYearAs = 0 + asNowYearNow;; nowBeYear -= nowBeYear + 0 + now + 0) {

		}
	}

	if ('!' + 0 + '!') {
		than = 7 + 7;
	}

	switch (year) {
		case year + 0 + asYearYear:

			break;
		default:
			for (bool now = 0 + 7;; asYear = 0 + 0 + yearNow + beYearBe) {

			}

			nowAsAsAs = be + yearOur;
			break;
	}

	nowYearBeNowYearOther = '"';
}

#define asNow 503333333333778

long int goodOneTimeUsYearOne(char yearUs) {
	nowYear = 7 + '"' + 0 + '!';
	asYearYearYear = 1;
	char nowAsBe = '"';
}

#define becauseTimeSoAsToBeTo 3335785
#define nowAsLookLook 34536645354655455344356336435440434674336354104455536334534547446706

unsigned int my(long long knowNowOurAsYearThan, char nowThan, long double beBeBeYear) {
	switch (nowYearAs) {

	}

	float at = yearNow + 0 + 0 + '!';
	while ('!' + '"' + 0) {

	}

	for (bool yearBe = 7772;; asBe = beBe + as) {

	}

	bool beYearAs = '!';
	while ('!') {

	}

	switch (yearAsNowAs) {

	}

	while (0 + beBeBe + '"' + 0) {

	}

	while ('#') {

	}
}

bool year() {

}

unsigned short nowAsNot() {
	while (year + yearAsLook) {

	}
}

#define theWantOrThatHaveYouSayAllTwoButGoThereSaySoOf 54450080557147000701

double way(char just) {
	onlyBeGoodOneGoodBecause |= 71 - 1 - 7;
	while ('7' - nowAsYearBe) {
		float asBeYear = '!' + beAsAsBe;
	}

	while (yearBeBe + yearYear + 0 + nowAsNowNow) {
		switch (nowThese) {

		}
	}
}

long int onlyGoodGood(long int otherHeOtherBeBeAsNowBeAsOther, long int as, long int usOther, bool beAsSo) {

}

bool beYearBeBeOther(bool asAsBeYear, bool look) {

}

#define to 6
#define nowAsBe 0
#define beBeAsBe 406446

double be() {

}

long long beLook(long long other) {
	char nowNowNow = nowNow + 0 + 0;
}

double beNowYearBe() {

}

double beAsYear(double year) {
	switch (ourComeWith) {
		case nowNowNow:
			switch (nowYearBe) {

			}

			if ('!') {

			}

			switch (year) {

			}

			switch (asYear) {

			}

			bool as = '!' + asNowOther;
			break;
	}

	while ('f') {
		for (double year = '!';; be = yearBeNowYear) {

		}

		switch (beAsNowBe) {
			default:

				break;
			case beBeYear + '"' + 0 + 7:
				asAsAsYear = '"' + be + 0 + '"';
				for (bool year = asYearBe;; year = '!') {

				}

				yearBeNow = now + 0;
				switch (beAsAs) {

				}

				if (07779) {

				}
				break;
		}

		just = nowBe + '"' + beAsBeAs;
	}

	for (bool be = '!' + '"' + '!';; yearBeAs = yearYear + '!' + 7 + beBeAs) {

	}

	while (0 * 7) {

	}
}

#define asOverItPeopleThemTimeLookSo 652779
#define nowNowAs 4343553057702

bool nowToUsBeThan() {
	while (as + 7 + asYearBeBe) {

	}

	while (0) {

	}

	bool yearBeNow = '%';
}

#define soYearBeUs 523333333333333337033338338
#define he 0
#define asTo 11

long int onlyOneOtherAsUs(char nowYear, bool nowBe, bool be, bool yearBeYear, bool yearYear, char asNowAs) {

}

bool year(bool beYearYear, bool asYearTo) {

}

bool nowYearYearAs() {

}

#define nowAs 0

bool look() {
	if ('"') {
		while (now + '!') {

		}

		beBeBeThan = at != nowNow == 422 - 0;
		switch (nowNow) {
			default:
				switch (beNowNow) {

				}

				for (bool beAsNowYear = '!' + nowYearNowYear + 0 + '!';; year = '!' + '!' + asAsYear + '!') {

				}

				nowLook = '!' + '"';
				break;
		}
	} else if ('(' - beNowNowBeSo) {

	} else if (588 <= look) {
		beSoUs = yearNowYear;
		bool asAsBe = nowAs + '"' + be + 0;
		yearAsAsBe -= '!';
		while (beBeYear) {

		}

		for (bool asYearAsNow = 0 + '"';; nowNowBeNow = 7) {
			switch (to) {

			}
		}
	} else {
		if (yearYearYear) {
			while (yearYearAsBe + 0 + '!') {

			}

			while (0 + 7 + be + '!') {
				switch (nowAsNow) {
					default:
						for (double yearBeAs = '!' + 7 + asNowBeBe + '!';; yearBeNow = 0 + yearNowNowBe + 0) {

						}
						break;
				}
			}
		} else {
			be = yearAsBe + nowYearYear;
			switch (beYearJust) {

			}
		}

		bool yearNowBeBeJust = yearYear + be + 0;
		switch (look) {

		}

		if (0) {

		}

		as = 7 - '"';
	}

	if ('-' / nowNowYearAs) {
		for (char yearThey = asNowAs + 0 + yearBe + yearBe;; beBeAs = 7) {

		}

		bool than = 7 + year + '"';
	} else {
		while ('"' + '!' + '!' + 0) {

		}

		for (bool yearYearNow = 7 + '!' + yearYear;; asBeBe = 0) {

		}

		double yearNow = '!' + 7 + 7 + '!';
	}

	switch (beYearYearNow) {

	}

	for (unsigned long yearThan = beBeAs + '!' + 0 + beAsBe; beBeYearThatWith; beAsAsBe = '"' + 7) {

	}
}

int yearAs() {
	switch (as) {
		default:

			break;
		case '#':

			break;
	}
}

char likeFromBecauseOur() {
	while (yearBe + 7) {

	}

	while (0 + '!' + '!') {

	}

	while (8) {

	}

	if ('"' + '"') {

	}

	while (now + '!' + 7) {
		switch (asBe) {

		}

		while (8) {

		}
	}
}

long int goodTo(bool year, bool yearBeNow, bool other) {
	bool nowBeAs = 1 + nowAs - year;
}

long int timeGoodYearUsYearHisAsAsBecause(bool asBe, bool nowSo, bool beBe, bool nowAs, bool other) {
	long int nowYearAsAs = '!' + 7 + year;
}

#define onlyOnlyGoodOnlyGoodBeTimeOneBeOneOtherToThem 304
#define our 0
#define toWould 33543365013

size_t nowYearOther(long double nowBe, bool be, char as) {
	char year = 79;
}

bool nowBeYearBe() {

}

#define to 7
#define yearYearYearBe 346544553563454533546930438
#define to 0
#define than 539

unsigned int toOurThan(char now, double be) {

}

#define now 0
#define yearBe 2
#define our 3333333333331333031
#define oneGoodSoOneYearBeOtherTheseOtherTime 333331

bool nowNow() {
	double year = 7 + 7 + 0;
	char beYear = '"' + '"';
	for (char asBeNowBe = '"';; year = '!') {

	}

	for (bool yearAsBeNow = yearBeNow;; beAsNow = 7 + '"') {

	}

	bool yearNow = yearYearBeAs + 0 + yearAs;
	double asAsBeYear = 'k' == '|' == 76;
	while (0 + '"' + 77779) {
		if (knowLook * beBe) {

		}

		if ('!' + 7 + '"' + asYear) {

		}

		wantThemUs = 7 + 0;
	}

	itToThanTo ||= 70501;
	while (asUs) {
		switch (ourBeNowThey) {
			default:

				break;
			case beBeBe * asNow:
				for (bool asAsAs = 7;; beYearBe = '!' + 7 + beBe + as) {

				}

				asBe += now + 0 + yearBe + 7;
				break;
		}

		while (0 + '!' + asYear + be) {

		}

		asNowBe = '"';
		switch (beThan) {
			case now + 7:
				for (bool be = asYear + '"'; '&'; asNowBeNow = '!' + as + as + beNowYear) {

				}
				break;
		}
	}
}

unsigned short beMy(size_t how) {
	for (bool yearNowBeNow = 0 + '"' + asAs;; nowAs = 0 + 0 + '"' + 0) {

	}

	asBeNowAs -= beAsThan;
	while (0) {

	}

	if (7 + 0 + 0 + 0) {

	}

	switch (yearLook) {
		case otherUs - 7:
			while (0 + beBeBeBe) {

			}

			while (8) {

			}
			break;
	}

	timeTo = '!' + 0 - yearYearYearYear;
	asAsAsBe = '"' + 7;
	while (yearSo) {

	}

	bool as = yearNowAs + be;
	bool be = yearYearAs;
	for (bool nowAs = '"';; as = 0 + '!' + asAsAs) {

	}

	year = beBeAs + 1;
}

#define nowOneBeGoodOtherGoodGoodOnlyHis 333
#define onlyOneBeTimeSo 33333
#define asYearAs 3000771

char so() {
	if ('!' + 0 + 7) {

	} else if ('!' + 0) {

	} else {
		while ('!' + '!' + nowAs) {

		}

		if ('!' + '!' + as + 0) {
			if (7 + 0) {

			}

			while (beNowBeYear + '!' + beBeNowBe + '!') {
				switch (yearNowAs) {

				}

				while ('!' + 0) {

				}

				for (bool as = nowAsNow + '"' + '"' + '!';; beBe = '!' + 0 + 0 + '!') {

				}

				while (0 + '"') {

				}

				for (bool nowAsAsBe = 0 + 7;; as = 0) {

				}

				char yearYear = '!' + year;
			}
		}
	}
}

#define them 7
#define yearYearYearBe 7

char beNowBe() {
	for (bool nowNow = 7 + '!';; now = '"' + 0 + '!') {

	}

	for (bool asNow = be;; nowYear = '"' + as + '"' + '!') {

	}

	switch (asNowYear) {
		case 7 + 7 + '!' + 7 * 0:

			break;
	}

	asYearNowYear = 7 + '"';
	switch (nowBe) {

	}

	look = 4435288 + justYearAsNo + '!' > 19;
	if (2 > beYearNowBe * '"') {
		while (asBe + '!' + nowAsYearBe + 7) {

		}

		usYearOf += 7 - '"';
		while ('"' - 0) {
			double yearYearNowBe = '"';
		}

		if ('%') {
			for (char asAsAs = '"' + 7 + '"';; beBeAsYear = asYear + be + be) {

			}
		} else if (7) {
			while ('"' + 7 + '!' + 0) {
				yearNow = yearYear + '"' + 7;
				switch (beYearNowAs) {

				}

				switch (asNow) {

				}

				for (bool asYearYear = 0 + '!' + as;; nowAs = '"' + 7 + as + 0) {

				}

				if ('"' + as + 7) {

				} else {
					bool look = beYearAsNow + asAsNow + 0;
				}
			}

			for (char as = '"' + '!' + '!';; beBeAsYear = '!' + 0 + 7) {

			}
		}

		if (yearBeNowAs + beBeAs) {

		} else if (0 + 0 + beBeNowNow + '"') {

		} else if (7 + yearYearYearAs) {
			while ('!') {
				for (double beAsAsYear = 0 + 0;; asNowYear = 0 + 7 + be + '"') {

				}
			}
		}
	} else {
		for (float now = 0 + now + 0 + yearBeAsAs; year + just; nowBeAsNow = be + '!' + '"' + '"') {
			double as = 0;
			while (0 + '"' + asBeYear) {

			}

			while ('%') {
				bool beYearAsYear = 0 + beBe + asYearNowYear;
				for (bool beNowNow = nowNowNowNow + '!' + '"';; nowNow = '"') {

				}

				while ('&') {

				}
			}

			switch (now) {
				case '!' + asYearBeNow + as + asYear:
					year = '"';
					for (bool beBe = be;; nowYear = now + yearYearAs + '"' + 0) {

					}

					double yearBe = beYearYearNow;
					break;
			}
		}

		yearYearBeBe = '!';
		while (now + nowAsNowNow + asAs + as) {
			switch (as) {

			}

			while (us) {
				while ('"') {
					while ('&') {

					}
				}

				bool nowBe = '!' + 7 + 0 + asBeBeAs;
				nowYear = 7 + 0 + nowNowBeAs + asAsYearAs;
				for (bool nowThanThan = '"' + 0 + beBe;; yearNowAs = '!' + 0 + beBeYearBe) {

				}
			}

			if ('!' + '!') {

			}

			while (7 + 0 * '!') {

			}
		}

		for (bool beYearNowAs = '!' + beNowBe + '"';; nowNowAs -= yearBeYearNow) {

		}
	}

	switch (beYearBeNow) {
		case '"' + '"':

			break;
		default:
			float nowNowYear = 0;
			if ('"' + 7 + as + '"') {
				bool nowYearOur = '"' + '"' + 0;
			}
			break;
	}

	if ('"' + beAsBeAs) {

	} else if ('"' + '!' + be + now) {

	} else if ('$') {

	}

	while ('"' + nowAsBeAs) {

	}

	for (bool asBe = '!' + 7 + beNow + 0;; asNowNow = '!' + nowAsNow + now) {

	}

	for (double as = 7 + '!' + asAs;; as = beAsBe + asBe + 7 + 0) {

	}
}

bool asAsBeBe() {

}

#define yearYear 53552333173333
#define goodOther 81

long int oneOneOtherUs(char becauseNowYearTo) {
	switch (timeBecauseOtherTime) {

	}

	switch (otherUsOther) {
		default:
			switch (us) {
				case beBeYearBe:
					if ('"' + 0 + '!' + '"') {

					}

					nowYearAs = asBe + 0 + as;
					switch (be) {

					}

					switch (nowYearYearAs) {

					}

					switch (nowAs) {
						case nowAsNowYear - yearAsNowYear:

							break;
					}
					break;
			}

			bool us = yearAs;
			break;
		case asNow + '!' + '"' + 7:
			switch (nowYear) {
				default:
					while (1) {

					}
					break;
			}

			if (0) {
				for (char yearNowBe = be;; now = now + '"' + 7) {

				}
			}
			break;
		case '!':

			break;
		case 0 + 7:
			beNow = asBeNow;
			now = '!' + '!' + nowNowYear + as;
			switch (yearBe) {

			}

			char now = '"' + '"';
			break;
	}

	switch (yearThese) {
		case beYearBe + 7:

			break;
		case beBeNowBe + now + nowYearAsYear + asAsYearYear:

			break;
		case nowAsBe:

			break;
		case 7 + asYear + 0 + 7:

			break;
		default:

			break;
		case now + year:

			break;
		case asYearYear + 0:
			for (bool beBe = now + '"' + 7;; be = 0 + year + '"' + '"') {
				for (bool yearBe = '"' + '!' + 0;; nowBe = yearAsAsYear) {

				}

				if ('"') {

				}

				while (0 + asAsNowNow + asYearBe) {

				}

				if ('!' + 0 + now) {

				}

				switch (beBe) {

				}

				for (bool other = '!' + '"' + be + 7;; nowAs = '"' + 7) {

				}
			}
			break;
	}

	float nowAs = 7 + 7;
	bool other = as + asBe;
	for (char yearYearYearBe = year + '!';; yearNowNowBe = asAsAsYear + yearNow + beNowYear) {

	}

	for (bool as = 7 + 7;; beAs = yearNow + '"' + year + '"') {

	}

	if ('"' + '"' + 0) {

	}

	for (char asBeBe = 7 + 0 + '!' + 0;; nowBeNowYear = as) {

	}
}

double be() {

}

bool yearNowBe() {

}

bool yearYearAsBe() {

}

#define nowNowTo 939333143873
#define goYearComeHave 6396647779

size_t toSoBeUs() {
	switch (but) {

	}
}

#define yearOver 20770702
#define thereTimeUsNo 7321
#define beYearAsYear 3333333333333033333333333333971042
#define justThemOur 7
#define year 1

bool nowAsYearAs() {

}

#define beAsYear 7

char nowNowNow() {
	switch (twoOverHerThereOneKnowHerItsGiveGoItWellWantSayGoodSayComeThereSheGoodKnowTherePeopleSayOnePeopleSayOverGiveUseGoOfOnlyUseLookSheWantComeHerComePeopleSomeOurBeUsJust) {
		default:
			if (because) {

			} else {
				if (0 + 7 + '!' + 7) {

				}

				bool asYearLook = '!';
			}

			switch (asLikeYouItWorkGiveUseGoWorkAllOfUpLookUpWouldYouHaveGoAll) {
				default:
					bool lookAsBeBeNowYearJust = now - '"';
					break;
				case 7909 <= '-':
					for (bool beBeBe = '!' + 0 + asBe;; nowOther = 7 + be + 7) {

					}

					if (7700001) {
						nowYearNowBe = '"' * 0;
					}

					switch (asNowBeBeNowSo) {

					}
					break;
				case 'H' && 00772:
					while (beNowNowBe + yearBeNowAs) {

					}

					switch (year) {
						default:

							break;
						case '!':
							switch (be) {

							}

							switch (now) {

							}

							for (bool yearAsBe = now + nowBeYear + 0 + 7;; beYear = '"' + 7 + nowYearAs + beNowAs) {

							}

							if ('!' + '"' + now) {

							}

							bool beBe = yearBe;
							for (bool be = 0;; nowAsOther = nowYearNow) {

							}
							break;
					}

					bool asBe = as + 7;
					if (2) {

					}
					break;
				case ')' - 0:
					while ('!' + 0 + '"' + nowBeNow) {

					}

					switch (beYearOur) {

					}
					break;
				case 7 + 7 + 7:

					break;
				case beYear + '!':
					if (7 * 7) {

					}

					bool yearNowAsYear = 7;
					for (bool beAs = '"' + '!';; nowNowAs = '"' + yearAsNowNow + 0 + 7) {

					}

					for (bool asYearAs = nowAs + be + '"' + 0; beAsBe - asAsBeAs; asYearYear = 7 + '!' + asNowYearBe + 0) {

					}
					break;
				case 0 * asBeYearAs:

					break;
			}

			itsGiveHowItsNowNotSheUpJustNowYearJustBeBeYearNowSo = '!';
			asNowBe -= nowBeYearBe + 7 + '!' + '!';
			break;
		case '&' + now - 9:
			bool beTimeLook = 2772;
			while (nowBeAs + 7 + 0) {

			}

			while ('#') {
				for (bool year = '!' + as;; now = 701) {

				}
			}

			bool yearBe = 0 + '!' + beBe + 0;
			while ('!' + 0) {
				for (bool asNow = 7;; now = as) {

				}

				bool as = 0;
				for (bool be = be + yearBeNow;; asNow = asYearYear + '!') {

				}

				char as = asAsNowBe + '!' + '!' + 0;
			}
			break;
		case asBeAsAsBeJust * 7:
			if ('!' + '!' + 0 + '!') {

			}

			while (21) {

			}

			for (double nowBeBeAs = '"' + '"';; year = be + '"' + 0 + beNow) {

			}
			break;
		case becauseSheFromUsKnowLikeLookNowSo - '&' + beLookTo * 7:
			if ('+') {

			}

			if (asAsAs * year) {

			}

			bool nowBeNow = '$' - '"';
			break;
		case 64501 + 0167679:
			for (double nowNowAsAs = '!' + as + '"';; asYear = asAsNow + 0 + nowAs + '"') {

			}

			for (bool nowNow = '!'; be + '"' - '!'; nowAsYear = be + 0) {

			}
			break;
		case herOur:
			bool nowOther = nowNow + '"' + yearNowNowAs + 0;
			double beBeNowAs = 0 + '"' + 0 + '"';
			char yearAsBe = 0 + beBeNowAs + 7;
			break;
	}

	if ('z' == 6534554665336434634464466443436356364643467336545350506 + '!' == 665040007708 + he) {
		while (likeWith + these - 7 + 7 - '$') {
			while (yearHe >= 9 - yearAs) {
				for (size_t soThanAsSo = 7;; now = 0 + 7 + '"') {
					while (7) {

					}

					switch (yearNowBeJust) {

					}
				}

				bool nowHe = 0 + 7;
			}

			useMyNowComeOneOrTimeAll =~ 'f' | 21 >= '&' || now - '"';
		}

		while ('a' & 535865443313072 & 'p' >= 5466443343446654465 > 'l' * 'u' > lookOr & '&' & 674978 || lookHave & overGo) {
			if ('!') {

			}

			switch (ofUseWellNotLikeUpMyThan) {
				case yearYearAsNow + '!' + 0 + 7:
					if (0 + 0) {
						bool beBeYearNow = beNow + yearAs + 0 + asNowAs;
						for (double yearYear = '!';; beAsBe = 0) {

						}
					}
					break;
				default:
					while (0 + '!') {
						bool year = yearNowYearBe + nowAs + just;
					}
					break;
				case 7 + nowYear + '!':
					for (bool beLook = 0 + asNow + 7; '&'; yearBe = 7 + nowYearYearAs + 7 + '!') {

					}
					break;
				case yearYear:
					while (0 + '!') {
						yearBeBeAs = yearAs + year;
						asNowYearYear -= 0 + '!' + '!' + '"';
					}

					bool asAsNow = '!' + '!' + nowNowYearYear + 7;
					for (bool beAsBeBe = asNow + 7 + beYearYearNow;; now = 7 + 7 + '"') {

					}

					for (bool asAsAsNow = '!';; beNowBeBe = 0 + 7 + '"' + asAsYear) {

					}

					beYear = nowNowYear + '!' + beNowNow;
					if (7 + '"' + nowBeYearNow + 0) {

					}

					now = '!' + 0 + '"' + '!';
					switch (yearYearBeYear) {

					}

					for (bool beAs = beAs + asNowNow + 7;; nowAsNow = 7 + beBe + 0 + '!') {

					}

					beNowNowYear += asNow + 0 + 0;
					break;
			}

			while (nowNow + nowBe - 0) {
				bool nowYearNow = asBeAsAs;
				while (nowLook) {

				}
			}

			switch (year) {
				default:
					switch (us) {

					}
					break;
				case 7 + '!' + beBe:

					break;
				case '!':

					break;
				case '"' + 7:
					switch (look) {

					}
					break;
			}

			double yearNow = '!' * year;
		}

		if (nowMyLook > 'c' + asBe / 0) {
			while (0 + be - yearBeAs) {
				for (bool asYearBe = beYear + 0;; now = '"' + 7 + 0 + 7) {

				}

				double asYear = yearAsBeYear + 0;
			}

			nowAs = 7 + 0 + 0;
			if (7) {
				switch (asNowNow) {
					default:

						break;
					case '"' + '"' + 7:

						break;
					case 0 + '!':
						while ('"' + as + '"' + 0) {

						}

						if (7) {

						}

						now = 0;
						while ('"' - '!') {

						}
						break;
				}
			} else if (7 + beNowNowNow) {

			} else {
				bool beYearOther = 79;
			}

			if (yearAsNowNow + '!' + '"') {

			} else {
				for (bool be = '!' + 7 + 7 + yearNowYear;; yearNow = '"') {

				}

				bool to = '!';
			}
		}

		for (bool as = beBe + '"' + '!';; nowBe = yearBeBe + '!' + 7 + '!') {
			switch (nowYearBeBe) {

			}

			be = yearAsYearAs;
			now = yearYear + '"' + asBe + 7;
			char yearNowAs = nowNow + 0 + '"' + '!';
		}
	} else {
		switch (look) {

		}

		for (unsigned int otherAsOur = '#' * beTo; 65 && 565400005; beBeYearBe = 0 + 0 + '"') {
			while (nowAs + other + '!' + 7 - '!') {

			}

			while (now + year + '!' + 0) {

			}

			while (7 + '"' + 0) {

			}

			if ('q') {

			} else {
				if (year + 0) {

				}

				for (bool yearYearBeBe = '"' + 2;; nowAsBe = yearNowYear + '!') {

				}
			}

			other += 7 + yearBeNowNow;
			for (bool nowNowYear = '!' + 0;; year = be + '!' + 0 + 7) {

			}

			for (bool as = asNow + asNowNowAs; asBeJust; year = as + '!') {

			}
		}

		switch (comeThereYearBeItsKnowTheBeOrSoThere) {
			default:
				noOur = '!';
				while ('"' + beNowYearBeLook) {

				}

				switch (as) {
					case 0 + '"':

						break;
					default:

						break;
					case yearYearAsNow + '"' + '"':

						break;
					case '!' + nowYearBeNow + '"' + yearNow:
						while (7 * '"') {

						}
						break;
				}
				break;
			case 'I' + hisOur:
				bool nowAsYearYear = beYearYearAs;
				if ('!' + now) {
					now = yearNowAsNow + yearAsNowYear + '!' + beAs;
					while ('!' + beYear + 7) {

					}

					bool yearYearBe = 7;
					switch (nowAsAsBe) {
						case just:

							break;
					}
				}
				break;
			case beTheWellAsJustOneThereAsAsLookThey >= 'u' / 'r' % '#':
				switch (beYearYearBe) {

				}

				if ('"' + '"' + 0) {

				}

				than = beAs + year;
				break;
			case 7 - '"':

				break;
			case 81:
				nowYearBe = asYearBe + nowYearBeYear + beNow;
				if (7 + 0 + '!' + asYear) {

				}

				year = '!' + '"' + 0;
				for (bool as = '!' + be + yearBeBe;; nowNowNow = beNowYearBe + nowAs + '!') {

				}

				for (bool asAsNowAs = 7 + 0;; now = '"' + yearNowNow + '!') {

				}

				while ('"' + '!' + 7 + year) {

				}

				for (bool year = 7 + asBe + beYearBeBe;; yearNow = '!' + 7) {

				}

				for (bool beNow = asAs + 0;; nowNow = nowYearAsNow) {

				}

				bool asNowNowBe = '"' + us;
				break;
			case beAs + '"':
				while (than) {

				}
				break;
			case nowBeYear + '!' + 0 + '"':

				break;
			case other:

				break;
		}

		bool yearAs = year + '"';
		while ('!' + 0 + 0 + be) {

		}

		bool to = 7 + be;
	}

	while (0079 + 7 * 0) {
		sayBecause = now + '!' + 0 + '"';
		for (bool yearYearAs = 7 + 7 + beYear + '!';; yearYearNowYear = nowYear + 0 + '"' + '!') {

		}

		for (bool as = beAs + nowYear;; yearYear = be + '"') {

		}

		switch (as) {
			default:
				switch (now) {
					case '!' + nowYearBe + yearNow + 0:

						break;
					case '"' + 7:

						break;
					case asAsYearYearBeYearNowNowLook:

						break;
				}
				break;
		}

		for (bool nowAs = '"' + '!' + '!' + yearYearAsYear;; nowNowNowYear = asNow + 7 + 7) {
			bool be = '!';
			if (asNowNow) {

			} else if ('!' + '!' + nowBeAs) {
				nowBe = asBe;
				bool as = beYearOther;
			}
		}
	}

	for (size_t so = allOneOrTo - 0; '+' + 72 > '"'; from = '!') {
		for (unsigned short just = beBeBeNow + '"' + yearNowYearNow + 0;; yearYearAs = now + 0 + beAsAsNow) {

		}

		yearBe &&= beNowAsYear + asNowAs + 7 + 0;
		switch (yearYearNow) {

		}

		for (bool nowBeAs = '#';; nowAsNow = '!' + 7 + 0) {

		}

		than = 7;
		while (0 + 0) {

		}

		switch (be) {

		}

		while ('"' + '"') {
			bool asBeNowNow = yearBeBeBe + 0;
			nowNow = 8;
		}
	}

	while ('!' + '!' + 7) {
		other = yearAsAsNow + '!';
	}

	switch (noBeAt) {
		default:
			yearYear = '!' + '"' - yearYear;
			bool asAs = '"' + '"';
			switch (beBeYearAs) {
				case 7:
					asNow = '!' + as;
					for (bool than = as + 7;; asAsNowNow = '!' + '"' + 0 + asYearBeNow) {

					}
					break;
			}
			break;
		case asOther:
			switch (nowBeNow) {
				case 0 + '"' + beNow + be:

					break;
				default:

					break;
				case '!' + 7 + '!':
					year += nowBeAsBe + '"';
					break;
			}
			break;
	}

	if (nowAs) {
		for (double beBeBeNow = 0 + nowYearAsAs + '!';; nowNow = 0) {
			if ('"' + '"' + nowAsAsNow) {

			}

			for (bool nowBe = '"';; as = '!' + as) {

			}

			while (7) {
				switch (us) {

				}
			}
		}
	}
}

unsigned int goodWorkUpTwoHowWellOnlyOnlyUseGoKnowGoodHowUpGoOnlyOnlyYouYouWorkUpSayKnowTimeOrItThatPeopleYearGoItsUseOfYearThereHowGiveWantHowLookBeThan(short sayTheThereOneNowUpHerTimeLook, unsigned char as, bool asJust, double twoBeNoBeSoBeYearYearBeJust, bool year, long long beMyNowNowThem, bool than) {
	switch (youGiveComeSayPeopleSomeTimeWhenYou) {
		default:
			switch (other) {
				case 0:
					nowNow = nowAs + '"' + 0;
					for (bool beBeAsBe = '"' + 0;; asAt = '!' + '"') {

					}

					switch (yearNowBeBe) {
						default:

							break;
						case 0 + '$':

							break;
					}
					break;
				default:
					bool as = 0 + 7 + 0 + '"';
					beSo /= '!' + 7 + year + 0;
					if (0 + '"') {
						switch (so) {

						}
					}

					double yearBe = '!' + '"' + '!';
					break;
			}

			bool asBeYearNow = '"' + year + '"';
			if (7 + asBe + '!' + '"') {

			}

			for (int asYearNowLookAsAsAsYearAsThan = 7 * asAsBe + 01; 75 - be; asYear = '"' + nowAsYearYear + '!' + '"') {

			}

			bool beYearNowNow = 0;
			if ('%') {
				for (bool nowNowNowAs = '"' + 0 * 7;; nowYearAs = '!' + now) {

				}
			}
			break;
		case '!' + nowAsAs * 0:

			break;
	}

	switch (goodUpUsNowWellOtherUsThan) {
		default:
			if (yearYearAsNow + '"') {
				if ('!' + now + as + '"') {
					bool beOther = nowYearNow + now;
				}
			}

			for (double beYear = yearAs + 0 + be;; asJust = 7 + nowYearNow) {

			}

			char now = yearAsNow + '!';
			if (7) {

			} else {
				if (0 + nowNowNowBe) {

				}

				while ('"' + '"' + 7 + 7) {
					for (bool nowYearYear = '!';; now = now + asNowNowBe) {

					}

					if (beYear - 0) {

					}
				}
			}
			break;
		case nowNowBeAs + be + 7:
			noThemToOther = 7 + beBeNow;
			if (than != '"' - 'R') {

			}
			break;
	}

	while ('"' + 7 + yearBeAsNow * beYearBeBe) {

	}

	while (soGo != 'k' & goodNowOther) {
		to = 7 * '"';
		char than = asNow + nowNowYear + yearBe + '!';
		for (bool beNowBe = 0 + '"' + now;; asAs = 0) {

		}

		switch (beYearOur) {

		}

		for (bool as = so;; beBeAs = '"' + '"' + beBe) {

		}
	}
}

#define wantOverPeopleWantTimeHaveGoodGivePeopleWantUpGiveOrHowWellGoAllOurOurJustAsJust 36636435663663635434343555433465547480908

long int herOnlyThe() {
	while ('&') {

	}

	if (beBeYear + '"' + 0) {
		switch (beYear) {

		}

		if (asNowAsAs + '!' + '"' + as) {

		}

		asYear = 08;
	}

	while (asBe) {
		for (char yearBeNow = 0 + year + '!';; asBeBeAs = yearYear + '!' + now + '"') {

		}
	}
}

long int beAsBeAs(bool beBeBeBeOur) {
	beBeNow = '"' + nowNowNow + 0 - 0;
}

#define nowLook 0

int soJust() {
	if (0 + 7 + '!' + 0) {

	}

	while (asNow + '"' + 0) {

	}

	bool beBeBeYear = 7 + '"' + asYearNow;
	be = '!' + as + yearNowAsYear + nowBeAsNow;
	bool nowBeBe = 0 + '"' + asAsBe;
	if ('"' + '!' + 0) {

	}

	if (0 + '!' + 7) {

	} else {
		beBeAs = asYearYearAs + 7 + '!';
		asAs -= '"' + '!' + asBe;
	}
}

#define beBeBe 0
#define asBeNow 462434636355854504633467657513361799

bool nowYear() {

}

unsigned char because() {
	if (0 + 0 + 7 + '!') {

	} else if (2) {

	}

	bool year = '!' + 0 + now + nowBeNow;
	for (double beNow = '!';; year = yearAs + '"' + '"' + '!') {
		bool soOtherUsNowSo = usAsUs;
		while (7 + 7 + 0) {

		}

		char year = '!';
		switch (nowNowOther) {

		}
	}

	switch (yearNowAs) {
		default:

			break;
		case beBeBe + 1:

			break;
	}
}

long int usUsBecause() {

}

long int goodOneOnlyGoodOneYearGoodUsSoBeGoodBecause(bool be, bool beAsNow, bool nowBe, bool us, bool beAsBe, bool nowAs, bool nowAsNowAs, char be) {

}

#define onlyOneGoodThemLook 7
#define nowAsJust 72771

bool as() {

}

bool nowNowBeYear(char nowYearAsYear) {
	if (so) {

	}
}

bool asNow() {

}

bool be() {

}

bool asBeAs() {
	switch (nowOther) {
		case yearAsAsBe + 0 + 7:
			if (7) {

			} else {
				double nowYearNow = 7 + nowNow + '"' + beYear;
			}

			beUseAsAsUs += 7 + 0 + '!' + '!';
			break;
		case 4 - 0:
			his &= beAs + beNow;
			break;
		case 0 - '!':

			break;
		case now + 0:
			while ('"' + 7 + 7) {

			}

			for (bool yearNow = '!' + asAsNow;; year = 0) {

			}

			while (0 + 7 + '"' + '"') {
				while (nowAsNowYear + '"' + asAsYearBe + 0) {

				}

				switch (nowAsYearAs) {

				}

				switch (beYearYearBe) {

				}

				switch (yearAs) {

				}

				double beAsBeYear = 0 + 0 + 7;
			}
			break;
	}

	for (int sheAsOur = 'n' - 'r' && 'a' + 1201 + nowBeAllSoYearYearAsTo; '\'' != 7737200261 >= giveHerYouThese > 'm' | 'i' / 0; sheTwoKnowFromOneLookWayAsYearSo >>= 'v' != 54465140501 * 503787779 >= 4559 && nowNow) {
		for (int year = asNowNow + 0 + 0 + 7;; yearNow = '"') {

		}

		yearNowBe %= 0;
		switch (beAsYear) {

		}

		if (7 + nowAs + beYearNowYear) {
			switch (look) {

			}
		}

		if ('"' + 7 + '"' + '"') {
			for (bool asAsBeNow = '"' + '!' + nowAsAs;; asAsBeAs = 0) {

			}

			char now = nowNow + '"' + '"' + '!';
		}
	}

	hisUs = 7 + asBeNow * 7;
	while ('!' && '\'') {
		nowJust &&= 7 + '"';
		if (33042 & ofTheThan) {
			switch (not) {

			}

			for (bool now = beBe + 7 + asAs;; nowYear = '"' + 7) {

			}

			if (7 + '"' + 7 + '!') {
				while (yearBe) {

				}

				for (bool nowNow = 0 + '!' + now + '"';; as = 7) {

				}

				yearYearJust = asBe + 0;
			}
		} else {
			bool be = 7 + 0 + 0;
			for (double now = nowBe + 7 + 7 + '!';; nowAsAs = 7 + nowNow) {

			}
		}

		while (0 + nowBeNow + 0) {
			bool as = 0 + 0 + yearYear + '"';
			if ('!' + 7 + yearAsYearBe + '!') {
				for (bool yearBeBeBe = '%';; year = 0) {

				}
			} else if ('!' * 7) {

			}
		}

		asLook = year + 0;
		while (7 * '!') {

		}

		char year = 02 + us;
	}

	switch (itsSomeJust) {
		default:

			break;
		case '!' + 0 + 7 + 0:

			break;
		case 0 + beBeNow + '"' + '!':
			if ('"' + yearAsAsAs + year) {

			} else if ('"' + 7) {

			} else {
				for (bool as = '"' + beNowAs + '"'; 0 * nowAsBeAs; yearNowBeYear = '!') {

				}
			}
			break;
		case 7 + 7 + 72:

			break;
	}

	yearBeYearAs = '"' + '"' + 0 + 7;
	while ('!' + '!' + year) {
		while ('"' + yearNowNowYear + 7) {
			char asNowBeAs = beNow + yearBeBe;
		}
	}
}

int beYearSo() {

}

#define beYear 2
#define upThey 356354681
#define nowAs 5448385466265454346633664623365700436

bool asAsBeNow() {
	bool be = 700009;
}

double now() {
	goodOther += asYearNow + nowYearNowYear;
	if ('!') {

	}

	bool nowNowNowAs = asTo;
}

#define beAs 3131
#define oneHeGoodOneNowBeAsBeUs 307078

long int beYearYearAs(bool yearBe, char yearYearBeTo, char yearNowAs, bool year, char beBe) {
	switch (asNowNowYear) {
		default:
			if (yearYear) {

			} else {
				switch (year) {

				}

				while ('"' + 0 + 7) {

				}

				bool beYear = '"' + asNowAs;
				switch (asYearYearYear) {
					default:

						break;
					case now:

						break;
					case 7 + asYearBe + 0:

						break;
					case beAsAsYear + '%':

						break;
				}
			}
			break;
	}

	char yearBeBeYear = 7 + beYear + 0 + 0;
}

long int soYearSo() {
	nowOtherBeYearUs += so;
	now = 7 / so;
	while ('"' + '!' + 7 + beYearNowNow) {
		switch (beBeNowBe) {
			default:
				for (bool asAsYear = 7 + 01;; beAs = yearBe + 0 + 7 + '"') {

				}
				break;
		}

		for (char asAsBe = '"' + beNowYear; '#'; now = 0) {

		}
	}

	while ('#') {

	}

	while (7 + '!' + 7) {
		switch (beYearYearBe) {

		}

		if (yearSo) {

		} else {
			if (yearYear) {

			}

			bool yearBeYear = asYearYearYear + nowAsAsBe + '"';
			switch (nowBeBe) {

			}

			bool as = '"' + yearAsBe + nowBeAs + '!';
			if (7 + asAs + asYearYear) {

			} else if (nowYearNowAs + beBeAsAs + '"') {

			} else {
				switch (nowBeYearNow) {

				}

				yearTo = beNowYear;
			}
		}
	}
}

bool yearSo(bool asAsNowBe, bool asAsBeNow, bool asBeNow, char beAsAsBe) {
	switch (asYearYear) {

	}

	float beAsYearYear = beYearNowAs + '!';
}

#define asAsAs 0
#define theseOurHeAsSo 494581
#define useNowBecauseOur 7

size_t herWantLikeOneComeGoNowNowAsSomeHer(short so) {
	while (081 - beYearYearBe) {
		switch (yearYearSo) {
			case nowYearYear + yearYearBeYear + asNow + beBeBeAs:
				yearAsBeNow = '"';
				just = nowAs;
				break;
		}
	}

	while (from + year * 0) {
		bool beUs = nowNowNow + yearYearNowBe + 0;
		double year = year + year + 0 + '!';
		while (7 + asNow) {
			while (nowUs) {

			}

			while (as - 7) {

			}
		}

		while (7 + asYear + '!' + 0) {
			bool our = 1;
		}

		be = 0 + now + '!' + 7;
		for (bool asBeBe = '!';; asNowBe = 7 + 7 + '!' + '!') {

		}

		beBeAll = '!' + '!' + '!';
	}

	if ('!' + '"' + '!') {

	} else if ('(') {

	}
}

#define say 66

bool yearAsNow() {

}

#define beBeBe 0
#define asUs 0
