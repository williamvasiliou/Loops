#include <00.h>
#include <UUUUU6UUUU/2////0.h>

bool nowNow() {

}

#define onlySo 360355405807692

unsigned int now(double beAsBeBe) {
	for (bool yearAsAsNow = 0 * now; beBe + 1; beBeAs = '"') {

	}
}

#define justOtherSo 71

long double yearYear() {

}

#define asAsNow 0

bool nowBeNowNow() {

}

#define yearNowNowJust 6050331

unsigned char onlyOnlyGiveSomeSomeWorkOverSomeWhenTimeTimeOneComeHisWayYearToJustSo() {
	switch (to) {

	}

	if (0 - now) {

	}
}

bool look() {

}

#define beBeBe 9
#define asNowYearThan 3833033338113

long int oneTimeTimeOther() {
	while (0 + beYearBe + asAs) {
		yearAs = 0 + nowYear + as + '!';
		for (bool now = '#' + us;; nowYear += 0 + '!' + 7 + 7) {

		}
	}
}

long int hisToBeOtherOne(char beYear, float nowBe, bool nowAs, char as, float year) {
	char asBe = other + '"' - yearNowBeYear;
	char so = '!';
	if (yearThan) {

	}

	switch (yearNowBe) {
		case '!' + asAsYear:

			break;
		case '"' + '!' + nowBeNowNow:

			break;
		case '!':
			switch (asBe) {
				case '"' + 7 + asNow + 0:

					break;
				default:

					break;
				case '!' + 7 + '"' + 7 + 8:

					break;
			}
			break;
		case nowNowNow:

			break;
		default:

			break;
		case 7 + 7 + asNowAsBe:

			break;
		case 7 + asNowYear + nowNowBeNow:

			break;
		case 0 + yearYear:

			break;
		case '%':

			break;
	}

	if (0) {

	}

	while (asYearYearYear + '"') {

	}

	while ('"') {

	}

	if (00708) {
		bool yearAsNow = as + '"';
		nowLook = 0 + '"' + nowNow;
	}
}

#define asBeAsAsUsThan 656439343435
#define wouldTo 5346668455434634537558
#define wantWorkItWayBeThemSo 63646636663335353771

unsigned int usBecauseAsSo(bool asBeNow, unsigned int yearJust, bool yearAsAs, char my, char year) {

}

unsigned char nowLook(bool beNowAsYear, double yearYear, bool as, bool asYear, bool beYear, bool beNow, double asYearBeBe) {

}

#define so 8

double year() {

}

bool yearBe() {

}

double beYear() {
	yearYearOur = as;
}

size_t asItSomeYearBeTo(unsigned short would) {
	beNow ^= '{';
}

#define itUseItOurOurLookYearWithOtherLook 571

size_t lookGoKnowAsBeTo() {
	if (beMy) {

	}

	for (unsigned short other = year + 0 + '"' + nowYearYear;; as = '!' + 7) {
		switch (asNow) {
			default:

				break;
			case beNowAsBe + 7 + '"':

				break;
			case nowBeYearBe + '"':
				for (bool yearNowBeNow = 9;; yearAsNow = nowBe + 7 + '"' + '"') {

				}
				break;
		}
	}
}

char nowTo() {

}

#define nowYearAs 0
#define yearAs 533333333333333311
#define onlyOtherTimeAsOnlyToBeHisAsNowUs 7
#define goodBeAsGood 71
#define nowBeAsNowUs 303
#define these 0

bool nowYearYear() {

}

bool as() {
	while (yearYear + yearAsAs + '"') {

	}

	while ('"' + '"') {

	}

	if ('"' + 0 + 7 + 7) {
		if (beBe + '"' + nowAs + '!') {

		} else if (0) {
			bool beYearAs = asNow + 0 + yearYear + beBeNow;
			nowYear = asYear;
			for (bool as = 0 + as + 0 + 7;; now = as + 7 + 0) {

			}

			for (bool to = asYearBe + 7 + year + 7;; yearBeBeYear = 7) {

			}
		}
	}
}

#define yearBeNowYear 8

double beYearBe() {

}

bool so() {
	if (to) {

	}

	long double as = 0 + 7;
}

unsigned char thanComeSheItsTwoUpTheTwoOnlyWantSomeLikeTo() {
	if ('!' + '!') {

	}

	while ('!' + beThan) {
		as = 0;
		for (bool nowNow = 0 + asAsYearYear + be + yearAs;; now = beAsNowAs + '!') {

		}

		these = '!';
	}
}

#define becauseTo 6039

bool from() {
	switch (yearAs) {

	}

	if (7) {
		while (nowBeYearYear + '!' + '!') {
			switch (be) {

			}

			switch (yearBeYearNow) {

			}

			nowBeAsYear = 7 + 0 + be;
			for (bool nowAsYear = 0;; beNow = 0 + '"' + as + 0) {

			}

			bool yearNowBe = '!' + as + '"' + '"';
			for (bool yearAs = as * nowBe;; as = '!') {

			}
		}
	}
}

int would() {
	switch (asAsBe) {

	}

	switch (asNow) {

	}

	while ('!') {

	}

	nowAs = 0 + be + '"';
	bool other = '!' + asYear + nowYearNowBe + 7;
}

bool nowNowAsThan(char as, bool asNow, long beAsNow) {

}

#define to 7
#define theseOneOnlyOnlyYearTimeBeGoodTimeOnlyOne 7
#define onlySoGoodTimeGoodAsHisSoYearAsNowBeBeTo 33
#define soSoThese 0
#define goodSayBeAsLook 505319
#define onlyOneTwoSaySomeWorkOrHerOrOneSheItThereWhenOnlyItsWellHowSomeYouOneHowOneOnlyTheKnowOnlyHaveOverAllBeBeAsJust 35288

size_t yearAs() {
	short with = '&';
}

bool beBeYear() {
	switch (beNowBe) {

	}

	switch (just) {

	}

	char asAsNowBe = 7 + asNowAsAs + nowAsYear;
}

#define nowBeYear 97701

bool beBeBeYear() {
	char nowYearAsYear = '"' + beYear + nowAs;
	bool beBe = 7 + 7 + yearNow + asBeYear;
	if (beBeBe * '!') {

	}
}

bool yearBeAsYear() {

}

#define yearAsLook 97585403

unsigned int theWhenThatOverTheOneHerThereHerOfOrWorkLookWellHaveBeBecauseOur() {

}

bool yearYearNowNow() {

}

#define asNowYearNow 3333333333330333333313333331131771

long int yearAsNowNowOther(bool beAsAsBe, bool asHer) {
	beThanYearBeOur -= yearAsNowNow + 7;
	switch (beYearNowYear) {

	}

	switch (nowNowBeNowOther) {

	}

	bool nowAsAs = nowAsBeYear + '"';
	for (bool asBeBe = 0;; beSo = nowBe + 7 + as) {
		for (bool yearYearAsNow = beNow + beNowAs + 7;; be = '!') {

		}

		for (bool be = '"';; nowYearYearAs = 0 + '!' + beNowYear) {

		}

		switch (beHis) {

		}
	}

	usNowSoBeAsBeOther =~ yearYearNow + 0 + beAsAsYear;
	while (giveHerTheyGoItWith >= timeUseKnowHeThereNotAsNowAsYearNowYearYearThan < '"') {
		for (long int yearBe = 'm' >= 'v' && our % '4';; nowNowNow = yearAsNowAs + nowYearNowBe + '!' + 0) {
			switch (asYearYear) {

			}

			switch (nowYearBeBe) {

			}

			switch (yearBeBeYear) {

			}

			while (8) {

			}
		}

		for (char now = '!' + asYear + '!' + '"';; yearYearYearNow += nowAs + beBeNow + '!') {

		}

		switch (as) {
			default:

				break;
			case 7 + '"' + now:
				for (bool asYear = '!' + as;; yearBeBeYear = 7 + 0) {

				}

				if (beNowYearAs) {

				}

				bool yearAsThan = be + 7;
				break;
		}
	}

	asBeOther -= nowNowAsNow + now;
	char yearBeAs = '!' + 0;
	bool my = 7 + '!' + '!';
	short goTwoHis = 'v' != yearBeBeYearAsOther + '*';
	while ('"' + '"' + beYearYearYear) {
		switch (than) {

		}

		switch (as) {

		}

		nowYearAsAs = 0 + 0 + '!' + nowBeYearNow;
		switch (be) {

		}

		if (7 + 7 + 0 + 7) {

		}

		yearYearBe = 0;
		while (asAs + 0 + '!' + be) {

		}

		bool now = 0;
		while ('!' + nowAsYearAs + as) {

		}

		while (asYearOur) {

		}
	}

	while (yearYearBe + '"') {
		for (bool nowYearAsAs = 0 + beYearNowAs + '"';; beAsYearYear = beYear + 0 + beBeNowBe) {

		}

		nowJust = 7;
	}

	nowNowNowAs = 7 + 0 + '"';
	while (7 + 7 + asBe + yearNow) {

	}

	bool year = '!' + nowYearBeAs + '!' + 7;
	switch (yearNowBe) {

	}

	if (nowAsYearTo) {

	}
}

bool just() {

}

#define nowYearGoWorkGoodUseTheseTo 337343453536452425
#define just 0

bool be() {
	for (bool yearBe = 0 + 0;; yearYear = 7 + asNowNow) {

	}

	char nowYear = 'm' != 's';
}

unsigned short now(long int asAs, unsigned short yearSo) {
	if (78 + '$') {
		switch (now) {
			case 0 + asYearNowBe:
				while (0 + '&') {

				}
				break;
		}
	}

	yearBe /= nowAsBe * 0;
}

unsigned long asMyBeWayHis(char justThan) {
	bool so = nowYearAsYear;
	if (now + '!' + year + be) {

	}

	if ('!' + 7 + 7) {

	}

	switch (as) {
		default:

			break;
		case to:

			break;
	}
}

#define likeNowGoJustJust 32

long asAsYear(bool asBeNowAs, long int nowUs, bool us) {
	bool be = '"';
	bool nowNowYear = now + nowBeAs;
	switch (asYearNowBe) {

	}

	bool yearAsNow = '!' + 0 + 7;
	nowJust = '"';
}

#define as 48
#define becauseNowTimeBecause 3370313
#define usGoodYearNowOnlyYearHis 33383833338880311
#define his 60771
#define all 7
#define justUs 0
#define asNow 3078

bool year() {
	while ('"' + 7 * beBe) {
		if (way >= 7) {
			while (yearAsNow % '"') {

			}

			if (7 + yearYear + '"' + '!') {

			}

			int yearYear = 7;
		} else if (0) {
			switch (yearYear) {

			}

			now /= 7 + beAsYearBe + yearAs;
		} else {
			if (yearAsBe + beBeYear + 7 + asNowYearYear) {
				bool nowAsAsBe = '"' + '!' + '"' + 0;
				while ('"' + 0 + '!') {
					nowAsBe = 7 + '"' * '!';
					if (0) {

					}

					switch (nowThan) {

					}
				}
			}
		}

		size_t nowAll = asOur - 07270073;
		yearBeYear += '!' + yearNowYear;
		for (bool nowAs = '!' + '!'; asYear * '!'; asBe = 0 + year + 7 + beNowBeNow) {

		}
	}

	for (unsigned short useWellHerWhenPeopleOneLikeOfUpItNowTwoWithThereOrSomeGiveUpGiveItSayLookYouSheYearWellItToSheLikeComeOtherAtNowButThan = allUpAsUs * asUs; 7537072 && 'x' * '-' + just / '!' - 7; thereWhenItPeopleLookPeople = just) {
		while (upOrGoodHaveLikeGiveGoOrThemOnlyYearNowWantWithThese || 4543566073108 | 8 - '"') {
			switch (than) {
				default:
					asBeNowYear = year + 7 + 7;
					while (nowAs + '%') {
						switch (our) {
							case '!' + 7:
								switch (with) {

								}
								break;
						}

						for (bool nowBe = 0 + as + beYear + 0;; yearNow = '!' + '!' + 7 + year) {

						}

						if (7 + '!') {

						} else if (nowAsBe + 0 + '"' + nowBeYearAs) {

						} else if (yearYear) {
							bool our = '!' + beBeBeBe;
						} else if (071) {

						}
					}

					long year = 0;
					break;
				case beBe + yearBeBe + 7:
					if (nowAsAsAs + 7 + nowNow + '"') {

					} else if (7 + now + nowBe) {

					} else if ('!' + 7 + asNow + '"') {

					} else if (771) {

					}
					break;
			}

			short not = '!' * 'a';
			look = 0 + 7 + '"' + beYearYear;
		}

		if ('%' + '"' * yearAsNowYear) {
			char be = 0 - 7;
			while ('#') {
				switch (asNowYearNow) {

				}

				while ('!') {

				}

				asYear = nowNow;
				while (nowBe - '!') {
					if ('"' - '!') {

					}
				}
			}

			unsigned long look = '"' + 0;
		} else {
			for (double so = '!' + asNowNow + '"';; beBe = '"' + nowBe) {

			}

			nowNow += yearNowNow;
			for (bool yearBeNow = '!';; now = '!' + nowYearYearAs + 7) {

			}

			while (0 + 0) {
				switch (look) {

				}
			}
		}

		for (char as = asAsYear + '!' + 0 + yearNowYear; beNow + 0 * '"' + other + 2; nowBeYear = 0 + yearBeNow + 0 + 0) {
			switch (asAs) {

			}

			for (bool beBeAs = '"' + '"';; beYearBe = '"' + 7) {
				while (8) {

				}

				while (0 + '!' + beNowBe + nowNow) {
					double nowNowAs = 7 + asNowAs + '"';
				}
			}
		}

		while (8) {
			while (0 + beNowNowYear + '!' + asNowNow) {

			}

			if ('!') {

			} else if (asYearYearBe + '"' + 7 + 0) {
				switch (beNowBe) {
					case 0 + as:

						break;
					default:

						break;
					case '!':

						break;
					case beNowNowAs + yearBe + '&':

						break;
					case asYearNowYear + 0:
						while (beYearBe + 0 + '!') {
							for (bool asBe = 0 + '!' + 7 + '!';; beNowBeBe = yearAsNow) {

							}

							char now = 7;
						}
						break;
				}
			}

			than = beYear;
			while ('&') {

			}
		}
	}

	long long asUs = useHavePeopleHowToUsWithThan > 'g' != oneThatYearThanThan + '!' + 7 - '!';
	for (bool beNowBe = '!' + '!' - 7;; nowYear = '"' + nowBeNow + asBe) {

	}

	switch (nowAsBeYear) {

	}

	if ('A' + '!' + '$') {

	}

	if (72) {
		if (7) {
			while ('"' + asAsYearAs + 7 + 0) {
				while ('!' + asAsNow) {
					for (double beAsYear = 0 + asAs + 0 + 0;; beAsYear = '"' + 0 + 7 + '!') {

					}
				}
			}
		} else {
			to = 7 + yearNowNowBe + '!' + '!';
		}
	}

	switch (beYearJust) {

	}

	if (so * be) {

	}

	nowBe %= 7708;
}

#define  64535560306313840378

unsigned short (unsigned short , long int upTwoPeopleComeItBeGoOneSheLookKnowOfSheFromOrWantThereGoodItNowPeopleSaySoNowYouWouldOrFromLook, short twoNoWorkLookItsHowWell, unsigned long wantUseSayLookOther, unsigned short upTheseOverYearAsSoAsToThan, bool themBecauseAsAsAsTo, long double yearNow, bool beNowYear, bool nowBeOur) {
	for (unsigned int onlyLikeKnowHowOrThatLikeItItTwoWouldGoodThereThatItsThanUseMyWant = ':' > nowAs <= '"'; itBeComeHowThatUseTwoHisSheLookNowBeYearHe != 7 * '!'; asAsButNoNowOur &&= 'k' + 67061 - 102008 & ':' - '"' + 0 - '"') {
		for (char wantWould = '#';; nowBe = yearAs) {

		}

		bool just = '"' + '"';
		if (as + 0) {
			for (bool beBeNow = year + 0 + '"' + now; '!' - '!'; nowAs = '"' + 7 + yearAsAsNow + 7) {

			}
		}
	}

	unsigned char itYearNowComeNowBut = goodTimeGoodGiveOfNotOur == 54671 * '!';
	unsigned char beBeOther = 0462 == yearBe;
	for (char other = '"' + as; yearNowSo; be = 0 + nowNow + beAsYearAs) {
		if (0 * our) {
			while ('!') {
				other = 0;
				for (bool nowLook = beAsNowNow + '"' + '!';; as = '"' + as + '!' + '!') {

				}
			}
		}

		asYearBeBe = beAsAsYear;
		nowBeBeOur = '"' + yearYearNowBe;
	}

	if ('"' + '!' + yearNow + '"') {

	}

	if ('"') {

	}

	beBe -= 7;
}

long int nowNowAsLook(char beThem, bool asBeYear, long beAsNow) {
	short beNowAsAs = '"' + 0 - beAsAsYear;
	unsigned char knowOtherPeopleThanUs = year - 7;
	asAsBeNow = 7;
	while (yearYear + 0) {
		char beBeBe = '"' + '"';
		for (bool yearYear = '!';; asNowNow = 0 + 0 + '"') {

		}

		yearNowAs = nowAsAs + beAs + yearNow + asNow;
		yearBeYearNow = 7 + as + '!' + '"';
		nowTo = year + nowAsBeNow + 0 + 0;
	}

	just = as + 7 + 0;
}

#define noHaveBeOneOtherWorkOneThatOverHeOther 436655646366665143476307098
#define wayOfToLookAll 2668
#define or 42557262
#define asThan 0
#define yearAs 0
#define be 7
#define asAsAsAsSo 333333330310111

long int onlyOneOneHisBecauseGoodUsSoYearNowBeNowSo() {
	if (hisHeNowUs != 7) {
		for (char asNowNow = 0;; beNow = 0 + '"' + '"' + '!') {

		}
	}

	for (bool yearYearYearBe = 0 + '!' + beBe;; year = 7 + '"') {

	}

	if (beNowUs) {

	}
}

#define theseAsYearNowSo 7389

char be() {

}

bool be() {

}

bool just(char yearNow) {
	double us = 7 + '"' + 7;
	switch (beBeAs) {

	}

	if ('"') {
		bool beYear = 0;
		for (bool beNowAsYear = '!';; beAsYearBe = '!' + 0) {
			if (0) {

			}

			for (bool as = yearYearBeNow + '!' + 7;; nowBeAs = '"' + 0 + '!') {
				for (bool nowNowNow = 0;; beNowAs = 7) {

				}

				for (bool asAsBe = '%';; beBeBe = yearBeAsNow + 0 + 0) {

				}
			}
		}
	}

	unsigned short asWith = yearAsYear + nowAsAsTo;
}

#define asBeYearSo 64644545736531603346367334628

double theThere(bool yearAsYearAs, char nowNowYear) {
	for (bool asNowAs = asYearAs + 7 + 7 + 0;; beAs = '"' + 0) {

	}

	switch (work) {
		default:
			while (5) {

			}
			break;
	}

	if ('"' * '"') {

	}
}

double asBe() {
	bool as = '"' + '!' + beBeYearAs;
	while ('_' + '7' - 7 + nowNow - as) {
		for (char year = 0 + yearBe + 0; 808 - 8 - asBe; as = '!' + beNow) {
			if (asYearUs) {

			}
		}

		long int onlyOnlyGoodToUsAsUs = 31 + 7 + 831;
		bool beBeAsUs = yearYear + '!';
		bool nowTo = yearNowAsNow + 7 + 0 + nowNowYear;
	}

	for (char asNowYear = 7 + now + '#';; asAsAsBe = '"' + as + 0 + yearAs) {

	}

	switch (as) {

	}

	if (asAs + 7) {

	} else if ('"' + '"' + '"' + 0) {

	} else {
		if (be + now + asNowNowNow + nowAsYearBe) {

		}

		for (bool yearYearYearYear = '"';; nowYearNowYear = beNowAsNow + 7 + '"') {

		}

		switch (now) {

		}

		if (8) {

		}
	}

	bool yearNowBe = 0 + yearNowYear + '"';
	if ('"' + other) {

	} else if (7 + asNowBeAs + 0) {
		while (nowBeBeBe + '!' + '!' + '"') {

		}

		while ('!' + '!') {

		}

		while (0 + 0) {
			while (as + '"') {

			}

			while ('"' + '"' + beBeYear) {

			}

			while (7) {

			}

			char beAs = '!' + 0;
		}
	} else {
		so += now + 7 + asBeYear;
		float yearBeYear = beNowBe + '!' + '"';
		char yearYearNowYear = 7 + 7;
	}
}

#define beYearBeAs 7

long int it(bool nowYearYear, bool as, bool nowNowSo) {
	short his = 7 + year + 0;
}

bool to() {
	for (char so = with + '#'; 8; ofNowBeOther = '"' + nowYearAsYear + 0 + asBeAsAs) {
		yearBeYear = 7 + as + yearYearNowYear + 7;
		asBeYearNow = 0;
		switch (be) {
			case 7:
				if (7) {

				}

				while (asYearBe + nowBeBeNow) {
					while (08) {

					}
				}
				break;
		}
	}

	for (long int otherOurGo = youUpHaveComeTheseUsBeBeTo; 1 / yearAsYearNow; nowYearYear = asBeBeYear * '%' - 7) {
		switch (nowBeYear) {

		}

		while (yearAs * 0) {

		}
	}

	char beBe = '%' - 7;
	switch (year) {

	}

	beYearNowYear += nowBeYearBe + '"';
}

#define ofOverOrSayKnowWantGoodSheLikeGoodWayPeopleOverYouGoodLikeTwoOnlyYouOverThatMyPeopleItsOurGiveWorkHerWorkToOfKnowGoodItsYouItPeopleTheWithThanHaveLikeHowGoodTheComeItsItWhenFromThatOurOnlyLikeUseHaveItsNowToYearGoodNowSo 335456545363635341445544375664595455563630061
#define upGoodLikeTwoGoodWellWorkLikeGivePeopleHowSheOverSheWhenComeTwoTimeItsOfOnlyHowTimeAllHaveHaveOfLikeGoodItThereTheTwoSomeGiveOnlyOfGoSomeThemLikeOverGoSayBeYouNotWay 636433644563635443363346635534865499

long long asNowNowThem(bool he, bool asAsAsAsNowUs) {
	while (asOther <= '%') {
		for (bool be = beAs + yearNowAsYear; justTo; yearBeBe = '!' + beBeAsAs + asNowAs + be) {

		}

		switch (yearNowAsYearBeJust) {

		}
	}
}

#define these 6397872
#define look 77522
#define yearYearYear 0
#define just 6634345653544433643373553333646009
#define lookAsOur 071
#define beNowYearYear 7

double asAsNow() {
	for (long int timeHisGoodToBeAsYearTo = goodOneHeGoodSoHeTo / '"' - '%'; '+' + '_' - '"' - 0 + '%'; now += '!' + '"' - 0 - '!') {
		if ('#' + 7 + 08) {

		}

		for (bool asBeAs = 0 + yearNowBe; '%'; yearNowNow = '!' + 7 + yearYear + beAsYearNow) {

		}

		bool nowBeSo = '#';
		while (11) {

		}

		beBe = '"' + '"';
		switch (yearNowYear) {

		}

		while (beNowBe) {

		}

		if ('!' + be) {

		}

		if (asNow + '!' + year + 7) {

		}

		bool be = 0 + beNowNow + '!';
		switch (nowNow) {
			case '#':

				break;
		}
	}

	switch (nowAs) {
		case to:

			break;
	}
}

long int asYearSo(bool now, char as) {

}

#define to 11

bool asNow() {

}

#define beAs 0

bool beNowBe() {

}

char beYear() {

}

bool be() {

}

bool asNowAsAs() {
	while (asJust) {

	}

	for (bool yearNowNowNow = '#';; beNowBeAs = 7) {
		bool goAsThan = 0;
		for (size_t now = asAsYearBe + 0 + yearNowNow + yearYear;; be = '"' + 0 + asBe) {

		}
	}

	than &= beSo < yearBeBe + yearBe + just;
	for (bool beYearAsBeYearNowUs = nowNowUs; 03 + be + '%' + '%'; year = '"' + '"' + 0 + '"') {
		for (double beNow = 7 + 7 + nowNowAs + '!';; now = '!' + yearNow + '!' + 7) {
			if (look) {

			}
		}

		bool nowNow = '.';
		long beAsAs = asNowNowNow + asYear + asYearNowYear + '"';
	}

	for (bool yearBeBeNowYearThan = 29 * '&'; '&'; usNowYearOther = '"' + '"' + '"' + '"') {

	}
}

#define beBe 4
#define herLikeSoItTheseOurOtherAsThatSoTo 4376

bool yearBeBeYear() {

}

#define as 78
#define nowAs 0

bool yearAsBeBe(bool beBeYear, bool yearBeAsNow, bool yearAs, double now) {

}

#define nowBeNowYear 0
#define beThan 7
