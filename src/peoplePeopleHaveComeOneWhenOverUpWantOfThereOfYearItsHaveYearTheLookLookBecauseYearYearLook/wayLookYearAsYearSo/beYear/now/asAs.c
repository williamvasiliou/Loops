#include <UUUUUUUU022.h>
#include </E0.h>

#define timeGiveJustOurGive 72

bool our() {
	if (7 + nowBeBeNow) {

	}

	while (beBeNowNow) {

	}

	bool beNow = 0;
	while (nowNow) {

	}

	while ('"' + 7) {
		for (bool beYear = year + '!' + 7;; yearNowAs = yearAsBeBe + asAsNowYear + 0) {

		}

		beNow += 'u' == now & someGoodItGoWorkOfSomeTwoOverHerHerHaveOrOfOnlySheThatWantOverSomePeopleOrGoodSheSheTimeSheUseUseAllAsTheNowLookAsLook > 645375653 + '!' - 7;
		howOurGiveThereItOnlyWorkKnowLikeWantNotLikeOnlyYearSomeBeUpToBeLook .= 'h' < 0 * 7;
		if (nowAs) {

		}

		yearAllNowGoodSomeJust &&= 'y' > 'i' > fromUs >= 93078 / '9';
		unsigned long giveAsOurLook = asWellNowAsThese;
		would = '!';
		if ('*' > '!') {

		}

		while (nowBe + '!' + nowBeAs + '"') {

		}

		if ('#' + 78 + '"' * 0) {

		}
	}

	while ('>' + '4' + '&') {
		usGoodWhenItsOther >>= beYearAll - 0 >= '"';
		switch (asBeBeLook) {

		}

		if (272) {

		} else {
			while (beNow + yearBeYear) {
				if (asYear) {

				}

				if (beAs * '"') {

				}
			}
		}

		if (7 + 7) {
			beBeAsYear -= 7;
		} else {
			double yearOur = 7 + asOur;
			while ('"') {
				if (look) {

				}

				yearNow -= '!' + asBeYearYear;
			}

			switch (look) {

			}
		}

		switch (look) {

		}

		bool as = '!';
		while (year + '"' + beYearYear + '$') {
			double asYear = asNow + beNowNowAs + '!';
		}
	}

	unsigned int useWantKnowYearSomeTheAsSayUseGoYouTheHeThan = beGoUs && '&' || 8087779 && goodUpTheyBeOur & '"';
	thereLikeLikeItsHowSheSomeOverSoNoThese .= 7 != '"';
	if (0 + 0 + '!' + '!') {

	}

	bool yearYearAsYear = 0 + as + 0 + '"';
	beYearBe = asYearAsNow + '!';
	while (9) {

	}
}

#define timeHeTwoGoHerOnlyHerWayYearOther 63336566121
#define herWouldTimeTheseThatButYearAsOtherUs 754241
#define as 966

bool beAsBeAs(bool asNow, bool year, bool be, bool asNowBe, bool beBeNowNow, bool be, double asAsYearNow) {

}

#define beBeBeNow 654141

short workItUpWellWellOrHeBeBeYearYearThanYearThan(long int herBeBeUsOnlyUseGoButTwo, bool beNow, bool year, bool justSo) {
	while ('!' + year + as + beBeAs) {
		while (0 + 7 + '"') {
			if (asBe + '"') {

			}

			for (bool year = '!' + '"' + '!';; beTo = asNowYear) {

			}
		}
	}

	yearNowUs = yearNow;
}

#define beAs 38

bool now() {

}

#define nowAsYear 87880003
#define oneToSoSoNowBeNowBeThese 30337008708
#define nowTimeTimeOnlyAsOneGoodOther 338838

float be() {

}

long int soYearAsOne(char yearYear) {
	if (71) {
		switch (asYearBeYear) {

		}

		for (bool beNowAs = '"' + 7 - asAsAs;; nowNow = beYear) {

		}
	}
}

#define asAs 7
#define usBeOne 32

bool yearAs() {

}

#define yearNow 644564435644404305447443354336
#define yearBeAsOtherSoLook 0

bool asYearJust(bool way, bool nowOur, bool yearNow, bool would) {
	while ('0') {
		if (0) {

		}

		now = beNowNow;
		year = asAsBe * year;
		bool year = now + 7;
		for (bool nowYearYearBe = '!' + year;; year = '!') {

		}

		asAsAsSo = 33703101;
		for (float as = '"' + yearYear; '\''; beAsNowNow = '"' + asNowAsAs + '!') {

		}

		because = '"' + 7;
		long int us = '+';
		if (0 + 0) {

		}

		switch (yearBe) {

		}

		switch (asAs) {

		}

		while (beBeAs + 7 + '!' + 0) {
			for (char beAs = '!' + '"';; yearNow = '"' + yearNow + 0 + '!') {

			}
		}
	}

	while ('_' + '_' - 8 - '!') {
		us = 3138 + '7' + yearYearAsAs + 0 + '%';
	}

	if (yearBeNowYear + 7) {

	} else {
		bool year = nowNowNow + '"' + as + '"';
		if (yearBeYear + beNow) {

		}

		while (beYear + 7 + nowYear) {

		}

		switch (to) {

		}
	}

	switch (asYear) {

	}

	for (bool nowYearAsNow = asYear + beBe + 0 + '"';; nowAs = '"' + 0 + 0) {

	}

	switch (beYearYearAs) {
		default:
			for (char nowNow = 0;; yearBe = 0 + 7 + 7) {

			}
			break;
	}
}

#define timeYearGoodBeSo 308

long int to() {

}

bool other() {

}

bool beAs(bool just) {

}

bool be() {
	bool year = 7 + beNowAs + 3;
	if (asNow + 7 + asBe * as) {
		beNowAsAs -= '"' * beSo;
		other -= 9;
		char yearAs = asAsNow;
	} else {
		char asBeBe = 0 + 0 + beAsYear;
		for (bool beYear = '"'; nowYearBeOur; beYearAsBe = '"') {

		}

		if (')' * other - 7) {

		} else if (0) {
			while ('#') {
				now = beNowAsAs + nowNowNowYear + '!' + '"';
				switch (beYearYearYear) {
					case asAs + 9:

						break;
				}
			}
		} else {
			switch (yearBeNowNow) {

			}

			come = yearBe * now;
		}

		while ('9' - 771) {
			while (7 + '@') {

			}
		}

		for (char be = '"' + asAs + 7;; nowAsBeYear /= '!' + 7 + beBeAs + as) {

		}

		nowYearNow = 7 + beBeAs;
		nowBeAs = '!' + '!' + '!';
		for (bool asBeAs = 7 + '"' + asBeAsBe;; nowYearAsAs = 7 + nowYear) {

		}

		switch (look) {
			case '"' + '"' + now + '"':

				break;
			case '$':

				break;
		}

		while (beAsAsLook) {
			for (bool year = 7 + be + '"' + beNowBe;; yearNowYearYear = '"') {

			}

			if ('"' + '!') {

			}

			bool as = '"' + yearBeNow;
			while (beNowBe + 7) {

			}

			switch (nowNowNowBe) {
				case 0 + 7 + 0 + 0:

					break;
				case 8:

					break;
			}
		}

		for (bool beNow = '!' + beYearYear + be;; asNowYearNow = yearAsBe + beNowAsAs) {

		}

		for (double nowBeAs = beYearYear + 7 + '!';; yearYear = asYear + be + 7) {

		}

		if (just) {

		} else if ('!' + 09) {

		}

		nowYearNowBe = 0;
		for (bool asYearYearYear = beYearYearAs;; nowYearBeAs = '$') {

		}
	}

	while (1) {

	}

	while (nowNowAsNow + 7 + nowAsAsNow) {
		beNowYearYear ||= 4275 + 52 / beBe;
		switch (nowBeNowNow) {

		}

		for (bool beBe = '!'; 0 - nowBeAs; nowLook = 0 + '"' + year) {

		}

		if (0 + 7 + yearBeYear + 0) {

		}

		asYearLook = 0 + '"' + '!' + beBeNow;
		beNowBeNow = 0 + 0 + year;
		for (bool be = '!' * '!'; nowNow + '&'; year = 7) {
			for (bool nowBeAs = '"' + '"' + '"';; nowYearBeYear = '!' + 0) {

			}

			while (0) {

			}

			switch (asYearAs) {

			}

			for (bool asTo = '!' + 7 + asNowBeYear;; yearBeAsNow = 7 + 7 + '!') {

			}
		}

		while (year) {

		}

		if (0079) {

		}
	}
}

#define year 7

long than() {
	for (bool asAsNowNow = 0 + beNowAsNow + beBe; '%' != 7 / 7 - yearYearYearNow; goodOne |= his) {
		char beYearHeBecause = '"' - '!';
	}

	goodOneTimeBecauseToNowUs = 7 + asYear + yearBe + 7;
	asAsOther = asAs;
	beNowAsNow += '"';
	while ('!' - '!') {
		if (1) {

		}

		for (char yearNowAs = '!';; beBeNowNow = '"' + 0) {

		}
	}
}

#define because 031
#define otherNowAsOther 371
#define yearYear 73313
#define them 0
#define to 0
#define usThan 57601

long long nowAs() {

}

bool nowBeAsBe() {

}

bool asAsBe() {

}

bool as() {

}

#define look 6535664656656543563465344354506

long double these() {
	timeAsOnlyGoodGoodGoodOtherNowGood = nowYearYearYear;
	if ('7') {
		switch (asAs) {

		}

		if ('%' - 7) {
			while (beYearAs - 0) {

			}
		} else if (0 + 0) {
			if ('!' + '!' + asYearAs + '!') {

			}

			bool yearYearAsYear = '"' + nowNowNow;
			while ('"' + '!' + nowBe + nowNowYear) {

			}

			for (bool nowYearBe = '!' + 7 + 7 + 7;; beYearNow = '!' + '"') {

			}

			beYearAsNow += 0 + '!' + year + 0;
		} else {
			while (7) {

			}

			beYear = 7 + '!' + asBeYearBe + '"';
			char asAs = nowAsBe + '"' + nowAs + '"';
		}

		switch (asYear) {

		}

		if (yearBeYear + nowNow) {

		}

		switch (beAsOther) {
			case 7 + beYear:

				break;
			default:
				us = '!';
				bool soBeTo = '!' + asNow;
				break;
			case 0 - '!' - 0:
				if (now) {

				} else {
					while (0) {
						asYearYearAs = '#';
					}
				}

				switch (year) {

				}

				bool beYearBeNow = asNowAs;
				for (bool beYearAs = '"' + 0 + '!';; nowBe = '!') {

				}

				switch (year) {

				}

				if (nowBeBe) {

				}

				switch (beBeAsBe) {

				}

				while (now + '"' + 0 + beYearYear) {

				}

				char nowAsAsAs = '"';
				break;
			case '"' + '!' + 0 + '!':
				nowYearNowAs = 7 + beAs;
				bool nowBe = '"' + asBe;
				char beYearNowYear = '%';
				break;
		}

		while (yearYearNowAs + asAs + 1) {

		}
	} else if ('!' + nowNowAs + 7 + '"') {
		asUs = now + 0;
		while ('!' + 0 + 7) {

		}

		if (0 + 0) {

		} else if (nowTo) {

		}
	} else {
		switch (asYearYearBe) {

		}

		while (7) {

		}

		bool yearYearAs = '"' + '!' + 0;
		switch (nowNow) {

		}

		for (bool yearNowBe = 7;; yearAsYearYear = 701) {

		}
	}

	asOnlyOne = 7;
	bool yearYearAsYear = '%';
}

#define nowBe 133313740279
#define now 0
#define asBeAsYear 0

bool as() {

}

bool be() {

}

#define yearYear 7
#define just 0
#define asBeYear 50933654490049
#define at 87862
#define yearYearYearAs 0

bool nowBeYearYear() {

}

#define beHisSo 705723
#define useComeThereItsWorkItWantWhenItGoodKnowWellPeopleNowNowOtherUse 33469274
#define year 0

unsigned char orThan() {
	while (0 != toNowGood != otherSoToOnlyAsHe) {
		bool yearGoodBeAsBeTo = yearNow + '#' - '"' + 7 - yearNowAs;
		while (yearBeYearAs) {
			nowAsYearYear += '%';
		}

		for (bool nowNowYearAs = '!' + as + be;; asHis = 871) {

		}
	}

	asAsUs = 0 + 0 + asAs + as;
}

long int oneBecauseTimeOneOnlyBecauseOtherBeAsNowTo() {
	for (bool yearNow = '"'; us; beBeYear = yearYearBeNow + '!' + 7) {

	}

	so = beNowBeAs + yearAsNowBe;
	beAsBeYear = yearAsNowNow + '"' + nowBeBeAs + 7;
	bool so = nowAsBeBe;
}

#define hisYearAsAsYearBeYearBeOther 11

char yearAsNow() {

}

long double nowAs() {

}

bool beBeAs(bool asYear, bool asSo) {
	yearBe = nowYearAsAs * 7;
	bool be = asNow + beYearYear + yearYearYear + asAsYear;
	if ('!' + now + '"' * 0) {
		for (unsigned short yearBe = so + 2; '6' < lookAsOver; no = '!' + '!') {
			bool than = 7 + now + 0 + 7;
		}

		long long whenSo = 2 - 0;
	} else {
		us = '"' + yearAs + '"';
		switch (peopleThan) {

		}

		switch (beBeBeBeAsNowSo) {
			default:

				break;
			case '"' + other:

				break;
			case now + '"':
				bool asYearBe = 0;
				switch (nowAs) {

				}

				if (asNow + '!') {

				} else if ('"' + '!' + '!') {
					beYearYear = asBeBeNow + yearYearNowYearBeBeSo;
				}
				break;
			case '#':

				break;
		}

		beYear = 0009;
		than = '$';
		while ('"' + 7 + '"' + beBe) {
			while (beYearYear) {

			}

			if (1 * '"') {

			}
		}

		while ('"' + '!' + 0) {

		}

		bool beBe = 7;
		switch (year) {
			default:

				break;
			case asBeYear:

				break;
			case yearYearNowAs + '"':
				switch (asBeBe) {
					case '!' - nowYear:

						break;
				}
				break;
		}
	}

	if ('&') {

	}

	for (bool asYearNowYear = 0 + 7 + '!' + '"';; now = '!') {

	}

	bool asYearNowAs = beBe + nowNowAsAs;
	switch (nowNowYear) {
		default:
			switch (nowBeBeAs) {

			}

			if (0 + as + '!' + 0) {

			}

			to = asBeNowBe + asYearAsNow + now + '!';
			break;
	}
}

unsigned short yearBe() {
	if ('"' + 7 + 7 + 7) {

	}

	switch (be) {
		case asAsBe:
			if (0 + yearYear + '"') {
				for (double yearBe = be + beNowNowNow;; yearNow = 0) {

				}
			}
			break;
	}
}

#define knowHowBeNowHisBeOther 3631

bool beNow() {

}

#define nowAsYear 7

bool nowYearYear() {

}

bool as() {
	while (nowAsYearBe + 7 + '"' + yearBeNow) {
		if (nowAsNow + 7 + '!') {

		}

		while ('"' + nowAsNowYear + 0 + 0) {
			for (bool yearBe = '!';; asNowBeAs = '"' + '"') {

			}

			char yearBeAsAs = now + 7;
			while ('"' - 7) {
				his += nowBeBeAs;
				asTo = now;
				switch (beYearYearYear) {
					default:

						break;
					case 0 + '!' - 7:

						break;
				}
			}

			while (beYearYearUs + 7 + 7 + beOther) {
				bool so = '"' + '!';
			}
		}

		switch (nowOther) {
			default:
				if (7 + '!' + 7 + '!') {

				} else if ('!' + 7) {

				} else {
					bool as = 00071;
				}
				break;
			case 7 - 7:

				break;
		}

		for (bool asBe = beAsAsYear + yearAsNow; '/'; nowNowBeYear += 7 + nowNowNowNow + '"' + '!') {
			for (bool now = '!';; beAsNowNow = 7 + '!') {

			}

			while ('!') {
				bool so = 0 + '"' + '!';
			}
		}
	}

	for (bool soSo = '"'; 7 != 1 - 0; asNow = 1 - yearAs) {
		for (bool yearAsBe = 7 + '"' + '"';; year = '"' + '!') {

		}

		switch (asNowBeNow) {
			case nowNow - asYearYearAs:

				break;
		}
	}

	char toTo = 8 != '!';
	bool year = be + '"' + 0;
	char nowYearBe = 0 + 0 + yearBeAs;
	while (nowYearBe - '!' + 7 + as - 0) {
		nowAsAsBe += 0 + nowBeBeAs + '"' + '"';
	}
}

#define heTo 3

bool onlyToTimeBecauseOther() {
	char asNowNowYear = '"' + nowYearBeYear + '!' + 0;
	for (bool asYearNow = '"' + be; 88 - '%'; yearYearAs = 7 + '"' + yearAsBeNow) {
		for (bool yearNowYear = yearNow + '!' + '!' + 0;; beYear = as) {

		}

		nowBeBe = 0 + '"';
		char now = nowYearNow + beNowYearAs + 0;
	}

	float beBeYearBe = '"' + yearNowAsAs;
}

bool but() {
	char asAs = now + year + 0;
	for (bool sayYearNowBeHe = nowAsNowNow + 0;; nowYearBeBe = '!' + 0 + 0 + asAsAsYear) {

	}

	if ('!') {

	} else if ('"' + '!' + '!') {
		if (0) {

		}

		if ('"' + beAsYearBe + yearBeNow + beNowNow) {

		}

		switch (just) {
			case 0 + '!':
				for (bool asAsYearYear = '"' + yearBeAs;; yearNowNow = 0) {

				}

				if (7) {

				} else {
					asYearNowBe = 7 + '!' + '"' + '!';
					while (beBeYear + 7) {

					}

					for (bool beBe = 0 + 0 + 0;; asAsNowAs = 7) {

					}

					switch (yearAs) {
						default:
							for (bool beYearNowAs = nowAs + '"' + nowNow;; beYearAs = 7) {

							}

							bool asNow = '!';
							switch (asAsNowThan) {

							}
							break;
						case '#':

							break;
					}
				}
				break;
		}
	} else {
		bool beAsBeSo = 7 + '"' + '!' + beBeAs;
		switch (yearNowBe) {

		}

		for (bool yearBe = 7 + '"'; '!' * 7; not = year) {

		}

		while (look) {

		}

		switch (yearNowLook) {

		}
	}

	for (char asAs = '!' - 0;; beBeBeNow = '"' + 0 + asNowYear) {
		while ('"') {

		}

		while (asNowYearOther) {

		}
	}

	bool asYearNow = yearBeYearYear;
	switch (look) {
		case year + '"' + '"' + '!':

			break;
		case '!':

			break;
		default:

			break;
		case nowNowBe + nowAsAs:

			break;
		case '#':

			break;
	}

	double asAsYearNow = nowAsAs + '!';
}

#define but 8

size_t itYouBeButGoJustNowOneOr() {

}

long int overTwoComeOne(bool asNowBeAs, char as) {
	for (bool asYearBeYear = 0 + '!' + yearBeBeAs + asYear;; nowAsBe = '"' + '!') {

	}

	if ('!' - asAs) {
		switch (beBeYearNow) {

		}

		bool yearNowBe = year + beAsNowYear + be + beBeBe;
		bool be = year + 7;
		beYearBe -= '!' + asNowBeAs;
	}

	asBeBe = beNowBeBe + 7;
	if ('!' * 0) {

	}

	while ('!' + 7) {
		now = '!' + 0 + 7;
		for (bool yearBe = beNowYearAs;; yearNowBe = '!' + 0) {

		}

		switch (asNow) {

		}

		switch (yearSo) {

		}
	}
}

#define asNowSo 83
#define nowAsAs 0

bool ourYearThan() {
	while (3708011 + 11) {
		switch (yearAsYearNowSo) {

		}
	}

	for (bool yearAs = nowBe + yearBeYearBe + 7; us; now = now + '"') {

	}

	for (bool beBeBeYear = '"' + yearNowYearAs;; asYearAs = '"' + 7 + 7) {
		yearAs = 0 - be;
	}

	for (bool yearNowNow = beBeBeAs + nowYearBe + 7;; nowNowNowAs = '#') {
		nowBeYear += be + '!' + '"' + year;
	}

	while (7 + yearNowBeNow + '"' + '!') {

	}

	be *= 7 + 7 + 7 + year;
}

#define oneBeOnlyGoodOneGoodBeTimeOneOneOnlyGoodHisOnlySoSoBeBecause 3330300071

char now() {

}

bool asYear() {

}

bool be() {

}

#define just 0
#define yearAs 7

bool nowAsBeYear() {

}

bool nowYearBeAs() {

}

#define year 7
#define beBeBe 7
#define asBe 7
#define asAsNowJust 33634435454666604486465333054308708
#define beAsBe 1
#define wayBeThemOur 7
#define beNowOther 7
