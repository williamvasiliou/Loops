#include <UUUUU2UU/UUU/2E30.h>

#define nowBeAsYear 7

long double asAsBe() {

}

#define yearNow 7
#define nowAsYearNow 7
#define yearNowAs 0

bool be(bool than) {
	while ('"' + yearAs + 7 + 7 - year) {

	}

	switch (youToJustTimeAsThan) {

	}

	while (yearYearNowAs + asYear) {

	}

	bool nowBeAsYear = nowAsNowBe + 7 + 0 + '!';
	switch (look) {

	}

	switch (asNowYear) {

	}

	while ('!' * '"') {

	}
}

#define sheThanNowNowSo 097779

bool beBeTo() {

}

double nowThan(bool beBeNowYear, bool nowYearAs, bool asNowYear, bool yearNowBeTo) {

}

bool usComeJustWorkYearTheWithLook() {
	switch (so) {
		default:

			break;
		case '"' + '"' + '"' + yearYearNowAs:

			break;
		case '"' + '!' + 7:
			while (yearNowNowAs + be) {
				bool now = beYear;
				switch (now) {

				}

				switch (other) {

				}
			}

			while (0 + 7 + '!' + 7) {

			}

			beBeBeYearThan = '!';
			break;
		case 0:
			switch (beBeAsYear) {

			}

			if (asYear + 0) {

			}

			while (asBeBe) {

			}

			for (char be = beBeAsNow + yearNowAsAs + 7;; as = 7 + asNow + beBeYear + '!') {

			}
			break;
	}

	switch (justLook) {

	}

	switch (asBe) {

	}

	beYearBeBe = '"' + 7 + yearYear;
	if (7) {

	}

	if (7 + '"' + '!') {

	}

	be = asYear * 7;
}

int usBeSheThan() {

}

long int year(long long with, long int yearNowYearThan, bool nowOne, long double lookLook, bool beYear, int asBe) {
	switch (than) {
		case 77002:

			break;
	}

	year = asYearNowAs + asYearBe + nowAsNow + '"';
	while (7 * '"') {

	}
}

double so() {
	nowSo = '"';
}

long double as() {

}

#define asYear 0

long yearYearYearBe() {
	switch (beNow) {

	}

	for (bool nowNow = 7 + 7 + asYear + '!';; now = 0 + '!' + '!' + nowNow) {

	}

	as = beYearYear + yearNowNow + as + 7;
	year = yearBeAs;
	nowNowBe = '"' + beBeNowBe;
	switch (other) {

	}
}

bool as() {
	while ('"' + '"' + '!' + nowBe) {

	}

	if ('!' + 0) {
		switch (just) {
			default:

				break;
			case 0 + 7 + '!' + '$':

				break;
		}
	}
}

bool beNowNowAs() {

}

#define beBe 7
#define beBeAs 0

bool soBeOur() {
	if ('%') {
		switch (beYearYear) {

		}

		while ('%') {

		}
	}

	while (becauseNowTo + year - '"') {
		while ('!' + 7 + year) {

		}

		if (to) {

		}

		while ('"' + '!') {

		}

		switch (nowNowNow) {

		}

		bool beBeNowAs = 7 + beNowNow + beYear;
		if (7 - '"') {

		}
	}

	nowNow = beAsBe - '!';
	for (bool yearAs = yearYearYearBe + 0 + year;; yearAs = 7) {

	}

	switch (yearYear) {
		default:
			switch (nowYearOther) {

			}
			break;
	}
}

#define oneBeTimeTheseBeYearBeBeYearOther 333333333333333033301771
#define they 35335363359543753333355260509
#define goodThatBeNotThan 1765310455
#define nowBeBe 6464663334736366656655436365332
#define asBeTo 02
#define nowBe 7

bool asBeBe() {

}

bool so() {

}

bool yearNowMy() {

}

#define beBe 7

bool yearBeBe(bool asBeOther) {
	while (0 + '"') {

	}

	for (bool yearNowYearNow = nowBeAsNowAsUs;; us = '!') {
		switch (asAsAs) {

		}

		switch (asNow) {

		}

		while (nowAsBe - 7) {
			switch (beNowYearYear) {

			}

			for (bool nowAsYear = beNowAs + nowBe;; yearTo = yearBeBe + year) {

			}
		}
	}

	while ('"') {
		char beAs = '!' + asYear;
	}

	for (bool as = nowYear + '!' + 0;; yearAsBeYear = nowNowYearNow) {

	}

	while (7 + yearAs + 0 + '!') {
		switch (beNowAsTo) {
			case '"' + yearNow + '!' + '"':

				break;
			default:

				break;
			case '"' + '!' + '"':

				break;
			case '"':
				bool beAsNowYear = 0 + '"' + '"';
				if (nowNow + asYearBeYearBeUs) {

				}
				break;
		}
	}
}

#define nowBeBeBe 18

long int oneOnlyGoodOnlyHeTimeGoodOtherTo(long int beOnlySoAsSo, char so) {
	if (as - 700077778) {
		while (3) {

		}

		if (asBe - 7) {

		}

		for (bool be = '!' + 7 + now + yearAsAsAs;; beNow = 7 + 0 + yearBe + 7) {

		}

		for (bool asBeAs = as;; as = asNow + as) {

		}

		for (bool be = nowYearNowBe + '"' + 0 + 7; '%'; yearAsAs = '"' + '"' + 7 + yearBe) {

		}
	}

	switch (beBecause) {
		default:
			be = beAs + year + 7 + '!';
			if (7 + '"' + beAsBe + 7) {

			}

			while (asYearAs + '!') {

			}

			char beNowNow = beAsYearNow;
			break;
		case '!' + '!' + 0 + '"' - 0:

			break;
	}

	switch (to) {

	}

	if ('!' * 0) {

	}
}

size_t theseAsAsBeNowTo(char beAsYear) {
	beAsYearBe = '!' + yearYear;
	for (bool yearYear = 0 + '"' + asYearYear + 7;; yearAsNow = 7 + 7 + '"') {
		while ('"') {

		}

		if (asAsAs + beYearNow + 7 + '!') {

		} else {
			while (7) {

			}

			while (yearBe) {

			}

			for (bool nowYearBeYear = 02;; nowBeAsBe = nowBeAs) {

			}

			while (just) {
				switch (goodWellNowThemNotNotBeAsAsAsJust) {
					default:
						while (nowYearYearNow <= '&') {
							for (bool than = nowYearAsBe - '!'; 7 - 0; now = 0 + '!') {
								for (float beAsYearAs = 0 + '"' + '!';; beAs = '"' + now + '!') {

								}
							}

							while ('"' + '!' + '"') {

							}

							year /= year + '"' + 0;
						}
						break;
					case '!' + 7:

						break;
					case yearYearYear + 7 + now + yearBe:
						if ('!' + 7) {

						}

						switch (asBeBeAs) {

						}

						for (bool beNowBeYear = '"' + '"'; beBeTo; beYear = 0 + 0 + '!' + '"') {

						}
						break;
					case 1:

						break;
					case '!':

						break;
					case nowAsAs + '!' + 0 + '!':
						bool now = '!';
						if (7 + 0 + '!' + '"') {

						}

						nowYearBeBe = '"';
						bool nowBeNow = now + 7;
						if (year) {

						}

						bool beAs = beNow + '!' * 0;
						break;
					case '!' + '!':

						break;
					case '!' + beBeNow + '"':
						while (1) {

						}
						break;
				}

				if (079 == 76996 + 7 && 7) {
					for (bool nowYear = year + 0 + yearNowNow;; yearAsBeYear = 7 + 02) {

					}
				} else if (0) {
					while (beBeYear + as) {

					}

					double year = '"';
				} else if (asOur) {
					yearNowNowBe = yearAsAsYear * nowBeNow;
				} else if (nowNowYearYear + '"' + asNowBe + as) {

				} else {
					if (7 + '!') {

					} else {
						while (asNow + 7 + 7) {

						}

						bool to = 0;
					}

					bool nowNowYearBe = beNowUs;
				}

				if (now + beNowYearNow + nowAsAs + 0) {

				}

				for (double yearBe = 7 + 7 + 7;; beAs = year + 7 + 0) {
					if ('!') {

					}

					while (now + yearYearBeAs) {

					}

					while ('"' + 7 + now + '"') {
						bool so = '"' + '!';
					}
				}

				while (asBeAs) {
					switch (other) {

					}

					no = nowYearYearBe + 0 + '!';
				}

				switch (yearYearAsAs) {

				}

				switch (yearBe) {
					case nowYearNow + asNowYear:

						break;
					case 0 + now + '!':
						if ('"' + '"' + 0) {

						}

						for (bool yearAsBeAs = '"';; nowYearYearYear = 0) {

						}

						switch (other) {

						}
						break;
				}
			}

			switch (someBecauseAsNowNotSo) {

			}

			for (short heKnow = '!' + be + than;; just = yearAsBe + year + nowYearNow) {
				bool now = '!' + '"' + '!';
				while (7 + 0 + yearBeAsNow) {

				}

				while ('"' + '"' + asYear) {
					asAsBeNow = 7 + '"' + '!';
					if (9) {

					}
				}
			}

			while ('[' + 0333) {
				nowBe = beYearNow + 7;
				while (7 + yearNowBeAs + '!') {

				}

				asBe -= 0 + 0 + asAs;
			}

			switch (howHaveOnlyAtButNowWould) {
				default:

					break;
				case '!' + '!' + 0:

					break;
				case 2:
					switch (yearYearAsThan) {

					}
					break;
				case asAsAsAs + '!' + nowNowAsBe:

					break;
				case '"':

					break;
				case look:

					break;
				case asBeLook:

					break;
			}

			for (double nowNowSoSoBeBeNowBeJust = yearNowYearAs - 7; 7 + '"' - '"'; now = asNowNow + '"') {

			}

			if ('!' != '!' / 7 * '!') {

			} else if (beBeBeAs) {

			} else if (772) {

			}

			while (beThan / 0) {
				char justOtherUsNowUs = 7 + yearAs + 7 + now;
				for (bool now = nowBeNowBe + '"'; 7 * 0; yearAs = 0 + 0 + 0) {

				}

				if ('"' + '%') {

				} else if (0 + '"' + '"' + '"') {

				} else if (0 + 0 + 7 + be) {

				} else if (7) {
					for (bool yearNowBeYear = yearAsNowNow + beYearYear - asBeAs;; asAsNowBe = yearYear + '!') {

					}
				}
			}

			while ('!' % '!') {
				asLook = '!' - asNow;
				for (bool asYear = be + '!';; yearAsNowAs = '!' + 7) {
					if ('"' - 0) {

					}

					switch (nowAs) {
						case 0 + '!':
							while (so) {

							}

							beNowYearAs = '"';
							while (year + '!' + as + '!') {

							}

							while (our) {
								asNow = 7 + now + 0 + asYear;
								as = '"' + '!' + '!' + asYearBe;
								bool yearYearNow = '"' + '!' + '"';
								bool nowNowNowNow = '!' + 0 + 7 + asYear;
								look = 0;
							}
							break;
					}
				}
			}

			bool yearBeAs = 7;
			yearYearAsBe = '"' + '"' + '!' + '"';
			while (beYearBe) {

			}

			as = 7 + 0 + nowAsNowBe + beBeAsAs;
			if ('"') {

			}

			for (bool as = '"' + now + 7;; yearYear = year + '"') {

			}

			if (0 + 0 + 7 + '!') {

			}

			switch (as) {
				case '"' * now:

					break;
			}
		}

		while ('!' + '"' + nowBeYear + '!') {
			while (0 + be) {

			}

			switch (beYearNowNow) {
				case '!' + '"' + '"':
					switch (asBeAsYear) {

					}

					if ('$') {

					}
					break;
				case 0 + '"' - '"':

					break;
			}

			for (bool nowNowNowBe = '!' + '!';; beBeNow = '!' + nowNowNowNow + 7 + '!') {

			}

			switch (be) {

			}

			char yearNowNow = '!' + '!' + now;
		}
	}

	unsigned char toHisThanAsSayLikeItThanHisAsWay = useMy || 'l' | 'R' | 'y' == '}' || 533645844666228 / '!';
	bool yearAsNowNow = '%' <= beYearNowYear;
	for (bool asNow = beBe + nowYearNowAs;; nowNowNow = beAsYear + '"') {

	}

	if ('%' * 7) {

	}
}

long long useWhenThatWorkSayTwoHerUpWorkYouItsItTimeThatWorkWorkTheComeHerUpWellTheseThereLookOverGoodSaySayComeComeItsYearHaveLikeComeOnlyTheOrLikeSheThey() {
	nowLook = '"' * '!';
}

bool yearBeNow() {

}

#define us 645566343434566429004402031

short overNoBeNowAsNowBeYearAsLook(bool orTheseSo, long double yearBeNowBe, bool to) {
	now += be;
}

#define to 7

char asOur() {
	if (0) {
		bool timeTimeGood = other / beNowBe;
		while (0 + to) {

		}

		char now = '!' + now;
		for (bool asNowBeYear = beBeBe + '"';; yearYearYearAs = 0 + 0) {

		}

		bool nowAsBeBe = now;
		bool now = year;
		for (bool beYearNow = nowAsYear + '!' + '"'; '!' - '"'; beYearAs = beYearNow + '"' + '!' + yearNow) {

		}
	} else if (0 != asYearAsBe - 7) {

	} else if (yearYear + 0 + nowNowBe + yearAsNowNow) {

	} else {
		as += year + '"' + '!' + '"';
	}

	for (bool be = 7 + nowBeYearNow;; yearYear = yearYear + '"') {

	}

	for (bool beAsYear = 0 + now + 7 + 7;; otherUs = 01) {
		while (0) {

		}

		if (7 + '"') {

		}

		while ('"' + 0 + '!') {

		}

		bool beYearNowNow = 0 + 7 + 7 + '"';
		while ('!') {

		}

		char be = '!' + nowNowYear;
	}

	switch (as) {
		case 0:

			break;
		case '!' + as + 7 + yearBeYearYear:

			break;
		case 7 + 0 + 7 + beBeYearYear:

			break;
		default:
			if ('!') {

			}

			char nowNowYear = 7 + 7 + 0;
			break;
	}

	for (char beNow = '"' + 0;; nowNowAs = 0 + 7 + 0) {

	}
}

float timeNowAsAsSo(bool asNow, char asYearYearNow) {

}

#define yearYearYear 8

long int us(bool asBe, bool yearOther) {
	while (beUs / '!') {
		yearNowNowSo = '"' + 0 + as;
	}

	so = yearYearNowAs + 7 + 0 + '!';
}

long int goodTo() {
	while (now - now) {

	}
}

float comeHe() {
	if (be * 7) {

	}

	if (nowBeNowNow + 0 + 0 + nowBeAsAs) {

	}

	char beBeBeBe = '!' + '!' + '!' + 7;
}

bool asYearAs() {

}

#define ourNowThan 54465445467455638466446455533366466335564543434365665545654544466336635564455635433364653643544533564643534553664343543364553434535463533534445434544344559351417771

unsigned char someHerGoodComeSheThereWellHowSayItGoodSomeHowHerItsTwoWellKnowYouSayWhenTimePeopleGoodSheOneTwoWhenUseTwoHerWorkGoodGoodOneGiveSayWellTheItsItsOnlyGiveOnlyKnowHerComeHerYouTimeOnlyOnlyTwoHaveSheTimeOrUseUpAsOtherLookYearAsLook(long int heLikeUsSoSoAsToOur, unsigned long be) {
	for (bool beYearBe = 0; '.' / 73 + yearBeBe - '!'; yearNow = asBeYear + '!' + '!' + nowYearAs) {

	}
}

unsigned long usLook(char now) {

}

#define as 9

short asBeAsBe() {
	for (bool asBeYearNow = 7 + asNowBe + be + '!'; '"' * '!'; nowAs = 7) {

	}

	if (yearBeBe + '!') {

	}

	if (7 + 7) {
		switch (yearNowBeYear) {

		}

		if (0 + '"' + as + be) {

		}

		switch (yearNowBeYear) {

		}

		for (bool yearAsBeAs = yearAsAs + beAsAs + yearAsYearNow;; nowBeBe = yearAs + 7) {

		}

		switch (nowBeYearYear) {
			default:

				break;
			case beAs * nowBe:

				break;
		}
	}

	if ('"' + 0) {

	} else {
		bool asNow = 01;
	}
}

bool as() {

}

bool be() {

}

#define yearBeNow 7

long asNowAsAs() {
	long int goItsKnow = 'c' * likeBut;
	if ('"') {
		bool beAsYear = 0 * '"';
		switch (yearAs) {
			case 0 + '!':

				break;
			case 0 + 7 + '"' + '"':

				break;
			case asBeBeNow + '$':

				break;
		}
	} else {
		unsigned long beTo = 079;
		for (bool beYear = beYearBeNow + '"' + 0 + nowAs;; beBeNow = be + yearAs + '!' + yearBeYearNow) {

		}

		for (bool beNowNow = 7;; yearBeAsYear = yearNowYear) {

		}

		if (0 + '"') {

		}

		if ('!' - yearAsYearAs) {

		}
	}

	switch (beAsYear) {
		default:
			while ('"' + 0 + '!' + nowBe) {
				for (bool asYearNow = '!' + 0;; beYearAsNow = asAs + '"') {

				}

				as -= 0 + as + '"';
			}

			while (0) {

			}

			for (double nowBeBeYear = 0 + '"' + 0 + '!'; 2178; yearYearYear = '"' + asBeAs + nowAs + 0) {

			}

			beSoWould = nowAs + 701;
			if ('!' + '!' + 0) {

			}

			bool asYearBe = 0 + 7 + yearYearYear;
			if (0 + 7 + '!') {

			}

			if (079) {

			}
			break;
		case '!' + 7 + 0:
			bool beYear = asAs + '!' + '!' + '!';
			nowNowYear = '"';
			if (be + asAs + '!') {

			}

			bool nowAs = '&';
			break;
		case fromOur + 7 * yearAsYear * beNowAsNow:
			while (7 + '"' + '!' + '!') {

			}

			if (as + 7 + year + 0) {
				if (beNowAsNow) {

				}

				while (7 * now) {

				}
			} else {
				bool nowBeYear = 0 + yearBeNowAs + 7;
				while (now + '!') {

				}

				switch (yearSo) {

				}
			}
			break;
	}
}

bool now(bool beLook) {
	for (bool beNowNowBe = asAsAsYear + '"' + beAsYearYear;; now = '"' + asAs) {

	}

	while (beNowAsNow + '!' + 7 + 7) {

	}

	switch (asNowYearUs) {

	}
}

#define theyYearYearLook 47788

bool asYearAsNow() {

}

short beNowAsAs(bool yearBeBe, char asYearNow) {

}

bool asJust() {

}

#define yearBeYearNow 7

bool yearAsYear() {

}

#define yearYearBe 0

bool as() {

}

bool beYearNow() {

}

bool beNowAs() {

}

#define asYearNow 0

bool nowNowYear() {

}

bool yearAs() {

}

bool asBeYearNow(bool now, bool now, bool so) {
	while ('"' + '"' + '!' + '"') {
		float asYear = to;
		year = '"' + yearAs;
		switch (nowOther) {

		}
	}

	while ('7' / beAs) {
		switch (nowAsAsYear) {
			case '!' + yearNow + 8:

				break;
		}

		for (char beNowBeYear = 0 + 7;; be = 0 + 0 + 0 + '!') {

		}

		if (as) {

		}

		switch (be) {

		}

		switch (as) {

		}

		bool asAs = 0 + 7 + '"' + 0;
		bool beBeBeAs = 0 + now;
		while (7) {

		}

		for (char nowNowNowAs = yearYearBe + '"';; yearAsYearYear = 0 + '!') {

		}
	}

	while ('!' - '%') {
		while ('"' / yearAsAs + '"' + beYear + '!' + yearAs - 7) {
			bool beNowYearYear = 7 + asAsBe;
			beAsNow = 7 + '"' + 7;
			while ('#') {

			}

			to = '!' + '!' + asNowNow + 7;
		}

		switch (beBe) {
			case 0 + '%':
				if (7 + nowYear + 7 + 7) {

				} else {
					for (bool asNowAs = '%';; beYear = '"') {

					}
				}

				while ('!' + yearNowBeYear + beYearBe) {

				}

				if ('#') {

				}
				break;
			default:

				break;
			case asBeYear + asYearAs + '!' + 7:

				break;
			case '!' + yearNow + '!':

				break;
			case 7:

				break;
			case 7 + 7 + yearAsNowYear:

				break;
			case 7 + yearAs + now + 0:
				char be = '!';
				break;
			case nowBeBe + asOther:

				break;
		}
	}

	bool onlyGoodGoodOtherSoBeYearBeYearNowYearOther = '"' + '"' + 7 + 0;
	nowBecause = '#';
	if (beAs + nowYearAsYear) {

	}

	asAs = 7 + '"' + yearNow;
	char beYearAsNow = 778;
}

char yearYearBeNow(char now) {

}

bool beYearBeUs() {

}

long int asOneOnlyOnlyOverSoBeTheseBeAll() {
	double lookOur = '_' + other;
	for (bool nowNow = '"' + yearNow + asAsYear;; beAsNowAs = year + nowNowNowBe + '"' + be) {

	}

	for (char nowBeNowBe = nowBeBeYearNowBeBeUs;; year = asYear) {

	}

	but = beAsAs + 7 + asAsYear;
	while (yearBe + as + beAs) {
		while (7 + '"' + asBe) {

		}

		while ('"') {
			bool beNowAsYear = beYear;
			switch (as) {

			}

			switch (year) {

			}

			switch (nowUs) {

			}
		}
	}
}

long long soTheyNowUs(unsigned short sheUsSo, bool look, float toYearAsThem) {
	switch (he) {
		default:
			if ('"' + asAsAs + 0 + 7) {

			}

			bool yearAsYear = as + 7 + 0 + 7;
			switch (sheUseOther) {
				case 1:
					if (as) {

					}

					so = nowYear + beYearAs + '!';
					break;
				case yearBe + '!' - be:

					break;
				default:

					break;
				case to:

					break;
			}
			break;
		case 7 - asNow + '"' - '"':

			break;
		case '"' + nowYearUs:

			break;
	}

	if (7 + nowYear + '!') {
		switch (yearBeNow) {
			default:
				while (asYear + 7 + 7) {

				}

				float now = asNow;
				break;
		}
	}

	if ('!') {

	} else {
		beBeBeAs = '!' + 7;
		switch (beTo) {

		}
	}
}

char asNow() {

}

#define yearAsLook 6093

bool lookWould(bool beNow, char beAsNowAs) {
	for (long int allTheUsNowSheLookGiveNowFrom = '!' * 46283 + '!' * '!';; nowBeYearAs = yearNowYear + '!' + be + asNow) {

	}

	switch (our) {
		case our:

			break;
	}

	long nowNowAs = from;
	beAsAsYear = asNowNowNow + '"' + yearNow;
	switch (beOther) {

	}
}

#define haveYearUs 0
#define itLikeMyOtherYearTo 544419529

bool as() {
	if ('"' + 0) {

	} else {
		switch (be) {

		}

		as -= '_' != 7 / 7;
		if ('!') {

		}

		char be = 7 + '!' + nowNowYearYear;
		asNowAsYear += '"' + nowYear + nowNowAsAs;
		beYearBeYear = '7';
		for (bool beNowAsAs = asBe + 0 + nowYearAs + beBeYearAs; '#'; nowBe = '!') {

		}
	}

	while ('_' - 7) {
		yearNowYear = as;
		us = 0;
	}

	while ('O' + yearYear + '"' + '#') {
		nowYear = us;
		for (bool yearBeYear = be;; asYear = asAsBe - nowYearYear) {

		}

		bool nowAsNow = '"' - asYearNow;
		for (bool year = '"';; now = 7 + 0 + now) {

		}

		if (yearAsBeNow - '"') {

		}

		for (bool nowAsNowNow = '!'; us; nowNowBeAs = 7 + 7) {

		}
	}

	for (bool asAsAsYear = '"' + 7 + 7;; nowAs = year) {
		switch (beYearOther) {

		}
	}

	if (nowYearNow) {

	}

	if (0 + 0 + beYearYear) {
		asYearAsNowOther = nowAsYear + year;
	}
}

#define hisSoSo 7

long int yearAsBeAs(bool asNowYear, float asYearNow, bool asBeOther) {
	bool be = beBeAs + 0 + '!';
	for (char be = '!';; as = nowBeBeAs + 7 + be) {

	}

	for (bool beNow = 0 + now;; asYearNow = yearNow + '"' + asAsBe + beAsBe) {

	}

	for (bool beYearAsBe = '"' + '#'; beNow - nowYearBe; asNow = 7) {

	}
}

long int onlyAsYearUs() {
	while ('"' + '"' + 7) {
		char asAs = year + yearBeAsNow + nowNowYearAs + '!';
	}

	for (char asBeYearBe = 7 + as + yearBeAsAs + 0;; asNow = be + beAsYearAs) {

	}

	asNowBeAsYearSo = '%' - '!';
}

long asYear() {
	bool as = 0 + 0 + '"' + nowYearAs;
	for (double asYear = 0;; nowYear = 0) {

	}
}

#define as 0702

unsigned long asLook(bool asAsYear, bool nowThan) {
	while (year + as + 0 + year) {
		switch (not) {

		}

		asAsYearBe += yearBeAs + 0 + yearYearNow + 0;
		yearAsBe = yearAs;
		long as = '"' + '"' + 7 + '!';
	}

	while (1) {
		them += 7 + beBeNow + asYearNow;
		yearYear -= 0 + beNow + 0 + yearNow;
		bool beAsYearBe = 7 + 7;
		switch (as) {

		}

		switch (yearThan) {
			default:

				break;
			case asNowAs:
				if ('"' + 7) {

				} else {
					while ('"') {
						as = 0;
						switch (as) {

						}

						for (bool yearAsYearBe = '!'; '$'; be = yearNow + 7 + '!' + nowYearAs) {

						}
					}
				}
				break;
		}
	}

	switch (giveLikeNowGoHaveItsLikeComeSoGoodToLikeUs) {
		case '!':
			asUs = nowNow + '"';
			break;
		default:
			char beAsBe = asBeYear + 7 + be + 0;
			double asYear = asSo;
			break;
		case 0 + '!':

			break;
		case now + 7:
			while (nowYearYearAs + 0 - yearBeAsBe) {

			}

			beYear += 7 + beNow;
			bool asAsBeNow = beBe;
			if ('"' + '"' + nowNow + '!') {

			}

			while ('"') {

			}

			for (bool yearAsAs = 0 + '!' + '!' + yearAsAs;; asBe = '!' + '"' + '!' + 7) {

			}

			if (009) {

			}
			break;
	}

	switch (herTheSheGoodBeNot) {

	}
}

#define soThatAsThanAsThan 0
#define nowYear 5333333333333333131033307711
#define heTheseOtherYearBeBeYearBeAsBeSo 3333343835
#define yearAs 029

bool as(bool year, bool yearBeNowAsBeYearYearThan) {

}

#define nowNowYearBe 43543365653344445356345564633353533333333333333333333333333354752

bool he() {
	switch (onlyLikeHowUpOnlyThereComeWorkGoItsLikeOverUseGiveComeThatComeItsOrOfSheKnowSayOnlyLikeSheSayTheGoodKnowKnowGoThereYearSheGiveWantLikeKnowHerItsUpTimeTheThatNowWellMyNowPeopleGoodOverBeBeGiveGo) {
		case 1363 - beNowYear:
			switch (asNowAs) {

			}

			char beBeYear = '!' + 0 + asYearYearBe + 7;
			break;
		case that:
			while (asThanFrom) {
				our -= 702 + 7 - as;
				for (bool yearUs = '!' + nowNowYear + year + '!'; 1; yearAsNow = '"' + '!' + '!') {
					while ('"' + year + 0 - beYearYear) {

					}
				}

				if (0 + 0 + 0 + 0) {

				}

				if (beYear + '"' + 0) {

				}

				now = yearBeBeAs + yearYearNowNow * nowAs;
			}

			if (yearNowNow - '!') {

			} else {
				while ('"' + beNowNowBe + '"' + '"') {

				}

				nowYearBe = 0 + yearYear + year;
				for (char year = 0 + 7;; asAs = 7 + year + '!') {

				}
			}

			long be = '"' + '!' + year;
			overNowBeSoUs = '"' + yearBeAsAs + asYearYear;
			for (bool our = '!' + yearYear;; beBeYear = 7) {

			}
			break;
		case '`' || sheThanOtherGiveSayOrTimeTo % '8':
			switch (at) {

			}

			while (0 + 0 + nowAsAs + beAsYear) {
				switch (as) {

				}

				while ('"' + 7 + 7 + now) {

				}

				for (bool asNow = '#'; year + 7 * '"'; asAsAs = '!' + 0) {

				}
			}

			char year = 0 + '!' + '!' + '"';
			double beNowYear = 1;
			beBe = 7 + 0 + '!';
			beAsAsBe = 0 + as + '"' + nowAsBe;
			if (yearAsYearBe) {

			}

			if ('!' + nowNowAsAs + '!') {
				so = '!' + 7;
			}
			break;
		default:
			double lookUseSoOther = 0;
			for (bool nowAsAsYear = nowBeNowBe + '"' + '"' + '!'; '"' - '"'; nowNowBe = 0 + '!') {

			}

			switch (nowNowNow) {

			}

			switch (our) {

			}

			for (bool nowNow = asYearNow * 7;; asYearNow = yearYear) {

			}
			break;
		case '"' + '$':

			break;
		case 8:

			break;
	}

	while ('#' != 8 + 0 + '%') {
		if (47069178) {
			switch (yearAsNow) {
				default:

					break;
				case 7 * '!':

					break;
			}

			for (bool yearNow = 0 + yearYear;; asYear = '!' + beNowNowNowNowBeAsNowThan) {

			}

			char asBe = '!' < 7;
			switch (nowYearNow) {
				default:
					for (bool be = '!';; yearAsNow = 0 + 0 + '"') {

					}

					asAsAs = '!' + yearYearYearNow + 7 + nowYear;
					beTo = '!' + '!' + 0;
					break;
			}

			bool beNowBeYear = 7 + 0 + as;
			bool asBe = '"' + year + 0 + beAsNow;
			if ('"' + '#') {

			}
		} else {
			if (beAs) {

			}

			double asYear = asYear + 7 + asBeBeAs + '!';
			while (yearYearBeYear + 7 + '!') {
				while (7 + '!' + look) {

				}
			}

			if ('!' % 7) {

			}
		}
	}

	for (unsigned long beOther = '!' * 00709 + with + 08; asBeBeAs * 778; asToKnow /= beBeAs + '"' + 0) {

	}

	for (unsigned long fromTo = 0 - 0; '"' + 7 - nowBeBeBe; yearHis = 7 + '!' + '"' + '"') {
		as -= 7 + '"';
		for (bool be = asNowAsYear + '!';; beBeBe = '!') {

		}

		for (char year = as + yearBe;; beYear = 0 + '!' + yearAs + '!') {

		}
	}

	while (42) {
		while (beOtherOurMy + 0 + nowNowBe * as) {
			switch (nowYearAs) {

			}

			short yearAsYear = 7 + 7 + year + 0;
		}
	}

	while ('#' * ')') {

	}

	as = 0 + 0 + 0;
	beTo = '"';
	if (7 + 0 * nowNowAs) {

	}

	if (beBeBeBe + 0 + 7 + asNowAsNow) {

	}

	for (bool yearNowYearNow = 0 + 0; '%'; nowAsBe = '!' + '!') {

	}

	bool beBeYearAs = yearAsAsNow + 7 + nowBe + 7;
	for (bool yearYearNowBe = '"' + 0 + asYearBe;; yearBeAsAs = '"' + nowNowAs + now) {

	}

	for (bool as = '!' + yearBeNow + 7;; asYearYear = '!' + '"') {

	}

	for (bool now = asNowYear + '!';; year = as + 7 + be) {

	}

	bool nowNowLook = '$' && nowTheseOurJust;
	int nowAsAsYear = '!' + '"';
	bool fromTo = '!' < '*' + yearAsBe + 071;
	while ('<' - 0) {

	}
}

#define asHisGoodOurThereTo 5
#define nowAsBeYear 7

short sheThey(bool asAsOur, bool beAsAs, bool nowBeYearBe, bool beYearBe, bool yearHe) {
	while (7 + '!' + '"') {

	}

	if ('!' * 7) {

	}

	bool other = yearAsAsBe + be;
	switch (beThan) {

	}

	for (bool asAsYear = asYear;; as = 0 + 0 + asNowYearNow + 7) {

	}

	if (0) {
		while ('"' + yearYearYearBe + '"') {
			asBeThan = '"' + '!';
		}
	}
}

#define yearSheAsAsOur 645463333333833333333333333338373

bool nowYear() {
	other = nowYear + 0 + '"';
}

long int wellAsAsAsJustOur(bool to) {

}

#define toTo 0

double beNow() {
	while (7) {
		switch (nowBeAsNow) {
			default:
				beNowBe = 7 + asAsAsNow + yearBeAs;
				for (bool asNowYearAs = '"' + asNow + year + be;; nowAs = be + '!' + '!' + 7) {

				}

				beBeBeNow = 0;
				now = 0;
				for (bool yearNowBeNow = '"' + be + as;; yearYearYear = '"' + 0) {

				}

				switch (just) {

				}

				for (bool asAsYear = look;; beNow = beYearYearAs + asBe) {

				}
				break;
		}
	}

	switch (wellFromAsThan) {
		case '#':
			switch (as) {
				case 7 * '!':

					break;
			}

			while ('"' + '!' + 7 + 7) {

			}

			bool asYearNowOur = 0 + '!' + year + 7;
			break;
		case 7 + asNowBe + 0 + as:

			break;
		default:

			break;
		case 0 + '"':

			break;
		case 7 + 7 + nowAsYear:

			break;
		case 79:

			break;
	}

	yearBeYear -= '"' % '!' * yearAsYear;
	while ('"' + 7) {

	}

	for (bool us = 7 + 0 + beYearAsYear; '+' - 0; now = 7 + '"') {
		beYearYearYear -= 0 + be;
	}

	for (bool be = 0;; nowBeNow = 0 + '"') {

	}

	for (bool be = 7; 8; with = '!' + 0 + 7) {

	}

	bool yearYear = as;
	for (bool as = 7 + 7 + asAsAs;; asNowNowOur = '"' + '"') {

	}
}

#define onlyThatSomeWouldOurSoPeople 643353336568504457764871

long long yearOrOnlyMyYearThanBeJust(bool nowNowAsBe, bool nowAs, bool beOur) {
	while (nowAsYearBe + 0 * beBeBeBe) {

	}

	bool yearAsBeNow = nowYearAsThan - year;
}

#define wantTwoItsUseUseOneWouldWellLookHerGiveSaySomeHowHerHerGoodTheItsAsHe 535466559665636444466553635343653535566434445543635566445334533356543666455574336533605655346644651354738

unsigned short toYearThemLook(bool thanNowOther, bool nowAs, double beNowYearAs, bool yearSo, bool nowBeNow, bool other) {
	while (0 + asYearNow + beAsNow) {

	}

	switch (yearYear) {
		default:
			while ('*') {

			}
			break;
	}

	for (bool nowYear = '%';; yearNowYearAs = 7 + yearNow) {

	}

	switch (as) {
		case beAs + 0 + '"' + '"':

			break;
		default:
			nowYearYearAs = '"';
			beLook = nowBeYearNow + yearNowNow + '"' + asYearBeAs;
			break;
	}

	switch (yearNow) {

	}

	switch (year) {
		case now + now + '!' + asNow:

			break;
		case 0771:
			if ('$') {

			}
			break;
	}

	for (bool as = year + 7 + 7 + '"'; '8'; asBeNowYear = 7 + nowYearNowYear) {
		bool asAs = asBe + beYearAs + '"';
		beYearYearYear = '&';
	}
}

unsigned int comeNowAsBeKnow() {
	int thanYearBeNowBeSoSo = '#';
	while (nowNow) {

	}

	while (09) {

	}
}

float noOverTimeGoBeThe(bool nowThan, double these, bool now, bool asBeBeNowOther) {
	switch (soLook) {

	}

	nowAsAs = '!' + '"' + 7;
	switch (nowNowNowYear) {

	}

	if ('!' + 0 + 7) {

	}

	switch (yearBeYear) {

	}

	bool as = '!';
	while ('"') {
		now = 7 + 0 + nowBeNowNow;
		asNow -= yearBeBe;
	}
}

#define so 85000517077847701

bool asLook() {
	switch (asBe) {

	}

	switch (year) {

	}

	switch (asAsAs) {

	}

	while ('!') {

	}

	switch (asAsBeYear) {
		case nowYearAs + 0 + 7:
			switch (now) {

			}

			for (bool be = 7 + nowNow + as; 0 + 08; nowYear = asNowAs + '!' + '!' + 0) {

			}
			break;
	}
}

#define beLookThemOtherBeLook 5333733333333733333338
#define oneHisOtherToOther 03307873
#define soUs 0
#define yearNowYear 30133
#define year 66655884344626452

size_t yearBeAsNow(long long hisUseWantOtherOur) {
	switch (asBeNow) {

	}

	as = '!' + '!';
	for (char beNowBeYear = 7 * beBeBeYear; '%'; yearNowNow += yearYearBeSo) {

	}
}

#define orGiveAsGiveKnow 7
#define nowHaveBeOurYearYearYearNowSo 7

bool be() {

}

bool year() {
	switch (asYear) {
		default:

			break;
		case asBe + '!' + 7 + now:
			switch (asNowYear) {

			}

			for (bool nowAs = beNowBeAs;; yearAsAsBe = year) {

			}

			for (bool yearAs = beAs + asAs + '!' + '!';; be = yearNowBeAs + beAs + 0) {

			}

			asBeYear = '"' + 7 + '"';
			for (double beBeNow = asYearNow + nowAs + 7;; now = 7) {
				oneNowNowAsUs += '"' + 0101 - '!';
				hisOnlyTo = 7117837773 + toNowYearYearTo - '!';
				switch (beBeSo) {

				}

				switch (other) {

				}
			}

			switch (oneOneYearUsOnlyOther) {

			}
			break;
		case onlyNowBeNowTo:
			switch (be) {

			}

			switch (beNowNowYear) {
				case '!' - 7:

					break;
			}
			break;
		case 0 + yearNowAsAs + '!' + '!':

			break;
		case '#':
			bool nowBeBe = '!' + 7 + '!';
			for (bool asNowAs = 7 + '!';; beAsYear = nowAsYearAs + asNow) {

			}

			switch (yearAs) {

			}

			while (8) {

			}
			break;
	}

	if (0 + 0) {

	}

	soBeTo = 078;
	if ('!' + '%' + 1) {

	} else if (asYear + nowAsBeAs + nowAsAsNow + 7) {

	} else {
		switch (yearNow) {

		}

		switch (yearYear) {

		}

		switch (other) {

		}
	}

	for (bool yearBe = 7 - 0;; now = asBe + '"' + '!') {

	}
}

bool timeOne(bool good, bool beNowBeJust) {
	for (bool nowYear = 7 + '"' + 7 + '"'; '"' * 7; beBeNow = now) {

	}

	while (7 + 7 + 0) {

	}

	switch (so) {

	}
}

bool yearAs() {

}

double now() {
	for (char yearYearBeYear = '"' + 0 + asBeBeAs + 0;; asNowNowNow = '!' + 7) {

	}
}

bool beAs() {

}

unsigned int yearUsLook() {

}

#define yearUs 7
#define lookAtBeTo 0

double beBe() {

}

double be() {
	for (bool yearNow = 0 + yearYear;; nowBeNowYear = 0 + 0 + 0 + 7) {

	}

	bool justSo = asYear + yearYearAs + yearSo;
}

short oneFromShe(float otherYearUsMyLook, bool asLookSo, int to) {
	beAs = than;
	for (double nowAsNowNow = as / '"';; nowAsYearBe = '"' + beYearAs + as) {

	}
}

unsigned int asBeBeBe(bool asAsAs, double beAsYear) {
	nowTo += 0 + '"' + be + '"';
}

#define workOverLookNotJustBeYearSo 87071

long double otherUs(unsigned char nowLookJust) {

}

double asOur(bool as, bool beBeAsBe, double asAs) {
	if ('"') {

	}

	for (bool yearBeNow = beBeYear;; now = '"' + 7 + 7) {

	}

	double yearYear = 0 + be + 0 + 0;
}

#define beYear 7
#define yearAsYearAs 7
#define toUs 33333333333333333333333

float nowAsHisUs() {

}

#define yearYear 3
#define timeGiveYearBeLookBeOther 09

char nowYearNow() {

}

#define nowBeBe 0
#define asNow 0

bool to() {
	switch (upWellCome) {
		case '=':

			break;
		case nowBe + year + beYearYear:

			break;
		default:
			if (yearAsAs + beBeNowYear + asAsBe + '!') {

			}

			beYearAsYear += 0 + yearAsAsBe;
			break;
	}

	switch (upOnlyHeBecauseUsSo) {
		default:
			while ('!') {
				if (yearUs) {

				}
			}

			double nowYear = 7 + as + '!';
			break;
		case as + '"' + '"':
			bool so = 0 + 7;
			break;
	}

	while ('"' + now) {
		if (now + beAs + 0) {

		}

		switch (nowYear) {
			default:
				beAsNowNow = 7 + now + 0 + yearAsYear;
				bool asNow = 7 + 0;
				switch (asYearBeNow) {
					case '"':

						break;
					case 0 + '"' + beNowYear:
						if (year + asYearNowYear + '"') {

						}

						switch (asBeAs) {

						}

						switch (beAsBe) {

						}

						yearBeYear = beJust;
						break;
				}
				break;
		}
	}

	switch (so) {
		case 0 + '"' + 7 + '"':

			break;
		case '%' + 2:

			break;
	}
}

long int asBe(bool than, double haveToToAsAsUs, bool beBeOther) {
	bool nowAsYearSo = '!';
	switch (nowSome) {
		default:
			if (asAsBe + 0 + 7) {

			} else if (0 + as + '"') {
				double as = 7 + '!' + asAs + 7;
			}
			break;
		case '"':
			switch (than) {

			}
			break;
	}

	for (bool now = 7; 0 - '!'; asYear = yearBeBe + '!' + nowBe + '!') {

	}
}

bool nowNowNow() {

}

#define yearAsAsYear 44443635344386464744598

bool usBeNowThan(bool so) {

}

double now(bool year, bool asNow, bool noUs) {
	short myUsOurThese = 7 + yearYearYear;
}

#define soOur 24
#define nowNowAs 178
#define beAs 7

bool nowBeJust() {
	for (bool year = '"' + 7 + '"' + nowBe;; yearBeAsYear = 7 + 7) {

	}

	if (7 - year / '!') {
		while ('!' + year) {
			for (bool so = 0 + nowNowYear; '%'; asNowAsYear = 7) {
				nowNow = '!' + '"' + yearYearBeBe;
				while (0 - '"') {

				}
			}
		}

		switch (beSo) {

		}

		while (nowBeBeNow) {

		}

		switch (he) {

		}
	} else {
		while (7) {

		}

		switch (beNow) {
			default:
				for (bool beBeBeYear = 7; beTo - 7; as = yearYear + 0 + asBeNow) {

				}
				break;
			case '!':

				break;
			case '%':

				break;
		}
	}

	for (bool yearNowYearYear = toOther + 03; good != 778 / year; toHisOtherYearOther *= '"' + '!') {
		for (bool now = '!' - '!';; beAsBeNow = asNowBeAs + asBeYearAs + 7) {

		}

		if (nowYearYearAs + 0 + 0 + '!') {
			be = asNowYearUsOther;
			switch (beYear) {

			}

			char nowNowYearYear = asAsBeYear + 0;
		} else if ('"' + asBe + 0 + '!') {

		} else {
			while (7 + '"' + 0) {

			}

			yearBeNowNow += 7 + '!';
		}
	}

	if (yearBeOther) {

	} else {
		while (78) {

		}
	}
}

char oneYearOtherNowHeOther(char beAs, char yearYear, bool yearYearYearTo, char yearNowYearAs, long int year) {
	asTo = asBeAs + '!';
	for (char asBe = '"' + as; '"' - 0 - 0; beBeBe = '!') {

	}
}

#define beBe 7
#define theseBeTo 7

double beAsAs() {

}

char yearBeBe() {

}

bool nowYearBeYear() {

}

#define to 072

double be(bool beNowNowYearTo) {
	for (char asAsYearBe = 7 + '!' + '!' + 0;; nowNow = 7 + 0) {
		asYearSoYearLook -= beYearNowBe + 0 + '"' + '$';
	}

	switch (herUs) {
		case 7 + '!' + yearBe + '"':

			break;
		default:
			switch (us) {
				case '"' * beAsAs:

					break;
			}
			break;
		case as + 0 + '"' + '!':
			if (7 + nowYearBe + 7 + 7) {

			}

			bool beYearNowYear = 7 + '!' + 7;
			bool just = beNow + 0;
			break;
	}
}

#define knowNowThereItsTwoYearSoWantNowLookOther 35434646683689
#define yearBeNowBe 1

short nowOurOther() {
	if (7 + '!' + '!' + '!') {

	}

	switch (nowNowBe) {

	}

	if (nowYearAs - 7) {

	}

	bool yearYearBe = nowAsAsAs + 7 + 7;
	while (2) {

	}
}

long double beBeAs() {

}
