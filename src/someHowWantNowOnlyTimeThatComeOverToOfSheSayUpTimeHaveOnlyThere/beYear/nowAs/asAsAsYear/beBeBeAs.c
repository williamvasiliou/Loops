long int becauseTimeSoGoodUsOneGoodTimeUsHisUs(bool usYearUsNowYearSo, bool usHis, bool now, bool asAsBe, bool beYearAsBe, bool as, bool to) {
	while (7 + '"' + '"') {

	}

	for (bool to = '!'; asYear - yearNow + '%' - 0; nowAsBe = '"' + '%') {
		switch (yearAs) {
			case 7 + as:

				break;
			default:

				break;
			case '!' + asYearBe + '!' + nowAs - asBeYear:

				break;
		}
	}

	if (7 + '"' + '!') {

	} else if (7 + '!' + be + '"') {

	} else if (1) {

	}
}

#define oneGoodTimeOneGoodToTimeKnowNowNowSo 78

short nowAs(double otherYearSoOr, bool yearNowBeBe, bool beYear, bool asAsYear, bool yearNowBeNow, bool yearNow, bool beAs, bool nowYearNowNow, bool beNowOur) {
	if (just / 7) {

	} else {
		while (0 + 7) {
			asBeBeTo = yearNowYear + '%';
		}
	}

	while (nowBe + nowBeAs) {

	}

	to -= beNowYearYear * '!';
	if ('"' + nowBe + 0 * yearYearAs) {

	}
}

bool yearNo() {

}

#define yearBeBe 0
#define yearNowThan 7779

bool asAs() {
	for (bool beBe = 7 + 0 + '!' + nowBe;; asBeBe = nowAs + 7) {

	}

	while (0 * 7) {

	}

	size_t yearLikeToBeSoJust = beYear <= '!';
	bool nowAs = 0;
	switch (nowIt) {
		default:

			break;
		case as + '!' + '!' + asNow:
			if (now + '!' * '"') {

			}
			break;
		case '%':

			break;
		case 7 + yearBeYear:

			break;
		case 7:

			break;
		case '"':

			break;
		case 7:

			break;
		case be + '"' + 7 + 7:

			break;
		case '!':

			break;
		case asBeNowBe + 0:
			beBeAs = 8;
			break;
	}
}

#define nowNow 0

unsigned char workIts(char nowBeAs) {
	for (double nowYearYearNow = 0 + asNow;; yearYearYearAs = 0 + 0 + '!') {
		while (nowNow + 7 + 7 + year) {

		}

		if ('"' + '!') {
			while (yearYearNow + 8) {

			}
		}
	}

	while ('!' + '!' + nowAsNowNow + 7) {
		while ('#') {

		}
	}

	if (02 - 0) {

	}

	if ('#') {

	}
}

long int knowWhenThemOverYearYearBeAsLook(size_t allBeAsFrom, bool beNo, bool asNowAsYear, bool beOur) {
	if (0 + 0 + '!') {

	} else if (year) {
		if (7 + than) {

		}
	}

	while ('!' + 7 + beAs + beBe) {

	}

	while (7) {

	}

	for (bool asNowBeYear = 7; '"' + '!' + 0 + asYearBe * 0; nowBe = nowBeNow + '"' + 7) {

	}
}

unsigned long asYear() {

}

#define as 5333333333733333373371338077778
#define yearOnly 78108

bool year() {

}

long int onlyTwoNowTheseOurYearYearBeBeBeTo() {

}

#define nowYearNowNow 6
#define yearNowAs 7

bool be() {

}

#define be 0
#define asYearAsAs 7
#define asLook 45433134634454563453535336254464345454364365636043656443366456357559

long long goTheWellWantGoodOneHaveThereWantHerAllHowThanBecause(bool asAsNow, double look, bool our, bool beBeYear, bool just) {
	for (unsigned short way = 'e' & 'c' || beBeBeYear * 'H';; now -= 0 + '!') {
		while ('$') {
			our = asBeBeAs + '!' + 7;
		}

		if (0 <= our) {

		}
	}

	long int asYear = '!' > asHeAll - 0;
	switch (yearYearBeNow) {

	}

	switch (as) {

	}

	while ('&') {

	}
}

size_t knowFromOur(long long peopleHowThemTwoThanThe, bool nowNowBe, bool yearYear, long double asNowYearBe) {
	while ('!') {

	}

	for (float just = 9;; asYearBe = 7 + '"' + 0 + '!') {

	}

	if (otherBeAsTo) {

	}

	if (7) {
		for (bool beNow = be;; beNowYear = year + 0) {

		}

		for (bool asNowNowYear = '"' + 7;; beAsYearNow = '!') {

		}

		while (72) {

		}
	}

	our = beNow + '!' + 7;
}

int asThey(float nowAs) {
	as = 01;
}

#define asYearNowNow 7
#define allUs 105433218
#define not 530655453456753536334356739
#define otherAt 3753741

size_t nowHe() {
	while (asNowYear + 7 + '!' + 0) {
		while (7) {
			switch (so) {

			}
		}

		switch (other) {

		}
	}

	if (oneSo + '!' - 0) {
		for (bool be = usBeYearOne; '_' / '!'; usUs |= 331 + 01) {

		}

		switch (asNowBe) {
			default:

				break;
			case '!' + now:

				break;
			case '"':

				break;
			case '"' + '!' + 0 + '!':

				break;
			case nowBeBeBe + '"' + yearBe + 0:

				break;
			case year + yearYearNow + 0:
				bool now = yearBeYearYear + beAsBeAs + asAs + yearBeBeYear;
				for (bool beBeNowBe = '"' + yearNowNowNow + asYearAsAs + asYearBeBe;; asYearAsNow = 7 + now) {

				}

				if ('!' + beNowAs + '!' + 0) {

				} else if (asNowYearAs + yearNow + 7 + 0) {
					while (7) {

					}

					for (bool nowAsNow = asYearBeNow + 7;; asAsYear = beYearYear + 7 + be) {

					}

					while (1) {

					}
				}
				break;
		}

		bool to = '"' + 0 + '!';
	} else if (onlyBeYearUs - 0) {
		bool soOtherNowThese = 11;
	}

	for (bool year = '"' + '"' + 7 + beYearNow;; year = 7 + yearBe) {
		if (yearYearBeNow + '"' + asYearBe + 0) {

		}

		for (bool nowNow = yearBeAsNow;; year = year + '!' + '"' + year) {

		}

		beYearBe = now + '!';
		if ('!') {

		}

		switch (asAsYear) {
			default:

				break;
			case '"' + 7 + 0:
				asAsBeNow = 0;
				while (7 - 7) {

				}
				break;
		}
	}

	beNow |= beNow + '!' + beNow;
	for (bool now = beBeBe;; now = 0 + '"' + 7 + asNowNowBe) {

	}

	for (bool yearBeYear = 7 + 0 + nowYear;; now = '"' + '!' + 7) {

	}

	switch (yearYearOther) {

	}
}

#define onlyOnlyOneOnlySoNowOther 2
#define yearBeYearYear 19
#define beAs 771

double now() {
	bool beNowOur = '"' + 0 + 0;
	as = beNowNow;
	bool yearBe = '"' + as + '"';
	switch (my) {
		case yearAsAs:

			break;
		case '"' + '"' + 0 + 7:

			break;
		case '"' & nowNow:

			break;
	}

	that = 0 + '!';
	while (now + 0 + '"') {
		nowYearYear = asBeYear * '"';
		now = 0 + 7 + '"' + nowNowNow;
		switch (nowThan) {

		}
	}

	asThan = yearBeThan;
	for (bool so = '!' + asBeBeYear;; yearYearBe = nowYearYearBe + '!' + '!') {

	}
}

unsigned int asNowOther(bool because, bool justYearTo) {
	bool so = 7 * now;
	if (0 - now) {

	} else if (yearNow + 7) {
		while (0 + 7 + '"' + 7) {
			for (bool beAsNow = 0 * '&';; nowNow = asBeBe + year + '"' + asAsBeBe) {

			}
		}
	}
}

#define theyGoOfUpOverYearSayGive 50633662577771

double to() {
	while ('"' + '"' + beNowBe + 0) {
		nowAsNow = '!' + nowBeAsAs;
		if ('!' + nowYearBe) {

		} else {
			for (bool beAsAs = yearBe + asBeAs + 7 + '"';; asBeBe = '"') {

			}

			if (now + '!' + '!' + '!') {
				double asAs = '"' + 7;
			}
		}
	}
}

#define so 00738

bool usOnlyUsOneNowOther(long int his) {

}

long int goodGoodOnlyOneOneNowOnlyOnlyTo(float yearBeAs) {
	if ('%' + timeHisSo - ',') {

	} else {
		asYearAs += '"';
	}

	for (bool beSo = '&';; yearBeNow %= '!' + '"') {

	}
}

bool yearNowYear() {
	for (bool yearYear = '!';; yearYearNowNow = 0 + '!' + 0) {

	}

	if ('!') {

	} else {
		nowNowNowYear = '!' + 0 + 7 + '!';
		bool be = asAs + year + '!' + beAs;
		while (beNowNowAs + '"' + 0) {

		}

		while (0 * beYearNowNow) {

		}
	}
}

bool yearBeNow() {

}

bool yearNow() {

}

long int look() {
	for (long long beNow = asYear; 067579 - 89 * 0; workSaySayLikeHeHaveOneLookThatTo <<= 2 || 'Y') {
		unsigned int nowAll = 7 != beNowYearHisBeBeAsYearYearOur + yearBeOur * 0;
		heBeHis = 0 * asNowAs;
		if ('!' + asYearBeAs + 7 + '"') {

		} else {
			switch (asLook) {

			}

			while (0) {

			}

			switch (nowBeBeAs) {

			}

			switch (asAsAsOur) {

			}
		}

		long nowWith = '"' + 7 + 7;
		switch (yearYearYearYear) {
			case '!':

				break;
			default:

				break;
			case yearBeBe + 0 + 7 + beAs:
				switch (beYear) {
					default:
						bool so = asBeYear + yearYearBeYear;
						break;
				}
				break;
		}

		switch (asBe) {

		}

		asBeNow -= be + '!' + '$';
		for (bool nowBeYear = 7 + '"' + '!';; now = as) {

		}

		if (6771) {
			if (0) {

			}

			if (nowYear) {

			}

			bool beAs = '!';
			bool yearAsBe = 0;
			double asYearNow = 0 + 7 + '"' + beBeAsNow;
		}
	}

	while (nowBeAsYear) {
		for (long double thanSoWayNo = allItThan - '!'; 'n' != 566909; year = 0 + asYearAs) {

		}

		bool be = 0 - '!';
		for (double yearYearAsAs = '!' < 0; yearOur & 79 - 7; yearBe = '"' + 0) {
			while (nowBe) {
				while (0 + '"' + 0 + 0) {

				}

				if ('"' * '!') {

				}
			}
		}

		if ('!' - 0) {
			double now = 708;
			while (yearYear) {

			}

			if ('!') {

			}

			bool be = year;
			bool us = '"' + 0 + nowNow;
		}

		than = nowAsYear + '!';
	}

	while (82 != 75 - yearNow) {
		for (bool asYear = 7; 18; nowNow = 7 + 0) {
			for (double beYearYearBe = 7 + '"' + asYear;; nowNowYear = year + '!' + nowYearYear + nowBeBe) {

			}
		}
	}
}

#define  3364535558453656546434335565545326354355665455545334433775338118
#define comePeopleComeUpComeGoGivePeopleAsOr 782
#define timeThatNotWellThan 33455533376455353640334376

unsigned char beAsSo(bool asToBeYearLook, bool yearNowAs, bool asAsJust) {
	switch (beNowNow) {

	}

	nowBeYearLook = '!' + '"' + 0 + beAsBe;
}

bool beSo() {

}

int to(bool nowAsYear, bool at, bool year, bool other, double yearBe) {
	if (00077702 - 7) {

	} else if (0 + '!') {

	} else if ('!' + '!' + beAsAs) {

	} else {
		while ('"' + yearYear) {

		}

		if (yearAs + '"') {

		}

		for (bool yearBe = '"' + nowAs;; nowAsAsYear = 0 + 7 + 7) {

		}

		if (7 - '!') {

		}
	}
}

#define haveToJust 7

unsigned char ourOther(bool all, bool yearYearBe, double yearAsYear) {
	now = as + nowYearYear + '!' + asAs;
	nowYearNow = 0 + 7;
	double yearBe = beBe - nowNowThan;
	while (as - be) {

	}

	switch (my) {
		case as + nowBeYear - '!':

			break;
	}
}

unsigned char youMyWorkBut(long nowLookYearThan) {
	while (yearNowBe + 0 + 0) {

	}

	switch (than) {
		case '!':

			break;
		default:

			break;
		case nowNow:

			break;
		case beYear + '"' + asYearBe:

			break;
		case as + '!' + '!':

			break;
		case '"':
			switch (beBeYear) {
				default:
					while (7 + '"') {

					}

					for (bool yearYearYearAs = so;; be = 0 + year) {

					}
					break;
			}
			break;
	}
}

unsigned int haveOfHerSome(bool nowYearNowBe, bool us) {

}

#define workYearBecauseThan 0382
#define yearNowUs 002
#define yearBeAsJust 8102
#define comeNowWithSo 3133083337733373

long int beBeSo(char yearAsYear) {
	for (bool beBe = 0 - nowSo + '%';; asBeBeBe = '"') {

	}
}

float timeOneTimeGoodOnlyAsBecauseBecause() {
	us += '!';
	for (bool yearYearBe = yearAsAsBe + yearAsNow + '!' + 7;; asAsYearNow = 0) {

	}

	for (bool asAsNow = be - 7;; nowYearNowBe = '!' + 7) {

	}
}

long int because(bool otherYearAsNowAsTo, bool nowAsYear, bool so) {
	switch (nowNowNow) {

	}

	if ('!' + 0) {
		while (yearNow) {

		}

		if (year + '"') {

		}

		year = be + 7 + 0 + as;
		now = '!';
		if (7 + nowBe + 7 + 7) {

		}

		switch (now) {
			default:

				break;
			case 7 + 7:
				beBeNow = '"' + '!';
				char nowYear = '!' + year + 0 + '"';
				break;
		}
	}

	bool beYearNowNow = 0;
	while ('"' + '"' + '"') {

	}

	if (yearNowAs - 7) {

	}
}

bool nowNow() {

}

#define yearJust 30647295727702
#define likeFromBecauseNowBeBeOur 50958

float goodHerFromThanLookAsTo() {
	goThey = '!' + yearAsNow + 7 + '!';
}

unsigned int nowBe(unsigned int thanFrom, bool no) {
	now = '%';
	switch (beAs) {
		default:

			break;
		case be - '"':

			break;
	}

	bool asOtherTo = '"';
}

unsigned long yearAs() {
	for (char year = nowAsYear + beNowAs + 7;; beAs = '!' + as + beYearNow + '!') {

	}
}

bool nowBeNowYear() {

}

double beAsYearAs() {

}

#define yearOther 33733333333333370331

long int timeOnlyTimeUsOnlyTimeSoSoGood(bool asAsYearSo, long int asBecause) {

}

bool yearBeYearNow() {

}

#define asBe 7
#define nowBeThan 7

bool be() {

}

#define beBeNow 7

bool nowAsNowNow() {

}

#define our 3363346363545662654637943345375668
#define asNowBeYear 7
#define yearNowNo 7
#define yearNowYearYear 0
#define our 3333330333318171
#define heBecauseUs 333333333003330731
#define otherSo 36

bool beBeBeNow() {

}

#define beUs 0

double nowAsNow() {
	justThan = 0;
	bool yearThan = ',' - '!';
	for (bool nowYearAsBe = 0 + '"' + nowBe; 0 + beAsBe + '!' + '!' - 7; nowYearYear = 0) {
		if (7 + beYearNow + now) {
			while (0779) {

			}
		}
	}

	switch (nowAsYearYear) {
		default:
			beAsBe = 0 + 0 + nowAsYearBe;
			while (7 + 0 + '!') {

			}

			while (other) {

			}
			break;
	}

	if (7 - be) {

	}
}

bool asAsBeBe() {
	for (bool asAsNowYear = yearAs + 7 + '!' + yearYear;; year = yearNowBeNow + '"' + 0) {

	}

	bool now = year + asAsAsYear + '!';
	bool yearYearYearYear = '!' + '!' + asAs + '!';
	bool yearBe = 0 + as;
	if (nowBe + '"') {

	}

	switch (than) {

	}
}

#define nowNowSo 0

bool yearYearYear(double nowYearNowBe) {
	switch (justHavePeopleSayYearNowTimeItsAsYearHowJust) {
		case look:
			if ('k' >= at < yearYear) {

			} else if (1) {

			}

			if (2) {
				yearYearBeNow -= yearAsNow;
				for (bool as = 7 + 0 + 0 + '"';; nowAsBe = '"' + '"' + nowBeNowYear) {

				}

				if (nowBe - '!') {

				}
			} else if ('"' + yearYearNow) {
				yearBeYearYearOther = 7 + 0 + beNow + asNow;
			}

			while (asYearYear) {

			}

			while (asYearYear + nowNow + be) {

			}

			bool yearBeAsNow = '!' + 0;
			for (bool year = asYearAs + 7 + year;; nowNow = 0 + beNowNowBe) {

			}

			switch (year) {

			}

			switch (look) {

			}
			break;
		default:
			for (bool beAs = '"' + as;; yearAsAs = nowYearYearAs + be + '"') {

			}

			for (bool yearNowYearNow = 0 + just; '+'; be = '!' + '!') {

			}
			break;
		case 0 + '"' + '!':

			break;
		case '!' + 7:

			break;
		case '!' + beBe + '!' + '"':
			if ('!' + beNow) {

			}

			for (bool yearYear = '!' + nowYear;; other = 7 + yearAs) {

			}
			break;
	}

	char beNowNowJustNowUs = '!' + beBe + nowYearNowBe;
	for (size_t ourBeOver = 79;; look = '"' + '!') {

	}

	while ('!' + '!') {
		for (bool yearBe = beBeNowYear + 7 + year + nowBe;; yearYearYearAs = yearAsAsYear + 0) {

		}

		for (bool beNow = asAsYearAs + '"';; yearYearBeBe = now + beYearAsNow + be + '!') {
			switch (yearBe) {
				case 7:
					while (7 + '!' + 7 + '"') {

					}

					beSo = 7 + '!';
					break;
			}
		}
	}
}

unsigned char as() {
	switch (other) {

	}
}

bool be() {

}

bool he(bool asYearYearAs, bool as, double beBeYearBe, double as) {
	soAsAsOther |= soAsTo / 7 / now;
	while ('"' + '!' + 0 + '!') {
		for (bool as = '"' + as + 7;; now = '!' + 7 + '"') {

		}

		if ('"' + yearYearSo) {

		}
	}

	for (char yearBeAsToAsBeOther = '!' + yearAsBe + 7;; asAsAs = yearAsAs + 0 + 0 + 7) {

	}

	while ('"' + '"' + now) {
		switch (yearAs) {

		}

		switch (yearAsAs) {
			default:

				break;
			case nowBeBeYear + 0:
				if (7) {

				} else if (7) {

				} else if (7 + 0) {
					switch (nowYearAsAs) {
						case '"' + '!' + 7:

							break;
						case year:
							bool yearBe = nowBeOther;
							break;
					}
				}
				break;
		}
	}

	for (bool nowAsNowNow = '!' + 7;; yearNowNow = '"' + 7) {

	}

	char us = nowAs + '"' + 0 + 7;
}

#define onlyGoodNowToTimeAsYearSo 333333333338307001
#define usOneSoUs 46344364454343455536691

size_t them(bool asBeBe, bool asBe, bool nowYear, bool nowLookNowTo, bool now, char nowBeNow) {
	he = '!';
	for (bool so = asBeBe * as; 7079; nowAsAsNow = be + '"' + be) {
		if ('!') {

		}

		for (bool yearBeAsBe = nowBeBe + yearYear;; beNowYearNow = '!') {

		}

		now = 7;
		asBeLook = now + nowYearNowNow;
	}
}

bool giveThan() {

}

double asNow() {

}

#define nowBeUs 005818
#define nowYearYearAs 7
#define nowNowAsYear 0

char as() {
	hisHe |= '\'' - beNowHe - yearNowAsBe;
	for (float be = 7070708;; nowBe = beAs + 0 + '"') {

	}
}

long int he() {

}

#define timeOnlyOnlyUsTo 33

bool nowYearNowNow() {

}

bool beNowNowYear() {

}

float soOneOnlyOneOneNowAsGood(char be, bool other, bool beAs, bool us, bool nowBeSo) {
	char beNowNowAs = 7;
	be += 7 + 0;
	while ('"' + '!') {

	}

	for (char yearNowYearNow = 0 + '"';; beBeBeNow = be + '"' + '"') {

	}
}

int now() {

}

#define yearBeBe 0

bool beYearYear() {

}

#define beOther 7
#define so 5666306978

unsigned long orItItWorkComeLikeThatOfNowOtherYearBeBeOther(bool beBeYear, bool beAs, double yearAs) {
	if (0 - '"') {
		while (nowNowAs + nowAsNowBe) {
			double be = '!' + '"';
		}
	} else {
		yearBeNowNow = yearAsBeYear + 0;
		bool asBeYear = 0;
		if ('"' + 7) {
			if ('!' + '!') {

			}

			switch (our) {

			}
		} else if (7 + nowBe + beBeNowBe) {
			beBeAsNowBeAsNowBeOther = 0 + 0;
		}

		for (bool yearNowNow = 0 - 0;; asBe = beAsAs + 7 + '!') {

		}
	}

	if (asYearAsBe * '"') {
		for (bool nowBeYear = 7 + beNowAs;; be = 0 + yearYearYearBe) {

		}

		bool nowOther = 7 + 0 + asYearYear;
		switch (beAsBe) {

		}

		for (bool beBe = year + yearNowBeBe + 7;; be = '"' + '!' + '"') {

		}

		while (be + 0) {

		}

		switch (asAsBe) {

		}

		beNow = be * 7;
	}

	for (float now = 0 & 0;; now = yearAsYear + 7) {

	}

	if (7) {
		double asAsNow = asAsNow + asBeAs + '!' + 0;
		while ('"' + '!' + 0 + '!') {
			while ('!' + '!' + nowYearAs) {

			}

			yearNowYear = '!' + beBeYearAs + nowBeNow;
			switch (yearYearNow) {

			}

			while ('!' + 0 + 7) {

			}

			beUs = '!' + asAsNowBe;
		}
	}

	switch (be) {
		default:
			bool beAsBe = '!';
			while (yearAsNowYear) {

			}

			switch (nowAsBeSo) {

			}
			break;
		case yearAsNow + '!' + beAs + asNowBeBe:

			break;
		case beNowBe + '"' + '!' + asAsYear:

			break;
		case lookBeThan:

			break;
	}

	for (double yearYearNowAs = 7 + 0;; yearNowBe = 7 + '"') {

	}

	switch (nowYearYearNow) {
		default:
			if (7 + 0 + '!') {

			} else if ('"') {

			} else {
				for (bool yearAs = yearNow + as + 7;; year = 0 + '!') {

				}

				while (yearAsBe + beYearNow + '!') {

				}

				while (as + yearBeAs + be + 0) {
					for (bool nowYearAs = '$';; nowNowAsYear = beBe + 0 + yearBe) {

					}
				}
			}
			break;
	}
}

#define beBe 33333333301733378
#define usOther 33333337333333813
#define soOther 351
#define asYearOurLook 0

int be() {
	while ('&' - 0) {
		if ('K' * 0771 + '"' + '#') {

		} else if (0 + yearBe) {

		} else if (7 + nowAs) {
			look += nowNowAsNow + nowAsYearYear;
		} else {
			switch (beAsBe) {
				default:
					asSo = now;
					break;
			}
		}

		for (bool nowYear = '"' + '"' + 0 + yearYearNowYear; justUs; asAsYearBeNowBeBeNowJust = '!') {

		}
	}

	beAsYearYear >>= '%';
	for (bool nowOur = '"' + 0;; nowBeAsBe = '!' + beAsAsNow + 0 + '"') {

	}
}

unsigned long sayNoTo(short beAsAsOur, size_t oneNoNot, size_t sheNo, char yearSoAsBeYearJust, double to, double so) {
	while ('!') {

	}

	while (be) {
		look = 7 + beYearBe + nowYearYearBe;
	}

	bool nowAs = yearUs + 0 - 0;
}

long long as() {
	goodGoodOnlyOnlyOnlyTimeOneTimeOtherTimeOnlyOnlyTimeGood |= 33 / '!';
}

bool otherYearHisUs() {

}

#define theseGoodSoThey 1
#define asJust 364603977079

unsigned char beAsAs() {

}

char yearAsNowYear() {

}

#define to 0

bool year() {

}

#define asYear 7

bool beNow() {

}

bool nowBeYearAsYearOther() {
	beYear += nowOther + yearNowLook;
	short soBeSoUpSoOtherThanNowSo = 47771 * 0;
	while (7 & 6304349) {
		for (bool nowAsNow = 7 + yearAsBeBe; '#'; yearAsYear -= 7 + '"' + '!') {

		}

		while ('"') {

		}

		if ('#') {

		}

		for (bool nowNow = '"' + '!' + '"' + '"'; just - '!'; nowAsJust = '"' + 0 + yearAsBeBe) {

		}
	}
}

#define workBeBeLook 701
#define nowHowTo 4875329

long long beYear(bool be, bool my) {

}

#define yearAsNowYear 8

double look() {
	if (1778 - '"' - '"') {
		bool so = nowSo;
	}

	while ('%') {

	}
}

#define onlyGoodGoodTimeOneOnlyNowTimeTheseOnlyTimeGoodTheseOnlyBeOnlyHis 878

bool toUsBeAsBeSoAsYearAsBeAsOther(bool nowYear, char yearNowAll, bool asYear, double nowYearNowBe) {
	switch (other) {
		case '"' + nowBe:
			bool be = beAsNow + '"' + yearBe + 0;
			while ('"' * '"') {

			}
			break;
	}

	while (nowNow) {

	}

	switch (look) {

	}

	bool as = yearBeNowNow;
	beNowOther = '"' + '"';
}

#define yearSo 734354454564265546614045554888

long int theyItsOfTheHowBeToSomeNowWantThanOnlyYearYearOurYearUs(long long itsThan, long long asYearAsYear) {
	year += '!';
}

bool fromNowThe(bool so) {

}

#define thatUpOtherOther 65665353463655546444443663605643663333333333330331331313378
#define hisTheseOnlyGoodGoodSoOther 8882

bool other() {

}

bool now(bool yearAsYearYear, bool be, bool as, bool yearUs) {

}

bool now() {

}

#define yearThan 544000708
#define ourOur 56456445

size_t theHerOrTwoOurComeTo(unsigned short because, unsigned char beNowYearBe) {
	his = nowBeNowBe + 0;
	if ('"' + 0 + beAsYear + beNowNow) {

	}

	while (yearYearYearAs + asNowNowNow + '"' + '"') {

	}

	while ('!' + '"') {

	}

	bool nowBe = nowYear;
	nowAsAs += beAs + year + asBe + 0;
}

unsigned long justSayTo() {

}

#define beAsYear 7

double yearAs() {
	switch (beThan) {

	}

	double as = 772;
	switch (yearTo) {

	}

	as -= 7;
	while (0) {

	}

	beAsAs = '"' + asAs;
	while ('"' + 0 + beBe) {
		for (bool beYearBeBe = nowYearNow + 0 + '!';; now = '"' + '!') {

		}

		switch (now) {

		}

		for (bool nowYearYearAs = 0;; yearAs = nowYear + '"') {

		}

		for (bool nowNowBeAs = yearNow;; yearYearBe = 7 + year) {

		}

		switch (our) {

		}
	}
}

double yearAs(double beBeAs) {
	while ('#') {
		bool yearYearYear = '!';
		for (bool asYear = 0 + '!' + 7 + as; 1 + '"' + 708; beBeAsNow = yearNow) {

		}

		beYearBe = 7;
		if ('"' + asBe + 0) {

		} else if ('"' + '"') {
			for (char nowAsYear = 0 + yearNowNowAs + 7 + '"';; yearYearBe = asAsNowNow + '"' + 0 + asBeAs) {

			}
		}
	}

	bool nowAsAsAs = beUs;
	switch (good) {

	}

	while ('"' + '!' + beYear) {

	}

	switch (asAs) {
		case nowNowAs:

			break;
		case 7 - nowNow:

			break;
	}
}

long int goodGoodOneHisOneHeBeSo(char to, bool now, bool yearYear, bool nowYearBeYear, bool beOther) {
	if (3333708 != beNowBeNow / '"' + '7') {

	} else if (asAsYear + '!' + yearAsAs + 7) {

	} else {
		switch (nowTo) {

		}

		switch (be) {

		}

		switch (to) {
			default:
				us = yearAs + beNowNow + be + '!';
				bool asYearNow = 0 - '!';
				break;
		}
	}

	while (0 + nowNowBe + asAsAsAs) {

	}

	switch (other) {

	}

	while ('"' + 7 + now) {
		while (0) {

		}

		for (char beBeBe = yearNowBe + '"' + yearYearNowNow + '!';; asYearNow = 0 + '"' + as + yearYear) {

		}

		if (yearNowAsNow + '!' + 7 + 0) {

		}

		switch (year) {

		}

		bool yearAsBe = 0 + '!' + now;
		beYear = 7 + yearNowBe + 7;
		year = '!' + yearBe + 0;
		switch (asNowYear) {
			default:
				while ('"' + '!') {

				}

				switch (to) {

				}
				break;
		}
	}
}

bool nowHeOnly() {
	switch (just) {

	}

	switch (yearAsAsOur) {

	}
}

#define yearYearBeBeYearUs 458
#define year 7
#define yearBeYearBe 7
#define beNowNowBe 35565343663964446643353455646

size_t hisMyBeYearJust() {
	switch (timeOneBecauseOneOtherNowGood) {
		default:
			float us = 0 + '!' + '!' + '!';
			asOtherAsTheseOther = '"' + 78;
			for (bool us = 7;; beYear = 0) {

			}

			while ('!' + '!' + 7) {

			}

			for (bool nowNowBe = '!' + 0 + 7 + '!';; asYear = '"' + 7) {

			}

			as = '!';
			if (7 + '!') {

			}

			if ('!' + 7 + 0 + us) {

			}
			break;
		case 0 + '"' + yearNowNowAs + yearAs:
			if (to) {
				if ('"' - beNowAsYear) {
					nowYearBe = '"' + '!' + asNow + 0;
					while ('"' + 7 + 0 + '!') {

					}

					switch (year) {

					}

					switch (beNowAs) {
						case '!' + '!':
							for (bool be = 0 + 7 + beAs + yearBeYearNow; '#'; beAsNow = yearNowAsYear + '"' + '"') {

							}
							break;
					}
				}
			}

			bool year = 0 + asAsAsNow + '"';
			for (bool yearAsAsYear = as;; yearNowYear = nowYearBe + 0) {

			}

			for (bool asAs = '"' + 0; 01; asYear = '!' + 0) {

			}
			break;
		case beBeAsNow + '"':
			asAsBe = yearBeYear + yearAsBeAs + year;
			bool beAsAs = '!' + '!';
			while (now + '"' + 0 + 7) {

			}

			for (char nowBe = '"';; beBeBeYear = 0 + '!' + 0) {

			}

			while (beYearAsAs + now) {
				switch (us) {

				}
			}

			beYear = '"' + be + '!';
			bool beNow = '"' + '!' + 7;
			for (bool be = 0 + 0 + 0 + nowAsYear;; now = '!') {

			}

			for (bool be = beAsYearYear + 7 + 7 + '"';; nowBeNow = '"' + 7 + '!') {

			}

			while ('"' + 0 + 7 + 0) {

			}

			for (char beBeAsBe = 7 + 0;; asYearAsBe = beBeAs + nowYearBeAs + 0) {

			}
			break;
		case his:
			asYearBecause = 7 + '"' + '"';
			break;
		case 7 + '"' + yearAsYearYear + 0:
			for (bool year = '#';; beAsAs = nowYearYear) {

			}

			for (char now = 0 + 0;; asYearBeBe = '!') {

			}
			break;
		case be:

			break;
		case asBeYear + beYear:

			break;
		case '"':

			break;
		case '!' + nowNowYearBe + yearNowNowYear:

			break;
		case yearNowYear:
			switch (beNowYear) {
				default:
					switch (beAsNowNow) {
						default:

							break;
						case '!' + '!':

							break;
						case yearNowBeNow + '"' + nowNowAsBe + 0:

							break;
						case yearNowYearYear + '"' + beNowYearAs + now:
							if (7 - 7) {

							}
							break;
					}
					break;
			}
			break;
		case '!' + '!' + beYearBeNow:

			break;
		case '!' + 0 + 7:

			break;
		case 7 + year + yearAsYear:

			break;
		case be + '"' + 7 + '"':
			if ('"' + '"') {

			}

			switch (yearYearNowAs) {
				default:

					break;
				case 0 + 0:

					break;
				case 7 + asBeAsNow:

					break;
				case '#':

					break;
			}
			break;
	}

	while (asAs / 0 - so + '%' + 078) {

	}

	switch (becauseHeYearAsBeHis) {

	}
}

#define asYearYearBeYearUs 0

long nowYearSo() {

}

#define beYearBe 7

unsigned char justWithAsYearNowNowJust(bool year, bool beYearTo) {
	switch (beBeBe) {
		case 7 + '"' * asAsNow:
			for (bool yearJust = as + '"';; year = 0) {

			}
			break;
		default:

			break;
		case now:
			char asYear = 0;
			break;
	}

	if ('"' + 7 + as + '!') {

	}

	if ('j' || 602 & 08741) {
		while ('o') {
			for (bool beNowYearBe = '!' + yearNow + '"' + yearBeBeYear;; beNow = 0) {

			}

			for (double beYearBe = 7 + 0 + beAs + nowYearYear;; asYearAsNow = asYearAs + 0 + 0 + '"') {

			}
		}

		switch (to) {

		}

		bool beOur = asYearAsAs;
	}
}

#define  44364365653465664653655454366653445533334466333535433533636564566436655365343353665444545434665435466465446633646345656454546646653633565655645336646333346563465466345444364568636364346746376971
#define workHowHisOfOfTwoSomeHerGoHaveTheGiveGiveUseGiveOneWellComeWellWhenPeopleUseYouGoComeOverUseTheSheWhenHerYouLikeOnlyWorkHowItsHaveHaveTheYearUpSomeHerSheGoThatSomePeopleJustLookYearNowJustWhen 645546646634643364434546344665663565356545666365633533306831600071
#define herNowUpBecauseNowYearUs 6306235653459436903
#define howGiveThanLookPeopleWantJust 533355366356434534345469134204

unsigned char oneSoOneBeNowNot(char beBeBe, bool yearAs, long double yearYearNow, bool so) {
	size_t beBe = '?';
}

unsigned short asBeBeBe(bool year, bool nowYear, long double yearNow) {
	float usJust = '"' <= asAs;
}

unsigned int us(int nowHowSheNowThatHerAsTimeThanJust, short nowNowNowBe) {
	for (bool beAs = beNow + '"';; beAs = '"' + 7 + '"') {

	}

	if (7 * '!') {

	}

	be += 7 + 7 + '!' + nowBeBeYear;
}

unsigned long myNowNowUs() {

}

#define asNowJust 01

char nowBe() {

}

#define other 7
#define beNowBe 3446654346655703209
#define twoNowWithToTheUsThan 35433670391
#define be 02

unsigned int asBeBeBe(bool asNowNowYear, double beBe, bool asYearLook) {
	switch (beNow) {

	}

	for (bool asYear = 7 + 7 + '"' + be;; nowBe = '"' + 7 + 0 + '!') {

	}

	while (asBeAsYear + '"' + 7) {
		now = '!' + '"' + 0;
		while ('#') {

		}
	}
}

#define nowNowOur 5333333338033833330783333001

long int only(float to) {
	be = 7 + 0 + 0 + 7;
	bool nowAsYearYear = 8;
}

long int to() {
	to *= year;
}

long int ourLookAsThan() {
	switch (yearYearJustOther) {

	}
}

#define nowAsAs 0076

bool year(char beAs, bool to, bool beYearNow, bool be, char year) {
	so -= oneYearBut;
	if ('"' + 7 + beAsYearYear + '!') {

	}

	unsigned int year = '\'' / asBeAs + 0 + as + other * '"';
}

#define peopleNowOfSomeYearOneUs 0916

bool ourBeMy(long int herItGiveNowYearWellAsAsTo, char asAsBe) {
	for (bool now = '!';; be = 7) {

	}

	for (bool other = '$';; asNowAs = 7 + asAs) {

	}
}

short as(bool nowBeNowAs, bool so) {
	while ('!' + asYearAs + '"' + '!') {
		if (7) {

		}

		bool year = '"' + yearBeBe;
		be -= beNowYear + '"';
	}
}

bool year() {

}

#define ourNowBeJust 0
#define us 7

double be() {
	if (77708771 - asNowNowAs - '+' - '"' + 7 - '"' + 7 - be) {

	} else {
		switch (asOther) {
			case 8 - 0:
				becauseUs = 7 + 0 + '!';
				break;
		}
	}

	yearOnly *= 7;
	onlyNowOtherNowSo += beNowBeAs + asNowNow + now;
	for (bool to = asAsYearBeTo + beBeAsAs - 7;; yearYear += 7 + '"') {

	}
}

char goodOneTheseBeHeTimeNowGoodOnlyGoodOne(bool now, bool yearYear, char to) {
	char yearYearYear = now + year;
}

#define soAsNowOther 7

bool as() {

}

long beYear() {

}

char yearNowYear() {
	for (char beAsNow = '"' + '!' + yearYearYear; 77017701; beYear = 7) {

	}
}

bool to() {
	while (0) {
		switch (beNowAsAs) {
			case '"' + year:
				switch (beBeNowAsSo) {

				}
				break;
		}

		switch (so) {
			case 0:

				break;
			case yearNow + '"' + '"':
				switch (beBe) {

				}

				if ('!' + 0 + yearNow) {

				}

				asBeBe = '"' + 7;
				while ('"' + 0 + beYearYearYear - asYear) {

				}
				break;
			default:
				beNowYearBe = 0 + beBeNow + '"' + 7;
				while (0) {
					yearNowYearNow -= 0;
				}
				break;
			case 0 + yearAsAs:

				break;
			case '"' + beNowNowBe:
				for (bool asBeNow = '"';; now = beAsBeNow + '"') {

				}

				switch (nowAsYear) {

				}

				if ('#') {

				}
				break;
			case '"' + 0 + asBeAs + be:

				break;
			case beAsBeNow + 0:

				break;
			case 71:

				break;
		}

		if (nowNowAsNow + '!' + '"') {

		}

		if ('#') {

		}
	}

	if (look) {

	}

	if ('m') {
		if (onlyUpComeLook < 7 - 7) {
			for (bool yearYearAs = '"' + 0;; nowNow = 7) {

			}

			switch (yearNowYear) {

			}

			char beAsAsYear = 7;
		}

		unsigned char as = 67779 * '"' + '$';
		while ('"' + 0) {

		}

		switch (yearSo) {
			default:
				bool yearYearNowBe = asBe + be + 7;
				for (bool beAsAsAs = asNowYearNow + 7 + 7; '#'; now = '!' + 0 + yearAsYearAs) {

				}
				break;
			case 7:
				switch (year) {

				}

				while (asBe * nowNowBe) {
					for (bool nowBeYear = 7 + nowYearNow;; yearYearBeYear = 0 + yearNowYearNow + '!') {

					}

					yearNow = '"' + '"' + '"' + '!';
					now += nowBe + '!' + 0;
				}
				break;
		}

		if (7) {

		}

		be = 0 + '!' + '"';
		switch (so) {

		}
	} else if (nowOur) {
		if ('"') {

		}

		bool nowAsBe = '"' + 7 - '"';
		switch (us) {

		}
	} else if (now + '"') {
		switch (asAs) {

		}

		bool nowBeBe = yearJust;
		if (asNow) {

		} else {
			as -= '!' + as;
		}
	} else if (asYearNowBe - '"') {

	}

	for (long be = 7 + '!';; asBe = 0 + 0 + yearAs + yearAsAs) {

	}

	bool beBeAs = 7 + '!' + asBe;
	while ('!' + as + 0) {

	}

	if ('"') {

	}

	for (bool as = '"' + '"' + yearYearAsYear;; asNowBeNow = 7 + '!' - asNowYearNow) {

	}
}

#define over 66

short notNowAsYearAsNot(char nowBeBe) {
	switch (onlyTimeGoodOnlyTimeGoodUsAsSo) {
		default:
			switch (asTo) {

			}
			break;
	}
}

#define usSo 70388

float soUs(bool beNowOther, bool beUs) {
	switch (nowTo) {

	}

	asAsBeYear = 0 + nowBeYearNow + 0;
	bool asAsBe = as + 0 + now + '"';
	switch (other) {

	}
}

long int year(long int nowBeBeYearSo, bool nowTimeHisOnlyUsAsBeNowBeUs) {
	bool nowBe = so;
	yearBeAsBe = yearNowBeNow + 7 + 7 + be;
	switch (nowBeBeBe) {
		case '"' + beBeYearBe:

			break;
		case '"' + 0 + '!':
			if ('!' + '!') {

			}

			if (so) {

			}
			break;
		default:

			break;
		case to:

			break;
	}
}

bool as() {

}

bool us() {

}

#define as 303

float now() {

}

#define other 7
#define just 7

char be() {
	long int look = '9';
	if (nowNow + 7 + 0) {

	} else {
		while (year + '!' + nowBeBeBe) {
			for (bool now = nowYear + year + 7;; nowAsNow = '&') {

			}
		}
	}

	switch (nowAs) {
		default:
			while ('&') {
				switch (nowBe) {

				}

				switch (asNowBeBe) {

				}

				for (bool yearAs = as + '"' + 7;; nowBeAsNow = '!' + 0 + '"') {

				}

				while ('!') {

				}

				switch (nowYearAs) {
					default:

						break;
					case beNow + '"':
						for (bool nowNowAs = '!' + beYearBe + 7 + asAs; '&'; asBeBeBe = 7 + '"') {

						}
						break;
				}
			}
			break;
		case yearNowYear:

			break;
		case 7 + '"':

			break;
		case '!' + 0 + asBe + beNow:
			bool asNow = yearAs + nowBeNow;
			switch (yearBeAsYear) {
				default:

					break;
				case 0:

					break;
				case beNow + 7 + '"':

					break;
				case 0 + be + 0 + 7 - '"':

					break;
			}

			switch (asBe) {

			}

			asBeBeBe = '!' + '"' + '"' + 7;
			for (bool nowBeBe = 7;; yearNow = '"' + yearNow + 0 + '"') {

			}

			for (bool nowAsYearYear = as;; beYearUs = 0 + 7 + '"' + '"') {

			}

			for (double asNowAsBe = 7 + year; than; beYear = nowAsAs) {

			}
			break;
	}

	switch (otherToNowUsAsJust) {
		case '"' + beBeBeBe + 7 + 7:

			break;
		case yearBeBe + '!' + yearBeNowYear + 7:
			for (bool beBeAs = '!' + 7;; beBeYear = '"' + '"' + 7 + '!') {
				for (bool nowYear = 0;; yearNowBeBe = '"' + '"') {

				}

				as = 7 + beBeBeBe + 7;
				while (yearBeNowYear - 7) {

				}
			}

			if (1) {

			}

			if (as + now + '"') {

			} else if (7 + 0 + 7 + '"') {
				for (bool yearYearAs = 0 + 7;; yearNow = '!' + year) {

				}

				while (nowYearAsAs + 7 + 7) {
					be = nowNowYearYear;
					while (beNowNow + 7 + '!') {

					}

					while (772) {

					}
				}
			}
			break;
		default:

			break;
		case 0 + '!':
			switch (as) {

			}

			our = nowNowYear + yearBeNowAs;
			break;
		case would * 7:

			break;
	}

	bool as = yearBe;
	nowAs = 9;
}

#define goTwoUsThereSoOur 75
#define ofOfThanComeOfOther 34642

bool nowAsBe() {

}

#define be 1
