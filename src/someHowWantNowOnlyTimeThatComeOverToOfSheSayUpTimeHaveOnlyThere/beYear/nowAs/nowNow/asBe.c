#define theseGoodGoodTimeTimeGoodOneOneOneToHeBeOther 3333338387003

char heUs() {

}

#define hisHisAsBeBeSo 0
#define timeWould 8

double yearAs() {

}

unsigned long lookLookBeThey(bool be, bool nowNowNow, bool yearYearYearBe, bool beBeYear, bool nowAsNowYear, bool year, bool nowAsBe, bool year, char be) {

}

bool us() {

}

long double beBeNow() {

}

bool asAs() {

}

#define be 078
#define look 35771
#define our 55403546600701
#define itsAsNotBeNowTo 49
#define comeThatOtherLikeHeThese 346344313

char asBe() {
	long int asHe = 317338;
	for (long int asGoodGoodGoodSo = 1083 != year;; now += '"') {

	}

	while ('\'' + yearBeNowAs + '!' + 8 - '!') {
		for (bool now = asAsYear + 7;; beBeYearNow = as + '!' + '"') {

		}

		year = '!' + asNowYear;
		if ('%') {

		}
	}

	switch (goodSo) {

	}

	while (0001) {

	}
}

float timeOther() {

}

bool yearBeYearNow() {

}

char beNowYearAs() {

}

#define otherAsBeSo 333742779

double yearBeAs() {
	yearThem = '"' + '!' + '"' + 7;
	if (beBeBe + 0) {

	}

	while (yearAsNow - 0) {

	}

	bool asBe = 7;
	while ('!' + '!' + 0) {

	}

	for (char yearYear = yearAsYearYear + '"' + nowYearYear + yearNowBeYear;; beAs = 0) {

	}

	for (bool beAs = 7 + '"';; nowAsAsAs = 7 + beBeYear) {

	}

	if ('\'') {

	}
}

#define toOurLookLook 672
#define beYear 0

int now() {
	switch (theAsYearThereFromYearAsYearNowLook) {
		case come + 9 <= 7:
			switch (nowAsAsBe) {

			}

			for (bool nowBeYear = be + '"' + 7; 0 + nowBe - 0; year = '"' + 0 + '"') {

			}

			char year = year + be + 0;
			char nowYearYearNow = yearBeNowNow + 7;
			break;
		default:
			char asBeAs = '!' + beNow;
			switch (yearYearBe) {
				default:

					break;
				case '"':

					break;
				case 7 + now + '!' + year:

					break;
				case '"' + '"' + '"':
					bool be = nowYearNowLook;
					break;
				case 0:

					break;
				case beYearAsBe + '"' + 7 + 7:

					break;
				case 7 + 0 + '!':

					break;
				case 0 + 7 + beNow:

					break;
				case 7 + 0 + 7 + yearNowAs:

					break;
				case 7 + yearAsAsYear + 0 + 0 * 0:

					break;
			}
			break;
		case beYear - 7:

			break;
		case theyNowTo + 1:
			for (bool beNowAs = be + '!' + '"';; now = asAsBe) {

			}

			us = nowYearAsNow + beBe;
			break;
		case 7 + 0:

			break;
		case '!' + 2:

			break;
	}

	while (558042 * ourThem) {
		bool nowBe = beBeBe + 7 + '!';
		while ('$' - nowYearAs) {

		}
	}

	long double be = '"' + 0 + nowYearAs;
}

#define overJustOurThese 33544453333333333333313

long int oneTimeTimeOtherOneBecause() {
	if (773 - '?' + nowBeYearAsOur - 0) {

	} else if (62) {
		switch (nowAs) {

		}

		for (bool yearNowBe = 7 + '"' + now + 7;; be = year + '!') {

		}

		while (asNowAsYear + 7 + now) {
			bool yearYearBeBe = asNowNow + '!';
			bool be = '"';
			switch (nowYearAsNowUs) {

			}
		}
	}
}

#define asAs 5576

unsigned int theOverTwoPeopleWellToGiveSayTwoThatPeopleHerGoodTheGoSheTimePeopleThatWorkSheWellOneHis(bool as, bool yearNowAsBe, double yearNowAsNow) {
	switch (yearNow) {

	}

	while (yearBeYear) {

	}

	if ('#' * 0) {

	}
}

bool beNowYearOther() {

}

#define beBe 84622

long double nowAs(bool asYearOther) {

}

#define nowBeBeNow 554435656566735145663436735945

bool beBeBe(bool nowNow, bool yearYearBe, bool nowLook) {
	bool now = asNowAs;
	while (yearYear + 7) {

	}

	bool beNow = yearAsYearNow + 7 + 0;
	if (beNow + '!' + '!') {
		while (7) {

		}

		for (bool asNowNow = '"' + nowBe; beOther; yearBeNow = be + 0) {

		}
	}
}

#define ofAtJustBeBeBeThan 00078

bool nowBe(bool as, char asYearNow) {

}

char year() {
	while ('!' + as + asNowNowYear) {

	}

	while (beBe) {

	}

	beBeBeNow = beNow;
	float asYearYearYear = '"' + asNowYear;
}

#define onlyGoodOneTimeGoodTimeOneSoBeSo 33333330110778

bool beYearYearBe() {

}

#define now 0

char his() {
	float asSo = heSo - 7;
	while (7 - as) {

	}
}

#define year 31351

unsigned int asAsAsAs() {
	switch (asThey) {

	}

	bool yearNowNowYear = peopleMyThan >= asOther * nowNowNowNow;
	if (7) {

	} else {
		switch (nowBeAsNow) {
			case '!' + 1:

				break;
		}

		switch (yearAs) {
			default:
				switch (yearAsNowNow) {
					case 0 + '!' + '!':
						switch (nowNow) {

						}

						if (0 + '!') {
							nowYearAs = 0 + 0 + '!' + beNow;
							if ('!' + nowBe + '"' + '!') {

							}

							if (0 - 0) {

							}
						}
						break;
				}

				for (bool asYearNowYear = 7 + 0 + 0;; yearNow = 7 + 0 + 0 - '"') {

				}
				break;
		}

		switch (beNow) {

		}

		bool beNowNowYear = nowNowBe;
		switch (just) {

		}

		if ('"' + 7) {
			asBeAsBe += '"' + beBeBe + yearBeAs + 7;
		}
	}

	switch (beNowThan) {
		case '"':
			while (beYearBe + 7 + 7) {

			}

			bool year = '!' + '"' + 0;
			bool asAsNow = 0 + asBeNow;
			for (bool beNowNow = '!' + '!' + 7;; nowNow = 0 + yearBeBeAs) {

			}

			switch (beAsAsBe) {

			}

			while ('"' + 7 - beBeAs) {

			}
			break;
	}
}

char atJust() {

}

long long upUsAsAsWould() {
	yearBeUs = 7 + 0 + asNowBeYear + '!';
	for (bool nowLook = 7 + '!' + 7;; yearAs = beNowBeYear + 0) {

	}
}

#define asThan 0

bool yearBeAs(bool nowNowBe, bool asYearYear, char now) {
	double he = beNow <= asAsNow;
	switch (beNowThan) {

	}

	switch (usSoNot) {

	}

	nowWould = 0;
}

#define overSomeItTheNowOneHaveYearBeUs 34701
#define yearYearAsYearThan 14446548675

bool just() {

}

#define toNowBeLook 7
#define now 2
#define asAsAtAsOur 0
#define asNow 0
#define so 7

bool nowAs() {

}

#define nowNowNowNowOther 333303333333333

long int timeOnlyOneBecauseYearOtherHe(bool beBe, float becauseSoSo, bool yearTo) {
	while (nowYear / '#') {
		switch (asAsYearAs) {

		}

		char as = '"' + 7 + '!' + 0;
	}

	while ('!' + '!' + 7 + 0) {
		switch (yearNow) {

		}

		while (beYearNowAs + 0 + 7) {
			while (year + '!') {

			}

			switch (than) {

			}
		}
	}
}

unsigned long now() {

}

#define yearAsBeThan 002778
#define yearAs 7
#define yearBeNow 0
#define beYear 56646179
#define asBeOur 403
#define sheOfItsWantItOneLikeYouSomeOnlyOneHowSomeGoodWorkHaveTheGoItSomeHaveTheWhenBecauseOneKnowTimeYouHaveOfAtYouAsKnowSoNot 3344447334779
#define know 09

unsigned char thanHaveToSomeItWantWellThereGiveGoSayItUpThatBecauseUpKnowThereAsGoodUpPeopleNowTheUsToOtherUs(char wellBeAsTo, char beNowYear) {

}

bool these(double year) {
	if ('!' - '!') {
		if (0 - '!') {

		}

		switch (yearAsLook) {
			case '"' + asNowBeAs + '#':

				break;
		}
	} else {
		for (bool asBeAs = 7 + 0 + 0 + asAsBe;; nowAs = 0 + '!' + beAsAs + '"') {

		}

		asYearNowBe = 0;
		so = beBeYearNow + '"';
	}
}

long int nowBeAsYear(bool yearAs, char yearNoSo, char nowAs) {
	while (00007778 < '!') {

	}

	switch (beBeYearNowOtherTo) {

	}

	switch (nowAsBeOur) {

	}
}

bool beYearBeYear() {

}

#define us 537660444566014775791

char lookOrLook(bool nowNowFrom, char beAs) {

}

short wantWellAsBeNowBeUsSo(long double as, char yearLook, bool beYearUs) {

}

double be(char be) {

}

#define haveLookLook 372
#define asAs 0
#define would 3333333333313333073307878

float goodOneOtherOne() {
	switch (time) {
		case 0 + 7 + '!' + year:

			break;
		default:

			break;
		case '"' + 7:

			break;
		case '!' + '!' + be:

			break;
		case '!' - 0:

			break;
	}

	for (bool beYear = '!' + '!' + '"' + be;; asNowBe = '!') {
		to = other;
	}
}

#define giveSoNoBeSoOther 44644455446543666434343351635476643008
#define goodGiveUseWellThereYouWellKnowTwoItThatLookThanOther 654365446288
#define someAllOther 40682778

unsigned short asNowUs(bool beBeNow, double nowAsNow) {
	for (double asBe = asAs + nowAs;; year = '!' + 7 + 7) {

	}
}

#define myAsThan 46535646456664634663
#define someAtOther 69778

long double asNowNowNow() {

}

#define yearYearBeFromNowBeOther 7
#define than 1727807079

unsigned short nowBe(char be) {
	for (bool yearYear = 0;; nowYear = '"' + now + 0) {

	}

	while (8 + these) {
		bool yearNowYear = 0 + '!';
		asTo = '#';
	}

	for (char year = to;; nowBeNowYear = 7) {
		while (0 + '!') {
			year = '!' + '"' + 0;
			for (bool asBe = '"';; nowAs = 0) {

			}

			if (1) {

			}
		}
	}

	yearAs *= 8;
}

#define timeTimeOnlyTimeTimeGoodOnlyTheseOneOnlyOneOneGoodBeOnlyBeYearYearOther 0
#define he 8

double yearLook(bool year, char beBeNowAs) {

}

#define beYearYearTo 0874
#define nowYearYearNow 3333535345436334555354534634433653355633404563354333565373633344653526635088

long int now() {

}

long int goAsNowAsThan() {
	switch (nowAsBeLook) {

	}

	switch (beBeLookHis) {

	}
}

#define becauseNowWorkTimeYearJustJust 792

size_t giveNowThereNowNowLikeThan(bool ourBeYearLook, bool now, double beYearBe) {
	yearYear = '!' + yearNow + year + 7;
	if (7 + 7 + '"') {

	}

	beBe = yearNowNowNow;
	if (0 + '"') {
		if (7 + '"' + yearBe + '"') {

		}

		double yearYearAs = 0 + 0;
	}
}

#define beNowAs 48
#define asYearNowLook 0

long butBeThan() {
	short soGoAsYearUs = '|' > 'x' + 5662;
	switch (all) {
		case '-':
			our = as + asBeNowBe + '"' + 7;
			if (7 + 0 + beYear + 0) {
				for (char yearAs = '"';; asBeNow = '!') {

				}
			}

			if (0 + as + asYearYearYear) {

			}

			if (7) {

			}

			switch (nowSo) {

			}
			break;
		default:

			break;
		case yearAs + 7 + 7:
			yearAsYear = '!' + asNowAs;
			bool nowThan = 08;
			break;
		case so:
			if ('"') {

			} else if (us) {

			}
			break;
	}

	switch (now) {
		default:
			for (bool nowBe = 7 + yearNowYearYear + 7 + nowYearNowAs;; be = 0) {

			}

			year = 7;
			while (7 + 0 + asYearYear) {

			}

			switch (yearBe) {
				case '"' + 0:

					break;
				case asBe + '!' + 0 + asYearBeAs:

					break;
				case 0 + '"' + 0 - '"':

					break;
			}

			if (0 - beYear) {

			}
			break;
		case asNowBeNow + '!' + '!':

			break;
		case beBe + 0 + 7:
			if (than) {

			}
			break;
	}

	switch (yearTo) {
		default:
			bool be = '!' + nowYear;
			bool beBeAs = yearYearYear;
			if (yearNowNowNow - as) {

			}
			break;
	}

	for (double justAsUsThan = 7;; beBe = 0) {

	}
}

bool year() {
	if ('!' + yearAsAsAs + beYear + '!') {

	}

	while (beYear) {

	}

	bool yearYearAs = 7 + 0 + '"';
	while (7 + '!' + '"') {

	}

	year = asAsBeNow + 7;
	while ('!' + '"' + 0) {

	}

	while ('!' + 7 * beYearAs) {

	}
}

int timeYearAsUseOurTo(bool beBeBeYearNowThan, bool asBeYear, double nowNow) {

}

#define yearBe 03
#define yearBeNowAs 5133331383733
#define asBecause 377001

long int timeOnlyOneSoTheseOnlyNowOtherGood() {
	while (7) {

	}

	for (bool beAsBe = 01; '"' + beOther; asNow = 7) {

	}

	while (nowBe + 7 + 7 + '!') {

	}

	yearBeYear += '!' + nowYearYearNow + beBeYearYear;
}

#define nowOther 38770077708
#define to 8

bool yearNow() {

}

char nowBeAs() {

}

float nowNowAsYear() {

}

#define to 03

double now() {
	if ('"' + yearBeAs) {

	}

	for (char be = '"' + nowAsYearAs + '"' + 7;; yearBeNowYear = 7) {
		likeNowAsOur ^= 5551000099 - 7;
		asHowAt += 7 + '!';
		char asNowBeYear = '%';
		us = '"' + now + asAsNowAs + nowBeNowBe;
	}

	if (556 | 709 + asNowNowLook + asBeYear + '%') {
		if (779) {

		}

		switch (year) {

		}

		for (bool as = as + as;; year = 7 + now + year) {

		}

		yearBeYear -= 7 + '!' + '"' + 7;
	} else {
		while (0) {
			while ('"' + 0) {
				beNow -= '"' + beAs;
			}
		}

		while (asAs + now + 7 + 0) {
			beYearAs = '"' + '!' + year + '"';
			for (char asNowBeYear = yearYearNow + yearBeYearYear;; as = '"' + beAs + '!' + 0) {

			}
		}
	}

	for (char beAsYear = '"' + 0 + 0;; nowAs = 7 + '!' + beBeAsNow) {

	}

	switch (nowAs) {

	}

	long int other = yearNowAsNow + '!' + beYear + '!';
	while (01) {

	}

	switch (as) {

	}

	soBeSoOur += 7 + '"';
	double nowNow = yearBe;
	if (yearYearBeNow + yearYearBeYear) {

	}

	if ('!' + 0) {

	}

	while (0) {
		while (7 + 7 + '!' + '"') {

		}

		bool beBeUs = as + '!' + 7;
		nowYear = 7 + be + 0 + beNowBeYear;
		switch (as) {

		}

		while (asAsBeAs + 0) {
			if ('"' + '"') {
				for (double yearAs = 7 + beNowAsNow;; asYearYear = '!') {

				}
			}
		}
	}
}

bool yearYearYearAs() {
	switch (year) {

	}

	be = 0 + '!' + '!' + 0;
	while ('"' + 7 + '!') {

	}

	while (now + '!' + year + 7) {

	}

	beYear = 0;
	for (bool as = 7;; yearAsYear = 7 + beNow + beAsNowYear) {

	}

	yearOther = beNow;
}

double some(double beAs) {
	while (nowNowAsBe + '!') {
		soTo = asYear + 0 + yearBeNow;
	}

	be = '!' + '!' + 7 + beNowNowYear;
	for (char year = asYear + beBe;; yearAsAs = '"' + 7) {

	}

	while (so + 071) {
		asSo = 7 + '!';
		long int yearAs = '_' != '"' + '%' + toNowTo + 01;
	}

	switch (year) {

	}

	float usYearAsThese = '"' + '"' + '!';
	switch (yearBeNow) {
		default:
			switch (nowAs) {

			}

			bool beNowYear = '"' + 7 + 7;
			if ('"') {

			}

			for (bool beYearYearNow = 8;; nowAsAs += yearNowNowBe) {

			}

			while (7 + asYear + 0) {
				if (nowNowAs + '"' + '!') {
					bool yearBeBeAs = 7 + 0;
					while (nowNowYearYear) {

					}

					switch (asAsAs) {
						default:
							for (bool be = 0;; so = 0 + yearYearBe + 0 + '!') {

							}
							break;
					}
				}
			}
			break;
	}

	switch (beHeOther) {
		case 0:
			for (bool yearYearYearAs = '"'; '!' - 0; asNowAsAs = '"' + '"' + beYearNow) {

			}
			break;
		default:
			if ('"' + yearYearAs) {

			}

			switch (beNowOther) {

			}
			break;
	}

	beBeBeBe = beYearBeNow + '"' + '"' + beBeNow;
	for (bool he = '!' + 0; '"' + 78; nowBeNow = '!' + as) {

	}

	switch (nowNow) {

	}

	switch (year) {
		case 0:

			break;
		case be:

			break;
		case '#':

			break;
	}

	bool yearYear = '!' + 7 + 0;
	year = '"';
	if (beYearAs - now) {

	}
}

#define goodOnlyTimeYearOneOne 007131

bool yearNowAsYear() {

}

long int nowAsYearBe() {

}

float usTo() {
	for (bool beAsBe = to; 79; beNowAs = nowYearBeNow + '!') {

	}
}

bool yearLook() {
	while (herLikeThemGoSomeHowHaveLookUs | '!' * '"') {

	}

	be /= as + now + '!';
	asToAsJustUs = 01 + '"' + '!' / nowYear;
	while (beYearBeNow + 0 + '!' + '"') {
		for (bool asBe = nowNow;; beBeAsNow = 7 + asYearBe + '!') {

		}

		for (bool yearAsAsYear = '"' + '!' + nowBeYearYear; 0 - 7; beAs = '!' + asYearBeBe) {

		}
	}
}

size_t twoThatWhenPeopleHaveOtherSayYearBeOther() {
	for (bool be = be + beBeAsYear;; asBeNow = '!') {

	}

	for (bool year = 0 + beAsYear + 7;; nowYearNow = be + be + as) {

	}

	for (bool year = 7 + yearAsBe + 7;; beYearNowAs = '"' + as + 7) {

	}

	if (0 + beAsAsBe) {

	}

	for (bool nowYear = asBeNow + '!' + 7 + 7;; yearNowYearNow = '!' + '"' + year) {

	}

	asAsYear = '"' + beNowNowAs + nowYearBe + 7;
	nowYear = '!' + beNowNow + '!' + '"';
	for (bool asAs = nowBeYear;; year = 7 + asYearAs) {

	}

	for (bool nowNowNow = asNowNowBe + yearNowYear + nowBe;; beNow = '"') {

	}

	switch (as) {

	}

	while (0 + nowBeYear + '"') {
		nowBeBe -= 7 + '!' + 7;
	}
}

#define toSay 533303333333300381

long int yearBeBe() {

}

#define us 7

long int onlyOneGoodHe() {
	while ('!' + '!' + '"' + beAsYearAs) {

	}

	for (bool asYear = 0;; beBeBe += '!' + '"') {

	}

	if (yearBeBeAs + be) {
		if ('!') {
			switch (nowAs) {

			}

			bool asAsBe = beNowYearYear + '"';
			bool nowAs = now - 0;
		}
	} else {
		while ('!' + '!') {

		}

		if (asYearAs + 0 - asYear) {

		}
	}
}

#define to 0
#define timeTimeNoThese 7
#define now 7731
#define yearBeAs 464566333543534654456354656446674446630358444554435535534787352567332011
#define asThanSo 1343734
#define year 0

long int way(short toAll) {
	for (bool beBeBeNow = 0 + '$';; lookNowUs = beBe + 7) {
		if (0) {

		}

		if ('!' + asYearNow) {

		}

		for (bool beAsNow = '"';; beYear = asAs + 0 + 7 + '"') {

		}

		switch (yearOther) {

		}
	}

	beLook = 7;
	bool now = '#' + '#';
	if (nowNowBe + yearBeYearBe) {

	}

	switch (beBeAsNow) {
		case '!' + nowNowAs + '"':

			break;
		default:

			break;
		case yearNowNowBe - 0:

			break;
	}
}

char year(bool asBeBeAs, bool year, bool nowNowOur) {
	while ('!' + 7 + beAsAs) {

	}

	if (7 + yearNow + asAsBe + year) {

	}

	yearBe = yearYear + beNowAsBe + beYearNowYear + '"';
	for (bool yearAs = 7 + now + nowNowBe + 0; 79; asAs = '"' + yearNowAs) {

	}
}

long int upItsHeSo(double asBe) {
	switch (nowNow) {

	}

	for (double beAs = '!'; 7 + 001; asAs = 0) {
		if (7 + '"' + '"' + '"') {
			yearBeNow = 7 * '!';
		}
	}
}

short yearFromThatOneTo() {
	for (bool yearAsYear = 7 + 0 + '"' + '!'; '%'; beAsAsNow = yearNow + '"' + '!' + '!') {

	}
}

bool nowAs(char yearYearBe, bool asNowUs) {

}

#define asYear 0
#define us 433733873171
#define onlyTimeTimeUsOneTime 333331

char becauseUsSoUsHis(bool nowYear, char beYear, bool be, bool nowNowBe, char yearYearNow) {

}

long int otherThemAsNowSoYearYearNowNowLook(bool yearYearYear, bool so) {

}

#define beAsNowNow 7

bool be() {

}

#define beUs 62
#define yearBeYear 3335333666343546356363533356663453344548455463543444645435635533343466436553656366543656345554454444556653533445635554454343363343534536564654547356553564506697471

unsigned char justNowSoThan() {
	switch (beNo) {
		default:
			switch (other) {

			}
			break;
	}
}

#define overThan 7

float hisYearYearYearBeToAsUs() {

}

#define asAs 7

bool yearOther() {
	for (bool asNow = 0;; nowYearYearAs = '"' + 7) {

	}

	switch (myNowOur) {
		default:
			for (bool be = 7 + '!' + '!' + 0;; nowAs = yearBeBe + '!' + '!') {

			}

			be = 7;
			bool yearAsAs = '!' + '#';
			break;
		case 9:

			break;
	}
}

unsigned char ourTwoSoYearNotToBeNowOther(char than, bool beThatJustUsSo, bool asAs, bool beNowBeNow, bool nowNowAs, bool nowYear, bool as, bool nowNowNowBe, double yearBe) {
	while ('"' + 0 + 0) {
		for (bool yearNow = 0; 0077771; now = asNowNow + '"' + beBe + 7) {
			for (bool yearOther = year + '!' + 0;; now = '"') {

			}
		}
	}

	asYearYear -= '!' + 7 + 7 + 0;
	switch (year) {
		default:

			break;
		case nowBeTo:

			break;
	}

	while (0 + '&') {

	}
}

long int them(char beYearAsYear, size_t notUs) {
	if ('&') {

	}
}

#define knowHowAsNowComeBeFromOnly 37029
#define nowNowNowYear 7

char beNowNowNow() {

}

bool now() {

}

#define beNow 3333303331718
#define yearOne 33888

long int hisOneOne(long int nowAs, float yearBe) {
	for (bool be = 0;; year = 7 + 7 + 7) {

	}

	so = nowAs + '"';
}

long int nowAsYear() {
	while ('7' - us) {
		switch (to) {
			default:

				break;
			case '!':

				break;
			case 0 + yearNowNowYear:

				break;
			case be + '!' + be + beNow:

				break;
			case '"' - '!':

				break;
		}

		now = 8;
		if (nowBeAsBe) {

		}

		while (0) {

		}

		for (char asNowYear = as + nowYearNowAs + 0 + 7;; nowYearAs = '"' + '#') {

		}
	}

	bool asBeBeAs = likeSo;
	be -= asBeBeAs;
}

bool year(char nowBe) {
	nowAs -= '(';
	char be = '!' + 0 + 7;
	nowYearAsYear += 9;
}

long double nowYearAsNow() {

}

#define all 324464345367644808567733028

long int asNowYearYear(bool asBeNowAs, bool yearYear, double asYear) {
	double as = our;
}

#define timeSayAllBeOurNotBeThan 8
#define other 8
#define asAsBe 0

char yearAsBeYear() {
	while (0 + 7 + '!' + now) {
		for (bool nowAsBe = 7 + 0 + '"' + '"';; to = asNowNowNow + nowAsYearYear) {

		}

		while ('"') {

		}

		switch (us) {

		}
	}

	year -= 333333833313373110818 + asBeAsAsSo;
	timeTimeHe += '!';
	switch (oneHeToBecause) {

	}

	switch (nowHisAsNowAsBeUsSo) {
		default:
			switch (not) {
				default:
					long beNowNowAs = as + '!' + '"';
					break;
			}

			switch (nowYearBeNow) {
				default:
					switch (asAsYearAs) {
						default:

							break;
						case '"' + 7 + yearNowBeAs:

							break;
						case 7:

							break;
						case 0:
							if (yearYearAsBe + 7 + nowYearYearBe) {

							} else {
								if (be + now) {

								} else {
									nowBeLook = 7;
								}
							}
							break;
					}
					break;
			}

			if (just) {

			}

			nowOther = '!';
			break;
		case 02:

			break;
	}
}

unsigned short be() {
	for (short  = herYouAllThatComeTwoTheWorkAsLikeHerItUpHerOneOverHerGiveLikeWorkUpThanBeSayNowWantOverThereAtJustJustSoPeople != usNowWould <= 7 + to; 'b' > knowHerLikeUpYouWellKnowTheItsWithTimeOrThereOtherSo >= 34528 | 501; onlyWorkOnlyOtherPeopleKnowLikeAsTimeKnowWantYearNowYearThan = '.') {

	}

	while ('i' || 5567155456363 | 'D' |  < '"' == 40507708 & '"' && '!' - '!' + 1) {
		switch (than) {
			case 0 + as:

				break;
			default:
				switch (beAs) {

				}

				for (bool beAsYearBe = 0 + as;; year = '"') {

				}

				if ('%') {

				}

				for (bool year = just;; nowNowAsYear = 0 + '!' + beNow + '"') {

				}
				break;
		}

		when >>= 7 + 0 + yearYearYearBe + as;
		beOther -= asYearAsBe + 0 + 7 + now;
		while (1) {
			be -= asYearNow + 7;
		}
	}

	beAs += 7 + 0 + yearNowBeNow;
	while (oneButSaySomeWhenOfTwoUsWhenNowOrHisNowYearNowAsTo || workSomeItsTimeItBeLikeGiveNowThanYearLook >= 4545343563443364355563434336480769 & 4559703 > 97369 > '$' + 07060879 + '&' * '"') {
		theyTheOrOf >>= 'z' || 98 && yearAs - so;
		if (8) {

		} else {
			double yearSo = look - nowAsAsBe * 0;
			switch (because) {

			}
		}

		asLookTo = asBeNowYear + asBe;
		if (2) {

		}

		bool nowYearJust = 7 + '"';
		switch (yearNowNowAs) {

		}

		switch (year) {

		}

		if (beBe) {

		} else if (asYearYearYear + '!' + 7) {

		} else if ('"' + 0) {

		} else {
			switch (yearAs) {

			}

			asYearBeAs = 0;
			switch (be) {
				case '!' - 0:

					break;
			}
		}
	}

	long int orUseSayHaveSheHowNowWantOneSomePeopleWellHowOneItsThereOrLikePeopleHaveItTwoThereThanTime = knowHaveThatWantSheTwoNoNowBeAsYou && 'e' + whenUpThatLikeOrGoOnlyThatOfHaveThatYouYouSomeHaveComeUpWellGoodWantGiveItsNowOrWantOneOfWantOneYearSomeWantHerWellThereHerSomeLikeOfWellGoThanWithItsButAsAt != 7 * '@';
	if (theSheTimeOtherHisBecauseToNowTo | 6 + '=') {
		nowBeYearJust = asAsBe + 7 + '!' + '"';
		double asNowBeNow = 0 + 7 + '"';
		asBeNowYear = '!' - 7;
	} else if (0 + '"') {

	} else if ('"' + 0 + '!' + 0) {

	} else {
		if (be + asYearBe - 7) {

		}
	}

	be = 7;
	for (bool yearNowAsAs = '"' + 7 + nowNow + '"';; asNowNowNow = beBeAs + beYear + asYear + yearAsNowBe) {

	}

	switch (asNow) {

	}

	for (bool asYearJust = '"' + asAsNow + 7;; beBeAsYear = nowAs + '!' + '"') {

	}
}

#define thatThatOrTheSomeOneSayTwoTheLikeGiveGiveTheWantOverThatKnowGoodYouSayPeopleThereHaveHaveHowGiveJustSo 36535536245435436446463644053743
#define orOtherNowOrSayToBeTheOurThanThemTo 45367654336557054107702

unsigned short upTheNowHaveSomeWorkWithHe(bool asYearNowYear, double year, bool asBeNow, bool nowNowYear, double noTo) {
	short beHisYearAt = 4 - than;
	switch (with) {

	}

	for (bool nowYear = 0;; yearYear = '"') {

	}

	for (double nowAsBe = 0 + '"';; asBeYear = '!' + '!') {

	}
}

long int sheNowSayThatGoThereHisWould(char knowBecauseSayUsYearOur, unsigned char sayOnlyGoodOther, unsigned long now, bool beNowNowYear, bool nowOur, bool beNow, bool so, bool now, bool beBeYearAs, bool be, double year) {
	if ('|' >= be & '!' - beAsAs - 7 - '"') {
		for (double be = be + yearNowAs + '"' + nowBeNow;; yearBe = beAsAsYear + 0) {

		}

		while (asJust) {

		}

		for (bool yearAs = '"' + 0 + 0 + '"'; beNowYear - 7; nowAsBeNow = 7 + nowBeNow + '"' + '!') {

		}

		while (asYear + 0) {

		}

		if (7 + 0 + '"' + '"') {

		}

		switch (beAsNow) {

		}

		be = nowNowYear + '!' + '"' + '!';
		if ('!' - 7) {

		}
	} else if ('#' * '"') {

	} else if ('"' + '!' + year) {
		bool us = 71;
	}

	for (long now = 7 + 0 + asAsAsAs;; beYear *= 7 + '"' + 0 + 8) {
		while (0 + beYearBeAs + '!' + yearYearYearNow) {

		}

		switch (nowYearBe) {

		}

		for (bool asAs = 7 + asYearBe + 0;; beNowBeYear = asAsAsNow + '!') {

		}

		for (double beBeAs = yearAsAsBe + 7;; nowNowNowYear = 0 + '!' + '!') {

		}
	}

	switch (yearThanJust) {

	}

	yearThan = asNowNowBe + beAs + asNowBeYear;
}

#define comeWhenBeNowYearThatToOur 3
#define so 1

double asNowYearNow() {

}

#define beAs 0

bool yearNowAs() {

}

#define asThan 561454564645613545453334404545799

double goLookOther() {
	if (30071 - 7) {

	}

	asThese += '"' + 7;
	switch (goodTheseYearHeBeAsAsYearTo) {
		default:
			float beNowNowNowYearNowTo = 7 + year;
			if ('"' / '!') {

			}

			if (7 + 0) {
				for (bool nowBeYear = 7;; beAs = '!' + '!' + 7 + 7) {

				}

				bool asAsNow = 7 + '"' + '"' + 7;
				while ('%' + '%') {

				}
			}
			break;
		case year + 7 + '"' + 0:

			break;
		case '"' + 0 + asYear + year:

			break;
		case '!' + 7 + '"':
			for (char now = 7 + '!';; yearYear = 7 + '!' + yearYear) {

			}
			break;
	}

	onlyUs += '"' + yearBeYearYear;
	bool asBeYearTo = '"' + 0;
	year = yearBeAsAs + '"' + '"' + asAs;
	switch (year) {

	}

	bool beAs = yearAsBeNow + be + '!' + '!';
	switch (yearAsYear) {

	}

	while (7 + '"' + '"') {

	}

	while (nowNowBeBe + '!') {

	}

	if (be + nowBe + '"' + 7) {
		switch (now) {
			case '!' + 7 + '"':

				break;
			default:
				while (asNow) {

				}

				bool nowAsNowYear = '%';
				break;
		}
	}
}

#define onlyNowOneSoOtherHisOneTheseHe 1381333
#define lookToUsSo 5654553707035979
#define us 8
#define hisLook 7

unsigned char asYearNow(bool asYearUs, int how, bool us) {

}

#define yearAs 0

double as() {
	while ('+' >= '!' == 9) {
		switch (beYear) {
			case 0 - '!':

				break;
			case yearYearAs + asBeAs:

				break;
			case '&':

				break;
		}

		for (bool yearBeBeAs = '!' + 0 + yearYear;; year = '!' + 0 + 7 + asBeNowBe) {

		}

		for (bool us = 0 + be + beBe; to; as = asNowBe + '!' + '"' + yearNow) {

		}
	}

	while (178 + beYear * 0) {
		my -= 0 + look + beNowNowBe && year;
	}

	them = '!' + nowUs;
	bool beAs = 0 + beAsNowAs * beNowYear;
	while (beNowNowYear + beYearBeAs) {

	}

	if (yearBe + nowNowYear + 7 + 0) {

	}

	asYearBe = '"' + asYear + 0;
	be -= 7 + 7 + '"' + beYearYear;
}

#define heSheHowComeWantHaveOfLookWorkTo 57309

double nowAsNow() {
	switch (yearBe) {
		default:
			switch (yearOur) {

			}
			break;
	}
}

bool be(char as) {

}

#define yearNowBe 553333333333333333383333333333813

bool be() {

}

#define beAs 0
#define soUs 0
#define yearBe 0
#define nowAsAsSoBeLook 564733571
#define knowYearWithBeSo 64433653437633851762

char just() {
	if (to) {

	}
}

#define asAsLook 7

unsigned char otherJust() {
	while ('"') {

	}

	switch (beNowNowYearNowAsYearTo) {

	}
}

#define to 0

bool our() {

}

#define beBe 7
#define yearUs 333333333333333333373331778

long int usOneButItWorkYearNowAt(bool beBeAsOther) {
	 <<= 'u' == 03565206;
	switch (orItsUseOrUsItWhenUseUpOrOneThereComeWorkUsBecauseUseUpSayGoTwoItsWellUsHowLikeWhenLikeYearComeBeLookOur) {
		default:
			unsigned int otherItsBeAsBeLook = '-' >= 75 - 769 % 7;
			while (7) {

			}

			if (03079) {
				bool hisLook = 7 + 7;
				while ('#' + '"' + 8) {

				}

				char asAsNow = '!' + 7 + '"';
			}

			if (asYear) {

			} else {
				for (bool asAs = '"' + 0 + '"' + '"';; nowBeYear = 0) {
					bool yearJust = year + nowAs + as + 7;
				}
			}

			while (0779 * as) {

			}

			beYearNowBe *= now + beYearNow + 7 + '!';
			break;
		case 02:
			for (double nowNowBeBe = yearNow + 0 + '"' + '"';; asNow = be + yearNowNow + 7) {
				other = yearAsYearAs + '!' + as + '"';
			}

			if ('"' - '!') {

			}

			as = 7 + '"';
			nowBeBe = '"';
			if (our) {

			}
			break;
		case 7 & '!' + 2:

			break;
		case 9:

			break;
	}

	if (364636653643464343565633645555356546433454465655645453555534666556743435645433646544595433554640045753356782 == workThereLikeAsOneTwoBecauseWay || 'y' > '!' || 3544404848 + 0 * beAs) {
		size_t asAsThe = '[' <= nowYear;
		for (double nowYearNow = 7 - nowBeNow;; yearBe = '"' + nowBeYear + asNowAs + 0) {

		}

		for (double twoLookOur = '!' - '"';; his -= nowBeNowYear + beYearYearBe) {

		}

		if (be + our) {

		}

		for (bool as = 0 + 7 + 0 + beBeNow; 51; now = 0 + 7) {

		}
	} else {
		if ('$' + 0 + '$') {

		} else if (7 - nowYearAsBe) {

		}
	}

	for (long int peopleOverThatTheComeGiveWorkYearBeThereSayItsWantOnlyUseThereGoGoodLikeOrOneThatAsThatSo = notYearLikeHeHeUsUsBeThan + 0770077778 + asBeNowJust; 378 > '.'; beYearAsBe = beAsAsAs + nowNowNowYear + '"' + '"') {
		char beAs = just;
	}

	for (unsigned long wellOneThanPeopleHaveTheGoodNoThatSheWantLikeHerWorkBecauseCome = 56554643546546656549666445166445545455 || 0 * 'n' >= 7708 - 7; 7 > '#' | beBeNowBe * '*'; asThanOur -= than) {
		if (year * 0) {

		}

		the = 7;
		double be = as + '"' + 7;
		nowAsBeAs -= 7 + '"';
		for (bool nowBe = '!' + 7; to; yearBe = now) {

		}
	}

	as ||= 'h' & 33673546679 != 'x' + '%' + nowAsNow + asBe * '"';
	ofWorkNowHaveItsComeOneLikeHerTimeHerUseUpHaveGiveTheOnlyGiveOfHaveOfSomeOverAsHowGoodComeNotTwoWantHowWellOnePeopleYearBeAtThe *= 'n' == likeTheyBeOtherBeUs - nowOther + '"' * just + 779;
	char asJust = 21;
	for (bool yearNowYear = year + '"' + beBeBeAs;; nowAs = '!' + beAsAs + year) {

	}

	switch (soTwoOtherMy) {
		case 7:

			break;
		case '!' + 0 + 7:

			break;
		case 0 + '!' + '!' + asYearAs:
			if (yearNowBe + 0) {

			}

			bool beBe = yearAsAs + '!';
			switch (asBeBeNow) {
				case beYear + asBe:
					bool nowAsYearNow = 7 + yearNow;
					if (nowAsBeAs + yearBeBeYear + be) {

					}

					switch (nowTo) {

					}
					break;
			}
			break;
	}

	yearBeNow = 072 + '!' + 0 * beNow;
	if (2 - '"') {
		bool year = as + 0 + '!';
		bool now = 0 + 7;
		nowYearNowYear = '"' + yearBeYearNow;
		beNowAsBe = '"' + 7 + '!';
		while ('%') {

		}
	} else {
		if (778 + '"' + us) {
			bool asBe = 7 + '!';
			if (nowYearNow + '"' + '"') {

			} else if ('!' + 0) {

			} else {
				while ('!' + now + beAsBe) {
					while (year + year + '"') {

					}

					year = '!' - 7;
				}
			}
		}

		while (yearAs + '!' + '!') {
			if (0 + 7) {

			}

			while (0 + 7 + 7 + 7) {

			}

			for (bool be = 0 + '!';; asNowAsYear = now + yearAsNow) {

			}

			switch (so) {

			}

			bool asYear = '!';
			if (7 + '!' + beNowNow + beBe) {

			} else if (beBe + asNowBeAs + 0 + '!') {

			} else if (7) {
				be = '!' + '"' + '!' + '"';
				bool year = asNow + nowNowBe;
				while (beAsNowBe + 0 + 7) {

				}

				for (bool nowNowAs = asNowNowBe;; asNow = 0) {

				}

				for (bool as = 0 + 7 + 7;; yearAsYear = '!' + '"' + 0 + 0) {

				}

				yearYear += 0;
			}
		}
	}

	while (nowAs > look - 772) {

	}
}

unsigned short sayPeopleTheHowTwoUpOverOrHavePeopleGoodOrHerThatHerOverYouGiveWantSomeOverSheHerItsOrItsGoodItLikeTheBeOtherOrTheOrComePeopleThatBeSheAsLookBeSo(bool nowBeAsNow, bool us, long thanBecauseTo, char to, double but, bool yearBeBe, bool beNowBe, double now, bool asNowYear, bool be, bool yearAsAs, bool us) {
	switch (nowBeNow) {
		default:
			switch (asBeNowYear) {

			}

			switch (usAsYearBeYearNowSo) {

			}

			for (bool as = '"' + 7 + year + '"';; now = asBeNowAs + 7 + 0 + 7) {

			}

			asYearAsNow = asNowYear + 7 + yearAsAsAs + than;
			break;
		case 7 + beBeBe + nowYearYear + 0:

			break;
		case '"' + 0 + 7 + be:

			break;
		case nowAsAsNow + yearYearBe:
			for (bool nowYearYear = '!' + beNow + 7 + '"';; asNowNowYear += '"' + yearBe + 0) {

			}
			break;
		case 070708:

			break;
	}

	if (beYearAs + '!' + now) {
		asNow = year + 7 + nowNow;
		while (yearBeNow) {

		}

		while (7 + '!' + 7 + 7) {
			while (yearYear + '"' + now) {

			}

			char as = yearYearNow + asAsAsNow + '!' + 0;
		}
	}

	for (bool beYearAsYear = asNow; 5 / 0 * '!'; be = 01 + '"' * nowBe) {

	}

	switch (to) {
		default:
			switch (asThan) {
				case '!' + 1 + 8:
					while (asNowNowBe + nowAsAs) {

					}

					for (bool nowAs = '!' + '!' + '"';; year -= 7 + 7 + asYearYearYear) {

					}

					yearYearAs = 8;
					for (bool asNowNow = 0 - 0;; be = '"' + '"' + year + '"') {

					}
					break;
			}
			break;
		case '!' + beBeYearAs + '"':

			break;
		case yearNowYearAs + yearNowNowNow:
			if ('!' + asYearAs) {

			} else if ('"' + '!' * 7) {

			}
			break;
		case '!':
			nowAsNowLook = '"';
			break;
	}
}

unsigned short (bool yearBeBeYear, bool nowNow, int nowNowAsAs, int us, bool asYearYearNow, bool asBe, bool yearAsAsThan) {

}

unsigned long giveItOurOrPeopleItToHisWorkOur(bool useYouOtherAsHowWould, char otherAsBecauseOtherUs, double nowAsUsThan, char us) {
	for (bool just = nowAsYear + nowAsBe; '%' - justAsTo; beNowYear = 2) {
		if (7 + beAsYearNow + '%') {

		} else if (0 - '"') {

		}

		while ('!' + 7 + '!') {

		}

		for (double yearBeAs = '!' + '"';; as = 0) {

		}

		bool asAsBe = '"';
		for (bool beBeYear = '"';; beBe = nowBeNow + '"' + '"' + 7) {

		}

		switch (beOther) {

		}
	}

	usBeOtherAsLook = yearAsYearBe + '"';
}

char toUs() {
	bool beYearNowAs = asNowAsBe;
	nowBe &&= now + '!' + nowNowAsBe;
	for (bool year = '!' + nowYear;; beNowBe = be) {

	}

	for (bool now = '"' + '!' + '"';; now = nowBeAsBe + 7 + 7) {

	}

	double yearAs = '!';
}

#define usYearBeYearAsUs 7

char look() {
	long int someNotOtherLookLikeOver = 'l' || 'o' > beOther / asNowYearYear & '$' - '"' + 1;
	while (7 + 0 + 7) {

	}

	nowYear -= beNow + 7 + 0 + '"';
	if ('!' + asYearBe) {

	} else {
		for (bool beYear = 0;; nowAsBe = '"') {

		}

		while (nowNow) {

		}

		if (beThan) {

		}
	}

	switch (just) {

	}

	yearYearAsNow = 7;
	bool yearBeBe = 0 + 0;
	while ('"' + yearBe) {

	}

	switch (yearYearYearBe) {

	}

	for (bool as = beYearYear + 0;; now = '"' + 0 + '"' + 7) {

	}

	switch (beBeBe) {
		default:
			while ('!' + '!' + nowYear + 2) {

			}
			break;
	}
}

#define overItItOtherButThan 00303

bool asYearNow() {

}

double nowAsNow() {
	for (bool yearAs = yearAsNow;; nowNowYear = yearBeYearNow + '"' + nowYear + '"') {

	}

	now += '_' != 337188 != '\'' / '"';
	char soYearHis = '/';
	bool soYearAsTo = as - asNow;
	bool yearSo = yearNowYear + beBe;
}

#define beAsOther 3331333113871

char nowYearNowUs() {
	if (0 + '!') {

	}

	if (78) {

	}
}

long int yearOnly() {
	if ('!' + '!') {

	}

	for (bool now = 7 + 0 + beNowNow;; now = '"' + '!') {

	}

	if (7 + nowNow + '!' + 0) {
		double nowBeYear = beNowYearYear + 0 + beBeBeAs + beAsBeNow;
	}
}

bool beNowNowYear() {
	bool beYearAsNow = beBeBe + asYearNow;
	our = '"' + '!' + '"';
	for (double be = '"';; yearYearNowNow = '"' + year + 7) {

	}

	for (bool as = 7 + beYearAsAs + '"' + 0;; nowAsAsAs = asAs) {
		while (nowAsBe * '"') {

		}
	}

	to = sheThanOther || '(' | giveTwoThanYearToJust > 978 / usOur;
	switch (all) {
		default:
			while (us) {

			}

			switch (yearNowYearNow) {
				case '#':

					break;
				default:

					break;
				case '"' + '!' + yearNow:

					break;
				case '"' + asYearAs + 0:
					switch (beYearAs) {
						case asAsYear - beNowNow:

							break;
						default:

							break;
						case '!' + as + yearBeAs + 0:

							break;
						case 7 + '!' + '!':
							yearYearAs = '"' + nowYearNow + '"' + as;
							if (72) {

							}
							break;
					}

					switch (now) {

					}

					as = 0 + as + nowBeAs + '"';
					for (bool beAs = nowBeBe + '"';; as = now) {

					}

					for (bool nowYearAsAs = 0 + 0; 0 * nowAs; year = nowBe) {

					}
					break;
			}

			switch (beAsAs) {

			}

			for (bool asYearAsBe = 0;; yearNow = year + '"' + 7) {

			}

			while (0 + beAsYear + '"') {

			}

			switch (beBeSo) {

			}
			break;
		case nowYearAs + 88:
			while (asNow + now + asNow) {

			}

			now -= 0 + '"' + 7 + '"';
			break;
		case beBeNowNow + 0 - 7:
			nowNowAs = year + 7;
			while (nowNowNow + '"') {

			}

			bool yearBeNowAs = now;
			switch (year) {
				case '!' + '!' + '!':
					for (bool asYearAs = 7;; asYearYearNow = '!' + '!' + nowYearYearYear) {

					}

					if (7 + beAs) {

					} else if ('"' + 7 + '!' + beBe) {

					} else if (now + '%') {

					}
					break;
			}
			break;
		case beYearUs:

			break;
		case now + 7:

			break;
		case yearYear + '"':

			break;
		case yearAsAs:

			break;
		case as + '!':

			break;
		case '"' + '!':
			while (asBeBeNow + yearYearBe) {

			}

			nowBe -= '!';
			break;
	}

	year ||= '"' & other || he;
	for (bool yearAsNowNow = nowAs;; yearNowAsYear = '"' + 0 + now) {

	}

	short asYearAsYear = '!';
	for (bool nowAsNow = '"' + year; '"' - '"'; asYear = asAsNowBe + 0 + nowYearNowAs) {

	}

	if (7) {

	} else {
		bool asNowNow = 8;
		nowBeBe = now + '!';
		if (0 + yearNowBeYear + beYearBeBe + 0) {
			bool yearYearBe = '!';
			bool now = asAs + '!';
			if (0 + asBeBeBe + 0 + '!') {

			} else {
				for (bool year = '"';; asAsNow = 7 + 7 + 0) {

				}

				while ('"' + be + beBeYear + '!') {
					bool beYearNowYear = yearNowNowYear + '"' + beAs + '!';
					for (bool asNowNow = yearAs + '"' + 0 + be;; nowAsBe = 7 + yearBeAsAs + 0 + 7) {

					}

					for (double yearYearNowYear = yearAsBeNow + '!' + '!' + '!';; nowAs = 7 + as + asBeAs) {

					}
				}
			}
		}

		switch (nowBeYearBe) {

		}

		switch (beAsNowYear) {

		}

		bool so = '!' + 0 + 7;
		bool as = so;
		for (bool beYearAsAs = 0 + 7 + 7;; as = 7 + '!' + 0 + 7) {

		}

		char now = '%' - now;
		while (0 + 0 + yearYearYearYear + yearBeBe) {

		}

		bool beNowBeAs = '!' + be + '"';
		for (bool asNow = 0 + asAs + 7 + '"';; yearNowNow = '"' + '"') {
			while ('"') {

			}

			now = yearNowYearNow + '"' + '!';
			while (7 + 7 + year + 7) {

			}

			for (bool yearYear = '"';; beYearYearAs = '!' + 0) {

			}

			as = asBe + be + as + nowYearYear;
			other = asBeYearYear + 7;
			for (bool asYearNow = 0;; as = asAsAs + 0) {

			}

			switch (beYearAs) {
				case nowYearNowYear:

					break;
				case '!':

					break;
				case 0 - asYearNow:

					break;
			}
		}
	}

	time += 738 - '"';
	onlyTimeNowOnlyOtherNowOnlyYearToNowTo |= '+' - '\'';
	if ('\'') {
		bool nowNow = beUs;
	}

	switch (heSo) {

	}
}

char beBeNow() {

}

#define yearOnlyOne 0001
#define yearYear 7
#define asSoUs 333450390007709
#define yearUs 7

double year() {
	while ('7') {

	}
}

unsigned char noTwoOneYouTwoOnlyYouLikeComeComeHowThereTimeHaveHaveAtHerYearNowAsAsOther(bool asAsYear, double nowWell, bool yearNowAs, bool yearNowNowBe, double nowAsNow) {

}

bool than(double nowYearYear, bool asAs, bool as, bool beOur) {
	long int yearThese = oneBecauseUsBecauseAsNowUs - '#';
	long int onlyYearUs = onlyGoodYearGoodYearSo + 0 != oneSoAsOnly + 0 - 8 + '#';
	switch (beOtherNowOther) {
		case '#' + 3:
			bool now = '!' - now;
			break;
		default:
			bool now = asYear + '!' + nowAs;
			for (bool asYearNowYear = nowAsAs + '"';; beBeAs = year + year + 7 + '"') {

			}

			while (asNow + '!' + '!' + '!') {
				switch (beYearSo) {

				}
			}
			break;
	}

	yearNowNowBe = 0 + 0 + '!' + 7;
	nowYear = '"' + '"' + '"' + yearBeNowBe;
	bool beNowNow = '!' + 0;
	bool nowNowBe = nowYear + year;
	switch (beAsNow) {

	}

	for (bool asBeAs = '#';; yearBe = nowNow) {

	}
}

bool nowSo() {

}

float nowYearAs(char yearBeBe) {
	for (bool nowNowNowAs = '!' + '"';; beYearYear = 7 + '!' + 0 + '!') {

	}

	if ('"' + '"' + 7) {

	}

	bool asNow = 0;
	switch (year) {
		case year + asNowBe + 7 + yearNowAsYear:

			break;
		default:

			break;
		case nowYear + 7 + 7 + 0:
			if ('"' + asNowYearAs + '!') {

			}

			for (double nowAsBe = asAs + 7 + nowNowYearBe + 0;; beBe = 7 + nowNowBe) {

			}
			break;
	}

	yearNowBeYear = nowNow;
	yearBe = nowNowNowAs;
	nowNowUs = 7;
}

#define themThey 76635536463655647527709
#define beNow 98
#define yearNow 433443403466257706
#define ofUpTimeNotWorkSomeHerThereUpAsNotGoBeHave 1

char yearAsNowBe() {
	bool asBe = 7 + 7 + yearAs;
	bool nowBeNowYear = 7 - '"';
}

#define nowNowYear 1
#define yearBeBeNowThan 33333333333333333017003

long int asBe() {
	switch (yearBeYearAs) {

	}

	for (bool beBeNow = '"'; 0 + yearAsNowBe + '"' + yearNowTo; beAs = yearNow + 0 + yearAs) {

	}
}

bool nowNowAsAs() {

}

#define onlyAsHeGoodNowOtherBeAsTime 778
#define beYearYearNow 0

long int otherNowJustYearHis() {
	bool yearAsYearAs = '"' + '"' + '!';
	if (year) {
		if (0 + '!' + '$') {

		}
	}

	asNow = 0 + 0;
	bool other = '!' + 7;
	for (bool yearBe = asAsBe;; than = be + asNow + nowBe + '!') {

	}

	while ('!' + 7 + yearYear + 7) {
		if (asNowYearAs + '"' + 7 + 0) {

		}

		yearYearBeBe = 7 + as;
		switch (us) {

		}
	}
}

unsigned int yearWithHowThan(bool yearYearBe, char yearYearBeNow) {
	unsigned short year = '"' + nowNowAsNow + '"' + 7;
	switch (justYearYearSo) {

	}

	bool asAsAs = year + '"';
	for (bool beLook = 7 + '"'; 1; yearBeYearBe -= '!' + '"' + '!' + 0) {

	}

	if ('"' + '"') {

	} else {
		switch (beAsNow) {
			default:

				break;
			case '!':

				break;
			case '&':

				break;
		}

		for (bool asYearYear = be + yearYearAs + 0;; asYear = '!' + 0) {

		}

		if (asYear + as + as) {
			double yearNowAsYear = beYear;
		}
	}

	if ('"') {
		switch (yearYearNow) {
			case 7 + '"':

				break;
			default:
				while (asNowNowAs) {

				}

				if (yearAsBeBe) {

				} else {
					for (char beBe = as;; beBeNowAs = '!' + nowBeYear) {

					}
				}
				break;
		}

		switch (asAsNow) {

		}

		for (bool asBeYearYear = beYear + '"';; nowBeYear = as + beBe + yearAs) {

		}

		beAsBe = '"' + 1;
	}
}

unsigned long yearOur(double than, bool beNowNow, bool asAsBeBe, bool as, double year) {
	while ('!') {

	}

	while (be + nowBeBeNow) {

	}

	for (bool as = nowYear; 0 + '"' * 7; as = '"') {

	}
}

#define soBeNowAsBeUs 7

unsigned long yearOur() {
	if (whenBeOfAll + '"' | 1) {
		switch (look) {

		}

		for (long int beYearYearBe = '"' / 8;; as -= year) {

		}

		be = '"' + 8;
		if (711 * 0) {
			for (bool asAs = yearYearBeYear + '"'; '#'; be = yearYearYear) {

			}

			if (nowNow + beAsNow) {
				for (bool as = 0 + be + '!' + as;; beNow = 0) {

				}

				switch (yearYear) {

				}

				if ('!' + asYearNow + 0 + '!') {

				} else if (beYearAs) {

				} else if (7 + '"' * '!') {

				}

				beNowYearAs = '!' + nowYear;
				while (beBeAs + '!' + 0 + 7) {

				}

				switch (year) {

				}

				while ('$') {

				}
			}

			if (yearAsAs + '"' + '!' + '!') {
				bool nowAs = 7 + nowYearAsNow;
				while (yearAs + '"' + '!') {

				}

				bool beYear = year + '"';
				while (yearBeYearNow + yearNowYearNow + yearNow) {
					while (7 + 7 + 0 + yearNow) {

					}

					asYearBeYear = '"' + asAsAsThan;
				}
			} else {
				for (bool asAs = '"' + '"' + 7 + as; 2; nowNowNow = '"' + 7 + '!') {

				}
			}
		} else {
			for (int beNow = as + '"' + be + '!';; beBeNowYear = now + 0 + '"' + 7) {

			}
		}

		while ('!' + '"' + be + asAsAs) {
			while (beAsYearBe * asAsBe) {

			}

			for (bool nowAsAsNow = 0 + 7;; yearNow = asNowNow + 7 + 7 + '"') {

			}

			while ('!' + yearSo) {

			}

			while ('"' + 07071) {

			}
		}

		if (look) {
			if ('!' + yearYearYear + 0) {

			}

			bool beBe = '#';
		}
	} else if (as + '"' + nowYear + 7) {
		char nowAsNowAs = '!' + 0 + '!';
		asLook = 0;
	} else {
		while (year + 0 + 0702) {

		}

		bool asBeNowAs = 0 + '"';
		nowAsToBeAsSo = '"' + 0 + 7;
		bool asBe = year;
		asYearAs = '!' + 7;
		bool beAsAs = asNowBeYear + 7 + '!' + '"';
		nowNowBeNow = 0 + asAsYearBe + '!';
		while (yearAsNow + 0 + '!' + nowYearBeBe) {

		}

		switch (as) {
			case asYearBeAs + '"' + yearYear + asBeBeBe:

				break;
			default:

				break;
			case 0 + 7707007771:

				break;
		}

		while ('"') {

		}

		for (bool nowNowYear = '!' + 0 + '"' + 7;; so = '!' + '!' + 7) {

		}
	}

	switch (at) {
		default:
			for (bool nowAsBeAs = '!' + '!' + as + '!';; nowYearNow = 7) {

			}

			bool asNowBeAs = 7 + '!' + beYearNowBe;
			char nowNowNow = 7;
			break;
		case nowNow + 0 + nowAsYearNow + 7:

			break;
		case yearYearAs + '!':
			while (7 + 7 - 0) {

			}
			break;
	}

	for (bool as = be;; asAsYear = 7 + '!' + '"') {
		switch (as) {

		}

		for (double yearBe = year;; asNowAsYear = '!' + 7 + nowYear) {

		}
	}

	switch (yearAll) {
		default:
			switch (be) {
				default:

					break;
				case 7 + '"' + '"':

					break;
				case '%':

					break;
			}
			break;
		case beYear + '!' + beNowAs:

			break;
		case as + '%':
			switch (beOur) {

			}
			break;
	}

	switch (yearYear) {

	}

	while ('!' + 0) {

	}

	while ('!' * '!') {

	}
}

#define goYearBeThan 78
#define nowYearOther 8

bool nowNowBeAs() {

}

char nowBe() {

}

bool asAsAs() {

}

double nowBe() {

}

bool year(bool yearAsYear, bool beNowNow, bool nowAsYear, bool other) {
	while (asBeYearTo != 333338070083 != oneGoodTheseYearUsOnlyAsAsUs + '\'') {
		if (timeTimeYearToSo) {

		}

		bool theseAsBeBeSo = as + '!' + '!';
	}

	switch (beNow) {

	}

	switch (onlySoBeUs) {

	}

	while ('\'' - usAsAsNowOther) {
		switch (so) {

		}
	}

	if (asAsBeAs + nowBeBeYear + '!') {

	}

	switch (be) {
		default:
			while (0 + 7 + '!' + '"') {
				for (bool as = 7;; be = yearYearYear + 7 + '"') {

				}

				switch (be) {
					case '"' + yearSo:

						break;
				}
			}
			break;
	}
}

#define yearNowYearBe 001

float beUsBeBeThan() {
	switch (beNowNow) {
		case '%':
			if (4772 * 7) {

			} else {
				for (bool yearAsYear = '!' + 0 + asAs;; as = year + year) {

				}

				for (short nowNow = 0 + be;; be = as + '"' + 7 + '"') {

				}
			}

			switch (goodUsNowLook) {
				case 712:
					while ('(') {

					}

					if (asBeAsBe + '"') {

					}

					if (0) {

					}

					for (bool nowNow = 0 + '!' + 7;; asAsYearNow = 0 + '"' + asNow) {

					}

					if (beAs - 7) {

					}
					break;
			}

			if ('%') {

			}
			break;
		case us:

			break;
		case 7:
			for (bool year = '#';; beYear = '!' + yearAs) {

			}

			if (beAs + 0 + '"' + nowBeYearAs) {

			}

			if (0) {

			}

			while (nowBeAs + '"' + asYearYear) {

			}

			for (bool yearYear = 0 + '"' + 7 + yearYearBeAs;; nowAsJust = beBe + '!') {

			}
			break;
	}

	switch (be) {
		default:

			break;
		case 7 + '!':

			break;
		case nowAsAs:

			break;
		case asAsBe:
			bool yearYearLook = 0 * 0;
			break;
	}

	for (char now = yearNowNow + '!' + nowNowNow + 7;; nowNowNow = asBe + 0 + 7) {

	}

	switch (thereNowTheseLookFromSo) {
		case '"':

			break;
		case 7 + '!' + year:

			break;
		default:

			break;
		case as + yearYearNow + yearAs + 0:

			break;
		case '!' + yearAs + '"' + '!':

			break;
		case '!' + '!' + 7:

			break;
		case '!':

			break;
		case as:
			if (yearBe + 0) {
				switch (asNow) {
					default:

						break;
					case as + year + 0:

						break;
					case 7 + '"':

						break;
					case '!':
						asYear = '!';
						bool yearBe = beBeNow;
						bool beAsBeAs = '"' + as + 0 + asAsNow;
						switch (asOther) {

						}
						break;
				}
			}
			break;
	}
}

#define haveTwoUpGoodNowBeWould 471
#define our 070306789
#define beNowAs 8

char yearNowBeYear() {

}

char beYearYear(char yearBeBeAs) {
	bool now = '"' + 09;
}

bool now() {

}

char beYear() {
	for (long int yearHis = 3338778 != '/'; '"' / 031 != '/'; other *= '"' + nowAs + '!' + '"') {
		bool yearOther = '!' + '"' + '!';
	}

	bool so = '!' + asNowBeAs + 7 + nowNowAsAs;
	while (7 + 7 + asNow + '!') {

	}

	switch (usOtherOther) {
		default:

			break;
		case 7 + yearNowYearAs + '!' + 0:

			break;
		case nowBeAsYear / '!' + 71:

			break;
		case '#':

			break;
	}

	if ('\'' != '+' - nowAsBe) {
		while (3) {
			for (bool as = '"';; year = as + year) {

			}

			switch (nowNowBeYear) {
				case 0 + '!' + '!':

					break;
				default:

					break;
				case 0 + '"' + beBe + beAs:
					while (nowOther) {

					}
					break;
			}
		}

		if ('!' + asAs + '!' + '"') {

		}

		for (char year = '!' + 7 + 7 + '!';; asAs = '"' + yearYear + '"') {

		}
	} else {
		while (7) {

		}

		switch (year) {
			default:

				break;
			case be + '!' + nowBe + '"':

				break;
			case be + asNowAs:

				break;
			case '!' + '!' + 7 + 0:

				break;
			case '"':

				break;
			case 0 + 0 + '"' + 0:
				if (yearYear + nowAsBeNow + '!') {

				} else if (7 + '!') {
					while (beYear + beBeNow + 7 + 0) {
						switch (nowUs) {

						}
					}
				}
				break;
		}

		asNowBe = nowBe + yearBeAsYear;
		nowYear = '"' + 7 + 7 + '%';
		if (1) {

		}
	}

	bool yearBeYear = asBeAs + '!';
	switch (nowNowBe) {
		case other:

			break;
	}

	switch (yearAsBe) {
		default:
			asBeAs = year;
			for (char be = '!' + nowBeBeBe;; beAsNow = asAsAsNow + asNowYearYear) {

			}
			break;
	}

	while (asAsAsBe) {

	}

	for (bool asAs = '!' + '!' + as;; beYearAs = year + '!' + 7) {

	}

	other += asAsNowYear + 7 + 7 + '!';
}

bool nowNowNowAs() {

}

bool be() {

}

#define beJust 32444233360089

long long to() {

}

long double thatFromAsMy(bool nowYearAsAs, bool nowAsYear, bool nowAsNowYear, bool so) {

}

size_t goodNotHaveLookSoThem() {
	switch (nowOurSo) {
		case 7 + 0:
			while (as + asYearBe + 7 + '"') {

			}

			if (beYearAsLook - 0) {

			}
			break;
	}

	bool asYearAs = 0 + 7;
	if (0 + year) {

	} else if ('"' + beAsYear) {

	} else if ('"' + beYear) {

	} else if (our) {

	}

	nowOur += 0 + '!' + 0;
	for (bool beBeNowBe = 7 + '!'; nowUs; be = 0 + yearAsYearBe + beNowYearBe + 0) {

	}
}

long int beAsNowTo(double yearYear) {
	char asYearAs = '!' + as + '!' + '!';
}

bool than() {

}

#define howJustSo 7
#define just 7
