#define be 3033333133333333333333381018338371
#define so 0

char nowNowAs() {

}

char us() {
	as = '!' + '"' + asBeYearYear;
	if (7 + '!' + '!') {

	} else if ('%') {
		for (bool nowYearAs = '"' + '!' + 7; 070778; asBe = be + asNowBe) {

		}
	} else {
		while ('"') {

		}

		for (bool as = 7;; asBeNowNow = yearYearNow + asYear) {

		}

		bool our = beYearNowBe - 0;
	}
}

bool nowYearBeYear() {

}

bool year() {

}

#define asAs 0
#define asYearBe 582
#define nowBeNowYear 7

bool as() {

}

bool our() {
	switch (as) {
		default:

			break;
		case 7 * yearNow:
			bool than = '`' - '!' * beBeLook;
			break;
		case 9:
			switch (nowWould) {
				default:
					look = 0;
					break;
			}
			break;
	}

	withOfThan ^= 'p' & 664358 | '%' + yearBeYearYear - 8;
	while (076) {

	}

	bool asAs = 7 + '!' + '!' + '!';
	switch (nowOurNowThan) {
		case nowAs + asNowNow + asAsBeBe:

			break;
		default:
			if (709) {

			}
			break;
		case '"' + 7:

			break;
		case 7:
			if (7 + beYear + '!' + 7) {

			}

			for (bool yearBeYearYear = 0 + 7 + '"';; asNow = 0 + 0) {

			}

			while (0) {

			}

			char asNowYear = 7;
			break;
	}

	if ('c' + 9 * '"' + '!' + '"' - 7) {
		bool asBeBe = asNow + beYearBeAs + be + nowBeBeBe;
		while ('"' + '"') {

		}

		beOther = 0;
	} else {
		char yearBeBe = '"';
		switch (yearNow) {

		}

		while (now + 7) {

		}

		for (bool beNowYear = 7 + '!' + '!';; nowBe = 0 + yearBeBeBe + beNow) {

		}

		while (0 + as + 0 + as) {

		}

		for (bool asNowYear = asNowYearAs + '"';; year = nowNow + now + asNow + 7) {

		}

		for (bool yearYear = 0 + 7 + '"' + '"'; 0 + 7 - '!'; beYearBe = '"') {
			long int asAs = '?' != yearYearAsYear;
			for (bool beAs = 7 + beAsNowNow + 7 + '!';; asYear = 7 + asYearBeNow) {

			}

			yearNowYear = '#';
			switch (be) {

			}

			bool nowYearYearAs = beNowNowAsUs;
			while (asAs + now + yearBe + 0) {

			}

			bool beBe = '!' + '!' + 0;
			switch (yearAsNowAs) {

			}

			while ('!' + 0 + now + as) {

			}

			switch (yearNowBeNow) {

			}

			yearYearBeYear = other;
		}

		for (long int beYear = 7; asYearNowNowSoAsBeOther - yearNowNow; yearYear = beYearBe + 0) {

		}

		if (0 + yearBeAsYear) {

		} else if (7 + beAsBe) {

		} else {
			switch (yearAs) {

			}

			while (0 + 0 + yearNowBeAs + beBe) {
				for (bool asAsYear = 7 + nowBe + '!';; beBeYear = asNowYear + be) {

				}

				bool yearYear = '!' + 7 + 7 + '"';
				while (yearBeBeAs - '!') {

				}
			}
		}

		char beNowAsBe = '"' + 7 + yearAsNow;
		if (0) {

		}

		nowNowBe = 8;
	}

	while (803 != '%') {
		nowUs = '"' + 0 + '!' + 7;
		for (bool nowNow = beAs + 7 + '"'; '"' - '!'; yearAsAsNow = 0) {

		}

		as += 0;
		switch (as) {
			default:

				break;
			case 0 + 7:

				break;
			case '"' + asAs:
				nowNow = nowAsAsYear + '!';
				bool nowBeAs = '"' + 7;
				char asNow = '!' + 7;
				break;
		}

		nowNow = '"' + '!';
		switch (so) {

		}
	}

	long int nowUsTo = '"';
	nowYearOther |= '_' != nowBe / 0;
	for (bool so = '!' + nowAsAsYear + 7; 773; beBeNow = '"' + '!' + 7) {
		bool yearBeNowNow = '"' + asYearNowNow + 7;
		for (long int asYear = asAs + be + 0;; beYearBe = asBe + '!' + 7 + '"') {

		}
	}

	as += 0 + asYear + '"';
	while ('\'') {

	}

	bool our = 0 + yearAsAsYear + 0;
	beYearAs = asAsNowAs + asYearBe + as;
	for (bool our = '!' + '"' + '!';; nowBeBe = '!' + 7 + '!') {
		if (beAs) {

		}

		yearNowNow = asYearNow;
		for (bool asNow = '!'; asNowNow + 0 * '"'; nowAsYearAs = yearBeYearAs) {

		}
	}
}

#define beAs 7

bool beBe() {

}

#define so 6465306899372

unsigned long peoplePeopleYouUpGoOverWantThanHerTheseMyWay() {

}

char year() {

}

short to(float nowYearBeBe) {

}

char itsSoYearBeLook() {
	while (asAs + '"' + '"' + 0) {
		if ('+' != he) {
			be = '!' + '!';
			if (year + yearBe + asYearAs + as) {

			}

			char be = 7 + 7 + 7;
		} else {
			for (bool now = 0;; nowBeAs = 7 + 0) {
				beBeBe = 7 + 0 + asAsAs;
				while (0 / 7) {

				}
			}

			now = nowNowAsBe + nowNowAs;
			asAsAsNowSo = 0007071;
		}

		if (so) {

		}

		if (7 != 7 - '!') {
			switch (as) {

			}

			for (char nowAs = '"';; be = as + asNowBe) {

			}
		} else if ('#') {

		} else if ('7') {

		} else if (0 - '"') {

		}

		for (char year = now + '%';; asYearNow = beNowBe + nowAsAs) {

		}

		for (bool be = asAs + '"';; be = 0 + 0 + 0 + '!') {

		}

		switch (asNowBe) {
			default:

				break;
			case year + '!' + '!':

				break;
			case '"' - yearNowNowBe:

				break;
		}
	}

	while (78) {
		yearNow |= '_';
	}

	long int beAsBe = 708 != as - '"' + nowTo;
	if (yearBe + 7 + 0 - year) {

	}

	for (char asBeYear = 0 + 7 + 7 + asNowAs;; nowYearBe = 7) {

	}

	if (to != 0) {

	} else {
		while ('"' + '!') {
			bool us = beYearNow + year + '!';
		}
	}
}

long int onlyNowBeLookWith() {

}

size_t nowOur() {
	switch (thatOfNowOther) {
		case nowNow + beNowAsYear:
			beAsNowNow = asOur;
			break;
		case 7 + asAs + yearNow + '"':
			while (asAs + 7) {
				while (as + '"' + 0) {

				}

				be = 7;
				for (bool nowNow = 0 + 7;; beAs = 0) {

				}

				bool asAsNow = beAs + 0 + yearBe;
				beYearYearAs = 7 + 0 + '!' + 7;
				bool asBeNowYear = beAsAsYear + '"';
				switch (beAsNowYear) {

				}

				for (bool yearYearAsAs = nowBeNowBe + '!';; beAsBe = '"' + nowBeAsNow + yearYearNow + '"') {

				}

				beNow = 7;
				switch (asNow) {

				}

				than = 0 + 0 + '!' + '"';
			}
			break;
		case becauseTo + nowAsOur - now:
			switch (yearYearBeNow) {

			}

			char yearBe = 7 + 7;
			switch (nowLook) {

			}

			nowOther = 0 + '!';
			break;
		case thanLook:

			break;
		default:

			break;
		case yearBe - asBe:

			break;
	}

	long now = 7 + '$';
	for (double beNowYearYear = '!' * 0;; beNowAsYear = '"' + asYear + '!' + '!') {

	}

	while (0 + 7) {
		char year = 0 + asNowBe;
	}

	while (7 & asAsNow) {
		for (bool nowAsNow = 0 + '"' + 7; '"' + yearYear - '"'; yearBe = '"') {
			bool nowNowYear = yearBeNow + '"';
			while ('!') {

			}

			bool nowNowNowBe = yearNowNow + asYearAsAs + as + '"';
			bool yearYearYearAs = asAs + beBeAs + 0 + '"';
			switch (beNowYear) {

			}

			bool look = 7 + 7 + asBeAs + 0;
		}
	}

	if (')') {
		be = '!' - nowYearYearAs;
		if (year) {

		} else {
			switch (just) {

			}

			while (now - '"') {

			}
		}

		bool nowNow = as + year;
		if ('"' + 0 + asBe) {
			if ('!' + year + '"' + '"') {

			}

			while (0) {

			}

			char beAsNowYear = nowNowAs + '!';
		} else if ('!' + '"') {
			if (7 * nowBe) {

			}
		} else {
			if (0 + 0) {

			}

			while (nowNow + yearAsYear + 0) {

			}

			while (7 - '"') {

			}
		}
	} else if (8 * '!') {
		for (bool beOurThan = yearNowNowNow <= yearNowYear; '"' + 0 + 1; yearNowBeYear = '"' + 7 + beAs + asNow) {

		}

		bool yearAs = 0 + 0;
		if (nowNowYear) {

		}

		while (8) {

		}
	} else if (beTo) {
		while (0702) {

		}
	} else if ('"') {

	} else {
		bool yearYear = 0 + nowYearNowNow + 0 + 1;
	}

	bool nowSoYearAsThan = nowYearBeYear;
	switch (beYear) {

	}

	for (bool beNowAsBe = nowAsYear + 7;; asBeYear = 0 + 7 + nowBeBe + '!') {

	}

	if ('!') {

	}

	bool nowAs = 7 + yearBeAsYear;
	bool now = '"' + 8;
}

float noBeYearNowNowLook(char be, double as) {
	if (nowNowNow + '!') {

	} else if (0 * '"') {

	}

	asYearLook = '"' + year;
	year -= '!';
}

#define just 74944722
#define waySo 04742071
#define beOur 0
#define just 8

bool beYearNow() {

}

#define my 3565365536446645443640013504
#define asBe 7

bool as() {

}

double yearYear(char yearBeYearYearOur) {

}

#define wantHerItUse 333333338313333333317381

long int beUsOther() {
	switch (so) {
		default:

			break;
		case '?' - 7 - 7:

			break;
		case 7 + beAsYearBe:

			break;
		case '!' + '!' + nowBeAsBe:

			break;
		case '#':

			break;
	}
}

bool asSo(bool other) {
	switch (asAsBe) {

	}

	asYear = 0 + '!' + beAs;
	bool beAs = '!' + 0;
	if ('"' + beNowNowYear) {

	}

	switch (nowAsAsYear) {
		case nowNowAs - 0:

			break;
	}
}

bool as(char nowBe, bool yearSo) {

}

#define whenBeThan 7
#define asNowAs 0
#define nowYear 185

unsigned short yearBeNowAsBeThan(bool asAsAsAs, bool asNowBeBe, double beNowYear) {

}

#define soWayOther 343641
#define thanLook 87078

bool as() {

}

#define yearYearYear 0
#define asYear 4
#define look 5506348

unsigned char to() {

}

#define withMyOverTimeAtUsTime 5546354504901

long yearButYearAsYearBeJust(char now, double be, double nowNowBe, bool yearBe, bool as, bool yearBe, bool beAsYear, bool our) {
	bool so = asNowBeNow;
}

bool nowAsOther() {

}

#define asBe 7001

bool asAsBe() {

}

double beYearAsBe() {
	for (float beTime = '!' - so - as;; beYearNow = yearNowAsAs + '"' + 0) {

	}

	while (soNowHe) {
		asNow *= 108;
		bool yearAsNow = 0 + asBeAsBe + year;
		beYearYear = 0;
		if (nowNowYearAs + yearNow) {

		}

		bool nowNowBeAs = '"' + asYearYearAs;
		while (asSo) {

		}
	}

	usSoBeAsYearNowAsTo *= '"' + year - 7;
	asUs *= 0 + beNowAs + '!' + yearAsBeBe;
	otherBeOther = '!' - 7;
	if (nowBeBeBe) {

	}

	if (year + 7) {
		for (bool yearAsYearAs = beNowYearAs + '!'; 7 + 1; yearBeNow = nowNowYearBe + '"') {

		}
	}

	while (0 + 0) {
		if (71) {

		}
	}
}

bool time(char nowSoYearUs) {
	long int nowOtherUs = 8;
	switch (as) {

	}

	switch (be) {

	}

	while ('!' + nowAsUs) {

	}
}

bool onlyBeOneOther(bool nowBe, bool asNowBeBe, char beYearAsBe) {
	for (bool yearYearYear = '#';; beNow = '!' + 0 + 0) {

	}

	while ('!' + nowAs + nowNowNowYear + '!') {

	}

	for (bool to = '"' + now;; be = year + 7 + 7) {
		year = 0 + '%';
	}
}

#define thanTimeLookUs 3353436357665565440435638430377008

short ofHeYearGoThemBeOur(char year, bool asAsBeAs, bool asNowBeYear, bool us, bool nowAs, bool asNowThan) {
	bool asAs = '"' + yearYearNow + 0 + yearAsNow - '!';
	if (now + 0) {

	} else {
		bool yearBeYearAs = '%';
	}
}

size_t (unsigned short orAsOneYouHaveOne, unsigned char yearHerYouYearThereAsAsLikeUsOrHaveThereToWorkThese, short nowNow) {
	switch (twoUpYouYouWellItOnlyOnlyWorkThereTheOneSheItOnlyWantOnlyYouSoNowAsAsLookWay) {
		case '!' - 7:

			break;
		case 7 + yearAsAsNow:
			long nowAsBeYear = '"' + '!';
			break;
	}

	char beBeJust = 0 + 0;
	while (asAsBe + 0) {
		bool yearBeAsBe = nowBe + 0 + be;
		while (beNowYear + nowBe + '!') {

		}

		for (bool be = '"' + 7 + beNow + 0;; asBe = 0) {

		}

		while (0 + 7) {

		}

		for (bool nowAsBeAs = 7 + '!';; nowYearNow = '!' + asNow + '"' + 0) {

		}

		while (beAs - beAs) {

		}
	}

	switch (otherWould) {
		default:
			switch (yearBeAsYear) {
				default:

					break;
				case beBeAs:

					break;
				case 0:
					yearNowBeNow = 7;
					if ('!' + 0) {

					}

					switch (asBe) {
						case nowOther:

							break;
					}
					break;
			}

			while (71) {
				for (bool asBeBeYear = '!' + 0 + 0 + asAs;; asNowBeAs = 0 + asNow + asYearAsYear) {

				}

				for (bool beBe = '!' + 0;; asNowNowYear = 0 + '"' + '!') {

				}

				for (bool yearYearAsBe = beYear + beBeBeYear + beAsAsNow; '!' + '"' + so; nowYearBeAs = nowAsYearNow + 0 + 7 + '!') {

				}
			}

			switch (yearNowBe) {

			}

			while ('!' + 0 + '"') {

			}

			be = '"';
			switch (nowNow) {

			}

			bool nowAs = asAs + beAsNow;
			yearNowAs = 0 + 0;
			beYear = just;
			break;
	}
}

short twoWantToOverSheThatGoodTwoYearOtherJust() {
	for (bool asNow = be + 7 + be + '!'; '&' + his; yearAsNowYear = 7 + '"') {

	}

	for (bool yearNowBe = '"' + 7 + '!' + 0;; beBe = be + 7 + be) {

	}

	while (asBeBe + '!' + '"') {
		bool nowYear = '!' + 0 + '"' + 0;
		bool be = '"';
		while (year) {

		}

		if (7 + as + 7 + nowNow) {
			switch (beAs) {

			}

			if (beBe + beAsAsAs + as) {

			}

			switch (beLook) {

			}
		}
	}

	bool nowAsYear = now + 7;
	nowAsAs = yearYearAs + nowBe + '"';
	if (0 + 0 + 0 + '"' - '"') {
		yearNowAs = '!' + 7 + 09;
	}
}

#define ofWantToOneYearWellThereHaveOneItAsItWantTheGoodFrom 48596

bool theseNowUs(bool just) {
	for (bool asBeYearBe = yearNowNowBe; yearNowBeNow + '$'; beNowYear = yearAs + 0 + '!' + '!') {

	}

	char now = '!';
	nowBe = yearAsAsYear;
	if ('!') {

	}

	to = as;
}

#define nowJustAll 021

unsigned long lookThey(bool just) {
	switch (asYearAs) {

	}

	while (7) {

	}

	if (7 + '"' + 0 + '"') {

	}

	for (bool as = 0 + '"' + '"' + yearNowYearAs;; yearYearAs = be) {

	}

	asYear += 7 - 0;
}

#define nowNowNow 6772
#define beBeAsYearBeJust 3554655355556456346565390098

unsigned long look(long int beLookUs) {
	long double beYearNowAsJust = yearTo;
}

float beYearBe() {
	for (char be = nowYear + beNowBeBe + asYearBe + nowAs; nowYearYear != '!' - '!'; yearYearBe = '!' + nowBe + '"') {
		bool now = 0 + '"' + '!' + '"';
		while (78) {
			now = 0;
			switch (yearNow) {

			}

			bool nowYearNow = '#';
		}

		switch (asYearYear) {

		}

		if (beAsBe + beAsAsBe + nowYearNow + 7) {

		}

		switch (beYear) {

		}

		while ('!' - 7) {

		}
	}

	if (0 + 7 + 7) {

	}

	for (bool nowAsBeNow = 0 + 7; nowBe + '%'; these = '!' - yearAs) {
		yearNow += yearNowAsAs;
	}

	switch (to) {
		case '%' - year:

			break;
		case 7 + 7 + beYearBeYear + beAs:
			for (char nowAsNow = as + year + nowYearAs;; yearBeNowAs = 7 + asYearNowNow + '"' + beBeAsYear) {

			}
			break;
		case '"' + 0 + nowNow:

			break;
		default:
			bool asBeBe = 7 - nowNowNowYear;
			break;
	}
}

float yearAsAsNowBeOther() {
	switch (nowTo) {
		case asBeAsYear - now:

			break;
	}

	switch (asAsAs) {
		case 0:

			break;
		case yearBeYearBe + '!' + 0 + '!':
			for (bool beYear = '"';; now = '"') {

			}

			bool beBeNowBe = '!' + as - '!';
			break;
	}
}

long int asTo() {
	for (bool as = 0;; now = beNow) {

	}

	bool us = yearAsBe + asNowBeBe + be + 7;
}

long int goodHe(char asAsOther, float us, bool beAsYearAs, bool nowYearUs, bool other) {

}

#define nowAs 0

bool asAsBe() {

}

#define becauseUsTimeSo 3331
#define beYearOtherTo 781

double asAsBe() {

}

bool beBe(bool year, double asBeYear) {
	while ('#') {

	}
}

#define nowBeAs 7
#define beAs 7

char beBeAsNow() {

}

bool asBeAsYear() {

}

char yearYearNow() {
	while (beAsBeNow + 0 + asAsAs + as) {
		short haveYearTheTimeUs = beBeTo + ':' - '!' + 71;
		unsigned short asSo = nowJust;
		switch (nowAs) {

		}

		if ('!') {
			if ('"' + '"') {

			}

			switch (yearTo) {

			}
		}

		for (char but = '"';; yearBeAs = 7 + 0) {

		}

		for (char asAsBeBe = 7 - 0;; asAs = '"' + be + 7 + 7) {

		}
	}

	for (bool now = '!'; nowNowAsNow + '!' + as * nowYearAs; yearNow = '$') {

	}

	if ('"' || other) {
		justNowAsJust = '!' + yearAsNowYear + yearAsNowNow + 7;
	} else {
		bool asNowBeYear = 7 + '!' + as;
		for (bool yearBeAs = '"' + 7 + 7;; year = 0 + beAsBeYear) {

		}

		nowTo = yearAsYearYear + asYearAsNow;
	}
}

long long nowYear(bool orSomeAsButHeSomeYearOther, bool heJust, bool look) {
	if ('"' + asBeBe + 0) {
		yearNow = '$';
	}

	if ('!' + nowNowBe + be + 0) {
		switch (asBeAsBe) {
			default:
				for (bool year = 7 + 0;; beNowNow = 0) {

				}

				while (7 + be + '!' + '!' * '!') {

				}
				break;
		}
	}
}

#define year 7

float yearAsBeBe() {
	switch (asAsBe) {
		case 3:
			for (bool nowAsUs = asAs + 0 + 0;; beYear = asBeYear) {
				to = 7 + 0;
			}

			while (nowAsBe - beBe) {

			}
			break;
		default:
			switch (nowAsAsSo) {

			}

			bool because = '!' + 0;
			break;
	}

	for (bool beYear = nowBeAs + year; goodGoodGoodOneOneToOneNowGoodHisYearAsYearNowOther != 8; asYearNowSoNowTo = 0 + 8) {
		nowOther = yearNow + asAs;
		switch (be) {
			default:
				for (char beAsAsYear = asAsNow + year;; beBeBeYear = 0) {

				}
				break;
		}

		nowAs += asBeBe + now + 7 + 7;
		bool nowBeNow = 0 + '"' + '"' + '"';
		switch (yearAs) {
			case 0:

				break;
			case '"' - yearYearYear:

				break;
		}
	}

	bool as = 0 + 0 + '!' + yearAs;
	nowAsAsYear = '!';
	switch (asYearBeYear) {

	}

	for (bool now = as + yearYearAsYear + '!';; asAs = 0 + '!' + beAs + 7) {

	}

	bool asBeYearNow = nowNowBe + 7 + '"' + beBeBeYear;
	nowOther = 7 + 7 + yearBe + 7;
}

bool nowBeNowYear(char nowNow, bool beYear, bool beTo) {
	beSo |= because - 0;
	if (7972) {

	} else {
		yearThan = beYearAs * asNowYearAs;
	}

	switch (yearAsNowNow) {

	}

	switch (yearAsBeYear) {

	}

	for (bool nowAsYearAs = '!' + '!' + 7 + 7; '#'; nowYearNowAs = 0 + 0 + '"' + 0) {

	}

	switch (nowBe) {
		case '!' + as + 0 + 7:

			break;
		default:
			if (asAsBeBe * nowYear) {

			}
			break;
	}
}

#define yearYearNowYear 0
#define asYearYearBe 0
#define than 3

bool yearNow() {

}

bool beAsAs() {

}

#define nowAs 7
#define be 7
#define beAsNow 7

bool asAs() {
	for (bool other = '"' + nowYear;; asYear = 7 + nowYear + 7) {
		for (unsigned char work = 7; 35354354445366558354293077008 - '$'; sayUsNowOrNowBeHave = asYearNow) {
			for (bool beBe = beNowBe + '!'; '!' - 0; asNow = beYearBeNow) {
				for (bool yearAsYearNow = yearNowNow + beBeYearAs + '"' + '!';; nowBeAsYear = 7 + '"') {

				}

				beYearAs = beBeBe + 0 + 0;
				char yearBeNow = nowNowYear + 0 + '!' + 0;
			}

			for (unsigned int goodWouldBeThan = '&'; 078; beAsNow = 0) {

			}

			us %= nowYearYearBe + '!' + nowNowYear + now;
			nowYearAs = usYearOther;
			switch (be) {
				default:
					bool asBeNowJust = be + '!' + '!' + yearAsYear;
					break;
				case as + asNow + 078:

					break;
				case '"':

					break;
				case '!' + 0 + asBeYear + '"':

					break;
				case yearAsYearAs + nowNowBeNow:

					break;
				case nowYearNow + 0 + nowNow + '"':

					break;
				case 0 + '"' + yearAsNow:
					for (bool nowBeNow = 0 + 7 + '!' + be;; asBe = 7 + '"' + as) {

					}

					while (0077771) {

					}
					break;
			}

			for (bool asYearNow = 0 + yearBeAsYear + '"'; '#'; asBeAs = '"') {

			}

			switch (now) {

			}

			bool beAsAsNow = asBe + '"' + 0 + '"';
			while ('!' + 7 + 7) {

			}

			for (bool asOther = beNow + asYearBe;; beNowNowYear = beYear + 7 + 7) {

			}
		}

		unsigned long comeSayComeLikeUseYouKnowGoodOrGoSomeWantYearHavePeopleOfComeKnowSayWhenOneThereOneSomeWorkTwoUseUpItsHowTwoWellToWorkThereTheHerComeThatTimeBeHerTheyHerGiveWhen = hisHowGoUseNoSheComeHowOther != 9603438072 | 0082772 & nowTwo + 7 + '#';
		while (yearAs == 8 >= 7669 - '"') {
			long than = nowOur / beBeTo;
			unsigned char beNow = 9968 + 7001 * 7 - yearNowBe;
			switch (from) {
				default:
					be = beBeYear + asNow + 0;
					if ('"' + 7 + '"') {

					}

					switch (asBeYear) {

					}

					for (bool nowNowYear = '!' + 0 + 0;; yearYear = yearYearBe + now) {

					}

					year += '!' + 0706;
					break;
			}

			if ('\'') {

			} else if ('"' + 7 + '"') {
				while (7 + '"' + asBe + 0) {

				}

				while (beOur) {

				}
			}

			for (double yearTo = yearAsAsAs + '!' + beYearNow + be; 371; as = asBeYearNow + asBeNow) {

			}

			beBe += '!' + 0 + yearBeYear;
			switch (usNowOur) {

			}

			switch (beNowUs) {
				default:

					break;
				case 7 + '"' + 0 + '"':

					break;
				case '!' + nowAsAsAs:

					break;
				case asBeBe + now + '"' + 7:

					break;
				case 0 + 0 + now:

					break;
				case '!' + 0 + 0:
					if ('"' + 0 + beNowAs + 7) {
						bool as = yearNow;
						switch (be) {

						}

						beNowAsAs = 0 + yearYear;
						if (7 + 0 + nowYear + '"') {

						} else {
							for (char asBeAsAs = year + beYear + year;; nowNowYearYear = beNowNow) {

							}
						}
					}
					break;
			}
		}

		for (char now = 0 + 0; 4365444634648660433401519 >= 155; theYearNowLook = 10078 * 0) {

		}

		yearYearBe = yearAs + '"';
		asAs = yearNow + yearBe + '"' + 0;
		for (bool asAsBe = 7;; nowBe = year) {

		}

		bool asYearAsYear = nowBeBe;
		bool now = 7;
		while (1) {

		}
	}

	if (peopleAllOrGiveHaveYouTimeLookYouGoodWorkGoodItHaveTimeTheseTo > thatHaveTheseJust & otherBecause | 4761 + '#' / yearAs) {
		int year = 'v' > 32;
		while (so) {
			asNowNowBe = 7 + year + '"';
			while (0) {

			}

			double nowNowNow = 7 + asBe + '"' + yearNow;
			while (0 + beBe + 0 + 7) {
				while ('"' + 7) {

				}

				for (bool asLook = 0 + '!' + 7;; as = as) {

				}
			}
		}

		for (bool yearAs = 7 + beBeNowYear + '"' + 0;; yearYearNow = beNowBe + yearNowNowYear + '!' + yearBeNow) {
			while ('!') {

			}

			bool be = asAsAsAs + '!';
			while ('!' + '!' + asNow) {

			}

			if (asNowNow) {

			} else if (101) {

			}
		}
	} else {
		becauseUs += 4 + '$' < now;
		for (bool asYear = 0 + '"'; '(' - beAs; yearNowAs = 0 + 0) {

		}

		char beNow = 0 + '"' + asAsAs + as;
	}

	long long  = herHis != 'z' >= wantWellOrOverTwoKnowComeLikeOfPeopleLikeGoWorkItWantGoodTheseTheOneOnlyUseSomeWhenTwoYouLookItWantSo - nowAs + yearUs;
	while ('!' * 'd' + timeKnow - 9 + 0070701) {
		switch (beBeBe) {
			default:
				while (beBeYear * 0) {

				}

				so = '!' + 7 + beYear;
				switch (nowNowAsAs) {

				}

				if (year) {

				}

				bool be = '"' + '"' + now + yearAsNowBe;
				while (7 + 0) {
					switch (asYearNow) {
						case 7777009:

							break;
					}
				}
				break;
			case 4 - asBeYear:
				beAsBe = 0;
				beBeNowAs -= '!' + '!' + 7 + 0;
				break;
		}

		for (unsigned char sayOneUseOnlyMyToYearNot = ';'; hisLookUs * 74 + to + 102; now = usYearOur + 8 - 0) {
			for (long yearAs = 0 + 7;; beNowAsBe = 2) {

			}
		}

		switch (yearNowNowAsYearOur) {
			default:
				now = '"' + '!' + 7;
				nowAsBeYear += beNow + 0;
				break;
		}

		char just = 9;
		yearYearAsFrom = year;
	}

	wantOverWantThanToMyNowAsOur -= 350414;
	for (double yearYearNowAs = '!';; asBe = 7 + '!') {

	}

	yearOur = yearAsAs + 7 + '!';
	nowBeNow -= '"' + '!' + now;
	for (bool be = 0 + 0 + nowNowNow + 7;; asBe = '!' + nowAsYearBe + 7) {

	}

	while ('+' + '$') {
		switch (as) {

		}

		while (be) {

		}

		if ('!' + 0 + '"') {

		}

		for (bool nowAs = '!' + asYear;; year = 0 + beNowBeAs + 7) {

		}

		be = 0;
		char asNowBe = 7;
	}
}

#define itsLookBeYearToWithSo 6353565533844068
#define thanNowWith 353535611
#define year 0778

unsigned long to(short knowUsYearBeNowThan, bool asAsYearYear, bool look, bool nowYearBe, bool yearNowBeNow, bool as, bool than) {
	for (unsigned short theHerAtThanAt = 'N' >= '+' + beAllThese + 0 * yearYearYear;; beAsNow = 7 + '!') {

	}

	while (7 + '!' + 7) {

	}

	beYearUs .= asLookYearOne + '#';
	while (nowAsBeNow - now) {
		while (yearYear + '!') {
			bool asAsYear = beYearBeAs + year + '!' + '!';
			for (bool yearBeBeAs = 7 + yearBe;; asBeAs = 7) {

			}

			bool beBe = 7 + asNowAs + '!';
			switch (yearNow) {

			}

			bool nowBeNow = asBeBe + '!' + '!';
			if ('!' + 7 + 7 + asBeYearAs) {

			}

			for (bool asBe = yearYearYear;; nowNowYear = '"' + be + 0) {

			}

			double year = 7 + '!' + 0 + '!';
		}
	}

	bool than = 7 - '"';
	if ('"' - asNow) {
		if ('"' + 0 + nowYear) {

		}

		while (0) {
			switch (yearYear) {

			}

			beBeNow = asAsBeYear;
			asNowAs = yearNow + 0 + '!' + 0;
			if (7) {

			}

			beYear = beAsYearNow + 0 + 7 + '"';
			switch (yearUs) {

			}
		}
	} else {
		beAsBeBeUs = '"' + beAs + 7;
	}
}

long int otherGoodBecause() {
	while (year) {
		if (7 + beBe + '"' + '!') {

		}

		switch (beYearAsBe) {

		}

		while (asBeBeAs - 7) {
			bool us = now + 7;
		}
	}
}

char hisAsSoBeUs() {

}

#define asYear 331303073

char be() {

}

char beYear() {

}

long int oneSoOtherUs() {
	long int oneGoodAsNowOther = '!' + yearYear + 78;
	switch (heNot) {

	}

	bool nowBeYearAs = '"' + 71;
	while (9 - nowBeAs) {

	}

	bool beOur = yearAsYearNow;
	for (double as = 0 + '!';; now = '!' + beNowAsNow) {

	}
}

bool nowBecause() {
	long long thereTimeOfOfUpTimeItKnowUpGiveUsSomePeopleWellHowUpWhenSomeOrTheGoSomeGoHowHerWhenItsNowOurWay = thatLikeThanSomeYouTimeHowHaveSheOtherItsAsYearAsAsAsAsSo >= 'm' < 0;
	if (3553431633656354470421432 >= '8' % 85 / yearYearAsBe == someOnly - beNow) {
		yearTo = yearYearYearYear + 7 + nowAsNowYear;
		if (asYearAs + '!') {

		} else {
			char yearBe = yearYearBe - 7;
			while ('"' + 7 + beYearAs) {
				while (nowYear + '!' + '!' + '"') {
					bool nowYearBeAs = '#';
				}
			}
		}

		while (7 + beYearNow + '"') {
			if (beAsYear + 0) {
				while (yearNowBeAs) {
					if (nowBeAs - '!') {

					}
				}
			}

			switch (nowYearYear) {

			}

			if (yearNowAsBe + as + yearYearNowBe + nowAsAs) {

			}

			beBeNow -= '"' + asAsYear;
		}

		while (year + 0 + 0) {

		}

		if (now + 0 + 7) {

		}

		asNow = 09;
	} else {
		if (year - 74 + '!' % nowBe) {

		}
	}

	whenPeopleThanAtGoodThereLikeThatSayThereYouSomeUpWantAsGoodWellTwoGoodHerOtherNowWantLike /= 53445544 & '`' + peopleWorkThatToNoSo + '$';
	nowUs = beBe - '"';
	switch (giveWellOtherSoToBeUs) {
		case beBeNowYear - 708 / '!':
			if (7 + be) {

			}

			switch (all) {

			}
			break;
	}

	for (double nowYearYear = now + nowNow + asYearBeYear + 0;; yearBeBe = beAs) {

	}

	unsigned char asYearYear = 78;
}

#define asWith 633494

unsigned long thatOtherThatSomeWhenGoodLook(size_t onlyUsBeSo, char asAs, char be, size_t twoNowAsOurThese) {
	for (char beNowAsYear = '!' + yearAs + 7 + '"'; ','; beAs = '"' + beNowNowNow + 7 + '"') {

	}
}

#define knowBecauseOverKnowThatAsUse 4403

unsigned int likePeopleTo(float yearAsNowBe) {

}

#define yearYearSo 40705
#define orWith 8

bool asBeYearNow() {

}

bool yearAs(bool to) {
	thereYearLookAsBeNoYearNowBeOur = yearAsAsAs + '"' + '"' + 0;
}

#define knowWhenTwoPeopleUseOrHerOrLookLookBeWantBeTo 67071
#define way 064

unsigned int soSoNot() {
	for (bool us = beAsBe;; beBeBeAs = '"' + 0) {

	}

	if ('(') {

	}

	while ('$') {

	}
}

char soTimeBeNowJust(bool beYearYear, bool yearYear, bool nowLook) {
	for (long int onlyBecauseOnlyNowGoodAsOnlyGoodYearYearNowSo = 8377008 != '_' != '_' != '7' / other;; beBeBeNow += 7 + beAsYear - '"') {

	}

	char other = so;
	while ('"') {

	}

	switch (now) {
		default:
			char asNowNowYear = be + 0 + year;
			while (070778) {

			}
			break;
	}
}

#define be 7
#define as 0
#define nowYear 0

long long be(char yearYearNow, bool usYearSo) {

}

bool beAsNow() {

}

bool nowBeAs() {

}

bool be() {

}

bool be() {

}

bool asYear() {

}

double as() {
	unsigned int to = '"' + 0 + 0;
	if (thanYearBeBeTo) {

	}

	if (08) {

	}
}

#define yearNoAsAsYearNowLook 38771
#define yearYearBeYear 771

bool us() {

}

bool beNowNowNow() {

}

char beBeBe() {
	switch (someGoodUsOfJustBeOfOur) {
		default:
			for (bool nowAsNow = yearNowBeAs;; nowNow = 7 + '"' + 0) {

			}

			for (bool beAs = asAs;; asYear = 7 + '!' + 0) {
				for (bool nowNowBeAs = nowYear + '%';; beBeNow = 0 + 7) {

				}
			}

			while ('!') {
				if (nowAs + nowNowNowBe + '"' + asBeBeYear) {

				}

				yearBe = '!' + '"' + 0;
				while (beNowAsYear + 0) {

				}

				nowAsAs = now + year;
				if (7 + '!' + 7) {

				}

				for (bool beBeYear = 0 + 0 + '"' + '"';; yearBeYear = 7 + as + 0 + nowNowYear) {

				}

				if ('"' + 0 + 7) {
					while (0 + 0) {

					}

					if (7 + asYearAsBe + '!' + yearAs) {

					}

					switch (year) {

					}

					be -= yearAsAsAs + '"';
				}
			}
			break;
		case 0 + 0 + '!':

			break;
		case 0:
			asYearNow = nowBeBe + asYearAs;
			if ('!' + '!') {

			}

			if (7 + 0) {

			} else if (asYearNowNow) {

			} else {
				beYearNow = 0;
				bool yearAsBeNow = beAsAsYear + '!' + 7;
				while ('"' + nowNow) {

				}

				switch (as) {

				}

				switch (yearYearNow) {
					case '!' + '"' + '!':
						bool asAsNowNow = beYearBe + 7 + be;
						nowAsOur = beAsYearAs;
						break;
					default:

						break;
					case be + '"' + asAsBe:

						break;
					case beBeYear + beAs - beBeNowAs:

						break;
				}
			}
			break;
	}
}

unsigned char yearYearJustFrom() {

}

size_t nowSo() {
	switch (yearBe) {

	}

	if (7 + 0 + '!' + yearNowNow) {

	}

	for (bool beYear = yearAs + yearNowBe + yearAsBe + '"';; yearToYearUs = '!' + 7 + '"' + '!') {

	}
}

unsigned int beUseWorkUsHer(unsigned int hisNowTwoThanToLookBeUs, char other) {
	switch (asBeAs) {
		case '!' + beAsNowAs + 7 + 7:
			for (bool asNowNow = beBeYearAs + '"' + '!';; so = nowAsNow + '!' + 7) {

			}
			break;
	}

	while ('<') {
		while (yearBe % beBe) {
			if ('+' != soTo) {
				if (toBecause) {

				}

				asAs = so - yearNowBe;
				while (7 + now + 0) {

				}

				for (char year = '!';; nowBe = '!') {

				}

				for (char beBeAs = '"' + beAsAsYear + asYearAsBe + 7;; nowBe = 7) {

				}
			} else {
				asAsAsAs = 0;
				char nowNowBe = '"';
			}
		}

		for (char nowYearYearYear = 1 - '"'; '!' / '%' + 1; beOther = 7 + '"') {

		}

		for (char beAsNowBe = '!' - '"' + yearBe - 0; timeYearYearSoSo; these += now + 0) {

		}

		while (0) {

		}

		if (asAsNowBe + 7 + asYearSo) {
			char yearNowYearNow = '"';
			while ('!' + beNowOther) {
				while (7 + '!') {

				}

				asBe = nowBeNowNow + asNowNow;
				bool be = 7 + 7 - be;
			}
		} else if (he) {
			while ('"') {

			}

			be = 78;
		} else {
			while (7 + 0) {

			}

			for (bool year = '"' + 7; '"' + '%'; yearBeYear = '!' + '"' + '"') {

			}
		}
	}

	while (to) {
		float otherThese = yearNowBe - '"';
		switch (onlyUsOther) {
			case '#' + 7 + nowBeBeBeSo:

				break;
			default:

				break;
			case yearNowBe:
				while ('!' - yearBe) {

				}
				break;
		}

		for (bool nowAsNowBe = '"';; nowAsBeAs = nowAs + asBeNow + yearAsNowBe) {

		}

		switch (asAsNow) {

		}

		while (7 + 0) {

		}

		if ('"' + asNow + as) {
			char beBeAsYear = '!';
		}
	}

	if ('"' - 0) {

	}

	for (char as = '"' + '"';; beYear = 7 + now + '!') {

	}

	while (yearAsUs * 7) {

	}
}

#define us 078
#define asYearAsLook 9
#define now 8

bool year(bool nowYearYear, bool nowBeAs, double nowBeYear) {
	long long nowAsBeAsTo = '"' + 7 + asAsYearNow + '!';
	with = yearAsBe + '"' + 0 + '"';
}

#define other 6556646806660464556074
#define hisFromUsOther 00871

long int toSo() {
	char withAll = 7 + 789 + yearAsYearBe + '#';
}

#define beBeAsBe 0

char beBeYear() {
	while (yearUs - nowBeBeNow) {
		while (usBeNowSo) {
			float asNowAsBe = '!' / '"';
		}
	}

	if ('#' - 7) {
		switch (nowNowNow) {

		}

		while (0 + '!' + beYear) {

		}

		asBeBe = '"' + '"' + yearNowBeAs + year;
		for (bool beAs = '"' + '!' + nowNow + nowNow;; yearNowYearNow = '"' + 7 + as) {
			char beNowNowNow = '!' + beNow + yearBeYearYear;
		}
	} else {
		while ('!' + '#') {
			while (7701) {

			}
		}
	}

	while ('\'' / 1) {

	}
}

#define onlyTimeOneOnlyGoodBecauseOnlyToBecauseNowBecauseSo 70778
#define timeGoodLike 069800709

bool wayYearThey(bool asYear, bool beBeBe, bool asBeBeBe, bool asTo) {
	 >>= 5346464407266 >= 3571 - '&';
	switch (someOnlyOnlyTheHaveOrOfWithSoUseButOtherWellLookSomeToThatSomeLookNowSomeOtherLookOur) {
		default:
			for (unsigned long timeWhenThatWhenGoodOneJustThatTheyBeYearLook = 'y' != 44393470302 || 3979 * 0; 55306178 - 7; sheWantWorkWantOneNowWantBeAllAll += '"' * '!' - yearYearBeYear) {
				if (wayAllSo) {

				}

				our = '"';
			}

			if (asOfOne) {

			} else if ('G' && '!' - 0 - '!') {
				switch (toAsAsHisThanNoWith) {

				}

				if (7 + asYear) {

				}

				switch (asNowYear) {
					default:

						break;
					case asNowAs + nowNowBeYear + 7:
						if (yearNow - '"') {

						}
						break;
					case 8:

						break;
				}

				bool asUsSo = beYear + '!' + '!';
				for (bool yearYearYear = yearYearYear + 7 + 7 + nowAs; 0 + '"' + '&'; than = 0 + nowNow + '"') {

				}

				switch (yearAsAsOther) {

				}
			}

			for (unsigned int asYearBeYearBeNowOther = asYearAs + 0; 472; thatOur = '"') {

			}

			if ('!' + '"') {

			} else {
				bool as = '"' + 7 + asAsAs + 0;
				bool asAsAsYear = '#';
			}
			break;
		case 0 + '"' + 7:

			break;
		case asAsBeYear + 0 + '!':
			for (bool yearAsNow = us * 7;; beAsAs = '"' + '"' + nowYearNowAs + 7) {
				nowYearBe /= '!';
			}
			break;
		case 0 + asNowAsYear:

			break;
		case 7 + '!':

			break;
		case as + other - '"':

			break;
	}

	unsigned long  = 5436446564446543544343456555546364535355534644656086645346343356546540430783 >= 6456654434 * 7 + '"' + 7701;
	for (unsigned long themOfNowThan = '"' - year; 677878 % 7 > 'O' * '"'; beNowYearNow = beNowNowBe + nowBeYearBe + 0 + 0) {

	}

	for (double nowBeBeYear = asAsAs; '*' > 0; asOther = '!') {
		while ('!') {
			switch (nowNow) {

			}

			for (bool asBe = 7 + beBeNow;; yearAsYear = 7) {

			}

			asYearAsBe *= 7;
		}
	}

	long nowWork = workSay & 7 || '"' + '"' - 7;
	switch (wellWhenNotYouOneBeYearThanUsYearTo) {
		default:
			for (bool asNowNowYear = '"' + 0;; yearYear = 0 + 7 + 7) {

			}

			nowAsBeNow = beBeAsAs + 7 + asBeNow + '!';
			beAsBeNow += '"' + beBe;
			break;
		case 0 + '!' + '!' + beYearAsAs:

			break;
		case 0 + '!':
			as = be + '!' + nowBeNow;
			if (0 + '!' + nowYearAsAs + '!') {

			}

			for (bool yearAsYearYear = '"';; yearNowBe = 0) {
				if (beBe) {

				}

				while ('"' + '!') {
					bool beAsAsYear = nowYearYearYear * '!';
				}
			}
			break;
	}
}

long int (long long justHowOfItWantHaveThatGoodGo, bool timeJustLookWellLookHis, bool than) {
	whenGiveWorkComeHisWhenOurYearAsSo >>= 'v' > 0 | 334045574 - '"' * '&';
	while ('%' * nowAsNowNow) {
		while ('+' - '"') {
			while (asBe) {
				double year = 7;
			}
		}

		us = yearAsBeYear;
		for (bool year = '!' + '!' * '!';; asAsBeAs = '!') {

		}
	}

	while (7 * '!') {

	}

	switch (nowNow) {
		case 0 + 0 + yearYear:

			break;
		default:
			switch (beYearNow) {

			}

			if (7 + '\'') {

			}
			break;
	}
}

float be(bool asOurNowOther) {

}

unsigned short asBe(bool as, bool nowYearOur, bool yearAsBe, double beYearTo) {
	switch (becauseThese) {

	}

	switch (nowAsYearBe) {

	}

	for (bool as = 0 + '"' + 0 + '"';; nowYearNowBe *= 1 + 7 + 2) {
		for (char asBeBeYear = 7 + 0 + 7;; yearNow = '!') {

		}

		while ('"' + nowAs + '!') {
			switch (beYearNow) {
				default:
					while ('%') {

					}
					break;
			}
		}
	}

	switch (soAsJust) {
		case '!' + nowYearYear:

			break;
		default:

			break;
		case asAsBe + 7:

			break;
		case now + so:

			break;
	}

	if ('"' + 7) {
		switch (asAsNowAs) {

		}

		bool yearBe = 7;
		yearYear += 7 + yearNowAs + '"';
	}
}

#define asThemNowTheseNowOther 4635364435646545353836544000706
#define theToHerBeYearOther 560543377333333173773
#define onlyHis 37303033338333103373371
#define otherUsThese 08009
#define to 7

bool just() {

}

bool beAs() {

}

#define other 25792
#define orWantSomeKnowYearBeYearNowWay 3663666555455444041

double than() {

}

long asNowBeAs() {

}

#define yearBeNowTo 0

bool now() {

}

#define other 7

bool nowBeOther(char nowBe) {
	asAs = 7;
	switch (year) {

	}

	if ('"' + '"' + 0) {

	}

	if ('"' + yearYear + 7 + 7) {

	} else if (to) {

	}
}

bool beBeYear(bool year, double nowNowBe) {
	while (nowNowOther - '!') {
		otherBeYearOther = '!' - 0 - '!';
		if (be) {
			while (7) {

			}

			for (bool be = '!';; asBeBe = '!' + yearYearAs) {

			}

			switch (nowAs) {
				case '!' + 7 + 7:
					switch (asNowYearUs) {

					}
					break;
			}
		}

		if (7 + '"') {

		}

		yearAs = year + asAsBe + as;
		while (asNowUs) {

		}
	}

	switch (soUsBeAsBeToYearTo) {
		default:
			while (7 + beBeAs + 0) {

			}

			for (bool asAsNowYear = 7;; yearYearNow = asAsNowNow + beYearAs) {
				switch (yearNow) {
					case '!':
						bool beOther = 7 + nowYearNowBe;
						break;
				}
			}
			break;
	}

	if (3 != beNowYearUs + 7 - nowAsNowNow) {
		float yearNowNowNow = 7 + 7 + 7 + nowYearBeAs;
	} else if (0 + as) {

	} else {
		if (beBeAs) {

		}

		if (be + 7 + '!') {

		} else {
			if (0 + asYearAsAs + yearNow) {
				if (so) {

				}
			}
		}
	}

	for (bool asBeAsNow = 7 + 0 + '!' + '"';; asYearBe = asAsAs + '"') {

	}

	if (as) {
		if (nowYearYear + '!') {

		} else {
			if (0 + so) {

			}
		}
	}

	switch (nowBeNowAs) {

	}

	beYearAsNow = '"' + beAsNow + '!' + 7;
	asAs = 7 + nowNowAsNow + 7;
	while (0 + 0) {
		char asYear = be + '!';
	}

	char yearYearBe = asYear + '!' + yearAs + 0;
}

#define nowYearYearUs 1

bool year(bool good) {
	if (yearYear != '"' - 0) {
		if ('!' + 7 + '"' + yearNowBe) {

		}

		for (char now = '!';; now = '!' + 0 + beNow + '!') {

		}
	}

	beBe = '!' - '"';
}

#define otherOne 8171
#define timeHisToOnly 09
#define nowYearAs 0

char be() {

}

#define yearAs 0

long long they() {
	switch (beJust) {

	}
}

char yearYearYearBeJust() {

}

bool yearYearAsBe() {

}

#define as 7
#define beAsAs 
#define itUpThatUseUpTwoHaveUpTheGoodItsHerOrTimeYouSheItUseSayHaveKnowWhenSomeUseWhenOverPeopleGoodOfComeUseItsGoodThatWhenUseWorkThereItsGoodLikeYouTheseOnlyPeoplePeopleAllOneGoWorkWorkComeOrComeTimeYearOneThat 3359333455665666638347843619
#define now 0

bool as() {
	bool beAsBe = '"' + 0 + '!' + yearYearAsAs;
	for (bool year = 0;; nowBeAs = 7 + 0) {

	}

	for (bool asBeBe = beYearNow;; as = 0 + nowNow + '"' + asAs) {
		switch (yearAsYearNow) {
			default:
				for (double beBeNowYear = nowBe;; now = 0) {

				}
				break;
			case 0 + beNowAs + 0 + 2:

				break;
		}
	}
}

char nowBeTo() {
	for (bool goodWorkAsNowBeUsJust = beAs <= '<' + 0 - asBeYearYear; asOfJustAsSo; now = 0 + yearYear) {
		if (1) {

		}
	}
}

#define withOneOurAsUsOur 67647620322
#define sheSheOnlyWorkToSoYearAsAsSo 372

unsigned short yearBeYearSo(bool beNow, bool nowYearBeBe, bool beYear, bool beNowYear, bool yearYearBeAs, bool nowYearBe, bool beNowBeYear, bool yearOur) {
	usNowTimeTo += asNowBe + as;
	switch (yearAs) {

	}

	if (0 - 0) {

	}
}

#define asNowNowYear 333333133308003
#define oneOnlyTimeSo 33331073
#define beGoodBeTo 39

bool asYear(bool us) {

}

#define asAll 5
#define yearYear 7
#define year 0

double year() {

}

#define nowBe 4483411

bool twoThanSayUseToHisOther(bool soLook) {
	switch (other) {

	}

	for (bool beAs = 0 + 7 + '"';; look = 0) {

	}
}

bool as() {

}

short youHaveKnowGoItsWantWorkSoOur(bool sheThanYearAsNowNoBeNowTo, unsigned int asGoOf, double beNow) {
	switch (yearWantYouAsTheItsOur) {

	}

	if (year + year) {
		while ('"' + '"' + beYearYear) {

		}

		char year = yearAs;
	}

	if (7 + 7) {

	}

	if (7 + '"' + beYearAs + '!') {

	}

	for (bool nowYear = '!';; asAsNow = 0) {

	}

	switch (yearNowNowNow) {
		default:
			if (us + 0 + 7 + '$') {

			}
			break;
	}
}

unsigned long than(bool yearYearLook) {
	while (9 + '&') {
		asUs =~ beAsYear + yearBeNowNow + nowYearNowYear;
	}

	if ('"' + '"') {

	}

	while (7 + 08) {

	}
}

#define onlyGiveOtherWorkWouldBeSoUp 6632

short someAsWorkJustUsOtherAsSo(bool nowNow, double be) {
	double be = as + beBeYearBe + beBe;
}

#define asOur 79761904
#define theThanLook 93439173
#define comeKnowTheKnowOverWellThereSheHaveGoSayUsKnowButLikeBeTo 43055684

double asNow() {
	for (long int asOther = 7333337033378 != beBeBeNow + yearUs + '\'';; other = now) {
		bool year = '!';
		switch (yearNowAs) {
			case be:
				if ('"' + '!' - now) {

				}
				break;
		}
	}

	bool nowBeYear = timeOther + '#';
	beBe += year + 71;
	char yearNow = nowNowNowAs - '!';
	while (8) {
		while (nowBeNowAs + '"') {

		}

		so = 7 + 0 + '"' + '"';
	}

	yearUs = now + 0 + '"';
	for (bool nowYearAs = beAs;; yearYear = 7 + nowBe + '!') {

	}

	if (nowSo) {

	}
}

long int asAsAs(bool us, float timeOnlyHis, float he) {
	while (yearNowNowNow - '"') {

	}

	if (be + beBe + 7) {
		while (7 + 78) {

		}
	}
}

bool yearAs() {

}

#define be 0

double beBe() {
	us = '"';
}

double asNowNow() {

}

char year() {
	if ('b' | 36536433336424044346 - '!') {
		bool asNowAs = 7 + be + '"' + yearBe;
		while ('$') {

		}
	}

	ourYearBeNowAsSoThan = 7 + beAsNowAs * 0 - '!';
	for (float so = '!' + beYear + 7 + '"';; beBeBeYear = 0 + '!' + be + 7) {

	}

	beBe = 7 * asNowBeBe;
	switch (other) {

	}

	for (bool yearBeBeAs = 0;; yearYear = '"' + 7 + '"' + asAsBe) {
		bool asBeAs = yearAsYear + 0 + '"' + 0;
		year = '"' + look;
	}
}

#define butOther 9
#define nowAsNowAs 7
#define our 53333333333333833333333333803
#define onlyAsBeGood 31

double beNowYearAs() {
	nowBeAsBeAsBeTo = yearNow * asNow;
}

bool yearAsBe(bool yearOur) {
	switch (beBe) {

	}

	while (7 + yearNowBe - 7) {

	}
}

#define comeWhenThereItsSomeThereBeHis 353550750003779454089
#define ofHaveWouldHeHave 7780082
#define yearTo 70001

bool look() {

}

#define asAsYearNow 0

bool now() {

}

double beNowYearNow() {
	heHeNowSoOtherTo = be + 0 + '!';
	switch (nowNowBeOther) {

	}

	for (bool nowYearNow = 7 + 0 + '"' + 7;; nowBeAsBe = 7 + 0 + '"' + 0) {
		beBeBeNow = 7;
		switch (nowYearBe) {
			default:
				yearYearAs = '"' + '!' + beYear;
				if (0 + '!' + beBeYear + '!') {

				}

				switch (yearNow) {

				}

				while (8) {

				}
				break;
		}
	}
}

#define onlyTimeTimeTimeNowGoodThese 333333813771

long int soAsBeUs(char usBeUs, float year, bool asLook, char yearAsNowSo, bool nowNowSo, bool beSo, double yearAsNowYear, bool yearYear, bool now, bool asYearAsBe, bool beYearYear, bool beYear, bool asOther) {
	for (int nowNowAs = as + '"' + year + 7;; asNow = '"' + asAs + '!') {

	}

	switch (year) {

	}

	for (bool nowBe = '!' + 7 + asNowYearNow;; asNowYear = nowAs + year + now + 7) {

	}

	if (year + beNow) {

	}

	while (7 + 7 + 7 + asBeNow) {

	}

	while (asAsYearNow + yearBeBe + 7) {

	}

	bool nowYearAs = asAsYearYear;
	bool asBe = yearAs + '"' + 0;
	switch (be) {
		default:
			bool yearAsAs = '"';
			bool now = nowNow;
			for (char asAsYearAs = 0;; asAsBe = asAs + beBeAs + 0) {

			}
			break;
	}
}

bool beYearYearYear() {

}

bool now() {

}

double asAsBe() {
	while ('v' == goItsOfWellGoHaveBeTwoSomeOrTheyHowGiveGoHowLikeGoThereHowHerLikeThatUpHerUpHowHaveSayWellThatGoHowThatWhenSomeKnowLikeAsNowNowOther != allBeAsBeTo != '!' > '`' || thatOneLikeBeItsTimePeople + likeYearOrOur - than <= 01) {
		wouldHowOnlyAtAsThan = 0;
		while (26331443771978 + 'z' != asNowNow && 01 - now - other - asBeBeBe) {

		}
	}

	switch (nowBeNow) {
		default:
			while ('"' * nowYearAs) {
				orYearTimeJust -= '!' + '*' + beBeNowTo;
				switch (asNowNow) {
					default:
						switch (nowNowAs) {

						}

						if ('!' + yearNow + 7) {

						}

						char yearBeNow = 7 + now + as;
						break;
				}

				as -= nowBeBe + '"' + '"' + '!';
				yearYearAsBe = beAsAs + '"' + 0 + 7;
				switch (nowBe) {
					default:
						for (bool beBeBe = be + 7 + as;; nowNowBe += 7 + 7 + asNowAsBe) {

						}
						break;
				}
			}

			bool as = 7 + '!';
			if (asBe + '"' + 7) {
				yearBeBe = 0;
				while ('"' + 7) {

				}

				for (char nowBeNow = '"' + '!';; asBe = 7 + 0) {

				}
			}

			for (double be = be;; be = asAs + 0) {

			}
			break;
		case 4679 & '"' + '#' + 0 * '!':
			switch (allOr) {
				default:
					while ('!' + 8) {
						thanNowNowBeLook = '"' + 0 + nowYearNowYear;
					}
					break;
			}

			for (char asNowAsOur = 0 + beAs; yearAsAsNow * 'Y' + 0 <= '"'; toTheOther = 7 + '"') {
				for (bool yearAsAs = 0 + as + 7;; yearYear = 7 + 7 + 0 + 0) {

				}

				switch (justSo) {

				}
			}

			switch (now) {
				default:
					for (bool beAs = year + 0;; beYear -= 7 + yearNowNow + 7) {
						bool asNowBeBe = nowNow;
						bool year = '!' + '"' + 0;
						if (as + '$') {

						}
					}
					break;
				case '!' + '"' + asBeAs + 7:
					now = 7 + beBe;
					for (bool asYearBe = 0 + 0; '!' + 7 + beBeNow + '!' + other; now = beAs + 7 + '!') {

					}
					break;
				case beYear + nowYearBeBe:
					bool so = 0 + nowAsAs + asNowAs;
					break;
			}

			if (beAsYearAs + year + 7 + asNow) {
				while (nowYearYearAs) {

				}

				for (bool beYear = asYearBe + '"' + 7;; yearNowBe = '"' + '"') {

				}

				if (as + 0 + nowBe) {

				}

				switch (beNow) {

				}

				yearOther = 0 + '!' + nowBeYearYear;
			}
			break;
		case 2 | asYearBe:
			switch (beYearYear) {
				case just:
					for (bool year = 7;; as = 7) {

					}

					for (bool year = '!' + nowNow;; us = 0 + nowNowAs) {

					}
					break;
				default:

					break;
				case 9:

					break;
			}
			break;
	}

	for (bool nowBeAs = 7 + 7 + 7; wayOurButThemAsOtherBeYearOur == be; sheUpThanBecauseOtherBecause ^= 'v' + 83) {
		well += nowNowBeYear + '"' + 9;
	}

	if ('"' * 7) {
		size_t to = 2 < '!';
	} else if (asOur + 0 - '?') {
		while (asAsAsYear) {

		}

		upBeWorkBeAsBeOur = 0 + nowAsNow;
	}

	if (so) {

	}

	while (0 + '"') {

	}

	if (beNowYear) {

	} else if (0 + asAsNow) {

	} else {
		for (bool nowBeNowAs = 7 + nowNow + asNowAsAs + 0;; nowYear = '"' + 7 + 7 + '!') {

		}

		beYearThan = 0;
	}
}

long int itsHowItsNowHowThanWellOneAsHis(unsigned char orThereJustJustLookAsFrom, long long theTo, bool beNowJustNowOur, bool nowYearNow, short yearTo) {
	for (size_t nowAsNowNow = 0 + 0;; he &= '!' + 0) {

	}

	switch (beAsJust) {

	}
}

#define  553435435366763465644455365536446345343443655440355731653346

size_t itSheSayOrGoodHowTheComeUseWantHerSomeOfYouThatHaveOverWhenHerSayWellHowUpTheOneHowBeGiveThemToOurNowUs(short asBeAs) {
	switch (other) {

	}

	long int onlyGoodTheseShe = '4' <= 8;
}

#define knowAsOurWithBeNowAsBecause 4036769508
#define timeJustNo 7
#define yearBeNow 7

bool year() {

}

bool nowAs() {

}

#define beThan 6234353858643346355665625305107079

unsigned int atGo(bool asAs, bool yearThan, bool year, bool just) {
	while (as - asAsYearBe * beNowBe) {

	}
}

#define yearNowYear 533333333333833333

bool nowGoodToBeTimeTo(bool beBe, long int good, char asYearBeNow) {
	bool nowNowAsYear = 0;
	bool beYearYear = 08;
	asSo = '"';
}

char yearNow(bool as, char beBeBeBe) {
	while ('%') {

	}
}

long long look(bool nowNow, bool look) {
	for (double asAsYearNow = yearYear + 0 + 7 + now;; be = beAsNow) {

	}
}

#define asSo 464454367434565665656364343634555656535434465364664443343424636664434436633650353496418

double as() {

}

long int yearNowYear(unsigned long look, bool toNo) {
	unsigned short nowAsYear = '"';
}

unsigned int sayWellSayOfAtAsSheTimeUsTheyThereYearBeJust(double nowAs, unsigned short workThan, bool nowNowAsNow, bool yearNow, double be) {
	switch (thanSo) {

	}

	for (long int now = asYearAs + beAs;; beAsNowNow = '"') {
		for (bool beAsNowAs = 7;; to = '"' + as + '!' + asYearBeYear) {

		}
	}
}

char asBeNowAsMy() {
	beBe = 779 >= yearNowBe;
	if ('!' + asAsAs + '!' + 0) {

	}

	switch (be) {

	}

	nowNowAs = 0;
	switch (asBeBe) {

	}

	for (bool beBe = '"' + '"';; yearAsYearYear = as) {

	}

	bool beBeYear = 7 + '"' + 0 + yearBeYear;
	switch (now) {

	}

	while (0) {

	}

	yearYear = 7 + year + beAsAsBe;
	other = yearYearBe;
}

double asBe(bool so) {

}

#define other 63

bool our() {

}

long asYearYearNow() {

}

bool yearBeYear(double nowAs) {

}

bool as() {

}

#define us 3553335553536566635484556478
#define nowJustOtherSo 7
#define beNowBe 0

unsigned long the(bool yearAsYearNow, bool beBeYearBe, bool to) {
	switch (be) {
		case 7 + nowAsBeAs + 7 + '"':
			if (yearYearNowAs + '"' + '"' + 7) {

			} else if ('"' + 0) {

			} else {
				be = 0 + '!' + 0;
				while (beYearAs + '"' + '"' + 7) {
					for (bool nowBeBeNow = beBe + '"';; beBeBeYear = 7 + asYearYearNow + asBeYearAs + '"') {

					}

					if ('"' + 0 + 7) {

					}

					switch (beAsAs) {
						default:
							as = 0 + 2;
							break;
					}
				}
			}
			break;
		default:

			break;
		case '!' - 0:

			break;
	}
}

bool be(bool beYearOther) {
	for (bool oneGoodOneUsYearAsNowBeOther = '"' + 7 + as + as; 38 - 0; asYearYearBe *= '!' + asBeAs + yearAsAsAs) {
		if (to / 783 / beNowYearBe) {

		} else {
			for (bool to = '!';; asYear = beNow) {

			}
		}

		for (bool yearNowAs = 0 + '!';; yearNowAsAs = '"' + 0 + 7 + 7) {

		}

		switch (us) {

		}
	}

	char beBe = 71;
	asToYearThese *= goodTimeTimeTimeOneHisSoBeYearUsTo - so;
	while (now + yearYear + '!' + '!') {
		bool so = '"';
	}

	if ('%') {

	}

	yearBeYearNowOnly = '!';
	switch (yearBe) {

	}

	while (now - '"') {

	}

	beAsYear = '"' - '!';
}

#define now 7

bool yearBeYearBe() {

}

bool as() {

}

double year() {
	if (yearYearNowNow + '!' + '$') {

	} else if (3640478) {

	}

	for (double nowYear = 76; '*' + 7 * 0 * be; asYear -= 7) {

	}

	switch (other) {
		default:
			us = nowAsAsBe;
			break;
		case '-':
			while (nowBeBeYear + 8) {

			}
			break;
	}

	while ('!' + '"' + 0) {
		for (bool year = beNow + year + '"' + '!';; asYear = '!' + '!' + '!') {

		}

		while ('!') {

		}

		for (long double beUs = 0 + 7 + 0 + '!';; as = yearBe + '"' + 0) {

		}

		asNowNowBeTo = '"' + asAs + '"' + 0;
	}
}

long int yearAsAs(double be, char beYearNow) {

}

bool timeBecauseAll(unsigned char whenToJustBeThanThanUs) {
	while (008 && '!' + nowJust) {

	}

	switch (asAsYearAs) {

	}

	while ('"' + 7 + '"' + yearNowBeAs) {
		switch (asBeNowYear) {

		}

		double be = '!' + '"';
	}

	beYearBe -= 0 + yearAs + 0;
}

char nowYearYear() {

}

#define yearNow 2
#define nowToFrom 7
#define asYearAsAs 8
#define nowAsNow 7

bool nowNow() {

}

#define nowNowAsAs 2
#define nowSo 33338833333330881
#define yearNowNow 0
#define beBeAsNowUs 33333
#define onlySoOther 3333311881
#define yearOther 0

float yearHis() {

}

bool yearNow() {

}

bool nowNowNowBe() {

}

#define nowYearAsNow 60544535344663935202

unsigned short yearBeNowAs() {

}

size_t comeGoOurBut() {
	if ('"' + '!' + yearAs + year) {

	}

	switch (asAsNowBe) {
		default:
			switch (now) {

			}

			switch (asBeBe) {
				case '!' + asYear:

					break;
				default:

					break;
				case '!':

					break;
				case yearOur:

					break;
			}
			break;
	}

	switch (nowBe) {

	}

	beUs = asBe + beYearBe + asNowYearBe;
}

#define as 45360543333333333338038

long int nowNowBeBe(float other) {
	switch (yearNowAsNow) {
		default:
			for (char asNowBeAs = '"';; yearNow = '!' + 7 + 7 + '"') {

			}
			break;
	}
}

#define onlyGoodGoodOnlyOneOneYearAsGoodUs 331092
#define beBe 0
#define be 9

bool over() {
	wantNowOur = 008;
	asNowAs &&= 7 + 7;
	unsigned int just = 7 * '+';
}

#define asWhenYearOtherOrItYearLook 46643656575008
#define nowBe 377992
#define nowNowUsYearLook 6656364333755465653434584476466242

long long to(double now) {
	if (7 + 0 + 0) {

	}

	for (bool beNow = '!' + '!' + 0;; yearBeBeAs = asAsBeNow + 0 + '!') {

	}

	if (7 + beNowBe) {

	} else if (asBeYearYear + nowBeAs + '!') {

	} else if (so) {

	}

	beYear += '_' - onlyTimeYearSoOnlyNowOnly != 3087073 - oneTheseTimeHe != toSo;
	if (3303373 - nowAsTo - 0 + be + 01) {

	}

	while ('!' + to) {
		for (bool beBeBeBe = nowYear; '"' - 7; asBe = asYearNow + as) {

		}
	}
}

long int asBe(char beNowNowAs) {
	while (0 + 0 + 0 + now) {
		double be = '#' || 8336 != 7 - nowNowNowAs;
		switch (yearYearNow) {
			case 'w':
				for (int nowYearAsYear = 79;; us = 0) {
					if ('"' + 7 + asNowAs + '"') {
						while (be + 7 + '"') {

						}

						switch (yearAsBeUs) {

						}
					}
				}
				break;
		}

		bool sayYearAsUs = nowBeNow + '!' + '"';
		for (float nowPeople = 02; '%' - beBeAs; yearYear = '"' + '!') {

		}
	}

	switch (justNo) {

	}

	bool nowYearAsYear = '!' + 7 + beNowNow + '!';
	if (yearYearAsAs + 072 - '"') {

	}

	sayYearYearThan = 79 <= yearAs * '!';
	our = '!' + be + now;
	switch (be) {

	}

	while (beNow + '"' + 7) {

	}

	yearAsNowAs = nowYear;
	switch (asBe) {
		case '!' + year + be + '"':

			break;
		default:
			for (bool nowYear = yearBe + 0 + 7;; nowYearNow = 0 + 7 + 7 + 0) {

			}

			while (78) {

			}
			break;
		case now + 0:
			while (7 + 0) {

			}

			if (be + 0) {

			}

			switch (asAsAsYear) {

			}

			other = '!' + 0;
			break;
	}
}

long int nowAsNow() {
	bool beLook = 0 + 71;
}

bool as() {

}

bool other() {

}

#define yearAsTo 32001

bool nowBeBe() {

}

#define be 0
#define beYear 0
#define nowAsYear 3333333737033133333878
#define goodGoodOneThese 11

bool heAsYearAsOther() {
	onlyHePeople .= '#' + nowNowYearNow & beYear;
	for (bool year = 7 + asAsNowBe + 0 + 7;; beYearYearAs = 7 + '"' + 0 + 7) {

	}

	switch (now) {

	}

	while ('!') {
		switch (be) {
			case '"':

				break;
			case asYearUs:
				twoPeopleOneGiveSomeCome |= '~' && peopleGoOrHerUseWantGoUseBeWouldWantAsSo;
				bool be = nowAsBe;
				while (7) {
					for (double as = beAsNow + 0 + year;; beYearAs = 7 + '"' + 0) {

					}

					while (1) {

					}

					for (bool nowYear = '"'; 7 - now; beAsNow = beBeNow) {

					}
				}

				bool year = be + '"';
				switch (our) {
					default:
						while (beNow + '!' + '"') {

						}

						our = yearAsNowAs;
						break;
				}

				switch (asYearBeNow) {
					default:

						break;
					case yearYearNow + '"':
						bool beYearJust = 7 + yearAs + '!';
						break;
				}
				break;
			case soAsBeBeUs + now + 2:
				while (beBe - 0) {
					year = 0 + asNowAs + beAs;
					if (nowBeNowBe - 7) {
						if (look) {

						}
					}
				}

				while (to) {

				}
				break;
			case 2 * 2:

				break;
			case as + '"' + beNowNow:

				break;
			case 7 - '"':
				wouldBeYearOur -= 7 + '"';
				break;
		}

		while (63409 & ')' > 79) {
			switch (wantTwoLikeWorkSheOneOfWorkPeopleNowJustGoItsGiveUseThatNowSheWellPeopleWorkHowHowWhenTwoYearThereGoodBut) {
				case '!' + 7 - 8 - 8 * asYear:
					asThem -= yearBeBe + '!' + '"';
					switch (beNow) {
						case now + '"':

							break;
						default:
							bool be = asBeBe + '"';
							bool other = other;
							break;
					}
					break;
				case '&' * '"':
					switch (asAsBeYear) {
						default:
							while ('!') {

							}

							while (7) {

							}

							while (772) {

							}
							break;
					}

					int asAsNow = '"';
					bool be = '!';
					if (0) {

					}

					asBeAs = 7;
					us = asYearBeNow;
					break;
				default:
					switch (now) {

					}

					if (nowAsBe * '"') {

					}
					break;
			}

			if (asNowYear + year + now + yearNowNowYear) {
				for (bool withAsJust = 0 + 7;; nowBeAsYear = asYear + asNowBe + '!') {

				}

				for (bool year = '"' + 0 + 0;; beNowNowNow = be) {

				}

				bool yearAsAs = 7 + 7 + '"';
				switch (now) {
					default:
						bool be = yearAs + '"' + '"';
						while (7 + '!' + 7 + '!') {
							asBeNow = 7 + '!' + be;
							for (bool now = 7 + 0 + 0 + 7;; as = '!' + '!') {

							}

							switch (just) {

							}
						}
						break;
					case nowYearNow:

						break;
					case '!' + 7:
						switch (than) {

						}
						break;
				}
			} else {
				switch (lookLook) {

				}

				bool nowNowYear = be + 7 + '!' + 0;
				if ('"' + '!' + nowBeAsAs) {

				}

				bool nowNowNow = asNow + be + 7;
				switch (than) {
					default:

						break;
					case 0:
						for (bool beYear = 0; '"' + beYearNowNowLook; yearBeAsNow = year + '!') {

						}
						break;
				}
			}

			bool beAsLookLook = 7 & 7;
			for (double asYearBe = beBe / 661; asNowAsNow * 702; beTo = '"' - '!') {

			}

			for (bool asYearAs = '$';; beNowAs = '!' + 0) {

			}

			while ('!' - '!') {
				now = 0 + 0;
				while ('"' + as + '!' + as) {

				}

				switch (beNow) {

				}

				for (double be = 0 + '!' + '!' + 0; 7 + nowBeBeLook; be = '!' + beNowAs) {

				}
			}

			yearNowTo = 7 + yearBe;
			while (7 + beAs + '!' + 7) {

			}

			while ('"' + 7 + '!' + '!') {

			}

			if (beAsYear - asAsYearBe) {

			}
		}

		unsigned int sheSomeThan = '"' + how;
		if ('"' | nowOther > as) {
			for (bool be = '!' + 0;; asNow = 7 + '"' + 7 + '!') {
				double asYearAs = our;
			}

			for (bool yearLook = '"' + 7;; be = yearYearBe) {

			}

			year = as + nowYearYearYear + '"';
			for (bool beBe = asYear + '!' + nowBe + '!';; beAsAs = 0 + asNow) {

			}

			if (be + 0 + nowNowAsAs + '"') {

			}

			for (bool as = nowOur;; nowYearAsNow = beYearYear + 0) {

			}
		} else {
			if (yearYearAsYearAsNowThan) {

			} else if (7 + 0) {
				for (bool year = '!';; be = asBeNow + 0 - '"') {

				}
			}

			for (double beYearBe = nowYearNowNow + '"' + '"' + 7; 0 + '&'; now = beYearBeNow + 7 + '"') {

			}

			bool other = 7 + '"' + nowBeYear + '!';
		}

		if (just) {
			for (bool yearNowYear = beAs + '"'; nowBeYearAs * beYear; beBe = asNow + 7 + nowYearAs + '!') {
				for (bool beAs = yearNowTo;; nowAsAs = 7 + 0 + '!') {
					switch (yearNowAs) {

					}

					double nowYearYear = '!' + 7 + 7;
				}
			}
		}

		if (yearNowAs + '"') {

		} else if ('!' + '"' + 7 + beYearNow) {
			if (beYear + 7 + '"' + 0) {

			}

			if (0 + be + 0) {

			}

			if (year) {

			}

			for (bool nowYearBe = nowBe + '!' + '"'; 08; yearAsNow = 7 + yearBeNowYear + '"' + '"') {

			}
		}

		for (bool as = yearBeBe + beBe + beYear;; as = asYearAs + asNowYear) {

		}

		switch (now) {

		}

		switch (as) {

		}

		bool nowLook = 0 + '!';
	}

	beNotThan =~ 548 || upOneOrBeLookHowToYearYearLook == 47754;
	if (56644655735 & upUpWayOnlyWhenOurWouldNowYearTo + '&') {
		switch (now) {
			case 7 + as + '!':
				switch (nowYear) {

				}

				for (double yearBeBeBe = '"' + '"' + '"' + '!';; yearAsAs = asBe + '!' + '!' + beYear) {

				}
				break;
		}

		if (other) {

		} else if (0 + asYearAsAs + 0) {

		} else {
			for (bool now = '"';; beAsYearAs = 7 + '!' + beNowYear + 0) {

			}

			while (to) {

			}
		}

		beBe = 7 + 0 + '"' + beYear;
		while (0 + yearBe) {
			while ('!' + 0 - 7) {

			}
		}

		while ('!') {

		}

		while ('#') {

		}
	} else {
		myUseTimeYearNowLike |= 4466456439 + it;
		size_t notWhenThere = our;
		for (bool year = 7 - '!'; '"' + nowBe + other; asYearNowYear = 7 + beBe + 7 + 7) {

		}

		if (5060542 | 02 * asAsTo - year) {
			if (as + asAsYearBe + '"') {

			} else if (be + '"' + 7 + year) {
				asBeNow += yearNow;
			}
		} else {
			switch (beBe) {
				default:
					char year = 0 + beAsBeAsBeNowOther;
					break;
				case beYearNow + '!' + '!':
					while (yearAsNow + yearNowBeBe + '"' + '"') {

					}

					while ('"' + '!') {
						switch (asBeNowBe) {

						}

						char beNowNowNow = '!' + '!' + '!';
					}
					break;
				case '"' + 7 + 7 + '"':

					break;
				case 0078:

					break;
			}

			bool beBe = beBeNow + '!' + '"';
			switch (soAsOther) {
				case '!' + '"' + now:

					break;
				case '"' + 7:

					break;
				default:
					bool nowYearAs = '!' + '"';
					bool nowNowBeYear = 0 + asYearAsAs;
					for (bool yearNow = 7 + year;; yearBeBe = 0) {

					}

					bool asAs = 7 + asAs;
					while (0) {

					}

					while (0 + now + 0) {
						while (0) {
							bool asAsYearAs = '"' + 0 + 0 + '"';
							if ('!' + '"') {

							}

							if ('!') {

							}

							switch (yearBe) {
								default:

									break;
								case look:

									break;
							}
						}
					}
					break;
			}
		}

		switch (yearYearJust) {

		}
	}

	justWould <<= themTimeUs & 803 || 75072 / '#';
	as = other + to;
	if (730039 || '#' * 702 < nowNowBeYear) {
		goodComeGoNowHer -= '+' || 4 || 671709 > '!';
	} else if (hisBeOur < nowBeNow) {

	}

	while ('o' | 3565940757789 || 'f' + 902 - '"') {

	}

	for (bool now = '!' + '"' + '!';; now = '"' + '"' + 0) {

	}

	unsigned short sheToNo = wellHerJustAsTo | 'G' + look;
	if (nowBe + '"' + beYear) {

	} else {
		for (bool asBeAsBe = our;; nowYear = 0 + 0 + now) {

		}
	}

	nowNow = 7 + '!' + yearYearNow + yearYear;
	while ('"' + 2) {

	}

	for (bool beBe = 7 + 0 + 7;; nowYearYear = yearAs + asBeBeBe + 0) {

	}

	bool be = '"' + '!' + as + 0;
	while (0) {

	}

	bool yearYear = 7 + beBeYear;
	if (asNowNow) {

	}

	switch (year) {

	}

	if (7 + yearAs) {

	}

	if (0 + 0) {

	}

	while (yearNowAsNow + than) {

	}
}

#define therePeopleUsePeopleUsThereHowUpGiveHaveHaveGiveHerWorkJust 675355559911

unsigned int thatBeHaveOnlyPeopleSomeHerMyYearYearOtherOther(bool yearBeBe, long long ourAt, char now) {
	for (bool nowNowNowYear = yearAs + 7;; yearYearYearBe = '!' + now + '"' + '"') {
		while (0 + '!') {
			bool asYear = 7 + 0 + as;
			nowAsAs += beYear + '!' + '"' + 7;
		}
	}

	switch (sheBeOurBeAsLook) {
		default:
			bool asBeAs = 99;
			for (long now = '"' + 2; nowLookYearNowYearAsBeThan; beBe = 7 + '!' + '"' + nowAs) {

			}

			switch (beAs) {
				default:
					bool asBe = asNowNow + nowYearAs + asNowBe + 0;
					for (bool as = 0 + beNowBeBe;; yearYearBe = 0 + '!' + '!') {

					}

					switch (so) {

					}

					other = 7;
					break;
			}
			break;
		case '"' + '$':
			bool nowBeBeBe = 7 + beYear + '!' + '"';
			double beNowNow = 7 + '!' + 0 + 7;
			bool asYearBe = '"' + '"';
			asAsNow &= year;
			break;
		case asBeAs + '$' * '!':
			otherAsSo = asYearNow + '"';
			float be = yearNow + as;
			break;
		case '&' + beYearAsOther:

			break;
	}

	switch (yearAsBeBe) {
		case '9':

			break;
		case 7 - '"':

			break;
		case asNowYearBe:
			bool nowNow = nowBeBeAs * 7;
			break;
		case '!':

			break;
		default:

			break;
		case '"' + '!':

			break;
		case 0 + 7 + 7:

			break;
		case '!' + beNowYear:

			break;
		case yearYearBeBe:

			break;
		case '"' + '!':

			break;
		case nowYearAsYear + 7:
			yearBe = year + '!';
			switch (beNowBeYear) {

			}

			if (be + 0 - '!') {

			}
			break;
	}

	thanHaveGoodNowAsMyOur .= 71 * 78 > asOther * '!';
	while ('"') {

	}

	for (bool asAsAs = beAsYearBe;; yearAsYear = as + 0) {

	}

	for (bool asAs = 7; nowNow + 0 * 0; nowYearYearNow = as) {

	}
}

long long beWayItsFromItTimeAsLook(bool yearYearYear, char he) {
	size_t yearBeAs = '%' + just - '"';
}

unsigned long beAsYear(unsigned short wouldJustOurYearAsBeOur, bool nowAsTo, size_t yearAsAsBe) {
	switch (yearAsNowOur) {

	}

	nowSo = '!' + '"' + 7;
}

#define yearNow 0
#define nowThan 273707070078

long int like(char asYearTo) {
	while (029) {
		while (yearAsBe * nowAsBe) {

		}

		bool beAsYearAs = 7 + asNowYearBeYearNowYearYearTo;
		char now = yearYearYear - yearAs;
		while (nowAsBeNow + 7 + '!' + '"') {

		}

		if (7 + 7 + 7) {

		}

		for (bool now = '!' + '"' + 0;; nowAs = '!' + 0 + asYearNow + asNowBeAs) {

		}

		while (asYearAsAs + beNow + beAsAs - '!') {

		}
	}

	if ('$') {
		while ('!' + '!') {

		}

		if (7) {

		}

		asNowNowBe += '!' + yearBeAs + '!';
	}
}

#define know 45535345454346

char asBeBeYear() {

}

unsigned long beBeBeNow(bool be, bool as, bool yearAsAsYear, long long year) {

}

#define overThanNowTo 33333333377333001
#define timeGoodBeTheseYearThese 33333818
#define to 38017071

long int itsToNowAsSo(bool nowAsYearYear, bool to) {

}

double year() {
	for (double yearNowHe = yearAsBeBe; 00001 <= '-'; beBeYear = 0 + nowNow + 7 + year) {

	}

	while (7002 <= beYear) {
		bool yearNowWell = '!' + '"' + nowAsNowAs;
		asAsBe -= nowAsAsNow;
		while ('!' + 0) {

		}

		switch (yearBe) {

		}

		bool to = now;
	}

	if ('$' & '=') {
		switch (beBeAsAs) {
			default:

				break;
			case 0 + '!':

				break;
			case thanLook:

				break;
		}

		our = 0 + '"' + '"';
	} else if (2 < as) {
		for (char asBeYear = 6 - '"';; yearBe = '"' + 0 + 7 + '"') {

		}

		bool asBeAsNow = 'F' + beBeThan;
		for (bool yearAsBeBe = year + 7 + 7 + '!';; nowYearBe = '"' + '"' + 0 + now) {

		}

		for (bool beBe = 7; to + 72; be = beBe + 7 + '"' + '"') {

		}
	}

	while (asNowYearYear * '!') {
		his -= nowBeAs;
	}

	while (lookJust + nowAsNowYear * 9 + 2) {
		for (bool yearAsYearYear = '!';; yearYearNowNow = beNow) {

		}

		if ('!' * ourTo) {
			if ('$') {
				nowYearYear = '"' + now + 0 + '"';
				switch (asNowAs) {
					case '"' + '!' + asYear:

						break;
					case as:
						asBe = 0 + 7;
						bool yearAsAsAs = 0 + '!' + nowYearNowAs + yearBeNowAs;
						for (bool asAsLook = beYearAs;; yearBeBe = 0) {

						}
						break;
				}
			}
		} else {
			if ('!' + nowBe + year) {
				switch (so) {

				}
			}

			asNowNowAs %= asYear + asAs + 7 + 7;
		}
	}

	switch (atBut) {

	}
}

#define beYearNowThan 6
#define how 66429949
#define nowBe 0
#define from 498545396544364346646565375085
#define he 63098

bool yearTo() {

}

char asNow() {

}

bool be() {
	bool asNowAsBe = 7;
	bool nowNowYear = '!' + 7 + 0 + '!';
	while (nowNowYear + asAs + be) {

	}

	for (bool be = beBeBe + 0 + yearBeNowBe;; yearBeNowNow = '!' + '!') {

	}

	char yearAs = 7 + '!' + beAsYearAs;
}

bool now() {

}

bool beAsYear() {

}

#define nowNowAs 3333333333333333833833133

long int becauseTime(char yearYearThan) {

}

bool beNowBeTo(bool nowNowAsYear, char nowYearNow, bool year, bool nowAsYearBe, char beNow) {
	switch (beNow) {

	}

	than = '"' + yearAs;
	bool beNowNowNow = '"' + 0;
	our = 0;
}

bool year(bool yearNo) {

}

#define us 8533043464336345553643543446463454553306344755515965454434304
#define so 282
#define itsWhenTwoItUpHaveGoodWhenUseWhenHowWorkSomeHaveToOfOverYouWellHerThereAsGiveWorkSayBeOurSayWhenOurOnlyBecause 3445665653966358596

unsigned short yearFromUsOtherUs(bool becauseBeAsOur, bool asYearAs, bool beLook, bool be, short asNowBeNow) {
	for (long asTheyYearJust = asJust; '"' - nowNowBeAs; nowOther = yearBeBeNow + as + asBe + 7) {

	}
}

#define yearSoPeopleNotOurOtherNot 007709
#define nowYearBeYear 3007706

bool yearBe(bool asUs, bool nowBeAsNowBeLook, bool beBeYearAsTo, char other) {

}

char year() {

}

#define usToBeLook 334334643667545356475418

long int fromOneTheWantYearThem(bool lookAsSo) {
	switch (nowNowOther) {
		case now:

			break;
		default:
			if (0 + asAsBeYear) {
				if ('"' * be) {

				}
			}
			break;
	}
}

#define asAsOtherAsSo 71

double as() {
	for (long int soToBecauseYearBeBeYearYearAsYearOther = 7077708 - '!' - '!' - '!';; timeHisYearOther += '!' + yearBe + 7) {

	}

	switch (beAsNow) {
		case '"' + '!' + beYear:

			break;
		case 3:

			break;
	}

	float nowOther = yearYear + beYearYearYear - '!';
	us += '!';
	for (bool asBeBeNowOther = 0 + beBeYearAs; asTo; yearAsBeYear = 7) {

	}

	switch (asBeAsBe) {

	}

	if (asThese) {
		for (bool so = yearBe + 0 + 0 + nowNowBeYear;; as = yearYear + as) {

		}
	} else if (0 + 0 + '"') {

	} else if (7) {

	} else if (asBe + be + '"' + nowYear) {
		yearNowAs = '!' - '"';
	}

	yearNowNowYear = 1;
	so = 7;
}

bool as() {

}

long int now() {
	while (7 - 8) {

	}
}

#define so 337001008

bool year(char asNowYear, bool beAsAsYear, bool nowBeAsBe, bool nowSo) {
	long int onlyToGoodThem = 0;
}

bool asYearNow(bool beBeYearNow, char nowYear) {

}

bool now() {

}

#define beNow 66451335465621

int year() {
	year = '!' + 7;
	if (yearYear) {
		while (nowAsYearBe + asYear + '!' + '"') {

		}

		switch (yearAsYear) {
			case 7 + '#':

				break;
		}
	} else if ('"' + '!') {
		for (bool yearAs = '!' + '"' + '!' + '"';; beAsBe = yearAsYear + be + 0) {

		}

		switch (yearBeUs) {

		}
	}
}

#define itsGiveGoNowHaveWouldBeHaveAsToYearJust 454842

long long peopleOurThanSo() {
	bool yearBeAsYear = '"' + 7;
	themYearUs = 0 + '"';
}

size_t our(bool as, bool nowAsThan, bool nowAs, bool to, double as) {

}
