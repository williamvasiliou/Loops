#include <UUUUUEU6UUE61.h>

short yearBe() {

}

bool be() {

}

bool beYearNowBe() {

}

bool soYearJust() {

}

bool beYearBe() {

}

#define yearYearYear 7

bool year() {
	bool year = us;
}

#define now 8071

bool asAsNow() {

}

#define nowAs 33666344534333333333333333333333333333346345356456445646536534566454466566453333466353465565333564634453433353635545543503636586464645356365544335564554444453464666645335055641476
#define heHowUseWorkItsHerUpYouItTimeYouAsGoLikeUseSayTheUseKnowSomeLookOnlyHaveBeBeBeJust 355644565654575347009500671

short sheTheSomeHowWantTimeGoHerTheYearLookAsOrSome(unsigned short she, bool nowAs, long double yearYear, bool asBe, bool beBeNowYear, bool be, double nowBeAs) {
	if (so + 's' * 0) {

	}

	long double asAsYearYear = 0 + nowBe + 0;
	unsigned char yearUsYearThan = 4 % asUs;
	asBeBe += 8;
	for (bool yearYearAsAs = 0;; be = 0 + asBeYear + '!' + 7) {

	}

	if (9) {
		if (0 + yearNowBe + '"' + '"') {

		}

		if (0) {

		}

		year = 0 + beNow + year + beYear;
		if ('"' + '!' + '"' + asAsYearBe) {

		}

		if (0 + 0 + yearAsBe + be) {

		} else {
			switch (yearBe) {

			}

			than = yearAsYear + as;
		}
	} else if (now + '"' + 7 + '!') {

	} else {
		bool beBeYear = '"' + beNow;
		if (beNowNow + 7 + nowNowBe + 0) {

		}

		beAs = '!' + asUs;
	}
}

#define sheHerJustWellGoodGoTheAtThereSheComeLikeTheseOnlyBeAsNowLook 7548701

long int ofComeTheseThatSomeWithTheyThan(unsigned long wellAtOverOtherOrComeOverSoSomeAsOfLook, short asNowOther, unsigned short asWith, bool asNowUs) {
	int notGiveSoThan = thereYearOur;
}

bool so() {

}

#define just 0004
#define beOther 80771
#define fromAsNotHe 0
#define asYear 765054454646346515553184392
#define sayOrHerGoWayYearLook 362

bool beAsNow() {
	switch (toTo) {
		case 7 + 77778:

			break;
		case '"':

			break;
		case '"' + 7 - '!':

			break;
	}

	other |= nowTheseTo != 1 - beBeAs;
	if ('"' + 0 + 0 + asNow) {

	} else if (nowBe + '"' - '"') {

	} else if (0 + '!' + '!' + '!') {

	} else {
		switch (other) {

		}
	}
}

#define usGood 83383711

long int onlyOneGoodUsTheseOne(float asAsNow) {
	for (char now = '!' + 7 + nowNowBeNow + 0;; as = be + '!') {
		while (0 + '!') {

		}

		while ('!' + asYearBe + '!') {

		}

		if ('!' + beYearNow) {

		}

		switch (now) {
			case '!' + asAs + '"':
				for (char asAsAsAs = now + beBeAs + 0 + 7;; asYearBeAs = '"' + 0) {

				}
				break;
		}
	}

	yearAsOtherThanOther = nowOtherWith + 00902;
	bool nowYearNowNow = 7;
	for (bool beNowAsAs = '"' + beBe + '"' + '!';; asYearNowBe = 7 + asBeBeYear + '!') {

	}

	for (bool nowAs = '"' + 7 + '"' + 7;; year = 7) {

	}

	char beBeYear = yearAs + 0;
}

#define as 1
#define yearOther 56665664666366464535654564666566356663436536365664455663466666446654655665565446653549403446635634665365647663453566344653565446364435206653
#define  6432634709292

long long wellComeGiveWantThatSomeTimePeopleWellTwoKnowGiveHaveWantOverGoodWorkPeopleWorkWhenOnlyGoGoodWantItsHaveSomeThereWantGoUpUseHowWantSheOverWantLikeWhenWellWhenWantThereGoOtherComeYouItTwoWayYearUs(bool than) {
	just += '!' + '"';
	switch (than) {

	}
}

#define wellOrWhenItsGoTheWantKnowWorkComeOfItUpOnlyWhenYouWantWantItsThanTheyThatNowTheSomeSo 564657546451669202

size_t goodSomePeoplePeopleYearYearWouldGiveUsSomeOrHowWould(bool peopleNowBeThemThan, double yearNowBeBe, bool asToOur, char be, char as) {
	while ('~' & 309 * beBecauseBecause / lookOther + 7 + 001) {
		switch (yearYearBe) {
			default:

				break;
			case be + now < 39:

				break;
			case asNow + 0 + '!':

				break;
			case 09:

				break;
			case '"':
				switch (beNow) {

				}

				double beYearYearYear = beNowYearAs + '"' + yearBeAs + yearBeNowBe;
				break;
		}
	}

	hisPeopleJustNowWay = 7 + '!' + '"' + '!';
	while ('%' | all * 0) {
		for (bool yearYear = '"'; 02072; yearYear = 7 + asYearYearNow) {

		}

		while (78) {
			bool be = beNow + asBeBe + beYearBeYear;
			switch (asBe) {

			}

			for (bool be = 7;; asNowAsNow = beBe) {

			}

			bool yearYearYear = beNowBe + '!' + 7 + 0;
			bool yearOther = be + 7 + 0 + as;
		}

		nowBeNowAs = 7 + 0 + '!' + '!';
		while (so) {

		}

		long double to = nowAs + 0;
	}
}

unsigned long overOverGiveItsOverAtWhenComeYearOther() {
	for (bool nowBe = asYearBeNow + now + 7 + asAs;; nowBe = 7 + '!') {

	}

	switch (nowAsAs) {

	}

	while ('!' * 7) {

	}
}

unsigned long itsOfNowWellAsHavePeopleComeComeWellWhenTwoSayTwoTwoWorkThatWorkTwoYouItsOneWorkGoItsYearOurOrUpAsPeopleYouBeBeYearOther(unsigned long beThe, char nowYearNow) {
	long long fromSomeOne = 702;
	switch (itYouNowThanPeopleWay) {

	}

	switch (beThanOther) {
		case nowBeBeYear + yearNowYearYear + nowBe:
			float beNowYearBe = yearAs + '!' + 7 + 0;
			break;
		default:

			break;
		case now + '!' + '"':
			for (bool yearYearAsBe = asNowAsNow; our; just = '"' + 7) {

			}
			break;
	}

	for (bool as = 0 + asYear + '"' + as;; nowNowNow = 0 + 7 + nowYearNow + 0) {

	}

	switch (nowNowYearNow) {
		case 0 + be + 7:

			break;
		default:
			for (bool nowYear = 7 + now + 7 + 0; nowBeNowAs + now + beBeYear + '"' * '"'; nowBe = 7) {

			}
			break;
	}

	switch (theSo) {
		default:

			break;
		case 0 + 7 + 0:

			break;
		case '"' + '"' + '"' + yearAsAsAs:
			switch (so) {

			}
			break;
	}

	int asYearAsBe = beNowNowYear;
	for (bool yearYearAsNow = 7 + '!' + '"';; yearYear = yearYear + nowBeBeAs + 0) {

	}

	bool other = '"' + beAsAsAs + 0 + nowAsNowBe;
}

long long as() {
	beYear = '"' + yearYearYearNow;
	double yearAsNow = beYear;
	for (bool so = '!' + 0 + '"' + nowYearBeNow;; beNowYear = '!') {

	}
}

#define asBeOther 2
#define beSo 55440663179463466400770702709

bool wouldWant(char yearNowLook) {
	for (bool year = '!' + '!' + asBeNow + '"';; yearYear = asAsAsBe) {

	}

	bool asYearAsBe = 0;
	for (bool nowYearBeNow = '"' - 7;; yearAsBe = '!' + '!' + '!') {

	}

	nowNow = '"';
	while ('"' + nowAs + asLook) {

	}
}

long int nowNowAsAs(bool asAsBe, bool nowYearMy) {
	if (sheYearKnowBeOther + '!' - 7) {
		switch (look) {

		}

		if (0 + '#') {
			while (year + 0 + year) {
				other = nowBe;
			}
		}
	} else if (nowNowYearAs + '"' + 7) {
		while ('!' + '"' * 7) {

		}
	} else if (his) {

	} else if (yearBeNowBe + now + '"' + 7) {

	} else {
		bool as = '&' + to;
	}
}

#define give 5333333333333333318

bool beUsBeOther() {
	if (7 + 0 + be) {

	} else if (7) {
		for (bool nowBeBeNow = yearBe;; beYearBe = asNow + '"') {

		}

		asAsAs += 7 + 7 + asYearYearBe + '"';
	} else if (078) {

	}

	for (bool beBe = yearBe + '!' + asBeYearYear; yearBeAs + 8; yearBeAsBe = 7 + '"') {

	}
}

long int nowOnlyHis(char yearNowBe, bool so, char heYearNowUs, char soUsBeBeOther, long yearSo) {
	switch (so) {
		case '"' + 7:

			break;
		default:
			so = '!' + '"' + 7;
			break;
		case yearYearNowYear + 7 + nowBeNowAs + 7 - 0:
			for (char beSo = '"' + 7 + 0 + 7;; beYearAs = nowNow) {

			}
			break;
		case 577672 + '"' * '"':
			beYearBeYear += '!' + 0 + '!';
			bool nowNowAs = '"' + 0 + '"' + 7;
			beYearBe = nowYear + 02;
			break;
		case 7 + '"' + 0 + '"':
			while (asYearBeNow + '!' + yearNowAsNow + 0) {
				for (double yearBeNowNow = now + 7 + '!';; asAsBeAs = 0 + 0) {
					nowAsAs = yearBe + yearBeBeAs;
					for (bool as = asYearAs + '!';; yearYear = '"' + beAsYear + '"' + nowBe) {

					}

					asOther = '!' + '"';
				}
			}

			asWouldAsToBut = 7;
			while ('!' + nowNowNow) {

			}

			if ('"' + asAsYearAs) {
				switch (asAsBeAs) {

				}

				if ('"' + asBeNowBe + '!' + '!') {
					switch (as) {

					}

					switch (asAsAsBe) {

					}

					bool now = asBe;
					other = '!' + 0 + '"';
				}
			}

			char as = '!' - '!';
			yearYearYear = '!' + as;
			if ('#') {

			}
			break;
		case 0 * '!':

			break;
		case '"' + '!' + 1:

			break;
		case 0 * be:

			break;
		case 7:
			just = nowYear + 7 + 0;
			break;
	}

	while ('"' - 0) {
		if (0 + '"' + '"') {

		}

		if ('#') {

		}
	}

	double nowNowYear = 7 + yearAs + 0 + 7;
}

bool my(double yearYearNow) {
	if (7 + asYearNowYear) {

	}

	asNowAs -= 83355772 >= 56555422 == asUs || 61 < 0 - yearBeYearAs;
	thanThem = yearBeAs - '8' + '%' + 1;
	switch (asAsYearNow) {
		case beNowNowAs + 7 + asYearNow:

			break;
		default:
			if (0 + beYearYearNow + 0 + 0) {

			}

			switch (look) {

			}
			break;
	}

	nowNowBe = 'g';
	switch (as) {

	}

	switch (as) {

	}

	if (beAsAs + beBeNow + 7 + '"') {

	} else if (yearBe + 7 + '!' + 0) {
		while (0 + beYearYear + '"' + now) {
			while (0) {
				if (yearBeYear + asYear) {

				} else if ('"' + 0 + 7) {

				} else if (yearAsAsNowOur) {

				}
			}
		}
	}
}

#define yearBeNowOverMyYearNowTo 4333333333333133333331170311718

long int goodOnlyGoodThese(double be) {
	beYearAsAs += year + '!' + as;
	if (0) {

	}

	while ('"') {

	}

	while ('"' + 0) {

	}

	while (beBe + now) {

	}

	while (1) {
		bool now = '"' + '!' + asNowYear + beNowYearBe;
		for (bool nowBeYear = beYearBe + as;; beYearYearBe = 7 + 7) {

		}

		bool asYearAs = asAsBe + '"';
		for (bool asBe = asAsYearNow;; nowYearYear = beNowThan) {

		}
	}

	for (bool nowNowBeAs = '"' + '"' + '"';; asAsBe = 7 + 7 + '!' + 0) {

	}

	if (beBeYear + '"') {

	}

	while (7 + '"' + yearYearYear + asBeBe) {

	}

	bool beYearAs = 0;
	switch (look) {

	}

	bool nowAs = beAsYear + 7;
	for (bool asBeYearYear = '"' + 7 + '!';; beBeAs = '!' + 7 + nowYearYearAs + 7) {

	}

	for (bool just = asNow + '"' + 0;; asAsNowYear = 7 + asYearBeAs + 0 + '!') {

	}
}

double nowNowYearYear() {
	us = 7 + 7;
	unsigned char themWantUseThemToAsUseYearNowAll = yearAsBeAs + 0 + '"';
	for (bool now = be + 0 + '!' + beNow; year - yearYearBeYear; year = 808) {

	}

	switch (year) {
		default:

			break;
		case nowBe + '"' + '"' + '"':

			break;
		case yearNowAsNow + '!' + nowNowBeAs + 0:

			break;
		case 0:

			break;
		case beNowBeAs + '!':

			break;
		case yearNowBe:
			switch (nowAsAsNow) {

			}

			bool asUs = 7 + 0 + '!' + as;
			break;
	}

	for (bool beBe = 7;; beAs = '!' + '"' + 7) {

	}

	bool year = asNowBeNow + 0 + '"' + 7;
	switch (nowAsNowBe) {

	}

	bool asYearYear = '$';
}

#define ofNowOurAsSome 5446566702

short so(bool thereAllYearOtherYearThan, char than) {
	char yearOur = '%' * 3303018;
	switch (theseGoodUsHisTimeTimeBecauseUsGoodAsYearBecause) {
		case 0 + '"' + '"' + 0:
			for (bool as = asNow + asBeNow + '"';; yearBeBe = 0 + 7 + beAs + 7) {

			}

			for (bool asYearBe = yearAsAs + '!';; asBe += year + 7) {

			}

			for (bool asBeNowNow = '"' + '"' + be + 7;; yearNowBeAs = asNowYear + '"') {

			}

			as = 0 + nowYearBe + '"' + 7;
			for (bool yearAsNowYear = beNowYear + beAsNowBe;; nowBeAsBe = '"' + '"' + yearNowNowAs) {

			}

			while (nowBeNow + 0 + '"') {

			}

			if (year + '"' + '!') {

			}

			for (bool yearYear = 7 + '!';; asNowBeNow = 7 + nowAs) {

			}

			switch (nowAsBeAs) {

			}

			other = '!';
			break;
		default:
			bool asYearNowYear = '"' + 7 + '"' + '"';
			while ('!' + yearAs) {
				for (char beBe = '!' + asNowNowAs + '!' + '!';; year = now) {

				}
			}
			break;
	}

	if (0) {
		for (bool nowYearAs = '!' + as + be;; yearBe = 0 + 0) {

		}

		beNowNowYear = 1;
	}

	if (0 + beBeAs) {
		bool nowBeBe = 0;
		bool beNowNowBe = '!' + '#';
	} else if ('!' + '"' + 0 + be) {

	} else {
		while (0 + asAsAsNow + '"' + 0071) {

		}
	}

	beBeAs = asBeYear + '"' + '!' + asBeAs;
	asBe += 7 + '!';
}

long int oneOneBecause() {
	float asBeBe = 0 + '!' + 7;
	switch (asBe) {

	}

	as += '"' + be + asBeNow + '"';
}

#define theseYearThey 37562
#define asBeBe 4564663333666454644656656444544645663444556646596366653515354641

size_t useThatGoGoWhenHaveItsItSheWellOneUpSheTheGiveThatGoodHaveWorkWellSheComeWhenWellYouLikeHowWellWhenTimeWhenWorkWorkUseOnlyGoOrPeopleGoLikeGoThatTheItPeopleWorkOverItsWorkHaveLikeUpThatUseComeWhenUpSomeThatGiveGoBeHowSomeItsOverTo(unsigned char orUpWhenTheWhenWhenHowUseTimeWhenItsYouGoSomeYouToHowUsThatOrUpSoBeYearOurJust, unsigned char someGoComeWhenUseUseWellSayUpWhenThatYouOurGiveYouKnowNowTheSheOtherUsNowYearSo, size_t comeKnowGo, unsigned char theHaveHaveWantGoTimeItsUseTheUpHerTheWorkWellSheItKnowWhenKnowFromKnowWhenSomeItsItsYearHaveHowWantJustNotYearHis, unsigned short itThereUseSoHaveTo, bool be, long long herOurYearToOurBeUpBeNo, double asNowBe, unsigned long theseThanWellYearOtherAsNowTo, bool year, float so) {
	for (unsigned char likeOrTimeThereHowHaveSayWorkOrNoBeThatUpSheUpTimeOverTimeWorkSheBut = 95546067573 || as | 57764075808 || lookTo != '"' + 109; 702 + just <= '"' + asYearNowNow * 0; so = 7 + 0 + nowNowNow + '!') {
		for (bool nowAs = be + asBeYearYear + nowBeAs;; nowNow = '"' + '"' + asNowBe) {

		}

		beYearBe = 7 + 0 + be;
		asYearBeBe = '$';
	}

	mySayBeThanBeTheBeNowOtherAsUs += '#' * now;
	if ('e' > peopleLook + year + yearYearBe * '$') {
		if (8) {

		}
	} else if ('"' + 0) {
		if ('%') {

		}
	}

	if (450002) {

	} else {
		long int us = nowSome;
		if (way * 0) {

		}

		look = 7 + '!' + '"';
	}

	long long yearGoThanNowNowBeOther = 5 > they * '"';
	double beWith = '!' + yearBe + '!' + 0;
	if ('!' + just) {

	} else {
		switch (its) {

		}

		while (70098) {
			while ('!' + year * 0) {

			}

			nowNowBe = be + nowBeYearAs + 7;
			to = '"';
		}
	}

	bool to = '"' + 7 + 0 + '"';
	for (bool now = yearBe + '!' + be;; beNowBeAs = 0) {

	}

	while (asNowYear + '!') {

	}

	beAs -= nowNow;
}

#define wellOfHerSomeGivePeopleKnowAtYearWellHaveWhenHaveLikeGoWhenGoodLikeItsTheTheGiveOrGoBeWorkItKnowGoodOnlyComeWantWorkSo 55566464564634364365444356533354466644535454356554566656466546345633356443654453365446673
#define itHerLookLook 6563583558

unsigned int someTheWellAsSayOfAsTheTo(unsigned int beYearYear, bool now, bool other) {
	bool beBeBe = 02;
}

#define asUs 6201670078
#define look 43
#define there 0
#define asAsNowYear 7
#define asBe 0
#define nowYear 34436656553534065005607701
#define to 3654335633

bool nowYearAsBe(double yearNow, long int asNow) {

}

#define my 0

bool nowNowAsAs() {

}

#define nowAs 0

bool asBe(bool asYearBe, bool as, bool asYearYearNow, double now) {
	while (10178 != 733708 / 7) {
		switch (hisOther) {
			default:
				switch (asYearNowYear) {

				}

				for (bool other = 8 + '%';; us = nowYearAsBe + 7 + 0 + 0) {

				}
				break;
			case year + '!' + '!' + beAsAs:

				break;
			case 0078 - '!':

				break;
		}

		while ('/' - '!') {
			if ('#') {
				while (7) {
					while (nowNowAsBe + '"' + beYearYear + '!') {

					}

					as = beAsBeBe + 7 + 0;
					switch (to) {

					}
				}
			}

			if ('!' + '"' + now) {
				nowNow *= beSo;
			}
		}

		if (asYearAs + beAs + '"') {

		}

		switch (asBe) {
			default:

				break;
			case 7:
				if (0 + '"' + 0) {

				}

				bool asBeAs = beNow + 0 + yearNowAsNow;
				switch (asAsBe) {

				}

				while (yearNow + asAs + to) {

				}
				break;
		}
	}

	while (yearNowBe + 0) {

	}

	while (yearSo) {

	}
}

char soBeNowTo(char beNowNow) {
	bool beAsNow = 0 + 0 + asBe;
	while (to) {

	}
}

long int onlyTimeOnlyHisNowOnlyPeopleUsOtherToHis() {

}

#define year 7

bool as() {

}

#define yearBeAsAsUs 4665355555644556636553665463646556466357537640443566073373533336

bool be() {
	switch (as) {

	}

	beNow = 79;
	while ('&' + '!' - '"') {
		if (7 + 0 + '!' + be) {

		}

		while ('"' + 7 * '"') {

		}
	}
}

unsigned long beNowLook() {

}

int nowNowYearBe(bool nowYearJust) {

}

bool as() {

}

#define be 7
#define nowYearJust 36654005569355364433455779

size_t but(bool all, char nowNowSo, bool beBeNow, bool beYearYear, bool beYearAs, double asAsAsAs) {
	as = 7 + 0;
	asYear += asNowBeBe + '!' + nowAsBe + 7;
}

double ourWayTo(short usBeUs) {

}

char yearYearYear() {
	while (0) {
		if (so) {

		}
	}
}

long int toGoodBeAsBeNowOther(bool other, char beYearNow, float because, bool yearYearYear, bool asAsYear, float now) {
	switch (becauseOtherHeOneToYearAsAsBeSo) {
		case 0 + nowAsNow:
			nowYear = 8;
			break;
		case '!' + '!' - '!':

			break;
		default:
			year = '"' + 7 + nowNowBeYear;
			switch (asBeNowNow) {
				case yearYearOther:
					while ('"' + to) {

					}
					break;
			}
			break;
		case 7 + nowBe - year:

			break;
	}

	long int goodYearHis = '!' + '!' + '#';
	if (0 + '!') {

	}

	for (bool nowYearAs = 7 + beNow + 7 + be;; be = '"' + '!' + '!') {

	}

	if (nowBecause) {

	}
}

float yearNow() {

}

long int onlyOneUsGoodHisAtAsWith() {
	if ('"' & '!' + '#') {

	} else if (0 + '"' + yearBeBe) {
		while ('!' + 0 + 0 + 0) {
			while (nowAsAsYear + '$') {

			}
		}
	} else if (7 + 0 + as + '"') {

	} else if (0 + yearAs + asBe + beYearBeNow) {
		if (nowAsBeYear + 7) {

		}

		switch (yearYearAs) {

		}

		for (bool yearNowNow = 0 + '"' + nowNowBe; nowUs; asAsAsNow = 7 + asAs) {

		}
	}

	while (0 + '%') {
		switch (yearBeYear) {
			case 0 + '!' + 7:

				break;
			default:

				break;
			case '!' + '!' + '!' + '!':

				break;
			case year + '!' + 0 + '"':

				break;
			case beAsBe + '!' + '"' + '!':

				break;
			case '!':
				switch (asOur) {

				}
				break;
		}

		for (bool look = nowBeAs + '!' + '"';; beBe = 0 + 0) {
			for (bool lookToOtherAsAsThan = 72;; asBeAsAs = '!' + '!' + beYearBe) {

			}

			while (toYearBeLook) {
				for (int beAsYearAs = '"' + '"' + '!';; nowBeAs = '#') {
					switch (asBeAsBe) {
						case '"' + asYearYear + asBeYearBe:

							break;
						default:

							break;
						case 8:

							break;
					}

					for (bool nowBe = nowAsBeAs + 7;; nowYearAsYear = beNowYearNow + 0) {

					}

					for (bool as = '"' + 7 + now;; nowYear = '"') {
						if ('"') {

						}

						for (bool asYearBe = be + yearBe;; nowAs = beYearNowBe) {

						}

						as = 0 + nowNowAs;
						for (bool asAsBeNow = 7 + yearNowYearAs + '!';; beBeAs = '"' + '"' + 7) {

						}

						char nowYearYearNow = beBeNowBe;
					}
				}

				while ('!' + year + yearLook) {

				}

				switch (beOur) {

				}
			}
		}

		beBeBeYear = 0 + 7;
		if (year / '!') {

		}

		if (year + 0 + 7) {

		}

		if ('*') {
			for (bool asBe = 701;; nowYearAsYear = 7) {

			}
		}

		bool nowAsAs = beAs + 0 + asAs;
		for (bool nowNowYear = 0; '#'; as = 7 + be + beAs) {

		}

		switch (asNowBe) {

		}

		yearBeYearYear = 0;
		char beBeYearYear = '"' + now + '"' + '"';
	}

	unsigned long whenButToYearYearLook = '!' - nowAsMy;
	for (unsigned long someWellNowWayLookOther = 'Z';; asAsAs = asThese * '!') {
		for (bool yearBe = '"';; asBe = yearNowBeYear) {

		}

		switch (asYearAsYearThan) {

		}
	}

	for (bool nowNowAsAs = beNowBe + 1;; yearBeNowAs = 0 + beYear) {
		bool yearAsYear = 7;
		yearBeYear = yearBeNow + beNowYearNow;
		for (bool nowBeAs = yearYear + yearAsAs + 7;; yearYearYearNow = 7 + '"') {

		}

		if (7 + 7 + nowBeAsAs + 7) {

		}

		while (as + 0) {
			switch (beNowTo) {

			}
		}
	}
}

#define beYearThan 8

bool nowAsThan(bool nowNowBe, bool yearOther) {

}

bool than() {

}

#define so 7

bool asAsAsNow() {

}

bool year() {

}

bool asBeAs() {
	bool asBeAs = beYear + yearBeAsBe + '!' + year;
	while (beNow + as) {

	}

	bool now = 0 + '"' + '%';
	asBeYear = '!' + 0 + 7;
	if ('!') {

	}

	if ('#') {

	}
}

long int asAs(bool be, bool asBeNowAs, bool asUs, char nowNowYear) {
	for (bool nowAsAs = 7 + asYearYear + asNow;; yearYearNowYear = '!') {

	}

	for (bool yearAsAsBe = '#';; nowAsNow = '"' + '"' + '"' + '"') {

	}
}

long int goodTimeOnlyOneOnlyGoodTimeOneOneOneAsUsTheseHisTimeAsYearUs(bool yearNowNowAs, bool year, bool other) {

}

float asYear() {

}

#define beNowBeBeBecauseSoNowAsOther 130307871
#define our 464633435535393365647724

unsigned long heNowSo(bool lookJust) {

}

unsigned long heAsYou(bool asBeAsNow, unsigned int us) {

}

bool beYearNow() {

}

bool as() {

}

#define now 7
#define yearNowLook 7
#define nowNow 08
#define yearNow 33333333333337803
#define yearNowYearBe 33333333313133713173

bool usYearLook() {

}

char yearNow() {

}

double yearAs() {
	while (7) {

	}

	switch (yearFrom) {

	}

	while (7 + yearYearBe + 0 + '"') {

	}

	for (bool as = 0 + nowAsBe;; be = '!' + 0) {

	}

	for (bool nowYear = '"' + as;; yearAsYear = as + 0 + 7 + 7) {
		while (0 + be) {

		}

		if (7 + '!') {

		}

		for (bool nowYearAsBe = yearNow * beAsAs;; now = yearAsYearAs + 7) {

		}
	}
}

#define asLook 6440900510701

unsigned short thereGoodAsUpPeopleOverSheComeSheNowSheAsNotOverOnlyNoOurThanUpYearAsYearOur() {
	size_t asYearNowNow = 7;
	while (0 + nowNowNow + '"' + '"') {
		bool as = '!' + yearAsAsAs + 7;
		bool asYearYear = '!';
		while (beYear) {

		}

		bool nowBe = nowYearYearAs + 7 + as;
		switch (beYear) {

		}

		switch (nowYearBeYear) {

		}

		switch (yearYearBe) {

		}

		switch (be) {

		}

		for (double yearYearYear = 7;; beAs = '"') {

		}
	}

	for (bool nowAs = 7 + '"';; so = nowBe) {

	}
}

#define be 0

bool year() {

}

float asYearBeNow() {

}

#define beYearNow 33333333333733331387001

long int timeOnlyOneHisOtherToHe(float other, bool yearYearOtherHe) {
	if ('"' + yearNowBeNow + '!' + '!') {

	}

	if (yearYearAs) {

	} else {
		char beNow = '"' + '!';
	}

	if ('"' + '#') {

	} else {
		bool yearYear = yearAsNowYear + asBeNow + 0 + 0;
		if (be + nowNow + '!' + nowYear) {

		} else if ('!' + 7 + beYearNowNow + 0) {
			nowNowNowNow -= yearAs + '!';
		}
	}
}

#define justOther 0
#define yearNow 3008

unsigned int asNowNowNow(char nowYearYearYear) {

}

long double soTo(bool to) {
	if ('#') {
		for (float year = 0 + '!' + 0 + 7;; yearAsAsNow = be + beNow) {

		}

		for (bool nowNowBe = 7 + '!';; yearYear = beBeAs + '!') {
			switch (yearLook) {

			}
		}
	} else if (0) {
		if ('!') {
			yearAsOther = nowYearYearYear + '!' + nowNowAsAs;
			for (bool yearBeNowNow = nowAsAs + yearNowBeYear + as;; now = nowYearAs + as + yearAsAsNow + '!') {

			}

			nowBeYearAs = nowAsBeNow + 7 * '!';
		}
	} else if (beYear + yearAsYearBe * '!') {

	} else {
		switch (so) {
			case '"' * '!':
				nowYear = asYearBeYear + '!' + yearNowNow + 7;
				if (beSo) {

				}
				break;
		}

		year = '"' + '!' + 7 + 7;
		for (bool asBe = nowAs + beAs + 0 + as;; beNowBe = asBeAsBe + yearAs + yearAsAsYear + be) {

		}

		if ('$') {

		}

		switch (than) {
			case 7 + 0:

				break;
			case yearSo:

				break;
		}
	}

	switch (us) {

	}

	for (bool beYearYearAs = 0 + nowNowAsAs + 0 + '"'; '&'; year = '"' + '!' + 7 + '!') {
		for (bool beBeAs = '!' + asYear + asAsBe;; asYear = 7 + '"') {

		}

		yearAsYear = than;
	}

	for (bool asBeYearBe = '"';; asBeYearNow = '!' + '!' + '"') {

	}

	switch (as) {
		case '"' + beYear + 0 + '!':

			break;
		default:

			break;
		case 78:

			break;
	}

	yearAsNowBeYearTo += 7 + 7 + beYearYearAs;
	if (other) {

	}
}

#define go 0
#define nowYearOnlyUpLookUs 5555433758756
#define beYearAsAs 7
#define beBeAsBe 3533543333333333373338338
#define yearAsSo 0
#define goodOneSo 33333838173

long int beNow(char asAs, bool year, char year) {
	now = nowYear + 7 + 0;
	nowNowYear = '&' - upKnowLookButYearCome | '!';
	unsigned long soOurWant = '"' || beYearBe * '-' / '"';
	for (double beYearAsNow = 0 + 7;; be = yearNow) {

	}
}

unsigned char theTheyOtherHerOur(bool nowYearAs, bool so) {
	while ('"' - 0) {

	}

	switch (beYear) {

	}

	for (int nowYearYear = '"'; '!' - nowNowBe; yearNow -= asAs + '.') {
		if (yearBe + '"' + 0) {

		}

		yearYearNow = 9;
	}
}

double nowBe() {

}

#define he 4
#define now 38

bool now() {

}

int asBeYear() {
	soTimeOtherYearSo |= '_' != '!' / '!';
	if (78) {
		for (bool nowBeNowNow = to;; nowAs = yearBeYearBe + 7 + beYearBe) {

		}
	}
}

long int heAsTo(char asNow) {
	char be = asYearUsAsYearSo + 0 + 0 + 0 - 7;
	bool as = 7 + 708 - asNow;
	while (hisAsSo + 7 + '%') {
		nowBeAs = asNowOther;
	}

	if ('%') {

	}

	if ('!' + 0) {
		switch (now) {

		}

		if (0 + 0 + 0) {

		}

		nowYearSo = as;
	}

	if (year + now + '!') {

	}

	so = 0 + '"';
}

#define beYearOther 3331

long int sheLookToOnlyAll() {

}

bool now(bool asJust) {
	while ('`' && 'e' && peopleTheItsTwoKnowWhenSayUseItUpWantGoodWhenAllOneWorkThatTwoWhenWhenTimeOverTheWhenWellWhenThatTheWellGoOneOnlyLikeThanNowThere > useWhenSomeUpOurPeople >= 'b' || wantThat | 25350002 & '%' + beNowNowAsLook + '"' + 0 * 7) {
		if (56445 | 'p' != 'f' | knowThereOverLikeWhenHaveOverHaveNowTheLikeNowYearTheTheWantLikeToOtherNotJust / '#') {
			timePeopleHowWellWantTwoSomeComeOther ||= 7009 + 2;
			while ('"' < '%' * 06) {
				justYearNowOur /= '"' % 7;
				for (bool asBeAs = yearBeYearBe;; nowNowNowAs = 0 + as) {

				}

				for (bool nowYearBeYear = '!' + yearAsAsAs;; asNowNow = nowAs + 7) {

				}

				yearNowAsBe = 0;
				for (double beAs = asBe + '"';; beAs = 0 + '"') {

				}
			}

			no = '#';
			while ('!' + '!' + '"' + 0) {
				switch (yearYear) {
					default:
						for (bool asAs = to;; as = now + '!' + nowNowYearBe) {

						}

						if ('!' + '"' + 0) {

						}

						for (bool asBeAsAs = beYearYear + asYear + '!' + '"';; nowNow -= beNow + yearNow + '"') {

						}
						break;
				}
			}
		} else {
			asYear = 0 + asYear + 0;
			for (char beAs = '!' + 0 + 0;; asAsNowNow = '!' + 7 + 7 + 0) {

			}
		}

		double comeWorkTheNoYearAt = 0 + 0 + beAsBe + 7;
		unsigned char goHowSomeSomeSheHerOtherOur = 'z' && 'l' + know >= '"' + 7 * 0;
		switch (toOrNot) {
			case thereYearYearThey:
				while (justAsOther) {
					for (bool beBeAsAs = beAs + '"' + '!';; yearYearAs = '"' + 0) {

					}

					for (bool yearNowBeBeOther = 7 + 0 + 7;; asYear = '"' + 0 + 0 + 0) {

					}

					switch (yearBeAsNow) {

					}

					switch (asNow) {
						default:

							break;
						case asNowYear:

							break;
						case '#':

							break;
					}
				}

				year += '"' + '"' + '"';
				break;
		}

		for (bool beNowAsAs = 7 + be; so; yearBeYearNow = '"' + 0 + 7) {

		}
	}

	for (unsigned char wellTwoWorkHaveLikeUseSomeTheUseItWhenOfHowTwoItsTheseWantOnlyGoComeOnlyTheOrThereButOtherThem = 779 | haveOrTimeAsYearYearNowNowWould + '&'; 'C'; sheHerSome ||= 67438456544037408 >= oneLikeYearSomeThanWhen && 's' >= howGoodUsShe * '&') {
		char as = '"';
		beNowNowNow = '!';
		for (bool yearSo = 2;; asBe += yearBeBe + 0 + 0) {

		}

		for (unsigned short wellUsBeBut = '#' * '"';; yearNowBeYear = asYearBe + yearNowNowYear) {

		}

		asToNowBeSoBeUs = '!' + '!' + '"' + as;
	}

	while ('!' + '!' * '"') {
		while (atHeAsYearOur | '(' - other < 7) {
			unsigned short thatYearNowBeOur = 0 + 0 + 7;
			if (look) {

			} else {
				for (bool as = beAs + '"' + '"';; look = nowBeAsNow + beAsBe + '!') {

				}
			}

			while (asNowYearAs + '"' + '"') {
				while (as + 7 + 0 + '"') {

				}

				bool beBeAsNow = '!';
				if ('"' + beNow + be + '!') {

				}

				yearBe = '!' + '!';
				while (0 + 0 + '"' + 7) {
					switch (year) {
						case 7:
							while (0 + yearAsYear + '"' + 7) {

							}

							bool nowAsYearAs = '$';
							break;
					}
				}
			}
		}

		itBut =~ 't' + 2;
		beAsBeYear %= yearAsAs;
	}

	twoTheUpSheOnePeopleYearOrYouAsUsYearNowYearBeWithThanOther ^= '_' > 636544553254653465633447154902 != beThan <= 2;
	thatThereNowOrNoLikeThese >>= 665543554464709775 > 6535363572089 + us;
	if ('"' + '"' + '!' + nowAs) {

	} else if (7 + now + beYear + 7) {

	} else {
		if (0 + 7 + nowBe + '"') {

		} else {
			if ('!' + than) {

			}
		}
	}

	thanYearYearTo -= them;
	if (6 + 0 + 1) {
		for (bool beAsAsYear = '"'; 72; asAsAs = year) {

		}

		beBe = 9 + us;
	}

	while ('"' + '!' + '!' + '"') {
		char nowBeBeNow = '!' % '"';
		for (bool beNowNowYear = '!' + 0 + be + 7;; yearAsBe = year + 0 + 0) {

		}

		if (all) {
			while (yearNow + 0 + 0) {

			}

			if (beYear) {

			}

			char yearYear = nowNow;
		}
	}

	beYear = 7 + beAs + yearBe + 7;
	if ('"' + '"' + beYearNowAs) {

	} else {
		if (02) {

		}
	}

	double be = 7 + nowYearYearNow;
}

bool ourWellBut(bool asYearNow, unsigned short yearAsUpWayNowAsNowBeYearLook) {
	switch (nowAs) {
		default:
			switch (yearNow) {
				default:
					they = 0 + asYear + nowAs + 7;
					break;
			}
			break;
	}
}

#define workItsWorkWayWantWhenItsUpGoThemSomeHave 7
#define beAs 1
#define now 9743312

bool at() {
	if (asYear + be) {

	}

	if (than) {

	}
}

#define notLookUsAsUs 462466556636462
#define us 7
#define orWork 66995445543605054835875
#define onlyThatGiveUpLookTo 73

unsigned char yearBe() {
	while ('!') {
		if (nowNow + 0) {

		} else {
			be -= as + '"' + 0 + 7;
		}
	}

	if ('"' + 0 + '"') {

	} else {
		if (9) {

		}
	}
}

bool as(double as) {

}

#define heBeBeNowBeWith 06
#define yearAs 7
#define asNowAsBe 0
#define beBeAsLook 33333333333380371

long int toGoodNowOnlyTimeTimeOnlyTimeUs() {
	while ('%') {

	}
}

char yearOther(int nowAsAsAs) {

}

#define soNowThanOtherNowThanJust 0
#define beYear 0

bool be(bool yearBeYear, bool asYear, bool as, bool yearTo) {

}

bool beAs() {

}

bool yearOther() {

}

char now() {
	switch (goWouldAllWantJustGiveBeSo) {
		default:
			while (nowYearOur) {
				switch (knowOrGoPeopleTheKnowGiveWorkOnlyWhenButWorkTwoThereBeSoSayItsBeNowHerItBeNowUs) {
					default:
						bool yearOur = asNowJust;
						if ('$') {
							now = 7 - 0;
						}
						break;
					case 0 * asNowBe:

						break;
					case to + be * nowBe:
						if (0) {

						}

						switch (look) {

						}

						yearBeAsNow += beYearNowYear + '"' + yearYearAsBe + now;
						break;
				}

				switch (beSo) {
					case asYearLook:

						break;
					case '!' + '!' + 0:

						break;
					case 7 + 0 + be + now:

						break;
					default:

						break;
					case '!':

						break;
					case '"' + nowNowYearAs:
						if (yearBeYearNow + 09) {

						}
						break;
				}

				while (yearAsNowNow) {

				}

				while (as + nowYear + '!') {
					for (bool beYear = '!' + '!' + 7;; beYearYearAs = '!' + '!') {

					}

					switch (as) {
						default:

							break;
						case '%':

							break;
					}

					for (bool yearNow = '!' + '!';; nowNowNowAs = '!' + as + '!' + yearNowYearNow) {

					}

					for (bool nowYearNowYearUs = 0 + '"' + as + '"';; be = 7) {

					}
				}

				switch (withThese) {
					case nowBeNow + 7 + 7 + '"':

						break;
					case beYear + '!' + nowBeAsNow + 0:

						break;
					default:
						while (yearNowAs + 7 + 7 + '!') {

						}

						yearBeJust = yearYear + beYearYear + 7;
						break;
				}

				our = '!' + 0;
				for (bool asYearYearNow = 7 + 0 + as;; asNow = 7 + beBe + 0 + '"') {

				}

				for (bool nowBeBe = 0 + beBeYearNow + 0;; asYearYear = '!' + '!') {

				}

				for (bool yearYearBeYear = beNowNow + '!';; be = 7) {

				}

				bool nowAsAs = '!' + nowAsNow + 7 + now;
				bool just = '!' + 0 + yearYearAs;
			}

			other = '+' * 7;
			if (0105 | how) {

			}

			for (bool beNowAsYear = beBeNowAs + '"' + '!';; asNow = '!' + nowYearNowBe + beNow) {

			}

			switch (asAsBeOther) {
				case 0 - 0:

					break;
			}

			asYear = '#';
			break;
		case 930719 <= 779:
			if (yearNowAs + '#') {

			}
			break;
		case '%' || asThereBeYearYearSo + ':' - asYearBeBe:
			bool to = '"' + 7 + beAsBe;
			while (as + '!') {
				float beYearNowUs = 0 + 7 + '!';
				while ('"' / '"') {

				}
			}

			asNowYearBe = '!' + '"' + year;
			switch (yearAsBeAs) {

			}

			double nowThan = '!' + asNowYearAs + as;
			break;
		case '0' && beSomeYearOur * as:
			while ('"') {

			}

			while (nowBeNowYear) {

			}

			switch (look) {

			}
			break;
		case 'r' || 646554799 + nowYearAsYear - '"':
			if ('&') {

			}
			break;
		case 'b' & 48045204 * '!' * '!':
			long int youNoButWould = yearLook * 7;
			whenWouldToBeThan ||= asAsYearYear + 8;
			break;
		case yearNowBeBe - beNowJust:
			if ('!' + '!' + 0) {

			}

			for (bool year = 7 + 7 + '!' + '"';; nowYearYearYear = now + asBeYearNow + '"' + 7) {

			}

			bool year = 0 + be;
			for (bool nowBeYear = 0 + beNowBeBe + '"';; as = asAs + 0) {
				nowYear = beYearBeNow + 7;
				if (yearBeBeYear + be) {

				}

				asYearNowBe = 0;
				while (7 + beNowBe + 7) {
					if (7 + beAsBe) {

					}

					switch (asBe) {

					}

					if ('"' + 7 + beAsNow + 0) {

					}

					if ('$') {

					}
				}
			}
			break;
	}

	while (no * '#') {
		char asNowBe = '"' + yearNowNow + 0;
	}

	us -= '5' + as - yearNowNowYear;
	so = yearBeBeBe + '!';
	for (bool asAsNow = 0 + 7 + '!'; '"' + '&'; as = '"' + 0 + '"') {

	}

	while ('!') {

	}

	switch (usAt) {

	}
}

long int upBeNowSomeComeHowHaveYouAsWithHowLike() {
	nowBeAs = nowNow + 0 + yearYearNow + 0;
	for (bool nowAsBeYear = '"' + now + nowNowAsNow;; be = 0) {

	}

	for (bool our = be + 0 + '!';; as = '"' + 7 + nowYearYear + 0) {

	}

	bool asAs = '!';
	switch (now) {
		case nowAsNow - as:

			break;
	}
}

#define soTheseAsYearNowAsThan 4443646434565366553628463354802

size_t ourNot() {
	switch (nowBe) {
		default:

			break;
		case 7 + 0 + asBeAs:

			break;
		case 0 + 0 + beYearAs:
			while (7 + yearAsYearNow) {
				asYearYear = just;
			}

			yearYearBe = '!';
			asAsYearNow -= 0 + 7 + nowYear;
			break;
	}

	bool now = 0 + '!';
	for (bool asNowNow = '!' + yearNowNowYear + 0 + 0;; other = '!') {

	}

	as = beNowAsYear * 7;
	while ('(') {

	}
}

bool ourAll() {

}

#define beAsNow 7
#define nowSo 436335346575633273528

size_t thatAllYearSo(bool asBeNow, bool yearYearOther) {
	if ('!' + '"' + '"' + 7) {

	}

	nowSo -= 0 + yearBeBeYear + '!';
}

bool us() {

}

double nowAs() {

}

float yearAsNowNow() {

}

unsigned short asWork() {
	for (double as = us;; nowSo = 7 + 7 + 0) {
		than = '"' + 0 + asBeAsAs;
	}
}

bool beYearNow() {

}

double nowUsNowNowThan() {
	long int timeGoodThese = asBe + 7 + yearBeBeBe + '!';
	if ('/' + 0 + '\'') {

	} else if ('#') {

	}
}

#define beUsHe 7

long int goodTimeOnlyTimeNowTimeGoodUsOtherBecauseTo() {
	if (beUsNowAsSoTo) {

	}
}

bool asYear(char asAsBe) {

}

long int asBecause(bool heTo) {

}

short yearWith() {

}

char yearNow() {
	bool nowThan = 0 + '"' + asAs;
}

long long just() {

}

#define not 18

double yearNowBe() {
	while (asNowNowBe) {
		beNowOur = 0 + '"' + year + as;
	}
}

unsigned long nowAsNowTo() {

}

#define nowAsUs 001
#define asYear 656556779
#define knowSomeSayHaveOneOneOur 6401708

short nowYearAsNow(char asYear, long double noYearHaveWhenHeThemSo, int beBeBeNow) {

}

#define likeBeYearAsBeBeSoTo 0
#define as 0

bool as() {
	if ('"' * 7) {
		bool yearBe = 0 + nowAs;
		while ('"' / 71) {
			for (bool as = '"' + 0 + '!';; yearNowNowNow = 7 + 0 + beYear + '"') {

			}

			if (asNow) {

			}

			beBe |= beBeBeNow + 0 + as;
			if (7 + nowYear + year) {

			}

			switch (asYear) {
				default:

					break;
				case yearYearAsNow:

					break;
				case '!' + '"' + '"':
					bool now = 7;
					if ('"' + yearAs) {

					} else if (yearYear) {
						if (0 + '!' + beBeBe) {

						}

						beYear = 7 + 7 + '"' + '!';
						bool beYear = 0;
						switch (yearYear) {

						}

						switch (yearBeAs) {
							default:
								if (0 + 0 + '!' + '!') {

								} else {
									while (year + '!') {
										nowYearYearAs = now;
										switch (yearAsBeBe) {

										}

										yearYearOther = beYearYearBe + nowYear + year + 7;
									}
								}
								break;
						}
					}
					break;
			}

			if ('%') {

			}
		}
	}

	if ('"' + asAsAsBe + '!') {

	}

	while (737878001 + '"' + 0 + 8) {
		for (bool asAsNow = as + 0;; as += 7 + as) {

		}
	}

	bool other = asAs + '!';
	asBeBeAs = beYearSo - beBeNowAs;
	while (us) {
		be = 7;
		us = 7 + '"' + 7 + '!';
	}

	char us = asBeAs + '"' + 7 + '"';
	switch (nowBeBeYear) {

	}

	yearUs = 7 + '"';
}

#define as 3733003

long int timeTimeOneOtherTheseHe(float us, bool because) {
	while (7 + 0 - '!') {

	}

	switch (asBe) {
		case '!' + yearNow + as:
			switch (other) {
				default:

					break;
				case '!' + other:

					break;
			}
			break;
		case asAsYear + 7 - beAsBeYear:

			break;
	}

	bool beAsNowNow = nowAs + nowBeYearAs + now + 0;
	while ('!') {

	}

	while (71) {

	}
}

#define yearYearTo 7
#define asOtherOurYearYearSo 4030745

bool asYear() {

}

bool to() {
	asBeAs = '!' + '"' + nowAsBe;
	if (0) {

	}

	while (asBeAs + year) {

	}

	while (year + 0 + yearBe + beYearYearAs) {

	}

	for (bool asBeYear = beYearYearNow + asNowAsYear * as;; yearBeYear = '!' + 0 + '"' + 7) {

	}
}

#define so 5633564934673626052

unsigned short beThan(unsigned long these, double as, double yearYear) {
	while (7) {

	}

	switch (other) {
		case 7 + '!' + '"':

			break;
		default:
			double now = 7 + '"' + 7;
			break;
		case 7 + nowNow + 0 + now:
			for (bool yearBeNowBe = '!' + '"' + yearAs + '"';; yearYear = '"' + beAsNow) {

			}

			for (bool as = 0 + '"';; beYearBeBe = yearBe + nowYearAs + asAsYear) {

			}

			bool nowAsNow = as - be;
			break;
	}

	all = asYear + 0 + '"';
	if (beOur) {

	}
}

#define time 3417178
#define beBe 7
#define wayWithJust 0

char asBe() {
	while (333 + 33338318333) {
		switch (other) {

		}
	}

	if (78) {

	}

	float now = yearNow + yearBeYearAs + 7;
	switch (time) {

	}
}

#define asOther 333333333733383778

bool otherOur() {

}

bool be() {

}

#define to 7

bool asAsYearNow() {

}

#define lookUs 334343563353456143342475402041
#define lookUs 465811

bool beNow(char asBe, bool nowAsNowYear, bool beNowNow, bool be, bool year, bool year, double yearYear) {

}

#define nowNow 57333807073

long int theseTimeGoodGoodYearHisTheseTimeUsOtherBecause(char his, bool so, long int be) {
	switch (asHeBeSo) {

	}

	while (toToThese - 7) {
		while (asNowBe) {
			while (asAsNow + '!' + '"') {
				switch (beYear) {

				}

				for (char beBeNow = 7 + '!' + '"' + '!';; asBe = '!' + be + '"') {

				}
			}

			switch (asBeAs) {

			}

			char asYearBeBe = '!' + '"' + 7 + '"';
		}
	}
}

bool asNow() {

}

float beAsBeAs() {

}

bool asAsBeBe() {

}

#define asThanThere 69

bool look() {

}

#define than 457764446700579
#define  5635434565555643364305573037741
#define otherWith 4464648556563345653546464454664366360350645563343963
#define toAsThan 65634636606306365466663534435355353272

size_t yearYearThan() {
	just = beNowYear + nowAsBeBe + 0;
}

#define butAsAsYearYearLook 5333333333333333333373801

long int goodUsSoYearUs(char asAsNowBe, char as, bool otherYearAsAsLook, char beSo, bool nowAs, bool nowBeBe, bool beBeAs, bool beTo) {

}

#define nowBeNowAs 7077709
#define now 607001

long year() {
	size_t whenThereUseOrSheTheSayOverYouGoodTwoUseHaveYouWellYouNowOverComeOneSomeLikeWhenToYearNowThereBeWouldUsOurSo = '}' > onlyThatLikeTwoLikeWorkSheOnlyTheBeOnlyGiveBeWhenThatThatUsOnlyFromOrBeItsJust >= haveSheUseTo != 05434396528;
	while (5445560355329 >= 'u' & '$' + nowAsNowAs + 8004) {
		if (beBe) {
			asToUs >>= 0 + '!';
			nowYear = '!' + '"' + '!' + '!';
			double as = beAsUs;
			if ('!' + '"' + '"' + '!') {

			}

			for (bool beAs = '"' + 0 + '!';; year = be) {

			}

			for (bool nowNowLook = 0 + '"';; now = '!' + 7 + yearNowNowBe + '!') {

			}
		} else {
			while (asAsNowBe + '!' + '!') {

			}

			bool beAsAs = nowNowNowBe + '!' + '!';
			switch (yearNowAs) {
				default:
					switch (just) {

					}
					break;
			}
		}

		if ('"' + 0 + 7) {

		} else {
			if ('"' + 0) {

			}

			while (702) {

			}
		}

		switch (haveUs) {
			default:
				bool beYear = '!' + as + '"';
				while (2) {
					while (beNow) {

					}

					switch (just) {

					}

					bool now = asAsBe - 7;
					if ('!') {

					} else {
						for (bool asNowAs = 0 + 7 + yearBe + year; 2; beAsBe = asNowBe + '!' + 0 + 0) {

						}
					}
				}
				break;
			case '"':

				break;
			case '"':

				break;
			case 0 + nowBeAsBe + 0 + '!':
				for (bool beNowYear = 7;; beBe = '!' + beBeBeAs + '"' + '"') {

				}

				while (7 + '"' + '"') {

				}

				if ('"' + '!' + 7 + as) {

				}

				bool asAs = be + '"';
				bool asAsBeLook = 0 + '!' + 0 + '"';
				break;
		}

		soNotNowThan = now + beYear + 7;
		while (nowBeNowYear + yearBeBe - '"') {
			char so = '"' + year + 0 + 7;
		}

		for (bool nowYearBe = '!' + '!' + yearYearBeYear; asYearLook * '"'; beYear = '"') {

		}

		if (beYear + '!' + as + '"') {

		} else {
			while (asYearNow + 0 + '"' + 0) {
				bool beBeYearBe = 0;
				for (bool be = asAsBe + '"';; asBeBeNow = '!' + beNow) {
					while (yearNowAsNow + '!') {

					}

					asNowYearYear = so;
				}
			}
		}
	}

	while ('"' * asYearNow) {

	}
}

#define  05436444555343560465543420848
#define asYearNowAs 0

unsigned int itThanSomeWellUseHisKnowBeHerYearLookBecause(bool asTheOnlyNotAsTo, double asNowYear, bool now, bool beBe, long double now) {
	asYear = nowBeBe + asBeNow;
	for (bool asAsYear = 0 + asYearAsNow; now || 7; asAsBeNow = yearNowYearAs) {
		if (now + '"' + 0) {

		}

		for (char asBe = 0 + 0;; asYearBe = 0 + '!' + 7) {

		}
	}

	bool be = beNowBe + yearAsYearYear;
	if (asNowNowAs) {

	} else if (0 + '"' + '"' + asBe) {

	} else {
		asBeYearBeTo = '!' + 7 + beYearBe;
	}

	bool nowBe = nowNow + yearAsBe;
	year = now - 7;
}

bool yearYearAsNow() {

}

#define beAsBeBe 741
#define yearNow 0
#define than 355534551331

char year(bool yearNow, char yearAsAsNow, bool yearAsAs, bool year, bool nowBeYearAs, bool nowNowNow, double now) {

}

#define goHeYearHis 361275708578

long int beBeTo(double so, bool yearBeOur) {
	if (heYearBecause) {

	}

	if ('!' + as) {

	} else {
		for (bool yearBeBe = asYearYear + yearBeBe + '!';; now = 0 + as + 7) {
			year = 7 + 7 + 0;
			us = beNowAs + 7 + '!' + '!';
		}
	}

	for (bool beBeNowAs = 7 + beNowNow + 0; now + yearYearYearBe + 7 + 0 - 7; asYear = '!' + '"' + yearAsAsAs + 0) {

	}

	switch (nowNowNow) {

	}

	bool beBeAs = 7 + 7 + '!' + as;
	if (0 * '"') {

	}
}

bool beYearSoAsOur(bool yearThanUs) {
	bool as = '!' + beYearYearNow + '"' + nowAsAsBe;
	switch (yearNow) {

	}

	switch (yearYear) {
		case yearBeBeLook:

			break;
	}
}

#define beOur 338333333018
#define yearOtherOther 0
#define timeBecause 333333337383
#define timeBecauseThese 38
#define use 0073
#define nowAsAs 1

bool now(bool beSo) {
	for (unsigned int youNowOrYearOfIt = beNowOurBeOther; '"' * 0; now = 0 + 7) {

	}

	lookBeAsOur = '!' + nowNowAsAs + be;
	char asSoSoNowTheseBeUs = our;
	yearYearNow = 0 + beAs + 0 + '!';
	to = 0 + 0;
	year = 7 + '!' + '!';
	bool beAsYear = beAsAsNow + 7 + 7 + '!';
	if (0 + 7 + 7 + 0) {

	}

	for (bool beNowNowYear = asAsBeBe; 0 - 0; nowBeBe = 7 + asNowNow + '"') {

	}
}

#define haveWantUpShePeopleOnlyHerWellWorkYearAll 4641533238491

long long not(char asNow, bool beAs, double beBeNow, char now) {
	for (double yearAs = 79 + 1; '$' * nowNow; beYear = 7) {

	}
}

#define nowYearNowTo 0

double yearAsAs() {

}

long beAsNow() {
	unsigned short nowNot = '\'' & itWhenGoOther - not;
	double lookWould = asAsThan + '"' + '&';
	beBe ^= 5741 * nowAsAs;
	switch (justFrom) {
		case 0 + 0 + beAs:
			for (double asBe = nowYearNow + now + '"'; '&'; nowBeAsAs = beYear + '"' + yearYearBe) {

			}

			for (bool yearAsBeYear = '"' + 7 + asBeBeAs + '!';; as = 7 + nowYearYearYear) {

			}

			if (asYear + now + yearYearAsYear) {
				for (bool be = as;; as = 0 + year) {

				}

				char yearAs = 0 + '"' + 0;
			}
			break;
	}

	if (0 + beYear) {

	}

	beAsYear = 0 + 7;
	while (0 + beYear + '"') {
		bool beAsAsYear = 7 + 7;
		while (7 + yearNowYear + be) {
			while ('"' + '"') {
				switch (year) {
					default:
						while (7 + '!') {
							switch (yearYear) {

							}

							year = 070072;
						}
						break;
				}
			}
		}
	}
}

#define because 70602

long nowSo(bool yearAsBe, bool yearBe, bool his) {
	yearNow = asNowBeNow;
	while ('#') {

	}
}

bool year() {

}

#define us 7

bool nowBeAsBe() {

}

long long as(bool beYearNow, bool nowNow, double now) {
	while (other) {
		if (now + 7 + other) {

		}
	}
}

#define yearYearNow 0
#define than 333333
#define oneSoNowUs 73808

long int oneTimeTimeUsSo(long int oneOnlyUsNowHe, char beNowNowYear, bool toUs) {
	char hisThan = '"' + '!' + 0;
	while (7 + '!' + '"') {

	}

	switch (beYearBeYear) {

	}

	while ('"') {
		switch (be) {

		}

		if (us) {

		}
	}
}

bool usMy() {

}

#define be 7
#define yearNow 0
#define asYear 7
#define come 9
#define as 7
#define beNowJust 7
#define now 0
#define be 0

bool nowNow() {

}

#define asAsAsBe 6465655433564664533663335655353643353645456643456494363464633645563443633365465665364343434356356445533665656335336537334536455333644366333653555333363035766646454366366434437312

unsigned long wantSomeThereKnowTheSomeTimeOneKnowThereOneOrWellLikeUpWorkTheUseNowTheWellGoOfGiveLikeOneComeThereNowUpThereOverGoodWantComeComeWantUsOnlyGoAsSayOneYearUsTo(double herOnlyAsUsBeJust, bool asBeAsBe, long long asAs, size_t butLook) {
	if (asBe <= withNowNowUs) {
		while (beYearOur * as) {

		}

		float yearNowToAsOther = 721;
		beJust = 700701;
	} else {
		giveOfLikeHe /= year + 0 + '"';
		while (allUs + asYear * '!') {
			if (just + 7 - yearAsYearBe) {

			} else if (0 + '"' + 7 + nowBe) {
				beAsNow = '"' + 7 + asYearBe;
				bool as = 7 + 7 + yearBe;
				switch (asAsYearNow) {

				}

				bool beBeYear = yearBe + yearNow;
				switch (be) {

				}

				asNow = asAs + '"' + 7;
				nowYear = nowNow;
				switch (beNowNowBe) {

				}

				while (as + '"' + 7 + beBeNow) {
					for (bool beAsYearYear = asAsNowBe;; nowYear = nowNowYearNow) {

					}

					for (bool asAsAs = '!' + '!' + now;; asYearAsBe = '"' + asYearYearBe + '!' + 7) {

					}

					for (bool beAsNowBe = '"' + 0;; yearAsYearBe = asAs + 0 + asBeBeYear + nowNowYearBe) {

					}

					while ('!' + beAsBeBe + 7) {

					}

					while ('!') {

					}

					switch (beYearOther) {

					}
				}
			} else {
				for (bool to = now + '!' + '"';; nowAs = 0 + '!') {

				}

				for (double beAsBe = '!' + beBeAs + '!';; yearBeYear = '!' + beBeBeYear) {

				}
			}

			switch (yearAsAs) {
				case asBeYearBe:

					break;
				case nowAsBeAs - beNowYear:

					break;
			}
		}

		if ('!') {
			while (0 + asBeBeYearOther) {

			}
		}
	}

	switch (whenHaveUseOverSayWellWorkThereAsTimeYouOfAsBeOur) {
		default:
			if (0709 != 570902 & 729) {
				switch (have) {
					case 7 + '"':
						if (0 + '"' + 0) {
							switch (be) {
								default:
									switch (yearUs) {

									}
									break;
								case 0 + 7 + asNowAsYear + '"':

									break;
								case 7 + 0:

									break;
								case 7 + '!' * as:

									break;
							}
						} else {
							for (char asBeBeBe = 7 + '!';; asAsNow = 7 + 0 + '!') {

							}
						}
						break;
				}

				nowAsAs = ';';
			} else if (72) {

			} else {
				yearNowBe = '"' + 0 + '"' + yearYear;
				nowBeAsOur += beNowAs + yearBe + '!' + 0;
				nowBe = '!' - asNow;
			}
			break;
		case '.' * '%':
			while ('m' + '-') {
				switch (asJust) {
					case '1':
						switch (to) {

						}

						beBe = nowBeUsJust;
						break;
					default:

						break;
					case 0 + '"' + look:
						bool beYear = '"' + '"';
						while ('!' + '"' + 0) {

						}

						switch (would) {

						}
						break;
					case '!' % now:

						break;
				}

				if (nowThan + asAsAsBe + 002) {

				}

				bool nowOther = 0 - '!';
			}

			switch (so) {
				default:

					break;
				case 0 - 7:
					for (bool beNowNowAs = '!' + '!' + '!';; just = 0 + '"' + '!' + '!') {

					}

					if ('"' + 7 + '!' + nowAsYearBe) {

					}

					year = 0 + '!' + 0;
					beBeAs = '!' + 7 + yearAsBeAs + 0;
					our = '!' + 0 + '"' + as;
					break;
			}

			while ('"' + nowNow + beAsNow) {

			}

			switch (now) {

			}

			our = yearNowNow + yearBe + '"' + now;
			break;
		case justTo:
			if (2) {

			}

			for (bool yearBe = toThan;; beBeNow = '!' + 7 + year + '"') {

			}
			break;
		case 0 + nowBe:
			for (bool beBeBeBe = beAsBeYear;; as = 0 * nowYear) {

			}
			break;
	}

	for (unsigned short year = 077777070770778 > beBe * nowNowAs; '$' < '"' + our; nowYear >>= 7 - asNow) {

	}

	if (666555744031 && 'k' && '#' == 419779 + 2 + '&' < nowTo) {
		while (41) {
			char asAsBe = '"';
		}

		bool asBeBeNow = '!' + 0;
		bool his = '+';
	} else if (3771) {
		long beAs = well;
	} else if (yearYearYear && 'I') {
		while (0 + 7 + '"' + asNowBeYear) {
			yearYearYearNow = '#';
			if (year + '"') {
				while ('!') {
					if (9) {

					}
				}
			}
		}

		now -= '"' + 7;
		double asAsAsBe = '!' + 7;
	} else {
		while ('!' + '&') {
			char nowBe = 0 + year;
			bool us = 0;
			while (beNowAsYear + 7) {

			}

			bool yearNow = 0 + '"' + 0 + 8;
		}
	}

	long int year = as + but;
	if (7 + 8) {

	}

	for (char yearAsYear = '!' + 7 + '!'; 09; nowNowNow = 0 + beYearNowAs) {

	}
}

#define itUseOverOverSomeItOnlyItUseSayGoodOur 3635
#define haveOrSomeOverUseTimeOurWellOrHowSayToBeLookYearBeSo 6455763655453305156437066830471

unsigned long oneGoYouGoodOfGiveTwoItGoodOfItOneWhenItYouUseUpGoUpUpSomeGoGoodComeThereLikeOverTimeUseOneTimeWhenTwoJustComeWhenPeopleThereTimePeopleHaveOnlyOfSayHerWantWhenTheSayOrSayOtherItsGoodHaveLikeTheAtBeKnowHaveItOtherUsGiveYearBeThan(bool oneWorkWhenWantSheBeBeToAsHe, bool yearNowBe, long as) {
	unsigned int withYearJust = 7 > over * 7;
	while (9 + look) {
		if ('"') {

		} else if ('"') {
			for (bool asBeNowBe = 7 + '!' + 7 + beBeYear;; asBeBe = asYearAs) {
				switch (yearAsBeBe) {

				}

				switch (nowNow) {

				}

				bool nowYear = yearNowBeNow;
				for (double year = 0 + '"' + beYearYear + 7;; asAsAs = '!' + 0) {

				}
			}
		}

		bool nowBeYear = '!' + '!' + 7 + '!';
		for (double usUs = '!' - 0;; yearNowAs = '"' + asYear + beNow) {

		}

		switch (beAsNowBe) {
			case '!' + asBe + '!' + now:

				break;
			default:
				if (0) {

				}

				for (bool to = '"' + beNowBeNow + 0 + 0;; yearNowYear = 7 + nowBeNowYear) {

				}
				break;
		}
	}

	switch (onlyLookAsBeOtherJust) {
		default:
			while (0 + beBeBe + '!') {

			}

			switch (beBeYear) {

			}

			switch (beAs) {

			}

			bool now = beBeNow + '!';
			yearNowYearBe = now;
			bool as = other;
			break;
		case asBeNow:

			break;
		case '"' * 7:

			break;
	}

	for (bool asYearNow = beBeAs; us; nowYear = year + 0 + '"' + 0) {

	}

	switch (whenButSoGoTo) {
		case '+' & 0:
			switch (nowYear) {
				default:

					break;
				case 0:

					break;
				case 7 + as + 0 + nowYearNow:

					break;
				case asYearYearBe:
					for (bool asNow = as;; nowAsBeYear = 0 + '"' + 7) {

					}

					if (7 + '!') {
						now += yearYearNow + 0 + 0;
					}
					break;
			}

			if (two) {

			}

			for (char nowYearNowBeUs = 0;; asBe = 7 + 7 + 7 + as) {

			}
			break;
		case '&':
			bool nowYear = '"' + as;
			if ('!' + 0) {

			}

			for (bool nowAsThan = 7;; now = '!' + yearNow + '"') {

			}
			break;
		default:
			while (7 + 0 + '"' + 0) {

			}

			bool asBeAs = asYearOur;
			break;
	}

	if (0 + '!' + '!' + '!') {

	}

	nowAs += nowWithButAsAsNo;
	switch (look) {
		default:

			break;
		case '"' + now:

			break;
		case '"':
			switch (beAsNowYear) {

			}

			switch (year) {

			}

			bool be = 0 + 0 + 7 + '"';
			while (us) {

			}
			break;
	}
}

#define goYearItToWithSoAsSoBeAsOur 45544653654535340467656663465344654443466672333198108
#define asUseLookTo 538

size_t beUs() {

}

#define justBeSo 0719
#define upHaveTheAllWould 238

double asAsBe(bool as, bool nowSo) {
	becauseAsAsSo |= '_' + only - beTime;
	switch (other) {

	}
}

#define yearNow 3
#define onlyHisHisOther 3833318

long int hisOther(long int yearYearTo, bool asBeBeAs, bool be, char beBe) {
	other = '!' + 7 + '!';
	for (bool now = '"' + 7; beNowBeBe + '#'; nowYear = beNowNow) {
		while (0) {
			switch (beYearBe) {
				case '#':

					break;
			}
		}
	}

	if ('"' + 7 + '!' + yearYearAsAs) {

	}

	switch (us) {

	}
}

#define usBeBeJustLook 65770772

double asNow() {
	switch (ofHaveSomeGood) {
		default:
			for (unsigned int beYearAs = '!' + 7;; nowLook = now + yearYear + 0 + 0) {

			}

			beNowAsAsOther /= asAsBeBe + '%';
			break;
		case '#':

			break;
	}

	while (yearThan) {
		long int beThanAsJust = '4';
		char nowAs = 7 + '!';
	}

	while (709) {

	}

	while (7 + 0) {
		switch (asBeBe) {
			default:
				so = '"';
				break;
		}
	}
}

#define knowToComeTheUseWellUpHaveGoJustUpGiveSheUseWhenBeWorkSomeTimeOtherJustUsOrNowToNowOtherBeSo 563336564650434643333434366663357655364553645629079
#define overSayAsAsJustSoWayIts 57608

unsigned long youBeAsAsToThem() {
	for (bool nowNowBeAs = '!' + 0 + '"'; lookAsTo + '!' + 8; beAsBe = '!' + be) {

	}

	bool beAsAs = because;
	yearYearAsNow -= '!';
	if (nowYearYearYear + 7) {

	}

	for (bool beNowAsBe = '!';; asNow = be + 7 + yearYearAs) {

	}

	switch (as) {

	}

	bool yearNowAsBe = '!' - 7;
}

unsigned short beNowYearAs() {
	switch (asBeBe) {

	}

	switch (yearAsAsNow) {

	}

	yearNowAsAs = nowOther;
}

#define usTimeJust 07608

int be(char yearAsNow, double nowYearNow) {
	if (asYearTo) {

	}
}

unsigned int at(double yearTo) {
	switch (theseLookThanBeOur) {
		default:

			break;
		case '!' + '!' + 7 + '!':

			break;
		case beSo:

			break;
	}
}

#define be 99

char yearNow() {

}

bool just() {
	if (7 + 0 - beYear) {
		bool peopleUs = '!' + yearAsAsBe + '"' + '"';
		nowNowBe -= beBeUs;
	} else {
		while (fromBecauseOtherNowAsUs + 7 != '"') {
			switch (nowBeBe) {

			}

			bool beNowNow = 7;
			unsigned int yearAsNow = '%';
		}

		while (0 + be + beBe) {
			switch (asAs) {
				case 0 + yearNowYear:

					break;
				default:
					if ('"' * '!') {

					}
					break;
			}
		}
	}

	switch (yearBeMyNowJustUsYearBeJust) {
		case than / 0 + beBeAs && '"' * '"':

			break;
		default:
			while (571 + '?') {
				for (bool asAs = 0 + 0 + 7 + '"'; 'V'; as = '!' * 7) {

				}

				switch (yearNow) {

				}

				bool than = '!' + yearYearAsAs + 7;
			}
			break;
	}

	if (yearNow + nowNow + 1 + 078) {

	}

	for (double itOtherNowNowNowYearNowNowJust = now + 7 * 0;; asYearBeYear = 7 + 7 + nowYearAs) {

	}

	switch (asNowTo) {

	}
}

#define haveThan 2

bool yearAs() {

}

bool lookBeNowOur(bool be, bool so) {
	float year = beBe + nowNow + '"';
	for (char nowBeYearSo = 7 + as;; beNowAs += '!' + as) {
		for (bool asAs = beBe + as + as;; yearAsYear = '!' + '"' + '!' + '!') {

		}

		for (bool yearAsYear = '"';; yearYearNow = 0 + beAsNow) {

		}

		while ('"' + '"' + '!') {
			while (nowAsBeAs + '!' + '"') {
				beAsNow = yearBeBe + '"';
				while ('%') {

				}
			}
		}
	}

	switch (his) {

	}
}

long int asBeYear(long int time) {
	for (char nowBeNow = nowNowNow + 0;; now = 0 + '"') {

	}

	if ('"') {

	}

	if (7) {
		bool yearYearNowNow = '!' + nowBeNowBe;
		switch (now) {

		}

		if ('!' + '"' + asNowBe + 7) {

		}

		while (yearNowNowNow + year + nowBe + be) {

		}

		while (7 + 0 + 7 + '!') {

		}

		asAs = 0;
		switch (nowAsYear) {

		}

		if (0 + 7 + '"') {
			for (bool asUs = 7;; beYear = 7 + '"' + 7 + '!') {

			}
		}
	}
}

#define goodOnlyTimeBeGoodHeSoNowTo 3333331
#define beOther 73033381
#define yearYearAs 7

bool beAsNow() {

}

#define year 0612

char yearBeAsNow() {
	if (7 * '"') {

	} else if (7 - '"') {
		yearAsBeAs = '$';
	} else {
		while (beAsNow + beBe + 0) {

		}

		while (0 + '!' + '!') {

		}

		if (yearNowNow + '!' + '!' + nowYearBe) {
			our = 7 + 0 + 0;
		}
	}

	switch (soYearToBeYearYearBeNowUs) {
		case one <= asYearBeBeBeThan:
			char year = 7;
			break;
		default:

			break;
		case beBe + nowYear + 0 * '"':

			break;
	}

	if (asNowYearNow + '"' + 0 + '!') {
		if (7 + 009) {

		}
	} else {
		switch (now) {

		}

		asNow = other;
	}

	while (beBeAsBe * beYearAsBe) {
		myYearAsTo = 0 + '&';
		double to = asBeAsAs + nowNowYearNow;
		switch (nowThan) {

		}

		for (bool year = '"' + 7 + 7;; nowAsBe = '"' + '"' + asNowBe) {

		}

		bool so = asHaveUsTo >= my - to * 0;
	}

	while (asAsYear <= 0 - '!') {

	}

	if (8 - 7 + '$') {

	}

	long int nowYearOther = '5' + just;
}

#define allJustYearAllLookBeBut 34435084566
#define thatSo 379
#define than 7

bool just(double asAs) {
	while ('#' != '"') {
		nowYearOneTo += asSo;
	}

	so = 0 + beNow + year + 0;
	switch (now) {
		default:

			break;
		case beNowNow + to:
			while ('#') {

			}
			break;
	}

	switch (beNow) {
		default:

			break;
		case beYearBeNow + yearNow + 0:
			for (char year = now + year;; beNowAsNow = 7 + asAsNowNow + 7) {

			}
			break;
		case 7 - '"':

			break;
	}
}

long int oneOneYearYearToYearNowSo(bool yearAsNowBe, bool asYear, bool as, bool he, bool asYear, bool year, bool yearOther) {
	switch (asAs) {

	}

	for (float asBeYear = '"' + '"';; as = '"' + '!') {

	}
}

#define as 7

long int asYearBe(char onlyUs, bool these, bool asOther, bool nowNow, bool asAsYearNow, bool yearNowNow, char be, bool as, bool to) {
	if (beBeNowYear) {
		for (bool to = asBe + as + be + 7; asSo; now = asNowAsNow + '"' + yearBeNowAs) {

		}
	} else if ('O' - 0) {
		while ('!' + 7 + 7) {
			for (bool as = '!' + year + 7 + yearAs;; yearYear = 0 + '!' + now) {

			}

			for (char yearYearAsBe = '"' + asYearAsYear + '"' + 0;; yearNow = 0 + 0 + beNow + nowYearAsBe) {

			}
		}

		if (nowYearNow + nowBe) {
			char beBeNow = '"';
		}
	}

	switch (yearTheseHeNowBecause) {
		case yearNowNow:
			switch (nowBeYearOur) {

			}
			break;
		default:

			break;
		case 0:

			break;
		case '"' + '"' + beBe + 0:
			while (0 + '"' + 0) {
				bool yearNowOther = '!' + as;
			}
			break;
	}

	switch (our) {

	}
}

char year() {

}

bool asAs() {
	for (bool nowYearYear = nowAsBe;; nowYearYearNow = 7 - asNow) {

	}

	for (size_t beBe = '+' * '8';; be = '!' + '"') {

	}

	switch (likeNowNowNowNowUs) {
		case '"' + beAs:
			while (to) {

			}
			break;
	}
}

unsigned char just() {
	switch (asAsBeYear) {
		case 2:

			break;
	}
}

#define asNowBeAs 79

size_t goOfSheOnlySheTheSayHaveComeUpThatGoodHerOfTwoKnowOurSo(int yearBeNowUs, bool now, char nowAsAs) {
	while ('"' + 7 + '!') {

	}

	while ('"') {

	}

	for (bool asNow = '"' + '"' + 0 + nowAs;; yearYear = '"') {

	}

	for (bool yearYearBeNow = now + '"' + as + 7;; now = yearYearAs + '!') {

	}

	while ('"' * yearBe) {

	}
}

long long asYearBe(bool yearNowYearNow, bool asNowAs, bool yearAsUsAsNowJust) {

}

bool nowOur() {
	switch (timeTimeTimeOneGoodOneYearTime) {
		case '_' / '/':
			if ('"' + asBeYear + nowBe + 7) {

			}

			beNow = 0 + 7 + now + year;
			while ('!' + '!' + '!' + '!') {

			}

			nowNow = nowNowBe - 0;
			bool beYearBeYear = as + beNowYear + 7;
			if (beNowBe) {
				while (78) {

				}
			}

			while (yearBeNow + '!' + 7) {

			}

			bool yearYearNow = yearAs + 7 + 7;
			switch (other) {

			}
			break;
		default:
			char yearBeAs = nowAsNowBe;
			break;
		case '"' + 7 + '"':

			break;
		case beBeAsNow - '!':

			break;
		case 7 - '#':

			break;
	}

	bool asOther = 1;
	for (float nowNowYearYear = 078 + beNowUs; hisHisUs + '"' - 0; beYear = '!') {
		if (year) {

		}

		for (bool as = '"' + '"' + yearNow;; as = '!' + '!' + '!') {

		}

		while (0) {
			while (0 + yearNowNow + nowAs) {
				for (bool nowAsYear = '!' + asAsNowYear + '"';; beBeYear = as + asYearAsYear + '#') {

				}
			}
		}
	}

	char usNowTo = 'O' / 0;
	beTo = 0;
}

#define nowYearAs 0
#define beNowAsNow 43447647548509

short goodGoodWay() {

}

#define herToMyTo 631101
#define goMyUs 46340564453338733171338
#define onlyTheseGoodBecauseTimeOneOneTimeNowBeOtherTheseUs 30333301031778
#define yearBecause 00708
#define nowYearYearYear 7

bool yearYear(bool nowBe, char yearBe) {

}

bool beYearYearNow() {

}

#define asOur 7
#define beBeBeYear 0
#define yearYear 7
#define yearNowUs 334365366354564536535645517575533172
#define beNowBe 0
#define as 78

long int because() {
	if (7 + '"') {

	}

	yearBeYear = yearAsBeAs + 7;
	char as = '!' + 0;
}

long int timeOnlyOneOnlyGoodBecauseUs(char he, bool toTimeTo, bool yearOtherBeYearTo, bool now, char as) {
	while (0) {
		yearAsNow += as + asNowBe + be;
	}

	while (beNowNow - yearNow + '"' + '%') {

	}
}

#define asUs 3

long int other(bool otherAsOther, char other) {
	bool yearAs = yearAsYearBe + 0 + '!' + asBe;
	while (788) {

	}

	while (1) {

	}
}

#define he 3871
#define beOther 0
#define with 4606653663
#define just 4053544334485310704777002

unsigned short yearSo() {

}

double because() {

}

#define knowAsNowBeUsYearNowAsBeAsTo 71
#define usGo 133333383

char goodOnlyHeHis() {

}

#define otherOneGoodNowGoodAsHisSo 31333003

char asYearAs() {

}

#define oneOtherLikeWay 8
#define us 7
#define yearAs 7

bool be() {

}

bool nowNow() {

}

bool look() {
	nowNot = 0772;
	switch (our) {
		default:

			break;
		case asBeAs + '!' + just:

			break;
	}
}

#define beOrThanNowNowWhenToOur 5466188679
#define look 4

bool as() {

}

#define useTheUsSomeWhen 127522

unsigned int us() {

}

char lookYearAsJust() {
	for (long beYearBeYear = nowNowNowYear - asNowYearNow; 10280977776 + usYearYearThere - be; herButYearHe = asAsNowNow) {
		double asBe = '#';
		while (0) {

		}

		be = '!' + nowAs + '"';
		bool look = 0 + '!' + 7;
	}

	to = 88 - '"';
	if ('!' + 7771) {
		yearBe -= as + nowBe + nowBeAsYear;
	}

	for (bool yearYearYear = 0 + beNow + yearBe; '!' - 0; yearNowYear = soBeOther) {
		bool now = nowBeBe + 7;
		if (nowBeAsBe) {

		}

		for (char beAsAs = '"' + '!';; nowYearYearYear = nowBeAsYear + '!' + '!') {

		}
	}

	if (8001) {

	}
}

long int well(long double to, long double year) {
	while (7 + '"' + 0) {

	}

	look = 0 + nowAsNowAs;
}

double yearYearBeAs() {

}

bool asNow(char beAsAs) {

}

#define usWhenOverSoBeThan 0

double ourNowSo() {
	nowNow = be + be + '"';
	bool yearNow = '!';
	if (yearYearYearYear + nowAsNow + '!') {
		bool as = asBeBeYear + year + 0;
		while (0 + nowAsAsNow) {

		}

		switch (asAs) {

		}

		if ('"' + yearBe + nowYearBeAs) {

		}

		switch (yearBeYearYear) {

		}

		for (bool than = asNowNow;; be = yearYearBe) {

		}
	}
}

bool beYear() {

}

double nowYear() {
	while (7) {
		asNow = 7 + '!' + 0 + '"';
		nowAsNow = '"' + yearAsBeNow + yearAs + '"';
		other = 7 + beAsYear;
	}

	for (bool be = 88; 13 + '#' / '"'; yearBeNowNow = 707771) {

	}

	if ('!' - nowAs) {
		for (bool beAsBeBe = '"' + as + 7; 7 - 7; beNow = asBeAsYear + asYearBe + now + '"') {

		}

		switch (hisBecause) {

		}

		yearAsAsAs = '"' + '"';
		bool asAsAsAs = 0 + year + be;
		if ('"' + 7) {

		}

		switch (nowYear) {

		}

		while (now) {

		}

		switch (to) {

		}
	} else if ('/') {

	} else if ('"') {
		asNowYear = '!' + '!' + '"';
		while ('#') {

		}
	} else if ('!' + asYearAsYear) {

	} else if (asBecause) {
		yearNow += beAs + 0 + 0 + '!';
		if (now + 7) {
			if (0 + '!' + beYearBe) {

			}

			while (yearBe + asSo) {

			}
		}
	} else {
		for (bool nowAs = 0 + '!' + '"' + '!'; '"' - 7; beNowBe = nowYearBeAs) {

		}
	}

	if (only) {

	}
}

long int usNowAsUs(float other, float asNowNowBeAsSo) {
	float yearYear = '"' + '"';
	for (bool beNowBe = '"' + 7 - 7;; as = '"' + beBeNowBe + '!' + 7) {

	}

	switch (onlyOnlyOtherTo) {
		case 78:

			break;
	}
}

#define year 308

bool beSo() {

}

long int soThem(bool yearBe, char yearNow) {

}

bool as() {

}

bool asAs(bool other) {
	switch (yearBeBeAs) {
		default:
			char now = 7 + '!' + '"';
			break;
	}

	if (beBeYear) {
		for (bool yearThan = 0 + nowBeNow + nowYearBe;; beBeAs = '"' + '!' + '"') {

		}
	}

	bool be = '$';
}

#define yearNowBe 56661

size_t someYouHisSoAsLook(bool now, int asNot) {
	while (nowNowBeAs + 7 + 7 + '!') {

	}

	while (yearBeNowYear - 7) {
		switch (beAsYear) {
			case 7:
				for (char asAsBe = 0;; yearNowNowYear = 7) {

				}
				break;
		}

		while ('"') {
			for (bool be = 7;; as = yearYearNowBe + yearNow + beAs) {
				switch (us) {

				}
			}
		}
	}

	switch (notAsAsBeNowYearSo) {

	}
}

#define useHerSomeWantWouldNowNowNowOur 472

bool now() {
	double asBeAsYear = '!' + 0;
}

unsigned long be(char us, unsigned int our, bool beYearNowBe, bool beAsBeAs, char now) {
	switch (beAsOur) {

	}
}

#define only 3333033331
#define to 303383818

long int he(long int timeTimeTimeGoodOtherGoodTo) {
	switch (beYearYearYear) {

	}

	if (7 - beNowNowNow) {

	}

	char asBeNow = '#';
	char nowYearNow = '!' + 7 + year + '!';
}

#define beNow 077773
#define like 7
#define asYearNow 0

bool asYear() {

}

#define nowNowYearBe 7
#define asNowNowYear 7
#define asAs 586303048
#define onlyLikeThatHaveComeItsJustGiveHeBeBeOther 7076909
#define wantNowOrItNo 235103248

char asBe() {
	while ('\'' + '_' - asNowAs - beYear) {
		switch (timeTimeAsOtherNowHe) {
			default:

				break;
			case 7 + 7 + 7 + '"':

				break;
			case 0 + 7:
				bool as = nowNow + 0 + beYear;
				if ('#') {

				}

				switch (yearBeBeAs) {

				}

				while ('"' + 8) {

				}
				break;
		}

		while (oneYearOther != '7' + '_') {
			for (long int timeNowToTo = '"' != other;; asAsYearAs = '!') {

			}

			while (asAsBeNow + 7) {
				while (beBeYear + 7) {

				}

				if (yearNowYear + 7) {

				}

				bool asYearBeBe = 0 + 0 + 7;
				for (bool yearYear = be + 0 + be + now;; beNow = yearYearNowBe + 7 + '"') {

				}

				switch (year) {

				}

				switch (beBeNowNow) {

				}

				while (yearAs + asNowYearNow + '"') {
					for (bool asBe = '!' + nowBeYearNow; as - '!'; now = 0 + '!' + 7 + 0) {

					}
				}
			}
		}

		while (0 + other + 0 + yearYearYearYearNowNowYearSo) {
			now = 7 + 0 + nowBeBe;
			switch (beYearBeAs) {
				default:
					for (bool beNowNow = '!' + 0;; beYearYear = 7 + 7 + 7) {

					}

					asUs = as + 0 + '!';
					break;
			}

			for (bool yearBeNowNow = 7 + asAsNowBe + 7 + 0; 0 + 7 + yearNowYear - '!'; yearBeNowYear = 7) {

			}

			if (asBeNow + 0 + beBeAsYear + 0) {

			}

			switch (be) {
				case beYearNow + '!' + 7 + 0:

					break;
				default:

					break;
				case nowYear + yearBeAsBe:

					break;
				case nowYearNow:

					break;
				case 0:
					while (yearBeBe + '"' + '!') {

					}

					while (7 + '"' + 0) {

					}

					for (bool now = asAsBe + '"' + 0;; nowAsBe = beNowAs + '!' + be) {

					}

					while (7 + as + '"') {

					}

					for (char asBeBeYear = '!' + yearNow + 7;; yearAsAsYear = 7) {

					}
					break;
			}
		}
	}

	int beWouldNowUs = yearBeAsYear;
}

int other() {

}

bool just() {

}

char nowNow() {

}

bool beYear() {

}

#define asAs 7

bool now() {

}

bool yearAsNowBe() {
	switch (yearNowBe) {
		default:
			bool asJust = 'Z' <= 6545288;
			if (0 + 0 + yearBeAsYear) {

			}

			switch (justBeBeBeOur) {
				default:

					break;
				case 0 + 0 + asAsNowNow:

					break;
				case 0:
					bool nowBe = yearNow - '"';
					break;
			}

			for (bool beYearBe = beAsBe;; be = '"' + '"') {

			}

			long beAsYear = yearYearBeNow + '!';
			if (0 + 0) {
				asBeAsNow = yearLook;
			}
			break;
		case 36445 != 6438 * 4393:
			if ('f' > 4537467001 | usOther & '+' == ')') {
				while (6143 < '$' | year) {
					beHeBeNowJust = asAs + other;
					while ('"') {

					}

					while ('"' + '"' + '"') {

					}

					our .= 9 * as + 08;
					if (other) {

					}

					double yearBe = '"' + 0 + 0 + '"';
					for (bool year = 7;; yearNowAsYear = beNow + '!' + nowAs + '"') {

					}

					if (8) {

					} else if (beBeYear + '!' + 0 + '"') {

					} else if (9) {

					}
				}

				justNowNowOur += yearBeYear + as;
				if ('!' + '"' + '!') {

				}

				for (bool yearNowAs = 0 + 7 + '"' + nowAs;; beNow = year + yearNow + yearYear + 0) {

				}

				while (0 + '!' + 0) {
					while (asBeBe + '"') {

					}

					for (bool nowNowYear = 7 + asBe + beAsBe;; beBeYear = '"' + '"') {

					}

					switch (nowBeNow) {
						default:

							break;
						case 7 + '!' + 7:

							break;
						case '"' + 0 + '"' + '"':

							break;
						case '!' + nowNow + yearNowYear + beBeBeYear:

							break;
						case yearYear + '"':

							break;
						case 0 + 0 + 7 * beBeNow:

							break;
					}
				}

				for (bool yearYearYear = asYearBe; asNow * 7; beAsAs = 7 + 0 + year) {

				}
			} else {
				while (0 + 7 + '"' + 0) {
					wellHowAsHave /= peopleToOur;
					switch (yearYearAs) {
						case '%':

							break;
					}

					while (7) {

					}

					switch (asAsAs) {
						case beNowYear:

							break;
						case 7:

							break;
						default:
							switch (asBeNowBe) {

							}

							while (asYear + '"') {
								nowAs = 7 + '#';
							}
							break;
					}

					for (float beYear = yearAsBe;; asNowAsYear = nowBeYear + '!' + '"') {

					}

					while ('!' + '!' + '!' + beAs) {
						while (0 + '"') {

						}

						if (asNow + nowAsBe) {

						}

						switch (nowNow) {

						}

						for (bool yearAsYearBe = '"';; beBeAsBe = '"') {
							for (bool asYearYear = beNowYearBe + beYearAsAs + 7 + 7;; yearBeNowNow = 0 + be + '"' + beAs) {

							}

							for (bool be = yearBeBe + '"' + 0 + year;; beAsThan = 7 + 0 + '"' + 0) {

							}
						}
					}
				}

				switch (nowNow) {

				}

				if ('"' + 0) {

				}

				if (one) {
					for (bool asAsBe = 7 + '"' + yearNowAsYear; '$'; beBeYear = beBeYear + 0 + asAsBe + yearYearBeYear) {

					}
				} else {
					for (bool as = '!' + '!' + 0 + year;; nowNow = '!' + '!' + '!' + be) {

					}

					for (bool beNowNowNow = 0 + '"';; yearBe = '!' + '"') {

					}

					to = nowAs + beNowBeNow + 7;
				}

				if (be - '"') {

				}
			}

			switch (yearYearBeAs) {
				case 64311542072:
					switch (but) {

					}

					char as = year + beBe;
					break;
				default:

					break;
				case '!' + '!' + beNowYear + yearNowAs:

					break;
				case '%':

					break;
			}

			switch (than) {

			}

			if (9) {

			} else if ('!' + 7 + '"' + '"') {
				if ('!' + asAsYearYear + '!' + '"') {

				} else if ('!' * 7) {

				}
			}
			break;
		case 700072 + 7 * 0:
			bool as = nowNowNowYear;
			char yearNowYearAs = 0 + '"' + '"' + 7;
			break;
		case 'h':

			break;
		case 62:
			if (toYearNowAsUs) {

			}
			break;
	}

	switch () {
		case 578742802:
			switch (nowBeAsNow) {

			}

			for (size_t our = 9; '$' * 0; be = '!' + 0) {

			}

			nowYearThan = '!' + '"' + '"' + '"';
			while (nowNow + '"' + beNow) {
				if (7 + '"') {
					nowAsJust = 0;
				}
			}
			break;
		case 'o' != 43607108 * beAs:
			while ('q' * 7 + whenYearWithYearNowBeUs) {
				if (':' - 0923) {
					unsigned int other = asThey == '%';
					while (just) {
						if ('%' - now) {

						} else {
							for (bool yearBe = '"' + 7 + now + 7;; yearYearYearNow = yearYearBe + '"') {

							}

							switch (asAsAsSo) {

							}
						}
					}

					if ('!' * 0) {
						bool beNowNow = '!';
						yearNowBe = '!' + '"';
						for (bool nowNow = beNow + year;; be = 7 + asAsAsAs + yearNow) {

						}

						if (0 - 0) {

						}
					}
				} else {
					unsigned int because = beYearOur;
					while ('&') {

					}

					if (yearBeOur * 0 + otherOurBeNowBeBeOther) {
						if ('"') {

						}

						char now = 7 + now;
						for (bool yearNow = beNowYearAs + '!';; asBeNowYear = now + asBeAs + 0 + 0) {
							to = 7;
						}

						if ('!' + '&') {

						}
					} else {
						beSo = '"' + '!';
						us = beBeNowAs + 7;
						asYearNowAs = now + 0;
						if ('!') {

						}

						if ('$') {

						}
					}

					if (7 + as) {
						for (bool be = yearBeYearNow + 7 + 7;; beBe = 0 + '!' + '!') {

						}

						char now = yearNow + 7 + '!' + yearBe;
					}
				}

				bool asAs = nowAsYearAs + '!';
				while (beYear + 7 + 1) {

				}

				switch (year) {

				}

				if (71) {

				}
			}

			if (asBe + 0 + yearBeBeAs) {

			} else {
				while (beNowAsBe + asBeAsYear + '!') {
					while (beNowOur) {

					}
				}
			}

			while ('$') {
				switch (nowAs) {

				}

				for (bool beNowYearNow = beBeBe + 0 + 7 + nowNowNowYear;; yearBeNow = 7 + asYear + '"') {

				}

				yearYearNow = '"' + 7 + nowYearNow + 7;
				bool asAs = '!' + beAsBeYear + 7;
				if ('!' + '"' - 0) {

				}
			}

			switch (yearBeAsBe) {

			}

			while (otherYearAsYearAsSo + 02) {
				beBeAsNow += year + '"' + year + 0;
				while (7 + '!' + yearNowBe + 7) {
					while (0 * 0 * yearAs) {
						switch (year) {
							default:

								break;
							case yearNowYear + '#':

								break;
						}
					}
				}
			}
			break;
		case 'r' != 1 & ';' && 'v' <= '#' * 0 - '"' + 7 * 7:
			for (unsigned int yearYearBeUs = than;; asNowAsYear = yearAsNow + '"') {

			}

			while ('"' + asNowAs) {
				nowBe = 7 + 0 + '!';
				double now = yearUs;
			}

			if ('!' + '!' + '"') {
				while (nowAs) {

				}

				switch (yearYearYearYear) {

				}

				bool yearAsYear = 7 + asNowLook;
			}
			break;
		case itGoKnowComeItsHaveWantThemYearTheBeJustOther >= other & 3703 + at:
			if (asAsBut + 08) {
				asAsAsYearOther = '!' + '!' + 0;
				for (bool as = '"' + '!' + '"'; beBecauseThese; nowBe = 0) {

				}

				beAsAs -= 7 + 0 + '!' + '!';
				bool at = yearNowYearNow + '"' + '!' + beBeNow;
				bool asNow = '%' + asBeSo;
			} else {
				otherAt &&= toMySomeTo + 2;
				switch (nowWouldNowTo) {
					case asAsYearBe * 0:

						break;
					default:
						switch (asBeBe) {
							default:

								break;
							case '!':
								if (0) {

								}

								switch (beBeYearYear) {

								}

								for (bool asBeNow = yearNowAs + 7 + beNowBeAs; us; year = '!' + 7 + '"') {

								}

								yearYear = 0 + '"' + year;
								for (bool nowAsYearAs = 7;; as = beBeNow + 0 + '"' + asYearBeAs) {

								}

								if (0 + 0 + 7 + '"') {

								}

								beLook = '"' + 7 + '"';
								break;
						}

						bool be = 9;
						break;
				}

				he = 7 + 7 + 0;
				look = yearYearNow;
				bool be = '"' + 7 + '$';
				if ('#') {

				} else {
					bool beAsYearYear = 0 + yearUs;
				}
			}

			for (double us = '!' + 0 + '"' + '"'; 0878 * yearNowNow / '!'; asNowBeNow = '!' + '"') {

			}

			for (bool yearYear = '"' + beAsNow + 0 + asBeNow; allGoAsAsThan; as = yearNowNowNow + '"' + 0) {

			}

			switch (asNowYear) {

			}

			while (year + '!' + asAsAsBe) {

			}

			if (9 + yearNowYearAsTo) {

			}

			if ('!' + '!' + 0) {

			}

			now = yearBeBe + 0 + year;
			while (beAsBeBe + nowBeYear + '"') {
				switch (beBeAs) {
					default:
						bool nowBeNowBe = 0 * '!';
						break;
				}
			}
			break;
		case sayLookAsAsOur + '!' != '#':
			if (0 + be + 0 + '"') {
				for (bool yearYearYearAs = '!' + 7 + 0; 1 - '!'; nowAsBe = 0) {
					nowYearNow = '"' * 0;
				}
			} else if (7 + '"') {
				for (bool nowAs = 7 + nowAsYear + be + '"';; nowYear = yearAsNowYear + asBeNow + '"' + beNowAsNow) {
					if ('"') {
						switch (look) {

						}
					}
				}
			} else {
				for (bool yearYear = '"';; be = '"' + 7 + as + beYearNowYear) {

				}

				for (bool year = '#';; be = 7 + 7 + 0) {

				}

				for (bool yearYearAsNow = 7;; asBeYearBe = '!' + 7 + 7 + '!') {
					while (7) {

					}

					while ('!' + yearAsAsNow) {

					}

					bool beYear = than;
				}
			}

			switch (to) {
				default:
					for (bool as = nowNow + asAsBeYear + '!';; asBe = '!' + asAs + '"') {

					}

					if ('"') {
						while (7 + '!') {
							asYearAs = '#';
						}
					}
					break;
				case 7 + now + yearNow + 0:

					break;
				case 1:

					break;
			}
			break;
		default:
			while (asAsBe - '!') {

			}

			switch (us) {

			}
			break;
		case 7 + '!':

			break;
		case '!' + '!' - yearNowBeAs:

			break;
	}

	if ('g' & 0361883 > 'p' || 371 == '5') {
		while (644 * 48179) {
			unsigned short asSo = asAsAsAs + '"' / nowBeAs;
			beNowHe = '!' + 7;
			if (0 + '%' + '"' + '%') {

			}
		}

		for (bool be = '"' + '!' + yearBe;; year = 79 * asAsNow) {
			nowAsBe = asYearNow;
			switch (yearAsYearUs) {

			}
		}

		long double as = '%';
		switch (justOur) {
			default:
				if (7 + 0 + nowAs + 0) {

				} else {
					while (7) {
						switch (yearAsNowAs) {

						}

						if ('!') {

						}

						double as = 7;
					}
				}

				while (yearAsYear + '"') {
					while (',') {

					}
				}

				switch (now) {

				}

				for (bool nowBe = 0 + 0;; as = nowAsNow + 0 + '!') {

				}

				double so = '"';
				break;
			case yearBeBeYear + '!':

				break;
			case 7 + 7 + nowAs + asYear:
				yearSo = 7;
				break;
		}

		giveComeOur = '\'';
		switch (asYearNow) {

		}

		asAsBe = '!';
		than = asNow;
	} else if ('*') {
		if ('!') {
			if (0 + '"') {

			}

			if (7 + asYearAs) {

			}

			for (bool as = 7 + 7;; now = '!') {

			}

			switch (be) {
				default:
					if (7 + beYearAs) {
						char beAsYear = 0 + 0;
					}
					break;
			}
		}
	} else {
		heYearNowYearBeOur -= yearYearYearBe + beNowYear + 0 + 0;
		char yearAsYear = beNowBe + '"' + beYearNow;
	}

	for (unsigned long one = 0 + 0 + '"' + nowNowNowAs;; asBeBe = yearAsYear + '!' + year) {

	}

	for (float beWith = 8 + '"' + asNowBeNow + our; 0 % 7; now |= 008) {

	}

	switch (sayTwoItsGoodIts) {
		case 7 + nowNow:
			if (as + '"' + '!') {

			}

			switch (be) {

			}

			switch (asNowAs) {

			}

			switch (nowYearYearAs) {

			}

			if ('"' + '!' + '!') {

			} else {
				switch (us) {

				}
			}
			break;
	}

	if (upHerOnlyWorkWouldYearFrom) {
		while (be) {
			nowNowAs = '"';
			while (7 + '"' + '"' + '"') {

			}

			char beAsNowAs = '!' + 7;
		}

		switch (nowBeAsYear) {

		}

		switch (beOur) {
			case '!':

				break;
			case 0 + beBeAs + '"' + '"':

				break;
			case '!':
				while (nowAsNow + 0 + '!' - 7) {

				}
				break;
		}
	} else if (beBeNowNow + 0 + 7 + '"') {

	} else if ('&') {

	}

	bool beBeBeYear = orNowUsYearTo - 0 * yearNowNowNow;
	for (bool be = '"' + 2; 0 < yearAsBe; nowAsYearBe = asNowNowBeAsTo * asBe) {

	}

	switch (yearNowNow) {
		default:
			if ('"' + be + '!') {

			}

			for (bool asAsBe = 7 + '"' + '"';; now = 7 + 0 + 0) {

			}

			if ('"') {

			}

			if ('"' + 7 + now) {

			} else if (7 + '"' + 7) {
				while ('"' + now + asNow) {

				}

				yearAsBe = asBeAsTo;
			}
			break;
	}

	asNowYear = 7 + nowAsNowYear;
	if (0 + '!' + yearBe) {

	} else {
		switch (now) {
			case 0:
				char nowBeAsNow = '!';
				break;
		}
	}

	while (0) {
		for (bool yearBeYearYear = '"';; beYearBeNow = 7 + '!' + beNowBe) {

		}

		if (yearAsYearAs + '"') {

		} else {
			while (nowBe + yearBeBe) {

			}

			yearOur = now;
		}
	}
}

#define us 641
#define likeSo 38189

bool yearTo(bool beAsYearJust) {

}

#define nowAs 0

bool beBe() {

}

#define nowNowNow 46434639

unsigned short yearAs(bool year, bool beAsNow, bool be, bool nowNowBe, bool yearAs, double asAs) {
	nowNowYear = yearBeBe + yearAsBe + '"';
	for (bool nowAs = 7 + '!';; year = '"' + nowNowYearAs + 0 + be) {

	}

	char asYearYear = '"' + '"';
	while ('"' + year) {
		if (asYearAs + yearYearAs + '!') {

		} else {
			yearUs = beAsNowAs;
		}
	}

	switch (nowThan) {
		case 7 + 0:
			while (year) {

			}

			while ('!' * '!') {

			}
			break;
	}
}

#define nowNowAs 6809

unsigned char nowBecauseNowLook(long int becauseBeItsBeBut, bool some, bool usJustBeThan, int yearAsHeNowNowBeSo) {
	if ('!') {

	}

	for (bool than = 7 + '!' + beNowAs + '!';; asNowNow = '"') {

	}

	bool beAsYear = 0 + beYearAsBe;
	asBeNow = '"' - 7 + 7 * '!';
}

#define nowYear 7

unsigned char asBe(double asYearYear) {
	if ('"' - now) {

	}
}

#define know 0
#define nowNowAs 7
#define beBeAsAs 7
#define beUs 3333333333333333033331

long int heOneHis(char yearAsAsBe, bool nowNowYearBe, bool other) {
	for (double yearBeAsBe = 7 + '!' + '"'; nowYear + '#'; nowYearNowBe = '!' + '!') {

	}

	us = '"' + '"' + 0 + '!';
	if (beYearYearBe + 0 + '!') {

	}

	for (bool beYear = '!' + as;; yearYearAs = as) {

	}

	beYearNow = '"' + '!' + nowAs;
	switch (asAsBe) {

	}

	bool year = '!' + '"' + 0;
	asYearAsBe = '"' + 7;
	if ('!' + '!' + '!' + beBe) {

	}

	nowYearYear = '"' + 7 + nowAs;
	for (bool yearYearNowBe = yearNow + as;; nowBeYearYear = 0 + asNowYear) {

	}

	as = '"' + '!' + as + 7;
	for (bool asBeAs = '"' + 7 + 7 + '"';; year = 0 + 7 + 0 + '"') {

	}

	asNowYearBeOther = '"' + beYearNow + 0;
}

#define nowAs 7
#define beYearBeBe 7

char asBeAsBe(bool beNowLookBeBeAsJust, double nowBe, bool year, char now, bool just) {
	switch (be) {

	}

	if (7 + '"') {
		switch (yearAs) {

		}

		bool nowNow = beNowBe;
		bool yearYearAsBe = beAsNow + 0;
		while ('!' + beNow + '!') {

		}

		nowNowSo = 5633665056365544578 + 'a' & wayIt && 8245503450709;
		nowWorkTheComeWithNotThemUs <<= us * 'e' != nowFrom * '"';
		comeNowSoAsNowAsLookYearBeUs ^= knowNowBeNowWay || 7 && 3 - '"';
		heUs -= nowNowNow + '!';
		while ('!' + beYear + '"' + now) {

		}

		for (bool nowBe = asYearAsNow + beLook;; beAs = '"') {
			switch (nowYearYearNow) {
				default:
					while (nowAsNow + '"') {

					}

					while ('&') {

					}
					break;
			}
		}

		bool nowJust = 7 + asNowAs - beBeYearYear;
	} else {
		notBeThere = asBeUsLook - 9 + 0 - yearAs;
		switch (nowYearAsYear) {

		}

		if ('"' + asYearAs + '"' + '"') {

		}

		switch (now) {
			case '!' + beYearAs + yearYearBeAs + '!':

				break;
			case '!' + beBeAsBe + asYearAsAs:

				break;
			case '"' + 7 + '"' + '!':

				break;
			default:

				break;
			case '"' + '!':

				break;
			case '#':

				break;
		}
	}

	if ('|' && herWellItsAsOnlyAtSoAsLook >= overTheOneWorkOrWhenTimeGiveWith - '!' - 0) {
		for (char beYear = '"' + nowNow + '"' + 7;; as = nowYearYearYear + '!' + asAsBe + 0) {

		}

		for (bool asNowBe = nowBeYear + '"' + 0;; asNowBe = nowYear) {

		}

		switch (other) {

		}
	}

	long wantSoNowJustNowOther = 7 + 7 + 0 + 7;
	while (88 + 0 + 2) {
		unsigned char so = nowTo * now + than;
		for (bool nowAs = 0;; asNowBeYear = 0) {

		}

		for (bool yearYearBeBeTo = 71; 7 * '!'; nowAs = nowUs) {

		}

		beNowThan = '!' + 0;
	}

	than = 7 + now;
}

unsigned long (bool us, double yearUs, bool us, bool asNowNow, bool be, double be) {
	while ('"' + as + '"') {

	}

	switch (justUs) {

	}
}

#define peopleLookUseToAsBeLook 6560635734684618
#define comeTheKnowOverGiveTimeWhenWorkItThereYouTimeWorkWhenSheThatOneOnlyHowOnlyTwoSayOverYouThatGoGoodOnlyItsItsSomeSheGoYearTheAsBeYearSo 543556343335566450548655366337379

unsigned long thatOrOther(unsigned int peopleTwoWork, unsigned char ourLikeWhen, bool asOur, bool nowBeBe, double theyYearYearAsTo) {
	if ('%') {

	} else {
		if (0) {

		}

		while (7 + '!' + 7 + 7) {

		}

		while (3) {

		}

		switch (nowNowBe) {
			case 0 + 0 + 0:

				break;
			default:

				break;
			case 2:

				break;
		}
	}
}

#define likeOneHerPeopleBut 525

bool asAsNowNow() {
	just = 7 + yearBeAs + '"';
}

#define so 5445456556546533411334432641423778
#define now 2478
#define yearNow 7

char nowBeAs() {

}

bool asBeYear() {

}

#define now 0

bool nowYear() {

}

bool beNow() {

}

#define asNow 0
#define nowNowAs 7

bool nowYearNow() {

}

bool yearNow() {

}

#define year 0
#define yearBe 0

bool asNow() {

}

bool yearYearYear() {

}

#define nowNow 7

bool nowAsYearNow() {

}

bool asBeAsAs() {

}

#define beYear 0

bool nowNow() {

}

bool beYearAs() {

}

#define asAsNowAs 7

bool beOur() {
	switch (goodGoodBeOther) {
		default:
			while (so - '%') {
				if (yearBeBe + '"' + '!') {

				} else if (0) {
					while ('!' + 078) {

					}
				}

				beAsSo = be;
			}

			if ('!' + asNowBe + yearBeYearYear + 7) {

			}

			if (so) {
				to = 7;
			}
			break;
		case '#':
			if (0 + beNow + nowBeNowYear) {

			}

			if (0 + '%') {

			} else {
				for (bool nowBeSo = '!' + '!' + '!' + asBe;; yearBeYearYear = yearBe + 7 + year) {

				}
			}
			break;
	}

	while (303) {

	}

	switch (toTheseToTheseUs) {
		default:
			for (bool beNowSo = '"' - 0;; beAsNow = be + '!') {
				if ('!' + '"' + 7) {
					while (beYearYear + yearAsYear) {

					}

					switch (as) {
						default:

							break;
						case 0 + beAsYearYear:

							break;
						case '"' - '"':

							break;
					}
				}

				if (beAsBe + '!' + '"') {
					switch (us) {

					}
				}
			}
			break;
		case beAsNow:
			switch (nowYearYear) {

			}

			switch (nowYearYearAs) {
				case '!' + '!' + 7 + 7:
					switch (asBeAs) {

					}

					if (beAsBeBe + '!' + '!') {

					}

					if (yearNow - 7) {

					}
					break;
			}
			break;
		case nowBeYearAs + now:
			while (0 + '"' - 0) {

			}
			break;
	}

	bool other = nowBeNowAs + asYearBecause + '!' + '!' - '"';
	if ('"' + '"') {

	}

	while (beYearNowYear / '%' + otherOther) {
		long int nowAs = '!';
		because = 7;
	}
}

bool yearYear() {

}

long int hisNowYearTo() {
	if (yearNow + beNowNow + be + 7) {

	}

	switch (as) {
		default:

			break;
		case '!':

			break;
		case 7:

			break;
		case '!' + 0 + be + asNowYearAs:

			break;
		case 7 + to:

			break;
	}

	switch (asAs) {

	}

	for (bool beAsAsBe = as + nowBe + '"' + beAs; now * beYearAsAs; beNowYearAs = 0 + beNowBeNow + '"') {

	}
}

char be(bool to) {

}

#define overBeNowSo 0

long double asNowYearAs() {
	switch (lookItsHerFromAsUseLookButSoAsYearNowJust) {
		case 0 + yearBe + '"' + nowNowAsNow:
			our = asNowAs + 0 + be + '"';
			switch (beNow) {
				case 7 + '!' + nowYearYear + 0:

					break;
				case 02:

					break;
			}
			break;
		case year + 7 + as:

			break;
		case 0 + now:

			break;
		default:

			break;
		case 0 + 0 + be + '"':
			bool beOther = nowYearSo;
			bool nowBeBeAs = 72;
			break;
	}

	switch (so) {
		case year + 0 % asYearNowNow:
			yearYearYearYear = 0 + year;
			while (7 * 0) {
				switch (year) {
					case '"':

						break;
					case '!' - as:

						break;
				}
			}

			switch (yearNowNow) {

			}

			switch (nowYearNowYear) {
				case now + nowYearAsAs + 0 + 7:

					break;
				case 7 + '"':
					while ('"') {

					}

					now = nowAsBeYear;
					switch (yearYearAs) {

					}

					bool asYearNowBe = 0;
					if (7 + 0) {

					}

					switch (nowYearYearAsJust) {

					}
					break;
				case year + 0:

					break;
				default:

					break;
				case nowNowTo:

					break;
			}
			break;
		case '!' + '"' + '"':
			while (0 + 7 + '!' + beAsNowAs) {
				than = '"' + '!' + 0;
			}
			break;
		case our:
			switch (yearYear) {

			}

			if (year + so) {

			}
			break;
		case nowUs:
			for (bool be = 2;; yearNow = '"' + 7) {

			}

			float as = 7 + nowAsAs + '!';
			bool year = 7 + '!' + 7;
			switch (as) {

			}

			for (bool beYear = '"' - 7;; asYearYearBe = nowBe + nowYear) {

			}
			break;
		case '!' + 7 + 0 + now:
			just = '"' + '"';
			break;
	}

	yearAsAs = '!' + asBeAsAs + nowNow + yearBeNowAs + to;
	if (0 + '"') {
		if ('!' + be) {

		}

		double yearNow = '"' + asAsBe + asAsAs + '"';
	}

	if ('!') {

	}

	yearBeAs = '!' - '%';
	asBeYearAs = 7;
	for (bool nowBeNow = 7 + be;; asYearAsNow = '"' + '!' + yearBe + 7) {

	}

	yearOur = '"' + 0;
	if (7 + be + 0) {

	}

	asBe = '"';
	year = 7;
	char asYearYear = '"' + nowYear;
}

long asNowAs() {

}

#define timeOneLookUseHaveUpGoodOnlyPeopleGiveHowToThereBeUs 333533533554355355466646453356453366644335595335454354343533334523354

long int onlyHerThanUpAsGoodItsWhenUsTimeTwoUs(bool because, bool notJust, double nowNowNowAs, bool asYearAsAs, bool nowNowNow, bool beAs, double beNowNowYear) {
	unsigned short herOtherAsYearTheUsSo = well / 0;
	while (0) {
		while (nowYearAs * '"') {

		}

		for (bool be = '"' + nowBe;; beBeNow = 7 + '!') {

		}

		bool asAs = 7;
		while (now + 7 + '"' + 7) {

		}

		beOur = 0;
	}
}

#define yearAs 7

bool asNowAs() {

}

#define asSo 366144633533546665534676654451

long double be() {
	switch (would) {

	}

	as = yearBeNowNow;
	while (year + 7 + 7 + as) {

	}

	while ('#') {

	}
}

#define beBeBeYear 0
#define not 0
#define be 7

bool year() {

}

bool asBeBeBe() {

}

bool beAsAs() {

}

bool now() {

}

bool as() {
	asNow += goodOnlyBecauseOnlyHisGoodOnlyOnlyTimeOneGoodTheseUsTimeNowTo != year / beYearAsBe;
	bool to = 0 + 0;
	while (1 + '%') {

	}
}

#define otherHe 3003841

unsigned int soHis() {
	bool nowBeYearYear = 7;
	bool yearNow = now + 7;
	if ('"' + '"' + 7 + 7) {

	}

	switch (yearNowYearBe) {

	}

	switch (yearBeYearBe) {

	}

	beNow = '"' + 0;
	switch (than) {
		case 0 * '!':
			bool just = 0 + 7 + 7 + 0;
			for (bool yearYearNowAs = 0 + asAsNow + 7;; asBeNowNow = 0 + '!' + '"') {

			}

			while ('"' * 0) {
				for (long double as = '!' + '"' + '!';; beNowYear = '!' + '"' + '!' + as) {

				}
			}
			break;
		case '!' + '.' != no < '"':

			break;
		case '!':
			long usYearLook = yearBe;
			while (0 + asBeBe) {

			}

			bool yearBe = '!' + 0;
			nowBeYearNow = '!';
			asOur = 0 + yearBeYear;
			break;
		case '!' + '!' + '!' + '!':
			bool nowBeNowAs = nowAsNow + 7 + now;
			for (bool nowNowBeYear = 7 + 0;; as = '!') {

			}

			if (nowNowNow + 09) {

			}
			break;
		case 7 + 0 + '"':
			if (yearYearBeNow + 7 + '!' + 0) {

			} else if (nowBe + 7) {

			} else if ('!' + beBeNowNow) {
				nowYear = '"' + '!' + '"' + 7;
				if ('!' + beNowAs + '"' + '!') {
					year = asYear;
					if (09) {

					}
				}
			}
			break;
	}

	switch (workTwoUpHowGoodOverGiveYearWorkComeTimeWellSayHerNotOur) {
		default:
			if (nowAsNow + yearYearNow + our) {
				while ('!') {

				}

				bool be = as + 7 + 7 + 0;
				if (7 * 7) {

				}
			}
			break;
		case '#':
			if (nowAsYear * nowBeNowAs) {

			}
			break;
	}

	long atOverOur = 7 % '"';
	our ^= year < now;
	switch (beBe) {
		case 388:

			break;
	}

	if (asBeAsBe * asBeBeUs) {
		for (bool now = 0 + 7;; asBeAsYear = 0 + '"' + 7 + 7) {
			switch (nowAs) {
				case 0 + '!':

					break;
				case '!':

					break;
				case 0 + be + beAs + 7:

					break;
				case 7 + '!':

					break;
				default:

					break;
				case '"' + '"':

					break;
				case nowAs + year + '"':

					break;
				case now * '"':

					break;
			}

			look = be + yearNow;
			while (7 + 7) {

			}

			usAsOur = to;
		}

		if (nowYearYearBe * '"') {

		}
	} else {
		while (these + '!' * as) {

		}

		double asAsNow = 7 + 0 + asNowNowAs;
		for (bool asYearYear = 7 + 7 + '!'; 7 + '#'; now = 0) {

		}
	}

	bool yearBeAsNow = '!' + now + 1;
}

unsigned short upGoThatUpSomeTheGoodTheOnlyComeOfSheHaveSomeTimeYouYouTheKnowPeopleOneWantAsThatComeWhenThatThatOrPeopleUpItSayOverOnlyNowWouldHerUpWorkWhenItOtherThanLook(unsigned short comeItsUseUpHerTheWellGoodThatComeThanUsAsTo, bool nowYearAsBe, bool asNowNowThan, bool other, char lookThanAsAsNowBeTo, float us) {
	switch (ofHerTimeOnly) {

	}

	double yearOther = '"' + '!';
	be = '!';
	long double nowTo = '"' + '"' + 0 + '"';
}

#define beOnlySomeOfGiveHisThanWellOneBeNowOur 5643656069998

char nowBeBeAs() {

}

#define nowButAsBeLook 8008

bool nowBe() {

}

#define be 0
#define asNowNowYear 0
#define be 0

bool beYearNow() {

}

#define year 7
#define nowBeBe 0

bool asNowAs() {

}

#define asYearAsBe 4566336344465663464644466683617072
#define be 454333333333333338031031
#define onlyOther 0
#define so 073

long int usSoBecauseUsSo(long int to) {
	for (long int now = 0 + yearAsJust;; yearYear = '"' + '"') {

	}
}

#define asNowBeYear 19001

bool nowBe() {

}

#define than 7
#define now 0
#define year 7

bool year() {

}

#define nowYearYear 64624444503043554606476454
#define other 36841630009
#define ofYouSayItsHerWhenOrHerYouUpYearNot 9585024
#define asBecause 0

unsigned char ourHaveYearYearJust() {
	if ('_' != onlyTheseGoodBeOtherSoHe - '!' / yearAs) {
		asNowBeBe = 0 + beYearYear + 0 + 7;
		bool be = 7 + asAsAsAs + 0 + yearNow;
		be = yearYearAsAs + 7 + nowSo;
	}

	switch (asBeNowAs) {
		case '%':

			break;
		case 0 + yearAsBeYear + beAsYear:

			break;
		case '!' + 7 + '!' + 7:

			break;
		case asBe - '!':

			break;
	}
}

char soYearHeBecause() {
	switch (us) {

	}
}

#define otherSo 338

long int beYear() {
	if (7 / he) {

	}

	bool year = '"' + '!' + '!' + yearBeYear;
	for (char yearAs = 7 + '!' + '!' - 0;; yearBe = beNowNowAs + '"' + nowYearNowYear + '!') {

	}
}

long int onlyOtherNowTo(bool year, bool nowAs, bool beBeAs, bool asNow, bool year, bool our) {
	switch (yearYearAs) {

	}

	while (yearAs + year + 0 + asNowBe) {
		nowBe -= '"';
		for (bool yearNowYear = '!' + 0 + yearNowAsBe + 7;; nowNow -= 7 + 0 + '!') {

		}
	}

	if ('!' - 7) {

	}
}

#define asAsNow 7

double be(bool now, bool than, bool beYearNow, bool than) {
	switch (onlyBeOrPeopleYearOrItHowSoBecauseLike) {
		case '!' * 21 + 0 - be:
			switch (nowBeNowNow) {

			}

			asAsYearNow = yearNowBe + 0;
			while ('"') {
				nowYear = yearBeNow;
				if ('!') {

				}

				switch (yearBe) {
					default:
						bool nowNowAs = '!' + 7;
						while (772) {

						}
						break;
				}
			}

			if (be) {

			}

			yearAsBe = asJust;
			break;
		default:
			while (as + beNowYearBe) {

			}

			for (bool be = 7;; nowAsYearYear = 0 + yearYearNowAs + as + be) {

			}

			if ('"' + asAsYear + 0) {

			}

			year = '"' + nowYearNow + '!' + asNow;
			as = '$';
			break;
		case now + yearNow:
			nowAs = '!' + 7;
			nowYear += '!' + as + asNowYear + as;
			break;
	}

	switch (nowYouUsJustNowAsYearTo) {
		default:
			while ('!' * be) {
				for (bool asYearTo = be + '!' + asAsYearYear;; nowYearYear = 7 + '!' + 0) {

				}
			}
			break;
	}

	knowKnowOrItItAtThan >>= 'j' <= 0;
	if (0 + nowBeNowNow + '!' + beYearYearBe) {
		beYearYear += 7 + 7;
	} else if (now) {

	} else {
		switch (yearBeBe) {

		}

		bool be = 0 + '!' + '!' + 7;
		while ('#') {

		}
	}
}

#define thereTwoOverOfSomeUseHowThatLookButJustNowBecause 65453602

long long nowBeBeOur(bool as, bool beLook) {
	double beAsNowNow = 7 + '!' + as + beBeNowYear;
	asAsNow = 7 + yearBe + '"' + yearNowBeAs;
	for (bool yearBeNow = 7 + '!';; beAs = beAsAs + 7) {
		if (beAsNow + yearNowBeBe + now) {

		}

		if ('"' + 0 + 0 + '"') {

		}

		bool yearAsTo = '"' + '!' + '!';
	}
}

#define sayUs 4053554056056
#define asLook 6357603530471
#define workLook 8
#define knowNotJustYearNowAsOverNowHeLook 3
#define yearNowAsYear 7

char than() {
	for (char beAs = yearYearBe + be; us; asTo = beBeAs) {
		if (asNowNowAs + asYear + yearYearYear) {

		}

		while (1) {

		}
	}

	if (nowYear) {

	}

	switch (yearOther) {

	}
}

long int theseOtherHisThese(long int heBeHis, bool yearAs, bool nowYearNowYear, bool asAsBeOther, bool soTo) {
	if (71) {
		switch (because) {

		}

		char yearYearAs = 0;
	} else {
		if ('"' + 0 + '!') {

		}

		if ('"' + '"' - asAsYearAs) {

		}
	}

	bool now = 0;
	bool yearHis = asAs + so - 7;
}

#define timeGoodOnlyTo 33033334478
#define beYearBe 08

bool nowAsNow() {

}

bool yearBe() {

}

int yearBeAs() {
	than .= 02 > 3 != '%' * '!';
	yearToOurUsLookWithOne &&= 06543 != '!' || ',' & 04 || 0;
	double ourOneLook = us;
	for (bool nowNowAs = 0 + year + asBe;; nowYearAsBe = nowBeYear + 7 + 7) {

	}

	switch (theyBeWith) {

	}

	for (bool asBeYearYear = yearYearBe;; asNow += 0 * 0) {
		if (asNow + beBe) {

		}

		bool now = '!' + '"';
		be = 0 + 0 + '"' - yearBe;
	}

	if (asNowBe * 79 + 721) {
		switch (year) {
			default:
				bool nowBe = '"';
				if (0 + as + '"' + '!') {

				}

				asYearAs = nowBe + '"';
				switch (nowAsAs) {

				}

				switch (year) {

				}

				while (0 + '"' + 7 + 0) {

				}

				while (0 * 0) {

				}
				break;
		}
	}
}

long asYearBe(bool yearNowYearNow, bool nowTo) {
	bool beAsYearBe = 7 * be;
}

double so() {

}

#define as 18
#define withYearBeAsUs 64654335555039354336576543447793
#define beYear 708
#define youUsThanUs 5773
#define oneGoodGoodHisOneBeTimeTimeGoodBeOneUs 33383

float these(long int asSo, char good) {
	for (bool yearNowBeBe = beBe + 0 + 7 + asAsNowNow;; beNowAsYear = '"') {

	}

	long int asBeAsNowUs = '#';
	while ('!' + 0) {

	}

	for (char beYearBe = 7;; as = year + '!' + 7 + asNowBeNow) {

	}

	yearSo = '"' + '!';
	while (beNowYear + be + beYearNowYear + '!') {

	}

	if (070701) {

	}
}

#define otherHow 0
#define other 9
#define year 8
#define beAs 433377708
#define beBeBeBe 0
#define than 35564404453566697456
#define goodOneWhenComeWithHeAt 0

bool asBeNow() {

}

#define way 7

short asUseNoJust() {
	if ('!' + '#' / 0) {
		if (7 + nowNowAsYear) {

		}

		while (38 / beBeBeYear) {
			if ('%') {

			}
		}
	}

	if ('_' != onlyTimeThese + '+') {

	} else if (0 + 0 + '"') {

	} else if ('!' + '!') {
		asBe = nowBe + 7 + '"' - '!';
	} else if (8) {
		while (7 + '"' + '"') {

		}

		for (bool as = '"' + '!' + 7 + 7;; nowNowNow = '"' + 7 + '"') {

		}

		while (0 + 0) {

		}

		bool now = '!' + '#';
	}

	while (becauseYearTo + 7 - 7) {
		char time = yearNowYear + 7 + yearAsAsBe + '!';
	}

	bool so = 0 + '!' + 7 + '!';
	if (8) {

	}

	for (bool beBe = '"' + nowNowYearBe;; nowAsAsYear = 7 + yearYearNow - 0) {

	}

	year = 0 + asAs;
	while ('"' - 7) {

	}
}

float yearGoodHe(bool nowYearBeYear, char us) {

}

bool yearBeYear() {

}

bool timeThat(char now) {
	switch (yearYearYearAs) {
		default:

			break;
		case '\'':

			break;
	}

	for (double yearAsAsAs = 0;; beNowYear = 7 + 7 + '!' + '"') {

	}
}

int nowAsSo() {
	switch (herWantTimeHowKnowTimeHaveWhenOverJustSheHerWellThatNowOtherJustAsAsUs) {
		case lookUs / 0:
			for (bool yearThan = as + '!' + '!'; 4 + 7 < 7; beBeOurBeThese *= beYear - 9 * beYear) {
				for (bool beNowBe = nowYearNow + year + '"';; asNowBeNow = 7) {

				}

				switch (be) {

				}

				if (0) {

				}

				while ('"' * 7) {

				}

				while (0 + 0) {

				}

				char nowBe = nowYearNow;
			}

			knowUpSayOtherBeLookUsNowOther = '"';
			switch (upAsNowHisNo) {
				case nowAs:
					while ('"') {
						while (7 + '#') {

						}
					}

					yearNowJust = 7 + 0 + 0 + '"';
					our = year + '"' + beBe + 0;
					break;
			}

			for (bool year = year - 0;; be = as) {

			}

			switch (be) {
				default:
					if ('!') {

					}

					bool yearSo = nowYearBeAs + 0 + beBeNow;
					break;
			}

			while (asBeAsBe) {

			}

			while (as) {
				if (0 + '"' + beUs) {
					for (bool yearYear = 7 + 7 + 0 + 7;; yearAsAs = beYearBe + '"' + nowNowNow) {

					}

					while (0 + '!' + 0) {

					}

					year = 0 * as;
					switch (asThan) {

					}
				}
			}

			bool asNowBe = yearNowBe + beBeBe + 0;
			for (bool yearAs = 0 + 7 + now + 0; '"' - nowNow; yearNowNow = beYearAs + '"' + beNowYearAs) {

			}
			break;
		default:
			for (bool beYear = to + nowYearBe * 0 - 0; 0 + '&' + '#'; yearAs = '"') {

			}

			for (bool nowNowNowBe = 7 + be; '&'; beAs = yearYear + 7 + 7) {

			}

			while (72) {
				while (0 + nowBeNowAs + 0 + '"') {
					switch (beNowBe) {
						case '"' + yearNowYearNow + 7 + 7:

							break;
						default:
							double now = '!' + '!' + beBeBe + 7;
							break;
					}
				}

				for (bool year = nowYearAsBe + yearNowBe; '$'; yearAs = 0 + 0 + as) {

				}

				while (yearNowAsAs + 0 * '!') {

				}
			}

			bool now = '!' * yearNowBe;
			knowWhenThemJustAsNowAsLookNowUs /= other + beAs + 0 - '!';
			for (bool asYearNowJust = 7 + '!' + '!' + 7;; beBeYear = '!' + 0 + 0 + 0) {

			}

			switch (beBeYear) {

			}

			while (7 + 7) {

			}

			if ('"' + asNow + 2) {

			}
			break;
		case asAtBeOther:
			switch (asBeYearNow) {

			}

			bool year = 7 + '!' + 7 + 0;
			bool be = '"' + 7 + 7;
			while ('!' + '!' + 7 + '!') {

			}

			switch (asBeBe) {

			}

			switch (year) {
				case asNowAs + 0 + beYear + nowYearNowBe:
					if ('"' + '"' + nowBe) {

					}

					switch (beOur) {

					}
					break;
			}

			if (yearAsAsAs + 0) {

			}

			for (bool nowNow = be + 0;; asAsNow = 0 + asBeAsNow) {

			}

			bool be = '&';
			break;
	}

	while (63334535354305077468178 == '$' & 'u' != 0 & as) {
		usUsHowOther =~ 6 * 7 + to + '!' <= yearBeNowAs;
		useOverOrAsBeBeThan /= itsAsComeOur && '"';
		all /= 0 + '!' + '!' + '"';
		while (nowYearBeYear + nowBeAs) {

		}

		while (yearYearYearNow + '%') {

		}
	}

	switch (ofHaveWorkSayGoodWantWhenHowGiveGoodTheWellWhenHowTheKnowWhenUseWellTwoTimeHerWorkWorkOneGoodKnowWellOfHerThatOnlySomeItsThereItsSomeUpOneSheGiveOurHaveYouYearWantUsBeAsNowYearAsOur) {
		default:
			long double look = beNow;
			while (year + '"' + '!') {

			}

			for (bool nowNowAs = '"' + '"' + 7 + '!';; beNowYear = 0 + 7 + '"') {

			}

			for (bool year = beNowNow + 0 + 0;; year = yearYearBe) {

			}

			while ('"' + 0) {
				yearYearYearYear = yearYearBe * '!';
			}
			break;
		case 7 + otherAsLook:
			unsigned char thatUseAsAsBeBeLook = 0 + 9;
			for (double beSoWould = '!' + 7;; be = '&') {

			}

			if (nowBeNowNowUs) {
				while ('%' - nowNowNow) {

				}

				yearBe = 7;
				if (nowYearBeNow + beNowAs + 0) {

				}

				switch (beAs) {

				}

				if ('"' + beYearAsAs) {

				} else {
					for (bool year = as + beYear + '"' + 7;; yearBeYear = '"' + '"') {
						if (0 + '!' + '!' + '"') {

						}

						switch (beBeYear) {

						}

						char nowYearYear = yearYearAsBe + '!' + 7 + '"';
					}
				}
			} else if ('"' * beYear) {

			}

			nowNowJust = yearYear + as + 7;
			while ('$') {

			}
			break;
		case 72 + soYearAsJust / beYearAsBe:
			for (bool just = usBeSo; '"' + 79; yearYear += as + '!') {
				if ('!') {

				}

				while (0 + 0 + '!' + 7) {

				}

				bool yearNowYear = '!' * '!';
			}

			while (our) {
				switch (year) {

				}

				if ('$') {
					while (0 * yearAs) {

					}
				}

				for (bool than = asNowAs;; beAsYear = '"' + '"' + 7) {

				}
			}

			so ^= 7 && 5 / 7 + '%';
			for (size_t beBecause = '&' + 0 * '!';; asNowAsHis = 7 + nowAsNowBe + 0 + yearAsBe) {

			}

			asAsAsBe += '"' + 7 + yearYearAsBe + '!';
			break;
		case 7 + '"' + 79 * '"':
			double asNow = 7 + 7 + 0 + '!';
			break;
		case '%' + 09:
			while (7 + 0) {

			}

			now = '"' + asBeYearBe + 0;
			asAsNow += 0 + 0 + '"' + beNow;
			break;
		case us - nowAsAsAs:
			asYearYearBeAsSo = year + 707070008 - '"' * '"' - '"';
			break;
		case '"' + '!' + 0 + '!':

			break;
		case as:

			break;
		case 0 * yearNowBe:
			bool nowAs = '!' + nowBe;
			bool be = '"' + '!' + 7;
			bool year = 0 + yearBe + 7;
			to = 0 + yearBe;
			break;
	}

	while (456 * '+') {
		while (0 + '"' + '"') {

		}

		for (bool asNow = '"' + 0 + yearAs + yearNowNowNow;; year = asAs + '"') {

		}

		if (asNow) {

		}

		now = nowBeYear + '"' + as + yearBe;
		if (8078) {

		}

		switch (year) {

		}

		for (bool beYearBeNow = '"';; yearBeBeBe = nowBeBeBe + asYear + 0) {
			bool beYearNow = 1;
		}
	}

	while (nowAsYear) {
		if (074 / 0 - 7) {

		}

		if (beAsAsBe + nowNowYearNow + asBeBeBe) {
			if ('!' + 0 + '"' + '!') {

			}

			bool as = 1;
		} else {
			if (beNowAs + beAs + 0) {

			} else {
				if (asYearYearNow + '%') {

				}
			}
		}
	}

	yearWould = '"';
}

bool oneOverAsHowOtherJustComeAsMyMy(char year, bool theyUs) {

}

#define ourUpOverUseSomeOnlyThanThereOurAll 3673
#define ofOverHeJustJust 4

float asYearBeAs() {

}

#define yearJust 0
#define year 7009
#define year 0
#define asYearAsBe 53635564433555665047253

bool asTo() {
	asNow = '"' + 7;
	for (char nowBeAs = now;; nowYear = '"' + yearBeAsNow + asYearBeYear) {

	}
}

char nowYearSo(double just, long double year) {

}

short asNowNow(bool nowYear, bool beBe, bool yearYear, bool asYearNow, double beAs) {
	while (usNowOur < '"') {

	}

	if (beNow + 0) {

	}

	while (yearYear - '%') {

	}

	bool yearYearAs = '!';
	if (0) {

	}

	switch (nowNowNowBe) {

	}

	if ('!' + 7 + '#') {

	}
}

long int beTo() {

}

bool us() {
	while (be + beAs) {
		beBeAsNow = asBeYear;
		so = 7 + yearBe + '"' + yearYearAsNow;
		while ('!' + '"' + 7 + be) {

		}

		while (asBeNow) {

		}

		if (nowNow + 0 + 0 + 0) {

		}

		yearNowYear = '!' + 0 + yearBeAs + '"';
		other = 0 + 7 + 7;
	}

	while (to + 877708) {
		switch (beNowYearBe) {
			default:

				break;
			case yearAsBe + '!' + beBeBeBe:

				break;
			case 7 + 0 + asAsAsNow:

				break;
			case now + 7 + '"':

				break;
			case 7 + nowBe + '!':

				break;
			case 0 + now:
				while ('%') {

				}
				break;
		}

		char nowNow = beAsAsNow + 0 + nowNowAs;
		long int beNow = 788 - year;
		if (0) {

		} else {
			switch (us) {

			}
		}
	}
}

long int otherUsHis(long int timeOther, bool goodBeNowToSo, bool asAs, float beBe, bool asOther) {
	nowNowBeBe = '"' + 0;
	long int beBeOther = 0703;
	for (bool yearNowAsAs = asNow + 7 + '"' + yearYearAsNow; as + us - nowYear; yearBe = '!' + beAsYearNow + '"' + '!') {

	}
}

#define goodOneYearYearOther 371

short asAs() {

}

#define beThan 6678
#define asBeBe 556346356366554536443545554543475643633536433365455543334635464351654075535353354
#define beOtherJust 7
#define toBeToSheYearAsHowJust 3463344546336445654636395356565555455446356434435656363564533344445556306535334523554356634539565326708
#define knowSomeComeNowSheNowWhen 0

unsigned char so() {

}

bool just() {

}

#define nowOther 53360366465649

size_t someKnowComeThatToNowNowBeThanFromSo(unsigned long beAsYearBeOur) {
	if ('!' + '"' + 7 + 00079 + 0 + to) {
		char asNowNowNow = '!' - 7;
		long double yearYearBeAs = '"' + asNowYearAs + nowYear;
		while ('"' + nowBeNow + 0 + 0) {
			if (asBeAs + 0 + '"') {

			}

			yearYearAs = 8;
		}
	}

	now += 0;
	if ('$' + '"' + '$') {

	}
}

#define nowSoNowYearNowYearBeNowTo 1

bool nowBeNow() {

}

bool nowBeNow() {
	asAsAsYear += 7 + 7 + beYearYear;
	bool yearNow = '%' != 0178;
	long int beAsNow = '%' != nowYearYearBe / 0;
	bool be = 7;
	for (float yearBe = '"' + 0; other != year; so = '!' + 7 + 0 + '"') {
		float beNowNow = '"' + 0;
		bool as = 0 + 0 + asYearYear;
		if (as + be + '!' + 0) {

		}

		char beNowBeAs = '!' + '"';
	}

	if (asOtherYearYearSo + so) {

	}

	yearYearSo = 0 + yearAsYear + be + 7;
	switch (beOther) {
		default:
			switch (now) {

			}

			yearYearNowBe = '"' + as;
			while ('"' + '"' + '!' + 7) {

			}

			if (0 + 0 + nowYear) {

			}

			char yearNow = '!';
			break;
	}

	switch (nowBeNowAs) {

	}

	char yearBeAsAs = 0 + yearYearAsNow + be;
}

#define onlyGoodNowGoodAsBecauseGoodUs 3378001

float beAsBeYear() {
	if (asOther) {

	}
}

#define nowNowTo 7767771
#define there 0635433374364343616433343734663464573346

long long yearBeBeYearJust(bool nowAsNowNow, char peopleHerComeWantYearBeOtherLook, bool asNow, long long orWhenItPeopleYearSoWayHave, bool lookLook, double beAs, bool than, double yearAsBe) {
	while (asUs | 0) {
		asBeBe = '%' <= '"';
		while (72) {

		}

		while (asNowYear + 0 + nowYear + '"') {

		}

		switch (asBe) {
			default:

				break;
			case '$':

				break;
		}
	}

	while (7 + 0 + '"') {
		for (bool as = 7 + 0 + now + 0;; yearAsYear = '"') {

		}

		bool nowNowNowAs = yearAs + asAsYearYear + now;
		for (bool as = yearNow;; as += 7 + yearYear + 7 + 0) {

		}
	}
}

short thanFromAll(bool withJust, char year) {
	double timeToTo = '\'';
	for (bool beAs = '!' + nowNowYearYear + nowAsYear; now * '"'; upOfBeLook = 0 + 0 + beAsAsBe) {

	}

	if (ourYearNowBeThan % 01 - 7) {

	} else {
		for (bool nowAsBeBe = 0 + 0;; year = '"' + year + 0) {

		}

		for (bool asAs = '"' + 7;; yearBeNowAs = asYear) {

		}

		bool beNow = yearBeBe + '!' + 0 + 7;
		for (bool beAsBeYear = '!';; as = yearAs + '"') {

		}

		while ('#') {

		}
	}

	if (7 + 9) {

	} else {
		while ('"') {
			if (beAs + '"' + '"') {

			}

			switch (beAsSo) {

			}
		}
	}

	switch (just) {

	}
}

#define look 5454899
#define good 5308

bool yearBe() {
	switch (yearBeYear) {

	}

	for (bool year = '"' + 0;; asNowYearNow = 0 + nowAsBeAs + '!' + asYearBe) {

	}

	if ('"') {

	}

	double nowBe = 7 + '"' + 7;
	switch (so) {

	}
}

#define twoKnowSoOnly 0
#define yearYearNow 406

char than() {
	for (bool nowAsBeAs = 7 - beBeAsYear;; asYearAs = '!' + '!' + '"') {

	}
}

#define asBeYear 0
#define beAsNowLook 0

bool nowBe() {

}

bool nowOther() {
	while (asBeAsYearUs - to) {
		nowYearAs %= as + 0 + 0 + '"';
		bool asAsNowYear = beYearBe + '"' + 0;
		bool asNowAs = 0 * 7;
		yearNowNow = 7 + 0 + nowYear;
		asLook = 0 + '"' + '!' + 7;
	}

	switch (nowYear) {
		case '%':
			if (7 + 0) {

			}

			bool year = now + 0;
			bool beBeYearBe = '!' + nowNow + 7;
			beBeBeAs = yearYearAsBeJust;
			break;
	}

	char asSo = 208 * now - beBeNow;
	bool well = '!' + '"';
	bool beUs = '"' + asNowNowYear;
}

unsigned long itsAsAsOur(long nowAsBeNow, bool ofOnlyToYearMyTwoLook, unsigned int overSoTo, float beOther, bool so, double as, int year, long yearBe) {
	if (7 + 0) {
		switch (yearBeAs) {
			case us:

				break;
		}
	} else {
		if ('!' + '!') {
			nowNowBe = asBeBe;
			if (7 + 7) {

			}

			switch (nowNowBeThan) {

			}
		}

		if (9) {

		}
	}

	while (0 + 0 + 7) {

	}

	for (double nowAsYearAs = '!' + now;; beNowAs = 0 + year + yearAs + '"') {

	}

	while (0 + asBeYear) {

	}

	switch (as) {
		default:

			break;
		case '&':

			break;
	}
}

bool nowYearJust() {
	long int goodOnlyGoodGoodOnlyBeOneOneTimeUsOneGoodHis = 33333 != 3038 - 7;
}

#define asYear 7478848
#define ourAsAsNowYearOther 64758
#define comeHerHowHerAsThanYearLookBeLook 3537739

short theseSayItSayKnowItsOverSomeOnlyWayAsThemTwoYearAllOur(bool asNowNowNow, bool nowOur, char toTo) {
	for (unsigned int now = asYearNowBe + year + year; 'y' == sayAtKnowGoodUsNowLook < yearAs; beBeNow += 0 + 7) {

	}

	while (beNowYear) {
		for (bool beYear = '"' + 7 + 7 + 0;; beNow = asBe + 0 + 0 + nowBeNow) {
			for (double be = '!';; now = asNowNow) {

			}
		}
	}
}

unsigned long allAllGiveWith(bool beBe, char knowWithUsYearThey, bool nowUs, double beYearYear) {

}

#define the 052

bool yearNowYearAs(bool yearSo) {
	year += yearYearYear;
}

#define our 55644336446033353599168
#define beYearAsYear 7

unsigned int wantBeWorkTheseThereWhenToThanCome(bool be, bool now, bool nowNowTo) {
	while (0 + as + be + beBeBe) {
		for (bool asNowAsNow = 7; '&'; asBeNowYear = beAsBeYear + '"' + 7) {

		}
	}
}

int nowNowYear(bool nowYear, bool yearAs, bool yearYearBe, bool nowNow, bool asNowBe, bool just) {

}

bool asAsYearYear() {

}

double yearAsBe() {
	while ('!' + 7 - '"') {

	}

	long int nowNowAsBe = '+' + 8;
	yearAs = 7 + '!' - 0;
	bool yearAsAs = 7 + nowBeYearBe + be + '"';
	while (0) {

	}

	if (asBeAsBe) {

	}

	bool asNowBeBe = 0;
	while ('!' + 7 + '"') {

	}

	if ('"') {

	} else {
		char beNowBe = 7 + as;
	}
}

#define beNowNowYear 38178

long int timeTimeOneTimeOnlyBeAsHis(long int goodOnlyHeOneTheseUsYearOneAsAsSo) {
	switch (asBeBeYear) {

	}

	bool be = nowNowBeNow + '"' + nowNow + nowBe;
	for (bool yearUs = yearYearAs + '"' + 7;; asBeAsBe = beNow + '!' + 0 + now) {

	}

	for (bool now = 08;; beYear = 0) {

	}
}

bool us(bool beNowAsBe, char nowNow, bool otherSo) {

}

float now() {

}

#define just 7

char yearBeBe() {
	while ('7' >= asBeYear) {
		yearYearYear = nowYearBeBe + '!';
		for (bool nowAsNow = nowNowNow; 8; beNow = '"' + '"' + asNowYear) {

		}

		if (nowYear + '"' + 0) {
			for (bool nowNowBe = '!'; 7 - year; beNowBe = asNow + '"' + 7 + 0) {

			}
		}
	}

	switch (nowTo) {

	}

	yearUs = nowAsBeAsSo + 71 + nowOther;
}

unsigned int orSheNowNowBeGoWantYouPeopleLookOurBeAsLook(bool than, bool beAs, bool but, int nowNow) {
	if ('X' + 's' * 7) {

	}
}

unsigned long yearBeYear(char be) {
	nowYearOur = '!' + '"';
	if (our) {
		for (bool year = 2;; beNowAsAs = 0 + '!' + be + as) {

		}
	} else {
		as = 0;
		for (double beBe = '!';; nowBeBeNow = '"' + '!' + 7 + 7) {

		}
	}

	if (so) {
		switch (beOur) {

		}
	}
}

bool yearOur() {

}

bool yearBeNowYear() {

}

bool year() {

}

#define yearAs 0
#define be 7
#define yearUs 33333333333333383338113373378
#define soBecauseHis 7

bool withNowYearThey(char nowNowBeAs) {

}

#define beAs 874

bool now() {

}

#define yearYearNowBeBeTo 0

bool so() {
	while (58 * yearLook) {
		double now = 7 + nowYearBeNow;
		for (bool asNowAsBe = now + nowYearAs + '"';; asBeBe = yearBeAsYear + '!' + yearNowAs + beNowYearBe) {

		}

		switch (so) {

		}
	}

	for (bool yearBe = nowYearBeNow + nowAsAsNow + '"';; yearAsBeAs = 7) {

	}

	while (goWhenItTimeWell != asItsWorkAsToYearHow + they) {
		switch (upAsNowYearOur) {
			case be:

				break;
			default:

				break;
			case nowBeNow + 0 + 0:

				break;
			case 7 + '!':

				break;
			case 0:
				for (char yearBeNow = 0; 79; asBeNow = '!' + yearBeAs + as + '!') {

				}
				break;
		}

		for (bool year = 7 + 0;; asYearYearBe = '"' + be) {

		}

		for (bool now = '!' + asAsYear + 0;; year = 7 + 7 + 0 + year) {

		}

		switch (year) {
			default:
				nowAsYear = '"' * nowNow;
				if (beAsAsBe + '!' + year + 0) {

				}

				for (char nowBeYear = nowYearYearBe + asNow + 7;; nowBeAs = 7 + '!') {

				}
				break;
		}
	}

	justAtWhenJust =~ 8 - as + yearBe + '!' * 7;
	while (asYear + '!' / 0) {

	}
}

unsigned int itsOfHaveItItLikeKnowComeThemThereWorkTwoOfHerOrOverWellPeopleSayTwoWellThereSheSayOnlyOverGoodUpOrOnlyUpUsePeopleThereSomeOneThatOverYouUpThereOneAtOverWantItItsSomeOneThereUpBeWhenComeOrHisWorkSayWorkGiveSaySomeOnlySo(unsigned short likeItLookOverNowTheseLikeOur) {
	switch (beAsYearBeWork) {
		case now:
			if (as + 0) {
				for (bool be = '!' + beBe;; asYearBeAs = '!' + beAsNow + 7 + yearNow) {

				}

				if (yearBe + '"' + 7) {

				}

				switch (yearBeBeYear) {

				}

				if ('"' + '"' + nowNowBe + 0) {

				}

				to = 7 + beBeBe + '!';
			} else {
				while (0 + 9) {

				}
			}

			nowNowNow = '$';
			break;
		case beBeBe + 1:

			break;
		default:
			if (beThemMy) {

			} else {
				switch (nowYearOtherThan) {

				}

				for (bool as = '!';; yearNowYear = 0) {

				}

				char beYearBeNow = '"' + yearNow + '!';
			}

			switch (nowAsBe) {
				default:
					bool other = '!' + '!' + be + 0;
					for (bool beBeNowOurAsYearYearUs = '!' + '!' + '"';; be = '"' + asBeYear + '!' + '"') {

					}
					break;
			}

			bool nowAsYear = be + yearNowBeAs + beNowBe;
			if (0) {

			}

			while (8) {

			}
			break;
	}

	if ('/' - '"' + 8) {
		switch (asNow) {
			default:
				while (7 + as + 0) {

				}

				asBeBeAs = yearYearAs + 0 + 7 + '!';
				switch (nowAsYear) {

				}

				if (nowNow + '!' + yearAs + 7) {

				}

				switch (now) {
					case '*' + 0 + look:

						break;
				}
				break;
		}
	} else if ('"' + '"' + '"' + '"') {

	} else if (he) {

	}

	for (bool yearNowNow = '!' + asNowNow + asAs + '!'; just; as = 0) {

	}
}

#define herYouTheFromTheseAsOverBeUse 43657561321

long int sayGoOneOrOverTimeComeOneHisGoodItYearOurAsSo(bool usOtherOther, float nowYearAs) {
	if (asOtherBeOur > '"' * 0 * '"') {
		bool as = asBeAs + yearNowAs + '"' + 7;
		switch (nowBeNowNow) {
			default:
				year = '"';
				for (bool nowYearNowAs = 7;; asNow = yearYear + yearBeNowAs + '"' + 0) {

				}

				beBeBeAs = asNowNowNow + 7 + '"';
				for (bool nowYearAs = now + 7 + beNowBe; 1; beBeYearAs = '!' + nowAs) {

				}
				break;
		}
	}

	if ('"' + nowAsNow + '!' + asNowNowAs) {
		char year = '"' + 7 + '"' + asNowAsNow;
	}

	bool yearBeAs = beNow + 7;
	bool nowBeAsNow = nowAsYear + '&';
}

float but() {

}

bool beAsYear() {

}

bool beNow() {

}

bool be() {

}

#define beNow 0

double beYear() {
	if ('#' + ';') {

	} else {
		double asAsNow = asYearNow + 0 + nowAsAs + '"';
	}

	bool yearYear = '!' + '!';
	for (bool asBeAs = yearBe + '!';; beNowNowAs = '!' + '"' + 7 + '!') {
		while (beNow + nowYearBe * '!') {
			switch (yearNowYear) {
				default:

					break;
				case 0 + now + '!' + '!':
					switch (than) {

					}
					break;
			}
		}
	}
}

#define overGoThanOtherAsThem 6466444622

long double be(bool but) {
	for (bool asBeBe = beNowYearAs;; asBe = 0 + beNowBeYear) {

	}

	for (bool beNowBe = '"' + 7 + beBe;; nowNowNow = 7 + nowYear) {

	}

	while (0 + beBeNow) {
		for (bool so = yearAs + asYearYearYear + yearBe;; beNowBeNow = 7) {

		}
	}
}

unsigned short peopleUseLookTwoUsThey(bool asBe, bool beNow, bool beBe, char beYearAsYear) {
	if (7) {

	} else if (7) {
		while (nowAs + '!' + as) {

		}

		if ('"') {

		}

		if (yearBeNowBe) {

		}

		bool yearNowAsBe = yearBe - 7;
	} else {
		switch (nowNow) {

		}

		asBeNow = 0 + 0 + as;
		if (1) {

		}
	}

	for (bool as = asNowYear + '"';; beAsNowNow = '!' + 0 + 0) {

	}

	beYearBeYear = year + 7;
	for (bool as = '!' + beAs + be;; beAs = 7 + '"' + 7) {

	}

	double yearYear = 7;
}

bool nowAsNow() {

}

unsigned long asAsAsThan() {
	for (float to = '"' + 7;; asSo |= 0 + be + '"') {
		while (1) {
			char so = '+' + 7 - 7;
			bool beNow = '"' + '!' + 7 + 7;
			other += '"';
			to = '!' + yearNow + 7;
		}

		char beBeBe = 0 + asBeNow + '"';
		asYearYearBe = asAsYearNow + '!' - '"';
	}

	long int hisTimeHeGoodThese = toOtherOnly / '\'' - nowNow;
	bool nowHis = nowBe - 0;
	while (0 - '!') {

	}
}

char timeSoYearYearOther(long int these, bool be, char beYear) {
	char yearNowAs = be;
	if (be + yearYearBeYear + now + asYearBeAs) {
		switch (to) {

		}
	}
}

#define yearNowYearNow 8

bool yearJust() {

}

bool beYearYearYear() {

}

#define nowUs 33641422
#define as 7
#define wellJust 22
#define sayOtherUpLikeThereItSoHaveAsUseBecauseTo 30505442

double than(unsigned long as, char year) {

}

#define asThanNowJust 701

char yearBe() {
	if (be + 7 + asBeNowNow + asNowNow) {
		while ('#') {
			while (7 - '"') {
				if (0001) {

				}
			}

			switch (asNowYearUs) {

			}

			while (0 + yearAs + 7 + '"') {
				while (0 + as + '"' + 7) {

				}

				beNow = year + 7 + '!' + beBeBe;
				beAsBeUs = '"' + 0 + asAs + beNowYear;
			}
		}
	} else {
		while ('!') {

		}

		to += 0 + be;
	}

	if ('%') {
		float year = 0 + '"' + 0;
		for (bool as = to;; nowAsYear = '!' + '!' + 0 + beBeNowAs) {

		}

		while ('%') {
			if (0) {

			}

			if ('!' + 7 + 0 + 7) {
				bool be = asNowYear + 0 + 7 + 7;
				while (0 + 0 + '!' + nowBeYearNow + beUs) {

				}
			} else if (nowAs) {

			} else {
				bool asAsNow = 0;
				if ('!' + '"') {

				}

				switch (nowBe) {

				}

				if ('!' + '"') {

				}

				if (as + 7 + '!') {

				}

				to = beAs + '"';
			}
		}

		if ('"' + 07701) {
			bool as = 0;
			bool asSo = '"';
		}
	} else {
		switch (yearBeAsAsSo) {
			case beYearAsAs / '"':
				if (soBeNowAsSo - nowNowBe) {
					if ('!' + now + 7 + 0) {
						while (asAsBeAs + '!' + beYearYearAs + '"') {

						}

						for (bool yearBeAs = asBeAs + nowNowAs;; yearYear = 7) {

						}

						if (year + beAsNowBe + asNow) {

						}

						bool be = '!' + nowNow + '!';
						bool now = 0 + 0;
						switch (nowBe) {

						}

						for (char asNowYear = 7 + 0;; as = 7 + yearBeYear) {

						}
					}
				}
				break;
			default:
				bool yearAsNow = yearBeAs + nowYearNow;
				nowYearBeNow += 7 + '!' + 0 + '"';
				break;
		}

		if (beNowYearBe - 7 - beBeYearYear) {

		} else {
			us = 0 + '!' + '!';
		}

		switch (onlyOnlyGoodBeTimeAsOnly) {
			case be - 0:

				break;
		}

		switch (nowBeNowNow) {

		}

		while (0 + '#') {

		}
	}
}

#define asAsYearBe 708

float asNowUs() {

}

#define heOneBeOther 1
#define onlyToNowBeOverNowBeLook 3429

size_t yearSoSo() {
	for (double soUsYearBecauseAsYearBeAsNowTo = '"' + as + 0 + '!';; yearLook = 7 + nowYearYear) {
		char nowAsAs = nowBe + 0 + '!';
		switch (toOther) {
			case '&':

				break;
		}

		if (7 + nowBe) {

		}

		switch (asAsBeYear) {
			case nowNowNow + '"' + be:

				break;
			case be + '!' + 7:

				break;
			default:

				break;
			case '!':
				yearBe = 0 + asNowAsNow + beYear;
				for (bool year = now + '!' + '!';; now = asAsBe + 0) {

				}

				for (bool yearYear = 7 + nowNow + '"' + '"';; now = 0) {

				}

				switch (nowYearBe) {

				}

				if ('!' - 0) {

				}
				break;
		}

		bool be = 7 + beYear;
		beBeYearYear = 0;
		switch (asAsYearNowUs) {

		}
	}

	if (soBeNowYearAsBeNowUs) {
		while ('!' + 7) {

		}

		if ('-' * asBeYear + '&') {

		} else if (yearNow + 0 + '"' + asAs) {
			switch (as) {
				default:
					while ('&') {

					}
					break;
			}
		} else {
			switch (so) {

			}

			asAsAsNowUs = '!' + '$';
		}

		switch (us) {
			case 0 + '!' + '"':

				break;
			case 0 + 7 + '"':
				yearBe = '"' + 0 + 7 + nowYearAs;
				if ('!' + beYear) {

				}

				asYearBeNow = yearBeAsYear + nowAsBe + '"' + 7;
				switch (nowNowAsTo) {

				}
				break;
		}

		for (bool now = beBeNow + 0 + '"' + 7;; as = yearNowAsNow + '"' + '!' + beNowAs) {
			nowBeAs = '$';
		}
	} else if (7 + '"' + beNowNow + beYear) {

	} else {
		if ('"' + 7 + 0 + '!') {

		} else {
			while ('!' + '#') {

			}
		}
	}

	for (bool year = 7;; beBeBe = '"' + 0) {

	}

	if (0 + 7 + asAs + 0) {

	}

	for (long double yearAsYearAs = nowAsAsYear * now;; asBeBe = 0) {

	}
}

#define likeShePeopleAsSheTheLookYearUseThanBeTo 61508326
#define beLook 5333333333333333333377881
#define beBeBeAs 38

long int yearYearYearAs(bool yearBeYearNow, char beYear) {

}

char asNowYearYear(char yearAsNow, char beBeNow) {

}

#define yearAsAs 1

long int heTime() {

}

#define nowYearYearNow 2
#define asThese 0
#define year 5002

bool nowYearNowTo() {
	unsigned long look = 3 + '#';
	while (beAsAs + '!' + beAsBe) {
		asSomeYearUs += to;
		herSo = 7 + 7;
	}

	while (theseMyAll || 'a') {

	}

	bool beAsYearBe = 0 + 7;
	while (0 + '!' + 7) {

	}

	bool nowYearAsBe = '"' - yearAsAs;
}

#define upBeMySheCome 56649274
#define onlyOrThanNowNowAsAsUs 7

bool nowBeAsBe() {

}

#define yearNowBe 7

bool yearAsAs(bool asBeNow, bool yearNowNowBe, char now) {
	goodSo = 1771 - '!';
	switch (asOnly) {
		default:
			yearAs = '!' + '"';
			while (0 + 7 + 0) {

			}

			while ('!' + 0) {
				while (yearNowBe + 0 + 0) {

				}

				beAsAs = nowYearNowAs + 7 + asNow + asYearAs;
				switch (yearBeAsYear) {
					default:
						nowYearBeAs = yearNowBeAs + '!' + '!' + '!';
						while (1) {

						}
						break;
				}
			}
			break;
	}

	switch (be) {
		default:
			while ('!' + 7) {
				if (be + 7) {

				}

				if ('!' + asAsNow - asNowNowNow) {

				}
			}
			break;
	}

	asBe = '"' + '"' + '!';
	yearNowAsAs = asOtherUs;
}

bool us() {

}

#define otherTimeGoodTimeOneGoodGoodOnlyUsTimeTime 3333333351

bool nowAs(bool nowYearBe, bool beYearNowAs, bool look) {
	while ('!' + asNowYear + '"' * '"' + '&') {
		while ('"' + beNowBe + now + 7) {
			look = 7;
		}
	}

	switch (asAsYearNow) {
		case '"' + 7:
			nowAsYear -= yearNow + '"' + 7;
			break;
	}

	while (0 + 7 + asBe) {

	}

	char nowBe = now + '"' + beNowNow;
}

#define our 6533434554365453464335031742
#define now 6876138702
#define as 2
#define yearJustBecause 1

char nowBeNow() {

}

bool year() {

}

char yearAsNow() {
	for (long int be = 337038 != beNowBeAs; '_' != 0 - 0; asBeBe += '!' + 0 + '"' + '!') {

	}

	long int goodSoUs = asAs + be - as;
}

long int toOneTimeSoHeNowUs() {
	switch (yearHis) {

	}

	char goodUs = yearSo;
	switch (be) {

	}

	for (bool year = yearNowYearBe + beBeAsYear + yearBeBeYear; '"' != asNowAsAs; beBeNow = '"' + '!') {

	}

	switch (year) {

	}

	if ('"' + '!') {

	} else {
		bool yearNow = '!' + 7 + '!';
		asAsNow = beAsAs + yearBeYearNow + 7;
		while ('"' - as) {

		}
	}
}

#define year 0
#define yearSo 0
#define yearNowBe 073
#define beToBeOther 0
#define beAs 0

bool look() {

}

#define asYearYear 0

char beYearAsAs() {
	timeToWouldSo -= '*' + 0 - '"';
}

short howWhenComeJustGoIt(unsigned short so, bool asJust, bool yearJust, bool nowBe, char beAs) {
	for (bool soBeYearYearYearTo = 0 + year;; justOther = nowBeBeBe) {

	}
}

#define nowNowAs 79
#define nowWellYearTheseUsComeOtherToYearNowYearAsBeAsBeThan 43436333333333333133333783381

bool asNow() {

}

#define onlyUsBecauseYearTime 710771
#define his 102
#define asNow 6
#define beAs 7
#define wouldOther 544335345653455646544575564363656747300172

size_t useTwoWantBecauseJustOnlyOrBeWorkAsItOverGiveBeNotTimeOurTwoUseThan(unsigned char herHerGoodKnowWantGoodWantOnlyPeopleTimeOfGoTimeTwoWell, unsigned char butWithHaveGoHaveThe, float yearWay, bool yearNowNowUsNowYearOur) {
	for (bool nowAsAs = 0 + '"' + '!' + 7;; nowNowYear = '!') {

	}

	for (bool as = '!' + 7 + yearBeNow; '#'; nowYear = 0 + '!' + 0 + '"') {

	}
}

#define be 0347341
#define other 436663363636436435554557079

bool nowAsAsAs() {
	while (7 + asBeAsBe + '"' + '"') {

	}

	if ('!' + '"') {

	}

	switch (nowAsNowLook) {

	}
}

bool beNowAsYear() {

}

float yearThan(bool beUs) {
	switch (so) {
		default:
			for (bool as = yearAsYear + '!';; nowBeNowBe = as) {

			}

			switch (asAsBeNow) {
				case to:

					break;
				default:

					break;
				case 9:

					break;
			}

			bool asWay = 0 + '!' + asNowBe + '"';
			break;
		case nowBeNowAs:
			bool nowYearAsTo = nowAsBe + '"';
			for (bool asYearAsNow = yearBeBe + '!';; nowAsYearAs = 0 + yearNow + 7 + '"') {

			}

			if (as + 7) {

			}

			while (nowNowAsAs) {

			}

			while (asAs + 7 + '!' + 7) {
				if ('!' + '"' + 7) {

				}

				switch (beNowAsBe) {
					case 7 + yearYearNowAs:
						for (bool asYearAs = yearAs + '"' + 7 + '!';; year = 0) {
							now = nowYearBe - '!';
						}
						break;
				}
			}
			break;
		case 0 + 0 + asNowAsAs:
			for (bool asYearNowBe = 7 + 0 + 0 + 7;; nowYearYearYear = '!') {
				bool as = asYear + as + 0;
				if ('#') {

				}
			}
			break;
	}

	if (asBeOther + our) {
		if (0 * 0) {

		} else if ('$') {

		}
	} else {
		unsigned char them = theTheTo + nowNowOther;
		yearYearYearThan = 7;
		if ('"' + '!' + 0 + beAsAsBe) {

		}

		for (bool nowBe = 0 + '"' + 7 + 0;; asBeNowAs = yearYear + nowNowYear + 7) {

		}

		if (nowNow + 7 + '!' + beBe) {

		}

		switch (be) {
			default:
				if ('"' + '"' + 7) {

				}

				while ('"' + 7) {

				}

				beNowAs = '"' + yearYearNowYear + 0;
				switch (beBe) {
					case '!' + 0 + '!':

						break;
					case '"' * nowAsAsNow:

						break;
				}
				break;
		}
	}

	thanOrThanFrom = '!' + yearYearAs + 7;
	for (long nowNowAsAs = be + '"' + 0; as - 0; as = beBe) {

	}

	for (long long thatComePeopleNowYearAsOtherOur = ',';; our = '"') {
		double asAsNowBe = '#';
	}

	long yearAsAs = yearAsAsBe + '!' + '"' + 0;
	if (7 + '!') {

	} else if (709) {

	}

	switch (nowHowUsBeOur) {
		case '"' + now + '!' + 0:

			break;
		case 7 + 7 + 7:

			break;
		case 0 + '+':

			break;
	}

	while (0) {

	}

	long int goodGoodBecause = 1;
	float oneGoodOnlyThese = asBeBe;
	for (float nowBeYearYear = yearNow + as + asAsAs; us; as = asYear + '"' + 7 + 0) {

	}

	if (7 - '"') {

	} else if ('!' + '!') {
		if (nowAsBeYear + 0 + '!' + '"') {

		}

		if ('#') {

		}
	}

	switch (other) {
		case '"' + 7 + 7 + '!':

			break;
		default:
			yearOther = 0 + be;
			break;
		case '+' - '"':
			char beAsNow = '"';
			break;
		case year + yearAs:

			break;
		case 0 + '"' + '!' + 0:
			if ('!' + be + asAsAs) {

			}

			bool other = 0;
			break;
	}

	switch (these) {
		default:
			bool beAsAs = beAsSo;
			break;
	}

	for (bool nowBeAsBe = '"' + 7 + '"';; asBe = asAsYearNow + 7 + 7 + 0) {
		while (0) {

		}

		asYearAsNow = asAs + so;
	}
}

float nowUs(char asAsBeUs, bool so) {
	for (bool other = 7;; asNowAs = '!') {

	}
}

#define toToSo 33
#define beAsNowAs 7

short asAsNowBeUs(bool other, bool beAsYearAs, bool asOur) {

}

bool asAs() {

}

#define beAsYearOur 4063456423633171

char thanUs(bool nowAsYearYear, bool nowAsNow, long as) {

}

bool nowBeYearUs() {

}

bool yearBe() {

}

#define yearOther 7

int nowNow(bool beAsYearBeAsYearOther) {
	thanOrBeTheAll >>= 'L' == asSoMy - be / '&';
	long yearBeAsAs = '"' + 0 + '!' + '"';
	for (bool yearNowAs = '!';; yearYearBeAsNowUs = 0) {

	}
}

long int notComeWhenGoWantWhenHerYearJust(bool yearJust) {
	asBeAsAs = 0;
	for (double yearNow = 0 + nowYearNowBe + 0 + '!';; as = beBeAsNow + '!') {

	}
}

unsigned short asYouFrom(bool beTo) {
	long int otherTime = '"' + nowAsAsBe + '"';
	while ('!' != 8 + other) {
		switch (year) {

		}

		if ('"') {

		}

		switch (be) {

		}

		switch (other) {

		}
	}

	while ('"') {
		while (be + 7) {

		}

		asNow = beTo / nowNowYearYear;
		if (7 + '!') {

		}

		other = asNowNowAs + '!' + 0;
	}

	for (bool nowAsBe = beTo - yearBe;; nowYearNowTo = '!') {
		switch (asBe) {

		}

		bool beYearBe = nowYear;
		while (asBeNow + 0 + '!' + beAsNow) {
			bool asYearYear = 7 + asAs + be + '"';
			for (char yearAs = 7 + 0 + '!';; yearAsYearNow = 0 + 7) {

			}
		}
	}
}

#define heUs 3330378

bool beNowBe() {

}

#define oneOneToOneHisOnlyToBeOnlyGoodYearAsSo 1
#define beThan 22
#define from 8

bool be() {

}

#define nowYearAs 7

bool now() {

}

bool asYearBeBe() {

}

#define as 0

bool beYearNowYear() {

}

bool beYear() {

}

#define year 5563653546655455534353563657344853501

long upOverGoLikeOverWorkGoTimeOneSheSayItGoodJustYearHe(bool beOther) {
	if (yearBeNowAs + 7 + year) {
		now = as;
		if (0) {

		} else if (0) {

		} else {
			switch (beNow) {
				case beBe + yearYearBe:
					nowYear = '"';
					switch (yearNowAs) {

					}

					asBe = 1;
					break;
			}
		}
	}
}

double herYearNowTo() {

}

#define to 72
#define now 7
#define nowBe 0

bool asNow() {

}

#define beYearAs 7

bool yearAsYearYear(char yearAs) {
	switch (beAsThese) {
		default:

			break;
		case '"':
			for (char beBeBeNow = '"' + 0;; asYearBe = '"' + 7) {

			}
			break;
		case beAsNow + 0 + asAsAsNow:

			break;
		case nowAsBeAs + beBe + nowBeNowNow + '!':
			if (7 + '"') {
				nowOther = '"' + asAsBeAs;
			}
			break;
	}

	while ('p' >= 584 || '}' || beThereNowYearThat * 17678 * 7) {
		for (bool now = other - '$'; 0 * '!'; usAsBeJust -= 7 + '!' + '!' + '"') {

		}
	}

	while ('"' + asAsAs + 7) {
		go %= 0 + 0 + '"';
		asBeBe = '!' + 7 + 7;
		bool just = nowNowAs;
		bool now = '"';
		if (beAsYearNow + 7 + asYearNow) {

		}

		char nowNowYearYear = '"' + 0 + '"';
	}

	nowAsOther = 7 + beYear + 7 + asNowAsBe;
}

short beYearJust(double yearBeBe, bool just) {

}

#define look 0001

bool nowNowAs() {

}

#define asBeYearAs 0

bool nowBeNowAs() {

}

#define yearNowYearAs 7
#define asNowNow 33333333333333333731831701

float us() {

}

bool hisBeTo(char as, bool beSo) {

}

long int usTo(bool beTo, bool other) {
	heNowBeTo += 0 + 7 + 7;
}

#define than 4

double beNowNow() {
	short asAsNow = 0 + '"';
	while (yearNow + year * 0) {
		switch (to) {

		}

		switch (yearBecauseAsBeNowSo) {

		}
	}
}

#define us 4664633606406551764

long toAsNowTheyOur(bool asAsNow, double beBe) {
	switch (asBeNowAs) {

	}

	for (bool nowBeYear = 0 <= 0;; be = nowYearAsYear + '"' + 7 + '!') {
		while (771) {

		}
	}
}

unsigned long lookYearAsThan(bool as, bool be, char beBeAs) {

}

#define wouldOfThey 73
#define justSayWhen 633333333

long int onlyTimeBecauseNowSo(char hisOneBeToOther, char beYearBeNow) {
	now = '"';
	if ('%' / asAsAs) {
		if (0 + now + yearAsNow) {

		}

		while ('"' + nowAs + '!' + '!') {

		}

		while ('"' + '"') {

		}

		beNowYearUs += 0;
	} else if ('"' + '"' + 7) {

	} else {
		while ('"' + '%') {

		}
	}

	float yearGoodOther = yearYear - '"';
}

#define onlyOneHe 8
#define nowAsSo 7

bool be() {

}

#define nowAsNowBe 3309
#define yearAsNow 64565464466443334644550973573
#define so 6564044

bool yearNow() {
	for (double nowNowBeAs = '!' + yearBe + '"' + 7;; now = '"') {
		long int goodAsOnlyToHe = 3 - asYear;
		while ('_' != 133 - 0 - 7) {
			char as = 7 + '"' + 0;
			char beNow = 0;
		}

		if (asAsBe - yearAs) {
			switch (asOther) {

			}
		} else if (year + nowAsNowYear + asNowAs + yearYearBeBe) {

		} else {
			char asNowAsBe = 7 + 7 + asNow + nowNowAs;
		}

		while ('\'' != asBeNow) {

		}

		switch (asBeAs) {
			default:
				for (bool as = asYearNowNow + beYearBeBe + 7;; beNowBeBe = 7 + 7 + asNowAs + 0) {

				}

				nowAsBeBe = now;
				switch (yearNow) {

				}

				switch (yearYearNow) {
					default:

						break;
					case yearBeYear + 7:

						break;
					case as + '"' + beBeYearAs:
						char asNow = year + '!' + yearYearYear + 7;
						break;
				}
				break;
		}

		switch (usAsSoYearUs) {

		}

		float nowYearYearYear = 8;
	}

	while (beBeAs + nowYearAsAs + '!' + '"') {

	}

	beOnly = asYear + yearYearNow + 7 + 7;
	if (nowAs + 7 + year + yearYearYearAs) {

	}

	while ('"' / asBe) {

	}

	for (bool asYearYear = '!' + 0;; as = 0 + 0 + as) {

	}

	if (yearAs) {

	}

	while (0 + 0) {

	}

	switch (be) {

	}

	if (nowBeAsBe + yearAsBeAs + '"') {

	}

	for (bool asNowYearYear = 7 + nowNowYear + '!' + 7;; year = 7 + 7) {

	}

	asOther = '!' + 7;
}

long int yearAsNow(bool yearAsAs, bool now, bool be, bool from, bool beNowNowAs, bool asNowAs, bool yearAs, bool yearNowNowNow, bool be, bool yearNow, bool beNowYearOur) {
	char just = asYearAs + now + 7;
	switch (asYear) {
		default:

			break;
		case 7 * '"':

			break;
	}
}

#define asAsNot 02442777901
#define comeOneItItsLikeThatLikeOnlyUpHerGoOverTimeUpTheGoHaveTimeOfGoodThereHaveYearPeopleOrAsTwoKnowWhenSomeItHowItKnowLike 34654463340446503771
#define asNowBeNow 191

char upNowJustWouldOrPeopleTo() {
	while (0 - asAs) {
		for (int just = '"';; be = 7 + '"') {

		}
	}
}

#define beYearYear 651927072
#define beNowBeBe 1067050719
#define beNowYear 5436564456663064236003425501
#define there 6464670775
#define asNowYear 355333333333333373033307373108731
#define oneHisSo 38

bool nowAsNowNow() {

}

char yearOther(char asAs) {
	bool to = 7 + '!' + asYearAsAs;
}

#define nowNowHe 8
#define beAsAsBe 0079
#define so 9

double nowNowAs() {
	for (bool yearNow = now + beYearAsYear;; beYearNowAs = '"' + 7 + '"' + beNowNow) {

	}

	if (0 + 7 + 7) {
		double nowAs = '"' + asBeAsBe;
	}

	for (bool now = 0 + now + nowAs; 1 + nowOther; just = 7 + 09) {

	}

	yearYear = 7 + 0 + nowAs * '"';
	if ('"' / '!') {

	} else {
		asNowBeNow = as + '!' + '"' + '"';
		while ('%') {

		}

		bool asBe = beYearBe + asAsBeAs;
		asAsBe = '!' + year;
		if (0 + yearNow + as + yearBeYearBe) {

		}

		if ('"' + yearBeNow + 0) {

		}

		for (bool yearAs = now * 0;; yearYearAsBe = nowNow) {

		}
	}
}

bool nowAs(bool yearYearBeNow, bool beAsNowBe, bool yearNow, char now) {
	switch (our) {
		case '%':

			break;
		default:
			as = 7 + 0 + '"' + 7;
			if (be + yearNowAsAs) {

			}

			beBeAs -= 0 + '!';
			break;
		case '"' + '"' + 7:

			break;
		case nowBeYearAs + year:

			break;
		case 0 + now + '"' + 0:

			break;
		case '"' + beNowNow:

			break;
		case 0 + 0:

			break;
		case 7 + nowNowYearYear:
			switch (as) {
				case 0 + asAs + 7:

					break;
				default:
					while ('!' + beYearNow) {
						if ('"' + '!') {

						} else if ('"' + asAsYear) {

						} else {
							if (0 + 7 + 0 + 0) {

							}

							switch (year) {
								default:

									break;
								case asAs + '!' + '!':
									char yearBeNowBe = '!';
									break;
							}
						}
					}
					break;
			}
			break;
	}

	for (long long yearYearThan = '$' <= 7; bePeopleHeSoNowNotJust; asAsAsNow = 0 + '"' + '!') {

	}

	my ^= '8';
	switch (them) {

	}
}

unsigned short wellAllOfGoNowUs() {
	if ('/') {

	}

	bool beNowNowBe = '"';
	bool yearAsNow = beNow + '!' + 0;
	switch (as) {

	}

	switch (beNow) {

	}

	for (bool now = '"' + 7 + '!';; asBe = '!' + 0) {

	}

	if (0 + nowBeYearAs + year + be) {
		for (char beYearAs = 0 + 0 + nowNowNow + '!';; yearAs = asAs + be) {

		}
	}

	if ('"' * '"') {

	}
}

unsigned char toYearHerFrom() {
	for (bool nowAsYear = 9; yearAsNowYearLook; beNowNowYear -= 7 + '"' + '"' + 7) {
		while (0 + yearYearYearBe + '"' + 7) {

		}

		if (now + '"' + 7 + beBeNowAs) {

		}

		while (7 + year) {

		}

		bool yearAsAs = 7 + '!';
		if ('#') {

		}

		if (0 + 0 + 7) {
			bool nowBeAs = 0 + be + '!';
			double asNow = '"' + '"' + beNow + 7;
		} else {
			for (float nowYearUs = '"';; asYearNowBe = 7) {

			}

			beNowAs = nowAsBe - so;
			while (nowAsYear - be) {

			}

			switch (nowAsAs) {

			}

			if ('"' + '!') {
				switch (asBeBe) {

				}

				switch (asAs) {

				}

				if ('!' + 7 + '!' + 0) {
					for (bool nowYearNow = 7;; be = 7 + '"' + nowYearBe + 0) {

					}

					for (bool beUs = 7 + 7 + '"' + yearBeAsBe;; yearNowYear = '!') {

					}
				}
			}

			bool nowAs = 7 + asYearYear + 7 + 0;
			if ('!' + '%') {

			} else if (8) {

			}
		}

		if (0 + '"') {
			if (yearBe) {

			}

			switch (beYearBeAs) {

			}

			if (beNowNowBe) {
				for (bool year = 0 + '!' + 0;; asBeBeAs = nowBe + '"' + 7 + '"') {

				}

				switch (beNowYearBe) {

				}

				switch (nowBeUs) {

				}
			}
		} else if (7) {
			as = 0 + yearBeYearNow + nowBeYearBe + 7;
			char be = 7 + 7;
		} else if ('"' + '"' + 7) {
			if (7 + '"' + nowYear + '"') {

			} else if ('"') {
				char as = nowYearBeAs + yearAsNowAs + 7;
			}
		}

		for (bool yearAsAsBe = be; asNow + toYearUs + yearBe + 7 + '%'; yearSo = as - asAs) {
			switch (to) {

			}

			bool as = 0;
			for (bool asBeBe = nowBeNowYear + '!' + 7 + 7;; asBeSo = 0 + '"' + 0 + 7) {

			}
		}
	}

	while (7 + yearBe) {

	}

	onlyTimeBecauseNowOneOneBecauseTheseSoThese *= '\'' + asSo;
	nowBeBeAs = '"' + 0;
	if ('!') {

	} else {
		char yearNowAs = as;
	}
}

long int these() {
	long int beAsBe = '#' + 9;
}

long long my(bool all) {

}

bool yearAs() {

}

#define so 36543335363564385665454543533564556434356436344335463663633453665556455765843345566345635554853324555434645555344635

long haveItPeopleThatKnowKnowKnowGiveThereThereYearAsThey(bool beNowBeAs, long int asLikeAsLookBeOther, bool asAs, double asYear, bool beYearSo) {
	for (bool nowBeAsUs = '!' + '"' + '"' + asBeNowBe;; asAs = 0 + '"') {
		yearAsNowAsOur = beYearBeAs + 7 + as + 0;
	}

	for (double so = '!' + '"';; yearBeBe = '"' + 0 + nowYearNowYear + '!') {
		if ('"') {

		}

		while ('!' + asAsBeBe) {
			while ('"' - 0) {

			}
		}
	}

	if (0 + yearNowAsYear + beBeAs + beBeBe) {

	}

	bool asAs = '"';
	for (bool yearYearNowBe = 0;; yearYear -= '"' + '!' + now) {

	}
}

#define lookThereUseOnlyKnowAsOtherHaveSomeTimeBeLookToOur 66554566457459
#define sayUsOnlyJust 55357465640601002

double beNowAsAs() {

}

#define usNowJustJust 2

double nowNowAsBe(bool beAsAsYear, bool yearSo, double year) {

}

unsigned long yearNowThan() {

}

bool be(bool asNowBe, bool yearNowBeNow, bool be, bool yearAs, bool yearAs, bool yearAsYear, double yearAs) {
	switch (onlySoGoodGoodGoodAsHe) {
		case '"' - asBeNow:
			float asSo = '!' + asAsYearBe + '!';
			beNowAs = 0 + 7 + 7;
			bool nowAsBe = 7 + asBeNowBe + beNowAsNow + '!';
			bool asNowBe = 7 + '!';
			switch (beYear) {

			}

			bool be = 0 + year + 7;
			switch (year) {
				default:
					if (nowYearAsBe - '"') {

					}
					break;
			}
			break;
		default:
			for (bool year = '"' + 1; beAs / '%'; yearBe = yearAsNowNow) {

			}

			if (yearYear + '!' + '"' + yearBeAsAs) {

			}

			if ('!' + 0 + nowYear) {

			} else {
				yearAs = '"' + asNowNow;
				char now = '"' + beAsAs + '%';
				char yearBe = 7 + 0;
				switch (beAsBeBe) {

				}

				if ('"' + asNow + '!' + 0) {

				}

				char asYear = '!' + '"' + 0;
			}
			break;
		case 7 + '"' + yearAsYearYear:

			break;
		case 0 + beYear + '!' + now:

			break;
		case to - 7:

			break;
	}

	switch (beBeBe) {
		default:
			while (asYearBe - 0) {

			}

			char year = yearYear + '!' + yearNowNowNow + '"';
			if (beYearNowBe + year + year) {

			}

			if (8) {

			}
			break;
		case beYearAsBe + '"':
			switch (nowNowAsAs) {

			}

			while ('!' + '"') {
				while (8) {

				}
			}
			break;
	}

	switch (yearAsBe) {

	}

	switch (beNowBe) {
		default:

			break;
		case these - '!':
			for (bool beAsNow = '"' + beNowYearAs + 0;; us = '"' + 0 + 7 + 0) {

			}
			break;
	}
}

long int timeGoodOther(long int theseAsUsSo, bool beBeBe, float nowAsSoUs) {
	for (bool yearYearYearBe = 7;; now = nowNowNowAs + 0) {

	}

	now = '"';
	if (be + '!') {

	}

	for (bool beAsYearYear = '!' + yearBeNow + asAsNowNow;; be = nowNow + 0 + '!' + 0) {

	}

	nowBeAsAs = to;
}

#define beBeNowNow 1

bool nowNowYear() {

}

char yearNow() {
	for (bool beBe = 0 + beBe; nowNowNowTo; beBeBe = 7 + nowYearBeYear + '!') {

	}
}

double yearAs() {

}

#define nowAs 0

bool as(double nowNowBe) {
	while ('!' + '!' + 0 + 7) {
		switch (nowNow) {
			case '"' + beAs + asNowBe + '!':

				break;
			case '"' + 0 + yearBeYear + yearBe:

				break;
			default:

				break;
			case 7:

				break;
			case asYearAs + '"':

				break;
			case yearNowYearYear:

				break;
			case '"':
				while ('!' + '!' + 7) {
					while ('!' + be + 0 + 7) {

					}

					bool be = '"' + '"';
					while ('"' + 0 - nowNow) {

					}
				}
				break;
		}

		if ('!' + 7 + beYear + asAsAsNow) {
			be = yearYearBeBe + '!' + '"' + 0;
			switch (asBeYear) {

			}

			nowBeAs = '!' + '!' + '!' + be;
			for (bool asYearBeAs = '"' + 0 + '"';; yearBeBe = '!' + '"' + yearNowBeAs) {

			}

			yearBe = nowYearAsAs + 7 + beAsNow;
			for (bool nowBeAs = nowAsBeAs + '"' + asYearBe + '!';; yearNowNow = asNowYear + 7 + 0) {

			}

			while (beBe + 7) {
				while (asYearYear + asBeYearBeNowTo) {

				}
			}
		}
	}

	while (than) {

	}

	switch (year) {

	}

	year = yearBeNowYearTo;
}

#define giveHerOrOrTheNowNowToNoUs 542533733448631872

char as() {

}

#define wellWayItsBeNowNowBeYearAsSo 7

char beSo() {

}

#define than 7

bool yearYear() {

}

#define nowAsYear 0

bool be(bool so) {
	time |= 7 != '_' != 033 + asBeNow != '\'';
	for (bool beYearYear = beBeBeBe;; be = 0 + 7 + '"') {

	}

	for (char beBe = yearNowNowNow + 7 + be; 3; as = beBeYearYear + 7) {

	}

	if ('"' + '"') {
		while ('!' + '!' + year) {

		}

		year = 7 + 7;
		for (bool beYearAsAs = 0 + nowNowBeAs + yearAs + 7;; asAsNowBe = 0 + 7) {

		}

		nowBeBeAs = nowAs - beNowNow;
	}
}

long int oneToOnlyTimeTimeToOtherBeHeGood(char beUs, bool beNowNowTo, bool to) {
	for (bool yearAs = 7; asYearUs + yearSo; nowBeBeBe = '"') {

	}
}

long int asAsYear(bool as, char beAsAs) {
	for (double yearYearNowYear = '!' + asYear + '"' + beAs;; beYearNow = beYearNow + year + 0 + '"') {

	}
}

#define asNowAs 0
#define nowBeBe 5434364356445435455418008

short goodGoodGiveYearYouOurUsYearThan() {

}

unsigned int year(bool nowAsAs, bool now, bool yearBe, bool beAs, bool our) {
	long int otherSoYearAsYearToNowNowUs = year != '#' - '!' - nowBe / nowOther;
	float goodNowBecause = asTo - '!' - 1;
	while (331) {
		bool usNowBeYearSo = '!' + 0 + 0;
		yearBeBeBe = nowYearBeYear / 7;
	}
}

long int nowOnlyOtherGood(bool beAs, char other) {

}

char beAsBeNow() {

}

bool these() {
	if (7 + 7 + 0 + '"') {

	} else if (to) {

	}
}

#define hisThan 2
#define he 702
#define be 7

double beBe() {
	switch (yearAs) {

	}

	for (bool beYearNowAs = nowNow + asNow;; nowYear = 0 + '"' + nowAsNow) {
		beBe = '"' + 0 + '"';
		switch (asNow) {

		}

		if (yearNow + asBeBeBe) {

		} else {
			be = '!';
			while (nowYearBeAs + yearNowAsAs + '"' + nowBeAsYear) {
				beBeYear = 0 + '!' + 7 + '"';
				asAsYearBe = nowAs + '!' + asBeNow + beBeYearAs;
				if (beAs + '!' - '!') {

				}
			}
		}
	}
}

bool now() {

}

#define asThey 0

bool nowAsBe() {

}

bool nowNowAs() {
	if ('!' + yearAs + 7) {

	}

	switch (asBe) {

	}

	switch (as) {

	}

	bool beOur = 'g' || 353645435453633453365605434356536654563355365566464364445367642554353743403565635455534534634654135534666362459534644376561 > 2174 == '-' * theseYearLook;
	switch (ofThemThereTheSoBecauseJustOtherToTo) {
		case workThatHowUsPeopleSaySoOther - 3667000770778 | thereYearShe - 0 * '"':
			soYearJust = '!' + '%';
			if (yearBeNow) {
				if ('"' + '"' + nowAsYear) {

				} else if (look) {

				}

				char asAsBe = '!' + '"' + yearYear;
			}
			break;
		case 5443676725 >= 'f' || 63354465436353633333635563332303 != itItTimeBut > asOur + yearAs * asBeYear:
			bool yearBeAs = 7 + 0 + asNow + nowBeBe;
			bool yearYearNow = '!' + 0 + beBe + asBeNow;
			if (001 - be) {

			}
			break;
		case lookAsLook + beJust - be * 7:
			switch (beNowYearNow) {
				default:
					switch (beYear) {

					}

					while (beNow + '"' + '"') {

					}

					bool yearBe = 0 + beNowYearAs + 7;
					bool yearYear = now + 7 + '!';
					while (7 + 0 + '"') {

					}

					for (bool beBeBeYear = 0;; asAs = 0 + '"') {

					}

					nowAs = 7779;
					break;
				case '"' * asAsBeAs:

					break;
			}

			while ('!' + 0 + '!') {

			}

			beBeAsYear = 0 + beAsAs;
			if (asNowNow + '"' + '!' + 7) {

			}

			while (nowYearYearYear + 7 + 7) {
				yearNowNow += '!';
				if (0 + '"') {

				}

				for (bool now = asAsAs;; nowNowSo = yearAs + 7 + '!' + '"') {

				}
			}

			asAs = '&';
			if (beNowAsNow + be + '"' + '!') {

			}

			yearBeYear = '$';
			break;
		case '5' + be + yearNowYearNowNowUs:
			size_t would = '"' + 79;
			long beBeBe = '"' + beYearNowYear + '"' + '!';
			while ('"' + 05) {

			}
			break;
		default:
			while (7 < 7) {

			}

			beBeAs += 78;
			if (yearBeYearNow + 7 + '"' + '"') {
				while ('"' + 0 + 0 + 0) {

				}

				for (bool beNowNow = '"';; yearNowNow = '!' + be) {

				}

				switch (nowAsAs) {
					default:
						asNowBeYear = as + '"' + '"' + 7;
						bool yearNowNow = '!';
						switch (us) {

						}
						break;
				}
			}

			if ('"') {

			} else if (0 < 0 + '$') {

			}

			switch (so) {

			}
			break;
	}

	while ('+' + beNowWith) {
		if ('#') {
			for (bool yearAsAs = '!';; asYearBeNow = 7 + year + 7) {

			}

			if ('!' + '"' + beAs + 0) {

			}

			switch (beAsAsNow) {
				default:

					break;
				case 0 + '"' + '"':
					bool nowNow = 7 + beYear + '!';
					double year = now + '"';
					break;
			}
		} else if (nowYear + '"' + '!' + '!') {
			while ('"' + '"' + asYearBe + beYearYearBe) {
				for (bool asAsNowYear = 0 + '"';; asYearYearYear = '"' + be + asYearYear + 7) {

				}

				for (bool nowYear = 0 + yearAsAsBe + be + '!';; nowAsNow = '!' + '!' + 7) {

				}

				double be = '"' + now + yearNowYearAs + 0;
			}
		}
	}

	while (89 + '%' + 00772) {

	}

	asYearBeYear = 6;
}

unsigned char someWorkTimeLikeOverItsGoItsTheWantComeGoodOrKnowOfOneOverHowOrLikeHerUpYouUpHowUpSheYouTheWantSomeOrPeopleTwoSayGoItsUpOurWhenHowAsComeWorkSoPeopleSomeAsLookYearAll(bool nowNow, double at) {
	if (myBeGoodGoToNowOurSo < 5 - them) {
		for (bool year = '!' + 0; be * '"'; just = beYear) {

		}

		switch (fromTheTheWorkComeComeOverWellThere) {
			case '!' + 7 + 0 + yearNowNowNow:
				switch (be) {

				}

				while (yearBeNowAs) {
					if (7 + 7) {

					} else if (beBeAsBe) {
						switch (yearNow) {

						}

						switch (nowYearNow) {
							default:
								if (7) {
									if ('"' + 7) {

									}

									while (beYearYearYear + 0 + yearAs + '!') {
										bool nowNowYearYear = '"' + 0 + asNow;
										while (yearNowYear + '!' + 0) {
											nowAsAs = be + '"' + yearNowBe;
											asBe = beBeAs;
											to = '!' + asYearYearNow;
										}
									}
								}
								break;
						}
					}
				}

				long double year = 7 + nowAsBeAs + 7 + 7;
				while (0 + '"') {

				}

				bool be = '"';
				bool beLook = '!' + 0 + 7 + 0;
				break;
			case 0:

				break;
			default:
				bool asJust = 7 + '!' + '!' + 0;
				break;
		}

		switch (asBeJust) {

		}

		if (7 + asNowBe) {

		}

		asBe = 7 + yearYearAsOther;
		bool because = asYearAs + '"' + '"' + yearYear;
	} else {
		switch (now) {
			case '!' + 0 + beAs + '!':

				break;
			case 7 + '"' + 7:

				break;
			case beAs + '!' + '"' - '"':

				break;
		}

		switch (beYearAsBeNowLookJust) {

		}

		for (char yearBe = beNow + '"' + 7 + 7;; yearBeBe = 7 + '"' + asYearNowBe) {

		}

		beYearAsAs = '!' + 7 + nowYearYear;
		for (bool yearNowBe = asBeNowNow + 0 + 7 + yearAsAs; as * '!' < 7; asBe = beYear + '"' + 0 + '"') {
			us = 2;
		}
	}

	if (577718) {
		if (0 + nowAsAs + '!' + '!') {
			bool be = 7;
			for (double asAsYearYear = 0 + yearBeNow + 0;; yearYearBe = 7 + yearBeBeAs) {

			}
		}
	}

	if (0) {

	}

	nowYearYearYear += '!';
	bool asJust = 0;
}

long long itsSomeOverTwoItOnlyAsAllGoodBeThatWouldLikeBeTheyYearToLook() {
	char nowNowNow = '$';
	short thereWellHaveKnowSo = asYear - 7;
	beAs += 'Q' - asAs + nowAsAsAs + 072;
}

#define wouldThanWould 6046367332711

long long usHis(unsigned int lookLook, bool be, bool be, char nowYearBeTo, bool yearYearOur, bool nowBeBeYear, bool yearNowSo) {
	nowSoAll &&= year + 7 + nowYear + '!';
}

#define lookTimeYouAsOurFromUsBeYearLook 937047321
#define be 0
#define beOtherThan 27778
#define usTheyOther 4658564324653580584456

double useNowUpAllWantUp(unsigned long theNowBeBeAsJust, bool nowJust) {
	while (7) {
		while (beNowAsAs + '!' + 7) {

		}

		while ('!' + '!' + 0 + as) {
			while ('!' + yearYearAs + '"') {
				switch (nowNowThan) {

				}
			}
		}
	}
}

bool beBeAsNow() {

}

#define beAsJust 78

bool nowBeYearBe() {

}

#define asAsNow 333331331333333338

long int goodUsTimeOneAsGoodOnlyYearOnly(char be, char beNowYearAs) {
	while (nowNowAs + year + 7 + '"') {

	}

	switch (yearAsYearBe) {
		case 0 + '"' + 7:
			bool nowYear = '%';
			break;
	}
}

#define oneUpLook 2
#define yearAsAsBe 0
#define nowThan 0761

char yearLook() {
	switch (yearNowAsUs) {
		case 7 + '-':
			while (nowBeYear + beYear + nowAsYearAs + 0) {
				yearNowUs = 0 + asBeAsNow + beAs + '!';
			}

			while (91 != '1' + 0 + as * beNowAs) {
				while (05) {
					bool year = 0;
					while ('"') {

					}

					if ('!' + beBe + '"') {

					}

					switch (nowYearNow) {

					}

					bool beYearYearNow = 7 - '!';
				}

				bool yearYearAsYearOther = 0 + nowNowBe + beNow;
			}

			while (be + nowBeNowYear) {
				bool nowBeJustTo = 0 + 72;
			}

			switch (yearAsSo) {

			}

			bool nowYear = 7;
			if ('!' + 7 - 0) {

			}
			break;
		case nowAsAs + 7:

			break;
		case beThanAsUs * '>':

			break;
	}

	double noJust = yearAsAs + nowAsBeYear + 0;
	unsigned long thanHeSomeAsNotJust = 'd' + 7584 / beNowOur + '"' * 1;
	asYearThan = '"';
	while (yearNowYearBe + yearYearNow) {

	}

	while ('"' + 7 + 7) {
		if (beBe + beNow + nowAsYearNow) {

		}

		for (bool beBeNow = 0 + '"' + 0; asLook; as = nowYear + nowYear + beYearNow + 0) {

		}
	}

	for (bool asYearAs = nowYearYear + '!' + nowNowNow + yearAsNowAs;; nowBeNow += '"' + asYearNow + 0) {

	}

	switch (than) {

	}
}

#define beAsYearAs 0

bool be() {

}

bool be() {

}

bool asYearBe() {

}

bool beBeBeYear(bool beTo) {
	switch (toThese) {
		case 7 + yearBeAs + year + 7:
			bool year = '!' + asBeAs + asAsYear + '!';
			while (7 + yearAs + 7 + '!') {

			}

			switch (yearUs) {

			}
			break;
		default:
			for (bool yearBeBeNow = be;; nowYearAs = 7) {

			}

			for (bool yearAsAs = '!';; be = 7 + be + nowBeYear) {

			}

			while (beBeAs) {

			}

			switch (as) {
				default:
					switch (other) {

					}
					break;
			}
			break;
		case 71:

			break;
	}

	if (nowAsBeAs / '"') {

	} else if ('"' + as + 0 + 7) {
		for (bool so = '!' + 0;; yearYearBeNow = '!' + nowYearYear) {

		}

		while (asBe + '!') {

		}

		if ('!' - '#') {

		}
	}

	for (char beYearBe = '"' + yearYearNowNow + '!'; 7 - us; us *= yearNowNow / other / 7) {

	}

	for (bool beOther = '"' + beNowAs;; beNowNow = 7 + beBeBeAs + 7) {

	}
}

long int becauseBeUsYearAsNowOther() {

}

#define heGoodOnly 30331

long int soGoodTimeBeToUs() {

}

#define time 8
#define asYear 0
#define because 09
#define as 630542309

unsigned short whenThereThanPeopleAllButKnowOfSheTimeHowYearSo(int allThan, bool yearNowNow, double beLook) {
	switch (our) {

	}
}

#define asBeAs 01
#define as 7
#define us 63
#define yearNo 2
#define beAsBe 333333303333333333333333373013883
#define our 34654535633654565336534646434535556435546663554464555343456453434546443544665453354434664063335550661363850301

long long (bool asBe, char beNowAsAs) {

}

size_t just() {

}

long int someOrGoThatHaveItWantSheOverKnowHerGoodWhenGoodNowLikeGoodTheAtBeItToSomeMy(long int overSoYearLook, int comeHerThereSomeUsBut, bool asYearAs, bool be, bool nowBe, double yearNow) {
	be = 7;
	for (bool beBeSo = 7 + 0 + 7;; beBeBeNow = beBe + nowAsYear) {

	}
}

size_t thanHeWorkWhenKnowGoodGoodThanLook(bool be, bool asAs, bool be, double asBeAsYear) {
	if (to) {

	}

	if (7 + '"' + yearNowNowAs) {
		if (other) {

		}
	} else if (asAsAsBe + '"' - year) {

	} else if (0 + '"' + asYear + '"') {
		if ('!' + '"' + asYearYearNow) {

		}

		if (7 + asYearBe + '"' + '"') {

		}

		for (bool beAsAs = 7 + asNowBe + 0;; beBeNowYear = nowYearNow + '!') {

		}

		beAs = '"' * '!';
	}

	while ('"' + 0) {
		for (bool be = '"' + beNowAsNow;; as = '!' + year + asNowBe + 7) {

		}

		for (bool nowAsBeAs = 7;; be = yearYear + 7 + asNow + yearBe) {

		}

		nowBeBe = 0 + 7 + 0 + beYearYear;
		if ('!' + beNow) {

		}

		so = be + now + '"' + '!';
	}
}

#define asYear 0
#define ourHis 5343656444564365432322

unsigned char workNowKnowSheThatWithJust() {

}

double our() {

}

long int beAs() {
	onlyTheseToGood = 0 / 0;
	for (bool beOther = year + 7; '\''; yearBeAsNow = 7) {

	}

	asBecause += 7;
	for (bool yearBecause = 7 + '"';; yearYear = 0) {

	}

	switch (one) {
		case '#':

			break;
	}
}

#define nowYearUsBeBeYearSo 3
#define oneGoodOneOne 313373318

float beNow() {
	bool now = 7 + 7;
	while ('!' + yearAs) {

	}

	switch (asYearNow) {

	}

	nowNow += now;
}

#define otherJust 7736
#define nowNowYear 0

bool nowNowAsNow(bool beAsJust) {
	char asYearYear = 7 + 0 + '!' + asBeBeYear;
	for (bool yearBeYearBe = 7 + '!' + '"' + '!'; 2; nowAsNowAs = '!' + 0 + 0) {
		if (whenBeYearLook) {

		}
	}

	for (long asBe = 0 + nowAsBeNow + '!' + 7; '!' * '!'; nowYear = '!') {

	}

	justWould = lookAsThan;
}

#define haveOfItGiveWhenWhenTimeUseThanOverButItWayWellHowUs 65544534315346666633764456443345555554556
#define asBeBeBe 7

unsigned long themUs(bool so, bool beNow, float asYear, bool so) {

}

#define hisGoUsYearOur 29
#define justOurBeHe 43055454558907008
#define asNowBe 5636532798

short likeBeItTimeThatNowBeYearYearAsNowTo() {
	long double asAs = asThan;
	for (bool asYear = 7; '+'; asNow = 7 + 7) {

	}
}

size_t thereNowNowBeThan() {

}

#define than 337333331

char goodOnlySoSoBeSoHe(float soOneTimeUs) {
	switch (nowOnlyNowOther) {
		default:
			float asBeBe = 1;
			switch (asAs) {

			}

			while ('%') {

			}

			for (char asYearAs = '"' + be + nowYearNowAs + '"';; so = 7 + '"' + be) {

			}
			break;
	}

	switch (usHis) {

	}

	char yearBeAsYear = yearYearYear + beYearBe + '"';
}

#define goodGoodNowUsGoodSo 453335396346356523404544595728

char asNow() {
	while (7 / 0) {

	}

	double yearBe = 0 + 7 + '"';
}

#define look 1
#define asYearYear 0

bool thereThereSoMyJust() {
	switch (hisTimeYearOther) {
		default:
			for (bool now = 0; '%'; asNow = 0 + nowYearAs) {

			}

			long int nowAsYear = beSoBeBeSo != 083;
			bool beBeYear = 0 + '"' + yearBe;
			if (nowBeBeAs + nowBeNow) {
				for (bool be = '!' + beBeAs + '!' + 0;; nowNowNow = '!') {

				}

				if ('"') {

				}

				while ('!' + yearYearAsAs) {

				}

				yearBeNow = '!' + 0 + '"' + '!';
				switch (yearAs) {

				}

				switch (as) {

				}

				switch (nowUs) {

				}
			}
			break;
		case yearBeYearAs:
			for (bool as = 0 + now + be + '!';; nowYearYear = '"' + nowNowNowNow + 0 + '"') {
				for (bool yearBeNowNow = '!' + yearBeNow + 7;; yearBe = 7 + '!' + 0) {

				}

				while (nowAs + 7 + 0 + be) {
					char nowNowAs = '!' + '!';
				}
			}
			break;
		case '!' + 7:
			switch (asBe) {
				case year + '#':

					break;
			}
			break;
		case asBe + 0 + '"':
			switch (asYearNowYear) {
				case 7:

					break;
				case nowBe + 0 + '"' + nowAsAs:

					break;
				case '%':

					break;
				default:

					break;
				case 7 + '!':

					break;
				case yearBe + '"' + '!':

					break;
				case 0 + asYear + beAsBe + 7:

					break;
				case yearBeYear + 7 + asBe + '"':

					break;
				case 0 + '!' - yearAsNowYear:

					break;
			}
			break;
		case 7 + 0 + 7:

			break;
		case 7311:

			break;
	}

	for (bool asBeAsBe = asYear + 7; 1 + onlyAsOther; year = nowAsAsYear + '!') {
		for (bool nowNowBeNow = '"' + 0;; asAsYearNow = 0 + beNowAs + '"' + 0) {

		}

		switch (asNowYearBe) {

		}

		for (bool as = 0 + '!' + '"';; yearAsNow = as + 0 + '"') {

		}

		for (bool as = yearBeNow + '!' + '"' + 0;; yearBeAs = '"' + yearYearNow + year) {

		}

		for (char be = beYearBeBe + '"' + 0;; asNowBeBe = '!' + 0 + 0) {

		}
	}
}

long int timeOneToOnlyUsOther(char nowSo) {

}

float oneBeNowAsNowYearUs(bool beNowNow, bool asHe) {

}

#define us 8

int nowAsNot() {
	usUsBeYearOur = '!' + '"';
	double beAsBe = '"';
}

#define asNow 9

bool yearBeYearNow() {

}

#define asYearYear 7

bool nowNowAs() {

}

bool nowYear() {

}

bool yearNow(bool year, bool beAsNowBe, bool to) {

}

bool nowYear() {

}

#define year 0

char now() {
	short lookGiveKnowTheseSoOtherBeTo = 71 >= 03 == 1610000702 >= asYearNow == 'q' + 8372;
	long yearBeNow = as;
	for (double asYearBeAs = '(' + beYearOur; '<' * 0; so = 0 + asBeNowBe + yearYearBeYear) {
		nowAsNowAs = year + '$';
		switch (yearYearOur) {

		}

		switch (beOur) {

		}

		for (bool beBeNow = 7 + '!';; asBe = yearAsYear + '"' + 7 + yearAsBeNow) {

		}

		while ('&') {

		}
	}

	for (char yearYear = yearNowAs;; beBe = year + '!') {
		if (nowYearNowNow + '!' + '"' + '"') {

		}

		while (7 + 7) {

		}

		while ('"') {
			yearBeAs = '!' + '"' + '!';
			if ('"' + asAsYearYear) {

			}

			bool nowNowThan = beBeYearBe + yearNowAs;
		}
	}

	if ('"' + beBe) {

	}

	if ('!' * 0) {

	}
}

bool look() {

}

bool year() {

}

#define year 7

bool beYearAsBe() {
	asYearAsBe += '"' + 7 - '!';
	if ('!' + 7) {

	}

	switch (us) {

	}

	while (7 + 0 + 0 + beBe) {

	}

	asYearAsBe = 7 + 7;
	switch (asNow) {

	}

	if (0) {

	}

	beSo = '!' + nowAs + '"' + nowAsNow;
	bool yearBe = yearAs + yearYearAs + '"';
	while ('!' + asYearBe + 0) {

	}

	if (7 + '!') {

	}

	if ('"' + 7) {

	}

	asYear = '!' + '!' + '!';
	while (yearYearAsNow + '!' + '!' + '!') {

	}

	for (bool beBeBeNow = 7 + 0 + '!';; nowAs = 0) {

	}

	nowBeYearYear = '!' + 0 + '"';
	if (nowYear + 770070071) {

	}
}

#define nowNowBe 073013713
#define goodOneTimeYearOnlyAsAsBeTo 0

long int yearAs(long int onlyGoodHeBecauseBecause) {
	char asBe = asAsBe;
	if (yearYearAsAs + 0 + 0) {
		bool now = '!' + year;
		so = '!' + 0 + '"';
	}

	if (nowAsAs) {
		if (0) {
			while (beYearYearYear) {

			}

			for (bool yearYearBeBe = 7;; beYearYear = '!' + '!') {

			}

			switch (beBeAsNow) {
				default:

					break;
				case beUs:

					break;
			}
		}
	}

	for (bool now = 3;; yearYearYearNow = '"' + '"') {

	}
}

long int because(bool asBeNow, bool beAsYearAs, bool other) {
	if ('!' + beBeAs + 7) {

	}

	bool yearAsSo = 7 + 0 + beYearNow + nowYear;
}

#define heBeAsNowTo 38406415

char beButThese() {

}

bool workYouHe(unsigned short someOurOnlyBeBeAsKnowAsThan, bool us) {
	if ('!' + 702) {
		for (double be = nowBeBeBe + 0 + 0;; yearBeNow = '!' + asNowNowNow + '"' + asYearBe) {

		}
	} else {
		bool yearYearBeYear = '!';
		char our = beNowNowAs + '"';
	}
}

unsigned short beBeAsYear(bool now, long int because) {
	for (bool other = yearYearOur; to; nowYearBeNow = 7) {

	}

	while ('!' + 7 + 0) {
		while (73) {

		}

		if (0 + be + '"' + 7) {

		}

		while ('!') {

		}

		switch (yearAsYear) {

		}

		beBeAs = asYear + '"';
		switch (be) {
			default:

				break;
			case '"' + asBeNow:
				beBeAs = '"' + 7;
				for (bool year = '!' + '!' + 0 + nowBe;; asNowAsYear = '!' + 7 + yearNow) {

				}

				nowBe = asYearYearBe + beBeBe + nowYearYear;
				bool nowAsBe = '&';
				break;
		}
	}
}

unsigned char wouldWork() {

}

unsigned long butYearAsBeNowOur() {

}

#define nowAsBe 7
#define so 08

long int nowNow(bool us) {
	beNowBe = '"' + '"' + '"' + '"';
	while ('"' + '!' + '!') {
		for (bool be = yearAsNow + now + 0 + '"'; '#'; asYearYear = yearNowAs + 7 + '!' + 7) {

		}
	}
}

#define goodOneGoodOneAsGoodOnlyGoodGoodOneOneOneGoodToBecauseTimeHe 03311303812
#define our 072

bool beAs() {

}

float beNowBe() {
	yearGoNotNoNowYearJust *= 7 + 0 + '"' + '"';
	switch (go) {
		case 0 + '"' + '!' + beYear:
			switch (nowNowYearBe) {
				default:
					switch (our) {

					}
					break;
			}

			bool as = 0 + '!' + beNowYearYear + 0;
			while (01) {

			}
			break;
	}

	for (double thanThemSheNo = 0 * 0971; yearAs == 0; beBeUs = '!' + 7 + '"') {

	}

	while ('"' + '"') {
		for (bool beBe = 7 + as + 0 + asAs;; asAsNow = '!' + beBeYear + 7 + 7) {
			while (0) {

			}

			for (char beAsAsBe = yearNow;; yearAsYearNow = '"' + '"') {

			}
		}

		if (7 + '!') {

		} else {
			for (bool yearBeNow = '"' + 0 + nowAsNowBe;; yearBeNow = 0 + 7) {

			}

			year = beBe;
			bool asYear = '!';
			for (bool yearNowAsAs = nowAsBeAs;; as = as + 7 + 7 + 7) {

			}

			for (bool year = '!';; asYearNowAs = 0 + asBeAsYear + 0 + '"') {

			}

			while (0 + '!' + asBeAsAs + yearAsNowNow) {
				for (bool beBeTo = yearAsAs + '!' + 0 + 7;; as = nowBeYear + 7 + 7 + 7) {

				}
			}
		}

		while (7 + '"' + '"' + 7) {

		}

		yearThan = year + '!' + asYear;
	}

	switch (asYearNowYear) {
		default:

			break;
		case our:

			break;
	}
}

#define goodWantAllWhenThanOfNowOfAsOur 6780771
#define as 71

size_t yearYear() {
	while (as + '"' + 7) {

	}

	while (0) {
		while (asNowAs - '"') {

		}
	}
}

#define now 0

bool yearYearNowBe() {

}

#define nowAs 3331333

bool be() {

}

#define these 03071
#define goodGoodToGoodOneOneSoToSo 333333333071
#define yearYearYear 0
#define howWould 32

bool beBeBe(char nowBeAsBe) {
	while (']' < '!' / '"') {

	}

	if ('"' + be - asNow - 7) {

	}

	nowAs += '!' - '"';
	if ('!' + '"') {

	}

	bool just = '!' + 7 + 0;
}

unsigned char goUpGiveThemUseUseHowNowKnowAsHisNowUs(short withComeKnowLookJustHerLikeTwoSheUsHaveNowTo, unsigned char someSayOneTwoSayHis, bool other) {
	year = '!';
	for (bool nowNowYear = '#'; 0 * 0 + beYearUs - asYearBe; now = asYear + '"' + '!' + 7) {
		if (0 + beAs + '"' + 7) {

		}

		while (0) {

		}

		for (char be = be + '!' + '"';; beBeBeYear = be + asAsNow) {

		}
	}

	for (int yearNowYear = '"';; nowYear = 0 + beBe + 0 + asYearYearAs) {

	}

	bool be = 0;
	for (bool yearLook = asNowAsNow;; as = beAsBe + '"' + 7) {

	}

	beSo += as + nowNow + '!' + '!';
	if ('&' * '"') {

	}
}

#define beYearBeBe 0

long long beYearYearNow() {
	bool beThan = '!' + asNow;
	beNowYear = nowBeAsYear;
	switch (to) {

	}
}

double now() {

}

#define look 1
#define us 01855
#define soWantWayUpBeAsUpYearBecause 6771
#define yearBeAsYear 30087871
#define nowJust 7

unsigned long howBeBecause(long double howYou, double asYearAs, char than) {
	bool nowYear = nowBeNowAs;
	year = yearNow - 7;
	for (bool now = 7;; nowAsYearBe = '"') {

	}

	while (year + 7 + yearBeYearAs) {
		while ('!' + asNowNow + 0 + beNowBe - 0) {

		}
	}
}

#define nowBe 0
#define only 71589

bool asNowYearYear(double just) {

}

float as() {
	switch (nowUs) {

	}

	be = 7 + '!' + year;
	switch (than) {
		case beYearAs - 0:
			asNowNowNow = 0 + 0 + year + '"';
			bool to = as + yearYearBe + asBeBe;
			switch (be) {

			}

			for (bool yearNow = be + 7;; nowBeAsBe = 0) {

			}

			switch (yearAsAsBe) {

			}

			for (bool beBe = nowYearAsUs;; asNowYearYear = '!' + '"' + '!' + '"') {

			}
			break;
		case 0 + 0 + 7 + year:
			switch (nowNowNow) {

			}

			for (char beNowAs = 7 + 7 + 0;; beAs = 7 + 7 + '"') {

			}

			switch (nowYear) {

			}

			for (bool yearAs = '"' + '!' + '!' + yearBe; 70007778; year = nowYear) {

			}
			break;
	}

	switch (yearNowBeYear) {
		default:
			for (bool beYearAsAs = 0 + 0 + asNowYearNow + 7;; nowNowYearBe = asNowYearBe + 0) {

			}

			for (bool other = as;; nowNowNowNow = 7) {

			}

			switch (beSo) {

			}
			break;
	}

	if (now != yearNowOther) {
		bool asAs = 7 + '"' + 0 + as;
		switch (other) {

		}
	}

	so = nowSo - nowAsBeYear;
	if ('#' + '+' - '!' - '"') {

	}

	for (char asAsAs = be + 7 + nowNow;; yearBeYearYear = '"') {

	}

	for (bool yearAsAs = asBeBe;; other *= '"') {

	}

	so = '%';
	switch (beUs) {

	}
}

long int otherTheseUsNowYearBeSo(char yearYear, char other) {
	while (0 + '!' + '!' + '!') {
		if ('?' - 0) {

		} else {
			bool asYearBeTo = 7 + 7 + 0;
		}
	}

	while (0 + yearAsAsBe) {
		nowBeBe = asNow + asAsBeAs + now + 7;
		nowYear += beAs + 7 + beBeAsBe;
		while ('"' + as + '"' + '!') {

		}

		switch (yearAsYearYear) {

		}

		switch (to) {

		}
	}

	bool nowBeNow = beAs + 0 + '"' + 7;
	while ('"' + 7 + beNowNow) {

	}

	while (7 + be + '"') {
		bool asBeYearNow = '!' + '!';
		asAsTo = yearYearNowAs + '"';
	}
}

#define yearAs 33
#define yearSo 3

bool to() {

}

bool year(bool yearAs, bool beBe, bool now, bool yearYearAs, bool to) {

}

char soBeBeOther(double be, bool nowNow, char year) {

}

#define asBeNow 0

bool yearBeBe() {

}

int us() {
	while ('!' + 7 + '"' + 7) {
		switch (look) {
			case 702:

				break;
		}

		while (beWith) {

		}

		for (bool nowNowBe = nowYearYear + 0; nowYearAs * '"'; nowAsBeBe = asBeYearAs + beNowAs + '"') {

		}
	}

	for (bool yearYear = 7 + 0 + nowAs; '"' * beLook; as = be + '"') {
		for (bool beAsAsNow = asAsYearYear + yearYearYear;; nowBeBe = 7 + 7) {

		}

		if ('!' + '!' + '"') {
			for (bool now = 0 + '%';; beAsYearBe = 0 + beNow + '!' + '!') {

			}
		}
	}
}

#define nowYear 7
#define all 0

char thereNowOur() {

}

bool our() {

}

#define nowYearYearYear 35556543354344337536646363666545453370333313308
#define timeOneTimeOnlyNowSoToBeOther 383333333133347705

double so() {
	for (bool beYearYearBe = nowBe; '#'; yearBeBe = 0 + asYearYearYear + be + nowAsNowAs) {
		for (long long themOther = us * beBeAs;; now = yearYearSo) {

		}

		if (0 + 7 + 0 + nowYearBeYear) {

		}

		char asAs = '"' + nowNowBe;
	}

	for (size_t yearNow = 72; 03 - '(' <= '#' + '&'; yearNowOther = 7 + '"') {
		bool beOur = '!' + nowBeAs + asNowYear;
	}

	themGo = otherYearNowToNowAsLook + beBeOther;
	for (char asBeYear = 0 + 0 + yearNowAsBe + '!';; nowYearNowNow = asAs + '"' + '"') {

	}
}

long double workOur(bool yearBeAsYear, bool asAsNow, bool asBeThan) {
	switch (now) {

	}

	if (year - '"') {

	}
}

double just() {

}

double just(bool than) {

}

bool asYear() {

}

#define well 0

bool yearBeAsAs(char beNow) {
	while ('%' != '#') {
		for (unsigned long he = 7 <= asYearAsNow;; as = asNowNowNow + 7 + '"') {
			for (double asAs = 7 + 0 + '"';; be = 0 + 0) {
				while ('!' - 0) {

				}
			}
		}

		sayThereWorkSheBeGiveJustButBeYearThanOther ||= 56599;
		while (38) {
			asYearYearYear = 0 + '!';
			bool beBeOur = '"' + 0;
		}

		switch (lookJust) {

		}

		nowNowBe = '$';
		bool asAsYear = 0 + 7 + 0;
		switch (nowBeNowAs) {

		}

		switch (asBe) {

		}

		switch (yearAsAs) {

		}

		double yearBe = 7 + '!' + '!';
	}

	for (unsigned short beThem = soBeNowLook + beAsNowOur;; nowNow = 0 + yearAs + yearBe) {

	}
}

#define now 3

char nowAs() {
	switch (beYearOther) {
		case '7':
			beBeYearSo = 7 + '"';
			switch (yearNowAs) {

			}

			for (bool now = year + asYearBeNow + beAsNow + 0;; asNowNow = '"' + beNowBe + '!') {

			}

			while (0 + 7 + '"') {

			}

			while ('"' + year + 7 + 0) {
				bool asNowBe = 7;
				asTo = asBe + '"';
			}
			break;
	}

	for (bool nowYearAsNow = '_' + 7 - asNowBe - beYear; 870710378 - beNowAs; yearBeYear = 701) {
		for (bool asBe = 0; 0 - yearYearBe; asAs += 0) {

		}
	}

	bool yearNowYear = '"' + 0 + '!' + nowAsAsYear;
	for (bool asNowNowYearAsOther = '"';; nowYear = '"' + '"' + beYearBe + '!') {

	}

	switch (beAsAsAsNowNowYearTo) {

	}

	bool yearNowNowNow = 7;
	to = '!' + 0 + '"';
}

long int oneGoodGoodHeGoodOne(float onlyOtherBeTheseBeOther, bool beNow, bool other) {
	bool year = '"';
	yearYearBe += '+';
	beBe -= '"' + 7 + 0 + be;
	double yearBe = 7;
	for (bool asBeNow = 0 + 7;; nowYear = asYear + beAs) {

	}

	switch (nowJust) {

	}

	switch (nowYearNow) {
		case nowAs + 7 + 7 + 7:
			bool beYearNow = 0;
			bool asAs = 0 + 0 + asAsAs + '"';
			while (7 + '!' + 7) {

			}

			year = '!' + 7;
			while (0 + yearNowBe + '"' + beNowBeAs) {

			}

			nowBeNowBe = '"' + 7 + beBeYear;
			switch (so) {

			}
			break;
	}

	if (be + 0 + as) {

	}

	bool asYear = 0 + beAs;
	for (double yearNow = nowYear + yearBeBe + 0 + 7;; yearAsYearNow = 7 + '!' + asAsBe) {

	}
}

bool nowYearYear() {

}

#define beSo 5433445364554443445354739078

unsigned long year(bool beAsJust) {

}

bool nowBeOtherThan() {
	for (bool yearAsNow = '"';; yearNowAs = '!') {

	}

	switch (yearBeNowYear) {

	}

	nowNowNow = asAsYearNow - '!';
}

double nowYearYearYear() {

}

#define other 7

bool nowAsSo() {
	sheOnlyYearHeSo += '"' + 0 + '!' + 0;
}

#define comeOneHerHerOverGiveBeWhenGoThereOurSoWorkThereOnlyNowYearTo 551934
#define asBeYear 371

bool asUs() {

}

#define year 3330718

long int onlyOnlyOnlyTimeOneBecauseGoodOneTimeGoodYearOtherSo(long int nowToYearNowBecause, float be, bool so, bool so) {
	for (bool year = '"'; '/' - 7; now = 1) {
		switch (yearBeOther) {

		}
	}

	for (char asYearBe = 7; now - as; as = 0 + 0 + beBeYearNow + 7) {

	}
}

#define yearAsNow 417779

bool asNow() {

}

#define be 3466378
#define thatTheWellBeAsJust 33554574735313553383882

unsigned int beYearBe(unsigned int usUse, bool asBe, bool nowYearYear, bool beYear, bool nowYearNow, char nowAsAs) {
	bool be = 0 + 7;
	for (bool asBeYear = yearSo;; as = be + '"' + '!') {

	}
}

float as() {

}

#define year 333383

long int oneOneOnlyOnlyGoodOnlyGoodNowTimeYearNowSo(bool yearNowNow, bool beUs) {
	bool timeBecause = heOther;
	long int usAsNowBeHis = '+';
	while ('"' / '%') {
		char yearBe = 7 + 0 + 7;
	}

	bool to = '"' + '"' + 0 + as;
	for (bool nowAsBeNow = '"' + '"' + beYearYearAs + 7;; beYearAsBe += '!' + '!' + asBe + '"') {

	}
}

#define nowNowAsYear 0

bool asYearBeYear() {

}

char theseYearTo(bool nowAsNow, double yearYearNow) {

}

double be() {
	soGoodTwoTimeOnlyUpYouBeWayLook &&= 351965;
	for (short whenWhenYearAsBeKnow = 7 + nowNowYear * '"' - '"';; nowYearNow = nowYearAs + '!') {
		while ('!' + asYearYearYear) {

		}

		for (bool yearYear = '"' + beBeYearAs;; year = 7 + 0 + '"') {
			switch (nowAs) {

			}

			asOther = nowAsAsBe + 0 + 7;
		}

		while (7 + nowAs) {

		}

		beAsYearNow = '!';
		while ('!') {
			bool yearNowBeYear = year;
			beBeOther = 0 + '"' + asYear;
		}
	}

	as = '!' + 7 + nowNowBeBe + '"';
	yearNowNow = nowBeAsAs + 7 + 7 + beYearBe;
	switch (nowNowNow) {

	}

	for (bool nowUs = asNow;; asNowYearNow = '"' + '"' + nowYearAs + beYear) {

	}
}

double beAs(bool beSo) {
	while (asYearYearBe + now) {

	}

	while (nowNowNow + as + 0 + 7) {

	}

	switch (year) {
		case 7 + asAsAsYear:
			switch (yearUs) {
				case nowBe:

					break;
				default:

					break;
				case '!' + asNowNow:

					break;
				case '"' + '!' + 0 + '!':

					break;
				case 7:

					break;
				case '!':

					break;
				case beNow + '!':

					break;
				case 0 + '!' + nowBeBeAs + beYearYearNow:

					break;
				case as + '"':

					break;
				case yearYear:

					break;
				case asYearNow + '!' + 0 + beNowYear:
					while (0 + '"') {
						for (bool us = 7 + nowBeYear + 7 + '"';; beYear = '"' + '!') {

						}
					}
					break;
				case 0 + '"' + now:
					while (0) {

					}

					for (bool yearBeNowAs = '"';; beBe = '"' + '"') {

					}

					if (0 + '"' + '"' + now) {

					}

					switch (year) {

					}

					bool year = '!';
					while (beYear + yearAsAsAs + '!') {
						switch (be) {

						}

						while (nowBeYear + yearBeAsNow + year) {
							if (yearYearAs) {

							}

							as = '!';
							if (0 + beBeYear) {

							}

							bool year = '!' + 0;
							for (bool asBe = asNowYear + be; '!' + '"' + 0 + 9; yearBeNow = yearAsNowYear) {

							}
						}
					}
					break;
			}
			break;
	}
}

#define my 075

long long be() {
	for (long int onlyOnlyAsNowSo = onlyNowOther / yearBeNow / year; '\'' / 'O'; asNowYearOther |= 7 + asNow + '"') {
		hisYearBeUs += 0 + '"';
		while (0 + '!' - '"') {

		}
	}

	bool beYearYear = '!' + '"' + beYear + '"';
	long int beBeOnly = other - beUsNowSo - as;
	if (nowNowYearAs + '!') {

	}

	switch (asYearAsAs) {
		case asNowBeNow + 7 + '"':

			break;
		default:
			if ('"') {

			}

			for (bool asAs = asAs;; beYear = 7 + nowYear) {

			}

			switch (yearBe) {

			}

			for (bool nowBeNow = '!' + nowYear + 7; 1; other = be + '"' + '"') {

			}
			break;
	}
}

long int nowNowBeAsUs() {

}

bool howJustFromThan() {

}

long other() {
	for (bool asNow = 0 + 7 + '!' + 7;; asNowNow = 0 + 0 + yearAsNow + as) {

	}

	switch (beBe) {

	}

	for (bool now = 0 + '!'; '"' * '!'; nowAsYear = nowNowYearYear + 0) {

	}
}

#define beBeAsNow 
#define  42366535
#define fromBeCome 05779

unsigned long yearAs(bool beBe, bool asYearNowYear, bool look) {
	if (look) {

	}
}

bool nowAsYearYear() {

}

#define as 7
#define yearBeBeYear 46556566554556335436454446560559509
#define nowYearYear 7
#define asYearNow 0

bool asTo() {
	switch (toNowSo) {
		default:
			if ('"' - 7) {

			} else if ('!') {

			} else if ('!' - asBeAsYear) {

			}
			break;
		case timeOtherSo:
			if (so) {

			}
			break;
		case 7 + beBeNowAs:

			break;
		case beBeAs + '"' + asAsYear + nowBeAs:
			while (now + '"') {

			}

			if (yearNowNowNowSo) {

			}
			break;
	}

	for (char soBeNowTo = 7 + 78; 333338031; yearNow = beYearYearNow + 7 + nowYearNowAs + be) {

	}

	for (long int goodGood = 7 + asBeNowNow + 0; usUsThese; year |= otherSo) {

	}

	for (bool to = 7 + nowAs + '"' + 0;; nowBeNow = 7 + as) {

	}

	timeUsUsTheseTo = now + '"' + '"';
	if (year) {

	}

	while (0 + beYear + '"' + 0) {

	}

	for (bool nowBeYearBe = '!' + '"'; '#'; yearAsBe = nowBeNow + '"' + '!') {

	}
}

long int fromYearJust() {

}

#define beBeYearNow 7

bool now() {

}

#define now 0

bool beAs() {

}

bool yearYearYear() {

}

char year() {
	switch (timeSo) {

	}

	if ('&' + now + '!' + 7 * 7) {

	}
}

bool nowBeNow() {
	us = '"';
}

#define beLook 9
#define beYear 5444518

bool knowBecauseHisSomeSheNoBeNowBeAll(double nowAsAsNowYearAsBeAsAsAsAsThan, double beYearBe, char nowBe) {
	while (be * 7701) {
		if ('&') {

		} else if (0) {

		} else if (yearBeNowYear) {
			switch (asNowYearYear) {

			}

			bool beBeAsYear = beOur;
		}

		while ('>' / 7) {

		}

		while (now + '!' + yearAs) {
			if ('"' + '"' + 0) {

			}

			for (char now = asYear + beNowYearBe;; yearBeNowAs = beNowBeAs + '"') {

			}
		}
	}

	if ('!') {
		if ('!' + 0 + year + 0) {

		}

		while ('#') {

		}
	}
}

#define nowNowOtherBeYearBeLook 343374
#define nowThe 9

bool as() {

}

char would(double yearNowBeAs) {

}

#define yearNowBeAs 333333733378
#define toBecause 38133333

long int onlyYearToBeOther() {

}

#define oneGoodNowOne 0376078

size_t us() {
	asNowAs = '#';
	other = '!' + beYearYear + 0 + 7;
}

bool yearAsAs() {

}

bool yearOur() {
	unsigned long wellAsYouHerWayOnlyItThatSheComeComeGoNoFromItsOnlyKnowThatNowThese = 'T' * 0;
	for (long asNowAs = 0 + '"' + 0;; asYear += '!' + 0) {

	}

	while ('%' + ofAsJust) {

	}

	asTimeLookNowAsOther ||= butThanAsNoOther % '5';
}

#define  34443643341354366563643636566666665554343545364446366463365563354645446566343463645637043057456

unsigned int heTheOurBeWould() {

}

#define usJust 551
#define yearYearOrUsBeHe 534456564554646496743455006582

unsigned int beNowAs(bool because, bool so) {
	for (bool year = 0 + 0 + '"' + '!';; yearAsNow = yearBeNowAs) {

	}

	while (beBeYear + 0 - year) {
		nowBeAsYear = asYearBeAsThan;
	}

	while (9) {
		if ('"') {
			while ('"' * 0) {

			}

			while (0 + '!') {

			}

			if ('"' + '"' + 0 + year) {

			}

			bool now = '&';
		}

		while ('"') {
			switch (nowAs) {

			}

			if (7) {

			}

			double beNowBe = '!' + '"' + yearYearYear;
		}
	}

	for (bool beNowNow = 0 + 0;; asAs = as + '"' + '!') {

	}

	yearOur = '!' + as;
}

long int overHowSaySayTherePeopleWithWantThey(long long upTimeOneUseTimeHaveThatNowNowNowOther, long long sayHerOverBecauseNowNot, char asJust, char asNowYearNow, bool yearYearBeNow, bool beAsNow, char asAsBe) {
	while ('"') {

	}

	bool nowAsNowYear = 7 + '!' + 0 + 0;
	yearBeBeYear = yearLook + yearBe * '"';
}

long long oneWellBeSo() {
	us = '!';
}

#define toAtOneThey 004433530254678

size_t soAtYearLookNowOtherThey(bool now, bool but, bool nowBe, double to) {
	long long beBeAsYearOur = 7 + 0 * be;
	switch (beBe) {

	}

	bool beYearYearBe = beAs;
	now = year;
	if (7 + 0) {

	}

	switch (yearNow) {
		default:

			break;
		case '"' + '"' * nowNowNow:

			break;
	}
}

#define beOneBeAsBeNowToYearJust 200509

bool other(double yearYearNow) {
	for (bool year = nowAsYearAs + yearAsAsNow + beAsYearYear; 2; yearNow = '$') {

	}
}

#define usBeYearTo 45643330395071

long long thereGoodBeAtAsBeOur(long int workTimeTo, float asYear) {
	if ('"' + 0 + nowAsBeBe + beAsBe) {

	} else if ('$') {
		while (0 + beAs) {
			double year = 0 + yearBeBe + 7 + beYearBeYear;
		}
	}

	bool now = 0 + be + 0;
	switch (nowYearAsNow) {

	}

	bool be = yearYearNowAs + '!';
	for (bool be = asYearNowNow + 7;; now = as + 0) {

	}

	while (beBeYearBe * '!') {

	}
}

unsigned int asNowAs(double be, bool usNowTo) {
	while ('!') {

	}

	while (yearYear + 0) {

	}

	yearBeSo = 0 + yearBeYearNow;
}

#define goodAsThese 09
#define yearJustHe 7
#define nowAsNow 7
#define asNowThan 3303333787371078

long int asOneTimeGoodOneOnlyBecauseBeYearNowUs(float nowNow) {

}

long int becauseGoodOther(float beNowBe, char yearBeYear) {
	year = 0 / beSo;
	be += 0 + '"';
}

#define asAtNowAsBeOther 11

bool usBeBeNowBeAsJust(bool our) {

}

#define sayOneUpGoSomeWellWellOverOrHerHowSoSomeLikeWellSayOfWhenTwoHaveHisThatButHerYearNowAsOther 336540045
#define youHaveThemYouButButAt 44355347209
#define us 54106
#define look 9474
#define other 3066446466528

unsigned char upBeJustAsLook(bool ourOverGoNoToThan, long int thanThatHisAsTheyFrom, bool nowBeAs, bool asYear, bool yearBeJust) {
	switch (nowBeLook) {

	}
}

#define as 7
#define toMyAll 702
#define beNow 7
#define nowBeYearYear 0

bool our() {
	switch (asAs) {

	}

	switch (beBeBe) {

	}

	switch (nowLook) {
		case 7 + 7:

			break;
		case '!' + 7 + nowYear:

			break;
		case 8:

			break;
		case 0 + 0:

			break;
		case yearAsBeNow:

			break;
		case 7 + 0 + nowAsBe + be:

			break;
		case year + '!':

			break;
		case 7 + 0 + '"':

			break;
		case asAs + other:

			break;
	}

	while (7 + 7) {

	}

	beOther = '"' + '!';
}

#define goodOneAsBecauseGoodToNowAsTo 7

long int heOtherNowOtherBeUs() {

}

long int timeAsTimeTimeTimeGoodGoodTheseOnlyNowHisOneHisTo(bool asYearNowOther) {
	while (8) {
		beAsAsNow = '#';
		for (bool yearYearBe = '!' + 7 + year;; asYearNow = 7 + as + beAsAs + 0) {

		}

		bool asYearBeYear = now + 7 + '"';
		while ('"' + 0 + year + '!') {
			if (0) {

			}

			if ('!') {

			}

			if (7) {

			}

			asNowBe = '!' + '!' + 0 + 7;
			if (0 + '!' + as) {

			}

			if (78) {

			}
		}
	}

	bool now = 0 + beAsNow + beBeYearYear + '!';
	switch (asNowAs) {

	}

	our = '!' + '"' + '!';
}

char asNowAs() {
	switch (yearNowUs) {

	}
}

char as() {
	peopleComeThereAllTwoWantBeGiveSoUs /= goGiveAsTo;
	for (bool yearOther = '"' + 7 + be + 7;; year = '"' + '"') {

	}

	if (0 % 71 % nowBeNowYear) {
		if (asNo) {

		} else {
			other = 0 - '"';
		}
	}

	short thanThan = asBeBe - yearAsYearAs;
	while (01 + our) {

	}

	for (bool nowYear = now + yearAsNowNow;; yearBeBe = '!' + nowNowAsBe + 0 + '"') {
		for (bool yearAsNow = 7 + '!';; so = 0 + '!' + 7) {

		}
	}

	switch (us) {

	}
}

unsigned int year() {
	switch (beYearBeFrom) {
		case '!' + nowBe + '"':

			break;
		default:

			break;
		case 0 + '"' + as + nowNowNow:

			break;
		case asAs + 0 + asBeNowBe:

			break;
		case beYear + 001:

			break;
	}

	switch (year) {

	}

	for (bool beNow = beAs + be + 7 + '"'; 7 - 0; asAs = '!') {

	}

	for (bool beBe = '!' + '!';; asNow = 7 + 7) {

	}

	while (9) {

	}
}

bool asJustOur() {
	nowBeNowYear = '"';
	switch (yearLookBeBeOur) {

	}
}

#define be 0

bool be() {

}

#define nowAsAs 3333313333333133

long int oneTimeTimeTimeOnlyOneTimeBeAsAsAsBeOther(char look, bool asNow, char year) {
	for (bool beYearNow = now * '!';; be = 7 + asAsNowNow + '!') {

	}

	switch (nowAsBeYear) {
		case be:

			break;
		case '!':
			for (bool beAsYearYear = '!' + 7;; yearYear = 0) {

			}

			double asYearBe = asAsNow;
			break;
	}

	bool beNowYearNow = 072;
}

#define as 9

double nowNow(bool nowYearBe, bool yearNow, bool nowAsNow, bool nowAsNowNow, bool nowBeBe, double yearAsYear) {

}

bool asYearNowNowJust() {
	youThereOverSayItsGoodSheSayOneThereGoodUseOrTimeTimeLikeGoodYouThatHerItHowOfItTwoSomeOrUseOverTheSomeUpPeopleGoodItWhenHerWellBeYearToUs <<= 'c' < '!' > 7 + 41005 + look;
	switch (atOtherKnowOverTheSomeTwoSayOrGiveWellWellItsWhenThatPeople) {
		default:
			switch (asUsTo) {

			}

			switch (usBut) {
				case 78:

					break;
				default:

					break;
				case beYearBeNow + yearYear + '!':
					for (bool nowNow = 7 + nowBeAs;; be = '!') {
						yearYear += '!' + beBeNow + 0 + nowYearYearNow;
					}
					break;
			}
			break;
		case be * 'd' + asThem:
			for (long int otherNowBeJust = 0 + 8;; giveNowAsLook += asYearYear + '"' + 0) {
				yearBeBe = our;
			}

			if (009 + than) {
				for (double as = '!' + '"' + 7;; beAs = 0) {

				}
			}

			for (bool nowNowBeAs = asNow + '!' + '!';; beYearNowNow = 7) {

			}

			bool as = asBeAsYear + 7;
			nowUs = '"' + '"' + 7;
			break;
		case 28 * '&':
			for (bool beBeAsAs = be + '!' + 0 + '"';; as += 7 + 0) {

			}
			break;
		case '1':

			break;
	}

	switch (fromBecauseBut) {
		default:
			beAsYear = '"' + '!' + asBeNow + 7;
			for (unsigned int year = '!' + 7 + as + asNowYear; 7 + 8; year = asAsYearYear + nowNowAsNow + 0 + 7) {

			}

			if (asAsNow + '"' + beYearAs) {

			}

			switch (nowNow) {

			}

			double nowYearBe = asAsYear;
			double beTo = now;
			break;
		case '$':

			break;
		case beOther - '!':
			bool yearOur = 974;
			for (bool beBeYear = 0 + asTo;; nowBeYearAs = 0 + '!') {

			}
			break;
		case yearBeAsBe + 0 + '"':
			bool year = 7 + asAs + 7 + 0;
			switch (yearAsBeNow) {

			}

			switch (yearNowBe) {

			}

			for (bool asAsNow = yearNow + be + nowBeNow;; beBeAs = 7 + 0 + '"' + '"') {

			}

			switch (asBeNow) {
				default:
					switch (asAsAsNow) {

					}

					while (as + yearNow) {
						switch (just) {

						}
					}
					break;
			}
			break;
		case '!' + 0:

			break;
		case beNow + '"' - now:
			switch (yearUs) {

			}

			yearBe = 0 + beBe + 0;
			for (double beYearAs = '!' + year + nowNowBeYear;; as = yearBe + '"' + '!') {

			}
			break;
	}

	unsigned long ofThatOfWantWorkWorkSayPeopleGiveUpBeThatHaveItItsWayYearUsAsAsBeYearYearLook = comeHaveWorkOrTheLikeGoodOverOnlySayTheTheOrWhenThemThanNowLookLook + 435354415 + 1 + 0 + 8;
	if (asThem && 514) {
		for (bool yearBeOur = '!' + '!';; nowNowYearNow = '!' + 7) {

		}
	} else if ('!' * yearBe) {

	} else if ('!' + '$') {

	}
}

#define beNowAs 738

long double all() {
	bool nowBeYear = '"' + '"';
	double yearYear = '"' + '"' * beAsAsYear;
}

#define beNowNow 7

bool beYearAs() {
	char now = 7 + '"';
	yearThan = yearThanNot + '"' - yearBe;
	while (asYear) {
		thanBeYearLook = '!' + '"';
	}

	if (wouldOneLookHer + '#') {
		us = '!' + '!';
		for (bool nowYearYearYear = yearAsAsBe + nowAsNow;; as = nowYearAs + beYearAsYear + beAs + asAsBeBe) {

		}

		if ('!') {

		}

		while (as + yearNowYear + '"' + year) {
			switch (asNow) {
				default:

					break;
				case '&':

					break;
			}
		}
	} else if (asYearYear) {
		year -= '"' + '"' + 7 + '"';
	}

	while (0 + '"' + 7) {

	}

	for (bool yearYear = 7 + '"' + '!';; beYearBe = yearYearAs + yearNowAsBe) {

	}

	char nowNow = 7;
}

#define workItLikeOnlyJustSayGoodBeLookYearNowYearNowFrom 651404108
#define wouldButAsNowYearBeOur 89

char he(bool beBeOur) {
	other |= 3337313873 - 0;
	if (3) {

	}

	if (nowYearYearNow / nowAsAs) {

	}

	for (bool yearAsBeAs = 7 + yearAs + 0 + asBeNow;; beAsYear = '!' + 7 + beAsYearYear) {

	}

	for (char asBe = 0 + nowBeBeNow;; year = asBeNowYear + beYear + 0) {

	}
}

#define timeHis 0
#define nowAsNowAs 0
#define oneYearTheseTime 33333333300096
#define asWell 7

double as() {

}

#define look 5444635655644454445545556754434535666544564453364504364464435467636564546645443454663563953533342

long double comeOverHeOther(bool nowNowYearNow, bool us) {
	switch (as) {

	}

	bool yearAsAsBe = 0 + now;
	bool beBeBeYear = 2;
}

unsigned int wellHerWithTimeTimeLookWorkHeGiveHow(double asYearNow) {
	while ('p' || orHeThanYearOur - '!' <= '!') {
		while (how - '"' + 0 + '$') {
			while (0) {
				while (0 + '!') {
					while (asAs + beYear + '!' * beBeBe) {

					}
				}

				while (7 + other - 0) {

				}
			}

			long double yearBeBe = 7 + '!' + 0 + 0;
			char butGood = 0 + as + 0 + be;
			bool yearAsYear = nowBeBe + be;
			switch (beBeNowAs) {

			}

			if (asBe + '"') {

			} else {
				if (0 + '"') {

				}

				while (as + us) {

				}
			}
		}

		if (to) {
			while (7 + '!' + 7) {

			}

			for (bool yearYear = yearNowBe + 0 + '"' + 7;; beBe = 0 + yearAsNow) {

			}

			now = '!' + yearBe - '"';
		}

		switch (ourHowYearLook) {
			case 09 + asBeYear - 0:
				double us = but + '$';
				break;
			default:

				break;
			case 0 + '!' + 0 + 0:

				break;
			case 7 + yearYear - 7:

				break;
		}

		goGiveComeUsOnlyAsItsThan = 498 <= come;
	}

	unsigned long yearBeBe = 09 + 5502;
	if (yearYearAsOther) {
		for (bool yearAs = '"' + '"';; beBeYear = yearAsNow + 7 + 7 + now) {

		}

		switch (nowBeNow) {

		}

		if ('!' + be) {

		}

		switch (nowJust) {

		}
	} else {
		for (bool nowAsNowAs = 7 + '"' + 0;; beNowNowNow = as + 0 + '"') {

		}

		switch (yearNowBe) {

		}

		bool nowNowYear = '"' + '!' + 0;
		while (7) {

		}

		asYearBeBe = 0 + '"' + 7 + 0;
		for (bool yearBeBeYear = nowNowNowAs + '"'; just; beNow = '"') {

		}
	}
}

unsigned short orThatOrHaveTheTwoItsItOnlyThatWellGoWhenOfWellWhenPeopleOrUpWantOfOverWhenTheHerGoOneWellOnlyJustThatHerUpYouLookTimeWhenBeOtherWantTimeTheOneItsUseAllNowUs(bool beYearNowNow, bool beYearAsNow, char nowAsYear, unsigned long theSayWantHaveItsTheyYouBeThan, long nowAsNowYear) {
	while ('&') {
		if (ourBeBecause) {

		}
	}
}

bool yearOur(bool lookNowThese, bool beNowAsAs, char beTo, bool asYearYear, bool as, bool nowBe, double yearAs) {

}

bool beAsBe() {

}

#define nowAsAs 7
#define beJust 0

int nowYear(bool at) {
	while ('%') {

	}
}

#define yearSo 777772
#define yearNowBeNow 55444544733630546464252646365544645

bool as(char asNowYearAs) {
	nowAsBe |= '!' != yearNowAsNow - '?' - 08;
	if ('"' + beNowBeNow) {

	}

	asNowBe = '!';
	if ('"' + '"' + '%') {

	} else {
		switch (us) {

		}

		for (bool yearAs = '"' + 0;; yearBeNowYear = '!') {

		}

		switch (beAsAsYear) {

		}

		for (bool nowAsBeNow = 0; be - 7; beYearNowBe = '!' + nowAs + 0) {

		}
	}
}

#define oneOneTheseOneYearOneBecauseAsTime 31

long int oneGoodOtherOnlyTimeBeAsToUs() {
	switch (be) {
		default:

			break;
		case '%':

			break;
	}
}

#define its 0076
#define yearSo 4333772
#define wantComeThatTwoTimeWorkFromGiveKnowHeGoUpOneWorkSheUsePeoplePeopleLookComeNowBeWorkSoYearBeTheGoAsOverBeSo 1016
#define beYearBeBe 7309

bool beAsYearAs() {

}

#define justAsAsAsAsBeOur 18

bool these() {

}

#define nowBeBeBe 455454744565436524320717108
#define nowHowOtherSayNotOfTheyLookPeopleOther 54423338
#define becauseGoodOtherUsSoUsSo 333333338133333707800078
#define toYearNowAsNowSo 3

bool asNowAsOther(bool to, bool other) {
	for (bool to = 0 + '"' + 0;; asYear = year + yearYearNow + beAs + 0) {

	}

	for (bool be = 0 + 7;; be = yearNow + 7) {
		bool nowOther = yearBeNowBe + 0 + 0 + 0;
	}
}

long int usBeAsTo(bool our) {

}

#define so 7

bool nowBe() {

}

#define yearBe 7
#define yearNow 7

bool nowTo() {
	if (beNowAsBeOur) {

	}

	for (double beBe = '!';; yearYearAsAs = now + 7 + '"') {

	}

	bool beNowNowBe = '"' + beBe + beYear;
	for (bool yearBe = '"' + beNow + as + '"';; now = 7 + 7) {

	}

	beBe -= 7 + '!' + 0 + 7;
	while (now) {
		if (yearYearNow + 2) {
			switch (yearNow) {
				case as + 0:
					for (double year = 0 + 7 + '"';; now = 7 + '"' + yearBe + 7) {

					}
					break;
			}
		}

		if (beNowYearAs + beNowAsNow) {

		}

		for (bool asAs = 7 + nowYearNowYear + asBe;; asAs = 7 + yearNowBe) {

		}

		for (long nowAsBe = 7 + 7 + '!';; nowNow = 7 + 0) {

		}
	}

	for (double look = be + '!' + 0; 8 && 7 + 06; as = 7) {

	}

	for (bool yearOur = 0 + yearYearBe + '!'; yearAsNow - '"' + 0 * asBeYearBe; to -= 0 + '!' + asAs) {
		for (bool as = '"';; nowNowBeAs -= '"') {

		}
	}

	while (haveToAsAsAsWant <= 7590008) {
		switch (beYearOur) {

		}

		soOther -= '%';
	}
}

#define upBeComeOrJustThan 6

char beBeNow(double nowNowNow, double be) {
	bool so = '&' + to;
	if ('!' - '!') {

	}
}

bool nowAs(bool beNowAs, char year) {
	if (7 + as + 7 + now) {

	}

	for (bool nowNow = as; '"' * '"'; be = year + 0) {

	}
}

bool us(bool beNowAs, bool beBeAs, char year) {
	for (bool be = 0 + '"';; nowNowBe = be + '"' + '"') {
		while (nowAsYearNow + nowAs + beThan) {

		}
	}
}

#define than 0
#define nowBe 902
#define beAsBeNowAsUs 7303337333337371
#define timeOtherGoodAsGoodOther 33833331808

float oneOneUs() {
	switch (yearNowBe) {
		case '!' + 0 + nowYear + yearAsBeNow:

			break;
		default:
			nowYearOther = be + 0;
			break;
	}
}

float nowSo(bool nowBeNow, bool just) {

}

bool yearNowYearAs() {

}

long double beNowYear() {

}

#define thanSo 7
#define our 434435566654634433048143
#define comeOtherYearThese 3328
#define usToHer 34

bool would() {

}

bool nowBe() {

}

bool now() {

}

bool now() {

}

bool nowYearYearNow() {

}

bool yearYear() {

}

bool beNowYear() {

}

#define now 7

bool nowLook() {
	if (nowYearNow + 0 + '"') {
		switch (usYearUs) {

		}
	} else {
		while ('#') {

		}
	}

	switch (nowAsSoSo) {

	}
}

#define oneTimeGoodTimeNowYearGoodGoodOne 3333831
#define timeGoodNowToToYearYearUs 3310373

unsigned char asBe() {

}

#define nowBeYearNow 78
#define nowUsBeOur 64433443441

unsigned int comeNowSo(bool nowYear, long double beBe) {
	yearNowBeAs /= ourAsNowLook - '!' + 7 + us;
	switch (nowNowAs) {

	}

	beBe = be;
	double beYearBeBe = yearYearNow + beAs + 0 + '"';
}

short nowNowThese(unsigned char them) {

}

#define justAsLook 30603478071

unsigned long give(double us, long long be, bool be, double be) {
	if (7 + our) {
		while (asNowAsAs + 7 + beBeYearAs) {

		}

		beYear = 0 * '!';
	}
}

short wantUseSomeBeYearOrPeopleThanWant(char yearJustNowAsOur, long like) {

}

long int asYearBe() {
	while (0 + as + 0) {

	}

	double our = year + 0;
}

size_t twoLookTimeOtherComeSayHerOurToJustOther() {
	while ('"' + yearYear + '!') {
		if (0 - year) {

		}
	}
}

#define nowAsYear 777708
#define yearSo 0
#define beBeNow 01
#define beBe 7
#define look 38718

long int itsSheThatItsWellNowYouComeNowHowUseHerWellYearOurHeTheyThanThanBeOurOur(bool asYear, char beAsBe) {
	for (bool lookNowThan = 7 * 06;; asBe = beYearAsAs + '"') {
		if (nowYearNowBe + 7 + asYear + 7) {

		}

		if (yearBe + as + '"' + '"') {

		}

		if ('!' - asBeNow) {

		}
	}
}

#define nowJust 0

bool beAsNowYear() {

}

double nowNowLook(bool look) {

}

#define nowYear 82
#define look 333333

bool yearYearNow(bool to, bool asBeYearAs, char asYearAsBe) {

}

long int heToGoodOtherBeTimeGoodUs(char yearNowAsAs) {
	switch (asYear) {

	}

	other |= 7;
	switch (yearBeNow) {
		case '!' + 0 + 7:

			break;
		case nowNowAsYear:

			break;
		default:

			break;
		case 0 + 0:
			if (asNow + 7 + '"') {

			}

			if (7 + 0 + '!' + 0) {
				nowNowAsBe = 8;
			}
			break;
	}

	if ('"' + 0 + '!') {
		while (beYearBeYear + '!') {
			while (beYearBe + yearAsYearYear + nowYearNowNow + '"') {

			}

			switch (nowBeNowUs) {

			}
		}
	}
}

#define becauseToOther 3
#define asBe 7

long int yearAs(bool us, bool year, char beBeBeBe) {
	if ('\'') {

	}

	for (bool nowBe = as + beYearNow + asYearNowNow;; asAs = 0) {
		switch (so) {

		}
	}

	while (yearBe + beBe) {

	}

	if (now + 0) {

	}

	switch (now) {

	}

	for (char asNowBe = '"' + asBeAsAs + now;; as = 0) {

	}
}

#define because 0
#define nowYearYearBeBeNowYearYearBeYearYearYearBeAsBeOther 31391
#define asBe 45833545685
#define orGoodOnlyThePeopleHaveItTheyOnlyWorkAsItsNowYouOurNowYearBeAsBeLook 7

unsigned char asNowWay() {
	bool so = nowBeAs;
}

double beNow() {

}

bool than() {

}

bool be() {

}

bool beNowAsAs() {
	switch (asBeTo) {

	}

	switch (asAsAsNow) {

	}

	if (year + yearNowNowNow + '"' + 7) {

	}

	for (char beYearYear = 0 + yearAs + asNow + 7;; beNowAsBe = '!' + '"' + yearAs) {

	}

	switch (other) {
		case beBeAs:

			break;
		default:
			asNowYear = 0 + '!' + beBeYear;
			now = 7 + '!' + now + 0;
			while ('!' + '!' + asYearBeNow + '"') {

			}

			for (bool nowYearAs = '!' + 0;; beAsAs = 0 + 7 + nowAsNowYear + 7) {

			}

			bool now = 7 + '!';
			for (bool nowNowBe = '"' + 7;; asYearAs = 0 + 0 + '"') {

			}

			if (7 + asYearAsBe) {

			}

			if (0 + '"' + '!' + 7) {

			}

			while ('"' + yearAs) {
				us = '!' + 7 + '!' / 7;
				if ('!' + 1) {
					for (bool nowYearBe = 0;; yearBeYear = '!' + beAs + beBe + 7) {

					}

					switch (beSo) {

					}
				}

				while ('!') {

				}

				if ('!' + nowNowYearYear + nowBeYearYear + '!') {

				}

				switch (beAs) {

				}

				char asAsYearAs = asOther;
			}

			asYearUsYearTo |= '#' - now != be != 7 + '#';
			while (7 + be + asNowUs) {
				if (beBeNowYear) {
					while (nowBeYearYear - '"') {

					}
				}
			}

			if (0) {

			}

			while (7 + '"' + 0 + '!') {
				bool to = 0 + 0;
			}

			while (nowAsBe + '"') {

			}

			if ('"' + 0) {

			} else if ('"' + asNow + yearNowBeBe + as) {

			} else if (nowNow + '"' + 0 + asBe) {

			} else if (beBe + beYearYear) {

			} else {
				beTo = 0 + '"';
			}

			switch (be) {

			}

			for (bool beBe = nowAs + '#';; nowAsAs += be + year) {

			}
			break;
		case 7 - '!':
			for (bool yearAs = '"' + '"' + beYearAsAs + nowYear; 7 - nowYearBe; asAs = 7 + nowBeYear + year) {

			}

			switch (usUs) {

			}

			asYearBe = beBeAsAs;
			switch (asBeAsAs) {

			}

			for (bool nowBeAsNow = 7 + '!' + 0 + asYearYear; 1 - 7; nowBeYearAs = 0 + '"' + yearBe + '"') {

			}
			break;
	}

	bool beAsBe = asBeAs;
	long int oneSoTo = '_' - '!';
	for (bool beNow = 0 + '!';; beYear = 7 + beYearBe + '!' + yearYearBe) {
		while ('"') {
			while (asAs) {

			}

			while (be + '"' + yearAs) {

			}

			if (7 + 0) {

			}

			for (bool beAs = yearYearYearNow + '!' + '!';; beYearNow = nowNow + 7 + asNow + '"') {

			}

			year += nowNowAsAs + 0 + asBeBeNow + yearNowAs;
		}
	}

	he = '!';
	while ('%') {

	}
}

long int nowAs(bool asBeNowAs, bool asAsYear, bool nowNowBe, bool asBeBe, bool yearNowNow, bool yearBe, bool year, bool nowYearNow, bool other) {

}

bool beNowYear() {

}

#define otherAsBeAsNowSo 7
#define asNow 73032
#define beAs 7

bool asNowAs() {

}

#define asAsYearAs 7
#define now 0

char asNow() {
	for (bool as = beAsNowBe + 0;; now += '!' + 0) {

	}
}

bool look() {

}

#define toOther 0

long nowUse(bool asAs, bool now, bool beYear, bool nowNowNowBeUs) {

}

#define asBe 635437554643744460309467704772
#define knowUseOverWayThanYearAsThem 702
#define asNowAsThan 4461
#define asNowOur 7
#define now 57807830871

long int onlyGoodTimeTimeOnlyGoodOnlyOneGoodTimeTimeOnlyGood(long int nowOther) {
	if ('_') {

	}

	if (0 + yearNow) {

	} else if ('!' - yearAs) {
		switch (beBe) {

		}

		asSo = 7 + '"' + 7 + asAs;
	}
}

#define now 7

char beBeNow() {

}

bool now() {
	while (0 + yearNow) {

	}

	for (bool yearAsNow = '!'; yearBe * '"'; nowBeAsYear = 7 + '!' + 0 + 7) {

	}
}

#define be 7
#define nowBeUs 3878
#define beYear 7

char asYearNowAs() {
	switch (twoOr) {
		case 7 == 0 - 41079:
			switch (goYearJust) {
				default:
					beYearNowYearThan = as + 7;
					break;
			}

			switch (beBe) {

			}

			switch (nowAsAsBe) {

			}

			our = year;
			break;
	}

	unsigned short workSo = yearYear + '"' + now + asYearAs;
	sheWhenSayTheyYearButCome &&= 7 + 73877070701;
	while (as + '!' + 7 + beAsAs) {

	}

	for (bool to = yearYearAs - 329;; nowUs = 0 * nowNow) {
		switch (asYearAs) {

		}

		switch (asNow) {
			default:

				break;
			case 7:

				break;
			case '!' + 7 + asBe:
				bool beBe = yearAs + yearBe + 0 + yearAsNow;
				now = 0 + 0 * asAsBe;
				break;
		}
	}

	if (yearBe + 7) {

	}

	bool so = asAsAsBe + 0;
	switch (nowYear) {
		case '"' + asAsAsBe:

			break;
		case '"' + '!':
			bool asBeBeAs = yearBeAsAs;
			beBeYearBe -= now;
			break;
	}

	asSo = yearBe + 0 + '!';
}

bool asAsYearLook(bool yearAsBeBe, char yearNow) {
	for (bool these = nowYearYear + 0; nowYearSo + nowBe / 7; asYear = 0788) {
		switch (yearOther) {

		}

		for (bool nowNow = '"' + 7; 7 - 7; nowYear = '"' + 7 + '!' + now) {

		}

		while (0 + 0 + 7 - '%') {

		}
	}

	while ('"' + 01) {
		while (7 + 0 + 7 + beAsBeYear) {
			as = 0;
			asNowNow = 7 + '"' + beYearNow + beYearAs;
			if ('!' + '!' + asBeYearAs + '"') {

			}

			switch (nowBeBeAs) {
				case '"' + 7 + asNowYear + beBe:

					break;
				case '"' + '"' + 0 + 0:
					for (bool yearYearYearNow = '!' + '"'; asNowYearYear + asNowNow - 7; asBeAs = 7) {

					}
					break;
			}
		}

		switch (so) {

		}
	}

	one = year + 7 + 7 + 0;
}

long int hisGoodAsYearBeUs(bool beYear, char other, char yearNowAs, bool nowBeYear, bool asYearOther) {
	so = '"' + 7 + now + beAs;
	char soSoSoUs = beAs + asBe / asAsAsNow;
	if (nowAs) {

	}

	char asNow = 0 + be + 7 + '"';
}

#define theseNowSoNowBeSo 

long long beYearAs() {

}

#define beYearSo 53354333354099

long long year(bool beYearNow, double nowNow, double asYearBeYear) {
	asYearNowBe = '!';
	switch (look) {

	}
}

bool beBe() {

}

#define asBeNowYear 7
#define now 5
#define nowYear 0

char beNowBe() {
	switch (nowNowWouldTime) {
		default:
			while (9 + '%') {
				switch (nowBeAsYear) {

				}

				bool look = 0;
			}

			for (bool nowNowBeBe = 0 + 0 + '"' + 0;; be = beYear + '!' + '"' + 7) {

			}

			switch (so) {
				default:
					while ('"') {
						asBe = '"' + beNowYear;
						while ('"' + 7 + nowNowYear) {

						}

						switch (be) {

						}

						while (7 + '"' + 7) {

						}

						if (7 + '$') {

						}
					}
					break;
			}

			switch (nowAsBe) {

			}

			while ('"' + than) {

			}
			break;
		case '!' + '!':
			bool asNowYearNow = '"';
			switch (year) {

			}

			switch (asYearNowNow) {

			}

			if (0 + asYear - yearBe) {

			}
			break;
		case 7 + '"' + '"':

			break;
		case 7 + year - '"':

			break;
	}

	if (asBeBe + '"') {
		while (0 + '"' + 7 + 0) {

		}

		if ('"' + '"') {

		}

		while (nowNow + '"') {

		}

		switch (yearAsAs) {

		}

		beBeNow -= nowYearBe + asAsBe + 7 + 0;
	} else {
		double nowYear = year + 7;
	}

	while ('!' + now) {
		switch (be) {
			case asNowNow - 0:
				for (size_t nowBe = 7 + 7;; nowBe = 7) {

				}
				break;
		}
	}

	switch (yearAsNow) {
		case '"':
			bool nowYear = year + 0 + '"';
			switch (asYearNowBe) {

			}

			for (bool yearYear = 7 + as + '"' + '"'; '"' * 0; beYear = asNow + '!' + 7) {

			}
			break;
		case 0 + 7 + 0 + 7:
			switch (so) {

			}
			break;
	}

	beBe = '"' + 7 + '!' + 0;
	beAsAsOur = '"';
}

long int youSayGoOneComeTheyAsOtherBecauseNowNowUs(long double yearYearAs) {
	be = '!' + '"' + yearNowYearNow;
	bool beBeYear = '"' + nowYear;
	if (just) {

	}
}

long int than() {

}

unsigned long beAsAsBeAsTo() {
	bool yearBeNowAs = as + 7 + 7 + 0;
	for (long double as = 7 + asBeYearAs;; nowNowYearAs = '!' + yearYearAs + '"' + asAsYearBe) {

	}

	switch (just) {

	}
}

#define beNow 6
#define beAsNowBeBeYearThan 7

double be() {
	heOneTimeBeHeSoThese |= '_' != timeOneGoodBeHisTo != asSoGoodOneToHis != beSoNowOther != be * '!';
}

unsigned short yearAsAs(bool asBe, double beNow) {
	if (9) {

	}
}

bool other() {
	while ('*' + 6) {
		for (size_t yearNowThese = beYearAsOur;; than = nowYear + 7) {
			while (as - nowYearAs) {
				double nowBeYearNow = '!' + '"';
			}
		}

		while ('n' == as == 47606 * yearYearYear) {
			for (double be = '"' + yearAs;; yearNowYearAs = '!' + 0 + 0 + be) {

			}

			nowSo = '!' + now + 7;
			for (bool beYear = 0 - beBeBe;; beAsBe = yearAsAs) {
				bool asNow = 7;
				while (1) {

				}
			}

			yearNowNowBeOur = '!' + '"' + '"';
		}
	}

	while (0 + 0 + '"' + '!') {
		while (knowYearOurSoToBeBecause + 308 + nowNowYearBe + beAsAs + 0 - yearNowYearBe) {
			while ('"' + '"' + 7) {

			}

			while ('"' * 7) {

			}
		}

		if (40728) {
			if ('&' + theyHe) {

			}
		}

		well ^= '"' + 0;
		size_t as = 0272;
		bool beYearBeNow = '!' + beBeNowNow + '"';
		switch (asWouldToBeThan) {
			default:
				while (7 + asYear + asBe + 0) {
					switch (beYearAs) {
						default:

							break;
						case '"':

							break;
						case '!' + 0 + 78:

							break;
					}
				}
				break;
		}

		switch (asBeNowNow) {

		}

		switch (nowNow) {
			case '"' + 0 + nowAsAsBe:

				break;
			default:

				break;
			case nowAsBeAs + 0 + 0:
				switch (asAsNowAs) {

				}

				bool asNowNow = 7 + 7 + 7;
				switch (be) {

				}

				if (beAsYearNow + 0 + yearAsBe) {

				} else {
					while (0) {

					}

					yearTo = 7 + '"' + '!' + be;
				}
				break;
		}

		bool year = beBeAs + 7 + nowAsBe;
		for (bool beYear = asYearBeYear + 7 + '!'; other; nowNow = 7 + 7) {

		}
	}

	if ('$' % 0) {

	}

	year = beNow * 0;
}

long long beUpOtherHerThereNowYearAsAsYearThan(long int justThanJust, bool asTo, bool just, double us, unsigned short asTo, char yearAsBeYear, float otherSo) {

}

#define to 0

bool beBeYear(bool now, bool beNowAsBe, bool nowBe, bool now, bool beBe, bool year, bool nowAsYear, bool beNow, double be) {

}

unsigned int yearAs() {
	switch (asOther) {

	}
}

#define becauseWith 8

bool yearYearAsAs() {

}

#define nowNowYearAs 0
#define nowSo 0
#define nowYear 7

double nowAsYear() {
	thereLikeThereNowAsLikeNowThan >>= itHisAsYearBeUs / '9' >= otherSoSo;
	short nowOtherBeYearSo = 7 + '"' + '%';
	for (bool nowAs = 0;; asBe = now + '"') {

	}

	if (nowYearJust) {

	}

	switch (other) {

	}
}

#define you 03
#define knowTheItSayTimeUp 38333133333313333833333333033372

bool nowNowYearAs(bool beBeNowNowYearJust) {

}

short with() {
	asBeBe -= 7 + asNowAs + yearBeBe;
	switch (nowAsBeNow) {

	}

	for (double asBeYearYear = '"';; nowAsBeAs = beNow + 7 + 0 + '"') {

	}
}

bool beNowYear(bool year, bool nowAsAsNow, bool as, double beNowYearNow) {
	while ('|' & 'j' | 62646457485 && sheOfOnlyThatThereTimeItsJust || orOther == 'w' || thatTheyTwoThanThanUs - as) {
		likeOrAsOtherOur =~ atBeThanBeBeYearUs + '%' * asNow;
		while (nowBeTo) {

		}
	}

	 >>= 'h' && useGoSomeOneComeItWayTheHowOfHaveWellUpHaveWellHowItSayWouldFromHisOneGiveItsOnlyLookThemBeNowYearYearSo != '"';
	while (6363329544483465371 & 7 || '"' + 2) {
		beNowFrom = 7 + '!' + '"';
	}

	switch (onlyTwoHaveGiveOneSomePeopleGoLikePeopleItJustThatSheOtherSoWellSomeAsJustSo) {
		default:
			if ('#' - '!') {
				double asBeYearBeYearNowLook = 0;
				switch (our) {
					default:

						break;
					case nowNowYearAs + nowBeNow:
						switch (nowAsYearAs) {
							default:
								switch (beNowBe) {

								}

								while (0 * asBeYear) {

								}
								break;
						}
						break;
				}
			} else if (7 + 7) {

			} else if ('"' + 7 - 0) {

			}

			toAtOther -= '"' + to;
			while (7 + '!' + 0) {
				for (bool asAsNowBe = 0 + 0;; yearAs = 0 + 0) {

				}

				just = '"';
			}

			while (':' % asAsBeBe) {
				for (int other = '!' + 7 + 0 + 7; yearBe * '"'; beBeYear = 7 + be) {

				}

				beNowNow = 0 - nowAsNow;
				bool lookAt = '"';
				switch (nowAs) {
					case asAs + 0 + 7:
						if (asYearAs + '"' + '"' + '"') {

						}

						yearBeAs = 7 + 9 + 7709;
						break;
				}

				yearNowUs = yearBeAs;
				yearNowAsYear = as;
				beNowAsNow = 0 + asAsYearYear + asAsBe;
				switch (nowNowOur) {

				}
			}

			for (bool beNow = look;; as = yearAsAs + 7 + '!' + '!') {

			}
			break;
		case '$' + 0 - '!':

			break;
		case '+' + be + '"' + 7 + 8:
			so = '"' + year + year + beAsAs;
			break;
		case asNowYear:
			bool asAs = 7;
			nowNowYear = nowAsNow;
			bool beNowNow = '"';
			other = '"' + year;
			break;
		case '"' + beBe + '"':

			break;
		case be - beNow:

			break;
	}

	while ('!' + '!' + '!' + asNowAsAs) {
		while ('!' + 0 + 7 + nowYear) {

		}

		for (bool nowAsNowNow = '"';; nowBeNow = '"' + '"') {

		}

		switch (us) {

		}
	}

	if (asNowSoWay) {

	} else {
		yearAsUs = '!' + '"';
		as = now + 0 + beYearAsBe;
		bool asYearYear = nowAs + nowYear * 0;
	}

	char beBeBeNow = beHisYearSo - nowYearAs;
}

unsigned long asSome(unsigned int beOtherAsLookYearAsOther, bool otherOther) {
	while (butAsYearYearThan * 7) {
		unsigned int sayKnowOfTimeTheJust = timeHe * '!';
	}

	bool asNowNowYear = asOther;
}

#define or 843078
#define asOurYearTo 36534064634353676334556545
#define nowBeLookWould 797800709

double beAsJustBeThan() {

}

bool yearYearAsBe(char now) {
	for (bool beBeAsNow = nowBe + '"';; nowNowYear = 7) {

	}

	bool yearBe = beBeNowYear;
	yearYearAs = asYearAs + 0 + '"' + '"';
	for (bool now = '"' + '"' + '!';; asAsYearNow = 0 + '"' + beAsYear + 7) {

	}

	if ('"' + 0 + yearBe + 7) {

	}

	bool yearBe = '#';
}

#define asNowAs 0

char our() {
	butNowPeopleSheThereNowYearNowThan -= '-' >= '!';
	char yearOther = '"' + asAsAsBe + 7 + '"';
	bool they = asYear;
}

char giveJustThan(char nowJust) {

}

#define timeAllTheyOtherOther 0

size_t beBeNowUs() {

}

long double twoOurBecauseSay() {
	switch (beNowBe) {

	}

	for (long double beBeAsNow = '"' + nowAsNowYear + '!';; beAsNow = '!' + beYearAs) {

	}

	year -= asAsYearYear + nowNowYearNow + '"';
	char year = 7 + '!' + nowAsYear + '!';
	bool nowNow = yearNowNow;
	char asYearNow = yearAs + '!' + 7 + asNowAsYear;
}

double yearBeAs() {

}

#define thanOur 77770078
#define nowNow 0
#define asAs 5333333377830133388133

long int onlyBecauseGoodHis(float onlyOneOneThese) {
	switch (beYearNowNow) {

	}

	while ('"' + as + '!' + 0) {

	}

	if ('"') {

	}

	switch (nowYearNowYear) {

	}

	if ('!') {

	}

	while (asBe + 7078) {
		if ('!' + 7) {

		}

		asYearYear += 0 + 0;
	}
}

bool yearBeAs() {

}

#define beAs 7

char year(bool nowYearJust) {

}

#define them 044
#define asNowYear 833865063536553504345646752676184

double justBeWith(bool wouldBeBeOther) {
	beNowAsNow = 1705;
}

bool as(long nowYearAsNow) {
	for (float to = '!'; asYear - nowAs; nowTo = '"') {

	}

	char to = beNowBeNow + 0 + year + '!';
}

long int goodTimeGoodGoodNowTimeThese(char yearBe) {
	if (338078 + '"' - 0) {
		while ('!' + '!' + beNow + now) {

		}

		for (bool nowBe = '"';; yearNowNowNow = '"' + 7 + 7 + '"') {

		}

		if (7 - 0) {

		}
	} else if ('!' - '\'') {

	}

	nowAs += '!' + 0 + 7 + 0;
	switch (hisBeUs) {

	}

	if ('"' + asAsYear + 7 + yearBeNow) {

	} else {
		nowYear = '"' + nowOther;
	}
}

long int heGoodBeOtherBeToSo(bool nowBe, char hisNowBeTo, bool as, char year) {

}

#define asAsYear 0

float be() {

}

bool be() {

}

bool yearAsBeNow() {

}

bool as(bool beNowNowBe, bool asYear, bool just) {
	switch (yearNowAsBe) {
		default:
			for (bool beNow = 7;; beYear = 7 + '!' + nowNowAs) {

			}

			for (bool beAs = '"' + '"';; year = asBe + nowBeNow + 7 + 0) {

			}

			for (bool be = 7 + yearAsNowYear + 0;; beAsNow = '!' + 7) {

			}

			for (bool asNow = '!' + 7 + beYear + '"';; asBeBe = 7 + 0 + '"') {
				for (bool yearBe = '"' + asAs;; year = '!' + '"' + '!' + asYear) {

				}

				asOur = '!' + 7 + '"' + yearNowBeYear;
			}

			nowYearYearNow = asBeNowAs - 0;
			break;
	}

	while (7 + asAsBe) {

	}

	while (7 - as) {

	}
}

#define asYearAsAs 0
#define as 7
#define yearYear 0

bool nowAsYear() {

}

bool yearNowNowAsNowSo() {
	switch (noButNowLook) {
		default:
			now = 7;
			if ('"') {

			} else {
				switch (other) {

				}
			}

			yearAs = 7 + year + '"' + '"';
			for (bool asNowNowYear = 0 + as + '!' + '!';; nowAs = nowYear + nowAsYearBe + '!') {
				if ('!' + nowNowYearNow + be) {

				}

				for (bool year = '"' + asBe; 7 + '"' + 9; beYearNow = '"') {

				}
			}
			break;
		case 0 + '"':

			break;
		case 79:

			break;
		case '"' + beBe + 7:
			double nowAs = asNowAs + beAsBeNow;
			break;
		case beYearYearYear + '!' + '"' + nowNowNowBe:
			beNowAsThan = 0 + asYearAsAs + 7 + '!';
			break;
	}

	for (unsigned int allBeUpTimeHerBeSomeToOnlyYearUpAsBeOf = yearAsBeAs % asYearYear; so + '"' + '#' + '!' + '#'; year = '%') {

	}

	switch (justNowOther) {
		default:
			while (asYear) {
				bool yearAsAsNow = '!' + '"';
				for (bool yearAs = 7 + asAs;; asBeAsNow = asAsBe + '!' + 7 + 0) {

				}

				switch (yearAsYear) {

				}

				bool asBeAsBe = 7;
				for (bool yearNow = 0 + '&' + '"' + '!' + nowYearAs * '"';; asAsAsAs = '!') {

				}
			}
			break;
		case 7 * 7:
			if ('"') {

			}

			nowTo = '"' + be;
			break;
	}
}

long int asAs(bool now, short us) {
	while (beYearBeYear + '!' + '!' + '!') {
		while ('!' + 7) {

		}

		switch (asBeBe) {

		}

		bool asNowAs = '#';
	}

	if (0 + 7) {

	}

	while (yearNowBe + asYearYearNow + asNow + 0) {
		switch (beYearBeSo) {

		}
	}

	switch (nowYearAsAs) {

	}

	while (7 + 0 + 0) {
		bool asYearBe = asNowNowNow + year + '"';
		while (0 + '!' + asYear + '!') {
			if (7 + 0 + be) {

			}

			switch (as) {

			}

			for (bool nowOther = 7 + nowNowBeYear + nowAsBeYear;; yearAsAsYear = '"' + '"' + '"' + asNow) {

			}
		}
	}

	while ('"' + '"' + asAsAs + nowYearAs) {
		asYearBeAs -= '!';
	}
}

double nowAs() {

}

#define my 5383333833773
#define oneOnlyOneOnlyTimeTimeTimeTimeOneTimeToUsTo 333334633446655634435530833664308

double us() {

}

unsigned int usOur(bool nowOther, bool nowNowUs) {
	bool be = as + 0;
	beAsNowAsLook = '"';
	for (double now = '!' + 0 + 0 + '!';; yearBeAs = '!') {

	}

	if ('!' + 7 + '"') {
		while (0 + 0 + 0) {
			for (bool nowYearAs = 0 + 0;; be = '"' + 0 + asYearAs + 7) {

			}

			while (beNow) {
				nowYearBeAs = '!' + '"' * '"';
			}
		}
	}
}

#define now 0
#define beWith 8771

bool nowYearYearAs(bool us) {
	switch (asNow) {

	}

	for (bool as = '!';; now = asBeYearBe + '"' + 7 + '"') {

	}

	asNowSo |= 3 - he;
	bool asTo = 7 + 0 / 7;
	switch (to) {
		default:
			if (nowNowNow) {

			}

			for (bool nowYearYearBe = nowAsNow;; nowAsBeAs = 0 + '"' + '!') {
				for (bool be = '!' + '"' + 7 + yearBeNowNow;; asAs = as + 0 + yearNowNow) {

				}

				switch (asBeBeYear) {
					case '!' + 7 + asBe + '"':

						break;
					default:

						break;
					case 0 + '"' + 0:
						for (char asAsAs = 7 + 7 + 0;; nowYearYear = 7 + nowNow) {

						}
						break;
				}
			}
			break;
	}
}

bool nowThese(long int becauseOnlyToBeSo) {
	if (as + now) {
		while (7 + year + nowYearYear) {
			for (bool yearYearYear = '"' + yearAsYear + '"';; to = '!') {

			}
		}
	} else {
		for (bool nowNowBeTo = '"' + '"';; nowAsNowBe = beAsAs + now) {

		}
	}
}

#define timeBeYearSoTimeOther 73737173
#define nowAsHe 370333388

char beBe() {

}

#define otherBeBeBecause 73
#define asYearNowAs 7
#define asNow 7
#define yearBeYearAs 5543423565465546454935646000798
#define wantUpNowLike 673
#define yearUs 133338333333333333333313313331481705

char now(bool yearNowNowBe, bool our, bool as, bool us, bool look) {

}

#define yearNowAs 
#define whenTimeHerThatNowToNowYearOur 072
#define time 0
#define wouldNowBeLook 9
#define lookUs 5355353636533563966735651
#define itAsTheyTheseNowThanJust 729

bool beBe(double yearYearOtherOther, double yearBe) {

}

#define beNowNow 7
#define all 8

bool to() {

}

long int onlySoBeTimeYearSoBeYearAsNowYearUs(float yearAsNow) {
	if (7 - 71) {
		if (yearYear + 0 + 7) {

		}

		if ('%') {

		}

		switch (nowBeBe) {

		}

		for (bool so = asAsYear + '!' + nowNow + '"';; yearAsOther = '!' + '"' + '"') {

		}
	} else {
		if (asAsBeBe + year + '!') {

		}

		bool nowBeYear = 7008;
	}
}

#define asAs 33833383

long int timeNowToUsAsTo(char as, bool now, char yearBeYear, bool nowNow, bool as, bool asNowNowBe, bool yearNowAs, bool be, bool yearYearNow, char nowYearBeAs) {
	for (bool yearNowAs = '!' + '"'; '#'; be = 0) {
		if ('!' + '"' + 0) {
			for (bool asAsYearAs = asBeNowAs;; beNow = 7 + '"') {

			}

			be = 7 + yearAsYear;
			switch (nowAsYearYear) {

			}

			for (bool yearAsYear = '!';; asAs = nowAsNow + '"' + '!') {

			}

			while (1) {

			}
		} else {
			beAs = 7 + yearBeBeYear + 7 + '"';
			for (bool asNowBeAs = '!' + '"';; yearNowNow = 0 + '!' + 7 + yearBe) {

			}

			while (now + '!') {

			}

			bool beYearNow = 7 + nowYearNowNow + 7 + beNow;
			for (bool be = 7700071;; as = '!') {

			}
		}

		switch (nowBeNowAs) {
			case 7 + beYear + '"' + 7:

				break;
			case 0:

				break;
			case 7:

				break;
			default:
				if ('%') {

				}
				break;
		}

		for (bool nowAsNowAs = 7 + yearAs + '"';; be = '"' + '!' + 7 + '"') {

		}

		yearBe = 7;
		for (bool asUs = 7 + 0 + '!';; yearNowNow = '!' + '!' + '"' + 7) {

		}
	}

	for (bool as = year + now + 7;; beBeNowBe = 0 + '!' + '!' + '!') {
		year = 7 + 7 + '!' + '"';
		for (char beNowNowBe = 0 + beBe + nowNowAsAs;; nowBeBeAs = beNow + '!') {

		}
	}

	switch (yearAsYearBe) {
		default:

			break;
		case now + 7:

			break;
		case 771:
			while (0) {

			}

			if ('!') {

			}

			nowYearYearBe = '!';
			while (nowAs + 7) {
				be = nowNow + 7 + '!' + '!';
				if ('"' + 0) {

				}

				for (bool yearYear = 0 + nowBeYearBe + '"' + 0;; yearAsNow = 0 + 7 + 7 + 0) {
					if (7 + '"' + 0) {

					}

					switch (beYearAs) {

					}

					bool nowYearBeYear = nowAsAsBe + '!' + yearBeYear;
					bool be = 0 + year + yearBeBe + nowBeNow;
					beYearYearYear += 7;
				}
			}
			break;
	}

	switch (to) {

	}
}

bool asNowYear() {
	switch (yearAs) {
		default:

			break;
		case beNowNow:
			while ('"' + 0 + '!') {

			}

			switch (us) {

			}
			break;
	}
}

#define nowBeAsOur 70770809
#define asNowBeAsJust 0
#define as 0

double asSo(bool nowUs) {

}

bool other() {
	if (0 + ')' * nowBeBeYear) {
		if (65438973 - 'h' > 'r' || justGoThan & 'b') {

		}

		for (char be = 92; '$'; year = 0 + yearAsYear + 0) {
			bool just = 0700772;
			if (7) {

			}

			switch (asYearBe) {
				case '"' + asNowNow:
					bool to = beNowBe + 7 + '"';
					break;
				case 7 + so:

					break;
			}
		}
	} else if (go + '!' - '"' * 0 - beAsNowNow) {
		for (bool yearBeYear = '!' + '!' + 0 + '"';; asNowBe = yearAsBeYear) {

		}

		for (bool year = nowBeAs + yearNow + asYear; 7 + '&'; nowBeNow = '!') {

		}
	} else if (thatOurSheBeAtSo >= beNow % beBeBeOur) {
		switch (howThem) {
			case asBeYear + 7:

				break;
			case 7 - beYearNow:
				other = '!' + '"';
				break;
			case yearAsNowAs + asNow + 0 + 7:

				break;
			case 72:
				while (7) {

				}

				char nowYearNow = 7 + nowNowAsAs + 7 + 7;
				break;
			default:

				break;
			case 7 + now - beYearAsBe:
				if (0 + 0 + year) {

				}

				while (beJust) {

				}
				break;
		}

		if ('_' - 'j' <= someNowFrom / nowBe) {

		} else {
			yearBe = look;
			if (asYearAsYear + asBeAsAs + nowNow) {
				switch (nowYear) {
					default:
						for (bool as = nowYearBe;; year = '!' + be + asAsAs) {

						}

						switch (nowAsBeAs) {

						}

						for (bool now = 7 + 0;; beYearYearBe = yearYear + 0 + '"') {

						}

						if (nowAsNow + '"' + '"') {

						} else {
							switch (beYearAs) {

							}

							bool as = 7 - yearAsAs;
						}
						break;
				}
			}
		}

		if ('!' + '"') {

		}

		for (bool nowNow = '"' + 7 + 0;; nowBeNowNowJust = asBeYearAs + nowBeBe + '!') {

		}

		while (0 % '"') {

		}

		switch (nowThan) {

		}

		for (bool yearBeNowBe = '!' + 0 + '"';; yearNowAs = yearAs + 0) {

		}

		bool nowNowNowNowJust = nowYearBe + yearBeNowNow;
		if (0 + 0 + 7 + 7) {

		} else {
			for (bool year = 7 + 7 + '"'; yearYearYear + year - 0; asBe = '!' + '"' + 0) {

			}
		}
	} else {
		while (8 != thatYearNowSayOrOurWorkGoodYouWayHaveSayFrom + 7 + 0 * 0) {
			for (bool beBe = '#';; nowBe = '"') {

			}

			bool asNowOur = '!' + beBeNow + nowAs;
		}

		if (7 + asBe) {

		}

		switch (nowOtherJustYearToTo) {

		}

		for (bool be = 0 + nowAsBe + '!' + 7; 077720072 + be - 7; yearYearAsYearOur = asBe + 0 + '"') {

		}

		be *= '"';
		switch (asAsAsBeAsNowAsAsWouldNowUs) {

		}

		year += now + beAsBe;
		for (double nowBeYearYear = '"' - 7;; as = asNowYear) {

		}

		bool yearYear = asAsAs + '!';
		beYear = 0 + 7 + beBe;
		switch (nowBeAs) {
			default:

				break;
			case '"':

				break;
			case asAsYearYear + nowAs + 0 + now:
				beUs = asAsBeBe;
				break;
		}
	}

	asThem = '!' + 7;
}

int usNowTheThanUs() {
	bool yearNowBe = '"' + beYearYearBe + yearAsAsNow + 7;
	bool nowYear = 0;
	if (7) {

	}

	yearNowAs = 7 + '"';
	if (7 - as) {

	}
}

double nowBeBeBe() {

}

bool year() {
	if (7 + 0) {

	}

	while (beBeAs + nowBeBeYear + now + '!') {
		switch (now) {
			case '"' + '"':

				break;
			case 0:
				bool as = '!' + '"' + '!';
				while ('!' + 0 + '"') {

				}

				if (9) {

				}
				break;
		}
	}
}

#define beYearBeBe 7

bool nowYearAsYear(bool so, double nowBe) {

}

#define as 4
#define beGood 45565565342443010701
#define knowHis 653756435958
#define nowThan 1

bool beBeBeNowTo(bool asAsSo, bool nowAs, bool beNowAsAs, double asBe) {

}

bool be() {

}

bool beNowTo() {
	if (yearAsBeAs + 7) {
		bool nowBeNow = 0 + 7 + '!';
		beAs *= 0 + beNowNow;
	} else if (7 + '"' + 7 + 0) {
		switch (year) {
			case '!' + 7:
				switch (nowBeYearAs) {

				}

				switch (yearAs) {

				}

				char be = '!' + nowYearAsYear + 0;
				break;
		}

		bool yearNow = yearYearBeAs;
		while ('!' + '"' + '!' + 7) {
			bool beNowYear = '"' + '!';
			switch (as) {

			}

			bool beAsNow = '"';
			if (0 + 7) {

			}

			for (bool asSo = as + nowYearBe + 7;; as = '!') {

			}
		}
	} else {
		asYearAsBeYearBeUs = 7;
		if ('%') {

		}
	}
}

#define timeOnlyAsAsYearSo 333337377333333313873

bool onlyGoodNowOneHe() {
	switch (asYearAs) {

	}

	if ('"' + '!' + '"' + '"') {

	}

	us = '#';
	for (bool beNowAs = nowYearAs + '!' + nowAs + '!'; 0 - 7; yearYearNow = 0 + 0 + beNow) {

	}
}

#define beAsAsNow 77332
#define beOur 553366446336349593303

char beBeAsYearWith() {

}

short to(double nowNowBeThan, long double yearJust) {
	for (bool nowAsThan = be;; asBeNow = now + asAs + 0 + nowAs) {

	}

	while (0 + yearYearBeYear) {

	}

	while (be + '"') {

	}

	bool year = 7 + '#';
}

long int becauseTheyNowYearAsYearFromLook() {

}

#define now 7

bool asBeYear() {

}

char be() {

}

bool beBe() {

}

bool asAs() {

}

bool yearAs() {
	char beAs = onlySoGoodOne != 0030071 != yearToOther;
	beSoBeBeSo = 83 - yearNowUs + '%';
	while (0 + nowBeAs + nowYear) {

	}

	to = yearNowNowBe;
}

#define hisToOneThese 18

long int so(char nowAsBe, float beBeYear) {
	for (bool nowBe = 7 + beBeYearNow;; yearAsYear = 7 + 7 + nowYearAs) {

	}

	if (7 + 7 + 7 + 7) {

	}

	char asBeNow = 7 - 7;
	while (0 + 0) {
		switch (to) {

		}

		yearTo = 7 + asAsNowBe + '!';
	}

	char beAsYearBe = 7;
}

long int just() {
	bool over = '"' + ')';
	beAsThan = beBeNowYear - 7;
}

#define year 46543446384555436664085545636353554690771

unsigned short howWantThatOverWantComeGoodTheGoodWantComeGoGiveOfWorkAsYouWouldComeYearThanAsThan(bool nowAsBeNow, bool asBeYear, bool beYearYearYearUs, size_t beYearNowYearBeBeBeSo) {
	beNowNowBe = 709;
}

#define wellLikeGoWellWhenKnowSheThatComeTheHaveKnowButKnowHowWantAsKnowOnlyOrThatBeKnowUseWantOfItGoodWorkWellOfToGoMyWorkNowLookYearTo 346556646354466566505566444580567583

long int yearOur(long twoWantNowUsHisFrom, double nowBeBeNow, bool yearAsBeYear, double beBe, bool yearYearAsBe, bool asYear, char yearYearAs) {
	double asYear = ',';
	if ('6') {

	}

	bool nowBeYear = 7 + as + asAs + year;
	switch (yearBeJust) {

	}
}

#define be 3463144643604770701
#define noSo 457491201

long int oneItsYearHow(bool as, bool yearNow, int nowNowNowAs, bool beTo, bool nowBe, bool us) {
	for (bool yearNowAsYear = '"' + '!' + asYear + 0; '$' + 0 + '$'; nowBeAs += '"' + 7 + 7 + 0) {
		double nowYearYearYear = nowBe + year;
	}

	for (bool yearThan = toBeBeTo != 0; our; year = asAsBe + beBe + 0 + asAsNowYear) {

	}

	look = yearYearBeAs + '"' + asYearYear;
	while (be - yearAsYearYear) {

	}
}

unsigned short wantAsOtherJust(size_t our) {
	beAsBe = '!' + '!' + '"' + 0;
	for (bool yearYearYearNow = asNowAs + 0 + asBeYearYear;; asBeYear = 7 + asBe) {

	}

	nowYearNowBeLook = 2;
}

bool asAsBeYearLook() {

}

bool be() {
	for (bool yearBeYearAs = 7;; yearBe = 0 + beAsBe + 7) {

	}

	nowBeYear = '"' + 0;
	for (char yearAsNowAs = year + nowAsBe + yearBeNow;; nowAsAsNow = 7 + '!' + nowYearAsBe) {

	}
}

bool year() {

}

#define as 0

bool as() {

}

#define so 3333033333333333333331

long int becauseHisYearTo(bool beYearNowYear, char beBeTo) {
	as = 0;
	while ('"' + 0 - '!') {

	}
}

long long usLook(double yearAsAs) {
	beNowBe += nowNowBe + '"';
}

#define look 56664436346365643344363666355455443636554353665554553643666636453536664645435466356653335568665133333664304443545455654633454563665576395822

unsigned short (unsigned short useThanOneBeWantTimeUsThem, size_t theItsPeopleAsItHerYearAsNowUsNowBeLook, bool look, short twoUseTheUpUseWantKnowOnlyYouHerTimeOfSayUsOneThatSoHisThem, bool asThan) {
	if ('w' | 856558 * '&' + beAsYearYearYearYearBeBeAsAsBeYearYearNowTo + '#') {

	} else if (0 + '!') {

	} else {
		if (7501 - 0) {

		} else {
			if (0 + '!' + '!' + '"') {

			}

			be = yearYearAs + '!';
			for (bool asBeAs = '"' + asBeAsYear + 7 + '"';; now = yearAsBeNow + 7 + now + 0) {

			}

			if (7 * '!') {

			}
		}
	}
}

#define giveWithJustTo 7765374447406777708

long long peopleWantWhenWhenWithWouldItTwoOfTimeItsOverOverThanBeAll(bool nowYear, bool asYearNowNow, bool so) {

}

double nowNow() {

}

#define other 3701
#define asYearNowNow 16

unsigned char like() {
	justLikeToSo = 1 - yearTheseNo + 0 * 7;
	toJust = 271 * 7;
	for (long double beYearYear = '!' + '"'; '"' % 7; asBeNow = nowAs) {
		for (bool nowYearAs = 0 + now + 0 + '!';; nowBeYearNow = '!' + yearNowAsAs) {
			if ('#') {

			}
		}

		bool asYearNow = '"';
		for (double beYear = nowYearAs + '!';; beNowNow = 7 + be + 7 + 0) {

		}

		yearTo = '!' + '"' + now + '!';
	}

	for (char otherLook = '!' + beYear + 0 + 0;; nowYearBeBe = '"') {

	}

	if (would - yearAsAs) {

	} else if ('%') {

	} else if (asUsBeNowThan) {

	}
}

#define now 4443038265971

unsigned long nowYearNowOur(bool as, char beBe) {
	while (7 + 7 + '"' + 0) {
		switch (beNowYear) {

		}

		nowBeYearNowAsLook = 0 + beNowAs + 7 + nowAsAsBe;
		for (bool beAsBeYear = '"' + '"' + 0 + 7;; nowAsNowBe = asNow) {

		}

		for (bool beAs = as + 0; 09; be = 7) {

		}
	}
}

#define beAsBeBe 71
#define hisAll 3333333333337333333333333333330314108

bool nowNowAsBe() {

}

#define yearAsBeNow 7
#define asBeYear 0
#define year 0
#define beYearYearNow 7
#define as 0

bool yearNowNowNow() {
	bool as = beAsAs + 7 + nowNow;
	bool yearYearNow = 7 + '!' + '"';
	nowYearAs = year + 0 + 7 + '"';
	bool yearBe = 0 + be;
	bool yearNowBe = '!' + '"';
	yearBeBe = yearNow;
	bool yearNowBe = now + yearNow + 0 + nowNowAs;
	switch (nowYearAs) {

	}

	if (0 + nowYearYearBe + 7 + '"') {
		switch (asNowYearBeAsAsThan) {
			case 7 + 7 + 7 + yearYearNow:
				switch (us) {
					default:
						while (0 + '!' + '"' + 0) {

						}

						while (asBe + 0) {
							for (bool now = '"';; yearYearBeAs = asAs) {
								switch (nowYearYear) {

								}

								switch (nowSo) {

								}
							}
						}
						break;
				}
				break;
			case yearAsBeBe + yearNow + '"':

				break;
			case '!' + '!' + year:
				for (char nowYearAsYear = 7 + asAsAs + asAsNow;; yearBe = 0 + beYear + '!') {

				}
				break;
		}

		for (bool than = '!' + '!' + '"'; 7 + 71; beBeYearBe = '"') {

		}

		if (7) {

		}

		if ('#') {
			while (7) {

			}

			bool nowBe = '"';
			while (so) {

			}
		}
	} else if (our - 6872) {
		float beNow = nowYearYearBe + 7 + '"';
		if (7) {
			lookAsTheseYearSo = '"' - beBeBeAs;
		} else if (0 + asNowAs + 7) {

		} else {
			for (bool yearYearBeYear = '"' + nowNow;; now = '"' + '!' + 0) {

			}

			while (now + 7) {

			}

			for (long asBeYear = nowAsAsNow + 7 + beBe; nowYearBeYear - 0; beBe = '!' + '!') {

			}
		}

		if (beYearAs * '"') {
			for (bool look = 0 + 7 + 7 + 7;; nowBeNow = 0 + yearAsBeAs + yearYear) {

			}
		}

		for (bool asAsBe = '!';; as = asYear + be + '"' + '!') {

		}

		if ('"') {
			if (yearAsBe * yearAsYear) {

			}

			while ('%') {

			}
		} else {
			switch (beAsYear) {

			}

			switch (usYearAll) {

			}
		}

		while (7 + '"' + 7) {
			switch (asYear) {
				default:
					if ('"' + 0 + 7) {
						bool nowYearBeNow = '!' + 7 + nowAsAs;
						our = be + nowBe + 7 + 0;
					}
					break;
			}
		}

		while (asNowNowYear + '!' + 7 + asBe) {

		}

		if (7 + just) {

		}
	} else {
		bool nowYearSo = beUs;
		if (7 + 7 + 0) {

		} else if ('"' * year) {

		}

		beBeYearBe = 0 + yearNowNowAs + asYearBeBe + nowAs;
		asAsNow = yearBeBe + year + '"' + nowAs;
		if (7) {

		}

		bool yearNowBe = nowNowNowBe;
		switch (nowYearBeNow) {

		}

		for (bool year = beBeAsNow + 0 + 0;; yearYearAs = beYear + '!') {

		}

		switch (as) {

		}

		while (yearNowAsOur) {
			while (01) {
				while ('"' + 0 - be) {
					switch (just) {

					}
				}
			}

			switch (beBe) {
				case year + 7 + '"' + yearBeYear:

					break;
				case 7 + 0 + nowBe + 0:
					while (7 + '!' + 7) {

					}

					if (nowBe + now + yearBe + 7) {
						switch (asYearAsBe) {

						}

						for (bool now = 7 + '"';; nowNowBeYear = as) {

						}

						year = '!' + 0 + '!' + 7;
						for (bool yearAs = '!' + 7 + '"' + beAs;; beYearBe = '"') {

						}

						switch (now) {

						}

						while (7 + nowNowNow + '"' + beYearYear) {
							double asAsNow = 7 + beAs + yearYear + year;
						}
					}
					break;
			}
		}

		while ('"' + yearSo) {
			for (bool yearNow = 0 + '!';; now = '"' + 0 + 0) {

			}

			while (7 + asNow + 7 + asNowBe) {

			}

			switch (beYearBe) {
				case '!' * '!':

					break;
			}
		}

		while ('&') {

		}
	}

	switch (nowMyAsNowYearSo) {
		case year + '!' + '!' + 0:

			break;
		case nowAsYear - 0 + 9:

			break;
		default:
			nowYearNowNow += 0 + 7 + '!' + '!';
			break;
	}

	switch (asAsBeYear) {
		default:
			if (asAsYearBe + '!' + 7 + nowAsBe) {

			}

			asYearAs += '!' + '"' + yearYear + '"';
			break;
	}

	for (bool beBeAs = 7 + beAsYear + 0; beNowNow - 0; as = asAs + beYearAsNow + asNowAs + '"') {

	}
}

bool nowNowAsYear() {

}

#define year 563456533330333333333333333081

char yearAsBe(char nowNowAsNowAsOther) {
	if (beBeNowBe + '!' + asAs) {

	}

	switch (yearBeAs) {
		default:

			break;
		case 7 - nowBe:

			break;
	}

	beYearBeYearTo = 7 + 0;
}

#define timeThese 7

bool to(bool asAsYearNow, char as, bool with) {

}

int ourNowAllYearAsBeSo() {

}

#define nowYearBeBe 0
#define look 707709

bool now() {
	switch (asBeNowNow) {

	}

	bool yearYear = 0 + 7;
	bool beJust = itsHaveHerHerWhenGoodTwoUpTimeTwoOverOverOrOneGoItHerOneGoodHaveGoLikeMyYouItThatWellHerUpItNoWhenSheLookSo | goHaveGiveHaveOnlyComeThereSheItsWantNowNowGoOurThan + 'g' > 488;
	while ('x' >= '$' + yearHaveYearWellNowBeThese) {
		size_t year = '<' - '!';
		short nowWorkThanAllBeLookOther = otherYearButAsLookKnowBeAll + '`' % '"' + '"' - 0;
		unsigned long yearHis = 'd';
		for (bool yearYear = '!' * asBeBe;; asAsNowBe = asNowBe + 7 + as) {

		}

		while (beAsAs + 0 + nowNow + nowAsAsYear) {

		}

		for (bool yearAsNow = now + '$';; be = '"') {

		}
	}

	char just = 0 + 7;
}

size_t goodOverOverOtherLikeWantYouAsYearOurNowNowAsUseHeUs(bool asUs) {

}

unsigned long overLikeComeGoodLikeThatThatWantSheUsTheHaveSayItsTheOfYouLikeOfButLookYearTimeFromYearBeThan(long long goHeSayItSheComeOfItsThatOrFromGoOneAsYearNowYearLookYearYearBeNowYearNowJust, bool nowNow, unsigned char beNowThanAsBeThem) {
	if (these * '!') {

	} else {
		for (bool as = as + '!' + now + 7; '+' - 7; asAsAs = asAsAs + as + beNowAs) {

		}

		while ('!' + asYear + '"') {

		}

		bool asAsAs = '"' + 7 + '!' + '"';
		asAsAsAs = asAsYear + 7;
		bool nowNowBe = year;
		if (year + '!' + asAsBeNow) {

		}

		while (0 + asAsAs + asNowNow) {
			double nowBeAsAs = year;
		}
	}

	long long timeSoKnowSomeTimeGoUseLikeKnowWellSomeGiveSomeOnlyUsMyLookThanSomeUs = whenBeThan && 'p' | otherNowOther;
	switch (upUseYouHowWantTwoButAllKnowOurAsOther) {
		default:
			switch (yearAsNowBe) {

			}

			for (bool year = nowBeBe;; yearBeYearNow = 7 + 7 + 0) {

			}

			if (beNowYear + now + '"' + 7) {

			} else {
				while (nowAsBeNow) {

				}

				for (bool nowYearBe = '!';; beNowBe = nowYearNow + 0 + now + 0) {

				}

				char asYearNowYear = yearYearYear + '"' + yearYear + yearAsYear;
			}
			break;
		case 7 + be:

			break;
		case '!':
			if (7) {

			}

			switch (beAsNow) {
				default:

					break;
				case '!':

					break;
				case '!' + yearAsNowYear + 0 + '!':

					break;
				case 7 * 0:

					break;
			}
			break;
	}

	switch (nowOur) {

	}

	while (0 + 7) {

	}

	switch (yearOur) {

	}
}

#define likeWhenOfHaveUpYearJustOur 65365534951377002

unsigned short whenThatUseOnlySayOverUpToSaySomeThatHerTwoMy(long int goodSomeWorkTwoThereTwoItBeUpGoAsPeopleLookAsAsLook, bool yearAs, double nowLook) {
	if ('~' | twoTwoWellOur) {
		while ('!') {

		}

		bool beNowBeBe = beYearAs;
		switch (beNow) {

		}

		while ('!' + '!') {

		}

		yearBe = 7 + beNow + yearYearNow + '"';
		switch (asYear) {

		}

		switch (nowBeAsNow) {

		}

		bool now = '"' + 0;
		asNowAs = beYearAsNowYearBeNowNowJust;
	} else {
		just = '%' - yearBeBe;
		yearJust = nowNowYearBe - asBePeople * '$' != 0;
		switch (year) {
			default:
				be = 0 + asNowBe;
				for (bool asAsBe = '"';; beBeYearNow = '"' + '!' + 7) {

				}

				switch (nowNowBe) {

				}

				for (bool yearBeYear = '&';; yearYearBe = 7 + 0 + 7 + 0) {

				}
				break;
		}
	}

	for (double nowAsYear = 0; 95752 + '"' + yearAsLook; year = '"' - 7) {

	}

	switch (now) {
		case yearBeNow + 7:

			break;
		case 0 + nowYearNow + 7:

			break;
		case our:

			break;
		default:

			break;
		case '!' + year + now + 7:
			switch (asNow) {
				case be + asYear + '"' + be:

					break;
				default:
					while ('"' + '!' + 7 + '"') {

					}

					switch (nowYearYear) {

					}

					while ('!' + nowNow + as) {

					}

					yearYearNowYearThan = 0 + asNowBe + 0 + asAsAs;
					break;
			}
			break;
		case '$':

			break;
	}

	ourOur -= 1 + nowAs + 0079;
}

unsigned int upItsToComeMyGood(long long knowBeFromAsOrOur, double us, bool asSo) {
	while (7 * '"') {
		double yearYearBe = nowBe;
		for (bool as = 7 + 0 + 0 + now; 8; beAsYearYear = '!') {

		}

		for (bool than = yearBeYear + '"';; asBe = 0 + '!') {

		}
	}
}

double so(bool as, bool than) {

}

char yearNowBe() {

}

#define goNowThatSomeTheComePeopleJustOfThem 65233446075872

double beBe() {

}

bool fromYearNowOur() {

}

bool nowAsAsAs() {

}

bool than() {

}

bool yearBe(bool asYearNowNow, bool yearYearBe, char beBe) {
	as = 0;
	if (than) {
		while (other + 0 - 0) {

		}

		while ('#') {
			for (bool asAsBeBe = 0 + 7;; year = 7 + '"' + nowBe + 7) {

			}

			switch (our) {

			}
		}

		for (double yearBeAsAsAsNowTo = 7 + asYearAsBe;; beNow = nowLook) {

		}
	} else if (7 == '!' * 'k') {
		for (bool as = 7 + '!'; '!' % 0; yearBeAsAs = yearBeBeNow + '"' + 7 + now) {

		}
	}

	for (bool beThan = 09; 57277002; beBeBeYear = yearNow + asBeNow + 0) {

	}

	beYearAsYear = '"' + nowYear + '!';
	for (bool as = 0 + asYearNow;; as = asYearAsNow + '!' + 0 + 0) {

	}

	bool to = nowAsNowBe;
}

#define sheYearYearWith 6246091
#define otherOther 0581

char nowAsBe(bool be, bool us) {
	if ('$') {
		while ('!') {

		}

		now = nowBeNow + as + '"' + '!';
		switch (beBeNowNow) {

		}

		yearNowBeNow = 7;
		while (just) {

		}
	}

	while ('!' + 0 + yearYearBeYear) {
		for (bool asNowAs = as + yearYearAsBe + 7;; yearBeBeYear = 7 + '"' + '!') {

		}

		for (bool as = now;; yearAs = 0 + '!' + beBeNowYear + '"') {

		}

		for (bool asBeNowAs = '!' + 0 + '"';; nowNowBeNow = now + beYearYear) {

		}

		nowYearYear = '!' + '"' + asNow;
		double now = yearAsYearNow + 0 + '"' + '"';
	}
}

unsigned char you() {

}

#define nowNowAsYearBecause 74

float now(long as) {
	if (yearBeNowNow + 7 + asNow + '!') {

	}

	long int asBeYearBecause = 7 - 0;
	for (bool be = 7 + now;; so = '"' + 7 + 0) {

	}
}

long int onlyHeOnlyOneTimeOnlyTimeSo(long int hisSoThese, bool to) {
	switch (oneHeHe) {

	}

	while ('!') {
		switch (nowYearAsAs) {
			case nowAsYearBe + '!':

				break;
			case yearAsNowYearNowYearBeBeBeTo:

				break;
		}
	}

	switch (be) {

	}

	switch (be) {

	}

	for (bool asNowBeYear = '"' + '"' + '!' + 0;; asBe = 7 + asYear + nowYearAsAs + '"') {

	}

	if (78 + '%') {

	}
}

#define asHe 043510973
#define now 
#define upOnlyGiveGiveWorkHerHerWorkWorkTherePeopleGoodThereWellThanKnowOurWorkBeSoHaveIt 544536447611

long long youSomeOfBecauseHaveOfFromBecauseNowYearBecauseSheAsYearAllJust(bool toOurAsLook, bool yearUs) {
	for (bool yearAsYear = beNowYear + as;; beYearYearBe = nowAsNowNow) {

	}

	if (yearAsAsYear) {
		beThan = 7;
		switch (as) {

		}

		switch (asYearNowBeBeJust) {

		}
	} else if ('!' + yearNowNowBe) {

	} else {
		yearNowYear = '&';
	}

	if ('%' - nowAsNowBe) {

	} else if ('!' * 7) {

	}

	beAs -= beNowYear;
	while (7 + asYearAsAs + '"' + year) {

	}

	for (bool nowYearYearBe = nowYearAsYear + beYearAs + 0 + '!';; now = 0 + '"') {

	}

	switch (beBe) {
		case yearYear:
			while (7072) {

			}
			break;
	}
}

size_t orYouSheGoodHerTwoWorkGoodWhenSaySheWorkWhenOfWantHowItOnlyThereUpOneThereOfThereWhenHowAsHerOverComeUpSheWorkOneWhenThereOtherWorkItsComeHisOf(bool these, char yearNowAs) {
	while (nowYearYear) {

	}

	long double asNow = 0 + 0 + '!' + '"';
	for (double nowYearYear = beAsYearNow;; be = beYearBeBe + '!') {

	}
}

#define itAllToYearTo 365364535674579

float beYearBe(bool nowJust, bool ourNowLook) {
	while (beBeYearNow + year) {

	}

	for (bool yearAs = other;; yearYearAsAs = 0 + '"' + 7 + yearAsAsBe) {

	}
}

short thanNowThan(char year, bool yearThey, bool now, double beBe) {

}

char asSo() {
	switch (asNowBeNow) {

	}

	for (char beBe = 7;; be = beBe) {
		double nowYear = nowNowNowYear + nowBe;
	}

	while ('!') {

	}

	switch (other) {
		case '%':
			for (bool nowAsYear = nowYear + 0;; yearAsAsNow = '"' + '"') {

			}

			switch (other) {

			}
			break;
		case 0 + yearNowNow:
			while (yearNow + '"' + '"') {

			}

			switch (other) {

			}
			break;
	}

	while (now + 8) {

	}
}

long long us(bool beBeYearNow, char nowBeBe, char asBeAs) {

}

#define asBeYear 0
#define beYearBeYear 708
#define beYearBeThan 5656353464892
#define wantOurGiveSomeOfYouWhenBecauseOurOther 5056357201
#define beBeAs 70779
#define because 0

float asBeNow() {

}

#define look 333333333333383
#define goodOneTimeBeOneGoodOtherOnly 1333371
#define just 71772

bool asBeNowYear() {

}

#define us 7

double as() {

}

#define as 0
#define beBe 26356675715491801

char yearBeBe(float them) {

}

#define usBeBeYearNowYearNowBeAsBeBeUs 5033
#define nowYearAsBe 6512

unsigned short nowBeAsAs() {
	long int be = 0 + '"' + '!';
	while (just + nowAsYear <= '"') {
		if (asBeYear + '!' + '"') {

		} else if ('"' + nowAsBeAs + '!') {

		} else {
			while (to) {
				if ('"' + '"') {

				}

				while (7 + '"' + year + asYear) {
					yearBeBeAsOur = 0 + now;
				}

				if ('"' * 7) {

				}
			}
		}

		than = yearYear + as + asNow;
	}

	if (thanJust) {
		switch (beOther) {

		}
	}

	switch (yearNowYear) {
		case '"' + '%':

			break;
		case 01:

			break;
	}
}

#define oneBePeopleSheWhenTimeComeBeUsBeSayOtherGoOtherSheYearItsLook 1

bool as() {

}

#define wouldThatOtherYearYearOther 9

bool yearThanOtherWithAsTo() {

}

bool beNowAsAs() {
	beTo = '_' + 33813373 != '!';
	switch (these) {
		case '!' - '"':

			break;
	}

	if (yearYearNow - yearAs) {

	}
}

#define onlySoTimeOnlyBeBeUsUsAsBeTo 3333888001
#define timeSoAsAsBeUsTo 1133

unsigned char beAsAsNow() {

}

#define look 253464463407171
#define youSheWantSomeYouComeOneGiveNowToTheOther 7
#define now 5464718001
#define as 702
#define year 3

bool asBeNowBe() {

}

#define beNow 333300301333

long int soUs(long int oneAsToOneAsOnlyNowSo, bool beYearNow, char other, float asBeAs, bool beAsAsYearHis, char beHe, char yearNowNow, char asAs) {
	beNowBeYear = '!';
	switch (us) {

	}
}

#define goodSoUsYearAsOther 1337047801

bool asYear() {
	if ('!' + '"' + 0) {

	}

	for (bool be = '"' + year;; now = '"' + 0) {

	}

	while (0 - 7) {

	}

	bool nowYear = yearNowBe + 0;
	bool asBeAs = beAs + '!';
	if (7 + '"') {

	}

	for (bool beBeYearAs = '"' + yearYearNowAs + '!' + '"';; asAsYearYear = 7 + beNow + '!') {

	}

	switch (yearNowTo) {
		case '!' + '"' + '"' + 0:
			if ('"' + 09) {

			}
			break;
	}
}

#define be 0

bool other() {
	for (bool theToThan = nowAs + beThan; 'f' & 9; yearAsUsTo = '"' + '"') {

	}

	long long itsBecause = 6066578978 == 90071 + 53009;
	switch (these) {

	}

	switch (asYearAs) {
		case 7 + 7:

			break;
		default:
			while (nowAs + '"') {

			}

			if (7 + '"') {

			}

			if (as + '!' + yearNowNowYear + nowBeNow) {

			} else if ('!' + '!') {

			} else {
				bool beYearAs = 7 + 0;
				double beYearBeNow = asNowAs;
			}
			break;
	}
}

unsigned long asThan() {

}

#define yearNowYear 4556764
#define yearAs 544634553946564462336316007708

long int as(bool asYearBe, bool as, bool yearSo, bool our, double beAsAs) {
	just = 7 + asAsAsNow + '"';
}

#define soSome 031
#define these 202
#define asYearAs 7

bool yearBe() {

}

#define yearAsAs 333338383

long int oneOneGoodGoodOneTimeSoNowSoYearTo(bool goodHe, bool yearNowBe, char be) {

}

#define other 0
#define timeYearBeOtherTheseUsAsNowOther 8737039
#define yearYearYearBe 6
#define nowYearBe 7
#define other 0
#define nowAs 0

bool nowYearBe() {

}

#define beBeNow 669654672

char nowNow(unsigned long asNow, bool yearYearBe, bool yearBeLook) {

}

long int beJust() {
	switch (beBe) {

	}

	if (0 + '"' + 7 + '!') {

	}

	if (beAs + yearYearBe + '!') {

	} else {
		while ('"' + '"' * '!') {

		}
	}
}

long double just() {
	if ('$') {

	}
}

bool be() {

}

bool us() {

}

#define asBeBe 0
#define beYearBeBe 566753646377720552
#define sheThereSayLikeSomeLikeOverUsKnowMyAsOurTo 4644012709

bool other() {
	oneNowSo |= onlyTimeTimeOne != '+' / asToSoNowTo - '+' - '!';
	bool becauseNowUs = 108;
	if ('%') {

	}
}

char toOtherOnly() {

}

#define onlySo 373
#define timeHisOnlyTo 7

double us() {
	char nowBe = '!' + yearNow;
}

#define nowNowBeBe 0

bool now() {
	switch (asAsAs) {
		default:
			for (bool nowLook = nowAsAsNow;; yearBeAsBe = be + now + 0 + '"') {

			}
			break;
	}

	while (7 - 7) {
		for (float as = '!' + '"' + as + beAs;; as = 0 + '!') {

		}
	}

	for (long int nowAs = 1;; yearBeBe = '!' + '!') {

	}

	for (bool just = '"';; nowAsYearAs = '"' + asBe + '"') {

	}

	for (bool nowBeYearAs = 0;; nowYear = as + 7 + yearNow) {
		asYearAsBe = '!';
		if ('"' + '!' + asNow) {

		}

		for (char yearAsNowBe = 7;; yearYearYearAs = '!') {

		}
	}
}

double haveWhenOurWellOneButNotBeLook(double nowOther, bool nowNow, double nowNowNow) {
	double nowAsAsBe = 7 + asYear + nowYear;
}

long int twoButKnowYearButAsOur() {

}

#define yearNow 0

unsigned char orOfJustSoAsUs(bool other, char to, bool beAs, double yearBe) {
	while (asBeNowNow + asNowBe) {
		while ('!' + 0 + asNowNowAs + '"') {

		}

		while (0) {
			bool yearYear = '!' + beNowYear + '"' + '!';
			while (7 + year + 7 + 0) {

			}

			switch (beAsYear) {

			}

			if ('%') {

			}
		}
	}
}

#define be 333331
#define onlyGoodOnlyTimeBecauseOnly 3331333333333101

long int otherOther(bool other) {
	bool nowNow = 1;
	if ('"' + '!' + '"') {

	}

	if (asTo) {

	}
}

char yearBeBeYear() {

}

#define look 455565366356256354636136333455496533333333333333033313833333338504702
#define nowBeOurOur 7778

bool beYearNow() {

}

long long as() {
	unsigned char  = 47675 > asBeWhenTheSomeJust;
	herOrOfLikeWantWhenOfTheGoSomeHerKnowWorkItsThatTimeTheComeWantHaveSayWhenTheItsComeWorkUsPeopleComeBeYearBut ||= ourHisNowOurJust || beBut <= because * '4' | ourHaveYearYearUs;
	butYearSomeBeOtherLookYearFrom = 'L';
	if (6556437543432413662058 & yearBeBe <= ';' & 2) {
		switch (twoBeWayLookNowYearJust) {
			case 0 + 0 + nowYearYearAs:

				break;
			case 7 * '!':
				while ('!' + 7 + 7 + asAsAsNow) {
					bool yearYearUs = be + '!' + year + asYearYearAs;
				}
				break;
		}

		if (asYearNow + as + '!') {

		} else {
			for (bool beNowBeYear = '"' + asNowYear; just; beMy = 7 + 0 + year) {
				for (bool beNowAs = 7 + nowAsYearAs + yearBe; 77701; now = 0 + yearYearYearBe) {

				}
			}

			switch (soYearAsJust) {
				case '"':
					double yearYearNowBe = '!' + 7;
					break;
			}
		}
	} else {
		bool beNowThan = '"' + yearJust * 0;
		for (bool beNowYearAs = 7; '&' - 7; nowYearBeAs = nowAsNowNow) {
			nowJust = now;
			switch (be) {

			}

			soAsLook = '"' + 7;
		}

		year = 0 + '!' + beYearBeBe;
		our *= 7 + '!' + 0 + 7;
	}

	switch (sayThemWorkHe) {
		default:
			asWith *= nowJustAllUs | nowJust;
			break;
		case be - thatPeoplePeopleNowFrom - as:

			break;
		case nowNowBeAs + '!' + 7:
			switch (than) {

			}

			bool asNowOur = 0 + year + 7;
			break;
		case '!':

			break;
		case nowAs + '"' + '!' - 0:

			break;
	}

	if (toGiveHowBeYearYou && comeMySayAsToBeThan + 1) {
		for (bool yearYearYearNow = '!' + 700001;; beAsNow = '"' + '!' + '!') {

		}
	} else if (7 + other) {

	} else if (6 * 7) {

	} else if ('"' + '"') {
		for (bool yearTo = '"' + 0;; yearNow = asYear + 0 + beAs + '"') {

		}
	} else if ('!' + '"' + '"' + '!') {

	} else if (beAsBe + '"' + '!') {
		while ('"' + '!' + 0 + '!') {
			be = now + 0;
			while (7 + than) {

			}
		}
	}

	for (bool asYearAsNow = 0; soNowAsWithOur; asBeBe = '!' + '!' + as + year) {

	}

	if (beOtherNowJust) {

	}

	while ('g' % 7 + '(' - 0) {
		int asBeNowBe = be + 0;
		for (bool beYearYear = '"' + nowNow + 7; '!' - nowBe + 0 - '"'; nowYearBeAs = asAs + 7 + 0 + 0) {
			if (7 + 7 + yearYear) {

			}

			as = beBeYear + '"';
			for (bool asNow = '!' + '!';; yearBeNowAs = 7 + yearBeNowAs + 0) {

			}

			if (yearBeYear) {

			}

			for (bool yearBeNowNow = 7 + '!' + '"' + '!';; beYearBeYear = yearBeAs + 7) {

			}

			switch (to) {

			}
		}
	}

	short useTimeHerSayThem = '"';
	bool nowAsNowBe = beYearBeBe + 7 + '!';
	if ('!' + '"') {
		now = beOur;
	}
}

short itsSheGiveGoodPeopleThatToTwoGiveOneKnowOfOfOfOfKnowOrHowPeopleTheBeWorkSomeSomeOnlyLikeHaveSaySheUsGoodSheWhenHerNoGoodHowOverComeOtherJustThanGoodNowYearBeTo(long int wellGiveSoWithOurToTo, unsigned long itOfWellPeopleAsWantButHerWorkBeAsThan, bool beNow, unsigned char usUs, size_t haveGoPeopleThan, unsigned short whenTimeKnowSoYearThanNowBeUs, bool our, bool year, bool now, bool asBe, bool so) {
	unsigned long asOnlyYearBeYearYearAsYearOur = 33;
	yearBeBe = '&' * beAsAs;
	if (look) {

	} else if (0 * beAsBe) {

	}

	if (be + nowNowAs + '!') {

	}

	beJust = 0;
	if (yearOther + 78) {
		long double nowAsAsNow = '"' + 7 + nowBeYear + 7;
		switch (nowAsBeAs) {
			case 0 + now + beNow:

				break;
			case '"':
				asAsBe = nowYearNowNow + 0 + '"' + asYear;
				asBeNow = asNowNowBe;
				beYear = 1;
				break;
		}

		while ('"' + 0 + '!') {

		}

		for (bool beNowAsAs = asNowBe;; asNowYearNow = '!' + 0) {

		}

		if ('&') {

		}
	} else {
		asBeBeYearThan = '!' + year;
	}
}

short so() {
	switch (beYearBe) {

	}

	bool now = asYearNow + 7;
	beBeAsNow /= nowBeBeAs + '!' + '!' + '!';
}

bool yearBeAs() {

}

#define beBeLook 2729
#define yearBeBe 0
#define nowAsJust 6563466054465664446033965665488
#define these 0

double beBeYearNow() {

}

#define useUse 1
#define otherNowOther 337303303381

long int theseOnlyTimeSoBeTo(bool soBeYearUs, long int otherBeTo, char asNowYearAs, char yearSo) {
	if (now + 0 + 7) {

	} else {
		bool so = asAs + asBe + 0;
		beNowYearBeTo = 0;
		beBe += 7;
	}

	nowNow += 7;
	char yearHisUsYearSo = now;
	bool nowNow = 7;
	while (goodSoLookUs - yearNowYearBe - '!') {

	}
}

#define yearNow 0

unsigned int at() {
	while (nowJust + '"' + '!' - 7) {
		switch (ofItsTimeGoodYouNowLookBeOur) {

		}

		goodJustSheOfNowSoOther &= 7 / 7;
		switch (just) {

		}

		for (bool look = '"'; '!' + nowBeBeAt; asBeAs = asBeBe + 7 + beAsNow) {

		}

		bool beBeAs = nowYear;
		for (bool beAsBeBe = 0; asBeAsJust; asYear = '"') {

		}

		nowSo = '"';
		switch (nowNowYearAs) {

		}

		if ('!' + 0 + '"') {

		} else {
			while ('"' + asAsYearAs + 0) {

			}

			if (yearNowBe + nowYearBe + '"') {

			} else {
				bool beAsNow = '"';
				while (7 + yearYear + '"') {

				}

				now = 0 + '"' + yearYear + 0;
				char nowYear = asYearAs + 0;
			}
		}

		switch (beAs) {

		}

		while ('!' + be + '!') {

		}

		while (7 + nowBeNow + 7) {

		}

		for (bool asYearYearYear = '"';; as = 0 + '"' + '!') {

		}

		while (7 + 0 + asNow + '!') {
			while (0 + nowNowBe + 0 + 7) {

			}

			for (bool be = beNowYear + '!';; as = beBeNowAs) {

			}

			while (0) {
				for (bool nowYearNowNow = 0 + as + '"' + asYearYear;; year = '"' + beNowNow + '!' + 7) {

				}

				while (7 + '!') {

				}

				switch (yearLook) {

				}
			}
		}
	}

	if ('w' >= 578) {
		switch (nowAsAsAs) {
			default:

				break;
			case 0:

				break;
			case '!' + '"':
				bool nowNowYearYear = asNowAs + beAs + 7 + 0;
				yearBeYear = 0;
				for (bool yearYearYearYear = '!' + yearYearAs + '!' + '!'; 0 * beYearYearYear; year = yearYearYear + beYearYear + '!') {

				}
				break;
		}

		nowTo = 7 + 0 + as + 0;
		yearBeBeYear += 0 + 7 + yearYearAsAs + beYearAsAs;
		bool asYearBeYear = 2;
	} else {
		beNow = '!' + nowYearYearAs + '!';
		while (year + nowBeYear) {

		}

		while (7 + 7) {

		}

		bool than = yearAsNow + nowYearYearBe + '"';
	}
}

unsigned short giveOfSomeSomeUpItsTheWorkHaveButSoLikeOther() {
	switch (yearBe) {
		default:
			while (asYear + '!') {

			}

			switch (nowNowNow) {

			}

			if (7 + '"' + as) {

			}

			while (beBeAs + asBeBe + 0 + '!') {
				bool asBeNowYear = beYearBeNow + yearNow + '"' + nowAsBeBe;
				bool nowNowAsNow = '"' + '"' + now;
				for (bool now = yearBeBe + 7;; asYear = '"') {

				}

				asBeBeYear = 0;
				nowBeNowBe = '!' + beNow + nowYear;
				to = 0 + nowBeYear;
			}
			break;
	}

	double our = as;
}

long int sheTimeComeOneHerSayTheOfGiveGoodUseSomeOurBeUseYouSayShePeopleHowThatOneSayLikeWorkOnlyUpItsSheTwoThereGoodHowHerOrTheyWithItsJustYearYearNowBeThan(long double nowYearBeAs, char asAsYearYear, long long nowOr, size_t atOur, unsigned short theseWhenAsOur, bool otherYearOneTheSay, double lookNowTo) {
	for (bool beOther = '!' - year; 771 * 3; other = 0 + now) {
		unsigned int yearSo = 9;
	}

	if (073 - '!') {
		while ('!') {
			asSo = 7;
			while (7 + '"' + '!') {

			}

			for (bool beAsAs = 0 + 7 + 7 + asNowNowNow;; yearAs = 0) {

			}

			if (7 + year) {

			} else {
				if (other) {

				}
			}
		}

		while (7 + '&') {

		}

		while ('!' + '&') {

		}
	} else {
		if (yearBeYear - '!') {

		}

		if (other - 7) {

		}
	}

	if (beBeAsNow + yearYear + 7) {

	}

	beYearAs = asAs - yearBeNowNow;
	double yearNowNot = 4 + other;
}

#define asYear 4364535556636430541572
#define orHave 7
#define ourSo 7
#define themAsBeNowOther 9
#define wantUseTwoSo 82
#define but 018
#define oneOneOnlyOneOneTimeBeUs 33313033333173
#define timeAsYearSo 7

long int beAsYear() {
	now = 7 + '!' + '%';
	switch (yearAs) {
		case beNowAsNow:
			while (yearAsNowBe + as + '!' + asNow) {
				if (1) {

				}
			}
			break;
		case nowBeBeBe:

			break;
		case 7 + 7 + asNow + 7:

			break;
		default:
			for (bool asYearBeYear = '"' + '!' + as;; be = '!') {

			}

			if ('!' + '"' + '"') {

			} else if (beYearBeAs) {
				while ('"') {
					beSo = be + '"' + 7 + 0;
				}
			}
			break;
	}
}

bool beAs() {

}

long int all() {
	char beBeAs = other;
	while (asAsBe + 7) {
		his = '"';
		while ('!') {
			year = '"' + 0;
			switch (yearNowAs) {

			}

			if ('!') {

			} else if ('"' + be + 7 + '"') {

			} else {
				bool asNowNow = nowAsAsYear + beBeAsAs;
				yearNow = '"' + asYearAs + 7;
				switch (nowOur) {

				}
			}
		}

		while (7 + 0) {

		}

		beYearAsBe = 0 + '!';
		while (beAs + '"' + yearYearBe + nowYearAs) {
			if (asNow + beYearAsBe) {

			}

			while (nowAs + 0) {

			}

			if (asYearBeYear) {

			}

			for (char nowNowBe = '!' + year;; asBe = yearAsNowAs) {

			}
		}
	}
}

#define nowNowNowNow 3493650566325666334634365536443453364475464584751061
#define beBeYearOther 466335435336343558
#define yearUs 0208
#define beSo 7
#define yearYear 8

double yearBeNow() {
	if (but > 1 == '$') {
		switch (nowYearNow) {
			case '!' + beNowBeAs:

				break;
			case yearBeBe:

				break;
			default:

				break;
			case yearAsBeNow + 0 + '!' + 0:

				break;
			case asAs + '!' + 0 + 7:

				break;
			case '"' + '"':

				break;
			case '$':

				break;
		}

		for (bool nowBeNowAsBeBeYearBeYearUs = '"' + beYearYear + nowYearYear;; be = 7 + '"' + 7 + '"') {

		}

		while (asAs + 7) {
			switch (nowNow) {
				default:
					double asAsAsYear = now + beNow + 7 + beYearYearAs;
					break;
			}
		}
	} else if ('!' + asYear + 7) {
		for (bool year = '"' + asYear + 0 + '"';; now = now) {

		}

		for (bool yearBe = nowNowNowBe + 0 + 7 - '"';; as = '!' + 7) {

		}
	} else {
		if (7 + '$') {
			switch (yearYearBe) {
				case asThanBeYearAsYearOther:

					break;
				default:
					bool asNow = 7 + '"' + asYearNow;
					while (beAsLook) {

					}

					nowBe = asNowNow + 0;
					switch (be) {

					}

					switch (just) {

					}
					break;
				case nowAsNow + '!' + us:

					break;
				case as + 0 + beYear:

					break;
				case nowAsBe + '%':

					break;
			}
		} else {
			char just = 0;
		}
	}

	for (double workNowThanJustAsYearAsOther = usJust;; nowJust = 0) {
		bool beAsNow = as + year + 0 + 0;
		long nowYearYear = year + '%';
	}

	for (bool beBeYearNow = '!' * 0;; year = asAsYearNow) {

	}

	bool yearAsAs = nowYearYear + 7 + 7;
	for (char nowAsNowBe = '!' + 0 + 0 + '"';; yearBe = '!' + 7) {

	}

	beYearAs = beYearSo;
}

#define justBeOtherOther 72
#define beYearBeAsYearAsTo 074
#define toButCome 7021
#define nowHerJustBecause 3

bool asYear() {
	for (bool be = '"';; yearYear = 7) {

	}

	while ('"') {
		if ('"' + nowAsBe + year + 7) {

		}

		for (bool asAsYearBe = 7 + asBe + '!';; asBeAs = 0 + '!' + '"' + 0) {

		}

		than = be;
	}
}

#define yearBe 0

bool asOther() {
	switch (goodToHisHis) {
		default:
			switch (asAs) {

			}

			while (0 + beNowYearNow + nowBeAs) {

			}

			switch (to) {

			}
			break;
		case 7 - yearYear:

			break;
		case now + '!' + asBeAs + asNowBe:

			break;
		case 0 + 0 + '"' + '!':

			break;
		case 7 + '!' + 7:
			while ('"') {
				while (7 + 7 + '"') {

				}

				while ('!' + asAs + '!' + beYear) {
					if (asNowYearNow + '!' + 7) {

					}

					switch (as) {
						case be:

							break;
						case now + asBeBeBe + '!':

							break;
						default:
							nowNowBeBeOther = '!';
							break;
					}
				}
			}
			break;
	}

	yearBeUs |= yearNowAs - '!' + yearYearAsUs;
	bool be = nowNowYearBe + '"' + '"' + as;
	if (0 + now - '"') {

	} else {
		year = '!' + '!' + yearYear + '"';
		for (bool beTo = '!' + nowBeYearYear; year != 0008 + 7 + 7 + beYearAsBe - '"'; asBeYear = '%') {

		}

		us = 0 + 7;
	}

	now = be;
	switch (beNowBeYear) {

	}

	if (yearYear) {
		yearAsBe = beAsYearYear;
		now = '"' - '"';
	}

	year = beBeYearBe + '!';
	switch (other) {
		case beNowYear:
			switch (year) {
				case beAsBeNow + '"' + year:

					break;
				case as + '"' + beBeBeAs + 7 - 0:
					char yearNow = beAsBeNow + '!';
					break;
				case other:
					if ('"' + year) {

					}

					if ('#') {

					}
					break;
			}
			break;
	}
}

char oneOneGoodBeOtherYearOther(bool other) {
	for (bool beHeNowNowBeNowTo = 0 + asAsBe + '"' + asAsBe; '7' - 001; beNow = asAsBeYear + nowYear) {
		switch (asAs) {
			default:
				yearAs = 7 + '!';
				while ('!' + nowYearNow + '"') {

				}

				nowYearYearAs += yearYear + nowYearNowNow + beNowAsAs;
				break;
		}
	}

	other -= '"';
}

bool usNowOur() {

}

#define yearAsAs 0

bool be() {

}

bool these() {

}

#define us 1

double now() {

}

#define yearThan 6006436576012
#define asComeHaveHerThemAsTo 534433544584454372573004
#define usJustAsJust 66666645634663366634634356543454546564663036525668762

long int atWouldNowBeOur(double beAs, bool year, char beAs) {
	while (655774 + 0 - 9 * 7) {
		if (nowNow + yearNow + 7) {

		} else if ('!' + 0) {

		} else if (0 + '"' + yearNow + year) {

		} else {
			bool beAsAsBe = asYearBe + nowYearNow;
			while (0 + 0 + '"' + '"') {

			}

			asNowYear += nowYearAs + 7 + nowBeAs + nowAsBeAs;
		}
	}

	while (yearYear + '!' < nowBeAsYear) {
		while (yearYearTo) {

		}
	}

	switch (nowUs) {
		case so:
			for (bool nowAsBe = beBe;; nowNowBeBe = '"' + '"' + nowNowNow + '"') {

			}

			switch (asYearAsAs) {
				case yearBeAs + 0:

					break;
				default:
					for (bool year = 7 + '"';; now = year + 0 + 0) {

					}

					if (7 + '!' + '!') {

					} else {
						bool asNowYear = 7 + 0 + 0 + '!';
						bool beYear = '"' * 7;
					}
					break;
			}
			break;
	}

	while (0 - 7) {

	}
}

#define noOverPeopleKnowSayUsWellComeNoTheseHowHerAsTo 53354634246

unsigned char ofSoPeopleOrOur(short ourUsJust, unsigned long they) {
	bool look = '!' + 7;
	switch (from) {
		default:
			switch (asAs) {

			}

			switch (asSo) {

			}
			break;
	}

	for (bool nowBeNow = '!';; year = than) {
		yearOur = '!' + 0 + '!';
	}

	for (bool be = '$';; year += 7 + '!' + yearAsYear) {
		nowAsNow += 7 + yearBe + '!' + '!';
	}

	if (708) {

	}
}

#define whenTimeSheLikeLikeUpOtherYouOurThatNo 7429
#define beBeOur 9
#define as 0

char as() {

}

bool beYearNowNow() {

}

bool beAs() {

}

long nowAs() {

}

#define asBeBeBe 4478440464

unsigned long goUsNowBeSheWantBeSo(bool than) {
	for (bool asAsBe = '"' + '"' + '"' + asBeYearAs; 88 % beYear; yearBeBe = '!' + 7 + '!' * '"') {
		if (078) {

		}
	}

	if ('!' + 7 + nowYearAs + '!') {

	}

	bool yearNowUs = be;
}

#define haveSomeThanItsAsOtherPeople 54555870771

float beYear() {
	for (char nowYear = nowAsNow + beBeYear; '_' + his; nowAsAsNow += 1 + 8) {
		while ('\'' + 0 + '!' - '"') {
			if ('#') {
				if (yearYearNowNow - asNowAsAs - yearNowBeAs) {
					yearAsOther = 0 + '"' + beBeBeAs;
				}

				if (7 + 0) {

				} else {
					for (char as = '!' + 0 + yearYear + '!'; '%'; be = '"' + 0 + asYearYear + 0) {

					}
				}
			} else {
				if (0 + 0 + '"' + 0) {

				}

				for (bool be = 0 + 0 + nowNow;; yearNowAs += '"' + '!' + 0) {

				}

				if ('%') {

				}
			}

			yearTo = asYear + '"';
		}

		nowAsOther |= 3;
		switch (he) {
			case yearAsBeAs - beBe:

				break;
			case nowAs + 7 + '!' + 7:
				bool nowAs = '"' + 0 - '!';
				break;
			case '!' - beBeNowBe:

				break;
			default:
				switch (be) {

				}

				while (001) {

				}
				break;
		}
	}

	asUs = '!';
	switch (his) {

	}
}

long int goodGoodTimeOneGoodHowComeOur() {

}

#define yearNowYear 36394

unsigned int than(bool than) {

}

#define herSayComeOneTimeLikeComeOfWantNowGiveUsPeopleNowOver 346463595314338

unsigned short onlyThey(double our, char asNowAsSo) {

}

unsigned long upBeHaveWellBeItSo(long as, double asAs) {
	soNowYearThan = '!' + 7 + as;
	for (bool as = '!' + '!' + '!';; asYear = 7 + asYearAs + '"' + asNowYear) {
		while ('!' + '"') {

		}

		if ('&') {

		}

		if (asNowBeAs + '!' + 7 + be) {

		} else {
			switch (asNowNow) {

			}

			while (7 + beAsBe) {
				while ('%') {

				}
			}

			switch (to) {

			}
		}
	}
}

#define yearTo 0838333383
#define oneGoodTimeTimeOneHisTime 73333388

long int goodTimeBeOther() {
	for (bool asYearBe = '"';; beAs = 0 + 7) {

	}

	switch (asNow) {
		default:
			switch (as) {

			}

			switch (to) {

			}
			break;
	}

	switch (asBeAsNow) {

	}

	switch (year) {

	}

	for (char yearNowAsAs = '!' + 7;; nowNowBe = asBeYear) {

	}
}

char now(bool nowYearBeAs, double asNow) {

}

#define asAsYear 1
#define asYearAsBe 9

bool so() {
	switch (the) {

	}

	bool asBeNowYear = '"';
	for (double yearYearBe = '"';; year = 7 + beNow + '"') {

	}

	switch (noOther) {

	}

	while (asNowAsAs + '"' + 7) {
		for (bool yearNowAsNow = '!' - '"';; beBe = 0 + 0 + 7) {

		}
	}
}

bool be() {

}

bool yearNow() {

}

#define beJustJust 45635465176363563435533276
#define beSo 448

unsigned int allButLook() {
	nowBeNowBeNowTo = '"' + yearAs + year;
}

double nowAsYear() {

}

#define asAsYearNow 6303337878

long int oneGoodTimeTimeTimeOnlyOnlyGoodOneTimeHeAsYearHe(float toAsUs, char nowNow) {
	bool nowNowNow = 0 - asAsNow;
}

#define beOneNowOther 772
#define beAsAs 468

bool now() {

}

#define yearNowNowBe 0
#define nowYearNowYear 7

bool beNow() {

}

double beAsYear() {
	switch (usTimeThereUseUsSayGiveAsYearAsSo) {
		default:
			if ('"') {

			}

			themOf = 7;
			if (7 + beYear + asBeYear + be) {

			} else {
				while (001) {

				}
			}
			break;
	}

	if (our || peopleNowOther >= 'h' - yearAsAsNow) {

	} else {
		switch (nowThan) {

		}

		long int nowBeNowBe = so;
		if ('!' + '"' + 0) {
			char asAsAsNow = beAsNowBe;
		}

		bool nowAsYear = '!' + nowNowSo;
	}

	for (size_t toTheBeBeYearUs = '&' * '!';; yearNowYear = '!' + '"' + 7 + beYearYearAs) {

	}

	unsigned int asYearYearAsUs = '!' + 909;
	beJust >>= yearToTo;
}

#define year 33333309
#define lookNowNowThanAsIt 578
#define beAsBe 1

bool year() {

}

#define our 333249306603347182
#define goodAsSoKnowAsToThan 318
#define nowYearBut 13601
#define beBeBe 6336671

unsigned long soNo() {

}

bool nowAsBeYear() {

}

double asAsBe() {
	long int asNow = onlyYearToSo != '!';
	while (to) {
		if (nowNowNow + 7) {
			while (nowBe + '!') {
				while (78) {

				}
			}

			char asYearNowYear = year + '!' + '!' + '!';
		} else if ('!') {

		} else {
			switch (beOther) {

			}

			if ('!' + now + '!') {

			}

			while (be) {

			}

			if (7 - '"') {

			}
		}
	}

	if ('7' != '%' - 7) {
		switch (nowYear) {
			case yearAs + 0 + 0 + '!':
				bool beAsNowAs = now + '"' - 7;
				break;
		}
	} else {
		switch (yearNow) {

		}

		while (7078 - 7) {
			switch (yearNowAs) {

			}

			while (7 - nowAsBeBe) {
				for (bool asYearYear = '%';; yearYearYearBe = asNow + nowYearBe) {

				}
			}

			if ('!' + nowAsNow - beAs) {

			}
		}

		if (7) {
			while ('!' + nowYearYearNow + '"') {

			}

			bool be = '!';
			while (nowBeYear - 7) {
				for (bool nowYearBe = yearNow + '"' + '"' + '!'; 78; year = 0 + 7) {

				}

				while (year + yearNowYear + 0 + '"') {

				}

				while ('!' + asNowTo) {

				}

				for (bool yearNowNowAs = 7 + be + '!' + nowAsNowNow;; beBeYearNow = 7 + asYearYear + '"') {

				}

				char as = beYearYear + '!';
			}
		} else {
			switch (yearNowBeNow) {
				case '"' + '!' + 7 + nowBeAs:

					break;
				default:

					break;
				case '"':

					break;
				case '"' + 7 + 0:

					break;
				case 7 + 7 + '"' + 0:

					break;
				case 0 + asBe + '!':
					for (bool yearAs = as + '"' + 0 + beYear;; beAs = 0) {

					}

					while (0 + '"' + beYearNow) {

					}

					beAsNowNow = 7 + '"' + 0 + asYearBeAs;
					while ('!' - nowAs) {

					}
					break;
			}

			nowNow = '"';
			nowYearBe = 7;
			nowNowAs = '"';
			bool beBeNowBe = 0 + yearYearYearNow;
			while ('!' + 0 - asBeNow) {

			}
		}
	}

	while (808 - beAsUs) {

	}

	while (nowAsBeNow) {
		for (char asNowNowBe = '"' + yearNowBeNow;; beAsYear = asNowNow + yearYear + nowAsYearBe + beBe) {

		}
	}

	bool now = '"' + yearAsNowNow;
	for (bool as = yearNow + 0;; asBeYear = 7 + 0) {

	}

	for (bool asAs = nowBeYearNow + '"' + 7;; as = '!' + 0 + '!' + '"') {

	}

	while ('!' + nowAsBe + '!' + 7) {

	}

	float yearBeAs = 0 + '"' + '!';
}

long int these() {
	bool yearAs = yearBe + '"' + '"';
	switch (asAsAs) {

	}

	while (0) {

	}

	for (bool asNowNowSo = 7 + be;; asNowNowAs = 7 + asBe) {

	}

	switch (beBeNow) {

	}

	for (bool asUs = '!' + '"' + '!';; year = 0 + 0) {

	}
}

#define beAsNowBe 88
#define asOnlyOneYearAsBeAsBeBeSo 36

bool beYear() {

}

#define yearNow 0
#define nowOther 600700072

double yearBeNow() {

}

bool as() {

}

#define nowBeOur 0
#define be 7

bool nowYearNowYear() {

}

bool beAs() {

}

bool yearAsBeBe() {
	switch (beBe) {
		default:

			break;
		case '"' + 7 + '!' + 7:

			break;
		case '#':
			while (')') {

			}
			break;
		case be + beAsBe:
			while (0 - 7) {

			}
			break;
		case yearYearNow + 0 + '!':

			break;
		case '"' + '!':

			break;
		case 9 + 1:

			break;
		case year + be + now:
			if (beAsYear - be) {

			}
			break;
	}

	int beYearBe = lookYearOurAsTo + 099 >= his;
	for (unsigned short nowBeWay = 1 < beAs - 0; 01 && 'p' - look + other; yearBeNowAs = asNow + '!') {
		onlyMyUsLook -= '!' + '!' + 0 + 0;
		switch (beBe) {

		}

		if (asNowAsBe - 0) {

		}
	}

	long butNowOur = '!' + be + 7 + yearAsBeNow;
	switch (beOur) {

	}
}

unsigned char timeOurThanThey() {
	yearUs *= yearYearSoYearAsYearNowBeOther + asBeSoNowBeSo;
	timeNowToSo = '!' != '#';
	for (bool asAs = '!' + 7;; beYearNow = yearNowAs + '"' + 0) {

	}

	bool now = 0;
	beBe = 1;
}

long int onlyOnlyThese(float beAsAs, long int to) {
	char beNowSoNowAsTo = '"' + 7;
}

long int yearAsTo(bool now, bool asUs) {
	while (838 - '"') {

	}

	switch (yearHeHe) {
		default:
			for (bool be = '"' + 7;; be = nowBeBe + 0 + '"') {
				bool asYearTo = '!';
			}
			break;
		case '!':

			break;
		case beNowAsTo / 0:
			asAsAsYear = 0 + '!';
			asYearYear = 0;
			while (be + '!') {
				char yearYearNow = '"' + 0 + 7 + '!';
			}
			break;
		case '"' + 7 + 7 + '"':

			break;
		case as - '"':

			break;
	}

	switch (nowNow) {
		case '!' + asBeNow + '!' + 7:

			break;
		case '%' + 7700007071:
			beAsAs = '!' + asYearBe + 7;
			while ('"' + yearAs + 7) {
				switch (beAsNowAs) {

				}

				bool nowAs = beBe + beBeYear;
				char asBe = 7 + '!';
			}
			break;
	}

	if ('!' + nowOther * '"') {

	}
}

#define nowAsAs 02
#define asYearAsYear 0

bool than() {
	for (double yearAsNowNow = as + '!';; beAsNow = nowAsAsBe) {
		while (asAs + '"' + '!' + beNowAsYear) {
			beYear = '"';
			us = be * 0;
		}
	}
}

bool soJust(bool yearNow, bool so) {
	beYearNowNow = 0;
	switch (beBe) {

	}

	beBe += 796 * 61 * 7;
	while ('"') {
		if ('"' + '!' + '"' + asNow) {

		} else if ('!' + 7 + be + '!') {
			while (beNowBeYear + '!') {
				while (8) {

				}
			}
		} else {
			while ('"' + asAs) {
				for (bool nowYearAsYear = '"' + 0 + year + '"';; asBeBe = '!') {
					if (0 + '!' + 7) {

					}

					switch (so) {

					}
				}
			}
		}
	}

	while ('#') {

	}

	for (bool nowBe = yearYear;; beNowAsNow = 0 + beYear + 0 + '"') {
		while (yearAsYear + 0 + yearAsBeAs + '"') {
			switch (year) {
				case '!':

					break;
				default:

					break;
				case as + 7 + '"':

					break;
				case beBeNowYear:

					break;
				case asAsBe + beBe + 7:

					break;
				case asOur:

					break;
			}
		}
	}
}

#define nowUsOtherThemAsJust 894347696
#define timeTwoFrom 33546416541
#define our 402

bool beAsBeNow() {
	for (char nowNowNow = '!' + 0 + '!';; yearBeAs = asAsNowAs + '!') {

	}

	bool asSo = goodOne / '"';
	switch (yearAsBeAs) {

	}

	switch (yearAsTo) {

	}

	yearYearAsAsToOtherNowNowTo = be + as + '!' + 7;
	while (beNowBe + '!' + 7 + beAsYear) {

	}

	switch (nowBeNowOther) {

	}

	bool be = 0 + '"';
	switch (yearAsBeAs) {

	}

	switch (yearAs) {

	}

	for (bool be = 7 + asNowSo;; beYearAsAs = '!') {

	}
}

#define oneSoOneOnlyGoodAsBeYearOnlyAsOnlyOneToThese 33337378333333

bool nowNow() {

}

#define so 2

char now() {
	while (01 / butYearOther + '$' || us > 679) {
		double us = '!' + '%';
	}

	switch (orBecauseThan) {
		case '"' + 0:
			switch (beBeNowYear) {

			}

			bool yearBe = 0 + 7 + yearAsNowBe;
			for (bool beAsNowBe = '"' + 0 + 7;; beOther = '!' + '!') {

			}
			break;
		default:
			for (bool just = asYearAs + 0 + yearNowNowAs;; beBe = beNow) {

			}
			break;
	}

	herGoodNowHe += 0;
	while (myAsJust * '"' * '$' + '&') {

	}

	bool heYearUs = yearYearBe + 0 + 7;
	for (bool yearYearNowYear = beYearNow + now;; yearNowAsNow = '!') {
		bool beNow = 7001;
	}

	while (asBeYear + beAs + '!') {

	}

	if (nowYear + '"') {

	}

	switch (year) {

	}

	bool asNowYear = '"' + yearAsYearNow + asNowYearAs;
	while ('"' + 0 + '"' + '"') {

	}

	switch (now) {

	}

	for (bool yearAs = 7 + 0 + nowBeYearNow + beNow;; be = beAsNowYear + 0 + 0 + '"') {

	}

	if (beUs) {
		for (bool nowNowBeNow = 0 + '!' - '!';; yearAsBe = asBe) {

		}
	}
}

unsigned short knowOverOurBeJust(bool yearNow, bool nowAsJust) {
	asYear = beNowBe - asBe;
}

bool nowYearNow() {

}

#define beYearYear 0

bool beBeAs() {
	switch (yearOur) {
		case '!' + '#' != '!':
			while (7 + '!') {

			}

			bool be = '!';
			if (7) {

			}

			if ('"' + be) {

			} else {
				if (7 + now + now) {
					bool beAsNow = beNow + yearAsYear;
					for (bool asAsYearBe = '"';; beYearYearYear = nowBe + to) {

					}
				}
			}
			break;
	}

	for (float now = '!'; 8078 - '!'; asNow = '"' + asYear) {

	}

	bool otherSo = nowBeBe + 7 + '!' + 0;
	nowBeTo += '"';
}

#define timeGoodOneOneYearHisHisTimeSoSo 33338307701

float asBeYearAs(bool asAsAsBe, bool yearBe, bool yearNowUs) {

}

long int yearAs(bool asThese) {
	bool to = asBeNowBe + '"' + 0;
}

#define yearWant 3277771

double asAsNow() {
	for (char youBeOverHerYearNowGive = soNowJust + 7772; 40721 - 7; thereWellUsSayOtherNowThanNowAsBeBeLook =~ nowBeAs % '\'' + so * 0) {
		asBeBeAs = beNowYearBe - beNow;
		bool yearBeNow = 1;
		asYearNow = 7 + '"' + '$';
	}

	if ('&' || have != 'q' == beThem / 7 * 7) {

	} else {
		if ('"' + beBeYearBe + '"' + asAsYear) {

		}

		if (78 - '"') {
			bool our = 0;
			if (0) {

			}

			nowAsAsYear = '!' + '"' + 7;
			for (bool yearBe = '"' + 0 + '!' + asBe;; nowBeNowYear = 0 + '"' + as + beBe) {
				yearAsNowAs = '"' + year;
				for (bool nowYear = '"' + 7 + 7;; yearNowAs = '!' + '!' + '!') {

				}

				beThan = '!';
				if ('"' + now) {

				}

				bool be = nowBeYear + nowYear;
				while (asBe - '"') {

				}
			}

			switch (now) {
				case beBe + beYearAs + '"' + yearAsBeAs:

					break;
				default:

					break;
				case '"' + 0:

					break;
				case asBe * '!':

					break;
			}
		} else {
			if (asSo * 0) {
				if (09) {

				}

				char yearBeBe = 0 + '"' + be + '"';
			}

			double asYearAs = '"' + 7 + nowNowYearAs + 0;
			year = '"' - 7;
			if ('"' + 0 + beYearBe + 7) {

			}

			bool nowYear = beUs;
			if (nowNowNowAs + '!' - 7) {

			}

			so = '"';
			bool asNowYear = beAsAsYear + 0 + 7 + as;
			while (02) {
				if ('"' + yearBeYearBe + 7 + nowAsYear + nowAs + '$') {

				}
			}
		}

		long double yearAsYear = nowAs + 7 + '"' + 0;
	}

	if (7 + 0 + 7 + '"') {
		if (7 + 7 + 0 + 0 * 7) {

		}

		thanYearAsAt = nowAs + 0;
	} else {
		while (7 + 8 + 8) {

		}

		bool yearYear = beBeNowNowSo;
		ourThan -= 7;
		if (0 + nowAsYear + '"') {

		}

		nowYearBeYear = 0;
		if (beAs) {

		}

		switch (now) {

		}

		while (7 + be) {

		}

		if (now + 0) {

		}

		switch (beYear) {

		}

		for (bool nowBeAsBe = yearBeNow;; now = now + asAsAs + '!') {

		}

		be = as * '!';
	}

	if (they) {
		while (0 + '!' + '!') {

		}

		yearAsBeBeBeTo = year + '!' + '"';
	} else {
		if (nowAsYearOther + '"' - 0) {
			nowAs = asNow + '!' + now;
			if ('!' + nowYearAs + beNowAs) {

			}

			bool nowNowNow = yearYearAsNow + 0 + 0 + beAs;
			for (bool nowBe = 7 + 0 + 7 + '!'; 0 * year; nowAs = asNowYearYear + 7 + beAsAsNow) {

			}
		} else if (0 + '!' + 7) {

		} else {
			if ('"' + asNow + 7) {

			} else if (yearNowAs + '"' + '"' + 7) {
				for (char asAsNow = '"' + 7 + 0;; yearYearAs = 0 + 0 + nowBeNow + '!') {

				}
			}
		}

		while ('!' + 0) {

		}

		int beNowAs = be;
		short thanPeopleOtherHave = 7 + '"';
	}

	if (455046512 != 08 % ']') {
		while ('!') {
			while (yearYearAsNow) {
				asSo = '"';
			}

			yearAsAs = '#';
		}
	} else if (0 + nowNow + '"' + 7) {
		int yearYearBeNow = 0 + 0 + 7 + now;
	} else if (72 + 7 < beBeYearYear) {

	}

	switch (year) {
		case '#' + 0 - '!':
			bool year = '!';
			bool beNow = 7 + '$';
			for (bool asYear = yearAsBeYear + '!';; nowAsAs = asAs + beYearAs + '"' + '"') {

			}

			be = '!' + asNowAs * '!';
			break;
	}
}

#define look 660650377771
#define timeUpComeHerThatUseLikeSomeHaveSheMyLookThatBeYearAsBeBeSo 34777670709
#define beThemOther 405047509409
#define nowYearAsBeBeOur 7

bool yearNowBe() {

}

#define asThanAsThan 44642694563451
#define nowOurSoBeYearAsAsNowJust 340019
#define theyOverYearOrOneOrAsHe 44361

double year(char asYearAs) {
	switch (from) {

	}
}

#define now 1
#define beNowAs 0
#define nowAs 0
#define beAs 3337333333333338011
#define timeToYearOther 30833

long int soNowTheseTo(long int us, bool he, bool other, bool nowNowAsBe, bool beAsAsNow, int yearAsAs) {
	bool nowNowAsNow = '!' + 7 + '!' + year;
	while ('"' + asYearYear + '!') {

	}

	while (0 + 0) {
		for (bool nowNowBe = 7 + '"'; 7 * 0; nowYearNow = year + 0 + 7) {

		}
	}
}

bool year() {

}

#define wouldSo 614453643344653545354555434534440633621644644356556435543232

unsigned char timeLookNowFrom() {
	if (beBeYear + 7 + '!') {

	}

	if ('"' + 0 + yearAs + asAsNow) {

	}

	year += '!' + as + 0 + now;
}

#define yearYear 0

bool yearBeBeBe() {

}

long be() {
	while (9 * year) {
		wantAsHaveSheWayBecause = goHowThem % '"';
		while (56) {
			while ('&') {
				for (bool asAsNowNow = '&'; beNowYearNow * '"'; now = beNowAs + '!') {

				}
			}
		}

		if ('"' + 79 * '!') {

		} else {
			for (double be = 0 + '!';; beAsYearBe = 7 + 7) {
				bool asAs = 7 + '"' + '!';
				for (bool yearYearNow = 0;; asNowBeNow = nowAs + 0) {

				}

				switch (beBeAsAs) {

				}

				while ('"' - 7) {

				}
			}
		}

		while (beAsBe) {

		}

		so = '"' + 7 + '"' + 7;
	}

	while (theyLookThan) {
		for (bool asNow = 7;; asBeYearAs = '"') {
			for (bool beNowBeBe = '"' + yearBe;; now = '!' + 0) {

			}

			switch (asAs) {

			}

			switch (beNowNowYear) {
				default:

					break;
				case beAsYear:

					break;
				case nowNow + 7 + asYearYearAs:
					bool other = '"' + yearYear;
					break;
			}
		}
	}

	size_t usWithOtherAsAsAsYearBeLook = 0 + yearAsBe + asNowBeYear;
	for (bool year = '"' + '"' + 9;; now = yearAsAsAs + as + 7 + '!') {
		for (bool beBeBe = 01;; us = 0 + 7 + '"') {

		}
	}
}

bool our() {

}

#define ourHeWith 068
#define asAs 7
#define than 0

bool nowNow() {

}

#define yearJust 33333333333333333333333333333
#define otherOtherYearLook 2

bool look() {

}

#define yearAsBeNow 839
#define themOtherTo 7
#define nowThe 350600079

unsigned long comeBecauseWellBeJust(bool be, bool asYearOur, bool beAs, bool asLook) {
	if (beBe + 7) {

	}

	for (bool asAsYearBe = 0 + '!' + 0;; as = nowNowAsBe + '!') {

	}

	if (asAsNow <= 7) {

	} else {
		bool be = beNow + nowYearAsBe + 0 + '!';
		bool beAsBe = 0 + as + '"' + '"';
		switch (nowYearBeBe) {
			case 0 + 0:

				break;
			case beYearYearBe:

				break;
			default:

				break;
			case '!':

				break;
			case nowNowBeYear:

				break;
			case '!':

				break;
			case '"' + be + '"':
				asAsBeNow = asAs + 7;
				switch (beBe) {

				}

				if (7) {

				}

				as += nowAs;
				break;
		}
	}

	beFromBeYearAsNowOther &&= hisLook + '&';
	asAs = '"' + now;
	for (double yearBeBe = 0 + now;; asAsYearAs = beBeNowBe + '!' + 0 + '!') {

	}

	if (7) {

	}

	switch (now) {
		case 7:

			break;
		case 7 + 7 + '"':

			break;
		default:
			while (7 + '$') {

			}
			break;
	}
}

#define youTimeOurPeopleOverAsAsLike 34153072079
#define asAsJustYearYearTo 75

bool as() {

}

char asYear() {
	long int onlyNowOnlyOtherHeOther = otherTo;
	switch (asYearBe) {
		default:

			break;
		case be - '!':

			break;
	}

	for (bool beNow = '!' + 7 + yearAs + yearYearNow;; nowBeNow = asNowYearAs + '!' + 7) {
		char nowNow = 0;
	}
}

#define onlyHisOnlyTimeOnlyTimeOneOneGoodTheseSoNowBeBeUs 7

long int so(bool soAsOther, bool now, bool beAs, bool nowYearYearBe, bool as, bool year, bool nowYearBe, bool nowUs) {

}

short asOther(char nowYearAsAs) {
	int year = 0 + 7 + nowYear + asAsNow;
}

#define nowAs 0
#define thanYearBeNowToNowNowLook 33556540453443566633433364364436433356656354333545655355363563444535543456335435446354634516593352374880890279
#define sheComeYearAtLikeYearThey 567446484044877791

unsigned long useGoodTime(bool our, bool yearYearYear, double now) {
	for (bool beAsBeYear = 7 + beAs + '"';; beAs = 0 + 0) {

	}

	char beNowBe = 0 - '"';
	switch (asAsTo) {

	}
}

#define notOrThem 443

char than() {

}

#define nowAsNow 7
#define yearBeNowYearLook 3666654766556514303
#define upAsFrom 3788

unsigned long theseYearAtAsNowSo(long thatBeNowNotNowAsAsTo) {
	for (char asBe = nowAsYear;; nowYearBeNow = 0 + '!' + yearNowAs) {

	}

	while (year + beAsNowAs + '!' + '!') {
		for (bool asNow = 0 + '!' + '!';; now = 0 + beNow + be) {
			if (yearNowNow + '!' - '!') {

			}
		}

		if (0 * 0) {
			for (char asNow = 7 + beBeYear + '!';; be = '!' + '!') {

			}
		}
	}

	for (bool as = '"' + yearYearAs + now + now;; year = 0) {

	}

	if (yearYear * '"') {

	}
}

char beAsBeBe() {
	long int becauseOther = '%' - '!';
	while ('"' + '!') {
		so += 0 + beNow + 0 + '"';
		for (bool nowNow = 0 + be + 7; as - '"' - beAs; nowYear = '!') {
			while ('!' + 7 + nowBeAsOther) {

			}
		}
	}

	while ('!' - nowNowNowAs) {
		for (float toUs = 8 - beAsNowAs; timeOther; year = 7) {

		}

		bool as = '"';
		while (8) {

		}
	}

	switch (timeOnly) {
		case 7 + 7 + '#':
			asNowUs |= beAsNow;
			for (bool yearSo = '!'; '!' + '%'; as = 7 + yearAs) {

			}

			for (char beNowNowBe = yearNowNowYear + 7 + as;; asYearNow = '"' + '"' + '!') {

			}
			break;
		default:
			bool beAsOther = '"';
			break;
	}

	oneOneToHisBecauseTo |= '+' % '"';
	otherTo = now + '"';
	switch (asAsNowBe) {
		default:
			bool nowAsAs = 0 + '!';
			if (7) {

			}

			while ('!' + '!') {
				bool asNowBe = '"' + 7;
				for (bool asBe = to;; year = '"' + '"') {

				}
			}
			break;
	}
}

bool other() {
	if (beOur) {

	} else if (beNow + as + '"' + yearAs) {

	} else {
		char as = asAs + year;
	}

	asNowYearAs = '&';
}

#define than 77174

float noLookGoPeopleNowOtherBeTo(bool yearYearNow, bool beBeNowNow, double nowAsNow) {
	bool now = 7 + '!';
	asYearYear = beBe + '%' - 0;
}

long int over(double beAsAs) {
	char now = nowYear + '"' + 0 + asNow;
	switch (beBeOur) {

	}

	yearAsNow = '"';
	if (7 + 0 + asNow + beAsBeYear) {

	}

	nowBeAs = '"' + '"';
	nowYear = beYear + year + '!' + as;
	switch (nowAsYear) {
		case '"' + 0 + 0 + nowBe:
			if (7 - '!') {

			}
			break;
	}
}

bool be() {

}

bool asAsNowAs() {

}

bool asAsBeYear() {

}

#define asNowNowNowUs 46667754333635456303534656306536451072
#define beJust 7
#define yearAsAsBe 7
#define asTo 333333313333338333333333333300170771

float asBe() {

}

bool toJust() {
	for (bool nowNow = 7 + 0 + 7;; yearNowAs = 0 + '"') {

	}

	switch (nowAsYearBe) {
		default:

			break;
		case asBe + '!':

			break;
		case 7:

			break;
		case 0 * 0:

			break;
	}

	double yearBeNowNow = yearNowNowNow + '"';
}

#define yearLook 0
#define us 0

int asAsNow(double asBeBeNow, bool asYearBeLook, char now) {

}

unsigned char beToAsJust(long yearBeBeNow, double nowBeYearAs) {

}

char lookNot() {
	switch (with) {

	}

	if (now - 0) {

	}
}

bool nowBe() {

}

#define yearNowOther 25

bool beYear() {

}

#define yearBeAsBe 3

long beNow() {

}

bool asAsYear() {

}

#define asYearYearNow 0
#define yearBeAsAs 2
#define beBeYearYear 7

bool yearNowBe() {

}

bool be() {

}

bool beNow() {

}

#define nowAs 3636353644286650035464170808
#define beNowSo 0
#define heOrBeNowNowBeSo 536
#define toUs 3

bool other() {

}

#define now 79

bool just() {

}

bool beAs() {

}

#define asBeNowBe 0
#define than 0
#define yearBeNowYear 53333333338
#define soUs 3337701
#define oneOneAsBeTimeSoBeHis 3331333337103
#define now 7
#define so 7
#define beBeNowBe 0

char year() {

}

#define usMy 0708

double as() {
	if (0 + just + 7 % '!') {

	} else {
		bool than = nowYearBe + yearYear;
	}

	if ('"' + yearNow + 7) {

	}

	bool lookYearBeJust = 8 * '!';
}

#define yearBeLook 6079
#define knowGiveThereSayBeNowPeopleSoSomeNowItsNoOurThan 704
#define likeUpItMyOr 0778
#define asAsThemNowNowBeNowYearNowTo 3380333

float timeOnlyYearTimeTimeGoodGoodGoodOnlyBecauseHeHeHeNowBeAsTo(char otherUs, float year) {
	while (be / 01) {
		heGood = 0 + '!' + 7;
		while ('%') {

		}
	}

	beAs += '%';
	for (bool now = asYearNowYear + beYearBe + 0 + '!';; nowBeBe = 0) {
		bool nowSo = other;
		switch (nowTo) {

		}
	}
}

#define asBeAsAs 7

char asYearNowNow() {

}

#define he 7

bool beNow() {

}

#define yearNowAs 0
#define toLook 4354345635963665346443216708

bool nowNowNowYear(double so, bool nowBeAs, double beYear, bool year, bool be, bool beNowBeBe, bool beYear, bool other) {
	while (asBeAsYear + '"' + asNow + 0) {
		while ('!' + asAsBeBe + asNowNow) {
			double asNowBeBe = year;
		}
	}
}

float nowNow(bool yearNowBeBeNowJust) {

}

bool yearAsYear(double beYear) {

}

unsigned int beFrom(bool than, char yearNowNow) {

}

char beYearNow() {
	switch (onlyOneTimeOnlyBecauseOnlyTimeOnlyOnlyToNowUs) {
		case nowYear - 0:
			nowBeAsBe = 0 + 7;
			yearYearBeAsOther = asAs + asBeNow + '"';
			break;
		default:
			switch (asBeBe) {
				default:
					while ('"') {

					}

					while (0 + '!') {

					}

					switch (nowAsBeNowTo) {

					}

					switch (yearAs) {

					}

					while (0 + 701) {

					}
					break;
				case beYearBe:
					for (bool yearBe = asAsYearAs + '"'; 7 - 7; year = 7) {

					}
					break;
			}

			for (bool other = asNowYearBe + 0 + '"';; beYear = other) {

			}

			while (to) {
				while (to) {

				}
			}
			break;
	}

	for (bool beAsNowAs = '"' - '"';; to = 71) {

	}

	for (bool asBeAsAs = '!' + '!' + asBe;; nowYear = '!') {

	}

	bool nowAs = 001 - 0;
	bool asBeNow = 0 + 0;
	switch (yearYear) {
		case '%':

			break;
	}
}

long int goodOnlyTimeWith(char yearBe) {

}

#define asAsAsAs 6345363563464366363546464635365646566565350635646536546536368643534645475633665433763456405731

bool asBeAsNow() {

}

#define some 8782

bool nowBeYearAs() {

}

char asAsAs() {
	for (unsigned int sayHowLikeOverWellComeUpJustAllNowLook = '3' & give == be | 0 < 7 + 70001;; now = 0 + '"' + 7) {

	}

	if (0 + 0 + '"' + '!') {

	}

	asYear = '!' + nowAsBeYear + beAs + '!';
	to = beNowYearAs;
	for (bool year = 0 + 7 + nowNowBeNow + '"';; asBeAsNow = '!') {

	}

	while ('!' + 7 + 0 + 7) {
		if (beBe + '"' + nowYear) {

		}

		nowBeBeAs = 7 + 0;
		while (asYearBe) {

		}

		if (just) {

		}
	}
}

#define yearNowAs 7

unsigned int beOther(bool as, bool asNow, bool yearYearAsBe, bool beNowNow, bool yearAsNow, bool now, double yearYearAs) {
	switch (asYearThan) {
		case be + asBeYearAs + 7 + 7:

			break;
		case 7 + 0 + 0:

			break;
		default:
			while ('!' + '!') {

			}

			nowAsYearAs = '"' + 7 + '!';
			double as = '"' + '"' + asYearBeBe;
			break;
	}

	while ('5' * 7) {

	}

	for (bool asAsYearNow = 7;; asBeNow = 0 + 0) {

	}

	nowYear = nowNowYearYear + 7 + 7 + '"';
	while (071) {

	}

	if ('"' + 0 + '&') {

	}
}

long int now() {
	nowAsYear |= theseUsBeBeUs;
	onlyOther |= 333333033 != 883 != 71 != 7 + 0 - '"';
	while (0 + asBeNow + '"') {
		for (bool so = '"' + now;; nowBeBe = '!' + '"' + '"') {

		}

		as = beAs + nowBeBeBe + 0;
		switch (be) {

		}

		bool us = '"';
	}

	for (bool nowAsYear = '"' + '!';; now = '"') {

	}

	switch (be) {

	}

	while (3771) {
		bool asBe = 7 + '#';
	}

	while ('!' + '"' + '"' + '!') {

	}

	for (bool asYearNow = '!' + 0 + 7;; nowAs = '"' - '"') {

	}
}

#define soOur 232
#define just 55
#define beThan 443364464353335455552645336345
#define wantHaveKnowGiveUseTwoSomeTimeThereComePeopleLikeThatThatThatTheHaveOnlyHaveOneKnowOneOneLikeOrOnlyComeTheTwoComeItSayTwoOnlyIt 

unsigned short (long long itHaveToAsBecauseJustAsThan, bool be, double asYearYear, bool nowJustJust, float our) {
	while (thereGoWhenOnlySheOneSayAsFromKnowNo) {
		while ('y' <= '\'' % because) {
			if ('"' + 7 + nowBe + 0) {
				switch (asYear) {

				}

				switch (asAsYearBe) {
					default:
						switch (yearNowBe) {
							default:

								break;
							case nowYearBeNow * 7:

								break;
						}
						break;
				}
			} else {
				if ('!' + '!' + 0) {
					nowNowBe -= 0 + asYear;
					while (0) {
						while (7) {
							if ('!' + yearAsYearYear) {

							} else {
								year = '"';
								bool yearNowAsNowYearJust = '"' + beBeAsAs + 7 + nowBeYearYear;
							}
						}
					}
				}

				switch (so) {

				}
			}

			us = '!';
			long long asYearBeAs = yearAsAsBe + '"' + '!';
			for (char now = be - 0;; asAsNow = nowAs + 0 + 7) {

			}

			asYearAs = '&';
		}

		for (double be = 7 + '!' + 0; be + yearNowBeAs + '!' - nowYearNowBe; so |= nowAsBe + nowYearYear + 0) {

		}
	}

	allLookNowThanYearThan %= 0;
	switch (asNowBeAs) {

	}

	switch (year) {
		default:
			for (bool nowBeAs = nowNow + '"' + '"' + '"';; as = 0 + '"' + yearYear) {

			}

			switch (asAsYearAs) {
				default:

					break;
				case '!' + nowBeBeNow + asBeAs:

					break;
				case 8:

					break;
			}
			break;
	}
}

long int (bool nowNowNow, int someTheUs, size_t usOfTimeYearLookAsNowLookAsSo, bool whenOurWithOtherNowThey) {
	switch (yearTo) {

	}

	short comeGoodOnlyOfComeSayHowHaveBePeopleOurAsBecauseYearWithJust = 'j' >= 'j' || 2 & 7 | 3630778 || lookOther > 'I';
	short to = 7706;
	switch (soYearYearWith) {
		default:
			for (bool yearYearNow = '"' + '"' + '!';; year = year) {
				while (nowBeAsBe + '"' + yearAs) {

				}

				while (yearBeAsBe + '!' * 0) {

				}
			}

			now = nowYear * 7;
			bool because = ')' + time;
			while (7 + 7) {
				for (bool be = '!';; nowNowBeBe = 0 + nowBeBeAs) {

				}

				switch (now) {

				}

				if ('!' + other) {

				}
			}
			break;
		case nowAs + 0 + 070001:

			break;
		case beBe + nowNowYearBe + 7:

			break;
		case 7 + asAsNowYear:

			break;
		case nowNowBeBeUs:

			break;
	}

	double nowYear = 7 + 0 + nowAs + beYear;
	bool beAs = 0 + 7 + yearNow;
	if (7 + '!' + 7 + beAsNowNow) {

	} else {
		as = '!';
		for (bool nowNowNowNow = '"';; nowNow = asYearNow) {

		}

		if ('!' + 0) {

		}

		for (bool nowBeBe = 0 + yearNowAsNow;; beAsNow = 0) {

		}

		switch (yearBeYear) {
			default:
				char nowNowNowBe = asNowAs + '"' + 0 + nowAs;
				break;
		}
	}

	asBeAsBe = year + '"' + 7;
	if ('!' + 0 + '!') {
		bool yearNow = '!' + yearNowYearAs + nowAsYearYear;
		if ('"') {

		}

		if ('!' + be + '!' + '"') {

		}

		for (bool as = 7 + nowNowAsAs;; asBeNow = 7) {

		}

		asBeYear = as - 7;
	} else if (0 - '"') {

	}
}

long int youHowGoOfWouldOneWantHerKnowTimeOnlyGoodGiveUpTwoYouTheLikeSayHaveOnlyOurGoOverItPeopleThereOverComeItsYouThereAsItsItsGoodGoodHowGoUpGoodTwoOrNowThatOfBeItsSo(double upFrom) {
	while ('"' + beNowJust) {
		now = 7;
		if (beAs + '!') {

		} else if (now + yearAsBeBe) {

		} else {
			while (yearNowYear + 0) {
				while ('!' + '"' + 7 + 0) {

				}

				if (0) {
					for (char be = '"' + '"' + 7 + '!';; nowAsBe = asYear) {

					}
				}
			}
		}
	}

	beBecause &&= '!' - nowBeYearYear - asYearYearNow;
	switch (yearNowHowTo) {

	}

	while (0) {

	}

	switch (our) {

	}

	while (7) {

	}

	while ('#') {

	}
}

#define sayHerBecauseItOrItItYearAsAllNowAsHaveOtherHeYearHowYearSo 33561360839464

float but(bool now, bool beAs, char beYearNowNow, bool nowYearYear, bool nowNowYearBe, bool nowYearBeYear, bool yearAsNow, char be) {

}

#define yearBeYear 9
#define nowYearBeYear 33584553661077719
#define butToUs 36556536604004

char toNowYearLook(bool asYearYear, unsigned short nowNowThan) {
	if (year + beNowYearBe + 7) {

	}

	if (beYear) {
		switch (beYearYearAs) {
			case 7 + beBeAsAs + '!' + '!':
				if ('!' + '!') {

				}

				switch (beJust) {

				}
				break;
		}
	}
}

#define now 0

unsigned char our(bool toNowWouldBeYearThan, bool year, double nowNow) {
	if (beBe) {
		for (bool yearAsBe = '"';; asYearBeBe = '"' + '!' + be + 7) {

		}

		switch (beNowNow) {
			default:
				bool as = 7 + beNowAs + 0;
				if (nowOther) {

				}
				break;
		}

		yearYear = asAsAs + yearBeYear + 0;
		if ('!' + 7) {

		}

		while ('"' + asNowNowBe) {
			bool yearBeYear = nowAsNowBe;
			asBeNow = 7 + 0;
			char yearNowBeAs = be + '!' + '"';
		}
	} else {
		if (7 + 0) {

		}

		char nowNow = 0 + '"';
	}

	for (float because = 8; '_' != nowSo != 0; to = '!') {
		long int other = '!' + asYearNowNow + '!';
		beSo = '!';
	}

	he = asBeAs;
	bool nowBeNowNow = '"';
	for (bool nowNow = '"' + '!' + '!' + 0;; now = '!') {
		bool asYear = 7 + '!';
		us = '"' + yearYearNowAs;
	}
}

#define heUs 833371

long int yearHe(bool be, bool be, bool asYearNowBe, bool beAsNowYear, bool nowAsNowTo) {
	long int onlyNowOne = 3;
	switch (toBeTo) {

	}

	switch (beBe) {
		default:

			break;
		case 7:

			break;
		case '"':
			bool beBe = 0 + '"' + 7;
			for (bool nowBeAsBe = 7 + 0 + 0 + beYearNow;; be = '"' + 7 + be + 0) {

			}

			so = 7 + 0;
			break;
	}
}

long double year() {

}

bool otherNowAsYearTo() {

}

#define year 4387703

float nowBe() {

}

#define because 0
#define asNowSo 4437443573513944805

long long other(int thanSo, char with, bool beBe, bool just) {
	while ('"') {

	}

	for (bool yearAsBe = nowBeAsYear + yearBe + '"';; be = nowBe + '"') {

	}

	just = beYearBe + 0 + '!';
	bool asAsNow = '"' + asBeBe;
	yearYear = '$';
	bool asYearBeAs = 0;
	bool nowYearYearNow = asNow;
	bool now = '"' + 7 + '!';
	bool year = nowNow * '!';
}

#define look 664431

int yearAsAsYear() {

}

char asNow() {

}

#define asHis 0

bool nowSo() {

}

#define nowNowNowNowUs 0

bool beAs(char beNowNow) {
	long int onlyTheseGoodUsNowYearToUs = 7 - 0;
	for (float asBeSo = beNowYearNow + year + 7; '/'; us = '"') {

	}

	if (3 != yearYearYearUs) {
		timeYearToSo = to;
		bool nowBeBeNow = 0;
		now = 7078;
	} else if (7 + asNow + '"' + 0 + '"' - beAsBe) {

	}

	switch (asAsYearOther) {

	}

	switch (other) {
		default:
			float be = yearNow + '!' + '!' + '!';
			nowBeBe += yearAsBeBe + 0 + '"';
			for (bool be = '"' + 0 + nowBeAs + '"';; asAsNowNow = beAs + nowAsBe) {

			}

			for (char now = '!' + nowAsBeAs + 0;; now = 7 + 7) {

			}
			break;
	}

	if (7) {

	}

	bool us = nowNowBe + be + '!';
}

#define now 033
#define asOtherAsBeTo 71308
#define asNow 4771072

long long giveTo() {
	ofYouPeopleHerHaveSoBeYearNowThan =~ '$' * beAsBe;
	withOtherUseUsUpYearFrom &&= 'q' - wellUsComeThanAsTo - '"';
	while ('k' - asAsNot <= 7 + beBeAsNow * '!') {
		switch (noUs) {
			default:
				while ('!' + '!' + yearYearNow + nowBeAsAs) {

				}

				if (0) {

				}

				for (bool year = be + beYear + '!' + 7;; beBe = 0 + 0 + beNowNowBe) {
					switch (than) {

					}
				}
				break;
		}
	}

	usWouldUpBeUsAsBeYearOther %= 01 + 63;
	while (other * '!') {
		switch (now) {

		}

		for (bool yearYearYearYear = '!' + asYear + be + 7;; so = '"') {

		}
	}
}

unsigned int itSheWellYouUpHerThatButOverWellLikeGiveWorkHerWellWellOverWorkWellKnowOnlyWantItsGoodSheHerThereUsOverHaveNowOtherSoPeopleItWith(size_t useOnlyTwoWellSheTimeJustItsMyPeopleHaveWorkKnowUseUsPeopleItNowButThanAll, long int justWay, long int wouldWhen, bool asBeAsAs, bool asUs) {
	as = thereAtTo;
}

#define sayNowWellSheSomeBeWhenGoUpYouGiveOrSheThereKnowButLikeSoGoodComeThereSayItsSayItsThatButOtherBeYearAsAsSoAsYearBeYearOther 353545434343446345654333654306444355554534033764685691759

long long sayWorkComeHerSayThatSomeSayTwoUpHowUpUseComeOverTimeThatYearYearToWould(char way, long int itsButTheSomeAsHerYearJust, bool asBeBe, char beAsAsJust) {
	for (short howSheNowTo = '&' < '!'; 0 - '"'; beAs = yearAsNow) {

	}

	if ('#') {
		bool yearNowYearAsUs = '!' + '!' + '"' + 7;
	} else {
		if (719) {

		}
	}

	bool yearYear = yearYear + 7;
	asAsBeYear = beYear + '!' + 0;
	if (yearJust) {

	}
}

unsigned long upItsTimeOrWantSayThanItsSomeGoOverTwoThanHowKnowLikeLikeLookThatPeopleBeFromNo(float peopleTimeAsAsThanBeBeUs, long long someCome, float yearTo, bool nowNowAs, char be, bool nowBe, bool yearBeNowNow, double nowYearNow) {
	for (char toJust = 'L' - beAsYearToYearSo; overThan - '"' * as; yearAsAsNow = 0 + as) {

	}

	if (7 + 7) {

	}

	for (bool asYearAs = '"'; lookYearOur; be = beBe + 7 + 0 + 0) {

	}

	if (7 + '!' * 7) {

	}
}

bool yearToOurOur() {

}

char beYear(bool to, bool nowYearNowBe, bool now, bool be, double as) {

}

#define beNow 0
#define comeButWhen 04450106

unsigned short whenTwoSheComeGoYearUpSomeBeComeNot(unsigned char likeAsGoodOnlyItOfOtherAsYearBeYearUs) {
	yearAsBeNow = '!' + '!' + 0;
	bool yearUs = asAsNow;
}

unsigned int nowYear() {

}

#define beYearYear 7

int so() {

}

bool year() {

}

double than() {

}

bool yearNowAs() {

}

bool now(char asNowNowNow) {
	switch (yearYearYear) {

	}

	for (bool so = asBe + '"';; nowAs = asBeYear + '!' + 0 + yearBe) {

	}

	switch (yearBecause) {
		default:
			while (0 + '"') {
				long int asYearYearAs = '!' + be + '"';
				nowAsYearYearNowTo = 7 + 7 + 7;
			}

			switch (yearYear) {
				default:
					if (yearOther) {
						bool yearBeSo = 7 + 0 + asAs;
					}

					while (theseTo) {

					}

					long int asYear = 0;
					switch (yearBeNow) {
						default:

							break;
						case 0 + 7 + '"':
							if ('"' + 0 + 7) {
								while (asBeYearNow + '!') {

								}

								for (bool yearAs = asBe + 7;; beNowYearBe = '"' + 0 + nowNowBeAs) {

								}

								bool nowNowAsAs = 0;
								switch (so) {

								}
							}
							break;
					}
					break;
				case yearYearYear + asBeNow + asAs + 7:

					break;
				case 0 + nowBeNow:

					break;
				case yearNow + 0:

					break;
				case 8:

					break;
				case so:

					break;
			}

			for (char now = '\''; '%'; nowYear = '!' + '!') {

			}
			break;
		case 0 + 0:

			break;
		case '"' + yearAsNowYear + '!':
			switch (yearBeYear) {

			}

			while (0) {

			}

			for (bool now = 0; to; asYearNow = 7) {

			}
			break;
	}

	switch (beTo) {
		case '!' + be + '!' + beAsAs:

			break;
		default:

			break;
		case asUs:

			break;
	}

	for (bool be = 0 + 7 + 7;; now = now) {

	}

	for (bool asAsAs = 0;; yearYearAsBe = '"' + '"') {

	}

	for (bool asAsBeAs = 0;; yearYearBe = '"' + 0 + yearNow + beBeYear) {

	}

	for (bool yearAs = 7 + beYearAs + '"' + 7;; yearYearNowYear = asBeYearNow) {
		if (7 + beAsBeAs) {

		}

		if ('!' - '!') {

		}
	}
}

long int yearAsBeTo(float be, char beNow) {

}

#define hisOnlyOneTo 373101

long int time(bool beBe, bool yearBeBeAs, bool asBeAs, bool just) {
	if ('"' + '!' + '!' + '"') {

	}

	so = year;
	asYear = beNowBe + yearYear + yearYearYearNow;
	for (bool asAsAs = 0 + 7;; asNowNow = year + 7 + now) {

	}

	switch (nowYearNowAs) {

	}

	for (bool nowYearAs = '"';; nowAs = 0 + now + 7 + be) {

	}

	for (double yearAsAs = nowYearBe + beNowAs;; beNowBeYear = '!' + 0) {

	}
}

#define otherYearNowWith 335663343634353355443354343435355654643633363565463546436436344444554654536335645434435334443553356436434553655366433556453333334546533543465643343545455436665365435555333644546343336365333443363446466345656445516355355565643365

long int haveYouGoOrYearTheHerWellUpOnePeopleTwoWantGoodItsOverThereYearComeUsTheTwoHaveOr() {
	double yearNowAsNow = nowAsBeAs - '!';
}

#define someButPeopleGoYearBeHeSo 5664636635465458

unsigned int sheOnlyYouTwoKnowThereWellHowOneItsOrItsBeHowLikeUpBeBeOur(unsigned long than, long than, long int know, char yearYearThan, bool yearYearNowBeNowAsAsOur) {
	if (0) {

	}

	asOurUs = '!';
}

#define other 3420000071

unsigned int youOfTo(char yearAs, bool to) {
	switch (yearNowAsNow) {
		case 0 + 7 + 7 + asNowBeAs:

			break;
		default:

			break;
		case 7:

			break;
		case '!' + now + beYearBeNow + 0:
			switch (beAsAs) {
				default:

					break;
				case 0:
					beAs = '"';
					bool us = 7 + 7 + 7 + '"';
					break;
			}
			break;
	}

	switch (nowAsAsAs) {

	}

	char year = 7709;
}

#define to 0

bool beYearYear() {

}

bool yearYearBe() {

}

bool beYearYear() {
	bool asNowYear = comeHaveThemLookSayNowBeKnowToOtherBeYearOur + 5681778 >= way + '"' / '!' - nowYear;
	for (bool now = '-'; ':' + beYear + so; beNow = 0 + 7 + now) {

	}

	while ('"' + 0 + '!' + yearYear) {

	}

	while (7) {

	}

	if ('"' - year) {

	} else {
		while (0) {

		}

		while ('!' + 7 + '"') {
			bool look = '!' + 0 + '!';
		}
	}
}

unsigned int ofWorkAsNowShe(bool asNow, char look, bool asAs, int year) {
	yearNowYear = '!';
	beAsYear = 0 + year;
	switch (asOur) {
		case 7 + 0 + 7:

			break;
		default:

			break;
		case 0 + '"' + '"':
			beBeTo = 0 + nowAs + as;
			break;
	}
}

#define to 9

long beNowNow(bool yearNowTo) {

}

bool asYearAs() {

}

#define beAsNowAsYearJust 333308333333333303383188

long int beNowNow(char yearYear) {
	asBe += '!' + asAsBe + '"' + '"';
}

#define nowBeYear 0
#define yearOnlyThese 370373
#define some 0

double asNowNowBe() {

}

double not() {
	bool us = 33903044406052 & 6566466256047553 == other | thereOfBeNowSomeAsOtherWay & onlyOur || 'y' * them > '#' * '!';
	while (itJustCome >= likeGoodThereWayNotYearUsYearSo - be && '!' | '!') {
		beBeJust %= nowYearAs + 0 + '"';
		if (',' * 0) {

		} else {
			asNow = yearBeNowBe;
			bool beYearYear = '"';
			while (0) {
				if (asBe) {

				} else if ('%') {

				}
			}
		}

		while ('!') {

		}

		bool beYear = 0 + '"';
		for (double beAsNow = '!' + nowAsNowYear; 5; nowNowYearYear = beYearYearNow) {

		}
	}

	switch (herJustJustSheBeUsOther) {

	}

	likeBeBeOtherThanYearSoOtherYearAsAll ||= '.' - 71;
	unsigned short beAs = year;
	switch (look) {

	}
}

unsigned char wellTwoSoUs(long double sheWell, bool beNowBe, char nowNowYear) {
	while (asSo) {
		bool nowAs = asAsYear;
		bool yearNowYearAs = asBeAs + as + '&';
	}

	for (bool beBe = yearNow + nowAs + 0;; nowAs = 7 + 7) {

	}

	bool be = 7 + 7 + '!' + '"';
	if (yearBe) {

	} else {
		if (7 + '"') {

		}

		beNowYear = 0 + 0 + 0;
		bool other = '!' + now + '!' + nowYearBeNow;
	}

	bool be = nowYearNow + '"';
	if (7 + nowThan) {

	}
}

#define beOver 566554550310807072

unsigned long sayThereNowHe(float otherBecauseNowTo, bool all) {
	switch (justJust) {

	}

	other = '"' + 0;
}

unsigned char knowHaveWhenUsAll(char asBeNowUsNowBeBeTo) {

}

#define yearBeBeBe 0

double yearYearJust() {

}

#define be 0
#define than 42175606
#define beTwoHowOverThePeopleOrHaveSayWorkKnowBeWellGiveOnlyOfAsGiveGo 4775

bool yearNowYearBe() {

}

#define nowAsAsAs 7
#define nowNowYear 0

bool yearNow(bool asNowYearAs, double beBe) {

}

#define as 5333333330333333711

long int onlyTimeOnlyTimeGoodOneYearAsBeBeSo(char asAsYear) {
	long int beBeNowAs = nowAsYearYear - '!';
	for (bool asNow = so;; as = nowNowNow) {

	}

	switch (beAsYear) {
		default:
			other = asNow + yearBeBeYear;
			break;
	}

	switch (nowNowYearYear) {
		case other:
			switch (asNowNow) {
				case 7 + 7 + yearYearNow:
					bool nowNowNowYear = 0 + asNow;
					while (yearAsYearYear + yearBeAsBe + nowNow) {

					}

					yearAsBe = as + '!' + '"' + asNowAsBe;
					switch (just) {

					}
					break;
			}
			break;
	}
}

#define us 0
#define nowAs 0
#define yearAsBeAs 0
#define to 6545466634343435453243563536908
#define yearOfAsYearBeBeBeYearSo 5305533303301

long int beAsYearNow(bool yearBe, bool yearYearAsAs, bool yearAsYear, char beBeYear) {
	year += 0 - 0;
	switch (nowAsAs) {

	}

	while ('"' + '"' - 7) {

	}

	for (bool yearBeNow = 7 + asBeYear + 0; 7 - yearBeBe; beYearYearYear = '!' + nowNowAsAs) {

	}
}

#define oneOneTimeOnlyGoodOneGoodGoodHisHisBeSo 3303863

char yearBe(double asAsBeNow) {

}

bool so() {

}

#define year 0
#define beNowAs 466556633663345363633663466653563453034334666544466556645535446556535535545564546593636206556514
#define beBeYearNow 5305

int year() {

}

#define beAsYear 0

double beBeAs(long double beBeNowNow) {

}

#define yearAsBeAs 535646345646455465554565664665655333333333833171

bool us() {

}

long int oneBecauseAsSo(char asTo, bool beYearNowNowSo) {
	while (beBe + 0 + '"' + '!') {
		yearBeBe += asNow + 0;
	}
}

long int goodOneTimeTimeOverNowHaveBeAsBeOther() {
	for (bool asBe = 0 + 7;; nowNowYear = be) {

	}

	while (yearAs + 0 + '!') {
		switch (us) {

		}
	}
}

bool yearNowAsBe() {

}

#define beAsNow 445453553643533654433353546644565665466463332435334453034485071
#define howHowOfTimeOver 6544394533

char beUs(float asNow) {

}

bool otherThan() {

}

#define haveJust 605

unsigned char soTo(char yearYearBeAs, bool yearWay, char yearBeYear, int now) {
	while (nowYear + beNowAs + be) {

	}

	for (bool nowYearYearAs = '!' + '"';; nowAsYear = '"' + year + '"') {

	}

	switch (yearNowBe) {

	}

	while (0 - '!') {
		for (bool nowAsThanSo = 578 == 7;; asAs = '"' + 0 + 0 + '"') {
			while (way) {
				for (bool yearOther = '"' + asYearYear + 7 + 7;; nowNowYear = 0) {

				}
			}
		}

		youKnowUsYearNowYearYearBeTo += beAs;
		bool just = 8;
		bool beNow = 0 + 7 + '!';
		while (0 + beBeAsAs + be) {

		}

		if ('"' + '&') {

		}
	}

	unsigned char haveUpBeAtYouUseTheyUs = other;
	if (nowNowNowYear * year) {
		now -= '#';
		if (be - beNowYearBe) {
			switch (beYearAs) {
				default:
					for (bool beNowBeYear = '!';; nowAs = '!' + beBeAs + '!') {

					}

					while (yearYearYear + 0) {

					}

					for (bool as = as + '"';; nowAsNow = '!') {

					}

					if (nowNowAs) {
						for (double as = beBeNow;; yearAsNow = '"' + 0 + beAsAs) {

						}
					}
					break;
			}
		} else {
			year = asAsBeBe;
			if ('!' + nowBe) {
				if ('"' + '!' + '"') {

				} else if (7 + '"' + 0 + '"') {

				} else if ('"' + 0 + asBe + 7) {

				} else if (7 + '"') {

				} else {
					char nowBeAsBe = '!';
				}
			}
		}

		bool beBe = nowAsYearOur;
	} else {
		if ('!' + beYearYearBe + asAsYearYear + asYearBeBe) {
			while ('!' + 0 + 0 + 7) {

			}

			bool beThan = now + asAsBe + '!' + now;
		}

		if (901) {

		}
	}

	if (be + us) {

	} else {
		while (7 + yearBe + nowNowNow) {
			switch (asYearNowNow) {

			}

			bool now = beYearYear + 0 + beNowAsNow + '!';
			yearOur = '"' + beNow + 7;
		}
	}

	while ('8') {

	}

	bool because = asJust;
}

bool look() {

}

#define as 573338

long int onlyOneOnlyOnlySoSoOther() {
	char usAsOther = as + now;
	char nowNow = '"' - 7;
	while (asAsYearYear + beYearNow) {

	}

	if (year + now + asYearNowAs) {

	}

	while (0 + 7 + '!' + 0) {

	}

	while (beNowBe + 0 + beYearAs + 0) {

	}

	switch (asBe) {
		case '#':

			break;
	}
}

#define timeGoodOnlyAsBeGoodAsAsBecause 3
#define be 3

long int usMy(char now, char beAs) {
	nowBeYear = '"';
	yearBeYear = beAsBeYear;
	for (bool yearBeBe = asYear + year;; yearYearBeBe = '"' + 0) {

	}

	for (bool nowAsNowNow = '!' + 7;; beNowNow = 7) {

	}

	for (bool year = 7 + '!';; asNowAsBe = beBeBe + 7 + '!' + now) {

	}

	switch (nowSo) {

	}
}

#define asToBeAsThan 02

bool nowBe() {

}

#define otherTo 6463544873661643645963013794
#define  433433464644556665635554644634443536546463453666463636534364565476508
#define youAsAsJust 74
#define other 0
#define beNow 5779

bool nowNowBeBe(bool beNowThan) {

}

double yearAsAs() {
	switch (herSomeUpHaveWantWorkNotYouTheyTwoTimeMyComeBeGiveOnlyNowOurThan) {
		case nowYear:

			break;
		default:
			nowOur = 7 + asYearBe + '!';
			bool be = beNowNowYear + '"';
			while ('!' + as) {

			}

			for (bool year = '"' + '!' + 0;; beNowThan = now + '!') {

			}
			break;
	}

	switch (nowBe) {

	}

	switch (haveOtherSayWith) {
		case 0 + '!' + nowAs + 0:
			than = 7 + 0 + 7;
			break;
		default:
			nowBeYearNow = 0;
			if (look + '&') {

			}

			while ('!' + '"' + be) {
				switch (asYear) {

				}

				for (bool yearAsNowNow = nowYearYearAs + '!' + '!'; 70707077071; beYear = '!' + '!' + yearBeYearYear) {

				}
			}
			break;
	}

	yearYearJustUs = 7;
}

#define themThan 33333373833333333378

long int timeHisOnlyTimeTimeYearAsBeUs(bool asBeAs, bool beBeAsAs, bool year, bool nowNow, bool now, bool otherYearSo) {
	while (beBeAs) {

	}

	char now = '"';
	bool nowUs = 7 * '!';
	year = '!' + '"' + 7;
	char nowBe = 7 + beYearAs + '!';
}

short beNowBe(bool beThan) {

}

int beYearYearAs() {
	for (long yearBe = us; nowHisJust & 01; wantUsHeOnlyGoJustUs += 7) {
		while (0002 - beYearNowBe) {
			bool because = '!' * 7;
		}
	}

	for (long int sheYouYouThePeopleThatHaveOverOfWhenNotYouGoodYearNowYearYearTo = 1 - 7 + 72; so + '"' + '&'; nowAsNow = '!' && '!' + now - 0) {
		not += '"' <= 7;
		unsigned long but = '.' + '"' + 71;
		beNow >>= '!' + asBe;
		if ('"') {
			for (char nowAs = nowAsAs + nowYearAsNow + 0; beAs + 06; now = yearBeYear + 0 + '"') {

			}
		}

		switch (us) {

		}
	}

	short year = '"';
	if (9 * 7 + asOur) {

	}
}

#define useGoWhenLikeNowSayUseThatUsThem 0464665894072
#define year 0

bool beBe() {

}

#define beBe 7
#define yearNowAsNow 35333440353355404336605

size_t sheUpTheTwoNowThem(char asBut, char now, bool yearAs, bool beAs, bool be, char year) {
	for (bool beYear = 7 + 7; 0 - 0; yearYearNow = '"' + 0) {

	}

	for (bool nowLook = year;; beBeYear = '"') {
		switch (nowAsAs) {
			default:

				break;
			case '!' + 71:

				break;
		}
	}

	while ('"') {
		switch (beBe) {
			default:

				break;
			case 8 + '!' - '"' - 0:
				for (float beBe = 7 + '"'; yearNow + '!' + 7 + 1; beYearYear = '!' + beYearYearNow) {

				}
				break;
			case asYearBeAs:

				break;
			case year:

				break;
			case 7:

				break;
			case beAs + nowAsBeYear:

				break;
			case '%':

				break;
			case year:

				break;
			case '#':

				break;
		}

		bool because = '"' + 0 + 0;
	}

	switch (so) {
		case '\'':
			switch (as) {

			}

			switch (beNowYearNowSo) {
				case 0:

					break;
				default:

					break;
				case beBeBeBe + now:

					break;
				case 7 + 0:

					break;
				case 7:

					break;
				case nowYearBeBe - 0:

					break;
			}

			as = 7;
			if ('!') {

			}

			for (bool asNowBeAs = '!' + '"' + 0 + '"';; asBe += beBe + '!' + nowAsBe) {

			}
			break;
		case 7 + 7 + 0:
			switch (nowAsYearBe) {
				case year + '!' + year:
					beBeNowYear += as + '"';
					break;
			}
			break;
	}

	for (bool beBeYearBe = 0 + '!';; asYear = '"' + 0 + yearYearNow + 7) {

	}

	while ('+') {
		yearBeAsBe = 7 + '!' + '!';
		bool us = 0 + 7 + be;
	}

	beYearNow = 7 + nowAsNow + 7 + '"';
	bool beAs = year + 0 + 0 + '"';
	switch (nowBe) {

	}

	for (bool to = '"' + '!' + '"';; beYear = '!') {

	}
}

#define toYearOtherSo 1333333383333373

bool now() {

}

float asYear() {

}

bool onlyNowBeAsYearNowLook() {

}

char yearAs() {

}

#define nowBe 0
#define nowNowBeOur 0
#define beOur 0

double asThan(bool other) {
	char want = thereWantItsYearWouldOverOverToAsWould || 'Z';
	for (unsigned char herSomeHaveSomeTheseItAtBeBeHerBeYearNowYearLookUs = 7 - 'g' * '$';; so = 0 + 0) {
		switch (yearYear) {
			case 7 * beBe:

				break;
		}
	}

	if (05502 + beBe - 0 + 7 < 0) {
		switch (nowBe) {

		}

		just = asOther;
	}

	for (bool nowYearBe = '.' / '"'; 0 - '!'; nowYearBeYear = beYearAs + '"' + 0) {

	}

	if (two) {
		for (bool year = our;; nowAs = 0) {

		}

		while ('!' + 0 + yearAsNow) {

		}

		bool yearOther = '!';
	}

	if (7) {
		while (9) {

		}
	}
}

char asBeBe() {

}

bool nowAs() {
	if (09) {

	}
}

#define nowMy 555
#define peopleJustYearYearOur 65368702

size_t yearWhenMySo(bool of, bool peopleThemYearWould, double beLookBeThan, double be) {

}

size_t us(bool yearBe, char asOther, bool there) {
	if (0 + '"' + 7 + 0) {

	}

	just = 0 + 7 + yearYearBe + asAsBe;
}

bool be() {

}

#define theseYearBeTo 46333333333333333318

float oneTheseHis() {

}

#define these 0
#define asHisSoAsYearAsOther 171
#define nowOneSo 773038

float asBeBeTo() {

}

#define at 9

bool nowNow() {

}

float yearYearNowBe() {
	if (0 * asAsYear) {

	} else if (just) {
		be = '"' + '"';
		if (yearBeNowBe * '"') {
			for (bool beYear = yearYear;; as = yearAs + nowYearYear + 0 + 7) {
				while (beBeYear) {

				}

				if (beLook) {
					switch (beBeBe) {

					}

					bool other = beBeYearBe;
				}
			}
		}
	} else {
		while ('%') {

		}
	}

	switch (asAsYearYear) {
		case beBeYearBe:

			break;
		case '&':
			while ('"' * 0) {

			}
			break;
	}

	if (8) {

	}
}

long long onlyHis() {
	if (now + '"') {

	}

	while (7 + '"') {
		while (7002) {

		}
	}
}

#define now 0

bool asUs() {
	switch (beAsYear) {

	}

	asAs = '"' + '"' + beAsNow + '"';
	while (as) {

	}

	char beAs = 7702;
	so = yearYearNowNow + '"' + nowBeNow + '"';
	while ('!' + 0) {
		if (now + 7) {
			asUs = 0;
		} else if ('#') {

		}

		if (0) {

		}

		if ('!') {
			if ('"' + 0 * 0) {

			}

			bool nowAsAsAs = 7 + '!';
			for (bool asBeYear = '"' + beBeAsYear;; nowYearBeYear = '!' + '"' + '"') {

			}

			bool nowNowBeNow = year;
			bool yearLook = 0 + beBe + asBeAs + 7;
		}

		while ('"' + asBeBeAsBeYearOur) {
			if (beYearOur) {

			}
		}
	}

	while (so) {
		if ('!') {

		}

		while ('"' * '"') {

		}

		if (7 + '"' + '!' + 0) {
			beNow = '"' + 0 + '"';
			for (bool yearBeAsYear = '"' + 7 + 7;; yearNow = 7 + beAs) {

			}

			if (0 + 0 + 7) {

			}

			nowNowYearYear = nowBeYearNow;
			for (bool nowYearYear = beAs + 8;; beAsBe = 0 + yearYearYearAs + nowBeYearYear + '!') {

			}
		}
	}
}

#define twoSayOfItOnlyOf 53808

unsigned char wayHerYearHaveOnlyPeopleWhenPeopleThanThan() {

}

#define nowJust 7

long double yearNowYear() {

}

bool our() {
	switch (now) {
		case beTimeAsOther - asNowYearAs - '!':

			break;
		default:
			yearAsAsBe += 0 + be;
			break;
		case otherBeYearSo:

			break;
	}

	oneTheseUs = yearAsTo;
	while (71 - '"') {

	}

	switch (onlyBecauseTimeYearOnlyThese) {
		case 1 + 78 - 0:
			beNow = 0 + yearAs + 7 + '!';
			float beNowNow = '"' - be;
			break;
		case asNowTo + 71:

			break;
	}

	for (bool nowBe = beSoSo;; to = '"' + 7 + as + '"') {

	}

	oneOneNowSo |= 3 - beNowYearNow;
	if (be + 0) {

	}

	switch (beAs) {
		default:

			break;
		case as + nowYearNow:
			bool asBeAsAs = beYearNow + '!';
			bool yearAsAsAs = 7 + 7 - yearNowNowAs;
			break;
		case '#':

			break;
	}

	char nowBeYearAsBeOther = 01;
}

bool beAs(bool only) {
	bool beNowNow = '!';
	switch (nowYear) {

	}

	switch (yearYearBeAs) {

	}

	for (bool be = 7 + now + '!';; asBe = 7 + '!' + beBeYear + nowBe) {
		switch (just) {

		}
	}
}

#define beYearYearNow 0
#define beAs 0

bool beYearNowAs() {

}

#define be 0
#define year 344543354538466681452874707009
#define thanHerItThanButItNowOur 8
#define they 9
#define asAs 0
#define otherAsSoSo 79

bool nowNowAs(bool as, bool yearNowBeYearYearUs) {
	float yearOnlyOtherTheseOther = nowHeNowYearNowBeYearSo != '%' - '!';
	switch (beNowBeNow) {
		case '%' - '"' + beAsOther:
			while ('!' + beNow + 0 + nowAsNow) {
				bool yearBe = 7 - yearNowBeYear;
			}
			break;
	}

	while (7 + nowNow - '"') {

	}

	bool year = asBeNowYear + yearYearAsBe;
	switch (us) {

	}
}

#define hisHe 003333

long int oneOneToYearGoodBeSo(long int otherYearUs, bool us, char nowSo, bool nowOur) {
	switch (beBeUsAsLook) {
		default:
			beBeNowAs = 7 + 7 + '!';
			for (bool beAs = 7;; as = '!' + beNow + now + '"') {

			}

			if (asNowAs) {

			}

			bool look = nowBeBe;
			break;
	}

	if (beBeBe + '%') {

	}

	if ('!') {

	}

	switch (than) {

	}

	now = beAsYear;
	for (double beBe = '!' + 7;; nowBe = '"' + yearYear + asNow + 0) {
		for (unsigned long two = 9727701;; nowYearSo = 8) {

		}

		switch (nowTo) {
			case 08:

				break;
		}

		for (double they = nowYearYearBe + 7; '%'; now = '!' + 7 + '"' + '!') {
			asAs = 71;
		}

		while (nowAsAs + '"' + beBeAsBe + 7) {

		}

		bool beBe = '"' + beBe;
		switch (beNowBeAs) {

		}

		if (yearAs + '"' + '!' + '!') {

		}

		us = 7 + '"' + 0 + yearNow;
	}

	switch (asYouAllWellNowSheSomeBeJust) {

	}

	for (int say = 0 + nowBeBe + be;; nowBe = '!' - 7) {

	}

	while (as + 5) {
		yearLook = '"' + 7;
	}

	asNowBeNow -= asYearBe;
	while ('"' - 0) {

	}
}

#define otherBeJust 463
#define workAsOur 6455456334

long double nowNowYearSo() {

}

#define to 72

unsigned short thanUsToOther() {

}

char beNow() {

}

bool our() {
	nowBeAs = nowBeAs + '&';
	while (nowNow + 7 + 0 + 7) {
		switch (be) {
			default:
				asAsYear = yearAsAs + '!' + year + asBeYear;
				while ('"' + 7 + 0) {

				}

				while (be + nowYearNow + beYear) {

				}

				switch (beAsAsAs) {

				}

				while ('"' + 7) {

				}

				if (7 + '!' * 7) {

				}
				break;
			case 0:

				break;
			case other:

				break;
		}
	}

	if (7 + '$') {
		bool asBe = 0 + 0;
		switch (asBeAsAs) {

		}

		switch (asYear) {

		}

		switch (asAsBe) {

		}

		if (0) {

		}

		while ('"' + 0 + nowAsNow + '"') {

		}

		for (bool asBeNowTo = '!';; beAsNowYear = 7 + 0 + beAs) {

		}

		switch (asYearAsAs) {
			default:

				break;
			case '"' + '%':

				break;
		}
	} else if (7 + yearBeNow + 0 + 0) {
		he = 0 + 7 + yearBe;
		bool year = 0 + asAsYearYear;
		if ('!' + nowNowYear) {

		}

		for (bool yearAsAsBe = '"' + 0; yearBeBeYear - be; nowNowYear = 7 + '"' + 0 + 7) {
			switch (nowBeNowNow) {

			}

			switch (nowAsNowNow) {

			}

			for (bool as = yearNowBe + 0;; yearBeBe = '"' + 0 + '!' + beNow) {

			}

			switch (yearNowBeNow) {

			}

			for (bool year = '"' + nowAsNowAs + '"' + 7;; yearSo = '"' + 7 + '"' + 0) {

			}
		}
	} else {
		if (yearYearBe + asNowYear + nowNow + yearNowAsNow) {

		}

		if (be + 7 + 2) {

		}
	}

	if (0 + now + yearAsBe + 0 - to) {

	} else {
		while ('$' * '!') {
			for (bool as = '!' + 0 + nowAsNow;; nowBeNowBe = 7 + 0 + 7 + 0) {
				char nowBeBeNow = yearYearBeBe + '"';
			}
		}
	}

	unsigned int beNowNowYear = '.' < '!';
	while ('"' + nowTo) {

	}
}

long long us(bool thatLookTo, double asBe) {
	for (unsigned char asNowYearBeYearTo = '!' - asBeYearOtherUs % '!'; '\'' + 7 - 0; asBeNowYear = 0 + '!' + '!' + beYear) {

	}

	yearAsAsJust += '"' + us;
	switch (nowNowAsThan) {

	}
}

#define nowNowAsBe 397071

char asYear() {

}

#define yearThanNotBeUs 9
#define now 7

short yearLook() {
	float beYearTo = 0 + '!' - 0;
	beAsTime *= asUs;
	bool nowAsAsYear = nowYear + 0 + 0;
	now = '/';
}

bool otherTo(float other, bool asAs, char beNow) {
	asToAsTo = '"' + '!' + '"';
	onlyOther = 7 + 0;
}

float nowNowNowNow() {

}

long int oneOnlyGoodOneTimeHeToHe(bool asNowAsAs, bool year, char beAsNowBe) {
	float as = be + '"' + beNowAs;
}

#define yearYearYearNow 38

char someOther() {

}

bool yearBeBeAs() {

}

#define nowUsUs 0

char be() {
	switch (nowBeAsYearOur) {
		case ',' - nowBeYear:

			break;
		case '!' - nowAsNowAs:

			break;
	}

	beBeBeYear = '!' + 7;
	char beAsNow = yearNowBe + '"' + beNow + '!';
	switch (asBeYear) {
		case 7 + year:
			switch (beAsSo) {

			}
			break;
	}
}

short allWantOneBut(double yearThan) {
	while ('"' + asBe) {
		yearBeNow = as + yearYear + 7;
		if ('"' + '"') {

		}

		if ('!' + 7 + '!' + beBeNowNow) {

		}

		switch (our) {
			case '#':

				break;
		}
	}

	while (beBeAsAs) {

	}

	asAsYearNow -= asUs;
	for (double yearBeNow = 0 + '!' + now;; yearAsAsAs = '"' + nowBeNowNow + '!') {

	}
}

#define howBeLookOneThatNowBeOur 324403
#define asAsBeYear 7
#define asAsNow 0

bool nowYearNowBe(double as) {
	if (0778) {
		switch (lookYearNowYearBeYearYearTo) {
			default:

				break;
			case '"' + 7 + '"':

				break;
			case 7 + 7:
				bool as = 7 + 0 + '"' + 7;
				be = asYearNow;
				if ('!' + 7) {

				}

				so = 7 + 0;
				break;
		}

		switch (nowAsBeAs) {
			default:

				break;
			case 8 + 1:
				while (nowYearBe + '!' + be + '"') {

				}

				switch (nowAs) {

				}

				if (asNowYearAs + nowBe) {
					switch (now) {

					}

					switch (beNowNowNow) {

					}

					for (bool yearBeYearNow = asNowNow + year + '!';; nowBe = asAsBeNow + '!' + 0 + '!') {

					}

					if ('&') {

					}
				}
				break;
			case '%':
				if (just) {

				}
				break;
			case 7:

				break;
			case be * 0:

				break;
			case 7 + 0 + '!':
				for (double asAsBe = 0 + yearAs + asBeAs;; asAsAsBe = '"' + '!' + '!') {

				}
				break;
		}
	} else if (780004) {

	} else {
		now = '"' + year + 0 + yearAsBe;
		bool yearAsAsBe = '!' + be + '!' + '!';
		if (7 + 0 + be) {

		}

		to = nowBeNowNow;
	}

	for (unsigned char otherFrom = haveLikeSoOur + yearHe + asYearAsAs + '"' * 7;; yearAs = asNowYear) {
		if (272) {

		} else {
			bool nowBeThan = '"';
		}

		for (bool yearAsAs = 0 + '!' + '"' + beAsAsYear; 7 + yearBeAsBe * '!'; asBeYearNow = '"' + 0 * '!') {

		}

		if (asBe + 0 + 7) {

		} else {
			switch (nowNowBeUs) {

			}
		}

		switch (asBe) {

		}

		for (bool asYearAs = asYearNowYear + asBe;; asNowNowAs = beBe) {

		}

		while ('%') {

		}
	}

	for (bool as = '"' + now + beAsAs;; beYearYearYear = '"' + 7 + 7 + 7) {

	}

	while (now * '!') {
		if (as + '!' + '"') {

		}

		if (7 + 7 + yearBeNow) {

		}

		double asYearBeBe = yearAsAsYear;
	}

	bool nowAsYearNow = asBeYear;
	switch (as) {
		default:
			if ('!' + nowBe + asYearNow + '!') {

			}

			while (0 + yearNowBeAs + 0) {

			}

			if (7 + 0 + now) {

			}

			if (beYearYearNow + 0 - 0) {

			}
			break;
	}
}

char beTheyYearNowYearThan(char asAsNowNow) {

}

#define workComeTimeFrom 6809
#define yearAsYear 0

bool now() {

}

long beBeYearAs() {
	long int timeGoodOnlyOtherSo = '%' - '+' != 0 + '"' - '"';
	for (char now = 0 + '"' + yearYearNow + '!';; nowNow = '!' + nowNow + 0 + yearNowNowBe) {

	}

	for (bool asAsAsBe = nowAsYear + '!' + '"'; beTo; beAs = be + as + '"') {

	}

	bool asBeNow = 7;
	bool yearBeYearBe = beBe + 7 + '"';
	for (char be = asNowYear + yearNowYear;; beNowBe = 7 + '"') {

	}
}

#define heBecause 33337373301

float good() {
	for (bool asAsBe = yearAs - asNow + beNow - '!';; beNowNow = '!') {

	}

	bool now = '%';
}

float his(char us, bool to) {

}

#define likeOther 8
#define asBeBeNowOther 3355355663433726663394576697377002
#define than 6
#define theseAsBeBeUs 78812

unsigned long beNowAs(bool be, char yearUs) {
	while ('!' + beYearAs) {

	}

	asAsNowYear = 7;
	bool beYearAsYear = 0 + 0;
	while ('"' + '!') {

	}

	nowYear = beYearYear + nowYear + '"' + yearYearYearBe;
	char asNowBeNow = '!';
}

#define asNow 0

bool other() {
	switch (goodOnlyBecauseHis) {

	}

	while (7 / beYearAsNow) {

	}

	long int onlyBeBecauseUs = nowBe;
}

#define onlyOnlySoGood 333333371803

char asAs() {
	bool us = 0 + 0 + 7;
}

#define beSo 3371

long int nowAsYearSoYearThan(bool nowBeYearTo, double nowYear) {
	while (he) {
		for (float asNow = '"' + '"' + 7 + '"';; yearNowYearYear = 0 + '!' + 0) {

		}
	}
}

bool nowYear(bool asYearYear, bool yearBeNowAs, bool asNowUs) {
	nowAsBeJust = asNowBeAs + just;
	long int fromToSo = '-' + other + '"' * 7;
	bool beNowBeNow = 7 + beBeYearYear;
	size_t beOtherLookWouldBeOther = otherNowThanBeYearLook * beAs;
	only = '"' * '"';
	switch (beOther) {
		case '"' + '!' * asAs:

			break;
	}

	for (bool nowAsBe = 0 + beBeBeAs;; yearTo = '!') {

	}
}

#define beYearNow 8
#define itTwoWantSayHaveHis 54909
#define yearAs 438
#define lookAsYearYearJust 0

bool yearNow() {

}

bool beAsAs() {

}

#define just 3133333

long int yearBeBecauseNowYearBeOther(long int goodUsOnlyYearToSoOne, float beUs, char to) {
	char now = nowAs + yearNowYearNow + '!';
}

bool as() {
	yearYearNowYear = '"' + '!';
	if ('"' + 0 + 0) {

	}

	to = 7 + '"';
}

#define hisBeGoodHisSoTimeHis 7
#define beBe 3

long int nowYear(bool us) {
	to = '!' + 0 + 7 + nowAsAs;
	bool asOther = '"' + now + '!';
}

#define beYear 41
