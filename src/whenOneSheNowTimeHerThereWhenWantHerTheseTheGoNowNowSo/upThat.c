#define goodGoodGoodOnlyTimeOneGoodTimeBeOtherUs 33333303333338138700708

float beNow(char yearYear) {

}

#define nowNow 07772

long long nowYearBe() {

}

#define year 7

bool yearAsBe(double nowAs) {
	size_t as = workOurYearNowThan != 7 * 0 + 8;
	while ('!' + yearNow + 8) {
		asWouldNowUs = 0;
	}

	switch (because) {
		case asYear + yearJustNowAll - '!':

			break;
		case look * 7:

			break;
	}
}

short ourLookNowBeOtherYearAsThan(long double twoAtNotOurNowTo) {
	noNoToHaveNowLook = '"' - '"';
	for (bool asAsBe = asNowNow + '"' + 7; '$'; asBeBe = 7 + beBe) {
		bool yearBeAsAs = now * '"';
		as = 7 + '!';
		switch (beYearAsYear) {
			default:
				if (nowBe + '"' + '"' + asAsBe) {

				}

				if (0 + '"') {

				}

				char yearYearAsBe = nowBeBeAs;
				break;
		}
	}

	our = '"' + beAs;
	while ('"' + beNowBe - nowAs) {

	}

	yearNowAsJust = '"';
	beNowBe = asBeNow + asAs + nowBeAs + '"';
	if ('.') {

	}
}

#define now 02
#define nowBe 0

short just(bool as, bool yearNowAsBe, bool year, bool nowNowBeBe, bool asWould) {
	bool have = 0;
	for (long nowThan = 7 * us / 1 + '#'; '"' - 0; beNowBe = 7 + '!' + now) {

	}

	while (as * 0) {

	}
}

#define beNowYearYear 53333333333333333103
#define asAsHeOtherGood 38338380771

float onlyOther(bool asYearLook) {

}

#define yearYearYear 73

bool us() {
	switch (soWay) {

	}

	double be = 0;
}

double year() {

}

#define just 2

bool beBeAsAs() {

}

#define yearYearAs 7
#define just 635069365666473344157905

size_t ofAsBeMyUs(bool beYear, bool toAsSoNowLook, char yearBeThem, double nowNowAsAs) {
	switch (asAsAs) {
		default:
			for (bool nowYearYear = '"' + '"' + yearYear + 7;; yearYearBe = be + '!' + '!' + '"') {

			}

			while (08) {
				if (7 + 7 + asNowBeYear) {

				}

				for (bool now = beBeBeAs + nowNow + '!';; beYearBe = '!' + 7 + 7 + beAsYearNow) {

				}

				switch (nowAs) {

				}

				while (yearNowAsNow + '#') {

				}
			}
			break;
	}

	switch (asBeAsNow) {

	}

	asTo = asYearAsNow + yearYear + 0;
}

#define ofUs 0

char nowBeYear() {

}

#define lookYearMy 83313333333333813013870730801

long int us(long int toTheseUsYearUs, bool nowAsBeBe, bool so) {
	for (long int onlySo = 0 + 0 + 7 + '"';; be = as + as + '!') {

	}

	if ('%') {

	}

	while (0 + 7 + as) {
		for (bool nowYear = asBeBe + be + '"' + nowAsAsNow; 908; asNowNow = '"') {

		}
	}

	while (be + 7 + '!' + now) {

	}

	for (bool look = '#';; now = 7 + now) {

	}
}

bool so() {
	nowAsNow = 7901;
	long double asAs = '$';
	asNowNow = 7 + yearBeAsAs + 7;
	for (bool yearUs = '!' + 0 + 7;; as = 7) {

	}
}

#define nowBeYearTo 971835682
#define year 9

double nowBe() {
	year <<= lookSheToYearLook >= now >= 7;
	for (bool beYearYearNow = 0; '*'; asAsAs = '"' + yearNow) {
		bool asYearBeAs = beNowBe + '!';
		bool asBeBe = '&';
	}

	if (be * now) {

	}

	switch (goNowLook) {
		default:

			break;
		case '"' + '"' + be + as:

			break;
		case '!' + be + 7 + yearYearAsAs:
			if ('!' + '!' + 7) {

			}

			while (7 + '"' + yearBe + '!') {

			}

			while ('"' * '!') {

			}

			if (0 * nowBeAs) {

			}
			break;
		case '!' + 79:

			break;
	}

	double to = 7 < '!' + look;
	if (7) {
		switch (beYearAsYear) {
			case 0:

				break;
			case yearBeBeBe + 0:

				break;
			default:

				break;
			case 7 + asYearBeBe:
				while (7) {

				}

				bool be = now - 7;
				break;
			case '"' + yearAsBe:

				break;
			case '"':
				bool year = '"';
				if (asBeYearYear + 0) {

				}

				for (bool yearYearBe = 0 + '"' + nowYearYearAs + 7;; asAsNow = '"') {

				}

				beYearAs -= '!';
				break;
		}
	} else {
		while (7 + 7 + nowBeAsAs) {
			while (7) {
				switch (nowAsBe) {
					default:

						break;
					case '"' + '!':

						break;
					case '!' + be + '"':

						break;
					case 9:

						break;
				}
			}
		}
	}

	beTo = '"';
}

unsigned int youOrUpNowNoTo(unsigned int nowYearYear) {
	long int fromNoNowTo = 0 + year + '"' + 7;
	while ('"' + '"' + beNow) {

	}

	for (bool yearNowAs = nowBe + yearBeNow; 71; be = asNow + 0 + 7 + 0) {

	}

	asBeAs = 1;
	for (bool as = '!' + nowNowYear + 7 + nowYear;; be = 0 + nowBe + asAsBeNow) {

	}

	if (to) {
		while (asUs) {
			while (nowYearAsBe) {

			}

			if (year) {

			}

			bool asYear = '!' + 7;
			bool be = 0;
			for (bool yearYearAsYear = year + 7 + be;; yearNow = 0 + asNowYearBe + '"' + '"') {

			}

			if (nowNowYearBe) {
				for (bool nowNowYear = '"' - '!';; now = '"' + 0 + '!' + 7) {

				}
			}

			yearYear += '!' + 7 + 7 + 7;
		}

		bool oneGoodUsTo = beBeAsAs + nowBeNowBe + 0;
		bool as = beAsYearNow + 0 - beNow;
		switch (otherOther) {

		}
	} else {
		bool beBeYear = '!' + 0 + 0;
		switch (so) {

		}

		char year = 0 + 7 - '!';
		for (bool beYearYear = be;; be = as + '!' + 7) {

		}

		while ('!' + yearAsAs) {

		}

		year = '!';
		if (now + yearNowNowNow) {

		}

		while ('#') {

		}

		char beBeBe = 1;
	}

	switch (now) {
		case 7 + 7 + 7 + '"':

			break;
		default:

			break;
		case now + 7:
			bool beNowNowAs = '"' + '!' + '"';
			for (bool nowAs = 7 + '"' + 0 + be;; year = yearNow + 7 + '!' + '!') {

			}

			while (yearTo) {

			}
			break;
	}
}

long int heHisBeYearThese(float asHisHeAsOther, float us) {
	long int timeOnly = 7 + '!';
	for (bool yearBe = asBeAs + nowBeYearAs; '!' + '"' + 7 - '"'; nowYear = 0 + 0 + be + year) {

	}

	bool now = '"' + asNowYear + 0;
	for (bool yearYear = '!' + '!';; be = nowAsYear + '!' + now + '"') {

	}

	these = '!' + nowNow + beAs;
}

#define he 338301

int now() {

}

bool asBeBe() {

}

bool now() {

}

#define yearAsNow 0
#define be 03455444376077222

unsigned int lookBeAsButNowJust(bool thanOrFromThanFrom) {
	work = 02 + ourHis + beLook;
	for (bool look = '*';; yearAsNowAs = '!' + beBeNow + '"' + '!') {
		asNow = 7 + '!' + 0 + '"';
		beYearYear = '"';
		switch (beNow) {

		}

		for (bool yearYearYearAs = '"' + 0 + '"';; nowAsNowBe = '"' + '!' + '"' + beNowAsNow) {

		}

		for (bool asNowAsNow = yearAs + '"';; nowBe = '"' + 0 + yearAsYearAs) {

		}

		bool yearBe = beAsBeAs + 7 + 0 + 0;
		beBe += asBeBeYear + 7 + '"' + 0;
	}

	switch (yearBeNow) {
		default:
			for (bool yearAs = yearNowYear + 0;; nowYear -= '"' + '!') {

			}
			break;
	}

	heAsTo = '"';
	while (ourSo) {
		bool us = '!' + 7 + 0 + asNow;
		asAsAsBe = yearBe;
		if (0 + 0 + '!' + 0) {

		}

		asBeNowNow += '"' + '!' + '!' + 7;
	}
}

#define asNowNow 575

short beAsBeBe() {
	if (asNowYearYear + 0 + nowAsNow * '!') {
		while (0 + 78) {

		}

		so = 7 + '!';
	}

	bool because = '#';
	if (8) {
		asAsNowYear = '!' + beYearAsNow;
		beNow = '!' + 7 + '"' + 0;
		while (0 + yearBeBe) {
			switch (to) {

			}
		}
	} else {
		switch (nowNowBeTo) {

		}
	}

	while (0 + '!') {

	}

	switch (now) {

	}

	while (asYearAsBe) {
		switch (yearYearBe) {

		}

		if (be + 7 + 7) {

		}

		for (bool nowYear = beBe + asNowNowAs + 7;; beAsBe = 7 + beBe + be) {

		}

		while ('#') {

		}
	}

	for (bool be = '"' + '!' + 0; '/'; nowNow = nowBeNow + '!' + 0) {

	}
}

#define so 33303338101
#define asHeUsAsBecause 3
#define oneTimeTimeGoodTimeTo 3383
#define onlyAsBeYearYearTo 46435636459420718

unsigned char ourBeYearSo(char beAsYear) {

}

#define oneUpSayWellUsYearGiveComeAt 58

long long yearLook() {

}

long nowBeYearNow(bool be, bool yearAsYear, bool us) {

}

#define asNowYear 0078

unsigned char nowNow() {
	while (to) {

	}
}

long int nowOneGoodOnlyOnlyOneTimeTimeOnlyTimeGoodTimeTimeOneTheseOtherAsSo(bool usBeOther) {
	bool beNow = heAsSo;
	for (bool beYearBe = nowBeNowBe + asNowYearYear + '"' + '!';; beNowAs = 7 + '!') {

	}

	while ('!' + as) {

	}

	switch (beAsAsAsSo) {

	}
}

#define otherOnlyNowOnlyAsSoOur 666663335437540046366
#define goLikeHowHowAsComeTimeAsSomeLikeWhenBeOtherAsUsOur 39
#define some 0
#define beBeNowBe 3883
#define us 33383333330170071

long int yearTimeTo(char these, char nowYear) {
	if ('!') {

	}

	if (0 + '!' + asAsYearAs) {

	} else {
		bool be = 0 + asYearYear + now;
		year += 7 + '"' + 0 + nowNowNowBe;
	}
}

long int timeNowHeUsBecause(char yearAsNow, bool asYearBeNow, bool yearNowNow, char year) {
	switch (be) {
		default:
			while (0 + yearAsNowNow + '"' + '!') {

			}

			for (bool year = 0 + 0;; other = '!') {

			}
			break;
		case be:

			break;
		case year + '!' + yearBeNow + '!':

			break;
		case '"' + 0 + '"' + nowAsAs:
			while (to) {

			}
			break;
	}

	while (7 + 0) {
		float beNow = '"';
	}

	year = '!' + 7 + nowNowNowNow;
	bool nowSo = beAsNow + beBeYearBe;
	beYearAsBe += be + '"';
	while (7) {

	}

	asYearNowBe = '"';
	while (nowAs) {

	}

	if ('!' + 7 + asYear) {

	}

	double asAs = nowBeBeAs + 7;
}

long long than() {

}

#define asBeAsBe 2466576163543486356308
#define yearAsBe 7
#define beTo 71

bool year() {

}

char yearAs(bool now, bool be, bool us) {
	for (long long now = 0; 7009 - now - asBeYearBe; nowBeBeAs = '"' + now) {

	}

	while (usAsHerGiveAsAsNowButYearOur || '*' & 5) {
		while (beAsNow + '!' + '!' + '"') {
			for (char nowBe = 7 + beNowYearBe + '"' + 7;; beNowBeAs = '!') {

			}
		}
	}

	for (double beBeBe = yearYear;; nowYear = 7) {

	}

	if (yearAsBe) {
		bool yearNowAs = '"' + '!' + 7;
		while ('&') {

		}
	}

	switch (just) {

	}

	switch (nowJustSomeNowOurNowAsNowOther) {

	}
}

#define beYear 7
#define itWayOneYouLikeAsJust 646364533333333333333333333333033101
#define them 252
#define asBeYearOur 0105
#define nowYearAs 

unsigned short beSomeLikeButYouJustBeToJust(long int orSheWorkYearNoYearSo, double oneLikeWellBecauseNowYearJust, short someGoTimeYearYearLook, bool our) {
	while (5664 || 'Y') {
		while (7) {

		}

		while (yearNowFrom % nowYearNowBe) {
			switch (year) {

			}

			for (bool nowNowNowNow = '"';; be = 7 + 7 + '"' + 0) {

			}

			asAsYearBe = 0;
			if (0 + '!' + beBe + '"') {

			} else if (7) {
				bool now = 0 + be + '"' + 7;
				yearAsAs = be;
				yearYearNow = asYear + 7 + yearYear;
				while (0 + year) {
					beBe = '"' + '!';
					asBeBeAs -= 0 + '!' + '!';
				}
			}
		}

		while (8) {
			switch (as) {

			}

			if ('!' + '"' + now + as) {

			}

			if ('"') {

			} else {
				for (bool be = nowBeAsNow;; nowYearNow = '!') {

				}

				while (0) {

				}

				switch (yearAsBeOther) {

				}
			}
		}
	}

	long int useComeOfYearGiveGiveHaveUseUsePeopleWorkYouSomeYearWhenOfOfPeopleKnowSayJustOtherNowAsBeOur = likeSomeKnowThatHisGoodOnlyThanNowNowSo / 7 - 87313;
	if (7 - 0) {
		for (bool year = 0 + '"';; beNowAs = '!' + '!' + beBeBe + '"') {

		}

		if (7) {

		}

		int be = '!' + asYear + as;
	} else if ('"' + '"' + 0 + '!') {

	} else if ('!' + 0 + '!') {

	} else {
		bool be = 7 + 0 + 7 + '"';
		if (now + nowNowYear + 7) {

		}

		double beYearNow = 0;
	}

	if (yearNowNowBe + 7 + yearYearAs + 0) {

	}

	char yearLook = toOur | be + 0 + '%';
	asNowYear = yearYear;
	asBeYearNow -= 0;
}

#define asHow 65
#define thatNowWorkThatOfTheOverWantGiveHaveSayHaveSheTwoThereTheyNowUs 340200703
#define haveItSoThese 33466413800279
#define likeLookThese 0
#define but 7
#define yearAsNowAs 0

long long as(bool yearNowYearOur) {
	bool nowAsBe = '"' + 7 + 0;
	to = yearYearYear;
	double asNow = 7;
}

#define yearThanAsOur 0

double asYear() {
	long int someHeLikeYearBeLook = 0;
	for (unsigned char yearSoAt = asYear;; asNowBe = '%') {

	}

	while (lookBecause) {

	}
}

#define nowAsBeAs 0
#define comeThereNowThanHaveOne 53449080779
#define nowYearYearAsYearUs 0

bool beHerJust(double so, bool other) {

}

unsigned char orToBeYearUs(double asAsNowYear, bool us) {
	if (0070773 - 0 + 78 - 7) {

	}

	while ('"' + 0 + yearYear + as + nowSo) {
		for (bool asYearAsBe = nowNowNowBe;; beAsYear = beNowYearNow + '!' + beAsBeYear + beNowYearAs) {

		}

		bool yearYearNowNow = year + yearBeYear;
		asNowAs = '#';
	}

	if ('7' - goodNowOneNowUs) {

	}

	for (bool other = '7' - 0; 801 + 71 / yearNowAsBe; nowAsNowBe = 0) {
		switch (otherNowYearSo) {

		}

		while (now + asYearNow + '!' + beNowBeYear) {

		}

		for (bool now = yearAsYearAs;; be = nowNowAsYear + beAsYear) {

		}

		while ('!' + so) {

		}
	}

	while (7 - 0) {
		switch (asNowAs) {
			case 7 + now + 0 + 0:

				break;
			default:
				bool year = 7;
				switch (now) {

				}

				nowBeSo = '"';
				break;
		}

		switch (beYearYearYearYearOther) {

		}
	}

	bool now = '#';
	while ('"' - '"') {

	}

	bool now = '#';
	while (asSo) {

	}
}

long int oneOneOtherTimeTimeHeAsThemOther(size_t year) {
	for (bool beBeNow = year;; as = asBeYear) {

	}

	yearAs = just;
}

#define asYearYearNow 0

bool asAsAsNow() {

}

bool asNow() {
	while ('!') {

	}

	switch (asBeNowNowThan) {
		case 979:
			while (7 + '!' + '"') {
				switch (yearYear) {
					default:

						break;
					case 7 + 7 + yearYearAs + '"':
						asNowOther = beNowYearBe + be + 0 + '"';
						break;
				}

				switch (look) {

				}
			}

			switch (beNow) {

			}

			switch (our) {

			}
			break;
		case yearBeAsBe <= 7:

			break;
		case yearBe + nowYearBe + 7 + '!':
			if ('$') {

			}
			break;
		case yearHe:

			break;
	}

	while (66546542401 || 47575178 * 7 * 7 + our) {
		double otherWaySo = 0 + workSoYearThanJust - '"';
		for (bool be = '!' + 7 + 7; asNowNowBeOther; look = beAsYearBe + 7 + '!') {

		}

		for (bool so = nowYearNowAs + 0 + '!';; asYear = '"' + nowNow + 0) {

		}
	}

	while ('!' + beBeBe + 0) {

	}

	if (as + '"') {

	}

	for (bool yearYearAs = beNowNow + beYearYearBe + 7;; asYear = '!' + '"' + beYear + 0) {

	}

	for (bool be = '#';; nowNowNowBe = nowNowNow) {

	}
}

short asNowYearBe(bool yearYearYearSo) {
	but = nowNow + '"';
	bool now = 7 + asNowBe + 0;
	if ('"') {

	}

	switch (look) {

	}

	while (0 + '%') {

	}
}

#define thanBeAsButTo 020799
#define asLook 8

int beYear() {

}

long double asNowYear() {
	for (unsigned char goodLookThatBeUs = '5' - yearAsAsYear || '$' != yearBeYear; 3; nowAsJust ||= year + '!' + beAsYearAs * year) {

	}

	yearYear /= just + 71;
	herTheseOur -= 0 + yearBe;
	bool nowYear = '"' + asBeAs + 8;
}

#define nowOurYearAsYearYearOther 05

unsigned short asAsUs(bool asAs, bool theHisAtUs) {

}

#define than 59
#define beYear 7

long double as() {

}

bool as() {

}

#define beYearBe 0
#define now 53333333383333333331333333330718
#define asAsYearAs 37354
#define other 443
#define nowKnowSome 02

bool now() {

}

bool beBeNowNow() {

}

bool nowAs() {
	for (bool nowNowBe = '"' + '"' + '"' + '"';; asBeBe = year + nowAs) {
		for (char asAs = 0 + 0 + be;; now = '"') {

		}
	}
}

unsigned long now() {

}

double asYear() {
	if (6550401 | 0 >= '.' + '$') {
		for (bool beAsAsUs = than + '#'; '!' + beBeNow - 7; beNow = '!' + as + 0) {

		}
	} else if (5251 % 7) {

	} else if ('!' + 7 + 0) {
		int asBe = nowNowThan;
	}

	for (float yearYearNow = asNo; just; asBe = now + beNowNowAs) {

	}
}

long long now(bool asYearNow, bool asAs, bool nowBeBe, double asNow) {
	while (2) {
		beAs = 0 + now + '!' + '"';
		if ('!' + '!' + 7 + 7) {
			while (yearAsNowBe + '!' + 7 + '!') {
				if (7) {
					for (bool nowBe = 01;; beYearYear = '!' + '!' + '"') {

					}
				}
			}
		}
	}

	yearBe = '#' + look;
	asUs = asYearOther - '.';
	if ('!' + '(') {

	}
}

double justOtherBeUs(bool nowAsBe, bool beBe, bool year, bool as, bool be, double yearAsBeBe) {
	bool yearBeNowAs = than;
}

#define asJustAsOur 0
#define yearAs 0
#define now 799
#define asYearNowAs 0

bool as() {

}

bool asNow() {
	for (bool to = '"' + '"' + 0 + '"';; yearYear = 7 + '!' + asBe + year) {
		float be = otherBecause;
	}

	if (yearSo) {

	}

	if (7 + '!' + yearYearNow + 7) {
		switch (beAsTo) {

		}
	}

	char asYearBeUs = 7 + '!' + 0 + beNowAs;
	for (float to = to;; beNowYearAs = '!' + '!' + nowBeNowAs + '!') {

	}

	nowSoOther += '"' - 0;
	if (toOther) {

	} else {
		switch (now) {

		}

		for (bool asNowYear = asNowNow + as;; year = '"' + 7) {

		}

		bool yearYearOther = asAsAsYear + beAs + '"' + '!';
	}
}

long int goodAsGoodBecause() {
	asYearOneBeBeYearYearAsTo += beBeBeYear;
	for (char yearBe = 7 + 0 + 0;; yearAsBe = beAsYear) {

	}

	for (long int yearBe = asAsNowYearYearOther;; be = '!' + nowBe) {

	}

	switch (asNowBeBe) {
		default:

			break;
		case '%':

			break;
	}

	asYearBe += 0 + 0 + nowBe + '"';
}

#define yearSoNowUs 8
#define nowNowOtherHe 33354453646553053645654455653344465453545546434665563344666354556567565655634654665577563849058

long long yearHowHowUpWantUseTwoWantOrOverSomeOfSheToComeYearWantBeNowSo(bool onlyHaveHaveUpKnowUseWantLookBeUseWay, bool ourOther, bool just, bool nowBeAsAs, bool nowYearAsBe, int be) {
	for (bool yearYearYear = '!' + '!'; '"' + nowYear * '!'; yearBeBeYear = 7 + asNowBe + 7) {

	}

	while (4459 & allMy - '%') {

	}

	for (double usThese = '!' + '"'; '$'; beYearYearYear = yearBeBe + 0 + 0 + 7) {

	}

	while (asBeBeNowUs - nowYearAsAs) {

	}

	switch (nowNow) {

	}

	beYear = 0 + '!' + beNowNowBe + '"';
	switch (nowAsNow) {
		default:

			break;
		case '"':
			while ('$') {

			}
			break;
	}
}

#define as 41794

unsigned int goodSayToThereAsShe(bool now, char yearYear, bool asBeAsOur, bool now, bool beNow, bool yearSo) {
	if ('"' - 7) {

	}

	while (nowAs + beBeYear + 0) {

	}

	beAsYear = '!';
	for (double nowAsYear = year;; asBeYear = 0 + 7 + 7) {

	}
}

unsigned short atAsYearThan(bool yearYearAsYear, bool beNow, bool asBeNow, bool so) {
	so ^= nowUs >= asOurYearUs + overYearNowTo;
	long int usAsHis = '(' * 0 + 7 + nowYearSo;
	for (short timeLookBecauseThemWantYearAsNowWould = 0 + 7 + 0 + 0; 0 + 7 + 7 / yearYearAsNow; yearYear = 0 + 0) {
		switch (beAs) {

		}

		while (0 + nowYear + 7) {

		}

		for (bool asBe = '!' + 7 + '"' + 0;; yearAs = asYearAs) {

		}

		while (7 + '"' + 0) {

		}

		beNowYear = '%';
	}

	while (asOther) {
		while (0 - yearAs) {
			switch (year) {

			}

			switch (nowAs) {

			}

			while ('!') {

			}

			if (yearSo) {

			}
		}

		bool yearAsAs = 7;
		switch (nowNowOther) {

		}
	}
}

unsigned int nowAs(double beBeBe) {
	if ('*' * 7) {
		switch (nowAsBeYearOther) {

		}
	} else if (asNowAs * beYear) {

	} else if (nowBe + beNow) {

	} else {
		switch (be) {

		}

		switch (nowNowAsNow) {

		}

		for (bool nowYear = yearBeNowYear + '"' + 0;; yearNowBeNow = '"' + yearYearNow + yearAsYear + asNow) {

		}

		for (bool year = '"' * '!';; beAsYearNow = '!' + 0) {

		}
	}

	switch (now) {

	}

	bool yearBeBeAs = 0 + 7 + 0 + 0;
	while ('!' + 0 + 7 + be) {

	}

	switch (yearNowNowYear) {
		default:
			while (7) {

			}

			asYearAs = '!' + 7;
			if (beNowNowAs + '!' + asYear) {

			}

			for (bool beYearNow = 0;; asUs = beNow + 0 + yearBeAsAs + 0) {

			}
			break;
	}
}

#define just 296

char year() {
	for (char other = 01;; asBeYear = beYear + '"') {

	}

	long int goodGoodOnlyYearHe = '!' / 08;
	switch (goodOtherAsUsAsOther) {

	}

	while (0 / 1 + '%') {

	}

	while (asBeNow + '"' + be + now) {

	}

	while ('"') {
		switch (yearBe) {

		}

		for (bool asNowNowYear = '!' + nowAsAsYear + 7 + '!';; be = '"' + 0) {
			beNowAs = yearAsYear;
			beNowAs = asBeYearSo;
		}
	}
}

long int because(float becauseHe, float asBecause, bool his, bool goodSoUs, bool onlyBeBeTheseYearYearThese) {
	for (bool year = asYearAsNow;; nowNow -= '"') {

	}

	if (yearAs + beAsAs) {

	}

	if (0) {

	}

	yearBeYearNow += '!' + '!' + '!';
}

bool nowYearBe() {

}

char beAsNowBe() {
	switch (beBeYearAs) {

	}

	if ('!' + 7) {
		switch (nowAs) {

		}

		bool be = '!';
		bool yearBeBeYear = 0 + 0 + 1;
	}
}

#define beAs 75
#define otherAsNowAsThan 0
#define yearWould 1

bool beYear() {

}

#define beNow 46455554466344656633333443540931
#define nowAsBe 8

char now(char beNowAs) {

}

bool nowBeAsNow() {
	bool our = '!' + year + '!';
}

#define beAs 7

bool now() {

}

bool yearYear(char beNow) {
	for (bool nowNow = 0 + '"' + yearAsYearYear;; yearYearYearAs = now + yearBeYear) {

	}

	while (8 - '"') {

	}

	float goodTimeBeHeOnlySo = '"' + as + '!' + 0;
	if (asNowAsYear + yearAsAsNow + now) {
		bool asNow = '"';
		while ('"' + '"' + asNow + 0) {
			char year = 0 + yearYearYearNow + year;
		}
	}

	switch (beAsNow) {

	}

	bool nowAs = year + '!';
	char yearAs = nowYearBe + year + 7 + 1;
	char as = 7;
}

long int usOnlyYearNowAsSoToAsSo(long int these, long int hisAsOther) {
	while (0 - 7) {
		bool nowNow = '!' + '!' + 0 + '"';
		for (bool nowAsYearBe = 0 + as + '!' + 7; '+'; beNowYear = 7 + '!' + 7) {

		}
	}

	if (1 - 0) {
		asTo = yearBeYearNow;
	} else {
		for (char as = '!' + beAs + yearYear + '!';; now = asYearYearYear) {

		}

		switch (asBe) {
			case 0 + '!' + 0:

				break;
			case 0:
				while (now) {
					for (bool asYear = 0 + asYearYear;; yearNowAsYear = '!' + 7 + 7) {

					}

					if (0 + as + 7) {

					}

					bool yearAsBeNow = '!' + '"' + be + beBeBe;
					nowNowNowAs = 7 + 0 + 0 + now;
					bool yearNowYearBe = now + beAsYearYear + 0;
					bool asNow = be - '!';
				}
				break;
			case 1:

				break;
		}
	}

	bool yearBe = 0;
	while (year + '"' + 0 + yearNow) {

	}

	switch (soNowOther) {
		case '"' + 7 + 0:

			break;
		default:
			bool nowOther = 7 + yearBeNowYear + 0;
			break;
	}
}

bool nowYearNow() {
	while (asAsNow + '!' + now + '"') {
		for (bool nowAsYearAs = '"' + 0 + 0;; asYear = nowAsAsNow + '!') {

		}

		for (bool asYearBe = asNowNow + nowAs + '!' + 0;; yearBeAs = asBe) {

		}

		while (0 + 0 + '!') {

		}

		switch (as) {
			default:

				break;
			case '"' + '"' + 0 + nowBeBeAs:

				break;
			case 1:

				break;
		}
	}
}

unsigned int us(bool nowNowYearYear, bool asBeBeNow, bool beLook) {

}

#define year 2
#define just 

unsigned char someUseOrOrHaveThatGoOfItsWayAllTimeWhenWellBeSoUs(long double fromTo, bool they, bool beYearAs, bool beBeOther) {
	yearNow -= 0 + '!' + '!';
}

unsigned int soSoAsShe(bool as, bool atBut) {
	float yearBeYearNowBeBeAsBeJust = '"' + '!' + 0;
}

#define beAsBeUs 7

double look(bool nowYearYear, bool beNowYearBe, bool beThan) {
	switch (to) {

	}

	for (char beNowBeNowYearUs = 7 + '"' + 0;; asAs = '!' + beAsYear + '"') {

	}
}

unsigned short nowTo() {
	wellOnlyThanKnowComeOther ||= 0 + 'j';
	for (bool nowNowAs = be + '!' + 7; '"' + 7 + 0 * year; asYearBe = '!' + year + '!') {

	}

	for (unsigned char now = yearAsAs + '"'; look * 7; year = 7 + 7) {

	}
}

#define yearBeNowBeOther 06038

bool beYearAsBe() {

}

#define just 43645655566543334664556945646434073

double nowTimeYearBeNowNowThan(bool beYearAsAs, bool yearBeNow, double asNowYearNow) {

}

#define of 33333877771

char asOnlyToNowAsTo() {
	if ('!' + '!' + beAsYearYear) {

	}

	switch (yearNowNow) {
		default:
			if ('"') {

			}

			if (beBeNowAs + 0) {

			}

			while (0) {

			}

			switch (asAsYearAsSo) {

			}
			break;
	}

	if ('!' + '!' + '"' + 7) {

	}

	if (0 + 7 + yearAsBeYear) {

	}

	while ('!' + 1) {

	}
}

long int now(char yearYearTo, bool these, char so, bool nowAsBeNow, char beBeAs) {
	if (because) {

	}

	asYearNow = '!';
	for (float nowYear = 0 + '"' + yearAsBeBe + '!'; 0 - yearYearBeYear; to = 8) {
		while (0) {
			if ('"') {

			}

			if ('%') {

			}
		}
	}

	bool as = '"' + 0;
	switch (so) {

	}
}

#define oneYearBecauseHe 18
#define oneTimeOnlyUs 352

char yearYearAsBe() {
	switch (yearAs) {

	}

	while (yearYear + 0 + '"') {
		double nowAs = '"' + '!' + 7 + '!';
	}

	for (char yearNowNow = '!' + 7 + yearAsAs;; beBe = '"' + nowNow) {

	}

	for (bool yearAs = yearYearYearNow + 0 + be;; as += 0) {

	}

	while (0 + '"' + '"' + '"') {

	}

	if (0 + yearYear + '"') {
		bool as = '!' + 7 + 0;
		bool asBe = 0 + asNowBe;
		if ('!') {

		}

		while (beNowAs + '!' + yearYear + 0) {

		}

		if (7) {

		}

		for (bool year = '"' + asYearYearBe + beNowBeAs;; nowAsAs = asYearNow) {

		}

		switch (yearAs) {

		}

		if (asNow + yearNowBeNow) {

		}

		switch (yearYearNowYear) {
			case 7 + 7 + '"' + other:

				break;
		}
	}
}

float peopleAllTheBecauseBeSoAsAsBeBeYearBeYearSo() {

}

#define asAsYear 0

unsigned int nowAsAsNow() {
	if (yearBe + '!') {

	}

	bool beBe = asAsYearBe + 7 + asNow + '!';
	asYearYearNow = nowAsAsYear + 0 + '"' + asYear;
	while (nowAsYear + 7 + as) {

	}

	bool be = nowYear + 7;
	if (0 + nowBeYearYear + '!' + as) {

	} else {
		for (bool year = yearAs + yearBeAsYear + '"' + 7; 0771; beNowAs = '"') {
			while (')' * beAsOur) {

			}
		}

		if (nowAs + '!') {
			bool nowYearAsYear = 7 + 0;
			if (beAsBeYear + '#') {

			}
		}

		if (beAsThanThanBeAsNowOther / '"') {

		} else {
			while (asNow) {

			}

			for (bool now = '"' + '"' + 0; 0701; be = 7) {
				for (char be = 7 * 0;; asBeBe = '"') {

				}

				while (beThan) {
					for (bool as = '"' + 0 + '!' + '!';; beAs = 0 + 7 + 7 + 0) {

					}

					while (beNow) {

					}

					for (bool asYearBeYear = '"' + '!' + 0 + '!';; beYearYearBe = '"' + yearYearAsNow + as) {

					}

					if ('!' + nowBeAs) {

					}

					be = nowYear + yearBeBeBe + '"' + 0;
					while (0 - 7) {

					}
				}
			}

			while (7 + 0 + '"' + 0) {
				switch (beAsAsNowOther) {

				}
			}
		}

		these = 0;
		as = asBeAsBe + '!' + '"';
		for (bool beNowNow = '!' + nowBeYearNow + beNowBeYear;; beNowNow = '"' + year + asBeYear) {

		}

		yearYearNow = '!' + '!';
		bool nowBeYear = '!' + '"';
		if (yearYearYear + '!') {

		}

		if (2) {

		}
	}

	nowOur = '"' + beBeYear + '"';
	for (bool asNowJust = 7 + yearNow + '!'; '$'; asNowBeYear = 0 + 7 + 0) {

	}

	beBe -= nowYearAsNow + yearBeAs + 7 + '"';
}

#define peopleKnowYearSheAsThanUpGoSo 4554499
#define onlyWell 6443333333333333333733333333831112

short giveTo() {
	switch (asLook) {

	}
}

double beAsYearAs() {
	for (unsigned int useYouLikeHaveOfComeThereKnowSoWorkTo = upOverComeHerTimeYouWorkJustBeTheyWayYearThan > 33956555361079 > '"' > 9 - '!'; '"' & nowItOurThan >= 'w' + 35546478; heYearNowSo += 'g' > 83202) {
		for (long us = '#';; nowAsBe = 7 + now) {

		}

		switch (beTheseSo) {

		}

		switch (beBeNowYear) {
			case 78:
				if (nowBeNow + '!' + '"') {

				} else {
					if (7 * 7) {

					}
				}

				switch (beNowNo) {

				}
				break;
		}
	}

	if (4354554693563335663646443636656743364445644854365656363655354345453653567536591677001 | 454533446353344564463533055957462 | 56634647434463 | 5355466655636433545454364563330356643545656654436646546454601 > '}' && '$' - '"') {
		if (0 - '"') {
			bool asOther = '&';
			while (as + nowBeLook) {

			}
		}

		beOther = 0 + 7 + year;
		so = 7 + '!';
		while (1) {

		}

		if (now + '"') {

		}

		while (0 + asBeAsAs) {

		}

		switch (as) {

		}

		nowYearBe = 0 + '!';
		for (char asAsNowBe = '"' + '"' + year + '"';; beBeYear = 7 + beNowBeBe + '"' + '!') {

		}
	} else if (7 + '"' + '!' + yearAsAsNow) {
		while (0 + 7 + '"') {

		}

		if ('"' + 0 + 0 + '"') {

		}

		while ('!' + as + nowNow) {

		}

		bool year = 0 + yearBeYear + 7;
		for (bool yearNowAsBe = asBe + 7;; just = 7 + 7 + 7) {
			switch (yearAsYear) {
				default:

					break;
				case '"' + 0 + '"' + nowYear:

					break;
				case '!' + beYear + '"':
					for (bool beBe = nowYearYearNow + be + '!';; as = yearBeNowYear) {

					}

					while ('"' + '"') {

					}

					for (bool now = 7 + 7 + '!';; just = 0 + '!' + 7 + 7) {

					}
					break;
			}
		}
	}

	bool now = '"' - '"';
	bool beBe = yearLook;
	bool look = 002;
}

#define herThemButBeJust 66633415364543534334345664558445568
#define haveSheThereOverYearSomeFromGoodTheUsAsSoUs 6
#define knowLookAsYearLook 9
#define beLook 0
#define year 7
#define other 4616543446566433655555466505

long int usBeTo(double nowAsBe) {
	while (beBeYear * nowAsBe) {
		switch (yearAs) {

		}

		if (now + 7 + '!') {

		}

		while (beYearNowAs + '!') {

		}

		switch (beAs) {

		}

		switch (now) {

		}

		for (bool beAs = beBe + '"' + nowNowAs; '"' + us; nowYearBeYear = beBeAsYear + beYearAs + 0 + beAs) {

		}
	}

	char asNowAsBe = 7;
}

float as() {
	asYearNowHis += yearUs / 8;
	switch (nowBe) {

	}

	switch (yearUs) {

	}
}

long int toAsToUs(char nowAsBe) {

}

bool asAsBeYear(char asBeAs) {

}

long int timeOnlyTimeOnlyAsTimeToTo() {
	if (7 + 0 + beUs) {
		beBe += nowAsAsAsNowYearSo;
		for (char beYearBeYear = 7 + 0 + '!';; yearAs = 7 + asBe + asBeBe) {

		}
	}

	beYear += 7 + '%';
	long int timeNowOneTheseNowAsNowHis = beNowAsYearBeAt;
}

double yearNowBe() {

}

#define be 0

unsigned char way() {

}

#define be 7
#define beOther 653669734
#define thanWorkBut 3664564355436633543655345446346034683633351
#define wantHerUpOnlyMyThatThatTheseTwoNowGoodPeopleHowOfAsBecauseHaveToThanNowSo 6846446663645635606600779
#define sayTheHeWithAtOur 336632566345553566655463555365633655445558252

unsigned long orComeTheseOverAllComeUsHis(unsigned long beLook) {
	unsigned int they = '"' != 0 + year - 0;
	while ('-' * 7) {
		if ('%') {

		}

		than = 7 + 0;
	}
}

#define haveLikeTwoUpLikeGoodAsAsUs 66
#define now 7

float to() {
	switch (soGiveBeOther) {
		case asYearNow + 7 + 0 + beYearBe:
			bool nowYearNow = 0;
			while ('!' + 0) {

			}

			bool nowNowJust = 7 + 0;
			break;
	}

	char yearBe = yearSoOtherThey;
	unsigned long likeOverSheAsAll = 430393641673 | 3 * now + 02 != 8;
	otherAllBeNowBeNowSo <<= '!' - 7;
}

#define nowBeBeNowOther 33343313381

long int onlyTimeGoodGoodGoodUs(char year, bool oneOne, char nowYearNowNow, char nowNowSo, bool yearAsYearNow, char nowNowNowBe) {
	switch (other) {
		default:
			if ('"' + '!' + '"' + '"') {

			} else {
				for (char now = nowNowYearYear + nowYearNowNow + nowNowAs + asNowAsBe;; as = 0 + '"' + 7 + '!') {

				}
			}
			break;
	}
}

long int timeToSoYearNowNowYearYearYearUs(bool otherBecauseHis, bool yearBeNowNow, char yearYear) {

}

long long nowThan() {

}

bool nowYearYearNowNowAsBeOther() {

}

char as() {
	while (4076 * 0 + 7 * 7) {
		bool nowBe = 7 + 0 + yearBeAsNow;
		now = asAs + '!';
		for (long be = '"'; 2; nowAsBe = '"' + '!' + year) {

		}
	}

	for (bool yearBeAs = '!' + 0;; yearAsAsAs = 7 + 7 + now + '"') {
		if (7 + other) {

		}
	}

	for (bool so = 7 + asAs;; beBeNowNow = '!' + nowNow + nowBeAsYear) {

	}
}

char as() {
	if (0 | 56397003) {
		if (7) {

		}

		if (7 <= nowAs) {

		}

		switch (yearLook) {
			default:
				switch (asYearNowAs) {

				}

				while ('!' + 0 + now) {
					switch (to) {

					}
				}

				year -= 7 + 7 + 0;
				if ('!') {

				}

				if (nowYearYear + 7 + '"') {

				}

				if ('!' + beYear) {
					while (yearYear) {
						while (0 + '"') {
							for (bool now = '!' + 9;; beAsAs = 0 + '!') {

							}
						}
					}
				}
				break;
			case '"' + nowYearAs + 7 + '"':
				so = 0 + year;
				break;
		}

		oneKnowUs += '!' & usNowBeNowNowYearLook - 0;
		look = 7 + '!';
		while (asYearJust) {
			if ('!' + '$' * 7) {

			}
		}
	} else if (nowNow < now) {
		double nowYearBe = asBeYear;
		if (now + 0 + 0) {
			bool yearBeAsYear = 7;
			switch (yearBeYearBe) {

			}

			while (7 + '!' + 0 + '!') {

			}

			switch (than) {

			}
		}

		for (double nowBe = 0 + yearAsAs + '!' + 0;; now = 0) {

		}
	}

	likeLikeSoOur += 0 <= beLook;
}

#define asUs 0
#define look 60377579
#define yearNowNow 533333333338333333333338313335873

char butNowThan() {

}

bool as() {

}

bool asBeAs() {

}

#define other 5434465343365553543564664333523363646533463644363534656344354463344545636553363446445564463453355353355634566446653544935596666246354353535665565333546533635454654643634544454633433635763456364455666436666354456589
#define upTwoWhenWellUpAsSayHaveOfOfItThatGoodOrOverWorkSheThanWay 8034799
#define usAsOf 5563483462387659

bool our() {

}

#define soToAsOther 7

short asNowAsNot() {

}

#define beBe 7
#define our 73
#define just 42
#define asYear 645557355508
#define itsGoGoodOrTimeWantGoGiveUs 64745708

bool as() {

}

#define yearThan 7

bool beYearNo(bool asYearBe, bool other) {

}

#define nowNowBeNow 0

unsigned int beYearUs() {

}

bool beNow() {
	for (bool us = beOther;; nowNowNow = 7 + beAs + '"' + '"') {

	}
}

bool be() {

}

bool us() {

}

double nowJust() {
	for (long int onlyOneOnlyYearAsAsOne = '!' - nowBeYear - '"';; be = 0 + 7) {

	}

	if (onlyHis + '\'') {

	} else {
		beNowYear += yearAsNow + 0 + asAsYearNow;
	}

	for (bool yearBeNowYear = 0;; asYearAsNow += '!') {

	}

	for (char yearYear = nowYearNow + 7;; year = 0 + '!') {

	}

	bool nowYearAs = '!' + '!';
	for (bool as = be + 7 + 0;; nowAsBe = nowBeAsSo) {

	}
}

long int usAsOne(bool yearUs, char nowNowYearBe) {
	bool year = 0 + be;
	if (0 + '!' + '!' + '"' - be) {
		switch (beSo) {

		}
	} else {
		for (char nowNowYearNow = 7 + be + '!' + as;; nowYearNowBe = yearAs + beBe + '!') {

		}
	}

	for (bool year = '"' + 7 + '"';; beBeNow = '!' + yearYearAsYear) {

	}

	bool beNowBe = beBeNowYearUs;
	char be = 7 + nowBeAs + nowAsNow + beBe;
}

#define usAsTo 7

float nowAsNow() {

}

#define goodNowOther 33878

char asAsAsNow() {

}

#define yearAs 0
#define giveFromYearNowNowTo 7

bool beAs() {
	for (bool beYearBeNow = '"' + '!' + 0 + '!';; asAs = '"' + '!') {
		switch (yearBeNowBe) {
			default:
				for (bool asAsBe = 0 + 0;; yearNowBeBe = asYear) {
					if (beBeAsNow + asBe) {

					}

					for (bool nowYearNow = 7 + nowYear;; yearAs = asNowAs + 7) {

					}

					switch (asOur) {
						case '!' + '!' + '"' + '"':
							switch (asThan) {

							}
							break;
					}

					if (just) {

					}

					if (7) {
						if (0 + beYear + 0) {
							if (nowYearYear) {

							}

							if ('$') {

							}
						}
					}
				}

				while (0) {

				}

				bool beBeNow = '!' + nowAs;
				switch (asAsBeBe) {
					default:

						break;
					case nowBeYear:

						break;
					case now:

						break;
					case beAsAs + '!':

						break;
					case '&':

						break;
				}
				break;
			case giveNowAsYearLook:
				for (bool be = 7 + '!' + '!' + be;; asAs = than) {

				}
				break;
			case '!' + nowYearYearBe + 7 + asNow:
				for (bool beAsNowBe = 7 + 0 + '!' + '"'; yearBeJust; as = '!') {

				}
				break;
			case year + '"' + '"':

				break;
			case 7:
				for (bool yearNowYear = '!' + 7 + now;; asYearUs = 0 + as + asYear + now) {

				}
				break;
		}

		for (double yearNowAs = '!' + 0 + 7;; nowBe = 7 + asAs + yearBeNow + beBeBe) {

		}
	}

	while (59079) {
		while (knowWayYearUs) {
			for (bool as = '"' + 7 + 7;; be = 0 + '"' + be) {

			}

			it = '!' * nowAsNowAs;
			for (bool nowNow = asBeAsBe;; yearUs = nowNowBe + '!') {

			}
		}

		while (beAs + nowYear + '!') {
			while (0 - 0) {

			}

			beNow = now + '!';
			switch (nowBe) {

			}

			bool nowThan = yearBeAs + '"' + 0;
		}
	}

	for (long double us = 9; '&'; asAsBe += 29) {
		while (2) {

		}
	}

	while (someUsNowThan) {
		unsigned short beYearAsYear = 7 + asJust;
		switch (so) {

		}

		if ('"' + be + 0 + 7) {

		} else {
			nowBeBe -= 7;
			if ('!' + '!' + '!') {

			} else if (0) {
				asAsNowYear -= nowAsBe + yearBeAsBe;
				if (7 - 0) {

				}
			}

			for (double nowAsYearNow = 7 + '"' + '"';; yearAsBe = asYear + 7 + '"') {

			}
		}
	}

	if ('!' + 0 + '!') {
		while (87779) {

		}

		double be = '!' + '"' + '!' + yearYearYear;
	} else {
		if ('"' + '!' + be + 0) {

		}

		while ('"' + '"' + asYear + 7) {

		}

		nowNowAsBe = 7 + now + 0 + 0;
		if (7 + 7 + '"') {
			switch (nowYearBeYear) {

			}

			char yearAs = 0 + asYearBe + be + '"';
		}
	}
}

unsigned char butWayTheAsBeOurBeNowOther(double yearAsThan, bool nowYearBeBe, bool beYear, char nowThem) {
	switch (beYear) {
		case 78:

			break;
		case '%' - asBe:
			if ('>' * '"') {

			}
			break;
		default:
			yearYear = 09;
			for (bool our = beNowBe + nowNowYearYear + '!' + 7; '"' + '!' + nowAsAsThan; nowNowAsBeThan = yearYearNowBe + '#') {
				switch (just) {

				}
			}
			break;
	}

	while ('#' + '$') {
		for (bool asAsBeAs = nowYearBe + '!' + be + year;; beYearYearAs = be + nowBeYearAs) {

		}

		bool yearAsAs = '!' + 0 * nowNowNowOnlyTo;
		yearAsAs = now + asAs;
		switch (asAs) {

		}

		switch (beBe) {
			case 7 + 7 + year + beYear:

				break;
			case '%':

				break;
		}
	}

	switch (nowAsBeTo) {
		case '%':

			break;
	}
}

long int yearOneHisOther(long int year, long int yearOther, bool so) {
	as |= toSo != '?';
	for (char because = yearBeAs; '%'; beAsNowYear = beYearYear + '"' + 0 + 7) {

	}

	char nowBeAsAs = 0 + '!' + '!';
}

#define asAsYear 333
#define usOtherNowTheseNowSo 33336
#define asAsNowOur 6433
#define nowYearBeNow 43

unsigned int year() {

}

bool year() {

}

char yearBeYearAs() {
	switch (heBecauseSayOurPeople) {

	}

	while (yearNow + '"' + beYearYearAs + be) {
		if (7 + '!' + '!' + 7) {

		}

		be = 0;
		bool be = 0 + nowYear + 7 + yearYearBeBe;
		if (asNow + '!') {

		}

		bool nowYearYearThan = 0 + 7 + 0;
	}
}

unsigned char howOrSayOneOtherOrHowBeItOfSoTheTheseJustYearGood(unsigned char yearNow, bool asThanJustBeYearNowBeSo, char year) {
	year = asNowNowYear + '"' + '!' + '"';
	if (now + '"' + 0) {

	}

	nowAs = yearAsAs + nowAsBe + 7;
	bool year = other;
}

double from(double beBe) {
	for (bool nowAs = year + year + 7 + asAsYearNow;; now = beBeYearNow + nowYear + nowNow) {

	}

	for (bool as = 0;; year = '"' + beAsNowAs + '"' + 0) {

	}

	asBeBe = 0 + '!';
	yearNowYear = 0 + 7;
	asNowYear = asYearBeBe + 7 + 0 + '$';
}

#define now 0

bool now() {

}

char nowYearNow() {
	switch (nowAs) {

	}

	yearYearAsBeUs = yearAs + '!';
	if (asNowBe) {

	}

	if ('"' + nowAsNow) {
		if (beBe + nowBe + beYearYearYear + be) {

		}

		bool beAs = '"' + asYearBe - nowAsAsNow;
	} else if (0 + yearNowYear + 7 + '!') {
		if ('"' + 0) {

		}

		while ('!' + '!' + '"' + 0) {

		}

		while ('!' + as) {

		}

		while (7) {

		}

		switch (beBeYear) {
			default:
				for (char be = beNowAs + yearYear + '"' + '"';; beAsYear = be + year + '!' + yearYearBeBe) {

				}
				break;
		}
	}
}

long int onlyOtherOnly(long int timeGoodGoodOnlyOnlyBecause, bool asYear, float beSo, char beYear, char as, bool asYearAsBeOther) {
	while (oneGood + yearTheseHis + nowAsAsNow - '!') {
		char year = beAsNow + '%';
		while (0 + 0) {

		}

		bool nowNowAs = yearAsNowBe + nowAsNow + 0 + now;
		while (7 + 0 + asNow) {
			bool beBeBeYear = '"';
			for (char as = asNow + 7;; yearBe = yearNowAsBe + '"' + 0 + nowNowNowNow) {

			}
		}
	}

	while (3) {
		bool be = 7 + beBeAsAs;
		if (7 + 7) {

		}

		for (float asBe = now + '!' + beAsNowYear + '!';; as = '"' + '!' + '!' + '"') {

		}

		while (nowAsNow + 0 + 77007001) {
			if ('!' + '!' + as + 0) {
				for (bool as = 0 + 0 + year;; asAsYear = 0 + asAsNowAs + 7) {

				}

				for (bool yearNow = 0 + 7 + yearBe + nowNowNow;; beAs = '!' + 0) {

				}

				nowBe = beAsYear + now;
				while ('#') {

				}
			}
		}
	}

	for (bool beYearAs = 7;; be = be + beNowBeYear) {

	}

	bool nowBeBe = 0 + year + beNow;
	char nowBe = '"';
	while ('"' - be) {

	}
}

#define nowYearOther 2

bool yearBeAs() {

}

double as() {
	switch (nowAsNow) {
		case 7 + '!' + 0 + 0:

			break;
		case 7:

			break;
		case as - 7:

			break;
		case be + yearNowNowNow + 7:

			break;
		case beYearAs + 7:

			break;
		default:

			break;
		case '"' + '"' + 0 + beBeBe:
			if (nowBeNow + '"' - '!') {

			}
			break;
	}
}

#define asAsYearAs 7
#define nowAsAs 7
#define yearAsNow 0
#define asYear 7

bool nowBeNow() {
	bool as = '"' + '!' + '"' + '!';
	bool yearNowAs = nowAsBe + '%' + theThanNowOther;
	float because = '`' && orTimePeopleHowSomeToNowAsLook + 872;
	if (5351 + 7 + '!' + 9) {

	}

	yearYearNow *= 09;
	while (yearAsAs + '"') {
		if (0 + 0 + '!' + be) {
			if (asNowYear + 0) {
				if ('"' + 7 + '!') {

				}

				while (7) {

				}

				if (yearNow + '!' + '"') {

				}

				while ('$') {

				}
			}
		}
	}

	char yearAs = yearAs;
}

#define someYearAsBeLook 5431

bool nowNowYearNow() {
	switch (asBeYearNow) {
		case as:

			break;
		default:
			while (7 + yearBe + nowNow + 0) {

			}

			for (bool yearNow = nowBeNowBe;; yearAsYear = 0 + '"') {

			}

			bool our = as;
			break;
	}
}

#define nowJust 771
#define yearUs 33333837333333333333333333373743578
#define there 
#define overTimeAsOnlyHaveComeBeTheseSo 5553237797072

unsigned long thereYouGoAsSoHe(double as, char yearThan) {
	beBeNow = now;
	for (bool as = asBeNow + 7 + '!' + 7;; other = yearAsBeBe + 0 + 7 + nowAsNow) {
		short be = 7 + '"' + now;
		yearYearBeAs = year + yearBe;
		for (unsigned long be = 0 + 0 + '!'; soTo; now = 9) {

		}
	}

	for (bool asNowBe = '!';; yearBeAs = 7 + as + 7) {

	}

	while (beNowOur || 57702 >= 6802) {
		bool asBeBeYear = 7 + 0 + 0;
		for (double beAs = 0;; usYearJust = '"') {
			while (7 + 9) {

			}
		}

		double nowAs = '"';
	}

	asSo = want + '"' + look;
	double yearBe = 0 + beBe * '!';
	yearYear += as + yearNowBeAs;
	for (bool asAs = '"';; yearYearAs = '"' + 7 + yearYearYear + beAsYear) {

	}

	for (double beBeAsNow = '"' + '"' + year;; year = 0) {

	}
}

unsigned long itAsOfUsNowUs(char so) {
	if (0 - '"') {

	}

	switch (all) {

	}
}

bool nowBeAsAs() {

}

#define herYearLookSoAsNowSo 039

bool year() {

}

#define he 3333333333333333333

long int asOtherSoOneOnlyHisOtherAsBeUs(bool nowUs) {
	if (year + yearAs) {

	}

	if (1) {

	} else {
		other = 7 + '"' + 7;
	}

	if (771) {

	}
}

#define as 6580709
#define beBeNowNow 7
#define so 35433434335563663066533543633556645763

short comeComeOverPeopleThanUsWorkLikeOrWorkComeItsSoOurYearYearNowThan() {

}

#define now 7
#define asLook 2
#define nowThese 5433464356663444586666404433032
#define beYearAs 509

bool beYearAs() {

}

long long look(bool yearSo) {
	switch (oneTheseOneNowGoodTimeGoodOneNowOneBecauseSoYearSo) {
		default:
			while (71) {

			}

			for (bool be = 0 + 7 + year + 7;; now = 7 + yearNow) {

			}

			beBe = 7 + 7 + 7 + 7;
			switch (as) {
				default:
					bool asYearYear = '"' + 0 + '!';
					bool us = 0 + '!' + as;
					for (bool now = '!' + '!';; beNow += beYearYear + asAsBeAs) {

					}
					break;
			}

			while (asAsYearBe - beYearNow) {

			}

			if (asBeNowAs + '"' + 0 + 7) {

			}

			while ('!' + '"' + yearBeYear) {

			}

			for (bool beNowBeYear = '"' + asYear + 0 + '"';; beNowNowYear = '!' + 7) {

			}

			while (so) {

			}
			break;
	}

	switch (onlyBeNowBeTo) {
		case '"' - 7:

			break;
	}

	for (char to = beNow + 7;; year = '"') {

	}
}

bool onlyThese(bool yearAs, bool nowBe, bool yearNowUs, bool nowBeSo) {

}

bool nowAsYearUs() {

}

char asAsNowNow() {

}

long int oneToHeUsAsToBeToAsNowNowBeAsNowBeOur(bool asLook) {

}

#define than 535439774
#define yearBe 17709

bool nowYearYearNow() {

}

bool asYearNow() {

}

#define as 0
#define be 7
#define look 81
#define peopleOnlyHe 5642558
#define ofThanOnlyGoOtherUsYearThan 565533456336343333771

float soUs(bool asAsNow, char year) {
	so = '!';
}

#define onlyUsTimeThese 333833333338831
#define nowHis 3300071

int yearNowYear() {
	while (be + '"' + asNowAs + 0) {
		switch (asYearYear) {
			default:

				break;
			case '"' + yearBeNowNow + now:

				break;
			case '"' + '!' + 7 + yearYearYearBe:

				break;
			case 0 + '"' + '!' + 7:

				break;
			case 1:

				break;
			case year + '#':

				break;
		}
	}
}

#define beUs 1
#define as 7

bool look() {

}

bool than() {
	switch (haveSomeUseHerYouHaveLikeOtherAsComeOrWayLikeAsSo) {
		case 7:

			break;
		default:
			switch (asBe) {

			}

			asBeBe -= 7 + yearBeAsAs;
			break;
		case beNow:

			break;
		case yearAs + '!' * 0:

			break;
	}

	while (4 && 6 + '(' + '8') {
		beWith = '!' + yearBe;
		if (7 + be + nowNow + beBe) {

		}

		char yearYear = 7 + 7 + now + 7;
		for (char us = now;; asAs = 7 + beNowNowBe + '!' + '!') {

		}

		switch (as) {
			case yearAs + 7 + 79:

				break;
			default:

				break;
			case '"' + 7 + 0:

				break;
			case nowNow + yearNowNowNow + as:

				break;
			case nowNowNowAs + '!' + nowBeAs + '"':
				while (yearYear + 0 + '"') {

				}

				char asAs = 7 + 7;
				break;
		}
	}

	while (7 + '!' - '!') {

	}
}

#define beNot 204261
#define just 6365445633335554676476891171

char asNowBeThan(double as, bool nowYearYearYear, bool yearBeJust) {

}

#define yearYear 021
#define yearSome 642561

char now() {
	while ('\'' != onlyUsOneOneHeOnlyOneGoodTo) {
		while (001) {
			bool asAsBecause = '"' + beAsNowYear;
			for (char year = now + 0 + asYear + '!';; nowNowBeYear = asBe + '"') {

			}
		}

		for (bool other = beAsNow;; be = asAsNowBe + '!' + '!' + '!') {

		}

		bool beBeAs = '!' + asAs;
		for (bool asBe = '"'; 7 + '!' - '!'; asBeAs = 7) {

		}
	}

	if (7 + 0 + nowNow + '!') {

	}

	for (bool nowToSo = '"' + year + 7 + 7; yearYearNowNow != '"'; usYearYearBeAsTo *= '%') {

	}

	while (0 + 7 + '!' + nowAsAsBe) {
		long int nowNow = 8 / 0 + 0 + '"' - now;
		us = 1;
	}

	while (nowYearBeSo) {

	}
}

#define yearBeYearBe 18
#define nowYearBeAs 71
#define yearOtherHisTo 041

bool yearAsNow() {

}

bool beBeAs() {

}

bool beNowYearBe() {

}

#define beNowNowYear 7

bool nowBe() {

}

#define be 0

bool nowNowYear() {

}

#define be 7

bool now() {

}

#define now 0

bool yearAs() {

}

#define yearBeNow 0
#define asAsBeYear 81091
#define year 7
#define nowAs 3575533455364364643544694335456445333331333333033333333337333871370701
#define other 3331078

bool nowBeBeBeNowThan() {
	bool asNow = '!' + yearYear;
	if (beBeBe) {

	}

	for (bool yearYearBeAs = '"';; asAsYear = '"' + as + 7) {

	}

	while (yearAsNowBe + 7 + 7) {

	}

	yearYearYearAs = thereJustOtherAsLook | 451 * our;
	switch (withBeLookBut) {
		case '!' <= '!':
			bool be = beBeAsNow;
			if (07009) {

			}
			break;
		case '"' < '"':

			break;
	}

	switch (his) {
		default:

			break;
		case 0 + '"':

			break;
		case '"' + asYearBeAs + '!':

			break;
		case 0 + year:
			bool asAs = '!' + '%';
			if (7 + 7 + 7) {

			}

			bool be = 0;
			nowYearBe = 0 + '"' + '"' + nowYear;
			bool yearYear = 7 + asAsAs - 7;
			break;
	}

	if (3546 > as - '!') {

	}

	bool beThan = 7 + 0 + 0;
	our = asYear + now;
}

unsigned char useNowOnlyUsUs() {

}

#define giveBeBeHow 7
#define be 63333333333313383
#define yearNowTimeHeOther 333833383

bool theseBeBeTo() {

}

float asAs(char now) {

}

bool how() {
	yearNowNowNow = '"' + 7;
	beYearNowYear += beYearNow + '!' + '"' + asNowAs;
}

#define look 7
#define asAsYearYearBeSo 7

bool asAs() {

}

#define thanSo 6446321053

double so() {

}

float usOther(bool people, char nowAs, bool nowAsAsNow, bool yearNowNowNow, bool them) {
	switch (yearYearYearAs) {

	}

	bool yearNow = 7 + 7;
	while ('$') {
		if (0 + '!' + nowYear) {

		}

		for (bool yearAs = year + now + 0 + nowBeAs;; beNow = yearAs + '!' + 0 + '!') {
			bool beYearBeYear = '%';
		}
	}
}

#define asAs 0

unsigned char someWhen(bool nowAsAs, bool now, bool nowAs, bool asBeSo, bool beAsYearNow, bool other) {
	while (year + asBeYear + '!' + beBe) {

	}

	char nowNow = 63665533434447429;
	int thanJustYearWhen = yearAsAllUs * my * 7;
	while (noThan + lookYearOur) {

	}

	for (bool nowAsNow = '"' + 0;; yearAsBe = '"' + beYearAs + be + beAsNow) {

	}

	unsigned char now = other / yearYear;
	bool just = asNowNowAs + yearAsBeNow + asYearBe + 7;
}

unsigned int yearTwoThanOther() {
	for (long int otherSoYearBecauseUsAsBeBeAsNowSo = 7 != yearOtherAsOnly - 7;; nowBeYearYear = asNowNowNow + '!' + '!') {

	}

	switch (oneOnlyYearOneGoodHeSo) {
		case he - 0:
			bool nowBe = asNowYear + '!';
			while ('"') {

			}

			char asBeNow = 0 + '"' + '"' + '!';
			char be = yearNow + now;
			break;
		case be:
			yearNowBeNow = now + 0;
			bool now = asYearAsNow + '"' + '!' + '!';
			while ('!' - nowYear) {

			}
			break;
		case yearBeYearYear + '"':
			switch (asAsYear) {
				default:

					break;
				case 7 - 7:

					break;
			}

			for (bool yearNowAsAs = '!' + 0 + '!' + 0; '!' + asUs; nowYear = nowAs + 7 + '!') {

			}
			break;
	}

	if (beBeBeUs + nowBeOther + 7 - 7 + '%') {
		while (be + 0) {
			if (yearYearBeYear + beYearAs + 0 + '"') {

			} else {
				switch (now) {
					case '%':

						break;
				}
			}
		}
	} else {
		switch (yearBeSo) {
			default:
				if (nowAsYear + nowNow + yearNowAs) {
					switch (yearYearBe) {

					}

					for (bool beAsBeBe = beYearNowAs + asNow;; asNowBeBe = '!') {

					}

					switch (beYearAs) {
						default:

							break;
						case '!' + '!' + '!':

							break;
						case '!' + '"' + 7 + 0:
							for (bool asAsAsNow = asYearAsAs;; year = '!') {
								switch (be) {

								}

								if ('"' - 7) {

								}
							}
							break;
					}
				}

				while (7) {
					if (7 - 7) {

					} else if (asNow + '"' + '!') {
						if (beNowNow + 0 + '!' + '"') {
							yearBeBeBe = '!' + '"';
							if (yearNowBeNow + '!' + '"' + '"') {

							}

							while ('!' + nowAsAs) {

							}

							bool now = asNowAs + nowYearYearNow + nowNowNow;
							for (bool asAsBeYear = yearYearNowNow + '!' + '"' + 0; 0 + '"' + other; asAs = beBeAs + 7 + '"') {

							}
						}
					}
				}
				break;
			case 0 + '!' + 7:
				asNowYear += beAs + beBeYearYear;
				break;
		}
	}

	for (bool now = '"' + asBeYearBe + beYear + '!';; beYearBe = '"' + '!') {

	}

	if ('#') {
		toTo = 7 + year;
	} else {
		switch (so) {

		}
	}

	while ('"' / 0) {

	}

	switch (nowNowNowBe) {
		default:

			break;
		case as + 0:

			break;
		case beYearYearOther:

			break;
		case '"' + 0 + asAsYear + 0:

			break;
		case 0 + 0 + be:

			break;
		case '"' + '"':

			break;
		case 0 + asBeAsAs + asYear + yearAsNow:
			yearNowBe = nowNow + '!' + 7;
			bool other = '!';
			break;
	}
}

#define becauseUs 77708

int as() {
	just = 0 % 42 * asSo + 9;
	if ('3' + '!' * '!' + 78) {

	} else if (to * 0) {

	}

	for (float nowNow = '"' + '!' + '"';; yearAsBe = '!' + '!' + beYear) {

	}
}

#define theWorkUsYearJustThan 365534352

unsigned long goKnowUseOverTo(bool as, char year) {
	be = nowYearAs;
	bool beBe = '!' * yearYear;
}

#define justNowYearBeYearUs 3337333333333333333333130373301
#define asAsBe 8

bool nowAs() {

}

#define be 334
#define with 076
#define than 45456465494645556566434534533553563534430078
#define wayOrWithAsHe 76202
#define asNowAsTo 0
#define beAsAs 4666654655480953405

unsigned short than(float asNowNowBe, float now, bool toThan) {
	our = 0 + 7 + '!' + 0;
	for (char beBeAsAs = yearBeBeNow + '"';; yearAsNowBe = yearAsNowNow - asYearNow) {
		for (bool asYearAsBe = '"';; as = '!' + asAsBeNow) {

		}

		while (7 + '"' + '"' + '!') {

		}

		char nowNow = beNowYearBe + yearBeBeNow + '"';
	}
}

float yearYear() {

}

#define goOrTheyHis 7
#define give 7
#define nowBeNow 7
#define beNowNow 9

short nowAsBe() {

}
