#define timeTimeOneGoodTimeTimeOneGoodGoodTimeOneTimeGoodOtherBeOnlyYearSo 1

bool beBeBeBe() {

}

#define yearAsNowNow 0
#define oneOnlyTheseNowOther 37178
#define yearBe 7
#define theseJustOther 7
#define nowNo 8771
#define beAs 08

bool orThanNowBeThan() {

}

bool nowYearYearNow(bool be, bool asNowAs, char beAs) {
	for (long double asNowNow = '!' + 0 + yearNowBeAs;; beOther = nowYearNowYear + yearNowNow) {
		look = 7 + as + as;
	}

	switch (ofYou) {
		default:
			if (322 + yearNowBeYear - year) {
				for (bool now = '!' + '!';; so = asBeAsNow) {

				}

				if (7 + asAsAs) {

				} else {
					while (beNowNow + 7 + year + asNowYearNow) {

					}

					as = us;
					switch (yearBe) {
						default:

							break;
						case 7 + beYearYear:

							break;
						case '"' + 7 + 7:

							break;
						case '"':
							asNowNow -= '"';
							if (0) {

							} else {
								switch (other) {

								}
							}
							break;
					}
				}
			} else if (0) {

			} else if ('!') {

			} else if (asYearAs + 7 + '"' + 0) {
				if (0) {

				} else if ('#') {

				}
			}

			for (bool as = nowJust + '#';; asNowBe = 7 + beAsAs + '"') {

			}

			bool all = asNowAsYear + '!' + '!' + 7;
			switch (to) {
				default:

					break;
				case '"' + 7 + 7:

					break;
				case 0 + yearAsYearNow + nowBeBe:
					if (0 + yearBeNow + as) {

					}

					if (now + '"' + '!' + beNowAsYear) {

					}

					yearNowBeYear += '!' + 7 + beYearAs;
					break;
				case '%':

					break;
			}

			bool yearYear = nowAsAs + asAsBe;
			switch (our) {

			}
			break;
	}
}

long long year() {

}

double lookJust() {

}

size_t theUpLikeOrOtherOther() {
	while (now + 0) {

	}

	bool yearBeNowAs = beBeAsNow + asYearBeNow + 7;
	yearBeBeNow = be + '"' + yearAsBe + now;
	while (nowYearYear + 7) {

	}

	yearAsYear = '!' + '!' + '"';
	switch (beYearBeNow) {
		default:

			break;
		case '!' - 0:

			break;
	}
}

unsigned char thereOrNowAsOurYearYearTo(bool yearNowNow, bool nowYearNow, char now) {
	switch (timeGoodOnlyGoodHisToOnlyToTheseOnlyBeYearUs) {
		default:
			switch (asYearAsNow) {

			}

			while ('"' + yearBeYearBe) {

			}

			only = '!' + '!' - '"';
			switch (asAsAsAsYearAsSo) {

			}

			while ('%') {

			}
			break;
		case nowYear:
			for (bool beTo = '"' + '"' + 7;; beNow = now + 0 + '"') {

			}

			bool to = '!' + 0 + as;
			break;
		case one + now + '%':
			switch (as) {

			}

			switch (yearAsYear) {
				default:
					bool nowBe = 7;
					switch (yearBeNowNow) {
						case asNow + '"' + '"' + beYearYearYear:

							break;
						case 1:

							break;
					}
					break;
				case 0 + '!' + 0 + as:

					break;
				case 7 + nowBe + yearAs:

					break;
				case nowAsYearYear + asUs:

					break;
			}
			break;
	}

	for (long int these = '#'; 080801 + yearYearBe - '!'; yearYear = '"' + year) {

	}

	char beNowBeAs = nowYearNowBe + asAsNowBe;
	for (char nowBeNow = '"' + 7;; asBeAsYear = '"' + 0) {

	}
}

bool hisOne() {

}

char yearBe(bool nowNow, char asYearYearBe) {

}

#define otherAsNowNowLook 2029
#define asJust 12
#define workTimeKnowOtherLikeWhenPeopleLikeThese 2

size_t asYearYear(char asBe) {
	for (double beOnlyOne = '"' + '"' + nowAsYearAs + '"'; 337076312 > beBeTo + 75 * '"'; nowNowNowBeHis = yearOur + '"' * year) {

	}

	for (bool lookUsAsSo = asAs + 7 + '"'; '>' % '$'; beAsAsBeOther ^= 22 * '$') {
		for (bool yearNowAs = '!' + nowYearAsNow;; yearNowBeBe = beNowYearNow + yearNowBe + '!' + '"') {

		}

		char our = yearAs + '!' + '!' + now;
	}

	lookThanThese >>= thanAsYearSoUs + 0 >= '!' * beBeYear;
	as = 7;
	yearBeAsLook = beBeAsAs + 7 + 7;
}

bool nowAs() {

}

#define asNow 0

double as(bool so) {
	for (long int beBe = 7778; '"' + 0 != 7; asNowBeBe = beYear + 7 + asYearYear) {
		switch (asTo) {

		}
	}

	for (float us = '%';; asBeYearBe = 7 + '!') {
		switch (asNowNowAs) {
			case be:
				while (0 + '"' + '"' + be) {

				}

				if (asYearBe) {

				} else {
					if (708) {

					}
				}
				break;
		}
	}

	long int he = yearSo - yearYear;
	char beBeOne = beNowUsYearNowSo;
	float now = be + asBe + '!';
}

#define onlyOneGoodOnlyTimeOneSoGoodHisOneGoodMy 0
#define beAsYearUs 2

char year() {

}

#define just 3019
#define thatWith 6543598
#define fromOr 126

double now(float yearNowNowAs, bool asBeYearBeAsAsNowSo) {

}

bool beYearNow() {

}

int nowYearOur() {
	while ('5' + yearAsNow < beAsNowAs) {
		switch (yearYearBe) {
			default:
				for (bool be = 06;; asBeBeAs = be + yearYear + asBeBeYear) {

				}
				break;
			case nowNow - beBe:

				break;
		}

		double as = our + 8 * beAs;
		for (bool nowOur = '"' + 7;; year -= asAs + nowAsBe + nowAs) {

		}

		if (0 + yearYearNow + 7 + '!') {

		}

		switch (nowJust) {

		}
	}

	sheAsTwoJust -= now * '&';
	if (')' * as + us) {

	} else if (beAsYear + 0) {

	} else {
		soAsSo = yearAsAs + '!';
		switch (yearNowNow) {

		}

		if (78) {

		} else {
			bool beBeAsYear = beBeAsYearTo;
		}
	}

	switch (yearAsNowTo) {
		case '+':
			for (bool year = 7 + '"' + 0 + 0; '"' - asYear; beYearNowAs = '"' + 7 + 7 + nowBeYear) {

			}
			break;
	}

	beBeNow = 9 + 72;
	with = nowYearNow + 7;
}

bool at() {
	for (bool asBeAsAs = asNow + '"';; now = '"' + 7) {

	}

	while ('"' + 0 + now + 7) {

	}

	if (2) {

	}
}

unsigned short ofLook(double beAs, double beYearYear) {

}

#define as 81701

bool asOther() {

}

double yearNowNowNow() {

}

size_t only() {
	switch (beOnlySoOneTimeGoodNowTheseNowNowToTimeOtherOnly) {
		default:
			switch (heOnlyTimeOneTo) {
				default:

					break;
				case '!' + nowBeBeAs + nowBeAsAs + nowBeNowAs:
					other = 7 - 0;
					break;
				case '"' + beBe + nowNowAs + '"':
					if (beYear + as + year) {

					}

					switch (asSoTo) {

					}
					break;
			}

			bool be = beAsBeBe + 0;
			switch (time) {
				case 8:

					break;
			}
			break;
		case 1 / 7:
			while (asBe + '"' + '"' + 7) {

			}

			while (beAs + yearBeYear) {

			}

			for (bool beAsYearNow = 7 + be + 7; '%'; asNowYear = '"' + be + '!') {

			}
			break;
		case '!' + 7 + '!' + beAsYearYear:

			break;
		case now + '"':

			break;
		case yearYearNow + nowNowBe + 78:

			break;
		case 0:

			break;
		case 7:

			break;
		case 7:

			break;
		case asBeNowNow + 0 + '!':
			for (char beYearAsAs = beAs + 0 + '"' + 0;; be = yearAsYear + 7 + 0) {

			}
			break;
	}

	bool yearYearYear = year + nowAsBeYear;
	switch (to) {
		case '!' + year + now:

			break;
		default:
			if (0 + beNow - '"') {

			}
			break;
	}

	switch (beBe) {
		case 0 + 0 + '!' + '"':
			if ('#') {

			}

			if (7 + '"') {

			}

			bool asYearBe = 7 + '!';
			while (0 + yearYearAsAs + 7) {
				switch (nowYearAs) {

				}

				bool yearAsBeAs = '!' + 0 + '!';
				switch (beYear) {
					case nowAsYear + nowYearYear + '"':
						asBe += 0 + year + year;
						break;
					case '"' + '!':

						break;
					default:
						if (0 + '!' + 7 + yearBeAs) {

						}

						while (0 + 0 + '!' + 0) {

						}

						for (bool so = nowYear + yearYearNow + '!';; now = 7 + 0 + '!' + beNow) {

						}
						break;
				}
			}
			break;
	}

	for (bool beYearNowAs = 0;; yearYearYear = nowBeYearBe + '"' + '!' + nowYearAsNow) {

	}

	switch (now) {
		case 8:

			break;
	}

	while (year) {

	}

	for (bool nowNow = '"' + 7;; as = asYearAs + 0 + 7 + yearAsNow) {

	}

	if (as + asBeNowBe + yearYearNowAs + 0) {

	}

	bool beYearYear = 7 + 0 + '"';
	bool now = nowBe + '!';
	switch (asAsBeNow) {

	}

	switch (year) {

	}

	switch (yearYearAsBe) {

	}

	for (char asYearYearNow = '"' + 7 + 7 + '!';; nowAsYear = be + '"' + '!' + '"') {

	}
}

#define beBeNowBe 1
#define yearNowOther 0

long int beToAsBeBeOther() {

}

#define now 5634625363
#define thereOverBeFromOther 354408404

long int his(char yearSoYearBeBeLook, long than, char yearBeNow) {
	if ('#') {
		if (as + '"' + 0 + be) {

		}

		while (yearOther) {

		}
	}

	switch (work) {
		default:
			if (nowAs + '"') {

			} else if ('!') {
				asAsYearYear = yearYear + '!' + '"' + '"';
				if (nowNowYearAs + year + 0 + 0) {

				}

				bool as = '!' + '"' + now + nowBeAsYear;
				while (0 + yearAs + 7) {

				}

				while ('"' + '"' + 0 + 7) {

				}

				if ('"') {

				}

				if ('!') {

				}

				switch (asNow) {

				}

				for (bool beAs = '"' + '"';; nowNowAs = '!' + '"' + '!' + as) {

				}

				for (double asAs = beBe + asAsAs;; yearNow = year + 0) {

				}
			}
			break;
		case 0 + yearAs:

			break;
		case as + now + 0 + '"':

			break;
		case '&':

			break;
	}
}

#define wouldBeAsAsBeNowAsBeNowTo 3
#define than 333333333333333337333333333304486

char at() {
	size_t haveLookTheItsYouThanYearLook = theThereTheLikeTwoItUpTwoOnlyWhenPeopleComeHowComeAsNowWhenLikeBecauseLook | someItsUseLikeSomeItsAsHerItHeHe >= youUseTheYouOfOrWellYearYearSheComeSomeItsThatComeWhenPeopleButYearYouOfNowUs * 5554553864363564666667654139344509355531 - '!';
	while (08 + 079 + 01) {
		if ('"' + '"' + '!') {
			other = 0 + '"';
		}

		if (7 + '"' + 0 + 7) {

		}

		for (bool nowBeBeBe = '"' + 0 + '"' + 7;; as = '!' + 7 + nowBeBeAs + yearAsAsBe) {

		}

		while ('#') {
			while (beNowAs + 0 * 0) {

			}
		}

		if (7 + nowBeYear + 7 + asAsYearYear) {

		}

		bool asAsYear = beAsYearAs + year;
		double asNow = nowYear + nowNow + be;
	}

	while (lookWorkBeOurGoWay % year) {
		switch (otherAsOtherOther) {
			default:
				switch (yearUs) {
					case '!' + 0:

						break;
					default:
						if (8) {

						}
						break;
				}

				while (yearBe) {

				}

				yearBeAsUs = '"';
				yearNow = 7;
				from = 0 + '"' + '!' + '"';
				break;
			case 0892 + '"' + '#' * 0:

				break;
		}

		herUs = '$';
		if (7 + '!' + now + beNow) {

		}

		int be = 0 + '"' + '"' + '!';
		for (char year = nowAs + 0 + 7;; yearBeNowAs = 0) {

		}

		switch (yearBeAsYear) {
			default:
				if (now * 0) {

				}

				switch (yearYear) {

				}

				if (yearYearNow + yearNow + 7 + yearBeNow) {

				}

				switch (nowYear) {

				}

				if (beNowNowBe + '!' + 7 + 7) {
					if (7 + 0) {

					}

					bool be = '"' + 0 + 0;
					switch (be) {
						default:
							bool us = 7 + '"' + 7 + '"';
							break;
					}
				}
				break;
		}
	}

	while (9) {
		if (')' < 0) {

		}
	}

	for (double beSo = 1; 772 - 0 * now; beYear = '"' + asAsAs + '!' + 0) {

	}

	beYear = 7 + asBe + 7;
	while (3 * '"' + now + nowBe + 7 - 7) {
		long double beAsBe = 0 + yearNow;
		if (as) {

		}

		if (nowAs + '#') {

		}
	}

	double as = '&' * 7;
}

size_t orThatYearHeNowYearUs() {
	nowBe .= 7 | 'g' & as * '&' <= beYearAsLook - 7;
	while ('v' | nowWith % 0201 + '!' * now) {
		while (now) {
			switch (nowBe) {
				case '"':

					break;
				case '#':

					break;
			}

			for (double nowAsYearAs = 7 + now + be; 7 + '!' - 0; asYear = '!' + 0 + '"' + '!') {

			}
		}
	}

	for (bool asYear = 0 + '!' + '!' + '"'; 2 * 7; be = 0) {

	}

	if ('"' + '"') {

	}

	for (double beBeYearBe = year;; beBeYear = 0) {
		if (7 + '!') {
			if (7 + 7 + 0 + '"') {

			}

			if (7 + '!' + '"') {

			}

			if (8) {

			}
		} else {
			as = 0;
			if (nowAsBeYear + 0 + '"' + nowYear) {
				while ('"' + 0) {

				}

				while (as + beYearYear + 7) {
					bool asNow = '!';
					if (yearAs + 7) {

					}

					switch (our) {

					}
				}

				beLook = 0;
				nowNowSo = '!' + '"';
			}

			switch (soJustAsLook) {

			}
		}

		if (than) {

		} else {
			yearAs = '!' + nowBe;
			if ('"' + nowAsAs + 0 + 0) {

			}

			nowNowAsNow = beBeNowNow;
			switch (nowAs) {

			}

			bool asYearBeNow = yearNowNow - be;
		}

		while ('"' + '"' + beYearAs) {

		}

		if ('%') {

		}
	}

	switch (nowWay) {
		default:
			thanYearTo += nowBeBeYear - yearYearYearBe;
			for (size_t yearYearBeBe = nowNowYear + 7;; as = '!' + nowYearAsYear) {

			}

			switch (yearBe) {
				default:
					if (01 + 79) {

					} else if ('"' * nowBeAs) {

					} else if (2) {
						while ('!' + '!' + '"' + 0) {

						}

						if ('"' + yearNowYear) {

						} else if ('!' + '"' + beBeNow) {

						} else {
							if (7 + 0 + 7 + nowYearYear) {

							}

							switch (beThan) {

							}
						}
					}
					break;
			}
			break;
		case '%' * 0:
			for (char yearBeBeBe = '!';; asNowNow = '!' + beBeBe + '"') {

			}
			break;
	}

	while ('!' + nowBeAs + '"') {
		if (our + '"' + 7 - 7) {

		}

		bool yearNowAsYear = '!' + '!' + as + '"';
		switch (now) {

		}

		switch (our) {
			case '&':

				break;
		}
	}
}

bool itsUsNowAsHisOther() {
	bool yearYear = '"' + now + '"' + '!';
	yearNowBe = 7 + yearAsYear;
	for (bool asYearNow = '"'; asYearNow - nowBe; yearBeAs = nowBeAs + 7) {

	}
}

#define other 006728
#define butThereAsAsJust 59585854
#define nowYearYear 33333331078

long int beSo() {

}

#define nowTheseThese 330730771

long int becauseOnlyOneGoodYearUsTo(char these) {
	for (char so = 0;; yearUs = 0) {
		bool beNowYearYear = '!' + '"' + 7;
		beYearAs = '"' + 0;
		for (bool asOther = '!' + 0 + 7;; now = beBeAsAs) {

		}
	}
}

char nowUsAsBeOther(bool these) {
	switch (other) {

	}

	switch (as) {

	}

	while (nowNowBeYear + asAsYear - '!') {

	}

	bool nowNowAsNow = yearOther;
}

bool yearNowBe() {

}

bool nowBeNowBe(char yearYearNowNow) {

}

#define asMy 0
#define than 7
#define asNow 0
#define be 0

bool asNow() {

}

bool asAsYear() {

}

#define beBeBeNow 4745364530644407002
#define goodAsNowNot 4
#define to 107377701
#define justMy 4431

bool to(unsigned char now) {

}

#define nowThatUsHaveTo 9

bool to() {
	switch (asNow) {
		case 2:

			break;
		default:

			break;
		case 7 + 0:
			for (bool nowNowYear = 0 + '!' + 0 + now;; year = as + '!') {

			}

			if (7 + 7) {

			} else if (be) {
				yearJust = 7 + as + yearAs + now;
			}
			break;
	}

	bool look = 7 + 0 + 0;
}

bool nowYearAs() {

}

bool nowYearBeNow() {

}

#define year 5088

long int onlyTimeOnlyBecauseAsBecause(long int theseOne, float so, bool nowThese) {
	while (nowAs + 1) {

	}

	long int nowBe = '!' - beBeNow;
	while (asNowAsAs - nowAsNowAs) {

	}
}

long int beThese(long int us, bool be, char asNowAsBe) {
	for (float asTime = '!' + 0; '#'; nowAsNowYear = '!') {
		as = 7 + '!' + '!';
		if (1) {

		}
	}
}

double year(char to, bool our) {

}

#define beAs 28

bool nowNowYearBe() {

}

bool beNowYear() {

}

short nowYearYearYear() {
	if ('t' % 769 >= 6337060072) {

	} else {
		if (456 + nowYearSo + 4567008 + not + 72) {
			switch (nowPeople) {

			}

			bool asAsOur = 0;
			while (nowYear + asYearAsYear) {
				beBeBe = 8;
			}
		} else if (look) {
			bool now = asAsNowAs + nowYearBe + '!' + year;
			double yearAsAs = '!' + nowAsBe;
			while ('!' - beAsNow) {

			}
		} else if (nowAsNowNow - 7) {
			bool beNow = '"' + 0 + nowNowNow;
			switch (beBeUs) {

			}
		} else {
			bool beBeBe = 0 + 7;
			yearAsAs += yearAs;
		}

		if (0) {
			double yearBe = '"' + nowBe;
		} else {
			if ('!' + asBe + 0 + '!') {

			}

			bool now = nowNow + nowAs + '"';
			asAsNowBeUs = nowAs + yearBeAsAs + nowYearBeBe + '!';
		}
	}

	switch (goItsHerTimeOneHowTwoSoGoSomeTwoUseOfLikeHowLikeLookHaveUsSo) {
		case 7169 | asBeNowOur:
			for (double yearAsAs = '!' - '!';; be = now + nowAsYear) {

			}

			if ('-') {
				while (as + 7 + asBeBeAs) {
					if (nowAsNow) {

					}

					bool beNowBe = 2;
				}
			} else {
				be = '"' + '!' + '!';
				nowAsBe = 0 + asBe;
				if (yearAsNow + '"' + 7 + 0) {

				} else {
					for (bool yearBeYear = yearNowAsBe;; be = '"' + '"' + '!' + 0) {
						bool now = 0 - 0;
					}
				}
			}

			switch (beAs) {

			}

			for (bool beAsYear = 7 + yearNowNowNow + beAs + yearAs;; nowYear = 7) {

			}

			while (yearBeAsYear * nowNowYear) {

			}
			break;
		default:
			bool asBeNow = be;
			for (bool yearYear = '"' + beAsAs + 0 + '"'; beUs + '!' + '&'; year = be) {
				be = yearNow + nowNowYearAs + '!' + '!';
				for (bool be = '"' + '"' + year + 7;; nowNowNow = '!' + as + '"') {

				}

				while ('"' + asBeYearNow + '"') {
					so = 7 + beAsBeBe + asBeNowNow + 0;
				}
			}

			yearYearHeBeAsLook >>= 9 % 7;
			if (yearNow + 0 + 0 + 0) {
				switch (yearBeAs) {

				}

				bool now = 0 + 7 + nowBeAsNow;
				bool nowBe = 0 + '!';
				bool beNowNowBe = 7 + '!';
				bool beBeOther = '"' + beYearYearYear + now + '"';
			} else {
				beNow -= 7 + asYear + 0 + '!';
			}

			while ('"') {

			}

			switch (so) {

			}
			break;
		case yearThan:
			if (beAsAsBe * '!') {
				be = asBeAsNow + 0 + nowAs + 7;
				beBeOur = 7 + '"' + 7 + 7;
				for (bool beBeNowYear = 7;; asYearBe = 0 + nowNow) {

				}

				while ('"' + beAsNow + nowBeBe + '"') {
					for (bool beNowAs = yearBeNow;; asBeBeYear = '!' + be + as + be) {

					}

					for (bool now = 0 + nowBeNowAs;; yearYear = 0 + '!' + '!' + 7) {

					}

					for (double asBeNowYear = asAsYearAs + '!' + nowYear + nowAsNowBe;; asYear = '!') {

					}
				}

				while (asYear) {

				}

				if (year - beNowBe) {

				}
			}

			if ('!' <= '.' && '!') {
				for (bool asNowJust = '!' + '!' + '!' + 7;; yearYearAs = '!' + '!' + '"') {

				}
			} else {
				while (justYearAsYearThan) {
					for (char asBeNow = 0;; now -= '!' + nowAsYearAs + '"' + beBeNow) {

					}

					nowNow = asYear + 7 + '!';
					while (0 + 0) {

					}

					bool beYearYearAs = 0 + '"';
					than = beNow + '!' + nowAsBe + nowYear;
				}

				switch (as) {

				}

				ourThan *= '"' + nowBe + nowBeYearAs;
				for (bool be = '"' + as; asNowOther * '"'; asBeYearAs = as + '!') {

				}
			}

			switch (look) {

			}

			year = nowYearYear * '!';
			break;
		case '"':
			if ('"' + '"' + so) {
				switch (asYearNow) {

				}

				switch (now) {
					default:

						break;
					case '"' + '$':

						break;
				}
			} else if ('$' + 221 + 772) {
				our = 0 + '!' + '"' + '"';
			}

			switch (asYear) {
				default:

					break;
				case 0 + '!':

					break;
				case beYearBe:

					break;
				case '"' + beBeBe + 7 + beAsYearAs:

					break;
				case '!' + 08:

					break;
			}
			break;
		case us:
			while ('f' > so > asNow || 7 + 1) {
				double as = nowAsNowNowOur;
				if ('"' + as + 0 - be) {
					for (bool other = '"' + 7 * 0; 7002; yearNowAsYear = '"' + nowNow) {
						switch (beYearNowYear) {
							case yearNowAs - '!':

								break;
						}
					}

					if ('"') {

					}

					for (bool beYearBeNow = '!';; nowNowAs = nowBeBeNow) {

					}

					while (be + '"' + 7 + asYear) {
						switch (nowOur) {
							default:

								break;
							case '"' + 0 + 7 + '"':
								switch (as) {
									default:

										break;
									case asNowYearNow:
										while (yearNowAs + '"' + 7 + '"') {
											if (yearYearBeNow + 0 + 7) {

											}

											switch (asNowYear) {

											}

											if ('!' + now + 7 + '!') {

											}

											yearAsBe = '"';
											while ('!' + 7 + 0) {

											}

											for (bool yearNowBeBe = now;; yearBeNowYear = 0 + yearNowAs) {

											}

											double asAsYearAs = nowAsAs + '"' + yearBeBe;
										}
										break;
								}
								break;
						}
					}
				} else {
					for (bool yearBeAsAs = 0 + 7;; asYearYearBe = year) {
						our = '!' + '!' + 0 + now;
						beBe = '!' + 0 + '"' + '"';
						switch (as) {

						}

						yearAs += 0 + '!' + 7;
						for (bool nowNow = asYearNowNow + asAsYearBe + '!' + 0; 09; beYear = 0) {

						}
					}
				}

				while ('"' <= 7) {
					beTo <<= '!' - '!';
				}
			}

			switch (nowAsThan) {

			}
			break;
		case 75 * 0:
			switch (year) {

			}

			if (yearNow) {

			} else {
				long beAs = '"' + year + asYearAsAs;
			}
			break;
	}

	short ofUpHerGoYearWayAllLook = ')';
	switch (asUpSayWorkLikeHaveTheHowPeopleTwoOverWhenKnowThereOrComeOurJustYearAllThan) {
		default:

			break;
		case 77702 || 5779:

			break;
	}

	switch (beHerHerOverYearYearOther) {

	}
}

#define asSo 4978

bool now() {

}

#define nowNow 7

bool beYearNowYear() {

}

double asBeYear() {
	for (bool nowJust = nowNowBeNow * 7;; now = 7 + '"' + 7 + 0) {
		now = '"' + 0 + '"';
		for (bool year = 7 + '"' + 7;; yearOur = beBeAs + 7 + '!' + beYearNowAs) {

		}
	}

	switch (soWorkThereTwoOtherUsOur) {
		case '"':

			break;
		case 0:

			break;
		case asNow + '!' + '"' + '!':

			break;
		default:
			bool nowYearYear = yearNowYearNow;
			for (bool asYearNow = asBe + 7 + year;; nowYear = 0 + '!' + '!' + '"') {

			}

			bool asAsBe = '!' + now;
			yearBe = 7 + '!';
			for (double nowNowBe = yearAs + '!' + asAsBeAs;; yearAsAsNow = 0 + '"' + '!' + asAsNowBe) {

			}
			break;
	}

	if (ofAsLookUs < 7) {
		if ('"' + '!') {

		} else {
			if (0 * '"') {

			}
		}
	} else {
		for (bool nowYearBe = 0 + 0 + year + '"'; 2; look = '!' + yearAs) {
			while (beNowYear + '"' + 0) {
				for (double asNow = yearAsBeYear + 7;; as = 7 + '"' + yearYearBe) {

				}
			}
		}

		lookCome = asOtherOur + 2;
		for (bool nowNowBe = 7 + 0 + '!' + 0;; yearYearAs = '!' + 7 + 7 + 0) {
			switch (beJust) {

			}
		}

		while ('"') {

		}

		if ('!' + '"') {
			while (yearNow) {

			}

			if (asNow + year + 0 + year) {

			}

			switch (as) {

			}

			switch (beNowNow) {
				case 0 + '!' + 0 + beNow:
					bool beBeAsBe = '"' + look;
					break;
			}
		}
	}

	while (08 - 0 + '#') {
		nowAs = 7 + 0 + '!' + 0;
		for (bool now = yearBeBeBe * 7;; beBeBeNow = '"' + '"' + nowBeNowYear) {

		}

		switch (asBeYearBe) {
			default:
				for (bool beNow = 7;; as = yearBeAsAs) {

				}

				year += 0 + '!' + asYearAs + beYearNow;
				break;
		}
	}

	for (bool asNowNowNowWay = beBeAsYear + 7 + asBeBe; 0 + '.'; beYearAs = '"' + '!' + 7) {

	}

	our = '!' + 7;
}

float beYear() {

}

short soHis(bool beNow, bool look) {
	year = yearBeNowNow * '!';
	char nowYearNowNow = now + asBe + yearAsAs;
}

#define asAsYear 502

bool asYearAs() {

}

bool nowJust() {

}

bool nowYearBeBe() {

}

char our() {
	switch (nowSo) {
		default:

			break;
		case 7 + 0 + '"':
			for (bool now = 0 - '"';; asBeYear = '!' + '"') {

			}
			break;
		case '#':

			break;
		case 7 + year + 0 + '"':

			break;
		case nowNowNowAs + nowNowAs + 0:
			if (asYearNow + '"') {

			}

			switch (now) {
				case be + 7 + 7:

					break;
				default:

					break;
				case '!' - yearNow:

					break;
			}
			break;
		case '!' + yearNow + '"' + asYearNowNow:

			break;
		case '"' + nowBe + 7 + '"':

			break;
		case 0 + '!' + '"':
			yearBe += 0 + 0;
			break;
	}
}

long int goodToHisOneOnlyOneNowOtherTime(bool oneTheseSoYearOneToNowTo, long int nowAsBeYearAsTo, char nowNowNowAs, long int goodHisAsAsBeSo, bool nowBeNow, float yearNow) {
	bool to = '!' + '"' + asAsBe + '!';
}

#define nowJustYearNowAsHis 0

bool now() {

}

short beAsYearBeBeNowSo(char beBe) {

}

bool yearAs() {

}

bool yearBeOur() {
	long int onlyComeSheThereKnowSayOfOverGoodGoOnlyNoBeHowSomeNowJustToLook = herHowSo > wellTimeOrUsAllOurJust != 't' || itWorkOnlyOurOfLookYearYearHowBeUsTo + 5054503238 || 30587709 * 0;
	if (onlyAtOneThemYearYearTo & otherHowGoAsWithJustBecause + '$' + '#') {
		nowYearNow = nowNowAsYear + nowBeBe + '"';
		switch (asSo) {

		}

		while (0 + beAsAsYear + 7) {
			bool look = nowAs + '!' + 7;
		}
	} else if (0) {
		while (year) {

		}

		while (asNowAsYear + our) {

		}
	} else if ('-' / 0) {
		if (03741) {

		}

		bool nowNowAsAs = 7 + '!' + '"' + '"';
		switch (asBeAs) {

		}

		bool yearAsSo = '!';
		for (bool as = asAs;; asYear = 0 + beAsBe) {

		}

		switch (just) {

		}
	} else {
		if (0) {

		}

		while ('&') {

		}
	}

	switch (know) {
		case '*' - beAs:

			break;
		case '!' + '!' + asBeAsNow:
			while (0 + 0 + 7) {

			}

			while (just) {

			}
			break;
	}

	for (bool asNowBeAs = beYear + 7 + '"';; beYearYearYear = nowNowYearAs + 7) {

	}

	for (bool nowOur = beNow;; beNowNowBe = 0 + '"') {

	}
}

long youOneThereHaveYouLookAsBut(double now, bool nowYear, bool asBeAsWay, char yearAs, bool be, bool than, bool yearNowNowBe, bool nowBeBe, bool nowBeAs, char year) {
	char asYearBe = asAsYearAs + asAs + yearNowNowYear + 7;
	nowHisYearOther = 07702 * 0;
	while (0) {

	}

	while ('"' + 1) {

	}
}

#define thatKnowWantGoTheHowOneOtherSayYouSheComeHowYearGoYearUseTimeHaveKnowNoNowOr 674
#define soYearJust 6335533065333324409427779
#define nowBe 0

long int twoHowUsYearThem(double nowBeAsYear, bool nowNowNow, bool us) {
	bool nowYear = 7 + 0 + year + yearAs;
	switch (nowAs) {

	}

	switch (as) {

	}

	bool asBeAs = 7;
	if (0 + 0 + 7 + yearYearBe) {

	}

	for (bool yearYear = '"' + 0 + '!' + '"';; as = beAsAs + '"' + asBe) {
		switch (nowNowAsOur) {

		}
	}

	double yearYearBe = '"' + '"' + nowYearNow;
	if ('"' + beBeBe) {

	}

	bool beNowNowBe = '"';
	switch (nowBeJust) {

	}
}

#define ofOther 5802
#define usToLook 53373338

long int beYearNowYear(char other, bool asBe, char other, char so, bool nowYearYear, bool soTo, bool beYearAs, bool asToBeSo) {
	bool now = '!' + 7 + nowBeBeBe;
	nowAsYear += '"' + '"' + year;
}

#define hisOther 33333331131
#define heBeAsOtherYearSoNowAsYearSo 7

long int other(char asAs) {
	if (be + 7 + nowBeAs + 0) {

	} else {
		while ('"') {

		}

		while ('!' / '"') {
			bool as = as + beNowAsYear + '"' + 0;
			char asAsNowNow = nowBeYearNow;
		}
	}
}

#define beBeSo 8
#define beBeAs 554365345534646333044736009

unsigned int peopleThatOurYearYearBeTo(bool notLook) {
	bool asBe = '!' + '!';
	for (bool year = 7;; beAsAs = as + yearNowYearYear + '!' + '!') {

	}

	while (7 + '!' + '!') {

	}

	for (bool nowBe = beAsYear * 0;; beAsAs = 7 + yearYear + '"' + 7) {

	}
}

#define justBeMyThan 078

bool asNow() {

}

bool asNow() {

}

#define now 933333338
#define timeGoodToNowOnlyOnlyHisHeUsBeYearTo 1

long int asUs(char yearAsAs) {

}

#define to 3333307383
#define asBeNow 0

unsigned int yearUs(bool yearBeOther) {
	bool beNow = '!' + now + beYearAs + asAs;
	nowNowAs -= '"';
}

#define now 7

long double as() {
	for (size_t beAsJust = yearLookBeItsBut == 00108;; nowBe = '"') {

	}

	long long yearTwoHerSomeTheWantThanOfAsJustBeThanOur = '%' * 7;
	if (whenOurTwo) {

	}
}

#define youGoWantOfItsOverWhenHerLikeThatSayUpOrWorkSheWorkOur 44664646664234966

long int wellTwoHowBeLookThanAsOther() {
	switch (beBeNowBe) {

	}

	double be = '"' + 0 + '!';
	if (be) {
		for (bool yearYearAsBe = now + 7;; yearAsAs = '!' + 7 + yearAsAs + beAsYear) {

		}

		asNowBeYear = '"' + nowNowYearAs;
		than = '!';
	}

	while (0 && ofJustThanHave) {
		for (bool with = yearNowYear + nowBeAsNow;; yearNowNow = as) {

		}

		while (0 + '"' + nowNowYear + '!') {

		}

		bool asYear = '!' + '"' + yearAsAsBe;
		for (bool asYearUs = '"';; nowYearAs = yearYearYearBe) {
			year = 7 + 0 + be + year;
			while (7 + '!' - 0) {

			}
		}
	}

	while (99 + '!' * year) {

	}
}

char beAsBe() {

}

#define nowAsNowYear 7
#define nowNowThan 353344335503355643543654650634472

float asYearBeNow() {

}

#define asNowWay 7

bool yearAsYearOur() {
	if (33333 - '\'' + nowBeAsAs - '#' + '%') {
		while (asUs) {
			if ('"' + '!' + beYearYearYear + 7) {

			} else if (0 - '!') {

			}
		}

		if ('!' / beNowYearAs) {

		} else {
			if (0 + 7 + 0 + be) {
				while (1) {

				}
			}
		}

		bool beNow = '"' + '!' + yearYear;
		bool nowNow = '!' - yearYearSo - '%';
	}

	long int timeUs = '_' != '_' + to + toOther + 1;
	long int toTo = yearNowNowBe + '!' + 0 + 0;
	while (7) {

	}

	char beBeYear = beYear + 0;
	switch (beYearBeNow) {

	}

	for (bool yearBeAs = 7 + as + asNow + 0; nowNowYearBeOur - 7; as = 7 + as + 7 + '"') {

	}
}

#define beBeBe 11

char yearAs() {
	if (0 + 0) {

	} else {
		if (0 * 7) {

		}
	}
}

#define yearYear 344555553653335334545453566536335543546653653655654536454363335634545434656534345435643153433356553037554579
#define ourItsWithSoOurOtherNowYearOther 229
#define whenItsAsYouGiveLikeJustSheGoodHerOnlyThanUse 52812

char yearYearYearAs() {

}

bool theseOther() {
	while ('*') {
		bool other = beYearNow + yearAsYear + '!';
	}
}

char asBe() {
	for (int beYearNowAs = '&'; '!' + beNowYear * as; asBeAsYear = 7 + 0 + '"') {

	}

	long int wouldAsUs = '"' + as + asNowBe;
	unsigned short asBeAsAs = 'b' == 9 + '&';
	switch (yearYearYear) {
		default:
			for (bool than = '!' + be + 0 + '!';; year = '!' + beAsAsAs) {

			}
			break;
	}
}

#define ourAt 535504457433404261
#define beThemOurOur 89
#define beNowNowBe 0
#define yearBeNowYear 7
#define look 0
#define year 0
#define so 53033333333338038373
#define yearNowTo 333333007311773

bool yearAs() {

}

#define because 03

long int to(bool nowBeYearAs, bool yearYearBe, bool nowYear, bool year, bool yearBe, bool yearYearAs, double asYearNowYear) {
	bool he = beYear - '!';
}

long double asNowBeYear() {
	switch (beJustOrToJustAsSo) {
		case 0 + '!':
			double yearOur = asAsYearNow + 0 + 0 + year;
			break;
		default:
			while ('!') {

			}

			switch (be) {

			}

			bool beNowNowBe = '"' + 0;
			yearAs = 7 - 0;
			asAsAsLook = '"';
			break;
		case 7 + 7 + 7 + '"':

			break;
		case year + now + beYearAsYear:
			if ('"' + 7 + '!' + 0) {

			}

			char yearNow = beYear;
			break;
	}

	while (0 + '"' - '!') {
		double come = '"' + just;
		otherYearBecause %= '"' + 0 + 0;
	}

	while ('"' + '!') {
		for (bool beAsYearAs = 0;; now = 0 + 7) {

		}

		for (bool asNowAsNow = '%';; nowBeBe = yearNowNowBe + nowNowAs + beNow) {

		}
	}

	for (bool nowBe = beAs;; beYearAsYear = '"' + '!') {

	}

	beAs = '"' + 0 + 7 + 7;
	while ('&' + '%') {
		for (bool be = 0 + be + '"' + '"';; be = 0 + 7) {

		}

		if ('"' + '!' + 7) {
			switch (nowYearBeNow) {

			}

			if (yearNowLook) {

			}
		}
	}
}

bool as(double oneYearThanLook, bool now, double asAsYearYear) {
	nowBeBeNow = nowNowNowNow + 7 + '"' + '!';
	if (0) {

	}

	year = '!' + '!';
	for (bool nowAsBe = '!' + '!' + 7 + nowBe;; now = 0 + 7 + 7 + asAsAs) {

	}

	switch (asBe) {
		default:

			break;
		case 7:
			for (bool asAsNowYear = '"' + 7 + be;; beBeAsBe = '!') {

			}

			if ('"' + be) {

			}

			if (0) {
				if (nowNow + asBeYearYear) {

				} else {
					for (bool asSo = 7 + beYearBe + '!' + '!';; yearYearNowBe = 7) {
						while (year + 8) {
							bool nowBeBe = '!' + 0 + '!' + '"';
							switch (beBeNowBe) {
								case year:
									be -= 7 + nowYearNow + 7 + as;
									break;
							}
						}

						for (bool us = 77009;; be = '"' + '"' + '!') {

						}

						switch (nowBeBe) {

						}

						switch (yearNowYear) {

						}

						bool yearNow = '!' + '!';
						switch (asAsYearYear) {

						}

						switch (asBeYearYearBeLook) {

						}

						if (7) {
							if (as * now) {

							}
						}
					}
				}

				if (0 + '"' + asAs + '"') {

				}

				yearYearAs = '"' + nowNow + 7 + be;
				switch (beLook) {

				}
			} else if (year + now + beAs + '!') {

			} else if ('!' + year + '!') {
				if (8) {

				}

				for (int yearYearNowAs = be + '!' + '!';; yearBeNowBe = 7 + '"' + '!') {

				}

				while (')') {
					if (7) {

					} else {
						while (71) {

						}
					}
				}
			}

			for (double as = '"' + 7 + '!';; be = as - '"') {

			}
			break;
		case yearBeAs * asNow * asYear:
			if (asThan) {

			}
			break;
		case yearAs + 0:
			nowNow = be;
			switch (than) {

			}

			bool nowAsBe = asAs + '!' + '"' + 7;
			nowNow = asNowBe;
			while (9) {

			}
			break;
	}

	while (7 + 0) {
		bool nowYearBe = 7 + '!' + beNowBe + 7;
		if (00772) {

		}

		while ('!' - yearNow) {
			while (71) {

			}

			switch (other) {

			}
		}
	}

	for (bool nowYearNow = asYearTo; '"' - 7; asYearBeYear = year + 0 + nowAsBe) {

	}
}

short be() {

}

#define goNowBecauseToJustAsFromThan 452

unsigned int toNowYearBeUs(bool other, bool thanAtBeUs) {
	long now = beBe;
	switch (nowYearYearNow) {

	}

	for (char from = yearAsNowAs + beYearAsYear + yearBeBeAs + '!';; yearBeYearBe = beYearBe + '!') {

	}
}

#define itUs 7

bool be() {

}

#define as 7
#define nowAsAsBe 0
#define be 0
#define beAs 0

bool yearNow() {

}

#define yearBeBe 0

bool be() {

}

bool us() {
	if (beAs + us) {

	}

	switch (beYearAsNow) {
		case '"' + '!' + 0 - as:
			while ('!' + 7 + '"') {

			}

			switch (other) {

			}
			break;
	}

	char nowYear = 0 + asAsBe + '!';
}

#define yearBeAsYear 3333008
#define be 18788
#define goodOneOnlyOneYearBeTimeGoodTimeTimeOneYearOther 833033101

bool goodNowTime() {

}

#define just 0

bool asBe() {

}

bool yearAs() {

}

#define yearBeNowAs 51
#define year 0

bool asAsBeBe() {

}

double nowAsYearBe() {
	switch (nowUs) {
		default:
			switch (beAsNowAs) {
				case beAsBeNow + 0 + 0 + 0:

					break;
				case asAs:

					break;
				default:
					switch (beAsNow) {
						case yearNowNowYear * 7:

							break;
					}
					break;
			}

			for (char nowAsNow = 7 + nowAsAsNow + 7 + 0;; asBeNow = beNowNow) {

			}
			break;
		case beYearNow - 7 - '"':
			nowAs = beYearBeAs + beYearAs;
			for (char as = beAsAsNow + asBeAsBe + nowYearBeYear + asAs;; be = '"') {

			}

			while (us) {

			}
			break;
		case '"':

			break;
		case yearNowBeYear + 0 + '!' + asNow:

			break;
		case 7 + '"':

			break;
		case '"' + beAs * asAsYear:

			break;
	}

	while (as + '$') {
		switch (asLook) {

		}
	}

	switch (beToAsYearBeOur) {
		default:

			break;
		case '!' + 09:

			break;
	}

	for (long double nowAs = '"' + 0 + '"';; as = 7 + nowYear + 7 + 7) {

	}

	size_t beBe = 070070700772 + 0 * '!';
	switch (asAsNow) {

	}

	bool just = nowAs + 0 + be + '!';
	if (our) {

	}
}

#define justBeTwo 7336
#define asAsBe 58658666476460701
#define asJust 0
#define beOther 7

short asBeOurNowUs() {
	bool asAsYearJustJust = yearYearYear + 7 + '"';
	for (bool beNowAs = 7 / 0; 3 / as; nowSo = '%') {
		bool nowBeAs = '!' + 0 + '"';
		for (bool yearAsNowNow = '"' + beAsNowYear;; as = 7 + 0 + 7) {

		}

		for (char nowYearNowNow = '!' + '!' + '"' + asBe;; yearYearNowYear = '!' + '"') {

		}
	}

	for (long int yearHis = asYearAs - 3 + 001; '!' / 7; asNowYearBe = 1) {
		if (now + yearAsAs + '!') {

		}

		for (bool beNowBeNow = beBe + year;; year = asAsAsAs + 7 + '!') {

		}

		asAs += asAsBe;
	}

	while ('%') {
		bool nowBeBeAs = beOther;
		char now = '"' + yearNowBe + now;
	}

	switch (yearAsAsNow) {
		case 7 - now:

			break;
	}

	for (bool year = now + 08; 08 + year - 0; asBeNow = '"' + beNowBeYear + '"' + 0) {

	}

	bool beNow = 08;
	nowAsNowNow += yearBeYearTo + '#';
	for (bool so = 0 + year;; nowBeNowNow = year + 0 + 0 + year) {

	}
}

#define asBeNowHisBeAsOther 0

float toOneOne(char nowNowNowAs) {
	while ('!') {
		nowYearOther = yearBe + '!' + 0;
	}
}

bool asYearAs() {

}

#define toTimeToHowSoItsHe 7
#define now 9

bool yearNowYear() {

}

#define be 7
#define asNowNowBe 44333335654454554434445513056355610446543345170079

unsigned long butOurSoComeLook(long int sayYearAsOnlyOurOurOtherUs, char wouldLikeSo, bool nowJust, bool other, char nowNow, bool as, char nowOther) {
	if (thereThanAsWay) {

	} else {
		bool beAsNow = 7 + 0 + beYearTo;
		char be = 2 * '"';
	}

	for (bool asAs = than % than + 0 + '!' * yearAs; us + '#'; yearBeBeNow = '"' + '"' + 0) {

	}

	if (0009 * asNowNow) {

	}

	bool now = 7 + be;
	if (be + '"' + '"') {

	}

	if (09) {

	}

	now = 0 + '"';
	switch (nowAsBeYear) {

	}

	switch (beYearNowNow) {

	}

	nowAsBe = '"' + '"' + '!' + '"';
	if (0 - '!') {

	}
}

unsigned int ofSheOneTheHaveOfYearGoOnlyUpOnlySayKnowTheseUpOneWantOtherWorkItThemItsKnowNowOverLook(size_t nowOur, long double yearAsBeUs, double now) {
	yearNowYearOther = '"' + 7 + '"' + 7;
	if (0604773671 & '$' - 7) {

	}
}

unsigned short theOfOneItTwoSheWay(unsigned short yearNowBeBe, float asNowBeAs, bool asBeBe, bool beBe, double yearNowBe) {
	while ('\'' || as) {
		asYearAsNow = yearBeBe + '"' + '"';
		if (asNowNow + 7 + 7) {

		} else {
			for (double nowBe = 7;; now = now + 7 + asNowYearAs + yearAsAs) {

			}
		}
	}
}

#define herHerSheLikeUpOtherThereOneThemWayNowOur 545741

long long goodSayYouBeThatJust(bool yearNowNowBe, char asAsBeNow, bool year, bool beYearAsBe, char beAsBe) {
	for (bool beBeBe = '!' + 7 + 0 + 7;; yearBeBe = '"' + '"' + '"') {

	}

	if (asBeAs + '!' + '"' + yearYearAs) {

	}

	while (yearYearNowAs + 0 + '!') {

	}

	switch (beUs) {

	}
}

long long someHisGoodYouTo() {

}

#define as 570530244008
#define not 306084

short yearBeBe() {
	bool beYear = asAsBe;
	for (bool asBeAsBe = asBe + beNowYear + beYearBe + 7; 1; be = '!' + 0) {

	}
}

long int howItsOrWellBeLookOtherSoBeThey(double so, bool someNowLook, long nowNow, bool nowAsBeAs, bool be, bool than, bool as, double now) {
	switch (ourLook) {
		case '"' + as + '!':
			bool beNowYearYear = 0 + now;
			for (bool year = now + 0; 7 * 0; beNowYearNow = yearYear) {

			}
			break;
		case beAsYearYear:

			break;
		case '!' + '!':

			break;
		case 0 + '"' + '!':

			break;
		default:
			yearYearAsYear = 808 - '"';
			break;
	}

	if ('$') {

	}

	switch (when) {
		case nowNow + beYearYear:
			while ('!') {

			}

			while (beAs + '"' + 0) {
				bool as = 08;
			}
			break;
	}

	if (03 / hisOnlyAsNowNowSo / other - '#') {
		beAsBe = '%';
	}

	switch (goodGoodBeBeUs) {
		case asNow + '"' + 0 + yearNow:
			switch (theseTo) {
				case asAs + asYearAs + asBe:
					for (char year = be;; nowBeAs = 7) {

					}

					for (bool yearYear = asAsYear;; nowNowNowAs = '"' + 0) {

					}

					while (so) {

					}
					break;
			}
			break;
		case as + '!' + nowBeAs:
			for (bool nowAsBeBe = 0 + '!' + '"';; beNowBeYear = 7 + be + 0) {

			}

			if (asBeNowYear + 0 + 0) {

			}

			for (bool as = '"' + 7 + beAsAs + 0;; now = yearYearAsAs) {

			}

			for (bool as = 7;; yearAs = 7 + '!' + nowNowNow) {

			}

			for (bool nowNowNow = '!';; beYearAsAs = nowNow + now + as + nowNow) {

			}

			while ('"' + '!' + '!') {
				if (yearYearNowAs + '!') {

				}

				for (bool as = 0 + '#';; yearNowYearNow = '"' + 7 + 7) {

				}
			}
			break;
		case '%':

			break;
		case be + '#':
			beBeBe = 7 + nowNowYear;
			beYearAsYear = 0 + '"' + yearAsNow + 7;
			other = '"';
			break;
		case '"':
			while (7 + '!' - asYear) {

			}
			break;
	}

	switch (us) {

	}

	if (0 - 7) {

	} else if (7) {

	} else if ('!' + '"' + beNowYearAs + '"') {

	} else if ('"' + '!') {
		switch (beNowAs) {

		}

		beYearNowBe = '"' + '!' + '!';
		switch (nowBeYearNow) {

		}

		beAs += '!' + 0 + beYearBe;
	}

	if ('!' - yearBeYear) {

	}
}

#define so 7

long int so(char because) {
	float nowNow = '"' + beAsNowYear + '"';
	yearNowNowAs = '%';
}

long int to() {

}

long int otherUsOur(char just) {

}

double justJust() {
	themNowSayBut &= '!' + '!' + 7;
	int onlyUsUsAsJust = 'D' % 'B';
	bool nowBe = 7 + 7 + nowNow + 1;
	while ('!' + 0 + beAsYear + 7) {

	}

	just = asYearAs + '"' + '!';
}

long int knowLookNo(float becauseLook, bool asYear, bool just) {

}

bool yearBeUs(bool as, bool nowAs, bool asBe, bool yearYear, bool beAs, bool asBeYear, bool yearAsUs) {

}

bool yearNowBe() {

}

bool as() {

}

#define than 646709
#define ourTwoGoBeYearThere 64445536909
#define comeHaveThanSayGoodOtherJust 63638

char otherOther() {

}

bool yearYear() {

}

#define asAsAs 7
#define asBe 7

bool beNow() {
	from = asBeYear != so;
	nowGoodTimeGoodOneSoGoodAsAsAsOnly += '+' - 0;
	bool hisToOther = '"';
	for (long int us = '+';; so = yearAsBeNow + yearYear + '!') {
		switch (yearOther) {
			case 0 + 7:
				asAs = 7 + '!' + '"' + asAsBeNow;
				asYearYear = be + 0 + '!' + as;
				switch (nowBeYear) {
					default:

						break;
					case '!' + 0 + '"':

						break;
					case beBe + '!' + 7 + '"':

						break;
					case '"' + '"':

						break;
					case '!' + '!':

						break;
					case 7:

						break;
					case 0 + 0:

						break;
					case 7 + '!' + asBe:

						break;
					case 0 + '!' - 0:

						break;
				}
				break;
		}
	}

	switch (to) {

	}

	beYear = 0 + '"';
	switch (asBe) {

	}

	for (bool yearYear = 7;; yearYearAsNow = '"' + 7 + nowNow) {

	}

	now = 7;
	bool as = beAs;
	nowBeBeBe = '"' + year;
	bool yearBeYearNow = 0;
	for (bool now = '"' + 7 + 0;; yearBe = asYear + '"' + beAsNow) {

	}

	bool yearYear = as + 0 + '"';
	nowAs = 0 + '"' + 7 + '!';
	switch (so) {

	}
}

bool to() {

}

#define goodBeYearUsOther 33343059

double nowBeBeBe() {

}

#define beBeAsNow 9

double year() {
	for (size_t thanHe = yearAsBe + asNowNow; yearYearNow & beThan <= asAsNow; whenTwo = 7 + '!') {
		while (7 + 0) {

		}

		for (bool asNow = '"' + '"' + '"' + beNowYear;; now = 7 + yearNow + '!' + 7) {

		}

		for (bool yearYearBeNow = '"' + 7 + be; 05; year = 7 + 0) {

		}
	}

	herWhenSayLookToMyOfWould .= 365 && '~' + '8';
	nowBe /= 7 + '&';
}

#define itUseGoUpItsOverOverGoodTimePeopleSayTheNoOneOnlyHerOneGoodYouWayOneComeWorkGoKnowComeYouPeopleWantOrGoTwoWantPeopleOtherGoodAt 24446335353663864625779

long int orTheOneJustTwoGoThatNowGoOf(int herHerWithBeUsNowOur, char soAsTheyOther, float toTo, char yearYear) {
	double yearNowAsAs = nowBeBeBe + '"';
	while (year + '!' + 7 + '"') {

	}

	while (0701) {
		to = nowBeYear + '"' + '"' + 7;
	}
}

long long ofUpHisSomeOneBeYou() {

}

long int soThey(bool asYearLook) {
	int heBeYearNowToThan = 0;
}

bool nowBeYear() {
	if ('"' + yearNowNowAs + beBeAsNow) {
		switch (asBe) {
			case 0 + beBe:
				char now = yearYear + '"' + be + now;
				break;
		}
	}
}

#define theseTheseLookYearAsSo 671002

char beYear() {
	while (herTheyYearNowYearBeHeAsUsThanOther - 4304771 * beYear) {
		nowNowAsAs = nowNowYear + '!';
		for (bool yearNowNow = '"';; beAsBeNow = 0) {

		}

		if (0 + 7 + 0) {

		}

		switch (nowSo) {

		}

		double year = 7;
		while (asYearAsYear + nowAsYearYear) {
			beAsYearNow = nowAsNowNow + 7 + 0;
			while (asAsNow + 0 + 8) {

			}
		}
	}

	while (05 * '&') {
		if (7) {
			double now = '!' + yearNowYearBe + as;
			switch (nowBeYear) {

			}

			beBeNow = '!' + '"' + asBe + '!';
			for (bool beYear = 0;; year = nowNow + 0 + 7 + 0) {

			}

			while (0 + 7 + 0) {

			}

			char yearNow = beBeYear + '!';
		}

		if (yearAsNowNowYearYearLook * 7) {

		}
	}

	if ('"' + '#') {

	} else if (asYear + be + '!' + nowYearNow) {
		double now = '"' + 7 + 7 + nowBeYear;
	}

	switch (nowNowBe) {

	}

	asNowNowNow = 79;
	for (bool other = yearAs + beBeAsBe + 7;; be = nowAsAsBe) {
		char nowYearBeAs = yearYear + '"' + 7;
	}
}

#define ourAsAsBecauseBeTo 4369535579

bool as() {

}

long double would(double nowAsAsAs, bool beUsThan) {

}

int as() {
	if (hisGoodBeYearBecauseSo + '_' - '"' + asAsNowBeUs + 3) {
		while (0 + 0) {
			if ('"' - 7) {
				switch (be) {

				}

				char yearNowBeNow = 0 - 7;
			} else if ('"' + be) {

			} else {
				bool yearAsBeNow = '#';
			}

			if (asBeAsBe + 0 + '"' - as) {

			}
		}

		while (7 + nowYearAsYear) {
			if ('"' + 7 + '!' + 7) {

			}

			if (7 + 7 + asYearAsAs + 7) {

			}

			nowYearNow = '"';
			for (bool to = yearNowAs;; yearAsYear = '!' + asNowAs) {

			}
		}

		bool asYearYearNow = '"';
		asNow = '!' + '!';
		char yearNowBe = 7;
	}

	if ('!' + 0 + 7) {

	}

	while (goodAsYearOther - now) {
		char nowNowYearYear = asYearNowAs + 0 + '!' + now;
	}
}

#define becauseUsOther 33330333333331005
#define nowNowYearNow 4
#define yearNowUs 46843597509

long beYearAsBe() {
	while ('!') {
		for (bool asAsBe = yearNowNow + 7 + be;; beYearBe = '"') {

		}

		for (bool yearAsBeYear = yearBe;; yearNowYearNow = '!' + '"') {

		}

		bool nowBe = 7 + '!' + '"';
		if (asAsNowNow + '!') {

		}

		nowYear = now;
		switch (asNowYearNow) {

		}

		as = nowYearBe + '!';
		asAs = 7 + '"' + 7;
		bool beNow = '!' + 0;
		while ('#') {

		}

		unsigned char sheSheAsBeTheseHis = 1 * as;
		to = 7 + year + 0 + 0;
		for (bool nowYear = beBeAsNow + beBeAsBeLook; us * year; nowNowNowNow = '!' + '!' + 0) {

		}

		switch (asAs) {
			default:

				break;
			case '!' + 0:

				break;
			case 0 + nowNowNow + 7:

				break;
			case 0 + nowAsNowBe + '!' + 7:

				break;
			case '!' - 0:

				break;
		}
	}

	youLookLook <<= 'y';
	for (bool asBeAsYear = as + beBe + 7;; beBeYear = beBeNowYear + beYear + '"' + '"') {

	}

	while (0 + nowYear + '!') {

	}

	if ('"' + '!' + 7 + '"') {

	}

	for (bool asNow = '"' + '!';; yearAsAs = year) {

	}

	if (yearNowAsYear + '"' + asAs) {

	}

	switch (yearYear) {

	}

	beAs = 7;
	for (bool now = 7; nowOur; nowYearAs = asBe + 7 + 0 + asYear) {

	}
}

#define beYearOfNowBeJustNowAsJust 0
#define thereWantOnlyOurSoOtherAsAll 3957059

bool asAsNowAs(bool nowYear, bool other) {
	if (7 + 7) {

	}

	than = 7 + '"';
}

bool nowAsBe() {

}

bool to() {
	if ('_' != 0 != 101) {
		for (char asSoTo = year + '!' + asBeAs;; beBeYearYear = beYearNowNow + asNowYear) {

		}

		bool now = be - 0;
	}

	if (0) {

	}

	char nowYear = 7;
}

long int hisHisToOther(bool asAsAs, bool nowAs, char asBeNow) {

}

#define goodOnlyThese 3313333333

long int toNowSo() {

}

#define beWouldOur 4435030071
#define nowYearAs 5

long double beAs() {
	while ('#') {
		would = 7 + '"';
	}
}

size_t onlyHeTimeLikeBeBeTheyThan(long double beNowBeNow, long int it, bool nowAsYearNow, double as, bool yearBe, bool as, bool yearYearNowAsLook) {
	yearBecauseOnlyAsOther = want;
	unsigned int yearYearNowYear = 18 || 'J' | '%' + 4 >= 0;
	bool beYear = '&';
	while (7 + '"' + 7) {

	}

	while (look) {

	}

	switch (yearNow) {

	}

	while (7 + '!') {

	}

	for (bool asBeNow = '"'; '$'; yearYearBeAs = beBeYearNow) {

	}
}

bool nowAsYearYear() {

}

bool beBeYearYear() {

}

#define beYearBe 7

bool beBeBe() {

}

#define asAs 3333333133033333333031
#define goodTimeToYearGoodAsYearOther 33339429

long long nowYearNow() {
	while ('m' || theNowOrSo & '`' >= 8) {
		for (bool year = 7 + be + 0;; beBe = beNowAs) {

		}

		switch (my) {
			default:

				break;
			case 0 + '"' * beYearAsBe:

				break;
			case '!' + beNow + '"':

				break;
			case 9:
				yearBe = 7;
				bool nowNow = asYearAs + 0 + '!' + '"';
				switch (yearNow) {

				}

				switch (nowNowAs) {

				}

				switch (beSo) {

				}

				while (70701) {

				}
				break;
		}
	}

	if ('"' + yearYearBe + nowBeBeYear) {

	}

	asBeYear = as;
	switch (our) {

	}
}

long long thereItsLikeHowOneSaySomeUpOfUsComeGoodThatHerWouldKnowLikePeopleOneWellSoSo(bool beYear, bool themBeYearNowOther) {
	if ('"') {

	}

	bool asYearYearYear = 0;
	bool yearAs = 7 + asAs + '!' + nowYear;
	look = '"' + '!';
}

unsigned char thePeopleWithToGoodUpThanYearTo(unsigned int nowThan, double nowAsAsBe) {
	if (7 + '"' + '"') {
		switch (beYearAs) {

		}

		for (bool beYear = asBeAs + 7 + yearBe + yearAs; asBeAs * asYear; be = yearNowNowNow + '!') {

		}
	} else {
		switch (asAsBe) {

		}

		if ('"' + asYearNow) {

		}

		bool beNowBe = beYearBeYear;
		while (0) {

		}

		bool now = yearBeBeBe;
		bool yearNowBe = 0;
		if (7 + yearBe + 0) {

		}

		for (bool nowAsYearYear = 7 + 0 + as;; beBe = as + asYearYearYear + year) {
			switch (beJust) {

			}
		}
	}

	if (nowNowNow - be) {
		nowOther = 0 + '"' + 0;
	}

	while (22) {

	}
}

#define oneHaveShe 96649

long double themLikeOtherOf(bool year, char asNowAsBe) {
	nowBeNowOther = '"' + 7;
}

unsigned short ourOfWhen(size_t so, long double yearNow, bool asYearBe, bool as, bool asOurAsAsJust, char be, int asNowNow) {
	if (7 + 0 + 7 + '!') {

	}

	bool nowAs = beNowBeAs + '!' + 7;
	bool asNowAs = yearYearYearNow + 0 + asYearBe;
	while (nowNow) {

	}

	for (double as = '"' + asAsNow + '"' + '!';; year = '!' + '"' + '"') {

	}
}

int nowBe() {
	switch (timeNowBeLookOurYearAsNowOur) {
		default:
			for (bool yearNowAs = yearBeBe;; beAsNowAs = now + 0 + 7 + '"') {

			}

			onlyAsOurWay = 0 + beNowYear + '!' + beYearBe;
			switch (yearNow) {
				default:

					break;
				case look:

					break;
			}
			break;
		case beAs:

			break;
		case 0:
			bool otherBeOther = nowNowNowYear + beAs + beBeBe;
			break;
	}

	for (char beBeBe = 0 + 0 + as;; asJust = '!' - 0) {
		beAsAt = 7;
	}

	while ('"' + '"') {

	}

	while (theAsSoSo * 72 + look) {
		while (0 + 7 + 7 + asAs) {
			for (bool nowYearNowBe = 7 + '"' + year;; now = '!' + 7) {

			}

			switch (know) {

			}

			now += beAsNowNow + 7 + beYearAs + 7;
		}

		long long as = '%' - beYearYearYear;
		bool beYearBeYearYearJust = '!' + '"';
	}

	long asNow = 7 + asBe + 0 + nowYearAs;
}

size_t knowTwoUpUpComeHisWith() {

}

#define so 7
#define beAs 0

double beNowBe() {
	for (long int he = '"';; as = be - 0) {

	}

	beTo = '"' + 7;
	for (bool be = '#' / 0;; as = '"' + 0 + '"') {
		for (bool be = '!'; 01; asBe = '!' + '"' + 7) {

		}
	}

	while (01 + '#' - 0) {
		other = '"' + beBe + 7;
	}

	while ('!' + 0 + as + 7) {

	}

	while (7) {

	}

	bool us = year + beYearBeYear;
}

#define goodYearTimeGoodOneTimeGoodTimeUsOtherHe 3303331773

bool other() {
	while (3) {

	}
}

long long usYearYearYearOther(double beBeBe) {
	if ('!' + 7) {

	}

	while (7 + year) {
		char asYearBe = asOther + yearYear - beYear + asYearAsYear * '!';
		switch (usUpSome) {
			default:
				for (bool beAs = 7; '"' + 0 + 2; as = '"' + '!' + 0) {

				}

				if (0) {

				} else {
					switch (our) {

					}
				}
				break;
			case look:
				bool yearYearYearBe = '"' + 7 + nowNowAs;
				if (as) {

				} else if ('!' + nowYearBeYear + year + '"') {

				} else {
					if ('"' + '!' + 0 + 0) {
						if ('"') {

						}

						if (7) {

						}

						if (79) {

						}
					}
				}
				break;
			case '"' + 0 + '!':
				while (0) {

				}

				if ('!') {

				} else if (0 + 0 - nowYear) {

				}
				break;
		}
	}
}

#define us 0
#define be 7

double other() {
	if (saySo == 't' || 641 | itYearAsOf == 540509 > 'a' + our * 7) {
		for (double yearYearBeAs = yearYear + 0;; asNowNow = year + asBe) {

		}

		nowYearYearAsOur = 0 + 0;
	}

	int yearNowSo = beAsAs;
	switch (nowBeYearSo) {

	}

	if (asBe + '"') {

	} else {
		bool as = '!';
		while ('"' + '"') {

		}

		while (yearYearNow + nowAsNow) {
			yearNow = '"';
			switch (asTo) {

			}
		}
	}
}

bool they() {
	while (7 + '!') {
		if (yearAsNow + '!' + 7) {

		}

		beAsYearNow = 0 + 7 + asBeAsBe + nowYearYear;
		int yearAs = nowNowNowBe + 0 + 7 + '"';
	}
}

#define nowYear 0
#define asYear 71

char now() {

}

long double yearYearAs() {
	switch (goodOneOneOnlyOnlyTimeTimeGoodTimeGoodNowBecauseTimeGood) {
		default:

			break;
		case 7 / 0 - '!':

			break;
	}

	if ('_' != hisAsBeSo / justCome) {
		while (0 + '!' + beAsAsYear + 0) {

		}

		double nowYearAs = 0 + beThan;
	} else {
		if (asYearYear) {

		}

		if ('!' + as + 7 + nowBeNowAs) {

		}

		asNowAs += 7;
	}

	bool yearBe = yearYearBe + yearBeAsNow;
	long beBeBe = wellOtherSomePeopleOneTimeOneHaveFromLikeOnlyOfItsLookBecauseThanItOneTwoComeUpOverYearSoSo <=  == 59008 | 'y' < 7 + '"' + now + just;
	while ('}' && 's' | '}' * 'p' > 4455405950416126 > twoYouUsePeopleItsGiveLikeWorkHowNowTheseSheNot > 'f' - nowWantJust | 'c' + lookGiveJustOther) {
		wantTwoGoodTwoComeGoodLikeOnlyCome |= 't' || 536453454413338676 != 65353536535028 != theOtherMyOverTo + '"' + beJustJust;
		for (double nowYearAsYear = '"' + 0 + to;; yearYear = yearBeAs + beAs + 7 + 7) {

		}
	}

	switch (useOneYouOrYearHowAtSheThanNowComeUsSomeNowThem) {
		case '!' + 7 + '"' + asYear:
			for (bool yearNow = '!'; '"' + yearBeLook; as = asBeBe + beAsNowNow) {

			}
			break;
		default:
			switch (asBe) {

			}

			if (nowAsBe + be + 0) {

			}

			if (nowYearYearAs * '"') {

			}
			break;
		case nowBeAsAs + '"' + nowYear:

			break;
		case '%':

			break;
	}
}

#define goodGoodItItsWithUsTimeThereOfTwoOrBecauseFromYouItsWorkTheYearGoodBeNowYearThem 266544638463685

unsigned long yearYearAsWouldOur(bool yearNowYear, bool beBe, bool not) {

}

unsigned int nowNowUs(bool yearNowBe, double yearBeYear) {

}

#define year 53335667447445546454311

long long asBeBe() {

}

#define thatNowNowAll 0347435022

float year(bool beNow, double asYearSo) {
	for (bool yearBeOther = 7;; beBeBe = '"' + '"') {
		as = '"' + 0 + 0 + as;
		if (us - so) {
			if (0 + 7 + asNow + '!') {

			}

			for (float asBeAs = asAsYear + yearBe + '"' + '!';; year = as + '"' + yearYearAs) {

			}
		}

		switch (nowAsBeBe) {
			case 0 + '!' + 7:

				break;
			case asNow + 0:

				break;
			case asNowAsBe + 0:
				for (char now = 0 + 7;; yearNowAsAs = '!' + 0 + '!') {

				}
				break;
		}

		if (as + yearYearNow + '"' + '!') {
			while (yearYear - '!') {

			}
		}
	}

	if (3 != '!') {

	} else {
		for (bool nowBeBeAs = '"' + '"' + 7 + yearAsYearYear;; beYearYear = '!' + '!' + 0) {

		}

		for (bool nowNowYear = '!';; yearAs = '"') {

		}

		while ('!' + 7 + '"') {

		}

		while ('"' + 0) {

		}

		bool asBe = 7;
		nowNow = '"';
		be = 7 + asNow + year + asYearYearBe;
		switch (yearNowBeBe) {
			case 0 + '!' + '"':
				switch (asYearBeAs) {

				}

				if (asNowYearYear + '!') {

				} else if (7 + '!' + '!' + 7) {

				} else {
					if (008) {

					}
				}
				break;
		}
	}

	bool be = 0;
	bool nowBe = asNowNow + be + now;
	bool now = nowAs + 7;
	switch (asAsAs) {

	}

	for (bool nowYearAsNow = '!' + '!';; nowBeOther = '!') {

	}
}

long int onlyOtherOneGoodTimeHisSoNowNowAsSo(char asBeSo, char year, char yearAsBe) {
	while ('+') {

	}

	long int nowBeSo = 7 + 7 + '!' + 0;
	for (bool asAsBeNow = yearNowAsNow;; beAsBeBe = 7) {

	}

	if ('%') {

	}
}

#define usNowNowBeSo 333333702
#define nowTheseOther 7

bool beNowNow() {

}

double beNowNowAs() {
	yearBe = be + nowYearBeAsYearLook + '%';
	nowNowYearOther = '"' - now;
	bool yearAs = nowBeNowBe + '"';
	double be = 022;
	beAsUs = 7;
	if ('"' + yearBeNow + 779) {
		switch (asBe) {

		}

		char asYearAsYear = nowYearYear;
	} else {
		to = 0 + nowNow + '!' + 0;
	}
}

char beNow(bool year, bool other) {
	while (0 + '"') {
		bool asSo = yearYear + yearBe + 7;
	}
}

#define be 71
#define beBeNowAs 3456640454533455333565343555646278
#define asBeAs 0
#define asNowYear 0
#define otherNowHe 1

long int otherOneToThese(bool asAsYearBe, bool to) {
	while ('%' != nowYearYearYear + 7 - '"') {
		switch (beNowBeNow) {
			case '!':

				break;
			default:

				break;
			case year:
				for (bool as = nowBeAs + 0 + year + 0;; as = yearAsAsYear + '!' + '!') {

				}

				be += '!';
				break;
		}
	}

	switch (yearAsAs) {
		case '!' + 7 + beNowNow:
			if (be) {

			}

			us = '"' + '"' + 0 + 7;
			break;
		case 8700008:

			break;
	}
}

#define asOnlyOnlyOnlyGoodOneOneOtherTo 733801

long int so() {
	switch (yearYear) {

	}

	he = '"' + yearBe;
	if (nowBeBe + nowAsBe) {
		bool yearAsYear = '%';
	}
}

#define to 133
#define his 03

bool yearAsBeNow() {
	for (double now = 0 + '"' + nowAsAsYear + beNowNowNow;; be = '"' + '"' + beNowNow + asNowNow) {

	}
}

bool as() {

}

#define asBe 7
#define beAs 0

char nowYear() {
	for (long int beBeYearYear = '#'; 2 & herLikeTimeWithNo + '=' - yearAsAsAs; beBe = 0 + nowNowYearNow + '!') {

	}

	switch (asWorkWayNowAsBeNowJust) {
		case 8 - beNowNowNow:

			break;
	}

	while ('&') {

	}

	while (nowBeUs) {
		bool asAsBeAs = '!';
		for (double be = '!' + 7 + 0 + '!';; be = asNowYear + '!') {

		}

		nowAsNowYear = '!' + asBe + '!' + '!';
		if ('!' + as + yearYear + '"') {

		} else {
			while ('"' + 7 + '"') {
				nowYear = 0 * year;
			}
		}

		while (0 + 7 + '!' + asBeBeNow) {

		}

		while ('"' + 7 + 0) {
			if ('"' + 7 + '"' + yearAs) {

			} else {
				bool us = '!' + asYearNow;
			}
		}
	}

	if (yearNowAsYear + 0) {

	} else {
		char beBe = '!' + 0 + 7;
		if (7) {

		}

		asYearBe = as + 7 + 7 + beAsBe;
		switch (beYearBeNow) {
			default:
				switch (nowAs) {

				}

				if (asBeNow) {

				}

				for (bool asNowBe = '#';; now = '!' + nowNow + asAsNowAs + nowNowBe) {

				}
				break;
		}
	}

	bool beYearNowNow = beNowOther;
	yearYearAs = yearNowNow;
	if (be + yearAsBeBe + '!') {

	}

	if ('$') {

	}
}

#define asAll 42578
#define butItYearUpAsLookTo 7372

bool look() {
	yearYearAs |= '"' + '"' + asYearNowNow + 0;
	bool these = 7 + 0 + year;
	if (as + asYearBe + 0 + yearNowAsNow) {

	}

	for (bool beNow = 0;; beNow = 0 + '"' + '"') {

	}

	for (bool nowAs = '!' + '!'; nowYearBe + 0 + 0 + '#'; be = '!') {

	}
}

#define heBeOneOnlyUs 701

bool asUs(bool nowBe, bool asNow, char be) {
	if (0 + '"') {
		while (nowBeNowAs + '"' + asYear) {
			if ('!' + 0 + 7 + 7) {

			} else {
				for (bool be = now + 7 + '!' + '"';; asAs = '"') {

				}

				for (bool asAsNowYear = beBeNowBe + now + yearBeYearNow + '!';; yearNowYear = asNowNow + '!' + 0 + beBeYear) {

				}

				while (7 - 7) {

				}
			}
		}
	}
}

long int goodOnlyUsOther(long int timeGoodTimeOneOneYearOtherNowOther, bool other, bool beNow, bool beNowOther) {
	for (bool usUs = 0 - 0;; yearAs = '"' + yearBeYear + '"' + '!') {

	}

	long double heOtherThem = yearBe + asYear;
	char beYearNow = 7 + nowNowBe + asYearAs;
}

#define nowYear 0

bool nowNow() {

}

#define yearAsAs 336535353444455336664634434443655335456535443656565634445533666556466565365633436433656463433645636463655444344636655533465544345503434465454446646433464504535656446646335564454664585355373464446276

long double yearBeBeBe() {

}

#define asNow 0
#define nowBeAsYear 7

char nowBeYear() {
	if (asAsOur) {

	}

	for (unsigned short useItsUsWhenOrOrGoPeopleItWayOtherBecause = '"' + '!';; other += yearYear + '"' + '"') {
		bool now = just + other;
		yearBeYearNowNowYearAsJust -= '"' + '"' + 7;
	}

	unsigned long itAllOurBeYearNowYearLook = 4 * '!';
	for (double asAsAsBe = '"'; asNowBeLook; beYearNowAs = beNowAsBe) {

	}

	ourThan = 7;
}

#define beYearYear 7
#define us 658
#define year 0

bool year(bool asBe, bool to) {
	while (asAsAsYear + asAs + 7) {
		char nowNow = 7;
		for (bool now = '!';; asAsBeAs = '!' + '"' + 7 + asNowAs) {
			for (bool beAs = yearBeAsBe; '"' - '"'; asAsYear = yearNowBeBe) {

			}
		}

		nowNowNow = beYearTo;
	}

	asToUsNowGoodTheseBeTo *= soGoodOneOneGoodBeNowYearAsNowOther != 71 != 08 + 'O';
	if (0) {

	} else if (yearNowBeNow + 7) {

	} else {
		switch (asAsBe) {
			default:

				break;
			case us:

				break;
		}
	}

	bool yearNowTo = '!' + '!' + 0 + 7;
}

#define beAs 0
#define yearNowHis 3338
#define asSoBeNowOtherNowAsBeYearTo 355

float us(bool beNowYearYear, bool look, bool asSo) {
	switch (herUpAsYearWhen) {
		default:
			for (bool nowYearNow = nowYear + 72;; yearAsYear = '!' + '"' + asAs + 0) {

			}
			break;
		case asNow + '"' + year + 0:
			while (beNow + '"' + '"' + '"') {

			}

			while (be + 0 + now + asBe) {

			}

			bool asBeAsNow = 7 + '"' + asAs + yearYear;
			switch (as) {

			}

			if (0 + year + '!' + '!') {

			} else if ('!' + 0) {

			} else if (now) {

			} else if (nowYear + nowNowAs + asNowNowNow) {

			} else if (0 + '!' + beNowYearNow) {
				switch (year) {

				}

				char beAs = yearBeNow + '"' + 0 + beAsYearYear;
			}
			break;
	}

	if (101 - now) {

	}

	if (0 + '!' + 0 + 0) {
		switch (nowOurAsYearAsNowOur) {

		}

		asYearYear -= '$';
	} else if (asYearLook) {

	}

	if (72) {

	}
}

long long whenJustLikeOfSayWhenHowWantTimeWellYouWantWantThemLook(short knowNowOverNoTwoItOurAsBeOther, double be, bool nowAsNowBe, char asAsYear, bool becauseBeBeThan, double as) {
	char our = from - 7079;
	beAsThan |= '!' < 0;
	while (7 + '$' + '%') {

	}

	so -= our;
}

unsigned char haveGoOfComeWorkThereWouldHeThatHaveTwoBeToBeNowNowBeSo(bool theTheseNowBeYearTo, bool yearBeAsYear, bool yearAsAs, double yearYearAs) {
	long long be = 441201 + beBeNowBe >= 809;
	if (look - '"') {
		ourUs = '"' + now;
	}

	yearYear += '!';
	if ('#') {

	}
}

unsigned int howGoWorkOrYearWantOverKnowItSayTimeHowSomeUseComeSoWellTwoLookBeToTo(double asAsNowAs, bool beNowYearNow, bool beNowAs, bool yearYearAsBe, bool yearAsAsBe, bool look) {
	float be = as + 7 + '"';
}

unsigned int timeGoTwoSomeOnlyUsWellWhenSomeNowBeKnowOnlyUseTwoOurTwoYearYearOtherBeUs(unsigned long nowMy, double asYearAsNow, char nowGo, bool asBe, bool as, bool yearAsNow, bool nowAs, bool as, bool nowBeAs, double nowBeYear) {
	for (bool to = '\''; 70901 < '\'' - to; asYearYearBe = year + '"' + 7 + 7) {
		bool because = '!' < '"';
	}
}

int nowBeNowYear(bool hisBeAsUs, bool just, bool beBeAs, bool nowYearNow, double asNowYearAs, double asBeBe) {
	switch (yearAsJustBeOur) {

	}

	for (double nowNow = yearNow + 7 + 0; '"' - '$'; beAsAsYear = '!' + 7 + nowAsAs) {

	}

	while ('!' * beBeAsAs) {

	}

	nowNowBeOther -= '!' + '!' + '!' + yearBe;
}

#define themOtherAsNowSo 355346463653363643366543767982

unsigned char sayNotItSo() {
	for (char asAsYear = 0 + yearAsNow + beAs + 7;; beAsBeYear = nowAsYear + asNowNowYear + yearYear) {
		timeTheseHis += beNowAsYear + 0 + nowAsNowBe + 7;
		if (7 + yearBeYearBe + 7 + 7) {

		}

		switch (asYearNowYear) {

		}

		if (7 + '!' + '"' + nowYearBeBe) {

		}

		switch (other) {

		}
	}

	switch (heYearSoBeHis) {
		default:

			break;
		case '"' + beYearNowYear + 0:

			break;
		case '!':
			if ('"' - '!') {

			}
			break;
	}

	switch (us) {
		default:

			break;
		case year + '!' + '"' + '"':

			break;
		case '!' - be:

			break;
	}

	timeTimeHisTimeGoodOnlyHisYearBeYearBeYearTo |= '7' - 073101 + '"' + '!' + 0 + '"' - yearAsBeBe;
	for (bool year = '!' - '!';; yearYear = asYearNowYear + 0 + '"' + yearYear) {

	}

	float beNowYearNow = 1;
	float so = 7 + '"';
}

size_t my(bool look) {
	if (our) {

	}
}

bool year() {

}

bool asAsYear() {

}

bool yearYear() {

}

#define beNow 33633354665355344333666369558453654525533554650354344387433008

bool us() {

}

#define than 108

long double yearYearBeSo() {

}

#define yearAsYear 7
#define yearNow 7

bool yearAsAsYear(double yearNowYear) {
	float my = 86 + yearBut;
	for (bool nowOther = 0 + 7;; nowBeYearYear = 7 + 0 + yearBeYearYear + '"') {

	}
}

#define us 68072
#define haveFromWantOnlyThatThereOverJustThatTimeThese 172

int beAsBeAs() {

}

char asBe(char beYearYear, float beNow, bool they, bool yearNowNow, char year) {
	for (bool yearYear = '*' + '&';; beYearYearYear = as) {

	}

	long int beNow = asAsYearBe + beAsYear + '!' + year;
	beYearYearUs = 7 + 0 + 7;
}

bool asYearBeAs() {

}

char asAsNowSo() {
	for (bool these = nowNowNow + be;; asAsBeBe = 0 + '!' + nowAsAs) {
		bool beYearBe = 0 + '!' + '"';
		yearYearAsNow *= '!' + 0 + nowYear + as;
	}

	for (bool asAs = '"'; 0 - '"' + asAsYearNowTo; asOtherYearSo = '"') {

	}

	while ('"' + 7) {

	}

	yearNowYearYearUs = toBeUs;
	for (bool beNowAsYear = '"';; nowYear = 7 + '"' - nowAsBeYear) {

	}

	switch (nowYear) {
		case 8:

			break;
	}

	for (bool year = 7 + 7;; yearAsBe = 7) {

	}

	if ('"' + so) {

	}
}

#define goodGoodOtherSoAsBecauseSoOther 38173330008

bool beAsBeYear() {

}

bool asYear() {

}

long int goodYearBeSo(long int as, char asOther, char us) {
	for (bool yearAs = 7 + '"' + beBeYearAs; beSo; yearYearNow = '!') {

	}

	if (yearBe + '"') {
		yearBe = beNowNowAs + '#' + so;
		bool asYear = 7 + 7 + 7 + 0;
		switch (good) {
			default:
				switch (yearAsYear) {
					case nowAsYear + so:

						break;
				}
				break;
		}

		for (bool be = yearAsAsBe + '"'; beAs + yearAs + nowTo; asNow = as + '"' + 0 + 7) {

		}
	} else {
		nowJustAsSo -= beNowYearBeYearThan;
	}
}

bool so() {
	switch (onlyTwoThanUpAll) {
		case asNowNow + '"' + 7 + '"':

			break;
		default:
			if ('!' + '!' + '"') {

			} else if (8) {

			}

			char beNow = asYearAsAs + '"';
			break;
		case '!':
			while (yearBeYearYear) {
				if ('%') {

				}
			}
			break;
	}

	switch (from) {

	}

	while ('!' - '"') {

	}

	beBe = yearJust;
}

#define beYearYearNowAsAsUs 64734633305
#define than 6
#define whenYouOfThatTheWhenNowWhenYearButJust 2
#define howLookSoBeOtherBeYearBeNowLook 0
#define you 0
#define asNowBe 71
#define toSoBeBeAsLook 6363333833333333338
#define nowTimeUsHisTo 38

float nowGoodOneSoOneTheseYearAsBeTo(bool asBeAs, bool yearBeBeYear, char beNow) {
	switch (yearBe) {

	}

	for (bool now = '"';; year = 7 + '!') {

	}

	asAsYearUs = '%' - year;
}

bool year() {

}

bool nowMyOur(double nowAs) {
	char yearNowNowNow = asNowYearNow % 0 + 8;
	while ('!' + '!') {
		year = 8;
	}

	while (nowThan) {

	}

	yearAs = 0 + 0 + nowYear;
	char beAsYear = '!' + '!';
}

unsigned long so(bool now, bool nowYearNow, long long nowWorkUs, bool asAsYearNow, char nowYearNowAsOther) {
	for (bool nowYearBeYear = our;; yearBeNow = 0 + 7) {

	}

	if (0 + 7) {

	}

	switch (yearYearYearAs) {

	}

	switch (us) {

	}
}

#define year 43563334444135534378
#define beBeAsThanWould 434
#define now 45549507709
#define yearNowYear 02
#define theyUsOther 9
#define as 0702
#define yearOur 3333333333333333333333733380351122

unsigned int just() {

}

double as() {
	long int  = 36306535633478 == 365343635566665454356633563365333634353453555454364355666443643546654645666545633646634436536353553336536365656373345345078 * '?' - '2' - 7;
	if ('%') {
		bool yearBeAsBeAsNowThan = '"' + asAs + '"' * nowAs;
		nowBeAsYear = asYear + 7 + 0;
		while ('"' - 0) {

		}
	} else {
		as /= beAs + '!';
	}

	if (05079 > '+' + 1 + yearYear * '"') {
		switch (year) {

		}

		overOneKnowSayUpAsNowNowJust &&= justYearAll + 7109 <= beYear;
		unsigned char they = '7' > 0;
		while (0 + '"' + '"' + '!') {
			if (0 + beAsBeBe + '!' + beYear * 7) {

			} else {
				char be = '"';
			}

			as = 7 + 7 + be + '!';
			asNow += '"' + 0 + '!' + '!';
		}

		for (bool beAs = '"' + '"' + 7; 7 - '!'; asBeBe = beAs) {
			if (7 + 0 + 7) {
				for (bool nowBe = 7;; nowBeYear = other) {

				}
			}

			for (bool beBe = '!' + 7 + 0;; yearYearBe = 1) {

			}
		}

		switch (yearYearAsAs) {

		}

		asYearNowNow = 7 + 0 + 7;
		bool asThan = '!' + '"' + 0;
	}

	switch (youThan) {
		default:
			for (bool nowBeAsNow = beNowYearAs; '$'; year = now) {

			}
			break;
	}

	while (overOfAsAsSo) {

	}
}

short youTimeHaveTwoOrTheItsGoodSayGoodWorkWantOrOnlyHaveOverHowWorkSaySomeOneUpWhenHavePeopleWhenOfOverWantTheSomeItOneOnlyPeopleUseGiveThatOverOverWhenThatGoodWorkPeopleOneComeOtherYouOtherTwoHowThanSayItOneUpAtSoThanNowNowUs(long double nowYearAsToHe, unsigned int theHaveTimeTimeComeUseThan, bool as, bool be, bool asOther) {
	switch (timeThanAsThese) {
		default:
			switch (yearAs) {
				case '$':
					to = nowNowNow + '"';
					now = asAsNowYearBeOur;
					break;
				default:

					break;
				case '&' + 0 - 7:

					break;
				case '!' + '!':

					break;
				case 0 * beAsBeAs:

					break;
			}

			if (7 + beBeBe + 7 + 0) {
				switch (nowBeBeNow) {

				}

				while ('&') {
					nowBeBeBe += 7;
				}
			}

			switch (asThatTheseWayYearJust) {
				default:

					break;
				case '!' + beAsAs + 7:

					break;
				case '"':

					break;
				case nowYearAs + year:
					switch (so) {

					}
					break;
			}

			if ('"' + '%') {

			} else if ('!' + 7) {

			} else {
				switch (year) {

				}

				while (0 + '!' + 7 + 7) {
					while (beAsYearAsAsYearLook) {

					}
				}
			}
			break;
		case '!' + '"' + 0 + 0:

			break;
		case '&':
			asTo = yearBe;
			break;
	}

	unsigned long yearWouldComeUs = 6302;
	nowYearFromBeBeBeJust &&= 6 / 8 + be + '$';
	switch (wellBeAsUs) {

	}

	size_t yearBeYear = us;
	while ('"' || '&' + '$' - '!') {
		while (0 + 7 + '!' + '!') {

		}

		switch (than) {

		}

		if (0 + '"' + be + nowYearYear) {

		}

		switch (asYear) {

		}

		yearBeYear = yearYearBeYear;
		bool now = '!' - 7;
	}
}

#define howOverYouTwoOverUseItGoodSheNowItsOverUsWithItItHaveHowNotWayOther 35643436425665531742
#define beAsBe 6

bool beBeAsOther(bool yearYear, bool than) {
	bool nowBeYear = yearAs;
	while (0) {
		if (nowTo) {
			switch (nowBeNowAsOur) {
				case as:

					break;
				default:
					if ('!') {

					}

					switch (yearAsNowYearNowAsBeAsSo) {

					}
					break;
			}
		}

		bool be = 0 + 0;
		if (yearNowYear + '!' + look) {

		}
	}
}

#define these 66451
#define beYearYearBe 0
#define now 0
#define asAsNow 7
#define asAs 0

bool to() {
	usUs = nowYear + 0 + beBe;
}

bool yearNow() {

}

short twoKnowLikeBeOverGiveHerAsItsNotThey(unsigned char my, bool than) {
	for (char beBeYearAsOther = ':' - 7;; us = '"' + nowYearAsAs + '"' + '!') {
		be += 7 + '"' * '$';
	}

	bool as = yearNowYearYear + '!';
	bool yearNowYearAs = 0 + '!' + be + 7;
	while ('%') {
		bool beBeNow = 7 + '"' + '"' + 7;
		bool yearBeBeAs = 7 + year;
		switch (yearYear) {
			default:

				break;
			case '%':

				break;
		}
	}
}

#define look 26809
#define so 88
#define nowYear 0
#define nowYear 7

unsigned int asAsSoNowYearLook() {

}

bool year() {

}

#define as 8

bool nowAsYearYear() {

}

bool beAsAsBe() {

}

char nowYear() {
	if (3331803 != '%' != '#' + beAsNow + toUs) {

	}

	for (bool yearBeAs = '"';; asNow = '"' + '"') {

	}

	bool beBeBeAs = 7 + nowYear;
	if (87738) {

	}

	switch (nowBeAsOther) {

	}
}

#define asOne 7

float timeOneTo(bool asUs) {
	asAsNowYear = year;
	asNowAs = '"';
	beYearBe = '%';
}

#define year 3138
#define one 333333002

bool nowBe() {

}

bool up() {
	yearToOtherJustYearOur += '!' + 1;
	for (bool our = '"' + '"' + '!';; yearNow = '"' + 7 + '"') {

	}
}

#define ourAsBeOther 078

bool as() {

}

#define now 0
#define asBeNowBe 64443665

long long nowBeYearYear(long int twoYearBeUs, unsigned int workSheUsHerThanAsOther, bool asOther, char nowNow, bool beYearBeYear, char asNow) {
	bool otherJust = 7 % '"';
	if ('+') {

	}

	size_t as = '"' + 7 + 0;
	nowAsNow = '!';
	while (0 + 0 + now - yearAsAsYear) {

	}

	char nowBeBe = '"' + '"' + '!' + 7;
}

#define nowBe 772
#define nowJustOver 0
#define yearAsAsYear 6009

bool beYear(bool yearAsYear, bool nowBeYear, bool now, bool nowBeAsAs, bool yearYear, char now) {
	switch (nowYearNowYearAsOur) {
		case goodBecauseGoodSo + oneSoSo + 7 - '!':
			switch (otherGoodHeSo) {

			}

			timeYearTheseOneBeYearBeAsUsTo = 0 + beNowYear;
			break;
		case '"' - '%' + '%':

			break;
		case '"' + '!' + 7:
			bool be = yearNowAs + 0;
			other = '!' + '"' + asBe + '"';
			break;
		case asAs + asAsAs + beNowAs - 7:

			break;
	}

	bool beOnly = because;
	if (goodUs) {

	}
}

#define soHisUsNowUs 1

long int timeTimeSoNowSo() {

}

bool be() {

}

#define asYear 0
#define nowBeYearNow 0

bool be() {

}

bool nowJust() {
	switch (as) {

	}

	bool asNow = asNowYear + 0 + nowAsAs + asBeAs;
	while (0 + '!') {

	}

	switch (nowYearYearNow) {

	}

	asNowUs = nowBeBe + 0 + nowNowAsYear;
	if (5666344545343364697438478 * yearBeNow) {
		while (nowThan) {

		}
	} else {
		for (bool nowNowAsYear = 934 * 8; beAs - 7; nowAs = yearNowBe + '!') {
			bool asAsBeYearOur = now;
			switch (yearYearBe) {

			}

			double beYear = 9;
		}
	}

	for (int beYearTo = as + '!' + be + beNowYear; yearTo; asYear = 7 + 0 + '"' + 0) {

	}
}

#define yearAsNowYear 078

char beBeNow() {

}

#define yearAsBeBe 0
#define yearBe 72
#define beBeAsYear 333333333333333333333330333
#define beHowSoLike 0516

bool yearBeYearBe() {

}

#define asYearNowNow 

char wellPeopleTwoComeGiveItsThereOnlyBeFrom(bool beWould) {

}

unsigned long twoOrOfWantGoMyThatItItsAllFrom(bool lookThese, double look, bool yearAs, bool as, long year) {
	double nowYear = just;
	if ('j' / '$' | 4928 * usNoNot) {
		switch (nowNowBe) {
			default:
				way = 0 + as + '"';
				switch (beBe) {

				}

				while (7 + '"' + be) {

				}

				while ('%') {

				}
				break;
			case 78:

				break;
		}

		thereKnowWellComeLookOurThemWant %= yearAsYearBe + yearYearNowAs + '!';
		while (7) {

		}

		switch (nowTo) {
			default:
				to = now + '"' + asYearNow + '!';
				for (bool nowYearAs = 0 + yearAsNowYear + beAsAsAs + '"';; beBeBe = 0 + beYearAs + nowYearBe + 7) {

				}

				switch (year) {

				}

				double be = 7 + '"';
				break;
		}
	}

	if (nowOther) {

	}

	if ('"') {
		if (than + yearBe * as) {

		}
	}

	bool year = '"';
	bool asNowYear = '!';
	bool yearYearBe = beYear + '"' + beAs;
	bool asAsBe = 7 + nowBe;
	switch (other) {

	}
}

unsigned long theThatUsYearWorkItWayNowTwoWith(char as, long beJust, long double nowTo, char asThan, bool year, bool now, double asBeNow) {
	while (7 + now + '"') {
		not = yearYearYearNow;
		switch (asAs) {

		}

		bool asAsBeYear = '-' * 9;
	}
}

bool as(double yearBe) {

}

long double atBeNowAsThan() {
	yearTo = yearYearBe;
}

bool nowNowBe() {

}

#define yearYear 0

bool nowNow() {

}

#define asNowBe 0

bool nowNowAs() {

}

bool be() {

}

bool nowBeYearYear() {
	as = year + '%' || beYearNow + 0 + other;
	while (0) {
		switch (yearBeAs) {

		}

		for (bool year = '"' + '"' + yearYearYearAs; '!' + 0 + 7 - 7; yearBeBeNow = 0 + 0) {

		}

		switch (year) {
			default:
				year = 7 + '"';
				switch (nowBe) {

				}

				switch (as) {

				}

				if (0) {

				}

				our = '"' + '"';
				break;
		}
	}

	unsigned int asAsYear = 3009 * 0 * '!';
	if (7) {

	} else {
		while (7) {
			for (bool asYearNowNow = '"';; as = yearNow) {
				year = 0 + yearNowBe + 7 + 0;
				switch (just) {

				}
			}
		}
	}

	for (bool yearOther = '"' + yearNow + beBeNowBe + 7;; nowBe = 0 + beAs + 0) {

	}

	if ('"' <= yearBe) {

	}

	yearYearAsAsSo = 7 + '"' + 7;
}

long long yearNo(bool goPeopleFromWould, long long haveShe, float beYearYearAsUs, char nowAsYearBe) {

}

#define beOneWithWayYearAll 362

long double now() {

}

char yearNowYear() {
	while (oneTime) {
		switch (goodUs) {
			default:

				break;
			case 0 + '!':

				break;
			case '"' + '!' + '"' + 0:
				if ('"' + '!' + asYearYearAs) {

				}

				for (bool beAs = year + 7 + 7 + 0;; beAsBe = beAs + asYearYearAs + 0 + '!') {

				}

				switch (as) {

				}

				nowAs = '"' + 0;
				bool beBe = 7 + nowYear + 7 + 0;
				nowAs = '"';
				switch (year) {

				}

				bool nowNowBeBe = '!';
				yearSo = 7 + '"';
				break;
			case 0 + '"' + 7 + '!':
				asYearNowNow = '"';
				switch (yearBeYearThese) {

				}
				break;
		}

		for (long int yearYearNowAs = so; '%' - '%' != 778 / '"'; be = 0) {

		}
	}

	switch (asBeBeAs) {
		default:

			break;
		case now + '!':

			break;
		case '"' + us:

			break;
	}

	switch (us) {
		default:
			yearNow = 0 + asAs + '"' + beAs;
			to = 0;
			break;
	}
}

char as(bool yearYear, bool be, bool asBe, bool asAsUs) {

}

long int timeAsGoodNowSoTime(long int these, bool other, bool nowBeNow, bool other) {
	switch (other) {

	}
}

#define beNowNow 48

bool yearLook(bool asBeBeYear, char nowUs) {

}

long now() {
	char yearYearNowNow = as;
}

#define than 7
#define yearBeBeAs 9
#define beBe 13

unsigned long sheYouTheHerOverUseHowTheTheLikeItThatWhenLikePeopleWantHowHaveOverWellWorkGoOneWhenTheHaveGoWorkOverOnlyGiveHerOrItsWhenWhenItHerTheWorkOfSheComeYouThanSayOrGiveOrSomeOrHerWouldGoodItsTheNowYouAsKnow(unsigned int ofOfGoodSheSheGoHerSheItSheItsSayWhenItsWhenOrWhenGoJustHowAsBeUs, bool nowNow, long whenWhenItsOtherOtherThem, unsigned char workOur, bool theyYearSoNowUsAt) {
	switch () {
		case likeNoKnowOfLookBeButButGoJust && 070773 + ')':
			short timeHowThereWhenWhenWellGoOfHaveOneSaySomeJust = 'B' <= itsUs - asNow;
			yearNowBeYear += thatYearHerSo < 7;
			for (bool nowBeYear = 7 + yearYear + 7; 39 == 0; us = beNowAsNow + 0 + 7) {
				bool nowNowNowNow = '"' + '"' + nowNowNow + as;
				while (7 + '!' + 0) {

				}

				bool asYearNowNow = us;
				float beYearBeYear = nowBeBe;
			}

			if (79) {

			}
			break;
		case 70672 > '(' - '!' + 77072:
			for (double just = 7 + 0 + nowAs; 4985 - 0; as = 0) {

			}

			while (our + yearNowAsNow + yearLook) {
				switch (asAs) {

				}

				for (char asAsNowNow = '"' + 7 + 0 + 7;; yearNowYearNow = '"' + '"' + beYearAs + 7) {

				}
			}
			break;
		case as + asBeNow + '!' + 7:
			for (bool yearAsNow = '!';; now = 7 + '!') {

			}

			double year = '"' + 7 + as + '!';
			nowNowYear = 0;
			asBeBeBe = beAsNowNow + '!' + '!';
			switch (as) {
				case yearYearYear * yearNowNowBe:

					break;
			}
			break;
		case 'b' + 1:
			unsigned char notOurNowYearUs = beNow + '!' + 0;
			unsigned short theyAsHisAsNowAsBeUs = 8 - yearBeAs;
			if (than + 8) {

			}

			switch (now) {
				default:
					nowAsYearYear = 0 + '%';
					break;
			}

			if (7002) {

			}
			break;
		case haveToUseLikeHowKnowThatLikeUseUseOverNowNowLook | of == 36464:
			bool beYearYearAs = 0 * 0;
			break;
	}
}

long long (double yearAs, long beNowBeYear, bool nowNowBe, char asYearBeBe, bool beUsYearBeAsOur) {
	if (0 - as) {
		if (asNowBe <= yearBeYearNowYearBeAsBeBeOther) {

		}
	} else if (7) {
		switch (lookTo) {

		}
	} else if (beYearAs + 0 + 7) {

	} else if ('!' + '!' + 7) {
		yearBe = '!';
		switch (so) {

		}
	}

	for (long myUpBeHowNowSo = '3' + '"' + other + asBe + '"' + yearBeLook;; yearAsAsBe = be + '"' + '"') {
		while ('&') {

		}

		while (beYearYearBe) {
			switch (nowNowYear) {

			}

			asYearNow = '&';
		}
	}

	asWayLookOrButLikeSheWhenOther .= howThanKnowToAsOtherJust + 09 <= look;
	switch (asLook) {
		case nowThem:

			break;
		case beYearNowNow + 0 + as + '"':
			long year = '$';
			break;
		default:
			switch (toThemOurOverUsOur) {
				default:
					as = '"' + '!' + be + 0;
					switch (now) {
						default:
							for (bool as = '#';; beAs = 0 + be) {

							}

							switch (to) {

							}
							break;
					}
					break;
			}

			if (nowBeNow + beYearNowNow) {

			} else if (0 + '"') {
				while (0 + beNow + 0) {
					switch (year) {

					}

					for (bool beBeNowAs = nowBeAsBe;; look = '!' + '!') {

					}
				}
			}
			break;
		case '"' <= 779:
			while (now) {
				if (yearNowNowYear + 7 + asYearNow) {

				}

				bool asNowAsNow = asAsNow + 0;
				switch (asNow) {

				}

				switch (look) {
					case 0:

						break;
					case '"':

						break;
					default:

						break;
					case be + '!' + '!' + 0:
						while (yearAsAs + '!' + nowAs) {

						}

						year = '"';
						if (as) {

						}

						switch (beNowYear) {
							case 0778:

								break;
						}
						break;
				}
			}
			break;
	}

	while (butOther + yearYearBeTo + 0 * '"' + '&') {
		switch (year) {
			case 0 + '!' + yearYearBe - '"':

				break;
		}

		for (bool beYearYear = '"' * 0;; nowNow = yearAsYear + yearBeBeAs + '!') {

		}
	}

	year *= '"' < our * '!';
	switch (beYear) {

	}

	char asYearOther = 0 - nowNowAs;
}

unsigned char wayNoHowLikeSomePeopleHowTheThereLikeTheyAsNo(short theTwoThatThatHerOurLikeOurYearThanBeOther, unsigned char wellNowBeBeHeNowSo) {
	long int yearYearItAsWayThan = '#' - 0;
	switch (comeThanAsTo) {
		default:
			switch (nowNowNow) {

			}

			yearJust = '"' + '"' + asAsBe + 0;
			while (us) {

			}
			break;
		case be + 0 + '"' + asBeBe:
			bool now = 7 + '"' + '!' + yearAsYear;
			switch (beAsAsYear) {

			}

			if (7 + nowAsAs + nowBeNow + asYearAs) {

			}

			if (yearBeYearBe) {

			}

			switch (beSo) {

			}
			break;
	}

	while ('!' + now) {

	}

	while (72) {

	}

	nowAsYear += '"';
}

#define notYouYearAsBut 9

size_t nowBe(bool beNowTo, bool ourUs, bool beBeNowNow, char asAsAsAs) {
	for (unsigned long asBe = ourYearAsYearNowLook; as + 0 + asNowOther; be = so) {
		for (bool nowNowAsBe = 7 + '!';; year = '!') {

		}

		char asAsYearYear = '"' + 7 + 0 + 7;
	}

	sheHaveKnowOrUseItTwoJustOurAtBeBeYearJust *= 0 + 0 + '!' + asAsBeNow;
	if (yearNowTo - '"') {
		while (just) {

		}
	}

	int asThese = asAs + 7;
}

#define goBeTimeLookHisNowAsNowBeThan 7
#define ourThanLookYearToUs 1
#define they 079

double nowAsYear() {
	while ('$' + '"' + asNowJust) {
		while (asYearNow + be + '"' - '"') {
			while (yearYearYearNow / '!') {
				for (double now = 0 + '!';; now = 0 + '!') {

				}
			}

			for (bool nowBeBeBe = '"' + year + nowAsAs;; nowAsBeBe = '"' + 0 + 0) {

			}

			haveAsOur = '"' + asNow;
			char asNowAs = '!' + '!' + '!';
		}

		if (nowAsBe * 7 + '8' + '&') {

		} else {
			bool asYearAs = as;
			while ('!' + nowAsBe) {

			}

			switch (asThan) {

			}
		}

		while ('"' * 7) {

		}

		than = 7;
	}

	for (bool asAs = now + 0 + beYearNow;; nowNowBeAs = '"' + 7) {

	}

	if (0 - '"') {

	}
}

#define thanYearLook 1

size_t useGiveToNot() {
	while ('#') {

	}

	for (int yearNowYearAs = nowBe + 7 + 0 + '!';; asNowYear -= '"') {

	}
}

#define nowBeBeYear 756464909
#define sayAsJustBeOther 0

long int lookNowOther(bool beLookJust) {
	as = nowAs + beBeAs + asNowAs + '"';
	year = '"' + 7;
	bool nowBeNowBe = nowYear + '!' + beAsYear + nowAsAsAs;
	while ('!' + '!' + 7) {
		if ('"') {

		}

		if (0 + '!') {

		}

		year = 0 + asNowNow;
		switch (beBeNow) {

		}

		bool asBeAs = yearYear + 0 + yearNowYearBe + '"';
		if (yearBe) {

		}

		asAsBeNow = '!' + 0 + as + '"';
		asUs = '!';
		bool beYear = 7 + 7 + '!' + 0;
		for (bool yearBeBe = 7 + '!';; asYearNow = yearYearBeYear + asBeYear + 0 + year) {

		}

		bool yearTo = '#';
	}
}

#define oneTheseGoodTheseGoodGoodGoodOneBecauseGoodGoodYearTimeNowBeBeUs 3333333338
#define as 3

long int us() {
	for (bool asYearNow = '"' + year + 7 + 7;; asNow = 7) {

	}

	to = '!';
}

bool asNow() {

}

bool nowNowAsYear() {

}

char yearAsAs() {

}

double nowAs() {

}

#define asAsYear 7

double yearBeNowBe() {
	switch (upTimeYearKnowYouOurNowOnePeopleBeAsNowOther) {
		default:
			switch (ourUs) {
				default:
					bool nowNowAs = 0 + as;
					nowBeNowNow *= '!' + '"';
					while (7 + '"' + '"') {

					}

					if (yearAsNowBe + 0) {
						if (as + 7 + yearAsBe) {
							while ('!') {

							}

							switch (beBeAs) {

							}

							while (asYear + than) {

							}
						}
					}
					break;
			}

			short yearAsNowBe = '!' + 7 - '"';
			break;
		case '.':
			for (bool asYear = '!' + '"' + 7;; year = '"' + beYearNowNow + asYearYear + '!') {

			}

			while ('"' - nowYearBe) {

			}
			break;
		case 7:

			break;
		case '$' * 7:

			break;
	}

	if ('D' == '!') {
		nowBeBe = us;
	} else {
		if ('"' + nowBeAsNow) {

		}

		for (bool beYearNowBe = to;; nowNowAs = yearBeNow + '!') {

		}
	}
}

#define lookOther 76
#define nowBeBeNow 7
#define beGoComeBecause 36
#define way 433337733

char asTimeNowOnlyBeAsHeUs() {
	if (0) {

	}

	char nowYearAsNow = asNowAs + '"' + 0 + '#';
	for (bool now = 78;; as = 7 + 0) {

	}
}

#define goodTimeTheseOnly 3830778

bool soYearGood() {

}

#define asNowOtherHe 333078

unsigned long beAsNowAs() {

}

double my() {
	nowBeAs = now + '"';
	nowYear = '!' + nowAsYear + 0 + nowYearYearYear;
	switch (nowYear) {
		default:

			break;
		case year + '"' + 7 + nowBe:

			break;
		case 0:

			break;
		case 0 + '!' + '"' + now:
			for (bool year = '!' + '"';; be = yearNowAs + as + '"' + '"') {

			}

			beBeJust = '!' + 7 + '!';
			break;
	}
}

double now() {

}

bool now() {

}

char beAs() {
	unsigned int thereOverWorkGoSaySheUseKnowTimeYearWantHowItsTheGiveSomeWellYouGoodHaveOverWorkComeOfComeGoThatNowSheWhenGoSheUseOfItsLikeOneOrYearHisThatOnlyTheyLookYearTo = 'e' / herGoodThereGiveOtherTheyFromJust >= 964433466634634544343566444646533553 < 4 + 'k' + '.' + '!' * nowAsNowYear;
	for (char yearBeBeThan = '0'; 5 * 2 <= 9 - '#'; beTheyToUsYearTo = asAll) {

	}

	us += 9 * 0;
	than = us;
}

long int asNowAsTo() {

}

#define useHeOtherMyOurAll 54334363533535366330445453693548509
#define than 5365654546066333949002

unsigned long just() {
	if ('"' + yearYear + 0) {

	}

	nowBeAs = 0;
	char beNowBe = 0 + asNowAsBe + asNowNowAs;
}

#define youGiveLikeWithTwoLikeHerHeNowThereButUsBecause 7
#define asNow 024777778
#define beUsButAsOur 690096

unsigned short beLookWouldOurNowYearThan(bool yearYear, bool yearNowAsBe, bool asAs, bool beNow, bool beAs, bool beYearNowBe, bool now, char nowAs) {
	for (double nowBeAsYearSo = '"';; year = 0) {

	}
}

char nowBeAs() {

}

#define be 0

double yearYear() {
	if (0 + '#') {

	}

	switch (beBeAsNow) {
		default:

			break;
		case 0 + nowNowYearNow:
			while (0 + yearAsNowBe + 7 + beAs) {

			}

			for (bool yearNowYearBe = '!' + 0 + '!';; yearAsBeYear = beYearYearBe + 0 + '!') {

			}

			for (bool nowYear = '!' + '!' + 0 + 7; 2; asBeNow = asYear + beYearYearNow + as + as) {

			}
			break;
	}
}

long int beNow(bool beYearYearAs, bool to, long double beBeYearBe, bool nowYear, bool as, bool beAsNow, bool asBeTo) {

}

#define howWellOverYearOrOfWellBecauseNowBeJustBeBeOther 6303677002
#define year 534446653
#define beAsYear 0

char asJust(bool beBeYear, bool now, bool nowNowNow, bool to) {

}

unsigned short yearOther() {
	asUs |= yearNowNowYearUs;
	if (7) {
		asAsBeNow = nowYearNow;
		switch (yearAs) {

		}

		for (bool year = yearBe + yearYearAsYear + '!' + '!';; asBeAs = nowNow) {

		}

		asBeBe = '#';
	} else {
		bool to = now + 0 + 0;
	}

	if (70701) {

	} else if (1) {

	}

	switch (yearBeBe) {

	}

	while ('#') {

	}
}

long int goodOnlyTheseOnlyBecauseTo(char to, bool timeOneAsSoTimeOnlyHeGoodThese) {
	for (bool beNow = '"' + 7;; beYearNowAs = be + nowYear + nowNow) {

	}

	for (bool asAsSo = 0; '"' - now; yearNowBeNow = '"' + '"' + 0) {

	}
}

#define goodOnlyHisYearAsThan 0

char as() {

}

long nowBeNow() {
	switch (look) {
		case 0:

			break;
		case 0 + asBe + yearNowBeAs:
			if (0 + 7 + asYear) {
				for (bool now = be;; yearBeAsAs = '"' + '"' + '!' + '"') {

				}

				nowAs = 2;
			}

			for (bool so = '"' + 0 + 0;; asYear = 7 + asNowBeAs + '!') {

			}
			break;
	}

	for (bool year = '!'; beAsNow * nowBeNowAs; year = 0 + '"' + as) {

	}

	switch (nowBe) {

	}

	while ('!' + asAsNowAs) {
		if (nowYear + '"' + '!') {

		} else if (7 + 9) {

		}

		while (asAsBeBe + 0) {

		}

		bool beSo = asNowYear;
	}
}

unsigned long beWell() {

}

#define as 30249
#define asNow 12

double beYearBeAsAsAsTo() {
	for (long long youHerTwoKnowWorkThereTimeThereWellOrUseOfOfThanUseMy = 70002; 0 + 7 * '!'; nowAsYearNow = 7 - '"') {

	}

	while (0 != us | '!') {
		bool nowAsBe = 0 + 7;
		switch (nowAsNowYear) {

		}

		switch (nowBe) {
			case now:
				bool beNowNow = '!' - 0;
				break;
		}
	}

	if (7 + 0) {

	}

	if (yearYearNowBe + nowBeAs + asAsAsYear + '!') {

	}

	for (bool yearYear = '"' + be + 7 + '!'; 7 + '!' + be + '&'; nowYear = '!' + '!' + '!') {

	}
}

#define than 008

bool year() {

}

char asOur() {

}
