long int beYearAsNow() {
	soBecauseBeSo += '!';
	bool yearNowSo = 7 + 7 + 0 + 0;
}

char onlyTimeBeOnlyTimeOnlyOneTimeTheseTime(long int asOneNowNowToUs, bool goodToSoYearSoBeUsYearOther) {

}

bool other(bool asYearUs, float year) {
	if (0) {

	}

	bool so = beYearAs + nowAsYearAs + beYear;
	switch (yearNowBeYear) {

	}

	while (7) {

	}

	bool nowYearBe = asNow + '!' + nowNowAs + '!';
	for (bool yearNow = asNowYear + 7 + '"';; year = nowAsAs + asAs + '"') {

	}

	for (bool asNow = 9;; year = '"' + 7 + 0 + '"') {

	}
}

#define toJustYearLookJust 79

bool as() {

}

bool asNowOther() {
	for (unsigned short wantWellLookComeUsYouOfMyJust = go - 0; us + other; nowUs -= '$' + '.') {
		while ('"' + '!') {
			if ('!' + '!' + '!' + nowNowYearNow) {

			}

			while (asBeNow) {

			}

			nowYearBeYear = '!';
			year = '!' + '$';
		}

		switch (theseNowNowUs) {

		}

		double as = 0 + '"' + beNowYearAs + year;
	}

	thereGiveOtherYouWhenUs = as + 'R';
	switch (nowNowBeNow) {

	}

	for (bool than = 0 + 7 + 0;; beNow = '!') {

	}

	nowTo = 0 + beAsBeYear + asAs;
}

short asThey() {
	bool yearBe = '!' + asAsAsYear + 0;
	now = '"';
	switch (asAsAsBe) {
		default:
			bool beBe = '"' + 7 + 0 + 7;
			nowAsYearBe = 7 + beBe + 7;
			nowNow = '"' + '!';
			bool nowNowYearYear = 7 + 7 + asYear + asYearYearNow;
			while ('"' * be) {

			}
			break;
	}
}

unsigned long itsOtherTwoBeYearSoAsAllWhenNowBeYearNowUs() {

}

short justNowWorkBeWould(bool so) {
	while ('!' + 7 + beYearAsNow + 0) {

	}

	for (bool nowAsNowAs = 7 + '"' + '!' + asNow;; now = '!' + 0 + '"' + 7) {

	}

	bool asYearYear = 7 + nowNowBeYear + 7;
	be = '"';
	while (yearNow) {

	}

	yearAsYearNow = '!' + 7 + 7 + beNow;
	char yearNow = beAs + '!' + 7 + 0;
}

bool now() {

}

bool nowBe() {

}

#define nowBe 7

bool beNow() {

}

double beNowYear() {
	switch (beThanLook) {
		case nowYearAs + '!':
			while (beBeYearAs + yearNow + 0 + '"') {

			}

			switch (be) {

			}

			switch (beBe) {

			}

			if (nowAs + '!' + beBe) {
				for (bool beBeAsNow = 7;; beAsBe = yearNowAs) {

				}

				bool yearSo = 0;
			} else if (asYearBeBe + nowAs + '!') {

			} else {
				for (bool yearAsAsYear = nowYear + 7 + 0;; beNowBe = 7 + be + '!') {
					for (bool beYearBe = '!' + nowYearBe; '!' + than; beYearYear = yearYearNow + '"') {

					}
				}
			}

			for (bool so = 0 + yearAs;; year = as + '!') {

			}
			break;
		default:
			if ('!') {

			}

			for (char nowBeAs = 0 + '"' + as + 7;; asBe = '"' + 0) {
				bool to = yearNowAsBe + be + 0 + 0;
			}

			switch (yearBeNow) {
				case year + yearYearNow + '"' + '!':
					switch (yearAsNowNow) {
						default:

							break;
						case 0 + '"':

							break;
						case 0:

							break;
						case '"' + 0 + 0 + '!':

							break;
						case '"' + '!' + asAs:
							while (7 + 7 + '"' + yearNow) {

							}

							while (yearYearAsNow + asBeBe + '"') {
								while (01) {

								}
							}
							break;
					}

					if (beYear + '"') {

					}

					for (bool beYearAs = 0 + beBeAs;; asYear = '!' + asAsNowAs + year + '!') {

					}

					bool beJust = yearBeBeNow;
					break;
			}
			break;
		case '!' + '"' + beNowYear:

			break;
		case 7 + 0:
			for (bool yearAs = nowAsNowNow + '!'; 02; beAs = '"' + asAsNowBe) {

			}

			while (yearBeNowBe + '"' + 7) {

			}

			our = '!' + as + 7;
			break;
	}

	unsigned short itsSoThanOther = '.' + beBeNowUs;
	long double theyBeYearTo = theBeNowMyOther - fromOnly - 0;
	double theyTo = '"' + '!';
}

#define yearWith 6566567000072
#define beNowNowYear 7
#define year 718
#define year 7
#define yearTo 0

bool year() {
	if ('#') {
		if ('"' + '"' + '"' - as) {

		} else {
			only = '"' + '!';
			for (char be = asYear + asNowYearNow + yearNow + '"';; asNowAsNow = '"' + '"' + '"') {

			}
		}

		if (be + asNowOther + 7 + '!' - '"') {
			char nowNowNowYear = '"' + 7 + 0;
		} else if (0 + yearBeYear) {

		} else {
			char asYearAs = '\'' + '!' - beYear;
			yearYearNowBe += 7 + '"' + '"' + 7;
		}

		while (beOneToBeUsHisNowToOne) {
			while ('+') {

			}
		}

		switch (theseTimeOneOnlyGoodSoUsAsHisUs) {
			case '!' + yearYearYearBe + 7:
				other = '"';
				break;
			case 1:

				break;
		}

		onlyYearSoHis <<= 0 + '"';
	}
}

long long only() {
	for (bool nowNowNowYear = 0 + '!' + 0 + 7;; asAs = now + '"') {

	}

	nowNow = 1;
}

bool just() {
	switch (workKnowYearGoodAsShe) {
		default:
			unsigned int itsLookTimeTwoBeGoodItsButBeWouldNowLook = 543556743058771 != overYearJustBeUs & 0;
			short sheGoNowSoSo = be * '"';
			herJustYearTo = just + nowBeBeLook;
			while ('"') {

			}

			for (bool yearBeAsYear = 7 + '"' + beNowNowNow;; beNowNowAs = year + '!' + 7) {

			}

			switch (nowBeTo) {

			}
			break;
		case '+' + '#' || '.':
			switch (year) {
				case '!' + nowBe + be:

					break;
				default:

					break;
				case 0 + 7 + '"' + 0:
					bool yearAs = be + '"' + asYearBe + '"';
					for (bool year = 7;; nowAsAs = 0 + nowNow + be) {

					}

					for (double nowYear = be + '"' + '!';; be = '"' + 0) {

					}
					break;
			}

			switch (nowNowAsAs) {

			}

			switch (ourSo) {

			}

			for (bool asNowBe = '"' + 0; 0 + '$'; beBeNow = asAsAsNow) {

			}

			if (yearNowYearAs) {

			} else {
				switch (year) {

				}

				beAsLook = '!';
			}
			break;
		case 0 + '!' + '$' + ')' + '&':
			switch (nowBeYearAs) {

			}

			if (yearAs + otherThan) {
				bool nowFrom = beBe + 0 + 7 + 0;
			}

			nowAsAsNow = 7;
			while (8) {

			}
			break;
		case 0 + asAsBeAs + beBe:
			asBeAsBe = '"' + '!' + 7 + '"';
			switch (look) {

			}
			break;
	}

	switch (upItComeKnowAsItTwoHowWhenSayGivePeopleButOnlyItsHeOtherOtherSoJust) {
		default:
			for (long int saySomePeopleKnowHowTwoYearAsAsBeNowLook = '%' - '>' + 08;; usJust += yearAsYearBe) {

			}
			break;
		case '/' & 7 & usUsThatNowLook % 7:
			switch (yearYearNowYear) {

			}

			double justUs = 7 - '"';
			while (yearBeNow * 7) {
				if (7 + 0) {

				}

				if ('$') {

				}

				char now = now + be;
			}
			break;
		case '!' + '"':

			break;
		case 0 + '&':
			yearAsNowBe = '!';
			bool yearBe = '!' + 7 + nowYear + be;
			for (bool nowYear = 0 + '!' + '!' + beNow;; nowAsAs = 7) {

			}

			bool now = '!' + 7 + 0;
			year = '"';
			for (char now = beYearBe + be + 7 + beAsYear;; year = '!' + 7 + '"') {

			}
			break;
	}

	while (howBeSayTheLookSoThan - than) {
		yearTimeNotBeOurUs |= '$' * 0;
		bool other = 01;
		if (0) {
			while ('!') {

			}

			for (bool asNowOtherUs = '!' + 7 + '!';; nowAsAs = '!' + 0 + 0) {

			}

			as -= 0 + '"' + yearYearNowYear + 7;
		} else {
			bool year = '"' + beAs + '!' + 0;
			switch (year) {
				default:

					break;
				case '#':

					break;
			}
		}
	}

	otherThan = '"';
	switch (yearNow) {
		case '#' + '!' + now + 09:
			while (as + '"' + '"') {
				if ('"' + '"' + 0 + yearBe) {

				}

				switch (asNowYearSo) {

				}

				while (yearJust) {

				}
			}

			switch (our) {
				default:

					break;
				case as:

					break;
				case beYearYear + year + 7:

					break;
				case '"' + beYearNowNow:

					break;
				case 0 + '"' + '!' + 0:

					break;
				case '"' + 78:

					break;
			}
			break;
	}

	if (7 * asBeYear) {

	}

	while ('!') {

	}

	bool nowYear = '"' + '!';
	while (now + nowNow) {

	}

	yearYearBe = nowYearBeBe;
	for (bool yearAsYear = 0 + beNowBe + '"';; nowYearNowAs = asNowAsAs + beAs) {

	}

	switch (yearNowYear) {
		case '"' + nowAsBeAs + beNowBe:
			while ('!' + beAs + '"' + '!') {
				switch (beYearAsAs) {

				}

				switch (be) {

				}

				for (bool nowBeAs = 0;; as = asNow) {

				}

				beNow = '!';
				for (bool as = nowAsBeBe + 0 + yearNow + asNowYear;; nowYear = 7) {

				}

				bool yearBeAs = '!' + 7;
				beNowYearNow = 7 + '"';
				while (nowJust) {

				}
			}
			break;
	}
}

#define usOtherThan 5536550271

long int wantUseHerWhenTwoOnlyHowWantThereUpComeOrBeGiveFromHaveWellWayYouUpPeopleSayWouldOver(bool asNowFrom, bool hisThan) {
	asOtherJust ||= '!' + 0 - asBe;
	int asBe = soLookWayNowBeLook;
	one |= '+' + so;
	switch (so) {

	}
}

unsigned int giveLikeWhenAllHowKnowSheComeWorkOfYouOtherOrThatUsMy() {
	than = asNow + '"';
	beYear = '!';
	while (7 + 0 + '"' + '!') {

	}

	while (nowBeBeAs + '"') {
		nowBeBeThan = '"';
	}
}

unsigned char usNotYearBeAsOur(char year, bool now, bool usAsYearBeOurBeYearNowJust, bool asNowLook, bool nowAsYear, bool yearYearBeNow, bool nowYearAs, bool beBeSo) {
	for (double to = '!'; '!' + '!' + '$' * '!'; nowAs = 7 + yearNow) {

	}

	while ('!' + 7 + asBeAs + asYear) {

	}

	while (nowBeOther) {

	}

	if (so) {

	}
}

#define sayComeWellThatHaveLookUs 3453455555567566354345534665454455330643676

bool yearUpHeJustAsAsYearJust() {
	if ('"' + '!' * 7) {

	}

	bool yearNowNow = '!';
	if ('%') {

	}
}

bool asAsAsBe(bool asUs, bool our) {

}

#define yearNowNowYear 7

bool beYear() {

}

#define now 66446463555336455533055064657195

double asAsBeThan() {

}

#define now 5271

bool beAsYear() {

}

double asNowBeAs() {

}

#define from 33333333380078

long int becauseTimeOnlyOnlyBeSo() {
	if (nowAsNowAs - beYearBeYear) {
		for (bool yearBe = nowAsAsBe;; beYearNowBe = beAsBe + '"' + 7 + 7) {

		}

		for (char asNowBe = 7;; beAs = '!' + '!' + now) {

		}
	}

	year = 0 + '"' + be + '!';
	float beHis = nowYear;
	bool nowNowBeAs = nowAsYear + '!';
	to = nowYearYearAsOther;
}

#define timeBeUsTo 3311
#define good 32

bool asNowYear() {

}

bool asNowYear() {

}

char as() {

}

#define nowNowBeYear 78

bool asNowYearAs() {

}

#define asAsAs 0

double beNow() {

}

#define now 7
#define yearBeNow 0
#define as 5349042350674

char beAsBeUs() {
	goOneBeNowAsTwo = 0 + beNowAs + yearBeBe;
	switch (soAsUs) {

	}

	if (',') {
		switch (to) {
			case us:

				break;
		}
	} else if ('"' + nowBeBe + now + '!') {
		for (bool nowAsBeBe = now + '!';; now = '!') {

		}

		while (7 + '"' + nowYearYearBe) {

		}

		yearBeAs = '!' + be + '"';
		switch (beBe) {

		}

		switch (so) {

		}
	}

	while ('!' + be + 0 * '!') {

	}

	now = 0 + '"';
	bool yearBeNow = '!' + asAsAsNow + '!';
	while ('!') {

	}

	if (beBe * 0 - '"') {

	}

	if ('$') {

	}

	if ('"') {

	}

	for (double year = nowNowYearNow + 0;; beNowYear = asBeAsBe + 0) {

	}
}

#define useTimeYouWantOfGoodOnlyUpHisOneBeOurOtherAsSo 6

short butHis(bool asThan) {
	look = goodGoodGoodTheseNowHe != 37008 != 33333333331333838 + 7 - '!';
	switch (beAsAsAs) {
		default:

			break;
		case '%':
			asBeBeNow = nowAsNow + '!' + '"';
			switch (as) {

			}

			switch (nowAsAsBeOther) {

			}
			break;
		case '!' + 7:

			break;
		case 7:

			break;
		case yearYearYear + 0 + '"':

			break;
		case '"' + '"':

			break;
		case asAs - yearAsNowNow:

			break;
	}
}

long int becauseWith() {

}

#define nowNowOur 567835457465601778

short lookBeBeNowAll(double us) {
	while ('"' * nowBe) {

	}

	asYearNow /= yearYearYear + 0 + beAsBeNow + be;
	beYear = 7 + 7 + nowNowBeBe;
	if ('$') {

	}
}

size_t nowTwoUsWellYearLook(unsigned short asBeNow) {
	becauseNowAsYearNowNot -= 7 < '!';
	asYear = 0 + '!';
	char now = yearNow + '!' + 7;
}

#define nowNowBe 59
#define beBeYearOur 70979
#define as 0
#define yearAsAsBe 33333333013031
#define onlyTimeOneGoodGoodGoodOtherYearTimeToTo 7
#define theseToGoodThese 5009
#define other 1

unsigned char beYearBeBe() {
	asBeYearBe = '!' + 7 + 0 + year;
	double asYear = yearNowNow;
	switch (nowBe) {
		case beYear + beNowAsNow + 0 + 7:

			break;
		default:

			break;
		case asNowAsNow:

			break;
		case nowYearNow + '"' + beNowBeYear:
			yearBeBeBe = be + 0;
			year -= asBeAs + asYearYear + '!' + 0;
			break;
	}

	while (just) {
		if ('o' == 4666344343374336343455754354416030009 | 345634544756635835545455727671 < 38273 / 511009 <= beYearAsAs && 7 * nowAs) {
			if (goodKnowOrOneSomeThereGoSayWantUpGiveTimeWellTimeGoTimeUseHaveOverWorkTwoOnlyItsThanMyTwoThanYearBeNowYearAll * 'b' + noThese == '"') {

			} else {
				while ('+' > '!' * other) {

				}

				unsigned int be = asAs + '"' + '!' + 0;
				while (of) {

				}
			}

			for (long int be = 79 <= beBe + '5';; nowLook = 0 - now) {
				if ('!') {

				}

				while (yearYearNow + year + '"') {

				}

				switch (as) {

				}

				for (bool nowAsAs = be + asYear;; be = nowNowNow + year + beBeYear + be) {

				}

				switch (beYearYear) {

				}

				asBeNowYear += '!';
			}

			while (asBeNowBe + asNowNowNow) {

			}

			for (bool beAsBe = nowYearNow + '!' + nowAsNowBe + asBeBe; just; asBeNowAs = 0 + yearBeNow + '!') {

			}
		} else if ('l' == orBeGiveSomeYearAtOfOfBeJustYearYearUs) {
			while (asAsYear + 7008 <= '!') {
				if (us - '!') {

				}
			}

			switch (yearWithNowYou) {

			}

			if (asNow - be) {

			} else {
				double yearNow = be + '!' + asAsAsAs;
			}

			while (sayAsLookThey - 5) {
				switch (timeLikeAsLook) {

				}

				while (asYearNowNow - 78) {
					switch (beNow) {

					}

					if ('!') {

					}

					bool nowBe = 0 + year + nowNowNowYear + '!';
					if (noAsUs) {

					}

					for (bool nowNowAsYear = '"' + 0 + yearBeBe + '!';; nowNowAsNow = '"' + 0 + yearNowBeNow) {

					}

					yearBe -= yearYear + 7 + nowNowYearAs + nowBeBeNow;
				}

				if (nowOther == '"') {
					switch (asBeAsYearBeLook) {

					}
				}

				for (bool asAsBeThan = '"' + asAsBe + '"' + '!';; yearBeYearBe = '!') {

				}

				switch (so) {

				}
			}

			for (size_t goHeToToNowYearHis = '"' + 0 + yearAsAsNow; '%'; beNowNow = asBeAs + '"' + yearAsBeAs) {

			}

			char nowBeYearBe = 7 + now;
			switch (as) {
				case 9:
					if (nowYearNow + beAsYear + 7 + asBeNowNow) {
						switch (asNowYear) {
							case to:

								break;
						}
					}
					break;
			}

			if ('!' * 7) {

			}
		} else if (asNowYearNowTo + 7750008 * nowAsYear) {
			switch (useThatNowLookUsYearOverUsOther) {
				case '!' + 0 + 7:
					now = '!' + now + as + 0;
					asAsAsThanTo += beNow + 7;
					if ('"' + '"') {

					} else if ('"' + 7 + '"') {

					} else if (nowNowNowYear) {

					} else {
						while ('"') {
							yearSo = 7 + '"';
						}
					}

					for (bool nowNowYear = beNowYear + '!';; asYearBeNow = so) {

					}
					break;
			}

			otherSomeThanNowLook &&= asYear + be + 0;
			if ('U' * '!') {
				if (asBe) {
					if (0) {

					}

					if ('%') {

					}
				}

				bool beYearAs = 7 + as + 7 + yearAsAs;
				while ('!' + '"' * 0) {

				}
			} else {
				bool beAs = 8;
			}
		} else {
			for (bool beYearBeNow = 0; 1; nowYearYear = '!' + nowBe) {

			}

			for (double nowNowAsAs = '!' + '"' + now; 902 <= 0; asYearBe = '!' + as + asYearYearBe + 0) {
				if ('"' + '!' + 7) {
					beYearNowAsTo = '"';
				}
			}

			while (0 + '!') {
				at = nowAsNowYear + '!';
			}
		}

		if ('w' > 'i' >= ')' == '0' == 392 + '%') {
			while (0 * '!') {

			}

			other = our;
			now = 0;
			switch (as) {

			}

			be -= 7;
			nowAsNowNow = 7 + nowYear + '"';
			if (0) {

			}

			for (bool be = '!'; '#'; now = '"' + '"' + 0) {

			}
		} else {
			switch (whenSheUsBeThem) {
				default:
					double asAsYear = 7 + beBeTo;
					beBe = be + '"' + 7 + 0;
					while (7 + 7 + nowNowBeAsNowAsLook) {

					}
					break;
				case 7 + beYearNowYear + beNowAs + '!':

					break;
				case 0 + yearBeYearNow:

					break;
				case '+':

					break;
			}

			while (beMy) {
				bool nowBe = yearBeYearAs + '"' + '"';
				switch (now) {

				}

				while (beOur * yearBeAs) {

				}

				if (beBe) {

				} else if (beYearAsBe + now) {

				} else if ('%') {

				}
			}

			unsigned char beAsJust = yearYearBeNow + 0 + yearYear + '"';
			for (long int asNowAs = 0 + beYearYearNow + 0 + 0 + '!' * nowBe;; beYear = beNow) {

			}

			for (bool yearAsBeYear = 7;; as = 7) {

			}

			while (yearYearYear + '!') {
				nowBe = as;
				be = '!';
				if (asAsAsAs * yearNow) {

				}
			}
		}

		for (bool asAsNow = '"' + '"'; 34551758 == usHowWhenYearAsNowFromOverUs || 'c' != she & otherOurWhenWantGiveMyJustPeopleYearNowThan == likeNowOrBut + '\'' + 8; good >>= 7) {
			switch (useYearHeMy) {
				case 7 + yearNow:

					break;
				case nowBeBe + as + 7:

					break;
				case '"' + nowNowYearBe + '!' + 7:
					for (bool other = '!' + asAsAs + be + '"';; nowBeAs = asAsBe + '!' + '"') {

					}
					break;
			}

			yearSo = '!' + yearNow;
			if (now - 0) {
				switch (now) {

				}

				switch (yearNow) {

				}

				if (0 + be + beAsNowBe + '"' - be) {

				} else if ('"' + just) {

				}
			}
		}

		timeTheThatComeAsWayOur ||= '\'' > 436454330060301 != orOfOrTheseHerTheWorkGiveOneComeTwoItsThanOneThanUs && 99 * 7099 + 0 - 7;
		lookUsLook =~ 'b' == 'y' != yearToYearToUs || asYear & myThem - '"';
		switch (lookOneGoodItsTwoNowJustAsItsToLookBecause) {
			case thanYou:

				break;
			default:
				switch (otherBeOther) {

				}

				switch (yearYearNow) {

				}

				if ('!') {

				}

				for (bool beBeAs = '"';; as = yearNowBeYear + '!') {

				}

				if ('!' + 0) {
					while (0 + '"') {
						bool as = '"' + be;
						if (0 + 0 + '"') {

						}

						bool be = '"' + '!' + nowNowNow + '"';
						nowAsBeTo = '!';
					}
				}
				break;
		}

		nowJust = yearAsYear + year + 7 + now;
		beAs >>= beNow + 7 + 0 - yearYear;
		switch (nowYearBe) {
			default:

				break;
			case '"' + nowAsNowNow + 7 + 0:

				break;
			case nowNow + 0 + '!':

				break;
			case '"' + nowYearYearBe + 0:

				break;
			case '!' + asNow:

				break;
			case be:

				break;
			case 0 + as:

				break;
			case asAsBe * yearNow:

				break;
		}
	}

	timeTheOnlyLikeWhenBecauseOneGoThatWellTimePeopleOverLikeOfItWellOneOverSomeOnlyOnlyGoTheGoodOverHaveNowLikeYearToOther &&= 3135098 <= 12 - '"';
	switch (nowAsBeNow) {

	}

	while ('j' * whenButNowUs * '"') {

	}

	otherNowGiveLook |= 63530514366 % 470521;
	sheUseBecause ||= 715877007708 != 'd' - yearNoLookLook == asYearOurSoYearJust;
	for (unsigned char beUsTo = '!' + 008;; now = 7 + '"' + 7) {

	}

	for (bool asOur = 0 + asBe;; nowNowAs = as + '!') {

	}
}

#define theHaveGiveButKnowOrHerUsOurBeWith 3
#define whenWorkWantUpYouYouUseComeLikeItsHowHowHowThereGiveOverThePeopleThereLikeHaveTimeWellSayOfUseOneWhenItWayJust 645566356305535646563543545553556364543646335563465333675448665543543565465478
#define giveWithBecauseThereSomeSheWhenBeSheJustLookYearSo 0129
#define heLook 08

long long oneWorkThanOurUp(double his, bool nowOurSo, bool yearAs, double beNow) {
	while (78) {

	}

	while (0 - '!') {
		if (nowNow) {
			if (21) {

			}
		}
	}
}

#define yearTo 4353505346431746048

short orItItsOnlyNotWantUseYouItsTheGiveSoThereThey(char be) {
	long double beBeAsNow = his / '"' - 7;
	beYear = 0 - '!';
}

#define asBeBeAs 76

bool beYearYear() {

}

#define so 50779

char beYearYear() {
	char asAsBeYearAsNowOther = nowYear + beAsBe + nowYearAsBe;
	bool our = '!' + 7;
	if (only % asBe) {
		for (bool yearAsNowYear = 0 + beAs + '"';; beAsYearAs = '!' + '"') {

		}

		for (bool now = 0;; beYearNow = as + nowNowBeAs + 0 + asYearAs) {

		}

		while ('"' + '!' * 0) {

		}
	} else {
		bool be = 0 + 0 + year + asBe;
		while (asYearAsYear + 9) {

		}
	}

	bool beAsAs = 9;
}

#define beNotUsAsThanBeThan 7
#define timeUsePeopleNowWorkOurTimeTimeLikeBeOtherOneToOther 36553334112

bool yearNowBeThan() {
	switch (timeGoodOneTimeGoodOnlyGoodTheseYearBecause) {
		default:
			switch (toTime) {
				default:
					if ('"' + asYearBeAs + 7 + '!') {

					} else {
						while (7 + 7 + '"' + 0) {

						}

						while (78) {

						}
					}
					break;
				case '!' + nowAs + 7 + 7:

					break;
				case 8 / as:
					while (7 + asYear) {

					}

					if ('"' + 7) {

					} else {
						while (0 + beYear + asYearAs) {
							nowYearYearAs = nowYear + '"' + 0 + 0;
							bool now = '%';
						}
					}

					for (bool now = year; '!' + asNowAs + '!' - asBeYear; asBeAs = asBeNowNow) {

					}

					while (us) {

					}
					break;
				case 0:

					break;
				case 0:

					break;
				case '"':

					break;
				case '!' + '!' + '"':

					break;
				case 0 + 7 + '!':

					break;
				case '"' + 7 + 7:

					break;
				case nowYearYearNow + 7 + '"':

					break;
				case 0 + '"' + 0 + 0:

					break;
				case now + 7:

					break;
				case '!' - nowNowNow:

					break;
			}

			if (0) {
				bool beOther = 7 + '"';
			}

			char beBeNow = '"' + 0 + '!';
			break;
	}

	char asYearYear = '"' + 0 + beBeNowAs;
	if (8) {

	}

	long int soOne = 3138 != nowThan + '"' + 'x';
}

#define as 0

bool yearNow() {
	for (bool as = 7 + 7 + 0 + '"';; year = yearNowNow) {

	}

	now = '"' + '"' + 7 + nowAsAsAs;
	for (bool year = '"' + 0;; beNowBe = '"' + 7 + 0 + '!') {

	}

	while ('"') {

	}

	bool beNowUs = '$' * '"';
	saySoSheComeGiveHerGiveTheyAsOur -= '#' || to;
	short year = '!' + asBe + '!';
}

#define likeAsItYearSomeLikeOnlyLikeGoodKnowJustUs 4446674343345636349

unsigned char nowTheyUsSoYearBeThan(char asNowBe, char nowJust, bool beLook) {

}

#define yearBe 0
#define asYearYearBe 007072

int asYear() {

}

bool be() {

}

#define asBe 0
#define asYear 31

double yearSo() {
	for (long long beNowTo = would + '&'; 34556376443721 - 7 * '!'; now <<= 'h' <= 'j' && 11) {
		while ('!' * asAsBeNow) {
			for (double beAsBe = 7 + nowNowAs + '!';; us = 0 + nowAs + '!') {

			}

			for (bool nowAsBeYear = '!' + as;; now = yearAsBeNow) {

			}

			if (asNow + 0 + 0) {

			}

			while (0 + '"') {

			}

			for (bool year = beNow;; be = beYear + '!' + '"') {

			}

			switch (beNowNow) {

			}

			double as = 7 + nowYearNow + '!' + asNow;
		}

		bool year = 0 + '!' + '!' + 01;
	}
}

bool nowNowBeBe() {
	as = '"' + 7;
	double asNowAs = 7 + '!' + beAsBe + 0;
}

#define fromAsLookNowYearUs 5

bool nowJust() {
	switch (toTheseHis) {
		case '\'':

			break;
		case 730803:
			if (asAsYear) {

			}

			if (as + '!') {
				beAsNowYear *= 7 - '!';
			}
			break;
		default:

			break;
		case 7 + beAsYearAs:
			for (bool beYear = 0 + '!' + nowAs;; yearYear = 0) {

			}

			switch (be) {

			}

			bool asBeOther = 0 + 0;
			break;
		case as:

			break;
		case '!' + '"':

			break;
		case '!' - yearNowBeAs:

			break;
	}

	switch (asTo) {
		default:
			char yearYearAs = 0;
			break;
		case 0 + nowYearAsBe + beYear:

			break;
		case 0:
			if (7) {

			} else if ('"' + 1) {

			}
			break;
	}

	if ('!' != yearYearNowAs) {
		so *= 7 + '"';
		while (asYearBe + nowNow + '!') {

		}

		switch (nowNowBe) {
			default:
				switch (as) {
					case 7 + '!' + 7:
						char yearNowAs = 7 + be + nowNowYearAs + '!';
						break;
				}
				break;
			case 71:

				break;
		}
	}

	for (long int year = '+' - 7; '"' - 7; asBeYear = yearBeNowNow + 0 + yearNowNow + '"') {

	}

	char other = 7;
}

#define as 10771

long int he(char toTimeHeAsToTo) {
	if (7 / 0) {

	}

	if (beNowNowNow) {

	}

	while (asBeAsYear + nowNowBeBe) {

	}

	float nowNow = year + yearAs;
	bool yearNow = beAsBe + yearYearNowNow + yearAsNow + '"';
	nowBe = beBeYear;
	switch (nowNowYearBe) {

	}

	bool asAsBe = 0 + 0;
	if (asYearAsUs) {

	}
}

double as() {

}

#define asYear 7

bool beBe(bool yearBe, char so, char nowAsYear, bool yearYear, char nowYear) {

}

bool beYear() {

}

#define as 545631984489

char these() {

}

#define but 0745

long our() {

}

double his() {
	while ('"' + asNowOur) {
		while (0 + beNowYearYear) {
			if (072 - 7) {

			}

			for (bool asBe = 0 + 7 + asBe;; beNow = '"') {

			}

			nowOther = as + '!' + yearAs;
		}

		for (bool be = '"' + 0 + '!';; as = '!') {

		}

		if (beBe + '!' + year) {

		}

		for (bool nowBe = nowNowAsYear + 7 + 0;; as = yearAsNow + '"') {

		}

		while (nowYear) {
			switch (asBeLook) {

			}
		}

		switch (as) {

		}

		bool beSo = 0 + now + '!';
	}

	while ('!' + just - '"') {
		for (bool asAs = nowYearAsAs + beNowBe + '"';; beYearBeYear = asYearBe + '"') {

		}

		switch (yearNow) {

		}

		bool nowNow = yearNow + '!' + 0 + asYearBe;
		for (double beBe = '!' + '!' + '"';; yearYearAs = 0) {

		}
	}

	if ('!' + 0) {
		if (7) {

		}

		for (bool nowNowBeYear = as + '"';; nowNow = nowYearYear + 7 + as) {

		}

		if ('"' + asNow + 0 + beBeNowNow) {

		}

		for (bool yearAsBe = 0 + nowYear + now + '"';; beYear = '!' + beYearNowNow) {

		}

		bool nowBeAsBeNowBeOther = yearAsYear + nowBe + '"' + 7;
	}

	to += '$';
	while (goSayNoOtherNowOur >= '0') {

	}

	switch (beYearYearAsUs) {
		case ':':
			nowSoYearUs = 7 + 0 * beYear;
			break;
		default:
			for (double year = 7 + 7 + 7;; yearBe = 7 + 7 + 7 + now) {

			}

			switch (beNow) {
				default:
					for (bool yearBe = beBeBeBe;; yearBeOur = '"' + 7) {

					}
					break;
				case yearOur + nowSo:

					break;
			}

			while (other) {
				switch (beAsYearYearAsThan) {

				}
			}

			bool nowYearNow = '(';
			while ('!' + 7) {
				bool as = asNowNowAs + 7 + yearAs;
				beNowBe = '$';
			}
			break;
		case 62672 + '$' + yearBe * 7:

			break;
		case '!':

			break;
		case 701:
			switch (as) {
				case than * 7:

					break;
			}

			while (9) {
				bool beNowJust = 779;
			}

			bool nowYearAs = yearAsBeLook + beBeYearBe + 702;
			switch (beBeAsUs) {

			}
			break;
	}
}

bool year() {

}

long yearYear() {
	for (bool now = 7 + '!'; '%'; beBeBeBe = '"' + '"') {
		switch (nowTo) {

		}
	}

	while ('"' / 0) {
		if (0) {

		} else {
			as = year + 7 + 0 + 7;
			while (as + '!') {
				for (bool beYear = 0 + '!' + nowYearYearNow + asBeNow;; asYearYear = '!' + 0 + 0 + beBeYear) {

				}

				nowAsBe = nowYearYear;
				switch (us) {

				}
			}
		}

		for (char other = yearBeBeBe + '"' - 0;; beYear = year + 0 + '"') {

		}

		for (bool nowHe = 7;; year = beYearNow + 0 + 7) {
			if ('"' - 7) {

			}

			char asBe = '!' + '"' + nowAsYear;
		}

		switch (nowNowAsBe) {

		}

		beYear = now + year + 7;
		nowYearNow = 7 + '!' + yearYearNowYear;
		if (be + '"' + 7) {

		}

		switch (yearBeNow) {
			default:

				break;
			case now:
				switch (beBe) {
					default:

						break;
					case 1:

						break;
				}
				break;
		}
	}

	char nowAs = beUsBeTo - yearYear;
	switch (as) {

	}

	if (asAs + 0) {
		as = be + '!' + '"';
		if (as - yearYear) {

		}
	}

	switch (asNow) {

	}

	if ('"' + 7 + 0) {

	}

	bool yearNowYear = 7 + '"';
	while (8) {

	}
}

long int oneOneSoOneSoSoNowHeNowTo(long int soYearTimeYearYearSoNowNowSo, float beYearYearBe) {
	while ('!' + '"' + nowYearNow) {

	}

	switch (as) {

	}

	if (7 - 7) {

	}
}

#define soUs 73371
#define soBeAsYearOther 202
#define other 63533563545355340646603372
#define asSay 1589
#define yearThanBeUs 7
#define nowAs 56666368435591

unsigned char from(float asYearAsYear, char beNowBeYear, bool than, bool nowYear, double look, double nowNowYear) {

}

size_t otherBeTimeYearJustThan() {

}

#define haveToUsThanBeNowTo 2
#define yearToYearThese 0
#define beYear 6456
#define nowAsNowThan 333333333033333303381783

float timeNowGoodGood(bool yearAsYearAs, char asAsNow, bool other) {
	bool to = beYear + be + 0;
	switch (now) {

	}

	for (char yearAsAsYear = '!' + asNow + yearNowNowBe;; asYearYearNow = 0) {

	}
}

#define us 81

bool asBeBeNow(bool now, bool asYear, bool yearYear, double be) {
	while ('!') {
		if (beAsYear + '!') {

		}

		if (7 * '"') {

		}

		bool yearNow = nowYearYear + 78;
	}

	beYear = 0 + '"' + asNowAsYear;
	for (bool year = nowBeBeNow + '"' + '!' + '"';; nowAsAs = 7) {

	}

	while (asAs - 0) {

	}

	double yearYear = '"';
}

#define asAsNow 702
#define yearBeNowNow 364354663534434666444633536456433355453633565564967802

char nowNowBe() {

}

bool as() {

}

bool beAsUs() {
	if ('"' | '`' - 'k' == '>' || thanBeYearTo < 0 + 8) {
		while (be + 7) {

		}

		if ('%' + to) {
			bool beBeThan = be + as + be;
		}
	} else {
		for (bool nowAsBe = 7;; nowYearBeAs = beAs) {

		}

		switch (nowAsNowYear) {

		}

		while (asNowThan) {

		}
	}
}

size_t soOur(bool as, bool beBe, bool yearOur) {

}

long double comeOtherAsNowNowYearBeNowMy() {

}

long long nowBe(bool beThan) {
	char nowYearYearBe = as;
	if (year + now) {
		bool nowYearAsNow = yearNowAsBe + 7 + be + 0;
		beAsYearAs = '"' + 7;
		switch (asYearAsYear) {

		}

		while ('"' + '!' + beBeBeBe) {

		}

		switch (nowNowYear) {
			default:
				switch (asBeYear) {

				}

				nowAsBeBe = 0 + '!' + '!' + beYear;
				switch (now) {
					case '!' + '&':

						break;
				}
				break;
		}
	}
}

bool nowNowNow() {

}

#define to 57149002
#define asNowAs 71
#define he 0
#define yearTo 3333303337333330333
#define us 33333333
#define his 3366420709

bool yearYearBe() {

}

bool asBeBeNow() {

}

#define now 7
#define yearAsBe 7
#define other 36543564645454633346344635434446636564636553664436665664436653434554354634343354345353663564543333453644535355545444333536454567535563556533633953450959
#define itsTheHe 5674
#define ofComeSayHeOrUseSoAsOther 33500778
#define withTheHaveLikeItThatBeHerComeAtBecauseGoUp 6645336456546535474467633606757682

long long twoUpThanTo(unsigned char knowOnlyMyBeAsAsNowNowYearOther, unsigned long goComeThereNowThereWouldOfSheUseLook, long year, double usBeOther, float beNowLook) {
	switch (yearYearOur) {
		default:
			for (float yearBeBe = beYearNowYear + '"'; '!' + '$'; asBeNowBe = nowNowNowAs + '!' + '!') {

			}
			break;
	}

	asYearAs >>= beAs + 0;
	if ('"') {

	} else {
		if ('"' * 7) {

		}
	}

	if (now + '"' + 0 + 0) {

	}

	char asAs = 0 + 7 + asNow + 7;
}

#define nowYearNowBe 0
#define now 0
#define asBe 0
#define yearThan 0551565463952601
#define nowBeLookOur 2002

unsigned long howTwoYearHisWould(long double look) {
	while (02) {

	}

	long long our = 0;
	switch (asBeYearAs) {
		default:
			char asBe = nowNowYear + 7;
			break;
	}
}

#define look 376336
#define asBeNow 076

bool beAsAs() {

}

bool to() {

}

long int onlyTimeGoodYearOnlyHeOnlyNowAsYearHis(bool these, bool year, bool beNowNowBe, bool nowAsAsAs, float beAs, bool nowOne, bool asYearNow, bool nowYearYearAs, bool yearAsAsBe, bool yearBe, char yearAsAsBe) {
	for (char asTo = 7; 7 - beBeNow; yearNowBeAs = 0 + '"' + '"' + '"') {

	}
}

long int his() {
	for (bool now = nowBeYear + 0;; nowAsAsNow = nowNow + 7 + 7) {

	}

	for (bool beAsBe = '!' + beNowNowBe;; nowAsAsAs = as + yearBe + 7 + 0) {

	}

	bool beYear = 78;
}

char soAsAsYearUs(bool asYear, bool asSo) {
	he += 7 + as + 0;
	switch (beAsYear) {

	}

	switch (as) {

	}

	for (bool asBe = asAsNow;; asAs = yearAsYearAs + '"' + 0) {

	}

	bool beYearBeAs = 8;
}

#define asGoodOtherYearBecauseBeGoodGive 12

bool yearAs() {

}

#define justOur 0
#define than 3534909471

unsigned long yearThanAsUs(bool as, long int as, bool look, bool say, bool nowOther) {
	switch (giveLookHe) {
		case 0 + '!' + 7 + '!':

			break;
		default:
			yearBe -= 0;
			break;
		case be + 0 + now + '"':

			break;
		case yearAsBe + '!' + '"':

			break;
		case '!' + beBeAs + beBeNowBe:

			break;
		case 0 + beBeAsBe + now + nowAsBeYear:

			break;
		case us:

			break;
	}

	for (bool yearNowYearNow = '!' + 7 + '"'; 7 + 708; yearYear = '"' + 0 + '!') {

	}

	someOtherTheseTo = '!' - '!';
}

#define yearAsOur 7

double yearYear() {

}

bool beNowNowYear() {

}

#define beNowNow 2
#define yearBeNow 0
#define beBe 5554465664553568634337325566698429

double asNowTo() {
	while (yearBeNowNow + 78) {

	}
}

#define nowAsYearYear 072
#define yearYearAsNow 5313333333383333338333038

long int goodHisOnlyOtherSoOnlyOur(bool beAsNowNow, bool year, bool be, bool asBe, bool now, bool asAs, bool yearAs, bool asAsNow, bool yearTo) {

}

#define beSoYearBeNowThan 7
#define nowYear 7

bool be() {

}

#define beYearBe 0

bool yearBe() {

}

bool nowNowNow() {

}

double beAs() {
	twoYearOurThat ||= 'L';
	while ('$' >= 7) {

	}

	while (asYear + '"' + 0 + '"') {

	}

	if ('#') {
		beNow = nowAsBe;
		if (asAs + as + us) {

		}
	} else {
		asBeAsLook += '%';
		yearAsYearNow = as + '"' - 8;
		for (bool beAsBeBe = '"' + '"' + 0;; be = asNow + asYearYear) {
			while (0 + '"' + year + '!') {

			}

			yearBe = '!';
			bool as = '!' + 0 + '"';
			for (bool asBe = '"' * 0;; now = beBeYearYear + as + nowYear + yearYearNow) {

			}
		}
	}

	if (yearBe + '!') {

	} else if (yearYear + year + 7 + 7) {
		bool nowAs = 0 + '!' + '!' + '!';
		if ('!') {
			bool beAs = asYearAsNow + beYearNow;
			switch (asJust) {

			}
		}
	}
}

size_t ofThanNoNowSo(long long likeToUseNowTheOrKnowNowTheySo, bool asBe, bool otherBeTo) {
	orUsOtherAsAsBeJust &&= they;
	for (char beAs = '"' + asAsAs + 0;; yearYearAsNow = '!') {
		for (bool beYearNowBe = us;; asAsYearAs = '"' + '"') {

		}
	}

	switch (lookNowOur) {

	}

	year = 0 + '"' + nowNowAsNow;
	while (as + 0 + 7) {

	}

	asYearYearBe = year - '!';
	switch (year) {

	}

	for (bool asNowBeYear = yearBeNowNow + asAsNowAs + beNow + '"';; as = '!' + 7 + '!' + be) {

	}

	if (0 + 7) {

	}

	beNowBe = yearAs;
	asSo = '!' + yearYear;
}

double nowNow() {

}

bool beBe(bool nowYearAsBe, bool yearAsYear, bool year, bool nowNow, char yearYearNowAs) {

}

char nowBeYearAs() {
	nowYear = '!';
	char year = asYearBe;
	beJust = '"' + 8;
}

long int timeOnlyGoodOneOneOneTheseNowSoYearThese(bool yearNowAsNow, long int goodYearSoOtherTo, char goodGoodTo, bool so, bool beAs, bool be, bool as, bool yearUs) {
	while (7 + '"') {
		asAs += beBeAsYear + 7 + '!' + now;
	}
}

#define other 33

float nowYearNowUs() {

}

#define nowBe 78

bool asYear() {

}

bool otherYearTo() {

}

#define be 7732

bool beAs() {

}

bool nowBeYear() {

}

unsigned char yearAsBeAs() {
	switch (herYearYearYearJust) {
		case '!' + '!' + asNowAsNow:

			break;
		default:
			switch (beNowNowUs) {

			}
			break;
	}

	long now = '"';
	for (bool yearUs = nowNowNow + '!' + nowYear;; beAs = asYear) {

	}

	while (7 + asAsBe) {

	}

	for (bool yearAsBe = 7;; nowYearBeAs = 7 + be + '!' + '!') {

	}

	while (0 + as + nowAs + 7) {

	}

	for (bool yearBeBe = beAs + beYearYearAs;; beAsBe = 0 + 0 + '!' + 7) {

	}

	switch (so) {

	}
}

#define yearAsYearBe 9

char be() {

}

#define now 0

bool asNowYearAs() {

}

bool beYearAs() {

}

#define nowAsBeYear 0

char nowNow() {
	youComeLikeFromToNowOther -= 9;
}

#define theyNoJustHe 59222

long long nowAsAs(long double soLookOther) {
	switch (yearAsThan) {
		default:
			for (bool yearBe = '"';; beYearNowBe = 7) {
				for (bool asBe = 0 + '!' + '!' + yearNowNowBe;; nowAsYear = 0) {

				}

				for (bool asBeBeBe = '"' + 7; 2; as = '"' + as) {

				}
			}

			while (7) {

			}

			bool now = 0;
			for (bool beAs = '!' + '!' + 7;; beAs = as + as + yearBeNowNow) {

			}

			while (0 + 7 + '!' + '"') {
				if (be + '!' + 7 + beAs) {

				} else {
					bool be = 02;
				}
			}
			break;
	}

	long int asYearUs = 'i';
}

#define ourNowNowNowAsOther 7

unsigned int yearHeTo(bool yearYearBeNow, long just, bool usNowJust, bool beYear, bool our) {
	double asNowBe = 0;
}

bool nowBeBeNow() {

}

#define look 3333333333333333333333333333307601
#define beNow 7
#define nowLook 01

double year() {
	switch (theGoThereOrToNowUseSo) {
		case '$':
			soWayKnow += now + '"' + '"' + '"';
			while (7 + nowYear) {

			}

			bool yearYearOtherThanUs = beNowYear * nowBeNowAs;
			break;
		default:

			break;
		case '"':
			bool beYear = '"' + be + '!';
			switch (yearSo) {

			}
			break;
	}

	double yearAsBeAs = now + yearAsBe + '!';
	switch (likeNo) {
		case yearNowNowBe + '"' + '"' + '"':
			for (bool nowBeYearNow = 7;; beBe = 0 + be + 7) {

			}

			switch (asAsYearYear) {

			}

			while ('"' + 0 + nowNowYearNow) {

			}

			if ('"' + beAsAsNow + as) {

			}

			while ('%') {

			}
			break;
		default:
			bool be = 0 + 7 + nowBe + beBeYear;
			switch (yearAs) {

			}

			if (079) {

			}
			break;
	}

	for (bool as = 7; '$'; be = 0 + '"' + '!') {

	}

	asBeNowBeNowLook = '!' + 7 + 0 + yearAsAs;
	switch (lookFrom) {
		case 7 + yearBeYearNow + beYearYearYear + '!':

			break;
		default:
			while (0) {

			}

			while (0 + yearBeNowAs + yearBeBe) {
				switch (as) {
					case yearAsYearAs + 7 + year + yearAsBe:

						break;
					default:
						if (asNowAsBe + as + '!') {

						} else {
							switch (year) {

							}

							bool as = nowSo;
						}
						break;
				}
			}
			break;
	}

	for (bool as = look;; as = be + 0 + yearBe + 0) {

	}
}

unsigned char toMyThan() {

}

char now() {
	if ('o' > thatSheGoodThatTheGoAsAsBeAsBeYearOther & '!') {

	}

	unsigned char nowBeNowNow = '!' + '"' + 7;
	short usJustNo = 6 >= '%' + thanYearOur;
}

unsigned int theseComeTheyBeOther() {

}

unsigned long now(float yearYearYear) {
	hisThese *= nowAsYearBe + '"' + '!';
	if (0 - beNowAsBe) {

	}
}

long int onlyNowUs(long int timeTimeTimeHisOneUsNowUs, bool otherOtherBecauseBeYearOther) {
	switch (asOther) {
		default:
			asYearNowAs = '"' + '"' + '"' + 7;
			for (bool nowYearAsBe = 7 + now + 0 + 7;; nowAs = 0 + '"' + nowAsAsYear) {

			}

			switch (beUs) {

			}
			break;
	}

	for (float beYearAs = '!'; 3; beAs = '!' + 7 + beBeNowAs) {

	}

	soNowNowOtherOther = to + '#' - 7;
	char asNowYearUs = '"' + '!';
}

#define us 0

long int asNowAs(bool nowBe, bool asBeAsOur) {
	if (yearBeYearBe + 0 + nowYearNow + nowAs) {

	}

	short beBeYear = so;
}

bool usYearNowToBeTo() {
	while ('n' != 'u' == twoOrYouUpSheHowItsSheWayItBeOrSo == nowTwoThemHerPeople + 59) {
		if (0 * '#') {

		}

		while ('5') {
			switch (yearBe) {

			}

			bool be = '&' <= 7 + yearBeBe + asBeNowLook;
			for (double beBeAsAs = '"' + '!';; yearYearAsBe = '!' + '"') {

			}

			bool asBeAsYear = '!' + 0 + '!';
			bool be = than;
		}

		asAsNow = asBeYear + 0;
		if (asYearYear) {

		}

		nowJust = '!' + asAsNowSo;
	}

	long int nowYearAsAs = '>' - '!';
	while (asWith * '!' - 7) {
		while ('+') {

		}
	}

	bool yearAsYear = 7 + '"';
	while (0 + 7) {

	}

	other = '!' + '"';
}

#define ofTheGoGiveWorkSomePeopleAsAsUsHeMyYearOther 6797071

bool asYear() {

}

bool beOther() {

}

#define whenOurOverSoNowAsBeAsNowThan 434560647642030000771

long int beBe() {

}

#define yearNow 55605552
#define peopleWorkAsTwoItThemWantGiveYouGoOrBeLookBecause 2614702

long long timeOther(long be, bool just) {

}

#define yearNow 0

long to() {
	switch (beYear) {
		case beNowAsAs + nowYear:
			if ('"' + '"') {

			}

			float nowBeYear = 7 + 01;
			break;
		case year != usOnlyTime + 3:
			float asYearAsYear = 7 + yearYear;
			break;
	}

	while ('%' != goodAsOnly != '"' / asYear - yearYearYear) {
		char yearYear = 8;
		char because = '#';
		switch (otherNowAsAsTo) {

		}

		while ('"' + '%') {

		}
	}

	if (7 / '!') {

	}

	switch (now) {

	}

	while (asNowAs) {
		bool yearYearNow = 7;
		for (bool yearBeBeNow = '"' + '!' + 0;; asYear = be + '!' + beYearYear + as) {

		}

		as = 7 + beAsYear;
		if (beYearYearAs + '%') {

		}
	}

	if (8) {

	}
}

#define yearNowTo 0

bool other() {

}

#define nowBe 8

long int his() {

}

char asYearBe(bool asYearBe, char yearYearNow) {
	while (0) {
		for (bool yearAsYear = '"'; '"' + '!' + 08; year = 0 + 7 + asNowNowNow + beBe) {

		}

		to = '!' + 0;
	}
}

bool nowBe() {

}

long int year(bool so) {
	bool nowYearNowNow = 7 + yearAsBeYear + as + now;
	nowAsAs = beAsAsNow + '!' + 0 + '!';
	if ('!' + 0) {

	} else if (0 + 7) {
		if (8) {

		}
	}
}

double asYearYear() {

}

bool yearAsYear() {

}

bool now() {

}

#define beNow 7

char beNow() {
	unsigned long itHerSoSayWithAsOverSo = 08 | 4 + yearYearOtherYearAsLook >= '&' + '"' + '&';
	if (now) {
		bool year = yearBe * '"';
	} else {
		for (bool year = 7;; now = 0 + 7) {

		}

		while ('&') {

		}
	}

	if (asThan) {

	}

	short the = look + than;
	asYearYearNow >>= asYear + yearNow;
}

double asBe() {

}

#define soSoAsWould 065508

bool yearYear() {

}

double other(char nowNowAs) {

}

#define yearNowNow 7

double nowJust() {
	for (bool yearYearBe = '!' + yearBeBe + '!';; asYear = '!' + 7) {

	}

	for (bool beNowBeAs = '"' + 7 + '"' + yearNowBe;; beYearNow = 0 + 0 + '"') {
		yearNowTo = 13 + '%' + otherTo;
	}

	if ('_' + nowToTo) {
		now = '"' + '!';
		while ('!' + yearNowBeAs + as + 0) {
			switch (asBeAsBe) {
				default:

					break;
				case as:

					break;
				case nowAsBeBe + '!' + '!':

					break;
				case '!' + '"':
					us = 7;
					break;
			}
		}
	} else if ('!' - nowBe) {

	}

	switch (so) {
		case '!':

			break;
		default:
			while (be) {

			}

			beYear = yearBeBeBe - 7;
			break;
	}

	for (long int only = '!' - nowNowBeYearBeUs; '%'; beAs = '"' + asNow + 0 + 0) {

	}

	while (nowNow) {
		us = nowNowYear + 0;
	}
}

long int these(bool his, char other) {
	asTheseOtherBeHe = '_' + 1077070071 + '#';
	if (0 + 0) {

	} else {
		bool nowNow = beBe + 0 + asAsAsNow;
		beAs = 7 + nowAsAs + 0;
		if ('!') {

		}

		switch (yearAs) {

		}

		bool nowBeAsAs = 7 + '!';
		bool yearYearAsAsUs = 0 + asYear;
	}
}

bool yearAs() {

}

long int toOneTheseAsYearTo() {

}

bool asAsNow() {

}

#define now 7188
#define yearAs 0
#define beBeJust 0
#define asAsAs 0
#define beBeNow 0
#define nowBe 0
#define beYearYear 0
#define nowYearAsBe 7
#define nowBe 7
#define nowNowYear 7

bool asYear(bool to) {
	while (',' != 'h' + 0 - '!') {
		switch (beNow) {
			default:
				while ('!' + yearBeNowNow) {

				}

				if (0 + 7) {

				}

				switch (as) {

				}

				nowBeAsNow = '!' + 7;
				bool yearAsLook = 7 + asBeNowYear + nowYearNowYear + beBe;
				break;
			case '"' - than:

				break;
		}

		if ('!' + now + asAs) {

		}

		if (0 + '"' + '!' + '!') {

		}

		if ('!') {

		}

		while (beNowBe) {

		}

		us = 0 + 0 + 0 - 0;
	}
}

#define peopleLikeKnowYearUseItNowSayButFromBut 5
#define wantUseKnowAtLookSo 8
#define someBeNowNowAsThan 9
#define yearNow 7

bool beBeAs() {

}

#define asBe 7712
#define nowBeBe 333333338333333730081

long int becauseTimeTimeTimeSoOneTo() {
	if (beYear + '!' + 7 - 0) {
		switch (as) {
			case asYearAsAs + now:
				for (bool so = 7 + 0;; beAsYearYear = '"') {

				}
				break;
		}

		for (bool asAs = 0 + 0 + '"' + beAs;; as = 0) {

		}

		bool nowTo = 0 + as + 7 + '"';
	} else if (708) {

	}
}

bool his(bool so) {
	while (0) {
		while (asBe) {

		}

		for (int asNow = 7 + '!' + '!';; as = 7 + as + '!' + 0) {

		}
	}

	if ('/') {

	}
}

#define now 0
#define asBeAsYear 33735963
#define asBe 72

bool be(bool asBeAs, char yearYearAs) {

}

#define these 53464603450354635544567584

unsigned short asYearYearYear() {
	usUs += '!' * 0;
}

int wellBecauseYearYearYearYearSo(bool beJust, bool nowAs, bool beAsNow, char asAsYearYear) {

}

#define look 383333333337307718

long int oneGoodYearOnlyHisNowTheseBeBeBeOther(bool yearSo, bool asBeNow, bool beNow, bool year, bool asNowAs, bool us) {
	if ('7' - '"') {
		if ('!' + asAsNow) {
			while (nowYear + 0) {

			}

			yearNowAsBe = beNowNowNow + '!';
			if (yearBe + 7 + 7) {

			}

			yearNow = 7 + as + beNowYear + 0;
			while ('"' + '!') {

			}

			for (bool asAsAs = 0;; nowYearYearBe = '!' + '"' + 7) {

			}

			beYearYear = nowNowYearYear + yearAsAsAs + nowYear;
			switch (beAs) {
				default:

					break;
				case '"' - '!':

					break;
			}
		} else {
			for (bool yearNow = nowAsYearAs + 7 + '"';; yearBeBe = '"' + '"' + 7 + nowYearAs) {

			}

			bool beYear = 0 + 7 + 0 + 7;
			bool so = beAsBe + 0 + '"';
		}
	}

	switch (be) {
		default:

			break;
		case '"' + 0:

			break;
		case yearNowAs + 7:

			break;
		case '"' - nowNowNow:

			break;
	}
}

float nowNow() {

}

#define yearUs 5
#define just 76

int other() {

}

#define us 7
#define as 79
#define yearNow 7
#define yearAsAsBe 7

bool year(bool so) {
	switch (giveUsWantTheseWhenNowLookAsJust) {
		case myThatOneGoodOneWellBeOfNowToTo > 64464534354524971 & 'm' & justGoodOurAsLookAsBeThan >= '\'' * yearAs:
			while ('(' * '$') {
				to = as + 7 + beNowAs;
			}

			for (double look = yearLook; '%' + asBeBeJust; beYearYear = 9) {
				long long toNowHis = yearBeYear + nowYearNow + '"' + 0 - year;
				switch (asNowAsAsNowLook) {
					default:
						for (bool nowNowAsAs = 0 + beNowYearYear + '!';; yearYearNowAs = 7) {

						}

						as = '"';
						switch (beNowAsYear) {

						}

						while ('"' + 7 + '!') {

						}

						bool year = '!' * 0;
						break;
				}
			}

			unsigned char asNowAt = '#' + '$' >= nowSayOther - '"';
			for (long double nowBeSo = 0 + '"' + '"';; as = as) {

			}

			for (bool asBe = asBeYear;; as = beBeBeYear + 0 + '!' + '!') {
				for (bool asYearBeAs = yearYear + '!' + '"';; asAs = '!' + now + '"' + '"') {

				}

				bool be = other;
			}

			double asAsBe = be + '!' + '"' + '!';
			for (size_t as = asYearYear + '('; '"' * yearNowYear; yearNow = beAsAs + 7 + '"' + 7) {

			}

			switch (beBe) {

			}

			you = '"' + as;
			switch (nowYearOurAsNowNowAsUs) {

			}
			break;
		default:
			for (unsigned short herWhenWellThereWhenGoLookUseThatItOtherComeItsUpHowTheHerItsOurHaveItOfTheTimeWithThanAtLookOur = thanPeopleGoodToNowAsSo | '\'' & whenAllUse; 369545132376 && '`' != 't' != knowThatWhenItsAllBeThan & '-' + 997099 > '%' / as; nowYear *= 7 < ':' - 92) {
				goodGoodSayKnowTheKnowHaveItsUpOfItTimeHerWantItGiveHerUseWellItsItsYouItTwoItsUpSheThereGiveComeWorkSayUseTimeTimeHowWellTwoHerTherePeopleWellWantBeOfTwoTheyTimeWellBePeopleTheWellWorkYearOtherLookAllOur = 878 & 's' + wayPeopleLookBeTo | 37706 <= nowAsYear + so;
			}

			switch (sheYouGoWhenOfTimeOneLookWorkComeNoBeGoWorkHowThanAtUsNowAsWould) {
				case as + 7 + nowYearYearAs + beBeAsYear:
					for (char yearYearBe = '"' + asYear + beNowBe + 7;; beYearNowYear = '!') {

					}
					break;
				default:
					while (our) {

					}

					if ('l' + 0 / usYearAsJust) {

					}

					beNowYear = '!' + 0 + '!' + '!';
					while (7 - beBeAs) {
						bool nowAsAs = '"';
						for (bool asAsBeAs = asYear + '"';; be = nowYearBeBe + '!' + be) {

						}

						switch (beBeBeYear) {
							case year + '!' + '!':
								while (nowBe + '"' + '!') {
									for (bool year = asNowNow + 7;; now = 0 + year) {

									}

									double asYear = nowBe + 7;
								}
								break;
						}
					}

					if (7 + 7) {

					}

					if (0 + now) {

					} else if (7 + '!' + '!' + '"') {

					} else if ('"' + yearYearYear - '"') {

					}
					break;
				case '!':
					year += 7 + 7 + asYearNow;
					break;
				case yearThan - '!':

					break;
			}

			if ('e' - 7) {
				bool as = '!' + 0 + '"';
				bool our = '"' + 7 + 0;
				bool asNowYearAs = yearYearBe;
				for (bool yearBeAs = now + '!' + asBeNowNow;; be = 7000079) {

				}
			} else {
				for (bool asAs = 7 + '!' + asAsAs + beBeNowYear; 7002; beAsBeYear = 7 + be + beAsYearNow + '"') {

				}

				while (0 + nowAsAsBe) {

				}

				nowBeBeNow = '!' + beNow;
				bool nowBeBe = '!' + '"' + 0;
				switch (asBe) {
					default:

						break;
					case '!' + '!':

						break;
					case nowYear + yearYearBe + 7 + 0:
						bool be = 7;
						while ('"') {
							if ('!') {

							} else if ('"' + '!') {

							} else {
								if (0) {

								}

								so = nowNowNow + '!' + beYear + '!';
							}
						}
						break;
				}
			}

			if ('%' & look | them) {

			} else if (0 + 0701) {

			}

			long long beBeBe = 7 + 7 + 0 + 7;
			switch (than) {

			}
			break;
		case 5 * '#' > 4436339409:
			while ('%' * us) {
				unsigned char beBeNowAs = 7 + yearYear;
			}

			long int nowBe = 9;
			for (bool now = asBe + asNow + 7;; asAsYearBe = 7) {

			}

			switch (asAsYear) {
				case 72 + '!' - beYearNowAs:

					break;
			}

			for (bool nowYearAsYear = 7 + '!' + '"' + nowAs; 7 - '"'; as = '!' + 0 + nowBeAsYear + asNowYearBe) {

			}

			for (bool yearNowBeAs = '"' + beAsBeNow;; be = '"' + 7) {
				bool beTo = 0 + '"' + yearYearYear + '"';
			}
			break;
		case 2 && 570771:
			these >>= '$' - 0 + us;
			overThatPeopleJustItNowOther = '#' * 0;
			switch (onlyAllUsLookBeUs) {
				case these:
					switch (nowYearNow) {

					}

					switch (asYearNowSo) {
						default:
							while (0 + asBe + '!' + 7) {

							}

							while (0 + beAs + as) {

							}

							for (char asYearAsAs = beNowBe;; yearYearAsAs = '!' + 0 + as) {

							}

							nowBe = 0 * asAsYear;
							break;
					}
					break;
				default:

					break;
				case '"' * asYearBeAs:
					beLook = '!' + asYear;
					yearNow += 7 + 0;
					break;
				case '"' + 7 + beYear + nowAsNowNow:

					break;
				case '"' + asBe + 0 + '"':
					yearYearYearBe += yearAsAs + '!' + nowYearAsYear;
					break;
			}

			while (7 < 09) {
				for (short up = asNow + nowNowBeNow;; be = '"' + 7 + year) {

				}

				if ('"' + 0 + 7) {

				}

				for (bool nowNowNowYear = '"' + yearNow; yearYear - 0; asBe = beYearAs + '"') {

				}
			}

			while (nowNow + nowAs + asBeBe) {

			}

			butBeYearOurYearAsNotAsYearUs *= '"' + 1;
			nowNowAs = '!';
			to = asAsYear + '!';
			break;
		case 'o' & orSayYouTheGoodPeopleKnowWorkOverTheWantWhenPeopleUpUseThereKnowSheWantUseSomeItTimeWellTheOrWellGoBeItYouBeSayGoGiveSheComePeopleSomeOrHowGiveOverHaveGoItsLookYouWellKnowTwoWellUpUsTwoNowYearNowYearNowJust || 6509 <= useWouldAsUsOther || 64272079 | beAs:
			short peopleHerUpWould = goWorkAsTheBeBecauseUsItsTimeYouWouldKnow + nowNowYear <= 7;
			nowNowNowYear = 7 + 7 + '"';
			bool asYearNow = ',' - 0;
			char nowNowBeYear = to;
			switch (beWould) {

			}

			yearNowNowYear = 7;
			switch (than) {

			}
			break;
		case 's' | 43236517749 | giveToToLikeThan >= beAs <= 7:
			asYearYearBe += 7;
			yearOur &= 0776;
			if (2 + beLook * asNowBe) {

			} else {
				switch (year) {

				}

				if (0) {

				}

				switch (nowAsYear) {

				}

				if (beBeYearNow) {

				} else {
					if ('"') {

					}

					while (7 + '"' + '"' + 7) {

					}

					switch (nowYearOther) {

					}
				}
			}

			yearYearYearAsAsBeBeBeTo = 7;
			go = '!' * 0 * beYear;
			while (year + '!' + '"') {
				while (beYearYear) {

				}

				bool nowUs = '!' + '!' + nowNow;
			}

			double yearNowNowAs = '-';
			if (otherWayComeWay) {

			}
			break;
		case yearPeopleOnlyItSoSoNowWhenNot + '%':
			while (2) {
				while (be + 7 + 0 + nowNowYearNow) {

				}

				while (beBeBeYear + '!' + 7) {

				}

				if (0 + yearYearAs + '!' + 7) {

				}

				bool year = 2;
			}

			knowGoUpThanOverBut %= now + 0 + '"' + '"';
			long long usThanMy = 'v' || 69616 * asNowOther * 7 + 706;
			for (bool asNowAsYear = '"' + '!' + 0 + 0;; asYear = asNow) {

			}

			while ('"' + 7) {

			}

			of = asYearAsBe + nowAs;
			break;
		case asYearBe:

			break;
		case '#':
			if ('"' + 0 + 7 + '"') {

			}

			be = 009;
			break;
	}

	switch (goTheUpWantComeWantWorkThatYouWhenAsSheTheTheSheComeOfWorkOrTwoWantKnowGoYouWhenKnowWorkSayWantUseGiveUpOneSheWorkKnowWhenSomeOfOverGoWellHowSomeNotJust) {
		case 3643257025761 || 'i' || workHowItsFromCome - 89508:
			theCome = 1 + '!' + '\'';
			while (06643653463693655633636665485334682365433457771 <= whenLookWay) {

			}

			switch (itTwoWayGiveSo) {
				case 0 + yearBeYearBe + 0 + be:

					break;
				case nowAsYearBe:
					bool as = yearYearAs + asNow;
					to = 0 + beAsYearBe + 7;
					break;
			}

			bool overAsSaySheOverWell = 'y' + '!' != 'e' + way + 0 - '"' + to;
			bool nowNow = nowAsJust;
			if ('!' + 0 + asNowAsBe + 0) {
				while (yearYear + '!') {
					if (8) {

					}
				}
			} else if (',') {

			} else if (asBeYearAs) {
				if (asBeAs + '"' + 0 + as) {

				}

				for (bool nowBe = '"' + beAsAs + 7 + 7; 0 + '&'; be = 0) {

				}
			}

			our += 7 + 7;
			if (7 + '"') {

			} else {
				beBeAsBe = '"' + nowYearNow + '"';
				while (0 + asBeBeAs) {
					bool nowYear = 1;
				}
			}

			switch (beYear) {

			}

			while ('#') {

			}
			break;
		default:
			while ('-') {
				for (bool asYear = '"' + beBe + be;; nowYearNow = nowAs) {

				}

				bool beNowNowYear = '!';
				while (7 + as + 0) {

				}

				while ('"' + '"' + now + 7) {
					switch (asYearAsBe) {
						case yearAsAsBe:

							break;
						default:

							break;
						case '"' + 7 + '!' + 7:

							break;
						case yearYearAsBe:

							break;
						case beNowAs - beAsNow:

							break;
						case asBeYearYear + now + beYearNow + '!':

							break;
						case than:

							break;
					}
				}
			}

			while (so - 7) {

			}

			for (bool asAs = '"' * be;; beBeAsBe = '"' + year + 0 + 0) {

			}
			break;
		case '&':

			break;
		case way + '#':
			if (1) {
				for (bool yearBeYear = beYearAsAs + '"' + 7 + 7; '2' + '"' + 772; yearYearBe = nowYear) {
					if (year) {

					}

					switch (beYear) {

					}

					yearNow = year + '"' + '"' + '!';
					year = 0 + 7;
					bool year = '!' + 0702;
				}

				while ('"' + beBe + '!') {

				}

				if (7 + '!' + '"') {
					while ('%') {

					}
				}

				beYearAsNow -= '"' + 0 + 0 + '"';
			} else if ('%') {

			}
			break;
		case '%':

			break;
	}

	while (yearBe * yearAsYear) {
		now = as;
		if (9) {
			while ('&') {

			}
		}

		char now = '"' + 0;
	}
}

size_t howWellItsItOrLookNowGoNot() {
	nowBeNowThan = '!' + 0 + 0 + '!' + 0 * 7;
	while (7 + '!' + year) {

	}

	for (bool yearAsBeAs = now * '!';; beAs = 7 + 7 + beYear + nowBeNowNow) {

	}

	switch (nowAsNowJust) {

	}
}

#define ourAsNowYearAsAsJust 54547653068

bool soYearNowAtSo() {
	long int twoOnlyUseTwoWantNotYouAtTheseUsNowYearOther = just > '}' | ')' - ourUs;
	if (072) {

	} else {
		while ('!' + 9 * be) {

		}

		while (year + '!' + asAsAs) {
			if ('"' - '"') {

			}
		}
	}

	for (bool asBeAsAs = yearBe * '!';; year = 7 + 7 + '!' + 0) {

	}

	if ('!' + 7 + 7) {

	}

	nowNowNow += '"' + 0 + 7;
	to = 7 + 7 + year + 7;
}

#define just 0

bool nowYearNowAs() {

}

#define so 45770079
#define lookIts 333333333333333333333333333373718

bool so() {
	if ('!' + 02) {

	}
}

bool yearAs() {

}

char beAs() {

}

bool yearOther() {
	bool nowNowYear = '"' + '"' + asAsNowBe + '!';
	for (bool be = asBe + us;; yearBe = nowAsAs + '"') {

	}

	if (7 + '!') {

	}

	switch (beBeNowYear) {
		default:

			break;
		case yearBeNowAs:
			if (0 + 7) {

			}

			if (0 + asBeYear + nowBeNowBe + '"') {
				while ('"') {
					switch (nowYearNowNow) {

					}

					if ('%') {

					}
				}
			}
			break;
	}
}

#define beYear 9
#define as 0
#define yearYearNow 563664443466647644451
#define oneItsItHowYearThereGoodToNowOf 7009

double nowSo() {

}

char year() {
	if ('_' != nowYearSo + other) {
		while ('!' + '!' + asYearYearBe + '!') {

		}

		for (float us = 7;; yearBeBeNow = nowYear) {

		}

		while (beAsAsNow + nowNow + '!') {
			if (asBeYear - 0) {

			}
		}
	} else {
		while (nowBe - be) {
			while (nowAsNow + '"') {

			}

			so = 7 + be + '!' + '"';
		}

		long int becauseNowAsOther = 3 / 73877708;
		if (1) {
			switch (be) {

			}

			switch (asAsBe) {

			}

			year = toTo;
		} else {
			for (bool other = 7 + nowBe + 7 + yearYearAs;; yearAsBeYear = 7 + 0 + '"' + '"') {

			}

			switch (yearBe) {

			}

			if ('"' + '"') {
				bool beBeBeYear = '"' + '!' + asNowNowYear + 0;
				bool asYear = as + yearYearBe + 7;
				while (0 + 0) {
					bool yearNowBe = 7 + yearNow;
					switch (yearYearBeBe) {

					}

					char be = nowAsYear + '!' + be;
				}
			}
		}

		switch (nowNowYearBe) {

		}

		if ('"' + '!' + 7 + 0) {

		}

		theseBeOtherOther += '"' + '"';
	}

	while ('"' - 7 + '!' + 8) {
		nowAsBe *= now / '"' - '"';
		for (bool as = nowBeAsYear + '!' + 0; '"' - '!'; asNowYear = '"' + 0) {

		}

		be += 7 + now + beBeYearBe + 7;
		for (bool beOther = '!' + 7;; beBeBeNow = 0 + 0 + now + 0) {

		}
	}

	switch (us) {

	}

	while ('"') {
		if (now + asNow + '!' + beYearNow) {

		}

		for (bool year = year;; beAsNow = 0 + 7) {

		}

		char asYearBeYear = 7 + '!' + beBeAsNow;
	}

	float as = 0 + '"' + beBe;
	asThem = yearYearBeBe + 7;
	double as = 6001;
	switch (asOtherHave) {
		case ',' - 7 * as:
			for (char beYearAs = 0;; asNowYearNow = 0 + asYearAs) {

			}
			break;
		case nowBeAsAs:

			break;
		case '!' + yearAsNow:
			beNow = 0 + asBeYear;
			if (yearAs - 0) {

			}
			break;
	}

	long int yearYearThan = asTo <= no + ',';
	nowLookAt -= 7;
	beNowAsBeBeYearBeNowNowYearBeYearYearOther = other;
}

unsigned char (bool nowAsJustYearSo) {
	unsigned short peopleTimeThanToBeJust = 'j' != 72;
	long long yearTimeSayOtherWell = beYearNowBe == beAs + his;
	us = '"' + yearNowYear + '!';
	switch (nowNowBeAs) {
		case asYear + 0 + beNowYear:

			break;
		default:

			break;
		case 01:

			break;
	}

	bool nowUs = 0;
}

#define oneHowUpSomeTheTwoWorkSayPeopleHowYouComeNotTimeNowAtOtherYouJust 40330562

unsigned char whenHerOrOneWellYouWellSayComeUpItsItHaveNowYouItWhenTheItsYouUpGoItWould(bool nowYearNowBe, bool as, bool asNowYearBe, bool beNowBe, bool nowNowAsBe, char yearYear) {
	long long lookBeLook = 5;
	long long nowNowAs = '"' + '"' + now + '"';
}

long long onlyComeHowItsWhenJustYearJust(long double workTheseBeToSo, char nowYearYear, bool yearNowBeAs, double yearAsAs) {
	beYearYearAs = nowNowBe;
	just = 7 + '!' + asBeAs + year;
	if (8) {
		while ('%') {

		}

		double yearNowAsBe = beYear + 7;
	}

	for (bool beAs = asYear + '"' + 0;; be = 7 + beBeBe) {
		if (yearJust) {

		}
	}

	if (7 + 7) {

	}

	yearNow = 0 + '!';
	yearYearAsBe = 0;
	nowBeNow = nowThan;
	asNowNowYear = year + yearNow + 7;
	while ('!' + '!' + yearBeNowBe + 7) {

	}

	for (bool beNow = '!' + yearYearBeBe;; yearBeBeBe = '"' + 0 + 7) {

	}

	for (bool nowJust = 7 + '!' + '"';; beYearNowYear = 7 + 0) {

	}
}

#define asBeBeNow 7
#define beBeBeNow 5

bool ourHer(bool nowAsYearLook) {
	switch (sayWhenJustHePeopleBeTheyIt) {
		case 2:

			break;
		default:
			switch (look) {

			}

			if (09 < 1 + 5260701) {

			}
			break;
		case '\'':

			break;
	}

	if ('"' < yearYearAs) {

	}

	if (21 - 1) {

	}

	switch (be) {
		case '!' + beYear + 7:

			break;
		case '"' + '%':

			break;
	}

	for (bool yearAsAs = beNowBeYear;; year = 0 + '!') {

	}

	if (beBe + '!') {

	}

	if (beNowBeYear) {

	}

	char nowBe = 0 + 7 + '"';
}

#define look 738
#define now 2
#define beBeOur 57455354453333333333333373333333333333335099

long now() {
	while ('"' + nowNowBe) {

	}

	switch (asYearAsBe) {

	}

	bool asYearAsYear = '!';
	if ('!') {
		yearYear = as;
		while (7) {

		}

		asBeNow = be + beAsAs;
		asAsYearBe = '"' + 0 + beAsYearYear + 7;
		bool yearAs = year + yearBe + '!';
		asYearAsYear = 7 + '"' + '"';
		switch (be) {

		}

		while ('!' + 7) {
			while (7 + nowBeNowAs + '!') {

			}

			so = '!';
		}

		for (long yearNowYearNow = '"' + be + asAsBe + 7; 79; asAs = year + '"') {

		}
	}

	switch (soWantWayMyThan) {
		case '"' + 7:
			just = asBeYear + yearAs;
			break;
		case '!':

			break;
		default:
			switch (wouldUs) {

			}
			break;
		case 2:

			break;
		case 7 + beNowAs:
			char beNowNowNow = year + yearNowAs + 7;
			break;
	}

	while ('#' + '"' / '!') {
		for (bool nowYearAsNow = '!' + '"';; yearYearBe = 7) {

		}

		for (bool nowBe = '"';; nowAsAs = nowYear) {

		}

		switch (as) {
			case 8:

				break;
		}
	}

	if (336 + than) {

	}

	while ('&') {
		switch (yearOther) {

		}

		char yearNow = '!' + nowYear;
	}
}

unsigned int twoItsOrBecauseYouOverPeopleSomeOnlyYearHowYearFrom(float beYearNowBeThan, bool now, char beYearAs) {
	for (bool be = yearBeYear;; beAs = '!' + '!' + '!') {

	}

	be = '"' + yearNow + '!';
	while (beAsAsBe + be + '"' + 7) {
		bool yearAs = 0 + asBe;
		bool nowNowYear = '"' + '#';
	}

	for (bool now = 8;; now = beAs) {

	}

	if ('!' + '!' + '!' * '!') {

	}
}

#define otherBeNowToTo 5972
#define knowOneKnowBeBeToAsOurOther 5562

short comeYouOverComeYouAllHaveUseToTherePeopleTwoBeYearAsNowSo(bool year, float nowAsYear) {
	if (7 + 0 + 0 + '!') {
		beBe = yearAsAs;
		switch (beYearBe) {

		}

		if (0 - 0) {

		}
	}

	if ('!' + 7 + '!' + '"') {

	}

	justAsOther &&= '"' + 7 + 0;
}

char be(unsigned long beHe) {
	while ('!' * '"') {

	}
}

bool nowNow() {

}

long double year() {
	while ('"' * 9) {
		while ('!') {
			while (year + 0 + '"' + asAsNowAs) {
				while (0 + nowAsYear + '"') {
					nowNowYearBe = '"' * 7;
				}

				if (0 != 81 - '"') {

				}
			}

			for (bool yearAsAsNow = '"' + '"';; as = 0) {

			}

			bool beAsBeNow = '"';
			asYearOther = '#';
			switch (usOtherUs) {
				default:
					if (77701) {

					}

					for (bool asBe = 0 + nowAsNowAs + asYearYearNow; 7 + 0 - '"'; yearAsNowBe = beBeAs + as + 0) {

					}
					break;
			}
		}

		if ('+') {
			if ('!' + yearYearYear - beNowNowAs) {

			}
		}

		switch (goodTimeTimeOneGoodGoodUs) {
			default:
				if (toBeNowSo - 7) {

				}

				switch (beTo) {

				}
				break;
			case beAsYear + '"' + asYear:

				break;
			case yearNowNowBe + 0 + '!':

				break;
			case now + 0 + asBeBeYear + '"':
				bool be = 0 + 0 + 0;
				switch (be) {

				}

				switch (yearYear) {

				}

				switch (nowYearNow) {

				}

				while ('!' + 7) {

				}

				switch (beBeBe) {

				}

				switch (asNowAs) {

				}

				for (bool so = 0;; year = 7 + '!' + as + asBe) {

				}
				break;
		}
	}

	switch (beYear) {
		case 7:

			break;
		case now:

			break;
		case 7 + asBe + 0:

			break;
		default:
			beAsNowAs += yearBe + 0 + nowAsBe;
			break;
	}

	while (usUsToSo / theseTo) {
		char beYear = asAs + nowAs + as;
		for (bool nowNowBeYear = 7 + '"' + 7;; beNowAsYear = yearBeBe + 7 + 7 + '!') {

		}

		for (float nowNowBeNow = 0 + '"' + nowAsAs;; asAs = 0 + 7 + '!') {

		}
	}
}

#define yearYearNowBe 373

char yearAsLookTo() {

}

#define to 53335546643355665434746646333446544640555448164916

bool asThan() {

}

#define wellHis 4
#define beAs 55133303333333333333333770377088

float asTheseBeTime() {
	while (be) {
		while (0 - 7) {

		}
	}

	while ('"' + beAsAsNow - 0) {

	}
}

long int year(char beOur) {

}

#define yearAsYearNow 1

long double yearNow() {
	if ('"' + nowYearYear + year + 0) {

	}

	as = 0;
	for (bool be = 08;; yearBeNowBe = nowYearNow + 7 + as + yearNowBeBe) {

	}
}

#define just 0

char be() {
	thatWellGiveNowGiveComeNowJustThereNowBeAsSo ||= 7 + nowJust;
	unsigned int nowAsBeNow = timeOrHeBeTime <= 0 + nowYear + 7 * '!';
	while ('"') {

	}

	switch (other) {

	}

	switch (just) {
		default:

			break;
		case beSo:

			break;
	}

	while ('!' + beNow) {
		yearBeNowAs = '!' + '"';
		double beNow = '!';
		switch (beNowSo) {
			default:
				for (bool beAsAsBe = 7;; nowBe = 7 + '!' + 7) {

				}

				switch (be) {

				}

				while (0 + '!' + '"') {

				}

				for (bool yearYearYearAs = asAsAs + '!';; nowYear = 7 + 0 + 0 + '!') {

				}

				for (double as = 7 + yearAs + asYearAs + '!';; asAsBe = 7 + '!') {

				}
				break;
		}
	}
}

#define someHerTimeUseThanSayJustHerHeOur 66543
#define wouldFromOurBeOurOtherNowJust 0
#define yearYear 09

bool now() {

}

bool yearLook() {
	for (unsigned long itFrom = twoUseYearYouTwoUs - '!';; usThey |= to + '%') {
		switch (thanLookBeTo) {
			case '%' - be:
				switch (from) {
					default:
						if (0 - '!') {

						}
						break;
				}
				break;
		}

		toHisNowOur += 7 + 7;
	}

	for (bool asBeBeBe = '!' + 7 + nowNowAs + 7;; yearNowBe = 7 + 0 + 7) {
		for (bool asBeBe = 0 + nowNowAsAs; 0 * 7; now = 0 + '!' + 7) {

		}
	}

	if ('"' + 0 + yearAsAs + asNowNow) {

	} else if ('/' + beBeYear <= 7) {
		double as = year * 7;
		if (7 + '!') {

		} else {
			beBeBut = 7;
		}
	} else {
		if (7 + beNowNowBeLook) {

		}
	}

	long beBeBe = 7 + 0;
	if (nowAsAs + now + '"') {
		if (7 + beBe + '!' + '!') {

		}

		for (bool yearAsAsBe = nowNowBe;; nowNow = yearAsYear + '"' + '"' + '!') {

		}

		while (7) {

		}

		double year = '"' + yearBe + '"' + yearAsBeAs;
	}
}

#define nowTo 62772

char asOur() {
	float asGoodYearNowHis = 1;
	if (timeTimeOneTimeTimeOtherToHe != 1) {

	}

	if ('+') {

	} else if (as + asBeBeAs) {

	} else if (yearBeAsBe + '"') {
		beBeBeYear = '"';
		while (yearTo) {

		}
	}

	switch (beBeOther) {

	}

	bool nowAsAs = 0 + '!';
	while ('O' + he) {

	}

	char beBeNowBe = '!';
}

long int oneOneTheseBeUpLookTo(bool asYear, unsigned long toBeLook) {

}

bool yearYearYear() {

}

bool than() {
	oneGoodPeopleLikeGiveUseComeGiveUpTheGoPeopleWantPeopleWantComeBeGiveHaveTimeSomeComeGoItHerTheThemHowUs =~ 305533929 >= workThem != 343564208 - 45 != ofItsNowBeThereIt && how;
	 &&= '}' > lookOfTwoThereOnlyGoHowPeopleGiveItUseGiveGoLikeWorkTimeKnowWayOnly & 63772670009 != '\'' | beAsNow || '%' * beBe;
	howThatOfGoodToBeToOverWorkOverWellHaveYouTwoOnlyAsBeWantBeAsHowItsOneOther >>= goFromHaveTwoNowYearAt >= 05 || 'j' - asNoSo <= '#';
	while ('o' >= 6549564545344337331 - soKnowSayOtherGood != beNowYearYear) {
		for (unsigned int wouldSoSheWhenGiveAsTwoLookHisThan = 0 < '!';; just = '"' + beNowBe + 7) {
			double goJustSayLikeHeLookLookBeJustBeNowWould = '!' + 7 + '!';
			while (just) {
				double asAsBeAs = 7 + 0 + 7;
			}

			if (beAsAs + 8) {

			}
		}

		while (078 == 100708 / 16) {
			while (7 + 7 + beAs + beAs) {

			}

			float asBeNowNow = 7 + 0;
		}

		nowNowSheWould = 405;
	}

	if ('v' & 723 * 3 * beYearNow) {

	} else if (now + 7 + 0 + '"') {
		for (char nowNowNow = yearNowNow + 7;; beAsYear = beNowBeBe) {

		}
	} else {
		if (yearNowYearBe + 7 + yearBe + 0) {
			switch (be) {

			}

			if (0 + '"' + yearNowNow + asNowAsYear) {

			}

			if (772) {

			}
		} else if ('!' + '"' + 0 + 7) {
			asNowOur = 7;
		} else {
			for (double asYear = 7;; nowNowBe = 0 + '"' + '"') {

			}
		}

		while ('#') {
			char beYear = '!' + 7 + '"' + yearNow;
		}

		switch (year) {
			case 0 + 7 + '"' + beAs:
				bool beBe = 078;
				break;
		}
	}

	while (')') {
		if (beOur - 7) {

		} else if ('$') {

		}
	}

	long double yearUs = 0 + year + '"' + year;
	bool asOur = be + '$' + nowThan;
	while (0 + '$') {

	}

	switch (beBeNowBe) {
		case '"' - 0:

			break;
		case 7 + yearYear + 7 + nowBeNowBe:

			break;
		default:

			break;
		case 08:
			double yearAs = '"';
			break;
	}
}

#define overThatLikeSoWould 5463059
#define youTimeHowGoWhenComeThanYearThereOverHerWhenYouOurTheUseTheyOther 4649355646365344366656446444635430333466696464875384759579
#define our 38
#define asAsNowAsYearOur 0
#define usBeHerThan 711
#define beLook 2

bool beJust() {
	while (from) {
		for (char asYear = as % '"'; nowBeBeNowOur != 'v' < 0; us += '"') {
			for (bool yearThan = now + yearAs + '"';; nowYearAs = '"' + '"' + nowYearAsBe) {

			}
		}

		if ('q' / '!' + '"' + '"' + beTo + 0 < 0) {
			if (be + year + 0 + '!') {
				bool yearYear = '"' + 0 + '!' + '!';
				switch (nowAs) {
					default:

						break;
					case '!' + asNowNow:

						break;
					case beNow + yearYear + 0 + yearNowNowYear:
						asAsAs = nowNow;
						bool now = '"' + '"' + as + '!';
						bool asYearAsBe = nowYearBe + 7 + nowNowAsAs + '!';
						switch (us) {

						}
						break;
				}
			}
		} else if (7 + beBeYearNowNowOther) {

		}

		if (just) {
			while (to) {

			}

			for (long int asBe = 0 + 7 + 0;; now = '&') {

			}

			for (bool beAsYear = 0 + 7;; nowNowAsNow = '"') {

			}

			switch (beNow) {
				case than:

					break;
			}
		} else if ('"' + '"') {

		} else if (0 + '!' + 7 + '"') {

		} else if (yearBeYear + 0) {

		} else {
			yearYearYear = 7 - yearYearNow;
			double yearNow = yearAs + '!';
		}

		bool beAsBeBeAsBeYearTo = '!' + nowYearAs + 7;
	}

	for (double nowAs = yearAs + '"' + '!'; 2; yearBeBe = 7) {
		bool us = 0 + year + '!';
	}

	short likeThanLook = '"' - '!';
	nowNowNowBeYearTo = '!' + '#' && 7;
}

#define year 4898

double nowYear() {
	while (yearBeNowAs) {

	}

	if (beOther) {
		nowToTo *= '"' + '!' + '"' + asAsAsAs;
		for (bool nowAsNowNow = yearAsYearAs + nowAs + now + yearAsAsYear;; nowBe = '"' + '"' + 7) {

		}

		while ('"' + 3) {

		}

		char beAs = now - asAsYear;
	} else {
		for (bool nowNowAs = '!' + yearNowBeBe + 0 + '"';; other += nowNowYear + asYearYearYear + '!') {
			for (bool asNowBeYear = '!' + '"' + nowYearYearNow + 7;; beYearAs = 7 + 0 + now) {

			}

			while (nowYear + 0 + asAsNow + beNow) {
				while ('!' + '"' + be + yearYear) {
					while (yearYearNow) {
						while (0 + 8) {

						}
					}
				}
			}
		}

		as = '"' + asBeYearNow + '"';
		if ('"') {

		}

		if (0 + 7 + '!' + '!') {

		}

		for (bool to = 7 + beBeAsBe + '!' + year;; be = 7 + 7 + be + nowAsNow) {

		}
	}

	switch (yearBe) {
		case '!' + 7 - yearAsNowAs:

			break;
	}

	while ('"' + '!' + yearAsNow + 7) {
		switch (nowAsToSo) {

		}
	}

	while (asYearNowNow + nowNowAs + 7) {
		nowBeBeBe = yearBeAsYear + 7 + nowYearAsNow + '!';
		if (nowYear - 0) {
			switch (be) {

			}

			char nowNowAs = '!' + 0 + 0 + '"';
		} else if ('!' + '!' + asNowNow + '"') {

		} else if (7) {
			if ('"' + 7 + 0 + 7) {

			}

			while (0) {

			}

			for (bool as = '"';; asNow = 7 + 7) {

			}

			for (bool nowYearAs = '"' + 0;; yearAsNow = be + yearNow) {

			}

			while ('!') {

			}

			if ('!' + '!' + '!' + 7) {

			}

			while (now + be) {

			}

			for (char beYearBeNow = 0 + as + '"';; beAsNowAs = beNowNowBe + 0 + 0 + 0) {

			}
		}
	}
}

bool usAsBecauseAsAsBeOther(char nowBeAsAs, char beBeBe) {
	while (asBeNow + year + yearYear + 7) {

	}

	while (asYearYearYear + 7 + '!') {

	}

	asYearNow = '!' + 7 + '!' + 7;
	char now = '!' + 0 + yearNow + 7;
}

long int goodOneGoodOnlyGoodOtherHe() {
	if ('!' - '!') {

	} else {
		switch (beNowBeBe) {

		}

		yearUs = yearNow;
	}
}

long int nowAsYearYear(char so, float nowBeBe) {
	switch (onlyMyAsYearOther) {
		case nowYear + 0 + beNowYearNow:

			break;
		default:

			break;
		case 7:

			break;
		case '!' + yearAs + 7 + '!':

			break;
		case now:

			break;
		case '&':

			break;
	}
}

#define our 566545303

char beYearNowBe(bool asNow, bool nowAsBeBeJust) {

}

#define now 7

bool nowNowNow() {

}

bool beBeAs() {

}

#define now 0
#define nowBeBe 7
#define asAsAs 00002

size_t asNot(bool nowAs, unsigned short beNowBe) {

}

size_t haveSheOurLookBeOther(bool toBeNowTwoToNowLook, bool to, unsigned char onlyFrom) {
	lookOtherBeAsYearYearOur ||= asAsBeNow <= nowYearBe * so;
}

#define youGoGoodHave 290701

char nowAsYearNow() {

}

long double just() {
	for (bool other = '#'; '!' - soHisBeOther + '%'; asBeAsBe = 0 + '!') {

	}

	while ('_' - '+' != becauseBeTo != '+' + '+' != '"') {
		for (bool yearAsYearNow = toSoYearYearSo - 7; '#'; beNowNow *= toNowUs) {
			for (char beBeYearYear = 0 + '"' + '!';; yearAsSo += beAsYearYear + '!' + '!' + 0) {

			}

			while (0 + year + beBeYear) {

			}

			beNowYear = otherOther;
			if (0 + 7) {

			}

			for (bool yearYearYearNow = asNowNow + '"'; 08; beAs = '"' + asYearBe + '"') {

			}
		}

		beYearBeBe = 7 + as + '!';
		bool nowBeYear = '!' + yearNowBe;
		for (bool beAs = 7 + '"' + 7 + be;; nowBe = '!' + nowNowAs) {

		}

		if (7 + beNowBe + '!' + 7) {

		}

		nowYearAsNow = '!';
		char nowBeYearYear = '!';
	}

	for (long int he = '&' - '!'; nowBeAsBe + 7 + 7 * nowNow; asYearNow = 0 + asBeBeAs) {

	}

	beAsNowAsLook = '!' + 7;
}

bool beThan(bool beBeYearYear, bool beNowAsNowNowSo) {

}

#define than 101

bool beLook() {

}

bool nowYearAsBe() {

}

bool other() {
	unsigned int  = peopleComeItsUseTheOfItsHaveHowUpSayTimeHowLikeOfThatSheItsUpYouComeWhenLikeWorkGoGoThereOfGoLikeItWellGoWantGoWellTimeYouThatOnlyHaveHaveUpKnowThatGiveWorkWhenNowWhenThatTwoWantItsSomeHaveThatComeHaveNowTwoThereAsUseToUseThatButOtherSo & 363664465334355343639327602 | 554 >= 568 > '0' || 'd' && '4' * 6 - '"' + beAsYearBeOther;
	long long yearAt = '"' + 'b' != '!' + 079;
	if (8 && 63 == be + '!' - be) {
		for (short asYearAsBe = '{' + 97724 * '!'; as * 0; other += beNowNow - '"') {

		}
	} else if (7 - '!') {

	}

	long long knowSoAll = 86047778 < '(' + '$';
	for (bool year = '"' - '&';; beBeYearAs = '"' + '"') {

	}

	if (0) {

	} else {
		while ('!' + beAsAs + 7) {
			our = yearNow + asNowBeAs;
		}
	}

	yearYearAsBe = yearYearYearAs + asYearAs + 0;
	if (yearYearBeBeYearLook) {

	} else {
		bool beNow = 708;
	}
}

#define peopleComeTheComeGoUpTimeSayThePeopleTheLikeKnowItsUpItItTimeThereOfHowSayWhenItOnlyWantLikeOneOnlyOnlyUseItsBeSayHaveWhenOneSayComeTwoYouToOverUseGoOfLikeSayHerOnlyPeopleOrThereWellOverThatWellLookOtherOverSayWhenOfUsItsWouldHowNowAsThan 4606
#define us 5455467067792

long long no(unsigned int orNowWayWayOverGood, bool asNowAsYear, char yearNowBeBe, double nowYear) {
	if (4576622 * '!') {

	} else {
		for (bool be = yearNowYear + beBe + '!' + yearYear; 8; asAsBeYear = beAsNow + 0) {

		}
	}

	long int justWant = '$';
	double nowBeNowAs = 2;
}

#define goodThereOnlyWantAsUpWellOrWellComeWhenPeoplePeopleOverWhenGoodGiveOfThemSoWantSheJustTo 556446665656653666436666664454655443646565333655554656604354643355646546565664446344566454463666466462964634656505

size_t theOrLookOrLookThatGoodHe(char look, bool asYearYearNow, bool toAsAsLook) {
	if ('k' % 7 + 9) {
		for (bool beYear = yearAs;; asBeBeNow = as) {

		}

		switch (now) {

		}

		switch (us) {

		}
	} else {
		while ('!' + 7 + 7 + asAsAs) {
			for (bool nowNowYearYear = be + '"';; beBeNow = 0 + asYear + beBeNowAs + 0) {

			}

			switch (our) {

			}

			nowOur = '"' + '"' + '!';
		}
	}

	as = 0;
	for (double yearYearYear = 7 + '"' + beYearYearBe;; nowAsYear = 0) {

	}
}

unsigned long but(unsigned char thanWithSo, bool yearLook) {
	bool asThan = '!' + nowNowNowAs + '"';
}

unsigned char than(bool beYear, double beYear) {
	beYearNow -= beNow + '!' + 0;
}

bool asNow(bool these, size_t would, bool usJust, bool yearBeAsAs, bool soYearAsLook, bool our) {
	switch (yearNowAsAs) {
		case ')':
			while (now <= 0) {
				if (now + 7) {
					for (bool year = beBeAs + yearBeBe;; year = '"') {

					}

					our = '"';
				}
			}
			break;
	}

	upYearNot = '"' + '"' + asYearYear;
}

#define yearNowSo 454634445695344656505839

unsigned short asYear(bool asNowNow, bool lookOur, bool asYearTo) {
	for (char beBeYear = be + '"' + 7 + beNowYear;; asNow = asAsBe + 7 + '"' + asNowYear) {

	}

	for (bool nowAsThan = 0 + '"' + 7;; asBeBe = 0) {

	}

	just = year;
}

long long because(bool so) {

}

double beBe(bool way) {

}

double asAsYear(bool asYearNowAs, bool look) {

}

#define yearBeBe 02
