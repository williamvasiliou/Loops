#define beSoUsTheseYearYearUs 33333333333333333333333083701
#define wayGoodAsButNowOur 665445545656634554446466464344646644645463366333564446355553566555543564655534666365354476564566564435643465036665664353053333656576557503632

unsigned short whenTheseWorkWellSomeSomeSoYouItsGiveLikeUpThereUpYearSheYearOur(unsigned char upAsWith, long long would, int wantAsNowNotLook, float asSomeLookYearBecause, bool other, bool orThanSo) {
	beAsYear = '!' + nowAsAsYear;
	long double now = 7 + yearYear + 0;
	bool our = yearNowAsAs + year + asNowAsBe;
}

unsigned int beNowNowYear(unsigned short ourThey, char not, bool just, bool year, bool now, bool now, bool nowNowYearBe, bool year, double nowBe) {
	for (bool beAs = asNow + nowYear; '!' <= beYearYearNow; yearYearBeNow = 0) {

	}

	double yearAsAsBe = '!' + '"' + '!';
	be = yearNowNowBe + asAsBe;
	yearBeNow = '"';
	bool beNow = '!';
	char beAsAs = beBeYear + yearYearYear;
}

short justWhenNowYearYearMy(double when, long double yearYear, bool beBeYearYear, bool how) {
	while (asBe + '!') {

	}

	switch (nowNowSo) {

	}

	switch (us) {

	}
}

long long yearBe() {
	bool beAsAsYear = 0 + '$';
}

#define yearYearOurUsSo 0
#define now 709
#define asYear 7

double nowAs() {
	if (because > 5 + year < 4) {
		if ('!' + '"' + asBe) {

		}

		yearAs += '"' + yearYear + nowYear;
	} else {
		if (0 + other) {

		}

		for (char year = '!' + now;; yearBeAs = 0) {

		}

		nowBeYear += nowYearYear + 0;
	}

	for (bool yearYear = 0 + '!'; 7 + beYearAsJust; nowJust = 7) {

	}

	just /= 4 - 0;
	char yearBe = '!' + beBeYearNow;
	for (bool yearBeNow = beNowBeYear + '"' + '"';; nowBeYear = '!') {

	}

	nowAsBe = 0 + 7 + 0 + '"';
	switch (yearNowBe) {

	}

	beBe = asBeNowYear + 7 + yearBe + beNowNowBe;
	while ('%') {

	}
}

#define they 4065583445728
#define beUsThanNowUs 528
#define now 7

unsigned char thanThese(bool us, char yearNowNowBe) {

}

#define as 07979
#define now 7
#define nowYearNowYear 7

char asBe() {
	while (so - 0) {
		switch (usNowNowUs) {
			case 0 - yearYear:
				switch (asBeYear) {
					default:
						for (bool nowBeBe = 0 + now;; yearNow = 0 + nowYearNowNow + '"' + '"') {

						}

						while (7 + 7) {
							while (0 + 0) {

							}

							yearBeBe = 0 + '!';
							if ('!' + '"') {

							}

							switch (so) {

							}
						}
						break;
				}
				break;
			case 7 + '!' + 0:

				break;
			case 0 + '"' + '!' + yearNowAs:

				break;
			case '"' + '"' + beNowBe + yearNowYear:

				break;
			default:
				switch (asNow) {

				}

				while (nowYearBe + 77000071) {

				}
				break;
		}

		as = '!' + 7;
		long int his = yearAsBeYear + '!' + 7 + now;
		switch (other) {

		}

		while ('!' + '!' + yearNow) {

		}

		while (be + '!' + 0) {

		}

		for (bool beNow = '"';; asBe = nowYearAs + '!') {

		}

		if (0 + 7 + asAsBeNow) {

		}

		asAs = '"' + '"' + '"' + 0;
		bool nowBeYearNow = asYearNowYear + '"';
		bool nowBeBe = 001;
	}

	switch (beGoodSo) {
		case 7:

			break;
		case now - nowYear:
			while (nowYearBeYear) {

			}

			while (7) {

			}

			switch (yearNowNowNow) {
				default:
					for (bool now = 0;; beBeNow = yearYear + '!' + '"') {

					}

					for (bool be = beYear + 0;; be = 7 + '!' + 7 + '"') {

					}

					while (8) {

					}
					break;
			}

			switch (be) {

			}

			char yearAsAsAs = 7 + nowYearAs + yearAsBeAs;
			break;
	}

	for (bool yearAs = 7;; beNowNow = '"' + '!') {

	}

	switch (nowYearUs) {

	}
}

long int goodBecauseBeOther(bool us) {
	for (float yearBe = beAsAsYear + 0 + '!' + nowBeAsNow; '"' + 071; year = 7 + '!' + '"' + yearYearYearBe) {
		if (as) {

		}

		nowAsAsNow += asBeAsAs;
	}

	asNowAs = '%';
}

#define timeBecauseOneNowTimeYearOther 3333337552078

bool look() {

}

#define beAsAsAs 7
#define nowAsYearBe 0654663026376
#define butOther 009
#define thereUpItsItsLikeJustWellJustToToNowBeToBeTo 3

char us(double yearBeBeAs) {
	switch (all) {

	}

	yearYear = 7 + as + 2;
	switch (beTo) {

	}
}

#define knowYearAsSo 7701

char asYear() {

}

#define year 5

bool yearOther() {
	if ('_' != '?' + '_' != '_' - 7371 - yearBeSo) {
		for (bool yearNowNowYear = 7 + '"';; to = 7 + 7 + '!') {

		}

		for (bool yearOther = '%'; yearHis; asAsUs = 0 + 0 + now + 0) {

		}

		float as = 7 + 0 + '"';
		soOneYearAsUsOtherTo = '!' - '"';
		beThese = nowNow;
	} else {
		float beBe = '"' + 7 + 7;
		switch (asBe) {
			case '!' + asBeYearYear + 0:

				break;
			default:
				to = now;
				break;
		}
	}

	bool yearAsAsNow = '!' + '!' + 0;
	if (8) {

	}

	for (char nowAsYear = 7;; asYearYear = 7 + '!' + '!' + 7) {

	}

	if (us) {

	}
}

long int yearBeNowBe(bool asBeBeNowAsAsUs) {

}

float asYearUs() {
	for (bool nowYearNow = '"' + 7 + 0 + 0;; nowNow = 7) {

	}

	if (beAsBe + nowBe + '!' + 0) {

	}

	while (0 + asBeYearYear + '"' + '"') {
		for (bool be = '!';; yearNow = '!') {

		}

		for (bool as = 0 + '"' + beBe + '"';; asBeAsNow = '!') {

		}

		yearNowAsNow = 0 + nowBeAsBe;
		while (0 + '!' + yearBeAs + 0) {
			while ('"' + 9) {

			}
		}
	}
}

#define yearNowBe 7

bool year() {

}

#define nowNowYearYear 8

char than() {
	switch (someHerItSoNowSoHaveBeHis) {
		case 08 != 009:
			bool as = '"' + '"';
			while (nowAsNowNow + 0 + 0) {

			}

			while ('"') {

			}

			they = yearAsYearBe + beAsAsBe + 7 + 7;
			break;
	}

	while (0) {
		if (beNowNow + beBeThan) {

		}
	}

	for (long nowYearNow = 0 + nowBe + 0 + as;; yearYearNowYear = '"' + 0 + '!' + 7) {

	}

	while ('"' + 7 + '!') {

	}

	char asAs = nowBeYear + '!';
}

double year() {

}

#define goodHave 02
#define thatGoodTimeBeTheHerSoSo 35653333378

long int soTo(char nowYear, char asYearThese) {
	while (nowAs + 0 + yearAsYearYear + asYearBeYear) {
		while (nowBe + so - 0) {

		}

		switch (beYearBe) {

		}

		for (bool asAsNowYear = '"' + nowYearNow + beAsBeBe;; yearAs = '!' + 0 + '!') {

		}

		switch (yearBeBe) {
			default:

				break;
			case '!':

				break;
			case 0 + '"' + 7 + 7:
				switch (year) {
					case 0 + 0 + 7:
						for (char as = '!' + '"' + be + yearAs;; now = nowAsAsBe + as + 7 + 0) {

						}
						break;
				}
				break;
		}
	}

	long int goodToSo = beAsNowBe + 0 + '"';
	bool asAs = 0;
	bool be = 0 + be;
	char yearAsBeAs = '!' - beNowNow;
	while ('%') {

	}

	char be = nowBe + year + beAsNow;
}

long int timeOnlyGoodOnlyOther(bool be, char nowAsNowNow) {
	bool be = so - 0;
	for (char other = be + yearNowYearYear + yearAsNowYear + '"';; beAsYearAs = '"' + '"') {

	}
}

long int to() {

}

#define giveOur 751

bool beYearNowYear(bool now, bool asAsNow, bool beBe, bool so) {
	if (547776) {
		for (bool asAsAs = '!' + asAs + 0;; nowBeYear = '"' + '!') {

		}

		for (bool now = '"' + '!' + 0; '('; nowYearYearAs = '!' + beBe + year + asNowAs) {

		}

		look = be + 0 + nowBeNow;
	} else {
		just = asOther;
	}

	for (short yearAsYear = 75;; beYearYearBe -= 7) {

	}

	for (long beNow = 0 + asAsBeAs; other + asAsAs + '"' * yearAsYearNow; nowNow = 0 + asBeNowAs) {

	}

	switch (justAsYearYearTo) {
		case as:
			char asBeNow = yearAs + year + '!' + '!';
			break;
		case 0 + '"':

			break;
		case 0 + '!' + 0709:

			break;
	}

	for (double look = 0 + be;; year = now + '"') {

	}

	switch (asYear) {

	}

	if (beYearYear) {
		bool yearNowAs = 7 + be + 0 + 7;
		bool asNowYearBe = 0779;
		if (7 + asAs) {
			while (7 + 0 + 7) {

			}

			bool beYearAsAs = '$';
		}
	}

	size_t be = '!';
}

size_t atYearNowSo(long int withJust, char than) {
	if ('!' + 0 + 7) {

	}

	switch (beNow) {
		default:
			switch (asAsAsYear) {
				default:

					break;
				case 0 - as:

					break;
			}
			break;
	}
}

bool to() {
	if ('&') {

	}
}

#define year 7

float beBeOther() {

}

long long yearOur() {
	double to = '#';
	for (double other = 7; '#' <= 0 - 7; now += 7 + 7) {
		for (char beNowBe = '"';; yearAsAsNow = 0 + '"') {
			beBeYear = '"';
			for (bool beNow = asBeBe;; beNowNow = 0 + 0 + 7 + nowNowBeAs) {

			}

			switch (us) {

			}

			year = us;
		}

		for (bool justTo = be + '!';; beBe = '!' + beNowAsNow) {

		}
	}

	for (short toHaveThere = 2 + '#' - 0; 'k' && toJust + 0 & '\\' - nowBeAsAs; now -= 0 + 0) {

	}

	if (7 + 7) {
		for (bool now = '!';; year = as + 7 + yearNowBe + 0) {
			while (nowAsNow + '$') {

			}
		}

		yearNow = 0 + 0;
		while (7 + '!' + '"') {

		}

		for (bool nowYearYear = 7 + be;; beYear = yearBe + '!' + yearNow) {

		}

		while (7 + 0 + 7) {

		}

		while (asNowYearNow + asBeAs) {

		}

		switch (asBeAs) {

		}

		while ('!' + asNowNow + nowYearYearBe) {
			bool yearNow = beAs - 7;
		}

		bool as = nowNowBe;
		if (asJust) {

		}
	} else if (',') {

	}

	year = '!' + 7;
	while ('"' + yearAsNow + yearBeYear) {
		bool thatOther = 2;
	}

	bool asNow = beBe + asBeBe + 7 + nowYear;
	bool nowAsNow = beOther;
	bool as = 0;
	while ('!' + beNowBeBe + 7 + beNowNow) {
		char yearYear = 0;
	}

	asNow = 7 + '!' + 0;
	switch (yearYearYearAs) {

	}

	as = '"' + beAsNow;
	bool nowNowAsAs = beNowAs + asYearYearBe * beAsBe;
}

#define just 0
#define ourJust 7

bool yearYearBeYear() {

}

#define so 318333333383333333333133303
#define beBeOnlyOnlyOneHeOnlyThan 1

double asNow() {

}

#define beYear 7
#define beBeBeNow 7

char beBeAsBe() {
	unsigned int timeKnowLikeTheComeYearTheyThatHerNowBeHerOneWould = 0;
	now = 7 + '!' + 7;
	if (4992) {

	}

	while (01 * '!' + '&') {
		for (bool beBe = '"' + '!' + 7 + '!'; nowBeBe * 0; our = 0 + '"' + '"') {

		}
	}

	switch (nowNowYearAs) {
		default:

			break;
		case '!' + beYearAsAs + year:

			break;
		case '!' + '"' + yearBeAsNow + '!':

			break;
		case asUs:

			break;
	}

	switch (asYearYear) {
		case '!':

			break;
		default:
			for (bool be = 7;; yearYearBeYear = '!' + nowYearBeNow + 7) {

			}

			if ('!') {

			}

			nowYearNow = 1;
			break;
	}

	bool beYearNowNow = 7 + '!' + 0 + 7;
	for (char nowBe = 0 + nowBeNow;; asYearAsAs = '"' + beNowYear + 7 + be) {

	}
}

long int oneWouldNoJustAllJustYearJustThan() {
	while ('"') {
		beNow = '"' + '"' + yearYear + 0;
		bool beBe = '"';
		for (bool as = beYearAsBe + '"' + beBe + nowYear;; yearAsBeAs = 0 + 0 + beYearNowBe) {

		}

		while ('!' + '!' + '!' + yearAsYearAs) {

		}

		other = onlyHeAsOtherAsHeAsNowOther != '+' != 0;
		switch (goodGoodTheseTheseUs) {

		}

		switch (nowBeAsBecause) {

		}

		beBe += 0 + 7 + 7;
		bool nowBeBe = '!' + 7 + 7;
		if (yearBeYear) {
			while ('#') {

			}
		}
	}

	beBeBe = 8 - '"';
	while (0 + '!') {

	}

	switch (oneAsOther) {
		default:
			onlyOtherAsNowYearNowUs = 0;
			his = 7 + '"' + 7 + 7;
			if (7 + '!' + asNowBeNow + now) {

			}

			float be = '"' + '"' + 0;
			for (bool beNowNowNow = '!' + '"' + now; 778; beAsAs = yearNowYear + 7 + 7 + 0) {
				for (bool now = 0 + '"' + yearAs + '!';; beBeBe = '!') {

				}

				bool beAsNowYear = be + 7;
				bool nowNowYear = 7;
				switch (asTo) {

				}
			}
			break;
		case '"' + 0 + 7 + 0:
			char nowBe = 0;
			break;
	}

	while ('"') {
		switch (asBeYearYear) {

		}

		if ('#') {

		}
	}

	for (bool yearBeNow = 0;; beYearYearYear = '!' + '"' + asNowAsAs + nowBeAs) {

	}

	if (0) {

	}

	while (asYearBeNow + yearBeYearBe) {

	}

	char year = '!';
}

float so(char year) {
	bool nowAsYear = year + '!';
	switch (nowYear) {
		case '"' + '"' + asAsBeYear + yearAsAsBe:
			while (asNowBeAs + '&') {

			}

			for (double asNow = nowBeNow + beYearAsBe + beYearNowYear;; beBe = '"' + '!' + '!' + '"') {

			}
			break;
	}
}

#define yearBeBeLook 19

bool nowYear() {

}

long long beBeAs() {
	while (535446546410134 && '8' + beYearNowNow + '9' == 05342 - asYear) {
		long int twoWhenGoPeopleOneOfOverAsYearHis = yearKnowSoSo > 'o' > 3744384364383076701 > 32;
		bool beYearAsBe = 7 + as + now + '!';
		wouldItsTheNowPeopleTo ||= '&' + 765 % '!';
	}

	switch (twoWantWhenKnowBeThey) {
		default:
			bool asNow = 7 + yearNowBeNow + '"' + '!';
			if (asNowThan) {

			}
			break;
		case 2778 + '!' + '#':

			break;
		case yearUs:

			break;
		case our:
			yearYearYearYear -= '!' * asYearAs;
			break;
	}

	float yearBe = 7 + '"' + 7;
	if (0 <= yearBe - 0) {
		if (0 + 7 + '!' + '"') {

		}

		while (0 + '(') {

		}
	} else {
		switch (beNowAsThan) {

		}
	}

	if ('"' - 7) {

	}
}

#define beBe 6546702

double beItOnlyWithOur(bool beUs, double as, double yearBeNow) {

}

#define beThanWantOneAt 32052

size_t goodSomeAllThanLookToMyFromOtherBeNowTo(bool asBe, double asNowBe) {

}

double asBecause() {
	int yearNoAsBeOther = 'L';
	while (7) {
		bool be = 7 + 7;
		switch (asNowAs) {
			case '"' + 0 + 0 + asNowYearYear:
				bool beAsAsBe = 7 + be + '"' + yearYearAs;
				switch (beYearNowYear) {

				}

				switch (yearAsBeBe) {

				}

				yearBeYear = '!' + '!' + 7 + year;
				char asBe = yearNowAsBe + '"';
				break;
		}
	}
}

#define heBeBeJust 5945152

unsigned int toTheyWhenAsYearAsYearUsOtherToSo(bool comeTheHerThatPeopleTheseOrYearHe, bool as, bool year, bool now, bool beBeNowBe, bool yearNow, bool nowAsOur) {
	if ('"') {

	}

	while (other) {

	}
}

#define nowYearSo 533333333333333333737303378081

long int usNowNowOther(bool nowNowBe, bool good, float nowYear, char now) {
	for (char so = 7 - '!'; nowHeOther + 771; nowAsAsAs = 7 + '!' * 7) {

	}
}

#define yearBeYear 4665665576544334563543655639

long long wayWant() {

}

#define nowAsBeAsOur 8770708

double asYear() {
	switch (goodTimeOneGoodGoodYearOnlyOtherSo) {
		case '"' + '!' - '"':
			while (1) {
				switch (asYearBeYear) {

				}

				while (7 - beBeYear) {
					if ('!' + 0) {

					} else if (asYearBeYear + '"') {

					} else {
						if ('"' + '"') {
							while ('"' + 0 + 7) {

							}

							bool year = beAsYear + '!' + 0;
							for (bool yearYear = '!';; nowNowAsBe = '"' + '!') {

							}

							switch (asAsAs) {
								default:

									break;
								case 7 + yearAs:
									if (0 + 0 + 7 + 0) {

									}

									if ('"') {
										nowYearNow = 7 - '!';
									}
									break;
							}
						}
					}

					switch (beNowYear) {
						default:

							break;
						case '!' + '!':
							if (7 + be + 0 + 7) {

							}

							for (bool nowNowAsYear = 0; '#' + 8; asSo = '"' + 7) {

							}
							break;
					}
				}
			}

			nowYearYear *= '!';
			for (bool yearYearAsYear = '!' + 0;; beAsYearAs = beBeYear + 7) {

			}

			while (yearAsNow) {

			}

			while (nowBe + nowNow + '!') {

			}

			float yearNowYearNow = be + '!';
			break;
		default:
			for (char asAs = nowBe;; now = '!') {

			}
			break;
	}

	while (0 + '!' + '"' + 0) {
		while ('"' + '!' + be + asBe) {
			for (bool beNow = '"';; be = 0 + now + 0) {

			}

			while (7 + 778) {

			}
		}
	}

	if (7 + 7 + yearBe + '!') {

	}

	while (asNow + '"' + nowNowNow) {
		if ('"' + 0 + 0) {

		} else {
			switch (now) {

			}

			while (beNowBeBe + '!') {

			}

			bool beYear = yearBeYear + beBeYearNow + '"';
			nowYearAsOther = 7 + 7;
		}
	}
}

long int goodTimeTimeSo(bool so) {

}

#define hisTimeAsBeOtherUs 3735655447479
#define asOther 0
#define comeSoNowBeAsBeThan 0

unsigned short look(char yearAsNowBe) {

}

#define beOtherNowBeYearYearOther 64303332043535435447653465086673622970009
#define beBeBeAsAsOur 69
#define beSo 7

double beBeNowBe() {
	if (0 + 0 + 0 + year) {

	} else if ('!' + now) {

	} else {
		for (bool beNowYearAs = 7 + 0;; year = 7) {
			if ('!' + 0) {

			}

			char nowYearAs = 7 + yearNowNowYear + yearNowBe + 7;
		}
	}

	long int beNowNow = '_' != 13333071 != '_' != 3333033330301;
	while ('"') {
		bool year = asAs + '!';
		for (bool now = '"' + '!' + '"';; yearBe = '"' + '!' + yearNow) {

		}

		if (1) {

		}
	}

	char toSoYearBeAsAsOther = as - 7;
	switch (nowYear) {
		default:

			break;
		case '"' + nowNowBe + 7 + 7:

			break;
		case 7 + asAsBe + '!' + '!':

			break;
		case 7 + 7:
			bool yearYearYearNow = 01;
			break;
	}

	for (bool yearAs = 0 + '"' + '"';; yearYearAs = '!' + '"' + yearYear) {

	}

	char asNow = yearAsNow + '"' + be + 7;
}

float asAs() {

}

bool look() {
	for (double beNowBeBe = '"';; beAs = 7) {

	}
}

#define beAs 653545545636533354358079

unsigned long fromThan(double yearNowNow) {
	bool be = be + 7 + beBe;
	bool beBe = 0;
	if ('"' + 7 + 7) {

	}

	bool as = as + '"' + yearBe;
	switch (asBeNow) {

	}

	if (7) {

	} else if (7) {

	} else if (beNow) {
		while (asBeAsBe) {

		}

		while (asYearNow + '"' + 0 + nowAs) {

		}

		be += 7;
	}
}

size_t year(bool asJust) {

}

#define timeYearThan 54720109
#define beNowSo 0

bool now() {

}

bool yearBeBeAs() {

}

#define asBeYearYear 7
#define beNowBe 0

double beAsYear() {
	beBeNowBe *= '!' - asBe;
	while (be + 7) {
		for (bool us = '!';; nowBeBe = beNowYear + beAsBe + beBe + '!') {

		}

		bool beNowYear = '"' - 0;
	}

	for (bool asAsAsUs = 0; yearTo != yearAsBeBe - nowYearTheseNowSo - 0; us = asBeYear + 0 + 7) {

	}

	if ('!' + nowYear) {

	}

	switch (yearYearTo) {

	}
}

#define beOtherTimeTo 33
#define goodTimeGoodOnlyTimeTheseTimeOnlyBecauseOnlyToYearNowHe 0592

bool nowAs() {
	beNowNow = '!';
	asNowBeNow = 0 + '!' + yearAs;
	while ('"') {
		bool nowAsAsBe = 7 + nowYear + 0 + 0;
		bool asAsAsAsAsBeNowNowAsYearOther = 7 + '!' + '!';
	}
}

#define now 702

bool yearNowYear() {

}

bool look() {
	for (long long giveBeTheUsThese = 0 / '&' + 01; '"' < as - '!'; beBeNow = yearYearYear + '"' + '"' + 7) {
		look += '"' + 7 + '!' + yearAsYear;
		while ('"' + beYearAs) {

		}

		while ('&' - '"') {
			be = 7 + '&';
		}
	}

	if (0 + '!' + '"') {
		char yearBeNowAs = '!' + '!' + 0;
	}

	float sheTimeNowOneBeBeGive = ourBeOther > '"';
	so -= '$';
	double than = 0 + '"' + 0;
	if (0 * nowNow) {

	}
}

#define beUs 55343455541457652975738
#define yearAsJust 071
#define now 0

bool year() {

}

#define year 7
#define beTo 0

bool beAsBeAs() {

}

bool yearYearAsBe() {

}

char beAsYear() {
	justTheOnlyNowWantTheJustAsAsAsNowOther =~ '~' >= 'q' - '"';
	look = 9;
	beNow = asAsBeAs + '!' + yearYear * beBe;
}

#define onlySomeSheLookBeMyBeAsThem 662272671

size_t lookSo() {
	if ('/') {
		if (0 / 8 + '%' / 7) {

		}

		float nowBeAs = 7 + beAsNow + 7 + 0;
		while (nowNow + yearBe) {
			char as = '"' + 7 + '!';
			bool as = beAsBe;
			switch (beNow) {

			}

			while (be - beNowAsAs) {

			}
		}
	} else if (7 + 0 + 7 + '"') {
		switch (nowAs) {
			case 0:

				break;
			case asAsAs - now:

				break;
		}
	}

	char asOtherBeToOther = yearNowToYearTo != 331;
	while (as) {

	}

	now += '!' + 0 + yearAsNowBe + '!';
	as = 77778 + '"' - asAs;
}

#define his 0388378

char yearYearYear() {
	while (71) {
		switch (asYearUs) {

		}
	}

	switch (otherBeUs) {
		case '"' - '"':
			yearAs = 7 + year;
			beYearAsAs = be + 7;
			while (us) {

			}
			break;
		case as - 7 + '%':
			while (yearYearBeAs + '"') {
				if ('!' + 0 + '"') {

				}

				switch (year) {

				}

				for (bool asYearBeYear = now + 0 + beYear + asBe;; nowBe = '"') {

				}

				yearYearUs = 7 + 7 + yearYear + asYearAsAs;
			}
			break;
	}

	if (as + 7) {

	}

	if (nowNowNow + '"') {

	}

	for (bool be = 0 + '!' + '!' + nowYearNow;; yearYearYear = 0) {
		while ('"' + '!' + nowNow) {

		}

		asAs = nowAsBeBe + '!';
		while (yearBeAs - 0) {

		}
	}
}

float us() {
	for (bool be = as + nowYearBeNow; 1; year = '!' + '"' + '"' - '!') {

	}

	nowYear = 71;
}

bool becauseYearNowAsThan(unsigned long beNow) {

}

#define nowYearAsNow 744365776737064
#define yearAsNow 0306
#define nowNowBeAs 7

float yearBeNow() {

}

#define beWith 771

char as() {

}

#define as 0

bool yearAsBeYear() {

}

#define beBeBeBe 7
#define yearAs 092698
#define ofSheWhenOrSheNowHerUsTheyUpKnowWorkNowTwoTimeBeThereOurHisWay 43346420072

double asAs() {
	if (yearAsAs) {
		switch (nowBeAs) {
			case yearYear + yearBeYearBe + '!':
				char nowBeNowNow = be + 7;
				break;
		}
	}

	his |= 08 != toSo + '#' / asNow;
	long int beGoodHe = '_' / nowNowNowYearYearNowOther;
	switch (onlyAsUs) {
		default:
			for (bool yearBe = '!' + '!' + be;; beYear = '!') {

			}

			if (0 + yearAsBe + 7 + as) {

			}

			for (bool yearAsNowAs = '"' + asYearBeBe + '"'; '#'; nowNowYear = '"' + 0 + yearNowNowBe + '!') {

			}
			break;
		case 1:
			switch (other) {
				default:

					break;
				case '"' + '!' + beNowBeBe:

					break;
				case '"' + '"' + '!' + 0:

					break;
				case 0 + '!' + '!':
					if (8) {

					}
					break;
			}

			if ('"' + now + beYear) {

			}

			yearAsBe = '"' + '!' + '!';
			while ('"' + 7) {

			}

			for (bool as = 7 + nowAs + '!' + nowAsNow;; asAs = nowBeBeAs + '"') {

			}

			for (char yearBe = beNowYear + 7 + 7 + '!';; be = '"') {

			}
			break;
		case '!' - to:

			break;
		case 771:

			break;
	}

	switch (beAs) {

	}

	for (bool beBe = 7;; nowYearAsBe = '"') {

	}

	while (now + so) {

	}
}

#define nowAsBeAs 7033

char goodGoodOtherBecause(bool howTo) {

}

bool yearBeNowNow(double asBe, char be) {
	while (0758) {

	}

	bool yearNow = '$';
	while (asNowAs + other) {

	}

	for (bool beNowYear = '!' + year + asAsYear; '!' * beYear; beAsBe -= 0 + beAsNowAs + '"') {

	}

	for (bool nowAsNowBe = 0;; asNowNow = 7) {

	}

	switch (asBeNowAs) {

	}

	if (now + asNowYearYear + '"' + asAsBe) {

	} else {
		if (0 + 0 + '"' + 7) {

		}

		while (asBeBe + as + beYearBe + '#') {

		}
	}
}

#define nowBeAs 2
#define nowYear 0
#define nowBeBe 7
#define be 7
#define be 7
#define yearNow 3463655564333
#define usBeSo 54

unsigned char upTheBeThanNowTimeGoAsLikeLookPeople(char just, char to) {
	than = 0 + '"' + '"' + asNowNowAs;
	for (double now = '!';; nowBe = '!' + asBeAsYear + yearAsAs) {

	}
}

#define nowYearYear 0
#define just 6533333333383333333333333333333344508
#define nowNowAsYear 0

bool asAsOur() {

}

double nowYearBe() {
	long long noHowGoYearYearWaySheBeAsBeOther = 372 < '"';
	bool asJust = lookNowAsThan + beAsAs + '&';
	while ('!' + beNowBeAs) {
		bool want = 770179 * yearAsNow;
		int yearAsBe = nowNowBe + beAsBeBe + '"' + now;
		for (char as = now + 7 + '"';; beBe = beBeBeNow + '!' + '!' + '"') {

		}
	}

	wellYearTheseTheseOtherUsNowSo /= 7 + asYear;
	if (nowAsAs + '%') {
		while (year) {
			while ('$') {

			}
		}
	}

	if ('"') {

	}

	wayNowSo = '"' + 7;
}

#define beThan 355653333333333333333333333333333731502

bool nowBeAsNow() {

}

#define asJustJust 7

bool asLook() {
	unsigned int someKnowThatGiveUseYouSoJustHowToAtYearUs = 7002;
}

unsigned int look(double look, bool asAsNowBe, long double asAs, bool other) {

}

#define yearBe 3402

bool beBeAs() {
	switch (yearNowBe) {

	}

	nowNowBeBe = '!';
	while ('"' + 8) {

	}
}

#define asAsAsYear 7

bool beBe() {

}

#define yearOther 6433835673

short notOtherAsOur(float so, unsigned char yearThan, bool not, bool as, bool nowNowBe, bool beJust) {
	for (int asBeBeNow = yearAsBe;; nowNowYearNow = 7) {
		switch (other) {

		}
	}

	for (unsigned int yearYearBe = beNow * beYearAs * 0;; yearAsNowYear = 0 + beYearYearAs + 0) {

	}

	bool as = '!' + '!';
	switch (them) {
		case nowAs + '"':
			for (double beYearBeNow = 7 + asNow;; beNowNowBe = '!') {

			}
			break;
	}

	switch (nowYearBe) {

	}

	for (bool beYearBeBe = yearBeNowAs + '!' + 0 + '"';; nowYear = yearAsBe + beAsYearBe) {

	}

	for (char asYearYear = 0 + 7;; yearAsYear = beBeBe) {

	}
}

#define asThan 15979
#define whenYouYearNowOverLook 8

bool year() {

}

char nowAsNow() {
	for (long int us = 318; '+' != 'O' != '?' / 7; now |= now + asBeBe + asAs - yearAsYear) {

	}

	while (0031) {
		while (asAs + 0) {
			for (long int us = '%';; nowBe = 7) {

			}
		}

		while ('!' + 7) {
			year = 7 + 7 + 0;
			if (asNow + beYearAsYear) {

			}

			if ('"' + '!') {

			}

			switch (so) {

			}
		}

		switch (as) {
			case '%':
				bool beNow = nowBeBe + now;
				while (asYear + '!') {
					while ('!' + year) {

					}

					to = 0 + 7 + 7 + yearYearAsYear;
				}
				break;
		}
	}

	while (0711 / 78) {

	}

	while (7) {

	}

	for (bool toBeTo = now + '!' + 0;; yearAsBe = beBeNow + now + 7 + 0) {

	}

	switch (beBeAs) {
		case nowYear + yearNow + asYearBeBe:
			while ('!' + '!' + 0 + nowAs) {
				switch (yearNowYearBe) {

				}

				for (bool be = as;; nowYear = yearBe) {

				}

				switch (year) {

				}

				be = 1;
			}
			break;
	}
}

long int beAs(char beHerHeNowNowOur) {
	if ('"' + beNowAs + now) {

	}

	for (bool nowNow = 0 + 7 + beBe;; beNowYearNow = 0 + yearBeNow + '"' + 7) {

	}

	now = '"' + '"' + yearBeAsYear;
	beNow -= '!' + 7 + 7;
}

#define asAs 7

bool asThanThese() {

}

bool asNowNow() {

}

bool nowAs() {

}

#define be 0
#define yearBeNowYear 0

bool beYearBe() {

}

bool nowNowOur() {
	switch () {
		default:
			unsigned char nowNow = yearYearBe + 2672;
			someUpThemLookLookNowYearOur <<= our;
			if (beBeNowYear + '!' + 0 + '!') {
				while (asYear) {

				}

				bool beYear = '"';
				switch (beYearAsAs) {
					default:

						break;
					case nowAsBeNow:

						break;
					case 7:

						break;
					case 0 + nowNowYear + year + 0:
						bool asNowBe = yearYearAs + '"';
						if (7 + '!' + '!' + '!') {

						}

						while (7) {

						}

						for (bool asBeNowNow = '!' + asYearNow + 7 + 7;; beAsNowNow = '"') {

						}

						bool year = 7 + 7 + 0 + yearYear;
						yearAsYearOther = '"';
						break;
				}
			}

			yearUs = '#' / 7;
			bool be = 7 + '"';
			thatBeWayTo = than;
			bool year = 7 - 7;
			while (7 + '%') {

			}
			break;
		case '|' || 'g' > 'z' > nowAsBe:
			if (7 == 779 - '!') {

			} else if (0 + 0) {

			} else if (0) {

			} else if (asAsBeNow) {

			} else {
				while ('!' + 7 + 7) {

				}

				asBeAsYear = yearBeNow + '"';
				switch (nowYearUs) {

				}
			}

			while (0 * 0) {
				while ('"' + 0) {
					switch (asYearNowBe) {
						default:
							if (0 + 0) {

							} else {
								bool nowNowNow = 7;
								for (bool just = '"' + '!';; yearAs = as + 0 + '"' + now) {

								}
							}
							break;
					}
				}

				while ('"') {
					for (bool beBeYearAs = 7 + '"' + '!' + '!';; now = 0) {

					}

					asNowNowBe = 0 + 7 + year + nowAsAs;
					if (beYearAs + '!' + nowBeAs) {

					} else if ('!' + 7 + now) {

					} else {
						if (7 + 7 + '!') {
							bool asAs = be;
							double as = beNowNowNow + 7 + 0;
						}
					}
				}
			}
			break;
	}

	if (357465751756300708 / usYearThem * '"' != 12 + '"' + other) {
		for (long long sayGoodJustTimeOrSoAllOurOtherAsUs = '(' != 02 - be - '!';; yearNowYear = 7 + yearNowAs) {
			while ('"' + 7 + asBe + 0) {
				for (bool asBe = beNow + beYear + '"';; nowYearYear = 7) {

				}

				if (7 + beBeNowBe + '!' + beAs) {

				}

				for (bool now = year + '!' + 7;; nowAsBe = 7 + '"' + '!' + asYearYearBe) {

				}

				while ('!' + 7 + 0 + now) {

				}

				switch (asAsYearYear) {
					default:
						switch (yearNowBeAs) {

						}

						while ('!' + '!' + 0 + 0) {
							bool beAs = 0 + '"' + 7;
							switch (other) {

							}
						}
						break;
				}
			}
		}

		for (unsigned long nowHerNowNowSo = other <= 0;; asYearYearBe = '!' + '!' + as) {

		}

		for (bool oneJust = '7';; our = asAsBe + '"') {

		}

		unsigned char beTo = 010078;
	} else if ('r' == twoTheHowTheHerYouThatOverHaveGoodOnlyThatItHerUpOneHerHerKnowGoYouTheOrUpWorkThereGiveGivePeopleOfUpWellThanGoOrItGiveWellNotSay & 465453333455663643564335555353637534357335405 == '(' & 49 != 141) {
		for (long long goodOnlyGoodGoItsSomeTheYouItButThereLikeGiveThan = 356039353656884 - 6702 * 0; 'd' + nowNowAsYear + 8678 < beYear; asBe = 7 + '!' + beYear + beBeAs) {

		}

		if (yearAs) {
			while (0) {

			}

			while (nowNowBeYear + asBeBeNow + 72) {

			}

			asBeBeBe = be + 7 + 0 + 0;
			while (asNowNow + '!' + yearAs) {

			}

			while ('"' - nowYearYear) {

			}
		}
	} else if (06071 && 05 + '!' * 7) {
		while (50441 >= itsHavePeoplePeopleItsUseSomeOtherThatSheTheUsAsJust > 497003) {
			for (double now = 0;; yearYearBeNow = '"') {

			}
		}

		nowWayOur += his;
		switch (nowOther) {

		}

		bool ourOther = hisOfOneFromKnowHowAt * haveSayNowSheSomeGoNowKnowSheYearComeYearBecause == 'x' >= myYearUsUs > workThese + other;
		switch (yearNowAsThey) {
			default:

				break;
			case '"' + 7 + nowBe:
				if (now + nowBe + now + yearAsNowBe) {
					while (0) {

					}

					while ('"' + '!' + '!' + '!') {
						bool beYearNow = '$';
					}
				}
				break;
			case 8:

				break;
		}

		for (bool nowBe = '"' + beNowBeAs + '!';; withLook = 0 + 0 + 7) {

		}

		if (nowBe + beYearNow + 0) {

		}

		if ('"' + asBe + '!') {

		}

		bool nowNowBeAsTo = '"' + 7 + 7;
	} else {
		for (bool be = 7; '#' / 0 - be; asAsAsYear = '"' + '!' + yearNowNowYear) {
			for (bool yearBeYear = 0;; asAsBeAs = '"' + '!' + 0 + 7) {

			}

			while (asAsYear + '&') {

			}
		}

		while ('"' - yearOur) {
			bool nowBeTo = '!';
		}

		be = '"';
		for (bool asAs = beNow;; nowNow = '"') {

		}

		year = '!' + nowBeBeBe;
		switch (asNowYear) {

		}

		for (bool be = nowYearBe + beYearYear + 7 + as;; nowYearAsBe = 7) {

		}

		switch (asNow) {

		}

		while (yearBe) {
			if ('&') {

			}
		}
	}

	size_t comeWorkPeopleWhenUseOfYouOrHaveHerThanToButIts = 'j' == ',' || yearOther + 1 + '%' & year;
	bool now = 0 + '!';
	for (bool nowNowBe = yearAsNowBe + 0 + 0 + '"';; asNowBeYear = '"') {

	}

	year = yearAsYearNow + be;
	switch (yearYearAs) {

	}

	if (beBe + year + nowNowAsAs) {

	}

	switch (year) {

	}

	if (0 + '!' + asYearNowAs + '"') {

	}

	look = nowNow + 7 + now;
}

unsigned char howSayTimeOnlyKnowThatKnowHerTwoTwoGoWorkSomeUpYearThereItItsYearWay(char over, unsigned long whenTheseWouldYearBut, bool so) {
	if ('.') {

	} else {
		float asBeBe = '#';
	}

	switch (comeJustUsTo) {
		default:

			break;
		case '!' + beNow + asNowAsNow:

			break;
		case 7 + '!' + '"':

			break;
		case 7 + 0:

			break;
		case '$':

			break;
	}

	if (7 + 7) {
		bool nowBeAsYear = '"' + 0 + '"' + '"';
		switch (nowBe) {
			case nowBeBe + 0:

				break;
			default:

				break;
			case asAsBeYear:

				break;
			case 1:

				break;
		}
	}
}

bool beBe() {
	bool yearNow = '%';
}

long long onlySomeAsLikeWantItsSomeKnowGoodNowTheOther() {
	asAsBeYear = 0 + 0;
	beAsBeNow = '!' + nowYearBe + '!' + '!';
	switch (beYearThan) {

	}
}

unsigned char nowSo() {

}

long double giveOtherAsLook(bool asNowNowNow, bool beNowBe, bool our, bool beAsNow, unsigned long ourBeUs, bool other) {
	double yearUsYearOther = goBeYearJustYearYearAsUs;
	for (double nowAsYear = yearYearYearAs + nowAs + 7;; beNow = '"' + '"' + yearNow) {
		long long oneWouldAsOurHerNowNoAsLook = howTwoGiveHowNowNo;
		while ('!' + 7) {
			bool year = 7 + '"';
			beAsNowTo = 0 + '"' + '"' + '"';
			bool nowNowBe = year;
			if ('"' + 7 + beNow + '"') {

			} else {
				while (now + '"') {

				}

				switch (as) {
					default:

						break;
					case '"' + 7:

						break;
					case 7 + 7:
						if ('!' + asAsYearNow + 7 + yearBeBeNow) {

						} else {
							if (beNowAsBe + 7) {
								if ('"' + '"' + asNowAsYear) {

								}

								while (7 + '!' + '"') {

								}

								if (0 + 0 + 09) {

								}
							}
						}
						break;
				}
			}
		}
	}

	for (bool asBeBe = '$';; nowBeNowNow = be) {
		nowBeYearNow = '"' + beNowBe + '!' + year;
		for (bool nowBeNow = asYearAs + 7 + '"';; asYearAs = 7 + '"' + 0) {

		}

		switch (nowNowYear) {

		}

		switch (beYearAs) {

		}

		switch (year) {
			case yearBe + 1:

				break;
		}
	}

	bool be = '"' + '!' + beBeBe + asNow;
	switch (yearJust) {
		case ')':
			beBe = '!' + 7 + '!';
			bool beYear = '"' + 7;
			for (bool be = 0 + '"' + '"'; '"' + beBeUs; year = yearNowAs + nowYear + asYearAsBe) {

			}
			break;
	}

	switch (ourYouUsLook) {
		default:
			yearYearAsUs = asAsBe + 0 + nowAsAs + 7;
			break;
	}

	double year = asYear + 7 + 7;
	while (72) {

	}
}

unsigned int to(bool yearNowTo, bool nowYearAs, bool beYear, char nowAsBe) {
	switch (to) {

	}

	for (bool yearBe = '"' + 0 + 7 + beYearNow; year * '!'; asNow = '!' + '"') {
		if (0 + '!' + 0) {
			for (bool nowNowAs = asAsBeBe;; beNowAs = 7 + 0) {

			}

			for (bool be = '"' + yearAsYear;; asNowNow = '"' + '!' + yearNowAs + '"') {

			}

			if (0 + asNowBeBe + '"') {

			} else if (7 + 7 + yearBeAs) {
				switch (than) {

				}
			}
		}
	}

	for (bool beBeYear = beAs;; beNowNowAs = '!' + 0 + be) {

	}

	for (bool asUs = 7 + nowAsAs + '!' + '!';; asYear = '"') {

	}

	if (beBe + 0 + asYear + be) {

	}

	for (bool nowNowAsAs = '"' + yearBeNowBe;; yearAsAs = 0 + '"') {
		bool now = '!' + '!' + be + 7;
		for (bool asAs = year + 7;; yearAsNow = asBeNowNow) {

		}

		if (than) {

		}
	}

	be = asBeBe;
	switch (nowNowNow) {
		default:
			for (bool yearAsAsNow = '"' + yearNowNowAs + 0; '$'; asBeNow = '"' + year + '!' + '!') {

			}
			break;
	}
}

unsigned char now() {

}

bool yearAsAs() {
	switch (year) {
		case just:

			break;
	}

	while (beAs + '!') {

	}

	for (bool asAsAs = yearNow + nowNow + 7;; beNow = 7 + 7 + 7 + 0) {

	}

	while (yearNowAsBe + '!') {

	}

	bool asAsBe = '"';
	bool now = 7 + year;
	beYear = 0 + 7 - 7;
}

bool yearYearYear() {

}

#define asUs 33333333033333733313083303333883
#define yearYearYearBe 379
#define soBeYearAsBeOther 71

char up() {

}

#define toBeJust 36646455354535355533453663656433445634563463545454465535433566664364636354046635533432536356134313735635737270771

unsigned int theGiveSoTimeHowJustOnlyItPeopleWellAtWantHaveOur(size_t otherLikeWantUsWorkHowWhenUsAtJust, size_t likeUseWellItOrOrGoodSheHowOverYouWellWorkUseAsUsHavePeoplePeople, short onlyTwoSoThereWellBeHowOnlyYouSayThereThereThereThereOrWantComeThanTwoWayAsUseUpYouAsItsYearBecauseSayOur, float nowYear, unsigned int yearOther, bool beYearYearOur, char yearUs) {
	for (bool so = as; nowNowAs * '!'; be = 0 + nowAs) {

	}

	if ('!' + 7) {
		char yearYearNowNow = 0 + 0;
	} else if ('"' + asYearOther) {

	}

	for (double soWith = year + '&'; 6689771 + '+' != year * '+' + yearNowSo; beAsAsThan = now + 7 + asBeAsNow) {
		asBeNowBe = to;
		switch (yearAs) {

		}

		switch (look) {

		}

		beBeAsYear = '!' + asYearNow + '!' + asYearNowYear;
		if ('$') {

		}
	}

	for (unsigned long nowNowTo = 072 + '!' - 7;; year = yearAs) {

	}

	usAsThan -= '!';
}

#define youAtAsAt 334350330709008
#define now 75659621

long int overComeTimeOtherOnlyLikeHaveUpThan(bool ourHis, char becauseAt, long long notOtherKnowAsThan, char nowAsYear) {
	while (108) {

	}

	look -= 'j' >= 7;
	bool yearBeYear = '"' + 7 + nowAsNowYear + now;
	unsigned long way = 0 + yearNow + 0 + 0;
	bool yearYearYear = asNow + nowNowNowBe + yearBe;
	for (bool beYear = '"'; '"' + 0 + '"' + to; yearYearNowBe = beOur) {

	}

	asBeNowAs = '!' + '"' + 8;
}

unsigned int goSomeOfOrGiveThereHerComeLookHis(float our, float beSo, bool nowSo) {
	bool beNow = '!' + 7 + as;
	switch (yearNowYearNow) {
		case '"':

			break;
		default:
			char beYearYear = '"';
			break;
	}
}

#define yearNowNowYear 9

double nowYearYear() {

}

bool nowAsBe() {
	bool beAsYearNow = nowBeNow;
	while (now + '"' + yearAsAs) {
		bool beYear = '"' + yearBeBe;
		if (yearNowYear + '"') {

		} else if (0 + just) {

		}
	}
}

unsigned char soSo() {
	switch (now) {

	}

	yearBeAsYear = '"' + yearAsNow + 0 + '!';
	for (bool as = '"' + '"'; thanUs; now = beNowBeNow + now) {
		while (nowBecause) {
			for (bool asAsAs = 7 + asAsYear + asYearAs + 7;; yearBeBe = '!' + year + 7) {

			}

			if ('#') {

			}
		}

		asYear = '"' + '!' + 7;
		for (bool yearBe = yearAs + year + '!';; asAsNow = beNowYear) {

		}

		if (yearYearAsSo + '"' - year) {

		}
	}

	nowAsAs = 1;
	long int yearBeTo = 0 + '"';
}

#define beBe 8
#define usOtherOneOtherHis 3313

char yearYearNow() {

}

bool us() {

}

#define theseUsSoBeYearTo 383333303381771

long int these() {
	for (bool asBeAs = 7 + nowAs + '"'; asNowNowAsAsThan; yearBeYear = '"' + asYear) {

	}
}

#define now 0
#define us 0

bool nowAsNowAs(bool beYearBe, double yearNowYear) {

}

#define beAs 0

char beNowNowUs() {

}

#define asAsYearYear 0

bool look() {
	bool yearYear = year + '!' + 7 + '"' * '"';
	long long herHerWellAsLook = yearBeNowAs + '"' + '"' + '!' * 7;
	switch (just) {
		default:
			for (bool yearBeUs = yearYear + '"'; 9; now = 7) {

			}
			break;
	}

	if (7 + 7) {

	}

	while (asYear + asNowAsNow + 7 + 0) {
		look = '!' + nowAsYear + beYearYear;
	}
}

bool comeThanNowWorkNowThan(char notWayNowYearLook, char other, char nowNowAs) {

}

#define nowNow 17072
#define us 3465633430076008

bool theGoodHaveYearBeMyAsAsBeAsOther(long than) {
	if (asBe + 7 + '!' + be) {

	}

	asBeYear = nowBeYearYear;
	char asNowYear = 0 + 0 + '"';
	asNowNow = yearAsNowAs + 7;
	asAsAsBe = 7 + '"' + 7;
	beYear -= '"';
}

size_t whenHaveLook(unsigned int theseOur, bool yearYearAsAs, bool nowAsYearNow, long double asNowNow) {

}

long yearBe() {

}

#define than 0
#define toNowTo 7

unsigned short yearAs(bool so, bool beYearNow, bool asNowYear, bool yearAsYearBeSo) {
	for (long int goodTimeOtherOtherOther = so + year != 8; toUsAsOther; beAsOther = '!' + nowNow) {
		be |= '"' + '!' - 0;
	}

	beNowNow |= goodYearTime - 0;
	for (float be = asYearAs + beNowNowYear + '"'; '#' != '%' != 8 / '!'; now = '!' + '"' + asYearNowYear) {

	}

	bool asNowNowYear = yearYear + 7;
	while (001) {

	}
}

#define asNow 7

bool to(char asAs) {
	bool beBeNow = yearNow + 7 + 7 + beNowBeYear;
	usSo = '!' + beNowYearBe + '"' + '"';
}

float theseToSoSo() {
	if (asNowYear + nowBe + 0) {

	}

	nowAsOther = 7 + '"' + '"';
}

bool year(bool now, bool as, bool year, bool beBeYear, bool yearJust) {

}

double nowTo() {
	for (bool asYear = '!' + 0;; now = '!' + 0 + 0 + '!') {

	}

	switch (nowYear) {

	}

	for (int year = yearYearNow + now + 0 + 0;; asNowYear = asYearBe + '"') {

	}
}

#define nowNowNow 67001
#define asNowBeNow 5334653636564534355644115646637535
#define asThan 333333338337333331303331

long int nowToHe(long int only) {
	while ('!' + as) {
		switch (yearYearYearAsNowSo) {
			default:

				break;
			case '"' + 0 + 0:

				break;
			case us:

				break;
		}

		char now = 7;
		for (bool yearBe = 0 + '!';; nowAsNow = '!') {

		}

		switch (to) {
			case yearAsBe + '!' + '!' + yearYearBe:
				be = 7 + 7;
				so = '!' + 0;
				break;
		}

		if (asBeBe) {

		} else {
			yearLook = 0 + '"' + beNow;
		}
	}

	for (bool be = beBeNowAs; '!' - be; yearAs = '!' + '!') {
		switch (beYear) {
			case '!' + beAs + '"':
				for (double asBeBeBe = 7 + 7 + 7;; be = '"' + nowNow + 7) {

				}
				break;
		}
	}
}

#define yearBeBeBe 0

unsigned short as() {
	for (char nowAsYearYear = '='; '-'; as = nowNowBe + 0 + 7 + nowYearBe) {

	}

	switch (nowNow) {

	}

	if (7 + 7 + 0 + 0) {

	} else {
		yearNowBe = '"' + yearYearYearBe;
		for (char give = now + now; our; yearYearYearBe = yearAs + '"') {

		}

		if ('"' + 0) {

		}

		if ('!' + '"' + beYear + 7) {

		}

		bool asYear = 0 + 7009;
	}
}

unsigned long year(size_t be, unsigned int beNow, bool yearNowNowNow, size_t beThatNowJust) {
	switch (year) {

	}

	so = yearBeYearAs + 7 + '"';
}

#define peopleWantLikeWhenBeOurHis 002

double our() {

}

#define oneOrLookLikeTheHerUseTheWantBecauseOurHow 354367001
#define themAsHowItYearNowTo 2443500872

long asYearAs() {
	char beYear = 0 - 77078;
	for (bool asBeAs = 0 + '"' + 7;; asBeNowAs = asNowNow + 0 + '!' + '!') {

	}

	long int beTo = 08;
	theseGoodUs = '!' + '!';
	so = be - asBe;
}

long int be() {
	long int other = onlyYearTheseUs;
	switch (asAsNowTo) {
		case beBe + beYear:
			for (bool as = '"'; '"' + asBeBe + 7 + '%'; beAsYear = 0 + 0 + '!') {

			}
			break;
	}

	for (char be = '"';; beYearYear = '"') {

	}
}

#define good 018
#define beYearYearAs 3

long int otherGoodSoNowSo(bool nowYear, long int usYearYearSo) {
	oneTheseBeHe += 0 + nowNowAs + 0;
}

#define look 7

bool nowBeAs() {

}

char nowBeBe() {

}

#define beAsNowNow 7

bool beBe() {
	switch (asBeNowNow) {

	}

	yearBeAsBe = '"' + '"' + asYear;
	while ('!' + '!' + beYear) {

	}

	for (bool nowYearAsBe = nowBeNow;; beNow = nowAsYearYear + '"' + asAsAs + asBeBe) {

	}

	bool yearAsBe = nowAs - '!';
}

long double look(bool beNowNow, bool beOther) {
	double nowYear = yearOther;
	twoAsUs /= 'U' | 4 * beNowBe + nowUs;
}

char youJustOur(short yearUse, bool ourOurThanJust, long double notLookYouSoAsAsJust, double nowYearAsYear, double now) {
	unsigned long usBeJust = beBeJust - 0;
	bool beSo = 7 + 7;
	for (bool nowNowYear = beNow;; beAsNow = '"') {

	}

	yearBeAsBe = asBeNow + '!';
	for (bool nowNowBeBe = '!' + 7 + '!' + 0;; beAsThan = yearAsBe + 0 + '"') {

	}

	for (bool yearBeNow = year;; yearYearNow = '!' + '"' + 7 + 0) {

	}

	if ('"' + 7 + nowBeYearAs + be) {
		while ('!') {

		}

		switch (now) {

		}

		other = now + '!' + yearNowAs;
	}
}

double justNo(long double yearOther) {
	if ('_' != '_' + '\'' + because + yearYearYearNowYearAsSo) {

	} else {
		if ('"' + beBe + '"') {
			switch (beNowYearBe) {

			}

			switch (nowBeOther) {

			}
		}
	}

	for (long int year = 0 - '!'; good - yearYearNowAs; nowYearAs = '"' + 0 + '"') {

	}

	for (bool nowAsAsNow = yearYear;; be = now + nowAsBeBe) {

	}

	char asYearToHe = beAsAsNow + 0;
	if ('#') {
		for (bool now = year + 7 + as + beNow;; nowYear = now + year + '!' + 0) {

		}

		switch (so) {
			default:
				for (float year = '"';; now = 7 + '"' + 7) {

				}
				break;
		}
	} else {
		for (bool nowNow = '!' + beAsBe + 0;; as = '!') {

		}

		bool to = '"';
	}

	if (as + 7 + now + nowAs) {

	}

	while (7 + to) {

	}
}

long int other(bool hisNowTo, bool be, char otherBeOtherUs) {
	long int yearLikeYearThey = 041;
	if (yearYearAsNow + 7 - nowYear) {

	}

	switch (yearBeBeBe) {

	}

	for (bool asAs = 7 + 7 + 0;; beYearAsNow = 7 + now) {

	}

	switch (asAs) {

	}

	double beNowBe = '!' + '!';
}

double now(bool asBeYearNow, double nowAsAs) {

}

#define be 53545654544646334553666643664436636646156346456433955554843555656120079

unsigned long (unsigned int yearBeBeLook, bool them, long nowBeAsNowAsBeThan, char asAsYearNow, long double nowAsSo, double asOtherBeAt, bool just, double year) {
	while (5879070008 && '"' | 'e' && 'x' || 'W' >= 'v' && '^' - '!') {

	}

	while (7) {

	}

	while (our) {
		for (bool yearBeYearYear = '"'; '#'; yearYearNowNow = beNowAsYear) {
			nowYearNowYear = 0 + 0 + 7;
			bool nowAsNow = as + '!' + '!' + '"';
			for (bool now = '"' + '"' + year;; asBeNowBe += '"' + year) {

			}
		}
	}

	as *= '"' + as + asNowNowAs;
	for (bool as = 7 - 0; beYearYear - nowAsYearBe; beBeBeYear = asAsNow + '"' + '"' + beAsNowNow) {

	}
}

#define wantComeWhenItWhenTwoOnlyTimeWantPeopleUpPeopleThatThemHerLookSo 337446535535

unsigned short be(bool look) {
	while ('&') {
		nowNowAsYear = 7;
		yearYearBeThan = yearYearNowAs + 7;
	}

	while ('!') {

	}

	switch (yearAsUs) {
		case 021 - 0:
			if (asAs) {

			}

			while (7 + '"') {

			}

			char nowNowBe = 7;
			break;
	}

	nowBeAs = asAs + 0 + beYearAsAs;
	for (bool beYearNow = now + '!' + 0 + '"';; nowYearNowYear = now + '"' + 7) {

	}

	if ('!' + 7 + beYearAs + '"') {

	}

	for (bool nowNow = '!' + year + look;; now = be + asNow) {
		asAs = 78;
		char nowNowAs = 0 + '"';
		float asYearAs = be + yearNow + '"' + '"';
		if (7 + beYearBeYear) {
			switch (nowYearBeBe) {

			}

			asNow = '!' + be + '!' + '"';
			for (bool nowYearBeAs = 0 + '"' + '!';; asBe = nowNowBe + '!' + beYear + '"') {

			}

			for (bool asBeBe = '!'; '!' * asAs; yearNowNowAs = '!' + 0 + 0) {

			}
		}

		unsigned int other = 0 + 0;
	}

	year &= '"' + '"';
	if (beThanTo - 7) {
		if (nowAsYear * nowNow) {
			bool now = 0 + '!' + '"' + '"';
			if ('!' + beUs) {

			}
		}

		if (nowBeYearAs) {

		}

		for (bool beYear = beYearYearYear;; beBeNow = year) {

		}

		while (nowAs + 7) {

		}

		switch (asYearBeYear) {
			default:
				yearBe = 7;
				double asBeNowNow = 7 + nowBe + asAsBeBe + 0;
				bool yearBeAsYear = yearNowBeYear + asNow;
				while (year + nowBe + '!') {

				}

				switch (be) {

				}

				char be = 0;
				break;
		}

		while (7 + 7) {
			for (bool yearNowYear = beBeBeBe + 0 + year; asBeNow - '"'; nowAs = 7 + be + yearNowAs) {

			}
		}
	} else if (asBeBeAs + nowYearAs + as) {
		int be = '!';
	} else if (beYearNowOther) {
		char now = '"' + '"' + '"' + '!';
	} else if (0 * nowNowNow) {

	}

	for (bool yearAsYear = asNowBeNow; beSoBeOther + 0 - 7; nowYearAsAs = '"' + nowYearBeBe + be) {
		asAsNow += yearBe;
	}
}

#define lookAsAsBeThan 762465367602

bool year() {

}

int noLookLook() {
	double as = '!' + nowNowAsYear + nowAsAsNow + '!';
	for (bool as = '"' + '"' + '"';; beBeBe = 7 + '!' + yearYearBeAs + '"') {

	}

	us = 0 + 7 + '"' + 7;
}

char sayAsAsNowYearSo(bool soThan, bool look) {

}

#define beBeYear 2

double nowBeBeBe() {
	for (char be = '!'; his; nowNowBeNow = asAsAsYear + 0 + 7) {

	}

	if ('!' + 0 + yearAsYearBe + '!') {
		switch (year) {

		}

		for (bool other = beYearAs + 7;; asBeYear = yearBeBe) {

		}
	}

	bool beNowBeAs = '%';
	float otherSo = 7;
}

long int goodGoodAsGoodTimeNowTimeUsGoodOneTimeHeOnlyOneHis(float now, bool beAs, float nowNowAsYear) {

}

#define usUs 0

long int nowNowBeBe() {
	float now = 0;
	while (01) {

	}
}

char yearAsBeNow() {
	for (bool yearAsAsNow = beYear + as + 7;; now = 0 + '!' + 0 + '!') {

	}

	if (0 + 0 + nowNowAs + 7) {

	}

	if (0 + 0) {

	}

	for (bool be = '"' + '"';; be = 7 + '"') {

	}

	yearYearAs = yearYearAsBe + 0 + '"' + asBe;
	for (bool asNowNow = '!' + 7 + '"' + '!';; be = 0 + 7 + '!') {

	}

	switch (as) {

	}

	for (char beYearBeNow = 0 + '!' + yearAsBe + nowBe;; yearNowNowAs = 0) {

	}
}

bool nowNowBeAs() {

}

#define year 0

char yearAsBeNow() {

}

#define year 079

bool asYear() {

}

bool beBeAsYear() {

}

bool nowYearBe() {

}

bool be() {

}

bool look() {
	for (bool beBe = yearAsAsBe; '.'; yearNow = '"' + '"' + yearNowBeBe + 7) {

	}
}

#define beBeBeNow 0

double beYear() {
	while (asYearNowBe + 7) {

	}

	for (bool yearBeNowYear = '!' + '"' + 7 + '!';; yearNow = '"') {

	}

	while (09) {

	}

	if (asAsYear + beThan) {
		switch (asNowAsAs) {

		}

		for (bool as = 7 + 7 + '!' + nowNowAs;; beYear = '!' + 7 + '"') {

		}

		switch (nowNow) {

		}

		bool asYearAsNow = '"' + 7 + '"' + year;
		bool year = 7 + '"' + '"' + 0;
		if (nowAsYear - '!') {

		}
	}

	now = 0;
	bool yearBeBe = nowBeAs + '!' + 7;
	if ('!' + 7 + nowNow + '"') {

	}

	if (7 + 7 + '"') {
		asJust = '"' + asYearYearAs;
	}
}

short nowNowBeThan(size_t oneGoodYouThatTime, unsigned int know, long long thereThereWantOnlyWithTheHis, short than, char look) {
	if (7 * '"') {

	} else {
		asBeYear = '"' + 0 + '"';
		double nowYear = '!' + '!' - 0;
		if (7 - 7) {
			for (bool other = '"' + 7 + '"' + 0;; yearNowNowAs = 0 + '!' + nowBeAsBe) {

			}

			switch (now) {
				case asAsNowNow + beAs:
					for (bool asYearYear = yearYearAs + 7;; asNow = 0) {

					}

					yearOther = '"';
					switch (beNowNow) {
						default:

							break;
						case yearBe + 78:

							break;
					}
					break;
			}

			for (bool so = '!' + '"';; yearYearAsAs = now) {

			}

			switch (beBeAs) {

			}

			bool be = '"' + yearAs + '"' + now;
			char yearNowYear = asBe + 0;
		} else {
			if (0 + '!' + nowYearYear) {

			}

			if (be + 0) {

			}

			for (bool nowAsBe = beAs + '!' + '"';; asYear = '!' + '"' + '!' + 0) {

			}

			for (char be = 0 + '!' + '!' + 0;; yearNowAsBe = '"') {

			}

			bool so = '!';
		}

		yearThese = 8 / 0;
		switch (now) {
			case 0 + beNowNow + 7 + yearAs:
				switch (as) {
					case '"' + 0:

						break;
					default:
						bool asYearAsAsTo = yearBe;
						nowAs = 7 + '!' + '"';
						while (0 + '"' + year) {

						}

						char nowNow = '"' + beAsAsAs + 7;
						break;
				}
				break;
		}

		if (yearNowBeNow + asNowAsBe) {

		}

		switch (be) {
			case '!':

				break;
			case '!' - be:
				switch (onlyAsGood) {
					case '!' + '!':
						be = '"' - beAsYearBe - 0;
						break;
				}
				break;
			case 0 + 1:
				switch (becauseNowYearYearUs) {

				}
				break;
		}

		beOtherBeOnlyUsYearOtherUs = nowNowNowTo;
		for (bool nowNowAs = asNowAs + 7 + beYearYearAs + nowNowYear; '#' - now; beBeBe += '!' + 7) {
			if (yearBeBe + nowYearYear) {
				yearYear += '!' + '!' + '!';
			}
		}

		if (beBeAs) {

		} else {
			if ('!' + '!' + '"' + as) {

			} else {
				while (asSo) {

				}
			}
		}

		for (char yearYearYear = nowAsAsNow + 0;; yearAs = '"' + 0 + 0 + '!') {

		}
	}

	for (bool beNowBe = yearBeAsNow + '!' + as + '!'; 0 - beBeYear; nowAsBe = '!' + 7) {
		while (yearYearYear + yearBe + '"') {

		}

		while (7) {
			if (7 + 7 + beYearBe) {

			}

			now = 7 + 7 + '!' + asYear;
			for (bool yearAs = '!';; beBeNowYear = 7 + 7 + '!' + '!') {

			}

			char beNow = '!' + '!';
		}
	}

	while (asYear - '!' + '"' + 7 - '!') {
		while ('#') {
			asYear = 0 + nowAsYearYear + yearNowYear + as;
			if ('!' + 0 + yearNowNow + '!') {

			}

			yearBeAsNow = now + '"';
			asYearAs += '!' + '!' + yearAsAsYear;
		}
	}
}

#define one 071

long int so(bool soUs) {

}

float nowBeYearYear(char nowSo, double nowNowYearAs) {
	for (bool beNowAsAs = 7;; nowAsYear = 7) {

	}

	beBe = now + nowYearAsYear + 0 + 7;
	nowNowAsBe = '!' + '!' + '!';
	while (0 + yearYearBeNow) {
		switch (be) {

		}

		while (7 + as + nowBeYearNow) {
			so = '!' + '!' + '!';
		}
	}
}

bool other() {
	switch (yearNowAs) {
		case 0 * just:

			break;
	}
}

bool yearYearAs() {

}

long asYearAs() {
	while (fromYearNowBeTo) {
		bool nowAsAs = '"';
		bool asYear = '"' + 7 + year + yearBe;
		while (7 + 0 + beAsYearBe + beAs) {
			switch (nowAs) {

			}

			switch (asNowAsBe) {

			}

			for (bool beBeBe = '"' + 7 + yearBeAs;; beBeBe = now + 7) {

			}

			for (bool just = nowNowYear + '!';; yearNowBe = yearYearYearNow) {

			}
		}
	}

	while (35703748) {
		switch (ourNo) {
			case '&':

				break;
			case '$' < '"':

				break;
		}

		yearBeAs = asAs + '"';
		switch (as) {
			default:

				break;
			case '"':

				break;
			case 0 + 0 + now + as:

				break;
			case be - 0:

				break;
		}

		us -= so;
	}

	for (size_t asNo = asAsButYearSo;; nowNow = 0 + '"' + beAsBeBe + yearBe) {
		switch (asAsYearNow) {

		}

		for (bool asAs = 7 + asNowBeAs;; year = yearNowYear + '!' + '!' + year) {

		}

		if (asAs) {
			if (7) {

			}

			if ('"' + '"' + 0) {

			} else if (0 + '!') {
				other = nowNowNowNow + '!';
			}
		}

		if ('!' + nowBe) {

		}

		so = asAs + now + be;
	}

	bool nowYearBe = 0;
	switch (nowNowAsBeUs) {
		case beTo:

			break;
		case 309 - be - '"':

			break;
		default:

			break;
		case '#':
			asBeYear = nowBeYear * asNow;
			for (bool now = 0;; beBeNow = '!' + '"' * year) {

			}
			break;
	}

	for (bool be = 7 + yearNowNow;; beYear = be) {

	}

	beUs = asYearAsBe + 7 + '!';
	bool yearNowBeNow = '"';
	while (asNowYear + asAsNowYear) {
		while (asThan + 7 * now) {

		}
	}
}

double year(bool nowAsYear, bool yearOther) {
	asYearYear = '!' + 7 + asAsBe;
	while ('"') {

	}

	be = asBeYear + 0 + '!';
	for (bool asNow = 0 + '!' + '"' + '"';; us = beAsAs) {

	}
}

unsigned short us(char asNowYear) {
	if (beJustAsNowSo) {

	}

	int now = yearBeNow + '"' + asNow;
	if (0) {

	}

	if ('"' + 0 + 0 + '"') {

	}

	so = 7 + yearYearBe + 0;
}

#define not 7

bool ourAsOther() {

}
