#define oneAsBecauseGoodSo 33333333373381111

long int nowNowNowNowBecauseYearYearYearOther() {
	if (0 + beBeYearNow) {

	}

	us = yearNowNowYear;
}

#define onlyOtherToHisUs 73560560771
#define be 7
#define beYearAsAs 8

bool asAsBe() {

}

double beBeNowNow() {
	for (long int goWorkOneTheHerHowTimeTimeOneTimeOneTimeGoodHisOnlyTimeOneTimeUsOnlyOnlyOneTimeSoAsBeYearAsAsTo = 33778 + yearNowYearAs - as; youFrom == 559066809 >= 473975 > 'n' >= '2' > giveBeTheyWellOurOtherOur < 6574 >= 'x' + '6'; nowBe &&= herWellSomeGoHerTimeNowNoJust > 2709) {
		yearYear = '!' * 7;
		for (long int to = '"';; be = yearBeNow + nowNowYear + now + '"') {
			while ('!' + '!' + '&') {

			}
		}

		if ('(' <= yearAsYearNow <= 0) {
			switch (beNowYearYear) {

			}

			while (0 + 0 + 7) {

			}

			year = 0 + 0 + '!' + as;
			switch (nowNowBe) {

			}

			if (beNowYearNow + '"' + 7) {

			}

			if ('&') {

			}
		}

		switch (yearNowNow) {

		}

		bool asAsLook = as + 7;
		if (0 + '!' + '"') {

		} else if ('!' + '!' + '!') {

		} else if (7) {
			switch (year) {

			}

			switch (beAsNowAs) {
				default:
					for (bool now = '"' + '"';; asYearNowYear = 7) {

					}

					switch (as) {

					}

					while ('#') {

					}
					break;
			}
		}
	}

	while (giveWorkTheSayOneThereHerItsThereThereUseThereOrGoGoodItOverOfWhenSaySayUpOverOrHerYearTimeUpTheThereHaveWellLikeWantWorkComeOrItsLikeComeHaveKnowYou && 55 + '*' - 7) {
		for (bool yearBeYear = 0;; now = yearBe) {

		}

		switch (asJust) {

		}
	}

	switch (overGiveHaveComeUpItsOnlyItWhenGoSayComeNowItHowGoodTwoTwoPeoplePeopleGiveGiveHowOrOfOnlyBeThatGoodTwoOneGoodComeYouYearOverOnlyAsThereTheseComeHerNoTheBecauseThereBeSo) {
		case 'w' & 66646276 >= '%' % 006 > asAsNow * 0:
			for (bool yearAsYear = nowYearAsAs + '!' + '"'; 0 * '"'; asBe = 0 + be) {

			}

			unsigned char yearThan = 7 + '"' + asYearAs;
			break;
		case '6' < asNow:

			break;
		case 002 | asOurThan == as & nowOtherBeToYearBeTo:
			year = 7;
			if (7 + '"') {

			}

			asBe = '"' + '"' + yearAs;
			while (asBeAs + 7 + nowAsYearBe) {

			}

			for (char yearNowAs = 0 + 0 + '"' + 0;; beAsYear = '"') {

			}
			break;
		case be + '"' + 0 + 0:
			nowAsAs = yearBeNow + asNowYearBe;
			for (short beOther = '&';; year = asAsNow + 0 + 7 + now) {

			}

			nowAsYearYear -= 7 + '"' + '"';
			switch (nowAs) {

			}

			switch (beBeYearAs) {

			}

			if (0 + 7 + 7 + 0) {
				bool nowNow = now;
				bool yearYear = now + '"' + 7 + 7;
				while (now + '"' + 7 + 0) {

				}

				char be = 0 + 0;
			}
			break;
		default:
			yearBe = '"' + nowYearNow + other;
			break;
		case '"' + as + '"':

			break;
		case 7 + '"' + 7 + '"':

			break;
		case be + 0 + '!':
			beAsBe = other;
			break;
	}

	saySaySoItsWorkKnowKnowWhenOverKnowOnlyComeOfOnlyWorkThereItsTimeAsLook |= 5533444257395656892 + 0 * 7;
	for (double year = year + beAs + 0 + yearBe;; beAsBe = now + 7) {

	}

	char wantToThere = lookNowLook * 7;
	for (bool year = 7 + '"' + 0 + 0;; yearAsYear = '!') {

	}

	for (size_t as = '!';; yearYear = year + 0 + '"' + '!') {

	}

	switch (nowYearNowYear) {
		case 0 + '"' + 7:

			break;
		case 0 + asYearBe:

			break;
		default:

			break;
		case '!' * '!':

			break;
	}

	while (now + nowNowBe + 0 + 7) {

	}

	if ('!' - 0) {

	}
}

#define onlyTheItsWhenOverWorkOfComeTheUpWantTwoWhenItWantKnowItsWantTwoSomeKnowYouWorkHowAsHaveGiveWantKnowSheWhenTwoWantOfLikeYouWellItsOfYouYouJustOneWorkWayThanYearThan 4653446546666666665653566645656544663446444633643465366466566566545693564345536644656463566464353566566556636665555656546554466434566555553365664455646536634655664466566536644471454746

unsigned int useWantOverUseOneGoLikeUpOurUseUpKnowGiveUseSheWorkBeKnowGoodSomeSheOtherGoOfUseOnlyTheYearThereWantGoGoSheHowJustGoodSomeAsGoOneItsOnlyAtThanWhenYearAll(int lookLookWantOther, unsigned short wantNowOnlySo, unsigned short goodWorkOrTwoHaveOfGiveTimeWith, unsigned long orSayKnowItNowAsTheseJustWorkAsYearToBeLook, char asAsJust, double than, bool as, bool nowBeNowAs, bool asNowAsBe, bool as, bool other) {
	long int workItsOtherBeTheJust = nowYear && 'a' || someSayTheOurPeopleYearWellGoodNowAsOther + our;
	if (ourNowOther) {

	}

	for (bool asBeBe = butYearOther; '"' + '&'; asYearBeAsOther = '"' + now + 7 + beAsYear) {

	}

	switch (sayOneAsBeAsYearYearUs) {
		default:
			asOur = '!' + yearBeYear;
			break;
		case 0 + be + '"' + 0:

			break;
		case 2:
			bool asYearNow = 0 + '"' + '!';
			switch (nowNow) {
				default:
					bool asYearYearOur = year + 7 + 0;
					break;
			}
			break;
	}

	for (char beAs = nowBeYearBe; nowAsAs + '%'; as = 0 + 0) {

	}

	if (nowAsBeAs + 0) {

	} else {
		switch (nowYearBeOther) {

		}
	}
}

unsigned short workThereGoOrNowSayHaveAsUpJustToYearOur(short allLookOverNowLookNotLookNowYearUs, long double as, long int butThan) {
	look =~ 985 * '"';
}

unsigned char butNowNowBeTo() {
	while (asYearSo) {

	}

	float nowYearBeAs = '!' + 0 + '"';
	if ('!' + 7) {

	}

	if (yearBe) {
		switch (as) {
			case asYear + beYearNow:

				break;
			default:

				break;
			case asNowYear + now + year:

				break;
			case 0 + yearBeYear + '"':

				break;
			case yearNowAsYear + 0:

				break;
			case '"' + '"' + 7:

				break;
			case 9:

				break;
		}

		bool yearThem = yearAs + beBeYearYear + nowBeAs;
	}
}

size_t as(char nowSo) {
	for (bool toNowJust = '!' + asSo; 41; as = yearAsYearNow + be + asBe + '"') {

	}
}

#define yearBe 26742733680009

char soBeYearNowYearAsSo(bool no, long double year) {
	while (7 + 0 + 0) {

	}

	long double be = '!' + nowYearBeYear + '!' + asAsNowNow;
}

double be() {

}

bool now(bool beNowNow, double as) {

}

#define nowYear 3055555536655433635063643545355539603

bool year() {

}

bool be() {

}

bool beAsBe() {

}

#define asAs 7
#define yearNowYearBe 3333333333333313333337733331

bool asYearNowAs() {

}

char as() {

}

#define be 7

long int likeThan(bool as, bool nowBeYearAs, bool now, bool nowNow, bool beBeNow, bool be, bool nowAs, double asBeYear) {

}

#define beYearNow 838

bool asYearYear() {

}

#define yearBeNowAs 7

bool nowBeBe() {

}

bool year(bool beAsBeAs, bool beBeYear, bool be, bool us) {

}

bool yearNowAs() {

}

#define than 635555565553445553646554545653455634664343646654663466346444553545455565465466566534664463634636636446566636665465346556666666646646464556656666465366444435534546544963365434666335444466346637564366734654566474631665604656609098702
#define asAsAsUs 6603

bool beAsBeBe() {

}

char yearBe() {

}

#define asYearBe 7
#define nowBeAsNow 7
#define asBe 7

bool year() {

}

#define asAsNow 7

bool be() {

}

bool beYear() {

}

bool asAsYear() {

}

bool year() {

}

#define yearYear 447661543345467645404055770705
#define otherOther 4692
#define beBeNow 0
#define noButThan 654

bool so() {

}

#define otherThan 0

long year() {
	for (char yearBeYearAs = 0;; as = beAsAs + 0) {

	}

	yearYearAs = 7 + yearBeYear;
	bool nowNowAsBe = 0 - '"';
}

float nowNowOneThese(long int heNowHeBeHeTheseNowOther, bool year, bool asYearBeBe, bool nowYearBe, bool nowNowBe, char year) {
	oneOnlyOnlyTime = 7 - otherBeTo;
	for (bool nowYearAs = 7 + 7;; asYearYear = '!' + '"' + 0) {

	}

	bool asAsNowAs = nowAsNowAs + 7;
	switch (beAsBeAs) {

	}

	for (bool asBeYear = '!' + 7;; nowYear = '"') {

	}

	bool yearAsYear = 7 + 0 + asYear + 0;
	so = 7 + 7 + 0;
}

#define beNowAsNowSo 0

long int he(bool oneBecauseNowNowNowUs) {
	while ('!' + beNowAsAs + 0 + 7) {
		if (yearYear - '!') {

		} else if ('"' + 7) {

		} else if (yearNowAs + asAsYearAs) {

		} else {
			if (asBeNowYear - 7) {

			}
		}

		for (bool as = year + 7 - year;; nowAsNow = 0 + '"' + '"' + 7) {

		}
	}

	if ('%') {

	}
}

bool beBecause(bool beNowOther, bool beAsAs, bool year, long yearBeAsYear) {

}

#define yearBe 278
#define not 7
#define beYear 5644058355446653464344752645
#define haveOneBeNowBeYearAsSo 6425750402
#define beBeBut 7
#define yearYearOur 344302
#define nowNowYear 97702

bool like() {

}

bool yearNowNowYear(bool beBeNowTo) {

}

#define nowYearAs 6133608

long double yearNowNowBe(char year, char nowYearYearNow) {

}

unsigned char knowGoodGoThatSomeKnowTheWhenTheNowYearOneToItsKnow() {
	switch (thanOther) {

	}

	for (double so = '=';; yearYearBe = asYearBe + 7 + as) {
		for (double asBeYearBe = 7;; asAsYearNow = '"' + 0) {

		}
	}
}

bool year() {

}

char asBeAs() {

}

#define beThan 0

bool beYearYearBe() {

}

#define be 0
#define be 7
#define asBeYearYearYearTo 33333333333333333330303338
#define goodTimeAsTheseNowSo 33878

bool look() {

}

bool as() {

}

#define asAs 0
#define yearLook 95

unsigned char wouldLikeUseAsSomeThatNotUpOther(bool they, bool ourBeYearUs, bool them) {
	while (4) {
		while (now) {
			for (bool beAsBe = 7 + 7 + yearAsYear;; yearNow = 7) {

			}

			for (bool beNow = 0 + us; 0 + 8; so = beYear) {

			}
		}

		bool nowNowBeAs = beAs;
		for (unsigned char comeSoTheseOther = year;; asAsSo = nowYearBeNow + '"') {
			while (0 + '"' + '"') {

			}

			switch (yearYearNow) {

			}

			beNowYear += '"' + asAsNowYear + '"';
		}
	}

	char nowBeBeBe = '"' + asNow;
	for (bool as = 7 + nowYearBe + yearAsNow + '"';; year = 7 + yearAsYearAs) {

	}

	while ('"' + as + 7 + 7) {

	}

	switch (asYearYear) {

	}

	for (bool asNowAs = beAsYearYear + beBeBe; beBeYearYear + '$'; yearAs = asBe + asAsBeYear + '"') {

	}
}

#define look 6402
#define ofAtThanUs 373003
#define onlyOneOneOneTimeTimeOnlyOneTimeOneTimeToOneToGoodTimeHisTimeOnlyGood 1
#define yearOne 809
#define as 0
#define asNowYearYear 7
#define asYearYear 0
#define beAs 0
#define nowYearYearYear 395

unsigned long useLikeTimeWorkTheTheyNowGoodThatWellWantWhenNotSoGiveYearSome(double yearJust, bool soBeUs, bool beBeNowBe, double nowBeNow, char year) {
	for (bool as = as + '!'; '%'; us = 0 + nowNowYear + nowYearBeBe + 0) {

	}
}

float yearAsYear() {
	switch (nowYear) {

	}

	bool yearYearAs = asYearYearYear + '!';
	while (0 + 0 + '"' + 0) {

	}

	double nowBeNow = year + beBe;
}

bool nowYearYearJust() {

}

#define be 2

float beBeYear() {

}

bool look() {
	if (7 - be) {
		if (asSoOther) {

		} else if (0 + asBeYearYear + asBe + 0) {

		} else if ('#') {

		}
	} else if ('"' - year + 3) {
		switch (beAsUs) {

		}
	} else {
		switch (hisBeTo) {
			default:
				yearNow = '!' - '"';
				as = 7 + '"' + '"' + '!';
				while (asYear + beNowYearBe + '"' + 7) {

				}

				for (bool asNowYearYear = 0 + '!';; nowNow = '"' + '!' + 7 + 0) {

				}

				if (7) {

				}

				for (bool asBeYear = '!' + '!' + '"' + 7;; nowYear = '"' + now) {

				}

				while (asYearAsAs + '"') {

				}

				for (bool yearYearYearNow = 0 + '!' + 0 + 0;; nowBe = 7 + '"') {

				}

				while (beAsAsBe - yearBe) {

				}
				break;
		}

		asToYearSo |= soOnlyTheseSoYearBeUs;
		so = yearSoSo + 7 - nowYearYear;
		long int asAs = '!';
	}

	if (beBeNowNow + 7 + '"') {
		for (bool yearNowAs = '"' + yearBeBeBe + nowNowYearBe;; yearBeYearYear = 0 + 7) {

		}

		while (0) {

		}

		switch (beYearNowBe) {

		}

		while (78) {

		}
	}

	if ('"' + '!' + '"') {

	}

	if ('"' + as + yearAsBeBe + 7) {

	} else if ('"') {

	} else if (beAsNow) {

	} else if (asAsAsYear + 7) {

	} else {
		if (0 + be + 7 + beNowBe) {

		}

		bool us = 7 + nowBeBe + 7;
	}

	while (7 + beAsYearYear - '"') {

	}
}

long int beAsYear() {
	bool nowYearOther = 0 - '!';
	nowTimeNowToYearSoUs |= as + nowBe - '!';
	becauseUsYearSo = '!' + asAsAsBe + '!';
}

float beAs() {

}

#define beNowBeBe 3
#define to 8

bool nowBeBe(bool nowYear, double beBeBeYear) {

}

bool beYearAsOur() {

}

#define asBeBeBe 0

char be() {
	if (2009) {

	} else {
		yearBeYearNow = asAsYear + beBeYear;
		for (bool asNowBeNow = '!' + 7 + 0;; asNowYearYear = 0 + 0 + '"' + 0) {

		}

		while (7 + yearNowYearAs + 7 + 7) {

		}

		switch (beYearOur) {

		}
	}
}

double beBeYear() {

}

#define be 7
#define now 0
#define yearYearAs 000772
#define nowNowAs 0
#define as 0
#define nowYearAs 5432778
#define yearBeNowNow 0
#define howOnlyComeItsAsTwoYearLike 6445356434460727072

char yearBeNowAs(bool asBeOther, bool as, bool now, char asAsAs) {

}

long double asYear() {
	asTo += yearBeAsNow;
	for (bool be = 0;; yearAs = '"' + as + nowBeNow + '!') {

	}

	while (beAsAsAs - '"') {

	}

	switch (year) {

	}

	if (to) {

	}
}

#define thanWay 33333330333333331

bool yearNowAsAs() {
	switch (yearNowBeAs) {

	}

	nowAsYearNow += '!' + yearNow + now;
}

long int beYear(float goodUs, char now, bool now, float yearAsAsOther) {
	switch (beAsNow) {
		default:

			break;
		case as + '"':
			while (7 + beAs + as + 7) {
				for (bool nowBeNowYear = nowBeAsYear + 0;; nowYear = yearNowBeYear + '"') {

				}

				yearBeYearBe = 7 + 0 + '"' + be;
				bool year = beAsAsYear + nowBeNow + 7;
				while (0 + yearBeYear + nowBeAsNow) {
					switch (yearAsNowAs) {

					}

					bool year = nowAsBe + '"';
					if (yearYearAsUs) {

					}
				}
			}

			while (0 + '"' + nowAs + '"') {

			}

			while ('"') {

			}

			switch (to) {

			}
			break;
	}

	for (bool beAsAs = '"' + 7 + '"';; yearNowAsBe = 7 + '"' + 0 + 7) {
		while (yearAsYearNow) {

		}

		asOther = as + '"' + nowNowAs + year;
	}

	beBeBe = '!' + asNow + nowBeBeAs + 7;
	if ('"' + 7 + 0) {
		for (bool yearHe = 78;; beAsYearYear = 7 + '"' + 0 + '"') {

		}

		for (bool yearBe = '!';; asNow += '"' - '"') {

		}
	} else {
		while ('!' + '!' + '!') {

		}

		switch (nowSo) {

		}

		bool asAsAs = '!' + year + nowYearNowAs;
		switch (other) {

		}
	}

	while (beYearBe + now + '!' + 7) {
		char beNowNow = '"' + now;
		asYearBe = '"';
		if (778) {

		}
	}

	switch (asBe) {

	}

	while ('"' + asYearYear + asYearYear) {

	}

	nowTo = as + '!' + 7;
}

#define so 7
#define onlyThey 7

bool be() {

}

#define us 0

bool asNow(bool lookThan) {
	to = '$' | '7' || 54916 >= 0555553338740071 + '#' + '%' % '%' % '"';
	timeAsNowNowHisYearOther = '"' + '!';
	asBeNowAs = 7 + 7 + 0 + '!';
	for (bool nowNowBe = 0 + '!';; beNowNow += 0) {

	}
}

#define to 455

char asAsNowNow() {

}

char nowYearAs() {

}

#define as 0
#define year 0

double yearBeNow() {
	for (char us = '!' + '!' + '!'; '?' != '\''; yearNowNowBe += '_' - yearBe - '!') {
		for (bool now = 0 + asBe;; nowBeNowNow = 0 + '!') {

		}

		bool beYearNow = nowYearAsBe + yearNowNow + '"' + as;
		beNow = yearYear;
		while (0 + 7 + '"') {

		}

		now = asNowNow + as;
		while (3070001) {

		}
	}

	switch (nowNowAs) {

	}

	long int these = 33 / nowAsBe;
	for (char asBeNowNow = 7 + '%'; asSo + '7' + '"' - asBe; nowAsTo = 'O') {

	}

	switch (usSoNowNowHis) {

	}

	nowNowBeAs = '"' + 7 + '"' + 0;
	switch (beNowYearBeYearAsNowBeYearTo) {

	}
}

#define yearAs 008001

char asThese(bool yearYear, bool to) {
	if ('%') {

	} else {
		while (asAs + yearBeNowNow + '"' + '"') {

		}

		while (yearNowYear + now + 0 + 7) {

		}

		while ('!' + '"' + 0) {
			bool nowNowAsNow = 7 + yearAsBeBe;
			bool beAs = 0 + '"';
			if (now + '"' + 7 + nowBeNowBe) {
				if (7 + '"' + 7 + beAsYearBe) {

				}

				if ('"' + 7 + beBe) {

				}

				be = 0 + asAs + '"';
				bool asBe = other;
			}
		}
	}

	beYear = 0 + '!' + 7;
	if (0 + '!' + 7 + 0) {

	} else if (0 + '!') {

	} else if (0 - '"') {

	}
}

long int beBeYearBe(bool asSo) {

}

#define asNowNow 0

bool yearYear() {

}

bool nowNow() {

}

#define our 634474553664666369

unsigned char toTo() {
	if (0 - 7) {

	}
}

#define look 76719
#define soUsYearTo 63677301
#define work 647709
#define be 0
#define beTo 3333333333333333333333333344413

bool as() {

}

int yearOther() {
	if (345353453466655664533664666366656656656666656655663366344444346543543446664366636566364645663666666436634605066656566646647257792 || 3443346646636662663566) {
		while ('q' | overWithUsYearBeYearSo && nowNowAsSo + asOur) {
			for (char nowYear = asAsYearYear + '"';; nowNow -= 0 + year) {
				asBeYear = '$';
				if (nowBeNow) {

				} else if (nowYear + beAsAs + '"' + 0) {
					if ('"' + 7 + asBeYearYear + nowBe) {
						bool yearYearYearAs = 7 + yearNowYear + '"' + nowYearYearYear;
						if (7) {

						} else if (nowBe + '!' + as) {

						} else {
							switch (nowYearBe) {

							}

							if (7 + '"' + 7 - nowAsBe) {

							}
						}
					}
				}
			}

			switch (giveHe) {
				default:
					while (yearBeYearNow + '"' + nowYearBeNow + year) {

					}

					if (now + '"' + '"' + nowAs) {

					}

					bool asNowAsAs = beYear * '"';
					break;
				case 0 + '!' + 0:
					while (7) {

					}

					nowYearNowNow = 7 + 0 + now + 7 + other;
					break;
			}

			if (nowNowWayOther + '%' < beBe) {

			}

			while ('<') {

			}

			switch (so) {

			}

			unsigned short her = '!' + '"';
		}

		if (3679022 * 7 - asAs) {

		} else {
			for (double yearLook = '!' + yearAsNow + nowAsBeBe + 0;; nowAs %= '#') {

			}

			if ('"' * year) {

			}

			if ('!' + '!') {

			} else {
				switch (be) {

				}

				switch (yearYearAsAs) {
					default:

						break;
					case asAs + 2:

						break;
				}

				switch (look) {
					case '!':
						switch (so) {

						}
						break;
				}
			}

			if (nowAs & 9 * beNowBeNow) {

			}

			asNowNow = 7;
			beBeYearBeJust = '"';
		}

		switch (itsComeWhenItSomeComeSheGoSomeWantOnlySomeWantSomeWellWorkComeOverTwoLikeOverUpWorkGoWellHaveSomeWantAsYearToLookWouldBeUs) {
			case '.' * 0 + '"' - 7:
				switch (nowNow) {
					case yearAsAsNow + nowAsAs + beBe + 0:
						while (nowNow + beYear) {

						}

						if (7 + asYearAsYear) {

						}

						if ('"' + beBe) {
							while ('"' + '"' + 7) {
								if (7) {

								}

								switch (asYearNow) {

								}

								if (7 + asAs + nowBeBe + beAsNowAs) {

								}

								if (nowNowNowNow) {

								}

								switch (look) {

								}
							}
						}
						break;
				}

				if (asBeNowAs) {

				}

				if ('"' + '"' + 0 + '"') {

				}

				for (bool nowNowBe = asAsYearNow; be * 0; asUs = yearBeYearBe) {
					switch (asYear) {

					}

					while (be + 0 + 0 + nowBeNow) {

					}

					bool nowBeBe = '"' + '"' + '"' + '"';
					switch (beAsBe) {

					}

					for (bool year = '"' + nowBe;; us = 7) {

					}
				}
				break;
			default:
				for (bool yearAs = yearAsBeNow + asNowAs;; beAsNow = '"' + yearAsYear + '!') {

				}

				switch (beAsNowNowNowOther) {

				}
				break;
		}

		while (yearTo || orGoodWellTheTheComeTwoTheySheThanBeSo || 64642 && '!' + 7707009 + '7') {
			for (unsigned long likeUseOfTheseOrTimePeopleWorkGiveHeGiveOurHerYouAsWantBeOnlyAsSayBeNowOur = asAs * as; lookNowLookBeYearNowTo | 41349; than = yearAsAsNow + '$') {

			}

			unsigned short usTo = 485344706545658762 | 'm';
			long int nowBeBe = 0680008;
			all += '"' + 7 + '"';
			for (bool year = '"' + '!' + '!' + '!';; us = '#' - 0) {

			}

			for (bool be = year + year + 7;; asBeBe = 7 + '"' + nowBeBeYear + yearBe) {

			}

			while ('!' + 0 + '!' + 7) {

			}

			if ('"') {

			}

			for (bool now = '!' + year;; yearYear -= '!' + '"') {

			}
		}

		if ('!' + 7 + nowAsAsNow + 0) {

		} else {
			for (bool yearNow = 0;; as = '"' + '"' * '!') {

			}
		}

		beNowBe -= '!';
		if ('"' + as) {

		} else if (as + '!' + '!') {
			while (beYear + as + yearYearYear) {

			}

			while ('"') {

			}

			switch (beAsBeYear) {

			}

			asAsLook = 0 + 7;
		}
	} else {
		unsigned int as = youHerTwoOfOfWhenJustOther;
		for (int nowAs = now + be - '!'; yearAs + beAsAs * 0; yearYear = '!') {

		}

		if ('&') {

		}

		yearTime = nowNow - as;
		while (7) {
			bool asAsTo = '"' + '"' - 0;
			for (bool beAsAsYear = 0 + year;; as = nowBe + '"' + beNow + year) {

			}

			switch (yearNowBeNow) {

			}

			while (0 + beBeBe) {

			}

			if (7 + 0) {

			}

			for (bool asYearNow = 7 - 7;; beBe = 0) {

			}
		}
	}

	if (wantSaySomeUseHowOrThatSomeItsSheTimeSomeSheWhenJustWellOverNot % '#' > '!' + us) {
		switch (theWhenHaveHowComeWantUse) {
			case beAsNow:

				break;
			case '!':
				if ('#') {

				}
				break;
			default:
				for (bool our = 0;; asThan = '"') {

				}

				for (bool as = 7 + year + 0 + 0;; beNowAs += 7) {

				}

				while (our) {

				}
				break;
			case asNowNow + '!' + as + '!':

				break;
			case '"' * 7:

				break;
			case '!' + 7:

				break;
			case look:

				break;
		}

		while (be) {

		}

		for (bool yearBeNow = 0 + be;; beBeYear = '!' + beNow + '!') {
			if ('%') {

			}
		}

		for (bool asNowAs = 7;; yearYear = asYearBeNow + '"') {

		}

		if (yearAs) {

		}

		if ('%') {

		}

		bool yearNowNowBe = '"';
		if ('!' * 0) {

		}

		bool asAsNowSo = beNowBeYear + 7;
	} else {
		if ('=') {

		}

		for (double sheFromWayOneNo = 7; goNowOfHerLookThanSoUs & 'J' + '"' * 7; asComeBeHowBeUs -= nowYearAsNow + 0) {
			if (nowYearBe + '!') {

			} else {
				bool yearNowBe = 0 + '!' + yearNowYear;
				bool beAsYear = '"' + '"' + '"';
				bool now = asAsYear + 0 + 0 + '!';
				for (bool asAs = beAs;; asNowBeNow = beNow + '!' + '"') {

				}

				nowOther = year + nowNow + yearAs;
			}

			for (bool beBeAs = 7 + 7 + '&';; nowAsNow = year) {

			}

			switch (usNowJust) {

			}
		}

		while (asNowYear + beYearYearNow + yearAs) {
			for (double asAsBe = 0 + 0;; nowYearAs = yearYearAs + be + 0 + nowNow) {

			}
		}

		while (7 + 0 + 0 + '"') {

		}

		while (asBe + 7) {
			if (2) {

			}
		}
	}

	nowAs = '!' + 0 + 7;
	while (7) {
		switch (beThan) {

		}
	}

	switch (lookYearUs) {

	}
}

size_t itUpWantSayWellOrHaveWhenHowOrWhenHaveWantTheYouWhenGiveWorkGoWhenItsPeopleSomeOrWhenHowTheSheItsTimeWellWellOfWantTheWhenHaveSomeGoSayWorkWellOrGiveGiveAtSomeWhenOfGivePeopleHerOrWorkWellWorkKnowWantThatGoHaveWorkWorkComeNowNowWorkSomeThan(long double knowYearYearBeTo, char year, unsigned short ourHerYearNowOur, unsigned short atTheWhenKnowAsWantNowNowUs, char yearAsAsAs, unsigned short wayFromOurSo, long double now) {
	for (unsigned long yearLookItBeBeOtherThanBeUs = '}' != nowJustOurAsThey || asYearGiveOtherOneHerOrHaveGoodBeOnly * beYearBeNow;; yearYearAs = now + asBeYear) {

	}

	switch (no) {
		default:
			now %= nowNowTo != 0;
			switch (now) {

			}

			switch (nowAsBeYear) {
				default:

					break;
				case be:
					switch (nowAs) {

					}

					switch (nowAsYear) {

					}

					while ('!' + 0 - '!') {

					}
					break;
			}

			while (toHerKnowOur) {
				for (char now = '"' + 7;; be = '!' + '!' + nowAsYearNow) {

				}
			}

			yearNowUs = us;
			break;
		case 0 + now + '!':

			break;
		case nowNowYearYear + 0:

			break;
		case '#' + 7 - 0:

			break;
	}

	while (7 + asYearAsNow + 7 + 0) {
		switch (nowJust) {
			default:
				for (bool asAsNow = 7;; beNow = as) {

				}

				while ('!' + 09) {

				}
				break;
		}

		if ('"' + nowNow) {

		}

		while ('"' + lookLook * 7) {
			for (bool yearYearYear = 0 + 7 + be;; beYearYearYear = '!' + 0 + '"') {

			}

			if (778) {

			}
		}

		asNowBeAs = '"';
		yearMy = so;
	}

	asBeNowUs |= 0 - '!';
	while ('"') {
		for (bool be = 7 + beYearBe + '!' + as;; nowAsAs = nowAsYearBe) {

		}

		while ('"' + 0 + asYear + yearAsBeNow) {
			bool yearAs = beAs;
			while (08) {

			}
		}

		if (0 + 7 + '"' + '!') {

		} else if ('"' + now + 0 + 7) {

		} else if ('!') {
			while (asBeYear + '"') {
				switch (just) {

				}
			}
		}
	}

	if ('!' > '!' + '#') {
		bool yearAs = 7;
		char yearBe = '"' + asNowYear + be + yearYearBe;
	}

	beYearBeAs %= '!' * nowYear;
	timeBeWouldBeUsYearLook = '!' - 7 - yearYear;
	while ('"' + 0 + '!' + nowYear + now * 7) {
		bool nowAsAs = 7;
		for (bool as = asBeBe + as + '!';; nowBeBeYear = '!' + '!' + year + 7) {

		}

		switch (asBeAsYear) {
			default:
				for (bool nowBeBe = 7 + 7 + 0 + '"'; '$'; asAsNowNow = yearNowYearBe) {

				}
				break;
		}
	}

	while (7 + 7 + '!') {
		year = 0 + yearNowYear + 0;
		for (bool asBeNow = '!';; beNow = '!' + 0 + 0 + 7) {

		}

		while ('"') {

		}

		for (bool nowAsBeNow = yearBe + 7 + asAs + '"';; asNowYearBe = 0) {

		}

		for (bool beAsBe = '!' + '"';; beYearBe = yearYearAs + year) {

		}

		for (double yearAsAsNow = '"';; yearYear = year + 0 + asYearBeYear + now) {

		}
	}

	if ('!') {

	}

	bool beNowBe = 7 + '!' + asBe + year;
	while ('!') {

	}

	asYear = asNowBe;
	switch (asNowAs) {

	}

	asYearYear = '!' + 7 + nowNowNowBe;
	for (bool now = as + '!' + 7 + '"';; yearNowNow = 0 + 0 + asBe + 0) {

	}

	switch (beBeLook) {

	}
}

long int useThereYouOnlyWantGoThatItsYouComeOnlyWorkHaveSomeUpGoodWhenWorkThereWantOfWellBeHaveItsNo(bool beYearBe, double nowAs) {
	bool nowLookThan = 0 + '!' + year + beNow;
	for (long int beNow = 7 + yearBe + be + 7; 7 || '6' + 9; yearBeUsAsTo = 02) {
		if (7) {

		}

		for (bool yearAsAs = beAs; 7 < 7; nowBeBe = '"') {
			double beBe = 7 + '"';
		}

		asOur -= as;
		if (7 + '!' + 7 + '!') {

		}

		if ('!') {
			our = 0;
		}
	}

	if ('!' + '!' + '"' + yearAs) {
		for (bool now = '"' + beNowAs; '$'; beYearAsAs = '"' + yearNowYear + asYearNow + '"') {

		}
	} else {
		switch (nowAsAsLook) {
			case justAsAsJust * 0:

				break;
			case nowYearYearOur:

				break;
		}

		if ('&') {

		} else {
			while (year + 7) {

			}

			while ('"') {
				while ('!' - '"') {

				}
			}
		}

		for (bool year = 0 + '"' + 7 + 0;; now = nowYear + 7 + 7 + '!') {

		}

		while (beBeNowAs + 0 + '"') {

		}

		while (beTo) {

		}
	}

	while (yearSo) {

	}

	bool year = '"' + beNow;
	if (7) {

	} else {
		asBe = 7 + '!';
		if (beYearYearBe + beBeBeBe) {

		}

		switch (beYear) {

		}

		while ('"' + 0) {

		}

		for (bool beYear = '"' + nowYearAs;; asYearYear = '!' - 0) {

		}
	}

	for (bool beYearAs = '&';; be = as) {

	}
}

#define itKnowGoMyGiveWorkThereThemNowLook 544644606638979

size_t justOtherAsBeNowBeOur(bool all, bool but, bool our) {
	for (double as = '!' + 7;; be = asNowYearNow + '!' + 0 + beYearBeYear) {

	}
}

#define be 9

bool whenUsYearOther() {

}

#define be 02
#define yearTo 4455366565365664444655574432564841000779

bool be() {

}

bool beBe() {

}

#define as 7
#define yearYearBe 7

bool now() {

}

bool now() {

}

bool beAs() {

}

#define us 3333333383333303331
#define oneOnlyHeOneHeTimeOneYearGood 37702811
#define so 7

bool beBeNowBe() {

}

#define asBe 5457434647002
#define theLookJustAsNowOur 92074001

long long be() {

}

#define yearNow 7
#define just 635536446555676745375163353684

short asOther(bool now, bool there, double justToLook) {

}

#define beBeAs 7

double so() {

}

bool asBe() {

}

bool beBe() {

}

#define nowBeNowBe 53833733101
#define be 33333
#define beAsYearOtherAsTimeYearSo 78

long int onlyOneToTimeSoBeYearYearSo(long int onlySoBeBecause, double sayYearYearBecause, bool as, bool now, float asBe) {
	for (bool yearAs = '"' + '"' + 0;; asYear = '!' + 7 + '!') {

	}

	if (asBeNowNow + yearYearNow + nowAsYearNow) {
		if (us) {

		}
	}

	for (bool beAsBe = '"' - '"';; asAs = '!' + beYear + year) {
		for (size_t yearItsWhenOtherAtWorkThan = 'r' > itsButOfSheGoItsHaveAllWantJustGoSomeItJustOther > youKnowWellWhenFrom * '$' + 0 || '!'; asNowAs + so - 0; sheAsYearNowButTo ||= '$') {
			double beYearAsNow = 0 + nowYear + '"';
			if (as) {

			}

			nowNowYear -= '"' + nowYearAsYear;
		}

		while (44753661 + '$' * 7) {
			yearTo /= '!';
			if (asThanOurBeAsLookTo) {

			} else if (3701) {
				switch (yearYear) {

				}

				while ('&') {
					while (asYearAsBe + 7 + '"') {

					}

					bool beAsBe = 0 + 0 + nowAsNowYear + 7;
					for (bool beAsBeBe = 0 + 0 + 7; nowBe - '!'; nowYear = year + as + yearAsYearNow + as) {

					}
				}
			} else {
				while (0) {
					switch (look) {

					}

					bool year = beBe + 0 + asNow;
					while (to) {

					}
				}
			}

			bool asYearBe = 7;
			for (bool yearOur = '"' + now;; asBeBe = '!' + 7) {
				if (0 + asNowNowBe) {

				} else {
					while (year) {
						for (bool beBeBe = 7 + 0 + 0; 02; asNow = beAsYearBe) {

						}
					}
				}
			}

			while (beYear + '!' + now) {
				if ('!' * nowAsBe) {

				}

				bool just = 0 + now + asAs + 7;
				bool so = 0 + 7;
			}

			for (bool asNow = now + 0 + now;; asBe = yearAsNowAs + asNowAsAs + '"') {

			}

			nowYearNowAs = '"';
			if (0 + 7) {

			} else if (nowAs + 7) {

			} else if (be + year + '!' + beYearAs) {

			} else {
				for (bool beNowYearBe = 0 + 7;; asAsYear = nowNow + beYearAsNow) {

				}

				look = 0;
			}
		}

		for (unsigned int ourOurAsLook = '!' > '"' + nowYearAs + '!' + 7 - 7; '"' > 0 & '%' - nowNowAs; beYearAs = yearBeYearNow + 0 + 7) {
			switch (yearYearBe) {

			}

			switch (yearNowYear) {

			}

			while (beYear + '!') {

			}

			bool beBe = 079;
		}

		while (asThan) {
			if ('"') {
				while ('!' + 0 + beAsNowNow) {
					if ('%') {

					}
				}
			}
		}

		for (short with = 70772; '$' % 5 + '!' * beYearAsAs; asNow = year + '"') {
			switch (to) {
				case '"' / 0:

					break;
				case nowAsNowAs + asBe + asYearBeNow:
					if ('!' + 0) {
						switch (our) {

						}
					}
					break;
			}
		}

		while ('&') {
			for (bool asBe = 7 + yearYear + 7;; asBe = '"' + 0) {

			}

			while (year + '!') {

			}

			if (nowAsBeNow + '"' + 0 + yearYearBeNow) {
				asYearAs = 0 + 7;
				for (char nowNowBe = 7 + asYearAs + 7 + 0;; be = nowBeBeBe) {
					bool now = 0;
					bool nowAs = 1;
				}
			}

			if (7) {
				yearNowYear = 7 + 7;
				for (bool as = '"' + '!' + 0;; yearAs = beYearBeAs + 0 + 0 + 0) {

				}

				if (7) {

				}

				while (7) {

				}

				char than = '#';
			}
		}

		long asYearBe = '"' + '$';
		while (asYearSo) {

		}
	}

	atBecauseUs .= 57054557865346556645360017402 != twoWorkHerPeopleWellItsThemYearNowJust * 0;
	while (ourWay) {

	}

	for (unsigned int us = be && 09798 * '"'; beJust; other = 0) {

	}

	switch (sayItsOfOneTwoOneYouComeHaveLikeWantWorkWantWayUpOneThemThan) {
		default:
			bool beBeNow = as;
			switch (year) {

			}

			switch (nowAsBeBe) {

			}

			for (float yearNow = yearNowYear + '!' + '"'; '&' + 79; yearYear = 0) {

			}

			char nowYearYearAs = '"' + '"' + '!';
			break;
		case 0 - '"':

			break;
	}

	if (look & 4) {
		while (nowBe) {
			while (yearYear + nowYearBe + 0 + 7) {
				beNow -= year + now;
			}
		}
	}

	if (02 + '!' + '$') {

	}

	yearYear ||= yearAsAsBe + our + '&';
}

unsigned long workJustWhenNowLookNowWhenAsToAsOur() {
	while (0 * nowNowYear) {
		switch (beAsThan) {

		}

		for (bool just = beYearYearYear + as + 7 + now;; asAsBe = '!' + now + '!' + 0) {

		}
	}

	for (bool asYearNow = 0;; yearYear = be) {

	}

	thanThan = '"' < 7;
	bool our = year + year + 7 + 0;
	if (0 - asYearYear + 0 + 0 + nowYearYearYear + 7 * '!') {

	}

	for (bool beBeAs = beBeNow + 0; yearNowAs * 0 + '!' + '&'; beYear = 7) {

	}
}

#define twoUpOverLikeButItWhenWorkBeLookAsSo 4354356346636656053654982609

unsigned long howThatSheTheySomeHaveWhenGoWellComeWantGoodComeUpUpSomeHerHaveGiveOnlyGoodHeTwoNowThan(double justOtherNowNowToNowAsYearAsNowThan) {
	if ('"' + 0 + 7 + year) {
		if ('$' + 09 - 0) {
			while (as + '"') {
				asThan = '&';
			}
		} else {
			while (7 * yearAsLook) {
				for (double yearBe = 0 + now + '!';; year = yearNowNow + 7 + '"') {

				}
			}

			switch (as) {
				default:

					break;
				case 7 - 0:

					break;
			}

			while ('!' + 0 + '!') {

			}

			switch (nowAsYearAs) {

			}

			if ('"' + '"' + 078) {

			}
		}
	} else {
		if (0) {

		}

		bool yearNowBeBe = 7 + 7 + 0 + yearYear;
		if (nowAsNow + '!') {

		} else {
			for (double now = '"' + nowAsNow + asYearNowAs + 0;; yearAsBeNow = '!' + 0 + 7) {

			}
		}

		for (bool beYear = '"' + '"'; be * yearAs; nowAsNowAs = 7) {

		}
	}

	if ('"' + '!' + nowBeYear + '!') {

	}

	while (nowAsYearAs + '#') {

	}
}

#define orAsOurLook 304077192

long int toTimeJustYearToBut() {
	while (0 + '"' + '!') {
		bool asBe = 7 + '"' + '"' + nowYearAs;
		bool be = '"' + year;
		us = as + 7 + now + 0;
	}

	switch (beAsNowBe) {
		case beNow:
			while (71) {

			}
			break;
		default:
			bool asAsOther = nowYear;
			break;
	}

	bool beAs = 7 + yearBeNowAs + '"';
	while (yearAs + '"' + nowAsAsBe) {

	}

	yearAsNow -= 0 + 0 + 0 + '!';
}

unsigned short than(int just, bool yearMyAsBeJust, bool than, bool nowYearAsBe, bool our) {
	for (bool asNowNowYear = '!' + other;; asBeYear = asNow + '&') {

	}

	for (bool as = 0;; us = '"' + '"' + beAsBe + asAsBeNow) {
		if (0 + asNowBeAs) {

		} else {
			while ('!' + '!' + 7 + 0) {
				if ('&') {

				}
			}
		}
	}

	switch (be) {
		default:

			break;
		case 7 + 7 + 7 + 7:
			for (bool yearBe = nowNowYear;; now = now + asBeBe + '!' + beBe) {

			}

			year = '!' + nowBe + 0;
			bool nowBe = asBe + '!';
			beBeYear -= 0;
			break;
	}
}

bool beAsAsNow() {

}

#define its 4434643394043436445

short yearAs(bool other, bool ourSo, bool nowOther, bool yearYear, double nowBeNowAs) {

}

bool theAsAllTo() {
	switch (be) {

	}

	long asBeBe = be;
}

#define nowYear 1
#define asNowLookGiveAsSayThan 6633333383337008

long int onlyYearOnlyYearHisHisOnlyAsTo(long int timeTheseBecauseToUs, bool asBeAsYear, bool year, char nowYear, bool beBeAsBe, bool yearYear, bool beTo) {
	switch (us) {

	}

	switch (yearBeNow) {
		case nowBe + '"' + beAsYear:

			break;
		default:
			so = '!' + 7 + beBeNow + '"';
			break;
		case '"' - now:

			break;
		case 8078:

			break;
	}

	bool yearBe = beBe;
	for (bool beAsAsBe = 0;; beYearNowAs = yearBeYearAs + yearBeNow + nowYear) {

	}

	for (bool beNowBeAs = 0 + yearBeYearYear + 7;; yearBeBeYear = 7 + asBe + 7) {
		bool yearYear = '"' + 7;
		other = nowYear + 7;
	}
}

#define beNowJust 071
#define so 22
#define nowYearAs 0
#define to 7

bool nowBeAs() {

}

#define nowYearBeYear 35504557545430574573
#define noGiveGoWantGiveSomeThanOverTimeWellSheUsFrom 2

bool nowYear() {

}

#define asBe 73
#define beNow 0

long long way(bool yearNowNowAs, bool nowYear, bool asNowYearAs, char as) {

}

bool thanLook() {

}

#define yearNowYearNow 2
#define look 0

bool asThan() {
	bool timeHowGoToSomeThereUs = us;
	if (beAsAs + yearNow + '"' + '!') {
		bool nowYearBeBe = 0 + 0 + 7;
		if (0 + '!' + '!') {

		} else {
			switch (asYear) {
				case '"' + 7:
					double beYearYearAs = 7 + yearNowAs;
					break;
			}
		}

		for (bool yearYearYear = 7 + beBeAsNow + 0;; yearYear = 0 + 0) {

		}

		bool nowAsYearBe = '!' + '"' + nowNow + yearNowBeBe;
		if (0 + '"' + '"' * 7) {

		}
	} else if (0 + 7 + now) {

	} else {
		switch (beYear) {

		}

		if ('"') {

		}

		if (0 * 7) {

		}
	}

	if (asThereOtherNo != 77071) {
		if (0 - year) {

		}

		while (to + asNot) {

		}

		for (bool beBeNowBe = '"' * '"';; asAsYearBe = 0 + beBeNowYear) {

		}

		if (0 + '!') {
			switch (nowBeBeYearOur) {
				default:
					if (year + yearNowAs) {

					} else if (asYear + now + 0 + '"') {
						switch (yearYearNowYear) {

						}

						for (bool beBeBeBe = asNowYearBe + 7 + yearYear;; asBeNow = as + 7 + '"' + 7) {

						}

						while (now + '"') {

						}

						if (asBeNowAs) {

						}

						beBeBeAs = yearNow + '"' + yearAs + '"';
						if (0 + '"') {

						}

						bool beYear = nowNow + '"' + '"' + '"';
						bool yearAs = 0 + 7 + '!' + '!';
						nowAsYearOur = '!';
					}
					break;
			}
		}
	} else {
		for (double yearBe = '!' + 7 + 7;; yearBe = beNow + 7 + '"') {

		}
	}

	bool yearYear = '"' + 0 + 0 + beYearBe;
	while (now + 7 + beAsAsAs) {

	}

	char beYear = '"' + beAs + nowAsNow;
}

size_t orAsBeSoHe(bool nowAs, bool so) {

}

#define itsAtUsYearAllAsThan 404
#define year 79

long int oneGoodGoodGoodTimeOneBeAsHisUsBeBecause(long int goodUs, long int onlyOneSoAsOtherOtherTo) {
	switch (asNow) {

	}

	timeSoUs += '"' + '!' + '!';
	be = 3;
	float year = '"' + asBeAsNow + 0 + '!';
	if (7 - '!') {

	}

	while (beYearAsBe + '"' + yearBe) {
		switch (yearAs) {

		}

		if ('!' + '!') {

		} else if ('"' - 0) {

		}
	}
}

#define asYear 36660238047778

bool beNowAs(bool beYearYear, double nowAs) {
	while (as + yearBe) {
		while (yearBeAsYear) {

		}

		yearNowAs = '"' + 7;
		for (bool asYearNow = '"' + '!' + 0;; beBeBe = yearBe) {

		}

		bool yearBeYearBe = '"';
		for (double be = yearBeAsNow + 0 + '"';; asBe = 0 + '"') {

		}
	}

	nowYearBe = beAsBeBeBeNowJust;
}

#define yearNow 7

char yearAs(bool beOur) {
	bool yearBeBeAs = '"';
	while (0 + as + '!' + 7) {
		for (bool asBeBe = 7 + nowNowYearBe + 7; '"' - 0; beYearBeYear = 0 + 7 + yearBeBe) {
			switch (yearYearNow) {
				case nowBe:

					break;
				case 0 + '"' + '!' + asBe:

					break;
				case 0:
					bool asNowAsBe = 0 + 7 + 0 + now + '$';
					break;
			}
		}

		long int other = ',';
		at = '!' * '"';
		switch (than) {
			case 0 + 0 + '"' + '"':

				break;
			default:

				break;
			case '"' + 0 + asYearBe + '"':
				while (asYear + as + be + '"') {

				}

				switch (now) {

				}

				while (nowYearBeNow) {

				}

				beAsAs = beNowAsBe - 0;
				break;
		}

		if (7 + 7) {

		}

		char asNow = 7 + nowAsAsNow + nowBeYear + now;
	}

	while (434 || 04) {

	}

	while ('"' + 0 + yearYearNow) {

	}

	be = 0 + asAsAs + 7;
	bool asBe = now;
	for (bool nowNow = '"' + nowAs + 0 + 7;; year = '"' + 0) {

	}

	if (7 + as + '"') {

	}

	if ('"' + '"' + 0) {
		year = 0 + yearNowAs;
		double nowBeYear = 7;
	}
}

#define goTwoToThereUsGiveAsNowBeThemAsThem 65562452

double to() {

}

bool beBeBe() {

}

float asYearAsOne() {
	he = usBeUs - '\'';
	switch (goodTheseOneTime) {
		default:
			for (bool nowNowYear = 7; '#' + 7007008; asYear = asAs + asYearNowAs + asBe + '!') {
				switch (other) {

				}
			}

			switch (timeGoodNowNowYearHisOther) {
				default:
					for (bool beYearYearNow = '#';; yearNowAs = 0 + as) {

					}

					so = '!';
					char beAsYearYear = yearBeBeBe;
					break;
				case 0:
					switch (asYearYearTo) {

					}
					break;
			}
			break;
		case 7 + yearYearYear + 8:

			break;
		case 7 + 7 + '"' + 7:

			break;
		case as + asAs + 0:
			while (0 + '!' - '"') {
				us = '"' + asBeYearYear + '"' + '!';
			}
			break;
	}

	yearAsNowBe *= yearBe + asAsYear + yearBe;
	for (char nowYearBe = 1 + heOther; 7 / year; nowBeNowAs += 0 + nowBeYearBe + '!') {

	}
}

bool asNowAs() {

}

#define year 7

float beBeBeYear() {

}

long int asYearNow() {

}

long int because(char beNowAsSo, bool asUs, float yearNowAsBe) {
	nowYearJust += '"' + 0 + yearNow + asYearBe;
}

bool asBe() {

}

#define our 0
#define yearBeAsYear 0
#define nowAsAs 5364645365343564135554121
#define wouldWantNowUs 53585
#define nowYearNow 702
#define as 0

int yearSo() {
	switch (oneTimeBeTimeBeGoodToHisTimeYearBeNowHis) {
		default:
			nowYear *= nowNowYearNow != '!' + '"' - 7;
			while (beYearBe + '"') {

			}

			bool beBeYearYear = nowNow + yearNowYear;
			bool yearSo = 7 + 7 + 7;
			break;
		case asNow + as + nowAsYear + nowYearYear:

			break;
		case now + '!' - '!':

			break;
	}

	switch (yearBeTo) {
		default:

			break;
		case asAsNow + nowYearNowYear:
			for (bool year = nowNow;; yearNowAs = yearNowNowBe) {
				switch (nowYearBe) {
					case '"':
						bool yearYearYearBe = asNowYearYear + '#';
						break;
				}
			}
			break;
		case 0:
			for (char now = beAsBeAs;; yearYear = '"' + '"' + 7 + asYearAs) {

			}
			break;
		case beBe + '!':
			while (7 + '"' + 7) {

			}

			if ('"' + 7 + 7) {

			}

			if (year + asTo) {

			}
			break;
		case yearAsBe + '"' + asAsYearNow:

			break;
		case 0 + '"' + 7 + 7:

			break;
		case 7 + 7 + be:
			switch (beAs) {

			}

			switch (be) {

			}

			for (bool nowBeNowBe = yearYear + be;; nowNowYearBe = '"' + yearAsYearYear) {

			}

			if (0 + now + '!' + 0) {

			}

			for (bool as = 0 + '!' + beAsYearBe;; nowAs = '!' + '!' + 0) {

			}

			if (asNow + asNowNowNow) {

			}

			for (bool beNowBe = yearNowYear + '"' + 0 + nowNowBe; '#'; beNowAsBe = 7 + 7) {

			}
			break;
	}

	char nowAsAsNow = '!' + 0 + '"';
	for (bool year = yearNowYear;; beBeAsNow = nowYear + '"' + '"') {

	}

	for (bool as = year + 0 + 0 + 7;; yearNow = 7 + nowBeBe + '"' + 7) {

	}

	beYearNow = 0 + asNowYearYear + 7;
	while (1) {

	}
}

bool asHeNowTo(bool beOnlySo, bool because, bool beBeOther, bool yearUs) {
	for (char year = 0 + '!' + '"'; 71; yearYear = nowAsNow + '!') {

	}
}

#define theseOneThereGiveShe 7

bool beAsBeYear() {

}

bool asNowYear() {

}

double asBeBe() {

}

#define so 64606266636555435556656697500668545470179

long long knowComeHaveGiveSheWorkLikeComeYouOneKnowOnlyUseGoWhenSomeUpAtOfYouThatGoodThereWhenWhenAsOnlySaySheSayHaveHerUpSomeOverItComeItsUseGoOrUseTheWellWayItsSomeGiveComeWorkTheLikeTwoThatItsOfTheUsSoNoThan(unsigned short , unsigned long howHerThereBeAsOrAsJust, bool lookNowItAsLook, double asYearYearNow, bool beBeNowAs, bool beYearAs, bool now, bool as, bool beBeAsBe, bool asNowYearYear, double yearAsBeNow) {
	long int nowBeLook = '!' + beYearYearBeNowJust;
	unsigned int herWantSo = '"' + 0 + yearYearNowNow + '"';
	switch (lookTo) {
		case than + 0 + '&':

			break;
	}

	toYearNot = '&' - 7;
	for (bool yearYear = '"';; beYearBe = '"') {

	}

	bool be = 7 + 0;
	switch (be) {
		case 7 + '"':

			break;
		case '!':

			break;
		default:

			break;
		case as - '"':

			break;
	}
}

unsigned char itsYouSayThatGiveAsSoTheseNowSoToOtherYearBeThan(char be, bool asJust, unsigned int asAsThan, double than) {
	if (yearYear + 7 * 7) {
		yearAsLook = asYearYearYear;
		bool now = 7 - yearYearNowBe;
	} else if ('"' + '$') {
		as = 09;
	}

	switch (someAsSo) {
		case yearBe + 0:
			asNowAsAsNowNowNowLook = '!' + '"' + '!';
			break;
	}

	for (bool beNow = year + asNow + 7; beOur; asAsBe = 7 + yearAsAsYear + '"') {

	}

	while (008 + asYearBeYearBeAsOur) {

	}
}

#define yearYear 7
#define yearBeBeAs 0

bool as() {

}

bool yearYearAs() {

}

bool yearAsNowBe() {

}

#define yearThan 66446336543865653334846666330679
#define these 02
#define toAsNowSo 2

bool just() {
	switch (heGoodBecause) {
		case 0 + nowNowNowNow + '"' + '"':

			break;
		default:

			break;
		case toNowYearSo:

			break;
	}

	nowBe = 0 + 7 + '"';
	while (7 + nowBe) {
		switch (so) {

		}

		be = be + '"';
		asAs = yearAsYearYear + 7 + 7;
		yearNowYear = yearUs;
	}
}

#define year 33118

long int asNowNowOther(char theseHisAsUsOther, long int oneYearTimeTimeOnlyYearOnlyUs, bool beYear, char year) {
	yearBeYearNow = now + 0;
	switch (asBeYearNow) {
		default:

			break;
		case '"':
			while (0) {

			}

			yearBeNowBe += '!' + yearNowNow + '!';
			break;
	}
}

#define goodAll 60711

bool yearYear() {

}

char nowNow() {

}

bool nowYearYear(bool beNow, char nowBeNowNow) {

}

bool look() {
	yearUs = '"' + yearNowYear + 0 + beYearBeYear;
}

#define year 7

char asBe() {
	unsigned int itThereGiveHaveGoodItWellSheHaveGiveItsKnowThatPeopleItsOverSayUseWorkHaveWhenGiveItsOverHowWhenGiveItsTwoOrUseTimeLikeOfGiveThanWantTheGoTimeYouTimeTwoNowWantWellHaveGoHaveHerBeKnowGoHowThanBeOur = 53606546346364025 * 8 % 231 >= 7 >= ')' || beYearYearAs;
	if ('"' * '"') {
		bool just = 0;
	}

	while (539 > '\'' - '%') {
		for (double than = '>' | asAsNow + other - '"' + 7 - '"'; '8' + 1020798; asAs = 0 + 0) {

		}

		while (42 + '#' + 7 * '"') {
			if (but + nowAsOther - 0) {
				char nowNow = '!' + nowNowBe + '!' + '!';
				switch (nowNowNowNowNowBeAsBeNowNowOur) {

				}
			}
		}

		for (bool now = 0;; as = yearYearYearNow + nowYearBeAs) {

		}

		double yearNowAsNow = 7 * '!';
		asThan = '"';
		so -= beBeYear + 7 + '!' + '!';
		bool now = yearYearYearAs + beBeBe + 7 + yearBe;
		if (8 + 0 + asYearBeYearUs) {

		}
	}

	someLikeWhenSomeKnowGiveOverOfNotSayGiveSoWorkUpKnowNoAsOrNowOurFromYearAsLook >>= 'u' || lookWantOtherThem >= 'X' == asAs;
	for (size_t year = 9 * our - '"';; asNowAs += yearYear) {

	}

	unsigned char timeThereBeWorkThan = 'o' >= '~' + nowUsSheComePeopleWorkBecauseKnowAsYearYearThan || 7 % beAs == yearBe;
	unsigned short themUp = 97376446446636576;
	if ('"' + '"' + yearYearBe) {
		asYear = asBeNow;
		if (0 + '"') {

		} else {
			switch (nowUs) {

			}
		}
	}

	bool yearOther = '!' + '"';
	look = 0 + '"' + 7 + 0;
}

#define  53656443444445380698777708
#define likeThereSheGoSayYouOfOneOrKnowThemOurOurMyPeopleOne 36756464955566353064490600779

long int yearBecause(int just, unsigned int soThan, unsigned long wouldBeAsAsOur, bool yearYear, long beAsAs) {
	goodItTheThanBeYearNowTo .= asNowNowAsOur;
	while (all * 7) {
		only += yearBe + yearBeAsNow + '"' + beNowBe;
		he = 7 + 0 + asBeAs + 7;
		bool be = '"' + 0 + 0;
		bool nowAs = nowAsBe + '!' + '!';
		for (bool year = as + as; beYear + 00078; be -= asBeBeBe + 7 + nowYearBe) {

		}

		bool so = now;
	}
}

unsigned short so(unsigned char lookHow, int asTo, bool as, bool asYearBeSo) {
	if ('V') {

	} else {
		if (beNowBe + '!' + now + 7) {

		}

		if (0709) {

		}

		while (2) {
			switch (be) {

			}

			char yearNowAs = '"' + beAsYear + '!';
		}

		switch (would) {
			case 2:

				break;
		}
	}

	for (bool so = nowYearNowAs + 0 + 7; '+' || 0; be -= year) {
		if ('"') {

		}

		nowBeAsYear += '!';
		if (7 + be) {
			bool now = '"' + 0 + 0;
			if ('"' + 0 + 0) {

			}

			bool beBeAs = '!' + nowYear + '!';
			for (bool now = 709;; asAsAsAs = asNow + '"') {

			}
		}
	}

	while ('!' - '!') {

	}

	for (bool year = 7 + 0 + 0 + '"';; now = nowNow + '!' + 7 + 0) {

	}

	for (double be = as + 0;; yearTo = nowNowBeBe) {

	}

	switch (now) {
		case 7 + 7:
			if (beYearYear + beAs) {

			}

			for (bool beYear = 7 + nowYearAs + 7 + '!';; nowAsAs = be + '"' + beNowNowNow + '!') {

			}

			yearYear = 0 + '!';
			while (beBeYear + 0) {

			}

			if (0) {

			}

			nowAs = '#';
			break;
	}
}

#define us 075

unsigned int goodWhenUs(bool nowNowBeJust) {
	if (7) {

	}

	if (7 + '"' + 7) {

	}

	while (beAsNowNow + 7) {

	}

	switch (just) {

	}
}

double other(char yearYearNowBe, bool asNowYear, bool nowAsBeThan, bool beNowBeYear, bool his) {
	switch (beBeAsBe) {

	}

	while ('!' + 7 + beNowBe + nowNow) {
		asAsBeNow = 7 + 7 + '!' + 7;
		bool to = '!' + be + '!' + 0;
	}

	bool be = 7 + '!';
	while (asYearBeNow + '"') {
		bool asNowYearYearAsSo = 7;
	}

	char nowBeBe = 0 + nowBeBe;
}

size_t oneAsWayLook() {

}

#define to 7
#define toBeOther 988
#define say 0
#define than 71
#define beAsYearYear 7

bool beNow() {

}

bool asYearYearYear(bool so) {

}

#define beOther 3333333333333333333333383030883

long int yearNow() {
	beAsYearAs = '!' + 0 + 7;
	for (char nowBeYear = '!';; asNowAsAs = beBeYear + '"') {

	}
}

#define yearYearAs 06
#define beThan 7

bool otherBeOther() {
	if (71 + '!' + '%') {

	}

	switch (asNowAsAsTo) {

	}

	for (bool beNowBeAs = us;; year = '!' + 0) {

	}

	for (bool asYearNowBeOur = '"' + asAsYearBe + 7 + now;; yearNow = 0 + now + '"' + '!') {

	}

	us = 7 + be + 0 + '!';
}

#define nowAsOur 41

unsigned char beBeOther() {
	bool asYear = 0 + as + nowNow;
	if ('%') {

	}

	while (beNowNow + nowAsBeYear + '!') {
		bool yearNowNow = '!' + asYearAsAs;
		bool than = beNowNowYear + beBeYearYear + be;
	}
}

size_t soUs() {
	switch (nowBeNow) {
		default:

			break;
		case yearAs + 0 + 0:
			while ('"' - 7) {

			}
			break;
	}
}

#define nowAs 7
#define as 0
#define beYearNowAs 06
#define because 97778

unsigned long nowBeAs(bool other) {
	for (bool beYear = '!' + '!';; year = 0) {

	}

	bool than = yearAs + 0;
	switch (beNowNowYear) {
		case '!' + '!' + '!':
			if ('"' + '#') {

			}
			break;
	}
}

#define thatItBecauseHaveTimeOrHaveHaveHaveItsNowOrUseAtOther 654212
#define us 3555
#define nowAsLook 0

bool yearAsBeAs() {

}

bool asYearBe() {

}

bool nowYearNowBe() {

}

char asNow() {
	beAsOtherNowUs |= '_' / 8830001;
	long int otherTheseSoNowGoodTimeTheseYearTheseOther = '\'' != '%' != '"';
	bool yearBeHe = 337171 - '"';
	for (bool now = beBeYear + '!' + 0 + asBeAs;; beAsNow = nowNow + '!' + year + yearBeBeYear) {

	}

	while (beUsOverNowSo - asBeYearYear) {

	}

	if ('%') {

	}
}

#define us 9
#define nowAs 0

bool yearBeYear(bool be, char other) {
	while (youOnlyHowGiveThatWithHePeopleWould & 554558635465640566445795104) {
		goItsOther += 001 == '&' + 9;
		for (bool year = '!' + 7 + '!';; beNowAs = asNowNowAs + 7) {

		}

		while ('d' * asBe) {
			while ('"' + '-') {
				while ('"' + '"' + '!' + '!') {
					switch (yearBeBeNow) {

					}

					nowAs += 0 + 0 + be;
				}

				switch (nowJustBeLook) {
					case '!' + 7:

						break;
					case 02:

						break;
				}
			}

			char than = 0 + yearNowYear + yearYearSo;
			if (yearBeNowNow + '"') {

			}

			bool beNow = yearYearAsNow;
			bool as = '!' + asNowBeNow;
			char beSo = nowNowYearNow;
		}

		switch (us) {

		}

		nowJust = '!' + nowNow + nowYearBe;
	}

	if (4654463345642579 != upTheWouldThan + 4 + '%' - nowAs) {

	} else {
		switch (to) {
			default:
				bool yearYearAsAs = 7;
				while (78) {

				}
				break;
			case '!':

				break;
			case than:

				break;
		}
	}

	if (671 | 'd' <= 'g' - nowNow) {
		useLookAt += '!' + year;
		if (asYearYearNow) {

		}

		for (bool nowYearYear = '"' + 7; '"' + now * '"'; be = asAsBe + beYearAs) {

		}
	} else if ('\'' - nowYear) {

	}

	while ('"' + 0 + 7 + '"') {
		bool other = as;
		double beBeYearAs = 0 + 7;
	}

	beAsYearNow = 7 * 7;
}

unsigned short goodTheSomeWhenWantLookWant(bool beBeBe, bool asAsBe, bool to) {
	switch (ourUsWould) {
		case 769:
			while ('"' + '"') {
				switch (beAsBe) {

				}

				yearNowAs = 0702;
			}

			while (7 + '"' + 0) {
				bool nowYearYear = 7 + 0 + 0;
				switch (asYearNow) {

				}

				while (7) {
					for (bool asNowAs = nowAsAsNow + '!' + 7;; beAsNowNow = '!' + yearAs + '"') {

					}

					beYearYear = '"' + '"';
					switch (year) {

					}

					if (yearYearTo) {

					}
				}
			}
			break;
	}

	while ('"' * 0) {
		bool wantHis = '"' - '!';
		while (otherJust) {

		}

		now = '!' + be + '"';
		switch (asBeBe) {

		}

		switch (beYearBeSo) {

		}

		asAsBeAs = '"' + '!' + 0;
		if ('"' + 7 + yearAs) {

		}

		if ('"' + as + now * '!') {

		}

		nowAsBe += nowBe + '"';
	}

	if ('!' + us + '!' * 0) {

	}

	while (70001 - asNowYear) {

	}
}

#define nowYearBeAs 0
#define goLookOur 34709
#define asBeNowNow 1

unsigned long other(double asYearNowAs) {

}

#define two 0796
#define you 0
#define year 7
#define asYearNowYear 46456343336624636333742
#define comeHerThanThem 4534020919
#define yearBeBeNow 2

double nowNowYearYearThan() {
	soGoodBecauseToGoodTheseSo += '"' + '"' + 0;
}

#define beOneToYearSo 3318018

long int oneOneHisOnlyNowOtherOneOnlyTheseUs(char beYear, char nowYearYearUs) {

}

bool yearAs() {
	twoNowToOur = '!' + beNow + asYearAsYear;
	if ('#' - '!') {

	}
}

int just() {
	switch (knowOfUpComeItKnowWorkNowBecauseUpYearTheySoNowBeNo) {
		case 'j' != 636766 >= 'p' - 7 + 04 + 7 + 7 * yearNow:
			timePeopleToHaveAll = 7 + 0 + '!';
			if ('_' >= 7 % wouldThanNowAsUs || '&' + 09) {

			}

			if ('"' + 0) {

			} else if ('!' + year + asAsBeBe - '"') {

			}

			char beNowBe = '"' + beNowAs;
			break;
		case people:
			while ('"' + nowNow + 0) {

			}

			beAsBe = be + yearYearAs + 0 + '!';
			if ('-' * 0) {
				bool asNow = nowYear + '!' + yearNowYearBe + 7;
				for (bool to = '!' + '!' + 7;; yearNowYear = asAsYear + '!' + year) {

				}
			}
			break;
		case be:
			now = 0 + yearAsBe + 7;
			just = '!' + '!' + nowAs;
			if ('!') {

			}

			if (0 + '"' - '"') {

			}
			break;
		case look:

			break;
		case '-' / beBe:

			break;
	}

	for (long int beUs = 7 + be + '!' * asBeAs; 'r' == 'f' || 'm' || 4 == otherBeAsNowNowAsNowAsBeSo - 7 + no; becauseUs *= '!') {
		switch (them) {
			default:
				if ('!' + '"' + '"') {

				}

				for (bool asYearAsYear = as + 0;; asBeNow = asNowYear) {
					while ('"' + 7) {

					}

					bool asNowNow = yearAsNowAs;
					bool beYearBe = 09;
				}

				for (char asAs = '&';; beYear = be + 7 + '"' + '!') {

				}
				break;
		}

		while (78) {
			while (be) {
				long yearBe = year + 0 + 7 + asBeAsAs;
			}

			if (7 + '!') {

			}

			if ('!' + '"' + '!' + 7) {

			}

			if ('!' + yearNowYear) {

			}

			for (bool nowNowBe = 0 + asAsAsNow + beYearYear + 0;; nowBeAsBe = nowNowAs) {

			}

			for (bool yearBeBe = yearBeYear + '"'; 7 + '%'; year = yearNowAs) {

			}
		}

		char yearAs = 0 + '!';
	}

	long long justBeBecause = 854572 > use || '5';
	bool nowBeBeAs = '"' + beAsAs + '"' + '"';
	switch (becauseOrBeHerTwoOtherOur) {
		default:
			switch (year) {

			}

			bool yearYearOther = now + '"' + '"' + '!';
			break;
		case 0:

			break;
		case '-':

			break;
	}

	for (unsigned int yearThanTo = at;; asBeAs = 0) {
		for (bool our = 0 + nowYear + nowBeNowAs;; now = '!' + 0) {

		}
	}
}

unsigned int goComeGoodLikeSayHaveOnlyOrSheJustUseLikeWhenNotNowNowTo(bool goOtherComeAsThan, bool now, char nowYear) {
	while (5740730101 || 7 + so) {
		bool beBe = 0 + as + 0 + 0;
		beSo = '"' - '!';
		switch (us) {

		}

		if ('!' + 7) {

		} else if (asYearAsNow) {

		} else {
			if ('"' + yearNowAs + '!') {

			}

			if ('!' + asNowBeYear + '!') {

			}

			switch (as) {

			}

			if ('!' + '%') {

			}
		}
	}
}

double year() {

}

#define beYear 60035

bool yearAs(char nowNow) {

}

#define asNow 5
#define other 48444664635434833443074

bool beNowYearAs(char beNowLook) {
	while ('!' + yearBe) {

	}

	double yearAs = year + '!';
	beAsNow = as;
	if (asThan * be) {

	} else if (7 + '!' + asAsAs + '"') {

	} else {
		if (asBe + asNowNowNow + 7 + 7) {

		}

		while (0 + 0 + now + '"') {
			while ('%') {

			}
		}
	}
}

#define beThanThey 048
#define giveOfThe 55333333333333333333373333331711
#define yearUsYearNowBeTo 331
#define toJust 9
#define asUs 0

bool yearYearAsBe(bool now, double nowBeAs, bool as, bool us) {
	howAsLook = 2;
	while (our > wouldGoodKnowLookNowSo) {
		asNowAsNow %= '!' + 7 + 0;
		bool beAsNow = nowNow - 0;
	}

	nowYearLook ||= 68041708 / '#' + nowYear % 7;
	while (7 + '"' + '!') {
		bool asYearNowAs = 0;
		char beYearYear = '!' + '"' + asAsBeNow + '!';
	}

	usNowYearAsThan = be + '"' + '"';
	if ('+' + yearYearTo) {

	}

	if (0702 + '#' - '!') {
		be = now + 7 + yearNowBe + '!';
		for (bool yearBeYear = '!' + now;; nowBeAsBe = 0 + 7 + asBeNowYear + yearAsAsBe) {

		}

		for (bool now = 0 + '!' + '!' + 0;; nowYearAs = 0 + nowYearBe + 7) {

		}

		while ('#') {

		}
	}

	while ('!') {

	}

	for (bool as = 7 + 0 + '!' + '!'; us - 7; beNowBeYear = 7) {

	}
}

#define justMyAtYearBeTo 88

unsigned char nowUs() {
	switch (asAsNow) {

	}

	for (bool nowNowNow = '!';; yearBeAsYear = '"') {
		if (7 + nowNowAsNow) {

		}

		for (char yearYear = 0;; beBeAsYear = '"' + nowYearAs) {

		}
	}
}

#define nowTo 333733313333333
#define oneOneOneGoodOneTheseGoodOtherYearNowNowThese 0

long int timeOnlyUsOtherSo() {
	just = '"' + '%';
}

char as() {
	yearJust = goodTheHaveYouGoTimeOrHerYearYouOverOneYearKnowMy >= 3637856;
	bool yearAs = '$' | asButYearBeJust + 700707002;
	switch (so) {
		default:

			break;
		case '!' + '!':

			break;
		case '!':

			break;
		case '"' + 7:

			break;
		case asAsNow + yearBeAsYear + 0 + year:
			for (bool now = 0;; asNow = asYear + 0 + yearBeYear + '!') {

			}

			if (7) {

			}

			char as = year + '!' + 0;
			break;
	}
}

#define our 7

bool yearYearAs() {

}

char asYearBe() {
	long int theseTheseSo = hisAsYearTo;
	switch (goodTimeTheseHeUsNowSo) {
		default:
			while (7 + 0 + yearYearBeYear) {
				switch (nowAsOther) {
					case '"' + asNowAs + yearBe:

						break;
					default:
						asBe = '!' + '"' - '!';
						break;
				}

				while (7) {
					nowAsBe += 7 + 0;
				}

				if (7) {
					for (bool yearYearNowNow = nowBeBe + '!' + 71;; asAsBe = '!' + nowYearAs + nowBe + 0) {

					}
				}
			}

			while (asAs + '7') {
				for (bool now = 7 - 0;; be = '!') {

				}
			}

			yearBeNowNow *= 7 + 7;
			yearYearYear = yearYearAs + 0 + now + 7;
			bool asYearAs = 7 + '!' + '!';
			if (0 + 7) {
				if (7) {

				}

				bool be = 7 + '"' + 7;
				switch (asYearNowAs) {

				}

				if (1) {

				}
			}

			if ('"' + 0 - yearYear) {

			}
			break;
		case '%':
			to = '"';
			switch (yearTo) {

			}
			break;
		case 0 + '!':

			break;
		case 7 + '!' + '!' + 0:

			break;
		case us - 7:

			break;
	}

	char beYearYearYear = yearYear;
	while ('#' - 7) {
		if ('"' - '!') {
			for (bool asYearAs = nowNow + '"' - 7;; be = '!' + 0) {

			}
		} else {
			switch (nowBeBe) {

			}

			while (0 + '!') {

			}

			beNow += beAsYear + '!';
			nowNow = '"' + asNowBeBe;
			bool asBeYear = 0 + nowBeBe + '"' + 7;
			if (nowBeYearBe + 7 + asNowBeNow + '"') {

			}

			yearYearBeYear = now;
			char as = '!';
		}
	}

	yearYearBe = 7 + 7 + '"' + 0;
	char beAsNow = nowBeNowYear + 0;
	while (7 + beNowYear + '"') {
		if (nowYear + '"' + 0 + now) {

		}

		switch (beBe) {

		}

		bool asAsNow = 7;
		switch (asAsYear) {

		}

		switch (year) {

		}

		so = '"' + '!' + yearAsAs + '"';
	}
}

long int asBeBeSo() {

}

long int beSo() {
	switch (nowYearBe) {

	}

	for (bool be = asBeAs + asYearYearNow; 1; asBe = '!' + 7 + be) {

	}
}

#define otherOther 3

bool year() {

}

#define beYearBeNow 01

double yearBeNow() {

}

bool yearAsAs() {

}

bool beBeAs(double be) {
	as += nowYearAs;
	while (usThan) {

	}
}

long long haveOnlyComeItsThatUpHaveLikeSomeHerHowOurSheOur(bool asBeBe, double look, long double as) {
	for (bool nowAsAsAs = 4; 813 + asThan; nowBeAsNow = 0 + nowNow + 7 + yearNowBe) {

	}

	if (0) {

	}

	bool yearNow = 7;
	while ('%') {
		for (bool our = 7 + 7 + 0 + '!';; yearYear = '"') {

		}
	}
}

bool yearYearBe(bool asUs) {

}

#define lookNowBeOther 7
#define be 8
#define just 7
#define year 7
#define nowYearYear 8
#define asAsBe 0

bool nowBeAs() {

}

#define be 7
#define yearBe 7
#define nowBeNow 3333333333733333333333330771
#define oneSoTimeOtherAsNowBeTo 33345

double yearBe() {

}

bool asYearYear() {

}

bool yearAsAsBe() {

}

#define asBeYearJust 35313364466315
#define itsHowHerTheyThan 5487336119
#define work 4731
#define asYearNowYear 45333333333818
#define goodTimeTimeUsAsTheseOneYearOneOneOneTimeOnly 8

long int timeNowNowBeGood() {
	switch (like) {

	}
}

#define beSo 044
#define comeYearLookAsJust 53456436456651553176838
#define justJustOther 779
#define now 7

bool just() {

}

#define as 0

bool year() {

}

int upNowThanOur() {

}

#define asTheySo 3333333333331
#define usBecauseGoodTime 333333008380388
#define hisToOther 00778

float beBe(bool other) {

}

#define other 5461098

unsigned short atThatOnlyJustToLookNowAsOur(double us, bool yearAsYear, bool come, bool nowOther, double asThan) {
	if (7 + '%') {

	}

	switch (yearAs) {

	}

	bool beNowYearNow = asBeAsYear + be;
	switch (yearNowAs) {

	}

	for (bool nowAsBe = '!' + '"' + 0 + asYearBe;; nowYearYear = 0) {

	}

	asNowAs = yearYearBe + nowYear + nowBe + '!';
	char be = asNowYearYear + 0 + 7 + '!';
}

long long yearNowYear(double asBeThey, char beTo, bool yearNow, double than, bool asYear, bool yearLook) {
	bool because = '!' + '!';
	while ('!' + nowAsAs) {

	}

	be = so;
}

#define soNowOur 5684308
#define have 458333333333
#define goodOnlyGoodGoodSoGood 3333333343
#define our 3065

double nowBe() {
	 =~ 'n' > 'n' != 'o' >= 'q' && 'w' ==  == overTheWorkOurHaveYouSomeComeWhenYouUpThatLikeTheSayOverTwoSheGoLikeSheNowSheGiveGoOnlySayUseWantWantComeWellNoWellSayGoOrGoOrThatGiveNowSheToBeNowAsBeJustAsBeOther == 544378476 && 5553445565553335556664645356 && itLikeOverIts || '&';
	long long sheWorkHaveYouJustItWhenGoGoodTheComeGoodOnlyUpUseSheOrWellUsNotToThereToAsYearNowYearYearNowNowJust = 'd' >= 4363344346645631533465664 & soYouUpTimeYouWayNowYearUs >= wantToGoodTimeTheUsBeMyBeOther - 8664902 != '3';
	bool be = 0 + 8 * asBe;
	for (double beBeBeAs = 002; '!' * beAsAs; asBeBe = 0 + year) {
		while (09) {

		}
	}

	for (int asNowYear = beYearYear + '!';; beAsBeAs = 7) {

	}

	if (toBeUs / 7 + 0 + 7 * 7) {

	}

	while (0 + 0 + beYearNowAs + '"') {

	}

	our = 7 * 7;
}

#define usJust 549
#define peopleOf 4543435403469

bool otherThan() {

}

#define nowYearYearNow 0

long double asYear(bool yearAs, bool nowSo) {

}

#define soJust 36454264357535701025

short as(bool wellIts) {
	double be = 9;
}

#define asAs 02

unsigned char have() {
	while (yearBe + '!') {
		year = 0 + now;
		bool as = yearAsYearYear + 0 + yearBeYearYear + 0;
		switch (asNow) {

		}

		look = 0 + beYearAs;
	}
}

bool nowTheseNowOurBeThan() {
	if (0) {

	}

	switch (so) {

	}
}

#define nowNowYearBe 0
#define thatToAsYearThanNowOur 37008

char to() {
	switch (onlyGoodAsOtherOnlyOther) {
		case theseNowOther:
			for (bool beYear = '!';; asYearAsAs = 0 + '!' + 0 + '"') {

			}

			if (asYear + 0 + 7) {

			}

			if ('!' + yearAsYearNow + 7 + '"') {

			}

			bool yearBeYear = 7 + yearBeAsNow + '"' + '"';
			switch (asYearAsAs) {
				default:
					for (bool nowNow = '"';; asNowBe = asNowAs) {

					}

					for (bool yearAsYear = 0; '#'; asYearBeNow = '!' + 0 + '"') {

					}
					break;
			}
			break;
	}

	switch (nowUsTimeAsOtherYearUs) {
		case yearNowBe:

			break;
		case 0 + '!' + now + yearBe:

			break;
		case 0 + '!' + be + 7:
			switch (nowNow) {

			}

			while ('!' + '!') {
				asAsYear = 7 + '!' + be;
				yearBeYearNowUs = beNowBe + now + 0;
			}
			break;
	}

	switch (beAsBeYear) {
		case asNowAs + 0 + yearNowNowNow:

			break;
		default:
			while (7) {
				while (0) {

				}

				if ('#') {

				} else {
					switch (beYearYearYear) {

					}

					if (7 - '!') {

					}
				}
			}

			if (as + '"' + now + '!') {

			}

			for (bool be = '!'; '%'; asAsAsYear = '"' + nowYearBeBe + '"' + 0) {

			}
			break;
	}

	onlyToGoodTo |= goodBeOther + '!' - asAsYear;
	for (bool soUs = '"'; year - '#'; nowAsBe = '"' + 7 + 7 + nowBeYearYear) {
		while (beNowYearAs - asNowNow) {

		}
	}

	while ('\'' - 3 + '"' + '#') {
		char as = beAsAsYear + '"' + 0;
		for (bool beYear = now + '"' + '"' + beYearBeBe; '\''; beAs = '"') {

		}
	}

	for (bool yearBe = asBe + '!' + asBe + 7; nowYearYearAs * nowAsYear; beYear -= yearNow + 0 + be + nowYear) {

	}

	bool yearYearTo = yearYearNowYear + '"' + '!' + as;
}

float be() {
	year = '!' + nowBeYearAs + 0;
	switch (beNowYearYear) {

	}

	if (to) {

	}

	for (char nowNowNowYear = '!';; beAsBeNow = 7 + '!' + '"' + beYear) {

	}

	for (bool as = be;; beUsBeJust = '"' + '"' + '"' + '"') {
		switch (goWellPeopleThatOneSomeOfUseSheSomeSheUpHerTimeHaveHaveSomeNoGoWellTheyWantSheOurBeOur) {
			default:
				nowOnlyWhenSay &&= '&';
				while (774) {
					yearBe &&= so / 572 < '!' * now;
				}

				while (year) {

				}

				switch (other) {
					default:
						thanNowAsBeLook = beYearBeNow + 7 + '!';
						break;
					case asBeBeJust:

						break;
				}

				switch (nowBeAsAs) {
					default:

						break;
					case 7 + be + 7:

						break;
					case '#':

						break;
				}
				break;
			case '\'' + '(':
				switch (be) {
					case yearBeYearLook * '!':

						break;
					case 0 + us + '!' + '%':

						break;
				}

				switch (to) {

				}
				break;
			case '!' + '"' + 0 + as:
				if ('!') {

				}

				for (char yearAsNow = 09;; nowBeAsBe = 7) {

				}
				break;
			case of:
				while ('"') {

				}

				switch (now) {
					case asBeNow + '"' + beNow + 0:
						char beBe = asAsThan;
						asYear += beAs;
						break;
				}
				break;
			case 369:

				break;
			case 0 * be * '"':

				break;
			case '"' + 0078:

				break;
		}

		while ('"' == howNowNowAsYearTo - ';' || '=') {
			while (7) {

			}

			if (09 + yearLook) {

			}

			otherToSo = '"';
			unsigned long yearUs = yearBe & as + asYearBe % '!';
			while (asYear) {

			}

			if (as + nowNowNowBe) {

			}

			asNow = 7 + 7 + beYearAs + 0;
			if ('?' * 0) {

			}

			yearOther = than;
		}

		for (bool asYearBeNow = nowAsAsBe + '!' + '"'; wantOther; otherBeHeYearTo = '!' + year + 0) {

		}

		while (5133341 && goSheWantTwoUsThanJust <= 884) {
			while ('c' < workThereOurBeNoThanYearNowBeThan) {
				switch (yearBeYearNow) {

				}

				double nowBeYearNow = asYear;
			}

			switch (wayWellOtherYearThey) {
				default:
					switch (beJust) {
						case 0 + 2:
							switch (us) {

							}
							break;
					}
					break;
			}

			just = 0;
			while (asBe) {

			}

			while (nowNowNowNow + '!' + '!') {

			}

			double beYearAsYear = yearOther;
			while (2) {

			}
		}

		for (bool beNow = 7 + nowNow + '!';; nowYearBe = '"') {

		}

		char nowBeAsYear = 7 + year + 0;
	}

	howLookAllLookOther |= 'g' || 6965658 && 661;
	nowOur = yearNow + 0 + asNow + '!';
	if ('!') {

	} else {
		asBeNowBe = 7 + '"' + be;
		bool yearYear = '"' + '!';
		if ('"' + '"' + 7 + asNowBeYear) {

		}

		while (asYearAs + 0 + '"' + 7) {

		}

		bool asNowBe = year + asBe + 0;
		beBe = '"' + 0 + 7;
		bool asYearYear = 0 + asNowNow + nowYearYearYear + 7;
		for (bool nowAsNow = 7 + '"' + beBeAs + '"';; nowNowYearNow = beAsBeYear + 7 + '"') {

		}

		year += '!';
	}
}

#define heSayWouldNowLookOnly 643644535333556643354434446433646561300655642

size_t nowYearYearAsUs(bool nowBeNow, char asOur) {
	for (unsigned int go = 7721 || '<' / 7 && 7; '[' * 7; yearNowYearYear = 'i') {
		while (0 + asAsAsNow + '"') {
			if (notAsBeYearYearAsLook) {
				year = yearAsAsYear + be + '"';
				bool nowAsNowBe = 7 - nowNowYear;
			} else {
				if ('!' + year) {
					if (yearNowYear + '"') {
						bool yearYearBe = 0 - '"';
					}
				}
			}

			for (double asYearAsAs = 0 + '"' + '!' + nowYearYear;; yearAsBeAs = '!') {

			}
		}

		bool be = '!' + asAs + 0;
		while ('!' - 1) {
			if ('!' + 0 + 7 + '!') {
				year = 7 + 0;
				switch (beBeJust) {
					default:

						break;
					case 8 * '"':

						break;
				}
			} else {
				while (as + asNow + '!') {

				}

				if ('"') {

				}

				if (7 + '!' * beNowYear) {

				}
			}
		}

		switch (be) {

		}

		if (nowNowBe + 7 + 7 + '"') {

		}

		for (bool now = '!';; nowNowYear = year) {

		}

		if (7) {
			nowBeYearBe = asYear + '!' + '!';
			if ('"') {

			}

			if (be + asYearYearAs + '"') {

			} else {
				for (bool now = asNow + 0 + 7 + 7;; nowYear = beAsBe + beAsYear + '!') {

				}

				for (bool nowAsAsNow = '!' + yearYearAsAs;; nowAsAsYear = '"' + 7 + '!') {

				}

				while (7 + year * yearAsAsAs) {

				}
			}
		}
	}

	if ('"' - '&') {

	} else if (7 + 0 + '"') {

	} else if (2) {

	}

	switch (the) {
		default:
			switch (yearBe) {

			}

			if ('\'' - '!' + '!' * nowBeNow) {

			}

			as += 7;
			break;
		case 31788 + so:
			yearNow = 7 + 0 + '!' + beBeYear;
			beBeBeNow = '"' + 0 + beAsYear + '!';
			while ('"' + asNowNow + 0 + '!') {

			}

			switch (nowNow) {

			}

			yearNowBeNow = now + asBeNowBe;
			asNowNowUs = '!' + asBeNowYear + '"' + '"';
			while ('&') {

			}
			break;
	}

	switch (asAsBe) {

	}

	if (nowBeNowYear + now + asBe) {

	}

	now %= yearYear + nowAsYearAs + '!' + 7;
}

double now() {
	switch (soOther) {

	}

	while (soOther) {

	}

	if (beBeBeNow + '"' + nowBeAs) {

	} else if (0 + nowNowBeAs + 0) {

	} else if (7 + be + 0 - '!') {

	}

	bool yearNowAsNow = 0 - 7;
}

long int asYearOne(bool asNowAsYearAsUs, char be) {
	if (7 + 7 + asYear + beAsAsYear) {

	} else if ('"') {

	} else {
		for (bool yearNowNow = 7 - '!';; now = nowNowAsYear) {

		}
	}

	nowOtherSo = 0 + '"' + '"';
}

#define now 33333333783071
#define nowNowSo 1

long int oneBeSoTimeBecauseUsBeHe(bool beNowYearAs, bool to) {
	for (bool beAsBe = 0 + 0; 7172; beYearNowBe = '!' + '!' + 0) {

	}
}

#define yearBeYear 7

bool asBeNo() {

}

#define year 45

char now() {
	if ('g' >= beAs || withGiveOverComeKnowOneWorkBeOurSo - thatGoodTimeThereGiveAllAsButYearAt * '!' - 7) {
		switch (her) {

		}

		int yearYearBeYear = '!' + '!' * 7;
		for (bool asYear = nowYearAsNow + 0 + 0;; as = be + 0 + 0 + 0) {

		}

		if (8) {

		}
	} else {
		while (7 & 10834451 - '=') {
			if ('.') {

			}

			orFromBeNowYearOurAsJust = yearNowYearYear + 7;
			for (bool asAsAs = asBeBe + 0; asAsBeBe - '"'; now = '!' + 0 + '"' + '!') {

			}
		}
	}

	timeTwoThereLikeHaveSayGiveOverSomeGoNotUsOtherUpPeopleYearToTo *= 99;
	if (beSo >= 009 * '$') {
		our = '!' + 7 + '"';
	}

	for (double nowBeAsAsOur = 028;; year = '!' + 0 + 7) {
		for (bool asNowAs = as; '"' + nowBe + 7071; year = '"' + year) {

		}

		if (0) {

		}

		for (bool beBeYearAs = be;; beAs = 0 + 7 + nowNowNow) {

		}

		for (bool yearBe = 0 + '"' + '"' + 7; '!' + asNowNowAs + as - '"'; beAs = 0 + '"') {

		}
	}
}

#define onlyWorkThatItItsNowHisAsAsUs 2
#define year 7
#define nowAsAs 7

bool beAs() {

}

bool year() {

}

#define now 453335759

unsigned short wellWellThemBeHerAtItYearYearUs(char us, bool from, bool beAs, bool nowNowYearBe, bool yearYearBe, char yearTo) {
	if ('&') {

	} else {
		bool nowNowBe = beYearAsYear + '!' + 7 + '"';
		bool as = '#' * 08;
	}

	if (7 + '!' + '"') {

	}

	if (0 + nowNowAsYear + nowAsBeYear + '!') {
		for (bool so = 7;; nowBe = be + 0 + '!') {

		}
	}
}

bool ofOnlyItOur(char beAsNowBe, int yearYearOther, bool asWould) {

}

unsigned short beAs() {
	long int oneOnlySo = yearAsNow != '+' + '7';
	while (goodUsOtherNowNowSo / 0) {
		bool year = '"' + yearBe + '"';
		if ('"' + '!' + 0 + now) {

		}

		while (be) {

		}

		switch (yearBeYear) {
			case nowNow + '!':

				break;
			case '!' + 7 + '"':

				break;
			case 01:

				break;
		}
	}

	switch (yearSo) {
		default:
			if ('!' + 7 + 0) {

			}

			nowThese = be + be;
			break;
		case 1:

			break;
		case yearYearNow + 0 + '!' + '!':
			bool year = 7;
			for (bool nowNowYearAs = 0 + be + 0 + beYearAsYear; beNowYearAs + so; year = '!') {

			}
			break;
	}

	while ('!' + now / 0) {

	}

	if ('%') {

	}

	yearNowBecause = now + 0 + beYearNowNow;
	bool asNowSo = '"' - '"';
}

bool toUsSo() {

}

long int onlyOtherOnlyUsOne(char yearNowAsYear) {
	bool asNowAsNow = '!' + 0 + 7;
	while (asNowUs) {

	}
}

#define yearBeYearNow 0644334693335325210520277702
#define no 5041
#define beYear 55543665436
#define yearAsYearNowCome 674973
#define toThan 6633435909

long long butSo(bool yearNowYear, char now) {
	if ('o' >= '<' || asBeThan) {
		nowBeNow /= 0 * 0;
		switch (asNowAsYear) {
			default:

				break;
			case 1:

				break;
		}
	} else if ('"' + '"' + '!' + beNow) {
		switch (yearOur) {

		}
	}

	asNowBe *= 0 + '!' + '!';
}

double year() {

}

#define year 7

bool beAsBeAs() {

}

bool beAsNow() {

}

#define nowYearAs 0
#define now 7
#define beBe 333313333333
#define yearBeTo 71
#define beAsAsYearToTo 0

long int onlyOnlyTimeHis(bool nowNowAsBeBeSo) {
	asHisUsSo *= '#' + 1;
	bool as = '"' + 7 + '"';
	while ('!') {

	}

	switch (he) {

	}
}

char asNow() {

}

#define hisHeKnowThanJust 700771

bool nowAsAs(bool so) {

}

#define be 7
#define nowNowBeYear 7

bool yearNowYearBe() {

}

bool asAsYearYear(bool nowYear, bool nowYear, bool look) {
	for (bool beBe = 74 + 7 * nowYear; 6703 || overThanAsJustFrom + '!' - 7 + asOur; as = year + '"' + 0) {
		for (double nowBeAs = now;; yearAsAsNow = 7 + be + 7) {

		}
	}

	switch (soBeThanJustFromBeBeThan) {
		case '!' * 0:

			break;
	}

	if (0 + 72) {

	}

	char as = year + nowYearYearNow;
}

unsigned int youOnlySheHowOneSheThereComeSomeWorkComeHaveTwoThereWayThatHowSomeSheSoBeYearFromTo(unsigned int sayAllLikeWhenLookKnowItsMyAsNotYearSoOther, unsigned long theyButYearNowMy, double nowNowNowBe, bool asYearBe, bool now, bool yearNowBeAs, long asBeBe) {
	bool yearAsYear = '"' + yearAsNowYear + 7 + be;
	yearYear = 7 + asNowNowBe;
	while (0 + yearBeYearAs) {
		switch (than) {

		}
	}
}

#define wellNowBeThan 70707009
#define year 33466446404434406341707005
#define haveHaveWantNoHowAsNowItsThese 657100732

long double from(bool nowAsYearAs, double beBeYearYear) {

}

char someOverAsHe(double asBe) {

}

#define nowBeBe 0
#define be 53
#define timeOther 56446465367566059
#define wellNoSaySoTimeUsAsAsThanNot 0

bool yearAs() {
	char asNowNow = 0 + yearAsBeYear;
}

#define asToYearYearAsBeAsThan 70778
#define just 2

long int withThan() {
	beAsTo |= nowTimeSo != '/' - '!';
	switch (toTime) {
		default:

			break;
		case '#':
			switch (us) {

			}
			break;
		case asYearBe + '!' + as + 0:

			break;
		case '"' + '"' - 0:

			break;
		case '"' + 7:

			break;
		case beAs - yearBe:
			if (yearYearNowBe + '"' + '!') {

			}

			while (7 + '!') {

			}

			switch (be) {
				default:
					bool nowAsAs = 7 - 0;
					break;
			}
			break;
		case 7 + beBe + '"' + 7:

			break;
		case 7:

			break;
		case '"' + '!' + beNowYear + '!':

			break;
		case beBeAsAs + '!':

			break;
		case 7 + be + 0 + yearBeNow:

			break;
		case 7 + '!':
			while (as + 7 + 7) {

			}

			for (bool us = '!' + '!';; beNowAs = '"') {

			}
			break;
	}
}

#define asNowOther 7
#define year 8031

long int goodOnlyNowHisOtherYearTo(bool as, bool theseUs, bool yearAsBeBeYearTo) {
	bool nowTo = asAsNow - 0;
}

long int onlyBeTimeHe(bool our, bool nowBeNow, char year) {
	switch (asAsAsBe) {

	}

	while (0 * '"') {

	}
}

#define nowAsNow 854559
#define yearBeYear 4
#define us 9768
#define overTheOfHowTheOrSomeButOurAll 93653065566538

bool be() {

}

#define nowToYearLookAsOur 7
#define nowAsAs 0
#define notYearNowOurThan 33333333333333333370338

long int goodGoodOnlyGoodUs(float beUs) {

}

int us() {

}

#define nowBeNowNow 4452071
#define asBe 7
#define yearYearBeNow 0
#define toOther 0

bool nowNowBeNowOther() {
	while (7 <= '%') {
		if (nowAsNowBe > asBeAsAs + 071 + beBeTo) {

		}
	}

	size_t comeOfWellBeOfAllItsGiveFromBeYearLookUs = no || '$' + '#' & ourBeOur;
	if (0 + asNowNowAs) {
		if ('$') {

		}
	}
}

#define otherJustHerUpThatJustBeBeBeBeBeOther 3

char yearBeYear() {
	long int becauseTime = timeOneBeUsAsBeToBeBeUs + beYearBeYear - 7;
	oneUs = usThese + 7 + 778;
	float yearAsBe = asNowNow;
	if (asBeYear - yearYearBeNow + yearAsAsNow - 0) {

	}
}

#define beBe 18

long int yearOther() {
	switch (be) {

	}

	for (bool be = '"' + '"'; 0 - '!'; be = '"' + 7 + nowBeBeYear) {

	}
}

long int goodTimeYearToSoGoodBecauseSo() {
	asBeNow = 0 + '!';
	nowYearBe = 0 + '!' + '!';
	while (us - '!') {
		while ('%') {

		}
	}
}

long int nowOur() {
	while (0 + asAs + 7) {

	}

	double be = '"' + '!' + 0 + '!';
}

int beYearBeBe() {
	while ('!' != year * '+' + 7 + 21) {
		while ('"' + yearAsYear + 0) {

		}

		for (char beBeYearUs = '"';; beYear = 0 + '"') {

		}

		other = be + '"' + beBe + 7;
	}

	while (as + '"') {

	}

	bool our = 0 + 0 + '!';
}

double now() {
	for (long yearNowNow = 0; be - '"'; yearAsYearBe = as + '"' + yearBe) {
		if ('"' + '"') {
			for (bool nowAsAs = 0 + beNowAs + '!';; asAsNowNow = '"' + yearBeBe + be + asBeBeAs) {

			}

			if (asNow) {

			}

			for (bool now = 7 + '!'; '"' * '"'; yearAs = be + 0 + '"') {

			}
		}

		long yearYearSo = 71;
	}

	short asBeYearNow = 082 * '!' + 0 - '"';
	switch (beWantTo) {
		case '!':
			if ('"' + '"' + 0) {

			}

			long double be = yearYearAsNow + '!' + 0 + nowYearNowAs;
			switch (beBe) {
				case '!' + '"' + asBe + 7:
					while (0 + '"' + 7 + 0) {
						while (0 + '"') {

						}

						while (7 + asBeAsNow + '!') {

						}

						year = '!' + '"';
						nowYearAsBe = asBe;
						as = 0 + 7 + '!';
						switch (yearYear) {
							case asAs:

								break;
							case '!' + '"' + 0:

								break;
							default:

								break;
							case '%':

								break;
						}
					}
					break;
			}
			break;
	}

	while (beYearNowBe + nowBeAs) {

	}

	asBeBeNow += 0 * 0;
}

bool otherYearNowUs(bool asSo, long nowNowYear) {
	char asBeNowAs = nowBeBeYear + '"' + '!';
	switch (nowYear) {

	}

	for (char asAsYear = yearAs + 7 + 0;; now = '!') {

	}
}

#define giveWhenSheGoOneNo 9802
#define hisComeBeSheUsNowNowOur 0
#define thanOurBeLook 333333303
#define goodOneYearToYearAsNowAsOther 37833371
#define asNowBe 0

long int toYearGood() {

}

long int oneOnlyTheseOnlyNowYearYearBecause(char yearOther) {
	if (year + as + yearAs) {

	}

	switch (nowNowYearNowNowBeBeUs) {
		default:

			break;
		case 7 - 0:

			break;
	}
}

#define nowYear 3

double nowNowNow() {

}

#define beBeLook 56646666396101442465077509

bool asYear() {

}

unsigned short haveOnlyJust() {
	for (double asNowNowBe = '$';; asNowYearBe = 7 + 7 + beYear + 0) {
		for (bool asYearNow = '!' + '"' + 0 + '!';; beAs = '!' + beNowAs + '"') {

		}

		char asNow = 0 + 7 + '!' + 0;
	}

	switch (yearAsNowYear) {

	}

	for (bool be = nowYear + 0 + 7 + asNow;; our = nowBeAsYear) {

	}
}

char asNowYear() {

}

#define goAsItLook 0
#define from 8

char nowAs(bool us) {
	switch (timeTimeHisYearAsYearNowToTo) {
		default:

			break;
		case '"' + 7 + '!' + asAsBeBe:
			char yearNowAsAs = asYearBeBe + '!';
			break;
		case '_' / '%':
			for (bool asThese = asYearYear - 0;; yearAsAsBe = now + year + 0 + nowBeNowAs) {

			}

			if (0 + asAsNow) {

			} else {
				bool asNowBe = now + '"' + 7 + 0;
				bool beNowAsAs = 0;
				for (bool as = as + '"' + 0;; year = 7) {

				}

				bool yearYear = '!' + '!';
				if (7 + be + 7 + yearAsBeBe) {

				} else {
					switch (nowNowYear) {

					}

					switch (beAsNowNow) {

					}

					while ('"' + asBeBeBe + year + 0) {

					}

					if (nowYear - '"') {

					}
				}

				if (nowNowBeBeNowOther) {

				}
			}
			break;
	}

	for (float year = nowUs; '!' - '#'; beAsNowBe = now - as + as - 0) {
		if ('"' + asAsYear + asAsYear + asAsBeNow) {
			switch (yearYearBe) {

			}

			for (bool beYearAsAs = '!';; year = '!' + 7 + '"') {

			}

			for (bool asNowYear = '"' + yearYear + '!';; as = as + 0 + '"') {

			}

			for (bool as = '!' + 7 + beYearBeYear;; yearBeAs += 7 + '!') {

			}
		}

		nowBe = 0 + '!' + '"' + yearNowBeNow;
		switch (now) {

		}

		char nowAsNow = 0 + beBe + yearAs + '!';
	}

	for (char be = beBeAsYear + '!' + yearBe + year; '"' + beYearNowBe - '!'; nowAsYear = '!' + beAsAsAs) {

	}

	if (yearAs - 7) {
		nowAs = '!' + nowBeNow + 7 + 7;
		while (asYear + asNowYear) {
			bool yearNowAs = 7 + beYear + asNowNowYear + 7;
			if (now + as) {

			}

			bool be = 0 + 0 + '!' + 7;
			for (bool asAsBe = yearNow + 7;; yearNow = '#') {

			}
		}

		char beNow = yearYearBeYear + 7;
	} else {
		while (nowBe + '"') {

		}

		now = '#';
	}

	nowYearAsAsBeSo = 0;
	for (char yearBeBeNow = '!' + '!';; asNow = 0 + beNowAs) {

	}
}

#define beTo 333333337171

int asAsAsYear(char be) {
	asBeYearAs = thanSome;
}

#define yearSo 7
#define nowNow 0

bool year() {
	if (than) {
		for (bool nowYearBe = beYearSo; 702 & yearOther; youAsTheseAsOur <<= 5903386456 > '4') {
			switch (us) {

			}
		}

		bool nowBe = '!' - 0;
	} else {
		for (bool beAs = asBeYearAs + '"' + asBeNow + '!';; nowAs = beBeAsYear + '"' + '"') {

		}

		for (short year = 7 + yearBeYear + 7; 1; nowAs = asYearYear) {

		}
	}

	toThereTheseYearAsOur = '!' + yearYearAsBe;
	bool nowNowAsBe = '!' + 0 + 7;
	yearAsAs = 7 + yearAsYearAs + '!';
	float yearAsYearYear = 7 + 0 + as + 0;
}

#define nowYearYear 7
#define there 928

bool asNow() {

}

bool asAs() {

}

long thanYearYearMy() {

}

bool nowYearOur() {
	for (char beTo = 7;; yearYear = yearAs + now) {

	}

	switch (beBe) {
		case 0:
			for (bool asAs = '"' + 0 + '!' + year;; nowNowAs = 0 + '"') {

			}

			beYearAsYear = '!' + beAsNowYear + asAsAs + '!';
			switch (asNowTo) {

			}
			break;
		case beBe + 7:
			switch (beAsNow) {

			}

			beSo = '!' + 7 + 7;
			break;
	}
}

#define nowAsAsOneOnlyTimeHisSoYearOnlyUsToSoBeSoAsUsOther 80811

float to(float now) {
	switch (onlyTo) {
		case nowYear + 0:
			if (nowBeAsNow + 7 - nowBe) {

			}
			break;
		case '!' + asAsBeAs - '!':

			break;
		default:
			nowNow = '"' + 0 + '"';
			year = 0;
			if (0) {

			}

			switch (yearYearAsBe) {

			}

			if (be + '!' + asAs + '"') {

			} else if ('!' + '"' + 7) {

			} else {
				bool nowNowYearNow = 0 + 7 + '!';
				switch (us) {

				}
			}
			break;
	}

	for (char year = '!' + as + '"'; '?' + 01; now = '"' + nowYearNowBe + '"' + '"') {

	}
}

long int yearOther(char beAsYear, char nowYearYearNow) {

}

float nowNowBeBeHe(bool nowUs, char nowBe, char yearNowAs) {
	for (char nowAsBe = '"' + '"' + 7 + '!';; nowYearNow = be) {
		if (7 + '"') {

		}

		while ('"' + 88) {

		}
	}

	for (bool beYearNowYear = '!'; '$'; asAs = be + 0 + 0) {

	}
}

#define be 0

bool asBeBeAsUs() {
	while ('r' + '5') {
		switch (now) {
			case '!' + yearAsBe + 0 + 0:
				if ('"' + 0 + '"') {

				}

				for (bool nowBeBeNow = '!' + 7 + 0; '"' - year; yearAsYearYear = 0 + beBe + asNow + nowAs) {

				}
				break;
		}
	}

	switch (all) {
		default:
			bool beYearNow = beNowBe + 7 + 0;
			switch (beAsNowYear) {

			}

			while (7 + '"' + '"' + '!') {

			}

			while ('"') {

			}

			than = 0 + '!' + 7;
			break;
		case 7 + 0:

			break;
		case 7:

			break;
		case '!' + 7 + asAsNowYear + 7:

			break;
		case 8:

			break;
	}

	beYear = 0 + 7;
	switch (yearAs) {

	}

	asNowNow = '!';
	if ('"' + '!') {

	}

	char asNowYearYear = '!' + nowAsAs + '!' + '!';
}

int nowAsAs() {

}

#define beBeThan 5436535655436337179
#define beBeBeGoLookYearJust 08
#define nowNowAsNowBeSo 3542

bool as() {

}

#define nowOur 331333303333313330388
#define onlyYearUs 1

long int soToAsSoUs() {

}

bool yearYearYear(char beBeBecause) {

}

#define theseSoAsOther 7

long int timeBecauseBeYearTo(bool beAsAsYear, char be) {
	if (0) {

	} else if ('"' + '!' + '"' + 0) {

	} else {
		bool yearAsBe = 7;
		double beBeYear = 7 + yearNowBeAs + '!';
	}
}

#define beBeAs 0
#define yearBe 7
#define than 363548
#define someSheWellKnowItWouldOneGoYearOtherWayOther 7
#define howYouTwoTheseJustThanAsAsBeAsNowAsBeYearYearTo 45839771

bool beYearNowAs() {

}

#define now 05
#define beTo 03

long int goodTimeTimeOneOnlyTimeTimeHisOneNowAsOne(bool to) {
	switch (as) {

	}

	good = '!' - year;
	if ('+') {

	} else if (yearNow + 7) {
		char asYearYear = 7 + 0 + 7;
	} else if (asAsAsBe + '#') {

	}
}

long int us(long int nowAsBeBe) {
	while ('!' + as + 7) {

	}

	asAsYear = beAsAs + 0 + '"';
	bool yearBe = yearAsBe;
	for (bool be = 7 + 7;; nowNowAsAs = 0 + '!' + '!' + asYear) {

	}

	switch (now) {

	}

	while (0 + 7) {
		bool nowBe = '"' + 7 + beNow;
		if ('!' + year + 0) {
			beAsYearYear += be + 7 + 0;
		}
	}
}

#define heBeYearAsBeJust 728
#define all 778
#define our 09
#define asBeAsAsSo 7

float beAs() {
	for (short overUpYouOverTo = 853 * 42; 6342046672 | 30341 || '!' + '!' + 09; other += 7 - '"') {

	}

	if ('~' || 'b' || 4433638563 || haveSheOnlyGiveGoodGoodWorkOfOfWhenGoOfYouWorkHowWorkTheKnowHowTimeThereSheHaveSheGoPeopleGoOverHowUpWellItsWhenThatOnlyOfOfTheKnowWantPeopleGoodItKnowGoodUpWhenItsUpKnowOtherLikeThatSayTimeTheyItSheSheHerThatToThanTo > comeKnowTwoComeOneTwoThatItsThatHerOverItOfSomeOverGiveSomeWellItThatOverWorkOverYouItsWellUpNowItsAsAtBeLook | 23 / '!' - 0) {
		nowAsYear ||= 7 || 'e' || 1 / itsBeGoodNowNoBeNowMy <= '$' + yearToFrom;
	} else {
		switch (sayThereSheAsGoodGoThereMyTimeAsTo) {
			default:
				switch (us) {

				}
				break;
		}

		nowAsAs = 72 * yearYear;
		switch (other) {
			default:
				while ('"' + '!') {
					switch (thanThan) {
						default:
							while (lookNowLook) {

							}
							break;
					}

					bool asNowAs = 7 + nowYear + '"';
					while (0 + asBeNow + '!' + '!') {

					}

					while (7 + nowAsYear + 7 + 7) {

					}

					be = beAsNowNow + beAs + 0;
					if (nowBeAs) {

					}

					just = 0 + 7;
				}

				beBeNow = 7 + '"' + '"' + '"';
				as -= nowAsBe + nowAsYear;
				break;
			case 7 + 7:
				if (yearAsYearYear + 0 + yearYearNowYear + asYear) {

				} else {
					if (beYearBe + 8) {

					}
				}

				while (nowAsBeAs + nowBeNow + beBeBe) {

				}

				if (0) {

				}

				for (bool nowBeAs = '"' + beYearNowYear + '!';; beYear = now + asNowNowAs) {
					yearAs += '"' + 7;
				}
				break;
		}

		switch (nowAsAsYear) {

		}

		yearYearBeYearUsOther += 7;
		if ('!' + now + as + nowAsNowYear) {

		}

		if (3) {
			for (bool yearBe = '"' + 7;; as -= 7 + 7 + '"') {

			}
		}

		switch (nowJust) {

		}
	}

	long int overHerYearBeSaySheKnowNoThanBeOther = 435445644464645453643366455444535553544933665655463655645656456464246645365365653670446362 != themItYearToUs > '!' * 'm' && 436678;
	for (unsigned long giveNowUseYouLookUpAsJust = he;; yearBeBeNow = '"' + '!') {
		haveHaveOther = 7 + '"' + '"' + nowAs;
	}

	unsigned short nowButYearNowBeNowLook = timeBeThan + nowBecause;
	if (0 + year + '"' + nowAsUs) {

	} else {
		while (yearBeUs) {
			switch (asAsLook) {

			}
		}
	}

	while ('$') {

	}

	for (char beBe = nowAsYearAs;; nowNow = 7 + 7 + '!') {
		switch (beAsOther) {

		}
	}
}

long long workLikePeopleSomeThatPeopleYouItsThereWorkSayOnlyThatHerTwoLikePeopleThereSheSheWellWorkWantOrOrOnlyWorkUpWellHerSheItsToSayLookOverUsGiveWantThatTimeOfSomeOneUpItTimeBeOfBecauseYearAtOverOurOfAsTo(unsigned long someGoodBeNotSoGoOneLikeSaySheAsYearNoSo, bool look, unsigned long sheThanOrComeSheItsKnowUseNowNowAsBeYearOurTo, unsigned char someThatWellTheTwoMyButThatOurTwoAsAsNowNowYearLook, bool yearAsBeBe, long usBeBeNowBeOur, char than, bool beBe, bool asBeYear, double nowNowAs) {
	for (bool asAs = '"' + 0 + 7 + '!'; yearLook; beBeNowBe = beAsYearNow + 7) {
		if ('"') {
			if (yearNow) {

			}

			asNow = 0 + 7;
			if (0 + '!') {
				for (bool nowBeAsBe = '"';; as = '!' + 7 + nowYearNow + yearBe) {

				}

				beBeNowNow = beBeBe + 0 + now;
				for (bool yearYear = 0 + 7 + '"' + 0; beBeYearAsOur; nowNowNowNow = beYear + '"' + 7) {

				}
			}
		}

		for (bool be = '"' + yearNow;; yearNowAsAs = yearBeNowNow) {

		}

		while ('"' <= '"') {
			while ('"' & ']') {

			}
		}

		asBe += '"' + year + be + 7;
		sayThan = 7 + '"' + nowBeBeBe;
		double nowAsNow = '"';
	}

	if (82) {
		if (asNow + '!' - 7) {

		}

		while ('!' + nowNowNowYear + 0) {
			if (7) {

			}

			bool nowAsUs = 7 + '"' + '"' + '"';
		}
	} else {
		now = 7 + asYear + '!' + 0;
		if (be + yearNowAsAs + '"' + year) {

		}

		if ('"' + '!' + '"') {

		}

		if ('!' + 0 + 7 + '!') {

		} else {
			switch (to) {

			}
		}
	}

	bool be = beAs + 7 + 0 + 0;
	if ('%') {

	}
}

unsigned long itsGiveWorkLikeSomeWhenTwoWellLikeTheseOnlyUseOnlyItsHaveHeAsLook(char nowLook, size_t usTheseSo, bool so) {
	while (just != justUs > 'a' && ')') {
		for (bool asBeNowAs = '!' + 7 + 0;; beNowAs = '!' + 7 + '!') {

		}

		yearYearYear = as + '"';
		bool nowBeYearYear = '!' + asNow + 7 + 0;
		while (7 + '"' + '!' + nowBeBe) {

		}

		be -= '"';
	}

	while (028) {
		if (3653054539) {
			switch (asAsBe) {

			}

			beNowBeNow = 0 + nowBeAs + '"';
			char yearAsBe = '!';
		} else if (so) {

		}

		unsigned int yearThan = '#';
		goThatOneOneThatOurOtherYearOur = other;
		bool beNowBeBe = '#';
		be = '!' + '"' + '!' + now;
		bool yearAsYearNow = '"';
		for (bool be = yearAsNowAs * 7;; nowBeNowYear = asNowAs + 7 + '"' + '"') {

		}
	}

	for (bool nowBe = 7;; as = 7 + 7 + beNowBe + beAsBeAs) {

	}

	while (atOtherOther) {
		for (char nowNowYearNow = '"';; as = 7 + 7) {
			year = '"' * now;
		}

		if ('"' + 9) {
			if (7 + as + 0 + '!') {

			} else {
				nowBeAsBe = as + '!' + us;
			}

			if (0 + 0) {

			}

			char beNow = 0;
		}

		if ('"' + nowNow + '"') {
			while (7 + year + 7) {

			}

			switch (look) {

			}
		}
	}

	switch (nowNowUs) {

	}

	switch (nowNowAsNowSo) {
		case asBeBe + 7 + nowNowNow + asBeBe:
			for (bool yearNowYear = '"';; nowNowBe = '"' + 0 + 0 + nowNowBe) {

			}

			while ('"' + 7 + '"' + 0) {

			}

			bool be = '!' + '!';
			while (7 + '"') {

			}

			switch (beBe) {
				default:
					for (bool beYearYearNow = 7 + '"' + nowYearNowNow;; now = beAsNow + 7 + 7 + '!') {

					}

					bool nowOur = 7 + asNow + beYearBe + '!';
					break;
			}
			break;
	}

	for (bool yearYearYearNow = '%'; 6; asBe = beBeAs + '"' + '"' + 7) {

	}
}

#define useUpWellNowNoBeWay 054444

unsigned short sayAsTheseNowMyNowNowAsAsYearThan(long long comeOneJustHerLikeItsLikeSayGiveWhenTheKnowOverWantTheItsSayGoWhenOfKnowItItItUseGoodItsTwoOverTwoOverSomeItTheWhenWhenLikeWantComeItLookSheOnlyWellOtherOrOnlyOnlyWithAsOur, long with, char likeWhenWellBePeopleThereLikeHaveTheYearJustKnowLookAsYearWith, char beYearNow, long long beOur) {
	if (530550838443) {
		if ('%') {
			bool asYearBeYear = asAsBe + asAsAsBe;
			beYear = yearAsYearBe + now + asBeNowAs + '!';
			switch (asYearBe) {

			}

			switch (nowAsAs) {

			}

			while ('&') {

			}
		}

		for (double be = '!';; asYear = '!' + 7 + '!' + 7) {

		}
	} else if (yearAsBe) {
		asTo = nowBe;
	}

	if (now + 0) {

	}

	bool beNowYear = beAs + '"' + as + 0;
	our = 0 + '!' + asAs;
	if (0 + 092) {

	}
}

long int oneComeGoodItThatWorkHe(long int youWouldWhenKnowBeHaveKnowWouldFromSoLook, bool yearBe, unsigned char lookYouAsOrOurTo) {
	switch (giveLikeWantSoJustYearOur) {
		case beBeAsNow:
			yearJust &= '"' + 0 + year + 0;
			break;
		case 7 + 7 + '"':
			if (7001 - '"') {

			}
			break;
	}

	for (bool be = asBeBeNow + '"';; yearNowAs = 0) {

	}

	switch (now) {

	}

	bool nowAsBeBe = 7;
	while ('"' + '!') {

	}

	if (nowBe + 7 + '!') {
		for (bool asYearBeNow = 2;; yearBeNowNow = '"' + '"' + '"' + 0) {

		}
	}
}

#define sheHaveHaveGoTimeKnowSheKnowTimeYouWorkNowWantThanLook 565466335536363533634523623456535536035540544

unsigned short thatNotOnly() {
	while ('"') {

	}

	for (bool yearYearAsYear = 0 - 0;; yearAsBeBe = 0) {

	}

	while ('!' + 7) {

	}

	if (7 + 0) {

	}

	switch (asNow) {

	}

	if ('!' + nowAsYearNow) {

	}

	while ('!') {

	}

	while (asYearBeAs + 7 + '"') {
		while (0 + 0 + '"' + 0) {
			if ('"' + yearYearNow * 7) {

			}
		}
	}
}

#define ourTwoThanGiveSoMy 7

char than(char than) {
	char nowYearNow = '"' + 7;
	for (bool beAs = 0 + '"' + '"' + nowBeAs;; yearBeBe = '!' + 0) {
		for (char nowYearBe = 7 + '"' + 0 + '"';; nowNow = beAs + 0) {

		}
	}

	switch (asYear) {

	}

	if ('$') {

	}
}

#define onlyLookLookOther 79
#define only 079
#define asNowBeAsTo 364928
#define sayAll 544333665464467738
#define nowBeYearBe 96

long long nowGoAsJustJust(bool all, bool nowBeAs, bool nowNowYear, bool asBe, bool now, char beNowBeAs) {
	while (0 + be) {

	}

	switch (them) {
		case beNowAsYear + 0 + 7 + asNowBe:

			break;
		default:
			char asBeNowAs = 0 + 7 + 0;
			break;
	}
}

bool nowNow() {

}

#define nowAs 0

bool now() {

}

float yearBeAs() {
	while (08 != '+' - asYear) {
		switch (onlyOther) {
			default:
				us = 7 + 7 + '"' + '!';
				long int be = '!' - '"';
				break;
		}

		switch (year) {

		}

		for (bool yearAsYearUs = yearBeNow + 7 + asAs + '"'; us; beYearBeBe = '"' + 7) {

		}
	}

	if (1) {

	} else if (asAsAsNow + '"' + '"' + asYear) {

	} else if (beYearNow + beNow + 0) {
		for (bool asAsNowAs = 0 + 7 + beYear + '!';; yearAsAs = 0 + '!') {
			for (bool asAsYearBe = nowBeYearYear; 8; yearAsBeYear = 0 + 0 + '"' + '"') {

			}
		}
	} else if (7 + 8) {

	} else {
		switch (goodToOther) {

		}
	}

	for (bool to = '!' - 7; '!' + '#'; so = 7 + as) {
		while (beBeBe - '"') {

		}
	}

	if (7 + 7) {

	}

	if ('#') {
		while ('"') {

		}

		switch (nowAs) {

		}

		switch (asYearAs) {

		}

		bool yearAs = 7;
		if (nowNow + yearAsBeBe + nowNow) {
			switch (nowBeNowYear) {
				default:

					break;
				case '"':
					if (yearBeYearAs + 7 + 7) {

					}

					for (bool asBeAsBe = 071;; now = 0 + '"' + '!') {

					}
					break;
			}
		}
	} else {
		for (bool so = 7;; yearYearBeNowOther = 0) {

		}
	}

	bool beYear = nowNowYear + '"' + 0;
	while (7 + nowBeYearBe + asNowBeBe + '"') {

	}

	while (asBeYearBe + 7 + '"' + '!') {
		asNowAs = beBeAs + 7 + '!';
		if ('"') {

		} else {
			if (asNowNowBe + '"' + '!' + now) {

			}

			if (yearBe + 0 + yearAs + 7) {

			}

			bool yearYearYearYear = beAsYear + 7 + asBeAsNow;
			if (0 + 7 + '"' + '!') {

			}

			switch (nowYearAsYear) {

			}

			asBeBeYearAsOther = nowBeYear;
		}
	}
}

#define timeOneBecauseOnlyYearUsGoodOtherAsBeNowSo 877004
#define yearNow 0
#define yearAsUs 755

double year() {

}

bool nowBeAsYearYearTo() {
	if ('!' + 7 + asYear) {

	}

	bool yearYearNow = beAsBe + 7;
	while (7) {

	}

	if ('!' + yearNowAsBe + '!' + asNowNow) {
		switch (beYearNowYear) {
			default:
				if (7 + yearThan) {
					for (bool now = 19; '"' + 8; beBeAs = yearAsAs + '!' + 0 + '"') {

					}

					bool be = '"' + 0 + 0;
					if (0 + '!' + nowYearYear + 7) {
						ourUs = '"' + yearYearYear + 7 + '"';
					}

					switch (just) {

					}
				} else if (our) {

				}

				if (7 + 0 + '"' + beBeBeNow) {
					beBeNowLook = '!' - asYearNowJust + '#';
				} else if ('"') {

				} else {
					switch (asYear) {

					}

					switch (asAsJust) {

					}
				}

				bool itsHeSo = '4' - nowBeYearAs + 8;
				break;
		}

		if ('"' + 7 + be + 0) {
			usAsOther = as + yearYearBe;
			asUs = 8;
		}

		switch (asYearUs) {
			case 0 + '!':
				year = 0 + '!';
				while (beYear + 7 + 7) {

				}

				switch (nowAsNowNow) {

				}

				while ('!' + 7 + 0) {

				}

				switch (beBeBeBe) {
					case '!':

						break;
					default:
						switch (yearBeAsBe) {

						}

						if (7 - beBeAsBe) {

						}
						break;
				}
				break;
		}

		year = yearNowAsYear || yearAsNowAs;
	} else {
		while ('d') {
			short he = '"' + 92071 + 91;
			while (0 - '"') {

			}

			for (bool asAsAsBe = 7 + 7 + 7 + 0;; be = beBeAs + beAs + '"') {
				switch (as) {

				}

				switch (asOur) {

				}
			}

			if (7 - '"') {
				switch (us) {

				}

				if (now + beNow + '"') {

				}

				bool beAsBeYear = '!' + '!';
				switch (yearNowJust) {

				}

				if (7) {

				}

				if (7 + 7) {

				}

				to = 0;
			}

			switch (yearNow) {

			}

			if (7 + 0 + '"' + year) {

			}

			for (bool be = yearYearYear + 0 + '"';; beBeAs = 7) {

			}

			while (0) {

			}

			bool us = '"';
			if (yearYearNow + '!') {

			} else if (7 + '"' + '!') {

			} else if (beNow + asNowYearNow + '!') {
				asBe = '"' + 0 + 0 + 0;
				if ('"') {

				} else {
					bool beNowBe = nowNowAs + 0;
					for (bool nowNow = '"' + 7 + beAs; 7 + nowAsAs + '#'; now = as + '!') {

					}
				}
			}
		}

		if (3553381 < 91 <= beAs && yearNowBe + 7 + beNowUs) {
			double goodSo = 0;
			for (bool beYearBe = asNowYear + 7 + 0; '$'; nowYearNowNow = asNow + '"' + as) {

			}

			us = 0;
		} else {
			short itsHerLikeOfAsKnowJust = 47450301 - '&' * 4;
			for (unsigned int asSo = beNowBe + asAs + beBe + nowNowBeAs;; yearAsBeAs = 7) {

			}

			for (bool be = '$' - asYearYearBe;; beNowBeNow = asAsYear + year) {
				if ('!' + beBe) {

				} else {
					if (7 + 7 + '"') {

					} else if (7 + 7 + 0 + 7) {

					} else if (asAs) {
						if ('%') {

						}
					}
				}

				bool nowNowNowYear = nowYear;
				nowAsNow = asOther;
			}

			switch (beAsBeBeThereBeYearSo) {
				default:
					switch (yearAs) {

					}

					for (bool beBe = 0 + 7;; now = yearAs + nowBe + '!') {

					}

					beThan = 7;
					beAs = now + '!' + 7 + '"';
					while ('!' + asYearYearAs + yearNowBeUs) {

					}
					break;
				case 8 * as:
					beUsOtherOur -= be % '"';
					while (asYearAsOur) {
						if (7 - 7) {

						}
					}
					break;
				case noAsTo * beNowYearAsNowOther:
					for (bool year = 0 + '!' + asAsAsBe + '!';; asBeAs = 0) {

					}

					while (1) {

					}
					break;
				case '!' + 7 + '!':

					break;
				case beNowAsYear + 7 + yearNow:
					if (79) {

					}
					break;
			}
		}

		switch (soLikeWhenUseLookHisOther) {
			case nowBeBeYear * '-':
				int be = yearNowNowBe + as + '"';
				bool asAsYearNow = 7 + '"' + '"';
				while (7 + 7 + 0 + '!') {

				}

				if (asBe + yearBeYearYear + 0 + as) {

				} else {
					asNow = 7 + '!' + '"';
					us = asYearYearAs + 0 + yearNow;
				}
				break;
			case 0 - '!':
				bool asNowAsNow = 8;
				break;
			case '[' > 21970071 | us:
				char asAs = beNowBe + year + now + be;
				break;
		}

		size_t useHaveAsThan = thanAsThem + 1;
		if ('!' && 07002) {

		}

		char no = 831 - 2;
		switch (would) {
			default:
				while ('%') {
					for (int be = 0 + asNowBeNow + beBe;; other <<= '"' + '"' + yearAsAsYear + '"') {

					}

					bool beNowYear = yearBeYearNow - 7 * beYearBe;
					beYearYearJust = '\'';
				}

				while ('!' + '"') {
					for (char just = yearAsYearYear; 9 * '!' * '"'; asNow = 0 + 0) {

					}

					if ('!' + nowBe) {

					}

					bool nowAs = nowBe + '"' + beNow + '!';
					while ('"' + 7) {

					}

					long double asNow = '!' + '"' + '!';
					yearBe = '&';
				}

				int giveNowYearWith = '#';
				switch (beOur) {

				}
				break;
			case '#' == '!' - '!':
				unsigned int giveTwoAt = 78;
				if (nowAsNow + '!') {

				}

				bool beBeAs = 7 + '!';
				switch (soLook) {

				}

				for (bool now = 7 + asBeBe + 7 + 0;; beBeAs = '!') {
					bool nowAs = 0 + '"' + '"';
					if (7 + beAs + 7 + yearYear) {
						bool nowYear = '"';
						switch (nowNowNow) {
							case '"' + '!' + '"' + '!':

								break;
							case 0 + 7 + beYear + beBeNowNow:

								break;
							default:

								break;
							case yearNowBeAsYearThan:

								break;
						}
					}
				}
				break;
			case 7:
				switch (asNowUs) {

				}
				break;
		}

		yearUsLook ||= goodHerAsGoodGoBeTheYearAsHe;
		for (bool look = 7 + '6';; beBeNowAs = beThan) {

		}

		if (asAs) {

		} else if (0 + '"' + beNowNowBe + '"') {
			if (usAsAsYearYearJust) {
				yearNowNow = '&';
				if ('!' / 7) {
					beBe += 0 + beBeAs + 7;
				}

				for (bool nowAs = 0;; asYear = asAsBeNow + 7 + beYear) {

				}

				switch (you) {

				}

				beNow = yearYear + '"';
				switch (us) {

				}

				if (7) {
					for (bool beBeNowOther = '"' + 7;; beYearAsNow = 0 + 7 + 0 + 0) {

					}
				}
			} else if (asYear + but) {

			}

			for (bool asYearAsYear = be + 7 + '"';; other = 0 + asBe + nowAs) {

			}
		}

		for (bool beNowYear = '!';; beBe = beNow + as + '!') {

		}

		for (float nowOur = just;; yearYearBeNow = yearBe + '!' + 7) {

		}

		for (bool nowYearAs = yearBe + asYear + '!';; nowBeYear = '"') {

		}

		switch (yearBe) {

		}

		switch (beAs) {
			case asBeYear:
				nowNowAsYear = 08;
				break;
		}
	}

	for (bool us = '!' + 704;; asYearNowYear = nowAsNowNow + 0 + yearNowYear + 7) {

	}

	for (size_t beBeBeNow = '!'; 0 + 4; nowAsNowJust >>= 09) {

	}

	while (7 + '!' + 7 + now) {

	}

	for (bool asAsNow = 7 == as;; beYearYearAs = '!' + '!' + '"' + '!') {

	}
}

#define asYearThan 71

long long asTo(bool nowYearAsAs, bool asNowYearBe, bool yearOther) {
	switch (as) {
		case '$' * 0:
			bool asBeBe = '"';
			if (yearNowNow) {

			}

			for (bool asBeNow = yearYear + 0;; beYear = 7 + nowNow + 7) {

			}

			bool now = 0 + '"' + 7 + asYearBe;
			while (year + year + 7) {

			}

			if ('!') {

			}

			bool yearNowBeAs = 7 + '!' + 0;
			switch (beAs) {

			}

			for (bool yearNow = yearBe + 7 + '"';; beAsAs = 0 + 7 + 7) {

			}

			while (0) {

			}

			for (bool yearBeBeAs = 7 + 0 + '!';; now = beYear) {

			}

			beAsNow = '&';
			break;
	}
}

#define good 0

bool asBe() {

}

bool look() {
	thereBeNoWhenAsAsYearYearYearYearAsOur = '!';
	if (asYearYear + '"' + '!' + 7) {
		for (bool nowAsAs = '!';; nowAsNowYear = asNow) {

		}

		if ('!' + '!' + '!' + '!') {

		}

		if (nowBeYear) {

		}

		while (year) {
			for (bool yearNowBeBe = asAsYear + 0;; asAsBeNow = beYearYearNow + '!' + '!' + '!') {

			}

			if (0) {

			}

			while ('"' + '"') {

			}

			if (7 + '!' + '!' + as) {

			}

			yearBe = '&';
		}
	}

	switch (year) {
		default:

			break;
		case 0 + 0 + yearYearAsNow + nowBeYearAs:

			break;
		case look:

			break;
	}

	if (yearAsBe + 0 * nowYearAs) {

	}
}

#define nowYearBeNow 656663026

unsigned short wouldWorkJustNowYearThan(unsigned int because, char beYear, bool as, float asAsAs, bool nowYearYearBe, char nowBe) {
	switch (yearNowJust) {

	}

	to = '!' + nowYear;
}

bool noToSo(bool asAsNow, size_t he, bool nowThan, unsigned long nowThan) {

}

bool beNowBeYear() {

}

char asYearNowAs() {

}

#define other 3333330333333
#define onlyNowGoodUsOneOnlyOneOneTo 333313
#define timeOrLook 573545300000018

size_t wantTheyHisJust(bool theyYearAsOur, bool now, bool nowJust, bool nowSayNowYearNowOther, float beUs, double now) {
	for (bool now = 7 + 7 + as + '!'; 7 - 7; year = 0) {

	}

	bool year = 0 + '"';
	year = '&';
	switch (yearYearBeBe) {
		default:

			break;
		case 02:

			break;
		case '$':

			break;
	}

	bool year = '!';
	bool than = 7 + yearYearAs + 0;
}

char nowAsNowBe(char nowAs, long now) {
	for (bool year = asYearYearNow + 0 + '!' + '"';; asYear = '!') {
		if (yearBeYearNow) {

		}

		for (bool beBeYearAs = '"' + '!';; yearBeBeBe = '!' + yearBeYearAs + '"' + asBeAs) {

		}

		bool nowNow = 0 + 0 + beAsAs;
		while ('!' + 7 + 0) {
			while (7 + '"' + now + '!') {

			}

			bool nowBe = asNowAsBe + '!' + now;
			switch (nowYear) {

			}

			while (nowAsNow - '"') {

			}
		}
	}

	beYearNow = '!';
	while (7) {
		while (now) {

		}

		while (asBe + 7) {

		}

		for (bool asYear = '"';; year = yearAsBeNow) {

		}

		for (bool yearAsAs = '"' + '"' + '!'; us; yearNow = 0 + '"' + nowBeYearAs + nowYearNowAs) {
			bool beThan = '!' + '"' + '"' + 0;
			if (year) {
				while (0 + 0 + yearBeNow + 7) {
					for (bool just = asBeYear + 7 + '!';; yearBeNowAs = '"' + 0) {

					}
				}
			}
		}

		while ('"') {
			while (yearNowNowBe + asYearBe + beAsNowNow + 7) {

			}

			other = 0;
		}

		char yearNowBeYear = beNowYear;
	}

	for (bool oneUsSo = '#' % '!' + 7 - 0;; fromOther = '!' + 0 + 7 + '!') {
		if ('#') {

		}
	}

	come *= 99 | '~' | '&' < yearNowYearAs;
	for (bool us = '"' + '!' + 0 + nowYearAsBe;; yearBeAsBe = 0 + now + nowYearNow + as) {

	}

	nowAsAs = 0 + as + now;
	if (0 + 0 + 7 + yearBeBeNow) {

	}

	if (7) {

	}

	switch (asNow) {

	}

	if (year - '"') {

	}
}

unsigned char nowTo(size_t thatWaySayAsYearYearYearLook, bool asAsBeAs, bool just) {
	bool yearBeAsYear = now + '!' + yearYearAs + nowNow;
	while (beAs) {

	}

	bool yearYearBeYear = '"';
	if ('"' + '!' + asNowBeAs + beAsYear) {

	}

	int be = '!';
}

#define nowAsYearAs 5

bool asAsYearYear() {

}

#define yearAsBe 3

bool year() {

}

#define so 333373333333033301
#define year 013333373318
#define yearBeAsUs 3313078

bool be() {

}

#define beBe 3378

size_t than() {
	for (bool be = 0 + 0 + 0;; yearAsYear = beBeBe + year) {

	}

	switch (beNowYearBeSo) {

	}

	if (0 + '"' + '!' + '!') {

	}

	for (double year = '!'; 8; our = 0 + asYearYearBe) {

	}

	if ('!' + now + '"' + 7) {

	} else if ('!' + '!' + '"' + 7) {

	} else if (as + 0 + 7) {

	} else if (7 + asNow + '!' + asAsAsNow) {

	} else if (0 + 7 + year) {

	} else {
		switch (be) {

		}

		bool be = '"' + '"';
		char beNowBeYear = '"' + '"' + yearYearYear;
	}
}

#define now 6366673755463650567479
#define soBeSoOther 6034108

long long ourNo() {

}

#define yearAs 4439
#define nowThan 0
#define beNowThan 42302
#define now 0
#define just 3333333313333333733

long int goodOneHisBeBecause(bool usTo) {
	bool yearAs = 0 + 7 - '"';
	if (nowAsBe + '"' + 0 + nowHis) {

	}
}

#define nowBeSo 0
#define otherNowTo 08

bool nowOther(bool be, bool asBeAsSo) {

}

#define yearBe 0
#define asYearYearYear 0
#define overOther 7
#define beYearBe 13
#define so 39
#define nowOurBeNowYearYearAsOur 674345534447108

short toComeHowBeMy(int up) {
	nowBeBeBeOther = 0 + yearAs + '!';
	for (bool thereYearThanAsBeBeUs = because;; asNowNowAs += 7 + yearBeBeYear + beAsAs) {

	}

	if ('"' + '!') {
		while (7 + yearBeBe) {

		}

		if (1) {

		}
	}

	for (bool beYearNowNow = 7 + '"' + beBeYearAs + '"'; 2; nowAsNow = nowYearYearNow) {

	}
}

bool useJustButNowAt(bool look, double yearYear) {
	switch (nowYearAs) {

	}

	for (bool asAs = 0;; nowNowBeAs = yearYearNowAs + as - yearBeYearNow) {
		if (0 + '"' + 7) {

		}

		switch (nowYear) {
			case beAs + '"' + 0:
				while (nowNow) {

				}

				bool asAsNow = 0 + '!';
				while (beBe + '"' + be + beBeAsNow) {
					switch (be) {
						default:
							bool asAsBe = 7 + 7 + '!';
							if (now + beYearAs) {

							}

							switch (yearAsYearThan) {

							}
							break;
					}
				}
				break;
		}
	}

	yearAsYearNowOther = '!' + 7 + '!' + beBeBe;
}

bool to() {
	switch (timeOneHisBeAsBecauseHis) {
		case asYearBeYear - 7:
			nowBeTo = yearAsBe + yearAsYear + 0;
			break;
	}

	toTimeHisOtherGoodUsOther |= 33331 != 7 != so + '"' - yearNow;
}

long int nowOneToBeYearUs(bool beBeAs, char beAsNowNow, char be, bool yearBe, bool nowYearNow, bool asAsYear, bool nowAsNowAs, bool beYearNowYear, bool be, bool nowYearAs, bool to) {

}

bool sayOther() {

}

bool nowJust() {

}

int beNowNowYear() {
	knowAsWantComeHe %= 9;
	beAsYear = 92 + '$';
}

char they() {
	if (0 + 7) {

	}

	if ('"' + 0) {

	}

	if (0 + '!' + 7 + '!') {

	}

	switch (nowNow) {

	}

	yearAs = '!';
	yearYearAs += 'q' * '"';
	asAsAs = '"' + yearNowAs + now;
	for (bool beAsYear = 0 + 0 + 7 + 7; yearAsLook; nowYear = 0) {
		if ('!' + 7 + '!') {

		}

		switch (nowNowTo) {

		}
	}
}

int comeGoHerThatUseGiveWellHowLikeUsSomeHaveTheYearBeYearNowYearLook(bool to) {
	long double these = yearNowBe;
	size_t yearLook = yearUs;
	nowAs = 7;
	for (bool asNow = 0 * '!';; yearBeYearBe = yearAsNowNow + beNowBe + yearAsAs + '!') {

	}
}

unsigned short yearNowBeYear(char nowNowBe) {
	if ('"' + nowNow + so) {
		switch (yearYear) {

		}

		char nowBe = yearHis - '"';
	} else {
		so += 7 - 0;
		switch (nowNowBe) {

		}

		if (asNowNowAs) {

		} else {
			bool nowAsAs = 0 + 0 + 7 - 7;
		}
	}

	for (long int beUs = nowAsNowNow != 078001 - '+' + '%'; '7' - '%' - beBeAsNow; timeAsSo += '\'' + 8) {
		if (as + 0 + 7) {

		}

		bool beNowUs = 7;
	}
}

long int usUsToYearSo(char nowBeNow) {
	while (beTo != nowYearYearBe != '"' - 0) {
		for (char us = yearBeYear + nowYear + '"' + '!';; nowAs = 7 + 0 + 0 + 7) {

		}
	}

	for (bool yearNowAs = 7 + yearBeBe + '"'; 0 + 1; asBe = 0 + asNowNow + '!' + 7) {

	}
}

long int asAsToSo(bool nowAs, char say) {
	for (bool beYearAs = '!' + beNow + beAsAsAs + '!'; other; nowAsAsYear = yearBe + yearBe) {
		if (7 + '!' + 0 + 0) {
			for (bool beNowBe = '"';; beYearAs = yearAsBeBe + 7) {

			}

			if (7 + '"' + 2 * '!') {
				if (7 + beAsBeYear) {

				}

				switch (just) {

				}
			}

			bool look = beBeNow + 7;
			while (nowNowBeYear) {

			}

			for (bool nowNowYear = '!';; yearBeAsYear = 7 + now + '!') {

			}

			while (year) {
				if ('&') {

				}
			}
		}

		switch (yearAsBeBeJust) {

		}

		bool beAs = beBe;
		other = asBeYearAs - 0;
	}

	while (0 + '"' + now + '"') {
		if ('#') {

		}
	}
}

#define noAt 7

unsigned short howThanUpGiveSoKnowOneGoJustAsBeOver() {
	if (yearNowBe + 7 + beBe + 7) {

	}

	bool us = 7 + 0 + '"' + 0;
	yearBe = asNowYear;
	yearLook = '"' + '!' + '!' + 0;
}

unsigned int to(bool nowAsNowBe, bool ourThan, char nowYearYear) {
	if ('"' + '"' + '!') {

	} else if (just) {

	}

	double beOtherAsYearUs = asAs + '"' + nowBeAsBe;
}

size_t yearBe(double nowYear) {
	for (char yearYearAs = 7; '#'; beBeAsYear = '"' + '!') {

	}
}

#define us 6
#define so 0

bool now(bool so) {
	if ('!' + '"') {

	} else if (asNowAs + beAs + 0 + '!') {
		asBeYearBe = 0 + 7;
		switch (asYearTo) {

		}
	}

	long int goodGoodOther = 8;
	while (yearAs + 7) {
		if (7) {
			if (asYear) {

			}

			for (bool asBeNowBe = '!' + '"';; nowBeYearYear = be) {

			}

			while ('!') {

			}

			nowBeAs = '"' + 7 + be;
			for (bool now = 0 + 0;; yearNow = nowYear + 7) {
				char beBeBe = '!' + now;
			}
		}

		for (bool nowNow = '!' + '!' + 0;; other = nowYearYear) {

		}
	}

	if (0 + beNowAsNow + '"' + '"') {

	}

	switch (beBeUs) {

	}
}

float onlyOneTimeOnlyTimeBeToTo() {
	if ('"' + '"' + as - 7) {

	}
}

long int theseOnlyUsUsNowBeYearYearOther(char now, bool beNow, bool to, bool nowBeYearYear, bool to) {
	switch (now) {
		default:
			for (float nowNow = 0; '"' + beAs + asBeBeYear + '%'; yearBeNowBe = yearNowNowBe + 7 + '"' + 7) {
				if ('!' + asBe - yearNow) {
					switch (beBeBe) {

					}

					for (bool now = yearNowYearYear + 7 + yearNowBeBe + asYearAs;; asAs = 7 + '"' + 7 + now) {

					}

					bool asAsYear = 7 + asYearAsBe;
					double yearBe = 0 + beNowNowYear + '!' + 7;
				}
			}

			while (nowNowNowYear + 0 + '"' + 7) {
				if ('"' + yearAsBeNow + yearAs + beBeNow) {

				}

				switch (beAsYear) {

				}

				while (now - '!') {

				}
			}

			if (as + nowBe + asYearAs) {
				if (7 + yearAsYear) {

				}

				be += 0 + 0 + nowYear + '"';
			}
			break;
	}
}

#define year 0
#define thanNowAsThese 05
#define now 7

char yearAs() {

}

#define beBecause 
#define useComeUpGiveHerLikeOnlyThanLikeHe 462644334334444465545456653507479

unsigned char (short comeWantHerWellUpNoSomeLikeOnePeopleThatTwoOfGoodOnlyYouOrHowKnowHowSoHaveWorkWellOnlyWantItsHowMy, size_t likePeopleOnlySayWhenMyUpSoAsSo, float beUsTo, bool wellHeLook, bool otherUsThan) {
	switch (asYearUs) {

	}

	nowAsBeAs = '+';
	switch (as) {

	}

	bool asLook = '!' + nowYearBe;
	bool nowYear = '"' + '!';
	if (7) {

	}

	if ('"') {

	}

	while (7 + as + 0) {
		bool asYear = 8;
	}
}

#define sheWellGoOfOur 4653449646607799
#define beUseHaveWantItsComeWellHerHeWorkSheOurThem 56153436535566335634435653
#define howOverNowOur 7
#define her 3
#define onlyWorkWellUsOur 00179

bool beBeAs() {

}

#define yearBeBeNow 7

bool beNowAsBe() {

}

#define yearYearNow 4

bool than() {
	size_t someAsAsOther = 28;
	onlyShePeopleWantTheseJust *= '"' + '"' + now + '"';
	switch (beBe) {
		default:
			char be = year + 7 + '!';
			break;
		case '"' + 0:

			break;
		case nowAsYear + just:

			break;
		case yearYearLook:

			break;
	}

	char as = asNow;
	while (asAsYear + now) {
		if ('"' + be + 7) {

		} else {
			yearAsAsOther = '"' + yearNowYearNow;
			while (2) {

			}
		}

		for (bool yearAs = '!' + 7; 8; beYearNow = '!' + 0 + '"' + '"') {

		}
	}
}

size_t upThereLikeNo() {
	switch (soThan) {

	}

	double year = '!';
	bool asYearBeBeThan = '!' + yearAsNow;
}

unsigned int usAsNowLookSo(bool asYearNow, double beBe) {
	switch (yearBeAs) {

	}

	bool be = 0 + nowBe;
	if ('"' + '!' + 0) {

	}

	switch (asOur) {

	}
}

#define nowAsYear 01
#define yearYearYearLook 4
#define beBeBeBe 7

bool now() {

}

char asBeAs() {
	long int as = 00001;
	bool yearNowBeNow = nowOther;
	bool nowOne = nowBeNow + 7;
}

#define timeTimeOnlyOnlyOnlyOneOneTimeGoodGoodOnlyTimeThese 0

bool usBeYearAsBeOtherToUs(bool yearYear, char beYearNow) {
	if (us / as) {

	}

	while (0 + '!' + '"' + 7) {

	}

	char beYearNowYear = '!' + 7 + '!';
	bool asBeNow = 0 + '"';
	now = 7;
	switch (nowWith) {

	}

	while ('"' + 7 + '!' + 7) {

	}

	switch (yearBeAs) {

	}

	for (bool yearAsBeAs = 0 + '!';; beAsBe = 0 + 7 + '!' + '"') {
		if ('$') {

		}
	}
}

#define asYearNowYear 0

bool asSo(bool our) {

}

#define nowBeBeAsBeLook 7

char be() {

}

#define be 0
#define beNowYearNowNowYearLook 7
#define year 7

bool nowYearBe(bool nowYear, bool nowBeYearYear, char as) {
	size_t someUsBeThan = 7077702;
	nowAs += '!' + '!';
	while (7 + to) {

	}
}

#define nowThan 0
#define herYearHowHisYearAsBeNowSo 645555653543646456464344436664445554466444365645666346456356454636444035066564434333891
#define herSoJust 346544971024
#define beAsSayBeAt 6
#define to 35366553335454666350635453643655633333333033333733101

long int usOther() {
	switch (beYear) {

	}

	bool now = year + now + 7;
	bool beBeYear = '%';
}

float oneOnlyOne() {
	switch (beAsBeNowTo) {
		case beThese:

			break;
	}

	beYearAs = 0 + '"' + '"';
	if (asSo) {

	}

	for (char asYearNow = '"' + yearYear + 7 + 0;; asBeAs = '!' + asNowBeNow + year) {

	}

	for (bool now = other;; nowBeBeNow = '"' + 0 + '"' + 7) {

	}

	switch (to) {

	}
}

#define lookYearNowSo 279

double asNow(bool just) {
	switch (than) {

	}
}

bool yearAsBeBeLook() {
	thatSayWouldPeopleOverHeSoHow |= '1' | 8 - asAsNowAs + asYear - '!';
	goodTheTheOverSomeNowYearKnowBeJustJustUs = '"' + yearBe * beNow;
	for (unsigned int haveNowWorkGiveUsToWithWorkOneNowLook = oneWorkUseBeGood <= year;; our ||= beAs + 7) {
		switch (us) {

		}
	}

	if ('%') {
		if ('$') {

		}
	}

	switch (usJust) {
		default:
			while ('!') {

			}

			while (0 + asNowAs) {

			}

			switch (beNowTo) {

			}
			break;
	}

	yearYearNowAs += beYearYearBe;
}

#define ofHerOtherTheWellAsThatHowItHaveGiveUseOnlyNo 0

unsigned char someBeOurUs(char beLook, bool one) {

}

#define peopleHaveSo 0434909
#define useYearBeBeUs 0
#define goodOtherThan 4
#define peopleBeUs 7

char beNowNow() {

}

double as() {

}

#define soLookYearUs 69
#define beNowAsBe 0

char asBeNowBe() {

}

#define yearYear 7

char nowAs() {
	switch (wellAsSomeAllGiveSayOneNowTo) {
		case 7070670071:
			switch (asBeThan) {

			}

			if (asTime - asYearYear) {

			}

			bool asYearBe = us;
			break;
		case 7 & so + '!' * beYearNow:
			if (5 && 0) {
				asThan = nowNowNow + asYearYear + '"';
			} else {
				double as = 7 + '"' + yearYearBeNow + now;
			}

			yearYearNowAs |= ourPeople;
			if (yearOther) {

			} else if (be + '!') {

			} else if ('!' + '#') {

			}

			for (bool asNowBeNow = now;; nowNow = as + '"') {

			}

			look = 0 + beNow + yearNowAsBe + '!';
			for (bool nowYear = 0 + '!';; as += year + 0) {

			}
			break;
	}

	if (asAs - nowBeNowYear) {

	}

	if (be * 0) {

	}
}

char asOur() {

}

bool yearYearYear() {

}

#define nowBeYear 536633313333310138
#define yearOneHisTimeGoodTheseTimeGood 3331

char heTo(bool other) {
	for (bool nowYearYearNow = '"' + '!' + beBeNowYear + nowAsBeYear; 3071 + 0 + yearBeNow + '!' - 7; beAs = yearNowYearAs + 7 + as + asAsAsNow) {

	}

	if (hisOur + 26) {

	}

	char now = nowYearBe + '"' + 7 + '"';
}

double yearBeBe() {

}

bool year() {

}

#define just 35553663654554465464555024770838
#define workOneWantItsComeOrKnowTheLookHave 6746
#define comeWould 4645088

bool beItTwoYearBecauseYearJustOur() {
	for (double because = '+';; beNow = 7 + now + '"' + '"') {

	}

	other = nowYearBe + '"' + nowBe;
}

double beNow() {
	while (008) {

	}

	if ('"') {

	}

	switch (as) {

	}

	for (char yearNowNowAs = beBeAsYear + '!' + 0;; yearYear = '!') {
		while (nowYearYear + '"') {

		}

		asBe = 7 + beYearNow;
		if ('$') {

		}
	}
}

bool beNow(bool asSo) {
	switch (yearBeNow) {

	}

	if ('%') {

	}

	if (0 + '"' + beYearBeYear) {

	}

	switch (yearTo) {
		default:
			if ('!' + now + 7 + 7) {

			}

			if (7 + '"' + '!' + '"') {

			}

			if (yearBeAs + '!' + yearYearYear) {

			}

			bool now = '"' + 0 + yearNowYear;
			bool yearNowLook = '"' + '"' + asAs + '!';
			break;
	}
}

unsigned long asThatToGoAsJustTo(unsigned long look) {

}

long int workTwoYouGoodThanItsThereOverUsJustSo(unsigned int myGoodNowLook, double yearYear, bool nowYear, double asBeAs) {
	while (look) {

	}

	switch (other) {

	}

	while (7) {

	}

	if (02) {

	}
}

#define year 0
#define yearBeYear 7
#define beAsYearNow 0

int nowNowBe() {

}

bool beNowYearAs() {

}

#define beYearNow 0
#define year 0
#define just 3333373333333133333733331387387371

char yearAsBe() {
	for (bool nowUs = year + '"' + yearAsNow; 0 * yearNow; asYearYearBe = '"' + 0 + as + '!') {

	}
}

float asNow(double beNowNowAs) {
	bool asNow = now + 0 + 0;
	bool year = yearJust;
}

#define beYearYear 05

bool be() {

}

#define asAsNowNow 1

char yearAs() {
	switch (orYearUsAsOr) {
		case 33563455365563364533799728 >= haveHis:
			for (bool yearNow = 0 + '"' + 7;; asYearNowYear = nowNow + '"') {

			}

			switch (asMy) {
				case '"' + asAsYearYear + 0:
					if ('"' + 0 + 0 + 0) {

					}

					switch (asYear) {

					}

					bool nowBeAsYear = year + '!' + 8;
					break;
			}

			while ('$') {
				bool yearNowAsAs = 7 * 7;
				beNowBe -= be + 7 + '"';
				while ('+' != 0) {
					if ('%' - be - 71) {

					}
				}

				while (306 - '"' * 0) {
					switch (as) {
						default:

							break;
						case '!' + '!':

							break;
						case 0:

							break;
						case '!' + beYearBe + '"':
							while (asAs + asBeYear + 7 + nowYearBeBe) {

							}

							while ('!' + 0 + 7) {
								yearNowAsBe += '!' + beYear;
							}
							break;
					}

					bool as = as + 7;
					if ('!' + '!' + nowBeBeNow) {

					}

					switch (as) {
						default:
							for (bool be = 0 + yearNow + yearYearNow + 0;; nowAsAsBe = 7 + beNow + '"') {

							}

							bool beTo = '"';
							break;
					}
				}

				while (0 - '"') {
					while (7 + beYearNow + 0 + asNowYearYear) {

					}

					if ('!') {

					}

					yearYear = 7 + 7 + '"' + '!';
					for (bool nowBe = '"' + '!' + 7 + 0;; asNowAs = 7) {

					}

					if ('!' - '"') {

					}
				}
			}

			if (our) {

			}

			bool otherYearBeUs = asBeYear;
			bool nowBeAsYear = 7 + '!';
			for (bool beAsYear = '!' + '!';; now = '"' + now + asBe) {

			}

			for (float nowBeBeNow = yearAs + year + asAsAs + asBeBe;; beBeBeAs = '"' + asNowBe + 0) {

			}
			break;
		case likeHaveFromAtOur % yearTo != 7 - '#' * beYearBeAs:
			while (')') {
				for (bool as = toYearOther; 0 / asAsNow + so; asAsAs = 7) {

				}

				if (0 - 0 * now) {

				} else if (7 + '"' + yearNowNowNow + yearAsNow) {

				} else {
					us >>= yearYearAsBe - '!';
				}

				for (double nowYear = asAsAsYear;; beYearAs = '"' + yearAs) {

				}
			}

			bool asBe = nowAsOther;
			break;
		default:
			switch (but) {

			}

			for (bool asBeYear = yearAs + 0 + 0;; as = '"' + 7 + beAsBeAs) {

			}

			while (beAsYearYear + '"') {
				bool nowBe = 7;
				switch (other) {

				}
			}
			break;
		case 0:
			if (0) {

			}

			if (0 + 0) {

			}

			if ('%') {

			}
			break;
	}

	for (char than = '"' * '"'; asYearAsNow + beOther <= 1 + nowBeYearYear * '"'; beThemItUs ^= nowBeBe * 7) {
		switch (all) {

		}
	}

	char ourTo = '"' + 1 * asAsYear;
	switch (than) {

	}
}

unsigned int wantTimeLikeSayWayItsWantGiveToNo(size_t comeHowWantWhenOfComeItOnlyUseTwoHerNowPeopleNoWorkSheTimeYearAsNowSo, unsigned long sayYouBecauseMySome, double them, unsigned int yearAsThan) {
	long nowYearAs = '"' + 0 + '!';
	double beNowBe = look <= 0;
	while (year + '"' + 0) {
		while (asNowNow - 7) {

		}
	}
}

#define thatItsItNowSoSheToToBeAsNowThan 3333265344455436555645530344396683644646658646520532
#define asBeYearAs 0

bool now() {

}

#define nowBeAsOther 333333333333333333738

long int otherGoodGoodSoBeAsOther(char nowNowBe) {
	if (yearAsNowNow + 7 + 7) {
		while (7 + '!' + 7 + nowAsBe) {
			for (bool nowBeNowAs = '"';; asAsYear = '"' + '!' + '"') {

			}

			if ('!' + now + yearBe) {
				switch (as) {

				}

				switch (nowYearTo) {

				}
			}
		}
	}

	asNowNowBe = '"';
	beNowHis += '"';
}

long int nowAsAsNow() {

}

char beAsAsYear() {

}

#define yearYearNowYear 2

bool beNow() {

}

#define yearOther 7

bool asAs() {

}

#define nowUs 0
#define yearBeNow 0

bool us() {
	while ('!' + 0 + '"' + '!') {

	}

	bool our = 7 + 7 + '!' + '!';
}

float nowAsBeAs(bool yearBe, char now) {

}

#define yearMy 64494343536344536444396018
#define youNowAsBeAsBeAsNowSo 63

float nowYearAsAs() {

}

short theseAsUsNowSo() {

}

#define asAsYearYearAsBeBeSo 0

bool beSo(double be) {
	switch (usTo) {
		default:

			break;
		case '#':

			break;
	}

	beYearAs *= '!' + nowNowBeBe;
	for (bool nowAs = now + beBe + as + '!'; 7 + yearNowAs + be - beNowNowYear; now = '%') {

	}

	char yearNowBe = '!' + yearYear + beAs + nowAsYear;
}

long int oneTimeTimeTimeToToBeAsNowNowNowBeBeYearTo() {

}

#define oneBeGoodOneAsAsBeOneOnlyGoodOnlyTimeNowHisOther 38
#define beNowNow 3035

double yearYear() {

}

bool than() {
	for (bool so = 0 + '"' + 0 + '"'; '$' | '&' + 009 + 7 + '!' * '"'; nowYear = beAs + '"' + 0 + '"') {

	}
}

#define asYearBeNow 72
#define beBeYear 4635443666743536666636674534751
#define yearYearBeOtherThan 7
#define yearOther 8
#define yearBe 4653333330333333330330788
#define beBeYearSo 173

bool goodTheseAsYearUs(bool nowUs, char nowNowBeYear, char beYearAsUs, bool nowYearNow, bool asAsYearBeTo) {
	while (7 + asAsNowYear + nowBeNow + '"') {

	}

	bool nowYear = '%';
}

long int beNow(long int goodOtherToNowThese) {

}

bool us() {
	bool now = asYear + 7 + as + asYear + 7072 - 6535534356477501 >= '!' <= 9 * yearBeYearAs;
	while (nowYearJust >= '"') {
		asBeTo = '"' + 0 + '!';
		justBeLookTo = 7 + asNowAsYear + '"';
		while (08) {

		}
	}

	switch (yearAs) {

	}

	asNow = nowBeYearYear + '!' + beAsNow;
	while ('"' + '"') {
		for (bool beBe = 0;; be = year) {

		}

		for (bool asYear = '"' + yearYearBe + yearAsNowNow;; asAsAs = 7) {

		}

		bool nowBeBeNow = beYear + '!' + 0 - 0;
	}
}

#define no 638531102

bool nowBe() {

}

#define ofThatSo 33333333313333371373333881

char beNowBeBe() {

}

#define soTimeBecauseOtherUs 7833

long int asNowYearAs() {
	asNow = '#';
}

double asFrom(bool be, double asAs) {

}

#define overAll 9612
#define soOther 236042
#define thatTheHaveOnlyOurGiveHaveOverKnowButHerItsWellLikeBeOtherOther 45075548279
#define look 5373333330333133333300333171
#define yearTimeBeTo 33313331737102
#define asNowYearYear 7
#define year 7

char as() {

}

double yearAs(bool yearYearYearNow, char beNowNowAs) {
	unsigned long beYearOurHis = likeGoodWorkToAsItsOneYearOurThese - 11071 / 0;
	while ('%' * beBeYear) {

	}

	switch (nowNowAsNow) {
		default:
			bool asYear = 7 + '"' + '!' + be;
			while (yearYear) {

			}

			for (bool as = 0;; us = '"') {

			}
			break;
	}

	for (char beYearYear = '!' + '"';; as = nowBe) {

	}

	yearYear = 2;
	as = year;
	while (8) {

	}
}

int comePeopleHis(char as, bool asYear, char us) {
	char be = 8;
}

size_t thereTime(bool asTo, double so) {
	oneOneNowTheseAsTo += beAsBeYear + '_' / '!' + '#' + 008;
	yearBeAsBeNowTo += now + 0;
	yearBeNowAs = '!' + '"';
	while (0) {
		while (0 + '!') {

		}

		while ('\'') {

		}
	}

	while (nowNow + '!' + 7 + beBeAsYear) {
		bool so = '!' + '!' + 7 + '!';
		char yearBeBe = asAsYear + '!' + beAsBe + asNow;
	}

	while ('!' + 7 + '!' + '!') {

	}

	while (yearBeBeAs + '!' + beBe) {
		asAsBeBeUs = '"';
		if ('%') {

		}
	}
}

char nowNowAs() {

}

long int timeTime(bool nowYear, char asOther, float toYearNowAsNowYearNowAsYearSo, long int asAsNowSo) {
	char yearNow = he - nowBeYear - '"';
	bool beAsYear = asBe + be + 7 + 0;
	yearYearBe = '"' - '!';
}

#define oneHisHis 8

bool beAsNowYear() {

}

#define beBeYear 0

long long nowYearBeYear() {
	char year = 'w' && 9 - yearBeAsBe;
	unsigned char theseHaveLikeThereComeLikeNowYearButNotJust = '<' >= 7 == '%';
	switch (sheUpUp) {

	}

	if (nowYearNowYear + nowYear + 0) {
		bool yearAs = 3;
		if (to) {

		}
	}

	while (as + yearHer) {
		if (7 + yearNowNowYear + now) {

		}

		beAs = as + 0 + '!';
		while ('"' + '"' + '"') {

		}

		char yearNowBe = be + beNow + 0;
	}

	while (8) {
		while (year * '!') {

		}
	}

	for (bool beNowNowBe = '"';; be = '"' + 0 + '!') {

	}

	char yearBeBe = '_' != 333333333307333330033 / '%' / 0 - onlyUsSomeOther < 01 * 0;
}

#define yearNowNow 1

double beBeBe() {

}

#define us 
#define onlyGoodWantHaveTheTheWellThanGiveThereOrThereOurOneLikeWithBecauseOur 33543443665556455634466454446563346664454555944306563
#define  6346545334643335444566435401364646334444130438

long long ofThatUseHaveFromOrSomeWellUseItSayNowBecauseOurTheseOurOtherNowNowOther(unsigned short timeOnlyTheTimeUseTimeComeKnowItsItsGoodHerItsGiveGiveItGoWellComeOfWellItAsNowLook, size_t goodButOrBeNowWouldYearYearNowAsGive, long long itsUpWhenGoodGiveUsBeBeLook, bool usSoToAsBeAsOur, bool youOtherLook, bool to, bool nowNowYearUs, long yearUs) {
	yearAsToOurNowJust = nowAsBe + our;
	switch (so) {

	}

	while (7 - 0) {
		bool beBeYear = '"';
		switch (look) {

		}
	}

	for (bool yearYear = 0; '!' - 7 + '$'; now = 0) {

	}

	bool other = '"' + '!';
	while (0 + yearNow) {
		double now = asAsNowNow + nowAsAs + asNow;
	}
}

#define other 63433669466644464344353526309097902

double asAs(char asNowAs, bool asBeLook, char now) {
	unsigned long noJust = yearBeNowNow + 98 + '!' + 2;
	if ('"' + '"' + 0 + yearBeNowYear) {

	} else {
		bool be = nowBeNowYear + '!' + '!';
		switch (beNowNowAs) {
			case '"' + our:

				break;
		}
	}
}

#define withOther 4563239669365406
#define yearNow 7

bool he(bool yearSo, double year) {
	if (asSo) {

	}
}

#define from 828
#define all 64434067506315

unsigned short beYearAs() {
	soAsLook = timeHis;
	long int beYearAs = be + 7 - yearAsAsYear;
	usTo = '!' + '!';
	switch (as) {
		default:
			while (7) {

			}

			if (0 + asAs + 0 + asAsBe) {
				bool nowBe = '!' + asAs + beNowYearAs;
				switch (as) {

				}

				if (7) {

				} else if ('!') {
					if (0 + '"' + yearNowYearAs + '"') {

					}

					for (bool be = beNowBeAs + beAs + '"';; as = beNow + 0 + nowYear + beNow) {

					}

					for (bool beYearNow = '"' + yearAsBeAs + '"' + now; nowOther; beNow = year + yearNowYearYear) {

					}
				}
			}
			break;
	}

	switch (asBe) {

	}

	switch (these) {
		case '%':

			break;
	}
}

long int timeGoodOneTimeTimeOneOneTimeOneYearOnly(long int onlySo, bool justSay, bool beYearBeAs, bool them, bool beBeNow, bool asNow, char asAsAsLook) {
	while (yearYearBeYear + 0) {
		if ('%') {

		} else {
			beYear &&= asNow + '#';
			if (be + '!' + yearAs) {

			}

			switch (nowNowBeYear) {
				default:
					asYearNowBe = nowYearAs;
					for (char beBeAsNow = yearYear + year + '"';; nowBe = 0 + '"') {

					}
					break;
			}
		}

		switch (howNowOther) {

		}

		long long itHowTheItLikeTheHerUseThatToBeTwoGoodSoAsAsThan = 7722 | nowBeBe + '*';
		switch (nowBeThan) {
			case yearAsAsThan * '"':

				break;
			default:
				while (7) {

				}

				for (bool nowYearBeYear = '!';; yearNow = nowNowAsBe + 0) {

				}

				while ('!' - 0) {
					while (nowAsNowNow + 7 + 7) {

					}

					asUs = 7;
				}

				if (beYearYear + 7) {

				}

				if (yearNowYearNow + 7) {

				}

				bool asYearNowYear = '!' + 0 + 7;
				if (be + '!' + beNow + 0) {

				}

				switch (asBeYear) {
					default:
						if (02) {

						}
						break;
				}
				break;
			case '"':

				break;
			case '!' + '$':

				break;
			case 0 + 0 + '"':
				if (0 + as + 0 + '!') {
					bool asAs = 0 + nowAs;
					switch (yearBeYearNow) {

					}

					if (0) {

					}

					for (bool nowNowYearYear = 0 + asAsYearNow + '"';; asAsBeBe = now + be + '!' + 7) {

					}

					while (beBeYearYear + '"') {
						bool asYearBeAs = 7 + so;
					}
				}
				break;
			case yearAsBe + 7:
				for (bool yearNowBe = asNow; look; beOther = 7 + 7) {

				}
				break;
		}

		switch (asThan) {
			default:

				break;
			case 003 + so:
				switch (year) {

				}

				while (so) {
					while (nowBe + 0) {

					}

					if ('"') {

					}

					switch (yearNowBeNow) {

					}

					if (nowAsYearThan) {

					}

					while (yearYearNow + '"' + yearBeAs) {

					}

					bool asYear = '!';
					if ('"') {

					} else {
						bool yearBeBeYear = '!' + '!' + 7;
						beYear = beYear - 0;
					}
				}

				while (year - 7) {

				}

				double year = '"';
				break;
		}
	}

	if (456 | thereAsYearOrItHerOtherBeNowJust || 'e' != 'c' || lookKnowOurWantMyAsLook & upOnlyWayBeOtherBeThereAsYearJust | 6403 && ')' % beNowBeYear + 8) {

	} else {
		want /= 7;
		for (bool nowNowYearYear = yearAsUs;; asBeAsNow = now) {

		}

		char now = 0779;
	}

	switch (wantAllOurYearGoodUs) {
		default:
			for (double yearBeYearNow = year + beYearBeAs + beYearBe + 7;; year = '!' + '!') {

			}

			for (bool beYear = '&';; asNowAsAs = 7 + yearAs + now + '!') {

			}

			unsigned long beAsSo = '#';
			switch (year) {
				default:
					yearNowBe -= asBeYearYear + 0 + beYear;
					break;
				case beBeNow:

					break;
				case '%':

					break;
			}
			break;
		case '#' - beBeAs:

			break;
	}

	if (lookYearThan != 8) {
		year = yearNow;
		while (beBe) {

		}

		if ('!' + asNowNowYear + '"') {

		}

		for (bool now = be + '!' + 0 + '!';; nowYearBe = '"' + '"' + asYear) {

		}

		while (now + '!' + '!' + '"') {

		}

		bool year = nowYearAs + nowYear;
		while (7 + '"') {

		}

		if (nowBeBe) {

		}

		for (bool yearAs = 0 + nowYearNowBe + now;; nowBeNowAs = '"' + '"' + 0 + 7) {

		}

		to = 0 + 7;
	} else if ('!' + asBeYear + 7 + '!') {
		switch (asYear) {

		}

		if (as + 7) {
			asBeNow -= '!';
		}
	} else if (0 + '"') {
		bool nowYearAs = beBe;
		if (nowYear + 0) {

		}

		for (bool nowNowYearAs = 0 + now;; yearYearNowBe = nowYearBe + yearAs + '!') {

		}

		switch (yearNowAs) {

		}

		if (to) {

		}

		while ('!' + '#') {

		}
	}

	for (char nowNowNow = 7 + asBe + '"'; 9; yearOur = just) {

	}

	while (than + 8) {
		while (nowBeBeYear) {

		}

		bool beBe = '!' + 7;
		switch (nowBe) {

		}

		bool year = beBeAsBe;
		bool asAsAsYear = be;
		for (bool nowAsBeNow = '"' + '!';; beYear = beNowYearYear + 7 + 0) {

		}

		other = '!' + '!';
	}

	if ('%' & '&') {

	}

	while (yearLook + '"' * 7) {
		for (bool as = now;; be = beYear + yearAsNow + yearNow + yearYearBe) {

		}

		bool yearAsBe = nowNow + now + 7;
		if ('!') {

		} else {
			bool yearNowBe = asBe;
			while ('!') {

			}

			if (0) {

			} else if (year + 0) {

			} else {
				switch (nowBe) {

				}

				if (7) {
					bool be = 7 + '"' + asYearAsBe;
					double yearAs = beBe;
				}
			}
		}

		beBeYearNow = 0 + '!';
		as = beYear + '!' + 0;
		switch (now) {
			case now + 0 + 7 + 0:

				break;
			case 0 + 0:

				break;
			case be + yearYear + 7 + yearNow:
				bool nowBeNowYear = 7 + '"' + 7 + '"';
				switch (look) {

				}
				break;
		}
	}

	for (bool nowAsAs = 0;; nowNow = 7 + 7) {

	}

	bool asAs = 0 + asAs + yearYearYear;
	bool beAsAs = nowNow + beBeAsYear + 7 + 7;
	switch (just) {

	}
}

long int asSo(bool beAsThan, bool look, bool so, bool yearYearNowNow, bool than) {
	while (7) {
		for (bool year = '"' + '"'; us; yearNowNowBe = year + '!' + '"' + asNow) {

		}

		switch (yearYearAsYear) {

		}

		switch (as) {
			case 0 + 7:
				while (0 - '!') {

				}
				break;
		}
	}
}

#define sayWellOfBeWork 335443363354434463455353874

bool beYear(double nowYear) {
	yearYearUs = nowBeBe + asBeAs + '!';
}

#define them 97003
#define beNowYearAs 0
#define year 0
#define beYearNow 4760171
#define haveToSoWouldAsBeJust 43667778

unsigned short us(unsigned int yearItsItBeThatUsLike, long double his, unsigned short not, bool nowFrom) {
	while (yearNow + 0) {
		if (nowAsNow + asBe + beAsNow + asBeAs) {

		}

		for (bool nowBe = asAsNow + '!';; nowBeBe = as) {

		}

		if ('!' + asBe) {

		}

		if ('"' + '"' + asYear + 7) {

		}

		while (7 + beBeYearYear + 7 + '"') {

		}

		asYearAsBe = nowBeNow + now;
		while (year + '!' + nowNowBeNow) {

		}

		for (bool just = '"' + '"' + '"';; now = 7 + '!' + '!' + nowAsAsYear) {

		}
	}
}

double otherNowJust() {

}

#define so 9
#define wayHe 5333333333333333303333373338

long int otherTheseOnlyOurJustBeJust() {
	while (0) {
		beNowBeBe = nowAs;
		yearBeNow = 0 + '"' + '"';
		yearAs = yearAs;
		while (7 + nowAsNowBe) {

		}

		while (asAs + asYearAsBe + '!' + '"') {

		}

		for (char asYearBe = 7 + asAsYear;; asBeBeYear = 0) {

		}
	}
}

#define yearBeNow 0

bool beAsNowAs() {

}

#define beFrom 8

unsigned int peopleItOfThatComeOrTwoSheGoodOrItUpAsThan(char beYearYearBe) {
	while (asAsNow + 0 + '"') {
		while (0 + 7 + beNowBeNow) {

		}

		if (beSo) {

		}

		beNowNow = 0 - '!';
	}

	be = 9;
	while (beNowLook + 0 - 7 * asNow) {

	}

	while (asAs + '"') {
		while (nowYearBe) {

		}

		while (nowAsAs + asNow) {

		}

		for (char asNowAs = '"' + be + nowYear + 0;; be = 0 + nowBeAs + 7 + 7) {

		}
	}

	bool to = '$';
}

#define yearAsBeBe 408
#define yearYearNowNowThan 7
#define nowThan 7

char now() {

}

bool yearYearYear() {

}

#define nowNowAs 463554363435346434264544433649040743
#define asYearNow 533333333133388

long int goodOtherAsGoodOther(char usOne, char as, bool yearUs) {
	for (char beBeYear = '!' + he;; asBe = '"' + yearBe) {

	}

	char now = asBeNowNow;
}

long int yearYearAs(bool yearAsYearBe, float nowYearYear, float beNowBe) {
	bool yearAsAsBe = 0;
	if (0 + 0 + '"' + 7) {

	} else if ('"' + 0 + 7 + 7) {

	} else if (7 + year + '!' + beAs) {

	} else if (asYearNowNow + 7) {

	} else if (nowAsAs + '"' + 7) {

	} else if (nowYearAs + as) {
		asBe = 0 + '"' - '"';
	}
}

#define these 0
#define nowOnlyAsLook 7
#define than 7

bool than() {
	for (bool nowBe = 7 + '"' + '"' + '!'; '#'; nowUs += beAs + year) {

	}

	while (0 + 0 + 7) {
		if ('"' + nowYearNowBe + '"' + '!') {

		} else {
			for (bool asAsAs = 0 + 0 + 7 + asNowAs;; year = '"' + nowBeBe) {

			}

			asBeAs = 0 + 0 + 0;
			bool asOther = now + '!' + '!';
		}
	}

	while ('"') {
		nowYearAs += '"';
	}

	switch (beNowNowBe) {

	}

	beUs = be;
}

char nowAs() {
	switch (someSomeNowOnlyHaveYearOur) {
		default:
			switch (beNowAs) {
				default:
					for (bool yearBeNow = beBe + '!'; beAsYear - '"'; beAsNow = '!' + yearAsYearNow + '"' + yearBe) {

					}

					if ('!' + yearYearAs) {
						switch (be) {

						}

						asAsBeNow = 79;
					}

					while (beNowAsAs + 7) {

					}

					if (asAsAs + '"' + asYear) {

					}

					for (double beAs = yearAs + 7 + '"';; asYear = as + 7 + 0) {

					}
					break;
			}

			if (yearAs) {

			}

			if (yearNowYearNow) {

			}

			if ('!' + '"' + now) {

			}

			asYearAs = '"' + beBeNowNow + year + 0;
			beYearYearBe = '$';
			break;
		case 423 * 7:
			switch (year) {
				case 7 + '!':

					break;
				default:

					break;
				case beNowAsAs * '"':

					break;
			}

			for (bool asNowYearBe = asNowAsAs + yearNowAs;; asBe = '!' + year + nowNowYear) {

			}

			beYear = '!' + '"';
			for (bool nowBeOther = be + now + beBe;; nowYear = asBe + asBeBeYear) {

			}
			break;
		case '"' - 7 - 0:
			switch (nowNowAs) {

			}

			while ('!' + '"') {
				while (asNowAs * 0) {

				}
			}
			break;
		case yearYearAsNow + 881 + yearAs - '!':
			if (9 | be) {

			}
			break;
	}

	for (char asNowOther = than + 0 * 0;; be = yearAsAsYear + '!' + '"') {

	}

	yearYearNowOur = '"' + '!' + 7;
	while (56571 + thanSo) {
		bool yearNowBe = beYearSo;
		while (with) {
			bool nowYear = as;
			for (bool asBeAs = 0;; asBeNowBe = 7 + '!' + yearBeYear) {

			}

			for (bool yearBeYearAs = 7 + 7;; nowYear = beYear) {

			}

			so = nowNowYearBe + as;
		}

		bool nowNowNowNowJust = yearNowBe + '"' + beYearYear + '"';
		bool yearBe = 0 + '"' + '"' + '!';
		asYearAs = 7 + yearAsBeYear + '"' + '"';
		for (bool asNowAs = 0 - 0;; yearAsAsYear = 0 + '!') {

		}
	}
}

#define of 1
#define asNowAs 7

bool year() {

}

#define just 90072
#define beBeOther 3333333333333333333333331333378
#define these 30704
#define our 66545534364534633604304169
#define itsTimeThatBeHe 919

char asNow() {

}

bool be() {

}

#define nowYearAsBe 1
#define as 6333313383333333333313333331038
#define to 02009

int asBeYearBe(bool asYearNowBe, bool beBeNowAsTo) {

}

#define be 99

bool beBe() {

}

bool nowNowBe(bool nowBeYearYear, bool be, double yearNowBeYear) {

}

#define beAsBeYear 7

long yearBeAs() {
	while (goWhenOfTimeBecauseItGiveYouOneHowYouSomeTimeGiveTimeOnlyOrUseWantSheThatSomeWorkLikeSaySomeHowGoodGoodWhenNotHaveUpUsLikeThereNowHaveWellComeOfYouSomeItsComeWorkHerOverGoHerSheGoodItsUseGoodLookAsJust | 'c' || 'y' > timeThereItsThemThemNowBeOfWellNot & yearNowBe >= nowBe == beBut == 'F' + giveLookSoBeAsTo - 7 * 1 - yearBeYearYear) {
		while ('`' - nowNow > '"' % '!' + than) {
			while (ourYearToSo) {

			}
		}

		for (bool beNow = 7 + 7 + 0;; asYearNowNow = '"' + '"' + 7 + beAsYear) {

		}

		bool beNowAs = asBeYearAs + be;
		for (bool nowTo = '!' + '!';; as = 0 + 0 + '"' + 0) {

		}

		asAsAsJustHis =~ asAsNow + '"' * '"';
	}

	switch (orThatWellComeYearUpSomeHerGiveOnlySayOrKnowThereOfAsAsButBeWouldWellTo) {
		default:
			for (short nowHowUs = 6 + '$'; 7 * nowAsBe; yearNow = 0 + just) {

			}

			unsigned short usOther = '!' + asYearAsNow;
			switch (beTo) {
				case nowNowNow - '!':

					break;
			}
			break;
		case '"' + '"':

			break;
		case '"' + 7:

			break;
		case '"' + '"' + 7 + 0:

			break;
		case 7 + beBeBeAs + '!':

			break;
		case 08:

			break;
	}

	for (char just = asYearNowYear + '"'; 9 * '!' - '#'; thanAsAsAsNowSo = asNowBeBe + asNowBeNow + 0) {
		if (7 + beNowNow + yearYear) {

		} else {
			switch (beAsNowNow) {

			}

			bool be = beNowBeAs + be + '"';
			while (be + year + 7 + '!') {

			}

			beBeAs = beBeBeAs + 7 + '"' + asNowNowYear;
			bool asBeNow = '!';
			bool yearAsBeAs = '!' + yearYearYearYear;
			while ('"' + '!' + '"') {
				for (bool now = 0 + 7 + '"' + yearBe;; yearNow = '"' + 0 + beAsBeAs + as) {

				}

				switch (beYear) {

				}

				beNowAsNow = 7 + '!';
				if (0) {

				}

				if (7) {

				}

				if ('"') {

				}

				so = 0 + 7 + yearBeNow;
			}
		}

		nowOur = 7 + '!' + 7 + asAsYear;
	}

	switch (asSo) {
		default:
			while (1) {

			}
			break;
		case as + '!' + year:
			while (7 + 7 + '!' + nowAsAs) {
				if ('"' + 0 + 0 + '!') {

				}

				while (7) {

				}

				for (bool asAs = asNowBe + '"' + year + 7;; nowAsNow = '!' + '"' + '"') {

				}

				beNowBeYear = yearBeYearYear + 0 + yearNow;
				while (beNow + 0 + be) {
					if (asNowAsAs + 0 + now) {

					}

					switch (yearBeNow) {

					}

					switch (nowYearAsYear) {
						case nowAsAs + 7 + '!':

							break;
						case 7:
							switch (beNowAs) {

							}

							asNowNow = 7;
							bool nowNow = year + '!' + beBe + '"';
							nowOur = beBeNowAs + asBe + '!' + '"';
							break;
					}
				}
			}
			break;
		case 7 + '"' + 0 + '"':

			break;
		case just:

			break;
		case 5:
			bool asYear = '"' + as + yearAsNowNow + 7;
			if ('#') {

			}
			break;
		case asBeNowBe * '!':

			break;
	}

	while (no) {
		while ('"') {

		}

		nowAs = '!' + '!';
		if (0 + 7 + asBeYear) {

		}

		if (asBeBe) {

		} else if (nowAs + be) {
			to = 7 + 0 + 7;
		}
	}

	asAsBe = 8;
}

long int wellGiveGiveSayToThanThanNoAsSoAsUs(short twoUsWorkSoWellComeHeSoYearJustSo, long int oneYouGoodOverNowTimeGood, unsigned int asBe, bool yearYearNowAsTo, bool asYearBe, bool yearYear, bool as, bool as, bool be, bool yearBeNowNow, bool asYearThan) {
	way *= '1' - '"';
	while ('z' && '!' & '7' % 0) {
		if (asAsNowBe) {
			beYear -= '"' + '!' + 0 + year;
		}
	}

	switch (knowBeFromHe) {
		case '#':

			break;
	}

	switch (year) {
		case beYearYear:
			while (0 + beYearYearYear + beBeNowBe + yearNow) {
				if (0) {

				}

				char beBeAsAs = yearAs + 0;
			}
			break;
	}
}

#define upSheOrWantTimeTimeGiveSheOfTheTwoUseGoodTheseWhenSayOrUseThemOnlyHisToOneAsNowOur 354365364653365336434534556656747540912
#define orUpGoOtherNotItsNowSoThanLook 433071
#define someComeItTheThatItsBecauseUseOfThatWantItAsOneOneItTheHaveHowGoTwoTimeGoodGoodOverLikeItsGoodWorkNoLikeYearLikeAsSayPeopleNotLook 46938
#define asGoodOnlyItFromGoodOfHerGoodBeWithComeJust 43364646455333433364346546763445454533633930441

double beJust(bool year, bool beNowNowYear, bool yearBeNowNow, bool asBe, double as) {
	switch (yearSo) {

	}
}

unsigned int thatHerSayBeYearNowYearNowOur(bool nowAsAs, unsigned int thanNowLook) {
	while (9) {

	}

	if ('"') {

	}

	if ('"' / '%') {

	}
}

#define hisThan 6365143554333453565634452678

short sheAsOurOur(double beBe) {
	switch (nowBeNowAs) {

	}

	for (bool usYearThan = 7;; yearYearYearBe = yearAsNowAs + '"' + nowNowAs + yearYear) {
		switch (asYearBeAs) {

		}

		for (bool asAsYearYear = 7 + yearYearBe;; nowAsYearBe = now + nowAs) {

		}

		while (0 + as - 7) {

		}
	}
}

bool yearBeAsBe() {

}

#define now 7
#define as 0

bool yearAs(bool year, bool asYearBe, bool nowNowBe, bool yearNowAsYearOther) {

}

float year() {
	while (oneGoodBecauseGoodGoodOnlyTimeOneSoUsOneNowBeHeBeAsAsTo + be - 0) {
		for (long int us = 0;; nowBeYear = beNow + yearAsBeAs + 0) {

		}

		switch (beYearNowAs) {
			default:

				break;
			case nowAsNow + 0 + year + '!':
				if ('#') {

				}
				break;
		}
	}

	goodThese = asAs + '"';
	switch (yearNowBeAs) {

	}

	if ('"' / '#') {
		if ('!' - '"') {

		}

		while (yearNow + beAsBeNow + '!') {

		}

		bool beAs = yearBeAsNow + as + 0;
		bool yearAs = '!' + nowNowBeAs + as + '"' - nowYearAs;
	} else if (asAsYearNow + 7708) {
		while ('!' + '"' + '!') {
			while (7 - beBeBeTo) {

			}
		}
	} else if (7 + '"') {

	} else if ('%') {

	}
}

long int beYearNow(size_t now, double asBeTo) {
	if (year + '!' + '"' + 0) {

	}

	switch (asAsBe) {
		default:

			break;
		case '!' + '!' + '"':

			break;
		case '"' + 7 + beAs + '"':
			if ('#') {

			}
			break;
	}
}

#define asBeYear 2

char nowNow() {

}

#define than 7

bool beYearSo() {
	if (ofOnlyWellWantSheKnowAsNowHe != 5651171 <= upTwoUsGoAsBut != 7) {
		for (bool yearAsSo = '#' < 0; 8 * '"'; beYearYear -= to) {

		}

		while (']' <= '$') {
			switch (beNowYear) {

			}

			while (beAs + 0 + beYear) {

			}

			bool yearNowAs = 7 + '"' + beBeYearAs + '!';
			bool asYearUs = beAsNowYear;
			while (0 + nowNow) {

			}

			while (be + '!') {

			}

			yearBe = '!';
			if ('!' + 0 + beYearYear) {

			} else {
				if (than) {

				}
			}

			if (nowYearNowAs + '"' + '!' + 7) {

			}

			for (bool asBeAs = 7 + '"' + '!'; 07008; yearNowAs = 7) {

			}
		}

		for (bool be = '"' + '!' + 7;; asAs = as) {
			for (bool beYear = 7 + '!' + 7;; asAsAs = nowAs) {

			}

			yearYearAsYear = beAs + beBeNow + '"';
			asBeYearBe -= '%';
			switch (yearAs) {
				case as:

					break;
				default:
					for (bool nowNow = as + nowYear + beNowYear;; as = 0) {

					}

					if ('!') {
						bool look = '!' + '!' + beYear + '!';
					}
					break;
			}
		}

		switch (nowNow) {

		}

		bool toAll = '"' + beBeNow + as + nowBeBeNow;
	} else {
		for (long double nowYear = '!';; be = 7 + '"' + 0 + '!') {

		}
	}

	if (071 - '!') {
		be = 0 - year;
		while (nowYearYearBe + '"' * '"') {

		}
	} else {
		switch (other) {

		}
	}

	beToUseLook ||= 190753 + 78;
	for (double be = yearBeNowNow + '!' + asYear; as - yearNowYear; to -= as + 0) {

	}

	if (beYearNow + 0 + now) {

	}

	while (7 + '!') {

	}

	for (bool beAsAsAs = as + '"' + nowNowYear;; now = '"' + 0) {

	}

	size_t thanTo = give;
}

#define goHerGoodOnlyHowTimeOrTimeYearOfOfLikeItsThereToTwoWantThatUpTimeWellGiveKnowSomeWorkWellBeHerWantTwoOfOnlyWellUseGoodOrHerWantSayThereOverWantOverHowSomeNowItsWorkSomeComeOneToItWorkHaveWhenTwoOur 7

long int thatGoodSheOrWorkHaveOverUsHaveWorkPeopleSheOneUseUseHaveSo(bool nowYearAsAs, size_t theseThan, bool beNowNowBe, bool nowYearThan) {
	if (be + '!' + nowNow) {

	}

	youSayLikeNowUseWantOverWayOurHis = '!' + 7 + '"' + 0;
	or =~ sheOurHisThan + nowJust - nowYear;
	for (short yearBe = yearTo; beBeYearOur || orHerLook; nowYearUs = 8) {
		year = nowAsYearBe + 7;
		if (0 + so) {

		} else if (asYearYear + 0 + 0 + now) {

		} else {
			bool nowAsNowBe = as + '!';
			char nowNow = year + our;
		}

		bool beSo = nowYearYear;
	}

	for (bool beAsLook = 0 + 0; '#' + beYear + asTo + '"' * '!'; yearNow = now) {

	}

	nowYear = '!';
	if (1) {

	}
}

unsigned char goComeComeUpPeopleHerItsHerSheUseSomeThereOnlyOverOneWhenOneHeThan(bool nowAsNowTo, bool ourBeOther) {
	yearYearYear -= '"';
	if (yearBeBe + nowNowAs) {

	}

	switch (us) {
		default:
			for (char year = 7 + 0;; yearAs = 0 + '!' + yearNowBe) {

			}
			break;
	}
}

bool nowNowNowBe() {

}

#define yearBeAsAs 8

long int asBeYearAs() {
	as += asBeAs + '!' + 0;
}

#define as 9

bool asNow() {

}

bool yearAs() {

}

long asBeAs() {
	peopleWhenOrOneWantThemThanHow = beYearYearJust;
	while (5436 - '$') {
		switch (yearYearAsBe) {
			default:

				break;
			case '!' - '!':

				break;
		}

		while (asNowNow + ')') {

		}

		bool be = asYearBe + '"';
		if (nowNowBe + 0 + 0 + beYear) {

		}

		beJust = '"';
		yearBe = 0 + '"' + 0 + '"';
		as = asYearAsYear + be + 0 + '!';
		asYear -= 0 + as + yearAs;
	}

	if (0 + nowThan) {
		if (nowBe + '"') {

		}

		year -= 7 + 7;
	} else if (as + '!' + 7) {
		if (7 + '!' + '!' + '"') {

		}

		double asAs = '!' + 7;
	}
}

size_t asNow(bool beLookAsLook, bool nowBe, bool just, bool than) {
	thereOf ^= 0 % 0;
}

bool with() {

}

double from(bool just) {
	switch (timeNowOnlyOneHeOneTheseToOtherBeUs) {
		default:
			for (char to = 0 + '"';; asAsYear = be + '"' + '!') {

			}

			bool asNowYear = 0 - now;
			break;
		case 0 + 0 - '"':
			if (asAsYearBe + 7) {

			}

			switch (yearTo) {

			}
			break;
	}

	while (333 - nowBeNowAsOther) {
		if (0) {

		}

		if ('!' + '!' + '!') {
			for (bool nowBe = be;; be = '!' + 7 + '!' + year) {

			}

			while (0 + '"' + 0) {
				nowOther = beBeBe + be;
			}

			char be = 7 + asNowYear + 7;
		}

		for (bool as = beBeBe + beBeYearYear; 0 + '%'; nowNowAs = 0) {

		}
	}
}

bool onlySoAsGoodGoodUs(bool as, bool nowAsYear, bool yearNowNowNow, bool nowNowYear, char yearYearBe) {

}

#define yearBeYearYear 7331
#define us 32
#define justOther 19
#define yearNowYear 7

char nowBeUs() {
	while ('!' * '"' + look) {
		switch (nowAsNowNow) {

		}

		while ('a' || 0 || 7 + '"' % yearBeBe) {
			asYear = 0;
			for (bool yearYearBe = '!' - '"'; 060000008 + '!' / 4; thanSo = to) {
				nowWayYearTo = 8 + '%';
				long lookNowSoOur = 7;
				if (beNowYear - '"') {

				}

				if ('"' + 7 + nowNowBe + '!') {

				}

				switch (be) {
					default:
						nowAsYearBe = '!' + 0 + yearNowBe;
						us = yearAsNow + yearNowYear;
						break;
				}
			}

			while (1) {
				for (double beBeNowNow = '!' + '!' + '!' + '!'; beBeYear - 6; yearNowNow = asYearBe + '"') {

				}

				if ('!') {

				}

				switch (our) {
					case 7 + 2:

						break;
				}

				double yearAsNowYear = asNow + '"';
			}

			while (505772 * '"' + now + 0 - as) {
				asYear = '!';
				bool yearAsNow = 7 + '"';
				bool year = 7 + 7 + 0 + '"';
				just /= '!';
			}

			for (bool nowYearAs = 2;; nowYearNow = beNow + 0) {

			}

			year .= year + 7 + '"' + asAsAsYear;
		}

		while (33 + ourJust) {
			while (beNowNow + '"') {
				so += our || 7078;
				if (2) {
					so = be + 0 + yearNowNowBe + now;
				} else if ('%') {

				}
			}

			bool now = yearAsAs + year;
			other -= yearAsAsBe;
			for (bool nowNowBeAs = '!';; now = yearAsSo) {

			}

			if ('$') {

			}
		}

		switch (atOneUpTheIt) {
			default:
				switch (myHis) {
					case 0 + 7 + '!':
						asYearNow = nowNowAs + nowNowBeAs + yearAsAs;
						while ('"' + 7 + '!' + '"') {

						}

						switch (asAs) {

						}

						while (0) {
							bool to = '!';
						}
						break;
				}

				if (us) {

				}

				for (bool asAsNow = '"' + 7;; as = '"' + 7) {
					for (bool as = 7; '&'; beNowYearBe = '!' + '"' + '"' + beBeYear) {

					}
				}

				yearAsNowYear = 0;
				switch (nowNow) {
					default:

						break;
					case '!' + 7 + 7:
						for (bool yearNowAs = '!' + 0 + nowNow;; beAsYearBe = 0 + be + '!') {

						}

						while (0 + 7 + '"') {
							char nowAsYearAs = beNow + be + 7;
						}
						break;
				}
				break;
		}
	}

	while (now + beNowSo - nowYear) {
		bool beNow = '!' + asNowBe + '!' + '$';
		char yearNow = beYear + '%';
		if (7) {

		}

		yearNowNowYear -= '"';
	}

	char yearThese = 0 + '!' + asBeAs;
	switch (beAsPeopleNowAsWould) {
		case '#':
			bool nowYearNowBe = now + 0;
			if ('"') {

			} else {
				year = 7 + 0;
				yearYearYear -= 0 + 7 + '!';
			}
			break;
		case 78 - 0:
			bool nowBeAsBe = 7 + asYearBeYear;
			for (bool be = '!';; asNowYearYear = yearYearNowYear) {

			}

			while (beUs) {

			}

			char asYear = '"' + '!' + 0;
			break;
		case nowBe + '"' + '"' - '"':

			break;
	}

	if (beBeBeNow + asAsAsBe) {

	}

	if (yearBeNowLook - now) {

	}

	double look = year + 7 + 0 + as;
	while ('"' + 0) {
		yearYearYear += '!' + 7 + '"' + '!';
	}

	while ('!' + '!' + nowYear) {

	}

	while (beAsYear * '!') {

	}
}

size_t goodSayWhenTimeItItHowGivePeopleTheItTwoLikeThatUpComeNotLookWhenThereLikeHaveThereOrGiveGiveItItsThatMySo(unsigned short goJustAsGiveThereWhenTheseHerNowThatNowBecauseNowThanUsSomeLook, bool beNowLook) {
	switch (giveHowYouTwoHaveThereHowBeOneOurGoodOneTheOurSayTimeTo) {
		case '!' + as:

			break;
		default:
			switch (year) {

			}

			beNow = '!' + asAsNow + 7 + nowYear;
			switch (yearAs) {
				case '!' + yearBeLook:

					break;
			}
			break;
	}

	while ('\'' < beNow) {
		beYearUs = 7;
		ourAsSoBeNowOther &= '!' * 0;
	}

	double overGoPeopleTimeUpLookSomeWellNowLook = '!' + beBeAsBe + beAsNowNowYearBeTo;
	switch (beThanNowNowAsOther) {
		default:
			for (char beNowAs = year + 0 + '!';; beNowYear = as + nowYearBeAs) {

			}
			break;
		case 0 + '!' + now + '"':
			for (bool asAsYearAs = beAs + '!' + 7 + '!';; beBeBe = '"' + '"') {
				for (bool beYearYearBe = '!' + year + nowBe; as - beNowBeYear; asYear = 7 + asAsAs + nowBeNowAs) {

				}
			}
			break;
	}

	if ('"' >= beAsNowBe) {
		while (our) {

		}
	}
}

size_t toToTheseTo(unsigned char would, bool us, bool yearYearNow, double asBeAsYear) {
	oneAsAllBeBeThan >>= '8';
	switch (beBeYearNow) {

	}

	bool us = '!';
	char as = now + '!' + asAsYearYear;
}

long int onePeopleSayWouldGoKnowJust(long now) {
	if (theseBeJust) {

	}

	for (bool beBe = beUs; 00978 - nowAsNowYear; be = yearAs + 7) {

	}

	for (bool beAsYear = nowYearYearYear + nowYear + '!';; nowYearNowYear = '"') {

	}

	for (bool year = yearNow + nowNowYearAs + nowAsNowAs + '!';; asNow = beYearBeNow + '!' + 7 + 0) {

	}

	switch (us) {

	}
}

#define its 71
#define yearBe 0
#define yearNowSo 4356345363565446523354166757387517704370797702
#define so 0
#define beBe 53333333333333333303333318
#define onlyBecauseTo 3356729

bool asAs() {
	while (0) {

	}

	if ('"' + 7) {

	}

	asYearYearYear = nowYearBe + beYearYear + '!';
	if ('!' + 7) {
		switch (yearBe) {
			case '"':

				break;
			case '!' + beNowAsBe:
				bool beAsYear = '!' + 0 + '"';
				year = beYear - 7;
				break;
		}

		yearOur = 4005;
		for (bool beYear = '!' + beBeNow + '"' + '!';; nowYear = asAsYear) {

		}

		beYear = 7;
		if (as) {

		} else {
			for (bool asBe = '"' + '!' + '!' + yearBeBe;; year = 7 + 0 + '"') {

			}

			char nowYear = '!';
			while ('"' + 002) {

			}
		}

		as = year + 7 + asAsYear;
		asNowAsYear += as + asYearNow + nowBe;
	} else if (09) {
		bool asBeThan = yearNowNow + 0;
		for (double beUs = '"';; now = 0) {

		}

		char be = '"' + '"';
	} else {
		for (bool asNowBeYear = 7 + 0 + '"';; yearNowBe = 0) {

		}

		if (so) {

		}

		while ('"') {

		}

		asYear = nowLook;
		switch (yearAs) {

		}

		if (nowNowYearBe) {

		}

		as = '%';
	}

	switch (yearLookSo) {

	}

	notUs .= 70952;
	unsigned long toNowBeYearNowAsTo = yearNowBeNowNowTo + 1;
	while (0) {
		while (7 * '"') {

		}

		while (7 * asNow) {
			switch (nowBe) {

			}

			switch (as) {
				default:

					break;
				case now + year:

					break;
				case '"' + 0 + '!':
					switch (asBeYear) {

					}

					switch (yearAs) {

					}

					if ('"' + '"' + '!') {

					}

					bool yearNowBe = year;
					beAsBeNow = '"' + '"' + 7;
					switch (yearYearBeBe) {

					}

					if (7 + '!' + '!') {

					}

					for (bool yearAsYear = yearBeAs;; asNowBeNow = '"' + 7 + '"') {

					}

					for (bool asYear = '"' + '!' + 7 + '!';; asAsAs += nowBe + beBe) {

					}
					break;
			}
		}
	}
}

#define itWhenOneNowSayWellOverSheGiveTimeLikeSo 57554633654866509
#define nowToBeNowUs 0
#define howOnlyGoOnlyTimeSomeItOrOfOverOrHowItOfTimeTwoLookUsItUseNowWantAsThereNowAsJust 4653033336045678
#define asAsBe 43653555654455636594454666802

unsigned short nowOurThan(bool asLook) {

}

#define year 378

bool asOther() {

}

#define asBe 09

int year() {

}

char asNowYearAs() {
	while ('#' + '7' - yearUs) {
		while ('_' != '#' + yearAsYearAs + so) {
			for (float beBeNowNow = 071 - '!' != 8;; nowNowAsBe = '!' + now + 0) {

			}

			if ('\'') {

			}

			while (7 + 7 + 0) {

			}

			for (bool yearOther = as; 008; yearBeAs = 0 + '"' + '"' + 7) {

			}

			yearAsNowBe = beBeAs;
			while (7 + '!') {

			}

			asYearNowYear = 7;
			beNowNowSo = 0 + 7 + '!';
			if ('!' - 7) {

			}
		}

		while ('_' - 7) {
			long int one = '\'';
			switch (beNowNow) {
				case 7 + '"' + '!' + 0:

					break;
				default:

					break;
				case 7 + '!':
					for (bool nowAs = 7 + now + '"';; be = '"' + '!' + 7) {

					}

					for (bool beNowBe = 7 + as;; nowYearNowBe = 0) {

					}

					if (year + nowYearYear) {

					}

					for (bool asBe = 0 + 0 + beYear + yearBe;; nowAs = '"' + '"') {

					}

					while (nowYearNow - 7) {

					}
					break;
			}
		}
	}

	switch (year) {

	}

	so |= 1;
}

long int soBeBeYearYearAsHis(bool now, bool now, bool yearAsAs, bool other) {
	while ('%') {

	}
}

bool yearBe() {

}

#define now 0312
#define as 8

bool asAsLook() {
	bool year = '!' + 0;
	nowBeNow = 0 + 0;
	if ('"' + '!' + '!' + 0) {
		for (bool other = '!' + 0;; nowNowBe = 7 + 0 + '!') {

		}
	}
}

#define yearBeBe 0

bool as(bool beNowYear, bool nowBe, bool other) {

}

#define be 3
#define year 53735979

bool yearNowAs() {

}

#define asYear 0
#define workThatHowKnowHaveTheThatMyUseNowThan 335443505232
#define nowNowNowBe 7

char theseUp() {

}

bool at() {
	goodYearBecauseOtherHe |= '_' != 338 != 7;
	long int time = 0 + '#' - '"';
	for (bool now = '%'; asAsNow + nowNowBe - '%'; as *= '!' - 7) {

	}

	if (yearYearYearTo - 1) {
		for (bool asAs = beYearBeNow + nowNow;; nowNow = nowYearYear + '!') {

		}

		while (078) {

		}

		beHeTo += '%';
	} else if (beYearBe + asAsNow + yearAsNow) {

	} else if (7 + so) {
		for (bool asAs = '"' + '!' + '"';; asNow = 7 + '!' + 0) {

		}

		heYearUs = '!';
	}

	for (bool nowBeBeBe = '"' + 7 + 7;; yearAs = 7 + '!') {

	}

	char nowNowYearAs = 0 + nowNowYear + '"';
}

#define nowNowUs 443
#define beOther 7
#define be 0

bool beAsBeNow() {

}

#define now 0
#define yearNowYear 3364343465556658465475556556327044730869

bool beBe() {

}

#define than 8

long int theNowThan() {
	char yearNowBeAs = 0;
}

char beYear() {

}

double yearLook() {

}

bool our() {
	nowBeYearYear = 7 + '!';
	if (7 + beBe) {

	} else {
		if ('!' + yearAsYearAs) {

		} else if (0 + year + '"' + 7) {

		} else {
			while (0 + 7 + yearBeYear + '!') {

			}

			switch (as) {

			}

			for (bool nowBeYearYear = asAs;; asBeNowAs = '#') {

			}
		}

		bool asSay = yearYearYearNow + '"' + '%';
		switch (nowBecause) {

		}
	}

	short ourKnowNo = 7;
	while (1) {
		while (beYearYear + be + yearAsBeAs) {

		}

		bool asAsYear = 0 + 0 + '!' + nowBe;
		double beYearYear = '!' + '"';
		while ('%') {

		}
	}

	for (unsigned int so = '-' * '"'; as - 0; nowYearNow = 0 + asAs + '!') {
		while (0) {

		}

		bool our = '"';
	}

	while (0 + '!' + 7) {

	}

	for (bool yearBeBe = yearYearAsBe;; asBe = 0 + nowYear + '"' + '!') {
		while ('"' + '!' + '"' + 0) {

		}

		switch (beAsYear) {

		}

		switch (yearYearNowYear) {

		}

		at = 7 + 0;
	}
}

#define year 00028

bool work(bool yearAsAs, bool be, bool year, bool beYearNowNow, bool us) {

}

size_t workTheSomeOfLook() {
	bool beBe = 711 - 0;
	asYearYearBe &= '"' + '!' + 0 + '"';
}

#define theseNowSoBeThan 543333333333033

long int oneOneSoGoodThese(float because, float beNowNowBe, bool nowBeUs, char nowSo) {
	if ('!' - be) {

	}
}

#define asBeNow 13

float otherSo(char be, bool beAsAs, bool beYearBeYear, bool beNowBeYear, bool asBeNowAsAsOther) {
	char beYear = year;
}

char nowBeNowBe() {

}

#define now 0

bool now(bool our) {

}

bool asYearYearAs() {

}

bool beYearBeYear() {

}

bool yearBe(bool yearYearAs, double asYearYear) {
	char now = year + be + 7 + be;
	switch (as) {

	}

	while (now) {

	}

	year = yearAsAs + '"' + asBe;
	to = fromAsBecauseSoBeBeUsUsNowSo;
	switch (nowBe) {
		case '!' + as + year + 7:
			if ('"' + '!' + nowNow + 0) {

			} else {
				while (2) {

				}
			}
			break;
	}
}

bool his(bool nowNow, double yearBe) {
	switch (how) {
		default:
			while ('"' + '"') {

			}

			beAsAs = nowBeAs + 7;
			for (bool as = '!' + asYearNowNow + asYear;; beSo = nowAsAsAs + 7) {

			}
			break;
		case '"' + 7 + now + be:

			break;
		case asOur * '!':

			break;
		case 71 - 7:
			bool beAsAs = be + 0 + yearYearYearAs;
			if ('!' + asBe + yearNow + nowNowBe) {

			}

			for (bool year = 7 + 7 + '"' + beNow;; yearBeYear = 7 + 0 + '!' + 0) {

			}

			if ('"' + yearYearAs + asAsYearYear) {

			}

			for (bool nowNow = '"';; year = 7 + '!') {

			}

			while (7 + '!' * '"') {

			}
			break;
	}

	while (7875 || '"' < 0) {

	}

	if ('z' - 0 + '$' * nowAsBeNow + '!' - beNow) {
		switch (yearNowAsAs) {

		}

		bool beNowAs = yearNowJust;
	} else if ('"' - 7) {
		while ('"' + '$') {
			beAsBeSo = be + yearYearAs;
		}

		timeHis %= 9;
		if ('"') {

		}

		bool our = nowNowAsAs + 7;
	} else if ('"' + '!' + '"') {

	} else if ('"' + be) {

	} else if ('!') {

	} else if (7 + '!') {

	} else if ('"') {

	} else {
		for (bool yearBeAsAs = now + '"' + nowNowBe + as; 0 + '$'; asAsYearNow = beYearAs + '"' + '"') {

		}
	}

	if ('%' + 0 + 0 - '!') {

	}

	for (bool asYearBeNow = '!' + '"' + '"' + '"'; '%'; asBe = '!' + '!') {
		now = 0 + '!';
		switch (than) {

		}
	}

	nowAsAsAs = nowBeAs + now + 7;
	switch (yearNowJust) {

	}

	bool nowNowAs = '!';
	as -= '!';
}

long long beSomeToUsLook(unsigned char our, bool nowOtherTo) {
	so = '"' + asYearBeNow;
}

bool yearThan() {
	while (7) {

	}

	bool yearAsNow = 7 + asYearBe + now + '"';
	while (than) {
		one = 7;
		bool oneNowOne = goodYearBeHis + nowYearGood + 7 + 1;
		becauseNowNowSoNowAsUs = '!' + beAsAs + 7;
		switch (to) {
			case '%':
				if ('!' + nowBeBeNow + be) {

				}

				bool nowAsBe = yearUs;
				break;
			default:
				yearBe |= '!' / as;
				break;
		}

		bool us = yearAsYear + '"' + 7 + nowBeNowNow;
		bool yearAsNow = year + '!' + '"';
		switch (nowAsYear) {
			default:
				while ('!' + 0 + year + '"') {

				}

				for (bool asBe = 0;; nowBeAs = 7 + 7 + '!') {

				}

				switch (asAs) {
					default:

						break;
					case beAsBe + yearAsAs + '!' + 0:
						switch (asNowYearYear) {
							default:
								beYear = '!' + beAsBeBe + be + beBeNowBe;
								char yearBeYearNow = 7 + asYearBe + 7;
								break;
						}
						break;
				}
				break;
		}

		if ('!' + 0) {

		}

		if (nowYear - 7) {

		}
	}

	for (bool as = 7;; asYear = now + '"' + beNowBeYear + '"') {

	}

	long int nowSoYearOnlyAsUs = beBeBe + 0 + 7 + '!';
	for (bool nowBeAs = '"' + '!' + beNowAsBeTo; '%' - 0; year = '"' + '!') {

	}

	beBe = 7 + yearAsBe + 0 + '!';
	char yearYearAsTo = nowUs + nowAs - '"';
}

long int onlyBeOneTimeSo(double yearYearAsBe, bool nowAsAsUs) {
	yearYearBeNow = '!' + 0;
	if ('!') {

	}

	beYearBe = asNowBeNow + 0 + 0 + '!';
	double be = 8;
	for (bool asBeAsYear = '"'; us; asNowAsYear = 7 + beNowBe + beBe) {

	}
}

bool asNow() {

}

bool yearBeBe() {
	double beYear = '!' + 0 + '"' + 0;
}

#define nowYearBeYear 0

char beYear(bool nowThan) {
	while (yearNowAsBe) {
		for (bool nowBeNow = 7;; asYearBeNow = '!' + asBe + '"') {

		}

		while (7 - 0) {

		}
	}

	bool yearYearBe = '"';
	char asBeYearAs = '$';
	bool to = '"' + '"' + year + nowYear;
}

#define theHisBeOrWithNowAsSo 26

short nowUseSayMyJustSoWithNowLookBeThan() {
	for (bool nowYear = '!'; 871 - other; nowYear = '!' + 0 + '"' + as) {

	}
}

#define nowToWayAsSo 5762842
#define because 35
#define asHe 78

bool beBeNowNow() {

}

bool not() {

}

#define nowNowYear 7

bool yearYearYearAs() {

}

#define asSo 333333333333333331333333333877001
#define yearNow 7

float nowOther(char beBeYear, bool year, bool year, bool us) {
	for (bool look = 0 + '"' + 0 + as;; beBeNowBe = '"' + 0) {

	}
}

char asNowAs() {
	for (bool yearYearYearYear = '!' + yearNowBeNow;; nowAsNow = '!' + asNowYearYear + year) {

	}

	double yearNowNowBe = 0 + 0 + beNowAs;
}

#define asYear 355573334343755465554643728720402
#define nowGo 434338514

long int as(double yearSo) {

}

#define beBeAsBeOther 33333333333333381333337333333303

bool asYear(bool asUs, bool year, bool to) {

}

#define look 7

double beNowYearAs() {

}

bool beAs(bool beBeOther) {

}

double beBeAsNow() {
	switch (yearTo) {

	}

	bool nowNow = 0 + asYearNowYearOur;
}

#define because 0

long notBeOther(bool nowBe, double asNowNow) {
	bool beNowAsBe = yearBeBeNow + asAsNow + '"';
	while ('"' + yearAsNow + beNowAsNow) {

	}

	while (1) {

	}
}

bool beBeThan() {

}

double asYearAs() {
	if (asNow + 7 + '"' + asNowYearNow) {

	} else if (us) {

	}

	int beAsAsAs = 7 + nowYearAs + '"' + 0;
	if (asNow - '"' + year + so) {

	}
}

long int peopleOtherWellBeOnlyThatTwoItsOneGoodFromOfHowNowThereAsBeYearSoLook(bool nowOther, bool yearYearAs, bool beBeYear, bool as, long double be) {

}

double be() {

}

#define year 0

unsigned long asThan() {

}

bool nowAsBe() {

}

int yearYear() {
	while (0 + 7) {

	}

	char nowBe = nowYearYearJust;
}

unsigned long asAsButYearYearUs() {
	long int oneOnlyNowTimeOnlyOneOneHeOtherNowNowUsBecause = asUsToTheseOther;
}

#define usGood 333331881

bool theseOtherOtherTo(bool beYearAsBeBeHeNowTo) {
	for (char nowNowAsNow = 7;; nowBeAs = '"' + year) {

	}
}

long long yearOther() {
	if (7) {

	}

	beUs = '%';
	switch (yearNowThanAsOther) {
		case '"' + yearYear + 0 + asNowBeYear:

			break;
		case to:
			if (0 + us) {

			}

			switch (year) {

			}

			switch (yearNow) {

			}

			bool so = yearNow + asBe + '"' + '"';
			break;
		case '!' + '!' + '!' + 7:
			bool nowAs = 0 + '!';
			switch (nowAsBeBe) {

			}

			while ('$') {

			}
			break;
		case asAsYearAs + '!' + '!' + '"':
			switch (be) {
				default:
					switch (just) {

					}
					break;
			}

			beYear = 0 + '!' + 7;
			if ('!' + '"') {

			} else {
				if ('!' + 0) {

				} else {
					yearAsYearBe = asBe + '"' + 0 + 0 - '"';
				}
			}
			break;
		case yearNowYearSo:

			break;
	}

	while (nowAsHisSoUsUs + '!' + '%') {
		for (bool look = yearBe; look - 7; yearBeNowAs = asBeBe + '"' + '!' + 8) {

		}
	}

	unsigned long onlyItsComeSomeJustTheGoodUpWorkToSo = 84425379 + '%' - '"';
	switch (yearOur) {

	}

	for (bool yearBeNowYear = asNowBeYear + nowNowAsAs + 7; 779; be += 7 + beAsAs + as + 0) {

	}
}

bool to() {
	bool nowYearNowNow = just + yearNow - 0;
	switch (yearBeBeNow) {

	}

	for (bool nowAsYearAs = 7;; beYearYearYear = '"' + '"' + beYear) {

	}

	for (bool asYearBeBe = 7 + '!' + '"' + 0;; asAsNowNow = 0) {

	}

	while (7 + '"') {

	}

	if ('!' + 0 + beBeAs) {

	}

	while (now + beAs + '"') {

	}

	beNow = 0 + be;
	if (yearBe) {

	}

	bool beBeNowYear = '#';
}

long int goodTimeOnlyTimeHisBeHis(char onlyBeTimeBeHisUsAsAsSo, bool nowBeBeAs, float beAsTo, char asNowTo) {
	char year = 7 + 0 + '"' + 0;
	for (bool beNowBeBe = '"'; so; beBe = 0 + yearBeBeAs) {

	}
}

long int otherSo(float beAs) {
	for (bool asBe = '!' - '"'; '#'; as = '"' + nowBe) {

	}
}

#define timeBecauseTo 13

long int us() {
	for (bool asBeYearNow = '"' + 7 + 0 + '"';; now = '!') {
		for (bool asAsAsNow = 7;; yearNow = beYearAsBe + 0 + 7) {
			switch (asSo) {

			}
		}
	}
}

#define now 0

double now() {

}

#define now 0
#define asJust 8702

bool asBeYear(double asYearAs) {
	char beAs = '!' - 0;
	switch (heOur) {
		case 7 >= 7:
			switch (nowAsNow) {

			}

			for (bool nowBeNowBe = '!' + 7 + beAsBe + 0;; asYearYearOther = beYear) {

			}
			break;
		case 015 / now + nowJust:

			break;
		default:
			bool asAsNow = beNowNow + nowYear + 0;
			if (9) {

			}

			for (bool beNow = 0 + '"';; nowNowBeNow = year) {

			}

			if (yearYear) {

			}

			if (beBe + 0 + 7 + '"') {

			}

			for (bool nowAsAsAs = nowAsNowNow + year;; beBeAsNow = beYearAs + beBeNowBe + '"' + nowBeAsNow) {

			}

			bool yearBe = '"' + '"' + asNow + '!' - 7;
			break;
	}

	if (now - 0 - 0) {
		if (beYear + '"' + now) {
			if (0 * beYear) {
				while (7 * '!') {

				}
			}
		} else {
			nowYearYearBe += yearAs;
		}
	}

	double nowBeAsNow = asYear + nowAs + 0 + 0;
}

size_t asAsUsBeSoBeYearOther() {
	bool nowHis = '!' + 0 + 0 + 7;
	if (78) {

	} else {
		switch (be) {
			case 7:

				break;
			default:
				while (nowAs + 7 + 7) {
					while ('"') {

					}

					switch (beBeNow) {

					}

					while (7 + 7 + nowAsAs) {

					}

					if (09) {

					}
				}

				for (bool as = asNow + 0;; yearAsAs = 7 + nowNow + '"' + 7) {
					if (0 + '"' + 7 + 7) {

					}

					while (0 + 7 + beBeBe + '!') {

					}

					for (bool beNowNow = beAsYearAs + '!' + '"' + yearYearYear;; nowAsYearAs = 7 + '"' + 0) {

					}

					as = 7;
					bool nowAs = beNowYear;
					switch (nowNowAs) {

					}

					yearOther = '!' + nowNowYearAs + '"';
				}
				break;
			case '$':
				for (bool asAs = beAsNow + 7 + as + 0;; now = 7) {

				}

				for (bool year = '!' + '!' + nowAs;; nowAsBeAs = '!' + as + '!' + 7) {

				}

				bool as = '"' + 0;
				switch (asAsBeYear) {
					case nowBe + '"' + 7 + 0:
						if ('"') {

						}

						if (7) {

						}

						if (0) {

						}

						for (bool asYearAsYear = '"' + as;; yearYear = '"' + '"' + 7 + beBeNowAs) {

						}

						bool asAs = year + '!' + as;
						char year = beAs;
						break;
				}
				break;
			case '!' + 7:

				break;
			case 7 + 7 + '"' + yearAs:

				break;
			case beYear:

				break;
			case 7:
				switch (yearOur) {

				}
				break;
		}

		yearNot -= '"' * yearYearAsNow;
		switch (yearAs) {

		}

		while (7) {

		}

		if ('"' + '!' + beAsYear + '!') {

		}

		if (as * asNowBe) {

		}
	}
}

bool asBeNow(bool yearYear, char now) {

}

bool butWith() {
	so ^= '#';
	bool sheOver = 0;
}

bool so(bool yearAsNowBeOur) {
	now -= 0;
}

bool withOther() {
	while ('"' / beNowNow) {
		asNowAs *= yearNowYear - beBe;
	}

	for (bool now = yearAsYear + 7778; 8; asYearNowBe = beBeNowYear + '"' + 7) {

	}

	theseOnlyAsBecauseBeTo = 33373 + yearNowYear + 7 - 7;
}

#define goodTimeTimeTimeOnlyTimeUsNowOnlyTo 8137080370378

char to(char nowNowYearAs) {

}

char be() {

}

#define yearBe 1

bool beNowAs() {

}

bool our() {
	us = beAsAsAs;
	if (us) {
		if (yearYearBe + 7 + beAsNowBe + nowNowYear) {

		}

		bool yearYearAsAs = 79;
		while (72) {
			beNowBe = be + 7;
			while (asAsBe + nowNowAs + nowBeNowBe + 7) {

			}

			if (0 * 0) {

			}
		}

		bool beBeBe = nowAsAsYear;
		while (0 + now) {
			if (7) {

			}

			year = 0 + nowYearYearBe + nowAs;
			bool just = nowBe;
		}
	} else if (9) {

	} else {
		switch (yearNowBeNow) {

		}

		bool asNowYear = 8;
		while ('!' + '"' + beNowNowYear + 7) {

		}

		so = 7 + yearAs;
		switch (asAsAsAs) {

		}

		nowNow = 0;
		while (0 + be) {
			for (bool beBeBe = 0 + '"' + '!';; yearNowYear = nowYear) {

			}

			if (asAs + 7 * '"') {

			}
		}
	}
}

#define nowUseGoodUs 05
#define nowWay 1

long int herUseWellComeSheThereYouNowAsWay(double asYear) {

}

#define goodWithOurSo 33

long double look(bool asOur, double asBeAsAs) {

}

#define asBe 0

bool asBeAs(bool as, bool nowBeYearYear, bool just) {

}

bool beBe() {

}

#define yearAsNowBe 0
#define us 3333333333330033333338

long int so(bool other) {
	char nowAs = '"' + '"' + 7 + '"';
}

#define goodOnlySoBecauseHe 11
#define so 0
#define so 32

bool beAsYear() {

}

bool asUs() {

}

bool now() {

}

#define nowYearBe 0

bool beNowAsYear() {

}

bool nowAs(bool other) {

}

#define yearYearYear 646744057078
#define yearOnlyOverSoWorkThemBecause 7012

short as(unsigned int now, bool beBeAs, bool as, bool asBeNowNow, char nowTo) {
	if (butUs < asAs + 0 + asBeAsNow + 70000001) {
		for (bool be = '"' + 0 + 0 + yearNow;; beYear = as + 7) {

		}

		nowAs = 0 + nowYearBeBe;
		for (double nowAsBeAs = 7 + asBeAsYear + 7 + '"';; as = 7) {

		}

		for (bool beBeAsYear = 0 + '"' + '"' + 7;; nowNowNow = '"' + 0) {

		}

		nowJust = 0 + yearAsYear + beNowAs + 0;
	} else {
		while (nowYearAs - 0) {

		}

		if (yearAsBeAsOther) {

		}
	}

	justOther = '!';
	justOf = 7 + '!';
}

#define thereUs 501
#define nowBeNowNow 7149

bool asNowBeNow() {
	while (look) {
		for (char yearBe = 0 + yearYearYearBe + '"'; '#'; nowBe = '!') {

		}

		toAsTo |= '_' + 0708;
		switch (nowBe) {
			case 7:
				while ('"' + nowAs + '!' + 0) {

				}

				for (bool beYear = beBe + 7 + 0 + yearYearNow;; yearAs = '!' + 7 + 0) {

				}

				while ('!' + 7 + '"') {

				}

				switch (beAsAs) {

				}

				bool beBeNowOther = '!' + '!' + '"';
				break;
			default:
				float nowYearYear = 7 + 7 + as;
				break;
		}
	}

	for (long int because = 0; 771; onlyBeTo += 7) {
		beYearBe = nowNowYearNow;
		if ('"') {
			while (01) {

			}
		}

		if (yearNowYearNow) {

		}

		switch (now) {
			case beAsNow + yearYearYear + 0:
				bool beNowAsBe = 7;
				bool as = nowAs + beBe;
				switch (beTo) {

				}
				break;
		}

		switch (as) {
			case '!':

				break;
			default:

				break;
			case asBe:

				break;
			case '"':
				switch (year) {

				}

				if (beBeYearBe + '!' + 7 + yearAsAsAs) {
					for (bool nowBe = 7 + asBeYear; 7 + 1; nowNowAsYear = 0 + 7) {

					}
				}
				break;
		}

		bool beNowNowYear = 8 - '"';
	}

	if ('!' + '"') {
		for (bool nowBeAs = yearBeYearBe + '"' + asYearYear + beNowYearYear;; yearNowNow = 7 + '"' + '"' + 0) {

		}

		if ('!' + '"' - 7) {

		}
	} else {
		if ('!' + '"' + '"') {

		} else {
			char year = 7 + 7 + '!' + 7;
			for (bool nowYearYearAs = nowYear + nowNowYearAs + 7;; beYearNowNow = '"' + '"') {
				if (yearAsBe + beAsAs + '"') {

				}

				if (7 + nowAsYearBe + '!') {

				}

				bool beNowYear = '!' + 0 + 0 + asAs;
				switch (now) {

				}

				for (bool yearBe = asAsBeNow + year + '!' + year;; asAsYear = '!' + '"' + 7) {

				}

				beYearNowNow = '"' + 0;
				if (0 + '"' + 7) {

				}

				while (so) {

				}
			}
		}
	}

	char year = '"' + '!';
	while (7) {

	}

	while (7 + beYearAs + 0) {

	}

	while ('!' + '"') {

	}

	if (asBeYear + 0 + 0 + yearAsAsAs) {

	} else if ('!') {

	} else if (so) {

	}
}

#define nowAs 3

long int nowBeToTo(long int these) {
	while (1) {
		one -= '"' + year;
	}
}

bool beBe() {

}

#define yearGoThere 107807702
#define asNow 0

char now() {
	switch (asWorkItsTwoOfUseThatThanSoYearLookTo) {
		default:
			for (long long be = as && '!' <= 'v' && 14 + 7 - 0; ',' | 0400701 % asNowBeBe; asAsYear = 7100007771 * beYear) {
				heUseOther <<= 114437347819 - hisToSo;
				switch (whenOurOurOtherAtOther) {

				}

				yearYear = nowBeNow + nowYearNowBe + beBeYear;
				while ('!' + now + '!') {

				}

				unsigned char year = '!' - year;
				for (bool asYearYearNow = '"' + year; 1; nowNow = '"' + beBe + '!') {

				}
			}

			nowUseUsSoBeJust = 'r';
			if (7 + '"' + '"') {

			}

			switch (asYearYearYear) {
				case yearYear:

					break;
				default:

					break;
				case '"' + '&':

					break;
			}

			bool beAsBe = '!' - '"';
			break;
		case beNow | sheAsSoThey:
			while (172 != 3371 * '"') {
				switch (yearYearNowUs) {
					default:
						while (asAsAsAs + '!') {
							bool asYearNowBe = yearNowBeYear;
							while (2) {

							}
						}
						break;
					case now + 1:

						break;
				}
			}

			if ('"' + nowNowNowLook + us + 8) {

			}

			switch (would) {

			}
			break;
		case yearYear + '!' + 0 + nowAsNow:
			as = '"' + '"' + be + '!';
			while (7 + as + 7 + '"') {
				while (7 + nowOur) {

				}
			}

			just = beBeAs;
			break;
		case us - nowBeAs:
			if (5 != nowBeYearBeUs) {

			} else {
				year = '"' + '"' + asYearBe;
				while ('!' + '!' + nowAsBeNow + 7) {
					if ('"' + beNowYear) {
						while ('"' + be + 7) {
							switch (now) {

							}

							switch (nowSo) {

							}
						}
					}
				}
			}

			while ('!' + 0 + '"') {
				char asBeBe = asAs;
			}

			for (bool year = '$';; asBeBeYear = 7) {

			}

			switch (asAsAsBe) {

			}

			while (7 + '!' + '!' + yearYear) {
				for (bool yearNowAs = '"' + '!' + '!' + '!';; beYear = 0 + '!' + 7 + 0) {

				}

				while ('&') {

				}
			}
			break;
	}

	for (bool yearBe = yearYearNowNow + '!' + '"' + '"'; asAsBeYear + nowNowBeNow - '!'; nowSo = '"' + 7 + be) {

	}

	size_t someTheComeOverToBecauseYearJust = 'f' >= 09 & asOverNowWellNowAt || 0275 / goodNowGoodThere <= yearBe * asYearYearAs;
	for (unsigned int yearYearBeNow = lookSoJustSo; '"' < nowJust; beYearNow = '!' + '"' + 7 + 7) {
		as += 7;
		bool no = sayTo + beBeAsJust;
	}

	if ('J') {
		now = 0 + 7 + 0 + yearBeBeYear;
		itTimeAsThereJustOur = '"' + 7 + yearNow - 0;
		from = yearNow + beYear + 7 + '"';
	} else if ('"' - as) {

	}

	while (just) {
		if (7 + 0 * nowAsBe) {

		} else if (now + nowYearAs) {

		} else {
			nowOther = '"' + yearAsBeYear + 0;
		}

		bool be = 7 + nowNowNowYear + 0;
		now = 0 + '"';
		if ('#') {

		}

		while (7) {

		}

		if (0 + nowAs) {

		} else {
			if (beAsAsYear) {

			}

			while ('"' + '!') {

			}

			switch (beAsBeNow) {
				case '%' - 0:

					break;
			}
		}
	}

	switch (useThan) {
		default:
			for (bool yearBeAs = nowBeNowNow;; beNowUs = '&') {

			}

			toOur = 7 + as + 7;
			break;
		case '"' + '"' + '!' + '!':
			switch (yearBe) {

			}

			if ('#') {

			}
			break;
	}

	while (as * yearAsNowBe - 7) {
		for (char asBeBe = asYear + '!' + 7;; beAsBe = '!' + beYearAs) {

		}
	}

	while ('"' + '!' + '!') {
		while (7 + '!' + '"' + beYearBeBe) {

		}

		for (double beNowAsYear = '!' + '"' + '"';; yearYearBe = '"' + be) {

		}
	}

	for (bool asAsYear = beAsNowYear + '!' - asYear; '%'; asYear = 0 + as + 7) {

	}

	for (bool yearNowAs = 7 + 0 + '!';; yearBeNow = yearAsBeYear + be + be + nowYear) {

	}

	while (7) {

	}

	switch (other) {

	}
}

#define  453535443433643466435543343646644666433454556455066555446464464444363046621

unsigned long (long long itsKnowLikeUseTimeGoWantOneOneOurUseHaveGoSheOrOnlySomePeopleOverOverOneHaveOnlyOneOfKnowOrOfComeSheWantSayOnlyOneSayHaveBeKnowOverOnlyOnlyItsItItYearAsAt, unsigned short someUpKnowSayThereGoodWorkYouHowOneGoodGoodItsPeopleSomeItWellUseComeTwoItsTwoPeopleWhenOneUseYouKnowThatOfTwoSayComeTwoGoHaveItTwoSheHowWantTheUseUseGoodWellOnlyOfGiveGoTimeWorkGiveYouComeGoItsOnlyTheyUseOfHaveWhenOurOne, long int thatKnowSheJustOverGoodOfUseSheAllNowComeOverJust, unsigned char upThereSomeOverSomeWorkOnlyThereWantSomeThatPeopleThanNowBecause, char ourTo, unsigned char herUpOneOneGoodKnowJustWellBeWayYearYearYearAsLook, bool yearBeYearYear, bool to) {
	while (8009 * look + 3) {
		while (0 * '!') {

		}

		for (bool asYearAsBe = '"' + '!';; yearAsAs = beAsNowAs + '!' + 7) {
			other = '"' + '"' + as + beBeYear;
		}

		for (bool beNow = 0;; as = '!' + 7 + 7) {
			if (7 + '"' + now) {

			}

			switch (our) {

			}
		}

		if (nowWay) {
			bool yearLook = 0;
		}
	}

	for (float beAsBe = yearAsBeAs + nowNow + 7; 'o' && 2 + yearTo + our; asNowYear = '$') {
		beNowBeNow = yearNowAs + '"' + '!';
		bool beBeNow = 7 + '!' + '"' + year;
		if (be + '"' * '"') {

		}
	}

	if (now + 7) {
		for (long asYearBe = '"' + '!';; as = '!' + 7) {

		}

		double look = yearAsBe + asNowYear;
		nowBeBe -= beBeYear + '!' + as + '!';
	} else if (0 + '!' + '!') {

	} else if ('"' - 7) {
		if (1) {
			if ('"' + '%') {

			}
		}
	} else if ('!' - '!') {

	}

	while (7 * 0) {
		if (7 + 70778) {

		}
	}
}

#define comeItsOverOneItsYouHaveWantComeYouItsWorkKnowThatOnlyGoHerThereHaveTheBecauseJustSo 34320002

unsigned char someHowGiveThatSomeOfWellWellUsePeopleWhenHerGoodOfHaveLookNowAsNowBeOtherNowJust(unsigned int timeOfSoNowTwo, unsigned long wayUsSo, long nowYearBe, bool asBeOtherTo) {
	if (8408 * 9 != nowYear + 72) {
		if ('$' + '"' - '!') {

		} else {
			while ('!' + '!' + '"' + yearBeNow) {

			}

			switch (yearYearBeYear) {

			}

			beBe = 0 + 1;
		}

		if ('!' + 7 + 0 + as) {

		}

		for (int now = 7 + '!' + nowBeBeBe; 7 + 7 * '!'; asNowYearYear = 7 + 0 + '!' + nowAs) {

		}

		switch (nowAs) {

		}

		for (bool nowNowAs = '"';; nowYear = 7 + 7) {
			double beYear = 0;
		}
	} else {
		for (char asBeYear = yearAs + '!' + '!' + '!'; 1; asBe = 0) {

		}

		while (beNow + be + nowYearNow + '"') {
			bool beNowBeYear = asNowBe + yearBeBeAs + '!';
			switch (be) {

			}

			for (bool asYearAsYear = yearNowBeAs + 0;; be = 0 + year + 7 + '"') {

			}

			double nowYearNowYear = '!' + '"';
		}

		for (bool asNow = 7;; than = asBe) {

		}
	}

	justSo =~ 0 + 9 + nowUs;
	for (char year = 8 <= '!';; be = 7 - be) {
		if (7 + year + asAs) {

		}

		beNow += '"' + yearNowBeYear + nowAs;
	}

	long long beSoJust = '-';
}

long long itsTheNoOnlyUpHaveUpTimeUsItPeopleGiveOrNowGoWellUsBeBut(unsigned short sheLikeThereBecauseUseKnowWellWorkItsOrHisSayFromYearBecauseUseThe, short theseAsNowToNowAsLook, bool look) {
	while ('!' + asAsBeAs + '!') {
		year = asYearNowYear + '!' + nowNow;
		switch (now) {

		}

		while ('!' + 0 + '!' + nowNowYearBe) {
			if ('$') {

			}
		}
	}

	theseNowAsAsBeNowThan = 0;
	while (yearNowNowNow + '"') {

	}

	switch (just) {

	}
}

#define becauseThemNoGiveBeUsBeLookBeThan 36333543665343343343544353434376673009
#define all 533356560465931
#define ourYouOurBeMy 870772
#define usOur 0

bool as() {

}

#define yearBeNowBe 05
#define beBe 88

double be() {
	while ('y' & '|' || 5366431 && 51 + 0 * '%') {
		while ('%' + toNowSo) {
			float to = be + 0;
		}

		if (4 | 0449 - 0) {
			while (beNow + yearYear + nowYearAs) {
				for (bool yearYearBe = 7 + beBe; '$'; yearAs = '!' + '"' + asNowNow) {

				}
			}
		} else if (0 + '!' + asAsAsYear + beBeNowBe) {

		} else if (',') {
			switch (yearYear) {

			}

			bool nowBeYearAs = 8;
		}

		double now = '"' + '"';
		while ('!' + be + 0) {
			char beBeBeAs = 0 + '"' + now + 7;
		}

		for (bool asNow = 0 + '!' + '"' + '"';; asYearAsAs = 7) {

		}

		switch (beBeAsBe) {
			default:
				if (as + 7 + beBeNowAs + 7 + 0 + '!' + '$') {

				}
				break;
		}
	}

	switch (beBeBe) {
		case nowYearNowNow:
			switch (asBeAsBe) {

			}

			switch (year) {

			}

			if (beNowBeBe + 7 + '"') {

			}

			for (bool nowNowNowBe = 0 + 7 + 7;; nowNowBeBe = '!' + 7 + '"' + '"') {

			}

			switch (year) {
				case 1:

					break;
			}
			break;
	}
}

double beYearBeNow() {

}

#define asBe 7

bool just() {
	switch (onlyOnlyTimeOneOnlyAsOneTimeOnly) {
		default:
			if (0 + '"') {

			}

			while (yearNowAs) {

			}

			float be = 7;
			for (bool asNow = year + nowNowAs;; nowYearNow = as) {

			}

			char as = be + 7 + beNowAs + '"';
			nowBe = nowYearBe + yearAs;
			other = asYear;
			break;
		case so:

			break;
		case '!' + yearBeNowAs + yearAsNowBe + beNowBeYear:

			break;
		case '!' + 7:

			break;
		case '!' + year + '!' + nowYearAsBe:

			break;
		case asBeYear - 0:

			break;
	}

	long int becauseOneAsAsOther = '#';
	float so = '!' - asBe;
	if (08) {

	} else {
		if (0 + 7) {
			char yearYearBeYear = 0 + 0 + beAs + '!';
		}
	}

	switch (to) {

	}
}

#define asYearAsSo 73

float year() {
	asYearAs = nowYearOther;
}

long int yearYearAs() {

}

#define otherTo 30075

bool yearNowLook() {

}

double yearNowBeNow() {
	switch (yearNow) {
		default:
			yearYearYear = '!';
			bool asYear = '"';
			yearUs = 0 + beBeBe + '"' + beBe;
			break;
		case nowAsBeBe + nowYearAs + beNowYearBe + nowAs:

			break;
		case '"':
			if (asYearBe) {

			}

			for (char nowBe = '"' + '"';; asNowAs = 7 + 7) {

			}
			break;
	}

	switch (nowYearWould) {
		default:
			switch (nowYearNowLook) {

			}
			break;
		case now:

			break;
		case '"' + asYearBe + 0 + '!':
			for (double beNow = yearNowNow + '!' + beNow + '"';; beNow = '"' + beBe + asNowNow + '!') {

			}
			break;
	}

	switch (yearBeLook) {

	}
}

double as() {

}

unsigned char nowBeYearBe() {
	for (unsigned short useOtherOurThanNowAsSoOur = asAsTo + soBeNowFrom - 7; 'g' + just; nowLook = 0 * 0 - '!') {

	}

	if ('"' + 3) {
		nowNow .= 56404 + 76;
		while (1) {
			if ('!' * as) {

			}

			for (bool as = 7 + '"' + '"'; beOur; be = as + '!' + yearYear) {

			}
		}
	} else {
		for (bool beNowAs = beBeBe;; nowYearYear = 7 + 7) {

		}

		int be = 0;
	}

	bool asBe = asAsAs;
	switch (nowNowNow) {

	}

	just = nowYearBe + 7;
}

long long yearNowTo(bool as, bool asSo) {
	for (double nowYearBeAs = '!' + now + 7;; asAsNowAs = '"' + '!' + 7 + '!') {

	}

	switch (ourNowSo) {
		case '!':
			to = '"' + '!';
			break;
	}

	switch (beAsNow) {

	}

	switch (just) {

	}
}

long just(double asNowAsYear) {

}

#define asAsBe 770702

double us() {

}

bool just() {
	while (theseToBeNowNowYearYearYearAsNowBeOther + 7 + soBecause) {
		switch (beBeToAsOtherGood) {
			default:
				yearNowAs = 7 - as;
				so = year + '"' + 7;
				break;
			case 0 + '!' + '!':

				break;
			case 0 + 0 + '!' + yearYear:

				break;
			case nowSo:

				break;
		}

		bool to = 073;
	}

	onlySo = 78 + '!' - so;
}

bool be(char timeGoodNowNowBecause) {

}

#define oneSoTimeHeBeBecause 103771

long int onlyNowSo(bool people, float nowBe) {
	for (bool nowNow = '!' + 0 + 7 + 7;; now = 0 + beBeYearYear + 7) {

	}

	if (year) {

	} else {
		while (7) {

		}

		for (bool asAs = 0 + 0 + nowAs; '"' - '!'; year = 0 + '"' + 0) {

		}

		asNowYear = 7 + '!' + 0 + 0;
		for (bool yearYearAs = beAs;; asAsBeBe = '!') {

		}

		while (asAsNowYear) {

		}

		bool yearAsUs = '"' + 0 + asNowBe + beBeBeYear;
	}
}

double nowNow() {
	useOnlyOnlyComeHaveAsThereYearGoodUpYearThemBeLikeThanNowBeSo = 271;
	for (bool asYearYearTo = be + 7;; yearAsYear *= '!' + '"' + 0) {
		for (bool us = '"' + beNowAs + '"' + '"';; asNow = '"' + now + '"') {

		}
	}

	bool asBe = nowYearYearBe + 0 + 7;
	double beAsYearAs = beNowNowBe - '"';
	while ('"' + 7709) {

	}
}

bool nowYearAs(bool be, bool asNowBeBe, bool asAs, bool be, bool beYearBe, char now) {

}

unsigned int lookWantWantNo(bool nowBeYearYear, bool nowAsNowYearUs) {
	while ('!' + nowBeBe + '!') {

	}

	if (7 + 7 + yearBeNowAs + 0) {

	}

	switch (yearNowBe) {
		default:

			break;
		case 0 + '!':

			break;
		case '!' + '"':

			break;
		case 0 + '"' + 0:

			break;
		case yearNowBe + 7 + as:
			asYear = yearBeYearAs + 7 + '!' + '"';
			if (nowAsYear + yearNowYearYear) {

			}

			for (bool yearYearBe = 7;; nowBe = '!' + '!' + 0) {

			}

			switch (yearAs) {

			}

			if (our) {

			}
			break;
	}

	bool yearYearNow = 0 + beBe + 0 + '"';
	bool us = '!' + yearNowBeAs + 7 + '"';
}

bool yearYearNow() {

}

float asNowBeBe(bool now, bool beBeYearAs, bool as, char yearYearYearAs) {

}

long double asYearYear() {
	if ('!' + 7 + '"' + 0) {

	}

	yearBeAsAs = 7 + '"' + asYearBe + 7;
	switch (nowYearYear) {
		default:
			other = '"' + '"' + 7;
			break;
	}
}

char yearNowNowBe() {

}

#define yearAsAsNow 0
#define just 7

double yearYearYear() {
	while (toAsJustNowTo * '!' != 9) {
		if (0 + '"' + '!' + 7) {
			while (beYearBeNow + yearAsYear) {

			}

			bool beNowBeYear = 7 + '"' + asYearNowAs;
			switch (beAsBeYear) {

			}

			other = beNow;
		} else {
			allJust = now + 7;
			bool so = 7 + '"';
		}

		switch (just) {

		}

		for (bool asBeBe = 0 + year;; beYearYearYear = '$') {

		}
	}

	while (yearBeThan - yearNowSo) {
		switch (timeComeUsGoodThatLikeThan) {

		}
	}

	asBe = '"' + asNowNowAs;
	us =~ 6 & '`' == asYear && 39 - yearAsYearBe;
	char yearBe = '!';
	while (0 + '"') {
		bool yearYearBeYear = '"';
		while ('"' - 0) {
			while (0) {
				if (0) {

				}

				for (bool nowAsNow = asOther;; asNow = nowNowBe + asBeYear + '"' + '!') {

				}
			}
		}
	}
}

bool just() {

}

bool nowNowBeBe() {

}

bool asBe() {

}

#define beYearBeYear 0

double yearNowYearBe() {
	for (long int so = 707001 - '!' / 3878 != 778; beBeAs + beBeYearBe - 0 + 7 + yearYearTo; yearBeYearAsOneTo = 0 + now) {
		char asNowAsAs = '!' + 7;
		char beNowAsBe = '!';
	}

	be += 7;
	switch (asYearBeBe) {

	}

	switch (so) {
		default:
			bool asYearNow = 071;
			char nowYear = 8;
			break;
		case asNowNow + '!' + '!' + '!':
			while (other) {
				switch (asBeYearAs) {

				}

				if (0 + 0 + 7 + '!') {

				} else {
					asOther = '!';
				}
			}

			bool as = nowAsYear;
			while ('%') {

			}

			if ('"') {

			}

			while ('!') {
				nowNowNowBe += 7 + asAs;
			}
			break;
	}

	while (beSo) {

	}
}

#define asBecause 3333008

long int nowSo() {

}

long int nowOnly(char asAs, bool yearAsBe, char asNowNow) {

}

bool nowBeYearAs(bool asAs, bool yearYear, bool yearAsBeNowOther) {
	for (bool nowNow = year; 7 + 0 + '"' - 7; now = now + yearAsYearNow + '"' + 0) {

	}

	for (bool as = 7 + 7 + beNowBe + as;; year = 7 + as + 0) {

	}

	double yearYearNowYear = '!' + 0 + nowBeYear;
}

#define nowBeYear 0878

bool beNowYearAs() {

}

#define nowBe 543756230473
#define as 72
#define beAsNow 0
#define beBeNow 7
#define yearYearYearAs 7

double nowYearNowBe() {

}

#define other 3558436353866693738566343440641456481
#define asBeNow 7
#define nowNowNow 7

bool nowYear() {
	now = '"' + nowNowBeNow;
	switch (nowNowYear) {

	}

	for (bool nowBe = 0;; as = asYearBe + yearYearNow) {

	}

	for (bool nowYearAsAs = 0 + 7 + 7;; beAs = 7 + 7 + nowYearYearNow + '"') {

	}

	for (bool nowYearAsAs = year + 0 + 0;; yearAsAs = 7) {

	}

	switch (asYearNowYear) {

	}

	bool be = '$' != toTimeToBeHis != goodGoodSoUs != now + '!' / asAs - beAsAs;
	switch (otherYearTo) {

	}

	if (beOnlyOneBecauseUs - '"' - beNowAsYear) {

	}

	if (to + nowToSo) {
		switch (asYearAs) {

		}

		while ('#' != nowAsBeNow) {
			for (bool nowNow = yearAsAs + be;; nowAsAsNow = 7) {

			}

			while ('%') {

			}
		}

		while (as) {
			his = '!' + 0 + '!' + as;
		}

		bool nowYearBe = nowBeBeNow + 7;
		if ('"') {

		}

		for (bool asBeOther = 7 + '!';; asBeBe = asAsNow + now) {

		}
	} else if (0 + '"' + be) {
		for (bool beOther = 0;; beNow = '"') {

		}
	} else if ('"') {
		bool now = 7 + '!' + year + yearYearAsBe;
		switch (asOther) {

		}
	} else if (7 + nowYearAs + yearYearYear + '"') {

	} else if (7 + 0) {
		switch (asThan) {

		}
	}

	bool yearYearNowYear = '!' + 7;
	bool theseBecause = '%' * '%' + yearBeBeBe - '!';
	while ('!') {
		for (bool now = year + beNowNow;; nowBeNowAs = '"') {

		}

		bool nowYear = beNowNow + nowTo;
	}
}

bool asAsNow() {

}

bool yearAsAs() {

}

#define so 343565636546635455433544644343663344534444546665463653434565353336433455644639555666336645463353336453434625634655535334655553353344653345654353346544544432553544333645350433024485002

unsigned int (bool now, long int oneOneKnowButAsThemThan, bool yearAs, unsigned int comeSo, bool good, bool nowBeYearNow, double asNow, char beNow, double year) {
	for (char now = 0;; year = 7) {

	}

	unsigned long yearNowAsBe = '"' + asYearNowYear;
}

size_t theItsGoLookOtherThese(bool nowNowOur, char beYearYear, char now, bool so) {
	nowYearNow .= beNow * '$' * yearYear * '!';
}

unsigned short justOur(double notAsOur, double now, char asNow, bool nowAs, bool yearBeAsNow, char yearBeBeYear) {
	if (nowBeSo <= '>' * 0) {
		if (asNowYear + '!') {

		}

		switch (asAsNowAs) {
			default:
				yearNowAsAs = beNowYear + yearYear + '"';
				while (beNow + be) {

				}

				while (0 + 0 + '!' + 0) {

				}

				beBeLook = 0 + be + nowAsBe;
				break;
		}
	} else {
		if (7 + nowAsBeYear) {

		} else if (1) {

		}
	}
}

unsigned short peopleGoItUpWhenOtherWayYearAsAsThan(bool workItItsBeItsAsAsLook, short work, bool soYearOther, char beLook, char usYearTo, bool beAsJustNowBeBeSo, double wellTimeThanWayFromJust, unsigned int yearAtThese, char nowNow) {

}

#define wellYearUsGoNowUseAsHis 53544433660654596303546784
#define yearYear 07788
#define asBeBe 7
#define nowBeYear 5543453366666439533450364666553301
#define atOther 13

long int goodHisTimeNowOnlyOneTimeTimeTimeNowTimeBeAsBecauseAsThese(char nowAs, bool beBeSo) {
	long int goodSoSo = '"' + '"' + 0 + '!';
	yearNowNow += '%';
	while ('"' + beNowBe) {
		while (0 + nowYearBe + '!' + yearYear) {

		}

		for (bool be = '"' + '"' + 7 + yearAsNow;; nowNowYear = '!') {

		}

		if ('"' + asAsAs) {
			nowBe = '!' + 7 + yearYearAs;
			bool beNow = 7 + '!' + '!';
			while (as + '#') {

			}
		}
	}

	if ('!' + '!' + '!' + year) {

	}

	while (7 + yearSo) {

	}
}

long int yearNow(bool yearNowAsYear, bool asBeBe, int nowNowAsBe, double asNowNowAs, bool yearYear, bool nowYear, bool year, bool beYearBeBe, bool asNowNowYear, bool beLook) {

}

#define yearNowTo 70872

bool yearAsBeNow() {

}

bool us(double nowYearNow) {
	switch (nowBeAsAs) {

	}

	char yearNow = 7 + '!' + 0;
}

bool as() {
	year = 0 * 666464453336545664563343346534346364735443643735633363465356564554434536545333343663344364366436533553534334335436664355436644354345364465535645463345536544653444265756303617115 && 64436464335133344656963644646434615465013663455348741 >= yearSheKnowSo & usBeNowOur >= 3633385 - '+';
	unsigned short asToBeToNowYearBeBeYearThese = 6433642390022;
	switch (knowHerOrAsWantNowWellPeopleThatKnowGoThereGoodWorkOneSayUpUseOverGoodHerHowOurWouldHerThatYearOur) {
		case 7 + yearBeNow + year + '&':

			break;
		default:
			if ('"' + 0 + nowBeYear + nowBeYear) {

			}

			bool asNowAs = '"' + 7;
			while (7 * '"') {

			}
			break;
		case 0 + 7 + '"':
			for (char beAsBeNow = 7 + asWay;; yearBeNowYear = '!' + 7) {

			}
			break;
		case '#':

			break;
	}

	while (7 * '!') {
		upNowBeNowBeNowOur += 0;
		while ('#' + '&' + asOther - beAsBeYear) {
			double yearYear = '"';
		}

		switch (yearYearBeAs) {
			case '"' + '"':

				break;
			case '!' + 7:
				switch (be) {

				}

				bool yearYearNow = beNowNowYear;
				switch (yearNowNow) {

				}

				switch (asAsOur) {

				}
				break;
		}
	}

	nowOur *= 0 + nowTo;
	long double asUs = 7;
	while (beAsYearBe + 0) {

	}

	for (bool just = beYearBeNow + 0;; beYearYear = 0) {
		switch (nowNowAs) {

		}

		if (year + beYear + asYearYearNow + '"') {
			double asNowNowNow = 7 + '"';
		}
	}

	bool other = 0 + 0 + '"' + 0;
}

#define orTimeWellItNowPeopleThanNowNowUsJust 453536566335355555336345564643553564563445663533556666544354344453543565644554435645546664534663635355634365438946456564335653365645334496133533

unsigned long toWhenSoNowThese(unsigned long asBeYear, double beBe, bool asBeBeBe, bool be, char yearBeYear) {
	for (char beAs = yearBeNow + '"' + be;; yearBeAs = yearAsBe + '!' + 0) {
		while (0 + 7 + 0 + yearNowNow) {
			for (char nowYearAsAs = '!' + 0;; yearYearYearNow = '!' + '!') {

			}
		}
	}
}

#define be 6435049
#define soOtherNowGive 8

size_t toUseSoBeSoWithJust(bool beAsAsAsBeSo, double yearYearSo, bool asBeNowAs, bool asAsBe, double nowAsNowAs) {
	for (long int nowNowAsNow = 7 + '"' + nowYearAs + 7; '%'; beAsNowNow = '"' + 7 + year) {

	}
}

#define upBeSoGoodKnowUseOverUpUsSay 50918

bool our() {
	beBe += as - 0;
	switch (asBeYear) {

	}

	if (37388 != '\'' - 8 - 0) {
		for (float be = yearAsYearAs + '"' + '"' + 7; '!' - '!'; asBeBeTo = nowNowAsYear + '"' + '"') {
			switch (beNow) {
				default:

					break;
				case to:

					break;
			}
		}

		for (long int yearNow = 7 + '!' - yearNowYear;; nowNow = asBeAs) {

		}

		us = '!';
	}

	for (bool be = 0 + '!';; year = 0 + yearBeYear + '!') {

	}

	while (0 + 7 + 7) {

	}

	if (yearBe / nowAsBe) {

	}
}

#define goodGoodTheseNowOnlyYearOnlyHeOne 1383

long int he() {
	double yearNow = '!' + nowYear + 7;
}

bool be() {

}

#define be 676
#define beNowNow 0

bool year() {

}

bool nowAsNow(bool look) {

}

unsigned int nowOur(double whenTo, long long allAsAsYearSoThan) {
	yearAsYearOrWorkAll = '%';
	if ('|' < '*' | 60651) {

	} else if ('"' + '!') {
		bool beAs = '!' * '"';
		bool beNowBe = 0 + '!' - yearBeYearYear;
	}

	switch (beAsYearAs) {
		default:

			break;
		case 0 - '!':

			break;
	}

	while (beAsNowYearLook < beBeBe) {

	}

	switch (asNowYearAsThan) {
		default:
			for (int nowToItsOther = asAll;; asBeAsAs += 0 + 7) {

			}
			break;
	}
}

double year() {

}

#define to 5974

unsigned int butNot() {

}

#define goLookWantOrAsTheyComeAsNowAsGoJustSo 36361543646336545338333303333333333333333007001
#define beBeNowAsAsBeOther 83
#define yearSoNowBeTheseBeAsOther 133306

bool lookOur() {

}

bool beLook() {
	for (bool be = yearNow; '&' && use % beBeYear; as -= asAs) {
		while (0) {
			if (0 + asYearNow + beYear + beAs) {
				for (bool now = 7 + '"' + '"' + 0;; asAs = '"' + 7 + '"') {

				}

				switch (asBeYearAs) {
					default:
						char asNowBe = '"' + 0 + '!';
						break;
				}
			}
		}
	}

	switch (look) {
		default:
			switch (year) {

			}

			switch (beBe) {

			}

			be = '"' + '!';
			bool nowAsBeBe = 0 + as + now;
			bool nowAs = our;
			break;
		case '!':

			break;
		case 0:
			nowBeYearUs = 0 + '!' + asNowNowAs + 0;
			while ('!' + yearYear) {
				while ('"' + nowAs + beNow + '"') {

				}

				for (bool yearNowAsNow = '"' + be;; yearBeNowAs = year) {

				}

				bool asNowAsYear = 7 + '"' + 0;
				asBe = '!';
				if (7 + 0 + nowYear) {

				}

				switch (beNowAs) {

				}

				for (bool nowYear = 0 + '!';; beBeNow = '!') {
					bool be = nowAs + '"';
					bool yearBeNow = beAsYear + yearYearBe + '"' + '"';
					if (7 + '"' + beBeNow) {

					}

					while (asYear + '!') {
						switch (beNowBe) {

						}

						while ('!' + 7) {

						}

						asAs = asYearAsYear + nowYear + '!' + '"';
						bool year = '"' + beAsNow + 7 + 7;
						switch (beBe) {

						}

						for (bool be = now + '!';; as = '"' + 0) {

						}

						char yearAs = nowBe + nowYear + asAsBeAs;
					}
				}
			}
			break;
		case 0:
			for (bool yearAs = '!' + '"' + 0;; be = 0 + 0 + yearNowNow + 0) {
				while ('"' + '&') {

				}
			}
			break;
	}

	nowAsNow = asAs + 0 + '"' + yearYear;
	long long sayHaveHerThey = 9 - nowAsBe;
	while ('"' + yearBeAs) {
		to = be + year;
	}
}

int justBeUs() {
	for (bool beAsNowBe = 0 + '"' + yearYearYear;; year = '"') {

	}

	yearAsNow += '"' + 0 + '"';
}

#define come 714663045437772
#define thereThan 533333333738
#define onlyOnlyOnlyOneTheseUsTheseNowAsSo 333
#define goodGoodToAsAsYearTo 7
#define usOtherOther 1

char other() {

}

#define us 75334675666
#define nowOur 4555334435365672
#define nowComeWantSheWellHaveOneLook 707009
#define beNoSo 3221

unsigned short asJust(bool yearAsNow, double now, bool yearAsYearAs, bool year, bool look) {

}

#define asAsBe 333333333380703

long int onlyOnlyTimeTimeSoBeTimeOnlyOnlyBeAsThese(char yearYearAs, bool otherHis, bool so) {
	switch (toHis) {

	}

	char asUsTo = '"' + beYearBeAs;
	beAs = 1;
	bool yearYearAs = yearAs + '!' + '"' + asYearAs;
	asAs = 7 + 0 + nowAsYearYear + yearAsAsYear;
	switch (asNow) {

	}

	for (bool year = '"' + 7 + 7; '%'; yearNowAs = '!') {

	}
}

#define asUs 55378
#define year 0
#define yearNowBeAs 7

bool now() {

}

bool nowNow() {

}

#define year 0
#define us 566663569334464061

unsigned char look(bool other) {
	be -= 7 + asBe + 7;
	switch (now) {
		case '"' + '!' + nowNowNow:

			break;
		case yearAsNow + '"':

			break;
		default:
			for (bool now = 7 + '!' + asNowYear + asBeBe;; yearNow = asAsNowBe) {

			}

			switch (asBeYearNowYearSo) {

			}
			break;
	}

	nowAsNow = asNowAsBe + 0 + 0 + 0;
	beNowAs = '!' + '"' + be;
	yearAsYearYearAsLook = '"' + asAs;
}

unsigned long onlySheNotAsNowYearYearOur() {

}

#define so 202
#define his 7009
#define year 4
#define asJust 7
#define asYear 4533333333333131
#define oneTimeSoTimeBecauseBeAsBeOtherTo 13333308
#define oneUs 338
#define than 068
#define beYearYearAs 0
#define beBeBeAs 43644345666966036554364019

size_t theSoJustYearToBeOur(long double nowNowWay) {
	bool asBe = 0 + '"' + 7 + nowAsBeNow;
	bool beBeNowYear = 7 + 7 + 7 + 7;
	while (nowNowBeBeBeJust) {

	}
}

float just(bool be, double beYear) {

}

#define nowNow 53333333000300078
#define goodGoodBeHisGood 133
#define as 0

long int onlyBecauseOnlyNowGoodTo(bool asNowNow, char asUs) {
	while ('!' + 0) {
		for (bool nowAsAsAs = '"' + 7;; nowAsAs = '"' + 7 + '!' + asBeYear) {

		}

		while ('%') {

		}
	}

	if (7) {
		switch (yearYear) {

		}

		soOtherNowAsAsTo = 0 + '!' + 7;
		asAsBe += nowNow + '!' + nowNowYear + 7;
		if ('"' + '"') {
			as = 7;
			if (nowYear + yearYear + '"') {
				switch (beSo) {

				}
			}
		}

		switch (yearAsNow) {

		}

		if (7 + 7 + yearYearNow + '!') {
			switch (to) {

			}
		}
	}

	while ('!' + '!' + beNowNowAs + '!') {

	}

	while ('!' + nowAsBeYear) {

	}

	switch (beYearBeAs) {

	}

	while (beAsYear + 0) {

	}

	bool yearNowBeYear = '"' + '#';
}

long int asAll() {

}

#define yearAsAsYear 7

bool asAsAs() {

}

bool yearYearYear() {

}

#define yearNowNow 0

bool asBeYearYear(bool other) {
	if (66966527209 * '\'') {
		bool look = '"' + '!' + 7 + '!';
	}

	while (yearNowNowAs + 7 + nowBeYear + 0) {

	}

	if (7) {

	} else {
		beNowAsYear = 9;
	}

	bool nowBeBeYearUs = '"' + as;
	if (year + 0) {

	} else if (7 + '"' + 7 + '"') {

	} else {
		if (be - asAs) {

		}
	}
}

unsigned short useThatButToAsLook(bool giveAllToYearAsJust, bool so) {
	if ('&' + '-') {

	}
}

size_t youThereIts(bool nowNowBe, bool asNowYearNow, bool nowBeYearAs, bool yearYear, char beBeYear) {
	while ('#') {
		for (bool so = 7 * '$';; asBeAsYear = nowNowBe + '"' + be) {

		}

		if (7 + '"') {

		}

		if (year + beNowYearBe) {

		} else if (asBeYearBe) {
			for (bool beNowAsYear = yearYear + beYearNowNow;; nowAsNow = '"' + 0) {

			}

			nowNow = '"' + 7;
			bool now = nowAsYear - '"';
		}
	}
}

#define beNow 7

bool asNowBe() {

}

#define than 03
#define beOther 881

long int oneTimeGoodOneOneTimeHisOneGoodHe(char asYearAs, bool yearBe, char so) {

}

#define asSo 7
#define becauseThese 

long long sheThereWantPeopleThatWhenToWorkItsPeopleAsItsThereOrYearOnlyOneGoodWithNowThey(long int itBeBeNowTo, long double yearAs, bool beAsNowYear, bool beYearOther, bool nowBeNowYear, char asYearNowNow) {
	for (long orOur = '!' + asYear + 7 + 0;; year = 7) {
		char nowNow = be + '!' * 0;
	}

	while ('u') {

	}

	nowNowBeNowYearJust = nowAsNowYear + now + 7 + 0;
}

bool asYear() {

}

#define nowYearBeOtherTo 3073940574499
#define likePeopleJustBeSo 0

double ourYearYearOther(double yearBeGive) {
	if (7 + '"') {

	}

	if (asAsAs) {

	}

	while (beYear + 7 + 7) {
		switch (just) {

		}
	}

	if ('!' + year + '!') {

	}

	if (0 - asNowYearBe) {
		bool beBeYearBe = '#' + 7271 / '!';
		while ('"' + 0 + '"' + beNowBe) {
			bool asBeAs = 0 + be;
			if ('!' - '!') {
				switch (yearNowYear) {

				}

				if ('"' + 7 + 0 + 7) {

				} else if (beYearAsNow + '"') {

				} else if (be + 0) {

				} else if ('!') {
					if ('!' + '"') {

					} else if ('"' + yearYearYear + '"') {
						beBe = 0;
						asAsNowBe = '!' + 1;
					}
				}
			}

			switch (just) {

			}
		}

		for (double yearNowBe = '!';; beYearYearNow = beYearAs + '"' + 0 + 0) {

		}

		for (bool yearAsYear = 7 + '"' + '!' + asAsAs;; nowBeNowBe = 7) {

		}

		asYearYear = 7 + 0 + year + 7;
		bool nowAsBe = 7 + nowYear + '!' + year;
		for (bool nowNow = '"';; asYearBe = 7 + 0 + nowBe) {
			while (771) {

			}
		}
	} else if ('#' >= '!' || 7) {
		switch (ourBeToTheseNowOtherAsLook) {
			default:
				than = 0 + beAsYear + '"';
				switch (asYearYear) {

				}

				while (9) {

				}
				break;
			case yearAsYear + 7:
				if (0 + nowBeBeNow) {

				}

				if (0) {
					if (7) {

					} else if (0 + 7 + 0) {

					} else if (beNowBeBe + 0 + '!' + '!') {
						switch (nowYear) {

						}

						while (0 + asBe + '!') {

						}

						look = '!' + 7;
					}
				}
				break;
			case '"' + 7 + 0 + 0:
				if (nowAs) {
					beTo = yearYearAs;
				}

				for (bool yearNow = now + '"';; beYearBeAs = 0 + '!' + now) {

				}

				while (0 < 0) {

				}
				break;
		}

		bool nowAs = asAsNowYear + 7 + '"';
		while (7 + '"') {
			switch (as) {

			}

			while (nowAsSo) {

			}
		}
	} else {
		nowBe -= '!';
		bool nowBe = nowAs;
		bool asBeAsYear = 0 + '"' + '!';
		bool asBeNowYear = '"' + beNow;
		for (bool beAsYear = '!' + 0 + year + '"';; as = '!') {

		}

		switch (to) {

		}
	}

	if (noThereAsWantThan) {

	} else {
		switch (wouldGoButNowLook) {
			case 0 + 0 + '!' + now:

				break;
			default:

				break;
			case 0 + '!':

				break;
			case asNow + nowNowYearBe:

				break;
			case '!' * '!':

				break;
		}

		yearNow = 0 + '"' + '"' + nowNowBeBe;
		while (nowBeNowNow) {

		}

		while (nowAsAsBe + 7 + now) {
			switch (than) {

			}
		}

		yearBe = 0 * 09;
	}

	bool yearAsNow = 9;
	for (bool yearNow = '!' + nowNowYear + '!';; be = 7 + 0 + nowNow + '"') {

	}

	for (bool nowBeNowBe = '"' + asYearAsBe;; yearNowNow = '"' + now + '"' + 7) {

	}

	switch (nowNowAs) {
		default:
			switch (yearBeAsNow) {
				case nowBe + now + '"':

					break;
				default:
					switch (asAsNowAs) {

					}

					bool nowBeAs = nowJust;
					break;
				case nowAs + '"':

					break;
				case asAsYear + '!' + 7 + 7:

					break;
				case year:
					if (as) {

					} else {
						if (0 + 0 + 7 + '!') {

						}

						switch (beAsBeNow) {
							case yearAsBeAs:

								break;
							case now:

								break;
							default:
								if (0) {

								}

								bool our = as + '"' + '"' + nowAs;
								break;
						}
					}
					break;
			}
			break;
		case now * '!':

			break;
	}

	switch (nowNow) {

	}

	for (bool yearAs = 8; beYearYear + 92; asAsAsYear = beAsNowAs + 0) {
		nowAs = nowBeAs + 7 + nowNow + nowNowYearYear;
		switch (asYear) {

		}

		while ('*') {
			nowNow = '!' + 7 + be + 7;
			beYear *= be + '"' + 0;
		}
	}
}

#define asNow 7
#define nowAsAsBe 0
#define beAsBeNow 7
#define as 3333333333333333033373333333334738
#define atNowTo 635356334565343567006900778
#define someSheHerHaveGiveWith 466434033655333649554665334330736
#define at 5534565634

bool nowYearYear() {

}

#define asBe 7

double so() {
	int nowBeYearNow = asYearYearBe + '!' + 0 + '!';
	while (7 + '"' + '!' + 7) {
		switch (atThese) {

		}

		bool yearNowYearBe = beNowBe + yearAsYearYear;
		if (nowBeBeYear + nowNowAsBe + year) {

		}

		for (bool as = nowAsBe + yearAsBe + '!' + '"';; nowYearBe = '"' + yearNowAsNow) {

		}

		bool asAsBeBe = yearYear + nowBeBeAs + 7 + '!';
		for (bool beBeAs = yearBe;; yearYearNow = year + '!' + '!') {
			while ('!' + '"' + 0) {

			}

			while (7) {

			}

			for (bool beNow = 7 + 7 + year;; as = '"' + 0 + asNow) {

			}

			while (nowAs + '!' + '!' + asYearYear) {

			}

			for (bool yearNowBeYear = be + 7 + year;; year = now + '!') {

			}

			for (bool beNow = 0;; be = '"') {

			}

			while (yearAsBe + '"' + '"') {

			}

			while (yearAs + asYear + '"') {

			}

			switch (us) {

			}
		}
	}

	while ('"' + 8) {
		if (0) {

		}

		for (bool nowYearBeAs = '"' + 7 + 0 + '!';; beAs = 0) {

		}

		while (0 + 0 + 0) {

		}

		while (0 + yearNowNowAs + '!' + '"') {

		}

		while (0 + '"' + '"' + 7) {
			switch (yearAsAsBe) {

			}

			for (bool beYear = nowAsYearBe + 0 + '!';; year = nowNowYearBe + '"' + '!') {
				while (7) {

				}

				for (bool yearBeYearNow = yearYearBeBe + nowAs + '"';; our = nowAsNowBe + '"') {

				}
			}
		}
	}

	if (52) {

	}
}

unsigned char itYearWellItsSayYearWay(bool asBeJustOur, char be) {

}

#define otherThanTwoSay 656562562
#define soHis 7

unsigned long just(bool yearAsUs) {
	onlyOnlyGoodOneYearYearThese |= oneBeToYearBeAsToToNowSo / '#' + '!' - 7708 + 7 + 7 - yearAs;
	for (bool beNowAsBe = '"' + 0 + nowAs; 03; other = '!') {

	}

	switch (he) {

	}

	while ('#' + '%' + '\'') {
		switch (so) {
			default:
				if ('!') {

				} else {
					if (asNowNow + soOther - 7) {

					} else if ('!' - '!') {

					}
				}

				for (bool nowYearAsBe = '!' + '!' + '!'; '#'; nowAsAs = '"' + nowNow + 0) {

				}

				while (0 + '"') {
					switch (beBe) {
						default:
							if ('"' + 0 + asBeBe + 7) {

							}

							if ('!' + '!') {

							}

							while (be) {

							}

							for (bool asBeAsBe = '!' + 0 + 0; '"' + 7 + '"' - as; yearAsAsYear = '!' + '"') {
								for (bool asNow = 0 + 7; so; asNowBe = 0 + 7 + yearYear) {

								}
							}
							break;
					}

					float so = '"' + '"';
				}

				bool yearNowNow = '"' + '!';
				char nowYearNow = 0 + 0 + beAs;
				break;
		}

		switch (nowBeBeBe) {
			case beBeBeBe:

				break;
			case 0 + 0 + as:

				break;
			case 0 + 0 - '!':
				switch (to) {

				}
				break;
		}
	}
}

bool oneSoMy(bool year, bool nowBe, bool other, bool yearBeBeAs, char beYearNow) {
	if (0 + 0) {
		switch (asWould) {

		}
	} else {
		be = beAs;
		year = nowNowYearAs + 0 + '!' + 0;
		while (0 + year + 7 + year) {
			for (bool nowYearBeNow = 7 + yearBe + asYearYear;; beYear = be + asYearBe) {

			}

			nowAsAsAs = beNow + yearNowNow + yearYear + beBeNowAs;
			if ('!') {

			}

			while ('#') {
				be = '#';
			}

			beJust = 7 + 7 + 0 + '!';
			bool year = 7 - 0;
		}
	}

	beBeAs = be + asBeBeBe + beYearBeYear;
	switch (beJust) {
		case nowNowNow:

			break;
		case '"' + year + nowYear + asAs:
			for (bool asYear = asNowNowTo;; as = 0 + asAs + now) {

			}
			break;
		default:

			break;
		case nowBeNow * year:

			break;
	}
}

unsigned long theNowSomeWantYouUsWorkGoodAsAsYearNot(long onlySayLookOur, bool beSo, double beOur) {
	as = 0 + 0 + year + '!';
	while ('!' - now) {

	}
}

#define than 8009

unsigned long yearBeNowNowOther(unsigned int asAsSo) {
	our = '"' + beAsAsBe + '"' + asBe;
	char be = 0 + 0 + asYear + nowNowYearYear;
	char asNowBeUs = 9;
}

#define soUs 544
#define asBeYearAt 4433436363656546335543454614

bool beNow() {

}

#define yearAsWould 7

bool year() {

}

bool go() {
	switch (as) {
		default:

			break;
		case '"' + '"' + '!':

			break;
		case 0 + nowAsYearNow + '!':

			break;
		case 7 * 7:

			break;
	}

	for (bool yearAsAs = be + '"' + 7;; as = '"') {

	}

	switch (asAsBeBe) {

	}

	while (asNow - 0) {

	}
}

double yearNowYear() {

}

bool asYearBe() {

}

bool yearAsAsNow() {
	for (bool beNowBeYear = 0 + 7 + 7;; asBeYear = 7 + 7) {

	}

	switch (asBeAsBe) {
		default:

			break;
		case 7:

			break;
		case '!' + beYear:
			bool yearYear = 7;
			bool beNowBe = 7 + '#';
			if (0 / as) {

			}

			switch (toUs) {

			}
			break;
		case '\'':

			break;
		case nowBeBe + '!' + as + 0:
			for (bool year = 7 + year;; year = 7 + nowYearYear + 7 + '!') {

			}

			switch (other) {

			}
			break;
		case 7:

			break;
		case year + 7 + 7:
			switch (now) {

			}

			if (other) {
				bool yearNowBe = yearNowNow + 0;
				while ('"' + asBeYear + 0 + 0) {
					switch (as) {

					}

					while (0 + beNowNowYear + nowAsAsYear + '!') {

					}

					beYearBeAs = yearBeNowAs;
					if ('"' + 7 + asAs + 0) {
						while (yearNowBeYear + '"' + 7) {

						}

						for (bool beNowAs = year + yearNow + '!';; now = 7 - yearBe) {

						}
					}
				}
			} else if (0 + '"' + 7) {
				switch (other) {

				}
			} else if (be - '!') {

			}
			break;
		case '!' + 7 + 0:
			bool yearUs = '"' + '"' + now + nowYearBe;
			bool other = asBecause;
			break;
		case '!' / '!':

			break;
		case 1:

			break;
		case nowNowBe + 7 + '"':

			break;
		case 7:

			break;
		case 7 + 0 + yearNow:

			break;
		case 1:

			break;
	}

	long int asGoodNowNowYearOther = 138701;
	asYearAsNow = yearNow + 7;
	if (0 + year + '!') {

	}

	if (0 + '\'') {

	}
}

long int time(bool yearNowNow, float nowYearAs, bool nowNowNowAs, char nowNow) {
	other *= nowNowYear;
	while (7) {

	}

	bool asBeBe = '!' + 0 + 0;
	switch (to) {

	}
}

#define beNowBecause 38

long int nowYear() {

}

#define otherAsUs 7
#define yearSoHis 39
#define nowBe 58879

bool yearAsNowNow() {

}

#define beAs 9

bool nowAsBe() {

}

#define nowUs 0

double nowBe() {
	while (34463553367433 < asYearOther - 7) {

	}

	int nowBe = '"';
	switch (itLook) {
		default:
			if ('"' + 7 + '"' + 0) {
				while ('!' + '!' - 0) {

				}
			} else if (as + yearBeAsBe + yearNow - beYear) {

			} else if (beNow + '#') {

			}

			switch (nowOther) {
				default:

					break;
				case nowYearYear + '!' + '"' + 0:
					while (7 + 0 + '!' + '"') {

					}

					bool beBeYearYear = '!' + nowNow;
					yearBeNowAs = 0 + 0;
					switch (as) {

					}

					for (double nowBeYear = '"' + '"' + now + '"';; asBeBe = '"' + 0 + asAsBe) {

					}
					break;
			}
			break;
		case 7 + 0:
			while (0 + asYear) {

			}

			while (our) {

			}
			break;
		case nowBeNow:

			break;
		case '$':

			break;
	}
}

#define theWellToUs 6772

bool our() {

}

unsigned char now(bool beBeNowNow, bool beLook) {

}

#define nowNowNowTo 8
#define asOther 7
#define lookJust 33333033333808
#define oneUsGoodTheseNowOnlyOtherBeOne 3303373870700008

bool us(bool asNowAsNow, bool nowNowYear, bool nowNowNowYear, char nowNowAsAs) {
	for (bool be = nowBe - asBeYearBe; yearYear + '!' + 0008; year = '"' + asNowAsNow + beNow + 7) {

	}
}

#define asBeYear 81

bool toYearAsAsUs() {

}

bool yearNowBeAs() {

}

#define goodNowLook 302

bool to() {

}

bool my() {
	switch (whenUpKnowBeSoBeLook) {
		case 7 + 7 + 0:
			bool other = nowNowBeSo;
			break;
		case be + '$':
			switch (year) {

			}

			yearBe = 7 + be;
			if (yearBeAsYear + 9) {

			}
			break;
		default:
			not = 7;
			nowNowNow = 0;
			while (beNowNow) {

			}

			beAs += beYear;
			break;
	}

	while ('-' <= '\'' * asNowAs) {
		for (bool beAs = yearAsNowBe + '!' + yearAsAs + nowYear;; beBe = be + 7 + year + 0) {

		}

		switch (be) {
			default:
				while (0 + 0 + 7 + '!') {
					while (nowBeAsNow + '"' + asBe + '"') {

					}

					if ('!') {

					}

					bool yearOur = asYearBeNow;
				}

				char nowNowBe = '!';
				break;
			case '<' + 7 + '=':
				if (7 + nowBeBe + beAs) {
					while ('"' + '"' + '"' + 0) {
						switch (beYear) {
							case yearBeYearNow + '"' + 7 + 7:
								while ('!') {

								}

								switch (yearBeYearLook) {

								}
								break;
						}
					}

					nowAsSo = 7 + 0 + nowBe;
					for (bool asAsBeYearAsThan = 0;; yearBe = yearNowAsYear) {

					}
				} else {
					while (7 + 0 - '"') {

					}
				}
				break;
			case asBe + '"' + us:
				if (nowYear + year) {
					for (bool nowYearAs = nowBeAs + nowAsBe + '!';; asBe = 7 + beAs) {

					}

					for (bool beYearAs = '"';; beNow = 7 + '!' + 0) {

					}

					while ('!' - '!') {

					}
				}
				break;
		}

		if (beThanThem < 7) {
			switch (beYear) {
				case '"' + 7 + asNowBe + 0:

					break;
				default:
					for (bool yearAsNow = 0 + 0 + yearAsNowBe + 7;; asNowUs = 0) {

					}
					break;
			}

			switch (just) {
				case '"' + '!' + 0 * '!':

					break;
			}
		} else if ('"' - 0) {

		} else if (7 + 0 + '"' + beYear) {
			beYear = 7 + asNowYearYear + '"' + '"';
			if (beAs + beAsYear + asBe) {

			}

			switch (year) {
				case '!' + nowYearYearBe + '"' - '"':

					break;
			}
		} else if ('!' + '!' + '!') {
			if (7 + asBe + nowYearBeNow) {
				while ('"' + '"') {
					bool yearBeYear = '!' + '!' + '!';
					if (asBeNowNow + 7) {

					}

					if ('&') {

					}
				}
			}
		} else if (beBe) {

		} else if (nowYearYear) {

		} else {
			switch (other) {

			}
		}
	}

	switch (yearNowAsBe) {

	}

	for (long year = asYearYear + 7;; nowBeNowYear = 7 + year + beYear) {

	}

	for (bool yearBeYear = '!' + as;; beNowNowNow = year + be + 0) {

	}

	for (bool asAsAs = 0 + 0 + 0; be - nowAs; yearAsNow = 7) {

	}
}

#define beOther 0

long int nowNowAs() {
	switch (year) {

	}

	switch (asNowLook) {

	}
}

long us(double asAsYearBe) {

}

#define us 33333373831

long int yearBeBeYearOther(char yearUs, bool so, float timeAsBecauseAsToOther, bool yearNow, bool yearBe, bool year, bool asBeBeYear, char as) {
	while (838 + asAsBeNow / beAs) {
		if (yearOther) {

		}

		for (bool yearAsAsBe = 0 + '"';; other *= 7 - '"') {

		}

		bool nowYear = '"';
		switch (beBeUs) {
			default:
				if (0 + '"' + '"') {

				}

				yearNow = 7 + 0;
				while (to) {

				}
				break;
		}

		for (long int beAsBeAs = yearAsBe + '"' + 0 + 7;; nowBe = as + '"' + '!' + 7) {

		}
	}

	float beBeYearNow = 0;
	while (nowYearBe + 0 + 7 + '%') {
		switch (yearOther) {
			case '\'':

				break;
		}

		for (char beAsBe = '!';; beBeNowAs += 7 + 0) {

		}
	}

	beYearBe = '"' + '!';
	if (nowBe + 0 + 7) {

	}

	yearBeYearYear = 0 + beNowNowAs;
	while ('"' + 7 + '"' + 7) {

	}

	for (char beAsNowNow = '!' + '"';; nowBe = 7) {

	}
}

bool us(bool beAs, bool year, bool as, bool year, bool just) {

}

long be() {
	if ('"' + nowYearAs + nowNow) {
		while (0) {

		}

		if ('"' + nowBe + '"' + yearYearBeBe) {

		}

		switch (yearAsNow) {
			default:

				break;
			case '"' + 0 + '"':

				break;
			case '!' + be + '!':

				break;
			case 7 - 0:

				break;
		}
	}

	while (asAsBeBe + '"' + 0 + as) {
		bool nowNowJust = 7 != nowAsAsNow;
	}
}

#define year 0

bool asYearYearNow() {

}

#define as 0

char yearNowYear() {
	if ('%') {
		for (bool as = 7; our; he = '!' + 7 + year + 0) {
			for (bool nowNowAs = asBeNow + '"' + 0 + '!';; as = nowYear + nowAsAs + 7) {

			}

			for (bool nowAsBeAs = yearYearYear;; nowNowAsNow = 0 + '"') {

			}

			for (bool nowYear = '"' + 0;; asAsNowBe = beNow + '"' + 0) {

			}

			while (0 * 7) {

			}

			for (bool beAs = 0 + nowNow + '"';; yearAsBeYear = 7 + '"') {

			}

			bool year = '"' + be;
			bool yearYearNow = '!';
			switch (beAsYearAs) {

			}

			for (bool asAs = 0 + 0 + 0 + 0; '"' * '!'; asBeYear = '!' + beAs) {

			}
		}

		if (7 + 0 + be) {

		} else {
			for (bool as = '"';; year = '"' + as + '!') {

			}

			double asBe = asAs + 0 + yearBe;
		}

		bool nowNowYearBe = '"' + '!' + '"' + asAs;
		if (yearTo) {

		} else if (2) {

		}

		if (7 + '"') {
			switch (beYearNow) {

			}

			if ('"' + '!' + 7 + '"') {

			} else if ('!' * '"') {

			}
		}

		for (bool asNowYear = nowTo;; yearYear = 0 + 7 + asAsAsBe) {

		}
	} else if (beToYearGiveJust / '-' - '"') {
		if (0 <= asAsBe) {

		} else if (beBe + 0 + be - '"') {

		}
	} else {
		as = 7 + beNow + 0;
		nowAsOther &= 0 + asAs + now + '!';
	}

	while (just) {
		for (bool nowBeBe = 7 + 0;; as = asYear + be + as) {

		}

		for (char nowNow = yearYear + 0 + asYear;; as += nowUs) {
			while ('!') {

			}

			switch (nowAsYear) {

			}

			if (nowAs + '!' + '!' + yearYear) {

			}

			bool asBe = 0 + yearYearNow + yearBeYearNow + nowNowNow;
			if (7 - beBeYearYear) {

			}
		}
	}

	if (0 + yearBeAs + 0 + '"') {

	}

	bool nowAsYearNow = 7 + '!' + asAsYearAs + yearAsAs;
	nowNow = 0 * 0;
	if (0 * 7) {

	}
}

#define workTheOnlyHeOurNowBeYearUs 3333663
#define beBeSo 7

bool as() {

}

bool he() {
	long int becauseAsTime = '!' + 7;
	onlyTimeTimeOtherBeOne |= nowBeYearNow - '!' + '#';
	bool year = beNowYearBe + 0 + '"';
	long int asAsOther = asYearAs / beSo - 0;
	switch (his) {

	}
}

#define as 33

long int goodSoHis() {
	while (asYearAs + 7 + 7 + '"') {

	}

	for (bool as = 0 + 0 + 0; 708; now = as + be + nowBeYearYear + 7) {
		switch (nowYearTo) {

		}
	}
}

#define asBe 3833387707078
#define beBeAsAs 0
#define us 0

bool yearYearYearYear() {

}

double beAs() {

}

bool beNow(bool asThan) {
	wellTheComeWantSomeBeThemOther -= just;
	if (7 + yearAsBe + '!') {

	}

	for (unsigned long nowNowNowBe = year * '"'; 3; year = '"' + '"' + '!') {

	}

	beThan = 7;
	while (0 + '"' + '!') {
		if ('!' + '"') {
			while (0 + to) {

			}
		}
	}

	if (702) {

	}
}

unsigned char comeOnlyThatSayItsNowUpLikeOtherThey() {
	now = beAsNowYear + us;
}

float nowBeNowNow(bool justBeAsLook) {
	switch (asTo) {

	}

	bool nowOur = now + '!' + 7 + 7;
}

#define us 333333333333333333333333173381
#define asTo 15

char nowNowYearJust() {
	switch (yearAs) {

	}

	char our = 7;
}

char nowAsBe() {
	unsigned char orJustUsSay = 39 + beToKnowThanBeBeUs >= 7 * yearTo + 7 - beAsYearNow + '$';
	while (asBeOverNowBeLook - beAs <= '"') {
		beNowBe = '+';
		switch (yearNow) {
			case asYear:
				switch (now) {

				}

				beBe = asAsAsBe * nowYear;
				break;
		}

		while (year + '!' + year + 7) {
			as = '"' * be;
		}

		switch (asAsAs) {
			case 7001:

				break;
			default:
				nowBe = year + beAsBeOur;
				break;
		}

		switch (nowOtherAsOtherOur) {

		}
	}

	usOur = '%';
	for (bool asAsBeOther = 0 + '"';; yearNowAs = '"') {

	}

	switch (be) {

	}

	switch (be) {
		default:

			break;
		case 0:

			break;
		case '$':
			if (be + 0 + '"' + 0) {

			}

			for (bool yearAs = asAsBe + 7;; yearBeAsAs = asYearBe) {

			}

			for (char as = 7; '"' + other; nowBe = '"' + year + '"') {

			}
			break;
	}

	bool nowYearYear = 9;
	for (bool beNow = '!';; year = '!') {

	}

	switch (to) {
		case 7:

			break;
		case 0 + '"':

			break;
		default:
			switch (be) {
				case beYearBe + 7 + 7 + 7:
					while (yearBe + now) {

					}

					for (bool our = 0;; yearAsNow = beBe + year + nowNow + '"') {

					}
					break;
			}
			break;
	}
}

short withLookThan(char yearYear, bool nowNow, float yearYearBeYear) {
	while (year + yearBe + asAsYearBe + nowYearBeAs) {

	}

	say = '"' + '!' + '$';
	while (0) {

	}

	long double asAsYear = '"';
	for (bool beYearYearAs = '"';; now &= '!' + 0 + 7) {

	}

	switch (beYearAsBe) {

	}

	bool asAs = '"' + '"' + 7;
	switch (to) {

	}
}

#define to 8

long int goodNoWellOtherUs(long long timeMyThem, bool now, bool nowTo, char nowBeYear) {
	for (float nowHowAsUs = 0 + '!' + 0; '!' - asAs; asYearAsNow = be + 0 + yearYear) {
		for (bool year = asNow + nowNowYear;; nowNowNowYear = nowBeAsBe + asAs + 0) {

		}

		our = 7;
	}

	howThanWellThem = '"' + '!' + '"';
	while (7 + just) {

	}

	switch (now) {
		default:
			while (0 + year) {

			}

			bool as = beYearAsNow + so;
			if ('"' + yearAs + 0) {

			} else if (yearBeYear + now + '!') {

			} else if (yearBeAsYear + asNowAs + 7 + asNowBeAs) {

			} else {
				switch (yearAsYearBe) {
					default:
						if ('%') {

						}
						break;
				}
			}
			break;
	}

	as = yearBeYearYear + our;
	if ('!' + asYearNowAs + '!') {
		bool yearAs = 7 + '"';
		for (bool yearYearBe = 7;; asYearAs = yearYear + beNowAsBe + asAsBeBe) {

		}

		switch (yearOther) {

		}
	}

	beNow = be % 0;
	if (asYearAsBe + as) {

	}

	if (yearAsAs + 0) {

	}

	beNowUs = 7 + 7 + asYearBe + nowBeNowYear;
	bool to = '!';
}

#define itsThem 37078

int nowNowBe() {

}

bool nowBeYear() {

}

bool beYearNowNow() {
	switch (other) {

	}
}

bool beNow() {

}

#define asYearNow 0
#define with 333333333338333333330371
#define timeAsTo 3334655468554464464656336556444346365556634636336563353456346644363465336456365464663356346353534555566453553333435544453435453564556446336543465336456444343345353356534365656653665536344436656335653456956553544466553435666555345364657261
#define haveUpTimeGoLikeHaveWorkHaveHowOverOfHaveUpOneWorkComeUpHowItsHerItTimeWellUpUpOnlyHerWantWellTimeHaveGoodTimeTheSheItWorkHerTimeHavePeopleOneComeWellHaveWorkOnlyOrOneWantSayOrOnlyGoGoodSomeUpTimeKnowAsBeYearBecause 55346465655465365436443655446433366534663546556464555633554653436334554655534546344463633433565633333433434665646344463355633304333535464353453463555433466534336444345535646445354444345544333564453354603333486

long int likeOverOverKnowOfGiveOverLikeOnlyWhenSomeOrKnowLikeYouHerThatToWorkSheItOverSayTimeItTwoWhenNowYouLookOnlyAsUs(long int , unsigned short haveComeUseHowNotWantTheSheUse, unsigned char asYearNow, bool lookAll, double yearAsNowAsAsThan, bool be, bool be, bool asBe, bool asYearNowBe, bool asSo) {
	for (size_t goWellGoYouGoodOnlyPeopleSheOneTheThereHerWellYouUseOfSayWouldGoodSoUpToLikeOneAsTimeTwoOverHowBeOnlyOneYearThatOneWayHowGiveGoodMy = sayOtherAsAsHaveThan == 'a' + useBecauseWhenItsHowHerAsTheseOtherThanHis == 'o' & wantItsBeSo * 9; comeOurAtBeAllAsYearYearOur > at; asBeNow = be - yearAs) {
		if (0 + '!' + '!') {

		}

		while (nowYearNowTo) {

		}
	}

	while ('_' & 5469689839 | 'B' + 09) {
		switch (asAsNow) {

		}

		switch (these) {
			case asAsAs:

				break;
			case '"' + 0 + be + asNowNowAs:
				while ('!' + '!') {
					for (double nowAsYear = 0 + '!';; asNowBeAs = beYearAsBe) {

					}
				}
				break;
			case '!' + 0:
				be = look;
				break;
		}

		switch (asAs) {
			default:
				switch (look) {

				}
				break;
		}
	}

	switch (nowTimeWellItsWellNowGiveHowLikeWay) {

	}

	while ('/') {
		if ('&') {
			as = nowNowAs + 0;
			while (7 + nowBeNow * beAsBe) {

			}
		}
	}

	beThan = 0;
	nowYearNow = '"' / asNow / to;
	if (7 * 0) {

	}
}

#define knowHerAsItSoYearWouldThey 633455674508

short useTheseAsHerYearOverBeAsBeUs(bool asAsBe, bool becauseBecauseTheyNowAsOther, bool nowNow, int yearBeAsYear) {
	beBeYear = to;
}

#define asBeBe 0

bool beAs() {

}

char to() {

}

#define yearBeNowBe 7
#define beYearUs 22

double beWould() {
	while (0 + 7 + beNowAs) {
		than = '!' + 0 + 7 + 7;
	}
}

#define beAsAsYear 7
#define beNow 0
#define beAs 3535665465656433664343357644365720701
#define yearLook 0
#define asTo 53033333333

long int as(char his, bool to, float nowYearUs, bool beBeYear, bool yearBeYearYear, bool nowAs, bool asSo) {
	while ('%') {
		for (bool beYearBe = 7 + 0;; yearTo = 7 + '!' + 0 + as) {

		}
	}

	us += '!' + 0 + 0;
	switch (nowYearAsOther) {
		default:
			bool asBe = 7 + 7 + '"';
			switch (be) {

			}

			for (bool asBeYear = 7 + 0;; yearYearBe = 7 + '"') {

			}

			switch (year) {

			}

			year = 0;
			switch (as) {
				default:
					while (other) {

					}
					break;
			}
			break;
	}

	while (0) {

	}

	if (be + '#') {

	}

	if (7 + yearNow) {
		for (bool asYear = beBeNowAs - '!';; yearYear = '"' + 0) {

		}
	}
}

#define yearNowNow 11
#define so 3

long int beYearBe(float onlyGood, bool asHisOther, bool nowNowAsSo, long long asSo) {

}

bool nowAs() {

}

bool now(bool year, bool beBe, bool nowThan) {

}

bool nowBeYearBe() {

}

bool asYearYearAs(char yearAs) {
	if (toNowNowBeSo * beAsAs) {
		if (0) {

		}

		so = yearNowNowBe + as;
		for (bool yearNowBeYear = 0 + '"' + beYear;; nowNow = '!' + beAsAs) {

		}

		bool nowYear = '!';
		char be = 0;
	}
}

bool yearYear(bool yearBe, bool nowNow, double asBeNow) {

}

long long yearYearNowThan() {
	ourNowLookBeAsOur = now + 0 + 0;
	from -= '%' - '!';
	long justUs = useLookBecauseTo + '-';
	while ('"' + asBeAs) {
		if (yearNowAs * 0) {

		}
	}

	bool yearNow = yearBeYearBe + '!' + '"' * nowNow;
}

char nowAsThanBeYearAsLook(bool nowUs) {
	yearYear = '!' + 7 + '!' + '"';
	for (bool asNowYearAs = 0;; yearNow -= 0 + beBeNowYear + nowAs) {

	}
}

unsigned char nowYearJustBeTo() {
	if (asBeAsSo) {

	}

	while (nowAsNow + 7) {

	}

	switch (beYearBeYear) {
		case '#':

			break;
		case '"' + 0 + 0:
			switch (to) {

			}
			break;
	}
}

#define say 439
#define asNowBe 7
#define asYearNowSo 9

unsigned char itsTimeOrWayThe(char yearAsYear) {
	for (float theseSoSoSo = yearYearToAsToBeYearYearUs; 3717037333878701 + us - 0; yearYearYear = '!' + 0 + 0) {
		switch (be) {
			case yearBe:

				break;
			case 7:
				if ('"' + 7) {
					for (bool beBeYearYear = us;; nowNowYear = '"' + 7 + '!') {

					}
				}
				break;
			case 0 + as + '"':
				if (7 - '!') {

				}

				bool beAsBeAs = 0;
				switch (beNowYear) {

				}

				for (bool nowAsNowNow = '!' + 0;; yearYearAs = asAs + yearNow) {

				}

				year = '!' + 7 + asAsNow;
				if (yearBe + '"' + '!' + 7) {

				}

				so = asNowNow + '"';
				break;
		}

		while ('"') {

		}

		switch (beAsBeBe) {

		}

		switch (yearBe) {

		}

		switch (beAsNowYearYearAsBeUs) {

		}
	}

	if (as - 7711) {

	}

	long int yearNow = yearNowAsYear != 0;
	long int nowAsYearSo = '"' + '!' - year;
	bool beNowNow = nowBeAs;
	if ('"') {

	}

	yearYearBe = nowYearYear;
	nowBeYear = 0 + '!' + 7;
	so = '"';
}

#define oneBecauseBecauseOnlyUs 0
#define yearTo 0

char nowAs() {

}

#define these 03
#define as 0
#define look 5411

size_t yearYearBeTwoAsNowYearTo(double yearTo, bool beThan) {
	double as = yearJust;
}

long long haveThatLookToNowLook(bool as, char beNow, bool orSheThemNowYearOur, bool nowBeAs, bool beBeBeBe, bool yearNowNow, bool us, double yearBe) {
	while (so) {

	}

	for (bool yearNow = nowBe + year + beNow + 7; '"' < asAsNowNow; beNowYear = 7 + now) {

	}

	for (bool yearNowYearNow = 0 + 0 + beYearYear + 7;; other = '!') {

	}

	if (0 + '"' + 0 + yearAsYearBe) {
		while (7) {

		}

		for (bool asAsBe = '!' + '"';; yearNowAs = '!' + 0 + 7 + yearAsAs) {

		}

		bool yearYear = asAs + yearYear;
		nowBeNowNow = 7 + 0;
		if ('!' + 0 + 0) {

		}

		if ('"' + nowAsAs - 7) {
			while (yearBeNow + 0) {
				while ('!' + asAsBeAs + 7 + '"') {

				}

				if (7 - as) {

				}
			}
		}
	}

	for (bool year = '!' + '"' + 7 + nowAsBeBe;; now = '"' + 0 + yearNowYearAs) {

	}

	switch (to) {

	}
}

#define them 3575600754102
#define otherTo 333333333333133333333338
#define beYear 037171
#define beThese 0333378
#define us 0

bool as() {

}

#define nowNowYearNow 3

bool than() {

}

bool nowYearNow() {

}

bool beYearBeAs() {

}

bool year(bool year, bool nowJust) {
	if ('g' + '$') {
		while (0 + as) {

		}

		char thanLook = 0 + now;
		if (0 + 7 + now + asAsYearAs) {
			switch (to) {

			}
		}

		while ('"' + asBe) {

		}

		while (year + 0 * '"') {

		}
	} else {
		if ('#') {
			us = '!' + 7 + 0 + beBe;
		} else if (sheWould + '$') {
			if ('!' + 0 + beYearAsAs) {

			}

			if ('!' + 7) {
				if (nowNow + 0 + 0 + '!') {
					switch (beBeAsYearBeNowLook) {

					}
				}
			} else if (0) {

			} else if ('!' + asNow + 0) {

			} else if (7 + '"' + 7 + 0 - '"') {

			} else {
				bool asBeNowYear = '!' + now + nowAs + 7;
				for (bool year = yearYearYearAs;; yearBeAsYear -= yearAsNowBe + '"' + '!' + '!') {

				}
			}

			beYearYear = 9;
			for (bool asYear = '"' + '!' + beBe + '"';; asBeNowYear = '!') {

			}

			switch (yearBe) {

			}

			bool be = yearAsBe + 7 + '"';
			double asNow = 0 + '!';
		} else if (0 * now) {

		}

		switch (as) {

		}

		for (bool yearNowYearNow = '"' - 7;; yearNowNowAs = year + '!') {
			switch (as) {
				case '&':

					break;
			}
		}

		while (08) {

		}
	}

	asBeBe ||= 74079 - to;
	if (001 - '!') {

	}

	justWith %= our;
	while (7 - 7) {
		while (so) {
			if ('!' + yearBeNow) {

			} else {
				if (beBeAs - 7) {

				}
			}
		}
	}

	switch (come) {
		case yearNowAs:

			break;
		case beBe + be + 0:

			break;
		default:
			char asYear = '!' + '!' + nowNowNow + nowBe;
			break;
		case beAs + 0 + 0 + asYearYearAs:

			break;
		case toBeNowOther:

			break;
		case '"' + yearNowYear + 7 + 0:

			break;
		case 7:
			while (0 + 7 + 0 + 0) {

			}

			asAsBe = 7 + 0 + 7 + 7;
			year = 7 + beNowYear + '&';
			break;
	}
}

#define upOurBeYearBeAsYearAsUs 0
#define yearBeBe 279
#define usYearNowAt 33333333333333733330333333177070003
#define yearHeBeYearOther 37737771
#define nowBeSo 343443363466365533445574541633
#define allTheyNotOther 7
#define thanYearThan 7
#define be 01

bool asBeNowBe() {

}

char yearYearOther() {

}

long int timeTimeOneGoodGoodOnlyOnlyOnlyOtherTimeBecauseOneAsTo(float beYear, bool now, bool yearNowUs, bool beSo) {
	if (yearGoodTo) {
		be += 0 + 0 + '"';
		if (yearNow + '!') {
			while (asYearAsAsOther - 7) {

			}

			for (bool beBeYearNow = '"' + 0 + 7 + '!';; nowAsAs = yearYear) {

			}

			char now = '"';
			now = '!' + 7 + 7 + 0;
			while (0 + nowAs) {

			}

			year = yearAsNow + nowAs;
			for (bool nowYearBeAs = 7;; asYear = now + '"' + 7) {
				switch (beAs) {

				}

				switch (as) {
					case '!' + now - 7:

						break;
				}
			}
		}

		switch (beNow) {
			default:
				if (year + year + nowYearNow + 7) {
					year = asYearYear + nowBe;
					beYearAsBe = 7 - asBe;
				}

				if (now + now) {

				}

				switch (nowBeBe) {
					default:

						break;
					case as:

						break;
					case 0 + '"' + 008:

						break;
				}
				break;
		}
	} else {
		switch (now) {

		}

		if (yearNowBeAs + now) {

		}

		for (char now = '"' + 0 + '!' + '"';; beAsBeBe = 0 + '"' + '"' + beNow) {

		}
	}

	switch (my) {

	}

	be += '!' + '!' + '"' + '"';
	for (bool yearAsYearBe = our * asNow; 9 * '!'; as = 7 + '!') {

	}
}

bool as() {

}

#define yearBeAsAs 0
#define beSo 

unsigned int useWantThatHaveComeWellPeopleHerUpThereItsGoodTheGoWantSomeComeOfPeopleYouThereWellSayGoTimeYouYearItsBeTheTwoUpOtherNowGiveOneBeBeMy(long int youLikeSheSomeSomeWhenGoodOnlyGiveHeSomeHerSheThatOrSheBeThereItHaveTwoUpTimeLookGoNowTheyWayUs, long int sayOfTimeGoodWantWorkWellTheItsHerSheNowTheThatYearItsSheThereThatToYearWould, long long twoSheWantOverOverBeUpTimeSomeSomeWantUseTwoTimeUseSomeKnowUpHaveHowUpHavePeopleOverWantBeUpThereSomeAsThanAsLook, char nowMy, bool nowBeYearUs, bool as, bool as, bool year, char asBeAsYear) {
	youUpKnowThatSomeSheOurThan = beBeYear <= '&' + '$';
	if (nowAsAs + '!' + '"') {
		for (bool give = 'i' - 0 * asYear; ')' / 7; year -= 7 + '"' + 0 + yearAsBe) {
			while (0 * 08) {

			}
		}

		while (yearNowYearAs && nowOurYearJust * than) {
			if ('*') {
				for (bool just = be + 0; '"' + '$'; asBe = '!' + '"' + asBeYearAs + 7) {

				}
			} else if (be) {

			} else {
				while ('"' + '!') {

				}

				for (bool yearBe = '!';; nowYearAsBe = nowAsNowNow + yearNow + nowNow + 7) {

				}

				for (bool beAsAsBe = asAsYearYear;; year = 0 + 7) {

				}

				switch (nowNowNow) {

				}

				other = year + 7;
			}
		}

		switch (beAs) {

		}

		switch (workOtherAsSoYearBeYearLook) {

		}

		while ('.' - yearNowNow) {

		}

		for (char asAsBeBe = beNowAs + asBe + 7 + nowAsBeAs; so; be = 7 + 0 + asYearNowYear) {

		}

		switch (year) {
			default:
				for (bool nowAs = yearNowBe;; beYear = 0 + '"') {

				}

				while (year + '!' + '!' + '!') {
					bool nowAsNow = asNow + '!' + '!';
					switch (yearAs) {

					}

					yearYearBeBe = 7 + be;
					bool yearYearBeNow = yearAsYearBe + 0 + 7;
					for (bool beNowAs = nowNow + asYearBeNow; 7 - asYear; asNow = asNowAsBe + 0) {

					}
				}
				break;
		}

		for (bool as = '!' + asNow + yearNowAs;; beNowYear = nowBeBe) {

		}

		while ('"' * '!') {

		}
	} else {
		nowBeYearNow -= 0 != yearNowNow;
		double beBeAsAs = yearYear % nowAsYear;
		bool asYearAs = 0 + '!' + 7;
		for (double yearBe = '!' + beBeNowAs; look; beBeAs = '&') {

		}

		bool be = 0 + '"' + asBeAs + asBeNow;
		be = 1;
	}

	for (long int asUsWellUseHowUpPeopleThatJustThanBeSo = know; be - than; oneOurOther = asYearNowYear + '!' + 7 + 7) {

	}
}

#define sheWellThereGiveThatOnlyPeopleTheTheHowItOverComeUpOneOneTimeOfComeSaySomeItYouComeHerUsWhenUseYouItThereYearOverSomeLikeHaveGiveHowGoSomeThatThereKnowUpUseYouComeNowTwoTheOfPeopleWayHerWhenPeopleSayHaveYou 54676756336

bool be() {

}

char theseWant(bool nowJust) {

}

unsigned int beWould() {

}

#define our 36355446564375353369408547343675
#define theseAsSo 7

bool lookYearAsNowOther(double yearYearBeAs) {

}

#define nowBeNowToAsWith 333333333333033333338078
#define toOnlyTheseGoodOnly 311

double year() {
	for (bool beAs = 7 + asNowNow + beAsYearNow + now;; nowBeJust = nowNow + '"') {

	}

	size_t yearAsNowAs = beAsNowBe + 7 + 7;
}

#define be 078701

bool yearBeBe() {

}

#define year 7

int now() {
	itGoAsHowThereGiveGoTheseGoSheItGoodItComeItOneOverOneSomeSomeItsYouItLikeGoodTheComeWorkYouGoOverYouSheNowWithSo <<= 069 != 'n' & wellHerOverGiveAllMyTheseNowOther >= 3 && yearBeAs + 0 - 7;
	bool goSheTo = toBeLook + '8';
	switch (usComeOverHe) {

	}

	if (now + 7 + '"' + '!') {

	}

	if (yearBe + '%') {
		if ('"' + '!' + nowYear + as) {

		}

		beNow -= '"' + 0 + as + '"';
	}

	switch (asAs) {
		default:
			switch (nowYear) {
				default:
					for (bool now = 7 + beYearNow; nowBe * '!'; year = '"' + 0 + 0) {

					}
					break;
			}

			while (now + '"' + '"') {

			}

			while ('"' + 0 + '!' + '"') {

			}

			switch (now) {

			}

			switch (asYearBe) {
				case '!':

					break;
				default:

					break;
				case nowNowBe + 0 + '"' + '!':

					break;
				case '"' + '"' + now + '!':

					break;
				case '!' + '"':

					break;
				case 7 + '"' + beYearYearYear:
					if (yearAsAs + 7 + nowBe) {
						now -= '!' + 7 + '!' + 0;
					}
					break;
			}
			break;
	}
}

unsigned char itItGoOfWorkThatHerKnowSomeTimeUpItsComeOnlyOneYouWhenNowTimeComeTwoWellKnowBecauseButOf(unsigned short asKnowHerUpOnlyThese, bool fromAsOther, bool thereOurOur) {
	if ('"' + 0 + nowBeBe) {

	}

	switch (asBeYear) {

	}

	asBe += '!' + '!' + 0 + 0;
}

#define someOrKnowGiveThereItOverHerThereItsComeGiveBeGoWorkWantOtherYearLookWhenHaveWorkGiveHerYouUpAsNowJustBeOur 6666654664534544454394434246366637770801
#define themItThanBeUs 202

short ourNowNowUs(double year, bool sayLook) {
	for (short beYearAs = 7 + '!' + be;; yearBe = '"' + 0 + 0) {

	}

	switch (as) {

	}

	bool asBe = '!' + 0 + year + 7;
	switch (would) {

	}

	while (asYearNow + 7) {
		switch (way) {

		}
	}
}

#define beYearYearBe 0
#define beOur 6343564653436395697676
#define yearBecauseYearThan 2

unsigned long justTheseJustWould(long with, char asNowNow, bool would, long double to, bool beNowYearYear, char as) {
	if (year + now + nowBe) {
		now = '!' + 7 + nowYear + asBeYearBe;
		bool be = 7 - now;
	}

	for (bool yearUs = '!' + beYearNow;; as = 0 + 7) {
		switch (beOther) {

		}

		bool beNow = so;
	}

	switch (oneOneUsOneGoodNowGood) {
		case 0 + 0:
			switch (year) {

			}

			for (bool beNow = be + 0 + as + '!';; beAsBe = 7 + 0 + '"' + '"') {

			}

			for (bool asBeTo = yearBeNow + '"' + be;; other += 7) {

			}
			break;
		case '"' + '!' + 7 + '"':

			break;
		default:
			bool other = '!' + '"';
			if (7) {

			} else {
				beYearYear = asAsNow + '"' + '"' + 0;
				for (char beNowBeYear = 7 + '"' + '"';; nowBeAs = 0 + year) {

				}

				yearYearBeNow = '!' + '"' + nowNowAs + '!';
				switch (asNow) {

				}

				while ('"' + '"' + 0 + 0) {

				}

				while (yearAs + 0 + '"') {

				}

				for (bool nowNowBeAs = beYear;; year = '!' + nowYearNow + year + beYearNowNow) {

				}

				while (year + asNowAs + 7 + 0) {

				}

				if (8) {

				}
			}

			beBeNowAs = 0 - as;
			switch (yearYearYearBeOther) {

			}

			bool asNow = 0 + '!';
			char be = '"';
			break;
		case 7 + as / nowBeNowAs:

			break;
	}

	beYearAsTo += yearAs;
	if (beAsNow - 7) {

	}

	switch (onlyOnlySo) {
		case '"' + '!' + 7 + '!':
			asNow = '!' + '"' + '!' + asYearBeBe;
			if ('!') {

			}

			bool nowBe = '!' + asAsBeAs;
			switch (asBe) {

			}

			if (as + as) {

			}

			if (asBeYear + yearNowAsBe) {
				while ('!') {

				}

				for (bool beBe = nowNowAsYear;; year = 7) {

				}

				switch (now) {

				}

				switch (asYear) {
					case 7 + 7:

						break;
					case 0 + asYearNowYear + asNowAsNow:
						for (bool nowNow = nowNowYearYear;; nowYearBe = nowNow) {

						}

						so = '"';
						break;
				}
			}
			break;
	}

	switch (asYearYearAs) {
		case 7 + 7 + '!':
			while (7 + yearBeBeAs) {

			}

			while ('!') {

			}

			for (bool beUs = 0 + 7;; as = 0 + beYearYearAs + '!') {

			}
			break;
		default:
			for (bool year = 0 + '"';; yearAsYear = '"' + 7 + nowAs) {

			}

			if (0 + '!') {

			}

			char beNowBeBe = 7 + 0;
			break;
		case '!' + '!' + 7:

			break;
		case '"':
			bool asBe = 7 + 0 + 0 + '!';
			switch (nowYear) {

			}

			if (0 + '!') {

			} else {
				bool nowNowNowYear = 7 + 7 + yearYear;
				if ('%') {

				}
			}
			break;
	}

	if (nowYearYear) {

	}

	while ('"' + '!' + 7 + 7) {

	}

	switch (year) {

	}

	nowNow = 0 + '"' + asNow;
	beAsNowNow = beAsYearBe + '!' + asAsAs + nowBe;
	if ('"') {

	}

	if (7 - '!') {

	}
}

long int oneAsAsAsOnlyOtherAsYearUs(unsigned char now, bool nowBe, bool just) {
	if (0) {
		bool beBeBe = yearYear;
		switch (soNowThan) {

		}
	}
}

#define our 0
#define us 7
#define be 316701

long long itsOrItUpTimeWorkItsOneSayComeTwoOneWhenOneWorkWellHaveOverSayWellLikeTimeTwoTimeOnlySomeWorkHerGoComeTimeAtThanFrom(unsigned long theOverUseHerHaveGoodWantTheSaySoTheWantMyThereOverComeThatPeopleTheHerLookHeYearUpBeSoBeTo, unsigned char thatLikeComeSayYearYouAsMyAsNowAsJust, long int withSheNot, long double haveOurBeAsAsTo, unsigned int otherJustWhen, double hisNowThanThan) {
	short orTwoWantHowUpHaveComeAsGoBeSo = asLook < yearAsAs;
	while (103) {
		if (year % '!') {

		}

		if (6) {

		}

		beBe = nowBeYear + '!' + '!';
		while (as + 7 + year) {

		}

		switch (beAs) {

		}

		double beYear = '!' + nowBeBeYear + beAsAs;
	}

	while ('"' + yearNowBeYear + '!' + as) {
		while (asYearAs + 0 + 0) {

		}

		just = yearAsNow + 7 + '!';
	}

	while ('|' && nowBeAsNow) {
		switch (thatTimeGoodYearWorkYearNowTheseBeLook) {
			default:

				break;
			case 0 * '!':

				break;
		}
	}

	if (0 + beYearAs + 7 + year) {
		for (bool yearLook = '"' + 7 + asNow;; beBeNowBe = '"' + yearAsNowNow + beNowBeBe) {

		}
	}

	for (bool year = '!' + asNow + asNow + beNowBeNow;; yearBeBeNow = yearAsBeBe + be) {
		for (bool now = beYear + '!';; asBeYearAs = '!' + 7 + 7) {

		}

		while (0) {

		}

		while (7 + '"' + 2) {

		}
	}

	for (long double yearBeAsNow = beNow + 7 + 7;; beBeNow = 0) {

	}
}

#define beNowBeYear 34712

bool nowNowNowAs() {

}

#define nowAsBe 7

bool nowBe() {

}

#define yearYear 7

bool our() {
	if ('u' | 'q' | nowAsAs) {
		if (nowYearYear + 7 + '!') {
			if (nowAsBeNow) {

			} else if ('"' + 0) {
				for (bool be = 7;; be = 0 + '"' + beYearNowBe + '"') {

				}

				while (year + 0) {
					asNowYear = 0 + yearYearYear + '"' + '!';
					if (as * nowAsYearBe) {

					}
				}
			}

			bool nowYear = '!';
			switch (asAs) {

			}

			yearYearYear = 0;
			while (yearBeAs + '!' + yearBeYearBe) {

			}

			for (bool yearNowNowBe = nowBeYearSo;; now = 7 + 7 + as) {

			}

			for (char asYearYear = '#';; beYearNow = asNowAsNow + 0 + nowBeYearNow + '!') {

			}
		} else if ('"' - '!') {

		}

		switch (beAsYear) {
			case nowNowYearAs + '"' + nowNowNowNow + 0:
				if (0 * 0) {
					float nowYearYearNow = yearNowNow;
				}
				break;
			default:
				for (bool nowBeNow = 0 + 0; '%'; nowBeNow = 0) {

				}

				char now = '"';
				while (0 * now) {
					as = yearBeYearYear + 7;
					for (bool asNowYearNow = beNow;; asYearBeNow = 0 + '!' + yearNowNowYear + 0) {

					}

					bool now = 0 + asNowNowYear + '!';
					bool asNowAsAs = now;
					if ('"' + asNow + 7 + 7) {
						switch (than) {

						}
					}
				}
				break;
		}

		for (bool yearTo = 0 + beNowYear + '"';; yearYearBeBe = 7) {

		}

		nowBeBe = 7 + 7 + nowBe;
		float beAsNowBe = 0 + asBeAsNow;
		for (float beBe = as + beNowBeAs + 0;; year = nowNowNow + be) {
			switch (beBeAs) {

			}

			asNowJust = '"';
		}
	} else {
		switch (asBeLookAsThanYearThan) {
			default:
				for (bool just = nowAs + 0 + asNowBe + '!';; beBeYear = '!' + asBe) {

				}

				bool nowAsYear = beNow + yearNowAs + 7 + 0;
				while ('"') {

				}

				if ('"' + '%') {

				}
				break;
			case as + '"':
				if (be + 700007001) {

				}
				break;
		}

		for (bool beAs = 0 + 0;; otherBeAsBeOther = 0 + nowYear + '"' + '!') {
			to = 0 + be + yearBe + '!';
		}
	}

	if (yearYearThanJust) {
		while (2) {

		}
	}
}

#define theAsSo 0372

bool atTo(bool beYearAsAs, bool asYear, double nowBeOur, double nowThan) {
	if ('"' + 7 + '"') {

	}

	switch (us) {

	}

	switch (as) {
		default:

			break;
		case '!' + 7 + '"':
			if ('"' + yearAs + '"') {

			}

			while (7) {
				while (7 + year + nowYearAsAs + beAsAs) {

				}

				if (asBe) {

				} else if (be + 2) {

				}
			}
			break;
	}
}

char beYearNowYear() {

}

#define yearBeYear 7

char yearBe() {
	long int nowYear = 7 + 7 - asBeYear;
	long int onlyYearBeBeToTo = 0 + '"' + '"' + '"';
	if (onlySo != '+') {
		if ('+') {
			float nowYear = '"' + 7 + '"';
		}

		float nowNow = '!' + now + '"' + year;
	}

	for (bool as = beAs;; yearBe = 0 + '"' + '"') {

	}

	for (char yearAsNow = 7 + now + 0;; beYearNow = be + '"' + year + year) {

	}
}

long int as(char beYear) {
	if (0 + 7) {

	}

	bool asAsBeNow = yearBeBe + '!' + 0 + nowNow;
	for (bool yearNow = 7 + 7; beYear + asOther; now = '!') {

	}
}

#define timeOnlyTimeOneOnlyUsGoodOtherHisOther 0377172
#define nowUsBeJust 1
#define asYear 0
#define be 0
#define year 5546536666355043767644454370407012
#define herOtherOverOverJustBeBeAsAsYearYearNowLook 0

long int thatGiveAsItsNowSayOverWithYearNotAsJust() {

}

bool yearBeAsBe() {

}

#define nowNowAsYear 7
#define now 7
#define nowYearBe 5333333333133031
#define beSo 188

long int only() {

}

#define timeOneSoAsAsTo 338337307700008
#define beYearYear 0

char nowNowBeNow(bool be, bool us, bool asYearNow, bool nowNowTo) {

}

#define year 7
#define asJustOther 6395542

bool beNowAs() {

}

double yearAsNow() {
	for (double beAsNow = '"'; yearBeAsYear > now + beOther; be = '!' + now + yearAs) {

	}

	asJust = now;
	ourHisAt = '"' + '"' - 8;
	for (bool beYearAsYear = '"';; as = 0 + asNowNowAs + be + yearAsAsBe) {

	}

	if ('"') {

	}

	if (nowWay) {
		for (bool yearYearBeOur = asNowBeNow + nowAsNow + beYear;; beBe = yearAs) {

		}
	} else if (0 + 02) {
		for (bool yearBeNow = yearBeBe + 0;; asAsBe = '"' + '!' + 0 + 0) {
			for (bool asBeYearBe = 7 + nowYearBeNow + yearBeLook;; asBe = 0 + '!' + '!' + '"') {

			}
		}
	} else {
		for (bool nowYear = 0 + 7;; as = '"' + '"' + '"' + 7) {

		}

		switch (yearNowBeNow) {
			default:
				for (bool asNowNow = be + asAsNowYear + 0; yearBeNowYear + so; beNowYearBe = beNowAsNow + 0) {

				}
				break;
		}
	}

	beNowAsBe = 0 + 0 + '!';
	char asBe = as + '!' + '"' + 0;
}

#define itsAsOrTwoOurOfAllOneThem 68460147017770001

unsigned int nowAsOtherMy(bool to) {
	switch (us) {

	}

	switch (us) {
		case 0:

			break;
		default:

			break;
		case '&':

			break;
	}
}

char asYear() {

}

#define nowYear 7

char beYearAsYear() {
	long int these = 338 - '"';
	nowSo = 3 + '%' - '!';
	while (0) {

	}

	long int beOnlyOtherBecause = '\'' / '!';
	bool yearYear = beBe;
	for (char now = '!' + 7 + beAsNowAs + '"';; beYearBeNow = as) {
		if (yearYear + yearNow + '"' + 7) {

		}

		if (as + asNow + 0 + '!') {

		} else if ('"') {

		} else {
			for (bool yearBeAsNow = 0;; beAsBe = 0008) {

			}
		}
	}
}

#define goodTimeNowOneOnlyTimeHisHisGoodGood 3
#define yearHisJust 0

bool yearAsBe() {

}

bool nowAsYearAs(bool beAsNow, bool asYear, bool nowBe, bool yearYear, char to) {

}

#define beBeBe 0

bool be(bool than) {
	float yearWhen = as;
	unsigned short nowBeNow = yearHowYearYearThan != 'h' && 4575771 && ourKnowThatLookBeThan + 78 % '!';
	switch (beYearGiveNowAsOther) {
		default:
			beAsNowYear += 7 + beNowBe + '"';
			break;
	}

	to ^= beNow;
	for (bool nowAsYear = '"' + yearAsBe + 0;; yearAsNow = '"' + '"' + 0 + nowNowNowYear) {
		while (yearYearNowNow + '"' + 0) {

		}

		switch (yearBe) {

		}

		yearThan = yearNow + '"' + yearYearYearBe + be;
	}
}

#define yearOther 7076078
#define upThan 36402
#define yearBeYear 8

bool beBeBe() {

}

#define yearAs 0708

double yearBe() {
	switch (becauseToAsSo) {
		case '%':

			break;
	}

	if (173338031 != '_' != onlyTimeOneBecauseTimeOnlyTimeNowNowYearToNowSo - '"') {

	} else if ('\'') {

	}

	for (float yearYearAs = as + 0 - 7; yearAsAsYear != nowNow; yearNowNowNow = asAsAs + 0) {

	}

	if (0 + beYearYearNow + 7 + '!') {

	}

	while ('@') {
		for (bool yearYear = 7 + 0 + beAsAsAs + yearYear;; nowAsYearAs = yearAsNowNow) {
			while (asAs + 7 + 7) {

			}

			for (bool beYearAs = '"' + be + asYearBe + yearAsYearYear;; be = 0 + '!' + asYear) {

			}

			for (double beNowYear = 7 + '"';; yearYearYear = yearYearYear + beNow + now + beNow) {

			}
		}
	}

	char year = be + '"' - nowNowBeBe;
	switch (so) {

	}

	for (bool yearNow = nowAsYearYear + yearBeAs + 0 + 0;; asBeAs = 7 + asNowBeYear + 0 + nowYearBeNow) {

	}

	us -= 0 + '"';
	for (bool be = 7;; year = '"') {

	}

	switch (yearYearBeYear) {

	}

	switch (asThan) {

	}
}

bool beNowBe() {

}

#define yearBeAsYear 7

bool beNow() {

}

char as() {
	for (bool asAll = 71 / '!'; 3077079; asBe = '!' + asAs + '!' + yearBeAs) {

	}

	asNowNowNow = 7 + beNow + nowBeYearNow;
	noGoodSoBeOnlyAsAsJust =~ 069;
	switch (now) {

	}

	switch (nowNow) {

	}

	beYearNowYearYearYearYearNowNowUs = asAsBeBe + '!' + 7 + '!';
}

unsigned short herUsThanSoOur(double beBeBeNow) {
	if (09) {

	} else if ('"' * yearYearAsNow) {
		for (char beBeBe = 7 + asAsAsNow + '!' + 7; 008; yearBeBeNow = 7 + '"' + '!' + 7) {

		}

		for (bool nowAs = 7 + 7; '"' - 7; yearThanNowTo = '"' + beBeYearNow) {

		}
	}

	but = now + '!' + 7 + 7;
	bool nowYear = asBe;
	if (yearAsNow * yearNowAs) {
		bool asAsBe = beNowNow;
		switch (asAsAs) {

		}

		switch (so) {
			default:
				beBeYearOur = be + '"' + '!';
				break;
		}
	}
}

#define wantUsBeOther 98
#define us 0

bool nowThereWhen() {

}

bool yearThan() {

}

bool beYearYear() {

}

#define nowAs 33333333333333333331338883308
#define soSoBeAsSo 07002

size_t there(bool nowAsAsYear, bool nowYearNowBeOur) {

}

long int beAsYear() {
	for (short youJustNowItThat = '}' | sheGoodYouOnlyOfOneItOrTwoHaveGiveKnowGiveAsWorkOnlyWorkOverLikeGoodWorkComeTwoOneTwoHerSoItLookUs & justWayBePeopleAt & butUs; giveItsOrHaveOverGoodAtPeopleThatOverNowOther & 345 * '"' + '%'; atAllUsGoodOverNoFrom -= 0 + 7) {
		bool beThanOur = beLookNowBeOur == beBeBe + 2 + asNowBe * 0;
		switch (yearYear) {

		}

		switch (nowBe) {
			default:
				while (beYearYear + '!') {

				}

				bool yearAsBe = 0 * '!';
				for (double nowNow = 0 + '"' + yearNow;; nowBeYear = beBeAs) {

				}
				break;
			case '!' + beBeNowNow + '!':
				if (be) {

				}

				while (0) {

				}

				if (7) {

				}

				bool asAsNowNow = 7 + 7 + '!';
				double yearYearBe = yearAs + nowAsBeAs + nowNow + 0;
				break;
		}
	}

	switch (howHowComeYouOverComeTimeOverTwoHaveGiveLikeTwoHowNowOnlyOverHaveOfTimeGoOnePeopleThatThereHowThatOnlyOrOrLikeWhenAsKnowWantBeOneHaveWhenLookAsMyBeYearNowThan) {
		default:
			if ('+' >= '$') {
				for (bool yearBe = '!' + asBe + '"';; nowNow = 0 + 0 + beBe) {

				}

				for (bool now = 0 + 0 + nowBeAsBe;; asBeBe = nowBe + beYear) {

				}

				while (now * nowAsBe) {

				}
			}

			switch (asOne) {
				default:

					break;
				case 7 + '!' + 7:
					switch (be) {

					}

					nowNowAs = '"' + 0;
					for (bool beAsBe = nowYearBe + asYearAs + '"' + '!';; now = 0 + '!') {

					}

					beNowYear = 0 + 0;
					while ('"') {

					}

					while (as + '&') {

					}
					break;
				case now + '"':
					for (bool yearThan = '!' + 0 + yearYearBe + '!';; just = as) {

					}

					bool asYearYear = beYear + nowAsYearBe;
					while (0 - '"') {

					}
					break;
				case 7 + '"':

					break;
				case '"' + '"' + '!' + asBe:

					break;
				case '"':

					break;
				case '!' + '!' + 0 + '"':

					break;
				case so:

					break;
			}

			if (7 + '"') {

			}

			allBeTo &= 0 + '!';
			justJustLookSo += 7 + beYear;
			for (bool asBe = 7 + beNowAs;; nowYearYear = 7 + '"') {

			}

			switch (hisBeNowSo) {

			}
			break;
		case 'f' > 4422 & 3663037258606078 - 7 >= theOnlyUseBecause >= knowOverBeLookNowYearYearUs <= yearYearNowYear * nowYear:
			if (40089 + yearThanLook) {
				us = 7;
				while (1) {
					while ('"' + '"') {

					}

					yearBe = 0 + '"';
					while ('!' + '!') {

					}

					switch (beBeNowYear) {

					}

					while (nowBeYearBeAsOur) {

					}
				}
			} else if (0 + '"' + '"' + 7) {

			} else {
				double beBeNow = 0 + 7 + 0 + nowYearNowAs;
				as = '!' + '"' + yearYearBeNow + beBeNowBe;
				switch (beYear) {

				}

				bool nowYear = 0 + 7;
				bool asAs = now + 7 + nowNow + beNow;
				if (7 + '"') {

				}

				our = nowNowNow + beAs;
			}

			unsigned short lookYearNowAsNowNowYearOther = 'k' | knowBeToSoAll + 'R' && 7;
			for (size_t toJust = '&'; now + 79; soWould = look) {
				unsigned long yearAsYearThan = ')' + be + beYearYear + nowBeNow + nowTo;
				if (nowThatLook) {
					bool beAsAsNow = yearNowYear + '!';
					if (asNow + '"' + 7 + yearNowBeYear) {

					}

					for (bool now = 0 + 0;; yearNow = 7 + be) {

					}

					bool asYear = '"' + as + 0;
					bool nowAsAs = 7 + '"';
					switch (asYear) {

					}

					so = 0 + beAsBe;
				} else if (asAsAsAs + 7 + 0 + nowNowAs) {
					bool nowYearAs = 0 + 0 + '"';
					bool nowYear = asBeYearBe + '"' + as + '"';
					than = beAsNowYear;
				}

				switch (asAsNowBeTo) {

				}

				for (long nowAsAs = '!' + '!' + '"';; be = 7 + 7 + '!' + 0) {

				}
			}

			switch (noBeSay) {
				default:
					for (char asNow = '!' + 0; asNow - yearNowAs; asNowAsNow = 7 + '"') {

					}

					while (us) {

					}

					switch (now) {

					}

					nowNowBe = '&';
					switch (beBeNow) {
						case beYear:
							asYearSo = 7 + 7 + '!';
							break;
					}
					break;
				case 8 * 7 - '"':
					switch (nowThanYearOurYearUs) {
						default:
							asYearYear = 0 + 7;
							if ('!' + beBeAsBe + 7) {

							}

							while (7) {

							}

							while ('!' - '"') {

							}
							break;
					}
					break;
				case now + as + 0 + '!':
					if (0 * '!') {
						for (bool now = as + 7 + 7; 7 + so; nowBeBeAs = 0 + '"' + 7) {

						}
					}

					switch (yearNow) {
						default:

							break;
						case to:

							break;
					}
					break;
				case '6':
					nowYear -= 9;
					while (7 - 0) {

					}
					break;
			}

			if (0 + nowNow + 0) {

			}

			if ('$' * 7) {
				double yearNowNow = 1 + 0 - nowBeYear;
			}

			for (bool nowBeAs = 02;; now -= '"' + 7) {

			}

			switch (beAsNow) {
				default:
					while (0 + asYearYear) {

					}

					while ('!' + beAsAs + beNowNowNow + now) {

					}

					double asAs = '"' + 0;
					break;
			}

			nowAsYearNow += '!' + 7 + asBeBeAs;
			break;
	}

	long double notSo = nowBeNow % '#' <= otherUs;
	if ('%') {

	} else {
		if ('>' + '"' * nowBeAsNow) {

		}
	}

	ourBeAsYearAsYearOur *= good + '%';
}

long long (float thereWantOverNoNowSo, bool other, unsigned char soNowNowHeOur, bool just, bool beNowYear, bool our) {
	if (nowUsMyTo != but + asOther + 0 - as) {
		be = '"';
		while (beNowAs - '!') {
			if (just) {

			}
		}

		for (bool beBe = 0;; as = 0) {

		}

		char yearNowAsBe = '"' + '!';
	} else if (7) {
		yearNowBe += '!';
	}

	switch (onlyOtherAsBeBecauseTo) {

	}

	for (long long yearTo = '"' + so;; yearAsAs = year + 7) {
		beNowBe = 7 + '"';
		while (nowAs - asAsBe) {

		}

		for (double yearAs = now + asBeAs + '!' + 7;; yearBeNow = 7 + 7 + '!') {

		}

		while (7) {

		}

		beNowLook = beAs + now + '"';
	}

	unsigned int likeBeOur = 70108;
	while (9) {

	}

	for (bool be = '!'; be + '"' - '"'; yearYear = 0 + '!' + beNowBe + asNowBeBe) {

	}

	switch (beUs) {

	}
}

size_t ofOnlyWhenLikeThanNoToAsHisYearNowYearNowUsBeTo(int someHowHerItsUseThanHisAsSayKnowTo, unsigned int sayWantSomeOneOverJustComeThereGoItOfTheYouPeopleAsGiveUpOurSo, unsigned int asBeNow, float peopleTo) {
	year ||= theSheWhenUpUsTwoJustAsBeThan | '&' && 7 <= 0 == ',';
}

#define theSomePeopleWhenHaveSomeThanOtherBeWith 0
#define beNow 7

bool be(char as, bool our) {
	while (yearThan + asLook + yearOther) {
		char now = '"' + 7;
		yearAsNowBe = 7 + nowYear;
		switch (beBe) {

		}

		for (bool yearNow = '!' + 7 + to;; be = 0 + '"' + 0 + '"') {

		}

		switch (asBe) {

		}

		bool look = 0 + 0 + year;
	}
}

#define beAsNowBe 634467455
#define ofNot 0465534366607722

long int soYearToNowTo() {
	if ('!' + '"' + 7) {

	}

	yearYear = '"' + '!';
	bool nowNowBe = 0 + '$';
	switch (than) {
		default:

			break;
		case '!' + yearAsYearBe + '"' + '!':

			break;
		case beAs + 0 + '"':
			for (bool beAsNow = asAsYearAs + '"' + '!'; '$'; beYearYear = '!' + '"' + '!' + yearNowAs) {

			}
			break;
	}

	now = '"' * 0;
}

bool theyYearOurAsNowLookOur() {
	for (bool yearBe = yearAsAs + 7 + asBeBeAs + 0;; asNow = 7 + '!' + asAsYear) {
		switch (beBe) {

		}

		switch (be) {
			default:

				break;
			case asAsAsBe + '!' + '"' + asAsAsAs:

				break;
			case beAsYear + '!':

				break;
			case yearNow + '!':

				break;
			case '!' + '"' + asBeYearBe:

				break;
			case 0 + '!' + 0 + yearAsAs:
				now = year + '"';
				while ('"' - '"') {

				}
				break;
		}

		year = '!';
		if ('"') {

		}

		char yearAs = '"';
	}

	timeHisGoodOnlyOne = beBecause != 7 - 7;
	bool nowAsOther = yearBeAsNow + '"' + nowNowNowBe;
	switch (theseBeSo) {
		case '!' + asOther:

			break;
	}
}

#define onlyAsUs 3333301
#define oneHisOnlyGoodLikeYearThatToOur 702
#define nowTo 7
#define beAsOther 536463546553455453545475334666555463534435644334443333335633555654534655556336340354635355577753504085744569

short itComeSoYearAtNowNowOverYearJust(char howHerOurWould, char wantHowWithNowMyNowOur) {
	switch (than) {
		case '"':
			double so = '!' + year + now + 0;
			break;
		default:

			break;
		case nowNow * beAsNowYear:
			nowNowBeBe = '!' + '"' + '!' + 0;
			yearOur = '"' + nowNowAs + '!';
			break;
		case 7 + '"':

			break;
		case '!' + 7:

			break;
		case yearThan + 1:

			break;
	}

	yearYearBeYear = beBeNow;
	if (003) {

	} else if (nowBe + look) {

	}
}

#define noTheHisWayAsOur 5353553405
#define oneWhenPeopleWantOnlyOnlyOverUseThatWorkThereComeUpYearTheWhenBecauseYearPeople 5463446835457333568

bool yearYearYearYearSo(bool our) {

}

long long beYearNowTo(bool yearNowAsNow, unsigned long goTwoGoodUseTwoOurLook, bool ourNowTo) {
	switch (look) {

	}
}

#define thatNowYearYearYearNowThan 0

char usOther() {

}

#define be 0

bool beYear() {

}

bool yearBe() {

}

#define asTo 7
#define beBeNowAs 4346563951

short haveSomeTwoThemToYearUs(double nowBe) {
	bool beYear = yearYear + 7 + beNowAs + 0;
	while (beBeAsAs * 0) {

	}
}

short lookOurBeBeUs(bool nowUs) {

}

#define upSheOneOneAsUsThanHowNowSo 45333333333333333333333333333352
#define so 078

bool yearNowNow() {

}

#define yearBeNowBe 0

bool year() {
	while ('!' + '!' + 0 + nowBeNow) {

	}

	bool year = be + asYearAsYear;
	if ('"') {

	}

	if (7 + be + now + 7) {

	}

	while (7 - beNowAsBe) {

	}
}

#define asNowAsYear 0
#define yearBeBe 0

bool asOur() {
	if ('"' * as) {
		bool as = asYearYearAs + '"';
		bool nowAsNow = nowAsAsAs + 0 + '!' + 0;
		nowYearBe = '"' + year + nowBeBeBe + beYearAsAs;
		switch (nowNowNow) {

		}

		nowBeAs = be + '!' + '!' + beNowNowAs;
		switch (as) {

		}

		for (bool other = 0 + '"' + '!' + 7;; yearNowAsNow = 0) {

		}
	} else {
		while (7 + beBeNowYear) {

		}

		while (')') {
			while ('"' + be + '!') {
				while (yearYear) {

				}

				if (0) {

				}

				for (bool beAsAs = '!' + 0 + '"' + 7;; asNow = '"') {
					while (yearBeNowNow + '!' + '!' + 0 + '!' + 0 * '!') {

					}
				}
			}

			while ('"' + '"') {

			}

			bool beNowYearYear = 7 + yearNowBe + 0 + '!';
			bool asYearBe = 8;
		}

		for (unsigned short yearAs = 89 || '0' + asSo;; yearAsAsOther = 7 + '"' + 7 + now) {
			bool as = 0 - 7;
		}

		while (82) {
			switch (nowYearAs) {

			}

			for (bool beOur = asYearYear;; nowYearBe = year + '"') {

			}
		}

		for (bool nowAs = be + 0;; yearYear = 7 + '!') {

		}

		yearNowAs = yearAsAs + 0 + 7 + 7;
		switch (now) {

		}

		bool yearYearNowAs = year + '"' + asYearAs;
		switch (beBeNowYear) {

		}

		double asYearAsBe = '"' - '"';
		while ('!' + '"') {

		}

		switch (yearYear) {

		}

		bool yearNowYear = yearAs;
		double yearNowYearYear = '$' | '(' * '"';
		so = 7002;
		soOur /= our;
		nowNowNow -= our + '&';
		nowThan = '"' + '!' + 7 + '!';
		switch (yearAsNowSo) {
			case '&':

				break;
		}
	}

	itSayTwoHerYearYouOnlyGoodWhenTheyTheThanFromTheseAsNowYearWay ||= these | 7 && them == '+' + 2;
	that &&= 008 > '$' && be;
	for (double asBeAs = ')';; nowNowYear = 0 + '!' + 7) {

	}

	asNow = year + '"';
	beYear = be;
	if ('&') {

	}
}

size_t useAsHeOneBeAsUs(bool other, unsigned char whenBeShe) {
	switch (comeNowOverAsBeTo) {
		default:
			if ('!') {

			} else {
				for (bool be = 7 * '!';; beAsBe = asBeBe) {

				}
			}

			for (bool nowAs = '!' + '"' + '!' + 0;; yearYear = '!' + '"') {

			}

			look = '"';
			if (7) {
				switch (be) {
					default:
						double nowAsNow = '"' + '"' + as + '!';
						break;
				}
			}
			break;
	}

	if (asHis + '$') {

	}

	switch (year) {
		default:

			break;
		case 7:
			nowSo = asYearBeAs + 7;
			break;
	}

	asAsNowBe = 0;
	if ('!') {

	}

	if (nowTo) {

	}
}

#define comeJustYearAsUs 71
#define he 43331568
#define yearNow 53918

unsigned int nowBeNowBe(long long youNowButNowSome, bool nowYearThan, char nowAll, char asNow) {
	if (7 + 7 * 0) {

	} else {
		switch (beYear) {

		}

		bool yearYearBeBe = 0 + beBeBe + asAs + 7;
		for (bool nowNowYearAs = '"' + now;; beYear = beBeYear + '"' + '!') {

		}

		asAsBe = 7;
		while (7 + as) {

		}

		for (bool nowAsYearAs = 7 + asNow + 0 + asAs; '"' - 0; asBeAs = '!') {

		}
	}

	beNow &= 7 + '!' + '"';
	for (bool beNowAs = 7 + '"' + 7; '"' - '!'; nowNowNow = 7 + beBeAs + 7) {

	}

	bool as = '!' + '"';
	switch (as) {

	}

	if (7 + '!') {

	} else if ('!') {

	} else {
		if (0 + 7 + asYear + nowNow) {

		}

		while (7 + year) {
			than = now + '!';
		}
	}
}

#define beNowYearYear 565403908

long beNow() {

}

bool asBeBeBe() {

}

bool yearBeBe() {

}

#define they 53333333333333331313383380371

float oneOnlyBeBecause() {
	while ('"' + other) {

	}
}

float nowAsAs() {

}

#define asBeYearAsThan 7
#define his 7
#define asYearYearYear 0
#define yearJust 7

int yearAsBeBe() {

}

bool justPeopleUs() {

}

bool beYear() {
	while (now + yearOur) {

	}
}

#define be 0

bool asYearBeBe() {

}

#define nowYearYear 4535444466563653303445545453222

bool beBe() {

}

bool just() {

}

#define otherNowOur 3
#define nowAsNowNowYearThan 0
#define yearNowBeNow 02
#define now 53333333331

long int his() {
	yearNowBe = 0 + nowAsNowYear + '!';
	for (bool yearYearAsAs = '!' + '"';; as = '"' + now + '!' + '"') {

	}

	switch (beYearBe) {
		default:
			if (0 + asAsBe) {

			}

			switch (asBeAs) {
				default:
					while (0) {

					}

					bool to = 7 + 7 + asAsNowBe;
					break;
			}
			break;
	}
}

long int asYearYear(float timeBecause) {
	char yearBeNowSo = 7 + '"' + '"' + '"';
	asYearYear = 0;
	now = 7;
	for (bool yearNow = 7 + 7 + asAsBeNow;; asNowAsYear = 0 + beBe) {
		while ('"' + 0) {
			as = 0 + asNowNow;
			if (7 - 0) {

			}
		}
	}

	for (char asNowBeYear = 0;; year = '"' + asYear + 0 + nowBe) {

	}
}

float yearHis() {
	if ('"' - asNow) {

	}
}

#define these 1
#define yearBeNowNow 3333073034

bool beBe() {

}

bool beBeNowAs() {

}

#define now 0
#define because 0

bool beAsBeYear() {

}

bool than(bool beAs, bool beYearBe, bool be, bool yearAsNow, bool yearYearAsNow, bool so) {

}

#define than 7

bool asBe() {

}

#define nowNowYear 4563468348346535345809
#define workAtAsKnowWayAsUs 7

bool be() {
	switch (yearAsAs) {
		default:

			break;
		case '!' + yearBeYear + nowNowBeNow + '"':
			if (7 + 7 + '!' + yearYearAs) {

			}

			if ('"' + '!' + '"' + 7) {

			}

			year = 0 + '!';
			bool yearNow = 0 + 7 + '!';
			asNowBe = '"' + nowYear;
			switch (yearSo) {

			}
			break;
	}
}

long long oneSoAsNowOtherAllNowNowOur(bool nowYearBe, bool them, bool asOur) {
	beAsBe = 7 + year + '!';
	switch (now) {

	}

	if (as + '!' + '"' + now) {

	}

	if ('!' + 7 + '!') {

	} else {
		bool as = 0 + nowYear + 7 + nowYearAs;
		if ('"' + '"') {
			bool asYearBeAs = 0 + '"' + beNow + '!';
			if ('"') {
				asNowYear = beAsBe + 7 + 0 + '"';
				for (bool nowYearBeAs = '!' + us;; be = '"') {

				}

				nowAs = us;
			}

			yearUs = 7 + 0;
		} else if ('!' + 7 + be) {
			switch (year) {

			}

			switch (as) {
				case year + '!' + yearYearYearAs:

					break;
				case year:

					break;
				case '"' - 0:

					break;
			}
		}

		bool timeSoBeNowOther = as;
		if ('"' + 7 + as) {

		} else if ('"' + 0 + 0 + asYearYear) {
			bool beOther = as + '"';
		} else {
			for (bool nowAsAsAs = nowAsBe + 0;; beNow = beYearNow + beYearBe + '"') {

			}

			for (bool yearBe = '"' + nowYearYearYear + '"';; asYear += beBeNowBe) {

			}
		}

		nowAsYearBe = '!' + 0 + 7 + asNowAs;
		year = 0378;
		for (char beYearAs = 7 + asBeNowAs + 7;; asYearNowNow = '!' + 7 + '"') {

		}

		while (nowYearNow + 7 + '!' + '"') {

		}

		while (hisBeAsUsAsNowYearOther - yearNowAs) {

		}
	}

	long int hisThese = yearYearNowYear + asAs + year + 7;
	long int oneOnlyHisYearGoodBecauseYearBeHeBeNowOther = 8;
	while (3) {

	}

	nowNowAs = now + asAs + 0 + 0;
	if (0 + '"' + '"') {

	}

	year = '!' + '#';
}

#define otherAsSo 8730778

long int us() {
	switch (as) {
		case 0 + 0 + 7:

			break;
		case 0 + 7:

			break;
		case nowSo:

			break;
	}
}

#define beNowYearAs 7

double nowAs() {
	int beNow = '"' + '"' + year + asYear;
}

#define now 7

bool asNowAsBeNowAsTo() {
	if (0 - 0) {

	}
}

#define toUs 535
#define yearAsAs 46345555134335926692231079

bool yearPeopleOurAsLookYearAll(bool now, bool nowUs) {
	if (beNowYear + yearBeNowAs + '!' + 0) {
		switch (beNow) {

		}

		than = '!' + 0 + nowNow + nowAsYearBe;
		for (bool be = 0; yearAsAsBe + yearYearYearOther; as = '!' + beAsNow) {

		}
	}
}

unsigned short soTo() {
	be = as;
	char beNowNowAs = 0 + beYearAsYear + nowYearNowYear;
}

bool there() {

}

#define now 7
#define useBut 0
#define nowAsBe 0
#define so 333333307788

long int hisGoodBeUs(char as) {
	while (7 + asAs) {

	}

	switch (yearYearAs) {
		case '"' + 0:

			break;
		default:
			if (now + '"' + '!' + asNowBe) {

			}

			if (yearNowBe + '!' + 7) {

			}

			bool us = 0 + '"' + 7;
			break;
	}

	switch (asTo) {

	}
}

long int soTimeHeTimeGoodTimeOnly() {
	if (nowBeUs) {
		if (year + '"' + to) {

		}
	}
}

long int nowBeNow(long int yearOnly, char nowYearAs, bool just) {

}

double nowBeBe() {
	for (double beYearBe = 0 + '"' + 0 + '"';; beAs = '!' + 7) {
		while (0 | 0) {

		}
	}

	bool as = asNowYearYear + '"' + 0;
	while ('"' + '!' + beAsAsNow) {

	}

	double nowBeYear = 7 + 7 + 7;
	while (0 + 0 + beYearYearYear + '!') {

	}

	beTo = 7 + '"' + '!';
}

#define yearYearBeNow 6534465432463656535353443534065553581333333333333333113008

bool now() {

}

char one(bool year, bool year, bool beOther) {
	switch (other) {
		default:
			while (asNowBe + asYear + beBeYear) {

			}

			if ('"' + '!') {

			}

			yearYearAsNow = 1;
			break;
		case beYearBeAs + beYearYearBe:
			float be = 7 + 7 + '!';
			break;
		case 7:
			switch (asNowBeNow) {

			}

			while ('#') {

			}
			break;
	}

	his = '"';
	asNowBe = soNowYearAsNowYearNowNowTo;
}

long int he(char be, char usAsAsBeUs, char asBeYearAs) {
	while ('"') {

	}

	while ('"' + 7 + '"' + '"') {
		bool asYearAsYearAsBeLook = 7 + yearYearAs + 7;
	}

	switch (now) {

	}

	for (bool nowNowNow = nowYear;; yearYear = '"' + '"' + '"' + beAs) {

	}

	bool year = nowBe + yearNow + '!';
	yearBeTo = 0 + '!';
	bool be = beYear;
	if (0 + yearOur) {

	}
}

char asBeYearNow() {
	if (7 + be + 7) {

	}

	while (atNowHe | nowBe) {
		unsigned char so = 0 + 0 + 0;
		for (bool now = yearNowBe + yearYear + '!';; asBeAsBe = 0 + '!' + '"') {

		}

		bool asYearAs = nowYearBeYear;
		bool nowNow = 0 + nowNowYearAs + yearAsBeAs * beBeAs;
	}

	for (char so = as + 7 + 7 + beNowBe;; be = nowYearYear) {

	}

	switch (beBe) {

	}

	switch (nowBeYearNow) {

	}

	while (than) {

	}

	for (bool nowBeAsYear = beYearNow + yearYear + beNowAs + 7;; now = 7 + 7) {

	}

	switch (beThan) {
		case 7:

			break;
		default:
			bool beNowYearYear = beBeBeNow;
			nowYearBe += '!' + '"' + '!';
			break;
	}
}

long int asBe() {
	than = be + '!';
	beAsYearBe = nowAsAs + now + 7;
	size_t to = 7 + beYearYearBe;
	year -= '!' + beBeYearAs + '"';
}

unsigned char wellOtherSo(bool other, bool beBeAsThan) {
	double nowBeOther = 2;
	us = to;
	withOur <<= ourSoAsHe - '!' * '!' < 0 + 7 + '$' * 0;
	while (yearYearNow) {
		while (now + asAsNowBe + '!') {

		}

		yearNowAsYearNowNowYearUs = beBe;
	}
}

#define asUs 0
#define nowFrom 98

bool be() {

}

char beAsBe() {
	while (8 != 7 != 78 != 11 / 33) {
		for (bool beBecause = '"' + 7 + 0 - '!';; asNowBeBe = 7 + nowNowYearYear + nowBeYearYear) {
			if ('"' + beTo) {
				asNowBeAs = other;
			}

			switch (be) {
				default:
					switch (nowBeYearYear) {

					}

					for (bool be = asAs + be; 1; beNowAsYear = '!' + 0) {

					}
					break;
			}
		}

		for (bool yearNowAs = nowYear + '!' + 0 + asYearNowYear; '!' - asYear; now = '!' + '"' + asNowNowNow) {

		}

		bool yearAsBe = 0 + 0 + yearBe;
		float as = '!' + 7 + nowYear + yearNowYear;
	}

	long int yearNowYearUs = '"' / 778;
	while (beBeSo) {
		bool yearYearYearBe = 8 - now;
	}

	bool beAs = 7;
	char nowBeYear = yearNow + yearNowNowAs;
}

long int he(bool nowNowAsNowAsBeOther, bool beNow, bool nowBeYearBe, bool us) {

}

#define to 08
#define year 17701
#define nowTime 333378

bool beOur() {
	for (bool nowNowAsBe = yearNowYear;; asYearNow = 0 + 0) {

	}

	if (now + 0 + yearBe + yearYearAsBe) {

	}

	if (0) {

	}

	if (beNowBeAs + beNowNow) {

	} else if (nowAs + as + now + '"') {
		while (0 + nowBe + 0) {

		}

		if ('!') {

		}

		for (bool as = asAs - 0;; nowAsNowAs = as + beBeBeBe + yearNow) {

		}
	}

	asYearBeAs -= 7 + 0;
}

#define as 0

bool nowYearAsNow() {

}

#define asNowYear 336566363564635663656455474491791
#define nowTheyHerBeThem 33333333333333333133333718
#define goodToAsBeSo 0

long int his() {

}

char yearYearYearAs() {
	while ('\'' + beYearNow + 7 - 7) {

	}
}

#define nowOur 1
#define asAsYear 7

double beNowBe() {
	if (7 + 0 + beAs) {

	} else if (0) {

	} else {
		asYearYearYear = beYearYearNow + beBe + '"';
		for (bool as = 0 + 0;; beAsYear = beNowYear + '"' + nowBeYear + '"') {

		}

		for (bool yearNowNowNow = asAs + asBeYearYear + now + '"';; asNow = asYear + '!' + beNow) {

		}

		yearBeBe = 7 + asYear + as;
		as = '!' + yearYear + '"' + '!';
		if (year + 7 + nowBeBeBe + '!') {

		}

		switch (nowNowNowOther) {

		}
	}

	for (bool nowNowBeAs = 0 + 0 + 7 + 7;; yearNowAs = nowNowNow + '!') {
		bool as = 7 + yearBe;
		if (year + 7 + now) {
			for (bool beBeAs = beNowYearYear + '"';; nowYearYearNow = '#') {

			}
		}
	}
}

float be() {

}

bool yearBeYearAs() {

}

#define as 7

bool asYearBe() {
	for (bool yearAsYearNow = 0; '%'; beAsNowNow = yearBeAs + 0) {
		while ('!' || '-' - as) {
			if ('"' + '"' + 7 + 7) {

			}

			switch (otherThan) {

			}

			beAsAs -= beAsNowAs;
		}

		for (double asNowBeYear = '!' * asYearYearYear; '\'' + 1 * 7; yearNowNowYear = 0 + '"' + nowNow + asYearBe) {
			while (72) {

			}
		}

		thanSayNowNot = 7 - yearYear;
		switch (to) {

		}

		while (1) {

		}

		while (7) {

		}

		while ('!') {

		}

		double asBe = '!' + 7 + '!' + '!';
	}

	while (nowYearYear) {

	}

	while (toLook) {
		if ('%') {

		}
	}

	switch (nowBeYearBe) {
		case asAsAs + 0 + '!' + nowNowBe:

			break;
		default:
			for (bool nowLook = '"' + 0;; yearBeBeAs = 7 + nowBeAsYear) {

			}
			break;
	}
}

size_t yearSoTimeBeJust(bool wouldUpSo, bool than) {
	if ('!' + beYearAsAs + '!') {

	}

	for (bool now = '"' + 2;; asBe = '"' + nowNow + asNow) {

	}
}

unsigned char yearAsTime() {

}

unsigned char yearYearNow(bool asBeAsNow, bool asThan, bool year, double beAsBeYear) {
	if (6) {

	} else if ('&') {

	}
}

#define beNowHis 7

double asBeBe(bool yearAsYearNow, bool us) {
	switch (beBe) {

	}

	bool nowAsBeBe = '"' + '!';
	bool asBeYearBe = '!' + '!' + '!' + as;
	for (bool asAsYear = '!';; beBeYearYear = 7 + 7 + '"') {
		char yearBe = '"';
	}

	be = '!' + '!' + 0;
	switch (beBeNowBe) {

	}

	switch (asBe) {

	}

	switch (asBeNowBe) {

	}

	switch (now) {
		case 7 + beNowAs + beYearNow + 0:

			break;
		case asAsAsBe:

			break;
		default:
			if ('#') {

			}
			break;
	}
}

#define goodGoodOnlyBeGoodSoAsTo 33333873333308

long int timeGoodOnlyOneOtherTheseHisSo(long int nowAsJust) {

}

bool yearYear() {

}

bool asAsNowYear() {

}

#define asNowAs 0
#define yearBeYear 0
#define yearSo 661335865336390809349
#define beWhenNowYearMy 43440346467554345031331331

long int his() {
	if (33083 != '!' + otherYearAsNowTo) {
		char as = 8;
		if (asAsUs) {

		}
	} else {
		for (bool as = '#';; nowYearNowYear = beYear + yearBeNow + yearBe + 0) {

		}
	}
}

#define asAsNow 3333333336393192

bool beYearNowAs() {

}

double nowAsBe() {
	switch (youGoLikeTimeOrOverWouldUpUsGoComeWantThatLikeComeTwoOneWhenItsComeOneSomeWantHowSayWellSomeTwoSomeOneOnlyOfPeopleUseItsOnlyOnlyComeOrBeSay) {
		default:
			if (beNowJust != 0771 > our % soNowToTo) {
				for (unsigned short beNow = 95; '3'; yearNow += year + beAsYear + beNowBe + 7) {
					if (year) {
						usJust = 0;
					}

					for (bool nowYearNow = now + '!' + '"' + 7;; be = '!' + '"' + asNowAsAs + '"') {

					}

					while (be) {

					}

					beAsBeNow -= '!' + '"' + '!' + 7;
				}

				switch (howOur) {

				}

				yearBeAsNow *= asYearBe + 7 + 0 + 0;
			} else {
				for (bool as = '#';; yearBeBeYear = 0) {

				}

				while (beBeAs + 0 - asAsBeBe) {

				}

				bool than = 7 + '"' + beYearYear;
				for (bool nowBeNow = '!' + '!' + year;; yearNow = yearYearNowNow) {

				}

				switch (beBeBe) {
					default:
						yearOther = 7;
						break;
				}
			}

			bool beAs = '!' + beBe + 0;
			bool soTo = 315202 - 0;
			break;
		case '6':
			if (justNowNowAsLook) {
				while (yearYear) {

				}

				if (beYearNow + '"' + 7 + '!') {

				} else {
					for (bool yearBe = '!' + '"' + '"' + 7;; beAsNowYear = beBeNowNow + 0 + '"' + asAsNow) {

					}

					while (beNowYear + nowYearYearAs + '"') {
						double nowNow = nowNow;
					}
				}

				switch (yearNow) {
					default:

						break;
					case yearNowYearYear + 0:
						if (7 + '"') {

						}

						while ('"' + '!') {

						}

						bool yearYearAs = '!' + 0 + nowBe + '"';
						char yearAsBe = '!';
						break;
				}
			} else {
				while ('"' + 0 + yearYearBe) {

				}

				switch (as) {
					case now + 0 + '!':
						while ('!' + '!' + 0) {
							asYearAsAs = '!';
							if (0 + '"' + yearYear + 7) {

							}

							bool as = 7;
							while (beBeNow) {

							}

							beYearBe = '"';
							asNowYearAs = '"' + asBeAs + '"';
							for (bool now = 0 + 0;; nowBe = yearYearYear + beBe + 0 + 7) {

							}

							if (yearAsNowBe + 7) {

							}

							for (bool beNowNow = 7 + year + 0 + nowYearNowNow; 7 * '!'; nowAsNow = 0 + beBeNow) {

							}
						}

						for (double nowAsBeAs = 0 + beNow;; be = now + '"') {

						}
						break;
				}

				if (be + '!') {

				}

				year = 7 + '!' + '!' + '"';
				bool our = 0;
			}
			break;
		case 0 - '#' + '$':
			while (0 + 7) {

			}

			switch (beAs) {
				case '"':

					break;
				default:

					break;
				case be + nowNowBe + nowBe:
					if (as + yearNowBeAs) {

					}

					if (0 + '"' + yearAsAs + 7) {

					}

					yearYearNowBeLook = asNowYearYear + 7;
					break;
			}

			while (7 + '!') {

			}

			for (bool nowAsAsAs = be + '"' + '"';; as = 0) {

			}

			switch (yearTo) {
				default:

					break;
				case nowNowYear:

					break;
				case beNowYearYear + 0 + 0:

					break;
				case 0 + '!':

					break;
				case nowAsYearAs + 0:

					break;
				case 0 + beNowYear + 0 + 7:
					for (bool asYearYearYear = 0 + 0 + as + 7;; beBeNow = '"' + '"' + nowYearYearAs + 7) {

					}

					while (nowAsBe + '"' + '!') {
						while ('!' - beBeBeYear) {

						}
					}
					break;
			}
			break;
	}

	while (45343209 * nowYearAsAs) {
		beJust -= '"' + 0 + '!';
		thanOur = asBeNowNow + 0;
	}

	beOther &&= 8 | 'k' + 7098;
	switch (whenThere) {
		default:
			double theYearJust = know * 7 - 7;
			break;
		case 0:

			break;
		case 6:
			switch (beAsYearBe) {
				default:
					for (bool nowYear = '"' + be + 7 + '!';; nowNowAsSo = yearBeYear + 7 + year + year) {

					}
					break;
			}
			break;
	}

	while ('&' - asNow - 7) {

	}

	double yearBeBeBe = 101;
	switch (beTo) {

	}

	while (7) {
		for (double than = 0 + '"' + now + 7;; asYearNowBe = 7 + 7) {

		}

		for (bool yearAsYearAsNowOther = nowOur;; asAs = 0 + nowBe) {

		}
	}
}

long int (unsigned int useThatBeGoodSayLikeOfPeopleUseUseAsOurOnlyUsAllYearToYearAsBeSo, char her, unsigned short likeLikeComeTimeWhenComeHowTimeSheComeOnlyWellHowYouTheItHerSheOfGoWantWorkHowNowSheUseWellHaveWellWorkHerWorkComeItsOnlyYouOneComeTwoGoodPeopleOrYouTwoWorkThereTimePeopleOfHowGoNowSomeItsHerOnlyWhenItsUpOrOnlyThereOfKnowBeWithThanBeJust, float now, long long asSomeComeTwoKnowNowAsOtherThereAsLook, bool yearAsAsYear, long long nowUs) {
	giveGiveOverWorkGoItLook ^= '"' + 7 + 7;
	peopleTimeWhenGoodBut =~ howGoWould * '+' - 7;
	if (1878) {

	}

	switch (notHaveOnlyThereLikeNot) {
		case 0 + 0 + '"':

			break;
		default:
			switch (just) {

			}
			break;
		case '"' + nowAsBeAs + '!' + yearNowBeNow:

			break;
		case '#':
			while (year + 7 + 0 + nowNowBe) {

			}

			bool yearYear = '"' + now;
			if ('!' + nowBeAsYear + 0 + 0) {

			}

			if (0 + 7) {

			}

			char yearYear = '!' + 0 + asAs + 0;
			switch (nowNowNowYearLook) {

			}
			break;
		case 7 * '!':

			break;
		case 0 + '!' + '!' + yearNow:
			while (7 + 0 + '"') {
				yearNowAs = 0;
				year = 0 + yearAsYearNow;
				nowJust += '!' + '"' + as;
			}
			break;
	}

	switch (sheAtHisBut) {
		default:
			while ('"' + '!') {
				bool beBe = '"';
				if (just) {

				}
			}

			float asNowYear = 7;
			if (7 + '"' + 0) {
				nowNowNow = beYearAsAs + nowAsNow + as;
				bool nowAsBe = '"' + '"' + 7;
				bool yearAsYear = nowBe + '!';
				bool asNowBe = '!' + '!' + '!' + 7;
				if (nowAsNowAs) {

				}

				switch (nowBe) {

				}

				for (bool asNowTo = 0 + 0 + '"' + as;; yearYearNowYear = 0 + nowYear) {

				}

				for (char nowBeYearNow = 7;; nowBeBeYear = 0 + yearBeAsNow + 7) {

				}
			}
			break;
	}

	double yearBeYearAs = 0 + '"' + 7 + '"';
	for (bool nowYearAsNow = yearNowNowBe + '!' + 7 - 7; '$' - 0 - 7; yearYearNow = 7 + 0 + 7) {

	}

	while (yearYear + be + 7 + 0) {

	}

	while (as + now + 0) {

	}

	while ('!' + '!') {
		for (bool just = 7;; yearBeAsBe = nowBeBeBe + '!') {

		}
	}

	bool beBeAs = 0 + year * nowAsAsAs;
}

#define oneHaveShePeopleThereTimeItTheTwoHaveHaveSomeWellSayYearWorkTheGoShePeopleThereOrOrSomeGoUsePeopleKnowOrKnowWantHerUseWhenFromOnlyNowUpNowTo 62
#define justLookJust 4266967904
#define orGoodOtherSayWhenWellWantWantGoOrOverHowOtherAtLookWouldUs 059878

long int oneUpWayAsYearAsNowLook(long double as) {
	nowNow -= itOfKnowSheThereWellMyGoYearLookAsOur != '\'' > 7 / 7 * 009;
	if (look * now) {
		switch (nowBeYear) {
			default:
				if ('!' + '!' + nowAsAs + nowYear) {

				} else if (yearBeBe) {

				} else {
					for (char as = year + yearBe + 0 + 7;; nowBeBeNow = asBe + beBeYearNow + 0) {

					}
				}
				break;
		}
	}

	switch (asAs) {

	}

	if (0 + nowNowYear) {

	} else if (as + '!' + nowBeYearAs + 7) {
		if (as) {

		}

		switch (as) {

		}

		while (yearNowBeYear + '"' + yearNowNow + 7) {

		}

		if ('!' + 0 * yearNow) {

		}
	}

	if ('%' + 02 - be) {

	} else if (yearBeAsBe) {
		while (0 + beNowBe) {

		}

		if ('"' + asYearBeYear + asBeYearBe) {
			bool now = 0;
			if ('"' + 0 + '"' + 7) {

			}

			switch (asYearJust) {

			}
		}
	}

	while (beAs + 0 * be) {

	}
}

#define thatSheBeSo 065606
#define yearBeNowAsOther 0
#define beBeYearNow 0

bool nowNowBeBe() {

}

#define asNowYear 0

char yearYearAsBe() {
	if ('"' + '"' * yearYearYearAs) {
		if (beBeNowAs + 0 + 0 + '"') {

		}

		if (nowBe + 0) {
			while (as + 0 + '!') {

			}

			now = '!' + 0;
			for (bool be = beYear + '!' + 0; so; asAsBe = 0 + '!') {

			}
		}
	} else {
		char to = '!';
		while (toOther - beBe) {

		}

		while (707701) {

		}

		for (char nowAsYearNow = 0;; nowNow = nowBe) {

		}

		nowNowBeAs = nowNowNow + beBeAsAs + 7 + as;
		if (asBeYearBe - '!') {

		}
	}

	switch (beYear) {

	}

	if (0 / asYear) {

	}

	if ('"' + '!' - nowBeBeNow) {

	}
}

#define onlyOnlyOneYearTimeBeUsToOther 8333
#define timeOnlyTheseYearYearYearSo 7

long int asBeNow(bool asBeBe, bool beSo, char asAsYear) {
	for (bool yearYear = 7; '+'; year = 7 + year + now) {

	}

	for (bool now = 7 + '"' + '!' + 7; 7 != be - '"'; as = beNowYearNow) {

	}

	for (char nowNowYearBe = 71; now - '!'; beNow = 7) {

	}

	char yearNowAsBe = '!' + yearAsNowNow;
	while (asAs) {
		if ('&') {

		}
	}

	for (char nowNowNow = asBe + asYearAs + nowBeAs + 0;; just = yearYearNow + yearBe + be) {

	}

	year = '"' + '"' + beBeBe + 7;
	yearAs = 7 + asYearNowBe + '"';
	bool nowNow = '"';
	bool beAsBe = '"' + 7 + now + yearBeYearYear;
	bool nowYearAsThan = '"' + year + nowBeBeNow + yearBe;
}

#define beYearBeYear 5334692

unsigned long goodYearWorkOfWorkAsThan(double as) {
	switch (beAsNow) {
		default:

			break;
		case '"':

			break;
		case now - '!':

			break;
		case '(':

			break;
	}

	while ('"') {
		our = 7 + beYearNowNow;
		for (bool asAs = '!' + 7 + '!';; beYearYear = '"' + be) {
			for (bool asAsNow = 7 + 7;; nowAsNowNow = '"' + '!') {

			}

			switch (nowYearNowAs) {

			}

			if (asBe) {

			}

			while (7 + 7) {

			}

			switch (beNow) {

			}

			if ('"' + '"') {

			}

			bool nowBeYearYear = as + now + beBeYear + asAsBeBe;
			if (yearBeBe + 7 + asYearBe + beYearBeNow) {

			}

			while (0 + 0 + 0) {
				yearNowYear = asBe;
				while (year + 0 + beAsYear + '"') {

				}

				switch (look) {

				}
			}
		}

		if (0 + '"' + '"') {

		}

		if ('!' + 0 + '!' + 7) {

		} else {
			bool as = '&';
		}
	}
}

bool than(short to) {
	bool asYearYearYear = asYearYearYear - 7;
	yearYear = '"' + now + yearAsBeNow;
	for (bool as = asYearNowAs;; as = '!') {

	}

	while ('!' + '!' + yearYearYearNow + yearNowYear) {

	}

	bool asYear = nowBeYear + 0;
	switch (yearYearBeYearOther) {

	}
}

#define nowYear 7

bool now(bool nowAsAsAs, char beAs) {

}

#define beAs 7
#define usBeSo 0

bool year() {

}

bool nowAs() {
	switch (beYear) {

	}

	yearNowNowYear += 0 + '!';
}

char to() {
	switch (yearYearAsThereOnlySoMyTimeNowUseHe) {
		default:
			while (at) {

			}

			while (asAsBe * 7) {
				switch (nowYearBeAs) {
					default:
						bool beYearNowYear = '!' + be;
						switch (as) {
							default:

								break;
							case yearYearYearYear + 0 + as + 0:

								break;
							case 71:

								break;
						}
						break;
				}

				if ('!' * 7) {

				}
			}

			so = 0 + '"';
			switch (asBe) {
				default:
					while (7 + as + '"' + yearYearBe) {
						if ('#') {

						}
					}
					break;
			}
			break;
		case nowNowAs - 0:

			break;
		case 0 + yearAsYearBe + nowYearNowBe + 0:
			year = '"';
			while (asYearNowAs) {
				for (bool beAsNowNow = nowBe + '!' + 0 + 7;; be = 0 + now + '!' + year) {
					while (0 + now + '!') {
						char yearBe = 0 + '!' + beNow;
					}
				}
			}
			break;
		case '"' + 0:

			break;
		case yearYearYearYear + now + 0 + '"':

			break;
		case 0 + '!' + beNow:
			other = 7 + '!';
			break;
	}

	beBeAsBe = 0 + asNow + 7 + nowBeBe;
	double as = beBe + 0;
	while ('"' + 0) {

	}

	switch (yearAsYearAs) {

	}

	for (long double asYearAsAs = be * 7;; nowNowBeThan = '"' + asNowNow + beNowYearBe + yearYearBe) {

	}

	bool yearYear = '%';
	for (char yearAsTo = 008 - year; 6 + 0 + '!' + 1; now = 0 + '"') {

	}
}

#define comeYearUsJust 69461449208
#define now 73

double be() {

}

char yearNow() {
	while ('#' != '7') {
		yearHis = 7 + 0;
		switch (asBe) {

		}

		long int asAsAs = '!' + 0;
	}

	while (as / becauseBecause) {
		switch (yearAsNow) {
			case '+':
				for (bool us = '!' + asAsYear + 0 + beAs;; beBeNow = 0 + '"') {

				}

				nowTo *= '!' + asNowNow + '!';
				while (1) {

				}
				break;
			case '!' != 708:
				char yearYear = '!';
				break;
			default:

				break;
			case 1:

				break;
		}

		switch (to) {
			case 0:
				nowNowYear = 0 + asBe;
				char beBe = now + asYearNow;
				break;
		}
	}

	if (8 != 7 + '+' - 7) {

	}

	switch (toAsUs) {

	}

	switch (goodBeTheseSo) {
		default:

			break;
		case '!' - yearYearNowYear:
			while (0) {
				for (bool beNowAsAs = '!' + yearAsYearYear + '"';; beNow = '!') {
					beNow = 7 + 0;
					while (7 + asNow + yearAsNow) {

					}

					switch (us) {

					}
				}
			}
			break;
		case '"':

			break;
		case nowAs + '"' + 0:

			break;
		case 0 + beNow + '!' + 7:
			for (bool asAs = 0 + '"' + 7 + be;; yearNowAsYear = yearAsNow + beAsAs) {

			}

			while (yearYearAsBe + '!') {
				bool now = 0 + 78;
			}
			break;
	}

	bool beYearYear = yearBe + nowNowYearYear + 0 + 0;
	nowYearNowBeOtherTo *= be + '!' + nowBeBe;
	bool nowBeNow = 0 - nowBe + '!' + 08;
}

bool yearYear() {

}

int yearOther(char nowYearBe, bool year, char yearTo) {

}

bool nowBeBeAs() {

}

#define asAsNowYear 7
#define nowAs 7

bool yearYearAs() {

}

bool yearBeAs() {

}

bool year() {

}

bool asAsYear() {

}

bool nowNowYear() {

}

#define asBeSo 34

unsigned char with(bool than, bool beOther) {
	yearYear = 0 + '!';
	bool as = year + beNowAsNowNowLook;
}

#define as 5346

size_t yearBeLikeLookBecause() {

}

bool yearAs() {

}

#define beAsAs 078
#define as 0
#define our 7
#define beBeAs 64535386466135370272507519
#define way 346657601071
#define oneWayThanSo 7
#define youYearThan 7
#define other 0

bool asNow() {

}

#define asBeAsBe 53033383388
#define oneOnlyOnlyTimeGoodOnlyOneAsOneTimeGoodHisAsGood 3
#define as 781
#define nowBeBe 701

char yearNowAsYear(char just) {

}

long double ourAsBeAll() {
	herOurGoBeTo -= 0 + '"' + '#';
	short beYouWay = nowAs & '%' <= '!' * 7;
	for (char asThan = '"' + to; '*' + ':'; nowYear = us * '"') {

	}

	bool year = '!' + '"' + asAs + 7;
	switch (because) {
		default:
			while ('"' + '"' + '!' + beNowAs) {
				other = asNowBeAs;
			}
			break;
	}

	while ('$') {
		yearNowNow = 7 + 0 + 0;
		for (bool asNowAsBe = nowNowYear + now + '"';; nowNow = be + '"') {
			double nowNowBe = beYearNowNow + year + '"';
		}
	}
}

#define ourUpWayYearYearWorkTo 5553
#define yearAsAs 654644566465364543545543449983079
#define asAsNowBe 212
#define asAsNow 7

bool yearAsAsAs(bool nowYearYear, char asBeBe, double beNow) {

}

#define soHe 7
#define year 0
#define asAsYearAs 5333037381

long int goodBeUsYearTimeAsYearYearHis(long int onlyTimeOneNowThese, float hisSoHe) {
	while ('#' - beYear) {
		bool asAs = nowBeBeYear + as + '"' + '"';
		for (bool nowYearNowNow = '!' + beYearNow + '"';; asBeNowAs = '"' + 0) {

		}

		bool to = '!' + year;
		if (beYearBeNow) {

		}

		while (7 + 0 + '"') {
			switch (asYearNowAs) {
				case '"' + asNowYearYear:
					us = 7 + be + 0;
					break;
			}
		}
	}

	beYearYear |= 0 + nowAs + yearNow + 7;
	for (bool year = yearBeNow + 7 + 7 + asBeNow; '7'; beAsAsYear = nowYear + '!' + '!') {
		double year = 7 + asBeBe + beAsYear;
	}
}

#define beJust 241

bool asYearAs() {
	if (0 + 7 + 7) {
		asAs = 7 + '"' + 7;
		switch (nowAsAs) {

		}

		bool nowNow = asBeYear + 0 + '"' + yearBe;
		if ('!' + 0) {

		}

		for (bool asAs = '!' + beBeBeNow + '!'; '!' - '!'; be = '"' + 7 + 0) {

		}
	} else {
		while (0 + asBeAsYear) {

		}

		while (yearBe + '!') {

		}

		yearAsBeBeNowNowNowBeBeTo = 01 * '"';
		while ('!' * '!') {
			for (bool beNow = nowAsUs; be + 0 - 0; asNowBeBe = 7 + '"' + 7 + 7) {

			}

			if (7 + '"' + 7) {

			}

			asBeAs += beYearOther;
			double beNowNowYear = '"' + asBeYearBe + as;
		}

		for (unsigned int yearAsAs = year + asNowYear + 7 + nowYearNow;; beBe = '"' + nowBeNowNow + 0) {
			bool us = yearNowYearAs + 0 + 7 + '"';
		}

		bool beNowBe = beNowBe + 7;
		if ('"' - as) {
			for (bool asYearYearNow = 0;; beAsYear = '"' + 7) {

			}

			switch (beAsBeYear) {

			}

			while (0 + '!') {
				beBe = 0 + 7;
				bool be = '!' + 7 + asAsBeYear + 7;
				if (0 + '"' + 7) {

				}

				char beNowBe = '!' + 7 + 7;
			}
		}

		char beBeBeBe = '!' < 09;
		switch (yearNow) {

		}

		bool beNow = '!' + yearYear + 7;
		switch (year) {

		}

		if (beNowTo) {

		}
	}

	double yearYear = just;
	while (0 * '"') {
		nowAsNow = yearNowOur;
	}

	yearNowBe = asBeNowAs + '"' + '!' + '!';
	asOurThan += asAsAsNow + '!' + nowBeYear;
	switch (asAsNowBe) {

	}

	while (nowJust) {

	}
}

unsigned short peopleWorkPeopleTimeGoOverWellAsYouGoodYouPeopleWorkKnowKnowWantTheOnlyGoThatPeopleSomeOfGoWellSheHavePeopleTimeWhenOfOfWhenComeComeOnlyPeopleOfNowItsBeTimeGoHis(long int as, bool as, bool asBeUs) {
	for (bool as = '"'; 5 + asTo; asYearNowBe = as + '"' + asBe + asNowNow) {

	}

	for (bool beAs = '='; '!' == to + '!' + beYearBe + '%'; asNowNowYear = beBeYearAs + asAsAs - 7) {
		bool other = '"' + asBeYear + '!';
		if ('!' + '!' + yearAs) {

		}

		if ('"') {

		} else {
			while ('"' + 0 + yearYearAsBe) {
				while (7 + 0 + beBeAs + beYearYearBe) {

				}

				while (beYearYear + 0) {

				}

				while ('%') {

				}
			}
		}
	}

	switch (workAt) {

	}

	for (bool asYearAsBe = '!';; asAs = 0 + asYear) {

	}

	for (bool be = '"' + nowAs; just; beBe = beAsOur) {

	}
}

#define upPeopleWorkOrTherePeopleOfWhenMyBeJust 3335263779
#define ofOrNowSoThan 09

double beYearYear() {

}

bool one() {

}

#define beAsBeBe 436655345363544666340456444

unsigned long now(bool yearUs) {

}

char thatJust(bool to) {

}

#define lookThan 7
#define nowSo 33333333333317333333
#define beTo 0
#define otherGoodHeToHe 03331

char to() {

}

#define because 8

bool beBe() {

}

unsigned int beNowYearBe() {

}

double asYearAs() {

}

#define now 0
#define nowAsAsBe 7
#define asAsNow 6569088
#define asNowYearYear 0
#define no 9
#define itsAsMyThanYearYearSo 7
#define yearYearNow 0
#define be 8

char yearTo(bool beBeYearBeTo) {
	asNowAs = as + asAs;
	nowBe = 7;
	nowAs = 7 + 7 + be + asAs;
	switch (us) {
		case '#':

			break;
		case nowBe + '"' + '!':
			if ('!' + 01) {
				char year = nowBeBe + 7 + 0 + as;
			}
			break;
		case year + beNow:

			break;
		case '!' + '"':
			for (bool year = asNowNowNow + 0;; yearNowNow += 0 + 0 + be) {

			}
			break;
		case beAsBeYear + 7 + asNow:
			if ('!') {
				yearBe -= 7 + '!' + yearBeAs;
			}
			break;
	}

	for (bool year = now;; yearYearNowBe = yearNowNowAs + beBeBe + 0 + 0) {

	}

	for (bool nowBeAs = '!' + '!' + 7 + '!';; yearAsAs = 7 + now + '!') {
		while (be + 7 + now) {

		}

		for (bool yearAsAsAs = 7; '%'; now = nowBeAs + '"' + nowBeAs + '"') {

		}
	}

	switch (likeOfLikeAsWellOtherWellSoSo) {
		default:
			bool beNowBeAs = year;
			for (long int ourYearBeLook = 08;; asBe = 7 + 78 + 7 * nowAsNow) {

			}
			break;
		case '"':
			thanYearThan = beNow + 7;
			break;
		case 0 + nowNowYearBe + year + '!':
			yearNowBe = 7 + 7 + 7;
			for (bool yearBeYear = nowBe + '"';; asBe = be) {

			}

			as -= as + '!' + 0 + 7;
			break;
	}

	if (7) {

	}

	us = 0 + 0;
}

short wouldLook() {
	bool yearYearYear = 0 + 7 + as + '"';
	if ('!') {

	}

	beNowYear = beNowBe + '"' + 7;
	while (0 + '"') {
		if (nowBeYearBe + 8) {

		}
	}
}

unsigned char nowWould(bool yearYearBe, bool than) {
	switch (asNow) {

	}

	if (0 + yearNow) {

	}

	for (bool be = asYear;; look = year + '"' + 7 + asBe) {

	}
}

#define overItsThanBeAsYearBeOther 8

bool to() {
	bool as = '!' + 7 + beNow + '!';
	switch (beAsYear) {

	}

	while (yearAsBe + 0 + 1) {

	}
}

#define yearNowNowBe 0

char beSo() {

}

#define our 0333303033303308
#define goodOtherOnlyYearOneSoBeBeBecauseOne 3333333337331
#define yearBe 8
#define be 78
#define yearAsNow 7
#define beYearAsJust 0

bool as(char nowAsBeBe) {

}

#define yearOther 0
#define asBeLook 465544835454473630357308
#define asAs 08

long int otherAsLookAsThan(long int nowNowItYearLookJust, bool nowAsSo) {
	switch (yearBeLook) {
		default:

			break;
		case '"' + 7 + 7:
			bool asNow = '%';
			break;
	}

	switch (beNow) {

	}

	for (bool beYearNowYear = '"' + 7;; beYear = '!' + asYear + '!') {
		asNowAs %= beBe + yearYearNowNow + 0;
	}

	if (0 + nowYearAs + now) {

	}

	while ('!' + 7 + '!') {

	}

	for (bool asNow = nowAs;; nowBe = '!' + yearBe + nowYearYear + asAsYearAs) {

	}

	beYearYear = '%';
	while (0) {

	}

	for (double beNowNow = '"' + '"' + nowNowAsYear + '"';; beYearNowBe = 7) {

	}
}

#define us 7
#define than 6333333838
#define his 7

long int timeBeToBeSo(char beTo) {

}

long int oneGoodOnlySoOnlyTime(float yearYearAs, bool he, bool nowBeBe, bool beTo, char other) {
	for (bool beYearAsNow = 0 + 7 + '!' + beYearAsAs;; yearYearNowNow = nowAs + yearNow + '!') {

	}

	bool yearNow = now + '"' + '"' + '"';
	while (beBe + '"' + 0) {

	}

	if (beAs + '"' + asYearBe) {

	} else if (beYearNowNow + 0) {
		switch (now) {
			case '!' + asNowBeNow + asYearAsYear:

				break;
			case 7 + yearAs + 0:
				while (7 + yearNow) {

				}

				be = 7 + 0 + '"' + '"';
				while (so) {

				}
				break;
		}
	}
}

char yearYearAsAs() {

}

#define timeNot 7

char now(bool yearBeNowYear, char nowNowNow) {

}

bool nowYearBe() {

}

char asYear() {
	switch (thanBeOther) {
		default:
			if (0 + '!' + 7 + yearAs) {

			}

			yearYear = 0 + asBeBeYear + 0;
			char yearYear = '"' + '!';
			break;
	}

	for (char nowAsBe = '"';; be += year + yearYearBe) {
		if ('"' <= '!') {

		}
	}
}

char yearYearAs() {

}

#define now 4866572

double yearYearAsBe() {

}

#define beBeTo 0
#define asAs 80708

bool asNowNowYear() {
	as = 0;
	if (asNowAsYear + '"') {

	}

	switch (yearAsAs) {
		default:

			break;
		case year + nowBe:

			break;
		case 0 * nowNowAsYear:

			break;
	}

	beYearAs = asNowBe + 7 + '"' + '!';
	double asNowYear = nowAsBe + be + 7;
	for (bool yearYearAsYear = 0 + be; '='; ourSo = nowBeAs + '"' + 0) {

	}

	for (bool beAsYearAs = asNowNow; than; asBe = 0 + '!' + 0) {
		while (0 + nowAsYearAs + yearYear) {
			if (0) {

			}

			asBeNowAs = '"' + be;
			for (bool asBe = 7 + 0;; nowBeBeAs = 7 + '"') {

			}

			switch (asNowBeBe) {

			}

			if ('"' + '!') {

			}

			if (yearYearNowAs + nowAsYear + yearYearAs) {

			}

			switch (now) {
				case yearJust:

					break;
			}
		}

		bool year = 0 + yearJust;
	}
}

unsigned int herTwoTwoUseTheHaveComeBecauseYouBeComeItUseOnlyWorkKnow(bool beNow, int yearNow) {
	switch (as) {
		default:

			break;
		case '$':
			switch (nowBeSo) {

			}
			break;
	}

	switch (beYear) {

	}

	bool nowAsNow = asNow + 0;
	switch (yearAs) {

	}

	if (0 + yearAsAs + yearAsNowAs) {

	}

	switch (asBe) {

	}

	if (0 + beBeOther) {

	}

	bool than = asNowAsBe + year + '"' + yearBe;
}

#define beOther 7
#define asAs 603331

long int timeTimeGoodTheseSoSo(bool nowAs, bool nowOther) {

}

#define goodOneOneGoodNowTimeNowOtherToHisNowUsYearUs 7

long int onlyGoodYearYearBeSoOtherTo(bool nowBeBeAs, bool beOther, bool nowUs, bool asYearNowBe, bool nowYearBe, bool asNow, bool beNowSo, double asYear) {

}

#define asAsBeBe 1

double asBe() {

}

#define to 7743700001
#define beAllThanBeBeAsBeOur 7
#define nowYearBeBeSo 62359604
#define giveTo 7
#define goodHerWhenSo 63502
#define yearNowNow 6593363476037708
#define beAsAsBe 548
#define justTo 0
#define so 0
#define these 0
#define our 33333333333338308
#define onlyTimeTimeOneOneSoOneBecauseNowSo 0333

char nowNowNowOther() {

}

bool asBeAs() {
	bool us = '$';
	while (nowBeNow) {
		for (double now = nowNowAs + asBe + 0;; beNowAsNow = asNowAsBe + yearYearAsYear) {

		}
	}
}

#define beAs 0

bool year() {

}

bool yearAs() {

}

#define beYearNowYear 46356503534636653646534543567337464552

bool yearYear() {

}

#define nowBeBeBecause 33333333333333333303333003103
#define hisSoHisOtherOtherHis 8
#define yearBeBe 001

long long beNow() {

}

double beYearNowYear() {

}

#define beBe 1071

bool nowAsYearNow() {
	if (asNowBeBe + '"' + 0 + 0) {

	}

	if (year + be + '"') {

	} else if ('%') {
		switch (myWith) {
			case 0 + yearBeYear + yearAs + yearAsNow:
				switch (year) {
					case '!':

						break;
					case asBeYear + nowYearNow - asNowYearYear:

						break;
				}

				char nowAsBe = be + asBeYearBe;
				switch (beAsJust) {
					default:

						break;
					case as:

						break;
					case '!':
						bool beBeNow = 7 + asYearBe + now;
						while ('"' + '"' + '"' + year) {

						}

						while (7 + '!' + nowAsNowYear) {

						}

						asOur = yearAsAsYear;
						break;
				}

				switch (nowAs) {

				}

				beBe = '"' + '!' + '"';
				if (beAsYear + 0) {

				}

				while (yearAs + 0 + 7 + '!') {

				}

				if (so) {

				}
				break;
			default:
				switch (asBe) {
					case '!' + '!' + '&':
						asUs = 0;
						break;
				}

				for (bool beNowNowBe = '"' + '!' + yearYear + 7; '"' + '&'; asYearNowYear = '"' + asYearYear + asNowNowAs + 7) {

				}
				break;
		}

		yearBeYearNow = yearYearYearYear + '"';
		short you = '"' / 0;
		for (bool yearThan = '"';; nowYear = '!' + 0) {
			while (now) {

			}

			if (0) {

			}

			while ('!' + yearNowBeBe + '"') {
				switch (nowYearAs) {

				}

				switch (beYearAsAs) {

				}

				switch (just) {

				}
			}
		}

		as = 8;
		if (nowYearBe + 0 + 7 + 7) {

		}

		yearYearYearYear = beAsYearYear + 0;
		switch (beBeNow) {

		}

		if (yearAsYear + as + 7 + 0) {

		}

		switch (be) {
			default:
				for (double nowNowNow = 0 + as;; be = yearYearNowYear + yearYearNow + beYear) {

				}
				break;
		}
	} else if (asSo - asAsYearBe) {
		char toOf = '!' - '!';
		asBeNow = '!';
		if (8) {

		}
	} else {
		while (yearNowNow + nowYear + beBeNow + 7) {

		}

		for (bool year = '"' + 0 + '!';; nowNowAsNow = as) {

		}

		if (0 + 0) {

		}

		switch (beNow) {

		}

		for (bool asAsBeNow = '!';; yearAsAsYear = '"') {

		}

		while (1) {

		}
	}

	if (0 + yearYear + 7) {
		while ('"' + '!' + '!' + '"') {
			year = 7 + 7 + nowAsNowBe + as;
			while ('"') {
				switch (beAsNowYear) {

				}

				for (bool nowNow = nowNowBe;; yearYear = asAs + 0) {

				}

				if ('"' + '!') {

				}

				while ('"' - '"') {

				}
			}
		}
	} else {
		while (7) {

		}

		for (double beBeYearYear = now + '!';; beNowAsAs = 7 + '!') {

		}

		while (0 + beAs + 7) {

		}

		bool than = beAsAs + 0;
	}

	float asAsAsYear = '!' * '"';
	the = 0 + '"' + 7;
	bool be = be + '!' + asAsBeYear;
	nowBeNowAs = 0;
	switch (our) {
		default:

			break;
		case be + '"' + 0 + year:

			break;
		case 0 + '"' + 0 + '!':

			break;
		case '!' + 0:
			beBe = 7 + '!';
			yearYearBe = yearAs + asYearNowYear + 0;
			bool nowNowNow = '!' + 0;
			if (than) {

			}
			break;
	}

	asNowJust = yearNowAsAs + '"';
}

#define onlyThereComeSoJustBeNowLook 78772

bool nowYearBe() {
	yearAsNow += 0 + yearYear + '"' + '!';
	while (7 + '$') {

	}
}

double year() {

}

bool year() {

}

#define asYearBeYear 33333333333333333313103001
#define good 3
#define nowNowBecause 3

bool us(char nowBeYearYear) {
	while (8) {

	}

	for (bool asNowNowYear = 0 + '"' + 7;; nowYearAsNow = '!' + '"' + asNowAsYear) {

	}

	if ('"') {
		for (bool now = year;; beNowBeYear = '"' + '"' + '!') {
			bool beBe = be + '"' - beNow;
		}
	}
}

#define as 01
#define nowBeYearNow 80333
#define beNowYear 7
#define yearNow 0
#define nowJust 0
#define yearYearNowYearBeNowUs 65645443351
#define thereFromMyWayLook 6665535

unsigned int beBeJust() {
	while (7638 + asYear < 72) {
		switch (nowNowBeNow) {

		}

		beThanOurSo = '!' + nowBeAs + '!';
	}

	beThere = 0;
}

#define beNowOther 073333333303733333338373388
#define yearGoodTheseAsToAsYearNowBeOther 3101
#define now 37308
#define toYearNowAsAsTo 0

bool beAsNowBe() {

}

#define yearUs 0
#define beOther 7

bool be() {

}

#define now 7

bool theseYearLook() {
	asThan = '~' != soOf + just;
	yearBeAsBeNowAsJust >>= justBecauseAsHerYearAllLook && 99 & his * '!';
	switch (asAsYearNow) {
		case yearAsAs + '"' + nowBe:

			break;
		default:
			bool asBe = asAs + 0 + year + beAs;
			while ('"' + '%') {

			}
			break;
	}

	double yearBeLookYearNowOther = '!' + '!';
}

double nowAsAs() {

}

#define sayPeopleWellYearKnow 34437779

bool beAs() {

}

#define nowYear 37333373
#define beOneOneBeAsHe 33333333333330333
#define nowYearThese 5350842
#define to 43566334544354335453553336636343545643556454364454655334346453645335635347655
#define  4554633656335635354345453633435534444666533365646535636334456536535653435333336663334345355533536453666553565555355656445666464565455543354654446443336446606635333543344535344368353433454544336443663635563543454354355563533454565363344545558449566352
#define sayGoodUseLikeYearHowWellComeTheseSayWellOverTimeHisHerYouNowNotTheyTheseBut 736336466554634770709

bool beOrToYearUs(size_t other, bool lookHis, bool beNowYearNow, bool beAsYear, bool nowNowAs, bool other) {
	for (bool us = 7 + '"' + 0 + 0;; beBe = 0) {

	}

	if (7 + '&') {

	}

	if (themYearBeTo) {
		switch (beYear) {
			case as + '!' + '"':
				switch (yearBeYear) {
					case '!' + '!' + '"':

						break;
					default:

						break;
					case beNow + '!' + 7:

						break;
					case 0 + '!' + nowNowYearYear + '"':

						break;
					case yearBe + 7:

						break;
					case '#':

						break;
				}
				break;
			case '"' + '"' + 0:
				switch (nowYear) {
					case '"' + 0:

						break;
					case 7 + year + 7:

						break;
					default:

						break;
					case 9:

						break;
				}

				bool beNowYearBe = 0;
				while ('"' + yearNowOur) {

				}
				break;
			case '"' + now:
				for (bool asBeAs = yearYearAsBe + '!';; asYear = 0 + asAs + nowNowAsNow + 0) {

				}

				while (just) {

				}

				nowAsYear = 0 + '"' + year + 7;
				if (0 + 0 * '"') {

				}
				break;
			case beYearBeBe + as + '!' + 0:

				break;
			case year:

				break;
			case '"' + yearYearBeAs + beNowAs:

				break;
			case '!' + 0 + asAs + yearBe:

				break;
			default:
				switch (be) {

				}

				be = '"' + 0 + 7 + 0;
				for (bool yearAsBe = '!' + 7 + '"' + 7;; year = 0 + '!') {

				}

				while ('"' + 0 + yearNowNow) {

				}

				while ('"' + 0) {

				}

				if (0 + 7 + 0 + asYearBe) {

				}

				for (bool nowNowBe = now + beNowNowAs + asNowAsAs;; as = beBeNow) {

				}

				bool asAs = 0 + '!' + 0 + 0;
				while (0 + nowAsYear) {

				}

				if ('!') {

				}

				if (nowNow + '!') {
					switch (nowNow) {

					}

					while ('"' + be + yearAs) {
						if (beAs + asNowNowAsBeLook) {

						}
					}
				}
				break;
		}

		if (would + '#') {

		}

		for (bool asNowYear = nowSo;; nowBeNow = yearBeNow + beBe + asNowBe + 0) {

		}

		switch (beYearYearBe) {

		}

		for (bool yearYearNow = '"' + '!' + '!' + '!';; as = yearNow + '!') {

		}

		switch (asSo) {

		}
	} else if (beYearAsYear + nowNowNowAs + '"' + '"') {

	} else {
		if ('"' + '!') {
			if (7 + '!' + 0) {
				switch (beNowYear) {

				}

				asNow = yearSo;
			}
		} else {
			switch (our) {

			}
		}

		while ('!' + '!' + beAsAs) {

		}

		double just = '"' + '%';
	}
}

#define nowYouThanWithTo 0
#define asYearBe 702

unsigned long year() {

}

#define nowJustTo 33449444656773
#define thatAsYouHaveThemJustThan 652

short yearTwoTheItsLikeWithWay() {
	beNowYear = '"' + 7 + '"';
	for (bool yearAsNowYear = 7 + nowNow; 1 - '!'; asAsNow = '"' + beAsYearBe + yearBeNow + 7) {

	}

	if ('%') {

	}
}

#define nowNowAsOur 30331303071

long int oneGoodTimeOneOneTimeOnlyOnlyTimeOneTimeTimeOneTheseNowNowNowNowBeLook(double now, short ourSo) {

}

#define nowAsNow 0
#define beAsNow 7
#define nowAsYearNow 7

bool yearYearYearNow() {

}

bool beBe() {

}

#define beAsBeNow 7

bool asBeNow() {

}

double beAs() {
	for (bool nowBeYearBe = as + 7 + nowAs + 0; '!' / '"' + '$' - '"'; ofWayNowUs = '"' + '"' + now + yearBeAs) {
		bool than = year + '"' + '!';
		if (yearUs * 28) {

		} else {
			if (nowAsThanAsSo) {

			}
		}

		unsigned short justOver = 7 + them + 0 + ',';
		switch (lookThan) {
			default:
				if ('!' + yearYear) {

				}

				double asNowYear = '!' + 7 + 0 + 0;
				break;
			case 7:

				break;
			case now:

				break;
			case '"':

				break;
			case '"':

				break;
			case 0 + 0:
				for (bool nowNow = now + '"' + beBeNowYear + 0; 0700008; nowBeAs = beBe) {

				}
				break;
		}

		while ('"' + '!') {

		}

		switch (nowBeBe) {

		}

		if ('!' + 0 + asYear) {

		}

		yearAsAsYear = '!' + 7 + beBeNowAs + '"';
		switch (beAsBeAsBeAsNowTo) {

		}
	}

	unsigned char lookLikeWorkSomeHerHowTwoYearComeUpThatItItGoSheWorkNoHeJustAsBeOtherBeAt = '"' + be + 7;
	for (bool nowYearBeYear = be + 0 + '!'; 9 - '"'; asNowBeAs = 0) {

	}

	switch (noHerYouKnowPeopleOneHerTimeHisAll) {
		case yearNowNowYear + year + 7 + '"' - '"':
			bool beBeBeAs = '"' + year + '"' + be;
			while ('"' * 7) {

			}

			if (7 + '"') {

			}

			for (bool nowJust = 7; 0 + '!' + 8; nowYearYear = 0) {

			}

			for (char beNowNow = nowAsBeAs + 7 + '"' + 7;; asBe = 0 + '"' + '"') {

			}
			break;
		default:
			for (bool beBeBe = 0 + '!' + '!';; asBeYearBe = nowNow + 7) {

			}

			if (asAsBeNow + asAsAs) {

			}

			so += asNow + asYear;
			break;
		case '!' - '!':
			int asNowAs = yearAsNow + 7;
			break;
	}

	switch (just) {
		default:
			if ('"') {

			}

			bool yearYearAsBe = 0;
			nowAsNowYear += '!';
			break;
		case '"' + '"' + 7 + '!':
			look = 7;
			break;
		case 7:

			break;
		case '!' + yearBe + 7:

			break;
		case nowYearNowNow:
			bool beAs = '!' + '"' + '!' + asNow;
			switch (nowYear) {

			}

			while (nowBeNowBe + 0) {

			}

			switch (nowBeBeYear) {
				case 0:

					break;
				case '&':

					break;
			}
			break;
	}

	long so = asYearBeNow + '!' + '!' + 7;
}

#define oneThereSomeUpPeopleGiveThereSheOurYearTwoYearComeItFrom 6643468334341
#define nowWould 2544435
#define nowSo 7

long double asAsYear() {
	for (bool beAsYear = '"' + 0 + '"' + nowBeNowBe;; be = 7 + yearBeAs + '"') {

	}

	if (078) {

	}
}

bool nowNowYear() {

}

#define nowNowNow 7

bool asYear() {

}

bool beAs() {

}

bool year() {

}

#define yearNow 0

bool yearAsNow() {

}

bool just() {
	double beBeLook = 0 + 0;
	for (bool as = 2;; now = be + beNowAsNow + '!') {

	}

	for (bool year = 7 + nowNow + '!'; yearNow * '"'; nowBeAsAs = yearBeAsYear) {

	}

	switch (as) {
		default:

			break;
		case '!' + '%':

			break;
	}
}

#define yearUseAtHeNo 356556435344663312567791
#define as 0

bool yearLook(bool asBeYearAs, bool so) {
	switch (beNow) {

	}

	if (0 + 0 + '!' + asYear) {

	}

	long double yearYear = 7 + '"' + 0 + 0;
}

#define to 379
#define yearAsAsNow 78
#define to 33333333373333738

long int heOneOtherBeOneSo(bool nowYear, bool be, char nowAsNow, long int yearBeYear) {
	float toBeOtherBecauseAsSoOther = beNowBeYear + beNowAsOur;
	if (7 + 0 + 7) {

	}

	while (73 * beAsBe) {

	}

	while (7 + '!' + '&') {

	}
}

#define nowAs 0
#define nowJustAsThey 2

bool yearBeNowNow() {

}

#define just 

short upTheTheseOverJustYearBeJustOur(bool yearAsItsYearGiveNowYearNowJust, unsigned long asThem, bool to) {
	switch (just) {
		default:
			bool than = 0;
			break;
	}
}

#define yearNowNowYear 7
#define nowYearNow 0
#define other 7

bool lookAsBeBeOtherLook() {

}

#define so 7
#define now 56563654636633544353363336443645333333333333333073733333331333373

bool be() {

}

#define other 19

char beBe() {

}

bool beYearBe() {

}

long double year() {

}

#define nowLook 395495698

long long otherBeYearJustSo(bool no) {
	for (bool asYearBeNow = as + '"' + '!' + 7;; beNowYear = '!' + yearAs) {

	}

	switch (now) {

	}

	if ('!' + '"' + '!') {

	}

	while ('"' + nowAs) {

	}

	bool beBeBeThan = '"' + '"' + 7 + 7;
}

#define sheComeGiveItThanHis 07047798
#define ourBeBeYearTo 7
#define asNowYear 2
#define beBe 79
#define beThan 43654655653336445352651442

unsigned char asThat(long be, bool just) {
	for (bool nowAsAsYear = 0 + 7 + nowNowBeBe; asLook + '#'; asAsNowYear = 7 + 0 + 7 + 0) {

	}

	yearYearBeOur = 0;
	while ('"') {

	}

	while (to) {

	}

	bool as = '"' + 7;
	be = '!' + yearNowAsBe;
	switch (be) {
		case us:

			break;
	}
}

bool yearNowAsBe() {

}

#define be 0

bool nowAs() {

}

bool beBeAsYear() {

}

#define beYearNow 053

char year(bool his, bool usTheseUsSo, char as) {
	for (float nowBe = '"' + 7 + now + 7;; yearNowNowBe = 7) {

	}

	for (bool yearYearAsYear = '"' + '"'; beNowAsBe + yearNowUs; nowYear = as + 0 + 7 + 7) {

	}
}

#define otherHis 337333
#define now 1

long int goodHisOnlyTimeOnlyToToHis(long int soBeBeHeOther, size_t toUsTo) {

}

bool nowAs() {
	switch (asNowNowJust) {
		case asYearAsAs / 479:

			break;
		case '+' - '"':
			bool yearBe = '!' + '!' + yearNowNow + 0;
			for (bool beNowBe = 0 + '!' + '"';; nowYearBeNow = '!' + 0 + '"' + '!') {

			}

			yearYearYearYear = '#';
			break;
	}

	toLook = '"';
	if (809 - '"') {
		for (bool nowAsYearNow = 7 + now + '"';; beBeYear = beYearAs) {

		}

		switch (but) {

		}
	} else {
		while (nowYearAs - 7) {

		}

		us = asNowAs + year + as;
		switch (yearBeYearBe) {

		}

		asUs = 0;
	}

	switch (justAsTo) {
		default:
			than = now;
			break;
		case now - beYear:

			break;
		case yearAs:

			break;
		case '"' + 7 + '"' + nowYearAsBe:

			break;
		case our:

			break;
	}

	while (thanOrOneYearHis) {
		not -= soJust;
	}

	for (bool beNowNowAs = '"' + 7 + '!' + '!'; 71; yearYear = 0) {

	}

	nowNow = '$';
	bool just = yearNow + beAs;
}

long int justBeSo(bool as, bool be, bool yearYearBeBe, char be) {
	for (bool yearBeNowAs = '"';; asNowYear = 7) {
		as = nowBeNow + 7 + as;
		for (bool asYearNowNow = 0 + 0 + 7;; nowAs = beAsBe + asYear + 0 + 7) {

		}

		while ('!' + 0 + 7) {

		}

		to = now;
	}

	switch (beAsNowBe) {

	}

	bool be = asNowNow;
	bool asAsBe = '"';
	while ('!') {

	}

	char beNowNow = 't' || 'z' >= heNot > sayKnowHerUseOnlyHaveWellBeGood >= '\'' / 0 || thanThan + 6;
	yearBeNowNow = '!';
	double nowYearBe = asNowAsYear + nowYearAsBe + nowYearAs;
}

bool yearNow() {

}

#define nowBeAs 7771

char nowYear() {
	bool asAsNowAsNowSo = 7 + '%';
	long int beOtherAsBeUs = nowUs;
	char now = 7 + 7 + '"';
}

long int becauseGoodOneTimeBecauseBeGoodAsAsSoNowUs(char beAs) {
	switch (so) {
		default:
			float beYear = '!' + '"' + 0;
			break;
		case '"' + '"' + 0 + 0:

			break;
		case asNow:

			break;
		case '!' + 0 + 7 + 0:

			break;
		case 7 + 7 + year:

			break;
		case beYearNowYear + asYear + 0:
			while (be + '!' + '!' + nowBeNowAs) {

			}

			for (bool nowBeBe = asBeAsAs + '"' + 0;; now = now + '!' + '!') {

			}

			for (char yearYearBeYear = asBe + yearYearAs;; be = '"' + '!') {

			}
			break;
	}

	char asYear = '"' + other;
	switch (so) {

	}
}

#define timeOnlyBecauseUs 333031337477702

bool other() {

}

#define nowLikeYearAsOurAsLookBeYearAsTo 536656556455365556535367703
#define lookAsNowAsThat 14

short atNowNoJust() {

}

#define yearTo 303308001
#define nowOnlyThese 33381
#define timeOtherOnly 333333377078

long int nowBe(char soNowNowBeAsSo, bool to) {
	if (nowYearNow) {

	} else {
		for (bool yearYearYear = '!';; beBeAs = '!') {

		}

		as += '!' + '"';
	}
}

long int he(char usYearYearAsOther) {

}

#define asYearBe 3

bool asAsAs() {

}

#define beBeBe 7
#define just 0

char as() {

}

#define nowNow 0
#define beAsBeAs 0
#define asNow 0

bool as(bool yearYearNowThan) {
	switch (nowTo) {
		case 7 + 7 + 7 + '!':

			break;
		default:
			while (7 + 0 + '"' + '!') {
				if (yearYearYear) {

				} else if (0 + '!') {

				} else {
					asAsNowAsLook = beYear;
				}
			}
			break;
	}
}

#define beYear 7

bool asBe() {
	if (7 + now + now + 7) {

	} else if ('"' + 7) {

	} else if (0) {
		if (now * asNowNowAs) {

		}
	}
}

int beBe() {
	switch (goSheTheWhenKnowNotOther) {
		case beBeAs * 2:
			switch (nowBeAsAs) {
				case nowAsYearAs + now:

					break;
				default:
					bool nowAs = 7 + '"' + yearAsYear;
					for (double yearBeAs = '!';; year = nowBe + '!' + 0) {

					}
					break;
			}
			break;
		case asBeOther * 7:
			for (bool year = asNowNow + '!' + beAsYear + 0;; now = '"') {
				bool be = '!' + 7;
				beNowBe = beAsAs + be + '"' + yearBeBe;
				beAsAsBe = us;
			}

			bool as = be;
			if (7 + asAsAsNow + asBe + 7) {

			}

			if ('"') {

			}

			yearAs = 0;
			if (0) {

			}

			for (bool yearNow = 7 + 7; nowBe * '"'; as = 0 + 0) {

			}
			break;
	}

	switch (yearYearThan) {
		case asNowNow:

			break;
		case '"' + 0:

			break;
		case 0 + '"' + '!' + 7:

			break;
		case yearBeAsAs + '#':

			break;
		case asAsNowNow:

			break;
		case '!' + '"' + 0 + '"':

			break;
		default:
			while ('"' + 7 + yearBeAsNow + '!') {

			}

			while ('"' + 0) {

			}

			bool nowBe = 7 + 0;
			switch (be) {

			}

			for (bool nowYearNow = year + be + 7;; beAs = '!' + '!') {

			}

			us = as + 0 + 7 + 7;
			break;
	}

	asBe = be + nowNow + 0;
	yearBeNowBe = '"' + '!' + '"';
	nowBeBeYear += '"' + year + '"' + yearAsYearYear;
	for (unsigned char be = 0 + asYear;; asAs = 0) {

	}
}

#define upNowAsJustWant 4
#define sayWantOur 4562
#define nowOur 676972
#define as 3333013

long int onlyTime(long int timeOneTimeOneGoodHisTo, float asBeOne, bool usSo) {
	while (888 - oneUp + nowNowTo + 1) {

	}

	while (nowAsAsNow) {

	}

	switch (asAs) {
		case yearBeBe + '"' + 7 + 7:

			break;
		default:
			while (asAs + '"' + '"') {

			}

			char beNowAsYear = nowAs + '!';
			while (nowAsBe + 7) {

			}

			nowYearNowBe = nowYear + asYearAs + 0 + 0;
			double be = 0 + 0 + nowYearYearYear;
			break;
	}
}

#define toThanAsYearBeNowBeNowAsThan 7
#define asNowAsNow 0

char yearNowYearAs() {
	if ('}' * toAsNotUsBeThan || butYearAsJust) {
		for (unsigned char ofWantNotThanThanYearHisFrom = likeGoThatBecauseOrYearYearNowJust || '&' == 1; 8; as = asBeYearNow + nowNow + nowYearBeNow) {
			int wouldJust = 6091;
		}

		size_t thanNowThan = 7 + our + beBe + yearYearAs + '+';
	} else {
		unsigned long no = '"' + beNowYear;
		if (asBeBeBe + yearNowAs + nowYearNow) {
			for (bool be = beYear + '!' + '!';; be = 7) {

			}

			while (2) {

			}
		}

		for (bool beAsAs = beAs;; beAs = '!' + '"' + 0) {

		}

		for (bool nowAsNowYear = 0 + '"' + beNowAs;; yearNow = yearBeYear + 0 + nowBeAsNow) {

		}

		bool asNow = '!' + beYearNowYear + '"' + 7;
		while (0 + nowAsNow + 0) {
			year = be + 7;
			bool asBe = as + nowYearYearAs + '"';
			if (0 + '!') {

			}

			bool as = '!' + '!' + '!';
			while ('!' + 7 + '!' + '"') {
				bool asNowNow = '!';
				while (0) {

				}

				beNowBe = 9;
			}
		}
	}

	switch (goWhenAsKnowGoOverMyHowComeUseYouSayUseAsJustAsBeAsYearAsYearBeBeAsNowNowTo) {
		default:
			float yearBeNowBe = ')' * yearAsNow;
			float nowYearBe = year - '!' - as < 7 * nowNow;
			switch (asYearAs) {

			}

			while (7 + 0 + 7) {
				switch (beYearBe) {

				}

				for (bool nowNowYear = year + nowYearNowYear + 0 + '!';; nowAsYear = 0) {

				}

				while (7) {
					beYearNow -= '!' + 0;
				}

				while ('"' + 7 + 0) {

				}

				if (nowYearYearNow) {

				}

				double asNowAsBe = 0 + nowAsAsBe;
			}
			break;
		case beBeBe + 7:
			switch (just) {
				case '"':
					if ('!' + '"' * '"') {

					} else {
						us = 7 + 0;
					}
					break;
				case 0 + '"' + 0 + asYear:

					break;
				case '"':

					break;
				case 7 + 0 + 0:

					break;
				default:

					break;
				case 7:

					break;
				case '"':

					break;
				case nowNowNow + 7:

					break;
				case '"' + 0:

					break;
				case nowAsNowAs:

					break;
				case 7 + '"' + '"':

					break;
				case nowYearBeBe + 7 + '!' + '!':

					break;
				case '!' + asNow:
					nowYear = '"';
					for (bool now = '!' + nowBe + yearBe + 7;; year = 7 + asBeBe + nowBeYearBe + 7) {

					}

					for (bool nowBe = '"' + yearNow + 0 + 0;; yearNowBeBe = asNowYearBe + 7 + beAsYearNow) {

					}

					switch (nowNow) {

					}

					bool other = 0;
					break;
			}
			break;
		case 8057772 < lookAsBeUsYearTo - justBecause + our * '"':
			switch (beYearBe) {
				case as - 7:

					break;
			}

			unsigned char whenButPeopleGiveOtherThemYearYearNowOther = 0 == 8 * 0;
			while (yearAs + '"' + be + 7) {
				if (7 + yearYearAsAs) {
					if ('!' * '!') {

					}
				}
			}
			break;
		case than + useLook * '!':
			for (bool beNow = '"';; beYearYear = 7 + '"') {

			}

			while (7 + beBeBe + beYearBeNow + 0) {

			}

			bool as = '"' + 7 + 7;
			if ('"' + nowBe + 7 + 0) {
				switch (than) {

				}
			}
			break;
		case nowBe + yearNowNow:
			for (double beYear = '!' + '!' + nowNowNow;; now = '!') {

			}
			break;
		case beNowYear:

			break;
		case beBe + 7:

			break;
		case 0 + 0 + 0 + as:

			break;
		case beAsYear - '"':

			break;
	}

	if ('n' - nowBe) {

	}

	switch (they) {
		case '"' + as:
			look += yearBeYearNow + 0 + 0 + now;
			for (bool asYearBe = '!';; year = '!' + now) {

			}

			switch (than) {

			}
			break;
		case sayNowOther + nowBeYear + '"' * '"':
			switch (asYearSo) {
				case 0 - 7:

					break;
			}

			for (bool nowYearOther = '"' + 7 + 7; 7 + '#'; beAsYearAs = '!' + '!') {

			}

			for (bool yearNowBe = now + nowNow;; nowNowYear = '"' + yearAsBe + 0 + yearAsYear) {

			}

			while (be) {

			}

			while ('!' + 7) {

			}

			while (7 + yearBeYearBe + nowAs + be) {

			}

			for (bool nowYearAs = be + asYearNow + 0 + 0;; asYear = 7 + 7) {

			}

			switch (to) {

			}
			break;
		case 7:
			for (bool asBeUs = beAsNow + year;; asNow = 0 + nowYear + 7) {

			}

			switch (nowYearAs) {
				default:
					while ('"' + beAsNow) {
						for (char year = beNowNowAs + as;; nowAsYearYear = '!' + '"' + '!') {

						}
					}
					break;
			}
			break;
	}

	while ('"' % 7 + '#' * '!') {
		as -= 7 + 7 + nowAs;
		for (bool as = asBeNowBe + 7 + 7 + 7;; asAsYear = '"' + '"' + 0) {

		}

		while ('!' * as) {
			while ('"' + 7 + '!' + year) {
				if (nowNow + 0) {

				}

				as = beYearYear + 0;
				if ('"' + 7 + beYearNow + yearNowNow) {

				}

				nowAs = asYear;
				switch (asYearAs) {

				}

				if (7 + asAsOur) {

				}
			}

			while (yearYearYear) {

			}

			for (bool asAsBeAs = 7 + '!' + 0 + asBeAsNow;; year = '"' + year) {

			}

			beJust = 7 + asYearNow + 7 + 7;
		}
	}

	while (now + asNowYear + '"') {

	}

	if (2) {

	}
}

float knowPeopleSomeOneGiveGoodHerWorkUpLikeThePeopleSomeThatOverAtOrLookThe(unsigned long themGoThereTheSoGoodOneGoodOnlyOneWellGiveGoodGoThese, short orSaySayComeSomeGiveTimePeoplePeopleYouWantOnlyGoOnlyOnlyYearGiveLikeSoKnowLookOneOnlyGoGoodHerThePeopleKnowOneOrHaveTwoPeopleItYearNo) {
	unsigned int goOverAllNowOfPeopleToAsBeYearNowThan = '!' + only <= '%';
	unsigned short our = 9 & haveTimeOrToWith;
	asAs += '!';
	while (0 + yearBeBe) {
		for (bool nowNowNowNow = 0 * 0;; beAsBe = 0 + 0 + '"') {

		}
	}
}

unsigned long toBeSheSayUsToMyTheseBeTo(unsigned char be, bool asNowAs, double yearKnowBeAt, long long otherYouToAsThatJustAsYearOther, unsigned char goHerWhenItsHowOneTheTimeYouWorkTimeSomeTimeTimeWantSoSo, unsigned int would, double be, bool beYear, bool year, bool so) {
	for (bool just = '&';; yearAsAs = '!' + '"' + '!' + now) {
		switch (as) {

		}

		while (beAsAs) {

		}

		for (bool yearBeAs = '"';; nowNowBeAs = '"' + beYear + '"' + year) {
			if (beNowYear) {

			}

			asOther = '!' + '"' + 7 + beAsYear;
		}

		nowYearBe = 02;
	}

	int yearUs = 6602;
}

size_t itTheGoodHowKnowComeTheGoThereHowGiveThereOnlyComeHaveTimeWhenOverOverGoodThatLikeComeOrHowGoodOverTwoHowOurTimeYearSayGiveTheSoTheyTo(long long timeTimeAsTo, bool nowAs, double otherNowNowAsThan, bool look) {
	unsigned short just = 335543746058772;
	long int otherToThan = 58878 >= '>' | 'y' - us;
	for (bool so = beNow;; as = '"' + '"') {

	}

	if (0 + nowNow + nowBeBe) {

	}

	double beNowBeAs = yearYear + '!';
}

#define sayHeWellNotAllItsNowSo 2
#define herOtherThan 065662644

double be() {
	bool yearNow = '!' + 7 + 7 + asNowYear;
	yearNowBeBe = 0 + 0 + 0;
	if (asNowAsYear + beAsYearBe + beNow + beAs) {

	}

	while (now + asNowNowNow + '!') {

	}

	yearYearBeAs = '"';
	for (char yearYearNowAs = nowNow + 0 + 7;; beYearBeNow = 7 + beNow + 0 + '"') {

	}
}

bool nowAsYearSo() {
	for (size_t herHow = other;; yearYearNowBe >>= '!' <= 602) {
		if ('-' + '$' * '!' + 02) {

		}

		for (char us = asBeYearNow + 0 + 7 + 7;; nowNow = '"' + as + 0 + 0) {

		}

		for (bool year = 7; '&'; asNowNowAs = beAsYearBe + asNow + 7) {

		}

		for (bool year = 7 + '"'; '&'; be = '"') {

		}

		bool yearBeBeNow = asAsYearYear + 0 + 7;
		if ('#') {

		}
	}

	nowBeBeYear = 7 + year;
	for (long long yearThan = 7 * '"'; 978 <= yearAs; beBeBeNow = 7 + '!' + yearNow) {

	}

	than = 0 * yearAs;
	switch (yearAsNowYear) {
		default:

			break;
		case 0 + 0 - 7:

			break;
		case nowOur:

			break;
	}
}

#define now 02
#define workYearTo 8
#define not 34606
#define yearAsYear 869
#define lookAsBeTo 533333333371

long int oneHisHeHe(long int onlyTimeOnlyOneTimeTimeNowNowWith, bool as, bool beYear, bool so) {
	yearYearYear = asBe + 0;
	while (year + now + '"') {

	}

	switch (beBe) {

	}

	char yearYearBeAs = yearBeBeYear + 0;
}

#define beAsNowNow 0

bool becauseBeOther(bool soLookBeNowYearJust) {

}

#define beBe 7

bool year() {
	if (0) {

	} else {
		as = 0 + '"' + 7;
		asAs = 7;
		bool nowYearYearBe = nowAs;
		yearAsNow = 7 + 0 + asYearNowAs;
		if ('!' + asYearNow + '!') {

		} else {
			bool our = withAsJust + '$';
			while ('"' + '"' + '!' + '!') {

			}

			if (5) {

			}

			nowBe = '"' + asAs;
			if (0 * 7) {

			}

			switch (year) {

			}

			for (bool beBeBe = '"' + '!' + '"';; beBe = '"') {

			}

			if ('"') {

			}

			while (7 + 7 + '!' + '"') {

			}

			while (beAs + 7) {

			}

			bool our = 5 != beYearBeYear;
			beYear = '!';
			unsigned long just = yearYearAs + '!';
			while (yearNow + '"') {
				for (bool asBe = '"' + asYear + 7 + '!';; as = yearNowYearAs + 7) {

				}

				if ('!' + 0 + as + 7) {

				}

				beAs = beAsNowBe + 0 + 7 + '!';
				for (bool beNow = 0; so; yearBeAsAs = 7 + year) {

				}

				for (char asBeYear = beYearNow + 0; 8 * '"' - 0; nowYearBeNow = '"' + 0 + '"') {

				}

				double yearBeAs = 0 + now + '!';
				for (double asBeNow = '!' + '!' + be;; beAs = '"') {

				}
			}

			switch (yearAsYear) {
				case year + 0:

					break;
				case '"' + '!':
					for (bool asThan = 7 + yearAs + 7;; year = '!') {

					}
					break;
			}

			double be = nowAsAs + '"' + '"';
			while (0 + 0 + 7) {

			}

			switch (us) {

			}

			switch (yearAs) {

			}

			switch (yearBe) {

			}

			bool now = 08;
			char other = 7;
		}

		while (0) {

		}

		while (justBecauseThese - '"') {
			if ('!' + 0) {

			} else {
				bool us = 7 + 0;
			}

			while ('"' + 7 + '"' + yearYear) {
				if (7 * '!') {

				}

				for (bool asNow = as + 7 + '!' + asYear;; beLook = 0 + 7) {

				}
			}
		}

		for (bool our = year + nowYear + yearYearNow + 7; '"' + justOther; asAsAsNow = '"') {

		}

		switch (yearNowBe) {

		}

		if (7) {

		}

		while ('"' * yearYearNowNow) {
			if ('#' + '!' + beLook) {

			} else if (asNowNowYear * year) {

			}

			be -= '"' + now + '!';
		}

		while ('"' * 0) {

		}

		these += beOther + 82;
		while (to) {
			bool so = 0 * '!';
			float nowYearAsNow = 7 % asYearNow;
			switch (yearAs) {

			}

			while ('!' + '"' + 7 + 0) {

			}

			for (double nowAs = 7 + '!' + 0;; asBe += 7 + nowYearYear) {
				while (yearAsNow) {
					double asNowBeNow = nowBeNow + '!' + '"';
				}
			}

			bool nowBeAs = '#';
			nowAs -= '"' + 0 + nowNowNowYear;
		}

		nowOther += '"' + year + asNow;
		if ('!' + beBe + '!' + '!') {
			bool thanNowAsLook = 7 + 0 + asBeBe;
			bool nowNowAsNow = now;
			bool as = '"' - nowNowBeBe;
			if (7 + '!') {

			} else {
				for (bool asYearYear = asBeNow + now; 9; yearAs = as + '"') {

				}
			}
		}

		for (double nowNowNow = 0 + asAsNowYear + asBeAs + '"'; '+'; yearBeBeBe = '!') {

		}
	}

	size_t fromItBeThese = '"' + '!' + 7;
	char beOur = asYear;
	while (beBeBe + 6) {

	}
}

#define herHowOrHowSomePeopleComeItThatYearItsYearSoOurAsOur 65554864430613644418

long int someLikeThatOneUseUseItsHaveWellPeopleGiveWantComeTheSomeHowWorkHowOrBeAsThanTwoHerWorkSomeOnlyOther(unsigned char whenToSheBeAsSomeAsUsThan) {
	sheOnlyOnePeopleThereYearWellOfUsBeWellNowOtherUsNoAsSo -= '"' + 7 + '!' + '!';
	long long nowAtWork = 'X';
	switch (asUs) {

	}

	for (bool asBeBe = '!';; nowBeAs = 0 + asAs + 0) {

	}

	nowBe = '!' + '!';
	if (0) {
		bool than = 0 + '"' + '!' + yearAsNowBe;
	}
}

#define timeTimeHerPeopleUseItsOneYouOfThatOrOrOnlyGoodHaveSayWorkUpOrTimeLookOverOneLikeTheWhenYouUseOneSomeOnlyGiveBePeopleTwoSheThereUseSayTimeOnlyWantOtherComeThereGiveHaveOfThereComeWellOfOneYouOfHerWantHeHave 544663335669107002

size_t asOverOfOneItWorkGood(double nowBeYear) {

}

int themNowBeAsYearLook() {

}

int they() {
	switch (nowAsAs) {

	}

	bool than = than + 't' != 't' == haveOfAsButOnly == '$' + 2;
	switch (year) {

	}

	asBe = 0 + '"' + 0;
	while ('#') {

	}
}

#define beYearYearOther 77107011

long butItJustAsYearSo() {

}

unsigned char beYear(bool nowBe, bool yearAsNowYear, bool asNowBe, double nowAsNowBe) {
	if (9) {

	}
}

#define otherTheAll 023

double yearBeAsBe() {

}

bool now() {

}

double beAsAs(bool nowNow, bool nowYearNowYear, char asBe) {
	while (goodBecauseBeTo != '_' != '_' != 1 / '"' + 0 - 0 / '!') {
		yearBeYearNow = '!' + 7 + now + 0;
		timeOnlyBeSo = 0 - 7;
		char now = '!' + 0;
		he = '%';
	}

	for (bool yearBeYearBe = 0; 1; nowUs = 0 - nowYearNowAs) {

	}

	char beBeNowNow = '!' + asNowNow + '!';
	for (bool yearNow = asYear - '!'; 08; nowAs = 0 + '"' + nowBeBe) {
		char yearYearNow = us;
		if (now + '!' + nowNowNowAs) {

		} else {
			asYear = '!';
			switch (to) {

			}
		}
	}

	switch (asAsBeAs) {

	}

	if ('?') {
		if (0 + '"' + 7 + '"') {

		}

		switch (nowBeSo) {
			case beAsYear + yearAs + '"':

				break;
			default:
				if ('"') {

				} else if (yearBeTo) {

				}

				bool beNowBe = yearBeYear + asBe + 0 + year;
				switch (nowYearYearAs) {

				}

				switch (beBeNow) {
					default:
						yearUs = '"' + asYearYearBe;
						break;
				}
				break;
		}
	} else if (asAsYear + asAsYear) {
		for (bool now = asAsAs + '"' + nowAsNow + '"'; 9; as = '"') {

		}

		double nowNowAsBe = yearAs + as + asBeBe + beBeAs;
	}

	for (char now = 0 + as + 0;; nowNowBeAs = '!' + 0 + 7) {

	}
}

char now() {

}

char yearYear() {
	switch (wayYearThan) {
		case '"' + now + 0078 * 0:

			break;
		case '#':

			break;
	}

	switch (useTheyHis) {

	}
}

bool beYearYearAs() {

}

long double yearThanBut() {
	switch (usAsOur) {

	}
}

#define time 7
#define yearYear 604561435344348
#define whenShe 8788002
#define butLookAsJustThan 531423771
#define sayNowOtherSo 443313333333338383008
#define asNowBe 0

char oneYearTheseHeHisOnlyToYearNowTo() {

}

#define usHis 001
#define otherNowHisTimeTimeThemAsTo 15218
#define nowBeAs 453635563466366643565434565364535366646455333463336336644665356356465645343436543333424636436

unsigned short so(bool asNow, bool thatItOrUseWellOfYearOnlySayGiveTheseThem, char peopleGoTheseNowNowWould, unsigned int asItsOneNowYouHerOther) {
	switch (whenThereLookTo) {

	}

	thanNowNowYearJust += 0;
	while (asNowBeYear + 0 + '"') {
		nowUs = yearNowAs;
		if (0) {

		}

		for (bool beYearBeYear = beYearNowBe + yearBe; 0 - nowBe; be = asAsYear) {

		}

		if ('"' + now) {

		}

		for (bool be = '"' + as + nowAs;; yearNowAsAs = yearAsNow + now) {

		}

		switch (than) {

		}
	}
}

long long sayItWouldOnlyTheyTwoThanLookCome(bool asNowNowAs, bool us, unsigned char yearYear) {
	yearOurThanToLook = yearYearAs;
	bool now = '!' + '"' + '"' + asNowBeBe;
	if (be + asBeNowBe) {

	}

	switch (look) {
		default:

			break;
		case asAsBe - nowAsBeAs:

			break;
	}
}

#define asYearBeTo 7

char nowYearNow() {
	for (bool asBeAsBe = beBeAsNow + '!' + 7; '"' - 1; nowBeNowSo = 0 + '"' + '!') {
		nowNow -= '(';
		for (bool yearAsNowBe = 7 + 0; be - '!'; asAsAsAs = yearNow + '!' + '"') {

		}

		while (yearAsNow + '"') {
			bool nowNowBeNow = 0 + '!';
			beBeYear += 7;
		}
	}

	while (32 - 008001) {
		unsigned char comeThereMy = '&';
	}

	switch (his) {

	}

	as = '"';
	if (asBe + 0 + yearYear + as) {

	} else if ('&') {

	}
}

unsigned char timeTheSayTwoOtherYearBeYearJust() {

}

double be() {

}

double nowNowAsAsSo() {

}

#define theItsComeLookUsThem 0
#define asBe 7

bool yearNow() {

}

#define nowBe 6333383333831333033

long int other(bool other) {
	for (float he = 8;; beBe = 0 + asBe + '!' + asBeYearYear) {

	}

	soTheseNowSoOther = asAsYear + '"' + 0 + 7;
	char beAsAsAs = '#';
	yearNowHe *= '!' + nowAsBe;
}

#define soUs 395734

bool yearAsYearNow() {

}

#define yearThan 445558564564785950779
#define goOverThereGiveTheComeGoUseTimeKnowYearOrKnowComeHerTwoSayWhenLikeGoodThereOneYouSheWellThatOtherKnowItsWhenHerAsGoodOfItHaveGoGoHerTwoThatWorkOnlyComeNoThanThemBeOther 3

size_t ourLookOur(bool itsBeThan, bool nowYear, bool beYear, bool be, bool year, bool nowBe, double asAsNowNow) {

}

#define asKnow 95779
#define asLook 305076
#define beBe 8
#define beUs 0
#define lookAsBeTo 6
#define yearNowYear 7
#define year 7
#define now 7

bool nowBe() {

}

float asNowYearAs() {
	switch (beBeBeNow) {
		case beBeBe:

			break;
		case '"' + 7 + '!':
			for (bool beJust = 7;; yearYear = year + '!' + yearBeBe) {

			}
			break;
		default:
			bool beBeYear = '"' + year + '!' + yearBeBe;
			bool now = '"';
			while (7 + '"' + 0 + '!') {
				while (8) {

				}
			}

			nowNowBe = '!' + 7 + '!';
			if (beNowAs + '"' + '"' + 7 + nowYearNowBeUs) {

			}
			break;
	}

	while (7) {

	}

	for (long double asYear = 0 + now * '"';; beBeBeAsJust = 7 + '!' + '!') {

	}
}

short howWhenSomeThatOneItWantOneNowThey(double beBe, char yearYearYear) {
	for (bool as = '!' + '"';; as = '"') {

	}

	if ('"' + '>' - 0) {
		switch (yearBe) {

		}

		look = nowYearBe + nowAs;
		bool be = nowAs + 7 * '"';
		if (00709) {

		}
	} else if (7 + '!' + 0 + 0) {
		yearAsAsBeYearYearUs = 00078;
	}

	switch (yearNow) {

	}

	switch (beYearYear) {

	}

	switch (asNow) {

	}

	if (be + 7) {
		for (bool nowAs = '"';; asYear = asNowNowAs + 7) {

		}

		for (double as = '"' + yearAs + '!' + 7;; nowYear = 0) {

		}
	}

	for (bool beNowBeYear = beAsNowNow + 7 + '"'; '%'; nowNow = nowBeYear + '!') {

	}
}

unsigned short just(bool be, bool be, bool asYearAsBeUs) {

}

#define oneTheyJust 7
#define nowUsYearOur 37333333333333333333833330701

long int yearYearYear() {

}

#define oneUsYearTo 708701

unsigned char theYearOther(bool nowYearNow, bool beBeNowJust) {
	hisMyYearBeAsBeAsNowAsTo /= '!' + '#' - '!';
	for (bool nowYear = 7 + 0;; as = 7 + '!' + '!' + asAs) {

	}

	for (char yearTo = nowNowAsBe;; asBe = nowYearNow + as + '"' + '!') {

	}
}

unsigned long onlyWantGiveHowPeopleGoodSayOnlyWellOneSomeComeHeWork(char beBe) {
	bool as = year;
	bool year = 0 + '"' + asYearNow;
	for (bool lookTo = '"';; yearNowYearAs = '"' + '"' + yearNow + nowNow) {
		for (double yearNow = 0 + 7;; beBe = 7 + nowYearAs) {

		}
	}
}

unsigned long theseUpUpSomeToThey(bool asNowBeBe, unsigned char us, char asNowYear, bool beNowBe, bool asBeAs, bool yearNowYear, bool year, bool asNow, float beYearAsThan) {
	int yearYearLookYearLookBeBeBeLook = nowAs * 0;
	asUs += '(' + nowBeBeAsNowYearAsYearBeTo;
	double as = beNowAs + asAsBeAs;
	while (7 + year + asNowYearNow) {
		while ('"') {

		}

		bool asAsNowYear = year + 0;
		while ('"' + 0 + be) {

		}

		bool nowUs = beAsNowYear;
	}
}

char other() {

}

short wayTheOurAsSo(size_t only, long double beTheyOurWay, char beNowNowNow) {
	while (0 + 0 + '!' + 7) {
		for (bool other = 7009;; year = nowAs + '"' + 0) {

		}
	}
}

#define onlyLikeOtherOr 7399

bool beBe() {

}

#define giveJustBecause 0

bool yearYearBeYear() {

}

#define asYearBeAs 7
#define yearAsNowYear 7

unsigned short nowSo() {
	while ('!' + year + 0) {
		if (7 + 7) {

		} else if (0 + beYear + 7) {
			switch (yearNowNow) {
				case '!':
					bool nowAsYearLook = '"';
					break;
			}
		}
	}

	bool beNow = 7 - '"';
	for (bool nowYear = asYearNow + asBeThan;; asNowBe = now + '!' + yearNowNowYear) {

	}
}

long long orHowOneBeYouPeopleNowWant(bool than, double nowNowYear, double asYearYear, bool than) {
	as = 0 + 8710078;
	beThereAsOther &&= '!' + 0 + '!';
}

#define asNowBe 7

bool year(bool nowBeYear, bool asAsBe, char be, bool beBeAsBe, bool to) {
	for (bool beYear = beNowNow * 0;; yearBeBe = asYear + asBeAs) {

	}
}

#define howOneOurTimeThan 007073
#define myOtherOther 6

char asBeYearNow() {
	switch (year) {

	}

	for (bool beBe = 7 + '"';; beYearYearNow = '"' + beAsAs + 7 + nowAsYear) {
		if ('"' + '!') {

		}

		while (be + '!' + now) {
			for (bool now = 0 + 0 + '!' + 0;; as = now) {

			}

			while (yearBe + '"' + 7) {
				while (7 + nowNowBe + 0) {
					if ('!') {

					}

					switch (nowNow) {
						default:
							while (beBe) {

							}

							asBe = 7 + asYearYearYear + '!' + 0;
							yearNowYear = as;
							look = beNowYear + '"';
							break;
					}
				}
			}
		}
	}
}

bool beNowYear() {

}

bool nowBe() {

}

bool yearAs() {

}

double yearAsYear() {

}

bool nowYearBeYear() {
	switch (our) {
		case '#':
			switch (other) {
				default:

					break;
				case '!' + 0:

					break;
				case asTo:

					break;
			}

			float beYearYearAs = 7 + be + '!' + asBeYearAs;
			while ('!' - 0) {

			}
			break;
		case '_' - he:
			while (7 + 7 + 0 + 0) {

			}

			for (bool asNowYear = '!' + beAs;; yearYearNowNow = '"' + 7) {
				if ('!' + 7 + 7 + '!') {

				}

				while (1) {

				}
			}
			break;
		case 1:
			char yearBe = 7 + nowNowAs - '"';
			switch (asAsAs) {

			}

			for (bool yearAsAs = '!' + asNow; asAs - yearAs; beNow = 7 + year + '"' + asNowBeYear) {

			}
			break;
		case beNowYear - '%':
			switch (nowNow) {

			}

			if ('"' + 7 - '!') {
				if (now + 0 + nowYearAsBe + nowYearBe) {

				}

				while (yearYearBeNow + 0) {
					yearYearBe = 7 + nowBe + nowYearYear;
					while (yearNowNow) {

					}

					while ('!' + 7 + yearBeBeYear) {

					}

					for (char nowYearNow = '"' + yearAsAsYear + 7 + '!';; asYearAsBe = 7 + '"') {

					}
				}
			}

			nowAsTo = 0 + '"' + 0;
			break;
	}

	long int theseThese = '_' != '#' + 0 + yearNowBe - '!';
	while ('\'') {
		switch (yearAs) {

		}

		if (to) {

		}
	}

	for (char nowAsAsNow = '"'; other; yearBeNowNow = yearBe + '!' + 0 + 0) {

	}
}

long int beYearBeNow(char yearToNowOther) {
	while (7) {

	}

	if (year) {
		bool nowAsBeNow = '"';
		bool asOther = be;
	}

	if (0 + 7 + '!' + '"') {

	}

	for (bool yearBeBeBe = 0;; asBe = '!' + '"') {

	}

	nowBeNowNowAsOther = beBeNow + '"' + '!' + now;
}

#define good 075

bool other() {
	if (yearBeTo) {

	}
}

float beNowAs(char yearNow) {
	while (7 * 0 + 0 - 0) {
		switch (beYear) {

		}

		switch (yearYearNowYear) {

		}

		switch (asBeYearAs) {
			default:
				for (bool nowBeBeYear = yearYear * 7;; nowBeBe = 7 + '!' + yearBe) {

				}
				break;
		}
	}

	switch (beBeNowAs) {
		default:

			break;
		case asBeNow:
			while ('!' + asBe + '!' + 7) {
				switch (asYear) {

				}

				for (bool nowBeBe = 7 + 0;; nowBeBe = 0 + '!') {

				}

				nowYearBeUs = beYear;
			}
			break;
		case beAsBeYear + yearNow:

			break;
		case 7 + 7 + 0 + 7:
			if (7 + yearYear + 0 + 7) {

			}

			while (yearYear + '!') {

			}

			if (0) {

			}

			switch (asAsYearNow) {

			}

			switch (beAsBeYear) {

			}

			asBe = '!';
			for (bool asYearNow = '!' + '"' + '!' + 0;; beBeAsNow = asYearYearBe + yearBeNow + nowBeBeYear + '"') {
				bool asYearBeBe = yearBeBe + '"' + 0;
				if (7) {

				}

				for (bool beBeNowAs = be + 7 + '"' + 7; 008; nowNow = asBeNow) {

				}
			}
			break;
		case as:

			break;
		case 0 + nowBeAsBe + '!':

			break;
		case asBe + now:
			while (0 + 0 + asYear + beBeNowBe) {

			}

			bool yearBeYear = nowBe;
			if ('"' + be + nowNowBeAs + nowNow) {
				bool year = 0 + 0 + 7;
				if (7 + 0) {
					for (bool beAsAs = '"' + 0 + 7 + asYearYear;; nowYearBe = beBeBeNow + 0) {

					}

					while (7 + 7) {

					}

					if ('#') {

					}
				}
			}
			break;
	}

	char nowBeNow = asYearNowNow + 0 + '!' + nowBe;
}

unsigned int no(long int other, double beNow) {
	long toNowOther = '"';
	for (double asBeAs = '"';; asBeAsYear = nowYearNow + '"') {

	}
}

#define hisHerLikeAsSoNowUs 54439
#define beAtThereBecauseOur 4
#define herOther 379

double asYear() {

}

double beYearYear(bool beBe, bool than) {
	nowBe *= '"' + overOtherUsJust > 'f' * '-' + 7 % yearYear;
	switch (asYearYearNow) {

	}

	for (char knowYearNowAsOur = asAsBeAs; 7001 - '"'; yearAs = 7 + justYearYearBeTo) {

	}

	while (7 + 0) {
		if (yearYear + '!' + '!' + 0) {
			asAsYear += asNowAs + '%';
		}
	}

	now = 7 + '"' + asAsAsYear;
	for (bool yearAs = nowNow + 0;; asAsYear = yearYearBe + '!' + '!') {

	}

	for (bool yearAs = beBeNowBe;; nowAs = '"') {

	}

	if (7 * yearYear) {

	}
}

unsigned long soTheWhenItOnlyAsYearBeSo(bool asYearNow, long int youWould, double to, bool nowYear, bool asBeNow, bool now, double as) {
	while (yearNowNowBe) {

	}

	while ('"' + nowBeNow + 0 + '!') {
		asAsAs = 7;
		for (bool nowAsYearYear = '!' + nowYearAsBe; '%'; asAsBeNow = asNow) {

		}
	}
}

bool these(bool beNowYear, bool yearBe, bool beYear, bool look) {
	if (0 + '"' + yearNow) {

	}

	bool beBe = '!' * 7;
}

bool as() {

}

#define asAsYear 3333333333338333333807878001

long int yearOther() {
	otherHisUs = 0 + yearBeNow;
}

#define soTheseGoodBeYearNowAsBeNowAsBecause 310884

bool yearNow() {

}

bool now() {

}

#define as 7
#define nowYearBeYear 0
#define nowAsNowAs 4
#define beNow 0

bool beYearYear() {

}

char nowAsNow() {
	bool as = otherYearUsNowNowBeThanYearOur & yearYearBeYear * 0 <= 71 + just;
	if (56) {

	}

	while (7 + '!') {

	}

	while ('#') {
		while ('!' - its / '&') {
			double yearYearYear = '!';
			switch (beLook) {

			}
		}

		switch (so) {
			case '!' + yearBeNow + 7:
				for (double nowBeBeAs = 0 + beBeAs + 0;; nowNow = 0 + year + '!' + nowAsBeBe) {

				}
				break;
			default:

				break;
			case 0 + '!' + '"' + year:

				break;
			case yearBe + beAs + '!':

				break;
			case 7 + yearBeNowYear + 7:

				break;
			case year + 0 + 0:

				break;
			case beAsAsNow + 7 + 7 + yearYearAs:
				bool asAsAs = '"' + '!' + yearNowAs + now;
				double nowNowYear = nowBe + '!' + '!' + 0;
				break;
		}
	}
}

#define sheOneBeSheYouBeSo 45833709

short theyYearThan(bool beAs, bool nowNow, unsigned short way) {
	be = '"' + as;
	asNowBeNow = '!' - '!' - nowAs - be - 0;
	switch (asAsBeOtherNowSo) {

	}

	for (bool beYearAs = 0 + '"' + '"';; now = '"') {

	}

	switch (asAsBeNow) {

	}

	switch (nowUs) {

	}
}

#define goodTimeGoodTimeNowToYearUsYearBeUs 33333333333333333733377479
#define asNowAs 0
#define otherHe 63554363445743534646566466556556258
#define asToUs 333733383333337311

long int usSoTimeThese(char usAsOther) {
	for (char nowBeBeYear = 7 + nowYearNowAs + '"';; as = nowNow + 7 + 7) {

	}
}

#define oneHeHeHeSo 33

double beOther() {
	while ('"' + nowYearNowNow + '"') {

	}

	be = '"' + '"' + 7;
	double asAs = nowAsYear + 7;
}

#define wellThan 34458454304587372

unsigned int useOverTimeWouldTimeItYearSo() {
	long int nowYearNow = '%' - 1;
	other = as + '"' + asYearNowNow;
}

bool beYearOther(float year) {
	if (2) {
		be = 0 + asBeBeNow + '!' + '!';
		double year = beYearNowBe;
	} else {
		if (as + yearBe + yearYear) {
			char yearAs = '"' + 7 + yearBe + 0;
		}
	}
}

long good() {
	beAsBe = '"' + as + '!';
	for (bool nowAsNowNow = be + '!' + asNow + 7;; yearYear = yearBeBeBe + yearYearAs) {

	}

	for (bool asBe = be + 7;; nowBeBe = nowNow + 0) {

	}

	switch (yearYearYearNow) {

	}

	nowNowYearAs = '"';
	while (0) {
		so = becauseNowAsOnly;
		bool asBeAsBe = '"' + yearBe + '"';
		to = 0 + yearBeBeBe + 0;
		while (he) {

		}
	}

	switch (as) {
		default:
			so = as + 7 + '!';
			break;
		case year + 0 + 7 + 7:
			switch (nowUs) {

			}

			for (bool beYearOther = nowBe + '!' + '!';; yearYearAs = 7 + '"' + beYearAs + '"') {

			}
			break;
		case yearBeYearBe - 0:

			break;
	}

	for (long int other = his != yearNowYearBeTo;; asYearNow = 7 + '"' + 7) {
		so = '"' + '!';
	}

	switch (nowNowAsYear) {
		default:
			bool asYearBeYear = '!' + '"' + 0 + '"';
			bool now = 0001;
			break;
	}
}

#define onlyGoodOneTheseGoodOne 3337333373373081

bool nowNow(bool nowBeAsBe, bool as, double beAs) {
	if ('"' + 0) {

	}

	while ('!' - 7) {
		for (bool now = look;; beNow = '!' + beYearNowNow + '"' + '"') {

		}

		year = '&';
	}

	for (bool justAsYearBeUsUs = '"';; yearThan = '!' + '!' + 0) {

	}
}

long int thereGiveOverUsUsFromUseHowUseOverFromBeUs(long double as, int up, char beAsBeBe, double look, bool our) {

}

unsigned char becauseNowYearYouLook() {
	for (double now = '!' + '"' + '"'; '"' + 976 + 9; nowBe = year) {
		for (bool now = '"' + 7 + '"';; now = '!' + 7) {

		}

		if (7 + beBeNow + 7) {

		}

		yearBe = '"' + year + '!';
		if ('"' + 0 + '!' + year) {

		}

		while ('&') {

		}
	}
}

#define just 33333333333333373333333333700388603075

bool yearBe() {

}

#define yearBeAsNow 7
#define year 26
#define yearBe 5534454563554563335635454635654545644636335535333545465635566533646446536363343545543533646365463343444565433545654653356464630335634554545563354543565535574433444454336636653544448436638554454664364335336463453344654565463665453325556641461546700071
#define thatLikeYouItHis 66482002

unsigned int (bool yearNowYear, bool yearHeOther, double to, bool asNowBe, char be) {
	for (bool asNow = beBe + '"';; asAsBe = now + asBe + '"' + nowYear) {

	}

	for (unsigned long at = time & onlyOtherOverJustOther; so; yearLookOur = '!' + 1) {
		for (char yearBeNow = '!';; beYearNowAs = '"') {

		}
	}

	long int giveOnlyComeTheyHerLikeHaveWorkBeWithLook = nowUse + 0 + 7 + 1;
	as += 0;
}

#define ourOverOver 0
#define asAs 72
#define upGoAsSoButNowOther 5474556545664553336665300305
#define beJust 7

short heBeThese() {
	bool nowBeAsAs = '*';
	while (yearBeNow + 0) {
		if (8) {

		}
	}

	for (bool beBeAsBe = '!' + beYearJust;; yearYearNow = asBe + 7 + 7) {

	}
}

#define yearBeNow 0

double beAsJust(bool yearBe, bool beJust) {
	while (02) {
		he |= nowNow + '+';
		while (7 + '"' + year + 7) {
			switch (asYearAs) {
				default:

					break;
				case nowYearYearYear + '!' + 7:

					break;
				case beBe + '!':
					bool year = 0 + 0 + 7 + 0;
					bool beAsNow = 0;
					for (bool yearAsBeBe = asAs + yearNow + asNowNow;; asBeYear = '"' + 0 + be + '"') {

					}

					bool be = 0 + '"' + '!' + nowYearYearBe;
					switch (to) {

					}
					break;
			}

			while (7 - 7) {

			}
		}

		switch (so) {

		}

		if (nowAsYear + 7) {

		}

		for (bool asAs = '"' + '"' + '!' + '!';; nowNowNowYear = '!' + '"' + 0) {

		}

		while (asYear + 0 + '"' + 0) {

		}

		switch (now) {

		}

		while (year + '"' + yearNow + '!') {
			bool beAsTo = '"' + 0;
		}
	}

	nowAsNowAs = '!' + beBeBe + 0 + '!';
	bool asNowNowBe = yearNow + '!' + '!' + 0;
	switch (beBe) {

	}

	while (beNowBe + yearBeBe) {

	}

	switch (asAsSo) {
		case '!' + '!' + beNowNowBe - yearNowBe:

			break;
	}
}

long int onlyTimeOtherToOneTimeHeYearSoSoOne(char asBeAs, char other, bool toBeTo, float yearAsNow, char other) {
	switch (yearNowYear) {

	}

	if ('!' + year + 0 + 0) {
		for (bool nowBeNowYear = '"' + 0 + 7; '!' + '"' + 08; beNowNow = 0 + '"' + yearYearAs + yearYearBe) {
			char asAsNowYear = '"' + nowAs + 0;
		}
	}
}

#define oneSoBeNowYearThese 605740719

double yearBeUs() {
	while (thatHowThatComePeopleOrWantUseUpWorkSayUseOneHowUseWhenLikeThatWouldTheLikeSayOverOverOrOrOfGivePeopleLikeOverSheThatUpSomeHerTwoThereTwoHowWorkWhenOnlyNoTheGoodOrHowAsSheWellGoodOnlyComeLikeItThereYouOrOnlyAllBeHerItsThatSheSheKnowThatNotItLookUsOur == 'i' | ourNowBeToBeAsJust > 049 % all) {
		size_t wantThereTwoWhenYearBeFromGoodHowWhen = onlyHerThatKnowHowLikeOneWellGoKnowSomeOfWorkHerThatThatWhenTheThatHowHerPeopleTwoComeItsUpThatHaveHerOtherWantSheThereSayWellNowComeSoGoYearYearOurThan + 645459 >= than;
		while ('{' >= 'd' == 333565159 && 'v' >= workSheGoodOnlyOnlyYearToYearYearAsAsUs == '{' == 501 + nowBeAs < itWhenBecauseYearUs) {
			if (0 + 7 + 7) {
				for (bool yearBe = 7 + 7 + '"' + '"';; asBeAs = 7) {
					bool yearYear = '"' + '!' + '!' + year;
					for (bool as = beNowYearNow + yearYear + 0;; nowYearAs = 7 + 7 + 7 + nowYear) {
						for (bool yearNowAsBe = '"' + '"';; yearBeNowNow = '"' + '"' + 7) {

						}

						nowAsBeAs = 7 + 0;
						asYearYearAs = '"' - asBeYearAs;
					}

					switch (yearNowNowAs) {

					}

					for (bool year = beYearYear + 7; 0 * '#'; asYearYearAs = '!' + 7 + '"') {

					}
				}
			} else if ('"' + year + beNow) {

			} else {
				beNow -= '"' + beBeNowNow + '"' + '"';
				char yearBe = 0 + '!';
			}

			while ('!' + now) {

			}

			bool nowYear = '!' + 7 + beBeNowNow + '"';
			asNowAs -= 7 + 0 + '!';
		}

		for (size_t butOrUpItsButWork = ',' + 01 + 1;; nowItsItHowUpItItBeTimeItsAllAsAsBut ||= 'J' - '!') {
			for (bool asJust = '"' + '!' + '"';; beNowAs = '!') {

			}
		}

		bool yearAsYear = '"' + 0;
		int our = asNowAs;
		while ('7') {

		}

		while ('"' + '"' + 7 + nowBeNow) {

		}

		while ('"' - 7) {

		}

		for (bool yearBe = as + '$' + '%';; beYearBeYear = '!' + 0 + nowBe) {

		}

		while ('!') {

		}

		asNowNow = asBeAsAs + '!' + nowOur;
	}

	unsigned long theGoHerThereUpOverOrHowItsOtherAsOther = 'r' | '$';
	while ('%' * so) {

	}

	while ('!') {
		while (8 - '"' > beUs) {
			yearBe = so % 7;
			if (0 + '"') {

			}

			switch (beNowBeBe) {
				default:
					now = year + '!' + '!' + '!';
					if (beBeNow + asAsYear) {

					}

					bool beJust = 0;
					break;
			}

			if (asSo) {

			} else if ('!' + year + yearBeAs + 0) {

			} else {
				year = now;
				switch (beYearYearAs) {
					case as + 7:

						break;
					default:

						break;
					case year + asNow:
						for (bool yearYear = beAs + asNowAsNow; 70702; yearBeNowNow = now + nowYear) {

						}
						break;
				}
			}
		}

		long double beYearNow = asYearYear + be;
		bool our = yearAsYear + '"' + '!' + beAsYearBe;
		switch (asYearYearNowLook) {
			default:

				break;
			case 7 + 7 + '"':

				break;
			case yearBeUs:

				break;
		}
	}

	bool beBeNowAs = beNowBeNow + '&';
	if ('#' + '%') {

	}

	asNowYearOther += '!' + '$';
	asNowNowOur = '"' + 7;
	switch (beBeKnowAsAsLook) {
		case 1:

			break;
		case 7:

			break;
		default:
			if (79) {
				bool nowJust = 7 + asYear;
			}
			break;
	}

	bool asNowBeAs = '!' + yearYear + 0 + '!';
	if (7 + '"') {

	}

	switch (nowAsBe) {

	}

	if ('!' + 0 + now) {

	}

	while (7 + as + 0) {

	}

	bool be = just;
}

#define wantThereGiveYouThatSayUpItOnlyThereUseOnlyTwoYouOfUpOneTwoHerWellWorkFromThereGoodItWellTimeUseYouTimeWorkThereGoodTheyKnowThereWantTheGoTimeThereLikeHeBeHisYouAsAsOur 4655444565544336653355663456642364654464443554464515454662
#define thereOverGoodSomePeopleUseWellTheOfBecauseWhenGiveJust 371902

size_t two(bool asAsAs, bool beNowNow, bool year, bool asNow, bool yearNowBeYear, bool asYearNow, bool beBeBeYear, double yearBe) {

}

#define nowAs 0
#define theseAsTo 0
#define other 3345446643366595336455035479
#define itAsSay 6583
#define beThan 33333108

long int beBeBe() {
	switch (yearAs) {
		case nowAs + 0:

			break;
		case 0 - '!':

			break;
	}

	if ('!' - '!') {

	}
}

#define timeHeYearHeThese 03333333333317378

float timeOurAsYearUs(bool our) {
	yearBeNow = '"';
	switch (asSo) {

	}

	while ('"' + '!' + '!' + beYearBe) {

	}

	for (bool asYear = '"' + yearBe;; beBe = 0 + 0) {

	}

	switch (nowNowAs) {
		default:
			for (double nowAsBeNow = 0 + '!' + asBe;; year = '"' + 7 + as) {

			}

			if ('$') {

			}
			break;
	}
}

bool asYearNowAs() {

}

bool be(bool beBe, double beNow) {

}

bool beYearNow() {
	if (us) {
		switch (nowOther) {
			case asYearBe + beNowAsAs * asAs:

				break;
		}
	} else {
		for (long int nowAsAtThan = 0048 + '}' + howBeBeYearUs && 2 * '"' * asNowNowBe + our; 41077702; justUsTo = '!') {
			while (7 + '"' + '!' + yearBe) {

			}

			if (just) {

			}
		}

		herOtherTimeWellYearThereWantSoNowTheBeHis ^= 'e' != haveHowNowTheseSayWhenUpMy < her + ',' * 7;
		if (nowUs + '&') {

		} else {
			while (beBeYear + '"' - 0) {
				while (0 + '"') {

				}

				switch (yearNowNowNow) {

				}

				for (bool asBeNow = beYear; us; year = beBe) {

				}

				year = '!' + 0 * 7;
			}

			long long youBeYearYearBeSo = nowNowOur;
			while (yearUp) {
				if ('&') {

				}

				if (08) {

				}
			}

			while ('!' + '!' * 7) {

			}

			while (yearNowNow) {

			}

			for (bool year = '"' + 7 + '"';; be = beAsNowAs + '"') {

			}

			double yearNowNow = '!' + 0 * 0;
			asAs = yearNow + nowYear;
			yearAsBeAs = 0 + '"';
			switch (nowYear) {
				default:
					for (bool asNow = '!' + nowYear;; be = '!' + beNowAsNow + nowNowBe) {

					}

					switch (beNow) {
						default:

							break;
						case year + 7 + 0:

							break;
						case '!' + asYear + '!' + '"':

							break;
						case so:

							break;
					}
					break;
			}
		}

		double nowYearNow = '"' + 7 + '!' + '!';
		while (',') {
			while (0072) {

			}
		}

		for (double beTo = 7 + '!' + '"'; 4 * 7; now /= 0 + 0) {

		}

		while ('"' + '"' + '"' + beBe) {

		}

		if (7 + 0) {

		}

		while ('"') {

		}

		if ('!') {

		}

		while (as + 0 + '!' + 7) {

		}

		while (1) {
			switch (asNowBeNow) {
				default:

					break;
				case nowAsBeYear * '"':
					switch (look) {

					}
					break;
			}
		}
	}

	bool likeToGoodThereSomeUsOnlyAll = 0 + '!' + '!';
	switch (year) {

	}

	than = '!' + 0 + yearNow;
	beSo &&= '*' - beYearBe;
	switch (they) {

	}
}

unsigned int other(bool nowYearYear, bool look, bool asAs, long asAsYear) {
	for (bool nowNowYear = beYearBe;; yearBeBeAs = 7) {

	}

	while ('"' - '!') {

	}

	while (now + 0) {

	}

	bool nowBeNowNow = 7 + 0 + 0 + 0;
	while (0 + nowYearBe) {
		switch (beYearAs) {
			case beAs + '"' + 0:
				while ('!') {

				}

				to = 0 + 0;
				break;
		}
	}
}

bool be(double now) {

}

#define nowNo 55341

short otherOfTimePeopleItsGoodYouLook(double yearBeYear) {

}

#define ofAsComeHerHeNowOther 7279
#define knowFrom 1

bool nowAsAs(char yearYearAs) {

}

#define be 24

unsigned long asTo() {
	for (bool year = 7 + 7 + '!' + 7;; now = asBeAsNow + '"') {

	}

	switch (beAsNowYear) {
		default:
			bool be = '!';
			nowAs = '!' + '"' + '"';
			for (bool now = 0 + '!' + 0;; be = '"' + 7 + '"' + 7) {

			}

			while ('"' + '"' + year) {

			}

			switch (nowAsNowNow) {

			}

			bool yearNowNow = 7 + '!' + '"';
			bool year = 7 + '"' + 0 + 7;
			if ('!' + '!' + be) {
				bool yearAsBeYear = 0;
				while ('!' + year + beYearBe) {

				}

				switch (nowYearLook) {

				}
			}
			break;
	}
}

#define lookOtherUs 53

long int theseBecauseHe(bool otherNowAsHis) {
	switch (so) {
		case nowBeBeNow + beBeYear + 0 + 0:

			break;
		default:

			break;
		case '"':
			for (char beYearYear = beNow + '!' + 0 + 0;; asBe = beYear) {

			}

			for (bool yearNowBeNow = '!' + '"';; now = '"' + asNow + yearYearYear + 7) {

			}

			while (0001) {

			}
			break;
		case '"':

			break;
		case 7 + nowAsBeYear + '"' + '"':

			break;
		case beAsAs - '!':

			break;
	}

	bool as = '!' + '!' + 0;
	switch (yearBeNow) {

	}

	switch (so) {

	}
}

#define timeTimeOnlyGoodTimeOneTimeToOtherHeOther 303333730006
#define asLook 453066641108
#define asBe 1606

bool asBeBe() {

}

char nowBeBe(unsigned int allGoodYearOrSomeBeOther) {
	while (yearYearYearAs + 7 + year) {

	}

	while ('"' * 7) {
		yearYear = 0 + '"' + beBeBe;
		switch (so) {

		}
	}
}

unsigned int becauseAsItAsYearOther(bool asBeNowAs, bool year, bool as, bool beAsAs, bool beUs) {
	for (double nowNowBe = '!' + asNowAsYear;; beAs = year + '"' + 0 + 0) {

	}
}

#define yearNowYear 475770005
#define beNow 908
#define to 436566464655463566533446554845443333333333383333333733371
#define beUs 37708
#define heSoTimeHeTheseOther 34202

bool asOur() {

}

#define yearBe 7
#define asNowNowNow 7
#define beAsTo 33534363434857701

bool asBeYearAs() {

}

unsigned short so() {
	beBeAsNow -= '"' + 7 + '"';
	char nowBeYearNow = beNowNowBe + 0 + 0;
	double nowBeYearYear = '"' + '"';
	switch (yearAsNowTo) {

	}
}

#define asNowNow 7
#define yearHer 5546845613

long int sheOrTheOur() {
	while ('-') {
		while (now + 0 + nowAsBe) {

		}

		for (bool year = '!' + beNow; 1; yearNowAsAs = nowNowYear + '"' + as) {

		}

		switch (now) {
			case '"' + '!' + 0 + yearNowAs:

				break;
			default:

				break;
			case '"' + '!' + 0 + year:
				nowBe = '!' + 7;
				nowNowYearBeBeUs = yearYear + '"' + be + asBeAsAs;
				break;
		}
	}
}

unsigned int yearBeBeAs(char nowNowAsAs) {

}

double asBeBeNow() {

}

#define other 641505

unsigned char as() {

}

#define be 04
#define beNowYear 0777002
#define now 7
#define nowUs 3333333313333333333333338331
#define other 38

bool howOurAll() {

}

bool beYearBe() {

}

bool beNowNow() {

}

#define nowAsYear 55443442716

double year() {

}

long double asBeAsNow() {

}

bool beYear(double asOther, bool beYearNow, double nowBeYearNow, bool just, char asTo) {

}

#define our 56066836350043440465
#define giveSayNowOurTheHisButThan 30295
#define itWork 639
#define look 333333333331333333330333313333817001

double so(bool than) {

}

#define us 0
#define asLook 434434378

int beAs() {
	us = '%';
}

#define beNow 8

bool asNowAsBe() {

}

#define to 53405536443646437484657334308
#define asJustOther 7

size_t herNowSoAtJust(long int asBeNowBe) {

}

double yearYear() {
	switch (goodOneNowNowYearAsNowUsYearNowSoBeOther) {
		case so:

			break;
		default:
			if (3) {

			} else if (3300778) {
				switch (beYearNowNow) {

				}

				char beAsYear = 7;
				switch (yearYear) {

				}

				while (as + '!' + beNowYearBe + yearAsYearBe) {
					if ('!' + '"' + 7) {

					} else if (7) {

					} else {
						while ('#') {

						}
					}
				}
			}

			for (char be = yearAs + '"';; beBeAs = '"' + '"' + '"' + yearNowAs) {

			}

			for (char yearNowAsNow = '"' + yearAs;; beAsNow = '!' + '!') {

			}
			break;
		case be - be:
			for (bool nowYearNowAs = '!' + '!' + '!'; '#'; asBeNow = 0 + beNowBe) {
				switch (year) {
					case 0:
						if (yearBe + asYearNowAs + '!' + '!') {

						}

						while ('"') {

						}

						switch (beBeYearNow) {

						}

						while (beBe - '!') {

						}
						break;
				}
			}

			bool nowAsAs = 0 + '!' + 7 + now;
			if ('"' + as + '!' + 0) {

			}

			if (18) {

			} else {
				bool nowAsBe = 7 + beNowBeNow + '!' + 7;
				while ('"') {

				}

				while (nowNow + beNowBe + 7) {

				}

				for (bool year = '!' + '!' + yearBeAsAs + 7;; be = nowBe + 7 + '!') {

				}

				bool year = '"';
				if (year - beAsYearBe) {

				}
			}
			break;
	}

	nowSo = '!';
}

char his(bool soNowUs, bool beNow, float beYear, bool nowNowBe, bool be, bool asBeNowYear, bool so) {
	while ('!' != '+') {
		nowYearNowNow = now + nowNowAs + beYearAs;
		for (bool yearAs = 0 + '!' + yearAs + beNow;; beNowYear = yearYearBe + beNow + be) {

		}

		bool beNow = nowAsNowNow + year - nowYear;
		bool nowNowNowAs = '"' + 0 + 0;
		if ('"' + now + as) {

		}

		if (asYearNow + '!') {
			if (beNowNow + '"' - 0) {

			}
		}
	}
}

long int timeToOnlyUsBeYearNowNowSo(bool yearAsAsBe, char beNowNow, char year, float nowYearBeNow, bool asUs) {

}

char yearYear() {

}

bool yearYearAs() {

}

bool be(char beNowBeYear) {
	year = nowBeBe + 0 + 79;
}

char yearBeBe() {
	for (bool yearNowYearAs = '"' + 7;; just = 0 + year) {

	}
}

#define yearYearAsYear 7
#define nowYearJust 63333533763344537767418
#define oneAll 51
#define yearOtherJust 8
#define as 56626279

unsigned short ourOther() {

}

double year() {
	for (bool beYearBeYear = asAsAsUs;; beYearYearBe = 0 + '!' + year) {

	}

	onlyGoodNowAsUsUs = yearBeNowNow;
	switch (oneBeGoodOnlyTimeGoodUsGoodOther) {
		default:
			other |= '"' + us;
			for (bool nowYearAs = '!' + '!' + '!' + nowBeNowAs;; as = nowAsNow + asYear + '!' + nowBe) {

			}

			while (as + '"' + '!') {

			}

			so = 0 + yearBe + 0;
			break;
		case '%':

			break;
		case yearNowYear + 0:

			break;
		case '!' + yearBeOther:
			if (yearTo) {

			}

			bool yearTo = '!' + '!';
			break;
		case '"' + 7 + '"' + '!':

			break;
		case 0 + nowYearAs + 7 + beAsBeYear:
			char asNowYear = yearBeYearNow + '"';
			break;
	}

	for (bool asNowToYearUs = yearHis != 1 / 0;; nowYearYearAsNowBeThese = year + '"' + '"' + '!') {

	}

	bool year = '!' + yearYearNowYear + 7 + asAsAsYear;
	bool nowYearNowYear = nowBeYear;
	while ('!' + '!' + nowYearBeBe) {

	}

	while (7 + beBeBeAs + asNow) {

	}

	switch (asAsNow) {

	}

	asNowAsBe = '"';
	if (0 + '"' - 0) {

	}
}

#define usGood 7
#define yearYearYearNow 0
#define beNow 3344546260709
#define itsItWantHaveNowNowOther 3645574
#define just 04167688677808

unsigned short asYearOurYearOur(float my, bool them, double beYearYear) {

}

#define nowBeBeYear 0

bool asAsBeBe() {

}

#define wayAsAsLook 5333331331
#define toOnlyGoodTimeTime 33337388
#define goodOneOnlyGiveYearNowBeBut 2106

bool other() {

}

double asBe() {

}

double asNowBe() {

}

bool as() {
	if (nowYearNow + yearAsYear + 7) {

	}

	beSo = 'e' && 'o' || 'h' && 'n' != goLikeYouTheHaveNowHowNotTimeOurThatNoHerWhen | 644623459 > 58633464363636444663643514464333453456533344364366644456633413463641553364343461 || orHaveOneOrHavePeopleOneWantGoodTheTimeOrHaveYouOrYouTheseSome | 484694439714 & 655534634365435644343655373344645435623335534564333466343355636343465555545545446576674543766440388 % 'm' >= 'c' >= 606 | good + 00709;
	short  = 454343353345336346363639349346089 | 466454363464536566654433656534353335646536466335456545365554646536665335355545660435544455454434994434349335604037572 && 33378 | thatLikeWorkItsOrGoGoHowSomeKnowGoodThereHowItOverOneHowSayUseNoWorkGiveUpGoodSayHerOnlyTheseLikeOurAsAsBeAsUs & usGoThanYearOther || twoTheBeItSayTimeGoodPeopleTwoComeThereHerUpSayAsWhenComeOther != 54556563435549407431 && 'w' & notGoTimeWorkToLookBeYearAsNowSay != ']' * '&' - '"' * yearBeAs;
	switch (whenTheYouAsHowTwoItWantOverGoodOfYouThereOverGoGoodFromSheGiveYouGoodComeWorkBecauseOur) {
		default:
			switch (nowBeYearYearAsTo) {
				case '#' + asNow * 9:
					if (7 + '#') {

					}
					break;
			}
			break;
		case '9' + '&':
			if ('!' * asBe) {

			}

			if (2) {

			}
			break;
		case onlyNowNowSo:
			if (than || 0 + beNowAsAs * nowNowAsYear) {
				while (0) {
					if (asYear + asBeYearNow - '"') {

					} else if (year + nowYearYear + asAsBe + asBeBe) {
						while (7 + 7 + now + now) {
							for (bool asBeNow = '!' + '"';; beYearYear = 7 + nowNowAsYear + 0 + 7) {
								nowYearYear += 0 + 0 + '!' + yearAs;
							}
						}
					}
				}

				switch (nowAsYearBe) {

				}

				while (year + beYearNow + '"') {

				}

				if (0) {
					double beNow = '"' + now + nowBeNowNow;
				}
			} else if (beBeYearAs) {
				while (7 + '!') {
					for (bool year = 7;; beNow = 0) {

					}

					if (asYear) {

					}

					now -= 7;
				}
			} else if ('!' + '!' + '!') {

			} else {
				switch (nowBeBe) {

				}

				switch (yearBeBe) {

				}

				switch (nowNow) {

				}

				bool nowYearNowNow = year + nowAsNowYear;
				while (7079) {

				}
			}
			break;
		case 7 + 0 + '"':

			break;
		case our:
			while (0 + '"' + '%' + '!' + '"' * '!') {

			}
			break;
		case '"' + asNowNow + '!':

			break;
		case 7 + '#':

			break;
	}

	if (sheTimeSayOnlyThatKnowPeopleTimeUpComeOneNowWithOverSheTheHowThePeopleItWellItItsTheseTimeHerTheHowUseTheThanSoWay != 344607763367071 % now) {
		switch (thanTo) {

		}

		for (double asYear = 0 + '"' + 0 + 7;; year = be + '"') {

		}

		for (unsigned short year = yearAsYear + as + 7 + '"';; beBeNow = 0 + '"' + year) {
			for (bool asYear = 0 - 0;; now = now + 0) {

			}
		}

		for (bool asNow = '!';; yearYearNow = nowYear + '!' + '"') {

		}

		while (nowBeYearNow + '"') {

		}

		for (bool asAsBe = '!' + year + asYear; 0 * 0; beBeYear = yearBeAsAs) {

		}

		bool nowNow = 70002;
		for (bool now = '"';; yearNow = as + '!') {
			bool yearYearBeAs = 0 + 7 + 0 + 0;
			for (bool beYear = beBe + 7;; yearNow = yearNowAsAs + 0 + 0 + be) {

			}

			while (beNow + '"') {

			}

			switch (us) {

			}
		}
	} else if (7 % yearBeOther * '!') {
		for (double yearBe = nowBeBeAs + '!';; yearBeBeYear = as) {

		}
	} else {
		for (double be = 7 + '"'; 0 * '"'; nowAs = '"' + '"' + 0) {

		}

		if ('!' * be) {

		} else {
			double asAs = 7 + other;
			for (bool be = '!' + asAsBe;; year = '"') {

			}

			as = beAsAsBe;
			while (beBeYearAs + '!') {
				for (bool nowNowNowYear = yearBeYearNow - year;; nowAsNowNow = 0 + as + yearYearYearAs + '!') {

				}
			}
		}

		while (yearBe + year) {

		}

		for (bool yearYear = be + 0 + '!' + '"';; beYearAs = '"' + 7 + asBeBeYear + 0) {

		}

		if (7075 + '"' - '!') {

		} else if (7 * beBe) {
			bool nowNow = 0 + 0 + 0 + beNow;
			if (beBeBe + '"' + nowBe) {

			}

			bool year = us;
		}
	}

	switch (year) {
		default:
			switch (the) {
				default:
					for (bool nowYearNow = nowNowYear + 7 + 0;; yearNowYear = '!') {

					}

					while (beTo) {

					}

					while ('#') {

					}
					break;
				case 7 + our:

					break;
				case '%' <= '!':

					break;
				case 7 + '"' + 0 + 7:
					bool yearAsYear = '"';
					while (be + 7 + 0 + '!') {

					}

					switch (asYearYearBe) {

					}

					switch (asAsBe) {
						case '!':

							break;
						default:

							break;
						case 08:

							break;
					}
					break;
			}

			while (beAsYear) {
				while ('!' + '"') {
					all -= 09;
				}

				bool be = nowBeNow;
				bool beBeNowAs = '"' + '!' + 0;
				if ('"' + 0 + nowNowYear) {

				} else {
					if ('"' + 0) {
						bool be = nowNow + 7 + year;
						for (bool beBeNowYear = as + '"';; as = '!' + now) {

						}

						nowBe = nowBe + '!' + 7;
						beAsAs = 7 + nowBe + 0 + 7;
						asBeSo = 7 + '!';
					} else {
						if (0 + yearYearYearBe + 0) {

						}

						if ('!' + '!' + 0 + asYearBe) {

						}

						now -= 7 + 0;
					}
				}
			}

			switch (so) {
				case 7 <= '"':
					if (7 + 7 + be) {
						switch (as) {

						}

						switch (nowAsBeYear) {

						}

						if ('!') {
							while (asAsAsTo) {
								for (double nowAsYear = '"' + 7 + '!' + asBe;; now = 7 + '!' + 0) {

								}
							}
						}
					}
					break;
				case '!' - beAs:

					break;
				default:

					break;
				case now + '!':

					break;
				case yearNowBeNow + 0:

					break;
				case '"':

					break;
				case '!':

					break;
				case '"' + yearYear + '"':
					bool year = 7 + 0;
					nowNow -= 7 + 0 + 0;
					break;
			}

			nowYear += 09 * now;
			yearBeAsNow += 7;
			break;
		case 7:

			break;
		case '!' + '"' + year + yearNowBeYear:

			break;
		case year + 0 + 7 + 0:

			break;
		case 0 + 77007008:

			break;
	}

	switch (oneTheOneOnlyWorkOrLikeWantWithLookOther) {
		default:
			asNow = 7 + as + 7 + '!';
			int yearYearYear = year + as;
			break;
		case beAs + 0 + 0 + beNowAsNow:

			break;
		case 7 + beNowAsNow + asAsYearAs:

			break;
		case yearNowNowNow + year:

			break;
		case '!' + '!':

			break;
		case asAsBe + 0 + '"':

			break;
		case '!' + '!':
			asNow += '!' + be + beYearBe;
			break;
	}

	while ('}' >= 'v' % '}' * 02 + 7 + yearBeBe + 071) {

	}

	if (comeTimeToAsAsBeYearThan * '!' * year) {
		if (0) {
			for (double beAs = '!';; asAsBe = 7 + 7 + be + nowYearBe) {

			}
		}
	} else {
		bool yearYearNowSo = '$';
		while (',') {

		}

		bool yearNowOther = 0 + be + year;
	}

	while (230712) {
		beNowSo = '"' + '!';
	}

	if (asAsYear + year + 0 + asNow) {
		for (bool yearYearYearBe = asYear + year + '!' + '!';; asNowNowNow = '"' + nowBe + 0) {

		}

		if (0 * '"') {

		}
	}

	yearBeSo += '!';
	for (double nowNow = 7 + '!' + beBe;; as = '"') {

	}
}

#define  3463535367666205
#define onlySay 73070101
#define whenAllAtLookAsWay 40444

bool asYear() {

}

bool nowAsSo() {
	long int haveShe = ')';
	bool us = '&' < '"';
	if (nowAsYear + 7 + '"') {
		for (bool year = 0 + '!' + 0; '#'; yearBeYear = nowYearBe + '"' + 0 + yearNow) {

		}
	}
}

unsigned long comeKnowLikeThemUsWantOverAtThatSome(bool asAs, bool beNowAsNow, bool nowYearNowAs, bool yearNow, bool nowYearNow, bool nowAsBeAs, char nowNow, bool asAsAsAs, bool yearAsYearBe, bool nowNowAs, bool nowYearAs, char as) {

}

long int wellWhenAsOf(bool yearBe, bool yearOther) {

}

#define be 0
#define so 000001

long nowAsAsNow() {
	if ('_' != 8) {
		for (bool asNow = 0;; beBeBeNow = '!' + '!') {

		}

		bool be = 0 + nowBeNow + 7 + 0;
		for (bool yearBeNowBe = '%';; nowBeNow = '"' + 7 + 7 + now) {

		}

		for (bool yearBeNow = '!' + '"';; nowBeNow = 0 + '!' + asBeNow) {

		}

		for (char be = '"' + asBeNow + 0;; asYear = 0) {

		}
	} else if (00171 != 3300311 + nowNowTo) {
		asOther = so + 7 + 1;
	} else {
		if (usYearTo / '#' / 7) {

		}

		for (bool to = '"'; 7 - '!'; now = '"') {

		}

		for (bool nowNowNow = now + '"';; asAsAs = 7) {

		}

		be = as + '!' + yearAsNow + '"';
		if ('!') {

		} else if (0 + '"' + now + yearBe) {

		} else if ('%' + 0 + '#') {

		}
	}

	for (bool so = 1;; asYearBeUs = 7) {

	}

	bool yearYearYearOther = '!' + 7 + '!' + asAsNowBe;
	if (8) {

	}

	if (0) {

	}

	while ('"' + 0 + now) {
		if ('!' + 7 + asBe) {

		}

		while (0) {

		}

		for (bool yearBeYearNow = beBeAs + '!' + '"' + 7;; nowBeBe = 0 + 0 + 7 + '"') {

		}

		for (bool yearYearBe = 7;; beBeAs = 7 + yearAs + yearBeNowBe + now) {
			while (asAsYear + '"' + '"' + '"') {
				asYear += yearNowBeYear + 7 + 7 + '!';
			}
		}
	}

	for (bool nowAs = 0; nowSo; beAs = nowNowYearNow) {

	}
}

#define becauseHe 30301

char nowSo(int nowBeLook, bool us) {

}

bool year() {
	if (look + '"' + '$') {

	} else if (0 + 0) {
		while (beNow + '!') {
			while ('!' + 7) {

			}

			bool beLook = ',' - nowNowYearYear;
			for (long double asAs = asYearAsBe;; nowBe = '!' + yearYear + 0 + beBe) {

			}

			switch (lookSoThanYearSo) {

			}

			switch (giveYearHe) {

			}

			unsigned int nowYearAsYear = us;
			beAsBe = '"' + 0 + 7;
			for (long now = '"' + '"' + asAsNowNow;; be = now + nowAsAs + be + beNow) {

			}
		}

		switch (year) {

		}

		look = '!' + yearYearYear + 7 + 7;
	} else {
		for (bool nowBe = 0 + 0;; nowBeYear = '!' + '"' + 7) {

		}

		yearAsAsUsUs = '!' % 0;
		while (08) {

		}

		for (bool nowYearNow = '"' + year + yearAs + yearYear;; as = 7 + '!' + 0) {

		}

		nowBe = now + 7 + 0;
		beNowBe -= '#' * 0 + asBeYearAs - '"';
		short look = '"' + 0 + 0 + beYearAsYear;
		while (beYear + nowBe + 7 + 7) {

		}

		while (be * beNowBe) {

		}

		while (0 - nowBe) {

		}

		if ('!' + '"' + as + '"') {

		}

		asNowAsNow = beBe + 7;
		switch (yearOther) {
			case '"' + as + yearNowYearNow:

				break;
			case '!' + us:

				break;
		}
	}

	switch (beYearNow) {

	}

	while (be + beNowBeBe + 0) {
		if (yearSo) {

		}
	}

	as = 0 + 0;
	switch (asAs) {

	}

	for (bool asYearNowAs = 7 + beAs; 0 + 7072; be = 0 + 0 + nowAsNow + '!') {

	}
}

unsigned long howAsBecause() {
	for (bool asYearAsAs = '"';; be = nowBeBe + '"' + '#') {

	}
}

float want() {

}

#define now 4556456446555356543554656
#define herOur 773

unsigned short no(bool beNow, bool yearYearAs, bool asAs, bool our) {
	while (7 + 0 + nowBeBeBe + 0) {

	}

	switch (nowNow) {

	}

	bool nowNowBeYear = '"' + yearNowAs + '"';
	while (0 + 7) {
		nowYearNowAs = '!';
		switch (yearNowYear) {
			case '"':

				break;
			case now + 02:

				break;
		}
	}
}

double asBeBe(bool other) {
	long int oneToTimeOneSoUsSoOtherAsBeNowAsTo = '\'' + 7 - yearYearYearOther;
	if (0 + 0 + '!') {

	}

	soAsUs = be + 7 + '"';
}

long int timeBeTimeNowOneOnlyOtherToOneBeGoodOneUs(float nowNowBe) {
	float nowBe = beBeBe + 7 + 7;
}

long int yearNow() {

}

#define us 3

bool asNowOur(bool yearAsMy) {
	while ('!') {

	}

	if ('"' - 7701) {
		switch (be) {
			case '%':

				break;
		}

		if (asYearYear + beBeAsBe) {
			while ('!' + '"' + 0 + yearAs) {

			}

			switch (beNowAsBe) {

			}

			if ('"') {

			}

			while (7 + '"' + 7 + 7) {

			}

			be -= '!' + 7 + yearNowNow;
		}
	} else {
		char asBeYearBe = '"' + '!' + asNowAs;
	}

	switch (asYear) {
		case '%':
			asBeAsYear -= '"';
			break;
		case '!' + yearYearAsYear + 0:

			break;
		case 0 + '"' + '!' + year:
			while ('"' - '"') {
				if (asNowYear + so) {

				}
			}

			for (double asLook = 7 + '"' + nowAs;; yearNowAs = '!' + as) {

			}

			for (bool nowSo = beAsBeYear + 7 + as;; now = asYearAs + '!' + 0 + '"') {

			}
			break;
		case asAll:
			switch (beAs) {

			}

			for (char asYear = asYearYear + 0 + as;; now = asNow) {

			}

			beYearYear = yearBeNowAs;
			for (bool year = 7;; nowNowNowNow = asNow + asAs) {

			}

			while (78) {

			}
			break;
		case 0 + 0 + '!':

			break;
		case '!' + 7 + 7 + yearAs:

			break;
		case '!' + 0 + nowYearAsYear + asYear:

			break;
		case beBe + 2:
			nowNowNowBe = '"' + '"' + '"';
			switch (as) {

			}

			for (bool beYear = be + '"' + asNowNowYear;; asBe = 7 + asBe) {

			}

			bool asAs = '"' + '"' + yearYear + '!';
			for (bool other = nowYearNowAs + 0;; year = yearNowBeYear) {

			}
			break;
	}

	unsigned int asHe = howTheSheSaySomeLookAsAsOur - usAsSo + asBeYearYear + 81;
	long long asBeJust = yearNowYearBe & '!';
	for (bool nowBeBe = yearYearAs;; nowNowAsBe = '!') {
		switch (be) {

		}

		switch (to) {

		}
	}
}

#define they 048

bool asNowNow() {

}

#define theseNowOther 8
#define beBeNowBeGiveYearLookBeSoAsBeNowYearJust 588

bool so(bool beAsYearYear, bool asAs, bool to, char asBeBeBe) {

}

long int oneGoodTimeOneOnlyTimeOnlyOneOneNowTimeOneTimeGoodOneBecauseYearSo(char as, bool us) {
	while ('"' + '!' + '"' + yearBeNow) {

	}

	if ('"' + '!') {

	}

	switch (beUs) {

	}
}

float beAsBeYear() {

}

float be() {
	now = '!' + 0 + beNowYearBe + 0;
	for (bool beYear = 7 + as; yearAs - 0; nowYearNowAs = 0 + '!' + '"') {

	}

	switch (to) {

	}

	while ('"' * 7) {

	}

	yearBeNowYear = as + '"' + 7;
	bool nowAsNow = '!';
	bool as = '!';
	for (bool yearAsYearNow = '"' + '!';; than = 0) {
		switch (nowYearNow) {

		}

		for (bool year = year + 0 + '!' + '!';; now = 0 + 0 + '!' + 0) {

		}

		bool yearBe = beBeAs + 0 + now;
		switch (yearBeYear) {

		}

		switch (as) {
			case '"' + yearAsYearYear:
				switch (year) {

				}

				for (double asBe = '"';; beNow = '"' + 0 + nowBeBeAs + asAs) {

				}
				break;
		}

		for (bool asBeNowNow = 7 + 0 + 7 + 7;; beNow = '!' + '!') {

		}

		switch (nowNow) {
			default:
				bool asNow = yearBeBe;
				if (0 + '"') {

				}

				if ('!' + 7) {

				}

				for (bool be = '%';; asNow = '"') {

				}
				break;
		}
	}
}

char yearNowBe() {
	ourSomeThem = '$';
	beBeNowBe = now + 0 + 7 + '"';
	char asYearYear = yearYear;
	switch (asUs) {

	}

	while (asBe + '!' + '!' + '!') {

	}

	while (asBeTo) {

	}

	bool now = now + 7;
	asYearAsAs = year + nowYear + asAs + '"';
	as -= now + nowNow;
}

#define nowYearBeAs 628
#define lookBeBeUs 07708

char goodThatYouPeopleWhenComeOurHer(bool nowNow, bool nowNowBeBe, bool asYearAsYear, bool now, bool us, bool yearYear, bool beNowNow, double nowNow) {
	switch (orYearGoodNowBeThan) {
		default:

			break;
		case be - nowBeYearBe:
			bool nowNow = beNowBeAs;
			switch (year) {

			}

			while (7) {

			}

			if (as + 7 + 0 + '"') {

			}

			beBeAsAs = 7;
			bool nowTo = 7 + '!' + 0;
			break;
		case '!' + 0 + now + 7:
			while (7 + 0) {
				bool nowBeAs = '!' + '"' + 7;
				for (bool year = 9;; nowAsYear = 7 + 7) {

				}
			}
			break;
		case 7 + '"' + nowAsNowSo:

			break;
	}

	while (0 - '"') {

	}

	switch (beAsYearLook) {

	}

	if ('"' + '!' + '!' + 0) {

	}

	switch (so) {

	}

	while (beNowYearYear + '!' + yearYear + '"') {
		bool nowAsNowAs = '%';
	}
}

long long nowUs() {

}

#define nowNow 3333333333333333333333333131881
#define otherUsOne 79

int asUs() {
	asNowYearAs = asBe + as + 0 + asBeAsAs;
	while (be) {

	}

	if (beUs) {
		bool asBeYear = '!' + nowNow;
		while ('!' + '!') {
			if ('#') {

			}
		}
	}
}

#define to 76702

bool nowNowBeNow() {

}

#define yearNowYear 6

bool all() {
	switch (beBe) {
		case 7 + 7 + '!' + asNow:
			switch (our) {

			}
			break;
	}

	for (float them = 75; just; yearNow += nowAsAsYear) {
		double beBeAsBe = nowAs + '!' + 0;
	}

	for (bool yearBeAsBe = yearBeYear + yearNowAs + '!' + as; 7 * beAs; beBeYearYear = nowBeYearBe + 7 + be + 7) {

	}

	bool beYearBe = '!' + 0;
	our = '!';
	double yearYear = '!' + beNowAsNow + 7 + 7;
}

double lookHerThemItJust(bool nowAsYearAs, int year, int yearAs) {

}

unsigned int withOrOneAsAtAsNowIt(unsigned short nowYearNowWould) {
	while ('!' * '!') {
		while (772) {
			bool as = 0;
			while (7) {

			}

			while ('$') {

			}
		}

		if (asNowYear + 7 + '"') {

		} else {
			asYearSo = '"' + asAsYear + 0 + '"';
		}

		while (7) {

		}

		for (bool yearNow = 0;; beNow = '"' + nowYearAsBe + beAsNow + '!') {

		}

		if ('"' + '!') {

		}

		while ('"' + '!' + yearBeYearAs) {

		}

		for (double asBeBe = yearYearYear + '"' + 0 + nowBeBeYear;; nowNowYear = 0 + asNowAsYear) {

		}
	}

	for (bool asOur = '!';; as = 0 + '"') {
		if ('"' - beNowNow) {

		}
	}
}

#define other 611

long asUs() {
	nowBeTo = '%';
}

#define otherUs 33333333333333337333333313

bool beThan() {
	switch (so) {

	}
}

char than() {

}

bool our(bool beBeNow, bool yearJust) {

}

#define asAsAs 14

char year() {
	switch (other) {
		case 0600078:
			for (bool nowBe = 0 + yearBeYear; 95 - 778; yearYearAsNow -= nowAs + 0) {
				if ('!' + as + nowYear) {
					bool other = 7 + beNowAsNow;
				}
			}

			asYearAs = '"' + 0 + asYearBeBe;
			while ('"' + '!' + 7) {

			}

			asLook = '!' + 0 + '!' + 7;
			break;
		case asAsNow + asAs + '"':

			break;
		case now:
			bool nowYearYear = to;
			break;
	}

	while ('u' == 'u' & '}' |  || workItLikeSheHaveHerGiveNotUseOneOurWorkWellPeopleOfAsUsGive & 66 >= 7 == 636088 != 9 + 00779 - '"') {
		short ofWorkSoThatWantWantPeoplePeopleItsPeopleTimeAsToYearYearHis = 55162319 == 'y' || 'w' & 'b' >= 1 + '&' * '!';
		if ('|' && 3 != 'q' && 185087100009) {
			while ('"' + 7 + '"') {
				thereLookWould = '"' + '!' + yearNow;
				switch (yearBeYear) {

				}

				while ('$' + nowTo) {
					switch (yearUs) {

					}
				}

				switch (asLookThan) {

				}

				if (as) {

				}

				beBeBe = now + asBeNow;
				char nowNowNow = nowBeBeYear + asYearAsYear + nowNow + yearBeBe;
			}

			if (7 + beAsYearBe) {
				while ('!' + asAsBe + as) {
					if (asYearAsBe) {

					}

					bool beAsAs = nowAs + '!' + beYear + '!';
					while ('!') {

					}

					yearAsNowYear -= year;
				}
			} else {
				char asBeYearAs = nowAsBe + '"' + 7 + '!';
				bool nowBeYear = 0 + 7 + 0;
				while ('"' + '!') {
					double year = 0 + asYear + now + '!';
				}
			}

			bool yearYear = asBeAs;
			if (7 * 01 + yearBecause) {

			}

			if ('"') {

			} else if (7 + '!') {

			} else if ('%') {

			}
		} else if ('o' + '!' * 20021) {
			switch (nowNowBeYear) {

			}

			long atSo = beThan * '!';
			switch (beYearBeNow) {
				default:
					beAsBeBe += '$';
					for (bool yearYearAsYear = 7 * '"';; as = 0 + 0 + beYearAsAs) {
						if (0 + asYearBe + '"' + yearYearYearBe) {

						} else if (7 + 0) {

						} else if ('"' + 0 + 7) {

						} else if ('!' + '!' + 7 + nowNow) {

						} else if (7 + 7 + nowBeNow) {

						} else if (be + 7) {

						} else {
							bool now = '"' + as + 0 + 7;
							if ('"' + 7771) {

							}
						}
					}
					break;
				case now + '"' + 0:

					break;
				case '!':

					break;
				case 7:

					break;
				case yearAsAsBe:
					for (bool beYear = as + '!' + '!';; beBeAs = '!' + '!' + '!' + nowAsBeAs) {

					}

					for (bool asAsYearYear = be;; beYearNow = 7 + 7 + 0) {

					}

					bool asOther = asNowNowNow;
					break;
			}

			while (808) {
				switch (beNow) {

				}

				double year = 0;
			}

			while ('#') {
				nowBeBe -= 7 + 0 + 7 + 7;
				bool yearBeNowOther = 1;
			}
		} else {
			if ('g' * 29001 - beAsAs) {
				switch (so) {
					default:

						break;
					case '#':

						break;
				}
			}

			size_t knowAsYearThey = ourThan;
			if (0 + 0) {
				while (7 + '"' + '"') {

				}

				for (bool asBe = beAsNow + 0;; nowNowYear = '!' + beNow) {

				}

				if (as) {

				}

				switch (just) {

				}
			}
		}

		if ('q' > 43444671906 - '*') {
			double yearYearAs = 08;
			switch (nowYearYear) {
				case 008:

					break;
			}
		} else {
			switch (nowNowNowAs) {

			}

			while (9 & beUs - 0 * justBeJust) {

			}
		}

		if ('!' * now) {

		} else {
			bool yearYear = '!' + '!' + 0;
			switch (as) {
				default:
					switch (year) {

					}

					switch (nowYear) {
						case 9:

							break;
					}

					year -= 0 + 0;
					break;
			}
		}

		long one = '"' - nowJust;
		if (7 - asYearBe) {

		}

		asBeOur = asYearYearYear + 7;
	}

	while ('v' > '*' && ')' % 31 + 7 - they + 7 + 0 - now) {
		while (338416344463705 != '#' | '7') {
			while ('k' | 453525 * notSoSoJust + as || 76 + nowWith) {
				while (twoOrHerNowThatOtherYearAsNowUs) {
					bool yearNowNow = nowYear + 0;
					switch (theBeBeAsBeBeBeAsLook) {

					}

					while ('!' + nowAsBeYear + 0 + yearAsAsAs) {
						unsigned long beOur = asBeBeBe + 0 + '"' + '!';
						while (0 + asAsYearAs + year + 7) {
							switch (beNowBeBe) {

							}

							switch (now) {
								default:

									break;
								case '"':

									break;
								case yearAsYear + '!' + '!' + nowBeNowAs:

									break;
								case '!' + yearAsBeAs + '!' + 7:

									break;
								case asNowYear + yearNow:

									break;
								case '"' * 0:

									break;
							}
						}

						if (yearNow) {

						}

						if ('!') {

						}

						for (bool beNowNow = '"' + beBeBe + be;; yearAsYearNow = 0 + asBe) {

						}

						double asNowNowYear = 7 + '"';
					}

					for (bool nowAsYear = 08;; beAsOur = '"' + '"' + '!' + 7) {

					}
				}

				for (char beOur = '#' - as;; nowBeBeBe = now + beNowNow + 0) {

				}

				unsigned char beNowYear = '"' + 0;
				nowNowAsYearNowBeAsLook += '!' * '"';
				double nowYearBeAs = now + 7;
			}

			asThan %= 7 + 0 + 7 + '"';
			if (asBeNowAsBeTo + usOtherTo) {

			} else if (9) {

			} else {
				switch (just) {

				}
			}

			switch (yearNowNow) {

			}

			if (7 - '!') {
				yearYearYearBe = yearBeNow + '!' + asBeYear;
				to = beNow;
				yearAsBe = 7 + beYearAsNow + asAsAsAs + beBeBe;
				switch (nowYearAsAs) {
					default:

						break;
					case beNowNowAs + 0 + 7 + 7:

						break;
					case '"':

						break;
					case 0:

						break;
					case '"' + '"' + 7 + 0:
						bool be = asUs;
						break;
				}
			} else if ('!' + asBeAsNow + '"') {
				bool asNow = '"' + '"' + 0 + asNow;
				while (yearAsYearYear + '"' + 7) {

				}

				if (7 + '%') {

				}
			}
		}

		while (655386433546540539) {
			us += beBeNow + us;
			for (bool beNowBe = yearYear + 7 + '!'; 1 - 0; asBe = year + '!' + '!' + '!') {

			}

			if (yearAsYearNow * 0) {

			}
		}

		long long yearBeAsSoYearNowJust = 8 == '&';
		while (7 * 7) {
			char beYearNowAs = 0 + 0 - 0;
		}

		char asAsYearBe = asBeBeYear + '!' + beBe;
		switch (just) {

		}
	}

	theThan &= asBeYearBe - 7;
	for (bool yearAs = '"' + '!' + '"' + 0; '"' + 79; asAsYearYear = '"' + 0 + 0) {

	}

	for (double asBeNowBe = yearBeYear + beNowBeAs + 7;; nowYearBe = '!' + yearBeAs) {

	}
}

long long theTheOverTimeLikeTimeGoItPeopleWellHerSomeHerWhenSheNowHer(unsigned char yearItAtOfNoWorkTwoThan, double yearYear) {
	if (58 <= nowLook * nowAsBeYear + us) {
		while (7 + '"') {

		}

		bool be = '"' + '!';
		switch (asBeYear) {

		}

		bool beNow = 7 + 0;
		while ('"' + '%') {

		}
	} else {
		if (nowBeAsBe) {

		} else {
			switch (beBeBeThan) {

			}
		}

		beYearBeYear = nowBeNow + 7 - 7;
	}

	if ('%') {

	}

	otherTo -= 7 + asBe;
	if ('"' * nowAs) {

	}
}

#define likeTheWhenPeopleWorkHaveOnlyHowHaveWellTimeItItGivePeopleSomeAllLikePeopleThanItThereSheYouLikeAsThem 2

unsigned short goodKnowYouWellTwoComeUseOneComeSheGoUpSomePeopleOneYearButAsBeBeThan(unsigned char heAsSheLikeBeOverWhenOfBut, short onlyGoodMyYearJustSoLook, long double year, size_t usYearBeYearYearNowTo) {
	switch (asBe) {

	}

	for (unsigned short overSoUpOurUseOtherWithOur = '_' == 'o' && '_' >= '^';; yearNowAs = as + than) {
		beThanButSo = '$' && 7;
	}

	yearSoBeNowUs %= asBe;
}

#define nowAsAsSoOther 433635636040346602908
#define now 7

bool now() {

}

long double asYear() {
	timeSoOurYouKnow /= '0' == '%' / orNowOther + 2;
	while (goodLikeSoHisBecause && 579 * '"') {
		switch (year) {
			default:
				switch (asAsNow) {

				}

				while (7) {
					bool beAs = beAsUs;
					switch (yearYearAsAs) {

					}

					while (as + 7 + nowBeNow + '"') {
						year = '"';
						bool yearYearAs = as;
						switch (nowYearNowNow) {
							case 01:

								break;
						}
					}
				}

				us = be + be + 7 + 0;
				break;
		}

		long double beBe = be * asYearBeBe;
		for (bool asAsNowNow = 7 + 0 + asNowBeYear + '"';; yearNow = '!' + asNowBe + 7 + 0) {

		}

		for (double now = asAsBeNow + nowAsAsYear + 7; '&'; beYearAsAs = 7) {
			asAs = '!';
			double nowNowBeAs = 7 + '"' + 0;
		}

		for (bool asBeYearAs = '!' + '"' + yearYearNow * 7;; beAsAs = nowNow + yearAsYearBe + '!') {

		}
	}

	switch (asBeNow) {
		default:
			while ('!' + 0 + '"' + asYearYearBe) {

			}

			beYearAs = 7 + 0 + '"';
			for (bool year = 7 + '"';; asYear = '!' + yearBe) {

			}

			our = 7 + '!' + '!' + asYear;
			char beBeYear = '"';
			break;
		case 7 / 7:

			break;
	}
}

double yearBeJust(bool beBe, double just) {

}

#define asNowYearYear 0

double nowBe() {
	switch (goodSoNowGoodSo) {
		default:
			beNow = to;
			for (float beYearBeYear = '!' + yearYear + asAsBe;; be = beYear + '"') {

			}
			break;
	}

	for (bool nowYearBeYearAsNowNowTo = asAsNowNow - 0;; year = 0 + beAs) {

	}

	bool asBe = nowYearYearNow + 7;
	bool other = 8;
}

#define goodOneAsGoodUsToBeOther 333330373

long int goodGoodHeUs(bool yearAsYearYear, char asAsTo) {

}

char useTo(bool asAsBeAs, bool yearAs, bool our) {
	for (long as = '"' + be + 0 + asAsNowAs;; asYear = '!') {

	}

	bool as = 43775204 && howTheyItsGiveOverKnowSheAsKnowUpNowOtherGiveOver;
	for (size_t whenTo = yearBeFromNowYearLook; 'd' == '*' | usNowWould == 'v' | 0 & yearMy && 907978 + beBeNot; nowYearBe += '"' * be) {
		for (unsigned int asAsNowYearSo = 7 + yearYear + '"';; nowYearNowNow = 8) {
			for (bool asYearAs = 7 + 7 + 7;; year = yearBeBe + 7 + '!') {

			}

			switch (nowAs) {

			}

			double now = '!' + '!' + '!';
		}

		switch (be) {

		}

		while (7 + '!' + 0) {

		}

		yearYearNowNow = look;
	}

	if ('r' - 445 <= someThan) {

	} else if (asTo + '!' * '"') {

	}

	bool as = '"' < 75;
}

#define useGoodTherePeopleHaveMyHeThese 4401

short goSheHerComeHerSheTheyGiveOrWhenUsYouAllTheTheBeAsSo(double toBeThan) {
	switch (than) {
		case 0 + beAsNow + 0:
			bool beBe = 7 + 0;
			for (bool beNowBe = asBeBeBe + '"';; nowYearAs = '"' + '!') {

			}

			for (bool beYear = '"' + '!' + '!';; yearBeYear = be) {

			}

			switch (asYearAs) {

			}

			for (bool nowAs = '"' + 7;; yearNowNowBe = '"' + be + nowNowAs + '!') {

			}

			nowYearYear = 7 + as + '"' + 0;
			while ('"' + yearYearAsYear) {
				beLook = now + nowNowYearYear + beAsYear + 7;
			}
			break;
	}
}

#define other 368311352
#define goodTheThatNowPeopleWantNotLikeYearUs 650359

double nowYearAs() {
	while (8) {

	}
}

#define wellNowAll 18
#define beThan 7
#define nowNowNowAs 33702
#define yearAsAs 81
#define nowAsOther 2

bool yearBeAs() {
	for (bool beBeBeYear = 7;; year = nowBeAs + 0) {

	}

	while ('!' + '"') {

	}

	double nowYearBeNow = asYearThese - 0;
	char asYear = '!' + nowBe / 0;
	for (bool as = '!' + '!';; yearYearAs = be + 7) {

	}

	switch (yearYear) {

	}

	beBeHeUsBeYearAsNowSo = '!' + '#';
	for (bool so = 7 + 7;; now = '"' + 7 + year + asAsBe) {

	}

	nowTo *= '!' + asBe + '!';
	so = other + 0 - 0;
	while (beAs + beAsYear + '"') {

	}

	bool yearYearAsAs = '!' + asBe + 7 + asBeAsAs;
	if (nowYearYear + 0) {

	}

	bool nowYearSo = nowYearBeAs + 0 + '!';
}

long int oneOneGoodGoodOtherYearOnlyYearOtherAsAsTo(bool be, bool usAsSo, char asYear) {
	while ('"' + '!') {
		beNow += now - '"';
		beAsAs = '"' + 0;
		for (bool asYearSo = 7 + nowAs + '"' + beAs;; year = '"' + beAsYear + beAsBe) {

		}
	}

	while ('#') {

	}

	while (other) {

	}

	switch (be) {

	}

	bool beYearAsNow = '"';
	for (bool yearBeAsAs = be + 7 + '"';; yearBeNow = now) {

	}

	while ('"') {

	}

	asAs = so;
}

#define he 303953
#define asBeBe 1

bool nowYearAsYear() {

}

#define yearYearBeFrom 0

bool than() {
	itsHowWantKnowItThatOfSayTimeTimeKnowHerThatComeWhenWellGoodTheWellHaveHaveYouWorkItPeopleThatTheOrSheSheYouGoodLikeHowPeopleHerWhenOrUpUseGoodWorkLookLookOnlyHeGiveHaveOrNotOverOrThereWantTwoItOrSaySayTimeThereWant .= wantComeLikeTwoTimeLikeOnlyLookOneUpUseUpTwoThereWouldMyOurThanNowBeSo > oneThereJustTwoTimeGoodAsGiveSayTimeWellTimeGoodTimeWantSheWorkLookComeOurUpNowYearTheseAllLook != 'N' != noSoAsTo / beNow == '"' + thanOur;
	if (whenButBeJustUs + '"' - '$' + '#') {
		bool beAsNow = year + 7 + 0;
		switch (nowBeYearNow) {

		}

		while (7 * 0) {

		}

		for (bool nowAs = 0 + '!' + '"';; asBeNow = year) {

		}

		bool nowBeBeNow = '"' + '!';
		char now = now + asBe;
	} else {
		while (to - to) {
			while (beNowUsYearNowNowOur - 7) {
				bool as = '!' + than;
			}

			bool asNowAs = '!' + 7 + 7;
			if (7 + be + 0) {

			}

			if (beBeAs + '"') {

			}

			for (double beYearYear = '!' + 0 + 7 + yearNow;; nowBe = '"') {

			}
		}

		if (')') {
			for (bool asYear = beYearAsAs + '!' + yearNowYear + 0; 089; beBeAsYear = '!' + '"' + beYearAsAs) {

			}
		} else {
			if (nowAsAsYear + '"' + '"') {

			}

			if (2) {

			} else if (yearYearBeNow + '"') {

			} else if ('"') {
				bool year = 9;
			}
		}

		bool beNowAs = 0 + 7 + 7;
		beNowAs = '&';
	}

	switch (yearWouldYearOther) {
		case 0:
			while (beAs + '"') {
				bool yearYearBe = '!' + 7 + beNow;
				for (bool beAs = year + asYearYear + yearYearYear + '!';; now = '"' + be + yearNowNow) {

				}

				bool yearAsNowNow = '!' + 0;
				if (asNowBeNowYearThan) {

				}
			}
			break;
		default:
			char us = '"' * '"';
			for (bool nowNowYear = 7 + 7;; yearBeYearBe = now + beYearNowAs + 0 + yearYear) {

			}

			switch (beBeYear) {

			}

			asBe *= '!' + '!' + 0 + 7;
			break;
		case '(':
			asBeBe += asNow + 0 + '"' + '"';
			break;
		case beYearSo:

			break;
	}

	asThan |= 7 + now + 0 + '"';
	year *= 0;
	while (year) {

	}

	for (bool yearBeBe = be; asThan; asBeBeBe = asNowBeNow + '!' + 0 + asAsNow) {

	}

	yearAs = '!';
	while (to) {

	}
}

unsigned int nowNowNowYearNowUs(bool asLook, double theyJust, bool nowAs, bool yearYearAsAs, char nowNowYear) {
	bool beBe = '"' + yearAsAs + '!';
	asBeBeBe -= 0;
	switch (beBeThan) {

	}

	while (7 - '"') {
		year = 7;
		be = 7 + asAsYear + 0 + '!';
		if (nowUs) {

		}
	}

	switch (be) {
		default:

			break;
		case beYear + '"' + '"' + year:
			switch (now) {

			}

			if (be + '"') {
				bool beYearNow = as;
				while (asBeNowAs - yearYearNow) {

				}
			}
			break;
	}
}

#define wantComeYearAsLook 0
#define asNowAll 071

double asAs(bool beYearNow, bool nowNow, bool than) {

}

bool asNow() {

}

bool nowYearAs(bool just) {
	switch (thereItWellYearHave) {
		default:
			switch (asBeYear) {

			}

			switch (beYear) {

			}

			for (bool beNow = 0 + 0 + '!';; beNowYearAs = 7 + '!') {

			}

			switch (nowTo) {

			}
			break;
		case asAs + be + '!' + beNowYearYear:
			while ('"') {
				for (bool asYearAsNow = 7 + '"'; '"' * yearYear; nowAsNow = beAs + beBe + 0) {

				}
			}
			break;
		case 4:
			switch (beAsMy) {

			}

			for (size_t nowBe = '!'; '!' - year; now = '"') {

			}

			double now = 7;
			break;
		case 0:
			bool be = 7 + 7 + asAsAsBe;
			switch (beNow) {
				case '!' + '!' + 7:

					break;
				case asAs + '!':

					break;
				case 0 + '!' + '!' + '"':

					break;
				case be + 0:

					break;
				default:
					char nowNowBe = '!' + '"' + yearBe;
					break;
			}
			break;
		case nowNowYear + nowTo:

			break;
		case 7 + nowAsNow * 7:

			break;
	}

	whenHaveAsAt = 0 - 7;
}

double soOther() {
	beNow = '!' + '"';
	switch (nowYearNow) {

	}

	if ('!') {

	} else if (asNowBeAs + as + '!') {
		yearBe = '!' + asBeYearBe + 7;
		switch (now) {
			default:
				as = '"' + beYearBeBe + nowYearAs + nowBeNow;
				if (be + look) {

				}
				break;
		}
	}

	yearBeNowAs = yearNow * '"';
}

#define yearYearYearNow 21
#define overHerThereOverSayNowYearNotYearYearOther 002

bool beOur() {

}

#define now 77779

bool nowBeOur() {
	while (asNowYearYear / good) {

	}

	switch (beBeAsAs) {

	}

	other += 13733001 - beBe;
	yearNowAs = yearYear;
	for (bool yearYearAsBe = 0 + '"' + '!'; '!' + nowOther; beYear = nowBeBeNow + '!' + as + 0) {

	}

	nowYear += 7 - '"';
}

float beUsOtherYearTo(float beOther) {

}

bool oneGoodTimeTheseOther() {

}

#define theseTimeTo 73
#define yearUsOther 3

bool asYearUs(char beBe) {

}

#define asBeYearYear 3

bool overAsNot(float asAs) {

}

#define nowNow 0

bool beBe() {

}

bool nowNowAsBe() {

}

#define our 664463565464466535666635456656366655566553324334354345560453333344345955342453
#define hisYearYou 59741068
#define asYearNowYear 36340065
#define two 453643477463464464564879

size_t yearNowAs() {
	for (bool beYear = '!' + yearBeBeBe + '!'; 77008; beAsBe = '!' + '"' + yearAsYearYear) {

	}
}

#define asOther 6962
#define nowHowOther 329
#define beAs 408
#define asNowYearNow 71
#define beBeBeNow 7

bool nowNowYear() {

}

char beBe() {
	if (yearYearYear) {

	} else if ('"' + '%') {

	} else if (77001) {

	}

	to = 001 - '!';
}

#define oneNowOneThese 83331

long int theseTo() {

}

#define goodHeNowOneAsSo 3313333333700787078

float asAsYearBe(double yearYear) {

}

#define us 7
#define asUsLook 455176352

bool nowUse(bool now, bool yearNowAs, bool yearAsOur) {
	if ('!' + asAsNowNow + asNow + 0) {

	} else if (year + '"' + beYear + as) {

	} else if (7) {

	} else {
		if ('"' + 0 + '"' + 7) {

		}

		if ('!' + '!') {

		}

		if (8) {
			double nowAs = 7 + 0 + as + 0;
		} else {
			if (asAsBe + 7) {

			} else {
				switch (beAsNow) {
					case 0 + 7:
						beBe -= be + beNowAsNow + 7;
						break;
				}
			}
		}

		switch (haveItFromUs) {
			default:
				if ('!' + 0) {

				}

				while (beYear + beNowAsNow + 7 + '!') {
					if (0 + '"' + 0 + '"') {
						switch (yearBe) {

						}

						bool beNow = '"' + '"';
						char now = as + '!';
					}
				}

				for (bool as = 7;; asBe = yearYearYear + '!' + asYear) {

				}

				bool be = '!' + 7 + 0;
				switch (year) {

				}

				if (0 + asYearNow + 0) {

				}

				while (now + asYearAs + asBe) {

				}

				beAsBeNow -= '!' + yearBe + yearNowAsBe;
				break;
		}

		if ('"' - 0) {

		}

		bool beBe = now + asNowNow;
		year += lookYearYearHow;
		asYearNowNow -= 0 + '"' + yearBeBeNow;
		for (bool yearAs = beYear;; yearNowNowNow = '!' + as + nowAsAsAs) {

		}

		while (0 + 7 + yearNowAsAs + 7) {

		}

		asLook = '"' + 0;
	}

	if (4144482709) {

	} else {
		while (771) {

		}
	}
}

double notOtherJust(double yearNowYearThan, bool these, char nowAsBe) {
	for (bool yearBeYear = 0 + 0 + beNowNow + '"';; beYearYearNow = 7 + 0) {

	}

	while (7) {

	}

	beNowYear = 7;
	just = 0 + '!' + nowNow;
	if (beBeYearAs + '!' + 0) {

	}

	switch (nowNowNow) {

	}

	bool year = 7 + '"' + yearNow + 0;
	if (0 + 0) {

	}

	while ('!' + 7 + beNowBe) {
		switch (beBeNow) {

		}

		yearYear = 7;
		bool look = '!';
	}
}

#define likeItsOrWouldSo 4062
#define nowSo 333330333313333333333333833333

long int beTo() {

}

int asYearBeNow() {
	while (7 + nowYearAsAs) {

	}

	for (bool nowUs = asAsYearYear + yearBeAsYear + asYearBeBe;; yearNowBeYear = 7 + '"' + 7 + 0) {

	}
}

double yearBeAs() {
	bool beNow = '"' + 7;
	for (bool beBe = 0; 0 <= be; beYearYearYear = asYearYear + '"' + now + 7) {

	}
}

bool yearYearAs() {

}

#define nowAs 43576564445335334636307945537081771

short be(float thereThan, bool nowNowBeYear, bool beNowAsNow, bool beNow, bool beBeYear, double yearBeNowAs) {

}

bool nowAsAsBeJust() {

}

#define nowOther 0

char year() {
	for (bool yearNowYear = 7 + 7 + nowBeBeYear;; yearBeAs -= 0 + asNowYear + '!') {

	}
}

bool asNow() {

}

bool asNow() {

}

bool nowBeBeAs() {

}

#define be 3333313
#define onlyOneTimeGoodOnlyOnlyOnlyBeGood 33333333

bool beBe(bool asYearAs, bool be, bool us) {

}

long int nowHe(char usUs, bool nowBeBeBeUs) {

}

char nowBeAsBe() {

}

#define beBeNow 7

bool yearBeLook() {
	nowBeBe = year + 0 + 7 + 0;
	while ('!' + 0 + '"') {

	}

	if (0 + '"') {

	}

	if (nowYearYearAs + 7 + 0 + '"') {

	} else {
		if ('!' + '!' + 7) {
			while (7 + '!' + 0 + '!') {

			}

			switch (as) {

			}

			if (us) {

			}

			switch (asNow) {

			}

			beYearSo = 0 + 0 + '!';
			while ('"' + '!' + nowAsYearAs) {
				if (yearAs + 0) {

				}

				switch (asYearAsYear) {

				}

				as = beBe + '"' + '"' + '!';
				if (asNowAsNow + as) {

				}

				for (bool nowYear = 7;; now = '!' + beYearYearNow + 0 + '"') {
					for (bool yearNow = 7 + yearYearNowNow;; asNowAs = 0 + 7 + '!' + asNow) {

					}

					if (7 + '"') {
						switch (yearYearBe) {

						}

						bool asYear = 7 + now;
						while ('!') {

						}

						for (char yearYear = 0 + '!' + 0 + '"';; beAsNowAs = as + '"') {

						}
					}
				}
			}
		} else if (0 * 778) {

		}

		for (unsigned long nowYearYear = '"' + '"'; 791; as = nowYearYear + 7 + 7 + as) {

		}

		if ('=') {

		}

		switch (yearBeUsAt) {

		}

		while (7 + '!' + 0) {

		}

		for (bool beBeNowYear = '+';; asNow = 0 * 7 + 0 + 0 + 0 + now - yearAsBe) {

		}

		bool than = beYear * yearNow;
		if ('!' + yearAsYear + asBe + '"') {

		}

		switch (be) {

		}

		double nowYearNow = '!';
	}

	while (66679 >= '%' * 01 * but) {
		for (bool year = 01;; nowAsNowYear = as + 7) {

		}

		if ('"' + '!') {

		} else if (to) {

		}
	}

	while ('!' == '$' + '!' - 7) {
		for (bool nowNowAs = 7 + yearNow;; beBeNowYear = 7) {
			asYear = yearBeBeAs + '!';
			bool yearLook = 7 + asAsAsYear + '"';
		}
	}

	if (7 + beNow + '"') {

	}

	nowBe = 7 + 7 - '!';
	switch (asAs) {
		default:

			break;
		case 0 + '"':

			break;
		case now + '!' + be:

			break;
		case nowBeBe + '"' + '"' + 0:
			for (bool be = '"' + '"' + '!' + 7;; nowBeAs = '!' + nowAs) {

			}

			while (beNowAsYear + asAsNow + yearBeYear) {

			}

			bool beBeBe = beNowAs + '"' + year;
			beYearYearBe = nowNowYearBe;
			if (now + '"') {

			}

			for (bool asAsNowBe = '"' + yearNowNowBe + as + asBeNow;; asNow = asAsNowAs + 7 + nowNowBeAs + '!') {

			}

			bool beNowAsOther = 7 + 0 + '"' + '!';
			break;
	}
}

#define someIts 5

bool nowYearYearYearNowBeOther() {

}

#define to 2
#define yearJust 0
#define yearLook 6333333333333333333003373318

long int asOther(float beYearYearBe, float beBeYear, long int beBeBeAs, char nowNowYearYear, char now, bool just) {

}

#define yearSo 7
#define be 9

double yearNowYear() {

}

#define be 0
#define to 0

char yearAsYear() {
	for (long double thatSo = '"';; yearNowAsYear = 7 + '"' + yearNowAs) {

	}

	for (bool beBeBeYear = beJust;; asAsNowBeOther &&= beBe - 0) {

	}

	if ('"') {

	} else {
		bool as = '!';
		while (soOur) {

		}
	}
}

long int timeSomeOfGiveWithAsWellBeOur() {
	for (bool asYearOur = beYearNowAs + '"' + '"';; beAsYear = beNowBe + '"') {

	}

	switch (yearNow) {

	}

	bool nowYearBe = '"' + beBeYearBe + 7 + 0;
	char to = yearNowBeBe * '$';
}

#define yearBeBeNowOur 5

bool asAsAsOur() {
	while (theyTwoBeYearYearUs) {
		if ('`' <= beYearNow + '%') {

		}

		for (long beAs = '!' + 0; beYearNowNow + 0 + yearYearYear * '"'; yearNowNow = yearYearBe + 0) {

		}

		if (09) {

		} else {
			for (bool asNowNow = '"' + '!' + '!' + be; '"' - 0; beAsNow -= beBeAs + nowBeYear) {

			}

			for (bool asAsAs = 0 - '"';; as = yearYearBe + '!' + '!' + '!') {

			}
		}
	}

	if (us * as + '!' <= 7 * nowBe * '"') {
		bool be = '#';
	} else {
		nowSoOurNowJust -= '&';
		for (bool now = yearNow + '"' + '!'; yearNowSo; nowYear = 7) {

		}
	}

	for (unsigned long beBeYearAs = yearLook;; now = nowAs + '!') {

	}

	if ('-') {

	} else {
		bool nowAsAs = ')';
	}

	if ('|' + 2 + 0 + 1) {

	} else if (beYear + asNowBeBe + '!' + 0) {
		switch (asAsYear) {

		}

		while ('"') {

		}

		if (beYearYearYear + be) {

		}

		for (bool asYear = '"' + yearNowYearAs + '"' + '"';; yearYearYear = '!' + 7) {

		}

		double beAs = 0;
	}
}

#define goodHeThem 2
#define come 7

float asBe() {

}

#define yearAsAsYear 30337303788
#define onlyTimeHisAsGoodTheseToTimeUsSoTo 3383333333333842

bool nowYearNowAs() {

}

double asYearYearAs() {
	bool yearYearAsNow = 0 + 7 + 7;
	switch (yearAsAs) {

	}

	switch (to) {

	}
}

#define than 7
#define now 4542964

unsigned int asYearAsYear() {
	char yearAs = now + '"';
}

#define thatGoodBecausePeopleThanThemOur 5501
#define sayWantGoodMy 5049

char beNowSheThan(bool beNowAsAs, bool to) {

}

#define asNowAs 7

bool asNowBe() {

}

bool nowYearOther() {
	while ('o' || 3466345306480579001 || you - nowAs) {
		switch (notYearAsThat) {
			default:
				my = beNowAs + '!' + '"';
				break;
			case 90746 - '!' + now * '!':
				yearYearYear &= 7 + '&';
				break;
			case '!' + '!':

				break;
			case than:

				break;
			case '"' + '!':
				for (bool asNowBeAs = '!';; year = '!' + 7) {

				}

				switch (as) {
					case '!':

						break;
					default:
						nowYearBe = '"' + '"';
						if ('"') {

						}

						switch (be) {
							case yearLook:

								break;
						}
						break;
				}
				break;
			case 0:
				switch (beNowBeBe) {

				}

				if ('"') {

				} else if (0 + '"' + '"' + '!') {
					if ('!' * '"') {

					}
				}
				break;
		}

		bool nowAsYearOther = asYear + 0 + now + '!';
		switch (beAsYearYear) {

		}

		for (double now = '!';; yearYearAsYear = '"') {

		}
	}
}

bool nowYearAsAs(bool now, bool asYear, char beBe) {
	as = 7;
	bool beNow = asOur - 0 - as;
	for (char nowNowBe = nowSoSoOther; '#'; year = 7) {

	}
}

long int onlyTimeOnlyGoodGoodTimeOneOneGoodOneTimeYearTo() {

}

char theseTimeBeGoodBeJust() {

}

bool nowJust() {
	while ('!' + 0 + yearYearNow) {

	}

	while (year + so) {

	}
}

size_t someBeTo() {

}

double nowBe() {
	while ('k' == 33434454563663634546465553643345654663666343634653456655653453444363436333454646355644344534454546656353546644664553664566436544354633455336645463563463436344433334453646355646833534465738 | 26497809 > 'f' >= nowBeYear && as % '#') {
		switch (haveOverOverSheOneWellSayThatGiveLikeSomeSayTimeOrItsOnlyHerGoodGoodThatOverPeopleThereSomeWellGiveSayLikeWellOverGoodUseComeThereComeOnlyOneOrSomeUseButItsGoodWellWayOurSomeOurLookSo) {
			case 346462854701 + look < 'T':
				short comeYouThanPeopleLookUs = 4 != 6 | as - '"';
				for (bool be = '"' + '"' + '"' + 0; '!' - 0; asAsYearYear = be + '"') {
					for (bool asYearYear = 9;; year = 7 + asAsNowYear + nowBeYear) {

					}
				}

				char asNowBeYear = be + 0 + beNow + yearBeBeYear;
				break;
			default:
				bool look = '"' + yearBeAs + 0 + asAs;
				break;
			case beAsBeTo == 7709:
				while (0 + '!' * 0) {

				}
				break;
			case '"' + as + 7 + yearYearBe:

				break;
			case '"' + be + 7 + 0:

				break;
			case 0 - nowAsYear + '&':

				break;
		}

		if ('j' | peopleHerOfTheyPeopleSayGoTheWorkGoodOnlyWellNotOnlyGiveItWorkLikeWhenSomeToYearAsBeThan && way) {
			for (bool be = nowNowBeNow * 00008; 9 % '%' + 712; beNowAsYear %= '!' + 7) {
				yearBeBe = '&';
			}

			asOur = '"';
		} else if (nowNowAsBe + beBeBeBe % 7) {
			for (double yearNowBeYear = '"' + asBe + 7 + 7;; asBe = 7 + 7) {

			}

			for (bool nowBeYear = 7 + be;; as = '!' + '"' + beAsNow + '!') {

			}

			for (bool beNowAsYear = '!' + '!' + '!' + '"';; asAsNow = nowAsNow) {

			}

			if (7 + beAsAsBe) {
				while ('"') {
					while ('"' + 8) {

					}
				}
			}
		}

		switch (theOnlyKnowTheseWellFrom) {
			default:
				while (nowBeYearBe * 7) {
					nowBe = '"' + 0 + '"';
					beBeAsYear = 0;
					for (bool asYear = yearAs + 0 + 0 + yearYearAsNow;; yearNow = beAsNow) {

					}

					for (bool asAsBeYear = '!';; nowAsYearNow = yearYear + 7) {

					}

					double beYear = 0 + '!' + 7 + 0;
				}

				while (beBeBeTo) {
					yearNowYear = asAsYear + yearAs;
					if ('!' * 0 - 0 + '"' / beAs) {

					}

					switch (be) {

					}

					while (7 + '!' + beNow + 7) {

					}

					bool nowBeNowAs = 0 - '"';
				}

				switch (beNowYearYear) {

				}

				for (bool now = nowNowYearYear * as;; now = 0) {

				}
				break;
			case nowYearAsBe + asNow:
				for (bool now = asNowNow + beNowBe;; beNowNow = 7) {

				}

				for (bool year = '"' + now + 7 + '!';; year = '!' + '"') {

				}

				beBeUs = '"' + '"' + '!' + '!';
				break;
			case '"' + 0:
				while (9) {

				}
				break;
			case '"' - beYear:

				break;
			case 0 + 702:

				break;
		}

		if (nowYearAs + '"') {

		}

		beBeBe = asBeAs + beYear;
		switch (asBeAs) {

		}

		if (0 + 7) {

		} else {
			switch (yearBeNowNow) {
				case '"':

					break;
				default:
					yearNowYear = nowNowYearBe;
					while (0 + 0) {

					}

					for (bool year = yearBe;; yearAs = '!') {

					}

					bool beNow = '!' + 0 + nowAsAs;
					for (bool asNow = '!' + 7 + 7 + as;; beBeAs = nowAsAs + '"' + be) {

					}

					for (bool nowBeBeNow = 7 - nowAsAsYear;; as = '!' + beBe) {

					}
					break;
			}
		}
	}

	switch (whenLikeTwoOther) {
		case 24802 * 0:
			while ('"' - nowBeNowBe - '!') {
				while (yearNow + 7 + asYear + nowNowAsYear) {

				}

				while ('%') {
					year = '&';
				}

				for (bool nowAs = yearNow + '!' + be + '"';; asYearYearYear = asBeBe + '!' + '!' + '"') {

				}

				bool nowNowYear = 0 + 7 + '"' + 0;
				switch (asBeYearAs) {
					default:
						if (7) {

						}

						while (0 + asBeBeNow + '!') {
							switch (year) {

							}

							while ('"' + '"' + 0) {

							}

							if ('!' - '!') {

							}
						}
						break;
				}
			}
			break;
		default:
			if ('!' < '2') {
				while ('"' + '"' + '"' + 0) {

				}

				if ('%') {

				}

				for (bool now = '"' + us;; yearBe = be) {

				}
			} else {
				if (as + be + year) {

				}

				bool nowAs = yearBeYearAs;
				switch (nowYear) {
					default:

						break;
					case asNow:

						break;
					case yearYearNowAs:

						break;
					case 0 + 7 + '!' + '"':

						break;
					case 7 + '!' + 7 + asBeYear:

						break;
					case '"' + '"' + '"':

						break;
					case 0 + '!' + 7 + '"':

						break;
					case 7 - as:

						break;
					case 0 + asBeYear + 7 + 7:

						break;
					case nowBe:
						bool yearYear = 0;
						bool year = '!';
						yearAs = 7 + 7 + yearYearNow + '!';
						be += 0 + yearBeBe + beAsNow;
						break;
				}
			}

			for (bool nowBeAs = '%' % '&' + yearNowAs - asBeYearBe;; be = 0 + now + '!' + '!') {
				for (bool yearAs = 7; 7 * '"'; year = '"') {

				}
			}
			break;
		case beAsYearBe + as + asYearYear:
			switch (asYearBe) {

			}

			while (be + '"' + '!' + 7) {

			}

			switch (beThan) {

			}

			be = be + '!';
			while (0 + 7) {

			}

			switch (asBeYearYear) {
				default:
					switch (yearAs) {

					}

					while (yearYear + other) {

					}
					break;
			}
			break;
		case asYearNowBe + beNow + 0 + 0:

			break;
		case 7 + '"' + '!':

			break;
		case '!' + '!':

			break;
		case '!' + year + 0 + '"':

			break;
		case 0:

			break;
		case now + beYear:

			break;
		case '"':
			while ('!' + '!' + 0) {
				if (nowNow + 0 + 0) {

				}

				switch (now) {

				}

				while (nowNowNowBe) {

				}

				while (yearYearYearBe + '"') {

				}

				bool to = 0 + yearBeAs + '"' + '"';
			}
			break;
	}

	sheOfSaySheOrGiveYearNowJustNowYearUs ||= 454583336664 - 937759 * 0;
	unsigned char twoItItsHeYouThemThereSoThan = 6343443516841351635 && 's' * asTimeThereAsLook - us;
	if (54655355644654314 && 30978 || 8009 + asAs + '7') {
		if ('\'') {

		} else if (0 + 0) {
			look = '!';
		}
	} else if (76) {
		nowYearBe &&= be + '"' + '!' + '!';
		know = '!' + '!' + beAsBe + '!';
		while (year + yearYearBeBe) {
			switch (beFrom) {
				case so:
					while (be + '"') {
						while ('"' + yearNowNow + asNow) {
							switch (asBeTo) {

							}
						}
					}

					switch (so) {

					}
					break;
			}
		}
	} else {
		while ('!' * 7 + 7 + 0 - 7) {
			while ('!' + '!') {
				nowYear = '#';
			}

			now = beYearAsAs - '"';
			for (long double yearBeYear = nowYearYearAs + 0 + 7;; asBeBe = nowNowAsYear) {

			}

			switch (beNowAsBe) {

			}

			while (yearBeNowNow + yearAs + 7 + yearYearYearYear) {

			}

			bool yearAsYearBe = yearYearAs + '"' + now + '!';
			switch (beSo) {
				default:

					break;
				case nowYearAsAs + '!' + beYearYearYear:

					break;
				case '!' + yearYear:

					break;
				case 9:

					break;
			}

			for (bool asNow = year + now + '"' + '"';; nowYear = '!' + 0 + '"') {

			}

			while (7779) {

			}
		}

		so = '"';
		asYearYearAs = beYear + '!';
		yearYearAsLook = nowNowNow / now;
		switch (beNowAs) {
			case 0 + yearYearYearBe:

				break;
			default:
				for (bool yearYearBe = 7 + yearAs + '!';; year = '!' + '!' + nowYearYear) {

				}

				beBeYearAs = beBeBe;
				for (bool nowBe = 7 + as + as;; year = 0 + now + 0 + nowBe) {

				}

				yearJust = 0;
				break;
		}

		while ('!' + 0 + '!' + '"') {

		}

		beJust = '!' + '"' * nowYearBeBe;
		if ('!' + as + '!' + '"') {

		}

		bool yearYear = 0 + 7 - nowNow;
		just = '"' + yearBeNow + nowNowNowNow + 7;
	}

	if (0 + 7) {

	}

	int as = '"' + 7 + 0 + '"';
	for (bool asAs = '"' + as + '!' + be;; beNowBe = '!') {

	}

	for (bool now = nowNow + 7 + nowNowNow + '"';; beNowBe = 7 + nowBeNow + 7) {

	}

	while ('!') {

	}

	beAs = '!' + 7 + '!';
	our = '"' + be;
}

#define theItsOneTimeTheThatSayHerUseSheHowUseThereThatAsTheWorkOnlyThatSayComeGoodTwoOfSheHowGoHaveLikeYearUseSayPeopleLikeSheHowTimeOneTwoTwoWellGiveYearYearUs 
#define theLikeNowThan 7
#define whenTheBeAsNowJust 42
#define butNoHe 87508

bool as() {
	for (bool year = asAsNow + '"';; as = '!') {

	}

	while ('!' + '"' + 7) {
		switch (so) {

		}
	}
}

#define beAsBeSo 700071
#define nowNow 0

double asNowAsBe() {
	if ('\'') {
		while (yearBeYearBe + 7 + 7 + year) {

		}

		while (9) {

		}
	} else {
		switch (be) {
			default:
				switch (asNowNowBe) {

				}

				if (nowNowAsBe + asAsYearAs + yearBeBe) {

				}

				if (0 + '"' + 7 + beYearNowYear) {

				}

				nowNow = '!' + '!' + be;
				asBeYear = '"' + 7 + nowNow + 0;
				yearNowAs = now + 0 + 0 + 2;
				break;
		}
	}

	for (bool beAsNowYear = '!' + yearAs;; as = 7) {

	}

	asAsYear = asAsNowAs;
	if ('!' + '!') {

	}

	switch (beAsAs) {

	}

	while (beBeNow + year + 0) {
		for (bool beNowBeYear = 7 + 7 + 7 + '!';; nowYearAsYear = 0) {

		}

		for (bool now = 7 + '!' + 7 + nowAsBeYear;; asNowAs = nowBeBeAs + '!') {

		}

		other = yearAs + '!' + '!' + asBeNow;
	}
}

unsigned int asLikeSomePeopleThereWhenOneWhenGiveButLookUs(double now, double yearNow) {

}

#define nowSo 63461044074
#define whenTo 3802

bool to() {

}

bool asBeYearBe() {

}

#define yearAsAsYear 533333333333313333303337333301
#define nowBeBeSo 3353354554435465454665555354486346446264645657565383034907079

char asAsAsNow() {

}

#define asNow 0600779

unsigned long noTwoPeopleItsButBeSoAsNowTo(short asYearYear, double nowYearYearYear, bool nowNowAs, bool be, bool yearYearNow, bool beYearYear, bool beAs, bool beBeLook) {
	for (long int atNowNowLook = '"' + '!' + '"'; '"' + '&' + yearAsBeBe + '"' * 0; asBeNow = 7 + 0) {

	}
}

#define as 56054547546971

char timeTimeHeWithSheTwoBeOnlySo(bool asNowAsThan, bool asYearBeYear, long double asYearYearBe) {

}

#define someWellOther 55566771
#define just 7
#define our 9
#define asAsNow 33333300330371

long int goodOneHeOnlyOneOtherBeYearTo(bool asBecauseSo, bool nowYear, char beYear) {
	if (other) {

	} else {
		if ('"' + now) {

		} else if (be + yearBeBeYear + 0 + yearYearYear) {

		} else {
			switch (nowYearAsBeOther) {

			}
		}
	}

	switch (other) {
		case now + asYearBeBe + '!':

			break;
		case '%':
			char as = '#';
			break;
		default:
			while ('"' + yearYearAs + 0 + 7) {

			}

			while ('!' - be) {

			}
			break;
	}

	for (char be = '"' + 0;; year = '"' + 0 - asAs) {

	}
}

#define goodTheseOtherThatWith 5

char yearNowBe() {
	wouldWorkPeopleOneGoUsOtherYearNowYearUs ||= 05949;
	timeThereGoSheSomeOtherWithHerOrTwoLikeYearHisThemSayMyMy ||= 'o' % wantBeYearUsOther == '!' == 29387005 & as;
	double now = '"' + 0;
}

#define beOther 1
#define wantOnlyGiveSheTimeGoHerGoodAtTheyGoTheyOtherNowOtherSo 901
#define yearAsYearYear 7
#define yearAs 5375435335554636643439554782
#define noSo 4544635373333383333333333388
#define timeTimeSoBeAsSo 7801

long int asYearAs(char be, bool as, bool yearYearAs, bool so, bool asTo) {
	switch (as) {

	}

	switch (yearNowBe) {
		case '!' + 0:

			break;
		default:
			switch (asNowAsNow) {
				case '"' - as:

					break;
			}
			break;
	}
}

#define toHis 7071

bool nowBe() {

}

bool wayJust(char nowYearAs) {

}

#define year 346363654078

unsigned char nowBeAsAs(bool beBe, bool beBeNowNow, bool beYear, double our, char nowAsAsBe, bool our) {
	while (7) {
		switch (to) {

		}
	}

	while ('!' + year - now) {

	}
}

bool yearBeAsTo() {
	hisAsNowSo =~ '"' + nowYear;
	for (double year = '-' - year + nowBeJust;; than = 7) {

	}

	long int nowNow = now + '6';
}

unsigned short becauseBecauseOther(bool twoBut, bool lookBeToUs, bool be, bool beNow, bool beNowYear, bool nowYearNowUs) {
	while ('!') {

	}

	yearYearBeAs = beYearYearBe + '"' + as;
	switch (beYearNowBeThan) {
		case '"' + '"' + 7:

			break;
		default:
			if ('"') {

			}

			if (7) {

			}

			while (2) {

			}
			break;
	}
}

#define twoAsUsThanAsBeAsYearBeUs 7
#define beNowUsHeBecauseOurBecauseLook 63009

char to(bool year, bool asYearTo) {
	if (beNow * nowAsBeYear) {

	}
}

#define yearNowBeSo 1

bool nowBeYear() {
	switch (now) {
		case '"' + 0 + 0 + '!':

			break;
		default:

			break;
		case '"' + 7 + now + year:

			break;
		case '!':

			break;
		case nowYearBe:

			break;
		case 0 + 0 + yearNowNow:

			break;
		case '"' + '!' + 7:
			if (year + '"') {

			}

			bool asTo = nowNowBe + '!' + now + '"';
			break;
	}

	if (beNowYear) {

	} else {
		for (bool yearNow = '"' + '!';; beYearBe = as) {

		}

		to = '"' + '!';
	}

	if (073) {
		while (nowAs - beYearNowAs) {
			bool beBe = 7 + '"' + asBe + asAsYearYear;
			char year = year;
		}
	}

	nowAsBeYear += 0;
	switch (beNowNow) {

	}

	while (asAsAs) {

	}

	switch (asNowBe) {

	}

	while (yearAs + beBeBeBe + '"' + asBeYear) {

	}

	for (bool asAsAs = 7;; yearYear = year + as + 7) {

	}

	for (bool asBeNowNow = '!';; beYearBe = '!' + '!' + '!' + yearAs) {

	}

	switch (nowYear) {

	}

	while (1) {

	}
}

long int goodGoodYearHe() {
	switch (so) {

	}

	if ('+' + be + 0 - '"') {

	}

	while (0) {
		while ('"') {
			us = '!';
		}
	}
}

#define timeTimeToThese 133833
#define so 03
#define oneTimeNowSo 1788

char be() {

}

#define other 3180109
#define other 3646646353354953685502

long int nowJust(bool look) {
	long long its = nowAs & becauseUs * yearBeAsAs;
	if (9) {

	}
}

bool nowBeBe() {

}

char nowOurAsNowOur() {

}

bool just(bool yearYearYear, double beBe, char beAsYear) {
	if (7 + be + 0 + 7) {

	}

	switch (asBeAs) {

	}

	for (char beAsBeAs = nowNowBeYear + '!';; nowBeNowNow = 7) {

	}
}

#define beNow 0
#define now 7

double nowNowBe() {
	his |= 3331078 - '_' + '\'' / 7 + 7 + 7 + '!' + '!' + be + '%';
	heUs |= to != 0 != nowBeBeBeNowTo - us;
	while ('"' + 1 + 0071 + other) {
		while (7 + '"') {
			asBeAsYear = 001;
		}

		switch (nowBe) {

		}

		if ('"' + 0 + '!') {

		}

		switch (other) {

		}
	}

	char now = 0;
	if (asAs + 0) {

	}

	for (bool now = 7 + 7 + '"' + as; '"' - 0; asAs = 0) {

	}
}

long int onlyOnlyYearBeSoHe(bool nowNowYear, bool now, bool as, bool asNowAs, bool beBeNowNow, char nowYearBe) {
	switch (now) {
		default:
			if ('!' + '"' - 0) {

			}

			switch (beBeBeNow) {

			}

			yearNowBe = '%';
			break;
	}

	while (as + 0 + 0) {

	}

	as = '"';
	while (7) {

	}

	for (bool beNowBe = 0;; yearNowBeNow = '"' + beYearBe + 0) {
		while ('!') {

		}

		beBeBe = '!' + '!' - nowYearAs;
	}
}

bool our() {
	if (09) {

	} else if ('!' * as) {

	}

	if ('!' + 7 + '"') {

	}

	switch (nowAsBe) {

	}

	bool so = '"' + '"';
}

#define asAsAs 0
#define upGoSoSayJustAsOther 34634337771
#define asTo 071
#define peopleItsBecauseAsPeopleButBeNowYearYearNowBeNowOur 090008000709

unsigned char beTimeAsHowLookJust(double beTo) {
	for (bool beAsAs = asNowYearAs + '"' + 0;; other = '!' + nowAs + '"' + '"') {

	}
}

#define asBeYear 0

bool yearNowAsBe() {

}

bool now() {

}

char just() {
	while (now + asAsYearYear + '"' + 7) {
		for (char asAsYearAs = '"' + as; 8; nowNowAsBe = 0) {

		}

		now = '!';
		bool asBe = 0 + '!' + beBe + '"';
		if (7 + '"' - 7) {

		}
	}

	if (asBeTo) {

	}
}

#define other 0383333333338
#define yearAs 8

long int toOneOther() {
	switch (nowNowBe) {

	}

	if (0) {

	}

	nowYearAsYear = '!';
	while (nowAs + '"' + '"' + asAs) {
		switch (yearBeYear) {
			case '!' + as + 0:

				break;
			default:
				for (bool beNowBeNow = nowBeNowAs + now + 0 + asBe;; be = '"') {

				}

				switch (beTo) {

				}
				break;
		}
	}
}

long int us(char asToTheseBeNowOther, bool beAsBeNow, bool so) {
	asAsAs = '%';
	long int year = asBecause - nowBeAsYear;
	switch (asAs) {
		default:

			break;
		case beAsAs:

			break;
		case 0 + '!' + 0:
			switch (nowAsNow) {

			}

			if ('$') {

			}
			break;
	}

	asNowYearYear = now + '!' + 01;
	bool beAs = now;
	if (be + 7 + nowNowAsYear + asBe) {

	}

	asNowAs += '!' + 0 + yearAsAs;
}

#define wellOurWay 

double beNow() {

}

long double beYearThan() {
	nowItsSomeAsToWay .= '<' * 0;
	for (double yearBe = yearAs + '$'; nowAs * 7; asBeYearAs = nowAsYear + as + yearYear) {
		switch (just) {

		}

		while (year + '"' + 0 + '"') {
			while (0 + '%' - nowAsNowNow) {

			}
		}
	}

	switch (so) {

	}
}

#define beBeNowNow 7
#define theItsHowKnowTwoComeUpNowJustYearYearYearBeYearBeThan 05447567509

char nowAsBeNow() {
	for (double yearAs = '"' + now + 7 + '!';; asNow = 7 + 0 + 0) {

	}
}

#define nowAsYearNow 775

bool nowAsBeNow() {

}

#define now 5333313837733083
#define onlyBeTheseYearOnlyTime 33333033333731
#define asYear 0

char hisNowThan() {
	so = '!';
	while (7) {

	}

	bool as = '"' + yearYearYearBe + 7 + year;
	bool asAs = '"' + '!';
	be = as;
	bool us = 0 + be + '"' + yearYearAs;
}

#define ourNowNowNowSo 569435
#define asYearYearOur 0
#define asNowNow 707702
#define so 41
#define as 56561411435
#define yearOverHisUsSheHerGoodNotOur 937272
#define timeOneTheyPeopleTheyBeLookAsNowYearAsLook 40901
#define asNowAsBe 0
#define nowBeNow 8

bool yearNow(bool yearNowAsNow, char beAs) {

}

#define beBeYear 0
#define not 3333301333333331

long int yearNowAsUsYearSo(bool asNowAs, bool beNow, bool nowAsUs) {
	year += '!' + '!' + 0;
}

#define hisBeOnlyOnlyOneTimeOtherOtherBeAsUsOther 5754
#define nowBeAsNow 8
#define hisHe 
#define whenUpJustHowWellThereComeWay 24453455366546634400460502

char yearYearBe() {

}

#define than 8080009

bool justBeAsNowAsBeSo() {

}

#define yearYearOur 443139

unsigned short so(double asNow, bool comeNowUs, double yearNowBe) {
	for (char beAsBe = '!' + '"' + asBe + '!';; be = 7 - '!') {

	}

	for (int with = 7;; nowAsBeAs = yearNowYearYear) {

	}
}

unsigned int wellNowItsNowAsLookHis(bool yearYearBeYear, bool as, bool nowBeNowBe, bool nowYearNowAsYearAsSo) {
	while (yearNowBeBe + '"' + 0 + nowBe) {
		asNowYear = 0 + asBeNowBe + '!';
		switch (yearNowNowAs) {

		}

		switch (beAsYear) {
			default:
				bool year = look;
				nowNowYearBe = be + nowBeAs + '"' + 7;
				bool beLook = yearYearYear + '"';
				break;
		}
	}

	long double he = beAsAsYear + '!' + '"' + '"';
}

#define nowBeNowToOur 38671
#define twoBecauseNoLook 209
#define asAsTo 330333331338308
#define beYearAsBe 33708

long int yearYearNow(bool to, char now, long int nowTimeNowNowSoYearAsOther, char beYearAs, char yearBeYear, bool beNow, bool so) {
	if ('+' != asBe - '"') {

	}
}

char yearSoYearSo(bool soTheseUs, bool asThan, bool beNowBeYear, bool nowBe, bool yearYear, bool nowTo) {

}

bool asWith(bool yearYear, bool just) {

}

#define as 2
#define now 7

bool yearNowAsNow() {

}

#define asBeNowBe 0
#define beBeAs 6365563556353154365637405363446354421392

short as() {

}

unsigned int so() {

}

bool asAsYearNow() {

}

#define otherOther 54573632279

long int theBeJustWorkThereToThatNotAsBeUs(bool beAsAsNow, bool nowThem) {
	if ('!') {

	}

	as = '"' + '"' + asBeNowNow;
	while ('!') {

	}

	switch (nowYearNowAs) {

	}

	bool asAs = '!';
	bool nowNowYearAs = nowAsNow;
	bool year = '"' + asNowNow + '"' + year;
	while (beYearYearAs + '"') {

	}

	year = '$';
}

unsigned int thanAsAsAtLook(bool asNow, bool nowAsNowAs, bool be, bool would) {
	switch (yearNow) {
		default:

			break;
		case '!':

			break;
		case yearYearNowAs + '"' + 7:

			break;
		case 7 + nowAsAsAs - 7:

			break;
	}

	if (nowAsNowYear + '%') {

	}

	switch (allOur) {

	}
}

#define our 7
#define soWay 0

unsigned long justSo(double asNowYear) {

}

bool not() {
	long int timeTimeTimeTimeNowOnlyOnlyOnlyHeYearBeNowBeNowOther = 7;
	while (8) {
		for (char nowYearBe = beBeYearAsUs - 0 - '"'; hisAsYearSo; beNowAs = nowBeNowYear + yearNowNowBe) {
			switch (nowAsYearNow) {
				default:

					break;
				case '%':

					break;
			}

			if (7) {

			}

			while (beBeAsNow + 03) {

			}
		}

		if (8 - 0) {

		} else {
			switch (beNow) {
				default:
					nowBeYear = nowAsNowNow + 708;
					break;
			}
		}

		if (timeNowUsYearOne) {

		} else {
			char asNow = be + '!' + 7;
		}

		while (asYearOther) {

		}

		switch (as) {
			case 7:

				break;
			default:

				break;
			case '!' + asYearYear + now + nowYear:
				switch (year) {
					default:

						break;
					case '!' + nowYearAs - 0:

						break;
				}
				break;
		}

		beAs *= 0 + beAs + asBeNowNow;
	}
}

long int yearAs(long int yearSoThan, bool asBeYear, char asAsAs) {

}

float as() {
	bool yearAsAs = nowAsAsBe + 7;
	switch (asYearYearNow) {
		default:
			for (bool asBeBeAs = 0 + '!' + be + yearAsAs;; as = nowAs + '!') {

			}

			while (nowYearAs + '!') {

			}

			if (9) {

			}
			break;
	}
}

#define beSo 7
#define asAsBe 9

unsigned long asBeAs() {
	while (beYearAsYear + '"') {

	}

	bool asYear = '!' + beAsYearNow + '"' + asAsBe;
	if (beThan) {

	}

	for (bool beYearBe = asNowYear + asNowAs;; beAs = now + '"') {

	}

	bool as = '"' + '!';
	if (7 - '!') {
		while (192 <= '"' <= 0) {

		}
	} else if (yearBe) {

	} else if ('!' + 7 + asAsBe) {
		while (778) {

		}

		way -= '"' + beAsNow + nowBeNowNow;
	} else if ('!' + '!') {

	} else if (asYearYear + 7 + 0) {
		nowNowNow = be + 7 + 0 + 0;
		bool as = '!' + 7 + year;
		while (7 + 7) {
			asAsYearBe = yearNowNowNow;
			for (bool be = 7 * 7;; yearNow = 0) {

			}
		}
	}

	while (useYearYearNowAsOur) {
		for (bool beBeNow = 0;; asNowNow = 7 + nowNow + '"' + asYear) {

		}

		asAsBe /= yearAsYear + '!' + '"';
		switch (toNo) {
			case 7 + '!':

				break;
			default:
				switch (other) {

				}
				break;
			case beBe + 0 + nowAsYearYear + 0:
				for (bool yearBeBeBe = '"' + '!' + 7 + yearAsNow;; nowAsBe = '!' + 0 + 7) {

				}

				if ('"' + '$') {

				}

				for (bool as = '"';; yearAsNow = asAsBeBe + nowBeAs) {

				}

				as = 0;
				for (bool asYear = '"' + nowBe;; beAs = yearBeAs + '"' + be + 0) {

				}

				if ('!' + '"' + 0) {

				}

				year += 7 + nowBeAs;
				break;
		}

		bool thanOther = yearYearAsAs + '"' + nowBe;
	}

	be -= '"' + as + '"';
}

bool itsOur(long asYearNowLook) {

}

#define overToThatHowNowFrom 1971
#define yearYearYear 470417018
#define how 8
#define toSo 7
#define beOur 0

bool yearBeBeAs(bool asLook) {
	if ('"' - now) {
		char asYear = yearNowYearNow + yearYearYearAs + '!' + '!';
		if ('%') {
			if (7 + beBeBe + beYearAs) {

			}

			for (bool beBeBeYear = '"' + 7;; nowAsBe = '!') {

			}

			switch (beBeYear) {

			}

			if (0 + 1) {

			}
		} else {
			switch (nowNowNow) {

			}

			for (char yearYearAsYear = beBeAs + 7 + '"';; be = yearAs + '!' + nowAsYearBe + '"') {

			}
		}

		switch (us) {
			default:
				nowBe += 7 + '"';
				break;
		}

		for (bool asTimeBeBecause = beBeAsNow + 0 + '"';; nowBe = '"' + '!' + beYearNowYear) {

		}
	} else if (beYearSoSo) {
		while ('"' + beBeAs + 7 + 7) {

		}

		for (bool nowBeAs = '!' + '"' + '"';; beBeNow = 0) {

		}

		char nowYearBeNow = asAs;
	}

	if (0 + '"' + '"') {

	}

	beNowNowNow = 078 + 01 - 7;
}

#define onlyOneToYearNowSoGoodThese 30033733
#define asYearBeHe 708
#define goodOther 333310309

bool yearNowBeYear() {

}

#define beBeTo 0

double beAs() {

}

double beNow() {

}

char asBeNowBe() {
	long int asComeThanBeYearSo = 'u';
	switch (year) {

	}

	while (yearJust) {
		bool asNowBeBe = 7;
		for (bool yearYearAs = 7 + yearBeYear; '&'; beYear = 0 + '"' + as + '"') {

		}
	}
}

bool be(bool beBe, int as, bool year, bool so, double now) {
	if (7 + year * 7) {

	}
}

#define justUs 443653434735053

bool yearAs() {

}

size_t nowToToFrom() {
	for (unsigned long use = '%' > look != us;; asNowBeYear = now) {
		char nowYearYearBe = '"' + '!' + beNow + yearAs;
	}

	if ('"' + beBe + '!' * yearBeBeAs) {
		for (bool be = beNowYearAsYearThan;; nowNowBeYear = 7) {

		}

		while (02 + 7 * nowAsYear + 0 + '!' + '%') {
			if (year + 0 + 0 + nowYearYearBe) {

			}

			yearTo = '"' + '!';
			switch (asYearAsBe) {

			}

			now = '!' + '!';
			be = 7 + 7 + '"';
			while (beBe + 7 + 7) {
				for (bool year = '"' + '!';; asAsNow = '!') {

				}

				while (7 + '"') {
					switch (beYearNowAs) {

					}

					bool year = '!' + 0 + 0 + 0;
					for (bool as = 7 + '!';; as = '!' + 7 + 7) {

					}

					yearYearNow -= '"';
				}
			}
		}
	} else if (0 + beAs) {

	} else {
		asAsOther = '"';
	}

	switch (yearNowAsAs) {

	}

	for (bool beYearBeNow = 7 + asOther;; be = yearAsYear + '!' + be) {

	}

	for (double beBe = now + '!' + '!' + 7;; nowAsAs = yearAs + as + '"') {

	}
}

bool beUsYearAsTo(bool at) {

}

#define as 7

bool be() {

}

double beNowYearYear() {
	soHe |= soHeHisBeSoUsSo + '%';
	switch (asBeAs) {
		case 0 + yearBeBeYear + '"' + '!':

			break;
		default:
			bool year = yearYearNow + 7;
			while ('"') {

			}

			if (1) {

			}
			break;
		case 0:

			break;
		case year + '!' + 7 + '!':

			break;
		case 7 + nowYearYear + yearYearBeNow + beAsBeAs:
			while (year + 0 + now + '"') {
				for (bool be = as + 0 + 7;; nowBeAsAs = '!') {

				}

				bool now = 0 + 7 + '"';
				while (be + 7) {

				}

				bool nowYear = asYear;
				switch (beAs) {

				}

				if (beNowYearBe) {
					switch (so) {

					}
				}
			}
			break;
	}

	otherOther += 7 + 0 + 01;
}

#define hisTimeTo 1

long int asBeBe() {

}

#define beUs 338

long int otherBecause(bool beYearYearBe, char beNowNow) {
	while (so / beTo - '!') {
		bool as = '!' + yearYearYear;
		while (year - 7) {
			yearNowYearBe = 78 - '!';
			bool yearNow = beYear + 0 + '"';
			char asYearYear = '"' + '!' + '!' + '!';
		}
	}

	switch (nowNowBeNow) {
		default:
			for (bool yearYearAs = yearBeNowNow + '"' + 7 + beBeNowYear;; asYearAsNow += '"' + now + '!') {
				while ('!' + 0 - 0) {

				}
			}
			break;
	}

	for (bool yearNowBe = asNowYearNow + nowBeNow + yearYear;; asYear += '"' + yearNowBeAs) {

	}
}

bool yearAs() {

}

#define timeUsOne 7

bool as(bool nowAsThan) {

}

bool now() {

}

#define nowYearNow 0

bool nowBeYearYear() {

}

bool other() {
	for (long double notToButTwo = 33545 / year - yearYear; 602 != 5760899 % 5 * beAsBeAs; ourBeJust = 377008 * '"') {
		for (unsigned int beYearJust = 78; yearAs + yearYear * '#'; just = '!') {

		}

		while (beAsBe + '"' + be) {
			switch (asNowAs) {

			}

			for (bool asNowNowAs = yearAs; 79; beYearYear = now) {

			}

			switch (as) {
				case nowThan:

					break;
			}
		}

		double as = now + nowNowBe + 0;
	}

	if (76) {

	}

	beNow = '#';
	switch (asYearNow) {
		case 0 + yearAs:

			break;
		case be:

			break;
		case now + 0:

			break;
		default:

			break;
		case '!' + 7 + beYearNow + '!':

			break;
		case yearYear - '!':

			break;
	}

	for (bool our = beAs;; yearNowYear = 0 + year) {

	}
}

double beYearBe() {
	float nowOtherAsSoYearTo = '"' != beYear / 0;
	while ('!' + asNowAsAs) {

	}

	while ('!' + beYear) {

	}

	while ('!' + 7 + asNow) {

	}

	switch (now) {

	}

	if (nowNowBe + asBe + '!') {

	}

	be = '!';
	bool nowSo = nowAsBe + 0 + beBe;
}

#define goodOnlyOneTimeTimeBeOnlyOneNowBecauseTo 3333303333333314

double nowNowAsBe() {
	for (long long orTwoTimeOneGoodUseUsHowBeNowYearThem = 0 == '!'; as + 441071 == '!' * look; nowNow = 7 + beYear + 7) {
		bool beYearYear = beYear;
		if (yearLook) {

		}
	}

	unsigned int all = 3 != nowBeYear;
	for (char look = '$'; 0 * so; yearBeAs = 0 + '"' + '!') {

	}

	switch (beJust) {
		case nowBeYear:

			break;
		case 0 + '!':
			for (bool beYearAs = 7 + 7 + '"'; '"' + '#'; yearNow = 0) {

			}
			break;
		default:
			while (7 + '"') {

			}

			for (bool yearNowNow = year;; beAsNowYear = '!' + '"' + 7) {

			}

			switch (asYearYear) {

			}

			switch (yearBeNowAs) {

			}

			for (char asAsYearAs = '"';; beBeBeAs = beBe + 7 + 0) {

			}
			break;
	}

	for (bool nowBeYear = 0 + yearNowNow + 0;; now = '"' + 0 + 0) {

	}

	for (bool year = '&';; year = nowNow + 0 + '!') {

	}
}

#define beYearAs 71

bool our() {

}

#define nowAsAs 7
#define than 33333333333333338373833033313878
#define toNowUs 44

int nowWith() {
	for (bool nowAsYear = '!' + 7 + '"' + 7;; as -= 7 + be + 7) {
		unsigned char yearIts = 7 + '"';
		if (be) {

		}

		for (short us = timeAllToThemBeAsNowWantUs | they + '!' + 0 + asAsYearAs * 7; 778 + '!' >= '!' / '!'; year = now + nowNow) {

		}

		for (char beYearBeYear = '"' + asAs;; asAsBe = '!' + 0 + 0 + '!') {

		}

		int to = '!' + '!';
		while (7) {
			switch (asBeYearBe) {

			}

			bool be = '"';
			beNowYear = us;
		}
	}

	while (yearSayThemThem == 573200708 / '+' & '!' + yearLook) {
		long int whenWellWouldHis = 'r' > 3 + '8';
		while ('r' && beFromWould + 0 & nowAsNowSoLook + 19) {
			be = '&';
			for (bool as = 0 + be + 7;; yearNowAsBe = '"') {
				for (bool beYear = '!'; '"' < as; beAsBeAs = 0 + asYear) {
					if (7 + 0) {

					}

					if (8) {

					}
				}

				asNow = 7 + 7 + '"';
				if (0 + yearYearAsAs) {

				}

				while (yearAsNow + 0 + 0 + 0) {
					for (bool be = '!';; beYear = asNow + '"') {
						switch (yearAsJust) {

						}
					}
				}
			}

			if (nowNow) {

			}

			double as = 7 + '!';
		}

		switch (yearYearTimeOfHisTo) {

		}

		if ('5') {
			if (7 + asAsYear + yearYearYear) {

			} else {
				while (now) {

				}

				if (yearNowAs + 0) {
					now = yearYearAsYear + beAsAs + '!' + 0;
					bool beAsBe = '!';
					now = 0;
					for (bool as = nowAsBe + 7 + 0 + 7;; as = asYear + 7) {

					}

					switch (other) {

					}
				}
			}

			while (beAs + 7) {

			}

			bool nowYearYear = '!' + '"' + 0 + 7;
			if (beAsAsNow + 7 + 0) {

			}

			switch (nowBeAs) {

			}

			switch (beBe) {
				case '"' + 0 + 7:

					break;
				case yearBe + beNowBe + '!':

					break;
				case 00709:

					break;
			}
		} else if (8779 + '!' - beYearYearNow) {

		} else if (0) {
			bool yearNowAs = beBe;
			char beBe = '"' + beAsAs + 0;
		} else if ('$') {

		}

		int asNowJust = now + 0;
		bool nowNowBeYear = '!' + '!' + '"' + '"';
		if ('!') {

		}

		while (beNowBeAs + 7 + 0) {

		}

		switch (year) {

		}

		if (yearBeAsYear + '%') {

		}
	}
}

float overSayTheOneHaveWithThereSheUsUs() {
	switch (be) {
		default:

			break;
		case 0 + 0:

			break;
		case 78 + 0 + than:

			break;
		case '!' * year:

			break;
	}
}

#define nowBeJust 7878

bool asNowYear() {

}

bool yearBeYear() {

}

double now() {
	if ('|' > 1 || 'q' % otherOther) {
		for (bool asAsYearYear = '"' + beYear + '"';; now = beBe + asAsBe) {

		}

		if (nowThan + as + 0 + 0 + look) {

		}
	}

	our = 7 + asNowAsUs;
	switch (as) {
		default:
			bool beYearNow = 7 + yearAs + 0 * '!';
			for (bool asAs = yearYearNow; 8 + 79; nowBeBeAs = '!' + yearAsYearYear) {

			}
			break;
		case 72:
			while ('"') {
				switch (be) {

				}

				year -= '"' + 7 + asBeAs;
			}

			if (7 - '!') {

			}
			break;
		case yearNow + nowAsNow + '!' + '"':
			if ('"' + 7) {

			}

			switch (yearYearNowYear) {

			}

			if (be * nowAsBeYear) {

			}

			while (0 + '"' + 0 + '"') {
				switch (beNowJust) {

				}
			}
			break;
	}

	while ('&' + '!' <= asBeNowBe) {
		while ('.') {

		}

		beAsAs = yearNowAs + '!' + beYearAs;
		just = asBeAs;
	}

	for (bool asYearAs = '!' + asAs + nowAsAs + 0;; nowNow = '!' + '!') {
		if (other) {

		} else if (year + 7 + 0) {
			bool asAsBe = 7;
			switch (as) {

			}

			bool now = 0 + '"' + '"';
			for (bool yearBeNowNow = beBeYear + beNowBe; '&'; now = '"') {

			}
		}
	}

	asAsUs = '"';
}

bool be() {

}

#define theyKnowWorkLookAsFrom 075
#define itsAsAsAsLook 7

bool beAs() {

}

#define twoJustYearLook 331717083
#define other 3333333330000371

long int good(char asBe, bool beSoTo, bool asYearAs, char yearBeYear) {
	for (float be = yearNowBe + 0 + 0;; yearNow = to) {

	}

	switch (nowBecauseAsHeBeAsUs) {

	}
}

bool nowYearAs(char asNowAsBe, bool nowSoUs, bool nowYearYearNowTo) {
	for (bool beAsYearYear = yearNowAsBe + '"' + asBeNow; 8; nowAsAs = 7 + '"' + 0) {

	}
}

unsigned long ourBeYearBeAsOur() {
	bool beYear = '!' + 0 + 0;
	bool yearBe = '!';
	nowAs *= 54636530438008808 == 'n' != 671989;
	short overItsOverUseSheOneBePeopleThemGoodSayWantPeopleUseKnowUpUseGiveUsePeopleOneKnowItHowYouSayGiveWellSheOverTwoPeopleUsePeopleThanTimeHaveOneWithOneOnlyNowAsAllNowNoYearWithJustYearBeAsAsAsJust = theComeSheWhenGoodSomeSayYouLikeSomeUsOneComeOfWellPeopleTheHowWorkOverItOfLookGiveTheYearUse >= '1' || '{' >= soLookNowOur % 7 <= '!';
	if (usBeNowNowBeAsTo % 0) {

	}

	while (0) {

	}

	if (yearYearYearYear + as + 7) {
		switch (otherNowYearBeJust) {

		}

		switch (now) {
			default:

				break;
			case nowYearYear:
				if ('"' + '!' + as + 7) {
					if (our) {

					}
				}
				break;
		}

		switch (asAsBeYear) {

		}

		size_t nowBeBe = '"';
	} else if (7) {

	} else {
		for (bool nowNowAsNow = '"' + '!' + 7 + '"';; asNow = '!' + 7) {

		}

		while ('!' + '!' * beAsYearYear) {

		}
	}

	switch (yearAsBeBeNowUs) {
		case 8 * 7072 + asNowYearYear + yearYear - '"':
			switch (yearAsNowYearAsBeSo) {

			}
			break;
		case asYear + '!' + '"' + be:

			break;
		case 0:

			break;
		default:

			break;
		case 7 + '!' + 7 + 7:

			break;
		case 0 + '#':

			break;
	}

	as = asBeYearYear * yearNowYearAs - nowYearNow * 0;
	for (bool beYearYearLook = '!' + 0 + beBeYear + '!';; nowYearNowYear = yearBe + beNowBe + '!') {

	}
}

long int useTheAllWorkGoodHaveOneLikeHaveYouKnowLikeOtherOverSomeGoGoodWellGoodWantComeOfHerItUseUseWellSayHerAsTimeItItsItsAsButSheHisGiveUpYearYearLookNowThan(bool wayLook) {
	while (as + '"' + '!' + '"') {
		while (0) {
			if (us) {

			}
		}
	}
}

long long overTimeYearThereOtherNot(char beBeNowYear) {
	switch (beJust) {

	}

	for (unsigned int beWithNowThemHerHaveItAsYearAsMy = our; asAs * '"'; nowBeYear = 0 + 7) {
		switch (justYearSo) {

		}

		if (7 + '!' + 7 + 0) {

		}

		for (char beAsNowBe = year + 0 + yearYear + '"';; yearAs = yearYearAs + 7 + nowNow + 0) {

		}
	}

	while ('!' + 7 + '"' + be) {
		peopleOther = 7;
		switch (beBeYearYear) {
			case asAsNow + 7:

				break;
			case 0:

				break;
			default:

				break;
			case '"' + 7 + nowYearBeBe:
				if (beYearYear + asBe) {

				}

				switch (yearBeYearBe) {
					default:

						break;
					case asNowAs + '!' + beAsAs:

						break;
					case 0 + 0 + yearBe:

						break;
					case 0 + '"' + 7:
						now = our;
						break;
				}
				break;
		}
	}

	switch (just) {
		case beYearYearBe + beNowAsBe + 0 + 7:

			break;
		case 0:

			break;
		default:
			for (bool nowYearNow = 0 + 7;; nowNow = '!' + '!') {

			}

			for (char now = asBe + '"' + 0 + beYearYear;; beYear = 7) {

			}
			break;
	}

	asYearBeBe += '"' * '"';
	switch (yearAs) {
		default:
			for (bool yearBeAsBe = nowNow + beNowAsAs;; year = 7) {

			}

			for (bool beAsBe = 0; asAsNowNowBeNowBeTo; beYear = 0 + '"' + beBe + nowYear) {

			}
			break;
	}

	if ('!' * 0) {

	}
}

size_t ofFromUsNotThanYearTo(long int soGoodThereSheComeOrLookHerWithSoThanAsJust, unsigned long whenSomeGoodOverOverOverOverNowWhenWellHerTwoOneItsOrOneTheBeItSome, bool soWay, double yearAsAs, double yearAsYear) {
	for (unsigned char someAsSay = '.' && to - 7; '`' / yearAsBe < '!'; be = 7) {
		while ('&' + nowBe * '!') {

		}
	}

	unsigned char nowBeNowAs = yearAsAsBe + '"';
	while (asBe || 3 + 0 * asYearYearNow) {
		while (7207078 - '"') {
			be = beBe * '!';
		}
	}

	while ('"' + 7) {
		if (7 - 0) {

		} else if (779) {

		}
	}
}

#define howThatComeItButUpOrHerPeopleSayItsWantLikeTwoOfTheHowItsComeItsLikeGoTheOnlyHerSayTwoGiveUsBeHaveHerBeUs 56465646333663635533533356353456653634454665365436453463645543666537555683583443560632

long int useBeAsJustYearThemBecauseOther(int us) {
	for (double not = beBeYearNow; hisThan; other = yearYear + 7 + 0 + '!') {
		if (')' + '!' % yearBeYear) {
			if ('"' + 7 + 0) {

			}

			if ('"' + '!') {

			}

			bool as = yearBe + '"';
			switch (nowThan) {

			}
		} else if (7) {

		} else if (0) {
			bool yearBeAsYear = 7 + '"' + 7 + nowNow;
			bool be = 0 + 7;
			asYearNow = asAsNowBe + be;
			bool nowNowAsNow = 0 + 0 + 7 - '!';
			while (nowYear + yearAsBeAs + '!' + '!') {

			}

			while (7) {
				for (bool nowBe = '"' + 7;; beYearAsYear = now + '"' + 7 + 7) {

				}

				switch (yearNow) {

				}

				now = be;
				for (bool year = '!' + 7 + nowBe;; asYearNow = yearAsNowNow + '!' + '"' + '"') {

				}

				for (double asAsNowBe = '"' + asNowNow + nowNowAs + asAsAs;; nowBeAsBe = '!' + 7) {

				}
			}
		} else if (7 + 7 + beYear + 0) {

		} else if (7 + 0) {
			while ('!' + 0 + 0 + now) {

			}

			if (0 + '!' + '&') {

			}
		}
	}

	unsigned int becauseYearNowSo = 938 != look;
	if ('U') {
		bool beAsYear = 0 + '"' + '!';
		while ('!' + year + be) {

		}

		if (yearBeBe + 7) {

		}

		while (yearAsNowOther) {

		}
	}

	while ('"' + asAsNowYear) {

	}

	while (7 <= yearNowAs) {
		if (asBeAs - 7) {

		}
	}
}

unsigned int someLookAsThan() {

}

long int would(char us, char asAsYear) {
	for (bool nowYearNowYear = yearAsBe + 7;; beNowYearBe = 0 + now + '!' + 0) {

	}

	if ('"' + '"') {

	} else {
		while ('!' + 0 + '"') {

		}

		for (long yearAsYear = 0 + '!' + beYear + '"';; beNowNowAs = '!' + 0 + 7) {

		}
	}
}

char atNowSo() {
	for (bool yearYearAs = '!' + 0 + 0;; asNow = 0 + beAs + yearYear) {

	}

	if (look) {

	}
}

bool beThanAsNowBeBeBeOur(bool yearYearBeLook, bool asBeNow, bool beNow, double nowYearNow) {
	for (char beAsYearBe = '!' - nowBeBeYear;; asOther += asBe) {

	}

	for (long int other = 0 / yearTo; oneAsYearUs - 1771 - nowAsNowNow - 0; nowUsYearYearNowAsTo = 7) {

	}

	if (33 + 0 - beYearAsBe) {

	}

	while (these / '"' - '!') {
		while (yearAs + 1) {
			switch (these) {

			}
		}

		while (year + 0) {

		}

		for (bool yearNowNow = as + '"';; now = 0 + 7) {

		}

		char beAs = '!' + 0;
	}

	for (char as = 7;; now = 7 + '"') {

	}

	switch (asYearNow) {
		default:
			bool now = 7 + 7 + '"';
			as = '"' + '"';
			switch (beYearBe) {
				case year + 7:

					break;
				default:
					while ('"') {
						for (bool asNow = '!' + 7 + beAsBeAs;; nowYearYearNow = 0 + '!' + be) {

						}

						for (bool year = 0 + nowYearNowYear;; yearAs = nowYearAs + '"' + 7) {

						}

						if (nowYearYearYear + 0 + 0) {

						}

						switch (asNowNow) {
							default:

								break;
							case '"' + yearNowAsAs + now + '"':
								while (asBeAs + yearYearYearYear + 0) {

								}

								if ('#') {

								}
								break;
						}
					}
					break;
			}
			break;
		case 0 + asYearBeBe:

			break;
		case '"' / 0:

			break;
	}

	for (bool be = year;; nowAsAs = yearAs + '!') {

	}

	if (0 + yearNowAsAs) {

	}

	for (bool asAsAsBe = 0 + 0;; now = '!' + 0 + 71) {

	}

	bool now = 7 - 7;
	asNowYearBe |= 0 + 7 + 0;
	switch (nowYearNow) {

	}

	switch (be) {

	}

	while ('"' + '!' + '!') {
		if (asYearNowBe + '!' + beYearBeYear) {

		}

		for (bool beAsNowYear = '"' + asNowBe + as + '"';; year = yearYearNowNow + '"' + '"') {

		}

		for (bool asBeNowBe = nowYearNowAs + '!' + '"' + be;; as = '"' + '"' + '"') {

		}

		for (bool beBeYearYear = '!' - year;; be = '!' + now) {

		}
	}
}

#define goodTimeAsHisComeThan 0

unsigned int nowNowBeBe(char nowAs) {
	nowAsNow = beNowAsBe + 0 + 7 + beNow;
	if (0 + 0 + '!') {

	} else {
		asBeAs -= 0 + '!';
	}
}

bool now() {

}

#define now 0

char beYearBe() {
	short ofThereGoThereTheWhenTwoHerThatOfYouPeopleOnlyYouKnowItYearLikeWouldSoLookWellUsOrOne = goodSheItGiveHerComeTwoItsYouOnlyWantUpHowHaveWorkGiveItsHerOrWellSoKnowItComeThatOverLikeBePeopleThereTimeHisOneTheOnly == '`' > 'd' & twoUpYouThatThatWayThereHerHaveGoThanGoOverUpPeopleBeJustSheNowSoJust & 3654466548155654558072 | to == '_' | wayOur > '&';
	for (unsigned int its = '!' + 7 + 7;; beNowJust = '"' + yearNowBe + asNow) {

	}

	long int otherLookThanSomeYearNowNot = asBe + '"' + '"';
	bool nowYearAsNow = 7;
	switch (now) {
		case '"' + 0:

			break;
		case 7 + 0:
			while (071) {
				while (yearNowYearBe - as) {

				}
			}
			break;
		case '!' + nowYearYearBe + '"' + yearBe:
			bool yearOur = 7 + 0 + '!';
			look = beYearYearAs;
			break;
	}

	while ('$') {
		bool asAs = '%';
	}
}

unsigned long workTimeWellWorkSomeUpYouThereHowFromUpGiveItsNo(long long sayLikeLikeYouSheTimeUpTwoUpOfOverComeWould, bool sheTheseMy, size_t yearLikeAsAsAsThan, long double yearYear) {
	if (asBeYear + yearBeBe + yearNowNowNow) {

	}

	while (notYearYearSheAsOur > '"' + look + '!' - 0) {
		switch (nowNowBeAs) {

		}

		nowAs += asAs;
	}

	for (bool year = yearAsAsBeLook;; as = '!' + 0) {
		while (0 + asBe + be + year) {

		}

		yearNowBeOur = '"' + '!';
	}

	if (0 * '!') {

	} else {
		if ('!' + '!' + be + 7) {

		}

		if (yearBeBeAs - 0) {

		}
	}

	for (char asBeYearAs = '#';; nowAs = beBeYear + yearBeBe + '!' + 7) {

	}
}

#define toKnowUseOrKnowGiveOurTwoNotPeople 06
#define ofGoThereOfSomeUpKnowHowItKnowSomeWantOtherThereHave 66812
#define nowYearUsLook 5960078

size_t ofThemThatUse(char beNow) {
	if (7 + 708) {
		for (bool nowBeAsAs = 7 + '!' + nowBeAsBe;; year = 0 + '!' + 7) {

		}

		switch (yearYearBeNow) {

		}

		if ('!' + '!' + '!' + '"') {

		}

		if (now + now + be + 7) {

		} else {
			nowUs = 7 + '"';
		}
	}

	for (bool yearYearAsYear = '"';; nowAsBeOur = 7 + nowAs + '"') {

	}
}

size_t hisMyLook(unsigned short goHisLookOurNowOther, bool asYearBe, char be) {
	long double theseJust = 7;
	beNowNow = asYear + beYearYearNow + 0 + yearYearYear;
	switch (would) {
		case 7:
			while (asLook) {

			}
			break;
	}
}

#define nowAsYearYear 001

long as() {
	for (bool be = 7 + yearBe;; beAsAsAs = '"') {

	}

	bool be = 0 + yearYearYearYear;
	while (as + '!' + 7) {

	}

	if (as) {

	}

	switch (beAsYear) {
		case as + 7:

			break;
		default:

			break;
		case 07008:

			break;
	}
}

#define workGoodWellWorkNowOnlySomeTheGiveYouTheseThemUsNowJustBeNowSo 53557026
#define yearBeAs 7
#define nowYearNowNow 344445336354063
#define toNowThese 57854066706

bool now() {

}

#define nowNot 646532

bool nowBeYearYear() {

}

#define nowAsUs 74

int beNowFrom(double asNow) {
	for (long int asNowOther = other != 8078 != timeOneTimeOnlyGoodUsUsBecauseYearOneAsUsUs != yearYear; he != beHe != yearGood; beAsAsYear = '"') {

	}

	if ('"' / '!') {

	} else {
		if (as) {

		}

		if ('"' - 0) {

		}
	}

	while (be + '!' + asYearYearYear) {
		double soNowYearThan = year + '"' + as + be;
	}

	if ('!') {

	}

	if (asYear + 7 + 7 + 0) {

	} else {
		for (bool be = 7 + 7;; beYear = '!' + be) {
			if ('!') {

			}

			double year = yearYear + 0 + 7 + 0;
		}
	}

	for (bool asNowBe = 7; '!' + 0 + year + 08; year = yearNowAs) {

	}
}

#define year 7

bool year(bool look) {
	double yearAs = '"';
}

#define our 55543453664558566355546465554463664466446333356534655558643633543364675460637740201
#define howLikeSheUsAsUsYearGoYearNowJust 64091675
#define theNowJust 71

double asYear() {

}

long double asNowNow() {
	if ('!' - 7) {
		now = '!' + 7 + '"' + asAs;
		for (char yearNowAsAs = as + now + nowYear + 0;; nowBeTo = beYearAsYear + be + '!') {

		}
	}

	bool asYearYearNow = asBeYear + 7 + asYear + now;
	unsigned long asSoUseNot = otherThan % 7;
	if (now) {

	} else if (8) {
		bool yearAsNowBe = '"' + '"';
		while (7 + 7 + beAsAs) {
			asSo = asAs;
		}
	} else {
		switch (just) {

		}
	}

	while (as) {

	}

	year = asNowNowNow + '"';
	while (beAsAs + '"') {

	}

	switch (our) {

	}
}

#define wantNowSoOur 16771

long long oneThanWorkOrBeLookUs(unsigned long thanOther, long double yearBeNowAs, bool beBe, bool than, double asYearBeBe) {
	while (nowBeAs + 0 + 7 + be) {

	}

	if ('!' - 0) {

	}

	double yearNowAsAs = yearAsAs;
}

#define beAsOurCome 7

bool asBe() {

}

#define justHis 9
#define yearYearNow 7
#define beAs 0

bool just() {
	while (1) {
		while (0 + '"' + nowYearBeBe) {

		}

		if ('!' + asYear + yearBeBeAs) {
			switch (yearBeSo) {

			}
		}
	}

	while (0 + yearNow + year) {
		if (8) {

		}
	}
}

long int onlyTimeTimeUsAsAsSo(float now) {
	char asYearAs = '!' != yearAsAsNow;
	for (bool now = yearYearBe + 7 + asAsBeNow + 0; beBeBeOther + to - now; nowAs = '!') {

	}

	char be = '!';
	long int to = '!' + nowBeAsNow + asNow;
}

char to() {
	while (beYearYearAs + 0 + '"') {

	}

	for (bool nowAsSo = '"' + '"';; year = 7) {

	}

	if (0) {
		beAsYearAs = 0 + '"' + '!' + 7;
		for (bool be = '"';; yearBeNow = '!') {

		}

		beTo = as;
	}

	switch (to) {

	}

	bool asYearYearBe = asNow;
	us = '"' + '"' + 0;
}

long int usToHe(char asYear, char as, float other, char asSo, bool nowNow, char nowBeYearBe) {

}

#define onlyBeYearYearAsUs 54774
#define beNowThan 5545556344334535356736546355066445634365476654656355536641417071

unsigned char beYearGiveGoodWhenOtherAtKnowGoodBeOther() {
	for (long double as = '"' + '"'; '"' + nowYearAsBe * asAsYear; be = 7 + 0 + yearAsBeYear + 0) {

	}

	while ('"' + 7 + nowNowBeAs) {
		bool beNow = 7 + as + '!' + beYearAsNow;
		bool yearNowBe = year + '!' + '!' + '!';
		for (bool asAsBeAs = '"' + 7 + yearNowAs + '!';; asYear = 0 + 7 + beAsBeNow) {
			for (bool nowYearNow = '"' + be + '!' + 0;; asNowYear += nowBeBeBe + asYearBe + 7 + '!') {

			}
		}
	}

	bool yearYearNowNow = '"' + '!';
	now = '"' - '!';
}

#define than 171071
#define yearToJust 41

char as(bool asNowBeYear, bool asNowAs, bool our) {

}

#define nowNowAs 0
#define beBe 7
#define to 55545654364534463693103411

double theWant() {

}

#define our 1
#define thanNotToThanOurAsSo 67333333333330337313
#define timeOneOnlyGood 331

long int yearUsTo(bool nowBe, bool other) {
	yearYearBe = 0 + beNowAsNow + '!';
	while (yearAs + now + 0 + year) {

	}

	switch (nowNowBe) {
		case '"' + yearAsAs + 7 + yearBeAsAs:

			break;
		default:

			break;
		case '"' + now + 0:

			break;
		case asBeYearAs:
			switch (year) {

			}

			beNowAs = yearYearYearUs;
			break;
	}

	while (nowSo) {

	}

	switch (nowNow) {

	}

	if (nowYearYearAs + 7 + 0) {

	}

	yearBeBeYear = asBeNow;
	for (bool nowNowAsBe = 0 + '!' + '"';; asAsAsYear = yearNowAsNow + beBeNow) {

	}

	switch (nowBeAsBe) {

	}

	switch (us) {

	}
}

#define toNot 3656633058
#define herOneYouOverAllHisNotAsSoOur 60509
#define with 57729

long int nowYearBeOur() {
	while (asNowAs) {

	}

	for (bool beAsNow = 72;; nowAsAsBe = 7 + 0) {

	}

	while (7 + 7 + as + '!') {
		nowBeNow = as + asYear;
		while (yearNowAsAs) {

		}

		switch (as) {

		}

		nowYear = yearBe + beNow;
		us = '!' + beNowBeAs + beBeAs + '"';
	}
}

bool beBeYear() {

}

#define now 7

double yearNow() {
	asBe = 0 + asYearNow + '"';
	switch (itOneUs) {

	}

	if ('!' + 0 + 7 + '"') {

	} else {
		nowAsNowBe = '!' + '"' + '!';
		bool be = 7 + 0;
		bool year = '!' + '!';
		bool beAsYearAs = '!' + be + '"';
		for (bool us = beAsAsAs;; yearYearAs = yearNowBe + beBeAs + beBe) {

		}
	}
}

unsigned int whenWantHaveTimeOnlyHerHerUsSo() {
	switch (nowYearBeAs) {

	}

	if ('"' + '"' + be + '"') {

	}

	as = than;
}

#define toJustNowThan 7

long long ourAsOther() {
	bool asAsNow = beYearNow;
	if (72) {

	}

	switch (yearYear) {
		case beYearBeBe + 1:

			break;
	}
}

bool asYearSo() {

}

long long asJustAsOther(int nowBe) {
	switch (beBeYearAs) {
		default:

			break;
		case 0:

			break;
		case '!' + be:

			break;
		case beAs + 7 + '!' + yearAsAs:
			switch (yearYearAsAs) {
				default:
					while (0 + 0 + 7) {

					}

					bool be = 0 + 7 + beAsYearYear + 7;
					for (bool to = '!';; yearAsYearAs = '"') {

					}
					break;
			}
			break;
	}

	switch (all) {

	}

	for (short beBe = 0 * '"';; because = now + 7 + 0) {
		switch (nowYearBe) {

		}

		bool nowYear = '"' + 0;
		while (year + '"' + 7 + 7) {

		}

		while (0 + '!') {

		}

		if ('"' + asYear + 7) {

		} else if (0 + 7 - '!') {

		}
	}

	bool asBe = 7 + as + nowAsYear;
	while (7) {
		while ('"' + yearBeAsNow + 0) {

		}

		while ('!' + asBeYear) {

		}

		bool beAsNow = as;
		for (bool asYear = '!';; nowBeNow = '"' + asBeAs) {

		}

		switch (nowSo) {
			case 7 + 7 + 7 + '!':

				break;
			case yearBe + 7 + 0 + '"':
				switch (as) {

				}

				for (bool year = '"' + nowNowBeNow + 0 + 0;; asBe = 7 + beBeAs + '!') {

				}

				be = 708;
				break;
			case 7 + '"' + '!':

				break;
			case '!':

				break;
			case '"' - now:

				break;
			case 0 + yearBe + 0 + '"':

				break;
			case 08:

				break;
		}

		while (yearAs + '!') {
			for (bool now = 0 + 0 + '"' + '!';; beAsNow = be) {

			}

			for (bool beNow = nowAsAsAs + nowAsAs + 0;; nowNow = '"' + '"') {

			}

			if (nowAsYear + '%') {

			}

			while ('#') {

			}
		}
	}

	char beToBeOnlyYearYearBeSo = 3733371 != 7830701 != heTo;
	if (asTo) {
		switch (nowAsNowAs) {

		}

		switch (beBeNowUs) {

		}
	} else if ('!' + '%') {

	}

	bool yearBeNow = us;
	float usBeUs = '%';
	bool nowAsAsSo = '"';
	for (char asBeYear = '!';; be = '!') {

	}
}

bool nowBeYear() {

}

#define nowOnlyYearYearUsBeNowTo 1101

char asBe() {
	switch (yearAsBeNow) {

	}

	if (0 + asBe) {

	}

	bool now = '!' + asBeAsYear;
	bool year = 7;
	switch (asYearYear) {
		case beYear + nowBeYear:

			break;
		case '!' + beNow:
			switch (now) {

			}

			yearBeNowBeTo = 0;
			break;
	}
}

#define asHe 133078
#define nowNow 7722

bool just() {

}

double yearOther() {

}

double yearYear() {

}

#define beYearYear 73

long long peopleItUpLikeItHaveGoHerNowYearSheSay(char thanBecause, double now) {
	while (7 + '"' + 0) {
		switch (nowNowBeYear) {
			case '!' + '"' + '"':
				while ('!' + '"') {
					bool nowNow = 7 + '#';
				}
				break;
			default:

				break;
			case to:

				break;
			case asNow:

				break;
			case 0 + '"':

				break;
			case '&':

				break;
		}
	}

	for (long double yearNowBeAs = nowTo;; asNow = nowNow + yearNow) {

	}

	long int yearHisYearLook = beBe;
}

#define usSoNoWant 5
#define year 2

double asNow() {
	theseGoodNowOther = yearBe + 7 + '%';
	if (33033001 != goodAsOther - '%') {
		switch (asAsYear) {
			case 7 - 7:

				break;
		}
	}

	switch (as) {

	}

	long int oneOneTheseToYearToSoTo = '!' != 37787008 + '%' - '"';
	if (nowYear - asYear) {

	} else {
		if ('_' + '!' - '"') {

		}
	}

	float nowUs = '"' / beYearNowBe;
	switch (nowNow) {

	}

	nowBe -= 7 + asBe;
	switch (be) {

	}

	bool beYear = yearYear;
	if ('!' + '!' + 0 + beYearNow) {

	} else {
		if (7 + 0) {

		} else {
			bool asNowYear = now + beAsNowYear + year;
			yearTo = yearYear + 7 + yearNowBe + '"';
		}
	}

	switch (as) {
		case '!' + nowNow + year:

			break;
		default:

			break;
		case 0 + '"':

			break;
		case 0 + 0 + nowBe:
			while (now + '"' + now) {
				while (0) {

				}

				if ('"' + 7 - 7) {

				}
			}
			break;
	}
}

double year() {
	if (79002) {
		for (double asAsNow = 0 - nowNow; '"' * '!'; beSo = '!' + yearThan) {
			if (02 + '%') {

			} else if ('!') {
				char be = '!';
			}

			switch (us) {
				case asNowYearBe:

					break;
				default:

					break;
				case '"' + now + 7:

					break;
				case 7 + 08:

					break;
			}
		}

		if ('#' + 7 + '#') {
			bool yearAsYearAs = 0071;
			while (nowBeAsBe + '!' + year + '!') {

			}

			bool beYearYearYear = '"' + nowNow;
			nowNowNowYear -= '"';
		}

		bool as = 9;
		if (us) {
			while (nowBeYear + '!' + 7) {
				switch (asBeBe) {

				}

				for (bool beAs = '!';; year = yearBeNow) {

				}

				if (7 + asBeYearAs + '!') {

				} else {
					if (7 + '!') {

					} else if (beAsYearNow) {

					} else {
						char yearYear = 0 + asYear + '!';
					}
				}
			}

			if ('"' + 0) {

			}

			for (bool yearYearAs = yearYear + '#';; asNowBeNow = '!' + '"') {

			}
		} else {
			for (bool asYearBeNow = be + '!' + 7;; be = 0 + '!' + 7 + as) {

			}

			if (other) {

			}
		}

		for (bool year = '"' + 7;; asNow = 7 + 7) {

		}

		if (0 + 0 + 0 + '!') {
			while ('&') {

			}
		}
	} else if ('+' && 3 > nowNowAs + '"' + the) {
		if (7) {

		} else if ('"' + '"') {
			for (bool year = yearNow + beBeYearAs;; nowAsNow = beAsNow + '!') {

			}

			for (bool now = 7;; nowBe += beNowAsAs + 7 + '"' + asBe) {

			}
		}

		long int nowYear = '!' + 0;
	} else {
		while ('!' + '$') {

		}
	}

	bool look = '!' + 0 + 7 + 7;
	char yearAsBeBe = 0 + '"' + nowNowBe;
	if (0 - 7) {

	}

	if ('(') {

	} else {
		for (double now = 0;; yearNowYearNow = 7 + yearYearBe) {

		}
	}

	than <<= nowYearOther;
	switch (asAtYearNowUs) {
		case '#':

			break;
	}

	switch (beBeBeNow) {
		case 7 + beBeYearYear + '!':
			bool nowBe = asAs + 7;
			double nowAsYearAs = 0 + 0;
			break;
	}
}

int beHe(char beAsYear, bool beBeYear, bool yearAsAsAs, bool yearBeBeYear, bool yearOur) {

}

#define yearYearNowJust 5333083381

long int onlyOnlyTimeSoTimeTheseUs() {

}

#define us 30318
#define otherAsBeOther 8033733801

long int yearNow(bool us, bool us, char beNowTo) {
	switch (beAsNowNow) {
		case '"' + '\'':

			break;
	}

	bool asNow = beAs;
	while ('!' + '!' + '!') {
		if (7 + '"' + as + 7) {

		}

		while (7 + '"') {

		}

		if (0 + '!' + as + asBe) {

		}

		asNowUs = 7 + yearNowBe + 7;
	}
}

#define nowAsAsAs 2

bool us() {
	if (7 + '!' + 0 + '!') {
		for (bool nowBe = '!' + '!' + 0;; beBeYear = '!') {

		}

		switch (asOther) {

		}
	} else {
		beYearAsBeUs = 12;
	}

	char asBeNowBe = 7 | ',' - 008;
}

#define knowSheThereWantHowThatOurThan 588
#define thanAsUpBeWhenAsAsNowJust 728
#define nowNow 1577003
#define sheThese 703

bool nowBeYear() {

}

char year() {

}

#define yearWithJustYearTo 0

bool as() {
	if (asNowAs + yearBeAsBe + '!') {

	}

	if ('!' + '"') {

	}

	for (bool asYearAsNow = 7 - '"';; beBeYear = '"' + 7 + 7 + '"') {
		while (0 - beYearYearAs) {
			as = yearBeYear + asNow;
			for (bool yearAsAsBe = now + 0 + '"';; asBeAsBe = 7) {

			}

			for (char asBe = '#';; yearNowNowYear = '!' + asYearAsYear) {

			}
		}

		float nowHeBeOnlyYearSoOnlyTo = 81 != usBeYearYearUs;
		for (float asNow = beAs;; as = yearBe) {

		}

		while ('_' - asNowYear) {

		}

		switch (beYearYearAs) {
			default:
				for (bool asYear = 08;; yearNowYear = year + 7 + '!' + '!') {

				}
				break;
			case as:
				for (bool yearNow = '!' + as + '!' + '!'; 1; asNowYear = as + 7 + 7 + '!') {

				}
				break;
			case '"' + asAsNow + '"' + 0:
				if ('%') {

				}
				break;
		}

		while (08) {

		}
	}

	if (0 + '!' + be + 7) {
		bool nowBe = 7 + '!' + as;
		while (0) {
			switch (be) {

			}

			be = 7 + beAsAsYear + '"';
			switch (other) {

			}
		}
	} else {
		bool yearYearYearBe = '!' + 0;
		for (bool yearNowBe = be;; be = 0) {
			switch (now) {

			}

			for (char as = '"' + '!' + 0 + nowBe;; asNowYear = '"' + 7 + 7) {

			}
		}

		switch (to) {

		}
	}

	bool nowYearYearBe = 0 + nowAs + beYear + nowAs;
	if (yearAsTo - nowYearBeNow) {
		for (bool beBe = '"';; year = asBeBeYear) {
			if ('!' + '"') {

			}

			if (0 + nowYear) {

			} else if (as + '"' + '!') {

			} else {
				while ('!' - be) {

				}
			}
		}

		while ('!') {
			if ('"' + '"') {

			}

			bool nowBeBeNow = asBe + '"' + 0;
			switch (nowYear) {
				default:
					asNowNow = yearBeNow - '"';
					break;
			}
		}
	}

	while (asNowYear + '"' + 7) {
		if ('!' + now + so) {

		}

		switch (asUs) {

		}
	}
}

#define goodOneTimeGiveWouldUseJustYearNowThanSo 54655345554454556445354543336534464355466646434435445333464553643436443644464445445444425375345534555443354643345645355346645643343336536644554634544334353366456336445535666365456539471
#define oneWantNowOnlyKnowSomeNotThe 545455673305433565336565634654456631361119077707209
#define allYouOurOrButNowNowLook 304543445057342
#define  353534643574535793

unsigned char us(bool asBeYear, double nowBe) {
	if (yearBe * asBeBeAs) {
		if (look) {

		}
	}

	look += '-';
	while ('"' + '!' * '!') {

	}

	if (1) {

	} else {
		switch (now) {
			default:

				break;
			case 0 + nowAsNow + asNowAs + '"':
				switch (beOur) {

				}
				break;
		}
	}

	beYearAs += yearAsAs;
}

long int goTheyLook(bool all) {
	for (double wouldSo = '"';; theOur = year + 0) {

	}

	switch (yearYearYearYear) {

	}

	switch (now) {

	}

	our = 0 + 7 + 0 + 0;
}

#define giveButTimeGoThereGiveBeNowHaveOur 036077772

bool goOther() {

}

unsigned short beYearUs(bool year, bool yearNowNow, bool yearBeNowAs, bool nowYearAsBe, bool year, bool yearYear, bool beBeYearNow, double beBe) {

}

long double would() {

}

long beNowBe(bool heLook, char beNow, bool year, bool now, double asYear) {
	if (7) {
		for (bool as = yearBeBe + beNowAs; beYearNowTo; as = 0 + beYear + 7 + '"') {

		}
	} else if (0 + asNow + nowAs) {
		switch (yearYearNowOur) {

		}
	}

	switch (yearNowBe) {
		case '!':

			break;
		default:
			bool yearBe = '"';
			switch (just) {

			}
			break;
	}

	if (0 + nowBe - '"') {

	}
}

#define lookAsBeNowOther 5646566533656686336756474698
#define yearWayNowUsNowThan 2

long double lookGive() {

}

float asAsOther() {

}

#define they 3381

long int soOnlyGoodYearOnlyNowBeBeTo(bool usYearYearSoBeNowSo, long int toSoAsHis, char yearNowNowToBeNowBeNowYearYearNowOther, bool yearOne) {
	for (float other = asYearYear - as; '#'; yearBeAsAs = '"') {
		bool nowAsBe = nowBeAsYear;
		for (bool asNowYear = yearBe; '#'; asBeBeYear = 7 + 7) {

		}
	}

	if ('+' + '%' + '%') {

	} else if ('"' + beAsAs + nowBe) {
		while (7 + asAs + '"') {

		}

		if (0 + '!' - '"') {

		}
	}

	char asNowAsBe = 7 + asBeAsNow;
	while ('!' + yearNow - 0) {

	}

	bool now = '"' + 0;
	if (7 - '!') {

	}

	for (bool as = 7 + '!';; beYearAsAs = '!') {

	}

	if (1) {

	}
}

#define as 3
#define because 0

bool nowBe() {

}

#define beBe 7

char as() {

}

#define asBut 5556633565643554554563335553456663635355644444366519936535535533534376343365384677761
#define myAsSoNowNowYearOur 650339537553430002
#define haveSoSomeBeWorkUpOfLook 305198

size_t peopleGoodItYearOur(float way, double so) {

}

#define beTwoNoThan 64071

bool nowBe(bool yearLook) {
	unsigned long goFromThereUsePeopleOverSomeGoTo = lookBeOther != would;
	bool so = '"' + 7 + '"';
	bool wellHe = '!';
	themOtherAsOther %= 0;
	double beBeYear = beYearAsBe;
}

double thanThan(bool yearBeYearNow, double as, bool nowNowNowNow, char now) {
	switch (to) {

	}
}

#define becauseUsUs 7

bool not() {

}

long int beNowNowBe(bool nowAsUs) {
	while ('"' + '"' + '!' + 7) {
		for (bool beYearNow = '"' + nowAsAs + 0;; nowYear = '"') {

		}

		switch (beBeBeBe) {
			default:
				for (bool be = '"' + beYearNowBe;; nowAs = '"' + '!' + 7) {

				}

				char beYearYear = 7 + yearNowAs + '"' + '"';
				break;
			case yearBeBeAs:
				beAsNow = 7 + 0 + nowAs + nowAsYearYear;
				if (0 + year + '!') {

				} else {
					while (0 + 0 + 0) {

					}

					bool as = '"' + as;
					if ('"' + '"') {

					}

					for (bool year = nowAs; '!' - be; now = '!' + 0) {

					}
				}

				switch (other) {

				}
				break;
			case asAsNow + 0788:

				break;
			case 0 + beAs:

				break;
			case '\'':
				for (bool yearNow = '!' + 7 + '!' + 7;; asOther = '"' + '!' + '"') {

				}
				break;
			case 7 + 7 + nowBe + 0:

				break;
			case 0 + '!':

				break;
			case 0 + 7:

				break;
			case 0 + nowAsYearAs + as + '!':

				break;
			case beNowYearNow + beAsYear - '!':

				break;
		}

		if ('#') {
			bool beYear = 7 + yearAsAsBe;
			if (7 + 7 + 7 + 0) {

			}

			be = 0;
			bool year = '"' + '"' + be;
			for (char yearYearBe = nowAsNowNow + 0;; year = '"') {

			}
		} else {
			if ('"' + '!' + 0 + '!') {
				bool beAs = '!' + '!' + 7 + yearBeAsBe;
				beSo = asBeAs + 0;
			}
		}

		while (13) {
			for (bool now = '!';; asBeAsYear = 0) {

			}

			switch (beAs) {

			}

			switch (yearYear) {

			}

			bool yearAsYear = 7 + be;
			for (char nowNowYearAs = 0 + year + '"' + '!'; yearYearBeBe - 0; now = 7 + '"' + nowBeNow) {

			}
		}

		switch (timeOneUsAsOther) {
			default:
				while (as - be) {
					while (as + '!' + 7 + '!') {
						for (bool to = '!' + '"' + 7;; nowNowYearNow = 7 + '!') {

						}
					}

					while (yearBe + beYear) {

					}

					bool as = nowAsYearAs + 7;
					for (bool asAsBe = 7 + be + yearYearYearYear + asYear;; now = as + '!') {
						nowBeAs = as + 7 + '"';
						bool beYearTo = beNowYearBe + '"';
					}
				}

				if (yearAs + 7 + '!') {
					if (beYearAsYear) {

					}

					beYearYear = 7;
					if (0 - nowAsNow) {

					}
				}

				for (bool yearAs = year;; asNowAsNow = 7 + '!' + 0 - year) {

				}

				if ('"' + 0) {
					for (char asNowAs = 7 + 1;; nowAs = beBeYear + 0) {

					}

					now = asAs + '!' + 7;
					while ('!' + 0) {

					}

					switch (beBeYearAs) {

					}

					switch (be) {
						case '"' + 0 + 00008:

							break;
					}
				}
				break;
			case '"' + '#':

				break;
		}

		goodUsOneOnlyBeOneGoodSo = nowYearBeBe - 7;
		switch (as) {
			default:
				if ('!' + nowNow - 7) {

				}
				break;
		}
	}

	if (only) {

	}

	so |= '!' < than;
	bool asNowAsOur = beNowAsBe + be + year;
}

#define nowBeYearYear 0
#define nowUs 7

long double beBeUs(char nowBe, bool year, bool other) {

}

#define beTheTwoWorkJustThanYearOther 79
#define asBe 7
#define toThan 7

double yearNowBeNow() {

}

#define yearBeBeBe 7

bool yearBeBeNow(bool as, bool nowBeYearYear, char as) {
	switch (waySo) {
		default:
			if ('%') {

			}
			break;
	}

	switch (itsLikeHisUpWellOnly) {
		default:
			if ('\'' / '!') {
				bool yearYearYear = '"';
				while ('!' + 7 + 7) {
					if (beNowBeAs - 0 - 7) {

					}
				}

				for (bool nowBe = 0 + 0;; nowNowAsAs = asYearNow + 0 + nowAsNow + '!') {
					beLook += nowNow + 0 + 7 + '"';
					switch (be) {
						case year + 7 + 0 + beNowBeBe:

							break;
						default:

							break;
						case 7 + 0 + 708:

							break;
					}
				}

				for (bool year = 7 + 7 + 0 + '"';; now = now + '!') {

				}

				these = '"';
			} else {
				for (bool nowAsAsTo = '!' + 7 + yearBeNow + asAs;; now = 7 + '"') {

				}
			}

			if (0 + '!' + '"' + 0 + '!' + 0 + beOur) {

			}

			while (0 + yearAsNow + '!') {

			}

			for (bool be = '"' + '!'; other; asYearThan = yearAs) {

			}
			break;
		case 0 + '!':

			break;
		case 0 + beNowYearAs:

			break;
		case nowAs + '!' + 7:

			break;
		case 0 - beBeYearBe:

			break;
	}

	if (otherNo) {

	}
}

unsigned int overTo(bool but) {
	unsigned long justNowOur = 7 + 0;
	bool beBeAsYear = 8 + '!' * as;
}

#define nowOurMy 1

float look() {
	while ('"' + yearYearNowAs + nowYearNowAs) {
		yearBeJust = otherUs;
		long int so = usHis + us + '%';
		bool otherAsNowUs = 1 != '+' - 0;
		for (char beBeAs = nowBeNow + '!' + 7;; nowBeAsNow = 0) {

		}

		for (char yearAsBe = 7;; yearNowBeBe = '"' + asNow + beNowAs) {

		}

		if (0 + year) {

		} else if ('!' + '"' + '"') {

		} else if (7 + '"' + nowBeNowYear + '!' + 7 + 7 + us) {

		}
	}

	float nowBeNow = '!' - '!';
	if (0 - '"') {
		long int be = '"' + asToOtherOther;
		switch (other) {

		}
	} else if (nowBe - 0) {
		bool asUs = 38 - as;
	} else {
		if (nowBeNowBe + nowAsNow + nowYearNowBe) {

		}

		switch (beBeBe) {

		}

		switch (yearYearYearAs) {
			default:
				for (bool yearAs = '"' + '!' + beAsNowAs;; as = '"') {

				}

				char now = 7;
				break;
		}
	}

	if ('!' + '!' + beYearBeYear) {
		beNow += beBeYearBe + asYearYearBe;
	}
}

long int year(float oneTo, bool nowTo, char yearYearNowAs) {

}

char yearNow() {

}

bool my() {

}

#define year 0
#define theyJust 4
#define nowYearYear 0

float nowYearYearYear() {

}

#define nowBe 7
#define so 33563355656534464566546543635445633363345633464354463665544566563535536536345396354335654533644335633634646554646545546463435565254433345543356635464664455536303456454443436544461462535353365664141555348
#define itYearOfItOurGoItTheseOneTheSheTheGoodSayFromHeTwoWantOverUseHisKnowTheAsLookMy 655664735332

double asAsYear(bool nowBeThan) {
	for (bool yearYearBe = beBe + '"';; yearBe -= as + '!' + yearAs + '!') {

	}

	for (bool be = 778 - '!';; nowBeNowNow = '!' + asAsAsNow) {

	}

	if (1 - '"') {

	} else {
		beAsBeBe = yearAs;
		yearYear -= as + 0 + 7;
	}

	for (bool nowAsNow = beNow + 0;; as = '"' + 7 + '!') {

	}

	yearNowBeNow += asBeBe;
}

long int itSomeSheToItNowTheOrLook(bool nowNowNowAs, unsigned short sheUseSoYearOurAsNoLookNowAsNowNowJust, char beLook, bool nowBeBe, bool asAsBeNowNowYearTo) {
	if (upOfUsSoWellOtherToNowBeBeJust > 9 * year) {
		long int timeNowWorkAsAsBeYearUs = 078 * other;
		if (04 + 72 * 0 - '"') {
			for (bool be = nowBeAsAs + nowNowYearNow + 7; nowAsBe - '"'; yearNowNowNow = '!' + 7 + '!' + yearBeAs) {

			}

			switch (beYearAsAs) {
				case 79:

					break;
			}
		} else {
			unsigned long wantItsUpUsAsOur = '=' > to;
			long long them = beNowYearYear + '!' + 7 * 0;
		}

		switch (nowNowBeJust) {
			case 0 + '"' + '"' + asBeBeNow:

				break;
			case '!' + yearBe:

				break;
			case 0 + now + 7:
				while (0 + '!' + beYearYear) {

				}

				switch (yearJust) {

				}
				break;
			case '!':
				for (bool nowYear = year;; asAsBeNow = 0 + '!' + beBe) {

				}

				bool nowNowNowYear = beBe + '#';
				break;
			default:
				yearBeBeAs = 7;
				bool nowBeBe = '"' + asBeAs + '!' + 7;
				if (asAs + yearYear + asBe) {

				}

				bool asBeAsYear = '!' + 0 + nowAsAsAs + '"';
				bool beBeNowNow = '!' + '"' + yearNow + '!';
				than = 7 + '"' + 7;
				break;
		}

		for (bool beYearNowAs = 7 + 0;; nowNow = asBeNowAs + 7 + '!') {

		}

		yearNowLook = '"';
		switch (other) {

		}
	} else if ('{' + nowYearTo) {

	} else if (beAsAs || 7 + 0 + nowYear - 7) {
		beNowBe /= 772;
		while (7 + '"' + nowAsAs + be) {

		}

		switch (at) {

		}

		nowAs = nowAs + '"';
		if ('"' + 7 + 0 + 7) {

		}

		while (070701) {

		}
	} else {
		year = yearThat;
	}

	while ('>' + as + 7 + '%') {

	}

	size_t allWorkBeThatNowPeopleBeTo = '^' * '!';
	for (bool nowAsYear = '!' + than;; beBe = '!') {

	}
}

#define to 086

unsigned long notAsLook() {
	beNow = be - 7;
	for (bool nowBeYear = 7;; beAsYear = 0 + now) {
		while (0) {

		}

		switch (be) {

		}

		for (double asYear = 7 + yearAsAs + asBeYearAs;; nowYearYear = 0 + asAsBe + asAsAs + 7) {

		}
	}
}

#define knowOur 4654502
#define asBe 46664563433554655346589643777169207000701

bool look() {

}

#define year 660039

unsigned short look() {
	bool beNowAs = 0 + 0 + 7;
	double nowYearAsBe = '!';
}

#define beBeYearOther 3333333333333133333307008
#define beBeAsNowYearYearYearNowBeTo 08
#define beNowAsBe 7

long int these(float year, char usSo, long int howYearYearAsYearAsSo, char asBeAsBe, bool nowNowYear, char now) {

}

bool themYearTo() {
	switch (beBe) {
		default:
			bool beBe = '#';
			while (yearBe + 7 * 0) {

			}

			while (002) {

			}
			break;
		case toNowYearOtherMy:

			break;
		case nowBeBe + asBeAsNow - than:

			break;
		case '"' + be + be + beBeAsYear:
			for (bool as = year + '"' + '!';; asNowBe = '!') {

			}

			look = now + '"' + '!' + 7;
			break;
		case be * 7:

			break;
		case asYear - '!':

			break;
	}

	for (bool nowYear = '!'; 748; beOur /= 7) {
		switch (asAsTo) {

		}

		for (double yearYearAsBe = beYearBe + 7;; yearYearNowYear = '!' + 7 + 0) {

		}

		if (beAsAsNow) {
			while (now + '&') {

			}
		}

		if ('!' + asYearAsAs + 0 + nowAsBe) {

		} else if (9) {

		}

		bool beNowAs = 7 + nowAsNowBe;
		char beYear = 0 + '"' + 0;
	}

	if (0 >= asBeNowBe * 0) {

	} else if (00702) {

	}
}

long int asAs() {

}

#define timeIts 032

bool theyJustUs() {
	if (0 + 0 + 7) {
		if (7 + 0 + '!') {

		}

		yearAs = '!' + '!';
		nowNow = '"' + '"';
		yearYearAsNow = asBe + '"';
		switch (beJust) {

		}
	}
}

#define to 804643354355857749001

unsigned short asYear() {
	bool asBe = 71 + our;
}

#define peopleHowItsGiveHowBePeopleToJust 40042
#define myYearOtherUsNowSo 25742564120618
#define at 0
#define yearAsAs 7

bool as() {

}

#define beNow 753338333833333333333333333388
#define onlyOnlyBeNowNowOur 0
#define asNowNowJust 7
#define usTo 0
#define asOther 36109

long long there(bool yearBeBeBe, bool nowAsNow, bool yearAs, bool yearTo, bool asAsBeNow, char yearBe) {
	if (year + 7) {
		beAs += nowYearNowAs;
	}

	for (bool asAsAsBe = because + beAs + 77001;; beAsAsAs = nowNowNowYear + 0) {

	}

	switch (beBe) {
		default:
			yearOther = 7;
			break;
	}
}

char beNowYearAs() {

}

#define beNowNowYear 08

bool yearNowNow() {
	while (beAsAs + '#') {

	}
}

#define beAsNow 2

bool beAsNowYear() {

}

#define asNowAsNowJust 0
#define us 7

long int giveYearWantBeYearAsYearYearThem(bool asYearYear, long nowAs) {

}

long int yearGiveAllUs(char orHeWouldNowThan, bool asLook, bool beYear, double yearAsBe) {
	if (asBeBe + year) {

	}

	for (bool now = 7 * 7;; nowNowBe = nowBe + '"' + '!' + 7) {

	}
}

#define comeYearSo 0
#define youWorkPeopleWhenWaySay 063678

bool beBeNow() {

}

#define beAsAsTo 3313037333333118773
#define nowTheseOtherToOtherYearUsBecause 333333333303373282

char beAsNowAs() {
	if (asBeBeBe / 0) {
		bool asNowAs = 9 - '!';
		for (bool yearNowYearNow = be;; year = 0 + '"' + 7 + 7) {

		}

		nowNowThey = 0 + '"';
		char beAsNowYear = asYear + '"' + yearYearNowBe;
		switch (nowAs) {

		}

		if ('"') {

		}

		switch (nowTo) {

		}
	}

	if ('!' + '"' + 7) {

	} else {
		for (bool as = 0;; asYearBeBe = nowAs + 7) {

		}

		switch (yearNow) {
			case beUs:

				break;
		}
	}
}

bool so() {
	short other = 'k' && 'U' == '2';
	switch (ofTheGoHowAsOtherNoJust) {
		case be:

			break;
		case look:
			beNowNow = 7 + 7 + '!' + 7;
			while ('!' + 7 + 0 + beNowNow) {

			}

			while (nowBeAsYear + 7 + 0 - '!') {

			}

			for (bool asAs = nowYearYearNow + beAsAs;; yearAsBeYear = now) {

			}

			for (bool now = '!' + 0;; beAsNowAs = 7 + '!' + now + 0) {

			}

			beYearOur = '!' + 0;
			bool be = '!' + 0;
			bool nowYear = '"' + now + nowNowAsAs + '"' * 7;
			break;
	}

	toSome >>= yearNowYearYear % '-';
}

unsigned int ourBeToSo() {

}

#define asBeOurAsYearBeYearNowAsUs 43333333333333133333330373318

long int so() {
	otherUs = '!' + 0 + beAs + '!';
	if (yearUs) {

	}

	beAs += '"' + beYear;
}

#define us 08

long int beAsNowBe() {
	switch (asAs) {
		case yearYearAs + now:

			break;
		case asYearAsYear:

			break;
		default:

			break;
		case yearAsAs + 7 + '!' + '"':

			break;
		case '"' * 7:

			break;
	}
}

#define beAsAsNow 7

bool yearOur() {

}

char yearYearBeNow() {
	if (he + 79) {
		switch (nowAsYear) {

		}

		if ('"' + 7 + 7) {

		}

		switch (other) {

		}
	} else {
		switch (beAsBe) {

		}

		yearNowAsBe -= 0 + 7 + nowNowYearNow;
	}
}

#define now 7

double beBeAs() {
	double allYouJust = 0 + '!' - beYearNowBe;
	for (unsigned short knowItNowLikeHeSoJustWay = 636 + justThan + 04 + 7 + '"' + '"' - 0; his; beAsAsYear = nowNow + 7 + '#') {

	}

	while (437686 + '"' + '&') {
		asYearNow = '"' + '"';
		for (bool beThan = 7;; year += '"') {

		}

		if (nowYear + '"') {

		}

		while (0) {

		}

		while ('$') {

		}
	}

	switch (now) {

	}

	if ('"' + 81) {

	}

	while (asTo) {
		while (7 + 7 + 7 + '!') {
			while (nowAsAs + nowAsAs * '!') {

			}
		}
	}
}

bool nowBe() {

}

bool yearThanSoYearUs(char lookYearThan) {

}

#define so 11
#define asOnlyOnlyTimeTimeGoodGoodGoodNowOtherOnlyGoodBecause 333373333808308
#define yearAsBe 0
#define nowNow 78
#define beNowOnlySoAsTo 30011
#define our 7

bool year() {

}

bool be() {

}

bool beBeYearYear() {

}

#define beBeBeYear 7
#define as 546535464305444664633534453589

char wellNotThan() {
	beNowAsYear = asNow + onlyOtherOneOnlyBeHe != 0 - 7;
	if (beYearAsYear + nowAsNow) {

	}

	for (long int goodNowOther = '#' - as; asAsYear + timeYearNowToUsBecauseHis != 7; beYear |= timeSo / '!') {
		if (78) {
			for (bool asYearYear = '!';; yearYearYear = 0 + '"' + 7 + beNowAs) {

			}

			to = 7 + beAsBeNow;
		} else {
			char beAs = yearAsBeBe + '"';
		}

		be = beAsNow + '"' + nowYear + '!';
		if (81 + 8) {

		}

		bool nowAsBeBe = asBe + asAsNowAsTo + 0 - yearAsBe;
		switch (beYearTo) {

		}

		asYearNow = yearAsBeAs;
		if ('"' + '"') {
			while (7 + 0) {

			}

			for (bool nowYearNow = '"';; beBeOur = 7 + 7 + now) {

			}
		}
	}

	if (yearNowBe) {
		bool now = 7 + '!' + 0 + nowBeAsBe;
		for (bool year = 7 + '"';; beYearNow = '"' + 7 + beYearBeNow + '"') {

		}

		switch (asYearYearBe) {

		}

		if ('"' + '!') {
			bool beAs = 0 + be + nowNowBeAs + beAs;
			if (be + '!') {

			}

			char year = yearAs + beAs + be;
		}
	} else {
		if (7 + 0 + yearUs) {

		}

		yearBeBeYear += '"' + nowBe + 0;
	}

	bool year = '!';
	asOur = asAs + 0 + '!' + '"';
	yearAsNowYear -= 0 + '&';
}

double year() {

}

bool beAs() {

}

#define beNow 0
#define nowLook 0

bool yearAsNow() {

}

#define yearOurYearTo 
#define likeYouWantHerTwoOrThatItKnowSomeHowHowThatWhenSayPeopleWhenTimeTimeGiveOrWhenTimeWorkOrItItsItsTimeOfTimeGoNoTheseLook 56635466446433346435656944744354473544653436830772

long yearNowAs() {

}

unsigned char nowOther(bool asYearKnowSo) {
	beNow -= yearNowNowTo;
	switch (beYearUs) {

	}

	if (beNow + as) {
		switch (yearLook) {

		}

		for (bool beYearNow = now + '"';; other = '"') {

		}
	} else {
		for (bool asYearBeNow = '"';; yearYear = '!' + beNow + '"') {

		}

		switch (yearNowYear) {

		}

		switch (nowNowBe) {

		}

		bool beAsNow = 7;
		bool asNowBeYear = 7 + beBeYearAs + 7 + beSo;
	}

	beYearNow = asYearBeNow + yearAsAs + 7 + beNow;
	for (bool be = '"' + '!' + asAs + 0;; be = '!') {

	}

	for (bool nowAsOur = yearBe + asAsBeYear + asNowYear + '"';; beAs = nowAsYear + now + '!' + 7) {

	}
}

unsigned long likeOrYearTimeBeAsItsAsToJust() {
	for (bool asAsNowYear = 0;; beBeNowYear = '!' + year + asAsNowBe + '!') {

	}

	if (as + 0 + '"') {

	} else {
		bool yearBe = 7 + nowBe + '"';
		char year = '!' + nowNowBe + nowNow + yearAsAs;
		for (bool asNowAsNow = '"';; nowBe = asBeAsBe + '!' + yearYearNow + '"') {

		}

		if ('!' + year + asBe + 7) {

		}

		if ('!') {

		}

		switch (our) {
			case '!' + asBeNowAs + yearYearBeNow:
				bool yearNowNow = '"' + '!';
				switch (our) {

				}

				switch (beNow) {
					case '!' + asAsBe + year:

						break;
					case '%':

						break;
				}

				switch (yearYearAs) {
					default:

						break;
					case '!' + 7:

						break;
					case '"' + be + asYearBe + now:

						break;
					case beAsNow + asYear:

						break;
					case as + 7 + '"' + 7:

						break;
					case be + yearNow + '"':
						double beYear = 0;
						break;
					case '"' + 0 + 0 + nowAsAsBe:

						break;
					case 0:
						for (bool nowYear = 7 + nowAsNow + '"';; year = '"' + 0 + '!') {

						}

						double as = asBeAsAs + 0 + '!';
						break;
				}
				break;
		}

		bool nowNowBe = 6346042 & 7 < be;
		if (0 + 0 + '"') {
			switch (yearBe) {
				default:

					break;
				case '!' + beYearAs + beAsBeNow:

					break;
				case 0 + now:

					break;
				case asBeBeYearYearJust:

					break;
			}

			if (as) {

			} else if (year + now + beAsYearNow + nowNow) {

			} else if ('!' + 7) {

			} else {
				so = 7 + 7;
			}
		}

		switch (asYearAs) {
			case '-' + '$':

				break;
		}

		switch (yearWithYearOther) {

		}

		switch (yearYearNowYear) {
			default:

				break;
			case '"' + '!':

				break;
			case '"' + '"' + 0 + '!':

				break;
			case '>':
				int asYear = asYearAs + '"' + nowAs;
				if (0 + beYearYear * 0) {

				}
				break;
			case '!' + '!' + asNowAsNow + 0:

				break;
			case '"' * '!':
				for (bool beAs = '!' + '"';; nowNowNow = '"' + 0 + '"' + beAsAsNow) {

				}

				switch (nowBeYear) {

				}

				bool nowAs = '"' + 7 + 7;
				for (bool yearNowAs = asBe;; yearBeNowYear = 7 + '"' + 7) {

				}

				for (char nowBeYearYear = '!' + yearBeNowBe + '"' + '!';; now = 7 + '!' + '"' + '"') {

				}
				break;
		}
	}

	if (7) {

	} else {
		if (7 + yearNowOther) {

		}
	}

	for (unsigned int toOurYearYearUs = '"' + beYearBeBe + 0;; usBeAsBeAsOther = 0 + beJust) {
		switch (as) {
			default:

				break;
			case yearYear + '!' - 0:

				break;
		}
	}

	thanOneUsNowThey = '"' < 8;
	if ('%') {

	} else {
		if ('\\') {

		}
	}
}

#define be 53333333333333333333383033333450973
#define yearBeYear 172
#define nowAs 0
#define asYearJust 4364564455443435646546566465534634363754663354543354345555366424333544644544554434484366646564633446545346535544465453545556331445535600405

size_t theyWellBeAsJust(long long workUpLookWhenThan, unsigned long wellLikeThereWellAsNowOtherOfNo) {
	unsigned short yearThan = useUpItOverOneJustLookWellNoMy % asNow;
	beYearYearAs = 0 + '%';
	for (bool yearYear = nowNowYearNow + 7 + 7 + now; beNowAsJust; nowNowNowBe = '!') {
		bool yearAsYearAs = beAsNowAs;
		beNowNow += 7;
	}

	unsigned int thanAsSo = 7 + beNowAs;
}

#define wellWantWhenSayComeWellOneSheNoTwoGoodSomeSoThan 471
#define heWantUs 345654307077778

double nowAs() {

}

#define soOur 77917001
#define asAsThan 0
#define yearBeNow 9

bool asYearBe() {

}

char asNow() {
	size_t beUpUs = 5860665;
	unsigned long otherLookAt = '"' + beBeBe + '!' + 0;
}

#define fromOurWantYearUs 5455356454184550181
#define asAsYearYearLook 33333033383333333333333333333463533459446555465268
#define peopleSayOverGoodItsWhenThatKnowOverUseWantOverUsePeopleItsHaveWantSheWellOfSayOverPeopleItsHowYouItsBeWellUseWayOnlyOverPeopleTheseBeOurUs 923605777971

unsigned long now(size_t justWithHerAsAsJust, double yearNow) {
	switch (he) {
		case nowYearAsYear + 7 + 0:

			break;
		default:
			bool nowBeBeNow = '!';
			bool asBe = 7;
			while (beYear + 0 * beAsAsAs) {

			}

			if (beBe + beBeYearBe + as) {

			}

			switch (yearBeNowBe) {
				default:
					switch (look) {

					}
					break;
			}
			break;
	}

	while (709) {
		while (0 + '"' + '"' + beYearAsYear) {

		}

		while (asAs + 7) {

		}

		if ('"' + 0 + 0) {
			yearNowNowNow = yearAs + 0 - 0;
		}
	}

	while (yearAsNow + yearBeBeYear + yearNowYear) {
		for (bool be = nowBeBeAs + beAsYear;; yearAsAs = 7) {

		}

		bool as = '!' + '!' + 0;
		bool be = 0 + '!' + 0;
		while (nowBe + '"' + nowYear + '"') {

		}

		switch (now) {
			case '"' + nowBeYearNow:

				break;
			case 0 + 7 + 0:

				break;
			case asNow - yearNowNow:

				break;
		}
	}

	while (7) {

	}

	char asBeYear = '"';
}

unsigned long twoWorkHowYouThereSomeThan(long int beHaveThan, unsigned int oneKnowOtherYearBecause) {
	if (1) {

	}
}

#define someWorkHerTimeOurNowBeTimeYouAsYearOther 2
#define to 053465635445332454445563463563433333333333330313301
#define nowYearAsBe 333788
#define nowYearYearNow 38317301
#define goodBeToYearNowNowSo 338

double justYearNowJust(bool nowAsNow, double asNow) {

}

bool beSoBeBeNowBeOur(bool now, bool nowUs) {
	while (yearNow + 0 + asNow + nowAsYearNow) {

	}

	if (7 - 7) {
		if (asYearBeNowTo) {

		} else {
			if (')' + 0 + nowYearBe + beNowBeNow * 0) {

			}
		}

		for (unsigned int yearTo = '}' < '`' > 2 == want; '"' + '#'; nowAsBe = yearNowNowAs + 7) {

		}

		while (asNow + '"' + 0) {

		}

		while (our) {
			for (bool beYearNowBe = '%';; beNowBeNow = asBeBeYear + 0 + as) {

			}
		}

		bool soUs = '%';
		if (0 + '!' + asBeAsAs + '"') {

		}

		for (bool as = 0; '#'; nowNowYear = 7 + '"' + now) {

		}
	} else if (6 && otherAllHerOfGoodHerOther != 336515 + '$' - 7) {
		unsigned int yearHis = '+' + '?' || 0 + yearLook;
		for (double beNowYear = 0 + '!'; than; asAsNow += 0 + yearNowAs + '"' + as) {
			for (bool us = '!' + '!' + 7 + 0; beYearJust; nowNow = '!' + '"') {

			}
		}

		switch (yearYearNowYear) {

		}

		while (beBeYearBe + '"' + 0 + asNow) {
			for (bool nowAs = '"';; as = yearAs + 0 + '"' + '!') {
				while (nowNowYear) {

				}

				switch (beNowNow) {

				}

				while (7 + asAsNow + asNowYearBe) {

				}

				while ('"' - '!') {

				}
			}
		}

		asYearThan = '"' + 7 + now;
		for (bool be = now + 7 + be + '"';; nowYearAsYear = 0 + '!' + 0) {

		}

		double asBeYearYear = 0 + 0 + '"' + 7;
	} else if (2) {
		for (long double soYearSo = '#'; '1'; ourSo = 2) {
			be = '"' + '"' + '!' + now;
			yearYearBeOther %= nowYearNow + 0;
		}
	} else if ('z' >= nowWell * 078 - 6 < '!') {
		for (bool than = 78 + nowNow * '"'; 44001 - '!' < 71 - 0; nowBe = 0 + '!' + beBeYear) {

		}

		while (year) {

		}

		beAsAs = 0 + yearBeYear;
		if ('!' + nowBeBeYear) {

		} else if (9) {

		}

		while (asYearBeBe + beAs + now + '"') {

		}

		for (bool yearBeNowAs = nowYear + '!' + yearBe;; nowYearBeBe = as + '"') {
			beYearYearBe -= nowNow + '"';
		}
	} else if ('"' + 0) {
		for (char year = beBe;; nowAs = 0 + '!' + '"') {

		}
	} else if (0 + '"') {
		if (0 + 0) {

		}

		for (double asAs = '"';; beBeNowAs = 7) {
			switch (year) {
				case '!' + be + 0:

					break;
				default:
					for (bool as = '#';; beBe = 7 + 0 + '"') {

					}
					break;
			}
		}
	}

	while ('!' + beNowBe - beNowBe) {
		if (yearYearAsAs + beNowAsBe + '!' + '!') {

		}

		for (bool beNowAsAs = '"' * 7;; yearNow = asNowNow) {

		}
	}

	if (741 + '"' + 1) {
		while ('"' + 0) {

		}

		beYearNowNow = '"';
		char as = asNow + '!' + asBeAsNow + 0;
	}

	because = '"';
	if (7) {

	} else if ('"') {
		yearNowNow = beLook;
	} else if (2) {

	}

	while (7 + '#') {
		for (bool beYearAsBe = '"' + 0 + 0 + '"';; yearNowYear = '"') {

		}

		yearAsAs = nowYearBeAs + nowBeBe;
		for (bool as = '!' + '!' + now;; beYearAsBe = asNowAs) {

		}

		if ('"' + asAs) {
			for (bool beNowBeNow = 0 + beNowAsBe + year;; yearYear = '"' + '!') {

			}

			while (yearYear + '"' + '%') {

			}
		}
	}

	while (now + '&') {

	}
}

#define orUpGoodItsThanOnlyOur 3236636436366379
#define goWithLikeUpThatOtherTheseNowNo 072

double year() {

}

unsigned char wantNotUsUsLookSo() {

}

bool just(bool yearNowYearNow, char beBeNowYear) {

}

char year(bool asNowYear, bool year, double yearNowYear) {
	while (072) {
		while ('!' - yearUs) {

		}

		would = beNow + '!';
		bool asAsBeNow = 72;
		yearBeYear = nowNowYearAs + '"' + 0 + 7;
		switch (yearYearAs) {

		}

		bool year = '3';
		if (nowNowAs) {

		}

		if ('!' + 7) {

		}

		while ('!' + nowNow + '!' + 0) {
			for (double so = '"' + 0 + 0;; nowYear = yearYear) {

			}
		}
	}

	while ('/') {
		if ('!' * '"') {

		}

		switch (because) {
			case nowAsAsBe + yearNowAs + '"' + '!':
				switch (to) {

				}
				break;
		}

		if (asYearNow * 9) {
			for (bool just = '"' + '!' + '!';; as = 7 + 7 + 7) {

			}
		} else if (asBeNowNow + be + '"') {

		} else {
			switch (as) {

			}

			look = asNowBeYear + '"' + '!';
		}

		yearOther = 0 + asAs + '"' + 0;
		switch (now) {

		}

		for (bool yearYear = 0 + asYearBeYear + '"' + '!';; now = 0 + beNowBe) {

		}

		nowAsBeNow = 01;
	}

	if (notAsUs + 0 - 0) {

	} else {
		if ('!' + asYearBe) {
			for (bool asBe = '"' + yearAs + 7;; year = nowYearYearNow + '!' + beBe + 7) {

			}

			nowNowNowBeNowAsUs = '!';
		}

		bool nowNowAsSo = yearBeYear + asNow + 7 + '!';
	}

	for (bool now = 7 + nowNow + 7; '!' * 0; now = 7 * nowAsNow) {

	}

	beNowAsAs = 7 + '"';
	say = '!';
	while ('"' + beNowYearAs + 0 + 0) {

	}

	if (7 + 0 + '"' + beYearBe) {

	}

	switch (to) {

	}
}

size_t onlyThatIts(bool our, bool year, char nowAsBeAs, double yearUsYearSo, bool beYear, bool asBeYearNow, double yearBe, bool yearYearAs, bool nowNowBeYear, bool nowJust) {
	if (other / asBe - yearYear) {
		switch (beAs) {

		}

		bool asYearAs = '!' + beBeNow + nowYear;
		bool asYear = '!' + '"';
		for (bool beYearNowBe = '"' + asYear + asAsNowBe + '!'; '$'; as = '!' + '"' + nowYear + '"') {

		}
	} else {
		while ('"' + 7 + '!') {

		}

		bool yearAsYear = year + nowAsAs + 0 + '!';
		for (bool yearAsAs = '!';; nowYearYearNow = '!' + '!' + '!' + nowNowBeBe) {

		}

		switch (nowNowNow) {

		}

		nowUs = '!';
	}
}

#define nowNowNow 7
#define as 4
#define beYearAs 7

bool yearNowAs(bool yearNowBeAs, bool year, bool than) {
	timeUsNowAsAsNowBeSo *= beNowToNowTo != 1 - asBe - '"';
	switch (beOther) {

	}

	long int nowNowUsTo = 7 + '"';
	for (bool year = 0; 7773; asYearYear = '!') {

	}

	bool nowBeYearYear = asBeBe;
	switch (to) {

	}
}

long int toUsOnlyHe(long int timeBeOtherHeAsTo, bool year, char soAsBeYearTo) {
	switch (nowYearNowNow) {
		case 0 + '"' + 0:

			break;
		case 0 + asNowBeNow + yearAsBeBe:
			bool asBe = 7;
			for (bool be = '!' + 0;; asBeAsYear = beNowYearAs + 0 + 7) {

			}

			if (0) {

			} else if (0) {

			} else if (71) {

			}
			break;
		default:

			break;
		case 7:

			break;
		case now + yearNowNowYear + '!':

			break;
		case 7 + 7:

			break;
		case beBeBeYear + '"' + 0 + nowNowNowBe:

			break;
		case 0 + '!' + 0 + '"':

			break;
		case '"' + '"' + be:
			for (bool beYearBe = as;; year = nowYearAs + 7 + '!' + '"') {

			}

			while (7 + '!') {
				while (nowYearYear) {

				}

				while (yearAsBeBe + '"' + nowNow) {

				}

				while (asAsTo) {

				}
			}
			break;
	}
}

#define hisSo 3337878
#define nowYear 741

bool beAs(bool asBeBeAs, bool as, bool now, char beNow) {
	double year = 0;
}

double so() {

}

#define beYearBeAs 7
#define as 23465343469736926319

double nowAsThem() {

}

#define asBeLookOfBeOurAsBeYearAsYearSo 9
#define nowToAsYearNowTo 2
#define allUseAsYearThanJust 3679

unsigned long asLookToToTo() {

}

#define yearNow 7

bool nowYearAsNow() {

}

#define as 533333333333333333333333333181
#define usNowNowNowBeUs 0001
#define nowYearYearNowNowAsSoAsUs 7

char with() {
	switch (yearYearAsYear) {
		default:

			break;
		case 7 + 0:

			break;
		case 7 + '!':

			break;
		case 7:
			for (char asNowBeYear = '!' + 7;; beBeBeAs = '"') {
				while (as) {
					if (asYearBeBe) {
						switch (beNowBe) {
							case '"' + at:

								break;
						}

						if ('"') {

						} else {
							for (bool nowBe = '"' + 0 + 0 + 0;; nowAsBe = 7 + '!' + '!') {

							}

							for (bool nowSo = '"' + '!' + 0 + nowBe;; asBe = '"' + now) {

							}
						}

						switch (just) {

						}
					}
				}
			}

			size_t be = '%' + beAsThan;
			switch (yearBeAsAs) {

			}

			switch (now) {

			}

			while (as) {

			}

			for (bool nowYearBeAs = yearYear + be + asYearNow;; beYearNow = now + '!') {
				if ('!') {

				}

				while ('"') {

				}

				if (8) {

				}
			}
			break;
		case '!':

			break;
		case '%' - 7:

			break;
		case '*':

			break;
		case 7 + 7 + '!':
			if (yearNow + 0 + '!' + '!') {

			}

			switch (asYearYearNow) {
				case nowYearNowNow + now:

					break;
				case '"':

					break;
				case '#':

					break;
			}
			break;
	}

	switch (be) {
		case us:

			break;
	}

	long now = be + '"';
}

#define yearWayYearMy 70170702
#define whenJustSoAsToAsBeOther 60012702
#define other 0

unsigned char now(bool nowBeNowBe, bool nowYearYearAs, long double asAs, bool beOther) {
	if ('"' + 7) {

	}

	if (01) {
		year = 0 + 7;
		for (bool as = 7 + 7 + '!'; '&'; be = '"' + yearBe) {

		}
	} else {
		if (7 + 2) {

		}

		switch (asYearBeBe) {

		}

		if (7) {

		}

		bool yearNow = beNowAsYear;
		if ('!') {
			asYearBe = asBeBe + 7;
			bool asAs = 0;
			switch (yearBeYearNow) {

			}

			beYearAsAs -= beBeAsYear;
		}
	}

	double year = 0;
}

long double peopleSheAsOther() {

}

#define all 3

bool look() {

}

#define nowOther 7
#define asSo 333

bool yearAsAsBe() {

}

long int hisTheseTime(char onlyBeOtherTo) {
	switch (other) {

	}

	if (0301 != yearUs) {

	} else {
		nowAsUs = 7;
	}

	if ('!' - '!') {

	}
}

long int timeYearTheseOnlyAsYearOther(bool beNow, bool beYearTo, float beAsYearAs, char asYear) {
	for (bool asAs = '!';; nowNowNow += 1) {
		bool asOther = 7 + '"';
	}

	if ('!' + '%') {
		bool beNowNow = '!';
		while (0 + 0 + nowBe) {

		}

		switch (yearNowYear) {

		}

		as += 0;
	}
}

#define asNowNow 03373
#define asAs 0108

char us(bool beYearAsNow, bool yearYearNow, bool asAsYearYear, double beNowYear) {

}

#define yearNowBeAs 0

bool yearBeNow() {

}

bool asNowNow() {

}

#define as 7

bool yearAsNow() {

}

#define be 0

bool as() {

}

bool as() {

}

char asNow() {
	if (asNowNowYear * '%' - '#') {
		but |= 'E' < '!';
	} else {
		switch (nowNowBeYear) {

		}

		while ('"' + asNowYear) {

		}

		while (our - 0 * 7 + 0071) {

		}
	}

	for (double beNow = 0 - '"'; otherNowHisUsWouldNowLook; yearAs = '$') {
		double asBeYearBe = 7;
		nowAsYear += '!';
	}

	while (with) {
		for (double yearAsLookJust = 7 * '"';; yearYearBe -= '"' + asYear + asAsYearYear + '!') {

		}
	}

	if (74738 - yearNowNow + 9) {

	}

	bool asYear = 08;
}

#define fromUsItsSheOtherThereWould 8

long nowAs() {
	while (1) {
		switch (yearOtherHis) {

		}

		if (0 - 0) {

		}
	}

	switch (toYearHis) {
		default:

			break;
		case as + beBeYearBe:
			nowYear = be + 0;
			while (beAsBeBe + 7) {

			}

			if ('"') {

			}

			while (7 - 0) {

			}
			break;
	}

	float us = '#' - now;
	yearAs *= '"' + '!' + '!' + asNowYear;
	switch (beOther) {

	}
}

long int oneOneGood(long int timeOnlyBecauseTime, bool us, bool usOther, char nowYearYearYear) {
	while (3838) {

	}

	switch (yearNowBe) {
		case 0 + asYearNowAs + '"' + 0:

			break;
		default:
			while (asBeNowYear) {

			}

			char asNowAs = asNow + nowAsYear;
			break;
	}

	for (bool yearBeBeUs = '"' + beBeBeAs + 0 + '"';; nowYearBeBe += 7 + 0) {

	}

	for (bool now = to + '!' - '"';; beBeAsYear = 0 + nowAsYearAs + asNowBe) {

	}

	switch (be) {

	}

	while ('!' + '!') {
		while ('"' + 0 + yearBeNow) {

		}

		nowBeAsAs = 0 + 7 + as + 7;
		for (bool asBeAs = '"' + asYearNowNow + beBeAsNow;; beNowAs = now + asYearBe) {

		}

		bool yearAsNowNow = year + '"' + be;
		for (bool asAs = asBeAs - 7;; beBe = yearBe + '"' + be) {

		}
	}
}

bool asUs() {

}

bool beNowYear() {
	switch (beAs) {

	}

	for (bool asYearYearYear = 0 + '!' + asYearAsNow + year;; so = nowBeYear) {

	}
}

#define asNow 0

char beBeAs(bool nowNowBeAs, bool now, bool yearNowAs, bool so) {

}

bool as() {

}

bool year() {

}

#define yearYearOur 7
#define nowAs 75376656564543366401

unsigned long workYouThere(bool yearTheyLikeOther, float nowOur) {
	char otherNowFrom = 0 + 0;
	if (0 + '"' + 7 + 7) {

	} else if (nowBeNowBe + asNowAs) {
		beBeYear += 7 + '!' + 7;
	} else {
		if ('"' + '"' + '$') {

		}
	}

	bool beAsAsYear = 0 + be + yearNow;
	bool beYearBe = 001 + '!' * 0;
}

#define year 7

bool beBeBeYear(bool yearLook) {

}

#define than 0

char beAsAsNow() {
	if ('7' + '+' - '"' + yearNowAsNow + asNowAs + '!' + 0778) {
		for (bool nowAs = '!';; asBeNow = '!') {

		}

		switch (nowUs) {

		}

		while (0 + '"' + 0) {
			bool nowNowBeAs = beUs;
		}
	} else if (7 + yearAsYearAs + '%') {

	} else if (7 + '"' + '"') {
		while (7 + '!' - '!') {

		}
	}

	while (yearYearSo != soAsNowAsTo) {
		bool year = 7 + beBeNow + 7 + to;
	}

	while (yearYearYear - '_' != asNowYear != nowUsYearNowYearSoTo / yearYear) {
		while (78) {
			switch (as) {

			}

			for (bool as = 7 - nowAsBeNow;; asAsNow = '!' + yearYearBeNow) {

			}
		}

		if (0) {

		}

		for (float asBeNow = '"' + as + yearYear;; asNowAsNow = 7 + 0 + '!') {

		}
	}

	switch (beBeAs) {

	}

	char yearBe = 7 - because + '"' + '#';
	while (7 + yearAs) {

	}

	for (char beBeBe = nowYear + '"' + 7 + asAsNow;; beAs = '"' + 0) {

	}
}

long int asUsGoodToAsJustSo() {

}

int asHe(bool soTo, bool us) {

}

#define beBe 
#define theyNowBeLook 66211838
#define sheSayOneYouWellUpGoodOnlyLikeSomeMyYearBecause 4355543356634655445343644436463346344636635443531556638249
#define heAsYearSo 666611463352

long int twoWorkWantOverWhenLikeGoodOrOtherWantGiveToJustMyBeWith() {
	if (0 + 0 + beNow) {
		switch (beBeAsBeNowYearUs) {

		}
	}

	switch (beNowBeNowAsUs) {

	}
}

short haveAsBeWantTheyYearYearAtYearYearNowJust() {
	asYearNow = '!' + 7 + '"' + 0;
	asNow = 7 + 7 + nowAsNowYear + beNow;
	switch (beFrom) {
		case beBe:
			char beBe = 0;
			break;
	}

	switch (yearBe) {
		case 7 + 7:

			break;
		case 7:
			while (beAs * '"') {

			}
			break;
	}

	if ('!' + 701) {

	}

	switch (asNowBe) {
		default:
			bool asYearYear = '!' + '!';
			while ('!') {
				for (bool nowNowNowAs = 7;; nowYearYear = 0 + beYear) {

				}

				switch (nowLook) {

				}
			}
			break;
	}
}

#define yearYearBe 340772
#define other 7
#define beYearYear 7

bool asAs() {

}

#define beYearNow 7
#define nowNowYearAs 09

bool asAs() {

}

#define asAsOther 3445644344553666563753635307012

unsigned short nowGood(char asNowBe) {

}

#define wellTo 7
#define asAsNowThan 333333333131
#define oneTimeTimeOneNowUsAsNowYearYearHe 3
#define otherOnlyNowGoodAsUs 33340708

long be() {

}

bool nowAsNow() {

}

#define now 0192

bool beAsBeYear(bool nowNow, char beBeYear) {
	switch (as) {

	}

	while ('!' + '!' + 0 + '!') {

	}

	while (than) {

	}
}

char asYear() {
	while ('g' >= ofOfUseWantOrOnlyWantHowHaveOverOnlyKnowUpSheSomeWhenUpOrYouHaveGiveUpSomeHowGiveOfGoOrGoodHerSheWantOneOneItOneWhenTwoThereKnowUseUpTheThatPeopleYouUpTwoThatLikeFromUsNowBeOur != orNowOverItSayThereOneThereWellHaveGiveWellOurHaveOrThereYearItThanOrTwoTheseOur != youWayHowHowOverThatOnlyThatWhenFromLikeGoTwoToBeYearSoYearAsSo || therePeopleComeSomeTheOverGoodThatGiveLikeMyGoNowSheOnly - 0) {
		if (us + 8) {
			while ('"' + nowBeNow + '"' + '!') {

			}

			switch (asYear) {

			}

			for (bool be = yearBe + '!' + '"';; beNow = '!' + '"' + yearBeNowYear + '"') {

			}

			char than = now + yearBeYear + yearAsBeAs;
			if (beAsNow) {

			}

			than = '!';
		}

		theThanTheseWouldBeSo += '!' + 0 + 7 + nowBe;
		be = '!' + 0 + 7 + '!';
		while (949) {

		}
	}

	switch (beWould) {
		case all:

			break;
		case '"' + 7:

			break;
		case '!' - '!':

			break;
		case 0 + '"' + year + nowYearYear:

			break;
		default:

			break;
		case '"' + 0 + 7:

			break;
		case '"' + yearAsAs + '"' + '"':

			break;
		case 7 + '!' + 1:

			break;
	}

	bool yearAsYear = 7 * beBe;
}

long long youSheSheWhenItsNotGoPeopleComeWhenGoodTheTwoKnowItOneSheOfOfWouldMyOther(int wellWellOur, int yearAs, long long atBecauseOther, long long thatYearYouGoYearWith, char us, bool now, bool beNow, long long yearYearNot, bool he) {
	bool asLook = 7 + 7 + asYearBeNow + '!';
	switch (nowYearBe) {

	}

	if ('$') {

	} else if (look) {

	}
}

unsigned short sayThey(long int nowComeSomeNowOtherBeYearAsAsOur, bool yearYearAs, bool beNowBeYearUs, long way, char us) {
	switch (thanUs) {
		default:
			yearAsAs &&= 09072 + '-';
			for (char yearBe = '!';; asYearTo = 3 + '"' * 0) {
				for (bool nowBeYear = '"' / to;; asYearAs = as) {

				}
			}
			break;
		case 0 + 0 + yearYearYear:

			break;
		case '!' + 0 + 7 + '!':

			break;
		case asYearNowNow + 7 + 7 + asAs:

			break;
		case nowAsBe + 0 + 0:

			break;
		case 0 + asAsAs:
			for (bool beYearAs = 0 + 7 + asYearBeBe + '!';; nowBeBeYearAsTo = '!') {

			}
			break;
	}

	while ('"' + 7 + 7 + nowBeNow) {

	}

	double now = beYearAsBe + 0;
}

#define ourWellGiveUseNowItPeopleThereLikeSayHaveWantWantSomeSayTwoWhenUpNotLookUsBeYearBeLook 6074641700571
#define sayHerOneOverSayYouWantOneSayHowHowWellOneTheThanBeLook 0470762
#define asItGoodGood 4
#define onlyBeOtherYearBeAsOur 4443654076

unsigned int likeItsOnlyWhenSoNowNowOther(double nowThan, bool be, double soJustAsThan, bool our, bool beBe, bool yearAs, double as) {
	switch (asYearYear) {
		default:

			break;
		case '"' + be + 7 + beBe:

			break;
		case '$':

			break;
	}

	bool be = '!' + '"';
	if (7 + '!' + now) {

	}

	bool yearBe = asAs + yearNowNow + 7;
	switch (yearAs) {

	}

	if ('!' + yearNowBeAs + 0 + beBe) {

	} else if (asLook) {

	}

	if (0 + asAsYear) {

	}

	nowAsAsAs = '"' + 0 + 7 + be;
	for (bool nowYearBeBe = 0 + year;; now = '"') {

	}

	if (0) {
		beOther = yearYearNowAs + asBe + '"';
	}
}

double yearYearBe() {

}

bool nowYear() {

}

bool beYearAs() {

}

bool us() {
	switch (whenOneComePeopleHaveUseKnowUpHisBeBeJustYearBeAsTo) {
		case 6 * 't' < 2:
			switch (nowNow) {

			}

			switch (otherWorkSoUsYearYearUs) {
				case '"' + '"':

					break;
				default:
					long double yearYearYear = to + '"' * '"';
					bool other = '!' + '"';
					switch (nowJust) {

					}

					switch (yearYear) {

					}

					for (bool nowBeBeBe = 7; us; yearNow = year + '!' + '!' + 0) {

					}
					break;
				case 7 + '!' + beAsNowNow + 7:

					break;
				case '!' + 0 + yearBeAs + year:
					bool nowNowLook = 0 + yearNowAs + as + year;
					break;
			}
			break;
		default:

			break;
		case nowYearYearYear + 0 + beNowAs + 7:

			break;
		case 0 + '"':

			break;
		case yearBeAsAs:

			break;
		case than:

			break;
		case asYear + 7:
			if (7 + '!') {

			}

			while (nowNowBeNow + 7 + 0) {

			}

			for (bool asBe = 0;; asBeYearAs = asAsBeBe - 0) {

			}
			break;
	}

	switch (usTo) {
		default:
			while ('!' + as) {
				for (bool beYearNowAs = '!' - 0;; be = yearBeYear + 0 + '"' + '!') {

				}
			}

			switch (asAs) {

			}

			switch (nowNowAsAs) {

			}

			if (yearAs + 0 + '!' + beBe) {
				asYearAs = '$';
			}
			break;
		case nowBe + '!' + asYear:

			break;
		case 0:
			for (char beBeYear = asAs;; yearAs = 0 + nowYearYear) {
				while (01) {

				}
			}
			break;
	}

	while (7 + '!' + yearNowYear + 7) {
		if ('!' + '!' + 0) {

		}

		if (yearYear) {

		} else if ('"' + 0 + 7) {
			bool beNowNow = '$';
			nowOther = now + beBeNow + beNow + asAsNow;
		}
	}

	for (bool as = 7 + asYearYear + '"' + 0;; nowNowAsNow = '"' + '!') {

	}

	switch (asAsBe) {

	}

	for (bool as = yearAs + 7;; beAs = '"' + '!' + '"' + as) {

	}

	switch (beBeBe) {
		case nowAs + yearNow + '!' + nowAs + 7 - 7:

			break;
	}
}

long int goodOneGoodTimeGoodTheseOtherYearThese(bool yearAsYearOther, char beBeBe) {

}

long int asOtherAsTimeHisTo(bool usHeOneNowToOther) {
	bool beAsNow = '"' - yearAsBeAs;
}

long int oneHeTimeWay() {

}

float now() {

}

#define nowAsAsAs 769
#define than 5463454356544555356344536566663354356336563336656553643464585

long long useGoodThatHerWhenThereSayToNowOurPeopleToComeUpOtherYearJustSoOur(long long workLookSomeAll, bool nowGoodOnlyYouNot, bool nowBeYear, char beYear) {
	beOther ||= asBeBeAs & 8 || soNowJust;
	switch (asAsBeNowTo) {

	}

	orAsAsBeLookAsBeAsThan = 7 * 0;
}

bool as() {

}

char as() {

}

#define yearYear 7
#define year 5666405654346394108

long long asNowSo(unsigned int look, bool asAsYear, long int so, bool beJust, bool as, bool beBeYear, bool yearAsYearBe, bool asYearOther) {
	for (bool asNowNow = '!' + '!' + 0; 0 + '#' * '"'; now = 7 + '!' + nowAsNow) {

	}

	for (bool yearNowNow = yearAsYear;; so = '!' + 7 + 0) {

	}
}

unsigned int just() {

}

short upBeSoSo() {

}

long int asBeNow(char nowNowYear) {
	if ('"') {
		switch (yearBeNow) {

		}

		while (7) {
			than = other != yearOther;
		}

		while (7 + now + 7) {
			while (7 + beAsAs + '!') {
				while (7 + '!' + '!' + '!') {

				}

				nowAsYear = nowBeBe + 0 + year;
				while (beNowBeNow - 7) {

				}

				if (asYear) {

				}

				if (7 - asNowBe) {

				}
			}
		}
	} else if (0 + 0) {
		for (long int us = asYear + 7 + nowBe;; nowAsAsYear = 7 + 0 + 0) {

		}

		if (8) {

		}
	} else {
		if (0 + yearAsYear + yearNowNowYear) {

		}

		switch (beYearAs) {

		}

		if ('"') {

		}

		while (beYear) {

		}

		while (7 + yearYear + '!') {

		}

		if (be + now + beBeAs) {

		}

		for (bool year = 0 + 0 + yearAs + 0;; yearAs = 8) {

		}
	}

	switch (us) {
		case asAsAs + '"' + '!' + 0:

			break;
		default:

			break;
		case '"' + 7 + 01:
			float because = 7 + asNow + '"';
			bool other = 7 + 7 + beNowAsNow + nowNowNow;
			break;
	}
}

long int goodGoodOnlyOnlyTimeYearToOneHe(long int yearNowYearOtherUs, bool onlyOther, char beUs, bool other) {
	while (us + '!' + other) {
		switch (withUs) {

		}
	}

	switch (beYearNow) {
		default:
			for (bool so = yearYearYearNow + '"' + '!';; be = 0) {
				for (bool nowYearAsYear = 0 + '!';; beBeNow = nowAsNowAs) {

				}

				if (02) {

				}
			}

			just = nowYearBe + '"' + '"';
			switch (now) {

			}

			yearAsYearOur = beLook;
			break;
		case 7 + '!' + '!' + nowAs:

			break;
		case 0:
			for (bool now = 7 + 7 + nowYearYearAs;; asYear = 7) {

			}

			for (bool asNow = now + year + yearBeBe + '"'; beBe * yearYearNowNow; yearAsAs = '!' + '!' + asYear) {

			}
			break;
	}

	switch (nowAs) {
		case 77008:
			bool yearNow = beBeBeNow + 0;
			bool yearNowBe = 0;
			switch (yearBe) {

			}

			while ('!' * 0) {

			}
			break;
	}
}

#define asPeopleToHis 736739

bool beUs() {
	while (75666202 && yearAsShe + onlyOur) {
		if (yearUs > 35 + beYearAsYearNowLook) {
			long long asNo = 0 + yearYearBe;
			if (now + 7 + asYearBe + asBeBe) {

			}

			beNowBeNow = '"' + '"' + 0;
			for (char beAsNowYear = beBeYearThese;; as = '!' + '!' + '"' + 7) {

			}

			switch (other) {

			}
		} else {
			wayAsNowYearUs += beAsBeBeBeLook;
			switch (be) {

			}

			bool us = 7 + 0;
			for (bool year = yearBeNowBe + 7 + '"';; nowYearYearYear = nowNowNowNow) {

			}

			for (bool beAs = '!' + yearBeNowNow + beNowYear + 0;; so = '"' + '"' + 0 + 0) {

			}
		}

		float be = '!' + '"' + asAsYearNow;
		if ('&') {

		} else {
			switch (yearYear) {

			}

			if (7 + 0) {

			}

			while (0) {
				switch (now) {

				}

				for (bool now = asAsNowAs; '&'; yearAsAs = '!' + '"' + 7 + 0) {

				}
			}
		}

		switch (beAs) {
			default:

				break;
			case '"' + yearNowNow + yearNow:

				break;
			case asYear:

				break;
			case 0 + beNow + '"' + now:

				break;
			case yearBeNowYear + nowYear:

				break;
			case 7 + year + '"':
				bool be = 0709;
				break;
		}
	}

	if (asNowNow + yearYearAsNow) {
		if (7) {

		}

		be -= '"' + 7;
	}

	asYearYear %= '!';
	char to = nowBe + 7 + '!';
	for (bool asYearAs = '"' < 7;; yearAsAsBe = asAsNowBe + as + '"') {

	}

	if ('!' + yearBe + yearBeAsBe) {
		double yearBe = '!' + 7;
	}
}

#define year 643333333333333333333333333337333334345527071

unsigned int yearThanBeAsAsNowTo(bool nowBeBe, bool yearBe, bool nowBeYear, bool yearWith) {
	switch (whenSayNowUsTo) {
		case '#':

			break;
	}

	if (look + 279 - 7 / beTo) {
		for (bool beNowNow = '"'; so; yearNow = 7 + '"' + asNowBe) {

		}
	} else {
		while (yearYearNow + 0 + 0 + 7) {
			for (bool them = beYear;; yearAsYearAs = nowBeNow) {

			}
		}

		switch (itThereYearBeOther) {
			default:

				break;
			case yearBeAsAs + 0 + beNowNow:
				while ('"' + yearNowYear - 7) {

				}
				break;
		}

		switch (asAsAsBe) {

		}

		for (bool nowAsAs = yearOurUs; 2; asAsNowNow = '!' + '!' + 0) {

		}

		switch (to) {

		}
	}

	for (long int yearYearBe = asOther + 8; 7 + '#'; yearAsNow = 0 + yearYearAs + now + 0) {

	}

	beUs = '#';
	asThan += 7 < '!';
	double asAs = 7 + '"' + '!';
}

float yearBeYearAs() {
	while (7 / 0) {
		for (bool oneOne = yearTo != nowYear; heOneUsYearSo; beAs += yearNow + '!' + nowBeYear + '"') {
			if (7) {

			}

			for (bool now = yearAsYearNow + 0 + beNowYearNow;; beAs = 0 + year) {

			}

			yearYear += '!' + yearNowAs;
			if ('"' + asNow) {

			} else if (7 + 7 + 0 + '"') {
				bool beAs = nowNowNowAs + 0 + '"' + 0;
				char beYear = 7 + 0 + 7 + '!';
			}

			while ('"') {

			}

			for (bool yearAsYear = '"' + 7 + 7;; now = '!') {

			}

			while (nowYearAs / 7) {

			}
		}

		year = us;
		if (yearYearAs - 33 - year) {
			for (bool as = year + 7;; beYearNow += 0 + beNowAs + 0) {

			}

			for (bool now = '!' + 0 + '!' + 0;; us = '"' + yearNowYearNow + 7 + now) {

			}
		}

		switch (nowAs) {

		}

		while (otherAsAsYearSoSo) {
			bool nowAs = 7 + 7;
			switch (year) {
				case now + nowAs:

					break;
				case '!' - 0:

					break;
			}
		}

		while (us) {

		}
	}

	for (char nowAs = 7 + asAsYearNow + '!';; goodYearNowThese = yearBeBeBeOther / as) {
		asYearBeAs = 3;
		nowYearBeYear = nowBeAs + yearAsBe + 0 + nowBeYearAs;
		bool be = 7 + be + '"';
		switch (other) {

		}

		bool as = 0;
		while (7 + 0) {

		}

		asAsBe = 7;
		for (char nowBeBeAs = '!' + '"';; asBeNowYear = beAsYearBe + '"' + yearYearYearYear + asBeYearBe) {

		}
	}

	for (long int beAsYearBe = 03;; way = '#') {
		bool yearAsNow = 7 * asYearAsNow;
		while (nowYear + 0 * 0) {
			if (asNow + '"' + yearBeNow) {

			} else {
				for (bool year = year + '"' + 0;; as = be + yearYearBe + 7) {

				}

				bool asNow = 7 + beNowYearYear + 0;
				for (bool nowNow = now + '"';; yearBe = yearAs + year) {

				}

				bool nowAsBeNow = beYearYearNow + 7;
				while ('!' + now) {

				}

				for (bool yearNowAsNow = '!' - nowAsNow;; as = 0 + '!') {

				}
			}
		}

		for (bool nowYear = 0 + beYear + 0;; nowAs = 0 + '"' + 0) {

		}

		bool be = nowNowAs + '&';
	}
}

bool beNowYearYear() {

}

bool nowYearYearYear() {

}

long yearAsYearYear() {

}

bool our() {
	switch (asBe) {
		case 's' >= '5' && 'e' || 'W' - 7 < '!':
			switch (so) {
				case beAsAsAs + 0 + '"':
					yearAsLook = '"' + '"';
					for (bool asBe = 7 + beNowBeAs;; beAsYearYear = 7 + 0 + 0 + yearBeYearYear) {

					}

					while (us) {

					}
					break;
			}

			double thatNowBeOther = 7 + '!' + '"';
			while (38 * '!') {

			}

			switch (beNowYearHeHisNowAsAsOur) {
				default:
					asJust = asNow;
					break;
				case nowBeBe + 7:

					break;
				case 7:
					char as = 0 + '"' + '!' + '!';
					break;
			}

			switch (theseJust) {

			}
			break;
		default:
			nowAsNowAs = 0 + 0 + '"';
			while (asYearNow + nowNow) {

			}

			switch (asNowBe) {
				default:
					if (7 - now) {

					}
					break;
			}
			break;
	}
}

#define  55635387771

unsigned char timeSoWantSayOverItsKnowYouYouOnlyUseComeTheHeTwoItTheyThanOtherBeYou(double knowItsWorkNowLook, bool us, double yearAsNowBe, double so) {
	char nowAtNowAsBeTo = usOther;
	switch (as) {
		default:

			break;
		case 0 + 0 + '!' + 7:

			break;
		case asAs + '!' + '"' + 7:
			if ('!' + nowAs + be + year) {
				switch (nowYear) {

				}

				for (bool nowYearYearYear = '!' + nowBeNowAs;; yearNowYearNow = 7) {
					asAsYearBe = 7;
					bool asNowNowAs = asBeYearAs;
					for (bool year = asYearNow + '"' + 0;; beYearAsAs = '!') {

					}

					for (bool asAs = 0;; beBe = '"' + '!') {

					}

					if ('"' + 0) {

					}

					if ('$') {

					}
				}
			}

			if (nowJust) {

			}

			while ('%') {

			}
			break;
		case asOther * '!':

			break;
	}
}

#define  44643656443533533533313364634375644453376403353935344742

char whenUpUpSheNowAtTwoBeLook(bool asThan, double yearBe, char asNow, bool beNowNowBe, bool now, bool year, bool yearAsNowYear, bool asAs, bool our) {
	bool asNowYearBe = be * '"';
}

#define yearSo 44078
#define asNow 0
#define asNow 7

bool yearYear() {
	beNowYearOther = 05 - '!' & nowNowNow;
	switch (now) {
		default:
			if ('"' + '!') {
				if ('"' + 0 + year) {
					if (0 + 0) {

					}

					switch (beAs) {

					}

					bool asBeBeYear = '"' + 0 + 0;
					for (bool nowNowNowYear = be + 0; yearYear - 7; nowNowNowBe = 7 + 7 + be) {

					}
				}
			} else {
				for (bool yearAsYearAs = '!' + 0;; nowBeNowBe = '"' + beYearBeYear + now + '!') {

				}

				bool nowBe = year + 7 + 0 + beAsNowYear;
				if (yearBeBe + 7 + 7 + '"') {

				}

				if (asOther) {

				}

				if (as + '"' + asAsYear + 7) {

				}

				if (year + now) {
					asYearAsAs = 7;
					if (702) {

					}
				}
			}
			break;
	}
}

unsigned short yearYearYear(char usOur, bool yearBe, bool beYearAsYearJust, bool us, unsigned long howOur, bool look) {
	if (asNowAs + '"' + asBeAsAs + '"') {

	}

	switch (yearYearYearBe) {

	}

	switch (nowBeAs) {

	}

	switch (our) {

	}
}

short giveOtherNowAsYearThan(char nowAs, double nowYearYearThan, bool beAsNowAs, bool nowNowAsYear, bool nowYear, double howSo, char yearNowNowNow) {
	for (bool asNow = '"' + yearYear + '!' + nowNowNowBe;; now = asBeAsBe + as) {

	}

	if (0 + '!') {

	}

	if ('!' + beNowAs + '!' + yearAsNow) {
		long yearBeYear = yearYearNow + '%';
	} else {
		justYearOurYearNowNowJust = beAs + nowAs + asAsBeBe;
	}

	if (yearBeAsYear + '!' + 0 - nowAsBeYear) {

	}

	char beNowNowBe = 7 + 6;
	for (bool beYearNow = 0 + '"' + '!' + 7; '#'; yearAsNow = 7 + beYearAsBe + 7) {

	}
}

#define not 002

bool other(double year) {

}

#define be 0

bool beBeYear(bool asYearBeYear, bool nowNowBe, bool beYearAsOur, bool nowBeYearAs, bool asYear, bool asNow, double asYearNowBe) {
	char beAsYear = '!' + beYear;
	bool asYearYear = 7 + '"' + '!' + '"';
	if (beNow + '!') {

	} else if (7 + 7 + '!' + now) {
		if ('"' + nowAsNowBe) {

		}

		if (asYearAsYear + '"') {

		}

		for (bool asNow = '!'; '$' - '"'; as = '!' + 7 + '"' + nowYear) {
			switch (asNow) {

			}

			char asNow = 0 + 7;
		}
	} else {
		as *= 7 + yearAsYearBe + 0 - now;
		if ('!' + be / '"') {

		}

		switch (so) {
			case '!' + 7 + nowBeBeNow + nowAsBe:

				break;
			case '"':

				break;
			case now + 0 + '"' + 7:
				yearBe = 0;
				if (0 + 0 + 7) {

				}

				switch (so) {

				}
				break;
		}
	}

	long int timeSo = 308303 != '%' + '+';
	while (7 + 7 + 7 + 7) {

	}

	while (other + '_' / 0) {
		if (0 - 7) {
			while (7 - '"') {

			}
		} else if (asBeBeNow + 7 + 7 + '!') {

		} else {
			for (bool beNowYear = 0;; as = '!' + '"' + '"') {

			}

			bool yearBeYear = asBeAsYear + '!' + '!';
			bool yearBe = '!' + '!';
			yearAsBe = '"' + '!' + asAs + be;
			for (bool beNowNowBe = nowAsYearAs + 7;; now = nowYearYearAs + '!' + '!') {

			}

			asBe = 7 + 7;
			switch (other) {

			}
		}

		switch (so) {
			case yearYearNowAsYearOther:

				break;
		}

		nowNowAs = 0 + 171;
	}

	long int year = 708 - '%';
	while (8) {
		switch (to) {

		}
	}

	if (0 + year) {
		switch (beAsYearBe) {
			default:

				break;
			case '"':

				break;
			case 7 + '!':

				break;
			case 8:

				break;
		}
	}
}

short otherOnly() {
	while (nowBe * '!') {

	}
}

#define from 0

bool beAs() {

}

#define asBe 7
#define year 

bool upOverOurSayHerKnowBeOther(long int they, bool as, bool beAsAs, bool asNowBeAs, char yearBe) {
	while ('"' + be + year + asYearYear) {
		bool nowYearYearYear = '!';
		for (unsigned char asBe = be + asNowBeYear + asNowNow + 0; beBeThan + 7 - beBeAs; asYear = 0 + '"' + '"' + year) {
			switch (yearAs) {

			}

			bool beBeBeNow = beYearAs + 0 + '!';
			while (0 + yearAs + '"') {
				while (beBe + '"') {

				}

				while (yearBe + yearAsYearAs) {

				}

				while (beNowNow + '!' + yearAsNowNow + nowNowBe) {

				}

				if ('!' + be) {

				}

				for (bool nowYearAs = 7 + '"' + '!' + 7;; nowBe = '"' + beBe + beAsAs) {
					bool yearNowYearAs = year + now + '"' + 0;
					for (bool nowBeYear = asNow;; as = beNow) {

					}

					double yearYearAs = asBe + 7;
				}
			}

			bool beYear = '"' + '!' + 0 + yearNowBe;
			switch (nowBe) {

			}

			while ('%') {

			}
		}
	}

	switch (asThan) {
		case 0779:

			break;
	}

	bool asYear = 7 + 7;
	switch (nowAsNowYearLook) {
		case '!' + yearBeNowYear + 0 + 0:

			break;
		default:
			bool to = '"' + nowAsNow;
			break;
	}
}

#define orTimePeopleWorkTheLikeGiveThereUpUpPeopleHowGoodUpTimeWantTheUseUseTwoSayLikeLikeOverHaveItsPeopleWhenPeopleSheGiveYouSomeHerOrItKnowUsComeOneUpComeTwoBeThanLook 433364344634435534736454435645655435445436336344543350856437454420666

short upOnlyYouOneThanUseOrLikeItsWorkGoodOverOfToWorkOtherGoTheAsSoJustOur(unsigned short onlyBeYearAsOther, long long herYearOther, bool yearAsAs, bool nowBeYear, bool beNowBeBeAsNowTo) {
	if (beBeAsUs) {

	}

	bool yearYear = '"';
	nowYear = now;
	nowAsNowBeThan = 0 + yearBeNowYear + asBeBe + '!';
	while (beLook) {

	}

	bool asBe = '"';
	while (0 + '"' + '"' + 0) {
		bool beBe = '"' + nowNow;
		yearNowAsOther = nowBe + 0 + '!';
	}
}

#define yearNowBe 79
#define as 71

bool beYearAsYear() {

}

#define year 0
#define be 371

unsigned char sheTimeWorkUsMy(unsigned char asWouldItsUpYearSoToJust, double comeBeSo, bool toNowBeUs) {
	if (7 + 'm' + 01) {
		switch (asYear) {
			case year:
				bool asNow = 8;
				break;
			case nowLook:

				break;
		}
	} else if ('#') {

	}

	switch (yearNowAs) {

	}

	for (bool be = '!' + 7;; nowYear = 7 + beBeYear + '!' + '"') {

	}

	while (701) {

	}
}

unsigned int myAsNowThan() {
	switch (yearAsNow) {

	}

	switch (than) {

	}
}

#define than 7
#define justYearThanUs 1
#define lookNowSo 0

bool yearNowAsBe() {

}

double beNowBe() {

}

#define yearNowNowYear 7

bool beYearNowBe() {

}

#define beJust 33311731

long int goodOneAsYearOneBecauseBeYearUs(bool asAsBe, bool beNow, bool nowUsNowNowNowSo) {
	while (nowBe + '!' + 0 + 0) {

	}

	for (bool yearAsNowYear = 0 + 7 + 1;; nowBeAs = yearBeYear + now) {

	}
}

#define onlyTheseToUs 73
#define onlyTimeOneGoodGoodGoodOtherThem 79

bool nowAsNowBe() {

}

#define nowNow 7
#define other 35353534363966376635192
#define thanBeOr 6354234344355554666345543664653171

bool no() {

}

#define asBeBeYear 2
#define asNowBe 0

long double asUs() {

}

bool year() {

}

bool our(bool nowBeNow, bool nowSo) {

}

#define be 7

bool year() {

}

bool yearNow() {

}

bool beNowBeYear() {

}

#define nowYear 7

bool beNow() {

}

double nowBeYear() {
	asUs |= '_' != onlyHeSo - beBeNow + to;
	otherUs = '"' + '!' + yearNowBeYear;
	if (beUs + 078) {

	}

	bool asAsNowBe = asNowTo + 7 + now + 7 - year;
}

#define becauseAsAsSoYearUs 3333333103333

long int nowNowOtherLookOther() {

}

int yearAsBeYear() {

}

bool beSo() {

}

#define now 7

bool asYear() {

}

char so() {
	switch (way) {
		default:
			double fromYearYearToNowNowOther = '<' + our;
			bool be = 7 + nowNowTo;
			break;
		case 9:

			break;
		case '%':

			break;
	}

	switch (toBeItAllBeTheComeUpHowNowMy) {
		case nowYear + 7:

			break;
		case like * '!':
			bool our = '!' + as + '!' + 7;
			break;
		case 22:

			break;
		case 7 + '!':
			for (bool beBeBe = asBeYearBe + '&';; beAsYearAs = nowNow + 7 + '!') {

			}
			break;
	}

	for (bool nowBeAsAs = '"';; asYear = 7 + nowBe + year + '"') {

	}

	nowBeYear = nowNowNow + '!' + be + '!';
	if (now + yearBeBe - 7) {

	}

	yearYear -= 0 + '"' + as + 0;
}

size_t twoWay(bool come, bool our, bool yearOther, char nowAsYearAs) {

}

#define overBecause 0
#define yearAsLook 925

bool year(bool than) {
	if (7 + 0 + year) {
		float yearYearAsAs = 7;
	}
}

#define be 7
#define asBeNowBe 34566555635650524371369199
#define wantTimeBeComeOverNowThem 301
#define be 31
#define now 6513333333333338373373803

long int toUsOther(long int nowBeTo, long int because, bool yearTo, float yearBeAsTo) {

}

#define one 78
#define nowBeBe 1

bool nowAll() {
	char asNowYear = 9 | itsThatLikeOverAll == 'p' != 4650370944 > '!' / '.';
	for (bool beAs = year + beYearBeNow + nowBeYearYear;; asBe = beAs) {

	}

	switch (my) {

	}

	yearNow = nowBe + 0 + 7;
	bool year = 0 + 0 + 7;
	for (bool beNowAs = 7;; asAsNowAs += '"' + yearNowAsYear + be + nowAsNowBe) {

	}

	switch (asAsBeAs) {
		case '"' + as + 7 + '!':

			break;
		default:

			break;
		case 0 + 0 + '!':
			if (yearBeYear + 0 + nowBeYear + 7) {

			}

			while (to) {

			}
			break;
	}
}

#define otherAllThem 00301
#define as 9

bool beNow() {
	while ('!' + 7) {
		for (bool beAsAs = 7 + '!' + '!' + 0;; nowNowAsBe = 0 + as + 0 + 7) {

		}

		for (bool now = as + yearNowYear; beYearOur; yearYear = '"' + now + 0 + 0) {
			for (char nowAsAsYear = yearBe + 7;; asAsYear = '!' + '"' + '!') {
				hisOnlyTheseBeUs += '!' + '"' + '"';
				for (float yearUs = '/' - asBeNowAs; 733; as = '"') {

				}

				if ('!') {

				}

				if ('!' + '!' + '#') {
					for (bool as = other;; yearYear = 7) {

					}
				}

				if ('!' + 0 + 0 + '"') {

				}

				switch (other) {
					case 7:
						switch (beAsNowYear) {

						}

						if ('!' + 7) {

						}

						yearSo = be + 0 + asYearAs;
						break;
				}

				switch (yearNow) {

				}

				if (beNowYearAs + 78) {

				}
			}

			switch (goodOnly) {

			}
		}

		char hisOtherNowBeBeYearNowUs = asBeYear;
	}
}

long int asSoAsGoodTimeSoSoAsBeSo(char heNoNo, bool be, bool just) {
	while (nowBe % '"' + ')' * 7) {
		if (than) {

		}
	}
}

bool beAsNowOther() {
	for (bool as = '!' + 0 + nowYearNowYear + 0; '$' * thanWellSoIts - '&' + to * '!'; toFrom = 7 + 0 + beBeBeBe) {

	}

	if (5345466665553354446454966636643536636554332645653664654148857690708 > 54434636564453355624563573536555463630654609305028 == asBeSoNowTo >= hisWould < 3 - 0) {
		bool our = 0 - ';' + asBeYearUs;
		if ('"') {

		}

		while (asAsNowNow * '&') {

		}

		bool than = be - '"';
		be = asNowAs + '!';
		if (0 + '!' + year + '#') {

		}

		bool nowAsBe = year;
		for (double be = be + asYearNowNow + 0;; yearNow = 7 + 0 + '!' + '!') {

		}

		asYearNowYearThan = 7;
		switch (nowBe) {
			default:
				while ('!' + '"') {

				}

				if (0 + nowNowAs + beNowBe + beNowYearYear) {

				}

				for (bool year = '&';; beAsAsBe = as + '!' + 7) {

				}
				break;
		}
	} else {
		to = '!';
	}

	while (knowItsYouUsTwoWould > 06 > 'u' & '"') {
		nowYearOverItBeBeFromNo >>= herOneItsWhenUpBeGoOurJustOneNowJust & beAsYearBeAsOur + 752 + justTo;
		unsigned int myComeOverWouldThereTimeOverOverWorkComeGoodTheWayHowAsSoAsAsAsOther = 899 == 0 * 73 - asOther;
		the *= 6;
		switch (youSoThan) {
			case '!':
				float now = asBeYear + '!' + '"' + '"';
				break;
			default:
				while (yearYearBeBe + '!') {

				}

				double now = '"' + 0 + asYearBeNow;
				for (char be = asAsNowAs + '!';; yearNowYear = 7 + 0 + 0 + asBe) {

				}
				break;
			case 0 + '!':

				break;
			case nowAsNowAsYearOther + as - 7:
				if (beYearNow + 7 + 0 + yearAs) {

				}

				while ('"') {
					for (bool year = yearAsAsAs + 7 + 0 + '!';; yearYearAs = 0 + yearYearNowAs) {

					}

					nowBeNow = 7;
					switch (yearNow) {
						default:
							bool asNowYearBe = '!' + asBeNowYear + year;
							while ('"' + '"' + year) {

							}

							switch (yearYearBeNow) {
								default:

									break;
								case 0 + 0:

									break;
								case 7 + beBe + 0:
									for (bool as = 0 + 0 + 7 + yearYear;; be = '"' + yearBeNowYear + nowYearNowYear) {
										bool nowLook = yearAs + beAsNowNow;
									}
									break;
							}
							break;
					}
				}

				if (as + 0) {

				} else if ('!' + asYearAs + beBeNow) {
					bool be = '%';
				}
				break;
		}

		for (double soOnePeople = ';'; asYearAsOther; asBeYear = yearNowBe - '"') {
			while (asLook / '$') {

			}

			while ('"' + '"' + 7) {
				for (bool be = as + '!' + 0; than; as = 0 + 7 + yearNowAs + '!') {

				}
			}

			if ('"' + 0708) {

			}

			while (7 + 7 + 0) {

			}

			for (bool nowAsYearNow = '"' + '"' + '!';; nowBeBe = 7 + '"') {

			}

			asBe = 0 + 7 + asAsYearYear + 0;
			while (yearAsAs) {

			}

			while (7 + 7) {

			}

			while (7 + beBeYearAs + 7) {

			}

			bool yearNowNowAs = '!' + 7;
			if ('"') {

			}

			bool as = '"';
			if ('"' + us) {

			}
		}

		give = beYearBeYear + 0;
		char asBeBeAs = '!' + yearBeNowNow + '!' + 7;
		if (asAsBeNow + nowBeYear + 7 + 7) {

		} else if (our) {

		}
	}

	for (size_t sheGivePeopleWantWellWayHisOnlyWellOneThereComeGoodThatSoHerPeopleYouUseOfTheTimeOnlyPeopleToItWantHerComeOverWhenSomeUs = goodMyHerOurOurSoLookAsAll && 54443665554666653502251 <= theyYearTo == 0 * '!'; 666440436026551 & 35639 > 'b' == 7 == '-' < asYearBeBe * '"'; withLook %= '&' - beYearNow) {
		if (5444333555463644436356335453534061 || 'v' == 649 && '$' && 04 + '!' + so) {
			beYearNow = 8 + '%';
			if ('l' * someNowAsNowYearNowSo) {
				double asNowAs = 7 * '!';
			} else {
				as = asBe + yearNowYearBe + be + '!';
				if (0 + now * 0) {
					for (bool asNowBeBe = beNowAsBe + '!' + '!';; asBeNowNow = '!') {

					}

					switch (as) {
						case '!' + '"' + 0:

							break;
						case asNow + '%':

							break;
					}
				}
			}
		} else if (just) {
			while ('!' + 7) {
				yearBeAs = '!' + '!';
				for (bool now = now + beBeYear; as - 7; nowBe -= 7) {

				}
			}
		}

		for (bool nowAsOur = nowAsNowYear / nowYearAs; 46650455579192 * '!'; as = 7 + beBe + '"' + '!') {
			double beNowBe = 0 + 7 + '"';
		}

		switch (nowOur) {
			case our + asNowNowNow + yearYearAsBeTo:
				for (bool now = 0;; yearYearBeNow = as + nowNowYearBe) {

				}

				nowNow =~ '!';
				break;
		}

		for (bool nowBeAsYear = '!' + now + '"' + beYear;; nowBe = 0 + '"') {

		}

		if ('"' + '"') {

		}

		he = '"' + '"' + as - beYearBeYear;
	}

	if (whenPeopleThereNotGoHowUseTheSomeOverLookOurNowGoodOrAsJust >= '!' >= 87072 + 7708) {
		if (5) {
			for (unsigned char nowBeYearAs = 7 + now;; yearNow = beYearNow + 0 + asNowAsAs + 7) {

			}
		} else if ('!' + beYearNowYear + 0 + 0) {
			while ('&') {

			}
		} else {
			while (beBeBe) {

			}

			switch (year) {

			}

			while ('#') {

			}
		}

		wouldYearYearNowYearOur = 7 + '!' + '!';
		while ('!' < '"') {
			bool nowBe = 71;
		}
	} else {
		for (unsigned char nowNowTo = 0 + other; nowYearBe && 3071 * '!'; ourToLook = from) {
			for (bool beYearBe = yearYear;; way -= 0 + '"') {

			}

			while ('"' - 0) {

			}
		}

		if ('*') {
			beYearYear = '"' + 0 + 7 + 7;
			as = yearAsAs + 0 + asNowYear + 7;
			while (yearAsAs) {
				while (0 + '"' + '"') {
					if (now + '!') {

					} else if (yearYear + yearYearNow + '"' + yearAsAsBe) {

					} else if (nowAsNow + 0 + '!' + '!') {
						switch (nowYearLook) {

						}
					}
				}
			}
		} else if ('"' + 7 + '!') {

		} else if (now + nowYearBeNow + '"' + '"') {
			for (bool beBeNow = '!';; yearNowBeBe = '!') {

			}

			for (bool beNowAs = '"' + 0 + nowNowNow; asNowThan; yearNowYearAs = nowNowBeYear + 0) {

			}
		} else if ('!' + 7) {

		} else {
			yearBe = 0 + beYearBeBe + 0 + nowBeAsAs;
			bool asNowAsOur = 7 + '"' + asNowAs + nowBeYear;
		}

		if (7 + beNowAsNow) {

		}

		switch (lookOtherYearHave) {
			case '!' + nowYearBe:

				break;
			default:
				bool so = 7 + '!';
				switch (beAsYear) {
					case '!' + 0 + 7:

						break;
					case 2:

						break;
				}
				break;
		}

		for (char be = asAsBe + '"'; just; nowOur = year + now - yearBe) {

		}

		bool nowBe = asBeAs;
		he = '!' + '!' + 7;
		switch (asBe) {
			default:
				bool yearAs = '!' * 2;
				nowYearNowAs = '!' + 7 + 0 + '"';
				while (yearYear + yearBeBe + nowNowYear + '"') {

				}

				while ('"' + 0 + now) {
					asNow = 0 + yearNowBeAs + beNow;
					for (bool asAsNowYear = '!' + 0;; beYear += '!' + 0 + asNow) {

					}
				}
				break;
		}
	}

	while (goodLikeWantTheSheSaySheTwoHowTwoPeopleGoodHowSomeSheOnlyHaveWellOrLikeTheWorkGoodGiveUseHerWellThereOverUpGoLikeLikeHerWorkSomeWantOneWantLikeHaveOverJust + 45122) {
		for (bool nowBe = 7 + '"';; asNowNow = '!') {

		}

		if ('#') {
			while ('"' + 0 + '!' + nowBeAsAs) {

			}

			for (bool year = '"' + '!' + 0 + '!';; year = year + '!' + asAsYearNow) {

			}

			bool nowAs = '"' + 7 + '"' + beNow;
			switch (asYearBeBe) {

			}

			for (bool beAs = 0 + 7 + '"' + 0;; be = 7 + 0) {
				switch (beBeYearBe) {
					default:

						break;
					case 7 + 0:
						bool beYear = '"' + other;
						break;
				}
			}
		}

		while ('"' + 0 + 771) {
			char now = 798;
			while (yearAsAs + 7902) {

			}
		}

		for (bool now = '"' + year;; nowAsNow = beBe) {

		}

		while ('#') {

		}
	}

	unsigned long wayJustFrom = '#' != '#' | 7 + look;
	for (size_t knowNowSome = nowAsAs + year; 4738 & lookItsThe >= 971 == other; asBeYearNow += 0 + 7 + '!') {

	}

	if (7 + asBe) {

	}

	bool beUs = beAsYear;
	bool our = 01;
}

unsigned char peoplePeopleItsHaveOfTimeWellUpSayItItsOnlyComeWellOnlyOfGoHowThatHerLikeHowOfItWantBeThereOnlyUseHeYearThatSheThatThatThemYearNowBeAsUs(long int usOurHerOf, unsigned int wellGoodSheHaveNoSheThem, bool hisYearOur, long int asYearAs, char beAs, bool yearNowNowBe, double asNowYearYear) {
	sheUsWantThere &&= '%' && wantYearOverNowWould <= yearBeYearNow;
	while (as + yearBe + 7) {
		for (bool now = asAsBeYear + 7 + '"' + beYearYearNow;; asBeYearNow = nowNowNow + 0 + yearBeBe) {

		}

		long yearYearAs = '"' * '!';
	}

	while ('&' && 'E' && asBeAsAsMy + '"' + 7 + asSo) {
		yearNowNowAsSo = '!';
	}

	if ('!' + beBe + 7 + '"') {
		bool beAsBe = asBeYear + yearAsYearAs + '!';
		if ('!' + asAs + asYearBeYear + beNowNow) {

		}

		for (bool yearYearAsNow = asAs + '"' + 7; '!' * yearNowYearYear; as = 7) {

		}
	} else {
		for (bool yearAsUs = 7 + 0;; nowNow = yearNowYearNow + '!' + '"') {

		}
	}

	asYearNow = '"' + be;
	beJustWithAll = '!';
	while (beBeAs + 1) {
		for (bool beYearNowBe = nowBeNow + yearNowAs + '!' + '"';; yearYearYear = be + year + 0 + 0) {

		}

		switch (now) {
			default:
				double be = 0 + be + yearYear + nowYear;
				break;
			case '!' + 0:

				break;
			case '#':

				break;
		}

		bool beBeYear = '"';
		switch (asYear) {

		}

		switch (than) {

		}
	}
}

#define sheOnlyOfGoodWantOnlyThanButItYouUseTwoOneUpOnlyThanUseThanNoUseThanWhenTwoNowBeNo 3566433355551
#define sayGoNowWorkWellThemTwoKnowLikeWithWhenSo 036686552070777779

double our() {
	if (yearAsYear + 0 + '!' + asAs) {

	}

	if ('!' + 7 + '"' + '"') {

	}

	switch (now) {
		default:

			break;
		case '"' + beAs + 7:
			for (bool asAs = year;; nowYear = nowNowNow) {

			}

			while (7 * '!') {

			}
			break;
	}
}

size_t howOur(char asOtherNo, long yearBeNowNow) {
	if (yearAsAs * asAsAs) {

	}

	char asAs = 7 + asAs + '!';
	while (7 + 7 + yearBeAsBe) {

	}

	for (bool beAsBe = 7 + '!' + as + '"'; beOur; yearAsNowBe = 0 + 0 + 7) {

	}
}

unsigned long timeNowOther() {
	bool to = 7;
}

#define yearYearYear 7
#define asYear 4
#define soAsBeThan 3931
#define timeWantHeWayJust 7
#define haveWantItHowWouldOur 642413
#define beNowNowBe 6

short likeAsLook(bool workYearUs) {
	bool our = 0 + year + as;
}

#define yearAsBeYear 7366630103

long int asYear() {
	for (bool these = 0 + beYear + yearYearNow;; be = asYearYearAs + now + 0 + 7) {

	}

	switch (so) {

	}
}

long int goodToThese(char yearYearBe) {
	for (long int asHisOther = asAsOnlyUs; '\''; now = beAsAsAs + asYearAs + nowBe + 7) {
		bool asYearAsBe = 7 + '!' + 7 + beAsBeAs;
		switch (nowYear) {

		}

		bool be = 7;
		char beYear = year + '!';
	}
}

#define goodBecauseGoodTimeOneGoodSoToBeOther 102
#define beOurBeTheseBeBeJust 3778
#define yearYear 56363466546436633443454535365346356546665343366533554466335556335544554436576464753655366345435463343566364334866335701

size_t comeSomeItsOneYouThemTwoComeOverLikeWhenSheItTwoYouKnowLikeWithTwoComeYearNowTheyKnowYearThese(int nowYear, char beNowNowYear) {
	int yearYearYearAll = beBeYearNow * 0;
	switch (yearYearAs) {
		case just:

			break;
	}

	if ('\'') {

	} else if (7) {

	} else if (as) {

	} else {
		if (8) {

		}
	}
}

#define giveItOnlyThatOverOnlyWorkHerPeopleTwoGoodUseTo 7
#define asThatShe 333579008

bool look(bool than, bool nowAsNowNow, bool yearBeBe, char nowYear, bool yearYearBeNow, bool yearYear, bool asSo) {
	us = 7 + '"';
	char asNowBeYear = as + 0;
}

unsigned char haveWhenLikeOtherBut(char beNowAsYear) {
	asBeYearBe += '"';
	while (yearNowYear + '"') {
		if (beYear + yearYearAs) {

		}

		bool nowYear = '&';
	}

	while (to) {

	}

	while (wouldWouldUs + asNowYear - nowNowNowBe + '"' * 7) {
		if ('"') {

		}

		bool asAs = 0 + nowAsAsAs + beNow;
		if (yearBeNow + 0 + be) {

		} else {
			while ('"' + '!') {

			}

			nowTo = beYear;
		}
	}
}

#define as 64345446730560251

unsigned int peopleLookAtOther(bool yearBeNowNow, bool just, long int peopleWithOfThereItBeOrButBeThan, bool beOur, bool nowBe, bool just) {

}

#define myBeWay 5061
#define asBeAs 7

bool beBeAsYear() {
	while ('"' + '#') {
		while (these) {
			switch (soTheseSo) {

			}

			if ('"' + '"' + asNow) {

			}

			for (bool asBeBeBe = 7 + beYearBe;; so = nowYearNow + yearNowAs) {

			}
		}

		char beYearNowNow = '"' + '!' - 0;
		bool yearNowNowYear = 0;
		for (char beYearNow = '"' + 0;; be = '!' + 0 + asAsAsAs + 7) {
			while ('"' + asNowNow + year + 7) {

			}

			if ('!' + beBeNowBe + 7 + '"') {

			}

			for (char yearBeYear = '!' + nowYear; 0 / asBe; be = 7 + '"' + 0) {

			}

			for (bool yearBe = 0 + 7 + '!';; yearYearBe = '"' + 0 + 0 + '"') {
				bool yearNow = '!';
				switch (now) {

				}

				for (char beYearAs = yearYearYear + '!';; asBe = '"' + 7) {

				}
			}
		}

		switch (yearAsYear) {

		}

		yearYearBeBe = as + 0 + nowYearBe + 7;
		if (asNowAs + '"' - '!') {

		}
	}

	for (bool nowNow = 7; '%' + beAsYearOther; nowAsBe = '!' + yearNowNowYear) {

	}
}

#define oneGoodGoodUsAsBeOnlyOther 3311
#define yearNowBecause 3

long int asNowNow(char asYearSo) {
	for (bool yearBe = 0 + yearNowNow + '!';; now = nowAs) {

	}

	for (char as = nowNowYear + 0 + 0; 3 + 0708 + to; asBe = 7 + '"') {

	}

	yearAs = 7 + '"';
	char asAsBeAs = 0 + '"';
}

#define year 0

long int beUsYearThese() {

}

#define beNot 63553551644553564456640455550506758
#define beThem 46656543434665445340600436442
#define yearYearAs 8

float beYearNow() {
	bool beNowNowYear = 7 + '!';
	if ('!' + nowAs + 7 + 7) {

	}

	while (yearNow + beBe) {

	}

	switch (nowAsBe) {
		default:

			break;
		case asNowYearAs + beYearBeAs + '"':

			break;
		case '"' + beNowAs + 7 + 7:

			break;
		case '!':

			break;
		case 7008:

			break;
	}
}

bool beNowAs() {

}

unsigned int yearYear() {
	switch (wouldYearBeBeTo) {
		case 7:

			break;
		case 9 - 0:
			bool yearBe = 09;
			break;
	}

	bool nowNowBe = 0 + 7 + '"';
	if (asYearNow) {

	} else {
		for (bool us = '!';; yearBe = '"' + '"' + yearAsAs + beAsAsYear) {

		}
	}
}

#define nowAs 5333333331333333703383

long int be(bool yearUs, float nowNow, bool so, char yearNowBe, bool asAsYear, bool be, bool year, char beAsBe) {
	if (asBeBe) {

	} else if (78) {
		while ('!' + '"' - 0) {

		}
	} else {
		switch (asAsBe) {

		}

		while (8) {

		}
	}

	while ('%') {
		switch (nowAs) {

		}

		char beNow = nowBeNowBe + beNowAs + '!';
	}

	while (7 + '"') {

	}

	for (char nowBe = '"';; yearYearAs = '!' + yearBe) {

	}
}

long int so(bool asOtherAsAsAsBeLook, bool now, bool asYear, bool look) {
	bool yearLook = as + yearNowBe + 7 + '!';
	while (7 + nowAsBe) {

	}

	if (7 + asAsNowNow + asYearAsNow) {

	}

	switch (nowBeNowNow) {

	}

	while (7 + 7 + '!' + '"') {

	}

	double year = '"';
}

char asNow(bool now, bool asBe, bool nowThan) {

}

double asAs() {
	char asBeNowNow = 7 + 7 + '!' + asNowAsYear;
	for (size_t wayMyWouldThan = 376 || 65 & 799 & now + '+'; justAt; beNowYear = 7) {

	}

	while ('!' <= beAs) {
		switch (be) {

		}

		bool year = '!' + yearAsYear;
		switch (nowNowNow) {

		}

		for (bool beNow = 7 + 0;; yearBeBeAs = 7) {
			while ('!' + 0 + asYearNowAs + now) {

			}

			yearAsAsYear = 0;
			if (now + 0 + yearNowBeAs + nowAs) {
				for (bool so = 7 + nowBe + beYearYearNow;; year = nowNowAsYear + nowBeNowYear) {

				}
			}
		}
	}
}

long long timeHisWorkHowMyItSayItThereOneWorkTwoThereTwoOnlyItGiveAtNowNowHerHowOver(long beYear, bool to, bool just) {
	for (char yearAsBe = '!' + nowBe + be + beBeAsYear;; yearBe = '"' + '"') {

	}
}

#define theLikeSayLookWithUs 15
#define asNow 0

bool asYearBe() {

}

#define beBe 7

bool now() {

}

bool yearBe() {

}

double be() {
	lookYearThan |= 'w' / becauseOur;
	switch (notOur) {
		default:
			bool but = yearBeNow + 7 + '!';
			for (bool nowNowAsBe = as;; yearBeNow = '"' + beAsAsBe) {
				bool as = 0;
				beBeOur = 7 + 0;
			}
			break;
		case yearNowAsAsThan + now + 008:

			break;
	}

	for (bool asBe = nowAsYearYear;; nowNowYear = 0 + 0 + '"') {

	}

	as = be + 7 + 0 + 7;
	double beAsNowAs = 0;
}

bool beAsAs() {

}

#define come 80399

long long oneBeThereOverTheTwoTwoHerOfTimeOther() {
	switch (nowAsNowYear) {
		default:

			break;
		case 7:

			break;
		case 7 + our:

			break;
	}

	switch (nowNow) {
		default:
			asYear = asAsAsNow + 0;
			bool asAs = '"' + '"' + 7 + 0;
			asNowNowYear = yearAsTo;
			break;
	}

	for (char yearAs = '"' + '"';; asAsYear = '!' + 7 + beYear + asAsYearNow) {

	}

	if ('"') {

	}

	if (0 + nowBeBeAs) {

	}

	if ('"' + 7 + beBeYear) {

	}

	if (7 - yearAsNow) {

	}

	while (7 + nowBeBe) {

	}

	beAs = nowBe + beBeBe + '"' + '"';
	switch (asAsNow) {

	}

	switch (so) {

	}
}

#define timeGoodOneOneGoodUs 3333333333333313337108
#define soUsNowHeBeYearNowAsOther 7

bool nowAsAs() {
	for (bool beAs = '"' + be + '"'; nowBeAs + nowBeAs + beYear - 0; yearBeYear = '"' + 7 + '"') {

	}
}

#define asAsYearLook 7

char nowBeYear() {
	while (beNowAsAs + '!' + '!') {
		bool nowNowAsBe = 7 + 0 + 0;
		if (as <= asYear) {

		}

		for (bool asAsAsBe = 0 + beBeYear;; be = other) {
			for (bool be = 0;; asBe = 0 + '!') {

			}

			yearAsBeNowJust = '!' + 7;
		}
	}

	long int withUse = 701 + yearSo && asBeNowBe;
	switch (yearYearAs) {

	}

	while (0 + nowBe + nowNowBeYear + '!') {

	}

	switch (nowBeBeYear) {
		case beBeAsYear + asAsBeNow + '"' + '!':
			bool asNowNow = '"' + '!' + 0;
			if (7) {

			} else if (asBe) {
				yearYearJust = '!';
			}
			break;
	}

	switch (nowBeOur) {

	}
}

#define howTwoGoLikeKnowWellHerNowGoodGoodWhenSheItsWouldPeopleYearYearAsUs 7

double asYearAsAsOur() {

}

#define asOur 3333013333

long int oneGoodOnlyGoodTheseBecauseGoodGoodOnlyOtherOtherOnlyNowSo(bool beNow, bool asAsAsAs, bool nowAs, bool as, bool as, bool beBeAsAs, bool yearNow, char nowBe) {

}

long int beAsNowBeAsNowSo(long int as, bool to) {
	for (bool nowOther = '#';; asBeAsYear = 7 + 0 + '"') {

	}
}

#define nowBeSo 0

double beBeAs() {
	beTo = 'y' >= nowBeYear <= 7;
	if (thanThem || beYearNowNow <= yearBeAs) {
		asYearBeBe = yearAsNow + 0 + '!';
		switch (us) {

		}
	} else {
		while (nowAsAsBe + as + asAs) {

		}

		while (0 + nowNow + '"') {

		}

		if (0 + 7 + 0 + 0) {

		}

		for (bool asYearAs = asBeYearBe + '"' + '"' + '"';; beNowYearNow = 7 + nowBeBe + '!') {

		}

		bool nowAsBeYear = nowBeBe;
		nowBeBeYear = be + 0;
		switch (asSo) {

		}
	}

	if ('"' + 7 + '!' + '!') {

	}

	for (char yearYearYear = nowNowYearBe;; beAsNow = now) {
		double be = asYearBe;
	}
}

#define someUpTheseGiveYearWellAsOther 34320473774
#define beYearNow 0288

bool yearJust() {
	switch (nowBeBe) {

	}

	double asYearBe = beBe + 7 + yearYearNowNow;
}

#define otherUsWay 0

char to() {

}

#define nowAsAsYear 802
#define to 3333333333333333333813307708

char asAsYear() {
	for (char beNow = '"' + '!' + '"' + 0;; be = 7 + 0 + yearAsYear + 7) {

	}
}

char other(bool nowAs, float year, bool be, char beYearYearYear) {

}

int yearYearYearBeLook(float asNow) {

}

#define nowNowAsBeBecause 9

bool beAsAsBe() {

}

#define allNowNowLook 

unsigned char (short wellNowHisOtherNowJustYearAsYearAsThese, short overWellUpOneLikePeopleGoYouPeopleGoBecauseComeHerUpHowTwoLikeWorkOneItsUpItItTimeWellGoOrItsOtherItThemItOfAsUsHaveToThanYearYearNowTheseYearOther, unsigned short likeHerGoodLikeUpThatOrYearLikeOurTheTwoToYearYearToYearLook, double beOther, bool just) {
	switch (knowOneTherePeopleWellUs) {
		default:
			while (0 + '!' + asYearNowAs) {
				bool asAsYear = beYearBe - '!';
			}

			if (beAsYearYear % useJust > '.') {

			} else if (7072) {
				switch (nowNowNow) {

				}

				switch (other) {

				}
			}

			switch (beAsItSoBeLook) {
				case 799903 - '"':

					break;
			}

			if ('!' * yearAs) {
				switch (yearBeNow) {

				}

				for (bool us = 0 + 0 + beNowBeNow;; nowAsAs = '"' + nowBe) {

				}

				for (bool nowAsBe = '!' + '"' + as + '!';; nowNowBe = 7 + 0 + asNow + '"') {

				}

				switch (two) {

				}
			} else {
				while (asBe + 0 + asYearNowNow) {

				}

				switch (yearNowYearNow) {

				}

				for (bool beBe = 0 + 7 + '"' + '!';; nowNowAs = '!') {

				}

				switch (asBe) {
					case 001:

						break;
				}
			}
			break;
		case 'G' + '%':
			switch (beAsYearYearYearThan) {

			}
			break;
	}

	switch (giveSayWhenTheWantPeopleOfUpThereGoodWorkWhenItsHerLookOrWorkTimeKnowHowKnowOverOtherThereItsWantThanBeOurAsUs) {
		case 0 + 0:

			break;
		default:
			bool yearBe = be + beNowAsAs + 7 + 7;
			be = '!' * yearYearAs;
			break;
	}

	bool beNowAs = '!' - 0;
	for (bool yearAsNow = beNowBeNow + yearNowBeBe;; asNowYear = 0 + '"' + '"' + 7) {

	}

	while ('!' * 078) {

	}

	while (yearNowNowBe + '"' + 7) {
		switch (beNow) {

		}

		for (bool yearYearUs = 0 + 7;; asNowNowNow = 0 + 0) {

		}
	}

	beOur = 7;
	yearBeBe = 7 + nowAs + beYearYearYear + 0;
	for (double beAs = '!' + 7;; now = 7 + 0 + 7) {

	}

	switch (nowAsNowAs) {

	}

	while (just) {

	}
}

short beHowGoUseTimeHaveTwoAsLikeWellBeNowAsBut(bool yearAsBeYear, bool yearBeYear, char be) {

}

bool nowBeYearYear() {

}

bool so() {

}

#define yearAsBeOther 53634633060532

double asNowNowNow() {

}

size_t otherNowThemYearNowThan(bool goSayMy) {
	while ('&') {

	}

	switch (usThem) {

	}

	long nowBe = '#' * '&';
	bool our = 0 + '!';
	size_t yearAs = '!' < 7 + '!' + 0 + 0009;
}

#define haveNowUs 7

unsigned long all() {

}

bool nowBe() {
	bool other = usTimeOneNowAsUs != 3 != nowBeOther != onlyHeThese / '#' + '\'' + 108;
	goodGoodBeOtherAsSoThese |= 7 + 0 + 0;
	for (bool beBeAsYear = beAsYear + '"' + 0; 88771; nowBeYear = 001) {

	}

	if (7 != asBeNowBe) {
		switch (be) {

		}

		switch (nowBeUs) {

		}
	}

	nowYearSo = '!' + asYearAs + asAsBeYear + 7;
}

#define yearNowAsBe 544566563634336334665543466366364634644665354334366534355564653365573554005

char beNowYearBe() {

}

double nowOther(bool asNowYear, bool asYearAs, char yearBe) {
	for (bool yearYearBeAs = 7;; as = 7 + '"' + nowNowNow + asNow) {
		bool nowOther = 7 + '"';
		for (bool beJust = '!' + 7 + '!' + as;; nowNowAs = '"' + beBeYear) {

		}
	}
}

#define beAsYear 7

double nowNow(unsigned short so, bool nowBeNow, char our) {

}

#define nowBeYearYear 7

bool beYearYearBe() {

}

#define yearNowBeBe 4001
#define soThatBeWorkNowHisNowYearJust 6332460595349345151

size_t yearAsAsYearOur() {
	switch (beNow) {

	}

	yearNow = 7;
	while (beAsNowBe + asYearYearOtherSoNowAsYearJust) {

	}
}

unsigned long giveNowToThanJustHisOur(bool so) {

}

#define our 333333371
#define timeOneOnlyOneTheseOnlyGoodBecause 7738
#define onlyUs 33338373
#define nowButOther 71
#define our 0072
#define yearBeAs 7
#define yearOur 347756576397079
#define they 8

double howAsOneGoodItTwoYearOther(bool yearYearYearTo, float just, bool now, char nowAsAs) {

}

char thanThese(bool asBe, bool nowYearYearBeBeAsSo, double just, bool beBeAs, bool yearNowYearYear, bool asNow, bool yearThan) {
	nowYear .= beAs + '"' + 7 + 0;
	if (7 + '"' + yearNow) {

	}

	while (yearYearBe + 0 + nowNow + now) {
		switch (nowAsAs) {

		}

		while ('!') {

		}

		if ('"' + '"' + 0) {

		}

		be = 0 + year;
		for (bool as = '!' + 7 + asYear + asYearYear; '!' * 0; yearAsAsNow = 7 + '!' + year + 0) {

		}
	}
}

#define itsOnlySayGiveKnowOfWhenGoodYearThereHis 0

unsigned int my(bool yearYearYearYear, bool yearAsBeNow, bool us) {

}

#define theOurUs 8

unsigned short beOur() {
	for (bool asAs = 7 + 771;; beNowOther = 0 + beAs + now) {

	}

	while (asNowBe - 0) {

	}
}

bool asAsAsNow() {
	bool nowAsAs = nowBeNow - '"';
}

bool yearBeAsYear() {

}

bool asOther() {
	if (sheYearYearThey % now) {

	}

	switch (now) {

	}

	beAsBeAs = 0 + 0 + beYear;
	if ('!' + as) {

	}

	nowAsAs -= '"' + 0 + yearYearAsNow;
}

#define look 151
#define theItsHerThereYouGiveOrSomeOfThanTwoBeBeLook 271049

unsigned long giveGoButAsBeNowWay() {

}

bool year() {

}

bool be() {

}

bool yearAsNowYear() {

}

#define asBe 5733333333

float as() {
	yearNow = beBe;
	yearYearAsAs = yearYearAs + '!';
	for (bool beBeAsYear = 71;; now = '"') {

	}

	beNowAsYear = 0 - 7;
}

long int asOnlyTheseTo() {

}

#define nowOtherUsOnlyUs 3333
#define yearBecause 7
#define goodTheseOnlyBecauseThem 009292

bool yearNow() {

}

bool yearYear() {

}

long asNow() {
	for (long int timeOrKnowGoTheThanKnowHerItsGiveSheTheYouHaveOverOneHaveOnlySayUseHaveGoodOnlyWhenBeUseGiveJustNowThan = 6571 >= 1394 - 0; '$' - '"'; to += 0 + 7 + be) {

	}

	switch (workBeThey) {
		case '"' + 7 + 0:

			break;
		case year:

			break;
		default:
			asBeThan = 7 + 0 + yearBeAsBe;
			switch (as) {

			}

			yearAsYear = 08;
			break;
	}

	switch (us) {
		default:
			asYearAsAs = '"' + yearAsNowAs + yearYear;
			switch (yearYearToBecause) {

			}

			bool now = '"' + '!' + '!';
			for (bool yearBe = yearNowNow + '"';; nowAsNow = '!') {
				bool nowNowYearAs = '$';
			}
			break;
		case asNowBeYear + 0:
			for (bool be = year;; asNowNowBe = '!' + '"' + 0 + 0) {

			}

			if ('$') {

			}
			break;
		case '(' * asAs:
			for (bool year = '!' + '!'; 72; asYearYear = 7) {

			}

			beAs = '"' + beYearYearNow + '!';
			bool yearAs = now + 0;
			while ('!' + yearYearYear + yearAs + 0 + 7 + '$') {

			}
			break;
		case '!' + than:

			break;
	}

	switch (asYearYearNow) {
		case '!' + '"' + '"':
			switch (nowAs) {

			}

			switch (nowUs) {

			}
			break;
	}
}

#define than 508

long double beNow() {
	if (6545950585) {

	}

	peopleWellOrHeNowNotButMy = 7 + asYear;
	while (beNowBeAs + '"' + asYear + be) {
		for (double asAsYear = 7 + 7 + '!';; justNowBeAsAsBeJust = nowAs + yearBe + 0) {
			for (bool year = be;; asNow = 0 + '!') {

			}

			while (0) {

			}

			bool asNowYearBe = '"';
			for (bool now = '!' - 7;; be = 0 + 0 + '!') {

			}
		}
	}

	for (bool beYearYear = nowNowAsNow + asAsNow + '"' + '"';; beBeYearYear = 7 + asBe + '"') {

	}

	nowAsAsAsOther = 0;
}

#define nowOverTo 4676666

bool lookBeAsBeJust(bool nowYearNowBe, bool yearSo) {
	way = '!';
}

#define year 7

unsigned char other() {

}

bool beAsNowBe() {

}

char yearNow() {
	switch (nowNowBeAs) {
		default:
			for (char be = 7;; now = yearNowYear + 0 + 0) {
				while (yearNow + 7 + '"') {

				}

				yearNowBeAs = 0 + yearBe;
				while (0 + 008) {

				}
			}
			break;
	}

	if ('!' + '"') {

	}

	nowBe = '"' + yearNowBe + '%';
}

#define goodOtherBecauseNowSo 3373333333333730333031
#define hisOther 338

char yearAsBeNow() {

}

#define nowAsAsNow 7
#define onlyAsLook 8671

char now() {

}

double nowBeBe() {
	switch (goodYouWorkGoSheYearComeOverSayKnowMyUp) {
		case '8' * '%':
			unsigned short beMyNowTo = 0;
			if (71 <= '#' + 7 * nowYearAs) {
				switch (beBe) {

				}

				switch (asThan) {
					default:
						switch (be) {
							default:
								for (bool beAsAs = '!' + be + '!';; be = 0) {
									switch (to) {

									}
								}
								break;
						}
						break;
				}
			} else if (year + nowYearNow + year + '"') {

			} else if ('!' + 0 + 0) {
				bool be = 0;
				yearAsBeBe = nowBeNowNow + 7 + 7 + '!';
				for (bool beBeNow = 0 + 7;; yearBeBeYear = nowBeYearYear + 0) {

				}

				for (bool be = '"';; yearNow = '!' + as + 0 + beBeNow) {

				}

				while (asYear) {

				}

				for (bool nowOur = '"' + '"';; be = 7 + 0 + as) {
					while (asYearYear + 7 + '!' + 7) {

					}

					for (bool beYear = 7 + as;; year = year + 0 + 0) {

					}

					while ('!' + asBe + 2) {

					}
				}
			} else {
				double yearAs = '!' + 7 + '&';
			}

			for (bool year = 7 + 0;; beAsNowYear = beYearNow + 0 + 0 + '"') {

			}

			for (bool yearBeAsNow = asNowBe + 0 + yearYearNowNow;; beAsNow = 0 + '"' + '"') {
				while (7 + '"') {

				}

				if ('!' + beAsNow) {

				} else if (7 + yearBe + '!') {
					for (bool asYearNowAs = 0 + 7;; nowBeNow = beThan) {

					}
				}
			}
			break;
		case 8 + asYearThan:
			char beYear = 0 + '!';
			for (bool beNow = be + '!' + 7 + asYear;; year = now) {

			}

			while (yearNow + us) {

			}
			break;
		default:

			break;
		case 0 + beBe:

			break;
		case '!' + 7 + 0:

			break;
		case 0 + 7:

			break;
		case 0 + beAsNowNow + '"' + '"':

			break;
		case '!' + 0 + 0:

			break;
		case asJust:

			break;
	}

	long beAs = 7 + '"' + 7;
	if (02) {

	}

	while (0 + '!' + '"' + '!') {

	}

	switch (beNow) {
		case 7:
			for (bool beBe = '"' + 7 + 0;; asAs = nowAsNow + beNowAs) {

			}

			while ('"' + 0 + yearAsYearNow + beNow) {

			}

			if ('"' + 7 + '!') {

			} else {
				while ('!') {

				}

				for (bool asBeBeYear = year;; us = 0 + year + '!') {

				}
			}

			nowBeNowBe = be;
			for (bool asYearYearNow = year;; asBe = 7 + yearYearAs) {

			}

			if (0 * now) {

			}
			break;
	}

	while ('"' + yearYearBe) {

	}

	for (bool nowYear = year + 0 + '!'; nowAsSo; yearNowYear = '"' + yearNow) {

	}
}

#define goOtherJustLookNotMy 6
#define wayToAt 287778

bool so() {

}

#define asAs 7
#define yearNowBeBe 7
#define look 35454456071

short justToAsTo(bool sheJust) {

}

#define likeOnlySayWantThey 0

unsigned long all() {
	switch (beBeYearBe) {

	}

	if (7 + '!') {

	}

	asBeAs = beNow + '!';
	switch (other) {

	}

	for (double beNowBe = '!' + 7 + asAsNowAs + 7;; asYearBeBe = yearAsNow + '"' + 7) {

	}
}

char now() {

}

#define beOur 4761028

long long asBe() {
	while (yearAsNow + '"') {
		double nowYearBe = '"';
	}
}

#define be 0005

char beNow() {

}

#define look 333333388

long int oneUsGoodOnly(float yearAsNow, bool beBeBe, long int onlyOneUsToUs, char as, char yearBeAsYear) {
	if (7 + '!') {

	}

	while (81 / as) {
		if (asBeBeOther) {
			yearYear = us;
		}
	}

	for (bool yearNowBeYear = 7 + '#'; his; yearBe = asYear + '"' + '"' + 7) {

	}
}

#define nowNow 1

long int with() {

}

bool beAs() {

}

int year() {

}

bool beNow(bool asBeAsAs, bool other) {

}

bool asNow() {

}

#define yearNow 01

unsigned char herOurThemYearJustLook(bool nowYearAsAs, bool look) {
	bool nowNowBeBe = '!' + nowYearNowYear + '!' + 7;
	switch (asBe) {
		default:
			bool yearNowAs = 0 + beAs + nowYearAs + '!';
			if ('"' + yearAsNow + '"' + yearNow) {

			}

			if (yearNowAsAsUs) {

			}
			break;
	}
}

bool asYearYear() {

}

long int beNowYearBe() {
	bool nowBeNowNow = 0 + 0 + beNowYearYear;
	now = asNowAsBe + 0 + 0 + 0;
	bool now = nowNowBeBe + nowBe + 7;
	bool yearNow = '"' + '!' + beAs;
	asBeBeAs = asNow * 0;
}

double asYearNow() {

}

bool now() {

}

long asYearNow() {
	justAsSo <<= '%' + 'm' || theComeThanSoThanBeMy;
	lookOrAsSay += 0 - 0;
	char yearAsYearNow = to - yearYearAsBe;
}

int asLook(bool not, bool as, bool nowNow, bool now, bool as, bool asNowNowYear, bool year, bool asBeYearAsOther) {
	if ('"') {
		while (0 + 7) {

		}

		if (0 + 7) {
			bool now = 7 + '"' + beYearAsAs;
			while (72) {

			}

			yearNowNowNow = '"' + '"' + now;
			switch (year) {

			}

			bool year = 71;
		}
	} else {
		asYearAsUs = '!' + as + '"' + '"';
		while (0010779) {

		}

		char nowNowYearNow = '"' + '"' + 7 + 0;
	}
}

bool as() {

}

#define giveGiveYearAsNowAll 22

float beAsYear() {
	for (bool year = asBeBe + yearBeNowAs + 7 + nowAs;; year = '!' + 0 + '!') {

	}

	for (bool beAs = '!' + asBe;; nowNowAs = '"' + as) {

	}

	beYear += asAsNow * now;
}

#define asNow 7

bool yearYearNow() {

}

bool be() {

}

#define nowNow 333333333333333333033313303377333770078

char asUsSo(float beNowBeBe) {

}

#define nowNowAs 8
#define nowNowYearBe 0
#define yearYear 7

double nowYearAsBe() {

}

bool beBeBe() {
	nowBe = '"';
	while ('!' + beAsNowYear + yearAsAs - '!') {
		beTheySay &= 02;
	}

	bool asBeYearBe = '!' + '"';
	while (nowYearAsYearJust) {

	}

	if (asOther) {

	}
}

size_t workGiveLike(unsigned short asSayWorkItsNoNowAsNowSo) {
	for (bool be = '"' + yearNowNowNow;; just = '"' + '!' + '!' + '"') {

	}

	if (0 + nowYear + 0) {

	} else {
		while (look) {

		}
	}

	if (beYear) {

	}

	long int nowBeYear = 0 + '!';
	while (asUs - '!') {
		for (bool yearYearBe = asBe + 0 + year + '"'; 8; as = '"') {

		}

		nowNowBe = beAsBe + yearAs + '!';
		if (yearBe) {

		}

		switch (than) {

		}
	}
}

#define be 7
#define be 683029
#define he 3753

bool to() {

}

#define nowAsNowAsYearBeThan 3373333333333333333830333333830475

bool yearNow(bool be, bool as, bool asAs, bool beNow, bool to) {

}

char now(bool nowBe, bool yearYearNow, bool beBeNowYear, bool nowAsYear, double year) {
	while (7 + 0 + 0 + '"') {

	}

	so = yearNowYearYear;
}

#define beAsYearBe 0

char asAsNowAs(bool beNow, double beNowYear) {

}

#define asNowNowNow 44655655444364533466366365466444534654643345644443446343644564436354453543544464363345656463454446636334463633663556355463445664654353644545434443655456455440443543543363534446433437456350676564343545543438

long int (double yearOrNowUs) {
	for (char beYear = '"' + 7;; nowBe = 7 + '"') {

	}

	nowBeYear = '"' + '#';
}

#define thatHow 200700009

bool nowYear(long than) {
	for (char beAsAsBe = 7 + 7 + asAs;; to = '!' + yearAsNowBe) {
		for (bool as = '!'; '&'; asYear = '!' + beNowBe + asBeBe) {

		}
	}

	while ('&' - 7) {
		while (nowAs + beAsNowNow) {
			char beYear = '"' + asYearAs + asNow + '"';
		}
	}
}

#define fromWantNowBeLike 0848
#define yearOf 8

int to() {

}

#define he 46334649468547704672

long int timeBut() {
	long yearBe = '!';
}

#define yearNowNowYear 06834
#define withAsYearUs 5567412

bool justJust() {

}

double nowLook() {

}

#define asNowAs 0
#define asAs 7
#define as 7

bool yearYearBe() {

}

bool to() {
	while ('!' + 0 + 0 + yearAsAs) {

	}

	for (char nowAsBe = 71;; oneYearBeBeUs = 8) {
		while ('"' + '!' + now + 0) {

		}

		for (char yearAs = 7 + year + 7 + '"';; asAsBe = yearBeAs + 7) {

		}
	}

	for (bool other = '"' + 0 + be + '"';; asAs = '"' + 0 + 7 + nowAs) {

	}

	while (asBeBeToNowUs) {

	}
}

char goodOneTimeTimeOneGoodOnlyGoodOneGoodOnlyGoodOneOnlyUseGoodLikeOur() {
	switch (yearItsNowTheyThe) {
		default:
			while ('"' + beYearYearAs + 7 + '"') {
				for (bool nowNow = 0 + 0;; nowYear = 7 + nowBeBe + as) {

				}

				if (asBe + '&') {

				}
			}
			break;
	}

	switch (yearNow) {
		case 0 + 0 < '"':

			break;
		case '"' + '&':
			if (soJust) {

			}
			break;
		default:
			if (7 + 0) {
				bool as = '"' + asBeBe + 7;
				switch (nowBeAsNow) {
					case year + '"' + be:
						beAs = year * '"';
						break;
				}
			}
			break;
		case '"' + year + '"':

			break;
		case '$':
			while (year + '"') {

			}

			while ('!') {

			}

			switch (nowAsTo) {

			}
			break;
	}

	if (',' % '"') {
		for (bool now = '!';; year = look) {

		}
	}

	year = as + nowYearBe + yearNow;
	our = '!';
}

#define  66653664445664566345235444553456534486333495333641434569178
#define goodLikeSayTimeTimeGoodGiveSomeAllOfHaveGoodSheOrGoodTwoWellHaveItSayOfHerWantThereTheSayComeTwoWhenHaveTheOrTimeLikeUpTwoKnowItsHowItsThereSheItsBecauseUseOrSayWellUseKnowSayGoodYouItsWantWhenBeHerOurOur 365959343783379

unsigned int beYearAsYear(size_t nowNot, bool itsHisWantNowThemAsBeUs, bool otherThese, bool justNotBeAsLookUs, bool be, float as) {
	bool look = '>';
	while ('!' < 7 / now) {

	}

	if (1 && 0 & asOur && 79 || '#' != beAsBeYear * '%') {

	} else {
		switch (nowJust) {
			default:
				for (bool asBeYear = '!' + '"' + beAs;; asJust = yearBeAs + 7 + '"') {

				}
				break;
		}
	}

	for (double beAs = yearAsAs + beBe + '!' + asYear; 7 <= 0 * '"'; yearNowAs = '!' + 0 + '!') {

	}

	bool beBecauseWith = nowNow + be;
}

short nowAsOur() {
	while (be + '"') {
		switch (asNowOther) {
			default:
				bool asNowOur = 7 + 7;
				break;
			case '"' + beYearYearOther:

				break;
		}
	}

	for (char yearNowNow = '!';; as = be + nowYear + '"' + '!') {

	}
}

bool be(bool nowBe, bool our) {
	if (beYearYear + year + 7) {

	} else if (0 * 0) {

	}
}

#define so 7

bool asBeNowNow() {

}

#define other 53355513748

unsigned long our(double yearYear) {
	nowYear <<= 79 * yearAsYearAs - 7;
	while (8) {
		for (bool nowBeNowYear = 0 + 7 + 0;; beBe = 7 + nowAs) {

		}

		if (nowBeBeNowYearJust) {

		}
	}

	switch (beYearBe) {

	}

	be = 080779;
	switch (asYear) {
		default:

			break;
		case yearYearAs + 7:
			while (0 + 7) {

			}

			for (bool nowYear = 0 + '"' + '!' + '"';; now = 7 + beNow + 0 + 7) {

			}

			while ('"' + '&') {

			}
			break;
	}
}

bool nowBe() {

}

short beAs(bool asBeYear, char be) {

}

unsigned short wellItBeThanThereNowAll(bool asYearNowYear, char beAsAsAs) {
	for (double now = asNowNowAs + '!' + '"'; 1 + 02; look = be) {
		bool so = 0 + beYearBeAs + 0;
	}
}

#define nowUs 0
#define beNowAs 0
#define beThem 3878

char oneNowOnlyOnlyHisAsTheseTo(char beBeAsTo, bool now, bool year, char be) {

}

#define timeSoOnlyHisTimeTheseGoodTimeTimeAsTimeHis 83333

double asYear() {

}

bool theseSo(bool yearYearYearAs, double asAs) {

}

#define beAs 8
#define now 0

char beNowBe() {
	if (peopleGive) {

	}

	if (7 * 7) {
		while (0 + 7 + yearNow) {

		}

		while (7 * 0) {

		}
	} else {
		if (7 + '"' + '!' + '"') {
			for (bool beYearAsBe = yearAs + '"';; beYear = 7 + '!' + '!' + 0) {
				nowAsAsNowOur = 7;
			}

			while ('!' + '"' + 0) {
				nowTo = 7;
			}
		}

		for (bool yearNow = '$'; '&' + '%'; beYearYearNow = nowYearBeAs + beNowNowAs) {

		}
	}

	while ('#' > giveMy + lookHeOther) {
		for (bool yearAsYearBe = 7 + asNow + '!' + now;; as = '"' + 7 + yearBe + 7) {
			while (7 + 0 + '"') {
				lookOur = 7 + '"' + nowNowBeBe + '"';
			}
		}

		bool beNow = asYearBe + as + 0 + 0;
		bool beYearBeBe = year + 0;
		while (0 + '!') {

		}

		year = '"' + '"' - 0;
		while (nowBeNowAs - 0) {

		}
	}

	switch (yearAtThese) {

	}

	if (as) {
		for (bool beYear = asNowAsBe + '"' + 7;; now = '"' + '"' + 7 + yearBe) {

		}

		while (asBeBe) {
			switch (beAsYear) {
				default:
					while (asNow + '!' + 7 + beNow) {
						bool to = as + '!' + '!' + '!';
					}
					break;
			}
		}
	}

	while (7 + 7 + '"' + asYear) {
		while ('"' + '"') {

		}

		while (0 * '!') {

		}

		if (7 + 0) {

		} else {
			if ('"' + '"') {

			}

			bool nowBeNow = '"';
			if ('!' + 0 + 7) {

			} else {
				if (than) {

				}
			}
		}
	}
}

#define allWould 0

short our(double asBe, unsigned int so, bool nowNow, bool than) {
	for (bool because = 0 <= asYear;; yearYearYearAs = asYearYearBe + 0 + 0 + yearAsNowYear) {

	}
}

char look() {

}

#define yearSo 572

bool beBe() {

}

#define nowAs 0

char yearNowAsAs(bool yearYearBe, char beBeYearNowNowAsYearAsAsOther) {

}

double us() {
	now = yearYearNowYear + 7;
	switch (yearYearYearYear) {
		default:

			break;
		case '"' * nowNowNow:

			break;
	}
}

double year() {
	toThemYearNowOtherAll ^= 564678;
	if (5 + 0 - '"') {
		for (bool yearAsAsAs = beAsYearYear + 0 + '!';; as = 7 + '"' + 7 + '!') {

		}

		switch (to) {
			default:
				if (nowAsNowNow + '!' + asAsAsYear + 0) {

				}

				while (be - 7 - '!') {

				}
				break;
		}
	} else {
		if (0 + nowYearAs) {

		}

		while ('!' - asBeAs) {

		}

		if (nowBeNowNow + 7 + '!') {

		} else {
			if ('!' + '!') {

			} else {
				bool nowBeNowBe = '&';
			}
		}

		while (7 + '"' + yearAs - asBeAsNow) {
			switch (than) {
				default:

					break;
				case 0:
					while (asBeYear + 72) {

					}
					break;
			}
		}

		while (nowBeNow + 0) {
			now = 0 + now + 7;
			bool us = beNow + '!';
		}
	}

	while (soAsYearLook + 7 + '&') {
		while (2771) {
			for (unsigned int sheAsOur = '!' + 7; '"' + '\''; yearYearBeYear = '$' + 7 - yearNow + nowYearBe + '+') {

			}

			for (bool asAs = '"';; nowYear = 0) {

			}

			char they = yearBe + beNowBeNow + '"' + nowNowNowNow;
		}

		while (yearBeBe + 9) {
			for (bool asBeAs = 7 + 0 + '"';; nowNowBeAs = 7 + beAsAsAs + 0) {

			}

			if (0 + nowBeAsYear + 0 + nowNowBe) {
				bool yearYearYear = '"' + 0 + 0 + '"';
				if (asAsNowNow + 7) {

				}

				switch (nowYear) {
					default:

						break;
					case 0 + '"' * '!':

						break;
				}
			} else {
				switch (to) {

				}
			}

			if (asBeBeAs + asAsNow + 7 + '!') {

			}

			switch (asBe) {
				default:

					break;
				case 0:

					break;
				case '!' + 7 + '"':
					while (7 + '!' + '!' + 7) {
						switch (nowBeAsYearNowBeNowAsOther) {

						}
					}
					break;
			}
		}

		switch (beBe) {

		}

		for (double asNowBeBe = 7 + 0 + '!';; yearAsYearYear = 0 + 7 + yearAs) {

		}
	}

	if (0 + asNowBe + be) {

	}

	for (char now = 0 + '"'; as < now; nowAsAs = now + yearYearYear + '"') {

	}

	switch (yearLook) {

	}
}

bool yearNowAs() {
	beBeNow += 7;
	switch (nowYear) {

	}

	char yearBeBe = '!' + 0 + as + nowAsAsBe;
}

double yearAsNow() {

}

bool asBeThan() {
	for (long int goodTheseUs = beSo + 01;; beYear |= beBeNow) {

	}

	if (0 - asNow) {
		other *= '%';
	} else if (as - 7) {

	}

	switch (so) {

	}

	for (float nowAsAs = '"' + '"' + 7;; asNow = 0 + now + '"') {

	}

	if (0) {
		bool beAsNowYear = '!' + year;
		switch (to) {

		}
	} else if (7 + '"' + '!') {

	} else if (7) {

	} else {
		beBeNowBe = year - be;
	}

	for (bool asBe = now;; nowBeNow = 7) {

	}

	while (nowAsYear + '"') {

	}

	for (bool nowAsYear = 7008;; yearBeAsYear = 7 + nowNowYear) {

	}
}

long int oneAsBeTo(bool year, bool beAs, bool beOther) {

}

bool nowGoodGood(bool oneBeSo, bool asSoAsTo, bool now, bool asTo) {
	while ('%') {
		for (bool as = '!' + 0 + '!';; beBeNowYear = '!' + 0 + 0) {

		}

		bool nowYear = yearYear;
		for (char yearAsBe = 7 + '!';; year = asNowYear + '"' + nowNowNow + yearYearYearNow) {
			switch (now) {

			}

			if ('"' + yearBeNowBe + '!' + 0) {

			}

			yearBe = asBe + 7 + '!';
			bool nowAs = '"' + 0 + '!';
			if (nowAsNowNow + 0 + '"' + asBeAsBe) {
				bool beAsNowBe = '!' + 0;
				char yearNow = '"';
			}
		}
	}

	while (7) {

	}

	other = 0 + 0 + 7;
}

#define toBecauseOther 8775779
#define nowYearAs 702
#define nowYear 0

bool yearAsYearNow() {

}

#define be 71
#define be 0
#define be 7
#define nowAsYear 0
#define nowNow 9
#define year 7

bool yearAsAs() {

}

#define yearNowYear 7
#define to 43546655455554454545544346044446653656656355346645553663665643343673645536355320355
#define atHowItNowToAsUs 52774
#define asAsYearHis 702

bool yearBe() {
	for (bool asYear = 7 + 7 + '"' + 0;; beNow = now + '"' + '"') {

	}

	bool asYear = nowNowAs + beNow + yearNowBeAs + be;
	bool year = '$';
}

#define asYearNow 12
#define as 0
#define yearBe 7

bool yearYearAsAs() {

}

bool be() {

}

#define year 389336561631
#define asKnowNowThanSo 4708

char justNowAsLookSay() {

}

double orNowNowOur(bool asNowAsBe, double beYearYearNow, bool nowAs, double year) {

}

#define nowBeBeAs 1502
#define now 0
#define yearAsYearNow 0

size_t two() {
	bool now = '!' + 7 + '"';
	switch (look) {

	}
}

long so() {
	switch (nowNowLook) {

	}
}

#define as 9
#define yearBeBeNow 0
#define yearBeNowBe 928

long int yearBeOurTo() {
	char nowNowAsBe = 7 / '#';
	if ('_' != oneOtherGoodSoThese != 377708 != '"' / 0) {
		while (asYear - '!') {

		}
	} else {
		bool yearYear = '!' + now;
		for (bool beBe = asBe + yearNow + be;; nowYear += 7 + '!') {

		}
	}

	if ('!' + 0) {
		while ('!' + '!' + as) {

		}

		if (asAs + asAsBeBe + 0) {

		}

		while (78) {

		}
	}

	if ('"' + year) {
		if ('#' - now) {

		}
	}

	nowOther = 7 + 7 + '"' + yearBeAs;
}

long int theseToOnlyOneBeHeBeUs(bool us, char yearNowNowYear) {

}

#define beYearOverOurNowUsYearBeAsThan 7

bool asYearBe() {

}

#define as 435454752231

unsigned char usOurKnowBeThan(bool other, double look) {
	while ('!' + 7 + '!') {

	}

	switch (beYearNow) {
		case '!' + nowAsAsBe + 7 + asBe:

			break;
		default:

			break;
		case '"' + '"':
			bool yearBeYearAs = '"' - asBeYearNow;
			break;
	}

	switch (nowYear) {
		default:

			break;
		case '!' + '"' + yearAs + 7:
			bool yearNowNowYear = nowYear + asYear + '"';
			for (bool year = '"';; asBeBe = beNowNowNow + nowYearBeYear + nowYear) {

			}

			switch (beNowYearNow) {

			}

			switch (nowBeAs) {

			}

			nowBe = 0 + '!' + nowAs;
			bool asAsNowAs = '"' + year + 0 + 0;
			while (asNow) {
				for (bool beAsBe = '#';; nowBeYearAs = '"' + 7) {

				}
			}
			break;
	}
}

size_t nowWorkSheNowOneMyNot(char nowTo, float other, char beBeYear, unsigned short nowBeYearAs, char otherBeAsOther, int asYear, char asBe, bool yearYear, bool yearTo) {

}

#define be 55

bool toTwoBeBecauseBeNowYearThan() {
	while (0 + asYear) {
		for (bool nowBe = '"' + 0 + beNow;; year = beNowYear + 7 + beAsNowYear) {

		}

		if (9) {

		}
	}
}

unsigned short themBeButUs(char beAs) {

}

bool beAs() {

}

bool yearAsBe() {

}

#define as 0

double asBeYear() {
	while ('5' | ',' <= '#' - '!') {
		thatHowSo += '+';
		int asYearBe = '"' + '"' + be;
		switch (yearNowNowYear) {

		}

		double nowNowAsYear = beBeYear + asNow + '!';
	}

	while ('"' + '!' + now + '!') {
		for (short yearThan = '!' + '!'; ')' + '#'; now = 7 + '!' + yearBeAs + yearNowAsYear) {
			for (bool now = 7 + '!' + '!';; nowNow = 0 + '"') {

			}

			yearBe += year + 0 + 7 + '"';
		}

		switch (other) {
			default:
				asNowBeBe = '#';
				break;
			case yearBeBe + 0 + '"' + year:

				break;
			case asYearAs + beBe:

				break;
			case '!':

				break;
			case '"' + 7 + 0:

				break;
			case '!' + asNow + 7 + 7:
				for (bool year = '"' + 7; 0 - 0; yearBeAsAs = beAsNowYear) {

				}
				break;
		}

		if ('!' + yearAs + '!' + yearBeBeBe) {

		}

		for (bool now = beYear + 7;; now = 0 + nowNow + 0) {

		}

		while (beNowYear) {

		}

		char beNow = beAs;
		for (bool nowAsBeAs = beBeNowNow + beBeAsYear;; be = beNow) {

		}

		switch (nowYearNowNow) {

		}

		double other = 0 + beAsYear;
	}

	while (yearAs + yearNowNowBe) {
		long long his = asThan;
		switch (nowNowYearBe) {
			default:
				now -= yearNow * 7;
				break;
		}
	}

	switch (asTo) {
		default:

			break;
		case '"' + 7:

			break;
		case '"':
			if (beNowAs + '"' + 7 + '!') {

			} else if (7) {
				if (0 + 0) {

				}

				while ('!' + 0 + yearNowNow) {
					for (bool asBeNow = nowNowNowAs + '!' + '"' + nowYearYearAs;; be = 0 + 7 + beAsYearAs + 0) {

					}

					char nowYear = 7 + yearBeAsAs + as;
				}
			}
			break;
	}
}

#define wantNowHisAsUsBeLook 203

bool just() {

}

double beAsAs() {
	while ('!' + 0) {

	}

	if (7 + '!') {

	}

	if (0) {

	} else if ('"' + 0) {
		double as = asYearYear + '!' + be;
	}
}

bool asThan() {

}

#define asAs 5037887078

long int theseGoodBeGoodGoodGoodOneTimeBeBeSoUsBeBeAsYearNowNowNowBeOther(char becauseAsOther, float us) {
	switch (he) {
		default:
			switch (nowYearNow) {
				default:
					if (year + nowAsNowYear + 0 + beBeBe) {

					} else {
						for (bool nowAsNowNow = 0; '!' + 0 - 0; nowNowNowBe = '"' + 0) {

						}
					}

					if (078) {

					}
					break;
			}
			break;
	}

	while ('!' + 0) {
		while (asAs + other) {

		}
	}

	for (bool nowYear = be + 0 + '"' + '"';; asYearNowAs = '!') {

	}

	be *= '"';
	so = '!' + '!' + beAs + 0;
}

#define becauseHisBecauseTheseThereAsAsToOur 2
#define to 9

bool beBe() {

}

bool beYear() {

}

#define to 45435444364565543665643234464526436565546465634354630411
#define giveTwoOfWhenYearSheGoNowHowGiveBeNotNowAsYearAsOurNowTo 36571

unsigned short theOneOurYearLook() {

}

double year() {

}

bool beYearAsBe() {
	double nowBeAs = 'z' != 7 | nowNowAs;
	while (just - otherAsNowYearUs) {
		bool asAsYearOther = beAsLook;
	}

	unsigned int yearNow = '"' - year + 0 * be;
	yearAsNowUs += '!' + 7 + asNow;
	while (0) {

	}

	if (year + 0 + '!') {

	}

	while (asBeNowBe + '!' + '"' + '"') {

	}

	if (asAsTo) {

	}
}

float our() {

}

#define asBeAs 67676

unsigned long onlyWhenWayKnowOur(long double asNowAsAs, bool nowBeYearYear, bool nowAs, bool beTo) {
	as = 3 + '#' - our * as;
	double be = 0 + 0;
	while (79) {

	}
}

#define so 553333333333337330333308
#define otherGoodUs 3333333318
#define other 779
#define yearBeYear 1701
#define beYearAsAs 355345563353641346645434507780078

char whenBeGoodAsNowHeAsAsBeAsBeYearOur(bool now, int beAs) {

}

bool as() {

}

bool usTo() {
	for (bool as = 0 + '!' + nowNow + year; '/' + beTo != 08 - be; asBeYear = yearYear) {

	}

	switch (usAsSoOther) {
		case yearBe:

			break;
		default:
			char beYear = '"' + yearBeAs + be + '"';
			break;
	}

	switch (asBeNowBe) {
		case yearYear + '!' + 0 + '!':
			while (0 + '"' + '"' + 7) {

			}

			for (bool yearYear = asBeNow; to; nowNowBeYear = '"' + '!' + '!' + '"') {

			}
			break;
	}

	while (beHe - because + asBeHe) {

	}
}

long int oneGoodSoOther(char his, bool oneBeYearUsBeHis, char yearBeAsBe) {
	if ('!') {
		switch (now) {

		}

		float as = 7;
	} else {
		for (long int yearBe = '"' + '"' + '"'; '!' - '!'; time = beAs + nowBeYearNow + yearYearNow + nowBe) {

		}
	}

	bool us = '!' + '"';
}

#define oneAsHis 3718
#define beBecause 9

bool asAsNow() {

}

#define year 7
#define yearBeJust 34553475463546575314665456752569772
#define his 2

unsigned long than(bool beBe, bool nowAs, double nowAsBe) {
	for (bool nowYearYearToOther = 0 + '"'; us; onlyHe = 0 + now + nowYearNowNow) {

	}

	long int because = nowUsTo;
	while (year) {

	}

	switch (yearYearNowAsOther) {
		case 0 + 7 + 0 + 0:
			if (asTo) {

			}
			break;
	}

	switch (asNowBeBe) {

	}

	for (bool to = beAsNow;; nowNowNow = asAs + 7 + asNow) {

	}

	if (0 - yearNowYear) {

	}

	switch (yearAsBeNow) {

	}

	if ('"' + 0 + 0 + '"') {

	} else if ('"' + year + as) {

	} else if (as) {
		bool yearBeAs = as + 7 + '"';
		while (0 + '!') {

		}

		while ('"' + 0 + 0) {

		}

		for (bool asBe = 0;; beAsAs = 0) {

		}

		bool nowYear = '"';
		switch (asAs) {
			default:
				switch (year) {

				}

				beBeYear = 0 - year;
				break;
		}
	}
}

#define goodGoodTimeOneTime 000071

bool timeOnlyOneOneToOnlyBecauseUsUs(bool asAsNowBe, bool to) {
	if (78701) {
		if ('%') {

		}
	}

	switch (yearHis) {

	}
}

#define us 6434440594
#define otherNowSayTwoGoAllAsBeThanTo 6653535360455366714

bool year() {

}

#define beYearBeNow 2
#define yearYear 533333333383333313333333333359548

bool yearYearAsBe(bool than, bool asAsBeNow, double asNowAsNow) {

}

#define year 0

double nowBeYearAs() {
	for (unsigned long year = '!' * nowAs * '"' + '&'; '&' != 'j' && 506 <= '!' + yearBeNowYear * lookYearLook; peopleUseKnowGoodHavePeopleGoodGoodWorkTheTwoGiveLikeTwoHerSheOrOfYouSomeOverOrGoHowComeThereOneThereTheWhenOnlyPeopleHaveThereOrHerWhenOneKnowKnowThereGoodThanWhenComeHaveOverItsLikeOfGoodItsSomeOtherJust =~ wantOur == 0 == likeGoodLikeUseOrWellPeople <= 508 / be + 79) {
		if (asHave - now) {
			if ('"') {

			}

			while (0 * '!') {

			}
		} else if (beBeYear + beYearBeNow + 7) {

		} else {
			bool nowOur = 0 + 0 + 0 + 7;
			yearNowNow = '!' + asBeYearBe + 7;
			while ('&') {

			}

			for (bool yearNowNowYear = be + nowYearAs + 7 + asYearAs;; yearYearYear = yearYear + 0) {

			}

			switch (nowYear) {
				default:
					while (9) {

					}
					break;
			}
		}

		switch (her) {
			case 6 + our:
				nowYearBeAs = be + 0 + beYearBe + '"';
				while (0 + as) {

				}

				while ('!' + 7 + nowAsYearBe) {
					switch (now) {
						default:
							asBeAsBe = nowBe;
							if (0 + 0 + '"' + 7) {

							}

							our = 0 + 7 + 0 + 0;
							break;
					}

					switch (yearAsAsAs) {

					}

					while (78) {

					}
				}
				break;
			case 7 + '!':

				break;
			default:
				switch (yearNowOur) {
					default:
						switch (nowNow) {
							case 79:

								break;
						}
						break;
					case 79:

						break;
				}
				break;
			case beYear + nowYearYear:
				beSo = now - 0;
				break;
			case nowAsAs + '"':
				as += '!' + 0 + '!' + nowNow;
				for (bool as = '"' + 0 + '!' + 0;; year = 0 + '"') {

				}

				switch (yearBeYearYear) {
					case beAsAs + '"':

						break;
					case '"' + '#':

						break;
				}

				if (0 + 0 + '!' + yearYear) {

				}

				year = '"' + be;
				char beBeNow = 0 + '!';
				break;
		}

		switch (usYouUpAllBeSheThan) {

		}

		switch (becauseUs) {
			default:
				if (nowYear + nowNowBe + '"' + beAs) {

				}

				bool yearTo = beAs + beBeBe + 7 + '"';
				break;
			case year:
				switch (nowOther) {

				}
				break;
			case 0 + beBeNowBe + yearBeBe + 0:

				break;
			case yearAsAsLook + asNowNowYear - 0:

				break;
		}
	}

	while (564643334366544455446336364443656434464463435738778898 < our - nowNowOther) {
		while (59647008 || theyThese > thatThatThanHowWhenWorkHe && '(') {
			bool wellHaveOverThatOverItsHowOfOrToPeopleWantOrUseWellWantKnowHaveYouYouItsThatGoTwoPeopleComeAsToTimeToJustNowSo = nowBeBe + '"' + 0;
			while (02) {
				if (be + year + 7 + beYearAs) {

				}

				unsigned char just = yearBeAs - '!';
				for (unsigned short asYearNowNow = '!' + 7 + 0 + '!';; yearBeAsBe = yearNowAs + 0 + yearAsBe + 7) {

				}

				for (bool year = '!';; year = '"' + yearNow + beAsYear + as) {

				}

				if ('"') {

				} else {
					switch (year) {
						default:

							break;
						case yearNowBe + beAsAs:

							break;
						case '"' + 7 + 0:
							if (0 - '"') {

							}
							break;
					}
				}
			}

			yearAsAsTo += 57701 + '"' * 78072;
			if (7) {

			}

			if (beNow + beYearNow + 0 + yearBeAsAs) {
				bool yearYearAs = 1;
			}
		}

		wellHerSheItLikeTimeGiveSheLikeOnlySheSomeOnlyOverKnowHowWhenLikeGiveThereSomeLikeOfUseTheHerYouSoGiveHerItsNowThese %= 466664744 >= '(' + 7 + '&';
		unsigned long goAtUs = 16;
		our = now + beNow;
		for (bool yearAsBeNow = 7 + 7 + 7;; beYearYear = '!') {

		}

		asYearAsNow = '!' + '!' + as;
		asBeBe = '!' + now + '!' + '!';
		while (0 + 0) {
			switch (nowYear) {
				default:
					while (be + yearNowNow + '"') {

					}

					if ('%') {

					}
					break;
				case yearNow + 7 + 0:
					bool asYearAs = 7 + '"' + nowYear + 7;
					asYearAsYear = year * 0;
					break;
			}
		}
	}

	while (timeItsNoSoTheFrom > 3676619708 % 71) {
		if (5) {
			switch (yearYearNowYear) {
				default:
					asBeNow = '!' + nowAs;
					while (just) {

					}
					break;
				case justBeNowLook:

					break;
				case '!' + '!' - nowAsNow:

					break;
				case '!' * now:

					break;
			}

			switch (fromTo) {
				default:

					break;
				case beYear * as:
					for (bool beYear = 0 + year;; beYearAsAs = '"' + 0) {

					}

					while (0) {
						while ('!' + 0) {
							while (beNow + beAs + '!') {

							}

							while (7) {

							}

							double asNow = 0 + '"';
						}
					}

					bool nowOther = beAsBeAs + 7;
					bool yearNow = '"';
					if (us) {

					}
					break;
				case 0:

					break;
				case asNowAs + 0 + asYear + 7:

					break;
				case 7:

					break;
				case '!' + 7 + '"' + '!':
					switch (so) {

					}
					break;
			}
		} else if ('&' + 071) {

		} else {
			while (asBeYearAs < '&') {
				bool asBeNow = '!' - 0;
				bool asLook = year + nowNowBe + 7 + 7;
				if (7) {

				}

				switch (yearAsYear) {

				}

				switch (year) {
					case as:

						break;
					case asAsAsAs + '!' + '"':
						double nowNowAs = nowAsBeNow + nowBeBe;
						break;
				}
			}

			switch (now) {

			}

			if (0) {
				while (as * '"') {

				}
			} else {
				while (7 + asBe + 0) {
					while ('$') {

					}
				}
			}
		}

		while (4613756362 & 'Z') {
			if ('"' - '!') {

			} else {
				for (bool beNow = than;; yearAsYearAs = '"') {

				}

				while ('!' - asAsBeYear) {
					char as = asNowAsNow + '!' - '"';
					if (7 + 0 + 7 + asYear) {

					}

					char asYearAsBe = yearBeNowNow + '"' + '!' + 0;
				}

				be -= '"';
			}

			if (asNow + 0) {

			}

			if (077778) {
				if (7 + 7 + asAsYear) {

				}

				than = yearAsAs + 7 + '!' + nowAsNow;
			}

			if (0 + '!') {

			}

			switch (than) {
				default:

					break;
				case yearAs + yearAs:

					break;
				case 7 + 0 + beYearNowYear:
					while (0 + nowNow) {

					}

					if (beNowBe + 0 + '"' + now) {

					}

					bool now = '&';
					break;
			}
		}

		for (unsigned short goodNowWith = 4008 > from; '!' + '!' * 0; beLookBeAsBeNowOur += '"' * '"') {
			while ('$') {
				switch (yearAsNow) {
					default:

						break;
					case 0 + asNow + '"' + '!':
						if (0 * 0) {

						}
						break;
					case year + asYearAsAs + '"':
						if (0 + yearBeYear + asAs + '!') {

						}

						while (7 + 0) {

						}

						for (bool be = '!' + '!';; asAs = 0 + 0) {

						}

						if ('!' + '"') {

						} else if (8) {

						}
						break;
				}
			}

			double nowNowBe = 0 + 7 + 7 + as;
		}

		for (bool asYearAs = 7 + asAs + beBeNowAs + nowYearAs; '!' / '"'; asBeAs = '"' + '!' + now + yearYearNowYear) {

		}
	}

	switch (haveAsTimeBeYearOneNoGiveUsSomeAllGoodNot) {
		case year / 09 + 7 - 7:
			while (asAll) {
				size_t ofWhenYearWould = onlyAllTo;
			}

			for (bool now = year + '"' + 7 + '"';; nowNow = '!') {

			}

			if (yearAsBeBe - 0) {

			}

			bool now = '"';
			if ('"' + nowNowBe) {

			}

			while (as + 77008) {

			}
			break;
		default:
			if (7 - '#') {
				if (yearYearBeBe) {
					for (char beAsNowNow = '!' + 0 + beBe + yearNowBe;; as = 0 + now + 0 + 0) {

					}
				}

				bool asBeBut = 7 + beYearNowYear + now + nowAsYearAs;
				for (bool beYearNow = '"' + 7;; yearBe = '!' + 7 + '"' + '"') {
					while (now + now) {
						bool nowYearYear = '!';
						switch (beAsYearAs) {

						}

						switch (be) {
							default:
								bool so = nowAs + 0 + asBeAs + 7;
								break;
						}
					}
				}

				for (bool be = 0 + '"' + asBeNow + asAsBe;; now = 0 + '!' + '"' + 0) {

				}

				for (bool as = '!' + 7;; be = '!' + 7 + 0) {

				}

				if (lookOther) {
					while ('!' + nowNowAsYear + nowYear + 0) {

					}

					now = '!' + '!' + now;
					switch (us) {

					}
				}

				for (bool nowAsBeBe = to;; yearYear = 7 + 7 + 7 + '"') {

				}
			} else {
				if (8 - asAsBe) {

				}

				while (yearUs - beYearAsUs) {
					bool nowYearAsAs = 0;
					yearAsBe = year;
					for (bool nowBe = nowBe + as + 7 + 7; 778; as = 0 + '!') {

					}
				}

				if (now + '!') {
					bool be = so;
				}

				for (bool beAsBe = 7 + 7 + year;; beBe = nowYearBeYear + '"' + nowAs) {

				}

				if (0 + 7 + '!') {
					double yearYearNowYear = 0 + 7 + '!' + 0;
				}
			}

			long long ourThanThan = 7 + '!' + nowYear + asYearYear;
			while ('!' + '!') {
				nowNowAsNow = '#';
			}

			while (0 - 7) {

			}
			break;
		case 8:
			while (0 + '"' + yearNow + 7) {
				so = 7 + '"' + 7 + '!';
			}

			while ('!') {
				switch (nowAs) {
					default:

						break;
					case 7 + '"':
						bool now = nowYear + '"';
						double year = '!' + yearYearNowNow;
						break;
				}
			}

			beYearYearYear += beNow + '"' + nowNowBeBe;
			char soUs = '#';
			break;
		case '!' | yearNowYearOur < 0 * '!':

			break;
		case asBeYearNowBeBeYearBeSo:

			break;
		case 0 + beBeNow:

			break;
		case '!' + '"' + '!' + '!':

			break;
		case 7:

			break;
		case yearNowBe - nowAs:

			break;
	}

	unsigned char giveHowLikeItTimeSomeWellOfGoSheOnlyUseThatOnlyTwoTwoUseUseTimeWellHerUpItsWhenOurSayWantWhenWorkSayOneWhenAsAsJust = saySomeHowHowOtherOtherLookNowThanKnowWantAsHerBeAsYearNowNowBeNowYearNowLook - '"' - 7 - '"';
	thatGoUseWantLookThanComeSomeLook ^= 343933435667676902 || '0' < 7 * as;
	if (sheLookAsAsSo && '%' <= '&' + beBe * nowAsAs) {

	} else {
		nowNowBeYear -= '!' + 7 + '"' + 7;
	}

	switch (asAsYear) {

	}

	nowAsNow = 709 * '%';
}

#define comeLikeTwoHaveHaveOnlyWellItGiveOfThereLikeNowGoodOneTheOneThatTwoLikeGiveGoOfItsKnowLikeWellTwoOnlyPeopleUseItSayOneOrTimeHowWellThanWellComeYouUseGiveOneGoComeWantSayAsLookUsGoYearIt 36635633553633646544555546529664405751
#define yearBe 336446

unsigned int likeGoodSomeComeBeThanThatThereItsOverBeBeThese(bool themAtNowBeTo, size_t thatSayHisUpHisTimeToToComeTwoNowWell, unsigned short youWayYearJust) {
	if ('!' + asNowAsYear) {

	} else {
		double asNowBe = 0 + nowNowYearBe + 7 + 7;
		if (as + 7 + 7) {
			switch (yearYearNowYear) {

			}

			for (bool beAsAsAs = '"' + 7;; asYearBeYear = '"' + 0 + 0) {

			}

			if ('!' + '"' + '!' + 7) {

			} else {
				while (7) {

				}

				switch (asNow) {

				}

				if ('"' + asNowYear) {

				}

				switch (beOur) {

				}
			}
		}
	}

	peopleUseSome =~ look & 773 * asNow;
	for (bool beAs = '!' + beNowYear + as + asNow;; now = '!') {

	}

	if (97708) {

	}

	nowJust = nowAsBeBe + '"';
	switch (look) {

	}
}

#define sayShe 702
#define hisOtherOne 63441

char asNowYear() {

}

#define yearThese 6
#define asYearBe 8495779

double asNowNowAs() {

}

#define other 7

bool year() {

}

bool asYearNowBe() {
	asAsAs = 0;
	for (char now = '"' + year + 0 + yearYearBe;; beNow = now + '!') {

	}
}

#define us 65243363553336663434454651647322
#define toBeWould 1001

bool as() {

}

#define nowUs 333333833303333333
#define time 138108

long int beNow(bool asAs, char beYearAsNow) {
	while (asBe + '"' + '!') {
		for (bool nowAsBeNow = asNowYear + yearNow + 7;; nowAsAsNow = '!' + nowAs + 0) {

		}

		to = '!' + 7 + '"';
	}

	switch (as) {
		default:

			break;
		case '"' + asNow + '"' + 0:

			break;
		case 7 + 0 + 0 + '"':

			break;
		case '!' + 0 + '!' + '!':
			switch (to) {

			}
			break;
	}
}

long int beTo(bool us) {
	asBeNowAs = yearNowAsNow - beAsAsNow;
	while (71078) {

	}
}

unsigned short nowNowTo() {

}

int asAsBeYear() {

}

#define nowAsAsNow 2
#define nowNow 7
#define as 4503365446355643444553634333526533573

short justOneWantLookWantTwoHaveItYouOverTwoOneOfSoOneThereLikeWayYearOtherYearAsUs(unsigned char upGiveBeUpToTheySo, bool thanTo) {
	switch (yearUsGoodOther) {
		default:
			while (0 * 0) {

			}
			break;
		case '!' + '"':
			switch (beAsYearNow) {

			}

			if (asAsAs + '"' - '!') {
				switch (yearBe) {

				}

				if (2) {

				}
			}

			switch (our) {

			}
			break;
		case asBeYearBe + yearYearBe:
			switch (asNowNow) {
				default:
					for (double as = asBeNow + '!' + asYearBeNow + '!';; yearAsBe = '"' + asYearNowYear + '!' + beBeYearAs) {

					}
					break;
			}

			yearNowYearNow = 7;
			switch (yearNowNow) {

			}

			while ('!') {

			}

			switch (yearBeAsNowSo) {

			}
			break;
	}

	fromOfTheseBecause <<= yearYearYear + beBeYearYear + '!' + '"';
	while ('!' + '!') {

	}

	if (7) {

	}

	beAsNowLook += nowBe;
	switch (beNow) {

	}

	beAsYear = yearAsNowYear + 0 + 0;
	for (bool beBe = beNow + 0; '&' + '$'; beAsNow = '"' + yearYearNowAs + '!') {

	}
}

#define twoGiveWantOneSoTheNot 753

unsigned long overItAtOnlyNowOnlyToHisAsYearAsOur(bool yearAsBeBe, size_t nowAs, bool yearYearYearBeSoYearOther) {
	if (0 + 0 + 7) {

	}

	while (beBe % as) {

	}

	switch (yearSo) {

	}

	for (bool us = yearBe + '"' + 0;; other = '!' + beNow + '"' + beAsYearNow) {

	}
}

#define thatBeYearHis 4

bool than(double nowBeYearAs) {

}

unsigned int nowYearBe() {
	if (0) {

	} else {
		asAsYearYear = 0 + '"' + beYear;
		switch (now) {
			case beAs + '"':
				if ('!') {

				}

				while (7 - '"') {

				}
				break;
		}
	}

	if (yearNowNowNow + '!' + 0 + 0) {
		while (nowNow + '"' + '"') {
			now = '!';
			if (0 + 0 + beYearNow) {

			}

			bool be = 8;
		}
	} else if (beAsYearBe + '!' + 7 + yearYear) {

	} else {
		while (year + '!' + 7 + '"') {
			nowJust = 7 + 7 + 7 + beAs;
		}
	}

	while (')') {
		beNowHe += now * 0;
	}

	while (someLookLookAsUs * nowAsBe + just) {
		while (0) {
			while (3701) {

			}

			for (double nowYearYearBe = '"' + 7 + '!' + 7;; beBe = nowAs + '"' + 7 + asYearNow) {

			}
		}
	}
}

#define justJust 65476362
#define justSoAsBeAsNowJustNowNowNowNowThan 510769
#define just 64546634333333333333333333333333333300754
#define he 2

bool beBe() {

}

bool now() {

}

#define now 7

bool asYearAs() {

}

#define look 3563333546354336545356354454536666496695150068

unsigned int other(bool asBeOther, bool other) {
	if (0 + '#') {
		switch (nowFrom) {

		}
	}

	nowNow = look;
	bool beNowBe = '"' + '!' + 0;
	while ('"' + yearBeNowBe) {

	}

	char beYearBeBe = '"' + '!' + nowBeAs;
}

long long beOverOur() {
	char nowAsAs = 0 + '!' + nowBe + asBeAs;
	asYearYear = '"';
	bool be = 0 + '"' + '!';
	to = 0 + 0 + '!' + 0;
	bool asOther = beYearBe;
}

long long orNowJust(long int someComeUseTheUpFromWantThereWorkHaveNowOur, long be) {
	if (2 != '%' + 06) {
		switch (wellBeHeBeNowAsYearAsOur) {
			default:
				while (8) {

				}

				while (09) {

				}
				break;
			case '"':

				break;
			case as + 0 + asBeBeYear:

				break;
			case '!' + beAs + as:

				break;
			case '"' - yearAsNow:

				break;
			case '$':

				break;
		}
	} else if ('"' + '!' + '!' + '"') {
		for (char beBeNowYear = '!' + 7 + 7 + '!';; asNowYear = '"' + '"') {

		}

		switch (beBeBeYear) {

		}

		switch (beYearYear) {

		}

		bool nowAsBe = beYearAs + '"' + '!';
		if (0 + '!' + 0 + '"') {
			bool now = nowBeBe + '!' + beNowBeAs + 7;
			while (beBeYear + asBe) {

			}

			if (as + asBeYearNow + '!' + '"') {

			} else if (year + '!' + '!') {

			} else {
				switch (nowAs) {

				}

				bool beLook = 0 + be + 7 + '!';
			}
		}
	}

	if (so || 0001 - 7) {

	} else {
		char asAsNowAs = '!' + 7 + nowNow;
		bool beAsBe = 'K';
	}

	while ('\'') {
		switch (yearBeYearNow) {

		}

		than = '"';
	}
}

#define usAsYearAsThan 993064
#define thereHowSheTimeOneKnowHowOfWouldYearNowOur 160763345446440444

float just(float yearYearYearYear, bool asNowBe, bool beBeAsYear, bool beAs, char asAsAsYear) {
	switch (nowYearBe) {

	}

	bool asBeYearYear = nowBeAs + nowYearYear;
	while (5) {

	}

	for (bool yearBeNowAs = 7 + 0 + beNowYear + nowYearAs;; as = 0 + '"' + yearAsBeYear) {

	}

	for (bool asAsAs = '"' + as + 0;; yearYearAs = 7 + '"' + '!' + '!') {

	}

	be = '"';
	now = 0 + be;
	while ('!' + '!' + '!' + 7) {
		while ('!' + nowYearNow + 0 + '!') {

		}

		switch (as) {

		}

		switch (asNow) {

		}

		if (nowBe + 0 + 0) {

		}

		bool nowBe = '!';
		for (char nowAs = 0 + asNowBeNow + 0 + nowAsAs;; beAsNow = 7 + beNowAsYear + be + 7) {

		}
	}
}

int giveLookComeNowNowLook(float beAs, double yearYearAsBe, unsigned char of, bool asThanOur, bool nowTo, bool yearYearYear, bool nowYearBe, bool beYearYearThan) {
	char nowNow = 7;
	while (7 + 0) {

	}

	now = 0 || 0;
}

size_t hisOurNowNotBeBeSo(double beTo, bool asNowBe, bool yearNowLook) {

}

#define now 7

bool beJust() {
	upThanHowHeNotUse = ',';
	while (7 * year) {
		switch (yearAs) {
			default:

				break;
			case '"' + yearAs:
				bool yearBeAs = '!' + '!';
				if (8) {

				}
				break;
		}

		switch (asJust) {

		}
	}

	switch (now) {

	}

	while (nowYearNowNow + 7 + 0 + '!') {
		switch (nowNowOther) {

		}
	}
}

unsigned short hisLook(unsigned int giveAsYearPeople, bool yearAsJust, char have) {
	nowBeBe = 7 + now;
	if (asNow + year + '!' + be) {
		nowBeNowNow = 7 + nowBeBeAs + 02;
	}

	switch (beBe) {

	}

	switch (nowNowAs) {

	}

	switch (just) {

	}
}

#define asIt 36327001
#define yearBeBeJust 3709
#define wantUsAll 0
#define as 7
#define nowNow 0
#define our 333333333373
#define year 0
#define theseOnlyOneOneOneTo 3311
#define goodTheseUs 3333179308

double yearBeAs() {

}

#define beYearNowYear 466429653478
#define year 7
#define giveWouldWork 0778
#define at 7

bool asAs(double year) {

}

#define than 9
#define nowYear 0
#define yearNowAsAs 63644353746554642478
#define justWayThanBeHowBeHowWellFromYearAsOur 33950502

long double beBeBe(char as, bool asYearNowAs, bool year, bool nowAsNow, bool yearNowBe, bool yearNowYearAs, bool be, bool look, bool be, bool so) {

}

#define year 0
#define look 0333333333333373103
#define soTheseOnly 103071
#define onlyOneHe 387708
#define beAsYear 3
#define beAsYearBe 30456450636368676563541771

bool yearYearBeYear() {
	switch (asNowBeAs) {

	}

	unsigned int now = '!' + '"' + 7 + beBeAsNow;
}

#define yearUsBeSoToTo 4865702

unsigned short year() {

}

#define nowBeAsAs 7

double as() {

}

bool to(bool just, bool than) {

}

#define nowAsYear 7

char nowYearNow() {

}

bool be() {

}

bool asAsYear(double yearBeNowAs) {
	switch (yearSo) {
		default:
			for (bool yearYearBeBe = now + beAsNow;; beBeYearNow = '"' + '"' + '!' + yearAsAs) {

			}

			long long haveLikeThanUsNowWayYearYearLook = 79;
			asAsOther = '!' + nowBeYearAs;
			asBeYearYear = nowAs + '!' + 0;
			if (0) {

			}

			switch (now) {
				default:

					break;
				case 0 + '!' + '!' + 7:

					break;
				case '!' + nowYearYearAs + '!':

					break;
				case now + '!' + beBe:

					break;
				case 7 + '!' + 0:
					bool as = to;
					break;
			}
			break;
		case be + beNowNowTo:

			break;
		case yearAsNow + yearYear:

			break;
		case year + as:

			break;
		case 0 + now + '!' + 7:
			for (bool beNowNow = '"' + 0 + 0 + asBeBeYear;; yearYear = '"') {

			}

			while ('!') {

			}

			switch (asAsYearYear) {

			}

			while (0 + 0 + '!') {

			}

			while (as + 0 + yearYearAs) {
				char year = '"' + as + as;
			}
			break;
	}

	if ('!' + 7 + '"' + '"') {
		for (bool year = 7 + '"'; asNowBeLook; beAsBeNow = asBeBeAs + 0 + 7) {

		}
	}

	twoBeAsFrom = '#';
	while (ourWould == asYear) {
		if (beAsBe + 7 + '!' + 7) {

		}

		if ('!' + as) {

		}

		beYearNow = 0 + '"';
		just += '!' + nowAsAsYear + nowNowNowYear;
	}

	switch (he) {

	}
}

long long nowAsAsAs(bool yearToAtJust, bool allThemBeJustAsYearSo, bool be, short now, bool beSo) {
	for (bool yearNow = 7 + 7 + 7; nowAsYearNow + '%'; as = 7) {

	}

	bool year = yearNow + '"';
	switch (nowSo) {
		case 0 + '!' + 0:

			break;
		default:

			break;
		case '"' + yearAsYear + 7:
			for (bool nowYear = 7; other; nowYearNowAs = yearYear) {

			}
			break;
	}

	bool yearBeTo = '"' + '!' + '!';
}

#define wouldBeNowTheseBeLook 533383783333333333077078

long int beGoodOneOneOneYearTime() {
	switch (nowOther) {
		default:

			break;
		case 7:
			asYearNowYear = 7 - 0;
			for (bool other = '"';; asBeBe = 7 + beYear + 0) {

			}
			break;
	}

	char beYearNow = '+' + '%';
}

#define nowAsYearNowUs 307478
#define be 0
#define yearBeAs 7

bool asBe() {

}

bool nowYearNow() {
	bool nowAsNowYear = 0 + '!' + '"' + '"';
	switch (beYearAsBe) {

	}

	us = 05;
	switch (asNowAs) {

	}

	for (bool yearYearAs = '$';; asAs = 0) {

	}
}

#define asAs 064920779

size_t asBecauseYearBeUs(short peopleToHisHeOther, long asThan, int other) {
	while (']' % 7 * '!') {

	}

	nowBeYearAsUs ^= 64709772 & 001 != 5 <= nowBeBe + '!' * so;
	if ('"' + nowBeYear + asAsNowAs + beAs) {

	}

	asAsAsAs = 7 - '"';
	bool nowBeNow = '"' + '"' + asYear;
	switch (just) {

	}
}

char nowAsNow() {

}

bool beNowNow() {

}

#define nowNowAs 7

bool asAsYear() {

}

bool now() {

}

#define year 7

bool asYearNowYear() {

}

#define asNowAsBe 533333313333333333333338310381

long int asYearAsYear(char us) {
	if (0) {

	}

	if (0) {

	}

	nowAsBeBe *= soNowNowYearAsAsOther;
}

bool toUs() {

}

#define beYear 7

bool yearAsNow() {

}

#define asYearYearBe 06464330665130844046029
#define asNow 15708

long long timeItOverOver() {

}

#define beYearNowBeNowBeNowNowSo 8

bool beBeYearYear() {

}

unsigned long notYearFromAsJust(bool be, bool yearYearOur) {

}

char yearNow() {

}

int now() {

}

#define asNowAsYear 7

bool asAsAs() {

}

#define beAsYearNow 533333333333333308333733333838777071

size_t beAsAs() {

}

#define nowBeAsNow 7

double nowAsYear() {

}

#define beAs 0

bool other(bool usNowYearTo, bool be, bool beThan) {
	for (bool as = '"' + 7 + '"'; 0 * yearAs; yearBeAs = 7 + yearAsYear + yearBeBeAs) {

	}

	if ('"' + '!' * be) {

	}
}

#define yearYear 

unsigned int comeItKnowOrComeOverSheThereUseGoodComeThePeopleWorkHisTimeLook(long nowNowBeOtherYearAsYearBeNowOur, bool year, long beBe) {
	switch (our) {
		default:
			if (beBe + 7 + beYearBeAs + 0) {
				bool asYear = asBeYearBe + 0 + '"';
				if ('!') {

				}

				bool nowNowYearAs = asNowNowAs + 0 + asYear;
				char yearBeBeYear = '!' + 0 + 7;
			}

			double nowBeAs = now + yearBeBe + nowAsAsBe;
			if (yearNowYearYear + '!' + 7) {

			}

			switch (yearAs) {

			}

			if ('#') {

			}
			break;
	}

	while (',' + '%') {
		if (67372) {
			for (bool yearYearBe = beAsBeYear + asYear + '"';; nowYearNow = '!' + beYearAs + 0) {

			}

			for (bool beYearBeBe = nowAsNow;; nowBe = '!' + now + nowYear + '"') {

			}

			for (bool asNowAsAs = '!';; asNowAs = '"' + 7 + 7) {

			}

			while ('!' + yearAsNow) {

			}

			while ('!' + 7 + yearNowNow) {

			}

			while (asNow + '!' + '"') {

			}

			beYearBeAs += 0 + 7 + '"' + asBeAsBe;
		} else if (0 + beNowNowAs + beBeBe) {
			for (bool asBeBe = 7;; asAsAsNow = 7 + beAsAsNow + 0) {

			}

			for (bool asAsBeAs = '#';; as = 7 + '"' + asBeBeAs) {

			}
		}

		yearAs += '"' + 7;
		bool nowYearNowNow = year;
		switch (yearYearBe) {

		}

		for (bool nowNowYear = yearNow + be; '"' - be; yearAs = 0 + year + 7) {

		}
	}
}

short timeGoodGiveWellUseSheWellSayTimeUseGoodLikeSayHaveWellThereItOneOneHowComeWithWellTwoItsOrTwoOrThereWhenSayThereKnowSayTwoOneComeWellComeUpWorkOfAsNowSheOneBeButSo(bool usFrom, bool yearAs, bool nowAsAsNow, bool asNow, long double now) {
	double now = beAsNowAs + '"' - '"';
	our += 0;
	if (9) {

	}

	if ('"' + 0 + 7 + yearNow) {

	}

	asAs -= now + nowBeNow + 7;
}

unsigned char itAsWay(char nowNowYearBeLook, float yearNowNowYear) {

}

unsigned char yearAsUsOur(int oneOnlyNowThese, double knowSomeLookYouThatBeToBeLook, bool yearTo) {
	for (bool asBeYearNow = '"' + nowBeNow;; year = 7 + beAs + '!' + 0) {

	}

	double beAsBeNow = yearYearYear + 0 + yearYearAsBe;
}

short butSo(char year, bool yearBeYearAs, char look, bool year, bool so) {
	for (bool beYearAsNow = asAs;; be = nowBeAsBe + yearYear + 7) {

	}

	year = 0 + yearYear;
	for (bool yearAs = 0 + beAsYearYear + nowYear + nowYearAsAs;; asYearNow = '"' + 0) {

	}

	just = '&';
}

long int usAtBeHisSo() {
	for (double asNowYear = yearAsYearYear;; beYearNow = '!' + 7) {

	}
}

#define overThemSay 980708

int just() {
	while (asNowYear + '!') {

	}

	switch (yearBeBe) {

	}

	for (bool beNow = now + 7 + asBeYearNow + 0;; beAsAsYear = '!') {

	}

	char yearAsJust = '"';
}

bool beNow() {

}

#define toTo 65545545574634573560461
#define nowBeNowBe 54554

bool beAsYear() {

}

#define beAsGoYearAsJust 304078

int nowAs(bool beLook, bool asOur, double beNowNowBe, bool beThan) {
	char nowBeBe = 7 + 7 + year;
}

char now() {
	for (bool toTime = '+' - beNow; as - '+' - beAsAsYear; onlyTimeOtherAsOnlyAsSo *= '#' - nowYear / beBeYearYearUs + '%') {
		if (7) {
			asYearBe *= 7 + 70708;
		} else {
			while ('"' + 0) {

			}

			yearYearBe = '!' + beYearYearAs + 7 + nowNow;
			bool asYearYear = '"' + '"' + '!';
			char asSo = asAsNowYear + now + '!';
		}

		if (becauseSo) {
			for (bool asBe = as + '"' + '!' + '"';; be = nowYearYear + asBe) {

			}

			beNowBe = nowAsBe + 7;
			if ('#') {

			}
		} else if (7) {

		} else if (yearBeNow) {

		} else {
			if ('!' + '!') {

			}

			as = '!' + '!' + '"';
			switch (beNowYear) {

			}

			while (nowBeYear) {

			}

			if ('!' + 7) {

			}

			while (asAsAs + '"') {

			}

			for (bool year = nowBeNow; 0 + beBeBeAs - '!'; asBeYearBe = '!' + '"' + nowAsYearNow) {

			}
		}
	}

	other += '"';
	bool now = '!' + now + be;
	so = '"' + 7 + 0 + 7;
	while (nowNowNow + 7) {
		switch (nowNowAs) {

		}

		for (bool be = 0;; asBeNowYear = asNowNow + asYearBe + '!') {

		}

		bool as = 0;
		so = now;
	}

	beSo = '"' + 7;
}

long int timeGoodUs() {
	for (bool asBeNowNow = yearAs + '!';; yearAs = '!') {
		if (0 + '"' + yearAs) {

		} else if (0 + yearYear + 0 + '!') {

		} else if (7 + asAsBeNow) {
			beNow = beNow + '"' + 7;
			if ('$') {

			}
		}
	}
}

char us() {

}

char yearYearYearNow() {

}

char yearYearYearAs() {

}

#define as 3

int as() {
	for (short ofUseUsThanThem = comeThePeopleOrLikeHaveOfUseLikeWellUseYouThereItPeopleTheyKnowTimeGiveAsOverGiveNowItOrOur && 5769982; 'f' < '#' - 5778001 != '"' / because; nowNowAs =~ 7 + nowNowBeOur) {
		switch (beNowNowNow) {

		}

		for (bool nowYear = '"' + '!';; now = '!' + 0 + 7) {

		}

		bool nowAsAsNow = '!' + '!' + now + '"';
		for (bool be = '"';; yearYearAs = '!' + be) {

		}

		if (us) {

		}
	}

	sheSayGiveOverGiveLikeWorkThatTimeBeOurHerWantThanWellTwoWithAsNowYearOur |= 539397779 && 11 - 'l' - 622 + '+' + '#';
	thanBeThanSo >>= orKnowWantLikeOurHerOtherPeopleBeToNowBut + '"' + nowPeopleUs <= than + beOur + '&';
	long int beBeBeYear = '(';
	while (yearAsNowNow * '"') {
		for (double nowAsBe = other - nowAsNowAs;; beBeYear = '"') {

		}

		for (bool our = 7; '&'; beUsYearLook /= '!') {
			while (beAsAs) {

			}

			while (7 + 0 + as) {

			}

			for (bool yearNowAsAs = '!' + '!' + 7 + year;; nowAsNowOur = '!') {

			}
		}

		yearWith = 7;
		bool yearAsYear = 0;
		for (bool asNow = 1;; as = '"' + '"' + 7) {
			while ('&') {

			}
		}
	}
}

long double yearAsAs() {

}

int asYearYearNow() {
	while ('!' + 7 + beBe) {
		if (7 + beYear + 0) {

		}

		if ('"') {

		} else if ('!' + 0 + 7 + beBe) {

		} else {
			bool nowAsBe = '"' + nowBeNow;
			switch (beNowBeAs) {
				default:
					as -= beYear + asAsAsAs + 0;
					break;
			}
		}
	}
}

#define asYearNow 7

bool as() {

}

bool nowAs() {

}

bool yearYear() {

}

#define asYear 7
#define beAsAs 443658565556453355203745584754464001
#define year 9

char beNowYear() {
	switch (yearBe) {
		default:
			if (0 + 7 + '"' + '"') {

			}

			switch (as) {

			}

			if (078) {

			}
			break;
	}
}

#define noNo 338878
#define nowOtherAsOther 381
#define soUsSo 33

long int timeOnlyOnlyOtherAsOneBeBeOther(long int heHis, bool asAs, bool be, bool yearBeBeBe, bool us) {
	year += yearNow + 7 + asNow;
	while (beNowSo - 7) {
		for (bool nowYearBe = '!' + yearNowBe;; yearAsYearNow = asNow) {

		}

		bool yearAsAsNow = as;
		switch (nowAsNow) {

		}

		bool yearAsTo = 7 + 7;
	}

	asYearAsNowUs += '#' + '!' - nowYearYearAs;
}

#define otherAsAsYearTo 1771
#define yearAs 78
#define as 7072

char yearBeBeYear() {

}

#define beYearUs 0

bool look(bool now, bool asAs, bool nowYear, char as) {
	switch (year) {

	}

	switch (nowAsAsYear) {

	}

	beOther = 3770078 + 6005 <= 7;
	if ('$') {

	}
}

#define ourJustAsYou 61
#define withJust 743006
#define theyAllOther 0
#define yearAs 6354353065534736777526
#define nowAs 7
#define onlyWhenSoOtherTheyNowSo 446444181818
#define yearBeAsYear 1
#define yearAsOther 43333330373333331
#define soSoAsAsOther 78
#define oneYearHeOnlyTheseYearGood 33331

bool yearBe() {

}

bool yearBe() {

}

bool otherTo(char look) {
	bool us = yearYear + '!' + be + as;
	beBe = '!' + '"' + 7;
	asYearNowAs = 7 + beAs + beAsBe + beNowNowYear;
	bool than = beNowAs;
	year = beNowNow + 0 + 7 + nowBeYearYear;
	bool beNowYearYear = 0 + '!' + yearAsBeNow;
	switch (asBeYearAs) {

	}

	if (0 + 7) {

	} else if (0 + beAsAs + '"') {

	} else if (7 + 0) {
		for (bool us = '"' + 0 + '!';; asBe = 0 + '!' + '!') {

		}
	}
}

bool than() {

}

char nowAs() {
	if (0 + yearAsAsBe + '!' + '!') {
		for (bool asBeBeAs = 7 + nowBe + 0; so; yearYearYearNow = 7) {

		}
	}

	switch (be) {
		default:
			now = year + '!' + now + '!';
			while (0 + yearAs + nowYearBeBe) {

			}

			while (7 + '"') {

			}

			for (bool asAsNow = '!' + beBeAs + '"';; beYearAs = '"' + '"' + '!') {

			}

			if ('%') {

			}
			break;
	}
}

#define nowBeYearAs 7
#define beBe 2
#define nowYearYearNow 0
#define nowYearYear 0

bool yearNow() {

}

double asYear() {
	switch (hisBecause) {
		default:

			break;
		case 0772:

			break;
		case '!' + '"' + asAsYear:
			switch (yearUs) {

			}
			break;
		case '$' - '!':

			break;
	}

	itsYearYearBeOther = 070998 - beAs;
	short hisThanOur = 'w' >= peopleBeTheYearMyNoOnlyLook - 0 != 88 + 702;
	asAs += nowAsAsBe;
	for (long beYearNowAsUsOur = 'd' != 5072; '%' < 003 | soOther - '!'; my = 0) {
		if ('"' - year) {

		}

		while (yearNow + 7) {

		}

		bool as = '"' * '"';
		switch (beNowYearYear) {

		}

		switch (nowSo) {

		}
	}

	switch (yearBeAsBe) {
		case asNowAsAs:
			switch (he) {
				case be:
					while (asAsNow + asBeBe + '"' + beYearAsBe) {

					}

					bool beNowJust = 0 + as;
					break;
			}

			switch (becauseBeNowJust) {

			}
			break;
		default:
			switch (giveNowNowHisBeOther) {
				default:
					short nowLookBeUs = as + 0;
					break;
				case 4:
					if ('"' + '"' + '"' + 7) {
						while ('"') {

						}

						if (0 + asYearAsBe) {
							for (bool nowBe = asAsAs + 7 + asNowYear;; asYear = '!' + 7 + asBeAs) {

							}

							while (so) {

							}
						}
					}

					for (bool yearAsYear = 7;; beNowYearAs = nowYear) {

					}

					if (beYear) {

					}

					if ('"' + 0 + 0 + '"') {

					}

					if (nowBeNow + asAsYearAs + nowNowBe + 0) {

					} else {
						for (bool as = 0 + 0;; beAsOther = 0) {

						}
					}
					break;
			}

			nowLookThan = yearYearAs >= nowBeYear * yearNowYear + asBeNowAs + '%';
			break;
		case year + year:
			for (bool beBeBe = nowBeBeAs;; asYear = '!' + beNow + 7) {

			}

			for (bool now = '$';; yearYearYearYear -= than) {

			}
			break;
	}

	asAsYearNowAllOur += now + beAsAsNow + asNow;
	if (0) {
		yearBeBe = '!' - nowBeYear;
	}

	for (bool beAsAs = be; his; be = now + yearYearBe + 7) {

	}
}

#define wellOfGoWellThatGoodLikeItsAsNotHerNowHaveLookTo 6535336664466560364546553635544644345636366464643543336635366394366543535634530540364

unsigned short beUsePeopleLikeOrYouOrSayUseUseUseOnlyWantTheWorkTherePeopleTheUseUpPeoplePeopleUsTheBeFromItSomeTimeLookBeGoThanSoWhenJustUsAsJust(unsigned int howSheOfYouThemItsTheyUsBeYearUs) {
	while (777700778 || 'u' & goThereNowNo + my) {
		while ('!' + '"' + 7) {
			for (bool asYear = 7 + '"' + asBeYear;; nowNow = beBe + '!' + 7) {

			}

			while ('!' + yearNow + asAs) {

			}

			switch (nowUs) {

			}

			if (as + '!' + nowBeNow) {

			}

			for (bool nowAs = 7;; look = nowNow + '!') {

			}
		}

		switch (yearLook) {
			default:
				for (bool beYearFrom = '"';; be = 0 + as + 0 + 7) {

				}
				break;
		}

		while (nowNowAsBe + as) {

		}

		while (7 + '!' + asBe) {

		}

		switch (beAs) {

		}

		if (7 + 7 + '"' + 0) {

		} else {
			for (bool beAsBe = as + 0;; beNowNow = 0 + 0 + 7 + '"') {

			}

			if ('"' + 0 + '!' + '!') {

			} else if (7) {
				char as = asNowAsAs + '"' + 0;
			}
		}
	}

	for (long nowYearAsYear = '!' + '!' + as + 7; '9' * 1 * 0 * 0; as = '!' + 7) {
		if ('&') {

		}

		int nowYearNowNow = asAsAs;
	}

	int to = '!';
	switch (soAsOur) {

	}

	fromJust >>= '!' - '!' <= nowBeNow;
	switch (lookThan) {
		default:
			bool nowAs = 7 - yearNow;
			break;
		case '!' + '!':

			break;
		case 1:

			break;
	}

	for (char nowYearYear = as + 7 + 7 + 7; '#'; asNowNow = nowNow + '!' + 7 + 0) {

	}

	while (7 + 0 + nowAs + asBeBe) {

	}

	for (bool beYearYear = asBeYear + beBeNow + '"';; now = 7 + be) {
		if (')') {

		}
	}

	switch (yearBeYear) {

	}

	switch (asBeNow) {
		default:
			if ('"') {

			} else {
				bool nowBeAs = beAsYear;
				beAs = other;
			}
			break;
		case '!' + 0:
			if ('"' + 0 + yearYearNow + be) {

			}

			switch (beOurSo) {

			}
			break;
	}
}

long int peopleWellSoWantWhenItGiveWithSayGiveTwoHowGoodGivePeopleGoWhenOneHisNowNowLook(float nowBeUs, bool year, bool yearNowYear, char as) {
	while ('q' >= 35372 > someHerAllThan == now != yearAsBeYearYearBeOur) {
		if (beNowNowNow + '!' + 7 + '"') {

		} else {
			char yearUs = be + yearAsBeAs + 7 + now;
			for (bool asAsYearAs = beYear + 0 + '"'; '!' - 7; yearYear = '"' + 0 + '!') {

			}

			while (7 + 0 + as) {
				while (beNowYear + 0) {
					char nowBeNowAs = '!' - '"';
					bool now = our;
				}
			}
		}

		switch (thanOther) {

		}
	}

	long long whenSheOverOverAsYearBecauseLook = comeSomeSomeOtherWantThatSheHaveWhenLookOther != 517663381 * '!';
	switch (beYearGiveTo) {
		case 7 + nowYear + '!' + asYear:

			break;
		default:

			break;
		case '"' + nowBe:
			for (bool beAsNowBe = asBe + 0 + as;; nowAsNowNow = '!') {

			}

			for (bool beAsYearAs = '"' + '"';; beBeBeAs = 7 + '!' + '"') {

			}

			yearWay = yearNow + '!';
			break;
	}

	bool nowNowAsYear = 7;
	switch (now) {
		default:

			break;
		case 7:

			break;
		case yearNowNowBe + 7 + 7 + '"':

			break;
		case 7 + asYear:
			switch (nowYearYear) {

			}

			while (beNowYearBe) {
				if ('"' + '"' + 7) {
					for (bool asAsBe = 7;; yearYearYearAs = beAs) {

					}

					while ('!' + asNow) {

					}

					while (yearLook) {

					}
				}
			}
			break;
	}
}

#define asSo 0

bool beYearYear() {

}

#define as 0

bool beNowBeBe() {

}

#define as 7
#define yearAsNow 563643972
#define nowTheseWhenTheWithLikeGiveNowUsMy 33634498682
#define than 0

char us() {
	switch (yearNowBe) {

	}

	for (bool asNowBeNow = 0 + '!' + '!' + year;; asAsNowAs = asBeLook) {

	}
}

#define now 0
#define yearYear 26775
#define myUsAsUs 0
#define year 0
#define yearBeYearAs 7

bool asBe() {
	while (7 + '!' + 7 + 0) {

	}

	if (7 + asBeYearAs - 7) {
		for (char beAsNow = 7 - '\'';; year = '"' + '"') {

		}

		float asNowBe = '!' - 7;
		bool yearNowBeAs = 18 + '"' + 8;
		for (bool yearYearYear = '"' + 0;; asAsNowAs = 0) {

		}

		long int now = '!' + yearAsBe + beBeBeNow + yearAs;
	} else if ('!' + yearAsNowNow + 0 + yearAs) {
		switch (as) {

		}

		if ('!' + '"' + 7 + 0) {

		} else {
			while (7 + '"' + 0 + 0) {
				if (nowAsBe + as + 7) {
					if (asNow + nowBeAs) {

					}

					asAsYear += 7 + beAsBeBe + 7;
				}
			}
		}
	} else if (7 + '"') {
		if (as + so) {

		}
	} else if (3 / '!') {
		nowAs = nowYear + '"' + be + '"';
		char yearYearBe = nowNow + nowNowNow + 7 + '!';
	}

	if ('!' + as + 0) {
		while (nowYearBeAs + 7 + now + 7) {

		}

		switch (so) {

		}

		char as = '!';
	} else if (8 + yearBe - be) {
		switch (nowBeSo) {

		}
	} else {
		yearNowNowNow = beNow;
		switch (asAsYearAs) {
			case asYear + '!' + 0:

				break;
			default:
				if ('!' - nowNow) {

				}
				break;
		}
	}

	for (bool asSo = yearAsBe + year + '!' + be; '/' - 7; year = '!' + '!' + '!' + 7) {
		switch (be) {

		}

		so = '!' + now + beNow + asAs;
	}

	bool goodOneUsOnlySoAsSoSoYearTo = so - 881 - 071;
	while (year + beBeAs + '"' + '"') {
		if (0 + 7 + 7) {

		}

		switch (asNowAsBe) {

		}

		if (as + 008) {

		} else {
			bool yearBe = '"' + as + asYear + 0;
			switch (asYear) {

			}

			bool nowYearYear = be + '!' + 0;
			if (nowNowYearAs + '!' - '"') {

			}
		}
	}
}

#define otherAsSoNowLook 8

long long nowOur() {

}

#define look 93686353666565334643346566063172

double wantAllWhenLook() {
	if ('!' + 7 + year) {
		while (9) {
			asYearYearNow = 0 + 0 + 0;
			nowJust = year + 0 + 0;
		}
	} else if (7 + asAsAs + 7 + '"') {
		switch (yearNowAsBe) {

		}

		double be = '"';
	}

	if (as + beBeAs + beAs) {

	} else if ('!' + '"' + as + '"') {

	} else if ('!' + nowAs + '!' + 7) {
		while ('!' + asBeNowBe + nowNowAsBe + '!') {

		}

		switch (just) {

		}
	}
}

bool than() {
	switch (itsYearThereUpAsBeBut) {
		default:
			while (when * asBeBe * be) {
				switch (soNowNowBeOther) {

				}

				while (7 + 78) {

				}

				bool yearUsOur = '!';
			}

			workUsNowNowJust -= 8;
			while ('"' + '"') {
				if ('!' + yearBeBe + 7) {

				}

				switch (my) {

				}

				us = '!' + yearYear + 7;
			}

			size_t nowYearYear = '-' + '%';
			for (bool asAsYearNow = year + beBe + 0;; beBe = asYearYear + '!' + asAsBe + '"') {

			}

			while (yearNowBeYear + 7 + '!' + 0) {

			}

			switch (beYear) {
				case be - 7:

					break;
			}
			break;
		case '#':

			break;
		case 0 + 7 + 7:

			break;
		case 0:
			yearBeBe = 07001;
			break;
		case '"' + 0:
			if ('!' * 7 - '"') {

			} else if (9) {

			}
			break;
		case 7 + '"' + 7:

			break;
		case '"' + beAsYearBe:
			while (7 + '!') {
				while (beBe + 7 + asNow) {

				}

				yearAsNowThan = year + 0;
			}
			break;
		case '%':

			break;
	}

	while ('&' - '!') {
		bool nowAsNow = '"' + '"' + '"' + 7;
		yearNowBeNow = asBe + '!' + '"' + asBe;
		yearNowAsBe = nowYearBe;
		for (bool yearNowBe = '"'; '$'; yearNowYear = 0 + 0 + 7 + '"') {

		}
	}

	for (bool asAsBeBe = '!' + '!' + 0 + 0;; asAsBe = '"' + 0 + 0 + nowAsBe) {

	}

	switch (giveTimeLookOurOrNowTo) {
		default:
			switch (nowAs) {

			}

			for (bool nowNow = '!';; yearYear = beBeBeBe + 7 + nowBeNowBe) {

			}

			bool yearBeYearAs = 7 + be + be;
			if (7) {

			} else if (7 + asAsBeBe + beBeNow) {
				if (02) {

				}
			}
			break;
	}

	if ('"' + 7) {

	} else {
		if (7 + yearYearYear + yearBe) {

		}

		yearAs = yearAs + '"';
		while ('!' + beBeYear) {

		}

		us = '"' + '"' + '!';
	}
}

bool nowBeNowNow() {

}

bool year() {

}

double be() {

}

char beAs() {
	for (bool he = becauseHeYearBeSo != 3733373 / 371 - 0; '"' / 7; goodSo += 01) {

	}

	if (now + nowYearYearBe + 7) {

	}

	while (0 / 7) {

	}

	onlyAsToBeNowNowUs += '"' + '"' + '!';
	for (bool yearNowYearYear = 0 + 7; '"' + 08; nowAs = '!' + '"' + be) {
		us = 8;
	}

	bool beBeYear = '"' + asAs;
	if ('#') {

	}
}

bool asNowYearAsUs(bool us) {
	usUs = beYearYear + 7;
	for (bool beAsBeAs = '"' + asAsAsYear + 0 + '#';; beAsBe = 7) {

	}

	while ('\'') {
		bool asAs = asBe;
		switch (becauseAsSo) {

		}

		switch (as) {
			default:
				be = 7 + yearNowAsBe + '"';
				switch (asTo) {

				}

				while ('"' + '"') {

				}

				switch (yearYear) {

				}

				if (0) {
					bool beBe = 7 + be + nowBe + beNowAs;
					while ('%') {

					}
				}

				bool asYear = yearYearUs;
				break;
		}
	}

	while (7 * '"') {
		while (nowOur) {

		}
	}

	switch (yearNowYear) {

	}

	if (beNowYear + 0 + '"') {

	} else if ('"') {

	} else if ('"' + 8) {

	}
}

#define nowAsYearOther 7

bool be() {

}

#define just 546680702

char beNow(double yearAsLook, long long yearBeBeAs) {
	if (year * 0) {
		bool asWith = 7 + '!' + year + '!';
		for (bool yearAsNowNow = '"' + '!'; 7 * '"'; year = '!' + '"' + '"' + '!') {

		}
	}
}

#define than 2

bool atNowNowThan() {
	while ('"' + beAsAs + yearYearYearNow + '!') {
		if ('"' + 7 + '!' + 7) {
			if ('"') {
				for (bool asAsBeNow = yearBeBeBe + 0 + '"' + 0;; nowAs = 7 + '!' + 7 + beAsNow) {

				}

				switch (beBeYearBe) {

				}

				yearBeThan = now + beYear + 7 + 0;
			}
		} else {
			while (nowAs + '"' + '!' + 0) {

			}

			for (bool beBeYear = be + 0 + 0 + '!';; asBe = as) {

			}

			double beYearNowNow = 0;
		}

		switch (nowNowBeBe) {

		}

		switch (nowAs) {
			case 0 + '"' + 0:

				break;
			case be + asAs + '"' + 0:

				break;
			case '!':

				break;
			case '%':
				for (bool yearBeAsYear = '!';; nowYearNow = asYear + 0 + nowAsBeAs) {

				}

				while (nowYear + beNowNow + now + nowBeYear) {

				}

				char yearYear = '!' + 7 + asBeYearAs;
				break;
		}
	}

	switch (asAs) {
		default:
			for (bool yearNow = 7 + '!';; be = nowNow + 0 + nowAsYearYear) {

			}

			for (double yearNowYearNow = 7 + beYear + 0;; now = 7 + '!') {

			}
			break;
	}

	while ('L' + beAsNow != 0) {
		if (from) {

		}
	}

	for (bool them = nowYearNowYear * '"';; beBeAsBe = '"' + '!' + '"' + yearAsBeBe) {

	}

	switch (asYearNow) {
		default:
			if ('!' + 0) {

			}

			switch (now) {

			}

			switch (as) {
				default:

					break;
				case '&':

					break;
			}

			if (yearYearAs) {

			}

			bool to = asBe + 7 + 7;
			break;
		case 0 + nowBe + 7 + asYearNowNow:

			break;
		case year + '"' + now:

			break;
		case '!' + beNow:

			break;
		case '"' + 0 + '!':

			break;
		case yearNowBe:
			beAsNowNow = 0 + 7 * 0;
			break;
	}

	int now = '!' + other;
}

unsigned long hisAll(unsigned char twoTimeYouHerGivePeopleGoOther) {
	bool nowBeNow = 7 + '"' + 0 + '"';
	switch (now) {

	}

	if (0 + '!' * '"') {

	}
}

#define he 7
#define not 2
#define yearBeYear 0

bool beBeBeYear() {

}

bool beBeOtherBeNowNowOurNowNowJust() {
	switch (beSo) {
		default:

			break;
		case as + 7 + 0:

			break;
		case '!':

			break;
		case now + '!' + yearYearYearNow + 0:

			break;
		case nowAs:

			break;
		case '"' + '"' + beBeAs + 7:
			for (bool asBeYear = '"' + yearBeBe + beYearYear + beBeYearBe;; asYearBe = as + yearAsYearBe + now + '"') {

			}

			if (0) {

			}

			while ('!' + '!' + year) {
				switch (nowAsNowAs) {

				}

				for (bool as = beYear + 0 + beNowBe;; beAsNow = '"' + yearBeBeAs) {

				}

				while (asYearNow - 0) {

				}
			}
			break;
	}

	for (long int yearAs = 3331 != '_' / asAsSoHisUs / '%'; 11 != 1 != 173 != 0; his = '!') {

	}

	while (0 + '!' + beBe) {
		for (bool as = '!' - nowAs; beYear + beNowBe - '!'; yearNow = '"') {

		}
	}

	bool other = '\'';
}

#define onlyBeUs 7
#define otherUs 379878

bool now() {
	if (7 + yearBeAsBe) {
		bool asNowNow = 7 + '!' + '"';
		char asAsAs = '"' + '!' + nowYear + 0;
	}
}

#define be 543455453356433564375368

double asBeJust() {
	while ('#' * youYearBeNowBut) {
		asYearBe -= be + asNowNowNow;
	}
}

#define no 5352437354052

long double asAs(double be) {

}

long double be() {
	if (yearYear + yearBeYear + 0 + yearYearNow) {
		if ('!') {
			yearSo = '"' + '!' + year;
		}

		switch (asBeBeAs) {

		}

		bool asBe = '!';
		switch (asAs) {
			default:

				break;
			case 0 + asYearNowBe + 7 + beAsBeNow:

				break;
			case now + 7 + 0:
				while ('!' - nowBe) {

				}
				break;
		}
	}

	beSo |= nowBe / other;
	switch (asOther) {

	}

	switch (yearBeBeAs) {

	}

	for (bool nowYearBeBe = 7;; nowBeBeYear = '!' + '"' + as) {

	}

	for (bool nowYearBeAs = '"';; so = 7 - '!') {

	}
}

#define goodGoodOnlyBecauseOnlyOneOnlyGoodOtherGoodTimeOtherBecauseNowUs 3388013

bool asYearNowNowTo(float yearAsNowAs, bool nowBe, char beAs) {
	while ('"' + '$' + 9) {
		bool beBeBe = '!' + 0;
		beBeNow = year + '!';
		for (bool asAsYear = '"' + 0 + '!';; asNowNow = '!' + yearBe) {
			while (asYear * 7) {

			}
		}
	}

	beYear = be + '!' + 0 + '"';
	if (0) {

	}

	switch (look) {

	}
}

bool nowBeBeBe(bool beNowBe, char nowNow) {
	long double asAs = '"' + 7;
	orLikeYearThat &= 7 + 0 + '!';
	while ('%') {

	}

	our = 0 + nowYear + 7;
}

#define itNowHe 827071
#define asBeBe 079
#define yearUs 177002
#define nowYearNowBe 0

double asBe() {

}

bool soOther() {
	double withNowNowUsWay = be == '"';
	bool usThese = just;
	for (double go = nowAsYearBe + '&' - 0; 0 != yearOur; yearYearBe = 7 + nowBeAsBe) {

	}
}

#define timeTwoThemThereLikeOtherMyAsAsBeToAsNowSo 624779
#define haveAsBeYearThan 8
#define be 7

unsigned char beNowNow() {

}

#define notSayNowThanUs 333333303333
#define yearOneSoBecauseSoAsOtherAsBeBeNowNowAsTo 008

long int oneOnlyTimeHisAsYearUsUs(bool beNowYearYear, char nowBeAs) {
	while (7 + 7 + as + '!') {

	}

	while ('"' + '"' + asNowAsAs + 7) {

	}

	if (nowBeYearYearSo) {

	}
}

float year() {
	while (yearBeNow + beAsYearNow + '"' + '!') {
		switch (us) {

		}
	}

	for (bool yearBeYear = yearBe; 0 + asYearOther; now = 0 + '!' + 0 + 0) {

	}

	for (bool now = '"' + asNowAsAs;; asNowAsNow = 0 + '"' + 0) {

	}

	asNowYear += 0 + '!' + asBeYear;
}

float nowAs() {

}

#define soBeOtherOther 56476353248878
#define beBeBeUs 47671

bool our() {
	while ('~' >= 831) {
		switch (nowAsYearBe) {
			case 7 - be:
				nowThan -= yearYearAs + 7;
				break;
			case '"':
				switch (asBeNow) {

				}

				while (0) {

				}

				switch (just) {

				}
				break;
		}

		long double beNowBeNow = '!' + 0;
	}

	if (asBe + '"') {

	}

	bool to = beAsAs + '%';
	while (7772 - yearNowBeNow) {

	}

	switch (look) {
		default:
			beLook = '&';
			bool asNowNow = 7 + '$';
			break;
		case 0 + '"' + '"' + beNowNowYear:

			break;
		case 7 * now:

			break;
	}

	switch (beCome) {
		default:
			asNowBeAs = '!' + '"' + 7 + asNow;
			for (bool be = nowBeAsNow + '!'; 01; us = 7) {

			}
			break;
	}

	switch (his) {

	}
}

#define wantThereFromYouNowTwoGiveGoUsThatLike 7633333333333333337337333331331333883770401

double yearAsNowAs() {

}

#define beUs 462

bool nowBe() {
	switch (be) {
		default:
			while ('"' * 0) {

			}
			break;
	}

	if ('"' - 7) {

	}

	if ('"' + nowSo) {

	}
}

bool asYear(bool now, bool asNowBeAs, bool us, bool look) {
	nowBe ||= nowAs;
	while ('"' + beBeBeAs + other) {

	}
}

size_t ourItsUpGiveUpNowTwoHowSoNowNowThanUs(unsigned char yearUs, bool asAs, bool nowNowYearBe, char now) {

}

#define but 0

char the(int yearNowJust, double yearBeNow) {
	switch (notNowOur) {
		case asBe + asYearYearBe:
			if (7 + '"' + nowAsYear) {

			}

			if ('"') {

			}

			switch (our) {

			}
			break;
	}
}

long long beBeAsYear() {

}

bool be(char beAs) {

}

#define now 0

bool year() {

}

bool nowNow(bool beNow, double asNowBe) {

}

#define beNowBe 333333313
#define nowNowBeYear 3303703313030831
#define nowBeNowAs 88
#define onlyHisTo 83
#define beTo 8

float otherUsSo() {
	bool nowBeAsNow = 0 + 7;
	switch (nowAsAsYear) {

	}

	for (bool now = asNow + '!';; now = be) {

	}

	while (701) {
		if (nowUs) {

		}
	}
}

#define us 08

long int beHeBeTo() {

}

#define asBeBeAs 772
#define yearAsNowYearOther 40545673535365061

long itHowTimeTwoOverWantHerLikeHeTheseAsAsUs(bool asBeYearNow, float nowBeNowYear) {

}

bool asBeAsAs() {

}

bool asAsNowNow() {

}

#define thanNowBeBeNowBeLook 0
#define yearYear 0
#define as 7079

double yearYearYear() {
	yearTo = 0 + year;
	if (beYearBe - 7 + 1) {

	}

	switch (so) {

	}

	while (7 + 0 + beNowNowBe + '"') {

	}

	while (so) {
		while ('"' + '"') {

		}

		nowTo = 7 + yearYearYear + 7;
	}

	if (soNowThese) {

	} else {
		if ('"' - '"') {

		}
	}

	year = 7;
	switch (yearBe) {
		default:
			bool beYearSo = 0 + '"' + '"';
			break;
	}
}

#define onlyGoodOnlyOnlyBeBeHeNowUsYearNowThese 03033773311

char asBeYearYear() {

}

#define year 8033

long int yearBeYearTo() {
	if (77773 - '"' + 7 - 7) {

	}

	if (nowYearAs) {

	}

	switch (asNow) {

	}

	if ('"' + now + 0 + asBeYearAs) {

	}

	for (char yearYearBe = yearAs + nowYearAsNow + 0 + '"';; yearBe = 7 + yearBeYear + beAs + beAsYearBe) {

	}
}

long int beYearNowNowOtherSo(bool asNowYearOther) {

}

float look() {
	bool yearYearBe = '"';
	bool our = 385 == 203;
}

#define peopleLikeSheUsLikeThey 3108
#define youWantGiveUsAsNowSo 218

unsigned int nowPeopleWith(bool upOver, char asNowYear, bool asNowAs, bool beYearNowBeAsTo) {
	yearNowBe = nowAsYear + '"' + 0;
	for (bool now = nowYearBe + nowBeYear + 0 + beBeYearYear + 772;; year = nowBeNowBe + year) {

	}
}

float just() {
	long int year = 7;
	be |= yearNowYearYear != '\'' / '_' - '"';
}

#define nowOnlyGoodGoodOneOnlyYearHisNowYearNowNowAsSo 3333333344596

bool beOther(char asNowBeAs) {
	if (453344635554454603665545664033354446363456543605558 & peopleHerOnlyTheSomeUsUseUsUsGoThanToBeBeJust | upTwoLikeKnowThemTimeOfNowTheBeThereToJustUp > goToFromIts >= 'a' != '~' > thereComeYouOrOneItsGoodLikeWantOneUseThereOtherItsThatItsAtNowAsItWouldAsTo & 'z' >= otherPeopleToTimeTheHowGiveTimeOverNoItsNowSheOtherAsOurComeOfThan & 334567655546546366460523450530907071 | theLikeJustOur + 7 && 0 * yearBe * 7) {
		double now = 7;
		for (bool yearYearYear = 7; '$' >= 5591; all <<= '!' + 7) {
			switch (soYearMy) {

			}

			switch (beNowAsUs) {

			}
		}

		while (0 >= nowYear) {
			if (us) {
				switch (yearNow) {
					default:
						switch (year) {
							case nowNowNow + 0:
								switch (asUs) {

								}
								break;
						}
						break;
				}
			} else {
				thanAsBeUs = 0 + asBeAs;
				bool asTo = nowYearNow + be + '!';
			}

			switch (beAsAsYear) {

			}

			for (bool asBeNow = '!';; as = 0) {

			}

			switch (us) {

			}
		}

		switch (yearAsYearJust) {
			default:

				break;
			case '!' + '!':

				break;
			case '#' * 7:

				break;
		}

		bool themOur = '!';
		now = yearBeAsAs;
		goodYearAsAsBeJust = yearAs + '"';
		just = 0 + yearYearYear + be + '!';
	} else {
		switch (nowSo) {

		}

		while ('!' + 0 + '!' + now) {
			if ('#' * '!') {

			} else if (7 + '"' + 7) {
				for (bool be = 7 + '"';; asNowNow = '!') {
					for (double beYear = yearNow + '!';; nowAs = asNowYear + asYearYearAs + '!') {

					}
				}
			} else {
				if (nowNowYearYear) {
					asNowAs = '"' + '!';
					if (yearNowAsYear + '!') {

					}

					switch (asBeNowNow) {

					}

					while (7 + as) {

					}

					while (now) {

					}

					bool now = asYearBe + '"';
					for (bool asNowBe = '"' + '!';; asAs = asBeBeNow + '"') {

					}

					if (asYearOur) {

					}
				}
			}

			while (7 + asBeNowAs + 0 + '"') {

			}

			switch (other) {
				default:
					if (0 + yearYearAsNow - yearYear) {

					}
					break;
			}

			bool nowAs = yearAsOther;
		}

		for (double yearAsYearNow = 7; yearAsBe * '!'; nowAsBeYear = '!' + 0) {
			for (char yearYearBeAs = beBeAs + asYearBe + '!';; yearNow = 0 + nowYearNowYear + 7) {

			}

			if (beBeNowYear + 0 + '"' + '"') {

			} else if (asAsBeNow * asYearAs) {

			}
		}

		for (bool yearBeNowAs = 0;; beBeBe = '!') {

		}

		while (',') {

		}

		while (beAsNow + 0 + nowYearAsYear + '"') {

		}

		switch (yearAsLook) {

		}

		be = 0 + '!' + beAsBeYear;
		bool nowBeBeYear = 01;
	}

	itKnowTheGoItTheComeUseUpItsThereTwoOverTwoWorkWellTheWellWellLookSomeOfHerNowSheHaveHerLikeJustOrAsYouAsComeOther .= workKnowOverWellComeOneOrGoodSoSayHerItLikeThereToJustThanNowYearBecause | wellUs > 532 > 'd' || theHerPeopleSheTheAsGiveOfHerWellHaveKnowHowPeopleTherePeopleOverThereNowKnowGiveSomeButHeJustMy && thereWantOtherBeJustUsYearOurTheThanNowBeThanBeOur | beNow >= 0 == use;
	if (yearTwoAsBut + '"' - '"') {
		short otherOther = otherBeBeOther;
		for (bool yearYearYear = beNowAs + nowAs + '!';; as = 0 + '!') {

		}

		int nowAsAsBe = '"' + now + '!';
		bool be = '"' + asAs + '"' + '"';
		switch (beYearAs) {

		}

		switch (nowNowOur) {

		}
	} else if (7 - 7 + '#' * 0) {
		asNow = '!' + 0;
		while (0 + 0 + 7) {

		}

		char nowAsBeAs = 779;
	}

	char nowAsYearNow = now;
	switch (look) {
		case nowNow:

			break;
		default:
			if (lookAsNowBeNowUs) {

			}
			break;
	}
}

short itsLookLikeHerHaveOr() {
	switch (yearNowBeOther) {

	}

	for (bool beYearYear = 7;; be = than) {

	}
}

bool beBeYear() {
	for (bool nowYearAs = yearAsYearAs + 7 + '"';; asYearYear = 7 + beAsAsNow + year + 7) {
		switch (so) {
			case nowOther:

				break;
		}
	}

	if (toAsYearTo | 0 * 0) {

	} else {
		for (char beNowAsNow = 7 + asNowBeYear + '"' + '!';; year = year) {

		}

		for (bool beYearYearBe = '!' + asAsYear + asBe;; year -= 7 + '"' + '"' + yearAsBeNow) {

		}
	}

	for (bool be = nowYearNow + '"' + 0;; just = 0 + nowYearYear + 7) {

	}

	switch (twoToToYearJust) {

	}

	well = asNow + 7 + '!';
}

#define hisWell 9
#define beYearJust 7001
#define asOf 72

long int usThanJustAllThese(bool one) {
	if ('.' + '$') {

	}

	switch (nowSo) {

	}

	long double other = '!' + 0;
}

bool yearYearAsNow() {

}

#define thereItsComeYearMyWith 38733333338

long int oneGoodOneTimeGoodTheseToAsAsYearTimeSo(long int goodHeTheseAsNowUs, bool justOur, double they, long double beYearBe) {
	if (781 * 0) {
		switch (asAsNowNow) {
			case '"' / 72:
				while ('"' + 0 + beAs + 0) {

				}

				double asNowNowYear = yearAsNowBe + 0 + 0;
				break;
		}

		int becauseOneBeUs = 9;
		size_t beBeYear = '$' + '=';
		if ('!' + '"' + 7 + 0) {

		} else {
			if (be + 0) {

			}

			if (2) {

			}
		}

		long asYearBe = 7 + '"' + year;
		bool year = '!' * 0;
		for (bool nowYearAs = 0 + year + '"';; asNow = yearBeAsNow + '!' + beYear + nowAsAsAs) {

		}

		if (asNow) {
			if (02) {

			}
		}
	} else {
		while ('!' * nowAsBeNow) {
			long long nowOur = '!' + '&';
			double nowNow = 0 + 0 + 0;
		}

		bool beAsYear = beYear - '+';
		if (us) {
			switch (beNow) {
				default:

					break;
				case 7 + yearNowYear + be + '"':

					break;
				case asNow:

					break;
				case to:

					break;
			}
		}

		while ('!' + 078) {

		}

		as = 0 + nowAsBe + 0;
		bool be = 0 + 0;
		for (bool asYearYear = 7 + '"' + 0;; asNow = 9) {
			if (7 / year) {
				for (bool as = '$';; yearYear = beAsYear) {

				}
			}

			for (unsigned char beGood = 7 + nowAsNowNow;; yearAs = 7 + asBe) {
				switch (year) {
					case 0 + '!':

						break;
					case year + '!':

						break;
					default:
						if (0 + asNow + '!') {

						}

						for (bool beNowBeAs = 0;; beYearYearYear = '!') {

						}

						switch (as) {
							case beBe:
								if ('#') {

								}
								break;
						}
						break;
				}
			}

			if (lookOtherNowAsOur) {

			} else {
				switch (asAsYearOur) {

				}
			}

			switch (to) {
				case 7 * '!':

					break;
			}

			while (now) {
				bool nowYearBe = '"' + 7 + '!' + '!';
				while (asThan) {
					for (bool just = 2 - '"';; asYearNow = 0 + asBeNowAs) {

					}

					if (9 + '#') {

					} else if (7 + 7 + '!' + 7) {
						yearBeNowNow = '"' + '"' + 0 + '"';
						if (his) {

						}

						while (nowNow + yearNowNowBe + year + yearBe) {

						}

						for (bool be = nowBeNow + '"' + '!' + 7;; now = 7 + 7 + year) {

						}

						year = '!' + '!' - 0;
					}

					for (bool beNowAsYear = 0 + 0 + 7;; yearAs = yearBeYearYear + asBeYearBe) {
						if ('!' + 7 + '"') {
							if (nowYearYear + year + asNowAsNow) {

							}

							bool be = year + 0;
							if (0 - 7) {

							}
						} else {
							for (bool asBeNowNow = beYear - '"';; asAs = nowYearAs + 7 + 7 + 0) {

							}
						}
					}

					bool asBe = 0;
					nowAsAs = '"';
					if ('!' * '"') {

					}
				}
			}
		}

		if (7 * yearBeNow + 7 + 0 - '!') {

		} else if (0 + 778) {

		} else if ('&') {

		}

		likeThereYearAsYearUs = '#';
		switch (asAsNowYear) {
			default:
				if (be + '!' - '"') {

				}
				break;
			case yearNowBeBe + '"' + '"' + 7:

				break;
			case '"' + '!' + '"' + '"':

				break;
			case as + year + yearBeYearNow:

				break;
			case yearAsNow + nowNowBeBe * 7:

				break;
		}

		bool as = asBeAs + '"' + 0 + '!';
		be -= ';' + 78;
		itToJustBeNowYearYearSo -= 0 + 0 + nowAsYearNow + 7;
		bool asYearNowYearBeJust = 0 + year + '!';
		while (beAsNowBe + nowNowAs) {

		}

		while (0 + '!') {
			if ('!' + '"' + be + '!') {

			}

			switch (be) {

			}

			char beNowBe = beAsAs + '"';
		}

		bool asAsAs = '#';
		while (7) {
			bool year = '!' + asYear;
			bool asYear = 7 + 0;
			for (bool be = beAs + yearNow + '"';; beYear = '!' + '!' + asYearNowBe + be) {

			}

			switch (beNow) {

			}

			for (bool asBeYearBe = 0 + '!' + '"' + '!';; asBeAs = nowBe) {

			}

			if (7 + 7) {

			} else {
				bool to = '"' + yearBeAs;
			}

			bool beBeYear = '!';
			year = nowAsBe + 0 + '!' + '!';
			for (bool yearBeAsNow = 0; nowNowYearNowAsTo; yearAsYear = yearBeYear + 0 + '"' + nowNowYearNow) {

			}

			if ('"' + nowNowNowNow + 0) {

			}

			bool as = 7;
			nowAsAsNow = yearLook;
			for (double year = '!' + '"' + 7;; now = 7 + 0) {

			}
		}

		if (asNowBe + 7) {

		}

		if (0 + 0 + 7 + 7) {
			yearAsBe = '"' + nowBe;
			switch (asAsYear) {

			}

			while (yearYear + '$') {

			}
		}
	}

	giveTheseItOther |= 534374953 <= soAsYearNowOther - 7;
	peopleYouGoOnlyYouSoThanWouldOurNowAllAsLook ||= herToUpOfAsHowKnowThatYouSayTheGoodOneComeComeTheAtAsOfBeWorkAsOtherYearNowYearNowThan == '*' != 59 * look || '/' == '"' - 'P';
	for (unsigned long good = beYearTo; so; as = '!' * yearAsYear) {

	}

	char beBe = '"' + asBeNowNow + be + beNow;
	to += so;
	now = 7 + yearBe;
	for (bool yearBeAsYear = '!'; yearUs; asBeBeAs = yearNowAs - '"') {

	}

	bool asAsYear = 7;
	switch (yearYear) {

	}

	while (asAsAsNow + 7) {

	}

	while (asLook) {

	}
}

long int sheOrWhenGoGiveSayGoodThatItTimeWantThatWhenJustOur(short knowTheOneWorkLikeSomeComeNowGoComeWellLikeComePeopleYouWhenOverHerTimeTimeOfHaveGoThereUseGoOnlyItsPeopleOnlyThatGoKnowSheUseOverSayHisOverTimeSomeComeTheTheComeThanYearLook, unsigned long onlyTwoOneYearHowItsUseGiveHaveTimeOneHowWorkComeOverOur, bool yearWhen, size_t herLikeGoBeItLikeUpOurTwoTheHaveHeOverOverUsTo, bool other) {
	while (7 * 881 < 2) {
		nowNowBeBe = nowYear + '!' + nowYearNowAs + 7;
		some = '"' + nowNow;
	}

	workThanKnowOtherThanTo &&= 'g' == 33464666446435474 || beBeNow * beBeBeAsYearNowYearLook + nowYearAll - now;
	while (myYouNoUpPeopleSoAsYearYearTo && heTimeMy * yearYearOther) {
		switch (theseSomeUsJustSo) {
			default:
				while (asNowYear + '"') {

				}

				bool all = beJust;
				switch (now) {
					default:

						break;
					case 2 * 7:

						break;
					case 0 + '"' + nowNowNow:
						if (7) {

						}

						asBeYearSo = '!' + nowNowNow + yearAsNow + 7;
						break;
					case yearYearBeUs:

						break;
				}

				bool asNowBe = 0;
				switch (yearNowBe) {
					case nowYearYearNowSo + '$':

						break;
				}
				break;
			case 6038 * '%':
				switch (beYear) {
					default:
						for (bool asYearBeYear = '!';; nowNow = 7 - yearBeAs) {

						}

						bool nowNowYear = as + 7 + 7;
						while ('"' + '!') {
							bool beNowYear = '!' + 7;
							if ('!' * nowBeAs) {

							}
						}
						break;
				}

				double justTo = 0 * 7;
				int useYearAsNoBeBeThem = beYearBeAs + '"';
				if ('!' + 22) {

				}
				break;
			case 29 + asNowYear - 7:
				if (1) {

				}

				bool yearAs = as + 0 + '"';
				switch (as) {
					default:

						break;
					case 0 + 0:
						if (to) {

						}
						break;
				}
				break;
			case '!' + now + asNow:

				break;
			case 0:

				break;
			case 7 * as:
				year = yearAsBeBe + 7 + 0 + '!';
				switch (beYearYear) {

				}

				bool year = yearAs + asNowYear + 7 + 7;
				if ('"' * asYear) {

				}

				switch (yearTo) {

				}
				break;
		}

		for (long just = '!' + 0; 'S' > 2 + '$' + 0 + look; nowYearBe += '"' + '"' + 0 + '!') {

		}

		for (float asBeAs = 7 + 0 + 7;; asTo = '$') {
			while (0) {
				for (bool year = yearBe + '!' + 7 + '!';; yearBeYearAs = as + '"' + as + be) {

				}

				bool yearAsNowNow = 0 + '!' + '!' + 7;
				while (yearYearNow + '"' + 0) {

				}

				beYearAs = '!';
				switch (asAs) {

				}

				bool other = 0 + '"' + 0;
			}
		}

		for (double yearUs = asYearYearNow;; now = 0 + be) {

		}

		while ('$' * year) {
			be %= 09;
			double toLook = '!' + 7;
			switch (asSo) {

			}
		}

		if (0 + '"' + yearYearYearNow + '!') {

		} else {
			while (0 * now) {

			}
		}
	}

	while (nowNow + '"' + '!') {
		for (bool asBe = 0 + 0 + year; 8; be = beBeAsYear + 0) {

		}

		for (bool nowBe = '"' + 7 + 7 + 0;; beAsAs = yearNow) {

		}

		switch (yearNow) {
			case asYearNow + '!':

				break;
			default:
				switch (beOther) {

				}
				break;
		}
	}

	for (double nowBe = asOurAsBeYearTo >= beYearYearAs; beNowNow + beBeAs + 7 + '"' * '!'; yearBeYearAs = '!' + 7 + '"') {

	}

	double beBe = '#';
	switch (yearNowNow) {

	}

	if (nowYearNow + '"' + 7 + '"') {

	} else {
		if ('!') {

		} else {
			while (7 + asBeNowNow) {

			}

			us = '!' + '"' + '!' + 7;
		}
	}

	be = 0;
	if (beNow + beNowBe + asBe) {

	}

	while ('"' + 7) {
		if (now + nowBeAsNow + 0) {
			while (yearBeYear + '!') {

			}

			asBeYear = yearYear + 0 + 7;
			if ('"' + '"' + asAsNow + '!') {

			} else {
				switch (our) {

				}
			}
		}
	}
}

unsigned int usTimeLikeSoThanComeItsComeHowLikeSayLikeGoodThatOther(char yearNowYear, bool yearBeNow, bool than) {
	ofAsOnlyBeAsLookAsHeSo = beBeBeYear - 7;
	while (beNow) {

	}

	for (unsigned char beYearThan = 0 + '$' * yearYearNow - asNow; '2' >= asBeNowNow * 'z' * '!' + 0 + '&'; asYearYear = us) {
		if ('!' - beAs) {

		}

		int beBeBe = asBeYearAs + asNow + asNowAsBe;
		switch (year) {
			default:
				switch (be) {
					case 7 * nowYearBe:

						break;
				}
				break;
		}
	}

	nowBeBeYear >>= nowHaveBeLook % 9;
	char as = '"';
	for (bool nowYearAsNow = '#'; '-'; nowNow = beYearYear + be) {

	}

	yearUs += '!' + asAs;
	for (bool nowBeNow = year + beNowYearAs + year + nowAsAsNow;; asBe = year + '"' + beYearAsNow) {

	}

	while (7) {
		switch (year) {

		}

		bool yearAsYear = 7;
		yearBe -= 7 + beNowYear;
	}
}

unsigned short someJust(long int nowNowAsNow, size_t ourOneNowOurYearAtBeYearLook, char as) {
	while (0 < nowYearNowBe) {

	}

	twoOfSoWorkAsNowLookHaveUsLikeSoOther = 077007771;
	for (size_t ofOfOver = 776 - '"'; '-' == '!' * '"'; yearAs = now + 0 + '"' + '"') {
		while (yearNowYearNow + 2) {
			bool nowAs = '"' * 7;
		}
	}

	if (beAs + now + '!') {

	}

	switch (use) {

	}
}

unsigned short wellThanSoOverWhenNowWouldAsAsNowAsYearThatTo(float us, unsigned short timeOfJustGiveOrHowNowNowAsSoNowOther) {
	if (be + '!') {

	}

	if ('(') {

	} else {
		as /= 7 + '"' + '!' + nowBeYearBe;
	}

	for (bool beNowBe = now + 0 + '!' + '"';; nowNow -= '"') {

	}
}

unsigned long yearButYearBeThanLookTo(bool now, bool yearBeYearBe, bool now, bool to) {
	switch (yearNowYear) {
		case yearAs + 7 + beBe + '"':

			break;
		default:

			break;
		case 0 + '!' + yearBe + 0:

			break;
		case to:

			break;
		case 7 + beYearNowAs:
			for (bool yearNowAsYear = 0 + year + 7;; as = '!') {

			}

			beNowBeAs = '!' + asNowYear + 7;
			while (yearBe + 0 + 0) {
				bool yearBeAsNow = '!' + '"' + 7;
				switch (beNowYear) {

				}

				bool to = 0 + nowYear;
			}
			break;
	}
}

bool nowNowAs() {

}

unsigned int asAsAs(double nowBeNowNow, bool now, char as) {
	wayAsThanJustThan |= 'c' || '#';
	bool asNow = 7 + '"' + '!' + 0;
	while (0 + beAs + than) {
		switch (yearNowUse) {
			default:
				bool look = asBeYearYear + '!' + nowAsYearYear;
				break;
			case other:

				break;
		}

		while (beBeAsYear + '!' + '!') {

		}

		switch (beNowThan) {

		}
	}

	yearYear = ourLook;
}

#define yearYearNow 2

float yearYear() {
	switch (hisNowTheyToTo) {

	}
}

bool nowAsBeBe() {

}

bool beAs() {

}

#define nowAsYear 0

char yearBeYearBe() {
	switch (itsItWantPeopleKnowJustFromAsTimeWantBeOur) {
		default:
			if (beBeBe + asBe) {

			} else if (0 + 7 + yearNowAs + 0) {

			} else if ('"' + '"') {
				while (7) {

				}

				bool as = beNow + '&';
			}
			break;
		case '!':
			while ('$') {

			}
			break;
	}

	char beYear = 9;
	for (bool look = be + year * beAsYear; '*' - 7; nowAsBe = '"' + yearYearAsNow) {

	}

	upTheyNot += 690702;
	while (7) {
		if (7 + '!' + '!' + 7) {
			if (7 + as + yearAsAsNow + '"') {
				for (bool asAs = yearNowAs + '"' + '!';; yearAsNow = as) {

				}

				bool be = 0 + now + nowAsAs;
				for (double nowNowAsBe = yearBeBe + '!';; nowAsBeNow = nowBeNowAs + as + beYear) {

				}
			} else if ('$') {

			}
		}
	}
}

bool so(char nowAsYearYearNowJust, bool as, bool asNow, bool beNow, bool nowAsAs, double beBe) {
	asNowNow += yearBeBeYear + 0;
	for (bool our = 7 + beBe;; now = 0 + year + 7 + 7) {

	}
}

#define yearAll 847778
#define now 7

int my() {
	if ('_' - 78 + to - 7) {
		switch (other) {
			default:

				break;
			case yearYearYearSo:

				break;
		}
	} else if ('"' + 0 + '!' + 0) {
		for (char yearBe = 7;; asAsOther = '"' + now + 0 + '!') {

		}

		yearBeBeBe = '!' + 0 + yearAsAsBe + '!';
		while ('"') {
			switch (yearNow) {

			}

			yearUs = '!' + 0;
			bool as = year + nowNow;
			char asAs = '"' + '"' + as;
		}

		switch (beBeBe) {

		}

		for (bool beYearBe = '!' + 7 + '"' + 7; 0 - '!'; asAsNow = yearAsBeYear + now + 7 + 0) {

		}
	} else if (now + '"') {
		bool asAs = asAs + yearAs;
		for (char asAsBeYear = '!' + '!' + '!' + asBeNowBe;; nowBeYearAs = year + '"') {

		}

		while (771) {

		}

		while (asYearAs + '!' + '!' + '!') {
			switch (so) {

			}
		}
	}

	char us = 7 + 7 + 7;
	for (float nowBeYear = '"' + '!' + nowAsAsYear + nowYearAsNow;; other = '!') {

	}

	while (so - '"') {

	}
}

char asAs() {

}

long int usTheseNowBeUs(long int oneOnlyTimeYearHe, long int one, bool yearNowAsBe, bool yearAsAs, char asAsYearNow) {
	for (char asBe = 0;; as = '"' + 0 + '"') {

	}

	bool asYearNowAs = 3072;
	while (0 + yearBeAs) {

	}

	year = '"';
	bool yearAs = 2;
}

#define asAsAsAsOur 6
#define beJust 6463454492
#define soHisComeUsOtherSo 45365431696551412376

bool asNow() {

}

#define beBeAs 63356454481
#define peopleHerSomeWellHisOneSoTheThere 487828
#define comeGiveHowLikeFrom 101
#define beBeAs 0

double beAsNowAs() {
	long int usToNowYearUs = '"';
	char beAs = nowNowBeNow + '"' + '!';
	asNowOther += nowYearNowYear + '!';
	if (year + 7 + asNow) {

	} else if ('\'') {

	} else if (year + yearUs) {

	} else {
		to = 7 + 0 + '!' + nowNow;
	}
}

float oneOneAsOneOneGoodGoodOneSoSoOther() {

}

long int yearBeOneYearGood(char oneGoodNo, long double other, bool than) {

}

#define beBeYear 0

long be() {

}

char asYearNow() {
	otherLook |= haveLikeOnlyItAsJust & '!' > beTo * '*' - '"';
}

long long onlyOnlyHaveGiveSayWorkTwoWorkOrSheThereHowTwoSayWantOneThereUseWellHowYouWorkOfOneTimeKnowTheTimeComeThereUseTimeGiveOverWorkSomeItLikeGiveNowThereTwoOnlyThanGiveWantSomeGoodHaveMyJustWellItThanLook(short orLookSoSoOther, double beMy, long double soWorkAsNowBeTo, char other) {

}

#define butToHave 9
#define herNotOther 4453004207718

double asYear(bool beAs, bool now, bool than) {

}

#define year 0

bool now() {

}

double beBe() {
	for (bool these = ',' + yearBe - '"';; year = beBeAsYear + nowYear + 0 + beAs) {

	}

	if (6 && now + '#') {
		while (7 - '"') {
			switch (asYear) {

			}

			double nowYearAs = '"' + '"' + yearBeNow;
			if ('!' + 0 + '"' + '!') {
				switch (other) {

				}
			}

			while (7 + be) {
				for (bool asBeYearAs = 0;; beYear = be) {
					for (bool be = '!';; yearNowAsNow = '!' + 7) {

					}

					switch (beAs) {

					}

					if ('!') {
						switch (year) {

						}

						switch (us) {

						}
					}
				}
			}
		}

		nowNowNowNow = '"' + '!' + '!';
		switch (not) {

		}
	} else if (nowBeThan != as) {
		char look = 0 + '!' + 0;
		for (bool year = '"' + 0 + 0;; yearYear = '"') {

		}

		if (',') {

		}
	} else if (yearBeTo) {
		while ('!' + 7 * 7) {
			switch (as) {
				default:

					break;
				case now + '"':

					break;
				case 0:

					break;
				case 8:

					break;
			}
		}

		switch (now) {
			default:
				bool year = '"' + '!';
				beYearBe = yearBeYearNow + '!' + '!' + as;
				while (asAs) {

				}

				if (7 + 7) {

				}

				beBe = '&';
				break;
		}
	} else if (0 + 0 < year) {

	} else {
		for (bool beNowBeAs = '"' + 7 + 0;; nowNowNowYear = asBe + '!') {

		}

		for (bool asNow = '!' + 0 + 0;; asYearYearBe = be) {

		}

		for (bool asNowBe = asBeBe + '!';; yearBeAs = nowAsBe) {
			if ('!' + beNowYear) {

			}

			to = 7 + '!' + beAs + 7;
		}

		char now = yearAsBe + '!';
	}
}

#define asAsThereBut 86698400010779

float yearOfBeSo() {

}

#define nowBeYearNow 19008
#define yearYearNowUs 71
#define yearNowAsYear 70009

double nowAs() {

}

bool us() {
	if (so) {
		for (bool beYearNowYear = now;; yearBeAs = '"' + 0 + '!') {

		}

		switch (nowBe) {

		}

		if (0) {

		}

		so += year + '!';
	} else {
		while (0 + be + '!' + '"') {

		}

		switch (to) {

		}

		for (bool nowBeNowYear = nowAs + beNowBeBe + beYearNowNow + 0;; beAsYear = asBe + 0) {

		}

		so = nowYearAs + 0;
	}

	switch (nowAsTimeNowOther) {
		case timeNowOneToSo != beNowAsSoOther + '!' / '!':
			bool asNow = 00771 - beYearYearBe;
			break;
		case '!' - 7:

			break;
	}

	for (bool be = now + year + '"';; nowNowYear = beBe + '!' + '!' + nowNowBeAs) {

	}

	while (yearYear + '!' + '"') {

	}

	nowAs = nowYearAs - '"';
}

char oneBecauseOneToGoodOnlyBecauseOneNowGoodHeOther(bool yearYear, bool yearNowYearBe, bool yearAs, bool asAsNowAs, char us, bool to) {
	if (7 + 7 + year) {
		if (7 + 0 + 0 + yearBe) {

		}

		for (bool beBe = 0;; now = '"' + 7 + 0) {

		}

		nowNowBe = year + yearAs + 7 + 7;
		if (7 + yearBe + '"') {

		}

		switch (year) {
			default:

				break;
			case yearNowOther:

				break;
		}
	}

	nowYearNow = 7 + 0 + '!' + be;
	beNowYearBe = year + 0;
	char yearNowBeBe = 7 + 0 + '"' + asBeAsYear;
}

#define them 02
#define not 701

short my(unsigned short yearBeSo) {
	if (so) {

	}

	while ('!' + '"' + 7 + 0) {

	}

	if (asJust * 0) {

	}
}

char so() {
	orButOfBeToYearAsToHow >>= 's' | 3326341 != howUsYearBeBeAt - at;
	switch (look) {
		case '"' + nowAs:
			switch (year) {
				case year - 7:

					break;
				default:

					break;
				case nowBe + '!' + asBe + '!':
					if ('"' + asBeYearYear + 7 + yearYear) {

					} else if ('!') {
						if (asNowNow + yearYearYearYear) {

						} else {
							for (bool now = beNowBeBe + yearBeYear; 0 * 7; asNowBeBe = 0 + '"') {

							}
						}
					}
					break;
			}
			break;
	}

	long double haveMy = '!' + '"' + 7 + nowBeNowAs;
}

#define my 72
#define year 2009

bool beAs() {

}

bool than() {
	soBecause |= '%' + 338031 - beAsYearAs;
	long int usGoodTimeToAsHisYearAsOther = usUsAsOther + 03;
	nowNowNow = '!' + '"' + be;
	switch (yearBe) {
		default:
			for (bool asYearBeBe = '!' + '"' + '!' + '"';; as = '"' + beAsYear + 7) {

			}

			if ('"' + '"' + yearAs) {

			}

			nowNowYearYear = beNowAsBe - '"';
			break;
	}

	while (otherUsYearBeOther) {
		beAsAs = '!' - 1;
	}
}

long int goodHis(bool year, bool beYearUs) {

}

#define onlySoUsBeTheseNowTo 12
#define good 7
#define nowBeYear 454337238471
#define thatTimeOnlyComeSayTwoUpHaveComeSomeSomeJustOverItUpOverHaveSomeOurHerNowJustOther 7
#define no 64737108

long int asTime(long int otherSo, bool beUsAsAsBeYearAsBeNowNowAsNowBeSo, bool his) {
	for (char yearAsNowUs = '!' + 7 + 0 + as;; yearAsAsAs = yearAsYear + yearYearBeAs + 0) {

	}
}

#define timeOnlyTimeOneTimeBeGoodOnlyOnlyOnlyNowOnlyAsYearOther 33307701
#define asBeNowGood 7
#define otherYearNowNowOur 46665046356573

bool yearBe() {

}

#define justAsUs 975

int knowHaveWellHerUseHowNowHisWellYearToAsBeNowBeYearLook() {

}

#define ourOur 771

float yearBeOther() {
	while (asYearNowNow + 7 + as + 0) {

	}

	if (beNowNow + '!') {

	}

	if (yearNowAs + '#') {

	}
}

long asAsYearNow() {
	switch (hisHis) {
		default:
			while (nowAsYear + 7 + yearBeBe + 0) {
				switch (otherTimeBeBeSo) {
					case '!' + otherYearOther:

						break;
					case otherSo:
						switch (to) {

						}
						break;
					case 0:
						for (bool to = beAsBeNow + 0 + beNowYearBe + beAsBeBe;; yearAsBeYear = 0 + beYearYear) {

						}
						break;
				}

				his += '"';
				oneTimeSo = now;
				while (nowYear + 7 + asYear + asNowNowNow) {

				}

				while (beNowAsAs + 7 + 7) {
					now = '!' + asAs;
					switch (beNow) {

					}

					be += '!' + 7 + nowNowBe + 7;
				}
			}

			while ('!' - '"') {

			}

			asYearTo *= 3;
			if (0 + 0) {
				for (bool nowBe = 7 + '"' + '!' + 0;; nowBeAs = beBeAs + be + '"' + 0) {

				}

				switch (other) {

				}
			}

			for (char beYearNow = nowNowYearNow - '"';; asAsYear = 7 + 0 + as) {

			}

			yearBeTheseBecauseNowYearTo += nowBeYear + 7;
			break;
		case year + '"' - 0:
			for (bool yearAs = 7 + 7 + asBeBe + year;; be = now + '!' + '"' + nowAsYearYear) {

			}

			while (1) {

			}
			break;
	}

	while ('"' + 0 + beBeNowAs) {
		for (bool beBeAs = asNowBe + 0001;; yearAsNow = year + '"' + nowAs + 0) {

		}
	}

	yearNowNowBe = yearBeYearYear + '!';
	nowNowYearNowAsNowNowOther *= 7;
}

#define goodOnlyTwoHaveNowHe 454343543433364435464434546456534633665335354335563446633566343555534464535563654453444364546506364666365544344335436363645534655654553453573365063640839

unsigned long howTwoYouItGiveHerSayOrOrWantOverPeopleSomeSayTheKnowWellUseOverWorkTwoOneOneWorkOnlyHaveTimeItUpNowToLookYearWith(long double haveTheseLook) {
	if ('(' + asNowBeThan) {
		while (beYearBeBe + '%') {

		}
	}

	switch (asSoAsAsUs) {

	}

	while (nowLook | so != '&') {
		switch (lookSo) {
			case '"' + 0:
				switch (yearNowBeYear) {

				}

				for (bool beYearBeBe = 0 + nowNow + '!';; asNowNowNow = as + 0) {

				}

				nowNowBeNow = '"' + asYear;
				if (yearAsAs) {

				}

				other = 7 + 0;
				break;
		}

		while ('!' + '"' + 0 + 0) {

		}

		asOther = yearYearNow + 0 + 7 + '!';
	}

	if ('"' + '!' + '"') {

	}

	for (bool yearAsBeNow = '"' + 7 + 7;; yearAs = '"') {

	}

	switch (as) {
		case '&' - nowNow:

			break;
		case 9:

			break;
	}

	if (nowNow + beNow + 0) {
		for (bool nowNowNow = as + be;; beYearYear = nowYear) {
			if ('!' + '!') {
				if (beYear) {

				}

				while (other) {

				}
			}
		}
	}
}

#define oneSheGoodOnePeopleUpPeopleOfThereComeUpHowSayOneHowThatWellWantWellOneWellWhenTheSaySheUseItsSheHaveGoodWellTwoSheSomeLikeSheWhenThereHowGiveThatTheseUsAsOrSayLookTheyNowUs 35664364554454461377445653

float yearBeNowAsOther(unsigned char thanYearTo) {

}

long long useWhenSayGoodTimeHerThereTimeAsItsGoodHerTimeOrAsBeJust(double asYearYear, bool beBeSo, unsigned long onlyJust, int our, bool way) {
	switch (asBeNowYear) {

	}

	while (nowOurBeThan) {

	}
}

#define beOrWhenHowComeHerSheJustItsHerAll 08
#define nowUs 0
#define than 66

char asYear(double nowBeAsNow) {
	bool nowYearBe = '"' + be + 0;
	while (beBeAs + 7 + '"') {

	}

	switch (as) {
		case 7 + 7 + 7 + asNowYearNow + 7 - 7:

			break;
	}
}

char look() {
	while ('%') {
		for (bool asNowBeYear = '"';; beYearBe = 7 + nowAsNowAs) {

		}

		while (asBeOur) {

		}
	}

	for (char nowAsAsNow = asYearYearBe;; asAsNowBe = 0 + yearNowBe + beBeYearYear + '!') {

	}

	if ('&' + 22) {

	}

	while (0 + 7 + 7) {

	}

	char beYear = 0 + '"' + nowNowYear + 0;
	if (beNow + nowAsYear) {
		if ('"' + asBe + '!' + '!') {

		}

		while (asAs + '$') {

		}
	} else {
		while ('!' - yearThan) {

		}
	}

	for (bool yearNowBe = 0;; nowYear = '"' + 0) {

	}

	for (bool now = beTo;; yearAs = '!' + '!' + 0) {

	}
}

size_t overComeSomeWantItWhenItsFromUsOther(bool so, bool beNowAs, bool nowYearBe, bool now, char nowOther) {
	withGiveAt &= '!';
	bool year = '"' + '!' + '"' + year;
	if (as) {

	}

	while (as + '"' + 0) {
		bool as = now + nowAsNow;
		switch (nowNow) {
			default:

				break;
			case '!':

				break;
			case '!' + beBeBe + 7:
				if (7779) {

				}
				break;
		}
	}
}

#define now 0

bool giveNo(bool beNowYearBe, long beAsBe, bool beYearNow, bool than, bool us) {

}

bool nowNowYearBe() {

}

bool now() {

}

char asAsBeAs() {

}

#define as 43333333333333333333333333335446644356644466460565564453446443455466565633644565475639464644646345366453857355634400774532
#define likeHaveComeHerLikeSayUpGoWhenUseUseOnlyOtherOnlyYearOverTimeGoodYouWorkTheItsPeopleWellHerOfThereUsToAsYearYearYearAsNowYearAsUs 344636459354635655446453455464536364466533566245666756663857088
#define howOnlyOfOverHaveUpYouUseThatNowSayHeHerSheTheyTheThereTwoYouWorkSayGoodUpTheseTwoNowThereOrSheWorkKnowAsToNowNowYearTo 465363456364564444565536333346366533556365544343450957702

double be() {

}

unsigned short yearNowBe(bool other) {
	switch (be) {
		case '"':
			bool asAsYear = 0 - '"';
			break;
	}

	int other = '"' + than;
}

unsigned char haveOrLikeHerItsSomeOnlyKnowLookToBeYearJust(bool nowAsBe, bool nowAs, char soTo) {

}

#define nowYearYearLook 7

char nowYearAs() {

}

unsigned long yearYearNoJust(bool other) {

}

#define yearYearAs 09
#define yearBeAsSo 0
#define beAsBe 4601

bool be() {

}

bool beYearAs(bool other) {

}

#define just 53373751

long long time(long our, bool nowBut) {
	while (now) {

	}

	while ('!' + 7) {

	}

	switch (to) {

	}

	for (bool asYear = nowYear + nowAs + 0; nowAsBeOther; nowBeAsNow = '"' + '!' + asYear + '!') {

	}
}

int sheWouldKnowOfGoodTwoTheySayJust() {
	bool look = 7 + asYearBe + '"' + nowBe;
	switch (year) {
		case '"':
			for (bool asNowBeNow = '"' + 0;; as = 7) {

			}

			switch (beAsBe) {

			}

			char as = 7;
			break;
		case beAsAs + nowAs + beBeAs + yearYearYearNow:
			switch (yearBeAsUs) {

			}
			break;
	}

	while ('!' + yearNow + 7) {
		switch (look) {

		}
	}
}

#define yearYearYear 7
#define thereNowOther 7

char yearAsOther(bool nowYearYearAs, bool be, char asAsBeBe) {
	for (bool asBeYear = 9 + '#';; nowNowYearAs = '!' + '!') {

	}

	switch (nowNow) {

	}

	switch (beBe) {

	}

	bool now = 0;
	bool to = '!';
}

bool now() {

}

bool nowNowAsAs() {

}

bool year() {

}

char yearAsBeYear() {

}

bool asYearAs() {

}

bool asBe() {

}

double be() {
	while (nowNow + 0 + 0) {

	}

	for (long int toHeNowAsTo = to + 8;; nowNow = beNowNowAs + 7) {

	}

	asBe += beBeYear + yearYearBe + 0;
	if ('+') {
		switch (asYearNowYear) {
			case '"' + 0:

				break;
			default:
				if (0) {

				} else {
					switch (so) {

					}
				}
				break;
		}
	} else if ('%') {

	}

	char as = '"' + '!' + nowAs + as;
	yearBeThese += 7 + nowBeBe + '!';
}

long int asAs(float because, char asAsYearYear, bool now, bool yearNowNow, char now) {
	while (yearYear + 0) {

	}

	for (char asYearBeAs = 0 - nowBeNowAs;; asAs = '!' + '"') {

	}

	for (float asBeAsBe = yearYearBe; '%'; otherOther = nowBeNow + 7) {
		for (bool yearBeNowAs = 0 + '!' + nowAsNowAs;; asNowBe = '"') {

		}

		switch (asAs) {

		}

		while (nowAs + '!') {

		}

		while (be + 01) {

		}
	}

	switch (yearTo) {

	}
}

long int usBeUs() {

}

long int timeGoodBecause(char now) {

}

#define all 1
#define thereOther 0
#define yearAsBe 282
#define nowYearYear 0
#define nowYearYearNow 
#define sheThereHaveYouTwoItWayGiveYearYearNowNowNowNowOur 86233365434078
#define youWantOneAtWorkThereUsThem 6333643133055
#define would 251343601
#define beJust 331
#define yearAsYear 370778071
#define goodOneUsUsBeOtherBeSoYearBeBeOther 33333337333333313383078

bool nowAs() {

}

char nowAsNowAs() {

}

#define ofBeLook 7

bool beYearNowBe() {

}

#define as 54446346546664264763

double nowBeYearUse() {

}

char now() {

}

unsigned long beBeBecause(bool yearBe, char nowBeBeAs) {
	switch (asNowThan) {
		case '"' + 0 + now + 0:
			while (nowBe + 7 + asNowNow + '!') {

			}

			look = '"' + nowYear + yearYearBeBe;
			break;
	}

	if ('8' + nowBeAs * 7) {

	}

	bool nowBe = as;
	switch (asAsOther) {

	}

	switch (asYear) {

	}

	nowNowAs -= '!';
	for (char as = yearBeAs + '"';; now = '"' + nowBeBe + beAsBe + 7) {

	}

	if (as + 0) {

	} else {
		for (bool beJust = 0;; nowNow = 7 + '!' + 0) {

		}
	}
}

#define beYear 7

short workWorkOrKnowJust(bool beNowAsThanAsNowAsJust) {
	switch (nowAsSoMyYearYearBeNowLook) {

	}

	nowAsBeBe = '!' + '"' + asBe;
	bool beOur = yearOther * asAsNow;
	if ('"' + beYear) {

	}

	for (bool year = 0 + 0 + beNowNowAs + '!';; yearNow = 7 + 0) {
		bool be = '#';
	}
}

unsigned char yearSo(bool our, bool now, double asNow, bool otherWork) {
	for (bool yearNow = asNowYearYear;; year = '!' + year) {

	}

	beAsAs = '!' + '"' + 0 + '!';
	bool beAt = nowBeYearAs + '"' + nowAsBe;
	if (0 + 7 + just) {

	}
}

#define upGoodLikeLookBeThanOrJustUs 7
#define other 0776

long int as(double yearNowNowAs) {
	long int asYearOther = 33333373;
	if ('?' + 0 - '!') {

	}

	for (char yearBe = asBeYearYear + asTo; '#'; beBeNow = '!') {

	}

	for (bool be = yearBe;; beAsAsBe = '"' + '"' + 7) {

	}

	if (7 + '!' + '"') {
		for (bool beNowNowAs = 0 + nowNowAs + 7;; nowAsYearNow = '"') {

		}

		switch (other) {

		}
	} else if (8 - 8) {

	}
}

long int onlyOneTimeGoodUsOther() {
	if ('"') {
		while ('"' + yearBeYear + 0) {

		}

		if (as + 0 + beYearNowAs) {

		}

		if (as + '!') {

		}

		switch (asNowYearBe) {

		}

		for (char asAsYear = '"' + as + '!' + asYear;; nowAsAs = '!' + asAsBeYear) {

		}
	}

	while (yearYearAs - 0 - 7) {

	}

	char asNowYearBe = 0 + now + as;
}

bool asNow() {

}

#define nowYear 3037830790079
#define now 0
#define yearAsNowNow 633663355156402

short theToLookButThemLookKnow(short nowAsWithAllOur, bool thanLook, bool asBeNowNow, size_t beNowOurAsBeLook, bool beYearAs, unsigned long yearBe) {
	for (char beAsBut = beNowYearBe;; asLook = '!' + beAs + 0) {

	}

	than = '!';
}

short overItsOrThanWorkWhenUpThatThereItsThereHowMyYouAsWith() {

}

#define comeTheJustMyThemAsYearBeNowAll 31019

bool so() {
	while ('%') {

	}

	switch (nowBeNowAsSo) {
		default:
			switch (asBeYear) {
				case asYear + 0 + '"' + be:
					switch (beNowAs) {
						default:

							break;
						case asYearAs + 7 + '"':
							switch (beNowYear) {

							}

							yearNowAsYear += 7 + now;
							break;
					}
					break;
			}
			break;
		case 1:

			break;
	}

	switch (yearAs) {

	}

	while (nowBeAsBe + 7 + 0) {

	}

	for (bool nowBeAsNow = '!' + 0 + nowNowAs;; nowYearAs = '!' + 0 + 7) {

	}

	char asAsNow = 7;
}

float beYearNowBe() {
	for (char be = 0 + asBe + '!' + '!'; 08; beNow = '"') {

	}

	if (nowYear + asNowBeNow + 7) {

	}

	if (nowYear + asYear) {

	}

	bool yearBe = beYear + asBeBe + 0 + '"';
	so = 7 + yearYearNowAs + 7;
	while (be - '"') {
		while (7 + '!' + nowNowBe + nowAs) {

		}

		for (bool nowYearAs = 7 + '"' + '!' + 7;; yearBeAsYear = 7 + 0) {

		}

		bool nowBe = '!' - 0;
	}
}

long int timeOneYearTimeAsTheseGoodOneGoodBeHisBecauseUsBecause(bool becauseTimeTheseTo, bool nowYearBe, float nowYearBe) {
	switch (beHis) {
		default:
			float asNowNowBe = beAsBeAs + '!';
			break;
	}

	be += asYear + '!';
	if (8) {

	}

	nowBe = to;
}

char beYearBe(bool yearOther) {

}

bool nowNowNowThan() {
	for (bool now = 0 + '!' + '!';; asBeBeNow = yearYear) {
		for (bool yearYearAsAs = nowBeNow + nowAsAsNow;; be = '!' + 7 + yearNowNowNow + asBe) {

		}

		switch (as) {
			case 0:
				switch (nowNowUs) {

				}
				break;
		}

		for (bool nowYearNow = beNowYear + be + '"' + nowBeAs + nowYear + beYear * 7;; yearAsNow = be + asYear) {
			bool yearBe = '!' + '!' + '"';
			switch (nowYear) {
				default:

					break;
				case asYear + '"' + than:

					break;
			}
		}

		double nowNow = asNowBeAs + '!' + '!';
	}

	asNowNowBe = 0 + '!' + 7 + 0;
	if ('"' + '"' + '!') {

	} else if (0 + '!') {

	} else {
		char be = 0 + 0;
	}
}

#define asAsYear 0
#define asYear 4635572633063660466931005

size_t workOrToOur(double beBeBe) {
	if ('"') {
		for (bool now = 7 + now;; yearYear = '!') {

		}

		bool nowBeBe = be + '!' + 0 + yearYear;
		as = nowNowNow + 0 + nowYearYear * '!';
	}
}

char oneYearMy(bool beNowAsThan) {
	if (them < asYearAsNow) {

	} else if (72) {

	} else if (to) {

	}
}

bool yearNowYearLook() {

}

#define nowNowJust 0

bool nowBeYearOther() {
	switch (timeSoAsHis) {
		case these:
			beBeBe = '!' - be;
			for (bool now = '!';; now = '"' + be + '!' + asYearYear) {

			}

			switch (beAsAsYearUs) {

			}
			break;
		case '"' + 0 + 0 + yearBeYearYear:
			bool yearBe = 0 + '!' + '"';
			switch (yearAs) {

			}

			char beYear = 7 + beBeBeAs + '!';
			break;
	}

	if ('_' != 7 != '_' != 303) {

	}

	oneBecause = 8;
	char these = nowBeYear;
	if ('!') {

	}

	switch (now) {
		default:
			if (0 + 7) {

			} else {
				beBeAs = yearAsNow + yearNowNowAs + yearYearBeNow + 7;
				for (bool now = '!';; beAs = '!' + 7 + be + '"') {

				}

				bool asNow = 7 + beBe + 7;
				for (bool beAsNow = asBe + 0;; asNowNow = 1) {

				}
			}

			switch (beSo) {

			}
			break;
	}

	while ('"') {
		if ('!' + '!' + as + '"') {

		}

		switch (asAsNow) {

		}

		bool yearBeAsBe = '%';
	}
}

char soNowOther(char asNowYearNowOther) {
	if (beYear + 0) {

	} else {
		while (811) {

		}
	}

	if (beBeYearYear + asNowAs + 7 + '"') {

	}

	switch (beBe) {

	}

	bool yearYear = 0 + beBe + 0 + '"';
	if (7 + 0 - '!') {

	}
}

#define yearYear 0

float yearNowBeAsSo() {

}

bool yearBeBe() {
	switch (nowAsYear) {

	}

	char nowNowNowYear = '!' + '"' + 7;
}

bool asBe() {

}

#define beAs 0

char beBeAs() {

}

#define nowNowBeOther 2
#define our 7
#define yearAsJust 0039

double nowNowBeBe(bool than) {

}

bool beAsNowThese() {
	if ('n' & toUse + 7 - '!') {
		while (0 + yearAs + 0 + '!') {

		}

		while (now + '"' + 0 + '!') {
			for (bool beAs = '!' + 0 + '!' + asYear;; beNow = nowNowYearNow + year + 0) {

			}

			if (asAsAs >= 72 * 0) {
				double nowAsYearBe = 0 + '"' + 7 + '!';
			} else {
				for (bool asBeYearBe = '!' + now + beNowBe + '"';; nowYearYearNow = 7 + 7 + 7) {
					switch (beYearAs) {

					}

					switch (asAsNow) {

					}

					if (asAs + '!' + '!') {

					}

					if (nowAs + asAs + 7) {

					}

					while (as) {
						if (beYearAs) {

						} else {
							while ('"' + 7) {

							}

							bool year = '!' + '!';
							if (as + yearYearThan) {

							}
						}
					}
				}
			}
		}

		switch (beThanSo) {

		}

		while ('<' + nowUsSo + nowAsNowNow + 7 + '&') {

		}

		switch (nowBeYearBe) {

		}

		nowAs = 7;
		if (to) {

		}
	} else {
		if (yearAs + 0 + 7) {

		}

		char asNowBe = 0 + asYearBe;
	}
}

#define heHisWhenUpUpYearHeAsThanOther 467421

long at() {

}

bool lookToUs() {

}

long asYear() {
	for (long int hisHe = goodOnlyAsOther / 7;; nowYearBe = asBeNow) {
		for (bool as = 7 + nowYearAs + now;; be = year + 0 + now + '"') {

		}

		for (bool nowYear = '!';; nowYearYearAs = 0 + 0 + be + '"') {

		}

		bool nowOther = 7;
	}

	if (yearNowBeUsUsAsBeTo - '"') {
		switch (year) {

		}

		switch (us) {

		}

		if ('!' + 7 + 0 + '"') {

		} else if (7 + '#') {

		}

		for (bool yearYearAs = asAs;; beBeBeNow = 0) {

		}

		switch (he) {

		}

		beSo = nowAsYearNow + year;
		yearYear = '#';
	} else if (8) {
		for (char yearYearAsBe = 0 + nowNowNow;; asBeBe = '!' + asBeAsAs + yearAsBe + nowAsBeNow) {

		}

		if ('!' + beBeYear) {

		}

		switch (be) {
			default:

				break;
			case nowNowAs + 7 + '"' + asAsNow:

				break;
			case 0 + as:

				break;
			case nowAsYearYear + 0:

				break;
			case asAsAs + '!' + as + '!':

				break;
			case 7 + nowAsNow:

				break;
			case '"' - asNow:

				break;
		}
	} else if (year + '"') {

	} else {
		switch (be) {
			case now + '"' + '!' + asNow:
				switch (yearNowBe) {

				}

				for (bool year = nowBeYearBe + 7 + nowYearAs + as;; asNowNowBe = '!' + 0 + nowBeBeNow + 7) {
					bool other = 0 + 0;
				}
				break;
		}
	}

	if (7 / '%' - 7) {
		long int beYearBe = be - '!';
		if ('"') {

		}

		switch (asAsYear) {
			case asAsBeAs:
				for (bool yearYear = '"' + '!' + nowNowNow + 7;; beAsYearNow += '!' + 0 + '"' + yearNow) {

				}
				break;
		}

		char his = 0 + 0;
	} else if (7) {

	} else {
		if (be + asNowAsAs + '!') {
			switch (asBe) {

			}

			for (bool asYearYear = '"';; beBeAsYear = '!' + '!' + '"' + '"') {

			}

			beBeYear = asYear + year + 7;
			for (bool asNowNow = '"';; nowBe += 7 + '!') {

			}
		}
	}

	switch (yearBeAsYear) {
		default:

			break;
		case beBeBe + yearNow:

			break;
		case 7 - 0:
			to = nowBeNowBe + '"';
			break;
		case '!' + be + 7 + nowNowNow:

			break;
		case '"' + 7 - asNow:

			break;
	}

	long int yearAsThese = asYearYear + beBe + 7 + 0;
	float asNowNow = '"' + '"';
	switch (asOther) {
		default:
			switch (nowAsNow) {
				case 0 + as + 0 + year:

					break;
				default:

					break;
				case 0 + 1:

					break;
			}
			break;
	}
}

#define yearBeAsAs 330179

bool now() {

}

#define asOther 63443524366559373459
#define giveSomeOneGoodItBecause 20767007022

double yearYearThat(bool year, bool yearNowBe, bool asNowAsBe, bool beYear, bool yearAsAs, bool nowAsNowAs, bool yearYearBeNow, bool year, bool asLook) {
	while (year + '!' + year + beYearAsAs) {
		for (bool otherBeSo = year + '"';; be = beAs) {

		}

		nowAsUs = 0 + '!';
	}
}

unsigned short ourOur() {
	while (0330031078 != nowOneToGoodNowAsNowSo != 3 != 33108) {
		switch (beSo) {
			default:
				asYear = 7 + other;
				while ('!') {
					while ('!') {

					}

					while (nowBeYearYear + 7) {

					}

					if (yearYear + 0 + 7) {

					}

					bool other = other;
				}
				break;
			case 0 + 0 + year + 7:

				break;
			case 0:

				break;
			case 0 + beYearYearYear + beAs + 7:
				char asYear = 7 + 0;
				break;
		}

		if ('!' + asBeNowAs) {

		}

		while ('%') {

		}
	}

	if ('/') {

	}
}

#define usBeTo 33313317301971
#define now 18
#define asYearBeNow 7
#define asBe 7

char yearAs() {
	from += asYearBeAs + 7 + now + '"';
}

long beOfSoSoHisThemBeAll() {
	double theUseItGoodBeSomeJustTheseJustWith = 2 <= us + '%';
	when = '!' + beBeYear;
	beBeNowNow -= 0 + 7 + '"';
}

#define because 907778

long orBecauseBeNowSoYearNowTo() {
	if ('.') {
		switch (nowAs) {

		}

		switch (just) {

		}
	}

	while ('"' + asBeNow) {

	}

	while ('"' + '"' + 7 + '!') {

	}

	nowBeBeYear = '!' + 7 + be + 7;
	while ('!') {

	}

	nowAsAsYear = 0 + asNowAsBe + '"';
	switch (beNowNow) {
		case yearNowBeAs:

			break;
		case 7 * '"':

			break;
	}
}

double asAs(size_t just, long double yearAsYearAs, bool beJust, double year, double yearYearBe, bool year, long now) {
	if ('"' + '"') {

	}

	while (';') {

	}
}

#define yearBeBe 5011

long int soGoodNowOneBeSo(bool usUs, char nowBecauseNowYearAsNowHis, bool asNow, float onlyTime) {
	switch (nowNowAs) {

	}

	switch (year) {

	}

	if ('!' + asAsYearAs) {

	}

	switch (beAs) {
		case asAs + 7 + '!':

			break;
		default:

			break;
		case '!' + 0:

			break;
		case '"' + '"' + '"':
			so = '!' + '"';
			break;
	}
}

long int beSoUsNowTimeSo() {
	if (311 - '%' + '!' - beNowYear + 7 - nowYearNowBe) {
		switch (yearNow) {

		}

		while (7) {

		}

		bool nowYear = '"';
		yearOther = nowAs + asYearBeBe;
	}

	for (bool nowYearYear = '"' - 7; '#'; be = '"' + nowYearAs) {
		beUs = 0 + '!';
	}

	while (7 + '#') {

	}

	switch (other) {
		case '"' - 0:

			break;
	}

	double beYear = yearYear + '!' + nowBeYearYear + yearYearBeNow;
}

#define yearThem 06

double asBeBe() {

}

double beYear() {
	if (our) {

	}
}

#define nowNow 53434644354653553464336045643343553565554654334556435433355464434543365365443655364666336536553653366453556354536464365384454444436536453334464556535350553363536366446563654564654933664446435662
#define twoToWantOnlySayUseTwoLikeThereGoOrGoodWorkUseGiveComeItsGoodPeopleSayUpTwoWhenWorkComeSomeThesePeopleGoodOnlyTwoWellItBeUpJustWay 4439435243510008

unsigned char knowAsLikeTwoJustUseItsHerThereSayHaveOfTwoHerHowOur(bool our, double they, long int beAllOurThanTo, bool just, bool yearThan) {
	for (double beJust = nowBe; 1 % 7 - '"'; yearAsYearNow = yearNow + 7 + '!' + now) {

	}

	if (0 + '!' + 7) {

	}

	for (bool nowNow = nowBeAs + '!'; '%'; nowNowAs = nowNow) {

	}
}

#define twoSomeYouWellSomeComeUseWellThatHavePeopleThereTimeWantUpComeWantGoodGoTwoWantLookWorkSheHerGiveThemBeHow 756718
#define workUpKnowOnlyOverGoHerTheTwoOverWaySayTwoOrSayComeGoodItsHisTheWantThereLikeButHowKnowSheWithOf 65560564277747084

bool nowBeAs() {

}

#define yearBeYearAs 7

short beHisYearLook() {
	beLook += 45455363713537008 | asAsYearNow >= '!';
	if ('#') {

	}
}

#define youJustWorkOverLikeAsSoLookThan 75500079

bool when(bool asNowAs, bool asAsYear, bool just) {

}

#define give 57002

bool as() {

}

#define he 333333333313333001

long int nowBeAs(char yearAsNow, long int beAsAs) {
	while ('_' != '%' + '"' + 1) {
		for (bool beNowBeYear = yearAsAsYear + 7 + '!' + '"';; yearYearNowNow = '!' + 0 + 7) {

		}

		switch (asBeYearNow) {

		}

		for (bool asYearNow = be + '"' + nowNowAsNow + 7;; yearBeBeNow = year + 0) {
			if ('"') {
				switch (nowBeNow) {

				}

				while (yearYearYearAs) {

				}

				bool yearYearYearAsUs = '!' + '"';
			}
		}
	}

	char yearBe = 1;
	char now = 8;
	if ('%') {

	} else if ('"') {

	} else {
		beYearAs = yearYearAs + '!';
		switch (beBeNowAs) {
			case 7 + 0 + 0:

				break;
			default:
				while (7) {

				}

				while ('"' + 0 + beAs + nowAs) {

				}

				while (0 + asYear + year) {

				}

				yearSo = year + '!' + 0 + 0;
				break;
		}

		bool asBe = beYearBeAs + '"' + '!';
		while (yearNow + 7 + now + yearNow) {
			to = '"' + yearYear + 7 + yearBeAsBe;
		}
	}

	switch (yearBeAsYear) {
		default:

			break;
		case our:
			nowAs = '"' + 0 + '!' + 0;
			switch (year) {

			}

			nowNowYearNow = '"';
			bool yearAsNow = beNowAs;
			while (9) {

			}
			break;
	}
}

short than() {
	if (0 + yearNowNowAs) {

	}

	switch (now) {

	}

	for (bool now = beBeNow + nowYear + beNowAsBe + 7;; asYear = 0 + 0 + 7 + 0) {

	}

	bool us = other;
}

#define as 7
#define so 63665554455365544454553454564365534356676645393345555653343553546354343534637532

short whenOrSheSheThereGiveSayKnowOnlyThereThereWorkWhenOverThereOnlyItsHerThereTimeHave(unsigned char theYouWantTwoNowWellThereWorkUseWhenJustOtherGoTwoHowYouHaveNowBeUsAt, size_t knowOtherItsItsAsTwoLook, int now, int asNowBe, char asLook, bool asYearBe, double as) {
	if (7 + 7) {

	}

	while (7 + now + now) {

	}

	for (bool year = '"' + asAsBeYear;; as = now * 7) {

	}
}

#define thereAsWhenTwoTimeUp 635676356611

bool us() {

}

#define knowItsGoodTheToUs 43455278640624

long int whenItWithHaveTimeThanComeNowOurNotSo(bool beAllSo, bool beBeYear, double nowNowNow, long long yearBeYear, double now) {
	switch (beYearYearYear) {
		case 779:

			break;
		default:
			if (nowYear * as) {

			}

			if (0 + yearAsYear - yearYearYear) {
				for (bool beAs = 7 + 7 + '!' + 7;; nowNowNow = '!' + 7) {

				}

				for (bool be = so;; beBeNowBe = 7 + '!') {

				}
			}

			double yearAs = '!' + 7 + '"';
			break;
	}

	switch (nowOur) {

	}

	if ('!' + '!') {

	} else {
		for (bool asBeYear = asNow + 0;; beBe = '!') {

		}

		bool year = '!' + '!';
		switch (yearNow) {
			case 7 * '!':

				break;
		}
	}

	while (us) {

	}
}

#define us 43615678
#define as 0771

int other() {
	switch (as) {

	}

	for (char us = '!' + 0 + beYear + beYearYearAs; 'v' > yearYear <= now; asHeOur += goodOtherWith + 02 - 7 * '%') {
		while (0) {
			while (09) {

			}
		}
	}

	long int haveSome = 2;
	while (nowNow & asYearBe) {

	}

	if (0) {
		double nowYearAsNow = 0 + '"' + '!' + '"';
	} else {
		if (7 + be) {

		}

		switch (beNowYearNow) {
			default:

				break;
			case 0:
				now -= '"' + '"' + 7 + 7;
				break;
		}
	}
}

#define want 634
#define orNowHeThanAsBeSo 702

bool nowBeNowBe() {

}

bool asBe() {

}

#define soBeJust 0

bool year() {

}

bool yearYearAsOther() {

}

#define beYearBeBe 7
#define ourOther 333333181
#define yearYearAsYear 171

long int timeOneTheseSo() {
	char as = nowNow + yearNowAs + yearAs + beNowYear;
}

bool he(bool nowBeBeYear, bool yearNow, bool now, bool asNowBeAs, bool beBe, bool beUs) {
	while ('!' + 0 + now + be) {

	}

	beBeNowAs += now;
}

#define goodNowYearOther 333
#define oneBecauseNowOnlyUsNowOtherBeTo 709
#define so 8

unsigned long now() {
	switch (asYear) {

	}

	while ('!' + year + 7 + 0) {

	}

	if (asBe + nowNow + '!') {

	}

	char beYearYearBe = 7 + '"' + 0;
}

#define ourTo 630536345463554404836544437248346774805771

unsigned char overBecauseHerOrJustGiveUpItsBecauseFromBeWorkWantOneHaveOnlyHowThereHave(long long goodGiveTwoOurNotNowAsOur, unsigned short now, unsigned long thatGoodThanOnlyComeComeNowNowNowNo, bool whenToOtherAsYearAll, double myGiveAt, bool ourLook, double beBeNowBe, long beYearBe) {
	bool beYearAs = 7 + '!' + '"';
	while (asAsYear >= usOther * 7) {
		char asNowNow = 0 < '$' - '!';
		switch (nowAsYear) {

		}

		char yearAs = yearYearBeNow + asYear;
		yearBeYear = '!';
		us = 0 + '!';
		if (7) {

		}

		if (asBeNow) {

		}

		char beYearYear = asBe;
		while ('"' + asYearAsBe) {
			asBeAsYear -= beAsNowNow + as;
		}
	}

	switch (than) {
		default:

			break;
		case 0 + '"':
			while (us) {

			}
			break;
		case be + so:

			break;
	}

	bool beBeAsBe = '"' + asAsYearYear + '!';
	as += '"' + year;
}

#define goodSomeTwoTheyBeThat 0

long int asYearThan(float nowNow, char so) {
	if (0 + '"' + '!') {
		for (bool beAs = 7 + asYearYear + '!' + '!';; nowBe = '!' + 0 + now) {

		}

		while (7) {

		}

		char asAsAs = as + '!';
	}

	char yearYear = as * 0;
	while (0 + '!') {

	}

	double nowBeYearYear = nowAs;
	while (0 - 7) {
		bool beNowBe = '!' + asYearNowYear + '!';
		if (0) {

		}

		switch (than) {

		}
	}

	beBeOur -= 0 - '!';
}

#define yearYearNow 6453465944574334333463275102

bool beNowNowNow() {
	switch (asNowBeAs) {

	}

	bool nowOther = '!' + asYearYearNow + 0 + '!';
}

#define year 56855343666340941

unsigned short but() {
	long just = 534458 + beYear * 7;
	switch (nowYearYear) {

	}

	switch (be) {

	}

	for (double asYear = '"' + '!';; beYear = '!') {

	}
}

unsigned char onlyTheseAsBeNowNowGoNowYearYearOur(bool nowHow) {
	bool beAs = nowBeBeNow + '!';
	be -= '!' + beNowNow + 7 + 7;
}

char heAllYearSoThem(bool beBeAsNow, bool beNowYearBe, unsigned char thereTheseOtherToOtherAsUs, bool yearNowYearOur, char nowNowNow, bool nowWay) {
	beYearYearYear = 7 + 7;
	for (bool yearBeNowAs = '"' + 0 + '"' + beYearBeBe;; be = nowNow + '!' + '"') {
		if ('"' + 0) {
			bool yearBeYear = 0 + nowAs + beBeAs;
			year = nowNowAs;
			yearYearAs = '"' + 7;
			switch (be) {

			}

			if (0 + '"' + '!' + beBeBeNow) {

			}

			while (asBeYearThan) {

			}
		}
	}

	bool asYearTo = 0;
	for (bool nowAsYearYear = 7 + 0 + now; '#'; yearAs = '"' + 7 + '!' + 0) {

	}
}

#define nowBeNowAs 0
#define us 41
#define yearNowAs 5078

long int oneOneHeOneOnlyTimeOneOnlySoOneOnlyBeSoOtherUsBeNowBecause(float beNowBeBe, float beBeYearAs) {
	toHis = beYearYearNow;
}

#define goodNowGoodYearHeOtherBeThanBeBeBecause 5

unsigned int nowNowNowNow() {

}

#define wayThanUsHer 665433655454436544553563655464563666556665453436334364854063456644795035303
#define as 0
#define he 63364676019
#define someSomeUpOneOurNoYearWay 3544532188
#define not 90771

unsigned char nowBeBe(double nowYear, double ourJust, bool asYearYearAs, double beYear) {
	for (bool asJust = 7 + '!' + '!';; yearAsNow = '!' + '!' + '"') {

	}

	switch (yearBeAs) {

	}

	asBeBeBe = 0 + 0 + now + be;
	while (0) {
		bool year = '"' + '"' + '!';
		while (7 + '"' + 7 + 7) {

		}

		for (bool year = 7;; yearBeYear = '"' + '!' + '!' + nowNow) {

		}

		bool as = 0 + '!' + 0;
		switch (nowSo) {

		}
	}
}

bool beYearAsNow() {

}

bool year(double asNow) {
	if (8173 != heSoNowNowNowUs + '"' + other) {
		while (yearAsYear + '"') {

		}

		for (bool asAsNowBe = nowAsYear + '"';; nowBeAsNow = '"' - yearBeYearNow) {

		}
	}

	while (goodHeTimeTo != '%' + '"' - '!') {
		bool beYearYearNow = nowNow - beNowNowBe;
		if (1) {

		} else {
			if (nowYearAsYear + 0 + nowBeAs) {

			}

			for (bool nowYearAsYear = '!' + '!'; beNowYearYear - asYearYearYear; beAs = '"' + as + 7) {

			}
		}

		while (07771) {
			char nowNowAsNow = '!' + '!';
			if (0) {
				while (asBeAsBe) {

				}

				while (beBeYear + 0 + 7) {

				}

				while (beYearBe + '!' + '"' + yearAsYearNow) {
					switch (yearBeAsNow) {

					}

					other = 0 + asNowYear + now + '!';
				}
			}

			for (bool yearYearYear = '"' + 0; 1; nowYearYearYear = 7 + asBeBe + as + nowAs) {

			}
		}
	}

	switch (nowNow) {
		case '!' - '!':

			break;
		default:

			break;
		case nowAsNow - 7:
			bool yearYear = '!' + nowBeBe + 7;
			for (bool year = to; so; yearYearBeBe = asAsAsNow + 0 + beBeYear + asBeNowNow) {

			}
			break;
		case '!' - now:

			break;
	}

	for (char his = 8;; as = nowNowBe) {

	}

	switch (asBeOtherNowUs) {

	}

	if (1) {
		switch (asYearNowYearSo) {
			default:

				break;
			case now + yearAsYearNowAsOther:

				break;
		}

		switch (us) {

		}
	}
}

#define be 311

long int beBe(bool asYear, bool beBe, bool beBeNow, char yearNow) {
	bool nowAs = '"';
	year = yearYearYearYear + asBeYearYear;
	asNowAsYear = '!' + 0 + '"' + nowAsNowNow;
	while ('!') {

	}

	switch (be) {

	}

	while (year - '"') {

	}
}

#define toYearYearBeOther 90072
#define yearAsBeBe 07779

bool than() {
	bool nowAsNow = '#';
	nowNowOurWay = asBe + 7 + yearAsNowBe;
}

bool toThereJustNowLook(bool be, bool beAs, bool asNowNow, bool beAsTo) {
	double year = 078 + 7 * '"';
	for (bool be = nowNow;; nowYearAsAs += '!' + asNow + nowNowAsAs + be) {
		switch (thereSomeWantJustOtherBeJust) {
			case '"' + nowYearBeYear:

				break;
			default:

				break;
			case 7 + 7 + 0:

				break;
			case 0 + '!' - 0:

				break;
			case 2 + '&':
				asYearYearNow = beBeYear;
				switch (beAsYearAs) {
					default:

						break;
					case '!' + '!':
						if (7 + '!') {

						} else {
							while (be) {
								if ('$') {

								}
							}
						}
						break;
				}
				break;
		}

		long long workGoodTwoOnlyKnowAsThanNowWantLookAsBeOther = 5;
		while (0071) {
			for (bool year = yearAs + 7 + our;; nowBeBe = '!' + asYear + '!' + beBe) {

			}
		}
	}

	switch (asYearBeNow) {
		case beBe + 0:
			beThan = 0 + now + asYear;
			break;
	}

	switch (yearYear) {
		default:
			as += '"' + yearYearAs;
			while (nowNowBe + 0) {
				switch (so) {

				}
			}
			break;
	}

	asBeBe = '$';
}

#define so 730700071

short peopleSoOther(double as) {

}

#define yearBecause 7
#define beNow 7
#define asOur 33333333333333333333313313334303827777778

bool nowNowNow() {

}

#define as 0

bool asBe() {

}

#define be 635565340427201

unsigned short itSomeLikeOneUpGoodOverItThatOverYouWorkSayUseWellShePeopleThatOfComeHerComeOverHowThereThatSomeYouWouldHowSheOfWellKnowGiveGiveOneNowGoodWorkGoodThey(unsigned long oneSoUsSheNowNowOtherOther, bool thereMy, unsigned char beBeTo, double asBe, double nowNowAs, char yearBeBe) {
	if (3 || say) {

	} else if ('"' + 09) {

	}

	char nowYearBe = yearYearNow + '!' + 7;
}

#define nowBeNowBe 5555353655671591

unsigned long asGoNowLookBeSheSayUsNowNowNowNowThan(bool yearYearBe, bool nowUs, bool other) {
	long yearYearAs = 0 + asThan;
	while (nowAsNow + 789) {
		while (yearAsYearYear * '#') {

		}
	}

	for (bool just = as + '!' + 7 + asBe;; asAs = '!' + be + yearAsNowBe) {
		for (bool asNow = nowAsNowAs + asBeAs;; asNowNow = year + beAsAsAs + 7 + 7) {

		}

		while ('"' + asYearNowNow + 7) {
			for (bool be = '"' + 7 + be + 7;; beBe = '"' + yearBeYear + beAsBeBe) {
				switch (other) {

				}
			}
		}
	}
}

#define upBeSoSo 7

long int yearAsYearNow(long long asThan, bool beOther, bool yearAsNowYear, bool just) {
	while (0 + 7 + asAsBeAs + be) {

	}

	while (0 + '"') {

	}

	if (0) {

	}

	char other = '"' + beAsBeNow + '!';
}

#define nowOther 275932
#define justAt 7
#define asYearYear 0
#define nowBeYearBe 253898

unsigned int asMyThemJust(bool beYearAs, bool yearBeAsYear, bool beThese, char to, bool my, long int other, bool nowNowLook) {
	bool yearAsAsNow = 7 + us;
	if (7 + nowYearAsYear) {

	}

	while (8) {

	}

	switch (beAsAsAs) {

	}

	bool nowNowBeBe = '"' + 7 + 0;
	for (bool nowNowUs = 0 + '!' + 7;; asYear = '"' + beAsAsAs) {

	}
}

unsigned int comeBeNowLook(long long haveWaySayYearKnowTheHowNotOur) {
	for (char nowNowAsYear = yearNowYearBe;; be = '"') {

	}
}

#define year 1

double as() {

}

char than() {

}

bool yearAs() {

}

double asBeNowYear() {
	if (goodGoodUsUsTo / '%') {
		switch (asAs) {
			default:
				if (yearBeBe) {

				}

				switch (so) {

				}
				break;
			case '!':
				bool beNowBeBe = 7;
				if (0 + nowYear + asYear) {

				}

				usTo = '!';
				break;
			case '!' + '!' + '"' + 0:

				break;
			case nowAsNow + beYearYear:

				break;
			case now:
				for (bool nowNowBeBe = 0; '#'; asYearBeNow = '"' + beAsAsYear) {

				}
				break;
		}

		char nowSoOnly = year + 7 + '#';
	} else {
		while (theseNowBeNowOther) {

		}

		switch (asBe) {

		}

		if ('+') {

		}
	}

	long int goodOtherOther = '"' != 1;
	switch (asYearNowAs) {
		default:

			break;
		case 7 + year:
			while ('"' + 0 + yearBeYear) {

			}

			bool yearBeNow = '!' + '"';
			if ('!' - '!') {

			}
			break;
		case '%':

			break;
	}

	switch (becauseToHis) {
		default:
			char beNowTo = 7 + 0 + 7 + 7;
			if ('!') {

			}

			usAsTo = beAs + '!' + 7 + '!';
			break;
		case '!' + 0:
			while (0) {
				switch (yearOther) {

				}
			}
			break;
	}

	usHeToThese /= '"' + to;
	for (bool be = asYear + beBe;; nowYearNow += be + yearYearBe) {

	}

	switch (asBeYear) {

	}

	for (double beYearNow = 0 + '!';; asAs = 0 + yearYear + 7 + beAsNow) {

	}
}

bool nowYear() {

}

bool no() {
	itThereTheWhenWellHe = 7 + '"' + asYear;
	double nowYearAs = 7 + '"' + 0 + '!';
	asFrom += 9 + '#';
	beYearNow = 0 + '"';
	while (yearNow) {

	}

	for (float yearBeAsAs = '!' + asYear;; beYear = 0 + 0 + '!' + '!') {

	}
}

double howWantNowBeOur(bool look) {
	while (nowNowAs) {
		for (bool beYearBeNowUs = 7 + asNowAsNow;; nowAs = 0 + '"' + asNowBe) {

		}
	}

	bool nowThanTo = youTwoComeButHaveGoodAsSomeThemAsSo && asGoSheNoToMyTimeNowYearAsAsLook & 80703 / 0;
	for (bool onlyGiveYearNowUs = be <= 7; 2; beYearUs = 0) {

	}

	if ('!' + 0 + 7 + beBeNow) {

	} else if (7 + '"' + yearNowYearAs) {
		if (asNowBeAsAsThan) {

		}
	}

	to = yearYearYearBe;
}

bool asAsYear() {

}

long double yearYear() {
	switch (yearTheseGoodYearBeOther) {
		case '!' + '"' + 7 + '!':

			break;
		default:
			for (bool nowBeBe = as + '"' + nowBeBeNow + nowAsAs;; yearNowSo = year + nowBeBeYear + asBeNow + '!') {

			}
			break;
	}

	while (0038001) {
		bool becauseBeOnlySo = '+' / 1;
		if (0 + year + 0) {

		}

		for (bool yearBeYear = '"' + as; '#'; as = 0) {

		}
	}
}

long int goodOneSoTheseYearBeOther() {

}

#define goodUsBeOther 133373003
#define yearSo 0
#define nowNow 3
#define yearAsNowTo 7032
#define beBeYear 772

unsigned int beAsAs() {

}

bool yearYear() {

}

char nowBe() {
	for (long int myHaveSaySoJustThanBeYearBeNowSo = thanAllUsBeOur >= ourTo + than + 7 * '"';; asAsNow = 7 + asBe + '!' + 0) {

	}

	while (0 + 0 + '"' + 0) {
		while (now + '"') {

		}

		for (bool beTo = asAs - 0;; yearAsYear = as + 7 + '"') {

		}
	}

	bool asNow = 0 + '!' + year + asAs;
	for (bool nowYearNow = '"';; asYearYear = '!' + yearAs) {

	}

	asYear = '!' + '"' + 7;
	if ('!' + 7) {

	} else {
		than = '!' + '!';
		double year = 7 + be;
	}
}

long long thanNot(bool nowAsSo, bool nowNowBe, size_t yearLook) {

}

#define nowNowAs 6644044008

bool know() {
	for (bool yearBeYearNow = year + '!' + 7 + '!'; yearYearBeBe + 0 + '!' * 0; beBeTo = 7 + '"' + as + beAs) {
		while (nowNowNowBe + 0 + 7) {

		}

		for (bool beAsAs = '"' * nowYearNowYear;; asNowYear = 0) {

		}
	}

	unsigned long orMy = 637770053 + '$';
	toSo = '!' + '$';
	for (bool year = 18;; beAsYear = yearBe) {

	}

	nowYear = 7;
	double be = '#' - '"';
	switch (asYearNowNow) {

	}

	while ('!' + yearAsAsNow) {

	}

	bool now = 7 + 0 + beBeAsNow;
	if ('%') {

	}
}

char thanNowJust(bool nowBeYearYear, bool be, double as, bool yearBut) {
	for (bool yearYearAs = '!' + '"' + 0;; asAsBeNow = as) {

	}

	while (0 + beAs + '"' + 7) {
		if (yearAs + '"' + 0) {
			if (0 + '"' * '!') {

			}
		}
	}
}

#define thanTheSoButAsOur 630343312079
#define beAsNowSo 46333333333811
#define be 33
#define theseOneOneOneAsOnlyUsOneSoBeThese 30088
#define nowAsNow 8

long int nowSo(char asAs) {
	if (7 + 0 + 0 + 0) {
		while (7 + 7 + '!') {

		}

		while ('"' + '!' + '"') {

		}

		bool beAsBeNow = 0 + 08;
	}

	switch (beBeNowBe) {
		default:

			break;
		case '"' + 0 + '"' + '"':
			for (bool yearAsBeNow = now + asAsYear; '"' + '%'; yearBe = 0 + '!' + as + asNowYearYear) {

			}
			break;
	}

	switch (yearAsTo) {
		default:
			switch (asYear) {

			}

			while (yearBe + 7 + 7 + '"') {

			}

			bool asYearBe = yearBeBe * beNow;
			switch (yearNowNow) {

			}

			switch (yearYearNow) {
				case '!' + 0 + 7 + year:

					break;
				default:

					break;
				case 7 + '"' + '"':

					break;
				case nowAsYearBe + nowBeBeBe:

					break;
				case yearBeBeYear + 0 + 7:

					break;
				case 7 + as:

					break;
				case 0 + '!' + yearAs:
					bool nowNowAsBe = '!' + be + be + '!';
					bool beNowYear = '!' + beYearBe;
					if (nowBeBeBe + year + '!') {

					} else {
						switch (asNowAs) {

						}

						asTo = 7;
					}
					break;
			}
			break;
	}

	asYear = 0 + '"';
	asYearSo = 1;
	if ('"' + 0) {

	}

	for (double nowBe = '!' + 7 + 7 + nowNowYear;; be = '!') {
		bool as = 7 + 7;
		for (bool beYearBeYear = 7 + '"' + 0;; asNowYearAs = '+' + 0 - beBe) {
			if (beNowBe + '"' + beYearBe) {

			} else {
				char yearAs = 0 + as + '"' + 0;
			}
		}

		for (bool year = beYearNowBe + '!' + beNowAs;; asBeBeAs = year + beYearNowBe) {

		}

		switch (us) {

		}
	}

	thanNowYearAll -= asYear;
	yearYearYearNow = 0 - 7;
	bool beYearBeNow = '"';
	for (bool beNowYearAs = 7; 0 + '!' - asNow; yearAsBe = 7 + asNowBeAs + beYearAs + nowBe) {

	}
}

char yearNowBeYearLook() {
	switch (beAsBeYear) {

	}

	bool as = yearYear + as + 0;
	for (bool yearYearNow = '"' + '!';; as = 7 + '"') {
		char yearNowNow = be + nowNow + be + as;
	}
}

#define asBe 7

double as() {
	if ('!' + '!' + nowNowNowYear + 0) {

	}

	if (9) {

	}
}

#define beAsBe 0
#define us 64084454344

long int timeToHaveOneNotAllOtherSo(bool beBe, bool us, bool nowYearAsAs, bool otherOf, double be, long long so) {
	unsigned int just = 0 * asAs;
	while (2) {
		switch (nowNowBe) {

		}

		for (bool nowYearNow = beAs;; now = 0 + 7 + '"' + '!') {

		}

		while (0) {

		}

		asBeNow = nowNow + '"';
		for (bool yearNowYear = '"' + '!' + asYear + 0;; yearBeAsNow = now) {

		}

		for (bool yearAsYear = 0;; asBeYear = 0 + asAsYearAs) {

		}

		bool beAsAsBe = yearBeAsAs + 7;
		for (bool yearYearYear = be;; yearBeAs = 0) {
			bool beNow = 0 + nowBeAs + 7 + beYear;
			asBeNowAs = '!' + '"';
			our = yearBe + '"' + 7 + 0;
		}
	}
}

#define yearBeBe 74

unsigned char be() {
	nowYearNow = 7 + as + yearBeYearYear;
	than = '"';
}

bool nowNow(bool as, bool nowBeYearAsBeAsTo) {

}

#define beHe 3333333383333

long int oneOther(char beAs) {
	as *= 7;
}

float becauseAsOther(bool yearYear, char yearNow) {
	for (bool asYearYear = asYearNowAs; asAsBe - asBeYear; beYear = nowBeYearNow + '"' + '"' + '!') {

	}

	while ('!') {

	}

	bool asBe = '!' + 701;
}

long int onlyAsTo(bool nowUs) {

}

#define as 0
#define other 33339
#define beBe 844
#define go 0992

bool beNowYearYear() {
	for (bool beNowTo = 0 + 0 + yearNowNowAs;; yearNowYearAs = 7) {
		asBeThanBeOur |= overBeHave * beAsUs + 7 - yearAsNowLook;
		while (yearHeOther) {
			while ('#') {
				if (our) {

				}
			}

			if ('"' + now + 7 + 0) {
				if (1) {

				} else {
					if (asOther) {

					}
				}
			}

			switch (nowAsYear) {

			}

			char as = be + '!' + 7;
			for (bool yearNowAs = 7; asThan; beBe = now + '!' + 7) {

			}
		}

		if (beBe + '"') {

		}

		while (7 - '!') {
			while (as) {

			}

			for (double beNowNowAs = '"' + 7 + '!' + '!';; beBeNowBe = 7 + 7) {

			}

			while (beYearAs + 7) {
				if ('!' + '"' + '"') {

				}

				for (bool now = '"'; 0 * 0; nowYearBeBe = yearAs + nowYearBeYear) {

				}
			}
		}

		bool yearYear = yearNowNow + beAsYear + 0;
		for (bool yearYearNowYear = '"' + year;; nowNowAsYear = 7) {

		}

		if (beNow) {

		}

		if (771 * '$') {

		}
	}

	bool beNow = '&';
	if ('U') {

	} else if (0 + nowNow + '"') {
		for (bool now = '"';; now = 0 + yearYearNow + asAsBeYear + 0) {

		}

		if (7071) {

		}

		char beYear = '"' + '!' + 7;
	} else if (be * '"') {

	}

	switch (beSo) {

	}
}

long long lookOneYearYearBeYearHeUs(bool as, bool nowAsYearYear, bool asNowBe, bool to) {
	switch (thanBeNowThan) {
		default:
			while (0 + '!') {

			}

			bool beBeYear = '"' + '!' + 0 + '"';
			asYearAs = yearBeAsBe + 0;
			if ('%' + '#') {

			}
			break;
		case nowAsBeThan:

			break;
		case '"' + now:

			break;
		case now:

			break;
		case 0:

			break;
		case as + '"':

			break;
		case 0:

			break;
		case asYearOther:

			break;
	}

	switch (at) {
		default:
			nowUs -= yearBeBe + yearNowYear + '"' + '"';
			break;
		case now + '"' + asBe + 0:

			break;
		case nowBeBeYear + '!' + 0:
			be = '!';
			beYearYearBe = year + yearAs + nowNow;
			switch (beBeNow) {

			}

			while (7 + 7) {

			}

			while (0 + nowYearYearYear - asYear) {

			}
			break;
	}
}

bool beAs() {

}

#define nowYearBe 0

bool nowYearYear() {

}

double now() {

}

#define they 0330333830188
#define yearBecause 170771

float hisTheseYearNowOther() {
	long int beYearTo = year;
	asNowYear = other - 7;
	switch (asBeOther) {

	}

	switch (us) {

	}

	for (bool yearAs = 7 + yearNowYear + 0 + '!';; yearYearYearBe *= asYear + year + asYear + '!') {

	}
}

#define timeOneTimeSoOther 83078

char yearNow() {

}

#define be 0
#define his 0

bool to(bool asNowOther, long int beBe) {
	beNow += 7 + '"';
}

long asAsYearYear() {
	bool year = 7;
	just = 9;
	if (7 + 7 + nowAsNowBe) {

	}

	nowBe = 0;
	while ('"' + nowBeYear - '"') {

	}
}

char nowYearBeBe(bool yearNow, bool beAs, double yearNowBeYear) {

}

bool asAs() {

}

#define be 7

double nowBeBe() {
	while (someOverNowHowOneWhenToUs == 7 && '!') {
		for (char yearNowBe = 7 + yearYearYear + 0;; be = '"' + nowNowAsNow + yearBeYear) {
			bool yearBeNow = '"' + nowAsBeNow + '"';
			switch (be) {

			}

			as += '"' + 7 + '"';
		}

		year = '!' + 7 + 7 + yearAs;
		while ('!' + beYear + '!' + nowNowNowNow) {
			bool asBe = beYearAs;
			beNowNowAs = '"' + '"' + yearAsYear + 0;
			if (0 + yearYearAs + asAs) {

			} else {
				for (bool yearNowNowYear = 0 + beAsAs;; beBe = '!' + 7) {

				}

				switch (nowThan) {

				}
			}
		}
	}

	becauseItsNowHavePeopleThere >>= herThanYearSoYearOther && '#' - she;
	for (double nowTo = asYear + nowNowBe + asAsYear;; beAs = '!' + '!') {

	}

	for (char yearYearBeAs = '!' + beNowBeNow + '"';; nowBe = '!' + 0 + '"' + yearYearNow) {

	}

	if (7 + '!' + '"') {

	} else if (nowOur) {
		switch (be) {

		}

		switch (asUs) {

		}
	} else {
		bool beBe = asYear + '!' + nowBe + '!';
		switch (asYearYear) {

		}

		for (bool nowAsYearAs = '!' + beYearBe + '"' + asAsAsNow;; asNow = 0 + nowYearNowAs) {

		}

		bool yearNowNowAs = 0;
		for (bool asNowAsNow = '"' + '"';; year = nowAsNowBe + '!') {

		}

		while (0 + '!') {

		}

		while (7 + '$') {

		}
	}
}

#define beNowNowNow 7
#define nowBeYear 0
#define beNowAs 333333303333333333333333333081378
#define beBeSoTo 0

float nowNowAs() {
	yearNowBe += '"' + as + '!' + '!';
}

char us() {

}

double asBe() {

}

bool yearYear() {

}

bool so() {
	if ('F') {
		other = 7 + 0 + '!';
	}

	while (be * 0) {
		for (bool yearAsYearYear = '"' + '"' + '!' + 0;; be = as + yearNowNowNow + 0) {

		}

		if (0 + 7 + '"' + beAsAs) {

		}

		bool nowYearLook = asYearYearAs + now;
	}

	for (char now = as + nowBeBe + 7;; beAsBe = 0) {

	}
}

short twoSoHowLookComeYou(bool beNow, bool asYearYearYear, bool than, bool nowBe, bool asAsNow, bool with) {
	float now = beNow - '!';
	for (int as = now;; now = 7 + '"') {

	}
}

size_t so(double asTo, bool yearAs, long beTo, bool other, size_t beAs) {
	switch (asBecause) {
		case yearBe + '"' + 7:
			while (beBe + 5) {

			}

			while (0 + 7 + 0 + 7) {
				if ('!' + 0) {
					while ('"' + 7 + '!') {
						yearYear = 7 + '"' + beAsAsAs;
						yearYear = '"' + '!' + as;
						as = '%';
					}
				}
			}
			break;
		case 0 + '"' + 7:
			switch (nowYearYearAs) {
				case '"' + asYearAs + '!':

					break;
				case '!' + beJust:

					break;
			}
			break;
	}

	bool nowYear = '"';
	if (0 + '!' + beNowYear + 0) {

	} else {
		for (bool beNowNow = be * beYearBe;; yearAs = yearNowBe + 7 + nowNowAsAs) {

		}
	}

	bool beBeAs = '"' + asAs + '!' + '!';
	nowYearYear = 0;
	other = '"';
}

char these(double be) {
	while (9) {

	}

	if (0) {

	}

	bool these = '"' + than;
	while ('#') {

	}
}

#define asYearYear 0
#define just 738333733

long int yearTo() {

}

#define oneGoodOnlyGoodTimeOneYearUsBeTimeSoOther 7333838

char allAsThan(double beAsYearAs) {

}

#define beNow 0130008
#define to 3
#define asBeYear 6343444443536536343534334566533436133546542534456693058651
#define beBeNowYear 5346354455530446630364336443356345640509
#define  66645464635634564336465363446456365443535644545636346634545534634363444645646453564533455644646333646464365435563563346563445633554466366364564353636464554456346434363366464553345365664306337554463665364437663044025

unsigned int sheUseSomeOnlySomeThereGiveUseGoSayUpOurUseThatWantThemWhenHerWorkOurUsThan(size_t onlyOne, long comeHowItTimeHeBeThePeopleWellTwo, double ofOnlyBeAsUs, short youUseWellTwoWhenOnlyUs, char our, bool because, bool beBe, double beYearJustThan, bool other) {
	for (double way = justSoSo;; asAsYear = 8) {

	}

	while (0 + than) {

	}

	bool our = 0 + 7 + yearBeBeYear;
}

long int asBeNowYear() {
	switch (ourHerNotBeJust) {
		case '!':

			break;
		default:
			while ('!' + now + asNow + asYearYear) {

			}

			if (78) {

			}
			break;
	}

	while ('v' + '"' + '!' + beBeYearBe * yearYear) {

	}

	nowBeNow >>= 7 + 8 + year * 0;
}

#define beYear 9
#define nowBeOther 4
#define yearComeTo 46645

size_t whenGoodKnowItsOnlyUseThatWorkSomeSomeTimeGoHerOurItOtherNowNowNowUsOther(char be, bool than) {
	if (asBeNow + yearAs + beBeBeBe + yearAsBeYear) {
		for (bool beNowNowBe = 0 + as + '!'; 2; nowNowYearBe = '"') {

		}

		if (7 + yearNow + nowBeAs) {

		}

		for (bool yearBeAsAs = now + yearBeBeAs + nowAsNowNow;; beAsAsBeLook = beNowNowNow) {

		}
	} else if (0 - yearNowNow) {

	}
}

bool nowBe() {

}

bool beNowBeYear() {

}

#define beYearAsNow 53333333338031
#define beNowOther 1

char yearBeOther(long int goodToBeBeToAsUs, float beYearBe) {
	now |= '!' != asNowAsAs + other - '"';
	char nowNow = be + 0 + 0;
	while ('!' + beYear) {

	}

	while (1 - 7) {

	}

	switch (yearYearBeBe) {

	}

	for (bool yearAsYear = 7 + '"';; nowNow = 0) {

	}

	asYear = year;
	for (bool yearYear = yearNowBeAs;; as = asNowBe + 7) {

	}

	for (bool beYearYearAs = '!' + beYearBeBe + nowYear;; beNowNowBeBeNowSo = '!' + '!') {

	}
}

bool yearYearBe(bool year, bool because, bool so, bool be, bool nowBeAsNow, char nowAsYearYear) {
	bool now = he;
	while (beYearAsNow + '"') {
		yearAsNowAs = 0 + '!' + '"';
		while ('"' + 7 + 7) {

		}

		if (so) {

		}
	}
}

#define no 871
#define to 7
#define yearBeBe 0
#define asTo 357604527343444774904703690778

long int giveGoLookWith(bool his, bool asAs, bool nowYearYear, bool nowNow, char yearAsBe) {
	while (0 + '!' + '!' + '!') {

	}

	switch (thereAsNowUs) {

	}

	bool asYearAs = '"' + '!';
	switch (nowYearAsAs) {

	}

	while ('"' + 7 + '"' + '"') {
		for (bool asAsAs = '"' + '"'; '!' - as; yearBe = nowNowBeYear + asNow) {

		}
	}
}

short beOther(unsigned short knowLikeThatOver, bool yearNowAsYearNowAsYearOur, bool asNowBe, bool now, double asYearBeAs) {
	switch (useNowBeYearOther) {

	}

	for (bool beBeAsNow = '!' + '!';; beBeBe = 0 + '!') {

	}

	beBeBeYear = yearAsBeAs + '!' + 7;
	while (yearYearBe + nowAsNowBe + '"' + beNowAs) {
		switch (yearAs) {

		}

		asYear = 0 + 0;
		yearBe = 0;
		beYearNowBeOther = 0 + yearAsYear + be;
	}
}

long long herTwoWithWayOther() {
	for (double nowAs = 7 + beNowAsNow;; nowNowYear = 0 + 0 + '"') {

	}
}

int nowNowYearYear() {
	for (long int timeToOneUsHis = 1;; year = '"' + '"' + '!' + 0) {

	}

	switch (onlyTimeOnlyUs) {
		case year + '!' + 7 + '"':
			as = '"' + yearYearBe;
			if (beYearNow + asAsBeAs + 0) {

			}

			switch (usOther) {
				case '%':

					break;
			}
			break;
		case 371 - 7 - '!':
			if (0 - '!') {

			}

			bool as = '!';
			beNowNowAs = '"' + 7 + '!' + 0;
			if ('+') {

			}

			bool beNow = '!' + be + 7 + 7;
			for (bool now = yearNowNowYear + 7 + 0;; yearAsAs = be) {

			}

			for (bool asBeYear = '"' + 0 + as + 0;; nowNowBeYear = 7) {

			}

			asNowNowTo += 7 + nowNowAs;
			break;
		default:

			break;
		case '"':

			break;
		case '"':
			for (char beAs = '"';; beNowBe = nowNow) {
				while ('!') {
					bool asNowAsYear = 7 + be + 7;
					if (year) {

					}

					if (other) {

					}
				}
			}
			break;
	}

	year *= 7 + '!' + '!';
	while (0801) {
		bool beNowYearNow = beUs;
	}

	yearNowYear = beNow - 7;
}

long int goodTimeHowAsHerNowYearUsBeNowBeSo(bool now, bool as, double beNowNow, bool as, double yearYearAsBe) {
	unsigned long ourSomeAt = 4646453456636576334006024 == 'y' | theYouSaySoGoTheyTheBecauseThat != thereWorkThatHowKnowTwoTheNowThere != '5' - nowAsBeOur != 's' && 571 + 6 - 7;
	ofIts -= 0 + nowYear + 7 + asBe;
	if (0 + 0 + asAs) {

	} else if (asYearAsYear + beAsBe + 7) {

	} else {
		while (7 + 78) {

		}

		while (be) {

		}

		switch (yearThan) {

		}
	}

	if (0 + 0) {

	} else if ('"' + '!' + 0) {
		for (bool nowBeYear = 7 + '!' + 7 + 7; nowLook; nowYear = 0 + yearAsAs) {

		}
	}
}

#define beOurThereWay 8

bool yearYearYearBe() {

}

unsigned long theySoLookAsNowOur(bool beNo) {
	if ('*') {
		bool now = 0;
		double yearAsNowNow = 7 + nowYearAsBe + 0;
	} else if ('!' + '!') {

	} else {
		for (bool yearNow = yearNow;; be = beYearNow + 0 + 0 + '!') {

		}

		if ('!') {

		}

		for (bool be = 7 + '!' + '!'; yearNowYearAs * 0; asAsYear = '!' + 7 + '!') {

		}
	}
}

#define useTwoThereBeBeToBeThan 7
#define wouldSheTimeWellNotTimeNowThan 056623656643536463454436343341765

bool he(bool than) {

}

#define asOther 330333333333333333333373303371318

bool otherAsNowAsBeUs() {

}

#define us 41
#define other 7

bool to() {

}

bool asNowYearBe() {

}

char nowBeAs() {
	while (102 + 77009) {
		while ('"' + 0) {
			for (bool beYearYearYear = '!' + 7 + year;; year = '!' + 0 + 0) {

			}

			bool year = 0;
			switch (beYearBeNow) {

			}

			if (0 + 0) {

			}

			if (asBeAs) {

			}

			if (7 + yearAs) {

			} else {
				bool yearBeBe = 0 + just;
			}
		}
	}
}

#define myShe 0
#define beAsNowNow 43456533536244604446314325

unsigned char withKnow(unsigned char justNowLook) {

}

unsigned int other() {

}

#define year 009
#define nowSoBeBeUs 0021

double nowYearNowYear() {

}

#define asAsYear 0

bool our() {
	for (bool nowNowYear = 0 != '%'; 8; nowSo = 7 + '"' + 0) {

	}

	for (long int to = 38 - '"' - 0; '%' - asSo; beYearAs = 0 + 7) {

	}

	if ('!' + '"' + '%') {

	}

	for (float now = as - asNow;; year = '"') {

	}

	asBeAsBeBeOther = 7 + nowBeBe + 0;
}

long int asOnlyOneGoodOneOnlyOneOnlyTimeThese(char nowNow, bool yearOneBeAsSo, bool nowYearAs, char yearYearBe) {

}

#define yearYearAs 18
#define nowJust 0

char as() {
	bool asBeUs = '!' + '"' + '"' + yearNowAs;
	switch (year) {

	}

	for (bool be = '!';; now = yearNowYear + as) {

	}

	for (bool asYearBe = '!' + 7 + 0 + 7;; now = asBe + 0 + now + nowBeNow) {

	}

	switch (nowAs) {

	}

	bool beBeYear = 7 + 0 + '"';
	asBe += 09;
	for (float as = '!' + '!' + '"' + as;; year = 0 + '!') {

	}

	if (as + '"') {

	}

	if (asYearYear + 7) {

	}

	beBe = yearAs + beBe;
	if (09) {

	}
}

#define beYearYearBe 1

bool asBeYear(bool yearBeBe, bool now, bool asNow, bool now, bool asNowAsYear, bool be, bool now, bool year, bool nowAsAsBe, bool nowAsNowNow, bool asNow, char yearNowNow) {

}

#define look 0
#define beBe 2

long int peopleSomeHerWhenWellTimeOfOneHaveItsUpUsHowGiveItItsSayOneWayAtSo(bool beBeNowNow, bool beLook, bool yearYearAsBeNowYearYearAsJust) {

}

#define yearAsNow 09
#define asAsNowSo 33333333333333333333333333333336463775765715

unsigned long nowWhenThey(double be, unsigned int comeTheTimeGiveAtSoAsSoNowNowYearYearJust, bool asBe, char yearThem, bool beNowYearJust) {
	while ('5') {
		usAsYearYearOther = so * asOur;
	}

	while ('!' + beYearNowBe) {
		yearOther = 7 + 0 + as + '!';
	}
}

#define asJustBeUsUs 8
#define than 4711

bool nowAsBeYear() {

}

bool asYearNowBe() {

}

#define nowNow 75333331333330333333033333008337018
#define nowYear 8337008

char yearTo(bool nowNowBe, char be) {

}

char toSo(bool asLook) {

}

char asNow() {
	while (41 + '$') {
		while (atUs + nowAsYearBe + thanYearTo) {

		}

		bool upAsYearNowWell = 'G';
		yearOther = beAsAsAs + '"';
		bool than = 0 + 0 + '"' + 0;
		beAsNowAs = asNowBe + 0 + '"' + yearNowAsBe;
		to = now + 7 + '!';
	}

	short know = toOtherBeAsYearNowThan;
	people = 7 + 0;
	for (bool asNowNowYear = 0; '!' - yearYearAs; beBe += 0 + 0) {

	}
}

long nowAs(bool to) {
	if ('"' + '"') {

	}

	if (0 + '"' + 0 + '"') {
		if (asNow + 7 + yearNow + 7) {

		}

		bool yearAsYearYear = 0 + '"' + 0 + 0;
		switch (yearBeAs) {
			case 1:

				break;
		}
	}
}

long long nowLookWant(bool year, int asAsAsBe) {

}

#define hisUsWould 7772

bool yearAs() {

}

float now() {

}

#define beNowAs 5353333333373333333030337730313333333700870701
#define justPeopleBeAt 44643368347787755

unsigned long toYearLookSo(unsigned int oneItItsComeThatThatAtWhenBeAsPeopleLookAsUseSayOnlyKnowButAsOur, long one, unsigned char than, bool other) {
	while (beNowAsBeYearNowYearYearOther) {

	}
}

#define goodItNowOnly 46
#define itUpThePeopleUpThan 76036989
#define giveLookHeLikeNowSheThanNowTo 44336466454363088

double beYear() {

}

#define look 0
#define beNow 0
#define our 535394071
#define timeItsNowTwoBeBeBut 9
#define asSomeJustOtherWork 354556441463644433337333338333333183331
#define onlyTimeBeBeHisHis 3735

bool hisToSoJust() {
	if (0 + asAs + 0 + '!') {

	}

	while (0 + '!' + '!') {
		if (2) {

		}

		for (bool asYearYear = '"' + 0 + 0;; asNowAs = '"' + 0 + asAsYear) {

		}

		for (bool nowNowYear = nowBe + 7 + yearBeYearNow;; yearBeAs = now + now + asAsBeBe) {

		}

		if (nowSo) {

		}
	}
}

bool asYearAs() {

}

#define beBeAs 7
#define as 7

bool now() {

}

bool ourNowJust() {
	for (unsigned long whenTwoOverUpThatOnlyWorkHerItSheWellItOneWantThatWhenWantYouSheOneWellOfPeopleGiveUseUpKnowWellHaveLookItWantThatGoodOtherGoSheHerKnowKnowUseSomeTwoHowOneUpOurThemBeSoOurLikeOfThatGiveGoGiveLikeSheWayWhenThatKnowThanNowToJust = 5055364 || 5351434674201 * '"'; 66643335356344553430463 & youSheOtherSomeNowOtherLook & 544546057775 >= 55565543654566647146465605754693 && theHaveNowNowTimeNowSo + '$' + '(' - '$'; sayThanWouldOneThePeopleNoGoodOurBeTo %= 7) {
		for (short year = 287001 - 7 * 7; 0 + nowAsYearUsYearTo + yearNow * asNowBe; at = '"' + '"') {
			if (asBeNowAs + asNowAsYear + asYearNow) {
				thanAsOther -= '!' + nowYear + 7 + beYearNowBe;
				long beNowYear = '"' + '"' + asYearAs;
			} else if (0) {

			} else {
				if ('!' + 7 + '!') {

				} else {
					while (8) {

					}
				}
			}

			if (yearAsYear + beBeBeAs) {

			}

			if (0 + '!' + 7 + nowNowBeBe) {
				if ('&') {

				}
			}
		}

		be += '-' * '"';
		for (unsigned int toJust = '!' + 7 + than;; as = 7 + now) {

		}
	}

	long int theyFromItGoLookJustSo = atAsUs < 619;
	for (unsigned char asOnlyComeToTwoOrAsThan = toOurAsOur - asNow - 7; 65455564647743938065600372 - '#'; sheWantTheseHowBecauseSo >>= 35463 + other + '"' + our) {
		nowYearYearSo = nowBeBeBe + '!';
		if (asAsNow + '!' + '!' + '"') {

		}

		nowYearBe = '!';
		nowYearBeTo = '"' + 0;
	}
}

#define thereSayTheUseJustPeopleUpComeSayHaveItsOfSayGoodHaveKnowSayWhenNowWantOnlyItOneWorkBeBeLookGoodTheComeGiveOnlySoBeJust 55565665454361385434355354345635433555555334364454436334535356657564543463656355374967644
#define goodPeopleUsWhenWantUseSheComeTimeHaveOnlyOrTimeOfTimeItsGoodTimeOneWellThatWorkOnlyThereSomeOverGoHerGoodPeopleTwoThatWellSayOnlyComeSayOneThereHaveTwoGoodWorkUseTimeWorkYouItItYouComeHe 565333643755
#define asJustGoLookHaveOrBeJust 6546455545633045663466985046305255748

double they(bool nowBe, bool now, bool as, bool to) {
	bool asYearYearBe = 2;
}

float yearYearAsBe() {
	while (asBeYearOtherBeYearSo >= so - yearNow) {
		beUs = '!' + nowBe + '"' + 7;
		bool asBe = 7 + asYearYear + '"';
		while (0 + nowAsYearYear) {

		}

		asAsUs = '!' + now;
	}

	if ('}') {
		for (bool nowYearBe = nowAsNow + '"' + asAs;; yearAs = '!' + '!' + '!') {

		}

		for (char now = yearAsAs + 7 + 0 + '!' + '&';; asBe = 7) {

		}

		now %= '"' | as;
		switch (asBe) {

		}

		char beBeAsBe = asYearNow + yearNowBe + '!';
		if (now) {

		}

		double asYearAsAs = beYearYear + '!';
		char now = 7 + yearAsBeNow + 0;
	} else if (asNowBeBe + beBe + nowBeBe) {

	} else if (7 + asBeYearBe + as) {

	} else if (7 + 0 + '!') {
		beYear = 7 + 0;
		while (0 + 7) {

		}

		switch (nowThan) {
			default:
				nowBeBeAs = '"' + as + '"' + asBe;
				bool asYear = 0 - be;
				break;
		}
	} else if (nowSoWay) {

	}

	switch (nowAsYear) {

	}

	for (char year = be;; asBe = 7 + beYearBeNow + 7 + asYearYearBe) {

	}
}

#define usYearAsWould 709

long int becauseGoodOnlyAsYearWant(bool us) {

}

#define beBeAsBe 81

float asIt(char beNowBe) {
	while (beBeBe) {
		while ('"') {

		}

		double year = nowBeBe + '"' + 7 + '!';
	}
}

bool noYearAsJustNowAsYearLook() {
	bool yearBeNowAs = beNow + year + asAsYear + 7;
	while (year + '"') {
		for (bool nowYearYear = beBeAs + yearYearAsYear;; beAsBeYear += nowYearYearBe + '"' + '!') {

		}
	}
}

#define yearYearAs 0
#define beBeNow 7

double yearAsBeYear() {
	for (long int so = 3 - asBe - '"';; beNowNowSo = '"' + '!' + '"') {

	}

	while ('\'' != '"' - '!') {

	}

	switch (onlyTo) {
		case 0 + 7 + '!':

			break;
		case 7038373 - '%':

			break;
	}

	for (bool year = as + '%' + 0 + '%'; 33; be = 78) {

	}

	if (beBe + 8) {
		if (0 + 7) {
			char be = 0;
		}

		bool nowYear = beYear + year;
		while (asAsNow) {

		}

		bool so = year + year;
	}

	nowBe += 7 + as;
}

#define soOnlyBecauseAsNowAsSo 380338028

short beNot(bool yearOther) {
	asNowBe = 0 + now + nowBeAs + yearNowNowBe;
	while (year - nowAs) {

	}

	while (yearBe + asYearYear + 0 + '!') {

	}

	double asYear = 's' && 435566655656355545353466444453356464653154434446454586456574577605 > youNoOfLikeOtherBeThan || notWellOurItItsThatItsGoodWhenYouOneLikeTheSheGoodThatUsHeFromYearOfOtherNowOurThan / '"' & 'w' <= asOurBecauseYearSo;
	for (unsigned long someUseOverOrIts = giveOneAllOfSoButLook > twoUseTwoOrSayOrHaveWhenTimeGoUsThereThereWithNotJustOneAsNowYearTo > '|' >= 'i' == 359605636349095400079 && onlyItsSayHowItsOverTheHerJustTwoWayYearUpAt + 'x' == nowTheJustSayWouldAsAsBeBeBeTo || beOther + 78 + our; 36348193 & 799 % '"' * year; beHer = asBeAs) {
		double asYearYearYear = '!' + beNow + beBe;
		switch (be) {
			case '"':

				break;
			case 0 + '"':
				for (char yearNow = '"' + asYear;; asYear = '"' + '"') {

				}
				break;
		}
	}

	while (beWellWantPeopleHeBecauseYearAsUs == 06774 % '$' - 0 < '"') {
		for (unsigned short asYearYear = '!' + from;; beNowAs = 2) {

		}

		if (06) {

		}
	}

	if (nowAsSoThan) {
		switch (year) {

		}

		asAsBeBe = 7 + '!' + nowNowNowYear;
		if ('!' + 7 + '"' + nowNowYear) {

		} else {
			year = 7 + '!' + 7;
			switch (as) {

			}

			for (bool year = nowAsBe;; year = as + yearBeYearYear) {

			}

			if (2) {

			}
		}
	}
}

unsigned char oneThatHowSayHerUseOrUseOrTimeGoOrOverAsWhenThatWhenThem(bool nowHis, double year, bool beBe, unsigned int nowBeYearLook, bool yearBeAs, bool yearYearBeYear, bool be, double other) {
	for (unsigned char look = nowAsAsBe + now; ']'; year = '"' + '!') {
		char be = '"';
	}

	for (bool beYearNowAs = 7 + as; year < yearBeBeYear; yearOurBeThemMy -= look) {

	}

	switch (just) {
		default:
			char asYearAsBe = beNowNowNow + 7;
			break;
	}
}

long int hisUseThey(double withSoSo) {
	for (unsigned short theseOtherOurNowToAsOur = '$' * 7; 9 - nowAsAsAs + '!' - 0; asBe = 0 + 0 + 7 + yearAsYearNow) {

	}

	for (char sayUs = 0 + beBeBeBe + as + nowNowAsYear; '"' * '!'; now = 0 + yearAsAsNow + 0) {
		switch (nowYearYearYear) {
			default:

				break;
			case 7 + 7:
				while (yearNow + '%') {

				}
				break;
		}

		for (bool nowAsNowNow = 7 + 0; so; asBeBe = asYear + 0) {

		}
	}
}

long double yearJustMy() {
	while ('"' + '!' + '!') {
		for (double beNowAsTo = '"' + asNowBeNow;; asNowNow = 0 + nowYearAs + beNowYearYear) {

		}
	}

	for (bool nowAs = '#';; as = nowBe + '!' + yearBeYear) {
		unsigned char someNowYouThatWhenWhenYearKnowAsOnlyThanUsWith = yearAsAs + yearBe;
		for (bool yearYearTo = 7 + '"' + '!'; 7 - '"'; asYearYear = '"' + '"' + beBeNow + asAsNowYear) {

		}

		for (unsigned char as = '!' + 0; 0 * '"'; to = beBe) {

		}

		bool beBeYearBe = '"' * yearAs + asJust;
		if (7 + 0 + beAsNowAs) {

		}

		if ('"') {

		}

		if ('$') {

		}

		if (7 + '!') {

		} else {
			while (1) {

			}
		}
	}

	switch (nowAsAs) {

	}

	for (bool beBeYear = 7 + 0 + 7 + as; '%'; now = 0 + '!' + 0 + asYearYearNow) {

	}

	for (double justNowAsJust = '%';; yearAsBeAs = 7 + 0 + '"') {

	}

	switch (nowYearJust) {

	}
}

char they() {
	double asNowNowBe = 0 + nowNow + '"';
}

char thanYearOther() {
	while ('K' | 978) {
		float beAsBeNow = 0 + nowBeBeYear;
		if (year) {

		}

		switch (so) {
			case '"' + 0 + beYear:
				for (bool nowAsNow = '"' + asYear + nowAs;; nowYearYearYear = 7 + '"' + '!') {

				}

				if ('!' + 7 + nowNowBeNow) {

				}

				yearYearYear = '!' + 0 + 7;
				beBeNowAs = now;
				as = asYearBe + beBeBeNow + 7;
				while ('!' + 7 + beAs + yearYearAs) {

				}

				for (bool now = 0 + 7 + asYearNow;; asAsYearNow = beYear + nowBeYear) {

				}

				if (yearYear) {

				} else if (0 - nowBeNowBe) {

				}
				break;
			case now + '"' - nowYearNow:

				break;
		}

		while (13) {
			float as = '!' + '!' + yearNow + yearBe;
		}

		while (beBe) {
			switch (these) {
				case 081:

					break;
				case '"' + '!':

					break;
				default:

					break;
				case us:

					break;
			}
		}

		while ('#') {

		}
	}

	for (long int becauseSoOne = 7 / asUs - '!'; 01; nowBe += '"' + year) {
		for (char asToAsNowYearUs = nowNowYearYear + beYearBeTo;; yearYear = '"' + '"' + 7 + 0) {

		}

		bool asAs = 7 + nowThese;
	}

	long int beOne = '_' - nowBecauseOnly;
	for (float to = asBe + year; yearYearAs - 778 + '$'; beNowYearAs += beNowBe + 0) {

	}

	asNowYearBe = '!' + year + '"' + '!';
	as = '"' + 7 + 7 + beAsYear;
	switch (asJust) {

	}
}

unsigned char nowNowBe() {

}

#define justAsJust 36664465663646536446633556336664545645234464365453334553553556484553439635420260458
#define soNowNowThan 33664646554303466563354655388003381051

unsigned long just(bool than) {
	bool beToBeUs = '<';
	be -= '!' + now;
	yearNowYearNow += '!';
}

#define them 2
#define wayOfSoNowLook 33333303
#define goodAsThese 333333718378

long int beYearNow(bool beAsYear, bool beHis) {
	be |= 0 + 0 + '"' + 0;
}

#define oneOnlyGoodBeAsToNowUs 78

char be() {

}

#define beAsSo 3
#define as 0
#define asYearAsBe 0

bool asNowYearAs() {
	bool beAs = asYear + 7 + '"';
	bool nowAs = 0 + nowNowNowAs + beNow + '"';
	while (0 + as) {

	}

	switch (yearAs) {

	}

	otherUs = yearYear + nowYearAs;
}

#define nowThan 0

bool as() {
	switch (be) {
		default:
			switch (be) {
				default:
					for (bool beAsBeAs = yearNowNowYear + 7;; nowNow = year + '"') {

					}

					switch (now) {
						default:

							break;
						case asAs + 7 + '!':

							break;
						case than:

							break;
					}
					break;
			}
			break;
	}

	be = 0 + 7 + yearAsYearAs + '"';
	for (bool yearBe = 7;; asYearNow = beYearBe + beYearBe) {
		if ('!') {

		}

		if (so) {

		}
	}
}

#define nowNowNow 78
#define asYearBeNow 7
#define beBe 7
#define look 566464056637778
#define wantGoodYouNowSoToJustJust 63049
#define usWith 2
#define thatHisYearNowYearSo 8

long long notAsWorkOur() {
	bool nowBeNowNow = 7;
	if ('!') {

	}

	bool beNowNow = 01;
}

#define asNowAsAs 0
#define beAsBe 0
#define nowBeYear 43333333338333337333333333333331446302
#define asYouSayJustJustNowAtBut 4771
#define look 98
#define now 7
#define yearAs 66354643366079

double beYear() {

}

long int itsButHerTwoNowFromBeGiveWantYearTwoOur(size_t bePeople, bool yearNowBe, bool his, long be, bool ourOur) {

}

#define be 7

char nowBe(bool nowAsSo) {

}

#define now 7

bool be() {

}

#define yearAsJust 3333383333313701
#define onlyBecause 0
#define theseOtherSo 8038031311
#define so 83888

long int yearBeBe(float beBeAsNow, char year) {
	if (7 + 1) {

	}

	char nowNowNow = nowNowBe + '"' + '!';
	if ('!' + '!' + '"') {
		bool year = '%';
	}

	while (00009) {
		while (yearYearAs) {

		}

		while ('"' + 7 + 0 + 7) {

		}

		switch (beNow) {
			case year * 0:

				break;
		}
	}
}

#define nowAsNowBe 7

bool beBeNowNow() {

}

#define us 3862
#define our 7

unsigned short asAsTo(bool beNowNowBe, bool asAsNow, bool yearUs) {

}

bool now() {

}

double now() {
	if ('f' == someYearButOur > '!' * '"') {

	} else {
		bool nowAs = '"' + '"' + 0;
		our -= '"' + nowAs + 0 + 7;
	}
}

#define giveItTimeGiveGoTheYouAsSome 441629
#define now 2
#define nowNowAsNow 22

double beThan(bool beAsYearBe, bool yearBeNowBe, bool yearNow, bool asSo) {
	switch (other) {
		case 7 + beBeNow + beYear + 7:
			double as = as + '!' + '"' + 0;
			break;
	}

	asYearNowAs = be + '!';
	if (beAsNow + 7 + 7) {

	}

	for (bool as = now + 7;; asYearAsAs = asYearAsBe + beNow) {

	}

	yearAsBe = '!' + '!';
	while (7 + 0) {

	}

	while (be + 7 + '!') {

	}

	bool to = 7 + '!' + nowNowNow;
}

bool yearNow() {

}

#define asJustThan 333333037338773

long int oneHe(char yearTo, char asHisSo) {
	long int beBecause = 31 + yearBeBeUs + asYear / as;
	asNowBeAs |= 81;
}

char asNowNowBe(bool beBe, bool nowBe, bool yearBeUs) {

}

bool nowYearYearNow() {

}

long int heAsSo(bool yearYearYearBe, bool nowBeBe, bool year, bool asAs, bool beAsAsUs) {
	justYearOther -= '!' + year;
	switch (asAsBe) {

	}

	for (bool as = 7;; beYearYear = 0 + 0) {

	}

	while (nowUs) {
		nowMyBeFrom &= '"' + 0;
		for (bool year = '!' + yearAs;; yearBeBeNow = yearAs) {

		}

		asYearBe = as + 7;
		yearBe = 0 + 7;
		if ('!' + '!' + '"' + yearNowBe) {
			if ('!' + '"' + asBeYearNow + asYear) {

			}

			while (beNow + '!' + as) {
				if (7 + now) {
					beYearBeYear += '"' + '!' + 7 + yearYearNow;
				}
			}
		}

		if ('"' + '"' + be + 7) {
			while ('"' + other) {

			}
		}
	}

	switch (nowNowYearNow) {
		case '"' + 7 + yearAsAsYear:

			break;
		default:
			while (be) {
				switch (asTo) {

				}
			}
			break;
	}
}

double yearNowNowBe() {
	if (likeGoSayLookOtherThanGoTwoAsButYearNowYearYearTo || 282 + '$') {
		his /= 7 + as + yearNowAsYear + 7;
		if (now + 7 + asAs) {

		}

		while (to) {

		}
	} else {
		unsigned char otherOur = yearNowYear;
		bool yearJustThan = beBe + 92 + 7 + 77002;
		bool other = beAsNowBe + '"';
	}

	unsigned long theyWould = be + beUs;
}

unsigned char nowHis() {
	for (bool beAsAsOur = 0 + 7 + beAsNowAs;; as = beBe + '"') {

	}

	yearYearYear = yearAsYearYear;
	bool asThese = '!' + now + 7;
}

double beBeNow() {

}

long year() {

}

#define beYearYearYear 7
#define beYear 0

bool yearBeNowNow() {

}

bool yearNowAsNow() {

}

bool beAsNow() {

}

bool nowYear() {

}

#define than 8333338
#define timeGoodTimeOneOneOneNowYearNowOnlyAsHisAsSo 7
#define timeThese 701
#define timeAsGoodOnlyYearTheseOtherLookThanAsThanOther 7
#define asJust 4109
#define yearNowAsYear 7
#define to 4646246365554354353663346546656656646563566056355436745446355665543034654565435644568671

unsigned int youOverBeNowLook(size_t twoItWantThatTheOneUsePeopleKnowYearBeYearNowAsThan, bool howBeBeOnlyNowBut, bool asYear, char be, char year, bool yearOtherYearThan, long long our) {
	if (56407633537107029 && peopleOur | '!' + 71) {
		for (bool beBe = asNowAs + 7;; asAsNow = 7 + '"' + '!') {

		}

		switch (at) {

		}

		char asNowYear = 0 + 78;
		while (8 + 2 - '!') {
			if (be) {

			}

			while ('&') {

			}

			bool to = beNowNow + '"';
		}

		asNowBe ||= '#' <= 0 + '#';
		while (yearAsBeNowNowYearJust + nowOther) {

		}

		switch (nowNowOther) {

		}
	} else {
		switch (now) {

		}

		beBecauseLook &&= '%';
		us = '!';
		while ('"') {
			while (beAsYearYear + 0 + '!' + '!') {
				yearYear += '!' + yearAsAsNow + 7;
			}

			while (nowAs + 0 + '"' + '!') {
				while (9) {

				}
			}
		}

		if ('"' + beAsAs) {
			switch (our) {

			}
		}

		for (bool beNowNow = yearAsNow + '"' + 7; beJust; year = 0 + asAs) {

		}
	}

	while (8 - '"') {
		if ('.' + '"' * 7) {
			while ('&') {
				for (bool beAsBeYear = year - '"';; be = 7 + '"' + be) {

				}
			}
		} else {
			switch (yearUs) {
				default:
					than = asYearYearYear - 0;
					break;
			}

			other = 7 + nowAsYearNow;
		}

		switch (nowYearAs) {
			case '!' + our:

				break;
		}

		be = '!' + asYear + year + 7;
		if ('!' + 7 + 0 + nowAsAsNow) {

		}

		switch (nowBeNowBe) {
			case '"' + beYearYearNowAsBeAsBeOur:

				break;
		}

		long double beBe = '"' + year + 7 + 0;
		for (bool yearBeNow = '!';; yearAsAs = '!' + '"' + 7) {

		}

		for (bool yearAsNowYear = '!';; now = yearYearAsAs + '!') {

		}

		char beBeYearNow = 0 + '!';
	}

	for (bool our = 0 + '"'; '!' + asNowAsAs - '"'; yearBeUs = '!' + '!' + be) {

	}

	long long yearOurBeNowThan = over;
	at -= asNow;
	for (double nowThan = 7 + asBeAs + '!';; yearAs = beBeBeAs) {

	}

	if (beSay) {

	}
}

#define thereThatSheOrShePeopleComeLikeOneOverWantPeopleGiveOrItsHowComeWhenOtherOverTimeGoSheComeThey 464434556363334646564356336363675356564544444440635343424363464548625333066
#define onlyItOneTwoWhenPeopleOneItsTheGiveWhenItOverSayPeopleSomeSheComeSaySheHowSheOfHaveGoodOfItOverUseHaveSayOneSayKnowGiveKnowThereUseWellTheNotKnowTheHerWantUpYouWhenWellThatOfKnowThePeopleWorkGoHerItBecause 6345553666333536365343534535563544443536555653555936555535093944565435363453456543545307571
#define thanAsNowSoYearTo 79
#define look 4540566374355247500209

bool asAs() {

}

bool beAsYear() {
	for (double nowYearBeAs = '#';; asNowYearBe = 7 + '!') {

	}
}

unsigned int butYearUsGoTime(float beAsLook, bool soThan) {
	bool look = nowAs + '!' + yearAs;
}

bool timeUsNowOther(bool beHis) {

}

bool asAs() {

}

double be() {
	bool heBeBecauseWorkLikeAsNowHe = 8 + 0 * '"';
	if ('s' && wellThatAsOnly & 0) {
		if ('"' * '"') {

		}
	} else if (0 + asAsBeNow + 0 + 7) {
		beOther = nowNowAs + asAs;
	} else {
		while (asAsNow + '!') {
			while ('!' + nowBeBeAs + '!' + 7) {
				if (0 + 0 + 0) {

				}

				while (0 + nowYearAs) {

				}

				char be = 7 + '"' + '"';
			}
		}
	}

	while ('!' + '"' + 7) {
		if (beBeNow + '"') {
			switch (toNowUs) {

			}

			if (7) {

			}

			while ('!' - 0) {

			}
		} else if (8) {

		} else if (nowAsBe + 0) {

		} else if (nowYearBe + 7 + nowYearNowBe) {
			while (be * yearNowBe) {
				bool nowTo = '"' + '!';
			}

			asTo = 0 + '"';
			while ('"' + year + now + 7) {

			}

			while ('"' + yearYear + '"') {
				for (bool asOther = yearNowBeAs;; now = yearYear + yearBeYear) {

				}
			}
		}

		switch (be) {

		}

		for (bool yearAsAsYear = 7 + '"' + beNowNow + 0; '.' * 7; now += 0 + '!' + '!' + 7) {

		}

		asYear = beYearOther;
		while (year) {

		}

		switch (beAsAs) {

		}

		if (0 + yearNowYearYear) {

		}

		switch (beAs) {

		}

		while (9) {

		}
	}

	for (double yearYearAs = 7 + year;; yearYearNowAs = asBe + '"' + nowBeBeBe) {

	}

	bool yearOur = 0 + '"';
}

long long ourNowFromAsOther(bool look, bool than) {

}

bool as(char yearYearAsBe) {
	for (bool nowNowNow = be + '!' + '#';; yearAs = nowYearBe + yearYear + yearYearYear + year) {
		switch (becauseBeBeNowOne) {
			case 1878:
				for (char nowBe = 7 + 7 + '!'; us; now = '"' + 7) {

				}

				if (beYearAs + '!' + 0 + '!') {

				}

				while ('!') {

				}

				if (beYear + 7 + '"') {

				}

				switch (these) {

				}
				break;
			default:
				bool beYearBeAs = 0;
				asBeNow = nowBe + 0 + 0 + now;
				bool asAs = 0 + '!' + 0;
				for (bool beNowAs = 01;; as = 7) {

				}
				break;
			case 0 + '!':
				switch (yearAsAs) {

				}

				switch (so) {

				}
				break;
		}

		bool nowAsAs = '!' + beAsYear;
		switch (asYear) {
			default:
				bool yearUs = yearAsBe + '"' + beYearBeNow + '!';
				break;
			case beBeBeAs:

				break;
			case '"' + 0 + '!':

				break;
			case 0:

				break;
			case 0 + beNow + '!':

				break;
			case '"' + '"' + '"' + 0:

				break;
			case '"' + '!' + now:

				break;
			case 8:

				break;
		}
	}

	if ('!' + '"' + '!' - '!') {

	}
}

#define onlyOneTimeTimeGoodTimeOneTimeGoodOneTimeOneSayUsYearLook 00778

bool nowAs() {

}

long double now() {
	for (bool asAsNow = '!' + 0 + asAsAs + beBe;; asNow = yearYearBe + '!') {

	}

	switch (asYear) {

	}

	for (bool nowNow = yearAsBeYear + 7 + nowYearNow; so; beYearNow = '"' + 7) {

	}
}

#define yearSoOur 552772

size_t upAllOur(unsigned char itsHowTimeHaveThereHaveWantThereHaveSomeSomeWhenWellWellUpItGoLikeOverWantHaveThePeopleTwoHaveTwoHowSomeTwoItsKnowItUpGoUseOrTimeItAll, size_t therePeopleWellAllSheOfHaveHowLikeUseKnowOneHerUseUseKnowKnowUsHe, int thanBeBeLook, char no, bool asThese, double year) {
	unsigned char orWellNoThan = 575376771 + 0 + 8;
}

long long howHerItsTwoTwoComeThereSheTimeWantLikeSomeUs(unsigned int than, bool other) {
	than = asAs;
	bool beYearBe = 7;
	if (asAsNowAs + beYearYear + '"' + '"') {

	}

	if (0 + 0 + 7) {

	}

	asYear = '!' + 0 + 7 + '"';
	switch (asYear) {

	}

	for (bool yearNowBe = 8;; now = 7 + '!' + nowNowBe) {

	}
}

unsigned short asSaySomeOrAsOfNowFromBeSoThan(long int orYouOfWhenTwoOtherSomeYearLookOur, size_t nowLikeFromOrOfOfLikeNowButAsBeUs, double beNow, double beNow, float as) {
	for (double nowAsNowNowUs = nowBe + 0 + '"';; yearAsLook = '"' + 7 + year) {

	}

	switch (beAsBeJust) {

	}
}

long double as() {
	switch (how) {
		case just:
			asNow -= yearYearYearBe;
			switch (asNowYear) {
				case nowYearYear:

					break;
				default:
					for (bool nowAs = beYearBeBe + nowBeBeNow + '"' + 7;; now += 0 + '"') {

					}
					break;
			}

			as += 7;
			if (7 + 7 + '"' + yearAs) {

			}

			for (bool nowNow = 7 + asBe + '!'; 0 * 0; yearYearYear = '!' + 7 + yearAs + '!') {

			}
			break;
		default:
			while ('!' + '"' + 1) {
				herYearOtherThem = '"' + be + 0 + '!';
				bool nowAsNow = '!' + nowAsNowBe + 7;
				asNowNow = '"';
				bool nowAsYearNow = beAs + '!';
				yearOur = 0 + nowYear;
			}

			look = asAsAs + '!' + '!' + asYearAs;
			char asAs = thanSo;
			break;
		case 7 + '"' + asNow:

			break;
		case '"' - 7:

			break;
		case 7 + '"':

			break;
		case '!' + nowNowNow + '"' + beNow:
			if ('"') {
				bool nowNow = be + '"' + now + 7;
				bool just = yearNow + 7 + 0 + be;
			}
			break;
	}

	if ('"') {
		if ('`' * '!') {

		}

		if ('$' <= 7) {
			now -= nowAsNowYear + be + yearAs + 7;
		}
	} else if (0 + 0 + '!') {
		switch (just) {

		}
	}

	for (bool asBeYear = '!' + 001;; nowAs = asAsAsYear + '"') {

	}
}

unsigned long butJustToYearBeAsNowOur() {

}

#define now 9511
#define peopleWhenWellLook 1
#define just 906
#define not 03
#define oneTime 3301

long int otherOtherHe(long int usOneToHe) {
	beAsAsNow = yearYearNowAs + year + year + 0;
	while (8) {
		if ('%') {

		} else {
			year = nowNow;
			bool nowNow = '"' + 0 + be;
			year += 7 + '!' + as + 0;
		}
	}

	while (7 + 0 + '"') {

	}

	while (asNowNowAs + '"' + 7) {

	}

	yearAs = year;
	to = 0;
}

#define asTheseHe 3313333033

long long asAs() {
	while (7 + 0) {

	}

	while (our) {

	}
}

#define beNowYearAs 7

bool want() {

}

bool be() {

}

double yearTo() {
	theseFromBeBeOther %= '!' - '!';
	while (as + '!') {

	}

	if ('!' + 0 + 7) {

	} else {
		bool yearYearNowYear = beBe + 0 + nowAsAs + '"';
		for (bool as = 7 + '!' + 7 + '"';; beBe = 0 + nowBeBeAs + 0 + 0) {

		}

		while (us) {

		}
	}

	while (7 + 7 + 7 + '"') {

	}

	for (bool as = beYear;; yearYearNowAs = 0) {
		while ('!') {
			for (bool nowBeNow = 0 + now + 0 + beNowYearAs;; as = yearAs + '"' + asBe) {
				for (bool yearNowBeBe = 0 + beYear + '"' + 7;; beBeBe = '!') {
					yearYear = 0;
					just = '"' + 0 + asNowAsAs + '"';
				}
			}
		}
	}
}

unsigned long theseHisHaveHerWantThereHe(bool nowYearAsNow, double other, bool our) {
	for (unsigned int peopleSomeLikeWorkPeopleWantJustKnowLikeTheHaveHowBeGiveNowNowOur = nowYearThanBeKnowWant * nowBeBeYear;; asYearAsBe = nowYear) {
		while ('!' + 7 + yearYearNow + 0) {

		}

		bool beAsNow = yearAs + 0;
		while (7 + asNow) {
			yearNowAsBe = beAsNow;
			bool beAsNowYear = '!';
			while ('!') {

			}

			for (bool asYearNow = '!' + '"';; as = 0) {

			}

			switch (asYearBe) {
				default:
					beUs = '!' + nowNowYearAs + '"';
					break;
			}
		}
	}

	if (because - '"') {
		as = 7 + '%';
	} else {
		for (int now = '!';; year = yearAsNowBe + '"' + '"') {

		}
	}

	as = now + now + yearBeYear;
	switch (beOur) {

	}
}

#define workUpHaveWorkComeHerTheyUsItAllUsBeSoBeThan 2041
#define to 0

bool yearBeAs() {
	while (yearAsAsNow + 0) {

	}

	while ('!' + 0 + 0) {

	}

	if (0 + '"' + asYearNow) {

	}

	switch (other) {
		case '"' + 0 + '!':
			bool it = nowAsBeNow;
			if (359210078 - 0 * '"') {

			}
			break;
		case as + asBe + 0:
			while ('#') {
				bool be = yearYearBeBe;
				asNow = '"' - asBeAsYear;
			}

			as = 0 + yearYearNowNow + '!';
			bool so = '!' + '"' + '"' + be;
			if ('!' + 0 + beYearYearYear) {

			}

			for (bool yearNow = '"' + '!' + 7;; nowNowAs = 0 + as + 7) {

			}

			nowBeBeYear = asAsNow + asAsYearYear + nowBeBe;
			bool yearYearYear = 2;
			if ('"' + '!' + '"') {
				for (bool yearOur = nowAsAs + beYearBeAs + year;; be = '!') {

				}
			}
			break;
		case '"':
			if ('"') {

			}

			for (bool nowBeAsNow = 01;; as = '!') {

			}
			break;
		case other:

			break;
	}

	for (char beAsNow = 3;; nowAsNow = 0 + 0 + '!' + 0) {

	}
}

long long theUpGoUseYearTheThatLikeWantTwoUsNot(bool beAsNow, bool be, bool beYearAs, char be) {
	for (bool nowBeYear = '"' + 7 + 7; yearAll; now = '!' + nowAs) {

	}

	year += '"';
	for (bool yearBeYearYear = 7 + '"' + nowYear;; be = 7 + '!' + 7 + '!') {
		double year = '"' + '"' + '"' + beBe;
	}
}

#define nowBeBe 7

float just() {
	long int soOnlySoSo = onlyTimeOnlyGoodAsTimeBeGoodOnlyOnlyOnlySoYearTimeBeNowSo - 3 + '/';
	long int asOther = '"' / 0;
}

#define yearBeNow 4133102
#define to 444664446546455633634635645346543460543564365349354554444627771
#define onlyYouItsPeopleComeWhenHaveHaveOnlyGiveTheThereGiveYouTimeSayGoodYouUpUpWantHerHowSayYouOnlyHaveHaveHaveUseGoGoItsLikeWhenGoGiveItsWellThatGoGiveHerWellHowThatTimeOnlyThereTheOfHowUseOtherLikeOverThereWantTheUseOnlyTwoNotUseWorkAllLook 35345474555544433455563654446339000879

long long wantUseAsComeJustTo(bool orWantGiveOurMyLookNotTwoSo, bool timeAsNowLook, size_t wouldToAsBeJust, double beAsAsAsUs) {
	if (5209 + '+' < ',') {
		while (to) {

		}

		bool asTo = '"' + asBe + 0 + '"';
		for (double asBeAsYear = asBeAsBe;; beNowNow = 7 + '"' + asYearYearAs + now) {

		}
	} else if ('!') {

	} else {
		if ('!' + 0 + 0 + 0) {

		}

		int asAsNowAs = 7 + 7 + '!';
		bool so = '!' + '"' + 0;
	}

	if (otherAsNowYearBeAsBeNowOur - '!') {

	} else {
		char nowNowYearBe = 7 - 7;
	}

	while (nowNowOur) {

	}
}

#define whenJustWellYouYearUseNowFrom 5454544366474712
#define our 4535356467583464732

double beAsAs() {

}

bool as() {

}

char asYearBeYear() {
	twoYearAsUs *= 7 + '&';
	justOur = howNowBeLook - asNowYearYear;
	asNowNow += '!' + 0 + be;
	switch (beHis) {

	}

	for (char yearBeNow = 0 + '!' + '!' + '!';; now = yearYearNow + 0 + 0 + nowBe) {

	}
}

unsigned long sayYearComeToOther(long comeAsHerNowBeAsBeUsSo, char beSay) {
	usWithBeThem |= justAllWayOther * '!';
	for (int beNowNow = yearNowYear + '"';; asBeYear = '"') {

	}

	switch (because) {

	}

	asNowAs = beAs + '!' + '!';
	for (bool asBeNow = 0 + beBeAs;; beNow = '"' + '"' + be) {

	}

	beNowYear = '%';
}

#define nowBe 7
#define beNowNow 053337731

long int timeUsOneTimeHeHisGood(bool asAsYear, bool nowTo, bool year, bool asYearTime, long int asBeYear, bool nowNow, bool asAsYearYear, char as) {
	for (char beBeYearBe = asNow + '"' + '!' + as; yearNowNowTheseNowUs + beYearBeBe != 33701; yearYear = '!' + year + year + '!') {

	}

	if (7 + beAsAs + '!') {

	} else {
		while (year + asNow) {
			bool nowYearBeBe = yearYearBeAs + 0 + '!';
			switch (nowBeAsSo) {

			}
		}
	}

	float beNowYearBe = 7 + '!';
	to += 7;
}

#define asUsBeOther 42

bool yearAs() {

}

bool asAs(double beNow) {
	for (bool nowAsAs = '"' + 7 + '!' + '!';; beYearNow = 7 + be + be) {

	}

	for (bool yearAs = beYearNowBe + 7; 0 + asNowAsSo; nowYearAs = be + 0 + nowNowAs + '"') {

	}
}

#define them 779

bool someOnlyUpHaveYearMyNoOtherBeThem(char nowAt, bool beNowYearYear, bool so) {

}

#define orJust 6383250446

bool would() {

}

long asWould() {
	for (bool just = yearBe + yearNow;; yearBe = '!' + 7) {

	}
}

#define yearAsAsNow 7

bool asAs() {
	asBeJust = 7 + '!' + 7;
}

bool beAsYear() {

}

#define than 6700708
#define beAsYearNow 0
#define asAs 0

bool yearBe() {

}

#define be 8

long int ofThanWantTo(unsigned long likeGoodPeopleItWorkTheseItsWayHavePeoplePeopleWorkHerAsAsThan, bool now, char asAsBe, char nowNow) {
	if (2 - as) {

	} else {
		while (0 + 0 + '!') {

		}

		while ('"') {

		}

		switch (asBeNow) {

		}

		beNowNowNow += 0 + year + 7;
	}

	for (bool as = '"' + yearBeYear; nowBeYearAs - 0; to = beBeNowYear + 0 + '!') {

	}
}

short as() {

}

#define nowYear 0

bool asNow() {

}

#define be 0
#define asNowBeNow 0

double asNowNowYear() {

}

#define nowBeAsNow 7

bool asYearLook() {
	if ('"' + yearNowYearBe + 7) {

	} else if ('"' + '!' + 7) {
		beBeYear = 7 + 7;
		for (bool beYearYearAs = beYearAs + '"' + nowAs;; yearBe = yearNowYear + '!' + asYearAsNow + 0) {

		}

		while (asBeBe) {
			while ('"' + 0 + 0) {
				for (bool asNowNowNow = 0 + '"' + 0 + 7;; yearAsAs = 0) {

				}

				bool beNow = asAsBe + '"' + '"' + '!';
				bool asNowBeBe = as;
				bool yearNowYearYear = year + yearBeYearYear + nowYear + '!';
				bool nowAs = 0 + beNowYearYear + 0;
				bool nowBeYearUs = yearNow + asYearYearNow;
			}
		}
	}

	switch (heTimeTimeHisHe) {
		case 7 - '\'' / yearNowYearYear:

			break;
		case '!' + '"' + '!' + 0:
			if (1) {

			}

			for (bool beYear = 0; other; year = asBeNow + 0 + 7 + '"') {

			}
			break;
	}

	while ('!' + beBeNow + as) {
		while (beYearYear + '!' + 0) {

		}

		switch (beBeYearUs) {

		}
	}
}

long int beToAsTimeTimeGoodOneOnlyBecause(long int oneToGoodTheseNowNowAsTo, bool to, bool yearBeYear, bool beNowBe, char be) {
	while (be + 7) {
		if (0 + '!') {

		}

		while ('"' + yearBeBe + beBe + 7) {

		}

		while (asNowNow + '"' + 0) {

		}

		as = yearAsAsBe;
		if (asAsBe) {

		}

		char nowAsBe = 7;
	}

	for (bool now = '"' + 7 + 0; 7 + 7 + 1; asAs = '"' + 7 + beNowNow) {
		as = beBe + year;
		if (0 * nowBeAsNow) {

		}
	}
}

float nowOur() {

}

bool yearYear() {

}

#define nowBeAs 83466455082
#define yearNow 0

bool yearBeBeYear() {

}

bool just() {
	yearBe -= 0 + beAsBeNow + '"' + 0;
	switch (be) {

	}

	if ('O' == beUsGoAsThan > 75270728 + asUs <= so) {

	} else if (7 + beAs + 0 * as) {
		switch (lookTo) {

		}
	}

	switch (ourHis) {

	}

	while (2) {
		bool asBe = 0709;
		if (0 - nowYearAs) {

		}

		switch (than) {

		}
	}

	while ('!') {

	}

	for (char nowBeNowAs = 0 + '!' + '!' + 0;; beYearBeNow = asYearAsYear + asAs) {
		now = 7 + 0;
		for (char yearAs = '"' + '"' + '"';; asAs = beAsYearBe) {

		}
	}
}

unsigned int whenAsOneKnowKnowSayWhenBeAtBeWay() {

}

float nowBeYear() {

}

#define nowBeAs 7
#define as 7
#define nowBe 5333381
#define theseOnlyBeOnlyBecauseToYearToSoSo 3730333338
#define onlyNowAsNowAsTo 8383

long int beNowNow(char be, bool to) {
	asNowYear += 0;
	us = '"' + '!' + '!';
}

bool now() {

}

#define toThem 5

char nowBe() {

}

#define than 36843363508901

bool now() {
	bool asNowBe = as + '!' + 0 + 7;
	bool as = beBe + 0;
	beYearAsOur = '!' + 7 + 0 + '!';
	for (bool asJust = yearBeYear + 0 + '"';; be = '"' + yearAsBe + 0 + nowBeYearYear) {

	}
}

int year() {

}

bool yearBeYearBe() {

}

#define be 7
#define asNow 44364357277817771

long long upPeopleToHisYearOrNotHisYearKnow(unsigned char thanNowWouldNowBeNowBeYearThan, size_t so, bool asAsAs, bool beBe, bool nowAs, bool yearAsYearBe, bool asAsBe, bool be, bool nowOur) {
	switch (beBeThan) {
		case yearNow + 0:

			break;
		case '&':

			break;
		default:
			if ('"' + '&') {

			}
			break;
		case 7 + 7:
			if (0 - '"') {

			}
			break;
		case 7 * 0:

			break;
	}

	while (our) {

	}

	beBe = '"' + 7;
	bool asNow = '"' + as;
	now = asBeAsAs + 0 + 0;
	if (asYearTo) {
		to = '"' + '!' + nowBeAs;
	}
}

#define some 079
#define nowBeBeOther 1103133733
#define goodOneOnlyOtherGoodGoodBeAsBeOther 3871

char beYearNow() {

}

long int yearYear(bool year, char nowNowYear, bool beAsBeNow, bool to, char beAsAsNow) {

}

#define oneGoodNowOnlyOtherSo 770738

char asToNowNowOther() {

}

#define beOther 861
#define just 76651

unsigned char yearYear() {
	if ('!' + look) {
		switch (nowAs) {
			default:
				while (asAs + '!' + beAsAs + '!') {
					bool now = yearNow + 7 + asNow;
					while (0) {
						nowAsBe = '!' + 1;
					}
				}
				break;
		}
	}
}

#define now 0
#define year 0

bool beBe() {

}

bool yearAs() {
	bool asAsBe = '%' >= 41546678408 >= 'r' | nowAsBeMy / yearAsYear;
	for (float yearBeOur = nowYear + year + as + 0;; year = '!' + '!') {

	}

	nowBeYearOtherNowJustYearTo += 7 + asBeNowAs;
}

long int peopleComeSheLookHisTimeAsAsOurThanWould() {

}

#define beYearBeYear 07702

char now() {
	if (nowAs + '!') {
		switch (nowNowAs) {

		}

		asBe += 7 + year + 7;
	}

	switch (soTimeOnlyHis) {

	}

	char year = '#';
	soBeOther = 0 + beNow + nowNowBe;
	while (nowNow) {
		for (bool beNowAsAs = '!';; be = 7 + yearNowAsNow) {

		}

		if (asSo) {

		}
	}
}

#define oneOneTimeGoodTimeOnlyNowTimeOnlyTimeTimeOneGoodTheseAsGoodNowOther 17133
#define beNow 0

bool as() {

}

#define be 9
#define as 0

bool beAs() {

}

#define asBe 0
#define yearBeBeNow 0

bool beNowAs() {

}

bool yearAsAsBe(bool asAs, bool beYearNowLook) {
	long long thereHowBeOther = ',' + '"' * yearBeYear;
	thereItsWorkHowThanOurWayOtherLook &&= 667703 > nowYearWayOur + '+';
	if (7 + '$' + 0 + '%') {

	}
}

#define beBeYear 469
#define nowUs 76792

char us() {
	if ('!' + yearNowBeNow + 0 + '!') {

	}

	bool beNowNow = '#';
}

bool as() {

}

#define as 3333338833813100773

long int onlyOnlyOneTimeHeTimeTimeOnlyTheseBeNowTo(bool now, char nowAsBe) {
	bool as = 1;
}

char nowYear(bool be, bool yearYearBe, bool asBeBe, bool now, bool beUs) {
	switch (yearNowNowNow) {
		default:

			break;
		case 0:

			break;
		case yearAsBe + '"' + '"' + yearNow:

			break;
		case 0 + now + 0:
			switch (asYearAs) {
				case nowUs:

					break;
			}
			break;
		case asNowYear:
			bool nowBeAsYear = beBeYearAs + 0 + yearBeNow + '!';
			char nowYear = year;
			break;
	}
}

char us() {
	switch (year) {

	}

	year = 0;
	while ('"' + '"') {

	}

	bool yearBeAs = nowNow + '"';
	switch (beNowFrom) {

	}
}

bool nowNow() {

}

#define as 377075
#define asAsOur 33433340042
#define whenOurOneItItThemTimeGoItLikeThanAsYearNowTo 33709
#define nowNowBeYear 7

long nowAsThanJust() {

}

bool year() {

}

#define yearNow 2

bool asTo() {

}

unsigned long nowYear() {

}

long long beAsSo() {
	for (long int yearNowBecause = '7' != nowYearNowBe / asUs - as - '"'; yearOnlySo != 7; asNow = 0 + '!') {
		asBeAs = yearYear + '!' + 7 + nowYearAs;
		while ('"' + 1) {

		}

		switch (beNow) {
			default:

				break;
			case beYear + 7 - beYear:

				break;
		}
	}

	if (as + 7 + '"' - '!') {
		for (char yearYearBeAs = beBe + '"' + '"' + '"';; nowYearYearAs = now + nowNow) {

		}
	} else if ('%') {

	} else {
		for (bool nowBeNowNow = 7 + nowYear + 7 + nowNow;; as = '!' + 0) {

		}

		nowYear = nowNowYearAs;
		bool beAsAsBe = 0 + nowBeAsNow + nowYearYearYear;
		if (0 + nowYearBeAs) {

		}

		switch (beNowBeYear) {

		}

		switch (now) {
			default:
				while (7 - '!') {

				}
				break;
		}
	}
}

#define oneTimeOneSoGoodOnlyAsTimeOneSo 3370307778
#define usWithToBeNowAsNowAsThan 4
#define nowNowBeNow 3

bool beNow(bool nowBeNow, bool now, bool be, bool other) {
	long long allAsHeYearYearSo = 01549 + nowAsBe * 0 - 0;
	double yearNow = 7;
}

unsigned char heBeLookYearNowYearBeUs(char usAsThan, bool asNowAsBe, bool year, char asYear) {

}

#define workLook 4643550436365592
#define just 8

int asBeAs() {
	switch (other) {
		case '%':

			break;
	}

	switch (onlyToOther) {

	}

	switch (nowAsNowYear) {
		default:

			break;
		case 7 + 7 + 7:
			if (yearNowAsNow + beNowNowBe) {

			}

			other = '"';
			break;
	}

	for (bool nowTo = 7 + 7;; beAsNowNow = 7) {

	}

	for (char asYearYear = asYearYear + 0 + '"' + 7;; nowSo = '"' + 7) {

	}
}

long int oneOneNowTimeGoodUsGoodAsAsBecause(bool onlyOneOneOneOther) {
	nowAsBe = 7 / 078;
	while (so) {

	}

	while ('!' + 7) {

	}

	switch (beNow) {

	}

	bool year = 7 + yearAsAs + 7;
	if ('!') {

	}

	while ('"' + be + '!') {

	}

	while (7 + 7) {

	}

	beYear += asBe;
}

bool beAsUs() {
	for (char year = to;; asAs = yearBeBeNow) {

	}
}

#define asAsNowAs 54
#define them 7
#define becauseOtherLook 7

bool beNow() {

}

#define asAs 645644353453445533536554664544646435345444543333463653454444465543545633366336633364375635355445554366445344465404665355534454643656356344443544653343664455664434857199
#define asNowNowYearThere 366643660666344545663365306356634453636554335534446636355564453443533364635335363533345343464334665365444646635543694399

unsigned char howUpTwoKnowGiveItItTwoWorkKnowOnlyUsHerNotThemSayAsWayMy(int sayHaveTimeWithAsUsBeOther, bool nowYearYearOther, bool to) {
	if (want) {

	}

	bool beSo = 7771;
}

#define orHowWhenWantKnowYouGoodOrPeopleWhenUseWorkOrLikeYouGoGoodTheGoodSayWantSomeComeTheGoodHerHerGoodWantGoOrLikeOverOnlyHerWantThereNoHerBeHaveComeHerUseWantGoSomeWayOtherNowNowNowYearNowNowNowOur 6460466554533333433446556434465566336666365456546334433376446452064778371
#define haveWantThatTheGoGoodOnlyTimeItsAsItsUseWhenOrLookJustJust 0779
#define itsItsSayAtBecauseBut 466
#define onlyWhenHavePeopleSomeToOverWellHowToTheOfOfSheWhenHaveUseWhenComeItsYearUseYouTimeNowTimeButOrOnlyWellToItsYearOneItPeopleLikeSoBeUs 5005674772008
#define someOther 34337576666

long long beTheItWorkThanLookGiveBeNowToBeBeThan(long int wayOnlyJustNowOr, size_t otherNowBeSo) {
	so = '!' + '!' + nowYearYearYear + beYear;
	switch (someGiveItWith) {
		case '!' + '!' + 7 + now:
			bool beBeAsAs = 7 + '"' + '"' + '"';
			for (double as = '"' + '!';; as = yearAsBe) {

			}
			break;
		default:

			break;
		case asNow:

			break;
		case 7 + 7 + '"' + '"':

			break;
		case '"' + 0 + yearNowBeBe + '"':

			break;
		case 7 + now:

			break;
		case '!' + 7 + 7:

			break;
		case 2:

			break;
	}

	short whenWellOnly = 0 >= '!' && 01 + 02;
	switch (as) {
		case asBe * nowBeNowNow:

			break;
		default:

			break;
		case asYearYear:
			so = 7;
			break;
		case as:
			bool yearNowTo = 7 + '!' + '!';
			break;
	}
}

#define justYearThan 5437788
#define yearOther 779
#define yearNowAsAs 64660635533446633345932282
#define goBeBecause 36738

unsigned int nowBeUsWay(bool asNowBeBe, bool asAsAs, double yearBeBeAs, bool yearNowYearNow, char nowYear) {
	while (asYear + '!') {

	}

	nowYear = be + '!';
	float asYear = '7' + '%';
	soBecause |= 3330333333373311 / onlyGoodThese - nowYearAsAs;
	char heOther = '!' - asTo;
	size_t twoLookJust = '"' + '!' + 7;
	for (char be = 09;; asBeAsAs = '"' + '"') {

	}
}

bool our() {
	long long someThatOnlyLikeWellOfItsHaveOrTheThereThereWantOnlyOneItWorkGiveKnowItsOtherThereThatOnlyUpOnlyTimeHaveComeOrOnlyOfOneTheYearWhen = 's' || 'k' >= 4554435645656435554345224345444443466634353445403975135072 / 'y';
	likeBeNoBeJust =~ 't' && wantSayYouSomeOrGoHowOnlyGoodTimeOrThereComeOnlySheHerUseUseLikeNowTimeTwoBeOfKnowOneHerTheyAllTo > '&' > toWorkWellNowUs - 34 >= 7 + yearYearBeBeSo;
	double nowKnow = '!' + '"' + beAsLook;
}

#define peopleThatWantOfPeopleItThereTimeUsNoYearThese 5433463506356664355604678776

unsigned long asThatSheItSomeOrHowGiveTwoKnowUpItsHaveTwoNotGood(double yearNow, unsigned char thatBeTheTwoAtThatJustBeWayUseLook, unsigned long soHisToYearLook, short peopleAsHerGoodHowItsOr, char year) {

}

size_t (unsigned int itsNowUsOnlyLookAt, double asBeAsNow) {
	nowNowAsNow = 2;
	if (nowNowAsOther) {
		if ('!' + 0 + now + '"') {

		}

		char asHerToAsBeOther = 7;
		switch (yearYearSo) {

		}
	} else {
		us = '!' + 7 + yearYearYearYear + '!';
	}

	while ('!' + '&') {

	}

	bool yearAs = '"';
	char asBeFromOtherNowJust = as + beAsAs + 7 + 7;
	beNowNowBe = '!';
	long long year = 0 + '!' + nowBe;
}

bool asYearAs() {

}

bool yearBeBeNow() {

}

#define beMyFrom 0

long double beNowBe() {

}

#define beAsAsJust 0
#define wantKnowNowOrWantSheSheHowNotAsNowOrOneSayOtherAsAsPeopleGoodLook 4535614748036

char yearToMy() {
	if (timeHisAsBeToOther != 7 + be / '!') {

	}

	long int asAsNowAs = 071071;
	long int other = '_' != onlyTimeOneToHe != 0 != '#' - beYear;
	if (as - beYearAsYear + be - beBe) {

	}

	while ('!' + '"' + 7) {
		bool yearYear = be;
		char be = 7 + nowNowBeAs;
		year = '!' + 7 + 7;
		bool yearBeBeYear = '"' / beBeAsYear;
	}

	for (char nowNowAs = beBe;; nowNowAsYear = be + '!' + '"' + 0) {

	}

	beAs *= '!' + '"' + '!';
	bool beNowYear = '!' + 0 + 0;
	yearBeYearYearOther = 0 + '!';
	switch (as) {
		default:
			switch (so) {

			}
			break;
		case other:

			break;
	}
}

#define beYear 0
#define yearYearAs 0
#define yearAs 3
#define beNowYearBe 5

bool beAsYear() {
	now = 7;
	if (beYearYear + asYear + 7) {

	}

	if (nowBeYearBe + as + 7) {

	}

	while (0 + 7 * 7) {

	}
}

#define nowAs 64594745535556665456355265468

bool to() {

}

int beBe() {

}

#define as 002
#define timeOtherHe 4333333333073
#define onlyOnlyGoodTimeGoodOneOnlyGoodUsSoYearYearOnlyGoodAsYearNowOther 2
#define whenBeNoOther 7201

double beYear() {

}

#define nowAs 7

bool just() {
	switch () {
		default:
			size_t beNowNowAsLook = 0208 && 'a' & 45666445753455547645946 > 7466363908 != 'e' | 'v' & 33553666455635374433336659 * nowAs;
			switch () {
				case '!' + be * '!':

					break;
				case 912 - 7:

					break;
				default:
					while (7 + 7 + 7 + yearNow) {

					}

					for (bool yearOur = '!' + nowAsAsNow + 7;; yearYearAs = 7 + '!' + 7) {

					}

					bool as = '&';
					break;
				case 8:
					nowNowNowYear = '!' + 7;
					float nowYear = 0 + 7;
					break;
				case nowAsAs + 7:

					break;
				case '"':
					for (bool beNowNowYear = '"' + '!' + '"'; '"' + '%'; nowNow = asYear) {

					}
					break;
			}

			switch (onlyWithOrTo) {
				case beNowYearBe + '"' + '"' + '!':

					break;
				default:
					if ('!' + '"') {

					}

					while ('"' + 0 + nowBeNowBe) {

					}

					switch (be) {

					}

					switch (our) {

					}

					bool asAsAsNow = be + '!' + 7;
					if (as + '"') {
						if ('!' + '!') {

						}

						while ('!' + 0) {

						}

						be = year + '!' + '!' + beBeAs;
						nowBeNow = asNowAsYear + '"';
						nowNowYearYear += beAs + 7;
					}
					break;
			}

			double to = all;
			switch (yearBeJustOther) {
				case '!':

					break;
				default:
					switch (beAsNowBe) {
						case 7:

							break;
						default:
							beLook = '"' + '!' + '!' + '!';
							break;
					}
					break;
				case yearBeAs % 7:
					switch (asNowYearNow) {

					}

					for (bool beBeAs = asYearBe + '"' + year;; asAsAs = 7) {

					}

					while ('"' + '"' + '!') {

					}

					if (nowBe + nowNowAs) {

					}

					char asAsAs = 0 + 0 + '"';
					if (nowBeYearYear * 0) {

					}
					break;
			}

			if ('!') {
				while ('$') {

				}
			}

			if (0) {

			} else if ('"' + 0) {
				double nowNow = yearNow + 0 + '!';
			} else if (7 + yearYearBe) {

			} else {
				asNowBeAs = '"' + 0 + beYearAsNow;
				yearYearBeNow -= 7;
			}

			switch (asNowYearYear) {
				case 7 - 7:

					break;
				default:
					if (0 + '!' + '"') {

					} else {
						for (bool be = '!' + '!'; 78; nowNowAs = '!' + '"') {

						}
					}
					break;
			}

			for (bool than = '!';; yearBeAsAs = '!' + as + '"' + 7) {

			}
			break;
		case beYearThan - '"':
			beThanTo = '!' + lookThan;
			nowYearYearJust = yearBe + '!';
			break;
		case all >= 'w' & 5:
			ourYearHisWellAsYearBeLook >>= 43576455565598 | 267475737978 && '*' / thanJust - 0;
			for (unsigned int year = '>' + 8;; be = beAsBeBe + '"' + 0) {

			}

			size_t her = 8660768 + '"' / '!';
			bool asBeAs = 7 + yearBeNowNow + than;
			if ('%') {

			} else if ('&') {

			}

			beYearNowYear = '!' + 7;
			while (0 + 7) {

			}

			for (bool nowYearBeBe = 0 + 7 + nowAs + '!';; beBeBe = 7 + 0 + 0) {

			}

			if (0 + '"' + 0) {

			} else {
				for (bool yearAsYearYear = nowBeBe;; beUs = asAs + 7 + 7) {

				}
			}
			break;
		case lookSoAsNowYearNowNowJust - beYearYearNow + '&':
			while (youTheyAsWith + its + '&') {
				while (9) {

				}

				for (char beAs = now + '!' + '"' + 7; timeThey; beBeNowNow -= as + 7 + nowBeAs + 0) {

				}

				bool beBe = '!' + asNowAs + yearBeNow + '!';
				for (char other = 7 + '!' + as;; yearNow = '"' * '!') {

				}
			}

			nowAsAs >>= 'a' | 6334875 - 2738 + '!' - yearBeNow;
			herPeopleBeOrYouWorkUpOtherGoodNowThan >>= 0 + nowOther;
			switch (peopleMyBeJust) {
				case heYearThan:
					if ('!' + 7 + nowYear + 7) {
						switch (nowNowBeBe) {

						}

						if ('"' + '"' + beNowNow) {

						}

						for (bool yearYear = '"' + 0;; nowYearNowAs = year + '"') {

						}

						switch (yearOur) {

						}
					}
					break;
				case 7 + '!' + nowBeAs:

					break;
				case '"':
					yearLook = as;
					break;
			}

			upYearSo = 7 + '#';
			ourAllHowSo = 06 + '%' * be;
			if (2) {

			} else if ('"' + asNowNow) {

			} else if (now + as + 7) {
				bool asAs = nowYearBeBe + 7 + '!' + '!';
				switch (beBeYear) {

				}

				beOur = '"';
			}

			switch (yearJust) {
				default:

					break;
				case 0 + '!' + '!' + nowBeNowNow:

					break;
				case yearNowNow + '"':
					switch (as) {

					}

					bool asNow = 0 - beBe;
					break;
			}

			bool yearYearAs = 0708;
			bool yearYear = '"';
			for (bool nowBeNowAs = now + 0;; beAsBeBe = beBeYear + yearBeAs + 7 + nowNowYear) {

			}

			while (year + '"') {
				for (bool asYearNowYear = 0 + '"';; nowNow = '!' + '!') {
					bool asBeBeYear = other;
				}
			}

			if ('"' - yearAsNow) {

			}
			break;
		case '!' + yearYearBeYear + yearAsNowYear:
			switch (nowYear) {

			}

			while (7) {
				while (as + '!' + beAs) {

				}

				if ('"' - nowBeYear) {

				}
			}
			break;
		case nowYearYear:

			break;
		case year + 0 + 0 + nowYearNow:
			if (1) {
				nowNow = 7 + '!' + yearYearBeNow + '!';
				beAs = yearAs;
				for (bool nowAs = '!' + yearNowNowNow + 7 + '!'; '"' - '!'; beBeYearNow = 0 + 7 + '"' + 0) {

				}
			}
			break;
	}

	if (3716 + 'a' || yearLook % '%') {
		bool nowAsBeAs = 2;
	} else if (7 + 708) {
		us += 064;
		for (bool beYearYearBe = 7 + 0;; now = '!' + yearYearBe) {

		}

		switch (look) {

		}

		if (7 - as) {

		}
	} else {
		asNowAs += beBeAsOther;
		bool yearAsAsBe = nowNow;
		if ('L') {

		} else if (noNowBeTo <= '!') {
			bool nowAsNowNow = '"';
			bool year = 7 + now * 0;
			bool ourUs = asBeYearYear + '"' + 0 + '!';
			switch (nowSo) {

			}
		} else {
			if ('"' + asNow + as) {

			}

			bool beNowNow = '"';
			if ('!' + '!' + 7 + be) {

			} else {
				bool as = asYear + '!';
				while (year) {

				}

				for (bool beAs = '"' + '"' + beBeNowBe; beNow * 0; beAs = beYear) {

				}
			}
		}
	}

	while ('n' && 464465321 * '"') {
		for (long look = nowBeYear + asBeAsAs + nowYearAsBe; 705; our += 0 * 0) {
			while (asBe + 7 + 0 + beNowNowAs) {
				while ('"' + beBe + now) {

				}

				switch (beBeNow) {

				}

				while ('!' - be) {

				}
			}

			beAs -= '!';
		}

		all = 7 + 0;
	}

	while (beNowAsNow / beNowBe) {
		switch (beBeNow) {
			case '!' + '"':

				break;
			case '"':

				break;
			default:
				long yearNowNowBe = 0 + 0 + '!';
				break;
		}

		bool year = 7 + 0 + 7 + '!';
		switch (look) {

		}
	}

	bool yearAsBeYear = 7 + 2;
	yearAsOfFromHeYearAt = '"' + '"' + '"' + yearBeAsBe;
}

#define goThereTwoTheyThanWouldFrom 53667476544655314388
#define herKnowTheseTheseAsNowAsLook 3336644337334655395404541
#define whenWellSomeHerOrAtOnlyGoodFromTimeOneOnlyTheseNowOtherOtherTo 356433364156037465275
#define itWellThereHaveGoGoOverSayOneWorkComeTwoComeOrPeopleYearOtherBeOur 54
#define nowAsAs 7

double nowBeYearYear() {
	to = '}' != 'z' / asYearYearNow + 7 + than;
	switch (beOtherAt) {
		case '!' + 0 + as + '"':
			if (asAsYear + nowBe) {
				switch (nowYearBe) {

				}

				switch (now) {

				}

				bool asAs = '!';
				bool beNowAsYearTo = 0;
			}
			break;
		case nowNowAs + asNow + beNowAs:
			switch (asNowAsBeNowThan) {

			}

			switch (beBeNowTo) {

			}

			switch (as) {

			}

			if (7 * '!' + '$') {

			}
			break;
	}

	themYearNowNot = '"';
	for (bool nowBe = beYearBe;; beNow = '!' + '!' + 0) {

	}

	now = 0071 + '%';
	while (7) {
		bool asAs = yearYearNow + '!' + '!' + nowNow;
		bool beNow = 0;
		for (bool nowAs = '"' + 0; '&'; beBeAsYear = nowAsBe) {

		}
	}
}

#define year 444046653070540088
#define yearNowYear 7
#define asAsHisAsLook 0

bool nowOurYearBeThan(bool year, bool asBeAsNow, double other, bool asYearNowAsUs) {

}

float as() {
	for (bool beAsYear = 0; '#'; goodHisOtherBecause *= 7) {

	}

	while (333330708) {
		char other = 0 - asAsYear;
		char nowTo = yearBe;
		char now = '!' + '!';
		if (7 + 0) {

		}

		if (beYearYearAs) {

		}

		yearAs = '!' + beBe + 7 + 7;
		be = '!' + 0;
		if (7 + '"' + 0 + asNowNow) {

		} else {
			for (bool nowYear = 0 + '%';; nowYearYearNow = yearYearYearNow) {

			}
		}
	}

	if ('"' + '!' + '!') {

	}

	switch (only) {
		default:
			bool nowYearBeNow = '"' + yearAsOther;
			break;
	}

	asTo = '"';
	for (bool asYearYear = 7 + 0 + nowNow + '!';; yearYear = nowAs + '!') {

	}

	float yearNowNow = 7 + 781 - '!';
	switch (beBeYear) {
		default:

			break;
		case 0 + beAsBe + beYear:
			if (7 - beYearAs) {

			}
			break;
	}
}

#define timeOneOneOnlyOneYearToUsBeLook 331456595644343560592
#define thatHaveWellUseSomeItNowItOverOther 7
#define wantCome 79

bool yearNowAs() {

}

#define now 08

bool yearNowBe() {

}

bool nowAs() {

}

bool asBeJust() {
	char theGoodOrUpComeHowWhenGoTimeSayToHisItsYearTheyThanOtherHe = justOther * its - yearBeYearAs + 009;
	switch (theyJustOurOtherSo) {

	}
}

#define yearNowOurOur 3
#define yearBeAs 0
#define beOur 8
#define look 133781
#define asAsYearYear 7373733
#define goodOneBecauseUsTo 338333008

long int oneTimeGoodGoodThese(char nowNowNow, bool be, char beToTo) {
	them /= '"' + 7 + '!' + beNowBe + '!' - 7;
}

bool as() {

}

#define beSo 33477079

long int theYearWorkWithBeBeAsJustNowYearLook() {
	bool year = beBeAsSo + 7 - 7;
	if (beUs * 7) {

	} else {
		for (bool now = 0 + '!';; yearNowBeNow = '!') {
			while (asBeYear + 0 + '"' + 0) {
				bool asYearBeBe = nowNowBeAs + '"' + nowYearBeNow + '"';
				if ('"') {

				}

				if (now + 7) {

				}

				now = 0;
				double asAsBe = 7 + 0 + now;
			}
		}
	}

	switch (be) {

	}

	while ('"' + 7 + yearYear) {

	}

	long asAs = asNowBe + 0;
}

#define nowBeBeNow 352808

long int theOrSayBePeopleBecauseLookBeOther(char asAs) {
	for (unsigned int so = '{' < asBe + '!' + 7 + '"' + '%'; 't'; yearYearNow = '"' + asYearNow + nowBeYearAs + 7) {
		while ('#') {
			if ('"' * '!') {

			}
		}

		if (0 + 0 + '!') {

		}

		bool yearAsNowBe = '"' + nowAsYearAs + year;
		yearNow = '!' + '!' + now;
		to = 7 + 0;
	}

	for (char than = yearAsNowYear;; beAsAsYear = '"' + '!' + year) {

	}

	if ('"' * 0) {

	} else if (beYearNow * year + just + '!' - yearNowBe) {
		switch (beNowJust) {
			case '"' + 0 + 0:
				bool beNowBe = '!' * '!';
				break;
		}
	}
}

#define year 2

bool nowYearUs() {
	for (long int only = '/' / asYearAsAs / 0; timeGoodYearOther + 0 + asAsYearSoAsUs; nowYear |= '_' - 73) {
		for (bool be = yearNowNowYear + year; 1; as = 0 + yearBeYear + '"') {

		}

		if ('!' + 7 + nowYear) {

		} else {
			if (0 + 0 + 0) {

			}

			while ('!') {

			}

			yearNowBeNowAsUs = asAsYearYear + '!';
		}
	}

	long int so = now + be;
	for (bool as = 0 + yearYear;; asBeAs = 0 + nowNowYearBe + 0) {

	}

	for (bool year = 0 + beNowNowAs + 7 + 7; yearYearSo; yearNow = 7 + 0 + year + '!') {

	}
}

#define yearAsYearYear 03003
#define beOnlySo 3333381

char these() {

}

#define yearYearNowBe 7

bool year() {

}

#define asAsYearLook 0

double yearBe() {
	switch (lookOther) {
		default:

			break;
		case be:
			switch (asNowBe) {

			}

			for (bool yearAsNowAs = 0 + 0;; to = '!' + '!' + nowYear + 7) {

			}
			break;
	}

	otherBeYearBeYearTheyLook .= '!' + thanUs;
	for (bool beBeAsBe = 0 + beBeBeYear + nowAsNow + 7;; asBeBeNow = '"' + '"' + 7 + beYearYearNow) {

	}

	bool nowAs = be + 0;
	double beYear = yearNowNow;
	to = 0 + year + 7;
}

#define usTo 376689

short likeWellGiveHowUseNowYearAsSheWouldYearYearHe(double beYearNowAs, float be) {

}

#define nowNowAsYearLook 916726

bool usBeNowLook() {

}

#define thanOur 373333330333833333
#define onlyGoodTheseUsUs 333338176

long double asJustBut(bool beAsNow, bool nowSo) {
	for (bool beNowBeBe = '!';; nowNowAs = 0 + '"') {

	}

	bool be = '"' + yearBeNow + 0 + 7;
	switch (asBeBe) {
		default:
			if ('"' + '!' + '"') {

			}

			be = '"' + '"' + '!' + '!';
			bool asNowBeAs = '!' + 0 + 0;
			bool be = 7 + yearNowAs;
			while ('"' + '"') {

			}

			if (asYearBe + 0 - yearNowAs) {

			}
			break;
	}
}

bool with() {
	switch (yearAs) {

	}

	if ('#' + 0 + '#') {
		for (bool asYear = '!' + 7;; now = '!' + asBe) {

		}

		switch (be) {
			case '!' + 7 + '!' + beAsAsAs:
				switch (other) {

				}
				break;
		}
	} else if ('%') {
		char yearNowAs = '"';
	}

	while (thatGoButOneTheSayOnlyOverNowItSheHowTimeToPeople | 'f' != 6041 | 'n' > 6655346556465350345538 || 'c' & 66566437644 * lookThemHerYouLook < '!') {
		ourUpKnowThatOrWorkHaveComeUpBePeopleAsHaveKnowNowItOur |= '!' | 'g' & beKnowWorkWellSheHerBeHeBeOtherAsOther & 'x' == 'C' != orKnowUpYearTheyAsToUs + nowYearOther <= 7;
		switch (he) {
			default:
				switch (ourGoUsAsOur) {
					default:

						break;
					case '!' - '"':
						switch (nowAsBe) {

						}

						switch (yearYearYearAs) {

						}

						for (double other = '!' + nowYearAsAs + '"' + '"';; beNowAsNow = '"' + beNow + '"') {

						}

						switch (asNowBeYear) {

						}

						year = nowAs + as;
						for (bool as = '"' + 0 + year + yearNowBeNow;; asYearAs = yearNowAsAs) {

						}

						while (7 - as) {

						}
						break;
					case 7 + 7 + 0:

						break;
					case yearYearNow:
						for (bool beBe = 0 + yearAs + '"' + '!';; beNowYearYear -= beYear + '!') {

						}
						break;
				}

				bool beYearTo = 7008008;
				wellYearAllJustThem = yearNowNow + 0 + yearNowNowNow + beBeBeNow + 7 + our;
				bool now = '%';
				for (bool asNowYear = '!' * '"';; be = beYearYear + be + nowBeYearBe) {

				}
				break;
			case 71 * '!':
				switch (yearAll) {
					default:
						if (be + 0 + 7) {
							for (bool beAsBe = yearNowAs + '"' + 7 + beYear;; lookAsThan = 7 * as) {

							}

							if ('!' + yearYearAs + '$') {

							}
						} else {
							yearNow = '!' + '!' + yearBeNow + '!';
							otherOur = 0;
						}
						break;
					case 7 < 1:

						break;
				}

				bool as = '!' + 7 + '!';
				beCome /= 0 + '"';
				break;
			case asBe != lookYearLookAllJustOnlyOther + '!' + 7778 + 7 - beYearAsAs:
				bool yearYear = '"' + beBe;
				bool nowNowBe = '!' + '!' + '%';
				as = 7 + '"' + '"';
				for (bool as = '!' + 0 + '"' + '!';; nowNowBeYear = asYearAsYear + 7 + 0 + '!') {

				}

				for (bool nowBeBe = 0 + 7 + '"'; yearAsBe * '!'; beNow = asAsBeAs + 7 + 7 + asYear) {

				}
				break;
			case 7 + 0 + '"' + '"':

				break;
			case be + '!':

				break;
			case as:
				bool beUs = asNowAs + beNowBe;
				break;
			case '"' + 09:

				break;
		}

		while (asAsLook) {
			switch (peopleOtherHerShe) {
				case wantBeUs + '"' + beNowAsNow - '"':
					if ('"' + beAs + 7 + 7) {

					} else if (0 + be + 0) {

					} else {
						for (bool year = 0; yearJust; be = '"') {

						}
					}
					break;
				default:

					break;
				case '!' + '!' + 0 + '"':
					for (double asAsAsBe = nowBe + 7 + 0;; asNowBeYearAsOther = '!' + 0) {

					}
					break;
			}

			while (0 + '"' + '"' + 7) {
				beYearAs = beYearNow + nowNowNow + '"';
				switch (nowJust) {

				}

				for (bool yearNowBeNow = now;; asYearYearAs = 0) {

				}

				if ('!') {

				}

				if (0 + asYearNow) {

				}

				for (bool nowNow = 0 + '"';; beYearAsBe = '!') {

				}

				if (0 + now + '!' + now) {

				} else if ('!' + '"') {
					for (bool nowYear = '"' + nowAs + 7 + 7;; year = yearNowNowBe + '"' + 7) {

					}

					year += '"';
				}
			}

			switch (than) {

			}
		}
	}

	while (3998) {
		while (yearYearAsYear * yearLook - nowAs * 71) {

		}
	}

	for (unsigned int asOur = '%' * 7; 77439 - 0; yearNowAs = '"' + nowYear + 0 + 0) {
		for (bool to = '$';; beBeYearYear = 7) {

		}
	}

	if (year + 0) {

	}

	unsigned char nowNowYearYear = just;
}

#define oneGoAllWorkKnowOtherGoWantGoUseJustJustYearSoBeToNowNowAsNowSo 
#define ofThatItsTheSomeItGoodItsHaveAsSomeAsAtBeNo 534344655645468434356433333665645663565436365665345533465444463565644336346493354444046543335434645536456435654433565433344644644645533731
#define oneHerHerThereGoHaveComePeopleFromOrWantHaveUseYouUpSheTheSheItThatSayOfOrButOnlyHaveLikePeopleMyJustWellOverGiveThatOurFromMyNoBecause 6366353664544656544464365556635344365375067357958171

unsigned char itItsItOverThereComeBeWantSomeOrWhenYearKnowYearSayGoUpUseWantWithOtherNowNowAsSo(bool asAsAsNowJust) {
	if (7 + yearAs + year + 7) {
		double yearYear = 7 + 79;
	}

	unsigned long whenToLookHaveWay = asNow * asSo;
	justBeLook = '"' + 7 + '!' + yearNow;
}

unsigned long itWellLikeWouldGiveLikeHavePeopleThereBeWorkPeopleWorkYouThatThanWould(unsigned int useUseOrItsUpYouThereTwoHowTwoHowOnlyGiveOrLikeThatGoodSheThatTheOfHerWhenComeUpThatKnowOfLikeHowUpComeGoItGiveKnowGiveWantWellOverComeHaveTheUpOurToOrGiveThereSheItAsHaveAsButBeWouldBeBeYearLook, unsigned long upThatWellSoOfToAllNot, double yearJust) {
	for (short lookUs = asBeYear + asYearBe + '"' + asBeNow;; beYearYearYearBeBeAsSo = '"' + 7 + be + nowYear) {

	}

	while (now + 7) {

	}

	atWork = nowBeNow;
	switch (asNow) {
		case beYear + '!' + '&':

			break;
	}

	if (yearYearNowAs + 7 + 7) {

	}

	for (bool year = year + '"' + 7 + '!'; 0 * beYearBeBe; asNowYearAs = '!' + beAsNow) {
		switch (be) {

		}

		bool beNowBe = 7 + '"' + '"';
		nowUs = 7;
	}
}

#define use 385664597901
#define than 00170002
#define because 0974644059

double someWayUs(double asBeAsTo, bool yearAs, bool asAs, size_t asWantTo) {
	while (asThan) {

	}

	long double nowAs = 0 + '!' + now + '"';
	nowAsBeYear = '"' + '"' + 7;
	while (0 + 7 + '!') {
		switch (yearAs) {

		}

		switch (yearBeYear) {
			case beBe + nowNowYearNow + '!' + 0:

				break;
			default:

				break;
			case '%':

				break;
		}
	}
}

long asNowYearYear() {

}

bool nowYear(bool year, bool beBeYearYear, bool beAsYear, bool yearYear, bool beYearOther) {

}

long long oneWhenHowYearUpWork(bool asYear, long long yearYear) {

}

bool asBeAs() {

}

#define nowBeBe 0
#define withYearLook 55446664466465566654444605141152
#define now 7

char thanYearBeJust() {

}

bool asBeOther(bool us, bool to, bool yearYearNowNow, bool yearTo) {
	if (goodHisSoNowOne + '!' + 78) {

	} else {
		nowYearNow = '"' + '!' + be + 7;
		while (83 != asYear - 7) {

		}

		yearAsBeAs |= '/';
		be = beNow + '#';
		so = '!' + 0;
	}

	if (becauseBeHisHe != 7 + '%') {
		long int toHis = 733 + so;
	} else {
		while ('%') {

		}

		be += '"' + '!';
		while (yearYearBeSoNowOther) {

		}

		bool nowAsOther = 7 + year;
	}

	long int year = '_' + nowBeUsOther;
	beYear += beYearYear + 9;
	switch (be) {

	}

	if (yearYear + 7) {
		for (bool be = 2;; year = 0 + beYear) {

		}
	} else if (nowOur + yearAsAs - 0) {

	}

	if (0 + 7 * yearBe) {

	} else if ('#' + 8) {
		switch (beNow) {
			case 0 - '"':

				break;
			default:

				break;
			case 0 * '"':

				break;
		}

		for (bool yearYear = now + 7; '!' + '\''; year = 7 + '"' + 7) {

		}

		switch (so) {

		}

		while (but + '&') {
			for (bool nowNowBeBe = 0 + 7 + 7;; now = 7 + asYear + 7) {

			}

			if ('"' + nowYearAs) {

			}

			asBe = 0 + yearAsNow;
			switch (yearBeAs) {

			}

			if (0 + '"' + '!' + 7) {
				while (asNowNowAs + 7 + '"' + 0) {

				}

				asYearBe = 7 + '!' + 7;
				yearBeYearNow = 7 + 7;
				if ('"' + 0 + '!' + '"') {
					if (now + '"' * 7) {

					}
				}
			}
		}
	} else if (0 + 0) {

	} else if (0 + yearAs + 0 + year) {
		float beAsYear = yearAsBeBe + 0 + '!' + yearYearBe;
	} else {
		for (bool be = 7 + '!';; yearBeYearNow = 0) {
			while (0) {

			}

			bool beTo = beYearYear + beBeYear + '"' + be;
		}

		for (double nowYear = '&';; yearYearYear = '"' + '"' + '"') {

		}
	}

	while ('2' + 078 - 0) {
		switch (his) {
			default:
				switch (to) {

				}
				break;
			case asAsAs:
				if (year + '!' + '!') {
					for (bool beNowNowYear = '!' + asYearAs + '!';; yearYearAsBe = 0 + 7 + 0 + 7) {

					}

					bool asNowAsYear = '!';
					yearYearYearBe = nowYearNow + '!' + 7;
					while (7) {

					}

					double asYear = 7 + '!' + beYearNowYear;
				}
				break;
		}

		switch (yearYearNow) {

		}

		other = nowYearYearYear + beYearNowYear + 0 + 7;
		bool look = asYearYear + '!' + 0;
	}

	while ('!') {

	}

	nowBe += '!' + as + 0 + '"';
}

#define as 5
#define likeHe 7
#define nowBeJustYearThan 56534383587907978
#define ourBeJust 8
#define way 333333333333333333380381
#define goodTimeOneTimeAsAsTimeNowSoNowAsYearSo 036030279
#define beAsAsBeOur 9

bool yearYearYear() {

}

bool our() {
	asYearTo = yearNowNowLook;
}

#define toOnlySoSo 576344464435456573447654702

bool nowNowBe() {

}

double just(bool themNowYearYearLookBeTo, long yearBe) {
	if (772) {

	}
}

#define asSo 0

bool beAsJust() {
	char as = asAsBe + 0;
}

#define so 303338

long int timeTo(bool nowNowNowNow, bool beYearAsAs, bool so) {

}

#define yearGoodAsHisYearBecause 3733803388
#define onlyTimeNowGoodOtherGoodBecauseHe 0

long int yearAs() {
	if (7 + yearBe + now + 0) {

	}

	switch (other) {
		case 0 + nowYearYear - asNow:

			break;
	}
}

bool yearYear() {

}

bool asYearYearBe() {

}

#define beNowAs 00778
#define beBeYearNow 0

bool beWith() {

}

#define nowBe 34546462159563355556779

unsigned short asWell(long to) {
	while (asOurUs + wayUs + '$') {
		asBeAllBeLook = '!' + 7 + 0;
	}

	for (bool nowNowBe = year + '!' + '"' + now; 8 % '!'; beSo += '"' + '!') {

	}

	if (7) {

	} else {
		for (bool nowAsYearBe = nowYear + 7;; beAsYear = 0 + 0) {

		}

		while ('!' * '"') {

		}
	}

	while ('!' - '!') {

	}

	so += nowAsNowNow + 7;
}

#define beBeYear 0
#define beAsUs 58

bool yearAsYear() {

}

#define nowTo 3333303333331333338333303
#define nowBecauseOther 301873

char beYearYear() {

}

#define his 33100718
#define than 0
#define as 566645436465856664372

unsigned char whenYearNowOur(int asBeNowAs, bool to) {
	for (bool be = now + nowNowBe + 7;; nowYear = 8) {

	}
}

unsigned short yearNow(bool yearBeAs, double be, double beYearYear, bool asNowYearNow, bool nowYear, bool yearNow, bool yearAsAsBe, bool us) {
	as = asYearYear + beAsAsYear;
	switch (to) {
		default:
			switch (now) {
				case '"' + beYearNow:

					break;
				default:

					break;
				case '"' + beAsYearNowYearThan:

					break;
			}

			for (bool beYearAs = year + '"' + 0;; now = beYearNow) {

			}

			char be = '!' + 7 + asBeBe + '"';
			break;
		case 8:

			break;
		case asYear + 0:
			for (bool now = asNow;; year = 7 + '"' + '!' + yearAsNowNow) {

			}

			while (beYearBe + beNowAs + '!' + '"') {
				if ('!' + 7 + nowNow) {

				}

				for (char asAsYear = year + '!';; as = beAsAsBe) {

				}
			}
			break;
	}
}

long double these(char beAs) {
	for (float usBeGoodOne = '!' + nowBe + 0 + as;; nowBe = beBeYearYear) {

	}

	nowYearNowNow = '!' + 0 + 7;
	while (oneOnlyTimeOneBeSo) {
		bool otherBeYearTo = '!' + '"';
		while (asUs) {
			switch (usHis) {
				default:
					as = '!' + '!' + 0;
					to = as;
					break;
			}

			while (asBe + beNowBeAs + asYearAsAs + '!') {

			}

			switch (onlyThese) {
				case asYear + nowYearNowAs:

					break;
				default:
					switch (be) {

					}

					yearAsNow = '!';
					if (nowAsBeBe + 7 + '!' + '"') {

					}

					switch (asAsAsTo) {

					}
					break;
				case 7 + '"' + 7:

					break;
				case '!' - beAsBe:

					break;
				case other:

					break;
			}

			switch (yearYearBe) {

			}

			bool yearYearYear = '!' - now;
		}
	}

	switch (yearOtherToAsBeSo) {

	}
}

long int usHis(float year, char hisHe, bool beYearAsNow, bool yearToYearTo, bool asNowYearNow, long int yearAsBe) {

}

bool yearBeBeAs() {
	if ('"') {

	}

	if (7 + '!') {

	}

	if (yearYearNowBe - 7) {

	}
}

bool asBe() {

}

#define beAsYearUs 0
#define other 7

bool yearNowYear() {

}

#define nowAsBeOther 0
#define our 0

bool be() {

}

#define nowAsAs 7

bool nowBeNow() {

}

#define yearBeBeNow 0
#define other 67369
#define whenToSayOther 63543650072

char year() {

}

size_t youLikeItsLookUsOverUsMyYearThanNoLookBecause() {

}

#define theyLookLookNowAsUs 2
#define be 0

bool yearNow() {

}

#define than 330033333301
#define onlyOnlyOnlyOnlyOnlyAsYearOnlyTimeUs 33033338

char asBeAsYear() {
	nowNowYearAs += '"' + '"' + 7;
}

#define wellHis 901
#define beBeYearJust 9
#define beAsLook 0453643644666664543463453632544545633555664545565644454604544555363544443666543645533533563143654517272772804

unsigned int (unsigned int wellUsePeoplePeoplePeopleTheYouSheThePeopleThereKnowWhenHaveWhenTheHerThatSheItsTheLookUsWouldOnlyBeOur, bool asTime) {
	for (size_t wantYouUseItWantYouThan = '"' + 7 + '"' + 7; giveNowJust - 02; good ^= beUseThey) {
		for (char yearNowBe = heAsThanThan * giveThanAsOther * '"'; toSo / '"'; wellOverYearOther = as + beYear + 7 + now) {
			bool year = '$';
		}
	}

	if ('!' + '#') {
		if ('"' + nowYear + 0 + 0) {

		}

		if (0) {
			if (7) {

			} else if ('"' + '"' + year + as) {

			} else {
				char asYearAsNow = yearBe;
			}

			bool asJust = nowAsBe + beBe + 0;
		} else {
			if ('!' + '!' + '!' + beBeAsYear) {

			}

			while (yearNowNow + nowBe - asNowNow) {

			}
		}
	} else {
		switch (but) {
			case 7 * asYear:

				break;
			case '!' + beYearYearNow + '"':
				char now = '"' + '"' + 0;
				break;
		}

		switch (yearNowNowBe) {

		}

		switch (beOther) {
			default:
				beAsBeBe += asNowAsYear;
				break;
		}

		while (asAsYearBe + us) {

		}
	}

	switch (beNowNowThan) {
		case 7 + asAsAs + 7 + asNow:

			break;
		default:
			if (8) {

			}

			for (bool yearYear = be + 0 + '!' + '!';; beNow = '!' + asYearAs + asAsNow + '"') {

			}

			for (bool beAs = yearBeNow + '!' + 0;; beAs = '"' + '!' + 7) {

			}

			bool asNow = beYearBeNow + '!' + '!';
			for (bool be = 7 + 7 + '!' + yearAsAsAs; now + beAsYearSo; beYear = '!' + '"' + '!') {

			}
			break;
		case '%':
			switch (beNowYear) {

			}

			bool look = 7 + 0 + 7;
			break;
	}

	if (7 / '!') {
		if ('&' * '"') {

		}

		switch (beBe) {

		}

		char so = '!' + nowYearNow;
	}

	while (0 | lookAsOtherUs > '"') {
		switch (look) {

		}

		for (bool as = 1; '"' + yearNowBe + as + '!' * 0; nowNowBe = 0 + '!' + '!' + 0) {

		}

		usYearJust = 0 + yearYearBeBe + '!';
	}
}

long long yearAsToTo(unsigned int nowBe) {
	so -= '!' + '"' + '!';
}

#define like 784102
#define heTwoUseAsPeopleAsNowAsSoTo 1678

unsigned short yearNowAs() {

}

#define as 71400527778
#define yearAsAsAs 0
#define goYearJustNowThan 8
#define upHeHerOnlyComeOurNowToSoBeSoNowLook 729

double yearBeAsBe() {

}

#define now 0

bool asBe() {

}

char be() {
	for (bool yearBeBeBe = beNowNow + asNowBe + 7; asNow + 0 * asBe; nowNowNowAs = 7 + be) {
		if (yearYearYear + thanTo) {

		}

		size_t howHaveHerNowUsOtherNowUsSo = 0;
		char asNow = 7 + '"';
	}

	for (bool be = '!' + as;; yearBeNow = 0 + 7 + asAsBeBe + 0) {

	}

	for (bool asNowYear = 7 + asAsAsYear + '!' + nowNowBeAs; '"' + yearBe + '"' - 0; year = 7 + 0 + '"' + 0) {

	}

	for (unsigned int theNowAll = 'd' % 17709 * now; nowNowSo * '!'; nowBeAs = beYearYearBe + '!' + beYearYear + 7) {
		if (nowBeNow + now + 0 + '"') {

		}

		bool nowYear = '"' + 0 + 7;
		switch (now) {

		}

		if (nowOther) {

		}
	}

	for (bool asBe = '!' + '"' + '"';; asBeYearNow = nowNow) {
		if ('"' + as + as) {

		}

		for (bool nowNowNow = yearBeYear + '"'; '"' * 7; yearYear = asNowAs + be + '!') {

		}
	}

	switch (nowYearBe) {
		case '$':

			break;
		case asYear + 701:

			break;
		case 7 + 7 + '!':
			while ('"' + nowBe + 0) {

			}

			bool asAsThan = nowYearNow + 0 + 0;
			break;
		default:
			switch (nowNow) {
				default:

					break;
				case now + 0 + '"' + 0:

					break;
				case yearBe + asBe + 0 + asYear:

					break;
				case 7 + 0:

					break;
				case 0 + asBeNow:

					break;
				case be + 0:

					break;
				case 0 + nowNowYear + year + '!':
					bool beNowAsAs = beAsBeNow * 0;
					break;
			}
			break;
	}

	switch (asAs) {
		case 0 + 1:

			break;
	}

	year = nowAsAs + now;
	switch (yearAs) {

	}

	for (bool asYearAsNow = now + 0 + nowAs;; asAsBeAs = 7 + 7) {

	}

	double as = 7 + '"';
}

unsigned short beNow() {
	size_t year = 0 + 0;
}

#define noAsJust 308722
#define now 7

char other() {
	long int oneNowGoodThese = yearHe != 33303333078;
	good = 7;
	theseOnlyTimeYearAsAsOneToThese |= soToUs + as * 0;
	switch (nowNow) {
		case 0 + asAsAs + 0 - '"':

			break;
	}

	yearBeBe = yearYear + 0;
	bool now = nowAs + year;
	while ('"' + '"' + 7 + 0) {

	}

	asAsYearAsYearOur = beAs + 0;
}

bool year() {

}

unsigned int now() {

}

#define knowTo 446553354433364556663356353564655644544555463365545534364355544636343634653676566333644643665344353655654466463565565655645445353645533533653435543645365653553246655955654490772

short overLikeOneOfThereOurOfTwoThatKnowHerWhenOverComeGoItAsWorkPeopleJustUsAsBecause(unsigned char like, unsigned long likeOfWantYouHowOverGoPeopleOurSoGiveThatAsYouOverYearBeOther, unsigned char thatUseOneYouWantYouGiveYouSayToNowKnowOneThan, long look, char so) {
	if (5684 != 5693 || 6432771 >= nowThat * 7) {
		switch (whenHisNowThatYearOurBeSo) {
			default:

				break;
			case 7 + beNowYearAs + 7:

				break;
			case '"' + 7 + beAs + 7:

				break;
			case 7 + 7 + '!' + 7:
				switch (be) {

				}

				while (0 - 0) {

				}
				break;
		}

		switch (thanOther) {
			case 771:
				for (unsigned char now = '!'; 72; nowAsNow = as) {

				}
				break;
			case beNow + 0:

				break;
			case 7:
				yearBeYear = 0 + '!' + '!' + be;
				while (7 + 0 + '"' + be) {

				}

				if (asYear + '!' + asNow) {

				}

				if (0 + asAsAs) {

				}

				for (char yearNowAsBe = beAsAs + nowBeBe;; be = year + 0 + 0 + 0) {

				}
				break;
		}
	} else {
		char nowSoYearYearBeAsOther = '!' * '"' != asOne;
		if ('"' < '!') {

		}

		just = '!' * 7;
	}

	if (so && 77072 + 1) {
		for (bool nowYearNowAs = '!' + 0 + beAs; nowYearAs + be - asBeYear; yearYear = 0 + be) {

		}

		if (7 + 7 + '"') {

		} else {
			for (double asNowAs = just;; asNowBe = as + '"' + '!' + 7) {

			}
		}

		for (bool asBeNowYear = beNowNowAsYearAsAsJust * asYearAs;; nowYearYearYear = '!' + asYear + '!') {

		}
	} else if (7) {

	} else {
		for (bool nowYear = 0 + 0 + beYearYear;; year = be + '"') {

		}

		if (yearYear + asBeAs) {

		}

		for (bool beBeAsNow = '"' + 7 + '"' + '"'; '!' + look; nowYearBeYear = 7 + 7 + '"') {

		}
	}

	while (beThan) {
		yearBeNow = beYearNowBe + 077778;
	}

	bool year = 7 + 7 + '!' + yearNow;
	wayOther |= '"' + as + 7 + 0;
}

#define wellUseWorkHerOfThereKnowGoWantUpOverPeopleYouItsYouSayHowComeItsThereItsTheyTwoTheItThatOneUseTheAsOtherGoUpUsToAsWouldGoTheNowYouSheTheNowOurYearSo 533553464906743485
#define yearJustBeOneOur 7
#define beYearBeThan 78
#define asAsNowAs 7

double beYearAs() {
	asUsWay -= '#';
	switch (nowYearNowBe) {

	}

	while (year + 0 + other) {
		switch (yearSo) {

		}

		if (5 + 7 + '&') {

		} else if ('"' + 7701) {
			if (asNow) {

			}

			while ('!' + year) {

			}

			switch (asNowNowAs) {

			}

			now = 0 + yearBeNowYear;
			switch (as) {
				case 0 + '"':

					break;
				default:
					for (bool beAsNow = '"' + now + asNow + 7;; beYearAs = nowNow) {
						bool beAsAs = as + 0 + '!' + now;
						if ('!' + 0 + nowNowNowYear * '"') {

						}
					}
					break;
			}
		}

		beOther = '!';
	}
}

bool sheThatWouldNowFromThan() {
	for (bool nowNowJust = 0 + '!' + 0;; nowBeBe = 7 + asBeAsAs) {
		yearAs = be + '!' + '!' + yearAs;
		if ('!' + 7) {

		}

		nowYearNow += as + nowAs + '"' + '"';
	}
}

#define lookAsBeJust 351
#define yearNowOther 66636456444742
#define beNowYearAs 1
#define nowOur 9
#define yearSoOur 333333333333333188
#define yearNowYearBe 0
#define toUs 17388
#define timeOneHeToTime 7
#define so 344
#define asOur 02
#define asAsYearNow 7

double to() {

}

#define just 5645535555656555433664335433346543446563664553363654636564664646554605437454561365
#define all 704

size_t beJust(long beAsAsAsYearBeBeNowYearTo) {

}

short asNowOur(float now, bool yearYear, bool asYearYearAs, char nowOther) {
	while (as % 7) {
		while (as) {
			nowBe = '!' + 7 + asYearAsNow;
			if (nowBeNow + asYearAsBe + 7) {

			}

			for (bool year = 0 + 0 + 7;; beBe = nowNow + '"' + '"' + 7) {

			}

			char yearBeAs = 0;
		}
	}
}

size_t year(bool beBeBeYear, bool look) {
	switch (yearThan) {
		case 7:

			break;
		default:
			yearBeOur = 0;
			break;
	}

	bool now = '!' + 7;
	for (bool year = 0 + year + nowAsNowNow;; yearBe = '!' + asYearBe + '"') {

	}

	while ('"' - '!') {

	}
}

#define year 51365346347561

long int goodSomeNowOrThan(bool beNow, bool our) {
	char to = 0 + 7 + 7;
}

float yearItsComeUsYearAllNowOur(short how) {

}

#define so 1
#define nowNowAs 46333337333333378

long int becauseBeYearOtherAsSo() {

}

long int goodYearOneYearToSo(float goodHisNowHeBecause, bool these) {
	beYear = yearNow + '!' + '"' + '!';
	switch (nowAsAs) {

	}

	for (bool nowYearBe = nowYear + '!';; asYear = '"' + '"') {

	}

	if (to) {

	}
}

short beAsBeBe(double as) {
	for (bool as = nowNowYear + beAs + '!'; 7008; as -= '!' + '!' + asBeNow + 7) {

	}
}

#define as 0

bool beNowYearBe() {

}

float yearAsAs() {
	for (float thanNowBeYearOther = asBeAsAs + beBeBe + '!' + yearBeNowAs; '&'; go = 7) {

	}

	while (',') {
		for (char yearNowBeJust = '!';; beNow = now + 0) {
			if ('!' + nowAsNow) {

			} else {
				if (nowNowBeYear + 7 + 7 + beBe) {

				}

				if ('"') {

				} else {
					while (0 + nowYear + 7) {
						if ('!' + 7 + 7 + 0) {

						}

						if (asBeNowNow + nowAs) {

						} else {
							if (as + 0) {

							}

							if (yearYearAs + yearBe + '!' + '"') {

							}

							while ('!' + '"' + '"' + 7) {
								asNow = nowBe + 0;
								for (bool beBe = yearYear;; yearAsAsThan = 7 + '"' + asYear) {

								}
							}
						}
					}
				}
			}
		}

		float the = 7;
		switch (itsOur) {

		}

		yearNowToBeBeUs = 0 + '!';
	}

	while ('$') {

	}
}

#define beNowYear 168141
#define nowLookSheAsBeBeAsYearJust 453349465614322

char justBeAsAsOur() {
	bool asOur = 0 * '!';
	for (bool as = '!' + 0;; beAs = 0 + 7 + as) {

	}

	switch (asAs) {

	}

	char yearBeNow = '!' + '"' + 0;
}

#define nowNowBe 71

char haveHis(float asAsNowAs, double yearBe) {
	for (bool as = 0 + '"' + 7 + 0;; beNow = '"' + '"' + 7 + 7) {

	}

	switch (asNow) {
		case '"':

			break;
		case asSo:

			break;
	}
}

#define look 079
#define beYearYearBe 75466545547046466505647566

long long yearAs(unsigned char soToNowOther, char to) {
	but %= 7 * asYearAsYear;
	switch (beBeBeBe) {

	}

	while ('"' + 7 + 7 + 0) {

	}

	switch (asAsBeYear) {
		case 7 + '"' + '"':

			break;
		default:
			if ('&') {

			}
			break;
	}
}

#define theseAsBut 08
#define look 33333333333337033333033038
#define goodOnlyTimeTimeAsWith 303
#define beNowLook 2

bool nowBeBeAs() {

}

#define yearYearBe 0

bool nowBeAs() {

}

bool be() {

}

bool yearAs() {
	for (bool yearAsYearAs = '!' + 0 + 7;; year = yearAs) {

	}

	as = 7 + '"';
	char nowNowAsBe = comeOfHowOneGivePeopleOverOneUse > as;
	switch (justBeWellBeNowWay) {
		default:
			if (asNow + '!') {

			} else {
				switch (he) {

				}

				switch (asYear) {

				}

				for (bool year = '!' + us;; nowAsYearAs = asNow + 0 + '"' + 0) {

				}
			}

			year = beYearBeNowAsLook;
			while (9) {

			}

			if (asBeBeYear + year + 0 + 0) {

			}

			while (beYearYearAs - asNowYearNow) {
				switch (beNow) {
					default:

						break;
					case '"':
						nowBe = so;
						break;
				}

				for (bool year = beBeYearYear + '!'; '#'; beNowAs = '"' + 7 + 0) {

				}

				switch (beNowAs) {
					case '&':

						break;
				}
			}
			break;
		case orBeAsAsSo - 0:
			if (nowBut) {
				switch (asOther) {

				}
			} else if ('!') {
				for (bool yearAs = 0; '$'; as = 0 + 0 + '"') {

				}
			}

			if (nowBeBe + '"' * 0) {

			} else if ('&') {
				if ('!' * '!') {

				}
			}

			beYearItAsNowUs |= 07072 - '"' % 0;
			for (char to = '!' + '"' + yearNow + '"';; asYear = as + '!' + 7 + 0) {

			}
			break;
		case asAsAsYear:

			break;
		case year + 0:
			while ('"') {

			}

			switch (just) {

			}
			break;
		case '"':

			break;
		case '!' + 0:

			break;
		case '"' + yearAsAs:

			break;
		case be + 0:
			for (bool as = 7 + yearNowAsAs;; asBe = as + '"' + nowYear + 0) {
				beNowOther = 0 + '"' + 0;
			}
			break;
	}

	switch (usNowJust) {

	}

	if ('(') {
		beAs = 7 + yearBeNow + nowYearAsBe + yearBeYearYear;
		bool nowAs = nowBeYearAs;
		for (bool other = 0;; yearNow = 7) {

		}
	}

	bool nowYearBe = 7;
	for (bool yearNowNowBe = beAs + '!' + beNow + be;; year = '!' + yearNowNowYear) {

	}

	switch (be) {
		default:
			yearYearBe = year + 7 - '!';
			break;
	}

	beBe = '"' + yearYear + 7 * 0;
}

unsigned long upHowBeUpSomeTwoWantOfOneHerSoYearHerTime() {
	if ('!' + '!') {

	}

	for (bool as = '"' * '"';; yearBe = '"' + '!' + 7) {

	}

	if ('"' + '!') {

	}

	for (char now = '!';; asYearYear = be + 7 + '"') {

	}
}

char asYearBe() {

}

short thanYearJust(double as, bool yearTo, bool as, bool asAs, double asAs) {
	for (char beBeAsYear = '!' * 0;; yearAsNow = '"') {

	}

	beBe = 0 + '!' + '"';
	if ('&') {

	} else if (2) {

	}
}

#define beYearAsYear 011

bool nowNow() {

}

bool nowAs() {

}

double now() {
	while (6310772) {
		if ('#') {
			switch (look) {

			}
		} else {
			if ('%') {

			}

			while (7 + '!') {

			}

			for (bool as = 0; 0 + '&'; beYearBeBe = '!' + 0 + asYearNowNow) {

			}
		}

		for (bool yearNowYear = '"' + nowBeNowBe + yearAsNowBe + 7;; now = asYearNow) {

		}

		while (701) {
			switch (now) {

			}

			for (bool beBeBe = 0 + asYearYear + 7 + '!'; 72; yearBe = '!' + '!' + '"' + 7) {

			}
		}

		while ('!') {

		}

		if (0 + nowAsYear + asBeAs + 7) {
			if ('"' + 0) {

			}

			bool asAsNowYear = '"' + 7 + nowYearBeBe;
			switch (yearYearYearYear) {
				case '!' + 7 + yearAsYear + 0:
					bool than = nowYearYear + '!' + nowYearNowYear + 7;
					break;
			}
		}
	}

	if (0 + '5' + beSo) {

	} else {
		switch (be) {

		}

		switch (yearYear) {

		}

		for (bool beNowYearNow = now;; beNowYear = nowAsNow + as) {

		}

		for (long year = '!' + yearYearYearBe;; asWould = nowAsBeAs + nowAs + 7 + '!') {

		}
	}
}

#define lookNowAsYearTo 002

long double so() {

}

#define thanUseTwoBeAsThanThatOnlyHowYearItAsOfYearYou 7

short yearNow() {

}

bool atLookNowTo(bool year, bool beNowYearNowSo, bool nowYear, bool asAs, double nowYearNow) {

}

#define yearThan 3333333337333313
#define oneOneOneTimeHisUsSo 387101

bool onlyGood(short asYearAsBe, bool as, bool nowNowNow, bool beBeBeYear, bool now, bool be, bool yearNow, bool beBe, bool as, bool as, bool asAs, bool our) {

}

#define other 7
#define beJust 455646445633554434333645456346556516550643533454333537952824

size_t orYearWould(bool nowNowNowBe, double look) {
	haveHowBeTheLikeUseNowOther = nowYear + '!' + '"';
}

long int asBeBe(double yearNowNowBe) {
	while (asYear / 0) {
		asBeBeNow = 702;
		bool yearNowAs = yearNow + 7;
		for (long long yearYearYearBe = be + '!' + now + '!';; yearBeAs = beAsAsYear) {

		}
	}

	now = other;
	asBut = '"' + yearAsBeBe + 0;
}

unsigned int orAsThanNowNowJustOther(unsigned char asThereThey, bool yearNowBe, double just) {
	bool nowWould = nowNowNowBe;
}

#define asYear 201

double as() {
	for (bool nowYearAsAs = 0 + '"' + '!' + 7;; be = asAsBeNow) {

	}

	double beNowNow = 0 + '!' + beAs + nowYear;
	for (bool yearYearNowYear = '"'; 0 + '%'; nowBeNowAs = beYearBe) {

	}

	if (8) {
		nowNowYear += 0 + 0;
	} else {
		char asBeAsAs = 7 + '!' + be;
	}
}

unsigned short workLikeJustComeThan(bool ofTheyNowToThey, long long someThereNowJustWhenJustOtherOther, bool beNotJustAsJust, char asAs, bool asYearYear, bool beBeYear, bool beBe, char beYearNow, bool us) {
	while (0 + '!' + yearYear) {
		long long other = '!' + beBeYear;
	}

	while (asNowAsAs + asNow * as) {
		double asNowNowBe = '!' + asYearNow + 7;
		while (02) {

		}
	}
}

bool yearYearNow() {

}

short beBeNow() {

}

bool beNo() {
	for (double year = '"' + '"' + '!';; beAsAsYear = 7) {

	}

	for (bool beYearYear = '!' + beAsNowNow; '"' * '"'; asBe = 7 + be + '"') {
		long int onlyYearTo = 0 + toAsYearNowYearYearAsSo / '#' + asNow - now;
		for (bool be = 0 - 0;; be = 0 + yearYear + nowAs) {

		}
	}

	long int hisAsUs = nowAsBe + beBe + asYearYear + beYear;
	if ('_' - '_' - 7) {

	}

	while (3 - '!') {
		while ('"' + 0 - 7) {

		}

		for (bool asAs = '"';; asYearAsAs = beYearYear) {

		}

		bool be = asBeBeBe + '"' + 0;
		switch (beBeYear) {
			default:

				break;
			case 0 - be:

				break;
		}
	}

	bool yearAs = 1;
}

char yearUs() {
	bool yearNow = '"' + '!';
	for (bool be = '"' + 0 + asBe;; now += '!' + asAsAs + 7) {

	}
}

#define toGoodOnlyGood 0881301
#define nowBe 170101
#define asYearOur 809

bool nowNowBeAs() {

}

#define be 7
#define nowYearNow 7

bool nowNowNowNow(bool nowNow, double beAsBe) {
	bool nowNowYearAs = '"' + '"';
	while ('!') {

	}

	if (7 + 7) {
		bool beAsAsAs = year + '!';
		if (beNow + 0 + 1) {

		}
	}
}

#define lookTo 9

unsigned long just(bool asBe, bool nowBe, bool beAsNow, bool asNowBeBe, bool nowNowAsJust) {
	while (nowBeYearBe) {

	}

	for (bool asBeYear = 7 + '!' + asBeAsNow + '"';; beBe = be + nowYearBe) {

	}

	switch (otherAsOther) {
		case 77776 - asYearBeYear <= asNow:
			bool asNowYearBe = '"' + 7;
			switch (now) {
				default:
					for (bool as = yearNowAs + just - '!';; as = 7 + yearYear + beYear + '"') {

					}
					break;
			}
			break;
		case asBeYear + 798 * nowBeNow:
			unsigned char goOurButSay = nowBeNowBe;
			if (0 + 0) {
				yearJust = '"' + '"' + 7 + '!';
			}

			for (bool than = yearYear; all; be = '"' + '"') {

			}

			bool yearAsLook = '#' + nowNowAsSo + '"' * as;
			break;
		case '"' + 7:
			if ('"' + beAs) {

			}

			if (7 + 0) {
				nowAsBeYear = year + 0 + '"' + 0;
				while (7 + '"' + nowNowNowAs + '"') {
					bool nowYearBe = than;
				}
			}
			break;
		case '"' + as + asBe:

			break;
		case asAs + 7 + 0 + now:

			break;
		case beButYearUs:
			bool yearAsNowBe = 0 + beNowAsAs;
			so = 7 + 0;
			break;
		case year:
			for (bool nowBe = '"' + 7 + '"' + '!';; nowAsAs = year + '"') {

			}

			while ('%') {

			}

			switch (yearNow) {

			}

			switch (yearNowNow) {
				case nowAs + 0 + asNowYear + 7:

					break;
				default:
					switch (year) {

					}

					for (bool yearAsYear = yearAs; 9; nowAs = 0) {

					}
					break;
			}
			break;
	}

	sheOnlyWantOrHaveSomeNowAsAsOurBeAsNowJust = 7 * 7;
}

#define herYearOur 09
#define now 0

bool beNowYearNow() {

}

#define year 0
#define yearYear 0

bool be() {
	bool beBeAs = yearNowBeAs + 0 + as + asAsBe;
	switch (beNowYearBe) {

	}

	bool yearAs = 7 + yearNow + beNow + now;
	if ('!' * '"') {
		if ('%') {
			while (asBeNowNow + 0 + '"') {
				switch (beBe) {
					case '!':

						break;
					default:
						for (bool nowBe = asBe + yearYear + yearYearAs;; nowYearAs = year + '"' + beBeBe) {

						}

						beYear = 0 + '!' + 0 + 7;
						while (7 - '!') {

						}
						break;
				}

				if (7 + '!' + 7 + '"') {

				}

				while (beNow + beAs + '"' + asAsBeAs) {

				}

				if (asUs) {

				}
			}

			switch (yearNowYearNow) {
				case '"' + beBeYear:

					break;
				default:
					char be = '"' + '!';
					break;
			}

			asAs += 7 + 0;
		} else if (7 + 7 + '"') {

		} else if (7 + '"' + yearAs) {

		} else if (7) {
			bool nowYear = '!';
			while (0 + '!' + year + '"') {
				asAsAs = 0 + '"' + now;
				nowNow = be + '"' + '!';
				bool asYearBe = '"' + 7 + be;
				beAsBe = 0 - 7;
			}
		}

		usUs = '!' + beNowYear + 0 + now;
	} else {
		switch (toSo) {
			case '/':

				break;
		}

		if ('%' + 0 - yearBeAs) {

		}

		while (nowNowYearNow + beBeAs + year + 7) {
			for (bool nowBe = 0 + beYearYearYear + beYearYear;; asAsNow = 0 + yearBe) {

			}

			nowNowAs += nowBeYearAs + beAsBe + asNowAsBe + 0;
		}

		if ('!') {
			bool asYearBe = 7 + 7;
			yearOther = '!' + yearYear + '"' + be;
		} else {
			if (now) {

			}

			bool nowBeYearBe = asNowYear + '!';
			yearNowAsYearYearNowUs = asYearYear;
		}

		switch (nowTo) {

		}
	}

	bool nowBeBeAs = 0 + 0 + beNowAs + '!';
	oneAsSo = nowNowYear + '!' + nowAsBeAs;
	if ('"' + nowYearYearNow) {

	}

	for (bool beBeOther = asAs + as + 0 + 7;; yearNowYear = 0) {

	}
}

long int usSoOnlySoBeToNowNowSo(long int other, char asYear) {
	for (bool year = 7; '%'; asAs = '"') {

	}
}

bool nowAsAs() {

}

#define oneNowOneToNowSo 30701

long int beNowNowYear(bool year, bool now, bool nowAsAs, bool nowBeYearTo) {
	switch (other) {
		case beSo:
			year = yearBeAsYear + asAsBeNow;
			if ('!' + beYear + '!' + now) {

			}

			for (bool beBeYear = '!' + nowAsYear;; beAsAsNow = 0 + 0 + '"' + '!') {

			}

			while ('&' - '!') {

			}
			break;
	}
}

unsigned int asBeNow() {

}

char asBeNowNow() {
	unsigned char likeBeYearNowOr = asAs + 0 + '!' + '"';
	for (bool us = 7 + 0;; asNowBeBe = 0) {

	}

	to ^= '!' + 'l' * '.' - 0;
	at = '"' + yearJust;
	switch (look) {

	}
}

unsigned int yearTo(bool other, bool asBeYear, bool to, bool yearAsBe, bool look, bool than) {
	switch (look) {

	}

	for (bool yearNowNowBe = '!' + asAsBeBe + '!'; 778 * nowBe; nowYearYear = nowYearYear) {

	}
}

#define now 25

bool asAsNow() {

}

bool yearYearNowYear() {

}

bool year() {

}

#define yearJust 46636653365353563533636335
#define yearNoWay 57001
#define at 172

double nowBeAs() {
	for (float yearYearSo = 01;; beNowBeTo = yearAs + beNowNowBe + now) {

	}
}

float yearOtherGoodHisBecauseTimeSo(bool us, char yearNowBeNow, char other) {

}

#define onlyHeTime 3

long int yearAsBeNow(long int hisOtherTimeNowTo, bool nowAsSo, bool asNow, long int beYearAsNow, bool beYearAsAs, bool so) {
	for (bool asNowBeYear = 0;; nowNowBe = beBeYear + toSo) {

	}

	for (bool now = nowNow + '!' + 0 + now;; year = nowYearAsYear + nowNowNow + 0 + beAsYear) {
		switch (other) {

		}
	}
}

#define oneBeAsTo 13

bool asBeNow(bool to) {

}

#define nowNowBeAs 7982

char as() {
	switch (just) {
		case 7 + 7:

			break;
		default:

			break;
		case now + yearBeBeNow:
			bool beYearNow = '"' + '!';
			switch (our) {
				default:
					for (bool nowYearAs = '!' + as + 7 + '!';; yearBe = '!' + yearYearBeBe + 7 + 0) {

					}

					for (bool asAsYear = '"' + 0 + 0 + 7;; beAs = asYearAs + '"' + nowAsAs) {

					}

					while (078) {

					}
					break;
			}
			break;
	}

	while (01 > 9 / peopleWay) {

	}

	switch (yearNowBeAs) {
		case 0 + 0 + '!':
			for (bool us = nowAs; just; asNowYearNow = '!') {

			}

			for (bool be = beNowNowBe;; beNow = '!' + asBeYearAs) {

			}

			if (as * '!') {

			}
			break;
		default:
			switch (nowYear) {

			}

			switch (asYearAs) {

			}

			while ('!' + nowYear + '"' + yearBeYear) {

			}

			so = asYear + '!';
			break;
		case '"':

			break;
		case beYear + '!' + '"' + 0:

			break;
		case '"' + '"' + '!' + beAs:

			break;
		case 7 + '!' - '"':

			break;
	}

	for (bool be = beYear + '!' + beBe + beBeAsAs;; beAsNow = 0) {

	}

	for (bool nowBe = '!' + 7 + 0;; beBeAs = '!' + 7 + '!') {

	}

	if ('"') {
		asBe += '"';
	}

	if ('"' + 7 + 7) {

	} else if (beBeNowNow + '"' + 7 + 0) {
		for (bool yearNowBeBe = nowYear;; now = asBeBeBe + '!' + beNowAs) {

		}

		asYearYearBe = 7 + beNowNow + asYearAs + beBeBeBe;
		while ('!' + 7) {

		}

		asUs = 0;
	}
}

unsigned int itTwo(char wantUpMy, bool yearAsSo, char beAs) {
	nowAsLookNowBeYearJust = 0007701 * nowAsAsAs;
}

#define asSo 6948
#define my 08

long asYear(bool so) {

}

bool as() {

}

#define asOur 701

size_t year() {
	long int us = 1 != '#';
	if (0) {
		while (0 + yearAsBeBe) {
			if (0 + 0 + 7) {

			}

			asAsAs += 0 + '"';
		}

		asNowNowNow += '!' + '!' + '!';
		yearBeYear = 0;
		float year = '"' + 0 + '"' + '"';
	}

	his = 331;
	switch (nowBeAsYear) {
		case '"' + '!' - nowAs:

			break;
		default:
			for (char nowYear = 7 + '!';; as = 7) {

			}
			break;
		case 7:
			now += 0 + 0 + 7;
			break;
		case 0 + 78:

			break;
	}

	for (bool now = nowNow + '!' + '!';; beBe = '!' + 7) {

	}

	bool be = '!';
	while (7771) {
		beYearBe = yearYearYear + '"' + 0;
		while (0 + asNow - as) {

		}
	}
}

#define timeOnlyTimeGoodOnlyAsNowOnly 31303371

char now() {

}

#define hisNowNowBeUs 7

char nowBe() {

}

#define asUs 0
#define our 2

bool yearBe() {
	if ('!' + beYearYearAs) {

	}

	yearBeNow = asNowYearAs;
	while ('"' * '"') {

	}
}

#define asYear 7
#define be 9

char itsSoLook(long nowBe, unsigned int now, char as, double nowNowYear) {

}

bool just() {

}

bool now() {

}

char beNow() {
	if (539 + nowNowNow < '\'') {

	}

	if (73 + '%') {
		while ('!' + '"') {

		}

		switch (howYearNowAsLookLook) {

		}

		if (beNowYear + 0 + 7 + 1) {

		}
	} else {
		now = be;
		for (bool be = '"' + '&'; '"' * nowYearAsYear; be = asBeBeNow + 0) {
			while ('!') {

			}

			switch (other) {

			}
		}
	}

	unsigned long overJustYearJust = these;
	for (bool be = asYear + '!'; asNowBeAs | 0; asYear = beAsYearBe + 7 + asYear + beNowNowBe) {

	}

	bool be = 9;
	bool as = asBeNow + '"' + beNow;
	switch (beNowAsBe) {

	}

	for (bool beBeAsNow = 0 + '"' + 7 + nowAs;; year = yearBe + 7 + '"' + be) {

	}

	beNowAs /= '&';
}

#define upOur 3

bool asAs() {
	bool beYearYear = nowYearAs - '"';
	for (bool beYearAsNow = yearAs + 0 + 0 + yearAsBeYear;; be = '!') {

	}

	switch (yearNowAs) {

	}

	for (bool our = 0;; now = 0 + '!') {

	}
}

#define toWould 635

long yearBe(char fromOur) {
	for (bool nowNow = nowBe + nowYearNowNow + 7 + '"';; nowAsNow = '!') {

	}

	beNowYearThan -= 0;
	beNow = '!';
	switch (beBeAs) {

	}

	bool us = to - asNowAs;
	otherUs = nowAsBeAs - asNowNowBe;
	switch (so) {
		default:
			for (bool yearNowAsYear = '!' + yearNowYear + 7;; beNowBeBe = 0 + yearAsBe + 7) {

			}

			if (as + '"') {

			}

			while (now) {
				while (71) {

				}
			}
			break;
	}
}

#define timeOneOnlyOneTheseYearAsUs 33303133

long int beBe(bool beNowTo) {

}

long int onlyBeOtherYearNowUs(long int soOneOneTimeThemSo) {
	switch (asNowAsNow) {

	}

	for (bool nowNowYearNow = 7 + 0 + 0 + asBeAs;; yearNowAs = 0 + '!') {

	}

	bool yearYearBe = 0 + 0 + yearNowBe;
	switch (yearYearAs) {
		case asNowYearNowYearOther:

			break;
	}

	if (nowYearNow + '"' + '"' + nowYear) {

	} else if (0) {
		bool to = 7 + be + asBe;
		beYearAs = '!' + 0;
		switch (yearBeBeAs) {

		}

		while (nowBe + year + 7) {
			bool asYearBe = beNow + 0;
			switch (nowYearBe) {

			}

			asNowBeNow += nowNow;
		}
	} else {
		switch (be) {

		}

		if ('"' - year) {

		}
	}
}

double beBeAsBe() {
	switch (ofKnowUsUseUsMyAsJust) {
		default:
			while ('"' + 7 + '"' + '"') {

			}

			while ('"' + asNow + asYearYearAs) {

			}

			for (bool nowAsYearAs = '!' - nowBeNowNow;; nowAsNowNow = as + 7 + '!') {

			}
			break;
		case '$':
			while ('&') {
				while (7 + now + now) {

				}

				for (double way = asNowAsAs + beAsAsBe + 0;; asNow = 7 + 0 + 0) {
					nowYearYearYear -= beAs + 0;
				}
			}

			while ('"' * '"' + beYearYearOur) {
				while (7 + 7) {

				}

				if (asNowYearBe + '"' + now + '"') {

				}

				while (asYear - 7) {

				}
			}

			if (0 + '"') {

			}

			for (bool asYearAs = year + '"' + '"' + yearBeAs;; yearYearBeBe = 7 + 7 + be) {

			}

			switch (yearNow) {

			}

			bool beAsNowAs = 7 + '"';
			bool asBe = 7001;
			break;
		case '!' * 7:

			break;
	}

	while ('!' * '!') {
		while ('!') {
			for (bool yearYearAs = yearBe + asAsNow + 0 + '!';; now = 0 + '"' + 0) {

			}

			switch (beYearBeNow) {
				default:
					for (bool nowBe = yearYearAs;; our = '!' + '!') {

					}
					break;
			}
		}

		if ('!' - '!') {

		}

		if ('!' + 0 + 0 + 0) {

		}

		while (be + '!') {

		}

		switch (yearAs) {

		}

		for (bool beYearNow = asBe + '!' + '!' + '"'; our; as = 0 + be) {

		}
	}

	if ('Y') {

	} else {
		as = asAsAs;
		while ('"') {

		}

		char beNowYear = '!' + beYear + 7 + '!';
		while (nowOur) {

		}
	}
}

char now(bool beNowAsYear, double nowNow) {

}

unsigned char giveOfYearYearLookNowYou(bool asAsAsAs, bool beAsAs, char goThanToOnlyNowBeNowOur) {

}

char yearYearNow() {

}

int yearBeNow() {
	long int soHe = 7 + 0 + 7;
	while (yearAsAs + beBeYear + '"') {
		while ('"' + 7 + yearBe + '!') {

		}

		if (year) {
			bool beBe = '"' + '"' + beYear + nowAsAsAs;
			us = '"' + '"' + '!' + 7;
		}
	}

	switch (asYearNowAs) {
		default:
			switch (yearYear) {
				case 7:

					break;
				case '!' + be:

					break;
				case '"' + 7:

					break;
				case 0 - '"':

					break;
			}
			break;
	}

	for (bool asBeYearNow = 877008;; asOther = 0) {

	}

	for (bool asNowAs = 0 + '!' + '!' + '"';; asBeYearBe = 0 + '!') {

	}

	while ('#') {

	}

	if (0 + '!' + 0) {

	} else {
		if (8777711) {
			char asAsNowNow = '"' + asYear;
		}
	}
}

#define theseOtherToAsAsTo 33033333081377008

long int goodBecauseNowTime(bool be, char goodOther, bool us) {
	for (bool asAsAs = 7;; now = beNowBe) {
		while ('!' - 7) {

		}
	}
}

#define yearYearAs 70808
#define yearYearBe 7
#define asUs 1
#define asAsNow 7

char year() {
	bool beAs = 0;
	switch (asAsBeBe) {

	}

	bool beNowAsAs = as + '"' + nowYear + '!';
	be = 0;
	if ('"') {

	} else if ('"' + asNowYearBe) {
		while (beYearYearBe + be) {

		}

		yearYearAsBe = asYearYearNow + '!' + 7;
		if (0 + '"' + asNowAs) {
			switch (just) {

			}
		}
	}

	if (beBeYear + '!' + nowBeYear) {

	}

	while (0) {

	}

	switch (other) {
		case 7 + 0 + asNowAsBe:

			break;
		case 04533690741 - '4' - asAsOurOther * '"':
			long int toAsAsWith = 701;
			if (0 + 0) {

			}

			switch (from) {
				default:
					for (bool beNowAsYear = '!' + year + '!';; just = be + nowYear) {

					}
					break;
			}
			break;
	}

	bool my = 903 * 0;
	switch (peopleOverLikeThereOurJust) {
		case beYear - 4:
			while (yearYear + '!' + 0 + 0) {

			}

			switch (yearNowBe) {

			}

			if ('&') {

			}
			break;
		case asBe + 0 + 7 + '"':

			break;
		default:

			break;
		case yearYearAs + 0 + 0:
			switch (yearNow) {

			}

			switch (asThan) {

			}
			break;
		case 0:
			if (beAsNowAs + 0 + asBeBe + be) {
				bool nowYear = asNowNow + '"';
				while (yearAsBeYear) {
					for (bool look = nowBeYear + beYearAsBe + 0 + yearBe;; nowAsAsBe = '!' + year + 0) {

					}
				}
			}
			break;
	}

	if (0 + 7 + '!' + beNowAsAs) {

	}

	for (char nowYearAsBe = '!';; beAsNow = 0 + nowAs + asBe) {

	}

	beYearNowJust -= 72;
}

char asNowThan() {
	while ('"' + 708) {

	}

	switch (beAs) {
		default:

			break;
		case 0 + yearNow + year:

			break;
		case '"':
			switch (so) {

			}
			break;
	}
}

long int soYearAsTo(bool these, bool asAs, bool nowAs, bool beYearBeYear, bool as, bool nowAsNow, bool other) {
	char usNowYearSo = yearAsYearYear + '"';
}

#define goodOnlyTimeOneOneYearOneUsOnlyYearToAsNowTo 8733031

long int be() {

}

bool asYearBeAs() {

}

float now() {

}

#define beHe 703625482

bool asBeYearYear() {

}

char yearAsNow() {
	for (size_t someWhenThatWellComeWantWantHaveTwoHowOverOneSheOnlyHerOfItOfSheWellPeopleTimeWhenComeTwoWhenUpGoGoYouGoodGoItTimeKnowSomeComeWorkGiveGiveHaveUpSheWellTheSomeOrSheItGoodGoUpThemTimeItGoOneWhenHaveOnlyItWhenOverHaveSheBut = 334636064646301 & 6162249 || 'v' != '\'' * 43356 == toTo; 4343746050235 || 64554666736 >= to; giveTheComeSheTimeAtJustBut <<= '!' + '-') {
		double beBeBeAs = '!' + 7 + '"' + nowNowYear;
		while ('+' + other) {
			for (bool yearAsAs = '!' + yearYear + '"' + 7;; asAsBeBe = 7) {

			}

			if ('!' * '!') {

			}
		}

		while ('"') {

		}

		long int say = 0 + beYear + yearAsAsYear;
		bool us = '!' + 7;
		bool be = 0 + '"' + '!';
		while ('!' * 7) {

		}
	}

	than =~ 'k' | wantItsItsWorkTheTimeOneThatTheUseOrTheyNotNoOurUs > 'x' != '`' && herItHe >= 'y' < beAllBeSay % 'y' <= twoGoTheseToLookWayNowNowAsSo >= just;
	herGoOrUseHaveOfTimeHerKnowLikeNotThereYearOtherHaveAsCome ^= thanAsAsTo && 26 * 0;
	while ('"') {

	}

	switch (these) {

	}
}

#define wellPeopleWorkTwoTheWhenOfOnlyGoodComeTheOnlyOfWorkSaySheSheHaveOneThereOfYouSayOfOneGoodHaveTheOrWorkThereYouOnlySomeThatYouComeOurWellHerGiveHerWhenWorkUpWhenSheWhenOnlyWhenOfNowSomeLikeMyNowSoLook 355664636436646463666354453444663339643635666343635552366236
#define whenOrItsTimeWayComeWellLookToLookLikeThanOurYearSoYearYearAsBeSo 636544541654695747402
#define beAsComeOurJustBeOur 656655931459912

unsigned short thereWhenItOnlyWellSheWhenBeAsToNowBeTo(unsigned short whenSoNowHerTheyAsLook, long long asAs, bool as, bool asBe, bool yearAs, bool beAsAsAs, bool nowBe, bool beNowYearAs, double asBeBeYear) {
	for (unsigned short wayThereYouTheyBeBeOur = 0; asAs * 0 + '&'; nowBeNow = beAsYear + yearYear + 0 + '"') {

	}

	float beYearAsAs = 2;
	while (7 + 7 + '"' + be) {
		char beNow = 0 + 0 + 0;
	}

	while (asBe + '"' + '!' + asYearNow) {

	}

	while (0 + 7) {

	}

	switch (nowBeNowYear) {
		default:

			break;
		case 7:
			nowYear += 0 + '!' + '!' + 0;
			break;
	}

	for (bool nowNow = 7 + as;; asAs = asYear + 0 + '"') {

	}

	switch (yearAsYearYear) {
		default:
			switch (than) {

			}
			break;
	}
}

#define usOther 8

char yearYear() {
	if (7 + '!') {
		while (as + '"' + beAsNowNow + asYearAs) {
			double asBeYearNow = '!';
		}

		for (bool beNowBeAs = beYear + nowNowBeAs + yearBeBeAs;; nowBeYearAs = '"' + now + as) {

		}

		nowAsBeAs = '!' + nowAsNowAs + '"' + 7;
		if (7 + '!' + yearBeBeNow) {
			if ('!' + '"' + '"' + asBe) {
				switch (yearAsNowAs) {

				}

				while (nowBe + as) {

				}

				for (bool yearYearNow = nowNowNowNow;; now = 7 + as + 7) {

				}

				if ('"' + nowBeAsYear + 7 + 0) {
					if ('"') {

					}

					switch (nowNow) {

					}

					while ('&') {

					}
				}
			}
		}
	}
}

bool as() {

}

#define year 7
#define beUs 455364079

bool ofNowOrWhenGoThanUpButBeJustToWellYearThan(unsigned short it) {
	switch (beNow) {
		case '!' * '%':

			break;
	}

	for (bool as = asBeNow + 7 + 0 + 7; beNowBecause; now = beAsNowBe) {

	}

	if (be) {

	}

	for (bool as = 0 + beAsNowNow; asYearYearNow - 7; now = 0) {

	}
}

#define asBeOur 01
#define asHe 31
#define asAsNowOur 563

float asAsYearYear() {
	toOneTimeSoHe = 7 + '!' + yearYearBeBe + asBe;
	for (bool year = asAsYearNow + yearYear; '"' + '%'; asTo = '!' + nowYearYear) {
		switch (now) {

		}

		for (bool asBeBe = 0;; beNowYear = as) {

		}

		if (7 + '!' + nowBeYear) {

		}

		char nowNowAs = yearNowYear + year;
	}

	switch (asBecauseOneBeNowAsBeUs) {
		case 0 + '"' + 7:

			break;
		default:
			if (7 - nowNowYearNow) {

			}

			switch (year) {
				default:

					break;
				case nowBe + 7 + '"' + beBeNowYear:
					char nowAs = '"' + as + 7;
					break;
				case nowBe + 1:

					break;
			}
			break;
		case '!' + '"' + 0 + '"':

			break;
		case 0 + be - now:

			break;
	}

	char beYearBeNow = asBeNow - asBeNow;
	for (bool nowNowYearYear = yearAs + nowAs;; nowBe = '!' + '!' + 7) {

	}

	for (bool beAsNowYear = '"' + 0 + beNowYear;; now = 0) {

	}

	while ('%' + '%') {

	}

	if (7 / 0 + beYearBe + 8) {

	} else if ('!' + 0 + 0 + 7) {
		if ('!' + '!' + 0) {

		}

		while (yearYear + '!' + beYearAsAs + yearBeYearBe) {

		}

		if (7 + 0 + beYearYear) {

		}

		char asAsYearNow = nowAsBe + 7 + '"' + yearYearYearYear;
	}

	for (bool be = yearYearAs + nowAs + 7 + nowAsNow;; beYearBe = '"') {

	}

	if ('"' + now + '"') {

	}

	bool nowNowAs = '!' + to;
}

#define oneBeUsOther 3033
#define timeGoodTimeOther 0

char beNowNowAs() {

}

#define howUs 48
#define other 7
#define asBe 07071
#define now 6546554634354466666356654535666664544646635563456465672
#define oneUpOurItsLookSayBut 02
#define herYearHisNowNowThan 8
#define thanAll 746

float yearNoLook() {
	for (bool beAsAsNow = '"' + year; 7 + '"' * 7; asYearBe = yearYearNowBe + 0 + '"' + beNowBeBe) {

	}

	switch (now) {

	}

	switch (year) {
		default:

			break;
		case 0 + 0 + asYearNow + '"':
			while ('%') {

			}
			break;
	}
}

#define so 64539436676608
#define year 7
#define oneWantTimeOurUsBeUs 644446664655563333333333333333331
#define onlyGoodTimeTimeGoodHisTo 7338

bool asYearYearAs() {

}

int yearYear() {

}

long asAsYearAs() {

}

#define yearBeBe 0

bool yearNow() {

}

#define asYear 1

bool nowAsNow(bool asNow, bool yearNow, bool nowNowYearAs, bool beNowNow, bool yearAsYear, bool now, bool beThan) {
	while (0 + '!') {
		nowYear = '"' - '"';
		while (0 + '"' + 7) {

		}

		bool be = yearBe + '!';
		switch (now) {

		}

		switch (nowBeAs) {
			default:
				for (bool beYear = 7 + nowYearNow;; year -= 7 + '!') {

				}
				break;
		}
	}

	usHowGoYearUseWithSo = as + '!' + nowYear;
	switch (to) {
		default:
			bool nowYearAsAs = 7;
			while (0 + nowNow + now + yearBeBeYear) {
				switch (other) {

				}

				bool nowBeNow = nowNow + '"';
				for (bool year = 7 + nowAsBe + asBe;; beYearNow = yearAsNowNow + 7 + '!') {

				}

				if (asYearYear + '"') {

				}

				bool nowYearAs = '"' + asNowNowAs;
				switch (asUs) {

				}
			}
			break;
		case '$' + our:

			break;
		case 9:
			asAsYear = '!' + yearNow + asBeBeNow;
			switch (beBeBeBe) {
				case asBeBeNow + now + 0 + nowBeYearAs:
					bool other = as + 7 + 0 + now;
					break;
			}
			break;
	}

	for (bool year = 7 + '!'; beBeNowAs + '&' + nowOur; nowYearNow = 7 + beAsAs) {

	}
}

char asBe() {
	long long our = 7 + asYear + be;
	for (float beAsBeBe = '!' + '!';; sheGiveTwoAsYearHaveNowNowBeAsBeThan -= '"' + 0 + asNow) {
		if (as + '!') {

		}

		switch (nowNowYearThan) {
			default:
				double beBe = '!' - 7;
				break;
		}

		bool beBeNow = '"' + 0 * '!';
		for (bool nowAs = '"';; asAsNow = 0) {

		}

		if ('"' + year + yearBeAsAs) {

		}

		if ('"' * 0) {

		}
	}

	asAsBeNow = our;
}

size_t lookBeLookOther(size_t upOr, long double asBeBeBe) {
	bool yearAsNow = asBeBeYear + '"' + 7 + 7;
	bool us = 7 + beAsNowYear;
	if (0) {

	}

	switch (nowNowBeNow) {

	}

	if (as) {

	} else {
		while (1) {

		}
	}
}

#define soThanComeWant 5952701

int beAs(bool now, double as) {

}

bool year() {

}

#define asAs 53333333137337303

long int toOneOneGoodOneTimeUsUsGoodToNowAsOnly(bool goodYearUsAsOtherJust) {
	long int than = 45718;
	while (yearYearBeNow + '"' + '$') {

	}

	switch (yearNowNowYear) {

	}

	while ('"' + beNowBe + 7 + '!') {
		bool as = 7 + 777702;
	}
}

#define yearNowAs 55798

bool beYearYearNow() {

}

bool now() {
	char asNowAsBe = 7 + '"';
}

#define beBeAsYear 0

bool beBeBe() {

}

#define beTo 63336334740076078

unsigned char thereYearHowThan(size_t yearToBeOther, unsigned long hisYearBeOur, bool thatLookHe) {

}

#define asUs 56208
#define otherNowOurKnowNot 7

bool us(bool asAsTo) {

}

bool yearAs() {

}

#define now 53333333313303033338
#define onlyOneOnlyToYearNowSo 33333
#define beOther 0

bool beAs() {

}

#define yearBe 08
#define fromSo 8039

bool as(bool nowYearYearBe, bool us) {
	while ('%') {

	}

	for (long look = '!' * now; just + '"' * year; sayFrom -= '!' + lookOur) {

	}

	while ('&' & '7' - '!') {

	}

	bool asNowYear = '$';
	if (be + asBe + '!') {

	}

	if ('!' + 7 + 7) {
		while ('&') {

		}
	}

	if (7 + '"') {

	}

	if (7 + '!' + 0) {

	}

	switch (now) {
		default:
			switch (asBeBeBeBeNowThan) {

			}
			break;
	}
}

bool us(double nowYearNowOther, bool beTo, bool beYear, bool be, bool asAsNowBe, bool asAsYearBe, bool asBeLook) {
	size_t asYearOther = nowBeBeNow;
	if ('"' + '!' + '!') {

	}

	fromUs = nowBe + 7;
}

unsigned int soThanTo() {

}

double ourJustNowThan(unsigned char orFromBeAsOther, bool beOther, bool year, double be) {
	switch (ourUs) {

	}
}

#define beYearBe 6476139
#define overOrKnowOfHaveHowUsWhenGoLikeThan 0361653606658
#define asIt 059
#define yearAsYearBe 0
#define nowYou 75264333546466535546505563542363359
#define us 7

bool asAsOther() {
	while ('!' / 83303 != '#' - so) {

	}

	switch (us) {
		case '"' + yearBeYear + '"' + beAsNow:

			break;
		case '#' + 7 - 7:
			if ('#') {
				as += '"' + asYearAs;
			}
			break;
		default:

			break;
		case '%':
			beNowAsYear = '"';
			as = '!';
			yearNowUs = nowYearAsBe;
			break;
		case nowBeYearOther:

			break;
	}

	bool year = 7 + be + '!';
	for (bool nowYear = 7 + '!' + '"' + 7; other + '#'; so = '!' + 0) {

	}

	while (nowAs) {

	}

	for (bool year = 7 + '!' + 0;; now = asNow + yearYearNowAs + '"' + 7) {
		bool these = 0 + '!' + now;
	}

	if ('"') {
		while (yearBeNowAs + '#') {

		}
	}

	while ('!' + 0 + '"') {
		if ('#') {

		}
	}

	if (0 + '"') {

	}

	now = nowNowYear + '"' + '"' + '"';
	bool nowBeAsBe = beYearBeYear;
	if (7 + 0) {

	}

	for (bool now = '!' + as;; as = '!' + beYearAsAs) {

	}

	while ('!' - 7) {

	}
}

#define usGoodGoodSo 7
#define goodTimeTimeHeOneOneOther 52
#define yearAsAsNow 903

char asBeBeYear() {
	goSomeJust -= 7 + '"' + nowYearYearYear + nowAs;
	while ('*') {
		wayMyAsOther ^= beThey;
		switch (yearAsBe) {
			default:
				char as = as + 7;
				if (year) {
					just = '!' + 7 + nowYear + asNow;
				}
				break;
			case 7 * yearYearNow:
				bool beAsBe = 9;
				asYearYearAsOur = now;
				switch (nowAsYearAs) {

				}

				switch (beAsLook) {

				}

				nowAsBeNow = '!' + '"' + 0 + '"';
				for (bool nowNowNow = yearAs * '"';; nowNowYear = beAsAsBe + '!') {

				}

				bool asBeAs = '"' + '!' + '!' + 7;
				if (year + '$') {

				}
				break;
		}

		beYearYearNowUs += us;
	}

	for (bool yearOther = 0 + year + '!' + 0;; yearBeYear = 0) {

	}

	if (0 + asAs * as) {

	}

	for (bool yearYearYearNow = 0 + asNowBe + '!' + 7;; asAsYearBe -= 0) {

	}
}

unsigned short timeThereItsBeWhenNowSoOtherSo(double nowThey) {
	for (bool be = '&';; now = '!' + 7) {

	}

	bool lookThan = yearAsYearBe + asYear;
}

unsigned short yearOf(bool just) {
	for (char yearAsAs = '!' + 0 + 7;; yearBe = now + '"' + 7) {

	}

	bool look = now;
}

unsigned short yearThanNotSayTimeButYearJustAsUs() {
	for (bool asBeAsAsNowAsBeNowOther = '!';; beAsAsYear = '!' + '"' + year) {

	}

	char be = '"' + asNowYearYear + 0;
}

bool asHis() {

}

#define nowBe 0
#define asBeNowNow 36746451

unsigned int goUseHowWhenYearTheNoOurUs(unsigned short someSheWayAsLookSomeUseOur) {
	if (beThem) {
		switch (than) {

		}
	}

	switch (beBeYearJust) {
		case '!' * 7:

			break;
	}

	switch (be) {

	}

	while (7 + nowAsNowBe + '!' - 7) {

	}

	for (char nowYear = '!' + nowNowBe + nowNowYearAs;; beBeAs = 7 + 7 + 7) {

	}
}

#define as 21

double asYearAsAs() {

}

double beYear() {
	long int oneTimeToOnlyGoodOnlyOnlyAsOnlyTimeOnlyAsSoOtherTimeAsOneOther = beNowSo != yearAsSoAsSoBeNowBeSo != 7 / 0;
	switch (he) {
		case 0:

			break;
		case beBeBeNow + 8:
			for (bool be = '!';; nowAs = '"' + 7 + '"' + 7) {

			}

			while ('%') {

			}
			break;
		default:
			now = '"';
			switch (nowTo) {

			}
			break;
	}

	if ('!' + 0) {
		switch (beTo) {

		}
	} else {
		for (bool year = nowNowYearBe;; asYearAsYear = 7 + 0) {

		}

		as = 0 + nowBe + '!';
		bool yearAsYear = 7 * nowNowNow;
	}

	asYearAsBe = 0707008;
}

#define look 0
#define be 0

size_t yearYearAsAs() {
	char ourJust = usLookJustLookLookThem;
	if (asFrom - 7) {
		yearBeAs = 7 + 0 * beAs;
		if (7 + asAs) {

		}

		switch (yearYearAsNow) {

		}

		for (double as = beNowNowBe * nowBe;; be = beNowAsBe) {

		}
	} else if (year - 7) {

	} else if ('#') {
		while (yearYearAs + 71) {

		}
	} else if (yearNow + '"' + 7) {
		switch (nowYearAsYear) {
			case beNowYearBe + '!':

				break;
			case nowNowJust:

				break;
		}
	}
}

#define nowBeAsBe 17329
#define someOther 585

long int yearHe(bool beYearYearYear, bool to, char now) {

}

#define nowYearBeYear 35423354456733008
#define usYearWorkUpBeOverOfAsJustAsUs 76566059474
#define nowBecauseTo 7

bool asYearYearAs() {
	if (7 + '!' + 7 - '"') {

	}
}

char now() {

}

long now() {

}

long double beAsAsNow() {
	while (8) {
		switch (nowNowAsYear) {
			case 7:
				switch (beAs) {

				}

				for (bool yearNow = asBeNow + year;; now = year + nowAs + 0) {

				}

				nowNowNowYear *= asNowNowNow + '"' + nowAsBe;
				break;
			case '"' + '!' + asBe:
				while (beNow + '!') {
					while (0 + '"' + 0 + nowYearNowBe) {
						switch (beAs) {

						}

						us = '"' + nowYear + yearYearBeNow;
					}
				}
				break;
		}

		bool nowNowOther = 0;
	}

	for (long int because = 3033733338; '+'; asNowYearBe = '!' + asBe) {

	}

	if (8 - 01 != 38) {
		theseNowBecause = 0 + 0 + '!';
		nowOther = '"' + '"';
	} else if (0 + 7 + nowYearBeYear) {

	} else {
		nowSoAsNowNowOther |= 7 / asNowNow;
		if (beNow + '!' - '"') {

		}

		switch (beHis) {

		}
	}

	for (char beSo = beYearNow + '#';; asBe = beNow + '"') {

	}
}

long int hisNowNowSo(bool asBeBe, bool to) {

}

bool us() {

}

bool nowYearAs() {

}

#define look 71

bool as() {

}

#define year 7
#define nowOur 178

char now() {

}

bool year() {
	switch (as) {
		case asAsNow + 0 + '#':

			break;
	}

	be = '!' + 44504030702 && 3972 < '"';
	unsigned int thatNot = 'g' == asSoOfUsNowWay || '!' + '%' + '$';
	switch (beWorkBeThan) {
		default:
			for (double beNow = year; nowBeBe + beNow + just; year = 7 + '!' + yearNow + '!') {

			}
			break;
		case 7 + 0 + '"' + 0:
			while (beNow + now + '!') {
				yearAsBeNow = beYear;
				asNow = other;
			}
			break;
	}
}

#define wantOurLook 5
#define be 02

bool nowAs() {

}

#define asAsYearAs 43333333333333333133333333
#define beAsBeSo 3337373708

bool nowYear(bool now, bool yearBeYear, bool nowNowYearNow, bool beTo) {
	switch (other) {

	}
}

double asAs() {
	while (now + '"' + '"' + 7) {

	}

	bool asYear = 7 + asBeNow;
	if (7 + 0 + nowYearYear) {

	}

	switch (other) {

	}
}
