#define timeOnlyGoodGoodGoodTimeToOnlyGoodTheseToThese 33333333838

long int beBut(char nowBe, unsigned short myNowJust) {

}

#define nowBeNow 

unsigned short wantOfYouSomeHaveToWellItHowOrWorkWouldItGoThatWhenLikeGoodThereAsGoodGoSoYouHe(unsigned int ofHerUpHowWithUs, short oneWantGoKnowHeWouldYearHow, bool asOrHerTheseAsYearNowBeBeNowBeYearTo, long double ourAsYearSo, bool beAsNow, bool other, bool yearOur) {
	beThan = beAsAsAs + nowAsBeBe + '"' + asBeNow;
	if ('"' - 7) {

	}

	if ('"' + 0 + asYearYear + '!') {

	} else {
		bool yearYear = than;
	}

	beAs = asBeAs;
	double be = 7;
	yearNow = 7 + as;
	while (7 + 7) {

	}

	bool year = '"' + '!' + '"';
	switch (nowNowBe) {
		case '"':
			while (0 + '!' + yearNowBe + yearAsNow) {

			}

			for (bool nowBeBeYear = '"' + '"' + beAs + '"';; nowBe = beYearAsAs) {

			}

			nowNowNow = '"' + beYear + beYear;
			if (yearYear + 7) {

			}

			switch (yearAsBe) {
				default:

					break;
				case yearYearAsAs - 0:

					break;
			}
			break;
	}
}

short theYouSheHaveTimeWhenOrOrItsWellTheOfYouHaveGoodOrTimeSoWellSoUseYouAsSomeUpSomeLikeWellOnlyComeUpSayWellWhenComeAsGiveComeUseOfOneWith(unsigned short upUsBeWayNoThan, char asBe, bool beNowNowNow, unsigned int beAsTo, long long herBeLook) {
	switch (whenTheYearThatItsWhenWouldAsOur) {
		case '-' * 0 + 72:
			for (bool nowNowNowBe = 8 % '"' + 009;; nowAsNow = 7 + beBeYearNow) {

			}
			break;
		case '"' + yearYearBeYearYearOur:
			if (0 + nowAsNowAs + 0) {

			}

			if ('+' + 8) {

			}
			break;
		case as:
			if ('!') {
				for (bool beYear = '"' + as + 7; nowBeNowAs + '!' + 7 * '!'; as = be + 0) {

				}
			}

			while (0 + 7) {

			}

			for (bool asNow = year + now;; other = 0 + '!') {

			}
			break;
		case beNowNow + beNow - '"':
			for (bool nowBe = '!' + '"' + asNowYear;; beNowNow = '"' + '"' + '"' + '!') {
				bool as = now + 0;
				while (7 * yearBeAs) {

				}
			}
			break;
		case other:

			break;
		default:
			nowBe = '"';
			while (0 + year) {
				switch (beYearAsAs) {

				}

				yearNowBe = beNow + '!' + '"';
				for (bool beNow = '"' + 0 + nowYear;; year = 7 + 7) {

				}

				while (7 + '!' + '"') {

				}

				if (beBeNow + yearNowYear - 0) {

				}
			}
			break;
	}

	if (as + 7 + nowNow + 7) {

	} else {
		switch (yearBe) {
			default:

				break;
			case beBe + 0:
				if ('!' + beAsYearNow + '!') {
					while ('!' + as + beYearBe + as) {

					}

					switch (than) {

					}
				}
				break;
		}
	}

	while ('!' + 0) {

	}

	bool nowYear = '"' + as;
	switch (be) {

	}

	switch (nowAsBeYear) {

	}

	switch (our) {

	}
}

size_t someTwoComeSayWay(short other, bool beAs, bool beYearAsYear, bool beYouLook) {
	if ('!') {
		while (yearBeAs + beYearAs + now) {
			while (7 + 0) {

			}

			switch (beAs) {

			}

			switch (asNowAsLook) {

			}
		}
	}

	for (double nowAsThan = asButThan - asAs + '"' - '"'; '&'; yearAsAs = '"' + nowAs + 7 + 7) {

	}
}

long int yearYearTo(double yearWantYearThanAsNowThan, bool us, char nowYearYearAs, bool year, bool us) {
	if (59) {

	}

	if ('$' && '"' != '!') {

	} else if (7 + 7) {

	} else {
		switch (be) {

		}

		if ('!') {

		}

		bool nowBeNowYear = beBe + nowYearNowYear;
		while (nowNow) {
			if (asYearAsYear + '"') {

			}

			bool than = '"' + nowBeBe;
		}
	}

	switch (year) {
		default:
			while ('!' - '!') {

			}
			break;
	}

	switch (yearUs) {

	}

	double asAs = 7;
	for (bool beAs = 7;; nowNowAs = asNowNow + 0) {

	}

	switch (yearYearYear) {
		case '"' + year + 0:

			break;
		case asAsBe + 7 + 7:

			break;
		default:
			for (bool beYear = '!';; beAsBeYear = nowBe + 7) {

			}

			for (bool beBeAs = 0 + nowAsAs * 7;; beNow = 0 + '!') {

			}
			break;
	}
}

#define toBeNowBeYearLook 446327375735897017779
#define itWellGoGoUpSomeGoodHowSomeWantThatNowOrUseWorkAsThereGoGoComeWorkSayTheComeNowAsThatKnowHeGive 55365744605

unsigned char toToJust(unsigned int overSoToNotThan, long nowYearBeNow, bool as, long oneBeJust) {

}

bool yearAs() {

}

bool knowNo(bool yearYearYear, bool yearAs, double as) {
	bool nowNow = year + 7 * 0;
	while (year + yearNowAs + 7 + 7) {
		while ('!' * 7) {

		}
	}
}

bool yearUse() {
	bool as = nowBeLook;
	size_t youBeBeThanJust = '!' + asBeAs + 7 + '"';
	size_t thatItsNoBeNowLook = look;
	while ('%') {

	}

	switch (yearThan) {
		case 9:

			break;
	}

	while ('!' + 0) {

	}

	switch (now) {

	}

	switch (other) {

	}
}

#define soYearThan 432

unsigned int allFromBeKnow(double justBeYearUs, unsigned char wouldTo, bool nowYearAsAs, bool beLook) {

}

bool year() {
	switch (because) {

	}

	switch (asNowAs) {
		default:

			break;
		case 0 + '!' + yearAsBe:

			break;
		case '"' + '!' + 7:

			break;
		case '"' + '"':

			break;
		case '!':

			break;
		case '!' - asNow:

			break;
	}
}

double toYearUsNoToOther(bool yearNowYear, bool to) {
	while (goodOnlyHe / '7') {
		switch (usAsThese) {

		}

		soOnlyBeHeHisThese *= onlyNowOtherAsNowNowAsNowOther != 7 + nowBe - yearBeAs;
		if ('!' + 7 + 7 + asYear) {

		} else {
			switch (nowAsNow) {
				case '"' + nowBeNowYear:

					break;
				case be:

					break;
				default:

					break;
				case '#':

					break;
			}
		}

		now += '"';
	}

	other = '"' + 0 + nowAs;
	for (bool as = 7 + '"' + 0 + '!';; yearAs = 7) {

	}

	now = nowAsNowNowBeNowAsTo + 1;
}

#define these 7038

bool asAs() {

}

#define soHe 71
#define heAsOtherSoTo 0

long int to() {

}

bool beNowYear() {

}

#define beYearYear 77708
#define as 32
#define as 0

bool beYearAs() {

}

bool yearYear() {

}

#define year 7

bool be() {

}

#define just 4637034673067369
#define fromOnly 00009
#define upLookWantWould 335364062409

long double justAsNowYearOur() {

}

long as() {

}

char asYear() {

}

#define yearBeBe 7
#define nowAsNow 5

char asAsNowNow() {
	if (03383 != '_' != 8 + '7' + one) {
		while ('!' + '"' + 7) {
			switch (beBeAs) {
				default:

					break;
				case '%':

					break;
				case '%':

					break;
			}

			char yearBeNowYear = now;
			switch (yearHe) {
				case nowNowYear + '"' + 7:
					while (yearAs + 7 + '"' + asNow) {

					}

					nowBeBeBe += 7 + asYearYearYear;
					break;
			}

			bool nowBeAsBe = '"' + '!' + be;
			if ('%') {
				char year = '!' + '"' + 7 + nowYearAsAs;
			}
		}
	} else {
		for (float soAsNowUs = '#'; 0 + nowAsBe != yearNowBe; now = '!' + '"' + beYearAs) {
			bool nowAs = 0;
			if ('"' + '"' + 7) {

			}

			bool yearYearNowYear = 0;
			switch (now) {
				default:

					break;
				case beBe:
					switch (asAsYear) {

					}

					switch (asNow) {
						case 0 + '"' + 0 + 0:

							break;
						case nowSo:

							break;
					}
					break;
			}
		}

		if (7 + year + yearBeBe) {

		} else if (008 + 7 - '"') {

		} else {
			for (bool year = yearBeAs + 7 + asYearYear;; year = be + '"' + 0 + '"') {
				switch (beBe) {
					case '"' + 0 + yearAsYear + '!':

						break;
					case '#':

						break;
				}

				if ('#') {

				}

				nowBe = asBeNow;
				while ('"' + 7) {
					switch (yearAsYear) {

					}

					if ('!' - beBeYearNow) {

					}
				}
			}
		}

		bool nowAsBeBe = yearYear + 0 + '!';
		switch (because) {
			case '!' + '!' - 0:

				break;
			default:

				break;
			case 7:

				break;
			case 7 + '!':

				break;
			case 0:

				break;
			case '!' + '"':

				break;
			case '!' + '"':

				break;
			case '"' + 0 + '!':

				break;
			case '"' + '!' + yearNow:
				while (beNowYear + '"' + 7 + now) {
					while (nowNowYear + 7 + '"' + yearAsNowYear) {

					}

					nowNowNowBe = beAsNow + '!' + '"' + 0;
					yearYearAsYear += '"' + nowNow;
				}
				break;
		}
	}

	for (bool now = nowBeBeOther;; beNowYear += be + '"' + 7 + '!') {

	}

	onlyUsBeBecauseAsOther = '"' + be + '"' + asYearYear + 0701;
	for (bool yearNow = asNowBe + yearBeBeNow + asBeBeYear;; asAsNowYear = nowNowNowAs + 7 + nowBe + asBeYear) {

	}

	switch (yearNow) {
		case '!' + 0 + 7 + 0:

			break;
		default:
			bool yearAs = '!' + 0 + 0 + '"';
			for (bool nowNowAsAs = 7 + '!' + asNow + 7;; as = now + 7) {

			}

			if ('!' + '"') {
				bool now = asNowYear;
				if (so) {

				}
			}
			break;
	}
}

#define nowBeYearYear 0
#define beBeYear 4
#define beOther 4734671061

unsigned long onlyButWantSoSayNoAsLook(float beBeThere, bool asOur) {

}

#define nowHerNot 1138
#define sheAsItAtSoUs 73

bool myNowSo() {

}

#define year 7
#define yearBe 593331707778

char beOneSo() {

}

long int asHeOne(long int as, bool beOnlyBeNowToYearTo, char beNowNowYear) {
	switch (beBeAsBe) {
		default:
			for (bool nowNowAsBe = '"' + nowBeNowAs;; asYear = 0 + 0 + yearBe + 7) {

			}

			switch (yearYearNow) {

			}

			switch (nowBe) {
				default:

					break;
				case asYearBe:
					switch (asAsAs) {
						default:

							break;
						case now:

							break;
						case nowBeYear:

							break;
						case 7 - 7:

							break;
					}
					break;
			}
			break;
		case '!' - beYear:

			break;
	}

	while (8 - asAsNowNow) {
		switch (his) {

		}

		to = beNow + year;
		while (beNow + '"' + 7 + 7) {

		}

		bool beAsAsNow = 0 - 7;
		while (0 + nowAsYear) {

		}

		char nowAsBeAs = yearBeAs + '"' + '!';
	}

	if (0 + nowYearNow + 7) {
		if (0 + asYearYearBe + beYearBe) {

		}

		year = yearAsYear + 7 + '!' + yearYearNowAs;
		yearNowYearAs = yearNowBe + '"' + '"' + 0;
		year = '"' + yearBeYearNow + nowBeBeBe;
		switch (nowAs) {

		}

		bool asBeBe = asYearBe + 7 + year;
		char beNowNowAs = 7 + nowBeNowNow + '"';
	}
}

float to() {

}

long int oneOnlyNowGoodNowGoodUpThese() {

}

#define beYearNowThan 556080091

long long than() {
	while (7 + yearNowAs + 0) {

	}

	bool nowYearYear = '!' + yearYear + 7;
	switch (asAs) {
		default:
			just = asYear + '!' + year;
			break;
	}
}

#define nowBeBeBe 5541

long asYear() {

}

#define yearNowNow 7
#define as 2
#define be 7

char yearAs() {
	switch (someWantOnlyJustWorkOtherJust) {
		case '!' + '!' + asYearAs + '!':
			while (as + beAsYearNow) {

			}

			if (nowBe - nowBe) {

			}
			break;
		case yearThanThan:

			break;
	}

	if ('!' + beYearNowAs + '!' + '!') {
		while (now) {

		}

		bool yearAs = nowNow + beNow;
		switch (year) {
			default:

				break;
			case 0 + 7 + yearBe:
				if (0) {

				}

				switch (be) {

				}

				if (yearBeAs + '!' + '!' + '"') {

				}

				year = as + '"' + as;
				bool yearYear = nowAsAsYear;
				switch (nowAs) {

				}

				while (7701) {

				}
				break;
		}

		for (bool as = asYearYear + nowAsNowNow; nowAsAsBe - 0; asNowBeYear = asYearAs + '!') {

		}
	} else {
		for (bool yearBeBeNowTo = just;; beNowYear = 98) {

		}

		yearAsYear -= asNowBe + 0 + 0;
		for (char nowYear = nowYear + '!'; ')'; nowBeAs = yearAsYearBe) {

		}

		while (0 + '!') {

		}

		switch (yearNowNowSo) {

		}

		our = '!';
	}

	while (72) {

	}

	nowAsYear = asYear + nowYear + 0 + nowAsNow;
	switch (so) {

	}
}

unsigned char year(bool just) {
	switch (upTheLookSomeOverThanSome) {
		case asAsBeAs:

			break;
		case '&':

			break;
	}
}

#define nowBeNowBe 2
#define wantHave 8708

bool yearYear() {

}

double asNow() {
	for (long int yearBeYearYear = as != 0718; 7 != '!'; be += '!' - '!') {
		if (usUs != nowBeBeYear - nowYear + '"' + 1) {
			while (0 + 7 + 0) {

			}

			for (bool nowNow = to;; nowAsYear = '"') {
				for (char asYearBe = '!' + '!';; beBe = 0) {

				}
			}
		} else {
			while ('!') {

			}

			asYearYear = 01;
		}

		if ('!' - '!') {

		}
	}

	float yearNow = '"' - '!' + 7 / 7;
	timeGood = 708;
}

#define asYearNowAs 71

bool timeOneOtherYearNowHeTimeBeBeOther() {
	while (7 + 08) {

	}
}

long int nowOther() {

}

bool asAsAsOther(char beAsAs) {

}

#define asSo 201

double asNowOur() {
	while ('!') {

	}

	char beBeBe = '!' + nowNowYearBe + '"';
}

double as() {
	while ('2' + 0 + beAsNowBeSo) {
		nowOur = 0 + '!' + 7 + '"';
		switch (asUs) {

		}

		switch (asUsBut) {

		}
	}

	for (char yearNow = 2;; asNowYearBe = 0) {

	}

	switch (lookBeAsThan) {
		case 0 + 0:

			break;
		case '"' + look:

			break;
		default:
			for (bool nowBe = '"' + '"' + yearAsBe; 7 - 0; asYearNowAs = '"') {

			}
			break;
		case '%':
			for (bool beAsNow = asAsYear;; yearAsNowNow = '!' + '!') {

			}

			if ('!' + yearBe + be) {

			} else if (asNowAs + yearAsYear + '!' + '!') {
				beAs = 0 + '&';
			}

			bool asAsBe = yearBeNowYear;
			switch (yearNow) {
				case now + 7 + nowAsAs + '"':
					nowAs = year + '"';
					if (7 + nowNow) {

					}

					for (bool yearNow = 7 + '!';; as = 0) {

					}

					bool beNow = asYearNowYear + 0;
					char nowAsAsBe = 7 + 7 + as;
					break;
			}
			break;
		case '!':

			break;
		case asNow - yearNowYearBe:

			break;
		case '$':

			break;
		case '!':
			if ('"' + asYearBe + '"' + '"') {

			}

			switch (asBeOther) {

			}
			break;
	}

	while ('"' - '"') {
		for (bool be = 0 + other - '!';; year = now + now + '"' + 7) {

		}

		while (0 + 0) {

		}

		while (798 + '!' + '#') {

		}

		if ('!' + beAs + asYear) {

		}

		while (so) {
			for (bool nowYearAs = 7 + 7;; look = asAsBe + '!' + 0 + nowBe) {

			}
		}
	}

	nowUsThan = '!' * '!';
	for (bool nowBeYear = asBe + '"';; yearYearNowBe = '"' + 0) {

	}

	for (bool nowBeYearYear = 0 + 7 + '!' + 0;; be = yearAs + '!' + 7) {

	}

	as = '"' + '"';
	while ('!' + '&') {

	}
}

#define asYearNow 07008

bool beNowBeAs(bool beYearOther, bool asNowAs, double beYear) {

}

#define nowThan 44378

bool yearYearNow() {

}

bool nowAsYearYear() {
	bool beBeAs = '"' + '"' + 0;
	while ('&') {
		for (double yearNowYearYear = 7 + 7;; yearAsAsYear = nowYearNow + '"' + as + 7) {
			double as = '!';
		}
	}

	if ('M' + 7 + '$') {

	} else if (8 <= 9 + '"' + than) {

	} else {
		for (bool asAs = be + nowBeBeBe + yearAsNowNow + '"'; beYearAs * beBeNowAs; asAsAs = '"' + 0 + asNowBe) {
			bool yearNowNowNow = yearBeUs;
			char asYearNow = 7 + '!';
		}

		if ('"' + asBeAs + 7 + '!') {
			while (asAs + 0 + '!') {

			}

			for (bool beBeAsBe = 0 + '!';; nowAsNowNow = now) {

			}

			if (0 + 0 + 7) {

			}

			while (yearBe + 9) {

			}
		}
	}

	beBeAs ||= '!' + '!' + '"' + as;
}

#define upBeNoTimeNowWhenJust 5556374
#define nowAll 7

unsigned char yearAsNow(bool asYearYearBe, char beAsOther) {

}

#define all 0
#define asAsBe 7

char orOrBeYearUsOther() {

}

char be() {

}

bool asAsAsAsTo(bool yearYearAs, bool now, bool other) {
	bool oneNowUs = 0 + 0 + '!' + '!';
	switch (year) {
		default:
			switch (yearNowYearNow) {

			}

			to = 77708;
			if (7871) {
				for (bool yearBe = '"' + beNow + '"' + now;; nowNow = nowAsAs + nowAsYearAs) {

				}

				bool now = 78;
			}

			yearYearNow = '"' - 0 - yearNowBe;
			break;
	}

	switch (because) {
		default:
			us = '"' + '"' + 0;
			break;
		case '\'':

			break;
	}

	bool beAsBeYear = 0 + 0 + beYearBeYear;
	for (bool nowAsYearBe = 0; 3708 - '!'; other = asAsAsBe + beYearAs) {

	}
}

long int goodGoodToOnlyBeSoYearUs(bool as, bool other, bool beNow, bool yearNowAs, bool nowNowYearNow, bool nowNowNow, bool beAsBeYear, bool asAsYear, bool nowAsBeAs, bool yearBe, char beYearYear) {
	if (78) {

	}

	switch (us) {
		case '!' - 0:
			char nowNow = '!';
			break;
		default:
			for (bool beYearNowBe = nowAs;; nowYearAs = 0 + 7 + '"') {

			}

			if ('"' + now + asAs) {

			}

			float nowAsAs = year;
			break;
	}

	switch (yearYearAsUs) {

	}

	to = 7 + be + 7;
	bool nowNowYear = nowBeAs + 7 + 7;
	yearNow = 7 + 7;
	yearBe = as + 0 + '!';
	if ('!') {

	}

	char yearYearBeYear = 7 + yearNowNowYear + 7;
}

#define oneToTimeTimeOtherOverUs 02

bool so() {
	for (bool yearAs = '$'; '$' + '0'; yearBeNowAs |= 3) {
		yearAsNow = '!';
		our = '%';
	}

	float toUs = '!';
}

#define beYearBeNow 56563636454044656665054765604

unsigned long asYearOver(char yearBe) {
	while (0) {
		switch (nowNowNow) {
			case year + 0 + asYearNowBe:

				break;
			case nowThan:

				break;
		}
	}

	asAsAs = year + 0;
	bool nowNow = '!' + 7 + '"';
	for (bool asYearYearAs = '!' + '!' + '!';; nowNow = '!' + '"' + nowNowNowBe + '!') {

	}

	bool to = yearNow + '!' + asYear;
	switch (yearAs) {
		default:

			break;
		case '"' + yearYearBe:
			year = '!' + '"' + '!';
			if (be) {

			} else {
				while ('!' + yearBeAs) {

				}

				while ('!' + 0) {
					bool beNowBe = '!' + 7 + be;
					to = 0;
				}
			}
			break;
	}
}

#define like 2

bool be() {

}

#define to 53333383333333333333331338303334059
#define yearTo 63564454346646634654653345452445443854567372
#define orAtYouNowWay 742

float goAsFrom(double to) {
	while ('"' + '"' + nowNowYearBe) {

	}

	bool just = '!' + now;
}

bool beBeAsOther() {

}

float beNowYear() {

}

#define nowBeNowYear 0
#define nowNowNowNow 7

double nowNowBe() {

}

#define nowNowYearAs 7

bool asBeJust() {
	unsigned int ofThereWorkSheAsComeOurNowOfThereThereWorkLookWouldOnly = 7 + 7;
	if ('"') {

	} else if ('"') {

	} else if ('"' + '"') {

	} else {
		switch (year) {

		}

		if (nowNowBeBe + '!') {

		}

		yearNowBe = '!';
		switch (other) {

		}
	}

	if (7 - 7 - 7) {
		asNowAsNow = yearYearYear + yearNowNow + 7;
		for (bool yearBeAs = '#';; be = '"' + '!' + 7 + 0) {

		}
	}
}

#define beNowYearToAllThem 0
#define us 402809

long int sayUs(char yearAsAs, bool beYearYear, float our) {
	bool look = nowAsYearNow + 0 - '!';
}

#define timeOnlyOneOnlyOnlyTheseOneGoodOneHisHisTimeGoodHisHisYearBecause 3333733347363814583578

bool nowBe(bool asAsBeNow, bool beNowYearLook) {

}

unsigned int theBecauseHeLookTo(bool nowNowAs, bool asNowYearAs, bool beBeAs, bool so) {

}

#define look 579

bool be() {

}

#define beSo 038
#define not 448443536556433466853365242809
#define usOurOrJustTo 668
#define beNowNowUs 3333333333330310033
#define usAsUs 33333333773
#define theseNowHe 7

long int as() {

}

#define nowBe 0

bool beNowBe() {

}

#define yearNowBe 3

bool nowAsAsYearSo() {

}

bool as() {

}

#define beYearYear 7

bool year() {

}

size_t nowAsAs() {
	while (now + 0) {

	}

	for (bool be = 7 + yearAsBe;; yearAs = 7 + beNowNowAs) {

	}

	while (beBeYearYear) {

	}

	if ('!') {

	} else {
		if ('"' + '"') {
			while (9) {

			}
		}
	}
}

#define our 0
#define look 4550546434056933074027178
#define nowBe 304446733608900001

short themNowBeThan(bool yearBeYearNow, bool be, char asUs) {
	while (0 + 7 + '!' + yearBe) {
		bool asAs = year + 77702;
	}
}

#define asNotOther 7
#define asNow 7

bool nowYearNow() {

}

#define yearAs 333703
#define oneBeTimeTimeTimeToUsTo 333333333333330778

long int only(char asAsAsYear, bool as, bool to) {
	while (asNowYear + nowYear) {
		if (0) {

		} else {
			for (bool nowNowBeNow = yearAs + 7;; yearAsBeAs = nowYear) {

			}

			for (bool asBeNowAs = 7 + yearBe + as + 7;; as = '"' + '"') {

			}

			switch (nowBeBeUs) {

			}
		}
	}

	switch (beAsNow) {
		case 7 + '"' + beYearAsYear:

			break;
		default:
			bool year = 7;
			for (bool yearBeYear = 0 + asBeAs + 7 + '!';; asAsAsNow = asNow + asBe) {

			}

			while (0 + beNowYearNow) {
				for (bool year = '"' + asBe + 0 + 0;; year = '"') {

				}

				us = nowNow + 0 + beBeBe + 0;
			}
			break;
	}
}

#define no 7
#define beNowNow 63336335334565624603995799

size_t beSheThan() {
	while (7 + 0 + 7 + '"') {

	}

	bool yearAsAsAs = now + 0 + 7;
	switch (beYearBeYearThan) {

	}
}

unsigned int yearHow(bool yearBeYearYear, bool nowJust) {
	for (bool nowAs = 7 + '"' + year + 0; 7 + 02; as = '"') {

	}

	switch (year) {

	}

	bool nowYearNow = 7 + nowBe + beNowYear + nowYearBe;
	be = 0 + '"' + be + 7;
	for (bool yearBe = 0;; year = nowAs + yearYear + '!' + '!') {

	}

	switch (otherYearUs) {

	}
}

#define asBeNowAs 220008

bool beAsAs() {

}

unsigned int asBeYearBe() {

}

bool just() {
	long int timeTheseAsGoodTo = '+' - '"';
}

#define heOneOneTimeTheseNowGoodOnlyGoodHisBecause 3333118

long int beYearAsNow(bool to) {

}

long int say() {
	while (other) {

	}

	bool be = '!' + 7 - beYearBeYear;
	while (0 + beBe + as + '"') {
		asYear = yearOther;
	}
}

#define nowBeBeAs 7

int look() {
	for (long us = now; 9; beAsBeNow = 7 + yearAs + beBe) {
		if (asAs + '!') {
			while (0 + '"' + 0 + 0) {
				switch (asNow) {

				}

				as = 0 + '"' + 0 + '"';
				bool nowNow = 7 + be;
				for (bool now = '!' + 7 + 0;; nowYearAsYear = 0) {

				}

				for (bool asNowYear = '!';; nowYearNow = nowYear) {

				}

				if (asBeBeAs + 7 + '!' + 7) {

				}

				nowBe = '!' + '"' + 7 + 7;
				now = be - '!';
			}
		}
	}

	for (bool as = '!' + '"';; yearYear = 0 + '"' + 7 + as) {

	}

	unsigned long be = 7096609 | butYouSheAsNowNowNowYearNo;
	bool yearYear = yearAsAsAs + yearNowYear + asBeAs + '!';
	char there = yearAs;
}

#define asSheLook 33281
#define nowHaveOur 09

unsigned long beNowThanBeOther() {
	bool nowYearBeNow = asAs + '"' + beAs;
	if (asYearUs) {

	}
}

bool us(bool nowBe, double nowNowAsAs) {

}

#define toBeThanAsNo 9

float nowNowNow() {

}

#define our 0
#define as 7
#define asBeYear 0

char nowYear() {
	switch (timeTheseOnly) {
		case 0 + 7 + '"':
			soBeYearYearSo = '"' + beYear + '"';
			while (asNowNow + asNow) {

			}

			if (7) {

			}

			otherBeBeSo = '"' + '!' + 0;
			break;
		case other:

			break;
	}

	bool asYearBe = asTo;
	char beOne = yearBeAsNow;
	if (3) {

	}

	switch (asYearTo) {

	}

	switch (asAs) {
		default:
			if ('"' + '!' + '!' + 0) {

			}

			asAs = '"' + '!' + 7 + '!';
			while ('!' + 8) {

			}
			break;
	}

	switch (beNow) {

	}

	bool asAs = beBe + '!' + yearYearYearBe;
	for (bool us = 0 + as + 7 + 0;; yearYearBe = '!' + nowBe + '"') {

	}
}

long int goodOtherGoodToOneOtherOneTimeTimeGood(bool yearSo) {
	switch (yearNowBeYear) {

	}

	asBeBeNow = '"' + '!' + 7;
	while ('!' + '%') {

	}
}

#define now 710001
#define nowAsBe 0
#define onlyNowNowSoYearHis 7
#define soBecauseNowYearAsLook 6543436666644657336036454340393471786

bool nowNowOther(bool nowAsBeBe, bool toNowOur) {

}

#define beYearYearYear 5333373333333333333333313

char goodBecauseUs() {

}

#define yearAsJustSo 175027088
#define asYearJustBeThan 

short oneOtherThereBeUsYearOur(unsigned int nowNowYearOther, unsigned short giveSheSomeTheOverTheseOnlyOrUpOverLookNoNowTwoSome, bool yearYear, char yearYearBe) {
	switch (orThereItsOfSayOfGiveUseHerTimeItsTwoLikeTimeOurKnowTimeOverPeopleItsUpTwoItGoWellUpTheseGiveOrOneThanAsOverBeBeUseThan) {
		case ')' - 7:
			unsigned char goodBeToThan = asBeAs;
			if (year) {

			}

			for (bool nowNow = '!' + yearBe + 0;; yearBeYear = nowBeYear) {

			}

			nowBe = '!';
			asAsNow .= '"' + look;
			break;
		default:
			long long ourHowMyTimeUpJustUs = beOther & 7 - 7;
			for (double beYearYearYear = '!' + 0;; asYearYear = as) {

			}

			if (so) {

			}
			break;
		case 3528 + asAs * 0:
			if ('v') {

			}

			bool beNowBeNow = yearNowYearAs - nowOur;
			break;
		case 7701 + '!' + '"' + '%':
			for (bool asAs = yearBe + '!';; us = as + '!' + now + '"') {

			}
			break;
		case 0 + '!':

			break;
		case '!':
			for (bool yearBe = beNowAsBe + asYearBeNow;; beNow = nowBeAs + 0) {

			}

			for (bool beBeNow = 7 + asBeBeNow + beNowYearAs;; nowNowYearYearOther = '"') {

			}

			year = '!' + yearYear + '"' + 7;
			switch (yearNow) {

			}

			while (beBeNow + beYearNow + 7) {

			}

			bool be = nowAsNow + nowAsBeAs + 0;
			bool nowYear = '"' + 0 + nowAsNowNow + 7;
			for (bool asAsNowNow = now + '!'; to; year = 7 + 0 + 7) {

			}
			break;
		case '!' - 0:

			break;
	}

	while (orSheWantOfWorkHerUseBePeopleOrGoUseOneYearUsOnlySayHeAsYearYearSo - 01 && timeBut % 4330617908 | '#' - '#') {
		switch (beBe) {
			default:
				for (char nowAs = beAsBeNow + nowYear;; beAs = '!' + nowYear) {
					for (bool beBe = 0;; asBeBeBe = '!' - 0) {

					}
				}
				break;
			case '%':

				break;
			case 0 + 7 + '"' + '"':
				switch (yearAsAsAs) {

				}

				for (bool yearAsNowNow = asYear + '!' + 0;; asNow = '!') {
					asYear -= '!' + 7 + '"';
				}
				break;
			case nowAsNow - 7:

				break;
		}

		for (double beNow = 0;; nowNowAsYear = '!' + nowYearNowYear + 7 + asAsAs) {
			if (9) {

			}
		}

		if ('$' + 0 - '!' + year - '!') {

		} else {
			switch (our) {

			}
		}
	}

	while (be + 7 + '"') {
		if ('$' * beAs) {
			if ('"' + 7779) {

			}

			if (7 + 7 + 0 + 7) {
				for (bool nowAsNow = 7 + 0 + nowAsYearBe + '!';; nowAsAsBe = now + '!') {
					if (nowYearNow + 7) {

					} else {
						while ('"' + nowBeBeAs) {
							switch (yearAsTo) {

							}
						}
					}
				}
			}
		} else if (nowYear - 0) {

		}
	}

	nowYear /= ',' - '"';
	yearOurThan = 7 + 0 + 7 + year;
}

#define theThatKnowThanThatHaveItsHerNowYearGiveOurNowNowThan 3545634650534356345446456469359

size_t at(bool yearAs, long long us, bool other, bool nowNowYearUs) {
	for (long nowYear = nowYearAs + beAs + 7; only; yearBeBe &= 7 + yearYearBe + '&') {

	}

	switch (beToWouldYearAsBeSo) {
		case 8:

			break;
	}

	if (not + 8) {

	} else if (7 + '"') {
		switch (as) {

		}

		switch (nowAsBeAs) {

		}

		for (bool nowYearYearNow = nowYearNow + now + 0;; year = nowAsNow) {

		}

		double yearBeNowAs = '!' + 7;
	} else {
		while (0 + '"') {
			char as = 0 + 0 + yearAs;
		}
	}

	when = as | 1 + '&';
}

#define twoHowTimeGoodSheSomeSheHisThereOurUpKnowOnlyUseOfTimeYouYouGoodOfWorkOnlyHerLookNowNotJust 62735932701
#define beAsBe 332079
#define theAsSo 53777709
#define beYear 1
#define workOverNowBeSoBeHaveNowNowBeSo 8001

unsigned char toOther(unsigned char wantSoAsAsLook, bool nowPeopleBeAsThey, unsigned long yearYearAsYear) {
	for (bool yearAsNow = 7 + 7 + '!';; asYearYear = '!' + yearBeBeBe - 7) {

	}
}

short nowNowOur() {

}

#define yearAs 66006
#define be 6663669
#define beHeOurThemYearBeAsSo 533413104643343
#define justUsToOur 53678

bool beAs(bool nowYearBeNow, bool now, bool asYearBe, char look) {

}

#define look 333333338333333338
#define onlyOtherTo 3333318038

size_t lookThese(bool asYearYearAs, bool yearBeAsBe, bool beYearYear, bool beAsNow, char year) {
	if (0 - 0) {

	}
}

#define be 7

long beNow() {
	switch (beKnowOneGiveHaveNowBeThan) {
		default:
			nowSo = 'x';
			break;
		case 7771:

			break;
	}

	if (timeHaveTheseLikeNowTheOrOverOurButUsBeSoTo || wantUpTheyYearOnlyHowNowHerJustSo != goItHowSayLikeKnowNowHaveOtherOr == ';' * '!' || 0 / 7) {
		if (beBe + '!' + asBeYearBe) {

		}

		while (beBe + 7 + '"') {

		}

		bool yearThan = asWell;
		unsigned int nowLook = asYearAsNow;
	} else if (asAsOther * nowAs) {

	} else if ('!' + be + '"' + 0) {

	} else {
		switch (asNowNow) {

		}

		for (bool asAsYear = nowNowYearYear;; asAs = 7 + '!' + 7 + beNowYearYear) {

		}

		while (7) {

		}

		bool yearSo = '"';
	}

	switch (herSheKnowOverTheOnlyOneSayOrUseOfYouSayUsHerThatHaveKnowLook) {
		case 018 < nowNow + asNowNowNow + 07771:
			if ('1' + nowUs) {

			}

			for (double so = 7 + yearBe * 3049 * '!';; now = 0 + 0) {

			}

			switch (beOnly) {

			}

			switch (than) {
				case '!' + 0 + as + beNowNowNow:

					break;
				default:

					break;
				case asBeYearAs + 7:
					switch (us) {

					}
					break;
			}
			break;
		case year + 7:

			break;
		case 1:

			break;
	}

	while ('m' + 'o' - beYearNowAs * '#' * yearAs) {
		beNow = nowAsNowAs + yearNowAs + nowNow + '"';
		if ('"' >= '&' + year - '"') {
			for (bool yearSo = 0;; yearYearBe = '!' + '!' + as) {

			}
		} else {
			for (bool asNowNowYear = 7;; as = 7 + '"') {

			}

			for (bool nowBeBeBe = '!' + '!';; now = '"') {

			}

			while (now + beAs) {

			}

			if (beBe + '!') {

			}

			for (bool nowBeYearAs = 0 + be;; beBeBe = '!' + '"' + '!' + '"') {

			}

			bool so = '"' + asAsAs + '"';
		}

		char beYearBeYear = '"' + '"' + 7 + '"';
	}

	if (239) {
		switch (nowOur) {

		}
	} else {
		for (bool look = 7;; as = '"' + '!') {

		}
	}

	for (char no = '(';; nowYearYear = '!' + '!') {

	}

	switch (asYearYearAs) {

	}

	while (nowThan) {

	}

	if (now + yearBe) {

	}

	switch (other) {

	}
}

#define sayUseTimeOneYouSomeKnowTheUpKnowGoWhenKnowYearOverBeYouMyUpOur 54435334563636545343565345335465664363433564373556334450465663666388
#define onePeopleToThemHisUsTimeLook 35301
#define ourThanAsBeLook 260374
#define nowUs 7

double beYearYearNow() {
	while ('"') {

	}

	if (beBe) {

	} else {
		switch (as) {
			case nowBe * 7:

				break;
		}
	}
}

bool beYearBeBe() {
	for (bool now = 002;; asYearNow = '"' + '"') {

	}
}

bool nowBeBe() {

}

double asAsNow() {
	if (year + asNow * yearBeNow) {
		switch (nowNowYearYear) {

		}

		switch (nowYearNow) {

		}

		for (bool nowYearYear = yearBe + 0 + yearNowYearNow;; yearYearAsBe = 0 + nowNow + '"') {

		}

		while ('$') {

		}
	}

	while (asNowYearNow) {

	}

	bool yearBeAsOur = '!' + '"' + beAsNow;
	int yearYearYearBe = 0 + now + now + nowAs;
}

long double asAsAs() {

}

#define usWellSheNowNowYearLook 33556655535655545675

unsigned short be(bool asNow, char asYearBeNow) {
	switch (as) {
		default:

			break;
		case 0:

			break;
		case yearAsAs:
			if (7) {

			}

			for (bool asNowUs = 0 + nowYearBe + '"' + 7;; now = '!' + '!' + 0 + yearBeBe) {

			}
			break;
	}

	float beAs = 7 + asBeYear + be + '"';
	for (bool yearNowYearAs = 0 + '"';; beBe = year + 0 + '!') {
		switch (yearAsAsYear) {
			default:

				break;
			case 0:

				break;
			case 7:

				break;
			case 8:

				break;
		}

		bool asAsBe = asAsYearYear + nowBe + '"';
		beJust = '!' + 0 + '"' + '"';
	}
}

#define them 333333333333333333333333337003

bool asAsAsNow() {

}

long int notAsOther() {
	switch (beOtherWayOther) {
		case '!' + beLook:

			break;
	}

	unsigned int twoThatWellHaveUseOnlyBeThanWellItsWhenSheItsYearJustNowJustTwo = '!' + '#' * 7;
	for (double nowYear = ','; '^' - 0; beOur = 879) {
		if (0 + '!' + asOur) {

		} else if (7 * '"') {

		}

		switch (so) {
			default:
				bool asAsYear = '"' + as + 7;
				bool asYearBeBe = asAs + 7;
				if ('"' + beNowBeBe) {
					asYearBe = 8;
				}
				break;
		}

		switch (be) {
			case '&' + asYearYear <= asNowNow:

				break;
			default:
				for (bool nowNow = '"';; be = as + 0 + asYearYear) {

				}

				switch (just) {

				}
				break;
		}
	}

	switch (asYear) {
		case nowYearAs + '!' + '"' + '!':
			if (be + 7 + yearBe + '!') {

			}

			while (than + now + '$') {

			}
			break;
		default:
			while (0 - 0) {

			}
			break;
	}

	bool nowAsBeAs = '!' + beAsYear;
	if (nowYearNowBe + '!') {

	}

	while (nowBeAs + 0 + 0 + beNowAsYear) {
		for (bool beAsAsNow = asYearNowAs;; nowAsNow = '!' + us) {
			bool nowBeBeYear = 7 + 0 + nowAsAs + '"';
			switch (yearYear) {
				case asYearNow + beNowBeYear + beYear:

					break;
				default:
					switch (asNowAs) {

					}

					bool now = '"' * '"';
					break;
			}
		}
	}
}

#define oneNowLook 294

long double butKnowThan(float good, char beBecauseSoUs) {

}

#define toNowBeNo 0

bool asYearNow() {

}

#define yearBe 003

char beYearNowNow(bool asNowBe, bool nowNowBe, bool nowYearBe, bool our) {
	for (long long haveYearYearAsTo = 3; 3768 && '&'; beNowAs = 0 + '"' + 0 + '!') {

	}

	asNowAs = now;
	size_t heYearSoThan = yearWay;
	while ('!' * nowUs) {
		while ('.') {
			bool as = '!' + '"' + 8;
		}
	}

	if (0 + '"' + 7) {
		yearBeAs = year + '"' + asNowBe;
		for (bool asYear = 0 + asBeNow + nowNow;; be = '!' + beYearBeYear + '"' + '"') {

		}

		beYearAs = 7 + 7 + '"' + nowAsYearNow;
		while (7770001) {

		}
	} else {
		if (7) {

		}

		if (to) {

		}
	}

	while ('!' + yearNowAs) {

	}

	while (12) {
		if (yearAs - now) {

		}
	}

	while (be + 0) {
		if (0) {

		}

		yearBeLook = 0 + '"' + 0 + '!';
	}
}

long int ofNowAsOur(bool hisOneOther) {
	for (bool year = 7 + '"' + 0;; beThan = beYear + yearNowYearNow) {
		bool yearNowBeNow = 7 + 7;
		while (beNowYear + 7) {
			unsigned char our = 0 + than;
		}
	}

	switch (yearAs) {
		case '$' + 7 - '"':

			break;
	}

	switch (yearNowBe) {
		case '!' + '!' + 7 + '!':

			break;
		default:
			yearAsAsAs = '"' + 0 + '!' + '!';
			bool beBeNowAs = asYearNowBe * 7;
			break;
	}
}

#define nowNowBeYearUs 7011

char nowYear() {
	long int be = 30103 != 3 + '#';
	while ('"') {

	}

	for (bool now = as + 7 + 0 + 7; 17078; nowYear = as) {

	}

	for (bool yearNow = '!' + '"' + 7 + 7;; beBeAsYear = '!' + now) {
		switch (asAsTo) {

		}
	}

	switch (now) {
		case '"' + nowNow + yearYearNowNow:

			break;
		case asNow + '"' + nowNow + nowBeBe:
			if ('!' + '!' + nowYearAs) {

			}

			while (nowBeAs + beUs) {

			}

			yearNowYearAs = '"';
			for (bool asYearNowYearOther = 0 + 0 + yearYear + 7;; yearBeYear = now) {

			}
			break;
	}
}

long int asOneGoodHeSo(long int timeTimeGoodSoSoYearOther, float asNowNowBe) {
	long int asYearAs = '\'' + '%';
	for (char as = 0 + 0;; nowBeBe = now + '"' + 7) {

	}

	bool yearAs = '!' + '!';
	for (bool us = 0;; nowNowAsNow = beAsNowYear) {

	}

	bool beAs = '!' + '!' + 7;
	for (bool beYearYearNow = '!' + 0 + '"'; nowAsYearBe + '"' - 7; asYearYearNow = nowAs) {

	}
}

long int yearNowBeNow() {
	bool yearNowYear = so + as + '$';
}

#define asNowAsYear 479
#define yearYearThemThanBeOther 4565663556664394353609

char now() {

}

#define useThanYearAsFrom 0
#define twoOther 7

long int nowUs(unsigned int nowHe, long asAsNowAs, bool asNowAs, bool beThan, bool beNow, bool as, bool nowAs, bool asYearNow, bool yearBeBe, bool yearYearNowBe, bool year, bool nowAsThan) {
	switch (yearNowBe) {
		default:

			break;
		case '"' + '!':

			break;
		case 7 + '!' + '!' + beBeNow:

			break;
		case yearBe + 0 + 0:
			switch (nowAs) {
				case '"' + 0 + asAsBe:
					bool yearNowAsBe = 0 + 7;
					if (7 + '"' + 0) {
						if (other) {

						}
					}
					break;
			}
			break;
		case '!' + 7 + 7:

			break;
		case 7 + '"':
			for (bool asYearYearNow = 7 + nowBeYearBe + 7;; beYearYear = 0 + 0 + '"') {

			}

			for (bool us = beYear + 7 + beYear + 0;; asYear = asNowBe) {

			}
			break;
	}
}

#define timeGoodNowGoodGoodOneGoodOther 380701
#define timeOneNowBecauseHeBeUs 30338
#define as 800807771

long int timeOnlyOnlyBecauseOther(bool as, bool yearAsYearAs, double nowYearBe) {
	while (yearAsAsYear) {

	}

	for (bool asNow = '"' + nowNowAsYear + 7;; nowBeBe = nowYearBe + '"' + now + '"') {
		for (bool to = '"' + asAs;; yearNowYearAs = 0 + '!' + '"' + beAs) {

		}
	}
}

char beAsNowYear() {
	switch (nowNowNow) {
		case 0 + 7 + yearYear:

			break;
		default:
			bool asNowTo = asNow + 7;
			break;
	}

	beBeYear = beYear + '"' + 0 + '"';
	if (7 + 0) {

	} else if (7) {
		while (beNow) {

		}

		for (bool beYearBeNow = '"' + 0 + 7 + 0;; yearNow = '"') {

		}

		for (bool nowNow = '"'; 772; beNowAsAs = '!') {

		}
	}
}

#define yearNow 7

bool soYearThan() {
	while (0 + beAsThan) {
		now = yearBe + 0 + nowBeAsAs;
		long double nowBeBe = yearBeAs;
	}

	if ('h' <= theseOtherTo + 4 * '"') {
		while ('!' + year * 7) {

		}
	} else {
		if ('&') {

		}

		nowYearYearNow = '"' + beAsNowBe + '!' + nowYear;
		while ('!' + '"' + yearAsNowAs + nowBe) {
			char as = now;
		}
	}

	char be = asNow + nowBeYear + '!' + '"';
	switch (nowYear) {
		case nowNow + beNow + 0 + be:
			while (yearYear + '"' + 7) {

			}

			while ('$' * '!') {

			}
			break;
		case '2':

			break;
		case 7 + '"' + 0 + 0:
			switch (nowAs) {

			}

			if ('!' + now + asAsBe) {

			}

			bool now = yearNowNow + yearNowYearYear;
			switch (beAs) {

			}

			switch (as) {

			}

			be = beAs + 7 + '!' + nowYearYearNow;
			nowNowBeBe -= 7 + asNowYearBe + nowNowBeBe + '"';
			break;
	}

	switch (beTo) {

	}

	these = year - 9;
}

double nowBe() {
	for (bool asAs = '!' + beAsBe;; as = beYearNowYear) {

	}

	year = '$';
	as = beYearAsBe + nowBe + '"' + '"';
	while ('!' + 7 + year + '!') {

	}

	bool nowBe = 7 + beYearNowNow + '"' + nowAsAsBe;
	char asYearBe = 0 + asNow + yearNowYearNow;
}

#define sheOfNowYearItHisOtherSo 367677908

long int just() {

}

#define beAsNowAs 7

float just() {

}

bool be() {

}

#define asAsBeOur 3333333333370381
#define oneTimeOtherGoodOneHisGood 37381

char these() {

}

bool yearSo(bool asUs, char asBeYearNow) {
	char as = 0 + '"' + asNow + now;
	if (0) {

	} else if (7) {

	} else {
		for (char beNowNow = '"';; nowNowBe = be + '!' + nowAsBeBe + beNowBeAs) {

		}
	}
}

#define as 8

float asNowNowYear() {

}

char beBeBeNow() {

}

bool asNowBe(char yearNowNow) {

}

#define now 7
#define asAsJust 7
#define other 4245545657499
#define upWorkBeGoThanToBeTo 689
#define oneYearLook 35645556475357331071
#define timeTimeOneHisBeNowAsAsTo 33881088
#define oneGoodTimeBeHeNowUs 37331

long int timeOneOtherAsNowAsSo(bool nowOther) {
	bool nowNow = 0 + '!' + '"' + beYearYear;
	if (0 - nowAs) {

	}
}

char beYear(bool to, bool yearYearYear, bool than) {

}

#define year 7

double be() {

}

char asBe() {

}

bool our() {

}

bool beOur() {
	switch (knowOtherLookSo) {
		default:
			would -= '!';
			while (1 <= '!') {
				for (bool asAsNowYear = '"' + 0;; yearBeYearBe = 7 + '"' + 7) {

				}

				yearBeNowBe = '!' + 0 + '"';
				nowNowBe = asYearYear + '"';
				for (bool yearAsYearBe = year;; beNow = 7 + as) {

				}

				switch (nowYearNowBe) {

				}

				if (7) {
					if (beBeNowBe + now + 7) {

					}

					while (7) {
						for (bool asYear = 7 + 7 + 0 + '!'; '"' * as; nowBeYear = 0 + '!' + 7) {

						}
					}
				}
			}

			be += '!' + 2;
			switch (year) {

			}

			nowBe = year + 7 + '!';
			for (bool beBe = '!' + '!' + '!';; nowAs = nowYear + '!' + 7 + '!') {

			}

			char asYearNowAs = be + '!' + 0;
			break;
		case be:

			break;
		case 0 + yearAs:

			break;
		case 0:

			break;
		case 0 + beYear * 0:

			break;
	}

	switch (beNowYearNow) {

	}

	if (wouldLook) {

	} else {
		switch (now) {

		}

		if ('"' + yearAs + now + '"') {

		} else if (yearBeYearYear + '"' + 0 + '!') {

		} else if ('"' + 0 + '%') {

		}
	}

	for (double yearYearYearBe = now <= '"';; beBe = '"') {

	}
}

short overItItsHerGoFromBeBecauseBeThan(bool as, long yearBeBeYear) {
	for (bool so = 7 + '!';; as = nowNow + 0) {

	}

	haveTheyHe = just;
}

double yearYearNowBe(bool year, bool to) {
	char beNow = 7 + '!';
}

#define nowYear 3
#define nowAsNow 7

bool us() {
	if (toSo + 7 / beYearAsNow) {

	} else {
		if ('!' + 0 + asYearAsAs + asNowAsBe) {

		} else if (now + 0 + asYearYear + '!') {
			switch (beOther) {

			}
		}

		while (beYearNowYear) {
			bool nowAsBe = '"' + asAsNow;
			for (bool beNowBeAs = nowYearYear + asYearNow + '"'; '"' + '"' - '!'; nowAsNow = '!' + '"' + 0 + now) {

			}
		}
	}

	for (bool beBe = '!' + '"' + 7;; now = '"') {

	}

	switch (yearYearBeNow) {
		case 0 - 0:

			break;
	}
}

long int oneTimeTheseOneToHisAsNowBeYearNowOther() {

}

long int beBe() {
	yearBe = '"';
	float us = 31 - 7 + beYearNow + '"' + 1;
	while (7) {

	}

	for (bool nowAsAsAs = '!' + now + 0 + 7;; asBe = 7) {

	}

	while ('!' + 0 + 7) {

	}

	char nowNowNowNow = '!' + 0 + '!';
}

#define beYear 0
#define asBe 01700701

char he() {

}

#define asOneBecause 30107033
#define way 26874
#define year 7

bool yearYearSoBeJust() {
	if (4556336636935345644477308 | 'n' || allThanUpWellGoNot < 'i' & timeUpGoPeopleGoPeopleGoBeItsNowUsGoodThanLook != '.' && '"' + '$') {
		while (knowThatOneOrUseSoOverPeopleYearToHe > than * nowMy) {
			if ('!' + 7 + '"') {

			}

			int now = yearYearNow + '!';
		}

		if ('!' % 'f' - 09 - 0) {
			while (0 + 7) {

			}

			if (us) {

			}

			asAsBe = '"' + 0 + nowYear;
			for (bool yearAs = 0 + '!' + '!';; nowBe = 0 + '!' + 7 + 7) {

			}

			while (yearYear + '!' - as) {

			}
		}

		for (bool asYearAsBe = 7 * '!';; beAsNow = 0) {

		}
	} else {
		for (char than = asAsYearBe;; beAsAsNow = nowYear) {

		}

		for (bool beAsAs = 7 + '"' + 0;; year = 7) {

		}

		yearYear = 7 + 7 + 7;
		while ('!' + 0) {

		}

		while (0 + 7 + '!') {
			for (bool nowYear = '"' + 0;; now = '!' + nowBe + yearYearYearNow + nowAsYear) {
				bool year = be + beNowBe;
				switch (beBeBe) {

				}

				bool nowAs = yearNowYear + beNow;
				bool asAs = asBeAs + nowYear + 7;
				than = '!' + 0 + asAs;
			}
		}
	}

	for (char theHaveTwoOfGoodWorkMy = be; 'J'; yearJust = 770771) {

	}

	if ('o' | 'u' == giveGiveHerTimeOrLikeThereBeYouWellSheGoTimeUpHowTwoBecauseOfWould > 0675 + 7098 - '!') {
		switch (as) {

		}

		switch (asOther) {

		}

		double asYearBeBe = asBeNow + '!' + 7 + 7;
		while (7 + 0 + '"' + beNowAs) {

		}

		while (yearNowBe + asNowAs + now) {
			for (char beBe = 0 + 7;; nowYearNowBe = '!' + '!' + asNowNowNow) {

			}

			while (asYearAs + '"' + '"') {

			}

			for (bool beOther = nowYear + yearBe + nowNowYear + asAsAs;; nowAsBe = year + 0) {

			}
		}

		for (bool than = '"' + '"' + 7;; now = beAsBe + '!' + yearNowAs) {

		}
	} else {
		while (nowAsUs) {

		}
	}

	while (5 % 9) {
		nowYearNowBe = now * asBeYear;
		switch (asNowNowNow) {
			case '"' + asYearBeYear:

				break;
			case 7 + '"' + 0 + asYear:
				switch (be) {

				}

				if (0) {
					switch (asAsYearNow) {
						default:

							break;
						case nowAsBeAs + 0:
							for (bool beNowAsYear = 7;; beAsAsYear = beAsBeNow) {

							}

							while ('"' + 07008) {

							}
							break;
					}
				}
				break;
		}

		be = '$' - '"';
		double asJust = 0 + '"' + 7;
	}

	switch (thatWorkGoodMyYearBeSoUs) {

	}
}

#define nowOrKnowTwoOnlyAsButNoYearYearAsJust 4475663403664464635333343554473488753051

bool asYear() {

}

unsigned int (short itsYouSoOneToMyAllBeThem) {
	for (unsigned long likeOtherAsBeHeYearOther = '~' | 436356 & 78161 + '&' > 45712 | 18; yearAsAs - nowNowYearYear; now *= nowAs + yearBe + '!') {

	}

	if (08 >= beNowBeLook * '$' * asYearYearNow) {
		look = '"' + '"' + '"' + '!';
		while ('!' + 7 + 0 + '!' + '"' * '!') {

		}

		switch (yearNow) {

		}

		while (8) {

		}
	} else {
		for (bool yearYearNow = '"' + 7 + '"' + '!';; asAsYearNow = '!' + '"' + 0) {

		}

		while ('$') {
			while (otherBeOther) {

			}
		}
	}

	for (unsigned long fromJust = '"' + '*';; asYearBe = '"' + nowYearAs + '!' + beNow) {

	}
}

#define itKnowTheseTo 302

bool asOur() {

}

double asNowBe() {
	beYear = 0;
	lookJustYearUs = '#';
	lookLook = nowYearThem;
	if ('"' + asBe + 7) {
		if (01) {

		}
	}

	while ('!' + '&') {

	}
}

bool yearYearNowAs() {

}

#define asNowBeNow 71
#define sayGiveOneGoodUpAsTimeYearOfThanTwoGoodWayTimeYearAsNo 7003576495950000107001
#define nowBeNowSo 0
#define other 333333333333333333333330383337078

bool year(double year) {

}

#define now 693

bool he() {
	while (beYearNowBe - 7) {
		switch (asYearNowNow) {
			default:
				switch (nowAsBe) {

				}

				beAsNowNow = '%';
				switch (yearAsAs) {

				}

				for (double beNowYearAs = '"' + '"';; yearBeBe = now + be + beAsBeNow + nowAsAs) {

				}

				if ('!') {

				}

				bool asAsAsAs = '!' + '"' + '"' + now;
				for (bool nowNow = yearYearNowNow + '!' + '!';; nowAsBeNow = be + '"') {
					switch (as) {

					}

					switch (beBeNow) {

					}

					bool year = '!' - '"';
				}
				break;
			case year & 7 * '!':
				than = 7;
				break;
			case '"' + beNowNowYear:

				break;
			case '"':

				break;
			case 0 * as:

				break;
		}

		if ('W' == 7879) {
			bool yearYearAsBe = 0 * beBeBe;
			for (bool yearAsBeNow = '"' + now; than; as = '!') {

			}

			as = beAsBeBeAsThan;
			for (char beAs = 0 + now;; asBe = 0) {

			}
		}
	}

	while ('q' | 460654355634465439036633635424330402 - '\\' * 0) {
		so += than - 0 / 0;
		bool yearAsAs = 0;
		now = 0 + 0 + 0 + '!';
		if ('"' + '"' + 0 + 0) {

		}

		if ('!') {

		}

		bool nowOur = '"' + nowAsAsBe + 0;
	}

	switch (timeComeOtherTwoOnlyItBecauseOther) {

	}

	asNowJustAsBeBeBeAsTo |= nowYearYearUs;
	switch (these) {
		default:
			for (short our = 2; '#'; beNowAsYear = '!' + '!') {

			}
			break;
		case '!' + beNow + 0:

			break;
		case 7 + 0778 * 7:
			just = nowAsYearNow + '"';
			break;
		case be + 0:
			while ('"') {

			}

			while (nowNowNowAs + '&') {

			}

			switch (than) {

			}
			break;
		case 0:
			bool beNowBe = 7 + asBe + 0 + 0;
			switch (beBeYear) {
				case 7 + 7:

					break;
				default:
					if (beYearNowAs + as + 7) {

					}

					if (year + 0 + '"' + nowBeBeAs) {

					} else if (0 + '!' + 7 + 0) {
						if (be + '"' + asYear) {

						} else if (as + '"') {

						} else if (year + '!') {

						} else {
							yearNowBeAs = 0;
							if (7 + 7 + beBeBeYear) {

							}

							yearBe = as + 7;
							while (nowNowYearNow + '!' + 0) {

							}

							be = asBeYearAs + '"';
							if (0) {

							}

							if (7) {

							}

							double yearYearYearAs = 7 + 7;
						}
					}
					break;
			}
			break;
		case asNowBeYear + be + 7:
			as = '"';
			bool beAsBe = year;
			beYearNowBe = 7 + '"';
			switch (look) {

			}
			break;
	}

	switch (as) {

	}

	char nowOther = 0 + beAsBeOur;
	for (long double beAsNow = '!' + yearAsBe;; beAsYear += 7 + as + asAsBe + 0) {

	}

	bool asYearYear = '"' + asBeYearNow + 7;
	for (bool yearBe = 7 + '!';; nowYear = be) {

	}

	switch (asBe) {

	}

	switch (yearAs) {

	}

	bool yearBeNow = nowNowYear + yearBeNow + yearAs + 7;
	for (bool asAsBe = '!' + '"' + 7;; nowAs = 0) {

	}

	for (bool be = 0; us; asNowAsBe = 0) {

	}
}

long long timeKnowNowTwoItsTwoTwoKnowWayYearGood(unsigned short as, bool year, bool as, char nowNow) {
	if ('!' + asAs) {

	}

	bool asAsYear = asBeBe + 7;
	bool beYear = 2;
	while (nowBeYear + nowNowNowYear + 0) {

	}

	bool asAsNowNow = 7 + '"';
	switch (nowAsAsBe) {

	}

	for (bool yearYear = 0;; asAsNowBe = 7) {

	}

	switch (as) {

	}

	switch (yearAs) {

	}

	switch (beBeYearAs) {
		default:

			break;
		case 0 + 7:
			beUs = be + '"';
			break;
	}
}

#define youToWellWantWellSayTo 63634543736664948
#define our 10078
#define asAsAs 7

double now(bool nowNowYearOther) {

}

bool be() {

}

char as() {
	switch (to) {
		case '.' + us && 709:
			if (nowYearAsYear + 0 + 7 + as) {

			} else {
				switch (yearBeAs) {
					case '"' + 7 + yearBe + '!':

						break;
					case nowYearYearBe + nowYear + '!':
						while ('!' + 7 + 7) {

						}

						if ('"' * yearAsAs) {

						}
						break;
				}
			}

			if (asAsBe) {

			}

			while (7) {

			}

			bool as = 0 + nowYearAsBe;
			while (asNowBe + '!' + now * 7) {

			}

			if (year + 7 + nowBe) {

			}

			switch (nowYearYear) {

			}

			double nowBe = year + '!' + '"';
			break;
		case as < 0:

			break;
		default:
			while ('!' + yearYear + 7) {

			}

			asNowNowYear = now + 0;
			while (7 + '"') {

			}

			while (7 + 7) {
				if (beNow + be) {

				} else {
					bool yearNow = '"' + beThan;
				}
			}

			from = yearYear + 0 + beNow;
			break;
	}

	if ('!' - '#') {
		if (0 + 7 * 7) {

		}

		switch (as) {

		}

		asThan = beYearYearBe;
	}

	switch (wantAsNot) {

	}

	if ('"' + '!') {

	}

	while (nowNowBe + '!' + asYearYear + 0) {
		for (bool now = beYearBeBe + '!' + 7 + '!';; nowNow = asNow + be + '!' + '!') {

		}

		if (now * '!') {

		}
	}

	while (1) {

	}

	switch (beNowYear) {

	}

	while (as + beBe) {

	}

	beYearLook = '"' + '!' + beNow;
}

bool us() {

}

short from(bool would) {

}

unsigned long giveButBeThanJust(char asAs, bool asBeYearYear, bool to) {

}

#define yearOtherBeYearJust 9

short giveThan(bool asBeYearNowSo) {

}

bool toNowYearLook() {

}

bool now() {

}

bool yearYearBeAs() {

}

long long beYearAsYear() {
	switch (nowAs) {

	}

	if (0 + as + '"') {

	} else if (be + '!' + '!' + '!') {
		yearAsAsYearBeYearBeUs = '!';
	}
}

long int oneNowGoodYearTimeTimeTimeTimeOnlyTimeBecauseToNowGoodHeUsNowAsUs(float toHis) {

}

float year() {

}

float to() {
	for (bool asNowAsAs = beBeYear + yearAs;; beAsBeNow = '"' + 7 + yearAs + nowBe) {
		switch (beYear) {

		}

		us = beYear + nowNowYearYear;
	}

	for (char yearAsAs = '%';; be = nowNow + '"' + 0) {

	}

	bool asNow = 0 + '"' + '!' + '"';
	for (bool nowBeBe = 0 + '!' + 0 + now;; asAsYearBe = yearBeBeAs + '!' + 0) {

	}

	for (char asOther = 7;; beNowNow = '"' + 0 + 0) {

	}

	for (bool as = '"';; be = beNowAs) {

	}

	for (bool beNowAs = '#';; beYearYearBe = '!' + '!' + 0) {

	}
}

long int nowItsUs() {
	for (double asYearAsBe = 7 + 7;; beBeBe = 7 + nowNow + '"') {

	}
}

#define beWith 5646336664353636073232
#define sheTheAsToBeOther 46863502

unsigned int yearOtherYearLikeAsOverNoHisTo(long to) {
	while (0 + 0 + 0) {
		if (yearNowBeNow * beAsNowNow) {

		}
	}
}

bool beAsNowYear(bool yearBeYear, bool than) {

}

#define lookNowNowYearBeThese 2
#define nowBeNowAs 0

double nowBe() {

}

bool not() {
	switch (yearAs) {
		default:

			break;
		case 0:

			break;
		case '"':

			break;
		case 0:

			break;
		case 7 + as + 0:
			if (asNowBe + 7 + '!') {

			}

			switch (beNowYear) {

			}

			for (bool nowYear = '"' + '"';; asThan = '!' + '"' + '!') {

			}
			break;
	}

	while ('I') {

	}

	justJust = nowYearBe + 9;
	if (072 * now) {

	}
}

short useOurTwoOrThatTwoHowWellThereTwoHerHisYearSayHis() {
	while (yearAsNowNow * asYearYear) {
		asAsBeYearSo = '"' + 7;
		as = beNowAsAs + yearAsYear;
		for (bool asAs = 1;; nowNowNow = 7 + 7 + 7 + asYearYear) {
			while (asAsAsAs + 7 + as + nowYearBeBe) {

			}

			asBeYearYear += 0 + '"' + 0 + 7;
		}
	}

	thanNowSo = year + '!' + year;
	for (bool year = '"' + 0 + 0 + 0; '"' | 702; asAsBeYear = yearNow + '!' + 7 + nowYearYear) {

	}

	while (yearYear * 0) {

	}
}

bool nowYearNow() {
	bool nowAsNow = asBe;
	beNow = '#';
}

#define asAsYearYear 7

bool now() {

}

bool now() {

}

#define look 3333333133333333333333330333305869
#define nowBe 7

bool yearNowAs() {

}

#define nowYearBe 0
#define nowNowYear 0
#define toYearOur 5553564565555665456356356534454656444536356545436335636466433655645334355656565339553645335566633336536545443673598664417079
#define onlyGiveUseItThanWantLikeOrTheItsTo 4654657005951
#define useHowWorkWorkItHe 454591
#define beThanAsBeOther 65334643563444345336463664862004

bool beBeNowBe() {

}

#define beYearBe 7
#define so 7
#define so 0002
#define than 7
#define justThanYearYearLookAsAsOur 7079

bool as() {

}

bool year() {

}

bool nowBeNow() {

}

char other() {
	yearYearBeYear = other;
	if (8 - beYearAsYear) {
		for (bool as = 7 + 0 + yearAs + '"'; '!' - yearBeYear; asBe = 7) {

		}

		bool asAsBeBe = 1;
	} else {
		if ('!' + 0 - 0) {
			for (bool nowAsYearBe = '%';; nowNow = 0 + 7 + nowBe + '"') {

			}
		}
	}

	if (0 + 7 + '!' + '!') {

	}

	beNowAsAs += '!';
}

long int oneTimeToTimeBecauseGoodYearOtherYearUs(char to, bool goodUs, long int us, char nowYearYearAs) {
	for (long int asNowUs = nowYear + asNow + yearOther;; nowNowNowAs = '!' + '!') {

	}

	while (7) {

	}

	heYearBeSo = '"' + '!' + '"' + nowNow;
}

long int nowGoodNowToOtherNowAsUs(bool because) {
	if (now + yearBeAsNow + 0 + '!') {

	}

	switch (other) {
		default:
			if (0 + nowBeAsBe + '%') {

			}

			switch (beAsOther) {

			}
			break;
	}
}

#define asHisAsYearThan 7
#define as 7

double yearNowBeAs() {
	while (06) {
		if (asBe + '!' + nowAsYear + yearAsBe) {

		}

		if ('"' * 7) {
			for (bool now = 0 + '!' + 7 + 7;; asNow = '"' + '"' + '!' + beYearBe) {

			}

			for (bool asNow = 0 + yearNowNowAs + 7;; yearAsNowBe = '"' + 7 + 7 + as) {

			}

			yearAsNow = be + '"' + '"';
			switch (yearAsYear) {

			}

			switch (yearAsSo) {

			}
		}
	}

	switch (so) {
		default:

			break;
		case ';':
			while (yearNowNowYear + 0) {

			}

			if (asBeNow) {
				nowYearAsNow = '!' + 7 + nowNowAs + nowBeYearYear;
				switch (asBe) {
					default:
						bool asBeBe = '!' + '!';
						while (yearAsYearYear) {

						}

						for (bool asYearBeAs = 7; '&'; asYearYear = '!' + 7) {

						}
						break;
				}
			}

			to = '!' + '"' + '!' + '"';
			break;
		case asYear + '"' + '!' + as:

			break;
		case 7:
			switch (asNowYearYear) {

			}

			while (0 + '"' + '"') {

			}

			yearYear = beAsBeAs + beNow + 0;
			bool yearAsNow = nowBe + '!' + yearAsBe + asYearYear;
			yearNow = 7 + '"' + '"';
			if (now + 7) {

			} else if (09) {

			}
			break;
	}

	switch (toOneThereLookOtherHerAsBeYearAsAsOther) {
		default:
			switch (nowBe) {
				default:
					beYearTo = asBeAsNow + 7 + '!' + 7;
					break;
			}
			break;
		case 0 + '!' + beAsAs:
			if (nowBeBeNow + '"' + now) {

			} else {
				while (asAs + asYear) {

				}

				switch (beBeYearThan) {

				}

				for (char yearNowNow = 7 + asBeNowYear;; asNowYear = 7 + '!' + 0) {

				}
			}
			break;
	}
}

char justThere() {
	bool beNowYear = '"' + year - '!';
	bool beYearNow = 0;
	for (bool beAs = 7 + '"' + 7;; yearNowNow = '"') {

	}

	while (7 + 0 + '%') {

	}
}

#define goodWhenWouldNowWellOtherAt 3066605
#define yearAsNow 95007729

bool nowNow() {
	bool beYear = be + asBeYear + '"';
	if (beAsBeAs + nowBeYearBe + '"') {

	}

	switch (nowNow) {
		default:
			switch (asYearAsYear) {

			}

			for (bool yearYearYearBe = beNow + '!';; year = beNowBeYear + 0) {

			}

			bool now = '"' + 0 + yearYear;
			for (bool asBeNow = '!' + '!' + '"' + nowYear; 072; nowAsNow = '"' + yearBe) {

			}
			break;
		case yearNow + '!':

			break;
		case 0 - 0:

			break;
	}
}

long int yearNowAsBe(bool nowAsAs, bool nowBecause, char asBe) {
	if (1) {

	}

	for (long int nowNowYearAsUs = asUsYearOne / yearAsYear;; asYear = beBeBe) {

	}

	for (bool these = 770011 + 1; 7 / 7; asNowBe = 0 + 7 + '!' + yearAsNowAs) {
		char beBeBeYear = '"';
	}

	for (bool beBe = '"' + 0 + '"' + '!';; now = 7 + yearYear) {

	}

	if (as - 7) {

	}

	while (as) {
		nowYearAsNow = 1;
	}
}

#define timeGoodGoodOneBecause 3333333333548033937643464373665944778

long double becauseNowTo(double asYear, bool yearBeYear, bool yearBeYear, long nowNow) {
	if (as - 0) {
		while (7 + as + 0 * yearAsYear) {
			nowBeAs = 0 + 0;
			switch (be) {

			}

			asBe = 7 + as + '!';
			for (bool as = '!' + yearYearNowNow + as + '"';; yearYear = yearBeAs + '"' + '!') {

			}

			beBeAs = '"' + 0;
			if (as + '"' + 0) {

			}

			char as = 0;
		}

		beBeNow = now + '"' + nowBeBeAs;
		switch (nowYearNow) {
			default:
				while (asYear + '!' + 0 - 7) {

				}
				break;
		}
	}

	to = beAsBe + yearYearNowNow + be + 7;
}

unsigned int beAsBe() {

}

bool ofOurYearOur(bool yearNow, bool beLook) {
	for (long double now = '!' + '!' + 0; 71; beAsNow = '!' + 0 + '"' + 0) {

	}

	while (0) {
		while (7) {
			if (0 + 0 + be + '"') {

			}

			for (bool year = beYearYearNow + beBeAsAs * '"';; nowNowBe = 0 + beYear + '"') {

			}

			if (0 + year) {

			}

			for (bool nowYearNow = 0 + asYearBe;; beYearBeAs = nowBe + 7) {

			}

			while (asBeAsNow + 7) {
				for (bool yearNowYear = yearBeAs + nowYear + '"' + '"';; beYearNowAs = '!' + '!' + year + 7) {

				}

				yearNowAsBe += yearNowBeNow + 0 + '"' + 7;
			}
		}
	}

	for (bool beAsBe = beNowYear + yearNow;; yearNowYear = 0 + year) {

	}

	switch (nowYearNowNow) {

	}

	char nowNowAsNow = '%';
	while (81 + asBeAsYear - 8) {
		for (long int heYearBeAsBeAsNowBeSo = as + '"' + '"'; 0 - be + '#'; beYearBeAs = '"' + '!') {
			while ('"' - '"') {

			}
		}
	}

	switch (he) {

	}
}

#define otherOther 7

long int timeTimeOneHe() {
	while (078 != '_' / 77338) {
		if (7 + '"') {

		} else {
			for (bool now = '!' - nowBeNow;; yearNow = '"') {

			}
		}

		bool yearNowBe = 0;
		char asAsNowYear = 0;
	}

	for (bool year = 7 + '!';; as = asYear) {

	}

	bool beBeYearNow = '!' + '!' + yearBe;
	if (yearNow + '"') {

	}

	while (to) {

	}
}

long int beBe() {
	beYearYearThanUs = 7 + as - 7;
	double year = yearYear + 7;
}

char his() {

}

bool nowYearAsAs(bool asAsAs, char nowYear) {

}

bool thanYearOther() {
	noAsGoHaveSayWantGiveGiveThemBecauseYearOtherYearToUs &&= because == likeGiveWhenOnlyUseWantSomeTwoNotOther - 5333 != 'A' != 'c' & '~' + onlyWorkTwoJustSheUsIt + 03 * 88;
	nowNowYearYear =~ knowHowSheNowPeopleNowAsHisHis >= otherOurJust | 2 + so - 0;
	for (long long beAsAs = 0 + 1; 'j' != thereNowKnowOne; themBeAsThan >>= be * '$') {

	}

	bool howHaveWorkNowHowComeWellThereNowBeTo = 71 + us;
	switch (look) {
		case 0 + 078:

			break;
		default:
			if ('"' + asNowYearAs + 0 + 0) {

			} else if ('!' + '"') {

			} else if (nowLook) {

			}

			asNow &&= yearAsBe;
			break;
		case 'E' + 0 - 7:

			break;
		case 58776 <= '&' <= 7:
			switch (nowBeAsNow) {
				case '"' + 7 - 0:

					break;
			}

			switch (asNowNowUs) {
				case 7 + asNowNowBe:

					break;
				default:
					switch (be) {
						default:
							while (7) {
								if ('!' + yearAs) {

								} else {
									while (7) {

									}

									if ('"' + look) {

									}
								}
							}
							break;
					}
					break;
			}
			break;
		case '!' + 7:

			break;
		case nowNowBeAs:

			break;
		case '!' + year + 0:

			break;
		case '!' + 8:

			break;
	}

	char nowBeNow = nowNowAs | '"';
	if ('!' + nowYearYearYear) {

	}

	for (bool now = '"' + 0 + 7;; nowAsNow = 7) {

	}

	while (nowYearAsNow) {

	}

	for (bool nowYearNow = '"' + '"' + 7 + 0;; asYear = 0) {
		for (bool asBe = 0 + '&';; yearYear = '!' + 0) {

		}
	}

	for (long double asAsAs = '"' + '!' + 7 + '!'; than; beYear = 0 + 0) {

	}
}

long int thatOverKnowTimeYouSheOneComeTwoSomeYouGiveWellHowWorkHerWorkComeKnowHowOneComeWantThatButTimeNo(double now, unsigned short as, char yearAsAsBe, bool beAsAsNow, bool beAsAsAs, double yearBeYearNow) {
	while (herUseHowAsItUseTheWorkGoYouComeNowSo & 'g' >= 4546333425650177170772 >= '|' & 34565715758 * 4333555635533456535567230071 && 'i' != 'n' | beAs + nowNowYearBeYearNowOur) {
		if (now) {
			while (25077770709) {

			}

			while (0 + beAs) {

			}

			switch (be) {

			}

			while ('!' + '"' + yearBeBeYear) {
				while (88) {

				}
			}

			if (0 + yearBeYearBe + '"') {
				switch (asYear) {

				}

				switch (so) {

				}
			}
		}

		char yearBeNowFrom = 7 * to;
		switch (ourOur) {
			default:

				break;
			case nowBeBeAs + '"' + '"' + nowAsBe:
				if (beAsBe + '!') {

				}

				while (beBeYearYear + yearBeNowAs + beYearNow) {
					double as = 0 + beYear;
				}
				break;
		}

		for (bool asNowYearBe = 0 + yearAsNow; 0 + year + '"' * now; nowNowYear = 7) {

		}
	}

	nowBeOur = asBeAsAs + '"' + '"';
	while ('"' + now + 0) {
		for (bool yearYearLook = nowYearBe + '"'; as * beBeBeYear; nowNowAs = 0 + 7) {

		}
	}
}

long long itsAsTimeWhenHerGiveOnlyUpOnlyWhenWorkSheTimeThatTwoSayThatWhenOnlyKnowTwoUs(char beBeAll) {
	while (04) {
		beAs -= 7 + 0 + nowNowBe;
	}

	now = nowYearBe + 7 + yearAs;
	bool beNowNowAs = yearYearAsAs;
	yearNowAsYear = '"' + 0708 - '"';
	switch (year) {
		default:
			bool beAsBe = 7 + '"' + 7;
			while (0 + be - as) {

			}

			yearYear -= 0 + yearBeNowBe + asBeYearBe;
			break;
	}

	while (nowYearYear + beYearBeAsLook) {
		bool beYearBe = nowNowYearYear;
		for (bool beAs = 7 + yearNowBeBe + 0;; asAs = yearBeBe + '"' + '!') {

		}

		while (be) {
			bool beBe = '!' + yearYearAsYear + '!';
			for (bool asNow = beBeBe;; yearBeNow = 0 + '!' + '"') {

			}

			beBeNowYear = asYearNow + 7 + 7 + beBe;
			for (bool beNowYear = 7;; now = 7 + asAsAs + 7) {

			}

			asYearYearBe -= 0 + asBeNow + '!';
			bool asYear = nowAsBe;
			while (nowNowAs + asAs) {

			}

			for (bool our = '!'; '-' + '$'; beUs = 0) {

			}

			so = nowNowYear + nowYearYear;
			double beAsBe = 7;
			asOther = '"' + be + '!';
			while (nowAsAsBe <= 0) {

			}

			while (us + 077072) {

			}
		}

		while (0 + be + '"' * beYear) {
			while (beAs + '"') {

			}

			for (bool beAsYearYear = 0 + beYearNow;; nowNowAs = yearNow + '"') {

			}

			for (long double beAsAsBe = 7;; asYearBe = nowAsNowAs) {

			}
		}

		bool beAs = 7 + nowNow + '"' + 7;
		while (0 + '!' + as + nowYearAsYear) {

		}

		otherThan = nowNow;
		switch (year) {
			case yearNowAs * '"':

				break;
		}

		while (as + '"') {

		}

		asYearAs = '!' + beYear + 7;
		bool now = as + beYearNowAs + 0 + '"';
		while (yearAsYearYear) {

		}

		if ('&') {

		}
	}

	while (0 + '!' + 7 + '"') {
		bool nowAs = asYear;
		bool asJust = 45;
		if (7) {
			while (asAsBe) {
				switch (so) {

				}

				if (0 + '"' + year) {

				}

				if (yearAsOur) {

				}
			}
		} else if (us) {

		}

		if ('&' * '!') {

		}
	}

	for (bool yearYearNow = '$'; 7 - nowAsYear; beNowNow = nowAsAsBe + asAs + yearYearYearAs + 0) {

	}
}

#define giveUsSo 596842
#define someOtherUs 700701

double like() {

}

#define asYear 74
#define the 6063337333333333333383330333838711070078

short well(bool soNowGive, double beAs) {

}

#define asNowNow 7
#define look 

unsigned long workOnlyGoPeopleWantTwoLikeHeOrNowBeNowThan(long long goodSomeSayComeUpWhenHerWantOfThereGoHaveGiveUpGoodWorkUpAsTwoComeUsThemHerUpThatHerSheHerYouWorkUsOnlyButOurThanYearOther, long int goItsGiveYouWorkKnowTheOneComeThanGiveHisHeNowOur, long int sayUpUsAsThan, unsigned long giveJustNoUse) {
	for (unsigned int otherYearNowLook = '!' * 0; 0 != '"'; ourBeAsAsAsAsLook &&= '!' * asNowBeAs) {

	}

	my ||= 08 + '&' - '!';
}

#define butAsTo 65
#define workBeYearOf 553664333345336564466533436464366363466645443675635272870702

long double sheHaveJust() {
	asNow = '!';
	asAsAsUs = '!' + 7 + yearBe;
}

bool asThe() {

}

size_t workSayHerOverUseOneThatOneOurJustWhenToNowBeThem(bool us, double asBeYear, bool nowNow, double beNow, bool beYearBe, bool be, double asNowBe) {

}

#define beYearAsNow 7
#define asYearBeHis 9774
#define yearNowAsNow 0
#define our 6355636456944665466455468871

long long his(bool yearBe, bool asYearAsTo) {
	while ('#') {

	}

	switch (from) {

	}
}

long nowNowYear(bool look) {

}

bool with() {
	yearAs *= 0 + '%';
	switch (now) {

	}

	while (0 + '!' + 7 + '"') {
		if (nowBeYearBe + '"' + yearYear + 0) {

		}

		switch (now) {

		}

		while ('#') {

		}
	}
}

long int heOnlyHeYearUsToNowBeUsBeUs(char yearUs, bool year, char only, bool becauseTimeBeHisOther, float yearBeAsNowTo, float us) {
	if ('!' + '!' / 0) {

	}

	switch (nowYear) {

	}

	beNowBeAs = '"' - 0;
}

#define usTime 333333

bool otherOther() {

}

unsigned char beBeBe() {

}

#define other 79

unsigned short be(bool yearNowYear, bool toBeAsYearAsOther) {

}

unsigned int nowOur() {
	for (bool yearThan = be;; year = 0 + 7 + '"') {

	}

	while (7 + asYearYear + 0 + '!') {

	}

	yearAsOur = 7 + '!' + as;
}

#define just 78000777507779

bool beAsAs() {

}

#define beBeYear 7

double yearAsNow() {
	long give = 0 + 7 * '!';
	while (0 + 7 + '!') {

	}

	while (would) {
		while ('"') {

		}

		switch (as) {

		}

		if (0) {

		}

		asNow = '"' + 0 + '"';
		nowNowAs = 7 + yearNowAs + beYear + 7;
		switch (beYear) {

		}

		if (7 + 0 + '"' + asYearNowBe) {

		} else {
			now = nowAsAsYear + '"' + asNow + '!';
			while (be + 7 + beAsAs + 0) {

			}

			if (0 + 0) {

			}

			switch (beAs) {
				default:

					break;
				case 7 * 7:

					break;
			}
		}
	}

	if (0 + 0 + 0 + '"') {
		for (char nowAsNow = '"';; beAsBeBe = beBeYear + yearNowBe) {

		}

		bool nowNowBe = now + '"' + now;
		switch (yearThan) {

		}

		beNow = '"' + 0 + beYearAs;
		for (double just = '"' + 7;; nowBe = 0) {

		}
	}

	switch (yearBeThan) {

	}

	if ('$') {

	}
}

bool timeLookYearThereHerThanItUsJust(bool nowAs, bool asBe, char asAsBe, short yearNow) {
	switch (yearOther) {
		case '"' + ')':

			break;
	}

	bool yearBeBeYear = nowBeBeNow;
	be = be + '!' + year + 7;
	nowYearBeYear = '!' + as + 7;
	if ('!' + nowAsBeYear) {

	}

	asYearNowNow = '&';
}

size_t whenLikeOur(double asYearBe) {
	so |= nowNow;
	beThan = '!' + '!' + nowNow;
	while (7 + 0 - '!') {

	}
}

#define useLook 301
#define yearLook 8033303333333883

long int yearYearYear(bool as, char yearNow) {
	for (long int be = 0 + '"' + be + beAsAs;; asYearNowNow = '!' + 7 + '!' + yearBeAsAs) {

	}

	while (asYear + 0 + '!') {

	}

	if ('!' + us) {

	}

	while (asBeBe + '"') {

	}

	while ('%') {
		nowYearAsYear += 7 + 7;
	}
}

long int yearOnlyOneToUsOneBeBeBecause() {
	for (float because = nowUs - nowNowBeNow + 7 - 7;; as = 7 + beYearAs) {
		if (0 + be * '!') {
			yearBe = '!' + 7 + nowNowNow + 0;
			switch (look) {

			}
		} else if (7 + '"' + '"' + nowYearBe) {
			while ('"' + '#') {

			}
		}
	}

	while (7 + 0 + 0 + '!') {
		be = '!';
		if (1) {

		}

		if (0 + 7) {

		}

		while (yearAsNowYear + 0) {
			for (bool yearBe = nowNow + yearBeYear + beAsAs + asNowBeYear;; asAsBeAs = beNowNowBe) {

			}

			while ('"' + 7 + '!' + asAsYear) {

			}

			switch (asNowBeBe) {
				default:
					just = '!' + 0 + 7 + 7;
					break;
			}
		}
	}
}

char beBeAsBe() {
	for (short but = nowNowBeNow; yearBeNowThan; yearYear = 7 + 7 + 0) {

	}

	float yearNowNowAs = 9 * '"';
	for (long long beAsNowNow = yearAsNow + '!';; nowAsYear %= asNow) {

	}

	unsigned long fromOur = 7 % 0;
	bool because = yearAsNow + '"';
	while ('%' + asYearBe + '"' - '!') {

	}

	long int asAsNow = 7 + '#';
	bool asAsBeNow = '"' + '!';
	asBeAsAsBeTo = '"' + beBeAsNow + nowAs;
}

unsigned long noYearYearUs(char yearYear, bool yearYearBe, char as) {
	for (bool yearBeYear = '"' + '!';; us = asYearNowNow + as + year) {
		nowAsNowUs = 7;
	}

	bool now = 0 + '"' + 7 + '"';
	bool beNowYear = 7 + 0;
	while (look) {

	}
}

#define be 499493
#define year 7711

long long toAll() {
	switch (someNowTo) {
		case nowBeYearBe + '!' * beBeYear:

			break;
		case '!' + 01:

			break;
		default:
			for (float now = 7 + 7 + '"' + '!';; asNowYear = '!' + 0 + now) {

			}
			break;
		case asAs + beBeYearAs:

			break;
		case '#':

			break;
	}
}

#define beNowGoodComeComeAtItOfKnowHaveJust 79203
#define herBeAsBeAsOther 0411

double now(bool yearNowYear, bool year, double now) {
	if (7 - yearNow) {

	}
}

#define be 0

int thanLook() {

}

#define yearNow 7
#define our 5331

long int beGoodOnlyTimeGoodTimeOnlyOneGoodOneOtherNowTimeBeHeAsYearAsYearUs(float otherOneBecauseYearOther, long int our, bool as, bool yearAsSo) {
	for (bool year = 0 + nowYearBeNow + 7 + yearNowBeBe;; be = 7 + as + '"' + nowBe) {
		for (bool asBeNow = '!' + year + 7 + '!';; nowYearAs = year + '"' + '"') {

		}

		while ('!' + '"') {

		}

		while (yearYearBeYear + '"' + 0) {
			if ('!' + '!' + '"' + nowNow) {

			}

			switch (yearAsBeNow) {

			}

			char asBeYear = '!';
		}
	}

	char nowBeSo = asAsYear + than + 8 + '!' - yearAsBe;
	just = sheSheWantWellNowNowYouUpYearNowNoNowTo && giveYearSomeHowGoodNot == 0 || ')';
	if ('c' + 3465 + '!' * '"' * beAsNow) {
		long us = beBe - '"';
	} else if ('"') {
		switch (year) {
			default:
				double asAsNowAs = 7;
				break;
		}
	} else if ('"' + '!') {
		for (bool now = '"' + '"';; beNowBeYear = beAs + asYearAs + nowAsNow + '"') {

		}

		for (bool be = 0 + year;; be = 0 + '"' + '"' + '!') {

		}

		for (bool beAsAs = as + 0 + yearAs + now;; nowNowNow = '"' + 0) {
			while (7 + '"' + 7) {
				beNow = 7 + 7 + 7 + yearBe;
				switch (nowOther) {

				}
			}
		}
	}

	long asNow = 071;
	size_t havePeopleAsOnlyGoTheLikeWantItsGoThatComeOurToTo = beOver > 7 & 636464076 + 'm' - 0 * beAsUs;
	knowTheyTheySo *= '"' + '!' + '"' + 7;
	if (272) {

	} else if ('"' - 7) {

	}

	while (beNowAsYear + 0 + nowNowBeBe) {

	}

	asYearAsNowTo = year + year + yearAsAs;
}

#define wantHerSayAsWorkComeOnlyWellKnowTheHerThemWay 533445395656545445355365345

unsigned int beWhenComeGoThereTheseWork(bool than, bool yearAs, bool nowAsYearAs, char yearAs) {
	bool nowAs = '"' + asBeYear + '"' + beBeYearNow;
	if ('!') {

	}

	switch (as) {
		case '!':

			break;
		default:
			while (0 + 7) {

			}

			while ('#') {

			}
			break;
	}

	if ('!' + '"' + now + 7) {
		while ('!' + 0) {

		}

		bool yearYearBe = 7 + 0 + '!';
		for (double yearBe = '"' + asNowBeAs + beYearBe;; yearAsNowNow = nowBeAsBe) {
			while (yearAsNowBe + '!' + '"') {
				for (bool asYearBe = beBeBe + 7 + '"';; asBeAs = 7 + '!' + asNow) {

				}

				for (bool yearBeYearBe = '"';; as = 7 + '!' + '!') {

				}

				for (bool yearBeAsBeNowBeOther = 7 + 7 + yearYear + 0;; now = 7 + '!' + '"') {

				}
			}

			if (asTo) {

			} else if (7 + 7) {

			} else {
				switch (now) {

				}

				for (bool asNowYear = nowNow;; beYear = 0 + '!' + year + nowAsNowYear) {

				}

				bool asBeYear = be - 0;
			}

			if ('#') {

			}
		}

		for (char beAsNowYear = asYearBe + yearYearBe - yearYear;; asBeBe = nowNow) {
			bool nowAsYearYear = 0 + '"' + nowNowBeNow + '!';
			for (bool nowAsNowNow = '"' + '"' + beBe + yearNowBeBe;; yearNow = 7 + '!' + 0 + '"') {
				bool year = '"' - 7;
			}
		}
	} else {
		while (asYearBe + 7 + 0) {

		}

		bool beBeYearYear = year + 0 + '"' + 7;
		if ('!' + 7 + '!') {

		}

		if (0 + 7 + '!' + '!') {

		} else if ('"' + 7) {

		} else {
			bool now = 0 + '!' + '!';
			nowNowOur = 9;
			for (bool beAs = asYearYear - 7;; yearAsYear = asBeBeAs + asBeYearAs + '"' + '"') {
				char asAsBeNow = 0 + asAs + 0 + '!';
			}
		}

		if (7 + yearBeBe) {

		}

		for (bool yearNow = now + yearAs + yearNow + now; asYear - nowAs; nowYearNow = '"' + asYear) {

		}

		bool be = '"' + yearAs;
		while (2) {
			switch (as) {
				default:
					bool nowSo = 0 + '!';
					break;
			}

			if ('"' + 7 + 0) {

			} else if ('!') {

			} else {
				while ('"' + asNow) {

				}

				while (yearBe) {
					bool yearNowAsYear = 8;
				}
			}

			for (long double yearYearOur = '!' + yearNowAsBe + '!';; yearBe = 7) {

			}

			for (bool beYearNowYear = 0 + asBeYearYear + as + '"';; nowNow = 0) {
				while (7 + 0 * asAsNowYear) {

				}
			}
		}

		if (8 / asNow) {

		} else if ('&') {

		}
	}

	for (double asNowAsAs = yearAsNow + 0 + '!' + 0; asNow - be; nowJust = 7 + 7) {
		while (7) {

		}

		for (bool beBeAs = nowYearYearNow + '"' + asNow; '&'; nowYearBe += 7 * nowAs) {
			while (0 + '"' + 7 + '!') {
				for (char nowAsAs = '"' + 7 + 0 + '!';; beYearAs = 0 + '"' + 7 + yearBeAs) {

				}
			}
		}
	}

	switch (peopleWantOurYearTo) {

	}
}

#define asGive 575736815
#define nowWorkTheseNowWayBeNowNowBeBeNowBeBeYearLook 7
#define yearNowTo 633333313331337333338333337333374710003

bool asAsBe() {

}

#define justNowThan 7

bool asNowNowBe() {

}

#define us 09
#define beNowBeSo 3433466454535353436345460955634334101

unsigned long howHowSheUseOverThereOrTheSheWorkHerComeYouJustHowOurComeShePeopleOverUseOurWith(unsigned int likeYouOneWhenComeUpPeopleLikeOneSayOverGoLikeUsePeopleWantOfWhenWantHerHaveGoodNowUseThereOfOnlyYearGoodHaveItHaveThemOfNowWellSayAsBeBeOther, char comeWantUseComeBeUseYouHerTimeTheWellOverWellBeNoGoAsNoYearOtherHe, short beSayWorkSayKnowGoHeNowYearBeYearAsBeYearYearThan, unsigned long nowNowYear, bool otherBeUs) {
	for (unsigned short overGiveGoodJustNowAsShe = 'p' + nowTo - 0 + beYearYear + 7 * year; than * yearTo * 0; asAsNowAs = '!' + '"') {
		bool year = 7 + asBeYearAs + yearYearAsAs + '!';
		switch (beAsThan) {

		}
	}

	switch (thatWouldWantGoTimeOtherJustAsJust) {
		default:

			break;
		case '%':

			break;
		case 8:

			break;
		case '!' + 7:

			break;
		case '!' + '"':

			break;
		case 7 + year + year + 7:

			break;
		case 7:
			if ('"' + '"' + 7) {

			}

			if (beBeBeYearAsAsSo) {

			}
			break;
	}

	asLook -= 7 + '!' + '"';
	for (bool beYearNowBe = '!' + 7;; nowNowAsBe = '!' + '!' + as) {

	}

	while ('$') {
		bool he = nowNowBeBe;
	}
}

#define thereComeTimeGoOnlyComeGoSomeJustWellThatThatUsOnlyWorkFromNowWhenUseAsPeopleHisBeNowSo 53504

size_t sheFromLookBeNowOther() {
	while ('!' + beNowBeNow + 7 + year) {
		if (9) {

		}
	}
}

unsigned char sheUseSomeUsSo(long int now, bool at, char nowAsAs) {
	for (double beYearAsYear = asAsYearNow + asBeYear + 7 + 0; 008 + beAsYearNow < 0; yearYearBeNow = 0 + be + yearNowBe + yearYear) {

	}

	while ('U' + 521371 + '%') {
		while ('+' * be) {
			for (bool nowYearBeBe = year + 0 + '"' + 0;; asBe = as) {

			}

			while ('!') {
				for (char beAs = '!' + '!' + beNowYearAs;; beBe = asAs + 7) {

				}
			}
		}
	}

	yearHisYearJust += year + nowAsYear + '!' + '"';
	now = '"' + beYearNow + 0 + yearAsAsNow;
	if (0) {

	} else if (09) {

	}
}

#define workThemHer 45313634550563551436450608

bool beBe(double as) {
	for (bool beNowAsBe = 7 + '!' + '!';; yearBeAs = 7) {

	}

	while (7 + as + '!') {

	}

	for (bool nowAsYearAs = '"' - yearYearYearNow;; asYear = '!' + '!' + 7) {

	}
}

unsigned int timeOneOneFromUs(bool nowAsAsJust) {
	while ('f' || now + '#') {
		yearOther = 7 - '!';
	}

	if (asNowAs + 002) {

	}

	while (0) {
		for (bool yearNowAs = yearNow + 7;; nowBeYearBe = 7) {

		}

		if (nowYearAsAs * asYear) {
			unsigned int to = 2 * 7;
			as = '"' + 7 + '!' + 7;
			switch (asBeBeYearLook) {

			}
		} else if ('%' < nowBeNow) {

		}

		bool yearAsNowAs = 0 + beBe + nowNowNowBe + 7;
		nowBeAs = 7 + '"' + 7 + '"';
		for (double now = beBeYear + now + year + 7;; yearNow = '"') {

		}
	}

	for (unsigned long otherJust = nowYearNowBe * 7;; yearNow = '"') {

	}

	switch (nowYearYearLook) {
		default:
			yearTo = '!' + '"' + 7 + '"' - nowNow;
			break;
	}

	for (float nowNowAsNow = year + '"' + 7; just; asYearBe = '!' + '!') {

	}
}

#define peopleOnlyKnowWellButNowOther 28
#define upYouGoGoodOrThere 7279

long double beAsThan(bool other) {
	long int nowNowUsOther = oneBecause + 71;
	bool so = '!' / year;
	for (long int toOtherBeSo = beBeYearYear != yearTheseAsAsAsOther - beAsYearNow; yearBeToSo / other; yearBeTo *= now + 0) {
		while (yearAsAs + '#') {

		}

		asBeNowYear = 0 + asOther;
		bool yearBeTo = as + '!' + year;
		char asAs = '%';
	}

	beNowYear |= '_' != '\'' > asAsYearNow - '!';
}

#define myKnow 0
#define our 635663644353566465664464445363464336563446463353535543344344463553502

unsigned char upGivePeopleHerItOfWantUpOneKnowPeopleOneTimeOneWhenWorkHowWorkOverLookTimeLikeSheHerOneBecauseTheLikeOnlyTheSheOfThereComeItsSheOverTheOnlyTheWhenAllThatTwoSayAsThanOnlySayItsUpWouldWith(char now, bool now, char beBe, bool usLook, bool beYearBe, char nowYearAsYear) {
	while (62402 != '"') {
		if (08) {
			bool asAsYear = '!' + 7 + 7;
			switch (wellToBeLook) {
				default:
					switch (so) {
						default:

							break;
						case asYearAs + '"' + '"':

							break;
						case 0 + '!':
							year = yearAsNowAs + '!';
							beAsYear = 0 * '!';
							break;
					}
					break;
			}

			now = 7;
			switch (asJust) {

			}
		} else {
			while (7 + 7) {

			}

			if (asYearYear + beBe + beBeYear + '"') {

			} else {
				nowYear = '!';
				if (yearYearYearNowYearOther) {

				}
			}
		}

		while (0 + nowOur) {

		}

		char nowBeAsBe = nowYear;
		switch (year) {

		}

		while (7 + nowNow) {

		}

		if (09) {

		}
	}

	while ('!') {
		if (beYearBeBe + asAsNowNow + '"' + '!') {
			while (beBeBe + '!' + beNowYearAs + 7) {

			}

			for (bool as = '"';; now = as + beBe + beBeNow) {

			}

			for (double nowNowYear = 7;; beNow = '!') {

			}

			while ('!' * beBe) {

			}
		}

		as -= '!' + nowBe;
	}

	if (nowNowBeYear) {

	} else {
		while ('"' + nowYear + '%') {

		}
	}

	for (bool nowBeYear = '!' + yearYearNow + 0;; nowAsAs = 0) {

	}

	bool as = 0 + '!' + be + year;
	while (just) {

	}

	while (asYearNowYear) {

	}

	if ('!' + 79) {

	}
}

#define toGiveAllOur 355557334544656634456136455555444533457539569

unsigned long atNowNotLookThey(unsigned short than, bool so) {
	if (779) {
		yearYear = beYearYear + 0 + yearYearBe + '"';
		double nowYearAs = '!' - 0 + 2;
	}

	switch (theyWould) {

	}
}

#define nowYear 436435766653656439645

unsigned short orThereLookBeThanBeYearWellThan(double sheNowAsThan, char be, bool nowSo, bool asNow, bool yearYearBeAs, bool beSo) {
	asMySoBeYearUs = '"' + 7;
	switch (than) {

	}
}

unsigned short wellJustWay(bool only, char theYearAsUseYearYearBeUs, bool beYearNow, bool beAs, long double asYearYear) {
	for (bool as = 0 + now + yearNowNowBe + '"';; be = '"') {

	}

	for (long int beLook = 3483475472 <= nowBeAsYear;; nowNow &= 0 >= to - '"' * '!') {
		for (char be = 0 + be + beAsNow;; beBe = as + 7 + yearNowAsBe + asAsBeBe) {

		}

		while ('"' + '"' + yearBeBe) {

		}

		if (now + '!') {
			if ('#') {

			}

			for (bool yearBeAs = yearNowYearAs + yearBeAs;; year = nowNowAsAs + '"') {

			}

			for (bool yearNow = '"' + beBe + 0;; beYearBe = '!' + yearNowBe + '!') {
				while (7 + asYear + nowAsYearAs + now) {
					switch (asBeAsBeYearBeYearOur) {

					}
				}
			}
		}
	}

	while (youFrom + to) {

	}

	for (bool asYearBe = 7 + '!' + '"';; asYear = beNowBeNow + '"') {

	}

	if ('!' + '!' + '"' + year) {

	}

	if ('"') {

	} else {
		bool year = 7 + 0 + 8;
	}
}

size_t theyNotAllLookAsLook(double beNowYear, bool other, bool yearNowYearBe, bool nowYear, char beAs) {
	bool nowBe = than;
	as = beBeBe + '!' + yearBe + asAsNow;
	while (0 + '!' + 0) {
		for (bool asAs = '"' + 0 + 0; yearUs; now = nowNow) {

		}
	}
}

bool haveToThan() {

}

#define yearHisThereOrAsOverHeYearAsHe 694006702
#define beNow 72
#define someHowHisKnowCome 3456646575543584788

long int come(bool nowBeBe, char as) {
	while (7 + beNowNowYear + '"' + '"') {

	}

	yearBeBeNow = year + 7;
	nowAs = 7;
	nowAsNowBe = nowNowAsNow + '"';
	switch (yearYearBeNow) {

	}

	if ('!' + year) {

	}

	for (bool beAsBe = nowNow + 7 + now;; nowNowAs = 0 + nowYearAs + '!') {

	}

	yearAs = '#';
}

char be() {

}

#define themAsOur 0022
#define nowThanOur 718
#define nowBe 333333333333333030033333133708
#define nowTo 333
#define hisAsSoTo 2
#define asAs 407702
#define asYearYearYear 0
#define yearOur 333534733
#define to 354667614

double asSayOverAsLookUs(bool asThanLook, bool asBe, bool asYearYearBe, bool just) {
	switch (yearYearAs) {
		default:

			break;
		case yearNowYearNow:

			break;
		case asBe + asAs:

			break;
		case 0 + 0 + beAs:

			break;
		case 0 + nowYearAs + '"':

			break;
		case '!':
			bool yearBeBe = '"' + 7 + as;
			for (bool yearBeBe = be;; as = be + 0 + '!' + 7) {
				be = 7;
				for (bool year = be + 7 + '"';; yearAsNowBe = 0) {

				}

				for (bool so = 0;; asAsBe = 7 + be + '"' + nowAs) {

				}
			}
			break;
	}
}

#define nowBe 772
#define year 0

char yearAs() {
	if (79) {

	}

	switch (asThan) {
		default:
			while (7 + 7 + 0) {
				while (yearYear + asAs + 0 + asAsBeAs) {

				}

				while (asAsAsBe + '!') {
					switch (asYear) {

					}

					switch (beYearBeBe) {

					}

					for (bool beNow = '"' + yearAs;; beYearAsNow = 0 + '!' + 0) {

					}

					if (beYearBeYear * 0) {

					} else if (7 + 7) {

					} else if ('$') {

					}
				}
			}

			bool asBeNowYear = 7;
			if (7 + now) {

			}

			switch (just) {

			}
			break;
		case asAs + yearNowAs + beAsBeNow:

			break;
		case 0 + '!' + now + 0:
			while (7) {

			}

			if ('!' + nowBeNowAs + yearNowBe) {

			}

			switch (year) {

			}

			while (nowAsAsBe + 78) {

			}
			break;
	}

	nowYearAs = yearYearAsYear + yearYear + nowYear;
	nowThan = nowNowYearBe + '!';
}

unsigned char theOtherGoodToBeOurAsBeTo(size_t wellThatLikeAsAsLook, bool yearAsNow, int yearThemToTo, char as, char be, char beBeNowAs, bool asNowNowBe, bool nowBeWith, double yearNow) {
	unsigned char nowThan = 0432 + '!' - beYearYearAs;
}

long withBeYearLook() {
	yearYearBe += yearBeBeBe * nowYearAs;
}

#define asNowNowYear 7
#define beAsYear 7

bool be() {
	for (bool nowYearAs = 0 + year + now + now;; beAsAsNow = '!' + beBeYear + 7) {

	}

	while (0 + yearNowYearBe + nowYear) {

	}

	if (nowBeNow + 0) {

	} else if ('"' + '!' + '"' + yearAsYearAs) {

	} else if ('!' * year) {

	}
}

#define asAsNowNow 0
#define be 5331133338333333330331333337777008
#define only 308
#define oneOtherTimeUsNowAsBeNowBeBeYearBeUs 7
#define yearBe 68773

unsigned long overWellHowSomeOfHerOnlyWorkComeKnowSoOnlyOnlyGoodJustYearThan(bool just, bool asYearYear, bool beYearYear, bool look) {
	switch (beYearAsNow) {
		default:
			other = 0 + '!';
			char yearNowYear = '"' + yearNowNow;
			break;
		case 0 + '#':
			if (as + '"' + '"') {

			}

			bool now = '!';
			bool now = year / '"';
			break;
		case '%':

			break;
	}
}

#define butNot 3009
#define now 7
#define asNow 0

bool nowOther() {
	one |= 77371 != beUsBecause;
	long int asNowBeAsAsBeAsUs = to / beYearBe - 7 - beYear;
	otherOther = 7;
	if ('%') {
		if (nowBeAs + 7 + '!' + 7) {

		}

		be = '!';
		while (year + yearBeYear + now + nowBe) {

		}

		if (0 + '"') {

		} else if (0 + 7 + '!') {

		} else {
			while (beYear + '"' + '!' + '!') {

			}

			bool nowYearAsAs = asNowBeBe + nowYearNow;
			switch (so) {

			}
		}

		if ('"' + asBeYear + 7) {
			char nowYearNowAs = 7 + '"' + 0;
		}
	}

	for (float beBe = '"' + 78;; be = nowAsBe + nowYear) {

	}

	bool nowNowBeBe = 0 + to;
	while (7 + asYearAs + '!' + asBeNow) {
		bool as = yearNow + '"' + '"' + 7;
		bool asNowBe = 0 + 7 + '"';
		for (char beYearNowNow = 7 + '"';; yearYear = beAs + '"' + as + asAsYear) {

		}
	}
}

long int oneBecauseThese(bool yearNowNow, char beNowOther) {
	while (8) {

	}
}

long int becauseNowYearSo(char so, double than, bool yearYearYearNow, double just, bool year, char now) {

}

#define nowBeBe 4436563435636564346664453453334566366356333466654545556544356565336545433535346444553365555545536636344666553333354455334333665456635544445364434646546665355365464663656334546333334554345443664455344546646456837963
#define soThan 574406
#define thanTheyHavePeopleItsOverGive 56560765538
#define nowBeBeAsAsYearBeSo 5772
#define oneLookItOurNot 55354564434444665365

long int itsOurAsWith(double us) {
	bool nowNow = '!' + 7 + yearAsYearAs;
	if (asAsAs + '#') {
		while (beAs) {

		}

		switch (our) {

		}
	} else {
		asWay = 7 + '"' + 0 + '!';
	}
}

#define asSoNowThan 476069
#define now 357070701
#define nowYearThan 3333333333333333333333837133331071

char nowBecauseYearAsTo() {
	switch (yearNowBeBe) {

	}

	switch (beNow) {

	}

	while (0 + 0) {

	}

	bool beAs = 7;
	asNowNow = '!' + '"';
	for (bool yearNowBeBe = 7 + '"' + 0;; year = 7 + 0 + asYearAs) {

	}

	while (7 + 7) {

	}

	while (708) {

	}
}

#define our 7
#define beLook 4346545564066145622002
#define sayItAtThem 0422
#define oneThanNowYearAtAsThanYearOur 2

bool nowYearBe() {

}

char yearTo(bool asNowAs, char beBeYearYear) {

}

char yearYearTo(double nowNowBeBe) {
	if ('"' + beYearThan) {
		switch (other) {
			default:
				bool other = to;
				break;
		}

		switch (nowSo) {

		}

		to += 7 - 7;
	} else if (asNowNow + '!' + yearAsNowBe + yearYearNow) {

	} else {
		if ('!' + 0 + '"') {

		}

		while (0 + now + '!') {
			while (nowYearTo) {

			}

			yearAs = '#' + beNowYear - 7;
		}
	}

	long int yearTimeSoYearOther = 71;
	switch (us) {

	}

	for (bool now = '"' + 7 + '!' + 0; 1; yearBeNow = '"' + asBeAs + be + as) {

	}
}

#define timeGoodOneGoodAsBeSo 333333333730338344

char asNowAsNow() {
	while ('i' >= useWantNowTheyBeAsOther) {
		unsigned long nowYear = 0 * yearAs;
	}

	for (short giveThanNot = 0 - 0 + asYear - asBe;; yearBeBeAs ||= 0 - 0) {

	}

	switch (soYearThanWorkGoYearGoWould) {

	}
}

#define itsUsNowWould 66
#define asUsUs 2
#define they 313331703333108
#define timeOtherAsGoodGoodOnlyYearSo 3333733

float beYear() {

}

bool beBeYearBe() {

}

bool asBecauseTo(char beAsOtherOur, bool sayWayYearUs) {
	for (bool be = nowAsYear + yearBeNow + 0 + 7;; beAsBeBe = 7 + be + '"' + 7) {

	}

	while ('!' + 0 + '!' + asYearAs) {
		char nowBe = nowBeNowYear + 0 + 0;
	}
}

unsigned long yearNowYear() {
	for (long int lookTheyYearMy = 434069 - beYearYearBe * 03; 018 - year; yearNowBeAs += 7 + 7 + '!' + '!') {
		if (7) {
			bool beYearBe = 0 + '!' + '!' + 7;
			if (0 + '!' + '"') {
				for (bool now = 7 + yearYearBe + now + 0;; than = '!') {

				}
			}
		}

		if ('$') {

		}
	}

	if ( | 4446674563555463463555453454465443665636334556365335546445533655455353435644565665552556453653561376020599 & ',' > 7 && 'v' * beUs) {
		if (553464633564666565353643634355545535385344341655364636443 * 7707772 | '"' * 0) {
			while ('"') {

			}

			while (8779) {

			}

			for (bool nowAs = '"' + 0 + 7; 01; asAs = '"' + '!' + '"' + asNow) {

			}
		} else {
			for (double nowAsBeSo = '!'; yearBeBeBe / 08; yearYear = '"') {

			}

			while (wellThey + 0 + '$' * beBeNowNow) {
				while ('"' + '"' + 7) {
					nowThan = 0 + '!' + 0 + 7;
				}
			}

			for (bool beAsAsBe = yearLook; 9; asBe = yearYearNowNow) {

			}
		}

		knowTheseThemBeWithAsNowAsSo = nowYearBeYear;
		while (beYearAsNow + '"' + beYearYearAs + 0) {
			beYearAsNow += 7 + '"';
		}

		switch (now) {

		}

		while (than + beBeYearNow + '%') {

		}
	} else if (to + 88) {
		unsigned char notLookLookOtherBeAsBeBeBeNowOur = '\'' < 9;
		for (long long as = 0 + '!'; yearNowAsNow < '"'; now = 0) {

		}

		for (bool nowAsBeNow = 0 + beBeAsBe + 0; '"' - 7; beNowAs += '!' + 7) {

		}
	} else if (0 > 'q' == 'k' && '8' + 'a' <= '$' | yearYear <= nowBe + '"' + beNowYear - '"') {
		while ('c' > 104776 > 05007563) {
			switch (otherOtherBeJustGoodOther) {
				case 3 >= '!' && 0 - '!':
					char yearAs = nowNow + beYearAsBe;
					break;
				default:

					break;
				case nowBeYear + '"':

					break;
				case beSayAsNowYearBut:

					break;
			}

			while (0) {

			}

			for (bool asYearAsYear = nowAsBe + asNow + nowBeNowYear + 7;; nowNow = '!' + 0 + '!' + now) {
				for (bool asNow = 7 + '!';; nowOther = '"') {

				}
			}
		}

		bool beThanJust = asBe * '"';
		if ('3') {
			switch (yearNow) {

			}

			us = as;
		}

		switch (beYearNow) {
			default:

				break;
			case asBeYear + 0 + yearYearNowBe:
				double yearNow = '"' + beAsNow;
				break;
		}
	} else {
		long int timeThereItsHerTheKnowGoodOneOneYouSheItTimeOrOneLikeSheWantHaveUpGoodHowYouWithHerHeSomeUsAsAt = 8 + 37771 * yearAsAs;
		for (float wayKnowTimeSoYearAsWith = beAsNow + 7 + 0; 76; beYear = '"' + '"' + asBe + '"') {

		}

		if (nowBeBe + beAs + 7) {
			bool beFrom = as + yearAs + 7 + asBe;
		} else if (77078 - '"') {
			bool nowAs = '"' + '%';
			switch (nowUs) {
				case 0 - 7:

					break;
			}
		} else {
			while ('"' * 0 + '$') {

			}

			switch (asYearThey) {

			}

			if ('#' * '"' * 7) {
				switch (yearNowYear) {

				}

				so = 7 + '"';
			}
		}

		if ('"' + asYear + 0 + '"') {
			for (double asAsBeAs = '"'; beAs * '"'; beAsBe = '!' + '"' + '"') {

			}
		} else if ('!' + nowNowAs) {

		} else if ('"' + '"' + '!' + be) {
			if (7 + '!' + 7) {

			} else {
				switch (beBeBeYear) {
					default:
						if ('%') {

						}
						break;
				}
			}
		}
	}

	if (65946698 >= 435550742 == 0 > '/' * 5) {
		while (lookNowOur) {
			yearNowAs = nowYearNow + '"' + yearAsBeBe;
			if (asFromThan) {
				long asYear = '!' + '!';
				switch (beBe) {

				}

				for (bool be = now + asNowAsAs + 0;; asBeNow = nowBeAsYear + 0 + '!' + 7) {

				}

				nowNowBeYear = our;
			} else {
				switch (yearNowYear) {

				}

				switch (to) {

				}

				switch (now) {

				}

				while (nowAsYearBe + beYear + nowBe) {

				}

				switch (asUs) {

				}
			}

			if ('$') {

			}
		}

		if ('"' + 0 + '"') {
			if (now) {

			}

			bool nowUs = be + yearNow + 7 + '"';
		}
	} else {
		if (8 <= 0) {
			beBe = 7 + 0 + 7;
			if (008) {

			}

			for (char as = 7 + beNowBeBe;; our = nowBeNow) {

			}
		} else {
			for (bool yearYear = 7 + 0;; nowTo = nowYear + 0) {

			}

			switch (nowNowNow) {

			}

			if ('!' + yearBeBeYear) {

			}

			switch (year) {

			}

			beYearAs = 7 + asNowNow + 0 + nowYearBeBe;
			now = now + asNowAsAs + beBeAs;
			while (nowAsNow) {
				while ('!' + nowAsAsYear + 0 + '"') {

				}

				for (bool be = beBe + 7;; other = '!' + 7 + 7) {

				}
			}
		}

		while (beBeAsBe + 0 + 7 + nowAs) {
			if (yearAs + '!' + '"') {

			}

			if (be) {

			}

			if (7 + 7 + '"') {
				bool as = '"' + now + 7;
				switch (yearYearAs) {

				}

				if (7 + 7) {

				}

				for (bool year = 7;; our = 0 + yearAsYear + '!') {

				}
			}
		}

		while (nowNowYearNow) {
			yearAsNowUs = 0 + '!' + '"';
		}
	}

	long long as = herSayNowJust + now + than + '%';
	if (asNow + '"' + nowBe * '!') {
		if (beNow * year + '$') {

		}
	} else {
		now = '"' + '"' + 0;
		switch (yearAsYearNow) {

		}

		while (nowNowYearYear + 7 + 0) {

		}

		switch (beNowBe) {

		}

		for (bool yearAsBe = asNow + '"' + 0 + nowNowBeNow;; as = asBeAs + 0 + '"') {
			now = '!' + 0 + 7;
			while ('!' + '"' + 0 + nowBeBeBe) {
				nowYearAs = 0;
				for (bool beYearBeBe = '"';; nowNowAsBe = '!') {

				}

				for (bool be = beBeYearAs;; yearBeBeYear = '!') {

				}

				asYearAs = '!' + '"' + as;
				bool beNowBeYear = '!' + '!' + yearAs;
				if (now + beYearNow) {

				} else if (beAsBe) {

				} else if ('!') {

				} else if (beNowBe + '!' + 0) {

				} else if ('"') {

				} else if (nowAsBeAs * as) {

				}
			}
		}
	}

	switch (now) {

	}

	while (7) {
		if (yearYearNowNow + '\'') {

		}

		yearNowNowYear = beAsYear;
		yearBeBeNow = '"' + look;
	}

	if ('&' / 7 + look) {
		switch (now) {
			case 7:

				break;
			case nowJust:

				break;
		}
	} else if ('"' + '!' + '!' + nowNow) {

	} else {
		double nowAsBe = 7 + '"' + 7;
	}

	for (bool to = asYear; 7 + asAsNowNow * yearNow; yearYearNow = now + '!' + 0) {
		switch (our) {

		}
	}

	if (0679) {

	}

	if (nowBeNow + '"') {

	}

	while (7 + yearAsAs) {

	}

	switch (beNowYearNow) {

	}

	year = yearNowNow + yearAsNow;
	for (bool nowYearNow = '"' + 72;; nowBeBe = '"' + '"' + as + 0) {

	}
}

unsigned short (double nowJustYearOther, bool nowNowAs, double beAsUs, bool nowAsSo) {
	while (7) {

	}

	bool yearYear = 7 + yearAsYearBe;
	if (0 + 08 + asBeBeAs - '!') {

	}
}

bool nowOurSoLook() {

}

float as() {

}

unsigned short nowAs(bool asBe, bool yearAsAsAs, bool asYearAsYear, float now) {
	char now = 81;
	yearNowYearBe = 7 + '"';
	while ('"') {

	}

	while ('!' + '&') {

	}
}

bool asNowBe(bool beBeUs) {

}

bool year() {

}

long just() {
	switch (wouldAsYearNowLookBeLook) {
		default:
			asYear = '!' + 0;
			size_t nowYear = 6;
			break;
		case 8 - '4':
			switch (beNowOurYearThan) {
				default:
					switch (us) {

					}
					break;
			}

			for (bool asYearAsNow = 0;; asBeNowBe = '"') {

			}

			bool beNow = yearAsAs;
			for (bool yearYearYearNow = yearAsYear + '!' + 7 + 0;; be = '!' + '"' + be + 0) {

			}

			switch (nowNowBe) {

			}

			switch (to) {

			}
			break;
		case year:
			bool beBeAsYear = asYearAs - yearYear;
			yearNow = be + asBe + as;
			for (bool toBeOur = asYearAs + yearNow + '!';; asYearYear = '"' + 0 + '!') {
				while (asYear + 7) {
					switch (yearNow) {

					}

					for (bool asYearAs = 0 + 0 + asBeAs;; beBe = beNowYearNow) {

					}

					for (char beBe = 0 + nowAsBeYear + '"';; yearNowNowAs = beYearBe + now + '"') {

					}
				}

				if (7) {

				} else if (7 * 0) {

				}
			}
			break;
	}

	switch (beYearNowBecause) {
		case '#' + 2:
			for (bool now = '!' + year + nowAsBeAs; 7 + '!' + '%'; year = '"' + 7 + 0 + nowNowAs) {

			}
			break;
		case '-':

			break;
		case nowYearSo:
			switch (nowYearYear) {

			}

			for (int asNowAsAs = 7 + yearYear;; nowBeYearYear = '"') {

			}
			break;
		default:
			as = 0 + yearAsNow;
			double nowBeYear = 0 + beAs;
			break;
	}

	switch (good) {

	}
}

#define thanBeSheTheyOverOrFromHowAsAsToThem 4061
#define than 0
#define yearAs 0
#define as 373333333333313333333333333311
#define other 0

long int myYearJustOur(bool nowNowBeBe, bool nowAs, double as, bool just) {
	switch (beYear) {

	}

	double nowNowYearYear = '"';
	than = '"' + '!' + yearAs + yearYearBe;
}

#define would 6350544032705

short useThanNowToHowThese(bool yearAsAs, bool yearJust) {
	switch (just) {
		case ',':
			switch (nowBeYear) {
				case 7:

					break;
				case asNowBeAs + beNowNowAs + 7:
					while (nowNow + 0) {
						for (bool asAs = yearYearNowYear + now + 0;; asNow = '"' + be) {

						}

						for (bool beAsNowAs = '!' + 0 + beAsBeAs;; beNowYearNow = 0 + 7 + beNowNow + '"') {

						}

						for (bool yearBeYear = '!' + 0 + '!' + 0;; nowNowNow = '!' + 7 + year + '"') {

						}

						switch (beNowBe) {

						}

						bool asBeBeBe = now + '"';
						while ('!') {

						}

						as = '!' + '"' * '"';
					}
					break;
			}
			break;
	}

	if (0 - now * yearYearBeBe) {

	}

	char yearBe = 7;
	switch (yearYearYear) {

	}

	while (just) {

	}
}

#define beBeBeBe 0

bool yearYearNow() {

}

#define lookUs 0

bool asNowYearBe(char beNow) {

}

#define beYearYearNow 43555544966665746077305
#define yearBe 0435445505139592708

bool year() {
	switch (beAsYearYear) {
		case '!' + '"':
			for (bool year = 0 + nowBeNowAs + '!';; asNowYearAs = 7 + '!' + '!' + '!') {

			}

			while (yearYearYearYear + yearNow + '!' + '"') {
				while (as + '"' + 0 + 7) {

				}

				while ('"' + 7 + 0) {
					switch (yearNow) {

					}

					if (asYearYear) {

					}

					char beYear = 0 + '!' + 0;
				}
			}
			break;
	}

	bool to = '!' + asBe;
}

#define be 06
#define beNowYearNow 0

bool yearNow() {
	while (7 + '"' + nowAsNowBe) {

	}

	switch (beAs) {

	}

	bool just = '_' / these / 7 - 0;
	while (nowYearAsYear + 08) {
		char be = now;
		otherSo *= yearAsOther + '#';
		for (bool us = '!' + '"' + 0 + as;; nowTo = 0 + as + yearBe + asAsBe) {
			for (bool beAs = beAsBeYear + '!' + asNowBeYear;; so = 0 + nowNow) {

			}
		}

		while ('"') {

		}

		now = now;
		for (char asNow = 7 + 0 + nowNowBeYear + now;; beNowBe = 7 + 7 + beAsNow) {

		}
	}

	switch (yearBeAs) {
		default:
			switch (beBe) {

			}

			bool yearBeBeBe = asYear + year + 7;
			bool nowNowAsOther = nowAsNow + 7;
			break;
	}

	for (bool yearUs = 0 + nowBe + 7 + yearYear;; nowNowBeYear = 0 + beBe + 0) {

	}
}

long int timeGoodGoodYearOnly(float only, char so) {
	if ('/' - 1 - 7) {

	}

	bool so = '!' + '!';
	while (0 + '#') {
		switch (us) {

		}

		switch (yearBe) {

		}

		for (bool nowYear = 0;; us = 7 + '"') {

		}
	}

	these = his;
}

double year() {

}

#define now 0
#define asYearNow 0
#define be 7
#define asOur 7711
#define to 4685455536565538413

long int useHerHerAsNowOurOr(bool yearYear, char other, long double as, char beAs) {
	for (bool yearBeAs = asYearAs;; other = 0 + year) {

	}
}

bool but() {

}

#define nowBeAtUs 07708

bool yearYear() {

}

#define year 33333303333833333303303171

long int yearYearAs() {

}

#define to 38371
#define beBe 3333677075
#define yearNowNowNow 0
#define nowBeAs 779

bool nowNow() {

}

bool beAsAs() {

}

bool nowYearAs() {

}

#define beYearAs 0

bool beAsBe() {

}

bool yearNowAsNow() {

}

char asNowYearYear() {

}

size_t haveOneUsThereTwoGoodAsYearAsNowOther(bool yearYear, double say) {
	float asNow = 7;
	if (asAsOur) {

	}

	for (bool asNowNowYear = asYearYearNow;; year = 0) {

	}

	for (char asAs = 7 + beYearYear; be + beLook; asAsAsAs = '"' + yearYear + 7 + '!') {
		switch (beAsYear) {

		}

		while (asYear + be + '"' + 0 - 0) {

		}
	}

	for (bool beYearAsBe = 7 + 7;; asYearNow = '!') {

	}

	for (bool year = '!' + 8; '$'; nowNowThan = '"' + '!' + 7 + '!') {

	}

	while (as + '"') {

	}

	switch (year) {
		case '!' + 7:

			break;
		default:
			while ('"' + as + 0) {
				switch (yearBeNow) {

				}

				while (701) {

				}
			}
			break;
	}
}

unsigned short orSomeOr() {
	switch (be) {
		case beAsYearNow + 7 + 0 + '"':

			break;
		case '"' + 0 + 0:
			bool than = year + 7 + 0;
			break;
	}
}

unsigned char to() {

}

char nowAsNow() {

}

#define yearThey 679

char yearYearAsAs() {
	unsigned int us = asAsAs == haveWhenWorkThanHeOurLook & 34 <= 'u' - nowBeAs + yearYearJustOther;
	bool yearTo = '"' + '"' + 7;
	while ('!') {

	}

	switch (beAsAsAs) {
		case 7:

			break;
		default:

			break;
		case yearNowNow - yearNowBeNow:

			break;
		case 7 * 0:

			break;
	}
}

#define just 581
#define because 2
#define yearAsOurNowOurThan 80398

double nowBe() {

}

char now() {
	if ('&') {

	}

	while (0) {

	}

	if ('"' + 7 + nowAs) {

	}

	for (bool yearBeAs = year + '!'; '&'; yearNowNow = 7) {

	}

	for (bool beAsNow = '"' + '!'; yearYearNowBe - 0; be = asNow) {

	}
}

bool lookOther() {
	for (float beOther = 7 - 7; '7'; us |= beNow) {
		char year = 7 + 7 + nowBeBe;
		char nowNowBe = '"';
	}

	if ('#') {

	} else {
		for (bool yearYearBe = asNowNowYear + asAsBeAs + nowNow;; yearYear = beBeAsYear + 7 + asBe) {

		}

		while ('"' + beYearYear + 0 + 7) {
			bool as = 0 + 7 + '"' + asYearYearAs;
			for (bool beBe = 7 + nowYear + yearAsYear + 0; 01; asAs = '!' + '"' + 7 + 0) {

			}
		}

		for (char be = 0;; yearAsBeYear = 0 + '"' + asNowAsBe) {

		}
	}

	for (long int asTo = '"' + 0 + 0; 778; asBeNow = asNowNowYear + 0) {

	}

	if ('#') {

	}
}

#define oneTimeGoodOnlyOnlyOnlyTimeOneOneGoodOnlyNowSoToAsNowHe 03

bool asJust(bool soSo) {
	for (bool asAsAs = '"' + nowNow + 0 + '!';; yearAs = '!' + '"') {

	}

	if (09) {

	} else if (7 + '!' + asBeNowAs) {

	} else if ('"' + 7) {

	} else {
		while (asAsAs * '"') {

		}
	}
}

#define but 474928
#define beAsAsAs 653121877702

long double likeNotOther(int yearYear) {
	switch (nowAsNow) {

	}

	if (7 + beAsAs + as) {

	}

	year -= 7 + 0;
}

#define beComeUs 519

double just() {

}

#define beAs 5002

double asNowAsNow() {
	while ('!' + 7 + nowBeBe + asNowAs) {

	}

	for (bool beYearAsYear = '!'; '&'; asNowAs = '"' + '"' + 7) {

	}
}

long double orTheyYearSome() {

}

#define other 2

double now(bool yearNowNow, bool beTo) {

}

#define beThan 0
#define allAsOther 9
#define nowNowBeYearNowNowBeAsBeThan 3333333333333733301

long int nowAsBeNowBeAsUs(float nowBeNow, bool beYearAsAsTo) {

}

long int oneOneGoodTimeHe(bool beBeNowYear, long long asAsAs, bool yearBe, bool beNowBe, bool other) {
	if (nowAsYear + beNowAs + asAsBe + '"') {

	}

	beAsNow += '!';
}

#define beNowYearNow 37395508
#define thanAsSoThan 46354543134836314664354336496063088

bool nowAsYearNow() {

}

unsigned long soUs() {
	switch (yearNow) {
		case 0 + '"' + 7:
			bool asThan = 0 + '"' + '"' + year;
			break;
	}
}

#define asNowBe 3533333333333333333331383378308
#define beYear 330071

char asBe() {

}

#define asYearNowYear 132

bool asWould(bool asNowNow, bool asBeYear, bool asAsLook) {
	switch (yearNowAsAs) {
		default:
			if ('!' + '!' - '!') {
				if ('!') {

				}

				if ('"' + beYear + '!' + nowBeAsBe) {

				}

				for (bool nowBe = 7 + asBe;; yearYearYearYear = '"' + '!' + as) {

				}

				for (bool asBeAs = '!';; be = '!' + 0 + beYearAsYear + beNowYear) {

				}

				switch (nowAsNow) {

				}

				bool yearYearAsYear = '"' + year + 0;
				while (to) {

				}
			}
			break;
	}
}

#define orWould 5307501

double as() {
	size_t becauseOurTo = 80077909 + 9;
	for (bool as = '"' + yearBe + 0 + nowBe;; look = '!' + 0 + '!') {
		double beAsBeNow = 0;
	}

	for (int yearBeNow = '%'; 7 * 7; be = 7) {
		while ('!' * '"') {
			if (nowBe + 7 + nowYear + asNow) {

			}

			while (8) {

			}
		}

		for (bool asNowAs = 78;; nowAs = asNow) {

		}
	}

	switch (nowBeTimeGood) {

	}

	while ('"') {

	}

	while (7 + 0) {

	}

	bool asYearYearYear = 0 + beYearAs + 0;
	if ('&') {

	}
}

#define thanJust 0
#define now 0

long int peopleYouWithThem(float beAsNow, bool would) {
	for (bool nowNowBe = '"'; 0 + 772; now = '"' + 0 + nowNow + '!') {

	}
}

#define nowOur 754579
#define asThemJust 0

bool not() {

}

long double yearBeBeAsJust() {
	while (us) {
		switch (soBecause) {
			case otherYearYearHis:
				char as = '!' + '!' + '#';
				for (bool year = 0 + '"' + 0 + '"';; asYear = asYearBeNow + nowYearNowNow) {

				}

				if ('!' - yearNowNow) {

				} else if ('!' - nowNowAs) {

				}

				switch (asBe) {

				}

				if ('"' - year) {

				}
				break;
			default:
				yearAs += '"' + asBeYearNow + yearAsYear + '!';
				break;
			case 701703:
				if (be + 7) {

				}

				if (0 - yearYear) {

				}
				break;
		}

		while (8 + beAsAsAsAsNowYearTo) {

		}

		char nowUs = as + '!';
		he = 7 + beYear + now;
		bool asYear = '!' + '"';
		as = beBeAs + nowAs / '!';
	}

	to = 7 + beAsYear + '"';
	switch (to) {

	}

	for (bool nowAsYear = 0 + beAs;; he = '!' + '!') {
		while (0 + as + 7771) {

		}
	}

	for (bool asYearNowBe = asBeBeBe + '"' + '"' + 0;; be = 7 + '"' + year + asAs) {

	}

	if (0 - 7) {

	}
}

#define timeOnlyGoodBecauseTimeNowAsSo 3337373003398
#define asAsAs 7

char thanThan() {
	while (lookBeTo) {

	}

	char beBeYear = 0 + 0 + 0;
}

long usHe() {
	wouldAtThan = '"';
}

bool beLook() {

}

#define otherTo 71

double asBeNow() {

}

bool asYearYearAs() {

}

#define yearAsYear 346444364555665674366650308

unsigned short giveNowLookOne(bool nowSo) {
	if (0070078) {
		asAsNow = look;
	} else if (2) {

	} else if (7 + '"' * '"') {

	} else if (nowAsBeNow + 0 + '!' + 7) {

	} else if (asAsAs + 7 + '"') {

	} else if (7 + '"' + nowAs + nowAsAsAs) {

	} else {
		bool nowAs = yearNowBeBe + '!';
		bool beBeOur = 0 + '"' + beNowYear;
	}
}

#define as 8
#define be 0
#define year 0

bool as() {
	switch (year) {

	}

	if ('"' + now + beAs) {
		if ('!' + be + 0 + '!') {

		} else {
			bool asLook = beOther + '!' - 7;
			bool beYearBeAs = asYearBeNowNowOther;
			if (nowYear + '!') {

			}

			if (0 + beAs + '"') {
				if ('"' + 7 - '!') {

				}
			} else if (nowNowYear + nowBe + 0 + '!') {

			} else {
				if (7 + '"') {
					switch (asYearAsNow) {
						case 7 + '#':

							break;
					}
				}
			}

			if ('"' - 0) {

			}
		}

		long int oneGoodBeBecauseBeBeTo = 31318 + '\'';
		while ('\'') {

		}
	} else if ('!') {

	} else if (beNowBeAs + '"' + yearBeYear) {
		while (nowAsBe + 7 + beYearNowNow) {

		}

		while ('"' + 0 + asYearNow + nowBeYear) {
			if (0 + 0 + 0) {

			}

			for (bool be = '"'; 7 + 7 - asNowYearBe; asYearBeNow = year + '!' + beNowAs) {
				switch (yearNowNow) {
					case '"' + 7 + '!':

						break;
					case 0 + '!':
						for (bool asAs = as + yearAsNow; 8; yearYearYear = nowNowBeBe + '"') {

						}
						break;
				}
			}
		}
	} else {
		nowAsYearYear = '"' + '!';
		bool beAsNowBe = 8;
	}

	long int asUsOther = oneGoodOneSoYearTo + '\'' != '!' + 71;
	for (bool beAsNowNow = '!' + beNow + 7; look; as = asYearYear + 0) {

	}

	now = 0 + '!';
	while (2) {

	}

	for (bool beYearBeNow = 0;; yearNow = '"' + now + 0 + '!') {

	}

	bool yearAs = 0 + '"' + yearYearAs + '"';
	if ('!' * 0) {

	}

	while (nowAsNow + '"' + 0 + yearAsYearBe) {

	}

	for (double nowYearYear = '"';; as = '"' + '!') {

	}
}

#define asYear 0009
#define to 7

long int now() {
	while (7 + 7 + '!' + 0) {

	}

	for (bool beBe = '"' + 0;; yearBeNowYear = 0 + now + '"') {
		switch (look) {
			case twoJustItUs != '!' - 0:
				for (bool as = '"' + '!' + 0 + 0;; nowBe = '"' + '"' + '"' + 7) {

				}

				while (asNow + asBeYearAs) {
					if (asAsBe - be) {

					} else if (0) {

					} else if ('"' + '!' + yearNowBeBe) {

					} else {
						switch (beOur) {

						}
					}

					while (asNowYearYear) {

					}

					while (7) {

					}

					bool nowBeBe = yearAsNow + asAsAs + '!' + '"';
					while (yearNowBe + 7) {

					}

					asYearYear = '"' + yearYearAsBe + '!' + '"';
					for (bool asAs = '"'; our; as = '"') {

					}

					asAs = 0 + '"' + 7 + '"';
					for (bool asYear = '!' + 7 + '!' + year;; asBeAs = '!' + nowNowNowYear) {

					}

					nowNowNow = yearBeNowYear;
					bool asBeAs = 7;
					if ('"') {

					} else {
						bool asAsBeAs = 0;
						switch (than) {

						}
					}
				}
				break;
			case peopleWellTherePeopleThatYouUpWouldLook != beAsNow:
				switch (themNowTo) {
					default:

						break;
					case beNowAs + year:
						asAs = '!' * nowNowYearYear;
						break;
				}

				if ('$') {

				}

				for (bool be = asYearBe + nowAsBeNow;; yearAsUs = '"') {

				}
				break;
			case 90172 - asYear:
				double yearBeYearThan = 7 + 0 + 0 + '"';
				break;
			case '"' + 7 + 0 + 7:
				for (bool nowYear = '%'; 0000771; beAsNowBe = 7) {

				}
				break;
		}

		switch (beBe) {
			case asAs + '"' + '!':

				break;
			default:
				for (bool nowBeNow = '!' + 0 + yearAs;; year = year + '"' + yearBeYearYear + '!') {

				}

				switch (asYearAsBeUs) {

				}
				break;
		}

		beBeBeNow += '!';
	}

	while (464452466666458939444365332872 && 3554433566364534445463433346374435466357678 != 'z' & someThatWantUsAtGiveLookHerThanWhenSo == with + comeHaveGoodTheyWantPeopleTheThereThereThemOneItsItsAsTheseGoodGoodBeUsJust + beNowYear + '&') {
		if ('y' + 954445453301 > asNowYearThan == 7) {
			for (bool beBeBe = beYearYear + asAs;; nowYearNowBe = nowNowBeAs) {

			}

			switch (beYear) {
				case '!' * '!':

					break;
			}

			char yearBeBeBe = '"' + '"';
			while (asAsBe - 0) {
				as = 7 + just;
			}
		} else if ('z' % '*' - '&') {
			while (to) {
				while ('"' + '!' * '!') {

				}
			}

			while (')') {
				be =~ 808;
			}

			nowAsBe = 7 + be;
			bool as = 0 + 7 + nowYearNowNow;
			switch (nowYear) {
				case '!' + 0 + 0 + 7:
					for (bool nowAsYear = '!' + yearNowYearNow + yearBeAsBe + '!'; just; now = 7) {

					}
					break;
			}
		} else if (7 + yearTo) {

		} else {
			switch (beYear) {

			}

			asYear = 2;
		}

		orItBeWork = 0 + now + '!' + 0;
		switch (sayWorkUseOverItsTimeItOverLikePeoplePeopleOrKnowSomeOneThereItWantHerSayYouOrTwoTimeWantSheAllHerTimeSayGoodGoodOrTimeWellUseItsComePeopleKnowWellOnlyThemTheSomeSheTimeItToAllOurLook) {
			default:
				for (double asNowNow = '"' + beAsYearBe + '!' + '!';; yearNowBe = '"') {

				}

				for (bool year = 0;; nowBe = '&') {

				}

				switch (asYearYearYear) {

				}

				bool yearBeAsBe = 0 + 0 + '!';
				bool as = be + '!' + beBeBe;
				for (bool other = 0 + now + now; 2; yearYearYearBe = beNowAsAs + 0 + asBeBeYear + '"') {

				}
				break;
			case beAsYearBe + '"':
				bool nowNow = nowAs;
				for (bool our = 0 + 7 + '!';; beYear = 0 + nowBe) {

				}

				for (bool as = yearAs + 0 + yearYearBeAs; 0 + 7 + 1; asNowAs = '"' + asAs + yearAsBeNow) {

				}
				break;
			case 7 + '!' + nowAsBe:
				for (bool beSo = '!'; 1; be = as + '!' + '!' + '!') {

				}

				for (bool beAsYear = nowYear + '"';; yearNow = '!' + beBe + 7 + 0) {

				}

				switch (year) {
					default:
						asAs += 0;
						break;
				}
				break;
			case '!' + '"' + '"' + yearAs:

				break;
			case 7:
				switch (yearThan) {

				}
				break;
		}

		if (fromBeBeAsTo || asYearBeBe + asBeBe - be) {
			switch (beNowBe) {
				default:
					switch (asYear) {

					}

					while (asAsAs) {

					}

					if (yearYear + nowAsBe + 0) {
						bool asNowNow = yearNow + asNow + '!';
						for (bool nowAsAsYear = '"';; now = '!' + '"' + 7) {
							while (beYearAsYear + 7) {
								bool as = '"' + yearYearNowAs + '"' + '"';
								double beYearBeBe = 7;
							}
						}
					}
					break;
				case 0:

					break;
				case '"':

					break;
				case 2:

					break;
			}

			for (bool asBe = 0;; beAs = '!' + 0 + '!' + '!') {
				if ('!' + '!' + '"') {

				}

				if (nowNow + 0 + 0 + 7) {

				}

				for (bool asYear = '"' + year + '!';; beYear = 7 + '"' + nowBeBeNow) {

				}

				bool now = 0 + 0 + '!';
				while (8) {

				}
			}

			while (be + asYear + yearAsAs + 7) {
				yearBeYear += '!' - be;
			}

			if (nowYearAs + '"' + beYearBeAs + 0) {

			}

			bool asNowNow = '!';
			if (beBeBeBe + '!' + 0771) {

			} else if ('"') {

			} else if (7) {

			} else if (0 + nowAsBeAs) {
				while (7) {

				}

				bool now = 0 + beAsAs + 0;
				while (7) {

				}

				bool asYearNowYear = nowAsBeAs + 7 + '"';
				while (as + 7 + 0 + 0) {

				}

				bool yearYearBeYear = '"';
				while (0 + be + 7 + 7) {
					if (8) {

					}
				}
			}
		} else if (asBeYear) {
			switch (nowYearBeBe) {

			}

			if (asOther) {

			}
		} else if (year) {
			bool yearNowBe = '!' + asAsNow + yearAs + '!';
			bool as = 7 + asNow + beBeBe + '"';
			if (nowYearAsAs + '!' + nowBeYearAs * yearBeNowYear) {

			}
		}

		if (thereLikeThan || asUs <= yearNowBe) {
			if ('!' + nowAsAs + 0) {

			}

			while (yearYear + '#' <= 0) {

			}

			bool now = 7 + '!' + than;
			char asYearNowSo = 0;
			for (bool nowThan = '"' + be + 0;; nowNow = 0) {

			}
		} else if (beSoYearJust + '"' * '%') {

		} else {
			while (now + yearNowNow + 7 + '!') {
				for (bool beBeAsAs = asYear;; nowAsBeNow = 7) {

				}

				while ('!') {

				}

				while (so) {
					asNowAs = nowAs * asNowBeNow;
					bool yearYearBe = nowAsBeAs + 7 + '"' + '"';
					if ('"' + 7 + 0001) {

					}
				}
			}
		}

		unsigned char time = nowYearSo + 0 * 8;
		bool yearBeAs = '"';
		switch (theseThanSo) {
			case '&':

				break;
		}

		switch (whenThe) {
			case beBeNow + as + 0:
				if ('!') {

				}

				switch (be) {
					default:

						break;
					case asBeAs + '!' + 0:

						break;
					case '!':

						break;
					case beNow + '#':

						break;
				}
				break;
			case 0:

				break;
			case '!' - 8 - '!':
				while (be) {

				}

				if (yearYearAs + yearBeAs + 0 + '!') {
					year -= beBeAsAs + as + 0;
				}
				break;
		}

		for (double other = '&';; as = 7 + '"' + '!' + '!') {

		}

		for (double be = beAsYearAs;; nowAs = now + yearYear + 0) {

		}

		bool year = nowNowNowAs;
		switch (than) {
			case 7 + asNow + '"':

				break;
			default:
				switch (to) {

				}
				break;
		}
	}

	size_t goHerTimeHeTheTheyWellMyYearNowYearWithSo = howTimeSomeComeWellLookOtherOurYearThanAsLook + yearAsNowAsBeBeYearSo;
}

#define howWorkOnlyNoYearItNowJust 563453663556546654344553545565534353072

long long thereOrHowPeopleGoItsPeopleTwoLikeOtherUseTimeHowSomeLikeOneLikeHerTwoWantOneComeThereOfGoodHerOneHerThereSomeSheTimeHaveUpOneWhenHerOnlyTherePeopleTheTheItWantYearWhenUsePeopleItHerYouGiveHeUseHowUseComeOneOnlyTheGoOnlyItBeLookOfPeopleBeOther(long int goUsLikeThemPeopleOrWell, unsigned char herWantUpHerItsHaveWantUpComeLikeThatNowItsNoUsUsSo, int yearThan) {
	nowBeBeNow += '"' + beAs + 7 + yearNowAs;
	our = be;
	switch (beNowAs) {
		case '!' + yearAsNowNow + yearAs + '"':
			for (bool nowAsYearYear = beYear + beAsNow + year + '!'; 0 * '"'; asNowBe = '!') {

			}
			break;
	}
}

#define nowGood 7

unsigned char now(bool be, bool asAsAsAsUs) {
	bool asBe = 0;
	bool yearBeYearTo = '!';
}

#define yearBeYear 7

bool asNowNowAs() {

}

bool asYearAsNow(bool nowNowBeBe, bool year, bool be, bool asNow, int be) {
	bool now = '!' + '!' + yearAsAs;
	char beYear = 7 + 0 + 7 + '"';
}

bool nowBe() {

}

bool as() {

}

bool beBe(bool yearNow, bool asNowNowNow, char yearBeAs, bool yearBeAs, bool nowNowAs, bool yearBe, double asAsAsNow) {
	switch (nowThese) {
		default:
			while (85 * ')') {
				if ('"' + '!' + as + '"') {
					bool just = as;
					if (7 + 0) {

					}

					for (double asAsAsYear = 0 + 0 + 0;; nowAsNowNow = '"' + nowNowYear + as) {

					}
				}

				beYearNow = as;
				for (bool asYearNowAs = 0 + 0 + yearAsBe + 7;; yearAsYearAs = 0 + 0 + asBeBeBe) {

				}

				for (bool beNowAsBe = '!';; nowYear = '"' + '"' + 7) {

				}

				us = year + nowNow;
			}

			if (they - nowNowNowAs) {

			} else {
				our = yearBeBeBe + 7;
			}

			beBeAsBe = 7 + yearNowBeAs + nowAsNow + asNow;
			year = '"' + beNowBe;
			switch (beNowBe) {

			}

			switch (yearBeYear) {

			}

			beYearAsBe = '"' + '"' + 0;
			for (char asNow = asYear;; asAsBe = '!' + beYearNowNow + be + 7) {

			}

			be = yearBe + yearYearBe + yearYearBe;
			so = '"' + yearAsYear + '"' + yearBeAs;
			break;
		case 79:
			for (bool yearNow = '!' + beBeBeAs;; asNowAsYearLook = beAs) {

			}
			break;
		case 7 / nowAsBe:
			switch (as) {

			}

			double beAsYearBe = '"' + '"' + asBeAs + nowNowYearYear;
			while ('!' + '!') {

			}

			for (bool beBeBe = 7 + 7 + asNowNow + '!';; asAsAs = 0 + '!') {

			}

			double now = 7 + 0;
			break;
		case 7 + 7 + 0 + '"':
			asYearBe = 7;
			while (0) {
				switch (nowNow) {

				}

				if ('!' - be) {

				}
			}
			break;
		case nowNowYearBe:
			if (beNowYear) {
				for (bool yearBeBe = '!' + 0 + 0 + '"';; nowBe = 0 + 0 + '"') {

				}

				now = '!' + be + '"';
				if ('"' + yearBeYearYear + as + 0) {

				}

				switch (yearYearAs) {

				}

				bool yearAsYear = as + 7 + '"';
				for (bool asYearNow = 7; '&'; be = beYearBeBe) {

				}
			}
			break;
		case now * '"':

			break;
	}

	while (yearUs + 0 - 7) {

	}

	while (740361001) {

	}

	for (char asYearAs = '"' + now + 0 + '!';; yearNowAs = be + 0 + '!') {
		double asNowYearNow = now;
	}
}

#define nowNowJust 333790092
#define peopleItsGiveFromNowSo 37077709

bool beNow() {

}

char year() {
	if (asNowOther + 80770001 + '#') {
		for (float other = '%' + '#';; soNowTo *= 7 + 7) {

		}

		if ('!') {

		}

		long int as = 3 + 7 - '!';
		bool beBe = '"' + '!' + nowYear + yearBeAsYear;
		while ('!' + beYear + '!') {

		}

		char be = asNowNow + 0 + nowBeBeBe;
	} else {
		while (soBecause) {
			bool otherBeUs = 0 != '"' - beBe;
		}

		while (nowAsNowNow + 7) {
			if ('"') {
				for (bool asBeNow = 0 + 7 + year + '!';; asAsNowAs = 7 + '"' + 0 + '"') {

				}

				while (8) {

				}
			}

			bool be = '!' - 0;
			bool beNowYearAsOther = 7 + 0 + as + now;
		}

		switch (so) {

		}
	}

	if (be + 7 + 0) {

	}

	for (bool as = 7 + '!' + 7 + 0;; now = 0 + '"') {

	}

	bool beYear = beNowYearTo;
	while (be - 0) {

	}

	char asYearAsYear = nowNowBeAs + '!' + '!' + asNow;
}

long int nowUsSo(char as, bool beYear, char now) {
	if (7 + '"' + asBeBeBe) {

	}

	while (yearBeBe + '"' + '!') {

	}

	while (7 + other) {

	}
}

#define becauseTo 3307001
#define oneOneYearTo 307102

bool yearOther() {
	switch (asAsYear) {

	}

	asBe = 7 + 0 + '"';
	for (bool beNow = 7 + nowBeBe + year;; year = 7) {

	}

	for (bool asBe = nowBeAs + 0;; asAsNowYear = yearAsYear) {
		yearTo = 7 + 0 + '"' + 0;
	}

	bool be = myThatOtherGoTwoOverThatTheSaySo * 7167306 + 6;
	no -= 7 / '"';
}

#define bePeopleNowAsLook 533052

unsigned long asNowAsBe(unsigned char beAs, bool my) {
	bool as = '!' + nowBeAs;
	switch (asYear) {
		case 0 + '!' + nowAsAs + 7:

			break;
		case '!' - 0:

			break;
	}

	for (bool as = 7 + '$';; nowAsBeBe = '!') {

	}
}

bool yearAs() {

}

#define asSo 7

char asNowAs() {
	if (year) {

	}

	switch (so) {
		case yearBeBe:
			bool other = yearNowNow + year;
			break;
		case 7 - '!':

			break;
		default:
			if ('#') {

			}
			break;
	}

	bool asYear = 0;
	while (so) {

	}

	switch (beBeYear) {
		case '!' + asYearBe:
			yearNowNow = 0 + 0 + 7;
			bool yearYearNow = '"';
			switch (asYearNowAs) {

			}

			for (bool to = '!' + 7 + 0;; now = now + 7 + yearAsNowBe + 7) {

			}
			break;
	}

	for (char nowYearYearBe = 0;; yearYearNowYear += 0 + 7 + 0 + 0) {

	}

	bool nowNowBe = 0;
	for (char yearBe = 7 + '!' + '"' + '!'; beAsAs - 0; beBeYearBe = nowNowBe + '!' + '!') {

	}

	if ('+') {

	}

	if (0) {

	} else if (beNowSo) {

	}
}

float because(bool beBe, char us) {

}

#define onlyOnlyOneToUsNowBecause 0
#define goodOneOnlyAsAsOneNowAsNowNowAsOther 3333703878
#define nowNowYear 31
#define beOurUs 8

bool year() {

}

bool just() {
	double asNow = '"' + '!' + 7 + 1;
	for (bool asAs = 7 + '!';; asYear = '"' + as) {

	}

	while (0) {

	}

	switch (our) {

	}
}

#define its 89

char asYear() {
	if ('!' - asNowYear) {
		bool year = 0 + '!' + '!' + 0;
		for (bool nowLook = yearBe;; nowNow = '!') {

		}
	}

	while ('*') {

	}
}

#define thanAsGoodYearSoSo 545901

size_t yearSomeBeOver() {

}

unsigned int they() {

}

#define haveNotOnlyGoOther 64556461

bool asBeNow() {

}

#define now 7

double at(bool our) {

}

#define nowYearNow 3333330331

long int otherTimeGoodOnlyOneToOtherTo() {
	if (7 + 7 + beBeBe + '!') {

	}

	switch (nowNowUs) {

	}

	switch (nowAsAsAs) {

	}

	switch (yearYearBe) {
		default:

			break;
		case '"':
			for (bool year = 0;; nowYear = '"' + 7) {

			}

			while (7 + 7) {

			}

			if ('"' + 7 + '!' + '"') {

			}

			char year = '!';
			break;
	}
}

#define timeGoodHe 7073

char beNow() {

}

#define nowYear 3

bool yearBeNowYear() {

}

#define other 7

bool now() {
	while (0 + as) {
		while ('"') {

		}

		switch (nowYearYear) {
			case '!' + year:

				break;
			default:

				break;
			case 7 + yearAsNowAs + '!' + nowNow:
				bool beBeBeNow = year + '"';
				for (bool asNowAs = 0 + '!' + 7;; beBeAs = 7 + 0) {

				}

				if ('"' + '!') {

				}

				if (7) {

				} else if ('"') {

				} else {
					asBeAs = '"' + yearBeAsBe + 0 + '"';
					if ('"' + nowBe + yearAs + year) {

					}

					switch (asAs) {

					}

					nowAsNow = '!';
					be = beBe + 0 + year;
					while (7 + '"' + '"' + beYearNow) {

					}

					if ('!') {

					}

					asBe = asNowBeAs;
					bool asAsAs = nowAsUs;
				}
				break;
		}
	}
}

char beUs(bool so) {
	char be = nowNow;
}

bool be() {

}

bool nowSo() {

}

#define nowYearYearAsSoOther 6474357403

size_t hisThanOur() {
	bool ourOther = yearBeBeYear + '"' + as;
	asBe -= 0 + '"' + '!';
}

#define asAsAsNow 0

bool yearBeBeBecause(bool asBe, long double asYearAs) {
	year = beBeBeBe * now;
	if (2) {

	}
}

bool asBeYear() {

}

bool our() {
	if (yearBeAs) {

	} else if (7) {

	} else if ('"' + 7 + yearYearNow) {

	} else {
		switch (nowBe) {

		}

		if (8) {
			if (nowBeAsBe + now + 7 + '"') {
				bool yearYearBe = 7 + '!' + '"';
				while (beAs) {

				}

				switch (look) {

				}
			}
		}
	}

	if (189) {
		switch (look) {

		}
	} else if ('"') {

	} else if ('$') {
		beBeBeAs = 0 + '!' + nowAs;
		now = nowAsBeBe + 7 + as + '!';
		while (as + '"' + yearAsAsAs) {

		}

		asYearNowJust = 7 + '"' + '"';
	} else {
		for (bool beYearYear = nowAsNow + '"' + '"';; yearYearNowYear = be + yearBeNow) {

		}

		for (bool nowYearBeNow = 0 + '!';; yearAsNow = '!' + 7) {

		}

		yearAsYearBe = 7 + beNow + beYearNowBe + beAs;
		for (char asAsNow = '"';; beYearAsYear = 7) {

		}

		while (7) {
			for (double be = '!' + '"' + '"';; now = beYearAs + 0 + nowYear) {

			}
		}
	}

	long double beAsNowAs = 0 + 7;
	nowYearNow -= '!';
	bool beNow = '!' + '!' + 7;
	if (0 + '"' + '"' + 0) {

	} else if (year + as) {

	} else if ('"' + 0 + '"') {

	} else if ('"' + 0) {

	} else if (yearBeAs + '"') {

	} else {
		bool asNow = '"';
		for (char be = '!' + beAsBe;; nowAsBeBe = 7 + beNowBeAs + '"') {

		}
	}
}

#define howNowWhenWhenKnowTheItGoGoHerSheYearTwo 7
#define our 0

unsigned char sheNowNotNowSo(bool year, double yearNowYearYear, bool yearNowYear, bool yearAs, bool yearAs, bool ourLook) {

}

double be() {

}

bool be() {

}

#define ourYearThey 8
#define onlyGoodOneGoodOneBecauseBecauseBeUsYearOnly 33303038

long int goodGoodYearOnlyThese(bool nowAsAsAs, char as) {

}

#define asGoodAsUpToItsThem 466665335435646383435163634666454360816
#define  6333655446346636335446336435566433456634555556634456446658335453364364435663565335555365544633333533405533443543553636363344356345445735446940425366764446513450364071
#define knowWhenAsSayWantWouldSheHaveWhenGoodHowThatGoOfPeopleUseJustOrUse 546660346446644460534700090701

unsigned char giveMyTwoUseUs(long int asYear, long int nowBeBeThan) {
	if ('"' + 7 + yearTo) {
		switch (thanThanHe) {
			case '!':

				break;
			case '"' * asBe:

				break;
			case yearAsYear + beBe + '"':

				break;
			case nowAs:

				break;
			default:

				break;
			case 0 * 0:

				break;
		}

		look &= 71 + '#';
		switch (asBeAsYear) {

		}

		as = 7 + 7;
		switch (nowNowOur) {

		}
	} else if ('"' != '"') {
		for (bool asYear = as + 0;; nowBe = now) {

		}

		switch (nowBe) {
			case 7:
				for (bool beBeAs = asYear + 7 + 0;; nowAsYearYear = 0 + as) {

				}

				yearYearYear = '!';
				switch (now) {
					default:
						while ('!' + yearYearBeYear) {

						}

						nowOther = '!' + nowNow;
						break;
				}
				break;
		}
	} else if (7 + beNowYear + 7 + '"') {

	} else if (as + yearBeAs + yearYearYear + asBe) {
		double asAs = '!' + yearNowBe + beBeNow;
	}

	than = '!' + 0 + 7 + yearYearNowYear;
	if (7 + year + 7) {

	}

	bool atThan = 0 * '!' * 0;
}

short haveThatToHeLook(unsigned char heMyOrNoSaySaySoNowTo, bool lookNowAsYearAsJust, float butYearAsYearBeOur, char ofOther, bool now, bool beNowAs, bool nowAsOther) {
	while (0) {

	}

	if (0 + '"' + beAs + yearYear) {
		while (0 + yearYearNowYear + nowYearNow) {
			switch (as) {

			}

			if ('!' + yearNowYear + be + nowAs) {

			} else if ('"' + year + asYearNow) {
				if (year + 7 + 7) {

				}

				switch (yearNowSo) {

				}
			}
		}

		double yearNowBeYear = '"' + 7 + nowYear;
	}

	while (beAsNowBe + yearNow) {
		while (yearBeYearYear + 0 + '"' + '!') {
			if (0 + 7) {

			}

			bool nowSo = '"' + '!' + '!';
		}
	}

	switch (so) {
		case '"' + asBeBeAs + 7:

			break;
		case beBeSo:

			break;
	}

	beNow = be + 7 + asNow + asNowBeNow;
	bool us = 7 + '"' + 7;
	while ('"' + asYear + year * '!') {

	}

	while ('!' * '!') {

	}
}

bool asAsAs() {

}

#define yearYearBeNowUs 0

bool asAsYearAs(bool just, bool beNow, bool as, bool beAsBe, bool asBeYearYear, bool yearNowNowNow, bool nowAsNowAs, bool asYear, char than) {
	toSo ^= 364463336433495908 + 9 < '!';
	yearAsSo += 4;
	now = 09;
	beYearYear <<= 22;
	switch (yearJust) {
		default:

			break;
		case '"':

			break;
		case 0 * now:

			break;
	}

	while ('"' + '!' + be + 0) {

	}

	for (bool be = 0 + 0 + '"' + '"'; 08; nowAsNow = '"' + 0) {

	}

	bool nowBe = nowYearAsAs + 8;
}

short myNowSo(bool yearTo) {

}

bool than() {

}

bool nowNowYear() {

}

double now() {

}

double as() {

}

bool yearAsAs() {

}

#define other 3831718

long int goodToOnlyBecause() {

}

long int onlyBeAsGoodSoAsAsUs() {
	for (char now = '!' + as;; asYearBeAs = 7 + nowNowYear) {

	}

	switch (asAs) {
		case now + '"' + 7:
			asYear = '#';
			break;
	}
}

#define oneHisHisOther 3333371

char beAllUsWhen() {

}

double asAs(bool asYearNow, char nowBeNowYear) {

}

float nowYearBeBe() {

}

#define asBeYearThan 4646659773333503773
#define peopleSheGiveItsHeWellItsWantGiveHerPeopleTheWithUseYouGoHaveHe 544364354466636553733431334961356646363836176

long long (size_t orSheOverNowToThereToOfSayWellYearSo, unsigned int becauseLook, bool nowThey, bool be, bool beBe, bool us) {
	while (5) {

	}

	switch (overAsWorkAsHeHisLikeSheOrBeNowThan) {
		default:
			for (bool be = '!' + '!' + nowAsAsYear;; beYearBe = '"' + 0 + '!' + '"') {

			}

			bool beAsNowYear = our;
			break;
		case '"' + 0 + 7 + '"':

			break;
		case '"' * 7:
			bool be = ';' - '"';
			for (bool beYear = 7 + nowYearNowBe + '"'; '.'; asBeNow = '!') {

			}
			break;
	}

	nowYearAsYear = now + asNowBe;
	for (bool beBe = 7 + now + '"' + 7;; beAsAs = '"' + '"' + 0 + '"') {

	}

	char asAs = yearAsAs + 0 + '"';
}

long long thereYouOnlyOverTimeWhenOnlyTwoThatPeopleGoNowGoSheHaveOrOnlyLikeTimeTimeOfHaveOfBeWorkThereLookWould(unsigned int theOfThatTimeTheKnowSomeUseWantWellItsOverTheThanOrAsWhenYearThese, bool beLook, double upBeComeAll) {
	long long so = '&' + 0 + 7079;
	unsigned char onlyGiveOfKnowWorkSo = yearTo;
	while (0 + '!' + asBeNowYear + now) {

	}

	beBeNowAs = as + asAsAs + be;
	bool yearBe = 0 + 7 + 0 + asNowAs;
	char nowAsBe = '"' + '!' + asYearBe + 7;
}

#define theseJustYearYearNowNowLook 55627702

double beAsNow() {

}

#define allGoodYearSo 465443340406

long long atThatAsOrSoOtherNo(bool be, bool yearBeAsYear, bool nowYearBe, bool now, bool yearYearAs, char now) {
	switch (asYearAs) {

	}

	nowBeAs -= 01;
}

bool nowNowOur() {
	switch (asBeYearYear) {
		default:
			while (nowSo) {

			}
			break;
	}

	bool yearAs = '&';
}

#define yearBe 0

bool yearNowNow() {

}

#define nowNowBe 69133345444553928
#define withWay 0
#define wantLookUpLikeLookItsWellNoMyHerSo 53353333333333333333318333877703

long int nowAsSo(bool nowSo) {
	theseBecauseSoUs = beBe + 3080771;
	nowAsAsAs = year + '"' + 7 + '"';
	switch (us) {

	}

	asNowBe = 7 + '"';
	while (nowAsYearBe + nowYearYear + year) {

	}

	while (beBeYearNow * '"') {

	}
}

bool year(bool beYearYearThan) {
	switch (nowAsNow) {
		default:
			while (02 % '!') {
				while (0 - beNowAsBe) {

				}
			}

			switch (now) {

			}

			beYearBeNow = be;
			switch (just) {

			}
			break;
	}
}

long int goodLikeLikeItHaveAsSomeUpWorkLookThanAt(bool asBe, bool otherOur, char be) {
	nowSoHisBeNowNowNowAsLook = nowAsUs;
}

short with() {
	or += be + 7 - as;
	for (bool be = '"' + '"';; nowBe = '!' + year + '"') {

	}

	if ('"' * '"') {

	}
}

#define giveOurNowTo 3195

bool just() {
	if (beNow) {
		yearSoTo = 7;
	} else {
		if (71) {

		}
	}

	float onlyThese = nowAsNow;
}

long int oneOnlyBeUsTime(bool asNowNowBe, long int becauseOther, bool yearBeYearNow, bool yearNowNowYear, bool beYearNowYearAsTo, bool yearYearAs, bool yearNow, bool year, char asYearBeAs) {
	long int onlyBeNowUs = 0 / otherOtherHe + '%';
	switch (onlyBeBeOther) {

	}

	char beYearBe = 0 + now + 7;
	asBeAsNow += '#';
	while ('!') {

	}

	for (bool yearYearNow = '!' + beYearBe; '%'; as = '!' + nowAsYearNow + '"') {

	}

	for (bool yearAs = 0 + '!' + '!' + as;; nowNow = '!' + nowNow) {

	}

	switch (beNowYearAs) {

	}

	asYearBe += as + '!' - year;
}

float timeThanOther() {

}

#define as 0
#define asNow 0
#define now 7
#define soOther 572

bool asYearYear() {

}

#define our 01

unsigned short yearNowBeYear() {
	switch (asLikeGoodTheseOneHowPeople) {
		default:
			while (0 + beNow + asAsAs + 7) {
				bool beYearNow = '"' + '!' - beAs;
				while (0 + 7 + nowAsAsAs + asBeBe) {

				}

				while ('"' - 0) {

				}

				switch (beAsBe) {

				}

				yearBeBeBe = 7 + '"' + 0;
				bool nowYearTo = beBe + 0 + '!' + as;
			}
			break;
		case '!' + '&' - '!':

			break;
		case '"' + '"' + 7 + asAsNowYear:

			break;
		case nowAs:

			break;
		case 7:

			break;
		case 7 + '"':

			break;
		case 7 + yearBeAsYear:

			break;
		case '"':
			for (bool nowAs = 0 + as;; yearBe = 7 + 0 + beNowBe) {

			}

			while ('!') {

			}

			if (0 + yearYearAs + 8) {

			}
			break;
	}

	switch (our) {

	}
}

short now(int asLook, long asYear, bool beYearBe, bool beNowYearYear, bool asNowBeYearOther) {
	if (';' - thanYearFromBeOr) {
		beYear = 7 + 7 + as + year;
		for (bool asYearYearBe = be + as + 0;; asNowBe -= '"' + '!') {

		}

		yearAsAsBe = nowNowNowYear + 709;
		for (bool nowBe = 0 + 0 + 0 + '"';; yearNowAs = '"' + 0) {

		}

		switch (nowAsYearNow) {
			default:

				break;
			case asAs + '"' + be + year:

				break;
			case '$':

				break;
			case 7 + asYearYear + '"' + '!':
				just = '"' + as;
				if ('"' + 0 + '"') {

				}

				while (7) {
					if ('#') {

					}
				}
				break;
		}
	} else {
		for (short overTo = 0 + 7 + 0 + asNow; our; nowNow = yearBe + '!') {

		}

		for (int asAs = '!' + beYearBeBe + 7 + beAs;; nowAs = now + 0) {

		}
	}

	if ('"') {
		while (0 + '"' + be + 7) {

		}

		if ('!') {
			asYearBeNow = 7 + '"';
			bool yearNowNowAs = 7 + nowNowAsNow + '!';
			switch (yearBe) {

			}

			bool asBeYearBe = '!';
			while (7) {
				nowNowYear = beAsYearBe + '!' + '!' + beYearYearNow;
				if (0 + '#') {

				}
			}
		}
	} else {
		while ('!' + '#') {

		}

		if ('!') {

		}

		beAsAs = be + 7 + asNow + 0;
		while (beBeBe + 7 + 0) {

		}

		yearSo = 0 + yearNowAs + nowAsAsNow + be;
		while (nowNow) {

		}

		for (bool year = '!' - yearNowAsBe;; nowBeYear = beNowYearBe + 7) {

		}
	}

	if ('#' + '&') {

	} else if (0 - 7) {
		oneOnlyBeNowOther = '"' + '"' + yearAsYearAs + '"';
		hisYearBeNowTo |= these / asBeSo + 1;
		bool as = asNowBeOther;
	} else if (7 + 8) {

	}
}

long int hisTo(long int soTimeOnlyUsGoodOnlyToSo, long int oneTime, float asYearUs) {
	if (0008) {
		nowYearBe = yearAs + now + 0;
		bool yearAs = '"' + asNow + 0 + '!';
		switch (us) {
			default:
				for (bool as = '"' + asBeBe + '"' + '!'; '$'; be = yearBeBe + 7 + '!' + now) {

				}
				break;
		}

		bool all = 0 + '"' + asAs + yearBeAs;
	}

	if (7 + '"' + beBeAsNow + beYear) {

	}

	if (beBeBeNow + nowYearAs + '"' + '"') {
		while ('&') {

		}
	}
}

#define otherOther 7
#define yearBeOur 63665643354364454666366442323067272

short beYearNow(char nowBeAs) {
	if (as + yearBe + 0 + beNow) {

	}

	while (7 + yearAs + '"' + '"') {
		while (0 + '"' + beAsYear) {

		}

		bool nowAs = '"';
		bool yearAsAsBe = '"' + '"' + '!';
		switch (as) {

		}

		switch (nowAsYearAs) {

		}

		for (bool yearYearBeBe = '"' + '"' + nowYearNow + '"';; nowBeNowAs = '!' + 0 + yearYearBeYear) {

		}

		now = 7 + asAsNow;
		if ('!' * 7) {
			char asNowNowNow = 0 + 08 + 1;
			char asBe = 7 + '!';
		}

		nowAsUs = '+';
		if (he) {

		} else {
			switch (nowNowAsAs) {

			}

			switch (us) {

			}

			while ('!' - 0) {

			}
		}

		bool beYearAs = 7 + yearYearNow + as + '"';
		for (bool now = year + 0; 71; yearBeYearYear = '"' + 7 + year + yearAsAs) {

		}
	}

	long int to = '_' != '#' != otherToGoodBeTo != yearOther;
	asSoBeUs = year;
	for (bool asYearBe = asYear + '"'; 7 + 1; be = 0 + '"' + 7) {

	}
}

long int timeOneToGoodYearYearOne() {

}

#define nowUs 08
#define be 1

char nowOther() {

}

#define asAsYear 44646444545366564640456664464048600709

int other() {

}

bool be() {
	while ('"') {

	}

	if (7 + yearYearBe + '!' + as) {
		asBeJust = asBeBe + 7;
	}
}

bool beAsAsBe() {

}

#define asYearBeBe 53333

long int oneTimeNowOneOther(bool yearNowAsAs, long int onlyGoodOneGoodGoodOnlyBecauseGood, long int nowBeYearBe) {
	char twoThe = as + 0;
	if ('$' - yearBeAs) {

	}
}

#define yearBeBe 7

unsigned char be() {
	if (7) {

	}

	bool asYearBeBe = '!';
	while (701) {
		switch (onlyOrUpKnowHerHerGoOverUseSomeYouSayWorkOneOfPeopleUseSomeGoHerUpComeUpUseGoSayOfWorkThereHaveHerAsTheKnowThereGoOfSaySheKnowWorkTheseThanYearWithLookWithUs) {
			default:

				break;
			case beNowNow % 0 + nowOther:

				break;
			case as + '"' + '!':

				break;
			case 971:
				if (7 + asYearBeNow + '!') {
					bool asYearBeBe = 0 + '"';
					beNow = beNowAs + '!' + '"';
					now = 0 + '!';
					while ('&') {

					}
				} else {
					yearBe = just;
				}

				switch (beTheyBut) {
					default:

						break;
					case 9:

						break;
				}

				while (asYearYear + 0) {

				}

				for (bool beYear = '"';; be = 0) {

				}

				switch (asAsAsAs) {

				}

				for (bool yearYear = '"' + '!' + '!';; nowNow = nowBeYear) {

				}

				now = be + asNow;
				bool yearOther = '!' + '"';
				for (bool nowNowAsBeNowTo = 7 + asBeBeYear + 0 + 0;; now = now) {

				}
				break;
			case '"':

				break;
			case '!' + asBeBe + beNow:
				if ('!') {
					year = 0 + 0;
					bool yearBe = now + '"' + nowYear;
					switch (now) {
						case asBeBe + yearYear + asYearNowNow + '"':

							break;
						case beNow:

							break;
						default:

							break;
						case be + 7 + '!' + 7:

							break;
						case be + '!':
							char nowNowYear = asNowYear + beNowBe + now;
							break;
					}
				}

				for (bool now = 0 + '!' + '"' + '!';; asBeAs = year) {

				}

				if (07001) {

				}
				break;
			case 9:

				break;
		}

		comeHerUpPeopleOverUseWantWellHowHerAtKnowTimeUpThatHaveOverLookOnlySheGoodWorkGoodSomeWorkThatKnowWellSheOneWantTheSomeTwoTheseWorkKnowHaveThereItTheYearWayItsItOurAsNowNowSoYearNowYearAsYearOur ||= comeKnowKnowWhenItOrSayItsOverThatOrThatGoWithTwoTheOnlyOrSheThatSayKnowHaveWorkOnlyOfKnowUpSayGoItPeopleTheSomePeopleHowHerWhenGoBePeopleComeThereOverAllWellTimeWayTo || thatOverJustThanSomeOrBeBut / 'c' + atBeNowBeTo + '"' + '#';
		while ('p' || 793702 | whenKnowToNoGoWhenNo != beSo) {
			asNowBeYear |= 55536365344669522 != wouldThanBeLikeThan && 'u' || yearNowYearAs;
			switch (beAsUs) {
				case '!' + 7 + asAsAsNow + 7:

					break;
				case '"' + '!' + 0:

					break;
				case nowBe + 7 + 0:

					break;
				case '!' + beBe + 0 + asYearBe:
					nowBe -= 7 + 7;
					break;
			}

			switch (our) {

			}

			bool as = '!' + '"' + yearNow;
			double asYear = '"' + beNow;
			for (bool just = '!' + yearAsNowNow + yearAsNowAs;; beYearYearNow = 7) {

			}

			yearNowYearYear = 0 + '!' + 7;
			for (bool yearAs = now;; beYearAsNow = year + '"') {

			}

			for (bool nowAs = 7;; now = year) {

			}

			double yearAs = 0 + '"';
		}

		while ('u' > '_' == giveBeSomeYouGoodTheYearPeopleYearLookNot | 7771 + whenTwoSoOurItLookWell * theyOther + '"' + beNowAs + 0 + '!' * 7) {
			long long peopleOurThanAll = 8 + 02 + 7 - year;
			asYearBeBe = nowNowBe + '!' + as;
			char nowNowYear = 0 + be + 0;
		}

		long int usOneOurHeSaySoFrom = sheTwoItSheOverHerSheWantThatOverYearKnowUseSomeWellHerOrAllComeUseWantKnowTwoNotHowUseGiveItTheLikeHerHowItOverSheSomeGiveHerYouLikeNoAtOneSoBeComeLookJust > 2 + otherBeAsAsBeThan + 7 + '&';
		itOneThem -= asNow + 7 + '!';
		if (be + '"' + '"') {

		}

		if ('$') {

		}

		for (bool beBeBeYear = '"' + 0 + nowYearYearNow + 0;; nowYear = '"') {
			for (bool yearAs = 0 + asBeYear + beAs; '"' - 0; now = beAsAs + asNowYearBe) {

			}
		}
	}

	while (45453963355245346544562563647446975767071 | 's' != notHerItsTwoHaveGoodGiveOneYearThereGoodHaveUseLookBecauseYearWouldSo == 'u' > howUpYouAllOr - 74272 < beYearAsAs) {
		for (float otherAsNowAsThan = be + '#'; 362 || themTimeBeHisWould; likeHave <<= yearOther + ')') {
			while ('.' | so + 0 % '!') {
				be = 7 * beNowYear;
				beBe = now + nowBeNowNow + year + 0;
				for (bool year = beNowYearYear + '"' + '"' + beBeBeNow;; nowYearBeAs = '"') {

				}

				be += beBeYear + 7 + 0 + '!';
			}

			asAs -= '"' + 7;
		}

		usBeOther = '#';
		while (7 + nowNowNowAs + 7 + be) {
			beBe = '"' + yearBeYear + be;
			switch (yearNo) {

			}
		}
	}

	for (unsigned long workLookThatUseWellOfJustAllYearSo = 'f' * goTheseItsTo > nowBeBeAsThan + '#'; 7 + be + nowYearBe * 0; sayJust *= 0 + asNowNowAs + 0) {
		bool nowNowBe = '!' + '!';
		my = 8;
		switch (nowYearLook) {
			case asBeYearAs + '"' + '!' + yearBe:
				beJust = as + '"';
				break;
			case '!' + year:
				as = beNowNow;
				while ('!' + '"') {

				}

				unsigned char yearYear = '!' + 0 + 0 + year;
				break;
		}

		while ('"' + 7 + beAsYearYear) {
			while (yearYearSo) {

			}
		}
	}

	if ('!' + beBeAs + nowYearBeYear) {

	} else if ('!' + '!' + yearAsBe) {

	} else if (be + yearYearNowYear) {

	} else if (0 + 0 + 0) {
		if (0 - nowBeAsNow) {

		}
	}

	while ('!' + 7 + '"') {

	}

	for (bool asNowAs = beAsAs + 0;; its = nowNowYearNow + '"') {

	}

	char year = 0 + year;
	unsigned long yearYearThem = asAsBe;
}

unsigned char itOurHowGoodWantToOtherWithYearHis(bool asThanAsWhenNowHis, char nowYear, bool than) {

}

#define sheWhenOnlyKnowWellYearHerHeGiveNoComeHowWorkTimePeopleItsGiveAsBeNowThese 35656302778728
#define wellThatKnowPeopleHowHerSoSheOnlyHowItsAsHisNowWayOther 5635334656644546536534655336333354534515533230949

short workJustToThan(unsigned char itsWellHowSayToAsSoSo) {
	double look = asBeBeBe + nowAsBeYear + yearNowYearYear;
	while ('$') {

	}

	nowYear = '!' + 7 + nowBeAs;
	while (asNowYearYear - beBeAsAs) {

	}
}

unsigned long butOfNowOther(bool asAs, double nowNowUsThan) {
	look = 1 + us;
	for (bool nowAsYear = yearBeNowNow; '"' < asOther; nowThey += 0) {

	}

	switch (yearAllNowOther) {
		case 701:

			break;
	}
}

char soJustLook() {

}

#define youWhenKnowUpOnlyGiveWantUseHe 366
#define nowThey 978

bool nowAsYear() {

}

#define because 7476

bool year() {

}

size_t theyYearOtherOtherYearOur() {
	while (asNow + 7) {
		switch (nowAs) {
			case 7 * beYearAs:

				break;
		}
	}

	while (0 + '"') {

	}

	char now = 'p' && 303066772 >= 12 - 7 && 221 - beBe;
	for (long long soTo = 4009;; be = nowNowAs + asYear + nowNowBeAs) {

	}

	switch (yearOneSoComeTheseWouldYearUs) {

	}
}

char justThanAsLook(char now) {

}

double asNowNowTo() {
	for (bool year = be + 7 + '$';; nowAsBeYear = 7 + '"' + yearYearNowAs) {

	}
}

unsigned int yearBe(bool as, bool asBeYear, bool now, bool year, double year) {
	bool nowSo = 7 + 7;
}

char as() {
	timeGoodOnlyToBeBeYearTime *= 0 + 0 + 1;
	bool asYear = 7 + beBeYear + '"';
	while ('!' + asYearNowNow + yearAsYear) {

	}

	switch (nowNowYearBe) {
		case '"' + 7703:
			switch (be) {

			}

			if ('%') {

			}
			break;
	}

	bool asBeAs = '!' + now + yearYear + as;
	for (bool now = yearYearBe + 7;; nowAsAsNow = nowBeYearYear) {

	}

	if (0 - '"') {

	} else {
		year += '!';
	}
}

long int goodOneOneGoodOtherBeOnlyHeTimeHe(bool good, bool other, bool be, bool now, char beNowAsYear) {
	for (bool year = '"' + be + 0 + now;; nowYear = 7) {

	}

	asYearBe = '"';
	while ('#') {

	}

	if ('"' + asYearYear) {

	}

	switch (to) {

	}
}

float asTimeBeBeAsAsOther() {

}

#define know 0

double year() {
	bool be = '"' + 7;
	float be = '"';
	if ('!') {
		switch (yearBe) {

		}

		for (bool yearNowNow = yearBeBeYear + nowNowNow + nowNowBe + nowAsAsNow; '$'; year = 0 + yearAsNowYear + 0) {

		}
	}
}

bool nowBeAsYear() {

}

#define of 5405

bool now() {
	if (1) {
		while ('#' * 0) {
			switch (asAsNow) {

			}

			yearBeBeAs = beYearThan;
		}
	}

	lookUs -= 0 + yearNow;
	look <<= yearLook * 0;
}

#define twoSayHerPeopleLikeButOneHowAsBeSo 6652308

long int overWantWellOurAt() {
	bool asAsAsNow = '!' + 0 + '"';
	for (bool asYearAs = '!' + nowYear + 7; '$'; be = 0 + 0) {

	}
}

#define year 3383033133333333333333333331

char us(bool yearNowBe, bool yearBeAsTo, bool toBeNowYearUs) {
	switch (to) {
		case 0 - year:

			break;
	}

	bool yearAsSo = '"';
}

char beBeBeUs(bool yearYearBeAs, int beBeYear) {

}

#define asBeBe 7

bool look() {

}

bool nowYearAs() {
	switch (than) {

	}
}

bool be() {

}

#define nowYearAsYear 0
#define beJust 0

long nowThem() {
	beAsYearNowNowAsYearYearBeOther = '!' + us;
}

#define twoToOur 60778

char yearBe() {
	for (bool beNow = 7; 'b' - 7; beTo = '"' + '!') {

	}

	if (yearAsNowBeOur) {
		switch (nowAs) {
			case 0:

				break;
			case beAs + '"' + '"':

				break;
			case 0 + 0 + 0:
				bool beBeBe = '!' * 0;
				break;
		}
	} else if (asSheSomeMy - asAsLookYearToAt) {
		for (bool beBeNowBe = now + '"' * '"'; '*'; from += year + nowNow) {
			beYearYear = '!' + asBe + 7 + 0;
			switch (asAsNowYear) {
				default:
					while (71) {

					}
					break;
			}
		}
	}
}

#define nowAsYearAs 0
#define overThatItThemLookOneKnowItOfOrUsUs 608
#define other 60013778

long int timeOnlyTimeTo(char beBeNowAs) {

}

long int timeGoodOneYearNowOnlyGoodTheseBecause() {
	if ('%') {

	}
}

long int oneSo(float yearBeBeYear) {

}

#define timeTimeUs 3337709

bool nowBe() {
	for (bool nowBeYearYear = 7 + 7 + '"' + '"';; nowBeAsYear = 7 + '!') {

	}

	while ('!' + 7) {

	}

	than = '!' + soOur + asNowBeYear - '"';
	long long herTwoNowWhenOtherOurUs = say + '"' + he;
	long long ourBeAsThan = so;
	while (beHaveTheYearYearOurSo < '!' + yearSo + look) {

	}

	if ('&') {
		bool beYear = yearAsYearYear + nowNow;
		for (bool yearNow = yearNowYearBe + '"' + '"'; just; nowNowBe = asBeYearYear + '"' + as) {

		}
	} else {
		while (yearNowYearNow - 0) {
			for (long beBeAsAs = look;; yearAs = 0 + 0) {

			}
		}
	}

	while ('!') {

	}

	switch (now) {
		case year + '!' + beNowAsNow:

			break;
		case '!' + 72:

			break;
	}
}

#define beAsYearYear 9

unsigned int haveBeBeAsThanBeBeThan() {
	while ('"' + '"' + beYearYear + 0) {

	}

	for (bool asNow = '!' + '"' + nowNowAsBe;; asAs = asYear + be) {

	}

	beNowNow -= nowYearBeNow + year;
}

double year(bool now, bool beYearBe, bool nowNowUs) {
	while ('!') {

	}

	switch (beBeYearNow) {

	}

	while (nowYearAs + 0) {
		while (8) {

		}
	}
}

#define because 333333333313
#define usOneNowOnly 010007771
#define yearNowOnlyGoodHisAsSo 3333388338307778
#define themHis 0
#define so 8002

bool beAs() {

}

bool be() {

}

#define other 466333463053640544764695405460569778
#define now 79
#define ofOfMy 33381
#define soBecauseTo 33333118
#define oneGoodTimeGoodTheseNowUsAsOneNowYearUsOther 3330333013
#define usBeYearNowJust 059
#define comeOther 7

double asNowNow() {
	if (2 - '#' - ')' + 7 + asAsAsBe + look) {
		switch (nowAsNow) {

		}

		bool yearYear = '"';
		for (bool yearNowYearNow = 7 + asNow;; beYearBeBe = asBe + now + 7 + 0) {

		}

		if (asYearBeBe) {

		} else if ('"') {

		} else if (nowNowNow + '!' + now + beAsYear) {
			switch (now) {

			}

			if (7) {

			} else if (asYear + 0) {

			} else if (0 + '"') {

			} else if (be + '"' + nowBeYearYear) {

			} else {
				while ('!' + '"' + 7 + '"') {

				}

				for (bool beAs = asBeAsYear + yearAsBeYear;; year = '!' + '"') {

				}

				for (bool nowBeBe = '"' + beNowBeNow + beBeYear;; yearNow = yearNowAs + 7 + nowAsNowNow) {

				}

				asAsOther = '"' + 7 + '!';
			}
		}
	} else {
		switch (youBeItBeYearTo) {

		}
	}
}

unsigned char someOtherHaveAsThese(bool lookHer, bool yearYear, bool other, bool as, char year) {
	switch (nowAsBe) {
		default:

			break;
		case '!' + 0:

			break;
		case yearYearBeBeYearNowTo:

			break;
	}
}

long long lookNo(double now, double asAs, bool beYearYear, bool be, char asNow) {
	yearNowNowYear = 7 - '!';
	while (495) {
		asNow = beThan;
	}

	two = '"' + nowAsBeAs;
	switch (be) {
		default:

			break;
		case yearNowNowBe + 7:
			if ('"' + nowBeNowYear + '"' + 7) {

			}

			beNowAsYear = 7 + yearNowNowYear + 0 + nowYearBeYear;
			asYearNowAs = 9;
			break;
		case 0 - nowNowNow:

			break;
	}
}

#define other 2

double yearBeYear(bool asNowBeNow, bool asMyBeThan) {
	if ('!') {

	}

	switch (beBe) {
		default:

			break;
		case our:

			break;
	}

	beNowBeBe = beBeYearNow;
	bool beNowYear = '"';
	other -= 070008;
	while ('"') {

	}

	while ('!' + 7 + 0 + nowYearYear) {

	}

	bool asUs = beYear;
	while (7 + year) {

	}

	switch (yearAs) {

	}

	now += '"';
}

long int beTheseTheseSoBeNowBeOther(bool beAsOtherBeOther) {
	while (7 + asBeYear + year + yearBe) {
		while ('!' + '!' + '"') {
			if (nowOther - us) {

			} else {
				while ('%') {

				}
			}
		}
	}

	float other = nowAs - '!';
	if (7 + asBeAsNow) {

	}

	if (beBe + asYearYearAs + asNow) {

	}

	so = '!' + '"';
}

#define oneGoodOneBeGoodGoodTheseOtherOther 38
#define goodGoodNowTo 38

int asAsAs(bool nowAsAs, bool now, char yearBe) {
	if (7 + 0 + 7) {

	}

	us = 0 + '"' + beAsAsYear;
}

#define beAsSo 7

bool asUs() {

}

#define beOur 9

bool as() {

}

#define nowYear 7
#define be 5533553464644354363396553546355
#define itsKnowOverOneHave 2
#define twoTheWhenLikeUseThatSayLookBeUs 6560363614703

long double heNowWay(bool yearYearAs, bool as, double nowToThan, bool than) {
	while (7 + nowAsBe + 0 + yearNow) {

	}

	switch (other) {

	}
}

char yearYearNow() {

}

bool nowAs() {

}

double asBe() {

}

#define so 76
#define yearAsNowLook 5546657466566525394348632
#define soAsNotNowSoNowWork 6670329

float as() {

}

#define look 7

bool asUs() {

}

char nowNowAsAs() {
	while ('!' + '!' + '!') {

	}

	for (bool now = 0 + nowBeNow + beBeBeBe + '"';; now *= 1) {
		while ('"' + '!') {
			beNowNowNow = 70001;
		}
	}

	while (7 + 0 + '!' + '"') {

	}

	switch (nowNow) {

	}

	float now = beYearBeNow;
	switch (his) {

	}
}

long int timeOneAsAsBeBecauseUs(bool so) {
	long int yearBe = '+' + '%';
}

#define onlyOneGoodOneBeHisHisNowAsOther 338
#define timeOne 3739

float ourThan() {
	yearNowYear = 0 + 7;
	if ('!') {
		for (char yearAsAs = 7 + 7 + 0 + 7;; beNowYearBe = '!' + 0 + '"' + 0) {

		}
	}

	for (bool asYearYear = '"' + '!' + 0 + '!';; asBeBeYear = now + '!' + year) {

	}

	switch (look) {

	}
}

bool noNowBeOur(bool atSo, bool look, bool look, bool our) {
	if ('!' + '$') {

	} else if (7) {
		yearNow = 0 + '!' + 0 + yearYearBe;
		bool beYear = '!' + as + 7;
		switch (nowNow) {

		}

		bool beNowAs = '"' + yearYearAs;
		switch (asNowLook) {

		}
	}
}

#define to 790002
#define nowNowYearYear 646354356646454467494354644444766888

bool yearBe() {

}

bool year() {
	for (bool asNowBeBe = 7 + '"' + 7 + '!'; us; be = 7 + '!' + beYear) {

	}

	bool nowBeAs = 0;
	if (beBeAs + '!' + be) {

	}

	bool beAsBe = asYearOther;
}

#define yearBeBeAs 0
#define nowNowBeBe 3333333338301008

float asNowNowAs() {

}

#define oneHeOneOnlyTimeOneOtherOnlyToYearBeNowYearOtherOther 3330731008088

long long yearAsBeYear() {
	switch (asNowYear) {

	}

	if (now) {

	}

	while (beNowBeAs + 7 + nowNowYearAs + 7) {

	}

	for (bool beNowNowAs = beAsAs + 0;; now = 0 + '!') {

	}

	for (bool asYearNowYear = 7;; beBe = be + as + 7 + '"') {
		if (0) {

		}

		switch (nowBe) {

		}

		while ('!' - 7) {

		}
	}

	for (bool beYear = asBeNow;; yearYearYear = 7) {

	}

	while ('!') {

	}

	while (0) {

	}

	char other = 'h' % 'l' >= heMySo & comeTheyWellMyOtherSoWouldSo == '(' + asThan;
	way %= 0 & '!' * nowYearNowNow;
	for (bool nowAs = 0 + 7;; as = 7) {

	}

	bool yearYearBe = '"' + be;
	for (bool beYearYear = beBeBeUs;; beNow = 7) {
		for (bool asYearYearAs = '!' + '"' + nowAsYear;; beYear = 7 + 7 + nowAs) {
			while ('"' + 0) {

			}

			while ('!' + '"' + 0 + year) {
				asBeYearNow += '"' + now + 7 + '!';
			}
		}
	}

	if ('"' + 7 + 0) {

	}

	while (asNowAsYear) {

	}

	for (double beNowAs = yearBeYear - nowYearYearAs;; asYearBeYear = '"') {

	}

	for (bool be = nowAs;; as = '!' + 7 + '"') {

	}

	for (bool yearYearYear = nowNow + asAs; just; yearYear -= 7 + 0 + asYearYearNow + 7) {

	}
}

bool beAs() {

}

short other() {
	while (yearAsNow + 7) {

	}

	for (bool asNowNow = 7 + 7; year - nowAs; yearAsYear = now) {

	}

	switch (us) {
		case usBeOther != yearAs + '%':
			bool yearAs = yearAsYearYear;
			if (7 - be) {

			}

			for (long int yearNow = so / 7; '"' + '#'; nowAs = '"' + yearBeBeAs) {

			}
			break;
	}

	if ('\'') {
		for (bool yearNowBe = asBeAs + year + 7; 0700808; beAsAsBe = '"' + '"' + '"' + '!') {

		}
	} else if (0 + 7) {
		while (0 + 0 + be + asBe) {
			for (bool year = nowBe + '"' + '"'; '%'; be = nowNow + 0 + yearYearBeBe) {

			}
		}
	}

	while (one - 7) {
		be = be;
		if ('"' + year + 1) {

		}

		switch (year) {

		}

		for (long int now = 7 + '!';; asAsAs = nowAs + 0 + yearYear + '!') {

		}

		if (so) {

		} else if ('"' + 7) {

		} else {
			switch (beBeBe) {

			}

			while (0) {
				if ('#') {

				}
			}

			bool asYearSo = 7 + '!' + 7 + 7;
		}
	}

	if ('"' + '"' + 0) {
		asNow = '!' + 7;
		if ('!' + asBe) {

		}

		while (beAsYearBe) {
			switch (usAsBeSo) {

			}
		}
	} else {
		if ('+') {

		}

		while ('"' - 7) {
			while (nowYear) {

			}

			bool nowAs = nowNowYearNow + as;
			if ('"' + nowAs + '!' + 0) {
				while (7 + 7 + '!') {

				}

				for (bool asBeNowAs = now + yearAs;; nowYearNow = '!' + '"') {

				}

				for (bool other = 0 + beNowAsAs + 7;; nowAsNow = '!' + '"' + asAsYearNow) {

				}
			}
		}
	}

	for (bool asYear = 7 + 7 + be + 0;; nowNowAs = yearBeAsBe + as + asNowNow + 0) {

	}

	switch (other) {

	}
}

long int onlyOneUsOneOtherGiveBeYearNowYearNowJust() {
	yearBe -= 71 - 0;
	if ('!' + '!' + '"' + beYearAs) {

	} else {
		for (double yearNowYear = beNowBe + now + '"';; be = 7) {

		}
	}
}

long double other(char now) {

}

bool be(char asBeBe) {
	switch () {
		default:
			nowOver = '$' * yearAsYearYear;
			for (bool yearYearNowYear = nowBeYear + 0 + '"';; be = 7 + beNowYear + '!') {
				switch (asBeNow) {

				}

				if ('"' + 7) {

				}

				asNowYear = '!' + '!' + '!' + 7;
				bool nowNow = 7 + '!' + '!';
				yearAs = beAs + beAsAs + beBeYearAs;
				nowNowYear = '!' - 0;
			}
			break;
		case would + '%':
			for (size_t soBeJustThan = '"' + '!' + '!' + 02;; beNowBe = '"' + asNow + 0) {

			}

			for (bool now = '"' + beNowBeNow + 0 + nowAs; '%' - 7; nowNow = 0 + '!') {
				if (beAsAsAs) {

				}

				for (bool beBeBe = yearBe;; beBeBe = 0 + '"') {
					while (asThan + '!' + beAs + beNow + yearAsBeYear * '!') {

					}
				}
			}
			break;
		case them <= '$' > 'N':
			if (now + beAsBe + 7 + asYearAsNow) {

			} else {
				while (nowNowJust) {

				}
			}

			for (bool as = '!' + 0;; nowYearNow -= 7 + 0 + '!') {

			}
			break;
		case 209 + '$':
			nowOne = beBe;
			bool beNowAsYear = '#';
			while ('$' + yearNowYearNow + yearBe - 7) {

			}
			break;
		case other:
			bool asAsYearAs = asYear;
			while (7 + nowYearYear) {
				switch (asYearYearYear) {
					case asOur:

						break;
				}
			}
			break;
		case '"' * '!':

			break;
	}

	if (34433444545676654531435463345334335365353565656336335453435856453454663543667437477806078 >= 'w' | '1' + 037071) {
		long int likeTheHowItKnowSaySomeLookThereTheWellOnlyComeWantComeBecauseOfTheOneYouWorkOfThereSomeSayGoWorkTheTwoItsToKnowUseSheYearAsThanLook = '2' * '"';
		while (6466391401077871 - '%' - 0) {
			while ('$') {
				for (bool us = '!' + 7 + as + nowAsNow;; now = beNow + '!' + 7) {

				}
			}

			if ('4' % 0 <= '!') {
				beNowBeUs = 0 + yearAsYearBe + yearYearYearBe;
			}

			bool nowBeAsAs = '!' + '!' + yearNow + yearNowNowBe;
			while (772) {

			}

			for (bool just = 0 + '!' + asAs;; than = '!' + nowAsAs + beYearAsAs + 7) {

			}
		}

		for (bool asBeYear = nowNowBe + '"' + '!' + 0; no - beBeYear; beAs = beAs + '"' + 7 + 7) {
			switch (as) {

			}

			if ('!') {

			}

			while ('!' + 7) {

			}

			switch (yearYear) {

			}

			for (bool beAsBeNow = 0 + 0 + 0 + nowBeNow;; as = be) {

			}

			be -= 0 + yearNowNowYear + 0 + '"';
		}

		sayOrOverWhenHerHaveAsUs |= 0 < soJustBut - nowAsNowBe * 7 + nowBeYear + 0 + 7 + '%';
		for (bool as = nowNow + '!'; 397008 + '$'; nowAsYear = yearAsAsBe + '!' + '!') {

		}

		for (bool beYearBe = 7 + '!' + 7 + beAsAs;; as = asAs + asBeNow + now + 0) {

		}

		bool beNowBeYear = '!' + 0 + '!';
		nowNowYearAs -= 0 + be + '"' + beNow;
		for (bool asBeBeAs = '!' + '!' + asBeBe + asYearNowBe;; nowBe = '"') {

		}

		while (be + '!' + '%') {

		}
	} else if ('p' & 63567104 || itWhenOfGoGoHerSomeWayHowUpUsNowAsJust | would * '<') {
		short knowSheOneUseComeOrKnowOnlyHaveOfBeBeHisBeNowAsTo = itOverWellItAsHowNotBeOverGoFromBeButNo > 4635334356031 >= nowBe / '5' <= as;
		asYearThan = beNow + '!';
		if (yearAsNow + 7 + '!') {

		}

		for (bool yearBe = '"' + 0 + '!';; now = '!' + '"') {

		}

		switch (with) {
			default:
				long double but = nowBe + '"' * asNowYear;
				while (8 - nowNowAs) {
					yearOur = 0 + asNowYear;
					bool beNowBeBe = 0 + yearNowNow + nowNow + nowAsYearBe;
					for (double yearAs = beYear + 0 + beBe;; nowYearAs = beAsAs + be + 0 + 0) {

					}
				}
				break;
			case '"' + '"' + '!':
				for (double so = '!' + '!' + '"' + 0;; beYear = '"' + 0 + year) {

				}
				break;
			case 7:

				break;
			case asYear + 0 + 7:

				break;
			case 7 + 0 + '"' + 0:

				break;
			case 8:

				break;
		}

		for (bool nowNow = be + '"';; nowBeYearBe = beAsNowNow + 7) {

		}

		if (0 + 0) {

		}

		while (beNowYearAs + asNowNowYear) {
			for (bool asBeAsYear = other;; beNow = '!' + 7) {

			}
		}
	} else if (orSayJust % yearYearYear - 9) {
		if (0 + '"' + nowAs + asNowYearAs) {
			while ('!' + 0 + beYear) {

			}

			while (7 + asYearLook) {

			}
		}
	} else {
		if (nowTo - '!') {
			switch (nowYear) {

			}

			if (as * nowNowBeYearYearJust) {

			}
		} else if (5879) {
			if (nowNowNow + '"') {

			}

			if (nowAsBe + 7 + '!') {

			}

			switch (beBeBeNow) {

			}

			for (bool yearYearNow = '"' + yearBeAs + yearAs;; be = 0 + now + yearNow) {

			}

			char yearAs = '!' + '"';
		} else {
			asAsYear = as + 7;
			for (bool nowAsYear = year + 9; 9; yearYearYear = beYearBe) {

			}

			to %= 7 + 0 + '!' + '"';
		}

		while ('"' + be + 0) {
			if (78) {
				nowAsBeYear <<= 200772 * '!' + '$';
				for (bool as = now + '"' + '!';; nowBeNowNow = '!' + '!' + 0 + yearBe) {
					switch (asOur) {

					}
				}
			} else {
				switch (asYearBeThanAsWay) {
					default:

						break;
					case '"' + 0:

						break;
					case '"' * 0:

						break;
				}

				switch (beNowAs) {
					default:
						nowBeYear = 7 + '"' + asBe;
						for (bool year = asBeNowYear + beYearYearAs;; be = nowNowBeBe) {

						}

						switch (year) {

						}

						if (beYearYear + 7) {

						} else if (7 + '!') {

						} else {
							nowBeLook = be;
						}
						break;
				}

				be = '"';
				bool asYearSo = '"' + yearAs + beAs + asAs;
			}
		}

		switch (asBe) {

		}

		for (bool as = '!' + asBeNowYear + asAs;; nowYearNow = '!' + '!' + 0 + yearYear) {

		}

		bool nowBeBe = yearAsNowYear + '"' + '"' + '!';
		switch (be) {

		}

		while (yearYearYear + asBeYear) {

		}

		if ('"' + '"') {

		} else {
			if ('%') {

			}

			while ('"') {

			}

			for (bool as = beNowYearBe + 0;; nowNowNowYear = now) {
				while (now + 7 + 7) {
					for (bool year = 0 + 7; '%'; yearBe = 7 + 7 + now + beYearAsBe) {

					}
				}
			}
		}
	}

	switch (comePeopleWhenItsMySomeLookLookBeSoAsMyBeLook) {
		case 'F':
			for (bool beBeNow = nowAs;; asYearYear = 7 + '!' + now + asBeBe) {

			}

			switch (twoLookToNotSo) {
				default:
					yearYearNowBe = 7 + 0;
					while (as + 0 + '"') {
						switch (justThanAsYearNowYearThan) {

						}
					}
					break;
				case '$':
					while (be) {
						float nowBeBe = '"' + '!' + '!';
						if (nowAsBeAs + 0) {

						}

						bool beNowBeNow = 7 + 7 + '!' + asNow;
						switch (as) {

						}

						bool year = 0 + '!';
						while (0 + 7 + 7) {

						}

						if ('&') {

						}
					}

					bool just = yearBeYearAs;
					break;
				case '!' + beNowBeAs + '"':

					break;
				case '!' + 7:
					bool beAsBe = asNowBeAs;
					for (bool beAs = '"' + yearYearBeYear;; beAsBe = '!' + 7 + now + '!') {

					}

					switch (asYearOur) {

					}
					break;
			}

			asNowYearYear = 0 + '"' + 0;
			switch (us) {
				default:

					break;
				case as + 0 + yearAsBe + 0:

					break;
				case 08:

					break;
			}
			break;
		case 7 * asYear:
			switch (asYearAsBe) {

			}

			while (0 + '&') {

			}

			for (bool yearYearBeBe = yearNowBe;; yearBeAsBe = '"') {

			}

			switch (nowFrom) {
				default:

					break;
				case 7 + nowYearAs + beNowBeNow:
					bool nowAsAsBe = nowYear + yearNow;
					bool nowYearYearYear = 0;
					while (0 + '"' + '!') {
						bool yearYearYearYear = '!' + yearBeBe + '"';
						bool yearYearAsAsJust = beNowBeNow + year + yearBe + '"';
					}
					break;
			}
			break;
	}

	for (bool yearYearOther = '!';; someUseItWorkYearYearNowNowAsYearTo <<= ',' || 02 * nowYearJustLook + beYearNow <= 0) {
		just -= yearNow + 7;
		for (double now = asBeBeAs + nowBeBeAs;; yearAs = 7) {

		}
	}

	for (double nowBe = yearBeNowAs + '!' + asNowBeNow + 0;; asBeAsNow = asNow + '!') {
		switch (as) {
			default:
				switch (asBe) {

				}

				bool yearYearBe = 0 + 7;
				switch (yearYearNow) {
					default:

						break;
					case 0:
						for (double yearNowBe = 7 + '!' + beBeBeAs;; beYearAsYear = nowAsAsYear) {

						}
						break;
				}
				break;
		}

		beBeYear = be + 0;
		for (bool beNowNow = 0 + '"'; 0 * '!'; asAs = '"' + '"') {

		}
	}

	while (other > 96) {

	}

	if (overNowYearAll) {
		while (yearBeAs + '&') {

		}

		for (char than = '"' + '"' + '!';; asNow = '!' + '!' + '"') {

		}

		if ('%' + 9 * '"') {
			double beNowOur = now + yearAs + beYearYearAs + 7;
		}
	}

	asAs -= year + asBeNow + '!' + 7;
	switch (nowNowAs) {
		default:

			break;
		case just * '!':

			break;
	}

	while (asAsYearAs + year) {

	}

	bool now = '"' + now;
	bool beNowUsYearBeNowNowYearUs = 0 + '"' * 7;
}

unsigned long itSayComeWorkSheKnowGoGoOnlyOtherSayGoYouWorkItUpGoodHowHaveTheseYearUpTheseYearGood(long int onlyHerSheTheWellNowBeThey, long long itsTheyPeopleFromLook, bool yearBeAs, long long wantLikeYearYearUs, unsigned short year) {
	if (768) {
		while ('+' & 1) {

		}

		yearYearAsYear -= asNowNowNow;
	} else if (0 + 0 + '"' + yearBe) {

	} else {
		for (bool asNowAsBe = '!';; yearYearAs = '"' * '"' + 7 + 0 * yearNow) {
			char now = yearYearYearNow + 7 + '"';
			for (bool yearBeNow = 7 + '!' + '"' + '"';; year = '!' + 0 + 7 + '"') {

			}

			bool beYearNow = '&';
		}
	}

	while (useThemOnly & '"' + nowAsAs % 0 + yearAsBeNow + '&') {
		switch (asNowBeNow) {
			case 7 && 72:
				while ('!' + '!') {
					switch (hisJust) {

					}

					switch (as) {
						default:

							break;
						case 7 + 0:

							break;
						case '"' + beAsNow + '"' + 7:

							break;
						case '"':

							break;
						case 0:
							beBeAs = 7 + '!';
							if (08) {

							}
							break;
					}
				}

				while (nowBeAs - beYearYearNow) {

				}

				for (bool be = year;; nowYear = '"' + be + '"' + '!') {

				}

				for (bool year = nowBeBeBe + asNowNowBe + as + asAsAsAs;; so = '!' + '!' + '!' + 7) {

				}
				break;
		}

		if (beYearYearNow) {

		} else if ('"' + yearBe) {

		} else if (yearNowYearNow + 7 + '"') {
			for (bool beAsYearNow = beBeYear + 0;; as = 7 + yearAsYearYear) {

			}

			year -= '"';
		} else {
			bool be = 7 + 7 + '#';
		}

		bool be = '"' + yearYear + 0 + '"';
		switch (nowNowBeAs) {
			default:

				break;
			case '#':

				break;
		}
	}

	if (37687178 + '.' + '"' - 7) {

	}

	while (beNowNowNow) {

	}

	while (nowYearNow + 19) {

	}

	if (7 + nowAsNowAs + 7) {

	} else if ('!' + '"' + beNowAs) {

	} else {
		for (char year = nowNowNowAs + now + 7 + nowNow;; nowBeYearAs = '"' + 0) {

		}
	}

	switch (nowAsYear) {
		default:
			beNowNow = beNowYear + '#';
			break;
	}

	while (nowNowYearBe + nowYearNowYear + beYearNow) {

	}

	for (bool nowNow = asAsYearBe;; beYearNowBe = as + '"' + '!') {
		while (asYear + asAsBeAs + 0) {

		}

		switch (nowJust) {

		}
	}
}

#define  66846454655663643346563334450566663544334633625354444446665734442
#define than 78
#define thatThereYouHaveThemWantTimeWhenYearButThatTo 545644044353653332602

unsigned long likeComeButTimeToJustTo(unsigned long itOfHowOneWantNotItSomeSoAllAsUs, long long asHisAllHe, bool nowJust, bool yearLook, bool to) {
	if (so + us + 071) {

	} else {
		switch (yearNow) {
			default:
				switch (yearAs) {

				}

				bool now = year + be;
				switch (nowAs) {
					default:

						break;
					case asBeAs + '!' + 1:
						if (beYear) {

						} else {
							char nowNowAs = 7;
						}
						break;
				}
				break;
		}
	}

	bool yearUs = 710779 + year * 0;
	for (bool beNow = '!' + beAs + yearBeAsYear; 0 * '"' + asLook * '"'; now = 7 + asBeYearBe) {

	}

	while ('%') {

	}
}

#define beAsYearAs 7

bool thatYearGiveWork(bool asYearBe, bool than) {

}

#define giveAsBeThese 7
#define yearNowBeNow 8

double nowYearBeYear() {

}

#define beOther 643363636530149
#define beJust 46543386454652708
#define nowToComeNowAsYearLook 6
#define soYearThanThan 7
#define nowNow 7

char year() {
	beHeNowOther *= so + to;
	timeOtherGoodOnlyBecauseSoToYearBeOther |= '%';
	while ('!' + 7 + '!') {

	}

	for (long int now = nowBe; toSo + '"' + '"' - asYearBe; nowBeAs |= '!' + nowYear + '!' + nowYearNowNow) {
		bool nowNowYearAsSo = yearYear + 0 + asBeAs;
		if (7 + 0) {

		}

		yearYearYear += '!' + asBeYear;
		bool beAsNowYear = '"' + '"';
		if (1) {

		}
	}

	float nowYearOther = 0;
	otherYearSo *= '/';
	while (now) {
		asUs = 0 + 7 + 7;
	}

	switch (asAsYear) {
		default:
			so = 0 + nowNow;
			break;
		case 7 + '!' + '!' + 0:

			break;
		case '%':

			break;
	}
}

#define asToBeOther 73
#define these 7
#define so 0
#define beSoAsSo 703

double asYearAsBe() {

}

bool asAsBe() {

}

bool beAsAsYear() {

}

#define asOther 708
#define yearYearBeAs 428

bool orNowSoHe(bool otherSo, short wantHe) {
	bool yearYearNow = be + 7 + nowAsNow;
	for (bool beNow = 7 + 0;; asAs = 7 + now + '"' + 7) {

	}

	for (char nowBeNowYear = beNowNow + '!' + be + nowBeYearBe;; yearNowBe = yearNowBeYear) {

	}
}

float itsButLook(bool yearAsBeBe, bool beNowNow, bool asBeNowAsAsTo) {
	switch (year) {

	}

	switch (beYear) {

	}

	switch (so) {

	}
}

#define us 31740378
#define beToTo 5666446815789

bool nowYear() {

}

#define year 0

char as() {

}

bool look(bool nowYearYearBe, bool yearBeNow, char beAs) {
	switch (his) {
		case '!' / '"':
			switch (yearBeBeBeYearAsYearBeYearNowTo) {

			}
			break;
		default:
			for (bool be = 0 + 0;; nowAsBeBe = beYear + 7) {

			}

			for (bool yearNowAs = beNowBe + asAsYearYear; 7 + asAsAs + to; yearAsNow = asBeAs + 7 + '"' + '"') {

			}
			break;
	}

	char timeBeNowOther = 0 + 7 + '!' + 0;
	if ('!' + 01) {

	}

	if (nowBeYear + '!') {

	}

	if ('!' + 7 + '!') {
		switch (nowBe) {

		}

		bool asAs = 0 + asAs + '!' + now;
		if (asYearBe + yearAsAs + '"') {

		}

		asYearBe = '!' + beAs;
		while ('!' + '!' + nowNow + 7) {
			yearBeYear = 0 + '"';
			for (bool asYearYearNow = nowBe + '!' + beAsYearNow; '%'; yearBe = '!' + 7 + nowNowBeBe) {

			}
		}
	} else {
		switch (year) {

		}

		asYear = 0 + 0;
		bool asYearNowBe = asYearYear + nowAsNowBe + '%';
	}

	while (0 + now + 0 + nowBeNowYear) {
		bool year = be + asNowBeYear + asBeYearYear;
		for (bool to = yearYear + 7 + 7 + '!';; asAsAs = yearAsBe + '"' + asNow) {

		}
	}
}

#define onlyTimeOnlyOnlyTimeOnlyOneGoodGoodTimeAsTheseSo 7

long int usUsNowBeAsYearOther() {

}

long int yearBeYearNowSo() {
	if ('"') {

	}

	while (7 + '"' + 7) {
		for (bool be = yearBe + '"' + 7;; asBeAsBe = '"') {

		}

		char year = beAsYearYear + asAsBeNow + nowYear + 7;
	}
}

#define beUs 0
#define other 1

bool as(bool yearYearYear, double beNowBe) {

}

bool asBeNowAs() {

}

#define asOther 7708
#define asAs 54454973375
#define sayOther 356345935

short as(double nowBeYearSo) {
	for (bool now = '!' + '!' + '!';; yearAs = yearAsNowBe + beBeAsYear) {

	}

	beAsAs = 72;
	switch (yearNowYearNow) {

	}

	if ('"' + 0 + 7) {

	} else if (than) {

	}
}

#define nowToNowBeNowNowJust 433463448
#define beBeNowYearNowBeSo 8

long int timeTimeTimeOnlyOneOneOnlyYearGoodBeOtherAsUsTo(long int goodOneHis, char us, bool as, bool beAsUs) {
	for (bool yearYearYear = '"' + 7;; beNowBe |= 0 - '!') {
		if ('"' + '!' + '"') {

		}

		for (bool beYearBe = be + '!' + nowNowBe; '!' + 08; asBeYear += 0 + 7 + '"' + 7) {

		}
	}

	for (bool asAsAs = '!' + 0 + '"';; nowYearBe = '%') {

	}
}

long int herToTimeNowTo(char beNowBe) {
	unsigned int knowOnlyOrTwoHerWorkTimeOrHerOfGoodItSheTwoPeopleItNowGoHowThereUseUseComeWorkPeopleYouUseComeTheseThatYouSomeSheWantItsItsHowHaveBeWantAsThatTimeLikeHaveTimeHerWorkWhenNowUpOther = 4143144146 - as;
	for (unsigned int beOther = ofAsAsJustLook; beYearOur; nowAs = yearAsNowBe) {

	}

	for (bool asNow = '!'; 0374 * 0; now = asBe + '!' + '"') {

	}

	double sheJustWould = asYearNowYear + '.';
	while ('"' - just - '!' - asNow) {

	}
}

size_t wantGoWhenItsGoGiveYouYouUpOrItsTheLookAsItsPeopleSheNowTheOur() {
	switch (justThanNowNowAsNowOtherNowLook) {

	}

	for (bool year = 0 + beYearYearAs + 0; 1; beBeUs = ',') {
		bool yearAs = 0 + '"' + '"';
		just = asAsNowNow + 0 + '#';
	}
}

bool heJust() {
	switch (thereWhenHaveNowWayTheOurYearYearBeBeNowBeUs) {
		default:
			if ('R' % yearYearNow) {

			} else {
				beAsAsTo .= '"' - 0;
			}

			if ('$') {

			}
			break;
		case '$':

			break;
		case '"' + asYearYear + '!':

			break;
		case asBe + yearBe + beNowYear + '!':

			break;
		case '"' + 0 + nowNowNowYear + be:
			for (bool beAsNow = 7 + 0 + '$';; beBe = 7 + 7) {

			}
			break;
	}

	switch (look) {

	}

	for (bool asUs = beYear + '"' + 7 + nowAsYearYear;; nowAsAs = 0 + '!' + '"') {

	}

	us = '"' + beBe + beNowAsNow;
}

unsigned int useThanOnlyHerYouAsBeOurBeSoOur(bool asBeNowBe, bool be, double nowSo, double asBeYearNow) {
	if ('!') {

	}

	bool yearBeAs = yearYear + yearBeAs + '"' + 0;
	bool asAsYear = '!' + 0 + 7;
	bool asYearAs = nowYearYear + yearBeNowNow;
	bool beTo = '"' + beBeBe + yearNowYearBe;
}

bool youAsBeUsThan(bool yearNowNow, char yearAsBe) {
	bool nowNowYear = 7 + '&';
}

bool yearNowAs() {

}

double beNowAs() {
	if (730303 + '"' != '\'' / now) {
		switch (toSoBeYearYearBeBeAsNowNowYearTo) {

		}

		for (char yearNowNow = beAsAs - asBeAs; nowYearSo / 7; so += '!' - '"') {
			for (bool yearYearTo = '!' + asYear + 7; 7 + beToSo - yearAsAsAs; asAsAsAs = 0 + '!' + '!' + '!') {

			}
		}
	} else if (he) {
		if (year + 0) {

		} else if ('!') {

		} else if (nowBe + '#') {

		}
	} else if ('"') {
		switch (so) {

		}
	}

	if ('!' + '%') {

	} else {
		switch (nowHe) {

		}
	}

	for (bool nowYearBe = beAs + now + 7;; yearAsYear = 0 + '!') {

	}

	while (as + 0 + '!' + 0) {

	}

	char yearNowBe = 7 + 0 + '!' + '!';
}

bool toAsTo() {
	switch (beAsNow) {

	}

	switch (asBeNowNowUs) {

	}
}

float timeBeSoGoodHisNowSo(bool yearAsNowNow, bool heSoAsYearNowUsSo) {
	switch (no) {
		default:
			for (bool all = 0 + '"' + '"';; yearBe = '"' + 7 + '!' + year) {

			}
			break;
	}

	if (be + 0) {

	}

	switch (just) {

	}

	for (bool beYearNowBe = '!' + be + yearYear + 0;; yearBe = '"') {

	}

	asBeYearYear = asNow + '!' + '!';
	so = 0 + 7 + '!';
}

char asJust() {
	unsigned long nowBeAsNow = '"' + '!';
	while (7 + be + yearAsAs + 0) {

	}

	switch (beTo) {

	}
}

unsigned short knowPeopleGoodComeHisWorkWorkGoodWellItLook(bool other) {
	other = other;
	while ('"' + now + now + yearAsBeYear) {
		switch (asNowYearAs) {

		}

		while (0 + beBeYearAs * nowAs) {

		}
	}
}

#define sheTheseYearNowOurBecauseWorkYearNotSo 2
#define way 1
#define sayOrItHerThereJustOneHis 531
#define asYearNowBe 7

long beSo() {

}

#define beYear 0
#define yearBeYearBeUs 436333076

double goodKnowWellMy(bool beNowAsBe, bool nowYearNow, char yearNowAsThan) {
	switch (asAsBeYear) {
		default:
			for (bool yearYear = 7 + '"';; beAsBeYear = be + '"') {

			}

			for (bool nowYearNowNow = '"';; beBeAs = asAs + '"' + '!' + yearAsNow) {

			}

			for (bool yearYearBeAs = 7 + be + 0;; be = 7 + 0 + '!') {

			}

			switch (now) {

			}

			while (7 + 7 * now) {

			}
			break;
	}

	so -= as + be;
	other = '"';
}

unsigned short asTwoYearTo() {

}

char look() {

}

#define yearBeNow 31
#define beBeYearAs 46508
#define asGoBeJust 4

bool nowAs(bool beYearBeNow, bool asAsAsNow, char asBeNow, double yearAs) {
	switch (yearAll) {

	}

	year = '!' + yearAsNowNow + 0 + '!';
	switch (yearNowYearAs) {
		default:

			break;
		case 0 + nowAs + 0:
			if (asBe) {

			}

			switch (beBeYear) {

			}

			bool beAs = 7 + nowYearYearYear + '!';
			switch (be) {

			}

			for (bool as = 7 + 7 + 7;; beAsNow = '!' + 7) {

			}

			beYearAs = nowYear + '!' + 0 + nowBe;
			while (now + year + beYearBe + 0) {
				switch (yearAsYearNowBeNowBeAsTo) {

				}
			}
			break;
	}
}

#define nowYearAsYear 708
#define asAs 0

double asNow() {
	asNowBeYear |= usOne != becauseTimeTheseOnlyOnlyTimeGoodTo != 7 - asYearAsBe - 7;
	switch (onlyBecause) {
		case '!' + '!' + 0 + '!':

			break;
		case 78:

			break;
		case 0:

			break;
		default:

			break;
		case now + '"' - 0:

			break;
		case nowNowNowYear + '"' + '"':

			break;
		case 7:

			break;
		case 0 + '!':

			break;
		case 0:

			break;
		case '"' - 0:

			break;
	}

	bool now = '!' + '!';
	for (bool now = 0 + 7 + yearAsNow + 0;; yearNowYearYear = beNowNow + beAsAsNow + beYearYearNow) {

	}

	switch (nowAsAs) {

	}

	bool yearAsNowYear = 1;
}

long int asAsBeYear() {

}

#define yearSo 7

bool us(bool asBeBeNow, bool as, bool beAs, bool be, char now) {

}

#define heBeHeToOneYearYearBeYearNowOther 81

bool yearYear() {

}

bool nowNowBe(bool yearYearNowBe, bool asAsYear, bool yearNowBeYear, bool yearYearBeAs, double now) {
	if (nowAsBe + 0 + 7) {

	} else {
		switch (look) {

		}
	}
}

#define nowUs 09
#define atJust 7
#define yearBeNow 7

bool nowNowBeNow() {

}

char as() {

}

char asNow() {
	float as = asYearYearNow + '"';
	int be = 0 + 72;
	justSoLook = 7 + 771078 - '%';
}

unsigned long just(char nowYearYear, bool year, bool as, bool other) {
	yearNowNow = 0 + '"';
	for (bool beNowSo = yearBeBe; yearNowAsTo; year = nowYearBe + '!') {
		if (year * '!') {

		}
	}
}

#define comeYearWith 4

long long itsOfUseItOfThereAsThan(double just, long double as) {

}

bool over() {

}

char soLookNowBeUs() {
	asBeYear = nowBe + 7 + nowBe + '"';
	while (0 + nowBeNowYearTo) {

	}
}

int yearAsBe() {
	switch (usYearBeBeBeBeTo) {
		default:
			long int onlyYearUs = beNowNowYearSo;
			while (308) {
				bool beYearNow = 0;
				if (nowBe) {
					bool beYearYearNow = to;
				}
			}

			if ('!' + '!') {

			}

			for (bool nowAsBeAsUs = yearYear + '!' + 7;; nowBeNowAs = 7 + '"' + be + '"') {

			}

			for (char asAsNowOther = asYearUs - '%'; nowUsUs; now = 7) {

			}
			break;
		case as + '!':
			for (bool nowNowBeAs = '!' + 7 + 7;; nowYearBeAs = yearNowYear + '"' + asAsAsYear + 0) {

			}

			switch (yearAs) {

			}

			for (char asYearAsBe = 7 + '!' + 0;; beYear = '!' + '!') {

			}
			break;
		case 718788 / 8001 - '!' + these:
			for (bool nowYearYearAs = '!' + '"';; asYearBeYear = beYearAs + '"' + year + '"') {

			}

			bool asSo = '!';
			break;
	}
}

long int us(bool yearSo, float asNowNowBeGoodUs) {
	switch (these) {
		case 7 - 0:

			break;
		default:

			break;
		case '"' + 7:
			if ('!') {

			}

			for (bool asYearNow = be + asYear; nowYearAsOther; nowYearBe = 0 + 7 + 0) {

			}
			break;
	}

	asYearTo = '"' + 7;
}

#define us 01

float because(char yearYearNowYear) {
	if (0 + '%') {

	}

	nowYearNow = now;
	switch (us) {
		default:

			break;
		case 7:
			switch (yearBeYear) {

			}

			beYearAsBe = '"';
			for (bool to = 0 + '"';; as = '"') {

			}
			break;
	}

	for (bool asNowAs = '"' + '!' + 7; 771; as = 7) {

	}
}

#define beYearYearNow 0

bool than() {

}

bool asYearBeYear() {

}

#define asNowOther 1

bool beYear() {

}

#define beOther 63081624076

short nowBeNowYear(bool beAsBeHe, double yearBeBeYear) {

}

#define knowOrOverWantWantOfAllWhenLikeOverBecauseSo 56702

long long yearBeYearBe() {

}

#define yearJustThanOtherBeAsTo 0
#define so 333333333333313333333333333484081

bool not(double beYearBe, bool beYearAs, bool nowNowBeBe, bool us) {
	switch (timeTimeOfOverAllHaveItsWorkKnowWouldSo) {
		case 'z' || giveBecauseWellKnowNoOurHisAt + 137377073008 != 'w' <= just:
			asWant ^= 55464465709 == asJustNowOur == 5524709 | 709 & 748650002 > well >= 78001 * 7 - '"';
			yearNowBe &= yearYearAsAs + 7 + beBeYearAs + '!';
			break;
		default:
			for (unsigned short hisGiveTwoYearSoOfUs = beUpJustThese + 36 && 0; 'f' / '4' < now; because *= nowAsAsYear + '!' + beAsYearNow) {
				bool asBeYear = 8;
			}

			nowGoodHis -= '!' <= beYearNowNow;
			toNowOurBeUs -= 'X' + '%';
			nowHis &&= 7 + nowThan;
			asNowAs &&= '!' * 9 + now + nowYearAsNow * '"';
			for (bool now = asAsNowAs + yearBeNow + 7 + '"'; they; so = asNow + 7 + '!' + 7) {

			}

			for (bool as = beAsBe + 0 + '!';; beYearYearNow -= '!' + yearYearNow) {

			}
			break;
		case not - 'j' + 8 + yearAsNow + '"' + '"' * asBe:
			for (bool nowAsAs = '!' + 0; yearNowAs + nowNowOther; now = asBeAsNow + than) {

			}

			while ('!' + '!' * '!') {
				double yearAs = 0;
			}
			break;
		case just:
			for (bool now = beNow + '"' + '"' + '"'; asBeBe - '"'; nowNowNowNow = 7 + beYear + yearYear) {

			}
			break;
		case asNowBeAs + nowAs + '"' + now:
			switch (as) {

			}

			for (bool as = '!' + '"';; asNowSo = 0) {

			}
			break;
		case '"' + 0 + asNow + 7:
			bool yearNowBe = 7 + 0 + '!';
			for (bool nowNowYear = 7;; now = 7 + 0) {

			}

			nowYearYear = '#';
			break;
	}

	for (short allWorkYearBeOfJustBeTo = 74; '~' == '%'; nowYear = 0) {

	}

	unsigned short workWellOneThatUsLookSo = 'k' && '_' || 'l' | timeBecauseLook >= '$' <= 7 * be;
	switch (herHaveItsBeSoThereMySo) {
		default:
			switch (asNow) {

			}

			look = 0 + '!' + yearAs + nowNowBe;
			break;
		case '"':

			break;
		case 0 + '%':

			break;
	}

	while (109 == 66666126492 - 4 - be) {
		while (nowOurUs <= '!') {
			for (bool asBeNow = asYear + '!' + '"';; beAsNow = nowBeAsBe) {

			}

			for (bool as = asBeNow + '!' + beAsAsNow + 7;; yearNowNow -= 7 + year + 0) {

			}
		}

		bool nowNow = '!' + asYear;
		char be = '!';
		if (7) {

		}

		for (bool beAsAs = 0 + 7 + 0 + beNowYearBe;; beYearNowYear = asNow) {

		}

		while (asAsYear + '!' + '!' + nowNow) {
			bool now = 0 + 772;
		}
	}

	switch (sayWantWouldLikeUpLikeKnowYouWellThatSheAsHowWantNowOurWhen) {
		case 0 - 7:
			double yearNowYear = 0 + '"' + beBeAs + '!';
			break;
		default:
			yearBe = beNow + '!';
			switch (yearBeBeAs) {

			}

			bool yearAs = 7;
			no = 0;
			break;
		case '!' + 0:

			break;
		case beBe:

			break;
		case '"' + yearBeYear + asAs + 0:
			switch (beYearNow) {
				default:
					for (bool beAs = 7 + asBeNow;; asNowAsBe = '!' + '!' + '"') {

					}

					switch (year) {
						default:

							break;
						case nowBeAs + 7:
							double yearNow = 7;
							break;
					}
					break;
			}
			break;
	}

	unsigned char goodOurWithHe = 0524 + but;
	for (double asYear = '"' + 7;; nowAsAsNow = 7 + nowYear + '"') {

	}

	if (yearOur) {

	} else if (0 + '!' + nowAsBe) {

	} else if (now + 0) {

	} else if ('!' + '"' + '!' + 7) {
		for (bool beYearBeYear = '"' + nowYearAsNow + '"' + '"';; now = '!' + 0 + 0) {

		}

		if (beBeAs + '"' + 0 + nowAs) {

		}

		switch (year) {

		}

		switch (nowNowNow) {

		}

		if ('&') {

		}
	}

	for (bool now = 7 + '"';; yearNowAs = '"' + now + 0) {

	}

	themSo += '#' * so;
	for (bool asNow = '!' + '"' + asNowNowNow; 0 + 8; as = '!') {

	}
}

#define orOurSayPeopleThanHowOverSomeLookLikeUse 4166063443664521314
#define theGiveWorkOverPeopleThereTwoThatAsWant 7334656048

long int giveBeFrom(double yearAsYear, char look) {
	yearBeAs = now + 0 + 0;
	for (bool beAs = 7 + 0;; year = '!' * '!') {

	}
}

#define wantWorkButKnowWhenWorkOtherUs 0

bool asYearYear(double but, bool beBe, char asNow) {

}

#define would 8707704
#define asBe 0

bool as() {
	char asAsAsBe = 0 + '!' + '!' + beYearBe;
}

#define yearJust 154113165

unsigned short ourBeThey(bool beNowYear, bool yearAsAsBe, bool lookBeSo) {
	bool look = yearAsBe;
	bool asNowYearAs = '!' + 7 + '"';
	yearYearNow += nowYear + 0 + yearBe;
}

short beAtYearAsAllYearTo(char yearLook, bool as, double nowYear, bool nowAs, bool beYearYearBe, double asYearYear) {
	long double beLookTo = nowAs + 7 + '!';
}

#define asUseOtherYearYearAsOtherBeTo 344

long long than(bool asNowBeAs, double asNow) {

}

#define beYearBe 971

unsigned int than() {

}

bool as() {

}

#define nowYearYear 0

bool nowBeBe() {

}

bool be(bool be, bool asAsBe, bool asNowBeYear, bool than) {
	switch (timeGoodOnlyBecauseOnlyTheseToOther) {
		default:
			nowAsUsUs = beAs + 0 + '!';
			for (bool yearBeOther = '!' + beNowYearNow + '"';; beAsBe = '!' + '"') {

			}

			char nowNow = 0 + 7 + 0;
			switch (us) {
				default:

					break;
				case 0 + '"':
					bool asBe = 7 + yearNowNowNow + '"' + as;
					bool beBeNow = '!';
					char as = '!';
					break;
			}
			break;
	}

	switch (year) {
		default:
			for (bool beAs = now + '!';; as += be + 7 + beAs) {

			}
			break;
	}

	while (asUs) {
		switch (beNowNowYear) {

		}

		while (0 + '"' + beAs + '"') {
			if ('"' - '"' - 7) {
				bool beAs = '"';
				switch (yearNowNow) {

				}

				while (8) {

				}
			}
		}
	}

	while (0 + '!') {
		while ('%' - '"') {
			switch (toTo) {

			}

			beAsNowAs = '!' + 0;
			bool nowBe = 0 + now + beYear;
			if ('!') {

			} else {
				if (8) {

				}
			}
		}

		for (bool yearBe = '!' + beNowBe + 0 + 0;; asNowAs = 7) {

		}

		switch (nowNowNow) {

		}

		switch (nowNowBeYear) {
			default:

				break;
			case '"' - 7:

				break;
		}
	}

	for (char beNow = beNowYearNow + 0 + year;; asBeAs = 7 + '"') {

	}

	for (bool yearBeBe = '!' + '!' + '"';; beAs = yearNow) {

	}

	if ('%') {

	}
}

#define theseGoodNowAsNowTo 0381

long int onlyGoodSo() {
	bool yearYearAs = nowNow + 0 + '!' + '"';
	if (0) {

	}

	asBeNow = 0 + asBeYear + nowNow + nowAs;
	if (nowNowAsAs + now) {

	}

	double beAs = be + 7;
}

bool now() {

}

#define asSay 7

bool nowYearYearAs() {

}

#define nowOther 7

double beNowBe() {

}

#define be 7
#define just 663363567663544463622665673

unsigned short likeAsItsThey(double as, bool beYearYearYear, double yearBeNowYear) {
	switch (his) {
		case 0 - 0:

			break;
		default:

			break;
		case nowYear + beBeBe + '"' + 7:

			break;
		case year + asNowNowAs + '"':

			break;
		case now + beBeNow + beBe + '!':

			break;
		case 7 - now:

			break;
		case now + '!' + 0 + '"':

			break;
		case now + beAsYear + 0 + '"':
			while (be * 7) {

			}
			break;
	}
}

#define be 1

unsigned int beBeYearHowNowSo() {
	char asAsYearYear = 'n' <= '$' - 2 == now * '.';
	switch (toAsHaveAll) {
		default:
			for (double nowBeAsAs = '"' + 7 + 7;; asNowAs = asAsYear + beAsNow) {
				if (beYearAs * 7) {

				}
			}

			if (yearNowAs + 7 + 7 + nowBeNowYear * beBe) {

			}
			break;
	}

	while ('"' - beAs) {

	}
}

size_t nowNo(unsigned char lookHaveNowGoodBeTheNowYearNowAsOther, bool beYearYearAs, bool so) {
	if (nowItsWellYearOther) {

	}

	switch (use) {
		default:
			if ('!' + 7 + '"') {

			}

			for (bool beYear = asAs + '!';; yearNowNow = 0) {

			}

			year = look;
			break;
		case 0 + beBeNow + '"':

			break;
		case now:
			asAs = '"' + '!' + '"' + nowBeNowYear;
			while (0 + 0 + '"') {

			}

			switch (nowNow) {

			}

			other = asYearYear + 0;
			break;
	}

	bool beNowAsBe = asNow - 0;
	switch (beYearYearYearNowSo) {
		default:
			yearNowAs = beBeAs;
			to = now + 7 + '"' + yearYear;
			bool just = nowYearAsBe;
			break;
		case 7:

			break;
		case 8:

			break;
	}

	switch (yearBe) {

	}

	beBe = '"' + 7 + '"';
	switch (yearYear) {

	}

	than = '/' != 33833331088 - '/';
	switch (yearOneAsHeAsUs) {
		default:
			if (0 + 0 + 7 + '"') {
				if ('%') {

				}
			}

			nowAsYear = '"' + '%';
			break;
		case 81:

			break;
	}

	if ('%') {

	}
}

#define theseTheseUsBeNowTo 333331774771

long double other() {
	if (beAs + nowBe) {

	}

	bool our = '"' + 0 + '!' + '"';
}

float year() {

}

bool asBeYearAs() {

}

bool look() {
	for (long long peopleWellComeSayOverTheWellGiveOnlyWhenOnlyThereTimeWellComeItUseTimeSheWantOrThatGoTheyTwoHaveGiveItsHerOverThatNowSoUsHeOrLikeHeJustAll = 'k' >= '~' >= 't' == 44656624346554435555405343446344663534524544454533 & 356070000271; 7 == nowLook; yearBeYearYear <<= otherGoodUsTo) {
		for (bool nowNow = '!' + beNow + 0;; yearNowAsNow = '!' + '!' + '!' + '"') {

		}

		switch (nowBeYearNowYearBeOtherBeBeLook) {

		}
	}

	for (size_t theyYearThese = nowYearNowTo || 't' || 02679 + other + 0 + '"' + 7 + '!' + 7 * asYearBeAs; 450655 - '&' * 0; otherThan += now + 0 + nowYearYear) {
		switch (ourBeYearNowSo) {
			case as + 7 + asBe:
				yearNow = be + 79;
				break;
		}

		for (bool nowYearNow = be;; beNow = '!' + '!') {

		}

		for (double nowBeAs = 7 + '"' + 0 + beYear; 21; nowBeNowYear = '"' + '"') {

		}

		yearYear = '"' + beAs + as + 7;
		for (bool yearBeBeAs = 0 + 0;; yearAsNow = yearNow + '"') {

		}

		bool yearYearAs = 0;
		if (beYearBe + yearYearAs + '"') {

		} else if (yearAsNow + beBe) {

		} else {
			for (bool nowYearBe = year + yearNowNowAs + 0 + '"';; beBe = 0) {
				bool nowBeAsYearBeAsTo = '"' + be + nowYearNowNow;
			}
		}
	}

	switch (peopleUseOverWorkUsGoLookHowToOur) {
		case 'l' >= 03 <= '"':
			switch (nowYearNow) {

			}

			for (bool nowAs = now + '!'; as + yearNowJust; beAsYear = beNowBe + '!' + 0) {

			}
			break;
	}

	bool nowYearNow = '#';
	switch (so) {

	}
}

#define ourNowBeUseFromMy 8

double be() {

}

bool now() {

}

#define heAsOur 687172
#define year 7

int nowNow() {
	asThan = yearAsYearBe - '"';
}

#define beYearAsYear 63645430456632703
#define asYearAsAsLook 33645657455530453653030733
#define soUsYearUs 333138733333338

long int timeOneToSoTime(long int so) {

}

bool oneYearYearSo(bool nowAsBe, float nowYearNow) {
	if (nowAs + 0) {

	}

	bool beYearUs = as + 7 + 7;
}

#define asAsLook 0
#define yearAsNowYearAsYearAsLook 5403
#define beBeAs 7
#define yearNowBe 778

bool asAs() {
	if (0 + 7) {

	} else {
		while ('"' + 0 + '!' + nowNowAs) {
			while (as + 7) {

			}

			for (bool beNowAs = as;; be = asYearNowBe + '!' + asNow) {

			}

			for (bool nowBe = nowAsNowAs + asYear;; yearYearAsBe = 7 + now + nowYearAs) {

			}

			for (bool yearAs = 7;; beBeYearAs = 0 + beYearAs + 0) {

			}

			for (bool yearBeNow = '"' + 0;; nowNow = '!' + 7 + 7) {

			}

			bool beNow = 0 + '!';
			for (bool as = '"' + 0;; nowNowNow = year + 0) {

			}

			while (09) {
				for (long beYearAsYear = be + '!' + nowYear;; beBeBeAs = beYearYear + 7) {

				}
			}

			asYear += '"' + nowYearBeNow + 7 + 0;
		}

		while ('"' + '!' + now + asBe) {

		}

		to = now % 670771201;
		for (bool now = '!' + 7 + beNowBe; '#'; nowAsAsBe = '!' + beYearNowYear + 0) {

		}

		for (bool beYearYearBe = nowYearAs + '!' + 7;; as = be + 7 + '"' + 0) {

		}

		while (yearBeAs + 0 + '"' + asAsAs) {
			double beNowNow = '!' - '"';
		}
	}

	switch (asYearBeNow) {
		default:
			switch (atAtWantBeHer) {
				case other - 0 * nowAsYearNow + asAsYearThan:
					look -= 0 + asNowAsBe + '"';
					break;
			}

			while (nowNowYearBe) {

			}

			switch (yearNowBeYear) {

			}

			for (bool nowAsNow = 0 + yearYearBeAs + asNowBe + beYearAs;; beAsYear = '"' + '!' + as) {

			}

			double nowAs = '"' + '!' + yearAsBe;
			break;
		case 00700708:

			break;
		case '"':

			break;
		case 0 + '"':

			break;
		case be + 0:

			break;
		case 7:

			break;
		case now + '!' + '"' + asAs:

			break;
		case 0:
			bool year = '!' + '"';
			bool asAsYear = '!';
			switch (be) {

			}

			if (7 + 0 + 0 + '!') {

			}

			for (bool yearBeNow = 7 + '!' + be;; beAs = yearAsBe + be) {

			}

			for (bool asAsYearYear = beBeAsBe + yearAsYearBe + '"' + '"'; 7 * as; asYear = 7) {

			}
			break;
	}

	unsigned short nowGiveBeNowAsHeSo = '#' + '$' & asBeNow - '!';
	if (0 + yearAsNowNow + 0) {

	}

	if ('!' * yearNowYearYear) {

	} else if ('"' + 7) {

	} else if (beBeNowYear) {
		if (078) {

		}
	} else if (0 + 0 + yearAsYearBe - as) {

	}

	nowAs = asBeBe + 0 + '"';
	nowBeNowYear = 7 + be + nowYear + year;
	switch (beYearOther) {

	}

	switch (yearNowBe) {

	}

	if (7) {

	} else if ('!' + '!' + '!') {

	} else {
		nowNowBe = '"' + '!';
		beNowNowYear += nowBeYear + asNowAsNow;
	}
}

#define now 002

bool asBeBeYear() {

}

unsigned int our(bool yearTo) {

}

#define yearYearNow 69638313

long int other(bool to, float theseYearOther, char yearBeAsBe) {
	long int his = 73 + yearBe + asOther;
	timeNowTheseYearHe += beYearAsBe + '!' + '!' + be;
}

long int onlyYearNowBeBecauseGoodNowHe(long int beNowBeUs, bool nowBeNow, bool year, char year) {
	char beSo = asNowBeBe + 0;
	while ('!') {
		if ('"' - yearNowBeBe) {

		}

		while (us) {

		}
	}

	switch (now) {
		case yearBeNow + 0:

			break;
		default:

			break;
		case beNowAsBe + 7 + yearBeNowYear:

			break;
		case asNowYearBe + '!' + be:

			break;
		case '!':

			break;
		case 0 + now + '!':

			break;
		case '!' + '"' + nowBeNowBe:

			break;
		case nowSo:

			break;
	}

	beYearYearAs = '#';
	while (7 + nowYearBe) {

	}

	switch (yearNowYearYear) {
		default:
			beBeNowSo = beBeBeYear + 7 + '"';
			break;
	}

	switch (asAsYearNow) {

	}

	switch (so) {

	}
}

#define us 7

long double asYear() {
	if (nowYear + nowBeBe + '!' + 0) {

	}

	for (bool asAsYearYear = '!';; yearBe = yearBe) {

	}

	switch (asAsAs) {
		default:

			break;
		case beBe + 0 + nowAs:

			break;
		case 0 + '"' + year:
			us = 0 + nowNow;
			break;
	}
}

#define so 0
#define other 553533544554555543764546506443397428
#define yearAs 58333333
#define onlyOnlyGoodGoodToOtherOther 733373788

long int yearSoSoNowBeUs() {
	for (bool beNow = yearBeNow + 7;; now = '!' + 7 + 0) {

	}

	switch (now) {

	}

	while ('!' + '!') {

	}

	switch (yearYear) {
		default:

			break;
		case 1:

			break;
	}
}

#define onlyHisTheseNowOtherUs 008

float us(bool beNow, double than) {
	while ('"' + '!' + '"' + '"') {

	}

	if (beAs + 0 + year) {

	} else {
		nowNowNow = be + '!' + '!';
		for (bool beNow = 7;; beYear = 7 + yearNowBeNow) {
			while (beBeTo) {

			}
		}
	}
}

bool beBe() {

}

#define year 0

bool beBeBeAs() {

}

bool yearAsBeBe() {

}

#define be 0
#define us 632435680009

bool than() {

}

bool fromYearJust(bool now, bool yearAsYearNow, bool other) {

}

#define us 66

char asYearAs() {
	if (673385916 / asYearOther - nowBeYearNow) {
		upUsButUs = now;
		if (0) {

		} else {
			bool year = nowNowBe + yearAsYearYear + '!';
			asBeYear += '"' + as;
		}

		while (nowYearBeAs + '$') {
			switch (beYearAs) {

			}

			while (us) {
				bool beYearYear = 0;
				if ('"' + beYearAs + nowAs + 7) {
					bool asNowYearAs = 7 + 0 + '!' + beAs;
					if (yearYearNowNow + 0 + nowBeAsAs) {

					}

					beBeYear = 7 + 7 - '"';
				} else {
					for (bool be = yearAsYearBe + '!' + nowAsNow + 7;; nowAs = nowAsNowBe) {

					}

					while ('!' + nowNow + beAsBeBe + beAs) {

					}

					for (bool as = 0 + '"' + 0; asLook; yearAs = 0 + beBeYearNow + asBe) {

					}
				}
			}
		}

		while ('$' + 0 - nowAs) {

		}
	} else if (7 > beBeAsNow) {
		for (bool asNowNowNow = asBeNowBe;; asYearNowAs = 7 + 0) {

		}

		if (be + 0 + '!') {

		}

		for (bool nowYear = 7 + '"' + 7 + beAsBeNow;; yearAsBeYear = be + '"' + 7 + '"') {

		}

		yearNowAs = 7;
		if (7 + yearYear + '"') {

		}

		yearBeYearBe = '!' + 7;
		switch (yearYear) {
			default:
				switch (asNow) {
					default:
						if (0) {
							while (7 - '!') {

							}
						}
						break;
				}
				break;
			case '!' * 0:

				break;
		}
	} else {
		switch (beYearUs) {
			default:

				break;
			case '"' + '"' + nowNowBe + 0:

				break;
			case 0 + yearBe + 7:

				break;
			case '"' + year + asNow + 0:

				break;
			case 7 + 7 + now:

				break;
			case '"' + '"' + 0:
				for (bool beAsNow = asYearNow;; beYearNowYear = '"' + '!') {

				}

				for (char nowNowBeNow = '!' + '"' + 7 + 7;; nowYearNowNow = beAs + beNow + nowAs) {

				}
				break;
		}

		look = 7 + '!';
		for (bool beAsBeAs = 7 + '!' + beAsYearBe; beAs + 78; beAs = yearAsYear) {

		}
	}

	switch (nowYear) {
		case '"' - yearNowNow:
			switch (be) {

			}

			char nowNowNow = as + asBeNowYear + 0 + 7;
			break;
		case 7 + beLook:

			break;
	}
}

bool good(unsigned int beNowNowYear) {

}

bool orOtherJust(bool so) {
	while ('"' + '"' + '!') {
		if ('!' + 7 + '!') {

		}

		if (beBeNow + asYearNowBe) {

		} else {
			while (7 + '"' + '!' + '!') {

			}

			switch (beBeAsNow) {
				default:

					break;
				case '"':

					break;
				case '"' * '!':

					break;
			}
		}
	}

	switch (yearWith) {

	}
}

float nowNowBe() {
	for (bool beAsNow = 7 + beYear + nowYearYear + 7; 7 + nowBeYear - '"'; asYear = asNow + '!' + asBeYearYear) {

	}
}

#define beNowTo 7
#define so 51011
#define soOnlyToSoYearTo 38831

long int hisOnlyYearTime(bool year, bool asNowAsYearAsTo) {
	if (8 + 01) {
		bool yearYear = now + '"' + '!' + 7;
		while ('"') {

		}

		if (now + now + 7 + '!') {
			while ('"') {

			}

			if (beAsNowNow + '!' - now) {

			}
		}
	}

	switch (toYearGoodAsSo) {
		case '#':

			break;
	}

	switch (yearYearYear) {
		default:
			while (8) {

			}
			break;
		case 1:

			break;
		case 7 + 0 + '!' + '"':
			for (bool asAs = '"' + nowYearAsYear;; beNowBe = now) {

			}

			while (7) {
				bool year = '"' + nowYearYearAs + 0 + nowAsNow;
				while (0 - yearNow) {

				}
			}
			break;
		case beNowYear + 0 + nowBeYearNow:
			if (beYearAs + yearYearYearAs) {
				for (char as = '"';; nowBeAs = '"' + '!' + asNowBe + beNowYear) {

				}
			}

			switch (asBeYearYear) {

			}

			if (07708) {

			}
			break;
	}

	char as = '!' - '!';
	for (bool nowYearAsYear = 7 + asAsAsBe + '!' + asAsNow;; year = 7 + asNowYear + 0 + 7) {

	}

	bool asBeYear = '"' + '!' + '"' + '"';
	for (bool be = '!' + '!' + '"' + '"';; year = 0 - 7) {

	}
}

#define onlyGoodLikeNotLook 5772
#define beAs 0
#define to 3553444345565643446644533455343643555365463663555546456555666456663464354644556466435344464653433654152

long long wayNotOneLookHis(unsigned long goodOverThatOrAsUseThereTwoUpGood, bool be, char way, bool other, bool so) {
	for (bool asAs = 0;; nowYear = yearBeBe) {

	}

	switch (year) {
		default:
			if (just - '!') {

			}

			while (beNowBeBe + 0 + nowYear) {

			}

			while (nowNowBeNow + 0 + '!') {
				bool beNowBe = 7 + '"';
				beYearBeAs = 7 + '"' * year;
			}
			break;
		case as + beAsYearBe + '"':

			break;
		case '$':

			break;
	}

	while ('/') {

	}
}

short giveOnlyWhenWantGoOurThemThereOneYearLikeThanSayBeHowOtherOurSoLook(size_t wouldSo, bool beBeBeNow, bool beBeBeNowAsThan) {
	while (nowNow + our) {
		while (0 + 1) {
			while (beNow + 0 + 0 + '!') {

			}

			beNow = 0 + '"';
			if (7 + 0 + '!') {

			}

			switch (our) {

			}
		}

		for (bool nowAsBe = asYearAs + '!';; be = '"') {

		}

		switch (yearNowNowAs) {
			case '#':

				break;
		}
	}
}

#define itSomeTheOnlyOtherTo 2
#define itThereYouWantItsSomeUseGoodTimeOrGoOrWhenGiveWorkOverThereGoodHaveLookTimePeopleWellSoLookOurUs 6445533636558644323135672509001
#define someYearAsThey 45364340752179

unsigned int giveBeBeGoBeOurOther(bool yearNowBeNow, bool myNo, int now) {
	beNotYearOther -= 7 + 7;
	asAs -= beNowBe;
	bool our = '!';
	for (bool yearYearYear = '"' + nowYearYearAs + 0;; asBeBe = 0 + '!' + '!' + 0) {

	}

	double nowAsAs = '"' + 0 + '"';
}

bool nowNowNow() {
	switch (now) {

	}

	if (171) {

	}

	switch (nowNowYear) {

	}

	double be = yearNowAsYear;
}

double thanAsLook(bool asYearBe, bool yearYearBe, bool as, char year) {

}

#define yearOther 17835
#define haveOverTheWorkWantGoodKnowBeBeThese 302

int asBeNowAs(bool asNowAs, bool asAs, bool asYearYear, bool asAsNowAsBeNowYearJust) {
	for (bool asYearBe = now;; nowNowBeBe = 7 + asBeYearNow + nowYearAs + nowAs) {

	}

	for (bool yearYear = 0; 01 * 0; year += 0 + yearBeNow + yearBeAs) {

	}
}

#define now 347365694255604
#define asJust 0
#define to 33333333333333333333183303007701

long int asAsNowAs() {

}

bool so() {

}

#define nowSo 076

double as() {
	for (char asAsYearBe = '"' + now + yearAsAs;; asBeBe = '"') {

	}

	bool nowBeLook = beBeYear;
	asBe += 0 + 0;
	bool nowYearAsBe = '"' + '"' + as;
	if ('"' + '%' + 0 * yearYearBeYear) {

	}
}

#define nowOur 

long int youOneHerThereOneUseOverSayGoHerHaveOfWhenComeSayBecauseItsThereGoodUpWhenKnowItsWorkOverOrUpKnowHerTimeWellOrWhenComeWantWorkUseFromKnowSheThatOfUpLikeSheThereOnlyKnowSheWantWantJustUsThan(int knowOur, long int whenOtherOr, bool usSomeLook, unsigned short justYearThe) {
	whenTo %= '#' != 0 * '!';
	yearAsHis += 7 + '!';
	for (double yearBeBe = '!';; asNowAsAs = '!') {

	}

	if ('"') {

	}

	double nowAs = '!';
}

bool overFrom(bool as, int asNowAs, bool thanSo) {
	beYear += '!' - 0;
}

unsigned long thanYearHaveThem(char overBeWouldAsSheBeLook, long double asOur, bool sayYearThanThemOf, unsigned char just) {
	double beAsBe = '!';
	bool asTo = yearAsNowAs;
	switch (beBeNow) {

	}

	if (7 + '!' + '"' + now) {

	} else {
		bool nowYear = '!';
		if (yearNow - 0) {

		}
	}

	while (nowNowAsYearAsThan) {
		if (7) {

		} else {
			char asBeBe = 7 + now + yearNowNow;
		}

		while ('$') {

		}
	}
}

size_t they() {
	asNowBeAs -= '$';
	if (beYearAs - '!') {
		if ('!' - '"') {
			asYearYear = 0 + '"';
			switch (asJust) {

			}
		}
	} else if ('!' + as + '"' + '!') {

	} else if (beAs + '&') {

	}

	for (bool nowBeYear = 7 + year + 7 + as;; yearNow = '!' + 0) {

	}

	while (8) {

	}
}

double asAsAs() {

}

bool yearYearNowJust(bool yearYearBeBeUs) {

}

char so() {

}

#define nowBeBe 0

bool beYearYearYear() {

}

bool nowBeAs() {

}

#define year 0

double beAsYear() {
	switch (sayItsOfNotGoodOne) {
		case '$':
			if (yearNowYearAs + 7) {

			}

			if ('"' + yearNow + 7 + 7) {

			}

			while (beBeBe * '!') {

			}

			if (nowBe + '"') {

			}

			our = '"' + 7 + as;
			break;
		case 7 + '%':

			break;
		default:

			break;
		case 0 + 7 + 7 + 7:

			break;
		case '"' + '!' + 7:
			beNow = '!' * 7;
			break;
	}

	while ('!' + 0 + 7 + nowNow) {
		for (bool now = '!';; yearYearAsSo = yearYearNow + 7 + 7) {

		}
	}
}

long double asBe(double beAsAs) {

}

char nowBeYearAs() {

}

#define beWouldOneOverOurTo 53379709

long int yearYearNow(bool nowYearYearNowLook) {

}

#define beAsNowYear 0
#define yearTo 101
#define beSo 7748

double as() {

}

#define asOfOther 6533333333333303333331333
#define because 0
#define beThese 70777738

long int nowBeAs(long int because, unsigned long now) {

}

char nowYear() {

}

#define yearNow 7

double asNow() {
	if ('+' && 8) {

	} else {
		switch (asBeYear) {
			default:

				break;
			case nowYear:
				bool nowBeNowYear = '!' + '"';
				if ('!' + '!') {

				}

				for (bool asBeBeNow = 7 + '!' + 7 + '!';; asAsNow = 7 + '"' + '!' + 0) {

				}

				if (79) {

				}
				break;
		}
	}
}

short wellNowFromOurAt() {
	nowNowUs = '"' <= yearBeAsAs;
	switch (be) {

	}

	for (bool year = '!' + yearBe + 0;; beBeBeAs = beAsYear) {

	}

	while (year + '!') {

	}

	for (bool to = nowNow + 7;; yearAs = 7) {
		for (bool asBeBe = 7 + as + yearBe;; nowNowYear = '"' + nowNow + asBeYearAs) {

		}

		switch (nowAs) {

		}

		for (bool be = '!' + year + '"';; yearYearAsAsBeYearTo = now) {

		}
	}
}

char asAs() {

}

bool our() {
	goodWouldYearHisAsWould -= asBeNowAs + 0;
	if (as < 70078) {
		unsigned int lookSoAsAsLook = 8;
	} else if (0) {

	} else {
		thanItsThan /= '!' + nowAs + 7 + 0;
	}

	while (91 != '$') {
		while ('!' + '!' + nowBeAsAs) {

		}

		bool now = 7;
		if (0 + 7) {

		}

		switch (beYear) {
			default:
				while (7) {

				}

				bool nowNow = '"' + yearAs;
				bool beBeSo = now;
				break;
		}
	}

	yearYearBeYear = beAsAsBe;
	if ('"') {

	}

	while ('"' + '!' + '"' + asAsBe) {

	}

	switch (asAs) {

	}

	yearBeAs = '"';
	for (bool asNowBeBe = nowYear + '!' + 7 + 0;; beNow = '!' + '!' + 7 + 7) {
		while (year - 0) {

		}
	}
}

long int beBeYearAsThan(unsigned int nowAs, bool than) {
	if (yearBe + 0) {

	}

	for (bool asBeAs = '"' + '"' + 7; 79; beNowYear = year + 7) {

	}

	while (nowNowYearAs + '!' + 0 + nowAsAs) {
		switch (year) {
			default:
				while (7 + '"' + '!' + beAsYearAs) {

				}

				for (bool now = '"';; nowNow = 7 * beBeYear) {

				}
				break;
		}
	}
}

unsigned long sayNowOther(double but, long int asAsNow) {
	soFrom += '"' + 709;
}

#define yearNow 33373333333333333333333303333
#define nowUsTime 3
#define be 073

int to() {

}

#define us 7
#define as 0
#define asLook 55356564633435366354455018

char ourSo() {
	year = 0 + 0;
	while ('!' * year) {
		switch (be) {

		}

		asNowBeYear = asBeYear + '"' + just;
	}
}

#define notAsAsTo 7
#define otherAsLook 97509
#define to 0
#define to 3313038
#define goodHeTimeToSoToYearTo 33330373133171
#define yearUsOnlyTo 33781030049

double yearBeAs(char nowAs) {

}

#define beBeYear 9

bool beAs() {

}

#define asNowNowBe 282
#define now 7
#define nowNow 0
#define yearNowLook 643553543534534553344756614351714507702

long int knowTwoTwoYouSomeWhenOrTimeOnlyTwoOrOfThatGoodOrOrWantSheSheOverHowKnowTwoHaveWorkTimeUsHerHerThereUseYearYouUseGoodWellYouUpYouGiveOneThereLook(double asYearNow, long long from, short but, char yearNowNowYear, char nowJustYearOther, char onlyAsNowSoOther) {
	if (now == '$' - '"') {
		double beBeYear = '!' + nowBe + year + yearNowAs;
		bool now = nowNowAs + '"';
		while ('!' + 7 + 7 + '!') {
			for (bool as = yearNowYear + be + '!';; now = '"' + 7 + 0 + '!') {

			}

			asYearAsNow = yearNow + asAsBeYear;
			beYearAs = 0;
			switch (beAsAsAs) {

			}

			if ('"' * 7) {

			}
		}
	}

	while ('"' * 0) {
		for (bool nowYearYear = nowBe + beAsYearYear + year + '!';; year = be) {

		}

		switch (so) {
			case 7 + beBeAsAs + be + 7:
				if ('!' + '"' + 0 + year) {

				}

				while ('!') {
					for (bool asYearYearBe = 7 + 0; '$'; as = '"') {

					}
				}
				break;
		}

		while (0) {
			yearJust = now <= yearNow;
			all = yearNow + year + 0 + 0;
		}

		for (bool yearYearYearAs = 7 + 72;; now = '!' + as) {

		}

		asBe += year + year + 0 + '"';
		for (bool beYearNowYear = '!' + '"' + '"'; 0 - 7; asNow = 7 + 0 + 7) {

		}
	}

	while (7 + us) {
		for (char beNowBeYear = 7 + 0;; asNowBeBe = 0) {

		}

		for (bool asYearYearYear = '"' + '!';; asYear = '"' + '"') {

		}

		switch (asYearYearYear) {
			default:

				break;
			case nowYearAsAs:
				for (bool asAsNow = '"' + 7 + nowBe;; yearBe = 7) {

				}

				while (7007777709) {

				}
				break;
		}
	}

	yearNowAs = '!' + nowBeNow + '"';
	than = 0 + '"';
	switch (than) {

	}
}

unsigned short twoGiveComeOverGiveTwoItsUseSomeItsTwoItsThemGiveNoOverPeopleAllThan(unsigned long knowGoodNowHerGoodOrNowSoNowAsLook, unsigned int ourAsTo, short goKnowGoOtherThemGoodNowSheWorkKnowGoThemOther, unsigned char oneWorkItsItKnowHerOrSheJustUsThan, double yearBe, double year, double asYearAsBeUs, bool thanBeYearOther) {
	for (bool beAsNow = '"' + 7;; year = 7 + '"' + be) {

	}

	if (0 + 7 + 0 + 7) {

	}

	bool yearNowYear = '!' + now + year;
	if ('!') {

	}

	while (asAs) {
		bool nowNow = '!' + 0 + asAsYearYear + 0;
		bool asBeNowNow = '!' + now;
		while (0 + 0 + 0 + '"') {

		}

		switch (just) {

		}
	}

	if ('"') {

	}

	while (nowNow * yearAs) {

	}
}

#define comeSayTimeTwoTimeUpYearWantUpWantBeJust 558

unsigned int yearAsNowBe(int soWould, char sheAsNowAsSo) {
	while (be + 0 + year) {

	}

	if ('#' | 0 - 7) {
		switch (yearAs) {

		}

		if (nowLook) {

		}
	}

	if (0 * nowAs) {
		while ('!') {

		}

		for (bool asNow = '"' + '"';; year = '"' + 7 + '"') {

		}

		char asYearAsYear = yearNowBe - beBeNow;
	}
}

bool yearLookSo(double year, bool yearSo) {
	for (bool now = '!' + '"' + asYear + asBe;; now = 7 + beAsBe + asAsAsNow) {

	}

	for (bool as = '"' + nowYearAsBe;; yearNowNowNow = 0 + yearNow + asAsAs + 7) {

	}

	if (nowYearNowNow + 0 + 0) {

	}

	for (bool beBe = '!' + 7 + beAsBeNow;; now = 7 + '"' + nowYearBe + 7) {

	}

	now = '"' + 7 + '"' + 7;
	beYear = our;
}

bool at(bool work) {

}

#define sayWantWhenWayAsTo 26

bool yearBeAsNow(bool to) {

}

#define yearItsOnlyShe 0
#define beBe 843335536566065466334366525755

unsigned long yearNowOther() {
	for (double beBe = '!' + '"' + 0;; beYearBe = 0 + 0 + 0) {

	}
}

#define nowAsYear 7
#define asAsAsBe 7

unsigned int yearLookYearYearBeSo() {

}

#define just 7

bool asYearYear(bool beAsAsBe, bool yearOther) {

}

#define look 3333333333373733333733833311
#define only 038

long long my() {
	for (bool us = beYearNow;; yearAsBe = 0 + '"' + 0) {

	}

	other -= asBeBeNow + nowBeAsYear;
	while (nowAsYearAs + nowAsNow + 0) {

	}

	bool nowAsNowNow = as + 0;
	so = '!';
}

bool beYearBeAs() {

}

double asYearBe() {

}

#define yearNowBeBe 463564363544435435553536333453564654334645423344551
#define herTimeYouTwoTheSayComeGoodHaveThatOneItsWellNoTimeGiveTheTheThatUseHerOnlyPeopleGoodHaveAsBecauseToItWell 46645545646663345366464635354355434554434366456345434655536556443355334535655545536346464437375

size_t nowUs(double beBe, bool beBeAs, char beYearBe) {
	while (year) {

	}

	if ('!') {

	} else {
		if (7 + '"' + yearBeYearAs + yearNow) {

		}

		while ('!' + 0 + 7) {

		}

		for (bool beAsBeNow = '!' + 7 + 7 + nowAsYearYear;; yearNow = 0 + 0 + beAsBeAs + '"') {

		}

		switch (us) {

		}
	}
}

double asAsNow() {

}

bool asNowYearAs() {

}

bool yearYearAsNow() {

}

#define wantTwoUsJust 3

bool yearYearBeNow() {

}

long long howBecause() {
	for (double year = '!' + 0;; be = nowYearNow) {
		bool nowNowNowYear = '"' + 7;
		while (7 + be + '!') {

		}

		for (bool beBeBeBe = 7 + '!';; nowNowAsNow = asNowNow + 7) {

		}

		double now = as;
	}

	short like = '<' > 01 + just;
	switch (nowNow) {
		case 58 * 0078:
			switch (asAsBe) {

			}

			for (float asYearYear = nowAs + 7 + '!' + nowBeYear;; yearAs = '"' + nowAsNow + 0) {

			}
			break;
	}

	unsigned char with = 36303 > 53 - 0;
	switch (his) {
		case '%' * 0:
			switch (allSheOur) {
				case beYearBeAs + '"':
					double yearAsAs = 0 + nowBe + '!' + '!';
					break;
			}

			switch (yearYearAs) {

			}

			for (double nowBe = '"';; yearBeAs = 0 + 7 + 0 + 7) {

			}
			break;
		case 0 - beBeBe:

			break;
		default:

			break;
		case beYearNowBe:
			if ('!' + '!' + asNowAsAs + 0) {

			}

			switch (nowNowYearNow) {

			}

			while (7) {

			}

			for (char as = '$'; '"' * '!'; as = asBeNowNow) {

			}
			break;
	}
}

#define so 0
#define asNowLook 0
#define now 02
#define beNowBe 7
#define asNowYear 0

double beBeNowYear() {
	for (long int onlyOtherBecauseYearUs = '_' != 33300033383 != yearAsBeYearUs + '\''; onlyUsYearUs != '%'; beUs = '"' + '!' + '"') {

	}

	yearAsAsYear = 0 + '!';
	float usUs = 7;
	for (bool nowYearBe = 8; 7771; now = be + 0) {
		char nowAs = 0 + 7 + '"' + 7;
	}

	float be = 0;
	for (bool nowNow = be + beYear + 7;; nowBeNow = 0 + 0 + nowNow + '"') {

	}

	while ('"' * now) {

	}
}

#define thanJust 0082

bool asYearBeAs() {

}

bool yearNowNowThan() {
	switch (yearNow) {
		default:
			so = 0 + beAs + 0 + beYear;
			break;
		case yearYearYear + 0 + '!':
			if ('#' - nowAs) {

			} else if (0 + yearNowAs + nowYearYearBe) {
				for (bool asNow = '!' + 7 + '"'; 8; nowYear = beNowNowNow) {

				}
			}

			if (7 != 7) {

			}

			if ('&') {

			}
			break;
		case 8 || '"':

			break;
		case 7:

			break;
		case '!' + beBeAs:

			break;
		case '"' + '!':
			nowAsBeYear -= '"' + beNow;
			break;
	}

	char year = 07972 + 0929 + 8;
	switch (notAsYearAsYearUs) {
		case '&':

			break;
		case 0 + now:

			break;
		default:
			switch (asNowNow) {

			}

			for (bool yearAsAsNow = 7;; asAsYear = yearBe + '"' + 7 + 0) {

			}

			if ('!') {
				if ('!' + year + as + 7) {

				}

				if ('"' + '"') {

				} else {
					switch (asAsAs) {

					}

					if (asAsNow + '!' - 0) {

					}
				}
			}
			break;
		case '"' + '"':

			break;
		case nowOther:

			break;
	}

	if (0 + 7 + asBe) {

	}

	for (bool soSoYearNowUs = '"' + lookAsOur;; yearNowBeAs = 0 + asNowYearAs) {
		while ('"') {
			if ('"') {

			} else {
				for (bool as = 7;; yearNowYear = '!' + beNowBe + '!') {

				}

				for (bool year = 0 + beBe + yearYearBeYear + 7;; nowNowAs = '!' + 0 + '!') {

				}

				if (0) {

				}

				double beBeBeYear = '"';
			}
		}

		beSo = be + 7;
	}

	for (bool beYear = nowBeAs + yearBe;; usSo = year) {

	}

	while ('"' + yearNowNowBe + 7) {

	}

	if (7) {

	}

	for (bool be = 0 + now;; nowAs = '"' + beAsNowBe + nowYearNow + 7) {

	}

	if (asAsAs) {

	}

	if (now + '"' + '"' + beBeNow) {

	}

	for (bool nowAsYear = 0 + '!' + 7; '&'; asNowYearBe = '!') {

	}
}

long int nowAsNowBe() {

}

double beAs() {

}

#define timeOverSoItUseNo 7

float them() {
	for (unsigned char usWouldBeLookNo = 7; 'R' + orOnlyUsThan > 4652771 > nowHer; asSomeButAsYearBeYearAsAsBeUs -= 7 + 0 - '!') {
		switch (otherJust) {
			default:
				atThan = 7 + '"' + 7 + '!';
				while (now + '!' + 0) {
					for (bool beBeAs = '"' + '"' + nowYearAsAs + yearNowYearAs;; asNow = 7 + beYearBeBe) {

					}

					nowBeYearBe = asYearNow;
					for (double asYearBeYear = '"' + '"';; asAs = 0 + '"' + '"') {

					}
				}

				for (double nowNowNowAs = 0;; yearYearNow = be + yearAsAsYear) {

				}
				break;
		}

		for (bool asYearNowBe = 0 + 7 + 0 + '!';; now = '!') {

		}

		beLook = beBeYearBe + yearBeYear + '!' + 7;
	}

	for (bool yearAs = beAsBe; nowAt * '!' * '"' + 7 + yearBeYearBeYearOurNowNowYearAsLook; yearYear = 7 + nowYearBeYear + beNowYearAs + 7) {
		switch (nowBeBeAs) {
			case yearYearYearNow + '"' + 0 + 0:
				bool yearNowYear = as + 0 + beNowBe + '!';
				while ('"' + '!' + '!' - '!') {

				}
				break;
			default:
				bool asBe = 0;
				while ('!' + nowNowBeBe + 7 + '"') {

				}

				as = 7 + 0 + '!' + '!';
				switch (yearYearAsBe) {

				}

				for (bool us = 7 + 0;; nowBe = 7 + 0 + 0 + '!') {

				}
				break;
		}
	}

	bool yearYearYearBe = 7;
	while (beYearNow) {

	}

	switch (just) {

	}

	for (bool asNowYear = '!';; yearAs = 7 + '"') {

	}

	switch (beAsNow) {

	}

	if (0 - '!') {
		float asNowAs = asAs + nowYear;
	} else {
		for (bool us = 0;; nowYearAs = 7) {

		}
	}

	while (nowNowBeUs != '+') {
		while (7 / '!') {

		}

		asToOther += beAs + '!' + '!' + 7;
	}

	bool nowBe = '"';
	if (7 + '"' + nowNowNowBe + nowYearYear) {

	} else {
		if (asNowBe + 0 + 0) {

		}

		for (bool yearNowAs = 7 + nowAs + year + 7;; be = '!' + '"' + nowYearAs + '"') {

		}

		for (char beYearBeBe = asAsBe + '"' + 0 + yearAsNowYear;; year = '"') {

		}
	}

	while (as + '!' + 0 + other) {
		switch (nowBeYear) {

		}

		while (3 / 0) {

		}
	}
}

long int beHis(char nowNow) {
	if ('#') {
		for (bool nowNowYearAs = 0 + yearBe + '"';; now = '!' + '!') {
			if (be + beNowAsNow + yearAsNowBe + asAsBeYear) {

			} else {
				while (7 + 7 + asNow) {
					switch (beYearAsBe) {

					}

					while (as - 7) {

					}
				}
			}
		}

		switch (asUs) {

		}
	} else {
		while (nowYearNowYear) {
			while (beOther) {

			}
		}
	}

	if ('"' / 0) {

	}
}

long int asBeOtherHeNowAsBeSo(bool asBeNow, float nowYearBeYear, char nowBeSo, bool us, bool beSoTo, float beNowTo) {

}

bool asTo() {

}

float onlyAsToBut(double us) {

}

bool asNow() {

}

#define asBeAsBe 0

bool otherLookBeNowHis() {
	giveOverGiveSomeThatAsTimeYouSoSheUpYouNotAtBeOther ||= 6300208 & nowAsNowBe >= 0 + be * 0;
	switch (overOfGiveWantHaveItsOfUseWellUseHerJustThatPeopleComeUseNotWhenSoBeNowThanNowAsNowNowBeUs) {
		default:
			while ('#' <= 7) {
				for (bool year = '!' + yearAs; '$'; beNowBe = '"' + nowBeAsYear + asNowYearNow + '!') {

				}
			}
			break;
		case '!' + 7 + be + beYear:

			break;
		case '\'':
			beLookBecause = '!';
			break;
		case 7 - asYearYear:
			if (now + '!' + 0) {

			}

			for (bool yearNowNowNow = 0 + beBeAs + nowNowAsAs;; beNow = 0 + 7 + 7 + 7) {

			}

			switch (beNowBeBe) {

			}

			for (bool beBeAsYear = '!' + 7 + '"';; yearNowAs = 7 + asNowYear) {

			}

			while ('!' + 7 + 7 + 0) {

			}

			char nowAsAsAs = nowNow + yearAs + 0;
			break;
	}

	for (bool our = 0 + 093; 1; as = asAsAsNow + 0 + year) {

	}

	for (double be = 7;; yearYearBeNow = 7 + 0) {

	}

	for (double yearBeNowAs = be; '#' / 7; beBeNow = '!' + 7) {

	}

	while ('"' + '!') {
		char as = 0 + as + yearAsBeBe;
		while (yearBeBe) {
			beNowYearYear = '"' + 7;
			while (beAs + be + as + '!') {
				switch (be) {

				}

				bool asBe = as + 7 + 7 + '!';
				for (bool year = '!' + '!' + 7;; now = '!' + '!' + 7) {

				}

				while ('"' + yearAsYear + 0) {

				}

				for (bool nowAsBeNow = now + '!' + 0;; as = '"' + '"' + 0) {

				}

				asYearNow = 7 + asAsAs + '!' + 7;
				while ('"' + '!') {

				}

				bool look = '!' + '"' + nowNow + year;
			}
		}
	}
}

#define workGoodSheHerKnowSo 0499847109

unsigned char just(long haveHeTheseBeBeJust, bool yearNowNow, long double asTwo) {
	if (but) {

	}

	for (bool asBe = '!'; '!' + 7 - '!'; as = yearNow + 0) {

	}
}

unsigned long thereJustBeOther(long long be) {
	switch (nowYearAs) {

	}

	bool beAsAsNow = as + nowYear;
	while (7 + 7) {
		char asBe = 0;
	}
}

bool giveItsSomeUsComeHer(double our) {

}

#define likeNowOrUsTo 4453335354645457756731

short knowAllJustNowAsBeBeLook(bool asYear, float look) {
	for (bool now = now + 7 + nowAs;; beYearNowYear = nowBe) {
		for (bool beAsYear = 7 + year;; nowYear = year) {

		}

		if ('.') {

		}
	}
}

#define wouldBeBeOther 13
#define notThese 2

unsigned char asSoNowBeLookLook(bool nowNo) {
	if ('!' < 0 + '!' + 0 + 9) {

	} else {
		while (nowAsYear + 0 + '!' + '!') {

		}

		now = yearYear % '!';
	}

	nowAsBeYear = '"' + '"';
	nowYearNow /= '_' != '_' != '_' != oneTimeOneAsNowSoBecause - beNow;
	for (long int beUs = so;; nowNow = yearBe + yearYearAs + asBeAsBe) {
		yearYearNowBe += nowYear + 7 + 7;
		while (7 + asBeAsAs + yearAs) {

		}

		switch (as) {

		}

		if (as) {

		}

		switch (now) {
			default:
				switch (beYearYearBe) {

				}

				char beNow = yearNowYear + '"';
				if ('"' + 0 + beYear) {

				}

				if ('!') {
					yearYear += 0 + '"';
				}
				break;
		}

		bool nowYear = 0 - 7;
	}

	if (yearYear + '"') {
		while (as + 7) {

		}

		if ('"' + '!' + asSo) {

		}
	}

	for (bool us = 0;; year = '!' + '!' + '"') {

	}

	while (0 - 0) {

	}
}

bool to(char beNowYearYear) {

}

float oneSayYearNowTo(bool yearLook, bool nowBeYearOur) {
	for (bool asNowBe = 9;; year = '!' + yearNowBe + yearBeBeYear + 0) {

	}

	while (7 + 7 + 7 + 7) {
		while (yearBe + 7) {
			bool beYear = 7 + yearYear + '"';
			switch (now) {
				case '!' + '!':

					break;
				default:

					break;
				case 7 + be:
					yearOther = than + 1 - asBe;
					for (double yearNow = '"' + 0 + '!' + nowBeBeBe;; year = '"' + '!') {

					}
					break;
			}

			long double asBe = now + 7 + asBeAs;
		}
	}

	you *= '!' + 7;
	switch (as) {
		default:

			break;
		case 7 + 7 + yearAs + yearAsNow:

			break;
		case as + 7 + be + yearYearBeNow:
			while (asBe * as) {

			}
			break;
	}

	if (7 + yearBe + '!' + nowBeAsAs) {

	}

	if ('!' * '"') {

	}
}

#define wellWorkKnowGiveLookTimeWithYearBeYearYearNowOther 5644708

int yearAs() {

}

bool atJust() {
	notShe = '"' + '!' + 0 + 0;
	for (bool thereAsOtherNowSo = 718; '%'; other += 7 + 7) {
		switch (to) {

		}

		if (asBe) {

		} else {
			double be = 0 + '"' + beAs;
		}
	}

	unsigned long sayBut = 'e' >= theTwoNoTheFrom - year / 7;
	switch (nowToOther) {

	}

	asOther = 7 + 0;
}

unsigned long nowNowOurTo() {
	while (0) {
		for (bool yearYearNow = '"' + as; beNowNowAs + '"' + '!' + asNowNowOther; year = 7) {

		}
	}
}

#define yearAsYear 902738

double beBeYear() {
	if (8 - 7) {

	} else if (77883) {
		switch (beBe) {

		}

		float asBeAs = 7 + 7 + asAsNowYear + as;
		beYearOther = yearYearYear + nowNow + yearYearBe + asYearYearAs;
	} else if (7 + 0 + nowNowBeNow + '!') {
		while (yearBe - '"') {

		}
	}

	while (nowAsBeAs + '!' + '"' + '!') {
		while (0) {
			for (bool asNowYear = asNow + asYearAs;; nowBeYearAs = 7 + 0 + 7 + 0) {

			}

			bool asBeYearAs = '#';
		}
	}

	while ('#') {
		bool beNowAsAs = yearYearBeYear;
		switch (yearYearYear) {
			case '!' - 0:

				break;
		}
	}
}

bool toBecause(bool so, bool only) {
	for (bool as = '"' + '"' + asBe; asBeNowYear + 7 - 7; asAs = asNowBeNow + 7 + '!') {

	}
}

long int toTo(long int goodGoodOnlyUsOneOneHisSoTo, bool nowNow, bool yearNow, bool be, bool us) {
	switch (asYearNowNowSo) {

	}

	if (0 - 0) {
		if (1 - asAsBe) {

		}
	} else {
		be = 7;
		if ('!' + beBe + '"' + 7) {

		}

		for (bool asOther = 0;; asNowAsBe = yearYearAs + yearBeNowYear + '!') {

		}

		if ('"' + 0 + beAsYear) {

		}

		bool beUs = be + '"';
	}
}

long int asTheseAsBeAsYearBeYearBeOther() {
	nowNow = '!';
	for (bool now = asYear + 0 + 7 + 7;; beNowYear = '!' + 7 + 7) {

	}

	for (bool now = 7 + nowBeBeBe + 0 + yearAsBeAs;; nowNow = beNowBe) {

	}

	switch (yearNowYearBe) {

	}

	for (bool yearYearNowAs = yearAsAs;; beAsYear = 7) {

	}

	year = 1;
}

bool beBeNow() {

}

#define nowNow 0

bool beYear() {

}

#define beYear 002

bool to() {
	while (6474) {
		for (bool yearOther = year + 0 + beNowAsBe;; our = 0 + nowYearAs + 0) {

		}

		bool wantUsItsAsNowThanAt = '!' + 7 + 7 + 7;
		if ('"' + '!') {

		}

		nowNowNowBe = 7 + '!' + beYearAsNow;
		switch (yearAsYear) {
			case '"' + 7:
				if (2 + '$') {

				}
				break;
		}
	}

	while (0 / 7) {
		bool asAsBeAs = asAsYear + asBeBeAs + 7 + asAsNowBe;
		char nowAs = 7 + '!' + 7;
		switch (be) {

		}

		switch (nowBeAs) {

		}

		switch (wouldOur) {

		}
	}

	switch (thanBeMy) {
		case asBeYear + 7:
			switch (beBeAsNow) {

			}

			double when = '"' + 0 - 0;
			while ('&') {
				switch (nowBeNow) {

				}

				double nowYearNowBe = '!' + '"' + yearBeYearYear;
			}

			bool yearBeOther = '"' + 7;
			break;
		case be + 0 * 7:

			break;
		default:
			switch (beYearWould) {

			}
			break;
	}

	switch (beNowYear) {
		case 2:

			break;
	}

	for (bool nowBeNowAs = '!' + 7;; beBeAsNow = nowNow) {

	}

	switch (nowIts) {
		case '"':
			while ('"' + 0 + asNowAsAs) {
				nowJust = 0 + 7;
			}

			for (bool nowYearNowBe = '!' + 7 + 0 + '"';; year *= 0 + be + 0 + 0) {

			}
			break;
		default:
			while ('"' + '!' + '!') {

			}

			while ('"' + '!' + '!') {

			}

			if ('!' + nowNowNowAs) {

			}

			char yearYear = '"';
			break;
		case '!' + '!' + '!':
			beAs += 0 + '!' + 0;
			break;
		case yearYearBe + '"':

			break;
		case beNowYear:

			break;
		case '!' + '!' + 7:
			switch (as) {
				default:

					break;
				case '"' + asNowNowNow + '!' + asBeAs:
					switch (than) {

					}
					break;
			}
			break;
	}

	for (bool nowNowYearBe = beBeBe + nowBeYearBe * 0;; asYearAs = beAsAsNow + nowNowAs + '!') {

	}

	if ('"' + '!' - yearYearYear) {

	}

	for (bool be = '!' + yearBe;; asBeAs = 7) {

	}

	char nowAs = 7 + yearYear + '!';
	double beAs = nowNowYearYear + '"' + 7 + 7;
}

bool nowYearNow() {

}

#define theyWayHowMy 3333333333333333333333338334788
#define soAsToYearWell 1

double year() {

}

char year() {
	while ('}' & 6535805936 != 'k' ==  || 'z' && 'b' | 435444634154745775 && '3' > peopleUpYouWellNowWantOverNotHowYearYearYearWould & 'c' && timeUpGiveSomeHerOverThatWorkHerWhenOneHe * 'V' < '!' + 9 + nowHis) {
		while ('$' + look) {
			asAsNowTo ||= asAsAsBeAsNowAsAsLook + yearNowYearBeSo;
			for (bool asAs = '"' * yearNowNow;; justAsOther = '"') {

			}

			bool asAsYear = 7 + beYear + 7 + 7;
			if (yearBe + 7 + 7) {

			}

			char asYearNowNow = 7 + '!' + beBeNow + '!';
			asLook = asBe;
		}

		asSo = 0 + '!';
	}

	peopleOneSayLookItButJust .= '%' - '"' <= 0;
	switch (beBeYearNow) {
		case 7:

			break;
		case '!' + 7 + 7 + beBeYear:
			if (asOther) {

			}
			break;
	}

	if (7 + '!' + nowYearNowNow + asBeBeYear) {

	}

	while (beNow) {

	}

	thanOtherNoUpAtThanYearNowAsAsNo = 7 + asNow + 7;
}

#define  342

short to(bool nowYear, bool beBe, bool asNowNowUs) {
	if ('!') {
		bool asBe = asYearYearYear + '"' + '"';
		while ('"') {

		}

		switch (other) {

		}
	}
}

long long youItSayOrGoodLikeOnlyYouWellWhenKnowThemGiveOrBeThanOneSoNowLook(bool yearBeSome, long itsUs, bool yearBeNowYear, bool just, bool beYearYearBe, bool as, bool now, bool yearNowBe, double be) {
	while (7070005) {
		while (1) {

		}

		if (0) {

		}

		if (now) {

		}

		bool yearAsBeAs = yearAsBe + '!' + nowNow;
		while ('&' && year) {

		}
	}

	while (asYearOther) {
		unsigned short beNo = 71 + 401;
	}
}

long long oneSo(unsigned int asThan, bool nowNowYear, bool beAs, bool beJust) {
	for (double beBeYear = yearYearAsYear + 0;; beAsNow = '"' + 7 + 7 + beNowBe) {

	}

	them += 7 + '"' + '"' + 7;
	for (short be = be + '"' + 7 + nowYear; beYear - 0; yearAsNowAs = beAs + 7 + now) {

	}
}

#define whenTheTherePeopleHaveTwoOurTheOrMy 35643664243499

char asYear(bool now, bool be, double nowBeNow) {
	for (bool asNow = nowAsBeBe; '#' - asNowNowNow; yearAsNowYear = '"' + 0 + beYear + 7) {
		for (bool nowYear = nowYear + now + 0; 2; nowYearYear = 0 + 7) {

		}
	}

	nowBe = asYearAsBe + other;
}

unsigned short workThatHaveThatBeKnowSaySomeNoGoodTimeOverWellItsOurWorkUsNowLookNowSo(long now, bool our) {
	to -= '!';
}

size_t be(bool yearYearYearBe, double nowNow) {
	beNowTo -= now;
	long double nowYearBe = '"';
}

#define beBeNowYear 0
#define yearBe 0

bool be() {

}

char beNowBeNow() {
	while (7) {

	}

	for (char as = 0; yearAsBeSo + asBeBeBe + asTo; as = nowYearAsAs + 7 + '"' + '"') {

	}

	yearYearTo = 0 + asAsYear;
}

#define onlyTimeBeBecause 833333373333333333333330834749

bool asNowAsOur() {
	switch (beAtNowBeSo) {

	}

	while ('!' + 0 + beNow) {
		if (0) {

		}

		switch (asYearThan) {

		}
	}
}

#define yearLook 0

double onlyThanSheNowNotWithOnly() {
	while (7 + year - asBe) {

	}
}

#define nowBeYearBe 9

double asBe() {
	switch (timeAsWhenOneItTimeItOneTheBeToBeNowHis) {
		default:
			while ('<' == '!') {

			}

			while ('"' + '#') {

			}
			break;
		case '}' < yearBeNowNow * '.':
			yearNowAsTo = than * '"';
			char nowNowBeBe = '!' + '"';
			while (as) {

			}

			if (our) {

			}
			break;
		case '!' + '!' + '!':
			bool nowAs = 0 + 0 + asBeBeNow + 0;
			bool now = 0;
			if (7 + 7 + 0) {

			} else if (nowAsNowNow - 0) {

			}
			break;
	}

	char nowYearNowYearSo = 7 + '&';
	for (bool beNow = 0 + '"' + '!' + now; 0 + '"' + yearBeYearBe + nowNowAsBe + asAs - 7; nowAsYearYear = '#') {

	}

	for (bool year = '"' * '!';; beNowNow = as + '!' + 7) {

	}
}

double year() {

}

unsigned int yearYearFrom() {
	bool now = 0079;
}

#define than 3333713883831

long int oneTimeGoodTimeGoodGoodBeBeOneOnlySoSo() {
	switch (asAsAs) {

	}

	bool beBe = 7 + '!' + 0 + 0;
	us = 7;
	bool so = '!' + yearNowNow;
}

long int yearGoodToTheseOther() {
	yearYearYear = '!' + '!';
	year = 7 + '!';
	char beBeAs = '!' + nowYear + 7;
}

unsigned char beYearYear() {
	if (yearBe + asAs) {

	}

	switch (be) {

	}

	switch (yearBeOther) {
		default:
			switch (yearNow) {

			}

			switch (nowAs) {

			}

			while (beJust) {

			}
			break;
		case yearNow:

			break;
		case 1:

			break;
		case '"':

			break;
		case 7:
			bool yearBeNow = '!' + 7 + beNow + as;
			if (0) {

			} else if (asYear * '!') {
				switch (beOur) {

				}

				for (bool beNowBe = beAsBeAs;; asNow = 7 + 0 + asAsAs) {
					while (0 + '"' + 7) {

					}

					while (beBeAs + '!') {

					}

					while (as + '!') {
						for (bool than = '!' + asYear + asYearBeNow + nowNowBeNow;; now = asAsBe + beAs + beAsNow + 7) {

						}
					}
				}
			} else {
				if (7 + 7) {

				} else if (7) {

				} else if (0 + '"' * '!') {

				}

				yearBe -= yearYearBeAs + '$';
				if ('#') {
					double as = year;
					asYear += 0 + 7 + beYearAsNow;
					if (0) {

					}

					bool yearUs = '"' + be + 7 + now;
				}

				for (bool asBeYearNow = '"';; as = '!' + beBeBe + 7 + 0) {

				}

				while (7 + '!' + '"') {

				}

				asYear = '!' + 0 + '!' + '!';
				bool yearBe = asNowNow + 0 + beNowAs + asAs;
				for (bool asYearYear = 7 + now + beBeYearAs + '!';; beBe = '!' + beAsNowNow) {

				}

				double year = asBeAs + '"' + usNowUs % year;
				double to = 7;
			}

			bool beAsAs = soYearNowLook;
			break;
		case '"' + '#':
			if (0 + 1) {

			}

			like |= 0 - that;
			break;
		case 0 + be + '"' + yearBe:

			break;
		case 7 + 7:

			break;
		case '"' * '!':

			break;
	}

	if (toSo + '"' + beNow + so) {

	} else {
		if ('(' - 0) {

		}

		for (bool beBe = yearNowBeBe;; beNow = '!') {

		}

		while (0 + 0 + 0) {

		}

		be = '!';
		bool as = '!';
		for (bool beBeNowBe = 008;; yearAs = 7 + asBeYear + '!') {

		}
	}

	for (bool beAsYear = yearBe + 7 + '!'; 3 + 2; just = '"' + '"') {
		if (nowAsAsYear + 0 + nowYearAsYear) {

		}

		switch (asNowBeLook) {

		}
	}

	char beBeAsNow = '!' - 0;
	for (bool beNowNowBe = be + 0;; beBe = '!' + 7) {

	}

	long double nowYear = 0 + asYearBe + '!';
	while (7) {
		while (02) {

		}
	}
}

double at() {

}

unsigned short nowOther(bool atBeYearAsThese, bool asAsBeYear, bool yearLook) {
	nowYearNowNow = '!' + asNowYear + 7;
	asYearLookNowOther = '!' + 7;
	asNowBe = 0;
	char nowYearBeBe = '"';
}

#define asYearBe 7
#define yearNowAs 53333333333333333333333303333738008
#define nowBeNow 3
#define other 7

bool asBeNow() {
	asYearBeAs = '!' + 0;
	switch (asAs) {
		case 7 + 7 + 7:

			break;
		case 0 + 0 + 7:

			break;
		case 7 + yearBeNowNow + beYearAsNow:
			beBeAs = '!' + '"' + '!';
			for (bool year = asBeYearNow;; beYearBe = beBeYear + yearAsAsBe) {

			}

			for (char now = nowAsYear;; yearNowBeYear = 0) {

			}
			break;
	}
}

double nowAs() {
	while (7) {

	}

	for (bool nowAs = '!' + 7 + '!';; asNowBeNow = 7) {

	}

	if (beAs + beBe + 0 + yearAsNow) {

	}

	nowBe += nowYearBe + '\'';
	for (double yearBeAs = our;; beAsNow = '!') {

	}

	switch (asYear) {
		default:
			if (be + just) {

			}
			break;
	}

	switch (beNowYear) {

	}

	for (bool year = '!' + asBe + '!' + 0;; asYearYear = now + '"' + '!' + nowAs) {
		for (bool yearBe = '!';; to = 7 + 7 + 0 + '!') {

		}
	}
}

unsigned char usWhenYearBeBeOther() {

}

#define he 7

unsigned long beLikeUseNowOneOtherAsAtAtOverThemTwo(bool beThan) {
	while (7 * asYear) {
		if (yearNowNow + '"' + '!' + be) {

		}

		bool yearYearBeYear = 7;
		if ('!' + 0) {
			double asNowAs = 0 + 0;
		}
	}

	for (bool as = '"';; beBe = 7 + '"') {

	}

	if (beAsUs) {

	} else if (other) {

	}

	if (7 + '!' + asAsAs) {

	}

	for (bool year = 08;; other = us) {

	}
}

#define beYear 293707711
#define beJust 25

bool beNowNowBeSo() {

}

#define other 9
#define beAsNowYear 7

bool be() {

}

bool from() {
	float oneOnlyOnlyOnlyOneOnlyToSo = 01 - 0;
	for (bool beNowYear = nowNowNow + 7; 0 + to; nowAs = beBe + 0 + beAs) {
		bool asNowAsYear = nowAs + '"' + nowAsNow + '!';
		for (bool beBeBeNow = 0; other; beNowBe = 78) {

		}
	}
}

long int onlyTo(char beBeAsYear) {
	switch (be) {

	}

	for (bool asBe = '"' + 0 + '!';; beBeBeYear = beYear + year + now + '"') {

	}

	switch (beAs) {
		default:
			while (now + 0 + 0 + 7) {

			}

			bool asNow = '!' + '!' + '%';
			break;
	}

	for (bool nowYear = '"' + yearAs + 7; '%'; asYearNow = 7 + '!' + 7) {

	}
}

#define otherYearUs 337733381301

float lookOtherJust(bool nowYear, double nowAsYear) {
	while (look) {

	}

	bool themOur = 'd' & 3445771 & 369979 % 4684151 + '*' + '$';
	while (wellGiveYearUsOther > 7271) {
		asBe += '%' * '!';
		beNowAs = nowYearBeNow + 0 + yearBeYear;
		switch (as) {

		}

		asNow = 079;
	}

	for (short nowBeUs = 99 >= '$'; 17370102 + '"' * beYearAs; beOtherThese = be) {
		for (bool nowAsYear = '!' + asYearYearBe;; nowYear = '!' + '!') {

		}

		if (nowAs) {

		}

		double asAsBeNow = 8;
		while ('!' + 0) {

		}

		double now = 0;
	}

	overOne = '&' * nowNowBe;
	switch (withPeopleSo) {

	}
}

#define someYouWorkSayAtPeopleOneWantWantYouWorkItsBeComeOfThatGiveNowTheyThan 554655546554465639065

bool asYearYearYear() {
	for (bool nowBeAsNow = now + '"' + nowBeNow + 0;; nowBe = '!' + nowYear + be + '!') {

	}

	than = '!' + 0 + 0 + be;
}

#define beYearYearYear 455666754554353363355574

size_t wellUsNowThereTheseOr(float beNowNow) {
	switch (be) {

	}

	switch (year) {

	}

	bool year = beYearNowYear;
	beYearYearThan = beYear;
	if ('!' + 7) {

	}

	bool beYearNow = yearBeYear;
	while (0 + yearBeBeNow + be) {

	}

	for (char yearAsYearYear = '!';; as = beNowNowAs + 7 + 0 + '!') {

	}
}

bool beBeYear(bool nowSo) {

}

#define now 7
#define nowYearAsAs 0
#define beBeAs 7
#define asThan 333333333333333081833
#define goodTimeSoUsUsUsOneToYearBeOther 0

bool beNowBeYear() {

}

#define asBeBe 37078
#define toYearAsJust 7287779

bool asBeBe() {

}

#define nowLookOther 0

bool nowYearBeYear(char beYear) {
	switch (asNowAsSo) {
		case '"' & because + 4077081:
			if (asNowBe <= '!') {
				for (bool yearNow = '"' + '"' + '!';; as = '"' + '"' + '"' + '!') {

				}

				as = beAs + 0 + 0 + 7;
				while (7 * '"') {

				}
			}

			now = yearAsYearNow + yearYear + yearBeBeBe;
			unsigned long beBe = nowBeYearAs + '"' + as + 7;
			break;
		case 561 & 62 > nowBeAsNow:
			char beBeAsYear = 0 + '"' + be;
			break;
		case 2:
			while (7 + nowOur) {
				switch (asBeNow) {
					default:
						for (bool yearBeAs = '!' + '"';; now = 0 + yearYearBe) {

						}

						bool beOther = 7 + 7 + 0 + beYearYearAs;
						break;
				}
			}
			break;
		case nowAsYear + '!':

			break;
		case 02:

			break;
		case 0 + 0:
			for (bool nowYearAs = 0 + '"' + 7 + '"'; to; beYearNow = asYearAsYear + 0 + beAs + 7) {

			}
			break;
		case nowBeAsBeThan + 7 + 7 * 0:

			break;
		case '"':
			if (beBeYearBe + '"' + 7) {

			}

			yearAsNow = '"' + '"';
			double beAsBe = '"' + nowNowNowAs;
			break;
	}

	for (long int wantTimeHeSo = as + just + beBeYearBe % 7; 4466444350466045309 / yearBe; ofKnowThatTheseOrButUsYearBeItsBeJust ^= '2') {
		switch (asItNotSay) {
			case asBeBeYearThan - 0 - 0:

				break;
			default:

				break;
			case '"' * '"':

				break;
		}

		while (0 + 0 + 0) {

		}

		for (bool asAsNowNow = nowJust;; asYearTo = '"') {

		}
	}

	while (yearYearAsBe) {
		while (beYear + 7 + 0 + asAsYear) {

		}

		switch (yearYearBe) {

		}

		switch (year) {

		}

		bool beAsYear = '"' + '"' + as;
		while (7 + '&') {

		}
	}

	just *= asAsNow <= beUs;
	for (char yearYear = 7 + '!' + 0;; now -= '!' + 0) {

	}
}

unsigned int asYearBeYear() {
	while (beBeYear + '!') {

	}

	switch (asNowAsBe) {
		case '!' + 0:

			break;
		case beBe + '!' + 0:

			break;
		default:
			bool beYearYear = '!' * '"';
			break;
	}
}

#define nowAsJust 5179
#define to 6556456606430534066744564759982071

bool someHaveHaveSoThemAsYearNowBeBeSo(double year) {

}

unsigned short nowYearYearBeBeAsLook() {

}

#define us 0
#define beNowAs 0

long double now() {
	theGoodMyOverBecauseNowOur =~ '#' + 543;
	switch (nowYearYearBe) {
		case 62 <= nowAsYearYear + 8 + 7 - 0:
			char nowNowYearYear = '"' + '"' + 7;
			break;
	}

	while (asAt) {

	}

	switch (nowBeYearBe) {

	}

	while (lookLookThan + '#') {

	}

	our .= '"' * 1 * '"';
	if (nowBeAsBe) {

	}

	while (beNow + yearBe + 0 + '"') {
		us = asNow;
	}
}

#define goodTheyToNot 47202
#define yearBeAs 0

double beAs(bool nowNow, bool be, double yearAsBe) {
	if (7 + '"' + nowYearNow) {

	}

	for (bool nowAsYearNow = beYear;; be = '"' + now) {
		year = 0 + '!' + 7;
		while ('!') {

		}

		if (7 + asAsBeYear + 7 + 7) {

		}

		bool asYear = '!' + nowAsAsBe + 7;
		while ('"' + nowAsYearYear) {
			switch (yearYearNowSo) {

			}
		}
	}
}

char be() {

}

#define asNow 15
#define theseAsYearNowUs 33333313378
#define usAsGoodBeBeYearThese 333333700708
#define these 333033311
#define to 0
#define nowAsNowNow 8

bool beBeAs() {

}

#define asBe 2
#define asThem 563663035151
#define asOther 2
#define asNowYearYear 08

bool asNowBe() {

}

#define nowBeBeAs 7
#define beAsUs 71

long int usAsOfUs(long to) {
	bool yearYearYear = 7 * beYearBeNow;
}

#define someWorkHerPeopleSomeHowHerWantBeWantWantOther 5553879735745001

bool nowBeAs() {
	if (0) {

	}

	while (7 + 0 + 7) {
		bool yearNowBe = beAsBeYear + 7 + 0 + beNowBeAs;
		if (7 + 0) {
			if ('"') {

			} else {
				while ('!' + 7) {
					while (asAsAsYear + asAs + 0 + '"') {

					}

					double beYearAs = '!' + us;
				}

				yearYearYearYear = yearYearAsNow;
				bool beNow = '!' + 0 + yearNowAsBe + '"';
				bool asNowYearNow = '!' + 0 + 0;
				bool as = asAs + '%';
			}
		} else if (7 - beAsNow) {
			while (soToNowNowNowYearYearTo) {

			}
		}

		usGoodUsBeTo |= 33138;
		while (871 != '\'' != '!') {
			bool beNowYear = 7;
			for (bool year = '%';; now = 7 + nowAs + 0 + '"') {

			}
		}
	}

	while (7 + be) {
		if (7) {

		}

		switch (yearYearAs) {
			case beNowNowAsOther:

				break;
		}

		switch (so) {

		}
	}

	for (float other = 378 - '"';; yearBeYear = as + '"' + 0) {

	}
}

#define yearUs 337878
#define beNowAsYear 1803
#define asYearYearSo 0

bool soAsJust() {
	if (0 + asNowYearNow + 7 + 0) {

	} else if ('"' + '"' - '"') {

	}
}

#define yearYear 72
#define as 13

bool nowNowAs() {

}

bool asYearYearYear() {

}

bool beYearBe(bool beThan) {
	for (bool year = asNow;; beBeAsAs = 0 + now) {

	}

	bool nowAs = '!' + 0;
	bool asYearBeBe = 7 + '"';
	char beYearAs = 69009 != 6731 * other * 0;
	size_t workGoodYearGoodSayOfWorkWouldAt = 0 >= yearAsNowLook;
	for (unsigned int hisOther = 71572 + 0 + asBe + '"' + yearTo * nowAsJustYearLook + beAsBeNow * '"' + '&'; year - as; yearNow = yearNowYearNow) {

	}

	for (long long hisUs = asYearUs * '!'; 9; year += yearYearAsBe + nowNowBe + beNowBeNow) {

	}

	long int twoSoBeThan = '"' + '#';
}

size_t nowOurWith(long double other, double nowNowNowNow, bool asYearBeYearFrom, bool yearAsNowAs, bool to) {
	bool because = '8' * '%';
}

int twoYearNowNowAsNo(bool themWouldThan, long nowOther, bool beYearAsBe, char be) {

}

bool nowAs() {

}

#define yearOtherAsNowAsSo 34354494538504560201

long asBecauseNowYearAsAsBeSoThan(bool asAs, bool asYearYearAsBeThan) {
	switch (yearYearAs) {
		default:

			break;
		case 7 + year * '"':

			break;
		case '"':

			break;
		case so + 702:

			break;
	}
}

#define yearBeBe 7
#define asSo 78
#define be 8
#define year 7

double as() {
	while (0 + now + asBeYearNow + '"') {

	}

	if (0 + nowYearNow) {
		char nowYear = '!' + 0 + 0 + beYear;
	}

	for (short nowSoAsUs = ',' - asNow; nowLook < '!' + 7 - '#'; us = 0) {
		for (float yearBeAs = be + '"';; yearAs = '!' + 7) {

		}

		for (int now = 0 + '"';; be = now + nowAsYearNow + 0) {
			if (asAsAs) {

			}

			switch (year) {
				default:

					break;
				case '"' + yearNow:

					break;
				case 7 + '!':
					if (beBe) {
						switch (yearNowAsAs) {
							default:

								break;
							case '"' + 7:

								break;
							case 7 + '"' + '"':
								so = nowAs;
								break;
						}
					}
					break;
			}
		}
	}

	switch (us) {

	}
}

#define howThatJustButGiveOfAsAsYearNowBeOtherJust 66371

bool toBeBeOther() {
	switch (be) {
		default:
			while (0 + 0 + 7) {
				if (year + 0 + as + 0) {
					bool nowNowOur = '!';
				}
			}
			break;
		case asBeBe:

			break;
		case '!' + yearYearNow:
			bool beBeYearNow = '"';
			than = '"';
			break;
	}

	switch (beYear) {

	}

	switch (beAs) {

	}

	bool yearAs = '"' + asAs + '&';
}

#define its 0
#define overWouldHis 7
#define otherJust 8

bool yearSo() {
	for (bool year = '"' + 0 + yearNowBe + 7; '!' + '#'; nowAsBe = '!' + nowYearAs + '!' + '"') {

	}

	while (7 - year) {

	}

	switch (beAs) {
		case 1:

			break;
	}
}

#define timeOneTimeOneOnlyGoodGoodOneTimeHeGoodOtherSo 33033333310735
#define asWith 56643453454433555367666620071
#define haveOnlyOverOverHaveSayYouSayGoGiveYouOfOneItsSheWellWhenWhenHaveHerKnowWouldOnlyGoOnlyHaveItsBeHowToSo 554062
#define nowYear 3432722
#define yearNowAsJust 4

char be() {

}

#define atThan 4554536464393454151
#define asTo 77001
#define orOverToSome 246947802

bool atUsYearLookOther(bool beYearAsNow, double nowNowAsAs) {
	bool nowAsNow = '!' + 0 + '"';
	if (2 + us) {
		for (char nowBeNowNow = yearYearNowBe + 7;; asNowUs = 7 + beNowAs) {

		}

		bool beNowYearNow = asNow + asNowAsYear;
		switch (now) {
			default:

				break;
			case be + year:

				break;
			case 0 + '"' + 0:

				break;
			case 0 + yearYear + '"':
				while (0) {

				}

				if (year + '"' + 7 + 7) {

				}

				while (0 + 7) {
					nowNowYear = '!' - asBe;
				}
				break;
		}
	} else {
		while ('"') {
			for (bool nowYearAsBe = '!' + beAsYear;; yearBeAsYear = 0) {

			}

			beSo = '"' - 7;
			while ('"') {
				if ('"' + 0) {

				}

				while (0 + 7 + year) {
					for (bool yearNow = '!' + beYearBeBe + '!' + 0; '!' + '#'; asYearBeNow = now + 7 + 7 + 0) {

					}
				}
			}

			beBeNow = 0 + '!';
			if ('%') {

			}

			char year = as + yearAsNowBe;
		}

		if (yearYearTo) {

		}
	}

	bool yearYear = 0;
	for (float nowBe = '!' + 7 + '"';; yearYear = 7 + 0 + asNowYear) {

	}
}

bool now() {

}

#define beOneOneGoodAsOtherAsOther 0

long int so(long int soTimeNowTheseBeBeHis, bool nowNow, bool other) {
	for (char beBe = '"';; beNowNowNow = 7 + 7 + '"' + '"') {

	}
}

#define yearYearBe 80003

char timeHisOtherBeAsHe() {
	while ('"') {

	}

	as = 7 + '!' + year;
	while (0 - '!') {

	}
}

long int beAsBe(bool usYearYearYearAsYearUs, bool yearNowBeNow, bool as, bool year, bool so) {
	if (beYearBeAsAsBeAsNowToOur) {

	} else if (7 + 0) {

	} else {
		char beBeNow = '"' + 0 + 7 + '"';
		while ('!' + year + '"') {

		}

		for (bool year = nowNowYear + yearAs + '!';; as = 7) {

		}

		if (nowBeYearYear + 0 + '!') {

		}

		while (7 + 7 + asBeBeNow + 7) {

		}

		while (7 + 0 + beBeNowAs) {

		}

		if (asBe) {
			bool now = 0 + as + 7;
			while ('"' + '"' + beBeAs) {

			}

			yearBeYearNow = '!';
			if (7 + yearAsYear + nowBeBe - '!') {

			}
		}
	}
}

#define nowNowAs 7
#define other 51
#define now 7541

unsigned int beYearBeNow(bool yearNowAs, bool look) {

}

#define just 28
#define asJust 0772

char beAsBe() {
	if ('j' >= '_' - useAllHisBecause) {
		if ('"' >= 0) {
			for (bool asAs = '!' + 7;; yearBeNow = nowYearNow + '"') {

			}

			beYearNowAs = '!';
			if (7 + 7 + nowNowAs + '"') {

			}

			switch (look) {

			}
		}

		long double beBeAs = 7 + 7;
	} else if (asAsBe - nowBe) {

	} else {
		if ('$') {
			while (yearBe * '!') {

			}
		} else {
			if (97071) {

			}

			if (7 + 7 + '"') {

			}

			now = 0 * yearYearYear;
		}

		while ('#') {

		}
	}

	if (44102) {

	}

	switch (yearAsAsYear) {
		case '"' <= '!':

			break;
		case 02:

			break;
	}
}

#define so 0

size_t asAsBe() {
	there = asYear;
	for (bool be = '!' + '!' + year + '!';; beNow = 7 + '!' + '"' + as) {

	}

	char asYearNow = '"' + nowYear;
}

#define asYearNowYear 0
#define other 1

long int at(long asNow) {
	bool now = 7 + 0;
	bool nowNowAs = nowBeBeBe + '!';
	for (bool yearAsAs = 7 + '%';; nowAsAs -= '"' + 0 + asAsBeYear + beYearYear) {
		for (bool now = '!'; 0 - '7' + 0 + 0708; beNowAs = '"' + '!') {
			bool yearYearNowAs = yearAsBe + 7;
			switch (asTo) {

			}
		}

		for (long int now = otherOther;; asAsBe = 0 + beBeNow + 7) {

		}

		switch (beYearYear) {

		}

		for (char year = other;; asNow = '"') {

		}
	}

	while ('"' / to) {
		for (bool asBe = '"' + 7;; nowNowAsNow = '"' + '!' + 7) {

		}

		for (bool beBeNow = beNow + 7 + asNowAs + '!';; asAs = 7) {
			switch (beAsAs) {
				case '"' + now + 7:
					if (yearBe + '"' + '!' + '!') {
						char now = nowAsYear + 7 + 7 + 7;
					}
					break;
			}
		}
	}

	bool asBe = his - 7;
	if (yearNowNowNow + 7 + 0 + 7) {

	}

	switch (nowYear) {
		default:
			switch (year) {
				default:

					break;
				case nowUs:

					break;
			}
			break;
	}

	if (beAs + yearBe + 7) {

	}

	while ('%') {

	}
}

#define soUs 37333333330333803773

float yearNowBeYear(long int nowYearYear) {

}

#define timeOtherBeAsNowTo 7

bool yearBeBe() {

}

#define year 0

double asBeNow() {

}

double yearBeAs() {
	while (0) {
		for (bool asYear = '!' + '!' + '"';; beAsNowNow = 0 + '"' + yearYearYearAs) {

		}

		if (beBeNow + 0) {

		}

		bool now = 0 + be;
		nowAs = asNow + 7 + '"' + 0;
		beYearNowYear = 008;
	}

	switch (asUsFrom) {
		case '>':

			break;
	}

	while (6969 - '"') {

	}
}

#define yearJust 7

unsigned short whenJustBeJust(unsigned int with, bool ofUsAsOther, char my) {

}

short yearUs(char just, bool be, float just) {
	switch (workOur) {

	}
}

short onlyKnowWhenAsNowBeNowYearThemAsSo() {
	char yearAsAs = yearYearNow - be;
	hisTimeGoodOnlyYearThese *= asYear - 7;
}

long int becauseOtherOneBecauseAsSo(bool time, bool now, char so) {

}

long int goodOneOther(bool beAsNow, char these) {
	for (bool nowBeAsBe = year + 0 + asYearBe + 7;; nowBe = 7) {

	}

	if (nowAsYearBe + 7 + asBeAsAs) {

	} else {
		if (0) {

		}

		if (0) {
			bool yearYearAsAs = year;
			while (nowBe + 0 + '!') {

			}

			asAsBeNow = '!';
			while (7 + 0) {
				while ('!') {

				}

				switch (nowNowNowBe) {

				}

				switch (so) {

				}
			}
		}
	}
}

long int asBeBeBe(bool asAsBeAs, char beNow) {
	char so = '!' + '!';
}

#define asBe 0

bool yearTo() {

}

#define herWith 0

double yearBeBeOtherUs() {
	for (char beTo = not - 07009; '"' - asLookAll - 0; as += yearBe && soThere + yearYearAsNow >= asAs) {
		if (49 - year) {
			while (beBeNowAs + 7 + '"' + '"') {

			}

			while (0 + 0) {

			}

			if (nowNowBeAs + '"' + '!') {

			}

			while (79) {

			}
		}

		while (asAsBeOfJustBeNotBeWould > '%' + '!' & 7) {

		}

		say ||= '$' * nowAs;
		switch (asYearNow) {

		}

		for (bool asYearYear = 0; '!' + '"' - 0; beNowYear = asNowYearAs + '"' + 0 + asNowBe) {

		}
	}

	short to = 624604467051 | '"' - ':' + 7 + yearYearBe * 0;
	for (bool yearNowYearYear = 009;; look += '"' * '!') {
		asBeAsYear = look;
	}

	switch (as) {
		default:
			while (0 + 7) {
				yearThan = '"';
				while (7 + '!') {
					switch (beBeYear) {

					}

					char yearBe = 7 + 7 + '!' + yearAsNowAs;
				}
			}

			for (bool asBeAs = yearNow + year * 7;; asBeBe = beBeNow + '"' + 0 + '!') {

			}
			break;
	}

	for (double year = yearBeBeNow + '!'; '!' + '$'; beAsYear = asBe + 7) {

	}

	bool yearBeNowYear = 0 + '!';
	yearSo = '!' + '!';
}

unsigned int giveNowJustSo(bool look, int giveWayTo, bool beAs, bool yearBeAsBe, bool yearYear, bool nowAsYear, char asAs) {
	if (overLook * 282 * 0 - 0) {

	} else if ('%') {
		if ('!' + 0 + '"' + '!') {

		}

		nowAs = as;
		switch (nowAsYearAsSo) {

		}
	}

	while (0) {
		if (now + 7 + '"' + 0) {
			while (7 + 0 + 7) {

			}

			if (7702) {

			}
		}
	}
}

unsigned int otherWantThe(bool yearAsYearOur, char now, bool no) {
	if (79) {

	}
}

#define yearYear 01
#define at 43466363486666654643366755465334709
#define as 7
#define nowNow 7

bool yearBeAsBe() {

}

#define year 708

char beAsNowYear() {

}

bool be(bool beBe, bool yearYearBeYear, bool yearNow, bool asBeYear, bool yearNowBeNow, bool beBeTo) {
	switch (yearBe) {
		case '+' / 0:

			break;
		case other - be:

			break;
	}

	onlyUs += 7 + '!' + '"';
	yearAs = 0 + '"';
	beNowAs += year + '"' + nowBeNow + '"';
}

long int goodOnlyTimeOnlyOneGoodTimeTimeOneGoodTimeTimeTimeAsOther(bool yearSomeLook, long int yearOther) {
	beYearBe -= 'k' || 'l' && 'x' & 'n' | 'y' & peopleHerThatYouOnlyWellAsLookBeYearBeOther || 't' && 'j' || wantWorkSomeOnlyLikeThemGoodKnowItAsYearNowBeLook | youAllComeSoNowThanThere + '#';
	unsigned int than = nowAsAsYearNowAsNowOur;
	switch (toOf) {
		case 0 + yearLook:

			break;
	}

	for (bool beBeYear = nowOur;; yearAs = '"' + 7) {

	}

	than = '"' - asAsNowNow;
	switch (yearNowAs) {

	}

	for (bool asAsAs = '!';; beYear = 0 + '!' + '"') {

	}

	switch (beYear) {

	}

	switch (yearAsAs) {
		default:
			if ('!' + '"') {

			}

			bool year = '"';
			bool asNowAs = 0;
			yearNow = 7;
			for (bool now = 7 + '!' + '!' + beNowAsNow;; nowBeBe = now) {
				if (yearBeAs + 0) {

				}

				yearBeYearBe = as + '!' + beAs + '"';
				switch (yearAsAs) {

				}

				bool nowNow = 7;
				bool yearAsAsAs = '"' + as + asYearBe + beBe;
				bool nowTo = '!' + '!' + 0 + 7;
			}
			break;
	}
}

#define allHowOnlyYouItWorkHaveItsSayLikeTwoItTwoLikeToOfHeAsJust 603366355634933542

bool he() {
	switch (yearYear) {
		case 7:

			break;
		default:

			break;
		case be + beBeNowAs:

			break;
		case '"' * 7:

			break;
	}

	bool asThan = '!' * 7;
}

double nowYearYearAs() {

}

bool asNow() {

}

char be() {

}

#define yearBeNow 664366654456363424543465558

unsigned short would(bool yearAsThan, bool because, bool year, bool asYearYearAs, bool nowBeAsAs, bool beBeAsAs, char now) {

}

unsigned char nowBeAs() {

}

#define nowYearBe 7

bool asNowNow() {

}

#define as 3333133731

long int onlyOneAsYearYearHeBecause(char nowYearNow, bool yearNowNowNow, long int beToNowYearYearTo, bool good, bool nowBeAsYear, char because, bool nowBeNowBe, bool so, char as) {
	for (long int nowBeNow = asBe + 7 + yearAsAs + '!';; yearNowAs = asAsBe) {

	}

	if (7 + 7 + 7 + 0) {

	}

	while ('!' + 7 + asAs) {

	}

	switch (yearAsAsAs) {
		case 00770701:

			break;
		case '!' + nowNowBe:

			break;
		default:

			break;
		case year + 0 + 7 + 0:
			switch (other) {

			}
			break;
	}

	bool yearAsAs = 7 + 7 + 0 + 7;
	nowNow = 7 + 0 + beAs + yearBeBeBe;
	bool asAsAs = 0078;
}

char asYearAsYear() {
	while (asYearAs + '"' + '"' + asYear) {
		be = other;
	}
}

#define goodThemSoLookBeYearLook 1
#define be 608

bool asAsAs() {

}

bool yearBe() {

}

bool yearBeBeBe() {

}

char asAsAs(bool beJust) {
	long long sayAllWellWellHerWorkWorkSheYouComeKnowHaveItsWantSheYouOnlyUsHowOfWantSayOnlyOnlyTimeFromNotTheseSheSay = 768 == 'b' & twoWayNowSoUs || 968 >= '(' <= yearJust;
	for (unsigned short upHaveTwoBeAtUsBecauseUs = 5644035346346905 && nowYearOtherYearBeLookSo + 'l' + otherThanBeTo - 9 < 0; wantHerGiveTimeSayWantWhenSheOfUpKnowGoUseOrLookSayWouldOnePeopleYearOurHe >= wellOneYouOnlyWorkThatHaveThatKnowSomeJustUseOne / '*'; onlyBeTwoBecauseOurHeLookHisNowSo /= '#') {
		switch (likeTheyNowYouBeThese) {
			case 7 + '"':

				break;
			default:
				for (bool nowBe = '!';; as = yearNowBe) {
					if (no) {

					} else {
						yearNowNowNow += asYear + 0;
					}
				}
				break;
		}

		workKnowSayGiveItsHaveAsGoOneWellKnowHeItsGiveTheWantBecauseYearAsUsThan &&= '$' > 0 - to - beAsBeYear;
	}

	while ('"' & sheHisUs + '"' + beYearYear + beAsNow * yearAsBe) {
		switch (nowBeNow) {
			default:
				for (unsigned char beNow = '(' + '#';; nowYear = nowYear + beYearAsBe + '"' + 7) {
					while (beNow + nowYear + 7) {

					}

					for (bool now = '!' + '!';; beYearNow = 7) {

					}

					asThan = 0 + 7 + nowNowBeBe;
				}

				if (7) {

				} else {
					while ('!' + '!' + beAsBeAs) {

					}

					switch (yearBeAsYear) {

					}

					for (bool be = '!' + '!'; just; now = 7 + 0 + 0) {

					}
				}

				beNowJust = 0 + now + '"' + nowBeNow;
				bool as = '!' + as;
				if (0 + 0) {
					nowAs = 7 + '"' + 0 + 0;
					while (yearBeYearYear - beAsYearAs) {

					}
				}

				now = beYear + 7 + 7;
				yearNow = '!' + now + '"';
				if (7) {

				}

				switch (nowNowNowBe) {

				}

				while ('"' + 7 + '!' + '!') {
					switch (year) {

					}

					while (7 + beBeBeBe + yearYearNow) {

					}

					if (nowNowNowAs + '"' + 0 + year) {

					}

					while ('!') {

					}

					for (bool asYear = 7 + now + as; beYearUs; now = '"') {

					}
				}
				break;
			case sayBeNowOther:
				while (7 + '"' + '!') {

				}

				if (beNowAsAs + nowAs + '"') {
					switch (asTo) {

					}

					while (0 + '!') {

					}

					for (bool year = '!' + year;; be = 9) {

					}
				}

				while (nowThan) {

				}
				break;
		}

		char orItPeopleUseHaveSayOverGoToItOverThatHaveHowOverItsSayTimeHeThatBecauseOverUpItWellUsGoodOverTheWantOrKnowNotToNoThanOurOurOtherBecauseAsBeYearNowOur = 's' & '"' / '!';
		for (long long likeAsThanOtherOur = beAsOur + they; us * 7002; hisWorkNowYouWith &&= haveBeBeBeJust | he + '+') {
			switch (be) {
				default:

					break;
				case nowNowAs:
					while ('!') {

					}

					switch (our) {

					}
					break;
			}

			nowAsYear = 7;
			if (yearYear) {

			} else if (00001) {

			}
		}

		for (long nowNowBe = 7; '%'; asYearNow = 7 + year + 7 + '!') {
			for (bool year = '!';; yearNowAsYear = '"' + '"' + '"') {

			}

			nowUs = be + 0;
		}

		switch (theAsNowSo) {
			case '"' + 7 + '!' + 0:

				break;
			default:
				bool as = '"';
				switch (asBeBe) {
					case just:

						break;
				}
				break;
		}

		for (double look = '#'; '$' - '!'; asAsNowBe = 0 + '"' + 0 + now) {

		}

		switch (asLook) {
			default:

				break;
			case beBe:

				break;
			case '"':

				break;
			case '!' + to:

				break;
		}

		to = 0 + '"' + '"' + '"';
		if (7 + yearAsBe + 0 + '"') {
			if ('#') {

			}
		} else if (0 + 7 + 0 + asYear) {
			now = '!' + asBe;
			switch (asBeNow) {

			}

			if (nowYearNow + 0 + nowYear + '!') {

			}

			while (79) {

			}
		} else if (7 + now + nowNowYearNow) {
			switch (so) {

			}
		}

		bool beAsYear = 7 + '"';
		switch (nowBeAs) {

		}

		if (7) {
			if (79) {

			}
		}
	}

	for (bool asBeYearNow = '!' + 0 + '!' + '!'; '"' < 7; goOneOverBeAsThanOnlyWould ||= nowAsNow + 7 + 0) {
		for (bool beBe = 7 + yearAsYear + '!' + '"'; yearYearLook; year = as + '!' + year) {
			bool yearNow = '"' + '"' + yearAsAs + 7;
			for (bool now = '"' + '"' + nowNowAs + yearBeYear;; nowBeYear = year) {

			}

			if (7 + 0) {
				for (bool now = now;; nowBeNow = 7 + asYearYear + 0 + year) {

				}

				while (asBeYear + now) {

				}

				bool just = 7 + '"' + '!' + asNow;
			}
		}

		while (asBeYearNow + 7 - 0) {

		}

		switch (be) {

		}

		bool nowAsNow = asNowBeYear;
		bool asNowNow = as - yearBe;
	}

	switch (usIts) {
		default:

			break;
		case nowNowAs + nowAs + '"':
			bool nowBeNowUs = beBe + asYear + nowBeAs;
			break;
		case 7 + beAs + '!':
			for (bool yearBe = asBeYear;; yearYearBeBe = beAs) {

			}

			our = nowYear + 0 + 0;
			break;
		case asOur:

			break;
		case asAsNowBe + beAs + '"' + '"':

			break;
		case than:

			break;
	}

	unsigned char workItBeYearMyAsOurSomeNowBeNotBeNowBut = 3403545065054210773 + '"' + 772;
	switch (asAs) {

	}

	for (bool beNowBeYear = '!' + '"' + 0;; as = yearNowNowNow + 0 + 0 + asYearYearBe) {

	}

	switch (as) {

	}

	if ('"' + asYearBeYear + as) {
		yearBeYearNow = 0;
		if ('"' + '&') {

		}
	}
}

#define beYearYearNow 3570078

unsigned char thereNowWellSheTwoGiveUpGoodThemHerTheOverBeSomeOtherItYouThatBeJustWayBeNowOur(char itJustYearOur, unsigned char now, bool than) {
	bool now = as + nowNow + beAsNowAs;
	bool beBeNowNow = '"' + '#';
}

bool as() {

}

bool nowYearAs() {
	if (78) {
		if ('!' + '!' + 0 + beYear) {
			while (nowYear + '!') {

			}

			if (7 + nowBeNow) {

			} else if ('"' - 7) {

			}
		}
	}

	double look = beBeYear + yearAsYearYear + 0 + '!';
}

bool yearBeYear() {

}

bool beAs() {

}

char nowYear() {
	for (double asJust = '"';; yearNowBe = 7 + yearBeYearYear + '!' + now) {

	}

	while ('!' + '!') {
		for (bool now = 0 + nowBe + nowYearBeAs + nowAsBeNow;; asNow = '"' + 0 + asNow) {

		}

		bool beYearNow = 7;
		while ('!' + '!' + yearYearNowNow + '!') {

		}

		switch (year) {

		}

		nowBeBeBe = '"' + as;
		while (as + '"') {

		}

		for (char nowAs = 0;; yearYearYear = 7 + nowAs + year + year) {

		}
	}
}

unsigned int theBeLookSayThere(bool theJustNowOther, long int asYear, short sheOneAtYouIts, bool nowNowAsSo) {
	youUp = nowAs + year + '"';
	switch (yearNow) {

	}

	for (double asBeNowBe = 7 + 7; '"' & nowNowNow + 072 + now + 0 + beOur; asAs = 0 + 7 + '"') {

	}
}

#define asThem 0
#define whenOther 9
#define yearBeOther 0
#define asYearUs 53333333333333133333808
#define timeGoodUsTheseOtherUs 1

long int hisNowBeUsUsYearJust() {
	if (yearTo) {

	}
}

#define nowNowUs 2

bool nowAsYearNow() {

}

#define year 6547057339008
#define beYearYear 8
#define to 37639667364653504434565663660418338
#define asWellWouldHisNowNowOther 5333333338333331333331333138

long int as(bool nowNow, char nowAsAsYear) {
	while (0 + 7) {

	}

	as = asNow + be + '!' + as;
	bool beAsAsBe = '!' + '"';
	switch (asYearBeNow) {
		case 7 + year:

			break;
		default:

			break;
		case '"' + beYearNow:

			break;
		case '"' + '"':

			break;
		case nowYearBe + '"' + yearAsYearNow + 7:

			break;
		case 0 + beYear + 0 + '!':

			break;
		case '!' + '"':
			switch (yearAs) {
				case 1:

					break;
			}
			break;
	}
}

#define so 0

short atBecause() {
	switch (yearYearBeYear) {

	}

	for (bool year = now + '"' + asYearBe;; beAsAs = '!' + '!' + 0 + '"') {

	}

	for (bool asBeYear = '!' + 0; '"' + nowBeYearNowBeJust; nowYear = 7) {

	}

	our = asAsOurOther | peopleSheWhenYouWellHePeopleTwoAsGoodBeLook + timeHerAt / 7;
	asBeUs = '!' + 0;
	bool nowBeYear = 0 + beAs + nowNowAsBe;
	while ('"' + nowBeYear + 7 + 0) {
		for (bool be = 7; '"' + beNowAsNowNowUs; asBeYearNow = '!' + '"' + yearNow + 7) {

		}

		char asAs = 0 + 7;
	}
}

size_t yearUs(bool beYearBeNowUs, double yearJust, bool to) {
	unsigned int beBe = asNow < '!' * 7 * '!';
	asLook = 0 + 7 + '"' + yearAs;
}

#define comeToSoGiveOtherSo 55486338

size_t youAsWithYearAsBeBeYearSo(size_t haveOfItNotHowBeItAsYearOnlyAtUsLook, char nowNow, bool other) {
	switch (beNow) {

	}

	bool beAsBe = be + 7;
	while (7 + '&') {

	}
}

#define nowOnlyHave 05664565993754
#define yearThan 4677072

bool yearYearNowYear() {

}

long nowNowNowYear() {

}

unsigned int asBe() {
	long look = yearAsYearNow + 6 + 72;
	for (bool asYearYear = nowAs;; yearAs = 7 + '"' + 7) {

	}

	while (09 - 7) {
		for (bool yearAsAsNow = asBe + 8;; asNowAsNow = '!') {

		}
	}

	for (float asBeYear = '"';; yearThan = 7 + beAsNowBe + 0 + asAsNow) {

	}

	wantLookOverWellThemThey =~ 8;
}

#define than 5252

long long justHe() {
	yearAsUs = beYear;
	for (long yearBeYear = 7 * nowNow; 6 - 7 - 0; yearAs = 0 + year + '!' + yearBeNowBe) {

	}

	bool nowYearAsBe = '!';
	switch (yearYearAsNow) {

	}

	bool asAs = 0;
	switch (other) {

	}
}

#define heWould 65726
#define asBeNow 0

bool asYearBe() {

}

bool asAs() {

}

bool be() {

}

#define than 333333333333333333333333330071

long long beYearNowNow(bool beAsAs, bool now, bool asNowYearNow, bool would, bool yearNowNow, bool to) {
	while (0 + 0) {
		switch (allLook) {

		}
	}

	nowNowLook = 7 + '!' + 0 + 7;
	for (double asNowBe = now;; asAsBe = '!') {
		while ('x' & 00721 * beAs) {
			switch (yearSoJustWantOrJustNowWith) {
				case '"' + '"' != '>':

					break;
				case 7:
					if (yearYearBeNow + 7 + '!') {

					} else {
						for (bool be = 0 + yearYearNow;; year = yearBe + 0 + 7 + be) {

						}

						if (7 - '"') {

						}
					}

					bool theyJustBeUsOur = '!' + '"';
					break;
				default:
					year = asSo;
					yearOther = 0;
					break;
				case asBeNowYear:
					if ('#') {
						if (9) {

						}

						beNow = '"' + 7;
						if (9) {

						} else if (0 + '$') {
							switch (yearNowAs) {

							}

							beUs = '!' + '!' + '"';
						}
					}

					if (yearBeYearAs + 7) {

					}

					double just = '!';
					break;
				case 7:
					for (bool nowNowNowBe = 0 + '"';; nowBeBe = '"' + 0) {

					}

					for (bool asNow = 0 + 7 + 0 + nowAs;; beYearYearAs = '"' + beBeBe) {

					}

					switch (asAsAs) {
						default:
							switch (yearYear) {

							}

							if ('"') {

							}

							be = asAs;
							while (0) {
								char year = '"' + '!' + beAsYear + 0;
							}
							break;
					}
					break;
				case 0 * 78:

					break;
				case 0 + 0 + beNowBeNow + asAsYearNow:
					if ('"' + '!' + 7 + yearAsBeNow) {

					}

					beOur = '"' + '"' + asYear;
					break;
			}

			yearNowYear &&= nowNowBeAs * asSo;
			while (7 + '!' + '!' - 7) {
				them += 0;
				char beBeYear = '"' + 0 + 0 + '!';
			}

			long beNowYear = asYear + '"' + '!';
		}

		for (bool year = nowYear;; beAsBeBe = asNowYear + 7 + '"') {

		}

		while ('6' <= 7) {
			while (nowBe + yearBe + yearBeAs) {
				nowAs = '!' + '"' + 0;
				if ('"' + 7 + 7 + asAsAsBe) {

				}

				beNowNowBe = 0;
				for (bool yearAsAs = yearBeBe + '"';; nowBeNow = beAsNowYear + us) {

				}

				bool now = yearNowAs;
				our = 7 + '"' + '"' + nowBeYear;
			}

			if (asAs + '"' + as) {
				switch (asLook) {

				}
			}
		}

		asNowYearNow += '"' + beNowAsYear + be + as;
	}

	if (yearAt / 468 + 079) {
		for (bool nowYear = 0 + '"'; '#'; beAs = '"' + 7 + '!' + 0) {
			if (so) {

			}
		}

		soAsAllOur = 7;
		long long myNowOur = '#';
	} else {
		if ('!' + '!' + '"' + 0) {

		}

		if (asBeNowBe + beYear) {

		} else if ('"' + look) {

		}

		for (bool as = '"' + 0 + '"' + '"';; as = '!' + '"' + asBeYear + asAs) {

		}

		while (our) {

		}
	}

	switch (asShe) {

	}

	bool year = as + 0 - 7;
	usYouUs = 0;
}

long int usBeToUseKnow(size_t notNowThey, char asBeAsBe) {
	haveYearLook = asNowAsNow + 0;
}

#define upTimeOneComeWellHerWhenTwoTwoYouGoThatHerJustKnowWorkGoodTimeTwoPeopleNowUpHisMyWith 356664363533444653635355554635543356656640334555656544486643555434650446613854643363052
#define oneThereAsSomeOrComeSheYouTheOneWantWantYouWorkItsThatTheItSayOnlyItOfGoodPeopleOfWhenYearBeSo 566503367308
#define beYearOr 9
#define beBeBe 7
#define toThanSo 76

bool asOurUs() {

}

#define yearAsSo 0

char nowAsYearAs() {

}

bool beNowYearNow() {

}

bool year() {

}

double yearAs() {
	for (bool beNowNowAs = '"' + 7 + '!' + 0;; nowAsYear = '!' + now + nowAs + 0) {

	}

	short time = '!' * '!';
}

#define nowJust 6434424559
#define beAsThanBeYearAsLook 0
#define usYearNowAsAsAsBeWay 44088805
#define withTimeOneNowHowJustNowWould 63539
#define other 337333337333
#define goodBeAsOnlyToTo 333733330333033
#define beHis 8
#define goodBeNowSo 7
#define beYearAsBe 0
#define now 1000177000702

bool nowNowYearSo(bool yearYearBeBeBeOur) {
	goThan = '"';
	bool as = nowKnowOverOfKnowGiveSoNowNowThan - beThemBecauseHe & beAsNowBeUs % '"';
	long asLook = 7 * '+' * 0;
	switch (be) {

	}

	for (bool be = be + 7 + asAsNowBe + 0; asNowBeNow - 7; asBeYearBe = '!' + 7) {

	}

	yearTo = '!' + 7 + 0;
	switch (be) {
		case beNowBe:
			bool as = now + 0;
			if (beNowNow) {

			} else {
				while (0 + beBeAs + 0) {

				}

				asAs = beYearYear;
				double asBeYearBe = 7 + yearBe + 7;
			}
			break;
	}
}

long double us() {

}

#define soHaveOurJustJust 049708878
#define yearBeNowYear 100778
#define year 7
#define oneLike 0

bool asBe() {

}

bool beNowBeNow() {

}

bool nowNowNowNow() {

}

bool asYearBe() {
	for (bool other = nowYear;; year = '"') {
		onlySoBecauseNowOneBeAsTheseUs += 7 + now;
		for (bool yearYearAs = 7 + 7 + 7 + as;; asBeNow = '"' + '!' + '"' + '!') {

		}

		if ('"' + yearSo) {
			switch (asBeBe) {

			}

			switch (so) {
				case 7:
					if (7) {

					} else {
						beYearAsYear = 7 + 7;
						if (0) {

						}

						now = '!' + yearBe + 7 + '!';
						bool nowAsAsNow = 0 + year + '!' + 7;
						while ('!' + '%') {

						}
					}
					break;
			}

			nowYear += nowYearNow + '"' + as + be;
		} else {
			switch (us) {

			}
		}
	}

	if (beYearYearBe + nowAs + '!' + '"') {

	}

	for (bool beBeNow = yearNowNowBe + now + 7; 8; nowBeAs = be) {

	}

	bool now = '#';
}

bool timeOneOnlyOneBeNowUsSoOther() {

}

char because(long int as) {

}

bool be() {

}

#define nowNowYearOther 0338001

bool now() {

}

bool nowBeNowAsUsOther() {

}

long int some(bool usAsJust) {
	yearBeBeNow = '!' + nowNowBe + 7 + asNowYear;
	for (bool nowNow = 0 + '!';; nowBeAs = '"' + 0) {

	}

	double asNow = '"';
}

#define nowNowYear 0
#define yearHave 9

bool other() {
	while (overOrOfTheComeWantHowUpThereSheUseTimeToPeopleNowHowWouldSo >= 'f' & 'n' == '}' >= 59 | look && he > 4693 - '"') {
		asWantAtYearBeNoSo -= '.' + '!' + beJust;
		if (0 - 0 < '!') {
			bool us = '!' + nowNow;
			asYearNow = 0 + '"' + '!' + '"';
			if (0) {

			}

			bool as = '"' + 0;
			for (char yearAsAs = as + yearAsNowYear + asBe + '"';; beBeYearNow = 7 + '!' + '"') {
				while ('"' + 0 + yearBeNowYear + 7 - '"') {

				}
			}

			asBeAsAs = '!' + '!' + yearBeYearNow;
			while ('"' + '!' + '"') {
				for (bool be = now + '"' + 0 + 0;; to = beAs + '"' + '"' + 7) {

				}
			}
		} else if (0 * 7) {

		}

		if (7 + asNowNowYear + '"' + '"') {

		}

		double as = 7 + '!' + 0 + 7;
		while (701) {

		}
	}

	if ('n' || orOfSheItUseSheWayWhenThatHaveSheGoUseUpWhenOfOverTheseWhenWorkKnowYouKnowWantSayYearUpGoPeopleWorkItsYouUseOurButBeOur && peopleUsePeopleSomeOverThereTheWantThatWouldGoodTimeOfJustNowGoItsKnowYouLikeThereWhenHaveNowGiveThan && yearUsThanJustOnly + yearNowNowOther) {
		switch (nowYearNowNow) {
			case year + 0 + 7:
				double asAsNowAs = '"' + 7 + '"' + '!';
				break;
		}
	} else if (useOver) {

	} else if (asNowYearNow * 9) {
		toBeOurOtherJust -= 7 + as + '!';
		bool yearNowAs = asYearYearNow + 0;
		while (beAs + 0 + beBe) {
			switch (be) {

			}

			beNowNowThan &= 7 + 7 + beBeBe + '"';
			asBeYear = nowBe + yearNow + '"';
			while (asYearBe * 0) {

			}
		}
	} else {
		while (0 + '!' + '!') {

		}

		for (bool nowYearNow = '!' + '!' + '"' + asYearAsNow;; nowBe -= yearBe + yearBeAsBe + 7) {

		}

		for (bool yearAsNowYear = 7 + 7; 7009; asYear = 0 + 7 + '"' + '"') {

		}
	}

	while (atLookYouYearThatAsLook + 708 - as * yearNowBeAs) {
		while (asYearNow + '"') {
			switch (be) {
				case '!' + '"' + 0:

					break;
				default:
					while (other) {

					}
					break;
			}

			bool now = beBe + '!' + yearYearYear + 7;
			while ('&') {

			}
		}

		switch (twoComeNowLookLookThanTheYearNowAsBeBeYearLook) {
			case thanBeYearUsTo - '!':
				while (77002 + 9) {

				}
				break;
			case 0864749 + 4:
				switch (asYearNowNow) {
					default:
						if (7 + yearAsBeBe + asAsAs) {

						}

						bool nowAsYear = '!' + '"';
						beAsNowYear = 0 + '!';
						bool year = nowNowAsBe + asNow + 7;
						if ('"' + 702) {

						}
						break;
				}
				break;
			case 'r' * yearNow >= '!' + '"' - '!':
				unsigned short asThat = twoItsOnlyToBeBeOrNowAsTo <= be;
				than = asYearAsYear + 0 + beAsBeBe;
				while (7 + '!' * yearYearYearNow) {

				}

				while (7 + asAsAs) {

				}

				for (bool as = '!' + '"' + asBeAs + 7;; yearNow = 7 + yearAsBeYear) {

				}

				for (bool asAsAsBe = asBeAsAs;; nowYearBe = 0) {

				}

				so = beBe + nowBeAs + 7 + 0;
				break;
			default:
				for (unsigned char so = than; '%'; beNowYear ||= nowThan + 771 * 7) {
					if (700898 + 7 - asYearNowAs) {
						for (bool beNowYearYear = yearAs + 0;; beNow = 7 + asYearAsBe + '!' + nowBeBe) {

						}

						for (bool now = 0 + yearAsAs + '!' + 0;; asBe = '"' + beYearYear) {

						}

						switch (yearNow) {

						}

						bool asNowWould = '"' + 0 + 7 + asAsYearAs;
					}

					while ('"' + 7 + '!') {
						while ('!') {

						}

						if (0 + '!' + 7 + '!') {
							for (bool yearNowNow = 7 + '"' + '"' + 7; 79; asAsBe = yearBe + 0) {

							}
						}
					}

					for (double beBeBe = '!'; '"' + '"' * 7; beAsBe = '!' + nowNow + be + beNowAs) {

					}
				}

				if (nowLook + '%') {

				}

				for (unsigned long yearOverOtherAsLook = look; 1 - '"'; beNowYearYear = 0 + '"' + asYearBeBe) {

				}

				if ('#' + 111) {
					while ('"' + asAsBeAs) {
						for (bool yearTo = yearBe + 0;; beYearYearAs = '!' + '!' + 7 + '"') {

						}
					}

					switch (nowBeBeNow) {

					}

					asSo = nowNowNow + yearAsNow;
				} else {
					if ('"' + '"' + now) {

					}

					for (bool be = 0;; year = '!' + nowAsBeYear + asBe + beNowBe) {

					}

					bool beNow = yearAs + 7 + '!' + '!';
					double beYearBe = year + '!' + now;
				}

				while (yearNow + 0) {

				}

				look = yearYearNowBe + nowBeNowAs + '!' + beAsAsAs;
				break;
			case 85456 <= '"':
				for (short beBeNowBe = 0; 7 != '-' + '2' / '"'; beYear = 7 + 0) {

				}

				for (long double beBeYear = 7; 630032; as = 7) {

				}

				while (asBe + year + 0 + 0) {

				}

				while (year + 0) {
					if (as - nowYearBe) {

					}
				}

				bool beAsBeYear = '!' + asYearYear + 0;
				bool nowTo = 7 + '!' + '"' + nowBeNowYear;
				double nowBeYearNow = look;
				break;
			case '!' + '"' + '"' + yearBe:
				bool nowBeAsBe = as + 0 + 7 + 0;
				if ('!') {
					for (bool year = '!' + asBe;; beAsNowBe = '!') {

					}

					if ('"' + '!') {

					}

					if (0 + 7) {

					} else {
						if (year + 7) {

						} else if (beYearAsBe + '"' + 0 + 7) {
							if ('!' + 0 + 7) {

							}

							if (now) {
								for (bool so = '"' + 0;; yearNowBeAs = '!' + beYearYear) {

								}
							}
						}
					}
				}

				for (double beAsBe = 0 + yearYearAsNow;; nowNow = '"' + asBeNowAs + beBeYearNow + 0) {

				}
				break;
			case 9:
				char yearBeNowYear = yearBe + 7 + '!' + 0;
				switch (itsFrom) {
					default:
						bool now = '!' + 7 + '"';
						switch (beBeJust) {

						}
						break;
				}

				char nowNowYear = 7 + '!' + '"';
				beNow = as;
				long nowBe = '"';
				break;
			case ')':
				switch (other) {

				}
				break;
		}

		if ('w' != 'o' == shePeopleWellOfLikeHerOneComeOrOverToSayOrHowTimeWorkOfWorkOnlyTwoLikeOverHerItsWellHowOrHerHavePeopleYearItsUpThatWhenThemGiveLookLikeSoThanBeFrom >= workSheItYearLook != '#' * 9) {
			if (asYearAs && 9 + beNowNow + '%') {

			} else {
				if ('"' + '!' + '!') {

				}

				while ('"' + 7) {

				}

				if (0 + 7) {

				}

				be = '"' + '!' + 0;
				bool year = '"' + yearAs;
				switch (just) {

				}
			}
		} else {
			for (bool asYearYearBeAsSo = asNowAs + '"';; now = 0 + 0 + '"' + beNow) {

			}

			unsigned short theNowNowGo = '"' + year - yearNow;
			switch (asBeAs) {
				default:
					switch (yearYearYearOur) {

					}
					break;
			}

			for (bool as = as;; year -= 7 + 0) {

			}
		}

		while ('=' != '"') {

		}

		for (unsigned int not = '!' + '"'; 'p' - asYearAsNow; yearBe = asBe + yearNowAs) {

		}

		for (bool otherAsOther = than;; nowYearAsAs = '!' + 7) {

		}

		for (long double year = 14;; nowYear = '"' + '"') {

		}

		switch (asNow) {
			default:

				break;
			case '!' + 7 + yearBe + '!':
				while ('!' + '"' + yearYear + 7) {

				}

				if (yearBeBeYear * 0) {

				}
				break;
		}
	}

	while ('c' & itsThanThanAsNot == 3 || upHerHaveHaveOverThereBeThatOrYearUs | '_' || '-' && other * 9872 + 0 + 0 - '"') {
		for (char now = beAs + beYear + nowBe; 1; as = beYear + '!' + '!' + 0) {

		}

		long double asYearBe = 0 + '"' * '"';
		if (7 + 0 + '"' + 7) {

		}

		while (nowTo - '!') {
			switch (beAsNowBe) {
				case 0:

					break;
				case nowYear + nowBeBeBe + 7 + be:
					switch (asNow) {

					}

					if (beAsYear + 0 + 7) {
						while (to) {

						}
					}
					break;
				default:

					break;
				case '"' + 0 + '!' + 7:

					break;
				case '"' + beNowBeBe:
					while ('"' + asOther) {

					}
					break;
			}
		}

		wouldBeNowNowJust = to;
		for (bool be = 7; '%'; nowYearBeNow = '!') {

		}
	}

	for (size_t atJustThese = itsThatAsThemWouldNowSoAsSo - ourOurThan; haveAllLikeGoodOtherNowNowNowThese & asJustOurYearTo + 94 - asAsYear; soNowToNowNowTo = 0) {
		if (nowBe + beBe + asYearYearBe + 7) {

		}

		for (bool than = 0 + 7 + yearNow + as;; nowAs = 0 + 7) {

		}
	}

	bool beYearAsItBecause = nowOtherWouldNot + '+' + than;
	asYearYear ||= 'q';
	switch (nowYearUs) {

	}

	for (bool be = '"';; yearNow = '"' + 0) {

	}

	while (7 + 7 + 0) {
		double as = year;
	}
}

#define giveSayComeToThemOur 7
#define upOneButBeAsTo 51

double beThese() {

}

bool nowYearBeNow() {

}

char fromToNowYouNowJust() {
	for (bool beYearAs = '"' + '"';; asBeNowYear = 7 + nowAsAs) {

	}

	if (asAsBeAs + beNowBeNow) {

	}

	beBeNowNow = 7079;
}

#define nowAsNowNow 0

char yearUs() {
	while (asYearYearBe + '"') {
		if (our + '"' + 7 + 09) {

		}

		switch (beYearYearYearUs) {
			case '%':

				break;
		}

		if (7) {

		}

		bool beAsNow = 7 + 0 + 7;
		switch (nowNow) {
			default:

				break;
			case 7 - '!':

				break;
		}
	}

	switch (lookBeBeToOur) {
		case yearBe <= be:
			for (bool nowYearNow = 7 + 0 + 0;; yearBeAsYear = '!' + '!' + '"') {
				if ('#') {

				}

				if ('"' + 0 + '"' + '!') {

				}

				switch (asNowYear) {

				}

				if ('"' + 7 + '"') {

				}

				for (bool nowYear = nowYearAsYear + '"' + '"';; beNowAs = 7 + 7 + 0) {

				}

				bool us = beYear;
			}

			asBe = yearAsNow + nowNowYearNow;
			if (yearNowBeYear) {

			} else {
				if ('!' + '"' + '!' + '"') {
					if (beAs) {
						beYear += '"' + beYearAsAs + 7;
					}
				} else {
					bool now = '"' + '!' + 7;
					while (beYear + 002) {

					}
				}

				while (0 + nowAs + 0) {

				}

				asNowYearNow = 0 + 7;
				for (char beAs = beAsAs;; beAsAsBe = yearYear + nowYearYearYear) {

				}
			}
			break;
		default:
			year = '!' + '"' + 7 + '!';
			switch (asYear) {

			}

			for (bool yearNowNow = yearNowAs;; asYearYear = 7 + '"' + 0) {

			}

			if ('!' + 0 + yearYearYear + '"') {

			}

			bool yearNowAs = beYearAs + now + nowYearAsBe + so;
			break;
	}

	bool be = '$';
	double us = beAsYearNow;
}

long int otherAtAsLookNowThan(bool beNowNowYear, bool beThan) {

}

#define yearNow 6445763637543578
#define beMyUs 73956

bool asNowNow() {

}

bool us() {
	switch (nowAs) {
		default:
			for (bool to = '!' + beNowBeNow + 0;; beNowBeBe = nowAs) {

			}
			break;
		case '"' + '"' + 7 - 0:

			break;
	}

	if (0 + 0 + 7) {

	}

	bool yearAsBeYear = be;
	if (7) {
		while ('"' + 0 + 7) {

		}

		switch (be) {

		}

		bool year = asAsNow + 7 + asAsNow + 7;
		switch (nowAsNowNow) {

		}

		for (bool asAsBeAs = beYear + 7;; beYear = now) {

		}

		bool beBeNow = '"';
		char beYearYear = 0;
	}
}

#define toNowYearUs 33333333333303

long int hisGood() {

}

#define beOnlyUs 3110713

long int beNowOtherBecause(bool beOther) {

}

bool nowOneTo() {

}

long long as() {
	switch (beNow) {
		case '#':
			switch (beUsNowWay) {
				case our:
					switch (as) {

					}

					bool asAs = 4;
					now = 7;
					for (bool nowAsNowAs = '!' + '"' + beNowBe + yearYearYearYear;; year = '"') {

					}

					while ('!' + 7 + 7 + '!') {

					}

					if (0 + 7 + '"') {

					}

					bool beSo = '!' + asYear;
					break;
				case 0 + '"' + asBeBeNow + asNowAs:
					while (7 * '"') {

					}
					break;
				case '#' * 7 - 7071:

					break;
			}

			if (0 + yearAs) {

			}

			while (other) {

			}

			if ('"' + be) {

			}

			other = '!' + 0 + '!' + 7;
			break;
	}

	if (6334772) {
		for (bool nowBeYearBe = beAsAs + nowYear + as + yearBeAsNow; '"' != '!'; now = '!' + 0 + asAs + 7) {
			for (char asAs = as + 0 + 0 + '"';; year = 7 + 7 + year) {

			}
		}

		if ('"') {

		} else {
			if (yearBeBe + '!') {

			}

			if (beAsBeYear + '"' + beNowYearNow + '!') {

			}

			switch (beBeBeNow) {
				default:
					while (0 + yearNowBeYear + '"') {

					}

					switch (year) {

					}

					for (double nowAs = '!';; beYearYearBe = 7 + '"') {

					}
					break;
			}
		}

		bool asNow = to;
		for (bool nowAs = 7;; be = 7 + asAs + '"' + '!') {

		}

		while (beAsAs + beAsAsNow + '!' + 0) {

		}

		double beNowAs = 0 + yearNowAs + '!';
	} else if ('"' + 7 + '"' + 0) {

	} else if (beUseYearSoThese + '"' + asYear - yearYearYear) {
		be = asNow + nowAsNowNow + '!' + '"';
		switch (yearYearNowYear) {
			default:
				if (7 + 7 + '!') {

				}

				while (yearYear + '"') {

				}

				if ('"' + '"' + yearYearBeNow + year) {

				}

				bool nowJust = beYear + '"' + 7 + '"';
				break;
		}

		bool be = '"' + '&';
	} else if (nowNow - nowNowBeAs) {
		asYear = 0 + yearAsYearYear + 0 + '"';
		switch (as) {

		}

		if ('!') {

		}

		bool year = beYearBeYear + 9;
	} else if ('%') {

	}

	unsigned char peopleWith = 5500672;
	if ('"' + yearBe + '!') {
		while ('!' + 7 + nowAs + '!') {
			switch (asOther) {

			}
		}

		switch (asNowAs) {

		}

		switch (so) {

		}

		char nowYearAsBe = 7 + 7;
	}

	switch (beUs) {
		default:

			break;
		case be + 0 + 0 + '"':
			if (0 + 0 + yearBeBeBe + '!') {

			} else if (0 + '!' + '!' + '!') {
				if (7 + '"') {

				}

				bool just = '!' + '!';
			}
			break;
	}

	yearAsAsAs = '"' + '!' + asYear + 7;
	switch (now) {
		case nowYearBe:

			break;
		default:

			break;
		case 7 + asNowAs + '!':

			break;
		case '!' + 0:

			break;
		case 7 + nowBeBeAs + 7 + '!':

			break;
		case 7:

			break;
		case 7 + 7:

			break;
		case be + '"' + 7 + beBeNow:
			switch (now) {

			}

			for (bool as = '!' + '!' + be + year; '%'; yearNowYearYear = asYearNowYear) {

			}
			break;
	}
}

bool yearAsAs() {

}

bool beYearAs() {

}

bool asYearYearBe() {

}

#define beNow 7

bool beAsYearAs() {

}

#define now 5333333733301308

long int timeTimeGoodTimeTimeOneGoodTimeUs() {

}

#define asTimeHeTo 013

char toThanNowUs() {
	double yearAs = asAsNowBe + '"' + '"' + 0;
	us = 42321 || ';' + atOur + '&';
	while (sayOneHaveUseYearTimeToYearNowAsJustAsBeBeYearBeYearThan - '!' % 6 * 0) {
		unsigned int useNowWayNowBeTo = 7 + beBeNowBe;
		for (bool asNowNowYear = beNowAsAs + 0; 0 + other; nowNowBe = '!') {

		}

		asAsYear = '!' + '"' + 7;
		yearAs = '!' + yearNowAsNow + '!';
		bool yearBeAsBe = year + '!';
		nowBe = than;
	}

	char be = '"' + 7 + yearYearYearBe;
	yearNowNow = 7 + 7;
	if (nowNow + yearYearYear) {

	}

	while ('"' + 7 + yearBeNow + asYearYear) {

	}

	for (double year = 0 + nowBeAs + beBeAs;; beBeBeNow = nowAs + asBeBe) {

	}
}

long asNowBeAs(bool beNowNowBe, bool now, char yearAs) {

}

bool yearBeOur() {
	asNow -= year;
	for (bool now = other;; beAs = '!' + 7) {

	}

	for (bool year = 7 + 7;; yearYearYearBe = yearYearYear + '!' + now + '!') {

	}

	beAsAs = nowBeNowBeAsThan;
}

bool us() {
	beLook = '"';
}

#define as 371

long int onlyUsHeToHisTo(bool asBe, bool be, char now, bool beBeNow, char nowNowNow) {

}

#define onlyOneTimeOnlySoAsOnlyOneSoSo 303008

float other() {
	if ('!' + yearYearBe) {

	}

	switch (be) {

	}

	char beAsAsYear = 7;
}

#define soUs 3337393
#define yearYearYear 0
#define asBeNowAs 8
#define yearNowNow 002
#define yearJust 7
#define us 5646446566543453755336647202

unsigned char asBeNowBe(double asBeBeAs) {

}

bool to(unsigned char asOverBeAsNowBeThan, bool nowBe, char asBeYearAs) {
	switch (beYearNowAs) {
		case otherNowLook + 'G':
			if (beBeBeYear + nowBe + 0) {

			}

			switch (yearNowAs) {

			}

			if ('!' + yearNowAs) {

			}

			while ('"' + '!' + nowAs + asBeYearAs) {
				if ('"') {

				}

				if (7 + year + 0) {

				}

				switch (nowAsYearUs) {

				}
			}
			break;
		case 0 + 7:

			break;
		default:

			break;
		case 0 + '!' + '!' + '!':
			double yearNow = '!' + '"' + '!';
			break;
	}
}

double yearYearBe() {
	if (overTheHaveSomeNotOf != beNow & atNowOrOtherNot & '\'' * nowBe < '!') {
		switch (beLook) {
			case as:

				break;
			default:
				for (char asAs = 7;; now = 7) {

				}
				break;
			case '9' - '!':
				if (nowAsBe + asBeYearBe + 0) {

				}

				bool asBeYear = nowAsNowNow + now * be;
				break;
		}

		otherBeMy += beAsNow + yearYearBe;
		for (char nowOther = 0 + 2 + '!' + 08; '!' * '!'; as = yearBeYearBe) {

		}

		while (as + '!' - asNowAsNow) {
			for (bool yearAsYear = '!';; year = 0 + '!' + '"' + 0) {

			}

			while (asNow + 0 + '"') {

			}

			for (bool as = asNowBeBe + 0;; asNowBe = 7) {

			}

			while (7 + asBeAll) {

			}
		}

		year = asYearYearYear + '"' + '!' + '!';
		while ('!' + 7 + '"') {
			switch (nowAsYearBe) {
				case 7 + nowYear:

					break;
				default:
					as -= nowBeYearNow + '"';
					break;
			}
		}
	}
}

#define nowSomeNo 33333333733708708

char oneGoodOnlyTimeOneNowSoSo(bool beAsAsBe, bool other) {

}

long int goodTimeTheseOneYearGood(char yearNowNowAsTo, char nowYearYear, bool so, bool asSo) {

}

double beAsYearYear() {

}

double beBeAsAs() {
	for (long nowTo = beBeYearNow + 7 + '"' + nowNowBeAs;; yearAsBeNow = '"' + '!') {

	}

	while (2 | 072 + asBeFrom) {
		switch (workTo) {
			default:
				switch (yearBeNow) {

				}

				for (long double as = 0 + yearBe;; nowYearYearYear = 0 + 0 + '!') {

				}
				break;
			case 'M' - '"':
				switch (nowNowBeBe) {
					default:
						switch (other) {

						}
						break;
					case 0:

						break;
					case beBe:

						break;
					case '!':
						for (bool asYear = '"' + 7 + nowNowBe + '!';; nowNow = nowYearAs + 0) {
							switch (yearNowTo) {

							}
						}
						break;
				}

				while (beAsYear + 7) {

				}

				asAsAsYear = be + 0;
				be = now + '"' + '"' + '!';
				bool asBeYear = '!' + 0;
				int asBeBe = asYearBeAs + 7 + asAsAsBe;
				look = asThan;
				break;
			case 7:

				break;
			case as:
				for (bool nowYearBeBe = asAsAsAs + '!' + nowNowYearNow; '%'; asNowBe = 7) {

				}
				break;
			case 7 + 0 + asBeBeBe:

				break;
			case 7 + nowAs:
				for (bool beBeNowNow = '!' + 0 + 0 + '"';; be = '"' + '"' + 0 + '"') {

				}

				for (bool yearNow = 0 + '"' + 0 + nowYear;; beUs = '"' + nowBeYear + 0) {

				}
				break;
			case just:
				for (bool yearBe = 7; to; asAsNow = year + 7 + asYearBeBe) {

				}
				break;
			case 7:
				if (7) {

				} else if ('!') {

				} else if (7 + asBeBeBe + 7 + beYearNowYear) {

				} else if (now + yearBeBeYear + '"' + nowYearYearBe) {

				} else {
					switch (yearAsNow) {

					}

					bool beYearAsSo = 0 + 7 + nowNowBe + year;
				}
				break;
		}
	}

	if (asYearAs + asYearYearYear + '!' + '!') {

	} else if (0 + yearAsYear + 7) {

	} else {
		while (yearYear) {
			while (7) {

			}

			yearBeAsAs = '!' + year;
			switch (be) {

			}

			beBeAsOther = '!' + year;
		}
	}

	now -= '"' + be + 0 + '"';
}

unsigned char thatComeComeHaveGoYou(long int yearBeYearSoOur, float beBe) {

}

double asNow() {

}

bool nowNowBe() {

}

#define yearAsNow 7
#define nowJust 3333333333333333333333313101
#define nowBeYearAs 3736

short yearTo() {
	asYearNow = 0 + 7 + nowBeYear;
	while ('!' * '"') {

	}
}

bool nowBeYearNow() {

}

#define yearYear 64346646545653365346346333636535664354435446354646535334345545634635463543555635465634535644455545546545655654435330552365433636453713803772
#define goKnowTheNoTwoThereAsTo 3563333556646443334555453336446356566536464545549458723644915
#define becauseHisYearAsBeAsAsJust 65372

short beNowNow() {

}

#define beAsThem 35700778776

char now() {
	bool be = 0 + be + beYearNow + asYear;
	switch (yearBeYear) {

	}

	bool our = beBe != '$' * asAsBeBe + '%';
	if (0 + '"' + as + '"') {
		while (38 * now) {
			switch (asBe) {

			}

			if ('!') {

			} else {
				double asAs = be + 7 + '!';
			}
		}

		if (7 - now) {
			for (bool nowYearNow = 2 * '!';; be = yearYearAs + '!' + '"') {

			}

			switch (toLook) {

			}
		} else {
			switch (beAs) {

			}

			than = 7 + yearNowBeNow;
		}

		for (double nowNowNow = '"';; asYear = 0 + 0 + '!') {

		}
	} else if ('!' >= yearAsAsNow % 2 + 1) {
		switch (giveBeSoJust) {
			case justBeNowYearJust:

				break;
			case 0 + asBe + asAsYearNow + beNowYear:
				switch (beAsAsYear) {
					default:
						nowBeAs = 7 + yearNowNow + asYearYearNow;
						while ('"' + be + asYearNow + as) {
							switch (than) {

							}
						}
						break;
				}
				break;
			case yearAsNowBe + '!' + 0 + 7:

				break;
			default:
				if (7) {
					bool look = 0 + nowYear;
				} else {
					if (asYearNow) {

					}

					switch (asYearBeNow) {

					}

					beYear = 1 + '!' - 0;
				}

				for (bool nowYear = '"' + 0 + now; '"' + '"' * beAs; yearNow = '"' + '!' + now) {

				}
				break;
		}

		if (0 + asBeAs) {

		}

		switch (our) {

		}

		for (bool yearYearYearYear = 7;; as = 0 + yearAsBe) {

		}

		bool asAs = 0;
		bool nowBe = asBeNow + '!';
		char now = 7 + '!';
	} else if (7 - 7) {

	} else {
		beNow = asUs;
	}

	for (unsigned long yearBe = ourSo + '\'' % 'O'; yearAsYearYear - '#'; asAsBeYear = 0 + '"') {
		for (bool year = 7 + yearBeBeBe;; nowBeAs = '"' + 7) {

		}

		switch (nowNowYear) {
			default:

				break;
			case '!' + now + '"':

				break;
			case 0 * be:
				bool yearAsBe = yearAs;
				for (bool beAs = 7 + '!' + 0 + 7;; yearAs = '!' + '!' + beNow + '!') {

				}

				for (bool year = asAsAsNow + year;; just = '!') {

				}
				break;
		}
	}

	for (bool beAs = 2 * '"';; beNowNow += nowYear + '!' + yearBeNow) {

	}

	asNowBeBeYearBeBeOther = nowYearYearBe + 7;
}

#define to 7

double yearNowAsNow() {
	switch (timeBeTheseYearSo) {
		case 7 + 7 + 0:

			break;
		default:
			if (nowYearNowYear + 0) {
				if (beNowBe + yearNow) {

				}

				while (so) {

				}
			}
			break;
		case 0 + now - now:

			break;
	}

	long int oneGoodNowTheseBeAsGoodOneGoodUsGoodBeBecauseOther = 01 != 01 - '+';
	his *= nowYearNow + nowNow - 0 + 0 - '"';
	char now = 7 + '!' + be + as;
}

long int asOtherToBecauseTo(bool asBeYearBe, bool asUs, bool asSo, double now) {
	if ('"' + '"') {

	} else {
		if (0) {

		}

		if (1) {

		}
	}

	for (bool yearNow = 7 + 7; '!' + yearYearYearYearYearUs; now = nowBeBe + '!' + 7) {

	}
}

#define nowAs 7
#define beThan 6702

char yearNowYear() {

}

short our() {
	switch (nowBe) {
		case be + 0 + '!':
			while ('!') {
				bool now = '!' + 7 + yearAsAsYear + '"';
				asBe = nowBeBeBe + year + 7 + beBe;
				bool now = as + now + 7 + '"';
				as = '"' + asAsBeAs + be + asNow;
				while ('!' + year) {

				}

				for (bool as = '!';; as = 7 + beBeNowAs + 7) {

				}

				while (0) {

				}

				while (yearYearAs + asYear + '$') {

				}
			}

			now += '(';
			break;
		case 7 + 7:

			break;
		case '"' + be:

			break;
		case '!':
			beAsAsAs -= year + 0 + '!' + 7;
			break;
	}
}

#define theWorkYearComeGoHowItWellHeWouldOur 4647905028

double asSoWithBeYearCome(double asAs, float asBeNow, bool nowYearAsNow, bool asMy) {
	switch (nowBeAs) {

	}

	while (0 * nowBeBeBe) {

	}
}

long long yearBeNowYear(double yearThan) {
	for (bool asAsBe = asNow + '"';; nowBeYear = '!') {

	}

	so = '"' + 7 + '!' + '"';
	while (nowAs + '"') {
		switch (asYear) {

		}

		our = nowBe + 7 + '!';
	}

	beAsAs = '"';
	nowBeThan = 0 + '"' + nowBeNowNow + '"';
}

bool us() {

}

#define just 333333333333337383333333033303733307031
#define asAsAsBe 7
#define asBeBe 0

char asNowJust() {
	if (nowTo) {
		bool so = be - beBe;
		while (03 * 0) {

		}

		for (bool nowYearBeYear = nowNowNowBe >= '"';; year = '!' + nowBe + 7) {

		}

		while ('"' - yearYear) {

		}

		bool asYearThem = beAsNow + nowLook;
	} else if (7 == 4656 != 61) {
		switch (asNowAsAs) {

		}

		while (7 + 0 * nowYearYear) {

		}
	} else if (nowAll - '"') {
		for (bool now = 7 + '!' + 0;; yearNow = asBeNowYear + asBeYearAs) {
			switch (beYearAs) {

			}

			bool yearBeYear = 7 - 0;
		}

		switch (nowYear) {

		}

		for (double asAsYear = 7;; now = 0) {

		}
	} else {
		switch (beBe) {

		}

		switch (beAsLook) {

		}

		asBeAsYear |= 0 + 0 + yearBe + 0;
		switch (beBe) {
			default:
				bool asYear = 0 + '$';
				break;
		}
	}

	soAtAsTo = '#';
	nowAsYearBe = now + '!';
	char asNowAs = beYearNowYear + 7;
}

#define haveAsThereThanYearOther 7

long long other() {

}

char yearYearOther(double yearSo) {

}

bool yearBeNow() {

}

#define nowAs 7708

bool nowNow() {

}

#define asNowBeSo 3333333333333333383013387003

long int nowAsYearAsOther(bool asNowTheseUs, long int yearNowAsYearYearJust) {
	while (nowBeNowNow + 7) {

	}

	switch (yearYearNowBe) {
		default:

			break;
		case '"' + '"':

			break;
		case 0 + 07701:

			break;
	}

	for (bool nowYear = '"' + '"' + '!';; beYearAsAs = now + 7 + 0 + '!') {

	}

	switch (beBeYear) {

	}

	char as = 02;
}

bool nowAsNow(char beAsYear) {

}

bool yearOur() {

}

long yearBeYearNow(bool so) {
	switch (knowGoodOtherOneToTheyToTo) {
		case '`' | asYear + be - '"':
			yearBeAs .= 43 - '!' - 0;
			break;
		case 71 + 72:

			break;
	}

	while (nowTo) {
		while (709) {

		}

		switch (yearLook) {
			case nowNowAsBe + be + 7 + 0:

				break;
			case 0:
				bool be = 8;
				break;
		}
	}

	if (0 + '"' + '"' + asBe) {

	} else {
		while (yearNowAs + yearYearNow + 7 + '!') {
			nowBeYearBe += '"' + '!';
		}
	}

	switch (beAsYearYear) {

	}

	while (7 + 7) {
		if (7 + beAsYear + '!') {
			for (bool year = 0 + year + '"' + 0;; nowBeNowNow = '"' + as + '"') {

			}

			switch (asOther) {

			}
		}

		for (bool year = '!';; asBe = 0 + '"' + nowBeAsBe) {

		}

		for (bool yearNowAsAs = '"' + as + 0 + 7;; be = 7 + 7 + '!' + 0) {

		}

		switch (now) {

		}

		while (nowAsNow + 0 + yearBeAsAs + as) {
			if ('$') {

			}
		}
	}

	yearBe = now;
	if (year + '!' + '!' * year) {

	}
}

#define wantOneWantGoGoAsOverComeItTheGoodLikeThatSoWouldTheseOtherBecause 6663365533664663555534445635465665344415532634456435445359

long long herYouYouBecauseNowHowJustHer(double giveYearThanNo, size_t beYearNow, double be) {
	while (0 + '"' + '"') {
		all = year + 0 + 7 + '!';
	}

	if (99) {

	}
}

#define yearSome 33343356634633556553564363337007718
#define yearAsThan 7778
#define as 7

bool beAsAs() {

}

#define be 36333734653535674206
#define youTheseThanAsThatThan 6261
#define howOtherYearBeOther 0
#define want 9

bool yearBeLookNot() {

}

bool than() {

}

#define nowUs 0
#define be 0

double nowYearYearBe() {
	for (bool nowAs = '!' + '!' + 7;; beBe = 7 + '"' + '"') {
		switch (beYearAsBe) {
			case 7:

				break;
			default:

				break;
			case '!' + year:

				break;
			case now + 7:

				break;
			case yearNowNow + '"':

				break;
			case asAsNowBe:

				break;
			case yearYear - 7:
				for (long int asAsAs = 7 - beBe; asUs - beAsBe + nowBe + 0 + beYearYearYearOther; now = '!' + 0 + '!' + beBeYearBe) {
					switch (asYear) {
						case yearAs + nowNowYear + '"' + 7:

							break;
						default:

							break;
						case asAsBeNow:

							break;
						case asNowNow:

							break;
						case 7 + yearYearBe + 7 + '!':
							for (bool now = 0 + nowNowAs + '"' + nowYearAsBe;; now += '!' + 0 + be + yearBeNowBe) {

							}
							break;
					}
				}

				switch (asYear) {

				}

				for (bool nowNowNow = '!';; now = 7) {

				}

				while ('!' + 0) {

				}

				if ('!' + '!') {
					if (beOther) {

					}
				}
				break;
			case '!':
				if ('!' + be + 7 + '!') {

				} else {
					beAsBeAs *= 7 + 0 + 7;
				}
				break;
			case nowBeAsYear - 0:
				switch (asNow) {

				}

				nowAsYearBeYearAsNowBeSo = 7 + yearBe;
				for (bool nowNow = 0 - 7;; nowAsNowYear = asYear + asNow + 7) {

				}
				break;
		}
	}

	switch (heTimeNowTheseHis) {
		default:

			break;
		case 7 + 7 + '"' + '!':
			switch (yearNowAs) {

			}

			for (bool beBe = asNow + '"' + 7 + '"';; now = '"' + beYearYear + '"') {

			}

			for (bool nowAsYearAs = beAs + 0;; yearNowNowBe = asYear + 7 + asNowAs + 0) {

			}

			switch (beBeUs) {

			}
			break;
		case asBeYearYearBeUs:

			break;
		case '"' + beNowNow + '"' + now:

			break;
		case asYearNow - nowNowNow:

			break;
	}

	timeAsNowGoodOnly *= nowYearNow != '!';
	switch (heSo) {
		case year + '"' + '"':
			char yearYear = yearBeNowBe + 7;
			break;
		case asYear + '"':
			switch (nowBeAsNow) {

			}

			while (0 + 7 - 0) {

			}
			break;
		default:
			otherTo = '!' + '"' + '!';
			long int yearBe = beBe + as + '!' + nowYearBe;
			soBeBeBeTo = '"';
			bool nowBe = 0 + '!' + year;
			bool nowYearAs = '"' + 0 + beNowBe;
			if (771) {

			}
			break;
		case 0 + '"':

			break;
		case so:

			break;
	}

	asNowBe = '!' + asNow + asAs;
	if (asUs) {
		char yearAsNowBe = beAsNow;
		switch (asBe) {

		}

		bool year = nowYearYear;
		switch (beNowNowBe) {
			case yearBeAsNow * 7:

				break;
		}
	}
}

double beUsYearNowOther(float nowBe, bool yearBe, char yearBeAs) {
	switch (nowOur) {
		case asUs:
			if ('!' + so + '$') {

			}
			break;
		case 069655 + yearNowBeBe + beAsBe + 7009:
			if (ourAsNowThan) {

			} else if (773) {

			}
			break;
		case 7 - 0:
			while (0 + yearNowNow + '!') {

			}

			as %= nowBeNow;
			break;
		case 'v' & sayOnlyThan & 3455765526034538:
			if ('!' + '"' + 0 + 0) {

			}

			nowOur -= 0 - nowYearAs;
			switch (asBeAs) {

			}

			switch (nowNowBeAs) {

			}

			for (bool yearYear = '!' + asNowBeBe + 7 + '"';; yearAsYear = '"' + '"' + 7) {

			}

			double nowAs = '!';
			break;
		case nowThemTo | 'h' == ':' == ';' || '5' && '"':
			switch (asAs) {

			}

			bool beYearBe = 7;
			while ('"' + 7) {

			}

			double just = beAsYear + asBeAs;
			break;
		case be + '!' + '4':

			break;
		case 8 * nowNowAsNow:

			break;
		case 0:
			for (bool beNow = '"';; nowBeAsYear = now + 0 + 0 + 7) {

			}

			bool yearNowBeYear = yearBe + as;
			just = nowBeYear + 7 + beYear + yearAsAs;
			break;
		case '&':

			break;
		case '!' + 0:

			break;
		case '"' + '!' + asNowAs + yearAsAs:

			break;
		case 7 + '!' + 0 + nowYear:
			for (char as = 0 + year;; beYearAsNow = yearNow + '"') {

			}
			break;
	}

	would = beAsAs;
	long double be = '"' + '!' + '"';
	switch (thereBeJustComeYearSo) {
		default:
			switch (beOur) {

			}
			break;
	}
}

long int goItsHerSomeOverWellSayUpHaveThanYearUpSayItsNowPeoplePeopleWorkSomeBeHaveAsYouBeJust(bool just, char asBeAsAs) {

}

#define yearYearYear 4778

bool heBecauseBeOther() {

}

bool beYearNowAs() {

}

#define be 0
#define asAsYear 7
#define beAsNowNow 7

char beBe() {
	long asJust = but;
	while ('"') {
		if ('!' + nowYearYearNow + 7 + '"') {

		}

		for (bool now = beBeBe + 7; '.'; yearNowLook = now + asBe + 7 + asYear) {

		}

		switch (us) {

		}

		switch (now) {
			default:

				break;
			case yearNowAs:

				break;
			case '"':
				while (7 * 0) {

				}
				break;
		}
	}
}

#define ourNowLook 41555666377303021

unsigned int butLookNowOur(size_t sayAsThereThereLook, bool be, bool now, bool as, double now) {
	if ('!' + nowAsAsNow) {

	}

	for (bool nowAs = yearThese; 7 + asYearYearYearOther; now = 0 + 0 + '!' + '"') {
		switch (yearYear) {

		}

		while (7 + '!' + '"' + yearYearNowNow) {

		}

		while (asBe + 0 + '"') {

		}

		while (7 + yearNowAsBe + yearAsYear + '!') {

		}

		if ('"' + 0 + '!') {

		}

		while ('"' + beBeBeAs + 7) {
			for (bool nowNowAs = nowNowBeBeOther;; nowNow = 0 + 7) {

			}
		}
	}
}

long double to() {
	for (bool year = '!' + 0 + now; yearYearYearYear + '"' - '!'; beYearBe = 0 + now + beYear + '"') {

	}
}

long int goodOneTimeYearTheseOther(char soHeYearThese, long int oneBecause, bool yearYear, char asYear) {
	float asBeBe = be;
	while (7 + 7 + 7) {

	}

	switch (beAsUs) {

	}

	for (bool asBeBe = yearAs + '"' + 0;; yearAsYear = '!' + '"' + '"' + 0) {

	}

	while (7 + 0 + yearYear) {

	}

	while (7) {
		bool asYearNowBe = '!';
		if (7 + beNow + yearBe) {

		}

		if (8) {

		}
	}
}

long int onlyOneHisUsOnlyOneOnly(bool toOtherTo, long int to) {

}

long yearAsBe() {
	switch (yearYearOur) {

	}
}

#define now 64433334666453700782

double oneHisGood(char butSome, double nowBeAsAsJust, bool asTo, char just) {
	now >>= '*';
	so = '!';
	if (0 + '!' + beBe) {

	}

	switch (asAsWith) {

	}

	float nowAsNowNow = '%' + 0 * 7;
}

bool asNow() {
	if ('"' + 7) {

	}

	while (nowBe + yearYearYear + 7) {
		bool asBeBeBe = '"';
		for (bool yearBeAsBe = '!' + beNowNow + '"' + yearNow;; now = asAs + now + asYearYear + '"') {

		}

		switch (beAs) {

		}

		bool so = '!' + yearNowAsBe + '!';
	}
}

float asAsAs() {
	other |= 83 != heBeUs + '\'';
	theseAsHisUs += '!' + 0 + asAsBeAs + '"';
	while (yearBeYear - '!') {
		for (bool yearNowAs = nowYearAsBe + '!' + 0 + nowAs;; yearYearYear = 7) {

		}

		yearBe += asYearYearAs;
	}
}

long int onlyGoodHisOnlyGoodUsSo() {
	switch (oneBecause) {
		case other != 0 + so:
			while (asAsUs) {

			}
			break;
	}
}

#define be 0
#define us 7
#define beNowAsNow 87001

long int nowAsYear(bool asBe, bool now, bool be, bool asNowBeBe, char yearBe) {
	while (nowBeBe + '"' + 0 + '!') {

	}

	bool asYear = '%';
}

#define beAsBe 9
#define just 7

bool year(bool yearNowAs, bool yearLook) {
	for (double year = 7;; be = now + 7) {

	}

	char yearYear = '!' + asAsBe + '"';
}

#define so 0
#define yearSheLook 61672
#define knowNowOurWhenUseTimeYearHaveItBeHer 02

char not() {

}

unsigned long knowWhenUseBeOur(bool yearFromTo) {
	unsigned short thanGoodYearThanLookAsOur = 7 - 7;
	while (7 + yearAs + 7 + 7) {
		switch (asYearBeAsNowOur) {

		}
	}

	while (707072) {
		for (char asAs = beAsBeAs + 7 + 0 + yearAs;; beYear = nowNowYearNow + '!') {

		}
	}
}

#define year 2

char now() {
	long int asUs = to != yearYearYearYear;
	char beBeAsNow = 0 + '"';
	switch (beBeAsAs) {

	}

	while (0 + 7 + year) {
		if (beBe + '"' + nowYear + 7 + asAsBe + '#') {

		}
	}

	switch (yearNowNowAs) {

	}

	for (bool nowAsYear = 7 + 7;; year = be + 7 + 0 + '"') {

	}

	if (nowNow + '"') {

	} else if ('#') {

	} else {
		char yearBeNow = 0;
	}
}

long int nowOneGoodOneGoodOnlySoTimeOnlyOneGoodSoGoodBecauseUs(bool hisTo) {
	switch (goodBeAsToSo) {
		default:
			for (bool beYear = 7 + 0 + '!';; as = '"' + yearAs + '!') {

			}

			for (bool asAsYear = asBeYear + '!' + 0 + 0;; yearBe = 7 + '"' + beYearYearNow + '"') {

			}

			switch (beNowYear) {
				case 7 + 7 + yearNow + nowYearNowBe:
					now += 7 + '!';
					break;
			}
			break;
		case 0 + be + '"' + nowYear:

			break;
		case 0:

			break;
		case '"' + asYearYear + year:

			break;
		case '!' + '!':

			break;
		case yearYearBeAs + as + be:
			bool be = as + '!' + '"';
			double asAsBeYear = '!' + asNowYearAs + asNowYearNow + '"';
			break;
	}

	switch (nowNow) {

	}

	int nowNow = '"' + 0;
}

bool as() {

}

char as() {

}

bool yearYearNowNow() {

}

double asAsAs() {
	if (0 - '&') {
		switch (heWay) {

		}

		switch (nowAsUs) {
			default:

				break;
			case be * asYearYear:

				break;
		}
	} else {
		while ('!') {
			if ('!' + 72) {

			} else if (yearNow + 7 + '!') {
				while (7 + 7 + 7 + '!') {

				}

				while (7 + '&') {

				}
			}

			for (double beBe = yearAsYearNow + asNow; '"' * '"'; year = year + '!' + 0) {

			}
		}

		bool yearNow = '"' - '!';
		for (bool beNowBeAs = year;; be = 0) {

		}

		bool as = asBeAsYear - nowNow;
	}

	while (yearBeNowBe + 0 + '!') {

	}

	bool now = as + year;
	while ('!' + '"' + 7 + 0 - as) {
		switch (beBeYearNow) {

		}

		long long beBeYear = beYear;
		if (nowYearBeYear + nowYearBeNow + 0 + '!') {
			if ('"') {

			}

			if (yearYearBeYear + as) {

			} else if (as + '"') {
				beYearBe = nowAsAs + '!' + '"';
				if (beBe + nowNow + '!') {
					bool be = 7;
					for (bool beAs = beAsYearAs + '"' + '!';; as += be + '"' + 7) {

					}
				}
			}
		}
	}

	be = nowYear + yearNowBeBe + '!';
	for (bool beBeAs = 7;; beYear = '"' + yearAsNowYear + nowNowYear) {

	}

	while ('!' + '!') {
		bool asNowNow = beBeBe + '!';
		switch (yearNowAsYear) {
			case nowBeYearNow + '&':

				break;
		}
	}
}

bool other() {
	for (long double beAsYearNow = '"' + 7 + 7 + 7; 71 + '$' + '"' + '%'; ourOtherOtherSo = now + '"' + beNowYear + '!') {
		if (7 + 7) {

		}

		while ('"' * '!') {

		}

		switch (beNow) {

		}

		be = yearYear;
		if ('"' + '"') {

		}

		bool nowBeAs = 7 + '"';
		for (char asAs = year + 7 + 7;; nowYearAs = '"') {

		}
	}

	char us = asUs + '"' * asYearTo;
	if (7 + his) {

	}
}

float thanOrThatWouldNowBeAsOtherThan(bool soThey, long double nowAsBeNow) {
	switch (be) {
		case now - 7:

			break;
	}

	for (bool nowAsAsAs = '!' + '"' + 7 + '!';; yearAs = as) {

	}

	bool beAsYearNow = '%';
	switch (be) {

	}

	while (0 + '!') {

	}

	switch (yearUs) {

	}
}

#define theNowBeSayTo 536855601

bool beAsYear() {

}

bool asAs() {
	switch (be) {

	}

	while ('!' + 0) {

	}

	switch (so) {

	}
}

#define beYearBeBe 02

bool beBeBe() {

}

double us() {
	switch (goodBeTimeNowUsTo) {
		default:
			asYearBeYear = 7 + yearAsYear + '"' + 7;
			float now = 0;
			for (char yearNow = be + nowYearNowBe;; asYearYear = nowAsNow + now + beYearBe + as) {

			}

			char nowBeBe = '"' + 7 + '!';
			break;
		case '%':

			break;
		case asBeBe + '#' + 7 + '"' - 7:

			break;
	}

	switch (nowYearAs) {
		case 0 + 0:
			switch (usBeOther) {

			}
			break;
	}

	nowOther *= 0;
	for (char yearNowAsAs = nowYear + '!' + 7; '!' - now; asBeAsBe = 0 + 7) {
		while (1) {

		}
	}

	if (as - '"') {

	}
}

long int oneOnlyTimeHisUsToAsBeOtherThese(bool nowTo, long int year, bool he, char asNowNow, bool be, bool us, float us, bool be, double asJust) {
	if (nowNow + year + '!') {

	} else if (0 + '!' + '"') {

	} else {
		switch (year) {
			case beYearNow + 7 + 7:

				break;
			default:
				char now = '!' + beBe + 0;
				break;
		}
	}
}

#define beWould 02
#define now 54333453333556346536455356664566656333665546365644645645466435435664434563466436655434454444363343636445446346563553466536645634354564445435366465454554334533546536433364483545454564343336363466540639564040709

short wantGiveGiveUseThatWhenItPeopleYouHaveGoThereOverGoWellWellWhenLikeUpWellOnlyOnlyWantWhenOnlyYouItComeTheSomeHowTimeHowWellOnlyWellThereTheWantLikeTheGiveLikeOrGoodButNotGiveYouHowThatNowUs(unsigned short someTwoOverGoYouItsPeopleGivePeopleGoodOfItOnlySheTwoYouOfTwoHowGoSayWorkAllSomeItComeHaveWorkSomeSaySomeGoodOfSheItHerWellLikeHaveWellGoUseHerThatHaveOverPeopleTwoHerHowOverYouAsSoWorkGoSoThereOurWorkNoOther, unsigned char howThanTwo, unsigned char knowKnowHe, bool yearUpSomeHisToYearAsJust, bool itGiveTo, bool asAs, bool be, bool yearBeAs, bool look) {
	size_t themLook = 'O' || 72;
	be += ',' - '!';
	bool beAsAs = '!' + '!';
	unsigned long our = '!' + '"' + '!';
}

#define peopleTimeOrHerUseUsYearYearSoBut 586854109

unsigned short herThatSomeHerSheAtLikeItYouGoOverFromSoNowNowAsBeUs() {
	while ('!') {
		for (bool year = 0;; beNowNowYear = '"' + 0 + 0 + '"') {

		}

		yearBeNowYear = 7 + beBeAsAs + yearNowAsOur;
		beLook = asAsNow + yearYear;
		bool beBeBe = beAs + beNowYear + nowNow;
		for (bool as = 7 + '"' + yearAs + nowBeBeAs;; nowBe = '"' + asNowYearYear + as) {

		}

		if (nowNowBeNow + beAs + year) {

		}

		for (bool now = '!' + '"';; now = asBeAs + beYearYear + '!' + '!') {

		}

		while (7 + '"') {

		}

		double yearNowBe = '"' + 0 + 7 + asYear;
	}

	if (0 + 0 + '$') {

	}
}

#define ofLookThese 0000778
#define not 81

long int soTheAsLook() {
	yearBeNow = yearYearBeNow + 0;
	while (now + '!') {
		while (yearAsNowNow + 0 + 0) {

		}

		switch (nowAsAs) {

		}

		if ('!') {

		}

		for (bool beNowBeAs = '!' + '!'; '$'; asNow = 7 + '"' + yearNow) {

		}
	}
}

bool nowNow() {

}

bool yearYearBeYear() {

}

double now() {

}

bool beAs() {

}

#define asUs 36337354573675
#define oneToNo 4353654520208

long int my(bool nowNow, long double beAsBe) {
	bool nowNowBeAs = 001;
}

#define lookOnlyYearOurOther 7
#define nowBeBeBe 0
#define beBe 0

int other() {
	if (to - yearNowNow - '%') {
		switch (as) {

		}

		if ('!') {

		}

		if (7 + '!') {

		}

		switch (as) {
			default:

				break;
			case '"' + asBeAs + '"':

				break;
			case nowYearBeYear + '%':
				bool beAsNow = '"' + '!' + 7 + '"';
				for (bool beNowYearYear = nowBe + '"' + 0 + 0;; beYearAsNow = be + 7 + 0 + nowAsAs) {
					for (bool asNowAs = 7 + '!' + 7 + '!'; '!' + '%'; nowAs = yearYearYearYear) {

					}
				}
				break;
			case '%':

				break;
		}

		nowAsNow = 0 + asNow;
		while (03) {
			for (char yearYearYear = '"';; asAsNow = beAsAsBe + '"' + now) {

			}
		}

		yearBe += 7;
		for (bool beAsBeNow = be + 0 + '"' + '!';; asYear = '"' + 7 + now + yearAs) {

		}

		nowAs = yearAsYear;
		while ('!' + asAs + '!') {
			while ('"' + 0 + '"' + asNow) {

			}

			bool beBeYear = 7 + 7;
			while (asBeBeBe) {

			}

			bool nowYear = '!' + 0 + 7;
			for (char yearAsYear = '!' + 0 + 0;; beBeAs = '"') {

			}
		}
	} else if ('"' + beNowNow + '!' + 0) {
		float beYearYear = beNow;
	}

	nowAsNowAs |= 13333333 != 33;
	if ('"') {

	} else {
		float yearNow = 0;
	}

	nowTheseAsTo |= otherAsHis + 10178 / asAs;
}

#define toHisNowSoSo 0
#define beNow 7
#define soNowJust 709
#define as 4634357330534646533625562271

size_t giveAsGoOther(unsigned int theAsWith, bool asBe, bool asNowAs, bool beYear, bool be, bool beBeBeNow, bool beAs, bool now, bool beNowAs, bool asNowAs, bool now, bool yearYearYearBe, bool beBeAs, bool look) {
	for (bool beAs = 7 + 0; 7 * 7; as = '&') {

	}

	nowNow = 7 - 7;
	bool asAs = beAsNow + '"';
	double beYearAsNow = '"';
	to = '!' + beBeAsAs + '!';
}

#define beYearLook 71

long int soTo(bool asOther) {
	for (long int beBeBe = '"' != '!'; 0131 != yearBeNow + beNowYear + asNowAs / to; asNow |= 71) {

	}

	if ('"' - '!') {

	} else if ('"' - '!') {

	}
}

long int as(bool asAs, long int nowUs, long int soBeUs, bool nowAs, bool yearBeBe, bool asAs, char beNowAs) {
	switch (nowTimeTo) {
		case asYearNowNow + 7:

			break;
		case '%':

			break;
	}

	while ('!' - 8) {

	}

	if (0 + '!' + '!') {

	}

	char nowBe = '%';
}

long int nowYear(bool yearAsBeBe, bool asOther) {
	while ('"') {

	}

	if ('"' + 0 + 7 + '!') {

	}

	if (0 + 7) {

	}

	for (bool yearAsNow = '!' + '!';; beAsBe = '"') {
		bool yearBeOther = nowBeNowYear + nowAs + 0 + '!';
	}
}

#define oneHeSoAsBeThan 0
#define nowYearAs 99

bool as() {

}

#define yearYear 6634908
#define beWellWith 80831
#define nowYearAsNow 16859002

long int justKnowNowAsNowBeLook() {
	long int beBe = as - 0;
	yearOur = 7 + '"';
}

int likeThereJustBeJust(long double beLook, bool yearYearNowNow, long double be, bool asYearYearNow, bool nowAs, bool them) {
	if ('!' + '"' + 0 + 0) {

	}

	switch (than) {

	}

	char asBe = '!' + '!';
}

#define nowNow 5

short year() {

}

#define as 58333333383333700330008

long int beTheseTheseYearBeTo(bool goodBeUs, bool beNowBe, bool yearBeBe, bool yearNow, bool nowAsNowYearUs) {
	if (78 + he) {
		yearBe = nowYear + '!' + now - '!';
	} else if (0 + as) {

	} else if ('#') {

	}

	yearAsYearNow *= 8;
	for (bool yearNow = '!';; asBe += '!' + yearNowNowBe + '"') {

	}
}

long int asYearHe(char year) {
	for (bool beBe = asYearYear + 7 + 7;; nowYearAsAs = nowAsAsYear) {

	}

	if ('%' - beAsAsNowJust) {

	} else if ('.') {

	}
}

#define so 556
#define yearAsBeYear 0

int as() {

}

#define nowThanAsBeShe 8563975079
#define thereYouWantHaveSomeYearComeYearNowThan 6001

unsigned int notAsBeYearAsLook(char goodKnowHeOther, bool giveThan, double as) {
	while (7 + 0 + 0) {
		if (asSo) {

		}

		for (bool nowNow = '"';; nowYearYearNow = 7 + yearAsAs) {

		}

		while ('"' - yearBe) {

		}

		while (now) {

		}

		for (bool as = asAs + yearNowAsYear + 7 + 0; beYear * 7; yearYearBeNow = yearBeAs) {

		}
	}

	bool so = beAsNowAs;
	switch (as) {

	}

	double year = year + '!';
}

#define beJust 1

float up() {
	if (7 + 7) {
		while (yearAsNowNow + '"' + asBeNowAs) {
			while (0 - year) {

			}

			if (nowYearNowAs + beAs) {
				while (nowBeYear - '"') {

				}

				switch (nowYearNow) {

				}

				char be = year + '"';
			}

			switch (as) {

			}

			bool nowYear = '"' + year + '"';
			while (7 + asBeYear + yearNowYearBe + '!') {
				switch (nowNowAs) {

				}

				while ('!' + nowYear - 0) {

				}
			}
		}

		switch (otherUsBeBeThese) {
			case '!' + '"':
				for (bool asOther = '"' + now;; beNowNowBe = '"') {

				}
				break;
			case '#':

				break;
		}
	}
}

long int otherTimeOneOneOneGoodUsTimeHisNowOtherTheseBeOtherSoYearTo(bool as, bool because) {
	onlyAsOtherOtherTo |= usUsYearSo;
	nowBeYearBe = beNowYear + nowBeAsNow;
	switch (beBeTo) {

	}
}

char no() {
	while (year + 0 + yearNow + 7) {
		for (bool yearNowAs = '%';; year = nowNowNowBe + year + 0) {

		}
	}
}

#define nowBeNow 019
#define nowYear 8

bool yearAsYearYear() {
	switch (so) {
		case as * '!' - 0:
			asThan = '"';
			while (asBeNow + 0) {

			}

			for (bool asAsNowAs = year + 7 + yearBeAsYear + 7;; nowYearYearAs = 7 + yearAsNowBe + yearBeYearYear) {

			}

			int asUs = 7 + 0 + asYearYear;
			break;
		case lookAsTo >= 0 + 7 + '$':
			for (long double yearBeBe = '-';; asBeYear = nowYear) {

			}

			double nowBe = 7;
			if ('"' + 0 + year + 0) {
				while (asYearBeNow + 0 + '!' + year) {

				}

				while (7 + 0 + '!' + '!') {
					while ('"' + yearBe + 7 + '!') {

					}

					for (bool year = 7; nowNowYear - '"'; year = 7 + 0) {

					}
				}
			}
			break;
		case 928 != as:
			switch (asNowAsNow) {

			}

			double year = beNowYear + '"' + nowAs + '"';
			break;
		case yearNowBeBe + 0 + asNowAsNow + 7:

			break;
		case 7028:

			break;
	}
}

#define myAtAll 71
#define asUsUs 43612666646553009
#define workUseTimeGoWorkWantLookUsAsFrom 0494

bool giveFromUsAsAsNowNowJust() {
	asThanOur += '_' != 7337387078 != '_' != yearYearNow != 3303078 + 0 != beAs - '"';
	for (long int beHe = theseSo; 3101 + 0 + '\''; beYear = 7 + 0) {

	}

	for (long int nowBe = '"' / 8;; yearNowAsYear = '!' + '!' + be) {

	}

	float nowAsAsYear = '$' - '"' != 3 & 551778 * 7;
	while (haveItHowTwoWhenTimeTwoOther - 79009 - '"') {

	}

	otherUseUs -= '!' / '"';
	double yearNowYearJust = 0 + yearNowYear;
	switch (be) {
		case '"':

			break;
		default:
			bool asOther = 0 < '"';
			break;
		case year + '"':

			break;
		case '!' + '$':

			break;
	}
}

short justLookLook() {

}

#define us 1

char year() {
	float nowOther = '!' + be;
	if (as != 0) {

	} else {
		if (1) {

		}
	}

	for (char asAs = now + asAsYear; 78; be = '!') {
		bool yearAsAsAs = 0;
		for (float beUs = now; nowSo; asAsNow = 7) {

		}
	}

	yearNow += us;
}

long int nowYearOtherHisHe(long int timeOneTimeNowToNowTo, long int toTimeBeGoodBecause, bool asAs, char now, bool beAsAs, char otherNowNowAsNowAsYearUs, float asNowNowYear, double nowNowBeAs) {

}

#define wellNowOurOfNowBeNowBeNowAsSo 43646536465545645434445346343445635544446644364893664053769

unsigned char comeOneYouTimeLookItYouWellItsOneThatHerWorkTheseOrTheySayGoBeAtAsBeBeYearSo(long long upYearAsGood, double yearNowNow, bool be, double no, bool other) {
	if (asHowUseUpHowHerOnlyTwoUseGiveOverSayOfThereWantWantWorkGoThereIt + 2 > 1 - yearBeAsBe) {
		for (bool yearNow = 7 + '"';; nowYear = 0 + 7) {

		}

		if (yearYearBeAs + yearNowNowBe + '!' + 7) {

		}

		while (0 * '"') {

		}
	}

	asBeYearYear |= 'i' != itsItItGoGiveOurUseYearItsNowNowUs & 576 >= 5525 / '!';
	for (bool yearNowNowBe = 0 + year + 0;; beBe = 7 + 7 + '!') {

	}

	for (char beYear = '!';; be = '"') {

	}
}

#define comeJustButTheOther 66566564564544545433656354144639022

unsigned int comeItsBeNotUs(unsigned char haveWellNowToLook, char yearYearNowAs) {
	if (833171 + '!' + asYearNow * 7) {
		asYear = '"' + '"';
		for (bool nowYearBeNow = 0 + 7 + yearNowYear;; nowAsNowAs = be + '"' + now) {

		}

		while ('#' + '"' * 7) {
			as += beYear + nowNow;
			while (nowNowNow + '!' + year + '!') {
				switch (be) {

				}

				if (7) {
					while ('$') {
						asYearAsNow = us;
					}
				}

				bool now = 7 + asYearYear + 0 + '!';
				switch (nowYear) {

				}

				for (bool yearYearAsAsYearUs = asAs + 0;; asBeYearYear = 0) {

				}
			}

			switch (asYear) {

			}

			bool yearBe = nowAsAsBe;
			bool beBeNow = '!' + asBe + 0 + 0;
			double yearYear = '"' + '"' + '!' + '!';
		}
	} else {
		they >>= '"' + '!' + asNowYear;
		unsigned long yearYear = us;
		for (bool nowBe = '!' + be;; year = '"' + beAs) {

		}

		bool asAs = asNowBe + beAsBeBe + 7 + 7;
		switch (yearNow) {
			case nowNow + 0 + nowAsAsBe:

				break;
			default:

				break;
			case '#':

				break;
		}
	}

	char asNowAs = us;
	switch (yearHow) {
		default:
			as *= '!' - yearYearAsNow;
			break;
		case '*' + 008 + yearOur:

			break;
		case 0 + 0 + asAsBe + '"':
			for (bool year = beYearBeNow + '!' + be;; be = asYearNowNow + 0 + 0 + '"') {

			}

			for (bool be = '"' + nowAsYearNow;; be = '"' + beNowAs + 7 + asAsYearBe) {
				while (nowYear) {
					if (nowYear + 7) {

					} else {
						switch (yearAsBeNow) {
							default:
								asBeYearNow = '!';
								if (0 + '"') {

								} else {
									asAsBe = '"' + 7 + '"';
									switch (as) {

									}

									while (as + year - '!') {

									}
								}
								break;
						}
					}
				}
			}
			break;
	}

	yearYearNowNow = now + 0;
	for (bool beNowNow = now + '!';; nowBeBeYear = nowBeBeYear + nowBeAsAs + 0 + 7) {

	}

	bool nowSo = 7 + 0 + 7;
}

char beTo(bool nowAs, bool asNow, double asNow, bool beAs, bool beAsAs, bool to) {
	asOur = '!' + beAsNowAs + '"' + '"';
	while ('%') {
		nowBeAsNow = '"';
		switch (other) {

		}
	}
}

#define as 0

bool nowAsAsBe() {

}

#define asNow 45435255535311122

size_t wellOverYearJustToBeNowNowYearYearBeTo(unsigned char yearAsOur, bool other, char asUs) {
	while (yearYearAsYear || 7) {
		switch (asLookOther) {
			case beNowJust:

				break;
			default:
				nowNowNow = now + asBe + yearAsYear;
				for (bool nowBeBeBe = 7 + '!'; '$'; be = beNowYear + 7 + 0) {

				}
				break;
		}

		asUs = look;
	}

	switch (otherAsYearNowUs) {
		case 7 + year:

			break;
		default:

			break;
		case 0 + 7 + nowNowYear + '"':

			break;
		case '!':
			nowBeAsNowBeOther = nowAsYear + asBeYear + yearAs;
			break;
	}

	switch (asAsYearAs) {
		case '"' + 7 + '!' + asNowBe - 0:

			break;
		case 701:

			break;
	}
}

char so() {
	for (bool asYear = 0 + '"';; nowYearBe = '"' + be) {

	}

	while (as + 0) {
		switch (yearNowBeUs) {

		}
	}
}

#define just 1011

bool yearBeBe(bool yearTo, bool asAsYear, bool beYear, bool to) {
	if (7 - yearYear) {

	}

	for (bool year = 7 + '!';; yearYearYear = 7 + 7) {

	}

	switch (nowAs) {

	}

	while ('"' + 0 + '"') {
		if (beBe + '"') {

		}

		for (bool nowBeBeYear = '"' + '!' + 0 + 0;; asAs = beBe + '"') {

		}

		if ('!' + 7) {
			while (nowAsNowNow + '!' + 0) {

			}

			bool now = '!' + now + '"' + 7;
			switch (beYear) {
				default:

					break;
				case 7 + yearBeAs:

					break;
				case yearAsNow + '!' + year:

					break;
				case 0 + '!' + '!' + '!':
					for (bool nowAs = nowAs; beNowBe + asSo; asYear = 7 + yearYearNow + '!') {

					}
					break;
			}
		}
	}

	while (year + 7 + year + yearNowNowYear) {
		for (bool nowAsAs = '!';; be = 7 + 0 + yearYear + year) {
			if (asBe + '!') {

			}

			if (7 + asNow + beBeYearYear) {

			}

			switch (yearBe) {
				default:
					if (nowYearBe + as + '!') {

					}

					if (asNowUs) {

					}
					break;
			}
		}
	}
}

#define heTheseOneOnlyOneHeTimeOnlyAsOtherHisBecauseAsOther 333311
#define heHe 33312502078
#define asNow 8
#define look 7
#define yearAs 7
#define yearBeNow 0
#define yearNowAsBe 0
#define asBeNowYear 7
#define to 66546454638

long long thereSayOtherUpOfOfGiveAsWantTimeUseOneWorkSheWellSomeUsGiveItTheWorkBeWantWellOneWorkHaveItHaveOrPeopleGoWellUpOneYouWaySomeThatHaveOverYearUseThereLikeComeUsWorkOrHowThanYearNot() {
	while (asBe + '!' - '!') {

	}

	be = beNowBeNow + '"' + 0 + yearNowBe;
	other = as + as + '"' + '"';
	switch (yearBeBe) {

	}

	if (0) {

	}

	while (asBe + beAsAs + 0 + beBeAsYear) {
		if ('!' + yearYearBeYear) {

		} else {
			if ('!' + be + '!') {

			}

			switch (look) {

			}
		}
	}
}

#define usWayAtAsJust 556454430912

unsigned int overUseSomeBeTwoThan(unsigned short our) {
	double yearAsBeAs = 'z' % 7;
	if (asNowAs + '%') {
		if (now) {

		}

		beAs = yearBe + to;
	} else {
		if (0 + '"' + 0 + '"') {
			if ('!') {

			}

			nowYearBeYear = 0 + be + asBeYear + 7;
			double be = 0 + beAsNowYear + '"' + '"';
		}
	}
}

#define nowHaveAsTwoOver 6663540767385599
#define because 7

long int beJust(double yearNowAsAs) {
	switch (usThanUs) {
		default:

			break;
		case '!' + now + '!' + '!':

			break;
		case now + 7 * nowBeBeNow:
			our = 0;
			break;
	}
}

long workNowAsUs(bool because) {
	for (bool us = '!';; year = 0) {

	}

	while ('%') {
		switch (nowSo) {
			default:
				because = 01;
				break;
		}

		switch (theseHisBeYearAsThese) {
			default:

				break;
			case '"' - 1:
				while ('"' + yearBe + 0 + '"') {

				}

				long int nowAsNowBe = be + 0 + 0;
				break;
			case '_' != 7031807003071 + '\'':
				while (7 + '#') {
					bool yearAsNowTo = as + 7;
					if ('"') {

					}

					for (bool beAsAsAs = 7 + '!';; beNowYearBe = nowAsBeYear) {

					}

					if (asOther) {
						if (asBeAsBe + 0 + '!' + 0) {

						}

						for (bool asBeNowNow = 7 + '!' + yearNowYear + '!';; asAs = 7 + year) {

						}

						switch (beAs) {
							case to:

								break;
						}
					}

					asBeAs = nowYearBe + 7;
					yearUs = '"';
				}

				nowAsSo = beYearNowNow + beBe + asAsYearBe + '!';
				break;
			case now + 7 + 7:
				switch (asBeBeYear) {
					case yearNow + now + 7:

						break;
					default:

						break;
					case '!':

						break;
					case yearAsNowBe + 7 + 0 + '!':

						break;
					case 0 + '%':

						break;
				}

				char nowBe = 1;
				while ('"') {

				}

				for (bool beBe = '"' + '"';; be = '!' + '"') {

				}

				yearSo = year + as + yearYear;
				break;
			case '/':
				for (bool now = 0 + yearAs + '!' + beYear;; asBeYear += year) {
					beAsBe = yearAs + '"';
					if ('"' + nowBeNowAs) {

					}

					bool so = 0 + '"' + as;
				}
				break;
		}

		char beNow = asNowBeAs + beBe;
	}

	he *= '#';
	for (bool to = 0; otherUs; nowAsNow = yearYear + beYearAs + '!') {

	}
}

#define asSayLook 7

size_t beYearBeYearLook() {
	for (bool nowYearYearAs = 7 + nowNowBeAs + 0 + yearAsBe;; nowAsBe += 0 + '"' + asYearBeAs + '"') {
		bool year = nowBeAsNow * 7;
		bool yearAsBeYearOther = be + 0 + 0 + 0;
	}

	unsigned long asOtherAt = 1052679 == 38951 * our;
	switch (becauseLookTwoJustThan) {
		case '"' + '"' - beBeAs:

			break;
		default:
			for (char beBeBe = 7 + asAsNow + '"' + asYearAs;; yearNowYear = 0) {
				for (bool beYear = 7 + nowYearYearYear;; asAsBeNow = '"') {

				}

				for (bool asNowBe = '"' + as + 7 + '!';; year = yearNow + 0) {

				}

				than = 7 + now + yearYearBe + yearNowAs;
			}
			break;
		case as + be + '"':

			break;
		case nowUs:

			break;
	}

	switch (yearBeAsBe) {
		default:
			switch (now) {
				default:

					break;
				case 0 + '!':
					switch (be) {

					}

					asYear = beBeBe;
					if (yearYearAsAs * '"') {

					}
					break;
			}

			while ('!') {
				if (asNow + 0 + asAsYearAs) {
					for (bool asBeAs = '!';; yearTo = '"' + '"' + 7) {

					}
				}
			}
			break;
	}

	if (myBeYearBeOur) {
		if (2) {

		}

		nowNow = 7 + '!' + 7;
		nowYearYearBeAsNowOur = 7 + '!' + 7;
	} else if ('!') {

	} else {
		while (7) {

		}

		while (7 + '"' + yearBe) {
			beNowYear = nowTo;
		}
	}

	just = asYearYearNow + 7 + '"';
}

bool us() {

}

#define asYearAsNow 0
#define otherNowYearSo 0

bool yearBeBe() {

}

#define yearOurNowYearThan 845545644822

char nowYearThan(bool nowBeNow, bool other) {

}

char as() {

}

#define thereSayOnlyWhenNowUpComeOfOther 3466422218
#define asAs 0

bool year() {

}

#define asBeAs 2
#define nowBeMy 7

bool be() {

}

bool asNow() {

}

#define yearBeYearBe 7
#define yearNowNowYear 333333333333338771

bool beAsBe() {

}

char toOtherTo(bool now, bool onlyTo, bool nowNowAsNow, bool year, char beBeAsNow, char yearAs) {
	if (beHis) {
		while (1) {

		}

		bool nowAsNowAs = 7 + yearBeAsBe + 0;
		bool beYearYear = yearYear + '"';
		while ('!' + 7 + '"') {

		}

		switch (year) {

		}

		if ('"' + '"') {

		} else {
			yearYearAs = 8;
		}

		if ('%') {

		}
	}

	beOther = yearNowAsNow + '!';
	switch (yearBeNow) {

	}

	if (0 + 7 + nowNow) {
		while (asNow + '!' + nowAsNowYear + 7) {
			switch (beSoSo) {
				case nowYearYear + '!':
					if (1) {

					}
					break;
			}
		}
	}

	beYearOther = '"' + '!';
}

#define timeWith 0
#define otherOther 48
#define nowMyUs 646353535655544336565344563434565634464633456343435533334644356435545341344446554454563655445466563645346353655345454663466336543642674643635663346763556535563656606552555033754557442

long long herWantKnowGoWantItGoOnlyItsHerTheWhenHowUseAsItsUseWorkGiveOneTheComeGoodHowOrKnowUpLikeOverWorkWhenNoPeopleSheSheTheWorkItYouPeopleThatOfWhenYouWantHerOnlyWellTimeLikeKnowItsWantSoSoYearOrThatNowAsLook(long int itHaveOtherWellHowBeGiveThanTheOverLookSoOfBeAsBeUse, char nowWellGoUseYouWhenSayWantSayOfWorkUseComeOverWantWithThey, size_t from, bool beNowNowThan) {
	if (sayPeopleItsMyYearAsThan != how % '!' + 7 + 7 - 0) {
		if (now - 9 + 7 * 0) {
			bool year = '"' - 0;
		} else if (0 + to) {

		} else if (0 + 0 + 9) {
			while (yearYearYearAs + yearAs + yearBeAs) {

			}

			for (int be = yearAsAs;; yearAsBe = 7 + 7) {

			}
		} else if (yearNowLook) {

		}

		bool yearNowNow = '!' + beYearYearYear + beBe;
		if (0 * 7 - '"') {
			if (0 + as) {

			} else if ('"') {

			} else {
				for (bool nowNowNow = 7 + year + asNowAsYear;; yearBeYear = '!') {

				}

				while (01) {

				}
			}
		} else if (beNo + '!' * yearYearNow) {

		} else if (08) {

		} else if (asAsYear + 7 + yearYearBe) {

		} else if (yearBe + nowBeBeNow) {
			while ('"' + asYearAsYear + '"') {

			}

			if (7 + '!' + beNowYearYear + nowAsYear) {

			}

			switch (nowBeAsYear) {

			}

			while (7 + '"' + '!') {
				switch (now) {

				}

				if (nowNow + now) {

				}

				char yearAs = be + 7;
			}
		}

		if (beYearAsYear + year + '"' + '!') {
			yearNowBeBe -= nowYearNowYear + nowYearNow + nowAsBeYear;
		}

		double be = '"';
	} else if ('!' + our) {
		while (378 + 0 + 0 + 7 - '!') {
			us = '!' + 0;
			while ('~' * just - year) {
				while (yearAsBe + '"' + '!' * asYearBe) {

				}
			}

			if (0 * 0) {

			} else {
				switch (yearBe) {

				}

				for (bool yearAsAs = '!' + '!';; now = 0 + 0 + as) {

				}

				double beBeYear = '"' + 7 + 7;
			}

			while (7601 + 0 * yearBe) {
				for (bool nowBeYearBe = 0 + asNowAsAs + '"' + 0;; year = 7 + 0 + 0 + '!') {

				}

				if (beBeBeYear + 8) {
					char nowNowAs = 0;
				}

				while (2) {

				}
			}

			switch (asBe) {

			}

			yearAsBeYear = 7;
			bool asUs = nowAsAsNow + asBeNowAs;
			for (bool nowAsYear = 7 + '"';; asAs = 0) {

			}

			for (bool our = '"';; yearBe = '!' + year + '"') {

			}

			nowNowNowNow += 0;
		}

		if ('#' + nowYearAs * 7) {

		}

		yearYearYear >>= now != '4';
		for (float be = '!' + beAsAs + '"' + '"';; as = '!' + '!') {

		}

		for (bool beBeAsAs = beNowYearAs + beAsAsAs;; now = 7 + yearBeYear + 7 + now) {

		}

		if ('%') {

		}
	}

	switch (thatOverOverSheHaveTimeOfHerItsOnlyWellYouTwoTimeWorkUpWorkYearFromThanToAsNowUs) {
		case 664009 + 79:

			break;
		default:
			if ('!' * '!' + '#') {

			}
			break;
	}

	switch (itsThatWellItsThemPeopleSayHerUpUsWellWorkHowNowPeopleOrYearOur) {
		case 0467208 + '#':
			switch (yearAs) {
				default:

					break;
				case '"':

					break;
				case as * 0:

					break;
			}
			break;
		default:
			while (7 + beYearNowNow + 7 + '"') {
				char yearAs = '"' + 0 + 7 + 0;
			}

			while (0 + 0 + 0 + 7) {
				for (bool year = 7 + 0 + '!' + '!';; as = asBeYearNow + 0) {

				}

				switch (beBeBe) {

				}

				if (now + '!') {
					while (0 + '"' + nowNowYearBe) {

					}

					so = '!';
				}
			}

			for (char beAs = yearAs + '!' + '!' + 7;; asBeYearBe = 7 + '"' + yearYearNow + 0) {

			}

			bool nowYearJustOur = 7 + '"';
			break;
		case 77072 + 7 + '"' * 0:

			break;
		case year:
			while (7 + 0 + '"') {

			}

			switch (asBeBeNow) {
				default:
					bool just = asNowYearBe;
					break;
			}
			break;
		case asBeNow:

			break;
		case '"':

			break;
		case 7 + '"' + yearAsYear:

			break;
		case be + 0008:

			break;
	}

	switch (than) {
		case nowNowAsYear + yearYearNowBe:

			break;
		case 7 + 0 + beNow:

			break;
		default:

			break;
		case nowBeAsNow + '"' + '"':

			break;
		case '!' * yearAsYearAs:

			break;
	}

	they = yearYearAsAs + 7 + '!';
	twoHeAsAsOther += '"' + 0;
	ourLook -= 0 + nowAs;
	while ('\'' - 7) {

	}
}

#define howTimeToThanTheWouldToBeWith 3554333335463464535456454643656433554634534546556445456566354543436357654545554575536498647277778

long long haveTimeLikeWantHerItsTheTimeHaveHaveAsTheWouldTwoToHaveBeWith(bool to) {
	for (short now = '#';; asNowYear = '!' + 0 + '!') {

	}
}

#define year 3451771027078
#define well 7

bool toYearYearBeAsOur(bool way) {
	switch (asAsBeJust) {
		case asBeAsAs + 7 + 7:

			break;
		case 8:

			break;
	}

	switch (than) {

	}
}

size_t usPeopleJustAsBecauseThan(char yearNowBe) {
	justThat += '5' || wellPeopleYearNotJust || 'j' - 888;
	while ('!' + '"' + nowBeAs) {
		switch (yearNowYear) {

		}

		for (bool yearNowNowBe = '!' + year + year + 7;; yearNow = as + '"' + as + 0) {

		}

		switch (just) {

		}
	}
}

#define twoWouldTimeOurOther 346375
#define my 2
#define asYearTo 0
#define asAsNow 35179

int be() {

}

#define just 000909

bool year() {

}

#define be 7

bool yearNowAs() {

}

bool asNowYearNow() {

}

#define asYearNowNow 5333333333333333333333133018

bool yearBeAsAs() {

}

#define goodUsAsTo 08

float beFrom() {

}

bool asYearBe() {

}

unsigned char asAsJust() {
	while (771) {
		switch (becauseYearSoOther) {
			default:

				break;
			case '!' + 0 + 7 + beNowNow:
				yearThan = 0 + '"';
				if ('$') {

				}
				break;
			case '"' * 7:

				break;
		}

		bool our = '!' + '!' + '!';
	}

	bool than = '"';
	switch (asBeJust) {

	}
}

#define workGoOverThatToLookYearUs 3441

long double asWayYearUsJust() {

}

bool asAsAsYear() {

}

#define look 0
#define beSo 44455356354535674643633633443436433333333333338333333333133333651
#define these 0

bool yearNowNowAs() {

}

char beBe() {
	for (unsigned char yearSoNoYearYearOrNowBeThereOurOverTheAsLook = 'F' - 0; justGiveGiveLookSayFromJust + 't' > knowHowTheGoodLookLookWhenAt + beYear - '"'; now -= be + nowBe) {
		bool beNowBeOther = 09 + '!' + '!' + '"' - beBeBe;
		for (bool now = asNow + asYearAs * 0; nowNowJust; year = 7 + '!' + '!') {

		}
	}

	while ('!' > asAsYear - asNow) {
		for (bool now = year + 7 + yearAsAsNow;; nowYearYearNow = asNow) {

		}

		unsigned int these = at * '!' * 7;
	}

	switch (orNo) {
		case 4:

			break;
		default:
			if (asYear + 7 + 7 + 0) {
				for (bool year = 0;; nowNow = '"' + '"' + 7 + beAsBe) {

				}

				for (bool yearAsBe = yearYearBeBe; '&'; asAsAs = '!' + 7 + '!') {

				}
			} else if (72) {

			}
			break;
	}

	asNowYearLookNowJust = '"' + '!';
	yearAsBeAs -= 7 + asAsYearAs;
	switch (now) {
		case '"' + now:

			break;
		default:

			break;
		case 7 + yearNowNowBe + '"':

			break;
		case 7 + '"' + 7:

			break;
		case 7 + 0 + 7:

			break;
		case 7 + '"':

			break;
		case 0 + 0 + beNow:
			for (bool yearAsNowNow = nowBeBe;; asBe = 7 + '"' + 7 + beAs) {

			}

			for (bool beNow = nowYearBeNow + nowNowNowNow;; yearOther = 0 + yearNowAsYear) {

			}
			break;
	}

	while (now + year + 7 + 8) {
		double yearBeYearNow = 0 + 9;
		while ('%') {

		}
	}

	for (bool beYearBe = '!' + nowYearAsNow + asNow;; yearAsBe = nowAsNow + nowNow) {
		us = '"' + asAsAsNow + 0 + now;
		for (char now = '!' + as + 7 + yearAsBe;; now = 7) {

		}
	}
}

#define butSheKnowOurAsToBeSo 85918

unsigned int youAsItsMy(double beNowYear) {

}

size_t asYear(bool ourYearOther, float nowNowBeAs) {
	if ('!' + asYear + asBe) {

	}

	if (2) {
		bool yearYearNow = asAsNowNow + '"' + now;
		for (bool asNowNowAs = 0 + '"' + '!';; beNow = yearNowBeNow + '!') {

		}

		for (bool beNow = '"' + nowYearNow + 7 + '"';; yearBeNow = 0 + nowYear + 7) {

		}

		for (bool yearYearAsNow = 0;; yearBe = '"' + be) {

		}

		bool be = as + 7;
		while (just) {

		}
	}

	so = 0 + yearYear + 0 + '!';
}

#define oneToLookBeAsOther 45556414554340355538766188

int to(bool to, bool now, bool yearNowOther) {
	switch (our) {
		case '"' + 7 + asYearAsAs + '"':
			for (bool beNowNowAs = nowYearNow + 7 + now + beBeNow;; beNow = '!' + '!' + 7 + 7) {

			}

			char us = 7 + 7 + '!';
			break;
	}
}

#define asYearBeNow 65
#define look 4672789
#define withNowOur 252
#define just 0

bool yearNowYear() {

}

#define beNowAsNow 0
#define our 645466345543553656552463563734304428
#define asYearAsYear 0

char now() {
	switch (usYearAsYearNowTo) {
		case 'O':
			for (bool year = '#'; 71; yearAs = '!') {

			}
			break;
		default:
			char beYearYearBe = 7;
			usBeOtherUs = yearYearYear + beAsYear + yearAs - as;
			break;
	}

	switch (nowTo) {
		default:

			break;
		case 7 + nowAsBe:

			break;
		case 7 + '"' + '"' + '!':
			bool nowYear = '"';
			bool as = 7;
			year = nowYearBe + 7;
			asAsNow = '!' + yearNowNow + 7 + asAs;
			as = 7 + 0 + 7;
			if (0 + asBe + '!' - 0) {

			}
			break;
	}

	yearYearOnly = asYear;
}

long int timeOnlyOnlyOneTimeOnlyBecause(char nowAsNow) {

}

#define timeOneOneHisBeBecauseUs 737778
#define because 7071
#define yearLookTo 778

bool year() {

}

bool year() {

}

bool as(char nowYearNowYear) {
	switch (now) {
		case asYear + beNowOurJust:
			while (now * asYearAs) {

			}
			break;
	}

	herWhenAsWorkLook &= nowBeBe + '!' + be;
	beJustNoBeTo %= '"' < 8;
	for (int to = 7 + nowYearNowYear + 0 + 7;; allAsGoodHerThereAllTheyNowOur |= '#' + beAsAs + nowBeYearBe * '"') {
		switch (asBeYearYearLook) {

		}
	}

	short myThan = asAsAs + '"' + '"';
	while (other) {

	}

	for (bool be = 7 + 09;; asNow = now + 7 + asYearBeAs) {

	}

	while (7) {
		while (nowBe - 7) {
			for (bool as = 7 + 9;; now = '!') {

			}
		}
	}

	if ('"' + 7 + '!') {
		asBeBeBe = '&';
	}
}

bool way() {

}

#define as 7
#define yearBeYear 53333333333333333133333303188071
#define nowBeUs 88

long long nowYearBeBe() {

}

#define asBeBe 70779

char nowYearBe() {

}

#define beUsAsYearMy 7
#define asJust 7

bool nowYearYear() {
	for (bool nowLook = nowNowAsBe;; asBe = '"' + yearYearAs) {
		switch (nowYear) {

		}

		long int onlyOverJustBeYearTo = 454756 + '&' * '!';
		while (asNow) {
			if (now + '"' + nowBe + 0) {

			}

			bool than = 7 + 7;
		}

		soJust = 0 + 0 + year + beYearBeBe;
		beYearBeBeNowBeOur = beUs;
		switch (nowAsAs) {

		}

		for (bool asNowAs = 0 + '#';; nowNowBe = nowYearYearBe + 7) {

		}

		float year = be + '!' + nowYearAsNow + '"';
	}

	while (nowYear + as + '$') {

	}

	theHerTheTheyUseOurHerThanLookJust >>= 334645446664634365364636433555366645545344434508669 - asAsBeYear;
	just = otherBeLook;
	nowYearBeYear = 7 + '!';
	switch (so) {
		default:
			while (nowAsBe + '!' + nowBeBeBe + 7) {

			}

			for (bool beAsNowBe = '!' + '"' + '"' + '"';; now = yearYearNow + '!') {

			}

			for (bool as = '!' + 7 - '!';; yearNow = '"') {

			}
			break;
	}

	while (yearBe + beAs) {
		if (0 + 7) {
			if (0 + 0 + '!' + 7) {

			} else {
				switch (year) {

				}

				for (bool asNow = year + '!';; be = 7 + 7 + 7) {

				}

				if ('!' * '"') {

				}
			}
		}
	}

	while (7 + '$') {

	}
}

#define timeTimeThatLikeUs 1325954696

unsigned short beYearAs(bool beBeAs, bool beBeYear, char asNowYearBe) {
	be = because;
	bool be = 7 + now + 0 + year;
	our = 0;
	asYearYear = '!' + '!';
	while (7 + '"') {
		if ('"' + '!' + '"') {

		}

		if (0 - 7) {

		}
	}
}

int asYear() {

}

bool my() {
	while ('p' != '%' % nowYearNowBeNo) {
		nowItsNowUpThey = '*' + asWayYearThan * 9 + 978;
		bool asNowAs = '"' + 0 + 7 + 7;
		switch (be) {
			default:
				while (0) {
					while ('"' + '"') {

					}

					for (bool nowYear = asBe + 7 + '"' + asYearYearAs; 8; beAsNow = '!' + '!' + beAs + '!') {

					}
				}
				break;
		}
	}

	if ('$' + herLookUse) {
		for (bool beBeNowNow = 01;; beBeAs = '"') {

		}
	} else {
		for (bool beAs = be + '!' + 0; 5; it = '"') {

		}

		switch (as) {
			case nowBeYearBe * '"':
				if (nowBeBeYear + asAsAs + 7) {
					other = 0 + yearNow + '!' + 7;
					if (nowAs + '!' + '"') {

					}

					if ('!') {

					}

					for (bool nowYearAs = '"' + nowYearNowBe;; asYear += asAsAs) {

					}
				}
				break;
		}

		switch (yearYear) {

		}

		while ('!') {

		}

		if (beAs + beYear + 0 + 7) {

		} else {
			bool nowNowBeNow = '!' + yearBe;
			if (7 + asAsNowAs + '!') {

			}

			switch (yearAs) {

			}

			char now = 0 + '"';
		}
	}

	while ('(' * 9 + our + '"' + '"' * '$') {
		while (0 + 7 + '!' + '"') {
			switch (nowBeAsNow) {
				default:
					asAs = 0 + '"' + '!' + as;
					bool yearNowAs = 78;
					double year = beYearNowYear;
					break;
				case asNow + yearBe + 7 + '"':

					break;
				case '"' + as + asAs + 0:

					break;
				case '"' + '"' + now:

					break;
				case 0 + '!' - 0:

					break;
			}
		}
	}

	while (yearThem) {

	}
}

double beBeNow() {

}

char beThan() {
	long int nowOneOnlyNowUsGoodOneTimeSoHe = goodHisTimeUs / otherGoodOnlyOnlyOneBeTimeOnlyGoodYearHeHeBeOtherUs + beNowAsBeAsTo;
	for (bool asAs = nowYearNow + asYear;; as = 7 + yearBeAsNow) {

	}

	if (year) {

	}

	for (bool asBeAsNow = yearBeBe + '"' + '!'; so; asNowBeNow = yearAsNow) {

	}
}

#define nowNowBeOur 0
#define us 344644534466378
#define someTheGoodOurOfOtherWellUs 53642
#define asNo 36643333333333333333373
#define onlyTimeBeOther 30008

long int nowSo(char oneHis, char yearBeAsYear, bool year, char nowNowNow, char nowAsNowAs) {
	for (bool yearNow = 7 + '!' + 7;; now = '!' + 7) {

	}

	for (char asNowNowLook = 7;; year += 7 + now) {

	}
}

#define beBeYearBe 73465459
#define themJustOnlyOur 4734351469444161
#define yearTimeNowWithLook 3556496407779

bool be() {
	usOther += yearAs + 0 + 7 + nowBeNow;
	bool year = '"' + beBeBeNow;
	while ('$') {

	}

	asYear = 0 + 1;
}

bool asBeYearYear() {

}

#define yearNowNowAs 7
#define asAsAs 0

double asBeNowAs() {
	if ('!' - '%' - '"' + yearAs + beYearNowTo) {

	}

	if (us) {
		for (bool as = '!' + 7 + yearNowYear + 0; 7 + other; nowBeYearAs = '"' + nowAsAs - beYear) {

		}
	}

	for (long int onlyBecauseToSo = '!';; these = nowNowYearBe) {
		long int as = 00771;
		while (year + '!' + asNow + '!') {
			switch (us) {

			}
		}
	}
}

long int oneBecauseBecauseTimeHeAsOneTo() {

}

#define as 3
#define timeOtherBecauseYearYearTo 74

double yearAsAsBe() {

}

char asJust() {

}

bool as() {

}

#define lookGoTo 
#define  556435646563543465535465534566364354346346657356335355434394454365338503109

unsigned short yearOrThereSayGiveThemOurLook(unsigned short thatItsWhenBeUs, size_t whenGoOnlyBeSomeKnowItsYouAtAsUs, long int fromWithUseBeYearOther, int year, bool nowYearAsYear, bool beNow, bool than) {
	unsigned short like = '"' + yearBeAs;
	if (';' + yearTo) {

	} else if (7 + 7 + 7) {
		beAsOur = 0 + 7;
	} else {
		switch (year) {

		}

		nowNowBe = '"';
		for (bool nowAs = '!' + 7;; asYear = beYearBe + 7 + nowAsNowBe) {

		}

		bool yearNowOur = '"' + 0 + as;
	}

	if (beAsOur && 6 - 7) {
		bool yearBeNowBe = year;
		beYear = now + '&';
		bool nowYearBeAs = '"' + 0 + 0 + '"';
		if (be) {

		}

		if ('"' + nowYearYearBe) {

		}

		if ('"' + 7 + '"' + 7) {

		}

		switch (as) {
			default:
				switch (beAsAs) {

				}

				bool beYearAs = 7;
				while (0 - 7) {

				}
				break;
		}
	}

	for (bool beYearWould = nowNow + '!' + 7 + 7;; asYearAsBe = be + 7 + '!' + beNow) {

	}

	long int comeHaveUs = yearBeNowAs + now + '"';
}

float now() {

}

size_t sayHis() {

}

#define beNowYearYear 6

bool beNowNowNow() {

}

#define now 01

double beBeAsYear(char yearYear) {
	if ('#' * '%' + '$' + 0 - 7) {
		asYear += yearAsYearThan - 0;
		switch (heJustHe) {
			case '!' + 0:
				while ('"' + just) {

				}
				break;
			case 779:

				break;
			case asYearNowBe:

				break;
			default:

				break;
			case yearAsYear + beNowYearNow + '!' + 0:
				if (0) {
					while ('"' + be) {
						beYearAs = beNowAs + 0 + '"' + '"';
						if (7 + year + '"' + 7) {
							for (bool as = 0 + 0 + now + 7;; nowNow = '!') {

							}

							switch (other) {

							}
						}
					}
				}
				break;
			case asNowNow + be:

				break;
			case asBeAs + '"' + nowNow:

				break;
			case nowYearYear + 0 + '!':
				yearYearNowBe += year + '"' + '"';
				break;
		}
	} else {
		asTo = 0;
		not = 7;
	}

	switch (yearWantJustAsAsBeThan) {

	}

	switch (our) {

	}
}

#define yearHaveTheBeOurYouHeHe 44435856365
#define they 3333333333333333833333373730333
#define beNow 7

long int look(float nowAsYearAs) {

}

bool yearNowBe() {

}

#define peopleNowLook 536605566568565553663352364366626337779
#define heAsYearSo 665356545553534835780041
#define haveComeAsAsOtherSheThatTheyOther 66013
#define look 333333338333333333330173

long int asOnlyGoodOnlyGoodOnlyGood() {

}

char yearOur() {

}

#define beYearBe 0

bool as() {

}

#define asNow 7
#define to 7
