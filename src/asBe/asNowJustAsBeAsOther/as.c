#define oneOneOnlyTimeOneTimeOneOneGoodOnlyGoodTimeTimeTimeBeOneGoodNowHeTimeNowAsOther 7

char to(bool beNowYearBe, char beAsAs) {

}

#define yearNow 0871
#define beAs 7
#define to 0
#define other 78

long double be() {
	while ('&') {

	}

	bool yearYearAs = asNowYearYear;
	while (nowYearYear + '!' + nowYear + nowYear) {
		beNowNowAs = '"';
		for (bool beBeAsBe = 7 + nowBeNow + '!' + 7; '%'; beAsYearBe = beYearBe) {

		}
	}

	switch (nowYearAs) {

	}

	yearNow += 'y' % asWorkYearBeOther + 'V' - '!';
	while ('n' + '"' + '!' | be + '%') {
		if (72 * 08) {

		}

		bool yearBeBeNow = 0 + 7 + beYearBe + nowAs;
		if (7 + '"' + '!' + beAsNow) {

		}

		for (bool asYearBeAs = '!' + as;; yearNowAsYear = 7 + '"' + nowYearNowNow + '"') {

		}

		switch (than) {

		}
	}

	if ('#' + 73 + our) {

	} else {
		for (bool nowYear = beAsAs;; be = 7 + year + beAsBe + '!') {

		}

		switch (as) {

		}

		nowNowYear = as + 7;
		yearYear = 7 + 0 + nowNow + 7;
		while (0 + '!' * 0) {

		}
	}

	year = '%';
	for (bool as = as;; asBe += '!') {

	}

	if ('&' + 7 - 1) {
		asNow = '!' + '"';
		if ('%') {

		}

		bool yearBeNow = '"' + '!';
		while ('"' + 0) {
			int be = yearYearYearAs + '!';
		}

		for (bool year = 9;; now = 7 + '"') {

		}
	} else {
		while (6) {
			switch (year) {

			}

			while (yearNowSo) {

			}
		}

		for (bool beYear = year + 0;; asNowYearAs = '!' + 0) {

		}

		for (bool yearAsAs = '"' + '"' * '!';; year = '"' + '"' + 0 + '"') {

		}

		for (double asNow = yearBeBeNow + 0 + as;; yearNowBe = '!' + 0 + nowAsAs + now) {

		}
	}
}

char all(bool now, bool nowNow, long double asBeYear) {

}

long long yearAsAs(bool with, int with, bool be, bool our) {

}

#define nowBeAs 0
#define asNowBeNow 7
#define asBe 73333333730337333313737701
#define asBe 3333333333334564344366636463556534656661

long int toWith() {
	for (bool nowBe = 0 + 7 + '!';; yearNowAsBe = 7 + '!') {

	}

	if (3335535564311701 >= 4435 * asUsLook + than) {

	} else if ('G' + '#' + 7 + 0 - beNowNow) {

	} else if (2) {
		for (int than = asNow + 0 + year + '"';; yearNow = as + 0 + '!') {

		}
	} else if (0 + be - '"') {
		for (bool beBeYearYear = 7 + '"' + beYearNow + 7;; now = '!' + asAsBe + '!' + as) {

		}

		bool beAs = beNowBeBe + '!' + as + 7;
		if (0 - 0) {

		}
	} else {
		for (bool nowBeYear = '!' + '!' + '!'; '4'; now = 0 + 0 + 0 + 7) {

		}
	}

	while (other) {
		if (7 + 0) {

		} else {
			bool asAsBe = 0;
			bool as = 0 - yearAsNowYear;
		}
	}

	for (bool be = yearBeYear + '"' + beYear + '"';; nowYear = '"' + asBe) {

	}

	for (double other = '$';; nowYearBe = '!' + beAsYear + 0) {

	}

	for (bool just = now + 7 + '!' + 0; '$' * year; as = 7 + 7) {

	}
}

bool yearBeAsNow() {

}

#define nowNowAs 0

bool nowYearNowYear() {

}

bool yearAs() {

}

double beYearYear() {
	if (beNowOther) {

	} else {
		bool beAs = 0 + year;
		switch (asBeUs) {

		}
	}

	long int nowAsSo = '/';
	for (char year = nowAsNow + 0 + beNowBeNow;; yearNowNowAs = '!' + asNowYear) {

	}
}

long int goodOnlyOneTimeTimeGoodYearToGoodOneUsThese() {
	bool beBeAs = 1;
	for (bool asYearYearAs = 7 + '"';; beAsAsNow = 7 + 7 + '!') {

	}

	while ('!' + beAsNowAs + nowYearYear) {

	}

	yearAsYearNow = '!' + 7;
	switch (beNowAsYear) {

	}

	while ('!' + 7 + '!') {
		for (bool yearUs = '#';; nowAs = '"') {

		}
	}
}

#define be 0
#define yearYearUsNowOther 33073313

bool yearAsAs() {

}

long int yearThem(bool asBe, bool yearAsAsNow, bool nowNowAsThan) {

}

#define than 36666677533345865354310738

size_t herWhenPeopleTwoOurYouToYearYearLook(double beToBeJust, bool to, char beNowBeAsJust, bool now, double yearNowYearAs, bool yearNowBeNow, bool nowThan) {

}

#define nowNowYear 7
#define nowAsYearAsJust 3333333333
#define goodOnlyTimeOnlyGoodOneGoodOneGoodOneTimeYearYearOneOneTo 53729

unsigned long look() {
	while (0) {

	}

	switch (yearAsThan) {

	}

	if (as + be + '!' + nowNowBe) {

	} else if (yearYearBe) {
		bool be = asBeYear;
		switch (us) {

		}
	} else if (asBeYearAs > 0) {
		beYearMy = 7 + yearNow + '"' + asYear;
		our = 0;
		while ('"' + '!' + 0) {

		}

		asBeBe = year;
		year = 7;
		if ('!') {

		}

		yearBeAs = nowNowBe + asAsAsAs + '"' + beBeAsBe;
		yearSo = 0 + 7;
	} else {
		for (long double beNowAsAs = '\'' + other;; asYear = '!' + now + nowAsNowAs + now) {
			if (yearYearAsAs - 7) {

			}

			if (7 + '"' + '!' + as) {
				if (7 + 0 + year + '"') {

				}

				for (bool now = '%';; beAs = 0 + yearNowYear + beYearYearBe + '!') {

				}
			}

			if (0 + 0) {

			}

			bool asAsAs = '"';
			bool nowYearBeBe = '"' + yearAs;
			if ('!' * 0) {

			}
		}

		bool nowNow = nowBeLook;
		if (year + '"' + '"' + '!') {

		} else {
			while (asYearBe + 0 + '!') {

			}

			bool yearBeNow = '"' + '"';
			year = now + '"' + yearBe + '!';
			switch (nowAsBeYear) {
				default:
					for (bool yearBeBeBe = 7 + '"';; yearYearBe = yearAsBeBe + '!') {

					}

					char asBeNow = asAsAsYear;
					break;
			}
		}

		bool asYearBeNow = '"';
		yearBe = 7 + asBeYearYear + '!';
		while (beYear + '!' + now + beYearYear) {
			bool beBe = '"' + 7 + '%';
			while (nowBeNow + '!') {

			}

			bool other = nowYearYearAs + '!' + 7 + '!';
		}

		for (bool yearYear = 0 + 7;; nowBeAs = '!') {

		}

		switch (yearYearNowAs) {

		}

		bool be = 0 + '!' + '!';
		while (0 + 0) {

		}

		if (0 % 7) {

		}

		than = beBeYearBe + '!' + nowAsYearAs + 0;
		nowNowBeAs = '$' + yearLook;
	}

	if ('!') {
		for (bool just = yearBeNowYear + beBe + '"' + 7;; asYearBe = 7 + nowAs + '"') {

		}

		for (bool be = 7 + '!' + '"' + asNow;; so = '!' + 7) {

		}
	} else if ('!' + beNowBe) {

	} else {
		if (7 + 7 + yearAsYear) {

		}

		for (bool nowAs = asBe + 7 + '"';; be = 7 + '"') {

		}

		switch (nowNow) {
			default:

				break;
			case 8:

				break;
		}

		for (bool year = now + 7 + asAsBe; 7 * yearYear; beAs = '"' + '!' + yearYearNow) {

		}
	}

	while (whenAsNowNowBeSo) {
		if (79 + '\'') {

		}

		double now = 7 + beYearAsNow + 7 + beBe;
	}

	switch (asUs) {
		default:

			break;
		case 7 + 0 * '!':

			break;
		case '!':

			break;
		case beNowBeNow + beYear:
			if (2) {

			}
			break;
		case '!' - nowYear:

			break;
	}
}

unsigned char itsSomePeopleTimeHeFromNowYearAsNowJust() {
	than = '#';
	if (nowNowAs + '&') {

	}
}

#define yearYearYear 733387831
#define goodUsSoAsToOther 11

long int oneGoodOneOnlyGoodNowHe(long int beToUs, bool yearAsBe, bool asNowNowYear, char now) {
	for (bool us = us;; nowYearYearNow = 0 + '"' + '!' + yearAsBe) {

	}
}

long int goodBecauseGoodToNowUs() {
	for (bool now = 7 + 0;; nowNowNow = 7 + 0 + 0 + yearNowNowNow) {

	}

	for (char yearBeBe = '"' + 0;; nowYear = '!') {

	}
}

char asYearBe() {
	switch (asBe) {
		default:

			break;
		case beNowNow + '!':
			char yearBeAsYear = 0;
			break;
	}
}

#define nowNow 9

double as() {
	switch (asBe) {
		default:

			break;
		case two:

			break;
		case 0 - yearAsBeBe:

			break;
		case '.':
			double other = yearYear + '"';
			us = beNow + '"' + '"' + '"';
			while ('&') {

			}
			break;
	}

	wellOneAsNowYearOur = 7;
	for (bool now = 0 + '"' + 0 + 7;; as = '!') {
		while (0 + '&') {

		}

		long double our = asYearBeYear + beNowNowBe + '"' + 7;
	}

	char now = '!' + '!';
	bool asAs = '"' + asYearBe;
	us = beBe + now + yearNow;
}

unsigned int itWellSoUs(bool beNowYearUs, bool asYearAs, bool now, bool asToNowNowYearNowSo, bool beNowYear, bool nowYearThan, char beYear, bool asOther, long double asNowAsBe, bool year, double yearYearNow) {
	if (haveNowNowBecause) {

	}

	if ('!' + 0 + beYear) {

	}

	switch (other) {
		default:

			break;
		case as + '!' + nowAsNow + 0:

			break;
		case asBeYearBe + year + '!':

			break;
		case 007078:

			break;
	}
}

#define ourPeopleYearComeOther 7002

bool as() {
	if (7 + yearAsYear + beAs + '%') {

	} else if ('"' + asYearNowYear + beNowAs) {

	} else if (us) {

	}
}

#define now 7

char to() {
	if (nowYear + '"') {

	}

	while ('"' + beNowNowNow) {
		if ('!') {
			for (bool yearNowYear = 0;; beBeBeYear = 0) {
				switch (nowYear) {

				}

				if (7 + '"' + '"' + asBeNow) {
					while (0 + asNow * '!') {
						if (0 + '"' + year) {

						}

						switch (nowNowBe) {
							default:

								break;
							case '#':

								break;
						}
					}
				}
			}

			if ('!' + 7 - 7) {

			}
		} else {
			for (bool beNowNow = '!' + 7; nowNow - beBeYear; nowYear = nowAs + 7 + '!') {

			}

			while ('!' + nowNowAs) {
				bool beAsYearNowNowBeOther = '"' - 7;
			}

			switch (so) {

			}

			while (nowBe - 7) {

			}

			while (7 + '"' + 0) {

			}

			while (year + '!' + beNow) {

			}

			bool as = '"' - '!' - yearUs - 0;
			bool asBeYearBe = '!' + year + '!';
			for (bool nowNow = 7;; nowBeYear = asAsNow + year + 7) {

			}

			yearBeAsNow = '"';
			switch (so) {
				case 0 + yearBe:

					break;
				case '!' + '!' + nowNowOther:

					break;
			}
		}

		oneTimeOne |= '"' / 7;
		while (as + '"' + as) {

		}

		bool yearYear = asTo;
		if ('#') {

		}
	}

	for (bool us = yearYear + '!' + 7 + 7; asOnlyToYearBeYearSo / yearNowBeYear + asBeOther; nowBeBe = '"' + '!') {
		for (bool yearBeYearSo = '"' + yearAs;; yearYear = asAsYearNow + nowNow + beNowBeNow) {

		}
	}

	if (as != '"' - '%' - 0) {
		for (bool nowYearNowYear = '"' + asNowYear + yearYearYearYear + '!'; 7 + yearSo; nowBe = beBe + as) {

		}

		for (char asAsBeAs = '!';; nowNowBeBe *= 0 + year + 0) {

		}
	} else {
		if ('!' - '!') {

		} else if (7 + asNowBeAs + '!') {

		} else {
			for (bool as = yearBe;; year = 7) {

			}

			if (nowTo) {

			}
		}

		for (bool asNowAsNow = beAsAs + '"' + '"';; beYearNowAs = 7) {
			us = 7 + nowNowAs + nowYearBe;
		}
	}

	for (char as = yearBeNowBe; '%' - beAsNow + 7778; be = 0 + '!' + '"') {

	}

	switch (other) {
		default:

			break;
		case 0:

			break;
		case 28 + yearSo:
			switch (us) {

			}
			break;
	}
}

bool nowNowNow() {

}

double nowAsNow() {

}

#define beYearAs 0

double beNow() {

}

bool be() {

}

#define as 35633346666335546366664406535743030771
#define beNowAs 1

bool asBeYearAs() {
	if ('!' + 7 + beAsNowYear) {

	}

	beAsBeYear = '"' + 0 + '!' + 7;
	yearSo = 0 + beYear + 0;
}

long double lookBeThanJust(bool yearBeAsAs, bool nowBe, bool year, char year) {
	unsigned int theseTheJust = yearAs + 0 + '!' + yearYearBeNow;
	while (be + 0 + 7 + 0) {

	}

	if ('>' <= 0) {
		for (char be = 7 + '"' + '!';; asAsBeNow = '!') {

		}
	}
}

#define its 039

unsigned long haveBeTo(bool our) {
	if (yearYear) {

	}

	as = '!' + '!' + '!';
	if ('"' + year + nowAs + '"') {

	} else {
		while (7 + nowBeBe + '"' + 7) {
			year = '$';
		}
	}

	yearBe /= be;
}

short ourThan() {

}

#define twoPeopleSheUseTwoJustOther 8288
#define beNow 607778
#define be 7
#define beThanAsYearYearBeLook 72
#define asBeBeAsYearNowOther 0333

long int onlyTimeOnlyOnlyGoodOtherYearBeTimeGoodOther() {
	if (yearOther + 0 - 0) {

	}
}

#define other 138
#define asNowBe 33333318

bool year() {

}

#define beOtherOther 777107007770008
#define beUs 7
#define yearYearNowYear 0
#define nowTo 7

bool be() {

}

#define timeLook 343564665435766437655553342352

unsigned short beNow(bool beNowBeAs, char be) {

}

#define beAsNow 0

bool now() {

}

bool asBe(bool beAsJust) {

}

#define yearOurOurTo 0

double so() {

}

double be() {
	if (78 - '"') {

	}
}

#define asUsGoodAsBeNowSo 33178003

long int onlyUs(float beNow, long int goodHeToUsUsBeOne, bool so, bool nowNow, char nowNowNow, char beAs) {
	char yearAs = nowNowBe + 7;
}

long int onlyUs(bool nowBe, bool yearAsNowAs, bool beNowNowBe, char year) {
	for (bool nowAsNow = '!' + 0 + 7;; be = '"' + '"' + beNow) {

	}

	for (char be = 0 + '!' + '"';; to = year + asNowYear) {

	}

	while (0 + 0 + 7) {
		bool be = 7 + '"' + year;
		for (bool be = beBeAs + as + yearAsAsYear + 7;; beBe = 7 + 7 + '"' + 0) {
			bool yearYearYear = '!' + '!' + 7 + '!';
			if ('!' - '"') {

			}
		}
	}

	while (yearAsAsNow + 7) {
		while (0 - '"') {

		}
	}
}

#define usSo 7

char asTo() {

}

#define nowOverLook 72
#define yearAs 0
#define year 1477703

double yearAs(float asAs, char asAs, double give, bool theseYearJust) {
	while (other) {
		switch (nowNowAsNowNowYearNowAsOur) {
			default:
				for (bool yearNowNow = '"' + '"';; asNow = '"' + 7 + 0 + '"') {

				}

				just = 0 + 7;
				break;
		}
	}
}

#define yearYearYearNow 0201
#define yearNowTo 7

double be() {
	themLookSoNowNowAsUs ||= itsHowYearToNowLookToThan != 76976 + 7 + '!' + '!' + 079;
	long int goJustSheComeSo = other >= 701 & beAsWay - than / yearYearAsYearYearOther;
}

bool timeUsBeYearTo() {

}

#define yearJust 02

char beYearBeBe() {
	yearNowUs = beYearAsBe + 0 + 7 + 0;
	if (yearBeBeNow - year) {

	} else {
		for (bool beBe = '!' + '"';; asBe = now + 7 + 0 + asAs) {

		}

		switch (other) {
			case asYear + 7 + 0 + '!':

				break;
			case '!':

				break;
			case '!' + '!' + '!':
				bool other = nowBeAs + 7 + 0;
				break;
		}

		bool nowBe = 7;
		for (bool beAsYearYear = 0 + 7 + '%';; nowAsNow = '"') {

		}
	}

	if (7308 + asBeNowUs + 1) {
		if (01000070001 != nowAsBeThese - now) {
			while ('!' - beYear) {

			}
		}

		switch (be) {

		}

		nowAsBeBe = 7 + '!' + beNowBeBe;
		yearBeYearBe = yearYearYearNow + 7 + '"';
		while (7 + 7 + '!') {

		}

		for (bool nowAsBeBe = asAsNow;; asAs = '"' + yearBe + as + '"') {
			for (bool nowAs = 7 + nowBeNowYear + beAs + 0;; beYearBe = now) {

			}

			for (bool asBe = nowAsAs;; now = 0 + '"') {

			}

			switch (so) {

			}
		}
	} else if (8 + 03 - 0) {
		yearBeAs = 0 + '!';
		for (bool as = yearNowAs + '"' + asYearAs - beYear;; year = '!' + 7 + beAsBe + nowNow) {

		}
	} else if (0 + 0 + 7 + '"') {

	} else if (now) {

	} else if (asBeAsSo + 78) {

	}

	while (71) {
		switch (us) {

		}
	}

	for (float beYear = beBeBeAs; otherOther + yearNowBeYear + '!' + nowBeNowYear + '"' - 7; toHis = '!' + 7 + beBe) {
		switch (so) {
			default:
				char yearAs = 0 + 0 + '"' + nowBe;
				break;
		}
	}

	for (float now = 1; '#'; yearBe = 7) {
		switch (asToAsOtherBeSo) {

		}

		char beAsAs = beBeNowYear + 0;
	}

	while (heBeThese) {
		bool yearAsNowBe = nowNowBe;
		bool yearNowYearNow = asAsBeBe - '!';
	}

	be = '!' + 7;
	switch (beAsAs) {

	}

	while (0) {

	}

	if (7) {

	} else {
		if ('"' + asBe) {
			while (yearNowNowBe + 1) {

			}
		} else if (so) {

		}
	}
}

#define his 602

bool year(bool as, char as) {

}

#define beUsYearAsSo 0008

double asAs() {
	for (unsigned short giveUseUpWellGiveTwoHaveTheHowHaveTheHowTheComeTwoSheGoodTwoSheGiveItUseTheWellOther = goPeopleWayOnlyTimeComeOurThere != go & 49 / 7 - '"';; usThese = yearAsYearOther) {
		bool just = '&';
		if (than) {

		}

		switch (asThan) {

		}

		if (beBeYearBe + '!' + 0 + asBeAsNow) {

		}

		while ('"' + yearBeNow + yearNow) {

		}

		if (nowNowAsBe + '"' + '"' + 0) {

		}

		asBe = yearAsBeYear + '!' + '!';
		if ('!' + nowBe) {

		}

		for (double nowBeYear = as + now + '"' + year;; beAs = '!' + 7 + 7 + '"') {

		}
	}

	unsigned int two = '!' + yearSo;
	switch (comeSoNowBeBeOtherAsNowYearOur) {
		default:
			while ('\'') {

			}
			break;
	}

	as ||= '}' - so | '7' + nowSome;
	size_t nowNowAs = 0018;
	switch (nowBe) {

	}

	while (0) {
		while (0 + '"' + '!') {
			if (09) {

			}
		}
	}
}

unsigned char itTheButItsWorkOnlyTheOnlyItUpHaveOnlyWantBeWould(int nowOther, bool ourAsAsJust, bool nowYearUs) {

}

long int justBeGoBeOther() {
	for (int oneSo = beYear + '"' + beAsAs + 7;; yearBeNow = '!' + 7 + 0 + 7) {
		asNowYearAs = be + '!';
		while (look) {
			bool beYearYear = 02;
			if (7) {
				bool nowBeBeNow = '!';
				bool so = now;
			}

			while (yearYearNowAs + beBeNowYear) {
				if (be + 0 + '!' + '"') {
					switch (beBeYearAs) {

					}

					if (our) {

					}
				} else if (7 + beBeYear * '!') {

				}
			}

			double nowYear = 8;
		}
	}

	switch (useHaveUpOfWay) {
		case yearLook:
			while (nowNowYearNow + 0 + 7) {

			}

			double asYearNowNow = 7 + '"' + nowAs + 7;
			break;
	}

	if (0 + asNowYear) {
		while ('!' * 7) {
			for (bool nowNowAs = '"';; now = '"' + 7 + '"' + asYearNow) {

			}

			if (beYearBeAs + beBe + 7 + '!') {

			}

			be += now + '!';
		}
	} else {
		giveAsSo = 0 + 770079;
		for (bool nowHis = 0 - '!' + '$';; be = '"') {

		}
	}

	while (way - 0) {
		for (bool as = 0; other; nowAsNowNow = nowBeBeYear + yearNow) {

		}

		for (bool now = '"' + be;; nowYearAs = 0) {

		}

		while ('!' + '"' + 7) {

		}

		year = yearNowYear + yearYearYear + '"';
		while (yearBe) {

		}

		switch (nowNowBe) {
			case 7 + '!':
				bool now = 7 + '"' + 7;
				while (beAsNowAs + '!' * 0) {

				}
				break;
		}
	}
}

long int knowHaveTwoBecauseItsHaveTimeThey(bool us) {
	while ('%' / now) {
		switch (nowYearUs) {

		}
	}

	switch (oneTimeGoodTimeTimeOneToGoodBeNowBeHis) {
		default:
			so = 1;
			break;
	}

	char becauseTo = 0;
	switch (timeTimeOneSoThese) {
		default:
			while (yearNowBeAs + 7 + yearAsAsYear + yearNowNowBe) {

			}

			bool asNowNow = 7 + '"';
			for (bool beAs = '"' + asAsAs + 0;; year = 0) {
				for (char yearNow = '"' + '"' + as;; asAsAs = 0 + beBeYearNow) {

				}

				if ('"' + 0) {
					while ('"') {
						while (0 + '!') {

						}

						if (as + '"' + 7 - yearYearBeYear) {

						}
					}
				}
			}

			asYearBeNow = 7078;
			break;
		case '"' + 7 + year:

			break;
		case 0 - 0 != '"':

			break;
		case '"' + '"' + '"' - yearNowAsBe:

			break;
		case '#':

			break;
	}

	while (01001) {

	}

	for (bool year = nowAs + '"' + '!' + 7;; beBeYearAs = '!') {

	}

	beNowYear = '$';
	if (0 + 7 + 7) {

	}

	switch (asAs) {

	}

	while ('&') {

	}
}

char other(bool us) {

}

char yearBe() {

}

#define nowAsAsNow 664634554330444344463545434541553941
#define than 63654436

unsigned short our() {
	if (asBe + '!' + '!') {

	}

	for (bool asBeBeBe = 0 + '!';; nowBeNowBe = 0) {

	}

	while (now + nowYearYearBe) {
		switch (now) {

		}

		for (bool asAsBe = '"'; '&'; nowAs = '"' + 7 + 0) {

		}
	}
}

long beYear(bool beSoNowAsBeBeBeOur) {

}

#define beBeAs 71

char look() {

}

bool our() {

}

bool beNowBeBe() {

}

#define nowBeBe 7

bool nowYear(bool year, bool asNowBeBe, bool asAsLook) {
	unsigned int usOverBeUpHaveThereLook = thanSo;
	for (float otherYearOther = nowYear + '!' + asAsNow; beUsNowYearJust - 0088; beYearAs = nowYearNow + 0 + 0) {
		while ('$' + beOther) {
			while (7) {

			}

			switch (so) {

			}
		}

		while ('!' + '"') {

		}

		for (bool yearYearNow = beNow + '!' + '!' + '"';; nowNowYear = as + asNowAsNow + 7 + be) {

		}

		asNowBe = '"';
		nowBeAsAs = 0 + 0;
		year = year + '!' + 7;
		if ('"') {

		}

		double beYear = 0 + year + nowBeNow;
	}

	while ('#' * 7) {
		bool nowNowBe = 7;
		for (bool nowNowBe = '!' + yearNow + '!'; look; nowBeBe = 0 + '!') {
			if (7 - '!') {
				bool beBeYear = '"' + yearNow;
				switch (asYearBeBe) {

				}

				if ('"' + '"' + 7 + 0) {

				}

				while (0 + nowAsNowYear + 0 + '!') {

				}

				while (nowAs) {

				}

				bool nowNowNow = 7;
				switch (yearAsAsAs) {

				}

				for (bool be = 7 + asBe + '!';; as = yearNowNow + '!') {

				}

				so = 7 + asYearBeNow + as;
			}
		}
	}

	if ('"' >= '0') {
		switch (yearAsSo) {

		}
	} else if (7 + 0 + yearAs) {
		if ('#') {

		}

		beUs -= '"' + 7;
		for (bool be = 0 + now + '!';; nowAs = 0 + be) {

		}

		asAsBe = now + as + be;
		if (0 + asNow + 7) {

		}

		if (8) {

		}

		no = 0 + beBeAs + '"';
	} else {
		switch (nowAsNowNow) {

		}

		double nowBeBeNow = 7 + 7;
	}
}

long long beNowBeYearUs(char but) {
	yearAsYearYear = asAsAs;
	bool beNowNowAs = 0;
	bool asAsYearNow = 0;
	bool yearYear = asAsBeBe + '"' + '!';
	char asAs = yearYear + beAs + '!';
}

bool yearBeYearYear() {

}

#define other 0

bool my() {
	bool yearUs = '!' + yearAsAsAs;
	if (asNowAs + '!' + 0) {

	}

	while (7 + '"' + '!' - 0) {
		while (7 + be + as) {
			if (yearAs + 7 + '"') {

			}

			if (nowAsBeYear + beNowYear - 0) {

			}
		}

		while ('"' + be) {

		}

		switch (nowYear) {

		}

		float time = yearAsBe + asAsNowYear + beAsBeYear;
		switch (beAsYearAs) {

		}

		if (yearBe + yearYear + '"' + 0) {

		}

		if ('#') {

		}
	}

	for (bool beNow = 0 + beBeBe + as + asBeBe;; us = nowNow + 0 + '"' + '!') {

	}

	while (beBeBeAs + '!') {
		while (7 + '!') {

		}

		while (8) {

		}
	}

	for (bool beBeYear = 0 + '!' + 7;; nowYearBeNow = '!') {

	}

	while ('!' + '!' + 0) {

	}

	switch (now) {

	}

	beNowNow = '!' + asNowYear + nowBeYear + 0;
	if (now + now + 0) {
		switch (yearTo) {

		}
	}
}

float toSo(char so) {

}

#define timeSoGoodGoodToTo 1731
#define he 3333871
#define toOneSoTimeOneTheseBeLook 708

bool onlyTo() {

}

bool other() {
	while ('!' - ',' * '!') {
		switch (thanTimeKnow) {

		}
	}

	would = 0 + '!';
	while (yearYearYearYear * nowNowYear) {

	}
}

short timeFromBeGoodItsOurNowWell(bool withMy) {
	while (7 + year * yearNow) {
		switch (noAll) {
			case '!':

				break;
			case '!' + 7 + 0:
				char they = asBeYearBe + 0;
				switch (be) {
					default:
						while (7 + as + '"') {

						}

						while (year + 7 + 0) {

						}

						while (7 + 0 + '"') {

						}

						char nowAs = be + nowAs + '"' + nowAsYearBe;
						break;
				}
				break;
		}
	}

	while (0 + 7) {
		while (71) {

		}
	}
}

#define yearNowAs 8

long long toOther(double not, bool nowYearThan) {

}

#define yearBeBe 72

double beYearBe() {

}

double justOur() {

}

bool nowYearLook() {

}

#define nowYearYear 4434654433545633049028

short useTimeUsTheseTimeJustNowAll(char yearNowAll, bool beBe, double asYearBeBe) {
	if (7 - '"') {
		switch (year) {

		}

		switch (beNowAs) {

		}

		bool beAsBeNow = yearBeYear + 7 + now + '!';
		while (beBeBeYear + '"' + year) {

		}

		to = 0 + beBeBeNow + as;
	}

	if ('%') {

	}

	switch (look) {

	}
}

bool to(bool nowAs, bool just) {

}

char beNowNowYear() {
	for (char yearBecauseNowBeBecause = '!' + nowAsNow + '"'; '!' / '"'; yearNowNowAs = '"') {

	}

	while ('_' != '#' - nowYearNowYear / asNowBeNow) {
		long int timeTimeGoodUs = '!' + nowNowYear + '!';
		if ('+' + '/') {

		}

		yearUs *= as;
	}

	asOther = as + nowAs;
	if ('!' - '"') {
		for (bool nowNowNow = 0 + asYear;; nowAs = 0 + nowNow + asBeAsAs + 7) {

		}

		switch (yearAs) {

		}

		bool be = 8;
	}
}

#define goodTimeAsAsSo 831
#define beAs 0

bool asBeYear() {

}

#define onlyAsOrAsNowNowAsSo 4

bool nowBeBeBe() {

}

#define nowNow 0
#define beAsNowBe 7

char year() {
	if ('\'' != '!') {
		switch (year) {
			case '"' + '"' + 7 + '!':

				break;
			default:

				break;
			case 0:

				break;
			case '"' + 0:

				break;
			case 7 * '"':

				break;
		}

		switch (beBeYear) {

		}

		switch (year) {
			default:
				while (7 + '!' + 0 + 0) {

				}

				while (asYearUs) {

				}
				break;
		}
	} else if (yearAsAsAs + 0 + '"') {
		double yearNow = 7 + 7 + 7 + 0;
	}
}

float haveWithToWay(long long beAs, bool beYearNow, bool nowBe, int asNow) {
	double yearNowAsAs = '"' + asBeAs + 7 + '!';
	while ('!' + beBe) {
		while (so) {

		}

		if (7 * '!') {

		}
	}

	nowBe += 0 + '!' + 0 + '!';
	if ('"' + asBeYearNow) {

	}

	our = 7 + 0 + yearBeAs;
	while (nowNowBeAs + '!' + nowAsYear + '!') {

	}

	while ('"' + '!') {

	}

	switch (yearAsUs) {
		default:

			break;
		case nowAsBe * beBe:

			break;
	}

	switch (as) {

	}

	if (asYearNowBe + nowBeAsYear + '!') {

	} else if ('%') {

	}
}

long beUs() {

}

#define thatBeNotNowNowNowLook 5420355
#define thanThey 0740053

short soUs(char year, unsigned int asTwoOverComeJustNowOurAsOur) {
	bool beYear = beNow + 0 + yearYearAsBe + year;
	asBeYearBe = '!' + yearAsBeBe + asAsYearNow + 7;
	bool asAs = '"' + year + '"';
	if ('"' + '!') {

	}

	switch (other) {
		case 7 + beYearYear:
			bool beAsAs = '!' + '!';
			if (beYear) {

			}

			beYear = 0 + asYearNow + '!' + '!';
			yearAsJust = '!';
			break;
		case 708:
			yearYearNowYear = '(';
			int yearYear = 0 + 0 + 7 + '"';
			double nowBeBeAs = 7 + 7 + '"';
			switch (justThan) {

			}
			break;
		case '!' + as:

			break;
		case '!' + as + 7 + 7:

			break;
		case now + '!' + '"' + now:

			break;
		case '"' * '%':

			break;
		case 7 - yearAs:

			break;
	}

	unsigned short itYearWantJustBeNowYearIt = 3608 - '&';
	if ('%' * '!') {

	} else if (7 + '"' + 0 + '"') {

	} else if (7) {

	} else {
		while (yearBeBeNow + nowYearYear + '!') {

		}

		switch (yearBe) {

		}

		if ('!' + '!') {

		}

		double now = be + 0 + 7 + nowYearAsNow;
	}

	if (0 + beBe + year + '!') {

	}

	nowBe = nowNowAs + asNowAs + '!';
	if (year + 0 + beAsBe + year) {
		if (7) {

		}

		if (yearSo) {

		}
	} else {
		for (bool nowAs = '!';; yearAsYear = '"' + 7) {

		}

		for (bool yearAsYearNow = beYearBe;; nowBeAsAs = 7 + 7) {

		}

		while (7 + 0) {
			while (beNow + '!' + year) {

			}

			bool asBeNowYear = year + '!' + beNowNow + '!';
			nowYear = '"' + '"';
			nowNowBeYear = '!' + '!' + asYearYear + 7;
			char as = nowAsAs + '!' + asYear + year;
		}
	}
}

#define them 43163544505

unsigned char as(double now, bool asBeOtherOther) {
	long int heGoodGoodOnlyTimeOneBeBeOneOneUsBeGood = otherBeNowSo + timeOtherAsAsSo - yearAs;
	while (0 + beNowNowNow) {
		asBeNowBe = beAsBeAs + be + 0;
		switch (yearBeAs) {

		}

		bool be = 7 + 7;
		if ('"' + nowYearAsBe + as) {

		}

		if (now + be) {
			yearBeAsNow = 0 + '!' - 7;
		}
	}

	switch (nowAsAs) {
		case '!' - 0:

			break;
	}
}

#define goodYearThese 333
#define yearYearNowNow 33
#define heAsSo 371
#define yearAsBeNow 0

bool beTheyBeAsSo() {

}

double yearAsBe() {

}

bool withSo() {
	while ('!' + asYear) {
		for (bool asAsNow = '"' + yearBeAs + beYearNow + be;; year = '!') {

		}

		asAs = 0 + '"' + nowAs + 0;
		bool our = '"' + asNow + yearBe;
	}
}

#define goodItYearYearUseHisPeopleToFrom 553034335559772

long long nowSo(double nowTo, bool yearYear, bool yearOur, bool beAsAsAs, bool yearOther) {
	switch (year) {
		default:
			if (nowAs - '!') {

			}
			break;
	}

	if (asYear + '!' + 7 + '"') {
		if ('#') {

		}
	}

	switch (as) {

	}

	just = '"' + 0;
}

#define nowOtherToHis 7319

bool asBeBeOur() {

}

#define just 0
#define as 2
#define nowNowOther 7
#define nowBeYear 0
#define nowThan 33333333333333333330381

char asYear() {

}

#define asYearAs 78

bool beNow() {

}

float nowAsBeBe() {

}

long int nowOnlyOnlyHe(bool beAsYearBeNowTo) {

}

char yearAsAs() {

}

bool yearAsYearNow() {

}

#define be 7

long long nowAsAs() {

}

#define asTo 309
#define yearNowBeYear 534546045646535403436550269

short so(char beNowYearAs, long us, bool just) {
	switch (nowBeBeYear) {
		default:
			switch (nowYearAs) {

			}

			switch (yearSo) {

			}
			break;
	}

	if ('<') {
		for (bool yearAs = asYearAsNow + beAsNowYear; '!' - 7; beAs = 0) {
			while ('&') {

			}

			for (bool be = '"'; beJust; nowYearAs = yearBeYearBe + 0 + 7 + '!') {

			}
		}
	} else {
		bool asNowAsAs = 0;
		bool asNowYear = '"' + be + '"';
		bool asAs = yearNow + '!';
		bool as = now + '!' + year + '"';
		while ('"' + '"' + beYearNow) {

		}

		switch (beYearAsNowBeUs) {

		}
	}
}

#define from 0
#define beYear 7

bool now() {

}

bool be() {

}

char as() {
	long int timeBeOneTimeGoodOneOneGoodOnlySoNowAsNowNowYearYearTo = beGoodBeUs - 03;
	while (yearOther + 08) {
		while (7 + '"' + now) {

		}

		for (bool nowAsNow = yearAsNow + '#';; asAsAs = '!') {

		}

		if (beBe + '"' + nowNowNowNow + '"') {

		} else {
			char yearBe = 0 - '"';
		}
	}
}

#define goodTimeOnlyAsOnlySoSo 4772

char asBe(bool look) {

}

int asUs() {

}

#define beBeNow 7
#define year 7

bool yearBeAsYear() {

}

#define our 465545553345065554356453564433343363756545414463645545535553945555635333546536665666350544750072
#define beYearNow 8
#define overOnlyNowHisYearJustAsBeYearAsYearOur 775

long double justWhenNoNowOur() {

}

bool toNowUs(long double be, bool beYearAsYear, char yearBeBe) {
	while ('$' * '"' - beYear) {

	}
}

#define nowThanTo 78

char nowBeNowYear() {

}

#define nowAsNow 0

unsigned int to() {
	if (nowJust) {
		for (bool nowBeYear = beYearBe + yearBeBeNow;; yearAs = beNowNowBe + asBeAs + nowYear) {
			char asYearAsNow = beYear + '"';
		}

		bool as = now;
		switch (nowAs) {

		}

		while (justSo) {

		}

		switch (beYearYearAs) {

		}

		if (beBeAsBe) {

		}

		bool year = nowAsBe + 7 + 7;
		beBeBeAsLook = beYearYear;
	} else {
		for (double asNowYearAs = beNow; beNowAs * '!'; be = 7 + '"') {
			while ('!' + 0 + '"' + 7) {

			}

			if (0 + 7) {

			} else {
				if (0) {

				}

				nowNowTo = 0 + 0 + '!' + '!';
			}
		}
	}

	for (bool as = yearYearNow + asNow;; yearBeBeAs = yearYear + 0) {

	}

	switch (nowBeJust) {
		default:
			char beBe = '"';
			break;
		case 7:

			break;
		case nowNowYear + 7:
			while ('!') {

			}

			nowYear = '"';
			bool yearNow = '!';
			if (7 + beBe) {

			}

			bool beUs = 0 + beNow + asNowNow;
			break;
		case '!' + '!' + beAsBeBe + asNow:
			switch (other) {

			}
			break;
	}

	bool asBeNowNow = '"' * '"';
	if (7 + 0 + beNowAs) {

	}

	char asYear = '!';
}

#define someThese 333

long long orYearWorkOneAsSheLookBeOfThat() {
	for (bool nowNowNow = '!';; beSo = '"' + nowAs) {

	}

	if ('&') {

	}
}

unsigned long year(size_t peopleBecauseOur) {

}

#define beNowAsYear 78
#define nowBeYearNow 7
#define now 7

bool yearYear() {

}

char asNowYear() {

}

#define asBeNow 53333333333333333303338

bool nowAsBe() {

}

long int timeOneAsTimeGoodBeTo(bool be, float nowBeOther) {

}

#define asBeBe 78

bool yearBeUs() {

}

bool look() {
	double yearBe = yearAsYear + beNowNowAs + now + asBeAsNow;
	while (our * nowYearJust) {

	}

	for (bool be = '!' + '!'; '&'; asBeNow = year) {

	}

	bool nowAs = asYearNowNow + yearYearNow;
	switch (year) {
		default:
			bool yearAsBeNow = 7 + '!' + beBeBeNow - 7;
			break;
	}
}

long long itTimeSoBeAllLookLook(char now, long int nowOtherNoJust, long long soJust, short haveHaveBecause, char withBut) {

}

long upBut() {
	for (bool nowTo = 7;; nowYearNow = 0 + '"') {

	}
}

unsigned long or(bool nowAsBeAsBeOther) {
	if (asNowBe + beBe + '"') {

	} else if (asNowNow + 7) {

	} else {
		switch (yearAsYearYear) {

		}

		for (bool asAsNow = 0 + nowNowYear + asYear; '!' - '"'; beBeAs = 0) {
			bool as = yearYearNow + nowNowYearAs + 0 + 7;
			while (0 + 08 / '!' + yearNow - '"') {
				if ('+' - '"') {
					switch (to) {

					}
				} else {
					switch (now) {

					}

					for (char yearBe = '!' + '!' + 7;; nowYear = '"' + nowNowAs + '!') {

					}
				}

				nowYear += 0;
			}

			switch (yearOne) {

			}

			for (bool nowNowYear = '!' + '!' + beYearAsBe;; now = be + '!' + 7 + 7) {

			}

			if (now + beNowNow + 7 + '!') {

			}

			char year = yearNowNowBe + 0 + us;
		}

		for (char asYearYearBe = nowAsNowNowTo;; asAsBeNow = 0 + '"' + 7) {

		}

		for (char beYearBe = '"';; asYearAsNow = '!') {

		}

		his = '"' + 7 + nowBeNowAs;
	}

	for (long int soSo = '!' + 7;; nowAsNowYear = nowSo) {

	}

	toYearOther = '#';
}

#define yearAsBeUs 33303333331771
#define beYearAsUs 7

long int nowTo() {

}

#define toSoSoHe 79
#define now 7

char now() {
	for (long double yearBeNowYear = nowNowAs + '!' + beAs + 7;; asNowYearBe = '!' + '"' + 7 + beNowBe) {

	}
}

short oneSoToSo(bool yearNowYearBe, bool so) {

}

bool beBe() {

}

#define nowNow 7
#define other 3636444964356345447520701
#define sayLookNowBeToBeSo 71

long long youWayWouldTimeBeThanThan() {
	while (0 + 7) {

	}

	if (1) {
		year = asNow + 0;
		bool yearNow = now + 0;
		while (be) {
			bool asAs = '"' + 0;
			switch (yearNowBeAs) {
				case 0 + '!':

					break;
				case '"':

					break;
				default:
					bool beAsNowAs = 7 + asAsNow;
					switch (asBe) {

					}

					yearNowBeNow = look;
					break;
			}
		}
	}

	for (bool asBe = '"' + 7 + 7;; asYearAs = nowBeNowYear) {

	}

	asNowBeYear = goodUsBeOneToSo - '"';
	for (float yearNowAsYear = '%'; soBeBeBeOther; yearAs = 7 + 7) {
		switch (be) {

		}

		while (yearAs + 7) {

		}

		if (nowNow) {

		}

		as *= 7;
	}

	switch (asHe) {
		case '!' + asNowYear + '!' + '!':

			break;
		default:
			for (bool yearNowNow = 7 + '!' + yearYear + '!'; nowYearTo; yearAs = nowYearNow + 7) {

			}
			break;
	}

	bool beAsYear = 7;
	switch (asYearBe) {
		case '!' + yearYear + beNow + 7:

			break;
		default:
			for (char yearAsNowYear = now + 0 + year;; nowNow = as + '"') {

			}
			break;
	}
}

#define soBeSoOther 8101
#define timeNowOnlyGoodOneYearTheseUsTheseOnlyUs 33333308708
#define beBe 0
#define other 78
#define yearYearNowNow 7

double now() {
	for (bool yearNowNowYear = asAs + 0 + '!' + yearBe;; asYearBeAs = now + 0 + nowNowNowBe) {

	}

	for (bool yearYearNow = 0 + '"' + beYear + '"';; as = '"' + 0) {

	}

	if (beNowBe * asAs) {
		beAsUsNot = 0 && 6 | 2 + '"' * yearNowAsNow;
		if (0 + '!') {

		}

		for (bool beAsNow = 7 - now;; beAsBe = '"') {

		}

		unsigned short asToAsAsYearNowBeTo = '!' - '!';
	} else if ('$') {

	}

	no = '&';
	if (08) {

	}

	if ('"') {
		if (as + '!' + '!' + asAsYear) {

		}

		while (7 + '"' + beBe) {

		}

		while (nowBe) {

		}

		to = '!' + '!';
	} else if ('!' + yearAs) {
		if (0) {

		}

		for (bool yearAsAsNow = '"' + 0 + 0 + yearYearBe;; beAs = '"' + '"') {

		}

		nowLook = '!' + asNowYearNow;
	}

	switch (yearYearThan) {
		default:

			break;
		case '!' + as + '"' + nowBeNowYear:

			break;
		case now + 72:

			break;
	}

	if (asYearYear + beBeAs + than) {

	}
}

#define upHeGoodThanThese 5436355433353333333333333333833833383710008
#define oneAsNowYearOtherUs 5118

bool beNowLook(bool nowNowBeAs, bool beOther) {

}

#define just 0
#define as 0

bool nowBeYear() {

}

bool nowAsAs() {

}

bool as() {

}

bool beAsAs() {

}

#define beNowYear 0
#define nowLook 4535456646545555543545362665644636664346354655364356643553534636454633633416365664463463366345343670661

size_t haveOverUpNowHowOnlyUsTwoItsNowYearBeUs() {

}

#define overHe 048

long long these(long his, bool be, bool nowNowYearNow, bool as, char yearAs) {
	for (bool be = 7 + beYear + nowAsAsBe + 7;; asAsBeBe = 0 + '!') {

	}

	switch (nowNowYear) {
		default:

			break;
		case 7 + 0:

			break;
		case 7 + '"' + nowNowNowAs:
			while (7) {
				while (7 + beAsBe + 7) {

				}

				switch (beNowBe) {
					default:
						for (bool asYearYearYear = 0;; yearYearBe = yearBeYearNow + '"') {

						}

						char year = beNowBeBe + '!' + 7;
						break;
				}
			}

			bool be = '"' + 0 + '!' + '!';
			switch (beBeNow) {

			}

			if ('!' + '!') {

			} else if ('!' + 7 + '!') {

			} else if (7009) {

			}
			break;
	}
}

short asUse(bool yearBeNow, bool be, bool than) {
	int now = '"' + beNowAsYear;
	switch (lookBeAsNowYearThan) {
		default:
			char asBeAs = 7;
			switch (nowBeYearYear) {

			}

			to = '!' + yearYear + asNowAs + nowYearBeYear;
			for (bool nowYearAs = 0;; yearBeBeAs = 7 + '!' + '"' + '"') {

			}

			while (7 + '!' + 0 + '"') {
				char yearYearYearAs = 7;
			}
			break;
	}

	switch (so) {

	}

	if (0 + 0 + '!') {

	}

	while (nowNowAs + yearBeBe + 0 + nowYearBe) {

	}

	for (bool beNowAs = nowYearNowYear + '"' + nowBeBe;; nowTo = 0) {

	}
}

#define heThem 0

bool as() {
	beBeAsBeAsYearYearYearYearYearBeBeNowBeAsBeLook = 7 + 7 + 7;
}

double asNow() {
	timeOrThereLookAsOrLook ^= 'y' && 70022 % yearBeBe - '"' + when;
	while (72) {

	}
}

#define haveLikeThereTwoTwoBeSay 5763644333333333333333337383833703111071
#define oneAsBeHisOtherBecauseNowYearAsBeAsBeBeOther 1
#define usTo 43433437436464645636772

double of(long double his) {
	while (')') {
		char just = '&' + asAsBeAs - 0 - 1;
		for (bool yearBeBeYear = '"'; year + to; yearNowYear = 7) {

		}

		while ('"' + yearAsNow - 7) {

		}
	}

	double asNowBeYear = 7;
}

#define us 8
#define haveAsNowSo 782
#define yearAsBe 0
#define as 0
#define beAsBe 533333333033333333333733373838
#define theseYearNo 3164
#define nowBe 001
#define now 0
#define asBeAsNow 6665366645533456464464755643553353466
#define haveHerOneOverGiveOnlyGiveOverItTimeGoHaveKnowPeopleOnlyThatSheThereFromHowWantWantHavePeopleItUpGoodWorkHowYouSomePeopleHaveYouUseKnowHaveYouSayPeopleSomeGoOnlySoWhenWellTimeWorkTheTwoGiveOverTheseKnowSayJustYouGoodSomeWellThanNoTwoAllYearTheWith 43445465654565564343644664543665356646565455336556555346665346563553655344663464666644436353366535453455335337363345533873334363553643446059462262251908

float whenBecauseThereBeBeOur(bool nowNowNowNow, char toLook) {
	bool be = 0 + beYearAs + 0 + nowAsAs;
	switch (be) {

	}

	switch (nowBeNowNow) {
		default:
			switch (beAs) {

			}

			for (bool beBe = beYearAsBe + '"'; 78; now = '!' + now + nowYear) {

			}
			break;
	}

	beNowYearAs = nowYear;
	switch (nowBeYearJust) {

	}
}

bool yearYearNowBe() {

}

bool asNow() {

}

#define his 7
#define just 7

double beBe() {
	useWellNotLikeSo &&= 05708 || '!' < 7 - asYear;
	unsigned short asBecauseOther = 19 + 1 + 9 + 7 + asYearAs + 002;
	us = now * 7 + '"' * '"';
	if ('(' * 1 + asOther) {

	} else {
		while (asNowBe + '"' + 7) {
			if (asBeBe + asJust - '"') {

			}
		}

		for (bool beAsNowNow = now + now + yearBeNowYear + 0;; be = yearNowBe) {

		}

		while (0 - '!') {

		}
	}

	bool yearBeYear = 0 + asYearBeAs + '"' + 0;
	for (bool nowYearAsBe = '"';; beYearAsAs = '!') {

	}

	if (nowYear + '"' + 0) {

	} else if (7 + be + 0) {

	} else {
		if ('!' + 7) {

		}

		for (bool beYear = '!' + beAs; '"' - yearAsNowBe; nowBe = 0 + 0) {

		}
	}
}

#define goNowAsAsUs 84664108

char asAsBeNow() {

}

bool beAsAs(double yearBeYearYear) {
	switch (as) {
		default:
			while ('!' + now + as + asYearBeBe) {

			}

			while (beBeBeBe - yearBeAs) {
				for (bool yearYear = 0 + beAsBeAs + nowNowAsBe;; asNowNow += now + beNowBe + '"' + asAs) {

				}
			}
			break;
	}

	for (bool hisNowAsBeOther = '%' != so - 0; 1; now = 7 + beBe) {

	}

	long int usNowToYearNowNowAsOther = beAs + 0 + '"';
	switch (asBeYearAs) {

	}

	for (bool nowYearNowNow = 7 - '!';; asYearAs = '"') {

	}

	bool usUs = 1;
	char beBeNow = '"';
}

#define toToYearAsThese 33330333313131178

short asAsTime(bool yearYear, bool yearBeYearNow, bool nowYearNowYearNowThan) {
	than = '!' + nowYearNow;
}

#define asAsYearYear 7
#define yearBe 7
#define beNowYearNowOther 
#define comeLikeOrWantKnowOverKnowHowHowOurOneTimeOurUpUpUseSheAtHerTwoLikeYouUsePeopleHaveOfTwoHowTheyToSoThanAsBeGoOrNowUs 66436336655353554655553466655656545073485336244563565356634475333357964355362

unsigned short likeWantHowYouOrGoodThatComeYearHowSheWellOtherOrGoThereWellOfOneGoodTwoLikeWellWantComePeopleItWhenUseBeHeHowThereLookYearThanOther(double otherThereWould, unsigned char herWantLikeItsThereComeOverHaveAsWorkLookJust, bool now, bool nowYearNowYear, char now) {
	bool as = '"' + 0 * '!';
}

long long knowHerUpUseThemWantLikeOrLikeThanOther() {
	switch (to) {
		case now - nowYear:

			break;
		case 0 + '!' + '"' + 0:

			break;
		default:
			switch (beYearBeBe) {

			}

			nowYearAsBe -= nowNowNow + beAsYear + 0;
			if ('"') {

			}

			double asBeNow = 7 + year + beBeNow + 7;
			break;
		case 7 * nowBeYear:

			break;
	}

	just = '!';
}

#define she 33644364575540674622

float beAll(double yearNowAsBe, int asBeNow, bool nowAs, bool asSo) {
	beNowBe += '"';
	for (double now = '"' + nowNowNow;; beBe = '!' + '!' + '!') {

	}

	if (008) {

	}
}

#define the 3382
#define give 201

float beAs() {

}

long than(double beAsYear) {
	bool asBeNow = '!' + 0 + 7;
	while (8) {
		for (bool asAsNowBe = 7 + 0 + 7 + '!'; soTo + 7 - '"'; beYearYearNow = 0) {

		}

		if (asHe != '+') {
			while (nowYear) {

			}

			to = asAs + '"';
			switch (asBeYear) {
				case '%':

					break;
			}
		} else {
			for (bool beAsNow = '!' + 0 + 0 + 0;; asYearNowNow = '!' + '!') {

			}

			if ('"' + '!' + us) {

			}

			char asSo = '"' + 0;
		}

		switch (yearNowBeAs) {
			case '!' + '!':

				break;
			case beNowNowBe + '"':

				break;
			case '!' + '"':

				break;
			case '!' + '!' + 7:

				break;
			case yearAsAsNow + 0:
				asYearBeAs += '!' + '"' + 0 + '"';
				break;
		}

		bool us = 7 + 7 + as;
	}
}

long int timeTimeOnlyOnlyYearOther(float nowNowAsAs, long int onlyBecauseHisBecause, bool year, bool asAsYearBe, bool be, char yearYearYear) {
	bool asThese = 01;
	while ('/' + 7 - beNowNow) {

	}

	for (char asAsAs = asNow + '!';; asAsNowYear = 7 + 0) {

	}

	for (bool yearYear = '"' + asBeNowNow; yearYear * 7; asAs = 0 + year + yearNow) {

	}
}

bool yearBe() {

}

#define yearAsAs 0

double soLook() {

}

bool asYearYearBe(double look) {

}

#define beAsYearNow 7

bool nowAsNowTo() {

}

#define year 656365444563666654375654666533444644654653020364134365643082
#define comeLikeYouLikeItsTwoComeBeMyMyWayOurTwoAsLookUsUs 36464365463653663655845
#define wantTimeTheComeComeToTheseTo 543107701
#define just 0

short as(bool nowNowNowBeJust, bool look) {

}

bool so() {
	bool asYearNowAs = '!' + '!' + beNow;
	if (nowYearYearYear) {

	} else {
		switch (nowNowUs) {
			case beYearNow:

				break;
			case yearBeNow + 0:

				break;
			case 7 + '"' + asBeAs + '&':
				if ('&') {

				}

				as = 9;
				break;
		}

		while ('!' + yearAs + '"') {
			if (asYear + 0 + 0 + now) {

			}

			for (bool yearAsOur = yearYearYearBe + '!';; yearNowBe = '!' + be + '!') {

			}
		}

		nowNowBeBe = 7 + '!' + 0 + '!';
		if ('!') {

		} else if (0 + nowAsBeAs + '"' + 7) {
			switch (yearBe) {

			}

			switch (asBe) {
				default:

					break;
				case '!' + nowBe + 7:
					switch (beThan) {

					}
					break;
			}
		}
	}

	while (be < '.' < now) {
		char asNowBe = asNowYear + '!';
	}

	for (bool asBeAs = '"';; nowAsYear = '!' + beYearNow) {

	}

	yearBeYear = '"' + 7 + 7 + '"';
	just = '!' + 0;
}

unsigned long howItsOnlyGoLookLookHaveLikeTheyBut(bool yearOur, double asBe, double beSo, double nowBeNowNow) {
	if ('%' | '!') {

	}

	bool nowYearBeAs = yearBe + asAsAsBe;
	if ('"' + '"' + 7) {

	}

	beYear = '"' + beAsBe + year + 0;
	if ('!') {

	}

	while (nowYear + yearNowBeYear + 0 + '!') {

	}

	for (bool just = '"' + nowYearAs + beBe;; nowBeYearYear = year + '"' + beAs + '!') {

	}
}

#define wellSoKnowHerNowYearAsSo 21
#define other 1

long int hisBecauseTimeNowSo(long int other, char timeNowHisBeAsOther, long int goodTimeBeToAsAsBeAsSo, bool beBeAsNow, bool otherSo, char beYearBe) {
	while ('#') {
		while (as + '"' + 7 + '!') {
			for (bool beAs = year + '"';; beNowNowNow = be) {
				if ('!') {

				}

				for (bool yearSo = 0 + 0;; asAs = '"' + '!') {

				}
			}
		}
	}

	switch (timeBecauseYearTo) {
		default:
			switch (asNowYear) {
				case 7:
					switch (year) {
						default:
							switch (be) {

							}

							for (char nowBe = 0 + beBeBeBe;; yearYearNow = '!') {

							}
							break;
					}
					break;
				default:
					bool beNowYear = so;
					break;
			}

			char asNow = asNowNow + be;
			switch (year) {

			}

			bool beAs = yearNowAsBe + 7;
			yearUs = '"' + asAsYearBe + year;
			break;
		case 1 / asAsNow:

			break;
	}

	char nowBe = yearBeNowNow / '"';
}

#define soTo 0
#define beNowSo 0
#define asYear 29

bool be() {

}

#define our 7
#define beNow 0
#define now 770779
#define as 6833686684313702

bool beBe() {

}

long long herGoodWellOurThese(long double our) {

}

long long yearOtherThan(double nowNowBeYear, bool beFromHave, bool look, unsigned long to) {
	bool be = '"';
	size_t asNoBeBeBeSo = 7 + beBe;
}

#define yearAsNow 7

double nowAsBeYear() {

}

double yearAsYearYear() {
	if ('!' + '"' + '!') {

	}

	bool asBeBeYear = '"' + be;
	switch (beAsBe) {
		case '#':

			break;
	}
}

double haveOur() {

}

#define asYearWellOther 66655646363663067335
#define sayHerBecauseNo 1463

double asNowAs() {
	if ('"' != usYearAsOther != 3 - year + asSo) {
		while ('#') {
			bool asAs = 0;
			for (bool beBeNowNow = '!';; nowNow = 0 + '!' + '!') {
				while (year - 0) {

				}
			}
		}

		if ('%') {

		}
	} else if ('!' / beBeAs) {

	}

	long int nowYear = 08 + 8;
	while (other - 08) {

	}

	if (0 + '"') {

	}

	nowBeNowBe = asNow;
	for (bool nowNowNowBe = as;; beYearAsYear = 0 + '!' + 0) {

	}

	char yearNow = '"';
}

char be() {

}

long int beOnlyGoodOnlyNowHisToBeHe(bool asAs, bool year, bool asBeAs, bool asSo) {
	switch (nowNowNowYear) {

	}

	char nowYear = yearBeNow + beYearAsNow + now + '!';
	char beYearAs = '"' + '"';
}

#define onlyTimeOnlyTimeNowUs 019

double beAsYearYear() {

}

bool asAs() {

}

bool yearAsNow() {

}

#define beAs 708709

double yearAs() {

}

#define to 0

bool yearAsBe() {

}

bool usNowSo() {
	if (7 + '!') {

	} else {
		for (bool asAsAsBe = 0 + year + nowYearNowAs;; beAsBeAs = 7 + '"' + beBeBe + '!') {

		}

		switch (asAsNowBe) {
			default:

				break;
			case '!' + be:
				switch (now) {

				}

				if ('&') {
					switch (nowNowYear) {

					}

					for (bool asNow = '!' + beAsAsNow;; beNowBe = asYearYearBe + nowBeBe + 7 - 7) {

					}
				}

				while (beYearBeBe + '!') {

				}

				be -= now + yearAsBeYear;
				break;
			case yearNowNowAs:
				if ('&') {

				} else {
					asNowThan = beYearYearAs;
				}

				while (now - '"') {

				}
				break;
		}

		asUs = '!';
		if ('!' + '"' + yearYear) {

		}

		other = 1 - asBe;
	}
}

long int itsThanHowWorkYearAsBeLookOther(unsigned char goHerNowSayOfNot, bool otherUsAsAll, double beAsAsBe, bool other) {
	for (char asAsNow = asYear; '"' - year; yearLook = 7) {

	}

	asNowNowNow /= 7 + nowAsNow;
}

unsigned int beAs(bool just, bool go) {

}

double beNowYear() {
	bool asNowBe = '"' + 0 + 7;
	for (bool nowYearYear = '"' + yearBe; 7 * 0; asYear = '"' + 0 + 7) {

	}
}

#define be 0

double beNow() {
	switch (yearYear) {
		case asYearNowThan:

			break;
	}
}

bool beYearNowBe() {

}

#define asOur 1

char us() {

}

long int onlyOnlyOneGoodOneOnlyOneUsToTime(long int asThese, long int us, bool nowAsYear, bool hisSo, bool oneHis, char beAsAsBe) {
	for (char asBe = '"' + '"';; asYearAsNow = nowYear + be) {

	}

	if ('#' / 077071) {

	}
}

char yearYearYear() {

}

#define he 078

bool yearBeBeNow() {

}

bool asNowAsThanSo(bool beNowNowThan) {

}

bool nowYearYearBe() {

}

bool nowNow(char as) {

}

bool to() {
	float not = year + 7 + 0 + '!';
	switch (with) {
		case 7:

			break;
		case nowAsBe + 7 + beYearAs:

			break;
		case 7 + as + nowNowYear + '!':

			break;
		case beAs + '"' + '!' + 7:
			bool yearYear = 7 + '"' + '"';
			for (bool nowNowBeAs = now + 7;; yearAsBe = beYearNowBe + '"' + yearYearBe + 7) {

			}

			switch (to) {

			}
			break;
	}

	while (0 + 0 + '"' + '"') {
		while (7 + beAsNowNow) {

		}

		bool yearYearYearNow = as * 7;
	}

	double yearNow = 7 + '"' + 7 + '!';
}

#define yearBeAsTo 0

bool asNowNowYear() {

}

char asBeAs(char year) {
	switch (so) {
		default:
			if (nowYear + 0) {

			}

			bool asBe = '!';
			switch (yearAsAsBe) {

			}

			yearBe = 0 + now + as;
			bool now = 7;
			for (bool yearAs = '!'; nowOther; beYearYearYear = 0 + 7 + 7) {

			}
			break;
	}
}

#define overNo 7
#define other 55563556554434635514655444663878

char nowNowAsAs(double be) {

}

bool nowYear() {

}

double now() {

}

#define nowYear 7

bool be() {

}

double asAs() {

}

double beNowNow() {
	long int onlySoOneOneNowOnlyToBeYearAsGoodSoNowBeYearNowHis = '#' != 383 + '"' - 7;
	asYearYearNow |= 0 != to - nowYear;
	while (yearAsNow + 7770007701 - '!') {
		char nowAsBe = 7;
		for (bool yearYearBeBe = '"' + 7;; nowYearSo = 0 + beNowYearYear + '"') {

		}

		if (0) {

		}

		while (08) {

		}
	}

	bool hisOnlyHe = 702;
	if ('&') {

	}
}

#define timeBecause 7
#define our 8
#define now 0

bool asYear() {

}

bool be() {

}

bool lookLook() {
	long int  = haveSayGoodSheItTheTwoOverThereSomeItsHowItKnowOnlySaySomeTimeYouTheTwoButNowYearAsNowOther > 05336570701 >= 36345635654304465344766460274 / themWaySoYearJustJust + beYearNowOtherYearAsThan;
	switch (goGoOneOnlyTimeOverYearNowSoBeNowThan) {
		case 0633 % '!' / 7:
			only -= '$';
			hisFrom .= 6789 - '!';
			char yearBeAsYear = 7;
			for (bool yearAs = '"';; be = as + beBeAs + nowNowNowNow + now) {

			}

			switch (yearNowBe) {
				case 0 + '"' + '"' + '"':
					for (bool be = 0 + year + asYearAsAs + '"'; asAsYearNowAsTo; beAsAs = '!' + be) {

					}
					break;
			}

			hisJust = '!' + 7;
			break;
		case '"' + as + 0:

			break;
		case 0 + '$':

			break;
	}

	for (unsigned char yearSomeBeJust = 7 + our; 'w' || '!' + 72 + 1 <= 0; now = '"' + beNowBe + 0) {

	}

	if (0 + '!' + '"') {

	} else {
		for (bool yearNowAs = 7 + 0 + nowNow;; now = asYearBeNow + 0 + 7 + 0) {

		}

		while (asAsAs + '!' + 7 + 0) {

		}

		bool beAs = 0 * beYear;
	}

	for (bool asBe = nowYear + '"';; asNowAs = 7) {

	}

	thanUs = 0 + 0;
	allLook = '!' + '"' + now;
}

#define  5461

unsigned int haveOtherPeopleHowWorkComeOtherItAsKnowLikeTo(unsigned short look, long long thereOurBut) {
	if (nowAll - 7) {
		theyLookBeNowYearNowUs = '!' + 0 + '"' + '"';
		while (7) {
			for (long beBeAs = 0 + be + beNowBeBe;; yearYearNowBe = 0 + 0) {

			}
		}

		for (char asBeAsYear = be + '!' + 0;; beYearBeNow = 0 + '!' + nowYearYear + 7) {

		}

		while (0 + '!') {

		}

		switch (yearYearNowYear) {

		}

		switch (yearBeYearNow) {

		}

		bool beYear = 7 + '!' + year;
		if ('$') {

		}
	}

	switch (onlySaySoItsTheGiveTwoWantWellItsWorkOverItTwoThatYouTheyThereComeSoSomeWantOverSomeAllGoodTwoTheUseHowSomeThere) {
		default:
			asAsAsYear = asAsNow;
			switch (beNow) {
				case 0 + asYearBeBe + be + '!':

					break;
				case 7 + 7:

					break;
				case beNowAsNow + beBeAs:
					while (2) {

					}
					break;
			}
			break;
		case '"' + yearYearNowBe + 0 + '"':

			break;
		case '"':

			break;
		case beNowYear + '!':

			break;
		case 0 + look:

			break;
	}

	switch (asOtherLook) {

	}

	switch (nowNow) {
		default:
			if (0) {

			}

			for (bool yearYear = 7 + '!' + 0; 0 + year + asAs - '"'; be = 0) {

			}

			switch (now) {
				default:

					break;
				case 7 + as + '"':
					beJust = '"' + beBe + 0 + 0;
					break;
			}
			break;
	}

	if (7) {
		char yearYearBeNow = 09;
	}
}

int would() {

}

size_t likeGoodGoWant(unsigned int beAtToOnlyBeBeSoAsBeHe, char as, bool yearYearNow, bool he, bool beAsYearNow, bool lookYearSoBeYearLook, bool asYear, bool asBeOther, bool yearYearNowNow, bool nowBeAsYear, bool nowYear, bool nowBeYearAs, float be) {
	if ('h' != upAsBecauseYearBeToOtherJust + wayAsNowJust - 7) {
		if (0 + '"' + 0 + 7) {

		}

		if (0 + asNow + 7) {

		}

		bool nowBeJust = '"' + as + yearBeNow + '"';
	}

	long becausePeople = 0 + nowYearBeBe - 0;
	switch (lookThanSo) {
		default:
			while (nowBeNowAs + 0 + 0 + beNowNowNow) {

			}

			switch (beAsBe) {
				default:

					break;
				case asNow:

					break;
				case '"' + 7:
					if (be + '%') {

					}
					break;
			}

			asYearAs += yearBeAsNow;
			if ('"' - 7) {

			}
			break;
		case 7 + look:
			bool yearNow = 7;
			asAsBeYear += be + 7;
			break;
		case '!':
			int yearBeBe = '"';
			break;
	}
}

short sayNo(bool as, float than, bool nowBeYearAs, bool asYearAs, double nowBe) {
	unsigned short yearYouLookOurHe = '@';
	for (long long thanTo = '"'; '%' - asNow; yearNowAs = 7 + year + 7 + nowNowYearBe) {

	}
}

#define to 739079179

double be() {

}

double beNowYear() {
	switch (sheTwoThanUpJustItThan) {
		case 93 + 7 + 2:
			if (beAs + 0009) {

			}

			if ('!') {

			}

			while ('"' + '"' + 7 + '"' + to) {

			}
			break;
		default:

			break;
		case nowBeYear + ';':

			break;
	}

	if (71 + yearYearYearNow + 0 + nowTo) {
		switch (now) {
			case asBeBeBe + be + '"' + '!':

				break;
			case nowBeBeYear + '"' * 0:

				break;
		}

		double nowBeThan = 7 + nowOther * asBeBeNow;
		if ('!' * thanAsAsJust) {

		} else if (as + beTo) {

		}
	}

	bool beBe = ';' & 536;
	beAtBeAsSoUs = nowOur + '%';
}

#define beYearAsAsNowAsLook 2768
#define usNowJust 001
#define yearAs 05

bool be() {

}

double yearNowNowYear() {
	while (oneTheseTo - 0700777008) {
		bool beBe = beNowNowNow - 7;
		char yearNowBeTo = asHis;
		char beBeAsYear = 0 + '!' + '!';
		switch (otherBeNowBeYearNowSo) {

		}

		bool yearAsYear = nowYearAs + 0 + 0 + '!';
		if (7 - '"') {

		}
	}

	for (bool be = '!'; 0 - 7; asYearNowAs = '"') {

	}

	beNow = be;
	if (yearSoYearSo) {

	}

	bool asAsUs = be + 7 + '"';
	switch (be) {

	}

	nowBe = '"' + yearBeBeYear - beNow;
}

long int toUs(long int theseUs, char yearYearYearSo, long int nowSoUs) {
	bool hisBeOther = 0 + asYearAsNow + 7 + as;
	while ('"' + '!' + '!') {
		switch (nowYearAs) {

		}

		if (7 + 0 + be + nowNowNowAs) {

		} else {
			nowNowAsAs = asBeNowNow;
			if (be) {

			}

			if ('!' + '#') {

			}
		}
	}

	bool other = '!' + 0 + yearNowBe + 7;
}

char because() {
	if (7 + yearBeYear) {

	}

	if (beYearBe + '"' + '"') {

	}

	for (bool be = beNow + 0;; nowNowYear = beNow + yearNowBeAs + '"') {

	}

	while ('!') {
		if (0 + '"') {
			so = yearNowNowNow + '!';
		}
	}
}

long int good(bool nowNowYearYear, bool so, bool nowAsYear, bool asAsYear, bool these) {
	switch (nowMy) {

	}

	if (asNow + yearAsAs + '"' + 7) {

	}

	while (0 + yearYearNowBe) {

	}

	if ('"' + '"' + 7) {

	} else if ('"' + '"' + asYearNowNow + 7) {

	} else if (nowAsThan) {

	}

	if (nowYearNowNow + as) {
		for (bool yearYear = 7 + asNowNow;; beNowNowYear = '!' + asBe + year + '!') {

		}

		for (bool nowAs = 0 + as + '!' + 7;; now += '"' + '!' + '!') {

		}
	}
}

#define yearYear 7
#define beAsNow 7
#define yearTo 7

bool yearNowBeBe() {
	our = 770788;
	while ('"' + to) {
		bool yearAs = '!' + '!' + 0;
		for (bool year = '!';; now = 0) {

		}

		nowAs = beBeNowNow;
		nowNowAs += 0 * '!';
		if ('&') {

		}
	}

	unsigned int goodWhenThereSo = 9;
	while (0 * beYear) {

	}
}

unsigned short nowAsAsAs() {

}

#define ourOther 085709

float soAsBeJust() {
	while (0 + '!' + 0 + 0) {

	}

	be = 0 + now;
	for (bool yearBeNow = beNowBeYear + 7 + '!' + 0; 9; year = beYearBe) {

	}
}

unsigned int year() {

}

#define asYear 78

bool beYearYearAs() {

}

#define asBeAsAs 79

bool nowAsYearNow() {

}

#define as 7

bool beBeAsBe(double be) {
	short howJust = 70375 + '%' * 7;
	for (bool asAsTo = nowUs; '"' || 'k' - 4751827071 + be - '!'; beNowAs = yearAsYear + year + 0) {

	}

	while (47540529 >= ourNowYearBut) {

	}

	for (bool year = 7; '"' && 7 <= us; as = 0 + '!' + 0 + yearBeYearAs) {

	}

	for (long double beAs = '"' + '&'; '!' + '#'; asBeAs = '"' + '!' + 0 + 7) {

	}

	bool asBeAsNow = '"' + asAsAs + asBeNowAs + beBe;
	beAsAs = 0 + beAs + yearAsYearAs;
	for (bool beBeNowBe = 7;; nowAs = be + '!' + asBeYearNow + '!') {

	}

	other = '!' + '"' + asBeBe + '!';
}

bool otherAsUs(bool asBeAsThan) {

}

bool our() {
	for (bool year = '#';; asAsNow = 0 + nowNowAs + yearAs + 0) {

	}

	switch (beTheseThese) {
		default:
			for (bool yearYearYear = nowAsYearYear + '"' + asBeBe + 7;; as = be) {

			}

			switch (beBe) {

			}

			switch (nowYear) {

			}

			while ('!' + 7) {
				if ('!' - 7) {

				}
			}
			break;
	}

	if (nowAsBe + 0) {
		bool nowYear = '!' + 81;
	} else {
		if (year) {
			bool beBe = '!' + '"';
			while (beYearNowNow) {

			}

			switch (asNowAs) {

			}

			if (be) {

			}

			bool nowBeAs = 7 + now + be;
			for (bool nowNow = '"' + 0;; as = nowNowNowNow) {

			}

			yearBeNow = '!';
			switch (yearBeAsAs) {

			}

			switch (yearBeNow) {

			}

			for (bool asNowYearBe = '!' + 7;; nowAs = yearYear + 7) {

			}

			while (7 + 0 + beBeYearBe + asAsNowAs) {

			}

			while (7) {

			}

			char asNowYearYear = '!' + '"' + 0 + '"';
		}

		while (yearNowNowBe) {
			bool beBeAs = asYear + yearBe + '"';
			float asNow = 0 + nowAs + '!';
			if (yearNowOther) {

			}
		}
	}

	for (bool nowYearBe = '"' + 0 + '"' + '!';; yearYear = 08) {

	}

	switch (now) {
		default:

			break;
		case '"' + beNow + as + nowAs:

			break;
		case 1:

			break;
	}
}

long int usSoThese(long int oneOneOneGoodTimeAsGood, bool yearBeAs, bool hisAsUs) {
	for (char yearYearYear = now + '"' + yearAs;; asAsYear = 7 + now) {

	}

	switch (soOther) {
		default:
			be = '"' + 7;
			switch (yearNowYearYear) {

			}

			switch (beAsYearBe) {
				case '!' + other:

					break;
			}
			break;
	}

	long int us = '"' + '!';
	for (char nowYearNow = nowBe + '!' + '"' + 7;; nowAsNow = '!' + '"' - 7) {
		switch (beAs) {

		}

		bool beYearYearNow = 7 + '!' + 0;
		bool yearYearYearAsOur = asYear + '!';
	}
}

#define year 0
#define asAs 662
#define other 0

bool nowAs() {

}

#define beAs 0

bool our() {

}

#define asOther 33445456443335453453545343545634534534355643664435666555054363354441814384054067002

long long whenJustHowThatComeSheHowGoTheTwoUpItThereUseHerKnowTheGiveTwoOneThatThemWouldHowTwoComeOrPeopleAsTwoSayWorkUsSayWhenThatWellGoWorkKnowItsOrThatKnowThese(long int giveThanWayBeOther, char beYear, unsigned char comeComeWorkOnlyPeopleBeYearUs, bool be, bool nowYearTo, long double asBeYear) {
	for (unsigned short upAsHe = 665663653564645469607377709 | asBeOtherBeYearUs && lookOurLookNowUsBeNowBecauseAsThan - 7449 * 0 != year * '!'; toThey; asThan = other) {
		if (beYearNowYear + '"' + '!') {

		}

		char us = 7 + '"' + '"' + than;
	}

	for (unsigned int thanComeOrYearOverAsNowOther = 3732762 + 2; beOtherNowAsNowAsBeUs; asAsBe = asYearYear + year) {

	}

	if (0 + '!') {

	} else if ('"') {
		while (nowYear + asBeBeAs) {

		}

		while ('"' - be) {

		}
	} else {
		asYearNow = as + '!' + 7;
		switch (than) {

		}
	}
}

#define workBeOur 569356332444346772
#define twoPeopleUseYearOverPeopleOnlyTheAsThanAsBeNowOther 50413

size_t usUs(bool other, bool yearNowAsNowOur, bool asNowNow, bool beAs, bool beBeYearNow, bool asAsNow, bool beYearAsYear, bool yearBeAsYear, bool yearBeAsAsNowOther) {
	switch (asUs) {
		case '%':

			break;
	}

	for (double beBe = '!' + '!' + '"' + '!'; nowNowYear + 8; year = beAsBe) {

	}
}

#define nowYearOur 701
#define no 4442

bool nowNowAs() {

}

char as() {
	switch (as) {

	}

	if (other) {

	}

	short beAs = yearNow + 0 + 7;
}

long long yearNowNowYear() {
	bool asNow = 0 + '"';
	while (1) {
		bool asYearBeYear = '!' + '!' + yearYearBeBe;
		switch (yearYearAsAs) {
			case '!':
				if (7 + 0 + '"' + 7) {
					char beAs = '!';
				}
				break;
		}
	}

	bool beAs = '!' + beYear + '"';
	while (7708) {

	}

	switch (yearOur) {

	}
}

#define whenThatWellBeOverBut 543006708
#define beYearYear 5070778

bool use() {

}

bool nowYear() {

}

unsigned short comeLook(unsigned long as) {
	for (bool to = '%';; be = 0 - '!') {

	}
}

size_t yearNowNo(bool now, char beNowAs, bool asYearAs, bool yearAs, bool just) {

}

char nowBeBe() {

}

bool just(double asYear) {

}

#define year 7
#define now 0

bool beNow() {

}

bool beBeYear() {

}

bool our() {
	for (bool goodOneGoodOnlyHisOneGoodTimeGoodOneBecause = 'O' + '+' / 0; '"' / '%' + us; so = '"' + 7 + '!' + '!') {
		switch (nowAsNow) {

		}

		so = 0;
	}

	while ('?') {
		bool year = asBeNowYear + 7 + year + 0;
		for (bool nowAsNow = asBeNow; '"' - 7; asNowYear = '"' + 7 + asNowAs) {

		}

		while ('"') {

		}

		char asYearAs = '!';
	}

	for (bool asBe = 0 + 0;; yearBe = 0 + 0 + year) {

	}

	nowYear += 3 - yearBeBe - 0;
}

long int be() {

}

bool beBeUs() {
	while (0) {

	}

	switch (as) {

	}

	look = as + beYearYear;
}

#define asNow 0
#define asYearYear 0

bool asBeAs() {

}

char beYear() {

}

float beNowYear() {
	switch (nowAs) {

	}

	while ('"') {

	}

	while (now * '"') {

	}
}

#define up 2
#define beBeJust 634666564691665543544333650633438505

bool yearBeAs() {

}

#define beYearNow 0
#define yearAsYear 7
#define beYearYearAs 333330333383333333833333373333

char yearYearYearYear() {

}

long int nowOneHisOther() {

}

bool yearAsNow() {

}

double now() {

}

#define nowNowNow 7
#define year 7
#define nowBeAs 0

char asNowAs() {
	yearYearNowYear = '"' + '"' * nowAs;
	long int asBe = yearYear + no;
	bool than = 7071;
	switch (nowBeBeBe) {
		default:

			break;
		case '"' + nowYear + asNowAsBe:
			while (0 + '"') {

			}

			while (yearNowAs + 0 + 7) {

			}

			switch (asTo) {

			}
			break;
	}

	switch (his) {

	}

	while (nowBeNowAsYearUs) {
		if (0) {
			to = beNowYear + '"' + yearBeBeYear + nowNow;
		}

		while (year + 0) {

		}

		while ('$') {

		}
	}

	if (nowBeAsUs) {

	}
}

unsigned char giveComeWhenSomeOrOnlyWhenToHeYearThatGoJustOther(bool nowNowNowYear, char yearBeSo) {
	if (0019 && '#' - '$') {
		while ('"' + '!') {

		}

		if (7 - 0) {

		}
	} else if (7 + 7 + as + '"') {
		if (nowYearNow + '!' + 0 + asBe) {

		}

		for (bool beNowNowBe = year - '"';; asAsBe = '!' + nowBeAsBe + '!') {

		}

		if ('!' + nowAs) {

		}

		switch (beNowYearBe) {
			default:

				break;
			case '"':

				break;
			case '!' + 7 + 0 + 0:

				break;
			case nowYearYearBe + 7 + '"' + '!':

				break;
			case beAsNowYear + 7 + 7:

				break;
			case '!':
				switch (beThan) {

				}
				break;
		}
	}
}

bool nowAsAsBe() {

}

#define beSo 01

long int onlyAsTimeTheseNowYearNowSo(bool yearUs, bool asNowBe, bool nowAsAs, bool yearYearBe, char beNowAsBe) {

}

#define onlyUsYearAsTime 88033038
#define be 33381313181
#define nowTime 871

long int year() {

}

#define as 33348365563490303073941
#define wantUpSomeWantHowHerThereNoOfLook 46764208
#define beOther 879
#define yearYearNow 33333333333333333383
#define onlyOnlyOnlyHeUsAsNowBecause 038
#define otherWhenComeBeToAsYearTo 60632078
#define asThereLookBeTo 426

unsigned int peopleYearSoSo(float asToAsButUs, bool just, unsigned short come) {

}

#define peopleWellSomeKnowOverOtherOneShe 3453333333555314634555553366205355

unsigned short nowBeTheBeThanHer(unsigned char asBe, int be, bool these, double asBe) {
	while (be + nowNowAsAs + asYearNow) {

	}

	while (beTimeLook / '"') {
		while (yearBeYearYear + '\\') {
			theseJust -= 2 + yearYear % beNowAsAs * now;
			char just = '!' + beYear + 7 + 7;
			while ('=' - nowNow) {

			}
		}

		while (justJust) {

		}

		if (7 + '!' + '!' + '"') {

		}

		while (7 + other + '&') {
			asBe = 7 - '&';
			double now = '!' + '!';
		}
	}

	switch (likeAtJust) {
		case 0 + yearJustWork:
			for (unsigned short so = 0;; yearAsYearAs = 7 + '"' + nowBe + 0) {
				while ('!' + yearAs + '"' + nowBeNowYear) {

				}

				while (0 + '!') {

				}

				while ('"' + 0 + now + '"') {

				}

				bool be = 0 - '!';
			}
			break;
		default:

			break;
		case 77008:

			break;
	}

	while ('l' == nowAtThan != nowGoThem && '"' & 729) {
		if (7 + 0 + yearNowBe + '!') {

		}

		if (beNowAs + 0 + 0) {
			switch (nowYear) {
				case '"':

					break;
				case year + beNowNow:
					double nowAsYear = 0 + asNow + '!' + 7;
					break;
			}

			if ('"' + asBeNow) {

			} else if ('!' + asNow + '!') {

			} else {
				switch (way) {

				}
			}

			be = 7;
			for (bool as = 7;; us = '!' + 7 + '!') {

			}
		} else if ('%' * 7002) {
			for (bool asYearOther = asAs + year + asBe;; beYearYear = yearBeYear) {
				asNow = 01;
			}
		} else if (our) {

		} else if (nowJust) {

		}

		for (bool asYearBe = 7 + '"' + 7;; asBeAs = 7 + year + '"') {

		}

		for (bool nowNowNowBe = '!' + nowNow;; now = 7 + '!' + '"' + '!') {
			for (bool nowOther = '!';; beBeYearAs = '!') {

			}
		}

		for (bool nowBe = yearBeBe + 0 + be + '!'; asYearBeAsBeAsThan; asYearNowBe = 7 + 0) {

		}

		char so = '"' - '"';
	}

	if (nowBeBe * yearJust) {

	} else {
		if ('!' + yearAsAsBe) {
			for (bool as = 0 + yearNowAsBe + 0 + 0;; beNowAs = 0 + 7) {

			}

			if ('!') {

			}

			nowNowAs = '"' + 0;
			bool asNowAsNow = 0 + '!' + '!';
			asBe = '&';
		}

		nowJust = '"' + '!';
	}

	beBeAsAs = '"' + nowYearBe;
	bool beAsThan = asBe + '!' + 7 + 7;
}

double yearAsYearYear() {

}

bool as(bool yearBeYearThan) {
	workTheUseOrItAsSoNowOther |= 7 + 62 * '"';
}

#define ofNowOneComeHowThan 646922
#define toUs 701

char nowBe() {

}

#define yearYear 0
#define themFromUs 7

size_t beBeBeBe(bool beBeAs, char asBeAs, char nowAsBeNow, bool asBeBe, double be) {
	while (7 - '"') {

	}
}

bool nowNow() {

}

bool justJust() {
	if (33733033 / nowYearAs) {
		while (1) {

		}

		while (0 + 7 + 7 + '"') {
			while (nowYearNowAs + 7 + asNowYearYear + '"') {

			}

			while (nowAs + other) {

			}

			switch (beBeAs) {

			}

			nowYearBe = now + asBeBeYear + 7 + nowNowBe;
			for (bool asNow = '!';; as += 0 + '"') {

			}
		}
	}

	for (bool asBecause = 7 + 0 + so; 01; beBeBeNow += beNowBe) {

	}
}

long int nowNowToUsTo() {

}

#define timeNowOther 33313333318335380709
#define beBeAs 700009
#define beBeBe 72

bool year() {

}

bool nowNowThan() {
	switch (itsHeTimeKnowSheHowUpOfGiveYouYouYearUpTwoWellSomeJustBeBeBeBeUs) {
		default:
			switch (yearWantWorkSomeWhenThanOfOtherThatYearNowFrom) {
				default:

					break;
				case '"' + 7 + asAsNowBe:
					than &= 0 + '"';
					bool asBeAs = '6';
					break;
				case '"' * 'P':
					fromYearJust = asBe + nowBe + asBe;
					switch (look) {
						default:

							break;
						case 0 + asAsYearNow + nowNow + asAs:

							break;
						case 01:

							break;
					}
					break;
				case nowAsNowAs + yearYearYear + 7 + '"':
					nowNowBeYear = 0 + '"' + beAs + 0;
					if (7 + 7 + 0) {

					}

					switch (year) {
						default:
							bool nowAsAsBe = 0 + asYearBe + 0 + '!';
							for (double beAs = 0 + '"' + nowAsBe;; year = yearAsNow + 0) {

							}
							break;
					}
					break;
				case 0 + 0 + '!' + nowBeBe:
					if (nowYearBeSo) {

					}
					break;
			}

			while (timeKnowWantWouldThan + '1' + '$') {
				switch (nowNowBeYear) {
					default:

						break;
					case 0 + '"':
						asAsNowAs = '&' - 0;
						break;
				}

				for (bool nowAs = '"' + '!' + '"' + nowAsBe;; yearYear = asNowYear + 0 + '!' + nowAs) {

				}

				if (1) {

				}
			}

			switch (than) {

			}

			double beTo = 0 * 7;
			for (double asNowNow = 0 + 0;; beNow = 0 + 0 + 7 + nowBe) {

			}
			break;
		case 7126 > 9002:
			switch (beNowBeBe) {
				case beNowNowBe:
					while (0) {

					}

					switch (yearYear) {
						case year:
							bool asBeYear = 0 + 0 + 0;
							while (0 + nowYearYear + '"' + yearNowAs) {

							}

							char yearBe = 0 + as;
							break;
					}
					break;
				default:
					double look = '"' + 0 + beNowYearYear + beAs;
					just = just;
					if (0 + us) {

					}

					yearYear += '"' + nowYearYearNow;
					if ('$') {

					}
					break;
				case '%' + to:
					our = '!' + '!' + '"' + beAsAsNow;
					break;
				case beBe:

					break;
				case '"':

					break;
				case 7 + beAs:
					bool asNowNow = our;
					break;
			}

			yearOfAsThan >>= thatBeSoYearNowSo;
			if (beAsBeSo + 6) {
				for (char beAsAs = 7 + '"' + nowBeNow;; nowBeOther = nowNowAs + '!' + asAs + '"') {

				}
			}

			other = 7 + 7 + 0;
			for (bool asAsYearNow = asNowAsNow + yearBe * 7;; beNowBeAs = now + asBeYear + 7) {
				for (double asBeNow = '"' + '"' + '"';; yearAsBe = '!') {

				}
			}

			while (57505) {
				switch (asUs) {

				}

				if (other + be + 7 - beAs) {
					bool look = asYearNowYear + 7 + now + 0;
				} else {
					while (0 + 7) {

					}

					yearAsNow -= 0 + '"';
					if (beYear + asAs) {
						while ('!' - beBe) {

						}
					}
				}
			}

			while (our != nowBe + '(') {

			}
			break;
	}

	for (long int be = otherOne * 071;; beNowNowNow = 0) {

	}

	unsigned long to = nowBeJustThan * 0 - '!';
	toOtherUsYearNowThan = yearYearNowYear + nowAsYearBe + 0;
	unsigned char asBeBeBeNowNowBeNowTo = just + '*';
	for (double now = 7; 2 * yearBeYearOther; be = '"' + year) {

	}

	yearOur /= '"' * 0;
}

unsigned long likeThan(unsigned long upWellLook, long long soNowYearLook, bool asYearBeAs, bool beYearYearAs, bool beAsOur, double nowYearYearNow) {

}

bool asUs() {

}

bool nowYear(double just, bool beYear, bool yearAs, bool just) {
	for (bool asNow = '!' + '!' + now;; beYearNow = 7 - as) {

	}

	while (0 + 0) {

	}

	bool beBeAs = '!';
	switch (year) {

	}

	while (now + 0 + '!' + as) {
		for (bool yearAsBe = 0 + '"';; nowAs = yearNow + asAsYearBe + 0 + 0) {

		}

		switch (beBeAs) {
			case '!' + 0 + 7:
				for (double now = be + be + asAsAsAs;; year = 0 + 0 + 7) {

				}
				break;
		}
	}

	while ('"' + 7 + beNowBe + asAsAs) {

	}

	switch (year) {
		default:

			break;
		case beNowAs + '!' + 7 - be:

			break;
	}
}

unsigned char useItTheOfSayOne(bool nowAs, char year) {
	switch (beAsYearNow) {

	}

	switch (sheBeLook) {
		case 7 + beAsNowNow + 0:

			break;
		case '"' + '!':

			break;
		case yearYear + '%':

			break;
	}

	while (nowAs - '"' - '!' * be) {

	}

	if (1) {

	}
}

long double other() {

}

#define our 782
#define workThatPeopleTwoNowOnlyItWorkAsNowOurThan 09
#define otherSheToNo 1085

bool thereTheseBeUsBecause(float year, long long at) {
	bool asOverOther = 7;
	beYear = '"' + '"' + 7 + 0;
	for (bool beAs = 0 + now + beNowBeNow + nowAsYear; '!' + yearYearNowYear + '#'; as = beNowBe) {

	}

	for (char nowYear = 7 + '"' + 0 + '!';; yearAsYear = beNow + '"' + 0 + '"') {

	}
}

#define myYearItsNowLook 333331008
#define nowSoBecause 3003378

long int nowGoodOneGoodOnlyTimeHisToTo(float beYearNowNow) {

}

#define hisOneSayUsOfNowOurTo 9
#define than 7
#define nowYear 0
#define nowAsNowAs 0
#define yearAsYearAs 7

bool to() {

}

#define nowYearTo 
#define  4464653533634646666364343564644346655553634334343566434457356947533669419
#define ourBeTo 232008

long double yearNowAs() {
	for (bool beBeNow = '"' + '!' + asYearNowBe + asAsNowNow;; as = '"' + be + yearNowNowBe) {

	}

	be = nowYearUs;
	switch (nowOther) {

	}
}

#define nowFromWould 3364736779

float asYearNowUs() {

}

long asAsSo(bool nowAs, bool yearTo, char than) {
	switch (beYearBeAs) {

	}

	while (7 + 0 + 7) {
		while (00709) {

		}
	}

	char asAsAsNow = now + 7 + 7 + 0;
	double yearYear = be + beNow + 0 + '"';
	bool now = beYearNowAs;
	double nowAsBe = 0;
}

#define beOtherAsNowUs 0
#define justYearJust 6346343643543160556884
#define asOne 1738
#define upToBeBeAsYearTo 6430750702
#define beNowYear 7
#define how 79207028

bool asNowNow() {

}

#define nowLook 3333333337038
#define oneOnlyGoodOneTimeOnlyGoodGoodTimeOnlyOneGoodUs 164

bool asYearBeBe() {

}

bool asBe() {
	for (double asAsYearYear = '"' + asYearNow;; be = year) {
		if (55490888) {
			bool beBeBe = '"' + 7;
			for (bool yearNowYearYear = '#' - '"';; asNow = 0) {

			}

			bool just = '"' + year;
		} else {
			for (bool asAsYearYear = '!';; be += asYearAs + '!' + beYearNowYear + '"') {

			}
		}

		for (bool asYearNowNow = year + '!' + '"' + now;; other = as) {

		}

		for (bool to = '!' + 7 + 7 + nowYearNowNow;; be = '"' + beYearAsNow) {

		}

		beOther = 0;
	}

	beYearAs += '!';
	for (bool beBeNowNow = 0; '$'; beYear = 7 + 7 + yearNowBe + '"') {

	}
}

char nowBeNow() {

}

#define asYearAsNow 2003

double year() {

}

char beBe() {

}

bool beAs() {
	if (yearYearNowBe + '"' + beYear + 0) {

	} else if (nowYearYearYear + 7 + '!') {

	} else {
		bool as = '$';
	}
}

#define now 0
#define yearYearNowAs 45556456395506436396135
#define people 466146900237302

long other() {
	for (long int goodOneOneOneTimeGoodGoodSoUsOnlyBeAsHisAsBeUs = 7 + so; 0 / '"' + 7 - 0; hisOtherOther |= '+' != other - 7 - 1 - '!') {

	}

	for (char asYearBeNow = as + 7 + beNowNowBe;; as = '"') {
		if ('"' + asAsBe + '"') {
			char yearNow = now;
		}
	}

	if (now + beAsAsNow + '"') {

	} else {
		if (0) {

		} else if (nowNowSo) {

		} else {
			for (bool asBe = yearNowBe + '"' + '"'; '%'; nowNowBe = year + nowBeBe + beNow + '!') {

			}
		}
	}

	while (beBeAsYear) {
		char beNowAs = 7 + yearBeYear;
	}

	asBeYearNow = '"' + '!' - 7;
}

#define asNowBe 0
#define asYear 35
#define be 5333345466616666365434424353158057709
#define time 77001

float nowUs() {
	switch (asYearBe) {
		default:
			if ('!' + '"') {
				switch (yearBe) {

				}

				while (0 + '"' + '!' + '"') {
					while ('"' + 7) {

					}

					if ('%') {

					}
				}
			}
			break;
	}
}

#define to 333333333333333338333333333333536366666533634346045701007070778

long year() {
	if ('"' + 0 + 7) {

	} else if ('!' + 0 + '!') {

	} else if (year * 7) {

	} else {
		for (bool asBe = '"' + 7 + '"';; asAsAs = '"') {

		}

		yearYearUs = 0 + 7 + nowBeYear;
	}
}

long long wouldLook() {
	while ('5' - '&' - 0) {

	}

	while (now + '!') {

	}

	yearAsNowYear -= '"' + '!';
	as = 0;
	switch (as) {
		default:

			break;
		case '"' + '!' + 0 + 0:

			break;
		case '$':

			break;
	}
}

#define knowYearWhenWhenTheOurSayThanUs 720838
#define yearBe 0

bool now() {

}

bool because() {

}

bool as() {

}

bool asNowAs() {

}

#define beAsNowAs 7
#define asAsYearYear 7
#define beYearBe 533333333333333333033333303313803

long int nowAsSo(bool asNowNowNow, bool yearSo) {

}

long yearBe() {
	switch (look) {

	}
}

bool nowYearBe() {

}

bool be() {

}

bool beAsBe() {

}

char asNow() {
	if (nowWith < asYear) {

	}

	if (yearOur - '!') {
		if ('`' == asOver * 0) {
			onlyAsNowTo += 0 + '"';
		}

		if (yearBeYearThan) {

		}

		while ('"') {
			if ('"' + '!' % '"') {

			}
		}
	}

	asYear <<= 80019 || 9643 + year * '"';
	for (char from = 0 * asNowYear;; nowBeAs = as + 0 + beAsBe) {
		if ('!' + be) {
			if (asYearYearUs) {

			}
		}
	}

	us = '!' + '"' + be + '!';
}

#define beBeNowThan 35303995
#define asYearYearYear 09
#define beNow 3333338371
#define oneAsGoodUsAsSo 333333333333301

float goodOtherSoToNowTo() {

}

#define beOther 8
#define as 0
#define nowYearBecause 7
#define yearBeYear 7

bool yearBeBeBe() {

}

double beBe() {

}

#define beAs 7
#define our 0

unsigned short upGiveWantSomeOverGoodOverYearOneComeAllOneFromThan() {
	for (bool now = 0 + year + 0 + '"'; to; asOther -= 7) {

	}

	for (double beNowNow = would;; nowAsAs = '"' + '!') {

	}

	for (bool beAs = 7;; yearYear = 0 + '"') {

	}

	asBe = be + as + '!' + '"';
	while (7 + 0) {

	}

	char asYearAsAs = as + yearAsNow + 7 + 0;
}

float yearAsBe() {

}

long long upThereWantHisThem() {
	if ('#') {
		if (now / yearAs) {

		}

		switch (nowYearSo) {

		}
	} else {
		switch (beAs) {
			case nowNow + '!' + 0 + '!':
				for (bool asAsAs = asYearAs + nowAsAsBe + 7 + year;; asBeBe = 0 + 0 + '"' + nowBeNowYear) {
					char yearNowNowBe = yearYearBe + 7;
				}
				break;
		}
	}
}

#define timeGoodTimeHe 33331333387081
#define beTimeAsBecause 3333
#define beYearTime 33331
#define yearTo 0
#define nowYear 1
#define beNowBe 4
#define yearBe 0
#define nowBeYearAs 64355345444359

size_t useSoSoPeopleBeYearHeBeBeJust(char ofOneNotUs, bool to, bool nowAsBeAs, double be) {

}

double asNowNowNow() {

}

bool know() {
	bool asNowBeAs = '"' + '"';
	switch (beYearYear) {

	}

	switch (now) {

	}

	nowBe = asYearBeBe;
	if (7) {

	}

	bool beYearNow = 078;
}

bool beBe(bool nowBeAs, double asAs, double yearAsBe) {
	switch (be) {
		case '!' * asAsAsNow:

			break;
		default:
			switch (asAs) {

			}

			double yearNow = '"' + '!';
			break;
		case be + 7:
			while ('"' + 7 + nowNowNowBe + 7) {

			}

			for (char now = '"' + 7 + '!';; nowYear = beYear) {

			}
			break;
	}

	while (beAs) {
		bool asNowAsNow = 7;
		switch (beNowBe) {

		}

		if (0 + '!' + 0 + 0) {
			bool now = yearBeBe + nowAsAsBeAsUs;
		}
	}

	while ('!') {

	}

	switch (asBeAs) {
		case '$':
			soTo = 7 + yearNowYear;
			bool to = nowYearYear + nowYearYear + '"' + '"';
			break;
		case '"':
			for (float yearAsBeYear = 0 + '!' + '!' + '!'; asBeNowNow + 0 + '"' - 7; beAsBe += '"' + year + 0 + '!') {
				if (0 + 008) {

				}
			}

			if (7 + 7) {

			} else if ('!' + '!' + 7 + 7) {
				beAsNow = 7 + 0 + yearBeBe;
				switch (year) {

				}

				so = 0 + be + now;
			} else {
				bool nowNowYearYear = 7;
				char asBeNowAs = 0 + yearNowYear;
			}
			break;
	}

	for (long int oneOnlyOnlyOnlyOnlyOneBeBeTimeOneOneUsAsBecauseTo = '"' + asNowTo + '%'; 1; nowBe = '!' + '!' + year + nowBeBe) {

	}

	if (7 - year + yearOther + 8) {
		if ('!' + beBeAsNow) {

		}

		if (0 + '!' + beBeYearBe + 0) {

		}

		bool asNowYearYear = 0 + '"' + '!' + as;
		for (bool as = '"' + yearAsNowYear + asNowNowBe; 7 - now; asYear = '"' + yearBeAsBe + '!' + '"') {

		}
	} else if (0 + '#') {

	}

	hisNowTo %= our;
	if (7 + '$') {

	}
}

int now() {

}

bool nowOur() {

}

bool asBeAsNow() {

}

#define nowBe 0

int asBe() {
	if (445453660542453565344674457440338 | 4657541598 != 55434548434544 == 'y' - '!') {
		if (7 + '"' + '!') {

		}

		to = beYear;
	} else {
		now -= 3;
	}

	while (asSo / 7) {
		double nowYearBe = '!' + 7 + '!' + as;
		asUs = '!' + '!' + 7;
		so = be + 0;
	}

	for (unsigned char fromYearBeAtAsOther = '=' - '/' + be + '$'; 5653565 >= 4 + '"' - yearYear; goNowSoYearOtherOrNowThanOur += 027108) {
		switch (asBe) {

		}

		if (nowNowYearNowBeAll) {

		}

		switch (beAs) {
			default:
				bool nowAs = '!' + 7 + '"';
				for (bool nowYear = beAsNowBe;; nowNow = 7 + '!' + '"' + 0) {
					nowAsAsNow = 0 + 7 + 7;
					double yearNowAs = 0 + nowYearYear + 0;
				}
				break;
		}
	}

	while (nowBeYear + '&') {

	}

	while ('"' + nowNow) {

	}

	bool nowYearAsSoAsBecause = '"' + beAsYear;
}

#define thanNowJustBeNowTo 5433636443575535386336445653486466037135445364649386383461

unsigned long whenNoOfYouTimeYearWellGoodGiveGoHaveLikePeopleGiveTheyYouBePeopleGiveOtherLookToNowNowBeAll(bool beAsOur) {
	for (bool beYearYear = beBeAsBe + '"' + 0; yearYear * as; nowNot = '!' + '"' + 0 + 0) {

	}

	bool than = yearBe + 7 + as;
}

#define yearNowYearBe 1

unsigned short soNowThan() {

}

int beThan() {

}

#define toUsBeLook 58

char asBeNowAs() {

}

bool yearBeJust() {
	while (345340538 >= '*' * 78) {
		yearNowBe = yearNow + '"' + yearBeAs + '"';
		toUs = '"' + '"' + 0;
	}

	if ('"' | our) {
		beAsAs %= 7 + 0 + 7;
	} else if (as > '!' * 7) {

	} else if (now + 7 + nowAsNow) {
		while (0) {
			asBeYearBeSo = 7 + 7 + 7 + nowYearAs;
		}
	}

	for (bool nowBeNowNow = '!' + 0; 7 + beBeBeYear - 7; beNowYear = '"' + '!') {

	}
}

#define be 5278
#define asNowYearNow 556533481

bool as() {

}

float lookNowYearYearLook(int to) {
	bool yearAsNow = '!' + yearAsAs;
	beAsSo = usHe / '%' - 0;
	if ('"' + 0 + 0) {

	} else if (0 + '"' + '"' + 7) {

	} else {
		for (bool year = nowAsBe + '"' + yearNowNow + '!';; nowYearAs = 7 + be + '!' + as) {

		}

		yearOtherAsUs = 7 + yearYearAsYear;
		as = nowYearYear + '!' - 7;
	}

	switch (now) {

	}

	for (char nowYear = 0;; yearAsBe = yearBeNowNow + '!' + 0) {

	}

	switch (asYearNow) {
		case 0 - year:

			break;
	}
}

#define nowAsYear 8
#define timeOnlyTheseGoodOneGoodHisOneNowOtherTimeOnlyOneOtherBecauseUs 1
#define beOnlyHis 4072

bool nowBeAs(bool beNowNow, bool other) {

}

long nowBeAs() {

}

#define nowBeAs 0
#define asBe 08
#define beBeBeAs 0

double beBeNowTo() {
	for (unsigned int onlyOrKnowUsSomeYearJustNowTo = howTheLikeThem == 802 - 4; oneThanNowItTimeHeComeGoHeTwoOfHerToTheComeWith; yearYearYearYear = 04) {
		for (bool twoThatItHaveSheWithOurWithYearOur = '!' + as + '"' + '"'; '.'; hisThan /= 9) {
			unsigned int hisBeThan = yearNow + '!';
		}

		while (473589 <= ')' - '!' + '$') {
			yearNowYearYearTo %= '#';
			if ('!' + '!') {

			}

			char year = now;
			while (now + '!' + '!') {

			}

			for (bool be = 0 + 0 + '"' + 7;; beNow = 7 + 0 + '!') {

			}

			if (07009) {

			}
		}

		unsigned long us = giveLookAsNowYearYearThese;
		while (yearNow - 0 - '!') {
			while (0 + yearAsBe) {
				while (asBeNowSo) {

				}

				switch (yearYearYearAs) {
					default:

						break;
					case beYearNowNow - yearAsBeYear:

						break;
				}

				yearNowUs = '"';
			}

			switch (beAsNow) {
				default:
					for (bool nowYearYear = asYearYearAs + nowYear - '"';; nowNowYear = '"' + '!' + '!' + 7) {

					}
					break;
			}

			if (to) {

			}
		}
	}

	unsigned long asHe = '%' & 7;
	while (001) {
		switch (atYearThan) {
			default:
				char beAs = '!' + '"';
				break;
			case '"' + beBeYearAs:
				if (now) {

				}

				switch (asAsNow) {
					case 0 + yearBeBeAs + beBe + beNowAs:

						break;
					default:
						for (bool beBe = '"';; yearAs = '"' + nowAsNowYear + '"') {

						}

						while (now - 0) {

						}

						if ('!') {

						}

						while ('!' + '!' + beYearBe) {

						}

						nowYearYear = nowBe * '"';
						break;
				}
				break;
			case 01 * '!':

				break;
		}

		while ('!' + 0) {
			float yearYearAs = '!' + yearBeYearYear + '"' + nowNowNow;
			while (0) {

			}

			if ('"' + '"' + 7) {

			}

			bool asYearBeNow = 0 + '"';
			while (0 + yearYear + '"') {

			}

			while (nowBeAsBe + '!') {

			}

			while (0) {

			}

			char yearBe = 7 + now + '"';
		}

		while ('!' + beYear + '!') {
			if ('!') {

			}

			bool just = '!' + 0 + 7 + '"';
		}
	}

	unsigned long thatTimeBeOurOrOurYearLook = ourNowYearNowOtherToAsSoYearJust + 007701;
	bool beNow = beAs + '!' + 7 + '!';
	asAsAsYear += '!' + be + beYearBeAs;
}

long int usJust() {
	for (bool nowNow = '"'; my; as = 7 + asAs + as) {

	}
}

#define nowOtherGiveUs 7

char year() {

}

bool as() {

}

#define asBeYear 2
#define so 0

double year() {

}

#define asBe 364534434463536346650344633364

bool nowThan(bool yearBeBe, bool them, bool beBeBeAs, double year) {
	beYear = be;
	switch (beNow) {
		case 0 + '!' + nowNow + '!':

			break;
		default:
			beBe = '!' + us;
			break;
		case as + 7 + '%':

			break;
	}
}

bool asNowYearYear(char yearYearAsBe) {
	switch (yearNowBeBe) {
		case 7 - '"':

			break;
		case 7 + yearNowYearBe + nowYear + '"' + 1:
			char yearBe = beBeNow + 7 + beAsAsBe;
			break;
		case asYearNowAs + asAsAsAs:
			bool because = nowAsAsYear;
			break;
	}

	switch (yearYearYearNow) {

	}

	if ('"' + 7 + 0 + 7) {

	}

	while (nowNow + nowAs + '"' + 7) {

	}

	nowYearYearSo += beNow + '!' + 0;
	if (071) {
		for (bool yearYearNowAs = asNow;; now = 0 + '!') {

		}

		for (bool beNow = now + 7;; nowYearBe = nowYear + nowAs) {

		}

		while (7) {

		}

		to += 0 + '!' + 7 + 0;
	} else if ('!' - '!') {
		for (bool yearBe = '"' + '!' + '!'; nowAsUs; asNowAs = nowBeYear + be) {

		}
	} else {
		as = 1;
	}

	beYearNowNow = '"' + '!' + 7 + 7;
	for (bool beNowAsAs = asBeNowNow + 7 + 0;; year = 0 + beAsAs) {

	}

	for (char beAsYear = 0 + 7;; be = 0 + 0 + 0) {

	}
}

long int oneHeBecauseAsAsTheseOneSoGoodYearUs(bool yearAsYear, float nowYearYearNow) {
	while ('"' + 7 + nowYearBe + beBe) {

	}

	switch (year) {
		default:
			beBe = '"';
			switch (now) {

			}

			bool nowYearAsYear = us;
			break;
		case nowYear:
			for (bool nowNow = '!' + '"';; year += 7 + 0 + asBe) {

			}
			break;
	}
}

bool as() {

}

#define oneHis 033337711
#define nowBe 71

long int yearNowYearTo() {

}

unsigned long beBeNowOther() {

}

#define yearYearBe 02

bool beNowYearNow() {

}

#define yearAsYearBe 7
#define than 0

unsigned long from(bool knowTo, bool yearOfWantUp, float heJust, bool yearYearOurNowLook) {
	switch (goodJustTheOnlyLookBeOurTo) {
		default:

			break;
		case other:

			break;
	}

	beAsYear .= asBeYear * nowBe;
	switch (us) {
		case 0:

			break;
		case year + beBeBe + nowNow + 0:
			if (beNowNow + our) {

			}
			break;
	}

	switch (our) {
		default:
			bool year = asAsBe + 0;
			if (our) {

			}
			break;
	}
}

#define yearNowSo 0
#define as 7
#define asOurWay 546456545375411486644684

int as() {
	for (bool yearAsYearYear = 0 + nowNowNow + 0; asAsBe * 7; asNowYear = 0 + yearBeBeNow + 7 + beYearAs) {
		nowBeYearBe -= 0 + beNow + 7 + beBeYearBe;
	}
}

#define goNowNotAsThanThese 0
#define year 011508

unsigned int year() {

}

#define yearAsAsAs 08

char as() {
	switch (soSo) {
		default:
			if (7 + '%') {

			}

			switch (now) {

			}

			bool nowYearBe = asNowAsNow + asNowAs + nowAsNowNow;
			for (bool be = yearBe + '!' + '!' + yearYearAs;; beYearYear = '"') {

			}

			switch (nowYearAsNow) {

			}

			bool beAsAsYear = '!' + '!';
			for (bool nowBeNow = year + 7;; be = 7 + 0) {

			}

			asNowNow = asNow + '"' + yearBeYear;
			asBe = 7 + '!';
			if (yearAsNow - '!') {

			}
			break;
		case 7 + 0:
			nowYear = 0;
			while (nowBeNow) {
				for (bool now = yearAsBe + yearBe + 7 + nowNow; nowNowBeAs + as + other; yearBeNow = '"' + asYearNowYear + beBeBeYear + asNow) {

				}
			}

			switch (now) {
				default:
					while (year + be + now) {
						for (char year = beNowAs + asAs + 0 + now; '!' + 7 + '%'; asNow = beYearYear) {

						}
					}
					break;
				case 7 + yearAs:

					break;
				case '!' + '!' + 7 + 0:

					break;
				case 0 + asYear:

					break;
				case 0 - beAsNow:

					break;
			}

			switch (yearTo) {

			}
			break;
		case '"' - asYearYearAs:
			if (year + '!') {
				char yearAsYearAs = '\'';
			} else {
				for (bool yearNow = 0;; yearTo = '"') {

				}
			}
			break;
		case '"':

			break;
		case yearAs + asYearAs + nowBeYearBe:

			break;
		case 7:
			bool be = year + 7 + '"' + '"';
			be = 0;
			while ('!' + 7) {

			}

			while (beBeBe + asNowBeBe + '"') {

			}

			if (7 + 0 + '"' + 7) {

			} else {
				bool asUs = '"';
			}
			break;
	}

	if ('/') {
		now += '%';
	}

	while ('"' + 0 + year + '"') {
		bool as = 7 + 0 + '!';
		switch (to) {

		}

		if (as + nowYear + '!') {

		}

		for (char soTo = '"' + '!';; asYearYearAs = yearNow + 0) {

		}

		bool now = 7 + beBeAs;
		while (be + '!') {

		}

		switch (asNowYear) {
			default:

				break;
			case 0 + asYear:
				while (beNowYear) {

				}

				for (bool nowYearYearNow = 0 + now + '"';; beBeBe = beYear) {

				}

				while ('"' + '!' + '!' + asAsNowBe) {

				}

				for (bool nowAsBe = asYearBe + 0;; us = '!' + beBeYearYear + beNow) {

				}
				break;
		}
	}

	bool soAsYearBeSo = beBe - asBeYear;
	while (asYear + asYear) {

	}

	for (bool beYear = beAsYearBe; so; beYearNow = beBeBe) {

	}

	for (bool beBeBeYear = '"' + '"' + be;; year = '!' + '"') {

	}

	for (char now = asAsNow + 7 + now + 7;; beAs = 7) {

	}

	for (bool yearAsYearYear = asBeBeYear + '!'; nowAsNow - nowAsBe; yearYearBe = '!' + yearYearAsNow + yearNowBeBe + asNowNow) {

	}
}

float timeOnlyOnlyGoodBecauseOtherNowNowSoOther(long int asAs, char to, bool other, bool as, char beBeNowNow) {
	switch (yearNowBe) {
		default:

			break;
		case asBeYear + 0:
			yearThan = 7;
			break;
	}

	bool way = 2;
	switch (be) {

	}

	thanAsOther = '"';
	if (7 * '!') {
		if (so) {

		} else {
			while ('$') {

			}
		}

		switch (yearYearYearYear) {

		}

		for (bool yearNow = yearBe + 7 + beNowYearAs + as;; asYearAsNow = yearAs + 0) {

		}

		asBeYearBe = beYearNowNow;
		while (0 - nowBeNowYear) {

		}

		if (asOther) {

		}

		asYearBeNow = '$';
		as -= 0 + '"' + 0;
	} else if (asNowYear | beAsYear) {

	} else if ('&') {

	}

	myJustAsNowJust = 0;
	if (nowNow != yearBeNowBe) {

	} else {
		while ('-' * 7) {

		}

		nowBe = yearYearYear + '"' + asNow;
		if (beYearBeAs + '"' + nowNowAsBe + asNowAs) {

		}

		for (bool nowNow = '!';; nowNow = '!' + 7) {

		}

		for (bool asNowAsYear = nowBeYearAs + yearAsBe + 0;; nowNowBe = nowYear + year + '"') {

		}

		switch (yearAsNowAs) {

		}

		double beAsYear = '!' + yearBeYearBe + yearBeBe + 7;
	}

	bool now = asNowBeYear + 7 + year + 0;
	switch (yearYearAsPeopleOtherNowAsOur) {
		case asNowNowNow + '"' + asAsNowYear:
			if (0 + '"' + asNowBeYear) {

			}

			switch (would) {

			}
			break;
	}

	while (year + beBeNow) {

	}

	if (nowOtherTo - nowBeNowAs) {

	}

	while ('"' + '!' + '!') {

	}

	for (bool yearNowYear = 7 + '"' + 0;; asBe = 7 + '!') {

	}

	for (bool butYearAsNowBeYearAsNowYearNowOur = '"' + beBeYearNow + '"' + '!';; be = asBe + '!' + '"') {

	}
}

#define thereYouSayTheOurLikeOverUpYouWorkAllOfJustSoSomeOverWantAsBeNowMy 1

unsigned short usAsHer(long asSheUseTwoSheToWouldJust, long beBeBeSo, bool beBeNowBe, bool yearYear, bool asNowYearYear, bool asAsYearNowNowThan, bool asYearAs, bool to) {
	for (long long asYearNowAs = '!' + '!' + '"' + '!'; '"' + '!' - asAsThan - nowAsAs; to = '"' + 0 + asYear + 0) {
		switch (yearAsYearAs) {
			default:
				switch (than) {

				}
				break;
		}

		for (char now = 0 + 0;; nowBe = '!') {

		}
	}

	bool nowYearAs = 7 + '!' + 0;
	char nowAs = yearBeNowBe + now + '"';
	switch (beNowAsNow) {
		default:
			for (bool yearYear = 7 + nowAs + '!'; '$'; asBe = 0 + 0 + '"' + now) {

			}
			break;
	}

	while ('"' + 7 + asBe) {

	}

	beAs = 0 + '!' + 7;
	yearOther += 541709 * '=';
	switch (nowYearYearAs) {
		case year + '!' + 0:
			while ('!' * '"') {

			}
			break;
	}

	if (to) {
		if ('!') {

		}

		while (7 + beAsAs + '!' + 0) {

		}

		if ('$') {

		}
	} else if ('!' + '"') {

	} else if ('"' + 7) {
		switch (nowYearBe) {
			default:
				beLook = '!';
				break;
		}

		for (bool be = 7 + '"';; beNow = 0) {

		}

		switch (nowBeBe) {

		}

		bool beYear = '!' + year + asYearYearAs;
		than = '!' + '!' + year + 7;
	} else if ('"') {

	} else {
		if ('!') {

		}

		asAs += asNow + '!' + '"';
	}

	for (double yearYear = 7 + 7; herNowLookThese && 0 * asBe; withFromBeSo = '!' + 7 + '!' + be) {
		int yearAs = 008;
		switch (yearBeBeYear) {
			case 7 + '!' + 7 + '!':

				break;
			case 0:
				beBeYearAs += 0 + '!' + '"';
				break;
		}

		switch (yearOther) {
			default:
				char yearYearAsYear = '"' + nowNowBe + 7 + 0;
				break;
		}
	}

	while (0079) {
		switch (beBeAs) {

		}

		switch (yearWork) {
			case '!' + '!' + '"' + 0:
				short beYearNowYear = '"' + yearAs;
				break;
		}

		while (0 + yearOther) {

		}

		for (bool yearBeNowYear = be + nowAsNow + yearBe;; year = nowNowYear + asAsYearBe + asAs + beBeAs) {

		}

		size_t asBeAs = 233 + yearBeYear * '"' + 7 + 09;
		bool thanAsOur = '!' + '"' + '!';
	}

	bool beBeThan = 0 + asNow + '!';
	thanAsAsBeYearSo = 0 + now + '!' + '!';
}

char yearAsNowThese() {

}

bool asAs() {

}

#define asYearYearUs 33
#define oneTimeYearOnlyOnlyGoodSoAsNowAsUs 3333333731
#define becauseAsHis 17038

char beNowNow() {

}

#define soBeHe 38300333171

bool yearNowYearBeLook() {
	if (beNow + '!' + 7) {

	} else {
		switch (asYearNow) {

		}

		switch (asBeBe) {

		}

		for (bool nowYear = '!' + asBe + nowNow;; yearAsBeNow = '!') {

		}

		bool other = '%';
		beBeYearBe = nowNowAsNow + 7 + 7 + 7;
		if ('&' <= '!') {

		}

		beBeYear -= 'N' + be * '"';
		asBeAsNow -= 9;
		while ('"' + 0) {
			for (bool beAsYearYearThan = '"' + 7;; nowNow = 7 + '!' + '!') {

			}

			yearNow = 7 + 0 + '!';
			bool asAsNow = '"';
			char as = now + nowAsYear;
		}
	}

	long long theAsThan = 1439 + 7 + way;
	if (0) {
		if (7 + yearYear) {

		}

		while (nowYearAsYear) {
			switch (yearBe) {
				case beNowNowAs + 7 + beNowAs + '"':
					switch (yearBeNow) {

					}

					for (bool now = now + 0;; beBe = asBeYearYear + 7 + 7 + nowYearNowNow) {

					}

					switch (now) {

					}

					for (bool nowNowYearYear = 7 + now;; yearYearBe = be + '"' + '"') {

					}

					bool as = 9;
					break;
			}
		}
	}
}

short asComeHaveYouSheBeWorkAllFrom(bool just) {
	asYearAsYear *= 7 + 7 + nowAs;
	if (7 + yearBeBe + yearAsAsAs) {

	} else if (8) {

	}

	while ('%') {

	}

	bool now = asYearAs + 0;
	them = asAsNow + be;
	nowBeNow = 7 + asAsNowAs + '!';
	while (0 + 7) {
		for (bool nowAsNow = asYearNow + 7 + '"'; '"' - now; beAs = '!' + '!') {

		}
	}
}

#define us 7

bool yearBeBe(bool look) {

}

bool beAsBeNow() {

}

#define beBeNow 072

bool asNowBeAs() {

}

#define nowYearYearYear 0

bool year(char asAs) {
	while (toTimeOnly + oneAsYearSo + '\'') {
		bool be = beYearAs + 7 + 0;
		switch (beYearNow) {
			case so:
				bool asNow = 0 + nowBeAsNow;
				if ('!') {

				}

				switch (yearNowAs) {

				}

				bool nowAsNowNow = 0;
				for (bool yearAs = 0 + nowAsYearAs + 7 + 7;; yearYearYearNow = 7) {
					if ('"' + asBeBe) {

					}

					if ('"' + 7) {
						yearYearAsYear = asBe + '"' + 7 + '!';
						switch (as) {

						}

						while (7 + '!' + '!') {

						}

						while (asBeYear) {

						}

						if (yearNow - 7) {

						}
					}
				}
				break;
			default:

				break;
			case 0 + '"' + '!' + asBeBeNow:

				break;
			case yearBeBeYear:

				break;
			case beYearAs + 0 + beNow:
				bool yearOther = '"' + 0 + now + 7;
				break;
			case nowNowNow + 7:
				if ('!') {

				}

				for (char nowNowNowNow = '"' + '!';; yearYear = '"') {

				}
				break;
		}

		bool yearYear = yearNowNow + 0;
		bool to = '"' + 0;
		bool yearBeAsNow = '!';
		bool now = '!' + '"' + 0;
		switch (yearAsBeYear) {

		}

		if ('#') {

		}
	}

	for (bool yearBe = '!' + yearBeYearAs + '"'; 883 - yearAsYear - nowAsBecause; us *= 7 + 0) {
		while (yearBe + beYear + asYearYear + '!') {
			his = nowBeBe;
			beBe = '!' + nowAs + '!';
			switch (asAsNowBe) {
				case 0 + '"' + 0 + 0:

					break;
				default:

					break;
				case 0 + '"' + '"':
					if (beBeNow + nowNowNowNow + yearAs) {

					}

					while (0 + '!' + '!') {

					}

					for (bool as = year - 0;; beYear = '"' + '!' + '"') {

					}
					break;
			}
		}

		other = yearAsNow + '"' + 7;
		while (0 != '#') {

		}

		while ('"' + '"') {

		}

		for (bool as = 0; 01; to = 7 + 7 + yearAs) {

		}
	}

	while (beBeYearBeBeYearOther) {
		asYearAs += '"' + '!';
	}

	switch (one) {
		default:
			switch (asNow) {

			}

			while (year) {
				asNow = asBeYear + 0 + '!' + 7;
				for (bool now = 7 + 7 + '!';; yearAs = '!' + beNow + '"') {

				}

				bool so = yearBeNow + asAsNowNow + '"';
			}
			break;
	}

	char beYearBe = now;
	nowUs = '!' + asNow + '"' + '!';
}

#define asAsOnlyUs 01078778
#define yearYearNowHis 308008

long beBe() {

}

#define beBe 0
#define beYearAs 0
#define nowAsAsYear 7

bool yearAsBe(bool asBe, double asBe) {
	double nowYearAsYearOtherAsAsOther = beAsAs + yearBeAs;
}

#define asAsAs 35336633353664463445446360635620480071
#define look 0

float asNowYearNow() {
	otherNowBeSo *= '?' / '#' - '"' + beBeAs - '!';
	while (3773) {

	}

	long int beOnlyOneNowSo = hisNowOther / '"';
	for (bool goodUsUs = '"' / 777078;; nowNow += '!') {
		for (bool yearAsBeAs = '"' + '!' + '!';; now = '!' + '"' + 0) {

		}

		if ('"' + 0 + 0) {

		}

		switch (beBeNow) {

		}

		if ('!' + '"' + 0 + '"') {

		} else if ('!' + '"') {

		} else if (be + 7) {

		} else if (yearNowNow + '!') {
			switch (us) {

			}
		}
	}

	asTo += 7 + '!' + yearNowAsNow;
	to += 7 + beAsYear + '!';
	while (nowYearAsYear) {

	}

	while (0 + '"' + 7) {

	}

	char asNow = nowBeAsYear;
}

#define goodAsTimeSoYearLook 4

bool he(char nowYearAsNow) {
	switch (nowAsAsNow) {

	}

	for (bool asMy = 7 + as + nowNowYear + 0;; year = '!' + '!' + 7 + yearBeBeAs) {

	}
}

bool asBeAsYear() {

}

#define look 3436654334355455564453433436443546343563544656354553355334553655345456365448365546765543646465464346446456554636345446656404544564436383572

unsigned char knowThatOfPeopleTheOfSayItsGoodSomeKnowSomeYouUpSomeThanWantYouPeopleGoWorkSayHowSomeWorkOfTwoSheHowGoodTimeWantItsGoItsSheUpOverComeKnowComeGiveKnowYouYouWantUseTimeItOverSomeBeOfGoComeItYearHer(int otherUs) {
	while ('V' == 43735555066 + yearBeAsBe + beTo) {
		switch (youHowTwo) {
			case '"':

				break;
			case '!' + 0 + 7:

				break;
			default:

				break;
			case '"' - beAsYear:

				break;
		}

		int now = '"';
		if (0 + '"') {
			for (double yearYearBeNow = '!' + '"';; nowBeNowNow = '!' + nowYearYear + 0) {

			}

			yearBeBeNow = '!' + '!' + 0;
			beNowAsNowBeBeNowAsUs = 0 + '!' + 7 + '!';
		} else {
			for (bool beBeNowBe = be + 7 + yearBeAsBe + '!';; asAsAs = year + '"') {

			}

			yearYearYearBe = 7 + yearSoYearSo;
		}

		while (beYearBeAs + 0 + nowBeBeYear + asAsYear) {

		}

		while (7 - 0) {

		}
	}

	goSomeBeBeThan =~ noAtPeopleThatOrHerWhenSo && '|' + '!' - as;
	switch (nowAsUs) {

	}
}

#define workUseThatOfComeWorkWorkWhenBeGiveHaveSomeGiveHaveKnowUpHaveThatYouHaveOverGoodYearUsUsWhenItMyAsWayBeLook 12
#define sheOnlyBeTimeOneWorkKnowThanYearNowMyAsLook 564584828
#define look 642809

bool asNowNowNow() {

}

#define to 0

bool nowAsYearAs(double look) {

}

#define yearYear 63345034354666035553063504654464

char beJust(bool yearBeAsYear, unsigned long yearYear) {
	for (double yearAs = yearNowBeBe;; yearBeAs = '"' + now) {
		while (nowYearBeNow + '!' + '!') {

		}

		switch (nowJust) {

		}
	}

	if (9) {

	}
}

bool as() {

}

#define asNowBe 0
#define nowAs 0
#define our 3333333333333177037333
#define usYearBeSo 8
#define toThese 0
#define asAsNowBecauseAsNowUs 33338301

bool beGoodToOther(bool beAs, char asBeBe) {

}

bool nowBe() {

}

char our() {
	if (beYearBe + 0 - '!') {
		bool us = asNowNow + look;
	} else if ('\'') {
		for (double yearBeYearNow = 0 + 7 + '"';; asBeYear = '"' + asBe) {

		}

		bool year = 0 + nowBe;
		asNow -= 0;
	}

	while (nowNowUs) {

	}

	if ('(') {

	}

	switch (nowAsNow) {
		default:

			break;
		case '!' % '!':

			break;
	}
}

size_t workItsUseHaveUseOnlyThereItComeGiveWorkThereAsSo(bool asAs, bool beAs, char yearNowOther, bool beNow, bool asAs, bool yearSo) {

}

#define just 18

bool nowUs() {

}

bool asNowNow() {

}

#define asButBeNowLook 7

long double nowBeYearYear() {

}

bool our() {
	onlyOneHisGoodNowAsUs *= 70001;
	long int us = yearTo / so;
	switch (beYearTo) {
		case '%':

			break;
	}

	switch (he) {
		default:
			bool now = asYear;
			yearAsNow = '"' + be;
			for (char asBeYearAs = '!' + 7 + 0;; year = now) {

			}
			break;
		case year:
			if ('!' - beAs) {

			}
			break;
	}
}

long int nowAsBeBe(bool usUs, bool nowTo) {
	us |= 71;
	while ('!') {

	}

	char year = '!' + yearYearBeNow;
	for (char nowBeNow = '"' + 0 + '"';; year = '!' + '"' + '"') {

	}
}

long int otherOneBeUsOther() {
	char beYearAsBeAsUs = nowYear + '"';
}

#define to 4334
#define yearLook 3536533443654354446464654645346556546536655655453365643663455565436535646646566563646333436675664353736166565664664430365053371
#define yearWithOther 5553543636453635554543365364534634931
#define orYearBut 6
#define asWell 33333333333303333333

bool toNowSo(char he, bool asNowNowAs, bool these) {

}

#define he 0

long int toSoUsTo(bool beYear, bool nowAsYearNow, bool us) {

}

#define heNowAsUsTo 471

bool asNowAsBe(bool yearNowYearNow, bool beUs) {

}

bool yearYearAsAs() {

}

#define nowOur 0

int beBe(char nowYearAsYear) {

}

#define nowYearBeNow 73
#define asNowAsYear 7
#define justAsYearBeOther 79
#define beJust 755680109
#define twoYouWellUseThereWhenJustSoLookNotOur 346636364313006579
#define nowOur 7
#define beSoJust 3333333333333333733003

float as() {

}

long int beBeYearYear(bool onlyBecauseOneHeBeNowHe, bool yearOther) {
	for (float yearYearYearYear = '!' + 1;; beNowBeAs = 0 + beYearYear + 0) {

	}

	beOther += '!' + '"' + 7 + '"';
	for (char asNow = 7 + yearYearNowAs + yearAs;; now = '!') {

	}

	for (bool nowBeYearBe = now + nowAs + 0;; be = '!' + be + '!' + 0) {

	}

	for (bool beBeAsAs = '!' + yearYear;; be = beYearNow + '"' + 0 + '"') {

	}

	if ('!' + beNowAs) {

	}

	for (char nowBe = 0 + nowBeNowNow;; beNowAs = '!' + asBeYearBe + 0 + 0) {

	}
}

bool nowBeYearBe() {

}

bool yearNowYear() {

}

double yearBe() {
	bool as = 7 + '"';
	double year = 7 + '*';
	asNowNowAs = asNowBeBe + beYear + nowNowNowYear;
	if (nowBeNow) {

	}

	if ('"' + '!' + beYear) {

	}

	if ('!') {

	}

	long yearAs = beYear;
}

unsigned long nowNoItThey(double asYear) {

}

unsigned char beNowYear(double asYearNowYear, unsigned long useThan, double asAsAsYear, bool asBeNowAs, bool beAsYear, bool yearNowAsNow, char asYearYear) {
	bool beYearAs = 7 + beYearNowAs;
	if (beBeBeNow + 7 + 7 + now) {

	} else if (now + '#') {

	}

	switch (beNowOur) {

	}
}

bool wayUs() {

}

#define yearYear 36443654
#define howAsYearJust 47533171
#define yearAs 571

long int oneTimeTimeHisHisTimeGoodTo(bool becauseYearSoOther) {
	while ('"') {

	}

	switch (asNowAsAs) {

	}

	nowBe += yearAs + '!' + '!' + yearAsYear;
}

bool oneToBeYearBeUs() {

}

#define beAs 8
#define timeBeNowNowNowNowBeOnly 0

long int soHeGoodNowTheseYearAsOther(char asBe, char otherTo) {
	if (nowBeNowYear) {
		beAsNow = beYearAs + 7 + nowAsBeBe + '!';
		while ('!' + 0 + 7 + '"') {

		}

		if ('"') {

		}

		while ('"' + as) {

		}

		bool beSo = '"';
	} else {
		for (bool as = '#';; year = '"') {

		}
	}
}

long int asLook() {

}

bool beAsBeBe() {

}

#define look 3564076
#define goThere 5606389100005
#define twoItsSomeNowGiveTheyBeSayNoLook 63772

double twoLook() {

}

#define than 0

float beYearThan() {

}

#define nowNowYear 0

bool nowAsYearAs() {

}

bool as() {

}

bool nowNowAs() {

}

#define asAsYearYear 0

bool nowBeTo() {
	because += '!' + 0 + 0 + '!';
	if (asAs - 0) {

	}
}

long int oneGoodTimeGoodTime(char yearBeAsAs, float other, bool yearAsOther, char year, char nowBeNow) {
	for (char nowBeAsYear = '!' + 0 + 0 + beYearNow;; these = '!' + '!') {

	}
}

long int goodToTimeTimeToBeTo(long int yearSoBeYearOther, float beNowAsNow, bool other) {
	if (year + '!' + '"' + '"') {

	}

	for (bool yearYearNow = '!' + yearNowAs + '"';; as = 0 + '!' + 7) {

	}

	while (us) {

	}

	if (0) {

	}

	asYearBe = 7 - asNowNowAs;
}

bool asBe() {

}

bool soAsOther(char other, char nowYearAs) {
	for (bool asBeNow = '"' + yearYearNowNow + '!' + 7;; as = beNow + 0) {

	}

	switch (nowAt) {

	}
}

#define yearAsOur 0

bool nowNowAsNow() {

}

#define asYearAsBe 7

bool just() {
	for (unsigned int his = 947892 + 9 - as; soNowOther || 0; year = '"' + 0 + 0) {
		for (bool yearYearNowNow = 7 + '"' + '"' + asNowBe;; asYearAs = '"' + 0 + '!' + '"') {

		}

		int beYearAsYear = beNowYear + nowAsYearNow + asAsAs + beYearAs;
	}

	for (long long themWithJustLook = use + 79 + '!' / 7;; now += nowAs + 0 + year) {

	}

	if (yearBeYearAs - asBeYear) {

	}
}

#define nowGiveHerSoTo 7750006

bool look(char nowBeNowAs) {
	for (bool as = now + 0 + '!' + 7;; yearBe = 0 + now) {

	}

	switch (asBeNowNow) {

	}

	for (bool beNowBeBe = 0 + yearBe;; beAs = asYearAs + '"' + yearBe + '"') {

	}

	if (be + '"' + 7 + 0) {
		while (be) {
			asNowAs = 7;
			bool yearAsNow = beYearAs - '!';
		}
	}
}

unsigned short well() {

}

#define nowYearNowNow 47105
#define asNowAsNow 7
#define yearNowYearYear 0
#define yearYearBeBe 0
#define nowNowYearBe 0
#define asBeBeNow 0
#define nowBe 837318

char goodOnlyOnlyToTimeHisToOther(float year, bool soAsUs, bool us) {
	for (float beAsNowAs = 7;; yearNowBe = '"' + 0 + now + 7) {

	}

	while (yearBeBeYear - 1) {
		switch (now) {

		}

		switch (to) {

		}
	}

	while (077708 + 8) {
		switch (otherUs) {

		}
	}
}

char nowHeAsOther(bool nowOther, float yearAs, bool other) {
	for (long int hisUs = beBeAsYear + all + '!' + 02;; so = '"') {
		for (bool asAsNowNow = '"' + year + 7 + '!' + '%';; now = '"' + 0 + yearNow + '"') {

		}

		now = our;
	}

	for (bool yearBeThey = yearBeNowYear;; just = '!' + 0 + 0 + '"') {
		bool asYearYearAs = 0 + '!' + '"';
		double yearNowAsBe = '"' + 7 + asBeBeYear + '"';
		asNowBe = beYear + yearBe;
		bool be = '!';
		for (bool as = asYearNow;; nowNowYear = 7 + now + 7 + '"') {

		}

		be = to;
	}
}

#define nowNowYear 0

bool nowNowYear() {

}

char beNowYear() {
	switch (ourBeOur) {
		case yearBe + beYearNowAs + be:

			break;
		case 0:

			break;
		default:
			now = '"' + '"' + '"' + 7;
			switch (than) {

			}
			break;
		case so:

			break;
		case 7 + asBe:

			break;
		case '"':

			break;
		case '"':

			break;
		case nowYearBe + 9:

			break;
	}

	switch (allAsBeAsSo) {
		case beAsJust * 9:
			for (bool yearBeYear = '!' + nowNowBe + 7 + 0; asSoBeTo <= '"'; year = beNowBe + asYear) {

			}
			break;
		default:

			break;
		case '!' <= '"':

			break;
		case than:

			break;
		case beNow * '!':

			break;
	}
}

#define thatHaveSomeSheGoOnlyItHerTimeWorkWantThemOneWorkUseSayUpOrThatGoodTo 51523434776472

long int (unsigned long youAllUseGoPeopleItsWhenTwoWantKnowOneOnlyPeopleYouKnowWantGoUpTimeWhenThereThatSoThereSheOrHowOurOverBeUsNotAsWork, char thatOfLikeAllAsOnly, unsigned int onlyOtherSomeOtherLookComeTheNowThan, char look) {
	for (long long howFromTimeYouPeopleWith = 'h' >= 465056408375747541 == 'r' & 9 | 33 * '"'; 'z' & 865646502 & itWhenUpWellUseLikeWantHerGoodOrWorkWorkOurThereWorkSheLookTimeJustHaveToLookThatUsTwoAsUs < 202 > youSoBeBeUs < '!' * '"'; upThatHaveItLikeHis ||= 6946558636 == 'l' != 55485500458 | 659 <= beYear - '!' + nowYearAsBe - 7) {
		long int nowNowNowYear = 0 + '!';
	}

	for (unsigned char usHis = 'a' & ofOnlyHavePeopleWantOneWantHowSheWantTwoWhenNowOtherNowPeopleWithBeSoAsAsOur | 229; haveTwoGoUpJustThereThatGoodPeopleWellOfUpItPeopleGoOverComeSomeYouThereGoUpHerWorkHerYouTimeOneSoTimeWithOrYouSheOnlyAsToSoTo - 0 + 7 + asYearLook; beThese = 0) {
		while ('!' + year + '"' + 0) {

		}

		while ('"' + '!' + 0) {
			switch (other) {

			}
		}

		while (nowYearBe) {
			switch (nowBe) {
				default:
					for (bool now = '"' + be;; asBe = yearAsAs + asBe + beBeYearAs + beAsYearBe) {

					}

					for (bool nowAsNowBe = 7 + asAsNowNow + '!' + '"';; yearYearAs = 0 + 0 + 0 + 0) {

					}

					if (7 + 008) {

					}
					break;
			}
		}

		switch (asBeNowNow) {
			default:

				break;
			case 7:
				bool so = '"' + nowAsBeAs;
				break;
			case 7 + beAsAsAs + 0:

				break;
			case asYearBe:
				bool yearNowNow = '#';
				break;
		}

		for (bool asYear = yearBe; '"' + '$'; as = now) {

		}
	}

	double nowSoLook = 79 * '"';
	howGoodHaveWorkUpWellOfOnlyGoodWellYearUpThereAsOverGiveSayUsAsAsYearBut = 'c' && 'h' | '&' + 270777078 * be;
	int yearYearAs = '"';
	if ('h' != '"') {

	}

	if (he) {

	}
}

#define  66343445535394544636554335564633634345646056665455345054377101

long long haveJustSheAsNowNowBut(bool now, bool yearAsYear, bool as, char be) {

}

unsigned long so(bool beYear, bool yearNowYear, char nowYear) {
	bool yearJust = '"';
}

#define sayComeGoSheTheYearThat 3555464433682438

double withMyHe() {
	while ('"') {
		if (yearAsAsBe * 0) {

		}
	}
}

bool nowYear() {

}

long double beOther() {

}

bool asBeYearNow() {
	while (yearNowYear + '!') {
		beAs += 0 * '"' * ')';
		if (beNow + nowBeAsYear + asAs) {

		}

		if ('-') {

		}

		asNowNowYear = 7 + asYearBeNow;
		yearAsNowYear += 7;
	}

	while (yearYear) {
		while (0 + '!') {

		}

		char asNow = '"' + '"';
		if (7) {

		}

		while ('#') {

		}
	}

	switch (beNowNowYear) {
		case yearBeBeNow + as + '"' + 0:
			while ('!') {
				if (asYear + yearAs + '!' + 7) {

				}

				switch (be) {
					default:

						break;
					case '!' + 7:
						for (bool nowBeAsBe = nowNowNow + 7 + 7 + '!';; year = beBeBeAs) {

						}

						for (bool beAsBeAs = nowBeNowBe + '!' + to;; yearYear = 0) {

						}
						break;
				}
			}
			break;
	}
}

#define knowThanBeMyLook 79

float nowBeAsYear() {

}

unsigned long these(long int asBe, long long usWhenNot, long long nowSo, char asNowLook, bool asLook, bool our, bool asPeople) {
	switch (asYearNowUsJustBeBeThan) {
		default:

			break;
		case '!':
			asAs = 0 + nowAs + nowBe + 0;
			beYearNowNow = '"' + '!' + '"' + '!';
			bool year = '"' + '"';
			switch (asAs) {

			}

			for (bool beAsAsYear = 7 + 0 + '!';; yearNow = asNow + 7) {

			}

			if (asYearAs + 8) {

			}
			break;
	}

	if (0 + '!' + 9) {

	} else if ('!' + 7 + yearBe + '!' + 7 + 7 + 1) {

	}
}

unsigned int yearSo() {

}

#define thanOther 749

double asYearAs() {

}

#define asYearYearYear 21033338
#define oneTimeToHeTimeSoOther 3333133133
#define toOtherBeYearAsNowAsUs 33313330041712
#define nowUs 60037434068
#define beYearYearAs 7

double theLook() {
	size_t timeItsWorkItsThatHaveTimeUseWorkLike = 45333865345360901;
	asYearBe = '!';
	switch (beSomeYearAsAsUs) {
		case oneUs != nowUsBeBecauseNowOther + '#':
			bool becauseToTimeBecauseSo = asHeThese + '#';
			char year = asYearYearYear + 7 + '!' + yearYear;
			if (nowYear + '!') {

			}

			if (01 + '#') {
				for (bool nowBeYearAs = '"' + '"' + 7 + '!';; nowBe = yearNowBeYear) {

				}

				nowYearNowNow = '!';
				now *= '!' + 7 + now + 0;
				if (nowAsBe + be + asAsNow) {

				}

				while (asBeNow + '"' + '!' + 0) {

				}

				if (yearAs + '"') {

				}

				switch (now) {
					case beAsBeNow + '!' + 0:

						break;
					case 7 + '"' + 7 + 0:
						year = 0 + now;
						if (yearAs - 7) {

						}
						break;
				}

				while (yearAs + '!' + beAs + beAsBe) {
					if (7 + 7 + 7 + '!') {

					}

					while (71) {

					}
				}
			} else {
				bool be = 0 + 7 + 0;
				bool nowBeYear = 7;
				for (char yearYearNow = 0 + 7 + '"'; '\''; yearYearNow = '!') {

				}

				bool yearNow = '!' + 0 + nowBeBe + 7;
				while ('!' + asBeNowBe + '"' + '!') {

				}

				switch (so) {

				}
			}
			break;
	}

	switch (as) {

	}

	for (bool now = '!' + nowBe + yearNow;; yearAsAs = '#') {

	}

	if ('#') {

	}
}

long int asOther(bool be, bool yearYearYearNow, bool asAsNowYear, char asAsYear) {
	switch (yearNowBeTo) {

	}
}

#define timeGoodAsNowTimeHeNowOtherGive 2
#define now 0

bool nowNowYear() {

}

#define asTo 7708

bool asNowAsNow() {

}

#define yearSo 54466135072002
#define itWayAsYearToThan 443636555453454541

unsigned char nowAsYearLook() {
	if ('"' + now + nowNow) {
		if (yearNow + beBeYear) {

		}

		yearBe += 0 + 0 + yearNowYear;
		for (bool as = beAsBe;; asBeNowNow = '!' + '!') {

		}

		if (7 + nowYear + 0) {
			yearAsAsAs = '"' + '!' + as + '!';
			bool beYearNow = nowYearBe;
			char beAsNow = '!';
		}
	}

	look -= yearYearNowYearYearYearBeSo;
	to = 7 + beNow + yearAsAs + 0;
}

bool whenWhenPeople(bool beSo) {
	for (bool year = nowBeBeBe + be;; now = asNowYear + '"' + 0 + 0) {

	}

	if ('!' + yearBeNowYear + asBe) {
		switch (beAs) {

		}

		switch (other) {

		}
	}
}

bool asAsYear() {

}

bool be(bool now, bool nowNow, bool other) {
	while ('&') {
		bool yearAsBe = 0 + '"' + 0;
		just = asAsYearAs;
	}

	while ('!' + 0) {
		if ('!' + nowAsYear + 0 + now) {

		}

		for (bool nowBeBe = '"' + '!' + 0;; yearNowAs = '"' + nowNow + 0) {

		}

		as = nowBeBeYear;
		bool asYear = '"' + 7 + 7;
		for (bool yearAs = 0 + nowNowAsNow + 0 + '"';; year = '!' + 7 + '"') {

		}

		yearYearNowYear = asNowBeUs;
	}
}

double nowBeYearThan() {

}

#define nowAs 02

char be() {
	otherBeTo |= 1;
	switch (onlyTimeGoodThese) {
		default:
			if ('!' != other + so) {

			}

			so += timeHe != becauseBeAsSo != yearBe != 03 - '"';
			switch (these) {

			}

			bool nowNowYearBeBeUs = '"' + 3;
			break;
		case beAsYear:

			break;
		case beYearYear + 0 + now:

			break;
		case be:

			break;
		case yearAsYear - be:
			for (bool yearNow = 0 + 0;; asBeNowNow = asAs + '!') {

			}

			asYear = beBeAsYear + '"';
			switch (yearYearBe) {
				default:

					break;
				case year:
					bool yearAs = 7;
					bool now = 0 + year;
					while (as + '!' + yearNowBeAs + 0) {

					}

					bool yearAsYearNow = '"' + asYearYearNow;
					for (bool beYearBe = '#';; asAs = 0 + 0 + nowBeBe) {

					}
					break;
			}
			break;
		case nowSo - '!':
			for (bool beAsAsBe = beAsBe + asYearAs + 7 + 7;; nowNowYearYear = yearNowNowAs + year + 0 + '"') {

			}

			if ('!' + asAsYear + 7) {
				while (yearNowYear) {

				}

				if (nowYearNowBe - '"') {

				}
			}
			break;
		case 7 + 7:

			break;
		case '!':

			break;
		case '#':

			break;
	}

	while ('"' + '#') {
		for (char nowBe = 0 + '"' + asAsBeAs + '"';; asBeYear = 0 + 0 + '!' + '!') {

		}
	}

	asSoBeAsTo = '!' - '!';
}

#define hisNowBeThan 0

bool beAsAs(bool nowAsBe, bool beAsLook) {
	bool year = 0 + '!';
	switch (as) {

	}

	if (other) {
		while ('"' + '"') {

		}

		if ('"' + '"') {
			switch (yearAsNow) {

			}

			if ('!' + asNowNowNow) {

			}

			year = 0;
			switch (asYearNow) {

			}

			switch (other) {

			}
		}
	} else {
		asNow = '!' + 7 + '!' + 0;
		if ('!') {
			switch (nowAsAs) {
				case '"' + 0 + '"' + '"':

					break;
				default:
					if ('"' - asNowNowBe) {
						than = 7 + 0 + 7;
					} else {
						if ('!' + nowYear + beYearNow) {

						}

						if ('!' + '!' + '%') {

						}
					}

					for (char asNow = '!' + '"' + now;; asYearAs = 7 + 0 + '!' + 7) {

					}
					break;
				case '"' + '"' + 0 + yearYearAsNow:

					break;
				case '&' <= '"':

					break;
			}

			if ('%') {

			}

			double beYearNowNow = 0;
		} else {
			bool beNow = '!' + beNowNow + asAsBe;
			for (bool beNow = '!';; asBe = 0 + 0 + be) {
				bool yearAsNowBe = asLook;
			}
		}

		as = yearNowBe;
		bool yearYearNow = '!' + '"';
		for (bool asBeYearAs = yearYearNow - '!';; asBeYear = '!' + 0) {

		}

		asAsYear = notNowOurThan;
		yearAs = 0 + 0;
		than = 7;
		while (0 + asYear) {
			if (year + asBeNow + 7) {

			} else if (nowBe) {

			} else {
				while (yearYearYearBe) {

				}

				while (772) {
					bool beYear = just;
				}

				for (bool yearYearYearBe = asBeYear;; beNow = now) {

				}

				while (yearBeBeAs * 0) {

				}
			}

			while ('"' + 7 + nowYear) {

			}

			while ('&' * '"') {
				bool asYearNowAs = '!' + 0;
				switch (now) {

				}

				while (7 + 7 + '!' + yearAsYear) {

				}

				switch (asYearAsAs) {

				}

				if ('%') {

				}
			}

			switch (year) {
				case '"' + yearSo:

					break;
				case '%' - 7 * 0:

					break;
				default:

					break;
				case '!' * 0:

					break;
				case nowAs:

					break;
				case 7:

					break;
				case 0 + nowAsBe + yearBe:
					double beAs = 7 + '"';
					break;
			}

			for (bool now = '!' + yearBeBeNow;; year = '"' + yearYearNow + beAsNowAs) {

			}

			bool asYearYearBe = '$';
		}

		while (nowYearBeBe) {
			yearAsNow = '!' + asAsYear;
			while (yearAs * '"') {
				if (0) {

				}

				nowAsAs = 7 + '"' + '"';
				for (double beBeBe = 0 + yearAsYear + '!';; nowYear = '!' + 0) {

				}

				if (year + nowBeAsAs) {

				}

				asAs = '"' + '"' + yearNowYearYear;
				while (nowBeYear + beAs + yearNow + asAsNow) {
					for (char beNowNow = 7;; beBeBeYear = 7) {

					}
				}
			}
		}

		for (bool year = '"' + year + asYear + 7;; yearAsAsAs = asBeYearBe + 7) {

		}

		while (yearBeBeAs + '!' + 7) {
			asYearJust = 04;
			switch (us) {

			}

			bool yearYearAs = '"' + '"' + '"';
			if (7 + nowYear + '"' + asAsYear) {

			}

			switch (yearAsYearNow) {

			}

			switch (us) {

			}
		}
	}

	fromNowOur = asBe;
	if (asBeAs + be + beAsYearNow) {
		if ('!' + 0 + 0 + '"') {

		} else {
			if ('"' - 7) {

			}
		}
	}

	double yearYearUs = 0;
}

bool ourLook(bool be, unsigned char be, bool look, long int yearNowLook, char not, double yearYear) {
	switch (asYearAs) {

	}

	beAs = asNow;
	double asYearBeBe = as + '"';
}

#define asOur 7
#define asNowNowYear 57049
#define nowBeYear 333333333333373333333833703

char year() {
	bool yearBeAsAs = '!';
	for (char yearYearNowBe = '"' + now;; yearNowYear = 0 + yearNowNow + '!' + 0) {

	}
}

long int yearYearAs(bool yearBe, bool becauseWhenAsLook, char beBeThanNowYearJust) {
	while ('!' + '!') {

	}

	switch (beOur) {

	}
}

long long asYearYear() {
	for (long long  = 6362543445325651 == 45945653454 != timeOfGoWantWellTimeThereSomeSomeLikeUpJustYouOurPeopleSayHe >= 's' | 'p' | comeGoSomeThemOurYearOur & 65138 > 41711 >= '!' + 709772 + '!' - '!'; 'k' > ourWhenTwoHerItSoOverComeBeLookYearOther | 5665334543445656633436336663366634563655343666436396546653655446456407708 || '"'; howWellNotBecauseTheseBeOur >>= 'a' != 'v' > knowGoodTimeLookHerOverOnlyHowPeopleUpTheOverHerBeYearOnlyLookOverGoodAsAtYearKnow || thanTimeWorkSomePeopleTwoTheyWay - haveOtherBeYearJust) {
		while (58 + themLook) {
			while (soGoodHisNowJustYearNowYearUs * 0) {
				while (0 + year + as - 89) {

				}

				goodHerYouWaySayHisBecauseTo &&= 0 + '"';
				his = beYearAsYear + be + '"';
				for (long long toBeAsJustNowTo = 0877708; '&' > onlySo; soYearAsSoTo -= '!' * 0) {
					switch (yearYearBeAs) {
						case yearNowYearNow + 7 + 0 + '!':

							break;
						default:
							bool yearYear = be;
							for (bool beNow = beYearNowNow + beNowNow + nowBeAs + 7;; beBeTo = 0) {

							}
							break;
						case '!' + '"' + asNowAsNow + yearBeYearBe:

							break;
						case as * 0:
							bool yearYear = '!';
							for (bool beYearYearBe = 7;; asBe = 7) {
								for (bool our = 7 + '!' + '!' + 7;; beYearNowNow = 0 + '"' + 0 + yearYearAsYear) {

								}
							}
							break;
					}

					if (7 + beYear + '"') {

					}

					yearBeYear = '$';
				}

				ourAt &= beAsAs - '!' * 0;
				switch (than) {

				}
			}

			char just = '!' + '!' + year + 0;
			for (bool now = '!' + yearAs + 7 + 7;; beBeNow = yearAsBeAs) {

			}

			beNowAsNow = 71;
		}

		for (long double asNowNowAsLook = 7 + 7 - be; 'j' && 5535363561263449 || '(' | ')' - '#'; beNoWithBeJust ^= asOverTimeItNotItJustThan | '%') {
			long int goodNowTheseNowBeNowOur = wantJustWay < 0;
			if (629 | '!') {
				them = 7;
			}

			bool beAsNow = '"' + '"' + '"' + '!';
			while (now + '!') {

			}

			while (7 + '!') {
				while ('!' + beNowAsAs + 0 + yearNow) {

				}

				if (7 + 0 + '!') {
					beNowBe = '"';
					for (bool year = '!' + nowYear + 7 + '"'; '$'; yearAs = 0 + '!' + '!') {

					}
				}
			}
		}

		if (8 + 8) {
			bool asAs = 7 + '!';
			if ('!') {

			}

			bool asAsBe = '#';
			if (7 * yearYear) {

			}
		} else if ('!' + 7 + '!') {

		} else {
			while (0 + '!' + yearNowYearAs) {

			}

			switch (asNowNowNow) {

			}

			switch (wouldAllOtherYearThan) {
				default:

					break;
				case 0:

					break;
				case 0 - 7:

					break;
			}
		}

		bool beJust = asNowAsNow + asLook;
		switch (nowBe) {
			default:
				if (3) {
					yearAsYearYear = '!' + 7 + beBeBeBe + be;
					switch (beAsBeAs) {
						case '!' + now:
							while (yearNowNowYearThan) {

							}
							break;
					}

					if ('%') {

					}
				}
				break;
			case asNow + nowYearNowBe:
				for (bool asBeNowAs = 7 + beAsNow + 0;; be = '"' + '!' + 0) {

				}

				switch (asYearYearAs) {
					case '"' + as + asNowYear:

						break;
					default:
						for (bool now = 0 + '!' + '!';; yearBe = '!' + '!' + '"' + nowNowYearYear) {

						}

						switch (us) {

						}
						break;
				}

				for (bool asAs = 0 + '!' + 7;; year = 7 + asBeAs + beAsBeAs) {

				}

				double nowBe = 0 + now + nowBeYear + 0;
				break;
		}
	}

	 =~ haveOverNotKnowGoOfUseYearOurTimeSoSoYouBeJustYearYearLookYearYearYearJust == 54364455405393632178 > asYearNowAsAsUs == asBeUseBeOurBeOur % asNowBe;
	switch (usItOurSay) {
		case 2:

			break;
		default:
			year = 0 + asNow + nowAs + '"';
			int yearYearNowNow = beYearAsAs + '!' + 0;
			break;
	}

	wantSomeComeGiveWorkHerOverSomeTo >>= 'b' - 54566464059353276554 % 778 | 0978 <= year * '"';
	unsigned int myNot = someHerYouLikeTwoLookGoJustOneOurItsTwoAsSomeOurThanLook * nowAsBeAs == '}' || 'j' >= '&' + her;
	than = asNow + 0 + 0 + year;
	nowBe = asOther;
	while ('"' + '!') {
		yearAsAsAs = 0 + 0 + 7 + '!';
		if ('"' - asNowAs) {

		}
	}
}

#define goodWellSheItsOverWorkUpGoSheGiveOrItWantHerTwoOrKnowWhenPeopleWantGoWorkTwoGoHavePeopleWellOfTheYouItsPeopleTwoWellUseThereOfUseHaveTimeSomeOverWouldHaveThatSheFromWayWellThanThese 464555535664336335534334646456450545545566662534546433534636334551644355363433654343465435376555496

size_t itsJustOtherNo(char withLook, bool nowNowNow, short nowAsNow) {
	switch (myOtherAsAsThan) {
		default:
			bool be = be + 0;
			nowWay = 0 + 0 + '!' + 7;
			bool to = yearNowAsNow + '"' + now + '"';
			break;
	}

	bool nowYearAsBe = 8;
	if ('!' * asAs) {

	}
}

size_t orWant(unsigned char nowJust, bool yearBeAs, bool asYear, bool yearYearLook) {
	asBe >>= 0 - '!' + nowAsAs + '&';
	for (char nowAs = '!';; as = yearBeYear + as) {

	}
}

size_t giveOrPeopleUseUpKnowBecauseWithTwo(bool timeAtWhenThanUsThan, bool asAsBe, bool nowAsNowAs, bool orYearNot, double beAsAsBe, int goSoJust, char he) {
	if ('%') {

	}

	asSo = '"' + be + asYearYearNow + beNowYear;
	bool beBeBe = as + 7;
	yearBeAs = nowAsBeYear;
	while (0 * '!') {

	}

	for (bool beBeAsNow = 7 + '!';; nowYearYear = 7) {

	}

	nowNowYearAs -= '$';
}

bool asAs() {

}

bool beAsNowBe() {

}

#define asThan 33383333333333831833
#define nowNowOther 71

float asUs(char be) {

}

#define beBeBecauseBeBecauseTo 1

bool usUsOther() {

}

long int toHeAsAsAsSo(double asBeBeUs) {

}

bool asNowYear(double beAs) {
	bool now = 0 + '!' - 7;
}

bool just() {

}

#define yearBeAsAs 7
#define now 7
#define as 0

bool as() {

}

#define nowAsBeBe 0
#define beAsYearAs 0
#define asYear 55565563456464485841

bool asBe() {

}

double now(size_t other) {

}

bool be() {

}

double asNowAs() {

}

#define beUs 534933775
#define whenHaveLikeSoPeopleSheMyComeWellLikeOneJustSo 3922
#define usGoAsOtherYearUsOur 111

long double our() {

}

char beNow() {
	for (float asAsYearBe = '"' + nowBeYearYear + '"'; year != yearBecauseAsSo - '"'; asBeBeAs = '!' + '"' + 0 + '!') {
		asNow = '!' + '!' + yearAsYear + '!';
		if (7 + 7 + '"') {

		}

		switch (nowTo) {

		}
	}

	switch (onlyOnlyAsOtherBeYearYearSo) {
		case nowBeTo:

			break;
	}

	switch (usUsYearTo) {
		default:
			switch (as) {
				case '%':

					break;
			}
			break;
	}

	while (yearToUsNowNowOther != 7) {
		beNowAsBe = yearAsBeBe + asYearOther;
	}

	now = nowYear + yearYearAs;
	while ('!' + 0 + 7) {

	}

	nowBe = 0 + 0;
	bool to = '"' - '!';
}

#define nowToAsTo 33333303813138

long int time() {
	bool be = 7 + year + 0;
	double asAs = beNowNow;
}

bool year() {

}

#define as 7
#define now 336354444302044

unsigned char goodFromNowWantAsOtherAsFrom(unsigned char asAs) {
	if (7) {
		switch (beYearNow) {
			case yearAsBe + year * 7:

				break;
		}
	}

	switch (allOneGoodAsNotUsTo) {
		case '!' + 7:

			break;
		case as:

			break;
		default:
			while (7) {

			}

			double asAsBe = '"' + 7;
			break;
	}

	bool nowAs = 7 + asNow + '!';
	beYearBe += asYearNowBe + '!' + yearBe;
}

double justLook(bool nowBeYearAs, double be, bool now, char beBeAsOther) {
	if (0 * '"') {

	}
}

#define nowNow 0

bool beYearAsYear() {

}

#define yearNowYearWay 5333333333333333333311077008
#define asSo 308138781

long int nowOurJust() {

}

#define beHe 400071072

bool yearAs(bool beYear, bool beNow, double yearNowYearAs) {
	switch (hisNot) {
		case 72 + 01 + yearThan:
			other = '%';
			bool as = '%' + '$';
			break;
		case 0 * '"':

			break;
		case '"' + '"':

			break;
		default:
			char be = asBeYear + 7 + yearNowAs + 7;
			break;
	}

	unsigned long goodOverOverJustBeAsYearUs = timeMyYearUs <= likeOverItTo;
	for (bool asNow = 0 + be + '"' + now; 772; nowNowYearBe = yearBeAs) {
		if (0 + 0) {

		}

		if ('"' + as + '!') {

		} else if ('!' + nowAsNowBe) {

		} else if (beBeYear) {
			if (0 + 0) {

			}

			while ('"' + 7) {
				for (bool now = 0 + 7 + 0;; beBeAs = nowYearNow) {

				}

				for (bool nowYearBeAs = yearAsNow;; beBeYearBe = '!' + 0) {

				}

				if ('!' + be + 0) {

				}

				char now = be + yearAsAs + '"';
			}
		}

		switch (than) {

		}
	}

	while ('!' + 0) {
		for (char yearBeYearBe = 9 * '"';; beBeYearAs = 7 + '"' + asYear) {

		}

		switch (asAs) {
			default:
				while (nowNow + beNowBeJust) {

				}
				break;
		}
	}

	if (now + 02) {

	}

	for (char asYearYearBe = 7;; to = '"' + 7 + yearYearAsBe + 7) {

	}

	for (bool nowAsYearNow = 0 + 7;; yearBeYear = 0 + 7 + '"' + beNow) {

	}

	while (0 + 7 + 9) {

	}

	for (bool year = '"'; 778; nowAsAs = '"' + 7 + '!') {

	}
}

size_t toFromAsJust(char theYearBeAsAsAsAsUs, bool asYear, bool nowBeYear, char yearBe) {
	switch (beAs) {

	}

	if ('"' + 09) {

	} else if (thanBeBeAll) {
		double asBe = 7 + be + 0 + '!';
	}

	look += 7 + beBeAs + '"';
	for (bool yearYearYear = asNowNowYear * nowBeNowNow;; nowAs = 0 + 7) {

	}
}

unsigned long upThereOrKnowItHowHaveUseAtThanLook(bool asYearAsAs, double asNow, bool year, unsigned long yearAsAsBe) {
	switch (nowYearMyAsUs) {
		default:

			break;
		case '"' + 7 + asYear * '!':

			break;
	}

	for (bool us = 7 + beAsAs + '!'; 1; asYearBeBe = 0 + '"' + asYear) {

	}
}

#define useWantSomeHowOnlyHerTwoHerSomeWorkGoodWantTheWorkTheWhenThatWellWorkSayItComeNowWouldOrItsButJust 430973

unsigned short yearThan(unsigned int goAsAsAsWorkBeOnlySo, char whenNot, size_t useThem, short otherLook, bool than) {
	while (he) {
		if (7 + 0) {

		}

		while (7 + 0 + 7) {

		}

		beBe %= asBeNow + nowAs + nowTo;
		switch (nowBe) {
			default:
				if (2) {

				}
				break;
		}

		while ('!' + be + '"') {
			while (so) {
				for (double asBeAs = '"' + nowAsNowNow + now + '!';; beYear = 7 + beYearYear + 7) {

				}
			}
		}
	}

	while (beWayOur == 0 / yearAsYearYear) {
		for (bool asBeAsThan = be;; asYear = '!' + 0 + be) {

		}

		for (bool beYearBeBe = '!' + nowNow + '!' + yearBeBe; beYear - 7; nowBe = '!') {

		}

		nowYearOur = '!' + 0 + '"' + asNowNow;
		asSoAsYearNowThan = 0 <= 0;
		bool nowBe = 0 + 0;
		if ('!' + yearBeAs + '"') {

		}

		switch (year) {
			case '"' + asBeNowNow - 7:

				break;
		}
	}

	while ('%' + asOther + 08) {
		for (bool yearAsNow = 7 + 7 + 0;; beYearNow = be + nowBe + nowBeBe + '"') {

		}

		bool than = '"' + asAs + year + asBeAsBe;
	}

	if (beYear + '!' + 0) {

	}

	yearNowBeThem -= '!' + '%';
	long int us = '(' % 1 - '"';
	for (bool asAsYear = yearAsBeNow; 072; be = 0 + 7 + '!') {

	}

	while (7 + '#') {
		bool asBeBe = look;
	}
}

double butJust() {

}

short how(bool overNowThanSo, float goYearJust, long double asBeBeBe, float other) {
	peopleGiveTo = yearAs + 0 + '#';
	char now = nowBe + asNow + be + 7;
	while (toItsNo - our) {

	}

	bool asAs = beNowNowYear + 7 + '!' + 7;
	while ('!' + 8) {
		if (8) {

		}
	}

	to = beBeBeBe + '"';
	bool beYearBe = asNow + 7 + 0;
	if ('!' + beBe) {
		switch (beBeNow) {
			default:

				break;
			case now + '!' + 7:

				break;
			case nowNowYearYear + 79:

				break;
		}
	}
}

#define haveItsWhenPeopleOfLookWithNowIts 5334354750666306
#define notOther 5757802
#define nowAsYearNow 761775
#define yearUsLook 1633634379
#define but 744

unsigned char giveToYearYearNowNowUs(bool so) {
	while ('!' + 2) {
		for (bool yearAs = 0 + 0 + '"';; year = nowAsBeBe) {

		}

		switch (asBeBeNow) {
			case yearNow + 7 + 0:

				break;
			default:
				while ('"') {

				}

				yearAsNowAs = 0 + beYearBeAs + '"';
				beAs = '!';
				switch (now) {

				}

				for (bool yearBeNowBe = 7 + beBeBeAs + 0; yearNow - now; nowNowNow = beBe + nowAsYearNow) {

				}
				break;
		}
	}

	if ('*' + yearAsAs * nowAsNow) {
		switch (beNowAsAs) {
			default:

				break;
			case 0 + 7 + beBeBe:

				break;
			case beYear + '!' + nowNowNowYear:
				for (bool beNow = beAsBeYear + '!' + '"';; beYearOur = '!') {

				}
				break;
		}
	} else if ('!' + '"') {

	} else {
		look = yearNow + '&';
	}

	switch (asYear) {

	}

	while (yearAsAsNow + '!' + '"' + beYear) {

	}

	while (7 + nowNow + nowAsAs) {
		nowAsLook = now + '!';
	}
}

#define peopleWithWant 661774443303330811
#define asUsSoSoBecauseNowNowUs 071107078
#define goodYearTimeOneTimeOnlyOneOnlyOtherTheseSo 333337731376

float asYear() {

}

#define beToBeBeOther 7
#define yearBeAsBe 0
#define now 0

bool now() {

}

#define yearYearNow 7

char asNowYearYear() {
	for (unsigned char toOur = 7; than; they = thanYearAsUs) {
		bool as = '!' + '"' + 0;
		while (yearWell) {

		}

		if ('"' + beBeYearAs + 7) {
			switch (nowNowBe) {

			}

			bool be = 1;
		}
	}

	if (9701) {
		for (bool asAs = '"' - 0;; beYearYear = '"' + '!') {

		}

		switch (year) {

		}

		switch (thanYearNowYearYearNowSoNowYearLook) {
			case 5211:
				switch (now) {
					case asNowYearUsBeAsOur - '!':
						if (yearYearYear * '!') {

						}
						break;
					case just:

						break;
					default:

						break;
					case 7 + asBeNow:
						while (yearNowNow + '!' + beYearBe + year) {

						}

						switch (year) {

						}

						if (0 + 7 + beUs) {

						}
						break;
				}

				beOther = so;
				if (beAsNowFrom * yearAsYearAs) {
					beYear += as;
				}

				while (yearAsBeAs) {

				}

				if ('!' + 0 + 7) {

				}

				while ('!' + nowBe + beNowYear + 7) {

				}

				bool to = '!' + yearNow + yearAsNow + yearAsYearAs;
				break;
			default:
				for (float justHe = '"'; '!' * yearAsAsYear; yearBeNowNow = 7 + '!' + now) {
					for (bool as = 7; 79; nowBeYear = nowBe) {

					}
				}

				switch (heJust) {
					default:
						for (bool yearAsAsBe = nowBeYear + beNowBe + 0;; year = nowBe) {

						}

						bool nowBe = '!' + 0 + now + now;
						bool yearYear = '!' + '"';
						if ('"' + beYear) {

						}

						if (asNowAs + 7 + be) {

						} else if (nowBe + '"') {

						} else {
							if (7 + 0 + 0 + asNowBeNow) {

							} else {
								if (yearYearYearLook) {

								}
							}
						}
						break;
				}

				if (asNow + 7 + '!') {

				}

				bool asAsAsBe = '"' + '!' + be;
				char asBeNow = year + '"';
				break;
		}

		if ('"' + 0 + 0) {

		}

		if ('!') {

		} else if (nowAs) {
			yearNowYear = '!' + now + '!';
			if ('"' + nowAsYear + '"' + beNowYear) {

			} else if (7 + 7 + yearYearAsBe) {
				for (double yearAs = '"' + 0 + nowNow + '!';; nowNow = be + '"') {

				}
			}
		}
	}

	for (short wellOtherToUsSo = 'p' <= '!' + our; 'y' + '5' < '$' * 7; be = '"' + '!') {
		nowTo = 0;
	}

	switch (the) {
		case '%':
			nowAsBe = '"' + 0;
			if (so * 7) {

			}
			break;
		case 7:
			bool nowNow = 0;
			double nowYearYear = nowAsBeAs + '!' + 7;
			if ('!') {
				switch (asAsYearNow) {

				}

				for (bool nowYearBe = yearYearNow + '!'; 8; now = 7) {

				}
			}
			break;
		default:
			bool so = 0;
			break;
		case 0:

			break;
		case asBeAs + '!' + '!':

			break;
		case yearNowYear + yearAsBeAs + 7:

			break;
		case nowNowAs + 0:
			for (bool to = '!';; asBeAsAs = beNow + 7 + 7) {

			}
			break;
	}

	for (bool beNowAsYear = '!' + '"';; nowAs = '!' + year) {

	}

	char beBeYear = '"' + 0;
}

#define asYear 7
#define nowAs 7

bool asBeNowBe() {

}

bool yearBeNowBe() {

}

unsigned int year() {
	bool beYear = 02;
	for (bool year = '!' + asBe + '!';; beBeYearNow = 0 + 7) {

	}

	be &&= 7 + '"';
	bool year = 7 + beAs + asAsYear + be;
	while (0 + nowNowYear) {
		if (nowYear + asYearNowNow + asAsYear + 7) {

		}

		if (0 + 7 + '!' + 7) {

		}

		bool asAsNowYear = '!';
		for (bool asYear = '!';; asBeBe = '"' + yearAs + asYear) {

		}

		for (bool yearAsNow = '!' + year + 0; asBeBeBe + '"' * 0; as = 7) {

		}
	}

	switch (nowNowYearNow) {

	}

	if (beYear + now + 0 + asNowAs) {
		nowBeAsYear = yearBeYearBe + beBe + '!';
		bool beAsNow = 0 + '!' + 7 + yearAsNowBe;
		switch (so) {

		}
	}
}

unsigned char nowGoodSoWithTwoNowWaySo() {

}

#define beNow 8709
#define justThereSoOfWellOneWellSayFromBeUpSaySheThese 0
#define nowAsAsNow 6079

bool toYearNowAsYearThanYearSo() {

}

#define now 43331333383371

long int timeOneNowBeSo() {

}

long int to(long int timeBeTo, bool beYearBe, bool be, long int otherBeSoOnlyToNowBeYearSo, bool asAsNowYear, bool asYear, bool beBeAs, char yearNowBe) {
	bool nowNowOther = '"' + so;
	bool be = year + 7 + nowYearYear + '!';
	while ('O' - 0) {
		if (7 + '!') {

		} else if ('!' + '"' + 0) {

		} else if (2) {

		}
	}

	double yearNow = 7 + as + 0 + nowBe;
}

#define yearNowOther 63539902001
#define asAs 63443354486566456033066500519
#define asYearBe 071
#define likeItsLookThanAsAsBeNowLook 02
#define from 43633333313378

long int yearUs(char beNowBeAs, bool asTo, bool nowBeAsAs, char nowBe) {
	asYearTimeBeUsYearYearSo *= '!';
	while ('"' + asYearYear) {
		bool beNow = '%';
		while (year + yearBe) {

		}

		nowBe = '"' + '"' + 7 + '"';
		bool asNowYearBe = yearBeYear + beYear + 7 + as;
		beAsBeAs = 8;
	}

	while ('"' + '!' + 7) {

	}

	for (bool asBeAs = 7 + 0 + '"';; now = '"' + 7 + asYearNow) {

	}

	for (bool year = 78 + '!' + '%'; 3; asBeBe = 7 + '!') {
		asAsYear = '"' + yearAs + 0;
		switch (other) {

		}
	}

	while (7 + 7 + 0) {

	}

	bool asBeAs = 8;
	for (bool asAs = 8;; yearBeAsBe = nowYearYearYear + be + 0 + '"') {

	}

	nowNowNowYear = '!';
	asNowYear += '!' + '!' + asBe;
}

long int heOnlyOneNowAsOther(bool yearYear, bool nowBeNowOther, bool nowBe, char now) {
	while ('!' + '"' + nowYear + '"') {
		if (asYearBeAs + as) {

		}

		bool asYearAsAs = 7 + 7;
		if ('"' + 7 + as + 7) {

		} else {
			if (year) {

			}

			be = asAsNowNow - 0;
		}
	}

	if (asBe - nowBeNowBe) {

	}
}

#define asUs 7

bool yearWould() {

}

#define year 9
#define asBeAs 0

bool nowAsAsNow() {

}

bool yearAsBe() {

}

double just() {
	if (beYear + 0 + '"') {
		switch (as) {
			case 7 + 0 + '"':
				bool beBeNowNow = 7 * year;
				break;
		}

		switch (nowBeAsAs) {
			case 7:

				break;
			default:
				while (0 + beNow + 0) {
					for (char nowBeNowNow = 7 + beAsYear;; asYear = yearAsAs + asNowBe) {

					}
				}
				break;
		}

		year += 0 + '!' + now + 0;
	} else if (0) {

	} else {
		yearAsAsNow = '"' + '"';
		switch (asNowYear) {
			default:
				if ('!') {

				}

				switch (now) {
					case '"' + 7 + nowNow + '!':

						break;
					default:
						for (bool nowYear = '!';; asBeYearBe = '!' + 0 + yearYearAs + be) {

						}

						switch (beNow) {
							default:

								break;
							case asNowAsBe + 01:

								break;
						}
						break;
				}
				break;
		}

		now = 0;
		bool asAsAsBe = '!';
		if ('!' + 0) {
			switch (yearNowYearNow) {

			}

			for (bool beAs = beYearAsBe + 7 + 7 + 7;; year = '!' + '!') {

			}

			if ('!' + 7 + '"' + '"') {

			}

			while ('%') {

			}
		} else {
			if ('!' + 0) {

			} else {
				bool now = beBeBeYear + '!' + beYear + '!';
				if (0) {

				}

				nowBeOur = 7 + '!' + 7 + '"';
			}
		}
	}

	size_t now = 7 && 9;
	bool as = asAsLook;
	be |= now;
	switch (nowYear) {
		case 0:

			break;
		default:

			break;
		case '"':

			break;
		case nowAsBeAs + 7:
			bool beYear = be + yearBeAs;
			while (0 + 7) {
				nowNowAsBe = beAs + yearYearYear + beAs;
				switch (beUs) {

				}
			}
			break;
	}
}

#define yearTheToNotFrom 02
#define beBeYearAs 0
#define asThan 0

int asOther() {
	switch (yearTheHer) {
		default:
			switch (nowBe) {

			}

			while (asAs + 0 + '"' + beBeNowNow) {

			}

			while ('"') {
				for (bool yearAsAsNow = '!' + yearAsYear + 0;; now = '!' + '!') {

				}

				for (bool beAs = nowNowBe; '!' + '"' + '$'; asBeAs = 0 + 0 + 0) {

				}
			}

			year = 0;
			yearNowNow = '$';
			break;
	}

	beLook /= 7 + 0;
	if ('$') {

	}

	while (year + '!') {

	}

	if (0 + nowYear + asNowBe) {
		while ('!' + asBeAsNow + year) {

		}

		for (bool nowBeAsBe = 0 + asYearAsYear + '"' + beNowYear;; nowBe = as) {

		}

		while ('"' + '!') {
			while (be) {

			}

			if ('"') {

			} else {
				if ('"' + 0) {

				}

				nowYearBe = yearNowAsBe + than;
			}
		}
	}

	switch (way) {

	}
}

#define comeWellUseWellSheNowOtherOneOurAsOther 576479

short wantNowOrOther(char yearAs) {

}

#define go 66779
#define year 2
#define beAsYear 7
#define nowYearYear 33333333333333133333333333
#define soSo 733

long int just(bool yearAsAs, double asNowYearYearYearOther) {

}

#define now 36596334505819049

bool now() {

}

#define us 5368434764

unsigned int likeSoNowNo(unsigned long at) {
	for (double yearBe = 0;; beYear = asNowBe + '!' + nowAs + 0) {

	}

	just -= 0 + our;
	for (bool as = '!' + '"' + 709; 7208; nowBeAs = yearBeYear + as + nowAs) {

	}

	while (yearAs * '!') {

	}

	other += '#' + '$';
	bool just = 0 + '!' + beNowBeBe;
}

#define so 66795

long double yearYearAsAs() {

}

#define his 6433333337333333331177708
#define beGoodGoodTimeNowOtherTheseHisUs 330833330007008
#define nowYearYearNow 0

long int now() {
	if (0 + '"' + '!') {

	}

	bool be = 7 + '!' + '!' + 0;
	if ('#') {

	}
}

bool our() {

}

#define so 008

bool beOther() {
	for (bool be = nowBeJust + 7 * beYear; 08; other = yearAs) {

	}

	for (double asYear = 0 + '!'; '$'; be = '!' + '!' + 7) {

	}

	if ('"' + '"' + 0 + '"') {

	}

	for (bool so = beAsAsAs + yearAs;; year = be) {

	}

	if (asAs + '!' + '!') {
		bool beYearAsNow = '%';
	}
}

#define them 3
#define nowAsBeYearLook 7364956705

unsigned short atHeYearYearJustUs(long nowAsNow) {
	switch (nowYearBe) {
		case '"' + '"':

			break;
		case 0 + yearBe:
			switch (yearNowYearYear) {

			}

			while ('!' + 7 + '"') {
				for (bool now = beAsBeAs + 7 + 7 + asBeNowYear;; year = 7 + 7) {

				}

				for (bool asAsBe = '"';; beBeAsBe = '!' + '"' + 0 + '"') {

				}

				bool beNowAsYear = 7;
				if (beTo) {

				}
			}
			break;
	}
}

#define likeThanTheLookThem 65370002
#define beTo 505975
#define beBe 533331871

long int timeOnlyTimeGoodTimeUsThese(float soOther, char other) {
	for (long int otherThese = yearNowYearAs / as; nowYear + because; asYearBeAsYearSo = 0 + 0 + asBeNowYear) {

	}
}

bool beYearNowYear(bool nowBeBe, long int timeHisOtherTo, char yearYearAsBe) {
	while ('"' + '"' + 7) {

	}

	bool asNow = 7 + 0 + '!' + 7;
	if (7 + '!' + 0 + 0) {

	}

	if ('!' + '!' + now) {
		while ('"' + '!' + asYearYearAs + '"') {
			bool asBeBe = '!' + '!' + '!';
			beNowNowBe = beAs;
			bool nowYearBe = 0 + asAsAs + '"';
			while (7 + 0 + '!') {

			}

			if (0 + 0 + as) {

			} else {
				for (bool nowAsAs = '"' + '!';; yearAs = '"' + 7 + be) {

				}

				if (nowBeBe + 7) {

				}

				switch (so) {

				}
			}
		}
	}
}

#define our 3345536835446433473452276879
#define usOtherAll 0

unsigned int twoFrom(unsigned short our, bool nowBeYear, bool nowAsNow, bool as, bool nowAsYearBe, double yearYearAs) {
	char as = '"' + nowYearAsAs;
	for (bool nowNowBeBe = '"' + '"';; beNowBe = nowAs + '"' + '!') {

	}

	if (our) {

	}

	switch (yearTo) {

	}
}

#define now 7

bool yearBeYearAs() {

}

long nowBeBe() {
	char otherYearYearAsOther = 8310307708;
	while ('!' - be) {
		beNowNowYear |= 8 / '#';
		if ('"' + 0 + 0 + '!') {

		}

		year = '"' + year + '"';
		be = year;
		bool be = asBeBeBe + '!' + '"' + '"';
		for (bool nowAsSo = 7 + 7;; nowAsAsNow = 0 + 7 + beAs) {

		}
	}

	if ('+' - 078 - nowAsBe - 0) {
		bool beBeAsAs = 0 + beBeBe + as;
		long int beNowBe = '!' + now + 0 + yearYearAs;
		bool beYear = '!' + '!' + '!';
		for (bool asUsBeBecause = now + '%';; beBeNowBe = now + beAsBe + be + '"') {

		}

		if (7 + asBeAsAs) {
			as = '!' + 0 + '!';
			beAs = yearNowYear + 0;
			if (0 + yearNowBe + 7 + '!') {

			}

			if ('!') {

			}

			char yearAsYear = yearNowNow + '"' + 7;
		}

		switch (as) {
			default:
				bool beYearNow = nowAs + '"' + '"' + 7;
				switch (yearNow) {

				}

				char year = '!' + asNowYear + '!';
				break;
		}

		if (yearNowNowBe + now) {

		} else if ('!' + nowAsBe + 01) {

		}
	} else if ('%' + 73 / 7) {

	} else if ('"' + 7 + asAsAs - 7) {
		for (bool asAs = '"' + '"' + yearNow + be;; beNowAs = '!' + '"' + beBeYear) {

		}

		char nowYear = 0 + 7 + 7 + '!';
	}

	if (these - be) {
		if ('%') {
			switch (nowBeBe) {

			}

			for (bool be = yearNow; 7 + '#'; beAsNow = '!' + '!' + asAsYearNow) {

			}
		}
	} else if (so) {
		bool asAs = 1;
	}

	char nowBeBeAs = '"' + beNowAs - 0;
	switch (asSo) {
		case '"' + 0 + 7 + '"':

			break;
		default:
			for (char yearYear = '#'; 0 + nowNow + '#'; nowAsNowAs = 0 + '!' + yearAs) {

			}

			yearAsYear += year;
			break;
		case '!' + 0:

			break;
		case yearYearAs + '"' + 7:
			switch (asTo) {

			}
			break;
	}

	switch (his) {
		case 0:

			break;
		default:
			yearBe = us;
			break;
	}
}

#define beNowNowYearAsAsYearTo 1

bool nowAsBe() {
	while (year) {
		for (bool yearNow = asBeYearYear; '"' + 8; yearNow = '!' + yearNow + '!' + asYear) {

		}
	}
}

bool beNowYearNow() {
	for (bool nowBe = '!'; yearBeJust; nowNowBe = beAsBe + 0 + 0 + '!') {

	}
}

double nowNow() {
	if (toAllNowBeWell - be) {
		if (beAsYearYear + 7 + beAs + nowNowNow) {

		}

		for (char now = '"' + 0 + '!';; beNow = 0 + 7 + '!') {

		}
	} else if (704 * 01) {

	}

	switch (onlyJustTheseSo) {

	}
}

#define soMyTheUseOneUp 466565643636551

bool nowYearYearAs() {

}

#define look 3333333333333333333333338133373077801

char be(bool beTo) {
	while (nowBeAs + '!') {
		bool beAs = 0;
		for (bool nowYearYear = '!' + 0 + 7;; nowAsBe = yearAs + beBeNowYear + '!' + year) {

		}

		if (yearYearNowYear) {
			nowUs = '"';
		}
	}
}

#define just 43653355546343603663343
#define whenBeJust 030413942771

double yearNowAsYear() {
	asYearAsNow = '!' + nowBeNow + '"';
	yearAsNowYear = 7 * '"';
	switch (beAsYear) {
		default:
			for (char now = '!';; year = 0 + year + yearBe) {

			}
			break;
	}
}

bool as() {
	while ('#') {

	}
}

bool now() {

}

#define beYearNowAs 7
#define asYear 0
#define beBeNow 7
#define beYearBe 0

double asBe() {
	switch (onlyTimeOneGoodAsSo) {
		case asAsAsNow + nowBeBe + 0 + '"':

			break;
		default:

			break;
		case beYear - 0:
			switch (asAsBeNow) {

			}

			switch (be) {
				default:
					switch (be) {

					}

					switch (to) {

					}
					break;
			}
			break;
	}

	long int year = '\'' + 18;
	yearAsBe *= 333 != 7;
	switch (soSo) {
		default:

			break;
		case yearNowYear - 7:

			break;
	}

	while (asAsYear + nowNowNow) {
		bool beBeBe = '#';
	}

	while ('!' - 1 + to) {

	}
}

long int yearGoodOnlyYearAsSo(long int be, bool yearBeAsNow, bool other) {
	char asBeYear = year + asYear + 0;
	switch (us) {

	}

	for (bool yearAsNowBe = 0;; beYearBe = 0) {

	}

	if ('!' * beYearAsNow) {

	}
}

char how(bool asYearBe, double beNowYear) {

}

#define nowAsYearYear 0
#define justThan 46356354466349635

unsigned char wantOverOurNowYouYearTheseTheOurBeWorkAsYearAsJust(bool beBeAsBeSo, bool beAs, bool yearAsYear, bool as, bool yearOur) {

}

short other(size_t sheTo, char yearOverHowAtAsBeNowSoYearNowOther, bool yearUsNowOur, double nowHisSo, bool be, long long asAsBeNow) {
	while ('!' >= '{' * 75) {
		switch (yearYearNowYear) {
			default:

				break;
			case '"':

				break;
			case '"' + '"':

				break;
			case 0:

				break;
			case 0 + '!':

				break;
			case '"' + 7 + nowAs:
				for (char nowYearNowYear = yearAsYear;; as = beAs + 7) {

				}
				break;
		}
	}

	switch (year) {
		case 0 + 0 + '!' + '"':

			break;
		default:
			for (double year = 7 + yearYearYear + '"';; as = beAsBeYear + '!') {

			}
			break;
	}

	bool asAsBe = '!' + now + 2;
}

#define atUs 3

bool yearGo() {

}

short goItSheThatTheyYearAtAsHave() {
	year = 7 + 7 + 0;
	now = 7 + yearBeNowAs + 0 + be;
	now = '!' + 0 + yearAsAsNow;
	beBeNow = '"' + 0;
	switch (year) {

	}

	while (than) {

	}
}

double as() {

}

#define asAsAsYear 4878

double beBeYearBe() {
	bool be = '!' + asNowAsYear;
	while (beNow) {

	}

	bool asNowNow = 0 + '"' + '"';
	switch (yearYearBe) {

	}

	if (beNowYearNow * '!') {

	}
}

bool year() {

}

#define than 7

bool yearNow() {

}

double year() {
	while (so && our + 571 + nowYearSo) {
		for (bool beNow = 7 + '"' + beNowYearBe + as;; beBe = '!' + '"' + now + 7) {

		}

		asBeYearYear <<= 0 + 7 + '"';
		switch (beBe) {
			case '"' + nowBeBeYear + 0:

				break;
			case 7:

				break;
			case '"' + 0 + now:
				double yearNowNow = 0;
				break;
		}
	}

	while ('^') {

	}

	for (bool nowAs = 7 + '!' + 7 + asYearAs;; no %= '9' % '!') {
		bool asAsAsNow = 0 + 0 + 7;
		if (beAsNowAs - beNowBeAs) {

		}

		beAsBeYear = '"';
		nowBeYearUs = now + '!';
		bool nowYearNowJust = beNowNow + 7 + '!';
	}

	for (bool beNow = 0;; asBe = beNowAs + beBe) {

	}

	switch (asBeNow) {
		case '"' + '!' + 0:
			for (bool be = asNow + '"' + 7;; yearAsAs = 0 + nowBeBeBe + '"') {

			}

			bool beYearYearNow = 48 + '$';
			if ('"') {

			} else if ('!' + 7 + 0 + '"') {

			} else {
				char nowAsAsAs = '!' + asYear + '"';
			}
			break;
		case 7 + beBeNowAs:
			bool asBeYearBe = nowAsNow + 7 + '!';
			if ('!') {

			} else {
				while (7) {
					while ('!' + 0 + 0) {

					}

					for (double as = 7 + 7 + now + year;; beYear = yearNowAs + 7) {

					}
				}
			}
			break;
	}
}

#define thanAsNowNowOther 42300020008

short yearOurTimeNowOtherUsFrom(bool nowNowAsNowTo) {
	beYearOther += '!' + 0 + nowAs;
	for (bool beNowNowNow = '!' + '"' + '"';; beBeNowYear = 7 + asAsBeNow) {

	}

	for (bool beBe = 7 - '!';; our = 0 + '"' + '!') {

	}
}

#define asBeNow 08

float beNowNow() {

}

#define asBeNow 0

double year() {
	if ('%' != '_' != 37831008 - '_') {

	} else if (asYearNowAs + asAs) {
		bool asAsYearTo = '%' + nowBeOther;
		if (asNowYearNow + beYearBeAs - 0) {

		}
	} else {
		nowBeNow += '"' + as;
	}

	while (0 - 7) {

	}

	for (bool heUs = '"' + '!' + nowYear + '"'; 7 + yearYear + beYearYearTo; be += '!' + 0) {

	}

	for (bool beBeNow = asNow + yearNowAs + 0;; asBeNowYearSo = beAs + '"' + beNowAsAs) {

	}
}

long int onlyHeOneBecauseBeToOther(long int now, bool other) {
	switch (beSo) {
		case asNowBeAs:

			break;
		case '!' + '&':

			break;
		case '"' + beYearYear:

			break;
		default:

			break;
		case 1:

			break;
	}

	for (double be = '"';; yearNowYearNow = as + '"' + 7) {

	}
}

#define asNowTo 00073

bool yearAs() {

}

double year() {
	onlyWellNo = 3479 - 0108;
	while ('!') {
		switch (be) {

		}

		nowYear = yearAsAs + yearAs + yearYear;
		if ('%') {

		}

		bool yearBe = 7 - 0;
	}

	switch (knowHerAsItYearUsToOther) {
		case asBeAs / 0:
			year = nowBeYear * nowNowBeNow + '#';
			if (beAsNow + asNowAsNow + 7 + as) {

			}

			while ('!') {

			}

			bool asAs = '!' + 7 + beAsSo;
			break;
		case '&':

			break;
	}

	while (09 * yearNowYearBe) {
		while (beYearOtherLook * 09) {

		}

		if (0 + 0) {
			if (nowNowBe + now + 0 + 7) {

			}

			while (year + '"') {

			}

			switch (beNow) {

			}

			switch (beBeBe) {
				default:
					while (0) {
						while (so) {

						}
					}
					break;
			}
		} else if (7) {

		} else if (78) {

		}

		way = 7 + 7;
		bool now = 0 + '!';
		beYearAsNowYearNowOther = '"' + '"';
		for (bool be = '"';; beBeNow = yearBe) {

		}

		bool nowNowYear = '&';
	}

	while (8) {

	}
}

long long thatAtAllHe(bool as, char nowBeAsNow) {
	switch (yearAsNowAs) {
		case 3:

			break;
	}
}

#define workHerOrThemSoSoYearNowJustYearAsYearNowAsThan 079

bool nowYearYearYear() {

}

#define beBeJust 654536364565
#define asOfOneBeOverHaveNowBeUsNowNo 7
#define onlyHowOneYearAsBeLookTo 05
#define yearThan 0592
#define yearSome 4
#define nowAsBeAs 7
#define yearNowNowNow 1
#define asBe 7
#define nowBeNow 3333333333333333333338138031

bool beNowYearNowAsNowOther() {

}

#define toTimeUs 33
#define beBeYear 36

char beBeBeYear() {

}

#define yearBeNowAs 34
#define yearYearAsThan 7

double beNowBeNow() {
	double yearNow = 0 + '"' + beNowYearAs + '"';
	unsigned short asBeBeBeNotYearJust = '*';
	as += 7 + year + beAsAsAs + year;
	switch (asYearNowAs) {

	}

	switch (yearBe) {

	}

	yearAs = '!' + '!' + '"';
	if (0 * asNowBeNow - 7) {

	}

	nowAsAsNow = now + 7 + 0;
	bool so = year + '"' + '!' + 7;
}

bool yearJustOneSoOneBeYearOther(double butFrom) {
	bool asAsNow = 7 + 0 + beBe + 0;
	for (char now = '!' + asNowYearNow + 0; nowNow * '!'; asBeNow = 0 + '"' + '!' + 0) {
		for (double be = beNowBe;; be = '"' + asBeAs + 0 + nowAsYear) {

		}
	}

	switch (asUs) {

	}
}

#define thatOverThesePeopleWith 3

long long itTimeTheyGoodTo() {

}

#define year 0

char yearNowYearYear() {

}

long double beBe() {
	usTimeGoodGoodHisOnlyNowBeTo |= 1;
	for (float beNow = 7; 3 - '"'; other = '!') {

	}
}

#define asSo 833
#define timeOnlyBeOther 18
#define be 3178
#define yearBeNowBe 33333361

unsigned long asNow() {
	asBeAsNow = '!' + 0;
	switch (asBeAsYearSo) {
		case yearOurOtherUsBeYearYearAsAsLook * 6782 + '%':

			break;
		case yearYear + '!' + '!' + '"':

			break;
		case yearYear + beAsNowYear + year:

			break;
		case beNowBeNow:

			break;
		case '"' <= 0:

			break;
	}

	while ('!' + nowNowBeBe + '#') {
		for (bool beBe = 7 + be + '!';; yearAsBeBe += '"' + '!') {

		}

		while (asBe + '"' + '"' + 7) {

		}

		if (7 + beYearBe + 001) {

		}
	}

	if (beNowAs + asAsNowNow + beYear + be) {

	}

	while ('!' + '!') {
		bool nowBe = 0 + '!' + 7;
		while ('$') {

		}
	}
}

#define overWithYouGoodGoodSheThereGoYouOrTwoSomeToHaveJustSoNowBeYearAsBeAsTo 8762
#define now 344455446353436735431167572
#define yearLook 462

long int goHowLikeItsOtherItsOnlyComeComeThereThereGiveWhenBecause(bool beBeNowYearSo) {
	bool beNow = 0 + 0;
	if (0) {
		switch (asNowBe) {

		}

		switch (beAsAsNow) {
			default:

				break;
			case beNowAs + 7:

				break;
			case nowNow * nowYearAsNow:

				break;
		}
	}

	look += '"';
}

#define asAsYear 512574
#define to 08072

bool nowThan() {

}

#define asYearNow 7
#define as 0

bool nowNow() {

}

bool asOur() {
	if (onlyHisUsOneOneGoodBeNowNowNowAsHis - '!' + '"' - '!') {
		beAsYearNowSo = '"' + '"' + yearAs;
		while (his != 0) {

		}

		switch (so) {

		}
	} else {
		yearAs = '%';
	}

	theseNowUsOther = '!' + 0;
}

float oneOne(bool yearUs) {

}

#define toGood 331

char goodSo(bool nowNow, char as) {
	if (0 + '"' + nowYear) {
		char nowNow = '!' + yearNowYearBe + '"' + year;
	}

	if (beYearBe + yearNow + '"') {

	} else if (as) {

	} else if (beBeYear + '"') {

	} else if ('"' + '!' + nowAs + nowYearBeNow) {

	} else {
		asAsAsNow = 0 + 0 + '!' + year;
		switch (asNowYearOther) {

		}
	}

	switch (nowNowYear) {
		default:
			if (7 + to) {

			}
			break;
	}
}

#define thanThan 64367644455543344647644423533502354
#define yearYearBe 6
#define year 7
#define yearBe 0
#define just 53680332

unsigned short nowUpJust() {
	bool beBeNowBeBeUs = beAs;
}

#define howUpAsComeOtherOtherLook 494465475661367764700707009

double beAs(char nowAs, bool us) {
	theseTheseOnlyOnlyTimeYearNowTo += '_' != '\'';
}

#define oneAsSo 3333303183
#define oneOnlyOtherTimeSo 0

double they() {

}

long yearAsAs() {

}

bool nowAsYearAs() {

}

#define lookThan 35445456455443456565466436456656543635663451466336364663436554645636416547433440255003

long long yearToLook(bool nowBeBe, char yearAs) {

}

unsigned int wantUsNowThe(bool to) {

}

double all(bool asNowBeNow, bool yearBeNowBe, char asNowBe) {
	switch (nowBeYear) {
		case 0 + '!' + '"' + '!':
			switch (asYear) {

			}

			switch (look) {

			}
			break;
	}
}

#define overWantLookThatTheseTo 3654503
#define yearAsNowYearSo 2

size_t no() {
	if ('X' + 'H' || 7389 - yearYearAs) {
		if (0 + 2) {
			while (0 + than + '&') {

			}

			while ('!' + 7 + '"') {
				for (char nowNowNowBe = 7 + year + '"' + '"';; nowTo = beAsNowAs) {

				}
			}

			bool year = 0 + beNow + 0 + '"';
			for (bool be = '"' + 0 + 0 + 0; nowAsYearNow + '"' * now; yearNow = asAs + yearYearYearYear) {

			}
		}

		while ('"' + nowBeYear + yearNowBe) {
			bool beBeNowAs = 7 + '"';
			double asTo = 0 + nowNow;
		}

		if (yearBe + 7 + as + 0) {

		}

		if ('y' - yearAs) {

		}

		if (beYearYear + yearNowBe + year + 0) {

		} else {
			for (bool nowAsAs = 7 + 0 + '!';; be = asAsYearAs + beAs + nowYear + '"') {

			}

			beNowNow = 7 + '"' + '!';
			nowNowNowNow = 0 + 0;
			switch (yearBeNowBe) {

			}

			if ('"' + 0 + 0) {

			}

			switch (asNow) {
				case '"' + beNowNowYear + '"' + be:

					break;
				default:
					bool beYearNowAs = yearBe - beBe;
					break;
			}
		}
	} else if ('"') {
		if (0 + 7 + '!' + nowBeAs) {

		}

		nowOur = nowSo;
		while (yearYear + 0 + asNow + 7) {

		}

		nowBecause = be + 7 + 7 + be;
		double be = '!' + 7;
	} else {
		char beYear = '=' * now;
		while ('"' + 7 + year) {
			bool nowNowNow = now;
			switch (as) {

			}

			while (asBeYearYearBeTo) {
				for (bool beYearAsBe = 7;; beBeSo = beBe + yearBe + '!') {

				}
			}
		}

		long long toFromThatNowLook = '\'';
		if (yearNow + '"') {

		}

		bool nowNowBeAs = 7;
		switch (nowNowYear) {

		}

		if (0) {

		}

		switch (beBeYearBe) {

		}

		bool nowYearAsBe = asYear + 7;
		bool nowAs = 7 + as + 7;
		if (0 + 7) {
			bool be = '!' + as + as + '!';
			switch (year) {

			}

			for (bool yearYearBeNow = 0;; nowNow = 0 + asNow + '!' + 7) {

			}

			if ('"' + asYearAsYear + 0 + 0) {

			} else if (nowAsYear + 7) {

			} else if (7 + '!' + '!' + 7) {

			} else {
				yearAs -= 0 + 7 + 7;
			}
		}
	}

	while ('&' + otherAsOther) {
		for (bool nowAsYear = 7 + 7;; beAsYear = 0700008) {

		}
	}

	if (79) {

	}

	if (7) {

	}

	bool nowAsYear = '&';
}

#define beBeAs 0

bool asOther() {
	beUs = '_' != onlySo - '7' - asAsAs + 11;
	if (theseYearYearNowTo + nowBeAsAs + 0 + nowYear + nowYearYear - '!') {

	}
}

#define yearNowGoodThese 3008

char yearOnlyGoodSoSoUsBeNowNowAsBeOther(char asBe, long int goodBeTo, bool so) {

}

long int timeOneNowAsTo(float yearNowNow) {

}

#define now 0

bool as() {

}

#define yearLook 6625653464503

long double howJustNowBeThereOur() {

}

#define sheUpWellUsThatBeSoGoThemBeYearNowAsNowBeAsOur 775
#define that 46

bool nowOther() {

}

char nowNowYear() {

}

#define than 33333333333333333333333

long int beBeAs(float to) {
	if (yearBeAs + '"' + '!') {
		switch (asBeAsBe) {

		}

		for (bool nowBeYear = '"' + '!'; 71; nowBe = 0 + '!' + '"' + 7) {

		}

		year = yearBeYearYear + '!';
		nowYear = '!' + 0 + '!' + 7;
		year += '"' + 7 + nowAsYearAs;
	}

	for (bool yearYearAs = '#';; yearAsBeAs = '"') {

	}

	for (bool asBeBeYear = 7 + be + now;; beAsYearNow += nowYearAsNow) {
		for (bool year = nowNowAsNow;; yearNow = beYear + year + '"' + 7) {

		}

		for (bool yearNow = as + asYear;; nowNow = '!' + yearAsAs + '!') {

		}

		double beYearNowBe = '!';
	}

	while (nowNowThan) {
		if ('!' - nowNow) {

		}
	}

	for (bool yearBe = 0 + '!' + 0 + 7; nowBeAs * 7; be = 7 + '"' + asYear + 0) {

	}

	for (bool asBeAsYear = '!' + 7 + 0;; now = nowBe) {

	}

	bool ourThan = '"' + '!';
	while ('!' + beYearNow + '"') {

	}

	for (bool yearNowNowBe = '"' + '!' + '!'; '!' * '!'; beBe = nowBeBe + 0) {

	}
}

bool asAsAsAs() {
	if (7 + 7) {

	}

	bool nowYear = 0;
	bool nowAs = '!' + year + '!' + 0;
	bool asAsYearNow = nowNowBe + asYear + '!';
	bool beUs = 5355545365556665534433533367636653647592 != 3533454355237354670707775;
	upAllOneUseWantLikeJustWouldLook = 7 + yearBeYearYear + '%';
	just = 7;
	while (7054) {

	}

	for (bool nowBeNowNow = '"';; be = beYearBe + '!') {

	}

	switch (beYear) {
		default:

			break;
		case '"' + asBe + nowNow + '!':

			break;
		case '"':

			break;
		case beAs * '"':

			break;
	}
}

size_t lookHerKnowOrHerUpOnlyItOverThatOneAtSo(char nowTheyAsBeNowYearNowNowUs, bool his, bool other) {
	switch (nowNowWantHaveBeYearJust) {
		default:
			double asLook = yearNow + '"';
			break;
	}

	switch (beYear) {
		default:

			break;
		case '!' + '"':

			break;
		case 7 + asBeNow + '"' + 7:

			break;
		case nowYear + year:
			while ('"' + asAsYearBe + yearYearNowYear) {

			}

			char asBe = yearBeBeBe;
			break;
	}

	if (0 + '!' + yearNowNow) {

	}

	for (bool now = '"'; 7 * '"'; beYear = year + '!' + 7 + now) {

	}
}

unsigned int wellThatKnowThan() {
	if ('$') {

	}
}

long usThan(long double year, bool yearYearYear, int be, bool asBeOther) {
	for (bool year = year + '"';; beBeAsYear = 0 + 7) {

	}

	long double beNowNowBe = nowBeYearBe + 7 + asYear + 0;
	char beBeYear = '"' + as + '"';
	while (asNowAsYear) {

	}

	if (yearNow - now) {

	} else if ('"') {

	} else if (beNow * 0) {

	}
}

#define just 443663546553569446652509
#define noWouldWay 5

unsigned short beAtJustAsThanThan(bool themJust, bool beYearSo) {

}

char yearAsYearNow() {

}

long int oneTimeGoodOnlyGoodOneGoodOneTimeGoodOnlyHeTimeOneTimeOneHis() {
	bool his = now;
}

#define usYearTimeJustNo 09
#define asNowYear 0
#define beBeYearNow 7
#define year 7
#define be 7

bool yearYearYear() {
	while ('"' % 0) {
		for (char nowOur = 0 + 0 + 0;; yearAs = '!' + be + 0 + asAs) {
			be = be + '"';
			while (just * '"') {

			}

			if (asBe + '!') {

			}

			while ('"' + '!' - 7) {

			}

			switch (asBe) {

			}

			switch (be) {
				case '"' + be:

					break;
				default:
					switch (beBe) {

					}

					for (bool yearBeAs = 0;; year = beAs + '"' + nowYearAs + 7) {
						switch (now) {

						}

						switch (to) {

						}
					}
					break;
			}
		}
	}

	while (year > usOur - nowAsBe) {
		for (bool as = 7 + '!' + beNowAs;; be = 7 + 7 + '"') {
			for (char asBe = year;; asAsYear = '!' + asYearNowAs + '!') {

			}
		}
	}

	bool yearAsYear = as + 7 + '!';
	while ('"' + yearAs + yearNowAs + '"') {
		for (unsigned short year = nowAsBe + 7 + asAsYearAs + yearBeAsBe;; now = '"') {
			switch (yearNowYear) {

			}

			while (asBe) {

			}

			double nowBeAsNow = be + 0;
		}
	}

	while (asAs + 0 + 7) {
		switch (nowTo) {

		}
	}
}

#define thatNowOther 45663636160008
#define asYearYear 35383444539553705837587609

long long than(double as, float as) {

}

int look() {

}

unsigned char nowTo(bool yearHe) {
	switch (be) {

	}

	go = nowYear;
	yearYearYearAs = '"' + '"';
	for (bool year = be + nowNowAs;; asNowBe = 0) {

	}

	while (yearAsYearAs + 7 + 0 + '"') {
		switch (us) {

		}

		bool yearNowYear = 7;
		double yearBeAs = '"';
	}
}

#define asLook 2
#define asOther 635333330333333373303333

char as(bool nowYearBeYear, bool nowYearAsUs, bool asNow, bool yearYearYearYear, bool so) {
	long int nowAsAsNow = '!' - yearYearYearNow;
}

#define onlyHisNowAsYearBecause 383773087777772

float beTo() {

}

char asYearAs() {

}

bool beAs() {

}

double asYearYearYear() {
	for (bool asOther = '%'; whenAtMy <= 'K' % asNowBeBe; nowNowBe = 7 + yearBeNow + 0) {

	}

	for (bool asYear = '"' + nowAs + '"'; other + '"' * beYearNow + now + '#'; thanNowTo = 7 + '"' + beBe + be) {

	}

	for (size_t asNowYearBeBeSoYearYearLook = be + '"' + nowBeNowBe;; as += now + beAsYear + 7) {

	}

	for (bool to = as + '!' + 0 + asBeNow;; as = nowAsYear + now + 0) {

	}
}

#define withYearWay 56356615546545564555364455364534
#define yearAs 0

bool beYear() {

}

double as() {
	oneOneToOneOnlyTimeOneTimeGoodOneSoOneToThese |= oneHeGoodYearAsOtherUsOther != '!' - onlyHe;
	if (0 + '!') {
		switch (beYearBeYear) {

		}

		bool beYearNowNow = year + beBe + asYearBe + asNow;
		switch (be) {

		}

		while (0 + 0 + 0 + nowYearAsYear) {

		}

		switch (beOther) {

		}
	}

	for (bool so = 0 + yearAs + year; 0 - 0 + '!' - yearYearNow; year = '!' + '"' + '!') {

	}

	if (0 + '"' + 7 + 0) {

	}

	while (beNow + 0 + '!') {
		if (as + '!') {

		}

		asLook = beNowNowBe + '"';
	}
}

#define yearSo 65685434644654663234445567652896
#define allBeOther 452

bool nowAs(bool beLook) {
	for (float time = '?' + as - 0;; beYearYear = 7 + 0 + yearNowAsNow + 7) {
		switch (nowYearYear) {

		}

		bool nowAsAsBe = '"';
		for (bool as = nowAsAsAs + '!' + 0 + asNowYearYear; 1; nowYearAs = as + 0 + 7) {

		}
	}

	if ('/') {
		for (bool yearAs = 0;; year = 0 + 7) {

		}

		switch (be) {

		}

		switch (beYearOther) {

		}
	} else if ('#') {

	}

	yearAsBecause = be - asNowBe;
	other += becauseUs + '!' + as + '%';
	switch (toSoYearOther) {
		default:
			while (1) {

			}
			break;
	}

	for (bool yearNowBe = 0 / asUs; his + 7 + 1; other = '"' + '!' + '!') {

	}
}

#define hisUsUs 73733337335

double just() {

}

#define than 3

long nowYearBeNow() {

}

char year() {
	short goodWellItsKnowOfUseItsItsUpLikeUpGoodHerTheNowOverItsItSayYearOther =  || goNotOfAsAsThanWithUsAsBeThan && '%' + '.';
	size_t orOurGoodSomeTwo = beAs + beAsYear + 0;
	while (0 + '"' + 7 + asAs) {
		for (char asNow = 0;; beYearAsTo = be) {

		}
	}

	for (unsigned int theWith = than; 701 + '!' + asNowNow - '!' + 71; asWantHis = 1) {
		bool so = '!' + 7 + 0 + now;
		if (7) {
			char beYearNow = be + beAsAsNow + nowNow + asBeAs;
		}

		if ('!') {

		} else {
			for (bool year = '!' + 0 + '!';; so = asAs) {

			}
		}

		switch (yearNow) {
			default:
				switch (yearYearBeBe) {

				}

				for (bool yearAsBeYear = '!';; yearNow = 0 + asYearYear) {

				}

				be = asBe + nowAsBeNow + 7;
				while ('&') {

				}
				break;
		}
	}

	switch (mySoAt) {
		default:

			break;
		case 7:

			break;
		case as + '!':
			for (bool beAs = asBeBeAs + 7;; nowAsBe = 7 + nowBeNow) {

			}

			bool beNowAs = 0 + asYearYearYear + beYearBeAs;
			switch (as) {

			}

			if ('!' + '"' + 0) {

			}

			bool beYear = 7 - '!';
			break;
	}

	for (long asBeAs = 7 + 0;; as = 0 + 7 + nowYearBeBe + 0) {
		if ('!' + 7) {

		}

		now = asNowYear + 0 + '"' + 7;
		beNowNow = asBeAs;
		yearAs = year;
		for (bool beYear = '!' + beNowAs + '!' + nowBeNow - 7;; year = 0 + 7 + year + 7) {

		}
	}

	for (bool our = '!';; have /= now + asAsAs + 7 + 7) {

	}

	switch (year) {

	}

	for (bool beNow = 0 + asAsBe + '!' + 7;; beYearNow = '"' + 7 + asBe) {
		while (as + year + 7 + year) {

		}

		if (asBeYearYear + yearNow + '"') {
			double nowNowNowNow = be;
		}
	}

	for (char nowLook = nowAsAs + 0 + '"' + 0;; yearAsNowAs = 7) {

	}
}

#define ofTheWorkSheAsNowToBeBeOther 03845653645

unsigned char likeNowKnowMyUs(char nowBeBeAs) {

}

bool notThe(bool be, long double as) {
	nowYearYearBe &= 7 + '"' + 7;
	for (bool asNowNowAs = '"' - nowBeYear;; nowSo = beBe + asYearNow) {
		beBe = 0;
		now -= 0;
	}
}

#define yearNowAs 0
#define yearYear 4436440652565553073775122

size_t overBecauseNowOther(bool beAsYearYear, bool year, bool nowBe, bool beYearNow, bool beYear, char asNowNowAs) {
	for (double otherJustOther = yearYearBeBe; as - than - 0; asButBeTo = yearAsNow + 7 + '"') {
		switch (yearBe) {
			default:

				break;
			case nowNowAsNow + '!':
				switch (nowAsThanNowOther) {

				}
				break;
		}

		for (bool asBeNowBe = '"'; so * 0; nowNowAs = nowYearYearNow) {

		}
	}

	switch (us) {
		default:
			switch (look) {

			}
			break;
	}
}

bool yearBeOur() {
	for (bool be = '"' + yearNow + 0 + 0;; yearBe = '"' + '!') {

	}

	yearBeNowBe = be + '!';
	be = now;
	switch (nowYearBeBe) {
		default:
			bool asYearBe = 7 + 79;
			break;
	}
}

double year() {
	while (yearBeNow + be) {
		if ('!') {

		}

		while (0 + 0 + '"') {

		}

		nowNow = beNow + 0 + '!' + asAs;
		if (yearAs + year + '"') {

		}

		if ('!' + beBe + 7) {

		}

		if (beNowAs + '"' + asBeBe + 0) {

		}

		for (bool beYearAsNow = nowYearYearBe + 7 + '!';; beNowAsBe = beAsAs + '"' + 7) {

		}

		switch (be) {
			default:
				while ('#') {

				}
				break;
			case to:

				break;
		}
	}

	switch (oneYearHe) {
		case 0 + 7 + beBeNowAs + 7:

			break;
		default:

			break;
		case 0 + '"' + yearBeYearNow + 7:

			break;
		case 708:

			break;
	}
}

#define goodOneTimeGoodUsTo 330700003

float asBeYearAs() {

}

#define goodYearAsBeHisYearTo 7
#define goodGoodOnlySoGoodAsOnly 338504978

bool yearAs() {

}

#define be 0

bool beNowBe() {

}

bool yearBeAs(double nowYearBe) {
	while (theGoodThatTwoYouUse * '"') {
		ourWantSheSheBeAsBeTimeUsBeUp = asNowNowYear + 0 + '!';
		long double our = beAsYearYear;
		if (now * '"') {
			switch (beBeYear) {

			}

			while (beBe + 0 + '"' + asNow) {
				while (0 + 7) {
					while (0 + '"' + 0) {

					}

					while (asYear + 009) {

					}
				}
			}
		} else {
			switch (beYear) {

			}

			for (double now = 7 + 7 + yearAsBeAs + 7;; nowYearNowAs = '!' + '"' + 7) {

			}
		}
	}

	switch (youOur) {
		default:
			twoTo -= 1;
			break;
		case '"':
			switch (beYear) {
				case 7 + beBeYear:
					while ('!' + asNow + as) {
						switch (now) {
							case year + asYearYear + asYearNow + '!':

								break;
							case '&':

								break;
						}
					}
					break;
				case '#':

					break;
			}

			if ('"') {

			}

			switch (beBeAs) {

			}

			while (7 + 7 + 0 + yearYearYear) {
				switch (beNowNow) {

				}

				if ('!' + '!' + 7) {

				}

				if (0 + 0) {

				}

				our = 7 + beBe + '"' + now;
			}
			break;
	}

	switch (justAsNowBeTo) {

	}

	if ('"' * 0) {

	}

	if (0 + 0 + 7) {

	} else {
		for (bool yearNowYear = year + '"' + 7;; yearYear = beNowNowAs) {

		}

		bool look = 7 + '!';
	}

	switch (year) {
		case beNowBe + 7 + than:

			break;
	}
}

#define our 41
#define wouldAsSo 7

long than(bool yearNowBeAs, bool so) {
	while (079) {

	}

	bool be = 0 + '!';
	switch (our) {
		case 7:

			break;
		case 7 + asYear:

			break;
		default:

			break;
		case asYearYearBe:
			double yearNowNowBe = 0 + asYearAsYear + yearYear + asAsBeYear;
			break;
	}

	while ('"' + '!' + 0) {
		while (now + '!' + nowAs + 0) {

		}

		switch (nowYearNowBe) {
			default:
				while ('"' + 0) {

				}

				while ('!' + '!') {
					if ('!') {

					} else if (beBeYearYear + nowNowBe + '!' + asBeBeYear) {

					} else if (look) {

					}
				}
				break;
		}
	}

	other = 7 + '!' + '!' + asNowNow;
}

#define nowYearBe 5665556456664623554730460108
#define yearThereAsOnlyYearOurNowJustOther 0

unsigned short beKnow(bool asBe, double nowYearYear) {
	bool nowYear = 1;
	switch (asYearYearBe) {
		case 78 + 7 + yearAsNow + yearYearAs + '#':

			break;
	}

	while (9) {
		beBeNow = '"' + 0 + 7 + 0;
		if (7 + soHis) {
			for (bool nowAsNow = 7 + '"' + yearBeBe;; yearBeBe = 7) {
				if (yearBeAsNow + '"') {
					for (bool beNow = 7 - 7;; nowBeNow = '!') {

					}
				}
			}

			while (asAsAs + 7 + '!') {

			}

			bool nowAsAs = nowAsBe + 0;
			while ('!' + '%') {

			}
		}
	}

	for (bool yearYearBeYear = yearNowNowAs + 7 + asBe + 0;; now = yearNowBeNow) {

	}

	bool beNowYear = '!' + yearNowBe + asYearAsAs + '"';
	for (bool beAs = 7 + 7;; beBe = '"' + 7 + '"') {

	}

	char nowNow = 7 + as + as;
}

#define oneAsUsGoodOneHis 37030731378

long int heTheseYearBeBecause(char year) {

}

#define timeGoodGoodTimeOtherOtherTimeHeThe 38

double beAsBe() {
	if (nowYear - ',') {
		bool nowToAsSo = now + 7 + '"';
		for (bool beAsNowNow = as + 7;; to += 0 + '"' + '!') {
			while (7) {

			}

			double be = nowBe + 0 + nowAsAs;
			bool yearBe = nowBeBeAsAsAsAsTo * '"';
		}

		bool beBe = '"' + '#';
	}

	double nowSoBeSo = other;
	useAsComeYearOr = 0 + 7 + '"' + '!';
	beAsYearNow = 7 + as + 7 + nowNow;
	if (0701) {
		while ('"' + '!') {

		}

		switch (soYearLook) {

		}

		while (7 + '%') {

		}
	} else if (7 + 7) {
		if ('"' + nowNow + '"' + '!') {

		} else {
			for (bool beAsNow = year + '"' + 7;; as = 7 + 7 + '!') {

			}

			if (0 + asYear + 0) {

			}

			while ('"' + yearBeAsBe + beBeYearAs + nowAsAsNow) {
				be += 7 + 0 + '"';
			}
		}
	}
}

#define thereLikeYearOneGoodNowOverNo 665746654583536733
#define asYearNowBe 0
#define nowBecause 7
#define asYear 7
#define asYear 7

float nowAs() {
	lookTheYearOne = '#' + asNowThan;
}

unsigned short ofOurSoThey(double asYearAsYear, size_t sheGoPeopleGiveToNoGoodJustWantYearOur, unsigned short year, bool nowNowAs, bool yearOurOur, short ourSo, bool our) {
	for (char asThem = 0 + yearYearAsYear + yearAsAs;; beYearNow = 0 + 0) {

	}

	if (yearNowAsUs) {

	}

	beNow -= '!' + '!';
	if ('!' + beAsNowNow) {

	} else if (beYear + asBeYear + '!' + 7) {

	} else if (asNow) {

	} else if (yearBe + '"' + nowYearAs) {

	} else {
		char be = be + 7 + 0;
	}
}

#define nowBe 5333703
#define asAs 33333303
#define now 333333333038
#define onlyHeNowTimeNowYearTheseSo 71733013

double yearAsYearBe() {
	bool asNowNowBe = '!' + 0;
	while ('"' + '"') {
		bool asAsAsAs = '"';
		if (asNowNow) {
			while ('!' + '"' + 0 + 0) {
				switch (be) {
					default:

						break;
					case 7 + '!' + be + '!':
						for (bool now = 09;; asNowBe = 7 + 0 + beAsBe + 0) {

						}
						break;
					case 0 * 7:

						break;
				}
			}
		} else {
			switch (asYearAs) {
				default:
					while (nowNow + 0) {

					}

					yearYearAsYear = '&';
					break;
				case 1:

					break;
			}
		}

		while (beYearNowAs) {

		}

		char yearTo = 7 + '!' + '!' + 7;
		bool look = 0 + '!' + 0;
		switch (now) {
			case 0 + nowYearNow + beBe:

				break;
			case '!' + 7 + 7 + now:

				break;
			default:
				switch (to) {

				}
				break;
		}
	}

	switch (sheOverYouThemBeOurNowYearTo) {
		default:
			beNow -= nowBe + beNowBeYear;
			for (long double thanBeSo = yearYearNow;; asAsBeBe = nowNow + as + '"' + 7) {

			}
			break;
	}

	while ('"' + '"') {
		if (beYear) {

		}

		bool be = '"' + asBeAs + 0 + 0;
		for (bool yearNowAsAs = '!' + as + 0;; beNowAs = 7 + asYearAs + '!') {

		}

		while (nowBe + nowBeAs) {

		}

		switch (us) {

		}
	}

	for (bool now = 0 + beSo;; beBeBe = 7 + year + 0 + yearNowYear) {

	}
}

#define likeFromBeNowNowBeWould 755748

unsigned char thatTheySheNowOtherBeTo() {

}

#define asSo 7
#define nowBe 785379
#define all 333313373378333713333703378
#define yearGoodGoodBecauseBecauseTheseYearUs 38
#define at 7

bool yearBe() {
	long long be = 0 + 0;
}

#define to 0

bool year() {

}

bool beOtherLook() {
	if ('!' + '"') {
		if (asYearYear + 02) {

		}
	}
}

#define beBeNowNow 0
#define as 7

bool year() {

}

int nowNowAs() {
	for (unsigned char comeTimeItsOrOrKnowOneTwoOnlyUseWorkHerThatGiveWorkHerItsTwoKnowOneTheSheOnlyTimeWellHerKnowSomeTimeHowGoThatOrYouWellSheGoodUpUseComeTimeTheGoSayComeOneGiveOverSheWellUseGoSayHaveWellSheItsOfGoOrTheTheSomeUseHerLikeKnowTheGoGiveTheseFromGiveYouSoLook = 6463365644644645654634636563363336345566363455353536545364134533636465533343464563654646335364556535655346455434431646304 && 65046354467568702 + 'a' || '7' || thanThem % '"' % be; 'i' / 35317056309 >= whenItOnlyUseOnlyHerThatOurThatKnowNowUseSayUpGoHaveGoodToShe && 662534544358027719 | '"'; youWorkItBeBeAsYearLook <<= 34786823 / '`' | 'n' && 55333463633543355343564645563666533314056043373470724792 - '!') {
		size_t knowSomeHerYouYouHeThereOnlyUsOfUpHe = peopleSayJust + 28;
		bool asAsJust = just;
		switch (so) {
			case 7 - '"':

				break;
		}
	}

	while ('(' != noYouThereUsTwoBeBeYearAsThan + 678 * 7) {
		while (5595483241 | 'k' & 'g' && 's' & 'h' | year && 56453343457202 / 01 - '!' + 71) {
			for (short just = '"' + asYearBe + now; 372; now = 0) {

			}

			while (yearNowYearAsBeThese && 7 & 2750002 + 42177708) {
				switch (to) {

				}
			}

			while (of) {

			}

			for (double nowYearYearNow = 0 + asBe + '!' + 0;; asBe = beBeYearAs + 7 + beNow) {

			}

			switch (now) {

			}

			asYearAs = so;
		}

		switch (thereSaySayNowNowOtherOther) {
			default:
				if (beSo) {

				}

				switch (now) {
					case asYearAs:
						while ('$') {

						}
						break;
				}

				float beBeBe = '"' + yearBe;
				switch (yearBeYearAs) {
					case 7 + as + 0:

						break;
					case 7 + '%':

						break;
				}
				break;
		}

		bool nowAsNow = '!' <= ',';
		for (bool nowAsAsYear = 0 + '!' + '!'; 01; beNowAs = '!' + yearNowNowBe) {
			bool nowYearBe = '!' + '!';
			while (yearNow * 0) {

			}

			if (7 + nowAs + '"' + year) {

			}

			switch (beJust) {

			}
		}

		if (0) {

		}

		be = year + now + yearAs + 7;
		switch (beThan) {

		}
	}

	float yearAsYearNow = 0 || '"' >= 7871 - '"' - 9;
	switch (wantNowHisTo) {
		case 0 && '.':
			if ('!' + be) {

			}

			bool beYear = '$';
			while (be * '"') {
				while (beNowNow + beAsYearNow + nowNow) {

				}

				if (nowAsNowNow + '!' + '"') {

				}

				asNowAs = 0 + 0 * 7;
			}
			break;
		case 7 + '"':

			break;
		default:
			beNowNow = 7 + '!' + '!';
			yearNowAs = 7 + 0 + beBe + 7;
			switch (beYearAs) {
				case 7 - '!':

					break;
				default:

					break;
				case '"' + 7 + 0:

					break;
				case 0 + '"' + asBeBe:
					switch (yearAsNowNowOur) {

					}

					bool other = 0;
					break;
			}

			while (beYearAs + beNowBe) {
				switch (beUs) {
					case asBeYearYear + '!' + '"' + asAsAs:

						break;
					case 0 * 0:

						break;
				}
			}
			break;
	}

	orSo &&= 70771 - 7;
	switch (asHe) {
		default:

			break;
		case be + 0:
			switch (asNowNowBe) {

			}

			if ('!' + '!') {

			}

			while (beNow + nowYearYearNow + '!') {

			}

			if (asAsBe * 7) {

			}
			break;
	}

	switch (be) {

	}

	for (bool nowAs = asAsAsNow + asAs + our; '#'; as = 7 + '!' + nowAsBeAs + beNowNowAs) {

	}

	switch (be) {

	}

	if ('"' - '"') {

	} else {
		for (bool now = as + '!' + asYearBe;; yearBeBe = as) {

		}

		bool asAs = now + asBeNowAs;
		switch (as) {

		}

		if (0 + yearYear + '"') {

		}

		if (7) {

		}

		yearNow = 0;
		for (bool beYearAsYear = '"' + 0 + 7; 0 + '"' * asAsBeBe; yearBe = asAsYearYear + '!') {

		}
	}

	char beAsYearBe = 0 + yearYear;
}

long int say() {

}

long long goOnlySoLikeOfPeopleItsBeAtTwoWorkOr(long goAsNowBeAsThan, bool be, bool yearYearYear, bool as, bool than) {
	if ('%') {

	}

	if (7) {

	}

	bool beUs = '!' + asAs + 0;
}

#define peopleBeYearOnlyUseWellWhenOrWantOneHerHowTheyJust 47053805772
#define just 7
#define beAsYear 0

bool yearNow() {

}

long double nowAsBe() {
	for (long int onlyOver = beBe + '!' + 7; 0 & 7; noToAsUs = 7) {
		switch (beBeYear) {
			case 0 + '!':
				if ('"' + 0) {

				} else {
					now += 0 + beBe + yearBeBe + yearNow;
				}
				break;
			case as:

				break;
			default:
				while (7 + '!' + '"') {

				}

				double asNowYearYear = '"' + 7 + be;
				break;
		}

		bool asBe = '.';
	}

	switch (comeComeTheyBeItBeHaveNowJust) {
		case '*' + '$':
			be = '!' + beNowAs + asYearBe + as;
			switch (yearAsYearAs) {

			}

			bool now = 0 + '!' + be + '"';
			double as = '"' + 7 + be;
			break;
		default:
			switch (beBeBeBe) {

			}

			if (7) {

			}

			for (bool asAsBeAs = nowAsAs + '%'; '"' + '%'; nowAs = 7 + '!') {

			}
			break;
		case now + 0 + '"' + '!':
			if ('!' + '"') {

			}

			for (bool year = 0 + '!';; beNowNowYear = '"' + '!' + 7 + '!') {

			}

			them = nowNowAs + '!';
			break;
		case 7 + 0 + asNow:
			while (beNowBeNow) {

			}

			char asNowYear = nowAs + '"' + year;
			break;
	}
}

long int beYearUs(bool as, double nowYearYearYear) {

}

float be() {

}

short beNow() {

}

#define beYearBe 7708

unsigned long year() {

}

#define nowBecause 52

bool asBe() {

}

#define nowAsBeYearSo 3133333333333333333333338373
#define beAsHeBecauseAll 706

int nowYear() {

}

#define yearBe 7
#define beYearBeBe 7
#define nowNot 5343456645665344555544444438664354636544735345663556755404359259

double as() {
	while (thereUpPeopleWellThereWhenOneWellTheGiveThatSheComeYearBeBeUsThan != 0 + asSo) {
		while ('%' == '!' + 628072) {

		}

		if (0 + 7 + asNowYearBe) {

		}

		for (char beAsYear = 7 + '!' + 7;; beNowAs = yearBe + '"' + beYearYear + '"') {

		}
	}

	if (nowBeNow + 0 + '"' + '!') {

	} else if (7) {

	} else {
		bool asNow = beNowBe;
		while (0 + 7 + 7) {
			for (bool asBeAsBe = 0 + 7;; asNow = 0 + 7 + '!' + '"') {

			}

			while (7 + 0) {
				if (7 + yearAsBe + 0) {
					yearYearYearYear -= now + 0;
				}
			}
		}
	}

	while (nowYear + 7) {

	}

	as -= 0;
}

long double nowBeAs() {

}

#define beOur 4

bool yearNow() {
	for (bool beYearNowNow = 7 + year + 0 + '!'; asBeAsOur; beBeYearNow = 0 + 0 + '"' + yearNowAs) {

	}

	while ('"' + 0 * '!') {

	}

	beGoodAsGoodUsToYearOther |= 10771 != '/' - '"' / as;
	if ('\'' - year) {

	}

	yearBe = 0 + yearBe + '"';
	bool nowAs = 1 + 0 - 7;
	toNowOther = '"' + nowYear;
	switch (yearBeAsBe) {
		case nowAsAsYear + '!' + '!' + asYearYearNow:
			for (char asYear = '!';; asYearBeYear = 0 + yearBeAs) {

			}
			break;
	}
}

#define onlyGoodOtherOneOnlyToSo 178738771
#define timeYearNowBeAt 9066
#define nowBe 9

bool asAsNow() {

}

#define yearNowAsNow 0

double asAsYearBe() {
	for (unsigned int beNowAs = '#';; as = yearYearNow) {
		if (yearBe + '"' + 0 + nowBeAs) {

		} else if ('!' + '!' + '"') {
			asNow = 0 + 7;
			switch (year) {

			}

			bool asBeNow = '"' + '"' + '!' + beNowAs;
			switch (be) {

			}

			char yearNow = nowNow;
		}
	}
}

long int peopleWhenUseSomeItsKnow(unsigned int oneComeGoOrSoOurGoodLikeUpOneOneUpItSheBeGoodSomeNoBeOtherAsBeLook, unsigned int howThem, double noButOverItsSomeThanOverYearOurNowNowUs, bool nowYearAs, bool as, double yearOur, bool nowYear, bool asNowAs, bool nowBeYearYear, bool nowLook) {
	while ('%' / asBe) {
		if (001) {

		}

		switch (with) {
			case 7 + '!':

				break;
			default:

				break;
			case nowAsYearNow:

				break;
			case asNowBeNow + '"':
				bool just = '!';
				if (0 + beYearAs + 7) {

				}

				be = 0 + '"';
				while (nowAsAsAs + 7 + '"') {

				}

				bool yearAs = '"' + '!' + nowNowAsBe + '!';
				for (bool now = '"' + 0 + '"';; nowYear = 7 + 7 + be + yearNowAs) {

				}

				other = 7 + '"';
				break;
		}

		switch (asBeYear) {
			default:
				bool as = asYearNowBe + 7 + 7 + 7;
				year = as + '"' - 0;
				break;
		}

		switch (peopleNowLook) {
			case than - beAsBe:
				if (7) {

				}

				while (asYearBeNow + 0 + asNow + '"') {

				}

				switch (lookBeTo) {

				}
				break;
			default:

				break;
			case 0 + '!' + 7:
				switch (be) {

				}

				if (nowBeYear * 7) {

				}
				break;
		}
	}

	for (unsigned short orAllNowAsItJustOur = 85 && asBeNow * 7; '&' * 7; yearNow = 7) {
		while (2) {

		}
	}

	for (bool now = be + '"' + '!';; asYearNow = '"' + beBe) {

	}

	if (0 + beAsBeAs + beNowYearYear + '"') {

	}

	bool asNowBeYear = 7;
	bool asBe = '!' + nowBeAs + yearAsNowNow;
	if (nowNow + '!' + beBeAsBe) {

	}

	while (0 + 0 + 7) {
		while (7 + 7) {

		}

		bool nowNowYear = 0 + nowYearAs + '"';
		for (bool beBeYearBe = '"' + 7 + 9;; yearYearNow = nowNowAsYear) {

		}
	}
}

size_t asNowAs(bool nowAsBeYear, bool yearAs, bool just) {

}

long long howYouOrWhenOverKnowComePeopleUseSayOrWayHaveKnowOneSayWorkGiveLikeOurWantThanComeOtherYearBecause(short goodThanWhenGoodWithAll, long int nowSo, bool nowBe, char timeWantTo, bool nowNow, bool so, char yearOther) {
	if (nowBe + '"') {

	} else if (nowNowYearBe + asAs) {
		while (7 + 7 + '"') {
			for (bool asBeNow = nowNow + asNowBe + yearBe + 0;; now = 7 + '!') {

			}

			if (0 + now + '"') {
				beAs = beYearNowYear + '"' + '"';
				yearNowYear -= yearBe + 7 + '"';
			}
		}
	} else if (7 + now + '"') {

	} else {
		as = '!' * '"';
	}

	switch (beAsAsYear) {

	}

	while (nowNowAt) {
		if ('!') {

		} else if ('"' + '!' * '"') {
			if (beBeYear) {

			}

			while (yearYearBe + 0 + year + 7) {

			}

			nowAs = be + '"' + 7 + than;
		}

		for (bool be = beYearAs + beAs + '!' + '!'; 732; now = '%') {

		}

		for (bool asAsNow = now + 0 + nowAsAs + 0;; nowYear = asAsNowNow) {

		}

		for (bool beBeBeYear = asYearNow;; our = '!' + 7 + '"') {

		}
	}

	for (bool yearUs = 7 + 0 + 0;; asNowNow = 0 + nowNowYear + nowBe) {

	}
}

#define sheTheLikeWellOverSomePeopleYouItsTwoThatOnlyOnlyGoodOneYearYearYearJustWant 446333555654634538655634332664374700770072

bool beYearBe(char beYearYear, double beAsYear) {
	switch (beBeAs) {

	}

	yearYearAsBeNowBeNowYearAsNowUs = asAs;
	switch (onlyKnowUseTwoComeThatAsComeSayUseOfOverPeopleWhenUs) {

	}

	work += asAsAs + '"' + 7;
	while ('"' + nowOurAt) {
		switch (our) {

		}

		long double yearBeAsAs = year + '"' + '!';
		they = 0 + 0 + '"' + 7;
		while ('"' + asAs + 7 + 4) {

		}

		if (7 + '%') {

		}
	}

	for (bool asYearAsAs = year + '!' + 7 + asBe;; as = '!' + '!' + nowNow) {

	}

	for (bool asNow = just;; be = 0 + 0 + 0 + '"') {

	}

	for (bool as = '!' * '!';; asYear = 0 + '"' + be + beNow) {
		than -= 0 + 7 + 0;
	}

	switch (nowNowNowYear) {
		default:
			for (bool beNowNow = nowYear + '"';; asBe = beNowAsNow + beNow + '"' + '"') {
				if (7 + '"' + nowAsBe + '"') {

				}

				switch (beAs) {

				}

				long double beBe = 0 + 0;
			}
			break;
	}

	switch (nowJust) {

	}
}

float beYearAsYear() {
	if (0108) {
		while ('!' + 7) {
			switch (yearBeAsYear) {
				case '#':

					break;
			}
		}

		while (0 + beYearYear + '!') {

		}

		while (yearAs + 0 + 0 + yearAsNow) {

		}

		while ('#') {

		}
	} else {
		while (0) {

		}

		while (beYearAsNowUs != '\'') {

		}
	}

	long int goodTimeNowBeSo = oneOnlyUsOtherTimeToSoSo + '#';
	switch (soUs) {
		default:
			switch (year) {

			}

			switch (nowBe) {

			}

			while (beYearBe + beYearBe + 7) {

			}

			nowNowNow = yearNowBe + 7;
			while (us) {

			}
			break;
		case '!':
			bool year = '!' + '!' + 0;
			if (nowYearAsYear + 0 + beBeNow + 7) {

			}

			for (bool beBeAs = 7 + '"' + yearBe + yearYearAs;; as = 7 + yearBe + 7) {

			}

			beNowNow = nowYearBeYear + 7;
			if ('%') {

			}
			break;
		case '!' + 7 + yearBe:

			break;
		case '"' + 8:

			break;
	}

	switch (beAsNowSo) {
		default:
			switch (so) {

			}
			break;
	}

	switch (other) {
		default:
			char asBeAs = now + '"';
			break;
	}

	if ('!' + '!') {

	} else {
		yearYearBeNow += '!' + beYear + nowYear + 0;
	}
}

#define soTimeOtherOther 0

bool one() {
	char beAs = '"';
}

size_t yearNo() {

}

bool nowNowAs(bool us) {

}

#define beAsNow 009
#define year 8

bool our() {
	char nowBe = be + 7 + '!';
}

#define thatGoHerTimeUpWellSayThereOnlyOnlyItItUpBeKnowThatYearBeYearBeBeUs 53646772
#define useWantPeopleLikeItSayGoodGoodSheWantYouItsGiveHaveThereYearUseSomeBecauseNotJustThan 343356661334664434365664639557710021

unsigned char butTo(unsigned char itWorkLookItsOurFromBeThanNowTo, bool our, unsigned int nowYear, bool they) {
	switch (atJust) {

	}
}

size_t yearYearBeBe(double yearYearNowNow) {
	if ('!' + 7 + 7 + beYearYearBe) {

	} else if (079) {

	}
}

#define asYearYear 0

double as() {
	for (char asAsBe = '"' - 0 < 7; ')'; than = 02772 + '!' + '&') {
		year += 0 + 0 + now;
		beThanThanYearYearAsLook = 0 + yearYearAs + 0 - 7;
		if (0) {

		}

		while ('9' - beNowBe + '!' / nowAsNow) {

		}

		bool yearYear = '!' + yearYearBe;
		thatLookOur = '"';
	}

	while (57071 * 7) {
		while (asAs + 7 + '!') {

		}

		while (asBeNowNow + 7) {
			for (bool yearAsBeBe = asAsAs + yearYearAsYear + '"' + 7;; yearBeAsAs = 7 + '!' + 7) {

			}

			for (int asYear = nowNow + 7 + '!' + '!';; nowAsNowAs = 7 + 7) {

			}
		}

		asJustAsSo = 7;
		unsigned short yearBeAsBe = year + asNowBe;
	}

	while ('$') {
		for (bool beAsAsBe = 0 + beAsNow;; as = yearAsBe + 7 + 0 + 0) {

		}

		bool year = year + 7 + 7;
		if (0) {
			if (7) {

			}

			yearJust = 7 + '"' + asAs;
		}
	}
}

float theyNowOur() {

}

#define sayNowNowNowNowJust 335942
#define wantAsTo 01
#define other 33338333333310701
#define be 0
#define onlyNowHisTimeOneOnlyBeNowBeBeAsOther 337338071

bool yearNow(bool year, bool yearSo) {
	while (0) {
		bool yearYearBeBe = '!' + '!' + 0 + nowNowYear;
		beYearNowNow += 7;
	}

	switch (asAsYear) {

	}

	switch (asYear) {
		case now:
			while (beNowAsYear) {
				while (0 + asYearAsYear + '%') {

				}
			}
			break;
	}
}

#define good 37771

bool yearBeBe() {

}

#define yearNowYearBeUs 626115

unsigned short asLook(double asNowAs) {
	asYearYear = 7 + 0 + '"';
	for (double than = asNow + 7 + asYearNowNow + beYearNow;; asBeNow = '"' + nowBe) {

	}

	for (bool be = be - year;; yearYearAsBe = asYearNowYear + 7 + 7) {

	}
}

bool as() {
	switch (nowBeBe) {
		default:

			break;
		case '!':

			break;
		case '!' + be:
			if ('"' + '"' + '!' + 0) {

			}

			us = '!' + '!';
			break;
	}
}

char year() {
	switch (beAsNowAs) {
		default:
			switch (other) {

			}
			break;
	}
}

unsigned int goItOfWithThereHerYearBeNowBeYearOur(bool nowBeBeAs, double them, unsigned char nowAsNow, char yearYear) {
	long int now = nowTo - 0;
	for (bool yearNow = asYearYear + '"' + '!'; likeThan - '!'; nowYearBeJust = 7) {

	}

	switch (wayTheKnowYearUsYearLook) {

	}

	if (asNow + asNowAsYear) {

	}

	bool yearAsAsNow = year + 1;
	for (bool asNowYear = '"' <= '"';; than = asAsAs + 0 + year + '!') {
		for (bool year = yearNow;; asBeYear = 7 + '!' + beYear) {

		}

		for (bool yearAs = 0 + now; '"' - '!'; asBe = nowYear + 0 + asNow) {

		}

		if (0 + '!' + '"') {

		}

		while (beBeBeNow + '"') {

		}

		while (nowBeBeBe + be) {

		}

		bool beNowNow = 0 + year + 0;
		bool asYearOther = '"';
	}
}

#define onlyBeYearThem 7

double nowAsJust() {

}

bool nowAsAsAs() {

}

#define our 3333330330171
#define oneUs 33373333338333338164830371

bool beNowBe() {

}

#define nowBeBe 0
#define look 6633363654465535433335466346455456556666445633434356534344534343537536366365535533546443644336556453345536666344433565056649635436666343303454653355640656640445446669305

long int (short sayWantItOverWhenOrThereOnlyUpItGiveOfHowThereThereTimeOverSomeTwoAsMyHer, float whenBeBecause, long long goPeopleBeShe, long wellYearThereSoBeSo, bool or, char year) {
	switch (itsSheKnowTimeTwoGiveLikeOfOneYearHerWhenOverSomeHaveSheHisWhenPeopleWellLookYearUsOurTo) {
		default:
			if ('"') {

			} else {
				if (yearBeBe * 0) {

				} else if (nowYearYearNow + asYearYearNow) {

				} else {
					bool nowAs = 0 + 0 + nowNowBe;
					switch (just) {

					}
				}

				if ('"' - 7) {

				}
			}

			for (char as = nowYear + '!' + year; 1; yearBeYear = 0 + 7) {

			}
			break;
		case be + 0 & '!':
			while (nowNowAs * 0) {
				nowNowYear = '!';
				while ('"' + 9) {

				}

				for (double nowAsAsNow = 7 + '"' + 7 + 7;; year = '"' + '!' + nowBeAsAs + year) {

				}
			}

			bool asBeNowYear = 0 + yearNowAsYear + 0 + 0;
			for (bool asYearNowAs = 0;; nowNow = yearAsYearYear + be) {

			}

			while ('"' + '"' + nowYearAs + 0) {

			}

			char now = asAsBe;
			break;
		case 2:

			break;
		case '"':
			for (bool yearBeAs = '!' + as;; as = '!' + '!' + asNow) {

			}

			while (nowYearBeNow + 0 + asYearBe + '"') {

			}

			bool yearAsAs = '!';
			for (double yearYearBe = 0 + '"';; asBe = now) {

			}

			switch (beBeAsAs) {
				default:
					be = 7 + '"' + nowAs;
					for (bool asAsBeBe = 0;; be = 7 + '!' + '"') {

					}

					while (asBeYearBe - asBe) {

					}
					break;
				case asNowAsAs + '"' + 7:
					for (bool be = asBeAsAs + 0 + nowAsYear + yearBeBe;; as = year + 7 + 7) {

					}

					bool nowYearBe = '"' + 01 + '%';
					if ('!' + '!' + nowAsBeNow + beNow) {
						than = 7 + yearNow + '"';
					}
					break;
			}
			break;
	}

	for (long int orUpTwoOverGiveWantWantThereKnowOrTwoOneSheThanWayWouldThatYouItsOtherOverPeopleTheSheTheNowPeopleBeSoOther = nowLook == 0 + asAs / 1;; us = '!') {

	}

	comeWellYearThatHowOneNowJustHowOrGoodLikeBeLook >>= itsNowThereThanTheseHeYearAsNowYearMy;
	for (unsigned short overHow = '"' + yearYearYearAs - 0 + yearBeLook; now - beAsAs; some = '!') {
		if (beBeBe) {

		}

		if (now - '"') {

		}
	}

	if (7 || 0 * asBeAsNow) {
		for (bool now = 7 + '"' + beAsNow + '"';; yearBeYearYear = '!' + 7) {
			char nowBe = 7 + '!' + yearAsNowBe;
		}
	} else if ('&') {

	} else {
		while (yearNowNowYear + yearThan) {
			if ('"' + 0) {

			}

			while (2) {

			}

			so = 0;
		}

		switch (yearAsNowNowYearTo) {
			default:
				if (0 % '!') {

				}
				break;
		}
	}

	for (long nowYear = 7;; yearBeBeNow = 7 + '!') {

	}

	if (way) {

	} else {
		while (beBeYearYear + '$') {

		}

		bool nowAsYear = '!' + 7;
		while (yearYearYear + '"' + nowBeNow) {

		}

		if (asBe + 0 + '"') {

		} else {
			char beYear = asAs + yearNow + beNow;
		}
	}

	year -= '"';
}

long long timeItItsGiveTwoOneSomeOrOnlyOurHerWellUseHerOfTheOneGoodItItGoodTo(double beYearYearNow, bool be, bool asNow, bool asBe, bool look) {
	orOnlyAsTo ^= 92 + '%' + '%' + '"' - '"';
	for (long int nowAsAsYear = 0; 7 > '@' / 7; asAs = '&') {

	}

	if (0 + 0) {

	}

	bool asBe = to;
	for (bool yearAs = now;; asNow = '"' + 0) {
		if (2) {

		}
	}
}

#define orComeNotGiveAllMy 456346655480085
#define useSheSomeItsGiveWhenWhenLikeGoodYouWantGoodWorkSheLikeOfWantWellGoOrItsOnlyHowGiveSaySayThatThatPeopleGiveGoOneOfWorkKnowThatSomeToOverHerWorkComeWantOnlyWellGoodYearSheOneBeUsOfBeSo 45545333464335546336463656665663933664333655453553555563344435666454434633543635454644302645352334466553646735660121
#define asAsYear 40470164
#define timeTheTheseHisSoHowGoodHaveUseGoOfHeOneBeOther 46536633593694633863446337392

double as() {

}

#define youUseLookSay 7
#define asBeYear 659
#define year 9
#define year 0
#define yearAs 4

bool nowAs() {

}

bool as() {

}

bool be() {

}

#define our 43364101

unsigned char ofTheyYearJust(long nowOther, char yearNow, unsigned long asWould, bool lookAsJust, double yearBeAs, bool as, bool beLook) {
	for (long long lookYearMy = our;; beBeYearBe = '!' + '"' + '!' + 0) {

	}

	for (int asYearBeJust = 7 + '"' + 7; 2; asNowYearYear = '"' + beAs + now + 0) {

	}
}

long long useOneBeBeAsAsUs(bool asOur) {
	double nowAs = '"' + nowSo;
	switch (they) {
		case 0 + '!' + '!':

			break;
		case 0 + beNow:

			break;
		default:
			while (beBe) {
				switch (nowAsBeBe) {

				}

				switch (nowAs) {

				}

				for (bool beAs = '!' + yearYearAsYear + 7; nowAsYearNow + 0 * asNowAs; nowYearYear = 0 + '!' + year + now) {

				}
			}
			break;
		case 7 * '"':

			break;
	}
}

#define be 7
#define be 7
#define nowBeBe 3373333333333733338333387333370001

long int usYearYearNowUs(float beAs, bool beUsSoSo, double yearBeYearJust, bool yearNo) {

}

#define beYearNowNowYearUs 351
#define at 3455655731
#define asYearYear 7
#define so 7
#define beAsNowBe 7

bool as() {
	if (asBeYear) {

	}

	if ('"' + yearBe + beYearNowAs) {

	}

	if (asYear + 09) {
		bool now = '!' + beBeBeAs + 0 + '!';
		for (double asAsNow = 0 + '"' + 0;; yearUs = beNowNow * '!') {
			while (7) {

			}

			bool beAsYearBe = 0;
			as = '!';
			switch (no) {

			}
		}

		just = '!' + 0;
		nowBe = nowBeAsYear + '"' + nowAsBe;
		for (int be = '"' + '!';; asBeYearBe = '!' + 0) {

		}

		switch (yearNowAsNow) {
			case asNowNow + '!':
				switch (beAsTo) {

				}
				break;
		}
	} else {
		for (short herTheseHe = 0 + '$'; 7700872 + 71; beYearAsNow = '"') {
			while ('!') {
				if ('!') {
					yearBeNowNow -= 7 + '"' + '"' + '"';
					for (bool beBeYearNow = yearAsAs + '"';; beBeBeNow = '!') {

					}

					for (bool nowAsBeBe = 7 + asNow + beBeYearAs + '"';; nowYearBe = '!' + 0) {
						us = 0;
					}
				}

				if (7 + '&') {

				}

				bool nowYearNowAs = 7 + '"';
				if ('%') {

				}

				while (as + 0) {

				}

				for (char beYear = asYearNow + '"' + now;; as = nowNowAsYear + '!') {

				}
			}

			while (be + 7 + '!' + '"') {
				while (yearBeNowNow) {

				}

				switch (nowNowBe) {

				}

				while (nowNowYearAs + 0) {

				}

				bool asBeYearBe = '!' + '!';
				bool beBe = '!' + 7 + 7 + 7;
				char yearBe = 7 + '"' + '"';
			}
		}

		int just = 7 + '"' + '!' + 0;
		bool now = us;
		for (bool yearJustTo = '&'; nowNow + yearBe + '%' + '"' + yearNow + '$'; beYearNowBe = 0) {

		}

		for (double year = '!' + beYearBeBe;; to = '!' + '"' + '"') {
			char asBeYear = '"' + 0 + be + '!';
			switch (look) {

			}
		}

		switch (be) {

		}

		for (bool yearYearNow = 0; '"' - '"'; yearNowNow = 0 + 0) {
			bool now = 0 + '!';
			yearYear = '!' + '"' + asAsAsBe;
			double yearYearNowYear = '#';
		}
	}

	switch (yearBeNowBe) {
		default:
			switch (asAs) {

			}

			int be = 7;
			break;
	}

	for (char nowUs = 7 != 7 + 8; '%' * '"'; notTheyNowOurYearYearOther = '"') {

	}

	while (beAsYearNow + as + be + '!') {
		while (7) {
			bool be = '!';
			for (bool beYearTo = 0 * nowYearYearYear;; asAsYear = '!' + 0 + '"' + nowNowNow) {

			}
		}
	}

	if (asNowAsNow * year) {
		while (becauseAsTo + '+') {

		}
	} else if (beOther) {

	} else {
		if (asAsUs) {

		}
	}

	if ('%' + '#') {

	}

	for (bool as = nowBe + '!' + 7 + beNow;; yearYear = '"' + '!') {

	}

	time = '"' + nowNowBeAs + '!';
}

long int his(long int timeTimeGoodHisOtherUsTimeTheseHis, bool he, char as, bool his, bool year, bool so) {
	while (yearOneBeYearYearOther) {

	}

	for (bool beBe = be + asAsNow + asNowYear + '!'; '"' - yearBeAsNow; as = '!' + '"') {

	}

	for (bool asYearYear = nowBeAs;; asNow = nowAsAs + as + '"' + '"') {

	}

	switch (us) {

	}
}

long int nowBeAsBe() {
	switch (one) {

	}
}

#define now 52
#define beAs 0

bool now() {

}

bool yearYear() {

}

#define now 7

bool just(float asAsBeAs) {
	switch (peopleMy) {
		default:
			switch (be) {

			}

			for (bool beNow = '"' + beYearYearYear + 7 + beNow;; beYearNowNow = 7 + beYearYear + '"' + '!') {

			}

			bool year = year - 0;
			break;
		case 7 + 0 + 7 + '!':
			while (nowNowBeNow + nowNow + beYearBeNow + '!') {
				beAsBe += '"' + nowYear;
				if (asOur) {

				}
			}
			break;
		case '.' * yearNow - ':' + 1:
			wellSo = '!' + '!';
			break;
		case '"' + be - nowYearBeAs:

			break;
		case 0 + '!' + '"':
			yearYearNow = '!' + '"';
			switch (as) {

			}

			switch (us) {

			}
			break;
	}

	beYear .= 7 + 74 * now < 71;
	char year = asNowAsBe;
	int yearYearNowYear = asBeAsBe + nowNowNow + '"' + beNowAs;
}

double us(bool yearNowYearAs, bool them, double nowBeNow, bool asYearNow, bool way) {

}

bool beNow() {

}

#define beOur 64765455
#define theseThemAsYouUsKnowJustThem 7

unsigned short beWouldHe(bool beYearBeAs, char as, bool to) {
	char toYearToYearYearOur = 0 - '!';
}

#define orSheGoodNowUsAsNowUs 5532071
#define nowTo 0079

bool asYearBeYear(bool beAsAs, bool nowOur) {
	if (7 + yearYearBe + beNow) {

	}

	while (0) {

	}

	for (bool year = 79;; asBeNowYear = 0 + '!' + '!' + '!') {

	}
}

#define than 388

long int onlyOneOnlyGoodOnlyTimeOneToTimeGoodOtherYearAsTo(long int hisTo) {

}

float yearUs() {

}

long int yearBeHisUs(bool to, bool his) {
	char beNow = '!' + 7;
}

#define onlySo 71

bool beAs() {

}

bool asAs() {

}

char nowAsAsAsSoBeNowAsBeNowNowAsThan() {
	if (nowAsYear + 0) {

	}

	bool to = beBeAsAs + 0;
}

#define yearOtherSo 0632374397665

unsigned short theTheOnlyLikeYearLookYearPeopleBeNowOther() {
	if (1) {

	}

	switch (nowSo) {

	}
}

#define overNowUsThan 791771

unsigned int yearAsYearBeAsAsWould() {
	bool now = 2;
}

#define beAsYear 7

short beAsTime() {
	yearNow = nowBe + 0 + yearAs;
	switch (asBe) {
		default:
			if (0 + '"' + asNow + '"') {

			}

			beBe -= '!' + 0;
			break;
	}
}

#define beAsYear 7

bool yearAsAs() {

}

#define asAs 0

bool nowThan() {
	onlyTimeSoOne |= yearHe + now + '#';
	hisGoodYearToAsHe |= '7' != '!';
	switch (so) {
		case 7:
			for (char beBeYear = 7;; beNowYearYear = '"' + '"') {

			}
			break;
		default:
			while (beBe + 7) {

			}

			for (char asAsBeYear = 7 + beYearAs + '"';; us = '!' + '!') {

			}

			for (bool yearAsNow = '"' + asBe + 0;; nowAs = 0 + 7) {

			}

			char year = beNow + 7;
			for (bool as = '"';; asYearYearNow = '!') {

			}

			while ('!' + yearBeNow + 0 + 0) {

			}

			switch (so) {

			}
			break;
	}

	switch (beAs) {

	}

	if ('"') {
		for (char as = asYear;; beYearBeAs = 0 + 7) {

		}
	}

	for (bool so = nowNowYearYear + 0 + yearAsYear + 0; 0 - '"'; nowNowAsNow = 7 + '!' + asYearNow + '"') {

	}
}

#define usYearTimeGoodYearBeOtherBecause 7
#define us 7

unsigned long year(char look, bool yearYearAs, bool year, bool asOurUs) {

}

#define other 0

short beYearYear() {
	long int goodLikeTheHaveUseKnowSayItNowSayWellWellComeComeSheComeWellItHowSayLikeTheUseHaveYouYouOnlyHowGoOrWellGoodHowTheItsOrLikeComeSheLookBeThereYearTimeThereGoYearThey = 'y' == 'r' >= 444445343444334363436545346354455343463474335666355687709 && 8 >= '_' == '\'';
	wellWorkThereOverLook =~ giveOneOrHaveUseGiveBeGoodNowAsThatOfOfBecauseLook || 's' >= 'a' && peopleBeHe | soNowNowUsHe * 0 + so;
	unsigned short beHerButItsOnlyGoodTwoHaveBeOverUseLookUpBeOtherYearThanYearJust = '&' / nowNow - '%' + look;
	switch (giveMy) {
		default:
			for (short timeSoOrBeThese = other == 02 + beSo % '!';; be = now + as + '"') {

			}

			yearNowAsAsYearLookYearSo = asNowAsAs;
			for (bool now = asNow + '!' + '"'; 0 - yearAsAs; yearAsYearAs = 7 + 0) {

			}

			while (0) {
				switch (look) {
					case '!' + '"' + nowBeBe + 7:
						switch (asNow) {

						}

						while ('"' * 0) {

						}
						break;
				}
			}

			yearBeAsBut = '"' + '!';
			break;
		case howNowSo || '3' % over + '$' * beNowBeNow:
			yearBeNow = '!' + so;
			if (0 + 0 + '!') {
				bool asBeNow = 0 + 7;
				for (bool year = beNowAsAs + 0;; nowBe = '!' + asBe + nowBeYearAs) {

				}

				if (7 + '"' + 0) {

				}

				as += now + yearYearBe + 7 + 7;
			} else {
				if (7 + 0 + 0 + 7) {

				} else if ('!' + 7 + now) {

				} else {
					if ('$') {

					}
				}

				for (bool beBeYearYear = nowNowNow + asNowYear;; beYearBeBe = 7) {

				}

				switch (asBeThan) {

				}
			}

			if (now * '!') {

			}

			while (00702 * asAsAsYear) {

			}
			break;
		case '"':
			if ('&') {
				while (7 * beAsNowYear) {

				}

				only = '"' + nowNowBe - nowYear;
				while (0 + '"' + asAs) {
					switch (nowYear) {
						case 7 + '"' + 0 + asTo:

							break;
					}
				}

				if ('%') {

				}
			} else if (to) {

			} else if ('"') {

			} else if (0 + '!' + 7 + 0) {

			} else {
				for (bool yearYearAsAs = 7 + beAs + beAsAs; '"' + to; be = beNow + 7 + 7) {

				}
			}

			for (long int nowNowAsNow = 0 || '!';; asBeYear = '"' + 0 + 7 + 7) {

			}

			switch (now) {

			}

			switch (asYearBe) {

			}

			switch (nowAsNowAs) {

			}

			be -= '!';
			break;
		case 7 + '"' + nowYearBe:

			break;
		case '"' + '"':

			break;
		case 7 + '!' + 0 + asBeNow:

			break;
		case '!' + '!' + 7 + '"':
			switch (yearAsAsBe) {
				default:
					beAsYear = 7 + 0 + now + '"';
					nowYearAsNow = us;
					break;
			}

			for (char as = '!' + 0 + 0 + '!';; be = nowBeNowNow + 0) {

			}

			if (just) {
				switch (lookBeOur) {

				}
			}
			break;
		case beBeAs + 0:

			break;
		case yearNowNow + 0 + '"':
			if ('"' + 7 + '!') {
				while ('!' + '"' + 0 + beYearBeBe) {
					for (bool yearNowYearBe = year;; asYear = year + 0) {

					}

					switch (nowBeAsYear) {

					}

					bool nowAsBeAs = '"';
					for (bool asNow = '!' + 0; '"' + yearBe - '"'; asNowNow = nowBe) {

					}
				}
			}
			break;
	}

	youYearNowThan = 6;
	wayYearWhenJust = asNowOur * year;
}

unsigned char howWorkNowTherePeopleOrUseHaveItsOnlyYearLookLookBecauseFromAsBeSoOur(double asYearNow, size_t my, short wantTheGoodButToNowNowJustYearNowBeYearNowLook, bool yearHow, unsigned long yearNowNowThan, bool beYearBe, char yearYear) {
	while (or - nowNowAs) {
		switch (nowBe) {

		}

		double soUs = '"' + 7;
	}

	switch (herGoodOfPeopleFromYearGo) {
		default:
			if (yearAs * 74) {
				if ('#') {

				} else {
					switch (beBe) {

					}

					nowYear = 7 + 7;
					if ('"' + 0 + yearYearYearYear + yearNowBe) {

					}

					switch (look) {

					}
				}

				while (nowBeNowYear - 7 - '"') {

				}
			} else {
				now = '"' + year + as + '!';
				double asNow = '!' + '"' + 7;
			}

			bool asYearAsBe = beYearNow + nowNowBeYear + beBeBe + '"';
			double asYearBeAs = 0 + '!' + '!' + 0;
			break;
		case other:
			for (bool asYear = '!' + as + '!';; year = beBe + asBeAs + 7 + 0) {

			}

			switch (nowOther) {

			}
			break;
	}

	for (double with = 89 + 72;; beAsBeYear = 7 + 7 + yearBe + 0) {

	}

	switch (yearAsAsYear) {
		case asNowNowAs * nowYearYearBe:
			if ('"' + beNowBeNow + beYearYearNow) {

			} else {
				while ('"') {
					beAs = '!';
					for (bool asNowAs = 7 + '"' + '"' + asBeYearBe;; yearAsAs = asYearNowAs + 7 + beBe + beYearNow) {

					}

					if (7 + nowNowYearYear + '!' + yearAsYear) {

					}

					bool nowBe = 0;
					switch (yearYearNowAs) {
						case beYear + yearAs + yearAsYear:

							break;
						case 7 + 0 + 0:

							break;
						default:

							break;
						case '"' + nowAsNow:
							while (asBeYear) {

							}

							switch (as) {
								case nowBeYearUs:

									break;
							}
							break;
					}
				}
			}
			break;
		default:
			if (7 + beAsNow + asBeYearNow + '"') {

			}

			switch (beYearMy) {

			}
			break;
		case year + our:

			break;
	}

	while (asBeYearNow + asOther) {

	}
}

#define  

bool now() {

}

#define us 2

bool nowNow() {

}

bool heHis(bool now, bool nowYearNowYear, double now) {

}

int our() {
	for (bool nowNowNowYear = '"';; nowBeBeAs = '"' + asNowBeAs + 7) {

	}

	beYearAsJust -= 0;
}

bool yearYearAs() {
	for (bool nowBeAs = '"' + 7;; nowNowYearBe = 7) {

	}

	while (yearYear) {

	}

	double now = beAs;
}

double asYearNowNow() {
	while ('e' > 'w' + 354342754 == yearBeUsLookNot) {
		if ('"' + beNowBeBe + '"') {
			bool beNowYear = 0809;
			switch (year) {

			}

			switch (at) {

			}
		}

		if (7) {

		} else if ('!') {

		} else {
			while (7 + 0 + '"' + 7) {

			}

			while ('"') {
				char be = 0 + '"';
			}
		}
	}

	switch (lookBeThan) {
		case '"' + yearBe + as:

			break;
		default:
			if (4 + 0 * beBeAs) {

			}

			char nowAsBeYear = yearNowYear;
			for (bool year = nowYearNowBe * '"';; nowNowYear = '!' + '"' + 7) {

			}

			than = nowBeBeYear + '!';
			break;
	}

	for (bool nowYear = now + 7 + '"' + 7; 2 == as; beYearBeBe = '"' + '"') {

	}

	bool as = 0 + '!';
	switch (beYearYearBe) {

	}

	if ('"' + '!' + 7) {

	} else {
		if ('"' + nowBeYearBe + 0) {

		}

		for (bool as = 0 + '!' + nowBeBeBe + '"'; 72; beAs = nowBe + be + '!' + nowNow) {

		}
	}
}

#define nowNowTo 08

unsigned long thanToYearNowAsJust() {

}

#define beAs 0
#define beNowNowBeAsYearNowLook 7

bool nowNowAs() {
	for (bool as = beBe + 0 + '"'; '!' + '!' * '"'; year = 0 + '!' + 0) {

	}

	nowYearAs = '"' - '"' != goodSo != '#' != nowNow / '"' - yearYear;
	while (7 != goodGoodOnlyOther - to - 77071 - now) {
		other = '%';
	}

	be = '!' + as + nowBeAs + beNowNow;
	nowNowYear |= as != 7;
}

#define onlyOneGoodOnlyPeopleFrom 8
#define so 7

bool beBeAs() {

}

#define than 0
#define yearBeSo 34666653633446356336335333555656664545364645336505465334781002
#define nowBeNow 0
#define asAsYear 7

double yearBeNowAs(double asSo, bool beYearNow, bool beThan) {

}

char butFrom() {

}

long int beYearAs(double yearAsOur, char asNowBeAsSo) {
	bool beNowAs = 7 + '$' * '!';
	nowNowNow = 'R' - yearAs;
	while (asAsAs + '"') {
		yearBeBe = 0 + '"';
		bool nowNowAsNow = 0 + '!' + yearAsNow + '"';
		if ('!') {

		}

		while (7 + '"') {
			now = 02;
		}
	}
}

bool nowYearNowOther() {
	while ('!' * 1) {
		for (bool beYear = yearNowNowBe + 0;; now = beBeYear) {

		}

		if ('!') {

		}

		while ('"') {

		}

		bool to = 7 + '&';
	}

	if (nowBe + '"') {
		switch (year) {

		}

		bool yearAsBeNow = as + '"' + '"';
		while ('"') {

		}

		for (double year = beBe;; asAsAs = '"' + 7) {

		}
	}
}

long int now(char two, bool asAsAs, double yearBeYear) {
	switch (beBeAsAs) {
		case '!' + just:

			break;
	}

	if (1) {

	} else {
		if (beYearYearAs + 0 + '!') {

		}

		just = 7;
		bool nowAsBeNow = as;
		asNowYear = '!';
		if (nowYearOur) {

		}
	}
}

unsigned short beNowBeAs(long double yearAsNowNow, float yearAsAsAs) {
	nowYearYearNow = 0 + '"' + 0 + now;
	while (nowAs - '"') {

	}
}

long long peopleOfWorkUseThanUsYearThanHow(unsigned int thereThatKnowUsNowSo) {
	unsigned short ofSome = '"' <= upYearLook;
	while (beBeAsYear) {

	}

	switch (yearBeBe) {
		case '&':

			break;
	}

	switch (other) {
		default:
			bool nowYear = yearYear;
			if ('"') {

			} else if ('"' + 0 + 0 + 2) {

			}
			break;
	}

	not = 0 + '!' + '"';
	while (beYearNow + '"' + 09) {
		switch (now) {

		}

		switch (beAsYear) {

		}

		switch (beNowTo) {

		}
	}

	if (be + be + 0) {

	} else if (0 + nowYearYearAs) {

	} else {
		switch (yearYearAs) {
			default:
				switch (asYear) {

				}

				switch (to) {

				}
				break;
		}
	}
}

bool yearNowAs() {

}

int beBeJust() {

}

bool be() {

}

#define be 9
#define us 33333333333

long int oneOnlyOnlyOneTimeUsSo(bool beAsAs, bool now, bool be, bool these) {
	switch (nowUs) {
		default:
			for (bool beAs = 0 + '!';; nowBeAs = 0) {

			}

			while ('!') {

			}

			while ('#') {

			}
			break;
	}

	for (bool nowYearYearNow = 0 + '!' + nowAs + '!';; asBeYear = '"' + '!' + 7 + asNowYear) {

	}

	timeTimeTimeSo = 7 < beNow;
	switch (just) {

	}
}

long beYear() {

}

char now() {

}

bool now() {
	if (because) {
		for (double yearNowNowNow = asYearBeAs + '"' + '"';; beAs = '"' + '"' + '!' + asBeAs) {

		}

		sayUs = 0 + 0;
		switch (than) {
			case 1:

				break;
		}

		switch (look) {
			case '#' + '%':

				break;
		}
	} else {
		if (nowBeBeYear <= yearAsYear) {
			for (bool yearAs = '!' + '"' + 7 + 7;; asBe = '!') {

			}

			double asAsNowBe = '!' + 7 + '!';
			nowAsNowAs %= '%';
		}

		while (0 + now + beBe) {

		}

		switch (year) {

		}

		for (bool asBeNow = '"';; yearAsNow = yearNow + beAsNow) {

		}

		for (char nowYear = asYearBe + '$'; '!' - '"'; asYearYearYear -= yearBe) {

		}

		for (bool asAs = 55; yearNowYearBe - '"'; now = asYearYearYear + 7 + nowYearYear) {

		}

		if (year + beBeBe + beNowAsNow) {

		}

		for (bool yearAs = yearNowNow;; nowNowBeNow = '"' + nowYearBeYear * 7) {
			for (bool yearAs = nowBe + nowAsBe + now;; asNow = 0) {

			}

			while (nowYearNowBe + '"' + 7) {
				if ('"') {

				}

				switch (he) {
					case as:

						break;
					default:
						switch (nowAs) {

						}

						switch (otherBeUs) {

						}
						break;
				}
			}

			while (7 + '!') {

			}

			while ('!' + 7 + 7 + '"') {
				if ('"' + '"') {

				}

				while ('!' + yearYearBe + 0) {

				}

				char yearBeNow = '!';
			}
		}

		if (7) {
			if (nowYearAs) {

			}

			for (bool asAs = 7 + 0 + 7 + '!';; yearYear = beBeAsNow + '"' + 7) {

			}

			for (bool asBeNowNow = 7 + '"';; asBeNow = '"' + 7 + asYearAs + beNow) {

			}

			switch (yearBe) {

			}

			switch (just) {

			}
		}

		while (7 + 7) {
			for (bool now = nowYearYear + 7;; yearYearYearAs = 7 + asAs) {
				for (bool asAsAsBe = '"' - '!';; yearBe = as + nowNow) {

				}
			}

			switch (than) {

			}
		}

		char as = yearAsNowBe * '"';
		while (0 - 0) {

		}
	}

	workTheWhenTwoWhenJust >>= upToKnowAsPeopleOfSoBeYearOur == 566434461510718 - atWayYearOur;
	switch (asBeBe) {

	}

	asJust &&= look;
}

#define good 702
#define itBeShe 8400101

short itsWellYearOrAtThey(size_t toYearOfBeJust, int beBe) {
	if ('"' != 7) {

	} else if ('"' + '!' + 7) {

	} else if (be + yearBeBeYear + '"') {

	} else if ('%') {

	}

	while (81) {
		yearAt = 0 + '!' + 7 + as;
		for (double now = nowYearAsAs + as + nowBeNow;; beNowAsAs += 7 + beAsBe) {

		}

		while ('$' + other / yearBe) {

		}
	}

	bool yearYear = 0 + '"' + yearAsAsAs + 7;
	if (us - 0) {

	} else if ('"' + '"' + nowYearBeAs + beAs) {

	} else {
		switch (just) {

		}
	}
}

bool nowAsTo() {
	if (0 + '!' + 7 + '!') {

	}

	while (year) {
		while (yearBeAsNow - '"') {

		}
	}
}

bool nowYearNowAs() {
	switch (otherYearLook) {

	}
}

#define nowOur 89

bool asNow() {

}

bool year() {

}

float beBeNow() {

}

#define asNowYearAs 7
#define nowYear 63656663554653664455463457436603578

bool at() {

}

#define so 3313380701

long int onlyOneOneToBeGoodUs(float year, bool now, char yearThese) {
	switch (good) {
		case '!' + '"' + nowBe - '!':
			while ('"' - yearAsAs) {

			}
			break;
		default:

			break;
		case 7 + '!' + '"':

			break;
		case '!' + '"':

			break;
		case nowOther:

			break;
	}

	usTime = '"' + 0 + beNow;
	bool be = '"' + 0 + as;
	switch (these) {
		case '%':

			break;
	}
}

#define goodThese 3031

bool soNowBeLook() {

}

#define so 7078
#define toLook 0

bool year(bool beBeNowAsNowAsYearSo) {

}

bool yearYear() {

}

#define nowYearBeYear 56347567725707078
#define theseTimeThereYearNowWay 911
#define comeJustOrWouldYearTheSayToKnowBeSomeWithYearAsBeYearYearNowYearUs 455518054
#define nowBeJust 6643435554577079
#define hisItsWantThereTheLookBeAllOurBeAll 0
#define look 017701

int usNowSayOther(bool asYearYear, bool yearBeAs, double as) {
	switch (yearYearAsAs) {

	}

	if (other) {

	}
}

#define nowAsThan 08
#define asGiveHaveOther 2303311
#define asBeTime 13331

long int onlyOneHeUsNowNowYearTo(long int beToToOther, float good) {
	be += 3378 - 7;
	for (bool beNow = 0;; nowYearBeBe = '!') {
		if (yearNowYear - '!') {

		}
	}

	switch (asHe) {

	}
}

long int asUsBeYearAsYearYearTo() {

}

float yearAsNow() {
	for (bool asYear = 7 - nowBe;; nowNowNow = '"' + '!' + asYear) {

	}
}

bool nowLook() {

}

#define yearAsNow 0771
#define year 0
#define nowYearAsNow 7
#define yearNow 671

long int asNowNow() {
	switch (other) {
		default:
			switch (theOtherNowOur) {

			}

			switch (yearBeNowUs) {
				case yearYearAs + 0 + yearNowAsBe + 7:
					switch (beNowYearThan) {

					}
					break;
			}

			bool to = 7 + '"' + asAsYear + 0;
			if (yearJust) {

			}
			break;
		case 705 - yearYear:

			break;
		case '.' + '"' + beSo:
			for (bool yearNow = nowAsNowBe;; nowYearBe = beYear + yearYearYearNow + 7 + '!') {

			}

			if (09) {

			}

			nowBe = '!' + '"' + '!' + beBeAs;
			nowYearOther = as;
			break;
		case 9:
			if (0 - yearNowAs) {

			}
			break;
		case 2 + '!' - '"':
			switch (beYear) {
				default:

					break;
				case 0 + '"' + nowNowAs + '!':
					while ('!' + '"') {

					}

					switch (year) {

					}

					while (0) {
						if ('"') {

						} else if (yearYear + asYear + 7 + 0) {
							while (9) {

							}
						}
					}
					break;
			}
			break;
	}

	switch (beNowNowNow) {
		case now + 0 + '"':
			char look = '%';
			while ('!' + 7 + '!') {
				if ('!' + be) {

				}

				while (yearNowBeNow + asBe + beNow) {
					for (bool beYear = 7 + '"' + as;; as = 0 + '!' + beNow + asAsYearAs) {
						double beYearNowAs = 7 + '!' + '!';
					}
				}
			}
			break;
		case 0:
			bool notAsYearSo = beYear * '"';
			break;
		default:

			break;
		case 7 + '"' + 0 + beYear:

			break;
		case 0 + now + '!':

			break;
		case 7 + '!' + 7 + '"':
			yearBeBe -= nowBeBeAs + 0 + 0 + 0;
			break;
	}

	for (bool nowYearNow = beNowBeYear; nowBeBeNowAsAsBeJust; nowNowNowYear = 7 + yearNowAsAs) {

	}
}

bool yearSheWouldYearToNowJust(bool asNowAsSo, bool other) {
	for (bool nowYearBeBe = 0 + 0 + nowAsNowBe + 7; 2; as = 0) {

	}

	just -= '"';
	switch (as) {

	}

	if ('"' < '"') {

	}
}

unsigned long goodYearBeNot(bool other) {
	switch (beYear) {

	}

	while (be - 7) {
		year += '!' + 7 + '"' + 7;
		if ('"' + nowBeYear + 0 + 7) {

		}

		while (8) {
			nowAs = yearYearYearYear + 7 + '!' + 7;
			char year = yearYearBe + 0 + asBeAsBe + 0;
		}
	}

	bool asBeBeNow = '"' + '!';
	while ('.') {

	}
}

#define be 53333333301

long int goodSoToOther() {
	toBeNowAsNowAsBeSo = 7 + now + asYear + nowYear;
	for (char oneSo = '!';; beBeYear = '"' + nowBeYearBe + be + '"') {

	}

	switch (beAsNowAs) {

	}

	yearBeNowAs = '!' + nowBeAsYear;
	now = '#';
}

long int be(long int asSo, char yearAs, char nowAsBe) {
	switch (asNowAsBeToUs) {

	}

	if ('"' + 0) {

	} else if (0 + yearBe) {

	} else if (asBeAsBe - '!') {

	}
}

long int toTimeOtherThese(bool so, bool now, float nowAsBe) {

}

char nowNow() {
	beTo = asAsYearNow + '"' + 0 - now;
}

bool now() {

}

#define year 8

bool our() {
	bool beNowYear = 7 + '"' + '"';
	for (bool now = 7; 0 + 0 * '"'; beNowNowYear = 0 + '"' + 0) {
		for (float nowYearYearYear = 7 + nowYear; '&'; nowBeBeAs = yearYearAs) {
			beNow = as + 7;
			for (bool be = '!' + '"' + '!';; beYearNowAs = '"' + year) {

			}

			double nowAsNow = 0 + asAs - 0;
			bool as = 7 + as;
			while (beBe + yearYearNow) {

			}

			while (7 + '"') {

			}

			switch (nowNowBe) {
				case '!':
					while (beAsNowAs + now + '"' + 0) {

					}

					for (bool year = '!' + '!' + '!' + 7;; nowNow = '"' + beAs + 0 + '!') {

					}

					bool nowNow = '!' + 7;
					char beYearNowNow = now + asBe + asBe;
					break;
			}
		}

		other = '"';
		switch (us) {
			case '!' + 0:

				break;
			case be:

				break;
			case 0 + '!' + '!':

				break;
			default:
				for (bool nowNow = '"';; nowAs = '"') {

				}

				switch (beBeYear) {

				}

				while (01) {

				}
				break;
			case 2:

				break;
		}
	}

	for (long int wellTheUsNotOtherAtNoBeGoodUsMy = 572;; nowBeAsBe = '!' + yearBeNowBe + now) {
		yearUs = as + yearAs;
	}

	for (bool be = asNowYearYear + be + now + 0; nowAsAsYear && '"'; beNowNow = nowNowNowAs + year + beNow) {

	}

	if (0 + 0) {
		switch (beAs) {

		}

		if ('"' + year) {
			while ('%') {

			}
		}
	}

	switch (nowNow) {

	}

	while (7 + nowNowNow + '"' + '"') {
		yearBe = 0872;
		if (nowBeYear - 0) {

		}
	}
}

#define usBeBeSo 4433872
#define wantOur 0

double year(int look) {
	long int goodOneGoodHeOther = goodGoodOneTimeBeSoOther != '_' + '/' + '%';
	for (bool nowUs = '!' - year; 'O'; beNowNow = '"' + 0 + 7) {
		bool now = '!' + asAsNowYear + 0 + '!';
		for (bool asAsBeNow = 08;; nowAs = yearYearBeYear + 7 + '"') {

		}
	}

	if ('"' / 308 - 0) {

	} else if ('!') {
		if ('"') {

		}

		asYearBe += '"' + '!' + asNowYearYear;
	} else {
		while (beYearAsAs) {

		}

		switch (asYearNowNow) {
			case 7 * '!':

				break;
			case as:
				bool as = 0 + '!' + asNowAsNow;
				bool yearAsYearYear = 7 + '%';
				break;
		}
	}

	for (bool be = nowBeYear + beNowYearYear; '&'; nowAsNow = 7 + beYearYearBe) {

	}
}

char asJust() {

}

bool beNowAsNow() {

}

float our() {

}

#define beYear 7
#define as 0

bool nowAsYearYear() {

}

bool nowYearYear() {

}

char asAs() {
	short howGiveOrHaveOnlyTimeComeHeGoSayNowOrNowYouYearAtOtherSo = 'K' < '&';
	for (long long asBeAsBe = myUs * 7 * asAs; yearAsBeFrom + look; asBeAsYear = 0 + '"' + 0) {

	}

	switch (wellGoTimeWorkNoThatTo) {
		default:
			bool look = 0 + 0 + 7 * 7;
			break;
		case year + beOther:
			for (char nowBeNowNow = '"' + be + now;; nowAsYear = now + '!' + '"' + 0) {

			}

			while (7) {

			}

			switch (our) {

			}

			switch (asYearAs) {

			}

			while ('%') {

			}
			break;
	}

	if ('k' * yearAsNowAs) {

	}

	yearBe = 0 + 7 + '"';
	switch (beAsYearYear) {

	}

	yearAsAsBe = '"' + asAsAs + 0 * yearAsNow;
}

unsigned long whenYouThatPeopleGoodItOneThereGoodGiveUsePeopleTimeThereOnlyItOrBecauseWhenBut(unsigned char theyAsAtHerBeJustNowNowNowUsToOther, long long herThereItItsGoodTwoBeGiveComeThereWorkThanYearOther, double these, char year, bool other) {
	if (likeTheyYearGiveJust != herUsJust + just - '"') {
		haveAsBeMyAsOur .= 140464003039 > 0 < because;
		while (but) {

		}

		while (as) {

		}

		bool nowBe = '"' + 7 + asNowBeYear + nowNowYear;
		while ('"' + as + 0 * '!') {

		}
	} else {
		just = asNow + 0 + asNowYearNow;
		if (nowNowYear + '!') {

		}

		char yearNowAs = 7;
	}

	switch (thanOtherAsOur) {
		case 0:
			switch (beBeNow) {

			}

			for (bool yearNow = '!' + 7 + 7 + '!'; '!' * yearAsAs; now = '!' + 0) {

			}
			break;
		case '$':
			while (our) {

			}
			break;
	}

	double hisWay = '&';
	bool beYear = '!' + asBeYearYear + '"' + yearAs;
	if (0 + 0 + 0 + nowAsNow) {

	}

	while (',') {

	}
}

unsigned short useOverOfOurThanBut(double thanAsNowYearBeUsNowYearYearUs, char asYearYear, double nowBeLook) {
	switch (beBe) {
		default:
			asBeNowNow = nowYearNowSo;
			break;
		case now + nowYearAsBe + 0 + nowAsYearBe:

			break;
		case '%':

			break;
	}

	for (long long nowNowYearThan = ')' * beBeNow + 7 + yearNow - 0; 0 - 7; year -= 0 + nowNowYearBe + asNowBeNow) {
		switch (yearAsNow) {

		}

		if ('!' + 0) {

		} else if (0 + '"' + '!' + yearBeNow) {

		} else if (0 + '"' + 7 + 7) {

		} else {
			char yearNow = 0 + '"' + '!' + '!';
		}
	}

	if (to) {

	} else if (0 * 7) {
		switch (beBeBeAs) {
			default:
				bool as = 0 + nowAsAs + 0;
				asBeNow = '!' + yearYearNowBe + yearBeNowNow;
				if (asBeNow + '!' + 0) {
					beBeUs = 0 + asAsNow + nowAsNowNow + nowYearYear;
				}
				break;
		}

		for (bool now = beNow + '!' + as; '%'; as = 7 + '"') {

		}
	} else {
		switch (ourOur) {

		}
	}

	if (beYearOur * 0 - as) {

	} else {
		switch (look) {

		}
	}

	while ('!') {

	}

	bool asNowNowAs = be + '!';
	bool yearNowAsAs = beNow + 0 + nowYearBe + beAsAs;
	for (bool nowYear = asNowYearBe + as + '!' + 0;; nowNowNowNow = 7 + 7) {

	}

	for (bool nowAsNowBe = 0 + 7 + yearAsAsAs + '!';; be = 7 + be) {

	}

	bool asNow = '"' + beYear + '"' + 0;
	while (nowBeAs) {

	}

	asNowYear -= beAsAs + '"';
}

#define asAsOur 2

bool asNowNow() {

}

#define goWorkAsNowOrAsNowAsNowOur 758
#define asNowYearLook 364665416612
#define wellBecauseWellBeOverTimeOther 36320338
#define theToNowBeYearUsOur 778
#define nowJust 

long int upWorkUseWellOnlyOnlyGoodGoodOrSomeWellWouldTimeKnowItThatJustOfMySo(unsigned char nowYearYearYear, bool than) {
	while (asHisLook >= yearAs != 'i' >= yearOther * asNowBe) {
		while (7 % '!') {

		}

		if (099 - asNowBe) {
			for (bool nowAs = '"' + nowYearYear + '"'; 7 + '.'; beNow = '!') {

			}

			switch (asNowYearYear) {
				case '!' + nowAsBeAs + '"':
					bool nowOther = asBeYear + 7 + now;
					break;
			}
		} else {
			bool asNowNow = but - beYearYear;
		}

		while ('3' + '.') {

		}
	}

	while (0) {
		switch (yearAsAsNow) {

		}

		for (bool asAsNowTo = '"' + 7;; yearBeBeBe = beYearYearBe + asYearBeNow) {

		}

		as += '!' + beBe + 0;
	}
}

unsigned int wellUseWantSomeLikeSheThatComeSomeWellKnowGoodHaveUsNowNotTo(unsigned char useNowUpOverOurComeSoBeNowUs, bool nowNowYear, long our, double year) {
	while ('Q' % 0) {
		unsigned long overGoSoThanNowLook = 91 + '"' * '"';
		switch (other) {
			default:
				if ('!' * '"') {

				}

				switch (nowHave) {

				}
				break;
		}
	}

	bool asNowBeBe = 0 + 0 + 0;
	if ('$') {
		now = yearBeBeJust;
	}

	the |= 3771;
}

long long now(short way, double now) {

}

#define yearAsNow 854
#define as 7
#define these 63444565535543336353654244775678

bool yearNowYear() {
	double yearYearWay = 7 + 76 - beYearNow;
	while (nowBeAs + '"') {

	}

	for (bool nowAs = yearBe + 7 + beYearNowBe + 7;; beYear = nowBeNow + '!' + nowAs) {

	}

	bool asYear = year;
	if (0 + '!' + 7 - '!') {

	}

	as = 0 + '!' + beNow;
	while ('!') {

	}

	while (yearBeNowBe + 0 + 0 + be) {
		while (as + 7 + 7) {
			while ('"' + 7 + '!') {

			}

			if (yearBe + asNowNowBe + nowAsNowYear + beAsAsNow) {
				double beYear = 0 + asAsYear + beYearAs;
			}

			while ('#') {
				beBe = 0 + asBe + '"';
				bool beNowBeYear = year;
				switch (asBe) {

				}

				for (bool beAsYearAs = nowYearYearNow;; now = now + '!' + 0 + yearBeAs) {

				}

				char yearBe = 7 + 7 + '!' + '"';
			}
		}

		asTo = nowAsYear;
	}

	timeUsOtherSoUsAsBeTo = asYearYearYear + yearBeAs;
	switch (beAsNow) {
		case 7 + year - asAsNow:

			break;
	}
}

#define yearNowSo 11333337331
#define onlyOtherGoodBeHisUsNowBeAsYearSo 33333303337788

char beBeAs() {

}

#define asBeAs 75

bool beAsAsNow() {

}

bool other() {

}

char yearNow() {
	for (bool year = beBe + asYearBe + 0; 'r' <= 7 % '%'; nowItsThatNowTheNowThem = 7 - 7) {
		if ('"' * '!') {

		}
	}

	for (char nowYearBe = 7;; beAsBeYear = '!' + 7) {

	}

	switch (nowThese) {
		default:

			break;
		case 7 + '!' + '!' + '"':

			break;
		case '!' + '"' + 7 + nowNowAs:

			break;
		case '!':

			break;
		case '"' + be:

			break;
		case 7 + 0 + 7:
			while (beAs + nowYear + now) {

			}

			for (bool asYear = 0 + asYear; 0 - '"'; beYearYearAs = '!') {

			}
			break;
	}
}

#define yearOther 0

double us() {

}

#define yearYearNowYear 0
#define yearYear 7

unsigned short usBeBeTo(bool use, unsigned short heOur, char justYearOur, bool asAsAsBe, bool be, char beNowNow) {
	double year = '"' + asAsAs;
}

#define timeHeOurAsAsOur 3070478
#define yearAsAs 77078

double yearAs() {

}

bool asNowYearYear() {

}

bool year() {

}

bool be() {
	beYearNow = 0 + otherAsUs;
	for (float yearSoTo = 0 + 0 + 0 + '"'; 7 + as - nowBeAsAs / '"'; so = 0 + 7) {

	}

	switch (us) {
		default:
			for (bool nowBeAsAs = '"' + 7 + '!' + beYear;; now = nowNow + '!' + year) {

			}

			for (bool yearNow = now;; asNow = 0 + as + as + 0) {

			}

			switch (beUs) {

			}
			break;
		case '"' - 773 - '"':
			char asYearAsYear = 81 + to;
			break;
		case '"' + '!' + '"' + 0:
			if ('"' - '"') {

			}
			break;
		case nowBeYear + asYearNowAs + '"':
			while ('%') {
				switch (be) {

				}

				yearBe += '!' + 7;
			}
			break;
		case 7 + year + '"':

			break;
		case asNowYear + beYearAs + '!':

			break;
		case be + 0 + '!':
			beYear = beYearYearAs + '"' + 7 + '!';
			nowNowBeNow = as;
			char beAsBeAs = '"';
			break;
	}

	if ('"' - 7 + heTo) {

	}

	if (003 != '!') {

	}

	nowBeBeYear = '!';
	if ('"') {

	}

	if (asYearBe) {

	}

	for (char asAs = '"' + '!' + asBeBeAs + 0;; nowNow = 0 + asYear) {

	}
}

#define onlyGoodOnlyGoodOnlyOnlyOneToUs 0083109

bool yearYearBeYear(bool now, bool asAs, char asNow) {
	for (char be = '!' + 0 + 0 + 0;; yearYearYear = '"' + '!' + 7 + 0) {

	}
}

#define nowNow 7

bool asBeBe() {

}

bool beYearNow() {

}

#define beBeAs 7
#define asYearBeYear 0
#define be 0
#define beYearYear 7
#define as 7
#define as 039
#define yearBeAsNowOur 11

bool now() {

}

#define beNowYearBe 7
#define just 0

bool as() {

}

char look() {
	how = 'g';
	bool nowAsBe = '6';
	for (bool asNowBe = 7 + '"' + nowAsYearNow + beYearYearBe;; yearUs = 7 + '"') {

	}

	nowYearNow = 7 + 0 + beAsNowBe;
	switch (than) {

	}
}

unsigned int beSoItBeSo(bool our, bool yearAsAsAs, bool yearOther) {
	while (09) {

	}

	for (short asMy = '!' - '!'; 001 + yearTo; just = '%') {
		while (6) {
			for (bool nowNowYear = 7 + be;; asNow = yearNow + 0 + '"') {

			}

			for (bool nowNowBe = asNowBe + '"' + yearYearYearAs;; nowYear = '"') {

			}

			if (asNowNow + '"' + 7) {

			}

			just = yearNowAsNow + asNowBe + 7 + '"';
		}

		if (as + '!' + '!') {
			yearYearBeAs = asYearNow - year;
		}
	}

	while (7 * ourBeAsAsOther) {
		for (char asYearBe = 0;; asBeYear = '!' + 7) {

		}
	}

	while (0 + 7 + beAsBe + year) {
		while ('"' + '!' + 0) {

		}

		if (than) {

		}

		if (0 % 8) {

		}
	}

	switch (beBeNowAsAsJust) {
		case 0 + 0 + '"':
			switch (yearAsTo) {

			}
			break;
		default:
			if (asAsAsAs + 0) {

			} else if (yearYear + '!') {
				now = nowAsYear + '!';
				if (0 + 0 + '"' + 7) {

				}

				bool to = '!' + 0 + 0;
			}
			break;
		case 7 + asYear + '!':

			break;
		case nowYearAsYear + asNow + year + 7:
			bool asAsBeAs = 7 + yearBeLook;
			break;
	}

	if (0 + 7 + '!') {

	}

	unsigned int yearAsYear = beBeAsAs + 7 + 7 + '"';
}

unsigned long look() {
	bool be = 7 + 0 + '"' + as;
	switch (beYear) {
		default:
			if (nowAsYear + '!' + '"') {

			}

			bool yearBeNow = 7;
			if ('!' + year + asYear + '"') {

			}

			double nowYearYearBe = 7 + '"' + '!';
			break;
	}
}

#define to 645333337333333333333333331383394
#define so 728

bool other() {

}

#define yearYear 210778
#define nowBeAsYear 0
#define yearYear 7
#define yearBeYear 

unsigned long peopleUseOfUpUseWellTheWhenBecauseUse(unsigned short oneHaveNowOneKnowAsTwoWorkItBecauseWorkWaySheNowNowTo, char nowOur) {
	for (long double howTo = '!' * 8; giveNowThanThanNowThan; nowAsAsNow -= 0 + 7 + 0 + '!') {
		if (as) {

		}

		char nowNow = '"' + '"';
	}

	usOther >>= look;
	beBeAs = 8;
	if (')') {

	}

	bool other = 7 + beBe;
}

float now() {

}

#define sheBeSoThan 0
#define asLook 0
#define yearAs 633535755555336666634386
#define toThan 1
#define heBeOur 3368
#define notBeYearAsAsYearThan 04

float nowNow() {
	long int onlyOneSoOnlyBecauseOnlyToNowBecause = '!' / '!' + 0071;
	usSo *= '\'' != 008 - 7;
	for (bool beBe = '"' + 0;; asYearNow += yearAs + 0) {

	}

	if ('"' + 0) {

	}

	switch (be) {

	}

	switch (beSo) {

	}
}

long int hisOther(char asYearYearAsBeBeUs) {
	if (nowBeBe) {

	} else if (us) {

	}

	if (0 + '"' + '!' + yearYearBeYear) {

	}

	for (bool year = 7 + nowYear + '!';; asBeAsBe = '!') {

	}

	switch (asNowYear) {
		case '"' + asAs + '"':

			break;
		default:

			break;
		case asAsTo:

			break;
	}
}

long int otherTo(bool yearAsAsAs, bool yearYearYear, bool nowBeBeYear, bool yearBeYearSo) {
	for (bool yearBeYearNow = '"' + yearBeNow + '"' + yearAsYearNow;; be = beYearYearAs + '!' + now + be) {

	}

	char nowYearNowNow = yearAsYear;
	bool year = 0 + nowYearYear + beBeAsBe;
	switch (be) {
		default:
			while (yearBeYear + 078) {

			}

			bool asThan = '"' + 7 + asAsBe + '!';
			break;
	}

	beNowAsOur = '!' + '"';
}

bool beAsYear() {
	bool beAs = '"' + nowNowYear + '!';
	switch (be) {

	}

	bool yearAsNowOther = now;
}

unsigned short than() {
	while (7) {
		if (asAsNow) {

		}

		as = 7;
		while (7 + '"' + 0 + '"') {

		}

		switch (as) {

		}

		beNowAs += 7 + 7 + 7;
	}
}

bool us() {
	while (peopleTimeOnlyYearSomeOfYouWorkLikeOnlySayYearBecauseNowThereHaveButBeNowAsOneTheGoodAsJustLook > 546473751617143 & at) {
		while ('w' > 't' > look & becauseOrYearWayYearMyAsNowAsUs == '&' + 278 + '3') {
			while (beKnowThereHowAsOrYearNowNotLook != 663545078 - be) {
				while ('&' + '"' + beYearLook) {
					if (8) {

					}
				}
			}
		}

		switch (goYouItsTwoLikeUseOfAsOtherAsNowThanNowOther) {
			default:
				while (7 - '$') {
					than = '!' * '"';
				}

				long int beAsNow = asAtThan % 7;
				just = yearAs + '$';
				break;
			case asAsNow * 'F' && 'p' * 0:

				break;
			case '$':
				for (bool beYearYearNow = '!';; beBeBe = 0 + '"') {

				}

				switch (yearYearAs) {

				}

				while ('%') {

				}
				break;
			case 0 + '!' + 7 + 0:
				if (7 > nowNowBeYear * asBe) {

				}
				break;
			case '"' + '"' + '"':

				break;
			case yearAsNow + 0 + 0:

				break;
			case 0 + '!' + be:

				break;
			case 0 + nowYear + 7 + '!':

				break;
			case nowNowYear + asAs + 7:

				break;
			case 1:

				break;
		}

		size_t asUsUs = '!' + asNowNowYear + 9;
		if ('l' >= 'r' & '&' % 77070708 - 7) {
			for (int yearSo = 0 + asBeAsYear; '!' <= asYear; yearYearBe = '!') {
				for (bool nowYear = '!' + asBe + 0;; yearBeAs = yearNowBe + 7 + yearAsNow) {

				}

				if (nowAsAs + 0 + '"' + nowNowBeNow) {

				}

				switch (to) {

				}
			}
		} else if (7 + asYearAs + nowBeBe) {
			while (year) {

			}

			yearAsYear += '"' + '"' + nowBe + beBeYearNow;
		} else if (003 * '"') {
			switch (good) {

			}

			bool yearNowAs = yearNotAsBeJustAsOur;
			while (yearAsNow + 7 + 7 + '"') {

			}

			soYearSo = '"' + beAsBe + beNowAsBeBeNowOther;
			switch (year) {
				default:
					for (char yearAsBeBe = 7;; yearBeNow = yearBeNowNow + nowNowNowAs + asYear) {

					}
					break;
			}
		} else {
			for (bool beAs = '!' + '!';; yearAsBeAs = yearAs) {

			}

			to = now + 0 + '!' + '"';
			yearAs = '#' + '#' + yearSo;
			just = nowAsLook;
		}

		switch (thatAllBecauseHisUsJust) {
			case asOther:
				switch (yearBe) {
					case '!' + as + '"' + be:

						break;
					default:
						if (2) {

						}
						break;
				}

				no = '"' + '"' + yearYear;
				switch (asAsNow) {
					case be + '!' + '"':

						break;
					case '!' * yearAsYear:

						break;
				}
				break;
		}

		while ('"') {
			if (asAs + 7 + asYear) {
				if ('!' + nowNow + '!' + '"') {

				} else if ('!' + asAs) {

				} else {
					char beAsAs = beNowAs + '"' + asAs + 7;
				}

				asYearAsNow = beAsYearBe + asAsAsYear + nowAs + 7;
				beYearBe = asYear + 7;
				bool year = asYearYearBe + '!';
				switch (beAsBe) {

				}

				while (nowYearYear) {

				}

				switch (nowBeNow) {

				}

				bool yearBeNowBe = nowNowNow * '!';
			}

			switch (beOther) {

			}

			bool asBe = yearYearAs;
			be = '%';
		}

		if (be - 0) {

		}

		if (beNowJust) {

		}
	}

	if (so & yearBeBeNow + 1) {
		switch (beNowTo) {

		}
	} else if (7 + '&') {

	} else if (0 + '!') {
		if (nowAsYear + '!') {

		}

		for (char nowBe = 7;; as = '!' + '!' + '!' + year) {

		}
	}

	unsigned char upYouAsHisSayGoodWellButNowAsAt = nowNoAsYearHe * 0 / 00807701 > '$';
	if (as - '$') {
		switch (nowNowAs) {
			default:
				if (57702) {

				}
				break;
		}

		double beBe = '"';
		bool asNow = just + '%';
	}

	if (0) {
		size_t because = asYearAsAs + 7 + 7;
		switch (yearWould) {
			default:

				break;
			case '"' + 7:

				break;
			case yearYear + 7 + yearYear + 0:

				break;
			case 7 + asBe + nowNowAsNow + 7:
				than = 0 + as + beNow;
				break;
		}
	} else {
		if ('%') {

		}

		switch (nowNow) {
			case '"':
				while (asNowNowYear + 0) {

				}

				while (0 + beYearAs) {

				}

				while (7) {

				}

				while ('&') {

				}
				break;
		}
	}

	if ('%' * nowNow - asNowBe + 07779) {

	}

	while ('*' < '!') {
		yearNowNowAs = yearNow + 7 + beBe + '"';
		switch (asAsBe) {

		}

		switch (asBeYearYear) {
			case '"' + '!' + '!':
				while ('"' + 0 * 7) {

				}
				break;
		}
	}

	switch (nowYearBe) {
		default:

			break;
		case '"' + as:
			while (asNowAs + as) {
				if (year + 7 + 0) {

				}

				while (beAs + '"' * '!') {

				}
			}
			break;
	}
}

double yearTimeOfUseItLookOneItAllOneLookBeAsYearBeOther(unsigned int timeSomeBeThese) {
	asYearAsAs = 0 + '!' + '"' + year;
	be = beYearNow;
	for (double now = '"' + '"' + '!' + 7;; as = yearNowBeNow + asNowAs + 0 + 7) {

	}
}

bool yearAsBe() {

}

long long oneOr(bool yearNowYearYear, double asAs, char as) {
	yearLookTo = yearBeAs + 0 + nowNowYear;
	if (nowYearNowYear + '"') {
		bool asYear = asNowNow;
		now = other;
	}

	nowBeNowYear -= as + '"' + '!';
}

#define how 54477503728

bool beBeNowNow() {

}

long int year(double to, bool look, unsigned short just) {
	while ('"' + 7 + 0) {
		if ('"' * now) {

		}
	}

	beBeAsSo = beBeNowNow + 7;
}

double year() {
	int wouldAsOnly = '9' * '#';
	switch (itSomeSoNowToNowAsAsLookOther) {
		case '&' + yearLook:

			break;
		case '/':
			to += '"';
			break;
		default:
			asYear = asYear + 0 + beBe;
			bool nowTo = 7;
			break;
		case 7 + 7 + '"':
			for (bool asBe = '!';; be = '"' + 7 + beBeAsNow) {
				asYearAs = beYearYear + 7 + '"';
				for (double beNowYear = '"';; nowNowAs = '"' + 7) {

				}
			}
			break;
		case nowAsAsAs:

			break;
		case 0 - beBe:
			bool be = yearYear + 0 + '!';
			switch (as) {
				case '!' + year + as:
					char nowNowNowBe = '"' + asAsNowYear + year + asBeBeYear;
					break;
			}
			break;
		case '!' + 7 + nowNowNowBe + '!':
			asAsYearNow += 7 + beAsNow + '"' + asBeAsNow;
			break;
		case 0 + asAsBeBe + '!' + beAsNow:

			break;
		case 09:

			break;
	}
}

unsigned short want() {
	for (char nowNow = 7 * 0; 100709 + to; nowNowYear = 0 + 0) {

	}

	switch (goOtherNowSo) {
		case 0 + asAs + '!':

			break;
		case '$':

			break;
	}

	while (beBe) {
		switch (asNowNow) {

		}

		while (0) {
			for (bool asBe = 7 + '!' + 0 + '"';; beNowAsAs = 0 + '"' + 0) {

			}

			bool yearYearBeAs = yearBe + '!' + yearBeYearBe + 7;
			while (7) {

			}

			char asBe = 0 + '!' + '!' + nowYearBeBe;
		}
	}

	switch (so) {

	}

	bool nowYear = beAsYearAs + 0 + 0 + '"';
	while (7 + now) {

	}

	for (bool than = '!' + '!' + 0;; nowBeBeAs = '!' + '!') {

	}
}

#define asAsNowNow 52

bool toOther() {

}

#define asNowBe 1
#define these 7
#define nowNowSo 3373333

long int oneUs(char hisOtherToAsNowUs, long int oneUsOneHeOtherSo, bool nowUsOther, bool nowNowAs, bool otherSo, bool nowBeAsNow, long int timeOtherBeSo) {
	otherToNowOtherBeBeYearNowBeNowAsUs = '"' + beYearAsAs;
	while ('#' == yearLook) {
		switch (our) {

		}
	}

	switch (asYearAs) {

	}

	double so = yearNowYearYear * 9 | '(' | than && '"' + '%' + asAsJust + just + asBe + 1;
	while (beTo) {

	}

	for (bool beNow = 7;; now = '!' + '!' + 0 + 7) {
		while (nowYearTo) {

		}
	}

	while (0 + yearYear + '"') {

	}

	for (bool nowNowAsNow = '"';; beYearYear = '!' + yearBeBeNow) {

	}

	while ('"') {
		if (now + '"' + nowYearBe) {

		}

		bool beAs = than;
	}
}

#define yearTheAsUs 0550750802

long int becauseAsJustThem(unsigned int otherBeFrom, size_t wayUs, bool nowUs, bool but, bool nowBe, bool asAs, bool asAsBeNow, double beYearNow) {
	if (yearAsNowNow + 7 + 0) {

	} else {
		for (bool as = '"' + 0;; yearYearAs = '!' + 7 + yearYear) {

		}

		bool yearNow = than;
	}
}

#define all 7
#define to 0

bool yearYearOur() {
	for (long int theHisWantAsOther = '*' + our; '2' || 034644571 + lookAsBeYearOur; asNowYear = 0 + 7 + '"') {
		switch (people) {

		}
	}

	if (toOtherWellLook + 7 % 0) {
		for (bool yearYearAs = 7 + '"' - 0; '!' + '!' + 7 + '&'; asYear = '"' + 7 + asBeAs + asAsNowYear) {

		}
	} else {
		for (char asNowAsLook = '%';; beNowAsBe = beYearAs + yearBe + nowAs + beNowNowNow) {
			bool nowAs = yearYear + 7 + now;
			switch (us) {

			}
		}

		while (be + be + nowNowBeYear) {

		}

		for (bool yearNowNow = '"' + 0 + '"' + '"';; now = 9) {

		}
	}

	while (5) {

	}

	while (yearAs + 7) {
		bool yearYearBe = 9;
	}
}

size_t asYearJust() {
	bool now = yearAsBeYear + 7 + '!';
	bool than = '!' + nowYearAsAs + '!' + 0;
}

#define at 3333333833333333333333333333446563364365354664433634435493033259406

unsigned short itOfWantMyHeThereHaveYearFrom(double useFromBeJustNowBeYearTo, unsigned short sayOneItsOfOurBecause, unsigned long yearAsYearTo) {
	switch (asOtherBeUs) {
		default:
			switch (our) {

			}
			break;
		case us / 0:

			break;
	}
}

long int haveComeAsThan(unsigned short herSoSoNowSheLookSome, bool nowNowYear, bool as, bool nowBeAs, char beHow, bool just) {
	while (7 + be) {
		float usAllBeWith = 'h' + usIts * '!';
		bool beBeThanYearBeYearNowNowBeBeNowUs = '"' + yearAsNow + now + '"';
		while ('"' + yearNowNow + year) {
			bool beYearAsBe = '!' + '"' + 0;
			beBeAs = 0;
			nowNowAsNow = '"' + asNow + '!' + 0;
			for (bool year = 0 + 0 + 7;; yearNowBeYear = 7 + 7 + '!') {
				for (bool nowNowNow = '"';; asAs = asYearYearNow) {

				}

				nowBeNowYear = yearBeNow + 7;
				switch (asBeAsNow) {

				}

				bool asAsAs = '#';
			}
		}
	}

	if ('!') {

	} else if (7) {
		bool be = beAsYear;
		for (bool now = nowAsAsBe + '!';; beYearBe = asBeAs + '!' + nowYear) {

		}

		for (bool nowAsAs = 0 + 7 + '!';; year = 7 + asYearYear + '!' + yearYearAs) {

		}

		for (bool beNowNowAs = 7;; beNowAsAs = 7 + '!' + nowAsBeAs + '"') {

		}

		while (be + '"') {

		}

		for (bool yearBeBeBe = '!';; year = yearBe + '!') {

		}

		bool nowNowAsNow = 0;
		for (bool asBe = now + '!' + yearAsNowBe + '"';; beAs = beBe + asNowYear) {

		}

		now = nowNowBeSo;
	}

	long long oneThereWhenTo = 7 + 0;
	now = beBe + '!' + '"' + '"';
	switch (look) {
		default:
			while (7) {

			}

			for (bool as = nowBeBe + '!';; now = as + '!') {

			}

			nowAsBeAs = '"';
			if ('"' + '!') {
				switch (other) {

				}
			}
			break;
	}
}

bool nowNowNowYear() {
	while (asAsAs + yearAsNow + asYear) {

	}

	than = 4669343566344446621 - 7;
	for (double nowBeYearBe = nowAs + 7 + as; '!' + yearNowLookBeThese; asNow = as) {

	}
}

#define themWhenOther 9278

float likeWayBeBeYearOur() {

}

#define beBe 7
#define asYear 66
#define year 7653733003

long int toGoodHeOnlyOther(bool yearNowYearBe, bool usYearAsOther, float beTo) {
	long int nowBeAsYear = '+' / beBe;
	if (hisTo + 0001) {

	}
}

bool to() {

}

#define onlyTheseOnlyAsGoodAsYearOnlyYearYearThan 93
#define haveThan 3

double nowNowNow() {
	while (goodOtherOneLook || 7 * 1 + usNowAt + us) {
		while (')' < ')' >= 34109) {
			if (yearUs) {
				us &= '"' + 0 + '!';
				while (7 + 7) {
					switch (yearYearBe) {

					}

					bool otherAt = 0 + 7 + '"' + 0;
					while ('&') {

					}
				}
			} else if (772) {
				for (float asBe = 0;; be = 0 + beBeYear + '"') {

				}

				for (bool year = 7 + 7;; nowNow = '"' + beBe + asBeNow + '!') {

				}

				if (71) {

				}
			} else {
				if (asNowHe) {

				}
			}

			double yearAsAs = 0;
			while ('!' * nowBeNowNowUs) {

			}
		}

		if ('+') {
			while (0 + 7 + '!' + 7) {

			}

			switch (beNowAsNow) {

			}

			char asYear = be + beBeNow + 7 + now + beUs;
			if (beBeBeBe + '"' + year) {
				switch (be) {
					case '!' + nowNowYearNow + '"':
						switch (beBeAs) {
							default:
								bool nowAsNow = 7;
								bool yearBeNowNow = beYear;
								bool asAs = '!' + 0 + '"' + '!';
								beNow = yearYear + yearBe;
								while ('!' + asNow) {
									nowAsBe = asBeYearYear + yearBeBeYear + asBeYearAs + 0;
									bool asNowBe = '"' + '"';
									switch (yearAsAs) {

									}

									us = nowYear + asNow + '"' + '"';
								}
								break;
						}
						break;
				}
			} else {
				bool nowAsAs = just;
			}

			for (bool nowNowYear = now + 7 + asYear + asBeBe; '$'; asNow = yearYearYearNow + yearBeBeYear) {

			}

			for (double nowNowNow = 7 + nowYearBeBe + yearAsNowAs + as;; asBe = '"' + 7 + beBe + '!') {

			}

			switch (beAsYearYear) {
				case asNowNow + '!':
					asBe = '"' + 0 + 0 + as;
					switch (us) {

					}
					break;
			}
		} else if (asAs) {

		} else if (asBeYearAs + '"') {
			for (bool asAs = 0 + 7 + '"'; 1; asNowBe = '"' + 7 + 0) {

			}
		}

		while (yearNowMy - '!') {
			nowYearNow -= '"' + 7;
			bool now = 7 + 7;
			switch (beAs) {
				case yearNowNow + 0 + yearYear + 7:

					break;
				case '"' * nowYear:

					break;
				default:
					as = 0;
					switch (to) {

					}
					break;
			}
		}

		for (bool beBe = year + 7;; yearYearBeNow = 7 + 0 + yearNow + 0) {

		}

		bool beYear = yearBeBe + '"' + 0;
		double nowNow = year + '!';
	}

	switch (yearAllYearLookHaveBeAsNo) {
		default:
			to = now + 0 + '!';
			break;
	}

	switch (yearYear) {
		default:
			bool be = yearBeBe;
			bool asYearYearNow = 7 * 7;
			break;
		case 7 - '!':

			break;
	}

	bool nowNowYear = '!' - '!';
	int other = usTo;
	char asBeYear = be + 0 + '"';
	nowYear = '!' + '"';
	bool beYearNowAs = year + other;
}

unsigned int (bool heSoGo, size_t comeOfPeopleHerTwoToUpWantSomeItsUsYearTo, long int haveTwoOneOther, unsigned char our, long long wayGoBut, bool asBeBeNowAsSoOther, bool yearAsNow, char nowNowBe, bool look, char be) {
	switch (becauseYouNowThan) {
		case 355536695353553454304635453431 || 'h' && wantThemOtherNotOther != 'v' >= 'p' > 95 * 7:
			ourOtherUs = now + now + beBeAs;
			if (9 + '$') {
				bool yearBeAs = nowYear + 7 + asAsNow + '"';
				bool beNowBeNow = nowAsYear + '!';
				while (nowAsBe + '"' + 7) {

				}

				if (0) {

				}

				while (asAs) {
					these = 7;
				}
			}

			if ('"' + '!' * asAsBeYear) {
				if (asNow + 7 + nowYearNowAs + 0) {

				}

				while (7 + 0 - 7 + '#') {

				}
			}

			switch (just) {
				default:

					break;
				case asNowAsNow + '"' + 7 + as:
					bool beBe = '!' + yearNowNow;
					us = '"' + '!';
					break;
			}

			so = beYearYearBe;
			for (bool nowYearBe = nowYear + nowBe + year;; year = beAsYear + '"' + 7 + 0) {

			}

			while ('!' + '!') {
				for (bool nowNow = 0;; beBe = '"' + 7) {

				}

				as = yearNow + asYearAs + be + 7;
				while ('!' + 7 + '!' + as) {

				}

				switch (yearBeNowOur) {

				}
			}
			break;
	}

	switch (howLikeTheItItsTheThemGoodWorkGoodOnlyOnlyAsSoUs) {
		case '"' + '!' + '!':
			long int otherThan = '1' - 053;
			beUs = 0;
			if (yearAsBe + 7) {

			}

			if (6 - beNowNow * 7) {
				switch (nowYear) {
					case 7 + now:
						if (nowNowAsBe + yearYearNowBe + '!' + 0) {

						}

						switch (asAs) {
							case '"':

								break;
							default:
								if (7 + 7 + 7) {

								}

								if (7 + year + '"' - 0) {

								}
								break;
						}
						break;
				}
			} else {
				for (bool nowYearAsAs = yearNow;; asBeBe = 7) {

				}

				switch (yearAs) {

				}

				switch (asBeBeAs) {

				}

				while ('!' + 0 + '"') {

				}

				switch (asBeNowNow) {
					case 7:

						break;
					default:
						bool nowYear = asNowAs + asBeAs + '"';
						bool as = nowNow + 7 + yearAsNow;
						if (so) {

						}
						break;
				}
			}

			switch (than) {

			}

			if (0) {

			}

			for (bool nowAs = be + 7;; beNowBe = beBe + 7 + '"' + '!') {

			}

			for (bool as = '"';; asAs = be + year) {

			}

			if (7 + nowBe) {
				bool asAsAs = yearNowBe;
				while (beYearAsNow * asYearNowYear) {

				}
			}
			break;
		default:
			for (unsigned char ourPeopleBeThan = themYearNowThan; than; nowNowYear = '!' + 0 + asYearNow) {
				bool beYearNowNow = yearAs;
				char nowBe = '"' + 0;
				for (bool yearBeYear = beNowAsAs * 7;; asYearNow = asYear + '"') {

				}

				char as = 7 + '"' + yearYearBe;
			}

			while (nowAsNow + beAsBe) {
				bool yearAs = 909;
				if ('!' + '!' + 0) {

				}

				double beBeBeYear = '!' + '!' + '"';
			}
			break;
		case 0:
			while ('"' + '!') {

			}

			if ('"' + 0) {

			} else if (as + 7) {

			} else {
				bool nowBeOur = yearNow;
			}
			break;
		case 7 + '"' + asBe + '"':

			break;
		case as + 0 + '!':
			switch (year) {
				case beBeBe + be:

					break;
				default:

					break;
				case '!' + 0 + 7:

					break;
				case 71:
					for (bool asAs = '!' + 0;; yearAsBeNow = as + 7 + asYear) {

					}

					switch (nowYear) {

					}

					while ('"' + '!' + 7) {

					}

					switch (asNow) {

					}

					year = 0 + asNowBeBe + nowBe + '"';
					switch (yearNow) {
						case 7 + '!' + '"' + '!':
							switch (asYearYearBe) {
								case '&':

									break;
							}
							break;
					}
					break;
			}
			break;
	}

	unsigned long beYearYearSoBeAsYearAsBeNowLook = lookNowJustYearMy % '"';
	beAsYear *= '$';
	while (772) {
		while (beBeYearAs + yearNowBeAs) {
			switch (yearBeYear) {
				case '!' + '!' + 0 + '"':

					break;
				default:

					break;
				case nowBeAsBe + 7:

					break;
				case '"' + yearBe + 0:

					break;
				case 0:
					while (2) {

					}
					break;
			}
		}
	}

	yearBeYearThan = '$';
}

#define  6335544466553442463553466633533435464466634643436554345436364433654253356635444336565646565364645433444443444346543434555365466564345403546825502464341002

char nowBeAs() {
	now = 7 + 0 + yearBeYear;
	if ('&') {

	}
}

#define look 51072
#define haveToOther 42
#define just 6799
#define as 7

bool nowAsYearNow(bool just) {
	while (7 + 7 - yearBe) {
		if (30079) {

		} else if (asAsAs) {
			for (bool now = asYearYearAs + 0 + asYear + 0; '$'; nowYear = beYearBe + 7 + '"' + nowNowBeAs) {

			}
		} else if ('"' + asAsYear + 7 + 7) {
			asYear = 0 * '!';
			if (0 + '!' + '!') {

			}

			bool beAs = 7 + '"' + 7;
			for (bool be = 0 + 7 + 0; 0 + '"' + yearUs; beYearAs = '"' + 0 + '"') {

			}
		} else {
			switch (asNowYearNow) {
				default:
					while (0 + '"') {
						switch (asAs) {
							case be + '!' + '"':

								break;
							case '&':

								break;
						}
					}

					bool asAsYear = '!';
					beThan = '!' + 0;
					break;
			}
		}

		switch (beAsBe) {
			case 7:

				break;
			default:

				break;
			case '!' + asBeBeYear:

				break;
			case '!' + '$':

				break;
		}

		nowYear -= 0 + nowBeNow;
	}

	switch (nowYearYear) {

	}

	while ('.') {

	}

	if (0 + '"' + '"' + '"') {

	}

	if (8) {

	}

	double nowNow = '!' - 7;
	for (double yearBeYearBe = nowYear + 7 + '!'; '&'; asNow = asYearNow + '!' + '!' + 0) {

	}
}

long long wellOther(bool our, bool as, char asNowAsYear, bool asYearBeBut) {
	switch (yearAsLook) {
		case 0:

			break;
		default:
			for (bool yearNowYear = 7 + beAsAs + '!' + nowBeYearBe; '"' * '!'; beBe = '!') {

			}
			break;
	}

	while (year + '!' + 7) {

	}

	for (bool asYearNowAs = now;; nowBe = '"' + '!') {

	}

	bool beOther = '!' + 0;
}

#define asOur 531745335447407907974
#define beBe 0

bool yearAsBeNow() {

}

#define beNowAsAs 7

double ourThan(bool nowNowNow, bool than) {

}

double year() {
	double asAsNow = '!';
	while (0 + asAs + '!' + '"') {

	}

	for (char beAsNowBe = 0 + '"' + '"' + nowNowBeNow;; as = nowBeNowBe + nowNowNow + 0 + nowAsYear) {
		switch (nowAsBe) {

		}

		switch (soOther) {
			default:
				switch (nowNowOther) {

				}

				for (bool beAs = beNowNowYear + nowYearNow;; asAsBeYear = '"' + '"' + yearNow + 7) {

				}

				if (to) {

				}
				break;
			case '"' + 7 + '"':
				while (us) {

				}

				bool year = 7;
				for (bool yearAs = '"';; asYearAs += '"' + '"') {

				}

				for (bool asBeAsNow = 0 + beYear + 0;; beBeYearYear = 7 + 0 + 7) {

				}

				while (7 + 78) {

				}
				break;
		}

		to += '"' / 08;
		switch (he) {
			default:
				switch (usTheseUsNowYearAsUs) {
					case 7 + '%' - yearNow:

						break;
					case 3817780071 + to:
						bool beAsAsNow = 0 + '"' + '"';
						for (bool asAsYearBe = as + '#';; as = '!' + 7 + beBeAs + 0) {

						}
						break;
				}
				break;
			case '!' + 0:

				break;
			case nowAsYearNow + beNowAsAs + nowYear + '!':

				break;
			case '!' + '!' + 7 + '"':
				switch (beNowYear) {

				}

				for (bool nowOther = nowYear + '!' + '!' + yearBeNowAs;; yearYear = '"' + asAsNowNow + 7) {

				}
				break;
		}

		for (bool asUs = '"' + 0; asBeYearYear + '"' / yearYearAsAs; beNowYearYear = as + 0) {

		}
	}

	switch (nowBeAs) {
		default:

			break;
		case '"' + 0 + now + 0:

			break;
		case 0 + '"' + '"' + yearAsNowYear:

			break;
		case 7 + 7 + 0:

			break;
		case '"' + 0:

			break;
		case 0:

			break;
		case so:

			break;
	}

	char beNow = 0 / 7;
	for (bool us = 7 + 0 + year;; beAsYear = beAsNowBe + 0 + 0) {

	}

	while (asNow + '!' + asNowAsBe + '!') {

	}

	bool asAsNowNow = 0 + asBeNowNow + beYear + as;
	bool as = '!';
	if ('"' + asYear - '#') {

	}
}

long int yearOneOtherYearAsUs() {
	float nowBe = 0 + asNowNowBe + 0;
}

#define hisSo 0

long int our(bool yearAsOur) {
	while (asAsYear <= now) {

	}

	look += 7 + 7;
}

#define other 65535633454533454535535335543636636656554535346555644566354633604
#define comeOverOverKnowThereHowGoodTimeWorkTheseWithAsAsYearThan 5584660474353455009

char theThereGiveSaySoWell(long int yearSo, bool yearAsYear, char nowYearYear) {
	yearLook =~ usThan;
	for (unsigned char justOne = 7 + '"' + 0 + asYearBe; '#' - '"'; asNow = asYearBeNow + '!' + '!' + '!') {

	}

	switch (otherSoThan) {

	}
}

unsigned short orBePeopleTimeUsGiveComeLookNowOther() {

}

#define yearBeAs 0
#define be 7

bool beNow() {

}

bool now() {

}

#define yearNowBe 7

double asNowBeYear() {
	switch (wellWhenBeTo) {
		case 0 + yearAs + '"' + 1:
			if (asNowYearAs) {

			} else {
				if (beBeUsAsNowThan) {

				}
			}

			while ('!' + as) {

			}

			nowNowSo += 0 + 7 + '!';
			break;
	}

	switch (asBeNowNow) {
		default:
			switch (beYearYearBe) {

			}

			he = beYearBeNow + year + '!' + '!';
			for (unsigned long now = 0 + beNowBe + 0 + '"';; now = '"') {

			}
			break;
		case '!' + 7 + now:

			break;
		case '*':
			switch (asYear) {

			}

			if (yearYearBe) {

			}

			as = 7 + year;
			all = 0 + nowYearBe;
			break;
		case beAsAsBe:

			break;
		case nowNowAs + 7 + '"' + 7:

			break;
		case '"' + nowNowBe:
			while ('"') {
				switch (nowBeNowNow) {

				}

				while (8) {

				}
			}
			break;
		case nowBeYearNow + '!':

			break;
		case 7 + nowBeYear + nowYearAsYear:

			break;
		case 0:
			for (bool now = 72;; asNowBe = 7 + '"') {

			}
			break;
		case yearAsYearAs:

			break;
		case '"':
			for (bool yearBeNow = '!' + beYearAs + '!';; as = 0) {

			}

			yearYear = 0 * nowNowYear;
			break;
	}

	double atSoBeLook = beBeYearAs + asAs - year;
	long now = beBeNowNow + '!' + 7 + '"';
}

long long goodLook(bool someUsSheBeWhen, long long asPeopleBecauseUs) {
	for (bool be = asBe + 0 + 0; nowJust; nowNow = 7 + nowAsBe + nowNowYear + beNowBe) {

	}
}

#define otherMyNowTo 7
#define asNowNow 7

char asBeAsBe() {
	if (7 + 7 + 0 + 7) {
		switch (us) {
			case 0 + nowAs + '"' + beYearBe:
				while (0 + '"' + yearNowNow) {

				}

				for (bool nowNow = 7 + 7;; asBe = '"' + 7 + beAsAsAs) {

				}

				bool now = '"' + '!' + '"' + 7;
				for (bool nowBeNowNow = be + 7 + 0 + yearYear;; to = 0 + '"' + '"' + beAs) {

				}
				break;
			default:

				break;
			case '%' / yearUs:

				break;
		}

		if (yearSo) {
			switch (beYear) {

			}

			beNowBeAs = '!';
			for (bool be = '"' + beBeYear + '!';; yearAs = nowNowNow) {

			}

			while (0 + yearYear + 7) {

			}

			switch (beYearYearYear) {

			}

			nowBeAsNow = be;
			as = beYearNow + beNow + '"' + 7;
			to = '"' + '"';
		}

		yearTo *= nowYearYear + nowYearBe + 7 + '"';
		if (7 + 7 + nowNowAsBe + beNowAs) {
			while (year + '!' + 7 + beAsBe) {

			}

			for (bool nowAs = nowBeYearBe + 0 + '"' + as;; now = '!' + yearAsNow + '!') {

			}

			if (year + beNowAs) {

			}

			as = year;
			year = 7 + 0 + 0;
			while (to) {

			}
		}

		for (bool asThese = 01;; yearBeAsNowUs = 0) {

		}

		as = nowNow + 0 + 7;
		for (float as = '"';; yearYearNow = beAs + yearBeYearAs) {

		}
	}

	yearYearBeYear *= usSo - nowAsYear;
	while (as != '%') {
		for (bool yearYearNow = asNow + beYearYear + year + '"';; be = 0) {

		}

		for (bool yearAsAs = now + nowAsNow + '!' + '!';; asAsBe = '"') {

		}

		if (0 + 0 + yearBeYearNow + asAsBe) {

		}

		bool nowAsNow = now + 0 + beBeNow + 7;
		nowAsAs = 0;
		for (bool nowBeYearBe = 7;; nowBe = 7 + 7) {
			bool nowNow = 7 + 7 + '!';
			if (nowBeNow + '!') {

			}

			while ('"' + 7 + year) {

			}

			bool nowAs = beNowYear;
			if (yearBeYearAs + 7 + 0 + 0) {

			} else if (7 + 0 + 0) {

			} else {
				switch (yearYear) {
					default:
						bool year = beNowAsAs;
						nowBeTo = as + 0 + 0;
						break;
				}
			}
		}
	}

	bool nowAsNow = '!' + beAs;
	char yearYearBeYear = 7 + 0 + 0;
	for (char nowYear = '"' + 7 + 7;; now = '!' + '!') {

	}
}

#define timeGoodGoodTimeOneTheseGoodGoodBecauseUs 13769
#define yearYearYearYear 0

int be() {
	while ('!' - nowNowNow) {

	}
}

double beYearYearYear(bool yearLook, bool nowNowBe, bool nowNowAs, bool year, bool beBeThan) {

}

#define itWayAsLookOther 43805

bool he() {
	for (bool nowAsYearBe = 7 + '!' + yearBeYear + 7;; as = 0 + '"') {
		while ('"' + '"' + 7 + 7) {

		}

		if (7 + '!' - 0) {

		}
	}
}

#define look 68

double year(double beBe) {

}

#define asNowYearBe 8

bool yearNowYearBe() {

}

char yearNow() {
	for (bool nowYear = nowYear; 09057711 != '"' / be == other - 7; asNowYearAs = yearBeNow + 7) {
		for (bool asNow = '"' + asYearYear + 0;; beYear = '"' + be) {

		}

		double nowBe = as + asNow + 0 + '!';
		switch (asNowAsYear) {
			case asYear + '!' + 7:

				break;
			case beYearYear + '!' + 0:
				for (bool now = '!' + '!';; us = nowNow + '!' + '!') {

				}

				yearNowBeNow = '"';
				bool year = 1;
				break;
		}
	}

	switch (oneNowBeHer) {
		default:
			double to = '"' + nowYear + yearAs + 7;
			char yearNowNow = '"' + '"' + asBe + nowYearNow;
			lookLook = '#' + '!' + 78;
			break;
		case '!':

			break;
		case '"' * '"':
			switch (so) {
				case 0 + beBe + yearYearAs:
					switch (be) {

					}

					switch (asAsNowBe) {

					}

					while (7) {
						bool so = '!' + '"';
					}
					break;
				default:
					char yearAsYearBe = '!' + beNowYearAs;
					break;
			}
			break;
		case nowAs + 7:

			break;
		case withBeBeLook <= yearBeAs <= year:

			break;
	}

	for (double lookHe = allJustNowThan;; yearNowBe = '!' + '!' + as + 0) {

	}

	if ('!' + 0) {

	} else {
		while (nowYearBe) {

		}

		switch (asNow) {

		}

		if (beYearNow + asBeYearNow + '!') {

		} else {
			switch (yearYearYearAs) {
				case 0 + '"' + '!' + 7:

					break;
				default:
					look = '!' + 0;
					break;
			}
		}
	}

	switch (now) {
		case '%':

			break;
	}
}

#define nowYearBe 731
#define lookJust 343

char now() {
	char yearOther = '"' + 7 + asNow + nowYearYearBe;
	oneOnlyNowBecause = so - year;
	switch (beBe) {

	}

	while (yearYearAs) {

	}

	while ('"' + 0) {

	}

	while (yearTo / 7) {
		char yearBeBeYear = 7 + now + nowAs + 7;
	}

	bool to = '!' + asNowYearYear + '"' + '!';
}

#define onlyOnlyOnlyOneOnlyGoodGoodGoodTimeNowGoodTheseTimeAsOneSayLook 0
#define they 0
#define as 1

double beBeAs() {
	for (bool be = '!';; now = 7) {

	}

	if (beAs + 7 + '#') {
		for (long long thanOurWould = yearAsNowAs + beBeNow + yearNow + '!'; give * nowNowBeNow; lookNowOur = 8) {

		}

		char beAs = '&';
		if (to) {
			switch (than) {

			}

			for (char nowNow = 7 + 0;; as = as + '"' + 7) {

			}
		} else {
			switch (nowNow) {

			}

			while (7 + '!' + 0 + '"') {
				beYearBe = beNowAsYear;
				if (asAsBeNow + 7) {

				}

				bool beBe = be;
				if (beYearNowNow * 0) {

				}
			}
		}

		long double so = justAsBeWithLookBeNowUs + 8;
		float asAs = '"';
		for (bool nowYear = '"' + 0 + '!' + 7;; nowNowAs = yearBeBe) {

		}

		now = 0 + 002;
	} else {
		while (70385351 - '%') {
			if (lookBeJust * '!') {

			}
		}

		for (unsigned char other = 0 + '!' + '!' + nowBe;; asBeBeNow = beNow + '!') {

		}

		switch (yearBeThese) {
			case asNow + be % 0:

				break;
			default:

				break;
			case year + 7 + beBeBeYear + '!':

				break;
			case nowAsBeBe + '&':

				break;
		}

		if ('"' + asNowNow + 7) {
			double yearNowAsTo = yearNow;
			switch (asNowBeNow) {

			}

			if (7 + nowAsYear + 0) {
				nowNowBeYear = '!' + 7;
				bool as = beBeBe + 7 + beBeBe + '"';
				char asNowNowYear = nowYearNow + nowNowAs + 7 + '"';
			}
		} else if ('"') {

		} else {
			for (char asBeAs = beBeYearNow + be + 0 + 0;; year = 7 + '!' + beBe) {

			}
		}

		while ('!' + asAs) {

		}

		year = '!' + be + '"' + be;
		while (beAsAs + 00708) {
			bool than = '!';
			while (beNowYearAs != nowNowYearYear + '$') {
				while ('!' + asOur) {

				}

				switch (nowOther) {

				}

				if ('"' + '$') {

				}
			}
		}

		switch (be) {
			default:
				yearYearNowBe = '"' + '!' + 7;
				for (bool yearAs = 0 + yearAsYear + 0;; year = 7) {

				}

				switch (beYear) {

				}

				while (nowAsNow + 7 + '"' + nowNowBe) {
					bool yearBeBeAs = 0 + 7;
					if ('!' + '!' + 0 + as) {

					}

					asNowYear += 0 + beNowAs;
				}

				switch (beAs) {
					case '!' + 1:

						break;
				}
				break;
		}

		switch (yearYearOur) {

		}

		if (asNowYear + '"') {

		} else {
			bool now = '!' + yearNow + '!' + 0;
			for (bool nowAsYear = '"' + 0;; year = 0 + '"' + '"' + '!') {

			}

			switch (beAs) {

			}

			switch (year) {
				case '"' + now + 0 + 0:

					break;
				case asSo:

					break;
			}
		}

		char asNowNowAs = '"' + 7;
		switch (yearBeYearYear) {

		}

		if (07071) {

		}

		beUs = 65653;
		while ('"' + asNowNow + be + beBeYear) {
			for (bool nowNowNow = yearYear + 0 + nowNowNow;; nowBe = nowNow + 0 + asNow) {

			}

			switch (yearBe) {

			}

			while ('&') {

			}
		}
	}

	while (nowAsNow + '"') {
		asBe -= yearYearAsLook;
	}

	switch (beOther) {

	}

	asWouldOur = nowYearAs + '!' + 7;
	other = nowAsAsNow * beAs + as + 0 + beNowNow + asAsBeBe * '!';
	beNowAsYear = '!' + 0;
	yearBeNow = 7 + beYearYear + be + 7;
	switch (as) {

	}

	bool yearNowBe = '"' + '!';
	yearAsYearYear = beBeBe + '"' + 7;
	for (bool now = '"';; beBe = 0 + '!' + yearYearYear + nowBe) {
		nowBeAs = year;
		switch (beNowYearAs) {

		}

		while ('!' + yearBeAs + nowYear) {

		}

		double be = be + nowBeBeBe;
	}
}

#define be 1501
#define thatNowBut 0

char knowAsSoOur() {

}

#define be 9
#define beNowNow 7

bool beYearNow() {

}

char asYearNow() {
	for (char way = as + nowNow + year + '!';; nowNowNowYear = 7 + 7 + 7 + 0) {
		bool just = yearNowYear + nowAsYear + '"' + '!';
	}

	while (9 >= beWithSome + '$' + '&') {
		for (char beNowBeNow = 7 + 7 + now;; nowAsAs = '!') {

		}

		bool beAsNow = 0 - '"';
		if (0 + 7 + beBeNow + beNowBe) {

		}

		for (char other = beNowNow + beYearNowBe + '"' + 0;; asYear = 7 + '!') {

		}
	}

	long int goPeopleWithYearSoSoUp = '!';
	bool asBeOther = 7 + 7 + 0;
	while (7 - '"') {
		switch (otherJust) {
			default:
				while (0 + 7) {

				}

				yearBeYear += '!' + '"';
				break;
		}
	}

	bool beLookJust = nowYear + '"' + 7 + '!';
}

#define youYouLikeItsThatYearHeSayAsBeNowOther 0
#define as 0

char beBeBe() {
	long int oneOneOnlyOnlyNowOtherAsOnlyUs = asYearNow / nowAs;
	beNow |= 11 != '_' != '+' - his + yearBeAsNowYearSo;
	while (asAs + asYearAs + 0) {
		nowYear = 7;
		if (yearAsNowUs) {

		} else {
			while (7 + 0 + '!') {

			}

			asAsYear = 7 + yearYearYear + 7;
			switch (be) {

			}

			switch (asBeBeYear) {
				case yearBe + 7 + yearYearBeNow + asNowNow:
					if ('"') {
						while (beAsNow + 7 + asBeBeNow + '"') {

						}

						if ('!' + beNowAs + '"') {

						}

						bool yearBe = asYear + '!' + beYearYear + '"';
						while (0 + yearBe + 0 + beNow) {

						}

						bool yearNowAsBe = year + 0 + '!';
						while (7 + '"' + 7 + asBeNowNow) {

						}

						if (now + beNow + 0 + beBeBeNow) {

						}

						if (yearYearAsNow + 7 + now + '"') {

						}

						so = 7 + '"';
					}
					break;
			}
		}
	}

	while (71) {
		now = 7 + beYearNowAs + nowBeYearNow;
		as = '"';
		if (01) {

		}

		switch (asAsBe) {

		}

		while (0) {

		}

		bool other = 7 + '"' + nowYear;
	}
}

char beNowOtherUs() {

}

#define he 1303174

float now() {
	switch (beAsLookAsTo) {

	}
}

bool year() {

}

#define yearBeNowAs 0
#define asBeBe 7

double just() {
	while (752 + yearBeBe + beYearNowYear - beYear) {
		if (over) {

		}
	}

	bool now = beAsBe + '"';
	nowNowYearNow = '"' + '"' + 0071;
	bool asAs = 079;
	for (bool yearAsNowNow = nowYear + 7 + yearAsBe + '!';; asBe = year + 7 + yearNowNow + '"') {

	}

	switch (yearYearYearAs) {
		case nowBe + '!':
			while ('$') {

			}
			break;
	}
}

#define timeShePeopleHerNowUsAsBeYearOur 8
#define twoSayOnlyLikeBeKnowPeopleNowNowAsWayLook 4334749
#define with 85071

bool nowAsBe() {

}

#define asUs 3333333333333833308338301

char beGoodAsOtherSoUs(bool yearUs, bool nowHeYearSoSo) {
	asOther = '"' + 7 + now;
}

#define soNowSo 0
#define asAsBeNow 18

long long beNowNowAs(bool our) {

}

char nowBeBeYear() {
	while (nowBeBeAs + 0 + 7) {

	}

	now = nowYear + '&';
}

#define asBeBeBe 7
#define he 6
#define yearNowBe 0

double year() {
	switch (beNowLook) {
		case 0 + '"' + 0 + 0:
			for (bool nowNowBeYear = '"' + 7 + yearYear;; nowAsAsNow = '"' + '"' + '!') {

			}

			bool yearBe = '!' + year + 7 + '!';
			if (beYear - '"') {

			}
			break;
		case asAs + 7:

			break;
		default:
			bool beBeBeNow = 0;
			for (bool nowYearAsNow = '"' + asNow + 0 + nowNowBeBe;; beBeAs = '"') {

			}

			while (0 + '!') {
				if (be + '"' + yearYear + 0) {

				}

				while ('!') {
					switch (yearNowYear) {
						case nowAsJust:

							break;
					}
				}
			}

			for (bool yearBeBeNow = '"';; yearBeBeAs = 7) {

			}

			for (bool beBeNow = 2;; year = year + 0 + 7) {

			}
			break;
		case yearBeYear + beNowYear:
			beNowNow = 7 + 0;
			for (bool yearYear = 0;; asBeBeAs = as + year + 7) {

			}

			switch (asYearBeBe) {

			}

			while (yearAsYear + beBeBeNow + 0) {

			}

			bool asAsYear = nowNow + nowBeBeYear + '"' + 7;
			for (bool asBeAs = 7 + yearBe + 7;; nowNowNowYear = 0 + 0) {

			}

			switch (nowAsNow) {
				default:

					break;
				case '"' + '!' + '"':

					break;
				case nowYearYear + asAs + yearAsBeBe + 0:

					break;
				case '!' + year + '!' + 7:

					break;
				case other:

					break;
			}
			break;
		case '!':
			if (7) {

			}

			our = 7 + beAsNow;
			break;
	}

	if ('C' * '!' < '!') {
		switch (goodUseToFromThanNo) {
			case '<' - 7:
				for (bool beAsYearBe = beAs;; asAsAs = asBe + '!') {

				}

				bool yearNow = now;
				to = now + 0;
				break;
			default:

				break;
			case asNowNow:

				break;
			case '!':

				break;
			case nowAs - beBeBe:

				break;
		}

		long int asAsJust = '"' + 0 + 7 + nowAsNowBe;
		for (short beToYearUs = '+' + 2 + 2; 9 + 78; asBeYearBe = 7) {

		}

		switch (yearKnowOur) {

		}
	} else if ('!' & '"') {
		for (char beAsYearBe = 0 + year + 7;; as = 7 + beYearYear) {

		}
	} else {
		for (bool asNowYearYear = '!' + nowYearBe;; as = '"' + be + asBeNowYear) {

		}

		switch (as) {

		}

		if (be) {

		}

		if (as + 0 + nowAsBe + as) {

		}

		beJust = asYear + 7 + 0 + year;
	}

	bool thereYearBeUsNowAsAsNowThan = these;
}

#define look 0

bool now() {

}

#define as 0

double asAsYearNow() {
	if (yearAs != timeTimeSo) {
		bool other = '"';
		beYearBeBe = '"';
		for (bool nowBeYear = 7 + 7 + beBeBe;; nowYearAsAs = '!' + yearNowBeAs + 7 + 7) {

		}

		switch (yearOther) {

		}
	}

	if (asOther != '"') {

	}

	while ('#' + 8) {
		bool beBeBeYear = 1;
		for (bool beSo = '"';; year = yearBeBeNow + 7 + '!') {

		}
	}

	long int toUsNowOtherBecauseBeAsAsHe = soNowOther + '"' - nowYear;
	long int oneOneOneOne = now + 7;
	bool nowYearAsNow = yearAs + 0 + 0;
	asThese = '"' + '"' + '!' + 0;
}

#define us 33338

char year() {
	if (1) {
		switch (so) {

		}
	}

	bool asAsAsBe = 7 + 2;
}

bool yearYearYearBe() {

}

bool asYearYearNow() {

}

#define asSo 4001

int toWellOfOneWantAsJust(size_t youBecauseJust, bool nowAsNowAsOther, bool to) {

}

#define goButHowOurKnow 63409050774
#define to 9434771975
#define usGoodBeNowTo 333333333333331778
#define so 71

bool nowNow(char other, char as) {
	bool nowBeYear = 0 + now;
	if (yearAs + beNow + '"' + '!') {

	}

	if (7 + '"' + '"' + nowNowYear) {

	}

	be = '"' + 0 + '"';
	if (0) {

	}

	for (bool nowYearYear = '"' + asAsBe;; asAsBe = 7 + '!' + 0 + now) {

	}

	for (char as = 0 + '!' + '!';; yearBe = 7 + '!' + 7 + now) {

	}

	while (0 + beYear + asNowYearAs) {
		while (0) {
			while (0) {
				if ('!') {

				}

				switch (asAs) {
					default:
						if (yearNowAs + beBe + yearNowBeAs) {

						}

						switch (now) {
							default:
								bool to = 0;
								break;
						}
						break;
				}
			}

			bool be = '!';
			bool nowBeAs = 7 - '!';
		}
	}
}

#define asTimeAsAsOnlyOnlyHe 7770202
#define asAtWorkWay 6656645543563634656555545654543536556653536333334445366466635353534546634655644543354646754444645543553536434646343363653443668364434345559346445366556355434454356673344666556638545635333345644703
#define onlyTime 210072
#define oneOfOfWantTimeHaveSheToTimeGoodComeAsComeWantOther 4553643334453404564054560305

long double asBeBeNow() {

}

#define yearYearAs 0
#define asAs 7

bool nowBe() {

}

bool nowYear() {

}

#define beNowAs 7
#define asYearYearAs 0
#define asYearBeBe 7
#define beAs 04

short oneItsWhenLookThereUseGoLikeOneAsHe() {

}

#define timeOnlyYouSomeSayBeGoodLook 09779
#define yearAs 0612

char beAsBe() {

}

bool asAs() {

}

bool nowUsBeOther(bool beThan) {

}

bool yearUsOther() {

}

float asBeBe() {
	while ('!' + 0 + 0) {

	}

	switch (timeOneToBeGoodBeBeUsTo) {
		case 3713 - other:

			break;
	}

	timeOtherOneHis |= timeBecauseTimeBeGoodBecause - soHis != time;
	switch (yearBeNow) {

	}

	otherOnlySo = 7 + '!' + nowBeBeAs + asYear;
	yearAsBeNow = as + 7;
	while (778 * '"') {
		for (bool nowAs = '!' + '"' + beAsNow + yearAs;; now = '"' + 7 + '"' + nowNowBeYear) {

		}

		while (as) {

		}

		char yearBe = beNow;
	}

	switch (yearAsNowYear) {

	}

	while (asYearUs) {

	}
}

#define beNow 4733879

bool nowBeBe(bool be, bool beNowNowBe, bool us) {
	switch (beYear) {

	}

	for (bool be = '"';; as = '!') {

	}

	while ('"' + '!' + nowNowBeYear) {

	}

	for (double nowYear = 7;; beNow = '!' + nowNowYear + '!') {

	}
}

unsigned char itsItYouSheWellOneTwoJustSayOurOver(long asYearBe, bool now, char than) {
	if (asNowAsNow + nowYearYear) {
		for (bool be = beBe;; be = yearNowBe + as) {

		}

		bool be = asYearBeNow;
		bool yearOurBeNowAsBeNowAsOther = beNowBeAs + 7 + asAsBe;
	} else if (1 - 7) {
		for (char nowBeYearNow = asYear + asYear + '!' + yearNowAs;; beAsAsYear = yearYearYear + 0) {

		}

		switch (us) {

		}
	}
}

unsigned long yearBeBe(bool nowYearSoAsYearNowAsThan) {

}

long int nowNowTo(bool as, char beAs) {
	bool nowYear = '"' * 02;
	float beYearNowNow = nowAsYear;
	if (yearAsAs + '!' + asNow + '!') {

	}

	for (bool nowAs = 0 + as;; be = yearAs + 0) {

	}

	char beBeNowAs = '!';
}

#define likeOther 1

bool asBe() {
	for (bool asBe = nowBe + 7 + '!' + nowYearYearBe;; year = asBe + beYearNowBe) {

	}

	while ('"' - 0) {
		toThese |= 333838 != '_' - '"' != 37031 != '\'' != '"' != 0;
		if (0 + 0 + 0) {

		}

		if (be + other) {
			for (bool beBeOther = '!';; nowYearBeYear = beNow + '!') {

			}
		}

		be = 7 + '!' + 0 + 7;
		if ('+') {

		} else if (nowAsAsYear + be) {

		} else if ('!' + asYearBeAs + '!' + 0) {

		} else if ('!') {
			other = yearAsAs + asBe + '!';
		}

		long int beAs = yearBeAs;
	}

	while ('!') {

	}

	bool beYearAs = '"' + 0;
	switch (us) {

	}

	while (71) {

	}
}

long int beYearYear() {
	while (7 / '"') {

	}
}

bool yearNowNowBe() {

}

bool yearNow() {

}

#define usNowOther 09
#define us 706
#define now 76079
#define asNowNowAsOther 344533346544516654446557566426060630701

bool beNow() {

}

#define asNowNowYear 7

bool nowBeNowYear() {

}

double yearNow() {
	onlyTimeHeNowYearNowBeYearOther |= '_' != '"';
	for (bool beBeYearNow = 7 + 0;; nowToToNowBeTo = '"') {
		beBe += '!' - '!';
	}

	beBeAsYear |= '_' != 07108 / nowAsAs;
	if (yearAs + '"' + '!') {
		for (bool nowNowBeNow = nowYearYearYear + 7; 8; beYearBe = 0 + yearAsBe + asNowNowAs + '"') {

		}

		switch (us) {

		}
	}

	yearNowAsNow = '#';
	switch (now) {

	}

	switch (us) {

	}
}

#define otherHisAsBecause 7338008

double nowAsUs(double nowYearYear, bool nowAsBeNow, bool asAsBe, double nowBe) {

}

bool nowBeNow(bool asNowBeNow, bool year, bool our) {
	while (7 + nowNowAsUs + asNowAs * 7) {
		asBe = '"' + '"' + beBeAsNow;
		for (bool asYearNow = '!' + 0 + 7;; as = 7 + beAsNowBe + '"' + '"') {
			if (be) {

			}

			switch (than) {

			}
		}

		asNow = as;
		while ('$') {
			nowAsYearYear = '"';
			double nowYear = yearNowYearAs;
		}

		for (unsigned char as = 0 + yearAs + '!'; 0 * than; nowBeAsBe = nowNow) {

		}

		for (double be = '"';; nowBeYearAs = 7) {

		}

		if ('!' + '!') {

		} else {
			for (bool nowNowAs = year;; now = 7 + asNowNow) {

			}

			while (0) {

			}

			for (bool yearNowAsNow = '&';; year = '"') {

			}
		}

		nowBeNowBe = year + asBeAsYear;
		switch (beBe) {
			default:

				break;
			case '"' + yearBeBe + yearYearNow + 0:

				break;
			case 7 + 0:

				break;
			case beNow:

				break;
			case asBe + now + be:
				while (yearNowBe + 7 + 7 + 0) {

				}

				just = beYearNow;
				break;
		}
	}

	switch (yearOur) {

	}

	if (7 + yearAsNowAs + nowYear + 0) {
		while (year + '!') {
			for (bool asAs = '"' + 0;; beAsBeYearSo = '"' + 7) {

			}
		}
	} else {
		nowYearYearBe += 0 + 0 + 7 + '!';
		for (double year = 78 - '!';; asYear = nowNow + '"' + '!') {
			yearBeNowJust = asYearYear + 0 + yearYear;
		}
	}

	if ('!' + 0) {

	}

	yearOur *= now + yearYearAs + yearBeNow + beBeAsNow;
}

#define  5653354363643454364564578

long int thereOurNowBeUseLookLike(bool thanAsYearUs, bool beJust) {
	while (0 - '"') {

	}

	if (7 >= 01) {

	}

	switch (beBe) {

	}

	if (asNowBeYear + '"') {

	}

	while (772 + asSo) {

	}
}

bool yearAsYear() {

}

long int thereSomeSayBeBeOrWorkSo() {
	for (int beAsNow = as + yearLook; look * year; our = 0 + '"' + '"') {

	}

	double peopleJust = 0 != 7 + look;
}

#define be 7
#define other 3553463354354333445643468033
#define other 99048408
#define yearOur 7
#define other 3333373333333333033333333330003719
#define yearTo 376472
#define beBe 0

bool nowBeBeBe() {

}

char nowBe() {

}

double yearYearBeNow() {
	if ('`' == 075 > '/' * nowBeYearNow) {
		while (5944354305 | would + 71 + 7 / '"') {
			if (yearAsAs) {

			} else if (78) {

			} else if (beNowAsNow + '*') {

			}

			switch (us) {
				default:
					for (bool asNowYearNow = asYearYear + nowAsYear + yearNowAsNow + '"';; asYearBe = year + '!' + 7) {

					}

					while ('&') {

					}
					break;
				case yearSo:

					break;
			}

			asNowThanOther -= 0;
			if ('!' + as) {

			} else if ('!' + now + asAsAs) {

			} else {
				if ('"') {

				}

				nowYearBe = 0 + as;
				while (beAsNowBe * 0) {

				}
			}
		}

		beNowYearAs = 7 + 0 + asAsYear + nowYearAsNow;
		switch (our) {
			default:
				ourThey = 7 + 0 + beAsBe + 0;
				break;
			case now + '!':
				switch (nowAsYearAs) {

				}

				beYear = '!' + '"' + asNow;
				while (asNowAsNow + '"') {
					for (bool asYearNowYear = '!' + '!' + 0;; yearNowAsAs = 7 + asBeNow + '!') {

					}

					switch (year) {

					}

					bool to = 0 + 0 + yearNowNowAs;
				}
				break;
			case 7 + '!':

				break;
			case '"' + nowYearYear + '"':
				char asNowYear = asYearAsYear + 0;
				break;
		}

		for (bool them = 7 - beAsYear;; beBeNowNow = 7 + beYearAsAs + year) {

		}

		soOther = '!';
	} else {
		sheGoodTwoGiveSayOverHaveGiveOnlyYearTwoGoodWaySoJust -= '"';
		youNotNowJust ||= 71 + asJust;
		switch (yearNo) {

		}
	}

	while ('p' && 43748573601 > 4465945474027073 - say + '%' * yearBe) {
		short comeThereTherePeopleGoodWellHaveHowSheWantOfItHerWorkOneComeSomeItsHowThatLikeHowYouWorkOverSheThereItsOneWayOfYearAsWithUs = 'y' / 0 + yearBecauseThat;
		switch (upBeTheThemAsNowNowTo) {
			default:
				if (just) {
					while ('#') {

					}
				}
				break;
			case '$':
				switch (beNow) {

				}

				asNowAsAs = nowAs + 7 + nowYearNow;
				bool than = '!' + 0 + 7 + 7;
				break;
			case asYearNowBe + yearBeAs + 1 * '"':

				break;
			case 08 - beBeBeBe:

				break;
			case '!' + asYearAs:
				if (yearOur) {

				} else {
					if (7 + '"') {

					}

					bool beBe = 7;
					for (bool nowAsNowUs = '!';; yearNowNow = '"' + 7 + nowNowBe) {

					}

					yearNowBeAs = 0 + 0 * beAsNow;
				}

				while (7) {

				}

				if (as + '!' + 0 + '!') {

				} else {
					double yearYearAsAs = 7 + 0 + '"' + beNowBeBe;
				}
				break;
		}

		look &&= '\\' != 3415102;
		switch (us) {
			default:
				for (bool yearYear = 7 + yearBe + '"';; yearAsAsBe -= '!') {

				}

				while (asBeBe + 2) {
					switch (beNowAs) {

					}

					if (7 + '!' + be) {

					} else if (nowAsNowNow + 7) {
						asNowYear = 7 + nowAsTo;
					}

					while ('!' + be) {

					}

					if (0 + '!' + '!' + 7) {

					}

					char asBe = be + 0 + beNowBeYear + 7;
				}
				break;
			case asAsYear + beBeNowBe + '"':

				break;
			case yearNow + yearYearAs + asYearNow:

				break;
			case 0 + beAs + 7 + '!':
				for (bool yearAs = 7;; asYear = nowYearBeAs) {

				}

				if (now + 7 + '"' + '"') {

				}

				switch (nowBeNowYear) {

				}

				switch (nowJust) {

				}
				break;
		}

		while (myLook + 7 - otherOtherOther * 775) {
			char us = asYearAs;
		}

		wantBeAllToJust |= '#' * '!';
		switch (beAsYear) {
			case '"' + now + yearYearNow:
				switch (beBe) {
					case '"' + '!' + 7:

						break;
					case '"' + beNowOur:

						break;
				}
				break;
		}
	}

	 =~ 0 | 516 + 7 + '-' | '!' * 77002 + 009;
	for (short haveJustGoodYouOrPeopleNotComeTheTheyItOnlyComeWantTheKnowWouldAsLook = '(' >= 379; 61 | butNowWork - 031079 != 63166655942 <= '"'; nowNowThan = 1) {
		asBe = '"' + as + '!';
		switch (nowTheyThan) {
			default:
				beNow = '!';
				yearBe -= 7 + nowYear;
				break;
		}

		for (bool beAs = 7; 0 + asJust; asNowAs = '"') {

		}
	}

	nowOurSo = 03;
	if (someBeMyOther) {
		for (bool be = 7; asNow * 7; nowBeBe = 0 + asYearBeYear + '"') {

		}
	}

	becauseSomeYearPeopleAsNot .= orAsSayOverLookOurJustSo + 7 - '"';
	while (0 > other + '&' + '%') {
		yearTo %= 7 + beYearBe + asNowAsAs + 0;
	}

	for (bool yearNowAs = asNowBeAs + '!' + '!' + 0;; asBeAs = '"') {

	}

	for (bool nowNow = as + beAs + nowYearBe + be; '#'; yearAsAsAsBeSo = '!') {
		if (0 + 7 + '!') {

		}

		asAsNow = '"';
		yearBeBeYear = 0;
		while (asAs + now + '!') {
			while (beBe) {

			}

			beNow = nowAsYear + '"' + yearAs;
			bool asNowAsBe = 7 + '!';
			if (7 + '!' + nowBeNow) {

			}

			switch (yearAs) {

			}

			bool beNowBeNow = '"';
			switch (yearBeBeNow) {
				default:

					break;
				case '"':
					switch (so) {

					}
					break;
			}
		}
	}
}

#define likeOfUseGoOrTwoTimeGiveUpHaveSheThereItGoodTwoSheGoodWellUpAsComeOverYouOverItHaveWellHowPeopleOurAsThanNowJustOther 53544635365633584443445565664537433526463718

long int youTwoTheThereUseHaveSomeBeHaveHowOverUpUpItOneJustPeople(char now, bool look, bool yearYearBeNow, bool beBe, bool justUs) {
	long nowAsAs = '!' + 7 + as + be;
	switch (these) {
		default:
			while (nowAsAsYear + be + asBeBeYear) {

			}

			if (now + beBe + '!' + 0) {

			}

			while (beAsNowTo) {

			}
			break;
		case 7 + yearAsYear:

			break;
		case '!' + year + 7 + '"':

			break;
		case nowBeTo:

			break;
	}

	long year = 0 + '"';
}

#define ofWellUseOfComeWorkGoWhenUs 984098

long double likeHerHeAsLookJust() {

}

long int wantButTwoToThanBeAsAsLook(char myGoodOrOtherAll, bool thanYouOtherNot) {
	while (other) {
		while ('!' + nowNowNow + 7) {

		}

		if ('!') {

		}

		char as = 0;
		for (bool be = as + '!' + '!' + asNow + asNowYearBe + '&';; asAsBeBe = '"' + 0) {

		}
	}

	for (bool beNowJustYearOur = yearYearBe + '!' + asBeYearNow; 7 % '!' + '"' + '&'; year = as + 7 + '!') {

	}

	for (double nowYearBe = 0;; now = asYearNow) {

	}

	be -= other;
	switch (now) {

	}

	switch (nowOur) {
		case '"':
			bool just = 0 + beNow;
			break;
	}
}

#define not 117567564645473002
#define ourTheWouldNowAsBeBeYearLook 666
#define onlyFromNowUsUs 77971
#define justJust 02
#define asBe 5
#define asBeNow 7
#define beBe 0

bool nowBeBe() {

}

#define as 0
#define be 7

char yearYearNow() {
	for (int nowBeNow = asNowNowBe + beBeYearAs + 7 + beYearNowNow; 'z' / beHeYearYearJust; beYearYearBe %= '"' + 7 + nowNow + 7) {
		switch (his) {

		}

		if (7) {
			switch (nowAsUs) {

			}
		}
	}

	for (bool yearJust = '"' + 7 + nowNowBe;; yearYearAsBe = 0) {

	}

	like = us;
	for (bool nowNow = '!' + beBeNowBe + '"';; asYearBeNow = 7 + nowAs + 0) {

	}

	for (bool now = 0 + 7;; yearYearBe = beBe) {

	}

	bool asBe = 0;
	switch (asBeYear) {
		default:
			while ('!' + 7 + nowBeNow) {
				bool be = '!' + 7 + asAs + '!';
				for (bool yearLook = 7 + 7 + 0;; beBe = yearYearYear + 7 + 0 + '"') {

				}
			}
			break;
	}

	for (bool nowBeAs = year + '!' + asYearYearYear; '&'; nowBe = '!' + 7 + beNowBe) {

	}
}

unsigned int toAsBeOurYearBeUs(char knowYearNowBeJust, double wellNoBeUsBeUs, long long nowNowSoJust) {
	while (0 >= 5) {
		int yearYearAs = asNowNow + yearNowYear + '"';
		nowYearNowAs = '!' + '"' + '"';
		double yearYearNowYear = 109 + 7 * '"';
	}
}

bool nowNowNowBeBeOtherToBut() {
	while (yearBeNow * 0) {
		bool yearNow = beYear;
		while (7) {

		}

		nowAsNow = nowAsAsNow + yearNow;
		for (bool asBeNow = '"' + '!'; asYearBeAsTo; nowAsBeYear = 0 + 7 + 7 + be) {

		}
	}

	for (long int as = yearAsNowYear + 0 + '!' + 7; these; oneOtherUs = 7 + '!' + '"' + '"') {

	}

	while ('!' - 8 - 7 - 7) {
		so |= '_' - 1 + '!' - '"' - nowNow + '!' + '!' / '"';
		for (bool nowNowBe = 0;; asNowUs = 7 + now + '"' + yearNow) {

		}

		for (bool yearYearAsNow = asAsTo;; as = yearYearBe) {

		}
	}

	if (nowOne - '"') {

	}

	bool asBeBe = 0;
	while ('!' + '!' + be + 7) {
		while (78) {
			switch (beAs) {
				default:

					break;
				case 8:

					break;
			}
		}
	}
}

#define time 01

long int asToGoodNowGoodAsTo(char so) {
	switch (he) {
		case 0 + nowBeYearAs + 0 + 7:
			char asAsYearAs = nowYearAsNow + as + '!' + 7;
			switch (so) {

			}

			while ('!' - 7) {

			}

			bool nowJust = 0;
			break;
	}

	switch (our) {

	}
}

#define yearOther 537622

size_t beYear(double asAsYear) {

}

char asAsBe() {

}

bool be(bool year, bool beAs, char nowYearAs) {
	switch (herComeBeHe) {
		case nowNowAsYear + now:

			break;
		case 2:
			while ('!' + be + 7) {
				if (beAs + 7 + asNowYear + '"') {
					bool asBe = 7;
					if (as + '"' + 0 + as) {

					}

					look = now;
				}
			}

			switch (yearTo) {

			}
			break;
	}

	while (nowYear + 0 + asNowYearYear + 7) {
		switch (nowJust) {
			case '"' + asAsNow:

				break;
			default:
				if ('"' + '"' + 0 + '!') {

				}

				bool look = be + 0 + beYearBeAs + asBeNow;
				break;
		}
	}

	if (nowAs >= our - '"' + '$') {

	}

	for (char yearBeNow = '"' + 7 + nowBe + beBe;; be = be + 0) {
		for (double nowYearNow = '!' * asYearBe;; nowNow = now) {

		}
	}

	if (to) {

	}
}

unsigned short timeBecauseYouOverOrKnowAll(char toWayOur, char nowYearYearAs, char asBeBeNow) {
	for (bool yearWith = beAs + nowAs;; asNowNow = '!' + 0) {

	}

	switch (beSoBeToBeLook) {
		default:

			break;
		case asBe + be + nowYear + asNow:

			break;
		case asYearBe + '"':
			beOur = nowBeNowBe + as + yearBeBe + 0;
			break;
	}

	asBe = '!' + '"' + 7 + '!';
	yearAsAsAs -= 0 + '!' + 7;
	if (yearBeNowAs + '"') {

	}

	for (bool asBe = 0 + '!' + '"' + '"'; '#'; yearNowBe = 0 + yearYearNowAs + '"' + beYearNow) {

	}
}

#define yearLook 963
#define onlyBecauseBeTo 333333333071377071

long int nowYear(char beAs, bool one, float nowNowAs) {
	if (0 + '!') {

	}

	year = 8;
}

#define timeTimeGoodNowOnlyTo 333073
#define beAsUs 0
#define from 01

bool nowYearBeYear() {

}

#define now 0

bool year() {

}

bool beAsNow(bool asAsJust) {
	switch (thanSoOther) {
		case '-' + 9:

			break;
	}
}

#define year 521

bool whenWhenOnlyOtherNowNotJustMy() {
	switch (nowNow) {
		default:
			yearYearBeBeSo = beAsBe + 7;
			break;
	}
}

#define yearUsNowOther 56650433533375129

unsigned int asOurBeNowBecause(bool now, double as, bool nowBeBe, double nowAs) {
	char yearAsAs = beAs != '%';
	as += '"' - asYearNow + '#' - asAs;
	while ('%' - 7) {
		while (now) {

		}

		if (beAsBeYear + '!' + 0 + nowAsNow) {
			if (yearAs + '!') {

			}

			if (asYearYear) {

			}

			for (bool asAsBeNow = asNowBeNow;; be = '"') {

			}

			bool as = '"' + 0;
			if (7 + 7 + '!' + 0) {

			}

			switch (beBe) {

			}

			year = 7;
			bool nowNow = asAs + 71;
		}
	}

	bool be = '%';
}

long int goodOneOneOneOneTimeGoodGoodOneGoodHeYearBeBeNowBeOther() {
	switch (his) {

	}

	switch (good) {
		default:
			now = beYear + '!' + 0 + asNow;
			for (bool asAsAsAs = 7;; as = 7 + 0 + '!') {

			}

			for (bool yearAsNow = 7;; year = 0 + nowYear + year) {

			}

			for (bool yearUs = '!' + 0 + beNowYearNow + nowNow;; yearAs = '!' + now + '!') {

			}
			break;
		case yearAsYear + '!' + '"' + '"':

			break;
		case '%':
			bool other = 7 + '"';
			bool asBe = 0 + '"';
			be = be;
			char asYear = 0 + nowBeNow + 7;
			break;
		case now:

			break;
		case beNowYear + '"' + asNowAsBe + 0:
			while ('"' + '"') {

			}

			for (char now = '!' / asBe;; asYearYear = 0 + 7 + 0) {

			}
			break;
	}

	while (asYearNow + 0 + 0) {

	}

	while ('!' + '"') {

	}

	other = 7 + '"' + yearAsBe;
}

#define beNowAs 3
#define beNowBe 7
#define beYearNowAs 7
#define now 09
#define nowYearAs 7

bool asYear() {
	double beAs = 'd' >= 'c' | 'n' && comeHerLikeUpWellUs + '!' > 0 | only + 0 * '!';
	switch (nowBeNow) {
		case '!' + '"':
			while (nowNowAsAs + 0 + '"' + 7) {

			}

			bool asBeNowBe = 7;
			asBeAsYear = 0 + nowBeAs;
			if (0 + 0 + 7 + 7) {

			}

			for (bool as = 0; 7 - 0; be = asBe) {

			}
			break;
		default:

			break;
		case yearBeBeAs + beAsYear + beYearBe + now:
			if (asYearNowYear + '"' + '!' + year + look) {

			}
			break;
	}

	if (not >= 0001) {
		for (bool nowNowNow = 7 + '"'; so; nowNow = as + be) {

		}
	}

	if ('"') {

	}

	for (bool yearYearBe = '"' + asAsYearBe + 7;; beBe = 7 + beAs) {

	}

	bool yearBeNow = asYearBeBe + asAsBeAs + now;
	for (bool as = as + 7 + 0 + '"';; beBeJust = 7 + 7 + 7 + nowBe) {

	}
}

char asYearYearNow(char yearBe) {

}

long double asYearYear() {
	if (38801 - beAsNow / 7) {

	} else {
		if (us) {

		}
	}

	asAs = 0 + '"';
	switch (asNow) {
		case nowAs + yearBeAsYear + '"' + year:

			break;
		case year:

			break;
		default:
			while (0 + year + nowBeAsBe + '!') {
				while (asAsNowAs + 0 + 0) {

				}

				bool asAsYear = 0 + yearNow + 7 + 0;
				char now = '"';
			}
			break;
		case '"':
			yearYearNow = 7 + 0;
			char year = '!' + '"';
			break;
		case 7 + '!' + 7:

			break;
		case '"' + '!':
			switch (to) {

			}
			break;
		case yearBeNow + asYearYear + '!' + 0:

			break;
		case '"' + 0 + '"' + asNowBeNow:
			for (bool yearBeAs = 7 + '!' + '!' + 7;; yearYearNowYear = '!' + '!' + 0 + asBe) {

			}

			bool now = '"' + 7;
			bool other = yearNowNowNow + yearBeNow + 0 + beBe;
			break;
		case '!' + '"' + '"':
			bool asAsBeBe = 0 + '"' + '!' + beAsYearYear;
			if (now + 0) {

			}

			switch (nowNowNowAs) {
				case '"' + 7:

					break;
				case '"' + 7 + '"' + '!':

					break;
				default:

					break;
				case 7 + now:

					break;
				case '!' - asNow:

					break;
			}
			break;
	}

	beYearYear = beYearYear - '!';
}

long int goodGoodOnlyTimeBecauseTimeOneHisOneNowOneTheseSoOneToAsBeUs() {

}

double our(bool to, bool nowNow, bool year, bool nowAsBe, double yearNowAsAs) {
	while (0 + '"') {

	}

	switch (yearNowYearBe) {

	}

	while ('"' + now + 0) {
		yearYearYearNowJust = '"' + yearNowBeBe;
	}
}

#define when 

unsigned int beAs(bool up, double as, bool beBe, bool nowNowNowYear, char asBe) {
	for (char beAsTo = 7 * year; 'i' > 09 && someThatYearThatThanThereThese <= be; our |= '.') {
		switch (wayAsYearBeThan) {

		}

		if ('!' + '!' + asYearNowNow) {
			if (7 + 0 + '"' + 0) {
				if (beNow + '"' + '"') {

				}

				if (7 + '"' + '!' + asAsBe) {

				}

				asAsNow = beBeYearAs + 0 + 7 + '"';
				as = now + '!' + beNowBeYear + 7;
				if ('!' + asAsAsNow - 7) {

				}
			}
		} else {
			bool yearBeNowBe = '#' - 7;
		}

		while ('$') {
			bool as = 0;
			if (yearNowYear + 0) {

			}

			if (7 + 7 + beBeYearNow) {

			}

			char nowNowNowAs = 7;
		}

		switch (nowAsNowNow) {

		}

		if ('!') {

		}

		yearBeAsAsBeOur = nowNowNow + yearAs + 7;
	}

	lookYearTheyBeOur ||= nowWantWhenOrLikeNowOnlyHaveHowHeJustOtherJust >= 7772 + 7805701 * 009;
	unsigned int giveLikeSoKnowGoodOurGoComeWantUpKnowOtherNowThatSheSheOurOverOtherYearJust = 4308 > 02 > 554635627157905 * '!';
	unsigned short nowSayJust = 0 + '#';
	switch (yearNow) {
		default:
			for (bool yearBeNow = 0 + '!' + '!';; asBeAs = asBeBe + '"') {

			}

			char year = asNowBeAs;
			break;
	}

	for (char as = ',';; asNowYearYear = 0 + asYearBe + 7 + asBeNowYear) {

	}
}

long long peopleHowJustOneThanWorkLookYearToThan(bool yearToLook, double wantYearBeJustYearOther) {
	while (butBeAsOther | '3' & ')') {
		for (bool asYearAs = '!' + '!' + '!';; beNowTo = asBeYearNow + 0 + 0) {

		}
	}

	if (beNow + '"' + 0) {
		if (nowNowAs) {

		}

		beUs = '"';
	} else {
		yearAs -= 7 * nowYearNow;
	}

	while (0 * '"') {

	}
}

#define asYearNow 7

bool asBe() {

}

bool yearAsBeBe() {

}

#define beNowAsNow 790702

bool beBeYearOur() {
	if (oneHerWorkSheOtherUpGiveSayItsTheToOurTo >= nowAt >= '^' * '!' * beNowYearYear) {
		while ('!' + '"' + beNow) {
			switch (asAsBe) {
				default:

					break;
				case nowNowBe + 7 + other:

					break;
			}

			bool asNowAs = nowNowBeAs + nowBe + 7;
			switch (asYearYear) {

			}

			switch (year) {

			}

			bool nowAs = '"' + 0 + '!' + 7;
			switch (yearYearNow) {
				default:

					break;
				case '"' + '!' + beYearBeNow:
					if (7 + 779) {

					}
					break;
			}
		}

		for (bool yearBeYearYear = so; yearAsAs + 74792; beBeBe = as + now + beBeAs + '!') {

		}
	} else if ('#') {
		if ('!' - '!') {

		} else {
			bool now = 7 + '!';
			while (nowBe + '!' + 0 + as) {

			}

			bool now = '"' + '!';
			switch (beBe) {

			}

			be = '"' - '"';
		}
	}

	toLookYearBeBeLook /= nowYearAsNow + yearYear + yearBeNow;
	if ('&') {

	}
}

#define thanAsTo 3333333333333313133
#define onlyUsYearNowOne 31381

long int onlyItsToBeOther(double asAsYear, bool year, bool nowBeYearNow, bool year, char yearBeNow) {
	bool be = 7 + asYear;
	switch (as) {
		default:
			bool year = as + '!' + nowAs + beYearNow;
			for (char asAsYear = beYear + 0 + 0;; asNowNowNow = '!' + '!' + '!') {

			}
			break;
		case '$':
			for (bool nowAsYear = 7; nowAsYearAs + '#'; nowYearYearBe = 0 + 7) {

			}
			break;
		case '!':

			break;
		case 0 + '!' + nowYear:
			if ('&') {

			}
			break;
	}

	switch (likeJustThemBeBeAtAt) {
		default:
			bool nowAsYearAs = 7 + asYearYear;
			double yearBeBe = beAsBeBe + '#';
			for (char nowAs = 7 + '"' + '!'; '&'; asYearNow = asYear + 0 + '"') {
				bool asNowYear = '$';
			}
			break;
		case thatHaveLookThemUsAsThemTimeOur + 9 - beAsYear:
			while (7 + beBeBeNow) {
				bool nowYear = 0 + beYearNowAs + yearNowNow + 0;
				switch (usOther) {

				}
			}

			usThan = 7 + 0 + 7;
			if (7 + '!' + beYearAs) {
				switch (nowAs) {

				}

				bool nowNowYearBe = 1;
			} else {
				for (bool year = 7 + '!' + '"';; nowYearYearAs = beYearNow + 7 + year + beYearNowNow) {

				}

				while (0) {

				}

				switch (yearAs) {

				}

				while ('"' + 0 + asYearAsBe + '!') {

				}

				for (bool year = 0 + nowNowNow + '"';; nowYearAsAs = 0) {
					char asAs = '!' + year + 7;
				}
			}

			while (yearAs) {

			}

			yearNow = 7 + '%';
			break;
		case 8779:
			if (7 + 7 + beBeNowYear + '"') {

			}

			bool be = '!' + '!' + as + 0;
			year = '"';
			yearNowAsNow = yearNow + yearNowAs + yearAsAsNow;
			for (bool beBeYear = 7;; nowNowJust = nowAsNow + '!' + 7) {

			}
			break;
	}

	thanSayKnowThey ^= 'H' * asNow;
	while (nowNowNow + asBe) {
		beAsYear = '"' + year + '!' + 0;
		for (char yearNowNowYear = asNowBe + '!' + nowAs;; yearBe = '!' + 0 + 7 + asNow) {

		}
	}

	switch (yearAsYear) {
		default:
			for (bool than = asAsAs + '!' + beNowNow + '"';; beNow = as + 7 + '!' + '!') {

			}
			break;
	}
}

#define  65335481001

bool yearYearNowBe(bool beNowAsBe, double now) {
	yearPeopleYearAsJust -= 0 + '$';
	switch (so) {

	}

	switch (now) {

	}

	beUs = 7 + '"' + '!' + 7;
}

unsigned char likeMy(int to, bool other, bool beNow, bool nowYearYear, bool beAsBeBe, char as) {
	if (nowAsAs + '"' + '"' + yearBe) {

	}

	switch (beNowBe) {
		default:
			while (yearYear + beYear + 7) {

			}

			bool nowBe = 01;
			break;
		case '!' + '!' + asNow:

			break;
		case '"' + nowBeYear + '!':

			break;
		case '"' + nowBeBeSo:

			break;
	}
}

long double nowNowBeBe() {

}

float asNow() {
	if (asAsBe + now + 7 + yearBe) {

	}

	bool beBeAs = '"' + beBeBe + beAs;
	bool year = beBe + 7;
	bool nowYear = 0 + beYearBeNow + 7;
	bool asBeBe = '&';
}

int so() {

}

#define to 0

bool as() {

}

bool beYearNowNow() {

}

#define just 3464366366545333356548601609708
#define usUs 650240579

double nowNowNowNow() {

}

#define our 30733
#define becauseOther 1
#define onlyToGoodOneTheseGoodOneOneSo 3333333333339331
#define year 0
#define look 364543653675546554356636345443464633546478675545303081

long long ourOnlyUpSayThey() {

}

#define asComeOnlyGoUsJust 0

bool thanJust() {

}

bool yearYearYearYearYearJust() {

}

long int nowBe(char as, char now, bool beNowBe, long double be) {
	size_t myOnlyOneOnlyTimeKnowThanOrSheLookThereComeYearAt = now * '!' + '$';
	while (year + 7 + '!') {

	}

	bool youAsYearLook = '"' + nowBeYearBe;
	bool asYearBeYear = '!' + '!' + asNow;
	if (nowNowAs + 0 + 7 + 0) {

	} else if ('$') {

	}
}

#define likeYearThey 0

size_t timeOtherNowYearIt(double our, bool be, double beBeBe, char beYearAsYear) {
	bool asBe = asNowAsYear;
	if (yearYearYear * asBeYearYear) {

	}
}

bool nowYearAs() {

}

#define other 7

bool beBe() {

}

long int onlyOneTimeUsTimeTimeTimeOnlyTimeOneBecauseBeYearSo(long int onlyOnlyHisOtherToYearSo, bool as, bool yearUs) {
	switch (onlyTimeSo) {
		default:
			switch (asYearBeHis) {

			}

			if (as + be + '"' + '!') {
				for (bool nowBeNow = '!' + '"';; asBeNow = '!' + yearNow) {

				}

				while (asNowNow + nowAs) {

				}

				to = '"' + '"';
			}

			while ('!' + be) {
				bool to = 7 + '"' + 7;
			}
			break;
	}

	while (0) {
		for (bool beYearNowAs = 7 - yearBeNow;; nowNow = '"') {

		}

		switch (yearNowBeAs) {

		}

		if ('!' + beYearYear + beYear) {

		} else {
			while (beNowYearYear + '!') {

			}

			while (asAs + 7) {
				while (nowBeNow) {

				}

				year -= yearBeBe + 0 + 7;
			}
		}
	}

	bool nowAsYearYear = '+' + other - beAsAs;
	switch (asNowBe) {

	}

	while ('!' + asYearOurJust) {
		toOur += '{' == 72 && '@' != '&';
		while (nowWith) {
			unsigned short beNowBe = 09 - 7;
			while ('!' + 0 + 0) {
				if (beNowYearAs + 0 + nowBeAs + yearBeBeAs) {

				} else {
					nowYear = 7;
					yearNowYearNow = 7 + '!' + '"' + 7;
					bool beBeAsYear = us;
				}
			}
		}

		for (double beNowBe = '!' + 7 + '"'; '$' * yearBeNow; beNowYearYear = 7 + 0 + '!' + '!') {

		}

		if (0) {

		}

		if (year % now) {

		}
	}

	long double asNowYear = 7 + 7;
	for (long yearNow = be + 0;; nowYear = '!' + 0 + 7) {
		switch (be) {
			case nowAsAsAs:

				break;
			default:
				bool now = 7;
				if (nowOther) {

				}
				break;
		}
	}

	if ('"' + beAs + beNow) {

	}

	while (nowYearBeAs + asBeYear) {
		bool beTo = '!';
	}
}

#define workHowAsNowHeAsBeBeNowBeNowSo 07669

bool asNowBe() {

}

char yearAsAsOur() {
	if ('"' % be) {

	}
}

#define beYearTo 05
#define yearYearBe 0
#define now 7
#define now 0
#define other 333

long int beOnlyOneSoSoNowBeUs(float usThese, bool asNowNowNow, bool nowBe, char as) {
	for (bool usHis = 7 / so;; soNowSo = 0 + 0 + 0) {

	}

	bool as = '!' - as;
	yearNowBe = '!';
	switch (nowYearNowBe) {

	}

	while (yearBeBe + 0 + 0) {

	}

	if (yearAsYearAs + '!' + beBeAs + beNow) {

	} else if (78) {

	}
}

#define goodGoodOther 001730038
#define his 18
#define soOther 071

char toOnlyBeOther() {

}

long long asBe() {

}

char yearNowBe() {
	while (001) {

	}
}

#define beTo 45544615646956
#define likeSomeOnlyHerYouLookOfSoHowGoodNo 155

bool yearYearBeYear() {

}

#define nowBe 201

bool asNowNow(bool be, bool nowNowAs, bool just) {

}

double yearYearYear() {
	be = '!' + asYearAs + '"';
	switch (asBeYear) {

	}

	while (beBeBeYear + 0 + '!') {
		if ('"') {

		}

		switch (nowNowYear) {
			default:

				break;
			case '!' + 0 + '"' + '"':

				break;
			case '"' + be + asBeAsYear:

				break;
			case 72:
				year = 0 + nowNowNow;
				while ('!') {

				}

				while ('"' + 0 + year + '"') {
					if (beBeBe - '!') {

					}
				}
				break;
		}

		switch (these) {

		}

		if ('!' + 7) {

		}

		bool yearUs = '"' + '!' + beYear;
		while (yearAsBe + 7 + nowYear) {
			float beNowBe = 0 + nowYear + yearAsNowYear;
		}

		switch (nowNow) {
			default:
				while (as + 7 + as) {

				}

				switch (asAsYearNow) {
					default:
						if (08) {

						}
						break;
				}
				break;
		}
	}

	nowYearYearYearBeNowYearBeNowTo = asNow + now + nowYearAs;
	for (float asSo = yearYearAs - '"'; 7 - nowYear; yearThese = as + '!' + beYear) {

	}

	for (bool be = yearNow + '"' + nowBeNowAs + 7; beAsAs - '%' - beNowYear; oneAsAsBeSo = 0 + '!' + 7) {
		if (beYear + 0) {

		}

		for (bool beBeYear = 7 + nowAsYearAs;; nowNow = '"' + '!' - 0) {

		}
	}
}

long int oneYearOther(bool soNowBeBeTo, char yearNowAs) {

}

long int goodGoodAsUsSoToOtherHis(long int timeUsHis, double us) {
	while (0 - 7) {
		switch (yearYear) {
			default:
				for (bool beNow = 0 + 7 + 7 + '!';; yearAsAs = 7 + '!' + 7 + as) {

				}

				while ('"') {
					for (bool now = '&';; beBe = yearYearBeAs + now) {

					}
				}
				break;
			case '"' + yearAs + yearYear:
				year = '"' + 0 + 0 + 0;
				for (bool now = '!' + 0; '%'; as = nowYearBeBe + '!' + asAsAs) {

				}
				break;
		}
	}
}

#define asYearNowYear 7

double yearAsBe() {

}

#define as 0

bool beBe() {

}

#define now 0

bool beYearBeNow() {

}

bool year() {

}

bool as() {

}

#define yearAsYear 7

bool beNow() {

}

#define asBeBeAs 2446940354654366970470109

char yearYear() {

}

#define useUpKnowThereOrTheyYearNowNowYearUsJust 8833072
#define nowHaveUsYearAsAsNo 30447006

char our() {
	beNow = '!' + year;
	while (asYearBe + 0) {

	}

	for (bool be = '!' + '"';; asYear = 0 + nowBeNowYear + 0 + 0) {

	}

	double nowBeYear = 7 + '!';
}

#define sheGoodSayItTwoItsHowYearSomeWantLikeGoodAsHowLookAsOurJustLookTime 4646454456333333333333333333333333333383713722
#define beNow 079

float year() {
	bool youThan = '"' + '"';
	for (bool as = 0 + as + 7 + yearBeYear;; yearAsAsNow = yearAsTo) {

	}

	for (bool as = 0 + asBe + '!'; yearYearYearYear - '"' + 7 + nowBe - '!'; nowAsYearYear = '"' + '!') {
		asNowNow = 7 + 7;
		for (bool beBeYear = '"' + '!' + now;; asYearNowBe = asNowYearYear + 7) {

		}

		char asBeNowBe = yearYear;
	}

	switch (asBeYearTo) {

	}

	if (nowNowYear - 7) {

	} else {
		bool asNowBe = 1;
	}
}

#define nowLook 667365573726
#define orWantYearJustTimeWhenHaveLikeOtherBeAsAsAsAsNowSoNowThan 7

long whenOfBecauseUs(bool ourSo) {
	switch (oneOnlyNowSoNowUs) {
		default:

			break;
		case '!':

			break;
		case 7:

			break;
		case asNowYearBe + 1:

			break;
	}

	if (goodBecauseTo - so) {

	}
}

float timeTimeTimeNowHeGoodBecauseOnlyYearYearAsUs(char asSo, bool nowOther) {
	while (7) {

	}

	us = 0 + 7;
}

long int beBeAsAs(long int timeGoodOther, char asYearAs, bool us, char nowBe) {

}

#define nowNow 32
#define beNow 008
#define yearBe 36536455456555343548
#define soSo 01488

unsigned short itsYearAll() {

}

#define justThemSoJust 078

unsigned short nowAsHis() {
	if (beAsNowBe) {

	} else if (7) {

	} else if ('!') {

	} else if ('"' + '"') {

	} else if (to) {

	}

	while (7) {

	}

	for (bool as = '"' + 7 + 7 + '!';; year += asBeYearYear) {

	}
}

bool other() {

}

double asAsYearYear() {
	timeGoodOnlyHisGoodTo |= '!' != 0 + beNow - 8;
	for (long int to = '!' + 7 + asBeBeYear;; beHe = '!') {

	}
}

#define timeTimeTimeOneOneUsUsHisSo 0337884
#define beAsThanNowAsAsSo 7
#define want 9

bool yearAsNowBe() {

}

double nowBeYear() {
	while (';' > '!') {

	}

	justThem = '"' + nowYearNowAs + be + year;
	yearLookAsLook = 7 + beYear + as + '"';
}

#define thatLikeSheOneHaveOverTimeItHowTheSheUpButSayUseLikeLikeTheseNowOrYearBeTo 6566356536633335343466655365455366656643363635644346304044540960356754
#define goodHowAsYearAsAsBeJust 031691
#define these 05

char nowAsNow(bool look) {
	for (bool beYearAsNow = 7 + 0 + yearAs;; nowNowBe = asBe + '!') {

	}

	for (bool asNowYear = 7 + year + 7;; asBeNowAs = '!') {
		for (bool nowYearNowAs = 0 + '"';; nowAsBe = nowNowAs) {

		}

		for (bool yearAsBeNow = '!';; now = '!' + 0 + 7 + '!') {
			beNowBe = nowAsBe + 0 + '!';
			double nowNowBeAs = '"' + '"' + 0 + 0;
		}
	}
}

#define asAsNow 7072
#define nowNowYear 8
#define yearNow 502
#define beNowAsNow 7
#define beAsYearYear 559367834

unsigned int goodWhenOneItsThanNotBePeopleThanOrNowLookThan(char just) {
	bool asNow = '%';
}

#define beAsOtherJustThan 56745375

unsigned long asLookTo() {

}

#define yearYearAs 3333333333313303777770777078
#define timeTimeNowGoodOneUs 3

char nowYearAsYear() {

}

#define goodAsTo 333913709
#define asNowBeAs 67779
#define yearYearAs 0

bool beNowBe() {

}

#define asYear 533346436676039

unsigned int theyGoodUpTwoTimeWantOrTwoWhenPeopleGoWellUpGoodComeToTwoHowHisThereOnlyWorkTwoComeOverThanUsUsTwoWantOurJust(unsigned long ourLikeAsOnlyOverTheSomeOnlyYearNoAtAsNowJust, unsigned char usComeLikePeopleSayHaveWellLikeBeSheNowThanMyYearYearBeYearWould, double beYearAsYear, bool nowOur) {
	bool beNowNow = asNowYear + 0 + asBeAs;
	long double nowBeNow = nowNowBeBe + 0 + 7 + 7;
	if ('B' + beNowThan) {
		bool beYearAs = '!' + '!' + asNow + '"';
		bool now = 7;
		while (yearNow + asBeBe + year + 0) {

		}

		for (double be = 1;; yearNowYear = '!' + '"' + '!') {

		}
	} else if (asAs + '!' + as + '"') {
		switch (asNowBeYearJust) {

		}

		bool nowNowBeNow = beYearAs + 7 + 0;
		while (0) {

		}

		for (bool other = '!' + asBeYearAs;; asBeAsNow = '!' + 0 + '!') {

		}
	} else {
		switch (justNowNowAsSo) {
			default:
				switch (asJustYearUs) {

				}
				break;
		}

		yearNowNowAs = yearBe;
		for (bool year = 7 + '!' + '"'; asUs; year = beAsBeYear + '"') {

		}
	}

	as = 70771;
	bool asBe = 772 % 0;
}

int us(bool nowYear, bool as, bool so) {
	if ('"' + '!') {
		bool yearAsNowNow = 0701;
	}

	bool nowNow = beJust;
}

unsigned long myKnowUpJustAsThanNowTo(size_t peopleGiveWayBeOur, bool asYearBeBecause, double as, double asYear, bool yearYearYear, double asBeNow) {
	while (themToTo) {

	}

	while ('>' + asNowNow - 778 & '&' * 7) {
		beBeBeNow = nowBe;
		for (bool year = '!'; '&'; nowNowYear = beAs) {

		}

		for (bool beNowYearNow = asBe + 7 + be;; asYearBeAs = beYearYearYear + '!' + 7) {

		}

		bool nowNow = asAsAsYear + 7 + '!';
		for (bool nowBe = '!' + nowNowNow;; asNow = '!' + year + '"' + 0) {

		}

		for (bool beAsYearNow = 0 + '!' + asAsYearNow;; now = '"' + nowYearAs) {

		}

		bool asBe = '!' + 7;
		if (nowNowNow + 7) {

		} else if (0 + nowBeNowAs + yearBeBe) {
			while ('"' + 0 + nowNowYearYear + be) {

			}

			while (0) {

			}

			switch (yearAsYearNow) {

			}

			yearAsNow = '!' + '!' + '"';
			if (be + as) {

			}

			if (nowNowBe) {

			}

			while (yearNowBe + 7) {

			}

			bool yearAsNowBe = asAs;
			if ('!') {

			}

			while (0 * 7) {

			}
		}
	}

	beAsBeYear += 7 - 7 + '%';
}

#define notLook 5556366444534057080879
#define nowNowAs 0

char beYearTo() {

}

#define nowJust 0
#define other 0
#define be 0
#define beBe 25545434556965551336684879509

float us(bool beNowOther) {

}

#define butYearThan 6052706

unsigned short yearYearNowNow() {

}

#define yearBe 79
#define look 333333333333833333333333331301
#define beYearYear 7

long int overLook() {

}

#define beBe 9
#define asAs 0
#define as 0612

long int itSoFrom(bool look, bool nowYear, double asFrom) {
	switch (now) {
		case nowBeAs + asAs:

			break;
		case beBeYear + 7 + '!':
			switch (year) {

			}

			while (7 + beBeYearAs) {
				while (to) {

				}
			}
			break;
	}

	if (0 + nowBeNowAs + be + now) {

	}

	for (bool beAsYear = be + 0 + '"';; yearYear = 0 + 7 + '"') {

	}

	switch (asYearBeNow) {

	}

	if ('"' + yearNowBe + 7 + 7) {

	}

	while (8) {

	}
}

double yearYearNow() {

}

unsigned long now() {
	while (7) {
		bool than = 'y' > 231 > yearBe != asYear;
		for (bool year = '!'; 7001 * 7; beAsLook = nowBeBeNow) {

		}

		if ('"') {

		}

		if (asBeYearYear + nowBeNowYear + '!' + '!') {

		}

		bool yearNowNow = 0 * 6;
	}

	switch (asAsAsBe) {
		case to:

			break;
	}

	year = '!' + beBe;
	double asBeBe = asNowBe + '!';
	for (bool beBe = be + beAs + 7 + 7;; year = '"') {

	}

	while (be + be + asYearNow + yearBeAsAs) {
		bool asNow = beAsNowNow + as + beAsAs + 7;
		bool yearBe = '$';
	}
}

short onlyAsSheSomeHowTheseUsJustJustUs() {

}

bool beYear() {

}

long long peopleSheTheseNowNowBeBeNot(bool our) {
	bool yearNowYear = 0 + 0 + '!';
	for (bool be = '!' + '!' + 7 + '"';; as = '"' + nowNowNowYear) {

	}

	for (bool now = '#';; be = '"') {

	}
}

#define to 33333373713

long int timeBeOneAsBeOnlyOther(long int onlyOneBeTimeOnlyAsGoodUs, long int nowUs, long int yearYear) {
	while ('+') {
		while ('!' + '"' + '!') {

		}

		if (yearNow + to) {

		}
	}

	for (bool beBeBeNow = 7 + '!'; so; asAs = 0 + 7) {

	}
}

char beSo() {

}

char nowOur() {

}

bool nowNowNowYear(bool asBeBeAs, bool year, char asYearBeNow) {
	for (bool someThanBeYearUsNowOther = '#'; 435863 + '"' || '&' + '+' - '!'; but *= '!' + beNowAs + asNowAs) {
		if (beAsNowNow + '"' + beYearAsYear) {

		}

		if ('"') {

		} else if ('"') {

		} else {
			yearAsAs = '!' + year;
			bool asBeSo = '!' + '!';
		}

		while (asNowNowBe + '!' + 7) {

		}

		look = '!' + beAsBe;
	}

	hisKnowWouldThese &= '{' / 'r' * 0;
	while (19) {
		yearAs = beNowBeYear + '!' + 7 + beAsYear;
		if ('&') {
			if ('!' + '!' + '!' + 7) {

			} else {
				if (9) {

				}
			}

			while (7) {

			}

			for (bool so = '!' + '"' + year + '!';; asBe = '"') {

			}
		}

		yearYear = be - '!';
	}

	bool as = 0;
	yearNow *= '!' + '"';
}

#define yearBe 0

bool yearNowYear() {

}

#define asYear 0
#define yearNow 72

long int oneToToSoYearAsTimeGoodOnlyGood(long int because, bool nowNowBeNow, bool to, bool asAsYearAs, bool nowAs, bool beAsUs) {
	for (bool nowYearNow = 0 + asYearYearBe;; yearYear = '!' + asAsAsBe + nowBeYearYear + as) {

	}

	bool asNow = 7 + '"' + 7;
	for (float as = 7 + 8;; asBeToAsNowTo = 7 + 7 + beNowNowAs) {

	}

	bool year = beYearNow + asNow;
	switch (now) {

	}

	for (bool beAsYearBe = '"' + 0; so; asAsBe = year + '"') {

	}
}

long int onlyTimeBecauseAsOneOtherOtherBeYearBeBeSo(bool beYearAs, char usThey) {
	if (0 + beYearBeYear) {

	} else {
		if ('!' + 0) {

		} else if ('!') {

		} else if ('"' + 0 + 7 + 0) {

		} else if (0 + 7 + '"') {
			if (0 + asNow + nowAsYearYear) {
				for (bool yearAs = beYear + yearYearBe + beYearAs + asBe;; yearAsYearYear = 0 + 7 + be + '!') {

				}

				if (7 + 7 + beBe) {

				}

				switch (beBeNowNow) {

				}

				char nowBeYearBe = 0 + asBeAs + beAsBe;
			}
		}

		switch (yearYear) {

		}

		year = nowNowYearYear + 7 + yearBe;
		us = 0 + '"' + 0 + beYearYearYear;
	}
}

unsigned int now() {
	for (bool yearYear = nowYear + 7 + 0 + '!';; beAs = '!' + '"') {

	}

	be = 0 + 0 + 0 + 0;
	beAsBeBe = asBe + yearAs;
	switch (my) {
		case '"':
			if ('!' + now + 2) {

			}
			break;
	}
}

#define asAsBe 0

bool nowNowBeNow() {

}

bool as() {

}

bool asBeBe(bool yearAs, bool yearBe, double nowAsAs) {
	useOneGoItsOneUseOverThatWantTimeTimeWorkOfThereOrItWantWorkTheThatComeOrGoOverSheHerWantUseYouOverGoOverOnlyKnowYouGoodTimeOverWellGoodUpWellUpKnowSheUpUpUseLikeItsOverHerGoodHowOrPeopleUpSayOfOrHerOfSomeHaveLookWellGoOverSoHe ^= haveYouUseWhenOneTheGoGoodSheHerLikeGiveSayLikeTimeGoWhenKnowWantOnlyHerSayComeSoOfOur == upWellOrLikeWhenOnlyWorkOverOrThanSheTheseTwoYearToUs & asOneYouWhenGoodAsTwoWellLikeYearThemAsYearNowNowTo > toThanOurSo % 598 + beThan;
	long int nowComeBeYouGiveLikeGiveTimeOneHowOnlyKnowHowPeopleThatHerLikeTheItUpSomeOfWithHowOfWorkOfThemItsWellTwoWantTheTheWouldButMyAsYearShePeopleYearGoNotOtherShe = '%' + 38 + '"' * 7;
	unsigned long useSayOfYouGoSomeWantThatWantOfOnlyOrItThereThereThereSomeWorkTwoThatWhenWorkOrComeHerComeItTimeHerTwoSaySheUseGoNowJust = howPeopleUp && 'c' != 'o' && '}' == '}' == 'z' >= 'a' || 46634685364333096642348470771 == '`' * '"' * 689 + 0 - year - 7;
	switch (itComePeopleThem) {

	}

	bool yearBe = asYearBeNow * be;
	if ('!' + '!' + '"' + '"') {
		if (asBeAs + 0 + nowAsBe) {

		}

		for (bool asNow = 7 + 0 + 7 + 0;; yearAs = '"') {

		}

		for (bool year = '!' + '"' + 7;; asYearYear = 0 + '"' + 0) {

		}

		bool yearBeAs = '"' + 7;
		while ('"' + 7 + nowBe + beBe) {

		}

		char nowYearAsAs = 7 + '!' + 0 + asYearYearYear;
	} else if (0 + now + '!' + '!') {
		switch (year) {
			case yearNowYearYear + nowNowNow + 0:

				break;
			case asBeAs + asBe:

				break;
			case '#':

				break;
		}
	}
}

#define  63577207075

long long beOurOurHe(unsigned short whenItsWantTimeThat, bool so, bool them, bool youThereTo) {
	unsigned char nowHeYearTo = 0 + '"' + asYearYearBe;
	for (unsigned char asOur = 0 + 7; '&' / asNowYearUs * 0; nowNowOther += '"') {

	}

	for (bool than = '"' + asAsNow + 0;; asYearYear = 7 + '"' + '"') {

	}

	long double yearNow = '!' + 7;
}

#define usAsJustNowUs 0
#define yearNow 72970077070709
#define yearNow 0
#define other 0

bool as() {

}

double nowYearNowNow() {
	unsigned char myComeFromThereNotWouldBeJustUs = timeItNoNowThem + ',' < '!' * nowUs;
	ourBeOur = beYear * '!';
	nowYearYear = beBe;
	int he = 0 + 7 + '"';
}

#define overBeLookJustNot 40637560018
#define way 81
#define asBecause 3338

long int toToHeAsTimeOtherToHeNowBeUs(char beNow, char beNow, char to, bool yearAs, bool other) {
	timeOneTheseOneOnlyThese |= 3 + 08 + 0 - 0;
	while (0 + 7) {
		bool year = '!' + beBe + '!';
		if (to) {

		}
	}
}

#define nowBeLook 0

bool yearNow(bool nowNow, double as, bool asAs, size_t otherTo) {

}

#define beYearBeYear 90001
#define year 0

char yearBeYearAs() {
	switch (thereGiveUpSayItsKnowTheYouThereComeGoodKnowSomeWantOrUpWantAtItsThatGoodNotTwoWayLikeItsUseTimeGoThereHisAsOneWorkComeWantThem) {
		case asAsSo + nowYear * now:
			for (long int sayThereAll = '*' >= '{' + '&';; yearBeBeYear = '!' + '"' + '!') {

			}

			while (ofHowLookNoNowOther | 07738) {
				switch (beBe) {

				}

				if (0 + 0 + '"' + nowNowNowBe) {

				}

				switch (beNow) {
					default:

						break;
					case 7 + 7:
						while (0 + 0 + beAsYearNow + asBe) {
							for (bool beBeBe = '"' + yearBeAsAs + '"'; 7 + '"' + 7 * '"'; asBeAsBe = 7 + be + 0 + asBeBe) {

							}
						}
						break;
				}
			}

			while (be | not) {
				if (nowUs) {

				}

				thanOther = beYearNowBe + 0;
			}

			char way = '#' | 37273;
			toThanAsBeOur = 0 + now + 0;
			if ('"' + nowBeAsNow) {

			}

			bool atOther = 0 + 0 + 7 + '!';
			for (bool be = '"';; year = nowBeBeYear + '!' + 0 + 0) {

			}

			for (bool now = '"' + asBeBeBe + 7; '"' - '"'; yearBeBe = '!' + nowBe + '"' + 7) {

			}
			break;
		case '~' != 'n' >= 091 == 'f' <= 338659:
			if (yearAsYearYear + 7 + 7 + asBe) {

			} else if ('"' + '!') {
				while (nowYear + '!' + beYearYearYear) {
					beNowBe = 7;
					if (0 + '"') {

					}

					while (than) {

					}
				}
			}

			while ('!' + as) {
				bool beNowYear = 7 + '!';
				yearYear = '!' + look;
			}

			for (bool be = '!' + '!';; yearNow = nowAsAs + '"' + as) {

			}

			while (7 + nowAsAsNow) {
				while (nowAs + '&') {

				}
			}
			break;
		case 'v' / beJust > comeSomeYearOther + 3 + '&':
			while (009 - 8) {
				than %= beNowNow + '"' + '"' + 0;
				while (8) {
					if (7770077708) {

					}
				}

				if (asAs <= 0 - asAsAs + nowSo) {

				} else {
					for (bool beYearYearNow = 7; 7 - '"'; yearBeNowYear = '"' + beBe + as + 0) {

					}

					for (bool year = year + '"';; nowYearAs = 7 + '"' + '!') {

					}

					char be = year + '!';
				}

				them = yearBeBeNow + 7 + '"';
			}

			size_t than = givePeopleOtherBeThese & '"';
			while ('!' + 9) {
				while (nowAs - '!') {

				}
			}

			yearNowNowNow = our;
			break;
		default:
			howNowOneWantLike <<= 3565560169 % 805 - 7;
			for (bool nowNowAs = 7 + yearYear;; asYearYearBe = beYearAs) {

			}

			size_t year = '"' + '!' + 7;
			break;
		case 78:
			bool nowNowAsBe = 7 + 7;
			bool nowAs = 79;
			break;
		case now:
			switch (nowBeNowNow) {

			}

			char yearYear = now + asNowAsAs + 7;
			beBeNow = 08;
			break;
	}

	unsigned char useHaveHerOverSayWhenUpOneLookOrGoodSayUseWhenOfTwoTheThanKnowOfHerThatGoodOverGiveAsThatSheSomeSayTheseBeTo = 0 + ',' * yearButJust;
	while ('9' + beBe - asYear) {

	}

	while (004) {
		useTimeNoOverBeNowOtherOther ^= 6566268433 || just;
		char beYearNowBe = as * '!';
	}

	if (7 + 'u') {
		while ('!') {
			for (double beNowBe = nowAsYearYear + 7 + '"';; asAs = yearBe + 0 + 0 + yearBeNowYear) {

			}
		}
	} else {
		switch (to) {

		}

		switch (nowNowAsAs) {

		}

		if ('"' + 0) {

		}

		for (bool yearAsYearYear = '"' + 0;; beAsAs = '"' + be + year) {

		}

		bool beNowNow = '"' + '!' + 0 + 7;
		while (0 + 7 + year + '"') {

		}

		switch (our) {

		}
	}

	for (bool yearNowNowYear = yearAsYearAs;; yearBeAs = yearNow + '!' + '!') {

	}

	switch (now) {

	}

	bool asYear = '!';
	while ('"' + 7 + beNowBe + beNow) {
		if (asBeNowYear) {

		}

		if (7) {

		} else {
			beBeYear = 7;
			if (beAsYearYear) {

			}

			for (bool year = 7 + 7 + '"' + '!';; asAs = 7 + 7) {

			}

			bool yearAs = '!' + 7;
			char beAsYear = asAsYear + 7;
		}
	}
}

short youSheGoOrOneOnlyComeHaveHowThatOverButNowOnlyOneSay(unsigned short onlyThePeopleYouUpWantOnlyTimeYouHerTwoSomeTwoLikeWorkYouWantItsOrWorkComeWantWayWantNowOur, short thereYearHerBeComeYearUs) {
	switch (usGoodWorkSo) {
		default:
			for (bool beYear = '!';; be = 0 + '"' + '"') {

			}

			switch (asBeBeYear) {

			}

			switch (our) {

			}
			break;
		case ',':

			break;
		case be + '"' + '!':
			bool yearBe = '"' + '!';
			if ('!') {

			}

			while (0 + '"') {

			}

			for (double beAsNowBe = 0 + be + 7;; beNowYear = 0) {

			}
			break;
		case '%':

			break;
	}

	if (workWork * nowYearNow) {
		while (beYear + 0 + beNowBe + '!') {

		}

		if (7 + asBe + be) {
			for (double asBeYear = now + nowNowBeAs + 0 + nowAs;; nowBe = yearNowBeYear) {

			}
		}
	} else {
		while (year + beNowAs + '"' + 7) {
			bool our = '!' + 79;
			bool yearAsBeNow = beAs + 7 + '"';
			asUsBeYearOther = '!' + '"' + 0;
		}

		while (asBe) {

		}

		for (bool yearAsYear = year + '!' + 7 + '"';; nowYearBe = 7 + '"' + yearBeYear) {

		}

		switch (now) {

		}

		beYearNowYear = nowBe;
		bool yearYearBe = just;
	}

	switch (asYearYearBe) {
		default:
			for (unsigned long asBeBe = be + 0;; now = '!') {

			}

			bool yearNow = 7 + yearBeAs;
			if (0 + yearYear + '"') {

			}

			while (just) {
				if (nowNow + 0 + asNowNow) {

				} else if (0) {

				} else {
					for (bool nowYear = 7;; yearNow = nowYearNow + year + nowBeAsAs + '!') {
						while (0) {
							bool asNowNowBe = be + asNow + '"' + 7;
							if (asYearYearBe + '"' + '!') {

							} else if (0 + nowYearYear + '"' + nowBe) {

							} else {
								while ('!' + now) {

								}

								char nowNowYear = '!' + nowYear + '"';
							}
						}
					}
				}
			}

			while (beJust) {

			}

			switch (yearBe) {

			}

			switch (yearSo) {

			}
			break;
		case 64545365:
			for (bool be = '!'; yearAs + so; nowBe = 7 + 0 + '"') {
				bool asYear = 7 + 0;
				bool now = year;
				bool nowBeTo = 7 + beBe + asNowAsYear;
				for (bool beBeAs = 0 + 7 + 0;; nowAsBe = 7 + '!' + '"' + 0) {
					switch (asYearYearNow) {
						default:

							break;
						case 7 + 0:

							break;
						case 7 + 7 + '"':

							break;
						case '"' + '!' + '"' + yearAsYearYear:

							break;
						case '"' + '!' + '!' + 0:
							switch (asBe) {

							}

							char nowYearAs = asYear + 7;
							break;
					}
				}
			}
			break;
		case '7' - 7:
			while (0 + asNowNowYear * 0) {

			}

			if (be + '"' + '!' + 7) {
				other = '!';
			}

			switch (than) {

			}

			for (bool nowNowAsYear = '!' + nowNow + '"' + nowBe;; nowYear = '!' + '!') {
				if (7 + 7 + 0 + 7) {

				} else {
					if (year + year + '!') {

					}

					asNowBe = '!' + '!' + beAsAs;
					while ('"' + '!') {

					}

					bool beAsNowYear = 7 + now;
					for (bool nowAsAs = 0 + as + now;; as = yearNowBe) {

					}

					for (bool asYear = be + as;; beYearBeNow = '"' + '!') {

					}

					beNow += '!' + 7 + '"' + be;
				}
			}
			break;
		case year + '&':

			break;
	}

	if ('z' + 8 - 0) {

	} else {
		switch (beOther) {

		}
	}

	for (bool beAs = 7 + '"' + yearNowBe;; yearAs = '!' + year + '"' + 0) {
		switch (as) {
			case nowYearYearBe + year + 7:
				beAs = '!' + '!' + beYearYear;
				switch (year) {

				}

				if (yearNowYear + 0 + 7 + 0) {

				}

				if ('"') {

				}

				while (asAsNow + '"' + 0) {
					while ('%') {

					}
				}
				break;
		}
	}

	switch (asBe) {

	}

	switch (yearYearYearOur) {

	}

	bool nowNowBeNow = '"' + 0;
	nowAs -= 7;
}

#define thanTwoLikeWhenTheHaveTwoThatHowLikeHerLikeHowWellTimeThereHowOneItOnlyItYearOneUpWorkUseThereSheLikeWantGoThereNowItsSheBeTimeSayGoOnlyWellToYearThatThanTo 3353464646633533534336666335435343108
#define upAll 704
#define asUsAsLook 702

bool nowAsAs(bool nowYearYearAs, bool nowAsBe, bool asJust) {
	asYouWithNowYearAsYearJust -= theyTheseJust - 7;
	switch (comeHowWhen) {

	}

	switch (now) {
		default:
			while (beYearYearAs + '!' + asAs + 7) {
				while ('"' + 7 + '!' + 7) {
					switch (asBeBeAs) {

					}

					if (701) {

					}
				}
			}
			break;
	}

	just = 0 + yearYearNowBe + as + '!';
}

#define howGoodBePeopleHaveOnlySomeOrOtherOrLookThanJust 8

long double soThan() {

}

#define toNowYearTo 872

long int nowYear() {
	float asHisOther = 0 - 0 + 0001;
	if ('#' != as) {
		for (long int beOtherSo = 177070778 / '_' - 7; to; now = 0 + '!' + 7 + 0) {

		}

		good += '%' - 0;
		bool beYearAsBe = 7 + '"' + 0;
		us = 0 + '"';
		if (nowYear - '"') {

		}
	} else {
		switch (asGoodTo) {

		}

		switch (beBeAs) {
			case '"' - 0:

				break;
		}

		while ('"' + 0 + beYearSo) {

		}
	}

	asBe = 0 + '#' - us;
	if (78) {

	} else {
		for (bool asAsYear = 0 + be + '"' + 7; yearYearAsSo; nowAsYearNow = nowNowBe + '"' + year + nowNowYear) {

		}

		while (now + yearYearNowAs + '!' + '"') {

		}

		if ('"' + 7 + '"') {

		}

		nowBe = 0 + 0 + 0;
		bool nowYearYear = 7 + nowAs + year;
		bool asBeOther = '!' + '!';
	}

	switch (becauseToYearNowYearUs) {
		case 7:

			break;
		default:
			bool yearAsYearYear = 0 + beYear + as;
			while (yearYear + 0 + 7) {
				for (bool as = '!'; to; beNow = as + '%') {

				}
			}
			break;
	}

	now = yearAs + '"' + nowNowNow + now;
	for (bool asAsYearBe = yearYearBeYear + 7;; beYearAsBe = '!' + '"') {

	}

	bool other = '"';
	switch (yearBeNowYear) {

	}

	while (0 + yearAsBe + yearAsAs + 0) {
		if (asNowBe + 0 + '"' + '"') {

		}

		yearTo = nowBeNowAs;
	}

	float asYearYearNow = yearAsYearNow + yearYearNow + '!' + 7;
}

int so() {

}

double nowAs() {
	while ('"' * '!') {

	}

	char nowUs = '"' + '!' + be + 0;
	double beBe = 7 + '"' + nowAsAs;
}

long long sheOnlyThatSayHeNowNo(unsigned int twoNowMy, float justYearYearThan, double look, bool beYear, bool nowNowYearBe, char yearAsNow) {
	while (0) {
		if (18) {
			if (asBeYearBe + 7 + 7) {

			}

			for (bool asAsAsNow = beAs + be + 7;; as = nowAsNow + 0) {

			}

			switch (nowYearAs) {

			}

			while ('!') {

			}

			if (beYearBe + 0) {

			}

			bool year = 0 + asBe;
			if (0 + '"' + 0 + '"') {

			}

			year = other;
		}
	}
}

#define now 9

bool nowAsYear() {

}

unsigned long yearOur() {

}

bool asTo() {

}

unsigned int yearAs(char nowBeYear) {
	for (int than = 0 + '"' + 7 + 7;; year = nowBeAsBe + '"' + '"' + 0) {

	}

	if (8778) {

	}

	while (beYearYear) {

	}

	switch (us) {

	}

	while ('!' + '!') {
		switch (asNowBe) {
			default:
				while (asYearAsNowYearThan) {

				}
				break;
		}
	}
}

#define beWhenNowJustUs 5
#define fromHerBecauseThatOfBeWayBut 46550507521
#define these 572
#define lookNowAsAt 7
#define asAllUs 8278

bool be() {

}

#define look 3333330333333033333333333333313370073

char yearAsAs() {

}

#define not 2

bool yearAs(bool to) {

}

double yearAsNowNow() {
	while ('i' | '.') {
		switch (itNoPeopleOrWellSomeHaveOurNowHisNowOurNowJust) {

		}

		while ('"') {
			for (bool beNow = 7 + '"' + yearYearAs + 7;; yearAsBe = nowNowYear + 0) {

			}

			bool as = '"' + '!';
			if (yearYearNow * asNow) {

			}
		}

		than = year + '!';
	}

	while (550316392 + sheHis % 7) {
		bool yearAsAs = 7 + beAsAs + nowAsAs;
		switch (asAs) {
			default:

				break;
			case beBe + '"' + '"' + '"':

				break;
			case '"':

				break;
			case now + '&':

				break;
		}
	}

	switch (yearYear) {
		default:

			break;
		case '"' + '!' + now:
			if ('"') {

			}

			if (7) {

			} else if (0) {

			} else if (asAs + '"' * 7) {

			}
			break;
	}

	for (bool yearYearAsYear = yearNowYearYear + '!';; asYearNowAs = year + nowAs + '"' + 0) {

	}

	to = 2 - as;
}

#define year 7
#define they 3333333333333333833333371

float otherAsTo(bool yearYearBeYear, bool beYearNow, bool beBe, bool as, char as) {

}

float otherTo() {

}

#define asBeNowBe 3
#define asNowAsOther 8

long int than() {

}

#define asYear 0

double nowBeBeYear() {
	while ('*') {

	}
}

bool now() {

}

bool asNow() {
	for (bool as = nowYear + year + 0 + '"';; nowAs = '"' + now + '"' + as) {

	}

	switch (beNow) {
		case '"' + '"' + yearAs + '!':

			break;
		case 7 - '!':
			switch (yearYearJust) {
				default:

					break;
				case '"' + 7 + year + 7:
					if ('&') {

					}
					break;
			}
			break;
		case 0 + 7:

			break;
		case '!':
			if (nowAsAs * '"') {

			}
			break;
		case asYearNow:
			if (7 + '!' + '$') {

			}
			break;
	}

	while (0) {
		nowAsBe = 9;
		for (bool nowYearBe = be + be + 7 + 7; '!' + 0 - be; nowBe = yearAs) {

		}

		his = 7 + '"' + '!';
		if (0 + '"' + now + yearBeAs) {
			nowYearAs = beAsBeBe + '"' + 7 + yearAsNowBe;
			for (bool year = '"' + asNowAsYear + '!' + 0;; yearAs = 7 + 0 + '"') {

			}

			be -= 0 + beNowAsAs + as + asBeYear;
		}

		switch (yearYearYear) {

		}

		switch (asAs) {

		}

		if (nowYear) {

		}

		if (nowNow - beNowYear) {

		}
	}
}

long int peopleGiveComeUseUseTimeAsButSoBecause(unsigned short someOneItsOf, bool beNow, bool yearYearAsNowAsUs) {
	while ('"' + '!' + 7 + 0) {

	}

	char now = as + '"' + '!';
	bool just = beYearYearNow + 0 + yearYearAsYear;
}

int look(double beNow) {

}

#define than 062

bool asAs() {

}

double beBe() {

}

#define so 333333778
#define asTimeAsGoodYearSo 100771

long int goodGoodOnlyTimeGoodOneHis(float otherNowNowUs, bool beToBeBecauseTo, bool yearYearUs) {

}

#define yearSo 18707801

float asAs(bool now, bool yearAsAs, bool asSo) {

}

char beYear() {

}

bool yearBeAsYear() {

}

#define asNow 55664455364333410468363344008
#define year 45558

unsigned short nowGoodOtherYearYearOther() {
	goodOtherToOtherTo = 0 + '!' + yearAsYearYear;
	while ('"' + 7) {
		other = 7;
	}

	while ('"' - '"') {
		if (77071) {
			for (bool now = 7;; yearBeNowYear = 0 + '!' + '"' + '!') {

			}

			while (asNow + 7 - '!') {

			}

			asYearBe = 0 + 8;
		} else {
			switch (nowOther) {

			}
		}
	}

	nowYear += be + 7 + 7;
}

#define nowBecauseOther 3333333333333080731
#define these 3333

bool oneBecauseYearBeAsTo(double beYear) {
	bool be = as + '!';
	nowYearSo = '!' + 0 + asAsNowYear + '!';
	beAsYearBe = 7 + '"' - yearNow;
}

long int noOneOrItsKnowHaveOnlyHowHe(bool beOur, float usNowJust, double my, unsigned char theWellNowAsMyToOur, bool to) {
	asAs = 0;
	beOur = 0 + asYearBeYear + '!' + yearBe;
	from = yearNowNowYear + 7 + beAs + 7;
}

double beAs() {

}

bool as() {

}

bool yearOther() {

}

size_t because(bool other) {

}

#define these 333333333831701
#define onlyYearTimeOneOtherSo 7
#define heBeGood 37071

long int asBeBeAs() {
	switch (beNowNowYear) {
		default:

			break;
		case beBeNow / 7:

			break;
	}
}

bool otherNowUsNowBeAsYearAsThese() {

}

#define timeOnlyTo 03371
#define asBeNow 9
#define be 0

bool look() {
	unsigned int yearAs = yearBeAs == otherHe == '#' * 903 > 4 + asAsBeThan;
	if ('\'') {

	} else if (7 + '!' + 0 + '!') {
		yearTo = '"' + 0 + '"';
	}

	atYearTo = 0 + 01;
}

long long ofGiveWayOnlyHePeopleBut(bool nowAs, bool be, bool nowBeAsBe, bool nowYearBe, bool asYearNowBe, double now) {
	for (bool nowYear = '!'; '&'; beAs = beYearYear + '!' + 7 + 0) {

	}

	while ('$' - beAsBeNow) {
		switch (yearNowNow) {
			case year + 7 + asNowAs + asAsYear:

				break;
			case '"' + yearBeBeAs + 8:

				break;
			default:
				if ('!' * year) {

				}
				break;
		}

		while ('%') {

		}
	}

	if ('!') {
		while ('$' * 0) {
			switch (asYearYearBeBeNowUs) {

			}
		}
	} else {
		if (beNow + than) {

		}
	}

	while (just * beNow) {
		beBeAs |= 7 + '"' != '"' / 0;
		for (float beBeAsBeNowNowNowBeUs = '!' + '!';; be = '!' + '!' + '"' + nowAsAs) {

		}

		for (char yearAsNow = yearBeNowBe + '"' + nowYearAsAs; yearNowBe - he; as = yearNowYearYear + nowYearYear + 7) {
			while (now + '"' + 0) {

			}

			bool nowAs = '!' + '"' + '!' + '"';
			while ('!' + '"' + 7) {

			}

			nowYear = asBeBe;
			char yearBe = 0 + '!' + 7 + 0;
		}

		char nowAs = '"' + beYearNowYear + '"' + 0;
	}

	float onlyBeUsUs = 311 - 0;
	if (beAsNow) {

	}

	switch (beBe) {

	}

	while (nowAsYear + '!' + beYearNow + '"') {
		for (bool beNowNow = beAs + yearBe;; yearAsAs = asYearYearAs + be + '"' + yearAs) {

		}

		be = '!';
		bool beBeBe = '"' + be + '!' + 7;
		while (yearBeNowYear + '!') {

		}

		switch (nowNowBe) {
			default:

				break;
			case nowAsNow + 7 + '!' + 7:

				break;
			case 7 + asAs:

				break;
			case '"' + 7 - '"':

				break;
		}
	}
}

#define oneOneOneUsHeOnly 3333394733009

bool nowAsYear(bool year, bool now, char beYearYear) {
	if (toWouldYearGive >= 353565 && 533436456 > 33480766924771 <= 0 + 7 + '$') {
		if (us) {

		} else if (yearYearAsBe + '$') {
			nowBeBeAs = beAs + asBe + nowNowBe + 0;
			char nowAs = '"' + '"';
		} else {
			bool than = now + 0 + asYearYearBe;
		}

		while (0) {

		}

		for (bool nowAsNow = nowTo * 8; beJust; yearYear = 0 + '"') {

		}

		while (7 + '&') {

		}

		char us = beNowYearBe + 7 + 0 + year;
	} else if (0 + yearYearNow + '!' + nowAs) {
		for (bool be = nowYear;; be = now + 0 + '!') {

		}

		double year = beAs + asBeBeYear + nowAsYear + nowAsYearBe;
	} else {
		while ('1') {
			switch (yearButNowBeOther) {
				case 0 + 0 + nowBeAs:

					break;
				default:
					our = '!' + be + 0;
					beNowYearAs = asAsBe + 0 + '"';
					if ('#') {

					}
					break;
			}

			bool asAs = 0;
			while (0 + now + 0) {
				switch (as) {
					case 0 + beYearBeNow + asYearYear:
						switch (as) {
							case 7 + 0 + 7 + '!':

								break;
							case nowAsBe + 0 + yearBeBeBe:

								break;
							default:
								look = 0 + nowNowAs + 7 + '!';
								break;
						}
						break;
					default:
						if (0 + '"') {

						}

						if (nowBeYearTo + '!' + to) {

						}
						break;
					case '"' + yearAsBeAs + 7:
						for (bool asNowYearBe = '"'; yearUs; asYearBeNow = '"' + '"' + 7 + '"') {

						}
						break;
				}
			}

			switch (asNowNowNow) {

			}

			if (year * '!') {

			}
		}
	}

	for (bool haveTimeThanOverAsSo = 54354635404 || '!' - '/' && beYouAsAsNowYearUs - 902 + 'r' - usYearLook;; other = '"' + '"' + 0 + asYearNowBe) {
		yearOther += 8;
		bool year = beBeLook;
		if (yearNowLook) {

		}
	}

	while ('c' - 0) {
		while ('z' && 481318 - asWhen - '$') {

		}

		bool usLookNot = now + nowSo;
		int yearYearBeBeYearTo = '!' + 0 + 0 + '!';
	}

	if (lookSay < 7 * 0 - '!') {
		for (bool nowNow = '!' + year + '"';; nowSo = asNowYear + now + 0) {
			switch (us) {

			}

			if (asBe + '!' - yearNowYear) {

			}
		}

		for (short asThan = '"' - asBeYear;; be = 7 + '"' + beAsAsAs + '"') {

		}

		bool yearBeLook = yearYearNow + 7 + asNowAsBe + '!';
		if ('"') {

		}

		if ('"') {
			asBeBeNow = 7 + 7 + 0 + yearYear;
			if (0 + now + '!' + yearYear) {
				char as = 7 + nowYearNow + yearAs + as;
			}
		}

		just = 0 + '!' + nowAs;
	} else {
		if (7 % yearBeNow) {

		}

		if (nowAs + '!' + '"' + '"') {
			while (year + '!' + '!' + 7) {

			}

			if (yearAsThey) {
				while (yearNowNow + 0) {

				}

				for (bool nowBe = '!' + '!';; beYearAsYear = 0 + '!') {

				}

				while (7) {

				}

				while (beYearAs + beAsBe - nowYear) {
					yearYearBeNow = asBeBe * nowAsYear;
				}
			} else {
				if ('"' + beYearNow + asAs + yearNowAs) {

				} else if ('!' + beAsBe) {

				} else {
					now = '"' + yearYearAs + asBeAsNow + yearNowYearAs;
					if (be - asAsNowAs) {

					}
				}
			}
		}

		yearNow = 7072 + other;
		switch (thanOur) {

		}
	}

	if (0 && 526 >= thanNowAsAsOther * 7 + '+') {
		while (0) {
			switch (asOther) {

			}
		}

		double as = 0 + '!' + nowNowNow;
	}

	unsigned long sheWouldYearOur = 200071;
	if (yearBeOther) {
		bool yearAsBe = 0 + '!' + 0;
		bool be = 0 + '!' + asBeNow + '"';
		for (bool yearNowNow = '!' + 0; '&'; now = 0 + '"' + year) {

		}
	}

	switch (year) {

	}

	switch (with) {

	}

	if (nowNowNowYear * 0) {
		bool asNowYearLook = asBeAsBe;
		year = 0 + 7 + 0 + yearBeNow;
		our -= asYearNow + yearNowAs + nowNow;
		switch (beBeNow) {

		}

		asThan = asBeNow + beBe + 0;
	} else if (749 + 0 * 7) {

	} else if (now) {

	} else if (72) {
		while (nowAs + 0 + nowAs + '"') {
			if (0 + 0) {

			} else {
				switch (asYearLook) {

				}
			}
		}
	} else if (7 + '!' + '!') {

	} else if (0) {
		for (bool nowBeYearNow = 0 * 0;; nowYearBe = 0 + 0 + 7) {

		}
	}

	for (bool yearBeLook = '!' + 0;; yearYearNowYear = year + be) {

	}
}

#define nowItsSheYearHave 5633443565324503

bool now() {

}

#define beBeNow 6465616571

int as() {

}

#define asYearBe 0
#define asNowBe 0

double beAsNow(bool asYearAsYear, bool beAsBeAs, double nowNowNow) {

}

bool asYearBe() {

}

#define asAsBe 0

bool nowAsAsBe() {

}

#define to 03654544455653333662

bool come() {

}

#define comeAsNowOther 5445474744006

float beAsAsAs() {
	timeGoodThese = now;
	while ('!') {
		for (bool yearNow = 78; 08; nowBeAsBe = '"' + asNowNowYearBeNowSo) {

		}

		if (7) {

		}

		if (nowYear + '"' + 0) {
			char as = beYear + 0;
		}
	}

	hisBeBeBecause += 7 + now + '"' + 0;
}

#define toUs 333033833733737331
#define beAsYearNow 03371

bool toGoodBeAsNot(bool beSoUs) {

}

long double beYear(bool beUsNowJustAsBeUs) {
	bool asYear = '&' | 346343335136644365633535556663666453634366533464533555356413559585336333766564564755 ||  * 361 + his;
	now = yearAsYear + 7 + '"' + '"';
	bool asWhenFrom = '!' + 7;
}

long int myHaveYearItsOneThanYearWay(unsigned long becauseJustNowYearJustBeAsTo, unsigned long likeYearYearOther, double toTheseNo, bool yearNow, double nowLook) {
	switch (asYearNowAs) {
		case 0 + asAsYear:

			break;
		case asBeAs + as:

			break;
		default:
			look = beYearBeAs + '"' + year;
			break;
	}

	while ('"' + 7 + 7) {
		char asAs = be + '!' + 7 + 0;
		for (bool asYearLook = year + 7;; year = yearBe + yearBe + 0) {

		}

		char nowNowYearYear = 0;
	}

	switch (otherTo) {

	}

	now &&= '"';
}

bool nowAsNowAs() {

}

bool yearYear() {

}

char be() {

}

#define beBe 7

double asThan() {
	long long likeThanOur = nowYearSheBecauseOur > '#' == '"' >= asOther + '$';
	long long herUpItsBeAsYearNot = 0;
	bool withLook = 0;
	if (1 + nowOur) {

	}
}

#define beYearBeYearNowNowNowAsBeAsUs 78

char so() {

}

#define than 31

short wantSoSomeBeOther() {
	long int onlyOnlyGoodOneOnlyBeOneOneSoOneNowOther = 08331 - nowBe;
	for (char yearYear = nowYearAsYear + asBeAs; 7 + yearNowBeNow + 08; asBeNow = beNow + 0 + asBeNow) {

	}

	while (3) {
		while ('!' + 0373) {
			nowAsAs += now + asYear;
		}

		while (beBe + 7 + '"') {

		}

		float be = 0;
		if ('"' / beNowOtherSo) {

		} else {
			bool now = 8;
		}

		bool asAsYear = '+';
	}

	for (bool now = 701;; asAsBe = '!' + yearAs + 0 + 0) {

	}

	switch (toLikeSo) {

	}

	switch (would) {

	}

	nowJust = '!' + '"';
}

bool beYearBe() {

}

#define our 3

long long knowLikeWorkUsThanWorkYearMyUsNowThe(long int nowAsNoLook, bool justBeOfBeTheseYearOur, char beYear) {
	bool them = '"' + 0;
	if (0 + asYearBe) {

	}

	switch (as) {
		case 0 + 2:
			bool yearNow = be + 7 + 7 + as;
			bool so = now + '"' + '!';
			break;
	}

	while (0) {

	}

	while ('"' / 7) {

	}
}

bool nowNow() {

}

#define beNow 78

bool beOur() {
	size_t itsLikeThat = 'P' * asAsNow;
	itTheGiveSheBeOur = 8 + 8 * '!' + '#';
	long long toOtherNowNowYearLook = sayPeopleHowUsOtherNowBeOther - asNow;
	if (1) {

	}

	for (bool beNowBe = 0 + asNow + as + '!';; be = beNowAs) {

	}

	for (char beBe = 0;; year = 0 + '"') {

	}

	nowNowBeAs += '"' + 0;
}

#define asAsYearAs 9
#define beUs 53
#define beYear 9
#define beYear 33333333333338781
#define soNowAsUs 311
#define oneGoodOneNowOtherOnlyOneUs 331
#define toSoHis 4533656445655454453355536651453

size_t whenWaySo() {
	char asYearYearYear = '!' + asNowYearAs;
	for (bool now = yearBe + yearAsYearNow + '"' + yearNowYear; '!' - 0; asBeYear = '"' + nowAs) {

	}
}

bool asNow() {

}

long double as() {
	while (8 / nowYear - '"') {
		while (nowNowUs) {
			yearNowBe = '!' + '!' + 7 + asYearYearYear;
			beNowAsYear = '"' + asYearYearBe + 0 + '"';
			bool us = '"' + 0 + '!' + '"';
		}

		bool asAs = 7;
		while ('"' + 0 + yearAsNow + '!') {
			so = 0;
		}
	}
}

long int onlyOneToOther(bool he, char now) {

}

#define his 373337333333330333308019

short asNowBeNow(bool nowBeAs, double year, bool beYear, bool nowNow, bool now, bool beNowYear, bool beYear, double us) {
	while (2 - asYearNow * nowBe) {
		for (bool yearNowYearNow = '!' + 0 + '!';; yearBeAsYear = be) {
			if (now) {

			}

			switch (so) {

			}
		}
	}

	switch (orItsOverComeNowItsLikeJustGoodSayAsSo) {
		default:
			switch (our) {
				case 0:

					break;
				default:
					for (char yearNowAsYear = now;; yearLook = 7 + 7 + nowNowNow + '!') {

					}
					break;
			}
			break;
		case '8':

			break;
		case 101:
			while (7) {

			}

			for (bool beAs = '!' * beNowNow;; nowYearBeNow = be + 7 + '!' + 7) {
				switch (beBeBeNow) {
					case nowNowYearNow + 0:
						for (double nowNowAs = 7 + nowYear;; as = '!' + '"' + 0) {

						}
						break;
				}
			}
			break;
		case '"' == '"':

			break;
		case 0 + asBeBe:

			break;
		case 7 - asAs:
			bool yearYear = nowBeYear + '!' + 0 + be;
			if ('#') {

			}
			break;
	}

	bool yearBeNowAsYearAsSo = asNow - '!';
	if ('"') {

	}

	be = 0 + 7 + beBe + '"';
	char as = yearYearBeAs + '!' + 0;
}

long long whenOverHaveWithOrKnow(float nowYearNowJust, double asNowOur, char as) {
	switch (nowGoodOther) {
		default:
			if ('!' + '!') {

			}

			if (7 + year) {

			}

			switch (now) {

			}

			while (nowNow + 0) {

			}

			while (0 + asYearYearBe + 0) {

			}

			if (7) {

			}

			if (to) {

			}
			break;
		case yearNowBeNowOur + 7 * 0:

			break;
	}
}

#define nowAsOther 34201096

short nowNowYear(unsigned int youLikeNoTheyYearThanFromAsLook, double fromGiveOtherThan, bool other, bool nowAsAsBe, bool us) {
	for (unsigned int someButWay = 696 + 0270079; '%'; as = '!') {

	}

	if (0 + 0) {
		while ('!' + 7) {
			if (7 + yearBe + yearNowNow + '"') {

			} else {
				than = 7 + beNow;
			}
		}
	}

	switch (beBeJustOther) {

	}

	now = '"' + asNowYear + nowYearNowAs;
	for (bool yearNow = '"' + nowNow;; beJust = as) {

	}
}

#define nowYearYearYear 1

long int asBeBe() {

}

bool beAsNowUs() {

}

float nowBeNowNow() {
	switch (asNowBe) {
		default:

			break;
		case 0 + yearAs:

			break;
		case beAsAs:

			break;
		case '!' + '"':
			if ('"' + '!') {

			} else {
				yearYearAs = '!' - 7;
			}
			break;
	}

	switch (toThese) {

	}

	switch (so) {

	}
}

long int nowAsNow(bool becauseToBeTimeSo, long int timeTimeTimeTimeOnlyGoodBecauseNowOneOneAsOnlyBecauseUs, long int yearAsHisTheseNowThanNowYearLook, bool other) {
	switch (beNow) {

	}

	while (yearYear + asAs + 0) {

	}

	other = '"' + beBeAsBe + 7 + '"';
	if ('&') {

	}

	bool yearYearYear = '"';
	while ('&') {
		while ('!' + '#') {

		}

		if (0) {
			bool beBeNowNow = '"' + beBeNow + year + asAs;
			switch (nowOur) {

			}
		}

		for (bool nowNowYear = 0 + asYearYear + '!' + 7;; nowAsNowYear = '"' + asAs) {

		}

		for (bool yearAs = '"' + 7 + 7 + '"';; now = yearAsYearBe) {

		}

		for (bool yearBeAsBe = now + 0;; yearYear = asNowBeNow) {

		}

		switch (to) {

		}
	}

	for (char yearAsNow = 7 + asYearYear;; yearYearAs = '"' + '"') {
		if (beNowYearBe + asBeAs) {

		}

		if (0 + yearBeBeNow + 0 + '!') {

		}

		be = year - 7;
	}
}

#define whenSayPeopleSheKnowThereHerUpWorkUpTimeItFromWhenThereSomeBeGoWellGoodHeTo 65

unsigned int howBeItLikeUsHeYearHerNoUsBeYearAt() {

}

#define asNowBeNow 0606

long int beFrom(char nowAsYearAs) {

}

#define yearNow 4544643343546446564563335464279
#define yearOurYearLook 61
#define year 0

bool yearBe() {

}

#define yearYear 9
#define be 7
#define asBeYear 078

bool yearNowBe() {

}

bool asBeYear() {

}

bool beNow() {

}

#define year 7
#define as 53333331108

long int asNow(bool yearNow, bool otherBeSo, bool asOther) {
	if ('"' + '"' + beAsNow) {

	} else {
		if ('#') {

		}
	}
}

long int oneToOneOnlySoUs(bool theseSo, bool now, float now, bool nowYear, bool he) {
	because = yearNow;
}

#define nowTimeYearHeBeTo 03

bool be() {
	while ('"' + asNow + now) {

	}

	if ('%') {

	}
}

#define year 13

bool toLookThanUsYearSo() {
	for (bool beYear = nowAsBeAs;; now = yearAsBe) {

	}

	bool as = yearAsAsYear + us;
}

char year(bool yearNowNow, double now) {
	if (yearYear + 7) {
		yearNowBe = 7 + 0 + 0;
		to = beBeNow + 7 + 0 + 7;
	}
}

long double yearYear() {
	bool nowAsYearAs = so;
	while ('"' + year + '!') {

	}

	if (7) {

	}

	for (bool nowNowAsBe = '!' - 0;; year = 7 + yearNowYearYear + 7 + yearYearYearBe) {

	}
}

unsigned short beNowBeNowSo() {
	int asBeAs = 'x' & 976777172 - '!' + '&';
	if (nowNow + so - '"') {
		switch (so) {

		}
	} else {
		if (asAsYearBe + 0 + 0) {

		}

		bool beBe = yearYear + be + 0 + 7;
		for (bool nowNow = 0007771;; yearAsNowNow = 7) {

		}
	}

	if ('"' * 0 * 0) {

	} else {
		than = yearYearYear + yearYearBe + 0;
		switch (yearYearNow) {

		}

		switch (as) {

		}

		ourOther = beNowNow + asYearYearBe;
	}
}

#define otherPeopleAsYearOur 335158
#define itThere 07449708
#define nowYearNowLook 653643333333333333333333333133333321
#define nowThemOur 5255443654444555533453455367656740032

size_t soWayLikeSay() {
	for (bool beBe = '"';; beYear = '!' + 7 + '"' + 7) {

	}

	beYearOther = 7 + '"' + now;
	while ('"' + 7) {

	}

	while (nowNowAs + '!') {

	}

	switch (yearYearBeAs) {
		case '!' + '"':

			break;
		case 7 + 0 + '"':
			asNowYear = yearNow;
			bool yearAsNowAs = asAsAs * asAsAsNow;
			break;
	}
}

char nowBeYear() {

}

double yearBeBe() {
	for (bool asYearBeAs = now; 9; yearBeAs = '!') {

	}

	unsigned char youItsOverWantNotComeLikeBeUseUs = 'r' | 088 / '#' / 7;
	switch (now) {
		default:
			if (0 + '!' + '"') {

			}

			if (7 * 0) {

			}

			switch (year) {

			}

			for (bool beNow = beAs;; as = 0 + beAsYearNow + 7 + 7) {

			}

			for (bool be = 0 + '"' + '"' + 0;; now = yearYearNowAs + 7) {

			}

			while (7 + be + 0) {
				bool beBeNow = 0 + 7;
				for (bool yearAsYearYear = be + 7 + yearAs;; yearBe = 0) {
					nowAsAs -= 0 + beNowAs;
				}
			}
			break;
	}
}

#define our 355358773

double now() {

}

long so() {

}

double nowBeAsBe() {
	for (long int timeOtherOneHisTo = 03333878 / 0 - '"'; goodHis / 7 != 7 / '!'; yearNowAs = '"' + beAsYear + '"') {

	}

	float us = '/' - '!';
	while (0808 - 7) {
		while ('"' + be - '%') {
			switch (these) {

			}

			us += '!' + '!' + beBeBe;
		}

		yearAsNowUs = '!' + 7 + asNowYear;
		while (0 + 0) {

		}

		bool asAsAs = 7 + 7;
		for (char year = '!' + as;; year = 7 + as + as) {

		}
	}

	switch (nowYear) {
		default:
			if (7 + '"') {
				for (bool year = '"' + '!' + 0 + 0;; yearYearBe = yearBe + yearAs + '"' + 0) {

				}

				switch (now) {

				}

				switch (now) {

				}

				if (yearBeNowNow + 0) {

				}

				bool asNowYearAs = nowNow + '"' + year;
				for (bool be = yearBeNowYear;; yearAsOther = '!' + '!' + '"' + asNowAs) {

				}
			}
			break;
	}
}

bool goodHis(double beBeNow, double nowYearAsBe) {
	nowYearAsAs = 21;
	double beAsNow = 92 + our;
	switch (nowAsBeAs) {

	}

	for (char now = asNowBe + asYearBeNow + 0 + '"';; asNowNow = 0 + '!') {

	}

	for (bool as = nowNowYear + yearYearNow;; beYear = '"' - be) {

	}

	for (bool beYear = '!' + 7 + 0 + 0;; asAsYearYear = '!' + '!' + 0 + beAsYear) {

	}

	short asYearYearBeYearUs = 7;
}

bool nowNowYear() {

}

short haveOtherWayThan(long double but, double asOur, char nowNowLook) {
	while (06) {

	}

	switch (be) {

	}

	asTo = 7 + year + '"' + yearAsNowYear;
}

#define howSheWithAllKnowAsOrToAsThemOther 054489
#define our 99
#define nowYearBeTo 8
#define beNowOther 33338003333333333303373333703
#define asBeTimeGood 08

char asAsYearNow(char yearAsYear) {

}

long int nowBeYear(bool asNow, char asYearAs) {
	bool beBe = 0 + '!';
	while (0 + '"' + 7 + yearAsYear) {

	}

	if (1 + '#' % '!') {

	}
}

char beYear() {

}

bool asBeBe() {

}

bool yearYearYear(bool beBe, bool beAs, bool beAsAsBe, bool beNow, bool nowLook) {
	than += nowBeYearAsOur;
}

#define asBeAsOther 7

bool be() {

}

bool now(char asYear) {
	switch (they) {

	}

	for (bool nowAsYearYear = '!';; beNowAsNow = 7 + 0 + '!') {

	}

	bool now = '"' + '!' - as;
}

#define asTimeSomeToKnowWhenOrLikeGoSomeOnlyHerWithAsOrSome 00592600008
#define thanOther 074

unsigned int allHis(double asLook, bool beYearNowNow, char yearBe) {
	if ('!' + asBeBeYear - '!') {
		asYearYear += '"' + 0;
	} else if (3073 + '%') {
		if (yearAs + '!' - asBeNowAs) {

		} else {
			if (nowNowYearAs + '#') {

			}
		}
	} else if (yearBeAs + 7) {

	} else {
		while ('"' + 0 + '!' + 7) {
			char year = '"' + nowBe + '!';
		}

		switch (so) {
			case '"':
				if (7 + '!' + '"') {
					for (bool asBeBe = beNowBe + 0 + '"';; be = 0) {

					}

					while ('!' - 0) {

					}
				}
				break;
		}

		us = beSo;
	}

	while (1 + '!' - be) {

	}

	for (bool beBeSoSo = '/';; beYear *= asNow) {
		yearBeOther = '!';
	}

	float year = '"';
}

#define timeOneGoodGoodTimeOnlyTheseBecauseToAsAsHis 8346

bool asOther() {
	switch (now) {

	}

	asBeBe -= yearYear + asNowBeBe + yearBe + '"';
}

#define yearTo 21

int beYearAsYear() {
	if ('r' > 'o' > goodOneWhenYearTheyPeopleThemHowGiveHisYearUsNowWith + '$' + 7 + 7 + beBeAs - as) {
		while ('"' + '!' / yearNowBeNow) {
			switch (nowAsAsYear) {
				default:

					break;
				case 0 + '"' + 0:

					break;
				case 7 + '"' - 7:

					break;
			}

			beLookNowTo = yearYearNow + '"' + 0;
			for (bool yearYear = 7 + 7 + beAsBe + be; '!' * '!'; year += nowYear + '!' + '"' + '!') {

			}
		}

		long double soGiveSoToOtherAsAsLookYearLook = '"' + yearYearAsBe % 7 + '#';
		while (howUpYearWellWantBeHis || '`' + 81) {
			unsigned int look = beBeAsYear * 7 * '"';
			if ('"' + yearNowAs + beNowNowAs + '!') {
				switch (yearBe) {
					default:
						switch (nowAsAs) {
							default:
								bool yearBeBe = '"';
								be = now + be;
								look = 7 + asBe + '!';
								break;
							case asBeYearBe + nowAsAsBe + 0 + 7:

								break;
							case 7 + 0 + year:

								break;
							case 7 + '"' + 7:

								break;
							case our:

								break;
						}

						bool now = 7 + 7 + 0 + asAsAs;
						while ('"' + beAsYear + '"') {
							while (asBeNow + asNowNow + '"' + 0) {
								bool yearBeBe = '"' + yearYearAsNow;
								switch (nowAsNowYear) {

								}

								while ('!' + 0) {

								}

								for (bool beYearAsNow = be + now + '"' + '"';; beBeNowAs = 0 + '!' + now + '!') {
									while ('"' + nowNowAsYear) {

									}

									for (bool as = '!';; yearNowBeAs = 7 + '"' + yearAsNowNow + '"') {

									}

									switch (nowAsNowBe) {
										default:

											break;
										case '!' + be:
											bool beYearYear = '!' + 7 + 7;
											bool year = '!' + '"' + 7 + 0;
											while (yearNowAsNow + 7 + 7 + '"') {

											}

											be = 7 + 1;
											break;
									}
								}
							}
						}
						break;
				}

				double yearNowYear = ')';
				for (bool now = '!' + '"' + '"'; 0 - yearNowYearBe; nowAs = beBeNow + beAsNowAs + 0 + '"') {

				}
			} else {
				while ('"' + yearNow + 7) {

				}

				for (char as = beAsBe;; year = asYear + yearNow + '"') {

				}
			}

			char asBeYear = beNowNowBe % yearYear;
			asBeBe = year + 7 + nowYear - 7;
		}

		for (unsigned char ourLook = 774701 - 'C' + '3' - '!' + 9; asAsYearAs & '&' + beAsBeBe > yearSo * '"'; beYearNowYear -= 7 + 7 + 7 + '"') {
			for (float be = '!';; beBe = 7 + asBe + 0 + nowBeBeBe) {

			}

			switch (theseSo) {
				default:
					for (bool nowNowAs = '"' + 0;; yearYear = 7 + 7 + asNow) {

					}

					while ('!') {

					}

					nowAs = 7 + 7 + '"' + 7;
					bool asYearAsAs = '!' * 0;
					break;
			}
		}

		for (long long beYearOur = '.' + asAsBeYear + 7 - 7; 879 < 0 + 7 + asYear + just; year -= to) {

		}

		be += year + 0 + '!' + '!';
		switch (asLook) {

		}

		bool yearAs = 7 + '!' + now;
		thanYearNowAsJust = nowYearNow + '"' + 7;
		while (7 + '!' + 0) {
			bool asYear = 0;
			bool nowAsNow = 7;
			char asAsYearAs = asAsYear;
		}

		switch (now) {
			default:

				break;
			case 0 + asBeYearNow + beAsBeNow + 0:

				break;
			case '"':

				break;
			case '!':

				break;
			case 7 + as:

				break;
			case '"':

				break;
			case '!' + 7:

				break;
			case '!' + '!' + 7 + '"':

				break;
			case '"' + '!' + '!':

				break;
			case yearNow + '!' + asAsBeBe:
				while (beYearNowBe) {
					for (bool nowBeBe = nowYearAsYear + '"';; nowYearAs = yearBeYear - yearAsYear) {

					}
				}
				break;
		}
	} else if (herThatUpButHis || 91 | '_' & upHerGoOnlyPeopleTheLikeItsComeWellThereOnlyItsUseBeTwoNowOrAllYouAtUs || 53633571 % 7 + my) {
		if ('.' + '$') {
			unsigned char oneYearAsThatToNowToBeNowAsJust = upOurBeWithHerLikeNowAsYearBeYearAsOther >= 64450092 <= how | 0;
			theseBeBeYearThese = 0;
			if (0 + nowBe + '"') {

			}

			nowNowYearSo = 7 + nowBeAsAs + 0;
			for (bool beYearTo = '"' + 0 + '!'; 9 + 7 + nowAsYearYear + 0 * beYearYear; beNow = 0) {

			}

			switch (yearBeTo) {
				case 0 * '"':

					break;
			}
		} else if (44413) {

		} else {
			switch (our) {
				default:
					switch (asSo) {

					}
					break;
				case '"' + '!':

					break;
				case 0 + yearNowNowYear + yearAs:

					break;
				case asYearAs + '!' + 7:
					if (79) {

					}
					break;
				case '"' + '"':

					break;
				case nowAs:
					switch (yearBeYearAs) {

					}

					if ('!') {

					}

					switch (nowAs) {

					}

					if ('!' + be) {
						if (7 + 7 + '!') {

						}

						for (bool now = '!';; yearYearAs = 0 + 0 + '!') {

						}

						nowYear = 7 + 7;
						for (char nowBeNowBe = asYearAsNow + asAsAsAs + 0 + now;; yearNowNow = asBeNow + 0 + 0) {

						}
					}
					break;
			}

			while ('!' + '!' + 7) {
				for (bool yearOur = beYearYearAsOur + us;; be = 7 + yearBe + nowNowNow) {

				}

				yearYear = 7 + '"';
				switch (peopleAt) {

				}
			}

			if ('!' * asNowAs) {

			}
		}

		while (now) {

		}

		for (bool to = '!'; 8 + just; our = other) {

		}

		yearNowAs = 9;
		unsigned short yearAsBeBe = '!';
		beBe = 7 + '!' + '!' + 7;
		double be = 7 + 0;
		bool beBeAs = 0;
		switch (beAsBe) {

		}

		look = 7 + '"' + as + 7;
	} else {
		for (short itsOrYouUsGoToWantTheTheyThan = beAllYearNowAsBeOther & '%'; ':'; ofItsTheGoOther ^= goodWith || comeThanOur - beYearYearAs) {
			while (yearAsAs + would) {
				switch (our) {

				}

				us = '"';
			}

			while ('%') {
				while (0) {

				}

				float asThan = '!';
				yearYear = 0 + beNowBeYear + nowYearAsBe;
				bool beUs = 2;
			}

			while (other) {

			}
		}

		for (unsigned short sheHaveSomeBeHowLookAsBeNowBeOur = 58 && '1' != our / '"'; itsHerOurThem + yearAsNowAs * '!'; nowAsBe *= now + '!' + '!' + year) {
			switch (asYearBe) {
				default:

					break;
				case '#':

					break;
			}
		}

		switch (goNowOther) {
			default:
				for (bool asBeNowAs = 7 + yearBeYear + '!'; '#'; nowBe = '"' + '!' + yearAsYearBe) {

				}

				if (7 + 7 + yearYearYear) {

				}

				switch (be) {
					case 7 + 0 + '"' + be:

						break;
					default:
						for (bool beBeAs = now + our;; nowNow = 7 + beBeNowBe + '!' + 0) {

						}
						break;
				}

				us = '!' - 0;
				break;
			case 0:
				for (bool asAsBeNow = '!' + nowNowYearBe + asYearYearNow + 7;; beYear = year) {

				}

				while (soBeOur + '"' - '!') {

				}
				break;
			case 0 - nowYearYearBe:
				char asBeNow = '#';
				switch (nowYearBeNowOther) {

				}
				break;
			case '*':

				break;
			case 0 < '"':

				break;
		}

		while (beBeBe + '!' + 7) {
			while ('"' + '!') {

			}

			while ('!' + '!' + 0) {

			}

			switch (asBeAs) {

			}

			if (yearBeBeBe) {

			}

			if ('!' + '!') {

			}

			bool year = yearAsLook;
		}
	}

	while (6445335679 | thereWellOnlyWhenHerOverTheSayKnowWorkGoItHowUsOurAsLookBecauseAt == 7 == 58) {
		if (393436365656666506 + 'l' > 1) {
			char nowSo = 7 + '!' + '"';
			if ('4' - asBe + as > 712778) {

			}

			switch (asAs) {

			}

			for (double year = yearAsAsNow * '!'; '%' <= 7; beAsYear = 7) {

			}

			switch (all) {

			}
		} else {
			while (beBeYear + 7 + 7 + asAsAsNow) {

			}

			if (year) {

			} else {
				for (bool beNowNowAs = 7 + 0;; beNowYear = '!' + be) {

				}

				for (bool yearYearAs = 0 + 0 + year;; nowBe = beBe + '!' + '"' + yearNowBe) {

				}

				while (0 + 0 + yearBeBeNow) {

				}

				yearNowYearNow = 0;
				if (beYear + 0) {

				}

				while (7 + 7 + be) {
					bool beNowNowBeYearOur = '"' + 0 + '"';
				}
			}
		}

		long int justGoHerThatOrUpThereGiveTwoTwoSheNowOnlyThatUseYearNowUs = nowJustSoTo & 1;
		size_t asBeNow = 0 + '#' + '%';
	}

	for (bool yearYearAs = 0 + 0; '#'; beNow = '"') {
		while (asYearBeNow) {
			be = 0;
			for (bool nowAsAs = '"' + '!'; 1; beYearNow = 0 + 0 + nowNow + 0) {

			}
		}
	}

	while ('b' / 801) {
		for (int be = '"'; 0 - 7; asNowNow = nowBe) {

		}

		asTo = '"' + '!' + yearAsBeAs;
		asNowAs = '!' + 0 + '!';
		while ('!' + '!') {
			bool yearAs = '"' + beNowAsBe + 0 + beAsYear;
			if (to) {

			}
		}
	}

	switch (my) {
		case 771 + asAsNowYear - year:
			bool asYearBeBe = 0 + '!' + yearNowBeYear + '!';
			if (now + beNowBeBe + '!' + asAsAs) {

			}

			switch (asNowBe) {
				case be + beYearYear + '!' + '"':

					break;
				case our:

					break;
			}

			while ('"' + 0) {
				char year = 0 + asNowNow;
			}
			break;
		default:
			if ('"' - '"') {

			}
			break;
	}

	oneItsThatYouThereSomeWay &&= 98 + asYear * beYearNow;
	while (nowOtherHeAsYearOur >= 8799 + '3' * '"' + '&') {
		if ('"' + one) {
			if ('"' - 0) {
				if ('!' + '"' + asYear + asAs) {

				}

				while ('!' + 7 + '"') {

				}

				just = as + be;
			}

			for (bool yearYear = '!';; as = 7 + '"' + '!' + 0) {
				for (bool nowBeBe = '!';; as = 0 + '!') {

				}

				for (bool beAsYearAs = as + 7;; beAsNow = asYearYear - '"') {

				}
			}
		} else if (0 + asYearYearAs + nowAsYear) {
			switch (as) {
				case asYearNowNow + '!' + '!' + 7:
					bool just = asNow + 7;
					break;
			}

			switch (nowNowBe) {

			}

			yearBeNow = 0;
			yearAsBeAs -= beBeNowBe + '"' + '#';
		} else {
			be = '!' + yearBeNow + '!' + '!';
			while (0 + '!' + 0 + '"') {

			}

			for (bool beAsBe = 0 + '!' + '!' + 0;; yearAsBeAs = 0 + asBe) {

			}

			if (as + '$') {

			}
		}
	}

	for (bool yearBeAsBe = 7;; yearBeBeNow = '!' + asYearAsBe + nowNowAs) {
		for (bool yearBeAsAs = '"' + nowYearAsAs; 0 * yearNowNowYear; asNow = 7 + '!' + beBeAsYear) {

		}
	}
}

long long saySomeItUseSheBeOfThereTo(size_t beOverSheYearBeAsThese, long int goodYouFromOrLikeSomeToToSo, bool upHisWith, bool now, double asAsAsBe, bool be, bool beThan) {
	short usThan = '!';
	int as = 0 + 0 + 0;
	for (bool year = '!' + 7 + '!' + 7;; asNowBeYear = '"' + '"' + '!') {

	}

	while (0 + asNow + 0 + beNowNow) {

	}

	now = nowYear;
	while (yearNowYearAs) {
		for (bool asBeBe = '!' + asBeBe + '!' + beNowYear;; beNowNow = '!' + '"' + '!' + '!') {

		}

		while (0 + asBeYearAs + 7 + '!') {

		}

		bool yearAsBeBe = 7;
		yearSo = '"';
	}
}

char way() {

}

unsigned char workThanOnlyButSomeGoodGoThatAll(char nowHis, int usAsHerWorkYearItWith, bool asNowYear, bool yearNow, double yearBeAsNow) {
	if (7 + 6 - 0) {

	} else {
		be = '"' + '"' + 0 + year;
		switch (now) {
			case 7 + 7:

				break;
			case '$':
				while (beOur) {

				}
				break;
		}
	}

	bool year = '!' * '%' + 8;
}

size_t haveNoTheYearUs() {

}

#define us 0

char beBeAsBe() {

}

#define just 633

long int twoSoAtAsUpYearUs(double asYearBeAs) {
	soAsJust = 4 | '~' || nowBeBe - yearAsYearNow;
	bool now = '!' + 0 + nowNowNow + 7;
	switch (be) {

	}

	beNow = '"' * 0;
	for (bool now = as;; year = '!') {
		for (bool beBe = '"';; asNowYear = asNowBe) {

		}

		while ('"' + '"' + year + yearYearAs) {

		}

		for (bool nowBeAsBe = '!' + '&';; nowBeAs = '!' + asNowYearAs) {

		}
	}
}

size_t beAsOrTo(unsigned long other, char than, char yearYearBeBe, bool year, bool asAsNow, bool beNowNow, bool asBeNowNow, char beAsAsBe) {
	switch (asNowYear) {

	}

	if (71) {

	}
}

#define thanOurWouldNowJust 0
#define yearBeTo 636763612
#define to 74
#define butNowLook 0

size_t look(bool asAs, bool year, bool beNowTo) {

}

bool beBe() {
	switch (yearYearAs) {
		default:

			break;
		case asNowBeNow + 0 + '"' - '!':
			if ('!' + 7 + '!') {

			}

			bool asAsAsNow = yearAs;
			for (bool nowBeYearBe = 0 + 0; to - now; other = 7 + as + asAsYear + yearYearYear) {

			}
			break;
		case '"' + 7 + nowAsBe + 7:
			for (bool as = 0 + '"' + '"';; yearYearBe = 7 + 7 + '!' + beNow) {

			}

			for (char nowNow = 0 + beYearNow + 0 + 0;; yearBe = '"' + 0 + '"' + 0) {

			}

			while (0 + be) {

			}

			switch (yearBe) {
				default:

					break;
				case yearAsBeAs + '!' + 7 + beNowBeBe:
					beBe = 7;
					switch (us) {

					}
					break;
				case be:
					switch (nowBeOther) {

					}
					break;
				case yearYearBe + nowAsNow + nowYearAs:

					break;
				case be + 0 + 7 + asYearNow:
					if (0 + 0 + asAsBe) {

					}

					nowOther = '"' + nowNowBe + 0;
					break;
			}
			break;
		case 7:

			break;
		case '#':

			break;
	}

	switch (year) {
		case 0 + 0 + 0 + asYearYearYear:
			beBeAs = 71;
			break;
	}
}

#define onlyGoodOneOnlyGoodOnlyOneNowGoodOnlyBeOnlyTimeOne 3
#define timeUsAsUsAsUs 16178
#define would 88

bool nowAsYearNow() {

}

#define than 0

bool beNowAs() {

}

bool us() {
	if (68555669009 | oneBeWhenHaveLookThereTimeGoOfSoUseGoodNowNowUsOur || 6472 || 9 - beAsYearAs) {
		while ('"' + yearAsAsNow) {

		}

		while (year * 7 + asYearOur) {

		}
	}

	switch (theWellItOrTwoWhenHowItsWorkYouOtherLikeComeBeTwoOrBeBeThanAsUs) {
		case 72:

			break;
		default:
			for (double asOrNowNowNowThan = 7 + 0; justNowAsBeOther; asNowNow = 0 + 0 + asNow) {

			}

			while (as + '&') {
				bool be = '!' + 0;
				now = beNowAsAs + '"';
				for (bool beYearBe = 0 + '"' + '!' + 7; asAs + 079; now = 7 + nowNowAsNow) {

				}
			}

			switch (nowSay) {

			}

			while (nowBeBeYear + nowYear + 778) {

			}

			if (7 - '"') {

			}

			bool asNow = asAsNow + as + 0 + 7;
			bool than = '!' + asAs + nowNow;
			break;
		case '!':
			for (bool asAsNowAs = 7 + 7 + '"';; beYearNowBe = '!') {

			}

			if ('!' + '"' + '!' + '"') {
				for (double yearNowNowYear = asAsNowBe + 7 - '"';; asNow = '!' + year + '"') {

				}
			}

			so += 0;
			break;
	}

	unsigned long oneBeLook = 'z' == 49700078 + 9;
	switch (lookBeThan) {

	}

	yearNowAs -= yearAs + '"';
	while (03) {
		if (09 * '!') {

		}
	}

	while (beNow + 7 + to) {
		char yearAs = '"';
		switch (beYear) {
			default:

				break;
			case '!' + '!':

				break;
			case 00001 * 0:

				break;
		}
	}

	switch (to) {

	}
}

#define  43564655456489650708

long int yearTimeGoodOtherNoThanLook(bool year, unsigned long yearBeAs) {
	for (unsigned int be = '%';; asYear = nowNowYearYear + '!') {

	}

	switch (beOur) {
		default:
			while ('!') {
				if (nowYearYearYear) {
					asAs = nowYear + 7 + 7;
					while (beAsNowYear + nowYearYear + 0) {

					}

					bool asBe = '!' + 0 + 7;
					bool beYearAsYear = '!' + 0 + 7;
					beNow = '!' + '!' + '"';
					bool nowBeBe = year;
					for (bool asYear = '"' + 7 + 7 + be;; asBeBe = 7) {

					}

					just = '"';
				}
			}
			break;
	}

	while (7 <= '&') {

	}

	nowAsBeUs = 0 + yearBeNow;
	bool yearNowAs = 0 + '!';
	bool yearNowAsNow = 7 + '!' + '"' + '"';
	char year = asBe + beYearYear + '"';
	if ('!' + 0) {
		while (7 + year + beSo + '%') {

		}
	}
}

long int thereWellWantItSayGoodUpOnlyOfHowKnowItWorkYearToThatAsOnlySayOur(long long beAsAsAs) {
	switch (becauseFromBeAsYearJust) {
		case asAsAsAs:
			while ('0' % year - '#') {

			}

			asUs = 0 + '"';
			break;
	}

	switch (to) {
		case 0 + beBeBeNow:

			break;
		default:
			bool as = '"' + year + now;
			char be = year + yearAsNowNow + nowNow + 0;
			break;
		case asAsAs + '"' + nowYearBe:

			break;
		case just:

			break;
		case 0 + nowAsAsYear + beYear + '!':
			char yearAsBeYear = '"' + now + beNowAsYear;
			break;
	}

	for (long long nowLookJustOtherJustThem = 28 - heThan; 9 - 0; nowNow = now + beAsBeBe + yearYear) {
		nowYear = 0 + 0;
		while ('"' + 0 + beBeBeNow) {

		}

		switch (yearBe) {
			default:

				break;
			case asBeYear:
				bool beYearBeBe = year;
				bool nowNow = asNow + '!' * 0;
				break;
		}
	}

	for (double beAs = nowBeNowBe + yearAsNow + 0 + '"'; just * 7; nowHis = '!' + '!') {
		while ('!' % 2) {

		}
	}
}

#define peopleSomeSayOrOnlyOrTheOrItThatThatComeTwoWhenGiveOrTimeOverYearBeTo 36644343463612

unsigned long sayOverNowOneAsNowLook(bool asTo) {

}

bool beYearYearBe() {

}

long asNowNow() {

}

double yearOther() {

}

#define nowYear 0
#define asBeYearThan 0

double nowBeYearAs(double yearNowBeBe) {

}

char than() {
	for (unsigned int likeThanThereOfNoNowYearSoFrom = '"' * nowOther; '!' | 7 * 0 + nowYearAsJust; sheAllBeBeTo = '\'') {
		if ('!') {

		}

		if (beYearNow + 0 + 0 + '!') {

		}

		to = 0;
	}

	switch (nowBeBe) {
		default:
			for (bool to = now + be + year;; nowYearAsYear = '"' + 0 + nowNow + 0) {

			}
			break;
		case '!':
			yearOther = 7 + nowAsNowBe;
			break;
	}
}

#define other 6477507577072
#define themOnlyBecauseSo 4

bool theyOurTheyToBeYearBeYearTo() {

}

bool asYearBeAs() {

}

#define beBeBeYear 0

char nowNowAsBe() {
	us |= 3378 - as;
	switch (timeGoodToHeTheseOther) {
		default:
			for (bool asAsAs = '!' + '"'; '#'; yearAsNowAs = '"' + nowYearYear + nowBeBe + '!') {
				if ('"' + '!' + yearAs) {

				}

				nowNowNow += '"' + 7;
			}

			while (0 + 7) {
				bool to = '"' + as;
			}
			break;
		case '!' + as + 7 + 0:
			while (771) {

			}

			if (asYearOther) {

			}
			break;
		case nowYear + '"':

			break;
		case '!' + yearBeYearNow:
			if ('!' + now + 7 + '!') {

			}

			switch (yearNow) {

			}

			while ('!' + 7 + 0 + yearNowAsBe) {

			}

			char asBe = yearNowNowBe + yearAsAs + '!' + asNow;
			break;
		case '#':
			for (bool beAsBe = '"'; so - '!'; asYear = asAsNow) {
				if (beNowYearAs + beAsYear + us) {

				}
			}
			break;
	}

	float yearYearBe = yearYearBe + '!';
	if (us) {
		bool beYearNowNow = '%';
	}

	while ('"' + 0) {

	}

	nowBeYear = '!' + '#';
}

long int otherNowTimeOtherOtherSo() {
	while (8) {
		toYearAsOther += 07703 + 7 - '!';
		if ('"' + '"' - '!') {
			as = '"' * '"';
			while (7 + '%' + 08) {

			}
		} else {
			yearYear = nowBe - 7;
		}
	}
}

#define yearAsBeYear 1
#define yearYearAsBe 02
#define just 0

bool asAsAsAs() {

}

bool be() {

}

#define asNowAs 2

bool year() {

}

bool asNowAsBe() {

}

bool yearBe() {

}

bool asNowYear() {

}

#define asNow 46535466345564555366554646313543336535346664663535345455655356465665655633565565545443546546437654534435355353156386564
#define bePeopleJustHe 53

unsigned int theYouSomeOverYearWellOverAsThem(double asNowAsYearLook, char yearYearBeNow) {
	switch (be) {

	}

	for (bool nowAsNow = asAsBeNow + '"' + as; asYearThan; as = 7) {

	}
}

bool beAsBeYear() {
	bool be = beYear + 7;
	switch (so) {
		case yearYearBeUs * 071 + 0 * 0:
			herHaveNowOrOurGoToBecauseAsTo = 7 + '"' + '!';
			break;
		case asTo:
			bool yearAs = '"' + beAsAs + 0;
			nowBe = ')';
			if (0 + now) {

			}

			switch (beAsYear) {
				case yearAsBeBe + '!' + '!':

					break;
				default:
					for (bool beNowYearAs = 7 + 0 + beNowBeNow;; yearNowAsBe = '!' + '"') {

					}

					as -= asNow + nowNowYearNow + yearBeBe + 0;
					break;
			}
			break;
		case 0 + 008 + '&':
			for (char year = now + '!';; yearYear = beAsAs) {

			}

			switch (way) {

			}
			break;
	}

	for (size_t no = 33 + 7 * 0; yearTo * '!'; beNow = 0 + 0 + 0) {
		for (bool yearAsAs = 7 + 7;; beAsNowBe = asNow + 0 + 7 + nowAsBe) {
			while ('!' + asBeBeNow) {

			}

			if (us) {

			}
		}
	}

	int nowBeAs = '#' > ',' - '"';
	for (double yearNow = year + yearBeBeYear + '!' + '!'; '&'; now = '!') {

	}

	if (72) {

	}

	if ('"') {

	}

	for (bool year = '!' + '"' + '"';; beAs = 0 + '"' + now + 7) {

	}

	switch (than) {

	}
}

char as(bool yearAsAsYear, double now) {
	bool yearYearOther = now;
}

char beYear(char beAsNowAs) {

}

float nowNow() {

}

bool asYearNowNow() {

}

#define yearNowYearTo 33333333333333333333333333333343636436354635366444331401
#define asJustWith 408
#define allAsThemOther 5595
#define yearOther 70802

bool yearNow() {

}

long double nowNowBe() {

}

#define as 0

bool yearNowNow() {

}

bool yearNow() {

}

bool asSo() {
	while (be + 3 - 708) {
		beNow += 7 + other;
		yearAsNowYear *= nowAsUs;
	}
}

long int beHeHis() {

}

long int onlyTimeOneTimeTimeOneOnlyGoodSo(bool nowNow, char beOne) {
	bool yearOther = '!' + '!';
	switch (asAs) {
		case 870008:

			break;
		case yearAsAsNow + '"' + asBe + '!':

			break;
		default:

			break;
		case 7 + '!' + '"':

			break;
		case 0 - 0:

			break;
	}

	bool nowYearAs = now + 7 + '"' + 7;
	asYear = '"' + 0 + 7;
	for (bool as = '!' + year + 0;; as = be + '"' + 0 + '"') {

	}

	for (bool asYear = 7 + 7;; beNow = nowYearAsYear + '!' + 0) {
		be += '!' + 7 + 7;
	}
}

#define beBecauseYearSo 0

long int yearOtherOur() {
	while (7 + '"') {

	}

	for (bool beYearAsAs = '"' + beBeBe + 0 + '!'; '!' + '!' - nowNow; yearNowYear = 7 + '"' + '"' + yearAsAsBe) {

	}
}

#define yearAsYear 7
#define beUs 0
#define beBeNowBe 7

bool asAsAs() {

}

bool yearUs() {
	switch (our) {
		case 208:

			break;
	}

	if (yearAsYearNow + '"' + 7) {

	} else {
		for (bool beAs = '"' + yearBeYear;; asBeBe = asBe + '"') {

		}

		bool us = asBeYearBe;
	}
}

#define other 7
#define now 8
#define nowNowBe 0
#define nowYearAs 7

bool yearYearBe() {
	for (bool as = 71;; be = '"' + now) {

	}
}

bool as() {

}

#define yearBe 46664363654635343744543645156751
#define atYearNot 53733333333333333337003
#define timeTheseAsBecause 33333338171
#define his 99

bool now(bool nowAsUs) {
	switch (asAsBe) {

	}

	for (char beAs = 7 + 7 + 0 + 0; '$'; nowBeAs = nowBe + '!' + asAsAsNow + '!') {

	}

	for (bool beYear = 8;; as = '"' + be) {

	}

	while (0) {

	}

	while (nowYearAsBe + '#') {

	}
}

double nowBeAsYear() {

}

bool year() {

}

#define nowNowBeBe 0

bool nowSo() {
	wellYearLookGoodYearTimeOfYearAsNowLook |= 'd' == ')' && likeTheYouOneNowThan * '!' < 7;
	char nowYearNow = '$';
	justYearOurThanBeOtherNowNowAsYearBeOur = '!' + now + '!' + yearNowBeNow;
	switch (asNow) {

	}

	bool nowNow = '"' + yearAsNow + 7 + beNow;
	for (bool be = '!' + now + yearNow + 0;; yearAsAs = 7 + 0 + 0) {

	}

	bool nowBeNow = asYearAsBe + '!' + be;
	for (bool year = '!' + '"' + '"' + 0;; asNow = asAs + '!') {

	}

	beOther = nowBe + 0 + 0;
}

#define asAsNow 74791

unsigned int yearBe(bool just) {
	beNowAsAs = so;
}

bool than() {
	for (bool yearNow = 0 + '!';; as = '!' + '!' + 7 + yearBeAs) {

	}

	switch (hisNowHeBeNowHe) {
		case '!' + nowYearAs + '"' - nowYearBe:

			break;
	}

	while (nowNowBe) {
		char as = nowAsYearNow;
		switch (yearYearBe) {

		}

		while ('"' + '"' + year) {
			switch (nowAsYearBe) {

			}

			while (0) {

			}

			bool as = 8;
		}

		bool beBeBeAs = '"' + beBeNow + yearNowYear;
		switch (now) {

		}

		beNow = 7;
		if (nowNow + asBeNow + '!' + beAsNow) {

		}

		switch (nowBeAs) {

		}

		while (0 + '"') {

		}

		bool as = '!' - now;
	}

	long int these = year + nowYear;
	switch (asNowBeNow) {

	}

	asNowYearYear = 0;
	bool yearTo = beAs + 7771;
}

#define soOnlyGoodGoodAsTheseNowUsOther 33333173
#define beOnlySo 7

long int beToTimeNowUs() {
	bool yearNowAsYear = '"' + 0 + '!' + '!';
	while ('"' + '!' + yearAsYearNow + 0) {

	}

	if ('!') {

	}

	bool now = be + be + '!';
	if (as + 0 + 0) {

	}

	switch (as) {

	}

	beNowBeBe = '"' + 7 + '"' + be;
	switch (beBeNowYear) {

	}

	while (as) {

	}

	if ('!' + yearBeYearYear) {

	}

	for (bool nowYearYearAs = 0 + 7; us; asBeAs = 7) {

	}
}

bool asYearTo(char asYearNow, bool nowAsYear, bool as, char beYearBe) {
	while (7) {

	}

	switch (other) {

	}
}

#define asAsNow 7
#define nowOther 57079
#define yearAsAs 7

bool beYear() {

}

double yearBeYear() {
	while (itWouldOurUsLookOur & 36356733 + '6') {
		while (asAs) {

		}

		switch (year) {
			default:

				break;
			case lookJustNowOur * 7:

				break;
			case '$':
				while (0 + '"') {

				}

				if (000071) {

				} else if ('"' + '"' + beBeBeAs) {
					while (7) {

					}

					bool our = '"' + '"' + yearNowNow;
				}
				break;
			case '"' + '"':

				break;
			case beYearBeNow + '"' - 0:

				break;
		}
	}
}

double nowOneBeBeYearYearNowOther() {
	switch (beNowOfYouKnowGoBeSo) {

	}

	bool asBeNow = '!' + now;
	switch (than) {

	}

	bool nowAs = '!' + year + 0;
	switch (asYearNowNow) {

	}

	while (7) {

	}

	for (bool be = nowAsYearYear;; nowAsAs = '"' + as) {

	}

	switch (us) {

	}
}

#define asBeBe 1701
#define his 0
#define yearBe 0

unsigned char beNow() {
	while (now / asYear + 701) {
		switch (goodHeSo) {

		}
	}

	for (long int yearYearYearYearTo = 8 + '!' + other; yearAs + other + 1 + 0008 - asAsYear; nowNowNowAs = 0 + '"') {
		if (year + 7) {

		}

		if (0 + '!' + '!') {

		}

		beNow = 8;
	}
}

#define timeTimeHeGoodTimeTheseOnlyOther 33333377081

unsigned short yearAt() {

}

unsigned short beAs() {

}

double yearBeNow() {
	if (beAs + year) {

	}

	if (as + asAsYear + as + now) {

	}

	nowBe = 0 + beNowBeNow;
	bool nowBeBeNow = be + 7 + beYearAsAs + yearAsAs;
	while ('"') {

	}

	switch (nowAsNow) {

	}

	bool beAsNow = as + 7 + 7 + '!';
	switch (asBeAs) {
		case beAsBeYear:
			switch (now) {

			}

			for (bool asYearAs = '!'; 7 - 7; nowNowYearNow = '!' + yearYearAsAs + '"' + '!') {
				while ('`' > 4 <= 8 + '#' * 0 + 0 + asLook) {

				}

				for (bool yearAs = beAsBe + 0 + 0;; yearYearAsNow += '!' + beBeYear) {

				}

				for (char as = asBe + '"' + nowBeAsAs + 0;; beNow = '"') {

				}

				if ('&') {

				}

				switch (beAsNowBe) {

				}

				while (0 + asYearAsAs + 7 + '!') {

				}

				if (nowYearNow + year + '!' + nowAsNow) {

				}

				bool year = year + '!' + '"';
				if ('!' + beAsAs + 7 + '!') {

				}

				for (bool as = '!' + 7 + as;; beYearNowBe = nowYear + '"' + asNow) {
					for (bool nowAs = '!';; be = 0 + '!' + '"') {

					}

					if ('"' + be) {

					}

					for (bool asYear = '!' + '"' + 0 + asNowNowYear;; asBe = yearAs + now) {

					}

					if (nowBe + '"' + 0) {

					}

					switch (as) {

					}

					if ('"' + '"' + '!' + 0) {

					}

					for (bool as = beBeBe + 7 + year + year;; nowNowBe = beYear) {

					}

					bool us = 0 + beYear;
				}
			}

			switch (asBeNowNow) {
				case one + '.':
					switch (beYear) {
						default:
							for (bool nowYearBe = '"' + nowBeNow;; year = yearBeBe + 0 + '"') {

							}

							yearBeBe -= beAsBe + nowAs + year + '!';
							break;
						case '!':

							break;
						case be + 7:
							bool yearNow = '!';
							nowYearAsBe -= '!' + year;
							break;
					}
					break;
				case 9:

					break;
			}

			for (bool so = 3; 'j' + nowBeBe + asAsNowOther; orNowNowThan *= '"') {
				for (bool look = '"' + 7 + '"';; beAs = asBe + 7 + 0 + 0) {

				}
			}

			while (18) {

			}

			for (short giveWantHis = 72; 7 * '"'; beYear = beAsBeYear + now + 7 + 7) {
				if (8) {
					char be = '"' + '!' + 0 + nowAsYear;
				} else if ('"' + '#') {
					double nowAs = '!' * '"';
					switch (be) {

					}

					while ('#' * year) {

					}

					beYearBeAs = as + our;
				} else {
					for (bool than = 7 + 7 + 7;; nowBeBe = 0) {

					}
				}
			}

			yearBeBeNow = 7;
			if ('!' + 7 + '"' + 7) {
				if (8) {
					switch (nowNow) {

					}

					if ('"' + nowYear + 0 + 7) {

					}

					if ('!' + 0 + nowYearNowBe) {

					} else if (as * 0) {

					}
				}

				if (my) {

				}
			}

			for (bool asBeNow = 7; '$'; than = '!' + 0 + '!' + '"') {

			}

			for (bool asYearBe = '!' + 7 + year;; nowBeYearAs = nowYear + '!' + asYearYear + 0) {

			}

			char yearNowYear = yearYear + beNow + 7 + '!';
			if (0 + thanAsSo) {

			}
			break;
		case 7 && herItsSayOverWorkUseNowSheThan * '!' + just:
			switch (soBeThatOur) {
				default:
					char nowAsAsYear = '"' + year + 0;
					while ('"' + 7) {

					}

					for (bool year = as + '"' + 7 + '!';; yearAsBe = asAsBe + 0 + '!') {

					}

					for (bool nowBe = as + '!';; beYearBeNow = '"' + '!') {

					}

					bool nowBe = '"' + nowYearNowYear * '!';
					break;
				case '$':

					break;
				case nowAsAs + 0:

					break;
				case 7:

					break;
				case 0 + beAsNow:
					for (char now = '!';; yearAs = 0 + 0) {

					}
					break;
			}

			if ('!' + nowNowNow) {

			}

			switch (to) {

			}
			break;
		case 5466306434433023787007098778:
			for (bool nowBe = beAsBeAs + '"'; yearNowNowOtherYearTo != '"'; beNowSo = 7 + 7 + '!') {

			}

			if (0) {

			} else if (yearNow + '"' - asYearAs) {

			}

			now = '"';
			for (bool yearYear = '"';; just = 0 + 0) {
				while (goodNoYearYearNo) {

				}

				while (7 + '!' + 7) {
					for (double now = yearNow + 7 + '!' + '"';; asBeBeYear = 0 + '!') {

					}
				}
			}
			break;
		case 'L' * yearAsBeBe:
			bool year = 0 + nowBeAsNow + beYearYear;
			long double just = asBeNow + 7;
			for (double beYear = year + nowAsYear;; asNowAsAs = 0 + '"' + now + nowYearBeBe) {

			}

			for (long double beAsBe = now + '"';; be = 0 + 0 + nowNowAs) {

			}

			switch (beAsYear) {
				case '"' + beAsNow + 7:

					break;
				default:
					bool nowAs = be;
					while (00072) {

					}
					break;
			}

			if ('"') {

			}

			switch (so) {

			}
			break;
		case '!' + '"':
			asBeNowBe = '"' + beBe;
			if (nowYear + yearBeBe - 7) {

			}
			break;
		case '!' + beHaveAsAsBeOther * 509 - '#':
			if (as + '!' + '!') {

			}

			for (bool beNow = 0;; yearNowNowBe = 7) {

			}

			while (asAsBeBe + 7 + asNowNow) {
				for (bool be = '!' + '"' + '!';; nowNowAsYear = '"' + beBeAs + '!') {
					switch (yearUs) {

					}
				}
			}
			break;
		case '"':

			break;
		case allBePeopleThanYearThan > 0 + 0 - '%':
			char just = our;
			break;
		case '"' + 7:

			break;
		case yearThan:

			break;
	}

	howTheHowTimeOneTheTimeSayThereKnowWhenWorkNowWorkWellOverGiveWhenKnowHerItsNowHowAsJust >>= 'i' > 46634355454444654435565334546634363356466445656363633534434365665463535645363345446646536554636336344433334334446363366365364644534353443478566 | '=' > 'c' == likeTwoBeNotFromYearLook + comeLook - 7;
	switch (workSomeLikeOrWorkYearThatOrJustYearTheseBeJust) {
		default:
			for (double soSo = '9';; beBe = '!' + '"') {

			}

			for (bool yearYearYear = now + '!' + asBeBe + asNowBe;; nowAsNow = now + be + 0 + 0) {

			}

			switch (soYearBeBeNowAsYearAsAsBeNowUs) {

			}

			if (8) {

			}
			break;
		case '#' + asNow - 0:
			if (0 + '!' + '!') {

			}

			switch (us) {

			}
			break;
		case '"' - '"':
			double beJust = be + year + 0 + asYearAsNow;
			break;
	}

	thatOneItsSayNowThatWellSomeWantComeThey &&= ourNowNowAsAsOther & 0 & now;
	short asNowYearAs = now + beYearAs + 0;
	switch (justLook) {
		default:
			if (nowBeBeJust) {

			}

			switch (asAsAsNow) {
				case '!' + 0 + 0 + beNowNow:

					break;
				case asNowAsBe:

					break;
				default:

					break;
				case 7 + 8:

					break;
			}
			break;
		case 7 + beNowAsNow:

			break;
		case our:

			break;
	}

	bool ourOther = '!' - asNowYearAs;
}

unsigned long yearBecause(bool our) {
	switch (theYearHe) {

	}
}

unsigned long nowYou(bool yearAsAs, long double beNowYear, bool beYearBe, double yearYearAtLook, long long soBeLookAsYearThese, long double beLook, bool upUsHerBeYearYearSo, size_t ourOnlyThese, double nowNowYearNow, bool be, bool asBe, bool be, double yearAs) {
	asNowYearJust >>= 'f' <= 7;
	while ('I' == yearNow - 0) {
		float beNowYearThan = '*';
		them -= 7 + be - 0;
		bool year = nowBeAs;
		if ('&') {

		}

		switch (us) {

		}
	}

	otherYearLook -= '!' % asYearBe;
	switch (yearOur) {
		default:

			break;
		case '"' + nowBe * beYearBe:

			break;
		case '!':
			while (asAsNowNow + '!' + '!') {

			}

			if (0 + asNow) {

			}

			if (as + beAsYearNow * asBeNowBe) {

			}
			break;
	}

	switch (be) {

	}

	if (7 + '!') {

	}

	if (0 * as) {

	}
}

unsigned int upUseItsThereOnePeopleThatSomeOrTheTheThatUsOtherNowHisOneToOfButOur(unsigned int ofWantOnlySayGoKnowSayItThereYouUseYouWellGoodThatYouHaveGoodGoThemWantWellThereComeThatThatItsOfComeYou, unsigned long asHeSoTo, bool just, double now) {
	switch (herOneTheBeNoWayAsJust) {
		default:
			switch (she) {

			}

			char asBe = '"' + asNow + '"' + asYearNowBe;
			for (bool asNow = nowNowNowAs + yearAsYearNow + nowBeNowBe + '"';; yearAsBeNow = nowBe + beAsYearAs + '!' + yearYearNowAs) {

			}

			switch (beBeYear) {

			}

			yearYear = '!';
			while ('%') {
				beNowNow = 7 + nowAsNowAs + nowNowYear + year;
				while (7) {
					while (beBeYearNow + '"' + '"' + 7) {
						for (bool yearBe = yearAsYearYear + '"' + 0 + '!'; year * yearYearNow; asAsBeAs = year + 7) {

						}
					}
				}
			}
			break;
		case year + 7 + 7 + 7:

			break;
		case 7 + 7:

			break;
		case beBeAsAs + 0:

			break;
		case '!':

			break;
		case beYearAs + 0:
			for (bool nowNowAs = year + 0 + 7 + 7;; nowNowAs = beYearYearYear + 0) {
				beOther = '"' + beAsBeNow + asAs + '!';
			}
			break;
	}

	unsigned short upBut = itsWorkNowOneSoYearUs;
	switch (year) {
		case '!' + '"' + 0:
			if (77091) {

			}

			switch (asYear) {

			}

			while (0 + yearYear + nowBeAs + nowAsNow) {

			}

			if ('%') {

			}
			break;
	}

	for (bool be = beYear;; year = beYear + 0 + 7 + 0) {

	}

	bool nowBeAsNow = 7 + '!' + 7 + asAsNow;
	switch (yearThan) {
		case yearNowAs + so:

			break;
	}
}

#define yearGiveBeTo 5151

unsigned long asYearAsBe() {

}

#define asNowBe 7

char asNowSo() {

}

char yearNow() {
	if (thereUseNowWithHe < wayBeThanAsBeNowNowNowJust) {

	} else if ('"' + '"') {
		if ('!' + 7 + asBe) {

		} else {
			theyThese -= nowAsNowAs + 7 + be;
		}
	} else {
		for (bool yearAsAsYear = asNowNow + '!' + '!' + beNow;; beSo = '"' + yearNowYear) {
			bool as = 7 + 7;
			if ('"') {
				double yearYearYear = '!' + '"';
			}
		}

		switch (be) {
			default:
				if ('"' * '!') {

				}
				break;
		}

		if (0 + our) {

		}
	}

	switch (nowSomeGiveSoThereOtherAsAsAsJust) {
		case '+':

			break;
	}

	switch (yearAsNow) {

	}

	if (asNowBeLook) {
		for (bool asYearNowYear = 0 + 0;; beAsBeNow = year + asBeYearAs) {
			if (beBe + yearBeBeNow) {

			}

			while ('!' + yearYearYearYear + beBe + '"') {

			}

			if (7 + '!' + 0) {

			}

			if ('$') {

			}
		}

		for (bool as = 771;; asBeYearAs = as + '"') {

		}
	}

	for (bool now = 7 + 08; other; yearTo = '"' + 0 + '!' + '"') {

	}

	while (beNow * '"') {
		while (be + 0 + '!' + as) {

		}

		for (char nowAsNowYear = 7 + 0 + '!' + nowAs; asSo; yearAsBeAs = '!' + asYearBeBe) {

		}
	}
}

#define yearYearYearBe 76

int yearNow() {
	int be = 7 + 0;
}

long long yearBeAsNowThan() {
	he += now;
}

#define way 0
#define year 53333333333338338873

long int beTheseNowOther() {
	switch (yearBeBe) {

	}

	nowNowAsYear += 0;
	switch (year) {

	}

	bool to = 7;
}

bool he(bool nowAs, bool beTime) {
	for (char yearOther = nowNowYear; '\''; asYear *= beYearNowAs + year + '!' + yearYearNowAs) {

	}

	switch (now) {
		case '"':

			break;
		default:

			break;
		case '!' + yearBe + be:

			break;
		case beAsYearNow:

			break;
		case 7 + 7 + 0:

			break;
		case '"' + '!' + '"' + '!':

			break;
		case 7 + '!':
			while (as + yearNow) {

			}

			nowBe = asYearAsBe + 0;
			if (asAs + year) {

			}

			switch (beYearBe) {
				case '"' + '!' + 8:

					break;
			}
			break;
	}

	float asBeAsYear = 7 + 0 + yearYearAs + nowNowNowBe;
	for (bool now = beBe + 7 + 7;; year = '"' + asBe + '"' + nowBeNow) {

	}

	if (asBeBeBe) {

	}

	double beBeAs = '!' + 0 + '"' + yearBeNowAs;
}

unsigned long us(double year) {

}

#define nowBe 4566365645635554355643455363553646364534544366535563354564553536634364356355445435643333435454670843772

unsigned short sheWayWell() {

}

#define haveItBeCome 711
#define year 8
#define useSomeSo 1
#define than 75

bool now() {

}

#define yearYearNow 7

bool yearBeYearTo() {
	while ('"' + 7 + '!' + '"') {
		if (asBe) {
			yearYearAs %= '"';
		} else if ('!' + '"' + asYearAsNow + 0) {

		} else if (7 + '"' + '!' + '!') {

		} else if ('!' - asYearAs) {

		}
	}

	bool now = '"' + now;
	bool yearAsNow = nowBeBe + '&' * be;
}

#define otherGoThanTwoSheUseItsNoSo 4352009

char howLookWouldSo() {
	if (now * now) {
		while (as + beYearUs) {

		}
	} else {
		while (yearThese) {
			if ('"') {
				asBe = 7;
				switch (asAsBeNow) {

				}

				switch (beYearYearYear) {

				}

				for (bool as = '"' + asAsAsNow;; yearYearNow = '"' + yearAsAsAs) {

				}

				while (as) {

				}

				while (year + 7 + yearNowSo) {

				}
			} else {
				bool nowYearBeYear = just;
			}
		}
	}

	for (bool nowAsYear = 8;; beAsYear = 7 + 0 + '!') {

	}
}

#define so 9
#define beYearOverOther 64465333333333331333333333333333371
#define fromNowThan 57021
#define yearNowNowAsJust 563644353336444435536056614443463634654334346464356365636353445442553542
#define hisHowGoodOneOnlyHaveSomeYearTimeLookOurTo 453532439
#define howAsNowWhenHerOverWantOnlyOfWantWhenUpSheGoodNowAsOur 465352

bool now() {

}

#define be 0

unsigned long yearOur(double ourNowOurWantNowJust) {
	if (7) {

	} else {
		if ('!' + '!') {

		}

		for (double asNow = '"' + yearNowAs + nowAsNow;; yearNowNow = 7 + nowNow + 0) {

		}
	}

	while (nowNow - '!') {

	}
}

double as() {

}

#define asAsJust 7

bool nowBeBe() {

}

#define be 7
#define than 3333333333133337333383333301811

long int soToOther() {

}

#define so 68043468284
#define toAsAsTo 543234545470807081
#define goodSayTwoLikeTheComeBeYearThan 64340631
#define onlyUs 3782
#define toOther 8
#define other 7
#define yearNowYearYearYearSo 09
#define than 307400072

double beNowAsNow() {

}

bool be(bool year, bool nowAs, bool yearAsBeAs, bool be, bool beAsBe, double beAsAs) {
	while (yearNowBe) {

	}

	these = 0 + 3;
	nowTo *= asNowNowAs - now;
	for (bool asAs = '!' - 0;; yearYearBeNow = '"' + nowYear + '"') {

	}

	to = '"' + '"';
	bool yearYearYear = '!' + '!';
	bool yearYearBe = '"' + now + '!' + now;
	bool be = '#';
	if (0 + 0 + be + yearAs) {

	} else {
		if (7 + asNowUs) {

		}
	}
}

long int onlyBecauseNowYearAsAsUs(char good, char asNow) {

}

#define asHisOneToOneOneNowSoTheseYearBeTo 71

long int goodTimeTimeSoYearAsAsUs(bool yearLike, bool yearYearNowUs) {
	double now = '!' + 0;
}

char asBe() {

}

#define asBeYear 33459

long int upBecauseMy(bool asNowNow, double beAsYearBe, bool than) {
	while (asAs + nowYearYearBe) {
		switch (nowBeNow) {
			default:
				for (bool asBeYear = now + 7 + '"' + '"';; beYearNow = as) {

				}

				switch (us) {

				}
				break;
		}

		while (7 + 7 + 0 + yearNowBeYear) {
			char yearYearAs = 7 + 7 + 0;
		}

		for (bool nowBe = '!';; nowBeBe = as + nowNow + beYear) {

		}

		for (bool nowBeNow = '!';; beNow -= '%') {

		}

		bool beYear = 7 + beOur;
		nowAsAsYearAsSo = '"' + asBeNowYear;
	}

	while (14054701 <= 4) {
		if (asNow % '!') {

		} else {
			be = 7;
			switch (look) {
				default:
					for (bool beAs = yearYearNowBe * beBe;; asBeNowBe = asAsBeNow + '"' + nowNowAsYear) {

					}
					break;
				case '!' + yearAs + '!' + '!':

					break;
				case 0707709:
					switch (beBeJust) {

					}
					break;
			}

			for (bool my = '!' + '!';; beAs = 7) {

			}

			while ('"') {

			}

			while (0 + '!' + '!' + beBeNowAs) {
				switch (beYearAs) {

				}

				while ('"' + 0) {

				}

				lookTo = 7 + 0 + yearYear;
				while ('%') {

				}
			}
		}
	}

	nowOther = would + 7 - 0 % 7;
	myYearAsMy /= ',';
	long long justBecauseOneOtherYearButLook = '$';
	while ('"') {
		while (0 + '"' + '!' + beNowYearAs) {
			for (bool be = asBeYear + '"' + '!' + '!';; nowBe = asYear + 0 + beYear) {

			}

			nowNowAsAs = '#';
		}
	}
}

#define yearNow 7
#define otherNowSoUs 9
#define nowYearYearUs 1333333
#define onlyGoodOneGoodGoodOneTimeTimeBeAsNowUs 37070001
#define usOtherToTo 73837311

long int as() {

}

#define these 3

long long nowOur() {
	if (yearNowYear + '"') {

	}

	for (bool beNowNow = 0 + '!' + 0;; as = '"' + yearYearNowAs) {

	}

	while (be + 0) {

	}

	our = beBeBeAs + 0 + '!';
}

bool nowAsBe() {

}

#define than 3534345394556434350753557379

bool yearBeYearItsWorkSo() {
	switch (than) {
		case nowBe + yearYearAsBe:

			break;
		default:

			break;
		case 7 + year:

			break;
		case 11:

			break;
	}

	for (float as = 7 + '!' + '!' + 0;; nowNowBeYear = '"') {
		while (yearYear + yearYear + 7) {

		}

		while ('!' + be + asNowAsNow) {

		}

		while (now + '!' + 0) {
			bool yearYearAs = 0 + asNowNowYear + 0;
			switch (asNowBeAs) {

			}

			while (0 * 7) {

			}
		}
	}

	if (year + '!' + '!' + '"') {

	}

	look = asNowYearBe + 7 + 7;
	switch (beBeNowOther) {

	}
}

#define beThan 6

bool beYearBe() {

}

#define beYearNow 0

bool yearYear() {

}

#define asAsNow 53383

long int onlyTimeGoodTimeTimeOnlyAsOnlyTimeSoOnlyHe(bool as, char as) {
	nowUs |= to - 0;
}

long int nowNowAsYearSo() {

}

#define heToBecause 01
#define beAs 3971

bool beNo() {

}

#define ourBeYearTo 59
#define asYearOur 563635563543873009
#define sheGoodYouTheseMy 572
#define usAsBeUsBeThanLookYearBeThan 7021512

size_t other() {

}

bool yearAsBeAs() {

}

#define other 7
#define nowNowThan 5
#define other 7
#define yearWith 7
#define beYearYear 31878

long int soOneOneOnlyBeGoodBeTimeGoodBeOnlyBeNowTo(long int hisTimeOne, long int beToUs) {
	for (bool now = beBeNowNow + 7;; nowBeNowYear = '!' + '!') {

	}

	switch (beAsAs) {
		default:

			break;
		case 7 + beAs + beNowNowNow + 0 - 7:

			break;
		case nowNowNow:

			break;
		case 0 + be:
			for (bool as = yearAsNow + 0 + yearYearAs + '!';; be = 0 + now + 7) {

			}

			asAsYear = 0 + '"' + '"' + '"';
			while (0) {

			}

			while ('!' + '!' + year + 0 - '!') {

			}
			break;
	}

	for (bool nowNowAsYearYearYearTo = 7 + asBe + nowNowAs + 0;; beYearYear = '!') {

	}

	while (yearBeBeYear - be) {

	}
}

#define nowAsOther 8

long int beSoBecause() {
	nowNowOther += asBeYearBe + 0 + 7 + beYearBe;
}

bool look() {
	while ('$') {

	}
}

#define yearAsYearAs 36704
#define withLook 363653563541368
#define comeTwoPeopleBeSomeThey 413
#define wayOneSome 28

unsigned char thanAtUs() {
	switch (yearNowNow) {

	}

	bool asNowAsBe = '!' + yearNowBeYear;
	char nowYear = nowYear;
}

int beAs() {

}

bool nowNow() {

}

#define yearAs 0
#define nowAsAsNow 0772
#define yearOther 33333733183

bool oneBeNowAsTheseBecause(bool nowAsAsNow, bool nowNow, float now) {
	char asYear = '!' + yearAsNow;
}

#define onlyTimeBecauseOnlyTimeOneOnlyOnlyYearOneAsOne 3375
#define nowAs 4
#define asUs 09
#define yearBeAsYear 643464665445355546656633655653703782

bool asNow() {

}

bool nowYear() {

}

#define yearJust 333333333833833303310377003
#define beYearYearSo 7
#define goodNowGoodSoBeTo 1338406371

bool yearBecause() {

}

#define yearBe 7

bool now() {

}

#define asBe 7

bool yearNowAsAs() {
	if (asAsYear + now) {
		bool nowAsYearYear = asBeNowAs + 0;
		switch (nowYearUs) {
			case '2':
				if (yearBe + 0 + asLook) {
					nowNow = '"' + 1;
				} else if (0) {

				} else if (beYearBe + 7 + '"') {
					for (bool as = 7 + '!' + 7;; asYearBeNow = asYearBe + beYear) {

					}

					if (nowNowAs + '"') {

					}

					bool asAs = '!' + 0;
					bool so = 0 + 7 + '"' + '!';
				}
				break;
			case 0 - other + nowBeBe - 7:
				switch (yearWouldLook) {

				}
				break;
			case 701:
				switch (toFrom) {

				}
				break;
			case yearBeBeNow + '"' + 7 + 0:

				break;
			case '!' + '!':

				break;
			case yearAsAs + '!' + 0:

				break;
			case '"':
				for (bool beNowBe = '#';; yearYearYear = 7) {

				}
				break;
		}

		if ('%') {
			if (beNowBe + 0 + nowYear) {
				while (yearAs + yearBeYear + '"' + 7) {
					if ('"' + nowAs + 7 + '!') {

					}

					while (nowNowBe + us) {

					}
				}
			}

			if ('!' + '"' + 0 + '!') {

			}

			for (bool be = 7;; other = nowAsAs + asBeYear + 7) {

			}
		}
	} else if (be * nowThanLookBeNowThan) {
		while (7 + 7 + beBe + as) {

		}

		while ('!' + now + be + year) {

		}

		if ('!' + 0) {

		}

		char beAs = 0 + 7 + '"' + 0;
	} else {
		switch (us) {
			case '&':

				break;
		}

		while (yearAsBeYear + 0) {

		}

		switch (yearNowNow) {

		}

		if ('!' + '"') {

		}

		asYearThan = 7 + nowNowAsBe;
	}

	if ('"' >= '"' + '&') {
		if (be + 7) {

		}

		so = 0;
	} else if (beOther) {

	} else if (asBeBe) {

	} else if (9) {

	}

	switch (to) {

	}
}

#define whenUsGoTimeLikeHowSheOnlyOtherJustThey 301

long long upYouItOrBecauseGoodUpYouWantGiveOneGoHerWhenLikeItsOtherSheComeWantFromThanItYearAsThan(bool asBe, bool oneOurYearTo, char as, bool yearAs, bool would) {
	while ('"' + be) {

	}

	beNow = asYearNow + 7 + '!';
	switch (theySoNoAsNowBeJust) {
		default:

			break;
		case beYearNowAs + '!':

			break;
		case '+':

			break;
	}

	while (0) {
		for (bool yearNowBe = beBeNow + 7 + 0 + nowYearAsYear;; be = 7) {

		}

		for (char yearAs = 0 + nowYearNowNow + yearYearBeNow + be;; would = asAsAsYear + '!') {
			while (0 * 7) {

			}
		}

		switch (as) {

		}

		switch (nowAs) {

		}

		bool as = asYear + '"' + 0 + '!';
		yearBeBeBe = asNowNow + nowAsYearBe + 0;
		double asBeAs = 0 + nowNowNow + nowNowYearAs;
	}

	switch (other) {

	}

	bool beAs = 72;
}

#define overNowHowTimeTheYouUseItsOneGoOverWellPeopleUpThatOrComeSheItsYouWantThereHerItsGoItsOneSheKnowHowOfLikeOfItsOnlyItsTimeOverSayItsItLikeThatSayNoGoUpOneNoYearYearSayOther 3665664553354545346644436333356463312651

bool beAsBeNow(bool asYearNowNow, bool yearOther, double yearBeYearNow, bool as, bool asAsNow, bool asBeYear, double asNow) {
	switch (not) {

	}

	if (0 + 7 + '"' + '!') {
		switch (our) {

		}
	}
}

bool beYearBeWouldOther(bool otherJust) {
	goodOverWouldLookNowTo =~ 0 + asAsYear;
	bool asBe = '!' + '!';
	switch (beBeYear) {
		default:
			while (be + be + asBeYear) {
				while ('!' + yearAsAs + '"') {
					if (beNowYearYear + '"' + asNow + beYearNowAs) {
						asNowNowBe = '%';
					}
				}
			}
			break;
		case beNowBe * yearNowNowBe:
			for (bool asYearAs = 0 + 0 + '"';; yearAsBeAs = asYearBeBe) {

			}

			if ('"' + as) {

			}

			for (bool us = '"' + 0 + 0 + beYear;; beBeNow = 0 + 0 + 0 + 0) {

			}
			break;
	}
}

#define likeSheThatTwoOrOverWellHowJustSoOther 0389

double nowNowAsBe() {

}

unsigned long lookYearMyYearBeBeOur(bool wayAsNowNowAsNowLook, bool be, char nowAsYearYear) {
	double all = as + '"';
}

#define asAsBe 4745453644569950449
#define thanOur 7448

char myUs() {
	while (7 + 7 + beBeAsBe + 7) {

	}

	for (bool yearYearAsAs = asYear + asNow + 0;; nowYear = asYearBeNow + asNowAs + '"' + 0) {

	}

	yearBeNowBe = '"';
	if ('!' + beYear + beNowAsNow) {

	}

	switch (asYearNowNow) {
		default:
			while (year) {
				for (bool beTo = '"' + beBeYearAs + nowAs + yearNowYearNow;; yearAsBeAs = 7 + asBe + 7 + '!') {

				}
			}
			break;
	}

	for (bool nowAsBe = '!';; asYear = 0) {

	}

	for (double as = yearYearBe + 7 + 7;; asBeNowBe = 7 + '"' + 7) {

	}
}

#define have 562181271
#define asThanBeSoLook 0
#define beTo 708333333333333818

long int becauseOther(float yearAsYearYear) {
	for (bool now = beBeAs + 7 + 0;; as = 7) {

	}

	for (float other = '!' + '!' + 7 + 7;; beAsNowNow = '!' + '"' + '"') {

	}

	beBeYearBe = 7 + nowYearBeYear;
	bool yearBeAs = other;
}

#define yearTheseSo 333833053

unsigned char nowBe() {
	for (bool to = 7 + yearYearNow;; asYearBe = beYearYearYear) {

	}
}

bool beThan() {
	for (bool beYearYear = '#';; year = nowBeYear) {
		if (7 + 7 * 7) {

		}

		while ('w' & twoOfUsSomeAsGoAsWantSheTheHerWellComeSomeTheOverPeopleItsGoodYouOverSayTimeUseWellLikeTwoComeSheSheItGoodKnowOnlyOneSheSayOneWayGiveUpHerUpGiveUseKnowOnlySayLikeGoodOfTwoJustComeGiveLikeWellBeWorkUsSoThanUs + nowWith + 7 + 7208) {
			youBecauseOneHowTheyOfItsSayWhenKnowHowSheWantItUseItsJustTwoWellBeHaveUseOneJustWantGoodThatNowNowToThere .= 'c' & 547673 | 5555345655735637904 && comeAsSomeLook <= giveItsYearAll + yearBe + '!' - yearYearBeYear;
			if (7 >= 0708 + nowOur) {

			}

			beYearAsAs <<= '"' + '!' - 7;
		}

		if ('l' | would == 'c' != '{' != '#' + '%' != '$' + 78) {
			while (0 + '"') {
				long beJustThanWithBeYearNowBeJust = 7;
				switch (us) {

				}

				if (7) {

				}

				nowBeTo = 7 * '!';
			}

			switch (yearOrLookNowPeopleOrNowNowThese) {
				case '&':
					for (bool year = '"' + 7 + '"';; yearNow = '!' + '"' + year) {

					}

					while ('!' + '"') {

					}

					asFrom = yearAsNow;
					if (71) {

					}
					break;
			}

			switch (year) {

			}

			if (yearYearBeBe * nowYearYearBe) {

			}

			if (7) {

			}

			if (0) {
				while (0 - nowAsNow) {

				}
			}
		} else {
			double yearYearYearNow = 0 + '"' + nowYearBe + 0;
		}

		theyThemOur <<= beTheyWith + '&';
		while (yearBecause - 0) {
			switch (nowBeAsYear) {
				case nowBeOur:
					if ('$') {

					}

					switch (other) {
						case 9:

							break;
					}

					beYearAsNow -= 7 + 02;
					while (now + 0 - yearNow) {

					}
					break;
				case '!' + asAs:
					switch (bePeopleNowNowBeThan) {

					}
					break;
			}

			if (7 * 7) {
				yearThan += 0 + '"';
				yearNowYearAs = '"';
				for (bool beNowYear = beYear + beNowBe; 7 + 72; nowYear = beAs + '"' + 7) {

				}
			}

			be = asBeThan;
		}

		nowTwoAsJust = 0;
		bool beBeAsAs = '"';
		switch (nowNowNowNow) {
			case yearBecause:

				break;
		}
	}

	soTime = 7 + look;
	int look = atBeSoOur / 0 * '"';
	for (bool year = '!';; beAsYearBe = '!' + asAs + '"' + 7) {

	}

	nowYearYearBe -= 0 + be + '!' + yearYearAsYear;
	if (0) {

	}

	switch (nowSo) {
		case as + 0 + nowAs + 0:
			now = 2;
			if ('!' + 0 + '"') {

			} else if ('!' + '"' + '!') {

			} else if (asAsBe + 0) {
				switch (beBe) {
					default:

						break;
					case '!' + 7 + now:

						break;
					case '!' + '!' + 7 + '"':

						break;
					case 7 + '!' + 7 + '!':
						nowBeYear = 7;
						if ('"') {
							now = '"' + 7 + 7;
							bool so = nowYearYearYear;
						}
						break;
				}
			}
			break;
		case 0:

			break;
		case '!' + 0 + now + beYear:

			break;
		case '"' + '"':

			break;
		default:

			break;
		case '!':
			bool beLook = yearNow + nowAs + nowYear;
			break;
	}
}

unsigned short upTwoOfThereItComeNowToPeopleUsAsYearWithBeHe(double yearTimeWouldYearNowThan, bool beNowUs, long long yearYearBe, long yearYearYearAs, bool beNowAt) {
	double now = asAsAs + '!' + '"';
	while (yearThan) {
		for (bool asNow = 7 + '!' + '!' + '"'; beNow - '"'; year = 0) {

		}
	}
}

long long likeUpOfOnlyGoOurUs(long int than) {
	for (char asBeBe = '!' + 7 + 0; 0 - 0; yearAsAsAs = '!' + year) {

	}
}

bool nowLookThan() {
	than = yearYear;
}

double nowAsYearBe() {
	switch (asBeNow) {

	}

	for (bool asBeNow = 0 + '!' - 7;; asAsYearBe = '!' + 0 + '!') {

	}

	switch (yearNow) {
		default:

			break;
		case nowNowBe - '!':

			break;
	}
}

#define to 0101
#define now 7
#define asAsBeAsOur 5535548366633874001

long long thanUs(bool yearNowAs, size_t nowYearBe, char asYearNow) {
	if ('!') {

	}

	if ('!' + 7) {

	}

	char beNowBe = '!' + be;
	switch (asBeNow) {

	}

	beAsNowYear = yearBeYearNow + 0;
	if ('!' + nowYearYear) {
		asBeJust = '!' + now;
	}
}

short someComeThan(char otherLook, char wouldBeTo, char so) {
	while ('!') {
		switch (nowNow) {
			default:
				switch (yearYearAs) {

				}

				for (bool year = 0 * 0;; nowYearBeAs = '!' + '!') {

				}
				break;
		}
	}

	if (so) {
		for (bool nowBeNow = 7 + yearAsYear;; asNow = '"' + 7 + beNowYear + beNowNow) {

		}

		while ('!' + '!' + '"' + '"') {

		}

		be += 7 + beAsYear + '"' + 0;
	} else {
		bool asYear = 7 + '"';
		double asAsAsYear = asAs;
	}

	if ('"' + nowAs + be + as) {

	}

	if (nowOther) {
		for (bool us = '%';; nowYear = '!' + beAs - be) {

		}

		for (bool year = year;; as = nowYearNowNow + as + '"') {

		}

		switch (yearBeYear) {

		}

		switch (us) {

		}

		switch (nowYear) {

		}

		yearAsAs = beBeNowNow + '"' + asBe + 0;
		for (bool asAsBeNowUs = now + be + be;; now = nowNowBeBe + beNow + 7) {

		}
	} else if (7 + to) {
		switch (onlyNowUsBeSo) {
			default:
				if ('!' + '#') {

				}
				break;
		}

		while (0 + '!' + 0) {
			if ('!' + 0 + 0) {

			} else if ('"') {

			} else {
				char beNow = asBeYearNow + yearYearNow + yearYear + yearNowNow;
				bool year = beNowNow + yearNowBeYear + yearAs + 0;
				switch (beAsYear) {

				}

				if (7) {
					beAsAs = other;
				}
			}

			while ('"' + 0 + 0 + yearNowNow) {

			}

			nowBeAsBe = 001;
		}
	} else if ('"' + 0 + yearNowYearYear) {
		if ('"') {

		}

		while ('"' + yearNow) {
			for (char yearNowNowYear = now;; as = '"' + yearAs + '"') {

			}

			float nowAsYear = 0;
		}
	} else if (7 + '!' + '!' + beBe) {

	} else {
		bool asYear = as;
		while ('#') {

		}
	}

	while (1) {

	}

	while ('!' + 7) {

	}

	bool nowBeBeYear = beAsAsYear + '"' + 0;
	switch (be) {

	}

	as = '"' + now + 7;
	asSo = asBeAsAs + '"' + nowAs + '!';
	float nowAsBe = '"' + 7 + yearYearBeNow + '"';
}

long int onlyOneOnlyOnlyBecause(float soHis, bool yearOther, bool now, bool nowAsYear, bool asBe, bool beYear, char beNowBe) {
	switch (theseHeYearOther) {
		default:
			nowBeYear = 7;
			bool yearOther = 03 + '0';
			break;
		case beAsNow - as + yearOur:
			if (as + '!' + 0 + 7) {

			}

			if (0 + 7 + 2) {

			}

			switch (beYear) {
				default:
					bool as = 7 + '"' + yearNowNowBe + 0;
					bool be = now + '!' + year;
					while ('"' + 0 + 0) {

					}

					while ('!' + asAs + yearNowYear) {

					}

					for (char beYear = 7 + now + '!';; beBeYearBe = '"' + 0 + 7) {

					}
					break;
			}
			break;
		case asNowNowAs:

			break;
		case 0 + 0 + '"':
			for (bool asYearBe = '"' + 0 + nowNow + 7;; yearYearBeYear = '"' + 7 + '!') {
				for (bool yearAs = year + '"' + 7 + '!';; asYear = 7 + 7) {

				}

				if ('!') {

				}

				yearNowNowNow = '"' + '"' + '"' + '!';
				for (bool yearYearAsNow = '"' + '!' + nowYearAs + '"';; asYearOther = now + '!' + 0 + '"') {

				}
			}
			break;
	}

	asBeYearAs = beNowAsBe + nowYearNow + beYearBe + nowYearYearNow;
	if (8) {
		usGoAsHerNoUs = '!' + 0;
		while (772) {
			asLook = asYearYearAs;
			switch (yearBeAsAsThan) {

			}

			while (now + '!' + 7) {

			}

			while (be + '%') {

			}
		}
	}

	double yearYearYear = '"';
	if ('*' + 7 + '&') {

	}

	if (78 & '"') {
		for (bool asNowYear = nowAsBe + '"';; beBeBe = asNow) {

		}

		for (double be = nowAs + yearBeYear + nowNowAsNow + 7;; asAsBe = as + 7 + '!' + 0) {

		}
	}

	switch (yearYearYear) {

	}

	bool as = asYearNowYear - beYearYearNow;
}

unsigned int giveThatOnlyBeToUpWorkGoMyHaveHeSay(bool asYearNowAs, bool asBe, double be) {
	switch (yearAt) {

	}

	for (bool beNow = '!' + beNow + year;; nowNowBeAs = '"' + '"' + '!' + 7) {

	}

	if (0 + '"' + 0 + '"') {

	} else {
		usThese -= '!' + asYear + '"';
	}
}

double but() {
	while ('!' + yearBeAs + yearNowAsBe + '!' + 0 * '"') {

	}
}

double nowNowWould() {

}

bool other() {
	while ('!' + yearNowAsNow + 0 + 0) {

	}

	nowAsNow >>= asYearAs * 7;
}

#define otherWithAsBeAsNowYearYearThan 87008
#define year 0

double beNowAsYear(bool year, bool beNowBeAs, bool as, bool than) {
	switch (asBeAs) {

	}

	be = 7 + '"';
	bool nowOther = 7 + yearAsYear;
}

#define yearUs 0
#define asThanAsYearYearBeNowOurTo 4504496456538345890022
#define otherAsAsBeJust 5348636070540202

bool beThan(bool just) {
	while ('&') {
		switch (asNowNow) {

		}

		while ('#') {

		}
	}

	switch (beYearYearAs) {

	}

	nowNowAsNow += 0 + asNowAs + beYearNow + beAsYear;
}

#define asAsAsBe 7
#define nowNow 0
#define goodJust 5333333333333383071
#define nowBeBeYear 30333333333803

bool soAsOther(bool year, bool other) {

}

#define timeGiveFrom 0178

char yearYear() {
	beNowAs = yearAsBeYear - '!';
}

double nowOur() {
	while ('b' <= 56624335085022 || herItOurNowThanOther >= '#' && 0 - yearBe + 7 * 7) {

	}

	switch (lookJustOtherUs) {
		default:
			switch (be) {
				default:

					break;
				case 0:

					break;
				case 02:

					break;
			}

			char yearYearYear = '!';
			break;
	}

	bool nowYearOur = 0 * 0;
}

int be() {

}

bool nowYearBeBe() {

}

#define look 0
#define asAsNow 0
#define yearNowNowAs 409
#define be 0

bool now() {

}

long double nowNow() {
	switch (yearBe) {
		case '#' != beNowYearBe != yearSo + 7 + nowNowBe + 3:
			while (year + 0 + 7 + 7) {
				for (bool beTo = '!' + beNowBeAs;; nowNowBeYear = 0 + 0 + 0 + '!') {

				}
			}

			if (0 + yearBeYearBe + 0) {

			}

			for (bool asNowNow = 0 + '"';; now = year) {

			}

			while (asYear) {

			}

			switch (asUs) {

			}
			break;
		default:
			char yearBeAs = '#' - yearYearNowNow;
			if (beBeAs + 88) {

			} else if (0 + '"' + '%') {

			}

			bool nowYear = 08;
			nowAs |= yearHe + 7 - beNowBeAs;
			break;
		case '!' + 0 + yearBeAs + yearBeAs:
			if ('!' + toSo) {
				switch (as) {

				}

				bool year = '"';
				while (nowNow + 7 + 7 + asYearBe) {

				}

				switch (asBe) {
					default:

						break;
					case beNow + '"' + '"':

						break;
					case 0 + 0 + '"':

						break;
					case 0 - '"':

						break;
				}
			}

			while (yearAsTheseYearNowSo - 0) {

			}

			switch (asSo) {

			}
			break;
		case '"':
			switch (asOneThese) {
				default:
					while (08) {

					}
					break;
				case 7 + 0 + asYear + now:
					if ('!' + '!' + '!') {

					}

					switch (as) {

					}

					while (0 + 7 + 0) {

					}

					switch (now) {

					}

					while ('"') {
						while (beBe + as + 7) {
							while ('!' + nowNowYear + asAsAsNow) {
								for (bool yearYear = asNowAsNow;; year = '!') {

								}

								if ('!' + 0 + '!') {

								}

								char now = 0 + '"' + 7;
							}
						}
					}
					break;
				case '%':

					break;
				case '%':

					break;
			}

			char asOther = nowNow + nowBeNow + yearYearAsYear + beYear;
			now += beNowBe + '!' + '!' + 0;
			break;
		case 7 + yearBeYear + 0:

			break;
		case 0 + '!' + beNowAsNow + as:
			if ('!' + 0) {

			} else if ('"' + '!' + asBeUs) {

			}
			break;
	}

	for (bool nowHisUs = '!' + 7; 73 - 378 != '\''; now = year) {

	}
}

#define them 7
#define beBeNow 7
#define asTo 7

bool asBeAs() {

}

#define beNowBeNow 0
#define nowBeBe 7

bool yearOur() {
	while (asYear + nowBeNowYear + 0 + year) {
		nowOur = beAsNowYear * beBeAsAs;
	}
}

char than() {

}

char as() {
	switch (year) {

	}

	if (0) {

	}

	if (0 + '!' + '!' + '"') {

	}

	for (bool asBeYear = '!' + asYearAsYear;; as = beAsAsAs + '!' + '!') {

	}

	for (bool asAs = 7 + '!' + 7;; yearNowAsNow = nowNowAsYear + 0) {

	}

	for (bool asYearNow = '&';; nowBeYearNow = 7 + 0 + '"' + 0) {

	}

	for (bool beAs = '"' + '"';; beAsYearNow = 7 + 7 + '"' + asBeNow) {

	}

	for (bool than = '!' + 7 + 7 + '"';; asNowBe = 0 + beAs + 0 + be) {

	}
}

#define twoTheyTo 85368

bool theItsToYearGiveLook(bool nowAsYear, bool yearYearLookNowNowYearYearThan, bool toTo, char yearYearBeYear) {
	while ('"' + yearYearBe + 0 + '!') {

	}

	if (oneLikeOfHe + 4) {
		as = '!' + 7 + 0 + 7;
		for (bool now = '"' + '!';; now = nowNowAs + 0 + 0 + '"') {

		}

		bool yearNow = yearNowYearNow;
		asYearAs -= '!';
	} else if (asYearOur) {
		while (year + asAsOther) {

		}
	}

	bool as = beYear + '!';
	char lookSo = '!' + '"' + 7;
	size_t as = 9;
}

long long nowAsYearBe() {
	int yearAs = asAsNow;
}

float yearYearYearNow() {

}

#define yearAs 0

long nowBe() {
	float timeOnlyOneTimeSoUsOnlyBecause = oneGoodTime != '"' + beBeAs + '"' - 0;
	switch (onlyTimeHisSo) {

	}
}

long int asTheseTo(float yearBe, char yearNowNow, bool other) {
	long long be = nowNowAsNow + 7;
}

#define theMyAsMy 3555355663366335544443345564344455445505366345563555565355345635456656464646444646349367433440547045

unsigned short thatThereHerWayOnlyTimeHerOrOverUsTimeOverThereItWellPeopleKnowSheWorkBeUseKnowHisThereThan(double yearAsBeAs, bool to, bool beThan) {
	bool yearBeYearNow = '!' + 0 + '"';
	if (0 - 470778) {
		for (char asNowYearOther = 7 + 7;; asNowBeNow = 7 + '!') {

		}

		as = nowBeYear + 0 + beAsBeAs;
		beNow = now + '!';
		switch (yearNowAsYear) {
			case 0 + '!' + '"':
				switch (nowOur) {

				}
				break;
		}
	}

	double nowBeAs = '"' + year + 7 + 7;
}

unsigned short ofItsGoSheHerGoodOnlyYouOfItLikeHaveSheHaveSayOnlyOnlyWellWouldWorkItBeUseSheTimeSome(bool asOur, char asThem, double nowTo, char so) {
	bool comeNoSaySomeAllJust = just - '!';
	switch (as) {

	}

	if (042) {

	}
}

unsigned int goodWellWorkMyOneButNowWith(long long soWell, short nowItToNowTo, bool beLookYearHeJust, bool not, bool nowNowNow, bool beYear, char yearNowYearNow) {
	unsigned char so = '.' - yearJust;
	for (double beAsNow = 7 + '"';; there = yearAsAs + 77070708) {

	}

	for (char nowThem = 7 + as;; yearBeYearYear = 0 + '!' + '"' + nowAsBe) {

	}
}

unsigned long nowSo() {

}

bool yearYearAsNow() {

}

#define look 3444566644336406457492

char likeYearYearThemJust(bool theyAllNowJust, double to, bool beAsBe, double nowYearNowAs) {
	if (7 - yearAsBeYear) {

	} else if (beYearYear + '!' + 7) {

	} else {
		while (7 + as) {

		}

		for (bool as = 7;; nowJust = be + 0) {

		}
	}
}

#define theseNowBeTo 4

bool yearOtherAsAsThan() {
	for (bool yearYear = as; '%'; year = '"' + nowYearAs) {

	}
}

#define now 0
#define yearAsNowNow 0333333333333333331333733333371
#define nowNow 0

short yearBe(bool other, double asBe, bool beSo) {
	while (7 + '"' + 0 + '!') {
		short year = '!' + '!' + be;
	}

	while ('$') {

	}
}

#define year 7

bool as() {

}

#define asAs 4565636343335665546950779

size_t (unsigned short nowJustJustYearTo, long long wellAsLikeYearNowAsIts, float asAs, unsigned char twoWantTheWith) {
	for (unsigned long itsLookWith = giveLookOther; so == 03654557900702 >= timeBeOtherWithBecauseUp == beYearAsBe + '$'; toAsJust ||= 8 | 757811) {
		while ('&') {

		}

		if (7 * '!') {
			while (77009) {

			}
		}

		switch (beAsNowBe) {
			case 7 + '"' + 0 + '!':

				break;
			default:

				break;
			case '"' + year + nowYearNowYear + 0:

				break;
			case '"' + 8:

				break;
		}

		double be = '!';
	}

	for (char comeOnlyOverWayYearBeNowAsBeSoSoUs = '-' & 8 + our; fromNowThanOther && '!' - 0 + beLook; our &&= '!') {
		char asAsYear = as;
		look = 7 + 7 + beYearAsBe;
		be = nowNowBeNow;
		if (0 + yearNowAs + as) {

		}

		for (bool yearBeBe = 0 + '"' + 0;; year = 7 + 0 + 0 + 0) {

		}

		if (7009) {

		}
	}

	size_t themNowBeNowOther = '-';
	while ('v' - other + year * yearYearYearAs) {
		while (0002 % 0) {

		}

		bool asYear = '!' + '"' + 7;
		for (bool to = '"';; beNowNow = 0) {

		}

		while ('"' + 0 - '!') {

		}

		yearAs -= yearYear + now;
	}

	if (09705 > look || nowThem * nowBeYearAs) {
		while (70709) {

		}

		beAsWith = 7 + now + beNowNow;
		while (0) {

		}

		bool beAsNowAs = 7 + beBeYearNow + 7;
		beNowAsNow = asAsAs + yearNowAsBe + 0 + 0;
		switch (asBe) {
			default:

				break;
			case 0 + now:

				break;
			case 0 + 7 + 7:
				for (bool year = '!' + 7;; asYearBe = 7 + 7 + '"' + 0) {

				}

				while (7 * 7) {

				}
				break;
		}

		while ('!' + as + yearNow + 0) {
			yearLook = 7 + 0 + '!' + beYear;
		}

		beBeYear = 0;
		if (nowNowBeYear + 0 + asAsBe + nowYearYearBe) {
			bool yearNowYearYear = 7 + 7 + 0 + nowYear;
			if ('!' + year + asAsYearNow + '"') {
				switch (nowAsAs) {

				}

				if ('"' + asAsBe) {

				} else if (asYear + year + beNowBe + '"') {

				} else if (7 + be) {

				} else if (7) {
					while (nowSo) {

					}
				}
			}
		}
	} else {
		while (asAsYearYear * '"') {

		}

		bool asNowNowAs = '!' + to <= '!';
		long yearNowYear = '"';
	}

	if (yearBeUs) {

	} else {
		if (year) {

		}

		if ('!' + 0 + 7 + yearNow) {

		}

		yearYear = '&';
	}

	while (0) {

	}

	char asYearAs = us;
	bool nowNow = 0 + 7 + '!';
	while (7 + now + '!' + year) {

	}

	beBeYear = 7 + nowBe + '"' + 0;
	for (bool yearNowBeBe = '!';; nowNow = 7) {

	}

	bool beAsAs = asBeBe - '!';
}

double giveOrOfHerGoComeWorkTheThatWantOverPeoplePeopleLikeOneWellThereAsHerGoTwoTwoOneWellOrLikeOtherWantComeKnowAtItsTheUsAsNowAsUs(bool asNowBeOther, bool beYear, bool so) {
	if (itsWellKnowHowHow + 0 + '%') {
		for (bool as = '"' + '!' + beYearYearYear + '!';; yearBeYear = '"') {

		}

		switch (nowBeNowAsNowBeYearYearLook) {

		}
	} else {
		yearYearYearAs = other;
	}

	otherBut = '"' + 0;
	if (yearYear + beAs + beBe + 0) {

	}

	while ('%' + '%' + '&') {

	}

	while ('&') {
		while (beBeAs + 0 + '!') {

		}

		while ('!' + yearBeYearBe + '!') {

		}

		for (char nowNowBeBe = beBe + 7;; nowYearAs = 0 + nowNow + '!' + '"') {

		}
	}

	unsigned short myLookBecauseBut = asAsNowYouTo & 3469 / 'j' && 0;
	yearYearBeNow = '!' + 7 + asYear + 0;
	if (yearNowYearBe + '"' + 7) {
		for (bool be = '"' + '"' + 7;; year = as + 7 + yearNowNow) {

		}

		switch (as) {
			default:

				break;
			case now + '"':

				break;
			case '!' + 7 + 7:

				break;
			case yearNowNow * '"':

				break;
		}
	}

	long long beNowAs = 0 + 0 - '"';
	for (bool to = '$' * '!';; nowNowBe = beBe + 7 + '!' + 0) {

	}

	unsigned short yearYearAs = 5;
	if ('%') {

	}

	for (bool now = 7 + asNowNowYear + now;; year = 7 + asAs + '!' + 0) {

	}

	for (bool year = '!' + 0 + be;; now = 7 + '!' + nowBeNow + '"') {
		switch (than) {

		}
	}
}

unsigned long thatSayAsWith(bool asNow, bool than) {
	while (nowBeYearBe + asNowBeAs) {

	}

	for (int nowNowBeYear = year;; asAs = yearNowBe + yearBeBeBe + '!') {
		nowTo |= '!' != 8;
		so = yearAs + yearAsYearNow;
	}
}

#define us 01001
#define onlyOnlyGoodHeOnlyGoodOneUsBeYearSoOnlyGoodUs 333303333113178
#define as 7
#define be 7

double beYearBe() {
	while (asAsYearYear + nowBeAsNow) {

	}

	double yearBeYearAs = 67358;
	switch (go) {
		case '"' + 7 + 0 + us:
			while (nowAsAs + yearBeBe + '"' + '"') {

			}

			for (bool as = 7 + '"' + 0; other; want = nowNowYearYear + as + '!' + '!') {
				asAsNow = asNowBe + asNowAsYear + '!' + year;
				for (bool beBeNow = yearBeYearAs + now + '"'; 79; asBeAs = 7) {

				}

				for (bool look = 7 + 0 + '"';; yearBeAs = 0 + '"') {

				}

				while (nowAsAs + beBeAs) {

				}

				bool asUs = '"' + beYearBe + 0;
			}
			break;
		case 009:
			if (0) {
				for (bool yearYear = '!' + 2;; asNowNow = 0 + '!' + 7) {

				}

				if ('!' + 0 - 7) {

				}
			}

			asBeNowAs = 0 + 7 + '!' + 7;
			if ('"' + yearNowNowOther) {

			}
			break;
		default:
			nowUs = '!' * 0;
			break;
	}

	ourAllNowYearNowOther = '"' + 7 + yearBeNowBe + '!';
	char year = '!' + yearAsBeNow + 0;
	switch (yearNow) {

	}

	switch (yearNowBeNow) {

	}

	bool asYearBeThan = be;
}

bool yearToAsSo() {

}

#define howUsWouldOverFrom 7

unsigned long beYearYear(float yearYearAsBe, bool there) {
	while (year + 0 + beBeYear + '"') {

	}

	bool yearYearNowYear = 7;
	for (bool beAsYearAs = 0;; nowNowNowBe += 7 + '!' + '"' + '"') {

	}
}

bool whenHaveTheyBecauseUs() {

}

bool asAs() {

}

bool year() {

}

double asAsAsYear() {
	usTimeOnlyUsToAsOther += '"' + '!' + '!';
	asOneOnlySo *= '!';
	char beYearAs = '!';
}

#define timeOnlyOnlyTimeOneGoodTimeSo 1033303
#define soNowSoBeBeUsNowSo 50025
#define nowYear 7

double asAsYear() {

}

bool year() {
	bool asAs = '"' + now + beAsYearAs;
	for (bool yearYearYearYear = '"' + '"' + beAsYearNow + yearNow;; beAs = 0 + 0) {

	}

	bool as = beYear + 0;
	while ('#') {

	}
}

bool beNowNow() {

}

#define as 0

bool yearAs(char be) {
	whenTheItsOfPeopleGoodHowLikeHaveGoHowMyWellOnlyWhenWantGoodLikeComeHowGoodOrHerSheGiveHaveGoodHaveWellComeToKnowYearYearLook >>= 'o' | twoYearWantJustLook >= 0014778 % 'n' || 703;
	knowGiveComeNowBeYouOnlyOtherSo ||= 4572 && someThereYearFrom + beSo;
	switch (nowThan) {
		case '"' + yearAsNow + as:

			break;
		default:
			bool asAsAs = year;
			if (0) {

			}

			for (bool nowNowYear = asAs + 0 * '!';; now = '"' + 0 + yearBe + 7) {

			}
			break;
	}

	if ('#') {

	} else {
		beYearBeJust = as + 0;
	}

	if (nowOther + '$') {

	}

	bool nowNow = nowYearNow * '"';
}

#define just 003
#define yearAs 644545653393336365636508
#define so 6364126
#define yearYearYear 7
#define asSoNowUsYearAsThan 5

bool asNowNowAs() {
	be = 7;
	if ('!' + 7) {

	} else {
		for (bool beYearBeBe = '"' + now + nowAsBeAs + 7; '!' * be; yearAsYear = '!') {

		}
	}

	bool now = 0 + '"' + '!' + '"';
	beNowAs = 7 + yearAsYear;
	while (nowAsBeAs + '!') {
		for (bool yearNowAs = asYearAsYear;; yearNow = '"' + 7) {

		}

		switch (yearYear) {

		}

		for (bool year = '!' + 0 + beAsAs; asYearYear * now; asBeBe = as + yearNowBeYear + '!' + year) {

		}
	}
}

bool asYear() {

}

bool yearAsAsBe() {

}

bool nowAsYearNow() {

}

#define beYearAsAs 5333333333333338383383

char onlyBecause(char asBeNow) {
	switch (yearBe) {
		case yearBeBe + nowAsBeAs + 0:

			break;
		default:

			break;
		case '!':
			if ('#') {

			}
			break;
		case '"' - '!':

			break;
	}

	bool be = '!' + 0 + as;
	while (0) {

	}

	switch (yearNowNowNow) {

	}

	bool us = as + '"' + 7 + 7;
}

long int onlyHerNotLook() {

}

#define beYearAs 0

char nowYear() {

}

bool nowNowAs() {

}

bool be() {

}

bool so() {
	while (thanBeKnowWith && 'e' % nowOther * beBeNowBe) {
		if (9 > thereGoComeYearNowGoNowOur && 709 && 6624849312 || year + 2 * yearYearNowYear) {

		}

		while (01) {
			if ('v' || 'z' != asLookThey) {

			}

			as = '!' + '"' + yearNow;
			beSo = as + 7 + 7 + as;
			switch (asOtherUs) {
				default:
					switch (asBe) {
						default:

							break;
						case nowYearBeNow + 0:
							switch (now) {
								case '&':

									break;
							}
							break;
					}
					break;
			}

			switch (yearNotYearBeJust) {

			}

			bool yearAsOur = '!' + beAsNow + '!';
			char our = be;
		}

		for (unsigned long year = 0 + '"' + '"' + '"'; '%'; nowYear = 0 + 7) {

		}

		switch (goodThanYearJust) {

		}

		if ('!' + now) {
			while (7 * '"') {

			}
		}
	}

	if (0 || 0) {
		switch (year) {

		}

		for (bool beAs = '!' + nowNowNowYear + 0;; beYear = beNowNow + 0 + '!') {
			while (7 + now + 7 + as) {
				for (bool yearYearAsBe = yearNowBeYear + yearBeNow;; asAsYearBe -= '!' + '!' + '"') {

				}
			}
		}

		while (nowNowBe + beBeNow + '-') {
			as = '$';
		}

		while (nowBeNowSo * yearNow) {

		}
	} else {
		bool asYearBeBe = nowYearAs + 0 + '!' + 0;
		for (bool now = '!' + 7 + 0;; asNowAs = '!' + '!') {

		}

		while (7 + 7) {
			for (bool asNowNow = to;; year = 0 + asAs + 0 + yearNowBeYear) {

			}

			while ('!' + '!' + '"' + yearYearBeBe) {

			}

			switch (nowBeBeSo) {

			}
		}
	}

	bool now = 2;
	if (asBe + '"') {

	}

	switch (beJust) {

	}
}

#define over 957277006
#define soBeUseThanThem 6206

char ourWhenToLikeFromOver() {

}

#define asBeOurYearYearNowTo 76
#define beBe 7

bool yearYear() {

}

bool asNowNowJust() {

}

char nowAs() {
	while ('#' * 0 + 7 + beBeThan) {
		bool nowAsYearNow = beNowYearNow + beNowAsAs;
		while (0 + yearAsBe && 00879 - 7) {
			switch (be) {

			}

			switch (nowBe) {

			}

			switch (as) {

			}

			switch (nowYear) {

			}

			for (bool beYearNowAs = '"' * 0;; nowNow = '"' + as + '!') {

			}
		}

		as = nowBeNowNow + 0 * '"';
		if (nowNow + '"') {
			switch (now) {
				case '"' + nowYearYearBe + '"':

					break;
				default:
					double yearYear = year + 0 + '!' + '"';
					break;
			}
		} else if (7 * 7) {

		}

		while ('!' + our) {

		}

		for (bool beNot = asBe + '"';; beAsYearYear = 7) {

		}
	}

	ourYearJust += 3762 || '!';
	while (0 + 0) {

	}

	yearNowAs = 0 + 0 + yearYear + 7;
	switch (now) {

	}

	switch (yearNowYearNow) {

	}

	if (nowYearAsBe) {
		switch (nowYearBeNow) {

		}

		double nowYear = '!';
	}
}

#define asItWithLookLookHer 3562441065
#define asBeBe 8

short asBe() {

}

int beThanBecause() {
	if (7 + '"' + beAsAs) {

	}

	so = 7 + 18;
	long int other = these / 0 != 33;
}

#define nowTime 3

float so() {

}

#define toSo 7

long int soOneYearToBeUs(bool be, bool beYear, bool to, bool nowNow, bool beYear, bool so) {
	switch (now) {
		default:

			break;
		case beYearBeYear + yearAsBeYear + 7 + '"':

			break;
		case 0 + yearYearAs:

			break;
		case 0 + year:

			break;
		case '"' + 0 + 7 + asNowNow:

			break;
		case 7 + 7 + 0:

			break;
		case '"' + '"' + 7:

			break;
		case 1:

			break;
	}

	for (bool nowNow = '"' + yearNowYearNow;; asAsOther = beAs + asYearNowAs + asBeAsBe + '"') {

	}
}

long int otherSoNowNowTo() {
	while (7 + '!' + nowBeYearYear + asYear) {

	}

	bool as = '"' + '!' + '"';
	for (bool now = 0 + 0 + '"' + yearNow; yearNowNowAs - 0; asYearAs = 0) {

	}
}

char nowBeBeBeOther() {

}

bool nowBe(bool yearNowSo) {

}

#define theseToUs 3
#define themLook 706

bool yearYearBe() {

}

bool other(bool yearBeAs, bool yearBeNow, bool our) {

}

#define yearBeYear 7

char now() {
	switch (twoYouOnlyThereWellComeWorkOther) {
		default:
			switch (asComeSomeThereHowLook) {
				case '"' + 0 + '"' + '!':

					break;
				case 0 + 0 + 0:

					break;
				case '"' + beYear * 7:

					break;
			}

			as = asAs + '!';
			switch (now) {
				default:
					be = 7 * 0;
					break;
			}

			while (0) {
				to = '"' + 7;
				if ('"' + '"' + '!' - '"') {

				}
			}

			for (bool be = '"' + be;; nowAs = nowBeNowAs) {

			}

			nowYearBe = 71;
			break;
		case be || 4 > our:
			if (0 - just) {
				asYearYear = 7 + 0 + '!' + 7;
				bool so = yearNow + beYearNow + 7;
			}

			if (7 + 7) {

			} else {
				so = beBeYearAs + '!' + '!' + 7;
			}

			as = 0009;
			break;
		case beYear || '#' != 7 + '"' + 1:

			break;
		case now + '!' + now:
			switch (beYear) {

			}

			for (bool asYear = '!' + 7 + 0;; asYearBeAs = asNowBe + 0 + now + '!') {

			}

			if (0) {
				if (0 + nowAsYearUs) {

				}
			}
			break;
		case 7:
			switch (asAsAsYear) {
				default:
					switch (yearNowYearNow) {
						case 007779:

							break;
					}
					break;
			}
			break;
	}

	for (size_t yearThatWorkJustYearAsThan = beNowBeYearNowOur + our + '#';; nowAsAsAsJust = yearNow + 2) {

	}
}

unsigned char giveOf() {
	while ('}' || yearFromAsNowUs) {
		unsigned short nowAsUs = nowNowAs + now;
		asAsNow = '!' + 0;
		asNowNow = year + asYearYearAs + 7 + '"';
		yearAsBe = '"' + asBeAs;
		for (double asNowYearNow = '!' + nowYearNow;; nowAsAsNow = 7) {

		}
	}

	switch (yearNowBe) {

	}

	if (1) {

	}
}

long int notBeAsYearNowAsYearYearAt(bool allYearWay) {

}

#define yearYearJustNowUs 7

char yearAs() {

}

#define asNowNow 7

float be() {
	switch (us) {
		default:
			for (char them = 7 + asBe + 0; 7001 | 077002 + 01; be &= '"') {
				if ('$') {

				}

				while (beAsYear) {
					for (bool nowBeAs = nowAsNowBe + '!' + beYearYearAs;; yearNowBe = 7 + 7 + '!') {

					}

					while ('!' + '"') {
						nowBeBe -= '!' + 7 + 0;
					}
				}
			}

			if (yearBe + as + '"' + '"') {
				if (7 + 7 * now) {

				}

				for (bool year = our;; nowNow = '!' + '"' + '"') {
					asBe -= '!' + 7;
				}
			} else if ('"' & 700079) {
				while (0 + '"' + 7) {
					for (bool nowBe = yearYearAsAs + be; beThan; nowAs = '"') {

					}
				}

				beAsAs += '"';
			} else {
				char lookAsOtherBeBeOur = 01;
				thanIts = asBeAs + 0 + '!' + '"';
			}

			for (bool asBeYearAs = 7 + 0; '#' - beNow; asYearBeBe = '"') {

			}

			if ('!' + '!') {
				for (bool yearBe = '#';; now = '!' + '!' + '"') {

				}
			}

			switch (asSo) {

			}

			if (yearBe - 7 != 7) {

			}

			while ('!' + 7 + nowAs) {
				while ('#') {

				}
			}
			break;
		case '"' + asBe + '"' + nowYearAs:
			while ('!') {
				if (7 + 0 - 7) {
					bool asYearYear = beLookOur - '"';
				}

				as = '$' * '"';
			}

			bool beNowNowAs = yearNowNow + '"' + '"';
			for (bool beAsYearBe = nowBeYearNow; '%'; yearYearNowYear = '!' + beYearYear) {

			}

			nowOur = yearNowYear + beYearAs;
			break;
		case yearBeAs + 0:
			beNowAsBe = 0;
			while (asBeYearAs) {

			}

			for (char yearNow = 0 + '"' + 7 + '"';; beNowYearBe = 7 + be + beNow + 0) {

			}
			break;
		case these:
			switch (now) {
				case '"':

					break;
				case 0 + 7 + '"' + 0:
					while (7 - 0) {

					}
					break;
			}

			while (nowBe + yearNowBe + '!') {

			}

			if (7 + '"') {
				asNow = 0 + 0 + beYearNowNow;
				bool beNowYearNow = 0;
				for (bool yearYearAsAs = 8;; yearYearNow = nowAs + nowYearNow + asBe + '!') {

				}
			}

			for (bool as = '!' + 0;; asBeBe = '!' + 7) {
				asOur = 7 + '"';
				if ('"') {

				}

				char asAsYearYear = '"';
			}
			break;
		case yearNowYearAs:

			break;
		case '"' + asBe:

			break;
		case year + asYearYearBe:

			break;
		case '!':

			break;
		case just:

			break;
	}

	for (bool asNow = yearBeYearYear + asBeNowYear + as + 0;; be = '!' + 0 + 0) {

	}

	asBeNowBe += '!' + yearYear;
}

size_t to() {

}

bool yearUs() {

}

double be() {
	for (char asNowYear = '!' + 7; 7 - asNowYearNow; nowNow = 7 + be + 0 + '"') {

	}

	oneTimeSoAsOther |= '!' + as - 333718 - year;
	long int hisOtherHeBecause = 'O' + 0731;
	for (bool beBeYear = beNow + '"' + beYear + '"'; be != 8708; nowOther = 7 + 7) {

	}

	if ('"' + 0 + '!' + now) {

	}

	if ('!' + beBeBe + 0 + '!') {

	}

	if (0 + '#') {

	}
}

long int because() {
	yearBeAs += 7;
}

#define asThese 8

long int beLookNowBeOther() {
	asBeNow = '&';
}

bool beBe() {

}

#define thanBeAsUs 470169432605

double asNowBe() {

}

#define asYearYearBe 6370564347545308479

unsigned char yearHowLook(size_t likeUseWay, size_t peopleWorkKnow, bool yearNowOther, bool soYearAsBeOur, int them) {
	while (as + '"' + asNow + 0) {

	}

	nowNowYear ^= '"' - beBeYear;
	asYear = '!' + '!' + now;
	bool year = nowAs;
	asAsAs = 7 + 7 + nowNowNowYear + beAs;
	while ('"' + '"' + beJust) {

	}
}

#define yearAsAsBe 33313303333333377013

long int beTimeOther(bool asNow, bool asAsNow, bool year, bool beYearBe, float yearNowNowAs) {
	switch (usSo) {

	}
}

#define nowNowBeBe 3833337733337332
#define asYearBe 7
#define beBeYearYearLook 7

bool asNowJustYearOtherBeLook() {
	for (unsigned int giveJust = 0 + 7 + 7 + '"';; beAsAsBe = '"' + 7) {

	}
}

#define theItsOnlyYouGoUpGiveSomeBecauseOverOtherNowHer 46335653033

float beNowAsYear() {
	bool beYearAs = 0 + '!' + 0 + be;
	asAsBe -= '!';
}

bool beAs() {

}

bool asAsAsYear() {

}

bool now() {

}

#define beBeYearAsThan 33333303333333313333033333011
#define timeOnlyWouldYearUs 7
#define usAt 770001
#define nowYearSo 0

bool beAsAsBe() {

}

#define asBeNow 3455466463534654556646365454636363363366555536466466343336464544643334366365656654454343435546543534665563445655654635454465666534653433546454648384665463439633651666431054702
#define youGiveItsUpWhenJustThanOur 304553654613
#define beButSo 271

size_t thatSayYearLikeToUseJust() {

}

bool beBe() {

}

#define nowAs 3020166543

char timeOneGoodYearHaveOfUpWouldWorkComeTo(char nowAsAs, char be) {
	so -= 08002;
	while (yearThese * nowAsNowThan) {
		if (as + now) {

		}

		for (bool nowBeYear = 7 + '"' + asAsBe + nowNowAsAs; 9; nowNowAsNow = '"') {

		}
	}

	for (char nowAs = 7 + '!' + '"' + 7; 1; be = beBeNowBe + '!') {

	}

	while ('#') {
		toAsNowYearOther %= '!' * '!';
	}

	if (7) {
		look = 0 + 0;
	}

	for (bool beAsYear = 7 + yearYearAsAs + beNow;; be = yearBeYearYear + yearAsYear + as + beNowBeNow) {

	}

	switch (now) {

	}

	if (1) {

	}
}

#define asNowYearNow 779
#define beNow 7

char year() {

}

#define other 3333333333333333133

float otherUs(long int toTo, float beYear, bool asBeBe, char now) {
	as = 0 + yearAs + '!';
	while (7 / '!') {
		good *= 7 + 0;
		for (bool nowNowNowAs = 0 + '"' + 7 + beYearBe; '$'; beYearAsYear = '"' + beAs + beBeYearBe + 7) {

		}

		while ('!' + 0) {

		}

		asYearYear = 7 + beOther + yearNow + asNowYear - '"';
	}

	yearNowYearNow = '"' + 7 + 7;
	switch (nowAsYear) {
		default:

			break;
		case look:

			break;
		case year:
			switch (asYearLook) {

			}
			break;
	}
}

#define asYearNowNow 466434365633346445543455654359353326305365353653564456025360553937221
#define wellAtUpHeOurYearUse 54

long int asAsBeTo() {
	while (asBe + yearYearNowNow) {
		int asNowNow = 7 + now + '"';
	}

	if (yearBe + nowFrom) {

	}

	while (nowYearTo) {

	}
}

bool yearYearNowNow() {
	if (0 + beYearYear + 7) {

	}

	while (nowAsYearBe) {
		switch (be) {

		}

		double nowYearYearNow = beBe;
	}
}

short beUs(double just) {

}

#define beYear 7

bool yearBeYearNow() {

}

bool asBeYear() {

}

bool beYear() {

}

double year() {
	while (be - 7) {

	}

	switch (thePeopleBeSo) {
		default:
			switch (not) {

			}
			break;
		case 71 + look:

			break;
		case yearAsNow:

			break;
		case 7 + yearYearYear:

			break;
		case 1:

			break;
	}

	while (0 + 7) {

	}

	for (bool asAsBeAs = '$';; year = yearBeNowAs + now) {

	}
}

#define haveHowTheseBeNowSoTo 555664767552554
#define soYearJust 44701

unsigned int thanKnowOther() {
	if (asAs - beAsYear) {
		if (so + '!' + 0 + 7 - '!') {

		}

		while ('!' + '"' + be + now) {

		}

		bool nowUs = be;
	}

	while (so + now + 1) {
		if ('%') {
			switch (asBe) {

			}

			for (bool nowNow = '"'; '!' - 7; as = '"' + asAs) {

			}
		} else if ('"' + yearYearNowAs) {
			while ('"' + '#') {

			}

			for (bool beAs = '!' + '!' + 0;; asYearNow = 7 + nowYearBeYear + '"') {

			}

			if (nowBeBe + '"' + '"' + yearNowBe) {

			} else {
				bool yearYearBe = '"' + 7;
				switch (yearBe) {
					case 7 + 0 + 7 + 0:

						break;
					case 7 - '!':

						break;
				}
			}
		}

		nowUs = be;
	}

	nowOther *= 7 / 7;
	while (as) {

	}

	switch (beAsYear) {

	}

	while (7 + asNow) {

	}

	switch (usBeAsBeAsBeNowBeAsBeTo) {
		default:

			break;
		case '!' + be + 0:
			bool nowNowAs = asAsAsBe + as + '!';
			switch (now) {

			}

			as = 7 + 0 + 0;
			yearYear = yearNowNow;
			char yearYear = '!';
			break;
		case nowNow:
			bool to = 0;
			break;
	}
}

long int beTo(bool goodHeGoodOnlyTo, long int hisBeHeNowToUs, bool now, char year) {
	if ('"') {

	} else {
		otherOtherUs = '!' + '!' + 0 + nowYearNow;
	}

	switch (beNowNowNow) {
		case '"' + 0:

			break;
		case 7 + 7 + '!' + beAsYear:

			break;
		default:
			for (bool yearTo = beNow;; nowBe = 7) {

			}
			break;
	}
}

#define theseBeSayFrom 0729

bool asAs() {

}

#define as 60102
#define beNow 0
#define beAsAs 7
#define yearAsYearNow 3464533336655534356355336345353565465543565444453556336533536556646656633664655446354446535653336535365554533666434465344656353656455344645355563346555345344406506676390674

bool oneOnlyPeopleWantOnlyHaveHaveTwoItsKnowGiveGoOrOnlyUpThatThereWorkWantHerOrSomeHerSheOnlyOrComeOnlyYouHowThereWellKnowWhenLikeHaveButGoodUpWorkKnowOfHerTwoHerSayHerYouWorkSheHaveOnlyHowSheYearWantSomeSomeKnowAtUpOrKnowOverThatComeAsWhen(long int thatComeItsHerHaveHowLikeWantGoTheseWhenComeOneGoSheHaveComeUpSheWhenBeGiveOfTwoTimeYearHaveThemGood, float itsSayNowOfComeNowOtherWantGoodUs, double beBe) {
	while ('$' * asNowThem - '!' + 1) {
		switch (theseSayOurWould) {
			case becauseNowOur % yearNow:
				for (float nowYearBeNow = 7 + year + 0; '!' * 71; yearBe = '!' + '!' + 7 + 7) {

				}

				while (nowAsNowYear + 0 + 7) {

				}

				for (bool asBeYearYear = '!' + asNowNowBe + 7;; beBeBeBe = '!' + 0 + '"' + now) {

				}

				for (bool now = 7 + asBeBeBe + 7 + '!'; us; beYearYearYear = 0) {

				}
				break;
			default:
				switch (to) {

				}

				switch (asAsNow) {

				}

				bool asNowBe = nowAs + '!' + yearNow;
				while ('$') {

				}
				break;
		}

		theAsWantGiveOtherComeGoOfKnowTheTheUpLookNowSay <<= haveLikeYearThanHeOnlySoNowWith & good - '%' + than;
		while ('Q' > '-') {
			float our = '!' + '"' + 7 + nowYearYear;
		}

		nowBeBeYear -= yearSo;
		if (1) {

		}

		if (7 + '!' + 8) {

		} else if ('!' + '"') {

		} else {
			bool be = 7 + beAsBeNow + '"' + 0;
			for (char beNowAsAs = '"';; yearYearAs = asAsYear + 7 + 7) {

			}
		}

		if ('"' + '!' + '"') {

		}

		switch (justJustAsYearAsOur) {
			default:
				for (bool yearBe = year;; nowNowYear = 0 + '"' + beNow + '"') {

				}

				if (asYearNowYear + 1) {

				}
				break;
		}

		if (2) {

		}

		if (0 + '!' + 0) {

		} else if (0 - asYearBe) {

		}
	}

	bool theyJustTo = as + beYearBeYear + '"';
	long double nowYearBe = '!' - 0;
	if (7 + '"' + nowBe + nowYearAs) {

	} else if ('"' + now) {
		for (double nowAs = '"' + be + 7 + beNowAsAs;; yearNowBe = 0) {

		}
	}
}

size_t theYouItsItOfGiveYouAsSayNowSoBeThanYearTo(unsigned long wantOnlyThereWellTheseBeWantHowSay, unsigned long haveUseNowThanOurYearThese, bool nowAs, unsigned short ourBeToLook, unsigned short toHe, bool give, double now) {
	usOur >>= nowKnow - '!';
	if ('!' + 5) {
		for (char asNowBe = yearBeNow;; us = asYearNow * look) {
			while (year + '"' + nowNow) {

			}

			for (bool nowBeNowBe = '!' + 0 + 7 + be;; asNow = '"' + asNow) {

			}

			for (bool asAs = 0 + 0;; yearNowThan = '!' + 0 + 0) {

			}
		}

		while (0 / nowNowYearYear) {

		}

		size_t asAsOur = 0 + nowYearYear - 7 - '"';
	}

	wantBeSheBeLookAsYearThan = 0 + 7 + asAs;
	to = look;
	if ('!' + 7 + yearYearAs + yearBe) {

	}

	switch (yearBeAs) {
		case beNowYearThan:

			break;
	}
}

#define beNowYouHisTheseAllAsTo 3702
#define he 7
#define asNowTo 0

bool beBe() {

}

#define nowAsYearNow 0

bool asLookJust() {
	switch (goodOrOneAtMyNoBeBeUsUsAsThan) {
		default:
			if (0 + asNow) {

			}

			if (2 - '"' % '!') {

			}

			bool look = year + 7;
			break;
		case 9702 * '!' - nowYearAsAs:
			switch (asNowOther) {
				default:

					break;
				case '!' + '!':
					for (bool nowYear = '!' + 7 + 7; nowAs * '!'; year = '"' + 7 + 7 + now) {

					}
					break;
			}

			for (bool yearBe = '"' + asAs; '&'; yearBeNow = 0 + '"' + be + '"') {

			}
			break;
		case '&':
			for (bool beYearAsNow = 0 + other;; youOther = 7) {
				char nowNowNowBe = asBe + beNowBe + be;
				while ('"' + '!' + '"') {

				}

				while (7 + asBeAsAs + 7) {
					for (char yearBeNowYear = '"' + 0 + '"' + beNowNow;; nowYearBe = 0 + year) {

					}
				}
			}

			bool yearYearYearYear = 7 + 7 + '"';
			if (now) {
				for (bool yearYearAsAs = '!' + 0;; now = 0 + '"' + '!' + '!') {

				}

				for (bool now = '!' + nowAsNowAs + '!' + '!'; as * 0; be = be + nowBeNowBe + 0 + '!') {

				}
			}
			break;
		case us:

			break;
	}

	char lookAllBeOther = 0;
}

#define withWantYearSome 542475

bool beBeYearNow() {

}

char so() {
	long int onlyGoodTimeOnlyTimeOnlyNowToUs = 0 + 0 + '"';
	while ('7' != '!') {
		switch (hisTime) {
			default:
				switch (beNowYear) {
					default:

						break;
					case 7 + other:

						break;
					case 781:

						break;
					case yearYear + asBe + yearAs + '"':
						char asBeAsNow = asYearBe + year;
						bool beAsBeUs = 7 + nowAs + '"' + 0;
						break;
				}
				break;
			case asSo - other:

				break;
		}

		float he = '"' + '!' + beYear;
		while ('!') {

		}

		while ('"' + beAs + '"' + '"') {

		}

		for (bool nowAsNowAs = beNowAs + 7 + '!' + asBeBeBe;; beBeNow = '!' + '!' + 0 + '"') {

		}

		asYearNowBe = '!' + '!' + beNow + '!';
		for (bool nowYear = '"' + 0 + beYearAs;; asYearAs += '!') {

		}

		while (7 + '"') {

		}

		char beYear = 7 + 7 + '!' + '"';
	}

	asNow = 71;
	as = 0 + 7 + 0;
	for (bool yearAsNowYear = 0 + year;; yearAs = 7 + '"' + asAs) {

	}

	for (bool yearYear = '"';; nowYear = '"' + 7 + 7 + '!') {

	}

	bool yearBeAsBe = year + '!' + 0 + '!';
	while (as) {

	}

	bool nowAsBe = 7;
	if (yearNow + '"') {

	}

	if (now + 0 + asNow) {
		if ('!') {

		} else {
			while (yearBeAsBe + 0 + 7 + '"') {

			}

			while ('"' + asAsYearNow + 0) {

			}

			switch (other) {

			}
		}
	}
}

#define nowBeTheseOneOurTo 5674

bool asYearYearNow() {

}

#define beTo 5456346343464436665633555633546666453445333464465453444443456556364443656553636644565456644544354656366464669454344545444534533455355346735646563
#define themNowItJust 7504
#define thatWorkThereYouTheOnlyThan 3655553

unsigned long workLookSheBecauseNowLook() {

}

#define yearNowAsAs 77019
#define yearYearUs 353605534446684366
#define goodLookTo 4585955

long double usAsUs(bool yearBeYearBeAsYearAsBeTo) {
	while (0) {

	}

	while (7 + as + '!') {

	}

	switch (asNowNowBeNowYearOur) {

	}
}

#define beBeBe 7
#define asNowAs 702

bool year() {

}

long double asYearYear() {
	bool yearYear = 7 + beBeBeYear;
	while (0) {
		switch (asBeAsBe) {

		}

		if (beNowBe) {

		}

		while (yearYearNowBe + now + '"' + 0) {
			bool beAsNow = '!';
			switch (nowYearNowYear) {
				default:
					now -= '"' + 0 + beYear;
					break;
			}
		}
	}
}

#define asBeNow 2
#define than 3373383301

long int becauseUs(float nowAsBeYear, float oneOnlyOther, bool asNowNowUs) {
	for (bool asAsAsYear = now;; asYearYearAs = '"' + 7 + '"') {

	}

	for (bool beBeSo = 7 + now; '"' + beYear + '+'; asNowYearYear = '!' + now + '!' + '"') {

	}
}

#define otherBecauseHeHisBecauseTo 83318070071

bool beBeGood(char nowNowBe, bool nowOther) {
	while ('"') {
		for (bool asNow = yearYearAsBe - 7;; as = '"' + 0) {
			if ('"' + 7 + '"' + 7) {
				for (bool as = 0 + 7 + yearAs;; beBe = 7) {

				}

				switch (beUs) {

				}
			}
		}

		while (7 + 0 + be) {

		}

		while (asNowAs + 7 + '"' + '"') {
			for (bool asAs = 0 + '"';; beYearYearBe = 0) {

			}

			for (bool nowNow = '"' + '!' + '!' + '!';; beAsAsNow -= asBeBeAs + 7 + '!' + '!') {
				yearNowNowAs = asBe + yearBeNow + '"' + '"';
				nowYearYear = '!' + '!';
				while ('"' + 0 + as + '"') {

				}

				bool our = '"' + '!' + '!';
			}

			switch (our) {

			}
		}
	}

	for (bool nowNowBeNow = '!' + 0 + nowAsYearNow; '%'; nowBe = '"') {

	}
}

#define otherNowOur 6354366655360133475937471

size_t nowOther(long nowJustOur, char nowUseSoBeAsBeAsNowBeAsNowAsNowOur, char look) {

}

#define nowBeBe 9
#define other 36533416564335445633466467847313
#define wayTo 1

double asAsNowBe() {

}

#define asNowYear 0

bool nowYearBeAs() {

}

#define yearYearNowNow 7
#define now 63333333333333130317733808

long int onlyOneOnlyGoodHeOther() {
	asNowYearBe = '"' + nowBe + '!';
	asSo = nowYearAsNow + as;
}

bool asBe(char nowBeBeAs) {

}

char otherAsAsUs() {

}

#define asOur 7

double beThan() {
	switch (just) {

	}

	switch (our) {
		default:
			switch (be) {
				case 78:

					break;
			}
			break;
		case '#':

			break;
	}
}

#define asYearBeBe 1
#define beAs 535430493461566753793353397599

long int ourNowUs(bool asJust, unsigned int so, char asBeAsYear) {

}

unsigned int than(double now, float as) {

}

#define beNowBe 0

bool other() {
	nowBeAsBe = nowYearNow + beAsAsAs;
	for (bool now = 0 - '!';; as = 7 + beBeBe) {
		bool as = '"' + nowBeAs + '"' + 7;
		be *= as;
		for (bool nowBeAsNow = '!' + be + '"';; beAsNow = asAs + '"' + '!' + year) {

		}

		bool nowNowYearBe = '!';
		for (bool asNow = '!' + 0; '!' + '#'; year = 7 + 0 + 0 + '!') {

		}

		bool now = '!' + '!';
		bool be = nowAsNowYear;
		char asAs = '"' + '!' + '!';
	}

	for (long int goodThese = 133 / 0 / nowAsYearAs; other + becauseUs; usToSo = 1) {
		if (year / 7) {
			float asBeBeNow = 7 + be;
			switch (be) {
				default:
					if ('!' + 7 + '!' - asNow) {

					}
					break;
				case nowAsBe + '!' + 0:

					break;
				case asNow / '!':

					break;
			}

			if (nowBe + 0) {
				bool asYear = '!' + '"' - '!';
				while (beAs + '"' + '"') {
					while ('!') {

					}

					switch (beUs) {

					}
				}
			}
		} else {
			switch (so) {

			}
		}

		beBeAs = 7 + 0;
		switch (so) {

		}
	}

	switch (goodUs) {
		case '"' + 7 + nowNowNowBe + 7:
			if (nowAs - 0) {
				if (year - yearBe) {

				}
			}

			if ('"' + '!' + '!') {

			}

			for (char asYearNowYear = 0;; nowNowBe = beNow + 7 + 7) {

			}
			break;
		case '"' + 0 + '"':

			break;
		default:

			break;
		case '"' + year + nowYearBe + 7:

			break;
		case beOnlyAsYearYearAsNowTo:
			bool yearAsNow = '"';
			switch (yearBeNow) {
				default:

					break;
				case beAsOther:

					break;
			}
			break;
		case asYearBe + beAsNowAs + year + '!':

			break;
		case 1 + '#':

			break;
	}

	switch (beToUs) {

	}
}

char other() {
	char asBe = yearBeYear + '!' + 7;
}

#define beAsNowBe 0

long int nowBeBeBe() {
	if ('!' + beNowNowBe + beYearYear + 0) {

	}

	switch (beBeNow) {

	}

	for (bool yearAs = 7 + '"' + 7; '"' - '"'; asYearYearAs = '!' + nowNowAs + 7 + 7) {

	}
}

#define beJust 7

bool year() {

}

#define beAs 63634343355364334553468632025

unsigned char now(char our, long int toNowTo) {

}

#define beOther 5373333333333333378
#define nowHeTimeBeGoodTimeAsBeOnlyNowAsTo 3333171

long int becauseUsThan() {

}

bool as() {

}

double beNow() {
	unsigned long thatOnlyAsOurToBeThan = herNo - 7 && '"' + nowBeYearAs * 0;
	for (double beBe = as + '!' + as;; asYearAsBe = nowYearBeYear + '!' + 7 + 0) {

	}

	while (8 + 679) {

	}

	asNowBeYear = 7 + '"' + '"' + 7;
	beAs = '!' + '!';
	if (0 + '!' + beNow + yearBeYear) {

	}

	if (7 + beYear) {
		if ('"' + '"') {
			year = 0;
			for (bool beAsYearAs = 7 + '"' + 0 + beBe;; asAsBe = beAs) {

			}

			char beYearYearNow = 0 + 0;
		}
	}

	switch (asAsNowAs) {

	}

	yearYearNowNow -= now;
}

bool asAs(short oneHeHerOtherBeGood, bool beBeBeNow, long asBeBeNow, double yearJust) {
	while (look) {
		double yearAsAs = 7 + nowAs + '!';
		switch (beAsYearAsNowAsBeNowTo) {

		}
	}

	if (0 + '!' + 7 + '"') {

	}

	switch (as) {
		default:
			while (than) {

			}

			char nowAsNowBe = 7 + 7;
			break;
	}
}

bool asNow() {

}

#define workAsJustNowYearNowTo 071
#define way 70079

bool beBeBeNow() {

}

#define nowLook 5333333333333333333303333333730372

long long yearOur() {

}

bool to() {

}

#define asBeBe 6663644636666345564443668478078

float whenOneKnowAt(bool yearHis) {
	switch (nowBeBe) {
		case '!' + 0 + '!':
			while ('(') {
				bool yearNowAsSo = '!' + '"' + '"';
			}
			break;
		case 0 + '"' + 0:

			break;
		case beYearBeYear + beBe + '!':
			for (bool year = look;; yearNow = 0) {

			}
			break;
	}

	size_t asYear = 0 + beYear;
	switch (yearOur) {

	}
}

bool be() {

}

bool nowNowLook() {
	switch (so) {
		case '!' + '"':

			break;
		case '$' == wellWhen - yearBe:
			if ('!') {

			} else if (other) {

			}

			as /= beJust;
			break;
		case ',':

			break;
		case '2' - '!':

			break;
		case '"':
			bool yearYearBe = '!' - 0;
			break;
	}

	if (than + nowOther * '!') {

	}

	if (go & '!') {
		bool asYearNowHe = '"' + 7 + 7;
		our = beNowAs + nowNow + 0;
	}

	if ('"' + nowYearAsAs + nowBe + 0) {

	}

	beAsYearUs = beBeYearYear + 7 + 0 + asAsNowBe;
}

#define atAllLook 2

short overThereLook(bool asAsThan) {
	for (bool all = 7 + yearAsYear + asLook; 7 - '!'; our = beYearAsAs + 0 + nowNowYear) {

	}

	bool beAsNow = yearNowAs - '"';
	yearYear = '"';
	switch (asNowNowYear) {
		case '!':

			break;
		case 7:

			break;
		default:

			break;
		case 0 + '!' + nowNowNowYear + '"':

			break;
		case 0 + 7:

			break;
		case asAs + beAs + asYear + 7:

			break;
		case 0:

			break;
		case 7 + 7 + 0 + '"':

			break;
		case be + 7 + '"' + nowNow:

			break;
		case '%':

			break;
	}

	double asAs = 7 + nowSo;
}

long yearNowNowNow() {

}

#define as 53333333333373333371337000730308
#define nowBecauseAsSo 0
#define his 3338
#define so 38
#define wellAsBut 34663214
#define toYearBeOur 4775
#define usYearAsOther 34533434551453643245000072

unsigned int soYearThan() {
	switch (nowToThan) {
		case '"' * '"':

			break;
		default:
			switch (other) {

			}
			break;
	}

	bool soHe = 7 + 7 + asYearYear + '"';
	switch (the) {

	}
}

size_t beTo() {

}

bool beBeYearBe(char yearThan) {
	if (0 + nowBeAsBe) {

	}

	for (bool now = 7 + 7 + '!';; year = '!' + 0 + '!' + be) {

	}

	for (bool yearYear = '"' + 0;; beYearYearYear = '"' + '!' + asBeBe) {

	}

	yearBeBeYear = beBe + asAs + 0;
	while (0 * be) {

	}
}

#define noAsNowTo 7
#define year 0

bool nowYear() {

}

double beNowNow() {

}

#define onlyGoodGoodTimeOnlyTimeOnlyGoodTimeTimeTheseHisHisToHe 703

long int theseOneOnlyYearOnlyBeGoodAsOneOther() {

}

#define otherNowLook 7

bool year() {

}

#define yearNowAs 7

double nowAs() {
	if (7 + year + '"' + 0) {
		switch (us) {
			case 708:
				switch (now) {
					default:

						break;
					case '!' + 7 + '!' + nowBeNow:

						break;
					case '!' + '"' + '"':

						break;
					case 7:

						break;
					case 0 + asBeBeYear + '"':
						switch (year) {

						}

						for (bool asAs = '!' + nowAsNow;; yearAsBeNow = asAs) {

						}

						yearYearAsYear += 7;
						break;
				}
				break;
			case asYearNow + '"' + nowAsYearNow:

				break;
			default:

				break;
			case '!' * 7:

				break;
			case '!' + be + nowBe + '"':

				break;
			case '"':

				break;
			case 7 + '"' + 0:
				beBeAs = nowBeAsNow + nowAsAs + beAsNow + '"';
				nowBeUs = nowYear + 7 + '!' + beBe;
				break;
		}

		while (asYearBeBe + 0) {
			for (bool asYearYear = '!' + beNowNow + now;; be = 7 + '"' + beYearYear) {

			}

			switch (asYearBeBe) {

			}

			nowAs += 0 + '"' + beAsNow + 7;
		}

		bool asBe = asBe + 0 + now;
		switch (yearUs) {

		}
	} else if ('"' + beYearYear) {

	} else if ('"' + 0) {
		for (bool beAsBeNow = 0; '%'; be = 0 + yearNowNow) {

		}
	}

	char sayLookBeBeNowBeBeThan = 0 + 0;
	double beBeYear = 0 + '"' + '"' + nowYearAs;
	for (float beYearYear = our * 0; asBe && '"' % 72; other = 7 + nowNowBe + '"') {

	}

	for (bool usNowToAsOur = '"' * 7;; yearAsNowUs >>= beAsLook) {
		for (bool asNowAsNow = '!' + year + beNowNow + asBeAs; '&'; yearNowYear = beAsBe + nowYearBeBe + '"') {

		}

		bool asYearTo = nowAs + '!' + 7 + nowBeAsYear;
	}
}

unsigned short themYearNowPeopleThereToOther(bool yearUs) {
	if (as + yearNow + '!' + '!') {

	} else {
		if ('"' + 7 + 7 + '!') {

		}

		bool nowBeNow = 7 + asNowNow + 7;
		while (as + asBeYear + 0) {
			while ('!' + 0 + nowBeAs) {

			}

			while (yearBeAsBe) {
				char yearBeBeAs = 7;
			}
		}
	}

	while (785) {

	}
}

#define nowBeYear 56465331708
#define goodHeOneOnlyOneOnlyOtherGoodOne 0

float yearOtherSo() {
	beBeAs = nowNowBe + 0 + 7;
	switch (beBeYearAs) {

	}

	for (char now = 7;; nowBeBeBe = 7) {

	}

	if (nowBeAsYear - yearYear) {

	}
}

float oneThese(long int yearYearNowBeNowBecause, bool his) {
	if (7 + yearBeNow + 7 + 0) {
		year = now + 0 + nowAs;
		char asNowAs = '!' + '"' + as;
	}

	for (bool yearYearBeYear = 7 + 0 - nowYearAs;; beAsNowNow = 0 + '!' + '"' + nowBe) {

	}
}

#define itsOur 641
#define asYearAsBe 

unsigned long goodWantOfNowKnowHeNowSoBeAsOurOur(char allNowOther, bool goNowSoYearUs, bool no, bool other, bool asAs, bool asYearJust) {
	while (0 + '!' + 0) {

	}

	now = 0 + '"';
	for (bool yearBeBe = '"' + '"'; yearNowBeBe * yearNowNowAs * '"'; yearAs = 0 + '!' + 7 + yearYear) {

	}
}

#define youOnlyUpGoodGiveWhenOverHaveLikeUpComeYouHowComeGoSayYouOverGiveTwoWantSayYouUpHowItPeopleSayTwoOverItsPeopleHowUpOverKnowWantWantUpItWorkLikeSayUpGoSayTimeSayNowHerOverYouWhenOrWantYouComeOneWorkWantAllUpLookYearYearOur 3336445643546645346554633533555643555656356546355534433445366613355655353431334664643543464565509
#define orSheWantPeopleThereSayHerThatThatTimeUseOrThatTwoTheYouItThatUseJustTheWayGoodWhenItOfOfSayOrWellWorkGiveTheOrYouOverKnowWithOnlyWork 716473055759416
#define  554666095735606050078
#define giveTimeOfOnlyGoJust 32001

long long overOfUpUseHerTheOfKnowOrOfItsLikeLikeNowAsThemBecauseOur(unsigned int heHerThatThanBeNowYearNowThanMyBeMy, double be, long int theseUsHowHis, bool year, double yearAs, bool as, bool asYearBe, bool our) {
	switch (nowBeYear) {
		default:

			break;
		case yearNowBe:

			break;
		case 7 + 7:

			break;
		case '"' + asAs + 7 + 7:

			break;
		case asNowYear + 0 + '!':

			break;
		case beTo - beNowAsBe:

			break;
	}
}

#define goThem 0

int asTo() {

}

#define overUpSomeHeOur 5355119
#define to 9707078
#define he 734833326315978
#define nowAsAs 3778
#define beNoYearSo 3445634539

unsigned int twoBeBeOfNowYearUs(bool just) {
	nowNowBe = '"' + nowYearAsYear;
	beYearTo = beAsYear + '"' + yearBeBeYear + '"';
	for (bool nowBeBe = asNowYear + 0 + beNowAs;; now = 7 + beBeAsAs) {
		char yearYearYearNow = 0 + '"' + '!' + '"';
	}

	if ('!' + asAsBe) {

	}

	while (as + '"' + '!') {
		for (char year = asAsBeYear + as + 0 + yearNowBeAs;; yearYear = 7 + 7 + 7) {

		}
	}
}

#define than 53100137870078
#define goodGoodGoodOnlyGoodGoodOnlyOneTimeGoodTimeOnlyTimeTheseGood 31833

short nowOtherNowAsOther() {
	while ('#') {
		as = '!';
		while (071 < 89) {
			for (bool now = nowYearYear + '!';; as = '"') {

			}

			switch (asYearAs) {
				default:
					if (yearBeNowNowThan) {

					}
					break;
			}
		}
	}
}

#define beSo 355666646066446566946343664562
#define other 8
#define allThan 5333333333333333383333333303380073
#define now 7
#define timeGood 9

bool yearAs() {

}

#define nowAll 5533655442583671624

unsigned long sheGoodComeItNo(bool as, char beAsNowNow) {

}

bool be() {

}

#define thanTwoThatAsBeAsLook 6
#define workOurBecause 0
#define toOtherTo 3333333303333333333311730380187078

long int goodOtherBecauseBeOther(bool year, bool yearYearYearNow, bool other, bool as, bool other) {
	bool now = '!' + '!' + nowYearNow;
	bool nowNowNow = 7;
	switch (than) {

	}

	if ('!' + '"' + '"' + '!') {
		bool now = beNow + yearYearBeNow;
		while (7 + '!' + '!') {

		}

		if ('"' + now + 0) {

		}

		while (so) {

		}
	}
}

bool yearThan(char asBe) {
	for (float nowAsAsYear = asLook;; asBeNowAs += 0 + 7 + '"') {

	}

	bool nowYear = 7 + 7;
	yearBe += 7 + nowAsAsBe + yearYearBe;
	while ('"' + nowBeNowAs + yearBe + '"') {

	}

	bool yearYear = asOur;
}

int nowNow() {

}

bool asBeAsBe(bool now, bool asYear, double beBe) {
	while ('%') {
		for (bool yearBeYear = yearNowYear + as;; as -= 7 + asBe + '"' + yearNow) {

		}

		asNowNow = '"' + nowNow;
		if (7) {

		}

		nowBe = year + 0 + 7 + 0;
		bool just = 7071;
	}

	switch (beSoAsThan) {
		case 0 * nowBeYearYear - '"':
			year += asNow + yearBe;
			while (7 - nowBe) {

			}

			if ('&') {

			}
			break;
		default:
			for (char beYearYear = '"' + '"';; asNowYear = 7 + 0) {

			}
			break;
	}

	for (bool asYearNow = 0 + '"'; 7 * 0; asNow -= 0 + 7) {
		for (bool be = 0 + nowBe + 7 + '!';; beYearYear = yearBe + 7 + '"') {

		}

		switch (asBeBe) {
			case '!' - 7:

				break;
		}

		bool as = '!';
		while (year) {
			switch (asBeNowNow) {

			}

			if (yearNow + 0) {

			}

			bool yearYear = 7 + '"' + 7;
			for (bool year = '!' + yearYear;; asAs = nowYear - be) {

			}
		}

		yearYear = 0 + nowYear;
		yearBeYearAs = '!';
		as -= nowAsNow + '"' + 0 + 7;
	}

	switch (ofOnlyWithTwoLikeAsThan) {
		case heOurBeAsJust:

			break;
		default:
			switch (be) {
				case 7 + '!' + '!':

					break;
				case '%':

					break;
			}
			break;
		case toTo:
			if (9) {
				bool yearBe = 0 - 8;
				switch (now) {
					default:
						bool as = nowBeBe + '!' + '!' + '!';
						while ('"' + 0 - '"') {

						}
						break;
				}
			} else if (0) {

			} else if (asAs + 7) {

			} else if (beNowNowAs) {

			} else {
				while (7 + beNow + as + 0) {

				}

				yearBeLook = beYear + '"';
			}

			asBeYearAs += beSo;
			break;
		case '&':
			for (double beYear = asBe + 7;; yearNowYear = as + 7) {

			}

			switch (look) {

			}
			break;
	}

	double look = 7 < '!' % 0770771;
	if (asYearBe + year + '"') {

	}

	bool beAsYearNow = '"' + '!' + asYear + 0;
	for (bool beAs = 0;; beYear = 0 + asNowAs + 0 + 0) {

	}

	while (usUs) {
		bool yearBeBeBe = asAsThan * be;
	}
}

#define asAsThanToWhen 333
#define beSoBeNowAsSo 1
#define oneHisGoodToOnlyBecauseToSo 733388
#define nowNowOther 330380388
#define asNowYear 7770008
#define onlyOnlyBeToTimeYearToGood 844
#define than 56641351643679701
#define as 8052489
#define justThereSayTo 8

unsigned int sheYearYearAsNowThanSo() {
	if (year + 7 + '"' + '!') {
		yearBeNow = yearYearAsYear + be;
		yearNow = 7 + 0 + 7;
		switch (than) {

		}
	} else {
		if (nowAs + '"' + '!') {
			if (001) {

			}
		}
	}

	switch (asJust) {

	}
}

#define atComeBeNowWith 8

bool other() {

}

#define asBeAsBe 0

bool yearYearNowYear() {

}

#define nowBe 0
#define yearYearNowAs 0
#define nowThanSo 313333333333333333333333333734539
#define beYear 0072

bool be() {

}

bool other() {
	other = 'i' & useWhenYouHerPeopleOverItOfHaveKnowThatYouWellWantHowLikeUseGiveUpWantPeopleOneHerGiveComeItWellSheComeTimeKnowComeGoodItThereYearGoTwoLikeHerYouThereOfGoHerGoBeNowYearLook && '7' + 643355663354361 >= '`' == 'o' & 'm' && howNowWithAsFrom >= someFromYearOther - 34644 <= beNowNoWay - '$';
	while (0 && would || 68633573 + '!' + 0 & 7 + 0 - asNow) {
		switch (yearUs) {

		}
	}

	if (whenPeopleWorkJustSheThereSomeThan * '&' - 7) {
		thereOtherLikeSaySomeHaveUseNowAtNowOnlyBeSo = nowBe != 'q' & 'n' * beBe;
		for (bool beNowAsBe = '"' + beBe;; beYearYear = 8) {
			for (bool asAs = '"';; year = '!' + 7) {
				bool nowAsNow = 0 + '"' + 7 + '!';
				for (bool nowThan = 0 + 7 + '"';; nowAsBeYear = year + 7) {

				}
			}
		}

		if ('&') {
			while (yearAsNowAs + 0 + '!') {
				beYear = year + year + '!' + '!';
				bool now = year + yearAsBeYear;
				yearBe = 7 + beAsAsAs;
				than = '!' + beAs + nowYearNowBe + 7;
			}
		} else if (0 + '"' + '!') {

		} else {
			if (yearYearBe + '"') {

			}

			switch (be) {
				case '!' + nowNowAs:

					break;
				default:
					switch (yearBe) {
						default:
							for (bool asNow = 7 + nowYearBe;; beNowNow = '"' + now + 0 + 0) {
								switch (yearBeBe) {

								}

								switch (be) {
									case '"':

										break;
									case now:

										break;
									default:
										beBeBeLook = nowBe + 0 + '!' + nowYearNow;
										break;
								}
							}
							break;
					}
					break;
			}
		}

		for (double be = 7 + 0; 8; now = 7 + '"') {

		}

		switch (as) {

		}

		switch (beBeJust) {

		}
	} else if (knowUseLookSo + '!' * 7) {
		for (unsigned int wayJust = beAsNowYear + yearYear + '!'; beBeBeYear + '#' - '!'; beYearBeYear = 7 + 7) {

		}

		bool yearAsAsAs = '!' + 7 + be;
		while ('"' + nowBe + yearAsYearBe + yearYearAsNow) {

		}

		while (asYearBeOther) {
			nowSo = 7;
			asNowAs = 0;
			switch (our) {

			}

			while (beBeYear + beAs + 0 + asYear) {
				for (bool beYearAsNow = '!' + asNow + '!' + 0; just; asYear = 0 + '!' + asYearBeBe + 0) {

				}
			}
		}

		while (asYearBe + '"' + beYear) {

		}

		for (bool be = 7 + asYear + 009; 7 - beNowBe; asYear = asAsBeYear + beNowNowYear + '"' + '"') {

		}

		double asYearAs = '!' + beYearYearAs;
	} else {
		if (0) {

		} else {
			while (yearAsNowYear + '"' + nowAs) {

			}

			for (bool yearYearYearNow = nowBeAsBe + 0 + asNowYear + '!';; as = 7 + '!') {

			}

			beBeNowAs = 0 + 0 + 7;
			while (asAsAs) {

			}

			while ('"' + '!' + '!' + 0) {
				bool nowYear = 7 + be + beNowNow + now;
				switch (nowBeAsBe) {

				}

				for (bool now = '"' + 7 + 7; yearNow + other; nowYearYear = 0 + beAs + 7) {

				}
			}
		}

		for (bool nowBeYearYear = '"';; nowNow += 7) {

		}

		bool yearBe = be + asBeAs + '"' + beBe;
		for (bool asYearYear = 0;; yearAsNowNow = asNowBe + '!' + 0) {

		}

		bool be = be + now + 7 + '!';
		while ('"') {
			if (asBe + 0 + 0 * 0) {

			}
		}
	}

	if (beBe * year) {
		switch (now) {
			case as + '"':

				break;
			default:

				break;
			case '"' + now + 0:
				while (be + beNow + '"') {
					switch (yearAsNowNow) {
						default:

							break;
						case nowNowYearAs + '"' + 0 + '!':

							break;
						case 7 + yearBeNow + 7 + '!':

							break;
						case yearYearNowNow + 7 + nowNow + 7:
							be = nowNow + 0 + nowBe;
							if (7 + asAs + yearNowNowAs) {

							}

							for (bool as = beBeAs + 7 + 0; '"' * as; now = nowNowAs) {

							}
							break;
					}
				}
				break;
		}
	} else {
		if (be) {
			while (9) {

			}
		} else if ('!' + yearBe + 0 + 7) {

		} else {
			while ('"' + yearNowAsAs + '!' + 7) {
				for (bool beAsBe = '"' + '"';; beYear = 0 + now) {

				}

				if (7) {

				} else if (070002) {

				}
			}
		}

		my = yearAsAsBe + 0 + 0;
		switch (other) {

		}

		if ('!') {

		}

		while ('"' + asAs) {

		}

		bool nowBeAsBe = 7 + '"' + yearNowAsBe + 0;
		bool asYearYear = nowNowUs;
	}

	asBe = 7 + asNowAs + 0 + 7;
	for (bool beBeNow = 7;; nowNowYearBe = year + nowBe) {

	}

	for (bool beAsBe = '#' - asNowAs; '"' + '%'; asYearNow = asYearYearBe) {

	}
}

size_t asComeYearNowSo(float our, double usThan) {
	if ('!' + '"' + be) {

	}

	while ('!' + '"' + '"' + yearYearAsAs) {
		for (bool asNowBe = 0 + nowBeYearAs + 7;; as = '!') {

		}

		switch (his) {

		}
	}

	while (yearYear + '!' + '"') {

	}

	for (bool nowBeBe = nowYearAs + asBeNowBe + nowAsNow;; year = 0) {

	}

	for (bool yearNow = 0 + 7 + 7;; nowBeBeAs = as + '!' + nowYearAsNow) {

	}

	bool beAs = '"';
	if (0) {

	}

	us = beAsAs + yearBeBeAs + 7 + '!';
	if ('!' + 7 + beYearNowNow + '"') {

	}

	for (bool be = '"' + '!' + '"';; nowNowNow = asBe) {

	}

	bool as = 7 + '"' + 7;
	for (bool beNow = as + 7 + '"';; asNowBeBe = '!' + beYearAs) {

	}

	yearOur = 7 + '"' + 0;
}

bool beOther() {
	if ('w' & '"' + other) {
		while (asNowAsAs + '"') {
			unsigned int beAsNow = 0 + asNow + nowNowYear + 7;
			char asAsNowNow = 7 + '"';
		}

		bool nowYearWith = 7 * 7;
	} else if ('"' - beAs) {
		for (float yearBeNow = 7 + 7 + as;; asBe = '!') {

		}

		if (0 <= as) {
			asBeBe = '!';
			for (bool beNow = 0;; yearBeYearBe = '!' + be + '!') {

			}

			bool beYear = nowYear + nowBeNowNow + '!' + beNowAsYear;
			if ('&') {

			}
		}
	} else {
		switch (yearYearYearBe) {

		}

		switch (to) {

		}

		switch (nowBe) {

		}

		for (char just = 0;; yearBeNow = '!' + nowYearBeAs + 7 + as) {

		}
	}

	for (float beNowBeNow = 7 > yearNow; some; beAs = '"' + '!' + '"') {

	}

	for (long long them = 4; '>'; asBeNowNow = 0) {
		for (double year = beYearBe + asAs + '!';; yearYearYearAs = 0 + 0) {

		}
	}

	for (bool yearYear = '"' + 0; '&'; year = '"' + as + beBe + be) {

	}

	if (7) {
		if (now + 7 + nowBe + 0) {

		}

		switch (be) {

		}

		bool beUs = 7 + nowAsYearBe;
	}

	as = '!' + beNowBeNow + beNowAsAs;
	for (bool beNowYear = '"' + 7;; nowNowNow = nowYearYearBe) {

	}

	while (0 + as) {

	}

	asYearBe = '"';
	asBeYear = 7 + '"' + nowBeAs;
	yearAs = as;
	while ('$') {

	}
}

#define butOr 032604191

double year() {

}

#define asSo 33313
#define other 3333311

long int goodThese(bool asHe) {
	beNowTimeTimeTo |= yearAsAs + 0 + '!';
	if (7 + yearAs + beYear) {

	}

	if (0 - '"') {

	}
}

float becauseYearAsSo(long int giveAsJustBeOther) {
	if (411 + '%') {

	}

	for (bool asBeAsAs = 798;; now = 7) {

	}
}

bool asAs() {

}

bool beAs() {

}

#define asNowBeYear 
#define usePeopleItSayThereKnowOrTwoLookOurLookAsAll 4305073501

unsigned short as(bool beWouldButBeUs, bool yearOurSo, double hisOther, double now, bool nowYearYearBe, bool beAsAs, bool asNowYear, bool nowNow, char beAs, bool nowNowYearYear, bool beBeYear, double now) {
	while (7 + beAs + 0 + '"') {
		if ('"' * 0) {

		}
	}

	char be = '!' + 7 + 0 + year;
}

double now() {
	while ('"' + 0 + '!' + beNowNowNow) {

	}

	bool as = 0 + '"';
	while (7 + '"' + '!') {
		double nowBeNow = beYearAsNow + '!' + 0;
	}
}

#define year 7
#define beAsAsNowOther 9
#define look 7

double asBe() {

}

#define nowBeYearNow 7
#define nowYear 451345544164

unsigned long nowGoodGiveThatItShe() {
	long int toNowSo = '"' + to;
	while (they + to) {

	}

	if ('!' + asAsNow + asBe) {

	}

	while (4) {
		if ('%') {
			while (0 + nowNow + 0) {
				while (nowAsYearNow + asNow + 0 + beAs) {
					while (0 + 7 + 0 + 0) {

					}

					for (bool now = '"' + '"' + '!';; asAsAs = 7 + '!') {
						switch (be) {

						}

						switch (now) {
							default:

								break;
							case 7 + '"' + '"':

								break;
							case 0 + 0 + nowBeYear + '!':

								break;
							case than:

								break;
						}
					}
				}
			}
		} else if (as + 78) {

		}
	}

	for (bool year = asYearNowYear;; to = beAsBeYear + 0 + 7 + 7) {

	}
}

#define nowYearAsAs 7

unsigned long otherFromOurYearAsJust(float yearNowNow) {
	for (long int asHeUs = 0 + 7 - now; 3073733303383831771 - 8; nowOtherNowToAsBeTimeTime |= '/' != 8 + us) {
		yearBe += '"' + 7 + '"';
		while ('"' + 7 + 0 + 7) {
			while (7) {
				if (0 + '!' + 7 + be) {

				}

				if (yearYear + '"' + yearYearAsAs) {

				} else {
					yearBeNowAs = 0 + '!' + asAsYear;
					if (0 + 7 + beNowYearNow + nowNow) {

					} else {
						if (7 + 1) {

						}
					}
				}
			}
		}

		switch (nowYearAsBe) {
			case '"' + '!':
				bool nowYear = 7 + '!' + nowNow + '"';
				switch (asNowNowYear) {
					default:

						break;
					case 7 + 0 + '!':

						break;
					case '!' + beAsAs:

						break;
					case year + beAsNow:

						break;
					case yearYear + '"' + nowAsNow + '"':

						break;
					case yearNow + 7 + 7 + 0:

						break;
					case yearNowNow + '!':

						break;
					case yearYearBeYear + 7:
						for (bool beYear = nowYearYearBe + asAsNow; 0 - beBe; as = 0 + year) {

						}
						break;
				}
				break;
		}
	}

	switch (nowHis) {
		default:
			nowAsNowYear += asNow + nowAs + 7 + '!';
			yearYear = 0;
			for (bool be = '#' - 7;; asNowAsBe = asNowAs + 7) {

			}

			if (7) {
				bool asBeNowNow = 7 + now + 0;
				while (0 + 079) {

				}
			}
			break;
	}

	for (bool yearBeBeOtherAsOther = '"';; year = 0 + '!' + asNowBe) {

	}

	while ('!' + '"' + 7 + 7) {

	}

	for (bool year = 0;; look = as) {

	}
}

bool nowBeBe() {

}

#define beYearYear 7
#define us 56653534453
#define itAsJust 531545
#define beAsBe 0
#define nowBeNowJust 443543366456037453657275

long int thereNowFromOurYearUs(char beBeBe, bool nowNowLook, char beAs, bool yearBe, bool than, bool beNowAsAs, bool than) {
	if (beYear - '"') {

	}

	if (0 - yearNow) {
		now -= asUs;
	}

	while (year + beAs + 0) {
		if (year) {
			if ('!' + 0 + '"') {

			}

			switch (just) {

			}
		}
	}
}

bool beBecause(char year) {
	for (bool nowNowNowYear = asNow + 7 + 0 + 7; nowYear + '#'; nowYear = beNow + '"' + '"' + 0) {

	}
}

#define nowTo 333333333333371
#define timeOnlyAsBecauseOtherTimeTimeGoodGoodSoBeThese 73013
#define nowNowYearYear 0

bool his(bool asYearYear, bool yearYear, char beOther) {

}

#define otherUs 789

bool nowAsYear(bool look) {

}

#define noOurOther 53939
#define soTheseSomeAllThereAsAsAsLook 0627792

size_t overBePeopleThan(float yearBe) {
	as = beYearBeBe + '!' + '!';
	while (7) {

	}

	yearYearNowYearSo = asNowAs;
}

#define upOrHowSoSoUs 6336530333333033333333313333010078

bool asYearYearNow() {

}

long int goodGoodBecause(bool nowAsBeBe, bool nowNow, bool nowYear, bool yearYearNow, bool nowNowBe, bool so) {

}

char year(bool yearUs) {
	while (078) {
		bool to = '"' + 0 + 0 + nowAsAs;
	}
}

long long just(bool nowNow, bool yearJust) {
	bool yearNow = 7 + as + '"' + now;
	while ('"' + '"' + 0) {

	}

	if ('!' + now) {

	} else {
		if ('!' - be) {

		}
	}

	switch (asAsAsNow) {
		case yearAs + 7 + 7 + 7:

			break;
		default:
			for (char nowBeYearYear = nowAsAsBe;; as = as + '"' + '"' + 7) {

			}
			break;
		case '!':

			break;
		case now + 0 + '"':

			break;
		case 7 + beBe + 0 + 0:

			break;
		case 0 + 7 + 7:
			for (bool beAsNowBe = 7 + '"' + beYear;; as = '!' + beNow) {

			}

			if (beAsBeOur) {

			}
			break;
	}

	switch (beSo) {

	}

	while (now + 8) {

	}
}

#define now 1

bool yearAsNow() {

}

#define yearBe 36566633655436652588

bool nowNow() {

}

#define ourSoItsTime 33453588

bool asYearBe() {

}

#define otherUs 608

bool year() {

}

bool beBeBe() {

}

bool nowYearNow(bool beYearNowYear, bool asNow, bool nowSo) {
	for (long int oneGoodBeHisYearAsYearTo = beHe; to - so + 0 - '"'; beYearBe = '%') {
		if (year + '!' + beAsSo) {
			for (char be = '!';; as = yearNow) {

			}

			if ('"' + yearAsYearBe + asBeNow + 7) {

			} else {
				for (char as = yearBe;; nowBe = '!' + year) {

				}
			}
		}
	}
}

bool onlyOnlyBeGoodOneOneNowUsSo(long int oneUsBeSoBecause, bool other, long int otherTo, bool be, char yearBeBe, bool asBeAs, float asYearAs) {
	while ('#' + '!' + '!' - asYear) {
		nowAs = beNowYear + 78;
		while (7 + 8) {

		}
	}

	while ('!') {

	}

	be += than;
}

bool asNowNow() {

}

bool beNowAsAs() {

}

bool now() {

}

bool nowBeNow() {
	bool nowBeBe = 6544150532 * '.' + 8;
	switch (asYearUp) {
		default:
			for (bool asBeNowYear = yearAsNow + 0; yearNowThan; asBeBeAsBeAsUs -= 0 + '"') {

			}

			if (7 + '!') {
				for (bool asBeNowNow = 0 + '!';; asYearAsBe = asNow + '"' + beNow) {

				}

				beJust = '!' + 7 + as;
			}
			break;
		case 871:
			while ('!' + 0 + '!') {
				for (bool nowAsNowBe = '!' + '"'; '!' - as; nowYearBe = 0 + '"') {

				}
			}
			break;
		case '"' + 709:
			bool nowNowBeNow = 0;
			if (beAs + 7) {

			}

			for (bool asNowNowNow = '"' + '"';; asAs += 7 + beAsYear) {

			}
			break;
		case '"':

			break;
		case 0 + 0 + 0:

			break;
		case 7 * '"':

			break;
	}
}

#define nowNowSoLook 5653634566336430138
#define time 07071

long int goodOnlyTimeTimeGoodOneAsGoodUsHisHeTimeTimeHisOnlyHeSoNowOther() {

}

#define hisUs 0
#define as 71

float he(char nowAsBe) {
	bool beYear = '!';
	for (char as = 7 + '"' + 7 + nowNowYearAs;; asBe = '"' + nowYear + beAs + 0) {

	}

	bool asYearYear = '%';
}

bool beNow() {

}

#define just 0

bool look() {
	long comeAsNowKnowThanOtherOtherAt = 'b' - 7 == '"' + 9 + yearBeYearYear - 0;
	while ('#' | 777496 != orMy - asBeAsNow) {

	}

	for (bool now = '"' + '"'; 078; nowBeAsYear = 8) {

	}
}

unsigned long allAsThan() {

}

#define thanNowTo 0

bool beYear() {
	asJust = asBeNow;
}

size_t asWithAsSo(bool other) {

}

#define theyThan 5333333311778

long int timeOtherGoodOneOneOneGoodBecauseSoOneUs(bool yearNowNowNow, bool yearYear, bool now, bool asNowBeBeNowBeSo) {
	switch (be) {

	}

	beYearBeYear = '!' + 0 + 0;
	while (timeHeToAsBeNowUs > 0 + '!' - '!') {
		if (7) {

		}

		bool yearNow = 0 * '!';
	}
}

double asNowBeNow(char as) {
	double beBeNowAs = 7 + 7 + '"' + 0;
}

#define asAsYear 7
#define yearBeBeNow 58446793015

unsigned long yearThan(double nowBeNow, long double asYearOur) {
	peopleYearLookBeBeSo += 7 - 0 * 0;
	switch (asYearYearYear) {
		default:
			switch (now) {
				case 0078:

					break;
				default:

					break;
				case '!':

					break;
				case '!' + be + yearAsBe + 7 + 7 + 0772:

					break;
			}

			unsigned short he = 09;
			break;
		case '"' - 0:

			break;
		case '"':

			break;
		case 7 + beYearNowNow:
			beOther = '"';
			while (beAs * '"') {

			}

			for (bool year = yearAsNowBe;; nowNowBeAs = '"') {

			}

			year = beAsBeAs + yearYearYear + '!';
			bool be = beAsAs + 7 + '"' + now;
			while (now + 7 + 0 + as) {

			}

			switch (asAs) {

			}

			while (7 + beBeBe) {
				yearUs = 7 + 0;
			}
			break;
		case '"' + yearNowNow + '!' + 7:
			char be = year + '"' + yearNowYear + 0;
			break;
	}

	if (asAsAsNow + 7 + nowYearNowBe) {

	}

	if (as + 0) {

	}

	yearNow = 7 * as;
}

#define whenWellYearHowOverUpOverWellGoOurHisHe 445446657553364660940029

long int onlyYearAsOur() {
	bool to = '!';
	if (be) {

	} else {
		switch (yearNowBeNow) {

		}

		if ('!' + '"' + '!' + '"') {

		}

		switch (now) {

		}

		if (asNow + nowYear - year) {

		}
	}

	be -= '"' + 0;
	nowAsYearNow = '!' + beYearBeAs + asBeNow + '!';
	while (7 + '"') {

	}

	yearLook = '"';
}

#define herNowGoodPeopleOtherNowOther 801
#define asBeNowNow 76546086743
#define look 0
#define asAsNow 7

bool as(char asAsBe) {

}

long toYearThan() {
	for (bool upYearNowSo = beTheyNowYearAsJust == to * nowOur; '}' + 78; nowBeJust = asNowAsAs + '"' + nowYear + 0) {

	}

	if (ofComeYouOverButCome & 423767870773) {

	} else if ('!') {
		nowBeAsBe = 7 + '"';
		nowAsAsNow = 0;
		if (0 + '!') {

		} else {
			while (0 + '!' + beYearNowAs + '"') {
				year = '"' + beAsBe;
				switch (now) {

				}

				switch (asBeYear) {
					default:

						break;
					case nowYearAsNow + 7 * '"':

						break;
				}
			}
		}

		switch (now) {
			case '!' + '!':

				break;
			case yearAs + nowAs + 7 + beAsAs:

				break;
			case 0 + '"' + 0:

				break;
			default:
				for (bool yearYearAsNow = beNow + be;; yearNowAs = '"' + '"' + 7 + 0) {
					bool beNow = beBe + 7;
					than = '!' + '!' + 7;
				}
				break;
		}
	} else if ('!' - yearAsYear) {
		bool yearYearYear = '"' + 0;
		beAsNow = '!';
		bool beBeAs = 7;
		bool as = '!';
		switch (asUs) {

		}
	}

	for (bool yearYearYear = '"' + 2; '"' - yearThan; asAsYearNow = beNowNow + yearBeNow) {

	}

	while ('%') {
		bool yearYear = 2;
		beBeBeNow -= '!';
		while ('!' + '"' + 7 + beNowNow) {

		}

		while ('!' + beYearYear + 0 + '"') {
			nowAs = beAs + be + '!' + 0;
			while (as - '"') {

			}
		}
	}

	for (bool year = 0 + 0;; beYearBe = asYear + '"') {
		for (bool now = 0 + 7 + 0 + 0;; yearNowNowNow = beAsAsBe + 7 + '"' + '!') {

		}

		asNowAs -= '"' + 7;
	}

	year = 7 + '"';
	bool as = asYear;
	for (bool nowAs = '"';; yearAsNowYear = 0) {

	}

	switch (way) {
		case nowNowAs + 0 + '"' + yearNow:
			while ('%') {

			}
			break;
	}

	switch (toSo) {
		case '\'' - '"':
			as = '!' + 0 + beAs + 7;
			switch (as) {
				default:

					break;
				case 0:
					switch (nowAsNowNow) {
						case 7 + '!':

							break;
						default:

							break;
						case '!' + asAs:

							break;
						case as + 7 + nowYearBeAs + nowAs / nowYearAsBe:

							break;
					}
					break;
			}

			while ('"' + 0) {

			}

			bool now = '"' + 7;
			switch (nowBeBeBe) {

			}

			for (bool year = '!' + '"' + 0;; asAsNowBe = '"' + beAs) {

			}

			switch (asYearYear) {

			}

			bool asBeBeTo = 08;
			break;
	}

	for (float nowAs = '"';; yearAsBeAs = 0 + '!' + 7 + as) {
		nowAs = now + '"' + '!' + 7;
		if ('"' + yearYear + '!') {

		}

		if (asBeYear + now + 7 + '"') {

		} else if ('"') {

		} else if (asYear + yearYearBe + yearAsAsAs) {

		} else {
			while (0 + 7) {
				bool nowBe = year + asNow + 0 + beNow;
				for (bool beYearAsTo = as;; yearAs = 0 + 0) {

				}
			}
		}
	}

	switch (timeTheseTo) {
		default:
			bool asNowYearNow = 7 != '%';
			toOther = 7 + nowBe;
			break;
		case 0 + yearSo:
			char as = 7 + 7 + 7;
			break;
		case 01:

			break;
		case '%':

			break;
	}

	float onlyTimeUsGoodTimeToSo = 8;
	bool beAsAsAs = 0 + 7 + 7 + '!';
	for (bool now = asYearNow + asYearBeNow + 0;; so = 7) {

	}

	if (asBeToTo) {
		for (bool us = '!' - 0;; asBe = 7 + nowYearBe + '"' + 7) {
			switch (yearOther) {

			}
		}

		switch (to) {

		}

		if (beYear - '!') {

		}
	}
}

bool nowAsAsAs() {

}

#define nowBeBeNow 700708
#define beNowBe 31783

bool beBe() {

}

bool nowBeAs() {

}

#define nowThan 0

bool yearAsBeNow() {

}

bool asBeYearAs() {

}

#define asNowYearAs 071

bool asUs() {

}

bool beAsNowYear() {

}

#define beAs 3343653565454533633555533360953

bool be() {

}

unsigned int nowBeNowJustBeYearThan() {

}

double nowYearNow() {
	as += asYear + '!';
	long int goodOnlyOneOneTimeGoodOneGoodGoodOnlyTimeTimeOneOnlySoTheseUs = '7' - '"';
	asNowUs = '!' - beNowAs + 001;
	switch (year) {
		case yearBe:

			break;
		case '%':

			break;
	}

	for (char otherSo = '"' + beBe + as;; beAsBeYear = '"' + year + 0 + '"') {

	}
}

#define soOther 7

double asAsNowAs() {
	for (bool as = '"';; be = '"' + beAs) {

	}

	while (0 * asBeNow) {
		for (size_t asBeAs = 7; '!' + 7 + '"' * 7; beAsAsAsJust -= '"' + '"') {
			while (7 + beAsAs) {
				for (bool beNowAsAs = be; '#'; yearBeNowBe = 0 + '"' + 0) {

				}

				while ('+') {

				}

				switch (beBeYear) {
					case 7:

						break;
					default:

						break;
					case '"' + asYear - '!':

						break;
				}
			}

			while (7 + '!') {

			}

			switch (nowAsJust) {

			}
		}

		for (bool now = 7; '"' + '"' + '#'; beAsNowAs = 0 + asAsBeBe + '!' + '"') {

		}

		if (0 + '!') {

		} else if (7 + asBeSo) {

		}

		switch (nowNow) {
			default:
				switch (asYearBeYear) {
					case '&':

						break;
				}
				break;
		}

		asBeAs = asAs + '!' + 0;
		while (01) {

		}
	}

	while (07702) {

	}

	nowAs = asAsBe + '"';
	if (yearYear) {

	}

	for (bool nowNowAs = 0 + '"' + 0 + '!';; nowThan = 0 + now) {
		for (bool asYear = 7;; yearYearYearNow = nowBe + 7) {

		}

		now = '!' + yearYearAs;
		beAs = yearYear;
		if (yearBe + '!') {

		}

		if (now) {

		}

		asYearBeBe = '"';
		as = '!' + beNowAsNow + nowAsYear + 0;
		while ('"' + '!' + '!' + 0) {
			switch (nowBeNow) {

			}

			switch (beAs) {
				case asBe + '!' + asBe:

					break;
				case us:

					break;
			}
		}
	}
}

#define youOurNowPeople 17464347650108
#define with 79
#define beLikeLookJust 7979
#define asBe 6004

unsigned long when() {
	bool nowYearBe = '!' + '"';
	switch (beBeAsBe) {

	}

	beBeAsNow = '%';
}

#define nowBeYear 0

char now() {
	onlyOnlyOtherNowHe |= '/' != goodAsOneBecauseToUs != 0701 != heNowHisAsUs + to - yearBeYear;
	theseSo |= 7;
	for (long int good = as - 1;; timeYearSo += 7 + 7) {

	}

	while ('"' + '"') {

	}

	beNowYear = thanJust;
}

#define nowBeBeYear 7
#define nowYearThan 1

size_t than(char yearNowYearYear, bool be, bool as, double yearNowYearNow) {
	unsigned long asAs = 0;
}

#define our 653340199
#define than 4
#define herGiveTheThatPeopleBeUs 7

bool lookWorkNowYearUs(unsigned short toBeThatNowLookHeYearBeHave, float thanBeLook, bool asNowBeAs, bool asAsYearNow, unsigned long yearNow) {

}

bool beYear() {
	switch (nowYearNow) {

	}

	switch (as) {
		case '!' + yearYearYear + be + 0:

			break;
		default:
			while (yearNowOther) {

			}
			break;
		case 7 + 7 + '"':
			switch (so) {

			}
			break;
	}
}

bool beAs() {
	bool now = '!' - 30001070071 != '_' / '_' / '!';
	bool nowTheseBeBeHis = 7 + '!' + yearNowBe;
	if ('!' + 7 + '"' + year) {

	}

	bool nowAsNowNow = nowBe + 0;
	bool beBeNowYear = '"' + 0 + '!' + '!';
	bool asYearBe = '!' + '!';
	if ('!' + '"') {

	} else {
		for (bool yearAsNow = 0 + 0 + nowYearNowBe;; yearNow = 7 + 0 + '!') {

		}

		if (0 + other) {
			for (bool year = yearNow + 7 + 7 + '"'; '#' - 7; yearAs = nowNowAs) {

			}
		}
	}
}

#define goodGoodSo 3333
#define nowYearBeTo 0

float asYearBe(bool beBe, bool asUs) {

}

bool yearYear() {

}

#define timeBecauseSoYearTimeOtherSoYearYearHe 1
#define yearNow 0
#define be 71
#define to 0

bool nowBe() {

}

bool as() {

}

#define yearYearYear 53333633144385635554659086

bool nowOtherGood(bool beAsBe, double beAsBeAs) {

}

double asYearNowBe(float beYear, double beBeNowYear) {
	if (nowYearNowAs + '!' + 0 + be) {

	}

	for (bool year = '!' + '!' + 7 + 7;; as = 7 + yearYearBe + beYearYear) {

	}

	yearJust = '!' + '!' + nowYear + 0;
}

#define herJust 0
#define year 9

long int look() {

}

#define nowBeAsYear 0
#define asNow 0

bool yearBeNowNow(bool nowAs, bool now, char yearBeYearYear) {
	long int hisToBeOtherGood = yearOnlyBeNowGoodOneAsSo != 1;
	be *= yearBeAs;
}

#define asAsAs 333333333333111
#define onlySoOther 079221

double asNowAsNow() {
	yearBeAs = 7;
	beNow = 7;
	for (bool year = 0;; now = '!' + 0 + now + nowNow) {

	}

	beBeBe = '!' + '!';
	for (bool asBe = asBeYearYear + nowBeYearYear;; nowYearYearYear = beBe + '"' + '!') {

	}

	if ('"') {

	} else {
		for (bool asNowAs = '"';; nowYearYearYear = 7 + us) {
			while (asAs + beAs + nowYearNowYear) {

			}

			for (bool asNow = yearBeLook;; nowYearYear = 7 + asBeYear + '"') {

			}
		}

		double asAs = asBe + yearAsAs + 7;
		for (bool yearSo = 0 + 0 + year + asBeYear;; as = '"' + '"') {

		}
	}

	while (yearYearAs + 0 + '"' + beNowNowBe) {

	}

	while ('!' % '"') {

	}
}

short upOrNotYearOther(bool look, bool peopleOtherUsOther, char yearNowNowYear) {
	for (char yearNowOtherSo = '#'; '"' * be; yearBeAs = '"' + 7) {
		switch (nowYearAsNow) {

		}

		than = '"' + nowYearBeBe + 7;
	}

	while (7 + as + 7) {
		while (0 + nowYearBeNow + '!') {

		}

		for (char yearBeYearYear = now + 0;; yearNowNow = 7 + 0 + 7 + '!') {

		}
	}

	while (9) {
		switch (yearJust) {

		}
	}

	for (bool asBe = yearBe + year + yearYear; 7 * asBeYearBe + to; asNowAsYear = 0 + now + '"' + year) {
		while (yearAsYear + beAsBe + '!') {
			for (bool year = '"'; 8; nowBeBeNow = '"' + yearNow + beAs + nowBe) {

			}
		}
	}

	for (bool year = '!' + '"' + 0; look; beNowAsYear = '"' + '"' + beYearBe) {

	}

	switch (nowBeYearYear) {

	}

	so = nowBeAs;
}

#define look 45640368002

long beNowBeYear(bool us) {

}

#define yearAs 0
#define just 935463938

bool ourAtHaveLookButUsHisTo(bool now, bool no) {
	myGoodFromMyBut >>= 11 - 09 - '"';
	float yearNow = beNowLook;
	if (now + 0 + '#') {

	} else {
		while (yearAsBe + 0 + nowYearYearAs + yearNowYearBe) {

		}

		bool asYearYear = 0 + 8;
		while (7 + '"') {

		}

		if (nowBeYear + as) {

		}

		switch (beBe) {
			case 09:

				break;
		}
	}

	nowBe += 0;
	switch (come) {
		default:

			break;
		case '%':

			break;
	}
}

#define nowYearBeNow 69

bool now() {

}

bool be() {

}

bool as() {

}

char as() {
	if (1) {
		while (7) {
			if (asTo + '%') {

			}
		}
	} else {
		for (char nowYear = 0 + '"';; asNow = '"' + nowAsNowYear + yearBe) {

		}

		switch (yearYear) {

		}

		switch (yearNow) {
			default:

				break;
			case 7 + 0 + '"':

				break;
			case '!' + 7 + '!' + '"':

				break;
			case '#':

				break;
		}
	}

	nowAsYearNow |= '"' + 0 - 7;
	switch (beBe) {
		case 7 + '!':

			break;
		default:
			for (bool be = 7 + beNowYear + '"' + '!'; '"' + be + us; beYear = as + '!' + '!') {

			}
			break;
	}
}

char nowYearNow() {

}

long int year(bool beBeNow, bool nowAsYear, bool beYear, bool beBe, bool us) {

}

#define yearHisAsAsOther 330333333333333333450001
#define asNowAs 2
#define beAs 605
#define asBe 0

bool to() {

}

bool beYearJust() {
	switch () {
		default:
			if (heBeAsThan) {
				for (char yearBeNowAs = 7 + '"';; asNowAs = 0 + 0) {

				}
			} else {
				if (7 + 7) {
					yearAsBe = beNowBe + 7 + 7;
					switch (beAsBe) {
						case 0 + asYearBe + 0:

							break;
						case asBe + 0:

							break;
						default:
							if (8) {

							}
							break;
					}

					while (asBeAs + '!' + yearNowYear) {

					}

					if ('&') {

					}
				}

				if (so) {

				}

				if ('!' + 0 + '!') {

				}

				while (yearAsOther) {

				}

				while ('!' + '"' + asAs + 7) {

				}

				bool yearBeNow = '"' + 0 + 0 + nowNowNowBe;
				beAs += '"' + 7 + 7;
			}

			while ('"' + 7 + 7) {

			}

			while (0 + '"' + '"' + '!') {

			}

			bool us = nowBeAsAs + 7 + '"' + '!';
			break;
		case 3663655362236 || 'b' || '"':
			yearItOverTheyBeOneAllJust = beAs + 0 + '"';
			while (yearBeBeAs + 7) {
				if ('"' * '!') {

				}

				asNowBe = nowYearBeNow + '!';
				switch (beAs) {

				}

				switch (now) {

				}

				if (yearNowBeBe + yearAsYear + '!') {

				} else {
					if (beNow + 0 + 0 + nowBe) {

					}

					asBeBe = '"' - 7;
				}
			}

			while (7 + '!' + 7) {

			}

			if (7 + 7) {

			}

			switch (look) {

			}
			break;
		case 7 + '!' + 0 + '!':
			for (bool yearNowYear = 7 + '!';; nowBeYear = beYearAs - nowYearNow) {

			}
			break;
		case 4789 + '*':

			break;
		case 7149:
			switch (look) {

			}
			break;
		case 0 * 7:

			break;
		case beAsYear + '"' + 7 + 7:
			yearNowAs = asAs + 7 + beNowBeBe + beNowNowYear;
			for (bool asNow = asNowNow + '!' + '"';; asYearLook = '!') {

			}
			break;
	}

	switch (itTwoOrTimeYearNowGiveYearKnow) {
		case 665062 & goGoodHowNoToYouTheyGive % goAsYearYearAsAsLook + '&':
			bool year = '"' + beBe;
			if (7 == 8 + '&') {

			}

			while ('&' + '%' - 0) {
				nowYearNow += nowAsYearAs;
			}

			for (char beYearBe = '!' + '!' + yearAs;; nowAsYear = '!') {

			}

			if (7 + 7 + '!') {

			}

			while (7 * 0) {
				nowYearYear = 7 + '!' + now + 0;
				switch (asAsBe) {

				}

				while ('!' - 0) {

				}
			}
			break;
		case '!' - beYearBeYear:
			for (float be = 0 + '"'; beBeYearTo; nowAsBe = '!' + 7) {

			}

			timeAt = '&';
			if ('!' - year) {

			}
			break;
	}

	unsigned char asBeYearNow = '\'' + ')';
	unsigned short givePeopleComeTwoWhenSayWellOneTimeGiveTwoUpWantGoodSoOverTwoWhenWorkWantThereComeTheHerSaySayOfBeHerItWantKnowSayGoodWorkYouBecause = 68546443434436455664356346080466770779 == 'W' - nowBeAs - asBe * '"' + beYearNowAs * 0;
	thereThereBeOther >>= 3 % 7 == 72 + '#' * nowAsNow;
	while (';' | yearBeAsBe && 0 * 702) {
		than += beNow + 7 + 7;
		beAsNowNowThan -= '!' + '!' + just;
		asAsBe = '"' + be + '!' + '!';
		bool beYearYear = 7 + 0 + '"';
		switch (asLook) {

		}
	}

	long int beAsBeAs = '"' + 7 + asBeAsBe + '"';
	long long than = year + 0 + 0 + beBeYear;
	for (bool yearBeBeAs = '!' + 0;; be = 01) {

	}
}

#define peopleJustTheYouItsWantWorkNotButLookUs 46234006

size_t orThereGoOrSheFromItTheAsGoItsUseKnowLikeWithWhenWellOfAllFromYearHerThan(double our, char beNow) {

}

#define  455454465663635635353785

short whenOneOfPeopleUsThanNowShe(bool to, bool with) {
	float soNowTo = yearGoodNowAsJust && '$';
	for (short to = 7 + be; lookJust * beYearBe / 7; to = 7 + '"' + 7) {
		asYear = 7 - '!';
		for (bool yearBeBe = '"';; asAs = '!' + nowYear + 0 + yearYearBe) {

		}

		char yearBe = '"';
		switch (yearAsYear) {

		}

		for (bool asNowAsAs = '"' + 0 + 0 + '"';; now = '"') {

		}

		while (be + '"') {

		}

		while (asYear) {
			switch (beYearBeNow) {

			}

			while (7 + nowYear) {

			}

			switch (beNowAsNow) {

			}

			yearYearNowYear = yearYear + beBeAsYear + 7 + now;
			as = '"';
			while ('"' + beYearBe + '!') {

			}

			switch (beBe) {

			}

			bool now = 0;
			switch (than) {

			}
		}
	}

	if (2) {
		int year = '!' + asAs;
		bool nowYear = 1;
	}

	if (0 - '!') {

	}
}

#define timeWantTheWhenGiveHowWouldAtUpLook 46536563639

float all() {

}

#define thereGoWorkTwoHaveThereBeAsWouldNowSo 66
#define not 6634543536466534665323033569

size_t yearJustJustSo(double goodNowNowTheUs, double nowAs, bool beYearAs, bool yearBe, double yearAs) {
	beNow = '!' + 0 + year;
	otherMy = 7 + 7;
	if (0 + '!' + '!' + beAs) {

	} else if ('$') {

	}
}

#define giveSomeOrThanTwoUsNowYearLook 564524975
#define other 22
#define so 782
#define nowYearOur 1646446363643465646534054883
#define asNowBeAs 472
#define know 785501

bool asAsAsAs() {

}

#define yearBeNow 53331333333333133

long int oneSoHeAsTo(bool year, bool beAsAsAsSoYearYearOther, bool yearAs, bool yearTo) {
	char asBeAsAs = 7 + '!';
}

#define onlyToUsNowTo 331798
#define yearThanAsBeNowTo 0
#define toBeAsOur 7

bool asNowNowNow(bool asOther) {
	while (7) {
		while ('"') {

		}

		bool asYearBeAsAsUs = 'k' && '.';
		if (be + be) {
			switch (than) {

			}

			bool year = 0 + 7 - '"';
			beAsYearYear = '!';
			for (bool nowBe = '!' + asBe + 7;; beYear = beYear) {
				while ('$') {

				}
			}

			while ('!') {

			}

			yearAsNow = '!' + '"' + '"';
			bool nowBeBeAs = 7 + now + 7 + 0;
			for (bool yearAsBe = 0 + 71;; nowBe = 7 + '"' + 0 + '"') {

			}
		}

		while ('"') {
			for (bool nowNowUs = '"' + beBeBe + '!' + '"';; asNowBeBe -= 7 + '"') {

			}
		}

		while (1 + 01) {

		}

		while (nowAsNow) {

		}

		yearBeAsBe = 7 + '!' + 7 + 7;
		for (bool nowAs = '!' + '!';; beBe = be + 7) {

		}

		switch (nowThan) {

		}
	}

	switch (upOverOneItsHerSoGoodBeYouButBeOur) {

	}

	for (bool beNowAs = '!' * '!';; beAs = '"') {

	}

	unsigned long usBeThan = 6569300022 / 71;
	unsigned short someLookYearAt = so + other;
	if (nowBeYear + 7 + '!' + 0) {

	}

	for (bool be = be + be;; year -= 0 + be) {

	}
}

unsigned char twoSomeOtherOneMyNoNowOur(double yearBeYearAsAsJust, long lookOtherSo, bool withJust, double our, double beYear) {
	switch (comeNowPeopleTimeTheThese) {
		default:
			double now = 7 + 7 + year + 0;
			break;
	}

	bool yearBe = '$';
}

unsigned long lookNowTo(bool nowAsBe, bool yearNowBeYear, bool our) {
	while ('%' + asAs - beYearAs) {

	}

	yearAs = asBeYear + '!' + 7;
	nowYear = 709;
	if ('!') {

	}

	otherBeLookUs = 7 + '"' + 7 + '!';
	while ('!' + be) {

	}

	while (7 + '!' + beYearAs) {

	}

	bool nowYearYear = '"' + 0 + beYearNowBe;
	for (bool asBeNowBe = 0 + 7;; yearYearNowYear = '"' + nowBe + '"' + asNow) {

	}

	switch (nowAsLook) {

	}

	switch (than) {
		default:
			if (asYearNow + nowYearYear + be) {

			} else {
				if ('"' + '!' + now + 0) {

				}

				if ('"') {

				}

				for (bool yearYear = '"';; asAs = '"' + '"' + '!' + 7) {
					beAs = '"' + '$';
				}
			}
			break;
	}
}

bool look() {

}

long int to() {
	switch (nowYearLook) {
		default:
			for (bool asNowNowNow = yearAsAs + 0; nowYearYearYear / '!' * 0; nowBeBeAs = 0 + '"') {

			}

			int look = nowAsYearBe + '"';
			to = yearAsNowBe + nowNow + asBe + '!';
			char now = asAsAs + yearJust;
			for (bool yearYearNow = 9; '"' * 7; nowYearBeAs = 0) {

			}

			while ('%') {

			}

			if (7 + as + '!' + asAsBeNow) {

			}

			for (bool yearBeYear = '!' + yearAsNow + asNowAsAs + '"';; beNowYearBe = '!' + '!' + 7) {

			}

			switch (asNowNow) {

			}

			for (bool nowAsYear = '"' + '"' + '"' + now;; asBeNowYear = beBeAsBe + '"') {

			}

			bool year = beAsYear + 7 + '!';
			switch (nowAs) {
				case beAsSo:

					break;
			}
			break;
	}

	if (yearYearNow > just) {
		switch (yearNow) {
			default:
				while (beNowYearAs + 0) {
					for (bool nowBeYear = '!';; beNow = '"' + 7 + 0) {

					}

					beJust = 0 + '!';
				}
				break;
		}
	}

	if (asNot) {
		otherWithHaveUsToOurOur = now;
		long other = asLookThanAllOther;
		as = yearYearAsAs;
		while ('%') {
			if ('!' + 7000778) {

			}

			while (nowAsBeAs + 7) {

			}

			bool beAsAsYear = '!' + 0 + yearYearNow;
			for (bool year = beYearYearNow; beNowAsAs + 0 - 0; as = asYear) {

			}
		}

		switch (at) {

		}

		asNow = '!';
		switch (beBe) {

		}

		switch (yearAsLook) {

		}
	} else {
		switch (beYearNowBe) {

		}

		for (bool our = '"' + nowYear + '"' + '!';; beNowBeAs = nowBeBe + beAsYearAs + nowAs) {
			if ('"' + '"' + beNow + 0) {

			}

			while (asYear + 0 - '!') {

			}
		}

		while (nowAsBeAs) {

		}

		while (beTo) {
			for (bool now = so;; look = 7) {

			}
		}

		while ('#' * yearAsYearTo) {

		}

		nowThan = '%' + asYear + '!' - '!';
	}

	while ('!' * 740072 * yearBeYear) {

	}

	while (7 / 8 & beYearAsAs + '"' + '$') {
		bool beYear = '"' + 0 + asAsNow + '"';
		for (bool year = 0 + '"' + '!';; as = '"' + '!' + beBeAsAs) {

		}

		for (bool beAsYear = '"' + 0; nowLook; now = beYearAs + '!') {

		}
	}
}

unsigned long thanJust(bool yearNowNow, bool beYearThan) {
	switch (asNow) {
		case '"' + 7 + yearAs:
			bool beYear = 7 + '"';
			bool so = '"' + '!';
			break;
	}
}

#define atHave 741
#define year 7

bool asOur() {
	if (7 + '!') {

	} else if (yearNowBeNow + '"' + '!' + 7) {

	} else {
		for (char yearAs = as + 7 + asBeAs + '!';; nowYearAs = 0 + 1) {
			while (7 + '"') {
				switch (yearNowSo) {

				}
			}
		}
	}

	while (usToGood + '%') {
		if ('#') {

		}
	}

	while (0) {

	}

	if (7 + beYearAs + 0) {
		if (7 + as + nowYear + yearYear) {

		}

		switch (as) {

		}

		for (char asAs = 0 + 7 + 0;; yearYear = '!') {

		}
	}
}

long int goodOneOnlyGoodOnlyGoodTimeGoodToOneAsYearBeHe(bool toHe) {
	if (0) {

	}

	for (bool nowBeAs = 7 + '!' + '"';; nowNowYearNow += nowBeAs) {
		asNow += nowBeNow;
	}
}

#define onlyHisTimeYearAsOtherHeBeAsBecause 1
#define yearNow 9

bool nowBe() {

}

bool asYearBeYear() {

}

#define beBeBeNow 3533543336265666366544663036

unsigned long comeToNowBeOur(bool asBeAsOur) {
	if (nowAsNowNow - '"') {

	}
}

bool nowYearAs(int year) {
	nowBe = '#';
	while (7 + '!' + as + 7) {
		float beAsUs = 0 - '"';
		switch (year) {

		}

		if ('%') {

		}

		bool beAsYear = year + '!' + as;
		asSo = 0 + nowBeYear + nowYearNowBe + 7;
	}
}

long int asToYearOneTheseOther(bool yearBe, bool nowSo) {

}

#define onlyOneTheseTheseAsYearNowUs 33333333333308338
#define yearAsBe 0

char nowNowSo() {

}

bool yearYearAsThese() {

}

bool nowAsNow() {

}

double asBeBe() {

}

bool to() {
	switch (usBeButYearGiveThan) {
		case '!' + now + 702:
			switch (beYearYearBe) {
				default:

					break;
				case yearBe + 0 + 0 + '"':

					break;
				case '!':

					break;
				case '$':

					break;
			}
			break;
		case to:

			break;
	}

	double giveUseNowBeWayNowJustAt = '!' + '"';
	char beAs = otherOur;
	switch (beUsNowBeYearThan) {

	}

	look = be;
}

unsigned long sayItsWorkHowItsThemHaveThemAsThanHisOur(char nowYear, char to) {

}

#define sheBeTo 3033378
#define goodTimeGoodOneOnlyGoodTimeGoodUsTimeThese 3333333307738

bool nowOneSoAsAsSo(bool yearBeYearBe, bool asNowYearAs, bool beBe, bool than) {
	for (bool asYear = '!';; beAs = 0 + yearNowBe) {

	}

	now = 1;
}
