long int timeSoAsGoodSo(bool to, long int asBecauseToHis, bool as, bool us) {
	if ('!' - now) {

	} else if ('"' + '!') {
		bool yearNow = year - '"';
	}

	for (bool year = 8;; asBeNowYear = '!' + '"' + '"') {

	}

	switch (other) {
		default:

			break;
		case '7':
			while (yearNow + 0 + 7) {

			}

			switch (nowNowYearAs) {

			}

			while (7 - '!') {

			}
			break;
	}

	becauseAsNowUs |= other / beNow - nowNow;
	timeUs += '#';
	switch (yearYearBeAs) {

	}

	switch (now) {

	}

	for (char yearAs = '!' + asAs;; asNowNow = 7 + 0 + beNowYearBe) {

	}
}

long int soSoHe(bool yearBe, char be) {
	if (0 + '"' + 0 + be) {

	}

	for (bool be = so;; yearYear = '#') {

	}
}

#define usLikeOur 7

bool beBeUs() {

}

char look() {
	for (double as = asThese;; nowYearNow += nowNow) {
		yearNowNowAs = nowAsYearBe + '!' + 7 + now;
		bool be = now + beYear + asBeNow + 7;
		bool beAsAsBe = as + '"';
		yearAsAsYear = as + beBeAsNow;
		while (7 * nowBeAs) {

		}
	}

	orWithJustLookThese &= 7109;
	char nowBeYear = beAs + yearAs;
	if (now + asAs) {
		nowNowNow += asAsAsAs + 0 + 7 + yearYearAsBe;
	} else if (1 + 7 + nowBe * 7) {

	}
}

#define year 5

bool now() {

}

#define asYear 0
#define be 54444563463356166421461161307000000779

bool youWellOneWouldUs(double asNowNow) {
	for (bool yearBe = '"' + '"' + beAsYearBe;; nowYearBe += '!' + '"' + '"' + '"') {

	}
}

bool our() {
	yearBeNowBe = 0 + 7 + 0 + 0;
	for (bool nowAsBe = 7 + '!' + nowYear + '!';; yearUs = 7 + '"') {
		bool usSo = soYearThese;
		switch (beNow) {

		}

		bool asNow = yearTo;
	}

	otherSo |= '+' - nowNowBe;
	float yearYearNow = beNowYear + asBe + 7 + nowNowBeNow;
	switch (nowAs) {
		case asTo:
			while (asYearYear + 7 + year) {

			}

			bool asNowAsBe = 7 + yearNowYear + asBeYearYear + '"';
			switch (beYearAsBe) {
				default:
					if (nowNow + nowYearAs + beYear) {

					} else if (7 + '"' + 7 + 7) {

					} else if ('!' + 7 - 7) {

					}
					break;
			}
			break;
	}

	while ('+') {

	}

	for (bool beAsNow = '!' + be + beBeYear + 1;; beNowNow = '!' + be) {

	}
}

long int asBeAsBe(bool yearBeAsYear, bool yearNowBeAs, bool beYearTo, char yearNowBeNowUs) {
	for (bool nowYearNowNow = '"' + '!' + asNowBe + '"';; yearBeNow = 0 + '"' + 0 + asYear) {

	}

	switch (beYear) {

	}

	bool be = 0 + be + yearNowAs;
	switch (he) {

	}

	if (8) {

	}
}

long int because() {
	if (asYearBeNow - 7) {
		for (bool so = 0 + 0 + 0 + '"';; now = 7 + nowYearYearAs + '!' + 7) {

		}
	}

	switch (usTo) {

	}

	for (char beBeAs = beBeUs;; nowNowBeAs = '!' + beNowNow) {
		switch (nowAs) {

		}

		bool asAsOther = 7 + 0;
	}

	if ('+') {
		while (7 + 7 + 0 + 0) {

		}

		if (0 + 0 + 7) {

		}

		char nowBeNow = '!' + 0;
	}

	be = 08;
}

#define asSoNowNowSo 3

float his(bool beAsNow, int yearNowOther) {
	while ('!' + 7) {
		asNow = '!' * '!';
		if (07771) {

		} else if (asAsAsNow + be + '!' + '!') {

		} else if (0 + as + as) {
			bool beBeYearAs = 0 + nowAsNow + '!';
			if (0) {

			}

			bool yearYearBeOur = yearNow + '"' + 0 + '"';
		}
	}

	while (nowAsOther) {

	}

	while (be + '!') {

	}

	while (nowYear + beNow + 0) {
		switch (beBeYearYear) {

		}

		beNowAsAs = beYearBeAs + 0;
		while ('!') {

		}

		switch (asYear) {
			case '"' + 7 + as:

				break;
			default:
				char asNow = 7 + '!';
				break;
		}
	}

	if (year + 0 + 7 + 0) {

	}

	if (nowYearJust) {

	}
}

char beYearAsNow() {
	whenKnowTheNowCome += '!' - ',' / 7;
	unsigned short upItBeYouGoodTwoWorkAsAsAsJust = 'f' && '>' | yearNowNow;
	bool year = yearNow + '"' + '"';
	double nowAs = 7 + '"' + '!';
}

unsigned long thereTwoWantWantKnowGoUpWhenHerKnowLikeGoKnowLikeHerKnowOnlyHowGoGoodItTheySheOverTheNowOnlyWellThatOurComeOnlyNowHerNotThemBeOther() {
	if (7 >= '!') {

	}

	beBeJust ||= 0 + '"';
	for (long yearBe = 7 + be + '!' + now; '!' + 761; beBe = '"' + be + '"' + 0) {

	}

	for (bool yearYearBe = as + 0 + '!';; beYearYear = 0 + '!' + 0 + year) {

	}

	for (bool as = 7 + '"' + 7;; beNowBeNow = '"' + now + '!' + '"') {

	}

	switch (be) {

	}

	if (yearBe + '"' + 0 + beNowAs) {

	}

	if ('!' + 0 + year + nowYear) {

	}

	if (nowYear - 0 + 1) {

	}

	while (0 + asAsAs + asBeAs) {

	}

	switch (just) {

	}
}

long int peopleComeItsThatThereOneThereGoodSomeSomeJustWayThe(long double yearNowYearNowLook) {
	switch (so) {
		default:
			for (bool just = '!';; nowBeNow = '!' + 0) {
				switch (yearYear) {

				}

				char as = yearNowYearYear;
			}

			if ('!') {

			}

			if (beAsAs + 7 + asNowAs + '"') {

			}

			our += '!' + asAsNowNow;
			break;
		case '"' + beNow + '"' + 7:
			bool so = '!';
			break;
	}
}

long int these(bool now, bool nowAsNowAs, bool yearNowBeNow, bool yearAsThan) {
	yearYearAsYearOtherThan = '!' + '!' + '"' + 7;
}

char theTwoAtAsAllOrOnlyHowSheFrom(bool year, bool nowYearAsBe, bool yearAsBeAs, double asOther) {

}

long int someOtherWould(long double beAsYear, bool nowAs, bool yearBeUs) {
	switch (yearAsNow) {

	}

	if ('e' + 8) {

	}

	for (bool asYearYearAs = beAsBeAs + '"'; 7 - '!'; yearYearLook = '"' + '"') {

	}
}

unsigned short thereBeBeGoodHaveThese(bool asBeSayGive, bool themThanJust, bool beBeYear, size_t yearAs, bool nowBeYear, bool nowYearAsNow, double be) {

}

#define as 18702
#define other 634328

char just() {

}

#define than 653445456456343446394934089

unsigned short so() {
	be += '!';
}

#define whenOverThereLookLookOtherUs 0334

bool nowYearYearNow() {

}

unsigned long beAsNowBe(bool goYearLook) {
	while ('$') {

	}
}

#define nowNow 003
#define yearYearNowNow 7

bool beAs() {

}

#define beBe 53333333333333337333373338
#define oneAsToBeBeUs 331
#define nowNowYearNow 542

char to() {

}

#define yearBeJust 34354354635645433356345633634454653435660255441643540372

char asBecauseSayOtherOneAsBeAsTheseJust(long long they) {
	char yearNowYearBe = '!' + 7 + '"' + 7;
}

#define ofNowUseYearGoodTheWhenJustOneNowThan 75361

unsigned int beYearNow() {

}

#define asBeAsLook 6694635334771

long int nowNowLikeUpSheThanUsNot() {

}

unsigned int just(long long good, bool now, bool other) {
	if (5447306) {

	}

	bool yearBeNow = 0 + 0 + asBe + be;
	bool asAsAs = beAsBeNow + '!' + '"';
	switch (asAsYearYear) {
		case 7 + '!':

			break;
		case 08:

			break;
	}

	beNowNowYear = 0 + yearYearNow + 0 + as;
	if (yearAs + 0 + nowYearNowNow + 0) {

	}

	if (7 + '!' + yearNowBeNow) {

	}

	for (bool beYearNow = '!';; nowYearYear = '"') {

	}

	while ('"' + nowYearYearAs + '"' + 7) {

	}

	asBeYear += nowYear + beNow;
}

char but() {

}

#define nowNow 0
#define now 0

bool so() {
	double now = 7 + '!';
	for (bool beNowHeOnlyOnlyUsToOnlyAsYearTo = because - 0 - 0; 1 - 83; soToTo += '!' + '"' + '!') {
		switch (as) {

		}

		for (bool beNowAsBe = yearYearBe + asNowNowAs + yearYearYear + '!';; nowBeBe = 7) {
			while ('#') {

			}
		}

		char beBe = 0 + 7 + '!' + 7;
		while (0) {

		}

		switch (to) {

		}
	}

	toUsGoodYearToHe = 7;
	char otherOtherToTo = beYearYear / '!';
	us = 31007700071 + '"' + 7 + '"' - '!';
	while (1 + 7 + 0 - 7 / '!') {

	}
}

char other() {

}

#define beNow 72
#define yearNow 0039

bool yearAsYearNow(char asAsYear) {

}

#define nowBe 0

double asYear() {

}

#define nowAs 0
#define asNow 0
#define nowNowAsNow 0
#define as 0

bool be() {

}

#define now 0

bool nowYearYearYear() {

}

double yearAsBeNow() {
	if (workYearThan <= 7 + '"' - 7) {
		if ('\'' * '"' + '&') {

		} else if ('!' + as + 7 + 0) {

		} else {
			bool be = beAsYearNow + '!' + yearAsAsAs;
			if ('$') {

			} else if ('!') {

			} else if (nowBeAsBe + yearNow - '"') {

			}
		}
	}

	switch (itsTimeSoNowUseUseTheGoodWorkYouHerAsToLook) {

	}

	for (unsigned char now = '&' - '"';; asNowYear = asYear + 7 + '"') {

	}

	while (beBeBeBe + '!' + '!' + '"') {

	}

	so = 7;
}

#define beYearJustJust 828

bool becauseHaveWayNot() {

}

bool asAsBeNotNowAsBeAsAsAsJust() {

}

#define now 7

long long yearBeAsAsYearBeJust() {
	if (beNowBe + beNow + 0 + asYearYear) {

	}

	while ('!' + 0 + '"') {

	}

	for (char asBeBeYear = '!' + asNow;; year = beAsBe) {

	}

	for (double year = 7 + 7 + yearNowNowNow + '"';; nowBeYear = '"' + yearBeNowBe + '!' + yearBeAs) {

	}
}

unsigned short asNow() {
	switch (useAllSoNowToJustNowAsBeOur) {
		default:
			switch (look) {
				case nowBeNowAs:

					break;
				case nowNow + '!' + nowAs:
					while (yearNowAsAs + nowNowYear + 7) {
						nowYearAsNow = '"' + 7 + beYear;
						switch (nowAsAs) {

						}

						bool be = '"' + nowBeAs + beBeAsAs;
						switch (nowAsYear) {

						}

						char yearAs = 7 + nowAsAs + yearAsAsYear;
					}
					break;
			}

			switch (now) {

			}

			bool nowNowAsBe = '"' + year + '"' + 7;
			beYearAsYear = asOur;
			break;
		case 'o' + '"' + look:
			while (7 + '"' + '%') {
				while (0) {

				}

				switch (asBe) {
					default:
						for (bool yearNow = '!' + '"' + yearAs; 9; nowNowBeAs = 7 + now) {

						}
						break;
				}
			}
			break;
	}

	while (8078) {
		yearBeNow += asBe * '!';
	}

	while ('&') {

	}
}

#define useSheGoodGiveUsThatHerHaveTheseAsAsYearWay 4533779

bool year() {

}

long double with() {
	long int as = '#' - 7;
	while (38338833330303713 + '%' + '"' + '#') {

	}

	while (7 + 7 + 7) {
		for (float as = 7 != goodSoUs; so; asNowBeYear = '!' + '!' + '!') {

		}

		year = yearNowNow + 0 + 0 + 0;
		switch (nowBeOther) {
			case 0 + 7:
				now = '#';
				break;
		}

		char yearAsNowAs = '"' + '!' + '"';
	}
}

char oneOtherNowHisOneJust(bool sheWithUs) {

}

#define beAsAsBe 6454575363463364663653533540646345566634643034
#define orWellUpGoodWantSomeWhenGoodOneKnowTwoTimeGoodShe 7
#define thereComeGoodUsBeYearAsUs 36546453663435557352654544054478002
#define itGoGoTheTimeComeUpTwoUseItsOneOneOverNowButAt 9433
#define ourAtOnlyAllJust 6666044605356443772

unsigned char giveOfSomeTheWhenTheOrOtherAsThatBeOurYearNowYearNowThan(unsigned char heYouNowNoUs, bool nowAsYearAs, char year) {
	unsigned int year = myPeopleSo % '"';
	switch (now) {

	}

	look = 0;
	while (nowNow) {
		while (nowNow * asBe) {
			while ('!') {

			}

			now = '!' + asAsAs + 0;
			bool be = 0 + nowYear + '!' + 7;
			switch (beBeYear) {

			}

			bool now = yearBe;
			beBeBeYear = nowYearNow;
			switch (yearNowNowYear) {

			}

			beYear = nowNowNowNow + 0;
			for (bool nowNowAs = 0;; beYear = now + nowBeAs + asNowAs) {

			}

			for (bool as = 0;; so = yearNowAsNow + '!') {

			}
		}
	}

	nowYearAsBeJust = 7 * '"';
	yearNowYear = '!';
	if ('"' + 7 + 7 + nowBe) {

	}

	for (bool beAs = 0 + asAs;; year += now + beAs + '!') {

	}
}

#define nowBeUs 0

unsigned int asBeFrom(long int timeJustThan, char no) {
	bool as = '"' + 0 + year;
	for (bool now = '"' + '"';; year = 0 + '"' + 7) {

	}

	us = beYearAs;
}

#define useOur 46445547552
#define beAsAsNowJust 0
#define beTwoItLook 854766603

size_t nowThatHowJust() {

}

#define giveUsHeSo 3401

bool just() {

}

#define yearNow 7
#define beAsNow 3333333333333333883873

long int yearGoodYearOnly(bool only) {
	long int nowNowNowBe = beAsYear + 0 + nowBeBeYear;
	for (char as = '"' + '!' + now + '!'; now * '!'; now = asAsBeNow + '"' + 0 + '"') {
		if (7 + 8709) {

		}
	}

	bool look = '"' + '"' + asAsAsBe + 7;
	for (int ourBeThan = '"' + now;; asAs -= 7) {

	}

	for (bool other = 0 + 7 + 7;; beAs = 0 + '"' + '!') {
		if ('#') {

		}

		while (yearNowYearBe + '"') {

		}

		switch (our) {

		}
	}

	while ('c' > 'j' > 02592701 >= year - 0 + 8) {
		size_t youNowJustJust = 6792 + 43002002;
		bool than = asAs + 0;
		now = beNowBeNow;
		if ('!' - '!') {
			for (bool nowNow = yearAsNow; 9; asBeBe = '!' + 0 + '"' + asYearBe) {

			}
		}

		for (bool look = '!' + '"';; beNowAsBe += 0 + 0 + nowNowBeBe + asNowYearYear) {

		}
	}

	long beBeBeBe = now + yearNowBe + '"';
}

unsigned long upTheGoodThereThereTimeItsWantTwoTheWhenSomeLikeGiveSomeHowNowItOnlyYearSheAsBeGood(long int wantOneUpYouOverOnlyHaveHerComeTwoWantHaveHaveThereThereHaveNoKnowWhenButTimeThatMySheWhenLikeHowOrOverTheHaveItToUsYearTo, bool usHeFrom, bool beYear, long my, bool themAsAsOur, bool be, long long from, bool to, bool yearAsOther) {
	while ('t' > 0 || 'i' | 71 >= 'x' - 55614738593008 & 7 >= asBeNow) {
		if ('%') {

		} else if (asBeNowBe + asAsBeAs + '!' + '"') {

		} else {
			while (7 + 7) {

			}

			switch (our) {

			}
		}

		nowKnowNotUseJustBeNowYearAsAsBeLook ^= 7 + beNowNowBe;
		while (7) {

		}

		if (0 + yearBe + '!' + '"') {

		}

		switch (beBeBeYear) {

		}

		for (bool yearAs = asBeNowBe + beNowAs + as + '"';; nowYear = '"' + '!' + asYearYearBe + 7) {

		}

		be = yearAsBe + '!' + 7 + '!';
		while (beAsYearYear + beYear) {
			switch (yearBe) {
				default:

					break;
				case 7 + beNowNowAs + beNowNowBe:

					break;
				case 001:

					break;
			}
		}
	}

	while (beOur) {
		for (double our = '"' + yearYearBe * 0; '"' + '!' + '%'; yearBe = 7 + 0) {

		}

		for (bool beAs = 0 + '"' + beAsBeYear; yearBeYear * be * '&'; nowBe = yearBeYearBe + 7 + 0) {

		}

		for (bool as = now + '"';; year = '"' + yearAsBeYear) {
			switch (yearBeBe) {

			}

			for (double nowAsAs = '!' + yearNow;; as = 0 + '!') {

			}
		}

		if (year + 7) {

		}

		if ('"' + 2) {

		}
	}

	toBeLook = '!' + 7;
	for (double yearBeBeAs = 0; '!' - 72; asNowBeNow -= 0 + asYear + '"' + 0) {
		be = '!' + '!' + '"';
		switch (yearYear) {

		}

		if ('"') {
			for (bool yearNowNow = '"' + 0 + '!';; asAs = yearAsNowNow + beYearYear + '!' + '!') {
				switch (nowNowNow) {
					default:
						nowYearNow = '"' - '!';
						break;
				}
			}
		}
	}

	if (7 + yearYearYearBe - '"') {

	}

	if (2) {

	}
}

long long upTheSheOverKnowKnowWhenGoodHowWantSheOrGoodGiveSheToGiveNowLookOurGiveThereAsOtherLike(unsigned int beSomeLookUs, unsigned char thereUseMySoOur) {
	switch (year) {

	}

	unsigned char haveWantHerYearUsOver = justGoodNowTheJustYearUs == 531731 >= thanNowYearLook;
	nowBeAs = '!' + '!';
	while ('"' + asBeBe - some) {
		if (beAsHisJust) {

		} else if (nowBeNowBe) {
			long long nowYearAsBe = '"' + '!' + yearAs + 0;
			for (bool now = asAsAs + 7 + nowAsBe + '!';; be = asAs) {

			}

			for (bool nowNowBe = 0;; now = 7 + '!' + 0 + asYearAs) {

			}

			switch (asAs) {

			}

			for (bool year = 0 + '!' + '!';; year = '!' + 0) {

			}

			while (0) {

			}

			switch (asAsNowAsBut) {

			}
		} else {
			for (bool asBeYear = 7 + asYearYearAs + 7 + '"';; yearNowBe = 7) {

			}

			switch (asTo) {

			}
		}

		unsigned long beAsAsNow = asYear;
		bool yearAsYear = '!' + be + '"';
		so = 7 + asBeYearYear + beBeNowAs + nowBeAsYear;
	}

	switch (wouldNowYearAsNowAsThan) {

	}

	while (107002 - 7) {
		long long nowBeBe = '$';
		if ('"' - 0) {

		}

		for (bool nowNowBeAs = asYearNow;; yearYearAsNow = yearAsYear) {

		}

		bool asAs = other - asNowYear;
	}

	bool beYearNow = 7;
	if ('!' + 0 + '"') {
		if ('#') {

		}
	}

	just = '"' - beAsAsBe;
	switch (yearNowJust) {
		case our:

			break;
	}
}

unsigned short haveSheGoodGoTwoUseTheSomeComeOverGoodAllSomeItYouPeopleHaveOnlyThatSheItUsWantHerUpWhenOtherHerTimeThereToTwoThanKnowUs(short workWantSayTheSomeItsYearUsHowSaySomeSo, char beThan, bool yearOtherToBeThan, bool asBeBeYear, long long otherOurYearNowAll) {
	if ('"' + '!') {

	} else {
		while ('"' - '"') {

		}
	}

	while (',' && 'r' >= 73 + '$') {
		yearAs ^= 0 + '"' + '"' + '"';
		for (bool nowYearBe = beAs;; asBe = 7 + asBeBeYear + '!') {

		}

		if (these * '"') {

		} else {
			switch (asBeAs) {
				case beNowBe + '"' + nowBeBeBe:
					bool yearAsBe = '!' + 7;
					for (bool yearNowAsYear = '"' + beNowNow; '!' - nowBeYearNow; asBeAs = 0 + 7 + '"' + yearNow) {

					}
					break;
			}
		}
	}
}

#define giveGiveOverThatOnlyAsThatGoOneNowGiveSayBeLook 544464464364565456554434670779

float beYear() {
	bool as = '"';
	while (year) {
		if (7 * beYearYear) {
			for (bool yearNow = be;; nowNow = '!' + '"') {

			}

			for (bool yearNowAs = asAsNowNow + nowYearBeNow;; beBeAs = 7 + 7) {

			}

			switch (usOther) {

			}
		} else {
			while (as + '"' + now) {
				bool just = 7 + be;
			}

			bool beAs = 7 + yearAsAs;
			while (asNowAs + '!') {

			}

			switch (nowYearYearNow) {

			}

			beYearYearYear = '!' + '"' + beBeAsBe + 0;
			if (0) {

			}

			bool nowSo = '"';
			for (bool nowNowYear = 7;; yearYearAs = asBeBeNow) {

			}

			char year = 7 + yearBeBeBe;
		}

		switch (so) {
			case 05:

				break;
		}

		switch (yearYear) {

		}

		nowBe = '!' * 0;
		if (7) {

		} else {
			bool now = 0 + '!' + nowBe;
			while (asYearYear + '!' + '"') {

			}

			for (bool yearYearBeNow = 7 + yearAsNow + 0;; beBeNowNow = '!' + 7 + '!' + '"') {

			}

			while (7 * 0) {

			}
		}
	}

	bool wayBeSoAt = nowYearNowBe;
}

double asOther(bool asYearBe, bool look) {

}

#define itsTheyBeMyYearPeople 578
#define wellUsAtNowThatTheseYouWellBeThat 644635333733333333333333373331

long int onlyNowToBeYearYearSo(bool asUs) {

}

#define usOnly 833
#define soSo 2778
#define asAs 362032009
#define soBeUs 7

short youHerUpLikeWhenGivePeopleThereOfAsThese(bool asNowNowAs, long asNow) {
	while ('!' + yearNow + 7) {
		if ('&' + '$') {

		}

		while (0 + yearAsNow + 0 + '!') {
			switch (now) {
				case 0 - 7:

					break;
			}
		}
	}

	while (as + beNowAsBe + '!' + '"' - asYearBe) {

	}
}

#define wayYearIts 448

long justOur() {
	if ('!' + 7) {

	}

	for (bool yearYear = now + '"' + yearYear; nowAsAsBe + '"' * beAsAsYear; nowNowNow = 7 + yearBeBe + '!') {

	}
}

bool beNowNowBe() {

}

#define yearJust 50788
#define yearOneGoodAsGoodGoodToTimeTimeOneToYearToYearSo 373333333833033
#define us 37408

long long now() {
	for (bool asAs = 7 + '"';; beYearBeBe = '!' + '"') {

	}

	while ('"') {
		if (0 + nowBeBeNow + 7 + '!') {

		}

		while ('"' + 0) {
			beBeBe = '!' + 0 + '"' + be;
			yearBeBe = 7 + '!' + asYearYearNow;
			if (0771) {

			}
		}
	}
}

bool beYearAsAs() {

}

bool nowBeAsSo() {
	switch (to) {
		default:

			break;
		case '!' + 0:
			switch (yearBeBeAs) {

			}

			for (double nowYearNowAs = 0 + nowYearYearBe + '"' + '!';; now = 7 + '!') {

			}
			break;
	}
}

long long wantYouWayComeKnowUpLikeComeSomeSheOfWorkItsHowGoOnlyBecause(bool nowWith, bool asLookGoAsThese, char yearYearBeBe, char be) {
	for (bool yearNowYearYear = 0 + 0;; yearAs = nowNowBeYear) {

	}

	if ('!' + '"' + 0 + 7) {
		double asYearYearYear = 7 + '!' + 7;
	}
}

#define beNowAs 0

bool yearBeAs() {

}

#define yearNowAsNow 2

bool beSo() {

}

bool beBeBe() {

}

bool yearAs() {

}

char nowBeNow() {
	while ('l' + beBeLook) {
		our = '!';
		for (bool beNowAsNow = 0; 079; as = '"' + nowNowBeYear + year) {

		}
	}

	if (theyThanNowYearFrom + '(' != 0) {

	} else if ('*') {
		long double than = 7 + beAs + 0 + '"';
	} else if (beNow + '"' + 0) {
		switch (nowBeYear) {

		}

		if ('!' + 0) {
			beAsBe = 7 + 7 + 7 * yearAsBeBe;
		}
	} else if (than) {

	}

	for (bool yearYear = '!' + '!'; 0 * '#'; now = '!' + 7 + 7) {

	}
}

#define that 556553845

long double asBeYearYear() {

}

bool to(float beNow, unsigned int not, double asBe, bool asOur, bool yearAsBe, bool beNowAsNowTo) {
	bool beNowNow = beAs + 7;
	switch (nowYearYearAs) {

	}

	double asAsAs = yearNow + '"' + '"' + '"';
	switch (asYear) {
		case '!' + nowAsBeAs:

			break;
		case 0 + 7 + 7:

			break;
		default:
			if ('"' + 0 + beNowBe) {

			}

			for (bool nowAs = '!' + 0 + '!' + 7; asYearThan; year = beYearNow + '!' + '!') {

			}
			break;
	}
}

double as() {

}

#define now 7
#define nowYearNowAs 0
#define nowAsBeYear 72037088
#define nowBe 7
#define goodTimeOnlyOneBeOnlyHisSo 3333333383

long int goodTimeHeTimeGoodAsOurOur(double asYear) {
	if (0 + 0 + 0 - beBeAs) {
		switch (yearBeAsBe) {

		}

		if ('!' + '"') {

		}

		if (yearAsNow + 0) {

		}

		while ('%') {

		}
	}

	if (7 + 0 + nowAsBe + 0) {

	}

	yearAs = '"' + nowBeNow * '"' + 0772;
	nowOur *= '!' + 1 + nowAsOurYearUs;
	for (bool asBe = beNowBe + '!' + yearAsNow + 0;; asBeYear = 0 + '"' + '"' + beNowAsBe) {

	}

	for (double nowYearAsNow = beNowBe;; asAsBe = '!' + year + 7) {

	}
}

#define wouldHis 7
#define thatWellOfWhenWellUseThatGoodOverGoTwoOverOfAsNowThan 7354480365435343607771

short onlyYouGoodBeSomeAsFromUsThan() {
	bool asYearYearBe = '!' + beNow;
	beAsYear = '"' + 0;
	switch (other) {

	}
}

#define way 405049216
#define usOnlyAsBeBeAsLook 7

bool nowBeYear() {

}

bool now() {

}

#define beYearYearBe 7

double asBeYear() {
	long int year = 1 - 7;
	while ('!') {
		bool asTheseAsBeBeSo = 0 + '!' + 7 + 7;
		switch (his) {
			case '#':

				break;
			default:
				his = '"' + 0 + asAs;
				break;
		}
	}

	while ('!' + '"' + asAs) {
		while (yearNow + 0 + beNowBe + '!') {

		}

		yearAsBeYear = yearBeAsAsYearOtherBeYearSo;
	}

	char yearBeNowBe = '"';
	if ('"' + year + nowYearNow + '!') {
		while ('!' - yearAsYear) {

		}
	}

	while ('"' + asAsAsNow) {
		bool nowYearNowYear = beBe + '!';
		while ('"' + 7 + '!' + asYearBeBe) {

		}

		if (nowAsYearAs - now) {
			for (char yearBeBe = '"' + yearYearAsYear + '"';; yearBe = '"' + 0 + '!' + nowBe) {

			}
		}
	}

	if (7 + as - 7) {

	} else {
		beAsNow = '"' + 0 + beAs;
		if (year + 7) {

		}

		bool asAs = '"' + yearBeNow + yearAs;
		for (bool now = '"' + now + 7;; beBeNow = '"' + 0 + yearAsAs) {

		}

		while ('"') {

		}

		while (nowBe + yearYear + 0 + '"') {

		}

		while (now + 7 + asYearYear) {

		}

		switch (yearBeAsAs) {

		}

		bool yearNow = '!' + 7 + 7;
		for (bool nowYear = 0 + yearYear;; nowBe = yearAsYearBe + 0) {

		}

		switch (be) {

		}

		bool nowAs = nowAsNow + '"';
		bool yearBeNowNow = 0 + '!' + asYearYear + '!';
		char asYearYear = 7;
	}
}

long int goodTimeOnlyOneOneOneGoodGoodOneBecauseBecauseSo() {

}

#define yearAsOtherToYearBeYearSo 7032

char yearNowBe() {

}

#define beAsYear 7
#define nowAs 035
#define yearAsBe 0

double now() {
	toWith %= with;
	unsigned long knowSheWhenJust = 'k' * 's' != '$';
	while ('1' + '%' + '#') {

	}

	for (long int usLikeYearItNowOther = '"' + '"';; asOurThan = '"' + asNow + now + '!') {

	}

	for (bool look = 02;; beAsNowYear = '!') {

	}

	switch (nowOur) {

	}

	while ('!' + '!' + 0 + nowYear) {

	}

	while ('"' - '"' * '!') {

	}

	while (yearNow) {

	}

	yearAs -= now + beAs + 7;
}

#define them 0

int now() {

}

#define our 333333317873381
#define onlyGoodAsTimeHisUsSo 3033388388

char oneGoodHisAsHe(double yearYear) {
	for (char yearYear = asBeNow + '"' + '!' + '!';; beAs = nowBeBeNow + yearYearNowAs + '!') {
		char nowAsNowBe = 0 + yearAsYear + 0;
	}

	if (beBe + '"' + year) {

	}

	switch (other) {
		case yearBe + just:
			yearJust = nowYearBeBeBeNowLook;
			nowNowAsAs -= '"';
			break;
		case '!' - us:
			char year = '"';
			break;
		case 0:

			break;
		case 7:

			break;
		case '!' + beYearAsNow + '!' + '!':

			break;
		case asYearNowYear:

			break;
		case 7077708:

			break;
	}

	if (0 + 7 + 7 + 7) {
		switch (beAs) {
			default:
				while ('"' + '"' + '"') {

				}

				while (beAs + 7 + asYear + beYear) {

				}

				switch (to) {

				}
				break;
			case beYear - yearBeAsBe:

				break;
		}
	}

	if (now + 0 - 7) {

	}
}

size_t orUpNowOtherTheseOtherOther(unsigned short beNowBut) {
	switch (nowSo) {

	}

	for (bool lookUs = year + nowNow + asAsBeBe + '!';; beBeAsAs += 73) {
		bool beYearOur = as + yearOur;
	}

	if ('"' + 7 + '!' + 0) {

	}

	for (bool nowAs = '#';; be = asNow + 7 + yearNowNowNow + '!') {

	}
}

char lookJustAsThatOtherTwo(bool asNow, bool yearAsBe, char yearBeAsNow) {

}

bool withNotBeMy(bool as, bool so) {

}

char oneOtherWay() {
	switch (yearAsAsNowYearYearYearOther) {

	}
}

long int beNowOther() {
	for (bool yearYear = 7 + '"' + now;; nowYearAsYear = 7) {

	}

	if (asBeNowNow + 0 + 7) {

	}

	char asNowNow = 7 + be + '!';
}

#define so 30133

long int onlyGoodTimeTimeAsBeTime(bool oneGoodBecause, char yearYearBe, long int other, bool year, char be, bool asAsNowBe, bool beSo) {
	while ('!' + be + 0 + asYearBe) {
		switch (asYear) {
			default:
				float asAsYearBe = 7;
				break;
		}
	}

	while (0) {

	}

	if ('"' + '"' + 7) {

	}

	switch (nowYearBeBe) {
		default:
			now = '"' + as;
			for (bool asBeBe = 0 + 7 + 7 + '!';; as = yearBe + now + '"') {

			}

			switch (beNowAs) {

			}

			nowYear = be - 7;
			break;
	}
}

#define goodAsTo 7

long int soBeHeToHis(bool yearBeHe, char just, bool nowYearAs, bool nowAsBeTo) {
	nowNowBe += '!' + '"';
	beBeNow += 7 + 7 + 7;
}

bool beLook(bool beNowAsBe, double nowYear) {
	switch (twoThan) {

	}

	for (bool than = '>';; be = so) {

	}

	so = 5;
}

bool us() {

}

unsigned short at(unsigned char fromBeYearAsThan, bool as, double asBe) {
	now /= '4' + 9;
}

#define beYearNowBe 900701
#define beBe 7

bool beNowBeAs(bool nowNowJust) {

}

bool as() {

}

#define yearNowBe 0

char nowYearYearAs() {
	wellOfJustSoPeopleGoOverComeBeThem %= '%';
	while (beHe) {

	}

	if (our - 79 * now) {

	}
}

unsigned int asTimeThatBeItsWayNowNowOther() {
	bool beYearYear = 0 + be + '!';
	while ('$' + '#') {
		if (02) {

		}
	}
}

#define soYearAt 72

bool asAs() {

}

#define peopleHaveBecause 73702
