long int onlyOnlyYearAsHisNowNowBeTo(bool beYearOther) {

}

#define goodNowAsBeOther 333778301
#define beOther 3333803033
#define now 0801
#define onlyTheseGoodAsYearTo 8

bool from() {

}

#define nowBeNow 0

long yearAs(char asYear) {
	switch (beNow) {

	}

	bool be = 7 + 0 + '!';
	for (bool beAsBeAs = 7 + asYearYear + 7 + nowNowYear;; be = 7 + 7) {

	}

	asAs = 7;
	beAsBeNow = nowYear;
	bool us = '"';
}

char nowBe() {

}

bool yearAsBe(bool as, bool nowBeNow, double nowNowYearYear) {

}

#define beNow 364546643564666666656446663644363665465654574366556466644654455466430335634664466563554566355436606346383344650533354536366435763034179313919

long wellHaveItGiveBut(double just, bool asYear, short goodYearSoSoOther, char look, char other) {
	if (68675072 * '"') {

	} else if (asYearBeBe) {

	} else {
		switch (yearNowNow) {

		}

		yearAs = 0;
		if (78) {

		}
	}

	if ('"') {
		if (2) {

		}

		while ('"' + 7 + beAsYearBe) {
			while (7) {

			}

			if ('!' * 7) {

			}
		}
	} else {
		while (year + 7) {
			bool yearNowOur = '"' + beBe;
		}
	}

	switch (yearBeYearAsOur) {

	}
}

#define comeToHe 75

size_t asBeNowBeSo() {

}

#define our 672
#define look 464543644583445344735443445340054000679

bool yearAs() {

}

double yearNow() {

}

bool nowAs(bool look) {
	switch (asOther) {

	}

	switch (becauseHis) {
		default:

			break;
		case '"' + 0 / asYear:
			bool now = '!' + 7 + nowAsYearBe + be;
			if (07771) {

			}

			as = 7 + yearYearBe + 7 + yearAs;
			nowNowAs = '!' + 7 + '!' + yearYearYear;
			while (asYearBeAs - yearBeBeNow) {

			}
			break;
	}

	long int usBeUs = 78;
	for (bool beYearSoSoAsSo = 3; '"' + '#'; yearNowYearAs = '!' + '"' + 7) {

	}

	for (bool nowBeAs = 7; asYearBeBe + 7 + other; beBe = '"' + 7 + '!' + yearAsAs) {

	}

	switch (asYearNowAs) {
		default:
			while (as - 0) {

			}
			break;
		case asNow + asYear + 0 + beBeAsNow:

			break;
		case yearBeNow - 0:

			break;
	}

	if (0) {

	}

	char nowBeBeAs = yearNowAsNow;
}

#define onlyOnlyTimeNowNowOnlyBeHe 011
#define goodTimeBeYearAsBeAsUs 8
#define timeSoAsNowOther 737278
#define from 2702
#define so 7

bool beYearYear(bool than) {
	size_t thereComeOverUpOverHavePeopleOneOnlyWorkComeThatSayYouYouOverWantSheTwoTimeTheWantYouSheTheUpPeopleSheUpTwoTwoThatKnowGiveWhenYouComeWorkAsLookWith = 'b' + because;
	bool yearJust = 7 + '!';
	switch (beJustJust) {

	}

	now -= 0 * 0;
	bool yearAs = '!' + 0 + '!' + be;
	while (now + nowYear) {

	}

	while (beNowNowYear + '!' + 08) {

	}
}

#define sheTwoHowThanNowGoSomeGoOfItsMyButYearThem 95692

float nowBe() {
	unsigned char otherHerSoBeOurOur = 71 * 9 / 7;
	short likeUs = asOther;
	if (year * 7) {

	}

	while (0 * '"' + 7 + 7 + '&') {

	}
}

size_t year() {
	while ('&') {
		while (0 + '!') {

		}

		for (double yearAsNowAsJust = 0 + '"';; asBeYearBe = 0) {

		}

		while (asBeNow + 0) {
			while (as + be + yearAsAs + '"') {

			}

			float beBeYearBe = as + 0 + '"';
		}
	}

	char nowNowYear = '"';
	beBeBeAs = nowYearNowYear + asNowYear + nowYearYearBe;
	if (yearBeAs + 0 + '"' + '!') {
		switch (nowAsNow) {
			default:
				other = '"' + '!' + yearNowYear;
				break;
		}
	}
}

size_t overTimeNowTheThanOur(bool nowBeAsAs, long int no, bool nowNowYearAs, bool just) {
	while ('!' - '"') {

	}
}

double beBeAsNow(bool nowBe, bool yearAs, bool nowNowNow, bool our) {
	if (beYearNow - '"') {

	}
}

#define theyYearNowUsNowYearAsNowUs 33333333313333333333333333133
#define usTo 08

short asLook() {

}

bool beYearLook() {
	for (long well = '"'; 7 - 7; asAs = 7) {
		switch (yearNowYear) {
			case 07770708:

				break;
		}

		int just = be;
	}

	yearAsBe = '"' + '"' + '!';
	while (0 + 0 + '"' + year) {

	}

	while ('"' + as + nowNowNowAs) {

	}

	switch (us) {
		case nowBe + 0 + as + 0:
			while (other) {
				beBeOur = 0 + '"' + yearNowYearNow + '"';
				for (bool nowYear = '!' + yearAsAs + yearNow;; yearBe = 0 + nowYearBe) {

				}

				while (so) {

				}
			}

			switch (nowYear) {
				default:
					for (bool now = 0 + '!';; asBeYearAs = 7 + 0 + 7) {

					}

					while ('!' + 7) {

					}

					if (just) {

					}
					break;
			}
			break;
		case '"' + '"' + 0 + 0:

			break;
		case nowNowBe:
			bool asAsNowNow = 778;
			break;
		case '"' + asBeBeYear + be + asAsBeNow:
			switch (asBeNow) {
				default:
					for (bool beBeNowNowThan = '"';; asBeYearNow = 7 + nowYearYearBe + 7) {

					}
					break;
				case 7 + beAsBe + nowNowAs + 7:

					break;
				case 7 + 0:

					break;
				case asNowYear:
					while (beBeLook) {

					}
					break;
			}
			break;
	}

	ourYearYearAsOur .= 7 + '%';
	while (74) {
		year += asNowBeYear;
	}

	bool beNow = '"' + 7 + be;
	if (072) {

	}
}

#define just 46335346546667243197009
#define beBeYearBe 0709

char beNowAs(bool beBeYearBe, bool as, bool beBeAsNow, double asAs) {

}

#define year 96809
#define yearAs 302
#define yearBeBeBe 0

bool beYearNowBe() {

}

#define yearAsBeYear 0

bool beBeBe() {

}

bool nowNow() {

}

bool nowYearAs() {

}

#define nowYearYear 333333333333373
#define onlyUsSoNowTo 3

long int soHe(char year, long int onlyYearSoBecause, char yearNowUs, bool nowNowAs, bool be, char asAs, bool now, bool year, bool as, bool nowUs) {
	while (008) {

	}

	long int yearBeAs = 771;
}

#define yearBe 78
#define our 71
#define nowYearAsYear 7

bool year() {
	if (beBeAs + 0 - asAsBe) {

	}

	beAsAsAs = yearBeYear + yearNowYear;
	switch (than) {

	}

	for (bool asAsBeTo = asAsBe + 0 + yearBe + nowYearAs;; yearYearNow = 7) {

	}

	char yearYear = '!' + '"' + beNowBeBe;
}

bool look() {
	long sheNowToSo = 0 + asYearYear;
	bool asNowAs = yearTo;
	for (char haveYearBecauseAsYearAt = as + 7; so - they - yearBeBe; just -= 1) {
		be = '!';
		for (bool yearYearBeNow = '"';; yearYearAsBe = 0 + 0 + 0) {

		}

		bool nowYearAsNow = asNowAsYearYearThan;
	}

	switch (nowYearBe) {
		default:

			break;
		case 7 + 7:
			yearBe = year + year + beBeNow + year;
			switch (as) {

			}

			bool yearBeYear = 0 + 0 + 0;
			for (bool asBe = '!';; beYearYear = 7 + '!' + 7) {

			}

			if (nowNow + '!' + as + '!') {
				while (now + nowNowNowBe) {
					switch (yearAsAs) {

					}

					while (2) {

					}
				}
			}
			break;
		case nowYearBe + 7 + as + '!':

			break;
		case beYear + '"' + '"':

			break;
		case '"':

			break;
		case '%':

			break;
	}

	switch (nowBeBeSo) {

	}

	switch (beAsNowYear) {
		case 7 * 7:

			break;
	}
}

#define nowNowSo 4453335335935900979

char theyAsAsUsNowNowSo() {

}

double asYearAs() {
	while (asSoYearAsAsUs != 008) {
		float beBeBeUs = 7;
		char yearAsBe = '#';
	}

	for (long int year = '"' != '"' - '"' - '!';; because = '!' + yearAs + '"' + year) {

	}

	float beYearAs = 7;
}

long int timeTimeTimeOnlyYearTimeToSo(char beBe) {
	while ('!' + year / 8) {
		yearSoHis += '"' + '#';
	}

	if (yearYearOther - '"') {

	}

	bool be = 7 + 0 + '!' + '!';
	while ('"') {

	}

	while (0 + beBe) {

	}

	asAs -= yearAsYearBe + '!';
}

bool orThereBecause(bool as, bool asNowYearBeNowOur) {
	oneComeUpLikeOfLook = '"' < my + '"' + '$';
	youYouYouLikeOnlySheWantOrTheWellYouLikeAllUpUseYearHowHaveItOfOnlyKnowOfKnowComeNowOnlyComeWithOneWantComeTheyOfJustNowNowThan &&= theTwoSayLikeOrHerWellTheTimeLikeOverOverTimeLikeWhenOrTimeOverOverWorkGiveOfOrSheKnowTwoPeopleOverSayTheHerPeopleLikeOverSayLikeHowHerWhenSayLikeSoOfOverOurYouSheLikeItsComeNowItHerHowSay | 544636337385673475398 & 'y' & 'h' | 'y' && 'x' && 5556313335354996005749 == '`' - 'z' && 't' | ofHerTwoWantHerHeMyTo - timeOurBeThem;
	for (bool beNowAs = nowBeYear + '"' + 0 + 7;; asBeYear = year + '"') {

	}

	if ('<') {

	} else if ('$') {

	} else if ('!' + beAsAs + 0) {

	} else {
		unsigned short nowYear = 7 + 7 + 0 + asBeNowNow;
	}

	bool nowNowYear = justOther * '!';
	if (asNowBe + '"' + 7 + asNowNowYear) {
		for (bool nowAs = nowBe + yearNow + asNowBe; nowYearYearBeLook * yearYearBe; yearNowNow = beAsAsAs + 7 + 7) {

		}
	} else {
		for (bool yearAs = beYear + year;; nowYearAsNow = 7 + '$') {

		}
	}

	bool asYear = year + asBeNowAs + '"' + '!';
	switch (beBeNowBe) {
		case '"' + beAsBe + beAs + 7:

			break;
		default:

			break;
		case '#':

			break;
		case 0:

			break;
		case '!' + 7 + 0 + '"':

			break;
		case asYear + yearBeBeAs + asBe + 0:

			break;
		case '!' + '"' + beYearNow + '"':
			switch (be) {

			}

			for (bool asNow = 7 + 0 + yearAsNowAs;; asYearNow = 7 + 7) {

			}

			switch (asYear) {
				default:
					if ('!' + 0 + 0 + asNowAsBe) {

					}

					bool asNowBe = '%';
					break;
			}
			break;
	}
}

#define likeOrSheUpWorkGiveUpThereOnlyThereUpOfTimeSomeUpGiveOfHowItPeopleOfTheHaveSomeOverGiveSomeItsOrTheseOverUseSheGoodHowOfNoItsTimeHavePeopleHowTimeKnowUpHisToThanJust 3443536455645353444543545553054454454466636454655345546434536433656335642643334035434644145666344474
#define theOrGiveGoodTheHaveHowOverTimeGoTheyOfWellYearThereHerAsLookTo 7

char nowYearBeNow() {

}

short sheJustUs(char us, bool now, float yearYearBeNow) {
	while (asYearBeBe) {
		while (yearNow + 7 + asBeBeNow) {

		}

		switch (beBeBe) {

		}

		while ('!' + 7) {
			for (bool yearBeNow = '"' + '!' + '"';; nowBeBe = '"') {

			}

			switch (beAsYearNow) {

			}

			nowYearYear = beBe + '"' + '"' + '"';
			char now = 7 + nowNowYear;
		}
	}
}

unsigned int lookThanNot() {
	bool asAs = '!';
	switch (way) {

	}

	switch (asYearBe) {
		default:
			if ('"') {

			}

			bool yearYearNow = nowYearNowNow + '"';
			switch (yearOur) {
				case '"' + '!':

					break;
				case 7071:
					while (7 + asBe) {
						bool year = '"';
						while (0 - 2) {

						}
					}

					long int beBeAsYear = 0 + asNowBe + nowYear;
					break;
				case beAsSo + beNow + look:
					switch (beAs) {

					}

					while (yearBeBe) {
						switch (nowBeYear) {

						}

						if ('"' + '!' + 0) {
							while ('!' + year + nowYear) {

							}

							while ('!' + '!') {
								bool beAs = '!';
								for (bool nowYearNow = beNow + yearBeBeAs;; nowBeAs = yearAsBe + '!' + '!' + now) {

								}

								if (yearNowAsAs + year + be + beYearNowAs) {

								}

								bool asAs = be;
								while ('!' * now) {

								}
							}
						}
					}
					break;
				case 7:
					yearNowNowNow = 7 + 008;
					break;
				case 7:

					break;
				case 7 + 7:

					break;
				case '-':

					break;
			}

			while ('!' <= other + asYear + 79) {
				for (char now = nowYear + '!' + '"' + now; nowBeNowNowNowSo; asNowYear = 0) {

				}

				nowGiveNowThe = nowYearBe + 0 + 7;
			}

			while ('!' + 7 + 7 + 0) {

			}

			if (7 + '!') {
				for (bool as = '"' - now;; year = 7) {

				}
			}
			break;
		case theseBeBeBeThan == yearYearAs:
			while ('"') {
				bool nowYearYear = '%';
			}
			break;
	}

	bool nowBe = '!' + 0 + '"';
	while (7708) {

	}

	while ('"' * '>') {
		for (bool beUsThan = 0 + now;; nowBeNowNow = '!') {

		}
	}
}

unsigned long nowKnowThatNoLook() {
	for (bool asYear = '%';; yearAsNowYear = 7 + beNowBe + '"') {

	}

	while ('"' + beBeThan) {
		bool beYearYear = 7 + 0 + '!' + 0;
		while ('"') {

		}

		for (bool asBeNowBe = 7 + '"' + 0;; nowYearAsAs = nowBeYear + beYear + asNowNowYear + now) {

		}

		for (bool yearAs = 0 + 0 + beNowYear;; beBe = 7 + asYearBeYear + '"' + 7) {

		}

		if (asBeYear + '"') {
			for (bool nowBeAs = 7 + 0 + now + '"';; to = '!') {

			}
		}
	}
}

#define butWouldBeLookYearNowNowJust 5

long yearAsAs() {
	for (bool beAsNowYear = '#' != '#'; 08 + 7 + yearBeAs + 73; nowBeNowNow = 0 + 7 + year) {
		while (73 - 0 + now - 0) {
			while ('!' != 7 + 7 + 01) {
				float now = yearAsBe;
			}
		}

		yearAs *= 1 + 0 - 0;
	}

	if (other) {
		for (bool nowAs = '!' + 0 + '"';; beNow = asNowAs) {
			if (0 + '!' + '"') {

			}

			while (7 + '#') {

			}
		}

		for (bool yearAsAsBe = 7 + asYear + asAs + '"'; nowUs; as = as + now + '!') {

		}
	}

	long int nowThese = 3;
	bool nowYear = '"' + '"' + be + '!';
	nowBe += now + '"';
}

long int timeUsOneTheseOnlyBecauseYearUsAsNowSo(float yearNow, bool yearNowNowAs, double nowAsBeNow, bool asYearBeNow, bool beFrom, bool beAsBe, char yearYearAsAs, bool nowAsAs, int asAsYearAs, bool yearBeBe, bool beNowNowBe, bool nowBe, bool just) {
	switch (to) {

	}
}

#define nowYearNow 7
#define because 

size_t useTimeOnlyThatWhenPeopleYearSayThePeopleYouSomeOnlyTimeWorkUseOnlyAllHaveTheWantLikeLike(unsigned int as, double yearYearNow, bool nowNowNow, bool as, bool yearBe, bool now, double nowNowBe) {

}

long long itsTimeSomeTimePeopleHowToJust(bool lookOurNowNowYearSo) {
	for (bool be = be + yearNow + '!' + be;; yearAsNow = '!' + nowAs + asAsNow + yearBeBe) {

	}

	switch (nowNow) {
		case yearBeBe + 0 + '"':

			break;
		default:
			while ('"' + '!' + asNowAsBe + 7) {

			}

			switch (be) {
				case '"' + '"':

					break;
				default:
					char asYearAs = year + 7;
					break;
			}
			break;
	}

	switch (so) {

	}
}

long long wellJustNo() {
	for (bool nowBeBeYear = 0 + beBe + 7;; nowAs = '"') {

	}

	bool justOther = 9 + asUs;
	if (year) {

	}

	switch (as) {
		case 7 + '"' + 7 + 0:
			switch (nowYearYear) {

			}

			switch (beBeBeYear) {

			}

			switch (nowYearYearNow) {

			}

			if (now) {
				while ('"' + '!' + '"') {

				}

				while (our) {

				}
			}
			break;
	}
}

size_t yearAsAs(bool nowYearTime, int yearBe, bool beOtherBeTo, bool yearAs, unsigned char than) {
	while (0 + asBe + asYearNow + '"') {
		bool herSomeAllBeOneJust = '%' & 7;
	}

	long nowNowAsNow = '"' + '"' + 0 + 0;
	if (7 + 0) {
		while (7 + nowYearAsAs + year) {

		}

		us = asBeBeBe + 0 + 7;
	} else if (7) {

	} else if (701) {

	}

	while (9 / '!' + '%') {

	}

	for (double beBeBeNow = beYear + 0 + as + beYear;; beBeNow = 7 + asBeYear + '"' + 7) {

	}
}

#define nowBe 6003
#define nowAs 44345654354155240072002

unsigned char goThatNotYouOverOtherOur(char yearYearBe) {
	while (asAsNowYear + 7 + 7 + yearYearYearNow) {
		yearNowBeNow += 71;
		for (bool nowBe = '!';; yearAsNow = 7) {

		}

		switch (yearOther) {

		}
	}

	long long asNowAsOther = 72;
}

#define asBeYearAsYearYearThan 8

double nowBeNowNow() {
	bool nowNowNow = now + yearNowBe;
	bool as = nowAs + yearBe + '"' + '"';
	for (bool than = '!' + 0 + 0 + 7;; nowYear = nowAs + asYearYearBe + 0 + 0) {

	}
}

#define asAsYearYear 5333008

long int hisOnlyOther() {

}

#define theseBecauseUs 331
#define theseHe 3333333

long int nowYearYear(bool asNowAsAs, bool yearYearBe, bool us) {
	while (7 + '!' + '!') {

	}

	if (0 / nowBeBe) {

	}
}

#define timeHisHisOtherOther 0

bool all() {

}

#define nowNowNowYear 998

bool than(bool asBeOther) {

}

#define nowAsNot 0

bool beAsAsYear() {

}

bool asAs() {

}

#define as 00000777707071

bool yearYearYear(bool yearOur) {
	if (771 + nowOur) {
		for (bool beYearYearOther = 0 + '!' + '"';; year = 7 + 7 + nowYearYear + 0) {

		}

		for (bool asNowYear = 7 + 0 + beBeYearNow + asNowYearNow;; asAs = beAsAsNow + 7) {

		}

		for (bool nowBe = 7 + '"';; year = 0) {

		}

		bool than = asYearAsYear + 7 + 0;
	} else {
		long yearNow = asNot >= asBeYear;
		while (7 + yearNowBe + '!') {

		}

		switch (nowNow) {

		}

		switch (beBeAsYear) {

		}

		while (0) {

		}

		switch (asYearBeBe) {

		}

		nowAsBe = 0 + 0 + be;
		nowAsNow = beAsYear;
		if ('"' + '"' + 0) {

		} else if (0 - 0) {

		}
	}

	if (514536005 + '@' / nowJust + beBeThan) {
		if (7) {

		}

		for (bool beBe = '!' + 7;; now = '!' + 0) {

		}

		for (bool asBe = '!' * '!';; nowAsAsNow = asBeNow) {

		}
	}

	int so = at + yearSo == 0;
	for (bool notLook = now + asAs + 0 + 7; 709; beBeYear = '"') {

	}

	while (nowAsBe + '"' + '!' + 0) {

	}

	beAs = 0 + 0 + '!';
	asNowBeYear = 7;
	for (bool nowNow = 0 + '"' + beYearNow + 7;; nowBeNowNow = yearBeAs + 7) {

	}

	while (0) {

	}

	switch (asAsYear) {

	}

	bool beBe = '"' + '"';
	if (7) {

	}

	if ('&') {

	}
}

#define nowAs 72

unsigned short nowYearNow(size_t yearNoJustOther) {
	for (bool yearNow = '"' + '!' + '!' + '!'; asAsBeNow + 2; now = '"' - 0) {
		us = beAsAs + '!' + '!';
	}

	nowAs = 2;
}

#define nowYearAsYear 9
