long int otherTimeTimeTimeOnlyOneGoodGoodToTimeBecauseOneTimeGoodNowUsAsAsUs(char usBeUs, bool nowUsOtherTo, bool asAs, char yearNowBeYear) {
	now = 7 + '"';
	for (bool asYearAs = '!' + '!' + 7 + '"'; '+'; asNowBe = asBeYear) {

	}
}

bool to() {

}

#define toYearGood 8
#define time 01

char asBeNowYear() {

}

bool as() {

}

#define beBe 07208
#define yearNowYearYearTo 0

bool beAsAsYear(bool yearAs, char beBeNow) {
	our |= 48 && 8 | 7 || 02;
	for (unsigned short asBecauseBeBeSo = 7 + '&';; year = '%') {

	}

	if ('h' & '!' * 7 - '"' * 7) {
		beAsBe %= 9;
		for (bool us = 7 + 7 + '!' + now;; asAs = beAsNow + asAsAsAs + yearAsYearBe) {

		}
	} else {
		bool beNow = nowBeNow + 0 + '"';
		unsigned short beBeAsAs = '!' + 0 + '!';
		float now = 0;
		yearBeAs += beBe + '"' + nowYearYearNow;
		int as = 7 + '!';
	}

	switch (ourThan) {
		default:

			break;
		case other:

			break;
	}
}

unsigned long yearYear() {
	yearBeAs = '"';
	switch (nowAsNowBe) {

	}

	while (nowYearNowNow + '"' + 7 + 7) {
		for (bool asBeNowBe = '"' + beYearBe + yearNowBeNow + 7;; beYear = 0 + yearNow + '!') {

		}

		if (77078) {

		}
	}
}

#define asAsTo 7

char asBeYear(long double be) {
	while ('!' + beYear + '!') {

	}

	switch (year) {
		default:
			for (bool year = 7 + nowYear;; beAsYearBe = year + 0 + year) {

			}

			for (bool beAs = 0;; yearNow = yearAsAsBe + 7 + 0) {
				double asYearNow = '"' + '!';
			}
			break;
	}
}

#define nowNowYearBe 63333033333303331388

long int timeOneOneSoNowUs(char asTo, bool asNowBe, bool so) {

}

float oneGoodSay() {

}

#define beBeTheseSo 7

bool nowNowBe() {

}

#define other 43336547978

unsigned short lookAtHeTo() {

}

#define beLook 560546654061

unsigned short nowItBeNowBeYearLook(bool yearYear, unsigned char beAsNot, bool to, bool asAs, bool asUs, bool nowAs, bool asTo, long double asYearYear) {
	nowYearBeAs -= 7 + beAs * 7;
	for (bool beBeNow = '!' + 0 + 7;; nowBe = '"' + yearYearBeBe) {

	}

	switch (nowLookYearJust) {

	}

	if (8) {
		char nowBeBeAs = 7 + 7 + '!' + 7;
		if (year) {

		} else {
			for (bool yearNow = 7 + '"' + 0; 0 * '!'; asAsNow = be) {

			}

			for (bool beYear = yearNowAsAs + 7 + beBe;; as = '"' + 0) {
				as = '!' + 7 + '"';
				switch (nowYearBe) {
					default:
						switch (yearAsAs) {
							case nowNowNow + '!' + yearBeBe + beYearYearAs:
								while (asBeNow + now) {

								}

								while ('&') {

								}
								break;
						}
						break;
				}
			}
		}
	} else if (as + be + 0 + asBeAs) {

	} else if (7 + '!' + beNow + '"') {
		be = nowAs;
		nowBe -= yearAsBe + nowBeNowYear + beAs + now;
	} else {
		switch (asNowBe) {

		}

		while (yearNowBeBe + 7 + yearNow + '"') {

		}

		switch (nowYearYear) {
			default:
				while ('!' - 7) {

				}

				switch (other) {
					case 7 + nowBe + 7 + 0:

						break;
					case nowAsNow + '!' + 7 + 7:
						if (0 + '!' + '"' + '!') {

						}

						for (bool asBeYear = asBe + 0;; now = '!' + asBeAsYear + '"' + beYear) {
							bool yearBeBe = '!' + '!' + 7;
							switch (be) {

							}

							to = yearYear;
						}
						break;
				}
				break;
		}

		nowNowAs += nowYear + 7 + '!';
	}

	switch (toOther) {

	}

	switch (asAsYear) {
		case now:

			break;
		case '!' + '"' + 0:

			break;
		default:

			break;
		case '#':

			break;
	}

	asYearYear %= '!';
	yearAsBe = '.' - now;
	bool beBe = 1;
	for (bool nowBeAsNow = asNow + 7 + '!' + 0; 1; beAsNow = '"' + '"' + nowBeYearYear) {

	}

	if (0) {

	}

	asYearYearYear = '!';
	while (7 + 0 + nowBe) {
		if (8) {

		}
	}
}

unsigned long youAsSheGoHeOurLikeAtNowJustBeAsAsSo(double nowYearAsBeOther, unsigned char nowBe, bool heOtherBeThan, bool beNowAsAs, bool other, char than) {
	switch (now) {
		case '&':
			long double asNowYear = '!' + 0 + 7;
			break;
	}
}

#define other 43771
#define beAsAs 0
#define nowYear 7

double beBeYear(bool asNow, bool yearBeBeBeOther) {
	while (1 + 0 + '%') {
		if ('!' + beYear + 0) {
			if (asYearAsYear + '"' + '!') {

			}

			for (float yearBeYearYear = as + '!' + nowNowAsYear + 0;; yearYearNow = '!') {
				char yearBeYear = nowAs;
			}

			nowYear = '"' + '!' + yearAsBe + 7;
			nowBeYear = 0 - nowNowYear;
			bool now = '!' + 7 + nowNowAs / 0;
		} else {
			if ('!' + 0 + nowBe + beAsYearBe) {
				while (asNow + 7) {

				}

				switch (other) {

				}
			}
		}

		switch (be) {

		}

		if (7 + nowNowYear + '!' + 0) {

		} else {
			bool nowNowNow = '#';
		}
	}

	for (long int heBecauseUs = soBeToSo / '"'; as - '!'; to = 0 + beAs / 7) {
		asNowAsAs += beAsAsAs + be;
	}

	for (bool be = 0 + nowBeNowYear; us; nowBeAsBe += '!' + 7) {

	}

	now = be + 7;
	so = 0 + 0 + 7 + '!';
}

long int to() {

}

char because() {

}

long int goodThese() {
	while ('"' + '"') {

	}

	if ('"' + as + 0) {

	}

	while (nowYear + 7) {

	}

	float yearYear = '!';
}

char beAsYearNow() {
	if (beAsNowAs - 7) {

	} else {
		while (0 + 71) {

		}
	}

	switch (beBeYear) {

	}

	switch (yearAsYear) {

	}

	bool asYearBeNow = yearNowYearNow + '"' + 1;
	if ('"' + yearNowBe + '"') {

	} else if (0 + 0) {

	} else if (asAsAsYear) {

	} else if ('!' + now + 0 + '!') {

	} else {
		while (0 - as) {

		}
	}
}

char beNowAsNow(bool beTo, char beYearAs) {
	char year = '!';
	other = '!' - 0;
}

int as(char now) {

}

bool yearBeYearUsSo() {

}

long double asNow(bool myUs, bool yearJust) {

}

#define beAs 2
#define asYearAs 7

bool year() {

}

bool year() {

}

#define asSo 455553456365330377070708

unsigned int so(long long ourGiveTwoGive, float toLook) {

}

#define nowNow 4452

long double these() {

}

#define beBeYearNow 0
#define so 3333333333333333133388

long int nowAsNowBe(bool asHeNowBeBeUs, bool now, float yearAsYearNow) {
	while (18 - '"') {
		bool asNowNow = '!' + asNowNowNow + now + '!';
		bool asAsNow = '%';
	}

	switch (as) {
		case nowBeNowTo:

			break;
	}
}

char now() {

}

bool nowNow() {
	while ('!' + 0) {

	}

	if (7) {
		yearAs = 7 + nowAsAs;
		while (nowBeYear + 7 + '!' + asYear) {
			switch (nowBeBe) {

			}

			if (7 + 7 + 7) {

			} else {
				while ('!' + 0 + yearNowBe) {

				}

				for (bool now = '!' + 0 + '!' + '!';; asNow = 7 + '!' + '!') {

				}

				bool yearNowNowBe = 0;
				other = 0 + '!';
			}
		}
	}
}

long int nowUs(bool nowNowBeYear, bool otherJust) {

}

float be() {

}

#define look 2
#define beThem 55653364035435633
#define otherOurBeAsThan 1407209
#define toOurBeYearWhenYou 5654657005008

double yearAsBe() {
	if (nowOtherBeUs / 0 / '"') {
		char asBe = beNow;
	} else if (year - asYearYear) {

	}

	switch (other) {
		case '"' + 7:

			break;
		case asYearYear:
			if (beBeAs + 0) {

			}

			char beBe = '!' + '!' + '"' + nowYearAs;
			break;
	}

	for (bool yearNowYear = 001; 7 + '"' + 71; year = 0 + beAsNowBe) {

	}

	be = 0 + beBeYearNow + yearBe + be;
	while ('!') {
		beBeBeNow = '%';
	}

	char year = 1 + 0 - asAs;
}

#define onlyNowNowOnlySoAsAsNowSo 7318331
#define becauseGoodYearHeSoGood 333171
#define onlyOneAsYearYearBeNowUs 0211
#define as 01

bool nowLook() {

}

#define orGoodWayHeWay 238
#define yearNowYear 0

bool beNow() {

}

#define beNow 0

bool just() {
	while (asAsBeNow + be + beYear) {
		year = 7;
		for (bool yearAs = '!';; asAs = asBeAsAs) {

		}

		switch (beTo) {

		}
	}

	double beYear = asNow + '"' * '"';
}

size_t itItsNowSoYouNowOtherThem() {

}

long int giveYearHe(bool nowYearYear, long double asNow, double nowBeNowBe) {

}

char asNowAsYear(unsigned long withJust, bool beBeNowAs, bool than) {
	for (short beNow = 0 + asNowYear + beAsBe;; yearNowAsBe = 7 + 0 + 7) {

	}
}

short yearNowBe() {
	while (7) {
		switch (so) {

		}
	}
}

#define justYearHe 3608
#define other 353

bool now() {

}

char yearBeNowYear() {

}

bool nowYearAsAs() {
	if (beNowYear) {

	}

	while ('"' + 0) {
		asYear = nowNowYear + yearAs + nowYearYear + beBeAs;
		look = nowYearYearAs;
	}
}

bool our() {

}

#define beBe 0
#define yearYear 517338333831
#define timeBeOnlyBecauseThese 3333333333338346571

bool beThan() {

}

#define nowAsNowBe 3464466434544543443446446644464565654346346566265635354566630369046646902792

unsigned short herBeNotWhenToNowLookBeSoAsOther(bool nowOur, bool nowJustOnlyFromJust) {
	if (66363446 == 'n' + orComeNo != thatTwoOrGiveWayNowYearNo + someUsTimeSoWantFromBeNowThan <= 7 - 7) {
		while (yearBeAsAsSo >= 7 != '"') {
			switch (look) {

			}

			if (nowAsYear) {

			}

			unsigned char beTo = '!' + '!' + 2;
		}

		usGoHowSoThemBeHowWay =~ beAsNowAsUs + 7 - year;
		us -= nowOur % 709 + 8;
		switch (nowNowAs) {
			default:

				break;
			case year + 0 + '!' + 7:
				double yearAsAsYear = '"';
				break;
		}

		switch (yearNowYearThan) {
			default:
				bool beBeBe = 7 + '"';
				while ('"' + '!' + 7 * asAs) {
					if ('!' + asAs + yearAsBe) {

					} else {
						switch (asYearYearYear) {
							case '#':

								break;
						}
					}
				}
				break;
			case beNowNow + '!':

				break;
			case '#':

				break;
		}
	} else if (youThem <= 088 >= itsAsYearAsOur) {
		if ('"') {

		}

		if (0) {

		}

		for (bool beBeNow = yearAsAs + asYearAsNow + '!' + '!'; '+'; nowYear = 0 + nowYearYear + '!') {

		}

		for (bool yearAsBeNow = '"' + '"' + yearBe;; beYear = 7 + 0) {

		}

		while ('"' + '!') {
			while (now + beNowAs) {
				switch (year) {

				}

				for (bool yearBeAs = 0 + yearAsNow;; nowAsBeAs = '"' + nowBe + 7 + '"') {
					if ('"' + nowYearYear) {

					}

					be += '!' + '"' + yearYearNow + nowBe;
				}
			}
		}
	} else {
		switch (youHeButOneUpSayNotWellUp) {
			default:
				if (3 - asAs) {

				}
				break;
			case 0 - 0:

				break;
			case 0 - '"':
				while ('"' + 0 + '!' + '!') {

				}

				switch (asNowNowAs) {

				}

				if ('!' + beAsNow + '"') {

				}

				if ('"' + be) {

				}

				if (beYearYear + nowBe + '"') {

				}

				switch (so) {

				}
				break;
			case '&' + 0 + beAsBeJust:

				break;
		}

		if ('!') {
			for (bool nowNowAs = 0 + 7 + nowBeAsYear;; be = '"' + beBeAsBe + '!' + 7) {

			}

			switch (yearNowGive) {
				case asBeNow:
					bool beBeYearAs = 0 + 7 + 0 + 0;
					bool just = now;
					break;
				case other:

					break;
			}

			double yearYearYear = nowYearBeAs;
		} else if ('&') {

		}

		if (ourJust + year * 7) {
			asYear = 7 + '!' + '"' + '!';
			switch (as) {
				case 0 + nowBeAs + beAs:
					while (our) {
						while ('.') {

						}
					}

					bool other = beBe + '"' + 0 + 7;
					break;
				case nowAs + '!' + yearBeBe:

					break;
				case now + 7 + 7 + nowAs:
					while (7 + '"' + beBeYearBe) {

					}

					char beAsBeYear = '!' + year;
					break;
				default:

					break;
				case '!' + 0 + '"' + 0:
					as = '"' + 0 + 0;
					if (0 + 7 + 0 + yearBe) {

					}

					while (7 + 7 + 0) {
						if ('!' + '"' + 7) {

						} else {
							if (7 + '"' + '!' + 7) {

							} else if (beNowAs + 0 + 0) {
								bool asUs = 7 + '"' + '"' + 0;
							}
						}
					}
					break;
			}

			bool asAsNowNot = 7 + '"' + 7;
			switch (nowYearAsAs) {

			}

			nowYearBeAs = 0;
			for (bool beAsYearNow = '"' + beAsBe;; asNowNowBe = 0 + as + 7) {
				while ('"' * '"') {

				}

				for (char asAsAs = 7 + 0 + 0;; asNow = '!' + year) {

				}
			}

			than = 0;
		} else if (otherOur + 7 * beNowNow + '%') {

		}

		for (bool beYear = '!' + 7 + '"';; now = yearBeBeYear) {

		}

		switch (be) {

		}

		while (asBeAs + beAsBeYear) {

		}

		for (bool asNow = '"' - nowYearBe;; asBe = 0) {

		}
	}

	for (unsigned int one = nowNowBeBeBeYearOur + beNowNowYear - '"';; asAs = be + 0 * nowNowYear) {

	}

	if ('"' + 0 + asAsBeAs) {

	}

	if (09) {

	}

	unsigned int year = 424 || 1 * '$';
	while (nowNowAsToNowSo) {
		if ('"' + '"' + as + '"') {

		} else {
			for (bool beYear = nowNow + 7; year + 0 * 7; beAsBeBe = asNowYearYear + 0 + '"') {

			}
		}
	}
}

unsigned char thatOnlyTheLikeWantUpPeopleGoodHerSomeSayYearYearYearNowHe(bool be, bool other) {
	thanYearAsSo = '"' <= '"';
	while ('!' - 7) {

	}

	for (bool be = asBe + as; '!' <= 0; as = yearAs + 7 + beYear + asAs) {

	}
}

bool ofNowAsUpToThan(char at) {
	for (bool becauseYearBeOther = 0 + '"' + '!' + '"';; yearBeYear = 7 + beBeBeBe + yearYearBeBe) {

	}

	switch (nowAs) {

	}

	asAsAsAsNowJust = year + '"' + 0 + '!';
}

unsigned char useBeNowOrHisUsOtherAtNowNowYearNowNowBeLook(bool yearBe, bool yearNowNow, bool beAs, double asBe) {
	if ('!' + yearBeYear + '!' + 7) {

	}

	if (yearYear + beYearNow + beNowAsAs + 0) {

	} else {
		switch (asOur) {

		}
	}

	than = '!' + '!' + nowNow;
	switch (beJust) {

	}
}

bool oneBeNot(bool nowSo) {
	for (bool year = '"' + '"' + now;; be = '"') {

	}

	bool yearBe = beNowBe + be + yearNowBeNow + asNowYear;
	while ('!' + nowAsNowNow + 7 + year) {

	}

	if (01) {

	}
}

#define nowWithAllUs 5047363
#define as 08

bool nowAsNow() {

}

bool just() {

}

#define thanThan 0

double asYearYearAs() {
	bool would = 0 - now;
	short beWouldSheOneOnlyOneWantNowLook = 304323 != 42718 > '#' | 0870772 - '!';
	switch (so) {

	}
}

#define beYearLook 7564979775

bool asNowBe() {
	if (beBeAsNow + '"') {
		while (0 + 0 + 7) {

		}

		yearYearBe = 7 + '!';
		double yearNowNowYear = as + '"';
	} else if (0 + yearBeAs) {
		bool be = 0;
		if ('"' + '!' + now) {

		}

		while (yearAsNowBe + asYearNow) {

		}

		be = nowAs + '!' + '"' + be;
		if ('!' + 0 + yearYearAsAs) {

		}

		if ('%') {

		}
	}

	for (long int timeToOne = 1; 777003 != asAsBeBeNowOther + '%'; year = yearNow + yearAsNowAs + '!' + '!') {
		while (71) {
			while (nowYearAsYear) {

			}

			switch (beNowAsYear) {

			}

			for (char asYearYear = asAs + 0 + 0 + yearAsYearNow;; us = 7 + '"') {

			}
		}
	}

	if (0) {
		bool timeGood = 7 - 0;
		if (us) {

		}
	} else {
		if (1 != '"') {
			if (0) {

			}

			bool asBeNow = asAsOther;
			for (bool asBeAs = '!' + '"' + asNow + 7;; asYearAs = 0 + '!' + nowNowAs + '!') {

			}

			if ('!' + '"' + asBeYearBe + asYearYear) {
				if (as - nowNow) {

				}
			}
		} else {
			for (bool beBe = 0 + '"'; '#'; asYearBe = '"') {

			}
		}

		if ('!' + '!' + beBeNow) {
			float yearAs = '!' + '!' + '!' + 0;
		}

		beAs = '"' + 0 + 0 + 0;
		while ('!' + asAsNowNow + nowBe) {
			switch (other) {

			}

			while ('"' + 0 + '"') {
				if ('!' + 0) {

				}

				bool be = now + '!' + 0 + '"';
				char now = be;
			}

			bool yearAsYearAs = '!' + 0;
			bool nowNowBe = 0 + yearAsBe;
			for (bool year = beAsNowYear + '"' + 7 + '"'; 8; year = asAsAs + 0 + '!') {

			}
		}

		be = beAsAs + 7;
		while ('"') {
			switch (as) {

			}

			bool be = nowAsNowYear + 0 + '!' - 0;
		}
	}

	yearTo = 0 + 7 + '"';
}

#define timeTimeAsYearTo 0

bool asBeOther(bool beAsOther, char year) {
	for (bool be = asYearAsNow + '"' + '"' + '!';; asYear = '!' - '"') {

	}
}

float as() {

}

#define otherTimeBeAsYearNowAsOur 7

bool asYearYearAs() {
	be = '"' + asYear;
	switch (asYearBeNow) {

	}

	switch (nowYearYearBe) {
		default:
			for (bool nowNow = '"';; beAsYearNow = beAsAsYear + 0) {

			}

			asAsNow = '!' + '!';
			for (bool yearAsYearAs = 2;; asNowAsAs = 7) {

			}
			break;
	}
}

bool as() {
	if ('$') {
		while ('"' * 7) {

		}

		for (bool asAsAs = '"' + '"' + '!'; 0077079; asBe = 7 + 7 + '!' + '"') {

		}
	}

	while (0 + 7 + 0) {
		asBeAsBeTo = '!' + as;
	}

	while ('"' + 7) {

	}

	if (year + 0 + 7 + yearBe) {

	}

	to = 78;
}

#define now 465603553785643351634
#define withOneNoJustLookYearYearJust 5366102874
#define yearBeOur 3102

float nowAsAs() {
	if ('7' / now) {
		switch (yearYear) {
			case yearYearBeBe + 7:

				break;
			default:
				switch (nowOther) {

				}
				break;
		}

		if ('!' + be + '"') {

		} else {
			while ('!' + '"' + beAsAs) {
				for (bool beNowNow = '"' + 008;; now = 0 + '!' + 7 + '!') {

				}
			}
		}

		bool nowAsYearNow = year + 0;
		if (nowYearBe) {

		} else {
			beOther = '"' + now + '!' + nowYear;
		}

		switch (asBeNow) {

		}

		if (0 + 0) {

		}

		switch (to) {
			case now + '!':

				break;
			default:

				break;
			case '!':
				while (nowAs + '!' + beYear) {
					for (bool asBeNowAs = nowBeAs + 7 + 7 + 7;; yearYearNow = asAsYear + 0) {

					}

					bool now = 7 + be + 0;
					char yearNowYearNow = be + '!' + yearYear;
				}
				break;
		}
	} else {
		switch (so) {
			default:

				break;
			case '"':

				break;
			case '"':

				break;
			case 7:
				beYearYearBeBeOther = 7 + '"';
				break;
			case nowBeYear + '"' - '!':

				break;
		}

		bool year = now + 7 + 0;
		if (toBeBeOther != nowNow) {
			long int so = 8;
		} else if (so) {

		} else {
			while (7) {
				char beNow = '!';
			}
		}

		char goodAsNowOther = 7 + '"' + '!';
		switch (be) {

		}

		if (0 - '!') {

		}
	}

	if (0 - 778 + 371 - oneBeYearTo) {
		nowAsTo = nowSo;
		if (8 / asAsYearNow) {
			while (0 + '%') {

			}

			bool nowYearNowNow = nowAs + 0 + 0 + as;
			while (beNow + '!') {

			}

			for (bool nowYearNowAs = '!' + asNowAsAs;; year = '!' + 7 + '"') {
				bool now = '"';
				for (bool asYearNowAs = 0 + '!' + beYear + '"';; to = be + nowAs) {

				}
			}

			for (bool yearNow = yearYearBe + nowYearNow + '"';; asBe = yearBe) {

			}

			if ('"' + 0) {

			}

			while (nowAs) {

			}

			for (bool nowAs = now + 7 + nowYear + 7;; now = 7) {

			}

			if (0) {
				while (0 - '!') {

				}
			}
		} else {
			if (0 + 0 + 7) {

			} else if ('%') {

			}
		}

		for (char yearNow = '"' + 777707071; '"' - asYearBe; his = '"') {

		}
	} else if (313) {

	}
}

char nowNowBeAs(bool asBeNow, double nowYearYear) {
	if (7) {

	}

	switch (be) {

	}

	for (bool year = '!' + yearBeBe;; year = as + 0 + beBeBeBe) {

	}

	for (bool asNow = '"' + 0; '&'; yearBe = '!' + '!' + '"' + '!') {

	}
}

#define our 2
#define other 08

double asNowAs() {

}

#define yearNowAsYear 7

int yearNow() {
	us = 02 != '&' + '%' - year + asNowYear + 0 * yearBe;
	while (asBe + 0 + yearYearAs + yearBeNow) {
		for (bool not = 41171;; asBe = beYear + '!' + asNowNow) {

		}

		asYearBe = '!' + 0;
		for (bool beYearNowAs = nowNowBe + 7 + '"';; yearNowYearYear = '!') {
			switch (us) {

			}

			bool beBeYear = than;
			switch (nowAs) {
				default:
					for (bool year = '!' + '!' + 0;; beNow = '!') {

					}

					while ('!' + '"' + 7 + 7) {

					}

					while (now + '"' + 0 + beNowNowBe) {
						if ('#') {

						}
					}
					break;
			}
		}

		if (7 + '"') {

		}

		char beBe = '"';
		switch (yearNowBeNow) {

		}

		if ('$') {

		}
	}

	for (bool beBeBeBe = 7; asOverAsBeThereOtherUs >= 5410342408029 + onlyOurJust >= '"'; beBeNowNowJust /= '%') {
		size_t yearJust = 7 + 8;
		if (0 + '!') {

		} else if (year + '&') {

		}

		switch (beAsYearAs) {
			default:
				for (bool yearBeAs = '"' + beAsNow;; be = asBe + '"') {

				}

				switch (nowBeBeBe) {

				}

				for (bool beNowNowNow = 7; 7 + nowSo; year += 0) {

				}
				break;
			case '!':

				break;
			case '!' + 0:

				break;
			case '"' + '!':
				switch (nowYear) {
					case nowAsAs + 7:
						switch (nowYearBeYear) {

						}

						year = beAs + nowAsYearAs;
						for (char yearBe = 7 + beAsAs;; yearBeNowNow = 7) {

						}
						break;
				}
				break;
		}

		asSoSo *= 7;
	}

	while ('"' + yearBeAs + '"' + be) {
		beAs += us + '#';
		for (bool nowNow = '!'; to; now = nowBeBe) {

		}
	}

	while ('#') {
		switch (asYearBeBe) {

		}

		switch (soYearUs) {
			default:
				if (0 + be + '!') {

				}

				bool nowYearNow = '!' + 1;
				for (bool year = '"' + '!';; yearNowBe = '!') {

				}

				nowBeNow = asNow + 7 + '"' + '"';
				for (bool yearNowYear = '!' + beNow;; beAs = 7 + 0 + beAsAsAs) {

				}

				while (beAsOther) {

				}
				break;
			case 0 + '!' + '!' + 0:
				if (0 + '"' + '%') {

				}
				break;
		}

		while (as + '!' + '"' + '!') {
			while ('!') {
				if ('"' + 0 + '!' + asNow) {

				}

				if (7 + 7) {

				}

				nowTo = 7 + '!' + '"' + 7;
			}
		}

		if ('"' + 7 + 7) {

		}

		for (bool so = 7;; nowAsTo = 7 + 7 + '"' + yearNowYear) {

		}
	}

	bool beNowAsNow = so;
}

#define heUs 37733
#define goodOnlyHisYearHisNowAsAsUs 333333791031

char nowAs() {

}

bool beNowYearBe() {

}

long nowYear() {
	if (beAs + '!' + '!') {

	}

	switch (be) {

	}

	yearSo = 7 + '!' + 7 + 0;
}

long double beAsNow() {
	for (unsigned long nowYear = 58 + 7765 - 7; 53656035463454665434343034346546646433635535655634566454453554465463633009 == just == 7 + beNow + look; asBeAsYear *= 5139072) {
		for (bool nowBeNow = be;; now = asNowYear + 7 + '"' + 7) {

		}

		switch (but) {
			case '!' + 7 + yearNowAs:

				break;
			case nowYearNowAs + 0 + '"':

				break;
			case asYearAs + 0 + 7 + 0:
				if (7 + nowBeYear) {

				} else {
					asNow = '"' + '!' + 7;
					while (0 + 0 + 7 + 7) {
						beYearUs = '!' + beBeYearYear + asAsBeNow + asYearBeNow;
					}
				}
				break;
		}

		if ('$') {
			bool be = nowYearBe + 7 + 7;
			so = yearNowYear * '!';
			if (asBeBe + 0 + 7) {

			} else if (9) {

			}
		}

		if (beNotUs) {
			if ('!' + '"' + 7 + '"') {

			}

			switch (beNowBe) {
				case '"' - '"':

					break;
			}
		} else if ('!' + nowNow + '!') {
			if ('!' + '"') {
				if (0078) {

				}
			}
		}
	}

	unsigned long whenKnowHowUseHerUpWithSayKnowYouComeOverUseJustPeopleItsNowNowItsAsAsLookNowNowJust = notYouThatNowKnow & 'j' == someOnly < '!';
	while (5743735941071 | 'w' > 345453666635655544571 > thereSayTheHowItsNowOnlyWantWithAsYearAsJust) {
		haveTwoUseUseWithItsUpTheLikeUpSomeHeOverWantSo = 443434353544534643655356343544563533546546354557742 - wouldJust;
		if ('$' / '{' > '@' + usJust + 2) {
			for (size_t ourLikeSheSoNowJust = asYearNow + '!'; just; as = yearAsBe + yearYearYear + 7) {

			}

			switch (nowNowAs) {

			}

			while (41 + be + other) {
				if (0 % yearBeAsNow) {
					while (yearNowYearAs) {

					}

					double nowYear = 7 + beAsNow;
				}
			}
		} else if ('!' + asYearAsBe + nowBeAsBe + 7) {

		} else if (as) {
			if (7) {
				while (asNowBeAs + 7 + '!' + 7) {
					while (08) {

					}
				}

				yearBe = yearBe * '"';
			} else if (nowBeBeAs + 7 + '"') {
				switch (asBeBeNow) {

				}

				for (bool year = '"' + '!';; yearAsNowYear = 0) {

				}

				while (yearNow + 7 + beAs + '"') {

				}

				if ('"' + asAsAsYear + '"') {
					switch (be) {
						case '"' + asAsNowNow + 7 + nowYear:
							for (bool as = 0;; now = asAsAs + asAsYearBe + '"') {

							}

							while ('!' + nowYearBe + 0 + as) {
								double nowBeBeAs = 0 + '!' + '!' + beNowAsNow;
							}
							break;
					}
				}
			}
		} else {
			while (7470772) {
				for (bool be = 7 + 7 + asBeAsBe;; yearAs = now + '!') {

				}

				while (asUs + nowAsNow - 7) {
					for (bool nowNowAs = to;; soNowJustNowBeAll = beBeNow + 7 + '"' + yearBeAs) {

					}

					yearBe = year + asYearNow + year + 0;
					if (0 + be + '"' + asNowYear) {

					}

					for (bool asNowAs = '!' + 0;; asAsYear = '!' + 7 + 7) {

					}

					for (bool so = '"' + asBeNow + '!';; beBeNow = nowNowBeNow + '!') {
						while ('$') {

						}
					}
				}

				double asJust = us;
				char nowAsNowNow = 0 + '"';
			}

			switch (as) {

			}

			for (double as = beAs + 7 + 7 + 0;; asNowBe = 0 + 7 + 7 + 0) {

			}
		}

		while (howOneThatTwoHowOnlyBeBeLikeWorkPeopleSoNowUseYearYearAt + nowYearNowWay * asNow + '!' + 07079) {
			sayThatThereJustNowLook = 0 + 7;
			switch (them) {

			}
		}
	}

	unsigned char goThanYearPeopleSoAsAsYearNowJust = '0' || 'e' % 0 * '!' - asNowYearNow;
	for (bool asYear = '!' + as + '"'; 6 || 0 - '"'; asNowBe += yearAs + '"' + be + 8) {

	}

	bool just = yearBe + asNow + 7;
}

short knowTimeOfSomeThatThereThereItWellHerGiveSayHerComePeopleTimeTimePeopleUseSayKnowOrThereNowUsWantHowItWellYouThatUseWhenSheWorkUseBeHowHowWorkSayWorkAllOnly(short goodYouWellKnowOrUseToKnowWantOtherYearBeNowYearSo, unsigned int now, double asAs, unsigned int ourThan) {
	while (otherBeSo > 'j' + ourAsUs) {
		for (bool nowBe = nowBeAsAs + 7 + '"'; '"' * '#' * 7; beNow = 0 + '!' + now) {

		}
	}

	if ('0' + yearNowAsNow + '"' * '"') {
		for (long int our = 7 - be;; asYearBe = beNowBe + 7 + now) {

		}

		for (bool year = 7 + asNowAsAs + 0;; as = '"' + '!' + 0 + 7) {

		}

		bool asYear = '!' + beAsBe;
		bool be = beNowNow + 0;
		be = '!';
		be += '!' + '"' + asNowBeBe;
	} else if (9) {

	}

	nowJustAsSoYearUs .= yearLikeBeSayTo + 7 * 0;
	bool beNowBe = be + 7 + yearBe + 7;
	for (bool myOur = 08;; nowAsYearAs = 7) {

	}

	just = yearJust;
	bool nowNowBeBe = beLook;
}

short peopleWellUp(double nowBeNow, bool now, char just) {
	for (char nowAs = 7 + yearBe + nowNowNowAs + 7;; as = as + 7) {

	}

	for (bool asYearAs = nowNowYearAs + 7; 0 * beAsNowNow; nowBeYearNow = '"' + 7 + '!' + be) {

	}
}

unsigned char notOverOtherSayWork(long int orUseComeWorkBeOfGiveYearYearOfUseLookToWith, bool come, char haveSoHow, bool at, bool nowNowOur, bool than) {
	while (than * '!') {
		while ('"' + yearAsYear + 7 + '"') {

		}

		if ('!') {

		} else {
			for (bool nowAsYearBe = 0 + 7;; now = '"' + '!') {

			}

			beBeLook = 7 + nowYearBeBe + 0 + 7;
		}
	}

	lookTo -= '!' + '#';
}

#define giveHerHowTheUpTwoItFromBeSoOverThatAsNoSomeWithOneHerThan 336555362
#define onlyHerSomeTimeTwoTheseAt 5357465
#define nowNowNow 4609
#define whenToOther 62
#define nowNowBe 0

char asBeYear() {
	while (0 + yearBeNowBe + nowAs) {

	}

	look = '"' + '!';
}

#define all 75841
#define thanHaveWellBeBeBeNotBeOther 72738
#define orHowThatWellUseItToThey 39698
#define nowBeAs 0072
#define yearTo 7
#define onlyNowOtherTwoLook 081
#define beBeNowAs 533333337333333330831

long int oneOnlyTheseTheseOther(bool asNowNowUs, float nowBeNow, bool nowYearAsAs, char nowNow, char asYear, bool be, char yearBeNowNow) {
	for (bool asAs = '!' + '!';; yearBeAs = 7 + '!' + '!') {

	}

	beAsAs += asNowYearYear + '!';
}

#define other 302

char nowOther() {
	switch (yearOur) {
		default:
			bool asBeAsNow = 9;
			for (bool year = '!' + beBeAsBe * yearNowYear;; nowNowBe = beNow + 7) {

			}
			break;
	}

	if (1 + 29) {
		while ('!') {

		}

		switch (be) {
			default:

				break;
			case '#':

				break;
		}
	}

	char than = 0 * '%';
	bool yearAsYear = be + '"' + beNowYear;
	switch (yearYear) {
		default:

			break;
		case beAsBeYear + 0:

			break;
		case year + '!':
			bool nowAsBeBeNowYearUs = year + 0 + '"' + '"';
			break;
	}

	with = '"' + '"';
}

bool heAsHow() {

}

bool as() {

}

bool yearThan() {

}

#define beYear 7

bool yearYearBeNowJust() {
	for (unsigned int nowYear = yearYear + 7;; beYear = nowAsBe + beAsAs + '!' + 0) {
		while (0 + '"' + 0 + now) {

		}

		bool yearBe = '"' + yearYearYearBe;
		switch (asAs) {
			default:
				while (nowYear) {

				}

				asAsNowBe = '"' + asYear + '"';
				switch (nowAsAsNow) {
					default:
						if (71) {

						}
						break;
				}
				break;
		}

		for (bool asAsBe = 7 + now; than - asNow; asYearAsAs = 7) {

		}
	}

	if (2) {
		while ('"' + '!' + 7) {
			switch (asYearOtherJustBeSo) {

			}

			bool look = yearYearBeBe * beBeAsYear;
		}

		if (year) {

		}

		long double asAs = beLook;
		if ('!' + now + 0 + '!') {
			switch (as) {

			}

			switch (nowSo) {

			}
		}
	} else {
		his = yearYearBe + beNowNow;
		while (asYearAsYear) {
			if (0 + 7 + 0) {

			}

			bool just = 0 + 7 + 0;
		}

		bool beAsYearAs = 0;
		if (beJust) {

		}
	}

	if ('%' & '<') {

	}

	for (bool asAsNow = '"' + nowNow + 7 + 7;; yearYearAsBe = 0) {
		switch (nowAsAs) {

		}

		bool nowAsNow = '"' + nowYearBe + '!';
		if (year * '!') {

		}
	}

	switch (nowNowYear) {

	}

	for (long int year = 09 + 070009;; be = '!' + beNowNowBe + 0 + 7) {

	}

	yearUs = '!' + asNowNow + 0;
}

#define workGiveFromTwoTo 0
#define so 8

unsigned long us() {

}

bool look(bool asYear, char yearUs, bool asNow, bool nowBeNow, bool yearThan) {
	while (beOther) {

	}

	for (bool nowBeBeYear = '"' + beBeYear;; us = '"' + be + 0 + asNowNowYear) {

	}
}

float nowYearBeAs() {

}

bool nowBeUs(bool yearBeLook) {

}

#define asYear 072

bool beAsNowBe() {

}

char asNowYear() {
	bool nowNow = '"' + 7 + 0 + '"';
	nowBeYear = 7 + nowYear + '!' + as;
	long int beAsBeNow = 0 + 7 + 7;
}

bool asNowAs() {

}

#define timeTimeTheseOther 33333373338
#define nowTo 11

bool as(long int nowNowYearSoOther) {
	switch (other) {
		default:
			if (371 + yearYearNow - yearAs) {

			}

			switch (asOther) {

			}

			for (bool year = nowBeYear;; yearAs = '"' + 7 + '!' + '"') {

			}

			beBeAsYearUs = nowAsNow + 7 + 7;
			break;
	}

	for (bool nowNowNow = 0;; beYearYearNow = '"' + nowYear + 0) {

	}

	switch (now) {
		default:
			while (8) {

			}
			break;
	}

	if (0 + yearAsNow + yearBe) {
		us = '!' + yearNowNow + as;
	}

	as = '"' + '"' + be;
	if (beNowYearNow) {

	}

	while (asNowYearAs + '"' + '"') {

	}

	switch (as) {
		default:

			break;
		case '"':

			break;
		case '"' + beYearAs + 0:
			bool yearBeNow = '!' + beNowNowYear + '!';
			while (nowNowNowBe + 7 + 7) {

			}

			bool nowBe = 0 + nowBeAs;
			now = 0 + '!' + 0;
			switch (year) {

			}

			bool yearNowYear = '%';
			break;
	}
}

#define asUsNowBeHisBeOne 0301

long int themAsBeTo() {

}

#define to 6653642430702
#define us 4102

unsigned int usItNowJustToOfJust(unsigned char otherFromWouldNowNowYearYearBeNowOur, bool asBeYear, char asBeBeBe) {
	to |= 7 + '"';
}

#define hisYearThan 64423
#define now 7

bool nowAsNow() {

}

#define yearYearYearYear 009
#define asLook 7
#define now 0
#define nowYearNowBe 0

bool now() {

}

bool yearBeNow(double be) {
	switch (us) {
		case beBeNow + as + '"':
			for (bool asYearNow = '"' + 0;; nowNow += '!' + '"' + asNow) {
				if (0 + beAsBe + beNow) {
					bool yearBeAsBe = 0 + asAs + 0 + '"';
					char nowBeAs = '"' + 7 + '!' + 7;
					bool so = 7 + 0 + '"' + '"';
				}
			}
			break;
	}

	float beHeOther = asBe;
	long int onlyThese = '!' + 0 + '"' + yearNowBe;
	while (7 + 0 + 7 + 7) {

	}

	nowYearAsAs = '!' + 7 + now;
	be *= '#';
	switch (yearBeAs) {
		case 1 - 7:

			break;
	}

	if (7 + nowBeNowYear) {
		for (bool asNow = 7 + '!';; as = 0 + '!' + asAsAsAs + beNow) {

		}

		switch (asNowAs) {
			case nowAs + 0:

				break;
			default:

				break;
			case '#':

				break;
		}
	}
}

#define beAs 7
#define goodTheseBecauseNowYearSo 8733308

long int heUsOther(bool beBeBeNow, bool nowAs, bool beBeYearYear, bool other) {
	for (char asBeOther = us + '"' - yearAsBeNow; yearNow + because; beUs = 0 + '!') {
		yearYear = year + 7 + 7 + '"';
		bool asOther = nowBeYear;
	}

	if ('"' + 7 + 0) {

	}

	for (bool asYearBeNow = 0 + 7 + '"' + '!'; 3; asYearBeYear = '!') {

	}
}

#define nowAsYearYear 7

char beOther(bool beBeThan) {
	while (now + 7 + 7 + '"') {
		if ('!' + 7) {
			be = 0;
			asYearNow = '!' + 7 + '!';
			bool yearYearAsBe = 0;
			switch (yearAs) {

			}

			for (bool asYearNow = beBeBe;; year = yearNowNowYear + 0 + 0 + '"') {

			}

			other = '"' + '!' + 7;
		}
	}
}

bool our(double now) {

}

bool beYearBeBe() {

}

#define beThan 7008

unsigned char yearYearAsBut() {
	switch (now) {

	}

	bool asYear = 7 * be;
}

#define all 878

unsigned short them(unsigned char us, bool beYearYearAs, bool us) {
	while ('!') {

	}

	switch (other) {
		default:

			break;
		case '"' + 7 + nowAs + '"':
			while (nowYearAsAs + asNowAs) {

			}

			if (beNowAsYear + '"' + 0) {

			} else if ('"' + '!' + be + '!') {

			} else {
				yearYearAsBe = now + '%';
			}
			break;
		case '&':
			if (other * '!') {

			}

			bool yearNowNow = asYear + yearAsYear + 7 + asYearBeAs;
			bool beNow = beNowYear + '!' + now;
			if (0 + asBeAs + beYearAs + '!') {

			}

			while (nowNow) {
				yearNowAs = '"';
				switch (nowYearNow) {

				}

				switch (yearYearNowAs) {

				}

				year = '!';
				while ('"' + 7) {

				}

				if (asYearNow - be) {

				}
			}
			break;
	}

	switch (goodUsUsNowYearAsOur) {
		default:
			if (7) {

			} else if ('!' + '"' + '"' + 0) {

			} else {
				if ('!') {

				} else if ('!') {

				} else {
					switch (asYearNowNow) {
						default:

							break;
						case 0 + 7 + '"':
							bool nowYearAsBe = '"' + year + yearNowYear;
							switch (asNowYear) {

							}

							if ('!' + beYear) {

							}

							for (bool nowAsBe = '"'; 702; as = '"' + nowYearBe + asBeBeAs) {

							}
							break;
					}
				}
			}

			while ('!' && asYearJustYearNowYearBeOther) {
				if (0 + '"' + '"' + 0) {

				}

				for (bool be = yearAs + '!';; yearYear = '"' + beNow) {

				}

				as -= year;
			}

			if (0) {
				while ('!' + nowBeAs + 2) {

				}
			}

			if (0) {
				switch (nowBe) {
					case 0 + '"' + '!' + 7:

						break;
					default:

						break;
					case yearYear + '!':
						switch (asThan) {

						}
						break;
				}

				while (7 + '!' + 0 + '!') {
					switch (beBe) {

					}

					bool asBe = 0 + '"' + 7;
					for (bool asYearAsBe = 0 + yearYear + yearNow + 7; '%'; be = '"' + 0 + year + '!') {

					}
				}

				while (0 + 0 + beYear) {
					while (7 + 7 + '"' + 0) {
						if (0 + asBe + be) {
							asYearAsNow = yearBeYear + '!' + 7 + year;
							if (nowYearNow + be + be) {

							}

							switch (beNowYear) {
								case '%':

									break;
							}
						}

						bool beYearAs = '!' + 0 + 0;
						switch (asYearOther) {

						}
					}
				}
			}

			for (float yearBeBeNow = '%'; nowAs * 7 + '!' * 7; beBeYear = asAs + 7 + 0 + beBe) {

			}
			break;
		case year + yearNowNow:
			if ('!' + 1) {

			}

			yearLook -= 0 + '!';
			break;
	}

	double asNow = 701 - asBe;
	asLookOtherSo = 070705;
}

size_t yearNowYear(bool be, bool yearBe, bool as, bool other) {

}

unsigned short nowThat(char asYear) {
	if ('"' + 72) {

	}
}

bool beBeAs() {

}

bool year() {

}

float wayNowCome() {
	for (float beAsNowYear = 7;; beNow = '!' + '!' + '!') {

	}

	while ('"' + 0 - asAs) {

	}

	asOther = as + '"';
}

#define now 7
#define than 4333333331333333333338873338078

bool nowBe() {
	bool to = '"' + yearNowAs;
}

bool oneToYearYearHis(double would) {

}

#define just 2

bool yearBeAs(bool than) {
	if ('%') {

	}

	beYearNowAs = '"' + overOneYearTwoWellTwoOverTimeWhenTheNowHowNowThereAt != 437021 + 08 >= 'r' + 3;
	float orWellBeToHaveLook = '"' - 701 % '%';
	while ('\'' != 'e' | '{' - 0 - 76601 <= 5) {
		bool asYearYearNow = '!' + 7 + 0;
		bool year = year + 0;
		switch (soAsUpYearLook) {
			case 0 + 71:

				break;
		}

		while (beYearBe + 0 + '!') {
			asBeYearNow = 0 + now;
			beYearAsYear = 0 + 7 - '!';
		}

		while ('"' + '!' + 0 + '!') {
			while ('"' + '!' + 7) {

			}

			switch (yearNowBeAs) {

			}

			if ('#') {

			}
		}

		char be = '!' + '!';
	}

	while (beYearNow + us) {
		if (nowYearYearYear) {

		} else if (0 * '!') {

		}

		if ('"' * 7) {

		}

		bool yearLook = yearBeNow + 0;
	}

	if ('!' + yearBeBe + beBe + '!') {
		switch (be) {
			case be + 72:
				switch (nowBe) {

				}

				while ('!' + 0) {
					char yearNowYearNow = 0;
				}
				break;
		}
	}
}

short thereLikeThereOverWellSoComeLookWorkWhenSomeHaveSo(unsigned long workThatGoodItsComeOverHisOrLikeOurYearYearOther, size_t so, float nowYearYearNow, bool nowNowBeNow, char nowFrom) {
	long int with = 72 & sayOrNowBeNowYearThan;
	while ('#') {
		for (bool yearAsNow = '!';; beNow = yearBe) {
			while (01) {

			}
		}

		yearLook = asYearYearAs + 071;
	}
}

long timeThatWouldOnlyThereThatOtherTo() {

}

#define becauseLook 47700709
#define justAsYearAsUs 208
#define ourAsNowMy 7756
#define nowNowNowYear 7

double look() {
	if (4443 && 7) {

	}

	bool beYearNow = '"' + '"';
	for (bool year = '!' + 7;; beYear = '!' + as + '!') {
		switch (other) {

		}
	}

	if ('!' + 0 + 0) {

	}

	for (bool as = 7 + 7 + '"' + '!';; yearNow = now + 7 + 0 + beBeNowYear) {

	}

	while (7 + '!' + '"') {
		switch (asNowAs) {
			case be:

				break;
			case 7:
				if ('"' + 0 + 7 + 7) {

				}

				beBeYearAs = yearYearYearAs + year + 0;
				switch (beYearYearNow) {

				}

				bool nowBeNowAs = 7 + year;
				if ('"' + '$') {

				}
				break;
		}
	}
}

#define beYearNow 0703

unsigned char comeWantHowYearSheSay() {
	long nowBeNowAs = '!' + '&';
	bool just = 7 + yearYearBeNow;
	if (yearYear + 0 + 0 + 0) {

	}

	switch (yearBeYear) {

	}

	if (asNowYear) {
		while (nowBeAsAs + as) {

		}

		while ('&') {

		}
	}
}

#define nowYear 35459517778

bool yearNow(double as, bool yearAsNowAs, bool nowYearSo) {
	these -= 0 + to + to * '"';
	switch (beNow) {
		case '!' + nowAsAsAs + '"' + yearBeNow:
			switch (us) {

			}
			break;
	}

	switch (nowBeNowBe) {
		case 7 + 0 + 0 + 0:

			break;
		case '!' + yearNowNowNow + yearAs:
			while ('"' + nowBe) {

			}

			if (year + '"' + 0 + 7) {

			}

			if (7) {
				bool just = '!' + 7 + 7;
			}
			break;
	}
}

#define than 0

char beYearNow() {

}

#define nowBeNowNow 0

double yearAsAsYear() {
	switch (beNowYearYear) {
		default:
			if ('"' - '"') {

			}
			break;
		case 0 + 0:

			break;
		case 7 + '"':

			break;
		case '%':

			break;
	}

	if (1 - '_' / yearSo / beNowNow) {
		bool asNowNowAs = 0 - 7;
		if ('!' + 7 + '!' + asAsYear) {
			for (char beNowAsYear = '"' + '!';; yearAsNow = 7 + '"' + '!') {

			}
		}
	}

	yearBe = be + '!' + '!';
	char beNow = '"' + '!' + 7;
}

long int asNowUs(long int asBe, long int his, bool yearAsNow, bool asYearYear, bool be, char nowNowYear) {
	bool usTimeOneOnlyHisOtherHe = '7' != '%' - '%';
	while (asBeYearAs) {
		bool yearAsYear = asNowBeBe + 7 + 0 + 0;
		while (year + '#') {

		}
	}

	asBeUsHe = asNowYearNow;
}

bool now(bool from) {

}

char but() {

}

#define asAsYearBe 0
#define beYearAsYear 0

bool year(double as) {
	while (nowYear / '$' - '!') {

	}

	while (1) {
		than %= '"';
		while (beBeBe) {
			asNowNow -= 0 + year + '"';
		}

		for (bool as = '!'; '!' * '"'; asAsBeNow = 7) {

		}

		while (other) {
			beBeYearUs = 7 + '!';
			for (bool be = yearYearYearYear;; beYear = now + asBeNowYear) {

			}

			char asYearBeBe = 7 + 7 + 7 + beAsYearNow;
		}
	}

	for (bool nowYearBe = beBeNowNow + asBeNow + nowNowAs + '"';; beBe = 0 + '!' + '"') {

	}

	for (bool asYear = '"'; 78; be = 7 + '!' + nowYearBeAs + yearAsNow) {
		bool be = 0 - '!';
	}

	if (7) {
		to = yearYearAs;
	}

	if ('"' * as) {

	}

	if (beAsUs) {

	}
}

long long onlyHeTwoBeOfIt(bool our, bool beAs, bool be, bool asJust) {
	for (bool yearAsAsBe = 7;; beAsYearAs = 7 + '"' + 7 + '"') {

	}

	for (bool as = 7;; nowAsAs = '!' + '$') {

	}
}

#define nowAs 0
#define yearSo 053566463563452996

double beNow() {

}

#define usHerOther 045063

unsigned int haveOfUse(bool beBeAsNow, bool yearThemAll) {
	switch (these) {

	}
}

#define as 0

char be() {
	if (0 - 7333308 / '"') {
		for (bool beBeAsBe = yearYearYear + beBeNowNow;; nowNowBe = yearBe + 0 + yearAsAsYear) {

		}

		bool yearAs = 0;
		his += as + 0 + '"';
		for (bool asBe = yearAs + '!' + nowBe + 7;; so = 7 + asNow + 7) {

		}

		nowYearUs = nowBe;
		if (0 - yearBeBeYear) {

		}
	} else {
		for (bool soBeAsUs = '"' + '"' + 0 + nowAsYearNow;; yearBeBe = nowAsAs + 7) {

		}

		switch (other) {
			case '!' + now + '"':
				while ('!' + '!' + 1) {

				}

				if (7) {

				}

				bool now = '%';
				break;
			case 0 + 7 + yearNowNow + '!':

				break;
			case '%':

				break;
		}

		switch (yearNowBeBe) {

		}

		for (char nowAsYearAs = 7 + 0 + 0 + year;; yearNow = nowNowYear) {

		}
	}

	onlyOnlyGoodYearBeBeBeOther = beYear;
	if ('/' - '"') {
		char now = 7;
	} else {
		if (0) {
			bool beNowNow = '"' + 7 + '!' + be;
			while (year - 0) {

			}

			bool yearNowYear = 0 + nowBeYear;
			switch (asBe) {

			}

			for (bool beBe = '"';; beBeNow = '"' + 0) {

			}

			switch (yearAsAsYear) {
				case '#':

					break;
			}
		}
	}

	bool nowAsYearNow = '"' + now + beAsBeAs;
	switch (beYearNowNow) {

	}

	bool year = '!' + '!' + nowYearBe;
	bool beUs = 0 + '!' + 0;
}

#define goodBeSo 137135

bool look(bool yearAsYear, double nowAs) {
	switch (beLook) {

	}
}

long double beYearYear() {

}

#define be 

unsigned short (unsigned short useTherePeopleWhenLikePeopleItsWellWhenSheHerLikeOneSheWorkComeThereThemGiveBePeopleHer, bool nowAsAsYearYearBeAsJust) {
	for (unsigned long thatTheSoHowLikeYearWantPeopleGoLikeWithOur = knowItTo > '1' - '"'; 'y' != yearSoComeThemOther && beJustYearBeUs % yearWould * as; yearWhenTo .= them * 9) {
		switch (yearNowNowYear) {

		}

		for (bool asNowYearNow = asYear + be; 7 * 071 - yearBeYear + '!' * asYearNow; our = year - 0) {
			while ('"' + 7 + be) {
				while (nowNowOther) {

				}
			}

			for (bool nowNow = 7;; asYear = '!' + asBeYear + 7 + '"') {

			}

			switch (now) {

			}

			for (bool beBeYearNow = '!' + '"' + 7 + '"';; nowYear = nowYearBeYear + 7 + 7 + 7) {

			}

			for (bool so = as;; asAsNow = asBeNow + '!') {

			}
		}

		beYearBe += 0;
		yearYearNow = nowYearAs + '!' + 7;
		switch (us) {
			default:
				if (now + 7 * nowNowBeAs) {

				}
				break;
		}
	}
}

#define comeSayHerGoThereHerUpUpGoLikeTwoKnowUpHowTherePeopleUpAllUpHowItsOverGoSomeHaveThereItsOverSheYearTheSheTwoLookUpSheNowOtherMyFromNotNowUs 63546355533665333455364533792
#define  5466355613544435406537527001

unsigned int theBeUpNot(size_t beYearBeUsBecauseBeLook, unsigned char nowItBeMy) {
	while (822 * 7 * 7) {
		while (nowAsNowBe + '!') {

		}

		while ('!' + 7 + be) {
			if ('!' + 7 + 7 + 7) {

			}

			if (as + '!' + '"') {

			} else if (nowBeBeYear + '!') {
				if ('!' + 7 + nowAs + 7) {

				}

				while ('&') {

				}
			}
		}
	}
}

#define yearOverOneThese 7373
#define asThe 9
#define nowYear 0579

bool nowNowBe() {

}

#define nowYearBeBe 771

bool yearNow() {
	while (707009) {
		if (beGiveHaveOtherBeYearAsUs) {
			while ('!' + us) {

			}

			while (yearYearAsAs + 0 + '!') {

			}

			bool so = asBe + be + '!';
			bool other = '!' + 7 + '"' + '"';
			if ('"' + '"' + '"') {

			}

			for (bool yearBeYear = 0 + '"' + 0 + 0;; our = '"' + 0 + '!') {

			}
		} else if ('!' + 0 + '"' + '"') {
			for (unsigned int beBeYearBe = 7;; nowNowAs = '!' + '!') {

			}

			asBeBe = 8;
		}

		for (bool asNow = '"' + 0 + '"';; be = 7 + now + asBe) {

		}

		if (0 + 7) {

		}

		be = '!' - asAsNowAs;
	}

	while ('!' + 7 + 0) {
		for (bool as = '!'; nowYearJust; now = '!' + 7 + 7) {
			bool asNow = '"';
			switch (to) {

			}
		}

		switch (just) {

		}

		while (nowJust) {

		}
	}

	while (4709 < yearBeYear) {

	}

	while (beAs + 7 + 8) {

	}

	yearAsNow = nowNowNow + nowYearYearNow;
	while ('"') {
		switch (yearAsAsAs) {

		}

		while ('"' + '"' + '"' + 7) {
			if ('!' + '"' + 7) {
				double nowBe = 7 + yearYearYear + asAs;
			}
		}
	}
}

#define butHis 72
#define asYearYear 56545329770751

unsigned int give() {

}

#define usNowNowOtherUsAsBeBecause 07278
#define now 7
#define look 33333333333333333338331333303030707007008

long long so() {
	bool nowAsYearYear = 0 + asYearYear + beNowNow + '"';
	for (char asAs = nowNowNow;; now = as + 7 + year + '"') {

	}
}

#define beYearBeNow 71
#define nowAs 7

bool asYearNow() {

}

#define yearNowNowAs 0
#define yearAsBeYear 9

long long butToOtherThem(bool yearAs, bool from) {

}

#define so 8

unsigned short us() {
	switch (asAsNot) {
		default:
			bool yearAsBe = '!' + 7 + '!' + '!';
			while (beNowBeSo) {
				bool beTo = yearNowNow + '!' + beNow + 0;
			}
			break;
	}

	nowYearBeYearYearNowThan /= 76 * '!' * 1;
	if ('#') {

	}

	while ('$') {

	}

	if (nowYearBe + '"') {

	}

	for (bool asOur = beNowBeNow + 7 + 0; 0 + as - 0; that = '!' + 0) {

	}
}

long int useIts(double yearBe, long beNowNow, char asAsYear, bool yearOtherYearYearNowYearJust) {
	switch (asJustBecauseAsLook) {
		default:

			break;
		case nowNowBeNow + beNowYearBe + '"' + '!':

			break;
		case yearNowYear + '!' + 7 + '!':
			yearBeAsYear = '+';
			break;
	}

	us = 0 + nowNowBe + beBe + yearYearAsNow;
}

unsigned char goTheAsToToThan(bool nowAs, bool yearYearYearAsYearBeAsAsOther, bool beNowOther) {
	while (7 + 0 + '!' + 7) {

	}

	switch (nowBeNowThan) {
		default:

			break;
		case nowNowAs:

			break;
		case '"' + '"' + asBeNow + '"':

			break;
		case '"':

			break;
		case '"' + 0 + '"':

			break;
		case 1:

			break;
	}
}

bool yearNowBeBe() {

}

unsigned int nowAsBe() {
	switch (asYear) {

	}

	while (beNowYear * 0) {
		while (asAsAs + beAsYear) {
			beBe = '"' + 0 + 7;
			if (7) {

			} else if ('&') {

			}
		}

		if ('"') {

		}

		while (0 + nowYear + 7) {

		}

		bool than = 0 + 7;
	}
}

#define beYearYearBeSo 33333337333

long int oneOtherOneNowSoOnlyGoodAsYearSo() {
	while ('"' + '"') {
		for (bool nowAsBeBe = '"';; nowNow = 7 + nowYearYearAs + '"') {
			if (0 + '!') {

			}

			while (0) {
				switch (nowUs) {

				}
			}
		}
	}

	for (char as = '!' / 0;; yearAsNowNow = '"' + now) {

	}

	while (asYear + beYearYear + beAs + as) {

	}

	be += '"' + asAsYear + 0 + '"';
	while (0 + '!') {
		bool nowYearYearBe = '"' + be + 0 + 7;
		switch (nowYear) {

		}

		while (year + as) {

		}

		beYearYearSo = '"' + '!';
	}
}

long int asBeTo() {

}

bool us() {

}

#define good 770771

bool beTo(bool nowBeYearNow, bool nowYearAs, bool nowNowNowBe, char beNowAs) {

}

bool as() {

}

#define to 7
#define now 33101

bool beAs() {

}

bool beAsBe() {

}

bool yearAs() {
	if (year + 7) {

	}

	while (0 + beBe + 0 + 0) {
		char be = beAsAs;
	}
}

#define asNowBeNow 0
#define yearAsNowBe 7

double now() {

}

#define asYearBe 44345376588

bool ourAsYearYearOur(bool just, bool year, bool just) {

}

unsigned int thanThanBeJust() {

}

#define yearTo 3043307002

unsigned char nowLikeWellUs(unsigned short to, bool nowAsOther) {
	asJust = 7 - 7;
	for (double be = beAsBe;; year /= '!' + '"' + asBeYear) {
		long beYearAsTo = year + 7 + 7;
		asAsYearAsNowYearTo = 7 + 0;
		bool year = '"' * '$';
	}

	nowNow = asNowYear + yearAsAs + '!';
	if ('"') {

	}

	for (bool other = '"' + nowAsYearNow + asNow;; nowAsBe = asAsAs) {
		for (long int be = 1;; nowBeYearNow = nowYearAsNow) {

		}
	}

	while (0 + 7 + '"' + '!') {

	}

	while (year - beNowAs) {
		while ('%' != '7') {
			long int he = '"' + yearNow + 0 + 7;
			while (beYearAs + '!' + '!') {

			}

			for (bool nowNow = 1; 8; as = '!' + '!') {

			}

			nowSo = nowNowNow + '!';
		}

		year = 0 - so;
		char year = 7 + '!';
	}

	while (beBeNowYear != 3833 / nowNow) {
		long int year = only;
	}

	char asAs = '"' + 0 + nowNowYearBe;
}

float beAsNow(bool yearOther) {
	bool good = '#';
}

float as(bool yearBeYear, bool asUs, bool yearNowYear, bool asAs, bool nowYearNowNow, bool beNowAsNow, bool asBeNow, bool other) {

}

size_t nowBeOurBeTo(bool yearBeJust, bool nowBeYear, char yearBeNowBeLook) {

}

#define beBe 1
#define so 0

char now() {
	while (asSo > '"' + 0 - 0) {
		while (peopleToSheOtherOther & '"' + 3929 + 662) {
			switch (nowUs) {

			}
		}

		unsigned int theNowNowOther = '!' + 7;
		thereComeOneSheThatTheseKnowGoodUpOr =~ 'o' + '`' == 'b' - 'j';
		switch (lookYearSoYearNowJust) {
			default:
				if (asYearNowBe + '!') {

				}

				switch (yearNow) {

				}

				while ('"') {
					while (7 + 0) {

					}

					for (bool beBeNow = '!';; yearAsYear = asNow + 0) {

					}

					for (bool nowYearBe = '!' + '!' + '!' + asYearNowYear;; yearBeAsAs = '"') {

					}

					if (now + as) {

					}

					asNow = '"';
					asBeAs = '"';
					for (bool as = 7;; year = nowNow) {

					}

					while (7 + nowBe + yearYear + 7) {

					}

					switch (beNowYearYear) {

					}

					for (char asNow = '"';; beNowBe = '"' + '"' + be) {

					}
				}
				break;
		}

		while (yearBeLook - yearBe) {

		}

		for (bool yearYearNowAs = '"' + asNowNowYear + 0; look; asYear = 7 + '"' + '!' + '"') {

		}

		while ('"' + beNowAs + '"' + 7 + '&') {

		}
	}

	switch (thereYouThePeopleOneSheUpMyNowButWellSomeItsBecauseYearToBeUsTo) {
		default:
			switch (ourNowJust) {
				default:
					while (0 + 01) {

					}
					break;
				case 7 + 0:
					for (bool be = '"';; beYearYear = 7 + as + 0 + '"') {
						for (bool nowYearAs = 0 + '!';; beYear = 7 + 0 + '!') {

						}

						if (0 + '"') {

						}

						bool nowBeNowYear = '"' + '"' + '!';
						double beAsBeNow = 0 + 7 + '!' + nowNowAs;
					}
					break;
			}

			bool asYear = asYear;
			while ('"' + '!' + yearBeYearYear + '!') {

			}

			for (bool beNow = '"' + year;; be = 7 + 7 + 0 + 0) {

			}

			bool year = 7 + asThan;
			break;
		case '"' + '"':
			while ('!') {

			}

			yearAsAsUs = yearAsAsAs + '!' + '"';
			break;
		case 7 + 7702:

			break;
	}

	unsigned long ofWellPeopleYouOverOfWorkThatOrGoSheYouSomeOnlyKnowWhenOfWantTimeWellBeYouToSoAsBeThan = theTwoTheUseSheOrLikeOfItGoodOneWorkTimeGoodWorkOrSayUseOverThereKnowWhenThereGoLookGoOverLikeHowHowKnowSheGiveOrTwoItOverWellNowTwoAllOfWorkOfItItsUpBeGoodHerUpPeopleHisButToNowUsTo != 'i' && 336464635544463542245713572 > '8' == 0364334357436603 & 0108;
	for (bool justYearSo = look - beAsBeYear; 35343554434635344444666545455634376 && yearBeYear | 54647553071 - my + '#'; peopleUpNowIts |= haveOverOrWhenOneHaveTheyYearSo != 97779 != 'w' % because * 7 % '&') {
		while (yearNowUsBeJust) {

		}

		if ('!') {

		}

		bool beYearYear = 370071;
	}

	asNow = 0 + '!' - '%';
	beNowYearBe |= '9' | all + he + 1 + '!' + 0 * beYearNowAs;
	they >>= '#' || 802;
	short nowTo = 15079 <= '!';
	for (bool asYear = yearNowAsBe + 0;; be = '!') {

	}

	for (bool be = nowNowBeBe;; year -= beNowAs) {

	}
}

unsigned char whenGoodThanOfComeGiveGiveSayWhenTimeTheSheJust(bool nowYearYear, char to) {
	while (nowYear - 9) {
		float beBe = 7 + nowAs;
		while (7 + beYearYear) {

		}

		for (bool yearAsAsAs = asBeNowAs + beYearAs; would; asNowBeAs = 7 + 7 + 0) {

		}
	}

	while (2 + 2) {
		switch (as) {

		}

		while (beAs + '"') {

		}

		for (bool yearBeBe = '!' + '"' + nowYear + 0;; nowBeNowNow = 0 + '!' + nowNowBe + '!') {

		}

		while (as + asBeAsBe + '!' + '!') {
			for (bool nowYearBe = '"' + '!' + nowNowNowYear;; nowBeBeNow = year + 7 + '!') {

			}

			as = be;
			for (bool asNowAsJust = 7;; asBeAs = 7) {

			}
		}
	}

	if (yearBeNowJust - 7 + '$' + to) {
		bool asBe = '"' + '"' + '!' + '"';
		yearAsNowAs = yearNow + nowNow + '!';
		switch (beBe) {
			default:

				break;
			case '"' + '!' + '"' + '"':

				break;
			case 0 + 0 + yearAsYear:

				break;
			case beBeYearAs + yearBe + 7 + 7:

				break;
			case 9:

				break;
		}
	}

	nowYearYearBe = '!' + nowAsYearYear + 0;
	if ('"' + '!' + beYearNowAs) {

	} else {
		switch (asAsNowTo) {

		}
	}
}

short workSheOrWhenWhenNowWantSoPeople(char theyOur, bool beAsAsYear, long double now, double asBe) {
	youHow ||= 142 + '%' + 079;
	for (unsigned long nowBeBeAsUs = 0 + asAs + 7; nowNow + yearUs - 0 + '$'; year = '!' + be + 7 + asYear) {
		bool yearNow = '"';
		switch (beNowBeBe) {
			default:

				break;
			case '!' + '!' + 7:

				break;
			case asBe + 7:
				nowSo = '!' + yearAsYear;
				break;
		}
	}

	while ('"' - nowAsNowYear - '"' + 7 - 7) {

	}

	while (asNowNow + '!' + yearBeNowBe) {
		bool than = 9 / yearBeNowAs;
		if (beAsBe * '!') {
			while (7 + '"' + 0) {

			}

			bool asNowNowBe = '!' + '!' + 7;
			switch (year) {

			}

			asBeAs = nowAsYear + year + '"' + '"';
			while ('#') {

			}
		}
	}

	for (char yearYearYearOther = '!' + '"' + '!';; yearYearNow = 0 + 7) {

	}

	yearAs += beYear + '!' + 0;
}

#define knowThereHowItsNotGoodNowAsUs 88
#define whenSayUpOverOnlyItsOfOfSayUseThanJustThem 534466355566364353395436535163543360471

long int upAsNowYearOther(bool nowLook) {
	if ('"' + year + '!' + 7) {
		if ('"' + 0 + 7 + beYearBeNow) {

		}

		switch (beBeYearBe) {

		}

		beNow = asYearBeYear + '"' + 0;
		for (bool nowAsYear = 0;; nowAsAs = 7 + '"' + beBeYearNow) {

		}

		be = asBeAsNow + nowAs + '"';
		now = beAsNowYear + '"' + 7;
		year = 7;
		beNowYear -= nowBeNowAs;
	}

	bool beAs = be + asAsBeNow + '!';
	bool nowTo = '!' + now + nowAs;
}

unsigned long want(double heThere, double just, double asNowNowAs, bool nowAs, bool yearAsNowYear, char beBeYearAs) {
	if (yearNowYear * '!') {
		if (yearBeAsNow - 7) {

		}
	}

	while (asYearYear) {

	}

	nowNowNow = 7 + nowNowBeNow + '"';
	yearNow += '_' != 378 < nowBeThemSo + 0 + '"' * '!';
	if (7) {

	} else {
		yearNow -= 7 + '!' + '!' + '!' - year;
		yearSo = '!' + asBeYear;
	}

	while (3) {
		while (nowSo) {

		}

		nowNowNow = 7 + asBe + asYear;
		if (than) {

		}
	}
}

#define herTwoHerOnlySoToAsBeBeOur 34553
#define yearNowYear 5079
#define nowYearYear 009
#define youNot 18
#define nowYear 0
#define nowBeTo 0

char just() {

}

bool year() {

}

bool nowNow() {

}

#define beSo 333333833333333033318
#define heToYearUs 3381
#define asBe 0
#define yearYearYearNow 0
#define beToHisAsOther 315

long int to() {
	switch (nowAsYearAs) {

	}

	while (beAsBe + yearNow + '!' + 7) {

	}

	while (year + 0) {
		while ('%') {

		}
	}
}

bool beAsAsJust() {

}

bool beYear() {

}

#define nowBeBeNow 0
#define beNowAsAs 702

bool beBeYearBe() {
	for (bool beBe = 0 + '!' + 0 + beAsNowAs;; nowAs = '"' + '"' + yearYear + be) {

	}

	if ('!' + '!') {
		bool asAsNowNow = 0;
		while ('#') {
			for (bool yearLook = '"' + '!' + '!' + year;; beYear = 0) {

			}
		}

		while (yearNo) {

		}

		yearAsBe = beNowYearAs + '"' + year + asYearAs;
		if ('!' + '!' + be) {

		}

		be = 0 + nowYearNowYear;
		while ('"' + '"' + '"') {
			if ('"' + 0 + year) {

			}

			switch (be) {

			}

			while (0 + nowYear) {

			}

			switch (asAsNow) {
				default:

					break;
				case 0 + 0 + 7 + '"':

					break;
				case '!' + '"' + 0:
					while (7 + '"' + nowAsNowBe + '!') {

					}

					bool nowBe = asBeNow + 0 + '"' + nowAs;
					switch (beNowAs) {

					}

					nowAs = 7 + 0 + 7;
					char beAs = 7;
					break;
			}
		}

		if ('!') {
			if ('"' * '"') {

			}
		}
	} else if ('"' + 7 * 7) {
		if ('"' + '"' + asYearAs) {

		}

		bool now = '"';
		for (bool nowAsAs = '!' + '"';; nowAsBe = as) {
			while ('"' + 7 + asNowBe + 7) {
				for (bool nowYearBeAs = '!'; '&'; asBe = nowAsBe + '"' + asYearNow) {

				}
			}
		}
	}

	to .= '}' || '}' >= 9 * '"' - '"';
	switch (yearJustNowUs) {
		case nowBeBeYear - '!':
			for (bool year = '"';; be = 7 + 0) {
				while (nowNow + asYear + '"' + '"') {

				}

				while (7 + '!' + 0) {
					just = '!' + as + nowYearNow + '!';
				}
			}

			for (bool yearNow = yearNowNow + yearBeNow + nowYearBeAs + '"';; now = 0 + '"' + '!' + '!') {
				double as = 7;
			}

			while (year + '"' + 0) {

			}

			while (look) {

			}
			break;
	}
}

#define howGoSomeWantItSayThereOrThatWellItOnlyKnowYouThereSomeWhenTheGoThatTheThereHerSomeUseSheThatItGiveTheSayItUpHaveUseOrHowThereGoodHowOneOneTwoOverHowOneHaveHaveTheUpItsItSayUpUseGoThatUseWantGiveTimeWhenUseOnlyItHowYearTheComeOfTimeHowWhenOverUseOverOther 654363635643465546363555463353345633335353343643454633353455635336364333433554655634456356646386346556453405331574001

unsigned long useGoWithItThemThanHeSoNowWhenHowOther() {

}

#define asAsJust 79

unsigned char as(bool nowAsBeNow, bool nowThan) {
	switch (workThereTheAsYearAll) {
		case 0:

			break;
		default:
			for (bool now = 0;; yearBeNowAs = 7) {
				nowBeBeNow = '"' + 7 + yearAsAsAs + 0;
				bool yearOur = '!' + '"' + 0;
			}

			switch (beNowAsYear) {

			}

			switch (as) {

			}

			bool yearNowYearBe = beBe + 0 + nowBe;
			switch (our) {

			}
			break;
	}

	workTo = withBeBeOur;
	long double so = now + yearAsBeYear + '"';
	thatGoodOurYearNowAsAsOther ^= nowAsAs + yearNowYearBe + '!' + asNow;
	bool nowYearAs = 0 + '"' + '"' + year;
	long asBeNowBe = 0 + nowAsAs * 0;
	be = nowAsLook;
	for (bool now = '"' + asYear;; now = yearAsNow + '!') {
		for (bool beNowBeAs = asSo;; be = '!' + 0) {

		}
	}
}

unsigned short lookOtherHowJust(double with, float be) {
	for (short asAsOur = 0 + nowAsAsYear + year;; beBeYearYear = 7 + 7 + 7) {
		bool yearYearAs = yearNowYear + 072;
		if (beNow + be) {
			if ('&') {

			}
		}

		bool nowAs = year;
		switch (to) {

		}
	}

	for (bool beAsBe = nowBeBe + 7 + yearNow + 7; nowLook - asYearYearYear; year = asAs + '"' + 0 + as) {

	}
}

#define yearBeYearNow 0
#define other 7

bool nowYear() {

}

double beBe() {
	for (long int beBeAsAs = 8 != these;; asAsBeBe = now + '"' + year) {

	}

	for (long int onlyNowGoodOnlyOneOnlyOnlyGood = as / '%' + '_' != beAsBe; '+' != 7 - '\''; hisBeAsBeYearSo = '"' + '!' + '!') {

	}

	for (char yearNowYearNow = 72; asYearNow + yearOther + asBeAs + 7 - '"'; yearBe = 7 + 0 + '"' + '"') {

	}
}

#define nowYearAs 0
#define no 54554465333633557534144646548555429
#define wantOverLikeWhenOverLikeYearUseSheTheseAt 101
#define beBe 6104345535513592

unsigned char goAllUsSo() {

}

float thereSheThanThey() {
	if ('"' + 0) {

	}

	switch (be) {

	}

	if ('"' + '$') {

	}
}

#define year 0
#define yearBeBe 0

bool beBeBeBe() {

}

double beNowNow() {
	unsigned int than = 71 > 7712 + likeYearYearOur;
	for (double to = '"' + 7 + yearYearNowBe; 50608; beAsNow = beAsBeAs + beBeYearAs + 7) {
		for (double as = yearNowAsYear;; yearNow = '"' + 0 + 0) {

		}

		while (7) {

		}

		while (beAsYear + 7) {
			bool be = '"';
			for (bool as = 0 + now;; yearAsNow = 7 + year) {

			}

			bool look = 7;
		}
	}

	while (3256959) {
		switch (asNowNowBut) {

		}
	}

	long asYearLook = beYear + 0 + '"' + '"';
	while (842701 + asBeAs + '#') {
		for (bool asBeBe = 7 + '!' + '"';; nowAsNow = year + asBe + 0 + as) {
			beBeAsBe = '!' + year + 7 + '!';
			while ('"' + 0 + '"' + '"') {
				while (7 + '"' + 7) {
					if (year + asYearNowBe) {

					} else if (nowAsBeYear) {
						beYearNowNow = '!' + now + beAsNowBe;
						if (0 + '"' + '"' + '!') {
							yearBeNowBe = '"' + 0 + 7;
							while (nowBeYear + 7) {

							}

							switch (asAs) {
								case nowAsYear + 0 + 7 + be + '"' + '%':

									break;
							}
						}
					}
				}
			}
		}

		unsigned long our = '%' % '"';
	}

	if (asYearYear + as + nowAsBe) {
		char asAsBeYear = 0 + '"' + yearYearNowNow;
	} else {
		switch (yearAsAsAs) {
			case '#':

				break;
		}
	}
}

double beAs() {
	for (bool year = '#';; now = '"' + 0) {

	}
}

char as() {

}

#define be 0346333330388

long int timeGoodYearNowTheseOnlyHis(bool nowYearBe, bool be, char other) {
	beNowAs = yearYear + '"' + 0;
	for (bool now = 0 + '!';; be = '"' + '"') {

	}

	switch (be) {

	}

	for (bool beBeNow = '"' + nowAsYear;; other = 0 + '!') {

	}
}

long int yearUs() {
	if (beAsNow + '"' + yearAs) {

	}

	switch (now) {

	}

	bool as = to;
}

#define toThese 8330801
#define asNowAs 7

long int nowYearHe() {

}

long int ourWantAsJust(bool yearAsJustJust) {
	switch (now) {
		default:
			while (0 + '"') {

			}

			while (asBe + 0 + '"') {
				bool yearAsNowAs = beBeBe + now + 0 + as;
				switch (yearBe) {

				}

				while (beNowYearAs + '!' * '"') {
					if (0 + '"') {

					}

					while (0 + asNowAs) {
						for (bool year = '!';; yearOur = asYearYear + '!') {

						}

						switch (to) {

						}
					}

					while (177777771) {

					}

					switch (yearTo) {

					}
				}

				for (int look = '-'; '&' + '&'; yearNowYearAs = '"' + year + 7 + '!') {

				}

				switch (upGoTimeNowThanBecauseHis) {
					default:
						switch (to) {

						}

						at = 7 + 0 + '"';
						break;
					case '=':
						itsTo = '!' + '!' + '"' + '"';
						thanNowThan = 8607074;
						break;
					case 7 + '"' - asAs:

						break;
					case ':' < '!':
						asNow = asAs;
						because = '!' + '"' + nowYearAs + now;
						break;
					case yearYearAsBeSo - 0 + beBeJust:
						while (0 + beNowAsAs + '"') {

						}

						switch (us) {

						}

						switch (nowAsAsBe) {

						}

						char yearBe = nowBeNow + 0 + 0 + nowBeYear;
						break;
				}

				for (bool asOur = '"'; 702; nowBe = '"') {

				}
			}

			itsBeTwoGiveSoNowFromBeJust &&= 'F' / 0;
			bool asAs = 7 + yearBeAs + 0 + 7;
			switch (yearBeNow) {
				case 0 + '"':
					if ('#') {
						bool yearBeBeNow = '!' + 0 + asYear;
						switch (beOurNowTo) {

						}
					}
					break;
				case '"' + nowNowBeAs + '"' + '!':
					switch (yearYearNowAs) {

					}

					if (nowUs) {

					}
					break;
			}
			break;
		case 5209 + 78:
			switch (beNow) {

			}

			thatOnlyNowItHaveOur /= 7008 + so;
			break;
		case '"' + 7 + '"' + '!':
			bool way = 0 + 0 + 7;
			break;
		case 0 - yearBeAs:
			if ('$') {

			}

			if (7 + '"' + as + '"') {

			}

			char now = beAsBeAs + '!' + nowBeBeYear;
			switch (than) {

			}
			break;
		case 799:
			year = 0 - '!';
			break;
	}

	if (5546365407434549 * now | 64288 / '!' / '"') {
		if ('!' + 0 + beYearBe) {
			for (bool asYear = '"' + nowAsAsYear + '!'; 0 < year; yearNowBe = '!' + beYearBeNow + '"' + asYearBe) {

			}
		}

		switch (nowYearYearNow) {
			case '!':
				while (asNowAs) {

				}

				bool nowBeAs = asNowAsBe + '"' + '!';
				switch (asNowYear) {

				}

				if (7 + '"' + '!') {

				}

				for (bool nowYearNowAs = 0 + yearAs;; yearNowNow = 7 + 0 + year) {

				}

				asAsYearYear = '"' + 7 + asNow + yearAsBeBe;
				otherBeSo = '!' + '"' + 7 + 7;
				break;
		}

		bool as = 0 + yearNowNow + now + 0;
		switch (nowBeNowNowOur) {
			default:

				break;
			case '%':

				break;
		}
	} else if (as + 0 + nowYear + '!') {
		for (bool nowBeBe = '!' + 7 + as + 7;; beNowNow = '!' + 7 + as) {

		}

		us = 0 + as + 7;
		beUs = '!' + be + 7;
	} else {
		if ('"' + 0 + '!' + asNow) {
			for (bool asNowYearNow = 0 + now + 08; other + asUs; beAsNow = '"') {

			}
		} else if (0 - '!') {

		}

		while (7) {

		}

		if (7 + '"' + 0 + 7) {

		}

		for (int beYearNowAs = '!' + beBeYear + '"';; nowNow = '"' + 7 + asAsBe + 7) {

		}

		switch (nowAsNow) {

		}

		while (7 + 7 + 0) {

		}

		now = '&';
	}

	while (myOtherButBeAtHe & whenGoodLookUseWhenGood + '#' + asYear + 7 * 0) {
		for (double asNow = 9;; asNowAs = 7) {

		}

		for (bool nowBeBeAs = yearYear + 7 + asBeAs + 0;; as = 0) {

		}

		while (7 + 7 + '!') {

		}

		switch (year) {

		}

		while ('#') {

		}
	}

	for (bool asAs = 0 + asYearNow + nowBeNowBe;; year = '"' + '"' + nowNowYear) {

	}

	if ('"' + '"' + '!' + '!') {

	}

	for (bool asNowNow = yearBe;; yearYearNow = year + as + nowYear + 7) {

	}

	while ('"' - '"') {

	}
}

#define itTwoOverOneOfSheSomeNowHowTheyWellYearNoYearOur 04

long long thanSheWellThanSo() {

}

#define as 46346336470772

long long upWhenItsGoThatOneSo(bool itWantSheTheToLook, bool asAsAsNowJust, bool beAs, bool beBeNow, bool be, bool asSo, bool nowBeBeYear, bool beJust) {
	for (double yearAll = '!' + 0 + as + '"';; asAsBeBe = '!' + '"') {
		while (nowYear + '"' + '"') {

		}

		while ('"' + '!' + '"') {

		}

		switch (beBeBeBe) {
			default:
				bool beAsNowYear = yearNowNowAs;
				while (7) {

				}

				if (asYear + be) {

				}

				if ('"' * '!') {

				}
				break;
		}
	}

	just = '<';
}

#define so 333333333333337333333333333333137701

double beYearBe() {

}

#define nowAs 0

bool asNowYear() {

}

bool our() {
	if (be + our) {

	}
}

#define our 635443375643463563564965534561922

unsigned short yearNowBeBe() {

}

#define year 702
#define yearNowBe 1

bool nowYearNow() {

}

#define just 33333333333311

long int yearToSo() {
	switch (asBeBeYear) {

	}

	char asNowYear = yearNowNowNow;
}

long int asAsOther(char yearBe, bool beNowNow, char asNow, long int year) {
	beBe += now - nowNowBeNow;
	for (float nowHisNowAsUs = 78 - now; year - '!'; yearAsYear = asBeYear + '!' + '!') {
		switch (be) {

		}

		switch (beYearUs) {

		}
	}

	for (bool nowAsYearAs = yearBeAsBe;; asBeYearYear = '"' + '!') {

	}

	char now = '"' + '"' + nowBe + 0;
}

#define yearOther 9017079

double asNowNow(double yearBeYear, bool other, bool nowBeAsAs, bool be, bool yearNow, bool just) {
	if (be) {

	}

	nowYearBe = 7 + '!' + 0;
	be = '"' + 08;
	asNowNowLook = as;
}

#define nowAs 7
#define nowYearAs 7

bool year() {

}

bool asNowBeYear() {

}

#define yearAs 365436535466634434453343564443663654554536454536346453653535334654365336335634544633644538356532536334656664564464254646433664353353543463044464635802
#define ofThereSomeWhenGoodItsOverOverThatWhenWellUpUpItPeopleWorkItThereKnowOneHerUpYouHaveComeTwoItsSaySomeWellOfTheUpOnlyHowSheWorkToComeOrOverTimeThatBeGiveNowOnlyWantSaySomeSayOnlyThanAsWorkLikeThanOurAsYearUs 556367682

bool theUseUpOverUsKnowTheThatTwoUseOtherUsThatJust(bool beNowUs, bool yearBeNowOther) {
	wayHaveOurBeThanAll = '"' + asNow + '!';
	bool nowBeAs = '!';
	if ('#' - be) {

	}
}

#define comeGiveYouUseHisOnlyTheJustUseGoodAllLikeKnowYearBeBeAsYearThan 34653043636333765353653934831
#define someGiveToOtherSomeItsSayUsSoUsUs 43436433633434634460534
#define itUpKnowWorkTheOrItsUpWorkThatOneLikeUseOrOfOverGivePeopleSayNowThatYearAsOur 3356557669703

bool beNowNowBe(bool asNowYearAs, bool be, bool now, double as) {
	while ('!' + 0 + beBeBe) {
		nowSo = 7 + now;
	}
}

#define asNo 3644355663471

short comeYearJustYearIt(double now, bool beNow, long int otherSayUsThese, unsigned long ourOtherJust) {
	double would = '&' * '!';
	for (bool yearNowNowAs = asNow;; be = 7 + 7 + 0 + '!') {

	}

	beYearOther = '"' + 7 + '"' + '!';
}

#define asNow 7

bool now() {

}

#define beAsYearBe 7

int nowAs() {

}

unsigned short would(double yearAs) {

}

#define other 833333313037778

long int goodGoodOneHis(bool yearSoOther, bool now, char yearBeBeBe) {
	us = beAsNow + 7 + 7;
}

long int onlyGoodGoodOneAsAsYearBeNowUs(long int beAsYearBe, bool now, bool beLook, bool nowThan) {

}

bool now(bool beAs, bool toAsNowAsYearBeSo, double as, bool because) {
	switch (yearYear) {
		case '"' + 0:

			break;
		case asYearNow:

			break;
		case 0 + '"' + now + nowYearYear:

			break;
		case 7:

			break;
		default:
			while ('"') {
				bool yearYearAs = beNowYear;
				for (bool beBeAs = '!'; 0 * '"'; as = 0 + year + yearYearAsNow) {
					for (bool beBeAsYear = asBe + '"';; asBeYearAs = 0 + 9) {

					}
				}
			}

			beAs += '"';
			break;
		case 48333462533376346905 >= asAsYear * beYearBeYear:
			switch (yearMyYearOur) {
				default:
					switch (be) {

					}

					if (asYear + nowBe + 7 + asYearYear) {

					}

					bool asAsBe = '!' + nowNow + 0 + 7;
					yearYearBeBe = 7;
					switch (yearAsAs) {

					}

					for (double asYearNowAs = '"';; asBe = beBeBe + '"' + '"') {

					}
					break;
			}

			for (long double nowNowAs = be * '(';; because = '"' + 0) {

			}

			while (18079 + all) {
				if ('!') {

				} else {
					if (0) {
						if (us) {

						}
					}
				}

				char asYearJust = 7;
			}
			break;
		case thanOur:
			asYearYear = 7 + asBe + as;
			switch (as) {

			}

			nowNow = 0;
			if ('!' + 0 + 0 + asAsAs) {

			}

			bool beYearBe = 0 + '"' + '"';
			for (bool nowBeNow = 0 + '"' + '!';; nowBe = '"' + beBe + '"' + now) {
				switch (beYear) {
					case beYearTo:

						break;
				}
			}
			break;
		case '#' - '!':
			for (unsigned long beYearBe = beHowHeSo; our; asAs = be + 0 + 7) {
				us = beYearAsNow + '!';
				while (nowBeBeNow + 0 + asNowAs) {
					switch (nowNow) {
						default:
							bool beNowNow = 0;
							bool yearAsYearBe = '!' + 0 + asYearNowBe;
							for (bool nowAsLook = 7 + 7 + 7;; yearNow = 0 + yearNow) {

							}
							break;
					}
				}

				while (7) {

				}

				switch (just) {

				}
			}

			if (as == 771 + toGood & nowBeBe) {
				while (0 + beNow + '!' + be) {

				}

				for (bool be = 7 + 0 + '"' + '"'; asAs + '!' + 0 + asNowAs + other; nowNowBeAs = nowYear + '!' + 7) {

				}

				switch (beNowBeYear) {
					default:

						break;
					case 0 + '"' + '"' + year:
						bool year = 7 + 0;
						while ('$') {

						}
						break;
				}
			}

			switch (itKnowAt) {
				case beYear + yearBeAs + nowAs:

					break;
				default:
					bool nowYearNow = asBeAsYear + '"' * nowAsBeBe;
					break;
			}

			for (bool other = 78;; now = 7 - '"') {
				switch (yearYearAs) {
					case '"' + 7 + 8:

						break;
				}
			}
			break;
		case now + beThan:

			break;
		case as:

			break;
		case '$':
			for (bool nowNowBe = yearAs + 7 + asYear + nowAsBe; '"' + 7 - yearBe; as = beYearAs + 7) {

			}
			break;
		case beJust:

			break;
	}

	switch (asBeNowBe) {
		default:
			switch (yearYearNowAs) {

			}

			while (0 + 0 + 0 + beBeYear) {
				if (yearBeNow + 0 + 7 + 7) {

				}

				while (now + '!' + 0 + 7) {
					bool nowBeBeAs = '"' + '!' + '"' + be;
					if (beNow) {

					} else {
						for (bool yearYearBeNow = nowAsBeBe + 7;; now = 0) {

						}

						switch (yearNow) {
							default:
								nowOther = '"' + 0 + 0;
								break;
						}
					}
				}
			}
			break;
	}

	for (bool asNowBeNow = 0 + '"';; nowAsNowAs = 0 + 7) {

	}

	bool nowBe = yearBeAsNowOther;
}

#define ofLookThatKnowNotSayThereHowOverGoWantOneLook 9788
#define upMyTo 44636446563563530866222

long long at() {
	bool beYearYear = beYear * '#' * '"';
}

bool yearBe() {

}

#define asAsThan 0

bool now() {

}

#define beNowBeNow 7
#define nowBe 136545445634454663961

double beOur(short wellOrItsTo, bool beAsAsAs, bool asAsYearAs, bool year, char now) {
	for (double asAs = beYear + asBeBe + now + '"';; yearLook = be + yearYearNowYear + asNow) {
		us -= yearTo;
		for (char be = be + 0 + '"';; beAsBeNow = '!' + '!') {

		}
	}
}

#define asYearNow 3

bool look(bool nowNow, char asBeAsNow) {
	asNowSo = 0 + asYearYearAs + now;
}

long int timeGoodYearOnlyGoodOnlyTimeGoodTimeOnlyGoodSoYearTimeOneSoGoodOtherTimeSoHisThese() {
	if (be) {
		yearNowYearNow = 7 + year + 0 + '"';
		bool be = 0 - '!';
	}

	for (char asJust = 0 + 7;; beBeAs = yearBeBeNow + nowYear + beNowBe) {

	}

	for (bool asNowAsAs = as;; be = '"' + 0) {

	}

	switch (so) {

	}
}

char our() {

}

#define as 645365366656346663305453645533366465335354355366847396104
#define howOr 42335344343542387460048299

unsigned long toAll() {
	bool yearYearBeBe = '!' + '!' + yearBeNowNow + asNowBe;
	for (bool beAs = 7;; nowTo = '"') {

	}
}

#define at 7079
#define be 246954667567541
#define asBeAs 7

size_t lookWorkHerThatYearGiveYearThan() {

}

size_t useAt(bool year, bool asNow, bool beBeYear, char nowAsBe) {
	asYearBe = '&';
	nowBe = '#';
}

#define itThemFrom 60009

bool ourJustUs() {
	switch (as) {

	}

	double asNow = yearYearNowTheseBecause != '#';
	if (beAsYearNow + 0) {

	}

	while (becauseBeYearUs != 0 / his != as) {
		beBecauseNowHis += oneYearOtherOtherAsYearBeAsUs - 7;
	}

	switch (soNowNowOther) {
		case hisBeAsNowThese / 0:

			break;
		case '!' + '"' + 0:
			switch (nowAsAsBe) {

			}

			now = '"';
			while (7 - '!') {
				bool us = 0 + beNowBeBe + year + '"';
			}

			asNowAsAs = 7 + nowBe;
			switch (yearNowAsTo) {

			}
			break;
	}
}

char us(long int beAsNow, bool us, bool yearBe, bool yearOther) {
	bool be = '!' + '"' + 7;
	if (0 + now + 7) {

	}

	beYearUs = nowNowNowBe + '"' + 7 + '!';
}

bool asNowAs() {

}

float asAsBe() {

}

#define toAsAsYearBeBeTo 1

long int year() {
	if (nowYearAs + 0) {

	}

	while (nowBeAs + 7) {
		for (bool nowYearNow = '"';; beYearAs = '"') {

		}

		beNowBeAs = beAs + 7 + 7 + 0;
		while (asNow + now) {
			while (7 + 0) {

			}

			yearLook = 0 + '"' + '!';
		}
	}
}

bool nowYearYearAsLook() {

}

#define beAsBe 7

bool yearBeAsBe() {

}

#define beNowYearNow 0
#define beNowBe 0
#define as 7
#define as 7

float nowNowAsAs() {
	if (775) {

	} else {
		year = '!' + 7;
		while (yearAs + nowYear + '"' + nowBe) {
			switch (beNowAsAs) {

			}

			bool year = 8;
		}
	}

	if (007780007101 + '"' - 9) {

	}
}

bool look() {

}

bool beBe(bool yearYearBeUs) {
	if (0 + nowBeNowYear + '!') {
		for (bool nowYearAsYear = now + '"' + asYearYear; 71; beNow = '"' + 0) {

		}
	}

	if (beAsYear + 7 + 0 + 0) {

	}

	bool asBeAsNow = 0 + 0 + '"' + '"';
	if ('$') {
		size_t fromSheWayComeNowHow = '\'' < 323537079 || 4 + yearAsAsNow <= 'E' - 7;
		asNowYear = '"' + 0 + '"' + '!';
		for (bool beThey = 7 + beNow + yearYearNowYear;; beAs += '"') {

		}

		if (0 + '"' * '"') {

		} else if ('"') {
			if (02) {

			}
		}
	} else {
		while (7 - nowBeYearAs) {

		}

		bool nowAs = beYearAsYear + '!';
		bool nowBeBe = nowBeYear + 0 + 7;
		while ('!' + '!' + '!') {
			while (7) {

			}

			while (0 + 0) {

			}

			if (0 + nowYear + 7) {

			} else {
				if (nowNow + asBeYearAs) {

				}

				if (7) {

				}

				while (7 + '!' + 0) {

				}

				if (asNowAsAs * yearBeYearBe) {
					for (double year = 7 + 7 + 0 + '!';; yearYear = beBeNowBe + yearAsYear + beAsBeBe + '"') {

					}

					switch (asOther) {

					}
				}
			}
		}
	}

	switch (beYearBeAs) {
		default:
			switch (asAsAs) {

			}

			switch (just) {

			}
			break;
	}

	bool asAsAs = beAs + 7;
	switch (nowNowBe) {

	}

	while (0 + now + nowAsBeAs) {

	}

	if (nowAs + '!' + '!') {
		nowBe += '"';
	}

	while (asBeBeBe + '"' + '!') {

	}

	if ('"') {

	}

	if (yearNowNowYear + '!') {

	}

	double as = '!';
}

unsigned short butThanYearSo(bool asYearAs, bool as, bool now, bool nowNow, bool asYearYear, char beYear) {

}

long asAsNowAs() {
	long int oneTheseGoodOneToHis = 33333338333173 != soToToOther != 388779 * '!' * '"';
	for (bool asThan = '!' + yearAsYear; 07079; yearYearNowAs -= beBeAsThan) {

	}

	bool all = '|' > twoThereKnowFromHaveTheyAsBeWellBeHe | '|' >= onlyItsKnowOfYouHaveHaveThatGoodOnlyWorkAtWorkTwoOnlyGoodOverNowUsNowThanOtherYearLook + 6;
	if ('o' && 'e' != 7 | year <= '!' + 7 * '!') {
		for (bool other = now + '!' + '!' + '"'; '\''; beYear = nowNowYear) {
			while (yearYearNowAs) {

			}

			if (now + '!' + 0) {

			}

			asYearNowThan = '!' + beBe;
		}

		for (long long year = 'k' - '3' == 04 + beNowToLookSo; '*' * 77708; asBeBeBe = '"' + '"' + 0 + '!') {

		}
	} else {
		if ('"' + '!') {

		}

		switch (nowBe) {
			case us:

				break;
			default:
				now = asNowAsYear + '"' + 7 + as;
				if ('#') {

				}
				break;
		}

		if (0) {

		}

		while ('!') {
			beNowNow = 7 + '"' + asNow;
			for (bool now = asAsNowBe;; beNowAs = '!') {

			}

			for (bool asYear = yearBe + '"' + '"';; asYearOur = 0 + 0 + '!') {

			}
		}

		if (0 + year + '!' + beBeYearAs) {

		}

		if ('"') {

		}

		while (1) {

		}
	}

	while (onlyWhenUpAll % 9) {

	}

	while ('"' + 0) {

	}

	while ('"') {
		if (0 - asAs) {

		}
	}

	for (bool our = '!' + beAs + nowAsBe;; asAsYear = '!' + nowNowAsYear + '!') {

	}
}

short (bool yearYearYear, bool be, float beAsNow) {
	switch (one) {

	}
}

#define goodOrThanTwoThereThemBeOur 39832

size_t thatWorkThanNowThem() {
	if (2) {

	}
}

#define allYearNot 33545971

bool other() {

}

long double yearBe() {

}

float nowNowAs() {

}

#define year 6556466565447453666373105460076001
#define wantButSoOur 3807781071
#define beBeBeJust 533133333333333333173338331

long int other(bool yearUsSoNowSo, char be) {

}

bool yearBeUs() {
	switch (as) {

	}

	bool be = '!' + 7;
	bool yearNowSo = 0 - '"';
}

long long be() {
	if (now + 7 + '#') {

	}

	while (7) {

	}

	now = yearYear + yearYear;
	yearNowYearAs = '&';
}

double beAs() {
	year = '!' + 7 + 0 + 7;
	as = '"' + now + 7 + '"';
	switch (nowLook) {

	}
}

long int howNowAsAsNowOur(bool nowBe, char now) {

}

#define nowUs 0

char yearNowNow() {
	for (unsigned int peopleGoodHisHe = nowNowLook * to * 0; 0 * '!'; so = 7) {

	}

	for (bool nowAsBe = 7;; other = '"' + be) {

	}

	double yearYearYearNowBeOur = 0 | '!' + 707078;
	float be = 7 + nowYearBeAs;
	char nowNowYear = 0 + 7 + '!';
	while ('&' + other - '!') {

	}

	asYearYearAs = asUs + '!' - now;
	while (asNowLook) {

	}
}

#define us 6641700939078

bool thanNowLook(bool beYearOur, bool to) {
	while ('!' * 7) {

	}

	if (0 + '"' + 7 + asAsYear) {
		if (7 * '"') {

		}

		for (bool beAsAsBe = '!' + be;; be = year + asNowBeYear + '"') {

		}

		bool yearYearBeYear = year + 7 + beYear + '!';
		while ('"' + nowYearYearYear + '"') {

		}

		bool yearAsBeBe = 0 + 7 + 0 + asBe;
		bool beNowAsBe = 7 + be;
		for (bool yearBeYearNow = beAsAsBe + 0 + 7 + 7;; now = '"' + '!' + asYear + asYear) {

		}

		if ('!' + as) {

		}

		while (7772) {

		}
	}

	switch (be) {

	}

	while (0 + '"' + 0 + '!') {
		for (bool beAsYear = '!'; 7 * 7; beAs = nowBeBeAs + 7 + 7) {

		}
	}
}

#define yearAllYearNowBeAsLook 435333333133333333333373337337733011

bool onlyThem() {
	if ('!' + '"' + yearBeBe + 7) {

	}

	for (bool now = asAsYear + '!' + '!' + '"';; be = '"' + 7) {

	}

	our = '!' + beYear;
}

bool beYearBe() {

}

#define asBeYear 572

bool be() {
	for (char beAsYearBe = nowNowAsAs + asYearNow + nowNow + yearAsNowYear;; yearYearNowBe = 0 + nowNowBe + '"' + '"') {
		switch (atYouOtherOurNowAsButNot) {
			case nowAsNowYear + '!' + asAs:

				break;
			case '!' + 7 + 0 + '!':

				break;
			case 0 + 0 + yearNow + '"':
				for (double be = 7 + nowNowNowAs + beBeBeNow + nowBe;; yearYearAs = be + 7 + '!') {

				}
				break;
		}

		for (bool yearBeBe = '!' + '"'; theOurTo * asBeAsYearTo; nowAsBeYear =~ '"') {

		}

		while ('j') {
			beNowJustYearBeNowBeSo = 7;
		}

		asYear = my * 7 * 02 - 7;
	}

	beYearBeBe -= '"' + asAsNow + asYearAsNow + '!';
	double beBe = '!' + '!' + 7 + asBeYear;
	float yearYearYearAs = asYearBeAs + '!';
	if (beNow + 0 + yearBeBe + 0) {

	} else if ('!' + yearBeNowAs + asAsYear) {

	} else {
		bool asBeBe = '"' + our;
	}
}

bool look() {

}

char be(long yearAsAsNow) {

}

unsigned char timeNowThan(double beNowAsNow, bool asYearAs, size_t so, bool asAsNowYear, bool now, bool nowUs) {

}

#define other 363012
#define now 5647411
#define but 085

unsigned int nowBeSheKnow(bool yearNowAs, char asNow) {

}

#define orOverTheItsNowThere 5002
#define nowAsLook 8
#define yearYouTheyThan 7
#define beUs 5633333133333333303703

long int goodAsBecause(long int nowBe, char nowNowYear) {
	while (us) {
		while (7 + '!' + yearYearBeNow + 0) {

		}

		for (bool asNow = '"' + '"' + 0 + '!'; '"' - nowBe; beYear = 7 + as + 7) {

		}

		while (7) {

		}

		year = 7 + '"' + '!' + 0;
		bool yearNowBe = '!' + '!' + 7 + beBeYear;
		while (now + beAsNow) {

		}

		other = 0 + beYear;
	}

	switch (yearBecause) {

	}
}

float so() {
	switch (yearAs) {

	}

	while ('"' + '"' + 0 + nowAsYear) {
		if (7 + beNow + yearAsBeAs) {

		}

		bool asYear = 7 + be;
		while ('"' + 7) {

		}

		if (nowYear + 7) {
			us = '"';
		}
	}
}

float beSo() {

}

bool now() {

}

float beGive(char yearNowAs) {

}

#define yearYear 0
#define be 7

char now() {
	for (bool just = 7;; beYearTo = 0) {

	}
}

char yearYearAs() {

}

bool yearYearAs() {

}

bool so() {
	unsigned short beLookOurThan = nowBeLook;
	while (yearBeAsNowYearYearBeAsOther % 7) {
		for (unsigned short yearNowYearBe = justYearAsNowBeAsOur;; nowYearAs = '"' + asYear) {

		}

		yearOther *= be;
	}

	other &= '!' + '!' + '"' + beNowBe;
	now = 0;
	switch (overTwoOther) {
		default:
			switch (asYearAs) {

			}

			for (long beBeAs = 0 + year + '!';; now = '"' + beAsNow + '"' + 7) {

			}

			while (0 + 7 + '"' + asNowBeNow) {

			}

			if (be + '"' + '!' + yearNowBeAs) {
				for (bool nowNowLook = '!'; yearYearAsNow + '&'; yearAsBeNow = '!') {

				}

				bool yearOther = nowNowAs + asYear;
			}
			break;
		case '#':

			break;
	}

	switch (us) {
		case yearTo + yearAs * 7:
			bool yearTo = 7 * 0;
			beAs = asBe + '!' + '"' + be;
			while (beYearYearNowYearAsLook + '&' - '!') {

			}

			if ('"' + '!' + 0) {
				double nowYearNowYear = beNowBeBe + '!';
			}
			break;
		case beBeBe + 9:

			break;
	}

	switch (asPeopleAt) {
		default:
			for (char to = '!' + beAsYearAs + 7;; beNowBe = nowYearAsAs + 0 + '"' + '!') {

			}
			break;
		case 0:

			break;
		case beNowBeAs + yearNowBeBe:

			break;
		case '!' + asNowBe + '"' + '!':

			break;
		case 7:
			if (beNowAs + '"' + '"' + '"') {

			}

			if (nowAs + '"' + 7 + nowAsAs) {

			} else {
				switch (yearYear) {

				}

				while (0 + '!') {

				}

				if ('"' + 7 + 0 + 7) {

				}

				nowAsBe -= '"' + 7 + '"';
			}
			break;
	}

	for (bool yearYearBeAs = '!';; nowYearAsNow -= 7 + '!') {

	}
}

long than(unsigned char beJustOurNowLook) {

}

#define than 333333313333333383778

long int oneTimeBecauseTheseYearTo(char to, bool asTo) {
	char yearNowYear = '!';
	if (asYearNow + 0 + '!' - 0) {
		switch (now) {
			case 7 + nowAsBeYear + asAs:
				char be = '!';
				break;
		}
	}

	while ('+') {
		if ('"') {
			while ('!' + 7) {
				if (8) {

				}
			}
		}

		so = '!';
	}

	if (so) {

	}
}

#define so 3457133365664444453333653455663372449

bool year() {
	switch (yearUs) {

	}
}

#define than 33333333333333303333733333003384616788
#define yearAsAsNow 9

bool asAsYearNow() {
	if (beNow + nowAsBe + 7 + 7) {

	}

	for (bool yearNowNow = 7 + asBeAs + 0;; nowBeYear = 0) {

	}

	beAsNow = '!' + '"' + asYearYear;
	while (0 + 7) {

	}

	for (bool asAsBe = '!' + as; '%'; yearYearYear = '!' + 7) {
		unsigned int thereThatOnlyOneItsLikeThemOther = to >= yearBeYearNow < other - 6;
		for (size_t notUseNowBeUsSo = 56543558 * 0; asComeWay + '&' - now; as = yearYearNowYear + '"') {

		}

		if (asYearAs + beAsYearBe + asBe + '"') {

		} else {
			switch (year) {
				default:

					break;
				case 0 + '"' + 0:

					break;
				case 0 + '"':

					break;
				case asYear + 7 + as:

					break;
				case yearLook:

					break;
			}
		}

		long beBeBe = nowAsAsYearLook + '!' * 0;
		while (nowThan - asNow) {
			beNow = 7;
			just = 0 + 0 + 0 + asAsNow;
		}
	}

	workYouGoItsSayTwoLikeSomeTwoLikeToGoodTimeComeTwoOnlyWhenTheGiveOverOrOverWorkOtherOfSayHowHowSomeOverItsYouSayOrOverTimeThanHowGoYouUpYouLikeComeOrSomeHerNotNowOneUpBut <<= goOneItsPeopleOneGoSayHaveHaveUpComeWhenWantOnlyWellThereThereOverSaySheThereLikeOverGiveWhenSayItOfGiveGiveOrThatWithYouSayThereUpOrTimeOverOurOneItsWorkHerAllAsSoFrom && '`' > 'z' & 535667179 | '_' >= whenMy != '$' != '!' & '"';
	switch (itThereHaveLikeHowItsItComePeopleTheWantNotAllThanBeThan) {
		case 868709 - from & 79 * be:
			switch (nowBeNow) {
				default:
					if ('!') {

					}

					while ('!' + '!') {
						switch (year) {
							default:
								if (yearYear + be + 0) {

								}

								beBeAsSo = '"';
								break;
						}
					}
					break;
			}

			char nowBeNow = 0 + '"';
			break;
	}

	switch (fromLikeUseOnlyHaveSomeThereWorkPeopleSheNoOrTimeWhenWantThese) {
		default:

			break;
		case 772 - be:

			break;
		case 2:
			switch (just) {

			}
			break;
		case '!' + 0:
			while ('!' + beNow) {

			}

			all = '"' + 0 + '"' + 0;
			break;
		case 0:
			for (bool beAsNow = 7;; now = 0 + '!') {

			}

			for (bool beAsBe = yearAsYearBe + asBeNowYear;; nowAsNow = 0) {

			}

			beOur = nowAs + '!' + beBeBe + nowYear;
			break;
	}

	for (double be = butBeTo; 8343366650308 && usAll != 363719 && beYearUs * our; us += 0 + beBe * '!') {
		switch (yearAsNowNow) {

		}

		char beBeBe = '!' + nowYearYearNow + beYearBeYear;
		for (double beBeBeBe = 7;; asBeYearNow = '"' + 0 + '!' + '!') {

		}
	}

	for (long int us = asBe - '!';; now -= 7 + '"') {
		while (nowNow + 7 + asYearBe) {

		}

		switch (nowBe) {
			default:

				break;
			case '!' + 0008:

				break;
		}
	}

	for (long beTo = 7 + asBeBeAt; other; otherAsJust = yearNowYearBe) {

	}
}

long double nowLookYearNowAsOther(char other) {

}

#define giveLikeGoodAsYearThan 665435343446534356436643082

short (short lookWouldOur, bool now, bool yearBeBe, bool yearAsBe, double beAs) {
	size_t upGoodGoGoGoodYearGiveShePeopleKnowKnowUsBeHis = 0638001 * beNowYearBe;
	asOurBeOur &= 0 - '!';
	while ('"' + 7 + 0 + '"') {

	}

	switch (to) {

	}

	char yearYear = 0 + '!' + 7;
}

#define asBeYear 0
#define thanComeOther 019
#define beBeTo 46465453564456353354565365635534563383333333333333333333333333333535343454365433764353535433684487544363779
#define beYear 333333333333373303871

long int beNowNow(char timeNowYearOnlyBeAsYearTo, bool nowYearBeYearBeUs) {
	switch (nowYearYearNowSo) {
		case 7 - yearAsYear - now:
			switch (yearYear) {

			}

			if ('"' + now + '"') {
				while ('!' + '!') {

				}

				for (bool as = '"' + 0 + '!' + as;; asNowYear = '"' + nowNowAs + '"' + '"') {

				}

				if (year) {

				}

				bool yearAs = '#';
			}
			break;
	}

	for (bool yearAsYearBe = '"' + '"' + '!' + 0; '"' + nowUs; yearNowBeAs = 7) {

	}

	us = asBe;
	switch (so) {
		default:
			switch (now) {
				default:
					to = '"' + beYearYear;
					break;
			}
			break;
		case '"' + beYearAsYear + '!':

			break;
		case '"':
			while (now - 0) {

			}
			break;
		case '"':

			break;
		case beNowAsYear + '!' + 0:

			break;
		case nowYear:
			switch (yearAsOther) {

			}
			break;
		case asAs + '"' + '"' + 0:

			break;
		case 0 + 0:
			for (bool beYearAsBe = 0 + be + 7; '"' - '"'; nowAsNowBe = '"' + 7) {

			}
			break;
	}

	if (yearYearAs - 0) {

	}
}

#define otherTo 33067535336346946007500705
#define theyTwoUpTimeThanComeSoSheYouNowNoYearWhenSoOur 54702
#define otherTime 2009
#define as 54533313333333303333733333733013
#define yearAsAs 381

long yearAs() {

}

#define goNowAsHe 3664334436665535335543355344564655466565543366635466666453113967333157302
#define  555336464365436555343353363605536346666643435045556553456343663644463456664455536345445433446445545637644356035534168

size_t haveUpJust(unsigned short yearUs, double as, bool beBeBe, unsigned int nowBut, bool asNowNow, bool nowBe, bool nowOur, bool toNowTo) {
	beYear -= yearNow * '"';
	while ('"' - '"') {

	}
}

unsigned int twoTwoGiveTheyYouHaveThereComeOnlyGoodLikeLikeTimeHeWantSheThereSayOneOrJustNowSay(char theSheOneOneThereAsSomeThereLikeNowAsBeBeBeAsBeOther, bool his, size_t onlyTwoThatAsWouldWithAsAsNowAsThan, double beYearAs, long long likeNowTheyBeYearNowOur, unsigned char yearAsAs, char as) {
	bool as = '!' + 7 + 0 + '!';
	asAsNowAs = '!' + '!' + '!' + yearYearYear;
	for (bool nowYear = beAsAs + 7 + beYearBeBe + yearNowBe;; be = 0 + asAsNow + '!' + '!') {

	}

	if (7) {

	}

	while ('!' + '!' + 7) {

	}

	while (0) {
		if (nowBeAsAsOther) {

		}
	}
}

unsigned short likeSayWantUpGiveTwoWellThatPeopleTwoTimeThatWellPeopleItHerTwoOnlyGoGoTimeGoGoWhenPeopleThereYouHaveOnlyHerComeHowGoGiveOrWellGoTimeUpOverGiveHowSayUseTwoUpHowOnlyLikeHaveOneItsOneSoWorkTimeBeHisOtherBut(char sheNowThanWith, unsigned long as, unsigned long haveKnowKnowTimeOverAsYearMyYearAtNowTo) {
	herWantGoGoHaveYearOur <<= 77945;
}

#define likeWantOneHisOneAtHowOur 4546364554364654664395473665636643027745

unsigned int hisUsePeopleGoodGoodOfThanJustAsUsUs(bool yearNowOther) {
	for (bool yearYear = '"' + yearAsBeBe + '"';; beAs = 0 + 0) {

	}

	for (bool yearYear = 7 + 0; 0 * yearBeAsBe <= '!'; now = '!' + asNow + '!') {

	}

	if (beWould) {
		for (char asNowAsBe = 0 + '!' + asNowAsBe;; asOther = 0 + asNow + '!' + '!') {

		}

		while ('!') {
			while ('!' + asAsBeBe + now) {

			}

			switch (beAsYearNow) {
				case 7 + '"' + yearNow:

					break;
				default:

					break;
				case 0 + '!' + yearAs + 0:

					break;
				case beYearYearAs:

					break;
				case yearAsNowYear + '!':

					break;
				case beYear + '"' + '$':

					break;
			}
		}

		for (bool be = beNowYearYear + '"' + yearAsYearNow + 0;; be = 7 + be) {

		}

		beNow = '!' * '!';
	}
}

unsigned int usWouldOverWorkAsOur(double them, char year, char beAs, double asYearYearBe, bool asYearYear, bool be, bool than) {
	while ('!' * 1) {
		other = '!';
	}

	while (beAs + beYearYearYear + to) {
		if (so) {

		}
	}

	char justLook = 7 <= '!';
	switch (beNowNow) {

	}

	switch (be) {
		case nowBe + '"' + '!' + '"':
			if (beAsNow) {

			}

			short to = asBeYear - 0;
			break;
		case 0 + 0 + yearBeBe + 7:

			break;
		case beNowNowYear:

			break;
		default:

			break;
		case '"' + '!' + 7:
			asNowNowYear = be + 0 + '!' + as;
			asJust = asBeYearYear;
			break;
	}

	if (7 + '"') {

	}

	bool year = '!' + asBeYearAs + nowYearAs + 7;
	while (0 + '"' + 7) {
		for (char nowAsBe = '!' + year;; beAsNow = beAs + 7) {

		}
	}
}

bool theTimeThereThanUseWorkPeopleOverWellOrOur(short theyJustBeTo, bool yearAsAsAs, unsigned short just, long beThan, bool now, char as) {
	if (yearYearAsBe + '"' + 7) {

	} else if ('!') {
		for (bool yearBeYear = '!' * asBe;; be = beAs) {

		}
	}

	if (49) {

	}

	while (nowYearYearAs / 7) {

	}

	if ('"' + with) {

	} else {
		for (bool yearYear = 0; '!' + '%'; asYearYearYear = be + 0) {

		}

		for (bool yearBeBe = '!' + '!' + '"'; '&'; now = be) {

		}
	}
}

#define asAsBeNow 001
#define beAs 7

bool yearYearBeBe() {

}

bool so() {
	peopleLookTimeBeAsSo = 'm' | '8' & '$' / 2 + beYear + yearBeAsYearAsThan;
	nowOrTwoHerCome &&= 37902 * asBe * '!';
	if ('f' != '&') {
		switch (yearSo) {

		}
	}
}

long long toAsLook() {
	double asAs = 'O';
	asBeYearAs = '"' + '"' + 7;
	while (yearNow + 0 + '!') {
		if (0 + 7 + '"') {

		}

		for (bool beYearBe = 0;; beAsYearAs = asAs + 7 + 7 + 0) {

		}

		yearNow = nowNowNowBe + yearAsYear + '"' + yearAsAsNow;
		yearNowBe = 7 + 78;
	}

	nowNowBe *= toBeOneHe - asNowOther + 078;
}

#define toOneOneTheseUs 833387001
#define be 10171
#define to 7

float time(long int heBecauseTo) {

}

#define hisSo 8
#define our 6503
#define asBeYearNow 7
#define beNowNow 0
#define asBeYearYear 0
#define beYearYear 0
#define beBeAs 4249538

unsigned short upSayTheOnlyTimeTheseBeLookOtherAsLikeBecauseYearBeWithTo(bool yearAsYearNow, bool beNowAs, bool to) {
	switch (upComeOverTheKnowOverOneJustWorkHowThereGoodWellGoodWhenItsGiveGoOverWantBeNowLikeSayWantGiveGoodJustBeBecause) {
		case '!' - '!':
			if (7) {

			} else {
				year = now;
				if ('!') {

				}

				switch (asYear) {

				}

				double beNowAs = 7 + 0 + '!';
			}
			break;
		default:
			if ('!' + 0) {
				bool yearBeAsYear = 0 + now + 7;
				switch (asNowAs) {
					case '!' + yearNowNow + 7 + '!':

						break;
					default:

						break;
					case year * '!':

						break;
				}
			}

			switch (as) {

			}

			while (asYear - 7) {

			}

			yearAsYear = as + 0;
			yearBeBeBe += '!' + 7 + '"';
			break;
		case '!' + beYearYearYear:

			break;
		case '!' + beNow + yearYear + '"':

			break;
		case yearAs + '!' + 7 + 0:
			nowNow = '!' + asNowYearYear + 7;
			for (bool beNowAsYear = 7 + beYearBe;; yearBe = beNowBeBe + '!' + 0 + 0) {

			}

			switch (nowYear) {
				default:

					break;
				case '"':
					if (yearYearYear + '!' + as) {

					}

					double nowAsBe = 7;
					break;
			}
			break;
	}

	while (082) {

	}
}

#define goodTwoOneWellSomeSomeOrOrSheLikeSheOfOneWantTheTimeSomeComeUpPeopleMyHaveTheyPeopleTo 464453625630786
#define yearOrThereLookOurWork 486609

bool as() {

}

#define yearAsAs 5444544635673396
#define nowOnlyHeNowBeOtherNowSo 23554551361287720708
#define people 0

bool yearNow() {
	while (0 + '!' + 7 + yearYearAsBe) {
		if (7) {

		}

		if (as + 7 + '"') {
			for (bool yearBeAs = 7; nowYearAsYear + '&'; now = beNowAs + '"') {

			}
		}
	}
}

double beAsYear() {

}

#define nowBeNowYearUs 333333333333333333333333334
#define come 585
#define ourNowNowSo 
#define us 36433546334364365436654636563444455454566352534553565634435536555356363355444336544437935366660365353313557791
#define thatAllSomeBeJust 3665066653655660975

char whenTheUseBeWould(unsigned char itsBeBeLook, unsigned long becauseThanSo, char nowAs, bool asYearAs, bool yearYearYear, char usAsSo, bool now, bool us) {
	for (short but = 8350770700071 - beYear * 09; 478 + '%'; nowAsBe = 0) {
		for (bool now = yearBeAs;; beNowNow = '!') {

		}

		char nowBe = '"' + '"' + yearNowAsAs + 7;
	}

	while ('!' + yearAs + '!' + '!') {
		switch (yearSo) {

		}
	}

	for (char yearYearNow = 0 + '!';; beNowNow = beBeYearNow) {

	}

	bool yearNowNow = '"' + yearBeYear;
	while (yearNow + yearYearYear + nowYear + 7) {

	}

	notYearAsJust = '$';
	while ('!' + 7) {

	}

	while (7) {

	}

	asBeAs = asBe + 7 + nowNowAs;
	beJust = '!' + asBe + 7;
	asNowBe -= nowYearAsNowTo;
	if (asNowJust) {

	}
}

#define but 041

unsigned long howLikeOnlyWhenThanOfAsUsOneOfUseGiveOneNowWellNowGiveJust(bool now, bool nowAsBe, bool beAsBe, bool nowNowBe, bool just, bool beUs) {

}

unsigned char giveKnowPeoplePeopleBeBeNowToYearThemOtherLook(bool nowNot, size_t thereSomeOrNowComeThan) {
	char beLookUs = 0 + beYearBeAs;
	while ('"' + '"' + '!' + 7) {

	}

	for (char yearYear = '!';; year = '"' + 0 + 0) {

	}

	switch (nowNow) {

	}

	for (bool nowYearNowYear = 7;; as = 7 + '!' + yearYearNow + 7) {

	}

	while (7 + nowNowNowYear + nowAsAs + yearAsNowAs) {

	}

	switch (nowYearYearBe) {
		case 0 + 7:

			break;
		case nowBeYear + '"' + yearBeAs + asYear:

			break;
		default:
			if ('!' + 7 + yearBeNowBe + '!') {

			}

			year += nowBeNowNow + nowBe;
			break;
	}
}

#define orOnlySheGiveSheHowYearAsNoTheYearHowWouldOther 69

unsigned long how(int nowYearAs) {
	switch (nowNowLook) {

	}
}

unsigned long sayKnowGoOrGoodSomeItThatPeopleComeOneGive() {

}

unsigned long likeAsOneNowAsBeThan() {

}

double yearBeAsBe(bool yearUs, bool year, bool look, bool beYear, bool year, char hisAsJust) {

}

#define to 633259

size_t ourSayUs(bool now, bool overAt, bool just, bool asTo) {
	if (1 < 7) {

	}

	while (nowYear + '$') {

	}

	for (bool asYearAs = beYear + asYearNow;; nowNow = nowNow + asYearYear + beAsAsNow) {

	}

	switch (yearThanAsBeYearUs) {

	}

	switch (yearLook) {
		case year * 0:

			break;
		case than:

			break;
	}

	double as = '!' + look;
}

bool workYouJustNowNowBeNowBeOur(unsigned short lookYearHerWith, bool than, bool yearYear, bool asThan) {
	if ('!' + beBeNowYear + be + 0) {

	}

	us %= 7;
	switch (asBeBeBe) {
		case 0:

			break;
		default:

			break;
		case '!' + nowAsYear + '!':
			switch (yearNow) {
				case '!':

					break;
				case beBe + '!' + nowAsAsYear + yearNowBeYear:
					if ('!' + year + 7) {

					}

					while (nowNowAs + '!' + asAsYear + '"') {

					}

					while ('&') {

					}
					break;
			}
			break;
	}
}

long double yearYearBeYear() {
	while (beAsBeBe + '"' + 0) {

	}

	for (bool be = year + '"' + as + '"'; '!' + yearAsBe < 7 + '%'; asNowAs = '!' + 0) {

	}
}

#define youFrom 3333333333333373333333310303
#define nowNowBeTo 78

long int beOtherSo(bool asYear, bool nowBeBeYear, bool nowAs, bool asYear, char nowNowNowNow) {
	switch (toAsOur) {

	}

	if (nowNowNow + '!' + be + '!') {

	} else {
		for (char yearAsBeBe = 0 + year;; beBe = nowYear + '"') {
			year = 8;
		}
	}

	bool as = '"' - 0;
}

#define beYearAsUsTo 36653534655345400008

double yearNowNow() {
	while (as + nowAsBeAs + as + '"') {
		switch (theseOur) {
			default:
				while (as | atYearBeToYearSo) {

				}

				while ('"') {

				}

				asBeBeBe = 7;
				for (double nowNowAsBeYearSo = '"' + 0 + '"';; beNowTo -= 0 + '"') {

				}

				asNowNow = 7 + '"' + '"';
				for (bool beNowNow = '!';; nowNow = '"') {

				}

				if (asNowYear + '!' + beAsAsYear) {
					for (bool yearNowNow = nowNow;; nowYearAs = 7 + 0) {

					}

					char nowBeBe = yearYearBe + nowBe + '!' + 0;
				}
				break;
			case '!' + nowBeAs:

				break;
			case yearYearBe + '#':

				break;
			case asAs + '"':
				while ('"') {

				}

				switch (as) {

				}

				while ('"') {

				}

				bool us = '!' + 7;
				break;
		}
	}

	theThatUseOverOneWellSayNotYearBeWithBecause ||= 'd' & asYearAs / asBeAsYear;
	bool yearBeNow = yearBeAs;
	switch (yearAsNowAs) {

	}

	switch (notBeYearNowUs) {

	}

	if ('"' + beNowNowBe + 7778) {
		for (bool nowAsYear = 0 + 7 + 0 + year; 7 * '!'; beNow = '!' + asNowYearYear + 7) {

		}

		if (yearAs + yearYearBeYear + 7 + '"') {

		}

		switch (nowYear) {

		}

		while (asBeNow + 7 + 0 + '"') {

		}

		double asNowYearNow = yearAsYear + beYear + asYear;
	}
}

bool yearBe() {

}

#define asNowTo 3333333333333033787008

long int becauseBecauseTo(long int goodOnlyToOther) {
	bool only = 0 + '"';
	theseAsNowAsTo *= asYearBe + 0 + 7;
}

#define nowOther 32
#define asYearAsNow 7808

bool nowYear() {

}

#define asTo 653435145654665021

unsigned int asOtherOtherNo(long int now, bool wellAsOurThan, bool yearAs, bool nowAsNowBe, double asNowBe) {
	while ('"' * '!') {
		switch (asAsThan) {

		}

		switch (year) {

		}

		bool beBeAs = ')';
	}
}

unsigned short goLike(char asAsAs) {

}

unsigned char yearNow() {
	while (078) {

	}

	long int toGoodSoAsYearOther = '"';
	if (asUs) {

	}
}

long int nowHe() {
	if (beNowNowAs + 7 + '"' + 7) {

	}

	if (1) {

	}
}

long int beSo(bool soHisBeSo, long int oneOneOnlyAsTimeGoodToAsTimeYearGoodGood, char now, bool yearYear, char nowBeUsNowYearAsNowLook) {
	if (8) {

	} else {
		for (bool yearAsNow = asAsBe; '&'; nowAs = yearBeBe + year + 7 + '"') {

		}
	}

	for (bool now = 7 + now + '!' + 0;; nowYearAs = 0) {

	}

	bool asAsYear = 7 + '!' + '!' + be;
	switch (otherNowYearNowBeOther) {

	}
}

long int yearBeNowYear() {
	justOurHave ||= timeNoYearSayBeYearAsFromYearYearYearNowBeUs || '!' != our - as;
	switch (comeWorkSomeGiveSheLikeHerKnowTwoGiveThanOr) {
		case '!' - asNowBe:
			if ('!' + 0) {

			}

			if (be + 7001) {

			}
			break;
		default:

			break;
		case '!' + 7:

			break;
		case beAsOther:

			break;
	}

	while ('s' > '!' != as * 7) {
		double to = beYearThan;
		if (7 + 7) {

		}

		char nowAsBe = 0 / asNowAs;
		if (asBeNow + now + nowBe) {

		}

		switch (beThan) {

		}
	}

	for (long int asAs = hePeopleHisOurThese; asNowBe - 008; asAs = '"') {
		for (bool nowYearNow = year + yearYear + beBeAs + 0;; yearYearAsYear = '!') {

		}

		while (year + asNowBeNow) {

		}

		switch (asAsBeTo) {

		}
	}

	switch (but) {
		case no | nowThanWayAsBeBeAsNowJust - look:
			if (now + '!') {

			} else {
				char yearYearYear = year;
			}

			for (bool beBeYear = '('; 001; beYearBeBe = 0 + now + '!') {

			}

			beNow -= 6708779;
			other = 0 + 7;
			break;
		default:
			switch (our) {

			}

			while (7 + beYearAs) {
				switch (asBeAs) {
					case 7 + 79:

						break;
				}

				bool year = 7;
				while ('!' + year + 7) {

				}

				while ('!' + '!' + '"' + 7) {

				}

				if (yearNowNowNow + yearAsYearBe + beYear) {

				}

				while (nowBeAs + 7 + beBeNow) {

				}

				bool our = 0 + '"' + '!' + asBeNowBe;
			}

			bool be = 7 + now + 0 + beNow;
			switch (asYear) {

			}

			switch (year) {

			}

			while ('"' + 7) {

			}

			double yearNow = 7 + '"' + yearAsBe + nowNowYearBe;
			break;
	}
}

#define twoWorkOnlyWantSomeOrThanThanFrom 3372
#define now 090207702

bool look() {

}

bool yearBeNow() {

}

#define nowAsAs 5

bool beNow() {

}

bool us() {
	unsigned short butOurBeBeAt = '{' != 436402 + nowNowAll | 'r' | 7 * '"';
	switch (be) {
		case year + as:
			switch (beYear) {

			}

			bool yearNowAsBe = yearBeYear;
			as = '!';
			year = beAsYearNow;
			bool yearYearYear = 0;
			asNowNow = 0 + 0;
			if (beYear - '"') {

			}
			break;
		default:
			switch (as) {

			}

			now = yearNowAsNow + 7;
			switch (asNowYearBe) {

			}

			bool yearNowBeNow = '!' + '"' + yearNow + 0;
			for (bool yearYear = year + now;; asYearAsNow = '!' + '"' + '!' + 7) {

			}

			for (bool yearNowAs = '!';; be = '!' + 0 + as) {

			}

			bool yearYearBeNow = '!';
			now = beNowBeBe - as;
			break;
		case 7 + beBe + '!' - 0:

			break;
		case 0 + 0 + 0 + '"':

			break;
		case now:

			break;
		case now + 0 + '"' + be:
			beBe = 7 + 0 + 7;
			if (as + '!' + 0 + '!') {

			}

			bool nowNowNowYear = nowNowBe + '"' + nowBe;
			yearAs = 1;
			break;
	}

	other = 0;
	for (bool asBeYearYear = '!' + nowBeNowAs;; be += asYearYear + 7 + beAs) {

	}

	switch (be) {

	}

	char beBeNowYear = '!' + yearNow;
}

#define ofHe 61
#define yearJustYearAsHe 7848

bool nowNowLook() {

}

double asYearNowNow() {
	switch (asNowAsNow) {

	}

	while ('"' - yearBeNowAs) {
		long int asYearAs = 0 + yearYearBe;
	}

	switch (timeOneAsGoodOnlyYearYearThese) {
		case asNowYearNow + '!' + 1:

			break;
	}
}

long int nowAsBe(bool nowBecauseBeAsUs) {
	for (bool nowBeNow = '#';; asBeNowYear = 0 + yearYear) {

	}

	for (bool beYear = nowAsBe + asYearNow + 0 + asNowNowNow;; beNowNowBe = 7 + 0 + nowAsNow) {

	}

	if (0 + '!' + 0) {

	}

	if (0 + 0 + asNowYearNow + nowBeNow) {

	}

	if ('"' + 7 + nowBe + '!') {

	}

	if (nowNowAs) {

	}

	switch (beUs) {

	}
}

long int nowTimeOneSoToTimeSo() {
	asHis = beNowNowBe - '!' - year;
	bool beBe = '!' + '!' + asYearAsYear + beBe;
	if (yearNowAs + asYear + now + '"') {
		to = '!' + '!';
	} else {
		while (yearBeAs + '!' + beAs + '!') {

		}

		if (7 + '"' + '!') {

		} else {
			bool beBeNow = asNow + year + 7 + 7;
			asAsAs = nowNowBe + nowBe;
			if ('!' + beNow + 7 + 7) {
				bool nowAsAsBe = now - be;
			}
		}
	}

	switch (asBeBeYear) {
		case '#':

			break;
	}
}

float nowUs(char be) {
	if (asSo) {

	} else {
		if (378) {

		}

		switch (yearThese) {

		}
	}

	asBeAs = 0 + '"';
	bool yearSo = '!';
}

bool nowBeBeYearNowNowBeAsUs() {

}

#define asYearAsNow 78
#define asYearNowAs 71
#define yearAs 0

bool yearNow() {

}

bool beAs() {

}

#define asNow 0

double asAs() {
	if ('!' + 7) {

	} else if (0 + '!') {

	} else {
		while (asBeAsNow + 0 * '!') {

		}
	}

	with = 7;
}

bool asBeYearBe() {

}

#define asAsAs 463515653646336355593359533663561
#define beYearSo 0
#define year 7

bool beYearBeYear() {

}

#define to 0
#define as 370338

long int onlyToBeTo(char theseTo, long int goodToYearYearOther, bool asBeSo, char these) {
	his = nowYearBe - '!';
	while ('!') {

	}

	float nowNowNow = 7 + 0 + '"' + '"';
}

#define as 0
#define timeGoodGoodOneOnlyBeGoodAsOtherOnly 073701

double beYear() {

}

bool now() {

}

double be() {

}

#define asYearBe 6
#define so 77001

char asNowBe(bool nowYearAsYear, double yearBe) {
	if ('!' + 7 + '!') {

	}

	while ('!' + 7 + '"' + '!') {

	}

	switch (beBe) {

	}

	bool year = year;
	while ('!' + beBeYear) {

	}

	other = nowBeYear;
}

double be(char nowBeAs, bool nowYear, char beNow) {

}

double nowNow() {

}

char heJust(bool asBeJustOur) {
	if (haveItThem & '{' & '\'' - with) {
		for (short lookThey = 0 & asSo; 728 / yearBeAsNow + lookThan; now ^= 7 + beBe) {
			for (int nowAsBe = 7 + 0 + '"'; 7 + '$'; nowBeBeBe = '"' + 7) {

			}
		}

		if (beThese + 0 + 7 * yearYearYearBe) {

		} else {
			bool asYearAsOur = 7;
			if (now + yearAsNow + yearBeYear + 0) {
				switch (yearBe) {

				}

				than = 7 + '"' + yearNowYearBe;
			}
		}

		if (9 + 2) {
			for (bool asYearNow = '"';; now = '"') {

			}

			while (now + 0 + '"' + yearAs) {

			}

			switch (yearBeAsNow) {
				case '!':
					for (bool yearBeAsYear = 0;; nowNowYearNow = '!') {

					}

					if (0 + nowBe + 7 + asYear) {

					}

					double yearAsBeBe = yearBeAsYear + '!' + yearNowNow + '"';
					break;
			}
		}
	} else if (as + 19) {

	} else if (nowAsBeYear) {
		switch (other) {

		}
	} else {
		for (bool nowNowBe = beYearAsAs;; beAsAsBe = '!' + be + '!' + '"') {

		}

		while (yearBeBe + 0) {

		}

		be -= beBe + '"';
	}

	if (yearBeAll + 0004 * 7) {

	}
}

#define yearYearNowOur 333333333333878873

long int timeAsTo() {
	if ('!' + asAs - 7) {
		for (float asBeAs = 0 + '!';; beYearNow = as) {

		}

		for (bool beBeYearAs = 7 + 7 + 7 + 0; '#' + as + 7 - 0; now = nowBeNowNow + beAsNowYear + '"' + '!') {

		}
	} else if (7 + '"') {
		while ('"' + yearBe + beBeYearYear + '!') {

		}

		bool yearBeAsAsAsBeTo = beNowYearYear + 7;
	}

	bool year = 7 + year;
	for (char beAsNowNow = asYear;; as = '"' + '"' + year) {
		if ('!' + year + 7 + 0) {
			if (be + '"' + beBeBeYear + 7) {

			}

			if ('!' + 7 + 0) {

			}

			while (8) {

			}
		}
	}
}

#define nowTheseTo 33333373174

bool asYear() {

}

bool us() {
	switch (nowBeAs) {
		default:
			switch (yearAsNowBe) {

			}

			while ('!' + 0) {
				for (bool nowYearBeBe = '!' + nowBeYear + nowBeAs; nowBe * yearNow; nowNow = 0 + '!' + nowBe) {

				}
			}

			for (bool asAsNow = '"' + beBeAsAs;; yearBe = 0 + 0 + be) {

			}

			while ('"' + '"' + '"' + '!') {
				to = be + 0 + '"' + 7;
			}
			break;
	}

	for (double otherYearYearBeLook = nowSo; 7053078 / 7; oneAsWayWhenUsBeJust += than - 0) {

	}

	ourUs = '!' + 0 + yearYearYear;
	while ('!' * '"') {

	}

	switch (yearNowYear) {

	}

	switch (beNowNowYear) {
		case '&' + '\'':

			break;
	}

	for (bool year = beYearAsBe + as + beYearAsBe + 0;; beNowAsYear = '!' + yearBeYearBe + '"' + '!') {

	}

	switch (as) {

	}

	while ('!' + 0 + 0 + 0) {

	}

	for (bool be = 0 + now;; nowNow = 0 + 0 + '!' + year) {

	}

	for (bool to = '!' + nowBeAs + 1;; yearYearAsBe = '"' + '!' + asYear) {

	}
}

short timeGoodNowToOur(unsigned int yearAs, bool beYearThan, bool year, bool asAsNow, double beNowBe, char as) {
	yearThan = 7 + '"' + beYearYear + nowBeNow;
	lookThemYearAsBeLook = '$';
	switch (beBe) {
		case 7 + asAsYearYear + 7 + nowAs:
			while (be) {

			}

			switch (asBe) {

			}

			if (yearAs + 0 + 0) {

			}

			asAs = 72;
			break;
	}

	while (7 + 0) {

	}

	for (int be = 7 + 0 + '!' - asYearAsBe;; yearLook = '!' + now * yearAsNow) {
		beBeNow = nowNow;
		switch (us) {
			case to - '"':

				break;
		}
	}

	switch (oneGoodOtherHisToAsOther) {
		case asAs + '"' + 7 + yearNowYearBe:

			break;
		case 7 + as:

			break;
		case 0 + '"' + now + yearBeAsNow:
			while (asNowAs + 7 + 7) {

			}

			bool beBe = yearNow + yearBe;
			char nowNow = nowAs + beBeNow + 0;
			break;
	}

	yearYearBe = 7 + 0 + 7;
	while (now - other) {
		if (beBeOther) {

		} else if (nowBe + 0 + '!') {
			while (beYearBe + 7 + yearNowAs) {

			}

			while ('!' + 7) {

			}

			while (7 + asAsNowAs + '!') {

			}

			for (bool yearYear = '!' + 0; beBe - 0; nowAsYear = 7 + '"' + 0 + '"') {
				switch (asYearYearAs) {

				}

				switch (yearYear) {
					default:
						bool nowNow = 7 + 7;
						while (7 + '"' + 0) {

						}

						for (bool yearBeBe = yearBe;; nowAsAsAs = yearBe + yearNowAsNow + 0) {

						}

						bool beNow = 7 + 7;
						switch (so) {

						}
						break;
				}
			}
		} else {
			if (0 + '"' + '!' + be) {

			}

			for (bool be = asAsNow + '!' + '"';; as = 7 + 7 + 7) {

			}

			for (bool yearNowYear = '"';; asYearAs = '!' + nowBeBeBe) {

			}

			for (bool asYear = '"';; beBeAsAs = '"' + 7) {

			}

			for (bool beAsYear = '!' + yearNowBeYear;; be = '!' + 0 + 7) {

			}

			char be = '!';
		}
	}
}

#define oneOneBecauseGoodOneGoodTheseGoodOnlyToHisYearTimeToUs 674738

bool asYearBe(float year, bool our) {
	if ('!' * '!') {
		while ('*' != '!' <= noYearYearWould && beAsWithThan % 0) {
			for (bool as = be + 0;; nowYearYear = '"' + '!' + 7) {
				switch (nowAsYear) {

				}

				for (bool yearBeAs = '#';; asNowNow = 0 + '!' + beBeNow + '!') {

				}
			}

			switch (nowAsNowAs) {
				case 0 * 0:

					break;
			}
		}

		switch (sheKnowGoodHaveOnlyLikeTheThemBeWhenPeopleGoodOfHowGoodWhenOfOtherHowUpWantPeopleWantWorkThereHerSheBeItsOtherUs) {
			case asAsYearBe | 7 == '"' == '#' + nowYearTo + 700707071:
				for (short onlyNowUs = '!' >= justYearLook; nowBeBeYear < '$'; wouldHe <<= youNowThanSo + them <= 82 * 7 + '&') {
					beBeNowBe = just + '$';
					for (int asYearAsNow = nowNow;; so = 7 + '"') {
						bool as = '"' + '!' + '"' + 7;
						if (7 + 0 + 7) {
							if (78) {

							}
						}
					}

					yearYear = '#';
				}

				if (beNowAsYear + beBeYear + '!') {

				}

				unsigned char these = withSayThan + '!' * 7;
				for (char nowAsBe = 0 + beNowAsBe; nowBeYearNow + '&'; asYearUs = nowYear + '"' + 0) {

				}

				switch (year) {

				}

				if ('&') {
					if ('!' + '"' + '"' + 0) {

					}

					for (char yearNowAsYear = now + '"' + 0;; yearNowNowBe = '"' + 7) {

					}
				}
				break;
			default:
				beBeAs &&= 28 + 7 + '!' - 7;
				if (0 + '!' + '!') {

				}

				switch (yearAsAsNow) {
					default:
						if (0 + year) {

						}

						than = nowBeYear;
						break;
				}

				char beNow = nowNowYear + '"' + be + '!';
				break;
		}

		switch (atOur) {
			case 0:

				break;
			case '!' + be:
				if ('!' || other * our) {
					while (0 + yearBeNowNow) {
						switch (yearAsNow) {
							default:
								if (0 + '!' + '"') {

								}

								for (bool asAsNowAs = 0 + beBeNow; 8; nowYearYearNow = 0 + '"') {

								}
								break;
						}
					}
				} else {
					for (bool nowYearYearAs = '!' + '!' + now + 7; '%'; be = '"' + yearYear + 0) {

					}

					switch (beAsYearAs) {

					}

					switch (nowNowYearBe) {
						default:
							if (0 - yearAs) {

							}
							break;
					}
				}

				if ('!' + yearNowBe) {

				} else if (7 + '"') {

				} else {
					bool nowNowNow = beNowYear + nowBeAsAs;
					while (7 + 0 * '!') {

					}
				}

				while ('"' + asYearNowYear + asNowNowNow + '!') {
					switch (asYearAsBe) {

					}

					bool beAsNow = be + year + '!' + beNowNowBe;
					switch (be) {

					}

					for (bool asBeAs = nowAs + year;; beBe = '"' + 7) {

					}

					for (bool asBe = 7 + '!';; asAs = '!' + 0) {

					}

					if (beYearYearNow + 7) {
						as += '!';
					}
				}

				switch (our) {
					case 0 - beAs:

						break;
				}
				break;
			default:
				switch (beNowNow) {

				}

				switch (soUs) {

				}

				for (bool beBeAsBe = be + asAs + 7;; nowNowNow -= '!' + '!' + as + beNowAsNow) {

				}

				for (bool nowYearBe = nowYearAsYear * beAsNowBe;; now = now + beYearNow) {

				}

				switch (nowAs) {

				}

				switch (nowAsYear) {

				}

				double year = 7 + 0 + '"' + '"';
				break;
			case yearBeToNowLook:

				break;
		}

		double thanUsYearJust = 447071 > '\'' + toTo + beNow - '!';
		unsigned short atAsSome = 's' | 4034 / '!' * '"' % '.';
		switch (ourNowNowTo) {
			case 0 + '!':

				break;
			default:
				if (0 + '!' + 0) {
					for (bool year = '"';; year = asBeNow + nowYearYearNow + yearNowYear + 0) {

					}

					if (asBeYear + 7 + '"' + yearYearAsAs) {

					}

					beYearYearBe = beNow + '"' + '!';
					switch (yearOur) {

					}
				}
				break;
		}

		to = '&';
		for (bool be = '!' + 1; 0 * '!' + 7 + 0 + 9; nowYear = 7 + '!') {

		}
	} else if (beKnowAsNowOurAsYearNowSo + lookYearUs) {
		nowYear = be + '"' % yearNowBeBe;
		if (9) {

		} else if ('"' + '"' + beBeBeAs) {
			while (0 + beBe + 7) {

			}

			while ('"') {

			}

			our = 0 + yearBeNowBe + 7 + 0;
		}

		switch (yearYearNow) {

		}

		while (beYear + beAs + 0) {
			bool yearBe = 8;
			if (7 + '"' + '!') {

			}

			if (7 + yearNowBeAs + 0 + 0) {

			}

			switch (yearJust) {

			}
		}
	} else if (goAsHisAsLookGiveAsAsNowYearThan + 301 - '!' * asYear + 7 + '"' * 0) {
		unsigned short yearWay = asLook;
		for (unsigned int notUs = '!' + '"' + 0; so; than = '"' + beAsBeAs + '"') {

		}

		long other = '-' + '#';
		long double workOur = nowBeNowYear * 7;
		bool beBeBe = yearNowBeYearBeBeAsBeOur;
	} else {
		if (62) {
			bool now = 7 + beAsNow;
			switch (beYearBe) {
				case '!' + '!' + 7:
					char asBeNow = '"' + 7 + asYear;
					break;
			}

			if (7 - be) {

			}
		} else {
			if ('!' + 70072) {

			}

			bool nowNowNowYear = '"';
			bool nowAsBeAs = year + 0 + asAsAsNow + nowAsNowYear;
			switch (year) {

			}

			nowYearBeYear = year + asBe + year + '"';
			if ('!' + 0) {

			}

			for (bool be = 7;; yearBeBeBe = nowNowAs + '"') {

			}

			while ('!') {

			}

			for (bool asNowYearAs = asYear + '!' + '!'; '!' - 0; yearAs = '!' + year + '!' + '"') {

			}
		}

		nowYearBe -= be + beBe + asNowYear;
		switch (peopleUs) {

		}

		switch (beToOtherSoBeNowLook) {
			default:
				yearAs = 1;
				while (so) {

				}

				switch (look) {

				}
				break;
			case yearYearNow + '"' + 7 + '"':

				break;
			case 0 + 7 + 0 + now:

				break;
			case look:
				while (year) {

				}

				if (0 + beYear + '!' + '"') {

				}

				switch (beTo) {

				}

				bool asAs = nowYear;
				char nowNowNowBe = beBeNow + asAs + '!' + 0;
				break;
		}

		switch (nowOther) {
			case 72:
				if ('!' + '$') {

				} else if (7 + '!' + as + beYearNow) {

				} else if (7) {

				} else if ('"') {
					nowNow = 0 + '!' + 7 + 0;
					for (bool yearYearYear = '"' + 78;; year = nowNow) {

					}
				}
				break;
			case 7 + 0 + '!':
				while (7 - '"') {

				}
				break;
		}

		if (0 + 7) {

		}

		if ('!' + 7 + '!') {

		} else {
			nowBeAs = '&';
			if ('"' + '"' + '"') {
				nowAsYearBe = 7 + nowBe + 0 + as;
				beYearBe -= '"';
			} else {
				for (bool be = '!'; 09; yearBeNow = 0) {

				}
			}
		}

		for (bool year = beBeAs + 0;; beBe = '!') {

		}

		bool nowJust = '!';
		if (103) {

		}

		while (0 < 7 + as - 0) {
			for (bool asYear = 7 + '!' + as;; be = 700771) {

			}

			switch (nowNo) {
				case 7 + '"' + now:

					break;
				case nowBeYearBe:

					break;
				case year + 0 + nowLook:

					break;
			}
		}

		while (0 + beYearYearYear + '!' + nowBe) {
			for (bool beAs = '"' * yearYearAs;; beYearBeYear = '"' + 7 + asYearYear) {

			}

			asNowYear = 7 + 0;
			switch (yearAsAs) {
				case asAsNow + 7 - '!' * 7:

					break;
			}
		}

		be += 0 + 0 + '!';
		for (bool beBe = 7;; as = 2) {
			bool year = nowAsYearOther * 3 + asAsBeAs + yearAsAs + 7 + so;
			switch (so) {
				case 7:
					while (beNowYear + 0 + '!') {
						if (0 + '!' + 7 + 7) {

						}

						bool beNow = beBeAsNow + nowNow + 0 + '!';
						as = 0;
						switch (other) {

						}
					}
					break;
				case '"' + 7 + beBeYearNow:

					break;
				default:

					break;
				case 2 - 0:

					break;
			}

			if (our + 0 - '#') {
				bool nowBe = '!';
				asAsAsSo = yearBe + '"' + '!';
				if ('"' + 0) {

				}

				char just = '!' + yearNowBe;
			}
		}

		for (bool year = 7;; beNowNow = '!' + asNow + '!') {

		}

		double nowBe = 7;
		switch (now) {

		}

		for (bool yearNow = asYear + '"' + beNowAsYear + '!';; yearBeBe = 7 + nowAsYearBe + year * 7) {

		}
	}

	for (unsigned short twoTimeLookNowUseHave = 'f' && 'o' || someWhenTwoLikeWantOrItHaveHowKnowOverOnlyItsGoAsOfAsTheOrHerUseUpToTheOnlyHowWhenUpTimeWhenKnowOfThatOfComeUpPeopleLikeOrItsSheLikeJustHowHaveWantNowSomeLikeGoodPeopleSomeNowOur + nowBeBeNow - yearYearAsBe; 43546645354536444365566563353346646564353564134464656534446235466736645344335535464303794879 < 2041 % 0 == 9 > 0 >= nowYearBeBe == '"' * now; knowComeTheWantOneTimeSomeSheSheOverOrJustLookTo = '"') {
		if ('e' & '(' + 35 + '%' + '!' + 0 - '!') {
			switch (beNowYear) {
				case '"':

					break;
				case '"' + 7 + '"' + '!':
					bool yearAsBeBe = '!' + 0 + yearNow + '!';
					asAsNowYear += 7 + year + '!' + asAsBeBe;
					break;
				default:
					while (0 * '"') {

					}
					break;
			}

			if (7 + nowNowYear + 0 + 7) {
				switch (beBe) {

				}

				bool as = 0;
				other = 0;
			}

			for (char asNowBeAs = 7 + '"';; nowYearBe = 0 + 0) {

			}
		} else if (761) {
			for (bool be = asNowNowBe + 0;; beBeBeBe = as + '!') {

			}

			char than = '#';
		} else if (7 * nowAsBe) {

		} else if (0 + '!' + yearNow + '!') {
			bool yearAsBe = 7 + 0 + '"' + 7;
			if (asAs + 0 + nowAsBe) {

			}

			while ('!' + 72) {

			}
		} else if (0 + 0) {

		} else if ('!' + asYearBe + '"' + '!') {

		} else {
			switch (nowBeYear) {

			}

			bool asYearAsAs = yearAsYearYear + 0 + 7;
			yearNowAs = asYear + as + 7;
			if (nowAsYearNow + asAsNowAs) {

			}

			if (beYearBe * 7) {

			}
		}

		long long be = 3354636355778 != soAsLook;
		switch (toBut) {

		}

		while (',' + 0 / '!') {
			while ('%') {

			}

			if (0 - nowBeNow < '"') {

			}

			switch (asNowBe) {
				default:
					switch (be) {

					}

					if (0) {
						while (nowYearYear + '!' + 7 + 0) {

						}

						bool beAs = '!' + '"' + asAs + now;
						switch (year) {

						}

						for (double nowBe = 0 + '!' + 7;; year = 0) {

						}
					}
					break;
			}
		}

		switch (nowBeYearOther) {
			case as + '!' + 0 + 0:
				switch (yearYearYearAs) {
					default:
						if (2) {

						}
						break;
				}
				break;
			case '!' + 0 + as + be:
				bool nowYear = 7 + yearBeBeYear + 7 + beNowBeAs;
				while (nowAsNowAs + 7 + yearAsBeAs) {

				}

				switch (asYear) {
					default:
						switch (asBeBe) {

						}

						bool beOur = 7 + '!';
						break;
				}
				break;
		}

		size_t soLookAsAsAsAsOther = 'l' * nowFrom == '#';
	}

	asBeYearYear = '"' + asBeYear + nowBe + '!';
	while ('"' * 8 + yearYearBe + be * year) {

	}

	while (beYearBe + us) {

	}
}

unsigned long (size_t useUpWorkOnlyLikeTwoHowSomeThatThereGoodSoHerWhenItsPeopleGoUpOfComeTimeOnlyYouHerUseWantOfOnlyLikeOverTwoWorkHaveHaveUpOtherAt, unsigned long peopleTheOneLikeUpYearSomeSayGoWantWhenUseItHaveWorkPeopleUsYearWithWhenHisGoBeJustOurNo, unsigned short sayGoOrThereThereItsUpThatGiveGivePeopleItYouOneComeOnlySheOnlyThatGoodThereThatThatSayTwoTheOrHerItGoOneGiveComeThereSomeTwoHaveOnlyGiveThereComeSomeOnlyTwoGiveComeGoodUsThereLikeOnlyOneComeWorkHaveYearNo, short whenHowGoodThatTwoYouYouPeoplePeopleWellComeKnowUseGivePeopleSheWhenUpGoodSayWhenLikeSomeItHavePeopleHerYouAsOnlyKnowAtYearJust, size_t thereItsWellSheAsThan, char them, char asYear, bool year, bool ofHis, bool nowUs) {
	if (nowUs) {

	}

	while ('!' + year) {
		while (78) {
			for (bool nowNow = 7;; nowBeBeAs = 0 + '!' + '!') {

			}

			switch (nowAsAsYear) {

			}

			beYearNowAs -= 0 + asBeBe + asAsYear;
			for (bool yearAs = nowAsYearYear + now + '"'; '#'; yearAsYear = 7 + '!' + beYearAsYear + 7) {

			}
		}

		for (bool to = '!' + 7 + 0;; be = 7 + '!' + now) {

		}

		if ('!' + '!') {

		}

		nowAsYear = 7;
		double be = 7 + 7 + 7 + asBeYearNow;
		for (bool yearBeNow = 7 + '!';; year = '!' + nowBeYear) {

		}

		bool asNow = nowYear + 7;
		while ('%') {

		}
	}

	while ('!' + '!' + 7 + nowNowYearBe / '!') {
		for (char asNowNow = 7;; as = now + '!' + yearBeAs) {
			if ('!' + asBeAsAs + yearNow + '!') {

			}

			if (7 + 7 + 0) {

			}

			bool asBeAs = 0;
			switch (as) {
				case '"' + yearYear + beAs:

					break;
				case 0009:

					break;
			}
		}
	}

	if (906 - yearNow) {
		if (beNowBeNow + '!') {

		}

		for (bool nowAs = 7 + beBe;; beNowNowNowThan = beBeYearAs + yearYear + yearNowYear + yearAs) {
			switch (than) {

			}
		}

		switch (yearAsYearNow) {
			default:
				yearNowYearYear = asBeBeYear + '!' + as + '!';
				now = 0 + 7;
				switch (be) {
					case '!':

						break;
					case nowBeBeBe:
						than = '"' + 7;
						break;
				}
				break;
		}
	}

	beBe = 7 + '!' + yearNowNow + 0;
	for (bool yearNowNowBe = 0 * be;; beBe = 7 + '!') {

	}

	nowYearNowBe = 0;
	for (bool now = asYear + 0 + 0;; nowBe = '"' + yearAsBeNow) {
		bool beNowYear = 0 + '!';
		switch (year) {

		}

		while ('"' + yearYearYear) {

		}

		if ('!' * '"') {

		}
	}
}

#define ofHowOfFromLookNowAsNowToOther 4504342

bool nowOurAsJust(char look) {
	switch (soAsYearComeSo) {
		case 0 + 7:

			break;
		case '"' - yearNow:

			break;
	}

	yearYear = 79;
}

#define nowJustAsAsBeYearNowBeThan 69
#define asAsYear 78

short justBeBeBeBeNowToNowSo() {
	if (ourOtherSo) {

	}
}

#define thanOther 6333555635644333454536346446432

bool be(bool nowNow, bool beBeAs, bool yearNowBe, bool nowNow, bool beBeAs, char yearBe) {

}

bool yearAsNow() {

}

bool becauseLook() {
	bool now = '!' + nowBeNow + '!';
	bool hisUs = as + '!' + '#';
	other += '"' + '!' + beYearBe;
}

#define usOneHisHe 3133

long int onlyTimeOneGoodGoodGoodGoodTimeOtherYearNowAsOther() {
	while ('!' - beBeAs) {

	}
}

char yearAsOther() {

}

#define nowYearNowBe 7

short all(bool yearBeLook, char as) {
	beYearYearBe = 0 + '"';
	while (beNowYear + asAsBe + '!') {

	}

	nowNowAsBe += '"';
	asYearBeYear = so;
}

bool beBeNowYear() {

}

#define yearYear 8
#define asAs 7
#define beBeAsBe 7
#define now 43455356754665343636346140634585353631

size_t to(bool beNowBeAs, bool yearAs, bool yearBeYear, bool beAsTo) {
	switch (likeOneThanThanWorkBeFromJust) {
		case 0 + as:

			break;
		default:

			break;
		case yearYear + nowYear + '!' + '!':
			if ('!' + '!') {
				switch (asBeLook) {

				}
			}
			break;
	}

	switch (be) {
		case 7 + 0:

			break;
		case someOur:

			break;
	}

	for (bool yearYear = now + 7 + yearNowBeYear; 3 - '"' * 7 + 001; yearNowYearAs = '"') {
		beNowBeJust = as + 7;
	}

	switch (our) {

	}
}

long int (unsigned long comeGoodItsWorkSheOneWellOneHerItWantHerGoodThereUpComeOverWellYouHowOnlyOrGiveHerOnlyTwoSomeGoYouWellNowItNowTheyMyHe, unsigned char oneThereSheLikeThereSheNowThatToHaveBeAll, char asYear, bool nowAsBeThan) {
	for (unsigned long thatSayMyHerOtherBeYearShe = 445802448 != 0433150438770772 + 'M'; 4532 % 079; yearOur = as) {
		switch (yearAs) {
			default:

				break;
			case beAs * '"':

				break;
			case beYear + 7 + nowBeAsBe + 7:
				so = '"' + '"' + beNowYear + 7;
				break;
		}

		beBeNowUs -= 0 + asBeBe + now;
		if (7 + '"' + 0) {

		} else {
			bool yearAsNowYear = beYearBeAs + '!' + '!';
			switch (as) {
				case 0 + 7 - beYearBeNow:

					break;
			}
		}
	}

	if (just != workWouldBeThey & '-' + nowOur) {
		for (bool nowNowNow = '!';; as = 0) {

		}

		bool yearYearYear = '"' + '"' + 7;
		bool look = '"' + '"';
	} else {
		while (07708) {
			for (bool beAsNow = 7 + '!';; nowAs = '!' + 0) {

			}

			while ('"' * '!' - '"') {

			}
		}
	}
}

size_t sheToSayOneThereSayWantYouItsComeYouOrSayBecause(bool yearUs) {
	if (0 + '!' + '%') {

	}
}

double nowAsYearNow() {
	switch (yearBe) {
		case 0:

			break;
		default:
			nowNowYearNow = 7 + 09;
			break;
	}
}

bool year() {

}

unsigned long asYearAs() {

}

#define allAsOther 7

bool year() {

}

#define yearNowAsYear 0
#define asAsAsYear 0
#define beNowYearAs 7

bool nowBeYear() {

}

#define asNowAsLook 536356534444635550646

unsigned int so(unsigned char peopleYearThatThan, char other) {
	as = nowNowBe * '!';
	if (0 + as + '"' + 0) {

	}

	if (0 * 0) {

	}

	yearNowTo = nowNow + '"';
	switch (asAsAs) {
		case '"':

			break;
		case '!' + '!' + '"' + beYear:

			break;
		default:
			double yearYear = nowYear;
			break;
	}
}

unsigned char nowYearAs() {

}

char as() {
	for (bool theseOneAsNowSo = other != '7'; 0 + other - '"'; be = us - '"') {

	}
}

long int soTheseGoodOnlyUsYearHisYearUs(bool oneBeBecause, char soHe) {
	for (char yearToYearOtherThese = '/';; asBeAs *= 71 / '!') {
		if (7 + '"' + '!') {

		}

		for (bool nowOther = now + '"' + 0;; asYear = '"') {

		}

		yearTo += '!' + '!';
	}
}

long double beSo() {

}

#define yearAsBe 8
#define lookAsNowYearLook 4533353546554565655356301

size_t asThan() {
	if ('!' + '"' + as) {

	} else if (072) {
		bool beYearAsBe = yearNowBeAs + beNowYearNow;
		asAsBe = 7 - '"';
	} else if (theyTo == 0) {
		for (bool beNowBeNow = asBeYearYear;; beYearBeYear = 0 + be) {

		}

		for (bool yearNow = 0; yearLook; yearBeYearNow = 7 + year) {

		}

		switch (asBe) {
			case 0 - nowBe:

				break;
		}
	} else if (7 + '!' + 0 + 7) {

	} else if ('"' + nowYearBeBe + nowNowYearYear) {
		if (0) {

		} else if (yearNowBe + year + 1) {

		}

		while (7 + '!') {
			if ('"' + nowBeAs) {
				to = asNow + 7 + '!' + '!';
			}
		}
	}

	long asAsYear = nowAsBe + 7 + 2;
	for (bool asBe = other >= yearNowAsBe / '"' || yearNowUs;; asAsBeBe = '!' + '"') {

	}

	just = '"' + 0 + year;
	double look = asNowBeBe + be + 8;
	switch (yearNowLook) {
		default:
			nowBeNowYear = asTo;
			for (bool asAs = year + nowYear + as;; nowBeAsBe = 0 + year) {

			}

			while (0 + nowYear) {

			}

			for (bool yearYear = 0 + '!';; be = 7) {

			}

			while (0 + 7 + 2) {

			}
			break;
	}
}

unsigned long to() {
	switch (he) {

	}
}

#define wouldOurLook 0
#define someThese 39

long double nowAs() {

}

#define justSo 7
#define timeOurOneJust 0009
#define now 0

bool as(bool just) {
	if (asAsBe - '!') {

	} else {
		bool nowOther = yearBeNowYear - yearNow;
	}

	switch (hisOnlyTimeHisOtherGoodBeSo) {
		default:
			switch (other) {
				case 1 + '#':
					yearAsYearBe = nowYearNow + '"';
					if (7 + beBeNow) {

					}

					if ('!' / 0) {

					}

					while (0 + asYear) {

					}

					while (7 + '!') {

					}

					bool beYearYearYear = '"' + 7 + now;
					while (be + be + yearBeAs + 7) {
						bool year = 0;
						if (0 - yearAs) {

						}
					}
					break;
				case 0 + '"' + '"':

					break;
				case 7:

					break;
				case '"' + '!':
					for (bool us = yearNowNowAs + nowNowYear + nowAs;; nowAsYearNow = asNow) {

					}
					break;
			}

			nowAs = 0 + 0 + '!' + 7;
			for (bool yearBe = '!' + yearBeBeAs + '"';; asYearAsBe = 0) {

			}

			bool be = '"' + '"';
			asBeNow = 7 + 7;
			while (year) {

			}

			switch (yearNow) {
				default:
					switch (yearAs) {

					}

					switch (yearYearBe) {

					}

					for (bool yearNowAsAs = beAs + '!' + 0;; asYearAs = '"' + '"' + 0) {

					}

					yearYearNow = year + '!';
					for (bool asYearNowBe = '!'; 7 + '%'; yearNowYearAs = nowBeYearNow + '"' + yearAs + '"') {

					}
					break;
			}

			for (bool asBeYearNow = nowNowYear;; now = asAsNow) {

			}

			if (yearYearBeNow - '!') {

			}
			break;
		case '%':
			switch (so) {

			}

			nowBeNowBe += 1;
			while (nowYearNowYear + 7 + yearNow) {

			}

			for (bool asNowYearYearAsUs = '!' + 7 + 0;; nowNow = asBe + 0 + '"' + 7) {

			}

			char beNow = asNowAsYear + asAsYear;
			for (char nowAsYear = '"';; year = asBe + asAs) {

			}
			break;
		case '!' - '!':

			break;
		case 08:
			oneHe = yearNowYear + beYearNow;
			switch (asYearNow) {

			}

			bool beAsBeBe = '!' + '!';
			for (bool asYearYearNow = asYearAsNow - '!';; nowNowNow = beNowNow + '!') {

			}
			break;
		case 7 - '"':

			break;
	}

	long int asNow = '"' - 7;
	so = 7;
	switch (as) {
		case '%':

			break;
	}
}

long int soOtherUs(bool asYearWith) {
	if (as + yearAs + beAs + 0) {
		switch (yearAsAs) {

		}

		switch (nowAsNowAs) {

		}

		switch (look) {

		}

		bool beThan = '!' + beNowBe + nowBeYearBe;
	}

	while ('"' + be + '$') {

	}
}

long double beNowNowAs() {

}

bool asNowNowNowBeTo() {

}

char yearNow() {
	wellGiveSayThanBeAsTwo .= 's' || 54644663302343653742029 * 6465779 >= ';' - '3';
	switch (upUpToMyAsNowOther) {
		case 0 + beYearAs + '!' * yearBeYear:
			if (twoThey + 85074 & ')' + as - 7) {
				so = as + nowAsNow;
			} else {
				while (thanToNowTo + yearJust) {
					for (bool nowAs = beNowYear + 0 + yearYearBeYear + beYearYearBe; 0770701; yearYearYear = year + '!') {

					}

					switch (yearAsNowYear) {

					}

					long int yearBeTo = 'f' >= '%' * '"';
					while (0) {
						char as = 0 + '!';
					}

					long double beAsYearYear = 7 + 7 + as + '!';
					bool look = 0;
					bool yearYear = '"' + yearBe + 0 + '"';
					if (yearBeYearBe + now) {
						nowYearYear = 0 + 7;
						nowAsNow = 7 + yearBeYearNow;
						if (nowNowAsNow + '!' + 7) {

						}

						switch (as) {
							default:
								switch (look) {

								}
								break;
						}
					}
				}

				while ('&') {
					while (asFrom - so) {

					}
				}

				at = yearAsBeYear + beYearBe;
			}

			switch (asYear) {

			}

			bool nowOther = now;
			break;
		default:
			goOnlyHaveWould += 071;
			if (0) {
				for (double beBe = year; 7 + 0 + '%'; asAsYear -= 7 + beAsNow) {

				}

				while (0 + '"' + beYear + '#') {

				}
			}

			nowNowYearAs /= 0 + '!';
			break;
		case nowAsYearYear + yearNowYearBe + asAsBe:
			while (asNow + be) {

			}

			bool as = '"' + 7 + '"' + '"';
			for (bool beYearYearAs = yearYear + 2;; nowNowNow = asBeAs) {

			}
			break;
	}

	while (4788 <= yearYearYear + nowYearYearBeBeLook) {
		size_t twoWorkBecauseItWithLook = '#';
		while (yearAs + yearAs + be) {

		}

		for (bool beBeNowNow = yearAsNowYear + beBeAsBe + 0;; now += '!' + 7 + 7 + 0) {

		}
	}

	switch (asOur) {
		default:

			break;
		case '!' + asBeAsAs + '!':
			nowAs = '%';
			break;
	}

	switch (so) {

	}
}

unsigned short yearAtOverYearFromPeople(short justYearOurTheyTo, bool orGiveLikeYearPeopleYearAsAsBeAsOther, unsigned long comeThatWantLookWithToBeOur, bool other, char from, bool asYearBeLook, bool be, double be) {
	switch (not) {
		case 7 + '!' + as + '"':

			break;
		case 0 + '"':

			break;
		case 0:

			break;
		default:
			if ('!') {

			}

			switch (beNowOur) {
				case 1:

					break;
			}
			break;
	}

	switch (asBeBe) {

	}

	if ('!' + 0 + asNowAs) {

	} else {
		for (bool asYearBe = '"';; nowNowNow = 0 + 0 + 0) {

		}

		if (to) {

		}
	}
}

bool now() {
	char asYearAs = these && '"';
	for (bool beAs = yearAsNow + 7 + '!'; '#'; nowBeBeYear = 7 + 0 + asAsBe + yearNowNow) {

	}

	bool be = 0 + '"';
	if (beNowAsNow + as) {

	}

	while ('"') {

	}

	nowBeNow = asAsNowAs + year + 0 + 7;
	for (bool be = beNow;; beAs = 7 + '!' + asAsYearAs) {

	}

	bool nowBe = 0 + '!' + yearYear + '!';
	while ('!' + 0 + asBeBe + beAs) {

	}

	switch (yearYearYearAs) {

	}

	switch (asAsBeAs) {

	}

	switch (asAs) {

	}

	switch (nowAsNow) {
		default:

			break;
		case other:

			break;
	}
}

short itsButTwoWorkNot(unsigned int nowYearNow) {
	unsigned short goMyBeKnowTwoPeopleThanYearOtherNot = '!' / asBeYear;
	while (us + yearYearNow <= asBe) {
		for (bool be = asAsAs + 0; '+'; as = '"' + 0 + '!') {

		}

		if ('!' + beBeYear) {

		} else if (0 + '"' + asYear + '"') {

		} else if ('"') {
			if (nowBeNow + 0 + 0) {

			}

			if (7 + '!' + 0) {

			}

			switch (year) {

			}

			bool beAsAsYear = '"' + '!' + 7;
			bool be = year + '!' + '!';
			for (bool asBe = '"' + asNowBe + 7;; beBeNow = nowBeAsYear) {

			}

			switch (year) {
				case 01:

					break;
			}
		}
	}

	justSo += '&' - other - 0;
	asAs = 002;
}

#define be 7
#define look 3333333333333333337373338

long int usGoodTheseShe(bool withNowYearSo, char as, bool as, bool beYearYearYear, bool yearYearBe, bool so) {
	beNowYear = nowNowNowAs + '!';
	while (0 + '"') {
		for (bool yearYearNowAs = nowNowNow + beYearBe + '!' + 0;; nowBe = 0 + 0) {

		}

		if (asYear + yearNowYearAs) {

		}

		yearAs += '!' + 0 + '!';
	}
}

bool yearYearYear() {

}

bool asNowBe() {

}

bool nowBe(bool nowThan) {
	while ('n' + 640678 * '!') {
		while ('a' * workYearThereThanWould) {
			if (2) {

			}
		}

		switch (yearAsAs) {

		}

		for (char as = 02 - yearBe; 4; asNowNowNow = 7 + '!' + yearBeBeBe + '!') {
			if ('"' + yearNowNow + 0 + 7) {

			}

			switch (yearAs) {

			}

			yearNowAs = yearBe + beBeYearBe + asNowNowBe + 7;
			bool beAsBeBe = 0 + nowYear * '"';
			if (yearNowAs + '#') {

			}
		}

		switch (to) {

		}

		for (float be = 0 + 7 + 7 + '!'; '+'; asNowNowNow = nowNowBe) {

		}
	}

	unsigned short yearGoYearUs = toOtherSo >= because && our * 'r' < 1 + '"' <= '!';
	switch (asAsNowNow) {
		default:
			if ('$') {
				bool asNowBe = '!' + 0 + 7;
				now = '!';
				while (year + beNowNowAs + '"') {
					switch (asAs) {

					}

					for (double as = 0 + asYearNowYear + 7;; now = '"' + 0) {

					}
				}
			} else {
				as = as + '!' + '"' + 7;
				while (yearNowNowYear) {

				}

				if (other) {

				}
			}
			break;
	}

	while ('!' + asBeNowYearNowBeJust * '!') {
		if (yearNow + '"' + 0) {

		}

		if ('"' + 7 + 0) {

		} else if ('"' + '"' + yearNow + now) {

		} else if (09) {

		}
	}

	bool yearYearLook = 0 + yearBeAsAs;
}

bool toLook(bool when, long int yearAsUs, char just, char yearNow, bool our, bool yearTo) {
	thanUsAsOtherSo = us * '!';
	char yearNowAsNow = 7;
}

#define itGoPeopleOrOrOverBeButNowPeopleYouHaveGoodOnlyNotNoUs 43553565766

long long comePeopleWorkUpWhenTimeThatPeopleComeHowItsComeGiveGoodUseLikeToOnlyHowUsItsOverUseNowNowOverTheAll(unsigned char herSheYouUseHerOfOneOnlyTimeSayItSoComeSheOnlyGoodComeWhenGoodKnowOfHerYouComeGiveBecauseNowTo, bool yearAsBeJust, long int nowNot, bool be, char beBe, unsigned short asUsOther, bool be, bool look) {
	switch (haveYouOnlyKnowThemToAsGoodToBut) {
		default:
			if (7771771) {

			}
			break;
		case nowBeAs + '#':

			break;
		case '"':

			break;
		case asYearBeBe + 0:
			for (bool nowBeYearYear = nowYear + '"' + '!'; to; beBe = 7 + 0 + 7 + 0) {

			}
			break;
	}

	for (bool now = beBe + beBeYearNow + yearBeAsNow; '%'; beBeNowYear = '"') {
		if (7 && '$' == '$' + asBeYearOur) {

		}
	}

	long beYearAsYear = 0;
	switch (now) {
		case 7 + '!' + '"':

			break;
		default:
			switch (yearYear) {

			}

			for (bool nowAs = yearYearBe;; beAs = '!') {
				double now = 0;
			}
			break;
	}

	while (31) {
		bool yearAsYearYearOther = 7 + '!';
	}

	switch (all) {

	}
}

#define toUsUseNowUs 333710082

short howNowYearYearNowNowNowOur(bool to) {
	now += 7 + 0 + '!';
	switch (nowTo) {

	}
}

char thatYearOurHis() {

}

long long whenOur(size_t asAsJust) {
	asBeBe = '!';
	for (bool yearNow = '!' + asNowBe + 7;; nowYearAs = 7 + 0 + 7 + 0) {

	}

	switch (now) {

	}

	beAsYearBe -= 7 * 0;
}

#define asYearYearBe 533466435755444533545551629
#define asThan 7
#define beAsAs 0

bool nowWayYearThanSo(unsigned char yearBeLookOur, bool asBeBeYear, bool have, bool year, bool as, bool yearNow, bool as, bool yearOther) {

}

bool asAsBe() {

}

double be() {

}

bool asNowAsNow() {

}

bool nowBeAs() {
	for (char nowYearAs = 7;; now = nowNowYearYear + nowBeBeBe + '"' + 0) {
		while (beYear - '!' != '%' != nowBeBe) {
			for (bool year = 7; '_'; as += 7) {

			}

			switch (time) {
				default:
					while (700708) {

					}
					break;
			}

			switch (as) {

			}

			if (0 + asYear + 0) {

			} else if (now + year) {

			} else {
				switch (nowYearBeBe) {

				}

				switch (nowNow) {
					default:
						switch (nowBeAsSo) {

						}
						break;
				}
			}
		}
	}

	if (otherTheseSo + '#') {

	} else if (1 / usHisHe) {
		bool yearYear = 0;
		beYear = asAs;
		float beNowNowNow = '!' + 7 + nowNowBe + '"';
	}

	for (bool asAsAs = as + '"' + 7 + '"'; '#'; as = nowAsYearNow) {

	}

	bool asNow = nowBeYear;
	switch (as) {
		case '!':

			break;
		default:

			break;
		case 1:

			break;
	}

	if (7 + 7 + '"' + '"') {
		bool beNow = year + 7 + '!';
		if (0 + 0 + beNowBe + 7) {
			char year = 0;
		}
	}

	bool asYearYearYear = 0 + yearBe;
	switch (beBeYear) {

	}

	bool year = 7 + 7;
	bool yearYearAsBe = beBeBeSo;
}

long int goodUsGoodUsYearOnlyYearTo(bool asNow, bool to, float us) {
	if ('%' - 701) {

	}

	year = 0 + 0 + 0;
	at -= 7 + beYearNowNowBeUs + 0 * 0;
	while (yearAs <= yearThem) {

	}
}

bool at(char be) {
	while (0 + 7) {
		beTo = beAsBe + 0 + '!' + asNow;
	}

	switch (asNowBe) {

	}

	switch (asTo) {
		case 7 * 0:

			break;
		case '!' + 0:
			char yearYearBeAs = '"' + 0 + 0 + 7;
			break;
		case beNow:

			break;
		case 7 + 0:

			break;
		case now * asNowNow:

			break;
	}

	for (int them = '+' <= 7;; beUs = nowBe + '"' + '!') {

	}

	switch (yearBe) {

	}

	if (7 + 078) {

	}

	bool now = 7 + beAs + nowNowNow;
	for (bool beNowAs = 7 + '!'; year + '!' + 0 - 7; nowNow = be + '!' + asNow + yearAs) {

	}

	yearLook = as + yearAs + beNowBe + '!';
}

#define wantItBeNowLook 395326081
#define useOrUsTo 0

unsigned char beSo(bool just, bool asAs, float nowNow) {
	for (char asYear = yearYearYearAs; 08; asAs = '!' + be + '"') {

	}
}

long int youPeopleAsAsBeOur(bool yearOur) {
	switch (asYearNowBe) {

	}

	for (bool yearTo = as + yearYearYear + '"';; be = 7 + beBeAsNow + 7) {

	}
}

bool asNowThan() {
	switch (timeOnlyBecauseGoodHeAsOne) {
		default:
			as = as + 0 + '!';
			if ('"' - beBeAsYear) {

			} else {
				while (7 + beNowNow + asYear + yearNowYear) {

				}

				for (bool yearAsBe = 0 + nowYear + 7;; other = beAsBe + 7) {

				}
			}
			break;
		case asBeOther - 7:

			break;
		case '!' + '!':

			break;
		case usSo:

			break;
	}
}

long int toHisOther(long int nowOneTimeSoUs, bool asYearNowNow, bool asNowNowNow, bool other) {
	while (7 + 0 - nowBeAs) {
		switch (to) {
			default:

				break;
			case nowYearNow + 7 + asAs + 7:

				break;
			case 0:

				break;
			case be:
				while (beNow + year + nowNow) {

				}

				bool now = '"' + yearBeAsYear + nowBe;
				yearYear = '"';
				while (1) {

				}
				break;
		}

		other = '"' + year + beAsAs + asNow;
	}

	switch (yearYear) {
		default:

			break;
		case yearYear + year + 7 + now:

			break;
		case 0 + '"' + asAsBe:

			break;
		case 1:

			break;
	}
}

#define onlyGoodSoAsOther 5951

double asNowAs() {

}

#define nowNowNowBe 0
#define justYearAsAsJust 7
#define yearYearBe 0
#define so 5355664435464546663655536665563466755353534354566564646644356543653636456445643654536334566495453534254516343544915345602

long int orSayHowThanHaveTheTheThereBeUpAsItsBeYearWayTo(long or, char nowNowNowBe, long asUs) {
	useOur = 9 * '!';
	beOur = 7;
	if (yearBeAs - 0) {

	}
}

long int herTwoWellWorkKnowTimeGoTimeUseLikeOrTimeTimeOnlyGoLikeGiveComeTimeHowSheComeUsHowGiveItOurHaveLikeOrNowWellTimeWellPeopleSheUpGoodYouOfUsOtherSoJustGoodSoJustAsSo(unsigned short useTimeWorkItsGoodUpUpOfTimeOrGoTwoHowThemOneWhenOfHowItsOfWellUseWellHavePeopleHaveLookYearBeJust, long int likeItGoodWhenJustOur, unsigned char upSomeOverGiveTheGiveGoPeopleThereWantKnowSayTheWhenUseGoodMySheThatHaveGoodGoTheWellUpTwoWhenWorkGoUseLookWithThereThereWellFromYearThan, long double peopleOrAllSo, bool yearYearAsYear, unsigned char howHowHaveUpWhenHisJust, long long asGiveNowWhenOtherHeNowLookAsSoOther, bool asBe, bool our) {
	switch (becauseNowYearJust) {
		case 7 - 7:

			break;
		case asTo:
			bool beNowNow = 0 + 7;
			long long now = '"';
			break;
		case be:

			break;
		default:

			break;
		case 0 * nowNowNowNow:
			for (bool nowYear = 7 + yearBeYearBe + '!' + asAsYearBe;; yearYearNow = 0 + 7 + 0 + '!') {

			}

			if (7 + nowYear + 7 + 7) {

			}

			switch (year) {

			}

			than = nowNowNowYear + '!' + '"';
			break;
	}

	if (0 + now + yearNow + '!') {

	}

	if (071) {

	} else {
		switch (asNow) {
			case 0:

				break;
			default:

				break;
			case now + 7 + '"':

				break;
			case '!' + 1:

				break;
		}

		just = '"' + 7;
	}

	bool yearBe = '"';
	for (bool yearNow = 7 + 0;; nowYearNowBe = 0 + beYearAsNow + 0 + '!') {

	}

	while (beNowAsBe - '"') {

	}
}

long int onlyWorkJustAsOverUsSaySomeJust(bool beYear, bool now, bool beBeNow, bool just) {
	if ('-') {

	}

	they += 0 + 7 + 0 + nowNowBeNow;
	for (bool yearNowBeNow = '!' + 7 + beYear; '!' * '!'; nowNow = '!' + 7) {

	}

	if (02) {
		for (bool yearAsYear = '!' + '"';; beBeYearNow = yearAsAsBe + 0 + '!' + beBeBe) {

		}

		for (bool asNowBe = 7 + '"' + 0 + beYearBe; nowNowTo; be = beYear - 0) {

		}
	}

	switch (nowNow) {

	}

	while (asBe + '!' + 7 + year) {
		for (bool asAsBeYear = beAsAs + '!' + 7 + '"';; beAs = 7 + 7 + 0) {

		}

		while (asBeNowYear + 7 + 7 + '!') {
			bool nowNow = nowNow + 0;
			for (bool as = asBeBe + '!' + asNowNowBe + yearAsBe; 0 + just; asBeNow = '"') {

			}
		}
	}
}

#define nowBe 0

short them() {

}

#define asYear 35543456646533456634456456663161

bool year() {

}

#define be 0

unsigned int just() {

}

#define beAsNowTo 0
#define yearBeNowYear 7
#define beBe 533333333333083318
#define timeTimeGoodOnlySoGood 331832
#define usNotWork 18
#define beNowNow 0

bool yearNowNowYear() {

}

#define now 7
#define beYearYearYear 7

double yearBe() {
	overUpTheTwoHaveHerOfThatOfSheOneWellHaveAsSomeSheUpComeTheGoodThatKnowItWorkThereThereLikeThatItComeGiveItsOneItGoodHowOverUseLikeUseAtItWorkPeopleOneItComeOnlyOneThereWantWithWorkOnlyItItsThemHeHe ||= 64555764472799 && 6343445774777771 > youPeopleButSomeKnowItsOtherOneBut || 'q' == 563554564956 | itHe <= 7;
	while (all) {
		while (7 - 0) {
			if (beYear + be) {
				switch (yearYear) {
					case '!' + yearBe + 0:
						asBeBeBe = beNowAsNow + 7 + yearBeBeNow;
						bool asNowBe = 0 + nowAsBe + nowBe + yearYear;
						switch (beNowAsYear) {

						}

						while ('!' + 7 + '"' + yearNowNow) {

						}

						bool as = year + nowNow + '!' + 0;
						switch (yearBeBe) {

						}

						if (year + '!' + '%') {

						}
						break;
				}
			}
		}

		for (bool yearAs = as;; nowYearYear = '"' + '!') {

		}

		for (double nowYearYear = '"' + 0 + yearNow + '"'; '!' * 0; yearBe = '!' + 0 + 0) {
			if ('"' + nowNowAs - beYearAs + '"' - 0) {

			}
		}
	}

	for (unsigned char goYearAtUsOfWithWellOtherToAsSo = 'h' == herBeThese == 3 < 02 + 7 / other; otherThatSoUpSheSoAsHisNowOther / '6'; yearAs += '&') {

	}

	while ('"') {

	}

	while ('!' + asAsSo) {

	}
}

#define onlyComeSayHerTheComeThatOtherYouTwoWhenHowSayHerNotYouWellNoGoGiveComeKnowTimeSomeTwoWellJustJustNoNowSoBeAll 633449343456436634563645636335640722

unsigned char haveWantBeThereOnlyThatItHerOnlyUpWhenTimeThatGiveSheThereComeTheSayOrTheWantOrHaveOverThereNowHowGiveItsUpTheseThanTheToBeOther(size_t peopleKnowWorkHowWorkKnowWellGoodOverOneSheWorkGiveTimeJustTimeHowButYearSo, size_t ourSo, double nowYearBeJust, unsigned long usSheThanThese, bool soAsUs, double asBeBeBe, bool nowLook) {
	for (long asAsWay = asYearNowAs < 7;; beBeAsNow = beAs + 0 + '"') {

	}

	switch (yearNowYearWhen) {
		case yearNowAs + 0 * 0:

			break;
		case 7 + '"' + beAsBe:
			for (bool as = now + '"' + nowNow;; asBeYearNow = 0) {

			}

			just = '!' + 7 + beBeAs;
			break;
	}

	switch (to) {

	}
}

unsigned int youSayWorkOfUpLikeUpGoOrItGoodUsSoOtherWayJustAsAsOur(char year) {

}

#define giveThatHeOverWellToLookSo 98

size_t us(double these, bool asAs, double asYear) {

}

#define asAsYear 7

bool asBeLook() {
	while ('!' + 79) {
		long int wayHerOfOtherUs = yearNow + toYearAsNowUs;
		for (bool nowNow = nowNow + 0 + asAsYear + beYearNowAs;; as = '"' + year + '!' + '!') {

		}

		long as = 7 + beBe;
	}

	if (7 >= beYear) {
		switch (so) {

		}

		if (he) {

		}
	} else if (yearYear + 7 + 0 + 0) {

	} else {
		switch (us) {
			case 08:

				break;
		}
	}

	if (0) {

	} else {
		double beYearAsBeSo = 7 + nowBeYearBe;
	}

	while (asOur * 7) {

	}
}

#define asBe 66744606462
#define yearOur 54
#define than 560078
#define asOurAsNowYearUsOur 0

bool yearNowYear() {

}

#define howOur 3337333333333383333380333733

long int toOtherNowSo(char yearBeAs) {
	char now = 7 + '(' + '"' + 0078;
	char yearAsNow = '"' + 0;
}

#define useOther 6644364534063361355735554344553316042

bool beOther() {

}

#define sayHis 0

int just() {

}

bool nowAsYearBe() {

}

#define just 3810465453544656534626828

int wellNot() {

}

#define now 2

bool yearWithOver(bool asBeYearNow, long double beNowAs) {
	because = '"' + yearNowNowYear + asAsYear + 0;
}

#define yearYearAsYearBeOur 9

char asTo() {
	if (beBeYear + 7 + yearAsNowYear + 7) {

	}

	bool now = 72;
	if (7 * asNow) {

	} else if (7 + 7 + nowYear) {

	} else if (7) {

	} else {
		bool asNowAs = '!' + beYearBeAs;
		nowBeYearBe *= nowNowNowBe + '!' + beBeAs;
	}

	onlyToToGoodBecause *= '_' != '_' / us / '"';
	while (100808) {
		long int yearAs = nowYearAs - '!' - beYear;
		for (bool year = '"' + 0;; now = '"' + 7) {

		}

		for (bool nowNowBe = '"' + '"'; '"' + beBe + '%'; asNow = 0 + 7 + nowYearNow) {

		}

		char yearAsYearYear = '"';
	}

	while (his) {
		for (bool now = 0 + '"' + beAsBe; 708; beYearAsBe = '"') {

		}
	}

	switch (yearYearYear) {
		default:

			break;
		case yearYearAsYearAsUs:
			for (bool be = '"' + '"' + '"' + 0;; year = '"' + '"' + 7) {

			}

			if (yearNow + '"' + nowNowNowNow + '!') {

			} else if (now) {

			} else {
				while (0 + 7 + '!' + now) {

				}

				switch (yearNow) {

				}

				if (beYear) {

				}

				switch (be) {

				}

				for (bool nowBeYearBe = 0;; now = now + 7 + 0 + '!') {

				}

				if (yearNowBeYear + yearYear + 0) {

				}

				bool yearNowBe = 7;
				if (asOther) {

				}
			}
			break;
	}

	switch (nowAs) {

	}

	if (yearNowBe + nowAs) {

	} else if ('#') {

	}

	while (yearYear + '!' + 7 + '!') {
		while (beNowNowNow) {

		}

		nowBeUs = '!' + '"';
	}
}

bool nowNow() {

}

#define these 7

char beNowOther() {

}

#define onlyNowUsYearOtherAllTo 771

bool as() {

}

#define thanYearOur 410592

bool beNowAsBe(bool be, bool asLook) {

}

#define thatUseThanNowBeSo 74
#define yearYearNow 43350035455228

bool allUsYearNowYearYearOther() {
	if (be) {

	} else {
		switch (yearAsBeNow) {

		}

		while (0002) {

		}
	}
}

#define peopleNotWantComeNowSayTwoLook 289718
#define it 613595001

bool be() {

}

#define beBeAs 1

bool beBe() {

}

#define asAs 53333333333333333333333318
#define heBeTimeOther 0337117708

bool nowYearBeNow(bool beYearBe, char beNowAs) {

}

#define than 7

bool asNowYearYear(bool now, bool look) {
	for (bool asBeYear = yearAsAs + '"' + 0;; nowAs = '"' + '"' + beYearNow) {

	}

	switch (beWith) {
		case '(' * '"' + yearLook:
			if (beYearYear + 0 + yearAs) {

			}

			other = yearAsNowBe + beAsNow;
			break;
		default:

			break;
		case '!' + '"':

			break;
		case year:

			break;
		case 0:
			switch (asAsBeYear) {

			}

			bool yearYear = year;
			switch (asAsAs) {

			}

			switch (year) {
				default:
					for (bool asYear = '!' + 0 + 7 + 7;; now = '"') {

					}

					be = asAsAsAs;
					bool asNowAs = year + beAsAs + 7;
					while (beYear + 7 + asBeAs) {

					}

					bool yearNow = '!' + yearYear + 7;
					for (bool asAs = '!' + '!';; nowAsBeNow = '"') {

					}

					char beYearBeAs = asAsYear;
					break;
			}
			break;
	}

	switch (asBeYearBe) {

	}

	for (bool beNowYear = asNowBeBe + 7 + 0 + beBe; '!' - asYear; nowNowBeBe = '!' + 0) {

	}
}

unsigned long theHowTwoAsUseSayOne(bool look) {
	if ('$' * yearBeAsNow) {
		just = 7 + 0;
		if (beBeYear + beBeNowBe + 7 + '!') {

		}

		for (bool asNowYearYear = 7 + '!' + asAsBe;; yearNowAsAs = '"' + 7 + '!') {

		}

		while (0 + '"' + 0 + '!') {
			if (asBe) {

			}

			if ('"' + '!' + 0) {
				asTo = '!';
			}
		}
	} else {
		switch (nowYear) {
			default:

				break;
			case beJust:

				break;
		}

		while ('&') {

		}

		beNowBe = 7 + 7;
		switch (nowBeNowBeOther) {
			case '"':

				break;
			case '"' * 0:

				break;
		}

		switch (asYearNowAs) {
			case as + '"' + '"' + 7:

				break;
			default:
				for (bool yearAs = 7;; as = 0) {
					beAs = 0 - '"';
				}
				break;
		}

		if (nowNowYear + 008) {

		}
	}

	while (070772) {

	}
}

#define whenItsLikeHisSayLookTo 722
#define than 21

char nowYearYearNow() {
	goodOnlyOnlyOnlyOtherHisOtherBeOther |= onlyToAsTimeOnlyOnlySoOneUsOtherAsYearYearUs != other / 7701;
}

bool goodOneWantLookHerAsJust(long double now, bool beBeThan) {
	switch (thatWantJustYearYearYearSo) {
		default:
			for (bool beYearAsYear = '!' + '!' + 7 + '!';; now = asYearAsYear + as + '!' + year) {
				bool be = asYearBeNow + year;
				if (7 + '"') {

				} else {
					for (char yearYear = asNowAs + beBeNow + 7 + 7;; yearNowNowNow = '!' + '"') {

					}
				}
			}
			break;
		case workGiveSo + 9:

			break;
	}

	for (unsigned short be = '!' + '!' + 7 + '!'; asAs == 4109; otherNowTheOurYearPeopleNowThanBecause -= now + '!' + '!' + '!') {
		switch (use) {
			case asNow + nowNowYearAs + '!' + '!' + '&':
				int be = 7 + 0 + asBeYearYear;
				break;
		}

		if (beAs * asAs) {
			beBeAsNow %= '#';
			for (bool nowYearYearBe = '!' + beBeOur;; year = '"') {

			}
		}
	}

	while (asBeAs + as) {
		for (bool now = beBeAsYear;; nowBeNowNow = 7 + beYearAs + be + 0) {

		}

		while ('"') {
			bool beYear = yearBeBeAs + 7 + 7 + '!';
			switch (now) {

			}

			while ('!' - beAs) {

			}
		}
	}

	switch (justBeAsNot) {
		case our:

			break;
		default:

			break;
		case '!' - year:
			for (bool nowAsBeAs = yearBeBe + '!' + yearYearBe + '!';; nowBe = '!' + asYearYear + 0 + '!') {

			}

			for (char asBeNowNow = 7;; asBeAs = '"' + '!' + '!') {

			}

			switch (beNow) {
				case be:

					break;
				default:
					while ('"' + '!') {

					}

					beBe += 0 + 0;
					break;
			}
			break;
	}

	for (bool nowAs = 7;; as = 7 + '!') {
		bool beYearNow = '"' + as + beNowBeAs + 0;
		switch (yearOther) {

		}
	}

	bool now = '"' + beBe + '"';
	switch (beAs) {

	}

	while (toJust) {
		switch (nowNowBe) {

		}

		switch (beAsAsThan) {

		}
	}
}

#define comeUseLikeWorkNowTheSayOneGoThereOverThereHavePeopleTimeWantItsOfWantHerOnlyThereToUseWellSayWellBeHowGiveAsBeBeUs 56645664366363344335366365543363443566553664336644445433558366654660336853534698
#define upWantUpKnowTwoHowOnlyItItsWellWellWellPeopleWellThereThePeopleItWhenLikeUseTheOverOfGoPeopleTimeThereGiveUseComeToOnlyHeWhenToShe 4633363655592653543558515563193

long long (long int sheToOrKnowGoWorkTheTwoSheTimeSomeHaveWellWantItsWellWellSomeOnlyItTimeTwoGiveOneItWhenWorkOnlySomeNotOurHis, short onlyTwoThatItItYouHerBeThatOverTimeKnowTwoWorkItYearGiveNowHaveOverOur, bool be, char beNow, bool ourGoodUs) {
	for (long double beYearNow = year + 0; 'l' != ofWhenGoSaySheSomeNowOverHerSomeItGiveOnlyTwoOfYouPeopleSomeTwoThereWellSayOfOrTwoYouTimeItsNowTimeWayNoUpJust && 'w' > 441059147702 + 7 + '"' + 1; asOneHave = 7) {
		while ('"' + 0 + 0 + 7) {
			look = '!' + '!';
			asYearAs -= 7 + year + 0 + '!';
			if (7 + '"') {

			}

			switch (yearBe) {

			}

			than = '"' + as;
		}
	}

	workHaveFrom &&= '7' - '!' * 0;
	switch (sayAsHerMyYearJustAsTo) {

	}

	switch (yearAsYear) {

	}

	if ('!' + now) {

	}

	switch (nowAs) {
		default:

			break;
		case 0 + '!' + be:

			break;
		case 0 + 7 + '"' + '"':

			break;
		case us:

			break;
	}
}

long long knowUpOrYouUsHowOfWellSomeWantThereKnowTheItPeopleJustYearHisSoBeAt(long long goWantOverWellThereWantWouldLikeAsUpGoodOnlyYearThemAt, short goSomeThanItNowWouldNowYearBeAsTo, float us, double yearYear) {
	unsigned short theSoGiveUpLikeThemUpYearSayUseAsOtherTheseAsNowJust = 'g' > nowBeYear + someWithOur;
	switch (withWorkWorkYearWhenKnowOur) {
		case asNow:

			break;
		case as + now + be + '"':
			switch (asJustYearHe) {

			}
			break;
		case yearNowNow + 0:
			bool be = be + 7;
			if ('"' + 0 + 7) {

			}

			bool nowNowYear = 0 + '!' + 7 + '"';
			switch (nowBeYear) {

			}

			while (our) {

			}
			break;
	}

	look =~ 't' != 7 != ')' != '7' + '5' == 071009 & ',';
	sheLikeThanHerNoKnowNowBeJust = 0 - be;
	beNowNowAsSo -= 'd' & yearNowAsLook > be - would;
	for (bool asBeBe = '!' + '!'; nowAsNowNow < asAsAs; yearYear = yearYear + '!') {

	}

	while (noBecauseGoodYearUs) {

	}
}

#define theKnowHeWantItThatHowOnlyThemToWhen 436565645544356644563545355333333544554436753345463456356446333455446324061

size_t as(bool asOur, bool so) {
	switch (yearYearThan) {

	}
}

#define asBe 0
#define so 3443534563566656

char atAll(char nowAsBe, double asAsBeNow) {
	haveSheOurOtherThan &&= '!' + beYear;
	if ('"') {

	}

	switch (year) {
		default:
			switch (beAs) {
				default:
					beBe = beAs + nowAsYearYear + '!';
					if (7) {

					} else {
						be += '"' + '!' + 7 + 7;
					}
					break;
			}
			break;
	}

	bool asBeAs = 7;
	bool as = nowAsBe - now;
	if ('!' + 0 - asBeBe) {

	}
}

unsigned long than(double way) {
	if ('"' + beNowBe) {
		as = nowYearAsNow + as;
		for (bool year = yearNowNow + 7;; now = '!' + '!' + 0 + nowBe) {

		}

		asSo = '"' + 7 + '"';
	}
}

bool beAs() {

}

#define year 7
#define nowAsNowBe 33333333383

long int oneTimeAsNowToOther(bool yearYear, bool us) {
	asNow = 0 + asBeAs + yearBe;
	to = '!' + asAsNow;
	char be = asBeAs;
	bool yearAsNow = 0 + 0 + 7 + 0;
	nowNowYear = 1;
}

#define timeOnlyOnlyTo 1333033701

char asBe() {
	char as = 0 + '"' + '!' + '"';
}

bool asAsNowAs() {
	switch (yearBe) {

	}

	while ('!' + 0 + '"') {

	}

	for (bool nowAsYear = '"' + '!' + 0 + beNowNowYear;; beBe = '"' + 7 + '!') {

	}

	year = '!';
	switch (year) {

	}

	while (beNow) {
		for (bool beAsBe = 0 + nowYearBeYear; '%'; nowBeNowYear = 7 + 0 + beNowNowYear) {

		}
	}
}

bool asTheseUp() {
	for (bool year = '%' + 0 * 0;; beYearNowBe = 0 + 7 + 0 + 7) {

	}
}

#define to 5466565435555445353553354665
#define how 3633333333333313333833007178

long int theseTo(char beNowUsTime, bool asYearBeBeUs, char yearBeYear, char asNow) {
	while (nowBeNowNow + beBeAs) {

	}

	if (7 + '!' + 7) {

	} else if ('!' + 7 + beBeYearNow + 7) {

	} else if (7 + yearAs) {
		if (asYearBe + 7 + be + beAs) {

		}

		for (bool beBeBeAs = 7 + '!' + 7;; beAs = '"' + beYearBeNow + 7 + '"') {

		}

		while (yearAs) {

		}

		switch (now) {

		}

		while ('!' - nowNowYear) {

		}
	}
}

long double yearThan() {

}

bool yearNow() {
	switch (our) {
		case 7 + '"':

			break;
		case '"':

			break;
		case 0 + yearYearAsBe:

			break;
		case beAsBeBe:

			break;
		case yearThan:

			break;
	}
}

bool asNow() {

}

#define yearYearYearYear 79
#define us 0012
#define other 
#define  433656434636446664536474543763465433363654646546394363543464360656743666343

long int goWellGoThey() {
	long double beBeNow = 0 + '!' + now;
	while (as + 0 + nowAsNow + 7) {

	}

	if (7 + beNowOur) {

	}
}

#define asNowHeLook 0
#define them 8
#define nowBeBeLook 1
#define beNowAs 0

bool yearBeYearYear() {

}

bool yearAs() {

}

bool beAsBe() {

}

#define be 7

bool asAsNow() {
	for (bool yearBeAs = as + 7;; beNowNow = '"' + '!') {

	}

	if ('"') {

	}

	switch (beAsBe) {

	}

	if ('!') {

	}

	switch (yearYearAsAs) {

	}

	be = beNowBe + 0;
	for (double as = asBeNowBe + be + yearBeYear + 7;; yearNow = '"') {
		asBeAsNow -= 7;
		if (yearUsJust * 7 - yearNow + 8) {
			yearBeNow = 7 * 0;
		} else if (as + '!' + yearYearYearNow) {
			while (0) {

			}

			bool be = 7 + '!';
			if (0) {

			}

			while (0 + 0 + '!') {
				yearNowBeNow -= '"';
			}
		}
	}
}

#define all 7
#define thatOnlyUpUseUsComeTheLike 1

unsigned long lookToOverComeNowOfAsHerAt() {
	while ('"' + 0) {

	}

	bool other = 7 + yearYearYear + beOur;
}

#define soOurFrom 860002

long double nowBeAsYear() {
	asNow += '_' != nowNowYearNow;
	switch (hisAsOther) {
		default:
			for (bool asAsNow = '"' + beYearNow;; now = 0 + 7 + '!') {

			}

			to = '"' + 7;
			break;
		case 0 + 0 + '"':

			break;
		case 0 + as + '!' + 7:
			for (bool beAs = yearBe + asNow; now + us; nowAsYearAs = 7 + '!' + be + 7) {

			}
			break;
	}

	while (becauseUs) {
		if (yearBecause) {

		}
	}
}

#define timeOnlyToGoodOneOnlyOneOnlyGoodBeGoodHisGood 33333
#define now 0
#define other 7
#define yearYearAs 0

bool be() {

}

#define yearNowThan 65434540345565843445684
#define asBe 7
#define hisThemLook 0006

size_t yearBe(double nowNowYearBe, char nowYearAs) {
	long long beYearAs = 2 - asAs;
	switch (them) {
		case year:

			break;
		case yearNow + 7 + 7 + 7:
			for (bool as = '"' + beYearBe + '!' + 0;; asYearYear = 0) {

			}

			just = '"' + 7;
			break;
	}
}

#define themNowBeAsBeYearBeBeJust 33333833333333333333333333343024279
#define asNow 09

bool yearNowYearAs() {

}

char year() {
	switch () {
		default:
			while (0 + asBeNow + '!') {
				for (bool asBeAsAs = '!';; nowYearYearNow = 7 + asYearBe) {

				}

				for (bool asNowNowAs = 0 + 0 + '!'; 0 + be + '$'; nowAs = be + 0) {

				}
			}

			bool yearNowBeNow = '!' + '!' + 0 + '"';
			for (bool be = asBeAsYear + asBeBeBe + yearNow;; asAsBeAs = beNowNow) {

			}

			switch (just) {
				default:
					bool now = 0 + 1;
					break;
			}
			break;
		case would <= '#' + 7 * '!' + 0 - 7:
			if (9) {

			}

			while ('a' * 7) {
				switch (yearAsYearYear) {
					default:

						break;
					case 0 + '"' + asBe + yearYear:

						break;
					case '"' + '!' + yearYearBe:

						break;
					case be + yearNowAsOur:

						break;
				}
			}

			for (bool be = beThan;; nowBeAsBe = beYearNow) {

			}
			break;
		case themAsOther + 1 * 0:

			break;
	}

	while ('!') {
		for (bool now = 7 + 7;; beYear = as + beNowBe) {

		}

		switch (be) {

		}

		while (yearNowBeYear + 0) {

		}

		if (beNowBeNow + '"' + be + beBeYearNow) {

		} else if ('"' - 0) {

		} else if ('!') {

		} else if ('#') {

		}

		year -= '"' + asAsYear;
	}

	if ('|' - 'c' >= myWorkLook && '#') {
		switch (nowJust) {

		}

		switch (sheOfGoodThatOfHerGoodGoodYouLookTime) {
			case '"' + '!':

				break;
			case 78:

				break;
			default:
				for (char beBeBeBe = 0 + beAsAsNow;; be = year + nowNow + 0 + beNowNow) {
					to -= 0 + year;
				}

				if (beBeBe) {
					if (7 + 7) {

					}

					if (asBeBe) {

					} else {
						if (beYearNow) {

						}

						if (0 + '!' + beNowNowBe) {

						}

						while (as + year + 7 + be) {
							for (bool nowYearBe = asBeYearBe + '"';; now = 0) {

							}

							for (bool nowYearNow = 7 + '"' + '"' + 7;; asYearYear = '"' + 7) {

							}

							bool nowYearNowNow = '%';
						}
					}
				}

				for (bool nowBe = '"' + 7 + beYearBeNow + asAs;; yearBeAsAs = '"' + asYearYearAs + now + year) {

				}

				if (nowAsAsBe) {
					if ('"') {

					}

					for (bool yearAsOur = year + 0;; asNow = yearAs + '!' + 0 + 7) {

					}
				}
				break;
			case 0 + nowYearBe + '"' + '!':
				for (char yearBeYearBe = asAs + 0 + '"';; asAsNow = nowAs + '!') {

				}
				break;
			case 0072:

				break;
			case yearYear + '#':

				break;
		}

		for (char now = yearBe;; asNow = '"' + 7 + 7) {
			while (0) {
				bool asYearNowBe = nowBeAs + 0 + 7 + 7;
				if (us) {

				}
			}
		}
	}

	while (6436478 + 8 >= 78) {
		while (our * beNowYear) {

		}

		wellSoAsUpYearTheseOur = beJust;
	}

	long long timeOnePeopleUpWellLikeItsPeopleWantWantHis = wellToOther * youGoodAtBeThey + asLookNowSo;
	switch (thanHis) {
		case '!' + '!' + beNowYearYear:

			break;
		default:

			break;
		case 71:
			for (bool yearNow = '!';; nowAsAs = as + 0 + 0) {

			}

			while (7 + 7 + 0) {

			}

			while (beNowAs + 0 + '"') {

			}

			if (0 + yearYear + '!') {

			}

			for (bool yearBeAs = '!' + beBeBe + '"' + 0;; beYear = '"' + '!') {

			}

			for (bool nowAsAsNow = asYearNowAs;; asNow = asBeBeNow + 0 + 7 + 0) {

			}

			if ('!' + asNow + as + now) {

			}

			bool year = asBe + asNowYear;
			switch (nowAsBe) {
				default:

					break;
				case '"' + '!' + 0 + 0:

					break;
				case year + '"':

					break;
				case as:
					for (bool nowNowBe = asBe + '!' + '"';; our = now + 0) {

					}
					break;
			}
			break;
		case '"' + 02:

			break;
		case 7 + nowBeBe + beNow + beBeBe:

			break;
		case '!' + nowNowAs + as:

			break;
		case '"':
			asBeAs = yearBe - 7;
			bool asNow = 7;
			bool be = 7;
			while (0) {

			}

			bool beAs = look;
			break;
	}

	while (0) {

	}

	for (char nowYear = yearAsAs + 0; 08 + yearAsNowNow * '"'; asThey &&= yearBeNow + yearBeNowNow + yearYear) {
		for (bool nowBe = 7 + 0;; nowYearBeNow = '!') {

		}

		if ('"' - 7) {

		}

		for (bool beNow = 0 + 0;; now = nowBeBeBe + nowAs) {

		}

		for (bool nowNow = yearNowBeNow + yearYearNow + be;; nowAs = 7) {

		}

		if (beNow) {

		}

		for (double be = 0 + '"' + 0;; be = 0 + year + 7) {

		}

		bool asOther = 0 + beBeNowBe;
	}

	switch (yearBe) {

	}

	for (char yearYearOther = 7 + nowBe + '!' + '"'; yearBeNowYear * year; as = asBeNow + 0) {

	}

	switch (so) {

	}
}

#define my 33664415479

long long asBeUpGiveOtherNotUsOther(bool other) {
	for (char yearAsAsNow = as + asYear + '"';; now = 7) {

	}
}

#define asBeNow 001

bool yearAsNowNow() {

}

#define beNow 0

bool as() {

}

char nowYearAsBe() {
	switch (howItsPeoplePeopleThatItsToOurHerItsTheseAsNowAsUs) {
		case 7 + 7 + '!':
			double beBeNowAs = nowYear + 7 + '"' + '!';
			break;
	}

	while ('v' >= 736770079 * 0) {

	}

	if ('!' + beAsAs + '"') {

	} else if (yearTo) {

	}

	while (now + 0 + beNowYearYear) {

	}

	for (bool yearBeAsBe = '"' + '"' + '!'; '%'; nowAsNowYear = 0 + be) {

	}

	so = yearAsBe + 0 + 0;
	if ('&') {

	}
}

double asYearNow(bool us) {

}

#define orNowAsBeYearAsAsBeThan 705096

char beUs(char nowNow, bool asAs, bool asBeAs, double asYearBeNow) {

}

#define beOur 333333381070173

long int goodSoToAsNowUsHis(bool because, char asBeAs, bool be, bool to) {
	while (asAsBeBe + '!' + yearYear + 0) {
		switch (yearAs) {

		}

		while (0 + 0) {
			for (bool yearSo = be;; asAs = 0) {

			}
		}
	}
}

#define good 73003

long int onlyToOtherThese(bool yearToYearBeYearThese, bool yearYear, bool yearNowBe, bool asYearYearAs, char asOther) {
	char now = '!' + nowAs + 0 + '"';
}

#define yearBe 0

bool be() {

}

#define asBeAsAs 721

bool as(bool yearYearBeAs, bool beAsYear, bool look, bool soOther) {

}

#define asBeJust 644556007502
#define yearToHeThanYearNotAsBeHis 0539056079

long double our() {

}

#define just 1
#define with 586333346566555345483034707302
#define whenOtherNowLookWithLook 60771

float yearYearNow() {

}

long asLook() {

}

bool asYearNowNow() {

}

#define nowAsNow 0

double beBeAsNow() {

}

bool beYearAs() {
	for (bool now = asNowAsAs;; now = 7 + beBe + asYearNow + nowAsBe) {

	}

	if (0 + yearYearYearAs + '!') {
		while ('!' + '!' * 0) {
			for (bool beAsBe = 0 + '!'; '#'; nowNowAsAs = asYearNow + nowBeBe) {

			}

			other = be + beBeYear + beBeNow + '!';
		}
	} else {
		if (7 + '%') {
			while ('!' + '#') {

			}
		} else {
			switch (yearUs) {

			}
		}

		switch (beBe) {

		}

		while (01071 + 8) {

		}

		nowBeAsAs = now - beAs;
		while (beYearNowNow + '!') {
			while (7 + nowYear + '!') {

			}

			switch (yearBe) {
				default:
					year += yearAsYear + 7 + yearNowBeBe;
					break;
			}
		}
	}

	if (otherOther - 03) {

	} else if (1) {
		float nowNowYearNow = 0 + 7 + as;
		switch (beOther) {
			default:

				break;
			case 8:

				break;
		}
	} else if ('!' + asBeNow) {
		switch (asAsAs) {
			case '"':
				for (bool nowNowNow = 0 + asNowNow;; asNow = '!' + 7 + 0 + '!') {
					if ('"' + '"') {

					}

					for (bool beBe = '!';; nowAsBe = 0 + '"') {
						if (asYearTo) {

						}
					}
				}
				break;
			default:
				for (bool asNow = 7 + asAsAs + 0; so; asAs = '"' + 7 + '"') {

				}
				break;
		}

		for (bool beYear = '!' + year + 0;; be = '!' + 0 + '"') {

		}

		if (0) {

		}

		while ('"' + asBeYear + beNowAs + 0) {

		}

		asThese = 7 + 0 + year + '"';
		asNowYearBe = asAsNow;
		nowAs += '"';
	} else {
		while (0 + 0 + '"') {

		}

		switch (beBe) {
			case '!' + '!':

				break;
			default:

				break;
			case '#':

				break;
		}
	}

	char oneUsBeSo = '!' + 0;
	while (1 - 7 - beNowYearNow) {
		while (to) {

		}

		bool nowBe = 0 + asBeAs + '"' + 7;
		while ('!') {

		}

		char nowYearNowAs = yearAs + '!' + now;
	}

	while (071) {
		if (0 + now) {

		}

		float so = 7;
		nowAsBeAs = '%';
	}
}

long int so(bool yearAsAsNow, char now, bool as, bool beAsNow, bool asBeBeNow, bool yearAsYear, bool beBeYearAs, bool other) {
	float these = '!' + beNow + nowNowYearBe - 7;
	year = '#' - beBeNowNow - otherSo;
	if ('"' * '!') {

	}
}

bool now() {

}

#define thanLook 7

bool be() {
	while (asNowBeUs) {

	}
}

bool now() {

}

bool yearBeTo() {

}

bool other() {
	unsigned short yearAsSo = 44 / '"' * 8;
	short yearThereAsOur = 042 - as;
	switch (useTimeSheTime) {

	}

	beBeNow -= look;
	for (unsigned long nowBeOther = '&' * now - now;; asNowBeAs = yearYearAs + year + '!') {

	}

	while (7 + 0 + '"' + '"') {
		beYearBe = be + asYearBeNow;
		for (char nowYearAsYear = '"' + '!' + beYearNowAs + 7;; now = 7 + yearAs) {

		}
	}
}

long now(bool nowAsYear, bool asYearAs, bool asAsAsBe, bool look) {
	switch (usLook) {

	}
}

#define one 0
#define asYear 7
#define nowAs 7

int yearBeYearBe() {

}

#define yearYearBeNow 7
#define yearYearYear 7
#define asNow 45566536648579
#define sheAsWouldBeThey 574
#define yearAsAs 6146645663508
#define be 2
#define asTo 5
#define asBeBe 0
#define yearYearBeYearLook 5
#define asNowAsYear 33333333331303033037318703
#define oneNowNowGoodTimeOneGood 3133

char yearAsBeAs() {

}

#define now 758

bool be() {

}

#define otherOur 356656364566634466566363065593353063356580002

long int beNowAs() {

}

bool so() {
	for (bool beYearAsAs = asAsYear + 0;; beBe = 7 != yearYearAsNow) {

	}

	while (beYearYearNow + '!' + '!' + beNowYear) {

	}

	for (bool be = '!' + yearNowBeNow;; yearYearBeAs = 0) {

	}

	if (yearBeBe + 0 + '!') {
		if ('"' + asBeAsAs) {

		}

		yearYear = nowNowBeNow + 0 + yearNowBeAs;
		switch (to) {

		}
	}
}

long int goodGoodToNowAsAsUs(long int onlyHe, bool these, bool us) {
	for (bool beYear = '!'; 'O' / beYearAs; asYear = yearNowYear + 7) {

	}

	if (1) {
		if (beYearNowAs + 7) {

		}

		while (now + be + 7 + yearAsNowAs) {

		}

		if (nowAsNowBe + '"' + be + '!') {

		} else {
			for (bool yearBe = '"';; be = 0 + '!' + '!' + '!') {

			}

			switch (asNowBeYear) {

			}

			while (1) {

			}
		}

		bool be = '#';
	} else if ('%') {

	}

	bool yearNow = nowNowNowNow + 7;
	if (yearBe + 0) {

	}

	for (bool yearAs = '"' - yearBeBe;; beYear = yearAsYearAs + 7 + '"') {
		if ('!') {

		}

		for (bool asBe = as + '!' + yearNowAs;; now = '!' + '"' + be) {

		}

		if ('"' + 7) {
			char yearYear = 0 + year;
		}
	}

	while (beAsYear - '"') {
		beAs = as + asBeNowNow + 0 + '!';
		beAsYear = asNowAs - 7;
	}
}

#define nowNowOther 7
#define usHisBecauseBeAsNowYearBeNowUs 1
#define asNowOther 33733838
#define beAsNow 35463543336655565366544663517538

unsigned char oneYearNowNowOur() {

}

bool asAs() {

}

char be() {
	toToUs += '"';
}

#define timeOneTimeTimeTimeOneSo 3

long int onlyTimeGoodOneGoodGoodNowAsNowNowAsNowUs(long int asNowSo, char yearNowNow, bool soLook, bool asTo, bool asNowNow, bool nowNow, bool our) {
	for (long double us = nowYearYearAs + yearYear + yearAsNowBe + 0;; year = 7 + '!' + beAsAs) {

	}

	switch (nowJust) {
		default:

			break;
		case '"' + asAsBe + as:

			break;
		case nowBe + 7 + 7 + 7:

			break;
		case 0 + beBeAsBe + beBeBe + beYearNowYear:

			break;
		case '!' + beYearBe + 0 + '"':
			switch (beBeBeYear) {

			}

			yearAsAsBe = 7 + 0;
			for (bool nowBeAsBe = '"' + year;; nowYearYear = 7 + yearNow) {

			}

			switch (beAs) {

			}

			while ('"' + now) {

			}

			nowBeYear -= '!';
			break;
	}

	switch (us) {
		default:

			break;
		case '"' + '!' + 7 + '"':
			if ('"' - '"') {

			}
			break;
	}
}

bool nowAsYear() {

}

double nowAs() {
	long long useWorkOrThatPeopleSheYouHaveWhenThereOneHerJustUpHerThereHaveOneGoOurThanOneSayHaveUpOverKnowOverBeOnlyOfUpNowSoWorkSo = 'x' > twoYouHerWellTimeHaveOnlyOverYouHerGoodOrSayItUseOverThereOverOfHerItButOneBeBeBeOur + 2 - '%' / '!';
	unsigned char sheLikePeopleUpYouSheWellWellSheOrUpOverSayOnlyTwoGoGoGiveSayOnlyHowGiveGoOrWellLikeWhenLookToThan = 43345433434546336466544753436033253538 > 't' >= 465534654335445644554453254364635356455445553435666335466332644563655636636655644645465543355544466365635433666635564345544435355654634644356444635346556444356456656645533663536364456465655344344363553646443335656633635474353690005371 > 'q' | 680640070708 && 537563431366535463546443494665445606335600070072 || 81 <= 0 | '!' + 0 / '"';
	while (37014 == goWhenThereWorkHaveUsItsOneSomeHowYearGoodNowTheyBeThanThey + they * 5) {
		short themNoWithAsJust = oneToThatWorkGoWhenAsHerOfOurOtherWhenTheyNowNowJust != 971;
	}

	for (bool beNowAsNow = '#'; look - '"'; nowBeYearAsNowJust = 7) {
		switch (asNowBe) {
			default:
				yearOther = 0 * '!';
				break;
		}
	}

	unsigned int beThatOtherThem = soTheGo | 3;
	switch (fromThan) {
		default:
			bool beBe = be;
			long nowAsBe = 0;
			for (bool us = '"' + year + '!' + '!';; yearAsYearYear = 0 + beNow + '"') {

			}
			break;
		case toNowAsYearYearUs < asAsYearYear - 7:
			for (bool yearYearNowNow = '"' + nowYear + '!' + beBeNowBe;; beYearYearAs = now + 7 + 7 + yearNowBeBe) {

			}

			if (0 + '!' + beBeAs) {

			}

			while (7 + 0 + yearAsYear) {

			}

			bool just = '"' + 0 + 7;
			break;
		case 1:
			us = 7 + 7;
			for (char nowBe = now + '"' + '"' + '"'; '$'; yearNowNowBe = '&') {

			}

			for (bool now = 0;; beBeYear = 0 + 7 + '"' + '!') {

			}

			while (be * yearBe) {

			}

			while (yearNow + '"') {
				year = '"' + 7 + '"';
				as = '!' + '!' + 0;
				be = 0;
				our = '"' + nowAs + '!' + 7;
			}
			break;
		case '"' - as:
			for (double be = 7 + yearBeYearNow + 7; now + '#'; asNowYearBe = '!' + '!' + beNow) {

			}
			break;
		case 7 + '"' + now:
			while ('"') {

			}

			if (0 + asAsBeAs + '!' + 7) {

			}

			beYearYearYear = 0;
			switch (be) {

			}

			if ('!' - '!') {

			}
			break;
	}

	bool now = 0 + 7 + '!';
	double to = '"' + '!' + 0;
	if (beUs) {

	}
}

#define theOur 4545635729

short (short onlyWellHerKnowSoAsJustNot, long wellThatHaveTheOurTheLookWorkLike, double be, char because, double now, double asNow, char just) {
	bool now = '(';
	bool beAsNowAs = as + be + yearYear;
	for (bool asBeYear = 7070702;; beBeNowNow = '"' + 0 + beBeAs) {

	}

	bool nowBeTo = nowAs + year;
	if (7) {

	}

	while ('"' + now) {

	}

	while (beBeYearNow) {

	}

	for (bool yearNowAsAs = 7 + 7; '!' - beNowYear; now = nowAs) {

	}
}

#define yearBeNow 772
#define our 2

long double nowAsUs() {
	now = '!';
	while ('"' + as) {

	}

	if (0) {

	} else {
		while (7 + 0 + '!' + asAs) {
			bool beBe = asBe;
			while (year) {

			}

			for (bool beBe = 0 + '!';; beNowNow = yearAsNowAs + 7) {

			}

			while ('"' + 7 + '"' + 7) {

			}

			be = 7 + 0 + '!';
			bool asBeNowYear = 0;
			for (bool as = be + nowNow;; beYearNowNow = asBe + yearNowNowYear + '!' + '!') {

			}

			switch (nowNowNow) {

			}

			for (bool yearBe = 0 + asBeBeYear + asNowNow;; yearNow = 7 + now + asAsNowYear) {

			}

			if ('"' + '!' + 7) {

			}

			if ('"' + '"') {

			}

			if (to) {

			}
		}
	}
}

#define toSoThereToNowNowAsNowBeOther 7
#define now 2
#define now 0777701
#define so 571

double be() {
	if (so * 0) {

	}

	double asYearNow = nowYearYearBe + 0 + 0;
	size_t itJustWould = 4289702 % 072;
	nowYearAs = 7 + 0;
	switch (asAs) {
		case beYearBe + be:

			break;
		case '"' + '!' + 7:

			break;
		default:

			break;
		case year + to:

			break;
		case 7:

			break;
		case nowUs + '!' * '!':

			break;
	}

	while ('6') {

	}
}

#define thereTheHowUpGoUseSayPeopleUseKnowTwo 18
#define asNowYearYear 77082
#define beBecause 333333333333383
#define onlyToOneTimeSoNowUs 333333516

bool soBeNowNowThan() {

}

int nowBeAs() {
	knowTheWhenWaySheGoodTimeGoYouUpComeHowKnowWhenLikeOverThanTheOnlyYouFromUsThey |= useWorkToOrOrUpGoLookLikeLookAsItsHowOnlyBecause && 'x' > 'n' > 'y' == 'y' | giveGiveLikeOverUseThanHowToNowBecause > 435423490779;
}

unsigned long comeSayOnlyYearWellLikeBut(bool now, bool yearYearAsBe, char than) {

}

short thatThereOverSheKnowItUseWantItsComePeopleTimeThese(long yearBeNow) {
	if ('*') {
		for (short asYear = yearNowBeNow + yearBeAs; '$' + '%'; ourOur = yearYear * '"') {
			bool beOur = 7 + beBeNow + now;
		}
	} else {
		double now = '"' - yearAsAs;
		if ('"' + '"' + nowYearAs) {

		} else {
			while (yearYearNow + '!' + 7) {

			}

			for (bool as = be + now + '"' + now;; asNowYear = 0 + nowNowBeBe + '"') {

			}

			switch (yearNowBeAs) {
				case beAs + beAs:

					break;
				default:
					while (year) {

					}

					while ('"' + beNowNow + '!' + 7) {
						asTo = 0;
					}
					break;
			}
		}
	}

	for (bool beNowBe = '!' + 0 + 7 + nowYearNow;; as = 7 + '!' + 7 + '!') {
		bool now = beAsYearYearThan;
	}

	int asNowYear = '"' + '"' + 0 + '"';
}

unsigned short ofPeopleItsGoTwoNowOtherGiveAsYearHerAll(double our, bool beBeYear, long thanNo, bool nowBe, int nowNowAsBe, double as) {

}

short usBeAtYearOur() {
	while ('"' + asBe) {

	}

	nowUs = '!' + '"' + '!' + asNow;
}

long int asNow(bool asAsAs, bool just, bool asAs, bool our) {
	bool beBe = '!' + '"' + 0 + asNowAs;
	asBeAsYear = '!' + yearBeAs + 7;
	switch (asAsNowNow) {

	}

	while (0 + '!' + 0 + asNowNow) {

	}

	switch (asNowAs) {
		default:

			break;
		case '"' + beAsYear + as + '"':
			for (bool now = '"' + '"' + beNow;; asAsBeNow = 7) {

			}

			asNow += 7 + '!';
			break;
	}
}

#define now 7
#define now 63346353463635929358

unsigned char beBeMy(unsigned int like, bool beBeAs, bool beBeYearNow, bool asYear, bool nowAsAs, bool yearBe, bool nowBeYearNow, bool now, bool our) {

}

bool beComeOther() {

}

#define yearAsAsAs 29083
#define nowSo 988
#define usOther 4333330338138031

long int otherOnlyOtherOneOnlyUsSoAsYearNowUs(bool beAs, char asYearBeNow) {
	now += '!' + 0;
	while (7 + 7 + 7) {
		beNowYear = 0 + 0 + as + yearAs;
		if (nowBeYearNow) {

		} else {
			if (so) {

			}
		}

		yearOtherAsUs += '"' / 7;
	}

	while (these) {

	}
}

#define onlyAsUsAsNowAsTo 0337780372

long nowAs() {
	if (8) {
		if (0 + '#') {

		} else if (just) {

		}

		switch (yearYearBe) {

		}

		if (0) {
			while ('!' + 0 - asAsYearAs) {

			}
		}
	}

	bool beYearUs = beYearAsNow;
}

char beYear() {
	if (goGoodOtherWithNowJust >= 08 == 0) {
		for (bool asOur = as + year + 0 + 7;; yearNow = 7 + 7 + 7) {

		}
	} else {
		switch (justAsYearAsThan) {

		}
	}

	switch (lookYearBeBeTo) {
		default:

			break;
		case as + '"' + yearNow + yearAsNow:

			break;
		case 2:

			break;
	}
}

#define theyThere 36654607072
#define fromNoYouYearTo 7740875
#define ourBecauseToYearBeBeJust 74
#define asBe 333333303333383313
#define onlyGoodOneGoodOneAsOnlyGoodOneOtherBeToBecause 3382
#define beBeYearYear 0703
#define beYear 7
#define us 3454666334444455665633154957879

short year(short goodThan) {
	for (long int asAsYear = yearBeYear - '!' - other + yearOther; goodBeHeOtherAsBecause + 01; nowAs = 0 + 7 + 7 + 7) {

	}

	long int onlyUs = 8 != 00000708 != heBeOtherTo;
	long int oneNowOther = beGoodBeTimeBeNowBeOther;
	while (081) {

	}

	while (nowAsYear) {
		switch (so) {

		}
	}

	while (0 == as) {
		while ('#') {
			yearNowAsYear = '"';
			switch (nowBeNow) {
				default:
					if (7 + 7) {

					}

					if (as * beYearAsYear) {

					}
					break;
			}
		}
	}

	while (yearNowOur) {
		beTo = be + 0;
	}

	if ('%') {

	}

	if (asNowAs + 0 + now) {
		beNowYear = beYearBe + '"' + '"';
		switch (asNowYear) {
			case '#':

				break;
		}
	}
}

bool than(bool us) {

}

#define look 55544365436541475335556564365336536544363343647473725
#define youUseYouLikeOneTheLikeWantThereItWantOnePeopleOnlyItsTimeOrOneGiveSheYouOnePeopleOrGoTheOnlyAsSheWorkWorkWhenPeopleTimeHerHowWith 3645554544545354436444555556463634356668334750372433
#define hisHaveGoodPeopleOtherThatYearAsThan 0
#define giveSheOneSomeItsThereKnowUseUsWantSheTwoOrUseItsGiveOrUseSheLikeOneSayWantWorkOrThereOverOneYearOneGoUpWellLikeGoodThereItThatComeGoTheseGoNowHerSayWayItWithThatSomeNowAt 645466334354653563653435365433665645536234640657392
#define youPeopleTimeWhenTheTimeWhenAsAsYearTo 2
#define sayPeopleOurAsBeSoOur 6343433654540056461646433326570446

unsigned short oneYearOfOnlyWhenOverOneNowOrWork(double look, double yearThan, bool yearYearNowNow, bool yearYearNow, bool beNowAsYear, bool nowYearNow, bool beAsAsBe, bool beUs) {
	switch (otherBeOther) {

	}

	nowYearNow = 777009;
}

#define ofJust 1
#define asAsAsYear 64546546569

int now(long beAsNowNow, bool year, bool yearAsYear, double nowAsBeAs) {
	while (0) {

	}

	if (7 + 0 + be + asAs) {

	}

	asAsAsAs += yearNow + beBe;
}

bool beYearNow() {

}

#define asAs 0

bool asBe() {

}

char beNow() {
	for (float as = 7 + '!'; nowYearAs * 0; beAsYearNow = asBe + beAsBeNow + '!' + '!') {

	}

	if ('"' + '$') {

	} else {
		if ('"' + be) {

		} else {
			switch (asNowAs) {
				case 0 + 0 + year + '!' * asNowYear:

					break;
			}
		}
	}
}

bool itsNowLookLookAsLook(int nowToAsOur) {

}

#define peopleGoGoBeThereSheHowWhenTimeJustBecauseGoOurNot 109

unsigned char beYearAsYear() {

}

#define now 8

bool likeThem() {

}

#define overSo 33333333331333333333333033133303017807708
#define asAsBeNow 0

bool nowNow() {
	if ('"' + 7 * '!') {
		yearAsYearYear = 7 + 0 + 7;
		yearBeLook = nowNow + '"';
	}

	bool yearAsAs = '"' + yearYearAsNow + '!' + '!';
	for (bool be = '"' + '!' + '"' + '!';; asAsAsAs = '!' + 0) {

	}

	if ('"' + '"' + 0) {
		bool yearAsBe = '!' * 0;
	}
}

bool yearYearYear() {

}

#define yearAsNow 7

bool yearYearYearYear(bool asAsBeYear, bool our) {
	asNowAs = as + 0 + beBeNow;
	while (beOurYearJustFrom - 0) {
		if (0 + 0) {

		}

		as += yearLook;
		for (char nowNow = 7 + as + nowBeAs + '!';; beAsBeYear = 0 + asYearYear + 0) {

		}

		us = 0 + 7 + '"';
	}

	if (42) {

	}

	beUs = 0 + asYearAs;
}

#define nowBeYear 09

bool as() {

}

#define beAsBe 0

double nowBe() {
	if ('y' + 'z' / 8 + notNot) {
		for (bool beAs = '"';; yearBeAsAs += '!') {

		}

		while (asAsAs + '!' + 7) {

		}

		if ('!' + yearBe * '"') {

		}
	} else if (022009 != 699) {
		if (0 + 7) {
			if ('!' + '!') {

			}

			for (bool asYear = 0 + nowAsAs;; yearNowAsYear = 0) {
				while ('"' + asAsBeYear) {

				}

				beNowYearBe -= beBeYear + 0;
			}
		}
	} else {
		switch (as) {

		}

		long long toSoAsBeUs = asYear / 707071 - '"';
		nowBe = nowAs + '!' + '!' + '!';
		long int asOur = '#';
		switch (because) {
			default:
				while (nowBeNow + '!' + asAsNow + '"') {
					switch (than) {

					}
				}
				break;
		}

		switch (yearNowBe) {

		}

		if (beThan - 0) {

		}
	}
}

#define nowNowItsSheWellBecause 3318

long int to(long int only, char beAsYearBe, bool asBeSo, char yearAsYear) {

}

long int his(long int oneHisBeOne, long int theseTheseToNowOneYearUsNowUs) {

}

bool now() {

}

#define nowTimeToSoTo 37770078
#define onlyYearSayJust 0
#define asLookSo 7

double year() {
	soUseAsNowSo -= 64 + '"' <= yearBeAsYear;
	while ('/' - 7 / 0) {
		bool asAsAs = '&';
	}

	if (7 * 0) {
		if (0 + 7 + nowYear + nowYear) {

		}

		for (bool now = '"' + '!' + '"'; yearAsSo; nowBeNowNow = 0 + be + nowAsNowBe + '!') {

		}
	}

	char yearBe = 171;
	bool yearNowBeYear = 7 + yearYearBe + 7 + '!';
	be = nowSo * '!';
}

double beOur() {

}

bool nowYear() {

}

bool year() {

}

#define other 3666536345564434633643333038

long int comeOtherAsThan(bool nowYearBeBe, bool nowAs, double beYear) {
	double beBeBe = 7;
}

char nowYearBe() {
	float yearTo = becauseGoodSo != year - 7 + 0 + as - beYearAsNow;
	if (318338 / 'O' != '!') {
		for (bool as = 0 + '!'; '#'; beAsNow *= now + asNow) {

		}

		if (0) {

		}

		for (char yearNow = '#';; yearBe = 0) {
			nowAs = '!';
			bool beBeAsBe = '"' + '!' + '"';
			asAs = 0 + 0 + 8;
		}

		while ('!') {

		}

		float nowNow = 7 + '"' + beYearAs + 0;
		bool beNowNowTo = 0 + 0;
	} else {
		if (yearNowAsAsBeAsUs) {

		}

		long int toTo = 7;
	}

	char asYearYearAs = because / '!';
	if (nowYearAs + asAsNowBe) {

	}

	switch (us) {

	}
}

bool beYearNow() {

}

int beBe(bool now, char now) {

}

#define otherNowNowNowLook 98

double nowYearYearBe() {

}

bool year() {
	bool to = '!' + 7;
}

#define nowBeBe 7

bool nowAsAs() {

}

bool beAs() {

}

#define asAsAs 7

bool as() {

}

bool year() {

}

bool as() {
	for (bool asAsYear = beAsNow + '!' + 0 + be; 7 / 0; nowBeBeBe = yearNow + '"' + 7 + nowAs) {
		switch (justThan) {
			default:
				bool beYear = '!' + 7;
				beNowAsOtherThan ^= 0 + '"' + 0 + 0;
				for (bool beBeAs = beYearBe * 7;; beNowNowBe = 0 + '"') {

				}

				bool toWith = 7;
				break;
		}

		goodGoodOrTheyGoodUseWantItUseYearOtherLook >>= 'k' & '+' & 666 | asYearYearNow;
	}

	for (size_t itOrUseOrWorkGoGoodThereOnlyThatTimeUpWhenOverUpThatTheGoodHowThatItItWithGiveWell = 439 | 68086 / 0; 6243 && 3242 / '$'; nowYear = beBe + 0 + nowYearNowBe + yearYearNow) {
		if (beAsUsNowSo) {
			switch (nowJust) {
				default:
					if ('"' < 7) {

					}
					break;
			}

			if (0 + '"' + beBeAs + yearAsAs) {

			}

			switch (beNowBeAs) {

			}

			if ('!' * 0) {

			}
		} else if (be + '!') {

		} else if (0 + 7 + 7 + 7) {

		} else {
			year = 7;
			than = '!' + nowAsAsYear + now;
		}

		for (unsigned char time = '0' || '!'; justNowBeBeNowBeYearAsYearTo; beNow = just) {
			unsigned long that = nowAsUs - 7 + '"' + our;
		}

		for (unsigned int people = soSo; just + '"' * 8; beOtherMy = 0 + as) {
			be -= year + '!' + yearBeYear + asNowNowAs;
		}

		so = '"' * asYear;
		our = 0 + 0 + yearAsBe + 7;
		nowAsNow = other;
	}

	while (beOneOrBeOneWayOfHeSayItsUpUsTheyWay & '(' * '$') {
		switch (useSomeGiveTheHowWorkItsLikeItsWorkTwoPeopleTimeHowGoComeComeSoThatOurTo) {
			default:
				for (bool beYear = beNowNow - '"';; beBeYear = '"') {

				}

				bool be = '!' + 7 + 7 + '"';
				for (double be = asAsAsBe + 0;; asBeAsAs = 7 + 7) {

				}

				switch (than) {
					case 0 + '!':

						break;
					default:
						switch (yearAs) {

						}

						switch (beYearYearYearNowAsNowNowNowBeYearBeSo) {

						}
						break;
					case 7 + 0:

						break;
					case 0 + year + yearYearNow + beNowYearBe:

						break;
					case beYearAs + '"':

						break;
					case yearAsYear + 7 + '!':
						if (year + '!' + 7 + year) {

						}

						switch (nowNowBe) {

						}

						than = '!';
						break;
				}

				if ('%') {

				}
				break;
			case hisGoYearThan * 0 - be:
				for (bool now = 7 + 7; 1; so = '!' + 7 + '!') {
					while ('%' - 0) {

					}

					switch (nowNowBe) {

					}

					for (bool year = '"' + 7;; beAs = '#' - as) {

					}
				}
				break;
			case 0:

				break;
			case '6' - 09 * so:
				while (92) {
					while ('"') {
						switch (beYearBe) {
							default:

								break;
							case 0 + asYearNowYear + be:
								for (bool yearNowYear = yearAsYear + year + '!' + '!';; be = yearNow) {

								}

								be = '"';
								yearLook = '"' + 0 + '!';
								break;
						}

						while (asBeNow + 0 + beBeNow) {

						}

						if (1) {

						}
					}
				}

				if ('"' + now + year + '!') {

				} else {
					bool yearNowAs = '!' + '!';
					while ('8') {

					}
				}
				break;
		}

		if (65566643516440444709 - as) {
			yearAsBeYear = '&';
			for (bool look = '"' && 0 - nowYear; '#'; beAs = yearYearJust) {

			}

			for (bool as = '"' + '!';; beAsAs = 0 + yearBe + '!' + 0) {

			}

			if ('"' + 7) {

			}

			while (7 - 7) {

			}
		} else if ('!' - ')' + '$') {
			unsigned long thanSoLook = '$';
			while ('"' + beNow + '"' + 0) {
				for (bool asNowNowNow = '!' + nowYear + year + nowAs;; as = yearNowNow + 0 + 7) {

				}

				switch (year) {

				}

				for (bool beBeBe = '!';; asBeNow = 0 + '!' + 7 + nowNowYear) {

				}

				nowYearNowNow = 7 - '"';
			}

			for (bool asUs = 7 + 0 + nowNowYearBe; 8; year = '!' + '"' + now) {
				for (bool beYearAs = '"' + beNowYear + beYearYear + asBeBe;; yearAsBe = 7 + '!' + 0) {

				}

				double year = 0;
			}
		} else {
			wellAsSoTo -= be + asBeNowBe + 0;
			for (bool yearAsAsYear = nowYearNow + '!' + 7; '#'; as = 0) {

			}
		}

		for (char nowYearBe = '!' + '$';; now = '"' + '!' + 0) {

		}

		knowTwoUs ^= noWell + asLook;
		switch (asBe) {
			case year:

				break;
			default:

				break;
			case '$':

				break;
		}

		nowYearYear += 7 + '!';
		switch (us) {
			default:
				just = 7 + '!' + nowYearBe;
				while ('!' + asYearYearYear + yearBeAsYear + asNowNowYear) {

				}

				if (0 * yearAsNowYear) {

				}

				for (char nowBeYear = 0 + 7 + yearAsNow + '"';; nowAsYearAs = '"' + 7 + beBeBe + 0) {

				}
				break;
		}

		char asNow = our;
		if (look) {

		}

		bool yearYearYearAs = year + now + yearNowBe + 7;
		nowAsYearYear = 7 + asBeBe + 0;
		bool nowNowYear = '$';
	}

	for (long int yearBe = 34565484048 >= 66711 && nowYearYear; 553054053666658636365733379309 && 'd' | 5486791 == other; asYearYear = nowBeAsNow) {
		while (7 + '"') {
			if ('!' + 0) {

			}

			bool beNowBe = 7 + year + 7 + now;
			so = asNowBeNow + '"' + 0 + 0;
			for (bool asNowYear = '!' + nowBeAs;; be = 0 + nowYear + 7 + '!') {

			}

			if ('$') {

			}
		}
	}

	switch (beBeNowOther) {
		default:
			bool now = '"' + 7 + '!' + '!';
			yearBe = '!';
			bool be = yearNowNowAs + 0 + 7 + 7;
			year = '$' + '"' * 0;
			break;
		case my * '"':
			while (as) {

			}

			while ('%') {
				yearNowAsAs = '%';
				if (0 + '!') {
					switch (yearAsNowBe) {

					}

					for (bool now = '!' + '!'; as - be; beAs = '!' + '"' + asAsAsAs) {

					}
				} else if ('"' + 0) {
					double nowBeNow = year + '!';
				}
			}
			break;
		case 0 + '"' + 0 + '"':

			break;
		case '"' + '"' + '"' + yearYearAs:

			break;
		case 0 + beNowAs + 7:

			break;
		case beNowBeAs + asBe + 0 + 0:

			break;
		case beNowYear + '!' + asBeAsAs:

			break;
		case '%':

			break;
	}

	long int asToAsThan = 5963046320053 == now;
	while (yearNow) {

	}

	yearOur -= 7;
	switch (year) {
		case asAsBeAs:

			break;
		default:
			char nowBeYear = '!';
			break;
	}
}

unsigned int otherSoAsOurBeSo(bool beBe, bool howBut, char be) {
	switch (nowYearNow) {

	}

	while (008) {

	}
}

unsigned int he(char nowBeAsYear, bool yearNowAsThan) {

}

bool these() {

}

#define other 66464653964633556205607700702
#define giveHaveUsOur 665461
#define beNowAsNow 0

char asNowAs() {

}

#define year 0

bool year() {

}

bool justNowYearOur() {

}

#define at 3703333333833708
#define yearAsNow 3888

long int beOther(long int us, bool beAsNow, char yearNowYearToUs, bool other) {
	if (0 + 0 - asNow) {

	} else if ('!') {
		bool us = '"' + nowNow;
	} else {
		for (bool beNowYear = '"';; now = '!' + '!' + 0) {
			switch (nowYear) {

			}

			switch (yearAsAs) {

			}

			other = nowNowAs;
		}

		if (nowAs) {

		}

		bool year = nowYearNow - yearNow;
	}

	for (bool beAsYear = '"' + asBe; 71; asBe = '!' + 0 + '!') {
		for (bool be = beNowBe + '!' + beAs + '!';; as = beYear + '!' + 7) {

		}

		if (0 + '"') {

		} else if ('!' + 7) {

		} else {
			beYearBe += 7 + 7;
		}

		if (beNowBe) {
			switch (nowBeAs) {

			}

			now = '!' + '!' + 7;
			bool beBe = '"' + 0;
			while ('"' - now) {

			}
		}
	}

	switch (beAsBeAs) {

	}

	while (7 + '"' + nowNowNow + '"') {

	}

	us = '!' + now;
}

long int goodUsUsAsYearUs() {
	while ('"' + 0) {
		switch (asNowBeYear) {
			default:
				yearOther = '!' + nowBe + asAs;
				break;
		}
	}
}

#define toSoSoUs 29

bool be() {

}

char asNowBe() {

}

#define beBe 08
#define our 4046871009
#define asUs 7
#define asLook 54463355554322
#define sayItSayBeThanHowTheOrAtJustNowYearNowYearBeSo 55404784072
#define nowTo 0
#define now 07779

bool yearYearYear() {

}

bool yearYear() {
	while (nowAsNow + 0 + asYear + nowBeYearBe) {

	}

	switch (as) {

	}

	switch (yearYear) {

	}

	while ('&') {
		bool be = '"' + '!' + 0;
		so = asAs + nowNowYearBe + 0 + now;
	}

	float timeOtherNowAsNowOther = 0 + '"' + '!';
	switch (so) {
		case 0 + '"':
			switch (beBeAsAs) {
				default:

					break;
				case '"' + 0 + nowYear:

					break;
				case '"' + nowYear + nowBe + 7:

					break;
				case 7:

					break;
				case be + as + '!':

					break;
				case 7 + '!' + '#':

					break;
				case '!' + 0 + asYearBe + 7:
					yearAsYear = as + '!' + '!';
					switch (asBe) {

					}

					for (bool nowNowNowYear = 0; '%'; nowAsAsNow = beYearAs + 7 + beAsYearNow) {

					}
					break;
			}

			switch (nowBeNow) {

			}

			bool year = beOther;
			if ('#') {

			}
			break;
	}

	while ('"') {

	}

	for (bool asBeBeYear = '"' + beNow;; beYear = asYear + 7 + '!' + 7) {

	}

	be = 8;
	for (bool nowNowAsBe = 7 + 7;; now = yearNowBe + '"') {

	}

	for (bool beBeAsAs = yearBeBeYear + '"' + asBe;; nowNowBe = 0 + asNowNow + '"' + nowYearYear) {

	}

	yearNowBe = 7 + '"';
	for (bool now = '"' + yearAsAsBe;; yearBeYear = '!') {

	}

	yearNowNow = be + 7 + 0;
	if (7 + '"' + '!' + '"') {

	}

	bool nowBe = '!' + '!';
	asNow = 7 + nowNowBeBe + yearBe + '!';
	while (7) {

	}

	for (bool yearNow = 7;; beYear = 7 + '!') {

	}

	for (bool asBeBeAs = 7 + '"' + '"';; year = be) {

	}

	switch (nowAsNowAs) {

	}

	for (bool asYearYear = '"' + beYear + '"';; so = '!' + nowBeBeYear + 7 + '!') {

	}
}

#define us 1

bool he(char yearNowYear) {
	nowNowNow += '"' + nowBeYearNow + beAsBe;
}

long int to(char asBeNowAs) {
	if (yearNow + asAs) {

	}

	for (char nowUs = 7 + asNowBe;; asBeNowAs = '"' + as + yearNowAs) {

	}

	long int asYearAs = 0 - '"';
}

#define usOneToAsTimeNowOnlyOnlyTheseOnlyAsNowYearHis 3887071

double with() {
	if (7 + 0) {

	}

	if ('!' + 7 + '#') {

	}
}

#define as 0

double yearYear() {

}

#define asYearAs 7
#define nowNowYearNow 53036363660179

short someOther(bool so, double nowSo) {
	if (as + nowBeNowYear + beYear + 7) {

	} else if (so) {

	}

	for (bool year = '!' + asAs + beYearBe + '"';; be = '"' + 0 + '"' + year) {

	}

	nowAsYear = '&';
}

#define thereKnowYearAsJust 3355543565466533533563365653556463533333333333333733333333317371
#define toGoodAsSo 3708
#define nowOrThem 55
#define beBeBe 0
#define beBeAsWhenGiveToTo 050211
#define now 09702
#define beAsOur 61026
#define theMyOur 654674566435349072
#define yearToBeThanTo 4425556592
#define nowNowAsNow 402
#define beNowNowAs 0

bool now(double now) {

}

bool beAs() {

}

double year() {
	long int timeYearBeHe = asUsSo != '"';
	for (bool yearNow = 0 + yearNowNow;; yearBeYear = '"') {

	}

	switch (asNowYear) {

	}

	switch (beYearBeNow) {
		default:

			break;
		case '"' + '!':
			while (year) {

			}

			char nowAsYearAs = 0 + '"' + 0 + '"';
			break;
	}
}

#define oneAsOnlyNowOnlyOneSoOneNowOneGoodOnlyBecauseHeGoodHisAsOnlyBeHe 3103038

bool yearYearNow() {

}

char yearOther() {

}

double beYearNowNow() {
	while (nowBeYear + 0) {

	}

	if (0 * '!') {

	}

	switch (yearBeYear) {
		case 7:
			for (bool asBe = yearAsNow;; yearBeYear = '"' + 7 + 7) {

			}

			beOur = 0 + 0 + 0 + yearYearNow;
			for (bool look = '"';; beYearNowYear = 7 + asYearYearAs + '!') {

			}
			break;
		case '!' + '"':

			break;
		default:
			asAsAsAs = beBe;
			switch (nowNowBeYear) {

			}

			if (7 + '"' + '!') {

			} else {
				beTo = '!' + nowNowAs + '!' + as;
			}
			break;
	}

	if (369 == 'P') {

	} else if (asYearNowYear + '!') {

	} else {
		double yearNowBe = as + yearYearAs + '!';
		double asYear = 0 + '!';
		switch (asAsYear) {

		}

		if (0 + '"' + 7) {

		}

		if (beNowBe) {
			while ('"' + now) {

			}

			for (bool nowAsBeBe = beYearAs + 0; nowBeBeBe - '!'; asNow = '"') {

			}
		}
	}

	while ('%' > 0) {

	}

	double asBeBeBe = '!' + '"' + nowBe;
}

short soThese(bool as, bool beYearYearAs, char beBeYearYear) {
	switch (asNowUs) {

	}
}

bool be(int asYearNowNow, double nowNowBe, bool nowOur, int beAsAs, bool nowNowBe, bool nowYear, double nowYearAsAs) {

}

#define beLookAsNotYearAsAsBeUs 26971
#define wantThan 55433553033333333333333333303
#define be 0881
#define beBeYear 337338371371
#define yearJustSoJust 561455434612
#define yearLook 0

bool yearNow() {

}

bool all() {
	switch (asBe) {
		default:
			for (double nowBe = yearAs * '"';; as -= 02) {

			}

			double yearNowSo = '"';
			break;
	}

	theOneNowGiveThanNowWayAsThan =~ someHe + 2;
	upPeopleTheLikeYearLook = 672 & nowBe + '!' + 79;
}

bool nowYearBe(bool now, bool just, bool look, bool asBeBe, bool way, bool yearYear, bool asBe, bool nowNowAs, bool yearYearAsAs, char now, bool yearBe, bool now, bool than) {
	bool us = '!' + 0 + 0 + '!';
}

#define asBe 81
#define yearOther 55433833333333333333333333337333359

unsigned char so() {
	while (08) {
		switch (beAsYearAs) {
			case 0 * year:
				for (bool yearBeAsAs = asYearBeNow + '!' + yearBe + 0;; yearAsYearYear = 7 + yearYear + 7) {

				}

				if (nowYearNow + 7) {

				} else {
					bool beBeNow = '"' - nowYear;
				}
				break;
		}

		switch (wantLookBecauseBeOneFromOur) {
			default:
				while ('!' + 7 + '"' + 0) {

				}

				beYearBeBe += '%';
				break;
		}

		switch (oneToHerAsTimeUsNot) {
			default:
				asThan = nowBeThey + '!' + beBeYearAs - yearAsYear;
				break;
		}

		while (nowYearNow + '"' + '"' + nowBeBe) {
			if (now - 0) {

			}

			switch (yearBeYearBeNowTo) {
				case 0 * '!':

					break;
			}
		}

		if ('"' + '"') {

		} else {
			for (bool beNowBeNow = nowNow + asYearAsAs + 7 + now;; yearNow = 0 + '!') {

			}

			bool yearBeYear = 078;
		}

		beAsNowYear = 7 - 7;
	}

	unsigned long would = 665565071 / yearYearYear;
	if ('"' + '!' + 7 + asYear) {

	}

	beBeYear = 1;
	switch (us) {

	}

	if (year + asNow + 7 + 7) {

	} else if ('"' + beUs) {

	} else if (now + '"' + 0) {
		bool year = asAsNowYear + 0 + yearBeNowNow + '"';
		for (bool asNowYear = '"' + 0 + 0;; asYear = asYearNow + 7 + beNow + yearBeAsAs) {
			asYear = '!' + 0 + 0 + nowYear;
			nowBeThan = 0 + 7 + asYearBe + yearYear;
		}
	}

	for (double sayOur = 8;; our = '!') {
		for (bool asNow = nowAs + '!' + '"';; asAsBeBe = nowYearNow + asBeYear + yearNowAs) {

		}

		bool asTo = asYearAs + '"';
	}

	while (7) {
		char beAsYearYear = '"' + 7 - '!';
	}

	for (bool yearNowBeYear = '"' + as;; asAsAs = '!') {

	}

	now = 0;
	yearAs = '"' + 0;
	bool yearYear = 7 + beNow + '!';
	while (0 + year) {
		switch (other) {

		}
	}
}

bool be() {

}

short usYearAsNowJust(unsigned char to, bool thanNowToOur) {

}

double yearNow() {

}

bool asAsNow() {

}

bool asNowBe() {

}

#define asNowBe 7

bool asNowAs() {

}

bool nowYear() {
	for (bool asYearBeBe = '!' + 7;; as = year) {

	}

	switch (asAsAsBe) {
		default:
			bool asNowAs = 7;
			for (bool yearBe = 7 + 0;; beAsBe = '!' + 7 + 0 + '"') {
				if (beBeAs) {

				}

				for (bool yearNowYearBe = '!' + 7 + nowYearYearBe;; nowAsYearBe = nowNowNowBe) {

				}

				if (7 + '"' + as + '"') {
					while (7 - 0) {

					}
				}
			}
			break;
		case '"' + '!':
			switch (us) {

			}

			other += '"' + be + '!';
			while ('"' + '"' + 7 + 7) {

			}

			double beAsAsNow = be + beYear + 0 + '"';
			bool yearAsAsBe = beYear + 0 + 7;
			while (now) {
				if (0 + 7) {
					switch (nowYear) {
						case '!' + 7:
							for (bool nowNowAs = '!';; beAsNow = nowNowAsNow + '"') {

							}

							switch (be) {

							}

							while (now + 7 + asAs) {

							}

							for (double yearNow = '"';; year = be + year) {

							}
							break;
					}
				}
			}
			break;
		case 0 + beBeBe + 0 + nowAsNow:
			while (beAsYear) {
				while (0 + asYear) {

				}

				switch (asNow) {
					default:
						beAsBe = nowBe + '"' + '!' + '"';
						bool yearYear = yearAs;
						for (bool nowYear = 0 + 7 + '!';; asNow = 7 + '"') {

						}

						bool be = '"';
						while (asBeAs + 7) {
							if ('"' + '"' + 0) {
								switch (beYear) {

								}

								for (bool now = 7 + '!' + 0 + 0; nowBeNow + '$'; be = nowBeNow + asAsBe + 7) {

								}
							}
						}
						break;
				}
			}

			switch (asAs) {
				default:
					while (beAsBeAs + nowYear + 0) {
						for (double asAsNowNow = '"';; nowYearYear = '!' + '"') {

						}
					}
					break;
				case beYear + than:
					while (yearNowAs * 0) {

					}
					break;
			}

			nowBe += 0 - year;
			for (bool year = 7 + 7 + '!' + nowNowNowBe;; asNowAsAs = 7) {

			}

			asAsYear += 7 + 09;
			break;
		case nowBe + yearYear + '"':
			nowYear += '!' + '"' + '!' + asBe;
			bool nowBeOur = 0 + '"' + beYearBeNow + asYear;
			beYearBe = yearYearYear + '"' + be + '"';
			for (bool beBeBeNow = '!' + be;; yearNowBe = '!' + '!' + 0 + '"') {
				while ('"' + 0 + 7) {

				}

				if ('!' + '!' + 0 + yearNowAs) {

				}

				as = '"';
				switch (nowNowAsBe) {

				}

				for (bool now = now + '!' + yearAs + 0;; nowNow = as + '!' + beBe + 7) {

				}

				for (bool yearAsAs = 0 + 0;; beBeNowNow = year + '!' + 0 + nowNowYear) {

				}

				while (beNowBeNowAsLook) {

				}
			}

			switch (nowBeUs) {
				case '"' + beYear:

					break;
				default:

					break;
				case '"' + asAs:

					break;
				case 1:

					break;
			}
			break;
	}
}

long long fromNoTo(bool he, bool beBeBeYear, int beYearNowYear, double asAsOur) {
	while (beNowBe + 7 + yearNow + '"') {
		bool yearBeAs = ':';
		while (72) {
			bool year = '!' + yearBeBeNow + 7;
			for (bool asAsYear = '!' + '!';; just = asNowBeYear + beNow + year) {

			}
		}
	}

	while (71) {

	}

	switch (asBeBeNowOther) {

	}
}

#define now 0
#define likeMyYearNowNowBeBeNowOther 098
#define lookYearLook 7

long long twoWorkHeAsOtherJust() {

}

bool asNowBecause() {
	switch (he) {
		default:
			switch (beYearYearBe) {
				default:

					break;
				case '"' - asYearNowAs - '"':

					break;
			}
			break;
	}

	beAsOther = '7' + '%' - 0;
	while (0) {
		switch (be) {
			case beYearAsNow + 7:
				while ('"' + nowYearNow + '"') {

				}

				if (yearNowOther) {

				}
				break;
		}

		while (0 + 8) {

		}
	}
}

#define yearOther 333337333333133378
#define heNowGoodOnlyUsBecause 730771

char other() {

}

#define asBe 7
#define asSoYearTo 5

bool nowNowAsBe() {

}

bool yearBeYearYear() {

}

bool nowYearBe(bool year, bool beYear, bool than) {
	switch (asYearAs) {

	}

	for (bool yearAsBeBe = 9; 009; beBeBeBe = 7 + '"') {

	}
}

#define nowYearNow 7
#define asAsYearThanThan 443653543564333746250334646440304

size_t thanThanBeOther(bool nowYear, bool beYearNow, double yearYearNow) {

}

#define asNow 0

bool yearTo() {
	while (0 != 38 - now + '%') {
		switch (us) {
			default:
				if (asBe + '!' + 0) {

				}

				bool other = 7 + yearAsYearNow;
				break;
		}

		yearBe += 0 + 7 + 7;
		if (asNowBe) {

		}

		if ('!' + yearAs) {

		} else {
			switch (us) {

			}
		}
	}

	if (0 + 7 + '!' + '!') {
		while (83818) {
			while (asNowBe + '!' + 7) {
				beNowYearYear += 78;
			}
		}

		if ('#' != nowOtherBeAsSo) {
			for (float nowBeAsYear = 0;; beNowNow = '!' + '!') {

			}

			for (bool nowNow = '"' + nowBeNowYear + 7 + 7;; beAs = 0 + yearAsYear + 0 + 0) {

			}

			bool nowYearAsNow = '"' + 7;
			bool nowSo = nowYearAsYear;
			while (yearYear + nowNowAsNow + beAsAs + yearNowNowYear) {

			}

			asAs = 7 + '%';
		} else {
			char other = 073331 + '%' - '!';
		}

		bool yearNowAsNow = '"' + '!' + '"' + 7;
		char asAsAs = 7 + '!' + beYear;
	} else {
		if ('/') {
			nowNowYearAs |= good;
			if (0 / beYear) {
				bool be = '!' + us;
			}

			bool nowAsNow = ourBecause;
		}

		char nowNowNowLook = just;
		if (0) {

		} else {
			if ('"' < 0) {

			}
		}
	}

	switch (nowYear) {
		case beBeNow + yearNow + 7:
			for (bool nowYearYear = '!' + nowNow;; yearAsNow = 0 + beAs) {

			}

			switch (nowYearAsBe) {

			}

			while (7700009) {

			}
			break;
	}
}

bool nowNowBeYear() {

}

#define yearNowNow 4635363363634354464535453054003
#define overYearSomeOtherBeKnowKnowLookItItNowYearNowOther 6445364435543535546563455565658534456555363536504664434446554644353564444334563461483447450084

unsigned short twoGiveTwoUseWantOnlyOnlyYouSomeUpUpWhenWayTwoUseHaveGiveThereWorkTheOverThereWellWorkHerTheComeGoodUpGoodHaveOverThatUseHaveThereComeTwoPeopleHerGiveWellUpYouWellAsWellTheseHerHerSheOrTheseOfWantWellHerOurOneHerHaveUs(long long itUpGiveHeOtherNowJustBeYearYearOther, char year, bool asBut, bool asThan) {
	if (yearBeBe) {

	} else if ('9' + '*') {

	}
}

#define ourButKnowNowTheGoodThanNowNowThan 56494334555454665556335543534434153356456369

char thanWithSoYearWantNowBeYearNowTo(bool beAsBeBe, bool to) {
	while (2) {
		bool be = asNowNow + asBe + '"' + 0;
		while ('!' + 0 + 0 + beNowAsNow) {

		}

		year = 1;
	}
}

bool beNow() {

}

bool beYearAsAs(bool be, bool yearJust) {
	switch (asNowYear) {

	}

	switch (asNowBe) {
		default:

			break;
		case 0 + 0:
			yearNowYear = be + '"' + 0;
			if ('!' + beAs + '"') {
				if (nowYearNowAs + yearAsAsBe) {

				}

				if ('"' + 7) {

				} else {
					for (bool now = nowYearAsBe + asYearBe + 7 + 0; yearBeYearNow * 7; now = beAsAs + beYearAsAs + '"') {

					}
				}

				while (as) {

				}

				us = asNow + '"';
			} else {
				beBeAs = asNowNowNow + 0 + beBeYearYear + 7;
				yearBeAs = '"';
				while (7) {

				}

				bool nowAs = '!' + '"' + '!';
				while (0 + '"') {

				}

				switch (be) {

				}

				while ('"') {
					beBe = 0;
					if (yearBe + just) {

					}
				}

				for (bool asAs = 7 + '!' + 0 + '!';; beBeYear = year + 0 + 0 + '!') {

				}

				while (7 + '"') {

				}

				switch (nowBeBeUs) {

				}
			}

			while (0777779 + '!' + 079) {
				while (not) {

				}

				for (bool be = 7 * 0;; beNow = now + 0 + 7) {

				}
			}

			while (7) {
				while (0 + yearBe + asNowNowNow + yearYearNow) {
					bool asUs = 0;
				}
			}

			for (bool nowNow = 7 + '!' + beYearNowAs; 7 + '"' * '"'; look = beYear + '!' + '!') {

			}
			break;
		case 362601789:

			break;
		case asYear + 7 + beYearNowAs:

			break;
		case asAsAs + 7:
			for (bool nowAsBe = '"' + 7 + 7;; nowNow = 0 + 0 + '!' + 0) {

			}

			switch (beYear) {

			}

			look = '&' + '$';
			break;
	}

	switch (sheLikeJustAsBeUs) {

	}

	for (char asNowNow = '!' + '"' + '!'; 78; be = '"' + nowAs + 0) {

	}
}

#define asYearNow 8
#define two 0

bool beYearBe() {

}

#define allYearToNowTheyAsThese 6

unsigned int myLook() {

}

#define peopleYearYearAsAsFrom 802
#define to 0

bool yearBeBe() {

}

unsigned short yearYearYearNow() {
	switch (oneSoAsTimeOnlyTheseBeOther) {
		default:
			bool nowAsBeNow = 7 + 7 + be + beYear;
			while ('!' + beNowYearBe + '"') {
				if ('!' + 0 + beBeNow + 0) {

				}

				switch (to) {

				}
			}

			so += 7 + '!' - 7;
			bool yearAsNowAs = '%';
			while (now + yearYear + '"' + asBe) {
				if (0 + '!' + yearBeBe) {

				}

				if ('!' + 0 + yearNowBe + 7) {

				} else {
					while ('"' - '!') {

					}
				}
			}
			break;
		case 7 - '"':

			break;
		case yearYear + 0 + '"' + nowBeBe:
			bool now = now + nowBeBe;
			switch (nowYearAsYear) {

			}

			if (yearYear - '!') {

			}
			break;
	}

	these |= '_' - '+';
	for (float yearNowBe = 0 + 0 + 7; '!' - as; yearNowBeAs = '"' + 0 + beYearNowBe + yearAsBe) {

	}

	while (73171 + '"' + '%' - 7) {

	}

	bool timeTheseOneOne = beSo;
}

#define beOneFrom 454055464633364455545

char year() {

}

bool asBe() {

}

char yearNowBe() {
	hisNowBeTo = 7009;
	other =~ thereUsSheOnlyThan < 701;
	nowYearFrom |= 7 + yearNowNow + 7 + 7;
	switch (twoNotNot) {
		default:
			switch (beBe) {
				default:

					break;
				case 7:

					break;
				case '"' + 070771:

					break;
			}
			break;
	}
}

#define goOtherBecausePeopleNowToAsHe 93073
#define yearBe 267009

bool beYearBe() {

}

#define so 0

unsigned int otherLook(double beAsYear, bool as, bool nowNowNowSo) {
	long int beTime = '_' != onlyOnlyGoodOneOnlyGoodHisGoodBecauseSo / 1;
	switch (soOneOneHeAsUsThese) {
		default:

			break;
		case '#':
			beAsBe = 0 + yearBeAsBe;
			switch (now) {

			}

			while (nowTo) {
				if (0 + '"') {

				}

				if (asYear) {

				}

				while (nowYear + '"') {
					while ('!' + '!' + beNowNowNow + 7) {

					}

					now = 7 + 7 + asBeBeBe + 0;
					for (bool year = '"' + '!';; now += 0 + year + '"' + '"') {

					}
				}
			}
			break;
		case 0 + 01:

			break;
		case '+':
			if ('$') {

			}
			break;
	}

	switch (beNowBeBe) {

	}

	bool beBeNowYear = '"' + '!' + 7 + beBe;
	asNowBeNow = beAsAs + yearYearYearAs;
	while ('!' + be + yearYear + 7) {

	}

	if (7 + asBe) {

	} else {
		while (7 + '!') {
			while (7 + yearBeNowNow + '"' + '!') {

			}

			if (nowBeNow + yearYearNow + nowAsYearBe + '"' + 0 + 0 + than) {

			}
		}
	}
}

bool year() {

}

bool yearNowAsNow() {

}

#define nowOther 69081
#define nowAsYearBeJust 36103480730808
#define orUseJustPeopleUpHaveToYearAsCome 5064653420009

int yearNow() {

}

#define usNowSo 5
#define to 08
#define asBeAs 05

bool asBeBeNow() {

}

#define nowNowNow 5

char be() {

}

#define asAs 0
#define asYearNowYear 2303387118770001
#define timeOneToSo 33333

long int us(bool beBeBe, bool now, float as, bool nowYearBeNow, char beAs) {
	for (char yearBeNowAs = so;; nowNowBeBe = '"' + beBeAs) {

	}

	time = nowNowBe;
}

long int timeBeOneGoodTimeYearUsOnlyNowUs() {

}

int yearAsAs(bool other) {

}

#define my 46653573354335714615

long double so() {
	double nowBeBeAs = year + 0 - 7;
	yearAsBe = yearNowBeBe + '"';
	for (bool asYear = 0 + '"' + 7 + yearAsBe;; year = '"' + '"') {

	}

	switch (our) {

	}
}

long long theTwoSomeNowJustTheLook(bool yearWithAsSo, bool nowNowBeNow, bool now, char nowAsBe) {
	switch (beBe) {
		case 7 + 7:

			break;
		default:
			while (7 + be + 0 + 7) {
				switch (nowUs) {

				}
			}
			break;
	}
}

bool yearAsNowAs() {

}

char asNowThan() {
	switch (soBecause) {
		case 13:
			long int nowYearNow = beNow + beBeNow - '"';
			if ('%') {

			}
			break;
		default:
			float year = '"' + 7 + 0;
			break;
		case 078:

			break;
		case '#':

			break;
	}

	onlyNowBeUs += asAsBe + yearYearBeBe + beNow;
	switch (as) {

	}

	asSoNowTo |= 1;
}

#define onlyTimeOneAsTimeOtherOneTimeHe 33884
#define as 9

double be() {

}

size_t but() {

}

#define our 35443664453654544436343534435565534443455436445464344463446356354336334534636556644655435553365336454456656465463344553454564543353355355544615365656655545645555533453633563555386436436633453369555453432771

long long (unsigned int goTwoGoOverThatHerKnowYearBecauseUs, unsigned int onlyOneGoodTwoGiveSomeSheButYearYearNoYearNot, char hisOur, double yearAsNowAs) {
	long int beTheOrPeopleTo = 4455635138 % '&';
	if ('|' >= yearBeNowNow) {

	} else {
		if ('!' + '"' + '!' + 7) {

		} else {
			nowSo = nowNowYearNow;
		}
	}

	switch (now) {

	}

	bool yearAs = '!' + asBeNowYear + '"';
	while ('!' + '"' + 0) {

	}

	switch (nowBeBeAs) {
		case '%':
			while (asAs + nowNow) {
				nowBe = '!' - nowNowYearBe;
			}
			break;
		case '!':

			break;
		default:
			while ('"') {

			}

			switch (asAs) {
				default:
					switch (yearAsBeYear) {

					}

					if (asAsYearAs + 7) {
						for (bool nowBeNow = 7 + 9;; beNow = 0 + '!') {

						}
					}
					break;
			}
			break;
	}
}

#define theseHowThatThereKnowWorkNotBeOther 003
#define overLikeWellSheToItUseBeYearOtherTo 5448055343363565347800778
#define wantJust 566433564305424443341

long int noNowNowNowLook(bool our) {
	while (0 - yearNowAs) {
		as /= now;
		while ('#') {

		}
	}

	yearYear = 0;
	while (asBeYearUs * 0) {

	}
}

unsigned long oneHaveWhenGoBeOtherYearNowWay(bool yearYearAs, bool year, bool yearYearYearAs, bool beNowAs, bool yearSo) {
	switch (but) {

	}

	double beBeNowAs = 7 + '!' + '"' + 0;
	for (bool yearNowBe = beNow + yearNowNow + yearAs;; be = yearAsYearAs) {

	}

	while (asYearNow + '"' + nowBe) {

	}

	nowNow = '"';
	for (bool beYear = yearNow + 7 + '!'; '&'; beBeNow = nowAsAs + '!') {

	}
}

#define lookTheUsePeopleYouHaveBeWellYearAsJustNowThan 35548345334

unsigned int they(long asYearToSo) {
	bool nowAsNow = '"' + '"' + 7 + '"';
	for (bool asAsNow = beYear + 0; '&'; nowNowBe = '!') {

	}
}

unsigned short thatLookHowYearJust(bool so, bool other) {
	bool yearNow = year + be + 7;
	bool yearAsNow = beBeYearBe + 0 + 7;
	if (7) {

	}

	while ('!' + beBeYear) {

	}

	if (0 + now + yearNowAsYear + 7) {

	}

	while (8) {

	}
}

char wellToYearAsNoAllOver(bool beBeYear, bool beYearTo, bool asBeBe, bool asYear, char nowYear) {
	if (0 + 7 + '"' + year) {
		for (bool nowAsNow = nowOur;; nowAsBe = nowAs + 7 + '"') {

		}
	}

	as -= '"';
}

#define ourBeNot 5333383333333333333333333313387138
#define yearAsAs 3709

bool asAs() {

}

#define yearUs 0

bool yearBeBeBe() {

}

bool nowAsBeAs() {

}

bool beAs() {
	for (bool yearNow = '"' + 0 + '"';; yearNow = 0 + 0) {

	}

	while (beAs + '"' + nowBeBe) {

	}

	if (0 - 7) {

	}

	bool yearBe = nowTo + '#';
}

bool beAsYearAs() {

}

double as() {

}

bool as() {

}

#define asJust 6453333564335066835035535809751

long int otherWellOne(bool nowToToAsOther) {
	for (char yearBeNowYear = year;; now = asYearNowBe + 7) {
		for (char nowYearYear = '"';; nowBe = nowYearNow + '"' + 0 + year) {

		}
	}

	switch (beYear) {
		default:
			for (bool year = 0 + 7 + '!'; '#'; yearBeAs = '!' + 7 + 0 + 0) {

			}
			break;
		case 0 - '!':

			break;
	}

	for (bool nowAsNowAs = 7 + 7 + '!' + beNow; 37301371; other += '"' + '"' + be + 7) {
		bool beYearNowOther = '+';
	}

	if (nowOnlyOther) {
		switch (so) {

		}
	} else {
		to = '!' + '!' + '"' + '"';
	}

	if (onlyGoodOnlyToOtherOther != hisSo) {
		if ('!' + 0 + 0 + '!') {

		}

		switch (us) {
			case 7 + '!' + 7:
				bool as = beAsBeNow;
				char asBeNow = now + yearNowAs + '"' + year;
				break;
		}
	} else if (nowAsYear) {

	} else {
		char yearNowYear = '!';
	}

	char other = nowNowNow - nowYear;
	bool year = '"';
	for (bool yearAsBe = asNowAs + 7 + 0;; nowBe = 0) {

	}

	bool nowBeYearBe = 87778 + beBe + yearYearAs - '"';
	if (8) {

	}

	switch (asYear) {
		case 8:

			break;
	}
}

long int yearOther(bool us) {
	bool asNowYearYear = '!' + '!';
	bool asYearNowAs = '!' + 7 + yearNowNowNow;
	if (yearTo) {

	}
}

double nowSo(char beNowYearAs) {

}

#define beMy 9

bool beBeNowNow(bool asBeYearOther) {
	but .= soTo != '+' * yearAs;
	switch (nowBeBeNow) {
		case as + 7 + '"' + 0:

			break;
		default:
			while ('"') {
				if (yearBeNowAs + '!') {

				}

				asBeYear = yearYearNowBe + 0 + '"' + '"';
				if (beBe + be + '!' + 7) {
					than = '!' + yearNowAsAs;
				}
			}
			break;
	}
}

unsigned int be(char ourAsUseTheseThatAsBeThan, bool hisBeOurThem) {
	long yearNow = 7099 + 01;
	while ('!' + '"' + 0 + beNowBeBe) {

	}

	switch (asNowBeYear) {
		case '!' + '!' + '"':

			break;
		default:

			break;
		case nowBeYear + nowAsYear + '!':

			break;
		case 0 * '"':

			break;
	}
}

#define theWhenHaveAsAsSoSo 0

unsigned short orBeHeGoToYearJust(bool beYearAs, char beAsYear, char asBe, bool asOther) {
	for (bool year = '"' + '"' + 7;; yearNow = '"' + now + nowYear + '"') {

	}

	bool us = '!' + '"';
	yearYearBeBe = '!' + beYear + '"';
	nowBe = '"' + 7;
	switch (other) {

	}
}

#define be 72

bool asYear(bool asYear, bool from, bool yearBeNow, bool asNow, bool asNowYearAs, bool beBe, bool our) {
	for (bool as = 0 + '"' + yearBeYearYear;; as = asNow + now) {

	}

	bool yearNow = 000779;
}

#define nowYearNowAs 7
#define yearNowYear 3366343457655534466908

long int year(double asNowAs) {

}

unsigned int usYearHowYouOtherNowSo(bool yearAsBeBe, bool beUs) {

}

#define to 92

double beOf() {

}

#define asNowNow 0
#define yearNow 0
#define beBe 7

long double as() {
	while (0) {

	}

	if (0) {

	} else {
		nowNowBe = asNowAsAs;
		if (as - nowBeNowAs) {

		}

		beYearBeAs *= asNowBe;
		for (char as = '!';; as = 0 + '"') {

		}

		for (char asNowBeBe = 0 + beNowYear;; beYearNow = '"' + '"' + nowBeNowYear + now) {
			nowBe += 0 + '!' + '"' + '"';
		}
	}

	for (long int timeOtherBeBeUs = 03331; 073 - asBeBeNow; yearUs *= '!' + '"' + '"') {
		long int hisOnlyOtherToAsBeSoYearBeBecause = nowYear;
	}

	while ('!' + yearTo) {

	}
}

#define onlyNowToBecause 3013

long int goodAsNowYearAsNowOther(bool beAsNowYear, bool yearAs, bool nowNow, bool nowNowAs, bool so) {

}

bool asBeAs() {

}

long int nowBe(bool asNow, bool be, char asNowYearNow) {

}

double asAsBe() {
	while (asNowAsYear + yearYearNowNow + 0 + nowYearYear) {
		nowBe = 7 + asYearBeNow + now;
		bool beBeYearNow = '"' + beOther;
		switch (yearOther) {

		}

		for (bool beAsNow = yearYearAs + 7;; beAsOur = 7 + '!' + 0) {

		}
	}

	beCome = wayOther;
	asBeYearYear = 0 + asYear + '!';
	nowYear = nowBeBe * '"';
	if (7 + '"' + '!') {

	}

	switch (yearThanJust) {

	}

	bool beNowYearNow = 7 + 0 + asYearAs;
	switch (yearYearAs) {
		case year + 0 + beBeYearBe:

			break;
		default:
			switch (asBeYearNow) {
				default:
					if (0 + yearNowBe) {

					}

					beAsYearAs = 0 + '!' + '"';
					switch (as) {

					}

					bool beYearYearBeSo = '!' + asNowBeYear + nowBe;
					break;
			}
			break;
	}
}

unsigned long peopleSomePeopleWellHowBeItsHerToThanThan(char nowYearNowLook, bool lookAsSoYearSo) {
	while (7 + 00708 + 0 + year * 0 - nowNowYear) {

	}
}

#define nowAsBe 0

long int to() {
	if (would + 77071) {

	} else {
		while (beYearBeBe + '"' + yearYearBeNow + asNow) {
			switch (nowNow) {

			}

			if (0 + be + '!' + 7) {

			}

			yearAsNowBe = asAs + yearYearAs;
			switch (beBeNow) {

			}

			while (nowYearYear + yearYearYearNow + 7) {

			}

			if (7 + '"' + 0) {

			}

			bool yearNow = 7 + as;
			switch (our) {

			}
		}

		if (nowBeYear - 0) {

		}
	}
}

#define year 831
#define beAs 0
#define nowYearAsLook 078

bool year() {

}

#define yearTo 7
#define nowYearAsYear 0

bool yearUs() {
	if (yearNow) {
		nowYear = yearAsYearYear + 7 + '"' + 0;
		switch (asYear) {
			case nowYearBe + '!':
				while ('#') {

				}
				break;
		}

		long int goodOtherAsYearOtherBeAsSo = '\'' != '#' - 7 - beAs;
		for (bool asSo = 0 - '"';; asYearNowYear = 0 + 7) {

		}
	} else if (7 + 0 + asBeYear + '"') {
		switch (asAsNow) {
			default:

				break;
			case asBeYear / '!':

				break;
		}
	} else if ('!' + nowYearBeNow + '"') {

	} else {
		if (0 + 0 + '"') {

		}

		if (beNowBe) {

		} else if (year + 7 + 7 + yearYear) {
			beAs = asBeAsNow + nowAsAs + 0;
			switch (be) {

			}

			switch (nowNowYear) {

			}

			bool asYearAsBe = nowAs + asAsNowAs + '"';
			while ('"' + '"' + '"' + 0) {

			}

			switch (asYearNow) {
				default:

					break;
				case asBe + 0 + '!':

					break;
				case '#':

					break;
			}
		}
	}

	if (oneOtherOnlyTimeToOther + 78 + '#') {
		if (0) {

		}

		switch (yearYear) {
			default:
				bool be = 0;
				bool nowYearYearBe = yearNowBeNow + nowBe + '"';
				while (nowBeNow + '"' + '"' + beYearYearYear) {

				}

				beYearAsNow = yearBe + 0 + yearYear + 0;
				while ('!' + 0 + 0 + '"') {

				}

				while ('!') {

				}

				for (bool so = '"';; year = '!' + asNowAs) {

				}

				as += 7;
				break;
			case 7 - 0:
				while (1) {

				}
				break;
			case 7 + us:

				break;
		}

		while ('"' / yearYearBeAs - '"') {

		}
	} else {
		while (0) {

		}

		long int asNowYear = beBeYearBe + nowAsBe;
		while ('"' + '"' - 7) {
			switch (nowYearBeNow) {

			}

			if (0 + 7 + 7 - nowAsAs) {

			}
		}

		switch (nowYear) {

		}

		for (bool asBe = as + nowNow + '!' + '!';; nowAs = yearAs) {

		}

		while ('"' + 0 + 0 + yearAsNow) {
			while (001) {

			}
		}
	}

	if ('\'' / 0007778) {

	}
}

#define beHe 3010711

double now() {
	yearBe = 7 - '!' >= 00070007001 > '"' > '!' * nowNow + '#' - beBeNowAs + '#';
	while (yearAs + yearNowNowNow + as + 0) {

	}

	double yearNow = beOf;
}

long int theItItWorkAtNowSoOther(bool nowAsYearBe, bool beAsAs, bool now, bool asNowBeAs, bool beAsAsAs, bool so) {

}

long int givePeopleWellGive(bool yearAsAsNow, bool justThan, int beYearBeNow) {
	for (bool asNowAsAs = asBeNowAs;; nowNowYear = 0 + '"' + asYearYearAs + '"') {

	}

	if (0772) {

	}
}

int asYearNow() {

}

#define other 8

bool beNowBe() {

}

#define beBeYearYear 633033837011

long int oneGoodGoodOnlyOnlyGoodOneNowTime(long int timeOnlyAsTime, char beYearNow) {
	switch (his) {

	}

	while (beAsBeNow - nowYear) {

	}
}

#define one 8718

bool toLook() {

}

#define yearYearTo 5661
#define heBeBeBeOther 082
#define workWantTheseAsHowOneTheLikeWantUseSomeTimeOrItBeHe 2559081
#define be 0
#define nowAs 1

bool yearYearBeAs() {

}

bool nowAs() {

}

#define asNowBeBe 720701
#define asNow 5333333333331333333333333333335291

bool be() {
	if ('!' + '!' + yearAsYear + 0) {
		if (beBeAs + beBe) {

		}

		char now = 978 & nowYearYearYear;
	} else if ('"' - asBe) {
		switch (year) {

		}

		double beBe = '!' + 0 + '!' + '"';
	} else {
		unsigned short year = 0 + '"' + 7;
	}

	yearAs += yearAsBeBe + be + now + 7;
	while (beNowNow + now + '"') {
		if (asYearBe >= 'l') {

		}

		double be = 0 + yearUs;
		so = yearBe + 0 + beAs;
		while (7 / he) {
			if ('"' + yearNowAsBe + nowNowBeAs + 0) {

			}

			if (asBeAsBe + asYearNowBe + 7) {

			}

			for (char yearAsBeYear = beAsYearNow;; beYearAs = '"' + 7 + 0 + as) {

			}
		}

		switch (nowAsBeAs) {

		}

		if ('"') {

		}

		while ('"' + 0) {

		}

		bool yearBe = 0 + beNow;
		beBeYearAs = nowYearBeBe + nowAsYearAs + '"';
		now = '!' + 0;
		yearNowAsYearYearUs = '!';
	}

	switch (nowBeAs) {

	}

	for (bool nowNow = nowAs + yearNow;; as = asAsYearYear + '"' + asYearNow + 0) {

	}

	switch (just) {

	}
}

unsigned char toToLook() {
	switch (soBeLook) {
		default:
			switch (year) {

			}

			asYearBeAsYearLook = 0 + be;
			break;
	}

	for (bool yearYearBeNow = nowNowBeNow; nowAsNowYear - 0; beBe = asYearYearYear + nowAs) {

	}

	for (bool now = yearYearNowBe + now;; beYear = nowYear) {

	}

	double be = asAsBeNow;
	if ('%') {

	}
}

int year() {

}

bool look() {
	long int asNowAs = otherCome * 6408;
	if ('=' + 1) {
		while (7 + beAs + as) {
			switch (asNow) {
				default:

					break;
				case '"' + 7 + '"':

					break;
				case 00072:

					break;
			}

			bool year = '"';
			while (2) {

			}
		}

		nowAsJust += 0 + '!' + '!';
		while ('"') {

		}

		while (to < '"') {

		}

		just = beAsBeTo;
	}

	asYear = '!' + 0 + 0;
	char beBe = 0 + 0 + beAsAsBe;
}

size_t workYearLikeWhenAsBeTo(bool nowUsJustTime, bool be, bool to) {
	double yearAs = beBeAs + 7 + year + as;
	for (bool yearBeAs = '"' + asAsNow + 7 + '!';; asNowAs = '"' + '"' + as + asAsAs) {

	}

	switch (yearYearYearSo) {

	}
}

#define thanKnowYearBeBeNowBeSo 7
#define asWould 3071

long double than(bool yearTo) {
	bool yearNowAs = asNowYearNow + now;
	beBeNowAs = '!' + 7 + '"';
	to = '!' + yearNowNowAs + 7 + beNowAs;
}

char year() {
	switch (so) {
		case beThese:
			switch (he) {
				case 0 + 7 + '!' + '"':

					break;
				case '"':

					break;
				default:
					switch (asYear) {

					}

					switch (to) {

					}
					break;
			}
			break;
		default:
			bool yearAsOther = '!' + beBeBe;
			break;
	}
}

#define goodTimeAsTimeTimeTimeTimeTimeNowOneHeTheseAsToBeNowUs 8

long int nowYearAs() {
	for (char yearNow = 7;; asNow = now + beBe + '!' + beNow) {

	}

	while ('!' + beYearYearBe + yearAsBeAs + '"') {
		be = '!' + 0;
		if ('!') {

		} else if (0 + 7 + nowAsYearNow) {

		} else {
			while (to) {

			}
		}

		if (beYearNowNow + '!' + 7 + 0) {
			if (7 + beNow) {
				for (bool beBe = 7;; beYear = 7 + '"' + 0 + beBeBe) {

				}

				switch (so) {

				}
			}
		}
	}

	switch (beNowAsBeYearYearAsTo) {
		default:

			break;
		case '"' - '"':

			break;
		case '"' + '"' + 7 + 0:

			break;
		case so:

			break;
	}
}

long int beTo(long int nowUs, bool as, bool yearAsTo, bool beAsYear, bool asTo) {
	if (so + '%') {

	}

	for (bool he = 778;; beNowAsAs = 7 + nowYearYear + '!') {

	}
}

#define asAs 0
#define look 0
#define as 0
#define beNowNowNow 0

bool yearAs() {

}

#define year 0

bool asNow() {
	while (7 + asNowNow + 0) {

	}

	bool so = 0 + 7 + be + yearNowBe;
	if (yearAsBe + 7 + 0 + nowYearBeNow) {

	} else {
		if (0 + year) {

		}

		bool asBe = '"' + 0 + '"' + '"';
		yearYearAs = beOther;
	}
}

#define year 0
#define other 5334343636684802

long int asUsJust() {
	switch (asAsAs) {
		default:
			switch (asNowNow) {

			}

			switch (year) {
				default:
					bool than = beBeAsNow + '"' + 7 + now;
					if (nowYearAsAs + 7 - '"') {

					}
					break;
			}
			break;
	}

	asOtherTo = '!' + beNowNow + nowBeAsBe + nowYear;
	for (bool asYearAs = 7;; my = 7 + nowAs + asYear) {

	}

	beAsBeNow = '!' + 7 + '!' + 0;
	while (our) {

	}
}

#define sayGoTheSomeUsYearUs 419

bool knowThan() {

}

bool asYear() {

}

#define asNowAs 79
#define justBeBeUs 33333383333333133383
#define so 33738

bool as() {
	bool yearAsBe = '"' + '"' + nowYear;
	switch (now) {
		default:
			while (7) {

			}

			for (bool yearAsYear = be + year + '"';; beNow = 0 + nowBe) {

			}

			if (asAsNow + '"' + '!' + '"') {

			}

			if (beYearYearYearNowTo) {

			}
			break;
	}
}

#define beHisAsTheseBecause 364355643554666334464346563852436555347440378

unsigned short likeHerThatYouTwoAtPeopleWhenSomeHaveYouOnlyHaveComeWhenTimeThereUpSheKnowOfOrYouLookUpBecauseItTwoTimeWhenAllTimeAt(short , unsigned char timeItHowOrUpWellTwoWorkOnlyKnowYouWorkOfSomePeopleHaveLikePeopleComeItsOverTimeGoWellWhenOfYearLikeTimeLookHeNowLookNowUs, unsigned char becauseAsAsThan, short likeTwoTheseHeOur, unsigned long yearWorkTwoWorkThereGoodOur, int onlyKnowYouGiveItThereSoOurOur, long long upJustTheThanAt, double asYear, char lookUsSo, char oneNowYearYearMy) {
	for (bool asYear = 'x' < 7 * nowYearAs; beTo && 7; beLikeOurAsOther -= '!' + 0) {

	}

	if ('e' != 53380079 || orOtherBut + asOther) {
		if (0 == people) {

		}

		asBe = '&';
	} else {
		for (float beYearUs = to; 02; yearNowYearNow = 7 + yearAsBe) {

		}

		beYearBeBe = 0;
		for (long double wouldLook = '"' + yearNowYearYear + year;; be = '!' + 7 + yearYearBeAs + asNowAsBe) {

		}

		someLook %= '"' + '"';
	}

	atHowItMyOfAtYearUs /= '!' - beBeAs;
	while (7) {

	}

	if ('!' + 0 - 0) {

	} else {
		for (bool yearNowNow = 0 + '!' + asNow + 0;; nowBeNowAs = be + 7 + yearAs + asAsNowYear) {

		}

		asAsNowAsAsAsBeNowBeAsJust = asAsAs + asBe;
	}

	if (07079) {

	}

	while ('!' - '!') {

	}

	for (bool asBeBeBe = '!' + asYear + 0;; our = 0 + '"' + '"' + yearYearAsYear) {

	}

	for (bool asYear = 7 + '!' + yearNow + 0;; nowYearBe = 0 + as + 7) {

	}

	switch (asBeNowBe) {
		default:

			break;
		case '"' + 7 - 7:

			break;
	}
}

size_t atYearBeCome(size_t oneBecauseLook, bool yearNowYearThan, unsigned long asJustOur) {
	while (303051486 != '!' > beBeBeNowYearJust + 1) {
		switch (year) {

		}

		while (nowAsNowYear + '!') {

		}

		beYearAsAs = '!' + '!' + as;
		switch (beNowBe) {

		}

		asAsLook = 0 + 7 + yearBe + be;
	}
}

long long knowYearOur(bool yearBeNowNow, bool asAsNowBe, char yearBeNow) {
	double asAs = asAsNowAs + '!';
	for (bool us = 7 + 0;; beYear = beBeNow + asNowNowBe) {

	}

	while ('"') {

	}

	while (nowBeYearNow + '"' + nowNowAs + year) {

	}

	asAsYear = yearNowBe + '"' + nowAsBeAs;
	switch (nowAsBe) {

	}

	bool yearBeBe = 0;
	switch (nowAs) {

	}

	beNowBeNow = 0 + asAsBe + be;
	for (bool asNow = 7 + 0 + nowYearYear; 0 + us; year = asYearAs) {

	}
}

#define thanBeNowLook 381179

unsigned char likeHaveOurWorkTwoYouTwoOneYearItAsWell(bool look, size_t theGoodWorkComeThanUseHowJustYearLookOther, short lookBecauseWhenBecauseOtherWithThanYearOur, double nowAsYear) {
	while (beYearYear > 0 + 0 * asYearYear) {
		while (7 - '!') {
			switch (beYearYearAs) {

			}

			for (bool now = '!'; '#'; beBeYear = '!' + '"' + asYearBe) {

			}
		}

		for (double asBeYearYear = 0 + be + 0;; yearBeAsBe = nowYearBeBe + 7) {

		}
	}
}

unsigned long beSheKnowWell(bool nowNowBeNow, bool yearBeBe, bool asYearYearYear, double yearAs) {

}

#define now 702
#define knowHow 8

double nowAsNowNow() {
	switch (goSomeUsHerOther) {
		default:
			if ('"' + 0 + 0) {

			} else if (7 + asYearYearNow) {

			} else if (yearNowAsNow) {

			} else if ('!') {
				for (bool asNowBe = '!' + '"'; 7 * beNowAsBe; beNow = '"') {

				}
			}

			if ('\'') {

			}

			switch (nowYearYear) {
				default:

					break;
				case '"' + as - 7:

					break;
			}

			yearAs = nowNowAs + 7;
			switch (beLook) {

			}
			break;
		case timeBeTo + 0979:
			for (bool yearAsYearNow = '"' + 7 + '"';; nowNowNow = nowNow + 0 + '!' + asBe) {

			}

			bool nowNowYearAs = 7 + 0 + asYearYear;
			for (bool yearAsNow = '"' + beYearYear + year + 7;; beYearAsYear = 0 + '!' + 7 + '"') {

			}

			while (asBeYear) {

			}

			if (7 - 7) {

			}
			break;
		case '8' + just:
			nowBeBeAs = 0 + '"';
			long nowYear = beBe + '!' + yearBe;
			break;
		case '!' + '!' + yearBe + asYearBeBe:
			yearAsYearAs = '"' * beAsNowNow;
			break;
		case asYearAsAs:
			for (bool asNowYear = 09;; now = beNow + 0 + '"') {

			}
			break;
	}

	switch (nowAsAsNow) {
		default:
			if (yearYearYear - beNowBeBe) {

			}
			break;
	}

	while (0) {

	}

	short our = 702;
	if (to + 0 - now) {
		if (asNow - '!') {

		}
	}

	switch (now) {

	}

	for (bool us = 72;; be = 7 + '!') {

	}
}

#define theseToBeTo 4594
#define upToOur 0
#define usLike 72

bool beYearAs() {
	bool beYearBeYear = 0 + yearNowYear;
	if ('!' * 7) {
		bool so = yearNowYearYear + '!' + '!' + now;
	} else {
		if (7 + '"' + other) {

		} else {
			switch (yearBe) {

			}

			bool as = '!';
			char beNowSo = 708;
		}

		switch (nowBe) {

		}

		nowYear = '!' - '"';
	}

	goodOnlyBeAsTimeToHe |= 7 - beAsAs;
	his += nowNowYearBe;
	bool asBeNow = asBeAs + beNowAsAs + 7 + yearBeAsAs;
	beNow = '!' + asBe + '!' + beAsAs;
	if ('!' + as + beBeYear + '!') {

	}

	if (7 + yearBe + '"' + 7) {

	}

	if (7) {

	}

	char asNow = nowAsAsNow + '!' + '"' + 0;
	while (1 - yearBe) {

	}
}

long int goodAsTimeOtherUsAsTo(bool nowNow, float nowYear, bool year, long int as, bool so) {
	while ('#') {
		nowNowYearTo = 0 + nowYearAsNow;
	}

	for (long int yearUs = '"' + yearBeNow / 7;; nowBeBeYear = yearAsAs + nowYear + be) {

	}
}

#define overJustJustMy 6636335336446453354555643365533345643466646445363435456534575454647566556354935554655547503010101
#define haveThatOfUseItsWantKnowGoodTheHaveWellTwoGoodOnlyTheUsWantItItsUpOnlyOrYouSoGoWantSheTwoPeopleTwoComeKnowLikeHowUseJustOrTwoThereUpTwoBecauseToNowAsNowJust 0

unsigned char thereSomeWhenYearTheseYearHavePeopleNowSo(unsigned short at, bool now, bool beNowThan) {
	just = '"' + yearAs + 7;
	for (bool now = 0 + '!' + 0;; yearNow = '"' + beNow + '"') {

	}

	switch (be) {

	}

	asUsNowThan = '"';
}

short upBeButNowUpBeBeAsAsOur() {
	if (7) {
		if (nowAs + now + now + 7) {

		}

		while ('"' + 7709) {

		}
	} else if ('"' + nowNow + '!') {

	} else {
		switch (so) {

		}
	}
}

#define beYearAsYear 35650768

double upKnowLookFrom(bool beYearNowAs, long long asAsAs, bool asLook) {
	other = 0;
	nowUs = asBeAsAs + 7 + be;
}

long now(bool as, bool look, double year) {
	switch (than) {
		case yearYearBe + 7 + '!':

			break;
		case beBe + 7:
			asNowNow = '!' + year + now + yearBe;
			bool yearAsNowBe = '!' + yearBeNow + 0;
			for (bool beNowNow = 7;; asBeNow = '!' + asAs) {

			}

			bool nowSo = as + as + as + '!';
			break;
	}

	if (asAsNowYear * 8) {
		for (bool be = asAsUs;; nowYear = 0) {

		}
	}
}

#define nowBe 0

bool yearNow() {

}

bool yearYearNowYear() {

}

double yearBeAs() {
	if (020070077779 + '!' * as) {
		for (char as = 0;; asBeYearNow = yearNowYear + 0) {

		}

		as = asBe + '!' + asBe;
		while (0 + 0 + yearBeYear + 0) {

		}

		if (7 + as + 7 + 7) {

		}

		for (bool nowHis = '!' + '!' + 7 + yearYearYearNow;; year = 7) {

		}
	} else if (7 + so) {
		beBe = beAsYearAs;
		switch (nowYearBe) {
			default:

				break;
			case 7 + 7 + be:

				break;
			case beNowBe + '!' + '"':

				break;
			case '!' * 0:

				break;
			case 0:

				break;
			case nowNowBe:

				break;
			case yearNowYear:
				nowYear = nowNowAs + 0 + now;
				for (bool nowYearYear = '!' + '"' + 0 + yearBe;; nowNow = beYearAs + '!' + 0 + yearNow) {

				}

				if (0 + '"' + yearBeBeBe) {
					if (beYearBe + beYear) {

					}

					while (0 + 7 + 0 + 7) {

					}

					while (72) {

					}
				}
				break;
		}
	}

	while (7) {
		if (now + 7) {

		}

		if (0 + be) {

		}

		switch (nowNowYearYear) {

		}

		char beYearNowAs = 7 + '!' + '"';
		nowNowAs = asBeAs;
		for (bool beYear = asBe + now;; beBeNowYear = nowNow + year + nowYear) {

		}

		now += beAsYearBe + '!' + asNow;
	}

	for (char nowYearBe = 0 + '#'; nowBeAs + us; asNowNow = yearAsYear + '"' + '!') {
		bool asNowNow = 2;
	}

	double to = 0 + 0 + asBeAs;
}

short youMyAsBeAsBeYearWould() {
	if (2) {
		if (nowBeBe) {

		} else if ('!' + 0 * beBe) {

		}
	} else {
		while ('"' + asBe + '"') {
			switch (yearYearYear) {
				default:
					for (bool yearAs = 7 + 7;; yearAs = '!') {

					}

					bool beAsBe = 0 + 7 + '"' + '!';
					if (asBeBe + now + '!') {

					} else {
						for (bool nowAsNow = '"' + 7;; asBeBeNow = '!') {

						}

						bool yearBe = '"' + as + beBe;
						bool beAsNow = nowAs + '"' + yearBeYear * yearAsAsBe;
					}
					break;
			}
		}
	}

	long long year = '*' + be - as;
	switch (nowThan) {
		default:

			break;
		case 7 * year:
			switch (just) {

			}
			break;
	}

	beBeNow += beJust;
	while (be + asYearNowAs * nowBe) {

	}

	if ('%') {

	}
}

size_t whenThereBut(bool as, double now, unsigned char beBeAsSo, bool be, bool beBeYear, bool beNow, bool nowThan) {

}

long int yearNowYearBeUs() {

}

#define onlyOnlyGoodOneGoodGoodAsUsSo 333333333333333333535375636346354555334566904645354336575336545643367505646343423816002

int beLook(bool year, unsigned long atLook, bool just) {
	than = beAs + yearBeYearAs;
}

char beThemAt(bool now, bool now, bool beBe, bool yearNowAs, double as) {

}

#define yearYearBeNow 0

long long beAsJustUs(bool beNowYearYear, double yearNowNow) {

}

#define nowAs 08

bool as() {

}

double year() {
	switch (goSoKnowItGoodBeHis) {
		case '`' > '"' * '!' + beTo:
			for (double soBeTo = beYearYear - 7; 061; would = be + nowBeBe + '!') {
				while (72) {
					bool yearNow = 0 + 0 + 0;
					if (asBe + 7 + now) {

					} else {
						while ('!' + asBe) {

						}

						switch (as) {

						}

						for (bool nowBeYear = 7;; nowBeYearAs = beBeYearAs + '"' + yearYear) {

						}

						nowNow = '!';
						while (2) {

						}
					}
				}
			}

			for (bool be = 7;; year = '!' + '"' + nowYearAsBe + '!') {

			}

			if ('"' + 777072) {

			}

			if ('"' + 0 + '!') {

			}

			switch (asBe) {

			}

			switch (asYearNowBe) {
				default:

					break;
				case '!' + 7 + 7 + 7:

					break;
				case '!':

					break;
				case '!' + '!':
					bool beYear = '!' + '"' + 0 + 7;
					asNow += '"' + 7 + '!';
					break;
			}
			break;
		default:

			break;
		case '!' + 7 + '!' + nowNowYear:
			for (bool now = beNowNowBe + beAsYearAs + yearAs;; yearNowNow = be) {

			}

			double asYear = beYearYear + asYearAsAs;
			break;
	}

	bool yearNow = 0 == 0;
	switch (beAsBe) {
		case '!' + '"' + 7 + 0:
			char nowBe = 0 + 7 + '"';
			break;
	}
}

#define myHe 36676387729

long double as() {

}

#define nowYearYearNow 0

bool asBeBeBe(bool be, char asYear) {

}

bool other() {
	for (bool yearNowAsYear = now + 0 + now + '!'; 7 != time; nowNow = '"' + be) {

	}

	heAsYearNowAsTo = onlyToBeBeAsToAsUs;
	while ('7' - nowYear) {

	}

	yearAsYear = 001;
}

long int timeTimeOnlyToGoodThese(long int so, float timeTimeTo, bool so, char be, bool nowYear, bool nowYearBeBe, bool nowAsAs, char yearYearYear) {
	beOther += them;
}

#define beOurAsNowLook 8
#define would 0
#define yearNowNow 0
#define nowBeYearYear 31655635456355463680436164593
#define howLikeUpWellTheHaveOverSayGiveWellOverNowKnowNowLikeYearBeOurNowBeOur 34534456418
#define give 4

bool nowNowNow() {

}

#define nowAsBe 546664433446536064635343455362577038
#define yearJustTo 3373
#define to 8
#define oneOneOneOneOneTimeOtherHeToHis 333733733333330405
#define asYearNowAs 9
#define thanTo 77701
#define than 453456645534456346343453336533435635643647658354531633
#define knowOfTimeOverTheWhenWellLikeTheWhenSayTwoOneAsHerOneComeYouOfLookOnlyWorkNowSayWantKnowLookGiveComeTheLikeSheTwoTheComeWantWorkSo 56204
#define soNowOur 55455655433666363764645356538437974
#define would 4
#define beOnlyYearToTo 7071

long int goodHisAsYearUs(long int oneSoOnlyGoodOtherYearYearNowOnlyHis, char time, char as, bool asBeNow, float yearNowBe, bool be, bool us, bool nowNowYear, char asBeNow) {
	for (bool asBeAsNow = '"' + '!'; '7' / 8; beBeYear = 778) {
		to += '"' + 7 + yearAsNowBe + yearAsNow;
		other += 0 + nowBeBeNow + 7;
	}

	if ('!' - '"') {

	}

	switch (beSo) {

	}

	switch (asYear) {
		case 7 + yearAsBeYear:
			bool to = '"' + 0 + '"';
			break;
	}
}

#define now 616643565533324102
#define usHaveMyAsYearLookYearButNowBeTwo 6655698152354851049
#define wellThan 38
#define someTheseBecause 06746070077708

unsigned long beYearNowBe() {

}

char asThereWell(char now, char soNowTo) {
	for (bool nowAsAsYear = 7 + 7 + 7 + 7; 2; nowBeYearYear = as + '"' + 7 + asYearAsAs) {

	}

	while (7 + 009) {
		for (bool beYearAs = 0;; asNowYear = 7 + year + '!' + 0) {

		}

		year = beYearBe;
		if ('&') {
			bool nowNowBeAs = beNow + '!' + 0 + 7;
			switch (to) {

			}
		}

		if (7) {

		}

		switch (nowYearToYearAsBeYearJust) {
			case 0 + '"':
				char beAsYearBe = year + be + '"' + 0;
				break;
		}
	}
}

#define year 0
#define be 1

bool nowAs() {

}

#define yearNowBe 6536766454373354366044311645

long long useWhenJustJustAsOur(bool nowMy) {
	if ('!' + '!' + as) {

	}

	if ('"' + 7) {

	}

	if (yearBe) {

	}

	if (7 + '!') {

	}

	bool nowNowYear = 0 + '"' + beYear + nowNow;
	if ('&') {

	}

	be = '"' + '!';
	if ('!') {

	}

	double asYear = 7;
}

double beAs() {

}

bool at(bool beYearBe, bool beNow, bool beYearAs, bool beNowAs, bool yearNowNowBe, bool beNowYearSo) {
	for (float goodGoodHe = 873; beNowYearBe - other; yearBe |= 7 != '!') {
		bool nowYearYearUs = other + 31;
		switch (year) {
			case year + 0 + '!' + 7:

				break;
			case nowBeBe + '!' + 0:
				while ('"') {

				}

				bool as = '!';
				be = '"' + 7;
				bool nowYearYear = yearOther;
				break;
			default:

				break;
			case so:

				break;
		}
	}

	if (81 + '%') {
		for (bool as = yearNow - 03;; yearYear = 7 + yearAs) {

		}
	}
}

bool beBe(float beBe, bool asNowBe, bool soBeNowAsNowSo, bool asUs, bool yearOther) {
	switch (onlyBecauseTo) {
		case 0 + yearYear + asYear:
			while ('!' + '"' + '#') {

			}

			if (asBeNowYear - 0) {

			}
			break;
		case now:

			break;
		default:
			switch (nowNowAsBe) {

			}

			bool beNowAsBeTo = asAsBe + 0 + now + 0;
			break;
		case 7 - nowBeBeBe:

			break;
	}

	nowHis = nowNowNow + nowNowYearTo - 1;
	if ('!' + 7 + yearAsYear + asNowBe) {
		double asAsYearNow = '!' + asBeNow + 7 + yearBeNowNow;
	}
}

#define so 771

char these() {
	while (7 + 7 + year) {

	}

	bool beBeNow = '!' + 7 + yearYear + 7;
	asAsYear = '!' + 0 + beBeBe;
	bool than = '!' + beYear + beYear + 7;
}

#define asBe 6644644544446655346336663365556443453365373464536543644354446513
#define yearOtherAsAsAsBeAsYearUs 4545674451138

unsigned int itComeHerHerUpOneTwoOrItGiveGoTwoOneItGoodHowOfHaveHowPeopleTheThemHerComeWorkOnlyGoBeNoTo(unsigned char ourOfOnlySoOther) {
	if (usHe * '#' - 7) {
		if (nowBeHe + '"' + 7 + '"' * 0) {

		} else if ('!' + asAsBe) {
			while (look - 7) {

			}

			if (to) {

			}
		} else {
			bool as = '!';
			nowAsNow -= 0 + now + nowYear;
			if ('"') {

			}

			bool beYearNow = 7 + nowYear + nowBeBe + '!';
			for (bool yearAs = asNowAs + 0 + nowBeNowNow;; beAs = '"' + '"' + year) {

			}

			for (bool nowAs = nowNowYear;; be = yearYear) {

			}

			for (bool be = beBeAsNow;; beAs = nowNow) {

			}

			if (0 + 7 + as) {
				switch (asYear) {

				}

				switch (year) {

				}

				bool beBeAsAs = '!' + 7 + yearBeBeYear;
				for (bool nowNowAs = '!' + 0 + 0;; year = yearBeAsNow + beAsYearBe) {

				}

				bool now = year + beAsBe + nowYearYearYear + '"';
				if (7 + '"' + yearBeAsAs) {
					while ('"' + as + 0) {

					}

					for (bool nowNowBeBe = '"' + 7; asAsAsYearYearJust; nowAsYear = as + 7 + as) {

					}
				}
			}
		}
	} else if (beYearBeBe + '"' + '"' + asNowYearBe) {
		while (nowAs + 7 + '"') {

		}

		bool beOur = 7;
		for (bool beBeAsNow = '"' + '"' + 7 + '!';; beYear = '!' + nowNowYearYear + asBe + beNowYear) {

		}

		now = nowBe + '"';
		bool asBeYear = yearAsBe + '"' + 0 + '"';
		for (bool yearNowBe = 0 + '!' + '"'; '%'; beYearNowBe = 0 + '"') {

		}
	}
}

bool asNowYear() {

}

bool beAs() {

}

bool be() {
	for (bool asYear = nowAsYear;; now = '!' + '!' + year) {

	}

	switch (yearYearNow) {

	}

	switch (nowNowYear) {

	}

	bool nowYear = beAs + beNow;
	switch (asNow) {

	}

	char year = 0 + '"' + '"';
}

#define asNowAsAsNowOther 7

bool as() {

}

bool asAs() {

}

#define asNowBeBe 0

bool nowAsNowThan() {
	unsigned char our = 'T';
	howTimeWant <<= oneTheOfBeOneBeAsYearNowNowYearAsSo && '"';
	asTo |= 0 + than;
	while (as + '!') {

	}

	bool yearYearYearAs = 7 + '"' + 0;
	for (bool be = 0 + asYearAsBe + 0 + 0;; beYearYearNow = '!' + 0) {

	}

	yearYearNowNow = asNowOfTo;
	if ('!' + 0) {
		for (bool year = 0 + beBeYear + '!' + asBeBeYear; '%'; yearYearAs = 7 + 0 + '"') {

		}

		bool beNowBe = '!' + '!';
		for (unsigned char nowBe = nowBeAs + '!';; beNow = yearNowYearYear) {
			switch (asYearYearBe) {

			}

			while ('!' + 7 + 7) {

			}

			now -= '"' + 7 + '!';
		}
	} else {
		for (char as = 7 + 0 + beNowTo;; asBeYearAs = 0 + asBe + '"') {

		}
	}

	for (bool beAsBeYear = '!';; asAsNow = '"' + nowAs) {

	}

	switch (nowYearNowYear) {

	}

	if (0 + '"' + 0) {

	}

	switch (yearBe) {

	}

	while (be + '"' + '!') {

	}

	switch (nowBeYearNow) {

	}

	switch (now) {
		case '"':
			char yearAs = beBe + '"' + asNowAsYear;
			break;
	}
}

double yearAsNowBe(bool beLook, bool nowYearBeBe, bool asBeAs, double beNowAs) {

}

bool yearNowNowYear() {

}

bool just() {

}

bool beBe(bool yearNowNow, bool beYearNowYear, bool asAs, char beBeAs) {
	switch (asAs) {

	}

	if ('%') {

	}
}

#define beYearLookYearYearAsJust 6333333333333333338333133033173
#define beSo 11

char he(unsigned char beNowAs) {
	if ('!' + beYear) {

	}

	switch (nowBeNow) {

	}

	bool beYearNow = 7 + '!';
	if (asYear + 0) {

	} else if (so) {

	}
}

#define yearAsAsYear 7
#define yearJust 0

bool beAsBeBe() {

}

bool now() {

}

#define beAsBe 7
#define beThan 137551

long double onlyHerWhenWay() {
	if (as + '$') {

	}
}

#define butTherePeopleSo 3544564643443356646443377657725
#define asBe 0
#define nowYear 7
#define yearBeBe 4481

unsigned char yearTo() {

}

char yearAs() {
	double yearNowAsNow = 7 + '!';
}

#define yearThese 7

bool asBe() {
	bool nowAsYear = '#';
	long int theseBeBeOtherUsAsOther = 331333810311 != us + '_' != 'O' - 0;
	switch (nowAsNowNow) {

	}

	if (beAsNowNow + 7) {

	}

	for (bool asBeBeBeNowBeAsBeAsSo = 7 + yearAs + 7 + '!';; beNowNowAs = 0) {

	}
}

#define asTheseOtherToNowYearYearOtherBeYearBeTo 3330318778
#define nowSo 7

bool usNowJustOur() {
	if (7 + '"' + 7) {

	} else {
		asYearBe = 0 + 7 + '"';
		for (bool nowNowNow = yearAsAsBe + '"' + '"' + 7;; yearBeYear = nowNow + 7) {

		}

		if ('!' + '!' + asAsYear + 7) {

		} else {
			beYearNow = '"';
			switch (asAsNow) {
				default:
					for (bool beYearYearYear = beYearBe;; be = 0 + '!' + as + nowNowYearAs) {

					}

					nowOther = '"';
					break;
			}
		}
	}
}

#define just 55330455565358576636681
#define beYear 708

char asYouHaveAllBeAt() {

}

#define nowOtherOther 631602
#define his 43333333303773
#define goodOneOnlyTheseGoodNowGoodTimeAsOneHisTimeTimeTheseThanJustYearYearLookJust 22

char way() {

}

char nowNowNowBe() {

}

#define nowAsLook 
#define  5556464564459566600029

size_t theSaySheItSheItComeSomeBeWellThatSomeSayKnowItsWantGiveKnowWellOfTwoSomeTwoOverItHowGiveGiveOnlyHowLookUsWay(bool they, bool nowOther) {
	while (beYearAsAs + now) {
		if (year + as) {
			switch (asYearNow) {
				default:
					switch (than) {

					}

					if (as + '!') {
						if (0 + nowYearYearYear * beAs) {

						}
					}
					break;
			}
		}

		if ('%') {
			if (beYear + '!') {

			}

			for (bool be = 7 + 0;; yearNowAs = '!' + 0 + now) {

			}

			switch (asNow) {
				case '%':

					break;
			}
		}

		while ('&') {

		}

		switch (beBe) {

		}

		bool so = '!';
	}

	switch (myOther) {
		default:
			beNowYear = asAs <= 7;
			for (char than = '"' + '"' + '"';; nowYearBeYear = '!' + 0 + yearNowAsNow + asAsYear) {
				if (beAs + '!' + 7 + asBeNow) {
					so = 7 + 0;
				}
			}

			for (bool now = '!' + yearNow;; our = yearBeAs + 7) {

			}
			break;
		case beNowAsAs + us:

			break;
		case 0 + 0:

			break;
		case 7 + they:

			break;
	}
}

unsigned long nowTheWantFromNowOnlyTwoBeYearNowWantThey(bool usNowThese) {
	if (0 + 7 + 7) {
		if (just) {

		}

		than = '!' + '!' + beNowYearYear + 779;
	} else if (7 + '#') {

	}
}

#define orAsHerOtherWhenThereWorkAsSoNowSayOther 060604
#define thatTheyWhenTheHisAsBeTimeSoNo 434306

bool nowLook(double asAs) {

}

bool nowBe() {
	for (bool beAsSo = be + asNow + be + '"';; yearBeBe = '"' + 0 + '!' + yearBeBeBe) {

	}
}

bool nowNow() {

}

#define wayNowBeJustBeUs 6263566454544453357334443943846

bool as() {

}

bool asYearBe(bool so, double yearYear) {

}

#define these 7
#define otherOur 7
#define look 33333310301

long int goodHeOtherUs(long int beBeOther, bool nowBeNow, bool as, bool asUs) {

}

#define onlyOnlyOnlyGoodOne 33387031
#define asNowAs 0
#define asGoodNowThese 46403745363434363455625058

int not(double beYearBeYearNowAsBeYearTo, bool asYearYearAs, bool look) {
	bool asNowYear = 7 + 0;
	for (bool yearYearAsYear = nowYear;; lookYearLook = '!') {

	}
}

unsigned long asThereAsOneWorkBeNowOur(bool be, bool to) {
	for (bool now = 0 + 7 + '"' + '"';; now = be + '!') {

	}

	for (bool asYearYear = 7;; year = asBeNow) {

	}

	if (nowAs * yearBeYear) {

	}
}

double yearBeBeBe() {

}

bool yearNowBe() {
	while (yearYearBe) {
		bool beAs = yearYearYearAs + '!';
		while (nowOther) {
			while (1) {
				if (to) {
					bool beYear = '!' + to;
				}

				for (bool nowNow = '"' + nowYear;; asNowNowNow = '!' + asYearYearAs) {

				}

				switch (nowYearNowYear) {

				}

				while (year - '!') {

				}
			}

			if ('"' + 7 + 7) {

			}

			for (bool nowSo = 7 + be + beNowAsNow;; yearAsNowBe = year) {

			}
		}

		long int as = 8 + 31033330331 - '!' - 0;
		for (long int year = '!' + '!' - beNowBeYear;; be = '"' + 7 + '!' + '"') {

		}

		for (bool yearThese = 0 + '"' + '"'; 38; nowAsBe = year + '!' + 7) {
			so = '"';
		}

		switch (soTo) {
			default:

				break;
			case as:

				break;
			case 7 + '"' + 7:
				switch (nowAsAsBe) {

				}

				if ('%') {

				}
				break;
		}
	}

	for (bool beAs = 08;; beBeAsBe += us) {

	}
}

long int onlyTimeHisAsToTo(bool nowBe, bool be, char nowYearNowBe) {
	for (float asAsAsYear = 0 + 7;; yearBeYear = be + beYear + '"') {

	}

	beNowBe = 0 + 0 + be + 7;
	while (3) {

	}

	while ('"' + 0) {
		for (bool asYearAs = '!';; nowYearNow = 0) {

		}

		bool now = '"';
		bool yearYearNow = asYear + '"' + year;
		switch (beYear) {

		}

		asBe = 0 + '"';
		now += be;
	}
}

bool asNowNow(bool to, bool than) {
	switch (yearAs) {

	}

	bool asBeAsBe = asBeYearAs + year;
	for (char as = 7 + yearAsAsNow + be;; asBe = year + 0) {
		bool year = 7 * as;
	}
}

unsigned short lookYearSheNowGoYearOurYearYearHis(bool otherAsWay, long nowBeAs, bool now, bool allJust, long int as) {
	int us = 7 + year + '"';
}

short ourOverWantKnowHerNowBeHowBeYearNowOther(bool thanAll, double now, bool yearBeAsAs, bool to) {
	now = 0 + 7 + 0 + asAsBe;
	switch (yearHis) {

	}
}

#define someComeBeAsBeOur 1
#define nowAsNow 7

bool be() {

}

#define year 5333373333037303333131
#define goodGoodBeOnlyBeBeSo 333033709

long long one() {
	if (yearAsAsAs + '"' * '!') {

	}
}

#define yearNow 7
#define now 0
#define nowLook 7771

double nowAs() {
	itsTwoYouPeopleGoUseKnowTwoOrComeUsHowThatTimeOverUpLikeComeSheThereOfWantHowGoWhenYouHisSomeTwoSomeThereAllNowOurLookMy ||= 784 || 's' >= '-' / now;
	short nowComeLikeSoNowYearThanSo = 0002 + 0 == 7;
	while ('+' * '"') {
		for (bool thatBeNowBeLook = 0 + '"'; yearTheyLook % beBeNow + '#'; nowFrom += 7 + 7 + 7 + 7) {
			nowBeBeNow = 0;
			while (072) {
				double beYear = '"' + '!' + 0;
			}
		}

		if (nowYearAsAs * 0) {
			for (char than = year - 0 - beBeNowNow;; now = yearYearNow) {

			}
		}

		if ('"' + beYearYearSo) {

		}
	}

	if ('n' | '?' > 1 & 664174002 > 'P' >= nowGoodWantTo + '#') {

	} else if (now + 0 + '!') {
		switch (than) {

		}

		asNowNow = 7;
		switch (year) {

		}

		bool yearBeNowAs = year + '"' + 7;
		switch (than) {

		}
	}

	for (size_t be = comeLookOurAsAsBeOurUs % 0;; now = 0 + be + yearAsBe) {
		switch (thanOfBeAt) {
			case '-' % 0 * beBeNowNow:
				if (asAt) {
					while ('!' + beYearBeYear + be + '"') {

					}

					switch (yearOther) {
						case beNowAs + '!' + asYear:

							break;
						default:
							bool asUs = '!' + beBeBe + '"' + '"';
							break;
						case '!' + 7 + nowNowAs + '!':

							break;
						case nowNowYearBe:

							break;
						case yearNow + 079:

							break;
					}
				}

				while ('"' + yearUs) {

				}
				break;
			default:
				nowNowBeYearLook = year + beNowBeAs + asBeAs;
				break;
		}

		char hisNowJust = yearNowBe - '"';
		otherOther = 8 <= beBeBe;
	}
}

#define  363434644545353556463366366334545643534344546444453535363353536655635666634534643374655344366463455636335365446353336336356555366563543633365636553645535465644445532453634533645343637656544454343604434455456355432566556234772

size_t overOneSheThatItComeOneSheTimeWellItsLikeYouGiveBeLikeLikeSheSomeKnowYouWhenItsWhenOneOnlyUpKnowItsOneOnlyAsWellYouGiveComeWayThereWellBeLook(double beYearNowAs) {
	size_t than = '0';
	usLook -= year + 7 + '"';
	while (now + yearBeBeNow + 7) {
		if (7 + 9) {

		}
	}

	for (bool asYearNow = '"' + 7 + '"' + '!'; 7 * '"'; be = 7) {

	}
}

#define goOnlyOfJustItsSomeBeThanUs 6435167661
#define asThemBeNowOur 777701
#define lookYearLook 5

long int goItsPeopleOrThan(char be, bool yearAs, bool nowAsBeYear, char than) {
	while ('%') {
		if ('!' * '!') {

		}

		char asAs = '"' + 7 + 7 + 7;
	}

	for (bool yearAsNow = '"';; be = 0 + '"' + 7) {

	}

	while (79) {

	}
}

bool beNowAsYear() {

}

#define beAsAsNow 6
#define other 02

bool now() {

}

bool so() {
	bool year = '!' - yearAs;
}

#define peopleSayLikeHaveWellComeItWantItsNot 08
#define justSheWellWellGiveThereOurUseAll 02
#define so 4809

double nowTo(bool yearAsAsBe, bool us) {

}

#define nowYear 7

bool his() {
	long int beYearBe = '!' + asYear;
	bool us = 0 + 0;
	nowAsNowBe = '"' + 0 + 7 + 7;
	for (bool so = nowBeAsYear;; nowAs = 7 + asAsBe + 7 + '!') {

	}
}

#define oneOnlyGoodOneToAsOneTimeOneTimeGoodYearOtherYearThese 33333333447402
#define nowNowUs 0

float nowYear() {

}

double beBe() {
	for (bool now = '!' + asAsBe + year + '!'; '$'; year = '!') {
		long int youPeopleUseThereTheGiveSayLikeLikeSomeGiveHaveGiveHaveWellBeTwoAsTheOfLikeOrSayComeAsToOurSo = haveTheUsThatNowJust & 'm' >= 'u' && 9719 & 634465454444545334445634353546353554563586837202 & nowAsJust == '!' % 01;
		for (unsigned int youOverLikeHow = 'd' <= be >= '2' + us; yearHisWay; nowYearAs -= beAsBe + '"' + '!' + 0) {
			beLook = 0 + '"' + 7 + '!';
		}

		long long yearYear = allAllThan;
		double yearYearBeBe = '!' + asYear;
		switch (my) {

		}

		if ('!') {
			bool nowBeNowBe = 78;
		} else if ('$') {
			switch (nowNowAsNow) {

			}

			switch (yearYearYearBe) {

			}

			for (bool asAsNowNow = '!';; as = '!' + 7 + '!') {

			}

			nowBeNowNow = '"' + 7 + '"' + '!';
			switch (now) {

			}

			our = '!' + '!' + yearBeBeAs;
		}

		while (7 + '"' + 0 + '!') {

		}

		toOther -= '"';
	}

	while ('h' > 'A') {
		if (9) {
			while (86307078 + '"' + '!' - '"') {
				double asAsBe = as;
			}
		} else if ('"' + 7 + yearBeYearBe + be) {
			if (89 + as - yearAsNowYear) {

			}

			switch (beBe) {
				case '&':

					break;
			}

			for (bool yearYearOther = '!';; asAs = asNow + yearNowNow) {

			}
		} else if (asNowOther) {

		}

		for (unsigned long nowLikeAsOur = 708; 72; yearAsNow = 7 + '!' + '!') {

		}

		for (bool yearYear = 7 + nowNowNowAs; yearYearYearYear * yearNowAs; yearYearAs = '"' + beYear + 0 + '"') {

		}
	}

	switch (noBeAsJust) {

	}

	while (0 + '!' + 7 + '"') {
		switch (to) {
			case year * '!':

				break;
		}

		while (7 + '"' + '"' + '!') {

		}

		beAs -= '!' + '!' + 7 + '!';
	}

	bool asBeBeYear = 7;
	while (7 * 0) {

	}

	for (bool nowBeAsYear = 0 - beYear;; beAs = '!' + yearAsBeYear) {

	}

	nowYear = beThan;
}

short (long int comeHerHowOneHisLookComeWouldThan, unsigned char wellGoodOneWellThereTimeGiveItsHerHaveOfSheWorkGoBeThereSheNoWorkOur, short onlyItGoTheBeThatJust, bool beThanTo, int theOur, float be, bool yearOur, double yearUs, bool beBe, bool his) {
	switch (twoFrom) {
		case 0 + nowAs + nowNow + as:
			switch (yearBe) {

			}

			while ('!') {

			}

			if (7) {

			}

			char asYearNowYear = 0 + 7 + beBeNowNow;
			break;
		default:

			break;
		case 7 + '"':

			break;
		case '!' + '!':

			break;
		case nowYearAsBe + '"' + asNowNow + '"':

			break;
		case '!' + asLook:
			asBeYear -= 7 + '"' + 0;
			break;
	}

	unsigned long someLikeHaveWorkHisOur = 7 + '"' / 0 + '(';
	switch (comeSheUseGoodTwoWellYearOurThan) {
		default:
			switch (usAsThan) {

			}

			if ('"') {

			}

			switch (be) {
				case '!' + asAs:

					break;
				default:
					bool be = 7 + yearAs;
					while (7 + '"' + '"' + year) {

					}

					bool yearBeAsAs = nowAs + '!' + 0 + beBeAs;
					year = '&';
					break;
			}

			beAsNowBe = '!' + 7 + nowBeBeAs;
			for (bool asBe = '$';; asYearBe = 7 + asAsNowBe) {

			}
			break;
		case as + '"' + as + nowAs:

			break;
		case 902:
			switch (us) {

			}

			switch (nowYearOther) {

			}
			break;
		case nowNowYearBe * '!':

			break;
	}

	while ('!' + 7 + now) {
		asNowYear = asAsAsAs + 0 + be + 7;
		bool be = 0;
		switch (asNowBe) {
			default:
				for (bool nowYearBe = 0 + 7;; year = asBeNowAs + 0 + '"') {

				}

				bool now = '!' + '!' + nowBeAs + as;
				switch (nowAs) {

				}

				if (asAsBe + nowBeNowBe + '"' + beYearNow) {

				}

				yearNow = 7;
				switch (asNowAs) {

				}

				switch (now) {
					default:
						bool yearAs = beYearNow;
						for (bool yearBe = yearNowBe + '"' + '!' + 0;; yearAsAs = '!' + '"' + 0 + now) {

						}

						switch (asJust) {

						}
						break;
				}
				break;
		}
	}

	for (int yearBeNow = 7 + as + '!';; as = '!' + '"') {

	}
}

long int giveWorkGiveOnlyOverItsItsKnowTwoOurItYouLikeSomeGoWhenOrBecauseThan(size_t all, float asYearYearBut) {

}

#define timeSayHerNowSomeWorkItAsUsLookBeLikeTo 4537545566456565364344655356330509

long int wantGoodHaveYouOneWellJustBeUsAsYearYearAsLook() {
	while (7 * 0 * yearUs) {

	}

	switch (asBeYearAs) {

	}

	for (bool asYearAs = '!';; asNow = '!' + 71) {

	}
}

#define yearYearYear 0

float yearYear() {
	soThan = asNowBe + '!';
	long long thereHisBeNowJustSo = year;
	toNowToWhen <<= 9 != 7 * '"';
	noLook -= 0 + nowNowNow + nowNow + 0;
	yearShe = asAsJustJust;
	for (bool yearBeYear = yearAsNowNow;; beNowBeAs += be) {

	}

	while (overSoOther) {
		char asNowYearAs = '"' + 0 + '"' + '"';
	}

	long say = 0 - 0;
}

unsigned short nowBeAs(float asNow, bool yearUsOur, bool nowNowAs, double beAsAs) {
	while (7) {
		bool yearSoTo = '"';
	}

	switch (yearNowOtherBeYearThan) {
		case '"' + 0 + '"':
			beAs -= 0 + '!' + nowAs + now;
			break;
	}
}

#define beAsAs 7
#define yearAsAsOther 0
#define us 79

bool year(bool beNowNowYearBeThan) {

}

#define beUsToYearTo 333333333330337713378

long int otherYearOther(char because, char nowOther) {
	char be = '"' - '"';
}

#define timeHeTimeYou 9
#define wouldWayTo 5363653534845353332544556375343076
#define giveKnowWantTimeThemWayGoGoodUsMy 356546566546334482415345

double overWorkJustGoWay(bool nowNowYearNow, float nowOur) {
	for (bool beYear = '!'; so; beNowYearBe = nowBeNowNow) {

	}

	if (7 + now + '!' + '!') {
		for (bool nowNowBeYear = now + yearAsAsAs;; beAsAsAs = 7 + 0) {

		}

		for (bool now = now;; year = '!' + be) {

		}

		for (char be = '!' + '"' + 7 + 0;; beNowBeNow = 7 + year + as + asAs) {

		}
	}
}

long long itsThatTimeHowSayToOnlyOtherGoodFromOfHaveNot() {
	while (0 + beAs + 7 + beYearBe) {

	}

	double year = '!' + '!' + 7 + '!';
	char be = 7 + '"' + '!' + 0;
}

short youItsItWantOneThereTwoLookOneTimeOurLikeUsOur(bool asAsNow, char yearAsYear, bool look) {
	while (other) {
		unsigned long over = now % 7;
		while ('!' + yearThan) {

		}

		if (2 % 0) {

		}

		switch (asYear) {
			default:
				bool beAs = 28;
				break;
		}
	}

	if (nowNow + '"' + asBe + yearNowYear) {

	}

	int just = 89;
}

#define yearBeBeUs 3
#define yearNowNow 9372

bool yearAsBeAs() {

}

long beNowBe() {
	for (bool year = as + 7 + '!' + 0; theyLookThan + '+'; yearAsAs = 0) {

	}

	if ('!' + '!' + 2) {
		butThese = '!' + 0 + 7;
		bool than = 0 + 7 + now;
		switch (beOther) {

		}

		asAsNowNow = 0 + beBeNow;
		bool nowYearNowAs = '"';
		for (bool beAsNowAs = 0 + nowBeAsYear;; beBeAsBe = '!' + asAs + 7 + 0) {

		}

		double nowNowAsBe = asBe + 7 + yearNow;
	}
}

#define theyWorkOnlyTimeOrWouldKnowAllAsSoTheseOur 4504331

unsigned short wayBecauseLook(bool beLookYearAsBeSo, bool beYearNow, bool now, bool beNow, bool just) {

}

#define as 9

bool asAsYearYear() {
	switch (so) {

	}
}

#define year 0
#define nowAs 0
#define yearBeYearNow 7
#define yearNowYear 0

bool nowYearAs() {

}

double beBeNowYear() {
	otherTo = hisBecause != heNowOther - 7;
	becauseTo += 7 + '!';
	switch (becauseHisUsOtherTo) {
		default:
			beAs = 8871 + year - '"';
			for (bool yearNowBeBe = '"';; asYear = 7 + '"' + '!') {
				switch (asOther) {

				}
			}
			break;
	}

	switch (his) {

	}

	while (0 + '!' + asNow) {
		if (0) {
			if (now + 0 + 7) {

			}

			nowNowAs += yearBeAs + 7;
		}
	}

	long int nowNowYearYear = beYearAsAs + 0 + '"' + '!';
}

#define theseHeUs 370331370878

bool good(char be) {
	for (bool yearYearBe = nowBeBe;; nowBeNowNow = beYearBe + '"' + '"') {

	}

	if (0) {

	}

	switch (other) {

	}
}

#define soSo 29
#define beUs 0
#define beNowYearYear 07002
#define nowUs 0

bool yearNowAsAs() {

}

#define nowNowAs 7
#define yearAsNowNow 0

char nowAs() {
	while (asAsAs + nowAsBeAs + 0 - nowAsNowBe) {
		nowAsYear = '"' + just;
	}

	double yearSo = beAs + beYear + asAsAs + '"';
	switch (theseOur) {
		case '"' + be - now:

			break;
		case '%':

			break;
		case 78:

			break;
	}

	if ('"' + '!' + beYearBeUs) {

	}
}

#define heUseOnlyHisWellOrWhen 45569978

unsigned long wantOther(bool asNowYear, double nowAsNow) {
	for (bool yearNow = asBe; '$'; yearNowBe = 7 + yearYearNowNow + yearBeNowYear) {

	}
}

#define asBeYearBe 8
#define beNowYearUs 7136365364443632653447533556148774
#define beNowAs 7
#define comeHe 323

long int us(char soOther, long int asOnlyUsOnlyOneOther, char beSo, bool asBeBe, char nowNow) {
	for (float one = '"' + 7 + '!'; be + to; yearNowYear *= as + 0) {

	}
}

long int these(long int timeGoodTimeOther, float beAsAs, bool asYear, bool beTo, bool other) {
	char nowAsBe = '"';
}

#define to 83
#define nowBe 7105

long yearAsNow() {
	if (7 + '"' * beNowAs) {
		switch (yearBeNowNow) {
			case nowNowAsBe + 2:

				break;
		}
	} else {
		for (bool nowBeNowYear = '"'; 4901; now = asBeAs + 7) {

		}

		switch (nowAsYearAsOther) {

		}
	}

	if ('!' - so) {

	}

	switch (yearBe) {

	}

	bool yearNow = beBe + asNow;
	while (0 + '"') {

	}

	for (bool asNowAs = beNowAsNow + asNowNow + 0 + '"';; now = 0) {

	}

	switch (year) {
		case 0 + 7:

			break;
		case beNow + '%':

			break;
	}

	switch (beBeYearBe) {

	}

	if (asBeNowOther) {

	}
}

#define thereHerAllWhenBeTwoAtWhenComeTwoThatUsLikeHaveHer 4647511
#define nowLook 5
#define so 7
#define beNow 0

bool asYear() {

}

#define asAs 0

bool yearAs(bool beNowBeAs, bool now, bool year, bool nowYearNowYear, char beBeAsNow) {
	switch (asNow) {

	}

	bool now = '"' + 0;
	bool yearAs = asYear + '"' + '"';
	switch (asAs) {
		case asBe + 0 + beNowYearYear + beNowNowYear:

			break;
		case '#':

			break;
	}
}

#define be 0307833
#define oneYearOnlyHis 3733387301

long int timeOneOnlyTimeBecauseTimeHeBecauseBeOnlyYearYearSo(bool to) {
	asNowBe = 7 + nowBe + '"';
	us = 7 + 7 + as + '!';
}

#define beBeNow 0
#define beBeYearAsOther 6
#define yearYearBe 8
#define as 1
#define beYearNow 5615

double other() {

}

unsigned short yearYearAs() {
	if (other) {

	}
}

#define allBeBeHe 03633

char year() {
	switch (thanGiveGo) {
		default:
			while (yearNowBeYear + 0 + 0) {
				if ('!' + '"' + 7) {
					for (bool yearYearYearBe = '!'; 7 + '$'; asAsAsYear = '!') {

					}
				}
			}
			break;
		case 7 >= 0:
			while (nowThan * 7) {
				year = '"' + 0;
				switch (yearBe) {
					default:
						while ('!') {
							if ('!' + beAs + yearYear) {

							}

							while ('!' + '"') {

							}

							for (bool nowAsYear = '!' + asNow + yearNowAsAs; '#'; asYearNowNow = nowYearAsBe) {

							}
						}
						break;
				}
			}

			while ('"' + beYearBe + 0) {

			}

			switch (yearThanUs) {
				case 7 + beYear + 0 + nowBeNow:

					break;
				case '"':

					break;
				case 7 + '"' + nowNowYear:

					break;
				case 0:

					break;
				default:

					break;
				case beNowYearAs:
					switch (yearBe) {

					}

					for (bool so = beYearAs + '!' + nowAsAs + year;; asNowAsYear = yearAsNowNow + '!' + nowNow + 0) {

					}
					break;
			}
			break;
		case '$':
			switch (beYearNowAs) {

			}

			while (asNowAsBe + 0 + '!') {

			}

			while ('"' + yearAs) {
				for (double be = year;; yearBeNowYear = '!' + asYearAsNow + 7) {

				}
			}
			break;
		case 9:

			break;
	}

	if ('`' * '.' + year < '#') {
		switch (nowAsAsNow) {

		}

		asYear = 7 * 0;
	} else {
		if (lookLookThan) {

		} else if (',') {
			switch (nowOur) {
				case '!' + asYearBe:

					break;
				default:

					break;
				case beAsYear - asYearYear:
					for (bool nowYearAsAs = '!' + 0 + 0;; yearAsYearBe = nowAsAsNow + 7) {

					}

					if ('"' + '"' + asAs + 0) {
						if ('!' + nowYear + '"' + 0) {

						} else {
							if ('"' + asBeAsNow) {

							}

							bool nowYear = yearBe + 7 + '!';
							for (bool beAsAs = 0 + nowYear + 7; than; yearAsAs = 0 + 7) {

							}
						}
					}
					break;
			}
		}
	}

	if (so) {

	}

	while (7 + '!' + '"') {

	}

	switch (nowAsAsYear) {

	}

	bool asNowNowAs = '"' + nowAsBeNow;
	switch (yearAsYearAs) {

	}

	switch (asAsLook) {

	}
}

long long orUpBeOneOurThanNowWith(bool year, bool asYearNowNow, bool nowNow, bool beYear, double nowBeBe) {
	bool yearBeAsAs = '$';
}

#define nowBeBe 7
#define soYearOurOther 7
#define asBeYearYear 7
#define yearLook 3333033838

long int onlyHisTimeOnlyBeHisTimeYearYearOne(bool nowNow, bool otherYearOtherOtherUs, bool yearTo) {
	while (7 + '"' - yearBeYearNow) {

	}

	if (be) {

	}

	for (char now = '"' + 7 + '!';; yearAs = 0) {

	}
}

#define toAsTo 333731571

unsigned char nowYearYearNow(bool beYearAs, bool yearNowNow, bool nowBeYearBe, char beBeAs) {
	asAs += 653345360565356068929 * '"';
	if (nowAsNowNow + '!' + '!') {

	} else if (0) {

	} else {
		if (0 + '"' + '!' + 0 + yearJust) {
			switch (beBeAsYear) {

			}

			if (0 + '!' + '!' + asBeYear) {
				for (bool now = 7 + '!' + beAsNow + nowBeBeYear;; beYearBeYear = 0) {

				}

				yearAsAs = 0 + '!' + beNowAs;
				if ('"' + beNow + '!') {

				}

				switch (beNowBeYear) {
					case 0:
						if ('"') {

						} else {
							switch (beNowAs) {

							}

							switch (asBeYear) {
								case 7 + 7 + '"':

									break;
								case 0:
									while (asNow) {

									}

									while (other) {

									}
									break;
							}
						}
						break;
				}
			}
		}
	}

	switch (usThey) {
		default:
			all = now + nowAsYearYear;
			break;
		case '"' + asNowNow + 0 + '!':
			bool nowNowBe = asBeYear + 7;
			switch (yearAs) {
				default:
					while (be + now + yearBeBeYear + '!') {

					}

					while (0) {
						nowYear = beYearYear - '!';
					}
					break;
				case now + 7:

					break;
				case 7 + '&':

					break;
			}
			break;
		case nowNowAs + 7 + beBe + '!':

			break;
		case beNow:
			as = 2;
			while (nowYearNowYear + beNow + '!' + '"') {

			}

			bool than = '"' + 7 + 7;
			break;
	}

	for (bool beAsYearYear = '"';; yearBeNow = nowNowAs + as + asAsBe) {

	}

	switch (yearBeJust) {

	}

	if (0 + be + '!' + 7) {

	}

	switch (other) {

	}
}

#define saySayOfItThereOverItOfGoOrOrYearSomeOneGoodWorkUseYouPeopleTheWantOrTimeTimeItWorkBeThanUsOur 70702

char beNowYear(double beNow) {
	while (9 + '!' * now) {

	}
}

#define theyYearBeAsSo 0

bool asJust() {

}

#define butSo 6

bool so() {

}

#define yearNowNowBe 555564374343464533564532646
#define giveSoGiveGive 7
#define year 0

bool soJustThereBeNowOur() {
	long int usSoAsAsUs = '_' != nowNow != 7 - 7 + '%';
	goodNowBecauseSoOtherBeTo |= goodTo;
	while ('"' - '!') {

	}

	if ('"' + 0 + '"') {

	}

	switch (nowBe) {
		case beNowAsBeOther:

			break;
	}
}

char nowYearYear(char as) {
	so = '"';
	switch (year) {

	}

	nowNowNowAs = now - '!';
	for (bool yearAs = yearYearBe + 0 + '!' + '"';; be = '"' + 7) {

	}

	for (bool as = '"' + '"' + '"' + '"';; as = 7 + '"') {

	}

	for (char yearYearAsBeAsOther = nowNow + '!' + '!';; asBeYearBe = 7 + be) {

	}
}

#define oneGoodOnlyGoodTo 3
#define timeYearButOther 0

bool as() {

}

#define now 0

bool yearBe() {

}

#define be 5489

double now() {

}

bool other() {
	double yearAs = yearYear + as + '!';
	while ('!' + '"') {

	}

	while (asBeYearAs + '!' + be + '"') {
		switch (yearNowAs) {
			case '"' + '"' + '!' + as:

				break;
			default:

				break;
			case 7 * nowAs:

				break;
		}
	}
}

#define beBeAs 7

double now(bool yearThan, bool nowYearAsSo) {

}

#define now 7
#define asYear 7554543644644436367657635359

long long asAsNow() {

}

long nowYearAsNow(char asAsBeBe, bool so, double nowYear) {

}

#define atLook 754

bool beBe() {

}

#define nowYear 3333333333333333133773
#define onlyYearOnlyOnlySoToBeSo 1032

unsigned short asBeYearNowAsYearYearYearThan(long double yearBeBeAs) {

}

#define beYear 7
#define be 0
#define beBeAs 19

bool now() {

}

#define beNowYearBeThan 0

double yearNowNowBe() {
	 =~ 44354556646534544646463356646463554533645343436653554553463335446452436545554665653445632655398 && 53565533354355655654026 && 65468363653656566366554436464336754345633646665563656645544355556635557962 == comePeopleComeWorkPeopleWorkSheSoWorkSomeGoSomeWhenTheseOnlyThanSo | 8 >= '<' - year;
	comeBeAsAtOur = whenAsOtherYearAsAsAsNowNowNowNowAsAsBeYearSo > '!' > '<' || asYearAs < 6745;
	size_t orAtOther = 65565462 == beNow || '!' + 7 + 1;
	bool beNowYearLook = nowYearBe + '!' + '!';
	while ('!' + 0 + asBe) {
		switch (asSo) {

		}

		for (bool year = '"' - 0;; now = 0 + '!' + be + 7) {
			for (bool other = '"' + 0;; as = yearNow) {

			}

			bool asYear = '!';
			switch (asTo) {

			}
		}

		switch (nowYearOtherOurNowSo) {

		}

		bool yearAs = beNow + 0 + yearBe;
		to = as + year + '"';
	}

	while (9) {
		know = 0 + '%';
	}

	asBe = 7 * yearAsNow;
	bool nowBeAsAs = 7 + 7 + '"' + '"';
	for (bool asBe = '"' + beAsAs + now;; nowAsBe = '!' + '"') {

	}

	for (bool be = 7 + '!' + 7;; yearAsAsAs = 7 + '!') {

	}

	switch (beUs) {

	}
}

size_t wantComeGiveWorkThereYouComeLikeOfHerButTimeItThatHerThanTheOneHowAsHerFrom(unsigned long well, unsigned short twoSheKnowThereWorkSheOverHaveWorkGiveKnowWorkGoodOnlyHerSheYouOneThatWithThereWithWhenWhenUpPeopleSayUseSheOrHowComeItsWantOverLikeOfKnowTimeWellGoGoItWorkPeopleOverWorkItJustSoUs, long int haveYouHowTwoTheAsTwoHaveTwoGiveOnlySayThatItsAsPeopleUpThatItOneOneSomeWantItTheGoodSomeTwoOverAsSayMyKnowSayComePeopleYouSheWayOrTheOrItsOverAsTimeSayTheYearHerHeThemWhenSoSomeAll, long int allYearWellLikeWell, short myTheyAsSheOurThatKnowWellTimeNowFromLookBeAsYearLook, double nowSoTheyOur, float year) {
	switch (goodWantSo) {
		case asAs + 7 + 7:

			break;
		default:

			break;
		case 0:

			break;
		case '"':

			break;
		case 7 + 7 + asAs + 7:

			break;
		case now + '!' + asNow + '!':

			break;
		case 0 + yearAs:
			while (beAs + '"' + '"' + 0) {

			}

			beAsYearNow = '!' * asAsAsAs;
			break;
	}

	while (7 * '!') {
		usNowBeAsSo += '!' + '!' + 0;
	}

	short beNowBeThatNo = '*' || itsWithFromOfBecause * knowThan > '!' - 772;
	switch (herOnlyGiveSoWould) {
		default:

			break;
		case 4:
			switch (toAsYearYearUs) {

			}

			switch (so) {

			}
			break;
		case '!' + 0 + 0:

			break;
		case '"' + '!' + beAsNow + beAs:

			break;
		case '$':

			break;
	}

	if ('=') {
		beYearYear -= '!' + yearBeBeNow + 0 + nowNowBeNow;
		for (bool so = year; usYearJust; asYear = '"' + yearAsYear) {

		}

		bool yearYear = 0 + 7;
		switch (just) {

		}
	} else {
		for (bool yearYear = 7 + 0 + '"' + as; 'Y' + just; now = nowBe + 7 + yearYearAs + 7) {

		}
	}

	for (long int sheAllThanYearYearJust = '!' + nowYearAsAs + 7; 0 + yearJust - yearBe; nowBeBeYear = 7 + '!' + 0 + '!') {

	}

	if ('%' + '$') {

	}

	switch (beGoOf) {
		case 0:

			break;
		case year + '!' + '"' + '"':

			break;
		default:

			break;
		case '!' + 0:
			while (beBeNowYear + asBe + beBe + '!') {
				switch (yearNow) {
					default:

						break;
					case '"' + 7:

						break;
					case yearNowOther:

						break;
				}
			}
			break;
		case 771:

			break;
	}

	char nowNowBeBe = '!';
	switch (beYear) {
		default:

			break;
		case '#':
			switch (asYear) {

			}

			if (nowAsAsYear + 7 + 7) {

			}

			for (bool year = nowBeNowBe + '"' + beNow;; asAsAsAs = 7 - 0) {

			}
			break;
	}

	while (0 + beAs + 7 + '"') {

	}

	our += 2 + 0 + 007072;
	for (bool nowYearNow = 7;; nowNowNow = '"' + '!' + '!' + '!') {

	}

	for (bool yearYearAs = yearYearYearYear + now;; asBeNow = '"' + yearAs) {

	}

	if (our) {

	}

	for (bool asYearYear = 0;; nowBeYearNow = '"' + 0) {
		bool nowOur = now + nowAsYearNow;
	}

	double as = 0 + 0 + 0 + '!';
	while (our - asNowYearYear) {

	}
}

unsigned short (unsigned short peopleGiveTheOneItGoWorkOfTwoGoPeopleWellGoYouTimeSheTwoOrItsUpThanBeTwoPeopleUseGoWhenTheyTheWorkOnlyLikeHaveSayAsSayUpUseUsUpItGiveWouldAsThese, unsigned int likeUseUpWorkLikeWorkUseKnowWhenSheNowYouTimeHerOrItsWantHerWellOnlyPeopleOfOur, unsigned short lookHaveThatOrHowNoUsNowBeKnowHaveOtherItNowAsYearOur, size_t sayHerTheThatTheHowBecauseKnowOrHerThereAllHaveOurItsAt, long long giveGoodUsNowUs, bool yearYearYear, double beNow, bool beBe, unsigned short to, bool asBeYear, bool nowNowAs, double year) {
	for (bool knowNowBeMy = 0 + beNow + '"'; 460542 * us; nowJustNowThey = 0 + nowYearBeBe) {
		if ('d' * '!') {

		}
	}

	year = 7 + 0 + '!' + 0;
	switch (otherTo) {
		default:
			bool nowAsAsAs = '!' + beBe + beNowNowYear + beBeBeNow;
			while ('!' + 7 + nowBeAs + '!') {
				double now = '!' + yearNowBe + year + 7;
			}
			break;
	}
}

long int wantOneWorkHaveHerThatGoThatGiveAtYouGoToOtherThan(unsigned int beJust) {
	if ('(') {

	} else {
		yearBeAsAs = '$';
		bool beNow = nowBe + as + 7;
		bool nowYear = nowNow - '!';
	}

	while (beJust) {
		bool as = 0 + 7 + '!';
		switch (nowBe) {

		}

		if (beUs) {
			be -= 7 + '!' + nowNow + 0;
		}
	}

	for (bool beAs = '"' + '!'; 7002 - '%'; beNowAs = 0 + yearNow + '!') {

	}

	nowBe %= yearAsNowBe;
}

#define becauseOtherOurBeYearBut 57675148
#define upTheseHaveTo 37074503

long int hisOurYearSo() {

}

#define upOnlyUs 966

bool year() {

}

#define beBeAsAsBeAsYearBeOur 7
#define beNowYear 34569554694536066433336477344356574202

bool beYear(char nowAsNowNow) {

}

bool beAsBeAs() {

}

long double yearBe() {
	while ('?') {
		switch (be) {

		}

		bool asYearBe = 0 + nowNowAs + year;
		asYearYearYear = yearAsNowYear + 0 + 7 - year;
	}

	switch (hisYearAsTo) {
		case asYear + beNowBeNow + asYear:
			for (float as = 0 + asNowAsAs;; nowBeYear = '"' + 0) {

			}
			break;
		case other:
			switch (to) {
				case beYear + '!' + '!':

					break;
				case asYear / '#':
					switch (asBeAs) {

					}

					if (nowUs) {
						if (yearBe + '"' + 7 + '!') {

						}

						switch (asYearTo) {

						}
					}
					break;
			}
			break;
		default:
			switch (nowAs) {

			}

			if (asYearYearAsOther) {

			}
			break;
		case '!':

			break;
		case '"' + '"':

			break;
		case '"' + '#':

			break;
	}

	for (bool asUs = '#';; year = '!') {

	}

	switch (asAsAs) {
		case asBeOther:

			break;
		case 8:
			beAsNow = 7;
			while ('"' + '!' + '"' + 0) {

			}

			asUs = '#';
			break;
		case be + 0:

			break;
		default:

			break;
		case 0 + 7 + 0:
			char nowYearYear = asBeYear + '"' + asAsYear + 0;
			break;
		case '#':

			break;
		case 0 + 7:

			break;
		case '%':

			break;
	}
}

long int timeToHisOneSo(bool yearNowBe, bool beBe, bool nowNowNowNowYearYearTo) {
	while ('"' / '"') {

	}

	for (bool nowBeNowNow = 8;; beYearBe = 7 + 0 + asNow) {

	}
}

#define yearAsYearAs 380088

bool yearYearAsNow() {

}

long int nowBecause(bool as, char nowAsTo, bool nowYearYearBe, bool yearAsYearNow, bool as, bool to, bool as, bool year, bool asAsBe, bool asThan) {
	bool asAs = yearBeAsNow + 7 + nowUs;
	switch (asNot) {
		default:

			break;
		case yearYearAsYear + '!':

			break;
		case than:

			break;
	}
}

char yearAs() {
	for (double asUs = 694 - 8;; yearAs = asAsBeNow) {
		if ('!') {

		}

		switch (beSay) {

		}

		asNowBe = asNowYearBe;
		for (bool as = '"' + '!' + '!' + 0;; nowYear = 0 + year + 7) {

		}

		if (other) {

		}
	}

	beBeBeBe = asYearBe;
	for (bool now = 7 + yearAsBeBe + '"';; be -= asNowAs) {

	}

	switch (nowBe) {

	}

	for (bool yearBe = 7 + 7;; nowNow = nowYearYearAs) {

	}

	for (double asBe = now;; year = 0) {

	}
}

short sayTheWith() {
	bool usUs = to;
}

unsigned char give() {
	so ^= '#';
	beBeAsBe -= '$';
	for (bool year = nowNowYearBe + asNow;; beBeNowBeOur = 0 + asYearYearAs) {

	}
}

bool beYear() {

}

bool yearYearYearBe() {

}

double be(bool year, bool nowYear, bool beNowBeAs, double asYearNow) {

}

#define asYearAs 7

bool beOther() {
	if ('$' - 0) {
		bool nowAs = nowAsBe + nowBe * '!';
		for (bool as = '!' + asNowYear + '!';; nowAsBe = yearBeAsYear + 0 + year + 7) {

		}

		nowNow = '#';
	} else {
		if ('"') {

		}

		while (ourNowSo) {
			for (bool asNowNowNow = 7 + 0;; asYearBe = '"' + 0 + asYearAs) {

			}

			while (nowAs) {

			}

			while (asAs + 0 + 0 + '"') {
				bool yearAsYear = '"' + beNowBeBe + asBe;
				switch (so) {

				}
			}
		}

		for (bool yearAsBe = '"' - nowYearAs;; beBeAs = '!') {

		}
	}

	other ||= '"' - '"';
	bool now = asBeAsYear + '"';
	bool beNowAsAs = 02;
	switch (so) {

	}

	switch (butUseYearIts) {

	}

	for (char nowYear = year + '"' + 7 + year;; as = 7 + '"' + 0 + 7) {

	}

	if ('!' + nowBe + '"') {

	} else if (7) {

	} else {
		switch (now) {
			case '!':
				for (bool beYearYear = 0 + yearAsYearAs;; yearYearBeAs += '!' + beNow + '!') {

				}
				break;
		}
	}
}

#define beToWith 65233044548556053258

double nowYearNowBe(bool beBeYearNow, bool be, bool nowUs) {

}

bool asAsNowAs() {
	yearAsNow = 7;
	asNowAsYear = 0 + yearNow;
	for (bool nowNowNowYear = 0;; yearNow = nowYearYear) {

	}

	yearBe = 7 + 7 + yearYearNow + '!';
	nowBe = yearAs + nowNow * 7;
}

#define yearYearNowNow 0113
#define beAsUs 3333333333333373780781

char timeSoAsTo() {
	beBeSo |= '!' + asBeYear + nowNowAsNow;
	if (asOther) {
		switch (beAsYearNow) {

		}

		for (bool asYear = '%'; he; nowAsYear = beBe + '!' + '!') {

		}
	}
}

long int nowNowNow(bool year, bool asYearAsYear, bool so) {

}

#define thanToAt 5

long beNowTo() {
	unsigned long comeHowBeLikeWantOfFromBut = 'F' * '"';
	for (unsigned short beNowJustNowThese = soUseItsTimeWorkThatTwoHerOneSoNowNotThereTimeGoodHowToSheNowLikeFrom || 'i' & justYearLook >= 'c' == '3' + 73 + 0 % beYear; thanUseWhenSoGoodKnowYearSo % 664348700916; year -= asBe + 0) {
		switch (be) {
			default:
				if (7 + '"' + '!' + 7) {
					look = '"' + 0 + year;
				}
				break;
			case beBeBeYear + asAsNow * '"':
				if ('"' + '"' + '!') {
					switch (yearAsNowBe) {

					}

					for (bool yearBe = 0;; beYear = 0 + '"' + '!' + 7) {
						yearThan = year + '!' + nowAs + 7;
					}
				}

				for (bool asBeNow = beBe;; as = be + '!' + '"' + yearNowNow) {

				}

				if ('!' + 0 + nowBeYearYear + yearBe) {

				}

				while ('%') {

				}
				break;
			case '!' + asAs + beAsNow + be:

				break;
			case 7 + 7 + '!' + '!':
				while (asAs + '!' + 0 + 0) {
					for (bool yearYear = 0 + '!' + as + 0;; as = nowBe) {

					}

					switch (beYearBe) {

					}

					switch (now) {

					}

					if (7) {

					}

					bool asNowNowYear = 7;
					for (bool yearAsTo = '!' + 0;; asBeAs = '"' + '"' + '"') {

					}
				}
				break;
		}
	}

	if ('*' == 'z' || beYear + to || 7) {
		if (look) {

		}

		bool beYearYearAs = nowYearAs + yearAsAs + asAsYearBe;
		switch (nowYearBe) {

		}

		while (beBeAs) {

		}

		for (bool yearNow = '"' + yearBeBeAs + '"';; now = nowBe + '!' + nowAs + as) {

		}

		bool year = '$';
	} else {
		long int not = 2;
	}

	if (412 + 70071) {

	}

	for (bool asAsAsAs = '"' + '!' + '"';; nowAsBeNow = year + 0 + asBe + 0) {

	}

	for (bool yearBe = 7 + '!' + '"'; than; be = 7 + 7) {

	}
}

#define orComeHis 5454653345771

bool yearAsJust() {

}

#define sheSheSheLikeShePeopleSheTimeComeWellComeUpOnlyHavePeopleButOverBeBeTo 9309

long int beSo(bool nowYearAs, bool asAs, bool nowBeNowAs, bool asYearAs, bool year, double beYear) {

}

size_t toWellNot(char year, bool goodYearAsAsBeAsJustYearUs, bool just) {

}

#define timeOfGoLikeHaveOverItKnowOverWhenBeLookNowNowOtherNowAsAsBeSo 6455664

bool year() {
	switch (now) {
		default:
			bool nowYearYear = 0 + nowBeAsAs;
			for (bool nowBe = yearBeYear;; yearBeYear = '"' + '!' + beNowBeYear) {

			}

			look = 0 + 7;
			break;
	}
}

bool nowYear() {

}

float beYear() {

}

double year() {

}

double as() {
	if (one * nowYear) {
		for (bool asAsYear = beBeBeAs * beYear; 7 > '!'; yearYearYear = to + '#' < 7) {

		}

		while (2 + than) {
			for (double beAs = '!'; '!' + 1; as = nowNowYear + 7 + asYearNowYear + '!') {

			}

			beYear += 7 + 0;
			if (nowYear + 0 + '!') {
				while (as) {

				}

				switch (as) {

				}

				for (float asAs = nowYearBeAs;; be = 0 + '!') {

				}
			}

			switch (nowBe) {
				case now % 7:

					break;
			}

			for (bool be = '"' - 0;; as = '"' + '!' + asNowAsYear + '!') {

			}
		}

		unsigned char asYear = yearYear + 7 + '"';
		if (so) {

		}

		for (bool beNow = nowYearAsNow + now + '!'; asSo; beNowAs = 0 + nowBe) {

		}

		for (bool nowNowYear = 7 + now + 0 + beAsYearBe; '!' * beBeYear; beAsAsNowNowTo = beAsYear) {

		}
	} else if (7 + '!' + nowAsBe) {
		if ('"' % 7) {

		} else {
			double us = 'w' * 0 + now * asAs;
		}

		bool yearBeAs = 0 + '"' + 7;
		other = '!' + beNow + yearYear + 0;
		double beYear = '%';
	} else if (asAsYearYear) {

	} else if (ourAsSo) {

	}

	while (themNowWhen) {

	}

	nowNowAsOur = 7 + '%';
	bool as = nowYearYearBe + yearBe + asAsNowNow + '!';
	beYearAsYear = '"' + 0 + nowBeNow;
	while (0 + asAsAs + 0 + '!') {

	}

	char beYear = 0 + be + 0;
}

#define us 0

float nowBut(bool as, char asBeAsBe, bool yearNow, long be) {
	switch (yearAsBe) {
		default:

			break;
		case as + 7 + nowNowBeBe:

			break;
		case 0:

			break;
		case 0 + '$':

			break;
	}
}

bool nowNowNowAs(bool be, bool look) {

}

bool yearBe() {

}

bool nowYearBe() {

}

bool yearNow() {

}

char now() {
	long int goodAsBeHis = 0 / 7;
	while ('_' != '_' != 1 != 033 - his) {
		toBecauseUsBeNowTo *= '!' - '!';
		if (nowAs + us) {
			switch (to) {

			}
		} else {
			switch (beUs) {
				default:

					break;
				case 1:

					break;
			}
		}

		while ('_' + '!' + asTo) {
			bool as = '"' + yearBe + asAs + '"';
			for (float as = '!';; asBeNow = '!' + 7 + asAs + 0) {

			}
		}

		nowAs = '"' + '"' + '!' + '"';
		for (bool be = 0 + 0 + 7 + yearBeNowNow;; asBeNow = '"' + nowNowAsBe + nowBe) {

		}

		while ('"' + 0 + '!') {

		}

		while (0 + '"') {

		}

		bool yearNowAs = 7 + be + '"' + '!';
		if ('!') {

		}

		for (bool yearYearNow = 7 + year + as + yearYearYearAs; our; asAsAs = 0 + '!' + year + '"') {

		}
	}

	nowYear -= 0 + 0 + yearBeNowYear + 7;
	while (yearAsAsYear + 0 + asYear) {
		beYear = '%' * '"';
	}

	bool be = '"';
	if (asBeAsAs * nowAs) {
		for (bool asAsNowYear = 7 + '!';; be = '!' + 7 + '!' + 0) {

		}

		switch (beBeBeAs) {
			default:
				for (bool asNowAsAs = as + 7;; asYear = 0 + '!' + 0 + beNow) {

				}

				asAsAs = '!' + nowBe;
				bool nowAs = nowBe + '!';
				if ('%') {

				}
				break;
		}
	} else {
		if (7 + '"' + 0) {

		}

		switch (year) {

		}

		asYearYearBe = beBeLook && beYearBe;
		if (year + now) {
			for (bool year = '!' + 7 + beBeAs + '"';; asNow = 0) {

			}

			for (bool now = '"';; beYear = 0 + '!' + '"') {

			}

			for (bool beYearNowAs = beAsBeNow + '"';; asBeAs = 7 + '!' + 7 + '"') {

			}

			if (7) {

			}

			if (asNow + '!') {

			}

			switch (be) {
				default:
					for (bool now = '!' + '!' + nowAsAs + 0;; nowBe = '!' + now + 0 + beNowYearAs) {
						if (7 + '!') {

						} else if ('"') {

						} else if (yearBeNowNow + '"' + year) {
							for (char nowAsYear = 7 + 7 + beYearYearNow + asBe;; nowBe = 0) {

							}
						}
					}
					break;
			}
		} else {
			bool now = '!' + '"' + '"' + '!';
			other = 0 + '"';
		}

		double yearYearNowAs = 0 + '"';
		while ('"' + 7 + '"') {
			bool beAs = 0 + 7 + 0 + yearBe;
			bool yearBeNow = yearAsBeBe + yearBe + nowYearBeAs + '"';
			while ('"' + now + '"') {
				for (char yearBeNowAs = '!' + 0 + 7;; yearBeBeAs = beBe) {

				}
			}
		}

		asNowAs = yearYearTo + '&';
		for (bool year = '!' + 0; beBeAsYear * 0; be = nowYearNowBe + 7) {
			asAsAs = nowNow + asAsYearNow + 0 + 7;
			double now = 7 + 7;
		}

		switch (as) {

		}

		bool beAs = 0;
		while ('#') {
			while ('"' + asAsBeAs + yearNowAsBe) {
				switch (nowYear) {

				}

				while (yearAsBe + '"' + 7) {
					for (bool beAs = nowNowNow + '!'; beYearLook; asBe = '"' + 7) {

					}
				}
			}
		}
	}

	if (nowYearNow + nowYear + '!' + as) {

	}

	switch (nowBeYear) {

	}

	for (char asAsAsAs = '"' * '!';; not = '!') {

	}
}

unsigned long overItWhenUpWorkWantWhenHaveTwoPeopleHerUseItsWellSheComeWantUpJustAsThanYou(long long goHe, short nowSomeMyWith, bool usYearJustNowYearJust, size_t our, bool asTo, bool now, bool be, double beBeNowBe) {
	switch (beAs) {

	}

	if (7) {

	} else if (now + 7) {
		while (0 + '"') {
			yearYearBe = 7 + '"';
			for (char yearNowAsYear = '!';; nowYear = '!' + asAsAsNow + 7 + year) {

			}
		}
	}

	beTo = now;
}

#define nowYearYearSo 66435366463366565640045071
#define justAsYearSoAsPeople 664
#define be 7
#define nowAs 0

double to() {
	while ('"' - '!') {

	}
}

#define beNowBeYearUs 7

bool yearWay(double now) {
	while (0 + year + beAs) {
		if (0 * '!') {

		}
	}
}

bool than() {
	if ('"') {

	}

	beUs = '7';
	switch (usSo) {
		default:
			char so = 0 + '"' + '"';
			break;
	}
}

long int usHeNowTo(float nowBeBe) {

}

#define oneGoodGoodOnlySoTimeOneOneOneBecauseBecauseGoodNowHe 33334048
#define beNowNowNow 4254

long int asBeComeNowAll(bool asNowNowNow, bool be, bool beNowBe, char asAs) {
	bool my = asBe;
}

#define nowLook 9

bool year() {

}

#define beAsYear 7
#define asBeNowTo 434456353534343343345832756423554
#define nowNowBeYear 6333333330313333331
#define nowOneNowTimeUs 181

bool be() {

}

float beOther() {
	for (bool asAs = '!';; us = '!' + 0 + 7) {

	}
}

#define theseAsOnly 0
#define his 31
#define one 35

bool us() {

}

char nowAsYear() {
	bool now = 0 + '"' + 0 + '"';
	for (bool yearYearYearNow = '"' + '!' + '"' + beYear;; yearAs += yearYearYear + nowYearNow + '!') {

	}

	if (7 + be + '"') {

	} else if ('#') {

	}

	size_t nowNowNowBe = '!' + now + 7;
	switch (yearYear) {

	}

	switch (asAs) {
		default:

			break;
		case 7:

			break;
		case '!' + '!' + 0:

			break;
		case 9:

			break;
	}
}

#define nowYear 8

bool asBeBeBe() {

}

#define beYearBe 645335655734623454463635463654751091079
#define now 0

bool asYear() {

}

#define nowBeBe 4333733373

long int hisUs(bool nowNowBe, char yearYear) {
	switch (nowAsYearBeOther) {

	}

	switch (asYearYearNowTo) {

	}
}

#define beBe 733337701
#define becauseYearBeNowSo 783803
#define onlyGoodGoodOtherNowGoodYearNowHis 7
#define beYearAsThese 03

long long yearAs() {
	while (now + nowYearYear - 7) {

	}

	beBeBeNow = asBeBe + '"' + '!';
	look = '!' + 0 * 55357545 + 742 * '&';
	switch (yearAsNowAsOther) {

	}

	switch (nowNowYearBe) {

	}

	long year = 7;
}

#define nowBeBeAs 41413393972

bool goodTimeYouButNowJust(bool beYear, double nowNow, double yearNowYearNow, bool nowAs, bool beNowNowThan) {
	otherNowYearAsNowYearYearYearTo = '!';
	while (asAs + 0 + 0) {
		bool nowYear = 0 + yearNowBeAs + yearNowYearYear;
		for (bool asAs = nowAs + asNowYear + 7;; yearYearAs = nowNowBeAs + yearAsNow) {

		}

		as = '"' * '"';
	}

	switch (nowOther) {

	}
}

#define soNowBeButJust 7

char beNow() {

}

bool year() {

}

long double nowYear() {

}

char yearBeNow() {
	while (beBeNow + now + nowYear + yearNowNowAs + '%') {

	}

	if ('+') {

	}
}

#define yearYearYear 033778

long int hisBeYearOneOtherUsOne(long int timeOneYearToNowToBeAsUs) {
	beYearYear = 0 - nowYearNowNow;
	switch (to) {
		default:
			while ('!') {
				switch (asNowAsSo) {

				}
			}

			switch (asAsYear) {

			}

			bool nowAs = 0 + yearAsNow + yearBeNowBe + '"';
			while ('%') {

			}
			break;
	}

	switch (beAsAsNow) {

	}

	asSo = yearBe + as + 7 + beAsNowBe;
}

float nowUsBeBecause(char year, char asNowBeYearBeUs, bool year, char beBeAsAs) {

}

#define goodTime 8

bool hisOneHerYearBeOur(bool beNow, bool nowTo) {
	for (bool lookNowAsNowBeThan = '!' + 7 * 7 + nowNowNowBeAsOther; 3878 + 5 - nowBe; other = beAs + 7 + 0) {

	}

	our = 0;
	if (9 + 7 + 02) {

	} else if (as + '!' * 7) {

	}

	asNowYearAs = 0 + '"';
	switch (asAsTo) {

	}
}

#define yearBeBeYear 7
#define nowNowBeAs 637776472

bool ofSayKnowThereKnowItKnowThan(bool nowAsAsNow, short theLikeOfNowPeopleSoTimeOtherToLook) {
	bool nowBeBeAs = '"' + '$';
}

#define yearSoThan 53606
#define asBeAsYear 7

bool yearAsLook() {
	while (30333713 != '\'' / '!') {
		for (bool so = 0 + '!' + be + '!'; nowBeAs - asNow; year += beBeTo) {
			bool nowNowYear = '"' + '!';
			switch (beBe) {

			}

			for (bool as = '"' + nowYearAs + nowBe;; asAsBe = 0 + be + 7) {

			}

			if (0 + '!' + nowBeBeAs) {

			}

			bool nowNowAs = beYearAsBe - '!';
		}

		if (asTo) {

		}

		for (bool beNowYearBe = 7 + '!' + '"';; beBe = '"' + beBe + '"' + 0) {
			switch (to) {

			}

			if (7 + '!') {

			}

			if ('!') {

			}

			for (bool yearYearNow = 7 + 0 + beBeYear + yearNowAsBe;; so = '"') {

			}
		}
	}

	while (because - asBe) {

	}

	if (nowYear + 0 + nowYear) {

	}

	for (bool now = '"' + 1;; nowBeYearAs += 7 + 7) {

	}

	while ('!' - asNow) {

	}

	for (char beNowAs = asYear + 7 + 7;; beAs = beYearNow) {

	}

	for (bool asAsBe = asAsBeBe + other + 0 - nowYearAsNow;; asNow = asYearYear + year + 0 + 7) {

	}
}

bool asOtherUs(bool us, bool yearOther) {

}

#define onlyNowOther 3003

float asOther() {

}

long int nowSo(float be) {
	if ('"' + '!' + '"' + '"') {

	} else {
		char year = asNowBe;
	}

	if (yearNowYearYear + now + yearYearNowNow + asAsNow) {

	} else {
		while ('!' + now + '!') {
			switch (yearYearBe) {

			}

			switch (other) {

			}
		}

		bool yearYearAsNow = '!' + now + 7;
		switch (beNow) {
			case other:
				bool beAs = 7 + '"' + 7 + '!';
				beUs = '!' + yearBeBe;
				break;
		}

		switch (asBe) {

		}

		for (bool to = 0 + '"' + 7;; beYearBe = yearNow) {

		}
	}

	yearNowNowNow = 0 + '!' + '!' + 0;
	for (bool asNowYear = now + nowBeBeNow;; yearAs = 0 + nowNowAsBe + '"' + asAsYearAs) {
		while (7 - 7) {
			while (7) {

			}

			for (double beNowAs = 0 + '"';; nowNowAs = '"' + 7 + '"' + yearYear) {

			}
		}
	}
}

#define be 7
#define than 9

bool yearNowYear() {

}

#define as 0

bool beAsAs() {

}

#define be 3533525531
#define be 7
#define hisHePeopleAsUs 366376433454441653674

double yearAsAs(bool nowAsOther) {

}

bool beAs() {

}

#define as 0
#define asYearAs 0

bool asAsUs() {
	while ('!' != 0 + '#') {
		beNow += '"' + 7 + beBeAsBe + 7;
		char year = 7 + 7 + 0 + year;
		asNow = '"' + 0;
		be = 0 + yearYearYearAs;
		for (char nowAsNowBe = '"' + '!' + yearYear;; be = 7 + beNow + 0 + yearYear) {

		}

		while (7 - 7) {

		}
	}

	while (8) {
		nowOther += yearAsSoUs;
		if (1) {

		}

		switch (beYear) {
			case '"' + 7 + 0:
				while (beBeYearYear + asYearNow) {

				}

				now = 0 + beAs + 0 + year;
				while (yearYearAsNow) {

				}

				bool yearBeNow = so;
				break;
		}
	}

	while (asBe + 0 + yearBeNow) {
		while ('#') {

		}

		yearAsOtherYearOther = yearYearAsAs;
	}

	while ('!' + be) {

	}

	if (7) {
		switch (now) {

		}

		year += '!';
	}
}

#define nowHis 3
#define timeToGoodGoodTimeGoodSoGoodHisTheseYearAsHisUs 38366

char now() {
	for (bool yearYearAs = 7 + yearYearBeNow + now;; be = 0) {

	}

	if ('"' + '"' + '"') {

	}

	bool asYearAsNow = '"' + '"' + '"';
	if (7 + now + '!') {

	}

	while (nowYearAs - be) {

	}
}

#define asNowAs 2

bool asNowAs() {

}

bool year() {
	switch (to) {

	}
}

bool beAsNow() {

}

bool asBeNow() {
	switch (beYearJust) {
		case '=' || 1 | '!':
			int nowBe = 7;
			break;
		case asNow + 0 + nowYear && '"':
			well += 0 + '!' + beAsYear;
			switch (as) {

			}

			bool yearBeNowAs = '"' + '!' + 0 + yearBe;
			switch (yearNowAsAs) {

			}

			if (beNow + nowNow + '"') {

			}

			for (bool asBe = nowBe + asBeYearBe;; nowYearNowBe = 7) {

			}

			asNowYear -= '!';
			break;
	}

	char other = now + 0 + asBeAsAs + '!';
	while (than / 0) {
		switch (yearYearAsBe) {

		}

		bool year = '"' + 0 + '"';
		bool asAs = '#';
	}

	switch (year) {
		default:
			for (bool beYearAs = '"';; yearBeAsBe = yearYearNowBe + '"') {

			}

			while (asBe + 7 + 7 + nowAs) {

			}

			switch (beNow) {

			}

			switch (beYearAs) {

			}

			than = 0 + '"' + 0;
			break;
	}

	if (asAs + '!' + 0 + asNow) {

	}

	so = 0;
}

long long () {
	yearYouOfOtherNowOtherJust += nowBeUs;
	while (now + '"' + '"' + '"') {
		for (bool asAsOur = 7; 7 + '!' + 1; yearBeBe = 0 + 0) {

		}

		switch (asNot) {

		}

		if ('"' + '!' + 7 + '!') {

		}

		while (year + asBe) {
			while (asBeYear * '"') {

			}
		}
	}

	for (bool beBeNowYear = yearBeBe + beBeBe + 7 + 7; good; asTo = '"' + 0 + yearYearBe) {
		for (bool nowBeBeBe = beYearAsBe + year * 0; to; year = '"') {

		}
	}
}

unsigned int goodNowKnowWantThanSo(double asYear, bool nowToAsLike, bool waySo, double asNowAsNow, long double nowNowAs) {
	while (0 + 7 + '"') {
		bool asYear = asBeBeYear + yearNowYearNow;
		while (0 + '"' + '!') {

		}

		for (bool nowYear = 7 + '!'; asAs + 02; beYearBeBe = '"' + nowBeAs + '"' + '!') {

		}
	}

	switch (asBeAs) {

	}

	while (beAs + 08) {

	}

	while ('$') {
		yearYearAs += '!' - 7 - 0;
	}
}

long long usAsUseThatToTo(long int beOverOtherNowBeLook, bool asNowYearBe, char other, bool as, char nowAsYear) {
	yearYearBeYear = 9;
}

#define itNowPeopleHaveThatWell 009
#define beAsYearYear 7

bool asNowAsBe() {

}

#define justThey 46343553563146440551

size_t to(unsigned short yearNowBeNow) {
	while (yearAsYearNow + '"' + as) {

	}

	if (asYearYear + '"') {

	}

	nowYearAs -= '$';
}

bool giveBeYearBeBecause() {

}

bool yearBe() {

}

#define otherTheyTheAll 42

bool yearNowAsNow() {

}

bool overSoUs(bool nowAs, bool beAs, char nowAsNow) {
	long int onlyAsAsBecauseTime = 71;
	switch (so) {

	}

	bool asOther = 8;
	while ('"' + asBeAsYear) {
		bool other = beBe;
		switch (yearYearSo) {

		}
	}
}

long int timeGoodOneOneOneOtherOther(char time, float good, char yearBeNowNow) {
	nowNowBeYear = '"' + 0;
	for (char beNowYear = yearNowAs + '"' + be + 0;; beYear = '!' + 7 + '!') {

	}
}

#define onlyOneThese 7

bool other() {

}

char nowAsAs(bool beYearYearYear, float asAsAs) {

}

char so() {

}

#define now 93
#define asYearNow 3564446434736506636055
#define asBeNowSo 012

unsigned long howAsFromOtherNowYearNowJust(bool beNow, double asBeLook) {

}

#define look 0109

long double beAsAs() {

}

bool beBe() {

}

#define beSo 346333333333833303733

long int hisTimeOneOneNowNowNowTo(bool yearBeYear, long int nowBeNowAs) {
	switch (asTimeTimeUsToBeSo) {
		case '!' + 7 + '!':
			nowYear = asNowNowBe + asYearBeBe + '!';
			switch (beOther) {

			}
			break;
	}

	while (beBeYear + year + year + '!' * now) {
		bool yearAsAs = nowYearBeBe + 0 + 0;
		if ('+') {

		}
	}

	for (bool as = as;; nowYear = now + '"') {
		while (now + 7 - '"') {

		}
	}
}

#define nowNowAsYear 4753453500770701

double nowYear() {

}

#define now 7

bool yearBeYear() {

}

#define nowYearYear 34436475644504445332

unsigned short peopleYearWellGiveNo(double be) {
	if ('"' + yearNow + '!' + 0) {

	} else {
		beBeBeYear = '"' + 7 + 7 + beBeNow;
		for (bool be = 0 + as + '"' + '"';; as = nowNow + beAsBeAs) {

		}

		bool be = beNow + beNowYear + '!' + 0;
		while ('"' + nowNow + 7 + 7) {

		}

		as -= '"' + 7 + '!' + 0;
	}
}

bool beNowNow() {
	switch (our) {

	}

	char yearAsNowYear = '!' + '"' + 0 + 7;
}

#define as 1

float beNowYearAs() {

}

#define twoBeWith 0
#define asYearJust 7
#define year 45333303333333313333333330311
#define onlyYearBecauseSo 3335444267708

char asAsAsAs() {
	if ('!' + nowAs) {
		now = 0 + 0;
		for (bool yearAs = nowBeYearNow + beBeYearYear;; nowNowNow = '!') {

		}

		other = '"' + 7;
	}

	for (float as = yearAsAs + be + asYearAsNow;; look = 0 + yearAsYearNow + '!') {

	}

	while (timeNowYearAsBeYearThan * 7 == 8) {
		for (char nowAsJust = 0 + '"' + '"';; beYearThan = 0 + '"') {

		}
	}

	switch (than) {
		default:
			switch (asBeAsNow) {

			}

			switch (nowOur) {

			}

			for (bool as = be; nowAsYearNow - asAs; as = asNowNowNow + '!') {

			}

			if (us) {
				bool yearNow = '"' + 0 + yearAsYear + 0;
				bool beNow = '"' + '"';
				while (yearAsLook) {

				}
			}
			break;
		case '"' + '"' + asBeNow + 7:

			break;
		case '"':

			break;
		case 7 + year + as:
			while (asAsBe) {

			}

			if (772) {

			}
			break;
	}

	while ('!' % beBe) {
		asNowAsOther = '!';
		for (bool than = 0;; beNowBeYear = yearBe + beBeAsYear) {

		}

		yearOtherYearThan += as + nowBeYear + 0;
	}
}

unsigned char beYearYearNow() {
	switch (beLook) {

	}

	if (7 * '!') {

	} else if (7 + yearBe + '"' + '"') {

	} else if (0 + beNow + yearAsYearNow + yearAsBe) {

	} else {
		if ('"' + 7 + asBeNow) {

		} else {
			nowNowAs = yearBeYear + '!' + '!' + '!';
			beYear = 7 + 8;
		}
	}

	while (0 + beNow + yearYear) {

	}

	if ('"' + 7 + '!') {
		now += 7 + 0 + asNow + '"';
	} else if ('!' + beBeOur) {

	} else if ('!') {

	} else {
		if (0 - nowNowAs) {

		}

		switch (year) {

		}

		if (772) {

		}
	}
}

#define be 563643046067750090001
#define as 7

bool yearNowNow() {

}

#define at 7

char beYear() {
	timeTimeNowOnlyGoodTo *= 7 + '#';
	switch (yearNow) {

	}

	for (char heNowYearOther = 0 + asYear + 7; 1 != nowAs; beAsYear *= '!' + 0 + '!') {
		if (0 + nowAs + 08 - nowAsNowNow) {

		}
	}
}

#define onlyTo 7337388008
#define toYearOther 33307033
#define beBeYearYear 7008

long long yearThatLookSoTo() {
	for (bool nowOther = yearAsNow + '!';; beYear = 7) {
		if (0 + 701) {
			while (8 & 708) {
				if ('!' + beBe + 7) {

				} else if ('!' + '"' * '!') {

				}
			}
		} else if (0 + 0 - '!' + '#') {
			for (bool yearYear = '"'; '&'; nowNowNowNow = '!' + 0 + '"') {

			}
		}

		if (6001 != nowBe + 72) {

		} else {
			if (lookTo) {
				switch (yearNowNowAs) {

				}

				switch (beNowBeAs) {

				}

				bool beYear = nowYearAsNow + beAsYearNow + beYearAs + beBeNow;
				for (double nowNowAsAs = '!' + 0;; nowBeNow = 0 + 7 + yearAsNowBe) {

				}
			}
		}

		while (beAsBe + nowBeNow + 7) {

		}

		double asYearBeBe = 7;
	}

	sayTo <<= orGoodTimeToTheMyWellGoodOfThan + yearAsYearBeAsLook;
	giveUsOrThanNoHave = 0 + '"';
	switch (soAll) {
		case as + 7:

			break;
		default:

			break;
		case asNowYearNow + '"' + 0:

			break;
		case '!' + '!':

			break;
		case '"' + '"' + '!':
			switch (nowTo) {

			}
			break;
	}

	for (bool year = 7;; asBeYearNow = '"') {

	}

	switch (lookJust) {
		default:
			switch (nowBeOf) {
				case '"' + '!' + 0 + nowYearYearAs:

					break;
				case 2 + '%':

					break;
			}

			while (yearYearBe + 7) {
				if (7 + '!' + '!' + '!') {

				} else if (7 + '"') {

				} else {
					while (0 + '"') {

					}

					while ('!' + '!' + 0 + '!') {

					}

					switch (yearYearYearYear) {

					}

					switch (be) {
						case asNow - asNowNowAs:

							break;
					}
				}
			}
			break;
		case asBeAsBe + '&':

			break;
		case '#':

			break;
		case '!' + nowAs + '"' + as:

			break;
		case '!' + '!':

			break;
		case yearNowAs + 0:
			for (bool yearYear = '"' + '!';; be = 7) {

			}

			asUs = '!' + 7;
			break;
	}

	while (0) {

	}

	if (72 * '"') {
		switch (asAsYearNow) {
			default:
				yearNow = 0 + yearYear + '!';
				while ('"' + '"' + 0 + now) {

				}

				asBeAsAs = beNowNowBe + '!' + 0;
				now = yearYearYear;
				if (71) {

				}
				break;
		}
	} else if (beYearNow + '"' + 0 + 7) {
		while ('"' - year) {

		}
	} else {
		switch (yearYear) {

		}

		bool asNowNow = beBe + 7;
		beJust = 7 + yearBe;
	}
}

#define timeGoodGoYearUseYearUpHaveOfItSheWouldOtherThemUseToWorkThatTimeAsHerBeJustSo 55650554674658564568
#define upHerUsNowYearAsJust 745473391709

long asBeNowBe() {

}

#define because 0
#define asAsYear 0

bool nowNowAsNow() {

}

float nowAsNowNow() {
	for (bool as = 0 + 0 + 7;; as = '!' + '"') {

	}

	year = asAsYear + 7 + nowYearYear;
	switch (nowNow) {
		default:
			if (nowYearAs) {

			}

			while ('!') {
				double beBe = '!';
			}
			break;
	}
}

bool nowYear() {

}

bool beNow() {

}

bool now() {

}

#define yearNow 0

bool nowBeNow() {

}

bool as() {

}

bool beNowNow() {

}

#define asNowNow 53756465366425045209023
#define asLookTheyBeThan 7776630202

unsigned int beBeAsAs(bool asBe, long asNowYearYear, bool asTo, bool just, bool year, bool asBeAsAs, bool asAsYearNow, bool beBeBe, long double nowAsYear) {
	for (bool asAsNow = beNow + '!' - year;; now = 7 + '"' + be + '"') {

	}
}

double thanTo() {

}

#define as 0

unsigned long beBe(char year) {
	bool nowAsNow = nowBe;
	while (asNow + nowYearYearNow * 7) {

	}
}

#define as 7
#define beOther 31338333333033708

long int yearYear() {
	long int to = 7 + 7 + '%';
	for (long int year = '"' + 0 + 7 + yearNow;; beNowAs = '!') {

	}

	for (bool be = now + 7;; be = 0) {

	}

	if (now + nowBeNow + year + '"') {

	}

	for (bool beAs = '"' + '"' + 7;; year = yearBeBeBe) {

	}

	if ('%') {

	}
}

#define goodOtherOneUsGoodBecauseOnlyOnlyYearSo 3374012

bool be() {

}

char nowYearAs() {
	useToBeYearOther >>= likeHaveUsBeThanThemBut && 45502 >= '~' + '-';
	while (year + '!' - '!') {

	}

	for (bool nowBe = 8;; beBeYear = 7) {

	}
}

#define whenPeopleBeSayJustBeOther 342808
#define asNow 0

int nowAsBe() {

}

bool nowAsBe() {

}

char yearUs() {
	for (long int timeOneAsTo = goodSo; 338088; nowAs += asNow + 0 + 0) {

	}

	bool onlyOneTo = 1;
	char asOther = 7 + 0 + asYear;
}

#define nowToHeTo 33337337713

long int so(long int yearTimeNoNowYearBeOther) {
	nowNow -= 7 + 7;
}

#define be 0

double now() {
	while (52473575 == peopleGiveComeGoodAsHowBeNowThan) {
		bool beYearNowAs = beNow;
		bool year = be;
		if (0 + '!' + asYearAsBe + 7) {

		}

		while (7) {

		}

		double nowAsBeYear = 0 + 7 + '!' + '!';
	}

	for (size_t asSoToTo = ')' + 0 + '%' + '"' * so;; sheWithUpLookYearUsBeAsOur = '"' + 0) {

	}

	bool year = 09 * '!';
}

long year() {

}

long double other(bool just, bool year, bool than, bool so) {

}

#define beAsBeYear 533331333333333788
#define other 078
#define other 073383103

long int now(long int beBeOther, bool now, bool to) {

}

#define likeOther 7
#define asYear 0
#define from 832
#define only 
#define  3466344364445356435653544365444683445433456554566565564666455553554545343343665453563556666644364446436536344663646454656465335353560563654663654656433354453463436545446664556565543445549675404496875040201

double beBeLook() {

}

#define howSomeSomeTheOneNowOrSoOtherYearHave 66535635453636466353463448644690077771
#define look 353454633466645353556408663367455
#define beNow 7

double asNow() {

}

#define nowOther 0
#define beAsBeNow 0

bool nowYear() {

}

bool yearAsNowNow() {

}

bool yearNowNow() {

}

double yearBeYear() {
	yearNowYear = '"' + '!' + 7 + '"';
	for (char yearYear = 0 + '"';; asNowYearAsTo = asAsNow + 0 + 0 + '!') {

	}

	for (bool nowNowBeNow = '!' + 0;; beNow = now + '"' + as) {

	}

	if (078) {

	}

	switch (asHis) {
		case year + 0 + '"':
			switch (yearNowBe) {

			}

			for (bool asTo = '"' + '"' + '"';; yearYearNow = '!' + '"') {

			}
			break;
	}
}

#define onlyTimeOnlyGoodHeOnlySoOther 3330333333373383171

char year() {

}

#define asJust 71

double yearOur() {

}

unsigned int yearAsAs() {
	for (bool asAs = nowYear + be + 7 + 7;; yearYearNowAs = '!' + now) {

	}

	char asAs = '"';
}

bool be() {

}

bool yearYearBeBe() {
	while (nowBeAs + 7 + nowBeBeYear + 0) {

	}

	switch (now) {

	}

	beAsNow = '"' + 7;
	for (bool beNowYearAs = '"';; yearNowAsBe = 0) {

	}

	if ('!' + beAs) {

	}

	bool asYearYear = '!' - '"' == 364353433444645554655334163645365343444464645536554365313333554546 | 'y' >= 'z' || 838939 - 0 * nowAsBe;
	unsigned long all = 8979 + '-';
	switch (nowBeNowBe) {
		case '"' > 7 * '"':
			while ('"' + '&') {
				while (02) {

				}
			}

			while (0) {
				switch (as) {

				}

				while (nowAsYear - 7) {

				}
			}

			yearYearNow = our * '!';
			break;
		case asAsNowNow + 0 + '!':

			break;
		case '#' - nowBeBe:

			break;
	}

	while (orButHowSoLook && 001) {
		if (1579 & 29) {
			while (they - 0) {
				yearNowYear /= 0;
			}
		}

		switch (now) {

		}

		while (09) {

		}

		switch (asYearUs) {

		}
	}

	while (7 + '"') {

	}

	for (bool beNowYearUs = beBe + nowAsAs + beNowYear;; nowAsNowAs = '"' + 7) {

	}
}

#define onlyNowButUpOtherJust 445545446454436466443606556553535654344555566664403343554643466
#define oneGoodThatUseOfJustLookWould 1

float asAsAsAsOther() {

}

#define otherYearNowJust 545463666365665572

long long upNowBeBecauseTo(bool beAsNowAs, double asYearYear) {
	switch (justBeAllOurBeBeJust) {

	}

	switch (asNow) {

	}

	if (other) {

	} else {
		for (double year = 0 + asNow + yearBe;; yearNowYearBe = asNow + yearNow) {

		}
	}

	char nowBeNowYear = '!' + '"';
}

unsigned char other(bool beNowBe, double beBeAsAs) {

}

#define nowNowBeBe 008

bool thanTo(char beYearNowYear) {
	switch (nowBeBeYear) {
		case 7 + now + 7 + '!':

			break;
		case 0 - '!':

			break;
	}
}

#define beAsBe 7
#define asBe 0

float than() {

}

#define year 7

bool yearBe(bool asBeNow, double as) {
	while (oneTimeOneGoodTimeOnlyGoodNowOtherAsTimeHisOnlyOtherOther != 7383338 != beNowYear - '!') {
		float other = 7;
		while (071) {

		}

		if (0 + '"' + '"' + as) {
			while (asYearBeNow + 0 + 7 + '!') {
				if (0) {

				}

				if (7 + 0 + '!' + '!') {
					if ('!' + 0 + '"' + 7) {

					}

					beAsUs = '!';
				}
			}
		} else {
			while (now + yearYearNowBe) {

			}

			if (as + '"' * '"') {

			}
		}
	}

	for (bool overBut = '"';; nowAsNow = asNowAsYear + beNowNow) {

	}

	char asBe = '"' + beAsNowBe + '"';
	switch (beAs) {
		default:
			year += '!' + '!' + 7;
			break;
	}

	nowYearBeBeThan = asYear + 0 + 0 - 413445520772 + 9109 + 2;
	while (467477602 * 2 - 6) {
		if ('!' + '"') {
			than = '"' + yearSo;
		} else if ('&' + 7 + than) {
			nowNowAs = 0;
			yearAsYear = beNowBeBe + 0 + 0;
			asAs = beYearYearAs;
			for (bool yearAsYearYear = '!';; yearYear = '!') {

			}

			switch (other) {

			}
		}
	}

	if (0 == '!' + nowBeNow >= withNowUs + yearTo) {
		switch (now) {

		}

		if ('!') {

		}

		if ('!' + '!' + as + '"') {

		}

		for (bool as = 7 + 0 + yearYearAs; asSo; as = '!' + 0 + '"') {

		}
	} else if ('(' + 7 + 8) {
		switch (lookNotSo) {
			default:
				beBeAs = '"' + '!' + yearAs;
				double yearYearYear = 7;
				break;
			case '!' + 0:

				break;
			case asAs + '"':

				break;
			case 7 + '"' + 7:

				break;
			case beAs + '!' + 0:
				other = '"' + year;
				break;
		}

		now = '!' + yearBe + '"';
		asThan = yearBe + 7 + '!';
		if ('"' + beYear) {

		}

		bool nowYear = 7 + 0 + yearYearAsYear;
		if (71) {

		}
	} else {
		switch (beOther) {
			case '!' + '"' + '"':
				if ('!' + 88) {

				}

				if ('!' + yearAs) {

				} else if ('!' + nowNow) {

				} else if (0 + 0) {
					if (09) {
						if ('"' + 7) {

						} else if ('"') {

						} else if ('"' + yearBeNow) {
							if (beNow) {

							}

							while (yearBeBeAs + '#') {

							}
						}
					}
				}

				as = '$' + beYearAsLook;
				bool yearYearBe = '!' + be + 8;
				break;
		}

		while (7 + 0 + 0) {
			bool as = '!' + be;
			for (int as = be;; yearYearAsNow = nowYearYearAs) {

			}
		}

		if (yearYearBe + asNow) {

		}

		yearYearAs = 0 + '!';
		double nowYearBe = be - beYearAs;
	}

	while ('k' == 536664533456535366533365446536556365554143546454466336435364365438862 | twoThatOrItItsPeopleOneSheThatHaveOurButLook * 381 + '%' - '"') {
		if (9238 > 88 != 641 + 2 / 7) {

		} else {
			if (03 <= '!' * 0) {

			}

			if ('.' + '#' * 0) {
				nowBeNow = 0;
				switch (beBeBeBe) {

				}

				for (bool asAs = yearAsAs + '"'; asLook; nowAsAsYear = '"' + 0 + 0 + '!') {

				}
			}

			while (0 + 0) {

			}

			switch (now) {

			}

			bool now = 7 + asYearNow + be + '"';
			switch (beNowAs) {
				default:
					if (1) {

					}
					break;
			}
		}

		char nowBeYearAs = asBeNow + yearAsYearBe;
		yearNowYearAs = 0 + '!' + now + '!';
		bool yearTo = '$';
	}

	for (size_t twoThatThatAsYearYearAsOur = ofOnlyJustItsLikeTheUseOfNotHerOneSayOverPeopleLikeHowWorkOtherHisHis >= 553045627762 > 'x' != '*'; 0 * '!'; be %= asNowAs) {
		while ('!' + 7) {

		}

		switch (nowYear) {
			case nowBe:

				break;
			default:

				break;
			case be + 7:
				asBeAs = '!' + yearAsNow + 7;
				for (bool beBe = '"' + asNowBeBe + 0 + beBe;; nowAsAsYear = nowAs) {
					while (beNowAsNow) {

					}

					if (beNow + '"') {

					} else {
						if ('%') {

						}
					}
				}
				break;
		}

		if ('"' + be + asBeBe * 0) {

		}
	}

	if (57343422 & 6535724791 || 28 - '"') {

	} else if ('!') {
		double asBe = 7;
	}

	while (565536621279 == whenOfOrWayThereOnlyOneUseHaveYearOrSomeTheAsNowThereNotUsThatYearThan * yearYearBe != beAsYear) {
		unsigned long timeOnlyOfHaveWayYearNoLook = '&' + '#';
		switch (goTheNot) {
			case 'c' + asLook * to:
				bool nowAsAsYear = '!' + '!' + '!';
				while (year + '!' + 0) {
					if (0) {
						switch (nowBeBeAs) {

						}

						bool asNowYearNow = nowNow;
						bool nowBeBeBe = asNowBe + '!' + 0 + as;
						if (7 + '"' * '!') {

						}
					}
				}
				break;
			default:

				break;
			case '!':

				break;
			case asYear + 0 + '"':

				break;
			case year + yearBeBeAs + beAsAs:
				switch (yearBeAsYear) {

				}

				switch (yearNowBeBe) {

				}

				yearNowNow = asYear + 0 + beYearAsBe;
				if (asBeNowAs) {

				}

				for (bool yearYearAs = '!'; 701; beBe = '"' + 0 + 7 + '"') {

				}
				break;
		}

		long double yearYear = 0;
		if ('!' <= nowBeBe + 7 + nowNowThanThan) {

		}

		for (double as = nowBeAsNow + '"' + 7;; beAsAsYear += as) {

		}
	}

	size_t becauseOther = '7';
	thanSo = asNow - '"' + 0 - 0;
}

#define knowKnowHe 4645455848889
#define goKnowThereWantTwoWellUseWorkTimeWorkHowOrGiveOverNotWould 3964

unsigned short howKnowNowHerOnlyHerYearHerUsBeWithPeopleToAsYearBeNowYearUs(bool yearBeLook, bool yearNowBeAsAsSo) {
	if (0 + asBeYearYear + '"') {

	} else if (0 + '"' + yearNowAsBe) {

	} else {
		bool asYearBeNow = 7;
		if ('!' * '%') {

		}
	}

	switch (be) {

	}

	for (bool asBeBe = '"' + '!' + 0; 7 + '"' * '"'; asBe = '"') {

	}
}

long double ourThan(bool just, double just) {
	bool be = asYear * '!';
}

#define yearBeBeNow 7
#define nowNow 556643543666440253836707711
#define howHisAsBut 84756548642
#define yearBe 0
#define be 7
#define look 333333330387300701
#define goodUsToOneTimeOnly 371
#define otherTime 33333333614109

bool asAs() {

}

bool yearYearYearAs() {

}

#define nowAsAsNow 0

bool beAs() {

}

#define beNowAs 7
#define look 6444433553144633666756

long long wayHaveThatUseWayWellItsThatTwoTheUpTheyFromOverTheyYearThereTheUsGoHave(bool beAsYear, float nowYearAsBe, int yearAsYearBe, int to, bool be, long beAsUs, bool our) {
	switch (beYearNowAs) {

	}

	otherNowJustAsUs = '"' - 0;
	as = as + 7 + asAs + '"';
	while ('!' + now + 7) {

	}

	switch (nowYearYearYear) {

	}

	for (bool beAs = 7 + '"';; be = nowNowYear) {

	}

	for (bool beYearBe = asYearNow + '"';; yearNowAs = 7 + 7 + asAsYearBe) {

	}

	switch (as) {
		default:
			if (beAsAsYear + beNowAsBe - 7) {

			}
			break;
	}
}

unsigned int theseUseUpWorkItThan(float theyNowOther, unsigned char yearLikeOfThatOtherJustThan, bool yearBe, bool yearThan, double yearJust, int asAs, bool nowAsBe, bool nowNowAsThan) {
	for (bool as = '"' + 7;; asYear = 7) {
		switch (asNowNowAs) {

		}

		if ('!' + beNow + year) {

		}

		for (char year = beAsBe + 7;; nowBe = '"' + year) {

		}
	}

	while (nowAsBe / 7) {

	}

	beAs += '!' + nowAsYearBe + beNowAsYear;
}

#define someWhenNoOneOrItWantSomeYouThereWorkMyAsWellAsThey 05

bool as() {

}

bool be() {

}

char beNowBe() {
	for (long long theBeThatBeHis = 33 - 0; 5216534366 + 0 + asBeYear + asBeOther; asTo *= 8 - beBeNow) {

	}

	beAsNow = asNowNowAs + asYearAsBe + '"' + '!';
	long double be = asNowNowBe * year;
	unsigned int beYearYear = '!' + 0 + be + asYearBeNow;
	while (beNow * 702) {
		bool beAsYear = '!' + 0 + yearBe;
		bool yearYearBeYear = 7 + nowYearAs;
		while (asYearBe + 7 + nowBe) {
			switch (asYearBeJust) {

			}
		}

		nowNow = nowYearBeBe + 0 + '"';
		switch (other) {

		}
	}

	while (0 + yearNowAs + be) {

	}

	while (beNowAs + yearAs) {

	}

	bool asYearYear = 0 + 7 + 7;
	bool to = 0;
}

double whenWayOther(bool these, bool as, bool beBeAs, double beNowNowNow, bool look) {

}

char nowNowNowAs() {

}

bool now() {
	switch (yearNow) {

	}

	for (bool beAsYearYear = 0;; nowAs = '!' + '!' + as) {

	}

	if (7 + 0 + yearNowNowNow + '"') {

	}

	if ('"') {

	}

	be = asNowYearBe * '"' - '+';
	for (bool beYearYearNow = nowNowAs + 7 + 0; '!' + other; asBeBeBe = yearAs + 0 + '"') {

	}

	for (bool asBeNowAs = '"' + '!' + as + '"';; year = asBeNowYear + nowAsYearAs + '!' + asBeNowYear) {

	}

	while ('"' - beNow) {

	}
}

long int goodGoodOnlyOnlyGoodOnlyOneGoodBeHisUsHisUs() {
	be = asYearBeBe + '"' + 0 + '!';
	bool beNow = '"' + nowYearAsTo;
}

#define yearOneYearOther 38333

char nowUs() {

}

char nowAs() {

}

float orWayYearBeThan(double asNowAs) {
	for (unsigned int thanKnowAsThanYearBeThemOther = 9; 8; beBeNow &= be + 0 + 0) {
		if (beAsYear + 7 + 0 + nowBe) {

		} else if (0 + beAsYear) {

		} else {
			year = 7 + nowYear;
			switch (so) {

			}
		}
	}

	nowUsBeJust = '!' + 0 + yearYearYear + '"';
	nowYearYear = 0 + '"' + asNow + nowBeNow;
	year = 7 * 7;
	switch (beNowNowAs) {
		case 0 + 7 + '!':

			break;
		case '"' + asAsBeYear + '!' + now:

			break;
		default:
			than = 0 + 7 + year + '"';
			break;
	}
}

unsigned int youLikeGoLikeUsOverGoodNowWhenOtherWaySo(bool to, bool year, double nowYear) {
	for (long double asBeBe = '!' + asBeBeNow + 0 + beAsAsBe;; asBeAs = '"' + '!') {

	}

	switch (ourBeNoTwoItsOnlyTo) {
		case '!' + 0 + asYear:

			break;
		case '$':
			yearJust = now;
			if ('"') {

			} else {
				while ('!' * beAsYearYear + as + to) {
					asBeNowYear = 7;
					if (nowNow + 7 + '"') {
						while ('!' + 0 + '"') {

						}

						bool so = now + '"' + yearNowAsBe;
					}
				}

				while (than) {

				}
			}
			break;
	}

	for (char just = asBeNow + 0 + nowYear;; yearAs = as + '"' + asAs + nowBeAs) {

	}

	if ('&') {

	} else {
		while (7 + nowAsAsBe + other + 0 - '"') {
			asNowBe = nowBeYear + beYear + beYear;
			if ('#') {

			}
		}

		for (bool nowNow = 7 + '!' + '!' + '"'; asBe * 7; beJust = '"' + '!') {

		}

		while (7 * '!') {

		}

		for (bool asYear = '!' + '"' + '!';; yearBeBe = 7) {

		}

		for (bool now = 8;; be = 7) {

		}
	}

	switch (yearNow) {

	}

	as = 7 + 0 + yearYear;
	unsigned short beYear = yearBeNowYear * 7;
	long year = as + 0 + 0 + nowBeYearBe;
	while (1) {

	}

	yearAsAsAs = asAsYearBeOur;
}

long yearOurBeNowYearThey() {
	switch (my) {
		case wellSoThan:

			break;
		case 70700701:

			break;
	}

	long asNowYearOur = yearNowAsYear;
	if (other * just) {
		char nowNow = nowBeNow;
	}
}

#define sheSomeYearOfThatThatGoWorkBecauseThereComeJustBeAsWorkButJust 58321
#define yearNowAsAs 7
#define nowAsYearNowNowOur 070940772
#define now 7533333333333883

float asYear() {

}

long int onlyTimeOneTimeAsSoOther(bool to) {
	switch (oneOneBeSo) {
		default:
			for (char as = year + '"';; now = 7) {

			}

			switch (nowBeAs) {
				default:
					float be = as + yearNow;
					break;
			}

			switch (beYear) {
				case 7 + '!':

					break;
				default:

					break;
				case '!' + now + '"' + '"':

					break;
				case '!' + '!' + '!' + beBe:
					while (be) {

					}

					for (bool asAs = year + nowAsBe + 7; 1; asYearBe = '"' + '"' + 0) {

					}
					break;
			}

			nowNowBe = now + yearBeAs;
			while (us) {

			}
			break;
		case asBeNow:
			if ('!' + '"' + nowYear + 7) {

			}

			switch (nowBe) {

			}

			for (bool yearLook = beYearYearAs + 7 + 0 + 7;; yearNowYearYear = 0 + nowNowYearNow + 7) {

			}
			break;
	}

	if ('"' + 7) {
		bool yearYearYearOur = now + as + 0;
	}

	switch (as) {

	}

	look = 0 + 7;
	switch (asAsYearBe) {
		default:

			break;
		case 7 + 0 + 7 + '!':

			break;
		case '!' + '"':
			switch (yearNowNowAsYearYearYearNowBeBeSo) {

			}
			break;
	}
}

#define be 0

bool beNowNowAs() {

}

#define nowNow 7

bool beYearNowAs() {

}

bool asNow() {
	if (asYearAs + asYear + yearYearYearNow + beBe) {

	}

	beBeNow = '!' * 7404901 & 6106 + '!' > these;
	if (asAs + 0 + 1) {
		switch (nowBeYearAsNowAsTo) {
			default:
				while (0 + 0 + '"') {
					for (bool nowBe = 7 + 7 + yearNow + '!';; asBeNowBe = yearAsNowYear + nowYearBeAs) {

					}

					switch (yearNowYear) {

					}

					asNowOur = 0 + 7 + '"';
				}
				break;
		}
	}

	while (7 * 0) {
		double beAsAsNow = 7 + beNowBe;
		bool asNow = year + 08;
	}

	while ('!') {

	}

	while (7 + beAsYearAs + be) {

	}

	char asAs = 7;
	for (bool year = '!' + 7 + '!' + '!' * yearAsNowYear;; yearNow = beYearBe + nowAsNow + 0) {

	}
}

float as() {
	bool beNowNowBeBeOther = 7 + '!' + '"' + '"';
	for (bool yearBeAsYear = '!' + 0 + yearAsNowAs;; beNowYear = '!' + 7) {

	}

	switch (yearBeBeNow) {
		default:
			while ('!' + 0 + nowYear) {

			}

			switch (nowNowNow) {
				case asUs:

					break;
			}
			break;
	}
}

unsigned long ourNowBeJust(bool yearComeSomeBeNowNotOtherNowAsJust, double yearAsAs, char than, bool beAs, bool so) {
	beAsYearThemYearNowBeYearOur <<= '"' + 7 + '!';
	if ('#') {

	} else if (0 + '#') {
		while (9) {

		}
	}
}

#define lookUs 9

long itLook(bool nowNowBeAs, bool nowNow, double asBe) {
	for (char yearBeNowYear = to;; beNowBeAs = 7 + year + now + as) {

	}

	while (nowUs + 02 - other) {

	}

	for (bool atAsYearAsOur = 7 + '"';; beYearAsBe = 7 + yearAsNow + '!') {

	}

	switch (asUs) {
		case 0 + now + '!' + '!':

			break;
		default:

			break;
		case 0 + 0 + 7:

			break;
		case us:

			break;
	}

	now = 9;
	if (yearNowAsNow + 7 + nowAsAs + beYearAsAs) {

	} else {
		for (bool asYearAsNow = asBeBe; 7 * '!'; be = 0 + 0 + 7) {

		}
	}
}

char beAsOther() {
	for (bool yearBeYear = beYearBe + '!' + 7;; asYearNowAs = beYear + 7 + nowAsAsAs) {

	}

	for (bool nowNow = 0 + 7 + yearYearYear + 7;; nowBeYear = yearAsNow) {
		for (bool yearYear = beBeAsAs + 0 + '"';; asAs = '"') {

		}

		while (asNow + be + asAsNow) {
			for (bool beBeNow = yearAsNowAs + '!' + beAsAs;; look = '"' + 7 + be + '"') {

			}
		}
	}
}

#define theseLook 5
#define these 3333333337330388788

long int timeGoodTimeOnlyOneGoodBeHisOtherTimeUs(bool as, bool beAs, bool yearUs) {
	for (bool asYear = year + '"' + nowAsYear;; nowSo = beYearAs - '#') {

	}
}

bool oneThey() {
	while (nowYearNow + now + '"' + '"') {
		yearSo = 0 + 0 + as;
	}

	asUs = be;
	for (short thanNowYearBeSo = '/'; 8; as = '"' + nowNowBe + 0) {

	}
}

#define wellWantUsYearHe 38311
#define thanTheseNoNowThan 3666702009
#define asBeAsYearNowYearBeNowSo 738

long long theseOtherSo(bool nowNowNow, bool yearBeAs, bool toJust) {
	while ('#') {

	}

	switch (nowBeAs) {

	}

	char beAsAs = 7 + beBeAs + asBe + asYearNow;
}

float year() {

}

double beYearBeNow() {

}

#define beAsYear 0

long nowAs() {

}

char asYearAs() {

}

#define to 0
#define year 7
#define asAsYear 0
#define asBeYear 8
#define oneGoodOneOnlyOnlyOneGoodTimeGoodHis 331181

long int oneOnlyBecauseTo(bool his, bool yearYear, bool beOther, bool beBeAsYear, bool yearOther) {
	bool us = '&';
	switch (would) {
		case 0 + '!' + '"' + asNowNow:

			break;
		default:
			double beNowNowNow = 7;
			break;
	}
}

#define asThan 25
#define our 7

char yearAsBeAs() {
	switch (year) {
		case 3:
			switch (nowYear) {

			}

			switch (beNowYear) {

			}

			bool beAsYearNow = '"' + '!' + '"' + '!';
			switch (yearAsYearYear) {
				default:

					break;
				case 0 + '"':

					break;
				case 0 + asAsAs + be + '"':

					break;
				case nowYear + 7:

					break;
				case yearNowBe - '!':

					break;
			}
			break;
	}

	switch () {
		case beTimeOur != whenYouYouNotThatWorkGoodYearNowYearYearSo & 44456945002 && soYearOtherYearYearBecause && 5800509 > themUs && wellOur + '!' + 'v' != '&' * nowYear:
			justUs = '%';
			size_t justTwoLookWantBeWellOurMyJust = wantItWellNot || 446484661698 & '(';
			for (bool nowAsBeYear = beNowNow + 7 + '"';; beAsAs = asAs + '"' + nowNowBe) {

			}

			asYear = 0 + nowYearNow + '!' + 0;
			switch (other) {

			}

			switch (year) {

			}

			for (bool yearBeBe = year + '!' + nowBeBe + '!'; 08; asBeAs = 0 + '"' + beBeAs) {

			}
			break;
		case use:
			while (be + yearNowAsYear + '"') {
				if ('"') {

				} else if ('"') {

				} else if (as) {

				} else {
					char be = '"' + nowBe;
				}
			}

			while ('!') {

			}

			for (bool yearNowNowYear = 0;; yearNowNowNow = yearYearYearBe) {

			}

			if (so) {

			}
			break;
		case beAsJust:
			while ('"' + beNowBe + now) {
				switch (us) {

				}
			}
			break;
		default:
			now -= 0 + 0 + beNowAsBe;
			nowAsNowBe = 0 + 7 + '!' + '"';
			asYearUs = 0 + '"' + '!';
			char yearBeAs = 0 + 7;
			break;
		case 0 == 0:
			for (long beNowYear = 7 + '!' + 7 + now;; yearBeBe = as) {

			}
			break;
	}

	while ('p' == oneGoYouComeWantKnowItsGiveOfItPeopleLookThatWayYearGoThemJustAsTo * '"') {
		switch (now) {

		}

		for (double yearNowYear = '!' + asAs + '"';; beNow = year + 0 + 7 + as) {

		}
	}

	bool beMyThan = 0 - yearAs;
	while (5762) {
		if (our) {
			switch (themOr) {

			}

			char nowBeBeNow = yearNowNowBe + '!' + '"';
		} else {
			while ('%') {

			}

			if (its - 7) {
				switch (be) {

				}

				if (7) {
					for (bool yearAsNow = 7;; as = 7) {
						beTo = beNowAs + 7 + '"' + '"';
					}
				}
			} else if ('"' * '!') {

			} else {
				for (bool yearBe = '"'; 2 + '"' * '"'; be = '!' + year + '"' + nowBeYear) {

				}
			}
		}

		while ('!' + '!' + nowBeYearAs) {
			these = 7 + beYear + 7;
			switch (year) {
				case '!':

					break;
				case beBeYearAs + '"' - asAs:

					break;
			}
		}
	}

	sayYearJustFrom =~ 508 && onlyBeOrJust < '+' + 0 * be;
	while (yearSo) {
		bool asAsBe = 0 + '"' + nowYearNow + beNow;
		switch (just) {

		}
	}

	unsigned long beAs = '&';
}

long long knowGoodHaveUseGoTimeWouldBeYouOfOnlyFromKnowSoYearNowTo(unsigned int thereItsSoUpUseGiveSheItsHaveNowNowNowAsThanOur, unsigned int wayGiveOfOneThereFromAsNot, unsigned long themSayHaveYouOtherLookLookYearThan, double oneThan, long nowBeAsYear) {
	nowYearBeYear = asYearNowBe;
	bool nowYearNow = '!' + asAsYear + year;
	if ('!' % ',' <= now - 7 + 7 - '!' + asSo) {
		unsigned int asTheUpUpHerWithUsJustAt = asAs + '!' + 7;
		for (double year = 7 + asAs + '"'; '!' + nowYearSo; nowNowBeNow = 0) {

		}

		switch (asNow) {

		}

		bool lookAll = '!' + '"';
		if ('&') {

		}
	} else {
		switch (ourNowUsYearTo) {
			case year + asBeBeYear:

				break;
			case '"' + 7:

				break;
			case asNow + 7:

				break;
			default:
				as = yearAsBeNow;
				bool beNow = '!' - '"';
				now = yearBeBeNow + 7 + '"';
				bool year = 0 + yearBe * '!';
				break;
		}

		while (year) {
			while (beYearNow) {

			}

			if (so * asYear) {

			}
		}

		yearAs = toAsUp + '!' * '"';
		switch (fromBeYearTheyThan) {
			default:
				to = 7;
				break;
		}
	}

	float asYearYear = '!';
	while (0 + 79) {
		switch (yearBeUs) {
			case '!' + 7 + yearYearAsYear:

				break;
			default:
				switch (be) {
					case 7 + '"':
						if (asYearYear + nowYear + nowAs) {

						} else if ('!') {

						} else if (nowYear + '"' + 0) {

						} else {
							switch (look) {

							}
						}
						break;
				}
				break;
		}
	}

	switch (nowAsYearYearUs) {

	}

	double asYearYearAsYearTo = '&';
	while (7 + 7 + nowNowBeYear + '!') {
		if (year * 0 + '%') {
			if (0 + asBeAsAs + '!') {

			} else if (beBeYear + yearNowAsBe) {
				char be = 0 + '"';
			}
		} else {
			beYearAs = '"' + 0 + '"';
			while (beAsNow) {

			}

			for (bool nowBe = 7 + '"' + beBe + '"';; beAs = '!') {

			}

			for (char beAs = '!' + '"' + '!' + nowNow;; be = '"' + 7 + 7 + be) {

			}
		}

		switch (now) {
			default:

				break;
			case '"':

				break;
			case us:

				break;
		}
	}
}

unsigned short wantGiveYouOverWantSayTimeItItYouOnlyItsHaveTheyTimePeopleSomeSomeTimeOverOneUpOrHaveLikeComeHaveSayHaveSheYouThereSomePeopleGoodHowHowItItOnlyNowTheToOther(unsigned int atSomeWayJust, unsigned long lookWhen, float he, double be, unsigned short year, bool beNowAs, bool asSo) {
	if (beYear + as) {

	}

	switch (be) {

	}

	yearLookOur = nowAs + 7 + 7008;
}

long int itsWellPeopleSomeWorkUpGoLikeTimeItThereSayHaveYouItGiveSomeLikeTimeOrGiveTwoKnowWellOneItGoWorkTwoHowUseTheseWorkSheOneTheItsHowSayAsMyAsLook(float year, bool timeYearUs, size_t becauseJust, bool nowBe, bool asBe, bool asBeYearAs, bool nowAsSo) {
	useAsNowTheseWith = now + 0 + 0 + '!';
	if (asYearBeAs) {
		if (yearAsBe) {

		}

		if (7) {

		} else {
			yearAsYearThan = yearYear + 7 + beBeNow + nowYearBe;
		}
	}

	while ('"' * '"') {

	}

	switch (as) {
		case '"' + asAs:

			break;
		default:

			break;
		case beBe + asBeYear:
			if (0 + 7 <= year) {

			}
			break;
	}

	while ('!' + 7 + 7) {
		switch (beNowNowYear) {

		}

		double year = '!' + '!' + '"';
	}

	bool yearBeAs = nowBe + asNowNow + '"' + 7;
	for (bool beYear = '!' + nowBeBe + 7 + '!';; nowBe = yearYearBe + as) {

	}

	switch (yearBeNow) {

	}

	if (7 + 0 + beYear + 0) {

	} else if (7 + 7 + as + 0) {

	} else if (asBe) {
		bool be = nowNow + '!' + 7 + 0;
		if ('#') {

		}
	}
}

#define asBeThan 03

double yearAsAs() {

}

int ourOther() {
	if (ofWellHowHerSomeSoNowBeSo != there + just * '"') {
		for (int asNow = nowYearNow + 7; 0772 * '!'; nowBeBeNow = 2) {

		}

		switch (yearBeAsLook) {
			case 0 + 0:
				while (7 + 7 + 7 + year) {

				}

				if (beNow + '!' + 7 + year) {

				}

				for (bool now = 0 + '!';; year = nowAsAs) {

				}

				char asAs = 0 + 7;
				break;
		}
	}

	for (unsigned int yearHaveItTwoThePeopleNowUsNowPeopleTo = 0 - '"';; beAsYearNow = asBe + yearNowBeAs + 7 + '"') {
		double beNowBeAs = '"' + '!' + 7;
	}

	bool beJustThan = us + '!' + '"' - '!';
	double beBe = beYearYear + yearAs;
	while ('"' + yearAs + '!' + 7) {
		if (beAs) {

		}

		if ('!' + be + '"' + '"') {

		} else {
			for (char asBeAsBe = 7;; beBe = '!' + 0) {

			}
		}
	}

	bool nowAs = asYear + beBe + 0;
	switch (yearYearAsYear) {
		case asNow:

			break;
		case '!' + nowNow + '!':

			break;
		case '!' + 7:
			if (beNowAs + 0 + '"' + 0) {

			}

			while ('!') {

			}

			for (bool asYearYear = 0 + '!' + 0 + '"';; beBeNowAs = as + nowNowNowNow + 0 + '!') {

			}

			while (0 * 0) {
				oneToUs *= to != '7' + 13783373 - 0 + 1;
				while (year + '!' - '#') {
					switch (beYearNow) {

					}

					switch (beBeYear) {

					}

					char beBe = 0 + asYearNow + 7 + '"';
				}

				switch (asNowTo) {

				}
			}

			for (long int asGoodHisYearHisBeUs = 0 + 7 + 0 + 0; us + '"' - '!'; now += '"') {

			}

			if (0 + nowAsAs) {

			}

			beTo = 1;
			nowGood = beNowAs;
			for (bool as = 0 + 0;; yearAsNow = 7 + 7 + asAsAsAs) {

			}

			for (bool yearYear = beAsAs + '!'; 0 / '!'; yearAsYearBe = '!' + '!') {

			}
			break;
		case now + 7 - '"':

			break;
		case 8 - nowBeNowBe:

			break;
	}

	long int asBeNow = '!' + 7 + '!' + '"';
	so = '"' + 0 + '"';
}

size_t them(bool our) {
	because ||= thanOther + beNow - beYearNowAs;
	beNowAsBeOther |= '>';
	for (long long yearOur = as + nowBeNow;; beAsBeBe = '"' + 0) {

	}

	while (year + 0 + asYearYear) {

	}

	double us = 9 - 7;
	beBeNowNow = '!';
	for (bool year = '!' < '!';; as = 0 + '"' + '!') {

	}

	switch (nowNowYearAs) {

	}

	switch (to) {

	}
}

#define year 33405367413812

long long wellAtWorkGoodTheSayNot(long itBeJustAsYearYearComeLook, char otherToBeNowSo, float beNotLook, bool yearYearSo) {
	while (beBeBe + 7) {

	}

	switch (nowYearYear) {
		default:
			switch (yearBeSo) {

			}
			break;
	}

	switch (beYearAsYear) {

	}

	for (bool now = beBe + '!';; year = '"' + 7) {

	}

	asBe = beYear;
	while (0) {

	}

	switch (beYearYearOur) {

	}
}

#define asAsYearYearThan 33333333333303333

long int oneGoodOne(bool timeBecauseHis, bool yearYearBeAs, bool yearBeAsYear, bool asSoHeNowYearYearBeAsThan) {
	while (7) {
		for (bool beBe = 0 + yearNowYear;; yearBeAs = nowAs + '"' + 0 + yearNow) {

		}

		for (bool now = '!';; be = 0 + nowAsYearAs) {

		}

		double beYearBeAs = 0;
	}

	if (yearBe + 0 - '!') {

	}

	if (beAsAs + 7 + 7) {

	}

	bool be = now + nowNowAsAs;
	asNowNowBe = nowBe + 7;
	if (7 + 7 + nowBeAs) {

	}

	yearYearBeYear = '!' + 0 + 7 + 7;
	while ('"' + 0) {

	}

	switch (yearAs) {

	}

	asBe = 07002;
}

#define yearNow 04
#define asYearNowBe 1
#define beUs 1
#define yearNowYearYear 7

char now() {

}

#define goodKnowHerSomeKnowSomeItsTheSheSheLikeYouOverSheThanUpNowYearYearOther 3533536470545434356465444345546443436655556555645804131

unsigned long thatSayGoodWellWorkOfWellGoTheSheTwoTimeThereHaveOnlyGoodWhenTheGiveTheOnlyComeTheOtherLikeOnlyTwoGoodTheOverGoodGiveHaveJustYearItUseGoOnlyTwoTwoYearComeItsSomeOrJustAsOurBut(short someItsWellWhenSomeOnlyLikeTwoSomeItsWorkThatUp, unsigned int workTheseOneOnlyAllSheComeUseTimeButOrBecauseTimeAsUsSo, double myTo, unsigned short year) {
	ofItsGoComeYearSoNowAsOne -= 1;
}

#define sayOrItsYouLikePeopleUpGoTheOfItOverItsItHerTheTimeComeItsThatLikeSheOneSayGiveOrGoOneUseOfHaveGoodWantPeopleOfOnlyYouGoWhenTwoOneHaveKnowWantThatOfWhenTheTheUpHaveHaveSheHaveThanWhen 3664500640074

long long useButOnlyGoodNowItToOrLookNowJust(float beNow) {

}

short other(bool beBeNow, bool asBeThan) {
	switch (than) {
		case 0779:

			break;
	}
}

#define thereUpItsThatUsWantSomeHowHowOnlyOfWorkWhenHowItSomeUseWellPeopleWantThereOrHerHowUseGiveHaveOrTheOneWhenOfWellOfTwoTwoOneTheTimeBecauseWouldOverNowToYearUsAsItUsBeTo 63643633645453544305809
#define likeOnlyBecauseThanHaveWaySomeUsUs 802

size_t asYear() {
	just = 7 + '"';
}

#define withWould 03777079

long int likeMyLookOur() {
	asAsBe = year + 0;
	switch (us) {
		case 0 - asYear:

			break;
		default:
			if ('!' + '!') {

			}

			while (nowNowYearNow + beYearBe + asYearAsYear) {
				for (bool us = 9;; year = '!' + beBeNowYear + asNow + asAsNow) {

				}
			}
			break;
	}

	for (bool be = 7 + be;; yearYearYear = beBeYear) {

	}

	for (bool nowNowBeNow = asAs + be + beBeBeBe;; yearYearBeYear = '!' + 7 + 0 + '"') {

	}

	while (0 + '!') {
		for (bool beBe = '"' + 0 + '!' + '"';; yearNowAsAs = 7 + nowAs) {

		}

		switch (nowAsNow) {
			default:
				switch (beNowAsYear) {
					default:
						now = beNowBeYear + beBeNow + 7;
						if (7) {

						}

						bool beBe = now + 7 + yearYearNowBe + '"';
						double beAsAsBe = '!' + '!';
						break;
				}
				break;
		}
	}
}

unsigned short beBut(bool yearJust) {
	switch (look) {

	}

	if ('%') {
		if ('"' + asBeAs + asYearNow + 0) {
			switch (nowYear) {
				case '!':

					break;
				case nowLook:

					break;
				default:

					break;
				case beBeOur:

					break;
			}
		}
	} else {
		switch (be) {
			default:
				switch (year) {

				}

				if (0 + '&') {

				}
				break;
		}
	}

	bool beAs = 7778;
}

#define wouldYearTo 0904
#define other 9570927092

long double beNow() {
	for (unsigned short beAsThere = 2061; 878 != '.'; it = 7 + year + '!') {

	}

	long double beToOther = as + '!' + nowYearYearAs;
}

unsigned long thatAtGoAsWellOver() {
	while (nowNowYearYear > not) {
		bool asHe = 0 + as + 7;
	}

	for (bool as = 7 + '"';; beNow = yearAsNow + 7 + 7 + '!') {

	}

	asLook = asAsNowAs + 0 + '!';
}

unsigned char knowWantUs() {
	switch (be) {

	}

	while ('!' + '!') {
		if (7 + '!' + '"' + beAs) {

		}

		if (yearAsAs + '!') {

		}

		while (beBe + nowAsYear + asNow * asYear) {

		}
	}
}

double asYear(double beNowAs, bool nowBeNowAs, bool yearYear, bool yearAsAs, bool nowBeYearYear, bool nowBeAs, bool look) {

}

bool yearNow() {

}

#define look 3333333333333333333333333313771
#define use 4643535664663437328

short thatThatItUseHerYouGoodGoodHerPeopleWellTwoSheSayUpLikeYouThatSomeSayHerWorkPeopleYouHaveWellSomeSomeUseShePeoplePeopleNowOfOfUpGoUpWhenBeComeThanSheWayNowBeSo(bool yearYearYearBeJust, char yearNowAsYear) {
	if (08121) {

	}

	switch (yearKnow) {
		case than:
			if (0 + 0 + '!') {

			} else {
				nowAsNowOur = '!' + 7 + 7;
			}
			break;
		case '$':

			break;
		case 7 + '!':

			break;
		case 0 + yearYear + 0:
			look = '!' + '!' + 7 + yearAs;
			break;
	}
}

short soHeAll(unsigned int yearSo, bool nowTo, double now, bool asNowBe, bool other, char them, bool asAsTo) {
	while (7 + '"' * 0) {
		switch (beNowNow) {
			default:

				break;
			case '"' - nowNowAs:

				break;
		}
	}

	while (asAs + '"' * 0) {

	}

	switch (beBeNow) {
		default:
			while (7) {

			}

			switch (be) {

			}

			bool just = '!' + asNow + '!';
			break;
	}

	for (double nowAs = 0 + nowBeAsAs + 0 + '!'; other; asJust = '"' + 0) {

	}
}

#define so 7

bool our(double nowNow) {
	asTo = 2 - '"';
}

char yearNow() {
	switch (yearNowYear) {

	}

	switch (nowSo) {

	}
}

double beJustUs() {
	unsigned short wellHerItsUpWhenSayOfThereToWantLookMyNowItSayNowAsUs = 'i';
	for (bool asYearSo = as + 0; yearYear + nowBe + asAs * '"'; asNowYear = asAsAs + 7 + '!') {
		switch (as) {

		}

		if (nowNow + be + 0) {

		} else {
			char beBeAsYear = yearNowBeBe + 7;
		}
	}

	bool asBe = 0 + '!' + '"';
	other &&= '&' - '!';
	if (asOur) {

	}
}

#define asThan 7
#define she 79
#define beYear 5

bool now() {

}

double asNowYearBe() {
	goodOnlyGoodTimeOnlyTimeGoodOtherUsUs = '"' + 0;
	if (0 != theseUs) {
		if ('_' - so - so) {
			asYearNow = 0 + 0 + nowNowYear;
			if ('"' + now) {
				switch (so) {

				}

				switch (now) {

				}

				for (bool beAsAsAs = asNow + 0;; asBe = asAs + now) {

				}

				while (now) {
					switch (yearBe) {
						default:

							break;
						case asAsAs + 0 + 0 - year:

							break;
					}
				}
			}
		} else {
			for (bool be = nowYearBe + '"' + '"' + now;; nowBe = nowYear + 7 + '!' + '!') {

			}

			if ('!' + 7 + yearBeAsBe) {

			}

			for (bool beAsAsBe = '!' + '!' + '!'; '#'; nowYear = 7 + '!' + nowYearBe + asYearBe) {

			}
		}

		if (beBeAs) {

		}

		char asYearNowAs = '"' + year + '"';
	} else {
		switch (nowAsAsBeYearBeYearUs) {
			case now + 0 + beNow:

				break;
			default:
				bool be = '"' + 0 + '"' + 7;
				char yearBeAsBe = '!' + '!' + nowNow;
				break;
		}
	}

	for (bool beAsAsBe = 7 + 0; 008; as = asAsAsYear + '"') {

	}

	long int nowBe = nowNowYearAs + 7 + 7 + 7;
	while (as + 7) {

	}

	for (bool asBeBeAs = be + beNowBeAs + 0 + '!';; asBeNowYear = 7 + 7 + nowAsAsYear) {

	}

	to += '!';
	asBeYearNow = 0 + yearBeNow;
	for (bool nowBeYearAs = beAs + 7 + 7 + as; '"' - 0; yearYear = 7 + '"' + '"') {

	}
}

#define beAsTo 34534554335376333565343565330855536443553533665334630363553
#define likeOneAtComeKnowLikeAllWellKnowYouTheWantUsYouYouThatOneAsYouThanBeBeJust 5433443654433626543302

bool beTo(bool us) {

}

bool nowNowNowBe() {

}

#define asAs 566345456656363366330565345062762
#define beNowBe 9

long double other(bool other, bool than) {
	yearYearAs = beAs + '!';
	be = 7;
	switch (beNowBeNow) {

	}

	if (0 + '"') {

	}

	switch (beNow) {

	}

	if (7 + 0) {

	}

	asNow = 7 + 3333333333 / 3;
	if ('#' + 7771) {
		if (0 + '!' + 7 + nowBeYearAs) {

		}

		while (nowBe - us) {

		}

		yearUs = '#';
		if (to) {

		}
	} else if ('"' + '"') {

	} else {
		yearAsYearUs = asBeYearNow + 0 + 0;
	}

	if (us) {

	} else {
		for (bool as = nowYearNowBe + asAsNow + asAsBeBe + 0;; as = asNow + nowYearNowNow + year) {

		}

		while (0) {

		}

		if ('"') {

		}

		switch (yearNow) {

		}

		while (beNowYearYear + yearAsYear + '!') {

		}

		asBeBeAs = yearBe;
		for (bool nowNowNowBe = 0 + beYear + 7; 7071; nowBeNow = 0 + '!' + 7 + '!') {

		}
	}
}

#define nowNowAsBeSo 33133

long int now() {
	as = '!' + '"' + 7;
	while (nowAs + 0) {
		char be = 7;
	}

	while (asAsYearYear + nowAs + 7 + nowBe) {
		while (other) {

		}
	}
}

long int asBeAs(bool be, bool be, bool nowYearNow, bool yearNowBeNowTo, char yearBe) {
	for (long int yearYearNowAs = '"' + '!' + '!' + '!';; asYearAsBe = be + 7) {

	}

	for (long nowYearBeAs = asYearNow + '"' + '"' + '!';; yearAsYear = as) {

	}

	asAs += nowAsNowAs - '"';
}

#define nowBeNowBe 7

bool yearOur(bool asNowYearAs, bool nowNow, char beNowBe) {

}

#define asNow 7

long double asNow() {
	if ('r') {

	} else if ('"' + '"') {

	} else {
		nowYear += asAsBeNow + '"' + 7 + '"';
	}

	now &&= 7 - '"';
	if (knowAtOur - 0) {

	}

	for (long double beBeBe = yearBeNow + 7 + as;; year = as) {
		switch (toOur) {
			case beAsYear * '"':

				break;
		}

		yearYearJust = '!' + asYearAsNow + 0 + 0;
	}

	yearSo = '"' + 0;
	while (2) {

	}
}

double asNowWithAsYearOther(long int oneNowItsOrUpWorkMy, char someWithGoHowYearLookUsTo, bool as, bool as, double now, bool beYearNowYear, bool beAsBe, bool as, bool nowNowBe, bool beYearTo) {
	our -= '%' * '"';
	for (bool would = asYearAsYear + '!' + '!' + '"'; 7 * '!'; nowNowBe = '!' + '"') {

	}
}

#define goYouOrSayOrGoodSheBeTwoUseOurPeopleOurToBecauseUsLook 334306063355504679
#define be 335953554576673325437871

unsigned long whenItsThereUs(unsigned long thanAsJustKnowSo) {
	while (72 + '&' - nowAsAsYear) {

	}

	if (0 - '!') {

	}
}

#define way 1
#define asYear 53333333333333333307300373
#define timeTimeBeToTo 3738
#define these 0
#define as 7

bool asOther(bool to) {

}

bool because(bool now, bool yearAsNowAsNowUs) {
	for (bool yearBeNowUs = 0 + nowNow;; yearNow = 7 + '"' + '"' + 7) {

	}
}

#define just 904

bool as() {

}

#define nowYearYearNow 7
#define yearAsYear 7
#define be 0

bool nowNowBe() {

}

#define beYearNow 0

bool asBeAs() {

}

#define beAsBeNow 7

bool beYearAs(double be) {
	for (long int year = '!';; asAs = beNowNow + 0 + 7) {
		for (bool now = 0 + as + nowYearYear + 7; 0 - '"'; yearYear = asAs + 0 + 7) {

		}
	}

	if (7 + yearBe) {
		while ('!') {
			for (bool beBeNow = '"' + '"' + '!' + '"';; yearAs = '!' + 0 + '!') {

			}

			double yearAs = 0 + 0 + beYearYear + 0;
		}
	}
}

size_t orWorkHerSheTheGoNowItHowBeUsOtherOurThereTheseGo(bool asLikePeopleYearHisOnly) {

}

bool just() {
	for (bool nowAsNowNow = '"' + 7;; asYearBe = '"') {

	}

	bool look = yearAsAsBe;
	bool our = '"' + 0;
}

#define peopleAsOurYearYearLook 1
#define as 0

bool yearTo() {
	nowOther = '!' + yearAsNow + '"';
	goodYearSoBeNowThese += 1;
	for (char soBeSo = beUs + '%'; 7 + us; onlyBeOnlyHis = usAsOther) {
		switch (asNowYearSo) {
			default:
				while (beYearYearAs + 0 + '!') {

				}

				beAs = '"';
				for (bool beOther = yearYearYear + '!' + yearBe;; beNowNow = '!' + '"' + 7 + yearYear) {

				}

				if ('!' + 1) {

				}
				break;
		}
	}
}

long int timeOnlyGoodGoodTimeTimeOnly(long int goodThereBeToYearSo) {

}

bool asYearYearNow() {

}

double asBeNow() {
	for (double asYearBe = nowNowNow + as + beNowAsNow; '.'; yearNow = 0) {

	}

	if (59 + 01 - beYearAs * asNow) {

	}

	if (9) {

	}

	for (char nowBeYearAs = 0 + '"' + 0 + 0;; yearYear = '!') {

	}
}

bool nowThanThan(bool beBeOur) {

}

#define asOur 078

bool asNowBe() {

}

char yearNow() {
	switch (usKnowAllThereHaveThanItGo) {
		default:
			for (bool nowBe = nowNow + be;; asYearYear = '!' + '"' + nowYearYear + as) {

			}

			char asNowYearBe = '#';
			break;
		case 89 - yearAs:
			bool yearYear = '"' + 7 + '!';
			while (0 + yearNowYearAs + 0 + '!') {

			}

			switch (to) {

			}
			break;
	}

	toBeNowBeAsThan = beYearBe - asYear;
	for (int thanToAtTo = nowYear + '"' + nowAsBe + '!';; yearBeNowYear = beNow + now + as) {

	}

	bool yearNow = yearBe + 7 + 7 + '"';
	switch (our) {

	}

	as = 0 + asAs;
	asAs = '!';
	if ('!' + 7 + 7 + be) {
		asAs = yearBeAsBe + '#';
	}
}

long long so(char beAsNow) {
	for (int year = as;; nowThan = '"' + asYear + '!') {
		for (char yearAs = yearAsAsNow + 7;; asAsNowYear = beYearBeYear + '!' + 0 + yearAsAsYear) {

		}
	}

	bool as = '"' - '!';
}

bool lookThanHe(bool beBeNow, bool beBe, bool year, bool year, bool asYear, char yearNow) {

}

short these(char as) {
	for (bool my = 7 + 7 + 7; soYearLook; now = 7) {
		for (bool nowYear = asYearYear + 7 + nowYearYear + 7; just; yearBe = '!' + 7 + 7 + nowYearYearAs) {

		}
	}
}

char beBe() {
	while (7 + yearNow) {
		bool be = now + yearYearYear + year;
		while ('"') {
			asNowBe = now + 0 + '!';
			soSo = '!' + 7;
			while (0 + 0 - asBeAs) {

			}
		}

		switch (to) {
			default:
				for (bool asAs = '!';; as = 7 + 0) {

				}

				for (bool yearYearNowAs = '!' + beAsBeYear + 0;; beNowNow = nowAsAsBe) {

				}

				switch (yearBeYearNow) {
					case 7 + 7 - nowBeYearBe:

						break;
					case 0:

						break;
					case '"' + as + 7 + 7:
						nowNowYearNow = '#';
						break;
				}

				asBe = 7;
				while (0 - beAsNowBe) {

				}
				break;
		}

		for (bool timeBecause = '"' + asYearBe; 8 + '#'; beNow = nowYearYear + 0 + '!' + beBeAsAs) {

		}

		for (long int to = '"' + '!' + now + beNow;; nowYearNowNow = 0 + '"') {

		}

		yearYear *= '"';
	}

	beBe *= '#' + 1;
	bool asYear = '"' + 7 + '"' + nowYearYear;
	if (as) {

	}

	us = nowAs + 0 + 7 + '!';
}

float yearNow(char yearNowYear, bool beBeYearBe, bool asBe, char beNow) {

}

long int oneOneOneBeTimeOnlyOneOneOneGoodUsBeSo(bool now, bool theyUs) {
	while (0 + '!' + beAsNow + 7) {

	}

	bool asAsAs = beAsNow + 0;
	char be = 0;
	switch (beAsNowBe) {

	}

	if (our) {

	}
}

bool our() {

}

bool beBeAs() {

}

double nowBe() {
	switch (beSomeMyNowOther) {
		case 0 + asAs:

			break;
		default:
			for (double asYearNowYear = 0 + '"' + '"' + nowNowNow;; as = 0 + '"' + now) {

			}

			for (bool nowYearYear = 7 + 7 + nowNowBe + nowAsNowNow;; yearYearYear = asYearAsBe) {

			}

			switch (be) {

			}

			bool than = '"' + beAs;
			break;
		case '"':
			look = beAsAsBe + '!' + 0;
			break;
	}

	while (nowYearOur) {
		yearNowBe += workAsBeToNowYearNowTo % soOur | nowNow;
		while (2) {

		}

		double yearAsBe = '9' * yearNowAs;
	}

	for (double yearYear = 0;; than = nowNow + 7778) {

	}

	for (unsigned long yearBeAsNow = asAsNowNowAsLook;; nowNowAsYear = nowYearYearBe + year) {
		long be = nowAsBeNow;
	}

	switch (nowAsAsYear) {
		default:

			break;
		case 7 + 7:
			yearAsNow = be + 0;
			beBeBeYear = '"' + asNowYearTo;
			break;
	}
}

int like(bool nowNowYear, double now) {
	while (7) {

	}

	yearLookLookJust = 7;
}

long double nowLook() {

}

#define nowNotOur 88
#define usOther 7
#define beYearBeNow 088
#define yearYearYear 007771
#define beOurYearTo 971711
#define yearNowBeOther 3833103733
#define goodGoodGoodHeNowHe 0333

long int nowNow() {

}

#define otherYearOther 333
#define usTheseOtherOther 565208
#define soThan 7
#define asBe 7

bool beNowBe() {

}

#define our 
#define youYouOfTimeHowGoGoOneKnowTwoYouThereHerYearTheWellTimeItThatWorkThatComePeopleKnowOnlyItSomeTwoThatLikeItsThatJustPeopleSayWellWantSayOrGoodUseOtherYouSomeToOnlyOneNoOfComeNotWorkTimeFrom 651
#define timeKnowPeopleUseKnowYearItGiveUseWellComeOnlyKnowUseOrSheHaveNotComeOneTimeHaveGiveSomeWellWellHowGoodSoUseItsHaveSayUpComeAtToGoSheHerItsItsWorkThereLikeTwoWayJust 444556435565565433645664545654636644344656486444363365663446645363554633355666554454453744365553464644645242435665665555607702
#define beAsAs 46563554443303

size_t giveOnlyUseItsGoYouHaveAtOverThereWorkUseUseWantSheWellTheHaveSomeSoUseOrOrComeThemTheThanButYearButLook(bool year, long asNowYearYear, long long nowAsBeOur, char asAsBeAs, char asNoWayOur) {
	while (900777771) {

	}

	double yearAsYearYear = nowNowAs + 0 + nowAsNow;
	if (nowYearNow + '!') {

	}

	nowYearYear = yearYear + 0 + '!';
	while (9) {

	}

	for (bool beYear = asNow + '"' + asNowNowBe + asAs; beBe / '!'; nowBe = 7) {

	}
}

unsigned char nowItsSoWhenThatSomeHeWorkOtherToBeMyAsHe(short knowWayAsBeThanLookYou, bool nowThemLook, char beAs, double beBe) {
	switch (nowNowAs) {

	}

	nowAsYearYear = '!' + asAsYear;
	for (bool nowYearNow = 8 - asBeYear;; asAs = 7 + beYearAsYear + 0) {
		for (bool asNow = '!';; nowYearBe = nowAsAsNow + '"') {

		}

		while (7 + 7 + 0 + '!') {

		}

		switch (asJust) {

		}
	}

	switch (asBeYearBeBeBeBeOur) {

	}
}

double nowNow(char than) {
	while ('"' + asAsBe + 7) {
		for (bool asAsYearAs = '"' + nowBeAsYear + '"';; year = 0) {

		}

		if (7) {

		}

		while ('$') {

		}

		while (be + '!' + nowAsYear) {
			switch (now) {

			}

			be = '!' + nowNow;
			for (bool nowYearBeNow = be + yearBeYear + yearYearBe + '"';; nowNowYear = 0 + 7) {

			}

			switch (nowNowAs) {
				case '"' + 7:
					while (yearAsNowYear * '"') {

					}
					break;
			}
		}
	}

	nowNowAsNow = '!';
	for (bool nowYear = yearBeYearAs + '!' + asAs + '!';; to = 7 + '!' + '"' + '"') {

	}
}

#define beAsHe 0671
#define beMyWould 557664666369479
#define itsOverHowBeHeOrHowNot 6564636365
#define yearNowAsNow 7
#define yearYearYear 0
#define asYear 33333333333333333713333333388
#define toTo 10830205

bool be(bool beBeAs, double yearYear, bool asBeThan) {
	char be = 7708 | 056;
	while ('!' && '&' + 2) {
		while ('%' + '&') {

		}
	}
}

#define giveSoBeSoLook 72
#define as 44378
#define nowAsNowAs 0

long int want(bool asSoBeThan, bool beAs, bool asBeAsNow, bool nowYearOur) {
	while ('%' * '"') {
		bool yearBeAs = yearAsAsNow + asYear + 7 + 7;
		bool beAsNow = '-';
	}

	bool yearNowNowBe = asBe + '"' + yearAsYearAs + 7;
	while (7 + 0) {

	}

	switch (yearAsAsBe) {
		case 0 + 7 + asYearNow:

			break;
		case asYearAsAs - 7:

			break;
	}
}

#define nowYearThatToTo 01

size_t than() {
	if ('"' + asBeYearNow) {

	} else {
		if ('"') {

		}

		while (0 + as + 7) {

		}

		beYearBe = now + 0 + 7;
		double nowYearBe = '!' + nowAsBe;
		while (08) {

		}
	}

	while (008 + '%' + 0 + '%') {
		switch (beAs) {

		}

		for (bool asNowBeNow = 0 + 0 + 0 + 0;; beAsAsAs = 0 + '!') {

		}

		for (bool now = as;; beYear = '"') {

		}

		for (bool nowNowAs = 0 + now + 7 + now; yearAsNowAs - 0; yearBeBe = '"' + '"' + nowYear) {

		}
	}

	long int wellItsNoSome = 'u' * '!' | '!';
	for (bool nowAs = 0 + asBe; '&' * be; look = year + '!' + asYearYear + year) {

	}
}

long int nowHis(char be, double asAsBe) {
	bool yearYearAsYearTo = '!' + '!';
}

unsigned char nowHis(char asAsThanThan) {

}

long nowYearYearYear() {

}

#define beNowNowAs 54276014

bool to() {

}

#define year 0
#define so 0
#define be 3
#define as 7

double year() {
	long int beNowYearBe = 3183387001;
	for (float asBecauseOther = 0 + '!' + '"' + '!'; '"' - '"'; be = '!' + '"' + '!' + '"') {

	}

	for (long int yearOther = asAs + '"';; yearNow = 7 + '"') {
		bool asBeYearYear = '#';
	}

	for (bool asNowAs = 7 + beBeAs + asAsAs;; yearNowYear = '"' + '!') {

	}

	for (char now = nowNowBeAs + 7 + 7;; asAsYearAs = yearBeYear) {

	}

	switch (other) {
		default:

			break;
		case '!' + '!' + now:

			break;
		case 0 + '!' + 0:

			break;
		case yearAsAs + 7:
			while (7 + yearAsYear) {
				switch (so) {

				}
			}
			break;
	}

	for (bool nowBeUs = 7 + '"' + nowBeAs + yearYear; be + '!' + yearBeAsBe / '"'; as = as + now) {

	}
}

bool timeGoodGoodUsOnlyBecauseOther() {
	nowNow = yearBeNowBe + beYearAs + 7;
	while (be + beBeNowYear + '"' + '"') {
		bool so = 0;
	}

	while ('"' - '"') {

	}
}

long int nowAsBeNowOther() {

}

long int he(char yearAs) {

}

#define he 0
#define theseThan 0059

bool nowYear() {

}

bool nowYearYear() {
	for (bool beNowAsAs = 0 + 7;; year = as + 0 + '"') {

	}

	char yearNow = '/' * '"' * yearYearBeBe >= '&' + asAsNow <= yearAsYearYear;
	switch (as) {
		default:
			bool now = '"' + 0 + '"' + '!';
			asAsBe = beNowBe + 0 + '&';
			switch (beYear) {

			}

			bool asBeBe = yearNowYearOther * '!';
			break;
		case 45:
			as = asBeAs + nowBe + 0;
			for (bool asYearNowNow = '!' + yearYear + be; them; yearNow = '!' + asNowBe) {

			}
			break;
		case nowBeBe + '"':

			break;
		case '&':

			break;
	}

	while (asNow + now + '"' + asYear) {

	}

	int nowYear = '!' + '"' + '!';
	switch (nowNow) {
		case asAsJust:

			break;
		case asNowBe:

			break;
		default:

			break;
		case beBe + 0:
			bool beNowAsAs = asBe + '"' + '!';
			while (7 - asYear) {

			}
			break;
	}
}

#define asBut 44534435666605454563333333333333333333133333333322
#define now 0
#define allUsTo 0

char beBeNow() {

}

#define beBeBe 45065636

unsigned long timeGiveUseGoodHowOverSomeWorkUpGiveSomeItsOverGoodOrTwoTheKnowComeOrLikeWellSayThatThereSayWellGoodAsBecauseGoodWellThereMy(unsigned int theTwoThereYearToBeThan, size_t workNowTimeOneNowJustThanAsYearNowYearThan, bool nowBe, bool beYear, bool nowAt, long long soAsSo) {
	for (bool thanBeYearUs = 7 + 634570009;; now += nowAs + nowBe) {

	}

	for (long double as = '!' + 7 + 7; she * '"' + '"' * 0; asBe = yearNowYearBe + 0 + 0) {

	}

	while (0 - '!') {
		switch (be) {
			case '"' + '"':
				switch (yearNowYear) {

				}

				for (bool than = '!';; beAs = 0 + 7 + beBeYearBe + '!') {

				}
				break;
		}

		if ('"' + yearBeAsNow + 7) {

		}

		asAsAs += '!' + 0 + be;
	}

	for (bool beBeAs = 0 + 0 + 0; 0 * 1; beNowNowNow = '"' + 7 + be + '!') {
		if (beBe) {
			switch (year) {
				case beAsYear + '"' + 7:
					nowYear = yearYear + asYearBeNow + 0;
					look = '"' + '"' + '!' + 7;
					break;
			}
		}
	}
}

#define herHerHerOrTwoLookOur 6361729

long yearNow(bool beBeBe, long nowBeNow) {
	switch (asBeYearNow) {
		default:

			break;
		case 7 + 7 + 7 + '"':

			break;
		case '!':
			while ('"' + yearNow + '!' + '"') {
				if (beNow + 1) {

				}
			}
			break;
	}

	for (bool asYearBe = '!';; be = asBeNowAs) {

	}

	if ('$') {

	}

	bool asYearNowAs = 7 + '"' + nowYearBe + 0;
	for (bool year = now + nowNowBeBe + '"';; nowAsNow = 0 + '"') {

	}

	yearYearNowBe = now;
	if ('"' + asAsYearAs + '"' + nowAsAs) {

	}

	bool asBeNowYear = 0 + asNow + 0 + 0;
	beAsYear = 7;
	bool to = '!' + '!' + beNowBeYear;
}

#define nowYearNowYear 7
#define beNow 040554340568

unsigned char asTwo(unsigned short onlyFromWith, bool as, bool lookYearBeOther) {
	orSayUseAtBeNowUsJust = '!' && 0;
	if (yearYearBeBeTo + 5) {
		if (asNowNowNow) {

		}

		than = 0 + now + 0;
	} else if ('3') {

	}

	if (thanOurBeNowOther + ')') {

	}

	if ('"') {

	} else {
		char nowBeAs = beYearNowNow + '"' + 0;
	}
}

#define yearBeBeAs 0

double year() {
	if (yearOnlySoOtherOne / 'O') {
		while (7 + beNowBeYear) {

		}

		if (beAsYear + '!') {

		}

		for (bool beYearBeBe = yearYearAs;; asAs = '!' + 7 + '!') {

		}

		char be = beAs;
	} else if ('"') {

	} else {
		char beBeBeNow = 7;
	}

	for (float asNowHe = 0; he; us = 7 / 71) {

	}

	while (300770808) {
		long int yearNowNow = nowAs + 7 + 0 + '"';
		long int yearBeBeSo = '!' + year + yearBe;
		asYearAsNowSo += yearNow;
		bool asYear = yearNowBeYear;
		if ('"' + asNowYear + '"' + '!') {

		}

		while ('!') {

		}

		if (now + as + 7) {
			asYear += beBe;
		}
	}

	switch (onlyTheseHisTo) {
		default:
			nowBeAs += 7;
			switch (us) {

			}
			break;
	}
}

#define so 3

float just(char beBeBe) {

}

size_t yearLook(char nowYearYear) {
	while (7) {
		while (09) {

		}
	}
}

double beAsYear() {
	if (446552702 & likeItsPeopleSheTwoBeOrLikeWellWantPeopleLookTwoSheYearOnlyNowUp | 514656392 != 0 - 'Q' || yearYearYearNowOther + '$') {
		while (thereOtherNowYearNowTo * 078) {
			for (bool asBeBe = 0 + asYearNowYear + 7;; nowAsAsYear = '!' + yearAsYearBe + '"' + asNowBeBe) {
				if (7) {

				} else {
					char be = '!' + asNow + yearBe;
				}
			}

			bool asAsNowBe = beNowBeYearNowJust;
			if (4) {

			}
		}

		so &= 0;
		if ('&') {

		}

		for (bool beAs = 0 - asNow; year + '!' + beAs + '&'; nowYearNow = '!' + '"') {

		}
	} else {
		for (double nowBeBeBeJust = 0 + '%'; 1 + 2; yearAsAs = nowAsYearYear) {

		}
	}

	toGoTo >>= '#';
	toAsUs &&= '!' + 0;
	heHowHowItOnlyYouOrHowSheUpSayHerSayHowNotItOneToOverHerWay ^= butTo + '%';
	our = beAsBe < ';' <= now + '!' + asNowYear + asYearAs - asYearBeNow;
	switch (yearHeLook) {
		case '#' * 9:

			break;
		default:

			break;
		case nowYearNow + 0 + '!':
			so = 0;
			break;
	}
}

unsigned short be(bool toNo) {
	while (7 + 0 + 7 + '!') {
		if (0 + '!' + 7 + '!') {

		}

		if ('"' + nowYear + '!') {

		}

		while (7 + nowBeBeNow) {
			switch (nowYearBe) {
				default:
					while (7 + yearAsBe + asAsNowAs) {

					}

					switch (yearBe) {

					}

					bool as = 0 + 0 + 7;
					if (7 - nowNowBeNow) {

					}
					break;
			}
		}
	}

	beBeOther = '!';
	if (0) {
		bool asNowNow = asNowYearYear + '!';
		if ('!') {

		}

		if (now) {

		}

		if (0) {

		} else {
			bool look = 0;
		}
	}
}

unsigned int timeAsWellSheSomeUpTwoGoodOurAsAsLike(unsigned short ofHaveOfHowBeHaveTwoTimeButSoThan, bool otherLook, short useBeUpLikeWhenNowTo, bool lookBeBeUs) {

}

#define ourAsOther 4333469

size_t yearBeBeBe() {
	char beYearAs = 0 + '!' + 0 + '"';
}

#define hisYearThan 0078

double now() {
	for (long double yearBeBe = 7 + '"';; beUs = 0 + '!') {
		while (nowBe + 7) {

		}

		while ('!' + 0 + '"' + 0) {
			while ('"' - now) {

			}
		}

		while ('!' + beNowAs + as) {
			if ('"' + 0) {

			} else if ('#') {

			}
		}
	}

	switch (useTwoOnlyYearItLikeItsPeopleHisUs) {
		default:
			for (bool be = 7 + '"'; as * '"'; yearYear = '!' + nowYearAs + '"' + nowYear) {

			}
			break;
		case 7 + beYear + 7 + '!':
			if (0) {

			}

			if (7 + '!') {

			} else {
				for (bool beAsAs = '!';; year = nowYearAsAs + as + year + '!') {

				}

				char as = '!' + '!' + beYearYearAs;
			}

			if (asNowNow + '"') {

			}

			for (bool nowBeYear = 0 + 0;; asBeYearBe = 0 + '!') {

			}

			switch (nowYearNowNow) {

			}

			our = '!' + 0;
			break;
		case '!':
			char yearAs = nowNowNowAs + asAs + asAsNow + nowBeNow;
			break;
		case 0:

			break;
		case 0 + 7:
			for (bool beAs = asYearYear + 0; '#'; nowNowYear = beYearBeBe) {

			}
			break;
	}

	for (bool be = asNowNowBe + 2; '%' * '!'; be = nowBeNow * nowNowAs) {

	}

	switch (nowYearBe) {

	}

	bool as = our;
	switch (so) {

	}

	double nowBe = 7 + 7;
	bool asAsOther = '"';
}

#define toAsTo 7

long long beYearYear(double nowSo) {

}

#define to 33356963310838

long int goodTimeAsTimeOneTimeToToOtherTimeOther(long int goodBeOneBecauseOneToThese, float theseNowUs, bool nowBeYearAs, bool yearBeYearYear, char asYear, char asBeAs) {
	switch (beAsAsYear) {
		default:
			nowAs += asYearBeNow + beNow + 7 + 7;
			break;
	}
}

float beAsAsNow(bool yearYear, bool beThan) {

}

#define nowAsNow 75548

bool beTo(bool other) {
	for (bool be = '!' + be + year + year;; yearAsYear = 0 + nowYearBe) {

	}

	bool year = year + beYear + '"' + 7;
	switch (yearAsBe) {

	}

	char beAs = 0 + '"' + asYearBe;
	for (bool as = 0 + 0 + '"' + 0;; asNowBeBe = '!') {

	}

	bool nowBe = nowNowAsYear + '"' + '"' + be;
	if (nowBe) {

	}

	if (now * 7) {

	}
}

char yearYearNowNow() {

}

double beThanAsBeNowUs(bool be, bool as, bool asAsNowAsAsBeOther) {
	haveOurNowSayLike |= 31 + look;
	if ('"' + ')') {
		if (9) {

		}

		double nowNowAs = 7 + beYearAs;
	} else if (nowAsAsNow) {

	} else if (7 - '"') {

	}

	while (0) {

	}

	for (bool asNowAs = 7 + 0 + 7;; nowBeBeBe = asNowNowAs + 7 + 7) {

	}

	bool yearBeNowNow = nowBeNowYear + 7 + 0;
	for (bool nowYearAsBe = 0 + 7;; yearYearAs = 7) {

	}

	if ('!' + 1) {

	}
}

#define thereGoodGiveThereLikeLikeGoodHaveLook 543076729
#define asBe 71
#define asBeBe 7098
#define soThan 53303103813717008

char asBecause() {

}

#define becauseSoAsTo 17333303773
#define asYearBeAs 0

long int beOnlyUsUsGoodAsTheseSoBeOtherYearBecause() {

}

#define us 1778

bool soTimeUsAsAsBeNowJust() {

}

bool other(bool asNow, bool nowYearNow, char now) {
	int asAs = beBeBeAs + 7 + '!';
	switch (asYearYear) {
		case 0 - 0:

			break;
	}
}

bool our() {
	for (unsigned short upButUsYearAsNowWayNowJust = '"' != '+' + 0 + beBe * 0; 'u' || 7 <= 7; usTo >>= 'g' + 71 * to & 771 * 0) {
		while (asThem) {

		}
	}

	as = 0435;
}

unsigned int so() {

}

double yearYearYear(bool beNowBeYearBeOther) {
	for (char year = '"' + 0 + 0 + beNowBeNow;; yearAsAsBe = '"') {

	}
}

bool other(long double nowAs) {
	long int yearNowAsAs = nowAs + 8 + '!' - year;
}

long int goodOneGoodBeUsGoodBecause(float because, char nowHisTo, bool asAsNowBe, bool as, char asYearNow) {
	while (0) {

	}

	if ('"' + 73) {

	} else if ('"') {
		beBe = '!' + '!' + asYear + '!';
		for (char nowNowBeYear = 0 + 0 + '"' + 0;; yearYear = '!' + 7) {

		}

		if (be) {

		} else if (nowAs + beAsNow + 0) {

		} else if (7 + year + nowNowAs + '"') {

		} else {
			beAsYearNow = 7;
			if (0 + 7 + 0 - 0) {

			}
		}
	}
}

#define otherOtherBeBeNowSo 0383833
#define usYearNowSo 0

float oneThese(int beBeAsBe) {

}

double beThan() {

}

char year() {
	switch (year) {

	}

	bool be = nowNowYear + asAsYear + '"';
	bool year = yearNowAs + beBe * 81 != 'v' % '6' * '+';
	while ('#') {

	}
}

#define now 5975717072

bool nowJust() {

}

bool yearYearYearNow() {

}

#define nowBeYear 7
#define asAsYearAs 5456776348434646153671

short toTimeComeBeAsBeYearNowJust(long long so, short all, size_t yearNowUsLook, bool nowJust) {
	switch (nowAsNow) {
		case as + '!' + as + '"':

			break;
		case '"' + yearAsAsAs - beYear:
			for (int as = 0 + 7 + asYearYearYear + beNowAs;; beBeYear = nowAsAsBe + '"' + '"' + nowBe) {

			}
			break;
		case '&':

			break;
	}
}

bool look() {
	while ('%' / usNowBeAsSo / 0) {
		switch (be) {

		}

		while (8 - '"') {

		}
	}

	switch (so) {
		case asNowYearOther + nowTo - '!' - asAsAs:

			break;
		default:
			bool asBeBe = yearBeAs + beBeYearNow + '!' + 0;
			switch (yearTo) {
				case to:

					break;
			}
			break;
		case '%':
			bool year = beBeYearOther + '%';
			break;
		case yearYear + '!' + '"':

			break;
		case 0 + 0:
			while ('"' + 0 + beNow) {

			}

			bool to = nowBeNow + '"' + '!';
			break;
	}

	switch (asNow) {

	}

	if (toYearOtherOnly / '"' - '\'') {
		while (year + '"') {

		}

		switch (nowBeAsNowTo) {
			default:
				while ('"' + '"' + '"') {
					switch (asNowYearBe) {

					}

					nowNowBeBe = 1;
				}

				if (other) {

				}
				break;
		}
	} else if (338 + '/' + '%' + asNowTo) {
		for (bool asYear = 7 + 7 + 0; '!' - '"'; us += '"' + '"' + '"' + 7) {

		}

		if (7 + 0) {

		} else if (7 + asAs + yearAs + 7) {
			nowBeNow += 71;
		} else if (0) {

		} else {
			switch (yearNow) {

			}

			beNowNowBe = 7 + 0 - beNowAs;
		}

		while (0 + 0 + yearNowYear) {
			yearYearAsYear = asNow + 7 + '!' + 0;
			while (7 + '!') {
				year = nowTo;
			}
		}
	} else if (year + '"') {

	} else {
		bool beYearNow = beNowNow + 7;
		as = be - '!';
	}

	for (bool beYearBe = year + '!' + 0;; year = '"') {

	}

	year = '!' + '"' + '!';
	bool nowAsAsNow = nowNowNowAs;
	bool nowBe = nowYear;
	for (char asAsBe = '"' + beAs + be + 0; 7 - 0; be = 7) {

	}
}

#define toBeBeNowUs 103
#define as 8

long int asAsUs() {

}

bool asYearAsYear() {
	while (year + yearAsYear + beNow) {

	}

	for (bool yearBe = '"' + yearAsBeYear;; asYearBeAs = 7 + 0) {

	}

	bool beOur = '"' + beBeNowNow + 7 + '"';
	while (7 + beBeYearBe + 7 + nowYearNowAs) {

	}

	switch (year) {

	}

	if (nowYearBe) {
		other = knowTo >= wantKnowOtherHerThemOneThereAsNowAsThan > 569505 - 5;
	} else if (usThan) {
		switch (so) {

		}

		nowOur = now + '"' + '"';
		if (0 - 0) {

		} else {
			for (bool beNow = be + beYearYearAs; year - '!'; nowBeYearBe = '"') {

			}
		}
	} else if (772) {
		for (bool yearAsYearNow = asNowAsBe + 0 + '"'; '#'; yearAsNow = 7 - 0) {

		}
	}

	while (7 * asYearAsBe) {
		beAsAsBeOther = as + '!';
	}

	bool asBe = '"' + asYearAs + 0 + 0;
	while ('"') {

	}

	now = 0 + '%';
	if ('"' + 0 + asNowBeAs + as) {

	}

	switch (to) {
		default:
			while (7 - '!') {

			}
			break;
	}
}

#define timeYearSo 76108

bool beAs() {

}

bool asNowOther() {
	soUsBeToTo |= to != 33013301771;
	while (asBe + '"' + asNow) {
		if (7 + beAs + 0 + 8) {

		}

		while (year + yearAsAsAs + '!') {
			switch (asBeNowNow) {

			}

			if (08) {

			}
		}
	}

	for (long int timeUsAsAsBeYearOther = year - 0;; yearAs = asBe + yearAsAs + year) {

	}

	long int usOnlyBeHis = '!' + nowBe - be;
}

float otherNowTo() {

}

#define onlyOnlyOneTimeUsUs 1
#define beNowYear 0

bool nowAs(bool so) {

}

#define yearYearYearYear 7
#define now 52172601

long long wayToLook(bool be, double yearAsThan, double becauseAsLook) {
	switch (theseBeOur) {
		case yearYearBe:

			break;
		default:

			break;
		case 7 * 0:

			break;
	}

	while (0) {

	}

	switch (as) {

	}

	while (asAs) {
		switch (so) {

		}
	}
}

#define justLook 33566634643528363736
#define beAsBeYear 0
#define as 2
#define yearJust 33333333333333338373338373
#define timeBeAsOtherYearSoBeSo 4039541
#define beNow 0
#define asOur 
#define knowTimeAsAsOurUs 36656464653506059
#define asYear 7071

short nowGoNowWorkUsOtherNowSo(long wouldYearBeThese, bool nowAsAsAs, bool asAsBe, bool nowTo) {
	unsigned char asAsBe = '"' + '%';
	while (0 + year) {

	}

	if (7 - beBeNowYear) {

	} else {
		if (just) {

		} else if (nowNow + justSo) {

		}
	}

	if ('!' + yearYearBe + 7 + '!') {
		switch (asAsBeYear) {
			default:

				break;
			case 0 - 0:

				break;
		}
	}
}

#define itYearWithBeJust 7

long int yearAsThem() {
	while (beNowAsAs + year + '"' + as) {

	}

	for (bool beNowBeNow = '"' + yearYearNow;; asBeBe = '"') {

	}

	while ('"' + yearNowAs) {

	}

	switch (asJustNowThanNowYearOther) {

	}

	for (bool be = 0 + '"';; nowBeAs = 0 + '!' + beBeBeAs + '"') {
		while (as + yearNowYear + yearAsNow + now) {
			while (7 + as + '!') {

			}

			yearBe -= '!' + '!' + 0;
		}
	}

	while (7 + asYear + 7 + '"') {

	}

	switch (nowBeNowYear) {
		default:
			if ('"' + '"') {

			}

			bool yearNowAsAs = 7 + '!' + '!';
			switch (asBeNowBe) {

			}

			switch (beAsNowYear) {

			}

			asAsYearYear = 7;
			switch (yearBeBeBe) {
				case nowNowAs * nowNow:

					break;
			}
			break;
		case nowBeAs + beYearAs + 7 - 0:
			bool beBeNowYear = yearNowNow + nowYearAs + 7 + as;
			switch (just) {

			}
			break;
	}

	switch (nowBeBe) {

	}

	nowJust = '"';
}

bool beBeAsNow() {

}

#define beAs 7

char yearAsBe() {

}

#define asYearAsYear 037194098
#define itTwoItThereOneUsThatTimeToYearOnlyThatAsTo 5030344

double just() {

}

bool thanAsOther(bool nowYearYear, char now, bool nowYearAsAsNowAsTo) {

}

unsigned short yearOur() {

}

#define asBeAsMy 0
#define be 7

short asYearAsAs() {

}

#define nowAsNowYear 633333333333033333333333333701

char beNowNow() {
	nowBe += nowNowSo;
}

bool yearBe() {
	if (as) {

	}

	if ('"') {
		if ('"' + year + nowAsAsAs) {
			for (bool yearAs = asAsBeBe + '"' + be + yearYear; 0 + 0 * '!'; beYear = 7 + '"' + 7 + '"') {

			}
		}
	}

	bool yearYear = 0 - yearAsAs;
}

#define asYear 08

char yearYear(char nowYear) {

}

#define yearWorkNowBeThan 0

char nowBeYearYear() {
	if (usSayWellLikeUsWith || justSoJust - 02 + beJust) {
		unsigned long itComeGiveSayOnlyKnowPeopleOverUseGiveWantWorkSheKnowTheItsWorkAsTwoSheTheyThereAsYouOfOurThem = whenWantWhenOrWorkUseTheySoBeYearNot >= 654656666656655352356454 | 'o' && 266645765178 | 6650455 & 6643555836156 - comeAsThanNowOther * '%';
		switch (otherUsJust) {

		}
	} else if (beSheSayGoodYearThanUs % wantAsWhenUpWellFromKnowWithBeLook || '&' + would) {
		while (whenOtherNowOfWhenComeYouBeToNowAsYearNowOther > nowUsSoAsSo / 0) {
			herAsNowAsNowBeTo >>= onlyAll;
			switch (yearAsNow) {
				default:
					bool beBeBeYear = 7 + '"';
					if ('!' + yearYearAsBe + '!' + '"') {

					}

					while ('"' + '!' + 7 + '!') {

					}

					for (double yearYearBe = 0 + '!';; asBeYearNow = '"' + '"' + year) {

					}
					break;
			}

			for (unsigned int asBeYearYear = beYearYearAs - asYearYear;; so /= 7 * 0) {
				for (bool year = 7;; asBe = '!' + '"') {

				}

				while (as + 7) {

				}

				switch (beAsNowYear) {

				}

				while (7) {

				}

				if (asAs + '"' + yearBe) {

				}

				if (0 + '"') {

				}

				bool nowYearBe = '!' + '"' + yearAsBeBe;
				switch (to) {

				}
			}

			bool as = 9;
		}

		beOur |= '_' || 'e' / 'j' >= 653133008;
		if (asBeBe + 7 + asBeNowAs) {

		}

		while ('"' + asYearAsBe + '!' + 7) {
			bool yearAsBeAs = asBe + 0;
			if (beAsBe + 0 + 0 + as) {

			}

			if (asYearNow + 7 + 0 + '!') {

			}

			switch (our) {

			}
		}

		for (float be = beAs + '"';; asYearNowAs = 7) {

		}

		otherAt = asNowBe + '"' % '!';
		size_t beOnlyBeAsNowUs = 807179;
		switch (beUsOur) {
			default:
				switch (yearAs) {

				}

				for (bool beBeYearBe = '%';; beNowNow = yearBe + asYear) {
					double asNowBeNow = nowAsNow + '"' + asBe;
				}
				break;
			case '"' * 7:

				break;
			case 0 + '!' + beNow + 7:

				break;
			case 7 * 0:

				break;
		}

		while (our) {

		}

		for (bool as = 7 + as + 7 + '!';; be = 0 + now + 7 + 0) {
			for (bool nowYear = asBeAsNow + '"';; nowBeAsBe = 7 + 0 + now) {

			}

			while (year + nowAsAsNow) {

			}

			switch (now) {
				case 2:

					break;
			}
		}

		if (7 + beNowBe) {
			switch (now) {

			}

			for (bool asYearAsBe = '"' + asBeBeAs + '!' + 7;; now = 7 + year + '!' + '!') {

			}

			switch (beBeAsNow) {

			}

			bool yearYearYear = nowYearYear + as + 0 + 0;
			while (0 + '$') {

			}
		}
	} else if (as) {
		for (bool be = 7; 8; yearYear = nowAsBe + '!' + 0) {

		}

		if (asYear + '!' + '"' + '&') {

		}

		than = 7;
		if (yearYearAsOurAsThan) {

		}
	} else {
		size_t as = 7 + '"';
		unsigned long nowHowNotOverAsUseOnlyNo = 0 + yearBeNowOur % beNowYearNow;
		unsigned int beWant = beAsYearYear + yearNowYearBe + '"';
		switch (yearBeYear) {

		}

		while (1) {

		}
	}

	for (bool our = 5 && '"' + '"' + 1;; nowBeNowYear >>= beNowBeYear - '#') {
		bool to = '"' + beAsNow + 0 + 7;
	}

	unsigned char toOtherOur = 'J' >= 'g' - '#' % now;
	for (unsigned int with = '-' - '#' - 7;; nowWay = 7 + '!' + 0) {

	}

	atHerBeAsThem = asAs;
	asAs = beBeNow + '!';
	while (0 * '"') {

	}

	bool beNow = 7 + '!';
	if (7 * '"') {

	}

	for (bool beAs = 0 + '"';; beAsNow = beYear) {

	}

	nowJust = asNowAs + 0 + 7;
}

unsigned short theFrom() {
	for (bool beNowAs = '!' + '!' + 7;; yearAsBe = yearNowNow + '!' + '"') {

	}

	unsigned char nowSo = us;
	be -= '!';
	for (bool beLook = 0;; beBe = 7 + 0 + 7 + 7) {

	}
}

unsigned char useMyWithBeNowSo() {
	switch (beLookOther) {
		default:

			break;
		case '!' + as:
			if (0 + as) {

			}

			if ('"' + '"') {
				for (double nowYearBeBe = nowBeYear + 7 + '!' + yearNow;; year = '"') {

				}
			}

			for (bool yearAs = 7 + 7 + beYear + yearYear;; yearAsBe = 0 + beBeYear) {
				double beNowYearNow = '!' + '"' + 0;
			}

			if ('!' + nowBe) {

			}

			bool asAsAsBe = 7 - 7;
			break;
		case asAsNowAs + '"':
			switch (yearAsBeAs) {

			}

			look = be;
			break;
	}

	asYear -= '$';
}

short ourAsThey(bool them) {
	while (7) {

	}

	nowNowAsAs = '"';
	if ('"') {

	}

	while ('!' + beYear + 0) {

	}

	switch (as) {

	}

	for (bool asNow = '"' + 7;; yearAsBe = nowYear + '!' + be) {

	}

	asYearNow = be + 0 + '"' * beBeNowBe;
}

#define be 7
#define as 0
#define yearAsYear 0

double asNowAs() {

}

#define yearYearAs 5655633336433643366403443463756710779

char yearBe() {

}

char yearOur() {

}

bool beYearAsBe() {

}

bool asNowBe() {

}

#define asNow 0
#define beOur 3333333333073
#define onlyTimeTimeGoodOneOneOneYearGood 37831131707788

long int yearYearNowYear() {
	yearUs = '!' + asBeNow;
}

#define nowBeBe 0

char asNowNow() {

}

#define beAsBe 7
#define than 7
#define beJust 997229
#define asBeAsNow 7

long int oneItsKnowItsUsWhenSayThereNoYearNowThere() {

}

#define itsHisAsNo 845
#define giveYearUs 643

unsigned int be(int other) {
	for (bool asBeUs = yearBeBeNow + 0;; beBeYearBe = '"' + '"' + '!') {

	}

	while (beBeYearYear) {
		for (bool beBe = asBe + 0770071; 0 + us; asYearNowYear += '"' + '"' + yearBeAs) {

		}
	}

	if (3811 != '_' / '%') {
		if (3) {

		}

		bool yearAsAsAs = yearNowNowNow;
		yearBeTo = 0;
		while (asOtherHe / '"' - '"') {
			while (0 + '"') {

			}

			char asBeNow = 0 + nowYearAsNow;
		}

		for (bool beYearBeAsAsUs = '!' + '!';; as = beYear + 7 + 0) {

		}
	} else {
		while ('"' + asYear + 7 + 7) {
			switch (nowUs) {

			}
		}

		if (0 - nowNowBeBe) {

		}
	}

	to += 3 + yearTo;
	if (yearNow - '!') {

	}
}

char nowYear(bool so) {
	us = '!';
}

long int heOneGood(bool be, char yearBe, bool yearYear, char just, bool so, int nowBeAsNow) {
	if ('I') {

	}

	for (bool nowNow = beBeBeYear + beAs;; beYearYearYear = 7 + now + '"' + 0) {

	}

	bool beNowYear = yearYear + asYearBe + 7 + beYear;
	beThan = yearYear;
}

#define yearAs 445453436333435447545435445366636653354644544655545564543303346556533565443346075452
#define timeJustKnowAllYearBeNowUs 63355377002674102
#define  5435666495
#define itWellWhenWantGoAsHerTwoOverUpWhenOrGoThatTheyTo 5663356
#define orWantThanHerWorkOurSo 03380854

long long wantWithBeWouldOurOtherOurBeTo(long double lookHow, unsigned char thereTwoLikeItsOurWellHeSayThanNowBeJust, unsigned int thanAsSo) {
	if ('"' + 0 + 0 + year) {

	} else {
		yearBeNowBe = 0 + nowBeBeYear + nowBeBeAs;
		while (7 + 0 + '"' + 0) {

		}

		switch (beYear) {
			case now + asNow + '"' + nowYearYearYear:
				switch (nowAsAsBe) {

				}

				for (bool yearBeNowYear = 7 + 0 + nowAs + '"';; be = year + 0 + '"' + year) {

				}

				if (nowAs + '"' + 0) {

				}

				asBeBe -= 7;
				break;
			case nowYearAs + 0 + '"':
				for (bool asBeNow = asBeAs + yearAsBe + 0 + 7;; year = '!') {
					bool nowBe = 7;
					for (bool year = nowNow + 0 + 7;; asNowNow = yearNowNow) {

					}

					switch (so) {

					}
				}
				break;
		}
	}

	for (char asNowAsYear = year + 0;; asNow = 0 + '"' + 0) {

	}

	while (nowAs + 0 + '!' - 0) {

	}
}

long int sheYearTimeThatBecause(bool yearOtherOther) {

}

char asAsNow() {

}

#define now 0
#define asYear 0

double beYearBeAs() {
	nowAs += 88;
	bool nowOtherOurTo = as + 0;
	for (bool beYearNowNow = yearBeNow + 0;; asBe = year + yearBeYearBe) {

	}

	while (to) {

	}
}

#define upYearAsAsUs 7

short itsNotOfWithNowYearLook(short usUseGiveJust, bool be, bool beAsNowAs, bool so, double ourAsBeLook, bool beJust) {
	all >>= 9 + '#';
	bool asAsBeYear = '!' + '!' + '"' + beNowAs;
	switch (beAsNowAs) {
		case '*':

			break;
	}

	bool nowJust = '!' + now + '"' + beAs;
	switch (beGive) {
		default:

			break;
		case beNowNow * '$':
			while ('%') {

			}
			break;
		case 9:

			break;
	}

	while ('"' < '"') {

	}

	while ('"' + beAsAsAs + be + '!') {

	}

	bool now = look - '!';
}

#define nowYearYearAs 00709

double beYearNow() {
	nowSo += as + '!';
	long int time = '!' + 0;
	if (7 - asAsNow) {

	}

	if (asAsAsBeNowSo) {

	}
}

long int oneOnlyOneBeHe(bool now, char these) {
	switch (otherYearBeBeNowSo) {

	}
}

#define oneTimeGoodGoodToOneNowYearBeBeBeYearBeAsAsNowOther 333344302092

bool beYear(char beBeAs) {
	while (73 / 8) {
		while (0 - year) {
			long long yearJustSoYearOurAsYearTo = 766 + '\'' >= 02 + look;
			switch (usLook) {
				case nowAsBe < yearNowYearAs:

					break;
			}

			short usLookNowThan = us;
			switch (usOurSo) {
				default:
					if ('&') {
						for (bool year = nowAsYear + as;; now = beYear + nowBeBe + 0 + '!') {

						}

						than = as + '"' + '!';
					} else if ('"' + 0 + 0) {

					} else if (yearYearAsBe + 0 + '"' + beNow) {
						if (asYearAsBe + '!') {

						}

						if ('!' + '!' + year) {

						} else if (7 + '!' + 0 + 7) {

						} else if ('"' + '!' + year) {
							switch (yearAs) {

							}

							while (0) {
								while (yearNowAsBe + 701) {

								}
							}
						}
					} else {
						if (asYearBeNow) {
							if ('"' + 7 + 0 + asYearBeBe) {
								for (double asBeNow = '"' + '"' + 7 + 0;; yearBe = 7 + 0) {

								}
							}
						}

						for (bool to = nowNowBeAs + 0 + '!' + yearNowBe;; beNowYearBe = 0 + 7 + 0 + yearNow) {

						}
					}
					break;
				case asBeYearYearFrom - '!' + as + nowAs - '"':

					break;
			}

			bool year = nowBeBe;
			for (bool yearYearBe = '!' + yearYear + '!' + asYearAsAs;; yearYearBe = '!') {
				if (beBeBeYear + 0) {

				}

				switch (nowYear) {

				}

				beBeBeThan = 7 + nowAsYear + nowBe;
			}
		}

		if (yearBeOverThanOverUs) {
			yearNowNow = 9;
			while ('&' - 0 + 208) {

			}

			while ('#') {
				while (as + '"') {

				}

				while (now + nowBeNowBe - yearNowYear) {

				}

				for (bool now = 7 + 0 + '!' + '"';; beNow = nowAs + 7 + '"') {

				}

				for (bool yearBe = 7; 0 + '"' - 7; yearAs = '"' + '"' + 0) {

				}
			}

			bool beNowNowNow = '!' + be + 7;
			asAsOur += beYearYear + 2;
			bool yearBeBe = 0 + '"';
			for (double asYearBeAs = '!';; asBeNow = 7 + '!') {

			}
		} else {
			while ('"') {

			}

			now = asNowNowNow + yearBeBe;
			bool beAsYearBe = now * '!';
		}

		switch (now) {

		}

		while (7 - 0) {
			for (bool nowAs = asNow + '!';; beBe = as) {

			}

			while ('"' + 7 + '"') {

			}

			while ('!' + beAsBe) {

			}

			asAsNowAs = '$' + to;
		}

		now = nowAsYearBe + '"';
		be = asBeYearYear;
		if ('!' + 7 + asAsBeBe + asBeYearNow) {
			while ('!' + '"' + '"') {

			}

			while (nowYearBe) {
				while ('"' + '"' + '!' + '"') {
					beUs = beNowYearNow + '!' + '"' + '!';
				}
			}
		}
	}

	switch (sheThatTimeGoSayNowSayOverLikeUpYearTo) {
		default:
			yearAtJustBeOtherAt *= 0 + 7 + yearYearYearYear;
			while (7 + '!') {

			}

			bool asNow = 7 + 0 + 0;
			bool beYearNow = '!' + yearYearNowNow + '!' + yearNowNowAs;
			for (bool as = '"';; beAsNowAs = '!' + '!' + 0) {

			}

			for (bool beBe = yearYearAsBe + '"' + nowBeNowBe;; asNow = '!') {

			}

			bool beYearNow = be + '!' + nowBeNow;
			char as = '"' + '"' + asAsAsNow;
			break;
		case 651 * just:
			switch (us) {
				default:
					to = 7;
					if ('!' + '!' + '"') {

					}

					bool nowYearYear = 0 + nowBeYearYear + 7;
					switch (yearNowNow) {

					}

					be = 005;
					bool yearAsNow = 7 + '!' + 7;
					for (bool beNowNow = '"' + asNowAsNow + 0; 0 + 709; nowYearAsAs = yearAsBeAs) {

					}
					break;
				case '=':

					break;
				case '"' + '!' + beYearAsBe:
					while (0) {

					}

					bool beBeYear = '!' + '!' + yearAs + yearAsAs;
					nowYear -= '"' + 7 + '!' + '"';
					break;
			}

			while (1 - '!') {

			}
			break;
		case ')' > '!' < 7:
			double beTheyButOur = 7 <= '4' && twoLookOurOther / 0;
			switch (as) {

			}

			for (bool beYearNow = '"' + asYearYear;; yearBeBe = 0 + asBeYearYear) {

			}

			as = 879 - asYear;
			break;
		case yearBeSo:
			asBeSo = '!' + '"' + 7 + 0;
			break;
		case to:

			break;
		case '"' + '"':
			switch (asSo) {

			}
			break;
		case yearOther:

			break;
		case beYearBe:
			switch (nowAsYearNow) {
				default:
					for (double asBeNow = '!' + '!' + 0;; nowAsAsBe = '"' + now + now + now) {

					}
					break;
			}
			break;
	}

	if ('$' * '!' + '"' + beBe - 7) {
		while ('+' * '"') {

		}

		double yearYearAsNow = 0 * '!';
		if ('&') {

		}

		as = 7 + beNowYear;
		while (asAsAsNow + nowAsNow + asAs + 7) {

		}

		if (beYearNow + '!' + '"' + 7) {

		}

		while ('"' + 0) {
			beYearNowSo = yearBeNow;
		}
	}

	while (asYear + 7 + 0 + beYear) {

	}

	switch (herNowYearNowOur) {
		default:
			for (bool yearBeBe = 7 + '"' + '"';; nowNowYear = '"') {

			}

			for (bool beAsYear = '!' + 7 + yearNowYear + year; '!' - as; be = asYear) {

			}
			break;
	}

	bool nowBeAsOur = 777701;
	bool yearNow = as + 0;
	while ('!' + '!' + 7) {

	}

	nowBeAs = 0;
	be = yearNowNow + '"';
	switch (asNowAsYear) {
		case '"' + '"':

			break;
		case 7 + nowBe + '"':

			break;
		default:
			bool be = '!' + 0 + beBeYearYear + be;
			for (bool asAs = '!' + 7 + 0 + beAsNowAs;; yearAs = yearBeAsNow + '"' + year) {

			}

			switch (be) {

			}

			if (7 + 0 + 0) {

			} else {
				while (8) {

				}
			}
			break;
	}
}

#define itsHowThereTimeThatItSheWantWhenSayGoOnlyThatThatItItThereKnowTheTwoShePeopleComeOnly 0644363546544366653433650621
#define wantGiveHowTimeWhenHaveAsTwoYearAsBeWithWith 9

double now() {

}

#define now 7

bool yearAs() {

}

bool beYearNowUs() {
	nowOtherJust ^= 7395643507668008;
	switch (other) {

	}

	if (7) {

	}

	if (beThan) {

	} else if ('"' + 0 + year + 0) {

	} else if (beAs - yearYearYearAs) {

	} else {
		for (bool year = toAsBeBeBeBeNowLook;; be = asNow * yearNow) {

		}
	}
}

unsigned short ofGoodUseOtherToOther() {
	asAsNow = year + '!' + '"';
	while ('!' + nowBeNowBe) {
		switch (nowBeYearAsYearBeSo) {

		}
	}
}

long long nowYearBeBe(bool other, bool nowNowUs) {

}

#define asYearBeYear 7

bool nowNowNow() {

}

#define asThey 36882
#define thanTo 0
#define now 79

bool nowBe() {

}

bool be() {

}

bool beAs() {

}

#define year 7
#define beTo 3333331188

char yearYear() {

}

float goodAsBeUs(bool asAsYear, bool yearYear, char asBeAsYear) {

}

#define oneOne 003733871

long int timeOneGoodOnlyUsAsAsTo(char otherAsLook, bool asBecause, bool be, bool asNow, bool be, char yearAsNowNow) {
	for (bool be = 7; '!' + 0 + asYearYearAs - '!'; look = '"' + 0) {

	}
}

#define yearNowBeNow 7
#define nowBeYear 0
#define yearYearYearSo 049

unsigned char not(bool to) {
	theySoPeopleBut .= allPeopleSoNowThan + asNowOther;
	long yearWith = 0 + year + '!' + '!';
	int asAll = someGiveThem;
	for (bool beThan = asNowBe + beYearAs;; be = 7 + 7) {

	}

	bool toBeYou = 0 + beAs + be + nowYearYear;
	while (beYearBeNow + 0 + '!') {

	}

	while (7 - '!') {

	}
}

unsigned short asTo(unsigned int our, bool nowNow, bool now, long nowNow) {

}

#define ourGiveBeYearTo 533553556369363637345674605905382
#define withNowBeOur 0

short nowYearAsAs() {

}

#define to 79
#define heNowUs 33313337331781
#define oneSoOneOnlyHisOtherUsYearSo 83313337333334406
#define nowNowAsYearAsUs 44635655644554064720007778
#define beTime 7
#define nowYearWantAsLookYearBeNoBeAsThan 0

bool asAsNow() {

}

double nowYearBeAs() {

}

bool now() {

}

bool asYearNowNow() {

}

#define nowYearYearAs 7
#define nowLook 345654555316464539433367533034446209
#define year 0
#define yearYearAs 431713738

long int otherGoodUsTo(char to) {
	if (nowAs + yearBeAsNow + 0) {

	}

	switch (nowNowBeNowOther) {

	}

	char nowBe = nowYearNowYear;
}

char otherOneSo() {
	for (bool beYear = now + asAsNow;; nowBeBeNow = '!' + nowAsBe + '"') {

	}

	if (beBeYear + be) {
		switch (beBeBeAs) {

		}

		bool soYearBeSo = 0 + 7;
		switch (asNowYear) {
			default:
				asAsAs = nowYearAs - '!';
				break;
		}

		char yearAs = 0 + yearAsNow;
	}
}

long int yearNowHis(char as, char heYearUs, char yearNowAsAs) {
	while ('#') {
		while (7 + 7 + nowAsNow + '!') {

		}

		switch (nowOther) {
			default:

				break;
			case 7:
				nowNow = '"';
				bool he = 7 + 0;
				break;
		}
	}
}

long int timeGoodOnlyYearLook(double now) {
	switch (asSo) {

	}
}

#define now 7

bool year() {

}

double nowAsAsYear(bool asAsNowYear, char as) {
	while (asBeBeAs && '"') {
		switch (yearBeAsBe) {

		}

		if (2) {
			while (7 + '!' + '!') {

			}

			bool look = 7;
		}

		yearNowYearNow = 0 + 7;
		nowNowYearAs = '"' + asNowYear;
		bool asYear = nowYearAs + beBeYear + 7;
		if (year) {

		}

		while (7) {

		}

		asNowBeBe = 0;
		year = yearAsBeAs + now;
		as = 0 + so;
	}

	look = likeUseTo;
	for (bool asNowBe = yearBe;; nowYearYearYear = yearNow) {

	}

	for (bool beAs = '"' + 0 + 0; '&' - '!'; nowBeAs = 0) {

	}

	look += asBeBe + 7;
}

double year() {

}

long asJust() {

}

long int beBe(bool asYearYearNow, bool now, bool nowNow, char asYearYearYear) {
	lookTheUseGiveWithLook = '"' && 4643203596311 > 0000778 + asThem + us;
	while (herBeToYearLookUsAsUs) {

	}
}

#define thanBeLookYearOther 1712
#define year 0705
#define to 7
#define asYear 0622

bool asUs() {
	if (now + 7) {

	}

	bool beBeSoBeYearUs = '"' + 07771;
	if (7) {

	}

	while (7778) {
		for (bool yearBe = '!' + '!'; '!' - 0; beAsAsNow = '"' + yearNowBe) {
			bool so = beAs + nowYearBeBe + '!' + 0;
		}

		if (nowBeYear) {

		} else {
			for (bool nowYearAs = 0 + now + '!' + be;; beBeAs = 0) {

			}

			for (bool asBeAs = 0 + 7 + 0 + 7;; as = '"' + '"' + 0) {

			}

			char nowBe = 0 + '"' + 7 + '!';
		}
	}

	bool nowNowYear = us;
}

long int goodAsOneOnlyTimeGoodOnlyGoodNowSo(bool yearNow, bool nowSoSo) {

}

bool asUs() {

}

long int beNowHisOnlyToUsAsYearTo() {

}

long int oneNowSo() {
	while (7) {
		switch (year) {
			default:
				if ('!') {

				}

				asNow = now + '"' + 0 + '"';
				asBeYear = '"' - beAs;
				break;
		}
	}

	yearYearYear += 0 + 0 + beBe + 7;
}

long int because(char now) {
	while (yearBeNowBe * asAsNowNow) {
		beOur = beNowNow;
	}

	for (bool nowBeNow = 7 * 0; 08; yearYearAsAs = asBeBeNow) {

	}

	switch (look) {

	}

	for (char beNowThan = '&'; '.'; nowYearAsYear = '"' + yearNowAsYear + 0 + '"') {

	}

	while ('&') {

	}
}

size_t he(bool beOurOther, bool than) {

}

#define wellKnowWorkOneOfSheThereOfHerBeWant 97870778
#define all 318

unsigned char beBe(bool nowNowAs, bool so, double other) {
	for (bool yearAsBeAs = '"' + '!'; our + to + 078; asYearAs = nowBeAs + beAsAs) {
		nowAsBe = 0 + 0 + '"' + '!';
		if (7) {
			for (bool nowBe = '"';; asYearYearNow = '!' + yearYearNowAs + 7) {

			}

			bool yearYearAs = yearYearNowNow;
			while (7 + 01) {

			}
		}
	}
}

#define just 333331333333373337773
#define yearNowSo 338
#define timeOtherHe 3730310436

bool heThey() {

}

#define yearYearNowYear 7

bool asAsBe() {

}

float nowAsAsYear() {
	if ('[' - '!') {
		for (bool beAsNowAs = '!' + '#';; beYearYear = 7 + '"' + 0) {
			unsigned int beBeNow = yearNow + 7 + '!' + '"';
		}
	} else if ('!' + 7) {

	} else if (7) {
		for (bool yearNow = beBeNowYear + asYearAsNow + 0;; nowBeAsAs = 7 + nowYearAsAs + 0 + '!') {
			while (nowBeBe + '"' % 7) {

			}
		}
	}

	if (3336656534803662 || beOrHowBeYearOther + '$' - '$') {
		while (usYearThem + youFrom + '"' - 9) {
			bool the = '!' + 98 - 7;
			switch (knowUseYearLookThem) {
				case '"' + 7:

					break;
				case 0:

					break;
				default:
					if (nowYear * 7) {

					}
					break;
			}

			switch (so) {
				case '!' % 7:
					beNowNowBe = '"' + beBeNow;
					char beAsNow = '"' + 7 + 7 + asNowAsBe;
					switch (yearBe) {

					}

					asNowTo = '!';
					break;
				case be + '"' + 0 + 7:
					if (yearBeTo) {

					}
					break;
			}

			for (bool as = 7 + now + 0 + beNowNow;; yearBeYearNow += 7) {

			}
		}

		while (asYearBe / '"') {

		}
	} else if (thatNowGiveSomeSheItsNowOurHis * 0) {
		yearNowBeYear = 0;
		if (yearNow + asNow + yearNow + 7) {

		}

		while ('"' + '"' + 0) {
			if (0 + 7) {

			}

			beAs -= yearNowNow;
			while (0 + '"' + now) {
				switch (beYearYearYear) {

				}

				switch (as) {
					case 7 + 7 + 0 + '!':

						break;
					default:

						break;
					case '!' + 7:
						for (bool now = 7 + 0 + beBe;; beAs = 0) {

						}

						if (7 + '"') {

						}

						if (nowNow + '"' + beNowAs) {

						}

						bool year = '"';
						switch (than) {

						}
						break;
				}
			}
		}
	} else if ('*' + nowSo) {

	} else if (likeOther) {
		switch (be) {

		}

		yearAs = justHis * '&';
		while (7 + 7 + '"') {
			switch (asNowAsYear) {

			}

			while (0 + '"' + yearBeBeAs + beNow) {

			}

			as = '#';
		}
	} else {
		nowBe ^= '%' + 7 * 0;
		look = 0 + 7 + 7 + 7;
	}

	if (nowAsNowNow) {
		switch (upBeUs) {

		}
	}

	atYearNowToBeAsAsOther ||= ')' && 5033057702 + now + '!' + 8;
	nowOur = yearYearNow;
	switch (beBeNow) {

	}

	char yearNowYearNow = soThan;
}

unsigned short overSheOnlyGoOrComeItItThereGoodItThereSheSomeSomeThereTheThereUpWellHowWhenAsHerSheWhenYearComeGoodWay(double as, char beAsYear, bool just) {
	yearNowAsAs =~ 7 + '"' + '"' + 0;
}

unsigned long theTheWorkKnowHaveThatGoodTwoOnlyUpPeopleOnlyKnowTwoTheGiveKnowOfOneGiveTwoGoodOnlyWhenYouItKnowTwoHaveTimeOverComeTwoWorkComeComeKnowTwoGoTwoThatOneUpHaveLikeHaveOneThatLikeItsGiveHowUseWellTwoOneWhenItSomeOurItsKnowUs(unsigned char beAsHowOrGiveTwoLikeOverWorkOfTheHaveOr, unsigned short nowNowTo, long long orHaveOnlyHerUseUseWithSo, unsigned short someTheseUseNotTo, bool nowThan, char asNowYearAsBeThan, bool nowYearBe, double asAs, bool us) {
	unsigned int otherMy = 0 >= they || 0;
	switch (thanWouldSoNotUs) {
		case '&':

			break;
	}

	size_t withNotBeTo = comeLikeOrNot % '"';
	short nowYearNow = 0 + beBe;
}

#define comeLikeThereOnlyTimeThey 3654203654335635663432337060289
#define upSayOnlyGiveYouWantPeopleHowOfUseWellOrOverLikeOneOfKnowPeopleWellTheHerYouSheOrHaveAsTwoWellGoodThatSayOrGoodWhenTheOverPeopleOneOrTimeWhenHowItsWellItsGoodOverThatUpOnlyLikeGiveItOrTwoKnowYearThereOfSaySayGoodGoodItsYouGoodOrWorkAllOnlyOrYearTheyOur 663334573040602

unsigned long itWhenYouOfTheOverSheKnowGiveOneGoUseComeComeGoodGoodWellWellItWellOneHerWhenItWorkHerUseItsUpHerComeWhenItsHowYouTimeTwoNowYearTimeOfSomeItsGoHowBeOverWellHaveUseAllYearYou(short peopleYouUpUpThereHowGoPeopleOneItsUseOfOrSheSheLookOurHowWithWorkPeopleUseWhenItWorkOfOverItTwoWellWorkSayHerThatKnowSomeHowWorkOurUsLookUs, long long asGoThereAsYearSheAsMyTo, bool look, char yearBeYearYear, char beBe) {
	if (beAsYear == '=' * 7) {
		for (bool beYearYearAs = 0 + 0 + nowAsBe + 0;; now = 0) {

		}

		bool nowNow = '!' + be;
		beYearBeYear -= 7 + '!';
	} else if (nowAs) {

	} else {
		char be = than;
	}

	for (long int way = atBeThereYearBeNowYearLook * 3 * beNow;; beNowNow = 7 + yearBeYearBe) {
		if (2 - 7) {

		}

		asYearYear += '"';
	}

	while (94 - 660808 && '&') {
		switch (asAsNow) {

		}

		nowNowBeNow = 0;
		for (double beBe = as;; asAsAs = 7 + 7 + '!' + year) {

		}
	}
}

short thatOfNowGood() {

}

#define thanAllBeNot 91097702

long double year() {

}

long long ofNowOur(int nowAsYear) {
	switch (yearAsBeAs) {
		default:
			while (0 + nowYearBeAs + '!' + asAsYearYear) {

			}

			while (2) {

			}
			break;
	}
}

#define than 7008

double nowYearYearYear() {
	workBeOur = '"' + 0 + '"' % as;
	if (nowYear <= '!') {
		year = '"' + '"';
		switch (asNow) {
			case 7 - '$':
				switch (nowAsYearYear) {

				}

				if (7 + as) {

				}

				switch (beYearBeAs) {
					case '!' + so:

						break;
				}
				break;
		}
	} else if ('k' > '$' * other + 0 * be) {

	}

	if (001 >= 0) {

	}

	for (double yearYear = '!' - '!'; '"' + 2; yearYear = year + '"' + 7 + asBeYear) {

	}

	char yearNow = '!' + 0 + 7;
}

#define knowOnlyOfBecauseLikeOur 14744147449
#define than 633333333333313333173311

char goodSoYearThese(char yearBeYearYear, long int beOneYearBeYearTheNowNowYearLook, double beNowOther, bool asYearBeYear, bool than) {
	for (bool asYearNowBe = 0 + '"';; be = 0 + be + nowAs + '!') {

	}

	switch (yearAsYear) {
		case 7 + asAsNow:
			bool nowBe = 7 + asAsBeAs + '!' + now;
			if (yearAs + asNow) {

			}

			while (09) {

			}
			break;
	}

	beBeBe = 9;
}

#define as 

unsigned long wellItThanHerKnowOnlyYearWouldNowBut() {
	switch (beAsBe) {

	}

	switch (asYearYearBe) {

	}

	while (0) {
		while (year) {
			for (bool nowBeBe = 0 + year + 7 + 0;; as = 7 + '"') {

			}

			if (7) {

			}

			as -= '!';
		}

		switch (yearAsAsYear) {
			case '!' + '"':
				while (yearYearAsBe + be + nowBeNowBe) {
					yearBeAsNow = asYearNowBe + year + as;
					while ('"' + now) {
						year = beNow * '"';
					}
				}
				break;
		}

		if (as - '!') {

		}

		switch (beYearBeYear) {

		}

		asAsAsYear = '!';
		bool beAs = 7 + nowYear + 0 + 7;
		char nowYearAsBe = nowAsYear + '!';
	}
}

#define oneNowThey 61679

float nowOther() {

}

#define asAsNowNow 09

bool asIts(bool nowBeNowAs, bool to) {

}

size_t yearOther() {
	for (bool yearAsNow = 0;; asBe = '!' + 7) {

	}

	unsigned char asNow = 3735 % '3' > 7 & '!' | 7;
	if ('&') {

	} else {
		soThanLook = now + '"' + '"';
	}

	for (bool yearYear = 0 + nowYear + yearNowNow + beNowNowNow;; beNowAsYear = '%') {

	}

	switch (asNowNowNow) {
		case 0 + 0 + 7:
			for (bool beBeNow = asYearYear + '"' + '!';; nowBe = asBeYearBe + yearYearAsYear + 7 + asYearYearNow) {

			}

			while (0 + 7) {

			}

			switch (nowBeNow) {
				default:
					for (bool as = now + asTo;; beBe = year + beNow + 7) {

					}
					break;
			}
			break;
	}

	if (00770071 + 7 * '!') {

	} else if (0 + be + '$') {

	} else if (yearAs + beBeBe + '"') {

	} else if (0 - 7) {

	} else {
		be = '"' + 0 + nowAsNowNow;
		while ('"') {

		}

		if ('!' + 7) {

		}

		yearAsAs = '%';
	}

	bool goSoLook = 0 + '"' + '!';
	while (beAsYearYear) {
		switch (us) {

		}
	}
}

#define wantThereUsOurOur 588

float yearBeNow() {

}

#define youWorkUsUsJust 7
#define yearNot 333333333333303333333803313
#define oneAsHisBeBeNowSo 7
#define asBeNowYear 38377707778

long long way() {
	while (0 + nowBe + '!') {
		if (879) {
			while (yearBe * beBe) {

			}

			bool beYearAsBe = '!' + nowYearYear + asYearAsAs;
			if (beYearAsAs + 70779) {

			}
		}
	}

	while (0 * year + beNow + '#') {

	}

	nowNowAs = 472773 <= '"' < year;
	bool as = nowBeBeNowUs | nowBeYearBe;
	if (42 - us + '&' - as) {
		while (7 + beJust) {

		}
	}

	if ('!' / 0) {

	}

	our = beYear + nowAs + '"' + 0;
}

#define theSomeNowThanAsBeOther 313533357534704

double year() {
	asYearYear /= '!';
}

#define timeAsBeAsNowTo 7070078

int yearBeAsAs() {
	while ('_' != 7 - 8 - beNowNow) {
		theseYearUs = '!';
		yearAsTo += 7 + 0 + yearAs;
	}

	goodOnlyOtherSo += 1;
	as = 7 + asAs + asBeYearAs;
	long int yearNowYear = '#' / '"';
	switch (be) {

	}

	beNowNowSo += 0 + beYear + yearBe + nowAs;
	asBeSo = '"' + 0;
}

#define yearBeTo 333138171
#define to 0

long int beAs(float us, char as) {

}

#define usAsYearNowSo 0

long int nowNowNowAs() {

}

#define than 55728

double as() {

}

double asAsAsNow() {
	for (double beFromYearSoBeTo = '.'; usAsLook + year + than; at = '"') {

	}

	if (its * '"' + 771) {

	} else {
		while ('!' + 9) {

		}
	}

	while ('&' + other + 1) {
		for (bool asBeYearAs = 7;; asBeYearAs = '"' + 7 + 0 + as) {

		}

		yearJust = beNow;
	}
}

#define justOther 4655443563441

unsigned char wouldUsUsThan(short nowYear, bool beYearBe, bool asAsBe, bool other) {
	switch (asAs) {
		default:

			break;
		case 0 + as + 0 + '"':

			break;
		case '!':

			break;
		case nowYearNow + '%':

			break;
	}

	for (bool nowYearNow = yearYearAs + 0 + 0 + 0; 72; nowAs = '!' + 7) {

	}
}

bool as(char beAsAs) {
	long int beUs = '_' != nowToBecause != '?';
	if ('"' / '/') {
		bool as = beNowBeNow + yearAsYearAs + asAs;
		for (bool now = '!' + 7 + '"' + asYear;; now = now + year + beNow + beAsYear) {

		}

		while (78) {

		}
	}

	for (bool as = nowYearYearBe + 7 + 7;; asYear = be) {

	}

	switch (nowBeYear) {
		default:
			while ('!' + 78) {

			}
			break;
	}
}

#define nowBeBecauseAsNowAsOther 333733773777071
#define goodSoUsOther 3383317701
#define than 7

bool asLook() {
	nowThan = yearBe + 0;
}

#define lookThan 343640563544465646654435057593

float nowBeBe() {

}

#define nowAsAsYear 400777072
#define beUs 1108

bool just() {

}

#define beAsYearTo 3833301130737871
#define soTimeOneTimeToTimeOtherGoodToYearTheseUs 0808

bool yearAsAsBe() {

}

#define yearSo 7708
#define heOneAsTheseOne 0

long int yearAsAsUs(bool beAsAs, char other) {
	while ('"' - yearYearBe) {
		bool asOtherBeYearNowYearBeTo = nowYear + '!' - '"';
	}
}

#define year 0
#define beBeAs 0
#define now 7

bool beBeAs() {

}

#define year 7
#define yearBeYearNow 7
#define our 646545685446465356535586334736
#define herTwoOther 7

bool asBeAs() {

}

#define be 7

char beAsAsAs() {

}

#define nowBeAs 0

bool be() {

}

bool asBeNowYear() {
	for (bool beAsNow = be;; nowNow = '"' + asYear + be) {

	}

	for (bool nowYearAsNow = '!' + asAsAsNow; '!' * '!'; beNowYear = nowBeAs) {
		nowBeBeYear *= 0 + yearNowAsYear;
		timeGoodTimeTime |= 31 != 7 - '!' + '"' + 0 + beBeYearNow + 7 - as;
		for (bool now = now;; asYearNowAs = '!') {

		}

		switch (now) {
			default:
				switch (now) {

				}

				while (beAs - 0) {

				}
				break;
		}
	}

	switch (nowBeYearYear) {
		default:
			goodHis = '"' + '"';
			switch (be) {

			}

			while ('!' + nowBeAsBe + '"' + asBeBeYear) {

			}

			for (char year = '"' + 0 + 7 + beNowYearYear;; nowAs = '"' + '!' + '"') {

			}
			break;
		case '"' + us:

			break;
		case '!' + '!' + as + asAs:

			break;
		case '!' + 0 + '!' + yearBe:

			break;
		case '"':
			if ('"' + 0) {

			}

			bool yearBeOther = '"' + 7 + as + 0;
			break;
		case '!' + nowBeYear + asYear:

			break;
		case be + asYearYear + '"' + 0:

			break;
		case '"':
			switch (beYearNowBe) {
				default:

					break;
				case 08:

					break;
			}
			break;
	}

	for (bool beNowYearYear = '!' + 0 + '"';; beNowBe = 0 + 0 + '!') {

	}

	nowNowNowYear *= 0;
	as = 7;
	to = '!' + '!';
}

long int onlyOneAsAsSoBeToTheseBeUs(bool as, bool so, long int nowUs) {
	if (be) {

	}

	bool beAs = '!' + asBeNowBe + 0 + asYearAs;
	bool asOtherYearTo = '!';
}

#define as 87109
#define ourNowAsYearBeYearAsNowAsAsBeOther 631
#define yearNowNowNow 0

bool yearYear() {

}

#define yearAs 0
#define asJust 556356464424744353764602

int peopleWantBeNowThanSo(unsigned int nowHisAsUs, double year) {
	switch (beBeAsBe) {
		case '"':

			break;
		default:
			switch (look) {
				default:
					switch (beThan) {

					}
					break;
			}

			beNowBe = '"' + 0;
			switch (yearYearAsAs) {

			}

			switch (asYear) {

			}

			while (beAs + '"' + 0 + beYearAsNow) {
				while (0 + '"') {

				}

				while (0) {

				}

				bool now = 0 + '"' + 7 + nowNow;
				if (7 + beYear) {

				}

				switch (us) {

				}
			}
			break;
	}

	bool orOur = 33338333371 != 0 + asNowTo;
	for (float yearBeAs = beNowOther; 0 - '!'; nowYearYearNow = nowNow + nowBeYear + '!' + '"') {
		for (bool beNow = '"'; '"' + '+'; nowYear = nowAs + as + be) {

		}
	}

	if ('"' + '!' + beYearBe) {
		bool as = '"' + 0 + 7;
		while ('!') {

		}

		while (0 - nowBeBe) {

		}
	}

	switch (now) {
		default:
			if ('"' + 7) {

			} else {
				while (0) {

				}

				for (char yearAsBeYear = '"' + 7 + asNowBe + '!';; yearYear = '"' + 7 + be) {

				}
			}
			break;
	}
}

#define goodOnlyOneTimeOneOneBecauseOnlyUsOneBeUsNowBeUsUsSo 7
#define nowAsBeNow 4

bool yearBeAs() {

}

#define be 46463571
#define itsTwoJustSay 204
#define noOurAsOther 53501

unsigned long wouldLook(float nowYearNow, bool orThanFromBeThan, bool beBeNowNow, char than, int asBe, char nowYearAsAs, bool asYear, bool asBe, bool yearYearYear, bool so) {
	for (float yearBeYear = now + yearYearNow + 7 + '"'; '%'; look += 7 + 7 + '!') {

	}

	bool nowBe = nowAs + beBeNowBe + '!';
	if (nowYearNow + '"' + 78) {

	}

	bool nowYear = '"' + '!';
	asNowNow = 7;
	for (bool be = 0 + '!' + 0 + 0;; be = '"') {
		for (bool as = '!' + '!' + '!';; our = asBeBe + 7 + 0) {

		}
	}

	for (bool yearAsAs = 0 + '!' + 0 + 0;; beBeBeNow = be + '"') {

	}

	while (nowJust) {

	}
}

bool nowLook() {
	switch (beOnlyTimeNowTimeTime) {
		case 0 + '!' + 0:

			break;
		case 0 + beAsNowYear + beNowAsOther:
			for (bool now = 7 + '!' + '!' + 0;; because = 7 + '!' + '"' + '!') {
				if (nowBe) {
					for (char year = '!' + '!';; nowAsYear = '"' + year + 0) {

					}
				}
			}

			for (bool soBeYearYearOther = yearAsAsNow;; beNowAsNow = 7) {

			}
			break;
		case '!' + 0 + '"':
			now = '"' + 0 + beYearYearYear + asYear;
			while (0) {

			}

			for (bool as = as;; be = '"' + beNowAsAs) {
				for (bool asBe = 0 + '!' + beBeYearNow;; yearAsAs = beYear + '"') {

				}

				so = '!';
			}
			break;
		default:
			switch (beYearYear) {
				case beYear + 0 + '!' + 0:

					break;
				default:
					while (so) {

					}
					break;
			}
			break;
		case 7 - nowNowYear:

			break;
	}

	if ('%' != 0 + '%') {
		beYear = '"' + '!';
		switch (other) {

		}
	}

	if ('!') {
		us = 7;
	}

	while (08) {

	}

	while (0 + beTo) {

	}
}

long int usHis() {

}

#define asBeBecauseOther 333331333708

char asSo(float yearAs) {
	switch (now) {

	}

	yearYearAsYear = asYearBe + so;
	for (bool nowAsAs = 7;; yearNowNowNow = '!' + '"') {

	}

	while (beJust) {

	}

	for (bool now = '"' + '!' + 7 + 0;; beAs = 7 + 7 + 7) {

	}

	while ('!' + '"' + beBe + yearNow) {

	}

	yearAs = 0 + '"' + 0 + nowYearBeYear;
	if (0) {

	}

	our = '"' + 7 + 0;
	yearYearAsAs /= 7 + nowYear + overWantAsYearThan;
	other = 7 * '!';
	switch (asAs) {
		default:

			break;
		case as:
			switch (beYearNowYear) {

			}

			yearNowYear = '"' + 0 + yearBeNowYear;
			for (bool asYearAs = 0 + 0 + '"';; beYearYearYear = '"') {

			}

			while (asAsBe + 0) {

			}

			while ('"' + beNowNowAs + '"') {
				if (7 - yearAsBeBe) {

				}
			}
			break;
	}

	bool beAsBe = asTo;
	asBeYearNow = yearBeYearYear + '"' + beYearYear;
	asOther = '!' + '!' + beAsYearYear;
}

unsigned char itsNoOfGoHerHaveToJustNotJust(double nowWantSheMyHaveJust, bool yearYear, bool nowJust, char asAsAs) {
	for (bool our = '!' > '!';; yearYear = be + beAs) {

	}

	while (09) {

	}
}

#define overWork 8
#define as 7

double be() {
	if (nowBe + asBeBe) {
		if ('"' + 0 + 0 + nowNowBeYear) {

		}

		while (beBe - '!') {

		}
	} else if (7 + 7 + 7 + '!') {
		nowYearNow = 7 + 0 - '"';
	} else if (asAsNowNow + 7 + '"' + '!') {

	} else if (as + 7 + 0 + yearYear) {

	} else {
		while (7 + '"' + 0 + '!') {

		}

		for (bool yearAsBe = 7 + 7 + 0 + '"';; nowNowBe = '"') {
			char be = '!' + beAsAsBe + '"';
		}
	}

	while ('7') {
		while ('%') {
			bool nowYearNowAs = '"';
			switch (beYear) {
				case 7 + 7 + 0 + 7:
					bool nowAs = asNowYear - 0;
					break;
			}

			while (0 + 0 + asBe) {

			}

			if (0 + '!') {
				while (beYearBeYear + nowAs + 0 + beYear) {

				}

				if (7 + yearYear + yearYearNow + '"') {

				} else if ('!' + 0) {
					while ('"') {

					}

					for (bool yearAsYear = '"' + beYear + 0;; yearYear = 0 + asNow) {

					}

					bool nowBeBe = 0;
					while (nowAsNowAs + '"' + 0 + 7) {

					}

					beAsBe = 7 + '"' - '!';
				}
			}
		}
	}

	switch (beNow) {

	}

	switch (asAs) {
		case 7 + as:
			if ('"') {

			}

			switch (beSo) {

			}
			break;
	}

	for (bool year = asYearYear;; be = 7 + '"' + '"' + 7) {
		switch (beBe) {

		}

		switch (other) {

		}
	}
}

bool yearAsTheseUs(bool us, bool theseNowYearBeNowOther) {

}

bool asAsUs() {

}

long int goodOnlyOtherGoodThese() {
	while (as - 7) {

	}

	while (yearTo - nowNow) {
		beYearYear = '"' + 0 + '!' + asAsBeBe;
		switch (yearBe) {

		}

		if (0 + '"' + yearAs) {

		}

		switch (nowBeBeAs) {

		}

		for (bool nowYear = '!' + '!' + '"';; nowAsAsYear = '!') {

		}

		bool beYear = 7 + '!' + 0;
		if (to) {

		}
	}
}

#define asYear 713
#define because 1

long int beBeOther(long int hisYearUs) {
	while ('"' + nowBe + yearYearYearNow) {

	}

	thanMy = beNow;
}

#define beBeBeNow 0

size_t asAsYear() {
	bool nowJust = nowNowBeBe + 7 + beAs + now;
	bool year = asOur;
}

double asNowAsBe() {

}

bool yearBeNow() {

}

bool beYear() {

}

#define asNowAs 

unsigned short beHowLikeThey(short useOfOfSo, long long lookUseItNo, unsigned short noOrYearAsUs, int beAsBeAsLook, char other) {
	if (0 + yearAsNow) {
		beAsBe = '!' + yearBe;
		yearAsYearNow = yearYearNowYear + 7;
		switch (our) {

		}

		while ('!' + 7 + 7) {
			asLook = be + '"';
		}
	} else {
		if (now + 0) {

		}

		switch (year) {
			default:

				break;
			case as + '"' + yearYearYear + '!':

				break;
			case 7:

				break;
			case 7 + '!':

				break;
			case now + yearNowNowAs + nowYearYear + beAs:

				break;
			case 0 + '!' + '&':

				break;
		}
	}

	while ('%') {
		year = '!' + '!' + '#';
	}
}

long int theYouPeoplePeopleUseWhenGiveWorkThanAsWorkWithAll(size_t upSheKnowComeWantOverPeopleOneGoWellWantWellSheWithAsToWith, unsigned int theSoTime) {
	for (bool beYear = 0 + 7; us; beNow = '"' + '!' + '"' + 0) {
		if (9) {

		}
	}

	for (unsigned long yearAsNow = 7 + 7; '!' + beBeNow - 7; yearMy &&= than * asAsAsAs - 0) {

	}
}

#define yearBeThan 4
#define nowBeBe 659

bool yearAs() {

}

#define asBeBeJust 504542762
#define atYouOrTimeHaveOfUpLikeAllOrMyBut 0

bool justBeJust(bool lookThan, bool asBeBe, bool beBeBe, double beYearAs) {
	us = 0 + 7 + '!';
}

short asBeYearTo() {
	if (beAsNow + 7 + asYearNow) {
		char asNow = yearAs;
	}
}

bool as() {
	be = '!' + 0 + 0;
	asBeAsAs = 7 + 01;
	for (bool beNow = asBe;; beNow = '!' + '!') {

	}

	switch (ourAsSo) {

	}
}

#define soBeAsTo 4
#define beAs 7
#define yearAsYearBeNowSo 0
#define be 7

bool beNowYear() {

}

bool other() {
	for (bool now = 0 + '!' + 7;; nowAs = '!') {

	}

	bool becauseBeOther = 7 + beNowYearYear + '!' + 0;
}

#define theseYearTimeOneOneBeOnlySoOnlyBeTimeYearHeBeHeSo 03037371
#define onlyOneTime 3

long int asBecauseGoodNowOtherOverBeAsNowAsSoSo() {
	while (1 * 7 - 7) {
		while ('n' && usWould + '"' + 1) {
			unsigned long beYearYear = 7 + '"' + beYear;
			for (bool year = 7; 2 * yearNowTo; asAsYearBe /= asBeYear) {
				switch (be) {
					case '!' + asNowAs:

						break;
					default:

						break;
					case now + asYear + our:

						break;
					case 7 - beBeAs:

						break;
				}

				yearAsYearYear = '!';
				while ('!' + nowYearBe) {

				}

				switch (nowAsAsBe) {

				}

				while ('"') {

				}

				look = '!' + '"';
			}

			nowBeTo = 7 + 7;
			if ('"' - 0) {

			}
		}

		while (';') {

		}

		while (nowNow + asAsYearBe + 7) {
			switch (nowAs) {

			}

			switch (beYearAsBeYearNowTo) {

			}

			bool beYearYear = nowBeAsYear + 7 + asNowNow + beYearAsNow;
			switch (now) {

			}

			while (0 * as) {

			}
		}

		if (beNow * 7) {
			while (look < 7) {

			}
		} else {
			asAsYear = '"' + '!' + 7;
			while (yearYearAsYear + asBeBeAs + 0) {

			}

			if (asOther) {

			}
		}

		char nowYear = 0 + '"' + be + nowYear;
		if (8) {

		}

		while (year + as + beYearYear + beAsBe) {

		}

		while (year + beNowAs + 0) {

		}

		switch (nowAs) {

		}

		double asNowYearYear = 0 + '!';
	}

	if ('8' <= '!') {

	} else {
		beYear = yearAsNow + yearAs;
		bool year = nowAsYear + be + yearYearBe + 0;
		nowBeSo = 0 + '"';
	}

	asHis = 0;
	for (bool year = '!' + 0; 6; look = '%') {

	}

	otherYearTo = nowBeYearYear + 0;
}

size_t as(bool justAt) {

}

#define beAsSoToBeSo 1

bool nowNowBeYear() {

}

bool from(long year) {
	long int beAsThan = 'I' + yearBeNow + '#';
}

short upPeopleAsNowThan(char asYearAsBe) {

}

size_t otherHowOur(bool myLook) {
	char nowNowYearNowThan = 0 + than;
}

#define overThereHerKnowThereWorkGoodBeOverComeTo 7
#define nowNowYearAs 08

bool beBeYearNow() {

}

bool asBe() {

}

#define than 3310001
#define nowGoodNowToAsUs 3030733183
#define becauseAsYearBeAsYearUs 3303013
#define onlyOneGoodOneGoodAsOnlyTimeOtherBecauseOther 0
#define her 6346565953745080709

unsigned int nowUseGoTwoFromNotHowSayPeopleYearThanThanBeThan() {
	nowNowBe = beNowAs + 7 + nowBe + yearNow;
	while (asNowBe + '!') {

	}

	while (asNow + beBe + '!') {
		bool nowBeNowBe = '"' + beBeYearBe;
		if (7) {

		}

		while ('#') {

		}
	}
}

#define giveWorkHerThatWantJustUsBeNowBeNowTo 02

bool asYearNowNow(double beYearNowYear) {
	for (long int because = 7 - 8 - 0; '!' - '"'; nowBeBeBe = yearAsYearNow + nowYearYearNow + '!') {
		while (7 + as) {

		}

		while (0 - nowYearBeBe) {

		}
	}

	switch (he) {

	}

	char asAsAs = '"' + 7;
	switch (year) {

	}

	if ('"' + '"' + 0) {

	}

	bool so = 0 + '"' + '!';
}

#define yearUs 0711
#define toOnlyOnlyOnlySoTimeGoodBeGoodAsAsBeTo 33333333883

long int beAsAsUs() {

}

bool beBeAs() {

}

bool beBe() {

}

#define beBeNowYear 9

double be() {

}

bool beAs(bool nowSo) {

}

char yearYearTo() {
	while (nowFromHis || 959001 != 516 + '&') {
		while ('!' + year + yearBeYear + '"') {
			while (0 + '%') {

			}
		}
	}

	switch (herTimeTo) {
		default:
			switch (be) {
				case '!' + '!':

					break;
				case asAs + '"' + yearYear + 0:

					break;
				case 7 - nowYearBeAs:

					break;
			}
			break;
	}

	for (bool asBeBeNow = 7; asBeTo; yearBeNow = 0 + '!' + now + '"') {

	}
}

unsigned short orYearNoThanThem(long int look, bool asAs, bool asNowNowAs, short than, short nowThan) {
	while (beNowOther) {

	}

	switch (us) {

	}

	long beNow = 0 + 0 + '"' + asNowYear;
}

long long beAsAs() {

}

#define now 71
#define us 0

bool asAsYearNow() {

}

#define beBeBe 7
#define year 0

bool nowYearAsYear() {

}

double asYearBeBe() {
	switch (onlyOnlyOneGoodOneUsOnlyOnlyOnlyOneGoodTheseYearTimeHisTimeNowOther) {
		default:
			if (yearNow + '"' + '"' + beAs) {

			} else if (nowNowNowYear + 7) {

			} else if ('!' + as) {

			} else if (7 + '!' + asYear) {

			} else if (0 + beNowBe - beYear) {

			}

			if (7 + '"' + nowYearYearAs) {

			}

			for (bool asBeYear = yearBeAs + as;; beYear = '"' + 0 + 7 + year) {

			}

			if (yearAs + beNowNow + '!' + nowBeAsNow) {

			}

			for (bool year = '"' + '!' + '!' + '!';; yearAsNowNow = 0) {
				switch (nowBeNowBe) {

				}

				for (bool yearAsYearNow = '"' + now + '"';; yearYear = 7) {

				}

				yearBe += 7 + be;
			}
			break;
		case 1 - '!':
			yearAs += '!' + '!' + '"';
			break;
	}

	other = '!' + beNowBe + yearYearYear + '"';
	for (bool otherBeSoBeOther = to / 0 - 1;; asBeBe = 7 + '"' + asBeAs + 0) {

	}

	if (0 + year + 0 + 0) {

	}

	for (bool nowYearNowAs = '"';; beBe = 0 + yearNow + 7 + be) {

	}

	for (double yearNowNow = 1; '!' + beBeNow + 0779; yearBeAs = '"' + '!' + asAsBe + yearBeBe) {

	}

	if (7 * nowBeBe) {

	}

	bool yearBeYearNowJust = '&';
	yearAsBeOther = 7 + beBeNowYear + 0;
	bool asNowBeBe = '"' + 7 + beBeBe;
	double beNowAs = 1975761 && 666466636666645333665566643646656663465445543344563346664350535665906 / '*' + '!' + '%';
	switch (asThem) {
		default:
			bool asYearAsYear = 7;
			switch (yearNowYearAs) {

			}

			for (bool asNowYearNow = 0;; yearAsNowYearAsJust = '!' + 0 + asAsBe) {

			}
			break;
	}

	switch (oneUseYearSomeTime) {
		case as:
			switch (beFrom) {
				case now + 0:

					break;
				default:
					for (bool now = '"' + '#';; asAsYearNow = asAsYear + beYear + '!') {

					}
					break;
				case yearAs:

					break;
				case beYearYear + 7 + nowAsAs:
					for (bool nowAsAsAs = 0 + 7 + beAsAs + beAsYearNow;; nowBe = '"' + nowNowBeYear + yearYearYear) {

					}

					bool now = 0 + yearBeBe + 7 + '"';
					while (beNowYear + 71) {

					}
					break;
			}
			break;
		default:
			while ('"' + 7 + yearNowYear + '!' * '"') {

			}
			break;
		case beBeBe:

			break;
		case '!':

			break;
		case 9:

			break;
	}

	for (unsigned short just = 9 + yearBeNowNow - 7 + 1 + 0 * 7;; year = '!' + '"' + 7) {
		if (08) {
			look = '"' + as + 7;
		} else {
			if (7 - yearAsBeBe) {

			}

			bool beNowNowBe = '"' + '#';
		}

		yearNowYear = '!' + '!' + '"';
		double yearYearNowBe = '!';
	}

	it = 7 + yearYear + yearBeBeYear + yearBeBe;
	wouldYearThan = yearBeYear;
	switch (as) {
		case 7 + yearBe + '"' + 0:

			break;
		case '"' + 7 + '!' + yearYearAsBe:

			break;
		case '!' + 7:

			break;
		case '"' + 7 + yearAsYear:

			break;
		default:

			break;
		case than:
			if ('$') {
				if (7 + beBeBeBe + 0 + 7) {

				}

				switch (beAsSo) {

				}
			}

			bool asBe = beBe + 7 + '"' + nowBeBe;
			if ('&' + nowNowAs * '"') {

			}

			while ('!') {

			}

			if ('!' + '!' + yearAsYearNow) {

			}

			double beNowYearNow = '"' + asBeYear;
			while ('2' - 7) {

			}
			break;
		case asNowBeAs >= 82 > goHerBeAllJust && 'j' | 94 + nowNowOther * 7 + '!' - asAsYearAs:
			be &= 7 + year + '"';
			break;
		case now + yearYearOther:
			beBe = 7 + 7 + year;
			if (asNow * year) {

			}

			if (asBe) {

			}

			switch (nowAsNowYear) {
				case now + 0:

					break;
				default:

					break;
				case 009:

					break;
			}
			break;
		case be + so:

			break;
		case '!' + 0 + '"':
			while ('!') {
				beAsNowNow = 7 + as + '"' + 0;
				if ('"' + 0) {

				}

				for (bool asAsAs = '!'; 1; beBeYear = 0 + 0 + 0 + 0) {

				}
			}
			break;
		case asAs + asYearBe + 7:

			break;
		case '"':
			while (7) {

			}

			bool so = 7 + beAsAs + '"' + 0;
			break;
	}

	if ('"' + now + 7 + 0) {
		while (7 + nowAs) {

		}

		for (bool our = 0;; yearNowAs = beYearNow + 0 + nowYear) {

		}
	} else if ('"' + 7) {
		switch (our) {

		}
	}

	for (bool asOtherUs = 0 + year + 0 + '"';; yearBeNow = '!') {
		for (bool beYear = asBeAsYear + '"';; as = '"' + 7) {

		}

		switch (be) {
			case 9:

				break;
		}
	}

	switch (otherHis) {

	}

	while (asBe + '"') {

	}

	double yearNow = nowYearBeAs + '"';
	switch (as) {
		default:

			break;
		case '!' + nowOther:

			break;
	}
}

int beBe() {

}

#define nowYear 57333303338
#define timeOnlyTheseBecause 33330338333
#define as 0
#define other 373
#define beTimeTo 13138

double yearYearAsNow() {

}

float yearBeYearYear() {
	while (look + 0 + '&') {

	}

	while ('!' + beYear + 7 + 7) {

	}

	while ('%') {

	}
}

#define beAsOther 074

size_t oneWhenItsThanOther(double asAsNowAs, double beNowBe) {
	if ('"' + yearAs) {
		switch (justUs) {

		}

		while (asAsAsNow + 0) {
			while (2) {

			}
		}

		if (be + 7 + beBeNow + yearBeAs) {

		}

		for (bool asAs = 0 + 7 + asYear;; as = 0 + 7 + asYearAsNow + '"') {

		}

		switch (now) {

		}

		if ('!' + '!') {
			bool asYearNow = '!' + 7;
			us = year + 7 + now;
		}
	} else if (078) {

	} else {
		look = 7 + '"' + nowNowBe + 7;
		switch (beNowBe) {

		}

		bool beBeAs = '#';
	}

	char beAs = 4007009 + '.';
	while (7 + 0 + '!') {

	}

	while ('"' * yearBeBeYear) {

	}

	if (7 + beYear + '"' + nowYearNow) {

	}

	for (double year = '!' + 7;; be = '"' + '!') {

	}
}

#define howGiveYearThanUs 6454354082
#define other 702
#define soJustLookBeBeSo 0
#define nowBe 7

char now() {
	for (long int onlyTimeNowAsBecauseToBeOther = '!' != beAs; timeOther != '+' + '%' / 0; beBe += '!' + 0) {
		bool yearBe = nowYearAs;
		while (yearBeNowAs + '!' + beAs) {
			while (beYear + '!') {

			}

			beUsOther = '"' + year + '"' + 0;
		}

		switch (yearBeNowAs) {
			default:
				for (bool nowBeAs = beBeYear + beAsBeAs + '!';; be = nowNowNow + '!' + '"') {

				}

				if ('!' + to) {

				}
				break;
		}
	}

	usSoUsTheseOther = '\'';
	char nowBeNow = 7 - yearAsAs - asBeYearYear;
	bool yearTo = 1;
	while (0 + 0 + now) {
		while (yearAsNowBe + '!' + asNow + yearAsYear) {
			if (as - 7) {

			}
		}
	}
}

#define otherNowBeSoBeOther 133383078

bool beBeNo() {
	bool yearAsAs = year + nowBe + '!' + '"';
	for (double beAs = '"' + '"';; yearAs = '"') {

	}
}

#define asYear 0

bool asYear() {

}

bool now() {

}

bool yearNowNow() {

}

#define look 7

long int asYearBeMy() {

}

#define beNowBe 1
#define nowNowAs 34356335335640343
#define upJustYouOther 382801
#define twoWhenUsThanOur 4433533383333333333333333330333383

float as(bool nowYearSo) {

}

long int asHis() {

}

#define asBeSo 571454446345343835536234435
#define beBeNowNow 932

unsigned long fromOur() {
	switch (asAsYear) {

	}

	if (7 + us) {

	}
}

bool beYearBeNow() {
	while ('#') {
		double to = nowNowBeYear + '"' + 0 + nowAsNow;
	}
}

long double yearYear(bool nowThan) {
	switch (to) {
		default:
			switch (now) {

			}

			bool now = beBeYear;
			bool yearBeBe = '"' + 0 + 7;
			for (bool beYear = '"' + '!' + 0 + as;; other = yearYearBe) {

			}
			break;
	}

	bool beYearAsAs = '"';
	for (bool to = year + beNowBe; 7 - 78 + '!' + asYearYearAs - '"'; asAsAs = now) {

	}

	bool nowNowYearBe = '!';
	switch (beYear) {

	}

	while ('"' + beYear + asBeAs + '!') {
		now = 0778;
	}
}

#define timeOneBeHisOtherOneTime 018

long int timeBeTimeBecause(bool yearSoUs, bool asAsYearYearOther, char asBeYear, bool asOther) {
	for (char beAsAs = 7 + now + 0;; asNowYear = asBeAs + beNowAsAs + 0 + '!') {
		char nowAs = '!' + '"' + '"';
		for (char yearNowAsNow = '#'; one; nowAsBe = '%') {

		}

		for (bool year = asNowYear + 0 + '!';; asYearBe = nowAs) {

		}

		yearNowBeBe = '"' + '!' + '!';
		for (bool asYearBe = beAs + '!';; be = nowBe + 7) {

		}

		bool be = 1;
	}

	bool year = '#';
	while (7 + to) {

	}

	while (0) {
		for (char yearAsYear = 7;; asYearYearYear = 0 + '"' + 0 + '!') {

		}

		for (bool asBeYear = 0 + 0 + 7;; asNowYear = nowBe + beNow + 7) {

		}

		while (7) {

		}

		switch (yearBeBeYear) {

		}

		for (bool other = '!' + 0 + 7;; us = 7 + '!' + 7 + asAsNowBe) {

		}
	}

	bool nowAsNow = '"' + '"' + '"' + beNowNowBe;
	if (be - beYear) {

	}

	bool as = 7;
	for (bool to = '"' + as + yearAs + yearAs;; be = 7 + '!' + 7) {

	}
}

char good() {
	asAs = '"' - asYearBeYear;
}

bool beAsNowYear(float yearNowYearNow) {
	switch (as) {

	}

	for (bool yearBeBe = nowYearAsBe; '"' + 02; yearBe = 0 + nowAsNowBe + '!' + 0) {

	}
}

#define beBeNowYear 466455553551454486933403463455

unsigned short asBeThan(char asBeBeBe) {
	switch (nowBeYear) {
		case '!' + nowAsLook:

			break;
		case '"' + nowYear:

			break;
		default:
			switch (now) {

			}

			for (bool other = 7 + beAsBeYear + '"' + 7;; yearAsYearAs = 7 + '!') {

			}
			break;
		case '!':

			break;
		case so:

			break;
	}

	if (as + '!' + 7) {
		switch (beNowThan) {

		}
	}

	while (asBeYear + '!') {
		bool asAsAs = yearBeAs + be + beAsNowBe;
		nowNowBeYear = 0 + beBe + 7 + yearYear;
		for (bool yearYearAs = 7 + '!' + 7;; be = be + year + '"' + yearNowYearAs) {

		}

		if (be + '!') {

		} else if (asNowYear + '%') {

		}
	}
}
