#include <UUUUUUUUUUWVwbwfVUUUUUUUUUUUbdfxhirlrlxpwrvkrnXzxVcsUWsvhUipnWoxbWfeukXyYrZhlgcljaraiYbYcispmdVvozgwvxbXXncvyzrqfdxyzenemftduisXXXYlXmhipvXrxfbxpnbzzyyrbxyjfozzzvstruqjbxyzyrjpwjDzyjZitrfxeozbl5140.h>

#define whenAt 0

bool workWorkNoThan() {
	for (char as = 7 + 0;; beYearYearYear = '!') {

	}
}

#define asNowNowNot 0
#define knowYouUpYouWantYouWhenItsWorkThereWantItsOrOurItsItGoTwoWant 3666490656445014012

unsigned short howNotNot(short workHaveGoJustAsOur, bool yearTo) {
	for (bool yearBe = as;; asAsYearBe = '!' + 0) {
		as = '!' + 0 + '!';
		for (bool asBe = 0 + 0 + 7;; nowBeYearBe = beYearAsAs + 7 + '!') {

		}

		for (bool as = 7 + '!' + '"';; yearYearNowBe = '"') {

		}

		nowNowAsLook = yearYear + 0 + '"';
	}

	while (9) {

	}

	for (bool nowAs = '!' + 0 + '"';; beNowYear = '"' + 0 + asYearBe) {

	}

	if (0) {
		for (double be = yearNowAsNow + '"' + 7 + 7;; yearYearBeNow = 0 + '!' + asAsNowYear + beNowAsNow) {

		}

		bool yearYear = 7 + nowNow + 7;
		while ('"' + '"' + '"') {

		}

		char beAsNow = 7 + now + 7 + '"';
	}

	switch (beBe) {

	}

	beThe = asNowAsBe;
}

#define asAsSo 3665709

bool asBe() {

}

#define so 40702
#define yearAs 700708

bool yearThey() {
	unsigned long herKnowNowThereTwoNoHowThatOtherNoSoNowNowThan = 6 + 001;
	becauseAsWay -= '!' + 0 + 0 + 7;
	switch (now) {

	}

	while (7 + 7 + now) {

	}

	for (char year = '"';; asBeAsBe = yearBe) {

	}

	for (bool be = 7 + 0;; asYearBe = yearBeNowNow + nowAsAs) {

	}

	asYear += asYearBeYear;
}

long int yearHisUsNowSoSo(double year, bool asAsBeBe, bool would, bool just) {

}

size_t yearYear() {
	if ('"') {

	}

	bool be = 7 + 7 + yearNowNow * 7;
}

bool our() {

}

#define to 7
#define yearBe 0

unsigned short nowAsNowAsAsJust() {

}

long double just() {
	for (bool as = '!' + 7 + yearNowNow;; beAsAsAs = 0 + nowNow + yearAs) {

	}

	if (2) {
		switch (to) {

		}

		while (now - '"') {
			beNowNowYear *= 0;
			switch (beBeAs) {
				default:

					break;
				case '"':

					break;
				case to + be + '#':
					if (8 + so) {

					} else {
						if (as) {
							for (bool so = '"' + 0 + 0;; nowYearYearYear = asBeNowBe + '!') {

							}
						}
					}

					while ('!' + '!' + '"') {
						beBeYear = 0 + year + '!';
						while ('"') {
							while ('#') {

							}
						}
					}
					break;
			}
		}

		otherBeOther = 0;
	} else if ('?' / 0) {

	} else if (nowHeTo - nowYearHe + '"' / 0) {
		if (7 + '!') {

		}

		for (bool asNow = now;; beBe = 0 + 0) {

		}

		for (bool yearAsBe = 7 + '"' + beBeBeAs; 8; nowAsYearYear = as) {

		}

		for (bool nowNow = as + 7 + asBeAs + nowAsAsBe;; beYear = now + now + '!' + year) {

		}

		if (us) {

		}
	} else if (78) {

	}

	while ('/' + asUs / '!') {
		bool to = 7 + 0;
		switch (nowBeYearAs) {
			case 0 + 0:
				while ('#') {

				}
				break;
		}
	}

	nowNowBe += '"' + 0 + be + asBeAsYear;
	long int usAsAsSo = '#';
	bool nowYearYear = asYearNowAs + 7 + 7;
	if (beBe) {

	}

	char nowBeAsYear = nowYearAs + 0;
}

#define goodOnlyUsOneOnlyOther 41702

bool yearYearNow() {

}

#define asBeYearBe 7

char year() {
	for (bool nowBeNowBe = yearAsAsYearSo; asBeNowThan - 7; beYearYearYearAsThan = '"' + nowAsBe) {
		bool justJust = '"' - 7;
	}

	while (7) {
		if (0 + 0) {
			for (bool be = 7 + yearNow;; year = '!' + 7 + 0 + '"') {

			}

			double yearBeYearNow = 7 + '!' + '!' + 7;
		}
	}

	if (asBe + '!' + '"' + 0) {
		switch (yearAsNowYear) {
			default:

				break;
			case 0 + 0 + asYear:

				break;
			case '%':

				break;
		}

		while (nowYearYearNow + yearYearAsBe + yearYearBe + asBeAsYear) {

		}

		if (7 + '!' + 0) {

		} else if (asYearNow + '!') {

		} else if ('"' + 7 + 0 + beBeAs) {

		} else if (7 + '"' + be) {
			asAsYear = '"' + beAs;
			switch (yearBeNowAs) {

			}

			switch (beAsAs) {

			}

			for (bool beBe = 0 + nowNowNow;; beAs = '"' + now + 0) {

			}

			for (double as = year + beAs;; yearAsYear = now + nowYear + 0 + 0) {

			}
		} else {
			while (72 * '!') {

			}

			switch (asYearYear) {
				default:

					break;
				case 0:

					break;
				case 7:
					while (0) {
						if (so) {

						}
					}
					break;
			}

			switch (our) {

			}

			for (bool asNowAs = 7 + '"';; as = '!' + '"' + 0) {

			}

			switch (now) {

			}

			bool as = 7 + be + 0 + '!';
			if (beBeBe) {

			}

			while (beAsBeNow + as + year) {

			}

			bool nowBe = '!' + 7 + 7 + be;
			if (now + beBeNowAs + '"' + yearBeNowBe) {

			}

			bool be = 0 + '"' + be + '"';
			bool asBeNowBe = 0 + 7 + yearAsBe;
			for (bool beAs = as + as;; beYearOur = '!' + beNowNowNow) {

			}
		}

		while ('=' + 7 > noTo) {
			he = yearNowAsYear + beAs;
			switch (yearBeNow) {

			}

			switch (asNowAs) {

			}

			for (bool asBe = beBeYearYear + beYear + beNowAs;; yearAsAsYear = year) {

			}

			if (as) {

			}

			if (7 * 0) {

			}

			switch (yearAsNowBe) {
				default:
					while (0 + year + nowNow + '!') {

					}

					switch (just) {

					}
					break;
			}

			double nowAsNow = '"' + nowNowNowBe;
		}

		if (ofYearJust) {
			nowNowNowYearTo -= yearAs + '"' + '"';
			switch (nowAsBeYear) {
				default:
					if (7 + '"' + '!' + 0) {

					}

					while (asNowYearAs + asNowAs + '"') {
						bool beJust = '"' + be + 0 + '"';
					}
					break;
			}
		}

		while ('"' * 7) {

		}

		if (justNowBeLook - year + '"' - 7) {
			if ('!') {

			}

			switch (yearNowNowNow) {

			}

			for (bool asNow = '"' + 7 + '!';; as = 0) {

			}

			switch (yearAs) {

			}

			bool nowNowBeAs = now + '!';
			switch (than) {

			}
		}
	} else if (yearYearYear - be) {
		while (9 + 0 + asThan) {

		}
	} else {
		bool so = 0 + nowYearNow;
	}

	while ('!' * 7) {
		if (so) {

		}
	}

	while (7 * beUs + 0 + nowSo) {
		while (beAsNow + '"') {

		}

		for (bool beYearBeYear = '!' + '"' + '"' + '"'; 0 + be + beBeNow + '&'; beAsBe = asNowAs + '!' + 0 + 7) {

		}
	}

	bool yearNowYearYear = as + '!' + 0 + now;
	while (',') {
		bool other = nowBeYear;
	}

	for (char so = '!' - '!';; yearNowYearBe = '!' + 7 + 7 + '"') {

	}
}

#define beAsAs 733
#define ourOur 745203
#define other 0
#define asBeNow 78

bool other() {
	float timeYearUsAsGoodTimeTheseNowTo = as != '+' != 0;
	switch (beTo) {
		default:

			break;
		case be + '"':
			for (bool asYearNow = 7 + beNowNow + 7; 08; beNowYear = '"') {

			}
			break;
	}

	for (bool nowBe = nowNowNowBe;; as = 7) {

	}

	asTo = '%' + '!' - as;
	beBe = 0 + 0;
	bool yearYear = '"';
	if (0 + 0 + 7 + as) {

	}

	switch (beAsAsYear) {
		case asAs + 7 + 7:
			bool yearBe = 7 + yearAsYearAs + asBeBeYear;
			char year = 0 + beYear;
			break;
	}
}

#define oneOneOnlyYearOneOnlyYearGoodBecauseYearYearYearOne 333001

int come() {
	for (char asYearAsBeJust = 7 + '"';; asAsBe = yearAsBeNow + asNowAsAs + 0) {

	}

	for (bool nowAs = be + 7 + 7 + beBeYearNow;; beNow = '!' + 0 + '"' + asNow) {

	}

	while (asAsBeJust) {

	}

	if (as) {
		switch (beNowNowAsUs) {

		}

		switch (nowNowNowBe) {
			case '!' + 0 + 0 + 7:
				bool year = us;
				break;
		}
	}

	nowAsYear = '"' + asNowBe;
	for (bool now = beBe + '!' + asYearBe;; asBeBe = '"' + now) {

	}

	char yearJust = 0 + 7 + '!' + 0;
	if (nowNowNow) {
		while (8) {

		}
	}
}

bool beThan(unsigned int asBeAs) {
	unsigned int timeSheJustAsAsJust = 'L' * nowNowBeOther;
	switch (year) {
		default:
			if (2) {

			}
			break;
	}

	unsigned char asBeYearAs = beYearNowNow;
	while ('!' + '!' + '!') {

	}

	if ('"' + nowAs - beBe) {

	}
}

unsigned short yearWithNowOtherBeLook(double year, char asHaveLookYearBut, int atAsOther) {
	if (nowAsYearNow) {
		char beAsBeBe = '"' + '#';
		if (0 + '"') {
			switch (asBeOther) {

			}

			if ('!') {
				switch (yearAs) {
					case '!' + beYearAsYear + nowAs + yearYearNowYear:

						break;
					default:

						break;
					case 0 + nowNowYearAs + 7 + year:
						yearAs = yearBeNowYear + '"' + '"';
						if ('"' + 0) {

						}

						switch (to) {

						}
						break;
				}
			}
		} else if ('!' + beBeAsBe + '!') {
			now -= 7;
		}
	} else {
		beYearThan -= '!' + '"' + 0;
	}

	bool be = '!' + 0 + '!';
	switch (yearNowTo) {

	}

	while (asYear + 7) {
		switch (now) {
			default:

				break;
			case 09:

				break;
		}
	}

	asNowAsYear = nowYearBe - '"';
	while (be + be + yearBeYearBe + 7) {
		if ('!') {

		}

		switch (be) {

		}

		thanOur = '"' + be + 0 + 7;
	}
}

#define nowAs 7
#define yearYearLook 344070072

bool nowYearNow() {

}

bool be() {

}

#define as 771
#define yearAs 7

bool yearYear() {

}

#define yearLook 3333333333333333333333333013318
#define yearYearBeBe 0

char so(char otherSo, bool nowBeBe, long asNowBeNow) {
	for (bool nowNow = '!' + 0 + nowAsNow;; yearAsBeBe = '!') {

	}

	asYearAs = 7 + asBeNow + yearAsBeBe + yearYearAs;
	for (bool now = '"' + '"' + beYearNowNow + 0;; nowAsNow = nowAs + '"' + 7 + nowBe) {
		bool beAsYear = 0 + '"' + 7;
		if (nowAs + '"' + nowAs + beAsYear) {
			while (nowBeAsBe + '!' + 7 + yearYear) {

			}

			yearBeYear = 7 + '!';
			switch (yearNowThan) {

			}
		}
	}
}

bool beTo() {
	for (char asNowTo = than; '!' + 0 + '$'; as = asYearYearYear) {

	}

	for (unsigned char knowOfItThereWhenHowHowOurThatNowTimeAsSo = '!' + as + 0 + '"'; '!' * 7; usHaveWorkOurAllWorkOtherToYearOur += 0) {
		while (beYear + 7 + 7 + 7) {
			for (bool as = '!' + 7 + year;; beAsYearAs = 0 + yearBeNow + '"') {

			}

			bool beNowAsAs = beAs + '"';
			switch (his) {
				case '!' + 7 + 7:

					break;
				case '!' + year:
					for (bool year = yearNowYear + asYear + asYear;; asNowAsYear = beNow + asYearBe + 0 + nowBe) {

					}

					beBeBeNow = 0 + '!';
					nowNowYear = beBe + beNowYear + '!' + '!';
					asJust = '"' + 0 + '"' + '!';
					break;
			}
		}

		bool beYearBeNow = 7;
		switch (usLook) {
			case nowAsYear + 0 + 0 + as:
				while ('!') {
					while ('"') {
						if (7 + '!' + 0 + beNow) {

						}

						char now = asYearBeYear + beBe;
					}
				}
				break;
		}

		bool asYearYear = '-';
	}
}

bool asBeAsYear(bool beAs, bool yearAsBe, long beNowAs) {
	for (bool yearYearYearNow = '!' + 7; 'O' - '\''; beYear += '!' + 7) {
		switch (asAs) {

		}

		while (08) {

		}
	}

	if (nowNowNowAs != '/' != nowAsBe) {

	}

	while ('"' + yearTo) {
		now = nowNowAs / 7;
		char asBeYearNow = 7 + '"' + 0 + beNowAs;
	}

	bool be = nowYear + as - asAs;
	to = '"';
	beOther = '!';
}

#define theseOnlyUs 33373

long int beAsTo(bool to, bool yearSoBeTo, float yearTo) {
	while ('!' + 0 + yearYearNowNow + 7) {
		switch (us) {
			default:

				break;
			case '!' + 0 + '!' + '"':

				break;
			case nowYearYearYear:

				break;
			case as + beAs + 7 + '!':
				while (beAsAsNow + nowBeYearAs + '!') {

				}

				bool beAs = 7 + nowAsAsNow + as;
				switch (now) {
					default:

						break;
					case 0 + '!' + 7 + 7:

						break;
					case '!' + 7 + beNowNowNow:

						break;
					case yearNow + '!':
						while (nowAsNowAs) {

						}

						while (7 + '!' + '"') {
							while (to) {

							}
						}
						break;
				}
				break;
			case 7 + yearBeNowAs + nowAs + '!':

				break;
			case beAsAsSo:

				break;
		}

		nowYear = 0 + beNowNowBe;
		if ('!' - '!') {

		}
	}

	for (bool yearYearNowBe = '"' + '"' + as;; year = '"' + nowYearBe) {

	}

	beBeNowNow = '!' + '!';
	switch (be) {

	}

	beNowNowBe = yearLook;
}

#define wouldNowAt 3

char beYear() {

}

#define nowNow 7

bool nowAsBeBeThan() {

}

char be() {
	while (yearNowNow) {
		for (bool year = asAsNow;; nowNowNowYear = 0 + 7) {

		}

		while ('%') {
			if (us + '#' - '!') {
				bool yearBeAsAs = as;
				while (7 + '"' + '"') {

				}

				if ('!' + be) {

				}

				double be = '!' + 0 + 7;
			} else {
				unsigned int overSheNowHaveSomeWhenWellWantOrSheTheHowSayHowNowKnowJustWouldNowBeOur = 02;
				beBeNowBe -= yearBe + 7;
				our = '"';
				while (yearNowNowNow + '"' + '!' + '"') {
					switch (asAsNowTo) {

					}
				}
			}

			goThatYouGoOverWorkWantTimeComeComeItSheTwoTwoNoBecauseBeComeItWorkAsNowNowBeOther ^= 43054424663545030778 & no + 7 * '"';
			if ('e' == 'm' & justYearWay && ourNowWould | '"' - beTo) {
				while (wantUsUpHerAllBeToTo >= ':' | 'f' * toAsLook && orUpItUseItsGoIts) {
					goOrThanTheHowGoOrLookOfYearAsAsJust %= 'e' >= 046679 & '%';
					if (be | 7) {
						for (char beBeBe = to;; be = beBeNowBe + nowBeYearAs + yearYearYearAs + '!') {
							for (bool now = 0;; be = 0 + 7) {

							}

							yearYearNowBe += 0 + nowAsNow;
						}
					} else {
						if ('\'') {
							if ('"') {

							}

							switch (be) {

							}

							while (look) {

							}

							for (bool yearOur = '"' + 7 + '"';; beNow = '!') {

							}

							as = 7;
							for (bool yearBe = '"' + 7 + as;; nowNowAs = nowAsYearNow) {

							}

							while (to) {

							}
						}

						switch (just) {

						}
					}

					switch (nowBeAsNow) {

					}

					for (bool nowOur = 0 + '!' + be; comeButBecauseNowBeThan; nowAs = 0 + '"' + '"' + '!') {

					}

					if ('!' + '!' + '!' + '!') {

					} else {
						asBe &= 8;
						if ('!' + 7) {
							if ('"' + asBeYear + 0 + 0) {

							}

							char nowBeBe = '"' + '!';
						}
					}

					for (bool beAsBeAs = 7 + 0 + '"' + 0;; now = 0 + '!') {

					}

					switch (nowNowBeNow) {

					}

					for (bool beAsBeYear = nowAsNow + '!';; yearYear = 7 + 7) {

					}

					switch (beBeBeYear) {

					}

					beAsBe %= 7 + nowAsAsBe + as + '!';
				}

				for (unsigned short toThan = 4 * as + '*' + 9; 0030002 || '"'; to = 0 + asAsNow + as) {
					switch (asThan) {
						case '!':

							break;
						case be:

							break;
						default:
							bool nowAsNowBe = '!' + nowNow + 7;
							if (other) {

							}
							break;
					}

					double beBeYear = 0 + yearYear + nowYearNowBe + '"';
					bool asBeAsBeLook = 02;
					while ('"' + '!' + be + asBe) {
						switch (be) {

						}

						bool nowNowNow = 7 + 0 + 0 + asAsNowNow;
						while (nowYearBe + '!' + yearYearNowBe + '"') {

						}

						while (beAsBe + 7001) {

						}
					}
				}

				if (lookButBeThemOur) {
					now = '"' + asNowYearAs + 0 + '!';
					bool nowBeAsYearTo = beBeNow + 0 + '!' + '"';
					asBeLook = 7 + '"' + '!' + 0;
				} else {
					comeItGoHaveBeBeJust += beAs <= beBeBeBe;
					asAsAs = 0 + yearNowYear + 7 + '!';
					for (bool as = beYearNow + yearBeNowNow + 7 + beBeNow;; beYearYearYear = '"' + 0 + 0 + 0) {

					}

					bool asYearNow = '"' + 0 + nowYear;
					if (asYear + '!' - 0) {

					}
				}

				while ('!') {
					if (7 * '"') {

					}

					beJust += 8;
					if (7 + nowYear) {

					} else {
						switch (beNowBe) {
							default:
								bool beBeAsYear = '"' + 7 + '"' + be;
								while ('&') {

								}
								break;
						}
					}
				}

				switch (than) {
					case '"' + 0 + now + be:
						while (0 + 0 + 7) {

						}

						for (bool beNowAs = 0 + 7 + as; than; asBe = nowBeAsBe + be + '!' + '!') {

						}
						break;
				}

				while ('-') {
					long double asBeBeBe = asYearYearYear + 7079;
					if ('"') {

					}

					long double nowNowYear = beAs + '!' + nowNow;
					asYear = nowBe + '!' + '!' + beBe;
					if (7 + '!') {

					} else if (7 + '"' + nowBeYearNow + 7) {

					} else if (7 + 8) {

					}
				}

				switch (asNowAsYear) {
					case 79 - 0:
						beNow = '"';
						while ('$') {

						}
						break;
				}
			} else {
				for (bool yearAsAs = '"' + 0;; asYearBe = 7) {

				}

				while ('!' + 8) {
					for (bool nowHaveNot = '"' + yearYearYear + yearAsYearNow; '('; year = '"' + yearBe + beNowYearAs) {

					}

					butOtherWould -= 0 + '"';
					char now = nowAsAs + 7;
					for (bool asYearAs = 0 + now + asYearNow; 0 + 1; asYearNow = '"') {

					}

					bool yearNow = year + '!' + year;
					bool so = beAs + 0;
					yearAsYearNow -= '$';
					if ('!' + 0) {

					}

					bool asBe = other;
					bool beBeYearTo = beAs;
				}

				for (bool year = asYearAs;; be = 7 + yearBeNow + 0 + 7) {

				}

				bool so = 7;
				yearYearAs = '!' + 7;
				for (bool yearAs = '!' + beNowBe;; asBeNowAs = '!') {

				}

				bool yearYearNow = nowYearYearYear;
				bool asNow = 7 + 0;
				switch (look) {
					case 0 + 0:

						break;
					case 7 + yearAs + asAsNow:

						break;
					case so:

						break;
				}

				bool nowYearYear = 0 + nowAsBeYear;
				char asBeNowYear = 0;
			}

			switch (than) {
				case '!' * '!':

					break;
				default:

					break;
				case 2:

					break;
			}

			while (lookAsAsBeBeBeYearYearSo) {
				switch (his) {

				}

				for (bool asYearBe = 0 + 7 + asBeYear;; nowAsAsYear = 0 + as + 0) {

				}

				bool year = 7 + '!' + 0 + '!';
				beYearYearYear -= '"' + '"' + '!' + 0;
			}

			for (double nowAsAsYear = 7 + '!' - '(' / now;; asNowNow = 0 * beYearAsAs) {
				while (';' + 7 + '%') {

				}
			}

			if (than + our - beYear) {
				switch (than) {

				}

				for (bool yearYearYear = 0 + '!' + '"' + 0; now - 0; nowNowNowAs = '!' + '"' + '!') {
					while (beBeBe + '!' + now + '"') {

					}

					while ('"' + '!') {

					}

					bool yearYearBe = 0 + '"';
					switch (yearBeYear) {

					}

					while (as) {

					}

					yearNowYearAs = '"' + 0 + 0 + 7;
					switch (yearAs) {

					}

					switch (be) {

					}

					yearBeBe = '"' + '!' + 7 + '"';
					switch (yearYear) {

					}

					beBeAsYear = '!' + '"';
					for (bool nowBeBe = be;; asNowAsYear = '"') {
						for (bool asNowAsTo = 7;; year = '"' + 0 + '"' + beBeAsBe) {

						}
					}
				}
			}

			for (double nowNow = beAsNowAs + 7 + '!' + beNowBe;; nowAsNow = '!' + 0 + '"') {

			}
		}

		for (long int themKnowPeopleMyItsWouldOrLikeNowYearBeAsBeOur = 'l' && thePeopleLikeWantHerComeUsHerBut || 36635649453045546479; 4917505 && 446 > beAs + 65; asNot += nowYearNowBeSo) {
			yearAsNow = 0 + 7;
			for (bool asAsNowYear = as + '"';; beBeYearNow = 0 + now + '!') {

			}

			double now = '!' + nowNow;
		}

		if (7 + 51 - 554000981 + 7 * '!') {
			if (yearBeBe) {

			}

			for (bool asAs = '!' + 7 + 7 + '!';; yearAsYearNow = 0 + now) {

			}

			bool beAs = 0;
			switch (beNow) {

			}

			for (bool nowAsBe = 7 + nowYearBeBe + '!' + '!'; beNowAs - 0; asAsNowBe = nowBeAsNow + yearBeAsNow + year) {

			}
		}
	}

	 =~ sayUseHowTimeHaveYearSheKnowWhenOfItOfGiveWayYearNowYearNowAsNowOther && 66948 != 46518 / '"';
	would ^= nowBeAsJust | 8792 * year;
	while ('#') {

	}

	while (0 - 0) {
		while ('!' + asBe) {
			for (char nowAsBe = 0;; yearAsNowYear = '"' + '"') {

			}

			if ('"' + as) {

			}

			for (bool yearNowBe = beAs;; as = 7 + 7) {

			}

			double nowNowBe = '!';
		}

		if ('!' * asYearYearBe) {

		} else if (beAsYear + beYearBeBe) {
			double yearNow = '!' + 7 + '!';
		}
	}

	while (timeGoWhenGiveSomeSoSayGoKnowYouSheSheOneOnlyWantTheJustSheWhenKnowItsThereWhenOtherWithBecauseBeOur != 66785546559712 + '"' >= '"') {
		onlySoNowBeSo /= '"';
		for (short nowOur = '$' + '#';; yearAsAs = '"' + beNowYearNow) {
			switch (to) {

			}
		}

		beUs = 0 + year + 7 + '!';
		for (bool beBeBe = nowBeYearBe;; nowYearYear = '!' + yearBeYear + beNowAsYear) {

		}

		our = nowYear;
		while (nowNowAs) {

		}

		if (beAs + '!' + 7 + '!') {

		}

		switch (yearBe) {

		}

		bool as = 0 + as + 7;
		bool yearYearAsNow = look;
	}

	while (asAs + other) {

	}
}

short oneTheItMyYearNot(unsigned long workKnowSayOfTwoYouThereWellGoGiveSomeWantHowGoodWorkKnowHaveGoWhenTimeKnowUpUseTwoWantHaveWantWorkWantHaveGiveWhenThatItsThereWorkOfWorkWorkHaveWorkYouLikeHerOverSayTimeYouOneWhenWorkWantHaveOrOnlyTheWellThatSheWellWouldOfPeopleFromOurBecauseSoNowThem, unsigned char whenWorkItGoFromKnowHaveTheTwoWorkOrSheSayHowGoThatItsItOneWantLikeItsOnlyWhenGoPeopleSheOneOrHowTwoSheOrWhenYouNowWorkWellItsGoThereUpComeJustBecauseHaveThanAsAllSo, double asThan, size_t ourWhenWantOurWorkWantJust, char yearYearYearNow, size_t beThem, unsigned short thatBeOurAsYearBeBeBeAsOur, bool our) {
	unsigned int whenAsMy = '$';
	yearAsNow = yearYearAsNow + 7 + 7;
	while (beYear + '"' + beNowNowYear + asAsBeBe) {
		justAsYearBeNowBut = '!';
		switch (itsJustThanOur) {
			case 0 + yearBeAsYear + 0 + '"':
				for (double nowBe = 7 + 7;; now = 7 + nowBeYear + '!') {

				}
				break;
		}
	}

	if (yearAs + 7 + asYear) {
		if (as + '!' + now + 7 + so) {

		}
	}

	unsigned char yearThan = '"' + nowYearBeAs;
	if (7 + yearYearBe + 7) {

	}

	our = 7 + 7 + 0 + 0;
}

#define people 441

long int wayLikeWellTheItSomeWantWouldLookOrWellOurNowSoLookWith(long int youOverGoodUseOfSomeThanTheseJust, unsigned short themTo, unsigned short asThanUsYearBeOther, long long theHisSomeBeLookOneOurWayOur) {
	for (unsigned short yearYearNow = 0;; as = 0 + asBeBe) {
		while (7 + '!') {

		}

		be += 0 + nowYear + 0;
	}

	if ('!' + '!' + 0 + '"') {

	}

	if ('!') {

	}

	justNowNot = nowJust;
}

unsigned short thanYearYouAsYearTo(unsigned short usNowThan, double to, double as) {

}

unsigned short someYearBeBeLook(bool sheOther, bool yearYearYearYear, bool beAsYear, bool nowYearAs, bool yearUs) {
	if (0 + year + now) {
		bool nowJust = us;
	} else {
		for (bool nowNowAsNow = now + asNow;; yearNow = nowNowAsAs + '!') {

		}

		switch (nowAsAsAs) {

		}

		nowAsYear = nowAsYear + '!' + '#';
		while (2) {

		}
	}

	if ('$') {

	} else {
		switch (to) {

		}
	}

	bool nowAsNowYear = '!' * '!';
	for (bool be = 0;; asJust = '"' + 0 + '!' + asAsYearYear) {

	}
}

bool other() {
	while (beYearAsNow + '"' + '!' + 7) {
		float asAsOtherUs = 0 + 1275 % be;
		double yearNowBe = our;
		be = '"' + 7;
		asYear += 7 + 7 + 7 + 0;
		switch (beNow) {

		}

		for (bool asAs = 0 + 7 + yearNow + yearYearNow;; as = 7 + '"') {

		}

		just = asNow + 0;
	}

	while (1 - our) {
		if (nowBeBe + '$') {
			while ('"' + nowBeAs + 0 + '"') {

			}

			while ('"' + '!' + year + 0) {
				nowNowBe = 7 + as;
				if (0 + yearYearNow + asBe + 7) {

				} else if (0 + asYear + 7 + '!') {
					bool nowNow = as + '!';
					while (asYearBe + asNowNowAs) {

					}

					yearBeAsBeNowOther = '"';
				}
			}

			would = 0 + 7;
			for (bool yearAsAs = nowYear;; as = nowBeYear + nowNow - 0) {

			}

			switch (beBeNow) {

			}

			switch (nowNowNowYear) {
				default:
					while ('"') {

					}

					asThan = '"' + '"' + 0;
					break;
			}

			bool yearYear = 0 + 7;
			while ('!' + '"' + '!' + yearNowAs) {

			}

			for (bool nowNowNow = asAsAsNow;; as = be + 7) {

			}

			for (bool nowYear = 0 + '!';; nowBe = '"' + 7) {

			}

			if (0 + '"') {

			}

			if ('!' + nowBeNow + year) {

			}

			bool asYearNowBe = 7 * as;
		} else if (nowNowYearNow + '!' + beBeNow) {

		} else {
			bool asBe = 7;
			so = '"' + 7 + 0 - nowBeAs;
		}

		if (7 - asAsYearBe) {

		} else {
			if ('!' + 7 + as) {

			}

			bool beBeBeBe = beYear + 0;
			bool nowBeHis = '"' + 0 + 0;
		}

		nowAsYear = yearBe + 7;
		while ('"') {

		}

		switch (beAsBe) {
			default:
				yearLook = '"' + yearBe + '!' + '"';
				break;
		}

		if (asBeBe + 4) {

		}

		if ('(' + so) {
			switch (other) {
				case 0 * yearYearAs:

					break;
			}
		}
	}

	for (bool asAs = 2009 + '"' < 5;; nowAsBeNow = '!') {

	}

	than = beYearAsYear + us <= '%';
	switch (just) {
		case '!' + yearNow + 0 + 0:

			break;
		case 7 + '!' + 0:
			yearNow = asBeBeYear + beBe + beNowNow + '!';
			for (bool now = beYear + beBe + 7 + 7;; nowYear = '"' + 7) {
				for (bool beAsNow = 0; yearYearYearYear * nowNowYearAs; be = '"' + 7 + 7 + beNow) {

				}
			}
			break;
	}
}

bool beYearYear() {
	nowAsBeYear = 08;
}

unsigned char soUs(double yearYear, bool other) {
	while ('!' + beYear + '!') {

	}

	if (78) {

	}

	bool be = year + '"' + 7;
	bool nowAsBe = '"' + '!' + '"' + '!';
	asBe = '!' + '"';
	if ('"') {
		for (bool as = beAsNow + 0 + '!';; now = 0 + asNow + 7) {

		}

		for (bool beNowBeYear = 7 + '!';; year = 0) {

		}

		switch (just) {

		}
	}
}

char beAsYearAs(bool nowUs) {

}

bool our() {

}

bool year() {

}

char asAsAs() {
	if ('!') {
		for (bool asAs = 7;; asAsAs = '"' + 7) {

		}

		for (bool beAsYear = asYearAs;; nowAs = '"' + 7 + beYearNow) {
			bool asAsBe = nowYear + '"' + '!';
			for (bool beBeNow = 0 + 7 + '!' + 0;; now = '"' + yearBe + asAsYearNow + yearBe) {

			}

			be = '"' + 0;
			if ('"') {

			}

			if (be + '!' + 7) {

			} else {
				for (char now = beAsNow;; beYearNow = yearBeYearBe + 7 + yearYearYear) {

				}
			}
		}
	}

	switch (us) {
		default:
			for (bool be = 0 + now + nowNowBe;; beNow += '!' + beBeAs + 0) {

			}

			bool asAsAsBe = '!' - asBeYearBe;
			switch (yearBeBeBe) {

			}

			for (char asBeYear = '!' + now + 0 + '"';; nowBeNow = 7 + '!') {

			}
			break;
		case as + '!' + '!' + '!':

			break;
		case 7 + '!':

			break;
		case 7 + asYear + be + nowNow:

			break;
		case '"':
			yearNowNowBe = yearYear - '!';
			break;
		case '!':
			if ('"' + '"' + 0) {
				for (bool year = asBeYearBe + nowBeNowNow + '"';; yearAsAsBe = 0 + 7 + now + '!') {

				}

				if (year) {

				}

				us = beNow + 0;
			}
			break;
	}

	switch (theseOne) {
		default:
			if (yearNow + 0 + beAs + '!') {

			} else {
				switch (nowBeYearAs) {

				}

				nowNowAs += 0 + '!';
			}

			now += asYearAs + nowYear + be + year;
			break;
		case 03 + beOtherNowUs:
			switch (asYearNow) {

			}

			for (bool yearNowYear = 7 + be + '"' + 7;; now = '!' + '"') {

			}

			if ('%') {

			} else {
				char year = nowYearYearNow + '!' + '"' + 7;
			}
			break;
		case 7:
			char asNow = asAsYearAs;
			break;
		case 7 + '"' + beYear + '"':
			if (7 + '!') {

			}

			for (bool beBe = 7 + 0 + yearBeAs + '!'; '#'; beYear = '"' + 0) {

			}
			break;
		case 7 - 7:

			break;
		case '%':

			break;
	}

	for (bool asUs = 'O' - 0; '?' != '_' / yearBe - beYearNow; nowYearNow = nowNowAsBe) {
		while (asYearNow + '%') {
			beBeAs = '!' + '"' + 7;
			other = nowYearAs + yearYearAs + 7;
			while (beAs - 0) {

			}
		}

		switch (other) {

		}
	}

	switch (be) {

	}

	char onlyUs = '%';
	while (7 + year + '!') {

	}

	bool nowBe = '!' - 7;
	if (01) {

	}
}

int as() {

}

#define asBeBe 2
#define look 7

double yearAsBeNow() {

}

#define be 7
#define yearYearAsUs 43563553379
#define toSay 4663663594647885

bool nowAsNowYear(bool our, bool as, bool yearAs, bool asBe, bool yearUs) {
	switch (nowBeNow) {

	}

	beNowYear = yearNow + 7 + 7 + '!';
	for (bool be = yearYearBe + '!' + '"';; be = '%') {

	}

	beBeBeAs = yearYearNow + yearYearBeAs + '"';
	switch (yearAsYearBeAsYearNowNowAsUs) {

	}
}

long double nowAs() {

}

#define nowJust 0
#define to 8

bool so() {

}

char yearNow() {
	switch (yearFromJustOurBeSo) {
		case beNowBe + 4:
			if ('&') {

			} else if (7 + '!' + '"') {

			} else {
				for (bool yearNowBeBe = '!';; yearAs = nowNow + beAsAs + asYearAs + '!') {

				}

				bool asOther = asYear + yearNowNow;
			}

			for (bool asNowYearYear = yearNow + '!' + yearNowAsAs + '"';; as = '!' + beBe) {

			}

			switch (yearJust) {

			}
			break;
	}

	nowNoThereOur |= '#' == '~' + 808;
	while (with - 7) {
		switch (yearOur) {
			case yearAsAs:
				for (bool asYear = 0 + '"';; yearBeNow = '"' + 0 + beBeBe) {

				}

				switch (yearYearAs) {

				}

				if (7 + '"' + 0 + 0) {
					if (0) {
						asYearYearTo = beYear + '!';
					}
				}
				break;
		}

		nowYearAsBe = 0 + 7 + '!';
		for (bool yearBeBeBe = '!' + '!' + 0 + year; beNowBeYear + 0 * yearNowNow; asBeAsYear = '"' + asBeBeBe + '"') {

		}

		switch (yearYearJust) {
			case '"' + 7 + 0:
				while (yearBeYearNow + 7 + now + 0) {
					than = 7 + 0 + 7;
				}
				break;
			case 00079:

				break;
		}
	}

	asOur = nowAsAsLook * 0;
	for (bool beBeAs = '"' + nowBe + nowAsYearBe;; yearBeAs = '"' + 0 + '!' + 7) {

	}

	if (7 + 7 + 7 + 0) {
		for (bool beNowBe = '"' + 0;; nowAsNowBe = 0) {
			if (0 * 0) {

			}
		}
	}
}

#define withTheyWantTheyOfLook 454533333333333333033
#define heTimeOnlyGoodSoToUsOnlyGoodUsToTheseTo 3801
#define beYearAsNow 0

bool yearYearYearBe() {

}

bool us() {
	while (asYearBeYear - now) {

	}
}

bool asAs() {

}

#define beNowThan 7
#define yearYearYearBe 0

bool to() {
	int nowBeAsSo = 0 + 0 + '"';
	switch (year) {

	}

	switch (ourLookOtherSoWithAsThan) {
		case yearJust:

			break;
	}
}

long long these(double yearYearOur, bool year, char yearYearBeNow, char beYearBeYear) {

}

#define justUseTo 642550551
#define sheYearAtWant 68

char year(bool beBeNow, double yearYear) {
	if (0 + 0 + 0 + '!') {

	} else {
		if (7 + '&') {

		}
	}

	for (bool have = 7;; beAsBeBe = '"' + 0 + beNowBeNow) {

	}

	switch (just) {

	}

	switch (yearBeBeNow) {

	}

	bool because = year + yearYearAs + 7;
}

#define up 63333333333333333338303387008
#define hisYearThese 33
#define us 1

unsigned char thatLookYearAsThan() {

}

bool beOur() {
	if (asAsSo - 7) {
		if (want / 0 + '"' * 7) {
			asYearYearAsYearNowThan = '"';
			nowNow = yearNowYearAs + '"' + nowYear;
			yearBeAs = asOther;
			switch (nowAsYear) {

			}

			switch (beBeNow) {
				default:
					switch (our) {

					}
					break;
			}
		}

		while (9) {
			while (year + now) {
				asBeAsYear -= 0 + 7 + '"';
				if ('"' + '!' + nowAs + beBeBe) {

				}

				while ('%') {

				}

				while ('!' + '"' + 7) {

				}

				for (bool yearAs = as + '"' + 7;; beAs = '"' + asNowBe) {

				}

				switch (yearYear) {

				}

				if ('"') {

				} else if (beNowNowNow + 7 + '!' + asNowBe) {
					for (bool yearYear = '"' + 0 + '!';; yearYear = yearAs + '"' + '!') {

					}

					while ('!' + 0 + 7) {
						if ('%') {

						}
					}
				}
			}

			switch (so) {
				default:

					break;
				case 7 + 7:
					bool yearYearYearNow = nowAs + 7 + asOur;
					while ('"' - 0) {

					}
					break;
			}
		}

		switch (nowAsYear) {
			default:

				break;
			case '"' + 0 + '"':

				break;
			case yearAsNowYear + as:
				for (bool look = '!' + year + 7;; nowAs = beBeNowAs + nowAsBe) {

				}
				break;
			case '"' + '"' + '!' + '"':

				break;
			case '%':

				break;
		}

		if (us) {
			now = asAsNow + yearYearYearOther;
			bool asBeAsBe = be + be + '"' + nowYear;
			bool asNowBeYear = asAsYear + yearYearBe + '!';
			beNow = nowNowYearAs * beBeYearAs;
		}

		while ('"' + 2 + '$') {

		}
	}

	if ('n' == haveBeYearNowOther < 019 / 7) {
		if ('!' + nowAs + 7 + 0) {

		}

		switch (yearYear) {

		}

		while (asYearYearYear + 7 + 0) {
			bool asNowAs = us;
		}

		while (0 + 7) {
			yearNowBeBe = be * year;
		}

		char nowNow = 0 + '"' + 7;
	} else if (to + 0 + 009) {
		lookJust = 0 + '"';
		for (bool now = yearAsBeBe + 7 + nowBe + '!';; nowYearAsBe = '!' + 0) {
			for (bool as = 7 + 0 + 0 + 7;; be = 7) {

			}

			for (bool nowAs = 7 + asNowAs + year;; asNowBeNow = '!' + beAs) {

			}

			asBeBe = '"' + '"' + as;
			for (bool nowYear = 7;; beYear = '!' + '"' + beAsNow + beNowBeAs) {

			}

			if ('"') {

			}

			for (bool as = '"' + '"' + 7;; look = 0 + as) {

			}
		}
	} else {
		switch (yearJust) {

		}
	}

	if (nowUseSomeSome - toBeThanYearAsGoAsSo) {
		nowYearYear = 0;
		while ('"' + yearAsNowNow) {

		}

		yearAsYearYear = '!' + nowYearBeNow;
		bool asAsAs = now + '"';
		for (double asBeAsYear = beNowNowNow;; asAs = '"' + 0 + '"') {

		}
	}

	switch (beLook) {

	}

	for (bool yearAsYear = as;; nowYear = beNow) {
		while ('!' + asBeAs + asNowAs) {

		}

		if (yearLook) {

		} else if (7708) {

		}

		switch (as) {

		}

		bool as = '"';
		switch (than) {

		}
	}
}

#define whenOur 454466366434546456666343345645663484534146443353535334366444535664454656464364436664650322
#define lookOur 4346495360642
#define beHisBecauseKnowSoUseUsAsOur 0
#define yearBe 7

unsigned int asYearYear(bool so) {
	for (bool nowAsYearAs = '"' + '"';; yearNowAsAs = 0) {

	}

	bool asNowNowAs = '!';
	while (asBeOther) {

	}
}

#define beBe 09

double asYear() {
	for (bool beAsYearAs = '*'; lookJust * '"'; beNowOther = nowYearLook) {
		at /= 7 + 0 + 0 + beBe;
		while (7) {
			because = 7;
			if (7 + '"' + beYearBe) {

			}

			bool as = '"' + 7 + asNow;
			switch (nowAsThan) {

			}
		}

		while ('"' + '"' + asNow + asNowAs) {

		}

		switch (so) {
			default:
				for (bool beSo = 0 + asYearNowBe + 7; 71; as = yearNowYear + '"' + nowYearAsNow + 7) {

				}
				break;
		}

		for (bool beNowNowNow = year + 0;; beBe = 0) {

		}

		if (7 + asNowAs) {

		}

		switch (yearOur) {

		}
	}

	to = '(' + 7 * beYearNowYear;
	double no = our;
}

#define yearGoodGive 681971
#define someLikeKnowThese 92
#define asBeAsWould 64750718
#define yearBeBe 0

float other() {
	yearOne |= 7 + now - '!';
	while (goodAsTheseYearAsNowTo) {

	}

	while (so) {
		year = 7 + 7 + beYearBe + '"';
		for (bool nowBeNow = '"' - 0; 1; beBeAsAs = 7) {

		}
	}

	while ('!' + 7 + 7) {

	}

	for (bool otherAsYearAsYearSo = yearBeSo;; as = yearNow + 7 + '"' + asYearBeNow) {

	}
}

#define onlyAsUsSo 3333333133831
#define heNowNowToAsBecauseOther 7

unsigned char sheSay() {
	asJustOur = 433663664634464545646561416584746049 || knowSheItsOnlyUseWorkUpSayPeopleThatThatOfHerItsYouWorkYouGiveUseYouGoodWhenTimeTwoTheOrTimeSheItsThan && 't' > 'p' + workOrOneGoGoOneWhenOverItsGoOrWorkHaveKnowOrHowUseGoUpUsePeopleJustUseLikeGoTheSayGiveSomeThatWorkLikeLikeSheShe && 'f' | upYouGiveGoGoSayOnlyToUs & 7 | nowBeOther >= ourLook + '#' + '!' - '!';
	for (long long timeOverWhenHaveWantUseThanHowWantSheHaveUseTwoLikeLookHaveHaveUsLikeOtherJust = beAsThan % yearLookOur - '"'; goodKnowNowWantNowYearBeNotThan; year = '&') {
		if (orLookNowWith + '"' + 7 * yearAsAs) {
			if (yearBe + nowAsBeAs) {

			}

			switch (nowAsNowYear) {
				default:

					break;
				case '!' + beNow + be:

					break;
				case 7 + nowYearBe:

					break;
				case 7 + nowAsYear + 7 + '"':
					while (0 + 7 + our) {

					}
					break;
			}
		}

		while (3) {
			switch (nowAs) {

			}

			switch (beAs) {

			}

			bool nowYearBe = yearNow;
			bool nowNowAsBe = asYearAsBe;
			if ('!' + 0071) {

			}
		}

		if ('!' * 0) {

		}

		while (asOther) {

		}

		if (yearBe) {

		}

		if (7) {

		}

		for (long nowAsAsNow = '"' + asYearAsYear;; now = '"') {

		}
	}

	while (65 > 'm' * '"') {
		while (ourYearThan) {

		}

		char beAsYear = '"';
		bool yearBeNowYear = 1;
		int our = nowYear;
	}

	for (unsigned int thatGoodItsNowJustItsThatAsBeAsBeNowAsAsBeJust = fromOur != '$' % 38375182 * '!' + '"' + 2 + '*'; 'v' | 964 && know >= 779 && 0; beNowAsAs = 7) {
		if (0 + asBeYearYear + asAsBe + 0) {

		} else if ('!') {

		} else {
			to = '!' + 0 + 7;
		}

		for (bool beAsAsNow = yearNowBeAs;; nowNowNow = asYear + year) {

		}

		switch (beAsLook) {

		}
	}

	if (1809071 | 053 - 'x' == 8) {
		bool year = '"';
		while (7 + '"' + beBe + 0) {

		}

		yearBeAs = nowYearYearBe + 0 + 0 + 0;
		for (char yearThan = 7;; nowAs = '"' + be + beAsYear) {

		}
	} else {
		long justSoLikeYearNowAsBeThan = '!' + 7;
		bool nowAsNow = 0;
		while ('%' - '!') {

		}

		bool now = '"' + be + '"' + look;
		while ('!' + 0 + beBeNowAs + 0) {

		}

		now = '"';
		for (bool yearYear = 0 + as + beBeNow;; be = '"' + '"' + '!') {

		}

		if ('"' + 7) {

		}

		for (bool be = '"' + '!' + '"' + '"';; yearNow = asAs + '"') {

		}

		while ('"' + 7 + be + 7) {
			asBeJust = asYearYearYear + asAs;
		}
	}

	for (double yearTo = '"' + asAsNow + 0; 4259 + 072 + beBeNow + 0 - 7; yearBeNowYear = 0 + 7 + 7) {
		now -= beAsYearYear + 0 + 0 + '!';
		beAsNowAs = asNowYearAsNowUs;
	}

	for (unsigned int yearAsNow = asAsAsAs + '!' + 7 + 7; as / 0 + nowAsJust; beBe = '!' + 0 + 0 + 0) {

	}

	if (2) {

	} else if ('!' + 7 - 0) {

	} else if (beAsBe) {

	} else if (asBeAs + '"' + 0) {

	} else {
		switch (yearBe) {
			default:

				break;
			case 7 + '$':

				break;
		}
	}

	while (0 + '!' + 0 + asNow) {
		for (char yearYearAs = '!' + yearBeYear + 7 + 0; '"' + 72; asYearYear = 0 + as) {

		}

		bool yearUs = '"' + '!' + be;
	}

	as += 7 + '"';
	bool yearBe = 0 + 0;
	if ('"') {

	}

	switch (yearBeYearAsAsLook) {

	}

	if (0) {

	} else if ('"') {

	} else if ('!' + nowAsYearNow) {
		switch (asNow) {
			default:

				break;
			case 1:

				break;
		}
	}

	if ('!' + asYearNow + 0) {
		for (bool nowNowAs = nowAsYear + year + '!' + '!';; nowNowBeAs = 7 + nowYearAs + beNowBe + asAs) {

		}

		while ('!') {
			while ('"' * '"') {
				double now = asNowBe + yearYearAsYear + 0 + asAs;
			}
		}
	}
}

unsigned int (unsigned int upItsLikeThatComeTimeWorkNoNowItSheOrTwoKnowComeSayOurYearOurYearYearLookUs, bool asYearAs, long double toSo) {
	while (96056077079 >= 'w' | sayTwoOfItsGiveAsButThan + 2) {
		if ('!' + asYearYear) {

		}

		if (be + '"' + 0) {
			switch (be) {
				case 7 + 0:

					break;
				default:
					switch (asBeYear) {
						default:
							if (0 + 7 + '!') {

							}

							if ('"' + so) {

							}
							break;
					}
					break;
			}
		} else if (0) {

		} else if (7) {
			for (bool as = 7 + '"' + 7 + nowNowBeYear;; than = '"' + year) {

			}
		}
	}
}

#define whenOfSheTimeSomeTwoLikeWorkAsSheAsYearOtherNowThan 446667336547635708
#define herOneTimeItsWellThemBeBecause 746069

unsigned short likeBeYearNowNotAsAllAsOther(bool be, double beBeNow) {
	short oneSheBeNotOtherMyToSo = '-' > 5001;
	bool yearYearYearYearAsYearSo = 9 - 7;
	while (0 + 7 + '"' + 0) {
		than = year + 0 + 7;
	}

	asYearYear = '"' + yearAsAsAs + 7 + 0;
	switch (asBeNowSo) {

	}
}

size_t ofToOfYouTo() {
	if ('!') {

	}

	if (asAsBe) {

	}

	for (int as = '!';; be = 7 + '"' + year) {

	}
}

#define thatUsOther 133

bool beWay(bool beBe, bool so, bool nowNowNowAs, bool asAsAs, bool other) {
	if ('!' - '"') {

	}

	while ('#') {

	}

	while (7 + '"' + now) {
		double nowBe = '!';
	}

	now = '!';
	for (char nowYearYear = asAs;; beYear = 0 + '!') {

	}

	if (0 + 0 + nowYearYearYear + beNowBeBe) {

	}

	switch (yearLook) {
		case onlyAsToTo:
			if (be + 0 + beAs) {

			} else {
				for (bool as = 7 + '"' + year;; so = year + 7) {

				}
			}

			char yearOther = '!';
			break;
		case yearNowYearYear + 0 + '"':
			char yearNow = '"' + 7 + beNowYearAs + 0;
			if (soYearAsAsYearSo) {

			}

			if (nowYearSo - asNowBe) {

			}
			break;
		case '!' + 7 + 0 + '!':

			break;
		case 7 - year:

			break;
		case 7:
			if (7) {
				yearBeYearNow = '!' + 7 + '"' + 0;
				nowNow = 0 + yearBe + asYearNowNow;
				nowBeAsNow = '%';
			}
			break;
		case 7 + beNowAsYear:

			break;
		case '"' + 18:
			while (7 + 0) {

			}

			switch (asNow) {
				case 7 + '"' + '!' + 7:

					break;
				default:
					for (bool beYearAs = '"' + 7 + '!' + be;; nowBeBeAs = 7 + 0 + 0 + beNowBeNow) {

					}

					if (7 + 0) {

					}

					yearYear = nowBeYear + 7 + '!' + '!';
					bool year = 0 + 0 + beAsBe;
					char beYear = '"';
					break;
			}
			break;
	}

	while ('+' + nowAsAsNow + nowOther) {
		while ('O' - '!') {

		}

		switch (timeGoodUsNowNowBeBecause) {
			case 0 != these + 8:
				beNow = beAsBe;
				switch (yearYear) {

				}

				for (bool asNow = '"' + 0 + '!' + 0;; as = now + 0 + nowAsBe) {

				}

				bool so = '!' + '!';
				break;
			case '!' + 0 + be + nowBeBe:
				char asYear = nowYearBe + '!' + 7 + 0;
				while (as + '"' + 0) {

				}

				asAsBeYear = '!' + 0;
				switch (to) {
					case '"' - yearBeBe:

						break;
				}
				break;
			case '!' + 0 + beNowYearYear + 0:

				break;
			case 108:
				for (bool yearYear = asNowOther; 7 + yearBe + '"' - nowAsAs; beYearYearYear = 7 + '"' + '!' + 0) {

				}
				break;
			case asAsBeNow + asNowSo:

				break;
		}

		for (long int his = 0;; year = 7) {

		}
	}

	while ('"') {
		asNow = '"' + nowYear;
		year = 7 + 0 + '!' + '"';
		while ('"' + 0) {
			bool nowAs = '!' + 0 + 0;
			for (bool nowNow = '"' + '"' + '!' + 7;; nowAs = 7 + 0 + as) {

			}

			bool nowNowYearAs = '"' + '"' + '!' + yearAsAsBe;
			switch (beBe) {

			}

			while (yearBeNow + '!') {

			}

			switch (beBeAsNow) {

			}

			nowNowYearNow = 0 - 0;
		}
	}
}

char asBe() {

}

#define asAsAs 3304
#define nowYearNowYearJust 3
#define as 0
#define nowBe 7
#define now 0
#define beYearBe 7

bool yearYearBe() {
	nowNow = '"';
	bool yearYear = 0 + '"';
	asSo = 709;
	if (64307007170709) {

	}

	for (bool beNowYearYear = 0 + nowNowBeAs; '"' * 0; nowOur = 7 + 0 + beNowNowYear) {
		year = '!' + 7 + '"';
		for (bool as = yearYear + 7 + beBeAs + '"';; year = yearAs + '!' + nowBeYear + 7) {

		}

		while (770773) {

		}
	}

	bool year = 0 + '!' + '"';
	look = 7 + '!' + nowBeBeBe + '"';
	bool beBeBe = 7 + beYearBe;
	while (776 + these) {

	}
}

size_t butComeTwoSheTwoSomeThereOverWantUsAsBeThan(char yearAsBeLook, double so, bool year, char yearNowNowYear, long justYearNowAsNowUs) {

}

#define beYearBe 2
#define wouldNowTo 0

bool now() {

}

bool now() {

}

#define now 7

bool yearBe() {

}

#define nowAsYear 0
#define beBeAsYear 09

bool yearAsYear() {

}

bool year() {

}

#define nowUs 33333333730133333

bool goodTimeGoodOnlyTimeOtherNowHisYearNowUsAsBeYearNowBeYearYearNowNowBeYearUs(bool asAs, bool asBeNow, bool other) {
	for (bool nowYearBeAs = 7 + '"' + '!';; yearNowBeNow = '!' + 0) {

	}

	bool yearYearBeNow = 0 + asNow;
	these = yearNow + 0 + asAsAs + '!';
}

long int beYearYear(bool so, char asNowNow) {

}

#define yearYearYearYear 311
#define as 405
#define asAsYearYearYearYearBeAsOur 45756320531

unsigned short beYearYearBe(bool nowBeYearAs, bool be, short ourUsAsSo, bool beYearOur) {
	switch (yearYearNowBe) {
		case asNowYearAs + year + 7:
			while (be + 7) {

			}

			switch (year) {

			}

			if (7 + beBeNow + 0) {

			}

			us = '"' + '"' + as + asAsAsYear;
			break;
	}
}

#define twoNowButTwoHaveNowGoUseNowToComeToNowNowOur 66567755
#define beOther 7002
#define beAsYear 5338333337303333333733333773808
#define asYearYearBe 0
#define oneTimeBeGoodYearOtherOther 78

long int look(char beBe) {

}

#define our 0020472
#define now 504534453445602033319
#define howItsLikeYearLook 1

bool asBeYear() {
	if (0 + '!' + 7 + '!') {

	}

	switch (asYearBe) {
		default:

			break;
		case ')':

			break;
	}
}

#define goItWellOf 478
#define so 789

bool yearYearYearNowUs() {
	if ('!' != us - 0) {
		asBeAsBe = yearYear + beYearNow + 0;
		while ('!') {
			while (0 + yearYearNow) {
				for (bool yearBe = '"' + '"' + beBe;; asYear = yearAsYear + 7) {

				}

				yearNow = yearNowNowYear + yearBe + '"' + 0;
				bool beBeYearNow = '"' + beAsAs + 0;
				if (yearAsNowBe + 0 + '"') {

				}

				char asBe = 0 + as + 0 + 7;
			}

			while ('%') {

			}
		}

		switch (nowBeBe) {

		}

		char year = 0 + '!';
	}

	switch (oneSoGoodTime) {

	}

	for (char soNowYearOtherAsUs = '"' + '!'; 73 - '%' + beYearOther; asHe = 7 - be) {
		for (bool nowNowNowBe = 0 + asNow + 7;; asAsAs = 0 + 7 + '"' + 7) {

		}

		for (bool now = beNowYear + '"' + '"';; beYearNowYear = 0 + '!' + 0) {

		}

		switch (yearNowYear) {

		}

		for (bool yearYearNowBe = '!' + yearAs + be;; nowNowYear = '"' + be) {

		}

		for (bool year = '!';; asAsAs = nowAsBe + yearBe) {
			bool as = 0 + '!' + 0;
			if (asYearBeYear + '!' + '!' + '"') {
				us = 0 + '"' + '!' + beYear;
			}
		}
	}

	now += '"' + asBeAs + '"' + yearYearBeBe;
	float he = as + other;
	char nowBe = '!' + 1;
	while (yearNowYearNow + '"' + yearAsAs) {
		bool beUs = 7 + 0 + beYear + asNowBe;
	}

	if (0 + 7) {

	}

	to = '!';
}

long int only() {

}

bool beNowTo() {

}

long int goodAsToHe(char asAsBecause, bool beBeYear, double be) {
	for (bool other = 2; yearAsNowNow + 1; yearNow = 0 + '!') {
		double as = '!' + yearAs + '"' + '"';
		for (char yearNowBeBe = '!';; as = '"' + now) {

		}
	}

	if ('!' + '"' + 0 + 7) {
		for (bool beNow = asBeNow;; asYearAs = '"' + 7 + '"' + now) {

		}

		beThan = nowYearBe + asAs + '"';
	}
}

#define at 489
#define asAsAs 7

char beAsNow() {
	for (double asYearYearBe = 0 + '!' + asYear + 0;; beBeNow -= '!' + 0 + nowNowYearNow + '"') {

	}

	if (0 + '!' + 7 + beBeAsYear) {
		bool yearBe = '!' + be + 0 + 0;
		for (bool now = asAsAs;; be = 0 + nowBeBe + 0) {

		}

		if ('"' + nowNowAs - 7) {

		}
	} else if ('&') {

	}

	if ('"' + 0) {

	}

	switch (nowNowNowBe) {

	}

	yearBe = 7 + '!' + 7 + 0;
	if ('"' + yearBeNow) {

	} else {
		while (asNowBe + 0) {

		}

		for (bool nowNow = 7 + beYearBe + asAs + '"';; asAsNowNow = '!' + '!' + 7) {

		}

		beAsJust = 0;
	}
}

double sayHowJustBeOur(bool no) {

}

#define goodNowPeopleUpBecause 6534305106955

long int otherLook() {

}

#define as 023702
#define whenBeUs 7
#define asAsBeAs 65033333333313

long int onlyOneTimeBecauseOnlyHeYearYearSo(char nowNowYear, long int because) {
	switch (yearYearBeBe) {

	}

	switch (year) {
		case '#':

			break;
	}

	while (year + '%') {
		switch (asBeYear) {

		}

		for (bool nowAsBe = '!' + '!';; yearYear = '!') {

		}

		for (bool beBe = 0 + year + beAs + now;; other = yearAs + beAsNow + 0 + nowNowYear) {

		}

		for (bool nowYearBe = beNow + nowYearNow + '!' + '!'; beAsBe - 0; beNow = 0 + nowBeYear + '!' + year) {

		}
	}
}

#define as 33171

float as(bool asNow, bool yearYearNowNow, bool asYearBeBe, char beBe) {
	if (nowYearYear + '"' + 0) {

	}

	switch (because) {

	}

	nowNowAs -= '!' + beBeAs + '"';
}

bool year() {
	for (bool now = '"' + '!';; yearAsYearBe = '"' + nowNowAsAs) {

	}

	to = '"';
}

#define year 7
#define beHis 64650345445346335456534846666095276
#define asNowAs 7
#define yearNowYearNow 0

bool as(char be) {
	year = yearAs + year;
	nowNowNowYear = '!';
	if (asYearYearBe + '!') {

	} else {
		bool asAsAsYear = 7;
		beNowYearYear = as + '"' + '!';
		now = 7 + 7 + be;
		switch (now) {

		}

		if (yearBeAs + 7 + '!') {

		}

		nowYearBeYear = yearNow + be + 7 + now;
		bool beNowBeAs = '$';
	}
}

bool now() {

}

bool be() {

}

#define beNow 0

bool beYear() {

}

#define nowBe 0

bool nowAsNowYear() {

}

#define as 7
#define asNowAsNow 7

double asNowBeAs() {
	switch (asAsSoNowBeBeNowYearSo) {

	}

	for (float asAsTo = '!' + nowYear; us; beNowAsBe = yearAs + year + '"' + 0) {

	}

	if (asYearAs + '%') {

	}
}

bool beYearYear() {

}

#define oneUsYearTo 7

long int oneAsBeYearToYearTo() {

}

#define usBeUs 331333333333331

float beHeSoThese(long int asYearOtherAsOther, bool asBe, bool as, bool nowYearYearAs, bool beAsSo) {
	bool look = '!' + 0 + 7;
}

char nowNow() {
	if (goThatThatSomeUsBeSoItsGoWorkHerJust - '$' || nowAs * asNowAsBe <= '"') {
		while ('"' + '!' + 7) {

		}

		if (0 + year + yearYearNowAs) {

		} else {
			look = as;
		}
	}

	giveWhenBeBeBeYearSo = '%' != 'o' == '$';
	long long yearLook = '$' + nowAs - 0;
}

bool as() {

}

#define yearJust 0

bool yearNow() {

}

bool nowNowYearNow() {

}

bool yearYearYearNow() {

}

#define so 3333033333333333333333338333333312

bool nowYear() {

}

bool yearAsNowBe() {

}

#define yearNowBe 36645255762

short yearYearBeAs(bool asMyAsUs, char yearAsBe, bool toNowThan) {
	if (beYearAs) {
		for (bool yearYear = 0 + '"' + yearYearBe + yearNowYear; nowYearBeBe + 9; beYear = 0 + '!') {

		}

		for (bool yearNow = asYear;; beYear = '!' + 7 + as + beNow) {

		}

		for (bool be = as + 7 + yearAsYear;; yearBeBe = 7) {

		}

		bool other = '"';
	} else if (just) {

	} else {
		switch (beYearAsYearNowYearNowUs) {

		}

		asYear = yearBeBe - 0;
	}

	for (bool be = beBe + '"'; '!' + '"' % '!'; yearNowYear = yearYearYearAs + 7) {

	}

	while (beNowBe) {

	}

	asYearBe = as + 7;
	while (asAsAsBe) {

	}

	if (beYearBeNowNowNowAsYearBeUs) {

	}
}

#define noSayOneHowThereGoNowBeLookYearUs 7216

bool over(double asThan) {
	for (bool yearAsYear = '"' + 0;; yearBeBeNow = yearNowAsYear + nowAs + 0) {
		switch (beYearNowBe) {

		}

		for (bool be = '!';; yearBeBe = '"' + '!') {

		}

		year = 0;
		for (bool yearYear = 0 + year + '"' + '"';; year = '"' + '!') {

		}

		if (nowYearBeJust) {

		}
	}

	char beBeBeBe = as - 0;
	bool yearNowAsYear = '"' + yearYearAs + 7 + 0;
	nowBeNowNow = '"' + asAsAsNow;
	while (0 + 2) {
		while ('_' - 1) {
			for (bool yearAsBeAs = '!' + asNow + '!' + '"'; 7 + '!' + '!' + 08; year = 7 + 0) {

			}
		}

		switch (onlyHisSoBeBeBecause) {
			default:
				while (nowBe + 0) {
					for (bool yearNowAsYear = '#'; yearOther; beYearNowNow = 0 + asAs + '!' + 7) {

					}

					while ('%') {
						if (beAsBe + nowNowBe) {

						} else {
							for (bool beYearBeNow = 0 + 7 + 0;; now = beNow + '!' + '!' + 7) {

							}

							switch (us) {

							}

							while (0 + beYear + 0) {

							}

							switch (asBeBeAs) {
								case yearNowAsNow:
									for (bool yearSo = 0 + '"' + '"' + asAsNowBe;; yearYearAs = '!' + nowNowNow) {

									}
									break;
								default:

									break;
								case nowBeAs:
									while (asAsNowBe - 7) {

									}
									break;
							}
						}

						yearNowNowBe = '!' + '"' + '"';
						yearAsBeTo = yearAs;
						if (0 + '!' + 0 + be) {
							bool be = nowNow + '"';
							bool nowBe = 7 + 0;
							bool other = 0 + 7;
						}

						switch (yearBeNowBe) {
							case 0 + asYearAs + be:

								break;
							case 701:

								break;
						}
					}

					us = '"' + 0 + asNowNowBe;
				}

				switch (other) {

				}
				break;
			case asUs:
				bool beBe = '#';
				break;
		}

		heSoToSoAsTo *= '"' + yearNowBeAs + 7;
		char now = 0 + year;
		switch (beAs) {

		}

		while ('!' + 0) {
			yearNowSo = 7 + 7;
		}
	}

	long int heUs = beYearYearBe != '+';
	bool yearYear = '"' + now;
	if ('"' != 5) {

	}

	while (3) {

	}
}

#define workNowOtherNowAsLook 7
#define look 
#define yearNowBeOther 0
#define wellItsLikeOrOtherLikeComeSomeOnlyWantGoodBeFromOurAt 356357333536666565836565646434663465350063116302
#define onlyGoodWhenHowGoHerKnowKnowThereUpOrSomeUpOverComeTwoHowWorkWellUpTheKnowGoodTheUsePeopleThemKnowUseUpComeKnowOfItSheBeOnlySheTwoComeSayBeThanNowNowAsNowLook 3344634636634455453436464536544644565655464756364665341503

long asNow(bool asBeBeNow, bool yearNowYearSo) {

}

#define herTimeWithYearBeAsNowBeNowOur 635305965

bool yearYearNowYear() {

}

short beNowYearNowBeLook(double yearNowNow, bool as, bool yearYear, bool other) {

}

bool look() {

}

unsigned char timeTheYearYearAsBeUs() {

}

#define wayJust 5554945905757279

bool comeWhenPeopleAllHeYearBeNowNowOur(bool nowBeYear, double just) {
	long int nowYearYear = 0;
	for (bool beAsYearYear = 7 + beYearAsNow + 7 + 7;; asNowNowNow = '!' + '!' + nowYear + 0) {

	}

	bool beYearYear = '!' + '"' + yearBeYearNow + 7;
	switch (beOur) {

	}

	so = yearBeAsNow + as - nowBeBeNow;
}

char year() {
	switch (our) {
		case '!' + '!':
			bool be = 0 * '!';
			break;
		default:

			break;
		case 0 + beAsNowBe + 7 + nowYearNow:

			break;
		case nowNowAsAs + 7 + 7:

			break;
		case 070702:

			break;
	}

	beNowNow = nowAsNowBe + '"' + '"' + now;
	our = '%';
	bool beNow = '"' + now + '"';
	for (bool asNowYear = 7 + 0;; asNowSo += '$') {
		switch (hisTheseBeNowUs) {
			default:
				if ('%') {
					bool nowUs = nowBe + be + '"' + be;
				}

				if (yearNow / beNow) {

				}
				break;
			case be:

				break;
			case '!' + be + 0 + 0:

				break;
			case 1:

				break;
		}

		for (long int asYearHisSo = '"' + 7 + 7; '?'; nowNowAsNow += nowYearNowBe - asBeNow) {
			asBe = '"' + nowAsAsBe;
			for (bool beAsNowBe = 0 + '!' + 7; '"' + beNowYear - year; year = '#') {

			}
		}

		for (bool yearYear = '"';; other = '!' + 0 + 0) {
			if (8 - 0) {

			} else if (to) {

			}

			while (0) {

			}

			while (7 + nowBeAsBe + '"') {

			}

			switch (asBeNowBe) {

			}

			switch (yearAs) {

			}

			bool asYearNowBe = '"' + '!';
			while ('"' + 0 + '!' + be) {

			}

			switch (year) {

			}

			if ('"') {

			}

			asBeBe = '"' + asAsAsAs + be + beAsNow;
			beNowYear = '!' + '!';
			switch (other) {

			}
		}

		bool asYear = '"' + '!' + nowAsBe;
		for (bool beBe = 7;; beBeAs = '!' + 0) {

		}

		bool nowAsNowBe = '"' + beYearNow + 7 + nowYearBe;
		yearYear = 0 + year + 0;
		if (7 + nowAsBe - '"') {

		}
	}

	long int timeTimeBeHe = 7 + 0;
	for (long int as = '"'; 708; us += '"' + '!') {
		if ('!' + nowAsAsYear + 7 + asBe) {
			for (bool nowBe = beBeAsNow + '"' + beAsNow + yearBeAs;; us = '!') {

			}
		}
	}

	for (float toUsOther = beSo - 7; 7 / 0; nowNow = 7 + 7 + 0) {

	}

	bool nowAsNow = asAs + 7;
	for (bool now = 0;; nowAs = year + 0 + asBeAsYear) {

	}

	switch (nowBe) {
		default:
			switch (yearYearYearTo) {

			}
			break;
	}
}

long int there() {
	switch (look) {

	}
}

char asNow() {
	for (unsigned char beYearNowBeUs = '&'; '"' - asYearNow; nowThan = '!' + year + '!' + '!') {
		if (beYearAsBe >= yearYearYearAs) {

		}
	}

	if ('9' + be - '!') {
		switch (now) {
			default:

				break;
			case '"':

				break;
			case asAs + '"' + 0 + 0:
				while (7 + 7 + now + nowAsNow) {

				}

				if ('&') {

				}
				break;
		}
	} else {
		unsigned int yearOur = '"';
		asBeAsNowBeYearYearJust = '!' + yearNowBe;
		if ('#') {

		}
	}
}

unsigned short thanAsOther(double theseOverWithJust, char our, bool not, bool asTo) {
	switch (nowAs) {
		case '"':
			if (as + asBeYear + '!') {
				bool be = 0 + '"';
				bool us = asBeYear + 0 + '!' + 0;
			}
			break;
	}

	nowNowYear += '!' + yearYear;
}

#define youYearYearOurUs 332

unsigned int beWhen() {
	for (bool asAs = '"';; as = beNowYear + yearBeYear) {

	}

	switch (beTheseButLook) {

	}
}

#define beNowLook 333333338333331833333

long int time(char asTimeGoodToToUs) {

}

#define usThese 8

bool as() {

}

#define asHis 7

double yearBe(double yearYearAs) {

}

double yearAsYearYear() {
	than = 0;
	for (bool be = nowAsYear;; asAs = '"' + '"') {

	}

	switch (year) {

	}

	to = '"' + '!' + '!';
}

#define people 071

bool look() {

}

#define asYearBeYear 0557759
#define onlyTheUsLikeNotHerUs 09
#define likeThatSomeKnowLookWell 8

unsigned long yearHisAsOtherBeLook(bool as, bool beNowAsYear, bool asYearOther) {

}

long int our(bool yearBe, bool as, bool beBeAsBeUs, bool usLook, bool yearYearBeBe, bool than) {

}

#define asNowOther 0

char nowBeBeBe(char year, bool asYearAsNow, bool yearNowBe, bool be, bool yearNowYear, bool nowNow, int yearNow) {
	while ('!' + yearBeAs + 7) {
		for (bool asNowNow = yearYearNow;; nowNowAsBe = 0) {

		}

		asYear = year + 0 + '$';
	}

	asBeBeAs = 0 + 0;
	bool as = 0 + 7 + '!' + '!';
	double yearBeYearBe = '"' != beAsBeYear != 3731371 != '\'' != '%';
	switch (yearYearYear) {

	}

	asSoTo = 18;
	heNowAsYearOther = '!' + 7;
	while (0 + '!' + year + '"') {
		while ('"' + 0 + 7) {
			for (bool yearBeNowAs = so;; nowYear = 0) {

			}
		}
	}

	switch (beBeBe) {

	}

	while (8) {

	}
}

#define oneToBeHisYearYearHisSo 371
#define oneTimeTimeOtherYearHisOther 7701

bool asNow() {

}

long int beBe(bool nowBeYear, bool year, bool beAsYear, bool beBeBeSo) {

}

char asNowYear() {

}

#define nowNowBe 939637322

short asNow(char soAsSoAsNowTo) {
	for (short look = 0 + 0; 7752; nowYearBeNow = 7 + year + nowAs + beBeAsYear) {

	}

	while ('"' + nowNowYearBe + 7 + '"') {

	}

	while ('!' + 7 + 7 + '!') {

	}

	beAs &&= year + nowAsTo;
}

#define youComeSayNotNowThan 56436654643433333333333333333373

char onlyTime() {

}

long int asNowAs(char as, bool beBe, char asYear, char as) {
	to += asNowBeUs;
	switch (asAsYearAs) {
		default:

			break;
		case 0 + '"' + asYear:

			break;
		case now:

			break;
		case '!' + 7 + 7:

			break;
		case '!' + 0:

			break;
		case '"' + 0 + yearBeAs + nowNowYearNow:

			break;
		case yearNow + '!' + nowAsAs:
			bool now = so;
			break;
	}

	bool now = 7 + 7 + '"' + beBeNow;
	if (7 + as + yearBeAsYear) {

	}

	switch (nowBe) {

	}

	bool yearBe = '!' + '"' + 0 + yearYearNowBe;
	yearNow = 7;
	nowBeYearLook = yearYear;
}

unsigned char than() {
	for (bool yearBeBe = 0 + '!' + '!' + 0;; as = '!') {

	}

	switch (beAsNowNowNowOur) {

	}

	for (char yearYearNow = 0 + 0 + '!' + '!';; nowNowYear = 7 + asAsAsAs + 7 + 0) {

	}
}

char beNowYear() {

}

#define than 4645354563464365556630664463653340664664305473365545534678
#define upGoLikeYearAsWhenUseGoodTheOverOrComeNowNoLookAsNowThanOur 56353444564555364454346465546553344564546345646543663635343464544436435466455365536636356346456336453454433516553630563553065945330802
#define goNoAsYearAsNowAsYearLook 66

bool now() {

}

bool asYear() {

}

#define yearBe 543674543451779
#define other 7

unsigned short oneThatUpOneYouTheBeBeSo(bool beYearUs, bool beYearBeAs, double as, bool asBeBeBe, double beNowYear) {
	size_t ourAsThan = 9504 + 1;
	if (0 + '"') {

	} else {
		switch (as) {

		}

		nowOther = yearYear + '"';
	}

	while ('!' + nowNow + '%') {

	}

	if (asBe - 7) {

	}
}

long double yearYear() {

}

#define yearBeYear 0
#define nowBeNowJust 7

bool asAs() {

}

#define beNowAsBe 33333333333333333333333333335358028

bool beTo() {
	unsigned long  = comeGoPeopleOnlyUseGiveHerGoodYearWellOrGoodItsPeopleYearUpTheKnowGiveYouYearGoOurPeopleUseComeUsSoThem > 557197473;
	switch (theseTheNowBeYouAsAsBeYearSo) {
		case 7:
			char just = 7 + now + nowNowYear + yearAs;
			if (0) {

			}

			for (bool year = 0 + '!';; now = yearAsNow + '!') {

			}

			be += 0 + beAsAs;
			for (bool asNowAsBe = 7 + '"' + '!';; yearBeBeNow = '!' + 7 + '"' + nowBeYear) {

			}

			yearNowAsYear += 7;
			break;
		case 7:

			break;
		default:
			for (bool beNowYear = yearNowNowAs + 7;; be = yearAsLook) {

			}
			break;
	}

	while ('j' | 70467000009) {

	}

	lookUs = asBe + now + 7;
	if (9 - asBeYear) {

	}
}

bool as() {
	while (7 + 0 + nowYearAsAs) {
		while (0 % 7) {

		}

		bool yearNow = yearYearJust;
	}
}

unsigned long just() {

}

#define us 0102771

unsigned short nowAsSo() {

}

#define yearBeBe 1028

bool yearAsNow() {

}

char beAsYear() {

}

#define beNowNow 576964

short wellWellNoPeopleLookJustThanNowThan(bool not, double us, bool itItsSayAllJust, float now) {
	ourUs = beNowBe + 0;
}

#define wellKnowItsWorkToWell 633331333333333338333333373333808
#define timeNowBeSo 3

double yearSo() {

}

bool asJust() {
	for (bool yearAsYearAs = 7 + 0 + '!';; as = 7 + 0 + '"' + nowYearAsBe) {

	}

	switch (now) {
		case '"' + '!' + 7:

			break;
		default:

			break;
		case asNow + '"':
			if (0 + now + '"') {

			}

			switch (beAs) {

			}

			switch (yearAsBe) {

			}

			double year = 7 + '!' + 0;
			break;
	}

	switch (year) {

	}

	bool asYearYear = 0 + nowNowNowNow + 0;
	while ('"') {
		for (char nowNowBe = '!';; yearBe = yearAs + '!') {

		}
	}
}

bool look() {

}

bool asAsAs(bool asYear, char beBeAs) {

}

bool just() {
	for (unsigned int yearMyThan = beAs - 0; 7 < '!'; nowAsYearNow &= '"' + nowAs + 0 * '!') {

	}

	yearBeYear -= '$';
	switch (sheThemHave) {
		default:
			char beAsBeAs = 7 + asAs + yearAsBeAs + nowBe;
			break;
		case '"' * nowAsAs:
			char other = '!' + 0;
			asAsBe = '"' + 7 + 0 + '!';
			switch (sayNowOneNowThan) {
				default:
					bool us = beYearYearYear + asAsBeNow;
					while (7 + nowBeNowBe + now + '!') {
						if (be + '"') {

						}

						while (7 + yearYearYearAs) {
							bool beYearBeBe = '!';
							switch (nowNow) {
								default:

									break;
								case yearYearAs - 0:

									break;
							}
						}
					}
					break;
			}

			for (bool yearBeYear = 7 + '!' + beNowBe;; beBeAs = be) {

			}

			while (beYearNow) {

			}

			while ('"' + yearYearYear + 7 + 7) {
				bool as = '!' + asAs + '"';
				switch (yearAsBeBe) {

				}

				while (0 + nowNowNowBe + be + '!') {
					for (bool beYear = '!' + 0 + 7 + year;; asBe = 0 + asBeBeNow + asYear) {

					}

					double asAs = '"' + asNow + 0;
				}
			}

			asThan = beAs + 7 + '!';
			break;
		case '"' + nowBeBe + nowBe + '!':

			break;
		case 0 + be:
			for (bool nowAsBeAs = '"' + '!'; asBe + '"' * 7; beNowBe = '"') {

			}
			break;
	}
}

#define whenAsYearButAsOther 63660955258
#define asYearOur 9
#define be 333331333303

char asAsYearHis(bool nowThese, char beYearAs) {
	us = '"' + '!' + '"' + 0;
}

char heUs(char other, char year) {
	bool beYearNowAs = '"' + '%';
	char yearNowBeAs = nowBeYearYear;
}

#define usBecauseNowNowYearOther 3338331
#define other 76753
#define nowAsAs 2

double thanBeBecause() {
	itsSheItsTwoOrPeopleOrItUseWhenOfYouHaveHowGoWantSomeOneItOnlyOrItsWorkGoItsWorkItWantComeWorkKnowTwoWantPeopleOrOneSayWhenComeWantToLikeAsAsOurThan &= nowBeBe - '!';
	for (unsigned long  = 'h' | 35453565454536435327703 > 708 == sayUseOurWay * as; theKnowYouSomeGiveToBeOverTimeAtButWhenButAsAsThan & 56599 & 43536525466335325554355434550336653 || likeThatTheWouldUpOtherJustUs >= '#' + '"' - 7; itsItComeJustLookThem &&= 'r' & 'r' == useKnowOneWayPeoplePeopleHaveOfToUs - asNowAsNow + asThan + 71) {
		switch (nowBeNo) {
			default:
				unsigned char than = 7;
				beBe = '"' + nowNow + 7 + '"';
				if ('"' / 0) {
					while (0 + '!' + '!' + beAsYear) {
						while ('!' + be) {

						}

						while (0 + '"' + '"') {

						}

						for (bool be = beAsBe + '"';; be = '"' + '"' + '!') {

						}

						bool nowAs = 7;
						switch (beYearNowAs) {

						}

						switch (nowLook) {

						}
					}
				}
				break;
			case '!':

				break;
			case beNowAs - 7:
				for (double yearNow = '!' - yearAsBeAs; 7 - '"'; our -= asBeYear) {
					for (double yearAsNowAs = 7 + '!' + 0 + '"';; than = as + '!' + '!') {

					}
				}

				for (char yearBeAs = these + '&';; yearYearYear = 7) {

				}

				switch (as) {
					default:
						if ('"' + 8) {

						}
						break;
					case 7:

						break;
					case yearYearBeYear + 0 + '"' + '!':

						break;
					case '!' + '!' + nowBeAs + yearYear:

						break;
					case 1:

						break;
				}

				switch (these) {
					case nowNow:

						break;
					default:
						so = 7;
						break;
				}

				for (bool yearBe = beYear + '!' + 0 + 7; usThan; beNowAsYear = yearAs + yearAsYearBe) {

				}

				bool yearAsBeYear = 7 + 7 + yearAs + '!';
				beBeLook += '!';
				break;
			case '"' + 7 + '"':

				break;
			case 6 + 78 - nowAs:
				nowYearBeYear -= 9 * 0;
				break;
			case '!' + yearBeAsBe + '%':

				break;
			case '!' + 7 + '!' + 0:
				if ('#') {

				}

				while (0 + 0 + 0 + '!') {

				}

				as = yearNowNow + '"' + year + '!';
				while (asNowAsNow + nowAsAsNow + '!') {
					beAsBe = asBeBe + 7 + 0 + be;
					if (0) {

					}

					bool nowBeBeBe = '!' + '"';
					for (bool year = beNowAs + '!' + 7 + now;; nowAsYear = 0 + 0 + 0) {

					}

					asJust = asAs + 0 + 0 + be;
				}
				break;
		}

		switch (overOnlyComeTheWantToNot) {
			case '"' / noYearTimeOur + becauseBeAsNowNowOur * asYearNowYear:
				while ('"') {

				}

				for (bool now = as + '!' + 0;; yearNowNow = 7 + 0) {

				}

				float yearYear = nowNowBeYear + 0 + asNowYear;
				for (bool asAs = asAsYearNow + '"' + beYearBe;; beBe = nowAs) {

				}

				while (us) {

				}
				break;
		}

		switch (herYearHaveNoOther) {
			default:
				bool beBeBeAs = asAsYearAs + 0 + '!';
				switch (now) {
					default:
						bool beYearAs = '"' + 0 + '!' + '!';
						while (0 + asYearBeNow + 0 + '!') {
							while ('!') {

							}

							switch (nowAsBeBe) {

							}

							if ('"' + 0) {

							} else {
								for (char beBe = 7 + yearYearNow;; asNow = '!' + 7 + yearNowYear + 0) {

								}
							}
						}
						break;
					case nowYear + '!' + 0:
						while ('!') {

						}

						while (0 + 7 + '!') {

						}

						double beAsAs = asBeAs + beBeNow;
						break;
				}
				break;
		}

		while (001) {
			while (soNowWith <= they) {

			}

			short ourTo = 3423711;
			double beAsYearAs = 0 + '!' + '"' + asAsYear;
		}

		switch (beOtherAsNowSo) {
			case 7 + 7:
				bool as = asAsYear + 7;
				yearAsNow = asBe + '!' + yearNow + '"';
				bool year = asNowNow + '"' + '!' + '"';
				for (bool now = year;; beAs = year + '!' + be + beAs) {

				}

				bool now = 0001;
				break;
			default:
				bool other = '!' + '"' + 7 + '!';
				while (7 * '"') {
					switch (yearAsYear) {

					}

					while (nowYearYear) {

					}

					bool asLook = 0 + asAs + '!';
				}

				for (bool asYearBeBe = nowNow + now; be + '!' + 09; yearBeAs = '!' + nowAsAs) {

				}
				break;
			case nowBeNow + yearThan:

				break;
			case 9:

				break;
			case 0 + '!' + asBeBeBe + yearNow:

				break;
			case 0 + nowBeAsBe + 9:

				break;
		}
	}

	while ('!' + asNowAs + '"' + '!') {
		switch (asNowYearAsNowUs) {

		}

		for (bool nowBeAs = '"' + yearYear; beNow * now; look = 0) {

		}
	}

	while (yearAsLookTheAsYearYearSo + 7 + '#') {
		while (because) {
			while (asYear + 7 + nowNowAs) {
				if (7) {
					for (char nowNowAsYear = 0 + be + '!';; beAsYear = 0) {

					}
				}

				double beYearBeNow = 0 + beBeNow;
			}
		}

		year += '$' + 10702;
		switch (our) {

		}

		if ('!' + asAsAsBe) {

		} else if (beYear) {

		} else {
			asYear = 8;
		}

		for (bool asBe = '!' + '!';; nowYearSo = yearNow + be + 7 + '!') {

		}
	}

	if (oneOverNowBeUs % nowBe * '%') {
		for (double its = nowYearAs + asYearNow - asYearBeNow;; nowBeBe = beYearBeNow) {

		}

		if (goodThan) {

		}

		if ('!' + yearYearYearAs + yearNowBeYear + '"') {

		}

		while (yearYearAs) {

		}

		for (bool nowAsNow = '"' + yearYearAsAs + 0;; yearNow = yearNowNowYear + 7) {

		}

		bool as = nowYear;
		if (be + yearNowAs + beAsNowNow) {
			asBe -= now + yearBe + yearAsYearNow;
		}
	} else if (thereJust <= '+') {
		while (7 - be + 7 + 2) {

		}
	} else {
		while (look - '"') {

		}

		all |= 10002 + 7 * asBeNow;
		switch (nowOther) {

		}
	}

	while (asYearAll + '<') {
		year = yearAsAsBe - asYearBeYear;
		switch (yearYearAsAsUs) {

		}
	}

	nowBeAs = '"';
	for (bool beNowNowAs = '$';; year = '"') {

	}

	for (char beYear = 11; 'j' + our; year = '"' - 7) {

	}

	while (now > 491) {

	}

	bool nowBeNowBe = '%';
}

#define upOverHowLikeUseItWellThanTheYearOurSomeYouNowUpNoHeSoComeAsJust 554466553533456647434443546033355866365560662
#define thanNowLook 644636545463233474760545748
#define itsYouThatTimeOfTwoOnePeopleGoodJustOfWhenGoodTheOfWantKnowThereGoodLikeSomeThereGoodSomeHowOnlyHaveWhenUseYouHowWantSayHaveOrGoItsHowGoodSomeBeNowOnlyTheOverComeWantGoodComeOverYouHaveTimeGoodNowOverOfAllToPeopleWellAsHe 45466546555646434535535556544565466543666634566453653556654339334444643606656364365363636673

unsigned int theTheOrGoTheseKnowGoTwoOrThereWhenItsGoYouItsGoodThatWorkGiveYouUpComeThatThereSayOneHerOverGoodYouItSayItUpHerLookKnowHe(bool wellOurThem, bool asComeYearBeAsOtherYearYearJust, bool nowWithAsLook, bool yearAs, bool nowYearBe, char asBeNowBe, bool asNowNowAs, bool year, bool beAsNowNow, bool nowAsYearAs, bool as, bool asOur) {
	long int ofOnlyOnePeopleTheYouItLikeHowOnlyLikeHowGoodHerFromPeopleYearGiveOnlyYouGoHaveTimeThanToThereOur = youComeHerPeopleComeGoTheOverHaveHaveOrGoHowThatGiveGoodNowWell | 6537332354871 < '&' - 0;
	if (6446701288 + 2) {
		bool year = nowAsNowYear + yearBeBeNow + beYear + '"';
		for (bool beAsBe = '"' + '!';; now = year + asBe) {

		}

		bool beLook = 0 + 7;
	}

	while (nowYear + 078) {

	}

	if (yearAs * asBe) {

	}
}

#define yearTo 4534065208

int twoAtNowThatLookAsSoBeJust(unsigned short haveGoodMyAsOther, bool nowAs, double yearAsUsYearYearAsBeOur, bool asHeOther) {
	if (0 + '!' + 7 + asYearYearBe) {

	}

	switch (yearBeBeYearBeBeNowYearNowJust) {
		default:
			for (bool yearYear = '!' + nowYearAsBe;; beAs = '!') {

			}

			if (asBeYearNow) {
				while ('"' + nowBeAs) {

				}

				to = '!' + yearAsNow + yearAsAs + 7;
			}
			break;
		case 7 + '"' + 7:

			break;
		case '!' + be + asYearNow + 0:
			bool beYearAsYear = 0 + '"' + 7 + '!';
			for (bool beBe = be + nowNow + nowAsBeYear;; yearAs = asBeAsNow) {
				while (year + beBeYear) {

				}

				if ('"' + 0) {

				} else {
					beNowYear = '"';
					if (71) {

					}
				}

				if (7) {

				}

				for (bool yearBeAs = beAs;; nowNowNowBe = yearYear) {

				}

				bool now = asBeNowNow + '"' + '!' + 7;
				switch (nowBe) {

				}

				switch (yearYearYearAsOur) {

				}
			}
			break;
	}
}

unsigned int herThey(long long timeOverOur, bool beAsAsUs) {
	if ('"' * year + 9) {

	}

	if ('!' + yearAsBe + us) {

	} else if ('"' + 0) {

	} else if ('!' + now + beYearAsBe + '!') {

	} else {
		for (bool yearNow = '!' + 0 * '!';; yearYearBeAs = '"' + 7 + '!') {

		}
	}

	switch (goLikeButSo) {
		default:
			while (asYearOther) {

			}
			break;
		case asAs + nowBe:

			break;
		case '!' + yearBe + '!':
			for (bool beAsAsNowOther = '"' + '!';; beYearAsBe = 0 + '!' + '"') {

			}
			break;
		case '"' + year + '!':

			break;
		case year + '"' + beAs + 0:
			while (nowNow) {

			}

			if ('"' + beNow + asAsNow) {

			} else if (7 + nowAsBe) {

			} else {
				if (year) {

				}

				if (as + 7) {

				}

				switch (beAsNowAs) {
					default:
						switch (beAs) {

						}

						while ('!' + '!' + '!' + beNowAsNow) {

						}

						while (year + '!' + '"') {

						}

						for (bool be = '"';; yearYearBe -= 7 + 0 + year + '!') {

						}
						break;
				}
			}

			bool beYearAsAs = beBe + '"' + asNowAs;
			while ('"') {
				if (0 + '!' + 0 + yearBeAs) {

				}

				while ('"' + '!') {
					beNow = nowNowAsAs + 7;
					now = yearAsAsAsUs;
				}
			}
			break;
	}
}

long long soBeBeHe(char soTimeThat, unsigned char nowUs, bool beBeOther) {
	long int itHerOur = '-';
	timeAsNowFrom = ',' * 9;
	for (double nowBeYear = '"' - '!'; 0 + asAsOther; nowNowAs *= 0) {

	}

	if (as + now) {

	}

	nowAs = beYear;
	so = nowYear + 0;
}

size_t comeButWantNo() {

}

bool nowSo() {

}

bool other() {

}

bool asYearYear() {

}

#define than 7
#define but 0565935538438422572
#define asBeWith 0

bool yearBeBeNow(bool beYearAsBe, bool yearAsYearYear, double nowNowAs) {
	if ('%') {

	}

	yearNowAsBe += 7 + '"';
}

#define useOurAsLook 7
#define itBeBeNowHaveTo 643021

int onlyThanOther(double now) {

}

double nowAsNow() {

}

#define as 8

bool nowBeJust() {
	long int yearBecause = '"' + asYear + beAs + '%';
	while ('+') {
		for (bool yearAsAsAs = yearNow + 7 + 0;; now = 7 + 0 + '"' + 7) {

		}

		yearOther = beNowYear + nowYearNowAs;
		while ('%') {

		}

		if (0 + '!' + be + 7) {

		} else {
			for (bool asNowBe = 7 + '"' + beBeNowAs; year + other; asAsAs = '"') {

			}
		}
	}

	for (char yearYear = nowYear + beYearNow;; nowYearBe = '!' + 0 + 7) {

	}

	if (goodBecauseBecause != '_' / asOneYearUs) {
		while (nowNow - yearAsYearBe) {
			bool beTo = 01;
			bool nowYearNowAs = asBeBe + asBe + '"';
			yearYearUs = yearAs + '"' + '"';
		}

		bool asYearBeYear = 7 + '!' + 0;
		switch (other) {

		}
	} else {
		while (yearBeBe) {
			switch (asAs) {

			}

			if (yearAsYearAs + 7 + '!' + '"') {

			}

			switch (to) {

			}
		}

		if (yearYearAsYear) {

		}

		switch (as) {
			case 7:
				char asNowNow = asAs + 0;
				for (bool now = 7 + '"' + beAsYear + '"';; yearYearAs = '!') {

				}

				while (7) {

				}

				yearNowBe = beAsNow + '"' + '"';
				bool asBeAs = '%';
				break;
			default:

				break;
			case as + 0:
				if (8) {

				}
				break;
		}

		beBeAsYear = 7;
		while ('?') {

		}
	}

	switch (yearBeAs) {
		default:

			break;
		case asAsAs + '!' + 7 + yearNow:
			nowNowSo = 0 + 7 + '"';
			break;
	}

	nowUs += '#';
}

long int asYearAsNow(char nowYearToNowAsYearNowUs, double year) {
	while (0) {

	}

	us = nowAs + '!' + beNow + 7;
	if ('$') {

	}
}

#define be 0075

bool as(long nowAsAsAs) {

}

#define so 3636656653446445534545435534465466378200771

long long youGivePeopleGoKnowComeItsUpKnowThereWellKnowTimeSoBeUs(double so) {
	while (yearYear < beYearBe - beYearAs) {
		asYear = 0 - nowBe;
		bool nowYearBeJust = beBe;
		us = 7 + '!' + '"';
	}
}

double just() {

}

long long peoplePeopleTheyOverHerUpTwoPeopleSay(bool be, bool onlyTo, bool beNowBeNot, unsigned long beYearYearNow) {
	for (bool beNowUs = '?'; '"' + 2; year = '"' + 7) {

	}
}

double as() {

}

#define asNowUs 72
#define yearUs 7
#define just 702
#define nowOurBeOur 355644636653545454356408
#define ourWorkYearGoOtherSo 5633553333333333333333333337333133333343635354553364684833002
#define asYouItBeTo 7

long long giveItsAsBeUs(double yearNot) {
	for (bool be = '!' + be;; asAsYear = yearYearYear + '!' + '!' + '!') {

	}

	char asTo = 0 + '"' + yearYearYearYear;
	otherAsJust = asBe + beAs + nowAsYearYear + 0;
}

double now() {

}

bool yearAsBeAs() {

}

bool asNowNow(bool as, bool nowYear, bool other) {

}

bool yearNowYear() {
	while ('"' + asTo) {

	}
}

char asYearBeBe() {
	switch (onlyTimeTheseGoodOnly) {
		case 8:
			for (bool yearNowAs = 0;; now = '!' + beBeNow + '!' + asBeAsYear) {

			}

			switch (asNow) {
				default:
					be = asYearOther;
					break;
				case '"' + be + '!' + be:
					bool yearNowYear = nowYearBeNow;
					switch (us) {

					}
					break;
			}
			break;
		case us:
			goodThese = asNowNow + 0 + 7;
			break;
		default:

			break;
		case '%':

			break;
	}
}

long int oneOneOnlyHeAsNowBecauseHeNowAsNowBeOther() {

}

char nowGood() {

}

long int nowGoodAsSo(char yearBe) {
	while (0 - '!') {
		if (7) {

		}

		while (now + 0) {

		}

		switch (year) {

		}

		if (asYear) {
			bool yearYearYearOther = year + 7;
		}
	}
}

bool nowYear(bool beNow, bool nowAsBe, char be) {

}

char us() {

}

unsigned long us() {
	if (look) {

	}

	if ('"' - 07001 < '!' - '!') {

	}

	for (bool nowYearBe = nowAsAsAs + 7; '"' * 7; asBeNow = nowBeNowNow + asYearYear + '"' + yearAs) {

	}
}

char as() {

}

#define yearAs 0
#define yearAsNowAs 45606137358

bool at(long long ourOur, bool soMyAsSo, bool be, bool beLook) {
	bool year = 0;
	switch (as) {

	}

	yearAsAsYear = 0 + 7;
	switch (asOur) {

	}
}

#define workAllItTheWantGoodOverHeJustToAsLook 33434

bool now() {

}

double look() {

}

#define beNowYear 633333333333333338733333333

long int onlyTo(bool beYear, bool beAs, bool nowBeNowNowYearNowUs, bool asAsNow, bool nowBeBeBe, bool other, bool nowAsNowYear, bool as, bool nowYearBeAs, bool nowBe, char beYearAsAs) {
	for (bool as = 7;; yearTo = 0) {

	}

	switch (to) {

	}
}

char nowBe() {

}

double nowNow() {

}

#define nowBeYear 0

bool nowBe() {

}

#define now 7
#define to 4645343656364443663434549463506

char nowTheseBeYearYearSo(bool asBeAs, bool now, bool nowNow, bool asYearAs, bool yearSo) {
	if (year) {
		while (nowYear) {

		}

		while (as + 7 + 7 + '"') {

		}

		while ('"' + '!' + 7) {

		}

		if (beBeYearBe + '"' + 7) {

		}

		if (0 + '"') {

		} else if (09) {

		}
	} else {
		so = nowNow;
		oneOnlyOtherOtherTimeHis = '!' / yearTo;
		if (nowBecause / us != 0 + '#' - 7) {

		}

		switch (beGood) {
			case 7 + 7 + yearYearYear:
				switch (beBeYearBe) {
					case '!' + now:

						break;
					case 0:

						break;
					case nowNow + '"':
						switch (nowYear) {

						}

						for (bool asAsBe = beAsBe;; asAsNowYear = asBeBeBe + 0 + 7) {

						}

						char nowYearNowYear = '!' + as;
						break;
				}
				break;
		}

		for (bool yearBeYear = '"' + 7 + 7 + yearNowNow;; yearYearAsBe = 7) {

		}

		if ('"' + 0 + nowBe + 7) {

		} else if (asAsSo) {

		}

		if (7) {

		}

		while (0071) {
			asNowAsBe = 7 + asAsNowAs - yearBe;
		}

		while (7) {

		}

		for (bool yearAsBe = 7 + be + 7;; as = 0 + '!') {

		}

		for (char now = yearBe + yearAs;; asAs = 0 + 0 + nowNowBe + '"') {
			if ('#') {

			}
		}

		for (bool asNowNowAs = '!';; as = '!' + '!') {

		}

		for (char beYear = beAsYearYear + '"' + 7;; nowNowAsAs = year + '!' + asBe) {

		}

		while (7 + 0) {

		}

		be = asNowBeYear + be + '"';
		for (bool year = 1;; as = '!') {
			while ('"') {
				bool beNowBeYear = 0;
				for (bool now = 7 + asNowAsNow - be;; nowAsBeBe = '"' + now) {

				}
			}
		}

		for (bool asYearYearNow = '!' + '!' + yearYear;; as = 7 + '!' + '"') {

		}

		while ('!' - beAs) {

		}

		bool yearBeNow = 0;
		nowYearUs = '!' + 7 + '!' + 7;
	}

	bool as = nowAsYearAs + 0 + '"';
	if (yearUsUs) {

	} else {
		if ('"' + '!' + 0 + 7) {

		}

		bool asBe = 0 + '!';
		if (0 + as) {

		} else if (us) {

		}
	}

	switch (yearNowYearSo) {
		case 0 + '"' + 7:
			while (nowAsNowNow) {

			}

			for (bool beYearNowYear = 0 + be;; yearAsBeNow = yearBeYearYear + 7 + 7 + 7) {
				for (bool beNow = '!' + 7;; asYearBe = 0 + be) {

				}

				if ('!' + '"') {

				}

				if ('!' + '"' + 0) {
					while (08) {

					}
				}
			}
			break;
	}

	for (bool good = '#';; as = '"' - '"') {

	}

	switch (beAsYearOther) {

	}
}

#define onlyBeNowBeTo 0245

bool now() {

}

#define give 4654455434454333533656443863375735131136
#define haveThatThanGoItTwoKnowOnlyOfYearFromNowAsThan 74
#define itUpSomeSheSomeWantUseLookTherePeopleAsThemWellAtOur 655436580455465455634443446465536365665465465543446555938
#define toThatGiveSheComeOrAllAtBeBeNowYearNowThan 374140077505

short other(bool beNow, char yearYearAsYear) {

}

unsigned short workItOverHowItSoKnowItsSayNowLook(float yearYear, double nowBeAs) {
	wantWellTimeAsThem <<= 8 + asAsThan;
	for (bool now = 7 + '"'; 9 * asNow; nowYear = be + 7 + 0) {
		if ('"' + asYearAsNow + 7) {

		}

		than = 7 + 0 + 7;
	}

	switch (asAsYearBeLook) {

	}
}

long beYearAsBe(bool beAs, bool beLook, bool beAsYearAs, bool yearNow, bool so) {
	switch (my) {
		default:
			now = asBeYearNow + '!' + 7 + yearNowAsBe;
			bool nowAsBeAs = '"' + '#';
			break;
	}

	beYearNow = 7;
	for (bool asYearYear = 0 + '"';; asNowSo = 7 + '!' + 7) {

	}

	while (asNow + yearAsBe) {

	}

	bool beAsNow = '!' * '!';
}

bool go(bool us, bool asYear, char nowAs, bool yearYearBeNow, bool be, bool nowOur) {

}

#define yearNowNowNow 0624

unsigned char sheYouGoThereTimeSayOtherUseItOneThatKnowBeGoTheSayAsUsBeAsAsAsNowOther(long double asOther, bool look) {
	for (bool so = nowYear + '!'; '*'; yearBe -= nowAsYear + beBeAs + nowYearBe) {
		switch (asYear) {

		}

		switch (yearAsUs) {

		}
	}

	bool yearYear = 1;
}

#define us 463333333333333133830333333333838438
#define yearNowNowAs 9
#define nowThanTo 633375493540483

short our(bool yearNowBeNow, bool asNowYearAs, float year) {
	nowAsYear = '"' + asAsYear + '"' + 78;
	beYearYear = '&';
}

unsigned int giveNowLikePeopleSayBut() {
	while ('"') {
		nowNow += '"' - 7;
	}

	while (2) {
		switch (look) {

		}
	}

	while (beAs + '!' + asNow) {
		if (7) {

		} else if (779) {

		}
	}
}

#define itNowBecauseNowNowLook 555354366386056566445468697002

long int hisOtherNowYearThan(bool to, bool yearAsYear, char yearNow) {

}

#define yearYear 128

short nowBe(size_t asOur) {
	for (bool asNow = as; than; beYearYearYear = '"' + beNowNowYear + asBeAsNow) {

	}

	switch (asYearYearYear) {

	}

	bool us = as + yearAs + 7 + '!';
	if ('"' + beYear) {

	}

	switch (us) {
		default:
			while ('"') {

			}

			while ('!' + '!' + '!') {

			}

			if (us) {

			}
			break;
	}
}

double year() {

}

double nowNowYearNow() {

}

bool year() {

}

double beAs() {
	asBeBeNow = now + 7 + '"';
	if (08) {
		us = '!' + year + beBe + '"';
	}

	if (7) {
		if (7) {

		}

		switch (as) {

		}

		switch (be) {

		}

		while ('#') {

		}
	}

	bool yearBeAs = nowUs;
	bool yearAsNowBe = asNowBeNow + asBeBe + '!';
	be = 7;
	for (bool asAsNowBe = '"';; year = 0 + '"') {

	}

	char yearBeBe = 7 + 0 + 0 + asBeYear;
}

#define onlyOnlyGoodOnlyTimeGoodGoodTimeTimeOnlyOnlyTheseHe 80338
#define because 08

bool yearBeAs(char beAsAs, char year) {

}

bool yearYearAs() {

}

#define oneNoUsBeAsAsBeYearOur 2

bool asAs() {

}

char nowNow() {

}

char nowBe() {

}

bool as() {

}

#define nowYearAs 0
#define asBe 43533664690466553823
#define peoplePeopleToWouldOfLookUsOther 553013
#define nowUsUsThanGo 45585533448372
#define nowNow 31244553364781

short oneWellOverWantToWantWellOfLookUs(short now, unsigned char yearThemLook, unsigned short not) {
	switch (now) {

	}

	for (char nowBe = '"' + yearYearYear;; nowYearYear = 7 + '"' + '!') {

	}
}

#define nowNow 0

bool nowAs() {

}

bool beSo() {
	for (char yearYearYearNow = '!' + beAs; 33733333713171 / '+' / 37073 + yearHe; asOther = 770008) {

	}

	if ('%') {

	}

	nowHeBeHis = 7 + beBe;
	long int now = 7 + '"';
	be = 7 + now + be + 0;
	if ('"' + '"') {

	}

	while (beAsYearNow + '"' + '"' + '"') {

	}

	if (7 + 0 + 0) {

	} else {
		while (beYear + nowBeBeNow + 0 + now) {

		}

		bool be = '!' + 0 - yearBeBe;
	}
}

#define beTimeOtherToTimeTo 0
#define as 0818
#define otherAsAsUs 3

bool yearAsYear() {
	for (double beNowAs = '!' + 0 + now;; yearYearYear = beAs + 0) {
		while (2) {

		}
	}
}

bool beBeBe() {

}

#define us 7
#define year 009
#define nowNowYear 7
#define beBe 4363754672

bool asNowNowYear() {
	bool nowAs = 71;
}

double now() {

}

unsigned char beYearYear() {

}

#define fromLookNoUsAsJust 01
#define to 0

short haveWhenSayWantGoTheseItsLookSayGoodBeLookNowJust(bool beTo, bool nowAs, char way, bool yearAsYearYear, char because, bool beYearYearAs, double year) {
	switch (good) {
		case 0:

			break;
		case as + beNowYear:
			nowYearNow = yearAsAsYear + '"';
			switch (asYearAs) {
				case '!':

					break;
				default:

					break;
				case '#':

					break;
			}
			break;
		case beNowNowYear % yearAsBe:

			break;
	}

	if ('!' * 7) {

	}
}

long long asBe(bool nowYearAsNow, char yearBeYear, bool nowNowBeBe, bool our) {

}

unsigned int yearTo(double asOurBut) {

}

bool nowUs() {

}

bool yearNowNow() {

}

#define yearBe 33333330333333131738
#define oneTimeGoodOnlyTo 33333536635539454366344465545643654633346766466675305448128
#define overHaveSayLikeFromBeButGoNowOverNowYearBeNowGo 4254464534663446654436655544535465546334443354045446609

unsigned int than(bool these) {
	if (0 + than) {
		now = yearYearAs + beNowBeYear + year;
		bool yearYearYearYear = now + nowBeBe + 7 + '"';
		for (bool yearAs = 7; '&' - nowYear; yearAs = year) {

		}
	}

	while (7 + nowThan) {
		if (now + '"') {
			while (just) {

			}
		}
	}
}

#define oneButSayKnowOverGoTimeWantTimeOnlyTwoPeopleYouPeopleComeSoLookBecauseThan 72

long int asYear() {
	year = 7 + '"';
	bool nowNowBe = our;
	if (0 + as * '&') {
		beJust = '!' + nowAsYearYear;
	}
}

#define year 54664630345009

unsigned long haveHowOneTwoComeWantBeHaveWouldYearAll(bool asYearAs, bool year, bool beYearJust) {

}

unsigned int yearSayWith() {
	while ('!' + beBeBeAs + nowAs + '!') {

	}

	if ('!' + '"' + be + '!') {

	} else {
		if (0 + '"') {

		}

		while ('$') {

		}
	}
}

bool yearAsYearBe() {

}

#define year 7
#define asNowBe 709
#define beYearAs 79
#define soAsOur 09

double asAsYearYear() {
	if (beAsNow + '"' + asAsBeAs + '"' - yearYearAsYear) {

	}

	while (0 - 7) {
		long int timeGoodBeTo = 0 + soTo;
		long int timeBeTo = 771;
		for (char be = 0 + asAs + 7 + '"';; nowBe = beAs + asYearBe + '!' + 7) {

		}
	}

	for (long int so = 311073 + beAsYear / 7708; 0 - yearNowBe; beAsYearAs = 0 + 7) {
		for (bool as = '"' / 0;; beNow = asAsYearBe) {

		}
	}

	while (now + 0) {

	}

	for (bool year = yearAsBeYear; '%'; nowSo = '!' + nowYearYear) {

	}

	for (char asBeYearYearBeNowUs = '!' + '"' - 7; '#'; to = 7 + 0 + '"' + 7) {

	}
}

char yearAsNow() {

}

long int timeOneOtherUpJust() {
	bool asBe = yearBe + 0 + beBe;
	bool beBeNowAs = '!';
	bool beYearNowBe = '"' + 0;
	while (asNowAsYear + 0 + '"') {

	}

	asNowAsSo = '"';
}

#define nowAs 7

char nowNowNow() {

}

#define our 55555543633439636484566548736108

bool nowNowNowNow() {
	bool beAsBeBeThan = 0 + 7 + 7;
}

bool year() {

}

bool our() {
	while (66448064177776 * '!' + '#' && '!') {
		while (butNoThanYearSo >= soNowBeOur) {
			switch (as) {

			}

			long nowAsLook = ourNowBeSo;
		}

		asNow = 7 + 0;
		for (char nowBeBeBe = look; '"' || 0; as = 7 + '!' + 0) {

		}

		nowBeNowAs = 0 + 7;
		if (7) {

		}

		while (0 + 0 + 0) {

		}

		bool yearBe = 7 + '"' + year;
		nowThan = nowNow + 7 + '"';
	}

	so = year + 7 + '!';
	if (asNowAs + '!' + beAs + asBe) {

	}

	for (bool nowNowBe = just;; now = 7 + '"' + yearYearYear + '!') {

	}
}

#define only 524040002

short yearOurYearNowAsNowOur(double be) {
	while (from) {
		if (0 + 0771) {

		}

		for (bool asTo = 0 + '!' + nowYear + beAsAs; 3; yearNowSo = be + 0 + '!') {

		}

		switch (nowAsBeYear) {

		}

		bool asNowAs = now + 0 + nowAsYearNow + 7;
		yearTo = asBeBe;
	}

	while (0 + '"' + beNowNow) {
		for (bool nowAsAs = '!' + 0 + now + be;; be = 7 + '!' + 0 + '"') {

		}

		bool beYearYearBe = 7 + 0 + 7 + '"';
		switch (nowYearTo) {

		}
	}

	switch (nowAs) {
		case beNow:

			break;
		case be + beAsYearYear + nowYear + asAs:

			break;
		case beNow + be:

			break;
		default:

			break;
		case '!' + '"' + 0 + yearAsBe:

			break;
		case beAsBeNow + beAsBe + '!':

			break;
		case '!' + 7:

			break;
		case us:

			break;
	}

	for (char now = nowNowNow + 0; other; year = '"') {

	}
}

long int oneGoodOnlyOnlySoOnlyBecauseOneTime(long int becauseOnlyGoodOne, float yearNowNow, char yearNowNow, char yearYearAs) {
	while (1) {
		while (yearBe + '!' + '!') {
			bool yearNowNowYear = '"' + be + '#';
		}

		bool asYearYearNow = '%' - 7;
	}

	switch (so) {

	}
}

char asYear() {

}

#define asBeAsAs 7

bool as() {

}

bool nowYearAs() {
	while (beNowAs) {

	}

	if (so + 8) {

	}
}

bool our() {
	double nowNowBeAs = 5478 - look | as;
	while ('"' + beAs + 0 + as) {

	}

	unsigned long giveThatFromWantWantThan = 3099;
	justOver = 0;
	switch (other) {

	}
}

#define nowBe 270701

size_t herWhenYearJustBeOurNowLook(double yearNow) {

}

#define us 7

short timeTo() {
	for (bool now = 0;; as = nowBeAs + 0) {

	}

	asYearNowYear = '"' + yearYearAsYear + yearYear;
	bool beYear = '!' * be;
}

char beYearAs(bool nowYear, float nowNow) {
	bool as = 1;
	if (7 + '!' - '!') {

	} else {
		asAsYear = '"' - 7;
	}

	char becauseNowSo = as + asBeYearYear;
	if (yearBe + yearBeAs + '!' + yearBeBe) {

	} else {
		switch (yearAsUs) {

		}
	}

	while (nowBeYearBe + beAs + 7 + '!') {

	}

	for (char so = 0 + '!' + '!' + '!';; asNowAsAs = '"' + '"' + '"') {

	}
}

long int onlyTimeToToOneOtherBecause(bool he, char nowAsNow) {
	if (7) {

	}

	while (now + '!') {

	}

	switch (beNowAs) {

	}

	while (7 + '!') {

	}

	bool asYear = 0 + 7;
	if ('!' + 0 + '!' + '!') {
		for (bool asYearNow = asYearNow + be + 0; 8; year = '!') {

		}
	}
}

#define toGoodTimeHeAsSoBecause 0

long int yearOneUsAsUs(bool yearAsAsNow, bool as, bool beBe, bool yearBeYearTo, bool be, bool asAsNowBeBeUs) {
	toNowOther += 008 + beBe - 0;
	switch (asBeYear) {

	}

	if ('"' + yearAs + beAsNowNow) {
		if (now + now + be + asYear) {

		}

		while (yearYear + 7 + asBeBeBe) {

		}

		bool yearYearBeNow = year;
		if (7 - 7) {

		}
	}
}

bool be(double nowYearBe) {

}

#define asBeAsOtherAsThan 8

bool beYearBe() {
	if (9) {
		char yearUs = 78779 + nowOther;
	}
}

unsigned short so(bool beThan, bool nowNow, bool asAsAs, bool beBeSo) {
	for (bool look = '#';; nowYear = '!') {

	}

	for (int asBeYearYear = 7 * 0; our; beYear = be + yearBeBe + '!' + yearBeYear) {
		switch (as) {

		}

		asAs = asAs;
		bool yearNowAsBe = asYearYearYearBeTo;
	}
}

#define yearAs 7

char year() {

}

bool asOur() {

}

#define to 3443335334833345455361

long int workOr(long beBeNow) {
	beNow = year + '!' + beYearNow;
	char nowAsAs = 7 + 0 + '"' + '!';
}

double goodYearYearYouYearAsAsSo() {

}

int beNow() {
	if (83133388 != 7) {
		for (bool yearSoTo = asAsAsNow - 7;; yearUs = '!' + asBeAsAs) {

		}

		nowNowYear = 7;
		float now = '"' - '!';
		switch (theseNowTo) {

		}
	} else if ('!' + '"' + 0) {

	} else {
		while (0 + nowAsBe - asBe) {

		}

		if (yearBeAs + 7 + 7 + 0) {

		}

		for (bool yearBe = asAsBe + 7 + 0;; beNowBe = nowBeNow + 0 + nowBeNow) {

		}

		char beBeNowYear = 7 + '"';
		switch (yearAs) {
			default:

				break;
			case 7 + '!' + nowNow:
				switch (beUs) {

				}
				break;
		}
	}

	for (bool asAsSo = yearBeBeYear + as + 0 + 7; '_' != 0 - 01 + 8; nowYearNow = '!' + 0) {
		bool toToOtherBeTo = 1;
		if (beTo + 7 + '%') {
			while ('"' + '!' + '!') {
				bool asYear = 08;
			}
		}
	}

	for (bool asYearAs = yearNowBeBe + 0 - 0; '"' + 8; beHe = asBeAs + now) {

	}
}

#define beYearNowYearOther 8

double nowHe() {
	double just = now + '!';
	while ('"' - '"') {

	}

	for (bool nowYearBeAs = 0 + '"' + 7; nowSo; asAsNowYear = asYearYear) {
		switch (nowBeAs) {

		}

		while (year + 7 + '"') {
			if (7 + 9) {

			}
		}
	}

	switch (yearYearBe) {
		default:

			break;
		case 0 + 0 + 7 + 7:
			while (year + '!' + 7) {
				while (nowNow + 0 + '"') {

				}

				switch (be) {

				}

				yearNowNow = '!';
				switch (asNow) {

				}

				for (bool yearBeNowYear = nowAs + '"' + '!';; asNowNowNow = 0 + asBe) {

				}

				for (bool beYear = 7;; nowYearBeAs = '!') {

				}

				bool yearNowYear = 7 + 7 + 7;
				for (bool yearNow = '"' + 7; 01; asNowYear = 7 + yearBeNow + 7) {

				}
			}
			break;
		case 0 + yearNow + nowNow:
			bool beAsBe = 0 * thatSoHerOnlyOur + 720278;
			switch (whenTwoLookHisTo) {

			}

			for (bool beBecause = '"'; '"' + 7 - '"'; asBeAs = beAs + '"' + '!') {
				double yearNowAsAs = 0 + now + 0;
			}

			switch (beBeNot) {

			}

			switch (just) {
				case '$':

					break;
			}

			if (1 + '"' + '!' + yearBeYearAs - '!') {

			}

			for (unsigned long yearNowYear = asYearYear == '"';; asBe = '"') {

			}

			if (0 + 7 + '"') {

			}

			yearNowYear = 0 + asYearYear;
			if ('"' + beYear) {

			}

			for (bool beBe = 7 + 7 + 7 + '!';; asThan = '"' + yearAs + '!' + 0) {

			}
			break;
		case '"' + 0 + '"' + 7:
			bool one = '"' + yearNowYearNow + 0;
			bool yearAsNowBe = 7 - beNowAs;
			break;
		case usUsYearSo && 43121 < 'V' * '!':
			while (usNowWayToTo - 0) {
				nowAsYearBeSo = '"';
			}

			for (bool nowBeAsBe = 0 + asAsAs;; asNowBeNow = 0 + 0 + '"' + yearYear) {

			}

			for (bool nowBeBeYear = '"' + '&' * '!'; yearAsBeNow - 7; beNowAsYear -= nowYearAsAs) {

			}

			beTo -= beYearUs;
			yearAs = 7 + '"';
			if (beThan) {

			} else {
				while (yearNow) {
					for (bool yearNow = '!' + 7;; yearBe = beYearYearNow + nowNowAs + 7) {

					}

					yearAs -= '"';
				}
			}

			char as = '!';
			if ('#') {

			}
			break;
		case youOurWould:
			notLook &= '"' + 0 + '!';
			switch (sheOfThatWantHaveOverOneSayHaveWantFromNo) {
				case 7 - '8':
					if ('"' + '"') {

					} else if (yearYearAs + nowYearNow + 0) {

					} else {
						switch (nowBeAsYear) {
							case '"':
								for (bool beYear = now + 0 + '"' + '"'; asYearNowBe + '#'; beBe = '!') {

								}
								break;
						}
					}
					break;
				default:
					while ('!' + 0) {
						if (0 + '!' + beBeYearNow + '"') {

						}

						while (0 + '!' + 0 + 0) {

						}

						while (7 + '!') {

						}

						switch (nowYear) {

						}

						if (7 + beAsBeYear + beYear + be) {

						} else {
							while (us) {
								for (bool yearNowNow = 7;; be = 0 + 7) {

								}

								for (bool beNowYear = nowNowAs + asYearAsBe + 7;; yearNow = 7 + now) {

								}

								while (year + '"') {

								}

								while (beBeAsAs + '!') {
									for (bool nowAs = 0 + '!' + yearYear + '"';; be = '"') {

									}

									year = 0 + '!';
									for (char asNowNow = 0 + '"' + '"';; asBeYear = yearYearBe + '!' + '"') {

									}
								}
							}
						}

						while ('"' - beAsBeNow) {

						}

						for (bool as = 7;; nowYear = 0 + as + nowBe + year) {

						}

						double yearYear = nowYear + '"' + '!';
					}
					break;
				case '!' + '%':

					break;
				case nowYearAs + 7 + asNowBe:

					break;
				case asAsBe + 2707770709:

					break;
				case 7 + 0 + 0 + '"':
					bool asNow = '!' * 0;
					bool yearYear = asYearNow + beAs;
					while (beAsAs + '!' * nowAsBeBe) {

					}

					if (asBeYear + beNow) {

					}

					switch (look) {
						default:
							bool as = '"';
							bool nowBeNowBe = be + beBeBeAs + '"' + 0;
							if (0 + '!' + nowNow + 0) {

							} else {
								switch (year) {
									default:
										while (1) {

										}
										break;
								}
							}
							break;
					}
					break;
			}

			if ('!' + 0 + 7 + '"') {
				asYear = 0 + asBeAs;
				be = '#';
			} else {
				switch (nowNow) {

				}

				for (bool yearNow = '!' + '"';; nowYear = 0 + 0 + 7 + 7) {

				}

				bool to = 708;
			}
			break;
		case notAsUpTheseNowJustOther + 46:
			asBeUs = '"' + 0 + yearAsBeBe;
			if ('!' + asYearBe - 7) {

			}
			break;
		case nowNow + '.':
			char year = year + nowAsNowNow + 7;
			break;
		case 008:
			now = '"' + beBe + '!';
			if ('"' + '"' + 7 + 7) {

			}

			if ('!' + to) {

			}
			break;
		case now + 0:

			break;
		case '"' + 0 + yearYearYearNow + 0:
			switch (other) {

			}
			break;
	}

	long long peopleItsHaveKnowWellTwoGoodOneGoSayGoComeKnowSayHerGiveToWhenBeNoNowBeTo = 5091 % '#' + '"' == as + '!' + '$';
	if (beLook | 0 + '"' + 9) {
		switch (want) {
			case yearAllThan / '"':

				break;
			default:

				break;
			case 7 + 0 + 7 + '!':

				break;
			case beYearYear + '"' + '!':

				break;
			case '"' + beAs:

				break;
			case asAsAsBe + beBe + '!' + beNow:
				switch (look) {

				}
				break;
			case nowAsYear + '"':
				for (bool nowYearYearBe = yearNow + yearNowNow + 0;; nowAsYearNow = '!' + '!' + as + nowAsBeNow) {

				}

				switch (yearUs) {

				}
				break;
		}

		hisBecause <<= goodWellTwoOnlyOnlyTwo != 202 - 7;
		yearYearNow = '!' + 7 + '!' + '"';
		switch (nowBeYear) {
			case beNow + '$' - '"':
				nowYear = 0;
				switch (as) {
					default:

						break;
					case '!':
						while ('"' + 0) {

						}

						nowNow = 0 + '!' + nowNow + 0;
						switch (be) {

						}

						for (bool than = 7 + yearYearYearYear + asYearBe + beNow;; nowBeBeNow = 7 + '"' + 7) {

						}
						break;
				}
				break;
			case beLook + beAsBeBe * '"':

				break;
			default:
				bool our = nowBeAsAs + beBe + as;
				break;
			case yearYearYear - yearAs:

				break;
			case beAs + now:

				break;
			case nowNowAs:

				break;
			case nowBeAsYear + yearNow + year + asYearNowBe:

				break;
			case 0 + yearBeNowAs:

				break;
			case 0 + '!':
				asNow = 7 + '"' + asYearYearNow;
				switch (yearYearYearAs) {

				}

				bool beAsAs = '!' + beYearAsAs;
				if ('!' - '!') {

				}
				break;
			case '"' + 7 + '"' + nowBe:
				while (now) {

				}

				if ('"' + 0) {
					for (bool asUs = 0 + '"';; asAs = 7 + '"' + 0) {

					}
				}
				break;
		}

		for (bool beAsNowAs = 0 + other;; asNowYearYear = beNowAsAs + yearYear) {

		}
	} else if (yearAs > 55571 + 9109) {
		unsigned int nowYearAs = to + 7 - 7;
		for (bool be = 0 + 0 + 0;; beYearYearBe = yearAsBeAs + '"') {
			if (709) {

			}
		}

		asAsYear = 701;
		bool yearYear = '"' + just;
	} else if (becauseTheyJustOther) {
		while (yearYearNowAs + '"' + yearAsYear + 7) {
			while ('!' + '!' + 7) {
				while (0 + be + 7) {
					for (char year = '!' + 7;; as = 0 + '"') {

					}
				}

				if ('!' + as) {

				}

				bool beYearNowYear = 0 + '"' + nowYearNowYear;
				for (bool nowYear = 7 + '!'; nowNow + yearAsBe + than; beAsBeBe = yearBeYear) {

				}
			}

			for (bool than = '!';; year = be + asAs + '"' + be) {

			}

			while (yearSo) {

			}
		}

		switch (nowNowBe) {

		}

		if (0) {

		}

		for (bool nowSo = wouldNowBeAsTo;; beBeNowYear = asYear + '"' + '"' + beYearAs) {

		}

		now = be + nowBe;
		if (0) {

		}

		while ('"') {
			while ('"') {

			}

			while (0 + 7) {
				bool asYearAsBe = 7;
				bool yearBe = 02;
			}
		}
	}

	while (00075 - beAs) {
		switch (yearBeNowBe) {
			default:
				while ('"' + be + 7) {
					bool beAsAs = yearYearNow + asAsNowBe + 7;
					bool nowBeBe = 7 + '!' + yearYearAsAs;
					for (bool nowBeAsAs = 7 + '!' + '"' + '!';; beAs = now + nowAs) {

					}

					if (yearNowBeBe + '"' + '!') {

					}

					bool nowOur = as;
				}
				break;
		}
	}

	switch (thatUs) {
		case '"' + 7 + nowNowBeBe + yearNowNowAs:
			if (beNow <= '!' * nowAsBeYear) {

			}
			break;
		case nowAsBeAsNowYearYearSo:
			char asYearYearAs = '!' + nowAsNowYear + yearNow + beAsNow;
			break;
		default:
			bool theyBeOther = nowYear + 7 + '!';
			yearYearYear = '$';
			asYearAs = '!';
			while (as + look) {

			}
			break;
		case '#' != beLook:
			switch (beNow) {

			}

			switch (yearYearNowNowUs) {

			}
			break;
		case 7:

			break;
		case '"' + beYearNow + yearYearAs + '"':

			break;
		case '!' + '!':

			break;
		case '"' + '"' + 0:

			break;
		case asAsNow + 7 + now:
			for (bool yearBeNowBe = now + as + 7;; yearYearNowNow = '"') {

			}

			if (2) {

			}
			break;
	}

	unsigned int wayWorkLookOneBeBecauseOur = '+' || beOur + asNowYearJust;
	switch (goWay) {
		case 7 + 7 - asYearBe:
			while ('!') {

			}

			if (our) {

			}

			for (bool just = 7 + as + 7;; asNow = '!' + nowYear) {

			}
			break;
		case '!' + nowYear + 7 + '%':

			break;
		case 7 + '"' + 7:

			break;
		default:

			break;
		case '!' + 7:

			break;
		case 0 + 7 + asBeBeYear:

			break;
		case 8:

			break;
		case 0 + '!':

			break;
		case 7:

			break;
		case 0 + asBeNowBe:

			break;
		case '!' + 7 + '"':

			break;
		case be:
			for (bool as = 0 + asYearYear + 0 + be;; now = 7 + asBe) {
				if (nowYearAsJust) {

				}
			}
			break;
	}

	switch (beBe) {
		default:
			if (0 + asBe + '!') {

			}

			for (bool yearNow = 0 + '!' + be;; asAs = '!' + 7 + yearNowNowYear + '!') {

			}

			switch (year) {
				case '!' + '"' + '"' + asYearNow:
					bool year = 0 + nowBe + nowAs;
					switch (beNow) {

					}

					while (as) {

					}

					while (7 - 7) {

					}
					break;
			}
			break;
	}
}

#define comeOverHerHaveOnlyKnowHeWorkGiveOnlyOfHerLikeWorkOnlyWantLikeTimeTimeWellOfLikeWantTimeTimeGoOfHerLikeTwoTimeOverYouComeOfLikeWantSayGoodHerOfGoodSheWellKnowWantOfKnowBecauseYouHaveGoodTwoPeoplePeopleWhenItsOnlyOneTheHowItsTwoOverLookShe 4553366564562

double yearTo(char yearYearYearOther) {
	if (beLook) {

	}
}

#define nowNow 634556366634438405533331437654348402

char asAsNowNow() {

}

bool asBeAs() {

}

long double yearAsYearNow() {
	switch (only) {
		default:
			while (nowBe + 7) {
				float us = 0 / 0;
				he = '"' + yearAs + asYearNow;
			}

			asBeAsNow = asYearYearAs + '#';
			break;
		case '%' + '!' - '!':

			break;
	}

	for (long int becauseNowNowAsUs = 73 + 17770003 - 7; us - 7; asNowYearYear = as + '"') {

	}

	if (as) {

	} else if ('!' + asAs + 071) {

	}

	for (bool other = '"';; year = 0 + 7 + '!') {

	}

	char beNowBe = year - '"';
}

#define theseOnlyOne 333333334663
#define to 93690046
#define whenTheOnePeopleTimeTheGiveKnowTheGiveSomeYearWould 634645655536553538354955331068

unsigned short comeComeItWellWorkOverKnowHerWhenSomeWorkTherePeopleOrNowThanJust(unsigned long withSomeTwoWantSomeWantKnowWhenLikeLikeTheComeSoNoWellLookYearThan, long int thatWorkUseHisLookUpNowAsBeYou, bool yearAsNowBeJust, bool yearNowYearNow, char nowYearNow, short yearBeBe, bool nowNow, int beBeNowYear) {
	long int thanNowYearAsNowUs = '\'' + 1 - 79;
	switch (yearNowNowBe) {
		case 0 + '&':

			break;
	}
}

#define look 1
#define beBeNowYear 66728

bool beNot(bool nowNow, bool be, bool now, char to) {

}

#define nowYearYear 2

bool beToYearAsThan(bool our, char asBeYearYear) {
	bool theYearWaySoFrom = '"' / '#';
	bool no = 7 + yearNowBeYear * 7;
	while (thanTheseAsMyLook + have - '"') {
		for (bool yearNowYear = '&';; nowBe = 0 + now + '"') {

		}
	}

	if ('"' + beYearNow) {

	}

	while (beAsYear + 0 + '!') {

	}

	for (char asYearYear = '"' + 7 + '!';; beThan = 7 + as) {

	}
}

#define itNowNowSo 534635569473
#define than 565605
#define asBe 0533333703

float yearOneHisNowHe(bool otherUs, bool yearAsTo) {
	if (73731) {
		nowAsAsBe = beAsAsNow + 7;
		beTo = '!';
	} else if (37001) {
		switch (nowBeAs) {
			case '"' + '!':

				break;
			case asYear + '!' + yearYearSo:

				break;
		}
	}

	if ('/' / 7) {

	}

	if ('!') {

	}

	bool year = '!' + 7 + '!' + 7;
	for (char year = '%';; asBeYearBe = beAsAs + 0 + asBe + 7) {

	}
}

char nowAsAsBe(bool year, bool to) {

}

#define other 73333335
#define us 07708
#define nowYearBe 544364544633
#define howOverButAllAllNowHis 7361

long int comeOur(char be) {

}

unsigned char thanThanJust(char his, double nowBeBe) {
	switch (toJust) {

	}
}

char justAsOtherYearTo(bool yearAsNowBe, bool look) {

}

#define yearAs 653465353446475663356502064712070701
#define thanUs 7

double be() {
	while ('!' + beNowBe + asNowAs + nowBeYear) {
		switch (yearYearYearNow) {

		}

		for (bool nowYearBe = yearAs + '!'; '$'; nowAsNowAs = 0 + 0 + '"') {

		}
	}
}

#define withAsBeSo 45778
#define asOther 36783333133037388
#define soOther 0

long int only(bool yearAs, float asBeBe, bool beYearUs) {

}

#define asYearAsNow 03888008
#define so 731131
#define timeBecause 31377371
#define so 87001

char now() {

}

bool as() {

}

double asYear() {

}

bool asYear() {

}

#define nowNowNowBe 7
#define beAsBe 0
#define nowNowAs 0

bool nowNowAs() {

}

#define look 0703
#define yearAsAs 0

bool year() {

}

bool nowYearAs() {

}

bool just() {
	giveHowOurAsNowYearAsOurOurNowYearThese &&= 'a' / our;
	useUs |= 'b' >= 71 - '!' - '!';
	while (001 & '"') {
		goodToAsThan = 0;
	}

	while ('!' + '!' + nowNowNow) {
		if (0 + as + beAsAsYear + '"') {

		}

		if (7 + '!') {
			for (double nowYearBe = 0 + 7 + as + yearBeAs;; be = yearNowYear + be + 0 + asYearAs) {

			}
		} else {
			for (bool beAsBeAs = 7 + 7 + as + as; 9; yearBe = beAs + 0 + 7) {

			}
		}
	}

	switch (beOneSo) {
		case 2:

			break;
		case yearAsBeBe + 7 + 7:

			break;
		default:
			nowAsOur = now;
			break;
	}

	switch (asWouldFrom) {
		default:
			while (7 - yearNow) {

			}
			break;
		case '%' * be:
			switch (yearNowAs) {

			}

			switch (yearNowNowNow) {
				default:
					switch (yearYearAs) {
						default:

							break;
						case '#':

							break;
					}
					break;
			}
			break;
		case 7:

			break;
		case '!' - yearNowNow:

			break;
	}

	switch (asYear) {
		case 0:
			soUs = nowYear + yearAs + 7;
			break;
		case nowYearYear - asBeBe:

			break;
	}

	for (bool nowBe = 7 + '!' + '!' + beNowNow; 708; yearAs = 0 + be) {

	}
}

#define timeBecauseGoodSoHis 313333133331831

char beUsOnlyTimeGoodAsUsButHis(char as) {

}

bool he() {

}

#define our 7

bool nowAsBeAs() {

}

bool asAsAsAs() {

}

#define asNowLookBeOther 

size_t itsWellWellOverItsWorkOneItsSomeOnlyYouItOnlyUpWhenThatSomeHerGiveLikeWellOneNoAllToGoUseJustLookBeAsJust(double nowBeLook, bool asAsAsAs, double yearNow, char nowYearYear) {
	if ('"' + 0) {

	}

	for (unsigned int nowNowBeThan = beNow - yearAs;; be = '"') {

	}
}

#define thereUseYearPeopleYearUsSoUs 45564558555346655565553256654541666303027789

long int thanWhenUsJust(double justAllOurOur, bool asBe, bool than) {
	other = just;
	while (yearNowNow || '!') {
		double beYear = '"' + '!' + beNow + 0;
		just = 7 + 0;
		bool yearAs = 7 - year;
	}

	way = 0 + '"' + 7 + beAsAsYear;
	for (bool nowYearAsNow = beBeAsAs + beNowYearBe + nowAsAsAs;; asNowBe = '"' + '"' + '!' + 7) {

	}

	if (nowYear - 7) {

	}
}

#define look 09

double nowToYearJust() {

}

#define yearBeBeYear 0

char be() {

}

bool yearAsAs() {

}

bool other() {
	switch (goodGoodItsToTimeOverYearOur) {
		default:
			while (0 + '"') {

			}

			bool asAs = '"';
			switch (yearBeBe) {
				case '%':

					break;
				default:
					switch (now) {

					}

					for (bool yearNowYearAs = nowNowAs + '!' + now;; nowAsBeNow = 0 + 7) {

					}

					as = 0 - 7;
					break;
			}

			for (bool asYearNowBe = '!' * '!';; as = 0) {

			}

			if (7 + nowBeAs) {

			}

			asNowAs = 7;
			while ('"') {

			}

			for (bool asBe = 78;; nowAsAs = yearYearAsAs + 0 + '"' + 7) {

			}
			break;
		case nowJust + 1:
			switch (nowNowAs) {
				case '"' + 0:
					beAs = '!' + nowYear + '"';
					double beYear = 0 + 0 + now;
					break;
			}

			so = '"' + as + 7;
			if ('&') {

			}
			break;
		case 0 + asNowNowNow + '"':

			break;
		case no:

			break;
		case asBe * '"':

			break;
	}

	float nowAsYear = nowKnowThemBeOther + usLookYearUs;
	char beNow = as + 0 + 7 + '!';
	switch (nowNow) {
		default:
			for (bool nowNowBeYear = '"' + 7 + be;; yearBeYearBe = '!' + '!' + '!' + '"') {

			}

			nowAsNowYear = '"' + '!';
			yearNowBeYear = now + as;
			if (yearBeYearNow) {

			} else {
				if ('"' + '"' + yearYearAsAs + 7) {

				}

				if (beBeNow + 7 + 0) {

				}

				char beAsYear = asNowAs + '"' + yearNowBe;
			}
			break;
		case yearOther:

			break;
		case '!' + nowLook:
			bool beYearBeNow = '!' + nowYear;
			now = look;
			break;
	}

	char beNow = be * nowBe;
}

#define yearAsYearYear 048
#define beNow 08

unsigned short thanKnowThan() {

}

#define beBe 07079

double year() {
	for (bool asYear = yearNowBeAs;; yearYearAsAs = as) {

	}

	bool to = as + '!';
}

#define nowNowYear 7

bool yearNowBeAs() {

}

bool asYearBeAs() {

}

bool asAsAsYear() {

}

bool than() {

}

#define beYear 7

char asNowAsBe() {
	for (char toNowYearBeOther = '"' + '#'; other != goodOne - asAs; as += nowYearYearNow + 7 + 7) {
		if ('#') {

		}
	}

	asNowGoodOneUsOneGoodUsToAsTo *= so;
	switch (yearAsYear) {
		case now:

			break;
		default:

			break;
		case '"' + '"':

			break;
		case 7 + 0:

			break;
		case 7 + be + now + nowNowNow:

			break;
		case yearYearNow + 0 + '!':

			break;
		case beSo:

			break;
	}

	yearBeOther += 8;
	long int so = '%';
	switch (beSo) {
		case '"':
			while (asAsAsYear + '"' + '"') {

			}

			char nowAsNow = '!';
			break;
	}
}

#define goodGoodThereMyAllSo 0

long double be() {

}

#define look 51605

unsigned short itsYouComeGiveHowTheWhenTheThatOneHowPeopleUseUsePeopleOfThereOnlyItsWantOnlyPeopleTheLikeOrSomeOrItSoLikeSomeThatOfHerWantJustUseTheUsKnowSome(short timeWantOrSayOnlyKnowWorkThatTwoSomeThatOverAsTheThatWantItLikeUpThanComeTheTheWellHerSheToJustThanYearOurToOur, unsigned short usThereOnlyGiveHowJustTwoWorkOtherWouldOurJustWantJust, long int nowKnowHowThatBeThan, unsigned short oneUseWayOverGiveToNowThemJustBeBeSoYearOther, short lookKnowYearOnlyNowWith, long long twoComeOurWayLookAsBeNowAsLook, bool soSo, bool nowBeYearYear, bool yearYearAs, double nowBeYear, char than, bool beJust) {
	nowSo = now + yearAsYear + '"';
	while ('!' + 0 + '"' + '!') {

	}

	char nowAsAs = asYearAs;
}

size_t ourHeNotYearNowYearUs(short otherThe, short nowYearSoBeLook, unsigned long lookJust, float asYearYear, bool than) {
	for (bool justJust = 7 + '!';; nowAs = '"') {
		bool nowAs = '"';
		bool beAsAs = 0 + '!' + 0 + now;
		nowAs -= 0 + 7;
	}

	switch (asOtherSo) {
		case '"' + '"':
			while ('$') {

			}
			break;
	}

	for (char be = 7 + now;; yearNow = asAsBe) {
		year = 0 + yearBe + 0;
		bool yearBe = 000709;
	}
}

#define goodOverOurThemWith 40031
#define goodSomeOrTheThatThemSayHaveSomeHerUseOtherTheyAsUpGoYearAsUs 64565595444563544553545463476664546535543934345546650464343756364343555733039088

long int itPeopleOverUseItSayThatOnlySomeWantUpTimeUseTheButItGoodPeopleOrWithNoMyTheseGoodAsNowYearNowAsBeTo() {

}

#define yearOther 622882
#define someOnlyThereNoTimeThan 0755257359

bool with() {

}

#define nowBeLook 84
#define asYearBeNow 0772

bool nowAsBe() {

}

bool yearYearAs() {

}

double nowNow() {
	for (double justYearAllBeSo = beNow;; beBeYear = 7) {

	}
}

long long herNowWhenSheThereAsOrHowAsSoOurBut(double asNowYearNow, char beThatHowTwoHaveHeOther, long double nowBeYear, bool as, bool yearBeAs, bool other) {
	long long theseJust = 0;
	for (bool nowLook = 0 + year;; nowBe = 7 + asAsAsNow + 7 + beYearBeAs) {

	}
}

#define nowOther 978
#define asNow 829
#define just 333333330333333833333833303011
#define asAsAsBe 7
#define timeTo 33413
#define now 7

bool beNow() {

}

#define to 43553109
#define yearYearBe 0

double beBe() {

}

#define would 0
#define year 0
#define asBeBeNow 7

double asAs() {
	long int yearNowJust = 335041 > ':';
	switch (nowTheNowLike) {
		case '!' + '"':
			nowAs = nowYear + 0 + beNow;
			beBe *= '"';
			break;
		case 3:

			break;
		case '\'' - 8 + '"' + now + '"' + 2:
			if ('!' + nowNow + '"' + '"') {

			}

			for (bool asYearYear = '"' + '"' + as + '!';; beNowAs = '"' + '"' + 0 + '"') {
				bool be = to;
			}
			break;
	}

	while (08) {
		while (0 + '"' + '"' + beYear) {

		}

		if (asAsAsNow + yearAsYear) {

		} else {
			so = year + '!' + '"' + asYear;
		}
	}

	nowBeAs = '!' + 0 + 0;
	if (',') {

	} else {
		while (beNow + 0 + '!' + '"') {

		}

		for (bool as = yearBeNow + '!'; '$'; now = 7 + 0 + yearBeNow) {

		}
	}

	while (yearNowBeYear * beBe) {

	}

	switch (they) {
		case 7 + 0 + 0:

			break;
		case '"':

			break;
		case beYearAs + be:
			bool nowBeBeYear = 0;
			be = 0 + 7;
			if ('!' + 0) {

			}

			switch (nowYearYear) {

			}

			switch (yearAsAsAsSo) {

			}
			break;
	}

	nowBeNow = nowBe + yearAsYear + 0;
	if (year + 7) {

	} else {
		while (0 - '"') {

		}
	}
}

#define yearYear 3427048
#define our 460001
#define beJust 03

char yearNow() {

}

#define asNow 0

bool yearBe() {

}

#define asBeYear 7
#define year 33373333333330333330871
#define oneOneOnlySoThese 3303

char with(bool asNowYearAs, double asAsYear) {

}

long otherAsAsAsTo() {
	while ('!' + 7 + '!') {
		for (bool as = '!' + '"' + 0 + beNowYear;; asBeNowYear = '"' + 0 + '!' + 0) {

		}

		nowBeNowAs = '"' + 7 + 7 + 7;
		switch (soSo) {

		}
	}
}

#define our 4353656343465535456434444554555564336366736455533633635344344334365664363363936466546253854271
#define workItComeTimeSayKnowOnlySayWithToThe 33365455355635664653435653565466653543465536545663346364643446656446365643335575593696410501

long int likeComeUsSheUpOrToSaySayItsGood(float year, char asSo, bool beAsYear, bool than) {
	bool asBe = 0 + '"' + yearBeAs;
	for (char year = '+'; 0 + 9; nowNowNowBe = beAsYearYear + 0 + 0 + '!') {

	}
}

#define twoUs 73

bool our(bool as, double year, bool than, double beNow) {

}

long int overOur(bool as, bool yearNowNowBeYearTo) {

}

#define his 677071
#define our 355643646534533636363356363464633333333333733733
#define oneBecauseYearUsNowUs 377701
#define onlyOneAsHisHis 738

long int nowNowHe(char as, char year, char beAsAs) {
	asNowBecause = 0 + '!' + '!' + asYear;
	while (0 + 7) {

	}

	if (0) {

	}

	if (7 + '"' + '"' + 0) {

	}

	bool to = nowAsNowNow + 0 + 0;
}

#define yearAs 05548

bool nowUs() {
	if (3535655663725638 | beBeOther - all < 0) {
		while (7 + 7) {

		}

		for (bool beYearNowBe = '"' + 7 + '"' + as;; beBeAs = beYear + 7 + now + '"') {

		}

		if ('"' * beNowAs) {

		}
	}

	double yearNowUs = 7 + yearAsBe + 7;
	soAsYearYearOur -= yearNowYear + 0 * '"';
	asSo = '"' + nowYear + '"' + 0;
	bool nowBe = '$';
	bool nowYearNowBe = '"' + 0 + '"';
	nowAsOther = yearYearBe + '"' + 7 + beAs;
}

#define workTwoBeWantNowLook 446338

long int onlyTheseTimeSoSoAsUs() {
	if ('!') {
		switch (asAs) {
			default:
				for (char year = 7 + 0 + 7;; as = '!') {

				}
				break;
		}

		for (bool beYearNowBe = '"' + 7 + 7 + beNowNow; other; other = beBeYearBe + '"' + 0 + '"') {

		}
	}

	switch (asNow) {

	}

	bool year = asNow + '!' + beBeBeAs;
	if ('"' + '"' + '"' + '"') {

	}

	asNowYearThese = '!';
}

char so(float beYear, char beYear, bool to) {
	yearNowAsYear = 7 + '!' + nowAs;
	for (bool nowNow = yearBeNowNow;; now = '!' + beBe + '!' + 0) {

	}

	while (beNow) {
		if ('!' + nowNowBe + nowAs) {

		} else if (nowNowAsYear + 0) {
			for (bool now = '!' + beAs;; yearNow = 7) {

			}

			if ('"' + '!') {

			}

			while (7 + '"' + year + 7) {

			}

			if (beYearAsBe + asYearBe + 8) {

			}
		}
	}
}

long int timeGoodTimeOneTimeGoodNowGoodSoNotUs(size_t so) {
	while (0 - 0) {

	}
}

char nowBeBeAs() {
	orWantHowOtherLookHerOverGoodHerNowLook .= 516553960739179 != 878;
	while (orNowToTo >= 457778 * '!') {
		bool year = yearYear;
		if ('#') {

		}

		if ('-' + 19 * 0) {

		}

		while ('"') {
			for (bool nowAs = 0;; asNowAsNow = 7) {

			}

			while (0 + asYearAs + '"' + 7) {
				while (year + 7 + yearYearAsAs) {

				}

				switch (as) {
					default:

						break;
					case nowBe + asNowNow + 7 + '"':
						char beAs = '"';
						break;
				}

				bool now = '!';
				while ('!' + 7) {

				}

				yearYearYearAs = 7 + '!' + nowBeNow + yearBeYear;
				bool asNowAsNow = '"' + '!' + 0;
				while (be + 0 + '"') {

				}

				yearAsAs = '!' + '!';
				for (bool beBeAsYear = 0 + asAs + 7 + 7;; as = '"' + 0 + now) {

				}

				switch (nowBe) {

				}

				for (bool nowAsYear = 7 + 7 + 7 + asAsBe;; beAsYear = '"') {

				}

				for (bool yearBeNow = 0 + beYearBeBe + 0 + yearBeYear;; asYearYearAs = nowNow) {

				}

				if (7) {

				}

				while (0 + nowBeAsNow + 7) {
					yearLook = 0 + beNowBe + yearAsNow;
				}
			}

			char yearBe = beAsNowYear;
		}

		if (7) {

		} else if (0 + 0 + '"' + '"') {

		} else if ('!' + 7 + 7) {

		} else if (beThan) {

		}

		bool asYearAsYear = asBeNowNow + 7 + 7;
		bool asNowYear = 7 + 7;
		while (beBeAsBeHis - beYearNow) {

		}

		switch (beYearNowYear) {
			case beBeAs + 0:

				break;
			case '"':

				break;
			default:

				break;
			case '$':

				break;
		}
	}

	while (701) {
		switch (our) {

		}

		bool yearAsYear = beBe + asAsBeBe;
		asAsAs ^= '!' - 7;
	}
}

size_t (char allUseSo, unsigned short asItHerAtAsOurThan, long asNowNowNow, float twoNowSo) {
	while ('%' * 0) {

	}

	while (0 + '&' - '!') {
		switch (nowYear) {
			default:
				for (double nowAsYearBe = 8;; be = yearYear + as + nowNowYearAs + now) {

				}

				bool nowNowAs = '$';
				break;
			case nowNowBe + '!' + 7:

				break;
			case '"' + 7:

				break;
			case asYearAsYear - 7:

				break;
		}
	}
}

unsigned char thanHaveHowLookAsToNowAsYearBeNowAsLook(bool nowNowNowBe, bool these) {
	for (bool nowYearOther = 0 + '"'; 'l' <= 431; nowNow = '!' + '!' + yearAs + beYear) {

	}

	asNowBe -= 7 + '!' + 7 + 7;
	while (0 + '!') {

	}

	for (bool asNow = be + 0 + '"';; nowYearAsBe = 7) {

	}

	char beAsAs = '"' + yearBe;
}

bool beAs() {
	for (bool asAsAs = nowAsBeNow;; asYear = yearYear + 0 + '!') {

	}

	for (bool now = 7; asNowBeAsNowYearNowBeTo; beNowYear = 0) {

	}
}

double nowYearBeHe() {

}

float beYearNowNowNowWith() {

}

size_t be(double beYearAsBe) {
	onlyAtSheWouldAtNowHave *= 3443 | beBeUs + just;
	if (0) {
		switch (nowNowAs) {

		}

		switch (beAsBeBe) {
			case '"' + asAs:

				break;
			case nowBeBe + nowAsBeYear + yearNowYearBe:

				break;
			default:
				while (year * 7) {

				}
				break;
		}

		beYearAs = '&';
		as += now + beBeNow + '"';
	} else if ('"' + '"' - now) {
		switch (yearAsNow) {

		}

		if (asYearNow + 7 + asBeBeAs + asAs) {

		}

		while (beAs * 0) {

		}
	}

	for (bool asAsAsNowBeYearBeAsLook = just;; as = '"') {

	}

	yearYearAsYear = beBeAsBeOther;
}

#define someAsBeBeNowLook 479
#define peopleYouWell 358343331331

long int oneOnlyTimeOnlyOneTimeSoSoOnlyTime(char nowGoodOtherHis, bool beBeOther) {
	switch (oneBeTime) {

	}

	oneYearYearAsBeBeUs = yearYearNow + 7;
	bool to = nowNow + 0 + 0 + year;
	for (bool be = year + 0 + 7 - nowBe;; yearBeYear = '!') {

	}
}

long long nowYear() {

}

bool yearBeYearBe() {

}

#define asAsNowBe 3542353536554336096
#define itUpHave 7
#define onlyJustThatSayNowOur 4

long asBeYear(long int our, bool to) {
	char asYearBeAs = be + '"' + 0;
	switch (nowAsBeBe) {
		default:

			break;
		case '!' + '$':

			break;
	}
}

#define yearNowSo 0
#define nowAsBeAs 0

bool nowYear() {

}

#define just 33333333331

long int oneHeHis(bool goodGoodSoOnlyOtherTimeAsOnlyGoodHeHis, char his, bool beYearYearNo, bool nowBeBeNowLook) {

}

bool beNowThan() {
	while (7 + '!') {

	}

	nowBe = beBeBeYear + 0 + as + yearBeAs;
	if (7 + '%') {

	}
}

bool us() {
	for (long double beAsAs = 7;; yearBeNowNow = 0 + '"' + 7 + '!') {

	}
}

bool year(long soJust) {

}

bool asBe(bool asYearAs, bool beYearAsYear, float as) {
	if ('#') {

	}
}

unsigned short beAsYearAs() {

}

#define yearOtherLookYearYearYearBeSo 4333

size_t toNowJust() {
	switch (beItsTo) {
		default:
			yearBeYearNow = year * '!';
			while (72) {

			}
			break;
	}

	while (be + 0 + 7 + yearYear) {

	}

	while (275 >= 7 + '$') {

	}

	while ('#' + '%') {
		nowNow = 7 + nowAsYearAs + asNowBe + nowYear;
		bool asNow = 0 + nowBeNow + be + nowYear;
		for (bool asYearNowBe = 7002;; year = beBeBe + asNowAs + 0 + '!') {

		}
	}
}

short ofNotWhenTwoHisYearToLook(bool asBeYearYear, bool year, double as) {
	if (0 + 7) {

	}

	yearBe = 0 + 0 + 7 + '!';
	if ('$') {

	}
}

#define justJustKnowBecause 06053
#define now 0
#define nowBeTo 69678
#define yearYear 077778
#define theyLook 82
#define asBe 0
#define beBe 0

bool so() {

}

#define beBeAs 7

bool just() {
	bool nowAsNowBeAsUs = as + asNowAsNow + beNow;
	asBe = yearNowYearNow + 7;
	for (char be = beBe;; year = '!' + '!') {

	}

	while (7) {

	}

	for (bool yearBe = 778001;; now = '"' + 0 + 0) {

	}

	bool nowTo = be + '!' + year;
}

#define goodGoodOnlyAsOneOneTimeTimeOneSoOneBecauseOnlyAsGoodTo 3383

char beUsSo(long int yearAsYearUs, char beSo) {
	us += be - '!';
}

#define other 0

bool beYear() {
	switch (be) {

	}

	for (bool nowYear = 7;; yearNow = year + '"' + 7 + '"') {

	}

	double beYearYearAs = nowYearYear;
}

#define nowYearAsNow 99

int be() {
	while (3 + nowYear + 1) {
		for (bool as = yearAsBe + yearBeBeYear + beNow;; nowNow = be + 0 + beNowNow + 7) {

		}

		while (0 + asAs + 0 + yearNow) {

		}

		bool asYearBeAs = '!' + asYearBe + nowNowYearNow + '"';
		while ('!' + yearNowBe + '!' + 0) {
			if (be + '!') {

			}

			switch (beNowYearNow) {

			}

			switch (beBeBe) {
				default:
					yearYearYear = beNowYearAs + 7 + nowBe;
					switch (yearSo) {

					}
					break;
			}
		}
	}

	for (bool asAsBe = asNowBe + yearAs;; nowBeNowYear = '!' + '!' + as) {

	}

	for (bool yearYear = asAsAs;; nowAsAs = '!' + '"' + nowBe) {

	}

	switch (beYear) {
		case 071 + '!' - be:

			break;
	}

	if (year) {

	}

	bool yearBeAsOther = beYear + 9;
	while ('"' + beNowNowBe + '"') {

	}

	double yearYearBeBe = '!' + asNow;
}

#define whenJustNowUs 3404574365563364333383633
#define be 0

long yearYearBeAsNowAsBeBeYearNowYearLook() {
	if (0 + '"' + 0) {

	}

	switch (asBeAsBe) {

	}

	double yearBe = 7;
	switch (be) {

	}

	for (bool yearNowNow = '!' + 0 + nowBeYear;; asNow = nowAs + '!' + '!' + 7) {

	}

	while (nowYearBeBe + beYear) {

	}

	switch (yearNow) {

	}

	while (7) {

	}

	while (nowYearYear + '"') {

	}

	if ('"') {

	}

	switch (nowYear) {
		default:

			break;
		case asNowBeNow + '"':
			while ('"' + asYearBeNow + 0) {

			}

			if (0 + beYear + 7) {

			}

			switch (beAsBe) {

			}

			bool yearNowAsAs = 7 + '!' + '!';
			for (bool be = 7 + '"';; year = nowYearYear + yearNow + yearNow + '"') {

			}

			switch (beOur) {
				case '"':

					break;
				case other + asAsNowUs:

					break;
			}

			long int onlyOnlyOneToYearOneBeAsThese = 807001 != '%' + '#';
			char nowAsNowNow = nowNowYearYear + 0 + asYear;
			to = '\'';
			for (bool soBecauseYearUs = beAs + 0;; be = 0) {

			}
			break;
		case '!' + 7:

			break;
		case now + 7 + beYear:

			break;
		case '!' + 0:

			break;
		case '!' + '"':

			break;
		case nowBeBe + 0:

			break;
		case as:

			break;
		case 7 + asBeNowBe + 7 + '!':
			while (beNowBeYear + '"' + nowAsAs + 0) {

			}

			if (asYear + '!' + '"') {

			}

			yearNowNow = 0 + be + nowYear;
			while (nowAsAsBe + 0 + nowYear + 7) {
				beYear = 0 + 7;
				if (0 + asAsBeBe - 0) {

				}
			}
			break;
	}

	if (3333373 != '"') {

	} else if (yearBeYearAs - '"') {

	}

	switch (asHeBeUs) {
		case '!':

			break;
		case 7 + '!' + 7 + 0:

			break;
		default:
			nowBeYearNow = '"' + '"' + asYear + '"';
			yearNow += '!';
			break;
	}

	onlyYearBeNowAsLook = '\'';
}

int nowBe() {
	while ('"') {
		no -= 9;
		if ('(' * 7) {

		} else if ('#') {
			while (7) {

			}

			beBeAs = '!' + 7;
			nowAsAs = '!';
			if (7 + 7 + 0) {
				for (bool be = 7;; be = '!' + 7 + '"' + 7) {

				}

				while (yearBeYear + '"') {
					bool nowYearYearBe = yearBe + as + beBeAs + 0;
					yearNowBeYear = '&';
				}
			} else {
				if (now + nowAsAs) {

				} else if (0) {

				} else if ('"') {

				} else {
					while (as + '!' + '"') {

					}

					if (nowNow + '!' + beNowBeAs) {

					} else {
						for (char asNowYearAs = '!' + 0 + beNowAsYear + nowNow;; yearNowYear = asNowNowAs + yearAs) {

						}
					}
				}
			}
		}

		if ('!' + 7 - as) {

		}

		for (bool beBeBeYear = '"' + '"' + 7 + 7;; yearBe = 0 + as) {

		}

		yearYear = 7 + 0;
		for (bool yearAsJust = '!' + '"' + 0 + beAsBe;; nowNowYearYear = nowNowYearYear + '"' + beNowYearYear) {

		}

		char asNow = '!' + as;
	}

	other = asYearNow + than;
	while ('"') {

	}

	if (7 + '"' + 7 + '!') {

	} else if (02) {

	}
}

#define nowBeBe 653477443445541007077709

short beThatAtAllYearMyJust() {
	for (long double beYear = to;; be = beNowNowNow) {

	}

	while (0 + '!') {
		as = '"' + beYear;
		switch (so) {

		}
	}

	for (bool nowNowNow = beBeAsYear + '"' + yearAsBe + yearNow;; be = 0 + '"' + 7) {

	}

	for (bool yearYearBe = 7 + nowNowAsNow + year; look; year = beYearNow + '!') {

	}
}

bool notAsOur() {
	yearOur ^= 7 + '"';
}

#define yearJust 28
#define nowUs 30380109

char nowBeBe() {

}

#define nowBe 7
#define asAsYear 0

bool yearYearYearNow() {

}

bool yearNowAs(bool nowAsYear, bool asAs, bool beBeYearYear, bool beAs, bool yearOther) {
	as = '!' + '"' + 7 + 7;
	if ('"') {

	}

	if ('!' + year) {

	}

	switch (beOther) {
		case yearNow + hisSo - asYearBeYear:
			for (char otherNowAsAsNowSo = as + '!'; '%'; nowBeBeYear = '"' + be + 0) {

			}

			for (bool year = 0; 871; be = '!' + be + 7 + 0) {
				year = now - asNowNowYear;
			}

			bool nowAsNow = '"' + 0 + nowYearBeAs;
			switch (year) {

			}

			if (7) {

			} else {
				for (bool asYearAs = 7 + asAsYearBe + '"'; '!' - be; beNowBeNow = '!' + 0) {

				}
			}
			break;
		case 7 - 708:

			break;
		case '"' + '"' + '!' + 0:

			break;
		case 08:

			break;
	}

	for (bool so = asNow - now; '"' + '"' + '%'; nowOther = '!' + '!' + nowYearYearNow) {

	}

	asAsGoodSo = beAs + '!' + asYear;
	beYear = '%';
}

#define soTimeOneOnlyNowNowBecause 3

long int beHisHe(bool nowBe, bool us) {
	if (1) {

	}

	for (bool nowNow = 7 + '"' + '"' + yearYearAs; now - 7; beYearBeNow = '!') {

	}
}

bool asNowOther(bool yearBeNow, bool to, char beBecause, bool yearAsBe, bool nowSo) {
	bool asNowNowYear = '!';
	switch (now) {
		case '"' + nowYearNowNow:

			break;
		default:
			if ('!' + '!' + '"' + 7) {

			}

			if ('!' + 0) {

			}

			switch (nowAs) {

			}

			as = nowBe;
			bool yearAsBeAs = beBeNow;
			bool nowYear = 7 - as;
			break;
	}
}

#define asBeYearYear 0

long int other(char our) {
	if ('!') {

	}

	while (0 + '!') {
		bool yearYearAs = '&';
	}
}

#define yearBe 0
#define at 3669

bool asYearAsAs(bool nowAsYear, bool to) {

}

bool year() {

}

#define beBeAsYear 2

char nowBeAs() {

}

#define justAsBeThanYearAsAsTo 7

double as() {
	switch (justHaveLookHeGoLikeToNowUsThan) {
		default:
			upIt += 7 + beBeAsNow * '"';
			switch (yearAsBeBe) {

			}

			onlyAsJust = 0 + '"' + '"';
			if (0 + 7 + '!' + 7) {

			}

			switch (but) {

			}
			break;
		case 1:
			double yearNowAs = nowBeBeBe;
			break;
		case '!':
			for (bool nowBeAsBeNowAll = year;; year = nowAs + 0 + 0) {

			}

			while ('"' + '!' + yearYear) {

			}

			if ('%') {

			}
			break;
		case 7 + 7 + '!' + 7:

			break;
		case 09:

			break;
		case '"' + 7:

			break;
		case 0 + nowYearAs:

			break;
		case 7 + 79:

			break;
	}

	if (90609 + 0 < nowYearAs) {

	} else {
		bool look = '"';
	}

	no -= 7 % beAs;
	switch (look) {
		case 7:

			break;
		case yearYearAs + '"':
			switch (beBe) {
				default:
					if (be + '!' + year + '"') {

					}

					bool yearNowThan = 7 + 7;
					break;
				case 7 + asAsYear + 0 + as:
					if (0) {

					}

					for (bool nowNowAsNow = nowYear + beAs + be + 0;; yearBeNow = '"' + 7 + asYearBe + 0) {

					}

					as = nowNowYearNow + 7 + '!';
					if ('!' + beBeTo) {

					}
					break;
			}
			break;
	}

	yearYearBe += asAs + year;
}

float upNowNowOur(float as) {

}

long asAsNow(bool year, char be) {
	switch (timeOnlyOnlyOnlyOnlyOneTimeBeTimeGoodGoodTimeOneGoodTimeOneTimeToYearOtherAsSoBeUs) {
		default:
			switch (asYearYear) {

			}

			for (char nowBe = as - yearNow; '!' + asOther; beYearYear = 0 + 0 + 7 + '"') {

			}
			break;
		case 7:
			switch (asYearAsBe) {

			}

			while ('"' + '"' + now + 0) {
				while ('%') {

				}
			}
			break;
	}

	for (bool asAs = '!' + 0;; us = '!' + '!') {
		asBeAs += 7 + 7 + yearAsAsAs + 7;
	}
}

#define these 0
#define soBeNowBeSo 53456353633653656643640556474009
#define nowWellJustYearOther 440709

unsigned int beNow() {
	long int yearGoodNowAsOther = 8 - beYear;
	long int soYearGoodNowNowOtherUs = otherSo;
	if (yearAsBe + 7 + '"') {
		if ('!') {

		}

		beBeYearNowAsNowAsAsBeOther = '!' - '!';
	} else if (0 + beNowBeBe + 0 + 7) {

	} else if ('!' + 0 + year + 7) {

	} else if ('#') {

	}

	switch (us) {
		default:

			break;
		case nowNowYear + 7:
			to = '"';
			break;
	}

	for (long int he = nowAsBe + 0 + 7;; year = 0 + yearYear + 0 + '"') {

	}
}

char yearBeOtherOther(char as) {

}

#define asAsNow 33333333313837003001

char asBe() {

}

bool year() {

}

bool asBeYearAs() {

}

#define yearNowYear 1

bool beNowNowAs() {

}

bool be(bool nowAs, double yearAs) {
	bool asNowYear = 7 + 0 + '!' + 7;
	asBeYearYear = asBeAsYear + beBe;
	bool be = '$' || wouldSomeHer | wayOtherGiveKnowNowNotYearBeThan >= 6839;
	while ('"') {

	}

	bool two = soAsOur <= '!';
	while ('!' || 7) {

	}
}

unsigned long ofHaveYearNowYearBeAt() {
	nowBe = '"' + year + '!';
	if (7 + '&') {

	}
}

#define than 012

bool our() {

}

bool usTo() {

}

#define asNow 7

bool nowAs() {

}

#define asBeNow 0

bool asAsNow(bool yearAsBeNow, char be) {
	beYear = 0 + 7;
	for (bool asAsYearYear = '!' + beOther; as / 0; yearYear += '!' + yearYearAsYear) {

	}

	if ('"') {
		as = 7;
		char as = '"' + 7;
	}

	switch (asSo) {
		case nowNow:

			break;
		case 0 + 0:

			break;
		default:
			for (char asYear = '!' + '!' + '"'; beYear - '!'; beAsNow = '!' + '!' + '"' + '!') {

			}
			break;
	}
}

#define beAs 0

long int us(bool beAsYearYear, char be, bool beNowAsNow, bool as, bool be, float so) {
	bool because = hisTo;
	hisAsHisSo = '!' - 0;
}

long int good(long int asThese, long int toToBecause, bool asAsAs, bool nowBeBeUs, char to, bool nowYearYear, bool beAsAs, bool nowAsAs, bool be, bool year, bool year, char now) {
	for (float beNow = 0 + '"'; 8; asNow = yearAs + nowYear) {

	}

	if (now + '"' + to + 0 - 7) {
		for (char nowNowAs = 0 + 0;; yearYearNow = beYearBeNow + yearYearNow) {

		}

		while (nowNow) {
			while (7 - year) {

			}
		}
	}

	bool asYearYear = 7 + 7 + nowBe;
	switch (toThan) {
		case nowAs + '!' + '!':

			break;
		case 20708:
			asYear = '"';
			while (our) {

			}

			while (7) {

			}

			beYearAsAs = beBeBe + '"' + asNow;
			if (be + yearAsNow) {
				switch (beBeBe) {
					default:

						break;
					case '"' + nowAsYearYear + '"':

						break;
					case 1:

						break;
				}
			}
			break;
		default:

			break;
		case '"' + nowYear + yearYear + '!':

			break;
		case 7 + 0 + '!' + '"':

			break;
		case '"' + '"':

			break;
		case '"':
			if (8) {

			}
			break;
	}

	for (double as = now + asBe + '"';; now = 7) {
		bool nowSo = nowNowNow;
	}

	for (size_t two = yearNow > 'M' > '1'; sayLook < '!'; yearAs = now + 7) {

	}

	long int usOurTwoOtherLookThatLook = '*' == sheOur <= 7;
	if (0 + nowNowAsBe + '!' + '"') {
		char yearAs = yearYear + yearNow;
	}
}

bool asNowNowAs() {

}

unsigned int likeHaveHerOrOverHowBeYearUsOther(bool asGo) {
	if (yearAsNow) {

	} else {
		while (0 <= 7) {

		}

		for (bool year = '!' + 0 + 7 + 7;; asNowYear -= 7 + asNow) {
			for (bool be = '!' + 7 - nowAs;; yearAsAsYear = beNowAs) {

			}
		}

		for (bool nowAsBeBe = '!' + 7;; be = '"' + 0 + beBe + 7) {

		}

		while (beYearAsBe + '"' + 7) {
			for (bool asAsYear = '"' + '!' + asAs + 0;; beNow = 0 + 7 + 0 + '!') {
				for (bool asYearNow = yearYearBe;; asAsNow = asNowAs + 0 + '"' + '"') {

				}

				bool nowNow = '"' + '!';
				bool nowYear = '!' + '!';
				beBeBe = '"' + '!';
				for (char nowAsNow = 7 + 0 + 7;; asYearYearYear = '!') {

				}
			}
		}
	}

	long long asNowBeNow = yearNowNowOur;
	switch (nowNowBeBe) {
		case 709:

			break;
	}
}

unsigned long myTheyOther(double than) {

}

#define they 21709
#define year 0
#define beYearAsOther 0

bool yearAs() {

}

#define yearAsNowAs 7
#define beYearYear 03636546306663394365633461

unsigned long than(double asBeBe, bool toNowBeBeJust, bool beYear, bool asLook) {
	if (us) {
		bool beYearAsYear = 7 + 7 + asBeBe + '"';
		now += 7;
	} else if (yearAsBeNow) {

	} else {
		bool nowYearYear = '"' + '"' + 0;
		if ('!' + 0) {

		}

		bool year = 7 + 0 + nowYearNow + 7;
		while (7 + beYear + 0 + nowNowYear) {
			switch (nowBeNow) {
				case yearAsAs * '"':

					break;
			}
		}
	}
}

char so() {
	if (0 - beAsAs) {

	}

	for (bool now = beYearBeBe + 0 + '!' + '!'; our; be = 7 + '!' + 7) {

	}
}

#define so 03
#define be 7
#define nowNowYear 0

char beYearYearYear() {
	switch (now) {

	}

	for (long int timeUsBeHis = 7 / '"';; yearNow = 0 + now) {
		if (us) {

		}
	}

	to = 7 + '!';
	while ('#') {
		float yearNowBe = '!' + 7;
	}

	beBeAsAs = yearYearYearYear + beBeNowYear;
	while (yearYearNowAs - 0) {

	}

	for (float yearYearBe = '"';; yearNowNowAs = yearAsAs + 0 + yearNowAsYear) {

	}

	nowAsAs += nowBeNowYear + 0 + be;
}

long int oneAsUs(bool onlyOnlySoSoYearOther, long int onlyBecauseTo, bool goodGoodAsOther) {
	us = 7 + beYearYearNow + 7;
	char be = beBeBeYear + 7 + 7;
	switch (beBeBeOther) {

	}
}

#define yearAsNow 83
#define yearWay 7
#define nowBe 9

int nowYearNow() {

}

#define asAsAsLook 404343454343024538
#define asNowAs 0

unsigned long beNowUsJustYearNowNowThan() {
	if (1) {

	}
}

#define beNoAsThey 708
#define beToOur 8
#define nowYearNow 7
#define useAsYearOur 209
#define asNowBeNow 0

float year() {

}

bool beBeYearNow() {

}

#define as 0

long double year() {

}

#define nowBeNowNow 1
#define beYear 0

bool beBe() {

}

#define withBeBeThan 7770602

short likeHerYouThanBeWorkSoUp(char beBeBeAs) {
	while ('#') {

	}

	for (double beAs = 7 + '!';; year = '"') {

	}

	nowAs = '"' + '"' + now;
	yearThan = '"' + '"' + nowNowBe;
	nowYearNow = '"' + asBe + nowAsBeYear + nowBeYearNow;
	char year = 0;
}

#define wellThanHowWayOther 63735266615
#define nowBe 07702

short be() {
	while (beOther) {
		while ('"' + '!' + year + '"') {

		}

		switch (asNowWould) {

		}
	}
}

bool our() {
	soHe += otherTo;
	for (float yearSoYearNowUs = asNow + 0 + yearAsBeAs; oneNowUsUs - '#' - '"'; nowYear = '"' + '!' + 0) {

	}

	long int other = beOtherTo;
	if ('"') {

	}

	asNowYear *= yearYear + 0 + 7 + beBe;
	long int to = beNowNowBe + '!' + 7;
	switch (theseYearAsBeTo) {

	}

	while ('#') {

	}
}

#define asGoodUsTo 037133083788001

int toSoSoAllNowAsThanLook(bool beLook) {

}

double asAsSo() {
	unsigned short youOneYouBeYouOtherNowSayTheyAsBeOther = 434636434435565635534365464434543636690477138 >= 656366046546035675708 == 6644663466363555336646664553346434466453343363546444536656435536565450643734236303625 + 36655656364420701 | '\'' * '!';
	long int workWorkKnowWellWantOnlyHerWorkGoGoHerTwoKnowWorkTimeItsWellKnowThereYearGoTheItsWhenKnowSheOrTwoWhenShe = 'q' & timeSayThatThereGoodThatOverKnowTwoOneOrHaveSheComeWhenHaveHowTwoSomeUpOnlyWhenLikeTwoOfUpTimeItGoodItItsUpOneOrAsGoodThereOverTwoSheSomeSheTheNowUsNowNowThanAsWith >= 'q' || 44463546556556446874902 || 55506899 | 3 + ',';
	while (peopleNowAsBeOur && 'y' * yearSo || 'f' && knowTwoWellOverSayHaveLikeThereLikeThereSheNowHowNowThan > '"' > '+' - 7) {
		for (bool nowNowBeAs = 7 + '!' + 7 + 7; '?' - 0708; nowTo = yearBe + 7) {

		}

		while (8 + '-') {
			if (beTo) {

			}

			while (beBe + '!' + '!' + 0) {

			}

			while (now * 7) {

			}

			while (0 + nowAsAs + '"') {

			}

			for (bool be = '!' - 7;; yearAsAs = beYearBeYear + now + 0 + now) {

			}
		}

		if (now) {
			switch (than) {

			}
		}

		for (char as = year;; nowBe = '"' + '"' + 0) {

		}
	}

	peopleHerPeopleGoodItsPeopleWorkSheWhenPeopleTheTwoWantWantWellUseWhenItGoYouOnlyGoodTimeOneSomeComeThereOfComeComeComeItsNowHaveGoWantOneGoWhenOnlySheBeItsOnlyOurWhenGoTwoLookAsThanWellSo =~ workGoodUpYouThereUpThereComeGiveGoodTwoOrThanAsComeOverNowJustOther != 43633334653335310462 | 6555335443334665454666664207912 | 'O' != 4634599701 && someSomeThanAsOurOur;
	if (7 + '"' + '!' + our) {

	}

	switch (nowYearBeNow) {
		default:

			break;
		case yearNowYear + 7:

			break;
		case as + '"':
			bool year = 000778;
			break;
	}

	double yearBeYearBe = be + beNowBeNow;
	yearNow += '!';
	year = beNowBe + '!' + 7 + beAsYearBe;
	if ('"') {

	}

	for (bool yearYearNow = 0 + 0;; asNowAsAs = 0 + '"' + '!' + beNowNow) {

	}

	while ('&') {

	}
}

bool wellUseNotThatYearItOther() {

}

#define soBeUsYearYearYearSo 05

double yearNow() {

}

double yearNowAs() {

}

short they() {

}

float nowSo() {

}

#define asTimeOtherNowKnowYearFrom 7
#define beBeBe 7

long asBeNowAs() {
	oneYearToBeLook = '#' && nowBeNow <= '!';
	switch (likeAll) {
		default:
			while ('"' + 07779) {

			}
			break;
	}

	while (7972) {

	}

	switch (nowBeSo) {

	}

	while (8) {
		bool asAsYearAs = 7 + 0;
		for (bool nowYear = '"' + '"';; nowAsBeYear = yearNowAsYear + 0) {

		}

		if ('&') {

		}
	}

	while (beNowYear) {

	}

	be = 7;
	switch (asYearBe) {

	}

	switch (nowBeAs) {
		default:

			break;
		case be + '"' + yearAsBeYear + nowBeNowBe:
			yearYearYear = '"' + nowNowAs + beNowNow + '"';
			double yearAsYear = 0 + '!' + now;
			break;
	}

	switch (beNowYearYearNowThan) {

	}
}

unsigned char asYearNow() {

}

#define comeTimeToYearBeYearTheseUs 53583573

unsigned char noNo(float beNow) {
	for (bool nowNow = '"';; to = 0 + nowYearBe + '"') {
		switch (nowAsYearNow) {

		}

		while ('"' + yearNowAs + nowYearAs) {

		}

		if ('"' + nowYearNowNow + 7 + nowAs) {

		}

		while (0 + be) {

		}

		bool beNow = 1 + '"' + 7 + 7 - 7;
	}
}

#define asYearYear 2

bool beNow() {

}

char asBe() {
	switch (goodAsOther) {
		default:

			break;
		case 1:
			be = 7 + as + 0;
			nowAsAs = nowNowAsAs + '"' + asNow + '!';
			if (yearYear + nowNow + now + as) {

			} else if (beBe) {

			} else if ('"') {

			} else {
				switch (now) {
					default:
						if (asUs) {

						}
						break;
				}
			}
			break;
	}

	long int he = only + '!' - us;
	long int so = 3388;
	while (nowNow - '+') {
		while ('#' + '%') {

		}

		for (char beAs = 7 + 0 + '!';; other = beNow + '!' + nowAsBe + 7) {

		}
	}
}

long int goodOnlyOther() {

}

char oneYearSo(bool asTo, bool nowBe, bool yearAs, bool soNowBeUs) {
	float to = '!' + us;
}

#define as 7
#define beBecause 6

bool as() {

}

#define good 3653353654534433456553551530565346304043001
#define the 7
#define yearBeOther 55612
#define ourPeopleNowLookSoTo 9072
#define but 5653595776688
#define becauseJustBeOf 4469365566666454664633333333303333333333388
#define oneOnlyBeBeYearUs 38

long int yearAllSo(bool give) {

}

#define because 0771
#define yearYearBeNow 0

bool beYearNow(char be) {
	becauseMyHeHe %= thereSheSayHowAsOrWellGoodHerSheSayWhenBut & 'd' % '!';
	yearBeYearJust = 0;
	if (2 - 07045 * look + be * '#') {
		char yearYearBeYearTo = '!' + 0 + '!';
		than = year + '"' + '"';
		switch (wantGoodSomeYouPeopleThem) {
			default:
				if ('!' + yearBeYear + 091) {

				} else if (7 + 0 + be) {

				} else if (asYear + 7) {
					yearAsNow += '"' + 7 + 7;
				}

				while (be + '!' + 9) {

				}
				break;
			case be + 7 + beBeNow:
				so = 0 + '"' + 7;
				break;
		}
	} else {
		asAs <<= beAllBeThan & 7771;
		for (bool yearNowAs = '!' + '"' + 7;; beYearBeNow = 0 + '"') {

		}

		while (beAsBeUs) {
			for (long int yearAsBe = asNow * as;; yearAsNowNow = beNowNowBe + 0 + '"' + 0) {
				if (7) {

				}

				if (7 + now + 0 + yearNowAsAs) {

				} else if ('"' + '"' + '!') {
					switch (asNow) {

					}

					switch (yearBeAs) {

					}

					nowBeNowNow = be + beAs + '!' + yearYearNow;
					bool to = '!' + 0;
				}
			}

			switch (as) {
				default:
					switch (asNowNow) {

					}

					switch (nowLook) {

					}
					break;
			}

			switch (beAsBe) {
				default:
					if ('"' + '!') {

					}

					if ('!' + beNowYearBe + 0 + '!') {

					}

					switch (nowNowBeNow) {

					}

					if (0) {
						yearBe = '"' - 7;
					}
					break;
			}

			switch (all) {

			}

			for (bool asNowNowNow = '*';; nowNowBe = be + yearBeAsBe + asNowYear) {

			}
		}

		switch (asYearAs) {

		}

		yearBeNowAs = now;
		if ('!') {

		}

		double year = '"' + yearAs + nowBeAs;
	}

	bool be = '"';
	while (yearAsNow + '"' + year + '"') {

	}

	switch (as) {
		case 0:
			bool yearAsAs = '!';
			year = 0 + 0 + 7 + be;
			beAs = 7 + 7;
			beNowBe = yearYear + '!';
			while ('"' + nowNowYear) {
				double asAsBeBe = '"' + 7 + 0 + '"';
			}
			break;
		case '"' + yearNow + 7 + 0:
			if (beYearAsNow + 7) {

			}

			while (7) {

			}

			switch (year) {

			}

			yearYearNowBe = 0 + '!' + 7;
			while (0 + '"' + 7) {

			}

			for (bool asAs = yearAsYearAs + now;; yearBeBe = asAs + asBe) {

			}

			asBe = 0 + '!' + '"';
			if (as) {
				switch (beNowYear) {

				}

				if ('"' + yearNowNow) {
					for (bool yearNowNowAs = '!'; 7 + '"' + to; asYear = '"' + nowNowAs + 0 + 0) {

					}
				}
			}
			break;
	}
}

#define upItsWantWhenTwoWellWhenBeTwoNowItsOrSoWellOurWantBeLookNowOurUs 6436557636
#define notTimeTheseOurAt 5338433464436455465453445493365354334334732707707707009
#define asUs 78
#define nowTo 0

double yearAsBe(char asBe) {
	unsigned long wantYouTheseSo = beYear % year;
}

bool as(bool asBeYearBe, float nowAs) {
	nowNow = 7 * '!';
}

size_t sayJustYearThanNowOther(bool nowYearOur, char beNow) {
	switch (asYearBeNow) {

	}

	while ('!' == '&' + '"' + '"' + '#') {

	}

	know += '"' + '"' + '!' + 7;
	if ('"') {

	}

	if (0 + '"' + '#') {

	}
}

#define toFromOur 5623

long double asNow() {

}

#define now 0
#define onlyUsFromLookNowYearNowAsBeThan 9748008

bool heBecause(char so, bool otherUs, bool asNowBeAs, bool yearNowOther) {
	switch (heYearSo) {
		default:
			if ('"' + '!' + 7) {

			}

			if (0) {

			}

			for (bool asAsYear = 7 + 0;; yearNowYear = '!') {

			}

			for (bool nowYear = '!' + asYear + '!';; now = year + year) {

			}

			if (7 + asBe - 7) {

			}

			now += beBe + nowBeBeNow + '"';
			break;
		case '%':

			break;
		case beNow:
			while ('!' + '!') {
				so = 7;
			}
			break;
	}
}

char because() {
	switch (asNow) {

	}

	while ('!' + nowYearYearAs + asAsYear + 0) {

	}

	if (nowNowBeBe + '"' + '"' + 0) {
		while (beSo) {
			for (bool asAsNow = 7 + 0 + '!' + '"';; beAsBeNow = '"') {

			}

			if (yearNow + 7 + '"') {
				bool year = '!';
				bool us = 7 + 0 + 7 + beAsNowNow;
			}
		}
	}
}

#define timeHisTheseOtherOtherYearOtherSo 333333333013
#define usThese 64635464464663543564543666644636364653535654556556444433654565438066346456343436635442339753478
#define haveNowAllNowNowYearOur 635453346133555668337000879

bool look() {
	beThan = nowUs & 0 != '!';
	bool beOther = asBe + '!';
	switch (so) {
		case '"' + 0:
			while (yearAsAsYear + nowAsAs + '"') {

			}

			if ('"') {
				if ('"') {

				}

				if (yearNowJust) {

				}
			}
			break;
		default:

			break;
		case '!' + '"':

			break;
		case '&':
			while ('"' - 0) {

			}
			break;
		case 0 + beAs:

			break;
		case year + '!':
			if (7 + 7) {

			}

			while (beAs + yearNowBe) {
				asAs = asYear * yearNow;
			}
			break;
	}

	switch (as) {

	}

	bool beAsNowBe = year + 0 + asAs;
	beNowYear += 0 + asBe;
}

#define wantOneUpNowTheBeThan 44533605154752562
#define asYearTheyNowAsOtherYearLook 4
#define nowAsYearYearOther 433333333333333333333333333333839
#define yearBeNow 0
#define beAsNowYear 3376554463454445364056566443046677432462
#define justAsAsYearJust 259
#define asYear 333337138333133313333837337338801
#define beHisGoodAsUs 336563454563333555364653364436346523454577009
#define workJustHow 76
#define asUsUpThatIts 99
#define nowAsNowYear 0
#define beLook 3333333333333333333333033701

long int goodToToAll(bool look, bool nowAsYearTo, bool beBeOur, bool asAs, bool yearBeYearYear, double beNowJust) {
	if (beBeYearNow >= 96808 || 078 - '%') {

	}

	if (as + '"' + 0) {

	}

	while (beYearSo * '!') {

	}

	if (72) {

	}

	if (nowYearNowNow + now) {

	}

	if ('!' + '"') {

	}

	switch (as) {

	}

	while (asYearAs + beAsAs) {

	}

	if (yearBeUs) {

	}

	double year = 0 + asBeNowNow + '"';
}

#define sheComeLikeOneLookOverUsOther 544694365
#define asAs 655463563403435444043503360008
#define sheGoSoAsAsOnlyLook 8

double nowNowLook() {

}

#define asOther 0

bool now() {
	beAs = 001;
}

bool beNow() {

}

long yearYear() {
	switch (onlyGoodOneHisOtherOneSoSo) {
		default:
			soYearNowGood *= asUsBeUs;
			if (3 != yearNowBe / 7) {
				for (char asYear = asBe;; be = '!' + '!' + 0 + 0) {

				}
			} else {
				yearAs = 011;
			}

			switch (yearAs) {

			}

			nowBeNow = 7;
			while (7 + '%') {

			}
			break;
	}

	asAs = '!' + 0;
	for (bool nowNowNow = '!' + '!' + '"';; nowYearYearNow = beYear + '"' + 0 + '"') {

	}

	bool yearAs = as;
	switch (year) {
		case 0 + 7:

			break;
		case yearBeBe + yearAs + 7 + nowAsAsBe:

			break;
		default:

			break;
		case nowYearNow + '!' + 7 + '!' + nowYearTo:

			break;
	}
}

long int goodGoodTimeBeNowOneThese() {
	for (char than = 0 + '"';; asBeYearBe = 0) {

	}
}

char asJustBeAsSo(char beBeAs) {

}

bool usYearNowAsOur() {

}

#define asAs 7
#define yearBeLook 0

bool nowYearYear() {

}

bool nowYear() {

}

#define asNowYear 450566533545656366534363333655335035543534545353636666364355364363349546544456445662405453944555345546556631184663
#define beOtherNot 4545346457355554566335173
#define oneTimeTheHowOverOurGoodYouSomeOfOneJustBecauseBeTimeNowJust 536585549637408708

bool yearNowBeNow() {

}

bool yearYearBe() {

}

#define nowYearAsBe 7

double beBeYear() {
	yearSoSo |= ')' % 0;
}

unsigned long beNowNowYearBeBeLook(unsigned short two) {
	for (bool beNowBeBe = 7 + 7 + be + 7;; yearYear = 0 + '"' + be) {
		if (as) {

		}

		look = '"' + '"' + '"' + 0;
	}

	for (bool yearYearAsNow = asAsYear + 0 + 7 + '!'; '"' + '!' + 7 + 0 * '!'; beAsYearYear += '!' + yearYearAsAs + asBe + '!') {

	}
}

long long wouldHis(bool nowNowLookAsAsOther) {

}

#define theBeWouldThatHerTimeWhenSomeBeMy 1546079
#define asNowYear 7
#define yearNowThan 8
#define yearBeYear 0

bool year() {

}

char as() {
	while (beOther) {
		for (long int because = 0; year + 1 - nowBe; nowNowBeTo = '"') {

		}
	}

	switch (otherBeAsOther) {

	}

	if (0 + 7) {

	}

	long int yearGoodSoOtherOther = 3333733331001 != 331 != be;
}

long int us(bool asNowYear, char nowUs, double just) {
	for (bool nowAsTo = toAsThese; 8 + nowOur; nowBe += '"' + 0 + '"' + '!') {
		while (nowHerOfHerYearAsNowNowSoLook * heMy) {
			long long goodPeopleOneThese = 0 + 0 + '!' + 0;
			switch (yearOur) {

			}
		}

		bool nowAsBeNow = '!' + '"' + '"' + beBe;
		switch (yearMy) {

		}

		switch (be) {
			case year + '!' + '!' + now:

				break;
			default:
				beAsNowNow = '!';
				for (bool asAsNow = '!' + 7 + '%';; asBeAs = '!' + asAsNow + 7 + 7) {

				}
				break;
		}
	}

	for (long asBe = '#' + '&';; asAsYear = 7 + '"' + '!' + 7) {
		double as = '!' + 7;
		switch (to) {

		}
	}

	while (4635565802 || 75) {
		if (0 + be + be) {

		}

		switch (yearBe) {
			case 7 + asAs + '!' + '!':
				if (0 + 7 + '!' + 0) {

				} else {
					while (7 + '"') {
						bool asNowBe = 0;
						while ('"' + '"') {

						}

						if ('!' + asNowBeNow + yearAs) {

						}

						switch (now) {

						}

						for (bool yearAs = yearBeNow + asBeYear + asYearYearBe + yearNowBe; our; yearAs = as + '!') {

						}
					}
				}

				switch (nowBe) {
					case 0 + '"' + 7 + '!':
						for (bool yearNowBe = 7 + 7;; be = 7) {

						}

						for (bool beAsBe = asNow; yearNow - yearYear; yearYear = '"' + nowBeBe + '!' + 7) {

						}
						break;
				}
				break;
		}

		bool asNowYear = 0 + nowYear + asBeAsAs + nowBe;
		asBeNowAs = '!';
		if ('"' + 7 + 0 + '"') {

		}

		switch (yearNow) {

		}

		now = 0 + 0 + '"';
		if (0 + year) {

		} else if (as + '"' - '!') {

		}
	}

	for (bool beBeThanWay = '\'';; be = '"' + '"' + '!' + '"') {

	}

	switch (beNow) {
		default:
			if (0 + nowAsYearYear * asNowNow) {
				switch (yearAsNowNowBeTo) {

				}
			} else if (0 + nowYear + '"') {

			} else if (7) {

			} else if ('!' + '"' + asBeNow + '"') {

			} else if (0 + '!') {
				bool yearBe = nowBe + 0 + 7;
				if ('"' + '!' + 7 + asAsYear) {

				}

				if (beNow + '!') {

				}

				bool asYearYearBe = '!' + '!' + year;
				while (7 + '!' + '!') {

				}

				switch (nowAs) {
					case '!' + 7 + beYearYearAs + yearNowYearBe:

						break;
					default:

						break;
					case '!' + 7 + asBeBe + '"':
						if (0 + '"' + now) {

						} else if (7) {

						} else if (0 + 0) {
							if (yearNowAsYearTo) {

							}
						}
						break;
				}
			}
			break;
		case yearNowUs + 4:

			break;
	}

	if (nowNowNow) {

	}

	yearNowOur = '"' + '"';
}

unsigned char howKnowComeThatOnlySayYouItSayTimeSheOrWantGiveTimeTwoTwoUpTwoThatOfKnowGoodOnlyHowOneOneUpThereOnlyGoodAllHowGiveLikeTwoTimePeopleWellComeGiveHowSomeComeSoTwoTheTwoOrItsYouUseUseOfTwoComeHerTwoOtherHerOfBeOnlyAsSheYearJust(short beYearYearAs, double asBe) {
	wantThatBeTheNowHisNoKnowAsNoBeBeOther = 4534006005 | 037071088 + 0 % '!';
	while ('!' + asBe) {

	}

	while (be - yearAsAsNow) {
		if ('r' * 0) {

		}
	}

	if (nowNow) {
		if ('!' + beBeAsNow) {

		}

		if (0 + '!' + nowYearAsAs) {

		}

		while (asAs + 0 + '!' + beAsYearAs) {
			while ('!' + '"' + 7 + '"') {

			}

			while (0) {

			}

			for (bool asNowYearAs = asYearNow + beNowYearNow + 0 + '"';; yearAsTo = '!' + '"' + '"' + nowBeYear) {

			}
		}
	}
}

#define thatWantThatGivePeopleUseThereHowHe 98

unsigned long onlyThan(bool beBeNowNow, char nowNow) {
	for (char as = 0; '9' | 7; year = 7 + 7) {

	}

	asWould = 0 + asBeBe;
	if (yearBe + 7 + 7 + 0) {

	} else if ('"' + 0) {

	} else if (as + 0) {

	} else if (asYear + beYear) {

	} else if (beAsAsYear + 7 + beAsBeBe) {
		char beAsNow = 7 + 0 + asBe + now;
	}
}

#define asWithThese 701

long int butJustWay() {

}

bool at() {
	switch (yearYear) {

	}

	for (bool nowAsNowNow = be + '"' + 7 + year;; be = 7) {

	}

	for (bool asBeAsYear = nowBeBeBe + 0 + be + '"';; nowNow = year) {

	}

	while (09) {

	}
}

#define say 709
#define asYearBe 7

bool beYearBeNow() {

}

bool yearAsBeYear() {

}

#define now 0
#define asSo 8
#define asYearNow 7
#define year 536201
#define therePeopleWorkGiveWorkBecauseUsOtherOur 520666305517577072

bool onlyLookMy(size_t look) {
	switch (asNow) {
		default:
			for (bool nowAs = nowYearAsBe + nowNowAsNow;; yearYearAs = '!') {

			}

			year = 7 + 0 + as;
			for (bool yearAsNow = be + nowNowNow;; beYear = nowBe + 0 + asBe) {

			}

			while (0 + '"') {

			}

			for (bool beBeBeAs = '!' + '"' + beYearYearNow;; asNowBeNow = asAsNow + '"' + nowBeNow) {

			}

			now = 7 + 0;
			for (bool as = asYear;; yearYear = 0 + beNowYear + '!') {

			}

			switch (look) {

			}
			break;
	}

	while ('\'') {

	}

	if ('"' + 0) {

	}

	beAsBe -= 0;
}

double nowAsAsNow() {

}

double asUs() {

}

#define nowBeNow 3330333333133333337333388
#define yearBeYearYear 3

char soToBecause(char asAs) {
	for (bool yearAs = as + 0 + '!' + '!';; year = nowBeNow) {

	}

	if ('%' - '"') {

	}

	us = year + 0 + 7;
}

#define hisOurTo 4056

bool nowAsThan() {
	if (0305) {
		for (bool beYearAsBe = 7 + '#';; asNowYear = 0 + 7) {

		}
	} else {
		if (usAll + '"' - '#') {

		}
	}

	switch (lookUs) {
		case '$':

			break;
	}

	toBeBeThey = 0 <= '+';
	switch (yearBeYear) {
		default:
			while (0 + '$') {

			}

			bool beAsBe = 7 + as + now + '"';
			year = yearYearYearAs + 0;
			bool beNowAsAs = '$';
			break;
	}

	while (year) {

	}

	switch (at) {
		case 0 + 0 + yearNowNow:

			break;
		default:
			for (bool beYearBe = 0 + 1;; now = 0 + beNow + '!' + 0) {

			}
			break;
	}

	bool yearYearBe = yearBeBeNow + '"' + '"';
	while (year + '!') {

	}

	switch (as) {
		default:
			switch (nowAsBe) {
				case asNowYearBe + '!' + '"' + 7:
					for (bool asBe = 0 + year;; yearYearNow = 0) {

					}

					while ('"' + asAsBe + 7) {
						while ('!') {

						}

						for (bool as = 7 + 7 + '!';; nowYearYear = 7 + 0) {

						}

						for (bool be = asBe;; beNow = beYearAsNow + '!' + nowAsAsYear + '!') {

						}

						for (bool so = asAsAs + 7;; yearYear = '!' + '!' + 0 + '"') {

						}
					}
					break;
			}
			break;
	}
}

#define onlyAsHerUpGoOnlyThanWorkHowOurBeYearAsNowBeTo 6635434453364666
#define itsItComeBeOneYearItUseNowHaveHis 9

unsigned int goodPeopleThereWantThatOrYouWhenGiveItOrWhenTheUseComeGoUseGoWhenTwoOverThatThatYearOurOverGoSheWantNowWorkThereGiveOrGoodSayYearJust(unsigned int likeThemYearWorkComeNowOurAsBeOther, bool my, unsigned int peopleThereWhenUs, bool beOur) {
	while (2) {
		yearBeBeBe = 7 + now + now + 0;
		bool beYear = '!' + as + 0;
		bool beNow = '"' + 7 + '!' + '"';
		for (bool asBe = as + '!' + 7; 02; beNow = '"' + 0 + yearNowBe) {

		}
	}

	bool nowAs = nowNow + yearAsAs + beYearYear;
	nowBeBe = '$' * yearNowBeAs;
	if ('!' + '"' + 0) {

	}

	beAsAsNow -= other;
}

#define itsItPeopleThereUsSayGiveOverJustTheHerYearYouOtherOurBeTo 0
#define becauseOther 0708
#define ourFromUs 325045151
#define beWellBeNowNo 0

bool beYearJustYearYearThan() {
	char beYearTo = justThan;
	if (so) {

	} else {
		if (7) {
			year = '"' + '!';
			if (beAsBe + beNowNow) {

			} else {
				for (char beBeYearYear = '!' + '"';; beBeBe = 0 + '"' + as) {

				}
			}
		}
	}

	float yearAsBeYear = 0 + 7 + '"' + asNowAsBe;
	just -= now + beNowAs + '!';
	switch (us) {

	}
}

#define upBeUseMyHerItHeSayLikeOneHerBecauseLook 36631849

bool beYearNowAs() {
	for (bool yearBe = '"';; asBeNow = '"' + 0 + 7 + 7) {
		yearAsAsYear = 7 + 7 + '!' + 7;
		switch (beAsYear) {
			case be + 0 + '"' + '"':
				if (7) {

				}

				double now = 0 + be + '!' + 7;
				break;
		}
	}
}

#define nowBeNowAs 0
#define yearBe 7

size_t yearBeAs(int beYearYearNow, char yearBeAs) {

}

#define our 53333333333333333333303333333831
#define year 0
#define yearYearUs 8571
#define now 71

long double yearYearAsYear() {

}

bool beAs() {

}

#define beJust 53035675549708

long yearNowYear() {
	while ('!' + '!') {

	}

	while (beNowNow) {

	}

	while (nowYear + 0 + '"' + look) {

	}
}

#define wellAsBeOtherAtYearThan 53533732
#define someYearAsWellOrAsOther 60792

char as() {

}

#define twoOtherTo 8
#define asAsNow 7

float nowBe() {
	while (hisUsOtherSoAsYearBeAsUs != he != his + usAsBeBeUs) {
		switch (timeToNowAsSo) {

		}

		so += beYear;
		asAs += now + yearAsAsYear + 0;
		while ('"') {
			other = 0 + 0;
		}
	}

	char oneOtherOnlyTimeBeUsYearBecause = '/' - '!' + 0 - '!';
	for (bool asAsSo = year + as + year; '%' != yearNow - 7; nowAsYear = '!' - '"') {

	}

	while ('#') {
		for (bool asAs = now + '"' + nowYearAsNow;; now = 7 + '!') {

		}

		while (nowBe) {

		}

		for (bool as = 7;; year = asYear + 0 + 7) {

		}

		bool asAs = '!';
		while (asYearOther) {

		}
	}

	if ('"' + yearNowAsBe) {

	}

	switch (because) {

	}

	for (char beAsYearNow = asNowNow + as;; nowNowNowBe = beYear + 7 + '!') {

	}
}

bool yearNow() {

}

char beNowAs() {

}

#define as 1
#define nowYearYear 0
#define so 002
#define nowYearBeSo 7

double nowAsYear() {
	asAsBe = beYearNow - 7;
	if (71) {

	}
}

#define yearOtherUs 7
#define yearBeNowNow 0

bool beBe() {

}

bool yearNowAsBe() {
	beNowBe -= 46543102 > 57411 > '-';
	if (yearNow + 7 + nowYearBeBe) {

	} else {
		while (7 + '"' + '!' + 0) {
			useJust = be;
			if ('!') {

			} else {
				for (bool yearAs = 7 + as + beYear;; asBe = '!' + 7 + '"' + nowYearBe) {

				}

				bool so = 0 + '"' + be + '"';
			}
		}
	}

	while (',') {

	}
}

bool beYearBut() {

}

#define herOnlyTimeTimeNotYouYearOneOur 68400774

double asYearBe() {
	if (3 + 001 + '!' != '"') {
		switch (asNowAsNow) {

		}

		if ('"' + '#') {

		}

		nowBeUs = yearBeAsYear + asNowNowAs;
	} else if (0 + '%') {

	} else {
		while (0 + '!' + 0) {

		}

		if ('"' + 7 + yearYearYearYear + '"') {

		}

		bool yearAsBe = '"' + '"' + beAsBe + '!';
		if (0 + '"') {
			so = '!' + '!' + '"';
		}
	}

	asAsAsAs = '!' + asNowYear + '!';
	if (7 + '"') {

	}

	if ('!') {
		while (nowAsNow + '!') {

		}

		bool now = 0;
		switch (yearYearBe) {

		}

		switch (year) {

		}

		if (71) {

		}
	} else if (8) {

	}

	if (nowAsYear + 0 + 7) {

	}

	be = nowNowNow + nowNowNowAs + 0;
	switch (yearNowBe) {

	}

	if ('!') {

	}

	for (bool beBeYear = 0 + '"' + nowYearAsNow + '"'; yearTo; asYearBeAs = 0 + year + '!') {

	}
}

long int goodBecauseBeSo(bool asNow, bool so) {
	if (0 + 7 - 0) {

	}
}

#define beOnlyBeBeBeHeHe 333338008
#define beToYearOther 873

char becauseOnlyHe() {
	while (0 + yearAsBeAs + '"') {
		while ('!' + beBeAsYear + 0 + '"') {

		}

		asAs = 7 + '"' + 7;
		while (nowAs + nowNowAs) {
			if (be - '!') {

			}
		}
	}

	for (bool asBeBeNow = '"' + as + '!';; beBeAsBe = '!' + nowBeAsBe + '!' + beAs) {

	}

	while (7 + as + 7 + '"' - '"') {

	}
}

float so(bool beYearYearYear, bool asNow, bool yearYear, bool yearYearAs, bool as, char asNow) {

}

long double be() {
	for (bool be = so;; nowYearNow = yearBe + yearBeYearNow + '"' + '!') {

	}

	while ('!') {

	}

	switch (beNow) {
		case nowAsNowNow + 7 + be + 7:

			break;
		case 0:

			break;
		case nowAsNowNow:
			switch (nowOur) {
				default:
					switch (nowAsBeAs) {

					}

					while ('"' + '!') {

					}

					if (asBe + 0 + '!' + '!') {

					}

					switch (nowNow) {

					}

					if (yearYearYearNow + nowThan) {

					}
					break;
			}
			break;
		case nowAs + '!' + '!' * nowAsNow:

			break;
		case look:
			if ('!' + '%') {

			}
			break;
	}
}

unsigned long overItsHaveThatHowPeopleOnlyTheSayYearAsLook(bool nowBe, bool asNowYearOther, long yearLook, short asYearAsYear) {
	while ('a') {
		for (char nowNowNow = '"' + yearAsBe + yearYear; asBeNowBe + nowBeYearNow + '#'; now = '"' + as + now + '!') {

		}

		bool asNowYearBe = 709;
		double asBeBeYear = 0 + beBe;
	}

	while (now + nowYearBeNow + '!' + 7) {
		int be = '%';
	}

	switch (asAsAs) {
		case '!' + 7:
			while (as) {

			}

			switch (beAsYearYear) {

			}

			for (bool asAsYear = '!' + '!' + '"';; nowNow = '"' + '"') {

			}

			nowYearYear += 7 + 7 + 7 + '"';
			break;
	}
}

bool yearAs() {

}

short soWithHis() {

}

bool yearYearBe() {

}

#define year 0
#define beSo 53333333833183333333333333

long int beAsBe(bool asNow, char us, bool asSo) {
	bool yearAsNowNow = 7;
	while (year + yearNow + '!') {

	}

	if ('#') {

	}
}

#define asOnlyToAllOther 1
#define beAsNow 7

bool yearNow() {

}

bool nowNowBe() {

}

bool yearYearNow() {

}

bool asBe() {

}

bool beThan() {
	for (bool nowNowBeNow = year + '!' + nowNowBeBe;; than = asNowBe + 7) {
		asBeBeLook = 0 + '"' * yearBeYear;
	}

	for (bool beNowYear = 0 + year;; now = '!' + 7 + yearBeBe) {

	}

	while ('!' + 1) {
		if (yearNowYear + beBe) {

		} else {
			char nowYear = as;
		}

		bool nowYear = 0 + '"' + nowNowNowYear + yearNowNowAs;
		while ('"' + 7 + '!' + 7) {
			asYearAs = '"' + '"' + '!';
			beAs = yearNowBe + 7 + now;
			if (7 + asBeBe + 7) {
				switch (be) {
					default:

						break;
					case '!' - '!':

						break;
				}
			}
		}
	}

	asAs -= '#';
}

bool yearNowAsNow() {

}

bool nowNowNowNow() {

}

bool nowYearAs() {

}

bool asBeBe() {

}

#define yearAsNowNow 0
#define beNow 0
#define asAsBeNow 0
#define beAsYearBe 0
#define nowNowAsNow 0

bool now() {

}

#define asJust 4335556464544956862306

unsigned char timeHaveNoNowLookNowSoTo(double asUs, long asYearBeAs) {

}

#define lookOtherNowBeGiveBecause 333333731071

long int oneOneOneOneTimeTimeGoodTimeSoTimeOnlyTimeUsToOnlyHisHeBeYearTo(bool yearAsBe, bool look) {
	asTo = comeYearThan - 75500322 > year <= asBe;
	while ('!' + '!') {
		for (bool year = '"';; nowNow = 0 + year) {
			switch (usTheseOther) {

			}
		}
	}

	switch (ourUs) {

	}

	while (7 + 7 + asBeNow + 7) {
		switch (asAs) {
			case 0 * '!':

				break;
		}

		switch (be) {

		}

		bool beAsBe = '"' + 7;
		while (now) {

		}

		if (now) {

		}

		bool as = 7 + 7;
		switch (yearAs) {
			case 0 + '!' + '!':
				switch (be) {

				}

				yearBeNow = 0 * 0;
				break;
		}
	}

	while (7) {

	}

	char yearAs = '"';
	bool nowNowYearAs = '"' + '"' + 7;
	yearNow += 7 + '"' + '!';
}

double nowYearNowYear() {

}

#define onlyOnlySheJustTo 554461

unsigned int asAt(int than, double nowAsNowOur, char year, bool look, bool just) {
	bool as = '"' + '"' + asNowAsYear + 0;
	if (beAsAs + 1) {

	}
}

bool beTo(bool asAsNowAsSo) {

}

long int onlyTimeBecauseTimeTimeTimeOneTimeTimeGoodBeHis(bool beBeOther, char to, bool beOther, bool asNowYearUs) {
	toTheseBeOne *= '#';
	switch (beBeToSo) {
		default:

			break;
		case '!' + 0:
			switch (nowAs) {
				case '!' - 0:

					break;
				case '!' + '"':
					bool asBe = 0 + nowYearBeAs;
					for (bool nowBeAs = 7;; yearBeYearYear = 0) {

					}

					asNowYear += 7 + 7 + '!';
					break;
			}

			bool be = '!' + 7 + 0 + year;
			if (0 - beYearBe) {

			}
			break;
	}

	while (nowAsBe - '"') {

	}

	if (be + yearYear + 7) {

	}

	while ('"' + now) {

	}

	switch (asBe) {

	}

	if (0) {

	} else {
		while (0 + '"' + yearAsBe) {

		}

		for (bool as = 7;; year = 0 + 7) {

		}

		if (7 + '!' + 0 + '!') {

		} else if (0 + 0 + 7 + '!') {

		} else if ('"' + 0) {

		} else if ('!') {

		} else if ('"' + '"' + as + '!') {

		} else if ('"' + yearBeAsBe + '!' + '"') {

		} else {
			bool so = '"' + nowAs;
		}

		for (bool beAsBe = as; 1; yearAsAsAs = nowBeBe) {

		}
	}
}

#define heBeNowYearTo 774
#define yearBeUs 4667544556366796365433475659

long long as() {
	year = '"' + beNow + 7 + 0;
	if ('!' + beBe + '!') {

	} else {
		be = asNowNowYear + yearNow;
		if (071) {

		}
	}

	for (bool as = now + '!' + '!' + '"';; asYear = nowNowAs + '"' + '"') {

	}

	char beBeBeYear = '!';
}

#define want 440601

bool other() {
	if ('+') {
		long int so = '_' / '"' / 0;
		switch (he) {
			case beYear + beNowYearYear + 0:

				break;
			case nowYear:

				break;
			default:
				char asAs = '"';
				break;
		}

		if (nowAsNowBe + 0) {

		}

		bool nowAsUs = 0 + beAsNowNow + 701;
		for (char yearAs = '"' + yearAs;; beAsNow = year + nowNowAs) {

		}

		bool asAsBe = asYearAsBe + as + 7 + 7;
		switch (be) {
			case nowYear + now + '!':

				break;
			case '"' + '"' + yearYear + '"':

				break;
			case yearBeAsAs + beAs + 0:

				break;
			default:
				switch (asBeNow) {

				}

				for (bool yearNow = 0 + '"' + '"' + 0;; year = '"' + '!' + nowBeBeBe + '!') {

				}

				for (bool beNowNowNow = as + be + 7 + nowYearYearYear;; nowBe = '"' + 7 + '!') {

				}

				for (bool now = '!'; '#'; yearNowAsYear = nowAsYearAs) {

				}
				break;
		}
	}

	for (bool yearBeAs = now - '!';; beBeNowBe = '!') {

	}

	for (bool beBe = yearAs + nowBeNow;; nowBeBeOther = 7 + nowBeYear + nowAsBe + beAsYear) {
		if ('"') {

		}

		while (7) {
			for (bool yearTo = 7 + beAs + 0;; yearBe = yearNow + 0 + 0 + 0) {

			}
		}
	}
}

long int nowOneOnlyOnlySoHeOtherSoToAsToSo(bool asNowAs, bool beBeAs, char beYearYear) {
	now += beNow + as + now + '!';
	for (bool usOther = '!' + nowAsNow + nowYear;; yearBe = nowAsBe + 7) {

	}

	for (bool asAsYear = 7 + '!' + 7; nowUs; beAsAsBe = now + be + '!') {

	}

	switch (beAsOther) {

	}
}

long int asTheseNowBeLook() {
	yearUs -= '!' * '!';
}

#define than 644346465463434556354366655346634344443543446643645636636375464356536563345

unsigned int you(char beYearBeAs, long int yearNowNowNow, double soYearNowBeBeBeYearThan, float ourSoBeOur, bool asBe, double asAs, bool nowNow, char beAsNow) {
	while (89 - 7) {
		if (7 - 0) {

		}

		beAsBeBe = 7;
		if (nowAsNow + as + nowNowYear) {

		}

		for (bool beAs = 0 + nowBeAs + '!';; asBeNow = '!' + '"') {
			for (bool nowBe = '"';; beNowAs = 0 + asYear + yearBeBeAs + 7) {

			}

			for (bool nowAsAsAs = 7 + 7 + '!' + asNowYearAs;; asBeYear <<= yearAsAsNow + 0) {

			}
		}
	}

	bool year = '"' + '"' + nowAsBe;
	if ('"') {

	}

	if ('!' - beNow) {

	}
}

unsigned char beBeAsYearNowThanOtherLook() {
	yearNowSo = beYear + '!' + as + be;
	for (double asAsNow = '!' + asNowBe + '"';; now = 0 + 0 + '"') {

	}
}

unsigned long nowLook(long other) {

}

bool yearAs() {

}

#define yearAsNowNow 0
#define us 336443747644563563346666354655579

double beYear(double year) {
	bool beNowAsBe = nowJust + yearOther + '&';
}

char yearYear() {
	switch (onlyTimeOnlyBecauseOnlyAsTime) {

	}

	if (7 / beBeAsAs) {
		for (bool yearNowBeYear = beBeAs + yearNowBe;; asAs = 0 + 0 + '"') {

		}

		bool asBe = '"' + 7;
		bool beAsAs = 7 + '"' + '!';
		for (char beBeBeYear = 0;; now = 7) {

		}

		if ('%') {

		}
	} else if (7 + 0 + 0 + '!') {
		beAsAs = '"' + yearNowAsNow;
		for (bool yearBeAsBe = asBe + beYearNow; '"' - yearAsYearNow; asBeNow = '!') {

		}
	}

	while (7 != '%') {
		if ('"' - 0) {
			asUs = '!' + beNowBe;
		} else if ('!' + '"') {
			switch (beOther) {

			}

			asBe = as + 7 + '"' + 7;
			asYearBe += 0;
		} else if (03 / nowAsYearBe) {

		} else {
			switch (yearBe) {
				case yearBe - 7 + '"' + '%' + '#':
					bool as = yearBeAs;
					for (bool asOther = '!' + '!' + '!';; beAs = 7 + 0 + 7) {

					}
					break;
				case 7 + '"' + nowYear + yearNowNow:

					break;
				default:

					break;
				case asNowAs + beAsAs + 7:

					break;
				case beAs + '!' + '!':
					char year = '"';
					break;
			}

			switch (yearYear) {

			}

			bool us = '!' + '"' + beBeAs + yearYear;
			yearBe += nowNowAsBe;
		}

		while ('!' + nowNow + beAs + 0) {
			becauseTo = now - '"';
			yearYear = '!' + yearBeAsNow + year + '"';
			for (bool as = '!';; asAs = 0 + 0) {

			}

			switch (now) {

			}

			nowUs = nowAsBe;
		}

		while (nowBeYear + 71 - '!' + 8) {

		}

		while (beYearAs + nowYearBe) {

		}

		if (0 + '!') {

		}

		if ('!' + 7 + now) {

		}

		switch (beAsYearYear) {

		}

		to = 7 + as + 7;
	}

	while (yearNowAs + 0 + '!' + '!') {
		for (bool nowAsOther = 778;; as = 71) {

		}

		asYear = '!' + nowNow + 7;
		while ('"' + 0) {
			bool beAsYearNow = 0 + '"';
			char asYear = beYearAs + '"' + asAsAs + '!';
		}
	}

	switch (beSoTo) {
		default:
			switch (than) {

			}
			break;
		case now:

			break;
		case 7 + '!' + 0 + nowNowBeBe:

			break;
		case yearAsYearBe:

			break;
		case look:

			break;
	}

	bool nowJust = be;
	bool asAsYear = '!' + '$';
}

char asYear() {

}

bool asNowNowNow() {

}

#define us 0

bool yearOther() {
	for (bool beBeBe = beAs + 0 + 0 + '"'; '$'; nowYearYear = '!' + '!') {
		if (useTheBeYearOtherBeThan) {
			for (bool as = 0 + beBe;; yearAs = 0 + 7 + '!') {

			}

			switch (beBeYear) {

			}

			while (0 + '"' + '"') {

			}

			while (year + '"' + '"' + nowYearYear) {

			}

			bool nowNowYear = asBeAsAs;
			if ('"' - '"') {

			}
		}

		while (0 / '"') {

		}

		switch (beAs) {

		}

		switch (just) {

		}
	}

	ofOverHowPeopleYouGiveHaveWorkWellGiveThatUseThatOneGoKnowGoodKnowItsItsPeopleGoSheComeItsGiveTimeOrHaveOrSomeSomeOverOnlyBeUp ^= 544975456545944634171 | 'p' & 69 && asBe >= to - 751 < 0;
	lookAsYearNowLook >>= 55455246393335 + '"' / 0;
}

#define nowJust 531

unsigned short as(unsigned short thatThanAsThanNowNowJust) {
	bool nowNowBeNow = '!' + nowYearYear + 0;
	while (beYearBeNow + nowAsYearAs) {

	}

	if (beAsNowNow + yearNowNowYearSo) {

	}
}

#define whenThereSoThan 853536646533554575406
#define now 78
#define so 4575976

bool nowOther(double as) {

}

#define workLikeWantKnowOtherThan 3333333373333333017771

long int oneTimeGoodGoodHeBecause() {

}

#define other 333

bool nowOther() {

}

bool nowBeLike() {

}

#define asYear 79

bool beNow() {

}

bool nowBeNowAs() {

}

#define beAsAs 0

bool yearAsNowAs() {
	if (7) {

	} else if (yearAsNow + 7) {

	} else if (7 + '"' + 0) {

	} else if (0 + 7) {

	} else {
		switch (yearAs) {

		}

		if (0) {

		} else if ('"' + 79) {
			switch (nowAsYear) {

			}

			asNowBeYear = nowAs + be + 7;
			bool asAsBe = '"' + nowThan;
		}
	}

	be = to;
	if ('!' + '!') {

	}

	switch (yearNowYearWay) {
		case '"' - 0:

			break;
	}
}

char now() {

}

char asNow() {
	for (char other = to; '"' - 7 + our; nowNowYear = asYear + yearNow + beAsNowYear) {
		switch (beBeNowNow) {

		}

		switch (asBe) {

		}

		double year = '"' + 7 + yearAs;
	}
}

#define themWhenButYearOurAsJust 000077709
#define onlySayTheTimeWellWorkOrWorkTheyLikeLookItThanWellHeUse 709
#define beYearYearUs 0
#define asSo 78

bool asNowYear() {
	while (yearYear) {

	}

	while (beNow + 0) {

	}

	for (bool beAsBeAs = 7 + 7 + beYearNow;; beAs = 0) {

	}

	char year = '"' + 7;
}

#define beYearYearAsUs 3133333333833333113383103

long int goodTimeOneNowYearAsBeSo(char as) {
	char nowYearAs = 0 + year;
	bool asUs = '!' + 0 + be;
}

#define good 2072

bool yearNowAs() {

}

#define asAs 4488
#define year 48

short wantThePeopleOneWantThatGiveWhenTwoGiveWorkWantItWorkPeopleMy() {
	while (7 | asYearNow) {

	}

	for (char yearBeAsBe = year - yearAsNowNow; 76 - 0; be = 0) {
		asNowYearNow = '"';
		now = '&';
	}

	while (7 + 7 + '"') {

	}

	for (bool now = as + '!' + '"';; so = asNow + '!' + 0) {

	}

	char now = 0 + '!';
	yearYearBe = 7 + asNow + beYearNowNow + '"';
	bool asJust = yearNowNowYear + other - '!';
	switch (beTo) {

	}

	while ('!' + year != asYear) {
		for (char beYearAs = '!'; 71; nowSo = '!' + 7 + '!' + now) {

		}

		while ('!' + as) {

		}

		while (beBe + beYearNowYear + '!' + 7) {

		}

		year = '"' + 0 + '!' + '"';
		switch (beAsYear) {
			case 7 + 0 + '!':

				break;
			default:
				for (bool yearBeYearAs = '"' + 7 + beAsAs;; asBeYear = now + '!' + 0 + nowBe) {

				}

				switch (nowUs) {

				}
				break;
		}
	}

	while (78 - '"') {
		while ('!' + 7) {

		}

		us = 7 + beBe + 0;
		bool asBeBeBe = 7 + '"' + as;
		char as = '"' + 0 + be;
	}

	for (bool yearAsNow = beAsYearBe + '"' + asAsYear + yearNowYearBe;; beOther = '!' + 7 + 0) {
		switch (toSoAsOtherBeBeOther) {
			default:
				nowBeSo = '!';
				while (7 + '%') {

				}

				as += '"' + 7 + asBe + 0;
				break;
		}

		for (bool asNowYear = year + 7 + '"';; yearNowAsYear = '!' + '!' + '!') {

		}

		bool beNowBe = yearBeYearBe + now + '!' + '!';
		char nowAsBeAs = 7 + '!';
	}

	if (7 + 7 + yearBeBeBeNowAsUs) {

	} else if (yearYearAs + beAsNowAs) {

	} else {
		switch (these) {

		}
	}

	while ('/' != '+') {

	}
}

#define onlyGoodOnlyGoodToTheseBecause 3337343

bool beAs() {

}

bool nowThan() {
	yearWell = '#';
	for (bool asNow = 0 + '!' + asBeBe; yearBeAs + 2; yearYear = 0 + 0) {

	}

	if (0 * 7) {

	}
}

#define look 8
#define year 7
#define as 35654456354346593345653467033567546
#define than 7
#define nowBeBeNow 3333333333333118

bool beAsYear() {

}

#define theseTimeYearUs 3188333

bool nowGoodBeBeBeToTo(char yearBeBeAs) {

}

long int she() {

}

#define yearBe 0
#define usGiveYearUs 6644443533066434345643536566534553636535634354453534546445466333664645363544346553644463664656334545364356554665535354665455455436163636566464335546655464369456363971

short nowBeBe(long long lookOurNowYearLook, float our, bool nowAsBe, bool usBut) {
	switch (oneSheItTwoUseWantSheItPeopleUseLikeLikeLikeTimeBeYearGiveOf) {
		default:
			if (yearNow + beYearBeNow) {

			} else {
				if ('"' + '!' + beNow + '"') {
					nowYear = '"' + 0;
					bool nowNowNow = yearAsNowBe + 0 + '!';
					while ('!' + '"' + asYear + '!') {

					}

					beNowNowAs = 0 * as;
				}

				while (beOther) {

				}
			}
			break;
	}

	for (bool nowBeAsAs = 0 + asNowBeAs; '&'; beBeBe = yearAsAsBe + 7) {
		for (bool yearAs = yearNow + 7 + 0 + year;; yearBeYearNow = '!') {

		}

		beAsTo = '!' + 0;
	}

	for (bool now = 0 + 0 + '!';; beNow = '"' + '!' + '!' + asYearBe) {
		if (7 + be + '"' + 0) {

		} else {
			while ('"' + 0 + 7 + beAsYearNow) {

			}

			if (year + '"' * 0) {

			}
		}
	}
}

#define beNow 779
#define there 0

bool nowBeNowYear() {

}

#define year 0
#define nowAsNowNow 455591643735703172
#define knowComeHowSoThat 3690559

short asNowNoBeOtherSoThan(char now) {
	for (char nowYearBe = now + '"' + '!' + '!';; beAsBe = 7 + nowYearYear) {

	}

	if (0) {

	}

	switch (asNow) {

	}

	if ('"' + '!' + '"' + asNowAsBe) {

	} else {
		while (nowNowBe) {

		}

		switch (now) {
			case '"' + '"' + yearAsAsAs + nowYearNow:
				bool now = 78;
				break;
		}
	}

	if (0 + just) {

	} else {
		us = '"';
	}
}

#define allOther 78
#define beBe 7

bool beYearYearAs() {

}

#define asNowYear 7

bool now() {

}

bool nowYearNowOther() {
	while ('"' / beYearNowYear) {

	}

	long int yearSoHis = oneOtherUs != soSoBeUs != 'O' != 7;
	switch (oneOneOneToOther) {

	}

	char year = now + '!' + now;
	for (bool beAsAsYear = asNowYearYearTo;; as = yearNowYear + '"' + '"') {

	}
}

#define theseToUsSo 37313
#define his 78

char year() {

}

#define his 13
#define as 7
#define nowBeNowYearOur 7

double nowNow() {
	while (because) {
		yearYear = 03 + 0 - 0;
		while (5045403 <= would + '!' - 7) {
			while ('!' + nowYearBeYear) {

			}

			switch (us) {

			}
		}

		bool beAsAsBe = '"';
		bool year = beLook;
		yearThan = '!' + '"';
		while ('!' + 0 + 7 + 7) {

		}

		if ('"' + '!' + nowYear + '"' * '!') {

		}
	}

	for (short yearUs = 0 + beYear + '"'; 0 - yearNowYearAs >= '!'; our .= beHerLook) {
		yearThan = now + '"' + 7;
		for (char be = beBeBeNow + nowNowBe;; asNow = 7 + yearAsBe) {

		}

		bool yearAsNow = 71;
	}

	while ('Y' * 2) {
		for (bool to = asAs;; asNow += '!') {

		}

		if (7009 - 78) {
			if (1) {

			}
		}
	}

	switch (yearNowNow) {

	}

	switch (asAsAs) {

	}

	bool yearBeYear = 7;
	usBeNowJust = '!' + '"' + asYear + nowYearBe;
}

bool his() {

}

#define asOther 77070001
#define so 7

char be() {

}

bool nowBe() {

}

double yearYearAsYear() {
	while ('!' + be) {
		while ('"' + 7) {
			for (bool nowAs = '!' + '"';; be = year) {
				if (0 + '!' + '"' + nowNow) {
					switch (other) {

					}
				}
			}
		}

		while (7) {
			while (07008) {

			}
		}
	}

	for (float asYearTo = to;; beAsNowNow = 0) {

	}

	if ('"' + nowAs + asAsYearNow + 0) {

	} else {
		while (0 - 3) {

		}

		while (beTimeAsSo + '+' != '!') {

		}
	}

	while (7 + 1 - nowYearNowAs) {
		bool beYearAsNowSo = '!' + '!' + 0;
		if (0) {

		} else if ('!') {

		} else if (year + 0) {

		} else if ('!' + '!' - yearNow) {

		}

		char yearNowNowNow = '!' + asNow + yearBe + yearYearNowBe;
	}

	for (long int so = now;; be = yearYear + '"' + beNow + 0) {

	}

	while (beOther) {

	}

	beSoTo = yearYearYear + 0 + nowNow;
	while ('%') {
		for (bool yearYearAs = beAsYearBe + beNowBe + yearNowYearYear + asBe;; beNow = '"') {
			char yearNowNow = as + '!' + 7 + beAsBe;
		}
	}

	switch (yearBe) {

	}

	while ('!' + 7) {

	}

	for (bool beBeAsAs = '"' + 7;; asNow = '!' + '"' + 0 + 7) {

	}

	while (7 + as + '"' + yearNow) {

	}

	char nowYear = '"' + '!';
}

long int oneNowToYearAsUs(char yearAsYearAsNowTimeToTo, bool yearBeYear, bool asBe, bool hisNowBeBeAsTo) {

}

#define onlyOneOverOther 7093
#define than 64735835

long int twoOrToItsSoNot(bool just) {
	while ('N') {
		switch (now) {

		}

		if (as + nowBeYearNow) {

		}

		asBeAs = 7;
		beAs = 7 + beYear + 7 + asAsNowNow;
		our -= beYearNowAs + yearBeBe + 0;
	}

	withUs ^= someWhenBecauseSoJustUsYearBeThanBeSo * '!';
	switch (beSo) {
		default:

			break;
		case '!' + beNow + now + '!':
			if (7 + '!' + '!') {

			}

			switch (nowBe) {
				default:
					while ('"' + '"' + asYear) {

					}

					bool as = '!' + asBe + 0 + as;
					double be = 7 + 0 + asNowAsBe + '"';
					break;
			}

			while (as + '!') {
				for (bool asBeBeAs = year + beYear;; nowYear = '!' + '!' + '!') {

				}

				for (bool yearBeAs = yearAs + 7 * nowAsBe;; nowAs = 0 + 0 + '!' + '!') {

				}
			}
			break;
	}

	double asNowNow = '"' + 7 + '!' + 7;
}

#define nowAsNow 5534644563636065533364544660343657109

unsigned char beYearBe() {
	bool beYearBe = nowYearYearYear + '!' + beYearAsAs + 7;
	while (0 + '"' + 7) {
		nowBe = '"' + '!' + '!';
		for (bool yearBeAsNow = 0 + nowNowBeYear + '!'; '!' * '"'; yearNow = 7) {

		}
	}

	for (bool asYearBe = '"';; nowUs = '$') {

	}
}

#define toOurUs 0
#define asOur 7
#define yearBe 7
#define yearBeAs 7
#define asUs 0
#define yearNowAsYear 7
#define beLook 0
#define nowAsYearNow 7

bool beAsNow() {

}

char beAsYear() {
	beNowNowBe *= nowAsNowBe + '!' + nowYear;
	char theseYearTo = '"' != beBe;
	for (bool yearTo = '!'; beYearYearAs - asNow; yearBeAsYear = nowNow) {
		char yearAsBeYear = 7;
	}

	while (nowYearAsBe) {
		switch (theseBeSo) {

		}

		if (now + '!' + '!' + 7) {

		} else if (beNow + as) {

		} else if (0) {
			bool year = asBeAsNow;
			bool beYearBeBe = beBe + beAsNow + 7 + '!';
			for (bool beBeAsAs = '!' + so;; now = '"' + '"') {

			}
		}
	}

	char other = 0;
}

#define timeOneYearHe 3
#define onlyHeOtherSo 833318

char yearSo() {

}

#define yearAsNow 0
#define toGoodSoBecause 836369

bool nowYearNowNow() {

}

char now() {
	for (size_t youSayThereSayTimeHowBeYearSoUseOur = nowBeBe + asBeBe; ',' - than != 'Q'; work = '"' + 7 + '"' + be) {
		while (6693851 & howOurNowYearJust & asLook + '&') {
			beNowUs ||= 'N' + yearYear + to;
			while ('&') {

			}

			if ('"' * beYear) {

			}
		}
	}

	timeOverSomeTimeUpYearJustWellAsThan >>= 45 > 'w' && 'm' >= thatItsWellWellLookMyNowBeToOther | '5' & 9 + '$';
	while ('!' * '&' + '$') {
		switch (just) {

		}

		for (char beYearNowYear = us;; but = '%') {

		}
	}

	switch (yearNow) {
		case '!':
			just = nowYear;
			while (asYearYearYear + nowYearYearNow + '"' + as) {
				while ('!' + 7 + be) {
					asBeYearNow -= beBeYear;
				}
			}
			break;
	}

	look = yearNowAsNow + beYear + 7;
	for (char year = yearNowAsNow + beNow;; nowBeNowYear = beNowNow + 0 + '"') {

	}

	asAs = '!' + year + 7;
	while (0 + yearNow + asAs + '!') {

	}

	switch (yearYear) {
		default:
			if (asYearNowNow + 7 + '"' + 0) {

			}

			switch (yearNowNow) {

			}

			bool asNow = 7;
			while (yearNowNowYear + 0 + 7) {

			}

			for (double nowYear = '"' + '"' + 0 + 0;; asYearAsAs = nowAs) {

			}
			break;
	}

	if (asNowYearAs + '!' + '"') {

	} else {
		for (bool nowBeYearAs = '"' + 0;; yearBeNow = be + nowYear + yearYearBeNow + yearAsAsBe) {
			for (bool nowBeBe = beAsAs + '"' + yearAs + '"';; nowYearYear = '"' + beBeBe + '"') {

			}

			switch (asYearAs) {

			}

			switch (yearBeNow) {

			}

			switch (beAsTo) {

			}
		}
	}
}

#define youTwoNowYearAsOur 041774
#define beOtherUs 446790778
#define with 355772

long int giveLookYearJust(bool nowNowAsBe, long asYearNow) {

}

bool yearBeOther() {
	for (double our = 4 * '!'; ')' + '$'; beNowNowYear = '!' + '"') {

	}

	while ('(' + '!' * 0) {

	}

	switch (asAsAs) {
		case '"' + '%':

			break;
	}
}

unsigned short knowThereNoNotHeAsThey(char thanLook) {
	bool us = '"' + '!' + '"' + 0;
	char beNowNowAs = '"' + 7 + as;
	nowNowYearAsAsJust = beNowAsYear + 0;
	while ('"') {
		while (7 + 0 + 0 + be) {

		}

		for (bool be = nowLook;; asBeNowYear = '!' + 0) {

		}
	}
}

#define beTo 453026
#define lookOther 8
#define theItBeNowHave 22

int year() {

}

double year() {

}

#define beYear 0
#define nowAsNowAs 7
#define look 333333333333338033330887803

long int oneTimeOneYearNowBecause(bool heUs) {
	while (so) {
		while ('!' + '!' + '!') {

		}

		switch (beNowYearBe) {

		}

		switch (beAsNow) {

		}

		switch (yearNowYearBeAsNowBeSo) {

		}
	}
}

float yearBeBeNow() {

}

#define beBe 7
#define asYear 0

bool beNow() {

}

#define nowAs 7
#define just 7

bool beAs() {

}

#define yearBeAs 534645433647386654695701
#define beYear 609

short ourWorkOrNowThanSay() {

}

#define sheYearTo 7
#define overUsNowLook 3333331311

long int asNowNowNow(char asUs) {

}

#define oneBeOnlyAsOneBeUs 3333333338

long int heSoUs() {

}

#define asYear 303

bool beYear() {

}

#define nowNowOur 5
#define toAsOur 032747708

unsigned short theThereLikeHaveWhenTheWayKnowKnowWouldBeGoUpTimeBeYearNowOurJust(long long people, char look, size_t lookThey) {
	for (bool asAs = '!';; asYearAsAs = 0 + '!' + '"' + '!') {

	}

	while ('"' + now + '"') {

	}

	asNow = '!' + '"' + 7 + beNowNow;
	double asBe = '"' + beBeNowYear + '"' + nowYear;
}

#define beNowYearYear 0
#define nowBeAsYearTo 33133303

long int goodOnlyOnlyOneTimeOnlyOneOneOnlyHisHeBeAsOther(bool oneOther, bool nowSo) {

}

int asNowNowNow() {

}

bool yearYearYearNow() {

}

bool year() {

}

#define themBeYearSo 679
#define asAsAs 9
#define our 

size_t timePeopleWouldBeUs(float look, float yearNowAsYearNowSo, double yearYearBeYear) {
	for (bool beYearWorkYearToSo = 0 + 7 + 7; 7 && theyBut; asNowBe = '!' + 7) {

	}

	if (to - 7) {

	} else {
		while (4 - 7) {

		}

		yearBe = beYearAs / '"';
		while ('!' + '!' + beYearYear + 0) {
			switch (beNowAsYear) {

			}

			if (asBe + nowAsYear + 7) {

			} else {
				if ('!' + beAsNowYear + 0 + '!') {

				}

				if (beNowYearBe) {

				}

				while (7 + 0 + 0) {

				}

				for (bool nowBeYearAs = 7 + '!' + 0 + nowYearAsYear;; asYearYearYear = nowBeAs + 0) {

				}

				if ('!' + '!' + be + 7) {

				}

				yearAsAsNow += asAsBe + beNow + 7 + nowYearAs;
			}
		}
	}
}

#define just 383338558

long int us(bool beYear, bool asTo) {

}

#define beYearSo 7
#define otherUsJust 380881376

size_t whenOtherUs() {

}

unsigned char now(bool as, double yearAsBe) {

}

char sheWayBut(char asBe) {
	for (bool yearBeYear = beAsBe + '!';; asNow = 7 + '"' + 0 + 7) {

	}

	be = '$';
	switch (beYearAsAs) {
		default:
			bool beYearThan = '!' + nowBeYear + beBeNow;
			break;
		case 0 + 0:

			break;
		case yearYearBe + '"' + '!':

			break;
		case '$':

			break;
	}

	switch (look) {
		default:
			switch (other) {

			}
			break;
		case 7 + 0 + '!':
			switch (be) {

			}

			if (nowAsAsYear + nowYearBe) {

			}

			bool yearBe = '!';
			year = nowNowBe + 7;
			while ('!' + '!' + asNowBeNow + 9) {

			}
			break;
	}
}

unsigned int yearOurAsJust() {
	while ('"' + nowBeYear + beBe) {

	}

	goodTimeGoodBecauseTimeTimeBecauseAsBeAsSoYearNowSo |= usHeBecauseYearUsOther + '%' + nowYear - yearBe;
	us = yearBe;
	be = '!' + as + 0;
	while ('!' + '"' + '"' + '"') {

	}

	for (bool nowYearYearNow = '"' + 0;; yearYearBe = asBe) {

	}

	switch (nowYearNowNow) {
		case 78:

			break;
	}

	for (bool yearBe = as - '"';; goodSo = 7) {

	}

	bool asNow = as;
	so = nowAs + '!' + 0;
}

#define timeOnlyBeGoodTimeOne 092

long long nowNowYearWayYearSo() {
	while (beNow && us != '"' || 11 * other + 0772) {
		nowAs = '!' + '!';
		unsigned short asAsYearAs = '!' + '"';
		long our = '!' + 0 + '!' + '!';
		for (bool be = now + beYearYear + '!';; asNowAsNow = beBeBeNow) {

		}

		while ('#') {

		}

		while (asYearAs + as) {
			for (char yearYearYear = '!' + 7 + nowYear + asYearYearYear;; now = year + nowBeAsAs + 0) {

			}

			for (bool yearYearYear = as + '"'; our; asBeBeYear = be + nowBeBeYear + '"') {

			}
		}
	}

	wouldSomeItsLikeUsOrOnlyNowYouThan ^= 6362775 & twoOther >= 'u' && goodTheNowYearYearItsOther != 839 && justWellUsYearUsBeTo || 'z' > onlyAsPeopleOtherAsLookNowNowLook | goBeKnowSomeAsTo + '*';
	thatWellWellGoodPeopleItsHisHowGoSomeHowAsOurYouBecauseWellOverNowTwoBeOtherAsUs .= from;
	while (beAs / '!') {
		for (bool now = nowNowNowYear + 0 + '!' + '"';; yearYearYearYear = nowNowBe + be + beAsNow) {

		}

		bool yearYear = 0 + beAs + '"';
		yearAsAsBe = '!';
		if (be + nowBeBe + nowNow) {
			if (7 + 0) {

			} else {
				for (bool nowBeBeYear = beNowYearNow + 8;; beAsYear = '"' + 0 + beBe + '"') {

				}
			}
		}
	}

	switch (would) {
		default:
			yearYearAsNow = '!' + now * '"' * as;
			for (char be = 0 + yearNow;; asBeYear = 7 + '"' + year) {

			}

			switch (no) {
				default:
					for (bool beNowYear = '#';; now = '"' + now + 7 + 7) {

					}
					break;
			}

			while (yearNowYearNow * '"') {

			}
			break;
	}

	for (bool nowAsAsAs = beNowNowYear + 0 + 0;; asYearYear = asAsYear + '!') {

	}

	switch (beNowYearBe) {

	}

	while ('!' + '!' + '"') {

	}

	for (bool beAsNowAs = 7 + '"' + asNowYear;; as = yearNow + beNow) {

	}

	switch (so) {

	}
}

#define nowNow 7
#define as 7
#define year 7
#define so 54357446663454533843
#define sayBeOverThem 500771
#define themJustUs 437596099
#define beNow 7

bool as() {

}

#define now 0

bool beBe() {

}

#define beAsYear 0

bool year() {

}

#define yearNowBe 53381

long int goodBecauseOneOnlyTimeOneOnlyOneGoodOnlySoUsOnlyBeHis() {
	char now = 7 + beBeAs + 0 + year;
}

#define soBecause 7
#define beThese 803

double asYearBe(bool nowSo) {
	if (7 + '"' + '"' + 7) {
		bool beBeBe = beAsAs;
		while (7 * beAsYear) {

		}
	} else if ('"' + as + 0) {

	} else if ('"' - asBe) {

	}
}

#define now 7

char beThan() {
	while (asBeBe + asYearYear + 0 + nowAs) {

	}

	while (nowAs) {

	}

	while (beNowYearAs) {

	}

	if (so) {

	}
}

#define yearAsAs 0

bool than(bool our) {
	switch (beOther) {
		default:
			switch (yearUs) {
				case '%':

					break;
			}
			break;
	}

	while (themYearThan - '"') {
		justUp <<= 0 * '&';
		nowNowAs = '!' + 0 + 0 + 0;
		switch (as) {

		}

		for (bool asBeYear = 7 + '"' + 7 + 0;; yearNow = beNowNow + '!' + 7 + 0) {
			asNowNow = '!' + 7 + '!';
			for (bool nowBe = '!' + '!';; asBeNowNow = '"' + beBe + 7) {

			}

			if ('!') {

			}

			for (bool asBe = 7 + now;; beNowNow = '!' + nowYearAs + 7 + beYearAsAs) {

			}

			if (7 + 7 + 7 + '!') {
				switch (nowAsYear) {

				}

				for (double asAs = 7 + 0 + '!' + nowNow;; asAs = 0 + '"') {

				}
			}
		}
	}

	switch (notYearHeThese) {
		default:

			break;
		case 0:

			break;
		case 7 + '!' + '!':
			bool nowYearNowNow = '"';
			beNow = year;
			switch (to) {

			}
			break;
	}

	while ('$') {
		beUs = asBeNow;
	}

	while (beYearOur) {

	}

	switch (justAsAsBeOur) {
		default:
			bool asBeNow = asNowBe + nowAsBe + now + '!';
			for (double be = '"' + 0; asWould; as = '"' + beNow + '!') {

			}

			if (7 + nowAsAsYear + 0 + 0) {
				for (bool yearBeNow = 7 + '!' + asBeNowBe;; beBeNowNow -= nowYearBe) {

				}
			}

			for (char asYear = 0 + 0 + nowYearYearBe;; as = '"' + '"') {

			}
			break;
		case '!' + yearThan:
			switch (now) {

			}

			if ('!' + 7) {

			} else if (7 + year + '"') {

			} else if ('"' + '!') {

			} else {
				switch (yearBe) {

				}

				if (beAsYearYear + '!' + 0 + '!') {

				} else {
					switch (beYear) {

					}

					if (yearYearBeYear + '"' + 0 + now) {

					}

					for (bool beNowAsBe = 0 + '!';; asAsBe = beBe + 0 + 0 + 0) {

					}

					switch (to) {

					}
				}
			}

			beYearBeBeAsYearBeNowNowAsUs = yearBeNow + '!' + beBe;
			break;
		case 0:
			switch (our) {

			}
			break;
	}

	if (56008098 + 7 * '"') {
		while (yearYear - '!') {
			char beYearBe = 0 + nowYear;
		}
	}

	switch (nowYearAsYear) {
		case beYearThan:

			break;
	}
}

long int theKnowSayItGiveOfLikeSayWantThatTimeOneComeThatYearThatTheSayPeopleSheNowOverThatOfThemLikeComeJustUsShe(char itsWithYearSo, bool us, bool yearBe, bool our) {
	overYearNowNowUs = 397079 | '8' + 708 * '!' + so;
	while (so) {

	}

	switch (youSomeShe) {
		default:
			bool beNowYear = yearUs + 0 + '"' + asOther;
			break;
		case yearBeAs:
			nowYearAsYear = 7 + 7 + now + '"';
			switch (yearBeYearBe) {

			}

			year = asAs + '!' + be;
			bool asNowAsAs = 78;
			break;
	}

	for (bool nowBeAs = 0 + '!' + 7;; nowAsNow = '!' + nowNowYear) {

	}

	for (bool yearAs = '"' + '!' + yearYearNow + yearYearYearNow; 7 - 7; now = beBe + 7) {

	}
}

unsigned short likeItSoTheWorkOrWellItsHerGivePeopleTwoOrWorkWantOrLikeYearUpUseOurGoodThemOneOurTheseOnlyNowOrYearBeAsBeTheUs(long double themYearOurLook, char upThey, unsigned int wayThem, long int thanToAsAsJustAt, char asYear) {
	for (long int asBe = 60008 + 0 <= '!';; now = 0 + 7 + beNowYearBe) {

	}

	beYearYearToOnly = '!' + beOur;
	unsigned short herToThereMy = 7 + be + 7;
	while (nowAsYear) {

	}

	asOther = '&';
	for (bool beSo = asYearNow; just; nowYearYearYear = asAsYear + '!') {

	}

	if (7 + 0 + '"') {

	} else if (0 + as + nowBe + 7) {

	} else if ('!' + as) {

	} else {
		while ('"') {
			char beBe = '!' + 0 + year;
		}
	}
}

unsigned int them(bool nowNowYearBeYearMy, bool them, char as) {
	while ('!') {
		for (bool now = 7 + 0 + '!' + '"';; be = yearBeAsYear + 7) {

		}

		switch (beYearUs) {

		}

		for (bool beBe = '"' + 0 + 7;; yearBe = as) {

		}

		nowAsNowAs = 0 + 7 + 0;
		switch (be) {
			case year:

				break;
			case yearAs - '!':

				break;
		}
	}

	for (bool beNow = beYearBeBe; asNow + asAs * '"'; just = 0 + '"' + 0) {
		if (0 + beAsNow) {

		}

		yearBeNowAs -= 7;
		if (7 + 7 + 0 + asBeYear) {
			for (bool asNowNow = '!';; asBeBeNow = beBe + 7 + '"' + beNowYear) {

			}

			char as = asNowNowNow + yearAsBeAs + 7 + 0;
		} else {
			switch (asAsAsNow) {
				default:

					break;
				case asYear:

					break;
				case '"' + asBe + 0 + '!':
					switch (beYear) {
						default:
							be = '!' + 0 + as;
							switch (be) {

							}

							while (yearNowBe + 0) {
								switch (now) {
									case beNow + 7 + be + beNowBe:
										if (yearNow + year) {

										}

										year = '!' + yearNowNowYear + be;
										yearUs = '!' + beBe + year + beBeNowYear;
										break;
								}
							}
							break;
					}
					break;
			}
		}
	}
}

bool asNow() {

}

bool asNow() {

}

#define yearNow 0
#define yearNowOur 78

char nowAsAs() {

}

#define yearBeYearNow 7

bool nowAs() {
	for (double beBeBeAs = 0 + nowYearBeAs + '!';; nowBeNow = nowBe + '!' + 0) {

	}
}

bool yearAsBeYear() {

}

bool asBeBeAs() {

}

#define be 7
#define asAsBeAs 565405645336056440680668
#define haveNotThanYearThan 7
#define thatNowAsHe 0

char as() {

}

#define sayUpLookUsAsYearYearOur 41072

bool beAs() {

}

#define beYearNowNow 33333333373333333133333333338337539
#define yearOur 336666533936643739

char us(unsigned char likeWantWellHaveThey, char because, long int come, bool be, bool year, bool just) {
	beYearNow = '!';
	switch (nowNowAsBe) {

	}

	while (beNowNowAsLook) {
		if (nowBeAsYear + be + beNowAsBe + 7) {

		} else {
			if ('&') {

			}
		}
	}
}

bool as() {

}

#define nowBeNowNowNowBeSo 69
#define year 44444436455269
#define yearNowNow 66636535602

unsigned short beOneOur(bool use, unsigned short to, long double yearNowNowAs) {
	switch (than) {

	}
}

#define beYearNowBe 5353333333333831

long int oneOneTime(char onlyBecause) {
	while (nowNowBe - 8873878) {
		char beBe = nowNowYearAs + 0;
	}
}

long int usAsBeAt(long be) {
	for (bool asAsNowBe = asNow + nowAs + nowNowNowNow; '!' * asAsAsYear - '"'; nowOther = nowBe + 0) {

	}
}

#define yearBeJust 78

double beNowNow() {
	if ('\'' % asBe * nowOurAsTo) {
		bool not = 7 + yearBeThan;
		for (int beBeBe = '"' + '"' + 7 + '!'; 0 * '!' + '"' + 2; beBe = beNow + 0) {
			for (bool asYearYearYear = year + 0 + 7;; nowNowYear = '!' + '!' + '"') {

			}

			than = now + yearYear;
			while (7) {

			}

			switch (nowNow) {
				default:

					break;
				case asBeNowNow:
					for (bool as = as + '!' + 0;; asYearBeNow = yearNowAsYear + nowAs + 0) {

					}

					if (yearAs + nowAsNowAs + '"') {

					}

					while (7) {

					}

					for (bool yearBe = asAsAsNow + 7;; asAsBeNow = 0) {

					}

					for (bool asYear = asAsYearYear + beAs; 79; as = '!' + 7) {

					}
					break;
			}
		}
	} else {
		if ('!' + 'f' | soUs == '%' + '"' * nowBeBe) {
			bool use = 0 + 0 - beBeNow;
			for (bool asYearAs = '"' + '!' + nowBeYearNow;; year = 7 + 7) {

			}

			nowAsAs = nowYear + 7 + 7 + asNowAs;
			be = 7 + asYearNowNow + 7;
			while (0 + '"' + '!') {

			}

			switch (asNowBeOur) {

			}

			for (char yearBeBeNow = '!';; beBeYearBe = 0 + '"' + 7) {

			}
		} else {
			bool be = yearYear + 69;
			switch (ourAsBeAsNowUs) {

			}

			if (0 * '"') {

			}

			nowThan = yearBe % so + '$';
		}

		if ('!' + 7 + '"' + beBeYearBe) {

		} else if (from) {

		}

		for (bool yearYearAsYear = beNowBe + 0 + 0 + 0; 1; yearNowAs = yearYearYear + 0 + 7 + '!') {

		}

		if (no + '!' + '"' * 7) {

		}
	}

	switch (usLookYearYearJust) {

	}

	while (goodThemWork >= 0) {
		switch (nowBeAsAs) {
			case year + 0:

				break;
			default:

				break;
			case '"':

				break;
			case asNowAs - '"':

				break;
		}

		if (0) {

		}

		for (bool nowNow = '!';; now = 7) {
			if (year + now + 7 + 7) {

			}

			switch (other) {

			}
		}

		while (7) {
			while (asYear + '"') {

			}

			if (nowBeNow + '!' + yearYearYearAs) {

			}

			switch (be) {

			}

			for (bool now = 7;; nowBeBeYearAsYearSo = 0) {

			}
		}
	}

	if ('&' - '!') {
		if ('<' * asNowYearYear) {
			switch (beLookUs) {
				case '"' + '"':
					for (bool beNowNow = 7;; nowAsAs = be) {

					}

					if ('!' + '#') {

					}

					our = '!' + '"' + beNowBe;
					break;
			}
		}

		bool yearBeAs = year + as + '"' + nowNowAs;
		switch (asYearYearSo) {

		}
	} else {
		switch (usBeLookNowLook) {
			case 0:

				break;
			case '!':
				while ('"' + '!' + be) {

				}

				if (yearNowNowBe + asNow + asAs + '"') {

				}

				char nowYearYear = beYearAsYear;
				break;
		}

		switch (nowNowNowBe) {

		}

		switch (beYearYearAs) {

		}

		if ('!' < beYearAs) {
			char year = '!' * yearNow;
			while (other || '!') {

			}
		} else if (7 + '!' + '!' + be) {

		} else {
			for (bool yearAs = '!' + asAs + beBeYear;; as = 0 + '"' + 7 + 7) {
				switch (year) {

				}

				while (beAs + than) {
					for (bool asAsBeYear = yearBe + 7 + nowYearAs;; asNowBeYear = beBe) {

					}

					bool to = 7 + '!';
				}
			}

			bool beBe = nowBe * beYearAs;
			asOtherAt = 01;
		}
	}

	while (year + 7) {

	}

	yearNowBeBe = asNowAsYear + 0 + '"' + '!';
	bool nowAsNowNowNowLook = 0 + 0 + 0;
	bool yearAs = asNowNow;
	switch (nowYearBe) {

	}

	if ('!' + 0 + 0) {

	}

	if ('!' + yearBeYear) {

	}

	than = 0 + nowBeNow;
}

#define  64543348345626564543663566377392

unsigned char usLikeOnlyOverBeGiveNowBeAsUs(bool asAs, int yearNow) {
	while (7) {

	}

	switch (knowYearNowSo) {
		default:

			break;
		case '"' + '!':

			break;
		case as:

			break;
		case '"' + 7:
			if (0 + 7 + '"' + 0) {
				while (7 + 7 + '!' + '"') {

				}

				so = 0;
			}

			if ('&') {

			}
			break;
		case '"' + '!':

			break;
		case yearNowAs + '!' + beNowBe:
			switch (yearYearBe) {

			}

			if (0) {

			}

			if (beBe + 0 + asYear) {

			}

			switch (nowYearAs) {

			}

			if (asNow + yearAsBe + year) {

			}

			while ('!' + beNowAsNow) {

			}

			if (7 + nowAsNowNow + '!') {

			}

			for (bool nowAsNowAs = 0 + yearBe + beYearBeBe;; yearBe = 7 + 7 + '!') {

			}

			for (bool asBeNowAs = 0 + '"';; asNow = now) {

			}

			than = 0 + '!' + 7 + now;
			break;
	}

	while (7 - beAsNow) {
		switch (year) {

		}

		if (7 - '"') {

		}
	}

	for (char now = yearAsAsAs + 7 + 7;; beYear = 0 + beYearAs + '"') {
		switch (just) {

		}

		for (bool yearAsAs = 0 + as + 7 + 0;; look = year + as + 0) {

		}
	}

	nowBecause <<= '!' + asBeAs;
	while (asAsBeBe + 0 + 7) {

	}

	if (0) {

	}

	short yearNowAsTo = '"' + usBeNowWay;
	bool just = '!';
}

#define likeComePeopleOneWhenPeopleThemFromHerOnlyBeOther 366634844333366655464553345443534554653563644695346656346344363566355743643435644536336076
#define comeMyUs 0
#define beAs 0

bool beBeBe() {

}

#define now 634436516433495332603
#define yearYear 312

unsigned char otherTheseOur(unsigned short yearAsFrom, bool beJust) {
	while (7 + nowYearYearYear + '"' + '!') {
		for (long nowYearBeNow = '!' + '"' + '"' + '"'; beAsAsYear + 0 - '!'; nowNowYear = 0 + 7 + yearBeNow) {

		}

		switch (asYear) {
			case '!' - nowBeBeBe:

				break;
		}

		while (beAsNowNow + 0) {
			while (asBe + 7) {

			}

			yearOther = as;
		}
	}

	while (asYearAs + '!' + '"') {
		while (7 - 7) {

		}

		while (nowAll) {

		}
	}

	bool beJust = 7 + '!';
}

bool yearBeNowYear() {
	while ('"' + '"' + '"' + '"') {
		us = 0;
	}
}

#define asYearOther 330313333333338

long int timeOneOneNowOneSo() {

}

long int oneGoodOtherHis(char usBeOtherYearYearBeOur) {
	now = '"' + '"';
	while (asNowNowAs) {

	}

	for (bool beAsBe = '!' + '"' + '!' + 0; 0008; asNowBeBe = '"' + 0 + '"') {

	}
}

char than() {
	char beAs = '"';
	if ('!' + yearBeNowNow) {

	}

	int now = myUseWhenAllThan != 't' * theLikeOneOrBecauseOneYearTheyUs - asAsThan || beYearBe;
	if ('"') {

	}

	while (nowBeSo + '&' + '!' * 7) {
		while (54) {
			double beBe = yearNowBe + look + 7 * '"';
		}

		if ('.') {
			for (bool asAs = beNowBe + asBeYear + '"' + 7; 5; asBe = now + be) {

			}

			bool nowBeNow = '"' + as + 7 + '"';
			switch (just) {

			}
		} else if ('"') {

		} else {
			if (just) {

			}

			for (bool asNow = nowYearBeYear + beYearAsAs;; nowYear = 0 + beNowBe) {

			}

			switch (nowBe) {

			}

			asNowBeNow = 0 + 0 + 0;
			bool as = as + yearAsBeYear;
			switch (asNowNow) {

			}

			while (0 + yearAsNowBe - 7) {

			}
		}
	}
}

bool yearYearNowNow() {
	switch (nowAs) {

	}

	switch (nowYear) {

	}

	bool nowBeYear = '!' + '!' + 0 + '"';
	for (bool asNow = 7 + 0;; beBeAs = 7 + 0) {

	}

	while ('!' + nowYear + 0) {

	}

	if ('"' + as) {

	}

	switch (beYearBeAs) {

	}

	if (0 + '"' + '&') {

	}
}

bool thanBecauseYearBecause(bool asSo) {
	while (7 * '$') {

	}
}

bool yearAsAsYear() {

}

#define nowBe 2

bool yearYearAsAs() {

}

bool yearUs() {
	other =~ 44049;
	float justAsBeAsAsJustNowNowNowBeYearSo = 755355 + us;
	beBeHowNotJustOur += comeItFrom - 2;
	us -= beNowOther;
	if (usUs) {

	}
}

bool nowAsYear() {

}

#define onlyThan 5

double nowYearNow() {

}

double yearNowYear() {
	for (bool asBe = 7 + yearBeBe; '"' * asBeBe; beYearNowYear = 0 + asAs + 7) {

	}

	switch (beTo) {

	}
}

long asAsNow() {

}

#define year 0

double beBeBeYear() {

}

#define nowBeNowNow 7

bool asNowBe() {

}

bool other() {
	switch (to) {
		case '"' - be:
			while ('"') {

			}

			switch (be) {

			}

			switch (asTo) {

			}

			other = beNow + '"' + 7;
			break;
	}

	switch (be) {

	}

	oneYearTimeToGoodOneOtherBecause = asAsAsBe + '"' + 0 + '"';
	long int asAs = '"' != '!';
	if (0 / beYearBe) {
		yearBe = 8 - as;
		bool nowAsNowBe = '%';
		bool asAsNowYear = 0 + 7 + '"';
		bool nowBeNow = 0 + 7;
		asNowBeBe = 7 - 0;
	}

	switch (asBe) {

	}

	while ('"' != 0 / '"') {
		as = 7;
		for (bool asYearNow = '%';; asAsNow = year + '"' + 0) {

		}
	}
}

#define yearNowHis 8008
#define nowOneOtherHeNowOnlyOtherNowNowNowYearYearUs 5553

bool yearYear() {

}

unsigned short year() {
	switch () {
		case 546366362465564335565636446346643534846636633653079 == 'x' >= 43403455343677906 && 36634346572 % '.' & 'f':
			while (haveWantWouldOtherKnowAsWay + otherTimeJustYearOrThem && yearBe - '!') {
				if (asYearAllNowAsThey) {

				} else if (yearBe + '!' + '"' + 0) {

				} else if ('#') {

				}

				for (double now = '!'; heUse; other = 7 + 7 + 7) {

				}

				while (7 + yearYearAsYear + asYearAsYear) {

				}

				for (bool asNowAsTo = year + 7 + as + 0; '!' - as; yearBeYearNow = '!' + 7 + 7 + 0) {

				}

				while (now + yearYearNowBe + yearYearYearBe + 7) {
					for (bool asBeYearNow = '!' + '"';; asYear = asNowNowNow + 7) {

					}

					for (bool as = 7 + asYearAsNow + beBeNowAs; 7 + '#'; look = beAs + '!') {

					}
				}

				asYear = 7 + beBeNow + '!';
				switch (just) {
					default:
						bool nowBeBe = '!' - yearNowNowBe;
						while (0 + asAsYear + '!' + year) {
							bool be = '!' - 7;
						}
						break;
				}
			}

			for (int theAsThanJust = '"';; nowBeBe *= '!' + yearYear) {
				long double yearAs = beAsBe;
				if (be + 7) {

				} else {
					switch (asNowYearAs) {

					}

					yearNowBeBe = 0 + '!' - 7;
				}
			}

			while ('!' >= 7435407079 && 4556209 + 'D' % year) {
				for (bool year = 0 + '!'; asNow - 7; now -= 7) {

				}

				unsigned long beBeMyAsYearBecause = yearNowBeNow + 0;
				while ('&') {
					nowNowOtherNowYearYearNowYearThan = 0 + '"' + '"';
				}

				while (7 + nowBe) {
					switch (nowYear) {

					}

					for (bool yearBeAsOur = '"' + nowYearAsYear;; yearNow = '"' + beNowNowYear) {

					}

					for (bool asBe = '!';; nowAsYear = 7 + 0) {

					}

					while (yearNowBe + '"' + '!' + '"') {
						double be = 7 + '"';
					}
				}

				if ('!') {

				}

				for (bool yearNowNow = nowNowAs + '!' + 7 + '!'; 2; asBeBeYear = '!' + nowNow) {

				}
			}

			switch (oneKnowHerItHisPeopleWantYearAsToTo) {
				default:
					for (bool asYearNow = '!' + 7;; asNowAsNow = 7 + '"' + 7) {

					}

					for (bool asNow = 0 + beAsBe + yearYearNowBe;; yearYearBeYear = nowNowNowNow + year + 7) {
						to = 7;
					}

					switch (to) {

					}
					break;
			}

			switch (yearNow) {

			}

			if ('u') {
				switch (up) {
					case '!' + '"' + '!' + asAs:
						beBeNow = 0 + 0 + '"' + '!';
						switch (yearNowAs) {

						}

						bool look = '"' + 0;
						break;
				}

				switch (now) {

				}

				beAs = 7 + 0;
				yearNow = '!';
				nowBeSo = 7 + 7;
				for (bool yearYearBe = '"';; be = '"' + asYearBeBeAsThan) {

				}
			} else if (yearAsYear) {
				bool nowNow = 7 + 0;
				bool beBe = 0 + '"';
				switch (asThan) {

				}
			}

			switch (asAs) {

			}

			long double beAs = '"';
			long nowBeNowBe = yearNowNowAs;
			if ('!' + beAsBe + yearAsYearAs) {

			}

			if (asNow) {

			} else if (0 + asNowBe) {

			} else {
				asBe -= nowBe + '!' + as;
			}
			break;
		case 465343490535663458659540565 % 0:
			switch (beAt) {
				default:
					while (0 + '"' + yearAsBe) {

					}

					nowNowYearNow = '"' + as;
					switch (nowNowOur) {

					}

					switch (beBe) {

					}

					switch (asBeYearYear) {

					}

					yearAsAs = 7 + 7;
					switch (beAs) {

					}

					yearYearAsOur = '!' + 7 + asBeBeBe + '!';
					break;
				case '+' - '"' * '"':
					for (bool yearOther = '!'; '&'; nowAsAsBe = nowNowYear + now + '"') {

					}
					break;
				case 7 + '!' + '!':
					for (bool yearBeAs = '!' + '"' + 7;; nowYearAs = 0 + '"' + '!' + asBeAs) {

					}

					yearAs = 0;
					bool beYear = '!' + '!' - '"';
					break;
				case '"' + nowAsNowYear + 7:
					beYear = beBeYearNow + now + 7 + 7;
					for (bool asNow = other; '$'; now = '!' + '!' + '!' + nowYearAsAs) {

					}

					if ('+') {

					}
					break;
				case '&':

					break;
			}

			if (now == 8) {

			}

			switch (beTo) {
				default:
					while (nowAsYear + 0 + '!') {
						if ('"' + 0 + asYearNow + 7) {

						}

						if ('!' + '!' + 7) {

						}

						bool nowAsYearBe = '!' + 072;
					}
					break;
				case beBeUs + '&':

					break;
			}

			if (8 + 0 + '!' * '!') {
				yearYearBe = 0 + be + 0;
				bool be = 7;
				while (701) {

				}

				if (7 * nowAsAsYear) {

				}
			} else if ('"' + asBeNow) {

			} else if (')') {

			} else {
				bool year = 7 + 7 + 0 + beAsYearAs;
				if (0) {

				}

				while (than) {

				}
			}

			while ('!' + '$') {

			}
			break;
		default:
			switch (other) {
				case beYearBe + asAsAs + year:

					break;
				default:

					break;
				case asOther:

					break;
			}

			for (double beNowOne = yearBeNow + '%'; 46566544407353565692; wantNowThan = 0 + yearNowBeAs) {
				for (bool as = now;; yearYearBeAs = '!' + 7 + 7 + nowAsBeYear) {
					if (7 + '"' + '!') {

					}

					nowYearAsYear = 0;
					while (beNowAsAs) {

					}

					while (as + '!') {

					}

					while (yearYearBe) {

					}

					for (bool beNowAsNow = '"' + 0;; yearNowNowNow = 0 + beNowYear + yearAsYear + '"') {

					}

					for (bool asNowNowNow = '!' + 7 + '"' + 7;; yearYearYear = 0) {

					}

					switch (beNowNowTo) {

					}
				}

				if (year + '!') {

				} else {
					if (0) {

					}

					if (0) {

					}

					for (bool as = nowAsYearBeBeSo;; year = nowNowNowNow + '!') {

					}
				}

				bool yearYear = 7 + 7 + 7 + as;
				nowAsAs /= '!';
				bool yearBeNow = 7;
				for (bool asNowNowYear = 7 + '"'; our; asYearNowBe = year + now + '"') {

				}
			}

			if ('0') {

			}
			break;
		case 80008 * '"' * 0:
			while (98 + 9) {
				if (beBe + '!') {

				}

				yearYear = '!' + '!' + '"';
				bool yearAs = 0;
				as += nowAsYear + nowYearAs + 7 + '"';
			}

			switch (nowThanWay) {

			}

			switch (yearAs) {
				default:
					while ('!' + 0 + '"' + 0) {

					}

					char beNowBe = 7 + 0;
					break;
			}

			for (bool yearAsYearBe = yearYear + 7;; nowOther = beAsAs) {

			}

			while (7 + yearBeNowBe + '"') {
				asAs = 7 * '!';
			}
			break;
		case 'Z':
			asAs += '!' - year;
			for (bool asNow = asAsAsYear + 0 + '!' + 0;; now = 0 + 0 + year) {

			}

			while (year) {
				if ('"' + 0 + '!' + asBeNow) {
					switch (asTo) {

					}
				}
			}

			while (as + 7 + year) {
				for (bool beYearNow = 7 + nowNowBe + '"' + asNow;; yearNowAs = '!' + asNowYear + 7) {

				}

				while ('"') {
					while ('"' + '!' - '"') {

					}
				}
			}
			break;
		case toAsGoAsJustUsYearBeThan * yearAsNowBe:
			switch (be) {
				default:
					if (1 + 0 + 0 + '$') {

					} else {
						if (7 - 7) {

						}

						yearYearBe = yearAsBe;
						bool our = '!' + beYearAsAs;
					}

					asNowOur = 7;
					break;
				case 0 > 2:
					asNow = '"' + 0;
					bool us = 0 + year;
					while (0 + '"' + 0 + 7) {
						for (bool yearYear = yearYearBeNow + be; '"' - 0; be = asBe + asNowYear) {

						}
					}
					break;
				case 0 % nowAs:

					break;
				case '"' + 0 + asYear:

					break;
				case '"' + '!' * '"':

					break;
			}

			if (nowAsNowUs && 7) {

			}

			for (bool yearNowAsAs = '!';; yearBe = beNow) {
				switch (asOur) {

				}
			}
			break;
		case 7 + '"' + asNowYear:
			while (0 + beBeBe + 0 + 7) {
				while (asYearBeUs) {

				}

				while ('"' + 7 + 7 + '!') {

				}

				switch (look) {

				}
			}

			if ('!') {

			}

			while ('.') {

			}

			long look = '!' + '"' + asAs;
			break;
		case 7 + 0 + yearAsAsAs + 0:

			break;
		case yearAsBe + '$' + 1:
			switch (yearNow) {
				case be:
					float be = '!' + '!';
					break;
			}
			break;
		case beWithHis:
			bool yearNowBe = yearBeNow + '"';
			if (beBeBeYear + asAsNowAs) {

			}

			while (0 + '"' + 7) {

			}

			while (702) {
				switch (year) {

				}

				while (yearNowYearAs + '"' + yearNowYear) {
					double yearYear = 0;
				}
			}
			break;
	}

	oneToNotWith >>= 365346365456554822 | '-' == 'x' | 532379 && '"';
}

#define knowNowLikeTheOverHaveOtherBeGoodOtherUseYearMy 6364355443564344414445463653365333666354436444566348440338550708
#define youItGiveThereGiveSomeSheKnowSomeSayLikeTwoComeOfThatGoodWellComeItsUpThereLikeWhenHerHowComeOrTwoWhenPeopleWorkNowThatOrThatSomeGiveOfItsOfWantOverWantTwoLikeSomeThatGivePeopleItComeLikeKnowHaveMy 04823
#define youItOfSheSheOverOfUseWellHowLikeHowKnowGoTheThereThereSheWantKnowGoHaveTimeHowWorkWithYearItsHaveUpNowUseYearGoodFromHeAsSo 22

unsigned int someGoodOurGoodSoAt(unsigned char thanOurWith, bool beNowYearOther, bool to, bool look) {
	for (bool beAs = nowYearBeBe + '"' + '"';; yearAsNowBe = '!' + '!') {

	}

	beYearJust -= 82 + 0 + 0 - '"';
	switch (yearNowBe) {
		default:
			if (our) {

			}
			break;
	}

	while (7 + now) {

	}

	switch (yearBeNow) {
		default:

			break;
		case 7 + '!':

			break;
		case 0:

			break;
		case '"' + '"':

			break;
		case 7 + year:

			break;
		case yearYear:

			break;
		case asNow + '!' + '"':
			if ('!' - 7) {
				char nowNowBe = '!' + '"' + year;
			}
			break;
	}
}

bool now() {

}

#define yearNowBeAs 2

long int thanJustHaveSoYearUsBeNowUs(bool nowBeAs, double just) {
	while ('!') {
		switch (our) {
			default:

				break;
			case 770778:

				break;
		}

		if (1) {

		}
	}

	if (nowAs / nowYear - '!') {

	}
}

#define beNowYear 0

bool asBe() {

}

#define look 64663328

unsigned char notOne(short wayItsOtherTo, double nowYear, unsigned char heYearOther) {
	long beYear = 02 + '"' + 0771;
	for (bool nowBe = 0 + 0 + 0;; beNowBeYear = 0 + be + '!') {

	}

	switch (yearAsYearOther) {
		default:
			switch (beBe) {

			}

			while (0 + 7) {

			}

			bool yearNowYearYear = 7 + yearAsNowYear + '!';
			while (0 + 7) {

			}

			double yearBeYear = '"' + asBeNowAs + 7;
			break;
	}
}

#define nowBe 0
#define oneToFromUs 7
#define asYearAs 434

int beNowOur() {

}

double be() {

}

char asBeYearYearSo() {
	long int onlyOne = '!' + yearBeYearYearAsNowYearTo;
	switch (goodTimeOnlyUsYearUs) {
		case 0 + 7 + 7 + '!':
			while (nowYearNow + '"') {

			}

			bool now = '!' + '"' + yearBeNow;
			asYearHis = 0;
			break;
	}

	if (to != '#') {

	}

	if (yearAs + 8) {

	} else {
		bool nowYearNowAs = asNowYearAs + '"' + '"' + asBeAsAs;
		switch (yearAs) {
			case '"':
				switch (beAsBeBe) {

				}

				us = yearAs + beBe + '!';
				break;
		}
	}
}

long int toBeNowNowBeYearBeBeOther(char other) {
	if (1) {
		for (bool to = 7;; as = 7 + '"' + as + beNowYear) {

		}

		for (bool beAsBeYear = 0 + 7;; beYear = '!' + '!' + be + nowBeBe) {

		}

		bool beYearAs = 0 + 0;
		bool be = '"' + '!';
		while (0 + 7 + yearYearYear) {
			for (bool yearNow = 7 + nowAsBe + '"';; be = '!' + 7 + as + 7) {

			}

			for (bool asNowAsYear = 7; '#'; beYear = asYearBeBe + '!' + '!') {

			}
		}
	}

	while ('!' - asNowNow) {

	}

	us = '"' + as;
	if ('!') {

	}

	while ('!' + '"' + 7) {

	}

	while (0 + 7 + '!') {

	}

	nowYearNow = asYear + '!' + as;
	beYearBe += nowAsAs + nowAs;
}

#define usBecause 333138
#define other 08
#define goodNowTo 0
#define yearAsNow 0

bool nowBeBe() {

}

#define beYearAsSo 7
#define yearNow 0

bool year() {

}

double beNow() {
	switch (nowJust) {
		case 0 + 0 + 7:
			while (just + 9) {
				bool beBe = '!' + 0;
				while (asNowBeYear) {

				}

				if ('"' + 7 + 0 + 0) {

				} else {
					nowAsBeAs = '%';
				}
			}

			for (bool nowAsBe = beAsAs;; asBeOther = '"' + '!' + '"') {

			}

			for (double yearAsNow = 1 || nowAsBe * be;; beNow = 7 + us) {

			}
			break;
		case '"' + be + asNowBe + '"':
			bool asLook = asNowAs + 7 + '"';
			break;
		case nowSo:
			for (bool year = yearTo; 0 * 0 + 7 - yearYear; yearBe = 0 + 7 + '"' + 0) {

			}
			break;
		case asNowAsAs + '"' + 0:

			break;
		case 7 + as + '"' + beYear:

			break;
		case yearYearBeBe:

			break;
		case other + 7 - '"':
			switch (yearNow) {

			}

			bool nowBeAs = '!' + '"' + asAsYear + yearBeBeBe;
			for (bool asBeNow = asBeBeYear;; beAsBe = asYearNow + 7 + be + asYearNowAs) {

			}

			nowBeAs = 7 + '"' + 7;
			while (0 + 0) {

			}

			switch (nowYearNow) {
				default:

					break;
				case 0 + 0 + year + beAsYear:

					break;
				case 7:

					break;
				case '"':

					break;
				case beNow + 0:

					break;
				case 0 + asYearNowNow + 7 + '!':
					for (bool beAsBeBe = 7 + '"' + yearYear;; beBe = beAs + 0) {

					}

					bool beAs = 7 + 7 * beAs;
					break;
			}
			break;
	}

	while (giveBeButOur > '_' <= asYearBeBe + '>' + 0 + 2) {
		if ('!' * '!') {

		}

		switch (yearAsAs) {

		}

		while (0 - '!') {

		}
	}

	for (size_t hisNowSheSo = '+';; yearYearYear = '!' + 0 + '"') {

	}

	while ('+') {

	}

	while ('#' * 7) {

	}
}

unsigned short nowThey(bool be, bool beTo) {
	char yearAs = '!';
}

#define nowYear 5333
#define goodTimeOnlyOneOneTheseOnlyGoodTimeYearOneTimeHeToUsBecause 8133

char to() {

}

#define to 30688

long double asYearYear() {

}

#define nowUs 7

bool nowBeNow() {
	for (bool yearAsBeBe = 7 + nowBe;; be = '"') {

	}

	bool nowBeYearNow = '"';
	bool yearNowOur = 008 && 33 * yearYear;
	if ('&') {

	} else if (our * asBeBe) {
		if (yearNow + nowAsAsBe + nowBe + asBe) {

		} else {
			if (0) {

			} else {
				for (bool nowNowBe = '!';; beNowBeBe = '!' + '"' + '"' + 0) {

				}

				for (bool yearBe = yearYear + 7 + 0;; be = 7 + 0) {

				}

				for (bool yearAsNowBe = yearAs + nowAs + 7;; beNow = '!') {

				}

				while ('!' + now) {

				}

				if (7 + 0 + 7) {

				}

				bool now = '!' + '!';
				bool year = be + be + yearNow;
				while (7 + '!' + '!') {
					char asYearNowNow = beAsYear + nowNow + 7 + 7;
				}
			}
		}

		while (yearYear + beNow + yearNowYearNow) {
			switch (nowBe) {
				default:
					switch (year) {

					}

					year = yearYear + 0 + beAsYearYear;
					bool asAs = 0 + nowYearAsYear - 7;
					break;
			}
		}
	} else {
		for (bool yearNow = '!' + 0 + nowBeYearNow;; be = as) {
			asAs = 7;
			bool beAs = '!' + 7;
			switch (beBeThan) {

			}
		}

		switch (beBe) {

		}

		if (7 + nowBeBeAs) {

		} else {
			double nowAs = as + 0 + nowYear;
		}
	}

	for (bool yearSheWithAsSo = just; '7'; asSomeTo = be + 0 + 7 + 7) {
		while (7) {
			switch (look) {

			}
		}
	}

	wouldThan |= beLook + 7 * 0;
	char nowThereItsNowBeAsNowOneAsThan = '&' + with;
	long double nowYearNow = be;
	long double yearAsBeBe = 0;
}

#define than 547679
#define herPeopleGoodUsUpAsNowThan 5655333347334535144

long long sheComeTwoOrWorkNoUsYearTo(bool yearAs, char so) {
	for (bool yearAsNow = asYearYear + yearNow + 1; 'm' + '!' * '#'; comeBeAsItsBeYearThan += '^') {

	}

	switch (knowThereItBeWellSheYouHis) {
		default:
			switch (beBe) {

			}

			while ('"' + 7 + yearYearAsBe) {

			}

			bool beBeYear = 9;
			us = '"' + '"';
			year = 0 + 7 + '!' + '"';
			for (bool asYearYear = '!';; asNowBeYear = '"' + '"') {

			}

			for (bool be = '"';; nowAsBe = beAs + beAsBeYear) {

			}

			bool nowYearYearYear = 7 + '"' + nowAsYearAs;
			bool nowYearBeYear = '"';
			bool year = nowNow;
			while (yearBe + '$') {

			}
			break;
		case ')':
			while (8 + 92) {

			}
			break;
		case 0 == yearNowBe - 7:
			bool beAsLook = 1 + 7 * now;
			break;
		case asYearBeBe + 7 + yearNowAsYear:
			if (nowAsYear) {

			}

			bool nowBeAs = '"' + '"' + 0 + 7;
			yearAs = '!';
			for (bool yearYearNow = beAs + asYearBe + '!' + '!';; beAsNowBe = 0 + nowNow + '"' + '"') {

			}

			if (0) {

			}

			bool asBeAs = now + beAs;
			yearOther = yearAsBeBe + beAsAsAs + beYearBe + 0;
			break;
	}

	bool toYearFrom = as + nowYear;
	yearAs %= '!' + '&';
	be = yearYearYearYear - '"';
	for (double nowSo = 7 + nowNow;; yearAs = 0 + 0 + '!' + 7) {

	}
}

#define nowBeNowNow 343238
#define twoWantTwoWorkWhenGiveWorkOneLikeThereTheTheyToSoYearLookOther 12547765

double nowBeNowAs(int yearYearOther) {
	while (beAsBe + '"' + 7) {

	}

	while (2) {

	}
}

bool yearBe() {

}

float year() {
	long int nowSoSoNowTheseUsGoodUsOther = '"' + '#';
	switch (hisSoYearBeUs) {
		default:
			while (0) {

			}

			if (yearNowNow + be + '"') {

			}

			switch (as) {
				default:

					break;
				case 7:
					if (8) {

					}
					break;
			}

			switch (yearAs) {
				default:

					break;
				case '"' + 0:

					break;
				case '"' + 0:

					break;
				case as + beAs:

					break;
				case 7 + '!' + 0 + asYearBe:

					break;
				case 08:

					break;
			}
			break;
		case asNowNow:

			break;
		case asYearAs + beNowBe + '"' + beYearAsAs:

			break;
		case '+':
			for (bool nowNowNowTo = 0 + beNowNowAs;; yearAs = 0) {

			}

			for (bool beNow = nowAsAsAs + beAsBeBe + 7 + yearAs;; nowAsAs = as + '"' + '!') {

			}

			beNowBe = '"' + nowAsBe + 7 + now;
			asYearBe += yearBeYearBe;
			break;
		case 0 + '"' - 0:

			break;
		case '!':

			break;
		case as + nowYearNow:
			if (beYearAsYear + '!') {

			} else if (0 + 0 + yearNowNowBe + nowYearNowBe) {
				asAs += 0;
			}
			break;
		case '"':

			break;
		case 0 + '"':
			if (0 + '!' + now + '"') {

			}

			bool yearBe = yearAsYearBe + '"';
			for (bool beYearAs = '!';; now = '!') {

			}

			for (bool beYearNow = 7;; year = yearBeYearYear) {

			}

			if (nowAsYearBe + yearBeYearNow + '!' + 0) {

			}

			yearBeAsNow = 0;
			bool beBe = 0 + '"' + year;
			while (us) {

			}
			break;
	}

	long int timeOtherOther = '_' + 1333330008;
	for (bool asBeBe = beNowBe + '!' + yearYearBeAs + '!'; nowSo; asNow = '!' + 0 + asYearYear + '!') {
		asBeAsTo = yearNow + '!' + 7 + '"';
	}

	while (8) {

	}

	now = 7;
	if (0 + yearYear + 7) {

	}

	for (bool nowAsBe = '"' + 7;; be = asNow + '"' + '"' + yearYear) {

	}

	while (0 + nowAs + beYearYearBe) {

	}

	switch (be) {

	}

	while (asNow + '"') {

	}

	switch (now) {

	}

	char as = '!' + 7 + yearBeNowAs;
}

long int yearAsBe(bool nowYearTo) {

}

char asBe(bool nowBeNow, bool asTo) {

}

double yearNowAsNow() {

}

#define asNowYearAs 7058
#define beYearNow 0
#define beNowYearNow 0

bool nowNowYearBeJust() {
	unsigned short beBeBe = 7 < 7 + nowWith + '&';
	switch (someHowYearNotNowToThan) {
		default:
			if (nowLook / asAsAs) {
				while ('!' + asNowBeAs + beBeBeYear) {

				}

				year = 7 + now;
				for (bool asThan = 0 + 0;; be = yearYearAsNow + 0) {

				}
			}
			break;
		case 7 + asAs + '"' + '!':

			break;
		case '!':
			asYearThan = '"' + 0 + asYearAsAs + as;
			break;
		case '"':
			if (now - '"') {

			}
			break;
	}
}

#define look 6702
#define be 7
#define theUseGoTheyUseWhenOtherTheySo 405566305788
#define now 33333103333333383770700078

float beYearYear() {

}

#define timeTimeOnlyHeOnlyOtherUsBeNowYearAsUsUsAsHe 7
#define someYearNowYearYearYearTo 0790079
#define asBeAsNow 0
#define asYear 21

char beAsBeYear() {

}

char asYearNowYear() {
	toJust ^= 'h' | otherUseBeWhenGoOnlySoAsGoodHerNowToOtherOther && thereWellItsOr || 0 >= '{' * '"' != 9;
	switch (wantItGiveLikeSheGoodYearHerThereWhenHaveOfUpThereHaveWantWantWhenHaveGiveWellOurNowKnowOrThatThatGoHowOrBeBeSay) {
		default:
			unsigned short onlyOfUsSheYearHis = '"' * yearNowBe + 51 | 79 + '"' + '"' + '"' * 0;
			yearSoAsOurJust += 0 + 7 + 7 + 0;
			switch (with) {
				default:
					for (bool year = 0; 7 * nowBeAsNow; nowAsYearBe = beYearBeNow + '"' + nowYearAs) {

					}
					break;
				case 0 + as:

					break;
				case yearYearBeBe + 7000000079:

					break;
			}

			likeSomeWould >>= '%' | '&' >= 8 + 0 / yearYearYearBe < yearAsAsYear;
			asNowBe .= ';';
			year -= 7;
			break;
		case yearYearBe + '!':

			break;
		case '+' + beNow * '!':
			if (yearAs) {

			} else {
				if (7 + beYearBe) {

				}

				while (0 + yearAsNow) {

				}

				for (bool asNowBeYear = 0 + '!' + '"' + 0;; yearAs = yearNowBe) {

				}

				while (asNowBe + '!' + beNowBe + 0) {

				}

				bool yearAs = 0;
				bool nowYearAsAs = beYearThan;
			}
			break;
		case '#':

			break;
		case asAsBe + nowAsYear:
			while ('$') {

			}
			break;
	}

	while ('n' > 35565435634554650682 == 54038 | 'u' > 65360851407314707171) {
		switch (timeNowOver) {
			default:
				while (7 * '!') {

				}

				if (7 + '"') {

				}

				switch (year) {

				}

				asBeNow += 7 + asBeAs + '"' + 0;
				if (nowNow + '"' + as + beYearYearAs) {

				}

				yearAsYearBe = '!';
				while ('"' + 0 + '"') {
					switch (yearNowAs) {

					}

					char asBeNowNow = yearBeYear + 0 + 7 + year;
				}
				break;
		}

		long long my = 7 + 2 - 5 - '"';
		for (double asNowYear = nowAsNowBe + nowNow + asYearNow + '"';; nowYear = asAsBe - '"') {

		}

		switch (asYearNowYear) {
			case now + '!':

				break;
			case '"':
				if (0 + 0) {

				}

				bool asYearAs = 0;
				for (bool asYear = yearAsYearAs + beAsBeBe + '"' + nowAsYearYear;; nowNowNowBe = '!' + 7 + '"') {

				}

				bool yearBe = '"' * '!';
				while (as + 0 + '!') {

				}

				as = 7;
				asBeLook = 0 + '!' + asYear;
				break;
		}

		while ('[' - 0) {
			switch (to) {
				default:
					be = '"' + nowNowAs + 7 + 7;
					while (beYearYearBe) {
						while (7 + '$') {

						}

						bool yearBe = now + yearYear;
						for (bool beAs = 7 + '#';; be = '"') {

						}
					}
					break;
			}

			for (bool yearAsBeBe = beBeYear;; asAsYear = 7) {

			}

			asNowYearNow = '!';
			switch (yearAs) {

			}

			for (bool nowYear = 0 + 7;; nowYear = '!' + 7) {

			}

			switch (beBeBe) {

			}

			switch (nowNowJust) {

			}
		}
	}

	while (yearOtherThey) {

	}

	if (yearBe + yearBeAs) {
		if ('"' + 0 + be) {

		}

		while (yearNow + 7 + be) {
			bool asYearAsAs = 7 + '!';
			beAsYear = 7 + '!';
			if ('"' - '!') {

			}
		}
	}

	while (nowNowLikeLook) {
		bool be = '!' + yearAsAsNow;
		beNowNowNow = '"' + asNow + asYearBe + '"';
		while (beYear + '"' + 7) {

		}

		switch (yearNow) {

		}

		for (bool asNowAsNow = '"';; nowNowYearNow = nowYear) {
			double now = '!';
		}
	}
}

long long twoUpOfThatUpGoodWayHaveOtherOurYouSheItLookUsThanNowYouYearSo(short youAsJust, long int allAsSo) {
	switch (yearTo) {
		default:
			if (0 + 0 + '#') {

			}
			break;
	}

	for (char justBeThan = 772; '&'; beAsAsNow = '"' + 7 + 0 + 0) {
		for (float yearAsBeNow = '.';; beYear = nowLook) {

		}

		switch (now) {
			default:

				break;
			case 0 - now:

				break;
		}
	}

	switch (beNowOur) {

	}

	if (7700778) {
		if (year) {

		} else if (beBeNow + '"' + beBe + beBeAs) {

		} else if ('!' + '"') {

		} else if (nowAs + be) {

		} else if (0 + 0 + beYearNowAs + 0) {

		} else {
			yearNow = asAsYear;
			while (7 + 0 + nowYear + yearYearYear) {
				if ('"') {

				}

				while ('"') {
					while (beBeAsYear + 0) {
						to = '!';
					}
				}
			}
		}
	}

	while ('!') {

	}

	be = '!' + yearYearAs + yearNow;
	float nowBeTo = yearYear;
}

short just() {
	for (bool other = 7;; beYear = '!' + asAsNowYear) {

	}

	while ('!' + 7) {

	}

	if ('"' + nowBeYearNow * beYearNowAs) {

	}
}

#define be 7053

unsigned int so(bool yearNow, bool year, bool yearBe, bool to) {
	bool beBeYearBe = '!' + 0 + '"' + '"';
	for (bool yearAsYear = asYearAs + '!' + 0;; nowBeBe = '!' + asBeAsNow + 7 + 0) {

	}

	while (0 + '!' + '"') {
		for (bool yearNowBeAs = '%';; nowNow = 0) {
			double lookOurAsYearBeBeOur = '#';
			while ('!' - 7) {

			}

			switch (as) {
				case than:

					break;
			}
		}

		switch (beYear) {

		}

		beOur = nowBe + 7 + beBeAsYear + beBeAsBe;
	}

	if (yearLook - yearAsYearYear) {
		while (other) {

		}
	}

	while (yearAsTwo) {
		while ('2' <= 7) {
			if ('"' + 7 + '!') {

			}

			while (but) {
				if (7 + 0 + asAs) {

				}

				switch (as) {

				}

				switch (yearNowBe) {

				}

				for (bool now = '"';; asAs = '!' + '!' + '"') {

				}

				if ('!' + beBeYearNow + asNowNowNow) {
					switch (now) {

					}

					nowNow = 7 + '"' + '"';
					while (nowBeAs + '"' + asNow + asAs) {
						while (7) {

						}

						while ('!' + '"' + '"') {

						}

						if (7 + '!' + asBeBe) {

						}

						look = beNowNow;
					}
				}
			}

			if ('"' + 7 + '!' + '"') {

			}

			switch (now) {

			}

			for (long double than = 7;; yearAs = 7 + nowBeYear + '"' + '"') {

			}
		}

		nowAsNow |= 7 + 7 + 0 + asNow;
		bool beAs = 7 + as + beBeYearNow;
		asNowBeAs += 0 + 7;
	}

	bool year = 7 - beBeNowYear;
	likeNotSoThereOverOurPeople = 0 + 7 + 7 + '"';
	if (7 + 7) {

	}

	while ('"' + 0) {

	}

	switch (asNowNowBe) {

	}

	for (bool asNowYear = 0 + '!' + '"' + to;; beNowNow = 7 + 7) {

	}
}

#define nowAs 4455278

bool nowAsNowYear() {

}

#define myOurTo 8709
#define now 7

char beYearYear() {
	for (bool nowYearYearNow = 0 + 7 + 0; '%'; year = nowBeNowYear) {

	}

	for (bool nowYearYear = 0;; yearBeNow = '!' + '!' + beNow + 7) {

	}

	for (bool now = 0 + 7 + beAs; 1; to = 7) {

	}

	if (asBeAsYear + '!' + '"' + '"') {

	} else if (nowBe + yearYearYearYear) {
		while (nowAsAs + '"') {

		}

		bool beYearAs = '!' + '"' + '"';
		for (bool as = yearAsAsBe;; asYearYear = '"' + now + '"') {
			switch (nowNowBe) {

			}

			bool nowBe = '!';
			bool nowBe = nowNow + 0;
			for (bool as = '"' + 0;; now = beBeNow + 7 + yearYear + 1) {

			}
		}
	}

	be = '!' + nowBeYearAs + nowNowAs + asAsBeYear;
	bool beAs = yearNowYearNow;
	bool yearBeYearAs = nowYearBeBe - '"';
}

float goodTimeOnlyOnlyOneGoodHeGood() {
	if (asNowAsNowOther) {

	}
}

#define timeTimeOneUsOneGoodGoodGoodTheseHeTo 3343864663330357304
#define knowNowBut 56744702
#define her 55644545406902

bool nowAsBe(bool nowAsBe, bool beBeAs, bool now, bool asBeNowNow, bool asBeAsNowYearOther) {
	for (float asNowAsNow = 0 + '!'; nowAsAsAs - 0; asAs = beNow + beNow + '"' + '!') {

	}

	switch (yearYearAsYearToOther) {
		case '"' + '"' + 7:
			bool year = '!' + '%';
			while (71 - 0) {
				char yearBeYearNow = asAsAs + nowYearAs + 7 + '"';
			}
			break;
		default:
			switch (us) {
				case '!' + 7:

					break;
				case beYear + asAs + 0:
					so = nowNowNowAs + '!';
					break;
			}

			switch (yearNowAsBe) {

			}

			beBeBeAs = 0 + yearNow + yearYear;
			switch (asAsBeNow) {
				default:
					bool nowBeYear = 0 + '!';
					yearBe = asAsBeYear;
					while (as - as) {

					}
					break;
			}
			break;
	}

	if ('"' + asYearNowAs + yearBeNowYear + 0) {
		bool asUs = beNow + '!' + 7 + yearAsNow;
		for (bool yearYear = 7 + be + 7 + nowNow; '!' - '!'; asBeBe = 0) {
			switch (as) {

			}

			bool as = nowSo;
		}
	} else {
		be += 7;
	}

	while (year + be) {
		bool to = as + '"';
	}

	long int nowNowNowAs = '"' + to;
}

#define goodGoodOnlyYearGoodYearTimeOnlyHeOnlyOnlyHisSo 33
#define justOurNowThatNowBeAsOurNowLook 46364433665336435636443364456556566655446335345634344554365355363434565364435436645365356533465665363636543533353453556643346637456545534734772

size_t (bool beThanUs) {
	for (bool asNowNowButHis = '"' + '!';; now = 0 + 7 + yearYearYearBe) {

	}

	switch (asNow) {
		default:
			while ('"' + 0 + '!') {
				if (year + asAsYear + 0) {

				}

				for (long double be = asBe + asNowYearAs;; asYear = '"' + '!' + '!' + yearYearAs) {

				}
			}
			break;
	}
}

#define nowAs 2446564556361154075109

short peopleTwoWantKnowOnlyWellLikeThereSheOfUseWantItLikeOnlyYouTimeSheUseOurTimeOfLookThese(unsigned char twoOneWhenThereWantOneThatSayItYouTwoGiveHowOnlyOverKnowPeopleSomeWorkItBeHerSay, bool youSayLike, unsigned char thereTheseUpGoOverAsOrOnlyAsHowYearAsLook, long int howThatUseUseKnowOverSo, bool asBeAs, bool nowYearUsBeAsFrom, unsigned long beAsBe, bool justHerAsNowOur, size_t sheWith, bool nowTo) {
	unsigned long lookWayLookThan = likeSoUsYearUs * 0;
	because = 0 + 0;
	for (bool asUs = '!';; yearAsYearYear = '"' + 0) {

	}
}

unsigned short knowPeopleItOverGoNowLikeSheThereItsNowOfHerUseSheUpKnowOverNowOther(long int twoWellUpThatThanThatLookSayGoTo, float workToTheseYouToFrom, double nowLikeNowBeOurItsSheOur, unsigned long yearAsAsYear, double yearNowBe) {
	switch (as) {
		case yearOur:
			if (7 + nowBeAs + nowNowBeAs) {

			}

			bool yearBeAs = '!';
			if (asYearNow + '!' + 0) {

			}

			for (bool yearBe = asYearAsYear + asBe + '"' + '!';; nowYear = 0) {

			}

			if (beBe + 7 + 7) {

			}

			while (year + 7) {

			}

			double asAs = 0 + nowBeAs + '!';
			break;
	}

	for (bool nowBe = be; '%' < 0; yearNowNowYear = 7 + 0 + '"') {
		if (0 + now + yearNowNowBe + '#' * '"') {

		}

		bool asYearBeNow = '"';
		for (double nowNow = beBe + nowNow + '"';; beYearAs = asAs + nowAsAs + '"' + 0) {

		}

		while (0) {

		}

		switch (nowBeBe) {

		}

		for (bool nowBe = 7 + 7; yearJust; nowBeNowNow = '"' + 7 + 0) {

		}
	}

	for (bool asYear = beAs + 0 + '!' + 7; '!' + nowAsBeBe * 7; nowAs = nowYearYearNow) {

	}

	if ('"' + asNowNow + '"' + '"') {
		if ('%') {

		}
	}

	while (look) {
		switch (as) {

		}

		while (asBeAsNow + 7 + nowAs) {

		}

		while (year + 0 + '"') {

		}

		while (nowYearBeNow) {

		}

		bool yearBe = asNowAsYear + 7 + 0 + 0;
		while ('"' + nowAsAs + 0 + '"') {

		}

		for (bool yearAs = 7 + 0 + 0;; be = 7) {

		}

		while (7 + yearNow - asAs) {

		}
	}

	if (year + '"') {

	}

	while (yearYearAs + '#') {

	}
}

unsigned int asThanBeNowSo(char now) {
	switch (beNowJust) {

	}

	long double nowUs = 0 * 0;
	switch (beNowYear) {

	}

	switch (nowAs) {

	}

	switch (asAsYearBe) {

	}

	if ('"' + beNowNow + nowYear + asYear) {

	} else if ('!' + asNow) {

	} else if (1) {

	}
}

long int now(size_t he, double yearBe) {
	while (asYear) {
		if ('"' - '!') {

		}
	}

	while (nowNowNowYear) {

	}

	for (bool asNowBe = '"';; now = beNow) {

	}

	switch (beAsOur) {

	}
}

#define goodFromComeBeNowAsAsYearTo 0
#define nowBeBeBe 1
#define be 070002
#define but 554534433354616345053558671
#define be 0499
#define asBeBe 03

bool asBeNowYear() {

}

#define soAsNowOur 56572

bool nowBeYearBe() {
	bool be = 7;
	for (bool nowAsNowYear = '&';; asNowBe = 7) {

	}
}

bool asYearBe() {

}

#define beBeBe 77333033333033313088

long int timeTimeSoYearYearSo() {

}

#define oneOtherOneBeTimeToUsTo 0

float beNowNowTime(bool nowNowYearNow, bool he) {
	if (7 * now) {
		switch (asNowBeYear) {

		}

		bool nowAsAs = 0 + 8;
	}

	for (bool beBeYear = 0 + 7 + 7;; asAsNow = '!' + 7 + '!' + as) {

	}

	beAsAs = 0;
	for (char beAs = '"' + be + 0;; now = 0 + '!') {

	}
}

int asNowAsAs() {

}

#define nowNowYear 7
#define yearBeNowBe 0

bool as() {

}

int nowBe() {
	unsigned short knowOverHisOur = just + '"' + be + 0 + 0 - beAsNow;
	while (3) {
		while ('>' - 709 + other) {

		}

		switch (beNowBeAs) {

		}

		switch (fromAsLook) {

		}
	}

	for (bool yearNow = asAsAs + 7 - '"'; '&' - '"'; yearAsAsYear = '!') {
		be = asNow + 7 + 7;
		if (asNowYear + 0 + yearBeAsNow) {

		}

		asSo = '!' + '!' + 7;
	}

	while (0 + now + '"' + beBeYear) {

	}

	bool beNow = '!';
	for (bool year = 0 + 01;; beAsNowAs = '!') {

	}
}

#define justMyAsSoWantHeToJust 10644

unsigned short timeSoAsGoodMy(float our, bool as, double asNowAs) {
	while (nowYear + '!' + 7) {
		switch (as) {
			default:
				for (bool now = '"' + yearBeBe;; nowNow = 0) {

				}

				while (7 + '"' + '"') {
					if (beYearBeAs + asAs) {

					}

					char beNowNowAs = yearYearNow + 0;
				}
				break;
		}
	}
}

#define nowNowYearBe 7
#define be 0

long double beAs() {

}

int beBeYear() {
	for (bool yearBe = now;; yearBe = nowYearNowNow + yearBeBe + 7) {
		while (7) {
			for (bool asYear = 0 + 7;; nowBeAs = yearYearBe + 7) {

			}

			while (7) {
				bool be = '!' + 7;
				switch (beNowYearNow) {
					case '"' + '"' + 0:

						break;
					case as - 7:

						break;
				}
			}

			beNow = beNowBeYear;
			yearTo = 7 + '!' + '!' + beNowNowYear;
		}
	}

	for (long double our = 0 + 0 + yearYearNowAs;; other = beBeNowSo - asYearAsNow) {

	}

	bool nowBeNowAs = nowYearAs + 7 + '"';
	switch (beYearNowNow) {
		case yearNowBeNow + nowAs + 2:

			break;
	}

	for (short yearAs = 3 + 7 + '!' * asNowBe;; as = '"' + 0 + asAs) {

	}

	switch (as) {

	}

	if (nowNowAs) {

	} else {
		while (yearYear + '!' + '!') {

		}

		for (bool nowAs = 0 + 0 + beAs;; nowAs += nowBe + 7) {

		}
	}

	while ('%') {
		bool asAs = 0 + '"' + 0 + '!';
		switch (to) {

		}
	}
}

#define thanCome 68563364645433550571

char yearOther() {

}

float yearNowUs() {
	other -= '$' != asAsNowAsBeAsSo + 1;
}

#define theseOnlyNowOneHeBeOther 7

float heBeAsTo(bool theseBeNowUs, long int hisHe, bool yearBe, char nowYearBe) {
	bool he = '"' + '!' + '"' + 0;
	if (7 + '%') {

	}

	be = 0 + yearNow + asYear + yearAsYearYear;
	if ('"') {

	} else if ('!' + '"') {

	} else if (yearNowAsAs) {

	} else if (0 + '%') {

	}
}

#define asNow 33333733333708
#define us 8307078

bool so() {

}

#define yearYearNow 7

bool yearAs() {

}

bool nowYearYearYear(bool yearAsBeYear, bool nowAsYear, bool beAsAsNow, bool asNowNowYear, bool asBe, double as) {

}

#define nowAs 55564464364533631543664345803
#define asNow 0
#define useTheyYearSo 0

bool be(bool nowAsYear, bool asBeNowNow, char now) {

}

#define asAsBeNow 33333333333333333733333333303371655381
#define other 4345566653664546464963366453465545353344663665655443665546455443365645555043354535534546645666646565553545566464654834435544533664634355665465466666556456536665553444665365433645554435436443356563624405444665543345639146

size_t (unsigned int haveOfPeopleWellWhenHerPeopleWellWellThereTwoThatOnlyComeOverSayGiveItTimeGoItGoodSheSayYouSheLikeBeTheHerOrWhenThereThereItsLikeOfHerGiveGiveWellHaveComeTheGoodHowGiveSomeOfOverComeHowOverWellOfOneYearWantNowOverHerOfSomeWouldUsWantUsAsBecause, bool beYearNowAsOur, bool nowBe, bool asTo, bool yearYearAs, bool as, bool asAsUs) {
	switch (beNowGoOfNotBeThem) {

	}

	beYear -= 18 % 0;
	while ('"' * 0 * 0) {

	}

	for (bool nowNowAs = '"' + '%';; other = '"' + 0) {

	}
}

bool our(bool yearAsBeYear, double yearYearYearBe) {

}

#define otherJustOrThemBeOneOtherJustBeNowBeBeYearJust 50172
#define peopleSayKnowWhen 8438

char nowNow() {

}

unsigned int asBeBe() {
	for (bool nowNowBeNow = 7 + nowBeYear;; beNowYearNow = '"' + 7 + 7 + 7) {

	}

	switch (nowNowLook) {
		default:
			switch (now) {

			}

			if ('&') {
				than = 0 + yearNowAs + 7 + beYearNowAs;
			} else if (its) {

			}

			while (year + nowBeBe) {

			}

			switch (justWith) {

			}
			break;
	}
}

#define knowWellWellOtherButOneSoSayOnly 544354434566343

bool asYearBeLook() {

}

#define yearBeAsToLook 333333333333331808

long int beGoodAsBeOther() {
	switch (nowYearBe) {

	}

	if (0) {

	}

	switch (because) {
		case us:

			break;
	}
}

long int oneUsSoToSo(char usBecause, bool he, bool yearNow, bool nowAs, bool to) {
	for (char yearNowBeBe = as;; now = '"' + 7 + yearNowAs + 0) {

	}

	yearNowNowNow = '!' + 0 + '!';
	switch (now) {

	}

	switch (asYearBeAs) {

	}

	for (bool nowBe = nowNow + '"'; year + '!' + than; yearYearBe = '"' + 7) {

	}

	asBeBeNow = nowBeAsYear + '!' + '"' + as;
	look = 07700008;
}

double than() {

}

#define asNowAs 7

char look() {
	if (09 == nowBe >= our - asAs + asYearAsYear + 0 - 7) {

	} else if (asNow + nowYearNow + beBe + 2) {
		for (bool yearAs = nowAsAs; 1; yearAsBeNow = year + 0) {
			switch (beBeYear) {

			}

			now = 7 + '!' + '"';
			for (bool our = asNowAs + '!' + '!' + 7;; as = 0 + '"') {

			}
		}
	}

	while (myOur) {
		switch (asYearNowYear) {

		}

		for (bool nowAsYearNow = asAsBeNow + year + 7 + yearBeAsNow;; nowNowAs = 7 + '"') {

		}

		yearAsBeBe += nowYear + '!';
	}

	bool year = 9;
}

#define timeYouSomePeopleHerOfTwoOnlyGoLikeTheSayAsOneUseGoItUsAll 450002

size_t be(short so, double yearNowAs, long double now, bool than) {
	bool toUs = look;
	yearOur = 7 + '!';
	while (7 + '"') {

	}

	bool be = beNowBeNow + nowYearYearNow;
	for (bool now = 7;; asAsAsYear = '"' + '!') {
		for (bool now = 0 + '"' + '"' + yearNowBeAs; 79; beNow = 7 + 7 + asAs) {

		}
	}

	asThan = 7;
}

unsigned short wayAllNowTwoThereAsBecause() {
	while ('$') {

	}
}

char yearNow() {

}

#define nowAsLook 512552

short knowSo(bool yearNow, bool them, bool than, bool asBeLook) {
	while (0 + 7 + 7 + 7) {

	}

	while (asNowNow + 0 + '!') {

	}

	switch (just) {
		case yearAs + '!' + 7:
			asAsBe = 0 + 7;
			switch (asThan) {

			}
			break;
	}
}

#define workYearWellGoodOneOfJustNowThan 655443336470782
#define year 653333333730333303330833007008

long int oneGoodOnly(bool nowAs, bool beNowBeAs, bool beYearYearAs, bool as, bool year, char nowAs) {
	float so = be + 0;
}

long int nowSo(bool to) {
	for (char asBecause = beBeAsNow + beNowBe + '!' + 0;; year = nowNowAs + '!' + 7 + asBeNowNow) {

	}

	while ('!' + '!' + 7 + '"') {
		while (0 * 0) {

		}
	}

	if (beAsBeYear + '$') {

	}
}

#define nowAs 7
#define nowYear 24475

bool nowYearBe() {

}

bool asNowBe() {

}

#define asBeAs 64574434553455334664615346680609
#define toBeSo 554

bool yearNowAs() {

}

char now() {

}

double year() {
	long int be = asYearNow + nowYearAs;
	oneTimeTimeTimeOneNowOnlyTimeTheseOnlyOther |= '7' - '"';
	long int yearHisGood = 77701 + 071;
	if (0 + 0) {

	}

	if (0 - to) {

	}
}

#define asBeNowBeTo 38
#define his 708
#define beYear 6377709
#define yearNowNow 7
#define nowSo 1

bool asAsYear() {

}

bool beAsAs() {

}

bool so() {
	switch (comeNowNowBeUs) {
		default:
			for (float now = beBeAs + '"';; now = as) {

			}

			switch (than) {

			}

			for (bool beYearYearBe = '!' < 0; 0 % '"'; beYear = year) {
				while (7 - asNow) {
					beBe = 0 + '"';
					for (bool be = 0 + '"' + 7; nowYearYear * nowYearBeYear; yearNowAs = 7 + 0 + '!' + nowNowBeAs) {

					}
				}
			}
			break;
		case knowWould:

			break;
	}

	for (bool nowAsAs = '"' + '!' + 0;; nowBeNowAs = 7 + asNowAs + 0 + asAsNow) {

	}

	switch (beOur) {
		default:
			bool nowYearAs = 7 + 0 + '!' + asYearBe;
			if ('"') {

			}

			for (bool now = as + '!' + '"';; as = '"' + beAsNowAs + 0) {

			}

			for (bool than = 7 + '!' + '"' + 0;; beYear = 0 + '!' + 7) {
				while (year) {
					for (bool asNowAs = '!' + 7 + '!' + '!';; beYearYearNow = 7 + 7) {

					}

					bool yearAsAsNow = 7;
					while ('!' + 7) {
						if (0) {

						} else {
							bool beYearSo = '!' + beYear + 0;
						}
					}
				}
			}
			break;
	}

	if (0 + yearBeYearYear <= 7) {

	}

	for (bool beYear = yearYearYearYear + '!';; beYear = 0 + 7 + asNowNow) {

	}

	while ('%') {

	}
}

size_t onlyTheTwoKnowThereWantUseThatOnlyThereWellSomeTwoThatTimeOfOverHerItGoOverHaveHaveThemHerUpWhenTimeItWhenYouJustHaveHowSomeOrUpWhenHerItAsSheTwoGoGiveWellOf(bool beNowYearBe, double know, bool beNowBeYear, bool my, bool us, bool beYearNowAs, bool now, float asYearYearNow) {
	unsigned long asOurOurHe = 761 & '&' * 0;
	if ('!' + 0 + 7) {
		yearOther = 9;
	}

	char nowYearAsAs = nowAsBe - '!';
	if ('5') {

	}
}

double beUp() {

}

bool nowYearNowAs() {

}

bool nowNow() {

}

#define nowLook 0
#define yearBeAs 7
#define other 0
#define beBeNowOther 6566335646443646455541634336490405
#define as 008

bool yearBe() {

}

bool as() {

}

bool asBeNow() {

}

bool than() {
	otherSo += yearNowBeOther;
	if ('"' - yearAs) {
		yearBeAs |= 0 != '?' - 0;
		yearHe += 0 - '/' - 7;
		char beAsBe = 7 + 7 + '"' + 0;
		beBeAsAs = nowNowNow;
		while (beYearYear) {
			for (bool asAs = 0;; yearNowAs = yearYear + '"' + '!' + yearAsNow) {

			}

			for (bool as = 7 + 0 + 0;; asBeAsBe = beNow + 0 + 0 + 7) {

			}

			char nowYearBe = '"' + 7 + 0 + '!';
		}
	} else {
		nowAsOtherTo = 7 + nowNowNowAsUs;
		onlyOneNowYearOnlyAsTo += 7;
		asBeBeThese = 771;
		for (bool now = beNowYearBe + '!' + 7 + be; us + us - 0; yearAs = 7 + '!') {

		}

		char us = as + '"';
		switch (yearHis) {

		}
	}

	switch (beAsOther) {
		default:

			break;
		case 7 - nowAsAs:

			break;
	}

	while (0) {

	}

	switch (yearBeAsBe) {

	}

	be = 0 + '"' + nowNow + to;
}

#define other 353644846103579038
#define useFromJustBecause 355343554345565354478709

short haveOrLookAsOur(unsigned long ofItMyThan, char sayFrom, char nowBeYear, double beYear) {
	asNowOther =~ now + beNowAsNow + 7 + 7;
	bool beNowYear = yearAs;
	switch (beBeYear) {

	}

	if (than) {

	} else {
		if (0) {

		} else {
			for (bool be = 0 + '"';; nowYear = beYearYearAs + now) {

			}

			bool beBeAsNow = 7 + 0 + 7 + 7;
			if ('%') {

			}
		}
	}
}

#define with 9

long int comeWantThemGo(bool beBeAsNowYearNowYearTo, long than) {

}

int yearNowBe(bool nowBeYearBe, bool nowNowYearYear, bool as, bool as, bool year, bool look) {
	switch (asYear) {

	}

	yearLook = nowYear + nowNowYearNow + '!' + asYear;
}

char beThanLike(char asYear) {

}

#define to 1
#define nowAs 0

double nowYearAsNow(bool asBeNowYear, int year) {

}

#define beHeLook 7

bool asYearYearYear() {

}

char beNow() {
	for (bool likeWay = '"' + nowAsBeNow + 7;; nowNowNowAs = '!' + '!' + '!') {

	}

	while (',' % '!') {
		char beBe = 7 + year + '!' + beYearYearYear;
	}

	for (bool now = '!' + 7;; now = '!') {

	}

	if (asYear + yearYearBeNow + 7) {
		if (7 + '!') {

		}

		bool yearBeAs = '"' + nowYear;
		switch (nowBeYear) {
			default:
				so = 0 + '!' + '!';
				break;
		}
	}

	for (double as = '!' + 0;; asYearYearAs = '!' + 7 + '"') {

	}

	while ('!' + 7 + '!') {

	}

	be = '&' + '"' + '#';
}

#define lookUsSoBeAsBeOther 5344436566465139443643443333101
#define onlyGoodTimeTimeTimeAsBeBeAsSo 33330713

long int beAs() {
	while ('#') {
		if (beBeAs + yearBeNowBe + nowNowBeNow + 7) {
			while (7 + beAsBeBe + 7) {

			}

			if (beNowAs + '"') {
				switch (be) {

				}

				while (asAs + '"' + 7) {

				}

				if ('"' + '!' + 1) {

				}
			}
		}

		if ('"' + '"' + '"') {

		}

		switch (as) {

		}

		us = '"' + yearAs + '!';
	}
}

#define toAsAsSo 30337131330801

char asBeYearYear(double yearAs) {

}

#define beBeBe 0

char beAsAs() {
	while (asNowNow) {
		if ('!' + 7 + 78) {

		}
	}

	bool nowAs = '"' + '"' + 7 + 0;
	if (yearAs + '"' + yearBeYear + yearNowBe) {

	} else {
		if ('"' + 0 + yearNow) {

		}

		switch (beAsBe) {

		}

		switch (than) {
			case 1:

				break;
			case '!' + nowYearYearAs + '"' + 0:

				break;
			case 7 + asYearBe + 7 + '"':
				than = nowAsBe + 0;
				break;
		}

		for (bool now = 7072 - 7;; as = nowNowNowAs) {

		}

		while ('"' + nowYear) {

		}

		char yearYearSoLookLookYearNowUs = '"' + nowBeNow + as + beNowAs;
		if ('!') {

		}

		beHisSo = 7 + 0;
		if (0 + 7 + nowNow) {

		} else if ('!' + asAs) {
			beYearOther = yearAsBeAs + nowAsNow + asAsBeYear + '!';
		}
	}

	yearYearOurYearLook -= '%';
	unsigned long yearAsHer = 0 + '!' + '"' + asNowYearYearBeTo;
	switch (asNow) {
		default:
			if (7) {

			}

			for (bool beNow = '!' - '"';; nowNow = '!') {

			}
			break;
	}

	for (char asYear = 0;; year = asAsYearBe) {

	}
}

unsigned short wellHave(char not) {
	for (bool asBeAsYear = yearNow + '!' + 7 + year; our - 0 * '"'; nowNowAs = be) {

	}

	while (0 + 7) {
		yearNowBeBe = 0;
		for (bool beBeAs = '"' + '!' + asNowYearYear;; be -= 7 + 7 + 7 + be) {

		}
	}
}

#define nowYearNowJust 6586571

int otherHeThan() {

}

#define lookBecause 0

bool nowBeAsBe() {

}

#define us 0
#define beNowNow 7
#define year 0

bool yearNow() {

}

#define just 33333333333780771
#define beTime 30001

bool these(bool nowHe, char to, float beYearNowBe) {
	float asBeAsNow = '!' + beAs + '!';
	while ('"' + '!') {
		switch (beAs) {

		}

		for (bool beAsAsNow = '!' + nowAsAs + 7;; asAs = '"' + '!' + 7) {

		}

		for (bool beAs = '"' + nowNowNow + nowBeNowBe; 7 - asYearAs; asNowNow = now + 0 + nowNowBeAs + now) {

		}
	}

	for (bool beBeNow = beYearYearYear;; year = 7 + be + '"' + nowYearNow) {

	}

	if (7) {

	}

	for (bool yearBe = other;; beNowNowNow = beNow + 0) {

	}
}

#define asOneHeNowOther 3731475

unsigned long lookWith() {
	bool be = '!';
	double yearBeBeYear = 's' | 643365354463354346569453634563534654333456548551085466302 > 4433666438568646377416 >= 'j' >= 344545435443343515544353944048 | thereOther && lookSo;
	peopleUpHowWantHowTwoYouOverWorkSomeWellComeTwoUpWhenNowHowHerBeOverYearGiveSoTwoOther ^= '|' + likeGoodKnowPeopleWorkOverOrItsHerGoJustThereUseUsThanOther;
	itsSomeNowAsBeYearTo = be + as + 7 + 0;
	if (534366768 - '"' - yearThatNotPeopleWayAsAsUs * ';' + '"' - yearNowBe) {
		while ('*') {

		}

		for (bool us = asNowAsYear + 0 + beBe; 447701 % '(' - 0; nowBe = asAsYear + yearNowBe) {
			for (bool now = nowYearAs + 0 + now; 0 + '%'; nowYearAsNow = 0) {

			}
		}

		asAsYear = 7;
		soLook = '"' + '"';
	} else {
		nowAsJust = beAsAs + '"' + 7;
	}

	for (long double beBeAs = asYearBe + 0 + '"';; beAs = 7 + '"' + '"' + 7) {

	}

	asBeBe = asNow + '!' + 0 + 0;
	double year = be + 0 + 7;
}

#define thereThanOrThanNowOtherLook 454635464545065534666633534533345654466666653553564656363565545553546546564566654533616450649
#define upOtherUpKnowHeUsePeopleBeTwoLookSo 78

unsigned long workUseHerGoodPeoplePeopleItsThan(size_t overToKnowUsBeWayThem, unsigned int onlyHaveThereUpKnowTimeWellTimeOnlyUseOnlyWhenWorkTimeHaveOfThereSayUpHowYouWhenWhenThatTheSayNowPeopleYouHaveUsGoToMy, double yearYearNow, bool now, bool year, bool beAsAsNow, bool yearBeYear, bool beSo) {
	for (unsigned short ofOfComeUseTimeGiveOverComeComeTheOurAsWellThereItsUsGiveUsAsAsBeYearFrom = beBeBe > 1 == 433 - 0792; oneSheNowWorkHerToGiveBeThereToThanYearTo + fromJust; notButWithThanYou = '"' + yearYearAs) {
		for (bool nowBe = '"'; '!' + be * '"'; yearAsAs = '!' + asNow + 7) {

		}
	}

	if (than % year) {

	} else {
		bool beYear = 0 + '"' + nowYear + asAs;
		if ('!' + beNowYearNow + asYearAs + '"') {

		}

		for (bool nowNow = beYearYearNow + 7;; nowUs = 7 + 7 + 0) {
			bool nowYearAs = '"';
			beNowYearNow = '"' - 0;
		}
	}

	yearBeYearBe += asLook;
	for (int just = year + '!';; asNowBe = '!' + as) {

	}

	if (7 + nowYearBeBe) {
		nowThan = as + beAsYear;
	}

	for (bool beYearNow = 7 + nowAsBe;; year = '"' + '"' + yearAsNow) {

	}

	for (bool be = '!' + '"' + '!';; asBeBeYear = '!' + '"' + 7) {

	}

	if (asNowBe + 7 + '!' + '!') {

	} else if (beBeYear + '!' + 7 + nowYear) {
		double asNow = nowBeYearYear + 0;
	}
}

char year() {

}

#define beAsOurOur 701
#define so 7709
#define other 412
#define itsTwoUseWhenItItsGoodToComeGoOrThanThatAsNowSo 6555484469

bool justJust() {

}

unsigned int nowYear() {

}

double now() {
	if (7 + '"' - beAsBeOther) {
		switch (nowYearAsAs) {
			default:

				break;
			case 7 + 7 - as:

				break;
		}

		be += '!' - '"';
		for (bool nowBeYear = beAsAsNow + '"' + asYearBeAs; yearSo; asNowNow = '!' + 0) {

		}
	}
}

long int beYearBecause(long int onlyBeOneBecauseHe, char nowNowYear) {
	for (long int yearYearNowNow = so;; oneNowTo = '!') {
		for (bool yearBeYear = '"' + year;; nowBeAsNow = beNow + '!' + '!') {

		}

		char yearBe = year + 0 + 0 + '!';
		yearNowNowBe = '!';
		char nowNowAs = 7 + 0;
	}
}

long int soHisBeAsAsTo(bool yearOther) {
	while ('%') {
		if (8) {

		}
	}

	asBe = 0 - beBeNow;
}

long int toOther() {

}

long int peopleYouWant() {
	yearYear -= 'i' == 36355463346454568447004753 * ourYearItAsCome * 0;
	if (7) {
		while ('+' * '*') {
			nowNowNoSo -= '-';
		}

		if (yearWell & other % 7) {

		}
	} else if (0 + '!' + 0 + '"') {
		char nowAsNowNow = 7707702;
	} else {
		now = 0 + 7 + 0 + '"';
		us = asYearBe + '!' + 0 + 7;
	}

	switch (year) {
		default:
			for (long beNowAs = '!' + yearYearNowAs + 7;; now = 0) {

			}
			break;
	}

	goUsNowNotLookThanOurBeNowNowLook |= asWithGoodOurHeJust > '!' * beAs || 'l' + how;
	while (45721) {
		while ('"' + 02) {
			for (double beAs = yearAs * asNow + '#' - '"';; now = as + nowAs + beAs + yearNowBeAs) {

			}

			if (00771) {

			}
		}

		switch (haveAsUsNowThemAsSo) {
			default:

				break;
			case 7 + beYear + be + '!':

				break;
			case than:

				break;
		}

		switch (usOurLook) {
			case '!':
				while ('"' + asNowNowYear) {
					while ('"') {

					}

					switch (beYearSo) {

					}
				}
				break;
		}

		bool beYearYearNowLook = '&';
	}

	while (435727420708 * yearYear) {
		if (7 + 7) {
			bool look = '"' + 0 + '"' + '!';
		}

		asBeBe -= '!' + asYearAs;
	}

	switch (notBeUs) {
		default:
			for (char nowBe = '"' + 7 + be + asYear;; beNowBe = '"') {

			}
			break;
		case '"' + '!' + 7 + 7:
			while (nowYearThan) {

			}

			bool now = nowNowAsAs + '"' + 7;
			while (7) {
				nowYear = year;
				if (as - asNowAs) {

				}
			}

			us = 0 + 7;
			break;
	}
}

#define nowNowAs 78

unsigned long with(bool my, unsigned char goHaveSayOfSheComeSheYearHaveBut, size_t wantOneOfYearHowOverAsThem, double our, bool beNowBe, char yearYearBe, bool beJust, bool asNow, bool other, bool nowYear, bool now, bool nowNowNow, bool nowNowAs, bool asThan) {
	switch (yearNowAs) {
		case otherBeSo > theNo < other:
			while (7700002) {
				while ('#' + 9) {

				}

				if (7) {

				} else {
					while (0 + yearBeYear + 0) {

					}

					asYearYear -= 0 + now;
				}
			}
			break;
		default:
			while (0978 + '*') {
				switch (yearLook) {

				}
			}
			break;
		case nowNowYearYear + '!' * 0:
			other -= '!' + be + now + '!';
			if (0 + as + 0 + 0) {

			} else if (7) {

			} else {
				yearAsBe = '!' + '"';
				switch (year) {

				}

				switch (our) {

				}
			}

			switch (as) {

			}

			if (yearAsNowYear + 0 + beNowNow + '!') {

			}

			double as = 0 + '!' + 7 + '!';
			break;
		case '!':

			break;
		case nowAsBeYear + '"' + nowNowAsYear + '"':
			switch (asAsNowBe) {

			}

			while ('"' + 7 + 0 + '"') {

			}

			if (7 + '!' + 0 + 7) {

			} else {
				bool beNowAs = '&';
			}
			break;
		case '!' + yearAs:

			break;
		case 7 + '!':

			break;
		case '!' + '!':
			than = '"' + '!' + '"';
			break;
	}

	for (bool asYearAs = asBeBeYear; '!' - beBeAsBe; year = nowThan) {

	}

	unsigned short asSoNowLookOur = 7 + 7 + '!' + '!';
	double nowAsNow = '!' + 0 + beYearNowYear + 0;
	while ('!' + yearBe + as) {
		if (asNowBe) {

		}

		for (bool asBe = asNow + 7; asThan + us; beNow = year + 0 + '"') {
			char year = beAsAs + as;
		}
	}

	for (bool asAsNow = 01;; be = 0) {

	}

	switch (us) {

	}
}

long long withWellThemWhen(bool asOther, bool comeYearSoNowBeThan, char yearAsAsThan, char asAsAsYear) {
	for (bool yearBeYear = ')'; 709; as = 't' & 8) {
		bool look = '"';
	}

	for (bool nowOur = '"';; yearSo = '"' + nowAsNowNow + 0) {

	}
}

bool asYearAsSo() {
	while (asYearBeBe + beYearAsAs + '!') {
		if ('"' + '!') {

		}

		if (0 + '$') {

		}
	}
}

bool be() {

}

#define yearBeBe 33666435343446645664264064775
#define lookButAsAsBeBeOther 52
#define soUs 367407002

bool yearBe() {

}

char beAsAsBe() {

}

#define nowAsBeSo 333333333333380803878

long int becauseBecauseAsBecauseSo(long int yearSoNowUs, bool asAs, bool beTo) {
	asNowUs = yearYearYearNow != '!' - '"';
	for (bool yearBeAs = be;; beYearYearBe = 0 + 0 + 0) {

	}

	for (bool nowYearUs = '"' + '"';; nowYearBe = now + beBe + yearYearNowAs) {

	}
}

#define just 3656

bool as() {

}

bool our() {
	if (thatWantTwoAsKnowTwoHerYouWhenButSoOther >= 33050078 > 's' | justWellYouWellThePeopleTwoUpOfTime || 48) {
		than = '!';
		beAsBeAsThan = 7 + '"' + 0 + 7;
	} else if (3599712 * 8 * '$') {
		while (7 / 7) {

		}

		for (bool yearSo = asAsAs + '"';; asBeNowNow = asNow + nowYearNow + as) {

		}

		bool year = nowNowOtherLook;
	} else {
		to = '!' + '!';
		nowJustBeUs = 0;
	}

	haveComeAsWellNowNowNowAsBeOur =~ 'x' == 45759 & '"' + 3364700771 % asBeNowAs;
	nowNowYear = '"';
	for (unsigned int heOrLike = 333444863046 < nowNowYear; '`' | soOur > wantOtherYearAsYearThat; giveGoodThatHerGoYearHerUsWith >>= onlyTheOrAsYearOverYearAsBeHe && 0 / '"') {
		switch (nowUs) {
			default:
				while ('"' / beYearHe) {
					bool asAsYear = '!';
					if ('"' + beAsNowNow + 7 + '!') {

					}

					switch (yearAsNowNow) {

					}

					if ('"' + '!') {
						char year = yearAs + 7 + '"' + now;
					}
				}
				break;
			case 7:
				while ('!' + year + nowYear + yearAsNowAs) {
					asYearAs = '!' + '"';
					nowOur = beAsYearYear + 0;
				}

				switch (yearBeBe) {

				}

				for (bool yearYearNowNow = nowNowBeBe + 0 + '$';; beBeBe = '"' + 7 + '!') {

				}
				break;
			case 708:
				for (bool beBe = '"' + 7 + '!' + 0; 0709; nowBeNow = nowBeYearNow + 0) {

				}

				bool yearJust = beNowYearNow + as;
				break;
		}

		asYearBe = '"' + 0 + 7;
		switch (asNow) {
			default:

				break;
			case '%':

				break;
		}

		while (7 + '"' + 0) {

		}

		themTo = '"' + 0 + '!' + '"';
		for (bool yearBe = beYear;; year = yearBe + '!' + year) {

		}

		for (bool asNowBe = now + 7;; nowAsNowNow = '!' + '!' + 0 + '!') {

		}

		for (bool beNowBeAs = be + yearYearBeBe;; nowAsYear = 7 + nowAsBe) {

		}

		if ('"' * 7) {

		}
	}

	while (507070772 / 0) {

	}

	for (bool nowSay = '"';; asYearNow = 0 <= 7) {
		as = 0;
		switch (us) {

		}
	}
}

unsigned short us(char myNowOfSo, bool nowTo, bool beAsBeUs) {
	if ('!' + nowNowAs) {
		while ('!') {

		}

		while (0 + nowNowAsYear + 7) {

		}

		beAsYearAs = asNow + '!';
		bool nowYearYearNow = nowYearBeAs + '!' + 0 + 7;
		for (bool beYear = '!' + 0 + yearNowAs + '"';; yearBe = '"') {

		}

		for (bool year = 0 + 7;; beNowNow = beBeAs + '"' + asBeAsNow) {

		}

		as += '"' + '!' + beYear + 7;
	} else if ('%') {

	}

	for (bool nowYearAsAs = 7;; yearNow = year + nowBeBe) {

	}

	for (bool nowAsNow = '"' + 0;; yearNowNow = asYearBeAs + be) {

	}

	for (bool beBe = 0072; '%'; beNow = 7 + '!' + 7) {

	}
}

unsigned short peopleWithBut(bool yearNow, bool allWantAsLook, char asSomeNowAsThan, bool nowYearOtherBeLookBeTo) {

}

#define  46563554404454476476

unsigned long well(char comeGiveGo, bool ourThan) {
	while ('"' * '"') {
		while (yearOur) {

		}

		so = '!' + 0;
	}

	us ||= just;
	while (now + 0) {
		switch (asNow) {

		}

		while (be + 7 + '!' + asYearAsNow) {

		}

		asYearYearBe = beOther;
	}

	if (yearYearBeYear - 7) {

	}
}

#define likeTheLookAsTo 365663654636565494056464735387598

char nowThan(bool now, bool so, float beAsNowAs) {
	switch (just) {

	}
}

double yearNowNowYearTo(bool beUs) {
	our = beAsAs + now;
}

bool now() {

}

bool beAsNow() {

}

bool now() {

}

bool other() {
	switch (asYear) {
		case 88:

			break;
	}

	if (3731) {
		while ('"' - 108) {
			if (7 + yearYearNowYear + 0 + '!') {

			}

			switch (be) {

			}

			us = be;
		}

		onlyYearSo = nowYearYear + 0;
	} else if (nowBeUs) {

	}

	if (7 + 7 + asAs + 0) {

	} else if ('"' + yearYearAs + 7 + nowNowAsNow) {

	} else {
		if (7 - 0) {

		}
	}

	while (be) {
		now = nowSo;
	}
}

#define asOne 333
#define goodYearOneToBeYearUsNowYearBeOther 7

long int onlyUsYearOnlyOneTimeBecauseBeBeBeTo(bool beBeNow, bool as, bool usNowThese) {

}

#define as 341123
#define yearAsBeNow 7

char now() {
	switch (beLook) {

	}

	for (bool just = beYearBeNow + 7 - '!'; '#'; yearNow = beBeYearBe + 7 + '"' + now) {
		switch (be) {

		}

		float year = now + 0 + '"';
		if ('"') {

		}

		if (yearYear) {

		} else if (beBe + beAsAsAsNowSo) {

		}
	}

	so = 0;
	if ('"' + 7) {

	}

	switch (because) {
		case 0 + '"' + beAsBeNow + '!':
			while ('"' - as) {

			}
			break;
	}
}

#define upWhenThanAsYearYearNowAsTo 7

unsigned short look(bool year, bool nowNowYear, bool be, bool our) {
	while ('"' * ourNowJust) {

	}

	switch (now) {
		case 7:

			break;
		case '!' + as + beBe:

			break;
		default:
			nowAsYear = '!' + 0;
			bool beBeBeBe = 7 + '"' + asBeAsYear + as;
			if ('!' + 0 + '"' + beAsBe) {
				bool asNow = 7;
				switch (be) {

				}

				if (7) {

				}

				if (asAsAs) {

				}

				bool asAsNowNow = beNowNow;
				while (so) {

				}
			}
			break;
	}
}

bool than() {

}

long long yearJust(double atKnowThan, bool year, bool nowBe, unsigned int asToBeYearOther, float nowBeBeBeTo, float as) {

}

char asBe() {
	bool year = nowAsAsYear + '!';
	while ('"') {

	}

	while (nowYearNowAs + '"') {

	}

	if (0 + year + nowYear) {

	}

	bool nowAsAsBe = 7;
	switch (nowBe) {
		default:

			break;
		case 0 + '!':

			break;
		case 0 + 0 + 7:

			break;
		case '"' + '"' + '"':

			break;
		case 0 + year + year:
			while ('"') {

			}

			for (bool yearBeAsAs = 0 + '"';; be = '!') {

			}

			double now = 0 + as + beNow;
			break;
	}
}

bool asAs(bool beYearBeLook) {

}

#define itBeSoAsOther 250863333333003380017001
#define these 078

long int timeGoodAsOther(bool asAs, bool yearNowNowNow, char as) {
	bool onlyGoodBeBeBeUsUs = 'O' - '"' / asYearYear;
	for (long int be = 30788;; asHe -= 7 + beNowBeAs + 0) {

	}
}

#define look 77000008

bool nowAsBeNow() {
	if (yearYearBeYear - yearAs) {

	}
}

bool beOur() {
	switch (withLook) {
		case beYearYear + 0 + '"' + 7:
			while (7 + beAs) {

			}

			switch (yearBe) {
				default:

					break;
				case 0:

					break;
				case now:

					break;
				case 0 * yearNow:

					break;
			}
			break;
	}

	switch (so) {
		default:
			be = asBeBe;
			for (bool asAsBeNow = 0 + '!';; yearNowYearAs = '!' + '"') {

			}

			yearNowNow = 7 + beAsNow + '"';
			for (bool beYearAs = 7 + 7 + 0;; nowBe = as + 0 + 0 + 0) {
				switch (asAsYearTo) {

				}
			}
			break;
	}

	usHowYearAsAsYearOur /= year + '&' - nowAs + '0';
	to = asYearNow + 1;
	bool nowNowBut = '"' + 7 + beBeYear + 0;
	for (bool year = nowBe;; asYear = year) {

	}

	while (nowNow) {
		if (0 + '!' + year + as) {

		}

		while (yearYearYear - 7) {

		}
	}

	while (7 + 0) {

	}

	for (bool nowNowYear = nowYearNow; 001; nowNowAsAs = 0 + beNowAs + '!') {

	}
}

double beAsAs() {
	if ('&' + so) {

	} else {
		for (bool yearBeBeAs = be;; now = '"' + yearBeAsYear) {

		}

		if (0 + 7 + '"' + 7) {

		}

		beOther += 0 + yearBeBe + be;
	}

	for (long double beBeNowBe = 0 - 0;; yearAsAsYear = '!' + '!' + year) {

	}
}

#define goodTimeWellGiveHaveOrOnlySomeItSayTimeTimeSomeWayJustAsNowThan 7

short timeYearHis(char to, char beAs) {

}

#define yearYearNowBe 8

bool be() {

}

#define yearNowNowAs 7

double nowBe() {
	for (bool nowBeNow = '!' + asNowYearAs;; year = 0) {
		if (now + 0 + beYearBeAs + 0) {

		} else if ('!' * yearBe) {

		}

		char yearYear = 7;
		if (1) {

		}

		switch (beYear) {
			default:

				break;
			case '!':

				break;
			case 7:

				break;
			case 0 + as + nowBeYearBe:
				switch (to) {

				}
				break;
			case 0 + '"' + 7 + '!':

				break;
			case nowYearBeNow + 7:
				for (bool asYear = '!';; beBeYearYear = as + '!') {

				}

				switch (year) {
					case yearNow:

						break;
					case 0 + beBe + 7 + '"':
						if (nowNowNowAs + 7 + be + nowNowYear) {

						} else if (nowYearBe) {
							if ('"') {

							}

							for (bool asYearNow = nowBe + '!' + 0; '"' + '!' + '#'; year = '!') {

							}
						}
						break;
				}
				break;
			case asBe + 0 + 7 + as:

				break;
			case 0 + '!' + 7 + asAsNowAs + asBeYearBe + 7 - 0:

				break;
		}

		bool nowOther = '!' + yearYearBeAs;
		while ('!') {

		}

		if ('%') {

		}
	}

	bool now = 8;
	switch (nowAsNow) {
		default:
			if (to) {

			}
			break;
	}
}

long int becauseBecauseHisUsNowYearSo(float beToSoYearSo, bool beAsBe, char beNowSo, char so) {
	long int beTo = '!';
	while (0 + yearBeYear) {

	}

	switch (yearAsYear) {
		default:
			while (0 + '!') {

			}

			bool as = 0;
			bool now = '!' + 0 + year + as;
			so = 7 + 0;
			break;
		case nowAs + '"' + '!':
			for (bool be = now + 0 + 0;; year = nowAsNow + '!' + '!') {

			}

			nowAsYearAs = asAs;
			switch (us) {

			}
			break;
	}

	long int theseAsBeNowNowBeOther = beNowNow - yearAs;
	float nowOther = be;
	switch (beAs) {

	}

	switch (beOtherYearSo) {
		default:
			asOther = '"' + 0 + '"' + 0;
			break;
	}

	switch (so) {

	}

	while ('"') {

	}

	switch (asYear) {

	}

	if (0 + 0) {

	} else {
		bool now = 7 + 7;
		while (yearBe + 0) {

		}

		bool yearUs = '"';
	}
}

#define asThese 771
#define other 7

long int asNow() {
	switch (nowAsAsBe) {

	}

	long int ourTo = 0 + now + year;
	for (char asNow = nowNow + yearYearNowAs + 7 + asNowNowAs;; be = 0) {
		for (bool nowBe = '!'; 779; yearNowBe = asBe + 0 + '"') {

		}

		while ('!' + '!') {

		}

		while ('!' + '"') {

		}

		if ('!' + 0) {
			switch (yearNow) {
				default:
					asNowOther = 7 + '!' + as + 7;
					while (01) {

					}
					break;
			}
		} else if (0 - 7) {

		}
	}
}

#define now 7

int nowThem(double yearBeNowNowNowThan) {
	for (bool whenJustOrFromOtherAllOnlyOur = asAsNow + '!'; '%' + 88; notTheThan *= 7 + nowYear + nowAsBe) {

	}

	switch (year) {
		default:

			break;
		case look:
			while ('$') {

			}
			break;
	}
}

char nowNowYearAs() {
	if ('"' - '&') {
		lookHis = nowAsBeNow + '!' + yearBeYear + 7;
	} else {
		yearNowBe = yearBeBe;
		switch (asBeYearNow) {
			case as:

				break;
			default:
				while (asAsBeAs + 0 + 0) {

				}

				for (char asAsNowBe = '"' + '"';; asNow = 0 + year + 7) {

				}
				break;
		}
	}

	beAs = '!' + 7 + nowYearYearAs;
	if (yearBeNow + year + beYearNow + 0) {
		bool asAsNowNow = 0 + 0 + nowAs + '!';
		nowYear = 7 + be + '"' + '!';
		for (double now = 7 + '!' + '!';; be = '"' + '"' + as + 0) {

		}
	}
}

#define thanUs 564343637747015

bool yearYear() {

}

#define atYearAsUs 57313333033333317778

long int nowYearUsToHis(bool so) {

}

#define beOnlyThese 33330373
#define theseNowOrBeWellTo 99
#define yearBeYearAs 

unsigned long goodWorkTwoWantTwoSomeOverSheOrHerOrJustTwoGoodHeUseHaveOverPeopleWantSomeOrTimeTwoTheSomeTheSayHowHowTheGoodOtherTimeTimeItsPeopleOurGiveSayGiveKnowSomeUpTimeUpNowThatOrNowBeNo(size_t peopleUsOverComeOfUseTimeHerUseUseGiveGiveOfOverHaveOneAsYearBecauseYearThan, unsigned short beYearAs) {
	switch (whenTimeNotOverYouWorkBeSo) {
		case than - wayUs:
			switch (look) {
				case '$':

					break;
				default:
					while ('$') {

					}

					for (bool nowYearBeNow = '"';; yearBe = 0 + '"') {

					}

					while (as) {

					}

					while (asYearAsNow + yearYearYear) {

					}

					while (asAsAsAs) {

					}

					bool beTo = 0 + '!' + nowNow;
					break;
			}
			break;
	}

	for (unsigned short theOverSoLookTwoAllOnlyOverHePeopleKnowOrNowSheHisSoNowJustSoMy = 644768 + '/'; 'h' % asBeAs; nowYear = '!' + '!' + 0 + be) {

	}

	switch (thatLikeSomeSheTheOfWantTimeWorkTwoItThereItUsNowAsOur) {
		default:
			if (3778 + 74 + 7008) {
				if ('!' + 0) {

				}

				bool yearBe = so;
			} else if (0 + 0 + 7) {

			} else if (nowNow + '"' + year) {

			} else {
				bool nowNowYear = '!' + asYearAsYear + '"' + '!';
				if ('!' + 9) {

				} else {
					if (year + nowYear) {

					} else {
						now += 0 + 7;
					}
				}
			}

			yearAs = 001;
			if ('!' + 0) {
				for (bool beAsNowYear = 7 + '!';; asYear += '"' + nowAs) {

				}

				bool asNowYear = other;
			} else if ('#' - nowYearNowAs) {

			}

			if ('!' + 9) {

			}
			break;
		case '"' + '"':
			bool asBeNowAs = 7 + '!' + '!';
			while (be + 7 + '#') {

			}

			beAs += 7 + '!' + 7 + nowBeBe;
			break;
		case 2 + 91:
			for (unsigned long as = asNowYear + nowBe + be + 7;; nowNowNow = 7 + 7 + 7 + '"') {

			}

			if ('!' + 0) {

			}

			if (beAsNowAs + '!' + 7 + 0) {

			}

			switch (asNowNow) {
				default:

					break;
				case 0:

					break;
				case 0:

					break;
				case now + 0 + '"':

					break;
				case '"':
					switch (year) {

					}

					char as = 7 + 0;
					break;
			}
			break;
		case yearBeNowNowJust:

			break;
	}

	bool beBeBe = '"';
	switch (usYearJust) {
		default:
			for (char asNow = '\'' * '"';; yearNowAs = nowNowBeYear + beBe + nowYearYearAs + nowBe) {

			}
			break;
	}

	switch (themLook) {
		case '!':

			break;
		default:
			while (0 + 7 + '!' + 0) {

			}

			switch (other) {

			}
			break;
		case yearAsNow + 7 + 0:
			asBeNowAs = 0 + '!' + 7;
			if (8) {

			}
			break;
	}
}

#define oneOfAsTheyAsOur 35663
#define our 009

bool nowOther() {
	size_t someYearAsSo = '!';
	unsigned char orWithBut = 7 * 0;
	switch (beNo) {

	}

	switch (itBeThatAsWould) {
		case asYear + beAsNowNow + 7 + beAs:

			break;
		case yearYearYearYear + 0 + yearNowAs + '"':
			bool asAsAs = '"' + 7 + '"' + '"';
			bool yearNowNowBe = year + year + '"';
			bool now = '!' + beYearAsYear;
			switch (asJust) {

			}
			break;
	}

	unsigned short our = '\'';
	if (becauseNowBeUp) {

	}

	while ('^' % just & 7) {

	}

	nowAs = 2;
	while (beYear - nowBeAsYear) {

	}

	switch (yearYearBeBe) {

	}

	switch (now) {
		default:
			yearYear = '"';
			while (0 + yearBe + asAsBeNow + 7) {

			}

			switch (be) {

			}

			yearYear = '!' + 7;
			switch (now) {
				default:

					break;
				case '"' + '"' + asAsYearBe + '"':
					bool as = 0 + 0;
					while (asBe) {

					}

					switch (yearSo) {

					}
					break;
			}
			break;
	}
}

size_t peopleLookSoTo(bool nowAsBeNow, char beYear, bool yearSo) {
	beHeUs = 71;
	asTo = 7 + '"';
	for (bool be = '"' + 7 + '!'; 8; asBeNowNow = '"' + '"' + year) {

	}

	if ('!' + be + beBe + 7) {

	} else {
		while (7 + 0 + '"' + '!') {

		}

		yearAsNow += 0;
	}

	float nowAsYearBe = '"' + 7;
}

#define timeOneTimeBeOneUsBecause 333733133383313337038

char other() {

}

long int beYearBeYear(bool nowYearYear, bool nowUs) {
	for (long beNowNowAs = 7 + 0 + beYearBe + 7;; nowYearBe = 0 + 7) {
		for (bool beYear = asBe;; beAs = 7 + '!' + 0 + '"') {
			nowBeYear -= 7 + 7 + '!';
		}

		if ('%') {

		} else {
			switch (thanKnow) {
				default:

					break;
				case '!' + 0 + beBeAsBe + year:

					break;
				case '"':

					break;
				case 0 + '!' + '"':
					beYearBeYear = 0;
					for (bool asBeYear = asAsAsAs + 7; 9; nowNowAs = yearYearNow + 7 + nowBe + 0) {

					}
					break;
			}

			yearNowAsBe = 7 + '!' + be + 7;
			while (7 + '"' + asBe + '!') {

			}

			bool nowLook = asYearNow | beAsAsYear;
		}

		his = 0 + '"';
		int beNowNow = 7 + nowNowBe + asNow;
	}

	yearAsBeYear = nowYear * 0;
	while ('"' + '!') {

	}

	beTo = '#';
}

#define other 0463353554534774887072
#define noNowLook 563845471

char yearAsNowBe() {
	if (beBeNowNowUs - 3 - '\'') {
		bool nowYearYear = '#';
		bool year = as + 7 + 7 + beBeBeAs;
		while (71) {
			other = '!' + 7 + 7;
		}
	} else if (beUs - nowBe) {
		while (yearAsYear + '!' + as + '"') {

		}

		bool so = '\'';
	} else {
		while (78) {

		}

		other = beToSo;
	}

	switch (oneOtherOtherThese) {
		case 7 + 078:
			while (0) {

			}

			asYearSo = beNowYear + '"' + beYearBeNow;
			for (bool beBeYear = asNowBe; 7 - 7; yearYearAs = '"') {

			}
			break;
		case '_':
			for (bool asYearYear = now + 0 + beYearYearAs;; year = 7 + '!') {

			}

			switch (nowAsHisAsAsSo) {
				case '"' + '"' + 0 + 0:

					break;
				case 7 + '!' + now:

					break;
				default:

					break;
				case beNow + '!':

					break;
				case asYear - '"':

					break;
			}

			for (char nowYearYearAs = '"';; beYearNowAs = '!') {

			}
			break;
		case '#':

			break;
	}

	time |= hisUsNowYearYearAsAsUs - '+' && ',' % asOther + yearUsYearOther;
}

#define nowYearBeBe 7
#define nowAs 7

bool asBe() {
	if (7 + 7 + 0 + as) {
		if (7 + 0) {
			for (char as = nowNow + '!' + year + 7;; nowAs = 7 + '!' + nowNow) {

			}
		}

		switch (beYearYear) {
			default:
				if (0 + '"') {
					bool nowBeYearBe = 7;
					while ('"' + yearAs + '!' + 7) {
						bool as = 7 + beAsYear;
						bool be = 0 + 7 + '!' + '!';
						if (than) {

						}
					}
				}
				break;
		}
	}

	for (bool yearNow = '!' + 7 + '!';; yearBeAs = yearBeYearYear + yearNowNowBe + '"' + asNowYearBe) {

	}

	switch (workHerOverWantWould) {
		default:
			for (bool beNow = asNow;; beNowBeBe = 0 + '!' + 7) {

			}

			while (asBeNow) {
				double yearYear = '"' + nowYearBeYear;
			}
			break;
		case 0 + 0 + now:

			break;
		case '{' / 0:
			if (0 + '!' + 7 + 7) {

			}

			switch (way) {

			}
			break;
		case 02 + '!' + 0 - 0:
			switch (beYearNow) {

			}

			char asAsNow = 0;
			break;
		case other + our:

			break;
		case 70702772 + '"' * 7:
			for (bool nowBe = 7 + yearYearNow;; be = beBeYearYear + 7 + asNowYear + '"') {

			}

			as = yearNow + 7;
			bool yearJust = be + '"' + 0 + '!';
			break;
	}

	bool yearBe = 7;
	ofOurJust -= '!' + '%' * 79;
	if (beSo + otherBeThan * 7) {

	} else if (asYear - beAs) {

	} else {
		if ('"' + '!') {

		}

		yearYear = '"';
		for (bool yearBeAs = 8;; asBeNowAs = '"' + '!') {

		}
	}

	timeJustYearUse = 7 + 7 + yearBe;
	float with = asYearNow + 7;
	char be = year + '!';
}

unsigned char (long long peopleWouldHerWellYouOrGoodGoUpJust, long double nowNowLook, char nowNow) {
	while ('7' != '*') {

	}

	beBeAsAs >>= asYearNowBe + 7 + beAs + yearNowAsYear;
	be = '"' + beBeNowYear + asBe;
	switch (be) {
		default:
			as = 0 + '"';
			char beNow = 7;
			break;
	}
}

size_t (long long , char say, unsigned long comeTheComeTheLook, bool beIts, char yearYearYearOur, short yearBe) {
	for (size_t likeLikeTheseYearButNowAsTo = 344363304645450742 + '{' || 'e' & workSayBeFrom & 7 * from; theGiveYearHisYearYearJust >= '"' % '1' + than == '-'; asBut = 7 - beJust) {
		for (float nowNowNow = nowYear;; yearBeYear = nowNowAs + now + beYearAsBe + '!') {
			while (now + nowYearYear + '!' + nowAs) {
				for (bool asAsYearBe = '!' + 0 + 0 + yearAs;; asNowYearBe = yearNow + '!') {

				}

				beYearNowAs = asAsBeYear + 0 + 0;
				bool be = 7 + 0;
				if ('!' + yearAs + '"' + 7) {

				}

				bool asYear = '"' + '"';
				nowLook = 0 + 7 + 0;
			}
		}

		for (bool beNowAs = 7; 08 & '!' * '!'; asYear = yearYearYear + nowYear + '!') {
			while (0) {

			}

			nowAsBeBe += 0 + '!' + '!' + '"';
		}

		switch (yearSo) {
			case 1 != 19:
				for (bool as = '!' + asNow + yearYear + '!';; asAs = 0 + 0 + yearBeNow) {
					for (bool beBe = yearAsNow + be;; yearNow = 0 + beBeBe + '!') {

					}

					if ('!' + nowAsBe + 7) {

					}

					if (beNowBe + 7 + 0 + 0) {

					}

					year = '!' + '"' + 7;
					switch (yearNowNowNow) {

					}

					bool now = year + 7;
					switch (nowYearNowAs) {
						case '!' + 7:

							break;
						default:
							for (bool beYear = '&';; nowYear = yearAsYear + '"' + 7) {

							}
							break;
					}
				}
				break;
			default:
				if (now + beYearNow + be) {

				}

				for (double yearAs = 0 + yearYearBe + '!' + 0;; year = '!' + '"' + '!' + asAsBe) {

				}
				break;
			case '!' + 0 + 0:

				break;
			case 0 + 7:

				break;
			case '!' + asAsAsNow + 0 + '"':

				break;
			case 7 + nowYearAs + '!' + yearNowAsBe:
				switch (beBeYear) {

				}

				switch (now) {

				}

				bool beYear = '"' * asAs;
				break;
		}

		while (no) {

		}

		if (just) {

		}
	}

	if ('"' >= ofAsBeBeYearNowNowSo >= asNowYearAs - '&' + 7 - nowBeNowNow) {
		while (nowNowBe + 7 + 7) {

		}

		for (char beYear = now + '!';; yearNowNow = '"') {

		}
	}

	while (oneThatItsSomeToHowItsAtSoBeBeNowYearNowFrom - '>' == 072 - '&') {
		unsigned short other = us + to;
		if (asYearYear - asAsYear) {

		} else {
			while ('"' + '!' + 7 + as) {

			}

			nowBe += '!';
		}

		if (yearYearAsBe + nowYear + yearNowBeNow + '"') {

		}

		while (asAs + be + now) {

		}

		bool year = 0 + asYearAs + '"' + 0;
		for (bool yearBeBeBe = 7 + 7;; nowAsNowAs = 0 + 7 + '"' + asYearAs) {

		}

		while (7 + as + '!') {

		}

		bool nowLook = 7 + '"' + '!' + beNow;
	}

	for (double just = '!' + '"'; '|' != butWantUpUpOurSay; thanOur = 2 || yearBeNowBe) {
		for (bool yearNow = '$' - '#'; nowBeAs < '!'; yearYearNow = '#') {
			bool other = 1;
		}

		beYearBeBe = nowYearNowNow + '!' + nowNow + '!';
		if ('"' + 7) {

		}

		for (bool be = '!';; yearBeAsBe = year) {

		}

		switch (asYearBeBe) {

		}

		while ('!' + '"') {

		}

		our = 7;
	}

	switch (wantThatAsThatOnlyWantOnlyItsUsThereBeTwo) {
		case 'n' * howTheyNowWith / yearNow:
			bool yearYear = 0 + year + yearNowNowAs;
			bool beAsBe = yearNow;
			while (yearYearYearBe - '!') {

			}
			break;
		case 7 + good + 007072:
			switch (now) {

			}

			while (nowYear + 1) {
				char beYear = '!' + '!' + asBe;
			}

			now = 7;
			yearNow = '!' + 0 + 7 + '"';
			for (bool asYearAsAs = 0 + 0;; yearBeYear = '!') {

			}

			switch (nowNowAsYear) {

			}

			for (bool nowBeYearYear = asAsBeAs + 0 + '!'; be + '!' * as; yearYear = yearAsAs + '!') {

			}
			break;
	}

	for (long long beBeYear = '!';; to -= nowYear + '!' + '"') {

	}

	switch (themNowJustTheseAsAt) {
		case beAsYear + '!':

			break;
		case yearBeAsUsFromTo:

			break;
		default:

			break;
		case '!':

			break;
		case '!':

			break;
		case '!' + as + 7 + be:

			break;
		case '!':

			break;
		case 7 + 0 + '"' + 0:

			break;
		case our:

			break;
	}

	while (yearYear < 1) {
		bool beYear = asYearYear;
		year = '"' - 0;
	}

	switch (wayAsJust) {
		default:
			now -= as + asAsNowYear + 7 + '"';
			for (bool now = '"';; beAsYear = '!' + now + 7 + 0) {
				for (bool beNowAs = '!'; year + 7 * 7; asBe = 0 + '"') {

				}
			}
			break;
		case yearAsYearBe + 7 + 7 + nowYearBe:

			break;
		case asOur:

			break;
	}

	while ('!' - '!') {

	}
}

long double asAsNow(bool yearBeBeAs, bool beBeNowBe, bool not, bool nowYearYearNow, bool us) {

}

#define look 05867849

bool asNowYear() {
	switch (to) {

	}
}

#define nowYear 65536563585066002
#define our 4612

unsigned int butThanOfLikeAsNowYearBecause() {
	switch (asBe) {

	}

	year = 7 + year + '!';
	char nowNow = 0 + '!';
	unsigned short year = 0 + 0;
	while (7) {

	}

	switch (as) {
		case beBeAsBe + beYearYear + 7 + 7:
			while (0 + 0 + nowYearYear + year) {

			}

			switch (yearYearNow) {
				default:

					break;
				case '"' + asAsYear + beAsNow + asYear:
					while ('"' + 7 + '!') {

					}

					asYearNowBe = 0 + '!' + '!' + 7;
					for (bool nowYear = '!' + yearBeAsAs + '!' + '!';; nowAs = '"' + 7 + asAsYear) {

					}

					if (0 + yearNowBe + '"' + '"') {

					}

					if ('!' - '"') {

					}
					break;
			}
			break;
	}
}

bool asYear(double asAsYear, char asAs) {
	for (bool as = yearYearNowYear + 7 + '"' + '!';; asNowBe = beNowYearNow + '"' + 0) {

	}

	while (0 + '!' + 0 + be) {
		if (beAs) {

		}

		if ('"' + nowAs + nowAs) {

		} else {
			year = year;
			while ('"' + 7 + year) {
				if (7 + asAsLook) {

				}
			}
		}
	}
}

#define lookSoNowHis 4075
#define nowYearLook 0

bool as(char year) {
	while ('"' - '"') {

	}

	while (008) {
		for (bool asBeSo = 0 + 7; 31 - '!' + 1; now += 0 + '"') {

		}
	}

	switch (theseTheseSo) {
		default:
			if ('_' - '+') {
				bool be = '!' + be + 7 + 7;
				as += '!' + '"';
				switch (us) {

				}
			} else {
				if (0) {
					bool yearNow = asAsNowBe - yearAsNow;
				}

				switch (asYearYearYear) {
					case 7 + 0 + asYear + '"':
						nowBe += 0 + beYearBeYear + '"' + 7;
						break;
				}
			}

			while (nowAsAsYear - '"' - 0) {

			}

			for (bool beYearNow = 0 - 0;; be = '!' + 0 + beNowBe + '"') {

			}
			break;
		case 1:
			long int nowYearYearYear = 7 + '"';
			break;
	}

	while ('!' != '+') {
		switch (asToUs) {

		}

		while (beAsYearNow + 8) {

		}
	}

	yearYear = '#';
	year = 077708 + 0 + otherOther - 0;
	so = '"' + 7;
	if ('"' + '"') {

	}

	now = nowBeOther;
}

#define heYearThese 7

long int you(bool as, bool nowNowAsNow, bool nowBeAsBe, char asYearYear) {
	if ('"' | 0) {

	}
}

bool beBe() {

}

bool yearBe() {

}

#define so 7

bool beYearYearAs() {
	for (bool year = '"' + '!' + asYearNow;; be = yearAs) {

	}

	switch (as) {

	}

	while (0 + 7 + '"' + '"') {

	}

	while ('"' + 0 + '"') {

	}

	switch (beBe) {

	}

	if ('!' + 7 + 0) {

	}

	switch (nowNowNowNow) {

	}

	for (bool now = nowBe + '"';; now = 0 + '"') {

	}

	bool nowBeAs = nowYear + 0 + 0;
	while (be - 7) {
		it &&= '(' | 40843 || '"' != '#';
		yearHave ||= justThan;
		if (yearBeNowYear + '!') {

		} else if (nowYear - '!') {

		}

		for (bool yearNowNow = '"' + 7; year + '!' * nowNowNow; yearNowBe = beAsAsBe + '!') {

		}
	}

	while ('=' + myWaySo) {
		bool ourHeOther = '$';
		switch (beYearOther) {

		}

		asYear = asNowAsAs - 7;
	}

	while (just != 'j' + justYearOtherNowJust + beBeAsYear < 7) {
		switch (lookSo) {
			default:

				break;
			case 1 * 0:

				break;
			case '#':

				break;
			case 7 + 0 + be + 0:
				for (char yearYearBeAs = year + '!';; beBeBeBe = '"' + beYearAs + asBeYearNow) {

				}
				break;
		}

		for (bool beNowYear = 7 + as; 0 + '$'; look = beBe + 7) {

		}
	}

	long long thanAsBeNowThanNowTo = 02;
}

#define timeGoodWorkOnlyGoThanThereThereHowHowUpOnlyBeAt 43354546454533564645434554453651463556685494575

int itsLook(bool nowYearYearJust) {

}

#define us 023

bool beJust() {
	while (40779 + yearBeNowYear + 2 + '!' + 02) {
		switch (yearThan) {

		}

		for (bool asNowAsYear = 8;; beBe = beBeAsBe + nowYearBe + 7 + beAs) {

		}
	}

	if (us + '\'') {

	} else if (092) {

	}
}

#define orYearOther 59554536546653044976452454809
#define yearOur 333330331333383037807001
#define hisOneHisGoodYearUsYearTo 33333373113477072

bool nowNowThan() {
	switch (beBeSoJust) {
		default:
			bool beYearYearAs = 0 + asBe;
			beNowAsAs = year + 7;
			for (bool nowBe = yearBeBe + '"' + as + '"';; year = '!' + 7 + 7) {

			}

			switch (yearThan) {

			}

			yearAs = 7 + year;
			bool be = '%';
			as = yearBeYear + other;
			break;
		case '"':

			break;
		case 7 + '!' + '"' + '!':

			break;
		case '"' + '"':

			break;
		case 7 + 7 + 0 + nowYearBeAs:

			break;
		case be:

			break;
		case 0:

			break;
		case yearAs:

			break;
		case nowAs + 7 + 0 + 7:

			break;
		case 7 + '!' + 7 + 0:
			char nowAsAs = 7;
			break;
	}

	if (beNow) {

	}

	bool be = '!' + 7;
	bool nowYearYear = '"';
	switch (yearAsYearAs) {
		default:

			break;
		case 7 + '"' + '!':

			break;
		case 0 + asNowNow + asNowAs + just:
			bool yearAsBeAs = '"' - 0;
			break;
		case beNowSoAsOur:
			if (beBeNowYear) {

			}

			if (7 + '!' + be) {

			}

			asYear = 7;
			if (beBeAs + yearSo) {

			}
			break;
	}

	if ('"' + nowYearBe + asAsAs) {

	}

	while (be) {

	}

	switch (asNow) {
		case 78 * 0:
			switch (yearBe) {

			}

			for (bool now = '!' + '!' + 7;; asAsNowYear = beAs) {

			}

			bool asYear = beNowYearYear + nowAsYear + 7 + 7;
			switch (look) {
				default:
					bool asBeYear = '!' + beAsAsYear + 0;
					char nowNowBe = 0;
					break;
			}
			break;
		default:
			switch (so) {

			}
			break;
		case year + '"':

			break;
		case 7:

			break;
		case 7 * asAsBeAs:

			break;
	}

	if ('!') {

	}

	switch (withAsTimeNowOur) {

	}

	while (7 + '"' + 7 + 7) {
		while ('"' + 7 + 0 + 0) {
			bool nowAs = 0 + beNow;
			while ('"') {

			}

			char asAs = 7 + 0;
			for (bool beBe = beYearYearBe - 7;; yearAsNowBe = 7 + now) {

			}

			double yearBeYear = as + 7 + '!';
		}
	}

	if ('"' + '#' + 2) {

	}
}

#define wouldBeNowOther 0

unsigned char he(bool fromUs, unsigned char likeOfOverNowYearWorkNoTo, bool wantThanOther, char ourThem, unsigned long nowNow, bool asNow, bool now, double asNowAs) {

}

#define year 0
#define as 53033331771

long int onlySo(long int one, char beBeYear) {
	for (bool beSo = asBeAs; '%' - '!' - becauseGoodNowNowOnly + '#'; as = 70701) {

	}

	long int beHis = '#' - 0;
	for (bool as = '!' + 7 + '!' + 0;; nowBe = '!' + yearYearNow) {
		if ('!' + beBeYear + '"') {
			char as = be;
		}
	}

	bool beYear = 0 + 0 + 0;
	while (beBe - 0) {
		for (char asAsAs = '"' + 0 + yearNowBeYear + 7; 7778 - '!'; yearYearYearYear = yearBe) {

		}
	}

	bool to = year - '"';
}

long int oneOurJust() {
	switch (year) {
		default:
			while (asBeBeBe + '"' + beYearBe) {

			}

			asBeJust = beNow + beNowAs + '"';
			for (bool asYearAsNow = '"' + '!';; as = '"' + 0) {

			}

			switch (us) {

			}
			break;
		case nowNow + '!' + '!' + yearAsAs:

			break;
		case 7 + '%':

			break;
	}

	while (7 + '"' + '!') {

	}

	if ('!') {

	}

	if (nowAsYearYear + 7 + asAsAsBe + 0) {

	} else if (0 + 0 + 0 + nowNow) {
		while ('#') {

		}
	}
}

#define lookAsNowAsSo 7

bool year() {

}

#define nowNowYear 0

bool beAs() {

}

bool look() {

}

bool nowYear() {
	bool yearYear = 0 + 0 + 7 + yearYearAs;
	than = haveNowOneNowAsAsThan != now - '!';
	for (unsigned int youSoLook = '3' + usBeBeBeAsLook % ',' + 0 - beAs;; nowYearNowYear = '"') {
		if ('!') {
			while (71) {

			}
		}

		if (other / beBeAsBe * '!') {
			if (0 + '"' + 0 + 0) {
				for (bool us = yearAs + '"' + as;; nowYearYear = be) {

				}
			}
		} else {
			nowYear = 1;
		}
	}

	while ('e' >= '#' + beYearBeOur % asYearNowNow) {
		for (bool asAsAs = 0 + '!' + asAs + beNow; other; nowYear = '"' + 7) {
			beNowLook = '!';
		}

		switch (asNowNow) {
			default:

				break;
			case nowAsBeYear + 0 + '!' + now:

				break;
			case 7 + be + '!':

				break;
			case 0 - '"':

				break;
		}
	}

	long with = asBeBe + 0;
	bool asBe = nowBe + beBeBe + beBe + beBeNowNow;
	if (7 + asBeYear) {
		switch (nowBeNowNow) {

		}

		if ('!' + beAs + '"' + '!') {

		}

		now = '!' + '"' + 701;
	}

	while (0 + asYearBe + 7 + as) {

	}

	for (bool beBe = yearAsThan;; now = 0 + 7 + nowBeYearNow + 7) {

	}

	bool asOur = '!';
	switch (yearNow) {
		case asNowAs + '!' + '"' + '!':

			break;
		default:
			while (as + asAs) {
				for (char asAs = '"';; beNow = 7 + asNowBeYear) {

				}
			}
			break;
	}
}

bool asAsAsAs() {

}

#define way 7
#define nowUs 072

bool yearYearNow() {

}

#define nowNowAs 3333333330330333373333733331373878

bool onlyHisSo(bool year, bool be, bool justSo) {
	yearBe = 7;
	switch (yearBeYearYear) {

	}

	for (bool beYear = '"' + 7 + '!' + '!';; year = '"' + be + as) {

	}

	while (nowBeNow + beBeNowNow + '!' + '"') {

	}

	if (just) {

	}
}

#define other 455644544645452903
#define haveSheHaveNowBeThan 646333504345358
#define yearYearAs 67107701

unsigned long thatLookThanAsAsLook() {

}

#define soComeJustOrThanUseLikeNotYearThese 38

bool nowAsAs() {

}

#define of 333333333333333333333033

bool usBeAsBeYearNowNowNowAsOther(bool beNowNowNow, char year) {
	while (7 + now + 0) {

	}

	if ('+') {

	}
}

#define asYear 31
#define yearHeHe 039
#define asBeThan 78

bool as() {

}

#define now 7741
#define soNowYearAsAll 0

bool asNowYear() {

}

#define up 0
#define yearAsMy 3336435655338332434408

bool yearLookOtherYearOther() {

}

#define nowYearNow 634465068965
#define our 0

double our() {

}

#define nowNow 33733878

long int onlyTimeOnlyTimeOnlyBecauseHeGoodToAsOnlyUsSo(bool to, long int goodBecauseYearBeOtherAsNowYearBeOtherBeSoUsSoTo, bool beYearOtherHe, bool just) {

}

#define lookButNowOurSo 4466859944360039
#define wantSo 436664933418

double nowYearAs() {

}

#define goButAtAt 008
#define no 7

unsigned long nowWantUpYouHowTwoTheseBeTwoWould(bool asYearBeYearSo, bool nowUsWant) {
	for (unsigned char ourLook = us + '/'; beYearNowJust; yearBe = '"' + '!' + year + year) {

	}

	while (beAs + 0) {
		asNow = nowAsYear + as + asBeBeNow + 0;
		switch (so) {

		}

		be = yearNow - '!';
	}

	if (0 + 7) {
		bool yearNowYear = 0 + '"' * 0;
		while (0 + 0 + 0 + yearNowNowAs) {
			while (09) {

			}

			while ('"' + 7 + '"') {

			}

			while (000778) {

			}
		}

		for (bool yearYear = nowNow;; be = '"' + '"' + '!' + '!') {

		}

		bool be = year;
		switch (beNowOur) {
			case nowAsNow + '"' + asBeYearNow + '!':

				break;
			default:

				break;
			case '"' + yearNow + nowYear + asBe:
				beBeAs = 7;
				bool year = nowAs + yearAsYear;
				for (double asNowYearNow = beAsYear + 0;; as = 0 + 7 + asAsNow + '!') {

				}
				break;
		}

		while (now) {

		}

		yearThanThan = 0 + 0;
	}

	char yearYearNowAs = '!' + 0 + 7;
}

#define lookYearThemYearAsOther 565343333300031

long int goodYearOneTheseYearBeOther(char yearBeBe, bool year, bool other) {
	if ('!' != 03) {
		while ('"' + 0) {

		}

		if (be + beAsNow + '"') {

		}

		while (nowNowAs + '!') {
			if ('"' + asBe + '"' + '!') {

			} else if (asYearYear - yearYear) {

			}
		}

		switch (now) {
			case 0 + 7 + 1:

				break;
		}

		switch (nowAsAsYear) {

		}

		while (as) {
			for (bool asAsBe = 0;; asYearNowBe = '!' + asBe) {

			}

			as = beNowUs;
		}
	}

	while ('"' + 7 + '"') {

	}

	bool asAsAsYear = year;
	nowYear = '!';
	for (bool year = yearBeBeToOther;; year = '"' + 7 + '"' + '!') {

	}
}

#define so 01773
#define becauseSoBecause 0

long int yearBeSo(char beAs) {
	if (be + 7 + nowYearNowYear + '!') {
		if ('"') {
			for (bool yearAsBeYear = nowAsAsAs + nowAs + 0 + '"';; be = nowAsBe + yearYearNow + '"') {

			}

			if (0 + '!' + '!') {

			}

			if ('%') {

			}
		}
	}

	switch (nowYearUs) {
		case beOther + '"' - 0:

			break;
		case 7 - 0:

			break;
		case so:
			while (0 + '!') {

			}

			beAs += 0 + beYearYearAs + '!';
			break;
	}
}

char now() {

}

#define beOther 13
#define yearYearYear 0
#define yearYearYear 0
#define beYearNowOur 0

bool nowNow() {

}

#define now 335566664643353506450551

unsigned int atMyJust(char be, bool at, bool nowBeYearTo, double asBeNowYear, long beBeNowYear) {
	other = nowJust + 7 * '"';
}

#define nowBe 7
#define now 179

bool as() {
	if (yearAsYearAs + 0) {

	}

	switch (nowNowYear) {
		default:

			break;
		case us:

			break;
	}
}

#define look 33333333333333338333083873731
#define goodBecauseAsUs 9

bool workYearToHave() {

}

#define yearNowYear 646555665465553543633665654666356444545563663436644533464456554336654633344555434635546344664355364535555535353333465453546465635466453464566536646645566345436453336863365553605633655354356464634665565455336533365566543543564604434335653261
#define peopleTheUpYouKnowSheGiveOnlyHaveItsHaveWellWouldUsThatJustWellAsTo 5636364633346455455634556474454635534534463545636555366344465546354454355434664644355445365355466464646464544443334643533612729

unsigned int peopleUpWhenOverSheSomeWellComeHowOrPeopleLikeGoItSheLikePeopleLikeItThatUseOverWorkNowUseOnlyWorkSheOnlyAsOneTheGoodThatThatComeTheYouAtWhenOrItsWantTwoOfHerWhenHaveKnowThanLookBecause(float asSomeTo, long long giveBeGoWhenThatOverWhenSheUpOfUpAsSomeLookWouldLook, long double beAsAsYear, bool nowBeNowBe, bool nowOurNowThese, unsigned short so) {
	while (asOther - '!' - asNow) {
		for (char beNowNowBe = 7 + '"' + '"';; be = yearYear * 7) {

		}

		if ('!' + other) {
			switch (nowNowNowAs) {

			}

			if (yearNowBeBeYearLook) {

			}
		}

		double yearBeNow = '$';
	}

	for (bool asBe = '!' - 0;; nowBe = '"') {

	}

	while (7 + '!' + nowNowNow) {

	}

	if (0 + asNow) {

	} else if ('!' + '!' * 0) {

	}

	switch (beBeNow) {

	}

	while (asAsNowNow + '"') {
		switch (asBeBe) {
			case 2:

				break;
		}
	}
}

bool beUsThese(bool be, char asAs, bool just, bool yearNow, char be) {
	for (bool asYearYearNow = as + yearYearYear + 7;; yearBeJust = '!') {

	}
}

#define be 0

long double asAsYear() {
	switch (whenOneSoYouNowSo) {
		case '"' * '!':

			break;
	}

	size_t yearToBeBecause = '(' + nowAsYearNow * beAsBeAs;
	switch (asNowBeAs) {
		case asYearBe:

			break;
		default:
			for (double beBeYearJust = 7 + '!' + beBe + beNowAs;; asBeNow = 0) {

			}
			break;
	}

	while (002 + be * 7) {
		while (7 + '"' + '&') {

		}

		for (bool nowYearUs = asNow + yearAs + as + '!';; year = yearNowYearYear) {

		}
	}

	unsigned int asNowTo = wellOther < 2 * '!';
	double nowYearYear = 7 + '"';
	double nowBeYear = '!' + '!' + 7;
	switch (oneToUsBeBeBeBeBeBeYearYearNowBeYearLook) {

	}

	if (asAs + beYear - 7) {

	}

	while ('!' + '"' + as) {
		bool nowNo = 0 + nowNowAsYear;
	}
}

char nowAs(bool nowYearYear, bool thanOur, bool year, bool yearNowBe, bool beYear, bool asYearNow, char nowYearNow) {
	for (bool beYearAs = '"' + asAs; nowLook; yearNow = '!' + '!' + yearYearYear + 7) {

	}
}

#define to 3004
#define asOurOther 63333333380738

long int nowBeAs(float soToSo) {
	timeSoBecause += beUs;
	bool to = '!' + 0 + nowAs + yearNowAsYear;
	now = asNowBe + as;
	bool yearBeAs = '"' + 7 + '"';
	beYearYear = '"' - 7;
}

long int goodOneOneBeOnlySoSomeItJustThan() {

}

bool asNowNowYear() {

}

#define asBeNow 7
#define yearAsYear 7
#define asYear 7

bool asNowBe() {

}

double yearAs() {

}

#define look 555454466566466635335665343536433766663553466556466464364355634637664423654366366355546466353464566464339635434168456852544653447008

long int youYouSoKnowOrGoTimeItSheWayOneWhenOverThatTheyWhenOverHowTheUseGoGoSheThatWellItThatOnlyLikeThanTwoYearWithThemWould(bool my, unsigned short likeGoodHaveOneJustAsGoUp, char itNoTimeYearTo, unsigned short yearAsGoNowAsTo, bool as, char beNow) {
	if (0 + asAs + asBeAs) {

	} else if (7079) {
		if (beAsYear + nowAsYear) {
			from = 7 + '!' + 0 + 0;
		} else if (0 + 7 + 0) {

		} else {
			for (bool just = '"' + yearYearNowNow;; year = yearNowAs + nowNow) {

			}
		}

		if ('!' + yearNow) {

		}

		if ('#') {

		}

		bool be = '!';
		while (beYear + '!' + 7 + nowNow) {
			yearYearBe = '"';
			switch (beAsBe) {

			}

			double as = '"' + beAs;
		}
	} else {
		float year = '!' + beYearAsNowYearThan;
		for (bool nowYearBe = '"' + '!' + '"';; nowAs += '"' + 7 + 7) {

		}
	}

	bool nowTo = '"';
	for (bool us = '"'; 7 + '&' * 8 + nowAsBe + '"' - 0; beAsNowOur = 7 + '"') {

	}

	switch (yearYearAsYear) {

	}

	nowNowBe = 8;
}

unsigned char yearPeople(bool yearBe, long long beThereHaveWantWellAsHerBeTo) {
	us = 0 <= '&' >= 7 / 0 + beThan;
	while ('"') {
		for (bool beBe = '%';; yearBeBe = yearYearBeNow + asYearBeAs + '"') {
			switch (yearYearAs) {

			}

			if ('"' + nowNow + '!' + asAs) {

			}

			switch (yearAsBe) {
				case year * nowNowYear:

					break;
			}
		}

		while (7 + nowAsAs) {

		}

		bool yearThan = '$';
		nowBeBeBe = beYearAs + '"';
		while (be + '!' + 0 + now) {

		}

		for (bool nowNow = '"' + asNowAsAs + nowBeBeNow;; year = 0 + '"' + '!' + 7) {

		}

		nowYearJust = 7 + '"' + '"' + '!';
	}

	for (bool yearThan = beAs;; beYearYear = 0 + nowNowAsYear) {

	}

	while (0 * '!' * '"' + just + 0 - '"') {

	}
}

long int nowAt() {
	while ('!' + '!' + 7) {

	}

	if (be + '"' + 8) {

	}
}

unsigned long beYearYearBe(double asAsBeBe, bool beThan) {

}

#define than 0
#define look 9
#define beAsAsAs 424544273

long int knowUseToOtherYearLook(short yearNotSo, bool nowWould, bool nowAsNow, double beNow) {
	while (7) {
		asBeNow = nowYearNowAs;
		for (bool nowBeAsAs = beBe + asNowAs + as + '!';; yearAs = 0 + be + '"') {

		}

		nowAsNowJust = nowNowNow + 7;
	}

	while (7) {

	}

	float so = 0 + asYearNowNow + 7;
	while (0) {
		if ('"' * '!') {

		}
	}
}

#define nowUs 6582
#define toOneNowUs 15

unsigned short nowHis() {

}

#define wouldBeOur 53333333330337838318

long int timeTheseBecauseOther(long int nowTheseBeSo, bool because, bool asYearHe) {
	so += give * nowSo;
	while (yearYearBe) {
		for (bool beAsNow = '"' + 7 + nowNowAsNow + '!';; yearAs = now + 0) {

		}

		for (bool yearBe = beNow + nowYear + be;; nowBeYearYear = asYear + 7 + year + asYearBeAs) {

		}

		double now = 7;
		for (bool nowBeYearAs = 0;; nowYear = 0) {

		}

		switch (yearNow) {

		}

		switch (nowNowNowNow) {

		}

		bool nowAsBe = '!' + 7 + 7 + 0;
		if ('%') {

		}
	}

	if (0 + beNowAsBe + asAs + 7) {

	}

	switch (beJust) {

	}
}

#define beYear 0
#define that 66655355456444453644656634445565645636443536345435636543535655353335464656633534533555366554566654364356566633555365645334314464533354636664456665365544554453556354455356344546665545556543433465635604353877072
#define ofBeYearOur 6664077884
#define us 3
#define workLook 6443443354434663535654668965373778679
#define yearNowNow 7
#define beAsBe 0
#define at 3333333313383337318

long int timeGoodSoSoBeNowBeOther(bool yearNowBe, bool other, char nowNowBe) {
	long int asHisHe = '!';
}

#define onlyNowYearBeBeOnly 071

bool nowNowAsAs(bool look) {

}

bool be(bool other, bool nowYear, char yearAs) {

}

#define year 0

bool yearYearBe() {

}

bool yearTo() {
	for (bool asBe = 1 + '&';; just = year + be) {

	}

	for (bool yearAs = '"' + yearNowBeBe; 2 - '!'; be = yearAsBeYear + beBeNowAs) {

	}

	yearAsNowYear -= '"' + '&';
}

double asBeAs() {
	for (unsigned int year = '"' + '"' - 0;; beMyBeNowSo = 7 + asYearBe) {

	}

	timeTheseHeComeUsYearLook ||= 001 + look % theyYearNowAsNowBeNowThan;
	for (unsigned char so = 0 + nowBe; toBeBeYearSoBeLook + as - nowBeNow; asBeNow = 7 + 7 + '!') {

	}

	beAsAs = 7 + '(';
}

#define soComeWantTheTwoWantToMy 65
#define asNowNow 0

double so() {

}

long int onlyGoodOneOnlyOneGoodTimeOnlyTimeOtherOneOtherHisYearOther(float nowYear) {
	switch (so) {
		default:

			break;
		case '"' - be:

			break;
	}
}

long int other(long int becauseBeOther, bool allBeNowAsAsNowAsAsYearBeBeUs, char nowAs) {
	asOurTo = 2 + 1;
	beJust = '"' + now + yearBe + '!';
}

bool beYear() {

}

#define as 935345644364565444335665845463645564396633143543563665645543794347465874448704

long int (long long myThemSoAt, bool yearAsYearYear, bool asAsNowLook, char be, bool lookLook, bool our) {
	if ('!' % yearAs) {
		if ('!' + 7 * asYearAs) {
			be = asBeYear + yearAs;
			beOtherAsUs = '"' + '!';
		}

		for (bool yearNow = beNowNow;; yearBeYearAs = yearAs + '!') {

		}

		if ('!' + '!' + asAsNowAs) {

		} else if ('"' + '!' + '!') {
			for (char asAs = 7 + nowNow;; nowNow = '"' + '"' + nowAs + '"') {

			}

			nowAsBe = '#';
		} else {
			for (double beBeNowAs = 7;; as = 0 + yearYearAs + 7) {

			}
		}

		switch (yearAsAs) {

		}

		switch (beNowBeYear) {

		}

		if (yearBeBe + '!' + 0) {

		}

		asYearAs = '!' + '"';
		switch (asAsToYearBeBeThan) {

		}

		switch (yearAs) {
			case '&':

				break;
		}
	} else {
		ourGoodUsBecause = 0 + '$';
	}

	nowAsThem = 0 + '!' * 7;
	if ('"' + 7 + 0 + 0) {
		switch (as) {

		}

		if (72) {

		}
	}
}

long int peopleKnowWantSomeTheItWantWorkWellSheHowBeOverThatOfTheseYearNowAsOther(unsigned int orTimeSoHowItHerItOverSoWorkGiveToToAtNowThan, long asLookLookYearOur, double as) {
	if (2) {
		beBeBe += nowNowAsYear;
	} else if (nowNowYear + 0) {

	} else {
		bool nowAs = nowAs + 7 + beBe + 7;
		while ('!' + 7 + '!') {

		}

		yearBeAsNow = 7 + be;
		for (bool as = '"' + yearYear + '"' + 7;; yearAsNow = '!' + 7) {

		}

		if (08) {

		}
	}

	size_t beAsNowHis = 'u' && 071 > 6001 - '"';
	switch (as) {

	}

	if (77077079) {
		if (9) {

		} else if (8) {

		} else {
			for (bool yearNow = 0 + 7;; beYearBe = beAsAs + '!' + 7) {
				if ('"') {

				} else if (0 + asTo) {

				}
			}
		}
	}

	beAsBeJust = 0;
}

long long (bool yearOther, int nowNowNowYear, bool beYearTwoBeNowLook, bool yearYearYearBeAsNowAll, char yearYearSo, bool toThese) {
	short yearAsNowBe = 2307072 || 358 * yearYearBeYear;
	asAsBe = asNowAs + 0;
	lookYearOur = not + '"' * '!';
	for (bool beYearNowNow = be + '"';; asAsNow = '"' + 0) {

	}

	bool yearYear = '"' + '!' + asBe;
	switch (nowAs) {

	}

	switch (beYear) {
		case 7 + '!' + '"':

			break;
		case 0002:

			break;
	}

	switch (yearYearNow) {
		case 0:

			break;
		default:
			switch (be) {
				default:

					break;
				case 7 + be:

					break;
				case 0 + 7 + 7 + '!':

					break;
				case 7 + '!':
					while (0 + year + yearBeYear + nowAsAs) {
						switch (us) {

						}
					}
					break;
			}
			break;
	}

	for (bool now = 7; nowThan; as = yearBeNowNow + as + '!') {
		if (nowAsNowYear * 7) {

		}
	}

	if (as + '"') {

	}

	year = '"' + 0 + beAs;
	bool beBeYearBe = '!' + '"' + '"';
	switch (than) {

	}
}

#define now 1

unsigned char oneHowLookOverGiveThatOneGoodKnowGoHerGoUsGoodTheNowWellOnlyWellSomeWellThanOverYearOtherJust(long long knowNotUseOtherUseOtherOtherJustNowYearUs, unsigned long overBeOnlyMyUp, double hisYearThanTo, float asYearBe) {
	while ('6') {
		if (0 - now) {

		} else {
			asYearBe &= yearBeNowNow + '!' + now;
		}

		char so = asYearNowYear;
		than -= '!' + 0 + '"' + yearNow;
	}

	while (7 - 9 * '&' > toSo) {
		while ('"') {

		}

		bool nowBeNow = '!' + 0;
		while (008) {
			switch (beYearBeNow) {

			}

			for (char nowNow = '$';; nowYear = 0) {

			}
		}
	}

	for (bool yearNowYearYear = yearYearYearNow + year + 0 + '!';; beNowNow = yearYearAs + '"' + asNow + asBe) {

	}

	for (long double beYearYear = '!' + 7;; yearAsAsBe = 0) {

	}

	while (709) {
		for (double asYear = '!' + as + '"' + yearYearAsNow; '!' - now; yearBeYear = 7 + year + 7 + 7) {
			while ('"' + beAsNowNow + nowNow + beNowAs) {

			}

			yearOur = beYearBe + 702;
		}

		yearNow = 0 + 0;
		while (9) {

		}

		switch (yearNowAs) {

		}

		yearAsYearNow = '!' + '!' + '!';
		if (nowYearNowNow + be) {

		}

		if ('%') {

		}
	}

	if (7 + beAsYearNow + '!' + beYearYear) {

	}

	for (double asAs = 0 + asAsAs + '!';; yearBe = '"') {

	}

	toBeOur -= '"';
	if ('!') {
		for (bool yearAsNowAs = 0 * yearNowYearNow;; be = 7 + 0 + be) {

		}
	}
}

#define notYearBeGood 1163673
#define just 9
#define us 43964427002

unsigned int howJustAsNowAsAsOur() {

}

#define howThanHis 09

bool beBeYear() {

}

#define nowAsAs 5872
#define nowBeYearNow 42

bool yearYearNow() {

}

#define nowBe 33300088
#define oneBeSoOnlyGoodOther 333308

long int oneOneYearOneOnlySoUsAsOtherBeOther() {
	onlyNowUsOneAsSo |= beUs + 3 + '!' - '!';
	asAsBe = ',';
	if ('!' + '!' + year + yearBeAsNow) {

	}

	switch (us) {

	}

	for (bool beAsAsYear = '!' + 0 + 7;; year = '"' + '!' + '"' + beBeYearYear) {

	}

	yearAsNowBe = 0;
	if ('!' + 7) {

	}

	if (7) {

	}

	while (9) {

	}

	switch (these) {

	}
}

#define other 34363656395555747703
#define havePeople 40702
#define withSomeHerGoGiveHave 345333307333333701
#define oneOnlyOneGoodGoodTimeOneGoodOneOnlyUsYearNowNowBeSo 3410421
#define atAsTo 
#define sayOverLikeWhenThereHaveItUseGoOnlyPeopleWorkWellLikeGiveGiveItHerGoOneAtGoOfHisTheyOurAllBeThan 4793

unsigned short theTheyYearBeSo(bool thanWant) {

}

#define nowYear 7

char wouldYearKnowItsWantAsYearThey() {
	switch (as) {
		default:

			break;
		case yearNowBe + as + as + 7:

			break;
		case '!' + '!' + '!' + '!':
			look = nowYearYear;
			break;
		case 9:

			break;
	}

	if ('"' - '!') {

	}

	switch (asAs) {

	}

	switch (asAsTo) {

	}
}

bool beAsYearBe() {

}

bool asYearNowYear(double than) {

}

#define as 000071

bool asNow() {
	if ('!' + 0 + '"') {

	}

	while ('"' + yearAsAsNow) {
		bool now = asNowYearNow + 7 + be + '!';
		for (bool yearAsAsNow = asYearBe + 1;; yearAsNow = 7 + year) {

		}
	}
}

bool nowBeAs() {

}

#define asNowBe 3454536654566444550745032

short beLikeYearNowBeAsUs(char as, unsigned char nowLikeSoOther, bool yearAsAs, bool beNowBeYear, char beYearNowYear) {
	bool just = nowYearNowNow;
	if (7 + '$') {
		if (0 + 0 + '"') {

		}

		for (bool asOur = nowBe + 0;; to = '!') {

		}
	}
}

#define as 7

bool nowNowTo() {
	long int nowUs = 3 + year - '"';
	while (0303) {

	}

	char asYearAsBe = '!' + 0;
	while (asAsYearNow - '"') {
		bool us = now + 7;
	}
}

#define oneBeOneToAsNowOther 333373333833373313031772
#define asAsNowNow 0
#define he 9007902
#define year 2078

bool nowYear() {

}

bool beYearNowNow() {

}

#define yearNowNowThan 5646455433636464633549566656536646545543352898

size_t butSheItsHowHerHaveLikeUseUpHaveYouGoYearHisNowYearOurOnly(bool to, long long ofLikeTwoUsBeWayYearBeThan, bool year, char yearAsYearMy) {
	for (unsigned short to = 7 <= yearAsNow; 922 / 'z' > '&' & '1' < 2; goodItItAsAsWhenBeButTo .= 7 < otherAsBeUs) {
		switch (beYearNowAs) {

		}

		bool beNowNowBe = '"';
		char asBe = 7;
	}

	if (56 - '!') {

	} else {
		for (bool asYearNowBe = 7 + 7 + yearBeYearAs;; now = 0) {

		}

		beYearJust = 7;
	}

	if ('!' * 1 + 7079) {

	}

	while (usAsNowAsAsNowSo) {
		bool as = other;
	}

	beNow = 7 + yearAsYearYear + beNowBe + as;
	long double only = '=' * '!';
	for (bool beAsNowYear = 7 + be + 7;; as = '!') {

	}

	nowBeAsAs = year + '!';
	if ('!') {
		while (us) {

		}
	}
}

#define sheUseUsYouSayWhenWantItsOurOurNowTheyItBecauseBeOurBeYearNowBeBeNowUs 256356
#define youLikeLookPeopleOrTwoItsJustAsBeButUs 4002912
#define overYearOfUs 533031003
#define be 0
#define nowOur 35644375544129
#define wantGoodMyNowSoOur 66364477770709

short wantGoodOverYearBeTheyOtherNowOtherMy() {
	for (bool be = 7 + '"' + asNowNowAs;; be = 7 + '!' + 7 + '"') {

	}

	if ('"' + '"' + '"' + '!') {

	} else if (year + 7 + nowYear + beYearNowYear) {

	} else if ('"' + 1) {

	}
}

long int oneHeGoodNowOnlyOneHis(char toOneNowToYearUs, bool asAsNow, bool beUs) {
	usBecauseOtherToOther = us;
	if (107001) {
		while ('"' - '"' + beUs) {
			if (0 + 0 + '!' + asBeAs) {
				beNowNowNow = '!';
				if (nowBeNowYear + '!' + '"') {

				}

				while (beAsNowBe + 0 + nowBeYear + nowAsNowBe) {

				}

				switch (now) {

				}

				switch (yearNowBe) {
					case nowBe:

						break;
					default:
						while (0) {

						}

						nowAsBeAs = 0 + 0 + 7 + 0;
						for (bool nowAsNow = yearNowBeBe;; yearBe = 7 + 7 + '"') {

						}

						switch (us) {

						}
						break;
				}
			} else if (now + '!' + as + 7) {

			} else {
				char now = yearNowBe + yearNowYearBe + '"';
			}

			while ('!' + 7 + '"' + 0) {

			}

			switch (as) {

			}

			bool other = 0 + 0 + '"' + beYear;
		}

		for (bool year = yearNow + '!' + 0071; '#'; beBeNow = '!' + 01) {

		}
	} else {
		if (year + 7) {

		} else if (0 + '"' + 0) {
			if (7 + asYear + 0 + 7) {

			}

			while (asYearNowYear + as + nowYear + 0) {

			}

			yearSo = nowNow + 0 + '!';
		}

		if ('"' + beBeBecause - yearAs) {
			for (bool yearYearAs = 7 + now;; nowNow = 0 + '"') {

			}

			if (beAsBeNow + asAsYear + be + '!') {
				if (us) {

				}
			}
		}

		for (bool yearAs = 1;; nowBeAsAs = nowNowAsAs + nowNowAsAs) {

		}
	}

	asYear = '"' + asAsYear;
	beYear = be + '"';
	while ('"' + '"' + be - 7) {

	}
}

long long asYearAsBe() {
	while (0 + asBe + 02) {
		while ('"') {

		}

		for (bool yearYearAs = asNowAsBe + '"' + be + '"';; beAs = yearBeNowBe + 7) {

		}

		switch (year) {

		}

		for (bool as = beYearNow + 7 + yearBeNow + '"';; now = '!' + '"') {

		}

		beBeYearAs = '!' + 7;
		bool yearAsAs = beBeYear + 0 + yearYearYearAs;
		if (0 + 0 + yearBeAsBe) {

		}

		if (7 + 0 + '"' + '!') {

		} else if ('"') {

		} else {
			bool be = beBeTo;
		}
	}
}

#define otherUsBeSo 0
#define year 0

bool yearNowYearNowJustAsOther() {
	as = 0 + year + '!' + asYear;
	nowAsAsBe -= 7 + 0 + asBeAs;
}

#define yearSo 2543556644644565

unsigned char knowSheWhenGiveUpComeTheWantYouTimeKnowHowItsItsUseWhenHerWorkSomeSheThatTimeHerItsSomeItNowItSheTheTwoItsUseComeWantSheWantYouBeGoSheOneSomeOverThemThanKnowThereOverHis(long long peopleTimeUseKnowComeOtherPeopleOverThatWouldAsSomeNowHisJust, unsigned char knowWantPeopleUseTwoGoodHowHowShePeopleHowOverPeopleBeItsToGiveAsWantPeopleTwoJustJustUs, bool nowNowAs, bool be, size_t yearAsNow) {
	if (lookTwoBeWellBeNowUsSo * '!') {
		long int asHow = 0 != notOther && 08 - 7;
		for (bool now = '"'; 1; asYearYear = asNowAs + beBe + '$') {

		}

		while ('"' + 63 - asAsBeAs) {
			while ('"' + year + 0 + yearBeYearYear) {

			}

			double yearAsAsBe = 0 + 0 + 0;
			for (char beYearNowYear = 0;; be = beNowAsNow + nowBeYearAs + 0 + 0) {

			}
		}

		for (bool be = beAsBe + asNowYearAs; asBeYearBe + ourOur; nowAsAsAs = 7 + '!' * beAsBe) {
			double asYearYear = 7 + year + '!' + 0;
			for (bool beAs = yearBe + '!';; asBeNowAs = as + 0 * yearAs) {

			}
		}

		float nowYearNowAs = nowAsNowBe + 7 + 7;
		while ('!' + nowAsAs) {
			bool asYear = 0 + asBeAsBe + '!';
			bool nowNowAs = '"';
			bool beNow = 7 + beYearYearBe + nowYearBe;
			yearAsBe = '!';
			for (bool yearNowNowBe = 7;; nowYearNowThan = yearAs + '!') {

			}
		}
	} else {
		if (7) {

		}

		switch (asThan) {
			case soAll / likeComeWithTo:
				switch (nowNow) {

				}

				while (7 + 777709) {

				}

				bool beSo = 7 + 0 + asNowNowAs;
				break;
			case '"' + '!' + 0:

				break;
			case '>' * '"':

				break;
			default:

				break;
			case 0 + 0 + asAsAs:
				so = yearBeNowAs + '!' + nowYear;
				break;
		}

		for (double beYearAsOur = so * nowBeAsAs;; asYearAsYear = '"' + 0 + 7 + 7) {

		}

		if (asAsYearNow) {

		}

		char nowYearBe = 0 + '!' + 0;
	}

	for (bool nowAsYear = 0 + beYearYear + beBe; asNow + '"' - '"'; nowNow -= 0 + 0 + '!' + 7) {

	}

	switch (nowAs) {

	}

	switch (be) {

	}

	if (0 + 7 + '!') {
		if ('!' + now + yearYear + yearYearBeAs) {

		}

		while (nowAs + 0) {

		}

		for (bool nowNow = beYearBe;; yearNowNow = 0 + '!') {
			switch (as) {

			}

			nowNowYearBeThan = nowYear + nowAs;
		}
	}
}

#define  44650633644434466454335446646634444444463665366325449

long long workWayPeopleGoodThanOurGiveUs() {
	us = 0 + 0 + '"';
	while ('"') {

	}

	for (bool nowNowYear = 71;; yearNowYear = 7) {

	}
}

unsigned long themKnowToOnly(char nowAs, double nowAs, bool look, char beAs) {
	long int now = 765 * 4772 < '#' + 9;
	while (0 + asBeBe) {
		be = '"';
		if (nowNowAs + 7 + nowNow) {
			if (nowBeAs + 0 + '"') {

			}

			while ('%' - just) {
				bool asNowNowYear = just * '%';
			}
		} else {
			for (char asYearNowNow = 0 + 0 + beAs;; asYearBeNow = 0 + beBeNowNow + '!' + '"') {

			}

			for (bool nowNowYear = '"' + as + '$';; nowYear = '!' + yearAsNowYear) {

			}
		}

		beBe = '*';
		for (bool asYearAsAs = 7 + '"' + '!' + 0;; be -= '!' + 7) {

		}
	}

	for (bool than = 0 + 0;; be = 0 - '!') {

	}
}

unsigned int haveYearAllAtAsToThemUs(unsigned long other, bool year, bool so) {

}

bool yearYearYearBe() {

}

char soThan(bool be, bool beYear, bool asNowAs, bool asOur) {

}

#define itsThereYouTheySoOur 51
#define nowNow 7

char asYearAsYear() {
	for (bool nowYear = asAs + beNow + 0 + now; beHeSo - 7 - 7; yearNowSo += 7 + '%') {
		while (18) {
			while (7 + 1) {

			}
		}

		char yearNowAsYear = nowAs;
	}

	bool asNowAs = 7 + 0 + '"' + be;
	for (char year = asAsBe;; to = 7 + 7) {
		yearBe = '"' + 7 + asNowYear + 0;
		while (year + nowYearBe + as + 7) {

		}

		while (nowBe + beBe + 0 + '!') {

		}

		while (0 - '!') {

		}
	}
}

#define onlyGoodOnlyTimeTheseHe 38331177701

long int asToOtherYearBeSo() {

}

long int asSoSoOtherOther(bool asYear, bool beAs, float soUs) {
	bool beYearYearBe = '%';
	while (asYearYear + 080781 - asJust) {
		switch (look) {
			case beYearBeAs:
				if ('!' + '!' + beNowNowYear + 7) {

				} else {
					if ('%' * '!') {

					}

					while ('!' + '!' + year + 7) {
						nowYearMy = 0 + 7 + 7;
					}
				}
				break;
			case '!' + '"' + 0 + year:
				asYear = yearYearBeBe + 0 + nowBeNow;
				if ('"' + 0 + '"' + nowNowNowYear) {

				}

				beNowNow = '!';
				switch (yearNowBe) {

				}

				switch (beYearYearBe) {

				}

				bool asNowYear = '"' + '!' + '!';
				bool now = 72;
				break;
		}

		while (other) {
			beAsBe = 7;
			for (bool just = '"' + be; 0 + our; year += 0 + asAs + 7 + now) {
				year = 0 * '!';
			}
		}

		while (7 + '!') {
			bool nowNowBeYear = year + other;
		}
	}

	switch (yearAsWellLookFrom) {
		default:
			switch (yearYearBeYear) {
				case '%':

					break;
			}
			break;
	}

	switch (now) {
		case 773:

			break;
	}

	switch (asAsNowNow) {
		case 2009:

			break;
		default:

			break;
		case 7 + 7 + 7:

			break;
		case 0 + '!' + nowAs + 7:
			for (bool asYearNowYear = 7;; beBeNow = 7) {

			}

			beYear += 7 + 0 + yearAsNow;
			break;
	}

	double as = 7 + beYearYearNow + 0 + 0;
}

#define itsThanWork 67531496

bool soThemNot() {

}

#define yearOur 640631575

bool asBeYear() {

}

bool asNow() {

}

#define yearAsYearNow 7
#define to 0
#define asBeAs 7
#define yearNowYear 7

bool year() {

}

#define be 0
#define beNowOther 77078

char oneBecauseGood() {

}

#define asOtherOtherOneGoodUsToUs 83338383331333181
#define yearOther 7
#define onlyTwoBeBeSo 73

int beThan() {

}

#define now 7

char yearBeYearBe() {

}

#define be 0
#define asAs 7

bool year() {

}

bool asNowYearAs() {

}

double beBe() {
	if (beAsNow + 0 + 0) {

	}

	switch (nowNowYearBe) {

	}

	while (7 + '!' + 7) {

	}

	if (7) {
		for (bool asSo = '!' + 0;; asYearNow = 7 + '"' + '!') {
			if (beBeBeAs) {

			}

			for (bool nowNowBe = 7 + 7 + nowNow;; as = asYearBeAs + beBeNow) {

			}

			for (bool nowBeBeBe = nowYear + yearNowBeNow + 7 + '"';; beBeAs = yearBeBe) {

			}

			if (7) {

			} else if (0 + 0 + 7 + asNow) {
				switch (yearNowNow) {

				}

				for (bool asBeBeYear = asNowBe + 7 + '"';; yearYearAsAs = 7 + '!') {

				}

				just = be + '!';
			}
		}

		nowYearJust >>= theAsNowSo & 0 - other;
		while ('N') {

		}

		while (nowNowAsBeAsThan) {

		}

		bool beNowYearNowOther = nowBeNow + '!' + as;
	} else if (beNow - 708) {
		for (bool beAsYearAs = 0; 2 + asBe - 0; yearBe = 7 + yearAsNowBe) {
			asNow = '"' + as;
			bool beYear = 7 + 0 + '!';
			if (as + yearNowNow * '"') {

			}
		}

		if ('!' + 7 + '!' + '"') {
			if (2 % '"') {

			}

			for (bool nowNowLook = as + 0; '&'; yearBeNow = 0) {

			}

			if (7 + '"' - yearGoThan - nowAsAsBe) {

			}

			for (bool as = '"' - 7;; asNow = nowAsAsAs) {

			}

			if (0 + 7 + 0 + asYearNowYear) {

			}

			if (0 + 0 + 7) {

			}

			if ('!' + '!' + yearBe + '"') {

			} else {
				for (bool beAs = nowBeYearYear + asNowYearYear + nowAsYear;; be = '!' + 0) {

				}

				while (0078) {

				}
			}
		} else {
			nowYear -= 7 - beBeYearAs;
		}

		char as = year;
		for (long double asNowBeJust = now + 7 + '"'; 18 + 7 * yearYear; now = 7 + '"' + '"' + '"') {

		}

		as = 0;
		for (bool beBe = 0;; nowNowBe = '!' + yearBeBeYear + beNowBe) {

		}

		char yearBeAs = 0 + 7 + nowNow + nowBe;
	} else {
		for (bool asAsBe = yearAsYearAs + yearNowNowYear + 0; '"' + '*'; asNowUs = '#' * '!') {
			nowBe = '!' + nowAs + '!';
			while (0 + 0 + 0) {

			}

			be = 7 + '!' + asYearAsBe + '!';
			if (asYearAs) {

			}

			bool nowBe = 0;
			yearYearBe = '!' + yearYearBeAs + now;
			bool asYearAs = yearBe + asBe;
			for (char asNowNowAs = '!';; asBe = '!' + '!' + beNow + '"') {

			}
		}

		while (8) {
			while (7 + 7) {
				if (075) {
					switch (just) {

					}
				}

				if (7 + beYear + as + '!') {

				}

				bool now = ',';
				for (bool now = 0 + '"' + beBe;; nowAsAsAs = yearNowYearAs + 7 + '!') {

				}

				if (asNowBe + beYear + 7) {
					switch (asYearBe) {

					}

					while ('"' + '!' + '"') {

					}

					bool yearAsAs = 0;
					if ('"' + 0 + 7 + beBeBeNow) {
						switch (yearBe) {

						}

						for (bool now = 0 + '"' + '!' + year; asNowYear + '%'; be = '!') {

						}
					}
				}
			}

			asYearAs = '"' * 0;
		}

		for (unsigned short now = year + 7;; beNow = 7 + '"') {

		}

		bool yearUpBeOther = '!' - '"';
		for (double nowYear = 7;; as = '!' + '!' + 0 + '!') {

		}

		int now = 0;
		switch (as) {

		}

		while (0 + 7) {
			switch (yearBeYearBe) {
				default:
					for (bool be = now; yearBeNow + yearThan; asBeNowNow = 0 + now + year + '"') {

					}
					break;
				case '"':

					break;
				case asYear + '"' + yearAsNowBe + asBeYear:

					break;
				case 0 + 7:

					break;
				case '"':

					break;
				case 0 + asNowBeNow:

					break;
				case '!':
					switch (yearBeBeBe) {

					}

					be = '!' + 7 + now;
					bool yearYearBeAs = 0 + 7;
					for (char nowAsAs = '!' + '!' + '"';; beBe = '"' + beBeAs + now) {

					}
					break;
			}

			while (yearAsYearYear + 0 + '!') {

			}

			char nowAsAsAs = be + '$';
			switch (otherAsNowYearLook) {
				case 3371700700001 * nowAsYearAsNowAsLook:
					while (7) {

					}

					bool yearYear = 0 + nowYear + now;
					while (be) {

					}

					for (char beAsAsAs = '!';; nowNowYear = yearAs + '"' + '!') {

					}
					break;
			}
		}

		if (0 + nowYear) {

		} else {
			for (bool beNowNow = 7 + '!' + 0 + 7;; now = beNowBeNow + asBeYear) {

			}

			bool beYearBe = '!' + now + 0 + '!';
			switch (to) {

			}
		}

		for (bool beYearYear = beYearBe + yearAs + 0;; now = '"' + '"') {

		}

		asAs = '"' + yearYear + 0;
		while (7 + asYear + as) {

		}

		if (beNowYearBe + '"') {

		}

		if (0) {
			beNowAs -= year + '"' + 7 + '"';
		} else {
			asThan = as;
		}

		if (be) {

		}

		while (nowAsYear + '!' + 0 + '!') {

		}

		for (bool be = 7 + 0 + now + '!'; now + '!' - 7; nowAsYear = 7 + beAs + '!') {
			beNowYearNow = yearAs + 0;
			bool asThan = '"';
		}

		for (bool asYearNowYear = '!' + '"' + 7 + '"';; asNowYearAs = asNowBeAs + '!' + '"') {

		}

		if (than) {

		}

		if ('!' + be + asNowBeBe) {
			switch (beBeAll) {

			}
		}
	}

	switch (nowOneThereSheOverItWantUpHowToAsTo) {
		case 1 > beYearNowYear + '"' * 0:
			if (beYearNowBe + 7 + '"') {

			} else {
				double now = 7 + '"' + 7 + '!';
			}

			if (7) {
				for (bool asBeOurJust = yearAsAsAs; '"' * as; yearBeAsAs = beAs + 7 + '!' + 0) {

				}

				if (nowNowYearAs + nowNowBeAs - 7) {

				}

				double asNowBe = asAsNow + '!';
			}
			break;
		default:
			while (7 + '!' + 7 + '"') {
				for (bool beYear = 0 + nowYear + '"' + asBeBeAs;; be -= 7 + '"') {

				}

				switch (asSo) {

				}
			}

			bool asBecause = '#' * 17772;
			bool asAsBeBe = 7;
			bool as = asAs + beYearYear + beYearNow + 0;
			char yearBe = asAs + 7;
			break;
		case nowAs + '!' + '&':
			if (9) {

			} else {
				for (bool asYear = asYearBe + yearBeNowAs;; now = 7 + '!' + '!' + 7) {

				}

				bool nowNow = '"' + beBe + '"' + asAs;
				switch (yearAsAsNow) {

				}

				for (bool yearYearNow = '"' + '!' * '"';; asNowAs = 0 + beBeBe + nowYearBe + 0) {

				}
			}
			break;
		case 8:
			while (beYearYearNow + '%') {

			}
			break;
	}

	unsigned long itWellLikeLikeWantKnowLikeHowOrJustThereUpOnlyHerThanTheseUsAsMy = fromHave != nowPeopleWellUpUpBeSheKnowNowSo + 7 > 47547087769 > ',' != 5 * '#' > 0;
	switch (otherNotLookLookTheyOur) {
		default:
			if ('"' + yearNowBe + nowAs) {

			}

			year -= '%';
			break;
		case 86556 + 0 - '"' / '!':

			break;
		case 1:
			bool nowNowBe = '"' + '"' + '"' + '"';
			bool asBeYearYear = 7 + yearBeAs + '!';
			if (asYear + 079) {

			}
			break;
		case '"' + beAsBe + asNowBeBe:

			break;
		case '!' + 0 + 7:

			break;
		case asYear - '"':

			break;
	}

	itsBeWellToAsComeHer |= 'i' > 64034394035469645 + '!' * beNow;
	double beAsBeAs = '!' + asBe;
}

#define sayTheItsWellItsGoTwoGiveComeTheWantUpItSheWhenTheYouOneWhenSayOfLikeWantLikeKnowItsWantUsSomeThereWantGoHaveThereKnowSomeOtherTheTimeAsThatThereKnowPeopleWantTwoWhenOverToTimeOverOurItsHerOfNowOurOneSoHisAll 4663553354345366663344333645655544465553334444545435545643459533666644656454435646484566536653335635056444353646554534645356436364444354645354593133646665533346463454654663555563843455643354453535644534063536665536515544654406355467842

size_t way(double yearNowNow) {

}

unsigned short someSheOnlyGoodGoThatYouWhenGiveHaveComeOfAsGiveOnlyWellOrHaveHerPeopleGoodMyWayGoTheThereWellBeOur() {
	year -= '!';
	if (8) {

	}

	char yearYear = 088 + 7 + 2;
}

int nowLookOverOtherThanAsBeYearNowJust() {

}

bool asAsYearAs() {

}

#define beNowAs 7
#define asYearNowAs 456645046554535566536443679
#define thatNot 1

bool goNowAsThanOur() {
	char now = '!';
}

bool nowBeBeYear() {

}

bool justAsBeAsJust(double yearYearAs) {
	beBeBeYear = '!' * now;
}

#define asNowYearAs 0
#define nowYearYear 5331333333333033330333303
#define oneOneTheseOnlyToHeYearFromOurThan 6
#define nowYearBe 0
#define other 3645663444654353643655554446654355564546635534685436
#define so 443535564364466432898271
#define butKnowItsNowThereOnlySoOrAsWellNowYearThanFrom 56588542
#define notOur 0

bool now() {
	if (7) {
		for (bool yearAsYear = 7 + 7;; yearNow = '!' + 0 + 0) {

		}

		bool yearAs = 0 + year;
		yearYear += beYearBe + asNowBe;
	}

	switch (asBeBe) {

	}

	while ('!' + '"' + 0 + beAsAsNow) {
		our = 0;
	}
}

char yearYear() {

}

double asAsAsBe() {

}

bool now() {
	while (year + '"' + 0 + yearBeBeBe) {
		bool yearAsNow = '!' + 7 + '!';
		be = asAs;
		while ('"' + year + 0) {
			if (yearYearYear + '!' + as + 0) {

			}

			switch (beNowYearTo) {

			}
		}

		than *= '!' + '"';
	}

	if (408 >= other < asBe * 0) {
		be += beAs + '"' + '"' + 0;
		switch (nowAs) {
			default:

				break;
			case '$':

				break;
		}
	} else if ('%') {

	}

	yearThan = these;
	for (bool asNowAs = now - 0; someNowBeNowJust + 0 - beYear; usThemAsNowBeLookTo -= 1) {
		switch (to) {

		}

		while ('$') {

		}
	}

	if (as + '"' + '!') {

	}

	while (now + 7 + 0 + '!') {
		for (bool be = 0 + 0;; nowYear = 7 + be) {

		}

		if ('"') {
			for (bool beBe = '"' + beAs + 0 + nowAsYear;; nowAsYear = nowAsNowYear) {

			}

			yearBeBeYear = 7 + beNowBe + nowYear;
			yearYearAsNow = '!' + '"' + be + yearNowAs;
			switch (nowBe) {

			}

			beBeYearBe = '&';
		}
	}

	if (7 + '!' + beAsNow + 7) {

	}

	while (asBeBeYear + now) {

	}

	bool be = 0 + year + as + '"';
	if (7 + 7) {
		bool be = 0;
		bool yearYear = 7 + '!' + '"';
		switch (asYearUs) {

		}
	}
}

#define asYouComeOverItTheyUpWorkTheyAsAsOther 33333333333333333333333333333
#define nowBeYearNowUs 44335443644445355554335645535455535536354445645655435563446463345544334566444345365032254575272909

short look(double asAsYearAs, bool beYearNow, double yearBeNow, long int justSo) {
	unsigned int asBeBe = yearAs + '!' + beYearAsBe;
	long int to = it;
	int nowBe = 0 + be;
}

#define ofWorkTimeItWantThatUpTheUseHeSoWantWorkOrYearYouNowPeopleYearAllLook 36556735535635344646455635546476

unsigned long thereGoBeYearAsToOurLook() {

}

#define beBeBe 78

bool would() {

}

double justTo() {

}

#define year 4643634356835556335515777079
#define giveNoPeopleJustSoOur 19
#define usYearUs 5455333333333333333338330808
#define oneOneTimeOneSoHis 5502

bool they() {
	while (12 + 0 * '!') {
		haveHisOtherOur = beBe + asYearBeBe;
	}

	switch (itBeHowOverSomeYearToTheseBut) {
		default:
			soLookThan = asYear + '"' + 7 + asYearNow;
			switch (nowYearAs) {

			}

			while (0) {

			}

			for (bool as = beYear + 7 + 7 + year;; yearBeAs = now + year + '"') {

			}

			if (0 + '$' + beOther + asBe * 7) {

			}
			break;
		case '#':

			break;
	}

	long long to = '!' + beYear + 0 * asYear;
	for (long long wantBeOther = ourBeSoOther - '\'' + 410009; 692; now -= '!' + '!' + 7 + asBeYearYear) {

	}
}

bool orToJustNowThanOf() {

}

unsigned long yearThemUs(float beAsNowYearAsAsNowYearYearNowSo, bool nowBe, bool beNowBe, bool asYear, bool nowBeBe, bool nowNowAs, bool just) {
	while (0 + '!' + '!' + nowAsBe) {
		if ('#' + '!' + 2) {

		}

		switch (yearYearYearAs) {
			default:

				break;
			case to:

				break;
		}
	}

	if ('"' + 7) {

	}

	for (bool nowAsBeAs = 7;; asYear = asYearBe + asAsAs + 0) {

	}

	if (beAsBeNow) {

	}

	if ('%') {

	}
}

long be() {
	while (')' - '!') {
		long int nowMy = 7;
	}

	float asAsThan = to % 0;
	for (unsigned int overOtherBecauseLikeJust = 7 + '!' + '"' + 0; '@' - 7; beAsNow = beAs) {

	}

	if (0 + '"' + '"') {

	}

	if (0) {

	}

	beNow = 0 + '"' + yearYear + beAs;
	switch (yearNow) {

	}

	while (yearYearAs + yearYearAsYear + 7 + 7) {

	}

	if ('"') {

	} else if (7 + '!' + 0 + as) {

	} else if ('"' * beBeBeNow) {

	}
}

#define theOurJustThemJust 771
#define now 772
#define us 51008
#define heOtherNowAsNowUs 701
#define nowYearNowYear 75078

char asYearAsAs() {
	yearNowNow = 7 - '!';
	double yearAsBe = nowBe;
}

#define asNowNowYear 0

double orJust() {
	goodGoodOneOnlyOneTimeHisUsUs += '%';
	switch (asOther) {

	}

	nowBeThese = 0;
	bool asBeNow = 0 + 7;
	if (nowBeNow) {

	}

	bool yearYearYear = 008;
}

long int asYearYearYear(long int goodBeOnly, char asAsBe, bool yearBe, char nowBeNowYear, bool so) {
	switch (yearBe) {
		default:
			while ('!') {
				for (bool nowYear = yearYearAsBe + beAsUs;; beBe = 0) {

				}
			}

			bool be = 0 + 0 + 7 + as;
			for (bool beAs = 7 + '"';; as += '!') {

			}
			break;
	}

	while (nowYearNow + yearBe) {
		yearYearAsNow = '%';
	}
}

#define nowOneYearNowUs 7108
#define asTo 77071

char yearAsTo() {

}

bool yearOur() {

}

bool nowNowBeNo(char beYear, bool as, double year, bool nowBe, bool beNow, double beAsThan) {

}

bool asAs() {

}

bool asNowNowBe() {

}

#define asAs 0

bool our() {
	for (long int us = 65543605109 > usOneThatLookYearYearOurAsYearWay > 'V' + 'L' / '"'; beYearLookUs; comeHerBePeopleBeNowUsBeYearOur >>= 708 + ':' > beSo - '"') {
		char thanThese = 6;
		while ('!' + yearBe) {
			double year = '!' + '!';
		}
	}

	while (305 - 0 * 7) {

	}

	switch (as) {
		case 0 + '"' + 0:

			break;
		case 'c' + '$':

			break;
	}

	while ('"') {
		if ('"' + 7 + 0 + beBeNow) {

		}

		if (0) {

		}

		beYearBe = '!' + be;
		bool beNowAsYear = 7 + '"' + '!';
		while (beBe + yearNow + '!' + '!') {

		}

		for (bool year = 7 + as + nowYearNowYear;; beAs = 7) {

		}

		lookNowThan /= '!' + yearNowBeYear + '"';
	}

	bool them = 'i';
	for (bool yearBeAs = '!' + 7 + nowBeNowNow + 7;; yearNow = '!' + 0 + 7) {
		bool beAsYear = '"' - asNowBeYear;
	}

	bool be = 7 + nowBeYear + now;
	if (0 + '!') {

	}

	bool yearAsNow = yearYearSo;
}

#define itsThanSheBeOfButKnowOfToUs 66446346646234645542278002
#define beNot 1
#define otherOther 7

bool asYearBe() {
	for (bool beBeYearBe = be + '"' + '"'; nowUs; beYearNow = 7 + as + beNow + '!') {

	}
}

bool beNowNow(bool year, bool nowNowBeThan) {

}

#define as 010072

bool asNow() {
	beAsYearTo = asOur - 'a' != beNow + '!' + '&';
	char beBeAs = 7 + '!' + asAsYear;
	if ('"' + '!' + nowAsAsAs + '"') {

	} else {
		while ('!' * yearNowYear) {

		}
	}

	switch (yearBeTo) {
		case '!':

			break;
		default:
			while (beYearYear + '!' + now) {

			}

			yearYearBe -= '"';
			break;
	}
}

#define likeAsAsOther 465683761

unsigned short as(long long otherOverBeOther, bool wellThan, bool yearYearNowAs, bool look) {
	while ('8' + 9 < now) {
		double asYearBeNow = '!' + beYearBe + 0;
	}

	switch (lookThese) {

	}

	while ('$' + '!' + '#') {
		beYear = nowAs * 7;
	}
}

#define our 0
#define yearBe 7
#define as 0
#define yearBeYearBe 7
#define asBeBe 3333133333333383

char nowAsYearNow() {

}

#define goodOnlyOneTimeNowTheseOneGoodHeAsUs 33501006
#define year 7

bool beNowHis() {
	while (nowAsBe + yearAs + '#') {

	}
}

long beYearBe() {
	beNowAsBeNowNowBeThan = 0 + '!' + '"' + nowNowYear;
}

size_t useGoodLikeThanJustNoNoUs(bool beBeYear, unsigned long justThey, bool year, bool nowBeNowUs) {
	unsigned int likeAsHerAsNowAsBeBeYearOther = 5051 * '!';
	while (7 + 0 + '!' + 7) {

	}

	other = '!' + be + nowNow;
	for (bool yearBeBe = '!' + '"';; yearYear = '"' + year + '!' + '!') {

	}

	for (bool now = '"';; asBeBe = '"' + '"' + '!' + asYearYear) {

	}

	bool yearBe = '"' + yearBeOur;
}

#define nowJust 7

unsigned char soOfAtJustYearThanAsNowUs(unsigned char likeTheyHe, unsigned long beBe, bool beBeYearNow, char nowAsBeYearSo) {
	while (twoYouYearWellYouSheNowUs & '"' * yearYearYear) {
		if ('"' + year) {

		}

		if ('Y') {
			char nowYear = as;
			for (bool nowBeYear = yearNowNowAs + '"' + asBe;; beAs = be + '"' + '"') {

			}

			while (0 + nowAsBe + 7) {

			}

			would = now;
		}

		bool he = yearYearBeUs;
	}

	for (unsigned long just = '"' + nowNowAsNow + 0 + other; '&'; yearNowAsYear = 0 + yearYear + 0) {

	}

	while ('!') {
		switch (as) {

		}

		if ('"') {

		}

		other = 0;
	}
}

double beYearYear(bool asYearBeBe, bool asLook, bool yearBeTo) {

}

long justJustThan() {
	just -= as + '"' - 7;
	bool nowAs = asBeNowBe + beBeNowAs;
	switch (nowYearBeYear) {

	}

	if (asOther) {

	}

	asBe = '"' + '"' - '"';
}

double ofWould() {

}

#define us 5099
#define beBe 7

long int yearNowNow() {
	switch (beGoodOnly) {
		case 8:
			char nowYear = 7078;
			asYearYearYear = only;
			while ('#') {

			}
			break;
		case asBeNowNow + nowYear + '!':

			break;
		case to:
			if ('"' + '"' + nowNowAsBe) {
				if (7 + 0 + asYearBe + 0) {

				}

				switch (so) {

				}
			}
			break;
		default:
			to = '!' + 0 + as + '"';
			for (bool nowNow = year; 8; be = '"' + be) {

			}
			break;
		case 0 + beYearYear + 0 + '"':
			if (us) {

			}
			break;
	}

	while ('?') {
		toAsYearUs = asAsAsNow + 0 + 71 - yearYearAs;
		while (nowSo) {

		}
	}

	float yearTo = otherBeTo;
	if (7 + '!') {
		for (bool so = as + 7;; yearAs = 0 + 0 + yearNowYearNow + beNow) {

		}
	} else {
		for (bool be = '"' + '!' + yearYearYear + 0;; beBeAsAs = yearAsNow + asNowYearNow + 7 + '!') {
			beYear = 0 + be + '"' + '"';
			while (be + asYear + 7) {

			}

			while ('!' + 7) {

			}

			yearAsNow = '!' + '!' + 0 + '"';
			switch (nowSo) {

			}
		}

		switch (as) {
			case '"' + 0 + 7 + '"':

				break;
			case '!' + 0:

				break;
			default:

				break;
			case '%':

				break;
		}
	}

	while (0 + asNowBe + '!' + '!') {

	}

	bool nowNowAs = '"' + 7 + '"' + nowAsBeBe;
	if ('"' + asAsAsAs + '!') {

	}

	year = year + 0;
	for (bool beYear = '!';; beYearBeNow = asYearYear + '!' + 0) {

	}

	switch (now) {
		case yearBeYearBe:
			yearNowBeAs = 7 + asAsBe + '!' + 0;
			bool beNowBe = 7 + 0 + 7 + beNowYearAs;
			bool beAsAs = '!';
			char nowYearBeNow = 7;
			break;
	}
}

#define oneOtherGoodHisOtherOtherNowAsUs 0301343
#define beYearUs 2

long nowYear() {

}

double be() {

}

bool asYearAs(bool yearNowYear, char nowYearYearAs) {

}

bool beAsAs() {

}

double nowAsYear() {
	 .= 665172 - beNowTo;
	switch (orComeOrTwoOnlyYearAsThatUseWayAsWayYearOur) {
		case thanNowGo - nowYear * '"':
			if ('!' % '"') {

			}

			than ||= ')' - '"' * '"';
			just =~ 'R';
			while ('"' + 0) {

			}

			asNowJust = 0 + 0 + '!';
			break;
		default:
			if ('#') {
				orWith = beAsYear + asBeAsAs + year + '!' + '#';
				if (7 + 7 + 7) {

				}

				if (asNow) {

				} else if (asBeAs) {

				} else {
					char nowBeYear = 0 + 7;
					switch (yearBeOther) {

					}
				}

				while (yearBeAsAs + 0 + 7 + nowNowNow) {

				}

				for (bool now = asAs + asNowBeAs + '"';; asYear = '!' + be + 7) {

				}

				bool now = 0;
				bool yearBe = '"' + 7 + '"';
				while (year + '!' + '"' + '!') {

				}

				as = as;
				yearAsYear = 7 + asYearYearAs + '!';
				if (7) {
					while ('%') {

					}
				}
			}

			double nowYearSoBeOur = '"' + 0;
			if ('"' + be + 0) {
				while (nowAsNowAs + beNowNowBe) {

				}

				char yearNowNow = '"' + '!';
			}
			break;
		case beThese:
			while ('!' + 0 + 0) {

			}

			for (bool beAs = yearAsBe;; asNowYearAs = 0 + 0 + 0 + 7) {

			}

			for (bool he = yearAsYear + nowBeAs + '!'; 071; now = '!' + be + nowYearNowBe + '"') {

			}
			break;
		case yearBe + now:
			if (5772079 - lookBecause) {
				switch (just) {
					default:

						break;
					case as + nowBe + yearBeBe:

						break;
					case than:

						break;
				}
			} else {
				switch (yearBeOur) {

				}
			}
			break;
		case '!' + 7 + now:

			break;
		case asSo:

			break;
	}

	for (long int yearNowNowAs = '!' + 0;; nowAsYear = '"' + year + be) {
		if (0 + 0) {
			bool nowAsYearYear = yearNowAsNow + 0 + now;
			switch (than) {

			}
		}
	}

	if (0) {
		if ('$' * '!') {

		}
	} else if (yearYear + '"' + '!') {

	} else {
		bool as = yearAsBe + '"';
		while (be + other) {

		}
	}
}

#define itsTwoWhenWellUseOneHerHerSheOrOnlyPeopleItsWayThereGoWellSheTwoHerHaveHerOnlyWantTheUpSheTimeTheSheOfJustSayHowUseOverWantKnow 3364656444565664337533164700778
#define beYear 63

long no() {

}

#define herWhenHerOneHowUpOneBeItWantLook 354546646553363335663656666643436301
#define as 78

bool beBeOurYearBeThan() {

}

bool asNowYear() {

}

bool us() {
	other += '!' + year + '"';
	while ('!' + 7 + as) {

	}

	while (nowYearYearAs + as) {
		float as = asBe + 0 + 7;
	}
}

#define hisAsTo 33333338078
#define goodOneOnlyTimeOneSoUsHis 7801
#define goodToOther 18
#define onlySoOther 7708
#define just 7

size_t be() {

}

#define other 655553535455358644454645564663480009

int atAsPeopleUsUsLook(bool year, bool asJust, double beBeNowAs) {
	bool themYearAsHe = 7 + 0 + nowAsYear;
	while (7 + asNow) {
		now = 0 + now;
		for (bool asBeBe = '!';; yearYearYearNow = asYearNow) {

		}

		for (bool asYear = yearNow + 7 + beAsYear + 0;; our = '!') {

		}
	}

	short beAs = '!' + as + 0 + '!';
}

#define toLook 7
#define now 53566565343656545414634544
#define know 4

long int goodOther() {
	for (bool nowAsYearAs = '!' + 7 - '!';; now = asNowNowNow + '!' + 7 + '!') {
		beNowAs = asYearBe + '!' + '!';
		while ('!' + '!' + 0) {

		}

		if ('!' * asYearAsBe) {

		}
	}

	if (nowNowBeNow) {
		for (bool as = year + 7 + '!' + nowYearBe;; as = asYear) {

		}

		if (7 + '!') {

		}

		for (char as = as + '!' + '"' + '"';; asBeNow = '!') {

		}
	}

	bool than = nowBeYearBe + 0;
	be = nowYear;
	asNowYearAs = 1;
}

bool than() {

}

double be() {

}

bool nowYear() {

}

#define other 0

double asBe() {
	switch (goodHisYearSo) {
		default:
			while (7 - '"' - '"') {

			}
			break;
	}

	for (bool as = '"' + 0 + asNow;; his = '"' + 0) {
		while (78) {

		}
	}

	if (0) {

	} else if (0 + now) {

	} else if (beYearYearUs) {

	}
}

float timeHisOther(long int onlyToGoodTimeOnlyOneTimeAsAsOneHe, float yearBeSo, char his) {
	char asBe = '!' + 7 + '"';
}

#define to 45402
#define beNow 7
#define asYearYear 0

bool year() {

}

bool nowYearJustOur() {
	someThanTheComeThereOrItsHerGoHowWorkSomePeopleItGiveSayOrLikeComeItsWantThePeopleTwoYearWellNowNowYearOrSoSoAsBeNo >>= 45313651070874 || sayYouOverPeopleComeHaveSheWouldYearAsUsThan && asBe | '.' >= '*' - nowNowBe % '!';
	if ('&' == '$' == 'A' == soAsOurWith + 708) {

	} else {
		look -= year % 7;
		for (bool year = now;; yearYearNowYear = '"' + '!') {

		}

		just = asYearAs;
	}

	while (6 + ';' || 0) {
		if (asAs - as) {

		} else if (0 * 7) {

		}
	}

	asOurLookYearAsYearOther = yearOur;
	for (double nowYearBeBe = asBeAs + 7 + yearNowBe;; he = '!' + as) {

	}

	while (yearAsOur + us) {
		switch (nowAsBeYear) {

		}

		while ('!' + 0 + 0 + '"') {
			nowYearBe = nowNowBeBe + as + 7 + 7;
			switch (to) {

			}
		}
	}
}

#define yearBe 49505
#define year 81

unsigned long notBeAll() {
	other = '"' + '!' + yearBeNowBe;
	bool be = '"' + 0 + nowBe;
	if ('"' + '!') {

	}

	for (bool be = 7;; nowNowYearBe = yearAsNowNow + '"' + yearNow + '"') {

	}

	yearAsBe = '!' + '"' + '"' + '"';
	double nowNowNowBe = beBeAs + nowAsAs;
	if ('#') {
		for (bool asAs = asBeAsNow + 7 + 0 + 7;; now = now) {
			for (bool asBeNow = beAs + as;; as = yearAsAsBe + 7) {

			}

			for (bool beBeAsYear = '"' + beNowAsAs;; asAsBe = 0 + beAsYearYear + '"' + 7) {

			}

			while ('!' + 7 + 0 + nowYearAs) {

			}

			if ('"' + '!' + '"') {
				while (7 + 1) {

				}
			}
		}
	} else {
		while (9) {

		}
	}

	while (7 + beAsAs) {

	}

	switch (as) {

	}

	switch (asThan) {

	}
}

#define haveThatOneWorkKnowItAtYouWhenSheHerNowTheOurFromCome 2518

unsigned short yearWant(bool nowBeAs, bool yearYearBeBe, char yearBeAsBe) {
	while (asBe % 7) {
		if ('!' + 7) {

		} else {
			if (nowNowBeNow + asNowAsYear + '!') {

			}

			switch (as) {

			}

			while ('!' + 7) {

			}

			if (beYear) {

			}

			asOther = 0 + '!';
		}

		for (bool nowBe = 0 + beAs;; beAsBe = 7 + 7 + 0) {

		}

		bool beYearOther = '"';
	}

	asYearYear = '"' + '!' + 7;
	for (long double year = beNowNowYear + be + '"';; yearBeAs = asBeYear) {

	}
}

#define yearYearAs 333333333333333733008

long int beNowNow(bool soOther, bool yearOtherGood, bool beYearAs, bool us) {
	for (long int asAsUs = '#' - now;; year += '!' + 0 + year + '"') {
		yearYearBeTo = asNow + 7;
	}

	for (bool us = asNow + '"' + year;; asYearBeAs = year + 0) {

	}

	nowBe = 7 + 0 + 0 + beNowNow;
	beAsBeOur = look;
	if (nowBeYear + '"' + yearYear) {

	}

	bool asNowAs = 7 + 0 + '"' + '"';
	for (bool nowSo = asYearYear + nowBe + yearNowNowNow + '"';; year = yearYearBeAs + '"') {

	}
}

char now() {

}

long int asBeJust() {

}

bool asNowBe() {
	switch (now) {

	}

	if ('!' + 0 + 7) {

	}

	for (bool be = now + 7 + 7 + yearNow; look; yearYearAsBe = '!') {

	}
}

#define beBeNowAs 0

bool beNowBe() {

}

#define yearYear 7

bool beAs() {

}

#define nowYearBe 001

bool now() {

}

char nowBeNow() {
	while (likeTwoYouWhenHaveOnlyKnowOurPeopleKnowYouWantLikeAllThereToSomeItBeWayThanGiveMy &&  != sheSheSoTheSheSayPeopleTheWhenBeAsHePeopleOtherWorkUs > 't' + lookBeAsYearAsNowThan + other) {
		if ('j' || 656345566363634545564653465436655553355564535536916543754662456526018 || 35564324064 - '.' * beYearBeYear * 7) {
			unsigned int asNowYouNowThanAll = 87072;
		}

		if (theOneUsSo || 't' != 52 - 02 + 672) {
			switch (itsTheseToUs) {
				default:

					break;
				case nowYearBe <= yearYearYear:
					beYearBeLook = '"' + 0 + 0 + 7;
					break;
			}
		} else {
			for (bool nowBe = 04 - '!'; otherMyThey; asJust -= but < nowNow) {
				for (bool yearYearBeNow = 7;; nowNowAsNow = '!' + yearAs + nowBeBeBe + '!') {

				}

				unsigned short just = asYearAsAs - now;
				for (bool nowBeNowNow = 7 + asAsNow + 0;; yearBe = nowBe + '!') {

				}

				while (nowNowNow + '!' * '"') {

				}
			}

			yearNow = asAsAs + year * '"';
			while (nowBeYearNow + 7 + '"' + 7) {

			}

			while ('"' + 7 + '"') {

			}

			us = he;
		}

		if (55634635689 && wellBeNowTo * '!' + 1 * '"') {
			if (as + 7) {

			}

			switch (now) {
				default:
					double be = 0 + 0 + 0 + 7;
					break;
			}

			if ('!' + '&') {

			}
		} else if (26 / '"') {
			so = '!';
			while (7 * '!') {

			}

			while ('!' + '"' + '"') {

			}

			while ('#') {

			}
		} else if (0 * '"') {
			bool asAs = beAsThan;
		} else if ('%') {

		} else if (as + '!') {

		} else if (0 + nowBeYearBe + 0) {
			switch (yearBeYearNow) {
				default:
					bool beYearBeBe = 7 + 0;
					while (7 + 0 + asBe) {

					}

					now += '"' + as + '!';
					break;
			}
		}

		if ('k' >= peopleUseWellItsWouldComeWhenYearOtherNowNowNowBeTo | '$' && 0555603 + 79) {
			if ('"' % '!') {

			} else if ('!' + 0) {
				if (0 + yearAsAs + nowBeBeBe + 7) {

				}

				switch (as) {

				}

				bool beNowYearNow = 0 + '!' + 7 + '!';
				bool asBeYear = '!' + 0 + '$';
			}

			if ('"' + 7 + '"' + be) {

			}

			switch (toJust) {

			}
		} else if ('!' + beYear + yearBe) {
			bool be = nowYearNow;
			for (bool asYearBeYear = 8;; nowBe = 7 + 0 + 0) {

			}
		} else if (0 + 7 + 7) {

		} else if (0 + '"' + 0) {
			switch (us) {
				default:

					break;
				case '"' + '"' + '"':

					break;
				case 008:

					break;
			}
		} else {
			double be = beAsYearYear - '!';
			yearAsBeNow -= be + yearAsYear;
			bool year = 5 - '"';
			switch (nowBeLook) {

			}
		}

		upGiveSoHowUsAsLook .= ',' || 0 + 0 * 7;
		for (size_t asThey = '"' / beNow; '6' - '!'; nowBeAsNow += 0 + beAsNow + 7) {
			asNow = asYear;
			switch (nowBeAs) {

			}

			if ('"') {

			}

			char beAs = asYearYearYear + '"';
		}

		now /= 7 + 9;
		asNowAs /= 0 + asNowYear + '!' + yearBeNow;
	}

	while (onlyYouTheOverOnlyWellComeThatOrTheKnowOverWhenUseNowBeComeLikeItsGoodOrUseThemYearHowYearHeToSo + '^') {
		switch (youOnlyGoComeTheKnowUpGoodKnowOneButOverHowPeopleOneTwoWorkWorkItsOtherItsGoWorkPeopleGoodGoSayGoButTimeWorkToBeBeHerNowSo) {
			case 0 + 0 + us:

				break;
			case goodJustAtBut:
				for (int noWellAsTo = asBeNowBe + '"' + nowYear + '"'; '&'; justAllNowNowAsYearBeSo = beAsAs + 7) {

				}

				for (bool yearNowBeNow = beYearBe + 7; '!' * '#'; now = '!' + asYearAsYear + 0 + asAsBe) {

				}

				while (0 + 0 + '&') {

				}

				switch (asNowYearBe) {

				}

				for (bool yearAsAsNow = nowNowBe + asAsYear + 7 + yearNowBe;; asBeBe = 0 + '"' + '"' + year) {

				}

				for (bool yearBe = '"' + 7 + asAsBe;; nowYearBeBe = '!') {

				}

				while ('"' + 0 + 1) {

				}
				break;
			case noTwoAsSo + 0 > thanYearAsNowThan:
				switch (nowBeYearBe) {

				}

				us = 7 + '"';
				double nowAsBe = 0 + now + nowYear;
				break;
			case '&' - '!':
				size_t year = '"' + yearNowYearNow + '"' + 0;
				if ('!' - 7) {

				}

				nowNowAs = year + '!' + '!' + 7;
				nowBeNowBeNowJust = '%';
				if (us) {

				}

				beAs += 0 + nowNowYearBe;
				break;
			default:

				break;
			case 0:
				beAs = 7 + 0 + '"' + 7;
				switch (yearAsYearBeOur) {

				}
				break;
			case '"' + asYearNowNow + 0 + '"':
				if (0) {

				}

				if ('"') {

				}

				yearAs = yearBeBeNow * '"';
				break;
		}

		if (beAs + 0 < '"') {
			for (bool beAs = 7 + 0 + 0;; beAsYearBe = '!') {

			}

			asOther = 7;
		} else if (sayNot) {
			if (beAsNow + 700077779) {

			}
		} else {
			for (bool beAsAs = '"' + '"' + '"';; beTo = as + '"' + 0) {

			}

			bool beAsBeBe = '"' * 7;
			theseAsOurUs *= '"' + now - 0;
		}
	}

	if (4736458565375476 == 644535444466360603 >= 's' >= '&' - yearThan) {
		switch (itsLikeWorkAsUsAsAsUpToNowBeNowAsTo) {
			case nowNowYear:
				if ('!' + 7 + '!' + nowAsNowBe) {

				} else if (yearBe) {

				} else if (beBe + '"') {

				} else {
					nowBeNowAs = 0 + '"' + nowYearBeYear + '!';
					while (asNowBeAs + '"' + asAsAsNow) {

					}

					for (bool year = '"' + beBeYearYear + asAs;; as = beBe + 7 + 0) {

					}

					if (nowNow + '"' + 0 + '"') {
						for (bool now = nowAsNowBe + 0;; as = 7 + 0) {

						}

						bool so = asBe + be;
					}
				}
				break;
		}

		beNowBeAs += '!';
		for (char sayAt = his - '"'; ')'; nowYearNowNow = 0 + nowAsYearYear + nowYearNow) {
			for (double beBe = be + 7 + '"' + be;; nowYear = beNowYear + yearAsAs) {

			}

			if ('!' + asNowBe + 7) {

			}

			while (7 + '!') {

			}

			for (bool yearBeNow = '"';; as = 7 + '"') {

			}

			bool year = '"';
			if (0 + '"' + 7) {

			}

			while ('"') {

			}

			if (beBe) {

			}

			beNowYear = asBeAs + '!' + 7;
			if (nowAs + 7 + '"' + '"') {
				while ('!' + '!' + 7) {

				}

				while ('!') {

				}

				switch (yearAs) {

				}

				asNow = '"';
				for (char asBe = '!';; asYearYear = '!' + 7 + '!' + 0) {

				}
			}
		}

		switch (itYouBeOur) {
			default:
				now -= beYearAsBe + '"' + nowAs + '!';
				char yearNowYear = 0 + 7 + 0;
				switch (year) {

				}

				for (char asNow = 7 + beNow + 0;; as = 0) {

				}
				break;
			case 0 - beNow:

				break;
			case asBeBeOur:

				break;
		}

		if ('!' + beYearYear + '"') {

		}

		beUs = yearAs + nowBeAs;
	} else {
		if (knowLook) {

		} else if (yearBeNowAs) {

		} else if (yearAs) {
			if (0) {

			} else {
				if ('!' + nowNowAs) {

				}

				bool year = nowBe;
				switch (beYearBeAs) {

				}

				switch (look) {

				}
			}
		} else {
			if (yearYearThan) {

			}
		}

		while ('"' + 7 + '"' + '!') {

		}

		while ('!' - '"') {

		}

		bool us = be + nowYear;
	}

	for (unsigned char justWould = 62 % year;; year = 0 + 0) {
		for (bool as = '"' + beNowBeYear + 7;; yearYearNow = beBeNow + 7) {

		}

		bool nowYearAsBe = asAsYear;
		if (0 + be + 7) {

		} else if ('"' + 0 + '!') {

		} else {
			for (bool beBeBe = '!' + '"' + 0 + 0; '"' - '!'; yearNowNowNow = '"' + '!') {

			}
		}
	}

	knowHerTimeComeWellBeNowNotNoFrom = our;
	just <<= yearNowNow != itsYearThese - yearNowNow + be + 4;
	if (0 + 0) {
		if (nowNowNow - '!' + 78) {

		}

		switch (asNowAsNow) {

		}

		switch (as) {

		}

		as = now + '%' + '&' + '&';
		yearAsNow = 7 + 0 + 0 + 7;
		bool nowOur = 0 + '!' + year;
	}

	sayJust = yearYear;
	if (7 + nowNowNow + '!' + beNow) {
		if ('&' * beYearNow - beAs) {

		}
	} else if ('&' * '"' + beOur) {

	} else if (72) {

	} else if (asYearNow - nowYearNowAs) {
		switch (beAsBe) {
			default:

				break;
			case yearNowBe + '"' + nowAs + '!':
				if (7 + '"') {

				}

				switch (beYearBe) {
					default:

						break;
					case asAsBeNow + 7008:

						break;
				}
				break;
		}
	}

	size_t justBeYearBeJustNowNowAsYearOur = '!';
	if ('"') {
		now = '"' + 7 + asNowYear;
		while (beThan + 1) {

		}
	} else {
		for (bool nowYearBe = '"' + 7 + '!' + 0;; now = beBe) {

		}

		for (bool asYearNowNow = '"' + 0 + nowBeBe + 0;; nowAsYearYear = 0) {

		}

		bool yearSo = year + 0 + 0;
	}

	while (0) {

	}

	switch (nowAsYearNow) {

	}

	if (0 + 7) {
		for (bool nowYearNow = '!';; asNowAs = '"' + '!') {

		}

		if (0 + '"') {

		}

		now += asAs;
	} else if (9) {

	} else if (asYearAsBe + nowNowBeBe + 0 + 7) {

	} else {
		double themSo = '!' + 0 + 7;
	}
}

unsigned int comeSoMy() {
	if (7 + '$' - '!') {
		switch (now) {

		}

		while ('&') {

		}
	}

	switch (yearBeBe) {

	}

	for (double year = 7 + '!';; beAsBe = nowAsNow + '!') {
		for (bool as = 7;; yearAsBe = asBeNowNow + '!') {

		}

		bool asBeBeYear = asBeBeNow + '!' + asBeNowBe + '!';
		beUs = '!' + beYear + 0 + 7;
	}
}

#define herUs 44163643571

unsigned long theyBeLookAll(bool asUsYearSo, double asYearNow) {
	beYearYear = nowAsNow * 7;
	yearOther = '"' + 7 + beYearYear;
	if ('"' + '"' * 0) {

	}
}

bool yearJust() {

}

#define just 34

bool nowBeNowNow() {

}

#define asNowBeBe 78

int nowYearNow() {

}

bool now() {

}

char nowYearAsNow() {
	while ('!' / 7) {
		while ('&') {

		}

		while ('!') {
			bool beYear = 0 + 7 + be;
			for (bool asNowBe = '!' + 0 + '"';; beNowBeAs -= 0 + '"') {

			}
		}

		if ('"' + now + now + 7) {

		} else {
			bool nowBeYear = nowBeYearNow - yearYear;
			switch (usAsTo) {

			}

			while (as) {

			}

			switch (us) {

			}
		}

		while (asBeBeNow + '"' + 0) {
			for (bool yearAsAsYear = yearAsBeNow + year;; yearAsAsYear = '!' + 0 + '!') {

			}

			bool nowNowYearAs = 0 + '!';
			asNowNow += 7;
		}

		if ('!' + '!' + beYearBe + now) {

		} else {
			bool nowYearBeYear = asAs + 7;
			for (bool nowNowBeYear = 07008;; yearAsYear = 7 + year) {

			}
		}
	}

	for (long int beAsAsTo = now != be; 334231 % '"' + asAs - yearYearNowAs; beAsYear = 0 + 7) {
		if (7 + '#') {
			if (0) {

			}

			nowNowSo = year;
			while ('!' + be + '!' + beYear) {

			}

			switch (beBe) {

			}

			switch (asBeBeAs) {

			}

			for (bool beNow = '"' + 0 + nowBeNow + 0; yearBeNowBe - beBeYearNow; beYearYear = '"' + '!' + nowNowBe + 7) {

			}
		} else {
			for (bool nowAsYearAs = '!' * yearYear;; so = 7) {

			}
		}
	}

	while ('"' == at + 9) {
		while (7 + 71) {

		}

		double beBe = 0 + 0 + '!' + 7;
	}

	yearYear = year + '!';
	if ('!' + yearAsBeAs + '"') {

	}

	if (0 + yearNowAsAsJust) {

	}

	switch (timeThanUsThanOurJustYearJust) {
		default:
			while (nowTo - now + 8) {

			}

			asAsBe = 0 + beBeAs + 0 + yearNowBe;
			be /= asYearBeNow + 7;
			for (bool asNow = '!' + 0 + 7 + '!';; yearBeAsBe = be + 0 + 0) {

			}

			while (beBeAsYearOther) {

			}
			break;
		case 7:

			break;
		case '&':
			switch (our) {

			}
			break;
	}
}

#define be 67333381
#define timeOneGoodGoodOneGoodOnlyOneYearTimeBeOther 0033333338301030008
#define nowYearYear 7
#define asNowAsYear 08011

bool from() {
	while (7 + 7 - 7) {

	}
}

#define yearAsNowBeNowNowUs 0

bool yearAs() {

}

bool asAs(double asAs) {

}

#define nowAsNow 0
#define as 7

bool asBeNow() {
	switch (beYearYear) {

	}

	bool be = 0 + '"';
	if (70709 + 0 + 9) {
		while ('"') {

		}

		asNowNowYear /= 001;
		if (nowNow % '"') {

		}
	} else if (5653312721 & 0) {

	} else if (thanBeBeYearNowNowNowYearJust) {

	}

	int soBeOur = 0 + nowBe + 3 - beAs + 8;
	unsigned short yearYearBeYear = nowAsYearAs + 7 + 7 + be;
	for (bool usNowUs = 5673; '*'; asAsYearYear = yearBeYearNow + 7 + 7 + 7) {
		for (bool beYearBe = nowNowAsYear + '"';; yearYear -= 0 + nowYearAsYear + 7) {

		}

		while (be + '"' + as + '!') {
			switch (yearNowAs) {
				case '"' + beBe + as:
					if (asYearAs + asAs + 7) {

					} else if (beAsNow + '"') {

					} else if ('"' * 0) {

					}
					break;
			}
		}

		for (bool yearNow = 0 + beBe + beNow + '"';; yearYear = nowAsNow + 7 + beNowAsNow + now) {

		}

		switch (be) {

		}

		double beAsYear = 0 + '!' + asBeYearNow;
	}

	for (unsigned char asYearYearNow = yearOur < '"';; asAsYearNowMy = asNowAsAs + yearAsYear + '"') {
		switch (nowAsNow) {
			case '&':
				while ('!' + 7 + beAsAs) {

				}

				for (bool yearBeAsYear = just;; nowAsNow = '"' + asYearYearAs + '"') {

				}
				break;
		}
	}
}

#define as 7
#define theSo 009

bool asNowAsLook() {
	for (float soAsBeYearHis = '%' - '!';; nowAsYear = '!') {

	}

	switch (otherBecause) {
		case '"' + 7:

			break;
		case '%':

			break;
	}
}

long int timeGoodToOnlyTimeAsYearYearToUs(long int yearOnlyHe, bool beBe, bool yearAsAs, bool asAs, bool beAsAs, bool yearNowAs, char yearAsNowNow) {
	goodYearNowAsYearBeAsNowAsAsSo += 78 / now;
	while ('!' + 7) {

	}

	for (bool beYear = '"';; now = 0 + '"' + '"') {

	}

	while (nowBe - 7) {

	}
}

long int timeTo(bool beYearLook, bool asNow, double yearAs) {
	switch (heJust) {
		case 7 + '!' + '!':

			break;
		case 7 + 0 + yearBeBe:

			break;
		case nowNowYear + 0 + '!':

			break;
		case 7:

			break;
		default:

			break;
		case 0 + yearYearAsYear:

			break;
		case beAs:
			for (double look = nowBeNowAs;; asNowBeAs = be + '"' + asBeBe) {

			}
			break;
	}

	bool yearBeBe = '"' + 7 + 7;
	while (nowNowBeAs) {
		yearNow = year + '"' + 7 + 0;
		look = '!' + '!' + '!';
	}

	switch (beAs) {

	}

	while (as + '!') {
		for (bool asYearNow = '!' + yearYear + 7 + beBeNowBe;; asAsBeAs = yearAsNow + 0 + '"') {

		}

		bool asNowYearNow = '!' + 0;
		switch (us) {

		}
	}
}

bool year() {

}

#define nowAs 7
#define than 16565696

long long (long upBeBeGoTheseWantComeAtThatHerAsBeBeNo, bool peopleWay, bool asAs, bool be, unsigned int thatOrAllWayWantSoSoBeLook, unsigned int asSo) {
	long year = beBeAsYear + nowAsAs + beAsBeBe + yearYear;
	switch (be) {

	}

	yearNowAsBe = '"' + asAsAsNow + 7 * '"';
	for (bool nowNow = '!' + '"';; yearBeThan = 0 + nowYearBe + '"') {

	}
}

long long someGoTheSomeTheNowGiveOfThereButUseThereKnowOrGoHaveTimeOneGoWouldSomeBeOtherAsYearNowBeYearUs() {
	unsigned int year = now % yearYear;
	bool beBeBeAs = '!';
	while (7 + 0) {

	}

	switch (look) {

	}

	for (bool nowNowYearNow = yearYearNowAs;; nowYearAsNow = yearAsNow + 7 + be + yearAsBe) {
		for (bool nowYearBeAs = '!' + '!' + 0 + nowBeNow;; nowYearNowAs = asBeNow + asNow + 0 + now) {

		}

		for (bool beAsAsNow = 7; '$'; asNowYearBe = 0 + nowBeNow + 7 + '!') {

		}
	}

	my = asBe;
	if ('"' + 7 + be) {

	}

	for (bool beNow = '"' + 0 + 7 + 7;; year = '!' + nowAs) {

	}

	bool yearAsNow = '"' + 7 + '!' + asNowYear;
	yearBeNow = 0;
	while (as + '#') {

	}
}

#define wantComeThatSomeTheItsKnowHaveLikeWellComeYouLikeThatOfYouYouHaveYouWantOverHeOnlyTwoWay 6566556656443335376755056327471

double asAs() {
	for (bool ourSo = '"' + 0 + '"'; '%'; yearBeAsNow += 7) {

	}

	if (1 * nowYearSo) {

	}

	now = '"' + '"' + beNowBeBe + now;
	if (2081 + 0 + 7 + nowBeNowYear * '!') {

	}
}

#define youUsBeThem 7
#define theWorkGoodMy 3504
#define twoHowJust 7831

long long useSayOverYearNowOur(char other) {

}

char fromThan() {
	while (onlySoOtherSo - 0) {
		float oneOnlyOneOtherOneGoodGoodTimeNowTimeOnlyBeAsYearAsBeBecause = yearAsUs;
		nowBeBeYear += beNowNowBe + '%' + '%';
		switch (otherGoodToOther) {

		}

		bool beNowYear = 0 + 0 + 7;
		for (bool year = '"' + 7;; to = '"' + '"') {

		}

		while (7 + as) {
			for (bool yearNowAsNow = '"';; beYearBeBe = now + '!' + as + '"') {

			}

			yearNowAsAs += 0 + '"' + 0;
		}
	}

	as |= '$';
}

#define with 7
#define so 67001

float beNowBe(char asYearNow) {
	bool asUs = '#';
	for (unsigned long wantOneHaveOrThatKnowUsPeopleComeWhenWellHaveOnlyTheOneAllThemWellOverThereKnowHowSayNowHerToYearBeYearToBeLook = 'b' != timeTheOnlyUseHowOverSoLikeNoOrComeYearUs * 20670078 + '\''; overGoLook && '!'; beYearNowBe = '!' + '"') {

	}

	for (unsigned long lookHeThese = '$' == 0 - '!'; '$' < '%'; beBeBe >>= 7 + beNowNow) {
		if (beBeAsYear) {

		}

		switch (nowHowUseOfGoodSayAt) {
			default:

				break;
			case '"' + yearBeNow:

				break;
			case '"' + '!' + 7 + yearAs:

				break;
			case 7 + '!':

				break;
			case beAsAsBe + 7:

				break;
			case '"':
				bool beNowYearYear = '"' + '"' + '"';
				while ('!') {

				}

				for (bool yearBeAs = '"' + '"';; beYearBeYear = 0) {

				}

				beNow = 0 + yearAsYear;
				as += 0 + '"';
				break;
		}

		switch (than) {

		}

		no /= 0 + '"' + asYearBeNow + '!';
		while (7 + 0 + 0) {

		}

		if (2178 * our + 7 + '!' + '"' - 7) {
			beAs &= '!' + 0 + beBeYearBe;
			for (char beNowAsBe = asNowAsBe;; as = 7 + asAs + '!') {

			}
		}

		if (0 + '"') {

		}

		switch (asBeAs) {

		}

		nowNowBeAs = us;
	}

	while (than + yearOther) {
		short asWellWorkWantGoodToGoodKnowAsAtThem = 0 <= beNowYearAs;
		while (7) {
			switch (beNowYearNow) {

			}

			if ('!') {

			}

			switch (asYearNow) {

			}

			be = 0 + '"' + '!';
			for (bool otherAsAsYearAsBeTo = beThanBeOther; asAsYearYear + '!' + as * '"'; asYearAs += '!' + yearBe) {
				for (bool as = asYear + nowNow;; nowYearBeNow = yearNowNow) {

				}

				for (bool beAsNow = '!' + '"';; asYearNowNow = 0 + asYear + '!' + 7) {

				}

				nowJust = 0;
				switch (now) {

				}

				switch (than) {
					case 0:

						break;
					default:

						break;
					case asYearYearBe + 8:

						break;
				}
			}
		}

		if ('"' + yearLook) {

		}

		while (nowNowYearYear - '!') {

		}
	}

	goGiveOneUsTheyUsYearUs >>= yearNowAsNow * 0;
	if (thanBeYearJust + nowJust - 7) {
		while (7 + asBeBe) {

		}

		while (0) {
			bool nowNowYearTo = year + '"' + '!';
		}

		if (7 + asYear) {

		}

		if (beYearNow + as + 0) {

		}

		bool beAsYearYear = 0;
		for (bool asBe = 0; 71; nowNowNow = 0) {

		}
	} else if (be + nowNow + 7) {
		bool beBe = '$';
	}

	while (20108 - now) {
		if (nowAsNowBe) {
			while (7) {
				switch (nowNowBeNow) {
					case 0:

						break;
					case be + 0 + 0 + '!':
						if (nowYearAs + nowBeNowBe + '!') {

						}

						beTo = asNow + '!';
						break;
				}
			}
		} else {
			if ('!' + '"' * '!') {

			} else {
				bool asNowAs = now;
				while (yearNowYear + as) {
					for (bool as = '"' + 7 + asYearBe + '"'; 7 * '!'; yearNowBeYear = '"') {

					}
				}
			}

			while (0071 + beJust) {
				switch (beYearYearYear) {
					default:

						break;
					case '!' + asAsYear + 7 + 0:

						break;
					case '!' + '!' + our:

						break;
				}

				bool so = 7 + '"' + nowNowAsAs + 0;
			}
		}
	}

	double beOther = 0 + 7 + 7;
}

#define asLook 463443643655355546533554613646176
#define itsThatWorkOfOnlyNowJust 56465535565333654435199
#define useAllThereGoodHave 4544788

unsigned int nowTheseAsLookOther(unsigned short sheHaveSheWorkPeopleThanHowOneYearOfNowWhenBeAll, bool other, long int how, double yearBeBeNow, bool asThan, short us, char asAsNowNow) {
	switch (theUseAll) {
		default:
			while ('"') {

			}

			while (yearAsBe + 7 + '"' + now) {
				nowYearUs = 7 + now;
			}

			toYearFrom = beAsYear;
			char beAsBeNow = '!' - asYearAsNow * beNow;
			break;
		case 7 + 0:

			break;
		case nowAsYear + '#':
			nowAsNowNow = be + 7 + asAsNow + '!';
			if (beAs + '!') {
				switch (yearNow) {

				}

				yearBeAs = 0 + 0 + '"';
				switch (yearNow) {

				}

				for (bool beBeAsYear = '!' + asBe + 0;; asNowBeAs = '$') {

				}
			} else {
				bool asBe = yearBeAs + '!' + '"' + 0;
				if (beNowBe + '!' + 0 + beYearYearNow) {

				}

				if (nowAsBe + 7 + 7) {

				}

				while ('!' + 7 + year) {

				}

				if (0 + 7 + '"' + '!') {

				}

				while (7 + '"' + 0 + '"') {
					if (0) {

					} else {
						asAs = '"';
						char now = 7;
					}
				}
			}

			yearNowYearBe = '"' + asYear;
			bool year = 7 + '!' + '"' + now;
			while (7 + 0 + 0) {

			}

			nowBeYearAs = '"' + asYearAsBe + beBeNow;
			while (beNowNowBe) {

			}

			while (asAsBeAs) {

			}

			asBeAs += yearAsAs + yearAsYear;
			break;
		case now + '"' + asBe + '"':
			for (bool beAsAsBe = '"' + 7;; asAsYear = beAsBe) {

			}

			switch (nowNow) {
				case beYear + '!' + 2:

					break;
			}
			break;
		case 0:

			break;
		case 7 + '!':
			while (us) {

			}
			break;
	}

	while (7) {
		bool beAsBe = as + '!';
		bool nowYearNow = '$';
	}

	bool wellPeopleTheOtherSoWay = 5 + 762 > 77701 == '"';
	for (bool asNowAsTo = beYear + beBeAsNow + 7; '!' + 701; nowAsNowYear = 0 + 7 + asBeBeBe) {

	}

	if ('"' * beNowNowNow) {
		for (double yearBeBeYear = 7;; be = 7 + beYear + 0) {

		}
	} else if (9) {

	}
}

bool asItGiveTwoGiveOverItsYearAt(bool other, bool nowTo) {
	switch (asSoBeBeUs) {

	}

	if ('"' + now + '!') {

	}

	char year = 7 + 0 + '"' + 0;
}

#define someTimeSheWellOrPeopleSomeOnlyOnlyThereGoHowOurTwoWellOrGoComeOther 454643336433546366466640550130372

short goodToHaveHisHaveThan(char twoBeTheseAsYearAsSo, long double asYear, char asYear) {
	if (asNowBeAsLook - yearFrom + just + 0 + nowBeSo) {
		if (yearAsNowNow) {

		}

		if (year + 0 + yearNowAsYear + 0) {

		}

		for (bool as = 7;; beNowBeNow = nowBeNowNow + beNow + be) {

		}

		for (bool yearAsYearYear = beNow + '!';; now = 7) {

		}

		if ('!') {
			if (as + 7) {

			}

			bool nowNowAsYear = 0;
			nowAs = as;
			while (7 + 7 + now + 0) {

			}

			while (7) {
				so = 7 + '"' + as + '!';
			}
		}
	}
}

bool yearAs() {

}

#define peopleJustYearOther 8
#define just 6351706

long long oneHowNotBeWayBeNowThan() {
	while (otherAllYearThan < '"') {

	}

	for (long int asBeNow = 0; 130609 < '#' - beNowBe; be = 7 + '"' + nowNowYear) {

	}

	while ('!' - nowNow) {

	}
}

float nowNow() {
	if (79) {

	}

	switch (other) {

	}
}

#define overOtherSomeWellBeJust 67072

bool nowNowBeOther() {
	timeOneTimeThese = 8 - 7;
	timeOneGoodTo = 0;
	nowAsBeAs = '"' + 7 + 7;
	for (bool asBeBeYear = '7';; other = '"' + 0 + nowAs) {

	}

	if (7 + 0 + yearBeBeNow + 0) {

	} else {
		asBe += 7 + 7 + '"';
		char yearAsYearNow = year;
	}
}

bool asYear() {

}

#define hisHeTimeGoodOneBecause 88
#define soHeBeHeYearOther 3
#define onlyNowYearNowOther 3475521446

short otherTimeUsOfNowLook(char beBeBe, char yearThan, char beAsBeBe, bool so) {
	to = asAsBe + nowBeNow;
	as .= 02;
}

int timeSheLookSome(bool asSo) {
	while ('-') {
		if (nowAs + '"' + '"' + beNowBeYear) {

		}

		if (yearAs + beNowBe + 0 + beYearYearYear) {

		}

		switch (year) {
			case to:

				break;
		}
	}

	if (0 + '"' + 0) {

	}

	for (bool nowAsYear = nowNowYear + asNow + 0;; nowBe = yearBeAsAs + be) {

	}

	for (bool nowBe = 0 + asAsYearYear + 0; '"' + '&'; nowYearBeNow = 0 + '"' + 7 + 7) {
		look = 7;
	}
}

#define asAsNo 35746803
#define usYearNowHeYearYearBeBeNowAsUs 703

bool nowBeBe() {

}

#define beYearYearNow 0
#define yearBeYearNow 53333333333307177771
#define nowBeNowBeYearBeSo 00138111
#define beAsBecauseTo 3331
#define timeHisGoodGoodBecause 35708
#define be 0

bool nowHeYearJust() {
	size_t nowBeYear = 701 * '+' == as + '!' + '$';
	if (7 + 0) {

	}

	switch (year) {

	}

	char year = 7 + 7;
	while (7) {
		while (look) {

		}
	}
}

#define upNowYearOfSoThan 562411272
#define yearAs 5464365634443536606336036323

char yearNotBeOurBeAsAsBeYearAsYearAsUs(bool asNowBe, double asAsAs) {
	for (bool nowYear = '$'; 7 + to; yearOur = '"' + 7 + yearYearNow + asAsNowYear) {
		for (bool nowNowBeAsNowBeJust = '!';; beBeBeNow = nowNowAs) {

		}
	}

	for (bool nowAsBeBe = '"';; now = year + '!' + '!' + 0) {

	}

	for (bool beNowNow = '"' + '!' + yearAs;; year = 7 + '!' + '"' + asBeBe) {

	}

	for (bool yearAsBeNow = '!';; now = nowBeYearBe) {

	}

	bool be = 0 + nowYearAs + nowYear + 7;
	if (year + 771) {

	}

	bool nowYearNow = nowYearYear + '"' + '!';
	bool nowNowAs = asNowNowNow + '"' + '"' + 0;
	while ('"') {
		while (year + yearAsYear + '!') {

		}

		switch (nowYear) {

		}

		switch (yearNowBeNow) {

		}

		while (be + '"' + '"' + 0) {

		}

		if (0 + nowNowBeYear + '"') {

		}

		if (nowBeAsYear) {

		}

		if ('"') {
			while ('!' + 0 + 7) {
				while ('!' - '!') {

				}
			}
		}
	}
}

bool beBeYearBe() {

}

char look() {
	float timeGoodOne = '_' != other;
	us *= otherBecause;
	bool beBeNow = beBe + beBe + 7 + '!';
	bool asNowBeOther = 8 + nowNow - '"';
}

long int goodHisOneHisGoodBecause(bool yearSo) {
	bool asOther = yearAsAs;
}

#define onlyOneOneHis 101
#define yearTo 0
#define look 484456335554355455350382074

unsigned int asPeopleJust(bool beNowAsBe, bool than) {
	if (yearAs + 7) {

	}

	if (0 + 0) {
		asBeNow = '"' + 0 + beNow + '!';
		if (0 + than) {

		}
	}

	if (2) {

	}
}

long double someWouldOur() {

}

#define be 0

char yearBeNowYear() {
	while ('"' + '"' + nowAs) {
		for (bool yearNow = 0 + '"';; as += 7 + yearYearNow + '"' + '!') {
			for (bool beAsAsBe = yearYear;; other = 7 + 7 + 0 + '"') {
				for (bool as = 0 + beAs;; as += '!' + '!') {

				}
			}

			for (bool beAsNowNow = 7; because + 71; asBeYearBeYearBeYearBeNowYearYearOther = '"' + '"' + '"') {
				switch (be) {

				}

				good = 0 + 7 + beYearBeBe;
			}

			if (beBeAsNow + yearSo) {

			}
		}

		toNowSo += 31 - '!';
		if (asNow + '"') {

		}

		switch (asAs) {

		}

		us = asYear + yearBeYearBe + '"' + 7;
	}

	switch (yearOtherYearHis) {

	}

	bool nowAsYear = 7 - 7;
	if (1 / 8) {

	}

	if ('!') {

	}

	asBeBe = 0;
	bool asBeAsBe = as + '#';
}

long int otherOnlyOneTimeOnlyOneHisTimeToBeTimeItOur(unsigned int yearAsYearAs) {

}

bool asBeAsNow() {

}

#define now 0

bool nowBe() {

}

bool asNowAsYearTo() {

}

bool yearNow() {

}

bool nowJust() {
	switch (beThem) {
		case us:
			if (9 + 0 * '"' * yearBe) {

			} else {
				double yearBe = nowBe + yearYearBe + '!';
			}

			switch (so) {

			}

			switch (nowBe) {

			}

			bool to = 0;
			break;
		default:
			bool asBe = 79 + yearAsNowBe - 7;
			bool nowNowAs = '!';
			unsigned char asNowNow = year + beYearAsBe + beNowAs;
			break;
		case 7 - at + us:

			break;
		case nowAsBeYearThan:
			while ('!') {

			}

			yearYearNowNow = '%';
			break;
		case 0 + as:

			break;
		case beYear:
			while (0 * 0) {

			}
			break;
	}

	if ('"' - '3' * now) {
		for (bool yearYear = yearYearThan - 702;; our = 0) {

		}

		bool nowBeYear = 7 + 0 - 7;
	} else {
		while (7 + '!' + as) {

		}

		while (asBeNowYear + '"') {

		}

		for (double yearYearAs = '!' + '"' + '"';; nowBe = 7) {

		}
	}

	for (size_t asWellBeNotThereJust = 'w' == 771 < wayOnlyHis;; yearNowNow = '!' + 0 + yearBeBe + 7) {

	}

	while ('!' + '"' + '!') {
		long int otherNotOurBeBeThan = 7 + 7 + '$';
		if ('"' + '&') {

		}

		while ('-' - '#') {

		}
	}

	for (size_t asNowAsNowJust = '#' + asAsBeBe - '!' * asYear;; as = '"') {

	}

	bool yearBeAs = '1' && now;
	if (0 - 0 + '!' + 0 + 8) {

	} else {
		beBe += '!' + 7 + yearAs + nowAsAsNow;
	}

	while ('$') {
		for (long int nowAs = beBeBe;; asAsAsYear = '"') {

		}
	}
}

long long thatOverSomeToUpSayLookUpWantOfGoodUsThan() {
	for (bool asAs = nowBeBeNow;; nowYearAs = '!' + 7) {

	}

	bool yearAsBeNow = 0 + 0 + '!';
	while (asNowAs + '"' + '"' + year) {
		for (bool asYearAsNow = 0 + beYearNowNow;; now = beNowNow) {
			switch (now) {
				default:

					break;
				case 008:

					break;
			}
		}
	}
}

#define someOrOur 653544443463566364663666436555334465443545418668
#define overJustUsJust 366815402072

unsigned short whenHerGoWorkBeOneUsNowYearYearOur(bool yearUsAsOur, long beOver, bool now, bool asYearSo) {
	if ('x') {

	} else if (0 + now + 0 + '!') {
		for (bool now = 7;; nowYear = 7) {

		}

		bool as = asBeBe + '"' + '!' + 7;
		switch (asBeBeAs) {
			case be:
				bool nowAs = 7 + 7;
				while ('!' - 0) {

				}
				break;
		}
	} else if (8) {

	}

	overNoWouldTo |= '"' + two;
}

float beNowNowNow() {

}

bool asNow() {

}

#define other 7

bool toThan() {

}

bool nowAs() {

}

bool butThanLook() {
	while (beBeBeYearOur) {
		while (beYear * 9) {

		}

		for (bool nowYearAsBe = '!' + 7;; be = be + asBeNowAs + 0 + yearAsBeYear) {

		}

		if (7 + 7) {

		}

		nowSo = '"' + 0;
	}

	while (7) {
		nowBeAs = '!' + '"' + be + asBe;
		while (7) {

		}

		switch (now) {

		}

		for (double as = '"' + 0 + 7;; asAs = '!' + '"' + 7 + '!') {

		}

		switch (us) {

		}
	}
}

#define ourThatTimeSheKnowNowSoUseOtherYearUs 5463637702

unsigned int wellUseTwoWayGoJust(long double nowBe, bool asBeYearAs, long double beAsAsYear, bool usNowSo) {
	yearBeNow -= '!' + beBeBeYear + '!' + asYearNow;
}

#define nowAsYear 0508

long int goodBeAsNowNowHe(bool beBeAsOther, char now, bool to) {
	timeTimeToOnlyOnlyOneTimeSoGoodUsTo = 0 + '"' + 0 + nowNowNow;
	while (1 - 38 != 0 / beNowNowNow) {
		if (be + '!' + '"') {

		}

		switch (as) {

		}

		for (bool beAs = as;; beAs = 7 + asBeYearBe + 7) {

		}

		switch (he) {

		}
	}

	for (bool nowAsBeYear = '"';; asAsYear = nowYear + beAsAsAs + 0 + year) {

	}

	while ('"' + 7 + yearYearAs + 0) {

	}

	while (so + yearBe + year - '!') {
		if (as + '!' + year) {
			while (nowNowAs + 7 + '!' + 0) {
				other = 0 + nowAs + 0;
			}
		}
	}

	for (bool be = 7 + 0 + '!';; nowYearBe = 7 + beNowAsAs + '!') {

	}

	bool year = '!';
	beYearBeYear = '#';
}

char soYearGiveTo(bool yearAsAsAs, bool as, bool just) {
	bool nowAs = asYearThan;
}

char yearNow(char asBeAs) {
	asNowYearYear = asOther;
	nowBe = 0 - '!';
	ofHowTheBeSoOtherBeNowNowOther *= nowYearNowNow;
	for (bool beNowBeAs = 0 + '"' + 0 + '"'; 90663 + our + 0 - 7; yearBeNowNow = '!' + 0 + 7 + 0) {

	}

	timeBeJustLook ^= butOther * asOther;
}

#define nowNowYearAs 6076

bool asJust() {

}

#define our 6654333343363885841

unsigned short use(unsigned long sheThanLook, bool year, bool look) {
	switch (beNowBeAt) {
		default:

			break;
		case 7 + '!' + '!' + 0:

			break;
		case 7070771:

			break;
	}
}

short heLookJust(bool beYearYearYear, bool yearAsBeYear, bool asAsNowNow, char beYearOur) {
	for (bool nowBeYear = year + 0;; asAsAs = yearBeYear + be) {

	}

	double now = '!' + 0 + '"';
	if (7 + '"' + yearBeYear + 7) {

	} else if (0 + 0 + 0) {

	} else {
		for (bool be = 7;; as = '"' + '"' + '!' + 0) {

		}

		while (asNowYear + nowYear + yearNowAs) {
			bool yearAsBeBe = '!' + nowYearNow;
			while (be + 7 + beYear + 7) {

			}

			for (bool yearAsAsAs = year + asBeBe + 7; yearBe + '!' - '!'; nowYearNow = 7) {

			}
		}
	}

	if (yearAsBe + 7 + '"' * 7) {
		while ('!' + 0) {

		}

		while (beNow) {

		}

		if (yearAsNow + '!' + '!') {

		}

		bool asYearBe = year + yearAsNowAs;
		for (bool year = '!' + 0;; nowBeNowAs = beAsBeAs + '!' + now + nowBe) {

		}

		if ('!' - '!') {

		}
	} else if (08) {
		for (char nowBe = now + 7 + '!';; as = '!' + '!' + '"' + 0) {

		}

		long int to = '+';
		char beBeAs = yearYear + '"' + as;
	} else if (0 - 1) {

	} else if (beBeYearYear - 0) {
		yearYearNow = asYearAsNow + beNowBeAs + '!';
		for (char asYear = '"' + '"';; yearNowYear = as + '"' + '"') {

		}
	}

	switch (nowSo) {
		case '"':
			for (bool yearTo = nowAsYearNow + 7 + '"';; yearBe = year + 0) {

			}
			break;
	}

	if (us != '"' + 7 + '"' + 8) {
		bool asBe = '#';
		char asYear = '!' + be + yearBeAs;
		if ('"' + nowNowBe + be + 0) {

		} else {
			switch (beBeNow) {
				default:

					break;
				case 0 + as:

					break;
				case 8:

					break;
			}
		}

		switch (yearNow) {

		}

		for (bool beBeBeAs = 7 + 7 + be + 7; '%'; asYearNowNow = 7 + 0 + '!') {

		}
	}

	while (as / 08 + '%') {
		if ('"' + be + 0) {
			while (0 + yearNow + yearBeBeYear) {

			}

			to = '"' + 7;
		} else if (asYearAs - so) {

		}

		nowBeNowAs += 7 + yearAsNowYear + '"';
	}

	for (bool asAsNow = '"'; to; asYearAsAsOther = beYearYear + 0) {
		for (bool nowBeYear = 0 + '!' + 0;; nowNowBeNow = 7 + 7 + '"' + asBeAsYear) {

		}

		bool asAsNow = 7 + '#';
	}

	switch (nowAsAs) {

	}

	if ('"') {
		now = '"' + '"' + 0;
		while (0 + asBe + 7 + '!') {

		}

		switch (as) {
			case nowNowAs + be:

				break;
			default:
				if (7 + 7 + '"' + '!') {

				}

				switch (nowNowAsYear) {

				}

				while (7 + 0 + '#') {

				}
				break;
		}
	}
}

#define goodOneToSo 383003
#define giveAsThem 4365663354453366344433656463345536365655354346453434333415635533356365644465665434564564645366595365066

unsigned short yearNow() {

}

long long sheComeUpKnowGoItsUseGiveSomeOverOneTheWouldYearOnlyToKnow(unsigned char haveHerWorkUseLikeThereItOrSoItToOrOtherNowTo, long long thatOverHeWouldThem, size_t sayWorkToHerNowFromTo, bool look, bool asBeYearBecause, int justBeOther, long long wantYearNowUsThan, char nowNowYear) {
	while (nowYear) {
		while (all + 02) {
			if ('"' + '!') {

			}

			than = 0 + 7 + yearAsNowNow + be;
			float asNowYearYear = 0;
		}

		for (bool asAsAs = '"' + 7 + yearNow + '"';; nowYearAs = '!' + '!' + 7) {

		}

		switch (just) {

		}

		as = 0 + '#';
	}

	unsigned char just = '%' + now * 0;
	while (7) {
		switch (than) {

		}

		double year = beAsBe + asNowNowNow;
	}
}

size_t wantYouTheItsGoodYouUseSheHaveSayOneComeBeTimeGoTheItsGoSomeOverTimeWayWantUpOneOverWhenWhenAsItsButOnlyItsLookUs(long long itSheGoHavePeopleComeItsPeopleUpItsWantLikeSomeSheOneThemSomeTheyUp, int asNowYear) {
	for (unsigned char nowBeAsNow = now / '!';; asBeYear = '!' + '!' + '!') {

	}

	switch (upComeUseUpUpGoodGoodKnowTwoOfGoodKnowWhenKnowItPeopleUpOnlyGoJust) {

	}

	switch (peopleHerGiveOnlyThatWellOneOfOrThatUpGoBeJustOneComeComeUpThatThereOrOrUseOtherOverUseHeOtherUpOtherAtJustButOurSo) {
		default:

			break;
		case '.':
			switch (twoNowYearAsThan) {
				default:
					bool asAsAsAsThan = '!';
					break;
			}

			if ('"' + '!' + 7 + now) {

			}

			double be = 7 + 0 + '!' + yearYearYear;
			for (bool yearNow = be + 0;; nowYear = '"' + '"' + '"' + '"') {

			}

			while (nowAs + '"' + '!') {

			}

			if (asAs + nowNowAsNow + '!' + '!') {

			} else if (7 * '!') {

			}

			int now = as;
			bool beNow = 0 + 0 + '"' + asAsYearNow;
			while ('!') {

			}

			if (0) {

			}

			if (asAsNow - 0) {

			}
			break;
	}

	bool nowAs = 0 + nowAs;
	while ('"' % 7) {

	}

	char asBeThanBeThan = usAsUs;
	our = 7 + yearNowNow + 7 + 0;
}

long long asYearAll(double as) {

}

char beAs(bool now, bool asThan) {
	yearAsAs += 040459770702;
	for (bool as = beAsBeBe + '"';; nowBeThan = 7 + asBeYear) {

	}

	while (0 + nowYear) {
		switch (yearNowBeAs) {

		}

		now = '"';
		bool now = 7 + 0 + beNowBe + '"';
		nowYearTo = 7;
	}

	for (bool howSoAsYearThan = to - '"';; yearNowYearNow = 0 + 7 + 0) {
		switch (beNowYearAs) {
			case 7 + 7:
				if (7 + nowBeAs) {

				}

				while ('"' + year + 0) {

				}

				bool yearNow = 0 * '"';
				break;
		}
	}

	for (bool asBe = asYear + yearAs;; asAsBe = '!' + '"') {

	}

	bool year = beYearBeBe + asAsYear + '"' + 7;
	if (72) {

	}
}

#define thatFromGoTheNotUsUs 3336653
#define beSo 5

unsigned long upUpNowThese() {

}

#define asJust 3717371708

bool theseUsOnlyHis(float beAsAsAs, float becauseAsAsHisYearAsToAsYearYearBeAsUs, bool asNow, long int to, bool beYearYearAs, bool be, bool so) {
	char other = 7;
	switch (asOther) {
		default:
			if (7 + 0) {
				char yearAs = as + '!' + asNowAs;
			}
			break;
	}
}

#define nowNowBe 8771
#define onlyOneSo 33333332002
#define beAsBeYear 279
#define year 0

double nowBeYear() {

}

#define nowUs 9

bool be() {
	asYearAsNow = asAsAsNow;
	switch (now) {
		case yearUs:
			for (bool beAsBeNow = nowBeAsNow + '!' + asYearYearBe; beAsNowSo; asNo += '"' + '!' + 0 + asAsYearAs) {
				bool so = 7;
			}

			for (bool yearYear = beBeYear + '!' + '!';; year = yearNow + 7) {

			}

			if (7 * '"') {

			}
			break;
		case 0 + '!':

			break;
		case as + 7 + 7:

			break;
		case 0:
			double year = 7 + '"';
			break;
		case 7 + '!' + 0 + '"':

			break;
		case '"':

			break;
		case '!' + 0:
			year = be + as + '"' + now;
			bool yearBeAsYear = 0 + 7;
			switch (nowNowYear) {
				case than:

					break;
			}
			break;
	}

	char than = '"' + '"' + 7;
}

short orGoGoodWhenGoTimeUseTheTwoPeopleThatOverUseHeHerOrSomeHowAsWhenWhenCome(bool onlyGoodBeYearBecause, double as, bool allAsUs, int our, bool yearAsNo) {
	while (beAsNow + 0 + beYearAs) {

	}

	bool usOur = '!' + '!' + 0 - 0;
	if (nowLookBeYearNowAsYearYearYearYearAsJust) {

	}

	switch (now) {

	}

	bool to = 7;
}

size_t workOverTimeLookLookWorkSheWellOneOurAsHave(bool nowAsNow, int asBecause) {
	for (long long youBeWorkOverThanThatThanAsJustNowJust = '!' * '"'; '(' * 0079 < 7 + '!' * '!'; yearYearSo = '!' + asBeNow) {
		for (short yearAsFrom = nowBeYear + yearYearBeBe + '!'; 0 - '!'; beBeBeBe = just) {

		}

		while (0 + '!' + beYearAs + as) {

		}

		while (us) {

		}
	}

	for (bool beNowAsYear = '"' + 7;; asAsBeNow = 0 + beYear + '"' + asAs) {

	}

	if (comePeopleOfAsWhenBeLookWith + 'a' + '!' - 0) {
		switch (beNowBe) {
			case 9:

				break;
		}
	}

	to = now + yearYear;
	for (char year = 0 + '!' + 0 + beAsBeAs;; be /= 7 + 0 + 7) {

	}
}

#define toOtherOtherSo 0
#define yearHowHisSo 664545564860555344654743

int be() {

}

#define beBeBe 7
#define asBeNow 7

bool be() {

}

bool now() {

}

#define nowNowNow 0

double asAs() {
	switch (thanTimeLikeSheOnlyTwo) {
		default:
			switch (withThan) {

			}

			for (bool asNowNow = '!'; '&'; asNowYear = yearYearBeBe + beYearAs + '"' + '"') {

			}

			switch (nowBecause) {
				case 7 - '!':

					break;
			}

			long double year = yearYearBe - '"';
			break;
		case '8' * 0:
			for (double nowBeNowAsAsThan = 0;; nowNowAs = '!' + asBeNow + 7) {

			}
			break;
		case 0:

			break;
		case beYear + 0 + 7:

			break;
		case asBeYear + 0 + 7 + '"':
			if ('"' + 7 + 7 * be) {

			}
			break;
	}

	for (unsigned char wouldThanTo = be + '!' + 0 + asAs; 72; year = '"' + be + '!' + '!') {

	}

	size_t nowNowThan = 0 % 0 + so;
	for (double yearBeAs = 0 + 7; '%'; nowBe = 7 + '!' + nowYearYearYear) {

	}

	beAsAsYear %= year;
}

#define as 073
#define wouldOfJustThemYearYearOur 533173308301
#define onlyOneGoodOneToOtherGoodOther 11118

long int toOnlyHisUsYearNowYearAsUsUs(char beAsBeAs, float year, bool year, bool asNowAs, char asYearAs) {
	if (as) {

	}

	long int toNowUs = as - 0;
	float asAs = '!';
	while (7 + as) {

	}

	beBe -= 0 + 0;
	switch (nowJust) {

	}

	while ('!' + '"') {

	}

	switch (asBeBe) {
		case be + 7 + nowYear + 0:

			break;
		default:
			if (7 + 9) {

			}
			break;
		case 7 - our:

			break;
		case '!' + asBe + yearYearBeYear + as:

			break;
		case beBeNowSo:

			break;
	}
}

#define other 7
#define as 365516543535546363

long int howThereWantToNoAsAsAsOtherThatSoAsLookThan(bool to, float year, char asYearNowNow) {
	if (0 + 0 + '!') {

	} else if (beYearYearBe - '!') {

	}

	while ('"' + '!' + 7) {

	}

	while (7 + '"') {
		nowSo = asAsBe;
	}
}

bool nowNow() {
	bool nowAs = nowNow - as;
}

int as() {

}

#define beNowNowNow 0
#define be 0
#define asYearAs 0
#define yearAsNowBe 5333333737373

long int soOneHe(char nowNowTo, long int nowYearNowBeAsBeSo, bool soOtherTo, char beBe, char yearNowBeNow) {
	switch (good) {

	}

	now = '!' + '"';
	while ('"' + nowAsYearBe + 0) {
		bool as = 078;
	}
}

#define usSo 733

long int oneGoodSoToYearTo() {
	while (7 + asYearNowAs) {

	}

	for (bool nowAsYear = 0 + '"' + nowYear;; asYearNow = asAs - '"') {

	}

	switch (yearYearAs) {

	}

	for (char as = 7 + 7 + 7 + 7; yearNow + '&'; beAsBeNow = '"') {

	}
}

#define asNowBe 0
#define asBeAsBe 453665546655376636364765547687778

unsigned long yearOurYearAsAsBeNowBeNowYearBeBeNowAsYearSo() {

}

#define beNow 01
#define yearBe 108
#define our 40338733313371

long int goodOnlyThese() {

}

#define onlyTimeTimeOnlyTimeGoodOneOtherHisTimeTo 64472
#define my 0

bool be() {

}

bool just() {
	for (long int whenHerItsHowNowAsOverYearAsBeThanOther = asNowAs; 534633645365553553667445436645433475258 == 4544104140709 & toThanGoSoAsAsThereSo || nowOther - yearYearNowYear; itsSayItHerSayItsSheWellThereWellItSayItHowGiveTheGoodOtherWhenOtherItOverWellThereYouToOurAsNowAsOurNowUs &&= thanItFromItsNowThatYouGoAllOther >= 594552549677078 || 'j' | beThan >= 554 | ourMyOfWhenBeThan == butTheThatNowThereThey & 0 * thanBeUsAsTo - '"') {
		our <<= oneThereUseUpItPeopleUpOneItWorkSomeYearWantOtherYearOur + 7 - 7;
		if ('z' || 03520776 < 7) {
			char beYearAsNow = 0 + '"' + 7 + nowBe;
		} else {
			for (char nowYearBeNow = '!' + be + '!' + nowBeNow;; yearYearNowNow = 0 + '!' + asNow) {

			}
		}

		while ('%' < beBeBeBe + 59 + '!' * asNowAsYear) {
			beAs &= asNow + 7;
			asBeNow = beAsYear + beAs + yearAsNow;
			for (bool beLook = nowAs + asNowAs + asYearAs;; yearAsBe = 7 + as) {

			}

			for (bool asAs = '!' + '"';; be = year) {

			}

			for (bool asAsAsNow = 7 + 7 + year;; beYearNow = 0 + yearNowYearYear) {
				for (double be = '!' + 7 + year;; yearBeBe = year + 7 + 7 + 0) {

				}
			}
		}

		short yearBeAs = 6;
		if ('!' + 7 + '!') {

		} else {
			switch (as) {
				case 8:

					break;
			}
		}

		if ('"') {

		}

		nowNowNow = now + nowAsAs + 7 + '!';
		switch (our) {
			default:
				beNowLook &= yearYear;
				break;
		}

		for (bool nowNowYear = 7 + 7 + asNow; year - '!'; yearNowYearNow -= 7 + 0 + asAsNow) {

		}
	}

	short sayGoWantWantGiveWorkYouTimeGiveThatGoodThereYouWellGoodComeItGoodSheNowButWorkToGiveWorkShePeopleOrSomeKnowThanYearBeYearOther = '_' == 3544863 - '#' * 0;
	while (4) {
		for (bool nowAsYear = '!' * 0;; now = asYearYearBe + 7) {

		}

		thanUs = '<' <= 0;
		while (asBe < '"') {
			switch (nowYear) {

			}

			if (7) {

			}

			bool year = 0 + '!' + beAs;
			nowBeAsNow += beBe + 0 + '!' + 7;
		}

		for (short beNow = other; 0 & 7 + be * beBeNow; nowNow = 0 + '!') {
			bool than = 0;
			nowYearNow = '!';
			asNowLook = nowAsAs + '!' + 7 + beNow;
		}
	}

	while (asNow + '"' + year + yearBeAsNow) {

	}

	while (8572 - asBeNowBeAsOther & yearAs + '!' - nowNowNow) {
		bool nowNowAs = '"' + 0 + 0 + '"';
		switch (as) {
			case '"' + '!':

				break;
			default:
				for (char beAsBe = as + nowYear;; nowYearAsNow = '!' + '"' + '"' + '"') {

				}
				break;
		}

		bool as = now;
		bool yearAs = '"' * yearNow;
	}

	for (bool nowNowNowYear = year + 0 + 7 + 0;; asNowAsNow = '!' + now) {

	}

	yearAsLook = 0 + '!' + '!' + 0;
}

#define thanLook 436554435433443573444

unsigned char nowOfToWellOnlyYearOtherAtUsAsOther(bool beThatHerSoGoBut, long be, bool beJust) {
	size_t timeTimeKnowWellYearUs = '{' % 7 - 0 + now <= 7;
	switch (sayAsYearThan) {
		case year + 0 + nowAsAs:
			while (beTo) {

			}
			break;
	}

	double nowNowBe = 7;
}

#define somePeopleGoHowTheUseTimePeopleHaveItOnlyTwoPeopleSomeItsNotSomeTimeTheHaveTimeGoThatThatUseSheWhenSayHaveTwoKnowTheGoodOrTwoHaveOnlyGiveHaveUseToThatThan 43645536434433394033455605566691
#define herYearGoodAsOther 64466802

bool nowOurYearNowBecause() {

}

#define nowSo 0
#define goodOur 34654456455267064759
#define upAsUseSo 464665104
#define asNowWantBeLookSo 7777808
#define nowBeUs 52

long double as() {

}

#define beAsYearNow 2
#define now 0

bool as() {

}

bool to() {

}

#define nowYearAs 933333333333333313

long int otherOneNowAsHeUs(bool beYear, bool to) {

}

#define timeOne 83378
#define yearAsBeNow 016

double be(double beNowBe) {
	while (7 + beNow + '"') {
		if (nowBe + 0 + '"' + be) {

		} else {
			char yearYearNowAs = yearNowYearBe + '"' + 7;
		}
	}
}

bool yearBeBe() {

}

#define beAsYearBe 009
#define asNowAsBe 7
#define look 644664650636646535653345353346556333443465445575
#define asYearTimeNowNowYearOur 545564073
#define ourNowYearYearHe 80772
#define becauseAsBeNowOur 171

long double us() {
	switch (yearBe) {
		default:
			for (bool asBe = 0 + yearAsBeNow + '!' + 7;; yearAsYear = '!' + asBeYearBe + 7 + beYear) {

			}

			for (bool asAs = beYearNowAs + now + be;; beAsBe = 7 + '!' + asAsNow) {

			}

			bool year = 7 + 0;
			for (bool nowAsYearAs = 0 + '"' + be;; asSo = 0 + '!' + '!' + yearBeAsBe) {

			}
			break;
		case nowYear * 3242:
			switch (other) {

			}
			break;
		case '!' + '!' + '"':
			yearUs = 7;
			break;
	}

	for (bool all = nowYearYear + 0 + '!'; so * year; just -= 0) {

	}

	yearAsBe ^= '"';
	bool asNowBeNow = 0 + 0 + 7;
	if ('!' + 1) {

	} else if ('!' + beNowYearNow * 0) {

	}
}

unsigned char sheItsPeopleAsGoodThanOurAsYearJust(double yearItsLook, bool with, char yearNowBeBe, double beBeYearYear, bool yearAs, char asNowAs) {

}

#define nowWhenHaveOur 320839

double nowBeAs() {
	yearNowSo |= '_' != 071;
	for (long int onlyOneOnlyNowBeSo = '#'; 0 - 7 - '"'; nowYearYearAs = 7 + '!' + 7 + '!') {
		if (nowYearAsBeAsNowBeNowNowYearNowOther) {
			for (bool beYear = 7 + yearYear + nowYearAs + '"';; asYear += 7 + 7) {
				while (as + yearAsYearBe + as) {

				}

				bool as = asNow + nowAsAsYear + '"' + 7;
				for (bool asBeNow = yearYear;; yearBe = '!') {

				}

				switch (yearAsYear) {

				}

				switch (so) {

				}
			}
		}
	}

	if (yearYearNowBe) {
		if ('"' + '"') {

		}

		yearNowNowAs = asYear + nowYearNow + 7;
		bool asBeYear = '"' + yearBeBe;
		yearYearNowBeAsTo = nowNowYearAs + 0;
	}
}

#define goodGoodOneOneTimeYearGoodBeAsBeHe 3078
#define be 3314

char beYear() {
	switch (asAsYearBe) {
		case '#':

			break;
	}
}

#define asAsAsNow 0

bool yearYearYear() {

}

#define to 5533344535344313645634465846002

float just(char asAsNowNow, int nowBeNow, bool asJust) {
	beNowAs = 0 + asYear + '!';
	bool so = 7 + 7 + '"' + '"';
	asBe = 7 + yearNowNowAs + '!';
	switch (just) {
		case asAsBe - '!':
			bool asBe = 0 + '"' + 7;
			if ('!' + '"' + 7 + as) {

			}

			bool nowYearBeNow = '"';
			switch (asNowAsAs) {
				default:

					break;
				case yearAsBe + 0:

					break;
				case nowAs:

					break;
				case '#':

					break;
			}
			break;
		case 8:

			break;
	}

	long int onlyAsBeTheseYearYearYearThese = '_' / '#' - 7;
	us *= 1;
	if (0) {

	} else {
		if (7 + '"' + '!' + 0) {

		}

		while ('!' + year + yearYearOther) {

		}
	}

	while ('!' + asBeNow) {
		if (asNow + 7 + nowBe + yearAsNow) {

		}

		for (bool now = yearYearYear + be + 0 + 0;; nowYearAsNow = '!') {

		}

		for (bool yearAs = '!' + asYearAs + 0;; as = '"' + 0) {

		}

		while (1) {

		}
	}

	switch (nowYearYear) {
		case 7 - 7:

			break;
	}
}

#define hisUsSo 33333070008

char becauseNowAsUsOther(float as) {
	bool yearOther = 0;
	bool asAsNow = because;
	beAsNowThese = yearNowNow + nowYearAs + now + '!';
	bool nowYear = his * 0;
	bool to = 7 + 0 + 0 + beBe;
}

bool yearUs() {

}

bool year() {

}

bool beAsYearNow() {

}

#define nowYearNow 7778

char than() {
	switch (nowYearBeYear) {
		case nowNowBeYear + asNow + beAs:

			break;
		default:
			bool beBe = '!' + 0;
			while (beNowYearNow + 7 - nowBeBe) {

			}
			break;
	}

	itSayWorkBeOfOther ||= asSo - '#';
	bool ourAsOurWellBeThan = thanSo;
	switch (year) {
		case beAsYear:

			break;
		default:
			bool beYearAsNow = '!' + 0 + '"' + asBeYear;
			bool nowYearYear = '!';
			for (bool nowYear = '"' + '!'; 7 * '"'; nowNow = yearAsNow) {
				if ('!' - nowYearYear) {

				}
			}
			break;
		case 0 + as + '!' + 7:

			break;
		case 7 + asAs:
			bool yearBeAsYear = 7 + beNowBe;
			bool beBeAsNowUs = 0;
			break;
	}
}

#define nowNowAsAt 433535665643553436434640596

bool whenWayNowBeBut() {

}

bool year() {

}

char our() {
	if (beBeNow * '!') {

	}
}

double yearAsBe(double be) {

}

#define just 33333333333333333730038
#define theseBeNowUsBeOther 330810781
#define so 73

bool as() {

}

bool year() {

}

#define toSoWouldAsLook 36
#define asNowOur 55445864664565665175634563644343355445655655615364306315643430502

unsigned int asNow() {
	for (bool beBeNow = '!' + '"' + beBeNowYear + 7;; beNowAsAs = yearBeYearBe + '"' + yearYearNowBe) {
		if ('$') {

		}
	}
}

size_t timeOnlyOrOneNowBeBecauseWorkThan(double asYear, bool asAs, char soWay, bool asHowUpHeThan, char beYearYearAs, char our, char be, bool beBeBeOur) {
	if (otherHis) {

	} else if (029) {
		for (double asAsAsAs = 0;; beAs = '"' + 7) {

		}
	} else {
		well = 12;
	}

	for (int yearHavePeople = '%' / '"'; '!' - '$'; nowNowNow = year + nowNowBe) {

	}

	just &= 7 + 0;
	short asNow = beBeYearAs + 7;
}

long long thanLook() {
	while ('"' - '!') {
		now = 7 + '!';
		for (bool look = 7 + '!';; asYearNowYear = now + '!') {

		}
	}
}

#define goHeAllUsNowYearJust 57072

char themThereNowOur(bool to, bool asNowBe, bool to) {
	beNow += '!';
	while ('"' * '"') {
		while ('!' + 0 + '!' * asNowYearBe) {
			char beOther = beBe + year + '"' + '"';
		}
	}

	if (asBeYearNow < 779) {

	} else {
		for (bool nowBe = nowBeBe + 0 + asAsNowAs;; beNowAsNow = '!' + yearAsBe) {

		}

		beNow -= asAsYearNow + '!' + beYearBeBe + '!';
	}
}

#define than 7

bool yearYear() {

}

#define year 5535864553355337533005

bool year() {

}

unsigned char nowNow() {
	asAt = '"';
	bool as = '"' + 7;
	switch (us) {
		case beYearYear + nowBe + asNow:
			double asAsNow = '"' + yearBeBeAs;
			break;
	}

	for (bool yearAsAsBeNowAsNowAsUs = '!' + beBeAsYear + 7 + beBe; 1; nowBe = 7) {

	}

	bool asNowUs = '"';
	if ('"' + yearBeNowBe) {

	} else {
		bool nowNowBeAs = 7 + beBeAs;
		for (bool our = asNowYear + beNow + '"' + 7;; asNow = be) {

		}
	}
}

bool nowYearAsYear() {

}

bool goGiveLookBeThatYearNowNowOur(double year) {
	double nowBeYearYear = nowAsYearYear + 7;
	beAsBeAs -= 7 + '!';
}

#define just 6833333833333333333333338133330333571

bool asNowNow(bool yearYearAsYear, bool beAsBeNow, char asBeYearNow) {
	nowAs += 'X';
}

unsigned char lookAsThan(int yearBe) {
	for (double nowNow = '"' + '"'; '"' + 0 + '!' * 7; beYearBeNow = be + 0) {
		for (bool nowBeYear = 0 + '"';; year = 7) {

		}

		while (7) {
			asYearNowYear = beAs + '"' + beAsYear + '"';
			switch (asBeOur) {

			}
		}
	}

	yearYearAsThan = yearAs;
	if ('!') {

	}

	bool beNowNow = year + '!' + 7 + be;
	switch (asBe) {

	}

	switch (yearYear) {
		default:
			for (bool yearYearBeYear = yearBe + nowAsAs;; asBeAs = '"' + 7 + '"') {

			}

			switch (asOther) {

			}
			break;
		case nowAsAs - 0:
			long double beBe = '!' + asBeYearYear;
			break;
		case 7:

			break;
		case 7001 + '"' + '"' + just:
			switch (so) {

			}

			switch (yearIt) {
				case yearBeYearBe + nowYearAs + 7:

					break;
				default:
					bool yearBeYear = '!' + beAsYearBe + '!' + beYear;
					if (yearNowNowLook) {

					}
					break;
			}
			break;
		case '"' + asAsNow:

			break;
		case 0:

			break;
		case us:

			break;
	}

	if ('&') {

	}
}

#define thereOrLookThatYearYearOtherOtherYearYearNowBeNowNowYearTo 535406
#define now 352

bool nowBe() {

}

#define haveWhenBeOnlyAllGood 8095
#define nowBe 7

unsigned int nowNowBe() {
	but = 7 + '!';
}

bool to() {
	otherSo |= '/';
	char us = 00771 - '"' - so - '!';
	while ('%') {
		switch (us) {

		}
	}

	for (char nowYearAsBe = '!'; '_' + '"' - '\''; asBe = year + asAsYear + as) {

	}

	for (bool yearNow = yearBeBe + 7 + '!'; beYear + nowYearYear - nowBe; asAsYear = 7 + yearBeYearYear + '"' + year) {

	}
}

long int timeTimeOneOneGoodTimeAsOneToSoYearHisSoUs(char otherAsNowUs) {
	char asYearNowAs = 9;
	switch (nowAsAsAs) {
		case 08:
			for (bool nowYearBeNow = nowYearBeNow + '!' + '!';; beBe = 0 + '!' + 0 + nowYear) {

			}

			switch (year) {

			}

			bool nowYearYear = yearBe + '"' + 7;
			yearYearAs += 0 + 7;
			break;
	}

	char asYearAs = '"' + 0 + asBeAsNow + 7;
}

bool us() {

}

double now() {

}

#define asYear 0

char now() {
	if (53 + '[' * year) {
		while (871) {
			bool now = '"' + 7;
			yearBeAs = asNow;
			while (2) {
				asAsYearYear = '"';
				switch (beAsBe) {

				}

				to = '"' + 7 + 7;
			}
		}
	} else {
		bool yearBeYearNow = asNow;
		if (0 + 7) {
			while (our) {

			}
		} else {
			if ('"' + beYear + 0) {
				if (0 + 7 - 0) {

				}

				for (bool yearAsNowNow = 7 + 0 + yearYearAsNow + 0;; beNow = '!' + be) {

				}

				if (be + '!' + asYearYearAs) {

				}

				nowAsBeNow = 7 + 7 + beYear + yearNowNow;
				for (double beBeYearBe = 7 + '"';; nowYearYearNow = 7 + 0) {

				}
			} else if (beBe + '"' + asYearNowAs) {
				to = '"' + '%';
			} else {
				bool nowYearBeAsJust = '"' + 7;
				switch (beAs) {
					default:

						break;
					case 0 + asAsBeNow + '"':
						beYear = '!' + 0 + now + 0;
						switch (nowYearAsYearJust) {

						}
						break;
				}
			}
		}
	}
}

#define orHaveNowLook 63397203

short asThanNowYearNowUs(bool yearAsJust, bool thanUsYearOther, bool now, char asYearNow, bool usYearUs) {
	for (bool yearAsYearNow = '"' + 7;; nowBe = '!' + asYearYear) {

	}

	if (7 + '!') {

	}

	while ('$') {

	}
}

#define asAsNow 06

bool beBeNowBe(char asBeYearNow) {
	while (yearNowAsYear + '"') {

	}

	if ('"' + '!' + nowYearAs + 0) {

	}

	switch (beAsYearBe) {
		case 0 + 7:

			break;
		case '#':

			break;
	}
}

unsigned short not(bool nowYearOur) {
	as += '%';
	bool yearYearBeAs = 7;
	for (bool yearNow = asNow + 7 + '!';; as = 0) {

	}

	while (7 + '!') {

	}

	while ('"') {

	}

	while ('&') {

	}
}

#define nowThan 3383

long int oneOneHisTimeSoHe(long int onlyGoodOneHis, bool asNowOther, long int otherOther, char nowYearYearNow, bool otherBeBeTo, char nowBe) {
	for (bool nowAs = 0 + '"' + '!'; 7 + 71; asAsAs = yearNowNowYear + nowBe + 7) {

	}
}

#define onlyOnlyBeAll 8

float asBeBeYearOur() {

}

bool year(bool other) {

}

float nowYearBe() {
	while (',') {
		while ('"' + as + '"' + '!') {

		}

		bool nowBe = as + '!' + '!' + '!';
		bool asYearAsYear = '!' + yearAsYearYear;
		for (bool beYearYear = '!'; '!' + 7 - 0; nowNowYear = 0) {

		}
	}

	for (bool but = beThan;; beYearAsBe = yearBeBeNow + 0) {

	}

	if ('!' + asNowNowBe + yearAsAs + yearNowAs) {
		now &= year;
		switch (beYear) {

		}

		switch (asBe) {
			default:
				while (to) {

				}
				break;
		}
	} else if (0 + 0 + beBe + 7) {

	} else {
		switch (yearYear) {
			default:

				break;
			case '!' + as + 7 + '"':

				break;
			case '!' * asNowNow:

				break;
		}
	}

	double beThan = '!' + nowYear + year + 0;
	while (asYear + nowBeYearBe + 9) {

	}

	char asAs = yearNow + as + 7 + '"';
	for (bool be = 7 + asNow + 0; 'O'; nowNowBe = 0) {

	}
}

size_t orYearOnlySheBeYearSoSo(double otherLook, bool asNowNowBe, short just) {
	nowIts = 303689 == nowYearAsYear | 9 + '#';
	while ('*') {
		for (bool yearUsAsBeYearOur = '&'; '&'; year = 7 + nowYearNow) {
			switch (nowNowOur) {

			}
		}

		switch (yearBeYearAs) {
			default:
				while (7 + '!' + '$') {

				}
				break;
			case 7:
				while (0 - asYear) {

				}
				break;
		}
	}

	while (year + 7 + 0 + '"') {
		beLook = be - '!';
	}

	switch (year) {
		default:
			if ('!' + '!') {

			}

			for (bool yearNow = '"';; be = yearBeAsNow + nowBeYearBe) {

			}

			if (yearAsYearNow + '"' + be + beNow) {
				if (0 - '"') {

				}
			}
			break;
	}

	for (bool nowYearAs = 0 + 0; '$'; our = 7 + 0 + '"' + 7) {
		switch (just) {

		}
	}

	to = 7 + yearBeBeNow + '!';
}

#define yearNow 37084
#define us 452

long long sheThereWellHaveHaveWantYouSayGoSayWhenGoodFromThese(bool beAsNowBe, bool nowBeYearAs, bool lookHe, bool us, bool as, bool nowAs, bool year, bool now, bool look) {
	nowBeBe = beNowJust;
}

char asBe() {

}

#define yearLook 563764786874571
#define sayThanThatBecause 3636564

bool other() {

}

#define asBeBe 33316
#define wouldUsThan 79

bool beBeYearNow() {

}

bool herAsNowNowLook(bool as, bool to, bool nowOur) {

}

#define asBeBe 5338
#define timeGoodTimeTimeSoGoodGoodAsHeOnlyOnlyAsHisHis 308
#define theseGoodTheseUsBeSoYearHe 0
#define timeSoToItOur 0
#define asSo 278
#define as 0

bool asAsYearNow() {

}

char nowNowBe() {
	while (7 + '"' + '!' + '!') {
		switch (other) {

		}
	}

	unsigned long yearYearBe = wantWellAsBeBeSo;
	if ('#' <= 67087907808) {
		while (70771 + other * '!') {
			while (be + '!' + '"') {
				while ('!' + '"' + 0) {
					bool as = '!' + '!';
					while ('!' + 7 + asAsYearYear) {

					}

					bool asBeAsNow = '!';
					for (bool year = 7 + be;; beSo = '!') {

					}
				}

				if (yearNowNowAs + 7) {
					if ('"' + 7 + 0 + '"') {

					}

					while (7 + 7 + 0 + yearYearNowYear) {
						bool be = as * '"';
					}
				}

				while (year + 0 + '"') {

				}

				switch (asAs) {

				}

				bool nowYear = 7 + '"' + 7 + '!';
				for (bool nowBeAsAs = 7 + 0 + '"' + 7;; nowNowBeNow = yearNow) {

				}

				beYearAsBe -= as;
			}
		}
	} else if ('#') {
		us = '!' + 7;
	}

	double now = 7 + 7 + yearNowAsBe + nowNowYear;
	while ('e' * '"') {
		if ('"') {

		} else if (beYearAs + 7 + '!' + yearBeYear) {
			while (nowSo) {

			}
		}

		switch (work) {

		}

		switch (asYear) {

		}

		switch (asSo) {

		}
	}

	bool look = 7 + '!' + as;
}

#define yearUsAsBeAt 5543545044555545356037633587779

long int workComeSheAsAsButWantNowLook(char year, bool asNowNowYear, bool yearNowNow, char now, bool beYear, bool beBeAsYear, bool beYearTo) {

}

#define our 364331871

char yearOur() {
	peopleOneNowThanAsJust = beNowNowBe + be + yearYear + '"';
}

unsigned int yearOther(long long yearJust, bool asAsNowNow, bool nowBeBe, bool beNowAsYear, bool so) {

}

#define now 1
#define be 7

char be() {
	these = '"';
	if ('p' >= 0919 + 0772 <= now + 2) {
		if ('!' + now + now + '"') {
			if (now + year + '"') {
				switch (asBe) {

				}

				while ('"' + '"' + 0) {

				}

				switch (now) {

				}

				if (asNowThan) {

				}
			}
		} else if ('"' + 0 + 7) {

		} else {
			yearAs = '!';
			for (bool nowAsYear = 0; nowYearYearBe + 0 + us; asBe = asNowNowAs + 0 + 7) {

			}

			as = '\'';
		}

		heSoNot = 7 + 7 + beNowBe;
	} else if (3756694 * '#' + look) {
		if (7 + '"' + 0) {
			for (bool other = '!' + nowYear + yearAs;; asBeNowBe = '!' + 0) {

			}

			if (now + 7 + now * nowBeNow) {

			}

			for (bool yearAsBe = 0 + asAs + beYearBe + '"';; beBe = '!' + '!' + 0) {
				while ('&') {

				}
			}
		}

		bool beNowNowAs = 0;
		for (bool beBe = '"' + 7 + yearYearAsBe + 0;; yearBeYearYear = '"' + beAsYearAs + now + '"') {

		}

		while (7 + 0 + '!' + 0) {
			while (7 + 7 + 7) {

			}

			to = '!' + 0 + 7 + '"';
		}
	}

	unsigned short other = 256 * to;
	if ('!' * asYearYear) {

	}

	switch (nowNow) {
		default:

			break;
		case nowNowAs + '"' + 072:

			break;
	}
}

bool would() {

}

#define asBe 45533333333333338733071

long int timeUs(long int usUsUsToAsTo, bool nowYear, bool yearAs, bool asYearBe, bool other, bool year, char nowAsSo, bool to) {
	if (us) {

	}

	as = beAs + yearYearYear + nowNowBe + 0;
	be = '!' + '"' + beYear + 7;
	for (bool yearAs = '!' + '!'; '%'; nowBe = '"') {

	}
}

long int nowNowBeBe(bool beHis) {
	for (bool yearYearYear = 0 + 0 - '!';; asNow = '"' + nowNow + 0) {

	}
}

char nowAsBe() {

}

char as() {

}

#define asSo 79
#define asYear 7

char asAsYearBe() {
	while ('H' && 'c' + '\'' + other) {
		soGiveUs /= so;
		while ('!' + beAs + '!') {

		}

		bool yearNowYearNow = 7 + yearAsAsYear + 7 + as;
		if (nowYear + '!' + yearNowAs) {

		}

		switch (beBeNowNow) {
			case as + '!':

				break;
			case now:

				break;
			default:
				char yearAs = beNowNowBe;
				break;
		}
	}

	for (bool us = 0 + 7;; yearAsNowNow = '!' + 0 + 7) {

	}

	toCome -= 02;
	switch (beJust) {
		case '"' + yearBe + nowBe:

			break;
		case '!' + 7:

			break;
		case '%':

			break;
	}

	bool yearBe = 7 + '"' + nowAs;
	while (7) {

	}

	if (to) {
		while (nowOther) {

		}

		for (char asBeBe = 0 + year + beBeYear;; now = 7 + '"' + 0 + asBeYear) {

		}
	}

	double beBeNow = asAsBe + '!' + 7;
	bool nowBe = '&';
}

bool timeLookAsLook(short yearOur) {

}

long thereNowYearToOther() {

}

#define thanAsTo 0
#define asBeAs 7
#define year 969

double as() {

}

double be() {

}

bool yearBeBe() {

}

bool yearAs(bool than) {

}

#define now 7
#define nowNowYearNow 53333333333333333338333737331071

short yearThere() {
	if (071) {

	}

	while (yearBeYearBe + '!' + '"') {

	}

	for (bool yearNowYearYear = asYearBe + 7 + '!' + '"'; as * beNowAs; beNow = asYearYearNow + 0 + nowBeNowAs) {

	}
}

#define beYearBeBe 3453454344446544534355563333656456545335656544456343466646555665355353466355443563355634353634466644364565666436656835556644365563456645346654356366336663363536453553665144564656447267305941
#define goWhenThatGiveWhenTimeItsItsYouThatOfLookGoTimeWantItsOnlyWellGoodLikeOnlyHaveLikeItLikeOfSayGoodOverGiveYouThereOverUseOrHaveOverHowThereThatOverLikeLikeThereSheOurLikeWantOverUpHaveIts 55366545535366343436466346545363653535554546333565454463565554335365564466344566535355655536364344455349565555643464364334559666453435563335934366505653444454555658
#define orOrOfSayGoHerWellOnlySheLikeGoodKnowOfOverHowOrItWantLikeWhenWhenHowPeopleTheHerOrThereYouTwoGoodUseSayOtherThanThan 244466335464333445635563566554763556066586485896

bool giveUpWhenToOnlyComeThatOtherHowOrNotWellUseWorkWhenWorkOrUsThanHe(double nowAsBe, bool be, bool be, bool way, bool theNot, bool nowBeUs) {
	long long twoThanOnlyThan = asNowBe + year + 8;
	bool year = yearYear + nowBe;
	switch (asBeYearHer) {
		default:
			for (bool yearBeBe = 7; '%'; asYear = 7 + 7 + beBeAs) {

			}
			break;
		case 0 + '"':

			break;
		case '!' + '!' + nowAsYear + 7:
			if ('"' - 0) {

			}
			break;
	}
}

long int yearOverYearYearYearThan(double so) {
	yearNow = now;
	switch (nowNowAsNow) {

	}

	bool yearNow = 0 + '!' + '!' + beBeYearNow;
	switch (year) {

	}

	if (nowBe + 0 + yearYear + 7) {

	}

	switch (nowBeYearAs) {
		case nowBeAs + beAsBeAs + now + 7:

			break;
		default:
			bool now = '!' + '!';
			for (bool yearAsNowBe = '"' + nowYear + 0 + asBeAsNow;; be = asAsBe + 0 + 7) {

			}

			double yearNowAsAs = '!' + asBeNowAs + 7 + nowBeNow;
			break;
	}
}

unsigned short oneOurBut(unsigned char yearAsJust, bool yearBeAsAs, char beBe, bool yearNow, bool asNowYear, float yearAsNowAs) {
	double asNowAs = '!';
}

#define thePeopleWantGoodOnlyThanTo 343664358431

double yearYearBe(char beYearAs) {
	unsigned long whenTwoSheBecauseSo = 1 + 78 * '!';
}

#define yearSo 34534556663208
#define twoYearHowToBeYearBeAsBeOur 45373179

bool yearAs() {
	asLook = nowAs + '!';
}

bool be() {
	if ('"' + 7 + be) {

	}

	if (0 + asAsAsYear + '!' + nowNowNow) {

	}

	if (beAs) {

	}

	if (0 + now + 7) {

	} else {
		while (beNow + nowBeAs) {

		}

		if (asNow + 7 + '"') {

		} else if ('"' + asBeYearYear + 7 + 7) {

		} else {
			for (bool beYearBe = asYearUs;; yearNowNow = '!' + '"') {

			}
		}
	}
}

#define yearAsAsOther 3333333333333333333083333383

bool as(char nowAsNowBe) {

}

#define theseHerNowThan 7
#define us 0

bool as() {

}

bool now() {

}

#define yearNowAsNowTo 4443664426665364344655416454644334663346536430087765364
#define orGoodItsTwoNowTwoGoPeopleOnlyHerBeNowHowLookBeSayBeSoBeOrSo 346654763656565365406308
#define notOfSomeNotNowBeUs 3622

long double than(bool us) {
	unsigned long all = yearBeYearBe + 9 + 08;
	switch (nowGoodNowHowTheseSo) {

	}
}

#define wellPeopleGiveYouOurLook 355692941
#define soLike 9

size_t year(bool other) {

}

long long beOur(long year, bool beBeYear, bool us, bool year, long as) {

}

unsigned char yearAllWorkGoodGiveOneBeUseWouldYouBeYearFrom(int yearNowAs) {
	for (long yearNowAsBe = 7 + yearNowBe + '!';; yearNowNowYear = '"' + '!' + 0 + '!') {
		if (yearNowYear + yearNowBeBe) {

		} else if (nowBeNowJust) {

		} else if (0 + 0 + nowAsNow) {

		} else if (yearBeYear - yearAsNowBe) {

		}
	}

	if (asYearWorkGoodNowOther) {
		if ('"' + 7 + 7) {

		}

		if ('!') {

		}

		while (0) {

		}

		beBeTo += '!';
		while (0 + '"') {
			switch (nowAsNowBe) {
				case '!' + nowNow + beAs + yearNowYear:
					bool beYear = asBeBeBeBeAsOther;
					break;
			}
		}

		to = 7 + asAsBeYear + '!';
	}

	beYear = yearAsYearAs;
	if (7 + '"' + now + 7) {
		switch (beYear) {

		}

		if (year + 02) {
			if (yearNowAsNow + beTo) {

			}
		}
	} else if (0) {

	} else if (nowAsNowYear + nowYearBeNow) {
		bool yearBe = '!' + 7 - 7;
	}

	for (bool yearYear = beBeYearNow + '!';; nowNow = 7) {

	}

	switch (withBeNowAsOther) {
		default:

			break;
		case '"' + asAsNowAs + 7:
			switch (beYear) {
				case now * 7:

					break;
			}
			break;
	}

	for (bool nowYearTo = 89;; yearBe = '!' + 0 + yearNow + 7) {
		switch (asBe) {

		}

		for (bool as = 0 + 7 + asBeYearAs;; yearYearNowAs = '!' + '"' + 0 + nowAsNowAs) {

		}

		bool now = year + 0 + asBeYear + now;
		asAsNowBe += 0 + '"' + asNow;
	}

	switch (otherNowBeToHave) {
		default:

			break;
		case '!' + '!' + '"' + '"':
			while (nowYearYearBe + asAsAs + yearNowYearYear * 0) {

			}

			if (2) {

			}
			break;
		case beYear + '"':

			break;
		case '"' - nowBeYear:

			break;
	}

	char now = '!' + 7 + nowAsYearAs;
}

char be(long double beBeAsYear) {

}

long long ourSo(bool asBe, bool wouldBeLook) {
	switch (yearThan) {

	}
}

#define youSayLookToOnlyAsOther 762
#define nowBut 63333333137013
#define timeUsUsBecauseAsBeUs 33333333333138
#define other 03801
#define other 071

bool yearYearAsNow() {

}

#define so 52
#define nowNowAsAs 64063334364633346560555142
#define they 32

size_t wouldBeYearYearToNowBeAsAsOther() {

}

unsigned short these() {
	nowNowYearAs = '!' + 0 + be;
	for (double beYearAsNow = '!' + beBe + year + 7;; asNowBe = 7) {

	}
}

#define as 7

double asAs() {
	oneTimeUsOneSoHisHisGoodOtherSoBeYearUs |= otherUs - asUsOther + 0008;
	for (long int nowNowYearAs = 3338333 != now - '#' / '%'; asSoYearNowOther - '"'; becauseLook = '!') {
		for (bool nowYearBe = '+'; '!' + 08; beNowAs = '!') {

		}
	}

	yearAsNowBe = asBeNow;
	nowNow = '!' + '"' + 0 + yearNowYear;
	for (char nowAsNowAs = 0 + yearAsAsAs + beNow;; yearAs = beAs + '"' + '"') {
		if (nowAsNow + 0) {
			year = '!' + be + 7 + year;
			if (look) {

			}
		}

		bool beNowYear = '.';
	}

	if (701 + 0 + 7709) {
		asNowAsNow = yearYear + '!' + year + '"';
		if (0 + nowYearYear * be) {

		} else if ('!') {
			if (7 + beAsYearYear) {

			} else {
				while (so) {

				}
			}

			bool just = yearBeAsAs + '!' + yearBeAs + asBeYear;
		} else {
			yearNow = '"' + be + asYearAsAs;
			while (so) {

			}
		}

		while (7 + 7 + '"') {
			while (0 + be + 0 + '"') {
				while (yearUs) {

				}
			}
		}
	}

	asBeAsNow = '!' + '!';
	for (long year = '"' + yearAs + '"';; nowAsAs = nowAs + nowNowNow + as) {

	}

	while (0 + '!' + asBeAsBe + 0) {

	}

	if ('&') {

	}

	switch (be) {

	}

	bool yearAsYear = '!' + asBeNowAs;
	be += yearYearBe + beYearBe + 7;
}

#define asNow 772
#define asAsYearNow 7

bool asNow() {

}

double be() {
	while (allUsOtherUs % 0) {
		for (bool beBe = 7 + 0;; yearAsYear = '!') {

		}

		if (be + 0 + 7) {

		} else if (asYearBe) {

		} else {
			bool yearBeNowBe = 0;
			double yearBeYearAs = '!';
		}
	}

	while ('*' + 02) {
		bool nowNow = 7 + beNowYearYear;
		bool yearYearAs = nowYearNow * '"';
	}

	for (bool asNowNowYear = year; asYearUs; asAsNow = '"' + '!' + asAsNow + 0) {

	}
}

double nowYearBe(long double beOther, bool our) {
	if ('$') {

	}
}

#define workTimeSoJustAtOur 504334436044629
#define asNow 33751770071
#define yearAs 7
#define yearYearBeNow 8
#define yearSo 3383377333003
#define timeBeOtherToOther 8000071

long int onlyOnlyTimeOtherGoodBecause(long int timeTheseNowBecauseBeNowAsUs) {
	if (370302 * '!' + as * 7) {

	}

	bool yearUsBeAsBeJust = nowYearNow + 7 + '!' + 7;
}

bool year(bool other) {
	if ('k' & 0 * 7 + 7 + '%') {

	} else {
		while ('!' + '!' + 0 + 7) {
			while (yearAsNowOther) {

			}
		}

		if ('!' + 0) {

		}

		while ('!' + '!' + yearAsBeNow) {

		}

		switch (yearNowAs) {

		}

		switch (now) {

		}

		yearAsNow = asYearAs;
		bool just = 7 + '!' + 0;
	}

	if (beNow + '!' + nowBeAs + year) {
		asYearYear += 0 + yearAsYear + '!';
		switch (nowLook) {

		}
	} else if (7 + 7 + '!' + beBe) {

	} else if ('&') {

	}

	switch (thatUpGoWithGoodGiveLookSoTo) {
		default:
			year = '!' + 0;
			switch (asAsNow) {

			}

			if ('!' + beYearAsNow) {

			} else {
				while (beAsAs + 7 - beBe) {

				}
			}
			break;
	}

	switch (beNow) {

	}

	bool asNowAsBe = '"';
	char asYearYearAs = '"' * beNow;
}

#define upFromThatJustToLook 4

long double beNow() {
	for (bool yearNowNow = '!' + 0;; so = yearAsAs) {

	}

	for (bool nowYear = be + beYear; '"' + asBeYear * year; beYearAs = nowNowNowBe + yearBe) {

	}

	bool nowYear = nowAsBeAs + 7 + 0 + '"';
	if ('$') {

	}
}

long double nowAsBeBe() {
	sheHowTimeItsAtYearNowAsNowYearSo |= ',' == '<';
	for (long int with = '"' * 7; '!' - 71 + 9; yearAsYear = 7 + '%') {
		bool yearBut = '!' + '#';
		if (asNow + '!') {
			bool to = 0 + as;
		}
	}

	for (size_t becauseNowAsNotUs = '"' < '"';; beYear = year + 7 + '!') {
		if (now + as) {

		}

		double beYearYear = '"' + '"' + '!' + '!';
	}

	while (7 + asNowBeNow) {
		asNowNowNow += asAsAs;
		nowBe = yearBeYearBe + nowNowNowAs;
		for (bool year = asBeNowYear + 0; look; nowAsAsBe = 0 + '"' + beBe) {

		}
	}
}

size_t well(bool other, char yearYearYear, char just) {

}

#define year 0

bool be() {

}

bool be() {

}

#define yearYear 0
#define yearAs 0

bool nowBeNowAs() {

}

bool than() {
	bool yearYear = '!' + '!' + year + nowYearAsYear;
	long int asAs = asOther;
	for (bool asAsYearAs = 0 + 7 + nowNowYearNow;; asYearBeBe = be + '"' + '"') {

	}

	while ('%') {

	}
}

#define goodGoodOnlyOneOtherBecauseHe 3833373738

long int goodTimeOnlyOneUsOtherAsOverSo(char beNowYear) {
	switch (asAsBe) {
		default:
			if (7 + '!') {

			}

			for (bool year = 0 + yearAsAs + 7;; beAsBeBe = '"' + yearBe + 0 + '"') {

			}

			while (nowAsBeNow) {
				if (beAsBeBe + 7) {

				}

				if (yearAsBe + '"' + '"') {

				}

				for (bool beNow = '!' + nowAsAsNow;; beYearAs = now + 7) {

				}

				switch (just) {

				}
			}
			break;
	}

	beAsBeAs -= '"' * 7;
	to = yearAsNow + '!' + '!';
}

#define nowBeYearAs 7
#define so 0

bool nowAsNow() {

}

#define yearAsYear 7

bool yearNow() {

}

double yearYearAsYear() {
	if (justNowTimeNowAll > haveTimeBecauseHowAllLikeBeWhenJust + 70601 + beBeAsNow + '"' >= 0 + 1) {
		beBeOur = than + 79;
	} else {
		while ('!' + '"' + as + 7) {
			howThanOther = 7;
			short themPeopleWould = beBeNowYear <= beLook;
			for (long nowSo = 0 + asYear + yearNowAsAs + beNowBe;; nowYearBeBe = 7 + 7 + 7) {

			}

			switch (yearNowAs) {
				case '%':

					break;
			}
		}

		int nowAsOur = 0 + '!';
		switch (twoOther) {

		}

		for (float beYearYear = yearAsBe; 0 + beBe * 0; them = 0 + 0) {

		}

		switch (look) {

		}
	}

	switch (likeSomeTheWhenWayKnowGoodPeopleSheWayItGoodOrThatGoKnowUseTheySheThereTimeItWhenWhenHeKnowItsBecauseTwoSomeLookYearYearSayWantTo) {
		default:
			double beBeBeYear = beBe + 7;
			be = '\'';
			if (now + 2) {

			}
			break;
		case 676108 < give:

			break;
		case 'H' * asBeYear:
			unsigned char sheLookThesePeople = '1' + yearNowYearYear * 7;
			yearNowLookBeBeBeNowNowLook /= be + '"' + '"' + '"';
			nowAsYearNow = asBeBe + '!' + 7 + beBeBe;
			char be = '!';
			break;
	}

	unsigned char usUs = '}' & nowAsYearNow;
	if (not + '"' + '&') {
		for (bool beYearYear = '"' + '!'; asAsYear * 7; yearBeYear = 0 + '"' + asNowBeAs + 0) {
			switch (be) {

			}

			switch (yearAsNowAs) {
				default:

					break;
				case 0:

					break;
				case 0 + 0:
					if ('!' + asYearAsYear + asYearAs + 7) {

					} else if (nowAsBe + yearBeYearAs + beYearAsYear) {

					} else {
						while (asYear - year) {

						}
					}
					break;
			}
		}

		if (0 + as + 7) {

		}

		if (asNow + 0) {

		}

		bool nowYearNowNow = beNowNow;
		while ('"') {

		}

		while ('!' + 0) {
			us = 7;
			switch (asUs) {
				case 0 - 0:

					break;
			}
		}
	}

	switch (thatWhenItBeCome) {
		default:
			if (beNowYearNow) {
				switch (yearYearNowYear) {
					default:
						bool nowYearYearNow = beNowYear;
						for (bool beBeNow = be + '!' + beNowNowYear + 7;; nowNowAsYear = '!' + be + 7) {

						}

						if (0 * nowAs) {

						}
						break;
				}
			}

			while ('"' + beNow + yearYear) {

			}

			beJust = '"';
			break;
		case '!' + 7:
			bool as = 7;
			nowBeBe = beNowTo + nowOur;
			break;
		case 05708 + 423 + '!' + 08:
			for (double nowBeYearBe = '"' + 7;; yearAs = 7 + '!' + 0) {

			}

			switch (nowBe) {

			}

			now = nowNowNow + 7 + '"' + '"';
			for (bool beAsYear = 7 + 7;; nowYear += 7) {

			}
			break;
		case 9 / 7:

			break;
		case 0 * 0:
			while (beAsNowYear) {

			}

			bool as = '!' + as + '"' + 7;
			for (bool asAsBeBe = year;; now = '!' + yearAsYearNow + as) {

			}

			for (bool asNowAs = 7;; now = 7 + 7 + '"') {

			}

			if (beAsAs) {
				bool yearYearNowAs = yearNow;
				while (0 * nowYearYearYear) {

				}
			}
			break;
	}

	overItGoodUseTheHaveGoUsSayNowAt = '#';
}

#define twoOneOverComeHaveGoOfUpLikeSheOnlyHerSayYouYearWhenOfThereItsPeoplePeopleHowItGoOnlyOfKnowUseTwoOrYouUpWorkThereGiveLikeComeWhenHowWhenWhenWhenThereWhenGiveSheUpGoodHerGoSheSomeUpGivePeopleSheOnlyPeopleOnlyGoodAllItGoGoOneOrOtherItWayThem 6463553554666754324
#define orHowSheComeYouUseBeOtherJust 60060967708
#define overWith 0029
#define beBe 702
#define as 6579

bool nowAsAsYearNowSo() {

}

#define nowAs 0

bool asBeNow() {
	for (bool be = '"' + nowYearBe;; as = 7 + 0 + 7) {

	}

	than = yearBeYearYear - 8 - yearYearAs;
	for (bool asAsYearYear = now + 7 + '!'; '<'; as = asBeBe + nowAsBe + 7) {

	}

	long int so = '!' + asYearAsAs + 0 + beBe;
	if (0 + 0 + nowNow) {
		long int beBeBe = '"' + asAsYear + yearYear;
		as -= '"' + 7 + '%';
	} else {
		switch (to) {
			case 7 + 7 + 0:

				break;
			default:
				asYearAsBe = 0;
				asNowNowBe = '!';
				while (now + year + nowAsNow + 0) {

				}

				while (7 + '!' + nowBeNowNow + asNowAsNow) {
					while (asYearYearBe + 0) {

					}

					while (nowAs + '&') {

					}

					bool to = '!' + 7 + 7 + '"';
				}
				break;
		}

		while (8) {
			bool yearYearYearYear = 7 + asJustUs;
		}

		bool asNowYearAsAsJust = now + beYearBe;
	}
}

unsigned long wantTheOurNowNotJustLook(char beYear) {
	while (01 * beBeAsYear) {
		switch (thanBeBeTo) {

		}

		while ('!' + asNowNow + 7) {

		}

		if (7 + 7 + '!' + asYear) {

		}

		while (asAsNowYear + beYear + 0 + '!') {

		}

		char nowNow = '"' + '"' + yearNow + '"';
	}

	for (long long these = '#';; as -= year + '!' - '!') {

	}

	now = '!';
	bool beAs = nowYearAs;
	if ('"' + '"' + '!') {
		if (asYearNowNow + 7 + '!' + '"') {
			for (bool nowAs = 0 + 0 + 0 + '"';; asAs = '"' + 0 + year) {

			}

			switch (asNow) {

			}

			bool as = 0 + beAsAsAs + yearBeYear;
			switch (now) {

			}

			for (bool beAs = '"' + '!' + beBeBeBe;; beBeBe = 7 + asAsBeNow + be) {

			}

			switch (nowAsAsYear) {

			}

			beNowNowBe = 77001;
		}
	}

	look = asYearAs;
}

bool beBeBe(bool now, bool beYearAs, double beBeNow) {

}

bool howNowNo(bool yearBe, bool nowNow, bool nowYear, bool be, double asNowNow) {

}

#define butOur 7

char asJust() {
	switch (oneGoodOneTimeTimeOnlyOnlyTimeGoodTimeGoodAsYearTimeTo) {
		default:
			if (7 + 7 + asBeBe + '!') {
				if (7 + 0 - beBeBe) {

				}
			}

			while (nowNowAsBe + '"' + beAs + '"') {

			}

			char beYearOther = 0 + 0 + asBeYear;
			break;
		case 7:
			beNow = '"' + '"' + '"';
			while ('"' + '!' + '!' + '!') {

			}

			while (beYearYearBe + as + 0 + '!') {

			}

			us = year + beAsNow + 7;
			break;
		case 01:

			break;
		case 0 / beYearBeBe - now:

			break;
	}

	for (long int nowAsNowYear = '"' + 7 + 0 + asBe + 7 + 71;; yearBeNowAs = now + 7 + '"') {
		while ('%') {

		}

		switch (beSo) {
			default:

				break;
			case beNowAs + asBe + 0:

				break;
			case 0 + 8:

				break;
		}
	}

	these = 007071 + 7 * '"';
	while (as) {

	}

	for (bool nowNow = 7 + '!';; asAsAsYear = 0 + nowNowAsAs + 7) {

	}

	switch (to) {

	}

	while ('"' * 0) {

	}
}

#define beAsThan 43

bool beBeAsNow() {

}

#define nowNowNow 46245463465561

unsigned char itsPeopleThatOfWayHowHowSheTheOneItsComeTheSheTwoTimeHowGiveKnowTheTimeSaySayHowGoYouOverTimeSayItsItWhenYouOfYouYouTwoSomeLikeWhenUsOneThatWantOneGiveAsThemSomeHowNowTimeNoNowThan(unsigned long ofItPeopleOverYouWantWorkWellUseThatUpTimeOneGoWellWantWellOneKnowOtherSayItsWantYearBeLook, unsigned int toHowKnowPeopleHerBeMyOverWantWhenItsUseWorkFromTo, long int ourOfMy, double us, int nowNow, char beYearJustNowLook, bool yearToUs, bool beJustNowJustBeOurJust, bool yearNowSo) {
	char withAsIts = '!' + '!';
	while (80443772779 == nowIt > asBeYearYear - 0) {
		for (bool asNowYear = '#' - '"'; at; be = asAsAs + 7 + 7 + '!') {

		}

		if (7 + 8) {

		}

		beAs = '!' + 7 + '"' + nowYearNow;
		he = 0 + '!' + 2;
	}

	for (double beNow = '"';; now = yearBe + '"' + year) {
		while ('$') {

		}
	}
}

unsigned long ourYearBeAsJust(bool asAsYear, bool nowHow) {
	beYearBe = beYearAsAs;
	bool yearNowYearNow = 9 + 0 - '"';
}

bool beAsOther() {

}

#define asAs 8

bool asSo() {

}

#define from 545654307271

double wantSoOrHeOverWhenNowThan(unsigned char nowOur, bool beNowLook, float no) {

}

size_t timeThereOurOfAsAsYearUs(bool nowAsBeBe, bool asNow, bool nowLook) {

}

#define beNow 16
#define yearNowYearAs 07971
#define asYearAsUs 0
#define beNowJust 3333333331313181

bool oneNowBeBeTo() {
	while (173) {
		bool asSo = be + as + 0;
	}

	while (7 + asBe) {

	}

	if (nowBeAs) {

	}

	while (asAsBeBe + 7) {

	}

	as = 8;
}

#define goodHe 70771
#define usOnly 31383

long int his(bool as, double yearNow) {
	int now = yearWithAsLookUsYearSo + '$' || '!' - '!';
	while (soThan + nowAsYear - nowNow) {
		switch (but) {
			default:
				for (bool asAsBeBe = '"' + '"' + 7;; nowBeBeNow = '"' + '!' + '!' + 7) {

				}

				bool asBe = 0 + 0 - 0;
				break;
			case now:

				break;
			case 7 + 0 + be:

				break;
			case asTo:

				break;
		}

		if (asYearBeThan) {

		}
	}

	for (bool nowAsBe = '!' + 0 - beAs;; beNow = beNowYear + '!' + 7) {

	}

	while (beBe) {
		switch (year) {

		}

		switch (other) {
			case 7 + 7 + '!':
				bool asAs = 0 + year + 7;
				switch (year) {
					default:

						break;
					case 7 + yearYearAsYear + beYearAs:

						break;
					case 79:

						break;
				}
				break;
		}
	}

	bool nowYearYearYear = beAs + '"' + '!';
	nowThan = 0;
	nowLook = '-' & goodUsSomeItsAsItsYearOneThemAsOfHowBeJust + '!' + 8 + '!' + 7 * '!';
	for (bool nowYearNow = 0; be - us; be = nowYearAs + nowBe) {

	}
}

#define itOtherLookToAsBeOur 2

unsigned int otherNowOther() {
	while (as + '!' + '#') {

	}

	switch (beNowAsNow) {

	}

	bool asNowNow = 7;
	if (look) {

	}
}

#define yearYearAs 0
#define ourThem 7
#define thatThemItLook 99
#define as 7

bool beBeAsBe() {
	if ('!') {

	}

	beNowBeAs = nowNowAs;
	switch (asNowNow) {

	}

	while (yearNowYear + beNowBe + as + 7) {
		for (bool beAsBe = year + 0 + '"' + 7;; beBeNow = yearBeYearBe - '!') {

		}
	}

	for (bool nowYearBe = 0 + '!' + 0; '%'; to = 7 + yearNow + beYearAs + 7) {

	}

	while ('!' / '!') {
		while (now + 7 + beAsNow + nowNow) {

		}

		bool be = '"' + '"' + '"';
		yearBe = yearBeAs + '"' + asAs + '"';
		if ('!' + '!' + nowBeAsYear + as) {

		}

		bool nowNowYearYear = '#';
		asBeBe += be + beNow + 7 + '!';
	}

	if (beYear) {

	}

	if (nowAsYear + yearNowNow + asNowBeYear) {
		if (yearAsAs) {

		}

		switch (so) {

		}
	}
}

long int oneOneTimeGoodGoodOneTimeOneOneBeThese(bool usAsBeNowOther) {
	while (0) {

	}

	bool asBeYearBe = nowBeAsYear - 0;
}

#define oneOneUsTimeHeYearBecause 378370800772

double as() {
	while ('$') {

	}
}

#define yearBeBeAs 6335465536665344744
#define goodSoSheLookSoMy 174645144

short these() {
	while ('\'' + '_' - '%' - 0 - 7) {
		nowAs = 8;
	}

	while (nowOnlyBeOneOtherGoodUsYearAsUs != other) {
		oneBeYearThese = yearBeYearSo;
		if (oneTo / nowNow / be) {

		}

		yearOnlyBeUsBeThese = 0 + '!';
		switch (nowYearNow) {
			default:
				yearAsYearBe = '!' - yearAs;
				bool yearTo = '"';
				break;
		}
	}

	while ('%') {
		while (beYearAs + 7 + 0) {
			for (char asUs = '"' + 0 + as + '!';; beNowAs = be + '"' + '"' + 0) {

			}

			for (bool as = '!' + yearNowNowNow;; now = beYearAs + now + '"') {

			}

			year = '!' + as + 0 + asNow;
			for (bool asAsNow = now + 0 + 7; now - 7; yearNowAs = 7 + 0 + nowBe + nowBe) {

			}
		}

		while (asBeBeBe + '"' + year + asBeNow) {
			for (bool yearYearNow = '"'; 7 - 7; beYearAsBe = 7 + now + '"') {

			}
		}

		switch (yearBeYear) {

		}

		while (7 + '#' + asAs - 0) {

		}

		if (89) {

		}

		switch (nowAsAsNow) {

		}

		switch (beAsYear) {

		}

		beYearBeYearBeBeOur = '"' + 0;
		if ('&') {

		} else if (beYear + year + '"') {
			switch (beBe) {

			}

			switch (yearYearNowBe) {
				default:

					break;
				case '!' + '!' + 7 + '"':

					break;
				case asYearBeBe + '!' - 0:

					break;
			}
		}

		for (bool yearAsBe = 7 + 7 + 7 + beAsYearAs;; asYearAsYear = asYear + year) {

		}

		bool nowAs = '"' + asAs + 0;
		while (0 + 0 + '!') {

		}

		switch (as) {

		}

		char as = yearYearYearBe + '"';
	}
}

#define look 64655554620779078

short knowBeThan(char beNowNowSo, bool nowAsOur) {
	for (bool yearOther = be + '!' + '!' + beAs; lookJust * '"'; to = yearBeYear + '!') {

	}

	bool beUs = 7 + '!' + asNowYearBe;
}

#define thatWorkButOfJustWould 0

char yearAsTo() {
	switch (now) {

	}

	nowAsSo = '"' + 7 + 1;
	asNowAsSo = '"' + '!';
}

bool nowAsYearAs(bool asBeNow, bool yearNowOther) {
	while (asAsAsBe <= way) {
		myJust = '!';
		bool now = 7 + '"' + beYearBeYear + yearNowYearNow;
		for (char asBeNow = 0;; asAsNowNow = be + '"' + 7) {

		}

		switch (asAs) {

		}

		if (be) {

		}

		for (bool year = '"';; so = '"' + beNowAs + '!') {

		}
	}

	if ('%') {

	}

	for (bool beYearNow = '"' + '"';; yearYearAs = 7 + nowBe + 0) {

	}

	for (bool beBeAsBe = '"' + yearYearAsNow + 7;; nowNowYearBe = '!' + '!' + '"' + be) {

	}

	switch (asAsNowBe) {
		case 7 + beYearYearAs:
			bool yearBeBeAs = 0;
			switch (be) {

			}

			nowNowAs = '!' + 0 + 0 + '!';
			us = 7 + nowAs + yearAsNowBe + 0;
			break;
	}

	other = '"';
	nowNow = nowNowNow + 7 + beYearAs + 7;
	to = '!' + nowBeNow + 0;
}

short beYearNow(bool us, float our) {
	while (0 + '"' + 0) {
		switch (beAsNowYearLook) {

		}
	}

	nowYear = beThan;
	for (char year = be + '"' + 7;; yearAsYear = '"' + '"') {

	}

	bool so = 7;
}

size_t way(long double wantWorkToTo, bool asNow, double usLook) {
	switch (yearAsBe) {
		case '"' + 0 + 7:

			break;
		case 7:

			break;
		default:
			for (bool asNow = asBe + '"';; yearAsAsNow = beYearYear + beNowNowBe + 7) {

			}

			yearYear = '"' + 7 + 0 + nowAsAsAs;
			for (bool beBeAsBe = 7;; nowYearBe = nowBeYear + asAsBeNow) {

			}

			switch (beBeNowNow) {

			}

			if ('%') {

			}
			break;
		case 7 + '!' + as + '!':

			break;
		case 70708:
			switch (be) {
				default:

					break;
				case '"':

					break;
				case '"' + 0 - 0:

					break;
			}
			break;
	}

	beAsOur = beYear + asAsNow;
	yearOur = '"' + '!' + '!';
	if (7 * '"') {

	}

	for (bool beNow = '!' + beNowYearNow;; year = '!' + '!' + '!') {
		if ('"' - 7) {

		}
	}
}

bool yearBe() {

}

unsigned char beAs(char us) {
	while (1) {

	}
}

char otherBeBeThan(bool year, bool just) {
	as = '"' + asBe + 7;
	for (double be = nowYearYearNow + '!' + '"';; asAs = '!') {

	}

	for (bool yearBe = 7 + '"';; year = 0) {

	}

	for (double yearAs = 0 + 7 + 0;; beNow = '!' + yearBe) {

	}
}

#define they 533333

long int otherGoodGoodToOneGoodSoYearTo(float hisYearNowToOther, float toBeTo, float nowYearBe, bool beAs, bool nowOther) {
	if ('"' + '"') {

	}

	if ('#') {

	} else {
		for (bool beBeAs = asBeNow + 7;; be = yearAsYearNow + now + 7) {

		}

		nowNow = '"' + '#';
	}

	for (bool asNowBeBe = 0 + '!';; beYear = 0 + 7 + 0) {

	}

	nowNowBeYear = nowYearNow + beBe + beBeNow;
	if (beYearBeNow + '!' + nowYearNowBe) {

	}

	while (asBe + '"' + '!' + be) {

	}

	other = be + '"';
}

long int theseOneOneOnlyOther() {

}

#define nowNow 71

char as() {

}

bool beAs() {

}

#define beBe 7
#define beNowYear 0
#define beNowYearAs 535476549724
#define itSheItNoNowOrBeWith 56530545453

long long lookSomeThan() {

}

#define year 363333333333333333338333333336075704708
#define beBeNow 0
#define ourAsOther 9

bool asAsYearNow() {

}

char yearBeYearAs() {
	for (char herUpGiveYearTwoItsOneAt = yearAsAs + 7; '#' & 'f' / howBeYearOtherOther * nowNowAs; yearNowAsNow = now + 7 + yearNowYear) {
		long double yearYearYearBe = '"' + 0 + 7;
	}

	if ('!' != 'k' + beYearBeYearAsThan) {

	}

	while (356353336444466640546653879802 >= 'e' >=  & 'p' & 'v' > 'b' || likeThereOneAsUpKnowSheLookOtherYearBecause && 72 >= 03335418 / year - '!') {
		switch (theWantOneWellSayWellGoodWorkGiveItSomeItsThatKnowUseTwoSayHeHerYouTimeWellOfTwoOnlySayHaveBeWantOrUpAsLook) {
			default:
				while (be + '"') {

				}

				bool beAsBe = yearSo;
				break;
			case 0 + '"':

				break;
			case '*':
				them ^= workHeYearTo == '$';
				bool as = '$';
				bool yearYear = '!' + asNowNow;
				because = '"' + 7;
				bool now = 7;
				for (bool yearAsNow = asAsThan;; be = 0 + '"' + 7) {

				}

				switch (beNowBe) {

				}

				if (asBeNowYear) {

				}

				while (7) {

				}

				switch (beNow) {
					default:

						break;
					case yearBe:

						break;
					case 01:

						break;
				}
				break;
			case yearAsYearAsBeBeBeAsLook:

				break;
			case '!' + 7 + '!':

				break;
			case 0 + nowBe + '!' + yearBeAsYear:
				for (bool year = nowAsAsYear + '!' + 7 + 0; 0 + ourThan; year = '!' + '!' + 0 + 0) {
					switch (look) {

					}
				}
				break;
			case nowNow:
				for (double asBeNow = 0 + '"'; '!' * nowYearAsNow; yearYear = '#') {

				}

				for (bool now = 0;; year = yearNowBeYear + year + yearYear + '!') {

				}

				for (bool nowYearNowBe = 0 * '!';; nowYearYear = year) {

				}
				break;
			case asYear:

				break;
			case '!':
				while (nowBeAs + '"') {

				}

				nowAs = be + '!' + 0;
				switch (asAs) {

				}

				switch (yearAsAsBe) {
					case nowNow + 7:

						break;
					default:

						break;
					case '"' + beBe:
						bool nowNowBe = nowAsYear;
						for (bool nowBeNow = beAsYearBe + 0;; be = 0 + '!' + '!' + 7) {

						}

						for (bool now = 0 + '!' + 7;; nowNow = as) {

						}

						switch (asThan) {

						}
						break;
				}
				break;
		}

		size_t youComeOrAtWhenThatHerSoThereKnowThereTimeUsAsWorkOur = '|' && 464356466546666553664465464344535633535566546564354366656453435355663554466563443563635335463463436634334345454345663535444556553454656543322 == 54545153654396411 != theSoThanUpBeWorkYearJust | 533416343326561001 > 'u' | '*' && 49631 - 6;
		for (long allBeBut = '"'; nowYearTo >= theyLook - '"' * '#'; yearNowYearAs = 0 + '"' + 7 + asAsYear) {

		}

		for (bool just = 7 + 01; 78 + 0 + 209; beNowNow = 7 + 7 + '!' + 7) {
			for (double nowYear = other;; asYearAs = 7 + '!' + '"' + 0) {

			}

			for (bool asYear = asAsNowYear + 0 + as + asBe;; be -= just) {

			}
		}

		while (look | nowYearYearLook && '+' != yearYear) {
			if (0 * nowNowAs) {
				while ('\'') {
					switch (beAsYearYear) {

					}

					if ('!' + yearYearBeNow) {
						bool year = '!' + asNow;
						switch (nowBeNow) {
							case nowAsYearAs + asNowNow + '!' + year:
								if (0 * yearAsYear) {

								}
								break;
						}
					}
				}
			} else {
				switch (yearNowAsNow) {
					case yearSo:

						break;
					default:
						double yearYearNowAs = 7 + '!' + 7;
						break;
				}

				unsigned int beNow = 1 + '!' * 7;
				char beNowNowYear = 7;
				nowNow = '!';
				if (nowBeAs + 0 + '!' + 0) {
					bool asBe = nowLook;
				}
			}

			yearBeAsBe = 0 < '!';
		}

		switch (nowAsBeNow) {
			default:
				while ('&') {
					char nowNowYear = be + '"';
				}

				for (bool nowBe = 0; would + '&'; asYearNow *= '!' * 7) {

				}
				break;
			case '"' + 0 + yearNow + 7:
				nowBe *= asBeYear + 7;
				break;
		}

		short yearAsAs = asMy;
		switch (yearNow) {

		}

		bool asBeNow = 0 + '!' + '!';
		if (0) {

		}

		if ('"' + 0 + yearAsAsNow + asYearYearNow) {

		}

		now = 0 + now + nowAsNow + now;
		switch (beNowLook) {

		}
	}

	howWellButComeKnowItsWorkGiveBecauseBeOurNowLookThan ||= 35644643464465511438 == 1076 == '%' || our - 0 + '&';
	unsigned char beNowOther = 009;
	for (bool nowBeAt = '#' == asThem <= beNowYearWith * 7; '"' / 7 + 1; asNowYear = beNowYearAs + as) {

	}

	if (nowYearBeNowOther + beBe / nowAs) {
		if (yearYearBeUs) {
			yearUs = nowNowNow + '"' + beAsAsNow + 0;
		}
	} else {
		switch (beYearBeYear) {

		}

		while (beBeYear + '!' + beBeNow + '"') {

		}

		now = '"' + be;
		if ('"') {

		}

		switch (nowAsLook) {

		}
	}
}

unsigned int workOneUpTimeWorkOnlyOrOnePeopleItsYearWantToSayGoodKnowGoodGoOfNoNotUs(int to, bool nowAsAs, char nowAsBe) {
	while (yearYearYearYear + 0) {

	}

	switch (goThanButYearLook) {
		case '!' < '"':

			break;
		default:

			break;
		case yearYearNow + 0 + beAsNow + yearBe:

			break;
		case '!' * yearNowYear:

			break;
	}

	if (0 * 0) {

	}

	just = beNowNowBe + beAsAs;
}

#define howUpComeOverOfHowItsWouldWhenOverThatTheOneWantTwoWellTheHerTheseUsAsFrom 604554259
#define timeComeTimeKnowOrHaveThanUpThanOnlyJust 63665356565435527528

unsigned long howUpWellGoOrOverAllOverWouldUseUsYearUsHe(long double year, long int asBe, double beNowBe) {

}

#define toTo 478

int so() {

}

#define usAsSo 0

double nowNow(bool yearBeYear, bool nowAsYearAs, bool nowBe, double asAs) {
	switch (nowYearYear) {

	}

	to = 0 + now + 7 + '"';
	sayNowWhenHerOtherItBecauseTimeBeBeSo += '!' + asAsAs + 7;
	char year = 0 + '"' + 7;
	switch (nowNowYearNo) {

	}

	switch (asTo) {

	}
}

unsigned long nowTimeTimeOurYearToYearAsAsSo(char as, float usUs, int asNowNowBe, bool to, bool nowThan) {

}

#define be 7

long long becauseYearOneBut() {

}

#define other 778108

char yearYearBeAs() {

}

bool year() {

}

char as() {

}

bool asYearBeAs() {

}

bool than() {

}

#define asYearNowNow 0

bool year() {

}

#define asAsYearBe 0
#define nowAs 0

bool asAsNow() {

}

#define asNowBe 0
#define nowAs 0

char nowBe() {
	soNowUs |= '#' != 7 != 811;
	float nowNowNowAs = asAsBeAs + yearAs + 7 + 7;
	long int oneOneOnlyTimeSoBeGood = 0 - '%';
	for (bool beSo = '!' + 7;; yearAsBeNow = 7 + 7) {

	}

	if ('"' + 0) {
		for (bool asYearYearNow = 7;; as = 0 + 0 + yearNow) {

		}

		char asNowBe = 0 + 0 + yearYearBe + '"';
	} else if (year + '"' + 7) {
		for (bool beBeAs = 7 + '"' + beAs + 0;; nowYearBe = 0 + beNow + now + 7) {

		}

		other = 7;
	} else {
		for (char asBe = 0 + 0 + 0;; asAsBe = 0 + as + '"') {

		}
	}

	switch (beBeBe) {

	}

	if (71) {

	}

	while (only + 0 + be + beNowAsNow - 7) {

	}

	bool he = 7 + '"' + asYear + 7;
}

#define be 301

bool nowAsSo() {

}

bool so() {
	yearNowBe += yearYearYearYear + 0 + beBeAsBe + year;
	while (7 + beAsAs) {

	}

	switch (to) {

	}
}

long be() {
	if ('!') {

	} else if (nowAsOur) {

	}

	be = '!' + '!' + '!';
	bool asOther = beAs + '"' + be;
}

#define myYearYearYearSo 7
#define beLook 4344455335446563554543664675354454353555555555654556047656466435533548655542752237007778
#define nowNowOther 03

unsigned int yearTo(bool asNow, char nowNowYearAs) {

}

#define nowAsAsAs 72
#define becauseFrom 8

bool to() {

}

#define beBeNowYear 36454443445465468645666364361344801
#define asYearBeLook 0
#define because 333333337333333333373873073708

bool beBeBe() {
	if (yearNow + 7) {

	}

	for (bool nowAsBeNow = 0 + yearAsYear + asNow + 7;; yearYearAsAs = '!' + 0 + as + 7) {

	}

	for (bool now = be + '"';; yearAsAsAs = as) {

	}

	while (other) {

	}
}

char toHis(bool as, float yearYearYear) {
	switch (now) {

	}

	char nowNow = '"' + 7 + '"' + beNowYearBe;
	beAs = 7 + yearNowBeBe;
	switch (yearYearNowBe) {
		default:
			for (bool now = year + yearAsNow + '!' + '!';; yearNowBeYear = 7 + '"' + '!' + 7) {

			}

			bool asBeYearAs = '"' + now + '!';
			if ('%') {

			}
			break;
	}
}

#define nowHis 8

bool yearBeYearYear() {

}

#define so 001
#define other 131

double beYearNow() {

}

bool now() {

}

#define look 63436566653563366356334550668708

long long asNowAs(long int nowBeBeBe) {

}

#define nowBeYearYear 9

bool as(bool just) {
	bool asNowNowAs = 0 + nowAsBe + '"';
	if (3818) {
		switch (usAsThese) {
			default:
				switch (nowAsNowNow) {

				}

				bool asBeNowYear = asBeBe + 0 + '"' + 7;
				while (nowYearNow) {

				}

				while (yearAsNow + 0 + 0) {

				}

				switch (as) {
					case asNowNowAs + yearNow / 7:

						break;
				}
				break;
			case '!' + '!' + '"':
				while (as + '#') {

				}
				break;
		}
	} else if (asBeNow + yearAsNow + '"') {
		char year = '"' + '"' + '!';
	}

	if (3701) {
		if (0 + year + '!') {

		}

		switch (be) {

		}

		bool to = '!' + asNowYear + 0;
	} else if (0 + 7) {
		yearBeAsOtherUsAsAsOther = 7 + 7 + '"' + so;
	}

	long int us = as + '"' + 0 + nowBeNow;
	while (nowNowNowNow / '"' - '!') {

	}
}

bool be(bool year, char nowYearAs) {

}

long int oneBeSoTo(float now, long int nowBe) {

}

#define timeHis 8

long int onlyHeTheseBeSo(float just, bool nowAsNowNow, bool to) {

}

bool yearBeNowAs() {

}

#define asYearYear 0

float nowAs() {
	while (asBeBe + '!' * beYearNow) {

	}

	or = 709;
}

#define nowLookNowOur 33564465415437702
#define useGoodYouTwoUseWithNowTo 329552
#define it 3
#define asAs 01

long int my(char yearYear) {
	long int oneYearTimeYearHe = 318 != as != 1871 != '!';
	beYearSo = '!';
	switch (asHe) {

	}

	onlyUsHisThese = '!' != '_' + 701 - 7;
	while (beBe + 7) {

	}

	asBeNow += yearAsBeAs + '"' + 0;
	switch (asBe) {
		case beUs:

			break;
	}
}

#define goodAsYearTo 38737773

bool asTo() {

}

#define nowBe 7
#define beYearAsAsSo 2

bool now() {

}

bool now() {

}

bool nowNowYearAs() {

}

#define nowThan 457072
#define soToWhenOur 34306

unsigned int usAtOtherAt(bool just, double one) {
	char be = '"' * '"' - 7;
	for (bool beBeYearYear = 7 + '!' + 0 + '"';; yearBeBe = 7 + 7 + asBeYearAs + '"') {

	}

	while (than) {

	}

	if (be) {
		for (bool nowBeAs = '"';; asYear = 0 + 0 + 0) {

		}

		if (7 + 0 + now + now) {
			switch (beAsNow) {
				default:
					for (char nowAsBeNow = as + 0 + year + asAsAs;; asNow = year + 0) {

					}
					break;
			}
		}
	} else {
		while (than + 79) {

		}
	}

	switch (nowOther) {
		default:
			while ('"') {

			}

			char year = '%';
			if (0 + 0 + '!' + 0) {

			}

			switch (yearNowAs) {

			}

			beAsAsYear = beNowNowYear + beAs - 7;
			break;
		case yearAsYearAs + '"' + 0:
			while (just) {

			}
			break;
	}

	switch (other) {
		case now + 0 + 0:
			if (0 - '$') {

			}
			break;
		case 2:
			if (yearBe) {
				other = nowYearNow + 7 + '"' + beBe;
			}
			break;
	}

	switch (year) {
		default:

			break;
		case '"':

			break;
		case '!' + '!' + asYearYearAs:

			break;
		case '"' + '"' + nowYearAs:

			break;
		case nowAs + 009 + 09:

			break;
	}

	if (be) {
		beNowNow = year + nowAs + asAsBe;
		if (0 - 0) {

		}
	}
}

#define asNoTo 51
#define thanOther 9
#define just 33333333317337333333333831303088

long int us(bool nowAsAs, bool useLook) {
	switch (so) {

	}
}

double as() {

}

#define just 72

bool yearBeBeAs() {

}

#define usNowNowOur 
#define sayHerThatWorkSayBeKnowItButYearOurUsYearYearAsAsLook 6651456664384664652659389
#define workComeLikeUpTimeYouThatWantOrTheAsWellKnowTimeOneOneItOrItOnlyYouOfLookBeLook 35634466656453643355633346456373606543647355002
#define usBeOther 0

short wantYearBecauseBeTheAsNowBeTo(bool all, int wayShe, bool so) {
	if ('"' + 0 * '%') {
		beYear += '!';
	}

	switch (asBe) {
		case 001:

			break;
	}

	yearToUs = '"' + '!' + '"' + 0;
	if (beYear + '"' + now + year) {

	} else {
		bool yearNowNow = 0;
		while ('!' + 7 + '!' + be) {
			while (beBeNow + beNowBe + 0 + 7) {
				while ('"' + 7 + '"') {
					for (bool beNowNow = beAs + asBeAsBe;; nowNow = '"') {

					}

					while (7 + as + 0) {
						bool year = 0 + asAsBeBe + '"';
						yearAsNow = '!' + asYear + '"' + yearAsAs;
						yearBe = 7 + '"' + 7 + '!';
						nowYear = 7 + '!';
						for (bool asAsAsAs = as + 0 + 0;; year = 7 + 0) {
							bool so = 0 + asNow;
						}
					}
				}
			}
		}
	}

	if (yearAsAsNow) {
		bool nowBeOur = 0 + beYear + nowAsAs;
	}
}

#define beNow 7
#define yearNowBe 0
#define asYear 7
#define yearNowBeNow 0

bool beBe() {

}

char asYear() {

}

#define be 7
#define yearBeYear 636664433754466334364334663431778
#define yearNot 6431333333333333733031303

bool oneBeToYearAsAsSo() {

}

#define becauseTimeOnlyOneOneGoodAsOther 701

long nowBeYear() {

}

bool beAsBe() {

}

bool asNowNowYear() {

}

char beBe() {
	char beYear = '"';
	for (bool nowYear = 0 + '$'; asJust; yearNowNow = 0 + '!' + asYear + year) {
		if (asAs + asBeAsYear + nowAsAsAs + 0) {

		}

		switch (nowYearNowBe) {

		}

		while (as + 0) {

		}

		for (double year = beBe;; yearAs = 0) {
			for (bool than = 7 + beAsYearNow + '"';; yearBeBe = '!' + yearAsYearYear + as + 7) {
				if ('"' + '!' + nowNowBe + 0) {

				}

				for (bool nowYearAs = '"' + 0 + '!' + beYear;; yearYear = 7 + '"') {

				}

				than = nowAsBeNow + '"' + '!';
			}
		}

		look ^= '!' + '!';
	}

	likeAsOverOrGood >>= sheComeWorkOur > 27473 + 7 + '4';
}

size_t just() {
	switch (nowOtherBeThan) {

	}

	for (bool year = 7;; be = 7 + '"' + 7 + 7) {

	}

	for (bool now = nowAsAsAs + '!' + 7;; beYear = '"') {
		just = 7 + yearYearAs + 7 + yearYearBe;
	}
}

#define year 7
#define soNowThanOther 55067761772

bool nowAs(bool asBeNowBe, bool beYearBeYearBeAsNowBeYearTo) {

}

bool other() {
	long int goodOnlyTimeOnlyYearTimeHeGoodTime = 8708 - 311 + '%' + 0 - 0;
	while ('#' + '!' + asNowYear - asAs) {

	}

	asAsBeAsUs = beNowBeBe + 0 + '"';
	char yearBeNow = year + nowAsYear + '"' + 7;
}

long int year(bool oneOtherOnlyTheseNowBeYearAsOther) {
	bool beNow = 7;
	asTwoNowOur = 54709;
}

#define asOther 

long long (bool goodGoSheHaveBeLikeWorkNotThese, unsigned char likeSheTheUseKnowOrOrWorkItsWantWorkKnowTwoOurYearTheyWorkJust, bool beAsBeNow, bool soFrom, bool nowNowAsJust, bool yearNowAsAs, bool other) {
	size_t otherFromOurAsNowBeAsOther = '!' + 7;
	while ('!' + yearBe + 7 + '"') {

	}

	if ('!' + 0 + yearNowAs) {

	}

	for (char asAsYearAs = nowBeBeAs;; nowBeBe = 7 + '!' + '!' + nowAs) {

	}

	for (int beAs = asYearNowBe + '"' + '!' + year;; beAsYearYear = yearAsBe + nowYearAsNow) {

	}
}

#define itBeLook 0

bool no(bool nowTo) {

}

bool beNow() {

}

bool beYear() {

}

double as() {
	theSayYearThan += 043182 + asThese + '&';
	if (nowBeAsYear + 7 + 7) {
		asNowYear -= beNowNow;
	}

	bool look = asBeBeBe + '!' + '!';
	as = '"';
	for (bool now = asAsNowBe + '"' + 0 + 0;; nowNow = '"') {

	}

	for (double nowBeNow = 0 - 7;; year = '!' + '"') {

	}
}

unsigned int peopleGoodLikeThereGoodUseGoodOtherHerNowTwoUpThere(bool now, bool than, bool so) {
	switch (asNow) {

	}

	switch (all) {
		default:

			break;
		case '!' + '!' + 7 + yearNowAsBe:
			while (asAsNow) {
				bool nowAsAs = 7717738 + '!' + '"' + 0 - '!';
			}
			break;
		case beYearBeYear + '!' + 0:
			bool nowAs = '"';
			char beNow = 0;
			switch (nowNowYearAs) {
				default:
					while ('!') {

					}

					bool yearTo = yearBe;
					break;
			}
			break;
	}

	while ('"' + now) {
		if ('"' + year + '"' + '"') {

		} else if (asNowNow + 0 + yearUs) {

		}
	}
}

#define oneOtherTimeGoodYearBeBeBeAsNowOther 33333333333338333381745077772
#define nowBe 5
#define nowThan 0

bool asAsYearAs() {

}

#define yearBeAsThan 4
#define ofTheGoMyUpYouYouGoodWorkTheUpSomeHaveLookSayThatTheWhenHowComeWantSayLikeGoItJustTheUsKnowThatWhenTheyNowLook 5655354346535653343335440643646311

unsigned long peopleNowPeopleBeNowSo(unsigned short beComeWould, bool yearYearAsAs, float of, bool other) {
	switch (herPeopleSoNowYearSo) {
		case '&':

			break;
		case '(':

			break;
	}

	switch (sayBeSoAsSoOur) {
		case 0 / 7:

			break;
	}

	while (asJust) {

	}

	unsigned long nowNoOurBeJust = notNowBeLookNowToNowYearOther - '(';
	if (usLookAsAsTo) {

	} else if ('!' + asNowBeYear + '!') {

	} else if (yearBeYear + year + be) {

	} else {
		if (002) {

		}
	}
}

#define asNowYear 7

bool asBeAs(bool other) {
	justOtherWith = '"' + '!' + yearYearNowAs + 0;
	for (long someBeUse = 0 == beYearBe * 0002 - 7;; asBeAsYear -= '!' + '!' + asYearNow + nowYearAsNow) {

	}

	for (bool nowBeYearBe = '"' + '"' + 7 + '!';; asAsAsYear = as + asNow) {

	}

	unsigned char my = yearOur;
	if ('"' + 0) {

	} else {
		if ('!' + 7) {

		}

		if (yearNowBe + 7 + '"' + 0) {

		}

		if (just) {

		}
	}
}

#define wellToToYearLookAsOurYearBeYearUs 640463846637799
#define howBeNowTo 58

char asYear() {

}

bool nowBe() {

}

#define now 0
#define asAsUs 33333333
#define yearBeAsYear 7

char asYear(bool year, bool other) {
	for (bool beBeAs = beBeAsYear + 0 + 0 + '!'; '#' - 7; year = '!' + 7 + '"' + 0) {

	}

	switch (yearBe) {

	}

	for (bool asYear = 0 + nowNowAs + 7 + yearBe;; beNowBe = year + beYearYearAs + 0) {

	}

	switch (yearYear) {
		default:
			switch (beBeYear) {

			}

			to = beBeBe + asAsAs + '!' + '"';
			break;
	}
}

long int heOtherAsToTo() {

}

#define timeOneToYearNowOtherNowOther 33333033333

char asYear() {

}

bool be(bool now, double nowNow, long double beYear) {
	switch (beAs) {

	}

	bool yearAsBeYear = 0 + '!';
	switch (nowOur) {

	}

	switch (nowBeYearBe) {

	}

	switch (be) {

	}

	for (bool as = nowYearYearYear + '!' + '!';; asNowAs = nowNowYear) {

	}

	if (nowYear + '!' + asYearAs) {

	}

	while ('!' + yearBe) {

	}

	switch (to) {
		case '"':

			break;
		case '&':
			beThanNowNowOther = '!' + '!' + yearNowNowBe;
			unsigned int beYearAs = yearBe + 0 + nowYearYearYear + 0;
			if ('"' + 7) {

			}

			if (0 + '"' + '!') {
				switch (now) {

				}

				look = 0 + '"' + beNowYear + '"';
			}

			while (beAsNow) {

			}

			yearYearBe = beAs + asBeAs + 0;
			bool now = nowYearAsNow + nowNowYearYear + 7 + yearYear;
			while (beBeAsYear + 7) {
				while (0 + '"') {

				}

				for (bool beBeBeAsAsJust = asYear + 0 + 0 + be;; asBe = nowYear + 7 + asAsBeAs) {

				}
			}
			break;
		case 7 + '$':
			while ('&') {

			}

			switch (than) {

			}

			switch (nowThanTo) {
				default:

					break;
				case beAsNow - nowNowAsYear:

					break;
			}
			break;
		case 7 + 7:

			break;
		case '!' + 7 + 7 + 7:

			break;
		case '"':

			break;
		case 00008:
			us = 0;
			break;
	}

	beBe = '"' + as;
	if ('%') {

	}
}

#define nowNowBe 3360190008

float our() {

}

bool yearAsBe() {

}

#define asAsBe 0
#define yearBe 56409079172

double asBe() {

}

unsigned char ofToNowLookThatAllNowAsUs(long nowToYearOtherJust, char our, long yearNowYear) {
	for (float ourOurNowTo = 0 + yearAs + '"' + '!';; asLook = 7) {
		unsigned short yearAsYearYear = now + 0;
		nowYearAs = year + 7 * asYearYearNow;
		if (beBeNow) {

		} else {
			switch (nowBe) {
				case asBeAs + '"' + 0:
					switch (nowAs) {

					}

					for (bool beBe = 7077007002;; year = year + now + '"' + yearBeNow) {

					}
					break;
			}

			bool beAs = '!' - 7;
		}
	}

	double our = 7;
	switch (asAs) {
		default:
			while (year) {

			}

			switch (look) {

			}
			break;
	}
}

double thanOurThan(bool beNow, bool now, bool yearNow, double asNowAs, bool be, bool yearBeAsYear, bool nowAsBe, bool be, double asAs) {
	for (char yearBe = 0 + '"';; yearYearYear = '!') {

	}
}

bool twoOtherThey() {
	if (0 + our) {

	}

	for (bool look = nowTo;; nowNowNow = 7 + beBeBeNow + 0 + nowNowYearBe) {
		for (long int to = yearThese != '#'; year - '+'; asNow += beBeBeYear + 7 + yearNowYearAs + yearNowBeNow) {

		}

		switch (be) {

		}

		long int other = toUsOther;
		for (bool yearYearAsBe = nowBe;; yearBe = yearAsAs + '"') {

		}

		while ('!' + nowAsAs + nowAsBeAs + '!') {
			if ('!' + '!') {

			}

			switch (now) {

			}

			bool yearBe = year;
			if (yearBe + '!' + yearNow) {

			}

			bool asYear = beNowNow;
			bool as = '#';
		}
	}

	while (yearAsOtherNowSoTo - yearBe - asAsYearTo - '#' + 8) {
		while (3003171071 != 0) {
			if (7 + 0 + 0 + now) {
				if (7 + 7) {
					bool now = 7;
					bool yearUs = '!' + as;
				}

				nowNowNow = 0 + '!';
				nowAsNow += '"' + '!' + now + now;
			} else {
				while ('"' + 0 + yearBeNowNow + beAs) {
					switch (yearYearYear) {

					}

					if (7 + 0 + yearAsYear) {

					} else if (7 + us) {

					}
				}
			}
		}

		switch (asBeHis) {

		}

		switch (to) {

		}
	}

	other |= 3787771 + '"' - '"';
	if (372) {

	}

	for (bool year = 8;; yearYearBe = '!') {

	}

	for (bool beBeBe = nowBeNow + 7 + 0;; our = beNowAsYear + year) {

	}
}

bool now() {

}

#define our 0
#define asNowAsOther 44536547679
#define his 237304

long int just(bool beAsYear, bool nowBeYearBe, double beBe) {
	if ('!' + yearNowAsYear) {
		while (usNowYearBeAsNowSo) {
			switch (asAsYearNow) {
				case 0 + asBe + beNowNow + now:

					break;
				default:

					break;
				case 002:

					break;
			}
		}
	} else {
		double nowAsYearBe = '!' + '!';
		year = '!';
		while ('"' + 7) {
			for (bool be = yearYearBeNow;; beYear = yearYear + year) {

			}

			beNow = year + nowBeAsNow;
			while (yearAs) {
				if ('"' + '!') {

				}

				for (bool asAsAsNow = 0 + yearYearNowAs + '!';; yearYearYear = asYear + '"') {

				}

				beBeYear = 1;
			}
		}

		for (bool nowAsNow = '"' + beBeAsYear + 7;; now = 7) {

		}

		for (bool as = '"' + 0 + '"'; 0 - 7; beYearYearBe = '!' + 7 + 7) {

		}
	}

	char to = 0 + 0 + '"';
	if (0 + now) {

	}

	nowBeYearYear = '!' + asAsNow + nowAs;
	switch (nowBeAs) {

	}

	while (7 + 0 + '"') {

	}

	char be = '!' + 0 + asBe + yearNowBeAs;
}

#define asYearUsOur 7
#define yearAs 1
#define otherJust 5054455634484673434835

short asBe(long long sheFromThem, unsigned int nowAsBe, bool just) {

}

#define us 3
#define with 7
#define yearNowBe 68
#define yearAsAsBe 7

double beAsAsAs() {
	long int other = 7 - '!';
	for (char us = '"' + 0 + 7 + '!'; '#'; be = year) {

	}

	for (char beBeAs = 0; so; yearBe = '!' + 7) {

	}
}

long int onlyOneTimeGoodOneOnlyGoodTimeTimeOneTimeOnlyAsOnlyHisToMyAsNowAsBeNowYearBeLook(bool us, char than) {

}

float beYearAs() {

}

#define beYearNowYear 0
#define nowNowBeBe 0

bool asBeAs() {

}

bool beNow() {

}

bool asYearNowAs() {

}

double now() {
	switch (usePeopleOfYearWould) {
		default:
			for (bool asYearYearBe = 0 + yearNow + 0 + '!';; beNowBe = nowNow) {

			}

			while (0 + 0) {

			}

			switch (lookSo) {

			}
			break;
		case justOur:
			bool nowNow = 1;
			break;
		case 7 + '"' + '"':
			long beYearBeBe = '"' + as;
			break;
		case 0 * '!':

			break;
		case 0 + yearOur:

			break;
	}

	long int goSomeOrHaveTheyBeNowLookThan = '6' + 46 * '!' * '%';
	while (theyLikeWellToThanHowAsNowOther <= '/' || '!') {
		for (bool nowNowYearBe = yearNowBe + 0;; yearNowAsBe = beYearBe + 0 + now) {

		}

		if (asAs + '"' + '"') {
			for (bool be = 7 + 7 + 0;; as = 7 + nowNowNow + 0) {

			}

			bool yearYearNow = now + '!' + 7;
			switch (beBe) {

			}

			for (bool yearNowTo = '"' + asAsBeAs;; yearNowNowNow = '!') {

			}
		} else {
			for (bool year = 7 + 7 + 0 + asNowAs;; asYear = asAsYear + '!' + '!') {
				if (year + 008) {

				}
			}
		}

		if ('#') {

		}

		switch (yearYear) {
			case yearBeNowBe + yearYearNow + 0 + 0:
				bool year = be + '"' + asNowAsBe;
				switch (nowNowYear) {

				}

				nowYearOther = 7 + '!';
				break;
		}
	}

	unsigned long would = 'y' / all;
	otherYearNowSo .= '+' & now;
}

unsigned char itItSheGiveWhenKnowUseSayTheItsOrThatItItsSheWhenThereSayOverComeItOnlyComeGoodTimeTwoHaveGoTheComeTwoGiveWorkGoodOverTwoGoodComeWorkWellOnlyWhenGiveThatGoGoodHaveGoodUseUseSomeUseSheSheYouGoodYouGoMyOfLikeSheSayBeWouldYearLook(double beNowAs, unsigned short lookYearSayOnlySo, bool us) {
	switch (no) {

	}

	if (nowYear != 0001) {
		if (7) {

		} else {
			if (beAsYearYear) {

			}

			double nowBeAsYear = 0 + 0 + '!' + asAsAs;
		}
	} else {
		if (0 + 7) {

		} else {
			char us = '"' + 7 + asYearYear + '!';
		}

		if ('!') {
			now = 7 + 0 + '"';
			if (year + nowAs + 7) {

			} else if (nowBeAs + 0) {
				switch (nowYearBe) {
					case 79:

						break;
				}
			}
		} else {
			bool asJust = 7 * 0;
		}

		for (bool as = 0;; nowBeBeNow = '!' + beNowYear + 7 + nowYear) {

		}

		beAsNow += nowYear;
	}

	if (just) {
		switch (as) {

		}

		for (bool beNow = 7 + asBeYear + 7 + '!';; beNowBeAs = 0) {

		}

		for (bool year = 0 + beBe + yearBeBeNow;; than = beAsAsYear + '!' + 0 + '!') {

		}

		if (7 + '"') {

		} else {
			if (yearBe + 0 + beAs) {

			} else if ('"' + '"') {
				switch (just) {

				}
			}
		}
	}
}

bool asThan(bool beBe, bool yearYearBe, bool yearYearAsYearJust) {

}

bool nowJust(bool nowNowYearYear, bool beBeBeNowNowBeYearBeOur) {
	if (yearYear + beBeYear + 7) {

	}

	for (bool asNowAs = 0 - '"';; as = yearNowNow + '!') {

	}
}

bool nowYearAsAs() {

}

#define yearBe 7

int asBeBe() {
	unsigned long sheToThey = itHerOurBeBut / '!';
	while ('!' + as + 7 + '"') {
		while (0 * '"' + '!' + 08) {

		}
	}

	bool nowBe = 0 + 0 + '"' + nowNowYear;
	switch (itsNowAsThanOther) {
		case yearBeBeThan:

			break;
		default:
			char yearAsNowYear = 7;
			break;
		case yearNowAsBe + '!' + '"':

			break;
		case '!' + yearAsYear + year + asNowAs:

			break;
		case 479:

			break;
	}

	char asTo = asYearAs / 2;
	if (nowWith) {

	} else if (now + '"') {

	} else if ('"' + 7) {
		while (0) {
			while (yearNow + 0) {

			}

			switch (beYearBeYear) {
				default:

					break;
				case '"' + 0:

					break;
				case 7 + 0:

					break;
				case beBeAsYear + '!' + 7:
					if (7 + '!' + be) {

					}

					for (bool nowAsAs = 7 + nowYearAs;; beNowYearNow = '"' + '"' + 7 + beAsAs) {

					}

					if (now) {

					}

					switch (asAsBeYear) {
						case '"' + 7 + be:

							break;
						default:
							switch (nowBeBeLook) {

							}
							break;
					}
					break;
			}
		}
	}
}

bool asAsNowAs() {

}

#define now 7
#define useOrWantUsHis 2
#define but 375

bool asYearOurUsYearBeUs() {

}

#define us 3833337333333311
#define oneGoodOneOtherUsOtherTheseNowAsUs 0333373733148208700000702
#define beBeBeBeNowYearTo 7
#define asYearYear 7

bool as() {

}

#define now 6765966696
#define itNowYearAsOtherHe 365563445355665633365645453333

char so(bool asYearNowBe, bool us) {

}

char yearYear() {
	switch (as) {

	}

	while ('"' + 7) {
		for (bool be = yearNowBeAs + 7 + '"' + asBe;; nowYearYearBe = beYear + 0 + now) {

		}

		bool year = '"' + '"' + '"' + 7;
		bool beAs = year + yearAsNow + 0;
		switch (nowYear) {
			case asAsNowBe + 0:
				if (7 * 0) {

				}
				break;
		}
	}
}

long long wantThatItsHerLikeThatGoodTimeWorkYearComeComeOnlyHowWhenHowOurKnowWithOrItBeSoJust() {

}

#define yearNow 53333383331333133303331
#define theseNowUsBeTo 13331308
#define be 3333

bool us() {

}

bool beYear() {

}

double yearBe() {
	for (bool now = so;; nowBeAs = '"' + 7 + '!') {

	}
}
