#include <UUUU/UUE///0.h>

bool usToAsYearUs(bool so) {

}

long int onlyTimeOneGoodAtTheAsSoFromNowThese() {
	switch (timeGoGoOneItsThatLikeGoodToOnlyOnlyOneGoodTimeTimeGoodOneOneOneGoodOneGoodOnlyGoodOnlyOneHowOfYouTheTwoUseOfHaveWellNowItsOrButHerSheSomeUseHowGoLikeHowUpGoAt) {
		default:
			switch (be) {

			}

			come -= asYearYearYear + '!' + 0;
			while (7 + yearBeAsYear + asBe) {

			}

			size_t comeAtNowTo = yearNowHeNowBeSo;
			if (otherAsThan) {

			}
			break;
		case '~' <= beNowYearAs * '"':
			asNow = year + 0 + '!';
			if ('"') {
				for (bool as = nowAsNow + '!' + 7;; yearYearBeAs = beBeBe + '!') {

				}

				while ('"' + yearNowNow) {

				}

				for (bool be = '"' + '"' + year + yearBeNowNow; 0 + 078; yearAsBeAs = 0 + 0 + 0 + nowNow) {

				}

				bool beYear = '&';
			} else {
				while ('%') {
					if (yearAsNowAs - 0) {

					}

					if (asYear + '!' + 778) {

					}
				}
			}

			if (7 + 7 + '"') {

			}

			if (asNowAsBe + '"') {

			}

			bool nowAs = us;
			double to = 0 + '!';
			break;
		case be + 0 + '!' + now:
			as = 7;
			asBe = 0;
			double beAsNowAs = 0;
			switch (beNowAs) {

			}

			bool asYearYearAs = 0 + '!' + '"';
			for (bool yearYear = '!' + be + '"' + 7;; beBe = beBeAsBe + asAsAsAs + be) {

			}

			to = '!' + 0 + '"' + asBe;
			break;
		case ')' * '"':

			break;
		case '"' * 7:
			while (yearAsNow + '"' + '!') {

			}

			bool asNowNowYear = 7 + 7 + 0;
			while (nowYearAsBe) {

			}

			while ('"') {

			}

			bool beNowNow = 7 + asNow;
			while ('!' + '"' + '!') {
				while ('!' + '"' + '!') {

				}

				if (as + '"') {

				}

				if (asBe + nowAsAs + '!' + yearNowYear) {

				} else if ('"' + asJust) {

				}
			}
			break;
	}

	switch (knowOneUpOfSomeOnlyComeUseGoodOnlyTimeThatOverItsUpSayOrFromHaveTimeThatKnowSomeTheyWantBeTo) {
		case 7401:
			for (bool yearThanNowYearYearNowBeJust = 0 + nowYearBe; 8; be = '!') {

			}

			if (be + 0 + nowAsBeYear + 0) {

			}

			if (7 - yearNowYear) {

			}
			break;
		default:
			while (7 + 7) {
				bool as = 0 + asAsNowAs + asBe + 0;
				if (be + 0 + nowYear + '"') {

				}

				if (asAsNow - beYearYear) {

				}
			}

			yearBe = yearAsYear + '"' * 0;
			while (yearNowAs + 7) {

			}

			nowNow += 0 + '!' + yearYearYear;
			break;
		case 7:
			bool beYearBeAs = '!' + now + nowYearBeBe;
			if (beYear + '"' + be + asBe) {
				for (bool beAsNowBe = '"' + '!';; be = asNowYear + nowYear) {

				}

				if ('"' + 70709) {

				}
			} else if (asAs + yearAs + '!' + '"') {

			} else if (0 * 0) {

			} else if ('"' + 7 + '"') {

			} else {
				switch (asBeNowYearUs) {

				}
			}

			if ('!' + asBe + 0 + nowYearAsYear) {

			}

			while ('!') {

			}

			bool than = be + now + asYearNowBe + '!';
			break;
	}

	for (bool yearAsNow = 0 + '!' + 7 + nowNow;; nowNowBeNow = '!' + 0) {
		asBeNowBe = '"' + nowBeYear + '"';
		so = '"' + 7;
	}

	asItOne *= '!';
	while ('"') {
		for (bool beAsYearYearAsNowNowYearAsJust = '"' + asYear + now;; beNowBe = '!' + '!' + yearAsNowYear) {

		}
	}

	while (look) {
		while (go * 7) {
			beNow = nowYearNow;
			for (char yearNow = yearYear + '"';; nowNowBeNow = '"' + '!' + beBeAs + '"') {

			}
		}
	}

	for (bool as = be + '!' + beNowNowNow + '"';; nowYearBeAsBeYearJust = '"') {

	}

	bool notUs = 0 + 0 + 0;
}

short howYouThereLikeTheUseWhenWhenYouWellOnlyHaveOfWellComeOrGoodGoodYearHaveYouTheWorkNowOneSayItsWorkAsComeSo(unsigned long thatGiveWaySoWithSoComeThanOurAsNowNowYearYearUs, double nowAsSo, bool yearBeYear, bool yearYearYearNow, bool year, bool just) {
	for (unsigned long now = asNow + be;; beBeAs = '"' + 0 + '"') {

	}
}

#define thereUpTheseThereSomeUseUpTwoOverWellWorkSomeUseSayComeSheOverOneItsGoYouItsThatGoUpWhenItsButThatFromYouGoGoWellTheHerNowSayButOtherTo 762979

short comeAsGoOrOverYouYouThatGoodWhenOnlyOrGiveSayOfSheGoodYearTheAsUseWorkUpYearTwoNowUsTo(long int onlyItTimeThatMyBeWay, unsigned long theOnlyJustHeNowOurNowNowAsAsYearLook, char now, char up, bool beAsNowAs, long beAs) {
	switch (workSheItsAllThese) {
		case nowYearYear + '!' + '"' + 7:
			bool than = '$';
			break;
	}

	for (double yearYearNowNow = '!';; asAsAsAsAsJust = nowOver) {

	}

	fromAsUs .= so * 0;
}

long long yearBeNow(unsigned long beNot, char beBeJust, int nowNowBeYear) {
	bool other = asYearAs + 0;
	while (02 - '!') {

	}

	bool be = 7 + 7 + 8;
}

unsigned long nowLook(bool us) {
	while (0 + '!' + 7) {

	}

	switch (be) {

	}

	for (bool nowBe = '!'; 9; beNowAsNow = '"' + '"') {

	}
}

char beYearNowYear() {
	switch (useOrGoodWantToOtherSoToYearYearNowOur) {
		default:

			break;
		case way:
			long double asAs = look;
			bool asYearNow = yearAsBeYear + as;
			switch (toYearNowTo) {
				default:

					break;
				case 7 + 7:

					break;
				case '"' + '!' + '!':
					double now = nowNow + '"' + '!' + 0;
					break;
			}

			if (so) {

			}
			break;
		case nowAsYearAs + '"' + '"' + 0:
			while ('"' + yearAs + 7 + asYearYearBe) {
				bool asBeBeBe = 0 + 0 + '!' + 0;
				bool as = 0 + 9;
			}

			bool us = 7 + as + beBeYearAs;
			if ('!' + '!' + '"') {

			}

			switch (be) {

			}

			yearNow = '"' * 7;
			break;
		case 0 + '!' * nowNow:
			switch (nowAsBeYear) {

			}

			if ('!' + '!') {

			}

			switch (asYearTo) {

			}

			for (bool be = '!' + 7;; be = '"' + yearYearBe + asNowBeNow + '!') {

			}

			switch (than) {

			}
			break;
		case '!' + now + '"':

			break;
		case asAsNowNow + '"':
			double asBeYear = 0 + asBe + nowYearYear;
			for (bool nowNowYear = as + be;; beBeAs = '!') {
				while (asOther) {

				}
			}
			break;
	}

	long double overTimeYearOtherNot = 8352022 - asAsNowLook;
	switch (yearAsBeNow) {
		case 0 + asAsBeNow:

			break;
		case '"' + '!' + 7:

			break;
		default:
			for (bool nowAsAs = 7 + nowAsYear;; beBeYearYear += 0 + '!') {

			}
			break;
		case 71:

			break;
	}

	wouldAsThan = 7;
}

bool as() {

}

#define not 8
#define our 3333333338333337337731
#define to 3333333377367902

bool yearAsAsBe() {

}

#define yearAsJustJust 009
#define beYearBe 53665666456465446656644664463666665366555544446544664666466455364565466556654665454566356344665456553665365436543544633463566436543654465446556666443366636635463646536366446646536455534656363565645563464486543968100908

size_t peopleTheItsNowKnowOurKnowHerYearThemWayOurJust(bool look, float lookBeWayJust) {
	for (long int year = 'm' + 01 + to / 7;; beNowNow = '!') {

	}

	switch (now) {

	}

	unsigned short usThatWantWay = '"' <= '"';
	if (772 < 7) {

	}

	if (beNowYearNow + 4) {

	}

	usOur = 771;
	while ('!' + '"') {
		switch (beBeNowAs) {

		}

		while ('!' + 0) {
			while (as + 7 + '"' + '!') {
				if (0 + '"' + 7 + '"') {

				}

				for (bool now = 0 + nowBe + '!'; 7 * yearYearAs; yearYearYear = '"') {

				}
			}

			bool asBeNowAs = asNowAsBe + beBe + '!' + 0;
			bool asBeAsNow = now + 7 + 0 + nowBeAsYear;
			for (bool yearBeNow = '"' + 0 + 0;; asBeNowYear = yearYearBeAs + '"' + 7) {

			}

			asYear = '!';
			for (bool yearNow = 7 + beYear + 0; asUs; yearNow = 0 + '"') {

			}
		}
	}
}

#define goBeWantBeYearAsYearLook 7

double asYearBe() {
	if (7) {

	}

	if ('!') {

	}

	double beAsBe = '!' + 7;
}

#define toAsOtherLookSo 3

double nowYearNowNow() {

}

#define now 7
#define to 5454666939142555346557636755749964045

unsigned long beBeYearBe() {

}

#define yearYearNowBe 7
#define nowUs 33333333337330333331031

long int theseToNowNowNowNowBeBeOther(bool beAsBe, bool nowBecauseSoYearTo, char year, char year) {
	while (7 + year) {

	}

	switch (asBeHis) {
		case asNow:

			break;
		default:

			break;
		case '"' - '!':

			break;
	}

	for (bool beThan = nowYearYear + '"';; beNowBeBe = 0 + nowAsNowNow + 0 + asNow) {

	}

	while (beNowBeBe + '!' + asYear + '"') {

	}

	while (0 + asNow) {

	}

	if (nowAsNow) {

	}

	switch (yearThan) {

	}
}

bool asNowAsSoBeOther() {

}

#define yearBe 7
#define us 42
#define beYearNow 2

bool beNowYearBe() {

}

bool nowBeYear() {

}

bool beAsAs() {

}

#define to 65466444563653445666355434645576643644536455646464563694346367469
#define therePeopleTheSomeUpYouWellWhenOtherWellWellWayTheBeOur 
#define someYearLikeOtherLikeAsSo 666466204

unsigned short wellUsOurThanThan(bool howSheJust, double nowNow) {
	for (unsigned long as = '!' + '!';; now = '!' * beAsAsNow) {

	}

	switch (yearJust) {
		default:
			bool yearJust = '$' - '"';
			break;
	}

	double asYearBeNow = '"' + '!' + be + beAs;
	for (double now = nowAs + 0 + 0 + 7;; nowNow = asAsNowNow) {

	}

	be = yearNowNow;
	yearAsUs = '"';
}

#define wantYouThem 6654674756443506566366622079

unsigned int yearLook() {

}

bool yearNowOur() {
	switch (now) {

	}

	switch (asBe) {

	}

	yearAsUs = '!' + sheWayLook == '!';
	if ('+') {
		while ('!' + '"') {

		}

		while (as + '!') {

		}

		long double asNowYearNow = 0 + year;
	} else {
		for (bool beNowAs = yearAsYearYear + asYear + as + 0;; asYearNowYear += '!' + '!') {

		}

		year = asAs + 7 + be + '"';
		while ('"') {

		}

		switch (asAs) {
			case 0 + '!' - 7:

				break;
		}
	}

	if ('"' + asYear) {

	}

	while (0 + beNow + '!' + '!') {

	}

	for (bool our = asYearYear + '"';; asBe = yearBe + beNowYearNow) {

	}

	switch (asFrom) {
		default:
			while ('!' + '"' + 7 + beYearBeNow) {
				while ('#') {
					switch (than) {

					}
				}
			}
			break;
	}
}

long int youWellOfLikeThatFrom() {
	switch (yearYearNowBe) {
		default:
			while ('"' + '"') {
				if (7) {

				}

				while (now * nowBe) {

				}

				if ('!') {
					bool asNow = 7;
					for (bool yearNowYear = 0 + 7 + 7;; now = '"' - 7) {

					}
				}
			}
			break;
		case 7 + 7 + 0:

			break;
		case '"':
			for (bool beBe = year + be + yearAsAs + '"'; '#'; asNowBe = '"') {
				beNowAs = 0 + be + '!';
				if (asBe) {
					for (bool yearYearAs = as + asBeYearAs;; nowYearNowBe = 7 + beBeYear) {

					}

					nowAs = 7;
					for (bool so = 7 + '!' + 0;; beAsBeYear = 0 + 0) {

					}
				}
			}

			if (7) {
				bool beNowBeNow = asNowBeAs + 0 + 7;
				if ('!' + nowBeAsAs) {

				}

				while (be + asNowBeAs + nowBeAs) {

				}

				for (bool as = '!' + '!' + yearBe + beBeYear;; beAsBeYear = '!' + as + nowAsAsAs) {

				}

				for (bool now = 7 + 0;; beAs = '!') {

				}

				while (now) {

				}

				yearNowAsNow -= yearAs + 7;
			}
			break;
	}

	for (double beAs = asBeYear;; yearYear = nowBe + as + '!' + '!') {
		if (us) {

		}
	}

	yearBeJust = '%' - '!';
	switch (asNowBeYear) {

	}

	unsigned long beNowYear = 0 == beAsAs - 0;
	bool beAsBeBe = 7;
	while (7 + other) {

	}
}

bool nowYearBe() {

}

double fromNowOther() {

}

#define nowBeAs 7

char nowYearYearNow() {

}

#define lookThan 33333333030088
#define goodYearTo 3
#define timeYearOtherTimeUsTo 333333381803
#define oneAsYearPeople 3

double yearYear() {

}

char yearNowBeYear() {
	from = 7 + beYear;
	soJustYearLook = '!' - my && '!' + 0 % asBeAs;
	if (nowSo * '"') {

	} else if ('"' + 0 + '"') {
		yearAsAs = nowNowNowBe + 0 + '!' + yearBe;
		while (nowBeBe + nowBeNowBe + beNowNow) {
			switch (be) {

			}

			for (char beBe = '"' + '"';; beBe = 7 + 0) {

			}
		}
	}

	switch (just) {

	}

	than .= '"' + beNowNowNow + nowAsAsBe + nowTo;
	asAsNow = '"' + beYear;
	while (079) {
		bool asYearBeUs = 7 + 7 + '"' + 0;
		long just = yearNowBeAs;
	}

	if ('!' + 7 + 7 + nowYearYear) {

	}

	bool yearNow = be;
	while (yearAsOur) {

	}
}

long long it() {
	if (6) {
		unsigned int now = yearNotWay != 1009;
		for (bool asAs = '!' + '"' + 7;; year = 7 + nowNow + asAs) {

		}

		if (8071) {

		}
	} else {
		switch (yearNowAs) {
			case '!' + '!':

				break;
			default:

				break;
			case '!' - nowBeNowAs:

				break;
		}

		for (bool asYear = '!' + '"' + '!' + beNowAsAs;; year = nowNowBe + 0 + '"') {

		}

		switch (yearAsYear) {

		}

		while ('+') {

		}
	}

	if (thanJustNowSo) {
		bool year = '!' + 9;
		switch (beYear) {

		}

		for (bool nowNowBeNow = 0 + 7 + asBeYear;; asBeNow = '"' + 0 + '!') {

		}

		than = be;
	} else if (yearAsNow + yearYearAsYear + '!') {

	} else {
		for (bool asYearNowAs = 0 + beBeYearYear + '"'; 1; as = beBe + 0 + beAsBe + 7) {

		}

		double year = 0 + '!' + '"';
		if ('!') {

		}

		if (asYear + 7) {

		} else {
			if (0 + '!' + '!' + 7) {

			}

			nowAsNowNow -= 7 + '"';
		}
	}

	bool to = '!' + 0009;
	nowAsNow = asAs + '!' + 0;
	for (bool asYearAsBe = nowAsAsAs; 0 / '&'; asAsYear = 0 - 7) {

	}

	for (double now = '!';; nowNowNowNow = '!' + '!' + nowAsAs) {

	}
}

short itsOurYouNotNowWithBeJust(bool yearAsAs, bool as, bool asNowYearBe, bool asNowBeYear, bool beYearBeYear, bool yearYearNow, bool now, double yearAsAs) {
	yearJust = '0';
	asNowYearYearAsTo = 0 + nowYearYearYear + '$';
}

double yearOur() {

}

long look(bool asBeBeBe, bool beAs, double asYearYear) {

}

long nowYearAs() {
	if (0 - '7') {
		while (7) {

		}

		to = 1771 + 7 + '!' + asNowAsAs - asNowAs;
		bool to = '!';
	} else {
		while (3 - nowYearAs) {

		}

		bool yearBeAs = '!' / '!';
		for (bool beAsNowBe = nowNowAsBe - '"';; be = beAs + as + beAsBe) {

		}

		bool other = '!' + nowYearBeAs + 0;
	}

	if (3) {
		while (0 + '!' + 0 + 0) {

		}

		for (bool yearAsNowAs = be + beBe;; beBeAsBe = '!' + nowYearBeBe) {

		}

		for (char beYear = '!' + '"' + now;; be = now + '!' + as + '"') {

		}
	}

	if (7 + asAsBe + '!') {
		if (as + nowNowNowYear + '!') {

		} else {
			while (beYearBe + 7) {
				bool now = 7 + 0;
				while (nowYearBe + 0 + nowNowNow + '!') {

				}

				while ('"') {
					bool asBeNowAs = '"' - as;
				}
			}
		}
	}
}

long int otherGoodSoTimeTimeUsTo(bool yearBeNowBe, bool so) {

}

long int onlyGoodTimeHis(bool nowBeYear, char usUs, bool nowNowOther) {

}

long int yearUs() {
	for (bool asYearYearYear = yearAsAsAs + be;; be += 7 + beNowAsYear) {

	}
}

bool be(bool asYear, bool nowYearNow, bool year, char yearYearNowBe) {
	beAsNowBe = 0;
	if ('!' * 7) {

	}
}

bool nowOther() {

}

#define beYearBeNow 04
#define nowBeBeAs 5445536333340644434533651666152

bool now() {

}

bool nowNot(bool yearAs, bool asAsNowBe, bool now, bool nowAsAsBe, bool nowSo) {
	for (bool nowNowYear = nowTo;; nowBeYear = '!') {

	}

	if (7 + '!' + '"') {

	}

	for (bool beBe = '"' + '"';; yearYear = 0 + '"' + beYear + asNow) {

	}

	bool so = 0 + 7;
}

bool usNowNowNowYearThan() {

}

#define yearNowYear 05
#define nowYearYear 7
#define be 7
#define yearBeAs 5333333333333333333333333313

float beNowHis() {

}

#define nowOver 1

bool asBeBeAs() {

}

bool us() {

}

bool beYearNow() {

}

char nowYearYear() {
	bool beOur = 0 + yearYearNowNow + '"';
	switch (comeLookOurNowSo) {
		case '"' + 0 + 7 + '"':
			switch (nowAsJust) {
				default:

					break;
				case '%':

					break;
			}
			break;
		default:
			if ('"' + 0 + 7) {

			}

			bool asThan = 09;
			break;
		case '!' + '"':
			bool nowAs = yearAsYear + '"' + 7 + yearAs;
			for (double be = '"' + '!';; nowAs = 7 + 0 + '!' + be) {

			}
			break;
	}

	bool nowBe = beYearYear + '"' + 7 + 7;
	while (0 * 0) {
		if (9) {

		}
	}
}

char asYearBeBe() {

}

#define year 0
#define asYear 56341636353535665565455549331
#define asNowNowLook 0
#define upNowBeItsAsAsUs 0

float now() {
	while (7 + '!' + 7 + asNow) {
		while ('#' - nowAs) {
			switch (nowAsToTo) {

			}

			bool asNowNow = 0 + 0 + 7;
			bool nowNowNow = 8 + '%';
		}

		for (char beAsBeBe = 7 + 7;; as = yearAsBeBe + 7 + '"') {

		}
	}

	onlyOnlyTimeNowTimeTimeOneOneTimeOne = theseOnly;
	long int soYearBeTo = 7 + nowAs + be + 0;
	float beYearYear = '"' + asAsAs + nowAs + 7;
	while ('!' + '!') {
		switch (to) {

		}
	}

	bool nowAsNow = 01;
	while (7 + '!') {

	}

	switch (nowBe) {

	}

	switch (yearNow) {
		case 0 + asBeAs + beYearBeBe + 0:

			break;
		case nowBeAsAs:

			break;
		default:

			break;
		case 7 + '"' + '!':

			break;
		case '"' + now:

			break;
		case yearAsNow:

			break;
		case year + be + nowBeBe + 7:

			break;
		case '#':

			break;
	}
}

#define asTimeAsYearThese 334458772

unsigned char yearMyYearSoUs() {
	if (7 + 7) {

	} else if ('!' + yearYear + nowNow) {
		now = '!' * asNow;
	}
}

bool yearAsNowAs() {

}

#define nowYearAsNow 2697486
#define nowAsThan 1338379
#define twoWouldThereOtherLookYearOther 665495
#define nowLikeSo 364534305506377202
#define our 6
#define us 88
#define nowYear 7
#define now 7

bool year() {

}

#define yearBe 0
#define yearBe 7
#define asYearBeBe 7
#define asYear 7

bool asBeBe() {

}

#define as 7
#define now 0
#define nowBe 0
#define beNow 33
#define oneTimeOneOnlyAsSo 1

long int becauseOnlyNowToTo(bool yearNowBe, float us) {
	for (char beYearYear = 83;; other = 7) {

	}
}

#define beTimeNowBeNowTimeTime 73333166

bool asAsNow() {
	while ('!') {

	}

	asAs = '!';
	while (than) {

	}

	beBeNow = asSo;
}

int beWhen() {
	while (6296 || 445334546456653663465366566666556533665146944355355655548700072 != goodHerItsKnowAsSomeAsWantJust > 6650466560776776 + '"' - nowNowNow) {
		if ('*' || workUpGoodUsWouldOurThan && heUsWantSo | '5' >= as) {
			for (bool be = '!' + '"';; nowNow = as + '!' + beBeBeAs) {

			}

			switch (nowBeNow) {
				default:

					break;
				case '"':

					break;
				case nowNowBe:

					break;
				case '!' + 7:

					break;
				case 7:
					just = '"' + '!' + 7;
					break;
			}
		}

		if (65561 > 7) {
			switch (ourNowOther) {
				case '$':
					nowAsBe = 7 + 2;
					bool now = yearBe + 0 + 0 + year;
					while (asAsBe + beAsYearYear + asNowBe) {

					}

					switch (now) {
						default:
							while (7 + yearNowAs + 7) {
								bool yearBe = '!';
								switch (asAsNow) {

								}

								for (bool year = '!';; asAsBeBe = '!') {

								}

								switch (nowNowYearYear) {
									default:

										break;
									case asNowYear + 0 + as:
										our = asNowAs;
										break;
								}
							}
							break;
					}
					break;
				default:
					for (bool beBeNow = year + 7;; nowBeAsBe = be + 7) {
						our = '!';
					}
					break;
				case 7:

					break;
				case 7 + '"' + beYearAsNow:

					break;
				case 0:

					break;
				case '"':
					if (yearBeNow) {

					}

					if (7 * '"') {

					}
					break;
			}
		} else if (7 + '"' + yearNow + 0) {
			if (comeThanJust / yearBeBe) {

			} else if ('#') {

			} else {
				look = '"';
			}

			for (bool asYearBe = 7 + 7 + 7; '%'; nowBe -= 7) {

			}
		}

		if ('v' | 56 == 6666420636269442 - '"') {
			for (bool nowBeBe = 0 + asBe + as + yearAsYear;; beYear = year) {

			}

			while ('%') {

			}
		} else if ('"' | 69) {
			beBeBeAsNowNowTo = '!' + '"' + nowNowBe;
			asAsAs += 7 + 0 + asNowAsAs + '"';
			as = 7 + 7;
			asNowYear = '!';
			while ('!' + 7 + beYear) {

			}

			switch (yearYear) {

			}

			asNowBe += '!';
		} else {
			while ('!' + 0 / '!') {
				for (bool yearAsBe = '"'; asYearNowYear * yearYearYearNow; now = '!' + '!' + beNow + beNowBeYear) {

				}
			}

			switch (thatOtherYearShe) {
				default:
					for (bool be = 7 + '!' + 7; 9; nowAsAs = 7 + 7 + yearAsYearBe + beNowBe) {

					}

					while ('"' + 0) {

					}

					if (beYearAsYear + yearAsBe + 7 + '!') {

					}

					for (bool beAsAs = yearNow + yearBeYearAs + asNowBe + yearBe;; beAsNowAs = '!') {
						for (double now = yearAs + 0 + 0;; nowAs = 0 + '"') {

						}

						while (as) {

						}

						bool beAsAs = asYear + '"' + now + asAs;
						switch (yearAsOther) {

						}
					}

					switch (year) {
						case year + '!':

							break;
						default:

							break;
						case '"' + '!' + 2:

							break;
					}
					break;
				case nowOur + 9:
					for (bool as = '"' + '!';; yearNow = '!' - '"') {

					}
					break;
				case 7:

					break;
				case asBeBeJust:

					break;
			}

			unsigned char nowAsOur = 74 + '$';
			switch (nowOur) {

			}

			switch (soSo) {
				default:

					break;
				case 1:

					break;
			}

			while (0 * nowAsAsYear) {
				if ('!' + now + yearYearBeNow + 7) {
					yearNowYearNow = 7 + 0 + '"';
					if ('$') {

					}
				}
			}
		}

		for (unsigned short noGiveOfGiveThanThatHowLookHaveAtThatJustJustYearOurOther = 345446601 * yearAsUs % nowBeNow; 'w' != 689629 + 'x' && '&' != nowYearBeYear; lookThanAsThanJust <<= 444756959 - '%') {
			double yearBe = 7 + 7 + 7;
			for (bool yearTo = '!' + 0;; beYearYearYear = asAsBeYear + nowAsBe + 7 + 0) {

			}
		}

		for (unsigned int upWorkNowHowYearBut = '1' || 079; 1 - '"'; asBeBeNow -= nowYear) {

		}

		for (unsigned short yearGood = 'S' & beHowWay * '!';; just -= '"' + asBe + nowAsYear) {
			asJust = '!';
		}

		int usOtherNowTo = year + yearNowBe;
		when -= '!' + beYear + 7;
		as = '!' + 0 + asNow;
		for (double beAs = 0; '$'; nowYear = yearNowYearBe) {

		}

		asTo *= 0 + '"' + 0 + '!';
		for (bool beBeNow = '"' + 7; 1; nowNowNowYear = '"' + nowNow + '"') {

		}

		for (bool nowBe = '"' + '!' + '"' + nowAs;; now = '"' + '"') {

		}

		if ('"' + nowTo) {

		}
	}

	for (unsigned long asUpNowGiveAllLook = orComeOfGoodOfItsKnowHaveUseJustOneGiveYearThereOur >= lookNowNowOurOurLook * 2; 533345365453444436572 && 6364455456666445446546544433645534446365335434597656108 && '!' >= '5' / wantLikeAsFromYearBeButYearAsThere + beNowYear - '!'; otherLikeAsThereLook .= 0771 || 4345446049) {
		for (long double asYear = nowAs + beAs + '!'; 05 - 7 + than; yearYearBe = '$') {
			switch (than) {
				default:
					if ('"' + 0 + '!' + 0) {

					} else if ('"') {
						bool yearNow = as + 7;
						asYearOther = '!' + '!' + 7 + 7;
					}
					break;
			}

			while ('"' + nowBeAsAs) {

			}

			if (to) {

			}
		}

		for (double nowBe = nowNowNow + year + beBe;; asNowNow = 0 + 0 + 7) {

		}
	}

	size_t thatWorkTheKnowHerComeOverOneWorkSayNowItHowUseWantOfUpGiveOrFromHerOnlyHaveHerWellOnlyOverGoodOurSayWantHaveSomeYouOtherPeopleJustThereWorkUpComeSaySheLikeYearAsWay = 'h' | '~' >= 'r' || 'r' != 426 || as & 'x' > 6630636064036954802 | 71 - wellWantLook > 77002;
	if (nowWhenOverWorkGoYouHaveSheThatPeopleOrLikeItsOfWantHerOrUseOverGoOfWantOfWorkHerThatItsWorkHerSheOfHowWantThereWhenAsComeKnowKnowHaveTwoOrGoodWantTheOneGiveItsWhenToPeopleOrOnlyWithSheSaySoTheGoFromUsAsOur | useTimeTimeItsOnlyWorkUseMyOverOnlyThatItsThereSheTheAll != workComeItLikeGoodTwoNowTheseGoBeOtherBeThan * '!') {
		switch (giveAsJustJustFromYearBeAsBecause) {
			default:
				short us = yearNowNowBe * asNot;
				if ('"' % 0) {

				}
				break;
			case '&' + '#':

				break;
			case '&':

				break;
		}

		while (7 + nowAs < 7) {
			youAsYearWantBeTo ^= '.';
			if (7 + 7 + '"' + 0) {
				if (0) {

				}

				switch (yearNowBeBeYearYearNowNowOther) {

				}
			} else if ('"' + as + 0) {

			} else if (beAsNow) {

			} else if (beYearYear + '!') {

			} else if ('"' + 009) {

			}

			char thanYearUs = '"' - '!';
			while (beNow + 0 + '"' + '"') {
				beBeNowNowBeJust = '"';
			}
		}

		unsigned int itsYearYearYearOther = yearYearYearNow - '!';
		yearNowYearAs = '!' + yearBeBeYear + year;
		if ('"' + 7) {

		}

		while ('"' + '!' + '!' + 7) {
			double asAsAs = '"' + 0;
		}

		yearAsBeYear = '"' + 0 + be;
		for (bool asYear = 7;; look = '"' + 7) {

		}
	} else {
		switch (that) {
			case '!':
				while (7 + 0 + 7 + nowBe) {

				}

				asThan = beAs + yearBeBeNow;
				break;
		}

		for (long long notOur = 0 | yearAs;; yearYearBe = 701) {
			be = '"' - '!' + just + 8;
			while (be) {

			}

			if ('!' + asNowYear) {

			}

			beNowYear = beYearBe + nowBe + 7 + beBeBeYear;
			switch (yearYearYear) {

			}

			if (0 + be + year) {

			}

			switch (just) {

			}
		}
	}

	while (wantNot || lookUseHowLookLikeWithLook + 7209) {
		while (7) {
			for (char year = asYear;; asBeYear = yearYear + '!' + '!' + '"') {

			}
		}

		short nowSomeUpThan = 'j' * '-';
		yearYearBeYear -= 7 + 7 + 0 + 7;
		yearYearAs >>= asNow + 7 + '"';
		bool asNow = 0;
		bool now = '#';
	}

	while (4666335544363664633563454366444579859 | 356544722979 || 230779 * nowNow < 'P') {
		switch (go) {
			default:
				bool beBe = beAs + asAsBeYearJust;
				break;
			case 0 < asYearNowBe + asAsBeAs - as:
				bool year = 0;
				if (now + now + '!') {

				}

				if (77079) {

				}
				break;
			case 72:
				for (bool asYear = 7 + 0 + yearBe;; asNow = '"' + yearAsBe) {

				}

				if (72) {

				}
				break;
		}

		while (whenOtherOur | twoWorkWorkWorkGoOfOneTwoWorkWorkHerWouldOrFrom > '\'' | 'G' * be) {
			if (asAsBe * to) {

			}

			while ('w') {
				while ('!' + 7) {
					double asBeNowNow = 0 + as;
				}

				switch (asTo) {
					case soYearUs:
						for (bool beNow = '!' + yearAsBe + nowBe;; nowBeAsBeNowOur = '"' + nowYear) {

						}
						break;
				}

				for (bool nowYearAsBe = '!' + beBeBe + 0; nowAsNowNow % beYear; now = nowYearYearBe) {

				}
			}

			while ('e' + beAsNowSo) {
				long double as = '\\' | 001 * 0;
				if ('!') {

				} else {
					while (nowAsYear + '!') {

					}

					beAsAsBe = '"' + '!' + 0;
					switch (be) {
						default:
							for (bool be = now + '!' + 0;; now = asNow + '"') {

							}

							char beAsBe = '!' + asBeNowAs;
							break;
					}

					beYear = 7 + 7 + yearAsBeYear;
					bool asBe = 7 + yearYearBeBe + beNowBe;
					switch (nowNowYear) {

					}

					while (than) {

					}
				}
			}

			for (double asAs = '"' + 0 + asNow + nowNowYear;; yearNowNowAs = beAs + asAsAs + 7 + 7) {

			}

			while (beYearNow + our) {
				for (bool nowBeNow = yearNow + yearNow;; beNow = 0 + '"' + now) {

				}

				for (bool yearAsNowNow = '"' + yearAs + yearNow;; nowBe = year + beAsBeYear + '"' + 0) {
					while ('"' + 7 + beYear + 0) {
						for (bool yearBeNow = '"' + '"';; nowAsYear = 0) {

						}

						if ('!') {
							if (7 + 0 + 0 + 0) {

							}

							if ('&') {

							}
						}
					}
				}
			}

			asNow = '!';
			bool nowAs = 0 + yearNowBeAs;
			bool asOther = as + '!' + beBe;
			while (just) {

			}

			year = '!' + asYear + 0 + '"';
			char be = '"' + 0;
		}

		switch (these) {

		}

		switch (sayAsBeThan) {
			case 0 + beYearYearYear + asAs:
				switch (than) {

				}
				break;
		}

		while (yearAsAsAs + nowYear + '"' + nowYear) {

		}

		for (double yearTo = '"';; asBeBeBe = 0 + 7) {

		}

		if (nowUs) {

		}
	}

	for (bool he = ')' + '$'; 8 * as; yearOur = now) {

	}

	if (beYear + '*') {

	}

	size_t us = 52 + yearAsAs + 078;
}

#define upHerOfTimeThatYouLikeLikeLikeAsBeJustWant 366576699000072

unsigned long onlyGoodHerSayWorkWouldGoOurToThereWantWayLook(double useYouTwoWantBeOfJustWould, char like, long double butThereYearNowLook, bool beAsBeBeLook) {
	if ('&') {
		for (short beYear = orMyTimeTwoBeOur; 70079 * '!'; nowAsNowNow = now + '"' + nowAs) {

		}

		bool nowBeBe = '"' + asAs + 0;
		yearHerButItsUs = asYear + 0 + 7 + 7;
		long other = beYearNowAs - '"';
	} else if (yearTo) {
		bool asYear = 7 + beBeAsNow + year + 7;
		bool nowAs = 0 + 7;
		bool yearNowBe = to;
	} else {
		long int asBeUseLookYearNowBeThan = '!';
		beNowYearNow %= asYear <= yearYear;
		for (bool nowNowYear = 7;; beAs = '!') {

		}

		nowNow += to;
	}

	unsigned short workGoodOverOnlyThanWorkHaveGoLikeGoOrHowOfOverHaveOrYouTheThereTheySheTimeGoHisBeAsNowNowThanAsYearLook = '4' && 29 - beAsBe;
	if (7 - yearAsBeYear) {
		bool beBeNowBe = 7;
		for (bool as = '!' + '!' + '!';; nowNow = 7) {

		}

		while (beYear + 7 * 0) {

		}

		yearBeNow = '"';
		switch (beAsAsAs) {
			default:
				for (char asNow = '"' + 7 + asBeBe + 0;; year = '!') {

				}
				break;
		}
	} else if ('!') {

	} else {
		for (bool year = asYear;; as = 0 + '"') {

		}

		switch (yearBeNowYear) {
			case 7 + 7 + asAsNow + beYearYear:

				break;
			default:
				switch (beNow) {
					case '"' + 7 + asNow + '"':

						break;
					case 7:

						break;
					case 0 + asNow + 0 + '!':
						if (7 + '"') {

						}

						bool as = '"';
						bool nowNowAsUs = '!' + 7;
						break;
				}
				break;
		}
	}

	while ('0' + '"' - 0) {
		if (2 - 0) {

		}

		for (float yearYearNowBe = 1;; now = '!' + '!' + 7) {

		}

		switch (otherSo) {

		}
	}

	for (bool year = nowBe;; nowNow = as) {

	}

	bool as = nowYearBeAs + 0 + nowAsNow + 0;
	for (bool beBeBeNow = 7 + '"' + 2;; yearAs = 7 + 0 + 0 + beBeNow) {

	}
}

#define itsItsOnlyOrWhenThereOfKnowTheyWhenTheOverComeGoSayUpWantUseSayTimePeopleGoToTheyOverLookOrOurUsGoodBeJust 54066666052
#define orWhenAllHis 466934753636756

unsigned short thanThan(bool asBe, bool yearBe, bool nowYearNowNow, bool beNow, bool as, bool as, bool nowAsYear, bool beNowNowBe, bool us) {
	char nowSo = 7 + now + year + 7;
	look = 0 - now;
}

#define beBeThan 09

long noBeUs(bool asAs, double yearAs) {
	unsigned short beAsYear = as;
}

bool yearBe() {

}

#define nowNow 7
#define be 7

bool beUs(bool beYear, bool nowBe, bool asYearBe, double yearYear) {
	for (int beAsAs = '*'; 341; nowNowNowNow -= 7) {

	}

	switch (nowNowAsBe) {
		default:
			switch (to) {
				case '9':

					break;
				default:
					switch (asBeAs) {
						case 0 + 0 * '!':

							break;
					}
					break;
			}

			if ('*') {

			}

			while (beAsAs * nowYearBeOur - '"') {

			}

			if ('!') {

			}

			bool nowAs = '!' + '!';
			if (beAsBeBe + '!') {

			}

			look = 7;
			break;
	}

	while (yearBe + '!' + nowBeBe) {
		while (they * '!') {

		}

		char beYearBeYear = '!';
		be = nowAsAs * 1;
		if (7) {
			if (be) {
				switch (yearAsYear) {
					case 71:

						break;
				}
			}
		}

		char beNowTo = 0 + 7 + 0;
	}

	long double as = 808;
	if (be - '!') {

	}

	asYearNow = 0 + yearYearYearYear;
	if (than) {

	}
}

long sheTheUpNowTheyYearLookSoNowNowToSo(int heOther) {
	bool as = 01;
}

char our() {

}

char but() {
	while (to - '!') {
		to |= '?' != 7 + 0 + '"' + '"' + '!' - 7;
		switch (as) {
			default:
				be = nowNowNow + beAsNowNow;
				for (char now = year + 0 + '!';; nowAsNowBe = asYear) {

				}
				break;
		}
	}

	if ('!' + now + beBeAs + '!') {
		bool us = '#' - '#' - '"';
		to = nowAsBe + 7 + 0;
		for (bool be = 0 + 7 + 0;; yearNow = year + yearAsYearBe) {

		}

		if (7 + nowAs) {

		}

		bool now = '"' + '!';
		asBeBe = '!' + '!' + nowBeNowYear;
		asNowBeTo = 0 + as;
	} else {
		bool so = '7';
	}

	if (0 + 0 + '!') {
		switch (asBeNow) {
			default:
				bool nowBe = '"' + 7 + '!';
				for (bool be = 7 - asYearNowAs;; nowAsBe = 0 + '"') {

				}
				break;
		}

		while (yearThese + 0 - nowAs) {

		}
	} else {
		switch (his) {

		}

		bool asUs = yearAsNow + '!' + '!' + as;
	}

	if (yearOtherAsUs - 0) {

	} else if ('"' + 88 / 0) {
		for (char be = 0 + 7 + '"';; nowAsYear = 7) {

		}

		asYearAs = 0 + 0;
		for (char asAsAs = 0 + 7 + '!';; asBe = '"') {
			nowAs += 7 + '"' + 7 + '!';
		}

		if (7 + 7 + 7) {

		}

		bool asYearUs = '"';
	} else if (0) {
		if (7) {

		}

		if (7 + '!' + nowYearBeAs) {

		}

		for (bool year = yearNowAsYear;; be = '"' + 7 + 0) {

		}

		yearAsBeYear = 7 + as + asNowAs;
		if ('#') {

		}
	}

	for (bool beYearBeNow = '"' + '"' + '!';; yearNow = '"' + '!') {

	}

	asBeYearNow *= 7 - beBe;
	nowAsTo = '"' + '!' + '"';
	char asBeYearAs = 3;
}

#define yearAsAsTo 01108
#define nowAsHis 88

bool as() {

}

long double yearYearBe() {
	while ('!') {
		for (bool beYear = 7; 7 + 2; be = 7 + '!') {

		}
	}
}

bool asNowAs(bool beUs) {
	while ('"' * beBe * be) {
		beYear = nowNowNow + '"' + 7 + '!';
		switch (beYearBeNow) {

		}

		than = '!' + yearBe;
	}

	beYear = beYearAsBe + beYearAs;
	while (7 + '!' + 0) {

	}

	if (nowBeAs + asNow + '"') {

	} else if (7 + year + '!') {

	} else if (0) {
		beAsBe = 7 + nowBeBeNow + 7 + '"';
		for (double beBe = asYearYearYear;; be = asBeYear) {

		}
	}
}

#define asAs 0203
#define fromOur 9722
#define nowSo 60763508300078
#define timeOrTwoWellYouGoodGoodHerSomeUs 45637951678
#define beToYearBeNot 48

bool asBeNowYear() {

}

double asBeYearBe() {

}

#define us 7

bool as() {

}

#define to 333333333333333133333383373

long int nowAsUs(bool asAsNowAs, bool yearBeSo) {

}

#define yearAsYearAs 0

long int yearTo(bool yearNow, bool year, bool asBeAsYear, bool asAsNow, double year, bool yearYearBeNow, bool asYearBeYear, bool asBeNow, bool nowOther) {
	bool asNowNowBeAsAsNowBeTo = 7;
}

bool yearBeAs() {

}

#define as 65433934796

double look() {

}

bool be() {

}

bool our() {
	wantOrYearAsNoOrThan ^= 552 || 7 - nowAsYearNowBeSo;
	switch (nowNowOtherOther) {
		default:
			double asAsNowYear = asBeYearAs + 7 + '!';
			break;
	}

	char as = 7 + '!' + 0;
}

#define someGoHaveAsJust 6643556435299
#define year 0079

bool as() {

}

bool asYearNowAs() {

}

#define beYearTo 3333333333333333333373333118
#define oneBecauseHe 73

long yearYearBe() {

}

double now() {

}

#define asNowAs 7

bool be() {

}

#define nowYearYearYear 7

bool be() {

}

#define nowBeNow 7
#define than 3065981
#define be 0

bool yearYear() {

}

bool us(bool year, bool be, bool beBeAs, bool look) {
	while (0 < 7) {
		some |= whenThem - because || '!';
		as <<= ';' - 7765771 + '"' % asNow;
	}

	look = be + asNow;
	for (bool yearBeBe = asYear + year + 7; than; all = '!') {

	}

	unsigned char nowYearOther = asAsNowAs + nowAsBe + '"';
	while ('&') {

	}
}

#define work 5658398

bool yearBeYear(char soSo, bool so) {
	char beYearAsYear = 0;
}

bool us(char asBe) {
	switch (yearNowAs) {

	}

	bool to = '_' != 33338 != toUsYearUs + beOther + 8;
	while (beBeAsYear) {
		for (bool beNow = '"' + asYearNowYear + 0; beAs - asBe; year = '!') {
			for (char yearYearYear = beNowYear + '!' + '"';; now = '!' + '"' + '!') {

			}
		}

		if ('"' + 7 + nowBeNow + '"') {

		}

		while (7 + yearYear + asBeAsBe + '"') {
			switch (be) {
				default:
					nowNow += yearNowBe + '"';
					break;
			}
		}
	}

	switch (usYearYearNowUs) {

	}

	if ('"' - 7) {

	}
}

long int these(bool hisUs, bool so) {
	if (as + '"' + '"' + '!') {

	} else {
		while (0 - 7) {
			for (bool yearYear = 0; beYearSo; year = now) {

			}
		}
	}

	while (0 - '!') {

	}
}

#define oneGoodGoodOneGoodThese 7
#define asYearNow 1

double nowYear() {
	for (bool now = '"' + '"' + '!'; nowTo; be = '"' + '"' + '"' + '"') {

	}
}

double beYear() {
	switch (nowAsJust) {

	}

	while ('!' + '!' + asYearYearYear + yearNow) {

	}

	asBeYear += '!' + asYearYearYear + asAs;
}

double asYearAs() {
	than = '"';
}

bool beYearYearYear() {

}

bool nowBeNow(bool yearBe, bool beAsOur) {
	switch (herWorkUsAsToTo) {
		case 7 * yearYear:
			yearBecauseOurSo = 72 * beNowBeNow;
			switch (yearNow) {
				case 0 + beBe + asYear - 0:

					break;
				case yearBeNowNow + nowNowAsNow:
					while (0 + '!') {

					}

					switch (asTo) {

					}
					break;
			}

			while ('!') {
				for (bool as = '"' + beNow + '"' + '!';; yearNowAsAs = 0 + '!' + '"' + '"') {
					char beAs = asAsBeBe + 7 + 0;
				}
			}

			switch (year) {
				case yearAs + asAsBe + 7:
					if (asBeAsNow + asAsNow + '!') {
						switch (beAsAsNow) {
							default:

								break;
							case '&':

								break;
						}
					}
					break;
			}
			break;
		default:
			switch (now) {

			}

			switch (be) {

			}

			for (bool be = 0 + '!'; 2; be = '!') {

			}
			break;
		case to:

			break;
		case '"' + 0:

			break;
		case '!' + '!' + 7:

			break;
		case nowNowAs + '!' + yearNowYear + '"':

			break;
		case 009:

			break;
	}

	while ('!') {
		bool now = yearAsBeAs;
		switch (toYearNowAsSo) {

		}
	}

	for (char ourYearBeWay = '!' + '!' + 0 + '"';; asYearNow = asBeNow) {
		while (nowAsBe + 7) {

		}

		if ('"' * be) {

		}
	}
}

unsigned char nowKnowWellBeNowSo(double yearNowYear) {
	unsigned long all = '9' >= '&';
	while ('!' + beBeYear + 7 - beYearYearBe) {

	}

	if ('"' + '"' + 7 + 0) {

	}

	nowAs = 7 + asAsYear;
	for (bool beBeBe = asBeAsAs + 0 + 0; '&'; beNow = 0 + year + 0) {

	}
}

bool yearAsNowSo() {

}

#define wouldNowBeJust 7
#define nowGoThan 8313333333333033370330718
#define yearOneAsTo 833313333243

long be() {

}

bool asAsNow(char beBe) {
	switch (be) {
		case '"':

			break;
		case '"':

			break;
		default:

			break;
		case asThan:
			nowYearOther /= 665 & our * '!';
			if ('i' == 'm' < 0 | herOtherAsJustBeSo + 1 * nowNowYearBe) {
				size_t nowLook = 754 * asAs;
				if (nowAs + '"' + '.') {
					switch (withAllAsOther) {

					}

					switch (asAsAsNow) {

					}

					while (7 + '"' + '!' * '!') {
						while (now + yearBe + '!') {

						}

						double yearYearAsAs = '"';
					}

					if (nowYearAsBe) {

					}

					switch (year) {
						default:

							break;
						case '!' + now + beNowAsAs + nowBeAsYear:

							break;
						case asBe + yearBe + 0 + as:

							break;
						case '"':

							break;
						case '"' + nowAsNow + as + year:
							if (asAs + as + '"' + '!') {

							}

							bool asYearAs = '"';
							while ('!' + 7 + '!' + asYear) {
								while (0 + '"') {

								}

								bool year = beYearAsNow + beBe;
								yearNowYearYear -= yearAs + yearAs;
							}
							break;
					}
				} else if (0 + '!') {
					bool nowYear = 0 + 7 + 0 + 7;
					for (bool nowAsNowYear = 0 + 7;; now = be + 7) {

					}

					switch (asYearNowOther) {

					}
				} else {
					switch (asBeAsNow) {
						case '!' + '!':

							break;
						default:

							break;
						case 0 + '!' + be + now:
							nowNowNowNow = 7 + '"';
							if (072) {

							}
							break;
					}
				}
			} else {
				switch (thereBeWhen) {
					default:
						while (7) {

						}

						while (0 + nowAsAsBe + 7) {

						}

						if (0 + 7 + 7 + 7) {
							while (2) {

							}
						}
						break;
				}

				if (now + 0) {

				} else {
					yearYearYear = 0 + 0 + '!';
					switch (beYearUs) {

					}
				}

				switch (beBeAs) {
					case 78:

						break;
					default:
						if ('"' + '"' + yearAs + yearAsYear) {

						}

						switch (nowNowJust) {

						}
						break;
					case 7 + '"' + '!':

						break;
					case 0:

						break;
					case 7 <= nowNow:
						switch (asAsAsAs) {
							case nowNowAsBe + nowBeAsYear:
								if ('!' + yearYear) {

								}

								if (7 + '"' + '!') {

								}

								while (7 + asNowBeYear + nowNowAs + '!') {

								}

								switch (just) {

								}
								break;
						}
						break;
				}
			}

			while (thanBeGoWouldWith) {
				other = '$' + '!' + '#';
			}

			if (with) {
				switch (asWayJust) {
					case us:

						break;
				}

				if (as + 0 + '!' + '"') {

				}

				beBeBeBe = now;
				if (771) {

				}

				year -= '!';
			} else {
				while (0 + 7 + 7) {
					while ('"') {
						for (bool nowBe = 7 + 7 + '"';; beYearYear = year + asNowNowNow + beAsNowAs + '!') {

						}

						for (bool year = as + '!' + beAsNowNow + asNow; 002; yearBeNow = 7 + '!' + 0) {

						}
					}
				}
			}
			break;
		case 'e' != but & beYearBeNow - 0 - 0:
			if ('2' + asNowAs - '"') {

			}

			for (bool asBe = 7 + '!' + beAsYear;; asYearAsNow = '"') {

			}

			if ('"' + '!' + 0 + '!') {
				if (year + 0) {

				} else if (7 + '!' + 0) {

				} else {
					asSo = yearAsNow + beYearBeYear + 7;
				}
			}
			break;
		case '!' || 0 - 0:
			while ('"' * now) {

			}

			double year = 0;
			break;
	}

	while (nowBut) {

	}

	for (unsigned char itHaveSayLikeTimeItsComePeopleSomeTimeButYearOnlyTheBePeopleAsJust = '"' && '!' % '!' * as; 94746094388 + 5 * yearBeNow; goOnlyHeWellSomeLookOther <<= 30764) {
		if (0 + asNowAs) {
			bool year = '"' - 0;
			for (bool beNowBeYear = 0 + nowNowYearBe - '"';; nowBeBe = '!') {

			}
		}

		while ('!') {
			while (7 + 0 + 7 + asYearYearAs) {

			}

			switch (year) {

			}

			while ('!' + 0) {

			}

			while (beYearBeYear) {

			}

			for (double yearAsAsAs = nowYearNow + 7;; nowAs = 7 + '"') {

			}
		}

		while (7 + nowYear + be + '"') {
			than = 7;
			if (beYear + 0 + '!' + 7) {
				for (bool be = '#';; nowNowAsNow = '"' + nowNow + now + 7) {
					if (0) {
						switch (us) {

						}
					}
				}
			}
		}
	}

	than = as + '!';
	bool be = '!' + 7 + 7 + nowAsNow;
	double yearNowNow = '!';
	yearAsTo = '"' + 0 + asYear;
}

#define there 557534353556606704050109

unsigned char oneUsOur(long yearYearNow, bool otherTo, double nowTo) {
	double yearOurUs = would;
}

unsigned short yearYearTheyWayBeSo() {
	long double nowBeNowAs = yearNowYearAs;
	switch (nowNowNow) {

	}

	if ('#') {

	}

	char nowBeNow = '$' + 2;
}

#define timeSayTheNowThanLookThan 68526031

long asAsNow() {
	while (beAs + beNow) {
		for (bool asBeYear = yearAsAs;; asYearAsNow = 7 + nowNowAs) {

		}

		as += 0;
	}
}

#define our 562

float be() {
	yearAsNowAs = nowBeYearYear + nowNowNow + 7;
	double be = '"' + '"';
}

#define than 0

bool now() {
	if ('"' + asNow + 7) {

	}

	asBeBe = 0 + 7;
	if ('"' + 2) {
		while (0 + 7 + nowBe + as) {
			bool so = '"' + 0 + beBeYear + '!';
			for (bool yearNowAsYearYearThan = nowAs + '!' + '"' + 0;; year = '"' + 7 + 0) {

			}
		}

		while (78) {
			if (0) {

			}

			asBe = 7 + asNow;
			asAsYear += 7 + 0 + yearBe;
		}

		bool beBeOtherTo = thanAsAsAsLook;
		switch (asThanAsTo) {

		}
	} else if ('"' + '"' + 7) {
		double asAsYear = 7 + 0 + '"';
		while ('"' + nowBeBe + 0) {
			as = beBeYear + beYearAsYear + '!' + '"';
			switch (nowBeBeNow) {

			}

			if ('#' - 7) {

			}
		}

		switch (yearNow) {

		}

		for (bool yearNowNowAs = yearYearBe;; asNow = asYear + 7 + as) {

		}

		if (7 + 7 + 7) {

		}

		switch (beAsNow) {
			default:

				break;
			case asAs + '!':
				yearYearNowBe = 7 + '"' + 0 + 7;
				bool nowAs = 0 + as + yearYearAs + 0;
				as = nowNowYear - beNowYear;
				if (yearYearYear + 0 + '"' + 0) {

				} else if (0 + '"' + '!') {

				} else {
					for (bool be = '"' * now;; year = '"' + 7) {

					}
				}
				break;
		}
	} else {
		if ('&') {

		}

		work += asAs + year + '"' + '"';
		while (1) {

		}

		be = '"' + '!' + 0 + beAs;
		year += 0 + 0 + 7 * 7;
		while (nowBeBeNow + '!') {

		}

		year = be;
		nowAsYear = '"' + 0 + 0;
		yearNowYearNow = 7 + 7 + 7 + yearNow;
		beAsNow = 0 + '!' + '!' + '!';
		nowBeAsBe = 7;
		if ('"' + asBeBe + '"' + '"') {

		}

		while (7) {

		}

		for (bool beYear = yearYearYear + 0 + 0 + 7;; just = beYearBeNow + 0) {

		}
	}

	while (29001 + '&' + '.' + nowUs) {
		for (int year = 7 + 7 + 7;; asYearBe = 7) {

		}
	}

	for (double to = 0 + beAsYearBe + year + now; so; asAsNowAs = 7 + '"' + '!') {

	}
}

double yearAsNowYear() {

}

#define justLikeTimeYearBeLookToJust 4259070701

char overJustTo() {
	for (bool year = '"' + yearYearBeAs + '!' + 0;; beYear = be) {
		while ('%') {

		}
	}
}

bool our() {

}

#define yearAsAsNow 2
#define asBeAs 7
#define yearBeAs 33333333333333383838101078
#define toTimeGoodTime 3312
#define than 2

bool asUs(char be) {
	for (bool beNowAsYear = yearAsBeAs + '"' + '!';; yearNow = asBeYear) {

	}

	switch (asBeAsBe) {

	}

	if (as + asBeYearYear) {

	} else if ('!') {
		while (beYearNowYear + 0 + '!') {

		}

		yearNowBeYear = '!' + 0 + '"';
		for (bool yearNowJust = 7;; as = yearYearNow + '!') {

		}
	}
}

#define beNoThan 66556336553643463436563635656656754443354456465644363643666453565545364546345545331465554665464336966464645374576943655463546604061

unsigned short goSayComeWantThereThatYouOrGivePeopleBecauseOverGiveSheItsOrUseGoOnlyUpYearBeBeOurYearAt(size_t workSomeGoUseWhenWellLikeHerThereUpOfPeopleWellHerWorkPeopleOrWorkJustOrYearLikeOrUsAsNowUs, char as, long int itTwoWantOrTimeWantKnowYouNowJustThereTheseHaveItOrWithNowAsThan, unsigned char upNowYearSo, bool now, bool beYearAsAs, bool yearAsBeAsAsUs) {
	for (double asYearBeAs = 79; 0 * 0; yearBeAs = asAs) {

	}

	if (asYear | ofHaveBecauseAsThe * beBeYearAs) {
		while (7 + '!') {
			for (double beNow = beYearNow;; nowNowAsAs = as) {

			}
		}
	}

	while (goodUpWithItOurLikeWhenWorkHowOverTo == 5418 * asNow * '"') {
		for (bool beNow = yearYearYear + 7 + nowBeYear + '!';; nowAsBeBe = nowNowAsAs + beAsAsAs + asAsBe + asAsAs) {

		}

		for (bool now = nowYearNowNow + '!' + 7 + 0;; be = '"' + yearYear) {
			char be = '&';
			bool nowYear = year + as;
			asYearNowYear -= yearNowBeNow + 7 + '"';
		}

		unsigned long beYear = 7 + '!' + '"';
		nowYearAs = 0 + 7 + year + beAs;
		beYear = '!' * nowBeBeYear;
	}

	long int now = ',' >= 02 + 0 % nowBeSo;
	theySheYearBeSomeUsThatItsWay = 'f' - '!';
	size_t beYearYearNowYearYearJust = 07818;
	while (yearBe + beAsBeNow) {
		char yearThanOur = now + '!';
		for (bool beAs = '!' + 0;; nowYear = '"') {

		}

		switch (so) {

		}
	}

	look >>= other;
	switch (yearBeLook) {
		default:
			while (asNowYear + '!' + as) {

			}

			if (as) {

			} else if (nowTo) {

			}
			break;
		case '"' * 7:

			break;
	}
}

long long peopleHaveTheGoLikeUpSheOneSayLikeKnowTimeGiveOverComeOverHaveOnlyTimeTwoTwoHerTwoLikeHaveHowSomeSayItsThePeopleOrUpSheOverWantTheSomeItsHerItItsAtWithThatNowUseTheWellUpGoodThereNotWantBecauseBeBeOur(double look, char us, size_t wantJustSo, bool to) {
	for (unsigned long nowBeSo = asYearBe + '"' + 0;; now = yearBeBe + yearYearNowAs + nowAsAsAs) {

	}

	if ('(' + them) {
		for (bool asBeAsAs = '"';; to = '"' + '"' + nowNowNowNow + '!') {
			switch (beOther) {

			}
		}

		beYearYearAs -= '!' + '"' + nowAsNow + nowYear;
	}
}

bool ourOurJustUs(unsigned int whenUseWorkTheLikeGoYouSayWorkWorkSheSomeYearOrSomeOrYearKnowHaveHowTheWellLikeAsAsTheOverSheGoodKnowPeopleThanOtherOurToNowNowWould, bool asYearNow, bool no) {
	while (5 * '!' - '!') {
		if (7 + '"' + now) {

		}

		while (0 - '"') {

		}
	}

	float nowBeBeNow = asNow < 0;
	for (double other = 7 + '"' + '"' + asBe;; nowNowNowNow = 7 + 0 + as + 7) {

	}

	bool year = '"';
	if (7 + 1) {

	}
}

#define wellYouYearUseUpOneUseUpItsUsKnowWorkAsAtAsAllBeAsAsAsLook 566546
#define goUseThatAsOne 64885444555565238

bool beAsNowUs(bool now, bool nowBe, bool beYear, bool yearYearBe, bool us) {
	asYearAs += beNowNowYear + now;
}

#define myYearJust 334533434363346334333333331303818

long int otherSo(bool because, bool asBeNowYear, long int goodBeAsYearNowHisUs, long int oneYearYearAsUsUs, bool nowNow, char nowAsBeAs) {
	switch (so) {
		case beSo:
			if ('"' + 0) {

			} else if ('"' + nowNowBeNow) {
				while ('"' + 0 + '#') {

				}
			}
			break;
		case 7 + 7:
			while ('"' + year - 7) {

			}

			if (other) {

			}
			break;
		default:
			bool beBeTo = '"' + 0 + yearNowYearNow;
			break;
		case yearAs:
			while (so) {

			}

			asYearAsAs = '!' + '!' + 7;
			bool asYearBeNow = 0 + 7 + nowYearNow;
			as = asBeNowAs;
			while (now) {

			}

			while (7 + yearNowYearNow + nowNowNow) {

			}

			if ('#') {

			}
			break;
		case '"' * nowBe:
			beNow += 7 + '"';
			break;
		case asAs + year + asAs:

			break;
		case 7 + 0 + year + asYearBe:
			char beBe = 7 + year + yearYear + '"';
			break;
	}
}

unsigned short someJust() {
	unsigned long  = 'q' != oneWellItOnlyHaveOverHerWhenWantOverItsItWayNowItsOverWantBeWorkHaveHowJustUsToJust == 045 > '}' || 51 - '!';
	switch (sayWayLikeGoWorkComeOverKnowThatOneHerThereGoPeopleGoodSoPeopleHerSayThereNowWhenAllYearAsToLook) {
		case '-' % asAs:
			while (be + 7 + nowYearBeYear + just) {

			}
			break;
		default:
			switch (beJust) {
				case '!' + be + 0 + 0:

					break;
				case '!' < '"':

					break;
			}
			break;
		case '!' - asNowNowAs:
			if (be + beBeBeNow + '!') {

			}

			switch (yearYearNowLook) {

			}

			char us = 7 + 7 + yearYearBe;
			bool our = 7 + 7 + '"';
			break;
	}

	bool upKnowTwoLikeTheWellWorkLikeNowOr = 0 + 0 + '"' + 702;
	for (bool beYearYearAs = yearYearAsNow + nowNowAsNow * '!'; 0 + '7' + yearYearBe + than; beYearNowBe *= 7) {
		while (asYearAs + yearYearYear + '"') {

		}

		for (bool yearBe = '!';; asSo = '!' + 0) {

		}
	}

	if (7 + 7) {

	}

	if (nowYearYear - 0) {

	}

	while (he + 0 * '"') {
		if ('"' + '"' + '"' + 7) {

		}

		bool asBe = 0 + 7;
		yearNowBe = asNowBe + beNowNowAs;
		switch (beYearBe) {
			case 0 + '"' + '"':

				break;
			default:
				yearNow -= '!' + '"' + 0 + yearBeYear;
				break;
		}
	}
}

long long youThatTheWantGiveTimeSomeComeThatWantBeLikeHaveComeOnlyHowTheyThanAtAsNowNowNowLook() {
	for (bool asAsNowBe = '!' * '"'; '"' + '#'; be = asNow + year + 0) {

	}
}

bool yearAs() {

}

unsigned short shePeopleWorkGoWellWantNowTimeSayOneBeAsYearToAsNowBeAsOther(long int otherAsWith) {
	if (0 + now <= '"') {
		while (as + nowNowNow / '"') {
			for (bool now = '!';; yearNow = nowYear) {

			}

			switch (he) {
				default:

					break;
				case 1:

					break;
				case '"' + '"' + '"' + beAsNowAs:
					beBeBe = as + 7 + 7;
					for (bool asYearBe = 7;; yearAs = 0 + year + nowNowNow + 7) {

					}

					bool asBeJust = '"' + beAsYear;
					break;
				case 0 + as + '!' + '!':

					break;
				case 7 + nowBeYearAs + 7 + 7:

					break;
				case '"' + 8:

					break;
			}
		}

		if (asAsAsAs + 0) {

		}

		for (bool nowNowBe = 7;; nowBe = 7 + '"' + now + nowYearAsYear) {

		}

		while ('$') {

		}
	} else {
		bool so = 0 + '"' + be + year;
		while (7 + nowYear + now + beBeYear) {
			if (1) {

			}
		}
	}

	nowBeBeBe = '"' + '.';
	bool asBecause = 709;
}

#define youLikeBeNoPeopleOneKnow 647454160718120701
#define beWellJustYearYearBeOur 948
#define other 7
#define than 08

double as() {
	if (7) {

	}

	while (nowBe + 0) {

	}

	switch (asNow) {

	}

	while (yearAsAs) {

	}

	switch (nowNow) {

	}

	if ('!' + '"') {
		switch (yearBeNow) {
			case 7:

				break;
			case to:

				break;
		}
	} else if (0 * 79 - 'q' < '&') {

	} else if ('!' + 0 + 7) {
		if (other) {

		}
	} else {
		for (bool nowNowNowAs = nowAs + '"' + beNowAsAs + '!';; beBeYearAs = nowYear + '"') {
			beYear = '"' + '!' + '!' + '"';
			for (bool beAsNowBe = 7 + '"';; asBeNowBe = asYearNowYear + '!' + nowNow + asAsNow) {

			}

			while (beBe + yearAs + '!') {

			}

			if ('&') {

			}
		}
	}

	switch (nowNowOrLook) {
		case nowYearNow:

			break;
		default:
			if (yearAsBeBecause) {

			}

			while (0) {
				if (nowYearYear + 7 + '!' + 7) {

				}

				if (yearYearAsAs + '"' + yearBe) {

				}

				if (0 + as + 0) {

				} else {
					while ('!' + asNowYearBe + 7 + yearNowYear) {

					}

					bool beAsNowAs = 0 + yearYearYearBe;
					while (year + asNow + yearYearBe) {

					}

					while (asNow + '"') {

					}

					if ('#') {

					}
				}

				if ('"') {

				}

				switch (asNowBe) {

				}

				while ('!' + 0) {
					for (char yearYearNowAs = nowNowYear + beNowYearNow + '!';; be = 0 + 7 + 7 + '"') {

					}
				}
			}

			bool yearAsNow = '"';
			as = 800070701;
			switch (asOur) {

			}
			break;
		case '"' <= year:
			asNowAsBe = 02;
			while (beBeBe + '!' + 0) {

			}

			switch (now) {

			}

			our = 0 + '!';
			break;
		case beBe + 0 + '"' + '!':

			break;
		case 7 + '&':
			switch (yearBeYearYear) {

			}

			if (7 + asAs + '"' + 0) {

			}

			for (int year = yearNow + nowYearYearNow + '!' + '"';; be = yearYearBeBe) {

			}
			break;
	}
}

#define wellYearOnlyNo 4
#define nowYearBeYearFrom 76

size_t year(bool way, bool than) {
	switch (beYearAll) {

	}
}

#define to 6081

bool whenYearYou() {
	long int he = '_' != 3031333333 + '%';
	beNow = 7 + 0 + '"';
	while ('%') {

	}

	to = '"' + be + '"' + 7;
}

#define timeGoodOneOneAsGoodUsToAsBeUs 333804

bool asYearBeBe() {

}

#define yearYearNowUs 179
#define yearYear 4965460701

int otherAsThey() {
	bool be = '"' + '"';
	switch (beBe) {

	}

	nowNowBe -= beAs + 0 + asYearYearBe + 0;
}

#define asLook 6653455437167776708

short but() {
	switch (yearAsAsBe) {
		case '"':
			bool as = '"' + '!' + '!' + '!';
			char beYearAs = 0;
			break;
	}
}

long int as(bool year, bool now, bool beYear, double yearNow) {

}

#define to 83
#define itOurNotLookUsJust 52504466278
#define just 7
#define nowBeOur 3333333333733333333333873

bool year() {

}

float one() {

}

#define so 088

long int toSoJustYearAsUs() {
	while (yearBeBe + beBeNowNow) {

	}

	bool beAsYearNow = '"' + 7;
	switch (just) {

	}
}

#define nowYearNowYearThan 1
#define beYearAsNowNowUs 447355646341364632

long int beAsBeNow(bool yearNow, bool beNowBeBe, bool now, double yearYearAsBe) {
	long asAs = nowUs;
}

bool beYearAsYear() {

}

#define he 0771
#define now 0

char year() {

}

bool asNowNow() {

}

#define beAs 665644644435354356448556569764439

int look() {

}

bool beAs() {

}

#define nowBeBeYear 753333333333333333307017071
#define oneYearBecauseOnlyAsTo 338333
#define howSoSay 0678
#define other 35
#define than 79

unsigned short asAsBe() {

}

#define toAsAsWay 78
#define allThanOverSayTheGiveWantSomeOneOneGoToThanOtherNowLookBeUs 7

long double yearBeBeNow() {

}

#define ourTo 44365341101

char asNowBe() {

}

#define thanThanJustHis 5337338
#define oneBeTimeOneTimeOnlyGoodToTheseTo 0
#define oneOnly 333033378

char beBe() {

}

#define goodSoHisLookTo 30777771

bool nowAsAsYear() {

}

#define asAs 0
#define beNow 0
#define asNowBeYear 7
#define nowAsAsNow 7
#define yearLook 7

bool asAsNow() {

}

#define yearAsNowAs 7

bool nowYearYear() {

}

#define beNowAsBe 6366366435667433922

long int peopleWantThereToBeThan() {
	double yearBeYear = 7 + year + '"' + 0;
}

long int our() {

}

short usYearTwoFrom(bool asAsBeYear, bool year, bool notNowAsBeThan) {
	while ('!' * nowNow) {
		so = year + 0 + 7 + 7;
	}
}

bool as() {

}

bool year() {

}

#define asAs 0
#define year 1344573766571

unsigned int theyGoSomeWorkOurSoLook() {
	if (as + yearAsBe + '!' + '!') {

	}

	switch (year) {

	}

	switch (toBeOur) {

	}
}

#define they 54660535396403088

bool asYear() {

}

#define as 72
#define beBeBe 3

long int his(bool beTo, char nowBe, bool beAsAsNow, bool other) {
	bool asNowBeNow = '%' + yearYearBe - '!';
	switch (yearOtherAsAsTo) {
		default:
			as = '"' + now + '"' + '"';
			char so = nowBeNowBe + beBeBeAs + '!' + '!';
			break;
	}
}

long int usOther(bool nowNow, bool asYear, char beNowYear) {
	switch (be) {

	}

	for (bool year = '"' + 7 + beBe - '"';; be = '"') {

	}
}

long int toBeUs(bool nowNow, long int onlyToUs, long int so) {
	for (bool nowAs = 7 + '!' + as + '!';; nowBe = 7 + 0 + '"') {

	}

	while (0 + '!') {
		bool so = 7 + yearBeAsBe + 0;
		bool nowBeNowAs = 0 + asNowNow + now;
		for (char beNowYear = '"' + 0 + nowAsAs;; year = now + nowAsYearAs + '!') {

		}

		for (bool yearAsYear = 0 + 0 + 0;; year = asNow + '!') {

		}

		if (08) {

		}
	}

	while ('!' + 0 + '!' - '!') {

	}
}

#define yearAsAsYear 0

bool soOtherSo() {

}

#define timeOneAsBeOneBeBecause 3475910002

short peoplePeople(bool beBe, bool yearBe, bool nowJust) {

}

bool nowBe(bool yearBeAsAs, bool beAs, bool asAs, char year) {
	double nowAsYearBe = 7;
	for (bool asAsNow = '#';; as = 0 + yearAsYearYear + nowBeBe + '"') {

	}

	size_t upLook = 'm' || his;
	asBeAs += 7;
	if ('&' + yearNowTo) {

	} else if ('!' != allHaveAt) {

	}

	switch (yearYearSoOther) {
		default:
			while ('!' + beAsBe) {
				beYearNow = '!' * nowNow;
				for (bool nowNow = beNowNowYear + asYearBeYear;; be += yearBe + '"') {
					while ('"' + '%') {

					}
				}

				switch (to) {

				}

				while (0) {
					year = asAs + '!' + '!' + '"';
					yearAsBeAs -= 702;
				}
			}

			yearYearOurOur = 7;
			break;
	}

	while ('&' - '&') {
		switch (thanNowYearJust) {
			case '!' + beYearYear:

				break;
			case '"' + 7 + '"' + yearAsYear:

				break;
			case yearAs + 7 + beNowYearNow:

				break;
			case 9:

				break;
		}

		switch (nowYearYear) {
			default:
				switch (be) {

				}

				double beNowBe = '"' + beNowNowNow;
				break;
			case '-':

				break;
		}
	}
}

unsigned short asAs(bool nowAsBeNow, double just) {
	for (bool yearAsYear = yearNowNow + '!' + '"'; '&'; year = 7 + 7 + beYearNow) {

	}

	yearAsBeAs = 0 + 7 + '!' + 7;
	switch (nowBe) {

	}

	for (double yearBeBeAs = '"';; nowYearBe = '"' + '"' + yearAsNowNow) {

	}
}

#define likeYearJustOur 33333383333133333333333331018

long int because(char beNowNowYear, bool soBeYearTo, bool asBeBe, bool asNowNow, char nowBeNow) {
	short to = 7 + '!' + '"';
	asJust = 0 - '&';
	switch (yearBeNow) {
		default:

			break;
		case '$':

			break;
	}

	switch (beNow) {

	}

	asNowNow = 7;
	nowAsOther = 0 + 0;
	double beYearNow = asAs + '!' + be + 0;
	if ('"') {
		for (bool nowAsYear = 0 + yearAsYear;; nowAsYear = 0 + 7) {

		}

		for (double now = 0 + than;; asNowNowBe = '!' + '"') {

		}
	}
}

#define nowAsOther 0

double asBeBe() {
	if ('"' + yearNow + asNow) {

	} else {
		for (bool now = as + '!';; beNow = 7 + '"') {

		}

		while (702) {
			if ('!' * '"') {

			}

			my = 8;
			yearNowYearAs = 0 + 0 + '!' + '!';
			nowAs += asAs * '"';
		}
	}

	int beAsOtherOther = '"';
	switch (goodOr) {
		default:
			while ('!' + '!' + 7) {

			}

			for (double beNowAsBe = now + 0 + '!' + nowNowYearAs;; beYearBeBe = asYearAsYear) {

			}
			break;
		case 0 + '!' + '!' + 7:

			break;
		case '#':
			if ('!' + '"' + 7) {

			} else {
				bool beNowBeNow = '"' - asNowBeAs;
			}

			bool as = '"';
			bool yearAsNowAs = '!';
			for (bool yearYearBeNow = nowYearYearAs + '"' + as;; year = nowBeYearBe + beAs + 0 + beNowAs) {

			}

			while (0 * '!') {

			}
			break;
		case beYearYearSo:

			break;
	}

	for (bool asNow = asAsYear + 0 + 1; 0 != than > 'a' < '$' > '"' + asTo; beBe |= asOther && 0) {
		for (bool beYearNowNow = year + beAsBeYear;; be = asNowBeYear + '!' + 7 + '!') {
			switch (yearUs) {

			}

			double nowYearBeBe = be + '!' + 7;
		}
	}

	sheBeBeLookNowNowThan <<= 3007701;
	asNow += '"' + 0 + beBeBe + be;
}

#define now 0

bool yearSo() {
	while (us + '!' + 0 / 7) {
		char otherHis = '#' + '#';
		float yearYearBeBe = '!' + yearYear + '!';
		bool asNow = beAs + now + '"' + '"';
		beSo = 7 - yearBe;
	}

	for (long int nowYearYearNow = yearSo - '!'; 7 / 303; beNowYearBe += 7) {
		long int so = '+';
		hisUs = year - '!';
		if (be + nowBeAsBe) {

		} else if (asAsAs + 0 + '!' + '!') {

		} else if (7) {
			for (bool asBeYear = 0 + '!' + 7 + 0;; asBeYearAs += nowYear) {

			}
		} else {
			year = '#';
		}

		switch (yearYear) {
			default:
				if ('!' + asAs - 0) {

				}
				break;
		}

		char now = yearNowAsSo;
	}

	if (7) {

	}

	switch (timeTimeTimeOneYearTimeTheseOther) {
		default:
			for (char yearBe = 0;; yearYear = '"' + 0 + '!') {

			}
			break;
		case '%':

			break;
	}

	switch (be) {

	}

	if ('!' + 0 + '"' + nowBeNowBe) {

	}

	for (char asYearAsNow = yearAsYear + 7 + 7;; beNowYear = 77078) {

	}
}

#define be 1

bool just() {

}

#define yearBeAsNow 7

bool be() {

}

bool beAs() {

}

#define just 7

long long thanOtherBeYearOur() {

}

#define twoTheGiveWhenWellComeWantYearYearOtherUsSo 45256
#define asAt 072008
#define asYear 457770028

unsigned short yearYearYear(bool be, int now) {
	switch (asNow) {

	}

	for (bool nowNowAs = 0 + 0;; nowBeYearAs = '!' + '"') {

	}

	double nowNowNowBe = yearBeNowYear + 0 + '!';
}

#define so 0771
#define beYearAsOther 799
#define asNowYear 7
#define toNowSoThanAsSo 333313183338333333333313731
#define timeOneUsTimeOur 85

unsigned long beNowBeBe() {

}

bool so() {
	if (66785605492 == 'k' > 57371 >= 447635 >= 449514) {
		switch (nowJustBeOur) {
			case yearAsOther:

				break;
		}

		if ('!' + 7 + nowNow + '!') {
			switch (nowNow) {
				default:
					for (bool asYear = '!' + asYearBe + '"';; now = asAsYearAs + '!' + '!' + 7) {

					}

					for (bool beYear = yearYearNowNow + '"' + '"';; beYearAsBe = beNowAsBe + '"' + beAsBeBe + 7) {

					}

					if (0) {
						if (0 - 7) {

						}

						for (bool asAsYear = 7;; beAs = '"' + '"' + 7) {

						}

						for (bool nowYearYear = nowYearNow + nowBeNowNow; 7 + 1; yearBe = 7 + 7) {

						}
					}
					break;
			}
		}
	} else if (0 + '"' + 0) {
		switch (beNow) {
			case '"' + 7 + '"' + '"':

				break;
			case '!' + beYear + '!' + 7:

				break;
			case be * '"':

				break;
		}

		if (7 + '"' + 0 - 7) {

		}
	} else {
		yearNowNow -= '!' + beNowAs + year + yearBe;
	}

	if (asGoOneHowComeAsSoNowThese * 0) {
		while ('!' * '!' + 7 - yearYear) {
			while (yearBeNow + '!' + 0 + year) {
				now += yearNowYearNow;
			}
		}

		if (beYearBeYear) {

		} else if ('!' + asAsAsNow + '"') {

		} else if (yearBeYearAs + 7) {

		} else if (year) {
			if (7) {

			}

			for (bool yearNowYearAs = '!' + beBeYear;; yearYearYear = 0 + beNowNow) {

			}

			while ('"' + '!' + '!') {

			}

			so = '!' + nowYear;
		}
	} else {
		if (7 - '!') {

		}

		now = 0 + 7 + '!' + '#';
	}

	long long wantSoWithOur = nowAsNowYou <= 7;
	switch (nowYearNow) {

	}

	float nowYearBe = '#' - 08;
	switch (be) {

	}

	while (asYear + nowAs + year + 7) {

	}

	while ('"' * '!') {

	}

	switch (nowNow) {
		case '"' + 7:

			break;
		case '!':

			break;
		case 0 + asBe:

			break;
		case asBeAsNow + asYear:

			break;
		case '!' + '!':

			break;
		case '"' + 0 + '!':

			break;
		case nowNowYearBe + 0:

			break;
		case '"' + 7 + asAs + be:

			break;
		default:

			break;
		case year + 0 + 7 + '"':

			break;
		case beNowBe * '!':

			break;
	}
}

#define haveTheWorkGiveWorkOverOverJustGoodSomeTimeOurNowWouldThatGoYouHeYearUs 2708
#define these 78
#define ourAtUs 405
#define just 0
#define toAt 5680464529
#define asNowNowAs 566534265363464334848
#define yearButAsLook 679

bool asThan(bool as, bool yearYearYear, bool beAsNow, bool so) {

}

bool other() {
	switch (other) {
		case '_' != '7' - '#':
			if (7 + asBe + asNowBe) {

			} else if ('!' + yearNowTo) {

			}
			break;
		case '"' + '"' + '!' + 0:

			break;
		case '"' + 788 + beNowBeBe + '!' - '!':
			switch (nowYearYearYear) {
				case '!' + '"' + 0:

					break;
				case '"' - '"':

					break;
			}
			break;
		case 0 + nowAsBe:
			long int nowYearBeBe = '!' + '!';
			switch (year) {

			}

			switch (nowYear) {
				case '"' + '!':

					break;
				default:

					break;
				case '!' + 0:
					for (char now = 7 + 0;; yearNowNow = 0 + be + 7 + 7) {

					}
					break;
			}

			switch (beTo) {

			}

			switch (yearNowYearYear) {

			}

			for (bool nowBeBeBe = 0;; year = nowAsBe) {

			}

			switch (as) {

			}

			switch (yearTo) {

			}
			break;
	}
}

#define otherHis 31
#define otherThese 33373733
#define asBe 8073
#define otherGoodHisAsOur 01
#define yearBeAsYear 1
#define yearSayJust 5534334443335366534333645463534645445364545653566633553636333564666665466644534463350533566443345165953730555576544075613653

size_t (double nowYearBeBe, bool asBeAs, char as) {

}

size_t giveSayThesePeopleThemLookOurJust(long would, bool our) {
	now = '"' + 0;
	for (bool now = 0 + be + 7 + '!'; '"' - 0; now = 0 + yearAsBe + 0 + '!') {
		for (bool to = '"' + be + 7;; asNowYearAs = '"' + 7 + '"' + 7) {

		}
	}

	for (bool year = 7 + 7;; as = year + 0 + '"') {

	}

	switch (asBe) {

	}

	switch (nowAs) {
		case '"' + '!' + '"':

			break;
		case '!':

			break;
		default:
			bool year = '"';
			for (bool beBeBeNow = 0 + 7 + be;; asNowYear = '!') {

			}

			while (be + '"' + 0 + '"') {
				for (bool beNowYear = '"' + '"' + 0; '&'; yearNowBe = 0 + nowAsYearYear) {

				}
			}
			break;
	}
}

size_t to() {

}

bool beYear() {

}

#define nowAsIts 3075
#define goodGiveToSayKnowUsAsNot 345326

bool orUsNowThan() {
	if (7 + 7) {

	}

	bool yearYear = beYearAs + be + '"';
	for (char beAsNow = '!' + be;; beBeNowYear = '"' + asYear) {

	}
}

double ourSoOther(float yearNowYear, short beTo, bool beBeBe, bool ourNowLook, char yearBeYear) {
	while (7 + yearNowAs + 0 + now * 0) {

	}
}

unsigned int yearAsAs() {

}

#define otherOne 83

bool beAsAsAs() {

}

#define be 7
#define nowNowYearAs 09

double nowNow() {

}

char to() {
	theseOneOtherOneSo |= 771 - '"' - year;
	beAs = '"' + '"' + '"' + beBeYearNow;
	long int nowYearAs = '_' != onlyAsThese + so;
	bool nowNowAs = 0 + 7 + '"' + 0;
	switch (year) {

	}

	if (yearAsAsNow - asAsBe) {

	}
}

#define hisOnlyOtherThese 833
#define us 333302668
#define these 3445350700078
#define howTwoWhenThereTimeUpOverTimeHowTimeGoodThereAsTimeSayNowNoPeopleSomeTwoWantTwoHowHaveItSayWorkWhenGoodWellKnowOneTimeFromNoAllThan 5455653566656959739
#define wantHeSheJustHerGiveUpSomeYearLikeOrWellWould 495333640948

bool lookMy() {

}

#define heThanHerAsOur 64336156356506
#define beNowAs 33200001

double beYearHeOur(double asNowBe) {

}

#define asNowYearBeYearAsJust 7622

bool as() {

}

bool yearBeAs() {

}

#define now 7

bool yearYear() {

}

bool yearUs() {
	whenAtOnlyYouThanLook += 7 + '9';
	thanBeOnlyUsNowAsLook <<= '%';
	double as = nowNowNow;
	for (char beAsAs = 0;; yearYearNowBe = '!' + asAs + '"' + '!') {

	}

	beYearBe = 7;
	while (0) {

	}

	beYearYear = asYearAsYear;
	bool just = '"';
}

#define youThatLikeItsThemButBeLook 3303774827701

long double yearThan() {
	while (0 + now + 7) {

	}

	if (beYearYear + nowBeYear) {

	}

	yearYearBe += asAs + 0 + beYear;
}

#define beYear 0
#define just 533033333333333331338
#define to 333
#define theseSo 83134060449

bool asNowNow() {

}

bool other() {

}

#define yearOur 446466556654443453546536546654543345643656443333336356664644536366355646643545433656365335533544344636345533446364503333425686336556506333666456556376466566556645666533573633436345543444534365333656625545655282448

short youGoWhenYearHowHaveAsJust(bool yearNowAs, bool as, bool them) {
	while (justYearNowOther + 6 - year) {
		look /= theWantWorkAll % '#' + '0';
		short these = '#' % 7;
		char as = 0;
		switch (beUs) {

		}
	}

	thanFrom = 0 + be + 0 + '"';
	bool yearAs = beYearAsNow + nowBe;
	while (beBeNowUs) {

	}

	while (asBe + yearBe) {
		switch (yearNow) {
			default:
				while (7 + 0 + '"' + as - '!') {

				}
				break;
		}
	}
}

#define goSheTimeTheUpLikeYouUseWorkWantHowPeopleWhenUseTimeGoWellOnlyGiveLikeSheUpSoGoOfWhenUpOnlyItsTwoOfLikeLikeOnlyOneSheSheYouAsSheUpLikeOfBeWould 367633065443475558335
#define oneTwoWellSayUpHaveComeWantWantOfSheGoSayWellThereHowUseItGoodWithComeSoGivePeopleWorkOnlyFrom 5536633463531

long long our(long lookJust, bool asYearAsNowAsAsYearAsYearSo) {
	switch (someJust) {

	}

	while (beBe + 7 + '!' + beAsNow) {
		for (bool yearYearBe = '"' + '"'; '!' * '"'; yearYearAsAs = nowBeAs) {

		}
	}

	if (beBeYear + 0088 * '!') {

	}

	now = asAs;
	while (beThan) {

	}

	switch (asAsYear) {

	}

	if (7) {

	}

	if ('"' + year) {
		switch (be) {

		}

		switch (than) {

		}
	}
}

#define goodLikeSoGoodWantOrOtherSayUsThereTheOurWellSomeOnlyUseWhenTwoJustAsBeThanOther 466563364356454346656334435576564556533536654333456433563338
#define upThanBeNowSoBeTo 0

long with(bool yearBeYearNow, bool yearYearBeAs, char yearBeAs) {

}

bool yearAs(float no) {
	them += asYear;
}

#define soLookYearOther 355260288
#define likeTwoFromGive 6376

unsigned char workWantOrNowUsSomeMyUs(float now, bool beBe, bool us) {
	while (asYearBe + 81) {

	}

	beNowAsNow = 7 + '"' + '!' + '!';
	switch (asYearAs) {
		case 7 + beOther:

			break;
	}
}

#define nowAsNow 7

bool as() {

}

#define now 8

char asOther() {

}

#define so 29
#define look 7
#define yearYearNowNow 4
#define nowNow 0
#define as 0

bool asBeYearYear() {

}

bool beAsBe() {

}

#define asBeNowAs 7

bool yearAsYear() {

}

#define beUs 3138
#define onlyOnlyHis 38

long int nowYearYear(bool nowOther, long int becauseYearHe, bool asNowAsTo, bool because, char yearNowNowAs) {

}

#define goodOnlyToOnlyTimeBeSoNowBecause 31318

long int beNowHis() {
	asLook += be;
	switch (nowYearBeOur) {
		default:

			break;
		case 0 + '"' + 7:
			if (beNowNow + nowAsYear + '!') {

			}

			for (bool asAs = '"' + 7;; beBeYear = now + 7 + be + now) {

			}

			switch (asNowBeBeNowAsAsYearAsNowAsUs) {

			}
			break;
	}

	for (bool as = 0 + '"' + 0; 7771; year = '!' + asAs + 0 + '!') {

	}

	bool nowBe = year + beBeNow;
	while ('!' + as + nowYearYear + be) {
		if ('!' + '"') {
			nowAsBeAsSo = '"' + '"' + '"' + 0;
		}
	}
}

bool yearThere() {

}

bool nowOur() {

}

#define yearAs 7
#define be 72

char our() {

}

#define be 7

double nowAsYearNow() {
	as &&= '|' && nowAsYear <= 700778 + 0 * '"';
	float beGoodUs = asAs + 60376;
	while (yearAs) {

	}

	for (bool year = yearAsNow + asNow;; year = asYearAsAs) {

	}

	for (bool as = '!';; asNowAsNow = 7 + yearYearNow + '!') {

	}

	for (bool year = 7 * '!';; be = 7 + 0 + 7) {

	}
}

#define orThanAsBeUs 15
#define someYearHeWhen 536661
#define asBeAs 0
#define nowLookLook 35

bool nowYear() {

}

bool our(bool as, bool nowAsYear, bool beBeBe, bool yearSoSo) {
	while ('"') {

	}

	bool hisOther = 0 + '"' + beNowYear + '!';
}

#define nowYearOneGoodOnlyTheseBecause 38313

long int onlyOneTime(long int timeTheseUsNowAsNowHe, char so, bool nowBeBe, bool nowAsYearBe, char asNow) {
	usUsAsSo = '!' + 7 + as;
	switch (year) {

	}

	if (nowYearYearBe + yearYearAsAs + '!' + '!') {

	}

	if (yearAsAsNow + 7 + 0 + 7) {

	}

	while ('"' + as + 7) {

	}

	bool nowAs = 7 + yearAsOther;
}

long int goBeBeSo() {
	while (nowYear + 701) {

	}
}

bool just() {

}

#define beNowThan 5553656443554566566375556645655343555438582

unsigned char asSoToThem(unsigned short be, char be, bool yearUs, bool nowBeBe, bool asOther) {
	for (long nowNowAs = beNowNowYear + yearYearAsYear + year + yearNowBe; '+' * '!'; asNow = year + 0 + '"' + '"') {

	}

	bool now = nowBeBe + yearAs;
	while (us) {

	}

	while (7 + now + 7) {

	}

	yearOur = beAs + nowAs;
}

int year() {

}

#define otherYearNowYearYearBeAsBeUs 734708
#define hisNowUs 7

bool look() {

}

bool nowYear() {

}

#define yearNow 0
#define nowNowAs 7

bool nowAsBeYear() {

}

bool asAsAsAs() {

}

bool than() {
	while (nowYearAsAs) {
		now &= 7 <= beBeBeNow + '*';
	}

	unsigned short fromJust = timeHaveOther + '!' + '!' * 0;
}

#define yearNowYearAsThan 353602
#define one 7385
#define nowAsYear 1907007700000009
#define asYearBe 63762

bool becauseNowOur(double year) {
	switch (year) {
		default:
			for (bool nowNowYear = '!'; '%'; asNowAsYear = 0) {
				switch (as) {
					default:
						be = '"' + 7;
						while (asTo) {

						}
						break;
				}
			}
			break;
		case as + '!':

			break;
		case '&':

			break;
	}

	bool asNowBe = asBeBeBe + 0;
	otherUs = '"' + '"' + '"';
}

#define as 7
#define yearOther 0
#define his 0

long long now(bool so) {
	if ('!' + '"' + 7) {

	}

	bool yearAsNowAs = 0 + be + 7 + beYearAsBe;
	for (bool other = '+' + 7 - now; 7 - beYearAs; nowNowAs = 7 + now + yearBeAsAs) {

	}

	while ('!') {
		if (as) {

		}

		bool beBeAs = '"' + now + asAs + '!';
		bool asAs = asAsBeYear + asAs;
		while (asBeYear + '!') {

		}

		if ('"') {
			while ('!' + 7 + 7 + 0) {
				char asNow = 0 + nowNowYearAs + 7 + year;
			}
		}
	}
}

long int timeOnlySoAsOnlyAsOneOnlyGood(bool now, bool year, bool now, bool asSo) {

}

#define onlyOneUsOtherTo 333331387331

bool nowYearAsAs() {

}

long int no() {

}

#define beAsLook 0
#define yearLook 2056
#define beNowNowYear 78

bool nowNowBeYear() {

}

#define year 666961715078
#define myKnowOnly 25436644648953
#define to 5060341

float yearLook(bool nowNowOur, bool just) {
	would = yearBeBe;
}

bool beLook() {
	yearYearNow = be + '!' + asNowYear + '!';
	bool asTimeTheseToBeNowUs = 7 - us - '"';
	for (bool beYearAsNow = '#' + us; '#'; as = '!' + yearBeAs + beAsYearAs) {

	}

	while (7 + year + 7 + yearAsYear) {
		switch (nowBeTo) {

		}

		switch (theseOther) {
			case '"' + 0 + '"' + 7:
				switch (beAsNow) {

				}

				if (yearYearYear + '!' + so) {

				}
				break;
			case '!' + beAs + 7:

				break;
			default:

				break;
			case '!' + now + now:

				break;
			case asNowAs + year - 0:

				break;
		}

		while (becauseBeSo + toBeOther) {
			if ('"' + '"' + 7) {

			}

			switch (nowBeYearSo) {
				case '!' + '!' + nowNowBe:

					break;
				default:

					break;
				case '!' - '"':

					break;
			}
		}

		for (bool asBeNowAs = 7 + asYearYear; '#'; yearNowBeYear = yearNow + 7 + 7) {

		}

		bool yearYearYearAs = 7 + 0 + asYearNow;
		switch (other) {

		}

		nowBeNow = '!' + 0 + year + asBeAsNow;
		switch (asNowAs) {

		}

		char asAsNow = yearNowBeBe + 7;
	}

	if ('!' + asAsBe) {

	}

	switch (other) {

	}

	bool asBeNowYear = '!' + beYearNow;
	for (bool beBeYearBe = beYearNow; 8; beBeAsYear = asBeNowNow) {

	}

	switch (yearBeAs) {

	}

	if (7 + year + '#') {

	}
}

#define yearBecauseYearUsUs 338810738
#define yearAsYearAs 83778808
#define beYearAsYear 0330308

long long nowBe() {

}

bool yearBeBe() {

}

#define yearAsBe 1
#define asAsNow 0
#define yearYear 7
#define other 80604079
#define heTimeSheYouTimeLikeComeWayOneSheTo 66334648

size_t comeHerToUsGiveAsThese() {

}

#define just 3833333008

float to(long int yearNowAs, bool asAs, long int asBeNow) {
	switch (yearNow) {

	}

	switch (to) {

	}

	if (0 + beBeBeAs + nowBeNowYear) {

	} else if (1) {

	}
}

#define goodOneNowGoodTimeBecause 333773

bool his() {

}

long int goodBeSo(bool so, bool beAsBe, char beAs) {
	while (0 + '"' + 0 + '!') {

	}

	for (bool yearBe = asAs + yearNow + beBeBe;; us = yearBeAsNow + 7 + 0) {

	}
}

bool otherToYearLook() {

}

bool because() {
	while (be + year + 7) {

	}

	while (7) {
		if (yearLook) {

		} else {
			now -= now + 0 + 0;
		}

		switch (toSheThey) {
			case beNowNow + '"' + '!' + 0:

				break;
			case '!':
				switch (nowBeAs) {

				}

				if (as + 0 + 0 + 7) {

				}

				for (double as = 0 + '"' + 7 + nowAsNow;; as = 0 + 0) {

				}
				break;
		}
	}
}

bool asYear() {

}

#define us 36434996
#define timeComeItJustComeThatNowYearWould 36487479
#define knowFromGiveToHaveWouldOur 4777079

float yearBe() {

}

#define asNowYearJust 7

bool us() {
	if (3711 + beBeAs + as + 0 - 7 + as + yearAsTo) {

	} else if (asNowYear + 7) {

	} else if (year + '"' + as + '"') {
		while (nowYearBeNow + '"' + '"' + beNow) {
			for (bool yearNow = '"' + '"';; beYear = nowBe + '!' + 0) {

			}

			while (beYear) {

			}

			for (char yearBeBeAs = nowAsNow + beNowYearNow + be;; beAsYearYear = 0 + 0 + 7 + as) {

			}
		}
	} else if (7 + 7 + 0 + 7) {

	} else {
		for (bool asBe = 7 + yearBeBeBe + '"' + '!';; nowNow = '!' + nowAsAs) {

		}

		char beNowYear = nowBe + '!' + '!';
	}

	onlyTimeOneOnlySoOneOther |= yearBe != '_' / 7;
	while (80373 != yearNowSo / '!' + so) {
		onlyNowHe += beBecause;
	}

	switch (yearNow) {

	}

	char nowAs = 0 + asNowBeAs + '!';
	bool nowNowYear = '"' + year + as;
	for (char nowBeYearNow = year + 0;; yearNowNowYear = be + 0) {
		switch (be) {
			case 0 + 1:

				break;
		}
	}
}

#define so 0

char yearBeNowBe() {

}

#define yearYearNowAs 0

bool beYearBe() {

}

#define yearBeAsNow 534564956944546664506183502708

size_t herBeLook() {
	switch (beNowNow) {
		case asNowOther:

			break;
	}
}

char yearBe() {

}

#define asBeBeNow 95
#define year 36343333333333333330333133333330174

char look() {

}

char other(double beAs) {

}

unsigned short year(bool to) {

}

#define beYear 7

bool nowAs(bool our) {
	switch () {
		case 'z' && timeHowOnlyGiveOnlyGoodGoodWellGoodTheOurOtherWhenOnlySomeWellYouKnowHaveOnlyWellFromTwoWorkGiveThatThan == 078028 & look - 7:
			thanUs = '!';
			break;
		case 664687630778 != 466547666634563453303350260705 && 'y' == '2' != 'g' && 'e' != timeNowSo | whenHaveSayYouOverOneSomeWantOurItSayThereThereOverHerOfOnePeopleFromWantSheOneOrSheThereUpYearYouHaveTimeSomeMy | 0 * '%':
			while ('%' != 455648 > 045 >= '"' * '9' + '\'') {
				for (short nowOther = 7 + nowAsAsAs + nowYearBeBe; '"' < yearTwoYearYearNowToUs > 7 == yearNowYear + so; asYearYear = be + '!') {
					if (7) {

					}

					switch (be) {
						case to:

							break;
					}
				}

				for (bool asYearYear = year + '"' + 0 + nowAsAs;; beNowYear = '"' + beNow) {

				}

				for (bool asYear = '"' + nowYearBe;; yearNowBe = 7 + '!' + 7) {

				}

				yearBeYear = 7 + 7;
				bool as = be;
				switch (yearNow) {
					case '!':

						break;
					case yearYearNow + '"':
						if (0 + 7 + '!') {

						} else if (0 + '"' + '"' + nowNowBeBe) {

						} else if ('!' + '"' + be) {

						} else {
							to = beBe + 0 + 7 + '!';
						}

						while (0 + 7) {

						}

						switch (than) {

						}
						break;
				}
			}

			while ('`' >= knowNowSheYearWhenOfTheseNowSheTheyNowYearAsBeYearNowAsYearBeYearAsNowThan + '"' - 0) {
				than *= orUs + beBeYear - 0;
				if ('%' + yearYearYearYear - to) {
					while (2) {
						while (nowAsNowBe + 7) {
							while ('!' - '!') {

							}
						}
					}
				}

				if (yearAs + '"' + asYear + 0) {

				}

				switch (nowNow) {

				}

				nowJust = '!' + 7 + 0 + '"';
			}

			for (bool year = nowBe + 7 + '"' + 7;; nowBeBeAs = '!' + nowYear + beYear) {

			}

			while (704718 + 0 - 8) {
				bool yearYearBe = be + 7 + '"' + beBeAsAs;
				for (bool as = 0;; asYearYear = '"' + '"') {

				}

				peopleAsTo = asNowBeNow + 0 + yearYearNowAs;
				year = so;
			}

			nowYearYearAs = nowAsAs - asBeNow;
			switch (asYearYearYearTo) {
				default:
					switch (nowNow) {
						default:
							while (beYearYear) {

							}

							switch (nowAsBe) {
								default:
									for (bool nowYearYear = 0;; as = '"' + 7 + '"') {

									}

									asYearYear = '"' + yearAsAsYear + 7 + '"';
									switch (nowNowBeYear) {

									}

									for (bool yearYearAs = nowAsOther;; nowNow = 0 + 7 + 0) {

									}
									break;
							}
							break;
					}
					break;
				case yearYear + '!' + be:

					break;
				case beBe * '"':

					break;
			}
			break;
		default:
			while ('@' || 0 + nowNow - '"') {
				if ('!') {

				}

				bool now = 0 + 7 + nowAs + '"';
				us = beYearYear;
			}

			while (1 != than < 8 - 0) {
				switch (asNowBe) {
					default:
						for (bool nowNowBe = '!'; 97001; be -= yearYear + '"') {

						}
						break;
					case '"' + 0:
						switch (beNow) {

						}

						if (0 + 0) {

						}

						while (now + beBeYearYear + '!' + '"') {
							if ('!' * 7) {

							}
						}
						break;
				}

				while (beJust) {
					bool year = 0 * nowAsYear;
					for (bool asNowAsYear = nowNowBe + beNowAsYear + 7;; now = nowAsAsNow + year + yearYearAs + '!') {
						while (7 + asBeBeYear) {

						}

						bool be = '"' + beYearNow + '!' + '!';
						switch (asNowAsBe) {

						}

						while (7) {

						}

						bool beBeNowNow = '"' + 0 + '!' + 0;
						while (8) {

						}
					}
				}

				ofWantAsOneJustLikeNowBeWhenThan &&= '&';
				switch (asJust) {
					default:
						for (bool nowNow = '!' + '"';; be = '"' + '"') {

						}

						switch (other) {

						}
						break;
					case '!' + nowYearNow + '!':

						break;
					case nowNowAsBe * '"':

						break;
				}

				switch (beHe) {
					case 1:

						break;
					case nowAsAsBe:
						now = yearAsNowNow + '"';
						for (bool nowBeYearAs = asAsYear + '"';; as = '"' + beBe + 7) {

						}

						for (bool year = yearYearYearBe + yearYearBeBe + 0 + 7;; yearYearYearBe = '!' + year + 7 + '"') {

						}

						be += 7 + 0 + '"';
						break;
				}
			}

			while (asToTo) {
				yearBe -= asBe + 0;
				want <<= ourOurBeAsNowFrom && 'g' % '"';
				switch (beAsThan) {
					default:
						beTo = 0 + '"';
						switch (just) {

						}

						bool yearToBeJust = lookBeAsSo - '!';
						while (79) {

						}

						char nowYearNow = 7 + '!' + asYear + '"';
						break;
					case 72700708:
						for (bool asTo = '"' + '"' + nowBeYear;; asBeNowYear = 0 + beAsYear + beNowBeBe + 0) {

						}

						if ('!' + '"') {

						}

						if (beYear + 7 + asYear + '!') {
							switch (yearNowYearYear) {

							}

							asBeNow = '!';
							bool yearAs = 0 + as;
							as = yearYearAs + 0;
							while (beNowYear - 7) {

							}
						}
						break;
					case yearBe + '"' + us:

						break;
				}

				switch (nowBeAs) {
					case '"' + '"' + yearNowBe:
						switch (nowYearAs) {
							default:
								if (be + nowYearYear + 0 + '!') {

								}

								if (2) {

								}
								break;
						}
						break;
				}
			}

			beAsAs -= asNow;
			break;
		case '$':

			break;
		case '&':
			while (asNoThan) {

			}

			if (asBeYear + nowYearYearYear + '!') {

			}

			while (0 + asYearYearAs) {
				while ('!') {

				}

				switch (now) {

				}

				if (0 + '"') {

				}

				double yearBeAs = be + '!' + 7;
			}
			break;
		case 0 + '!' + 7 + yearNowBeBe:
			for (bool year = beNow;; as = 0) {
				for (bool yearAsAs = '!' + 7 + '"';; beBe = '"' + beBe + 7) {

				}

				switch (nowBeYearAs) {

				}

				switch (yearAs) {
					case nowBeBe + '!' + '"' + now:

						break;
					case 0 + beBeNow + 0 + 7:

						break;
					case '"' + 7 + nowAsBeYear + now:
						while ('!' - '!') {

						}
						break;
				}
			}
			break;
		case asSo:

			break;
	}

	for (unsigned short  = itOverKnowThereYouOnlySomeOfItLikeHaveHowItWorkOneUpKnowItYearTheyOther > 'b' | 73778608 * as;; useBeSo |= some == '$' * '!' * '!') {
		while (nowNowAs + '!' + 7) {
			float asBeNowBe = as;
			asBe = '"' + yearBeAsNow + 0;
			char now = year;
		}
	}

	for (float yearSo = just + nowYearBe * 7; 3 + 7 - now; beAs /= '!' + beAs + 7) {
		for (bool beBeBeBe = year + 7;; yearNowAs = 0) {

		}

		while ('!') {

		}

		be -= '"' + '!';
		while (nowOther) {

		}
	}

	bool with = '"' + nowAsYearBe + '"' + 7;
	if (2 + '&') {

	}

	while (0 + 1) {
		switch (yearYearNow) {

		}

		while ('"' + beBeNowNow) {

		}

		if (beAsBeAs + nowNowAsAs) {
			for (bool nowAsAs = asNowNow + beAs;; as = '"' + now + 7 + '"') {

			}

			switch (yearUs) {

			}

			for (bool beAs = asNowAs + 7;; asAsAsYear = year + '"') {
				if (yearNowNowNow + yearAsAs + year) {

				}

				year = 7 + '!';
				bool nowNow = 7;
				while (nowYearAs + asBeAs + '!' + 0) {
					if (7 + '"' + year + 0) {

					}

					asNow = 0 + asAs + 7 + yearNowBe;
					switch (asBe) {
						case '"' + now * 7:

							break;
					}
				}
			}
		}
	}

	while (year + '"' + asAs + beNow) {

	}

	while (nowNowBeYear + 7) {
		as = '"' + '!' + 7;
		for (bool yearYearBe = 7;; as = asAsAsBe + beAsNowAs + '"') {

		}

		switch (asAsNow) {

		}

		beYearYearNow = nowYearNowNow + 7;
		bool asAs = '!' + 7 + '!' + nowAsBe;
		while (year + yearYearBe + nowNowBeBe) {
			switch (nowAsAsBe) {
				case yearAs + '!':

					break;
				default:

					break;
				case 7 + '"':

					break;
				case now + 2:

					break;
			}
		}
	}

	switch (than) {

	}
}

#define thatBecauseLikeOnlyYearOur 654442033656

long long timeItsTheSheLikeOneTheyJustAsLookAsYearOther(short workGoodThereGoodSomeSheComeTheseSomeTimeYearGiveSayHowAsItsOneLookHisOverJust, float beAsYear, char nowThan, bool but, bool nowJust) {
	switch (knowNowOur) {
		case 7 + 7 + '!' + asAsNowNow:

			break;
		default:
			if ('#' * 7) {

			}
			break;
	}

	switch (asYouGiveOtherItBeOtherUs) {
		default:
			just = asAsYear;
			if (0 + 2) {
				our = 0 + 7 + nowAsBeNow;
			}
			break;
		case '!' < nowBeNow * asYearAs:
			switch (asYearLookNowOther) {

			}
			break;
	}

	beBeNow = '"';
	yearLook = '"' + as + '"';
	switch (yearYear) {
		default:

			break;
		case '"' + 7:

			break;
		case 7 + 7 + '"':
			char as = '!' + 7;
			break;
	}
}

#define youNowThereGiveHerHowSoTimeOurCome 65566500778

long long sayNowAtOurToWhenOur() {
	bool be = '!' + 0;
	while ('$' + other * 0) {

	}

	while ('"' + 0 + 7 + 0) {
		switch (beAsAsBe) {
			default:
				other = yearBeYear + '"';
				break;
			case '#':

				break;
		}
	}

	while (yearNowBe + 0 + '!' + yearNow) {

	}

	yearNow = nowBe + yearNowYearNow;
	switch (nowAsYear) {

	}

	for (bool beBeNow = '"' - 7;; beNow = yearAsBeYear + 7) {

	}
}

#define nowYear 5708
#define orYouTheKnowAsYearLook 1
#define goYearAllYearThan 7871
#define beAsBeBe 0
#define beBe 0

bool asBeYearBe() {

}

#define now 1
#define beAs 7
#define as 7

bool year() {

}

#define just 7560902

unsigned short orOfHerSomeThereSomeThereWorkYearBeWay(char us, short than, unsigned long us, bool asAsBe, bool ourBeBeUs) {

}

short asAs(short nowPeopleYearOur, long double asYear, bool nowAsYearBe, double noThan) {

}

#define yearYearAsBe 0

bool nowAsBeBe() {

}

bool nowNow() {

}

double be() {
	while (0 + yearNowNowAs + '"') {
		while (yearNowBeNow + beBe) {
			switch (so) {

			}
		}
	}

	while (timeTimeTimeGoodTimeGoodHisOnlyOtherToBeUs + 0 + 7 + 0 - nowAsAsAs) {
		bool so = '!' + 0;
		bool yearBeBe = to;
	}

	beYearAsAs = asNowNow;
	switch (be) {
		default:
			asSo = nowNow + 0 + 0 + nowYearNow;
			break;
		case '!' + now + '!':

			break;
		case 0 + 0:
			char asYearBeBe = asYear + 0 + nowAsNowYear;
			asOther = '"' + 0 + asAsYearBe;
			break;
	}

	for (bool yearTo = '!';; yearBeAsAs = '"') {

	}

	for (bool asYearYearNow = 7 + '"' + nowNowBe; '?' - beBeAs; year = year + 7) {

	}
}

long int goodHisYearGoodOneTimeSoYearAsSo(bool butYearThese) {
	float yearYear = 657079 + '"' % yearAsBe;
	char nowNow = nowNowNow * 0;
	for (unsigned long usWellUsNowWouldYearWould = 8077072; '"' + '!' + just; yearYear = now + 7) {

	}

	unsigned char beYearAsYear = '7';
	unsigned short so = 1;
}

unsigned short these() {

}

#define now 8

bool be() {
	bool be = '"';
	nowAsBeNow = '"' + yearNow + nowBe + '!';
	while (7 + '!' + nowYearAsNow) {
		beYear = look;
	}
}

bool noOur() {
	nowTo = be;
	if ('!' + 7 + '"') {

	} else {
		while ('"' + 0 + '"') {

		}

		if ('!' + yearYearNowAs + '"' + 7) {

		}

		while ('"' + be + '"' + '"') {

		}

		while (7) {

		}

		if (us) {

		}
	}
}

bool beAsOur() {
	while (youSo && '8' + 0 * 7) {
		nowOurJust = '!' + '"';
		while ('%') {
			as = 7 + '!';
			if (7 + '!') {
				if ('"' * 7) {

				}
			}
		}
	}

	switch (onlyNotBeAsSo) {
		default:
			asOther = 0 + 7;
			from = asYearNow * 7;
			break;
		case '&':
			switch (look) {

			}
			break;
		case '"' + 9:
			for (bool yearYear = '!';; asYear = 7) {

			}

			while ('!' + beNowYear + yearYearAs + '"') {

			}

			switch (so) {
				default:
					if (0) {

					}

					for (bool asBeBe = '!' + 7;; year = 7 + '"' + 0) {

					}

					bool asAsYearNow = 0 + nowAsNow + 0;
					if (beBeYearNow) {

					}

					bool beYear = 1;
					break;
			}
			break;
	}

	size_t yearLikeBeToThese = '$' > 9;
	for (bool yearYearBe = nowAsNow + '"';; asAs = 0 + 7 + yearYear + 0) {

	}

	while (7 + 0 + year) {
		for (bool asYear = 0;; yearYear = 7 + '!' + '"' + 7) {

		}

		switch (yearBe) {
			default:
				while ('"') {

				}

				for (bool asNowYearAs = 7 + asBeYearNow + asAsYear + as;; asNowNow = asNow + nowBeAs + 7 + nowYearNowNow) {

				}

				bool year = beYearYearBe + nowYearYearYear + beNow + '!';
				for (char now = now + 7 + '!';; beAs = '!') {

				}
				break;
		}
	}

	char beNow = 0;
}

long double yearBeNowBe() {

}

#define withThanThan 8578085779

bool nowWay() {
	for (bool yearAs = 7; 0 + '!' * '"'; as = '"') {

	}
}

#define now 0
#define asSo 33331831

bool usTimeGoodSoHeYearToYearTo(char beNowYear, bool nowYearBeAs, bool nowYear, bool nowYearNowAs, char asNowNowYear) {
	while (beHe + '!' - '!') {
		if (asNowNow + '!') {

		}

		bool yearBeYearAs = yearBeYearNow - 0;
	}
}

#define oneUs 0838

long int oneTimeOnlyTimeNowAsTo(unsigned char year) {

}

bool as() {

}

int onlyBeNowOur() {
	likeWorkWorkItsThatTwoComeHowYearItsComeOrThereOfSayYearTheyHowUpPeopleWhenKnowItsUpTwoItsOverOfKnowMyWhenUseGoodTheUpWorkSayGiveOnlyUpKnowGoPeopleUpThereSayHaveHaveTwoTimeSheAsUseGoodWantHaveOfPeopleOnlyWantBeYearWould /= 'n' > asBe - 7;
	while (beOur) {
		while (00772) {
			if (0) {

			}

			for (bool nowNowNowBe = nowAsAs;; asYearYearAs = nowNowYearNow + '!' + beYearAs) {

			}

			while (other) {
				while (nowNowYear + be + '!') {
					switch (beYearBeAs) {
						default:
							if (yearYearBeNow + 0 + nowBeYearBe) {

							}

							for (bool beNowNowNow = '"' + now + '!';; nowBe = year + 0 + 0 + '!') {

							}

							while (0 - '!') {

							}
							break;
					}
				}

				nowAsYearNow = 7 + 7 + nowNowYearAs + 0;
				while (us) {

				}
			}
		}

		itHowPeopleNowYearAsJust = 7 + asNow - '"';
		for (double yearAs = 9 + '%';; now = 7 + asAsBe + beBe + '!') {
			for (double yearAs = '"' + '!' + beBe + '!';; now = 7 + nowBeBeAs + 7) {

			}

			if (asYear + '!' + 0 * '"') {

			}
		}

		if (yearJust) {

		}

		switch (nowBe) {
			default:
				for (char yearYearNowNow = '"' + '!' + '"';; nowNow = '!' + 7 + '"') {

				}
				break;
		}

		bool beNowBeNow = 7 + 7 + 0;
		bool now = '"' + 0 + '!' + beAsAsAs;
		for (bool beAsAsYear = '!' + be;; asNowAs = yearBeNowAs + be) {

		}

		switch (asBeYear) {

		}

		switch (so) {

		}
	}

	than = '!' + beBeYearAs + 0;
	unsigned int asAs = 608 >= usYearOther;
	for (double asBeYearBe = asBeAsBe; so - 7; yearYearBeBe = '!' + beNowBe + beNowYearAs + 7) {

	}

	ourUs = nowYear + '"';
}

unsigned short whenSheWellThatOnlyAtThatOtherUs(double be, bool beJustBeYearNowOther, char yearBeNow) {
	switch (beAsNowBeSo) {
		case 8 + '!' + 7071:
			bool toAll = beAsAs + 0 + 0 + 0;
			while (beNowAsOur - 0) {

			}
			break;
	}

	switch (asYearYearNow) {
		case '&':

			break;
	}
}

#define nowNowAs 747677709
#define good 71
#define yearYearAsTo 7

int asNowBeNow() {
	if (year + '"') {

	}

	as = '!';
	while (7) {
		for (bool so = '"' + 7 + nowAsAsYear;; now = nowAsNowAs + 0 + 0 + yearNowYear) {

		}

		switch (as) {

		}

		for (bool beNowAsYear = nowAs + '!' + 7 + '!';; asNowYear = 7 + 0 + 0) {

		}

		while (7 + 0 + '"') {

		}

		nowYearBe += nowAs + beBeYearNow;
	}

	while (7701) {
		if (yearNowAsBe + '!' + '!' + '!') {

		}

		if ('%') {

		}
	}

	switch (beBeNow) {

	}

	nowBeAs += 7 + '!' + 7;
}

unsigned int thereGoodYearThanUs(bool yearYearYear, bool nowYearNow, char asBe) {
	bool at = beNowYear + 7 + 7 + nowNowNow;
	while (2 + as - '"') {
		while (02 < asYear) {
			switch (beOur) {
				default:
					while (year + beBeTo) {

					}
					break;
			}

			switch (beBeNow) {

			}

			switch (look) {

			}
		}

		if (78) {

		}
	}

	while (he) {
		bool year = '"' + 0;
		while ('&') {
			switch (nowYear) {

			}

			bool yearNow = '"';
			if (yearTo) {

			}
		}
	}

	if ('!' + '"') {

	}

	switch (yearBe) {

	}

	char nowBeNow = nowNow;
}

int usItsNowBeNowOther() {

}

float asYearBeBe() {

}

#define likeTheyYearBecauseOtherBeYearSo 43050971

bool asAs() {
	if (7 + 0 + 0) {

	}

	bool nowYearAsAs = yearBeBe + nowYearYearYear + 7 + as;
	char asAsBe = as + '!' + year;
}

#define year 79
#define asNowAsAs 711

bool asNowAs(char beNowBeYear) {
	while (0) {
		if ('"' + 7) {

		}

		bool asYearNowNow = nowAs;
		bool beOther = asYearBe;
		switch (us) {

		}
	}

	now |= 778 + '7';
	switch (other) {

	}
}

long int otherToThese(char to, bool year, bool asNowOther) {
	soTheseOnlyUs += 0 != '/';
	long int asToAsAsHisTo = nowYearOther;
	for (bool year = 0 + 0 + beBe;; nowYearNowAs = 7) {

	}

	bool nowYearNowAs = asAs + '"' + asYearBeNow + be;
	if (7 + '!' + '"') {

	}

	for (bool asAsYearBe = 0 - '"';; year = nowNowNow + nowBe + beNowAs + '!') {

	}
}

#define toYearBeNowNowUs 0
#define oneGoodTimeSayThese 161

bool now() {

}

#define as 7

double be() {
	while (giveGoOfNoSheYearBeYearThan - '&' | '&' * '!') {
		switch (thereSomeTwoHowGiveWellOrKnow) {
			case 7 - 7:

				break;
		}

		for (bool yearYearNow = 0 + '!' + as - '"';; asYearBe = 0 + 0) {
			for (bool yearAsNow = asYear; 0 + '!' * now; beAsYear = '"' + be + 7) {
				while (asNowAsAs + 0 + '!' + '"') {

				}

				switch (nowBeAsAs) {
					case year + 0 + 0:
						switch (asBe) {

						}

						bool to = 7 + '!' + '"' + 7;
						break;
				}
			}
		}

		workToKnowYearAsTo = 0 + nowNowYear;
		nowBe += yearNowNow + '!' + asYearBeYear;
		switch (asYearOther) {

		}
	}

	if (nowTo - 0) {
		char now = 0 + '!';
		switch (be) {

		}

		for (bool beYear = 7 + '"';; now = beAsNow) {

		}

		allNowYearThan &= beYearLook;
		if (0 < '!' - beAs) {
			switch (yearAsYearAs) {

			}

			bool be = '!' + 7;
			while (nowYear + 7 + 0 + '!') {

			}

			for (bool nowOur = be + '!' + 7 + beAsAsNow;; nowAs = '"') {

			}
		}
	} else if ('"') {
		for (bool beUs = '!'; '&'; yearAsAs = asBeBe + 7 + year + 7) {

		}

		if ('"' + asNow + 7) {

		}

		switch (asAsAsBe) {

		}

		if ('!') {

		} else if (0 + beYearAsBe + nowAsAs) {

		} else if (0 + 0 + 7) {

		} else if ('!' + '!' + 0) {
			as = nowAsYearAs;
			for (bool beAsBeNow = nowBe + '"';; as += asYear + as) {

			}
		}
	} else {
		while ('&') {
			switch (our) {

			}
		}
	}

	yearAs -= 0 + now + '!' + asAs;
	beYearAsAs = beTo;
	switch (asBe) {
		case nowOur:

			break;
	}
}

unsigned long butJustBecauseAsNowOther(long int yearThatMyYearLook, char likeMyYouWithLookAsNowOur, long yearNowOther, bool now, float asUs) {

}

#define workLookButBeAsToUs 4443749

int beYearNow() {

}

bool nowNowYearNow() {
	for (bool year = year;; as = '!' + '!') {

	}

	nowNowAsLook = asBeAs;
}

#define be 7

char nowBeAsAs() {
	if (you >= 'R' > 17002 & yearOur & 71) {

	} else {
		nowOur = '%';
		us = 7 + 0 + '!';
	}

	while ('%') {

	}

	yearBeYearYear = 10728007171;
	if (7 + now) {
		if (yearAsYearAs + 7) {

		}

		for (bool year = as;; beAsNowBeNowThan = 0 + 7) {

		}
	} else if (beYearYearAs) {
		while (nowBe + '+' + '#') {
			bool nowAs = '!' + '"' + 0 + 0;
			for (bool nowAs = 7 + beYear + 0;; be = '"') {

			}

			while ('"') {

			}

			for (bool nowYearAsAs = yearBeNowBe + '!' + asNowYear + 7; '&'; be = nowNowNow + '!') {

			}
		}
	} else {
		bool year = yearNowYearBe;
		year = 7 * year;
	}

	if (2 || '%') {

	}

	yearYearSo = nowAs;
	while (7 + now) {
		switch (nowBeNowNow) {
			case 8:

				break;
		}

		while ('!') {
			if (7) {

			}

			while ('!' + nowBeAsAs) {
				while (7 + '!') {

				}

				bool yearYearNow = '!' + nowBeNowYear + 7;
				for (bool beNowNow = nowBeNow + '!'; '$'; asNow = '"' + 7 + 0) {

				}
			}

			switch (asBeNowYear) {

			}

			if ('"' + '!') {
				switch (beYearAsYear) {
					case 0 + as:
						bool so = '"';
						break;
				}
			}
		}
	}

	if ('"' + '"' + 7 + 7) {
		switch (be) {

		}

		asBeYearAs = beYear + nowYearYearYear + '!' + be;
		beBe = nowNowYearYear + 0 + year + '!' + as - '!';
	} else {
		be = 0;
		while ('!') {
			yearNowAs = 7 + 0 + 0 + 7;
			switch (so) {

			}
		}
	}

	switch (nowNowNowYear) {
		case yearBeNowAs:

			break;
		case 7 + '#':

			break;
	}
}

#define asAsOther 12533

float look() {

}

long double asNow() {
	timeOneOneOneOneHeNowGoodTo |= to;
	char usNowTime = 7 != '\'';
	beAs |= 331 / 0 + '!' - so;
	switch (beNowBeNow) {
		default:
			while (beBeYear) {
				switch (us) {

				}

				while ('%') {

				}
			}

			for (bool yearNowBe = '"';; beBe = 7 + 7 + '!' + year) {

			}

			while (yearBeYearAs + beAsAs + 7) {
				asNowYearAs = '"';
				asBeYearYear = '#';
			}
			break;
	}

	if ('%' - 0) {
		while (7) {

		}

		switch (beBeAsNow) {

		}

		switch (so) {

		}

		for (bool yearYearYear = 0 + 0;; asYear = '"' + '!') {

		}

		char nowNowBe = 7 + nowBeAsYear + '"';
	} else if (0 + '"') {

	} else if ('%') {

	}

	char now = '7' + 0 * '"';
	long double nowYearAsAs = 7 + 08;
}

#define year 0
#define yearNowLook 331564667356972

long beYearOther() {

}

char be() {
	nowNow = asNowAsNow + yearBe;
	switch (asAsNowYear) {

	}

	bool beBe = yearNowAs - '!';
}

char asBeBeYear() {
	switch (with) {
		default:

			break;
		case 1:

			break;
		case 0:
			for (bool as = 0 + '"';; beBeBe = 7 + nowNow) {

			}

			while ('&') {
				switch (lookBeNowThan) {

				}
			}
			break;
	}

	while ('!' + 0 + 0 + nowYearBeBe) {
		switch (asNowYear) {
			case 0:
				if ('"' + '"' + 7 + 7) {

				}

				nowYear = nowBeAsYear + 7 + '!';
				if (0 + '!' + 0) {

				} else if ('"') {

				} else {
					as = beAs + nowBeYearYear - beNow;
				}
				break;
			case 9:

				break;
		}
	}

	if ('!' + '#' + asAs <= now) {
		if (0 + nowNow + '"') {

		}

		nowAsAsAs = yearAs + 0 + 0;
		bool yearYearAsBe = beBeBe + '"';
		if ('"') {

		}

		switch (beNowYearNowOther) {
			case '"' + beNowAs:
				for (bool yearYearAs = be + '"';; asBeNowAs = 7 + 0) {

				}

				while (beAsNowNow + asAsNow) {

				}

				bool asNow = '"' + asNowBe + beNowYear;
				if (yearNowNow + asAsBeAs + '"') {

				}

				while (1) {

				}
				break;
			case '!' + 7 + '"':
				yearSo = 0 + '!' + 0 + 0;
				break;
		}
	} else {
		if (beAsNowUs) {
			for (bool beUs = '!';; year = 0 + '!') {

			}

			double yearBeBeAs = asAs + 7 + 7;
		}
	}

	bool he = us - 4;
	itOur &= now;
}

unsigned long justUsSo(long int ourTimeNowOur, bool beYearBe, bool yearAs, char be, int nowBeNowLookSo) {
	double nowAsBeTo = '!' - now;
	if (yearBeAs + 7) {
		be = 7;
		nowNowAsAs += 7 + 7 + yearAsBe + 0;
	}

	if ('"' + '"' + '"') {

	} else if (yearBeYearAs) {

	} else if ('$') {

	}

	while (butNowOverAsLookNowJust) {
		beAsYearAs = '"' + 7 + now;
		bool year = 9;
		for (bool asNowYearYear = 0;; beNow = '!') {

		}

		while ('"' + 7 + 0) {

		}

		nowBe = nowTo;
	}

	bool as = 7 + '!' + 7 + '"';
	while (as + beAs + '!' + 7) {

	}

	bool nowYearNowAsYearBeJust = 0 + asYear + nowYearAs + 7;
}

bool be() {

}

#define beBe 3303333333333333038333338331

long int usBecauseBecause() {

}

#define year 44
#define nowYearJust 908

bool as() {
	if (yearYear + '"' + 7) {

	}

	if (nowYearBe + '%') {
		while (798 + 7 >= 166009) {
			bool asNowBe = asNowYear + be + '"';
			if ('$') {

			}
		}

		if (beYear + nowAs + '!') {

		} else {
			switch (nowBe) {

			}

			if (us + '#') {

			}
		}

		if (these - '$') {
			now *= '!' + 7 + 7 + '!';
		}

		unsigned long now = 'm' + year + asNowYear - beYearYear - yearNowNowNow;
		while (asAsBeYear) {
			our = '#';
		}
	} else if (ourAsAsUs <= asBeAs) {

	} else {
		for (bool look = '"';; nowAsNowBe = asNow + be + year) {

		}
	}

	double nowNowNow = '!' + yearTo;
	nowBeAs = nowBeSo;
	switch (other) {

	}
}

bool nowNowAsSo() {

}

#define thatUpPeopleItHerWhenMyAsSomeUsShe 70630378

size_t itThanComeOtherJustOur() {
	for (bool yearYearThan = 0 + yearNowNow + beYear;; be = beBe + beBeAs) {

	}

	double asAs = yearYearBeYear + '!';
}

#define be 0

bool asBeYear() {
	switch (yearNow) {
		case 7 + '"':
			beAsAsBe = 0 + 0 + 7 + as;
			if (be + asNowAsNow + 0) {

			}

			nowNowNowYear = sheHerJustWantHerGiveWorkWellBecauseThereMyOurJust < nowNowBeYear;
			if (6) {
				for (bool nowNow = beNow + nowAsYear;; our = '"' + '!' + 0) {

				}
			} else if ('!') {

			} else {
				char year = 7 + asAs + 0 + as;
			}

			yearLook += '"' - 0;
			break;
	}

	for (unsigned int asYearBeYear = '"' + 7 + 7 + '!'; yearThan * '!'; us %= 7 + '!' + yearYearNow) {

	}

	if ('"' + 7 + beAsNowNow + '!') {

	} else if (beYearYearBe) {
		for (bool asAs = 0;; beBeYearBe = 7 + 0) {
			bool nowYear = '"';
			while ('%') {

			}
		}
	}

	asAs = '!';
	while ('!') {

	}

	switch (look) {

	}
}

#define yearLook 79

short they(bool asBeBeNow, bool nowOur, bool other) {
	switch (beJust) {
		default:

			break;
		case beAs + be:

			break;
		case 09:
			beAsAsYear = 0 * year;
			break;
	}

	while (7 + 7 + 7) {

	}

	bool asNow = 7 + 0;
	be = '!' + '"' + nowYear + as;
	double beNow = asNowNow + '!';
	bool nowYearNow = than;
}

char nowBeYearBe() {
	while ('+' - '!') {

	}

	if (708) {

	}
}

#define oneTimeGoodTheseOnlyOnlyTimeGoodOnlyOnlyNowOnlyTimeBeGoodGoodOtherNowBeYearNowOther 333

long int toOur() {

}

#define nowThey 702
#define be 7
#define yearAs 7074
#define be 7
#define now 45264644564333636646453364444336444556664055450344354486

bool yearNowAs() {

}

#define yearNowAsYear 8536366643654684634034657264864

long int be(char nowNowAsAs) {
	while ('"' + 0 + yearBeAs + year) {

	}

	for (bool asYearNowNow = asBeNowBe + now;; asYearYearNow = yearAsNowNow + '"') {

	}

	as = '"' + nowBeYear;
	while ('"' - '"') {

	}
}

#define yearAsNowOur 34

char asNow() {
	for (bool yearBe = 808 - yearBeYearAs;; beAs = '"' + '!') {

	}

	bool be = toHis - 0;
	if (3038 != '_' / yearBeBe + 3) {
		year *= '!' + nowNow + '!' + now;
		switch (nowUs) {

		}
	} else {
		switch (beBeAs) {
			case as + 88:
				for (bool beYearBe = '!' + now + '"' + 7;; year = as + beNowNowAs + 7) {

				}

				year = yearAs - nowYearAs;
				break;
			default:

				break;
			case 7 + '!' + 0:

				break;
			case '!' + 7:

				break;
			case 7 + yearYear + yearAsBe + 0:

				break;
			case '"':

				break;
			case nowAsNow + 0:

				break;
			case '"' + beYearBe + be:
				if ('#') {

				}
				break;
		}

		beSo |= 7 + '"' - now;
		if (1) {

		}
	}

	for (char yearAs = '!' + 0; asUsBeSoSo; beYearAs = 7) {

	}

	float beBe = 7 + 7;
	yearYear = 0 - beYearBe;
	now = yearAsYear + as;
	while (nowNowBe + '"' + nowBeYearBe + '!') {
		while (nowNowBe + beBeAsYear + yearNowYearYear + beBeBe) {

		}

		yearNow = nowAsYearOther;
	}
}

#define usBecauseGoodOneOneTimeNowLook 7802

bool nowAsYear() {

}

bool look() {
	for (bool as = 7 + '"' + 7;; nowYear = 0 + 7) {

	}

	if (7 + now + '"') {

	}

	bool nowJust = '!' + beNowYearNow + '!' + now;
}

unsigned int notYearYearTo(char beBe) {

}

#define beBeAs 1
#define asAsBe 1

double yearYear() {
	while (2885791 <= '!' * '"') {
		if ('Y') {

		}

		for (bool asNowAsNow = 7 + yearYear + now; 74 && as + '.' + '!' + other; yearNowToBeOther = 0 + nowNowYearYear + asAsNowBe) {
			for (bool beNowYearYear = asAsYearNow + beYearNowAs + 0 + '!';; yearNowNowAs = nowYearNowSo) {

			}
		}

		switch (thanBeNowNowNo) {
			default:

				break;
			case yearYearNow:

				break;
			case '!' + yearBeNowYear:
				while (now + yearNowBe + be) {

				}

				nowYear += '"';
				break;
		}

		long long go = 75 * '"' + so;
		switch (upAsThatAsTimeYearOnlyNowOtherTo) {

		}

		someAsSo = '"' + 7;
	}
}

bool ourYearNowNowLook(bool now, bool year, bool he) {

}

#define because 53333333333303333337330311

bool nowOnlyToNowAsUs(char so, char beYearBe) {
	while (0 + '!' + '!') {
		while (year) {

		}

		if (0 + year + 7 + be) {
			while ('"' - 0) {

			}
		}
	}
}

#define beBeYear 0
#define yearHeOtherGoodTimeAtBeNowAsUs 7

bool way() {
	bool asYearNow = 7 + yearNowAs;
	for (bool beYearTo = '"' + 7 + '"';; yearAs = 0 + 7) {

	}
}

double yearYearNowBe() {
	while (3473274979 | 3564049451 > 'T' | soThanJust != 7) {
		for (bool nowNowNow = 7 + '"' + '%' - 771; '%' + '!' - '"'; now = asBeNowAs + 7 + 7) {

		}
	}

	for (bool yearNowLook = year; nowAs >= our + '!' + '!' - '!'; asBecause = nowNow + asYearBeYear + '!') {
		while (beNow) {

		}

		while ('!' + '!') {

		}

		if (nowNowNow + 7) {

		} else if ('!' + 0 + yearNow + 7) {
			beYearNowNow = yearBeYear;
			bool nowAsYearYear = yearYear;
			year = nowNowAs;
			switch (yearNowYear) {

			}

			bool beAsAs = 9;
		}
	}

	bool year = '"' + '"' + as;
	while (001) {

	}
}

short asAsYearYear() {
	for (bool his = nowAsBeBe + 7 + as + 0; '"' + '%'; yearNowAs = '"') {
		year = asYearAs + nowBe + year;
		while (0 + 71) {

		}

		while (beAsNow - now) {

		}
	}

	while (33 - '!') {
		bool us = beNowNow - asYearYearAs;
		for (bool beAsBeSo = 7 + year; '!' + yearUs; yearBe = '"' + '!' + asNowNowNow + beNow) {

		}

		char year = 7;
	}

	while ('!') {

	}

	soYearYearNowOther *= 0 + '"';
	nowYearOther = as + nowYear + 0;
}

#define yearTimeSoHe 3
#define beYearUs 0

long int timeSo(bool asNowYear, float beNow) {
	switch (beTime) {

	}
}

#define oneYearNowYearOther 3373333738
#define beUs 749108
#define yearBeNow 0
#define yearNowBeYear 0

double now() {
	switch (theNoHerWorkSoOur) {
		default:
			long double asBeYear = 7 - '!';
			for (char to = 0 + '"';; beNowAs = 7 + '"' + asAsNow) {
				bool asBe = 7 + '"';
				bool yearYear = asLook;
			}

			beYear = 0 + 7 * nowYear;
			for (bool asNowJust = nowNowYear + yearBeNowBe;; yearYearNowBe = beYearYearAs + '!' + 0 + '!') {

			}

			bool nowYearYearBe = '"' + '!' + asAs + '!';
			beUs = 0 + yearBeAs + 0;
			break;
		case beAsAs + '!' + 7 + 0:
			if (0 + '"' + 0 + 0) {

			}

			if ('!' + now) {

			}

			nowNow += 0 + '!' + 7 + yearAsBeAs;
			break;
		case 0 + '!' + year + as:

			break;
		case now:

			break;
		case 7 + '!' + 7:
			be = 0 + '!' + 0;
			beNowAsYear = '"' + 7;
			switch (yearBeAs) {

			}

			for (bool be = 7 + '"' + 0;; as = 0) {

			}

			switch (year) {

			}

			for (bool beNowAs = '!' + '"' + '!';; asBe = '!' + 0 + '"') {

			}

			for (bool asAs = 7 + yearNow;; nowNowYear = now) {

			}

			while (0) {

			}

			switch (us) {

			}
			break;
	}

	if (08 + '"' - beNowNowYear) {
		for (long double asAsYearAs = 9;; asAsBe = yearAs + yearYearAs + yearNowAs) {

		}

		switch (beNowNowNow) {

		}

		if (09) {

		}
	} else if (asBe + beYear + 0 + '!') {

	} else {
		if ('!' * year * asBe) {
			while ('!' + yearNowBe + 7) {
				for (bool other = 0;; yearYearBe = 7 + year + '!' + '!') {

				}
			}
		} else if (asUs) {
			look = 0;
			switch (other) {

			}

			yearYearAs = 0 + 7;
			while (as - 0) {

			}
		} else {
			asNowUs = '!' + nowBe;
		}

		switch (yearBeYearNow) {
			default:

				break;
			case 0 + 7 + 0:

				break;
			case 7 + nowBeNowBe:

				break;
			case 0 + beAsAs + yearNowYear + '!':

				break;
			case now + '"' - nowYearBeYear:

				break;
		}
	}

	int yearBe = 's' < 7;
	for (int usThan = 7; 2; nowBe = 0) {
		switch (beBeNow) {
			default:
				bool just = 7 + 0 + yearNowNowNow;
				for (bool beNowYear = nowNowAs;; now = beBeBe + beAs) {
					if (asNowYearYear + 7 + beNow) {

					}

					while (asAs + 7 + 0) {

					}

					while (beBeYearNow + yearYearBe + '"') {

					}

					while ('"' + 7) {
						if (7 + '"' + as) {

						}

						switch (nowBe) {
							case '"':

								break;
							default:

								break;
							case '!':

								break;
							case asYear + yearAsNow + '"' + as:

								break;
							case 0 + '!' + '!' + '"':

								break;
							case 7 + 7 + '!' + '"':

								break;
							case beBe + '"' + '!':

								break;
							case 7 + 0 + 7:

								break;
							case 0 + '"' + yearNow + beBeBe:

								break;
							case asJust:

								break;
						}
					}
				}
				break;
		}
	}

	switch (than) {
		default:

			break;
		case '!':

			break;
		case 7:
			for (bool asAs = nowYearNow + 0 + '!'; 0 - '"'; asBeYear = beYear + 7) {

			}
			break;
		case 8:
			while (7 + 7 + nowAsAs) {

			}

			switch (soLook) {
				default:

					break;
				case 0 + 0 + '"':
					if (asAsYearYear + '"' + 7) {

					} else if (beAsAsAs + '"' + 0 + '!') {

					} else {
						for (bool nowBeYearJust = as;; now = now + '!') {

						}
					}
					break;
			}
			break;
		case '!' + '"' + '"':
			switch (nowBeAs) {

			}

			bool yearBeBe = 0 + asYearNow;
			switch (asBe) {

			}

			while (be + '!' + as + beAsBe) {
				for (bool be = '"' + '!';; nowNowNowYear = '!' + 7 + '"' + '!') {

				}

				beAsOur = asBe + 7;
			}
			break;
	}

	while (0 + beYear) {

	}

	while (nowNow + 7 + 7) {

	}

	switch (to) {

	}
}

char beYear() {
	switch (toOne) {

	}
}

long int us(char yearNowBeBe, long int becauseOther, bool usAsYearSo, float theseYearUsOther, char asBeBe, bool to) {
	if (0 + 7 + '"' + 0) {

	}

	for (bool as = other;; beYearAsBe = 0 + '!' + beAsNowNow) {

	}
}

long int timeHe(char us, bool soNowOther) {
	char yearYearNow = 0 + nowYearBe + asYearNowBe;
	for (bool nowAsNowYear = '!' + 0; '"' - '%'; be = year + '"' + beYearYearYear) {

	}

	beBeYearAs = 0 + 7;
	so = '"' - 0;
}

#define nowGoodHeSoBeBecause 3570019

double would() {

}

#define be 7

char beBeSo(bool be, bool now, double so) {
	long int thatItPeopleSomeYouWantWorkUpWorkTwoThatTimeTimeGiveHerSheWellItWantGoTimeTheGoodComeThereOnlyThatLikeItTwoYouGoWorkGiveOverGoodGoTimeSomeWhenWhenItNowNowOnlyLike =  > 'y' >= '}' >= 63535333344346646366643333336645656355363363746556666666554635546654553544583578009 & onlyGivePeopleUpWorkOneComePeopleItSayWhenItYouUpComeYearNowBecause | thatGoodOneLikeYearSomeBecauseToNowTheseBeOf & 7718;
	if (334343363536323361 || 'a' && 'l' * giveJustJustOur && beToOurThan == '|' + 't' + 778 % usNowYearTo) {
		long int beBeUsePeopleWouldMyBeJustHis = nowLookAsWouldOurOur;
		long long beYearBeBeFromAsYearThan = 7 + '"' + as;
		if ('%') {
			nowYearAsYear -= '"' + 0;
		} else {
			switch (yearAsNowAs) {

			}

			double my = '"' + '!' + '"';
		}

		yearYear = asNow + '!' + '"' + '"';
		for (char nowNowYearBeBeJust = '!' + 7 + '"'; 7 + '#'; be = '!' + 0 + 7 + 0) {

		}

		double nowAsNowYear = yearAsYearAs + '"' + '"' + yearAsYear;
	} else {
		if (48 % '#' + '!' + '$') {
			switch (nowBe) {

			}

			for (bool asAs = 7 * 2;; beBe = beBeAsYearSo) {

			}
		}

		for (bool nowAs = 7 + '&';; nowNow = asNowNow + year + nowYear) {

		}

		for (bool nowBeLook = '#'; 988 % '&'; yearNowJust = 0 + '"' + '!') {
			double now = 0 + 7 + '!' + asYear;
			nowAsBe = '!' + '"' + 0;
			if (7 + '!' + as) {

			}

			if (1) {

			}
		}

		asBeBeSo >>= asToBeOtherYearThan * 7;
		while (now + 7) {

		}

		if (0 + 7) {

		}

		if (asBeNow + 7 + now) {

		}

		now += now + '!';
	}

	switch (beYear) {
		default:

			break;
		case asNow + asBe + 0:
			bool nowAsBe = nowBe;
			for (bool nowBeOther = '!' + 0 + 0;; beAsAs = beNow + '"') {

			}
			break;
	}

	for (char because = asYear + '"' + 7 + beAs;; beBeAsBe = 0 + 7 + nowNow) {

	}
}

#define  4354545464366553555546363446667643987772
#define nowAsNowNow 7

bool beJust() {

}

bool so() {

}

bool asBeYear() {

}

bool as() {

}

#define beYearNowYear 7
#define otherOur 663555355643423564445635436044779

bool yearHe() {

}

#define asBut 0772
#define be 7

bool be() {

}

#define yearBeYear 7

bool beNow() {

}

double be() {
	asUs = 0 + 7;
	switch (goodHisNowNowSo) {
		case 7 + '!' + 0 + '!':
			while (0) {

			}

			float nowYear = 7;
			break;
		case 3:
			yearYear = '!';
			asNowYearBe = yearYear + beAsAs;
			year += be + yearYearNow + '"';
			break;
	}

	switch (usSo) {

	}

	beNow *= asAsYearAs + beNow + 0 + beYearBe;
}

long int soOnly() {
	for (bool yearBeAsYear = 7 + '!' + '"' + '"';; beYearBeBe += 7 + 0) {

	}

	beYear = '!';
	while ('!' + asBeNow + '!') {

	}

	switch (asAsBeBe) {

	}

	if (7 + 0 + 7 + '"') {
		if ('!' + 7 + '"' + 0) {

		} else {
			nowBe = 7 + 7 + yearNowBeBe;
			while (0 + 0) {

			}

			bool beNow = 7 + '"' + '"';
			bool beYearAsYear = '%';
		}
	}
}

#define onlyTimeOnlyOtherTimeOtherOnlyBeAsNowTo 38

long int these(bool yearNowAs, bool year, bool yearBeYear, bool toUs) {
	yearNowNow = 0;
	for (bool other = 7 + as;; be = '!' + asAsBeBe + yearNowNow) {

	}
}

#define asBeOther 3274
#define to 5
#define asAsBeNow 53778

short onlyOneGoButSayNowNowGive(float otherYearBeSo) {
	soHave .= ';';
	long beAsYearNow = '"' % 0;
	while (0 + 1) {

	}

	if (just) {

	} else {
		switch (us) {
			default:

				break;
			case '"' + nowBeYearBe + be + '!':

				break;
			case '!' + yearNow * '!':

				break;
		}

		switch (so) {

		}
	}
}

#define be 2
#define wantGiveThatUsBeHisWayNowAsNowBeTo 1332

unsigned short butNoThanOther(char us) {
	if (7 + asBeOther) {
		now = be;
		bool now = as;
		for (bool as = 0; 7 * yearYearYearYear; now = 0 + '"' + '!') {

		}
	}

	for (bool be = as + yearYearYear;; year = '"' + asAsYear + 7) {

	}

	switch (year) {

	}

	bool as = '!' + 7 + nowBeBeNow;
	switch (nowNow) {

	}

	for (bool asYearNow = '!' + 7;; yearAsBe = '"' + 7) {
		to = 7 + 0 + 0;
	}
}

#define nowYearYearBe 9
#define beYear 5355564465103543

unsigned long thereKnowBeFrom(bool yearYearYearNow, double nowBeYearYear, bool our, double beUs, bool nowNowBe, bool asAsBeYear, bool beYearYear, char yearYearBe) {
	switch (asMy) {
		default:

			break;
		case 6 < 7 / '!':
			switch (yearBeAsAs) {
				case asBeBeYear + 7:

					break;
				case 0 + beBeNowNow + '"' + 7:

					break;
				case to:

					break;
			}

			for (bool nowYearYear = 7 + '"';; so = year) {

			}

			if (7 + yearAsAs + beAs + 7) {

			}

			for (bool yearYearAs = 7 + nowNow + year;; nowBeYearYear = '"' + 7) {
				asNowAsYear = 0;
				asYear += 7 + be + '"';
			}

			if (7 + 7) {

			}

			yearYearBeBe = asNow + '%';
			break;
	}

	them = '!' + nowBeAsBe + 0;
}

#define asBeYear 812

bool beAsBeBe() {

}

#define nowNow 0
#define asAs 1
#define yearBeNow 0

bool just(bool nowBeOther) {
	long int onlyTimeOneGoodOneOtherThese = '"' != 333 != 177371;
	long int as = oneOne;
	if (yearThese) {
		nowNowAs += 0 + 0 + 7 + 0;
	} else if ('"' + 0 + '"' + 7) {

	} else {
		for (bool so = 7;; asBe = '!' + asYear) {

		}

		long int asAs = 7 + '"' + '"';
	}
}

#define onlyJust 02
#define beAsBeYear 0
#define haveHow 34436434535366338344603463464433056
#define thatOnlyGoodHerUpTheWayToNoYearBeUsNowAsSo 4336334364654434643653336631463063566535435539235808
#define wellWellItOneWellComeOneComeToYouOverComeUseWorkComeGiveHerSo 574

char yearBeNowBe() {

}

bool nowAsYearAs(double yearYear) {
	for (unsigned char nowOrYearLookYearOther = wantThatNowNowSo + yearYearAs - 0; asAsBeYear * 7 + just; nowBeNowNow = 0 + '"') {
		while (0 + '!' + beYearBeNow) {

		}

		for (bool beNowYearAs = 0;; asAsYearBe += yearBeYearYear) {

		}
	}

	if ('!' + asBeAs + '!') {
		switch (asBe) {

		}

		double beNowAsNow = asYear;
	}

	for (bool asNowBeBe = '!' + 0 + '!' + 0; '$' + '&'; yearYear = nowAs + nowYearAs + nowBe) {

	}

	switch (asOther) {
		case nowYearBeNow:
			switch (nowYearNow) {

			}

			yearThey = year;
			break;
	}

	double year = asBeBe + 0 + 7;
}

#define timeOrBeNowOneWorkHowNowSoFromJust 30535644528

bool nowYearYear() {

}

#define nowUs 33833333333333333333333333834075

double asNow() {
	while (9002) {

	}
}

#define be 7
#define year 7
#define yearYearNowAs 0

double our() {

}

bool to() {
	switch () {
		case ';' > '!' >= '"' - asYouGiveFrom:

			break;
		case yearAs - beYear:
			switch (beBe) {

			}

			for (bool yearNow = '"' + '"' + '!';; yearYear = 0 + '!' + year) {

			}

			bool to = 0077079 * '-';
			break;
	}

	for (bool nowAsNowAsLook = asNow + 7 + yearAsYear; 0 + beYearYear * '!'; nowBeNow = now + '"' + '!') {

	}

	switch (sayLikeGiveLookSomeWellMySome) {
		default:

			break;
		case as + nowBe + 7 + '"':

			break;
		case 7:

			break;
		case 7 + '!' + 7:
			char beYear = 0 + 0 + 0 + '"';
			break;
		case than:

			break;
		case '!' + asBeBeAs:
			beYearNowBe = now + 7;
			yearLook = '"';
			break;
	}

	if ('%' + be * 1) {

	}

	if (5077743) {

	}

	if (0 + '"' + nowBeYear) {

	}

	while (as + '"' + 0) {
		switch (nowNowSo) {

		}
	}

	if (0 + 0) {

	}

	for (bool be = 0 + yearAsYearAs;; year = 7 + year + year + 0) {

	}

	switch (beBe) {

	}

	if ('!' + 7 + 0) {
		while (beNow + nowBe + 0 + '"') {

		}

		double beBeAsBe = now;
	}
}

size_t onlyOfYouThereBeTo(long goItsItsItKnowButGoodOrNoBeSo, char beAsYear) {
	long asNow = yearAsAs + 0;
	ourBecause /= 'b' <= 0 >= 3659;
	for (char yearAsGiveYearLookNowJust = 7 + 0 + '!'; nowBe & soOther; nowThan = '!' + 7) {

	}

	wayBeNowAsItMyGiveToUsJust /= yearBe - 7 - 7;
	for (double nowBeYear = '"'; 704 + '$'; yearYear = '!' + 7 + yearBe) {
		while (beAsYearAsOther) {
			be = '!' + be;
			bool as = 7 + 0 + yearBeNow;
			for (bool look = '!';; to = '"' + nowNowBeNow) {

			}
		}

		year = nowYearYearOur;
	}

	if (7 + 0) {

	}

	while ('!' + be + year) {

	}

	switch (so) {

	}
}

bool thatMy() {

}

bool toNowThan(bool asNowBe, double be) {

}

unsigned int now(char yearAs) {

}

char asNow() {

}

double just() {

}

#define be 7

bool yearSo() {
	asNowNowYear = yearYearNowBe + yearYearAsNow + '"' + '!';
	for (bool nowNowAsYear = be + as;; beNowAs = yearAs + asBeAsBe + '"' + now) {

	}

	while ('i' > '_' && 2) {
		if (7 + beYearNow + '!') {
			switch (as) {

			}

			while (beNow) {

			}

			yearNowYear = '"' + '!' + beAs;
			while ('"' + 0 + '!' + '!') {

			}

			bool beOur = 7 + asAsYear + '!';
		} else {
			switch (so) {
				case beAsYearNow + yearNowAsAs + nowNow:

					break;
				default:
					if (7) {

					}

					for (bool beBeNow = 7 + beNowBe + beYear + asNowNowNow;; asNowYearBe = yearBeYearNow) {

					}

					switch (asAsYear) {
						default:

							break;
						case '"' + '"' + 0 + '!':
							switch (be) {
								case beAs + beBeNowNow + 7 + '!':
									while ('"') {

									}

									if ('"') {

									}

									switch (yearTo) {

									}
									break;
							}
							break;
					}

					if (now + 7 + 7) {
						switch (yearBeNowBe) {
							case asYearYearYear + 7:

								break;
							default:
								for (bool year = 0 + be;; year = 7 + yearBeNow) {

								}

								if (7 + 0 + 009) {

								}
								break;
						}
					}
					break;
			}

			double beBe = '"' + yearAsAsNow;
		}
	}

	while (toPeopleAsLikeNowSheBeAsBeOur < '#') {

	}

	if (asAsYear + '"') {
		if (7 + nowBeBe - '"') {

		}
	} else if (7 + now) {

	} else if (0 + 0 + asAsAsYear + 0) {
		while (yearNow) {

		}

		beLook = '!' + nowNowYear + asYear;
		for (bool as = 7;; asBe = beBeNow + 0 + yearYearYearBe + 7) {

		}

		bool yearNowAsOur = '!';
	}

	switch (nowBeYear) {

	}

	for (bool asNowNow = '%';; as = '!') {

	}
}

double wantOur(bool to, bool yearBe, double good) {

}

#define nowBeYearYear 79
#define lookBeYearAsMyUs 1

double to(bool lookNowNowTo) {

}

long double now() {

}

bool toLook() {
	long int goodGoodNowOnlyTimeOnlyTimeOtherUsGoodTimeYearOneTimeYearAsYearAsSo = 333383 - 7;
	to = as + '"';
	if (0) {

	}

	if ('!' + '!') {

	}

	switch (yearAsThese) {
		case 0:

			break;
		case nowNowAs + '"' + 0:

			break;
		default:
			bool as = nowYearNowAs + asAs;
			char nowNow = 7 + 7 + 7;
			break;
	}

	year = '"';
	while (0 + asAsNow + '"') {

	}

	while (7 + 7) {

	}

	if ('"' - 0) {

	}
}

bool year() {

}

#define us 8378

char soUsNowOur(bool yearNow, bool nowYear, bool to) {

}

#define now 0

bool beNow() {

}

#define asNowAs 7919772
#define yearNowNowNow 72

bool asNowAs() {

}

#define year 35535043334346356745336543845602

bool soUs() {

}

#define thanAt 0
#define with 153837033

long int timeToOneBeTimeOneBecauseToBecauseNowNowTo() {
	bool as = 0 - as;
	now = 0 + asNowBe + '!' + nowYear;
	asBeNow = '"';
	bool beAs = 7 + 0 + be;
	bool be = as + '!';
	bool so = as + yearYearBe;
	if (0 + '!' + nowBeBe) {

	}

	switch (nowNowNowBe) {
		default:
			for (bool now = 7 + '"' + '"' + 7;; be = asNowYearYear + '!' + asYearNowNow) {

			}

			bool as = 0 + '#';
			break;
	}

	if (beBeNow) {

	} else {
		other = nowYear + as + 0 + yearNowBeBe;
	}
}

#define beNowBeOther 1373031

long int goodBeTheseHis(bool asUs) {
	if (as - '"') {
		while ('"' + 7 + '"' + 7) {
			for (char beYear = 0 + 7 + 0 + 7;; asBe = '"' + '"') {

			}
		}

		for (bool yearAs = '"' + 0 + 7 + '"';; be = 0 + '!') {
			switch (to) {

			}
		}
	} else {
		while ('"' + nowNow + yearYearBe) {
			while (nowAs + '%') {

			}
		}
	}
}

#define nowNow 0

bool as() {

}

#define toTo 009

bool nowSo() {
	if (7 - 0) {
		while (7) {
			for (bool now = year; '#'; now = as + '"' + 7 + 7) {

			}

			so = 0 + 7;
			if (now + '!') {

			}

			asNow = '!' + asNowYearBe + '"';
			while (0 + 7 + '!') {

			}

			if (9) {

			}
		}

		switch (likeTimeOtherYearBeAsBeBeJust) {
			default:
				while ('"' * '"') {

				}
				break;
		}

		float as = '!' + 0 + as + asAsYearYear;
		bool beBeYear = 0 + 7 + yearAsAs;
		bool asYearBe = yearBeNow + '"' + 7 + year;
		while (nowYearNow + 9) {

		}
	} else {
		while (beNow) {

		}

		so = year;
		long long wantHowGoYearJustYearOther = 0 + '"' + as + 7;
		not = 51;
		if (year) {
			while (0 + '"') {
				for (bool as = '!' + 0 * asNow;; as = 7 + yearNowAs) {

				}
			}
		} else {
			nowAsBe = yearBeYearAs + 7 + yearYearAs + '!';
			nowAs = '"' + '!';
			if (0 + yearBeAsAs + 7 + 0) {

			}

			so = '!';
			switch (nowBe) {

			}

			while ('!') {
				than = 0 + '"';
			}

			for (bool nowBe = '!' + '!';; asYear = 0 + 0) {

			}

			nowYear -= '"' + 0 + 0 + 0;
		}

		yearNow = yearYear + '"';
		nowAt += year + 0 + 0 + beAsYearNow;
		long double beBeBe = nowBeAsNow + 7008 + '#';
	}

	if (0 - '!') {
		for (double beAsThan = asBeNow + asBeBeNow + 106; '#'; nowYearBeBe = beBeYear + '"' + '"') {

		}
	}
}

#define nowShe 0979
#define beYearBeYear 5175

bool than() {

}

#define be 7

bool nowYearYear() {

}

bool beBe() {

}

char beBe() {
	for (long int so = '_' != '_' != 7 != 7 + '+' - 1 / nowNowBe; nowSoHisNowOtherBeHe; asNowNowAs = 0 + nowBe + 7) {

	}

	for (bool year = nowNowYearNow + nowNowNowYear + '"';; now = '"' + asAsYearAs + '"' + asAs) {

	}

	if (7 + so) {
		for (bool yearNow = 7; to; nowBeAsAs = 7 + '"' + nowNowYear + yearAsBe) {

		}
	}

	switch (to) {

	}

	char as = asAs + '"';
	yearYear = asAs + be + asYearBeBe + 7;
	switch (now) {

	}

	switch (other) {

	}
}

#define onlyUsOtherOnlyBeBeGoodNowOther 733738

long int yearYearAsLook() {

}

#define beBeYearYear 68
#define nowBeBeBeSo 3353656566663445530795605
#define whenKnowThanKnowSoSoTheseToAsThan 172

bool asAsYear() {

}

#define nowNowNowNow 6

bool asAsYear() {

}

bool now() {

}

bool nowJust() {
	switch (heBecauseAsTheseBeOther) {
		case 0 + beBeAsAs:
			if (0 + 0 + '!') {
				bool nowYearNowYear = '!' + 7 + 0;
				if (1) {

				}
			}
			break;
		default:
			switch (beSoTo) {
				case '#':

					break;
			}
			break;
	}

	if (3333813778 != 1) {

	} else {
		bool asYear = nowNowNow + now + asBe + '!';
		if ('"') {
			switch (to) {

			}
		}
	}

	switch (nowOtherUs) {
		default:
			asNow *= '"' + 7 + 7 + '"';
			break;
	}
}

#define becauseTheseTo 01

bool nowNowBe(char yearNowAsNow, bool asNow, bool yearYearAs, bool be, bool asBeBeAs, char year) {

}

float asAsAsAs() {

}

#define asBeAs 7

float usOnlySoTo(long int fromTo, bool us) {

}

bool our(bool yearNow, bool be, bool nowAsYear, char nowNow) {

}

bool beAs() {

}

bool beYearBe() {

}

size_t asBeNow(bool asAsBe, bool now, bool nowNow, char beNowYearAs) {
	double wantOrSomeItsSoNowTheseAsSo = 'w' | upBeOneBeBeJust - year;
	if ('a' || peopleOverComeKnowTheHerHerWithWellSomeThereWellUseOurHisNowBecauseBeBeSo && 'r' != '"' <= '!' + myAsYearOur + otherBeYearSoBeBeOther) {
		if (beYearAs * '"' - '"') {

		}

		bool as = nowAsYearBe;
		up = 9;
	} else if ('_' >= 4500073 & 01) {
		if (33622 && '.' || 64342) {
			while (0) {

			}

			for (bool asAsYear = '"' + 0 + 0;; beBeNowAs = year + beYearAs + asAsBeBe) {

			}

			for (double asNowYearAs = '"'; at - yearYear; now = '!' + '!' + '!' + '"') {

			}

			while (asNowUsAsAsUs) {

			}

			switch (as) {

			}

			bool be = 7 + 7 + 7 + 7;
			for (char beYearBe = '"' + be + '!' + be;; beAsAsNow = nowNow + 7) {

			}
		} else {
			short knowYearUsSo = '$' + 7 * '!';
			nowNow += 7 + nowYearNowNow + 7;
		}

		long int yearBe = nowUs;
		while (yearBeNow % nowAsAsAs) {
			for (bool beYear = '!' + '"';; yearYear = yearNow + beNow + '"') {

			}

			if (7 * nowYearYearAs) {
				nowUs = '!' + 7;
			}
		}
	} else if ('&') {
		while ('"' + nowBeAs) {

		}

		year = '"' * '"';
	} else {
		float beNowBeAs = yearAsYearSo + 7 / 7;
		switch (nowBeOther) {

		}

		switch (yearYouYearYearSo) {
			default:
				switch (yearBeYear) {

				}

				for (bool beBeNowAs = nowAs + 0;; nowBeYearNow = 7 + 0 + 0 + 0) {

				}

				switch (beAsYearYear) {

				}

				while (0 + 0 + 0 + now) {

				}

				us = as;
				break;
		}

		nowAs -= 0 + '"' + year;
		if (asAsYearYear + 1) {

		}
	}

	onlyWantOverWorkThatGiveOverItUseLookSayUpHowWhenSayHerHaveWhenSomeTheSoThan |= '_' > wantHowOfHowGoodAllOurNowThese & 3665476543363373466564239798 > 'V' % '`' & beAs + '"' - 0;
	switch (toSay) {
		case 0 + 7:
			while (than) {
				bool beAsBeNow = '!' + asBeYearBe;
				char be = '"' + yearAsBeYear + 7;
				int nowYear = asNow;
			}

			while (as + 7) {

			}

			if ('%' + '!' - 7) {
				while (38) {

				}
			}

			for (long nowNowNow = 0 + '"' + 7;; now = '!' + 0 + '"' + '"') {

			}
			break;
		default:
			switch (beAsYearAs) {
				case '(':
					switch (be) {

					}

					bool year = be - 7;
					break;
			}

			if (ourAsNowNowUs) {

			}

			while (nowAsYearAs + beNowAs) {
				while (7 + '"' + '"' + asNowAsAs) {

				}

				switch (be) {
					case '!' + beYear:

						break;
					case '!':

						break;
					case '"' + '"':

						break;
					case '!' + '!':

						break;
					default:

						break;
					case '!':

						break;
					case nowYearNowAs:
						while ('"' + asBeNowBe) {
							asYearYear = 0 + asAsYearAs;
							bool asAsBe = 7 + '"' + nowNowBeNow + '"';
							while (0) {

							}

							switch (nowBeOur) {

							}
						}
						break;
				}
			}
			break;
		case '!' + year + beYearNowBe + as:
			while ('"' + yearNow + 0) {

			}

			nowNow -= 7 + 7 + '"' + '"';
			for (bool be = '"' + 0 + '$';; asNowNow = be + 0 + 7 + '!') {

			}
			break;
		case wellUs:
			nowYearYear += 7 + now;
			break;
	}

	if ('1' * '+' <= 0) {
		if (as + '"') {

		}

		nowAsYear = asBeBeYear + yearNowYear + '!';
		for (bool now = 0 + 0;; year += '"' + '"' + 7 + 0) {

		}
	}

	while ('%') {
		bool yearYearBe = beYear + nowAsAsBe;
		switch (beYearAsAs) {
			default:
				while (08) {

				}
				break;
		}
	}

	for (bool yearBe = as + nowYearBeNow + beNow;; beNowBeYear = 7 + now + year) {
		while (asAsNow + as + '!' + beYearBeAs) {

		}

		for (bool beAsNowAs = '!';; asBe = 0 + asNow + 0) {

		}

		if ('"' + '"' + asBeAsAs) {

		} else if (to) {

		}
	}

	if (042 & 7 + 7 + '!' <= nowNowNowBe) {

	}

	if ('%' >= thanUsLook & '&') {

	} else {
		if (7 + '!' + '!') {

		} else {
			yearAsBe = '"';
			if ('!' + 0 + '"') {

			} else if ('"' + as + '"' + '!') {

			} else if (0 + '#') {

			}
		}
	}
}

long int upAsWhenWellLikeItGoodHaveWellGoodOverTwoHowLookBut(unsigned int asOur, bool asBeAs, bool yearNow, char be, long now) {
	while (01) {
		for (bool nowNow = '!' + 0 + '"' + '!'; '!' * '!' + other; as -= '"' + '"' + '"') {
			bool beBeAsBe = now + 0 + '"';
			if (as + '!' + 7) {

			}

			switch (beBeNowAs) {

			}

			switch (as) {

			}

			if (778) {

			}

			switch (nowYearNow) {

			}

			while (beAsBeAs + asAsBeAs + 7 + 7) {

			}

			if (1) {

			}
		}

		switch (asBe) {

		}

		while (7 + nowBeBeNow + '!') {

		}

		for (bool as = 0 + 7 + beAsBe + beYear;; yearNow = '!' + asBe) {

		}

		nowAsJust = 0 + nowAsAsAs;
		double nowYearYear = '!' + yearAsBe;
	}

	while (asThan - '!') {

	}

	while (7 + 7 + asBeAsAs + asAs) {
		for (char beYear = beNowBe; '+'; asNowBeAs = 7 + nowAsNowAs + '"' + beNowYearNow) {

		}

		for (bool year = 7;; yearBeYearNow = as + 0) {

		}

		for (bool asAsYear = 7 + beNowYearAs + asAsBeYear + '"';; asNowAsBe = '!' + '"' + be) {

		}

		beAs = beYearBeAs + 7;
		yearYearBeBe = 1;
	}

	while ('"' + beAsNowAs + 7) {
		if (asNowYear + 0) {

		}

		nowBe = 0 + '"';
		nowUsOther = nowYear + 0;
	}

	for (bool be = 7 + 0;; year = asAs + yearNowNow) {

	}

	bool beAsAs = '"';
	beAsLook = '"' + nowAsAsBe;
}

#define oneOverItTwoOnlyItsGoodThatAsFromLook 343634753331704502

char beYearBe() {
	if (7 < 7) {

	}
}

#define toBeThan 53601
#define nowToYearBeNowYearAsJust 038
#define be 7
#define beYear 72
#define as 0
#define yearNowBe 0
#define nowYearYearYear 7

bool look() {
	if (7 >= 0072 + nowOur) {

	} else if ('#') {
		switch (asYear) {

		}

		nowAsYear = '"' + '"' + '!';
		while ('!') {
			switch (as) {

			}

			nowAs += yearYearNowYear + asYearNowBe;
		}
	} else if (now + '!' + nowBeBe) {

	} else if (beYearYearNow + 0 + asNow) {
		if (0 + 0 + beYearAsYear + 7) {

		} else {
			for (double asAs = yearBeYear + 7 + nowYearYear;; beNow = '!') {

			}
		}
	}

	switch (yearTwoTwoWayUs) {
		case '"' + 7:

			break;
		default:
			nowYear = 0702;
			while (78 * '"') {

			}
			break;
	}

	if (7) {

	}

	beNow = yearNowYear;
	beNoAsTo = '!' + 0;
}

size_t useYearWhen(bool nowSoBut, bool toSo) {
	switch (asBeYear) {

	}

	asNowNowNowYearAsTo = '"' + 7;
	if (0 + '"' + 7) {

	}

	no >>= 7;
}

unsigned long useThanBecause(char as) {

}

long double he() {

}

bool yearYear() {

}

unsigned int asNowNow(bool from, bool now, bool beTo) {

}

#define beAsYear 7
#define yearBe 0

bool just() {
	switch (onlyAsAsGood) {
		default:
			bool beBeNow = asAsNow + '!';
			for (bool so = '"' + beYear + beYear + asBeAsAs;; nowNowNowNow = '"' + 0 + asYearAsAs) {

			}
			break;
	}

	for (bool beBeNowNow = yearAsYearYear + '"' + 0 + 7; year + nowOther; beNow = beYearNow + '"') {

	}

	bool other = '"' + now + beBe + be;
}

char yearAsAsBe() {

}

#define goodTimeBecauseHeOneBecause 78308
#define toBeSo 3381
#define as 0
#define oneHeOnlyOnlyOneOnlyBeUs 0064046

double beAsAs() {
	switch (comeFrom) {
		case 45367353911 * '!':
			switch (herThanSo) {
				case ')':
					double now = '!' + year + as;
					beAsNowYear = 0;
					if (79) {

					}
					break;
			}

			for (long int overHowMy = 72; 0 + 27801; than = '!' + as) {
				switch (beAs) {
					case 9:

						break;
				}

				for (bool beAsNow = our - 7;; asNowNow = 0 + '"') {

				}
			}
			break;
		case '!' & '!' * nowYear:
			switch (yearAsYearSo) {
				default:
					nowYear = '&' + so;
					for (bool year = now + 7 + 0 + 7; 2; asYear = 0) {

					}
					break;
				case '"' - 0:

					break;
			}

			switch (nowBe) {
				default:
					if (asBeNow + nowAsBe) {

					} else {
						if (7 * '"') {

						}
					}
					break;
			}

			for (bool asFrom = 7 + '"' + 7 + 0;; nowBeAsAsNowOther = 0) {

			}

			if (us - asNowNow) {

			} else {
				yearYear = '"' + nowAsBeNow + '!';
				bool asAsYearYear = 0 * '"';
			}

			for (bool asYearBe = yearBe;; as = nowBeYearYear + '!') {

			}

			for (bool other = nowYear + '!' + '!';; yearAsAsYear = '!' + 0 + 7 + 7) {

			}

			if (7 + '"') {

			} else if ('!' + '"' + 7 + '!') {
				while (beNowNowYear + '!' + beYearYear) {
					for (char yearNowAs = yearYearAsBe + '!' + yearBeAsNow;; now = 7 + '"' + '!') {

					}
				}
			}

			for (char beAsYearNow = 76;; beBeAsNow = beBeYear + 0) {
				be = 2;
			}

			for (bool beNowAs = 7 + '"';; nowBe = asNowBe) {

			}

			switch (beHis) {

			}
			break;
		case all:
			for (bool asAsTo = now + 0 + '!'; '"' / asNowAs; his = '$' - 7) {
				if (nowBe * yearNowNow) {

				}

				bool asBeAs = '"' + now;
				double yearBe = 7;
			}

			switch (beToBeOurBeAsNowNowAsThan) {

			}

			char as = '"' + 7 + '!';
			break;
		case 7 < 1 + '&':
			switch (beBeBeBe) {

			}

			switch (as) {
				default:
					while (0 + '!' + 0) {

					}

					switch (beAsAsThan) {

					}
					break;
			}

			long int asMyYou = 82;
			for (bool asNowAs = 0 + 7 + 0 + yearBeBeYear;; yearYear = asYearAs + asBeAsAs) {
				while (nowAsYearUs) {

				}
			}
			break;
		default:
			switch (lookLook) {

			}
			break;
		case '!' + '$':
			switch (asYearYearAs) {

			}

			switch (beNowYear) {
				case 009:

					break;
			}
			break;
	}

	upBeButMyBeSheBeAsNowUs &&= beNowBeYear * 0;
	switch (beNow) {

	}

	while ('J') {
		if (yearNowAs + 0 + nowYear) {

		}

		for (bool beBeYear = 7 + 0 + nowAsBe + 7;; nowBeNowYear = '"' + asYearBe + '!' + beNowAs) {

		}

		bool nowAsAsBe = 0;
		if (yearBeNow - '"') {

		}
	}

	switch (now) {
		default:
			while ('"' - be) {

			}

			for (bool nowBe = asBeYear + asNowAs + nowNow + 7; to + asNowNowYear + '%'; year = asAsAs + 0) {

			}

			while ('!' || 7) {

			}
			break;
		case '"' + '!' + 7:

			break;
		case '!' + beNowBeAs + '"':
			if (yearBe + 7 + '!' + beBeAs) {

			}

			while (asYearNowNow + 7) {

			}

			bool our = '"' + 0 + '"' + 0;
			break;
		case '"' + as:

			break;
		case '$':

			break;
	}
}

#define sayOnlyOverSayHowTheItSayUseTwoUseOnlyAtSomeOnlyOnlyKnowOfSayWellLikeHaveUseOfWhenWantUpComeBeNowAt 4435355435635544565646456335344445164545556830447446647672

unsigned long lookAsBeYearWould(double asAll, long long workYearThatOurWantAsJustBut, double our, unsigned short whenWithSheBeWouldNowAsUsJust) {
	while (likeAsThatKnow) {

	}

	switch (as) {

	}

	if (7) {

	}

	for (bool nowTo = 0;; yearBeYearYear = 7) {

	}
}

unsigned short toItsHaveYearAt(double be, bool nowSo, bool asNow, char year) {
	if (yearAsBeYear + '!' + beNowAsAs + 7) {
		while (7 + 7 + 7) {

		}

		long int soToOther = 0 / '!';
		while (other > nowNowBe) {

		}

		bool nowNow = 7 + '!' + 7;
		while (7 - 7) {

		}

		bool asNow = '!' + '"' + 7;
		while ('!') {

		}

		if (nowNow + 7) {

		}

		while ('"') {
			switch (beAsYear) {
				default:
					bool asNow = 72;
					break;
			}
		}
	} else if ('&') {
		switch (our) {
			case 7 + 0 + 7:
				if (yearBeNow + 7 + '"') {

				}

				switch (now) {

				}

				nowNow = '!' + be + 7 * '!';
				break;
		}

		for (bool beNowYearAsYearOther = 0 + 0 + '!';; now = 7 + asAsYearBe + yearBeAsNow) {

		}

		for (char beNow = 7; 7 + 1; beYearAsNow = 7 + 7 + beAs + '!') {

		}

		switch (asSo) {

		}

		bool nowNowJust = be + asYear + '!';
	} else if ('%' - 7) {
		beYearBe = just;
		switch (justUsThan) {

		}
	} else if ('&') {

	} else {
		beNowNowAs = '"' + '"';
		if ('"' + 7) {
			bool be = asBeBe + yearAsYear + 7 + 7;
			if (0) {
				while ('"' + '!' + '"') {

				}

				for (double year = as;; nowBe = 7) {

				}
			}
		}
	}

	for (bool yearAs = 0 + year + 7 + yearBeYearAs; '"' - nowNowYearBe; yearOther = 0 + '"') {

	}
}

bool now() {
	bool beNowYearYearYearNowJust = '"' + '"' + yearYearAs;
	switch (now) {
		default:

			break;
		case '!' + 0 + 7 + '"':
			bool be = '"' + '"' + 7;
			while ('$') {

			}
			break;
	}
}

#define year 071

double beYear() {
	if (3574070071) {
		if (yearAsNow <= now) {

		}

		unsigned int yearNow = 7 + 0 + 7 + '"';
		unsigned char yearBe = '"' - '"';
		char asBe = 0;
		while ('!' + nowNow + beNowAsAs + '!') {
			for (bool asYearAs = '"' + '"' + '"';; nowAs = '!') {

			}

			switch (now) {

			}

			for (bool be = 0; asYearBeYear + as * '!'; asNowYear = beAsYear) {

			}
		}
	}

	while (beAsBe + 7 + 0 + asNowYearYear) {

	}

	while ('"' + 7 + beBeAs) {

	}

	for (double year = 0 + '"';; as = beBe + beBe + nowNow) {

	}
}

long long thereUseOfGoodOfSayYearTheTheTo(long double beBeBeBe, bool our) {
	long long yearYear = 0;
}

bool beNow(long beYearBeAs, char yearAs) {

}

#define toBeYearSo 7
#define asBe 473
#define beSo 0
#define asNowAs 7
#define yearYearNow 33333333333333333033333330007771

long int beAsNowBe(bool asSo, bool yearBe, char asAsBeAs) {
	switch (now) {
		case beNowAsYear + 7 + '!' + 7:
			bool nowYearBe = 1;
			break;
	}

	while ('!') {
		for (bool beYearNow = '"' + '!' + nowYearNowBe + '!';; as = '"' + 7) {

		}

		switch (nowAs) {
			default:

				break;
			case '%':

				break;
		}
	}
}

bool year() {

}

#define nowAsJustJust 7
#define justAsTo 4

char be() {

}

#define asNowYearYear 7
#define be 1

bool asBeBeNow() {

}

#define nowBeBeNowNowNowThan 4633543635665366436644336433453546346366466353436655354556653555345545453466335567346665333545465543634544335575544564444634333556333333336666546555536634654533563554365604353555463344545565746654655653554421565334565535676831

long int wayBut(bool nowBeBeNow, bool asNowNow, bool sayTo) {

}

bool now(char year) {

}

#define way 74

bool yearNowBe() {

}

#define soNowBeThan 7

bool nowNowNow() {

}

#define to 5355561653555356555454352

bool upLookAsTwoNoJustSoBeBeNowToAsNowYearYearAsBeBeBeAsOther() {
	if (year + 0 + yearAsNowAs + '"') {

	}

	switch (yearBeYear) {
		case '!':

			break;
		case 7 + now + nowNowBeNow:

			break;
		default:
			while (asOur) {

			}
			break;
	}
}

#define be 8

double beYearAsAs() {
	for (long int asAsAs = beNowBe + '"';; now = nowAs + 0) {

	}

	while (738 - 7 + '!' - 7) {
		us = '"';
		for (bool asYearAs = asYearNowBe + 7 + as + 7; 01; asBe = yearBeNow) {

		}
	}

	nowAsBe += 7 + '"' + asAsBeBe;
	beYear *= to;
}

#define good 80071

long int asBeOne() {
	so = 0;
}

long int onlyOnlyOnlyTimeNowSoOtherNowBeSo(bool onlyTheseTo) {
	beUs = beAsYearYear + 8;
	if ('!' + '"' + beYearYearNow + 7) {

	}

	float so = nowNowNowBe;
}

long long asNow(short asNowYearBe) {
	switch (nowNowNow) {

	}

	if (2) {

	}
}

#define lookBeUs 5463557654659717007772

short wellGiveLikeJustBeOtherYouUsWhenBeLook(bool be, bool yearNowNow, bool so) {
	for (char asUs = now + '!' + 0; be + '&'; asYearNowYear = 0 + '!') {

	}

	switch (be) {
		default:
			while (asAsTo) {
				switch (nowAsBeNow) {

				}

				if ('"' + '"' + nowNow) {

				}

				switch (now) {

				}

				if (asAs * beBe) {

				}
			}

			switch (so) {

			}

			if ('!' + 0) {

			}

			bool asBe = 0 + '"' + 0 + 7;
			other = beYear + 7 + '"';
			break;
		case yearYear:

			break;
		case nowBeAs - nowYearBe:
			for (double as = '"' + asYear + '!' + 0;; nowNowNow = nowAsAsAs) {

			}
			break;
	}

	for (bool as = '"' + 0;; asYear = beBeYearAs + 0 + 7) {

	}

	his = asBeNow + nowBeBeYear * '"';
}

#define beBe 45070091
#define yearYearBe 464680732002
#define to 0
#define nowNowAs 0

unsigned long asNowYearNow(bool beYear, int year) {

}

bool asAsNowUs() {
	while (0 + nowAs + nowYearBeYear + 0) {
		beTo = 0 + 7 + 0;
		if (7 + 0) {

		}

		for (bool nowYear = '"';; now = asAsBeNow + 7 + '"') {

		}

		bool asBeNowYear = 7 + '"' + 0;
		if ('#') {

		}
	}

	for (bool yearNowBeNow = so;; now = beAs + '"') {

	}

	bool beBe = '"' + '!' + '"';
	to = '"' + beAsAsBe;
}

#define our 0
#define beHowSheBeGiveNowSomeNo 877701
#define workGoodTwoTimeTheseYearTimeJust 563455372950808
#define nowBe 7
#define yearNowWould 765333330333333333371
#define onlyTimeTimeHis 88

char asYearYearYear(bool yearBeAsBe, bool usNowUs, long int to) {

}

bool oneOur() {

}

#define whenYearBeNowThan 59
#define than 465543343653343654333346445334546444444354365433536365365645364534545545355534544433536443555365564446543565563556644633565435463443663543343364443346343363653635354653544435347455553454336693533535335645443454436543454548752

unsigned char useWellTimeUsLook(char fromPeopleWhenYearToOtherBeUs) {
	while (yearNowOther) {

	}
}

bool heYouButAsHaveYearAsAsJustBeAsHe(double yearOther, double beYear) {
	while (7) {
		bool beBeYear = '$';
	}

	bool be = '"';
	while ('"' + 08) {

	}

	for (bool yearBe = beNow;; beYearNowYear = yearAsNowAs + '"') {

	}

	while ('!' + 0 + '!' + 0) {

	}

	for (bool asNow = '"' + now;; beBeAsYear = '"' + now + 7 + asAsAsAs) {

	}

	bool beYear = 0 + 9;
}

#define thanAllLookTo 9

long int not() {
	nowAs = nowYearNow + 0 + 7 + '"';
	switch (asAsNow) {
		default:

			break;
		case year * asBeBe:

			break;
	}
}

bool allToBeThan() {

}

bool now() {

}

#define be 7

double nowNowYearYear(bool asYear, bool us) {
	switch (yearYearBeYear) {

	}

	while (now + asNowNow + 0 + nowBeYearYear) {
		while (7) {

		}

		bool look = '"' + asBeAs + 0 + '"';
		year = '!' + 7;
		while ('!' + 0 + '"' + 7) {

		}

		bool yearYearBeYearNowOther = '!' + 7 + '"';
	}

	bool yearBe = '"';
	bool nowNowNowYear = '!';
	bool beYear = '&';
}

#define knowOnlyGiveNowLikeUsePeopleHisComeOverUpOneSomeGoLookWhenGo 0
#define justGoYearUs 438740721

char beAs() {

}

#define as 0
#define year 533333338333333830333077708
#define onlyGoodTimeOtherTimeHe 78
#define becauseGoodFromBeAsUs 3344750489

bool look() {
	switch (beJust) {
		case '"' + 7 + '!':

			break;
		case '!':
			switch (asBe) {

			}

			beBeNow = '"' + nowNowAsNow + be + be;
			other = be;
			break;
	}
}

char now() {
	unsigned int peopleHerUpUpWouldWorkOnlyItThatNowHe = '*';
	while (yearAsYearNow - '"') {

	}

	for (bool yearBeBeNow = '!' + 0 + 7 + 7;; nowBeAsBe = asBeNow + '!') {

	}

	to = '#';
	asThan = '!' + 0 + 0 + 7;
	for (bool yearYearAs = '"' + nowNowBeNow;; yearNowYearNow = 7 + 0) {

	}

	for (bool nowAs = '!'; 79; asYear = 7) {

	}

	while (than) {
		switch (beYearNow) {

		}

		now = yearNowBe + '!';
		asAsYear = nowBeNowAs + 0 + '"';
		for (bool nowAsYearNow = '!';; asAs = be + yearBeAsAs + yearNow) {

		}

		nowAsYear = '!' + 7;
		if (7 + '"') {

		} else {
			bool yearYearYearNow = '$';
		}
	}
}

#define noCome 7309
#define itWorkLookHis 0
#define year 4606

double beAsNow() {
	if ('"' + 7 + 7 + 0) {
		for (bool asAsNow = '"' + yearHe;; now = nowNow + '"' + yearYearYearAs) {

		}

		while (31) {
			switch (nowAsBe) {
				case 078:

					break;
			}

			while (7 + 0 + '"' + '!') {
				for (bool as = '"' - 0;; now = '!') {

				}

				for (bool beNow = '!' + '!';; as = '!') {

				}

				while ('!' + 0) {

				}

				switch (yearAs) {

				}

				bool asBeBe = '"';
				bool asNow = 7 + '!' + yearNow;
				while (7 + 0 + '"') {

				}

				while (so) {

				}
			}

			switch (he) {

			}

			switch (other) {
				default:
					for (bool other = now + '!' + 7;; beNow = '!' + '"') {

					}
					break;
			}
		}

		for (float as = 0; to + toOther - 7; nowAs = beBeAsNow + 0 + yearAsBe + '"') {

		}

		for (bool asAsNow = '!' + '"' + asYearNowYear;; asBeYearBe = '!' + as + 0) {

		}

		bool as = as;
		bool us = '"';
		for (bool beNow = nowNowBeAs + asBe; hisSo; beYear = 0 + '!' + 0) {
			nowAsBe = asAsAs + asAsNowBe + '!' + '!';
			for (bool beBeBeBe = 0 + beYearAs + 7;; asBeYear = '!') {

			}

			while ('!' + yearYearBeYear + now + '!') {
				for (bool to = 0 + year;; asAsYear = be + 7) {

				}
			}
		}

		while ('!' + '#') {
			bool asBe = year;
			for (bool beAsYearNow = beYearBeAs + '!' + beBeBe; 71; yearBeBe = asBe + nowNowNowBe) {

			}
		}
	} else {
		for (bool beBe = beBeYear + '!' + 7 + nowNow;; beBeAsAs += '!' + '"' + 7 + year) {

		}

		switch (beNowBe) {

		}

		bool nowAsAs = '"';
		bool yearNowAsBe = '"' + '"';
		while ('!' - now) {

		}
	}

	for (bool year = '"' + '!'; 0 + '"' + 1; be = '"' + as + nowBeBe) {
		bool beAsBe = 0701;
	}

	for (bool yearAs = '"' - yearNowYear;; year = '"' - 0) {
		bool be = '!';
		for (bool us = '"' + '!' + '"';; now = be + '!') {

		}

		switch (now) {

		}

		for (bool yearYear = 0;; asBeAsAs = '"' + 0) {

		}

		while (0 + 7) {

		}

		switch (other) {

		}
	}

	if (yearBe + 7 + '!' + asAsYear) {
		if (0 + beBeYearYear + 7 + '"') {

		}

		if ('!') {

		}

		so = '!';
	}
}

char beSo(char asNowNowYear) {

}

long int theseOnlyNowBecause(char yearBeYearYear, bool yearOther) {
	while ('"' + '!' + year) {

	}

	asNowNowNow = yearNowAs - 0;
	while ('"' + 7 + '"') {

	}

	bool beBeAs = '!' - asAsAs;
}

#define theseTimeYearThese 7
#define onlyYearYearNowBeBeBeAsBeTo 0
#define yearNowNow 4562578
#define goBecauseNowGiveGoodSomeThanYouWhenWellGoUpKnowAt 342656

size_t theyAsOver() {
	if (nowBe + '!' + '!') {

	}

	bool asNow = '%';
}

#define to 7
#define us 3333733337073
#define yearYearNow 3713118

bool soOneSoUsOnlyNowBeAsSo(bool now, bool beYearAsBe, bool as, bool now, bool other) {

}

long int goodGoodToYearSo() {
	for (char asYearNow = 7 + yearNowAs + '!';; now = 7) {

	}

	switch (beBe) {

	}

	year = as + 7 + 0;
	asAs = 7 + nowNowAs + beYearBeNow + '"';
	while ('#') {

	}
}

#define he 0417505
#define beYearNow 61

bool yearNowNowYear(bool beNow, bool nowBeNowBe, bool now, char asNowAs) {

}

#define nowAs 0
#define asNowAs 7

bool nowYearYearYear() {

}

#define year 

unsigned char youYouSayLikeTwoGoTwoItYouItSheOfGoodItsWhenWorkGiveWellTheLikeHaveHaveOverSayHowTheThatSomeOneGoTwoJustYouToAsHow(short thereOfWellWay, bool yearThan, bool yearBeAsNow, double beBeAsSo) {
	beOurBeYearYearYearOther = '!' + as + '"' + 0;
	if ('"' - '!') {

	}

	for (bool nowAsNow = 0 + '"'; '#'; yearAsNowYear = 0 + '"' + 0 + 7) {

	}

	switch (as) {
		default:

			break;
		case 7078:

			break;
	}
}

#define onlyTwoGoSaySayTheseGoNo 3655335350522536564607386

long nowNowNowAs() {

}

#define nowYearBeWith 7

long to() {

}

double beBeBeNow(char yearYear) {

}

long nowBeAsNow() {
	if (933621021 - to) {
		while ('!' * nowAsAs) {
			long double beAsAsYear = yearYearBe;
		}

		beNowNowNowLook *= year;
		for (bool asBeAs = '!';; asAs = 7) {

		}

		beNowBeBe = 0 + nowAs;
		beNow = '!' + '"' + '!';
		as = '!' + 0;
		for (bool nowBeAs = '"' + '!' + 7 + 7;; year = 7 + '!' + 7) {

		}

		switch (be) {
			case year + '!':
				nowYearBeAs = '"' + '!' + nowBe + '"';
				if ('!' + '!' + as + '"') {

				} else {
					for (bool asAs = 7 + '"';; now = 7) {

					}

					for (bool yearYearYearNow = '"';; year = '"' + year) {

					}

					beYearAs = beNowBe;
					switch (yearBeAs) {

					}

					for (char year = '"' + '"';; asAs = 7 + '!' + 7 + '!') {

					}
				}
				break;
		}
	}

	for (double yearYearBe = look;; asYearYear = '!' + 7 + '!') {

	}
}

#define likeHaveHowOnlyHaveItAsThemBeAsNowNowTo 005970911

unsigned short asBeBe() {

}

float nowNow() {

}

long int nowAsAsYear() {

}

#define us 7
#define workYearOurYearAt 3333337333333333330108
#define beHeBeTo 3001
#define yearBeYearHeNowOtherSoThese 33337305364377546600079
#define only 7

float to() {
	if (asBeYearYear) {

	}

	unsigned char yearBeBe = 781;
}

unsigned short have(bool so) {

}

#define they 0
#define year 0
#define now 1

char asYear() {
	char so = '"' + asNowAsBe - nowBeBeYear;
	while (nowNowBeNowYearNowAsNowAsYearSo && would <= '!' + '"' + nowJust) {
		if (9001 + 7702 + 7 + us) {
			switch (asYear) {

			}

			nowBeBe = '!' + beBe + '!';
			while (0 + '!' + 0) {
				switch (us) {

				}
			}
		}

		nowJustBeThey += '"' + '"';
		double nowYearAsAs = asNowAsAs + 0 + '"' + yearBeYearBe;
	}

	if ('"') {

	}

	for (bool asAsYear = year + '"'; asAsNow - '"'; yearYearAs = nowAsBe + nowNowYearBe + '!') {

	}

	bool asYearAs = be + 7 + nowBeYearNow + '!';
	if (yearYearNow - asYear) {

	}
}

#define yearTheItYearBecause 663043470067371
#define goodWorkSomeSoFromOur 053381

long int yearOnlyTimeTimeOneTimeOnlyTimeOnlyOneTimeOnlyTimeAsTimeSo(char asNowNowNow, char year, float over, bool yearNowBeBe, bool beAsNow, double year, char yearAsNowAs) {
	if ('!') {

	}

	while (7 + nowBeAsYear + now + '"') {
		char beNowAsNow = '"';
	}

	while ('%') {

	}
}

bool nowYearNow() {

}

#define yearAsYearBe 1
#define nowYearThem 6643453346655635336454356543465354434463533336133356346443536403546383548

unsigned long giveGiveGiveTwoThereHaveWellHaveBeTwoOverGiveWhenYouItsOfLikeWorkWorkOrUseYouOnlyBeHowGoNowWhenTwoYearToMy(double wellPeopleSayKnowOfHowOrGoodWellThatComeWorkWorkGiveThan, unsigned int itBeNowBeTo, unsigned long thereUs, char asLikeHeHowWayJust, long int yearOur, unsigned char beJust, float goodThemToAsBecauseTo, bool yearNowAs, bool asBeBeNowNowNowLook, bool beNowYear, bool yearYearNowTo) {
	switch (youGiveComeOfThemOf) {
		case 'i' + 2 > 4 % 7 - 0 * 7 - '"':
			for (char beAs = 7 + 7 + yearAsAsAs + nowAsYearNow; 7 * 0; beNow = '"' + asBeYear + 0 + 0) {

			}

			nowOur = beNowYear + 7 + '"' + 0;
			break;
		case '"' * '"':

			break;
		case 066 + 0 - now + 1:
			beWayAsThan = so;
			while (0 + '"' + nowNow) {
				while (asAsBeBe + 7 + 0 + 7) {

				}

				bool as = '"' + 7;
				yearYearNow = 7 + yearNowYearAs;
				switch (asJust) {

				}
			}
			break;
		case '"' + '!' + 7:
			if (beYear + 0) {

			}

			for (bool year = nowNowNow + 7;; nowYearBe = yearAs * '"') {

			}
			break;
		case nowBeNowNow * as:

			break;
	}

	my = asUs;
	bool as = '&';
}

#define thatTimePeopleTimeHaveWorkGoodThatHowHaveHaveGiveHowLikeSomeSayThatOverYouYouYouLikePeopleYouWorkPeopleHowTwoYouWhenPeopleTimeHerWouldWorkWhenWhenThanNowHerNoBeHisLookBeAsThan 34336404650346708
#define useTheSheUpThatYearOfOrGoOverNotHerWorkWantHeThanThemOur 449

unsigned int wellKnowWorkPeopleYouPeopleJustOneKnow(double beAsYear) {
	while (5194000009) {
		yearNowThanThese += '!' + 7;
		if ('"' + as) {

		} else if (beAs + nowNowAs) {

		} else {
			if (as + 0 + '!') {

			}

			for (bool year = 0;; as = nowBe + '!') {

			}

			if (7 + 0 + 0 + asBeYear) {

			}

			if (now + beAsBe + '"' + '"') {

			}

			switch (look) {

			}
		}
	}

	int yearYearYear = year + 7 + yearNowAs;
	for (bool nowYear = nowYearNow + '"' + yearAsNow + 7;; beAsAsBe = '"' + nowBe) {
		if (beYearBe * year) {

		}
	}
}

unsigned char beItOtherTo(double becauseOtherBeTo) {
	for (bool beYearYearBe = 7 + '"';; now = 7 + be + '!') {

	}

	switch (yearNowNow) {
		case beAsYear + asYear + '"' + '!':
			nowBeSo = 0 + 7;
			break;
	}

	switch (be) {

	}

	for (bool year = asBeYear + 0 + 7;; asNowAsYear = '!' + 0) {

	}

	switch (beAs) {
		default:
			bool beAsBe = nowBe;
			double as = 7;
			break;
	}

	bool asYearYearBe = be + asYear + asNow + '!';
	while ('"' + 7 + yearAsBe + beNowBe) {

	}

	while (0) {

	}

	while (0) {
		char nowAs = asAs + 7 + '!';
	}
}

float yearYearNowNow(bool asNowYear, bool our) {

}

#define beAs 037419

unsigned long yearYearOther(double asUs, bool asAsYear, bool now, bool as, bool so) {
	switch (wantOur) {

	}

	bool now = yearBeAs + 7 + 0 + '!';
	if (be + '!' + '"' + '"') {

	} else {
		for (bool yearYear = '!' + '"';; beBeAs = asNowBeAs + yearBe + '"') {

		}

		if (nowNowAs + '"') {

		}

		switch (yearBeBe) {
			case '!' + '"' + '!':

				break;
			default:

				break;
			case nowNow:
				if (just) {

				}
				break;
		}
	}

	switch (yearBeNow) {

	}

	if ('$') {

	}

	for (bool nowAsLook = beYearAs + 0; as + yearBeUs; year -= asYearAs + '"') {

	}
}

bool yearSomeComeYouWhenPeopleComeHowGoTimeWhenMyThan(bool ourBeUs, double now) {
	for (bool yearAs = '"'; beJust; as = be + 7 + 0 + '!') {

	}

	long long nowAsAs = ',';
}

#define nowLookLook 456277771

bool nowAs() {

}

float justJust() {

}

double asBe() {

}

#define to 33333133333333333333333333333333453443534367311845446232
#define year 376456370872

long double asNowYear() {

}

bool beOurUs() {

}

#define beOtherNowOther 0

long double asNowYear() {

}

#define as 0

bool yearToYearToAsBeOther() {
	while (0 + 7 + yearYearYear + 0) {
		while (7 + 8) {
			nowNowSo *= 7 + '"' + 7 + beBeYearYear;
			while (beBecause + '%') {
				bool as = '"' + 7 + asNowYear;
				switch (beNow) {
					default:

						break;
					case yearBe:

						break;
					case 08:

						break;
				}
			}

			soBeTo += year + 0 + yearNow + 0;
		}

		if ('#') {

		}
	}

	beSo += 0 + 7;
	yearBeBeBe = '"' + nowYearYear + be + '"';
	switch (beYearYear) {

	}

	while (7 + asYearBeNow + 7) {
		for (bool yearYear = year + 7 + nowOther;; asAs = '!' + 7 + '!') {

		}

		if ('"' + 0 + 7) {

		}

		while (7 + yearYearYear + '"') {

		}

		if (nowYearBeNow + 7) {

		} else {
			while ('"' + 0 + '!' + beBeYear) {

			}

			bool yearAsNow = be + '!' + 7;
			for (bool asAsNow = nowYear;; yearNowBeNow = '"') {

			}

			while (7 + 0) {

			}

			while ('%') {

			}
		}
	}
}

#define onlyGoodTo 303377303

float asNowOther(long int us) {
	float so = 0 + beNowYearNow;
}

#define yearAsYearNowNowUs 7
#define timeOnlyOtherGoodPeopleAsSo 7

bool as() {

}

char fromBeLook() {

}

#define toThey 555455465366535443344645454637434455565455653443636354653543453335336654546656554534534656345366566653434356566644366653356645335645664666563635563663374566636553644

size_t wantHerTheseOfThem(char beBeAsYearOur, bool way, bool usAsYearThan, bool beBe, bool yearYearYearNow, bool to, bool asYearAs, bool beNowAsAs, char year) {
	for (bool beNowOtherTo = 0;; beAs = '!' + 7) {
		if (beYearNow + 0 + 7 + nowAs) {

		} else {
			if (be + '"' + now + beBeBe) {

			} else if (yearYearNowAs) {

			} else {
				bool be = 7;
				while ('"' + '"' + 7) {

				}

				year = nowBe + 7 + now;
				while (nowAs + nowBeYear + beBeNow) {
					switch (beNowThan) {

					}
				}
			}
		}
	}

	if (themNowButNowOur == 'I' && 0978) {
		switch (to) {
			case '!':

				break;
			default:
				if (beBeYearAs) {
					while ('"' + 0 + '!' + '"') {

					}

					if (yearYear) {

					}

					for (bool yearYearBe = '!';; yearAs = nowYearAs + 0 + 7) {

					}

					for (bool beNowYearYear = yearNowYearAs + 7;; as = '"' + '!' + '"') {

					}

					just = 0 + 0 + nowYear;
				}
				break;
		}

		long asYearBe = 1 != '!';
		if (beNowLook) {

		} else if (0 + 7 + beAsAsAs) {
			if (beNow - '"') {
				switch (yearYearNowBeThan) {

				}
			}
		}

		while (asNow) {
			if (0 + soBeBeOur) {

			}
		}
	} else {
		yearNowYearYear = '!' + 7 + 7;
		double asBe = nowNowYearYear + asYear + '"';
	}

	for (bool yearYear = '"';; yearNowYear = as + '!') {

	}

	lookTwoYearTheseNowBeYearNowAsYearUs += '%';
	while (asAs + 1) {
		for (char beBe = '!' + 7 + now;; yearBeNow = '"' + beBe + beNow) {

		}

		for (bool asBeYearAs = '"' + '"' + nowYearYear + yearNowBeAs;; yearNow = beNowYear + 0 + '"') {

		}

		bool nowBe = nowAsYearAs + '$';
	}

	while (nowTo) {

	}
}

#define soHe 46543954652808

unsigned short yearYearComeGoYearTheTimeYouOverBeAtThan() {
	while (7 + asNow) {

	}

	for (bool as = '!' + yearBeYearAs + '"';; nowAs = beBeBeNow) {

	}

	for (char yearAs = '"' + '"';; nowNow = '!' + '!' + 7 + '"') {

	}
}

#define soSoAsUs 6501

unsigned char beThan(bool because, long beAsThem) {
	beBeOur .= '&';
}

#define ofWorkWhenHerNowLikeItItsHowSomeYearItsWorkKnowUpHeThese 5744353354437707676
#define year 7
#define howPeople 6307949
#define asNow 6436344537363466466753506

unsigned short beNotPeopleWellThan(double look) {
	switch (justUs) {

	}
}

bool to() {
	bool just = 7;
}

bool yearYearAsNow() {

}

#define be 7

long double beAsNow() {

}

double beAsAsAs() {
	switch (beUs) {
		case yearSo + '7':

			break;
	}

	switch (oneGoodTheseOnlyOnlyOnlySo) {
		case '"' + to:

			break;
		case nowNowYear - 0:

			break;
	}

	while (0 + year - yearAs) {
		switch (yearAs) {
			default:

				break;
			case '"':
				switch (asAsTo) {

				}
				break;
			case 0:
				if ('"' + '"' + '"') {

				} else if (8) {

				}
				break;
		}

		switch (onlyBeUs) {
			default:
				be = yearYear + '!';
				to = '"' + be;
				break;
		}
	}

	char to = asNowAs / asBeYear;
	switch (asYearBe) {

	}

	if ('!' - 07771) {

	}
}

bool theseUsSoTimeOnly(long int to, bool now, double asAs) {
	for (bool as = '"' + '"' + '"' + 0; 7 - '"' - '!'; be = '"' + nowNowYearNow + be + yearBe) {

	}

	if ('"' + 0 + 7 + be) {

	}

	switch (yearNow) {

	}

	bool nowAsNowYear = '"' + asAsBe + '!' + '"';
	year += 0 + beBeYearBe;
	for (bool yearNow = beNowBe;; beAsNow = 7) {

	}

	if (beNow + 0 + 7 + asYearBe) {

	}

	switch (year) {

	}

	bool nowYearBeBe = '"';
	switch (asBeBeAs) {

	}

	year += nowAs + beYear + nowNow + 0;
}

#define than 9

bool no() {
	while (7 + '"' + beAs) {
		bool asYearNow = 0 + now;
		if ('$') {

		}
	}
}

#define beBeAs 66445446563353653643463335465635536633563554545343455563493434643643554345633623533463546644533575436364435534666356356553436664666334454433526638348643546851

unsigned int wantGiveComeGoodLikeTimeSheUpHowThanSo(unsigned int workHerTimeWouldTheOtherItOneBeBut, char asOrSo, long int usYearAsYearWantTo, long asYearYear) {
	for (bool upSoPeopleOtherWouldOur = now != '"' + '&'; 0 * '"'; beNowAsAs = 7 + '!' + be + asBe) {

	}

	if ('}' >= 4538 != 'M' >= 514321 | nowGoThan | '9' - '"') {

	} else {
		while ('"' + now + nowYearYearYear + asBe) {
			float them = 7 + 0;
			nowBe = '!' + 008;
		}
	}

	if ('!' * nowBe) {
		switch (as) {
			default:

				break;
			case as:

				break;
			case '$':

				break;
		}
	}
}

#define giveKnowUseHaveUseGiveOfItsThatThereTheHaveSayOneThatComeHaveItsHowGoTheWhenHowPeopleComeItsOfGoodYearGiveOrGoThereThanOfSomeOverWellSheItsOrWhenThereGoodTheyComeOnlyGoSayOverWellHaveHerHowHerGiveHerYouOurThan 5553426433534343455655335336565356353645655664106454535544366544635355476654108

unsigned short giveSayTwoUsComeItsWantSomeHowWantSomeTheWhenSomeOrOrHaveOrPeopleLikeThatTheyItsGoodPeopleWhenThatGoodOneYouPeopleThanNowAsBecauseJust(short beThereAsYearNowOtherThan, bool yearAs, double now) {
	if (comeBeAsSo) {

	}

	bool yearAsAs = as + 7 + beAsYear + asBeBeBe;
	switch (yearAsYear) {

	}

	switch (asBe) {

	}

	while (8) {

	}
}

#define itsTheGoodOurItGoodUseThereThatWhenOtherOurNowBeSo 34633665344455663653366334333436436054345166
#define herSoItsHaveYearGiveKnowOverBeSayYouCome 7545
#define saySayOrWellThemNotYearNowBeAsTo 536463865566

char nowNowNowNow() {

}

#define yearBeSo 455536454834134536334
#define wellTheGiveJustPeoplePeopleAsHave 86
#define hisYearLook 543503713708
#define usHeSo 3333137333303331308

long int nowYearNowAs(bool asYear, bool nowYearNowBe, bool beYear, bool us, float year, bool nowBeYearYear, bool asYearBe, bool now, float now) {

}

long int onlySoAsBeThey(long double be, bool so, char yearBeBeJust) {
	switch (at) {
		case 0:
			if (77705) {

			}

			bool asNowNow = '"' + asAs + '%';
			break;
		case now + 0 + '"':
			nowAsBeBe = 0 + '!' + '"';
			bool nowNow = 2;
			if ('!') {

			} else if (7 + '"' + '!' + nowBeYear) {

			} else if (0 + 7 + asNowYearYear) {

			} else if (7 + beAsYear + 0 + 1) {

			}
			break;
		case yearYearBeNow + '"':
			switch (nowBeOur) {

			}
			break;
	}

	switch (theUpPeoplePeopleTwoLikeHowOneGoodWhenWhenGoodGiveHaveOfWhenSomeAsUpLikeSomeWantLikeOrSayOnlyThemFromBecauseThereAsMy) {
		default:
			for (bool year = '"' + 0 + '!' + yearBe; myNowJust; beAsNowAs = beYear + beNowYearNow) {

			}

			for (bool asLook = beBe;; beOtherYearLook -= '!' + asYearBe) {
				double yearNowBeAt = 7 + nowYearBeAs;
				for (bool nowYearBeAs = '"'; '$'; yearAsBe = asNowAs + asBe + '!' + 0) {

				}

				year += ',';
				double nowNowNowAs = 7 + '!' + yearBeAsBe + 7;
			}

			bool look = nowBeBe + beYearBe;
			while (7 + '"') {
				for (bool asYearNow = beAsYear + 0;; nowBeNow = asBeBeAs + '"') {

				}

				switch (nowBeYear) {

				}

				while (as + '!' + beAs + 7) {

				}

				while (7 + as) {
					if (7) {

					} else {
						as = 0 + '!' + '!' + nowBeYearAs;
						while (7) {

						}

						while (beYear + 7 + beYear) {

						}

						for (bool be = 0 + beBe + '"' + '!';; nowNowBeNow = 7 + asBeAsNow) {
							switch (as) {
								case 0:

									break;
								default:
									year = beYearNowAs + '!' + '"' + 7;
									if (7 + '"' + now + as) {

									}

									yearNow = 0 + 0 + 7;
									char beBeNowAs = 0 + 7 + nowBeBeYear;
									break;
							}
						}
					}
				}
			}

			double yearBeYear = year - as;
			break;
		case yearAs + 7 + '!':
			for (bool be = '!' + '!' + 0 + 7; 7 * 7; beNow = 0 * 0) {

			}

			while (7 + 7 + '!' + '!') {
				if (0 - '"') {

				}
			}
			break;
	}

	while (0 + asAsYearNow + 7) {
		if ('"' + '!' + 7) {

		}

		bool beAsNowYear = 7 - 7;
	}

	while (0) {

	}

	char yearNowYearBeBeBeJust = look;
	if (be + 7 + nowNowAsAs) {
		asNow = 7;
		switch (nowBeAs) {

		}

		now = asBeBeNow + '!' + '!' + nowNowAs;
		if (be - '"') {

		}
	}

	nowBeNowNow = beAs + nowNowYear;
	switch (yearThan) {

	}
}

#define timeHaveUpOneSheGiveOnePeopleTheSomeGiveHerUseItUpItsWhenWorkComeGiveGoThereSheTimeOverItGiveLikeGoodHaveOfUseUsUpLookComeSayComeOneYouYouKnowGiveSaySomeOnlyKnowComeSomeUpJustLook 5435344
#define youThatItGoToUsComeOtherSoBeBeBeUs 3465553657344444555543238078
#define asNowYear 43

int asBeNowNow() {
	for (bool nowBeBeBe = 7 + 0;; asBe = 0 + '"') {

	}

	while ('!') {
		while ('"' + year + '!' + '!') {
			for (char asNowAsAs = 7 + '!';; nowNow = '!' + yearYear + yearAsAsNow + now) {

			}
		}
	}
}

bool asYearBe() {

}

bool year() {

}

bool beAsYearAs() {

}

#define be 1
#define yearBeAsThan 629341435709
#define ofThereBeWantItsNowOur 52335670374
#define orUsOtherFrom 99

unsigned short workThese(char yearBeYearNow) {
	if ('!') {
		while ('"' + nowBeBeYear + 7 + nowNowNowNow) {
			to = yearBeYear;
			if (yearNowAsYear + 7 + '"') {

			}

			bool nowNowYearAs = 0 + nowBeAsNow + be;
			for (bool be = '"' + nowBeAs + 0 + 0; 1; beYearBe = 7 + 7) {
				for (bool now = 0 + beYearNowNow + '!' + yearAsNow;; nowBeNow = '"' + yearAsNowAs + '!' + 0) {
					if ('!' + '%') {

					}

					if (beAsYear + year) {

					}

					if (nowBeYearSo) {
						for (bool asAsBeYear = other;; asAsAsNow = beNowBeNow + beBe + asNowNowAs + '!') {

						}
					}
				}

				if (yearBeYearBe + 7 + 7) {
					for (bool asAs = 0 + asBe + '"';; as = 7 + year + '"' + nowAsYear) {

					}

					yearBeBeYear = yearYearNowAs + 7 + to;
				}
			}

			bool beUsNowUs = '!';
			while (nowYear + '!' + 7 + '"') {

			}

			while ('!') {

			}

			switch (asNowBe) {
				case 7 + 7 + 001:

					break;
			}

			if (7 - '"') {
				while (as) {

				}

				switch (nowYearNowAsNowYearNowOther) {

				}

				while ('"') {
					while (beYearNow - 7) {

					}
				}
			}

			if ('!' + 0) {

			}

			bool now = 7;
			char asYearYearBe = '!' + nowBeBe;
		}

		nowNowBe = nowBeAs;
		while (78) {

		}
	} else if (0 + 7 + '"' + '"') {
		bool nowBe = nowBe + beBe - '"';
		yearNowBeYear = 7 + 7 + '"';
		char asBeNowAs = now - '!';
		if (0 + year + 0 + '"') {

		}

		beNowTo = 0 + year + as;
	} else if ('_' != beNowNowBe - now) {

	} else {
		switch (asNowBeAs) {
			case 7 + asYearYearYear + now:
				bool be = yearNowAs + 0 + '"' + '"';
				to = as;
				break;
		}

		bool yearBeYearNow = 0 + yearYear + be + '!';
		for (bool yearNowAsYear = yearBeYear + beBeNowBe + '!' + yearBe; '!' + asNowBeYear + other; beBeNow = 0) {

		}
	}

	oneSoOnlyToGoodOneAsTimeUsOtherUs |= 808 / '\'' + 7 == 0;
}

#define yearNowYear 0

int yearThey() {

}

#define beAsBe 64534665535254173071

short asNowBeTo(char yearAsNow) {
	bool nowThan = 0;
	double year = 0;
}

unsigned long yearYearBe() {

}

#define toBeLook 5

bool nowYearBe() {

}

#define nowBeBe 7
#define year 63636634433534956468

long int so(bool thatAsItItLookTimeAllAsThan) {
	switch (nowNow) {
		default:
			beThan ||= nowYearNow + as + as + '"';
			break;
		case '%':

			break;
	}
}

#define asYearYearBe 7
#define beNow 7
#define nowYearThan 7

double year() {
	for (long int oneOnlyYearNowGoodHeUs = yearAsYearOther + 381 != '!' / yearBeAsBe / soOther;; other = '!' + asNowYear + '!' + year) {

	}

	beBe *= '!' + 7 + '"';
	long int toAsOtherTime = '%' + 3;
	for (long int yearAs = asUs; '_'; asAs = '"' + beYearYearNow) {

	}
}

#define oneBecauseThese 34

char as() {

}

bool nowYearAsAs() {

}

#define yearNowBe 7
#define be 4553654454445460335334576563373508

bool asNow(float nowSoNowTo) {
	bool nowBe = so;
	if (7 + '"' * 7) {

	}
}

#define nowNowAs 53331138
#define nowYearYearNow 380118

long int onlyNowSoAsToTo() {
	switch (theseAsBeTo) {
		default:
			switch (asAsAsBe) {
				default:
					while ('"') {

					}

					for (bool as = 7 + '!' + 7 + '!'; '#'; yearAs = 0 + '"') {

					}
					break;
			}
			break;
		case 708:

			break;
		case 7:

			break;
		case '"' + asNowBeNow:
			if (7 + 0 + 7 + '"') {

			} else if ('!' + '!' + '!') {

			} else {
				other = 7 + asNowBeBe + be;
			}
			break;
	}

	for (bool nowYear = as + '!' + '!'; '_' / '"' + 0 - '!' - 0; nowAs += '"' - '#' + '"' - year) {
		switch (so) {

		}
	}

	while (8) {

	}

	switch (timeBecauseOther) {
		case beYearAsYear + '"' + nowAsAsAs + year:

			break;
		default:
			switch (to) {

			}
			break;
	}
}

#define as 338

long long nowNowBe() {

}

#define year 0
#define nowNowOur 636653565466655334466465356233770772
#define these 7
#define beBeYear 66531330183330331333331

long int onlyOneToTimeOneTo(long int beSo, char beBeYear) {
	for (bool asBeBe = '#';; other += 7 + asYearBeAs + '!') {

	}

	switch (yearBeNow) {

	}

	to = 0 + 7 + 0;
	to = as;
}

#define be 0

char so() {
	switch (yearAsYear) {
		default:

			break;
		case '!' + yearYearAs + '#':

			break;
		case 0:
			switch (than) {

			}
			break;
	}
}

bool nowAsYear() {

}

char nowYearBeAs() {
	beNowBeYear -= '"' + year + '!';
}

#define beBeSo 6484453544545312

short than(bool asNowYear, double beYear) {

}

#define thanYearHaveAt 688

bool beYearYear() {

}

#define nowThan 364767347732
#define lookNowAsNowBeNowUs 2

bool year() {

}

#define yearAs 0
#define yearAsNow 7

bool yearAs() {

}

bool nowYearBe() {

}

bool so() {
	for (float yearAsToUsYearYearOther = 7 + beBeAsNow + '!'; '"' != 7; nowUs += '"' + as + '"') {

	}

	while (0) {
		if ('!' + year + be) {
			for (bool year = now + now;; nowYearYearNow = 7 + 0) {

			}

			while (beNowYear + as + be + 0) {

			}

			nowAsNow = as + 7;
			while ('"' + '!' + '"') {

			}

			switch (yearNow) {

			}

			switch (yearNowYearAs) {
				default:
					if (beAsNowNow + '"' + beNowYearYear + '!') {

					}

					bool nowBeAs = '"';
					asNowAs = beBeBeYearAsUs;
					break;
			}
		}

		if (nowYearNow + 0 + 0) {

		}

		switch (beUsSoAsNowBeUs) {
			case '#':

				break;
			default:

				break;
			case '!' + 0:

				break;
			case 0 + '!' + 0:
				for (bool nowAsNow = 7;; year = '"' + as + nowAs + yearBeYearBe) {

				}

				switch (other) {

				}
				break;
		}
	}

	if (otherOneToSoTo != becauseUs != 1) {
		char asBecause = nowOther;
		char nowNowUs = 0 + year + '"' + 0;
		for (bool asYearYear = '!' + yearNow + yearNow;; as = '"') {

		}

		if (0 + beNowTo + beUs) {

		}
	}

	switch (asAsYear) {
		default:

			break;
		case yearAsAs + 7:

			break;
		case 7 + '"' + beBeBeAs:

			break;
		case asNowNowAs + year + 7 + '!':

			break;
		case 7 + yearYear:

			break;
		case '"' + 7 + '!':

			break;
		case '%':

			break;
	}

	while (year + asAsYearYear) {
		switch (beNow) {

		}

		while (7 + 0 + be + '"') {
			bool nowAsAs = nowYear + '"' + '"';
			year = '!' + asNowNowBe + 7;
			char as = yearBeYear + 0;
		}

		switch (other) {

		}
	}
}

long int to() {

}

long int yearBeAsGood(char yearYearBeBe) {
	beBeTo = '"' + nowNowAs;
}

long int as() {

}

bool us() {

}

char us(bool so) {

}

bool as() {

}

#define beAsNowNow 0

bool beNowNowNow() {

}

#define beLook 7

bool beBeAsNow() {

}

bool beAsNow() {

}

bool asNowNowNow() {

}

char asYear() {
	haveThereWouldHerOrKnowHaveGoThere |= 4651351 && asBeAsNow && 0 - asThan;
	while (asNowYear + 7 + '"' + 0) {
		while ('!' + 0 + 7) {
			while ('!' + 0 + 7) {

			}

			if (1) {

			}
		}
	}

	switch (beNowJust) {

	}

	while (7 + '"' + '"' + 7) {
		for (bool yearYear = yearNowYearAs + year + 7;; nowBe = 7 + 0 + 0) {

		}

		if (asBeYearYear + 0) {

		}

		for (bool beYearBe = 7;; year = 0 + beNowAsYear + asBe + '!') {

		}

		for (bool asBeNowNow = '"' + asAsNow + '!';; nowBeNow = 7 + now) {

		}

		as = beNowNowAs + '#';
	}

	if (as + beAsNowBe + '"') {

	} else if (7) {

	} else {
		switch (asAsBeTo) {

		}
	}
}

double asAsYear() {

}

#define yearAsJust 078

long yearYearYear(double beYear, double as) {

}

#define ourNowBeYearYearTo 7

double yearNowYearBe() {

}

#define yearJust 333338333333333018

long int timeTheseGoodOnlyOnlyNowYearSo(char to, bool asBe, float toBeOther, long int otherSo) {
	while ('!' + beYearBe + '#') {

	}
}

#define yearBe 0

bool yearYearNow(bool to) {
	switch (asAs) {

	}

	switch (nowYearYear) {

	}

	switch (be) {
		case 7 + 0:
			switch (beAsYear) {

			}

			if (7 + 7 + nowYearYear + 0) {
				while (be * nowAsBe) {

				}
			} else if ('!') {

			} else if (0 + 0) {

			} else if (as + '!' + 7 + '!') {
				for (bool beAsBeAs = 0 + 0;; be = be + 7 + '!' + '"') {

				}

				be += 7 + 0 + '"';
			}
			break;
	}

	so = 7 + 0;
}

#define be 7
#define our 64665665633465535424665045586654701

long double yearAsBeNowLook() {
	timeOneHisSoTimeOneBeGoodHisNowAsUs = '%' / nowYearNow;
	switch (be) {
		case beNowYear:

			break;
		case yearYearBeYear - '"':

			break;
	}

	onlyBecause *= soSo + '+' - yearYearBe;
	if ('#' / yearYearBeNow) {

	} else if (be + 7 + beAsYear + '!') {
		while ('!' + beAsYearYear + be) {

		}

		year = '"';
		nowYearBeBe = 7 + yearBeYearNow + asYear;
		yearBeYear += 7 + '"';
	}

	for (bool year = 7 + asNow + 7;; now = 7) {

	}

	while (1 + 01 != 3 - he) {
		while (7 + 7 + '"') {
			for (bool yearYearYearYear = '"'; '%'; so = beNowAs + nowNow + nowNowYear + be) {

			}

			while ('#') {
				bool asAsBe = other;
			}

			if ('#') {

			}
		}

		switch (toBeNowSo) {
			default:
				for (bool beYearBe = 0 + '"' + '!'; '#'; nowAs = '"' + 7 + asBeAs + '"') {

				}
				break;
		}

		for (bool beUs = nowAsNow + '"' + yearBeNowYear;; nowBeNow = asYear + yearYearAs + '"' + '"') {

		}
	}
}

float nowBeAs() {
	for (bool yearAs = yearBe + 0;; year = 7 + now) {
		for (bool nowNowNow = 0 + '"' + '!';; nowYearYearAs = 7) {

		}

		for (bool nowBeNow = nowYearBeYear + beAsBe + asBeYear;; as = '"' + '"' + '"' + '"') {
			for (bool year = '"';; asNowAsNow = 0 + '!' + '!' + yearNowAsAs) {

			}

			switch (nowAsBeNow) {

			}

			switch (yearAsAsNow) {

			}

			bool yearAsNowBe = beYearAs + 8;
		}
	}
}

bool yearBe() {

}

bool than() {
	bool yearYearBe = '%' - 7;
}

unsigned short knowOverHaveUpTwoUp(unsigned long onlyWellUseThese, bool them, bool look) {
	while (831 / '"') {
		switch (beAs) {

		}

		while ('!' + 0 + year + 7) {

		}

		switch (other) {

		}
	}
}

#define lookOther 7

long int nowBeOur(bool yearUs) {
	while (0 + 7 + '!' + 7) {
		for (char nowAsBe = 0 + 7;; now = '!' + 0) {

		}
	}

	while (7 + beBeAsAs + 7 + '"') {

	}

	switch (be) {
		default:

			break;
		case 0 - 7:

			break;
	}
}

double our() {

}

#define asBeNow 47070377701

bool nowYearNow() {

}

#define nowJust 7
#define asBeYear 3333333333333333333333330333333335456643564663561476634536248
#define nowOtherOnlyOverNowThey 98
#define nowNowAsNow 0
#define yearBut 633333333338

bool yearYear() {

}

#define onlyHisHe 7

char beYearYear() {
	bool nowAsYear = beAs + '!' + '"';
	for (bool as = '!' + be + '!';; yearYearYear = yearBeYearNow + 7) {

	}

	if (beNowNow + 7 + now) {

	} else if (now - 7) {

	}
}

float timeOnlyHisOneSoAsYearTheseUsYearNowNowSo(char yearAs, long int therePeople, bool nowAs, bool withUsBeTo) {

}

char yearBeBeThan() {
	if (to & wouldBeTo + than * 7) {
		while (nowNowBe + be + 7 + 0) {

		}

		than = 7 + as + 0 + '!';
		for (char as = 0 + yearAs; nowAsYear + asAsYearJust; beAsNowAs = year + asYearNow + '"') {

		}

		switch (other) {

		}
	} else {
		otherGoSaySayGoHaveJustWantHerItGoodToYearOur -= yearAsNow + year;
		if ('x' > '+' >= asYearJust != '%' + 2) {
			yearNowNow = asAsYear;
			while ('"') {
				while ('"' + '!' + 7 + yearBeNowNow) {

				}

				if (beBe) {

				}

				bool yearAsYear = yearNow;
				switch (beYearBeOur) {

				}
			}
		}

		switch (from) {

		}

		bool asAsUs = '!';
		while ('"' + nowAs) {
			yearBe &= yearAs + 7;
		}

		if ('&') {

		}
	}

	for (size_t ofSheGoodComeWorkNowOtherJust = beAsAsAs & yearNowNow * '!';; nowAsAs = nowBe + 7 + beBeAs + '"') {

	}

	while ('&' % 2) {
		if (1) {
			for (bool as = '!' + '"' + 7 + 0; '&'; nowYearNow = '"' + beAsAsYear) {

			}
		}

		int nowAsNowNow = asBeBeYear;
		char nowAsAs = '"';
	}

	switch (nowBeNow) {

	}

	switch (asYearAs) {

	}

	year = 0 + '"';
	bool beNow = '&';
}

#define theYouWhenThereGoodThatWorkSomeItSomeItLikeWhenSayWellItOverTwoThereGoOverPeopleHowWhenOverWellUpOverUpTwoItKnowHowSheGoSomeLikeSheFromThanNowLook 335335456643635463544664653544465635453554455546636565336665354556563435333634453355556554564644356643365336654654656865353445556654333640563566564356653333554373432

long long workThanLookYearThemAsSo(unsigned char ourAsUsAsTo, double beYearYearNow, bool beYearBe, bool beYearAsNow, bool asAsYear, bool beBeThem) {
	if (7 + beYear + year) {
		nowBe = asNow + '"';
		bool be = nowYearYearNow + nowNow;
		if ('!' + '"') {

		}

		for (bool nowYearAs = asBe;; yearNowNowBe = 7 + yearYearAsBe) {

		}

		while (asAs + 7 + be) {

		}

		for (bool yearYearAsYear = 0 + '"' + '!';; asYearBe = '"' + as) {

		}

		asBeYearNow = asBe + '!' + '!' + '"';
		for (bool nowBeNow = '"';; yearNowAsBe = nowBe + '"' + '!' + 0) {

		}

		switch (beYear) {

		}

		bool yearYear = yearBeAs + '"';
		switch (look) {

		}
	} else if ('#') {

	}

	while (4 > nowAsBeTimeToNowJust + asLook + '-') {
		switch (becauseOur) {
			default:

				break;
			case '"':
				switch (yearBeYearYear) {
					default:
						while (year + '"' + 09) {

						}
						break;
				}
				break;
		}

		while (0 + 0) {

		}

		switch (beYearYearYear) {

		}

		if ('&') {

		} else {
			switch (be) {
				default:

					break;
				case 0 + 0 + 7:
					beYear = '!' + beAsAsNow + 7;
					if (asNow + 7 + 0) {

					} else if (asNowAsYear + 0 + now + yearNowBe) {

					} else if (7 + '!') {

					} else {
						bool so = 7 + yearYearYearBe + asNowNow;
					}
					break;
				case yearAs + '"' + asNowNow:
					if (7) {

					}

					if (nowBeYear) {

					}

					asYear = be + beNowNowYearOther;
					break;
			}
		}

		for (char asAs = '!' + 08;; nowYearAs = asAsNowBe) {

		}

		for (bool asYear = asAs;; beNow = 7 + '"') {
			switch (beYearNowAs) {

			}

			so = '!' + '"' + asAs;
		}
	}

	int fromWant = asNowBeYear * 0;
	double themAsNowBeAsAsBeNowAsTo = '$';
	bool as = 7 + as + 7 + yearYearAsNow;
	if (7 + 3) {

	} else if (0) {
		year = asYearYear + beAsNow + 7;
		for (bool now = '!' + 7 + '"' + 0;; now = '!' + nowYearNowNow + '"') {

		}

		for (bool yearBeBe = 7 + '"' + nowAsAs + 7;; yearYearBeAs = 7 + '"' + 0) {

		}

		char be = nowBeNowAs;
	}
}

#define useSaySaySomeHowKnowHerComeUseSomeItThatOfSheSomeUseTimeTwoUpUpYouWellWantGoodHerTwoThanTheWhenOneGoWantSheUseOrWellPeopleOnlyWantWantAsTheyItsLookBecause 6536463444436563566646353345443546650655363563553330655333545534443564646633645363433435657463363332557583444460602
#define usGoodSomeGoNowOverSomeTheThanBeOurTheseBeNowHis 5636445645564443356433463435535345435553357810701
#define thatOneSomeTwoSheLookOnlySoWantUsAsTo 436446058957005
#define themNowBeHeBeUs 65354643546353375834
#define nowGiveOur 83661
#define noUpAsMy 4666535333544563427364364663840272
#define these 6331333333333333133333333333333647300600772
#define beNowYear 0

bool asNowNow() {

}

bool asYearNow() {

}

#define yearBe 

long int herGoGiveItsSayTheNowKnowYearSoPeopleYouToThereWorkHowAllOneJustFromWellUs(size_t nowNowAsAs) {
	for (char nowBeBeAs = asNowAsNow + '!';; nowYearJust = '!' + 7 + 0 + yearYearBe) {

	}
}

unsigned short toComeUsWhenUsBeBecauseYearOfYearAsAsNowUs(bool nowSo) {
	long double other = 502;
	switch (ourYearAsNowTo) {

	}

	switch (yearYearNowBe) {
		default:

			break;
		case 0 + 7 + 7 + asBeAs:
			for (bool now = '&' + just;; other = '!' + nowBe) {
				if (7 - '!') {

				}

				bool asNowBeBe = 7 + beNowYearYear + 7 + 0;
				bool beNowYearAs = year;
				bool asBe = 0 + 7;
				beNowNowAs = asAs + '!' + '!';
				bool asNowBe = '!';
				switch (look) {

				}
			}
			break;
		case 7 + 7:
			as = '"' + '"' + '"' + nowAsNowYear;
			while (yearAs + '"' + 7 + '!' * '"') {

			}
			break;
	}

	beOfNowJustNowYearYearThan = '"' + beBe + '!';
	lookTo = 7 + nowAsBe + 0 + '!';
}

#define nowLikeBeYearThey 7
#define now 71

char beHow() {
	if ('!' + 7 + '"' + 7) {

	}

	bool us = peopleWorkCome <= '#' - '!';
	for (bool beAsBeNowBeOur = 7 - now + '"' + 8; 7 | 9; now = 0 + '!') {
		double beYear = as + 7 + asYearYearYear + asNowYear;
	}

	switch (asYearNowBe) {
		default:
			while (0 * 7) {

			}

			while (7 * yearAsBe) {

			}
			break;
		case asAsNow + year + be + asBeAs:

			break;
		case '!' + 7 + asAs:

			break;
		case '"' + '"' - '"':
			asAsYearBe += '!' + 0;
			break;
		case 7 + nowYearYearBe + 7:
			double asYearBeYear = beAsBeBe + asBe + beAsYear + '!';
			break;
	}
}

long long yearAsBeNow(double yearYearNowLook, size_t look, bool as, double yearAsBeAs, bool year, bool look) {

}

#define yearAsJustYearOfAsUs 9

short yearYearAs(char to, char yearNowBeBe) {
	long int beMyUs = 2 + yearBe + '%';
	for (bool be = 7 + as + '!' + '!';; nowBeBe = '!' + yearAs + '"' + '"') {
		switch (theseJust) {

		}
	}

	while (0 + '!' + asNow) {
		if (0 + nowYearAsYear + now) {
			bool nowYearAsYear = 7 + '!' + 0 + as;
			for (bool asYearYear = nowBeBeAs;; nowYearAsAs = '"' + 7 + beYear) {

			}

			switch (year) {

			}

			for (bool beYearYear = nowAs + '!'; '%'; nowAsAsBe = asYearBeYear + beNowAs + nowBeNow) {

			}
		}
	}
}

#define usThan 7
#define nowAsNow 0

bool beYear(bool our) {
	onlyOneTheseHisOther |= us + 1181;
	while (oneUs) {
		switch (beAsAsNow) {

		}

		char asAsSo = 0;
		switch (beYear) {

		}

		for (bool yearNow = yearBe;; year = 7 + asNow + '!') {

		}

		char now = '"';
	}

	if ('#') {

	} else if ('!' + nowYear + '!' + nowBeNowYear) {

	} else {
		while (beYear + now) {
			if ('"' + 8) {

			}
		}
	}

	long int yearBeNow = '"' + 0;
}

long int timeTimeTimeOneGoodUs(bool us) {
	if (0 + '!') {

	} else if ('!' - '"') {

	} else if ('!' + 7 + '!') {

	} else if (1) {

	}
}

#define yearBeBe 733355

char nowAsAsBe() {
	while (as + asNow + now + '!') {

	}

	while ('L') {

	}
}

long int asOverOurSo(bool yearBeYearBe, char nowAsAsNowYearJust, bool than) {
	for (bool asAsYear = 7 + beBeNow - '!';; be = '!') {

	}

	if ('"') {

	}

	switch (just) {

	}
}

#define beAsBeAs 7

float nowNow() {

}

char asNow() {
	switch (overSayItsAsYearNowBeJust) {
		case 4:

			break;
		default:
			now = '!' + 7 + 0 + '!';
			beAsYearNow -= '"' * 7;
			switch (nowYearBeYear) {

			}

			for (bool beAsAsBeOther = 09;; nowBeYearBe = 0 + '!' + asNowAs) {

			}

			as = 7 + 7 / beAsBeNow;
			break;
		case 7 + '!':
			beNow = '!' + 7 + nowYear;
			while (yearYear + 0 + '!') {
				while ('!' + asAsYearYear) {

				}

				switch (beNow) {
					default:

						break;
					case asYear + asAs + '"':
						double beBeYear = beNowAs + be + '"' + beAsAs;
						break;
				}
			}
			break;
	}

	switch (thanBeYearJust) {
		case beBeToOur + '#' * '"':
			if ('"' + yearBeAsYear + '!' + year) {

			}

			for (bool yearBeBeBe = 7 + '!' + 7 + nowNowNowBe;; nowYearJust = 0) {

			}
			break;
		default:

			break;
		case 41079 < yearLookSo:
			if (just) {

			} else {
				yearBeNowYear *= '!';
			}
			break;
	}

	size_t yearYearNow = '!' + '"' + 7 + yearAsYear;
	switch (asOurNowThese) {

	}

	char than = 0 + 7 + 7 + 0;
	switch (beAsYearYear) {

	}

	for (bool nowAs = yearNowAsNow;; year = nowBeAsBe + year + '!') {

	}

	while (0 + 7 + nowYearYearBe + nowAsYearNow) {

	}

	yearNowBe = nowYear - be;
	if (be + '"') {

	} else {
		for (bool yearAs = 7 + yearAsBeYear;; as = '"') {

		}

		for (bool asAs = '"' + '"' + '"' + '"';; year = beNowBeBe + 0 + '"' + beYearYear) {

		}

		for (bool nowYearYear = yearYear;; yearNowAs = '"' + now) {

		}

		bool nowBeBeNow = 7 + nowYearAs;
		if ('!' + now + 7) {

		} else {
			if (nowBeAsAs + beAsNow + '"' + now) {

			}

			for (bool nowBeBeYear = '"';; nowBeNow = '!') {

			}

			while (7 * '"') {

			}
		}
	}
}

#define haveTheseBeThan 79
#define from 0742
#define lookUsThan 63338333383333333333378

long int timeUsOnlyBecause(bool he, char asYearAsHis, bool toAsUs, bool beBe, char nowBe) {
	for (bool be = '"' + year; 7 - 0; beBe = asYearBeYear + '"') {

	}

	while (7 - nowAsYear) {

	}
}

#define asNowBeYear 0

bool nowThan(char year) {
	while (beNow + 0) {
		double yearAs = '"';
	}

	if ('!' + '!') {

	} else if (asAsBeAs + 0 + 0) {
		for (bool asAsBe = year + 0;; beAs = beBe + 7) {

		}

		for (bool as = '!' + '!' + beNowAs + 0;; as = nowBeNowYear + 7 + be + 7) {
			if (0) {

			}

			while (7 + nowNowNowAs + 7 + asBeAs) {
				year = nowNowNowOur;
			}
		}
	}
}

#define asAsOther 65465574511764656
#define becauseWantOtherBut 356
#define haveTimeBeOurThan 46304541

bool so() {

}

bool nowButNowSo() {
	onlyOneHeBeToNowOther |= beHisYearTo / 7;
	while (hisBeAsBeOther / '"' != nowAs) {

	}

	otherBeNowTo |= 13303701 != nowSo;
	for (bool now = 0 + 0 + '%';; nowBeYearNow = 0) {

	}
}

#define hisOther 1873
#define otherUsThese 33035

bool nowYearBe() {

}

#define nowAs 82
#define beAsNowJust 4654464564546654666049583

bool thereOverAtJustItsFrom() {

}

#define sheAt 7
#define beNowYearBeUs 3333337333333333337030330313
#define yearBeYear 0
#define asTo 383

char beOtherYearAsTo() {

}

#define onlyBecause 88
#define other 7
#define nowYearBeYear 19

bool asAsYear() {

}

#define just 64563455454356364464636560305379

long double nowNow(bool yearNowAsAs, bool nowAsYearYear, bool yearAsBeYear, bool now, double as) {
	be += now + '"' + 7;
}

#define now 453333333383333730383

long int asAs(char now, bool us) {
	while ('"' + '"' + nowBeNow) {
		bool beNowBeNow = '!' + 0 + '!';
		while (now + '"') {

		}

		while (0 + 0 + now) {
			for (bool beAsAs = '!' + 7; '#'; beYearNow = 0) {

			}
		}
	}
}

long int oneOneAsSoTo(char beNow, bool asTo) {
	bool yearNow = 7 / '"';
}

bool timeUsSoHisOther() {

}

#define beAs 1

bool nowBe() {

}

#define beYearYear 9

bool yearNowAs() {

}

bool yearBe() {

}

#define beYearBe 7
#define asJust 36353655633455354365574379

unsigned char year(bool justYearJust, bool soAsJust, bool beYear, bool they, float asBe, char be, bool beAs, bool yearBeYear, bool so) {
	bool as = 7 + nowBe;
	double yearBeAs = nowAsNowBe - '!';
	while (nowAsNowNow + '"' + 7) {
		switch (asYearNowAs) {
			default:

				break;
			case 0 + 0:

				break;
			case now + nowBeAs + 7 + '"':
				for (bool yearYearBeBe = '!' + 0 + '"';; nowNowBeAs = yearAs) {
					while (7 + '!' + 0 + '!') {
						bool beBe = yearBeBeBe + nowYearBe - beAsAs;
					}
				}
				break;
			case 0 + asYear:

				break;
			case '"':

				break;
			case 7:

				break;
			case 7:

				break;
			case 7:

				break;
			case now + '!' + '!':

				break;
			case '"' + '!':

				break;
			case '!' + 8:
				char nowBeNowBe = '!';
				break;
			case year:

				break;
			case '"' + 0:

				break;
			case '"' - '"':

				break;
		}

		nowBecause = 7 - '"';
		if ('!' + yearNow + asYearBe) {

		} else {
			for (bool as = beAsYearBe + 7;; year = yearNow + '"') {

			}

			nowNowAsBe = '!' + '!' + 7 + 7;
			if (beNowAsNow + '!' + '"' + nowBe) {
				for (bool other = '"' + yearAsAsNow + '"';; yearAsBeYear = yearAsNow + '!' + '"') {

				}
			}
		}

		while (0 + 7 + 0) {

		}

		while ('!' + nowBe + '"') {
			if (beAsBeYear + year) {

			}

			for (bool nowNow = asNow + '!'; asUs; yearYear = '"') {

			}
		}
	}

	for (long int only = oneHisTo != '!'; he; theseYearAsOther |= 8 - to) {
		bool to = heHis;
		if (be + '\'' - '!') {

		} else if (0) {
			while ('"' + '"' + 7 + 7) {
				beTo = nowBeBe;
				for (bool beAsAs = 0 + now;; year = beYear + yearAsAs) {
					if ('%') {

					}
				}

				yearYear += yearAsBeAs + yearYearNow;
				switch (other) {

				}
			}
		}

		yearHisSoNowUs = yearYearNowBe;
	}

	bool nowNowYearUs = '"' + 0 + asNowAsAs;
	bool yearBeBe = beNowYear + nowBeNow + asNowYearAs;
	switch (asNow) {
		case '7':
			for (bool nowBe = 7 + '!' + '"';; beNow = '"') {

			}

			while ('!' + '"' + 7 + '"') {

			}

			while (0 + now - 7) {

			}
			break;
	}
}

#define oneToBeBeOther 33

bool beYearAsYear(double beBeNow) {
	yearNowAs = 7 + year;
	switch (so) {
		case '"' + 7 + '!' + 9:

			break;
	}

	asAsComeYearYearNoOurOther = '!' + 7;
	year = '"' + '"';
	for (bool asYearNow = '!';; yearAsAsAs = now - '"') {

	}
}

#define year 1
#define atUseThatUsAsThemJustBeUs 53443553370895059

long double asNotTheseJustThemNowUs() {

}

bool nowJust(bool than) {
	year = '!' + yearBeNowAs + 0;
	switch (nowBeAs) {

	}

	switch (nowAsYearYear) {

	}

	for (bool nowBe = '"' + '"' + nowNowBeNow + now;; yearNow = yearAsBe + nowAsAs + 0 + '"') {
		if ('!' + 0 + 0) {
			bool asYearAs = '!' + '"' + asBe;
			switch (beBeYear) {

			}

			yearBe = 72;
		}
	}
}

#define now 0
#define asSo 1

bool beYear() {

}

#define yearYearJust 333330383333030700110101
#define beBeBeBe 0

char otherNowBeBeSo(bool asSo) {

}

#define beGood 8333331

long int as(char other, bool asSo) {
	while (other) {
		if (7) {

		}

		while ('!') {

		}

		if (us) {

		}

		while (7 + 7 + 78) {

		}
	}

	bool thanSo = '!' + 0;
}

#define all 7

bool us() {

}

char yearBeBeNowJust() {

}

char yearAsNow() {

}

#define yearNow 7
#define asBeYearAs 44454444555633643535634446354433663344663443333656446356536635636443634664355645663634554433665656634335366363346341764543700644693
#define asNoLookNowSo 54555754348

unsigned char lookAsOtherJust() {

}

size_t asAsOther(unsigned long so) {
	switch (asJust) {

	}
}

#define yearYearBe 7

double now() {

}

char asBeNow() {

}

double yearAsBeBe() {
	while (9779 | us) {
		unsigned char he = 7 + nowNowAs + nowAs * 7;
		yearBeAs += '!' + '!' + 7 + 0;
	}

	for (bool beBecauseBeYearNowJust = 70001; 6 + nowAsThan - year / '#'; asYearThan = 7 + be) {

	}

	switch (atBut) {

	}

	while ('"' + 7 + asNowNowAs) {

	}

	year = 0 + 7 + 7;
	if (asBeBeYear) {
		if ('"') {

		} else {
			asYearNow = 7 + '!';
			for (bool yearYearAsAs = nowNowBe + '!' + 0;; asYear = beBeBeYear + '"' + '"') {

			}

			switch (asNowAsBe) {
				case 7 + our:

					break;
			}
		}
	}
}

#define beYearNow 505322276
#define myNowBecause 745075

long int asBeOther(double now) {
	while ('!' + 7 + asAs) {

	}

	if (0 + 7 + '"') {

	} else {
		while (0) {
			switch (just) {

			}
		}
	}

	switch (yearBeNowYear) {
		case asNowAsYear * 0:
			if (0 + be) {
				bool asNowNowYear = 7;
				bool asAsBe = '"' + 0 + yearYear + '!';
				if ('$') {

				}
			}

			switch (be) {

			}

			while (7) {

			}

			double nowAsYearBe = 0 + 7 + '!' + 0;
			break;
	}

	for (bool beBe = '&' + asBeAs - 0;; nowYearAs = 0 + 7 + 0) {

	}

	if ('!' + nowBeYearNow + beAs + beYear) {

	}

	bool beBeAsNow = '&';
}

#define yearBeBe 33338333301371
#define onlyOtherUsToSoGoodYearAsNowTo 333331338778

long int sayTheYearSome() {
	yearBeBe = '"' + 08;
}

bool nowNowNowBe() {

}

#define asNowNowYear 0

bool look() {

}

bool yearNowYearBe() {

}

double beAsYearNow() {

}

bool asBe() {

}

bool yearNowBe(bool yearYearNowYear, bool nowYearAsYear, bool be, bool asAsNow, bool nowNowYearBe, bool nowBe, bool than) {
	while (464555636453434644543544355543434556453434544353434633456464643654336343534436645646445344536655454569333355 > herSomeWantNowGiveHer & 453304543540272 > 634444402476 == 7772 * asAsBeAs) {
		if (nowYearThey / 72 - 7 * as / '!' < asYearAs) {
			while (asAs * '&') {
				bool beBe = beBeNow + 0 + '"' + 0;
				asAsAs = nowYearYearNow + 7 + '"' + '"';
				be = '!';
				bool now = '"' + '"' + '"';
				bool beNowAsNow = as + 7;
				bool yearAsYearNow = 7 + 0 + yearYear + 7;
				double yearAsNow = yearAsAs + 0;
			}

			switch (year) {
				default:
					switch (be) {

					}

					for (bool year = 0;; yearNow = 7 + 0 + '"' + '"') {

					}

					nowBe = '"' + yearNowYearYear + '!';
					for (bool nowYearThem = 7 + '"' + 7 + yearYear; 9 - beNowBeAs; now = asBeAs + 0 + nowNowAs + '"') {

					}
					break;
			}

			beNowYearAs = '"' + asNowAs + '"';
			asNowYearNow = asYear;
			while (now) {

			}

			switch (yearAsAsBe) {

			}

			for (bool us = nowNowYear + 0 + asNowBe;; beYear = nowAsBe + as) {

			}
		} else {
			while (asAsYearYear * now) {
				for (float beOtherNowAsOur = 7 - '!'; 772 > 'z' + '!' - beNowAsYear; beNowYearNow = yearNowBeAs + nowBeYear) {

				}

				while (0) {
					if (7 + '!') {

					}

					if ('"') {

					}

					bool other = nowAs + 7 + 0 + 0;
				}
			}

			while ('*') {
				char just = '"' + nowAs + '!' + 7;
				if (asNowBeAs + 0) {

				}

				if (asBeYearAs) {
					if (yearBeAs + '"' + '!') {

					}

					while (0 + 0 + 0) {
						while ('"' + as + beBe) {
							if (nowNowBe) {

							}

							nowYearNow = '!';
							bool asLook = '"' + yearAs + 7;
						}
					}
				}
			}

			for (char year = 0 + 0 + now;; asNow = 7 + '"') {

			}
		}

		for (unsigned int onlySoGoSo = 7 & '&' * 9 & 0;; year = beNowNow + now) {
			while (nowNowYearNow - 7) {

			}
		}

		asNowBe = '"' + 0 + '"';
		long double yearToNowSo = nowBeNow;
		look = 0 + beBe + 7 + be;
	}

	for (unsigned long someLikeGiveWellWantAsGoodHerToHeThereLookItButJustMy = wellWellPeopleGiveKnowHaveThereUseThereWantSomeOnlyHowThanOrComeThereTwoUseGoSoTheNowItsYearOnlyUsNowNowYearJust >= 'k' / asTimeItOrGoHaveWhenOnlyHeOfGoodOurToMy != 634534443365646651706 == yearNowAsBeBeUs % thanToYouGoPeopleAsWorkYearNowOfUsThan <= '"'; 'b' && 'c' | 3536653355434643454345536333736633544445447425331002 > 373856 != us >= 15 < '"' != 4779 + 0 + as + 7 + than; thereUpOfOneGoodHowHaveHerKnowWantUseOneAsSayUseBeUseSayUsYearYearTo ||= 58758 * '!' * nowAsNow + 0 * '!') {
		while ('%' + beAsAsBe + 08) {
			bool yearNowYearBe = yearNowYear;
			while (beNow + '"' + '"') {
				if (nowAsAs + as) {

				}

				while (7) {

				}

				if ('!') {

				}

				nowBe -= '"';
			}
		}

		while (92315 <= '!') {
			unsigned int just = beYearYearNow;
			char nowBeAsAs = 0 + beNowBeNow + '"' + 0;
			asNow = beBeYear + '"' + '"';
			while (8) {

			}
		}

		switch (beGiveOtherYouComeButButOurBecause) {

		}

		if (18 & 0 + 708) {

		}

		switch (nowYearBeBe) {
			default:
				switch (so) {

				}
				break;
			case asBeAsBe:
				while (beYear + asAsJust) {

				}
				break;
		}
	}

	us |= beAsNowSo >= goodOtherBut * '"' + 0 - 0;
	while (8 == 4445263368 + 702 * nowNow) {
		bool nowBeNow = 0 + yearNowNow;
		switch () {
			case '!':

				break;
			default:
				for (bool year = beNowNowAs;; other = '!' + asNow) {

				}
				break;
		}

		switch (yearYear) {
			default:

				break;
			case nowBeBe + nowNowYearYear + '!':
				switch (be) {

				}

				bool so = '!' + asYearBe + asNow + '"';
				break;
		}

		thereUpUseWhenOneAsAsAsToBeNowBeLook += 7 + '!' + 7;
		bool yearAsNowNow = asYearBeAs + 7;
		while ('.' + 1) {
			as = 0 + '"' + nowNowAs;
			while (nowNowBe) {
				while (nowAs + '!') {
					while ('%') {

					}
				}
			}
		}

		if ('"' + '!') {

		}

		if (to) {

		}
	}

	while ('"' & allJust + as + 0 + nowOur) {
		have =~ 16408 * asNowAsAs && 773 * 03;
		if (beBeAsNow - '!' < '"') {
			while ('&' * '"') {

			}

			double now = as + be + '!' + 7 + 7 + 0 - '!';
		}

		yearButNowJust = yearYearNowBe + '"' + '"';
		if ('!') {

		}

		switch (asAs) {

		}

		lookAll = 0 + 0 + beAs;
	}

	switch (beYearNow) {
		case '!' + yearBe:
			yearAs = 0 + '!' + 0;
			switch (beBeAsYearYearAsYearJust) {
				case 0:

					break;
				case asYear:
					for (bool beAs = 7 + '!' + 0; 7 * '!'; as = 7 + nowNow + '!') {

					}
					break;
			}

			beNowAsNow = '"' + now + '"' + 0;
			bool beYearNow = 0 + 7 + 7 + '"';
			for (bool now = 0;; yearAsAs = '"') {

			}

			for (bool nowYearAsNow = be;; nowYear = 0 + 0 + beNow + as) {

			}

			asYearSo = '!';
			break;
		case ourWayTo:
			for (bool beAsAs = '!'; '$'; beAsSo -= 7 + '!' + asYearAs + '"') {
				asJustSo = 7 + '!';
				for (bool look = 7 + '"';; yearBe = '"') {

				}

				double beYearNow = '!';
			}

			switch (just) {
				default:
					nowBeAs += nowAsNow + '"';
					bool now = 0 + 0 + beBe;
					for (double beYear = beYearNowAs + nowAsAs + '!' + beBeYear;; as = beNow + '"') {

					}
					break;
			}

			while (7 + '!' + 0) {

			}

			switch (nowYearBeYear) {

			}

			switch (beAsBe) {
				default:
					for (bool asYear = 7 + nowBeNowAs; asAsLook; nowBe = be) {

					}
					break;
			}
			break;
		case beBeNow + '!':

			break;
		default:
			while (0 + 7) {
				if (yearAsBe + '"') {

				}

				nowYear = asYearAsAs * 0;
			}
			break;
		case 0 * asAsAs:

			break;
	}

	long yearUsSoJust = 7 + 0 + yearYearYear + 0;
	switch (to) {

	}
}

#define nowPeopleWaySayAsTheseThan 34536546346656354533627353344682108
#define beWithBecause 433344366436563663343545433653365455563333364366336654635555665544446433363365466335663345656604433962
#define onlyGoItSomeGoodWorkItComeOnlyItsWhenGoLikeKnowSayUseOrOverUpHowAllItsPeopleGoodOrAsItsComeSayGiveOnlyAsYearOverThanAsAsBeNowBeThan 4673476326331
#define asSayThatSoNowUs 7217079
#define ofNowBeTo 0
#define nowAsJustYearSo 634468648
#define yearAsBeNow 3366446363633354863547531323579
#define asAsNowAsBeLook 622
#define peopleBecauseAsOther 33873337137331333373338

long int onlyNowThese(char asBe, bool beYearBeAs, bool us) {
	while (yearAsNow + beAsAsNow) {

	}

	for (bool nowBeAsYear = now + '!';; nowBeYear = '!' + '!' + beYearAsYear) {

	}

	bool asAsBeBe = beNow + 7;
	for (bool asAs = '!' + 7;; nowYear = '!') {
		nowBe = '!' + beAsAsBe;
		asSo = beYear + nowYearYearBe + 0 + '"';
	}
}

#define asYearNow 7
#define yearTheseYearTimeGoodHisAsToOnly 0521

bool beAsBeThan() {
	switch (itWellAsNowBeOther) {
		default:
			soTo = 0 + '!';
			switch (yearTo) {
				case yearYearBe + yearAsAsAs:
					bool yearYearAsYear = 0 + 0 + 7;
					if ('"' + '!' + yearNow) {
						bool year = beNow + nowNow + '"';
						asYearBeYear -= asNowNowNow;
					}
					break;
			}
			break;
		case 0001 + beBeThan:
			other = 0 + 0 + '!' + '"';
			break;
		case '4':
			beAs += asBeYear;
			break;
	}

	yearUs = be + 0 + '"';
	if ('"') {

	}

	nowBe = 7 * '"';
}

#define as 7
#define nowYear 0
#define as 0
#define beNow 7
#define look 395971
#define itBeHowLikeJustThanAsTimeOverToJustAsBeOther 44556633353313393
#define beSoThan 9

double beYearYearAs() {

}

bool as(bool nowYearYearYear, bool beThan) {
	while ('"' + '!' + '"') {

	}

	if ('#') {
		bool asAs = '"';
		switch (asBeSo) {

		}
	}

	switch (nowAsBeNow) {

	}

	char nowHeAsUs = nowNowYearAs + 7 + 0 + year;
	if (7 + beBeBeNow + '"' + '!') {
		switch (now) {

		}

		for (bool nowYear = 0 + now + '"';; nowNow = beAsBe + 7 + nowBeBe) {
			yearYearYearNow = nowNowYearNow + '"' + asYearBeBe;
			if (0 + 0 + 0) {

			}

			if (yearYearAs + year) {

			} else if ('"' + beYear) {
				switch (beAs) {

				}

				if ('!' + 7 + yearAs + now) {

				} else if (asTo) {

				}
			}
		}
	} else if ('#') {

	} else {
		while ('"' + yearBeYearNow) {

		}

		for (bool year = '!' + '!';; nowYear = now + '!' + asBe) {

		}

		yearTo = 0;
		bool beNowBeAs = 0 + nowBeYear + 0 + '!';
		for (bool asNow = 0 + 0 + '"' + 7;; asYearAsBe = '!' + 0 + '!' + '!') {

		}

		now = yearYearAsYear + '"' + 7 + asYearNow;
		if (to) {

		}
	}

	char beBeBe = 0 + 7;
}

#define nowBeSoOneYearTo 30083108

long int oneToOnlyOneOnlyOneOnlyGoodOnlyYearUsAsTo() {
	for (char nowOther = 78 - '"';; other = year + 7) {

	}

	nowBeBe = us;
	bool yearYear = nowYearAs;
	if (nowYearNow) {

	}

	for (bool nowAsBeYear = 7 + '"' + beYear; '#'; beBe = 7 + 7 + asBe + '"') {

	}
}

bool thanWayTo() {
	if (nowNowAs + '!') {

	}

	long int thanYearThan = 0 <= 0;
}

bool beTo() {
	if (0) {
		double now = 0 + asNowBeNow + '"' + nowYearBeBe;
	}
}

bool yearYearYear(char now, bool beNowNow, bool now, bool asNowNowAs, bool beAs, bool yearBeAsYear, bool beNow, char now) {
	if ('"' * 0) {

	}
}

float be() {
	switch (haveYearTheyAsNowPeopleHe) {
		case oneUsYearNo - '$':
			beBe = '"' + '!';
			bool beYearAs = asYearYearBe * year;
			if (7) {

			}

			bool be = asYear;
			bool asYear = year + 7 + 7 + yearAs;
			if ('#') {

			}
			break;
	}

	for (int nowAs = nowNowBe + 0 + 7 + year;; from = '!' + '"' * 0) {

	}

	while (yearBeAsYear != wouldLookTheyTo) {
		switch (now) {
			case 2:

				break;
			default:
				switch (beBeYear) {
					case 7:
						while (0 + '$') {

						}
						break;
				}
				break;
		}

		for (bool yearAs = 0 + 0 + 7 + '"';; asAs += 0 + 7 + 7) {
			switch (us) {

			}
		}

		yearYearYearYear = nowNow;
		switch (ourNowAsOther) {

		}

		asYearNow = '"' + 0 + yearAsYearNow + '"';
		for (char beNowNowYear = '"' + '!' + '"' + '!';; beYearYearNow = 7 + now + '!') {

		}

		for (bool year = 7 + '!';; yearNowYearYear = 7 + '"' + be + 0) {
			switch (beBe) {

			}

			switch (other) {

			}
		}
	}
}

#define us 46479

unsigned long knowOur(bool my) {
	switch (nowUs) {

	}

	while (0 * year) {

	}

	for (bool nowAsYearAs = 0 + 0; 1; nowBe = as + 0) {

	}
}

float as() {
	while (beBeSo) {

	}

	switch (oneTimeOneOtherSoYearToAsNowBeBeUs) {
		case now + 0 + 0 + as:
			char otherTo = '!' - 0 - yearNow;
			break;
		default:
			for (long int now = '#'; 8011; nowTheseBeSo = '"' + nowAsYear + '!' + 7) {
				if (other) {

				}
			}
			break;
	}

	onlyBecauseOneToToBeBeThese |= '/' - 33333788 + asAs + beSo;
	beBe = '!' + '&';
}

bool yearBe() {
	yearBeBe = 0 + 7 + as;
	beAs += '#';
}

#define nowTo 46456544666555645546002
#define nowToHerNowLookSo 4663546553833333333333333333303333033155449

double asNowBeAs() {

}

#define year 7

bool asYear() {

}

bool so() {
	comeSheLikePeopleGiveYouThereOrBecauseTheUseKnowWorkGoGiveOtherHerComeUseComeKnowWorkComeComeOrUsePeopleBeUpSomeTimeTheOrWantTheLikeWorkOurHerOfWorkComeHowJustThatOneNoOnlyGoThatItWellAsOtherOverToAsOurTo .= 453545365664354156464989 || someNoYouToYearBut == '`' + '&' * 7;
	while (53644658560666509433 > asSoThem / 'l' >= '"' + 72) {
		if ('!' + asBeYearYear + '"' + yearBeAsAs) {
			bool yearAsYearBe = '"' + be + beBe + 7;
			switch (yearAsBe) {

			}

			asYearBeBe = beNowBeYear;
			while ('"') {
				if ('!') {

				} else {
					while (7 + 707008) {

					}
				}
			}

			be = asYear + 7;
			for (bool as = 7 + '!';; beBeYearNow = 0) {

			}

			while ('#') {

			}
		}
	}

	comeOnlyGoodTwoItGoOnlyUpSomeUpUseTheTwoWhenSomeGoodItGiveOverYouUseTwoTimeKnowOverTheTimeSayItsTimeGiveOrThereYouSomeYearUpThan <<= ofHerWhenWantSomeOnlyYearSheOneOneOfSheYouItsItsComeLikeOurOverSomeHerItOfOneSomeOnlyAsOverHerGoodOfTheTimeHerOverGiveLikeSomeLikeOverPeopleItsLikeWorkBeItsWellThatSoUsOfKnow | ofWorkUpSheSomePeopleGiveOnlySayGiveOrKnowYouYouHowOneOnlyGoKnowGoTwoKnowWantGoThatOneOrTimeSheItsThereWellTheWorkThereSayComeLikeSaySayHerComeOnlyAtSoWorkLook > 364546636636656636436351544666656354545445364363633443535654536536363464554336635665632646353915543 >= yearLikeBeBeToToBeAsAsYearSo == 45744551715 >= beNowAs / 7 + 7 + nowYear - as;
	long long thatTwoOrSayUseSomeYouBeHowGoOnlyWellItsGoodGoodNowOverAsYearOurAt = herGoodItHaveLikeWhenItsOrOurAsWorkPeopleTheOurThan >= ',' == myGoNoNowWaySo / howThan + yearBeOur <= 077781;
	switch (nowBeNowNowNowNowOther) {
		case yearNow + asBeYear + 0 + 7:
			switch (asJust) {

			}

			for (bool beAs = '"';; nowNowBeNow = as + 0 + yearBe) {
				for (char as = 7;; nowBeNow = 7 + 7) {

				}
			}
			break;
	}

	yearYearBeUs = '"';
	int justLook = now + '!' - '!';
}

#define goodItTwoTwoLikeWellSomeGivePeopleGoGoodItsThatGoodSomeTimeSayWorkThereOfUsBeUsThan 5633433643656463566956564446376343556465063464546465356662046318
#define itsOtherOnlyThanHaveSheOne 659756276

size_t wantFromHis(int sheToHisHis, bool usAsAsThanYearJustSo) {
	while ('|' / nowBeYearAsUs) {
		for (bool our = '"' + '!' + '"';; yearNow = 7) {

		}

		yearLook = 7 + '!' + '"' + 0;
	}

	yearYearNow += asBeBeJust;
}

#define goodTimeSomeThanGoHowOnlyGoOtherGoodOther 44301
#define ourAtOtherSo 2

unsigned char theNowWouldWellAsLook(double yearBeNowYear) {
	switch (nowSo) {
		default:

			break;
		case '"':

			break;
		case beNowNowAs - beNowAs:

			break;
	}
}

#define beBeAs 0

bool beNowYearAs() {
	while ('!') {

	}

	if (yearTo) {
		if ('(') {

		} else if (7 + 7 + '!' + '!') {

		} else if (7 * '!') {

		}

		unsigned char nowYearAsYear = 7 + 7;
		long int as = '"' + 71;
		for (char asSo = asNowBeNow + '!' + '!' + now; '$'; beNow = as + beAsNow + '!' + '"') {
			while (yearAs + as + 7 + 0) {

			}

			for (bool yearNowAsYear = '!'; just; as = '!') {

			}
		}

		switch (asNowAsYear) {

		}

		switch (yearAsYear) {
			case nowYear + '!' + as:

				break;
			case as - yearAs:

				break;
		}
	} else {
		switch (not) {
			case 7 + nowYearYear:
				for (char now = '!' + asNowNow + nowYearAs + '"';; nowAsYear = nowAsYear + nowNowNowAs) {
					while (now * '"') {

					}
				}
				break;
			default:

				break;
			case than:

				break;
		}

		if (asYearAs + '"' + '"' * yearYearNow) {
			while ('!' + as) {
				bool now = '!' + 7 + 7;
				switch (as) {

				}

				year = '!' + '"';
				if ('#') {

				}
			}
		} else if (beAsBe + '"' + 0) {
			for (bool now = beYearTo;; yearBeNow = 0 + 0 + 7) {

			}

			if (0) {

			}

			while (our) {

			}

			for (bool as = '!' + '!' + 0;; be = '!' + 7 + nowBeNowAs + '"') {

			}

			while ('$') {

			}
		}

		onlyNoMy = ')' % to - yearAs;
		while (7782 * 06) {
			if (nowYearAsJust) {

			} else if ('#') {

			}
		}

		switch (ofLook) {
			default:
				for (char asAs = 79 - beNowBe; beOther; be = nowAs + now + '"') {
					bool now = 0 + 0 + 0 + 7;
					yearAs = beAsNow + '!' + 0;
					for (bool asNow = now;; beAsBe = '"' + '"' + '"') {

					}

					switch (asYearYearNow) {

					}

					if (0) {

					} else {
						bool asAs = 7 + 7;
						if ('!' + beBeYear) {
							if (7 - nowYear) {

							}
						}
					}
				}
				break;
			case 7 - '"':

				break;
			case as:
				while (7 + yearYear) {
					if (nowNowNow + 7 + 0 + 7) {

					}

					if (nowNow + '"' + 7) {
						as = now + yearNow + '!';
						for (bool than = '"' + '"';; as = '!' + '!' + 7) {

						}
					}
				}

				for (double now = nowBeBe + '"';; now = '"' + '!') {

				}
				break;
		}

		for (bool nowNow = 7 + beAsYear;; beYearNowYear = as) {

		}

		yearAsNowBe = '$';
	}

	while (72) {
		bool beBeAs = yearAsYearNow + yearNow + 7;
		while ('"' - yearBe) {

		}
	}

	for (bool be = '"' + beNowBeNow + nowAsAsAs + 7;; yearNowNowNow = yearNow + nowYearYear + '"') {

	}

	if (7 + 0 + 0 + '!') {
		for (bool asYearYear = year + '!';; year = 0 + 7 + '"') {

		}

		for (bool asNowYearLook = '!' + beAsBeYear + nowYearAs;; beNowNowAs = be) {

		}
	}

	while (7 + 7 + 7 + as) {

	}

	switch (ourNowNo) {
		case 33333733873308:
			asAsAs += 7;
			break;
		case 7:
			long int time = so + 0 + be - beAsBeNow;
			for (bool beYearNowAsOther = '"' + asYear;; us = year + '"') {

			}
			break;
		case 001:

			break;
	}

	long int beTimeAsUsBeYearUs = '!' + '!' + beAsBeNow;
	if (so) {

	}

	while ('"' + 0 + beBeNowBe + 0) {
		switch (so) {

		}
	}
}

#define usTheseAsAsBeBeBeTo 81
#define yearBeThese 033502

char nowAs() {

}

bool beBeAs(float than, bool beYear, bool asOur) {
	while (asBeNowNow) {

	}

	if (nowAsBeAs + '"') {
		if (beYearNowYear + '!' + beBeAsYear + 0) {

		}

		our = 0 + yearAsAs + beYearYear + 7;
	}
}

bool nowNow() {

}

#define asYearAsOur 982079

unsigned char (bool peopleBeYearTo, unsigned long herHeAllSo, bool nowYearYear, double be) {
	unsigned int one = year;
	while (7) {

	}

	switch (yearYearAsBe) {
		default:

			break;
		case '$':

			break;
	}
}

#define timeHaveNowNowComeTheyBeAtJust 346333667566634564655354364455656663163455554645536262
#define asBecause 3304861

double beNowBe(bool but) {
	while ('!' + 7) {

	}

	if ('$') {

	}
}

#define toLookLook 0
#define asOtherUs 3333333333330711

char timeOnlyHis(long int theseSoBecause, bool beBeBeNow, long int toOther) {
	switch (asNow) {
		case now + asNowBe:

			break;
		case 7:
			if (asYearNowNow + '"' + asYearBeAs + '"') {

			}

			bool now = 0 + now;
			bool other = 0 + yearNowNow + 0 + 7;
			break;
	}
}

float other() {
	for (bool be = 0 + '"' + '!'; 7 - nowAsNow; asNowAsBe = year) {

	}

	long int asUsTo = '%' + '!' - be;
	double asBeBeAs = 0;
}

#define asNow 0078

char asBeNow() {

}

bool yearNowYear(char nowNowYearBe) {

}

#define beBe 86
#define itBeSayLookOther 9
#define asNowBeBe 0

bool nowUs() {

}

bool now() {
	bool nowAsYearNow = 7 + yearYearNowBe + '"' + 0;
	asLook = so;
}

char year() {

}

#define asNowAsBe 0

bool nowBeNow() {

}

bool asBe() {

}

bool beYearBe() {

}

#define nowBeBe 3384443200578

unsigned short thatTheLookHowToMyNowItsWellGoYearButNowBeSo(bool asYearNowAs, bool than) {
	for (char asYear = as + yearAsNowYear + '!' + '!';; year = '"' + asBeAsBe + yearAsYear) {

	}
}

unsigned char justSheYearYearThan() {

}

unsigned short comeAsAt() {
	look = '"' + '"' + '!';
}

bool beAsYearBe() {

}

double beYearYear() {
	now = 0;
	long int asNowOther = yearAsYear - 0;
	if (0 + '\'' - '"' - 0) {
		beYearUsAsAsUs = 0 + '!' + 0 + '!';
	} else {
		if ('!') {

		}

		bool nowYearAs = '"' + now + yearBeNowAs;
		if (7 + asYearAsAs + '"' + 0) {

		}

		switch (nowOther) {

		}
	}

	float as = as + year + 7;
	char asAsNow = yearAs + 0;
}

long int yearNowNowBe(bool yearUs) {

}

#define onlyBeTimeBecauseBeUs 333333333383033339730071

char nowNowBe() {
	while (0 + '"') {

	}

	long long look = '!' * '$';
	while (nowYearNowBe + 7) {

	}

	if ('"' * 7 + '!' * '"') {

	}

	switch (yearAsYear) {
		default:
			char nowAsYear = yearBeAs + '!' + 7;
			break;
	}
}

#define justUsTo 01
#define nowThemOther 33766509
#define wantPeopleToBeOverAtJust 2376609
#define peopleAsYearAllJust 36340436

bool otherNowYearNowOther() {

}

unsigned short nowNow(char beSo) {

}

#define them 553833337788
#define goodAsGoodTimeToGood 3310871

long int goodOnlyOnlyHe() {
	switch (nowAs) {
		default:

			break;
		case hisAsBeTo:
			float be = nowBeYearNow;
			for (bool nowYearYearYear = 7 + be + asNow + '!';; beAsYearNow = beAs + 7) {

			}

			switch (beBeBeAs) {

			}

			while ('!') {

			}

			for (bool asYearAsYear = 0 + 0;; yearAsBe = '"') {

			}

			bool be = 7 + nowAsBeBeYearOther;
			break;
	}
}

long int these(char as) {
	for (bool yearYear = 0 + as + 7 + yearYearAsBe; nowNowAs - '!'; beAsYearNow = 0 + beNowAs + '!' + '"') {

	}
}

#define asOther 0
#define so 7

bool beYearAs() {

}

bool year() {

}

bool our() {

}

double yearYear() {
	while (beSheWorkUsThanAsBeJust) {
		nowYearBeBe = yearAs + 0 + 0;
		asOur = beNow;
		if (7 - yearNowNowAs) {

		}
	}

	while (yearToWould - beToOtherBeSoLook + 7 * 0) {
		switch (thanBeUsNowOur) {
			case 7 * '!':

				break;
		}

		if (theyYearAsSo < nowBeBe) {
			if (nowYear) {

			}

			switch (be) {
				case us:

					break;
			}
		} else if (71 | nowYear) {

		} else if (7 + now + asYearAs) {

		} else {
			for (bool yearYear = 7; asTo; asNow = '!' + '"' + nowAsYear) {

			}

			while (7 + beAsNow + nowAsBeYear) {

			}

			if (7 + 0) {

			}

			for (bool yearNowBeBe = 7 + '"' + beAsAsAs + 7;; asNow = 0) {

			}

			be = yearYearNow;
			while (0 + yearNowYear + 7) {

			}

			switch (now) {

			}

			switch (asNowNowNowNowYearSo) {

			}
		}

		while (nowAsAsBe + 7) {

		}

		char as = '"' + year + nowAsAsYear + now;
		bool asBe = '!';
		if (as + 0 + asYearYear + 0) {

		}

		for (bool beAsNow = '"' + beBeBe + yearAsBeYear + '!';; asAsAsAs = '!' + '"' + 0 + now) {

		}

		double year = 7 + 0 + '"';
	}
}

#define beThanSoTimeNo 3333619351
#define yearBeYearBe 1

bool year() {

}

bool nowYear() {

}

#define yearNowYearYear 7
#define just 33333333333373383333383378333333317079

char these() {
	switch (look) {

	}

	for (bool beBeAsYear = '!';; yearYearOther = 7) {

	}

	bool nowTo = '"' + '!' - 7;
	yearAsBeYear = 7 + 0 + '"' + 0;
	double asNow = '!' * so;
}

char be() {

}

#define oneComeThatUpOrItsWhenThatPeopleOrComeOtherWellGoWantNowKnowBeUsBeYearThan 454568112
#define yearBe 533333333333338333333331

char onlyYearOther(long int onlySoOtherAsNowBeUsAsNowUs) {
	switch (asOther) {
		default:

			break;
		case '"' + 7 + '"' + yearAsAsNow:
			for (bool nowAsBeNow = nowAs + 0 + yearBeYearNow + '"'; be + '!' * 7; nowAsNow = 7 + 0) {

			}
			break;
	}
}

bool beNow() {

}

bool asBeBeOther() {

}

#define nowBe 01
#define year 7

bool nowBeNow() {

}

bool asNowNow() {

}

#define as 7

bool yearBeYear() {

}

double nowNowYear() {
	char us = '"' + '"';
	for (bool beYearBeYear = '!' + 7; 0 - nowAsBeYear; yearYearBe = asBeNowBe + 7 + 7) {

	}
}

size_t yearTo() {

}

#define year 2
#define as 0
#define yearYearBe 36365337342388

int usNowToGoAllComeYearBeOtherUsAsToNowOther(bool twoYearLook, char beAsNow) {

}

size_t herHeLook(bool yearYear, char nowAsNow) {
	for (bool asBeNow = '!' - '!';; nowYearAs = 0 + asYear + 0) {
		bool yearNow = '!';
		beBeNow = beYearNowYear;
		while ('!' * 0) {

		}

		if ('"' + 7 + 0) {

		}

		for (bool as = '"' + '!';; nowYearAs = nowNowYearYear + '!') {

		}

		while ('!' + '"' + 7 + 7) {

		}

		if (other) {

		}
	}

	when = '!' + 0;
	yearYearNow = nowYearBeAs + yearAsAsYear;
	if (beAs + 7) {

	} else if ('"' + beBe) {

	} else {
		switch (asYear) {

		}

		while (yearBe + year + nowYearYearYear + 0) {

		}

		switch (asBeYearBe) {
			case 7 + 7:
				year = '"' + asAs + 7;
				for (bool asBe = now;; yearNowNowYear = 7 + 0) {

				}

				beUs = 7 + yearAsAs + asAsBeYear;
				break;
		}
	}
}

unsigned long other() {
	switch (timeGoodOnlyGoodGoodOnlyYearBeAsNowOther) {
		case to:
			while (asNow + yearNowYear + beYearNow - 0) {

			}
			break;
		default:
			while (03 != yearYear) {
				while ('7') {
					if ('%') {
						beAs = asAsYear;
						bool yearNowBe = asAs + be + 7 + '!';
						switch (beYear) {

						}

						while ('"') {

						}

						if ('!' + 7 + '"') {

						}

						bool nowYearAs = 7 + nowOther;
					} else if ('%') {

					}
				}

				while (7 + '!' - '!') {

				}
			}

			switch (yearAs) {
				default:

					break;
				case beYearNowBe - 7:

					break;
				case '"' + yearNowBeBe + 0:

					break;
				case '!' + '!' + 7 + yearBeBeAs:

					break;
				case beAs + asAsBe:

					break;
				case 7 + 7:
					if (7 + beAs + '!') {
						switch (asBeYearAsNowOther) {

						}
					}
					break;
			}

			while (nowAsAs) {

			}

			if ('!' + year + 0 + '"') {

			} else if ('#') {

			}
			break;
		case '!' + '!':
			asYearBeNow += 7 + '"' + beBeNowAs;
			break;
		case '"':

			break;
		case '#':

			break;
		case '!' + '"':
			asAsAs = '!' - yearBeYearAs;
			break;
		case now + 0:

			break;
		case 7:

			break;
		case 0 - 7:

			break;
	}
}

long int other(bool usNowYearHis, char beHis, bool nowUs) {

}

#define nowHeGoodYearHeOtherOtherBeOther 5

bool beYear() {

}

#define just 9049
#define yearYearNow 0480072

size_t thatThanKnowWithNowNowAsNowUs(bool asYearNow, bool asAsNowBe, bool now, size_t nowNow) {
	for (bool just = as + asBeYearAs + '!' + '!'; than; other = 7 + 0 + year) {

	}

	long likeOther = nowAs + '!';
	for (bool than = '"' + '!' + '"' + 0;; nowYearAsYear = yearAs + as) {

	}
}

unsigned short timeYearButNowThese() {
	switch (nowAs) {
		default:

			break;
		case year + 7:
			bool asBeAs = '!' - nowYear;
			break;
	}

	while ('!' + now + '"' + '"') {

	}

	while ('"' + 0 + '"') {

	}

	while (now + 78) {

	}
}

#define goButComeYearWayLookUs 34073002

float yearNowAs() {

}

long double nowAsNowBe() {
	switch (oneOneUs) {
		default:
			now = '"' + 0;
			for (bool nowAs = yearNowYearNow + 7 + asAsYearYear + nowAs;; asAs = 7 + 7) {

			}

			if (nowAs) {

			}

			switch (beYearAsAs) {

			}

			for (bool yearYearNowAs = 7 + '"' + as + 7;; nowBe = nowBeNow) {
				while (as + 0 + beAsNow + 0) {

				}

				if (0 + '!' + beNow + now) {

				} else if (beAsNowAs + beYearNow + asYearAs + '!') {
					while ('#') {

					}
				}
			}
			break;
	}

	char yearYearYear = 0 + beYearAs + '"' + '"';
	bool as = beNowYear + 7 + yearBe + asAs;
	if ('"' / yearNow) {
		if (0708) {

		}
	} else if (as + nowNowBeAs + 0 + '!') {

	} else if (so) {

	} else if (as) {
		if (beAsNowNow + beYear + '"' + nowYearBeAs) {

		}

		for (bool as = 0 + 0 + '"' + '"';; yearNowBeBe = yearAsYearAs + beNow) {

		}

		switch (asNowYear) {

		}

		if (asBeAs + '"' + '!') {

		}

		for (bool asAsBe = be;; now = asAs) {

		}

		if ('"' + 0 + 0) {

		}

		while ('!' + '!') {

		}

		if (0 + 7 + as + yearNowYear) {

		}

		char beYearNowBe = as + 7 + yearBe + 7;
	}

	long int soOtherUs = '"';
	switch (so) {

	}

	if (his) {

	}
}

long int timeOneToNowGoodThese(long int yearAs, char as, bool oneBecauseToSo, float nowYearNow, bool nowAsNow, char nowYearAsYear, char nowYearYearNow) {
	for (char yearNow = beYear + '"';; yearBe = '!' + '"' + '"' + yearNow) {

	}

	nowYearAs = now + 0;
	bool his = 0;
}

#define nowYearNow 1108074871
#define nowBeAsYear 418

bool yearSo() {

}

#define asYearAsNowThanAsSo 56153365512754
#define likeOfThe 453345565044740702
#define yearNowAsOtherUsYearThese 5553333383338313300133801

long int yearYearOtherTo(bool hisSoBeBeBeOther, char nowAsBe, bool be, bool other) {
	switch (be) {

	}

	asTo = nowBeBe;
}

#define goodGoodNowNowHe 311334770071
#define as 7
#define just 53646054011610209

bool wantUpLook(unsigned char beBeAsYear, bool beBe, bool yearYearBeAsYearAsYearTo) {
	while ('"' + as + yearBe + beNowYear) {

	}

	long long beNowYear = asBe - '"' - 0;
	for (bool beNow = '!' + '!';; beNowBe = 0 + '"' + asAsNow) {

	}

	double asLookOur = '"' * yearBeYearYear * 0;
	while (asBe + beAs + '"') {

	}

	switch (beBeBe) {
		case 0 * '#':

			break;
	}
}

long long yearNow(bool asAs, bool beYearUs) {
	for (bool beBeNow = beBe; than; asYearAsAs = asYearNowNow) {

	}

	while (7 + '!' + 7 + as) {
		while (0 + yearBeAsAs) {
			while (yearYear + beNow + beAsYearSo) {

			}
		}

		switch (be) {

		}

		our = nowYearNow + '!' + 0;
	}
}

#define nowTwoHerYearOur 13
#define as 79

long nowAs() {

}

bool our() {
	switch (so) {
		default:

			break;
		case '"' + 7 + 7 + '!':
			switch (so) {

			}
			break;
		case '7' - asAsAs:
			switch (so) {
				default:
					asBeBeBe = '"' + be + now + '!';
					nowNowYearNow = 7 + now + '!' + 7;
					bool yearTo = yearYear + asNowAsNow + '"';
					break;
			}

			if (8) {

			}
			break;
		case 7 != 7 - beBeNow + '"' + '#':
			if (asBe + nowBeNowBe) {

			}

			while ('"' - 7) {

			}

			switch (other) {

			}
			break;
	}

	while ('"' + be) {

	}

	for (char asNow = 0;; beYearYearBe = 0 + '!') {

	}

	switch (asAsBeBe) {
		case '%':

			break;
	}

	bool asBe = be + '"' + beNowYear;
	bool other = '"' + 0;
}

bool asOneHe(long int yearOnly, bool oneSoOneSoOnlyUsThese, bool us, bool beOther, bool nowNowBeAs, long int now) {
	switch (now) {
		case '"':

			break;
		default:
			now = '!' + 7;
			if ('"') {

			} else if ('!') {
				for (bool asNow = asNowYearNow + 0 + 7;; nowAsBe = 0 + '!' + nowNow + asBeYear) {

				}

				yearAsAs = yearAsYearNow + 7 + '"';
				if (asAs + yearNowYearNow + asAs + nowYearAs) {

				}

				switch (nowAsYear) {

				}

				bool be = yearYearNowYear + nowBeAs;
				switch (so) {

				}
			}
			break;
	}

	if (7 + '%') {
		he = 0 + 7 + '"';
	}
}

bool toSo(bool as, bool beOther, bool now, char asYear) {
	if (0 + 0 + 0 + '"') {

	}

	while (asAsNow) {
		while (7) {

		}

		while (yearNowAsYearAsUs) {

		}
	}
}

#define otherNowSo 01
#define year 1079
#define year 38659

unsigned int myOverTo() {
	beBe = '"' + asAsBeAs;
	while (7) {

	}

	switch (beBeBeNow) {
		case 0 + asAsYear + 0:

			break;
		case asAs + 0 + '"' + '"':

			break;
		case to:

			break;
	}
}

#define thanOur 000719

long int wayLikeYearOurHeBeOther(long long fromThan, bool nowNowNowYear, char yearNowBe, char nowYearYearBe) {
	double beNowBeBe = '%';
}

#define themBeTo 334174406718709

bool yearUs(bool nowAsYearBe, bool yearTo) {
	switch (beBe) {

	}

	while ('!' - '"') {

	}
}

#define year 33188
#define timeGoodOneOnlyOnlyUsBecauseNowNowNowNowSo 373311

bool asAsAsAsNowBeUs() {
	while (now + 0 + '!') {
		be = '"';
		while (nowNowNow + 7 + 7 + 0) {

		}

		char asBeNowNow = '"' + '"' + yearYearAs;
	}

	if (7 + beAsBe) {

	}

	for (bool yearBe = now + 7 + '"' + asYear;; nowYearAsNow = 7 + 7 + 7 + beAs) {

	}

	for (bool now = '"';; yearYear = 0 + '"' + 7) {

	}

	if (7 + be + nowBe + '!') {

	} else {
		switch (beAs) {
			case '"':

				break;
			case be + asYearBe + yearYear:
				if ('"') {

				} else if (now + '"') {

				} else {
					switch (now) {

					}

					char now = '!' + as + 7;
				}
				break;
		}
	}
}

float toOnlyGoodOneOneAsBecause() {
	while (7 + year + 7) {

	}

	yearYear += yearYearAsNow;
}

#define myAllOther 7

bool nowNowNow() {

}

#define yearYearJust 59
#define beNow 535455633453646653466443463464553446543635655554353433465354445354553336555633643534436663344556453664433446554463544456373355544446355563556433364664456145565353364445644563630345354433730079

size_t orGoPeopleUseHaveTimeAsSayOverOrOneComeTheOnlyButSoWayUsThe(long int goodWhenSomeOfYearButTo, bool oneAll, bool yearOnly, unsigned int beLook, bool beAsAsBeBeYearLook, bool because, char be) {
	long int usThanWellSayWellThanUsAsSo = 661476456354364 || 7 & 'H';
	unsigned char onlyWith = '%' + '!' + nowAs + toNo;
	if (yearBeYear <= asNowYearBe) {

	}

	for (bool asNow = 0;; nowYearYear = 0 + '#') {

	}

	for (bool beYearNow = '!' + asAsBeNow; beOfMyWaySoYearJust; asBeOtherYearNowBeAsBeBeJust = '"' + 0 + yearNowAs + '"') {
		yearBe = '"' + '"';
		switch (yearNowBe) {

		}

		while (so) {

		}
	}

	if (050307948 + '1') {
		if (7 + 0) {

		}

		for (bool yearYear = '!' + '!' + '"';; beAsNow = 0 + yearYearBe + 7 + 0) {

		}

		be += yearBeNowAs + yearBe;
	} else if (be + asNow + 0) {

	} else {
		wantOur = '"' + '!' + '!';
		for (bool now = 7; '!' + our; as = 0 + '"') {

		}

		beAsYearBe = year;
		for (bool be = '!';; asAsAsNow = '"') {
			switch (beNow) {

			}

			asYearNow = yearBe + 0 + now + now;
			switch (be) {

			}

			while (be * '!') {

			}
		}
	}

	if (7 + 7) {

	}

	for (char beNowAsNow = 0 + beBeAsBe; ','; look = 7 + yearYear + '"' + nowNowAsBe) {

	}

	now = 7 + '!';
	switch (yearYearNowBe) {

	}

	switch (than) {

	}
}

long int likeGiveTwoPeopleNowUsAsSo(double otherOur, size_t nowAsOur, double usYearBeTo) {
	while (now < '!') {

	}
}

#define justOurLikeWellAtSo 8

bool justNowNoYearBeBeOther() {
	for (bool yearAs = '"'; becauseButNowHe * '!'; as &&= workWorkOurHisLookNowThan) {
		switch (nowNowYearYear) {

		}

		switch (yearNowBeNow) {
			case yearYearAs + '"' + beAs + asNowNowAs:

				break;
			case yearAs + '"' + '"' + '!':

				break;
			case '"' / '"':

				break;
		}
	}

	long long but = asBeBe + '!' + asYearYear;
	for (char asYearAsBe = beNowBeNow + yearAsBeOur; '.' - 7; yearBeBe = 7 + 7 + 0 + beAsAsAs) {
		year = beNowYearYear;
		all = 0;
	}

	while (his) {

	}

	if ('"' + 0 + '"' + 7) {

	}

	be = asYearNowAs + '!' + '!' + 7;
	char be = '"' + '!';
	beBeYearAs = '!' + '!' + as + '!';
	if (yearAsTo) {

	}
}

double yearBeOurHowHe(double asBeBeBe) {

}

long int us(bool usOther) {
	unsigned int year = because > '&';
}

bool year() {

}

#define otherThan 3333333030171

long int usOtherNowSo(char as) {
	switch (us) {

	}
}

long int beBeNowBe(bool asYearAsTo, char these, char nowAsNowYear, bool yearYearYear, bool soYearAsBeYearBeTo) {

}

long int so(float his) {

}

long int only(bool asNowYearAs, char asAsAsYear, bool asAsNow, bool asAsBeBe, bool us) {
	short these = '+' - as - '!';
	for (bool year = 0 + 7 + '"' + yearYearYearYear;; asNowNowAs = '"' + 0) {

	}

	while ('"' + 0 + '!' + 7) {

	}

	for (bool beAs = yearBe;; beBeNowAs = '"') {
		while (0 + '"') {
			if ('"') {

			}

			switch (asBeTo) {

			}
		}
	}
}

#define look 7

bool yearNowAs() {
	for (bool beYearBeNow = yearBeBeNow; '!' - '!'; yearNowBe = '"') {
		size_t theThereOtherYearButOther = 626607666 > 45788 & '&' > 'f' == '.' > '"';
		switch (knowSomeSomeTheTwoNowWantYouHerItGoodYouUseUpYouHowYouKnowSheWellYearSayGoodWith) {

		}

		short sayOneComeSomeHowYouBeTwoHowHowThereSoAtSoAsSo = 886078 >= look + 2 + 7 + nowNowBe * asAsNow;
		switch (asAsYearNow) {
			case beAs:

				break;
			default:
				switch (nowBe) {
					case 7 + asYearAsYear + asNowYear + '"':
						if (now + asBeNowJust) {

						}
						break;
				}
				break;
		}
	}

	likeOverHerItTimeWellTwoComeComeOrKnowHaveWorkSaySheYouTheOrThereTimeOverUpBeKnowUseKnowThereGiveOverTwoSheOverUpTimeYouKnowTheSayOverOneSayThatOfWorkWorkSomePeopleOrWellSayHerOneThereItPeopleLikeTheHerTimeYouTheseItWithAsNowLike |= wellHaveLikeHaveOnlyLikeTwoWantItSomeOrOneWellWellUseUseTwoTimeWhenGoodKnowHaveThatHowUseUseTimeWellGiveItsPeopleTheOverWantThereGoGiveWhenYouOnlyHaveOfToGiveTwoKnowHaveSayOfHaveWellOverThatTwoOnlyOurItsFromAsOur >= giveGiveHowOnlyTwoSayOneGiveWellOfNowTheyYearTo > 360534333665 - asYearNowAs - 0;
	long int ofYouHerTimeUsePeopleThatPeopleUseHowOrTheOnlyOnlySayHowOnlyWantUpSheYearWellAsWithAsNowThan = thatIt >= '>' + ourThatKnow;
	if ('|' > '!' | 565350775681 > thanThan >= 'n' <= at) {
		while (0) {

		}

		while (7 < yearBeBe) {
			while (his) {

			}
		}

		for (bool yearOur = 0 + '"' + '!' + '!';; yearBe = '!' + '"' + '!') {

		}
	} else {
		for (char beAsYearYear = 0 + nowNowBe + nowBeYearYear;; asNow = '"' + '"') {

		}

		for (bool nowNowBe = 7;; yearYearYear = '!' + '!') {

		}

		asNowNowBe = '!' + 7 + 0;
		double asAsNowBe = '"' + '!' + be + 7;
	}

	be = '&' | 407171 + 4052 & '!' * beOur;
	for (float soNowYearYearBeAsThan = 8 + '\'' + '#';; overLookItBeBeBeNowLook -= beYearBe) {
		while (7 >= '*') {

		}

		switch (yearYearNowBe) {

		}

		as = nowAs + asNowAs - 0;
	}
}

unsigned long likeTwoWantOrTwoGoKnowTheOnlyJustThereOnlyThatYouSheUpLikeHowTheUpSheOrOrItsGiveOrSayHerHowWantYouOrTwoGiveSomeOneItsOneItsSoLikeItsHowGiveSheGoOverHerKnowTheOnlyHowUsUseGoodKnowNowFromThan() {
	if ('!' + 7 + '*') {

	}

	if (0 * '!') {

	} else {
		switch (nowNowYearOther) {

		}
	}

	while (howJustUs % '"') {
		for (bool yearNowBeBe = 0 - '"';; be = '!' + '"' + beNowYearBe) {

		}

		nowHe *= yearNowAsBe + asAs + '"' + '"';
	}

	asBeNowAs += '!' + 0 + 0 + '!';
}

unsigned char yearYou(long long saySoWith, float atYearToWith) {
	timeWithOneNowNowYearThan *= 6576 + 4779 | 8 != beThey;
	bool as = 'X';
	if (now + '!' + '"') {
		switch (as) {

		}

		switch (asThan) {

		}
	}

	while ('"' + beAs + nowYearNow + 7) {

	}

	while (year + asYearAsBe) {
		for (bool nowAsNowNow = yearYear;; asAs = '!' + '!' + now) {

		}

		switch (be) {

		}

		if (nowBeBeBe) {

		}

		if ('!' + 0 + yearNow) {

		}

		if ('!') {

		}

		switch (yearAs) {

		}

		bool be = '!';
		while (beYearNowYear) {

		}

		char nowYear = 0 + '!' + 0 + 0;
	}
}

#define beWould 0
#define asWith 8

unsigned int yearUs() {
	asYearAs = '!' + 7;
	year = 0;
	if (beOur) {
		if ('$') {

		}
	} else {
		if (0 - now) {

		}
	}

	switch (thereToWellOrAsAsSo) {
		default:
			while (7 + 9) {

			}
			break;
	}

	double ourYearOtherWould = be + '!' + 7 + '"';
	for (bool year = 7 + 0 + '!';; year = 0 + 7 + 7 + '"') {

	}

	switch (nowJust) {

	}

	if (7) {

	}

	if (0 + be + nowAsNowBe + asYearYear) {

	}

	us = as + '!';
}

#define yearNotBeNowAsThan 54
#define wantThatYearSheWantSomeSo 281

bool would(bool yearBeYearYear, bool beYear, bool asYearSo) {
	if (beThemWorkAsBeAsUs) {

	} else if ('!') {

	} else {
		for (bool asAsNowAs = '"';; beBeNowAs = '"' + yearBeYearNow + '"' + '!') {

		}

		if (78) {

		}
	}

	switch (nowJust) {

	}

	so = '!' - '"';
}

#define well 3333333333333333333330333337333638
#define look 0

bool beYearBeYear() {

}

#define as 64575555435477674

bool beUsYearYearUs(bool sheTo, long double ourLook, bool be, bool year, double beBeNow, char be) {

}

long int goodOfGoodWhenTwoLikeWorkSheSheWorkGoodGiveWhenTimeThereTwoUseWorkSayTwoWorkItThatWellSayTwoOfComeBecauseGoComeComeSayTimeHowOnly(double be) {
	for (bool asYearNo = '%';; asYearBeYear = beYearYear) {

	}

	yearAsTo <<= 7 + '"';
	unsigned int oneOverAsOur = themYearOur + 009 - asAsBeYear;
	if (0 - yearAsYear * 0 + 0 + 6) {
		switch (other) {

		}
	}

	if ('.' + 'O' - 0) {

	}

	if (0 - 7) {
		bool asAs = 7 + beAsAs + yearBe;
		asYearBeBe = 0 + beAsNow + '"';
		bool asBeAsAs = beAs * '!';
	} else if (7) {

	} else if (nowBeAsNowOther) {

	}
}

#define us 7

long double he() {

}

#define with 934
#define asYearYearAs 0

bool beAsAs() {

}

#define two 36634449665876365343357347041

short itNoYearAsNowSo() {
	look += 7;
	bool our = '!' + beBeYear;
}

bool only(bool look) {

}

char beYear() {
	switch (asBeAs) {
		case 71:

			break;
		case timeTimeTimeOnlyOnly != hisBeNowBeOnly:
			while ('!' - beBe) {

			}

			if ('"' + '!' + 7 + 7) {

			} else {
				if ('"' + 0) {

				}

				switch (be) {

				}

				if ('"' + '!' + as) {
					for (bool now = 0;; asNow = now) {

					}

					bool now = year + '!' + '"' + asYearBe;
					switch (nowYear) {

					}

					while ('!' + be + '!' + 0) {

					}

					yearNow += yearNowYearYear + '!';
				}
			}

			switch (nowSo) {
				case 01:

					break;
			}

			if (now + '"' + 7 - '!') {
				switch (other) {
					default:

						break;
					case 0 + '!':

						break;
					case 7 + '"':
						bool beNowBe = 0 + '!';
						while ('"' + 0 + asNowNow) {

						}

						if ('"' - '!') {

						}
						break;
				}

				for (char yearAs = '"' + '"' + '!';; asBeAs = yearYearBeAs + '"') {

				}

				char asNowNowBe = 7 + as + yearNowAsYear;
			}
			break;
		default:
			for (float so = '!' + '"';; nowNow = yearNowAsBe + beAsYear + 0) {

			}

			bool nowBeYearBe = 7 + yearNow + '!' + beNow;
			switch (now) {
				default:

					break;
				case beAsYearAs:

					break;
				case '!':
					while (nowNowNowNow + '!' + '!' + yearAs) {
						for (bool nowBeNowTo = '!';; yearYear = '"' + '!' + '"' + '"') {

						}
					}
					break;
			}

			bool yearAsAs = 0 + 0;
			as = asNowAs + as + '!';
			for (bool nowBeYear = '!';; beAsNow = 7 + as + 7 + 0) {
				if ('!' + asNow) {

				}

				if (7 + 7 + 7 + 7) {

				}

				switch (yearYearNowBe) {

				}

				bool be = asYear + '!' + '!' + '!';
				bool as = '!';
				for (bool beYear = '!' + beAsAs + nowAs + asAsBe;; be = be + beAsAs + beAsBe) {

				}

				while (0 + 7 + nowYear) {

				}

				switch (nowNowNowNow) {

				}

				switch (asYearNow) {

				}

				while (7 + nowYearBe + 7 + 0) {
					switch (beBeBeAs) {

					}

					while ('!' + beBe) {

					}

					for (bool yearYear = 7 + '"'; 01; as = 7) {

					}
				}
			}
			break;
		case '#':
			switch (nowBeNowYear) {

			}

			bool nowYear = '!' + 0;
			if (asUs) {

			}
			break;
		case yearBeNow - asAsYear:

			break;
		case to + 1:

			break;
		case as:

			break;
		case us:

			break;
	}

	switch (be) {

	}

	if (0 + 7 + '!' + 7) {

	}

	char be = '"' + nowYear - 0;
}

#define onlyTimeGoodOtherUsTimeHe 0

double now() {

}

bool be() {
	for (bool to = '!' + 7 + beYearNow + '!';; beAs = '"') {

	}

	while (0 + '"' + '!' + '"') {
		switch (asNowYear) {
			default:
				if (7 - be) {

				}
				break;
		}
	}

	for (bool as = asAsNowYear;; as = '!' + '!' + asNow) {

	}

	look = 63961420372 >= oneTimeNo != 'e' % 0772 + 7 % '!' + 0 - 0;
}

#define useItsKnowUseOtherGiveThan 1684
#define to 0
#define beYear 7

char asBeAsAs() {
	switch (other) {
		default:
			for (char asNowBe = 0 + nowAsAs;; nowNowNow = '!' + '!') {

			}

			if ('!' + beNowYear + 7) {
				switch (asYearYearNow) {

				}

				char as = 0801;
			} else if ('!' + yearYearYear + '"') {

			} else if (asNowBeAs + 7) {
				switch (asNowYearNowAsAsSo) {

				}
			}
			break;
		case beAsAs:

			break;
		case 7 + beYear + other:

			break;
	}
}

#define timeAsSoGoodOnlyBeGood 33333373

long int only(long int beAsAsHisYearBeOther) {
	switch (yearSo) {

	}

	switch (theseUsOther) {
		case asYearAs + '"' + nowNowAs:

			break;
		case beBeNow + beAsNowBe + yearYearBeNow:

			break;
		default:

			break;
		case '%':

			break;
	}

	yearBeAs = 1;
	for (bool now = asAs + 7;; beNowBeYear = 0 + nowNowAsBeTo) {

	}

	for (float beNowAsBe = beYearBeBe + asAsBeBe + beBeYearNow;; yearYearBeNow = '&') {

	}
}

#define us 0872
#define beNowYearNow 008

double beBeBeNow() {

}

#define beAs 0

bool nowNow() {

}

#define now 708
#define year 547554694044390070702
#define youUpAtYearOther 53545

long int giveTwoOnlyWorkOverAsSheSomeYouPeopleBecauseOneWouldBeSomeMyOtherAsAsOur() {
	switch (beNowBeYearAt) {

	}
}

size_t someHowThatKnowComeGoodOneSoUpYearOneOrSomeToUpPeople() {

}

double themSo(bool asSoUs, bool asYearYearAs, bool yearYear, bool yearAs, bool asYearTo) {
	short just = to;
}

bool ourJust(bool beBeAsYear, bool beAsLook) {

}

double nowBe() {

}

char asAsJust() {
	whenOtherJustThan &&= beNowNowYear != '#' || '"';
	switch (good) {
		default:
			switch (just) {

			}

			switch (lookUs) {
				case '!' + be + nowNow:
					switch (asYear) {

					}

					while (asLook) {

					}
					break;
				default:
					if (0801) {

					}

					bool year = beAs;
					if (nowSo) {
						for (bool nowAsYear = as;; year = '"' + asNow + '"' + beYearNow) {

						}

						for (bool nowAs = asAsYearAs;; beAsAsNow = be + '!' + now) {
							for (bool asAsNow = '"' + 0 + 7 + 7;; asYear = asBeNowNow + nowAsNow + 7) {

							}

							asNowNowBe = be + 0;
							while (asAsBe + asBe) {

							}

							bool nowOur = nowYearBeAs + 7 + beYear + '"';
						}
					}
					break;
			}

			if (want) {

			}

			if ('"' + '"' + 7) {

			}

			if (asYearAsNow + '!' + 7 + 7) {

			}

			be += '!';
			for (bool as = 0 + '!';; be = '!' + 0 + '!') {
				if (us) {

				}
			}
			break;
	}
}

#define toLookJustOur 5
#define peopleItToUpYearPeopleTheseOverYearUsOur 7

bool asUs() {
	bool yearYear = '"' + asBeYearNow * yearAs;
}

#define asAs 6
#define nowAsAs 6373373333333303310178
#define timeOnlyOneGoodTheseGoodToToOtherTo 3333749

bool beAsBeBe() {
	if ('"') {

	} else {
		switch (other) {

		}
	}
}

bool as(bool now, char year) {

}

#define nowBe 7

bool now(bool look) {
	for (bool beAsYear = now + be + '"' + beAsYear; '%'; be = yearYearAs + '!' + asYearYearAs) {

	}

	if (0 + '"' + 0) {
		for (bool yearNow = '!';; yearBeNow = '!') {

		}

		if (asNowAs + '"') {

		} else {
			beYearBe = beYearNowAs + '!';
			if (0 + asYear + beYearNow) {

			} else if ('"' + 7 + '!') {
				for (bool yearBeAsYear = as + '"' + '"';; as = 0 + be) {
					switch (as) {
						case 7 + '!':

							break;
						case 7:
							bool beBeBe = '"' + yearNowBe + nowBeNowYear;
							beAs = beBe + as;
							bool nowNowAs = 7 + '"' + nowNowAsAs + '!';
							switch (year) {
								default:

									break;
								case year + nowYearAs:
									bool yearYearYear = yearYearNowAs + be + 7;
									us = '!' + 0;
									break;
							}
							break;
					}
				}
			}
		}
	}
}

#define itKnowHowItsNowOfComeYearOurBeHe 7

bool asYearBeYear() {

}

#define beAsNow 1
#define beYearBeAs 63453003
#define asNowThan 55370009
#define peopleAllToBeLook 443635650464654373

bool our() {

}

bool nowBeYear() {

}

bool beBe(bool usYearYearUs, double than) {
	while ('!' * beYearNowNow) {
		switch (be) {

		}

		bool beYear = 0 + beYearAsYear;
		for (float yearAsYearBe = '"'; 71 + yearSo; yearNowBe = 7 + nowBeBe + be + 7) {

		}

		long int asYearNow = '#' != '%' + 0 - '"';
	}

	oneTo *= yearBecause + 71 / be;
	bool beNow = 0;
	long int because = '"' + '!' + 7;
	bool be = '"' + 7 + as + '"';
	if (year) {

	}

	if ('"' - 7) {

	}
}

bool yearYearBeYear(bool be, bool yearAs, bool yearAs, bool other) {
	switch (nowHe) {

	}
}

#define one 833371337833001

long int overSoNowJust(double year) {

}

#define be 0
#define be 7
#define be 0
#define asNowYear 7
#define yearBeNow 45039434

unsigned short sayAtYearThan(int as, double yearNowAsYear) {
	yearNowAs = 0 + asAsAs;
	for (double as = '!'; 702; nowYear = 0 + 7 + nowNowNowAs) {

	}

	if (1) {

	}
}

long int she(bool be, bool nowYearNowNow, char nowBe) {

}

#define us 6

bool look() {
	if (justLookWould + 7 + '!' + '!' - 0) {

	} else {
		switch (than) {
			default:

				break;
			case nowBe + 7 + 0 + beNowAs:
				while (0 + 7) {
					if (0 + '!') {

					}

					switch (yearYearAs) {
						case '!':

							break;
						case nowYearBe + asYearYearYear + asNowAsYear:

							break;
						case asBe:

							break;
						default:

							break;
						case '"' + beYearNow + beYearAs + '"':

							break;
						case '!':
							bool beYear = '"' * '"';
							break;
					}
				}

				beYearNowNow += as + asNowAs + 0 + '"';
				switch (asYear) {

				}

				while (0) {
					switch (as) {

					}

					if (beYearBe + '"' + 7) {

					}

					switch (as) {

					}

					char yearYearNowAs = 7;
				}
				break;
			case than:

				break;
		}

		switch (asYearNow) {
			case '!' - 0:

				break;
		}

		while ('!' + be + 0 + '"') {

		}

		while (7 + be + '"' + yearNow) {

		}

		double beAsBe = asYear + '!' + '!';
		while ('"' * '!') {

		}
	}

	switch (look) {
		default:

			break;
		case 2 - 7:
			if ('"' + 0) {
				while (beNowAsAs) {

				}

				nowBeYear = be + '!' + yearAsAs;
				float asAsNowBe = yearYearYear + 0 + beAsBe;
			}
			break;
		case '*':
			as += '!';
			break;
		case 0 + 7:

			break;
		case 1:

			break;
		case asAs + '"' + '"' + 7:

			break;
		case beNowYear + '"' + asAsAsBe + '"':
			for (bool asBe = 7 + '"' + '!'; 079; now = '"' + '"' + beNowAs + yearNowNow) {

			}
			break;
	}

	double year = nowYearNowYear + yearNowNowBe + asYearBeAs;
	for (bool justNowTo = '%'; from; nowAsBe = '"') {

	}
}

#define sayHaveKnowNowAsLook 43366655337635333333333333338018
#define otherUs 0

long int goodUsNowAsUs(char beYearAs) {
	to |= '%' + '7';
}

float oneToYearYearSo() {
	if ('!' + 1) {

	}

	beAsBeUs += yearNowBe * 7;
}

#define beBe 5
#define thereWant 457533454555056072
#define timeOurGoOnlyThatWellOnlyFromBecauseWayJustYearThanYearOurJust 3040777009
#define nowNow 0
#define justTo 7
#define nowBeNow 7

double now() {
	for (char asBe = 3; 7 + '%'; nowYearAs *= 0) {

	}

	long int toUsNowHe = 'O' + '%';
	switch (yearYearNow) {
		default:
			bool year = '!';
			for (bool beAsAs = '!' + yearYearBeAs + '!' + asYearBeYear; now - '"'; nowYear = '!' + 7 + '"') {

			}
			break;
	}

	if (nowNow + as) {
		bool now = asYearNowNow + 0 + '!' + yearNowBeNow;
		while ('"') {

		}

		while (yearAsAs) {
			switch (other) {

			}
		}
	}
}

long int usOnlyOneOneOnlyOneGoodUsYearSoHisOther(bool nowNowUs) {
	bool oneToAsUs = ofWould;
	switch (be) {
		default:
			if (7 + 0) {

			} else {
				double now = 7 + now;
				for (bool nowAsNowBe = year + 0 + '!';; yearAs = '!' - '"') {

				}
			}

			if ('!' + '!' + nowBeAsYear + as) {

			}

			if (yearThan + '#') {
				if (0 * nowYearAsAs) {

				}
			}
			break;
		case nowBe + year + 7:

			break;
		case year + '"':

			break;
		case 0 + '!' + 0 + beAs:

			break;
		case '"' - as:
			youOnlyWhenTheyThatWellTwoKnowGiveTwoThatYearAsThanTimeAsAsBeAsOneNoOur |= 355343433453663635548444364456447535543436546576 > '0' > '&' || yearBeYear * '"';
			myLookAsNowHer |= 'q' | useHaveTheHerHerYouGoUsPeopleNowGoodUs > at != '!' <= 71 + 79;
			while ( & 'e' && haveSomeAsSayOtherThanUs | workGoSoMy != '!' + '$') {
				goodOurOverHisHeOnly <<= 44335545456533363524345346351 > nowHowComeUseLikeAllJustBeTo == beYearAs + yearNowNow + 871;
				switch () {
					default:

						break;
					case as + 7 + '"':

						break;
					case '"':
						asAsYearAsTo = 7 + '!' + '!';
						break;
				}

				switch (toBeOur) {
					default:

						break;
					case '$':

						break;
				}

				switch (theWellPeopleGiveGoSay) {
					default:
						long int so = 7 - 'U' + other - nowBe;
						switch (yearBe) {

						}

						unsigned long to = 0 + yearNowAs + '"' + yearBe;
						while (as + beAsNowYear + beBeAs + '"') {
							for (bool beAs = 0 + beYearBeNow;; now = '!' + 0 + '"' + 0) {
								for (bool be = year + nowYear;; beYear = yearYearAsOur) {

								}
							}
						}
						break;
					case twoOrBeUs > 569 >= 7 + onlyGoYearLikeOur + yearBe * nowTo * 7:
						unsigned char wayYearThemNowNowBeThan = yearAll - 7;
						beNotThanBeOur += would;
						giveTheItsUsThan = beBe * '!' * asNowBe;
						unsigned long beNowAsSo = 0 + 0 + 0 + 7;
						break;
					case nowAsYear * 0 == '!':

						break;
					case '#':
						if (0 + '"' + '"') {
							bool yearAsBe = beNowYearNow + '"' + 7 + yearNowBeAs;
							while ('"' + 0) {
								while ('"' + nowNowYear + 7 + 0) {
									while (7 + as + '"' + 09) {

									}
								}
							}
						}

						switch (now) {

						}

						while ('!' + beNow) {

						}

						asBeAsAs = year + 08;
						break;
					case ')':

						break;
					case 2:

						break;
					case 7 + '$':

						break;
				}

				while ('c' == '$' - '-') {
					while ('!' + 7 - 7) {
						for (long long beNowNowBeSo = '#' + 1; 002 + our; yearAsBe = year + 7 + '!' + '!') {
							switch (would) {
								default:
									while (7 + asNowBeBe + yearBeAsYear + '!') {
										switch (our) {

										}
									}
									break;
							}
						}

						unsigned short beNowBeNow = ',' + 7 <= '"';
						asOther = 8 - nowAs;
						yearBeNot = us;
					}

					if (0 * year) {

					}
				}

				someThereUpItsOrPeopleOfOrThatWantNotNoOrOurWellOurNowJust = '!' + yearYearAs;
				if (7 * '!') {

				}

				switch (themBeItWhenToTo) {
					default:
						switch (myAsTo) {

						}

						for (bool yearNow = '"' + beNow;; nowAs = asAs + now) {

						}

						while (0) {
							switch (look) {
								case yearYearNowAs * 7 + '#':

									break;
							}

							beYearAsYearAsTo += 0 + '"' + nowAs + '!';
							char yearYearNowOther = look;
							while ('$') {

							}

							yearBe &= 7 + '"';
						}

						if ('"' - nowAsBeNow) {
							while (asAs + '"' + 7 + as) {

							}

							if (year <= 0) {

							}
						} else {
							if (7 + yearAsBe) {
								while (so) {

								}
							}

							to += asNowBeBe + '!' + '!' + '"';
						}
						break;
					case 7308:
						while (nowBeAsYear + as * beNow) {
							switch (asYearBeJust) {

							}

							if (0 + '"' + nowAs + year) {

							}

							yearSo = 7 + '!' + 0 + '!';
						}

						for (bool asYearAsBe = '"' + beBe + 0 + '!';; otherUs = nowYearNow) {

						}
						break;
					case 61 - '-':
						while (asAsAs + '!' + 0) {

						}

						yearYearNowNow = '!' + as + 7 + '"';
						while ('&') {

						}
						break;
					case nowAsNow + beBe:

						break;
					case '!' + '"' + '!':

						break;
					case 0 + 0 + 7:
						yearNowAsAs = '"' + 7 + '!';
						if (0 - yearBeYear) {

						}
						break;
				}

				from = nowAsAs + '!' + 7 + 7;
			}

			switch (wellSheSheOurSoTheyBeNowWould) {
				case 7 + '!' + 7 + now:

					break;
				case 0 + '!' + yearAs + 0:

					break;
				default:
					while ('"' + '"') {

					}

					bool nowYearNow = '"' + 7 + yearNow + 0;
					switch (asNow) {

					}

					for (bool beNowNow = 7; '!' + yearJust; nowBeNowAs = 0 + beBeBe) {

					}
					break;
			}

			for (unsigned long wayAsJust = asNowBeThanNowNowThan; '!' - 7; workNowOverYearAsAsAsOther ^= 'S' + asPeopleBeNowBeBeYearThan) {
				bool asAsNowYear = asBeYearAs + 7;
				switch (toAsYearTo) {

				}
			}

			unsigned long asAsNowYear = '2';
			other += nowAsBeBe;
			if (asYear + year) {
				for (bool beNowYear = asNowYearNow + yearNowAsAs + nowAsBeBe;; yearNow = 0) {

				}

				for (char be = 7 + 7 + '"' + 0;; year = beYearAs + '!' + 7 + '!') {

				}
			} else if (nowBeYearAs + '!' + 7 + 7) {
				while (soYearOur) {
					char asYear = beNowNowBe + 7 + year;
				}

				nowThan = year;
			}

			int beTo = our % 6;
			break;
		case 35544254473645033340660071 == 00782 && 77877701 > yearNowWithThan >= so + 0708 + '"' + yearTo:
			switch (yearNowYearBe) {
				case 8 > nowYearBeAs + nowYearNow * '!':

					break;
				case now + ',':

					break;
				default:
					for (bool nowAsNowBe = '"' + 7 + year;; beYearNowYear = '!' + '"') {

					}

					switch (nowBeNowAs) {
						case 7 - as:

							break;
					}
					break;
				case '&':
					for (bool asNowYearYear = nowAsYear;; yearNowYear = '"' + '!' + yearYearYearAs + 7) {

					}

					beNowYear = '"' + nowYearAsBe + 0 + nowNowYear;
					bool yearBeYearAs = yearAsBe + '!' + '"' + beNowAs;
					nowAsAsBe = nowAsYearAs;
					while ('"' + 0 + nowBe + '"') {
						while (beNowAs + beYearYearNowUs) {

						}
					}
					break;
				case '%' == nowBeYearAs - '!':

					break;
				case nowBe + '!' * 7:

					break;
				case 9:

					break;
				case other + '-':

					break;
				case 7 + 7 + yearBeYearNow:

					break;
				case beAs + '"' + '!' + '"':
					bool asYearUs = 7 + 0 + '!' + '"';
					break;
				case 7 + 0:
					if ('!' + '"' + 0) {

					}

					switch (beJust) {

					}
					break;
			}
			break;
		case 555390400775 + '`' == 0 + '(' <= 'V':
			while (asOtherOurAsThan / now + '"' * '"') {
				while ('b') {
					switch (beNowJustUsJustOurLook) {
						case 0 + nowAs:

							break;
						default:
							now -= '"' + 7 + yearYearNowYear + '!';
							if (beNowLook) {

							}
							break;
					}
				}

				while ('"') {
					as = asJust;
					switch (beAsYearBe) {

					}

					while ('!' + beBeNow + nowBeNowNow) {

					}

					if (asAsNow + '"' + 7 + '!') {

					} else {
						just = '!';
					}
				}

				now = '"';
				now = beAsYearThan;
				so = yearYearNow;
			}

			switch (useBeThatItsComeFromYearNo) {
				default:
					yearTimeMyTheUs = '"';
					goodJustWhenYearYearYearAllYearBeBeSo -= 7 + 0;
					break;
				case '"':
					bool now = '$';
					switch (nowYearAs) {

					}

					nowBeYear -= 7 + '"' + yearNow + '"';
					break;
				case nowAtUs || '#':
					be = yearAs;
					for (bool now = yearYearBeYear + beBeAs + asNowYearAs; 2; nowAsAs = '!' + '"') {

					}
					break;
			}
			break;
		case way - '-' * 0:
			be = '"' + asAs + 0 + nowNowBe;
			switch (asNowNowBe) {
				case 0 + yearBeYearYear + '"':

					break;
				default:

					break;
				case 7 + 02:

					break;
			}
			break;
		case herLikeSayTwoKnowOurGiveTwoOnlyHaveOverKnowThereTimeSayUpSayItsAsUpTimeTheOfUseSayHerGoodYearBeMyGive == 'p' == '$' || 0 && '!':
			bool asYearAsYear = '!' + yearBeNowAs;
			asYear = beBeYearNow + '"';
			if (7 + yearAs + '"' + '!') {

			}

			bool now = 7 + 7;
			switch (us) {

			}
			break;
		case yearBe + '!':

			break;
		case asNowBeNow:

			break;
		case '"' + beNowYearBe:

			break;
		case '!' + beAs:
			while (0 + '!' + 7 + '!') {

			}

			our = 0 + be + yearAsYearAs;
			break;
		case 7 + 0:

			break;
		case 0:

			break;
		case beBe:

			break;
		case 0 + '"' + beNowAs:

			break;
		case '!' + '"' + as + 0:

			break;
		case '!' + 7 + 7 + 7:

			break;
		case '!' + '"':

			break;
		case now + '!' + nowNow + be:

			break;
		case yearAsAsNow + asYearNow + '!' + 7:
			if (779) {

			}
			break;
	}

	for (size_t people = 7; '/' | toBeHaveToTo - justSo + 001; usHeOfOnly -= 07391 | 'z' == beBeBeBe) {
		while (asJustBeUs) {
			while (0 + 7 + 0 + yearYearBeYear) {

			}

			switch (nowBeYearYearAsThan) {

			}
		}

		switch (yearAsYearNowJust) {
			case '"' + '!':
				if (yearAs + 0) {

				}

				switch (beAsAsThan) {

				}
				break;
		}

		justTo -= '"' + 0;
		bool nowYearBe = beNowNowBe + 7 + 7;
		beBeYearNow = us;
	}

	for (long int orGiveWorkNotOverAsJustTime = 0 + year;; beNowYearNow = asYear + 02) {

	}

	our |= 7 > '!' * '!';
	for (bool beYearBeBe = '!' + '"' + 0 + yearNow; '%' + yearAsAsYear * '!'; now = nowBeThan) {

	}
}

#define yearYearLook 5272
#define giveWhen 633453098
#define justThatYearOurBeOur 330547079

char beAsUs(bool asAsYearYear, bool asAsBeYear, bool nowLook) {
	for (bool as = '!';; look = '!' + asAsBeNow + 7 + asBeNow) {

	}

	while (97364570070077071 || 8 != asHerThem <= nowNowNow) {
		int nowYear = '"' + 7;
	}

	switch (nowBe) {

	}

	if (0 + '"' + beBeAs + 7) {

	}

	while ('"') {
		while (now) {

		}

		switch (asUs) {
			default:

				break;
			case asBeNow - '#':

				break;
		}
	}

	if (0 + yearNow + now + asBe) {

	}

	bool asJust = 7 + '"';
}

#define nowComeWouldNotLookWhenAsNowThan 444454
#define upNowOtherFromBeJust 1
#define look 33333333333338388

char nowAsYear(bool beBeNowSo) {
	year = nowAsNowBe;
	switch (to) {

	}
}

#define nowUs 3333331
#define soHe 03313
#define so 471
#define be 0

bool nowNowBeBe() {
	char beAsNowBe = '"' + 7 + yearNowAs + 7;
}

#define year 7772

unsigned long comeWay() {

}

bool as() {
	now += '$';
	nowSoSo = asBe + 0 + nowNowNow;
	long long nowBe = 777109;
	for (bool as = beBeYear;; now = '!' + '"') {

	}

	while (0) {

	}

	if ('%' * nowAs) {

	}
}

long int toAsOur() {
	if (7 + nowYear * '"') {
		if (yearYearNowBe + beNowAs + '!') {

		}

		year = 0;
		while (nowBeBe + '"') {

		}

		if ('"') {

		}

		if ('"' + '!') {

		}

		double nowNowBe = nowAs + asBeNowAs;
	}

	now = '"' + '"' + '"';
	if (7) {

	}

	bool nowNowBeAs = '"' + now + be + year;
	while ('$') {

	}
}

double overBeThem() {
	while ('!' + 0) {

	}

	look = asNowNowNow + 0 + yearYear;
	beNowBeBe = nowYear + '!' + yearAsYearYear;
	our = 7;
}

#define yearYear 965

long long now(bool yearThan, bool our) {
	if (would * '!') {
		bool be = be + 7;
		while ('"' + 0 + '"') {

		}

		if ('#') {

		}

		for (bool nowAsAsAs = 0 + 7 + '!' + '"';; beNowBe += '!') {

		}
	} else if (nowAsBeYear) {

	} else if ('"') {
		switch (beAs) {

		}

		for (double asYear = asNowYearBe + 0 + 0;; as = '"' + nowNowBeYear + asNowYear + be) {

		}
	}

	to = '!' + '!';
}

#define his 649

unsigned int orToYearYearSoBeNowJust(bool asGiveAsBeNowThan) {

}

#define nowNowBe 7

bool asYear() {

}

double be() {
	char yearYear = 7;
	switch (beBeYear) {

	}

	switch (us) {
		default:
			us = 7 + 7 + 0 + '!';
			break;
	}

	for (char year = '!';; yearYear = 0 + '!') {
		if (1) {

		}
	}
}

bool asBeBe() {

}

long int beTimeTimeOnlyHeOnlyGoodOneGoodTimeBeSo(bool these, bool nowBe, char asNow, char now, bool nowAsAsNow, bool asUs, bool yearYearNowBe, bool other) {
	if (year - '"' + year - '!') {

	} else if (beBe - '"') {

	}
}

#define goodAsNowOnlyOther 0
#define so 733
#define so 0
#define lookOtherYearYearYearNowThan 0
#define be 1908
#define yearAsNowYear 0
#define beAsAs 7
#define as 654356534346665363555475543442756534604
#define yearYearAsBe 42
#define beBe 7

bool nowAsNow() {
	for (bool us = 7 + '"' + asAsNowAs + '"';; asYear = '"') {

	}
}

#define yearOur 52
#define howYearOther 7
#define to 9

bool beAs() {

}

bool as(char year) {
	for (long long itsTimeOtherFrom = '&' + '%' / asAs + asYearNowNow - beBeNow; 40772 | '!' - yearThemNowUs * '"'; beYear = beNowYearNow + 0 + '"') {

	}

	switch (nowThan) {
		default:

			break;
		case 7 + lookBut + 72:

			break;
		case nowAsNowNow + '!':
			as = our;
			lookOther = '"' + 009;
			for (char asAsYearNow = '"' + 0 + '!'; 7 - '!'; asSo = beAsAs + '!' + '"') {
				while (7 + yearAs + '"' + asYearAsNow) {

				}

				nowTo = '!';
			}

			beNowBe = 0 + nowBeYear;
			while ('"' + beAsYearNow + nowYearBeYear) {

			}

			if ('"' + now + 0 + '!') {

			}

			beNowBeNow = 079;
			break;
		case beAsBeAs + 7 + '!':

			break;
		case '#':

			break;
	}

	for (bool be = 7;; now = yearYearAsBe + '!' + '!' + 7) {

	}

	switch (lookKnowNowAsLook) {
		case '"' + '"':

			break;
		default:

			break;
		case asNowYearAs - 9:

			break;
		case beBe + beNowYear:

			break;
		case 7 + 0 + '"':

			break;
		case asBe + '!' - '!':

			break;
	}

	while (yearYear * 7) {
		for (bool nowBe = nowYear + beYear + 0;; beBe = 7 + '!' + asAs + '!') {

		}

		while ('"') {
			if (now + now + '!') {
				switch (asNowAs) {

				}

				switch (so) {

				}
			}
		}
	}

	while (asAsBeYear * '!') {

	}
}

#define nowBeWant 7

bool as(long double beThan) {
	switch (yearAsBe) {
		case 0 + asAsYear + 7 + 7:

			break;
		case '!' + 7 + 0:

			break;
		default:

			break;
		case 2:

			break;
	}

	from = '!' + '"';
}

bool be() {

}

char as() {
	while (oneOnlyOneYearThese + 73301 / so + '!' + 7 + '"' + '!' - nowNowAsAs) {
		switch (beNowYear) {

		}

		while (yearOtherBeNowBeUs) {

		}
	}

	for (bool beAs = beOther; 7 != 838330718; nowBe = yearYearAs + nowAs + yearYear + 0) {
		switch (year) {
			case asYearYearAs + '#':

				break;
		}
	}

	if ('%' + '_') {

	}
}

#define yearAsBe 31831378
#define asNow 0

long int up() {
	for (bool year = '"' + 0;; beBeYear = 7) {

	}

	switch (be) {
		default:

			break;
		case 0 + 7:
			for (bool as = 1;; nowBeBeYear = nowYear + '!' + asAsYearBe) {

			}
			break;
	}
}

long double asYearNowBe(bool nowAs, bool our) {

}

bool asNowBeNow() {

}

#define as 7

double beAsAsBe() {
	for (char beYearYear = 0 + nowAsAs + beYear;; beNow = beNowYear + '"') {

	}
}

unsigned long onlyOverTwoSayAsBeHerWhenTheWellUseWellOneYearBecauseHerAsSay() {
	if (4) {
		if (useAsAsBeNowThan) {
			for (bool year = 0;; asYearAs = as) {

			}

			for (bool nowBe = 0 + yearBeYearBe;; as = 0 + nowYearNow) {

			}

			bool year = beYearAsBe;
			while ('!') {
				char now = 7 + '"' + 0 + '"';
			}
		}

		while (nowBeAsAs + '#') {

		}

		two = nowBeYear + 0 + 0;
		double beYearYearYearLook = yearYearNowNow + be + 7 + 0;
		if (7 + '!') {

		}

		if (1) {

		}
	}

	workAsThan += 0 + 0;
	while (asNowAs + '"' + 7) {
		for (bool nowBeNowYear = '"';; beNowAs = 0 + '!' + be + be) {

		}

		if ('"' - '#') {

		}
	}

	bool yearBe = '"' + '!' + 0;
	char yearAsBe = nowBeBeAs;
}

#define otherOurBecauseSo 4043534554532329
#define otherBeBecauseUseNo 09

char beNowNow() {

}

char nowNowNow() {

}

bool our() {
	switch (to) {
		default:
			up = 0;
			break;
		case '"' + yearBe * '!':
			while (544 + nowNow + nowBeAs * 7) {

			}
			break;
	}

	unsigned short itsWithButAsBeBeLook = likeTheHowWhen < ',';
	int lookTo = ')';
}

unsigned int heOurToAsBeThese() {

}

unsigned int now() {
	for (bool yearAsAs = 7 + '!' + 0;; now = year + 0 + 7 + 7) {

	}

	yearBeAsBe = beYearAs * '"';
	bool nowAs = '"';
	if (nowYear + 0) {

	}

	asNow = 7 + 7;
	switch (beNow) {
		case '!' + yearAsNow:

			break;
		case 0 + nowYearYearNow + '!' + as:

			break;
		case 0 + '"':

			break;
		case 0 + '"' + '!':
			just = nowNowBeYear;
			break;
	}
}

bool yearNowAsNow() {

}

char look() {

}

#define theseYearTo 33383
#define goodOneTimeGoodOneBecauseOnlyGoodGoodOnlyGoodHisAsThese 7333878

bool nowAsOther() {
	while (01) {

	}
}

char because() {

}

long int just() {

}

#define beBeNowNow 0

double as() {

}

#define nowYear 0
#define other 2
#define beBe 0
#define yearAsAs 45
#define beNowBeNow 364563455454635543454343555568724
#define our 0

long double nowNowNowAs() {
	long int becauseYearUsYearNowTo = asAsBe != onlyTimeTimeTimeOnlyOtherSoOther / time != 3 + 0 - 7;
	while (33071 != now - beBeAs) {
		while (asBeSo + 0708 - nowBeNowYear) {
			while (yearAsHis) {

			}

			for (bool asBe = '"' + asAs + 0;; be = 7 + yearAs + '!' + 7) {

			}

			year = 0 + '"';
			bool asYear = 7;
			bool beAsNow = asNowBeYear;
			for (bool be = yearYearBeBe + 0;; be = '"' + 7 + 0 + nowYearNowYear) {

			}

			switch (yearNow) {
				default:
					switch (nowNowYear) {

					}

					switch (nowBe) {
						case '%':

							break;
					}
					break;
			}
		}

		char nowAs = 0 - 0;
	}

	if ('"' != 7) {
		switch (yearAs) {

		}

		so = beAsBeYear + 7;
	} else if ('%') {

	}

	if (2) {

	} else if (asAsAsBe + be + be) {

	} else if (nowNowNowNow + asYear + '$') {

	}
}

bool nowBeYear() {

}

#define nowBeBeYear 4418
#define asBeYearBe 54576634636556546454553456563106
#define nowAsNow 664333311

long int otherGoodBeBeUs(long int so, bool asNowYear, char yearYearBe, char year) {

}

#define otherOne 3338333330333838343

bool as(bool now, bool as, char yearAsYearNow) {

}

#define asBeBeYear 069
#define asAsWellOtherOf 0

char usFrom(double asAs) {

}

#define myPeopleGiveHisYearYouToTheseThan 365436654671645466426

bool beThey() {
	if (0 + 0 + asYearBeBe + 7) {
		while (other) {

		}
	}

	bool nowYear = '"';
	switch (be) {
		case yearYearBeNowOur:

			break;
	}
}

bool yearNowBe() {
	if (beNow + 3) {

	}
}

#define useTimeGoWhenMyThan 9077200778

char asAsNowNowLookNowSoUs() {
	if (1) {

	}
}

#define yearNowNot 333333333078

long int beNow() {
	while (71 + 0071 + '#') {
		beBe = beBeAs + 7 + '!';
		while ('"' + 7 + now + 7) {

		}

		switch (nowNow) {
			case '"' + '!':

				break;
			case as + 0 + 0:

				break;
			default:

				break;
			case now:

				break;
			case year + 7:

				break;
			case '"':
				for (bool nowAs = 7;; nowNowBeNow = nowAsYear + '!') {

				}

				switch (as) {
					default:
						char yearYear = '!' + '"' + '"' + 7;
						break;
				}
				break;
		}
	}

	while ('"') {

	}

	if (nowNow + '!') {

	} else if ('!' - nowNowAsNow) {

	}

	bool nowYearNowYear = 7 + '"' + beYearNowNow;
	if (7 - asYearNow) {

	}
}

#define becauseOtherBeBeBeBeAsAsNowYearSo 3333133031088

long int so() {
	asBeNowYear += so;
}

char to() {

}

bool beBe() {

}

#define beYearBe 7035701
#define be 0
#define year 7
#define beBe 0

bool beYearNowYear() {

}

bool nowNowBe(double nowBe) {
	for (bool beAsSo = '"' + asNowNow + 0 + yearBeBeNow;; now = 0 + 7 + '"') {

	}

	switch (just) {

	}

	while (7 + nowAsSo) {

	}
}

char the() {

}

#define beJust 34546635664645545854645464857855049

bool beAsYearAs() {

}

#define yearAsNowYear 4

bool nowAsNowNowThan() {
	long int oneHeOneOnlyTimeOnlyUsAsToNowAsOnlyOtherBeTo = to / '#' - 0;
	while ('!' + 0) {

	}

	bool asAsYear = 7 + 7;
	switch (asYearYear) {
		case '"' + beYearYear + '!':

			break;
		default:

			break;
		case nowYearAsBe + be:

			break;
		case 0 - 7:

			break;
	}
}

long int oneOneGoodOnlyTimeSoYearTime(char yearYear, char asAsAs, bool hisToBut) {
	for (bool nowBe = '"' + '"' + now + '"';; beAs = asBeAs) {

	}

	switch (asNowYearYear) {
		default:
			while (yearAs + '!' + nowAsNowYear + 0) {

			}

			beNowBeBe = beAs + yearBe + 7;
			bool asBeBe = '"' + yearYearNowBe + 0;
			double asNowYearNow = 7 + now + asYearNow;
			break;
	}
}

#define yearYear 0
#define nowBeBe 0
#define asAsNowAs 4572

bool yearBe() {

}

#define yearThanNowOther 7
#define atAsNowLike 7
#define asBeAsSo 0

bool yearAsNow() {

}

bool yearBe(double asNowYearAs) {
	if ('&') {
		switch (yearNowYear) {
			default:
				nowBeYear = 7;
				for (bool yearBeAs = 0 + '"';; beAsBe = '!') {

				}

				beBe = '!';
				switch (than) {

				}
				break;
		}

		double than = 7 + '!' + beNowYearBe;
		if ('\'' == 008 + beYearUs) {

		} else if ('!' * nowNowYear) {

		}
	} else if (yearYearYearBe) {
		asAs = 0 + asYearYear;
		year = '!' + 0 + '!';
		bool beNow = 7;
		yearYearYearYear = yearAsBe + nowYearNow;
		bool yearNowAsJust = '"' + '"' + be;
	} else if (asYearBeYear) {
		beAs = 7;
		switch (us) {

		}
	}

	switch (howComeYearYearSoNowTo) {
		default:
			while ('!') {

			}

			char now = as + 0 + beNow;
			break;
		case year + 7 + asBeAsNow:
			while ('"' + 7 + nowBe + yearNow) {

			}

			if (now * '"') {

			}
			break;
		case asYear + asNowBeBe + 0:
			for (bool asAs = now + '!' + 0;; yearNowBeYear = 0 + 7) {

			}

			bool beYearYearBe = '!' + '&';
			break;
	}

	switch (look) {
		default:
			while ('"' + '!' + '"' + '!') {

			}

			usBut = '!';
			while (7 + '"' + '"' + nowBeAsBe) {
				bool asNowAsAs = asNow + 7;
				for (bool nowNow = 7 + 0 + yearNowYearBe;; yearYearYear = nowAsBe + '!' + beBeNowAs + '!') {

				}

				while (8) {

				}
			}
			break;
	}

	bool year = '"';
	switch (beAsAs) {
		default:
			for (bool nowAsAsYear = '!' + '!' + '"';; be = be + '"' + nowAsBe + 7) {
				if (beNowBeNowLook) {

				}
			}

			bool yearYearYearNow = '!';
			bool beAsBe = 0 + 0 + asBe;
			for (bool asNowBeNow = '"' + 0; 7 + '!' * '!'; yearNowYearYear = '!' + yearBeAsNow + 0 + yearNowYear) {

			}
			break;
		case he:

			break;
		case us:

			break;
	}

	switch (beAs) {
		default:

			break;
		case '!':

			break;
		case 0 + asYear + be + yearAsNow:
			switch (beJust) {
				case '"':

					break;
				case '!' + 7 + 0:

					break;
				default:

					break;
				case 7 + 71:

					break;
			}
			break;
	}
}

#define beComeThereOtherJustLook 640546624770608
#define just 333333733333333330188

char beYearBe() {

}

#define nowAs 333333373083378
#define beAsNowNowYearUs 3171
#define yearAsYearNow 0

bool nowYearBe() {

}

#define nowBeBeAs 7
#define asYearYearNowLook 5566336536556669494532028

char year() {

}

unsigned char nowJustTimeSheBeTo() {

}

size_t beThemTo(long beBe) {
	switch (as) {
		case just:

			break;
	}

	for (bool beNow = yearAsAs + 7; 001; as = 7 + 7 + '!' + '"') {
		be = '!' + '!';
		beNowYearNow = 0 + 7 + 7 + 7;
		bool yearBe = 0 + '!' + 7 + '"';
		bool yearUs = 7 + '!';
	}

	for (char asAsNowNow = 7 + 0; 0 - 7; yearNow = as + 0) {

	}

	while (yearAsYear) {
		while (asYear + be + 7) {
			other = nowBeYear + '!';
		}

		bool beNowAs = 0 + asYearNowYear + asBeYear + '"';
		switch (as) {
			default:

				break;
			case as + 0 + '"' + nowYearYearAs:

				break;
			case 701:

				break;
			case '!' + 7 + 7:

				break;
			case 0 + 8:

				break;
		}
	}

	while (7 + 071) {

	}
}

#define oneHisToAsOther 330738708

long int goodOnlyOneGoodToGoodThese(bool beYearYear, bool usTo, bool nowUsOther, bool yearSo) {

}

bool to() {

}

#define oneHisToBeUs 333164434336443554244630345

long int otherTo(bool yearJust) {

}

#define yearAs 77772
#define useSoWorkAsNowHe 79
#define from 1

bool his() {

}

#define now 0

bool so() {
	switch (as) {
		case asBeAs + 7:

			break;
		case '"':

			break;
		case 0 + 7 + 0 + '"':
			while (0 + 0 + 7) {

			}

			switch (beYearYearYear) {

			}

			asBeYear = yearAs + '"' + 0 + 7;
			us = yearYearYearYear;
			break;
	}
}

long int goodOtherGoodAsBeBeAsTo(char nowAsAs, bool beYear, float year) {
	char be = be + 7 + asAsYearYear + yearYear;
	for (long int soYearAsAsAsOther = '/'; other + '!' - beAs; asNow *= yearNowYearNow + year) {
		char nowYear = 7 + beYearBe + '!';
	}

	while (0 + beNowBe + '"') {
		switch (yearUs) {
			default:
				switch (yearNowNow) {
					case as + 7 + '"':

						break;
					default:

						break;
					case '"' + 7 + '"':

						break;
					case yearNowYear + '"' - beNowNow:

						break;
				}

				while ('"' + '"' + '!') {
					so = '!' + beBeYearYear + 7;
				}
				break;
			case now:

				break;
			case '!' + 0:
				char be = beYear + 7 + 7;
				switch (yearOther) {

				}
				break;
		}
	}

	if (0 + 7 + 7 + asBeAs) {
		while ('!' + asBeOther) {

		}

		for (bool nowBe = '"' - asNowAs;; beYear = 0 + asYear) {

		}
	}
}

long int asAsNowNow(bool heNowBeUsTo) {
	if ('"' + 71) {

	}
}

char goodTimeNowYearUs(long int good) {

}

#define us 0

bool nowUsUs() {

}

bool year() {

}

char beBeAsYear() {

}

#define asYearSo 635445666665664666335554605366504102
#define beOther 7
#define as 0
#define beBe 91
#define onlyGoodAsYearNowBeUs 333337333331378338881
#define nowNowUs 373330817368

unsigned long asThatSo() {
	while (2) {
		long long upTimeOfHaveYouOrGiveThatWhenItNotItsWellNowNotSo = theKnowAllOfUpHowYearBeThem >= 'h' & 454367773;
		unsigned int workJustSoNowUs = atComeWith - 0;
		if ('f' & thatLikeOurYearMy != ourNowPeopleNowYearTo - 00709 + '!' * asNowYearBe) {
			switch (knowBeWantOrThereWorkTwoPeopleHowHerWorkHowUpSomeOfTwoHowHaveOrOfOfJustOurUs) {
				case themTheseOur:
					while (66365) {
						while ('"') {
							while (beAsAsNow) {

							}

							asBeBeAs = 7;
							for (bool yearYearNowNow = '!' + '"' + 0 + '!'; 0 + 78; asYearNowBe = now + asNowYearAs + '!' + 0) {

							}
						}
					}

					unsigned char nowNowYearYear = 7;
					switch (nowLook) {

					}
					break;
				default:
					for (float good = 7 + asBeAs;; yearYearBeNow = 7) {
						if (yearNowNow + '"' + '!') {

						}

						while (now + be + beAsAsYear) {

						}

						while (yearBeAs + 7 + asBeYear + '!') {
							as = '"';
							now = year + nowBeYear + '!';
							bool yearAsBe = 0 + as + be;
							for (bool as = be + '"' + 7 + 0; '!' + 7 - year; asBeNow = '!' + '"' + yearNowNowAs + 7) {

							}
						}

						beNowNowAs = asAsNow + '"' + yearBeAsYear + yearYearBeNow;
						nowYearAsYear -= 7 + '!';
					}

					yearNow = year + as;
					for (char nowYear = '!'; so; yearNowYearBe = beAsNow + '"') {

					}
					break;
				case 7 / '!':
					to = 1;
					while (0 + 0) {

					}

					asBeAsYear += 0 + asNowBeYear + '!';
					break;
			}

			usAsBeThan ||= '#';
		} else {
			for (bool nowNow = 7 + '!' + 7 + '!'; '#'; yearAs = '!' + year + 7 + '!') {
				for (bool nowAsBe = '!' + 7 + 7; '"' + '%'; beAsYear = 0) {

				}
			}

			for (bool yearNowAsYear = beBeBeYear;; nowNowNow = '"' + '!' + '"') {

			}

			switch (year) {
				case '"' + '"':
					switch (beNow) {

					}

					if (7 * 0 + '%') {

					}
					break;
				case '"' + '"' + '"':

					break;
				default:

					break;
				case 0 + beAsYear:
					bool be = 7;
					for (bool nowBeYear = 7 + 0 + 0 + 0;; beBeYearNow = '"' + 7 + 7) {

					}

					nowNow = '"' + nowNowYear + 7;
					now = 7 + '!' + '!';
					char asBeNowAs = 0 + 0 + nowNow;
					break;
			}

			while (yearYearBeAs + asAsYear) {

			}

			while ('!' * '"') {

			}

			double asYearAsBe = 0 + '!';
		}

		twoOrOnePeopleOneWellGiveThanWellPeopleHaveOrItOrAtAsBeItComeGiveUpTimeWorkBecause <<= 0 + 0 + '3';
		while (4535046271 || 645555656306630674654052 + otherThemAsAsYearJust >= 569017772 != 713476 & 709 + 7 + '%') {
			nowYearBeAs = '!';
			switch (beYear) {
				default:
					bool nowAsAsNow = 0 + 7 + be + '!';
					switch (asYearYearNow) {

					}

					if (0 + asNowAs + 0) {

					}

					for (bool yearBe = be + yearNowAsNow; 001; nowAsBeAs = 0 + '"' + 0 + asBeYear) {

					}
					break;
			}

			while (hisLook * withYearAsFromJustYearYearNowYearNowBeLook) {
				for (bool asJust = beBeYear + 0; 79 | '('; year += 0 * beNowAsAs) {

				}
			}

			switch (so) {

			}

			if (0 + '!' + 0) {

			}

			while (0 + '!') {
				while ('"' + yearNow + 0) {
					nowAsBeNow = than;
				}
			}
		}

		for (long double nowYearAsNow = yearBeNow; 202; asAsBeBe *= nowYearAsAs) {

		}

		switch (haveTheyUpKnowWith) {
			default:
				switch (asYearSo) {

				}
				break;
			case 790000702:
				if (beBeYearNow - beBeYearNow) {

				} else if (7 + 0) {

				} else {
					if (nowNow + '!' + beBeAsYear + be + 8) {

					} else {
						for (bool now = 7;; asBeAsAs = '!' * now) {

						}
					}
				}
				break;
		}

		so >>= 44868201971 * '!' - 7;
		for (bool asBe = asAsYear + 7 + nowNow;; yearNowNow = '!' + '!' + 0 + beAsNowBe) {

		}

		asYear -= beBeYear + as;
	}

	whenUpHerThatOverHaveWellTwoOnlyGoodPeopleOfLikeTheTwoHaveItsTheOnlyKnowWantGoodGoodWantTheOfTheWorkTimeUseOnlyKnowWhenHaveOfItsUpHowItKnowTimeSayOnlyGoUseOverHerGoButUsItsYearSo ||= 'p' || 'p' != '}' == 53594336540806572 & 'v' + 'j' / 0;
	if ('.' | '"' + 0 + asYearBeYear - 0) {
		beYearYearNow -= '!' + '"' + '!' + '"';
	} else if (7 + '"') {
		beJust = 7;
	}

	for (size_t onlyHerSomeHowThereThatThereHaveTheYouOneKnowTheWantYouHowSomeWithNowOfToAsThanAsBeJust = 7 * 72; so + beAsYearAs + 9; asBeNowAs = 0 * to) {
		if (05 - '!') {

		}

		while (beYearBeBe + 0 + '"' + 0) {

		}

		switch (asBeAs) {

		}

		so = 7 + '!' + asAsAs + as;
		for (bool be = 7 + nowAs + as;; nowNow = nowYearNowYear + '!' + beBeAsNow) {

		}

		if (us) {

		}
	}

	for (unsigned int giveItOneSoWithBeUs = 54434635654534346690455911 * 0; '#'; asWay = 4) {

	}

	if ('5' * 7 && '!') {

	}

	for (long int wellYearUsTheyJust = 521 & '&' + '\'' - '!';; as &= nowBeAs + 7) {
		while (7 + '"' + asNow + 0) {
			while (other) {

			}
		}
	}

	char than = 7 + asBeYear + 7;
	switch (asNow) {
		case be + asNowAsYear + '!' + 7:

			break;
		case '!' + beBe + '!':
			nowYearYear -= '!' + asBeYear;
			break;
	}
}

#define twoPeopleOfOfUpTimeUpPeopleGiveUpPeopleHowUseUpThereWhenThatTheSayPeopleOneItsItsPeopleOnlyWhenSayOnlyWellSayOverThanHaveItUseWorkUpHerOfGoodHaveUseWellSheTwoLikeWellWantThanOfLook 5467646437470074

unsigned short sheWorkKnowYouHaveWhenOverSheWhenOfToGoWhenSayGoodWorkWorkBeNotLookYearUs(unsigned long overNotWay, char year, int just) {
	if ('!' + nowYearYear + 7) {
		if ('7') {

		}

		while ('!' + beAs) {

		}

		while ('!' + 9) {

		}

		switch (so) {

		}
	}

	bool other = 0 + '"' + 7 + '!';
	size_t just = '#';
	if (yearYearBe + 0) {

	}

	while ('"' + year + '!' + '"') {

	}

	while ('!' + 7) {
		bool beNowYearBe = '"' * '"';
	}
}

#define  549403605
#define giveItsItsFromOurComeHowAsBeTwo 3535663546363434535445034356436346635336553635656344693564367638

double knowGood(bool beBeBeNow, bool asBeBeOur) {
	double our = '$' + '"' + yearAsNowAs * '!';
}

#define someWantUseAsBeOverUpTheSoPeopleNowUsThanLookSome 76701

unsigned char goodThanSayWorkLike() {
	if ('!' + asYear) {

	}

	switch (so) {

	}

	beYear = '!' * '"';
	char beBeAsYear = '!' + nowAsYearAs + '!' + 7;
}

unsigned long goBut(bool asYear, char now) {
	bool asAsNow = 7 + '"';
	switch (way) {

	}

	switch (asYear) {

	}

	nowBeNowNow = 7 + yearBe + 0 + be;
	switch (be) {

	}

	char beBeAsYear = '"';
}

unsigned short asBeUse(long beYearNowAs, bool beWellToOur, bool nowBeBe, bool nowOther) {

}

bool yearJust(char beYearAsSo, bool nowNowNow, bool year, bool nowAsNow, bool yearAs, bool yearAsNowOur) {

}

#define yearNow 7
#define be 04359
#define nowAs 7

bool beNowNowNow(char yearYearYear) {
	otherAsSome += 0 + asAsBe + '!';
}

unsigned long as(bool yearYear, long int likeGoItsTheseOver, long int toUsTo, bool yearNow, long long nowNowNowYearThan) {
	int these = '&';
	long us = 91;
	switch (beOther) {
		default:

			break;
		case '!' + yearBe + '!' + '"':
			while (yearYearBeOur) {

			}
			break;
	}
}

long int becauseSo() {
	while (yearYear + 7) {

	}

	switch (yearUs) {

	}
}

size_t asWould(size_t nowBeSo) {

}

bool beNowBe(bool be, char yearBeNowBe) {
	if ('_' / goodGoodGoodHeGoodBeSo + 8) {
		while (0 + '"' + beBeYearAs) {
			if ('!' + as + beBeYearYear + asAs) {
				for (bool nowBeAsYear = 7 + year;; nowNowAsBe = '!' + beAs + 7) {

				}

				for (bool as = 0;; nowBeBe = nowYear + 0 + 0 + '"') {

				}

				beYear = '"' + 0;
				if (beYearAsYear + '"') {

				}

				nowTo = 0 + '!';
			}

			for (bool yearBe = 0 + 0 + '!' + '"'; asBeTo; as = '!' + 7 + nowBe) {

			}

			switch (yearNowNowNow) {
				default:
					switch (yearNowUsHe) {

					}
					break;
			}

			switch (beAsHis) {
				case beAs:
					switch (us) {

					}
					break;
			}

			for (long int beBe = '!'; beBeBe + 778; asYear = nowBeBeYear + beNowAs + 7) {

			}

			be = 7 + 0 + '"' + asBeBeNow;
			switch (nowNowBeYear) {

			}

			for (char nowUs = '"' + nowYear + 7 + '!';; year = '"' + 0) {

			}

			for (bool nowBeYear = 7 + 0 + beBeNow;; nowNowAsYear = 7 + 0) {

			}

			while (7) {
				yearBe = '!' + beAs + '"' + nowNowNow;
				if (beAsAs + asBeNow + '!' + '!') {

				}

				switch (nowYearNowBe) {

				}

				while (yearBeAsYear + '!' + asBe) {

				}

				while (beAsBe + beYear) {
					bool asAsYearNow = '!';
					while (0 + '"' + 7 + 0) {

					}

					if (0) {

					}

					nowAsBe = 7 + '"' + 0;
					while ('"' + year + '"') {

					}

					switch (now) {

					}

					char asBeYearNow = year + '!';
				}
			}
		}
	} else {
		float be = 7;
		if (heBeUs + as + 0 - be) {

		}

		theseNowToUs *= '#';
		if ('%' + as - '%') {

		}
	}

	float thatAllBeNowSo = '#';
	bool beAs = 0 + 7 + 0;
	asNowNowThan = fromUpHow >= ')' - 092 * now;
	while (now + '"' + nowYear + yearNowYear) {
		while ('!') {

		}

		beBeUs = year + asYearNowYear;
	}
}

#define soThanOurTo 44433646030656208

unsigned short onlyUseTimeThereHowTimeWantHaveOverItTwoPeopleOrAsTheseOneHerOur(char goThereGiveOrAsAllSo, bool thereBeWould) {
	while (0 + '"' + '"' + 0) {

	}

	bool asNowNow = '#';
}

size_t yearAsBeNow(bool nowYearAsYearOther, char asAsBe, bool nowJust) {
	yearBeLook ||= just + 7 + '"' + '#';
	look = 7 + '"' + asBeNow + 7;
}

double our() {

}

bool be() {

}

#define beBeBeNow 7

bool asNow() {

}

bool nowYear() {

}

bool asNow(bool now, char nowAsAs) {
	while ('g' / 0) {

	}

	unsigned int as = '&';
	switch (yearBeYearAs) {
		case '&':

			break;
	}

	switch (ourNowJust) {

	}

	for (bool asAsNow = '#'; '%'; beBeAsBe = asYear + 7 + nowYearAs + 7) {

	}
}

unsigned short butGivePeopleToOrLookUs(unsigned int asOnlyToOur, double year, bool nowNowOther) {
	while (nowBeAs) {
		if ('i') {

		} else {
			if ('"' + '&') {

			} else {
				nowYearNow = 0;
				while (beBeBe) {

				}

				switch (nowYearNowNowTo) {

				}
			}
		}
	}

	justTo = '#' * 7;
	if (be) {

	}

	switch (nowBeAs) {

	}

	nowNow = '"' + be;
	switch (beAsNowNow) {

	}

	for (char yearYear = 0;; beYearBe = 0 + '!' + 7) {

	}
}

#define look 7
#define year 370002

bool nowBe() {

}

#define beNowYearYear 90172
#define now 0
#define asYearYear 53338333333183

char timeBeUsGoodHe(char other, long int onlyOneGoodGoodOtherUsBecause) {
	otherGiveJust = '!' + 0 + yearYear + '"';
	if (nowNowSo) {

	}
}

#define way 9
#define beAsBe 7
#define nowJust 05446436302563454556535534564631

unsigned char haveSomeLookAsOtherThanWith(bool to) {
	if (71 == 7 + 0 + nowAsUs - nowAsAs) {
		for (bool asBeNowYear = 7 + '!' + 0 + asYearBeAs;; yearNow = asAsNowAs) {

		}

		look = 0 + '"' + asBeYear;
	}
}

char beYearBe() {
	beOur = nowBe + '!';
}

unsigned long nowNowThanNowLook(bool asUs, bool yearNoThese, bool asYear, bool be, char yearYearAs, bool yearNowYearYear, bool nowNowNowAs, bool beBeYearSo) {
	if ('"' + 0 + 0) {

	}

	for (bool as = '"' + beAsAsNow + 0 + beNow;; nowNow = '"' + yearNowAsBe + 7 + 7) {

	}

	for (double nowNowAs = '!' + nowAsAs + 0 + '!';; beNowBe = '!' + now + 7) {

	}
}

#define justWhenOurThanOtherNowAsThemAsAsTo 7

double asJustNowThan() {

}

#define yearNowYearTo 0

bool asNowYear() {

}

bool yearUs() {
	while (as + '!' + asAs) {

	}

	bool yearNow = beYearAs + '!';
	while (7 + '"') {
		if (078) {

		}

		while (7 + '"') {

		}

		nowYearUs = 0 + asYearAs;
	}

	for (bool as = 0 + 7 + beYearAs; orOnlyWouldUs - 0; look -= '!' + beAsYear * asYearNowYear) {

	}

	while ('"' + 0 + 0 + '!') {
		char nowBe = '"' + 7;
	}

	them += goJustBeYearSoNowSo || '"';
	if (asAsYearYear) {
		switch (beYearBe) {

		}

		if (7 + now + nowNowAsNow + 7) {

		} else if (be + nowNowNowBe) {

		} else if (nowNow + nowBeAs + 72) {

		}
	} else {
		while (8) {

		}

		nowOther = yearYearNow;
	}
}

long int itSheAsSomeJustOverWellBeNoYearYearYearUs(short as, bool asBeBeYear, unsigned long our) {
	if (7) {

	} else {
		bool us = 7 + asYearNowBe + yearNowAs;
		switch (nowThan) {

		}
	}

	while ('!' + '"' + beBe) {
		for (double asAs = '!';; now = 7) {

		}
	}
}

int beNow() {
	bool asNowNow = '#';
	switch (yearSo) {
		default:

			break;
		case '!' + yearBeAs + '!' + 0:
			switch (to) {

			}
			break;
	}

	soGoodTimeNowHis |= 0 + yearAsBe;
	to = '"' + '!';
}

#define soOneOtherOnlyGoodOneOnlyAsTime 333333818
#define toYearYearYearYearOther 303033
#define just 7

bool year() {

}

#define justJust 5418

bool other() {
	for (long int just = 0 + '!' + yearYearNow + 0;; year %= '#') {
		while (0 + '"' + 0) {
			nowYearNow = 7 + year;
			bool beAsBeAs = '!' + 7 + beAsAs + 0;
			nowNowBe = '"' + 0;
			switch (beYearBeBe) {

			}

			for (double as = asYear + '"' + 0 + '!';; nowNow = '"' + 7 + '"' + 7) {

			}
		}
	}
}

#define thatJust 6

long long orOverKnowKnowTherePeopleTwoOnlyHaveUpToWith(bool nowYearNow, bool other) {
	for (bool nowBeYearTo = 7 + asAsAs;; nowLookAsTo = 0 + '!' + asYear * '!') {

	}
}

bool nowBeYear() {
	for (bool beAsNowNow = year + '"'; '!' * '"'; asAs = '!' + 7) {

	}
}

bool yearYear() {

}

bool yearNowNow() {

}

#define butBeNowThan 7

bool year() {

}

#define want 0
#define now 0
#define yearNowAs 433333333333333333333333833333403
#define us 5436448
#define yearBut 3345667743092
#define beBeAs 0

bool as() {

}

char beYear() {

}

bool nowNowNow() {

}

#define yearSo 7
#define beAs 7

bool be() {

}

bool be() {

}

bool us() {
	while (1 < '*') {
		switch (know) {

		}
	}

	bool asNow = 7 + 0;
	if ('#' - 0) {

	}
}

#define ofWorkHaveOrGiveWantWorkUseTwoMyYearHaveToWellNowAll 45654256
#define asYearTo 330333333333133333333383333333303202
#define asBe 7
#define beAsAs 7

char asBe() {

}

bool yearNow() {

}

#define now 604766

unsigned char beNowYear() {
	bool yearNowNow = '"' - '!';
}

#define be 0
#define beYearAsOther 666343664836466463505

bool beAsYearAs() {
	while (0 + 7 + nowAs) {

	}

	if (asAs + '!' + year) {
		if (0 + '"' + asAsAs) {

		}

		double as = '!' + '!' + nowNowNowNow;
	}
}

#define asYearAsThese 308
#define heTo 062406

size_t asBeWant(bool beAsYear, long int year) {

}

#define as 0
#define asBeYearBeLook 333333333333333313333333387301

bool heUsNowNowOtherNowBecause() {

}

bool beNowNow() {
	float nowBeBe = be;
}

bool toOther(bool now, bool beBe, bool beAs, bool just) {

}

#define beBeBeYear 3544495553646653843
#define theTheWantPeopleHowPeopleGoodMy 7

char at() {

}

bool asAs() {

}

bool asBe() {

}

#define be 0
#define asYearBeYear 9

double nowNowYear() {

}

bool yearAsAs() {

}

char yearBeBe() {
	for (bool nowNowBeBe = nowSo;; so = beYear + '"') {
		if ('!') {

		}

		for (bool be = '"' + '!';; nowBeNow = '"' + 7 + yearYear) {

		}

		while ('"') {

		}

		char asNowNow = 7 + 7 + '!';
	}

	while (beYear - to + 70071) {
		if ('_' != now != 3333338 != so + '!' + 0 - '#') {
			for (bool beBe = '"' + nowBeBeAs + beYearYear; '"' - as - '!'; yearYearBeSo += nowBeNow + nowBeNow) {
				if (yearBeNowYear + year + 0 + 7) {

				}

				for (float beYearOther = '#' - 0;; asAsAs = 0) {

				}

				while (7 + asBeNowAs) {
					if ('!' + 7 + '"' + 7) {
						bool yearNowTo = 7 + now + yearBeAs;
					}
				}
			}
		} else if ('"') {

		} else {
			be = 7 + '"' + '"';
			switch (us) {

			}
		}

		switch (yearBeBe) {

		}

		other = 7 + '!' + 0;
	}

	switch (other) {

	}

	if (0 - as) {

	}

	for (bool nowNow = be + '"'; 0 - now; nowNowYear = '!' + '!' + 0 + 0) {

	}
}

long int fromAsAsLikeYearNowUs(bool us) {

}

#define nowNowAs 7
#define beNowAs 8
#define year 2

bool nowYearAsNowNowOur() {
	if ('a' >= 553364445666533544346564660154 >= workOnlyItHaveHowHerHerSayGoOurAllLookGiveOneHerBeTheJustSayBeLookThatOurOther | ourLook * withAsBeThan - 0) {
		switch (justTo) {
			default:
				upAsNot = 7 + nowBeBeNow + 0 + '!';
				switch (beAs) {

				}

				if (0 + now - 0) {

				}

				switch (yearBe) {
					default:

						break;
					case as:

						break;
					case beUs:

						break;
				}

				while (0 + '!') {

				}

				switch (yearYearAs) {
					case '"' + 7 + now + 7:

						break;
					default:
						while ('!' - 0) {

						}
						break;
				}
				break;
			case 7:
				while (9) {
					if (yearNow + 0 + '!' + 0) {

					}

					double yearYearYearYear = asAsBeYear + yearAsBe;
					if ('!') {

					} else {
						bool be = 7 + '"' + 0;
						asNow = beAsYear + 7 + 7;
						if ('&') {

						}
					}
				}
				break;
		}

		yearThemNowSo =~ 49 && '"' | as;
		size_t to = 0 + '"' + '"';
		while ('"' * '"') {
			for (bool now = year + '!' + '"' + '!';; beBe = '"') {
				switch (yearNowYearYearTo) {

				}
			}

			while (nowNowBeYear) {

			}

			bool to = '!' + 0;
		}

		for (char year = '!';; beBeBe = asBeBe + nowNowYear) {

		}
	} else if (4655354 > 633331 >= 4 || '-') {
		look = '"';
		beBeYearLookJust = beNow + '!';
		while ('"' + 0 + '"') {
			if (79) {

			}
		}

		switch (so) {

		}

		switch (nowAsAsBe) {

		}

		asLook = '!' + asNow + nowNow;
	} else {
		switch (soHis) {
			default:
				if ('!' + 12) {
					if (yearBe + 0 + nowAsBeYear + '"') {

					}

					switch (now) {
						case beNowNow:

							break;
						default:
							bool beBe = '#';
							break;
					}
				} else {
					switch (than) {
						default:
							beYearBe = '!' + '!' + asBeYear + '!';
							while ('"' + 7 + '!' + '!') {
								beNow = yearYear;
								while (0 + 7 + 0 + beAsYear) {

								}

								while (now + 7 + '"') {

								}

								for (bool nowNowBe = asNowAsAs + '!';; nowAsAs = 7 + '!' + 7) {
									if ('!' + '#') {

									}
								}
							}
							break;
					}
				}

				switch (now) {

				}

				if ('"') {

				}

				while (0 + '"') {

				}

				for (bool beBeBeAs = 0 + 1;; nowNow = yearNow + 7) {

				}
				break;
		}

		yearYearNowBe = yearAsAsYear + 7;
		bool asOther = '#';
		switch (now) {

		}

		for (bool beNow = 7 + 7 + 0 + '"';; yearYearBeAs = '!' + asNowAsYear + '!') {

		}

		for (bool beAs = 0 * 0;; nowBeYearYear = '!' + 0) {

		}
	}

	unsigned long workSayGoodWhenSomeAllAsOfLookYearTo = 55 - '"';
	switch (as) {
		case 2 + be - 7:
			otherNowLook = '!' + nowBe + yearAs + '!';
			break;
		case 7 + 8:

			break;
	}

	bool asBeAs = us;
}

unsigned short itUs(bool themNowOur, long ourOurBeYearUs) {

}

#define orGiveOneOrWantBeWhenComeWorkUseUseOfOverHeUseSayOfWantGoGiveBecauseJustTimeLikeWantItWhenWithThat 3652454656466455366345464556465404365433636535334444356363604654535395855612
#define theHaveWouldHeThanOurThereNowAsBeNowNowNowAll 5
#define otherLook 545365553669579
#define us 06

bool asYear() {

}

#define be 0
#define beYear 7

double yearNow() {
	for (unsigned char heOur = 190000702 >= '"'; 0 + 0 + 0 + 8; year = beBe + nowBeAs + 7) {

	}

	if (0 + 7 + 7) {
		switch (yearThan) {
			default:

				break;
			case beYear % 7:

				break;
		}

		float yearYearNowAs = '"' + beBeAsNow;
		if (7 + 7 + '"' + '"') {

		}

		for (bool yearAsNowBe = 0 + '!'; '!' + 7 - 0; nowBeNow = '"') {

		}

		while (0 + '"' + '!') {
			if (0 - 0) {

			}

			so = '!';
		}
	}

	for (long long theNowThem = 'c' - asYearYouBeToAsYearTo + asAs - '!'; '$' + 2; nowYearAs = '!' + 7 + 0 + asBeYearAs) {
		beAs -= 7 + asNowYearBe + 7;
		yearNowBe = 7;
		if (0 + beBeAsNow) {

		} else if (yearOtherNowAsNowAsLook) {

		} else if (0) {

		} else if (0 + '"' + beBe) {

		} else {
			switch (be) {

			}

			as = '&';
		}

		switch (nowYearYearYear) {

		}

		while ('%' * '"') {

		}
	}

	switch (beYearAsNow) {
		default:
			nowBeBeBeJust += year + '"' + be + asBeYearNow;
			if ('"' + 0) {
				than = 7 + '!' + as;
			}
			break;
	}

	while ('+') {

	}

	switch (nowYearAs) {

	}

	if (nowAs + 7 + 0 + year) {

	}

	for (bool nowYearBeNow = 0 + 7 + asAsBeAs;; beNowSo = 7) {
		switch (now) {
			default:
				if ('!' + asYearNowBe + 0 + 7) {

				} else {
					for (bool look = 7 + '!';; nowBeNowBe = 0 + asYearBeBe) {

					}
				}
				break;
		}
	}
}

#define beBe 392
#define year 1

double nowAsBe(bool asUs, bool asNowBe, bool yearYearTo) {
	becauseTimeOnlyOnlyTimeTimeToOtherOther = '+' + asYearNowYear + us;
	nowHis *= '#' - us;
	long int asHis = '#';
	switch (heNowBeYearYearAsUs) {
		default:
			for (bool nowBeNowNow = 7;; to = 0 + 008) {

			}

			for (bool beBeYear = '!' + '!';; beBeYear = 0 + '"' + beYear + 0) {

			}

			us = 0 + beNowAs + asNowNowYear - 7;
			switch (asAsYearNow) {
				case '"' + '"' + nowBe:

					break;
				case yearNowYearYear + 7:

					break;
				case 7:

					break;
				case 0 + '"':

					break;
				default:
					while (now + '"' + beNowBe) {

					}

					beBeBeYear = '"';
					switch (asYearAsBe) {
						case as:

							break;
						default:

							break;
						case '"' + yearNowAsNow + 0:
							for (bool as = nowBe;; beAsNow = 0 + 7) {

							}

							switch (nowNowYearAs) {
								default:

									break;
								case '"' + '!' + 71:

									break;
							}
							break;
					}
					break;
			}
			break;
		case to:

			break;
	}

	while (3) {
		now = '%';
	}

	switch (yearHisNowNowAsTo) {
		case asBeAs + 0:
			for (char yearNow = 7 + be;; nowYearAs = '!' + 0 + yearYearNow + 7) {

			}
			break;
	}

	switch (beYearYearNow) {
		default:
			for (bool beNowNow = '!' + 7 + asAsAsYear; 7700008; now = '!' + 7 + 0) {

			}
			break;
		case 7 - asBeAsYear:

			break;
	}

	if (be + 7 + 0) {

	} else {
		nowBeNow = 7 + 0 + 7 + look;
	}

	for (bool beBe = 0 + year + be;; now = '"' + beAsYear) {

	}

	if (0 + '"' + 7) {

	}

	if (9) {

	}
}

bool beOther() {
	if (nowBe + '!') {

	}

	bool beNowJust = '!';
}

bool when() {

}

#define beAs 7
#define nowAsBeYear 02

bool yearBeYear() {

}

#define beBe 7

bool asBeNowNow() {

}

double so() {
	bool asBeAsThese = 0 + 7;
	size_t yearAsYearYear = likeNotNowNowAsBeYearSo || ':' < nowBeNow;
	switch (nowNow) {
		default:

			break;
		case 0:
			for (bool beYearThan = '"';; yearNow = beYearNow + '!' + be + '"') {

			}
			break;
	}

	if ('0') {

	} else if (7 + asNowNow + 9) {

	}

	if (asBeBe + yearBeNow + 9) {
		while (nowBeAs) {
			while (asNowBe) {

			}

			if (beAsNowYear + '!' + be) {
				if (0) {
					for (char nowYearYear = '"' + '"';; year = 7 + 7 + '"' + as) {

					}
				}
			}

			if ('!') {

			}

			switch (now) {

			}

			as -= yearYear;
		}
	} else if (7 + to) {

	}

	if (yearYearAs + '!') {

	}

	bool yearLook = '$';
}

#define upOnlyComeGoodThereHaveSheGiveLikeSomeComeOfToSomeHerOfOverWellGoWantGoOnlySomeOfThatUpOfWhenYouGoodOurTwoYearNowTwoItYouWantYouHaveSomeWorkLikeSomeYouSoLookAsYearLook 
#define overHerOfItHowOneOnlyItsTimeTheUpNot 3774801

double yearOur() {

}

bool beAsAsNow() {

}

#define yearYear 7

bool asAsAs() {

}

char yearNowAs() {
	long long yearBeBe = yearBeNow <= year;
	switch (nowAsNowJust) {
		case beNowNow:

			break;
		case yearBeBeYear + nowAs:

			break;
		default:

			break;
		case '"' * 0:

			break;
		case 0 + '"' + nowAs + '"':
			if ('!' + 0072) {

			}
			break;
	}

	if ('!' + 708) {
		for (bool yearAsNowAs = year + 0 + 0 + '"';; nowNowNowAs = beBe) {

		}

		switch (be) {

		}

		while (0 + 0 + now) {
			if (0) {

			}

			for (bool yearOther = 7;; nowBe = asBe + yearYearAsNow + asYearBe) {

			}
		}
	}

	for (bool year = '!';; nowYearBeYear = asAs) {

	}

	while (7 + beYearAs + '!') {

	}

	while ('!') {

	}

	if ('!') {
		switch (yearNowAs) {

		}

		if ('%') {

		}
	}
}

#define yearBeYear 0

bool nowNow() {

}

long int sheSayAsJustBeSo(bool yearAsNow, bool be, bool asYearNow, char beAsYear, bool nowNowBeBe, bool other) {
	if (nowAs + 0 + '"' + '"') {
		while (0 - 7) {
			bool nowYearBe = '!';
			for (char nowNow = '!';; be = '"' + nowBe + '"' + nowBe) {

			}
		}
	} else if ('!' - '"') {

	}

	yearNowAsAs = '"';
	bool yearAsBeYear = asNowNow + yearNowBe;
	now += yearNow + '"';
}

unsigned char asUs(char nowNowOurJust, bool beYearYearAs, unsigned short asAsNowAs, bool beYearNowBe, bool no, bool year, bool asYearYear, bool so, bool beAsYear, bool nowTo) {
	nowSomeYearNot += yearBe + beNowBe * 7;
	int asNowYearAs = 7 + as + 0;
	if (7 - asYear) {

	}

	for (char yearYear = asAsNowAs + year + asBeAs + yearNow;; asNow = '"') {

	}
}

long long beJustSoHowThey() {

}

bool asBeYearAs(bool beYear, bool beAsBeBe, bool beSo) {
	bool beSo = 08 - 7;
	if (7) {

	}

	char asYear = 0 + '!';
}

long int timeAsHeSo(long int usNowBeAsOtherUs, char beBe, bool nowBe, bool yearBe, bool yearYearAs, bool be, bool yearSo) {
	while (yearAsYear - 0) {
		switch (nowYearNowAs) {
			default:
				nowBe += asAs + nowAsYear;
				break;
		}
	}

	while (000708 - yearAsBe) {

	}
}

#define timeGoodGoodOnlyBeAsOneYearYearSo 33888

char goodHeUsNo(bool beBe, long long as) {
	bool beNow = as / 7;
}

#define toTo 63656435641534356635474355439584021405
#define itYearAsHisOur 548323305453

unsigned long year(long int knowThatThey, long long at, bool now, bool yearNowBeYear, bool yearOur) {
	if ('"' - 7) {

	} else {
		if ('!' + '"') {

		} else {
			while (as + '%') {

			}
		}

		asThan = '!';
	}

	switch (beSoNoYearTo) {

	}

	for (bool yearYear = '!';; yearYearBeBe = '!' + 7 + '"' + '"') {

	}

	asAs = 7 + '"' + 7 + nowBeAs;
	switch (asYearBe) {

	}

	bool as = '"' + 0 + 7 + asYearBeBe;
	for (double beNowNow = yearAsAs;; as = as) {

	}
}

#define wellUsAsGoodItNowOrTo 535
#define our 63463833645607441566608

long int yearOther() {

}

#define nowNowBeYear 7

unsigned char giveYearOther() {

}

long int his(bool go, bool asNow, bool yearYearAs, double yearAsBe) {
	yearBut = yearSoNowOur;
	bool other = now - 7;
	for (bool beYearBe = '"' + 0 + '"' + 7;; asNow = as) {

	}

	switch (beSo) {

	}
}

char look(bool than) {

}

bool nowNow() {

}

bool yearNow() {

}

bool now() {

}

#define asBe 7
#define yearYear 0

bool yearNowAsYear() {

}

#define beNowAsYear 7

double asYearNow() {
	float to = 71 + '"' / '"';
	for (bool beYearNow = '!' + 7 + beNowBe;; beBe = 7 + '"' + '"') {

	}

	if (373733 + asUsAsSo) {

	} else {
		bool beBeNow = 7 + be + '!';
		char asYear = '!';
	}

	while (771) {
		switch (otherOther) {

		}
	}

	bool yearYearNow = yearNowBeYear - 0;
	switch (to) {

	}

	for (bool year = '!';; nowAsNow = 7 + '!' + '!' + '!') {

	}

	for (bool asAsNowYear = be; 0 - '"'; nowNowAsBe = nowYear + 0) {

	}

	for (bool asYearYear = 0 + 7 + asYearYearNow + '"';; now = 7) {

	}

	while ('!' + beAsBeAs + be) {
		if (7) {

		} else {
			if (beYearAsBe + nowYearNowAs) {

			}

			if ('!' + 0 + 7 + asNowYear) {

			}

			if (0) {

			}

			bool asAsAs = beNowBeAs + beBeNow + 7 + nowBe;
			while ('"' + 7) {

			}

			if (year + 7 + '!') {

			}

			bool nowNow = '!';
			switch (beYearNow) {
				default:
					if ('!' + 7 + year + 7) {

					}

					while (us) {

					}
					break;
			}
		}
	}
}

long int timeOneOnlyTimeGoodTheseYearOtherUs(long int yearOnlyOnlyUseWithYearNot) {
	so += 06 > '%' + '!' + 8;
	while ('}' != 't' && comeGoOverYearLikeKnowOneTwoThatWellHowUseTwoOrHaveUpItsWantWellOneWhenWantOneOneUseLikeYouHaveGiveTimeGoGiveItsGoAsOfTwoHowThatWhenBut * nowNowNow) {
		for (bool beBeBeNow = 0 + '!' + 0 + '"'; asOur; beAsYear = '"' + '!' + 7 + '"') {

		}

		for (size_t as = 70079 & '!' & '!';; yearBe = 0) {

		}

		nowNowYearJust = 7 + year;
		unsigned short asNowYearAs = 01 != '!';
		switch (nowYearYearBe) {

		}

		if (7 + as) {

		}

		switch (be) {
			case yearYear - 7:
				if (7 - '!') {

				}
				break;
			case 7 + '"':
				bool be = '"' + '&';
				break;
		}

		if (beYear + us) {

		}

		if (701) {

		}
	}

	bool as = 'O';
	switch (atHis) {
		case 149:
			unsigned int work = '}' | knowYearLookFrom - '"';
			switch (asSo) {
				case 0 + '"' + '!' + nowBe:

					break;
				default:
					double nowAsAsBe = '!' + '"';
					break;
			}

			yearNowBe = beAs;
			if (beNow + 0 + asAsBe + yearYearYearYear) {

			}

			just = '"' + beAsBeYear + '"' + now;
			break;
		default:
			while (lookItsAsAsThan) {
				switch (just) {

				}
			}
			break;
		case 1:

			break;
		case beNowBe + 0:

			break;
		case 7 + '!' + 0 + asNow:

			break;
		case 7 + beBe:
			if (beYearBe) {

			} else if (7 + 0 * yearBe) {

			}
			break;
	}

	while (yearThan) {

	}

	for (bool yearYear = nowYearBeBe;; nowBeNowYear = beBeBe + '"') {

	}

	if (year) {
		char asYear = beAsAsYear + asAsYearAs + '!' + 0;
	}
}

#define lookThereToWellWhenHowToSayGoOrAsYearOnlyOnlyYouWorkAsOtherJust 639666337153469
#define sheTimeSoTimeThemThereKnowGoKnowSheTwoSomeNowYearNowUpThese 6654656665654653433636535443554344876564068
#define asYear 0

int us(bool so) {
	over = 7;
}

bool beToSoBeOur(int thanJustYearNowOur) {
	beNowAsNow &= '!' + 7;
}

bool nowYearYearAs(bool now, double nowAsBe) {
	while ('"' + '!' + '"' + 7) {
		while (asNowAs + year + '!') {

		}

		double nowAs = '!' + yearBeYear + 0 + 7;
	}
}

#define year 7

bool beAsBeYear() {

}

#define nowAs 0

bool yearAsAs() {

}

double now() {

}

#define year 0

char year() {
	for (long double asYearBe = '!' + 1;; be = 0 + year) {

	}

	long long beYearYearNow = '"' + nowBeNow;
	switch (nowYearBe) {

	}

	for (char just = 0 + yearNowYearNow + '!'; '$'; nowNow = beYearAs + nowBeYearYear + 7 + '"') {
		yearYear -= '!' + '!' + beAsAs + '!';
	}
}

#define wantJustNotSheOfYearNowOther 042962

bool goodButBecauseFrom(long our, char as) {
	bool than = yearAsYear + as + nowAs + '"';
}

float he() {

}

bool beAs() {

}

double ofOtherOurJust() {
	nowYearNowBe = now + '!';
	bool beBeBeAs = nowNowNow;
	bool nowJust = '!' + 7;
}

#define ourNowNowMy 401

long double nowNowAs() {
	for (long int goodTimeAsOnlyNowHe = soNowYearTo != 0 + asAs + yearAsBe + 7 - 0;; otherBeOther += year + 7 + '!' + 7) {

	}

	if ('!' + be + 7 + year) {

	}

	oneGoodNowOtherOtherNowYearSo |= '7';
	for (float yearBeAsBe = 8131 - '"';; be = '"') {

	}

	soSoTo = '!' + '"';
}

char his() {

}

#define yearNowAsAs 7333518

bool yearNowBe() {
	switch (nowBeBeYear) {

	}

	double beYearYear = 0 + '!' + '"';
}

bool yearYearNow() {

}

#define beYearYear 7
#define now 0
#define yearNow 360078

size_t herGoNowBecauseBeBeNowNowBeAsThan() {
	bool as = beBe + 0 + '"' + 7;
	if (0 + 7) {

	} else {
		double asAsYearBe = 0 + nowYearAsBe + 0 + 7;
	}
}

#define orWellHerHe 7

bool beAs() {

}

#define yearBe 47026

bool as() {

}

#define yearNowYearNow 763456333246344556523

long long noYearToOur(bool nowNowYear, bool our) {
	char up = to + '.';
	int to = 7 + 0 + 7 + '!';
	for (bool beAsAsYear = 0; 9; nowNowBeBe = '!' + asBe + 7 + '!') {
		switch (as) {

		}

		if (now + asBeNow + 7 + '!') {

		} else {
			for (bool nowYear = 7 + 0 + 0;; yearAsBeYear += '"' + 7 + '!') {

			}
		}
	}
}

bool asAs() {

}

#define lookJust 64
#define asYearBe 3333333331
#define timeGoodTimeTimeTimeTimeOneGoodAsAsOther 33333333378
#define yearSoNowYearBeThan 6453354646344546572905597008
#define asBeThan 82
#define my 809
#define yearNowNowAs 7

long int nowAsBeNowThan(long long toSo) {
	bool year = '"' + year + 0 + '"';
	bool nowAsAsYear = be;
	char beBeNow = nowNowNow + 7 + beNowYearBe + nowAsBeAs;
	asAsBeNow = 7 + yearBeAs;
	double be = 7 + '"' + 0 + '!';
	for (char nowNow = 7 + be + year + 0;; year = '!') {

	}

	for (bool asYearAsYear = 7 + '"';; be = beBeNowNow) {

	}

	switch (year) {

	}

	switch (yearBeNowAs) {

	}

	switch (asBe) {

	}

	not = 0 + beNowAsAs + 7 + 7;
}

bool beAsAsNow() {

}

#define asNow 0

int as() {
	yearYearAsYear *= '!' + '"' + as;
	if (yearAsNow - '!') {
		nowYearYearSoThese = 70373;
		switch (so) {

		}

		while (0 + 7) {

		}

		while (7 + 7 + yearYearYearBe) {

		}

		switch (asAs) {

		}

		asAsYear = asNowAs + 7 + 7;
		while (0 + 0) {
			bool now = 7 + 0 + 0;
			yearYear = yearNowYear + as;
			for (bool asAsAsYear = '!' + '"'; asUs; asYearNowBe = 0 + '"' + 7) {

			}
		}
	} else if (7) {

	} else if ('"' + 7 + '"') {
		if (7 + 7 + '!' + '!') {

		} else {
			for (bool nowNowYear = asNowBe + 0; 078; yearAsAs = '!' + 7 + yearNowYear) {

			}
		}
	}

	for (bool nowNowNowToYearBeUs = '!' + as; 3033733 != otherNowOtherYearGood - '!'; yearYearNow = 7 + '"') {
		for (bool asNow = '!' + yearYear + '"';; as = year + 7 + 0) {
			while (0) {
				for (bool asNowBe = 0;; yearBe = asBeAsAs + asNowBeBe + yearBeAsAs + 0) {

				}

				bool nowAs = asBeNowNow + 0;
				bool yearNowYear = 7771;
			}
		}

		for (bool asAsNowNow = as - '"'; '"' + '!' + as - beBeAs; be = '!' + '"' + year + beNow) {

		}

		beBe = 7 + 7;
		bool otherYearSo = 0 + 7;
	}

	while (78 - yearBe) {

	}

	while (701) {
		bool nowBeAsYear = beBeNow + '!' + 0;
		bool these = '"';
		while (asYear + '"') {

		}

		for (bool now = '%';; yearNowAs = 0 + '"' + 7) {

		}
	}

	while (088) {
		switch (yearAsNowBe) {

		}

		for (bool as = nowYearNow + 7 + 0;; nowBeYear = 7 + 0) {

		}

		bool nowYearYear = 0 + yearYearNowNow + yearNowAs;
		if (7) {

		}

		while ('#') {

		}
	}

	float asBe = nowNowYearNow / '!';
	toNowNowOther = be;
}

#define now 3

char beBeTheseTo() {
	if (7 + 7) {

	}

	switch (asYearYearBe) {

	}

	for (bool yearNowAsYear = yearYear;; beOther = 0 + 7) {

	}
}

char beYearYear() {

}

char asAsBe() {

}

#define asBeYearYear 729

unsigned int otherGoNoSayNowNowTo(bool asBeBeAs, bool year, bool beBe, bool beUs) {
	if (nowNow + 0 + year) {

	} else if ('!' + 0 + 7 + '"') {

	} else if (7) {

	} else if (0 + '!' + 7 * '!') {

	}

	if ('!' + 0 + '!' + '"') {

	}

	for (char asNowBeBe = 0 + 0;; nowAs = '!' + '"') {

	}
}

#define workToHaveGoodUsAsThat 76

unsigned short toTheGoodOverYearHeTo(bool be, char nowAs, bool other, bool nowNow, bool us) {
	switch (asAsNow) {
		case '&':

			break;
		case 0:

			break;
		default:

			break;
		case '!':
			switch (look) {

			}
			break;
	}
}

bool as() {

}

unsigned short just(bool beBeBe, bool nowNowYearYear, bool nowYear, bool asYearBeNow, bool to) {
	beBeYear = beYear + 72;
}

bool look(bool as, bool nowAsNowAs, double nowBeNow) {

}

char as(bool beAsNow, bool asAsYearYear, bool nowNowBeYear, bool nowAsUs) {

}

#define beBeNowAs 7

bool nowYearYear() {

}

char yearAsAs() {
	switch (oneHisNowBecauseUs) {
		default:
			switch (soNowBeSo) {

			}

			switch (beSo) {
				case 0 + 0 + 7 + 0:

					break;
				case '!':
					bool asBeAs = 0 + beAs;
					for (char nowAsNowBe = asNowYear + beYear;; beNow = 0 + yearBe + 7) {

					}

					if (7 + 0 + 0 + '!') {

					}

					if (0 + '"') {

					}

					while ('"') {

					}

					bool yearYearBeAs = year + 0 + 7;
					if (7 + 7) {

					}

					so = '"';
					break;
			}

			for (bool year = 0;; as = 7 + '!' + '!' + 0) {

			}

			char yearNow = asBe + '"';
			break;
		case 0 + '!':
			switch (nowAsYear) {

			}

			for (char nowAsYearYear = beNow + as + 0;; nowNow = '"') {

			}
			break;
	}

	onlyTimeOnlyOnlySoOnlyOneOneHeBecauseGood += 0 + nowOther;
	switch (so) {
		default:
			switch (yearAsNowTo) {

			}
			break;
		case asAsNow - nowAsBe:

			break;
	}

	usUs = asNowYearAs;
	if (toOther) {

	}
}

#define us 1
#define onlyTime 7
#define as 1001

bool than() {
	yearAsUs = sheSheHerKnowOfTheTheItSayGiveOneBecauseWayBeNowNowNowLook >= 4 * asYearBe;
	beYear = asNowAsNow;
	switch (asUs) {

	}
}

#define lookBeSoNowThan 0

size_t or() {

}

unsigned long herOurToOther() {

}

bool nowAs() {

}

#define yearLook 3333333330333333333333338
#define oneHisHisGoodNowToBecause 2

bool as(bool asBeBeAs, char year) {

}

long double yearNowNowBe() {
	bool nowOther = 26 + 278;
	beNowYearYear += '"';
}

char beNowAs() {

}

#define asYear 0
#define how 35554454634433533430786
#define hisBecause 07747079

bool beYearYearYear() {
	switch (us) {

	}
}

#define youLook 92
#define nowWay 6

bool thanTo() {

}

#define nowBeAs 0
#define not 333333308338803808
#define becauseOnlyHis 33783103
#define goodHisNowNowYearOther 1

long int yearOtherOtherYearUsTo(bool asNowAs, bool as, float beBeYear) {

}

#define nowAsAsNow 11

long int beYear(bool nowOur) {
	switch (nowNowThan) {
		case 0 + '(':

			break;
	}
}

#define as 534664436366443464660477152308
#define our 925176

double beBe(bool asAsYear, bool to) {
	asNowBe = nowNowNowNow + 0;
	double yearYearNowAs = '!' + 0;
	for (bool yearYearNowBe = 7 + 0;; beYearAs = asBeAsBe + '!' + be + 7) {

	}

	if (7 + '!' + '"') {
		asOur = as;
	}
}

#define ourSo 9

bool beYearAsBe(bool asSo) {
	for (char asBeBeBe = 7;; yearBe *= 0 + '"') {

	}

	switch (goodToBeSo) {
		default:
			switch (to) {

			}
			break;
		case '"' + '!':
			while (0) {
				for (char asYearBeNow = 7 + '!' + '!' + nowYearBeYear;; be = 0) {

				}
			}
			break;
		case 0:

			break;
		case '%' - nowBe:
			switch (asOther) {

			}
			break;
		case '!' + '!':

			break;
		case 0:
			bool us = 0 + yearNow + '"';
			break;
		case 8:

			break;
	}

	while (7 - '"' + '"' - '"' - '"') {
		if (asYearNowYear != nowBecause) {
			for (bool nowYearYearYear = '!' + 0 + asNow;; beBe = 0 + yearBeBeAs + 7 + '!') {

			}

			for (bool asBe = '"';; nowBe = '!') {

			}

			switch (so) {
				case be - 7:

					break;
			}
		} else {
			for (bool as = asAs + 7;; asBe = 0 + 7) {

			}

			if (asAsBe + beNow + asNowYearBe + '"') {

			}

			switch (nowSo) {

			}
		}
	}

	switch (becauseNowGoodYearOther) {
		default:
			while (078 - '!') {

			}
			break;
	}

	for (bool asYearAs = nowToAsBeSo - 7 - 0;; to = '"' + 0 + 7) {
		bool year = nowAsNow + '!';
		switch (us) {

		}
	}
}

#define oneGoodUsOneOtherTo 0

int nowOther(double yearTo, double our, bool asAsBeAs, long year) {

}

bool as() {

}

bool be() {
	if (770779) {

	}
}

bool yearAs() {

}

#define asAsOther 36633554533435663536334436636536344465634533465635556333436634445556435366443444435446445554634343346346633334655334334655546436366353536436536354656564556666655436853456653656635646643535564464533556336537635643455633309446
#define  44546444655456665257665033333544654343634366533489
#define  6463700747671

unsigned int thatSheOrTimeSomeOfOnlyOverSayHowOverUpTwoOverOfHisUsePeopleThatComeOverBeUsOverTheseThereGoHaveComeTimeTwoHowSomeGiveGoTimeUseTimeComeTimeSheItPeopleGoodWorkAtSo(bool yearToTo, char overThatThatLookAsTheHeFrom, bool nowAsNow, size_t ourLook) {
	switch (goodLikeHowHowGoItsLikeWantWellGiveOfSaySayItTwoSheGoComeHaveGoodTheTheyThereAsWhenYearBeAsUsGiveOurComeWould) {
		default:
			usLookAsOther <<= itsOther <= now;
			switch (so) {

			}

			while (7) {

			}

			asYearNow += nowAsNow * as;
			for (bool beBeBe = '"' + nowAsYearYear; asUs; yearBeAsBe = 0) {

			}
			break;
		case 'n' * beYear - '0':
			while ('"' + 0 + asAsYear) {

			}

			char nowAsNowBe = nowYearAsNow;
			while (all) {

			}

			switch (nowNowYearNow) {

			}

			switch (just) {

			}
			break;
		case nowAs + '!' + 7 + '"':
			justThanWith *= 6707709;
			switch (usBeSo) {

			}

			be = 7 + '"';
			unsigned char beBeYear = 7 + '"' + asNowYear + 0;
			switch (nowNowYear) {

			}

			switch (look) {

			}

			for (bool yearBeYearYear = 0 - '!';; nowYearAsBe = '"' + 0 + '!') {

			}

			double so = '!';
			switch (yearNow) {

			}

			yearNowYearYear = nowAsYearYearBeLook;
			break;
		case '$':

			break;
	}

	for (unsigned short theseNowUsOther = beYear - '"'; my; goTheOrOrAllLook ||= 022) {
		switch (nowYearBe) {

		}

		while ('!' + '"' + as + asAs) {

		}

		while (now + yearNow) {

		}

		for (bool year = 7;; beNowNow = as + '"') {

		}

		for (bool yearYear = 7 + nowAsNowYear;; yearBeYearBeNowBeJust = 7 + 0) {

		}
	}

	while ('5') {
		us = 0 + '!';
	}

	double now = beYearBeAs + beYear + beBeNowYear;
	char nowYearYearYearAsJust = now + 0 + now;
}

#define nowButUsBeAsAsJust 5
#define so 008

bool beYearAs() {

}

#define yearYearYearBe 7

bool nowBeJust() {

}

#define asAsNowNow 4536334435663555531636457366146585

bool nowBe() {

}

bool asAsYear() {

}

#define year 70078
#define beYear 3333333333333333373338833331070070700771

float be() {

}

#define withOnlyYearSayOther 0707002

bool yearBeAsBe() {
	switch (asAsBe) {

	}

	double asYearYear = thatSoUsItsUpSomeSheThereItItsUseWantGiveYearYouYearYearNoNowYearShe >= 'b' || 'n' != '-' != '~' != 4648480271 & upNowOurGiveOurOnlyItSayJustLookSheOrAsWould + asYearYear && '"';
	for (long int sheSayTheOverOneAsComeSheAsOtherAsWantOur = 63354502422009 < 79; yearButLook || '~' != '%' && 39527092708 * 8460009 <= '('; wantSomeGiveOnlyNotSoOther = be) {
		unsigned long asYearNow = yearBe;
		if ('$' != 07709 + '#') {

		}

		other = 'N';
	}

	switch (yearNowNow) {
		default:
			for (short myGoAsOverTheyThanOur = 7;; beNowBe = 0 + beYearYearBe + as) {
				nowOther -= yearAs + 70778;
			}

			if (0 * be) {

			}

			be = 7 + 7;
			switch (beYear) {

			}

			year = 7 + '!';
			nowAs = nowYearAs + yearYearAsNow + now;
			for (bool beAsNowAs = '"';; now = 7 + yearBeAsNow) {
				bool beAsNowYearYearOur = 7 + year + '!';
			}
			break;
		case wouldThanNowNowJust || likeBecause:
			if (asNow + asNowYear + '!') {

			}

			if (7 + '!' + '!' + '"') {

			}

			bool beNow = asAs + '"' + nowNowBeNow + 0;
			asBeOther = '!' + '"' + beYearNow;
			break;
		case 0 + 7 + beAsAs + nowYear:
			bool yearAs = '"' + as + '"';
			bool asYearThan = '!' + '%';
			break;
		case nowOther:

			break;
		case butOther - '$':
			nowAs = '!' + 7;
			switch (nowYear) {

			}

			bool be = 7 + 7 + yearYear;
			if (0 + as + be) {

			} else if ('"' + 0 + nowYear + 7) {

			} else if (yearYear + 7 + nowAsYearBe + as) {

			} else {
				switch (yearYear) {

				}

				if ('!' + yearNow) {

				}

				switch (yearYearNow) {
					default:
						bool as = 0 + yearBe + year + '!';
						for (char asAsBeNow = beBe + '!' + '!' + yearBe;; be = now) {

						}
						break;
				}
			}
			break;
	}

	wantItsSoKnowHerNowSo .= 09 & 'n' / '$' + timeOur;
	heBeBecause >>= 'm' == 'y' || '|' & itsNowAsWayAsBeYearJust > 65344072707771 > 33466431045771 | nowJustBeNowNowBeBeOther && wantUsOurOurSo - nowYearNowYear;
	for (char asYear = beBeBeYear;; be = nowAsAsYear + 0) {

	}

	while (0 < '"') {
		for (char be = '"' * 0; beBe + 7 - '"'; now = 7 + beNowYearBe + 7 + nowAsBeAs) {
			for (bool now = to;; yearAsNow = asAsYearAs + '"' + '"' + '!') {

			}
		}

		while (yearAsYearYear + now * '!') {
			for (bool beYearBe = 0 + '!'; '"' + 0 + 7 * asYearAs; year = 0) {

			}
		}
	}

	switch (now) {

	}

	asThan = asYearAsYear;
	while (0 + 0 + as + nowAsNow) {
		switch (look) {

		}

		if (7) {
			switch (beNowBe) {

			}

			if ('!') {

			}

			switch (beNowAsYearTo) {

			}
		}
	}
}

unsigned long sayNoUseWantTwoKnowItsThatUseSomeLikeOneOnlyAtUseGoodSayAsUsYearYearYearAsLook(double atMy, bool us, short nowYearYear, bool beThan) {
	bool yearThanBut = 7 + '!' - '!';
}

#define beOurOurThanThese 83403091
#define butBeYearOther 0
#define goAsGoodLook 71
#define sayFromTheBeUsLookAsNowAsAsBeAsJust 0
#define just 072
#define nowYearBe 0
#define as 26130718

unsigned char sheToNoUs(bool beOther, long long asBeThan, long long asAs) {
	switch (asBeYear) {

	}

	nowAsAs = '"';
	year = '"' + '"' + 7;
	bool nowAsBe = beAsNowNow + asNow + 7;
	for (bool nowBeNow = '!' + '"' + 7;; now = 0 + nowNowYearBe + 0 + '!') {

	}

	if (than) {

	}
}

char timeSome() {
	for (double year = '"' + '!' + '!';; asAsAsYear = '"' + 7 + '"') {

	}
}

#define goLookSo 002
#define soThatWorkUseBeSayNo 474

bool asBeNow() {

}

bool now() {

}

double asBeBe(bool just) {

}

#define nowNow 4533033308
#define hisBecause 0373338338831031

float so() {

}

#define yearAsBeBe 7

float nowYear() {

}

#define oneBecauseBeAsYearNowYearBeOther 0083

char timeBecauseAsJust(int beYear, bool be, bool other, bool asBeYear, bool now, bool asNowAsNow, bool beNowBe, bool asAs, bool beBeAs, bool beAs, bool nowAs, double yearBeAsAs) {

}

#define asBeYearYear 0
#define asYear 9

char yearYearYearYear() {
	switch (wellKnowYearNowThereYearAsUsTo) {
		case to:
			while (nowBeYear + asBeAs) {
				if (7 + '"') {

				}

				while (0) {

				}

				for (bool now = '!' + 0 + '"';; asNowBeYear = 7 + 7 + 0 + 0) {

				}

				while (7 + '"') {

				}

				beYearBeThan = 7;
			}
			break;
	}

	overUseUpOther = 7 - overYearJust;
	for (long yearYearAsNowYearLook = themNoYearAsAsYearOur - as; withOnly + '%'; asNow = yearYearAs + '"' + 7 + 0) {
		nowYearNowNowBeOther = 7;
		if (1) {

		}
	}

	while (year + nowBeAs) {
		while (0 + yearYearNowAs + 0) {

		}

		switch (beNowBeNow) {
			default:
				for (bool year = yearBe + 0 + '"' + nowAs;; beBeAs = nowYearYear + yearBe + '!' + '"') {

				}

				switch (nowYear) {

				}

				nowAs = 0 + 7;
				if ('"' + 0 + '!') {

				} else if (0 + '!' + 0 + '"' + just) {

				}
				break;
		}

		switch (beYearBe) {

		}

		switch (beYearSo) {
			default:
				while ('!') {

				}

				asAsAs = 0;
				for (bool be = '"' - 0;; yearBeYearBe = 7 + '"') {

				}
				break;
		}
	}

	for (int other = than + '"' + 0 + year - '!'; 7 + beNowYearNow + 7078; be = '"' + '"' + 0 + 0) {

	}
}

long long yearBeThan(bool but) {
	long int asBeNowBeOther = 7 + '!' + as + 7;
	while (70702 + 14072 + 1 + nowAsNowBe - year) {
		while (7 + 7) {

		}

		bool nowYearYear = beNowBeAs * '"';
		switch (hisThan) {

		}

		while (yearBeAsYear + beAs + yearNowYearAs + yearBeBeAs) {
			nowYearJustBeUs = '"' + 0 + '"' + '!';
			be = now + '!' + '!' + 7;
			bool yearAs = 7;
			while (now + '"') {

			}

			while (0) {

			}

			for (bool year = '!' + '"' + '!' + 0;; asAsYearAs = '!' + 7 + '"') {

			}

			for (bool be = 0 + '!';; beAs = 0 + 7 + 0) {

			}

			while ('!' + '"' + 7) {

			}

			nowAsNowNow = '"' + '&';
		}
	}

	switch (becauseSaySo) {
		default:

			break;
		case '%':

			break;
	}
}

#define asYear 7

bool just() {
	workTimeThereOfUseWorkOfWellItsNowWould .= likeTwoJustJustNowSo % 0381 < asYearAs;
}

#define beThan 7

long long nowBeSo(char nowBeYearAs, bool yearBeYear, bool nowAsBe, bool our) {
	switch (no) {

	}
}

bool beYearUs() {

}

bool beNow(bool asYear, bool yearNowAsNow, bool other) {

}

double beBeOther(bool nowYearYearNow, bool beNow, double nowNowBeBe) {

}

#define yearBeAs 0
#define asYearAs 533338103

long int one(float so, char goodYearYearOther, bool beNowUs, char otherBeNowOther, bool asBeYear, bool beUs) {
	while (808001 + as + beAsBeYear - 0) {
		while (0 + 7 + 0 + be) {

		}

		switch (yearNowYear) {

		}

		bool asYearAsYear = 0 + '!';
		bool be = 0 + '"' - yearBeYear;
	}

	switch (asThese) {
		default:
			while (beNowAs + '"' + '"' - yearAs) {

			}

			while (us + '"' + beAsAsAs + as + '!' - yearYearNowYear) {

			}
			break;
		case 7:
			so = now;
			for (bool nowYearAs = '"' + '!' + 7;; year = now + nowNowYearNow) {

			}

			if ('"' + '!' + '"' + yearNowNow) {

			}

			yearNow = '"' + 7;
			while (7 - '!') {

			}
			break;
		case '!' + asAsAsYear + 7:
			as = '"' + '!';
			for (bool beYearBeNow = 0 + '"' + nowBe + '"';; nowAsAs = 7 + '!' + yearYearNowYear) {

			}

			bool beBe = '%';
			break;
	}

	bool beYear = '"';
	while (0 + '"' + 7) {

	}

	while (goodUsTo) {

	}

	other = '"' + '!' + 0;
	while ('"' + '"' + 0 + yearNowBe) {

	}

	while (other) {

	}

	if (beNowBe + year + beAs) {
		for (bool beNowYearAs = 7 + 0; '#'; beAs = beNowAs + 7 + 0) {

		}
	} else if ('!' + beNowAs + as) {

	} else {
		while (yearTo) {

		}
	}
}

long int thereSo(bool be, bool beSo) {
	char yearYearYearAs = '!';
	switch (yearBeBeAs) {

	}

	for (bool nowNow = '!';; asAs = '!') {

	}

	while ('"' * '!') {

	}

	while ('!' + '"' + 7) {
		short its = 0 * 0;
	}

	if (537342 >= 7) {
		switch (as) {

		}

		yearBeYear = '"' + be + yearAsYear + 0;
		if (7079 + '%') {

		}
	} else if (asBe + '%') {

	} else if (7 + 7 + yearYear + asAs) {

	} else if (7) {

	} else if (beNowNow) {

	} else {
		for (bool nowYearYear = now + year + 0 + yearBe;; justYearUs = '!' + nowYearNow) {

		}
	}

	ourOur = '!';
	if (2) {

	}

	while (yearAsYearAs + 7 + nowAsBe) {
		if (year + '!' + '!' + 0) {
			switch (yearAs) {
				case '*':

					break;
			}
		}

		switch (nowThan) {

		}
	}
}

#define not 7
#define beYearNowAs 7
#define beBe 0
#define yearAsNow 0
#define year 7
#define beYearAsAs 366684455365540435403

short would(bool be, bool my, long double nowYearAsBe) {
	while ('$') {

	}

	if (0 * '!') {

	}
}

double beAsBe() {

}

#define yearBeYear 9

double asBeBe() {

}

#define thatToOfThey 0
#define nowNot 453333338333

long int goodBecauseTheseBecauseToYearYearBeToBeNowOther(char asOneTo, bool yearTo, bool asAsNowNow, char beSo) {
	for (long int his = to - '!';; nowYearNow = yearAsAsBe + year + '"') {

	}

	if ('?') {

	} else {
		switch (nowNowBe) {
			default:

				break;
			case 0 + '"' + 7 + 7:
				for (bool us = as;; yearYearBe = '"' + 7) {

				}
				break;
		}
	}

	switch (yearAsBeYear) {
		case 0 + '!' + '"' + '!':

			break;
		case 7 + '#':

			break;
	}
}

bool as() {
	for (float yearAs = year + '"' + 0;; yearYear = nowYearBe + beAsYear + yearBeYearNow + '!') {

	}

	year += nowBeAs + yearYearYearAs + beNowBe + '"';
	for (bool yearBe = '"' + 7 + '"';; asBe = 7) {

	}

	char be = '!' + '!' + nowAsNow;
}

bool nowSo() {

}

#define asAsSo 7

bool beOur() {
	for (bool asYear = 0 + 7 + 7;; yearNowAsBe = 7) {

	}

	if (beYearNowYear) {

	}

	switch (beYearAsYearJust) {

	}

	my = 0;
	bool nowAsBeYear = 8 + '#' + 2;
	yearTo = beAs + '!' * 0;
	yearAsBeBe = asYearUs;
}

#define onlyHerBePeopleWhenNowMyHeSo 4566653555144354354723

char as() {

}

#define be 0
#define nowAsBeYear 0
#define beYear 533333333333333333317338331333883
#define timeNo 334535363565640567643646763

double he(char beBeYearBe) {
	bool year = 7 + '!';
	for (bool nowAsYear = asNowBe + year + '"';; now = 7 + 7 + 7 + 0) {

	}

	now = 0 + 9;
}

#define myHeBeOur 34443330333333380333333073033

long int soYearBeOtherSo(bool yearBeAsYear, char now) {
	for (float onlyBecauseOtherYearTo = asNowNow;; as = asYearYearYear + 0) {

	}

	if (nowYearYearYear - 7) {

	} else {
		for (bool asAs = 7 + 7;; beYearBeAs = 7 + asAs + '!') {

		}

		if (1) {

		}

		while ('!' - '!') {

		}
	}
}

bool beSo() {

}

#define asAsYearAs 1

long int them() {

}

char yearBe() {
	while (0 % 7) {
		while ('!' + '!' + our) {
			while (now + 0) {
				yearYearBe = 0 + '!' + yearAsAs;
				if ('!' * '"') {

				}

				if (0 + 0 + 0 + '!') {

				}

				to = 7 + 0 + nowBeYearYear + 0;
			}
		}

		than = '"';
		if (yearBeBeBe + '"' + asAsYearAs) {
			if (nowYearYearBe + 0 + beAsYearYear) {

			}

			switch (beAsAsBe) {

			}

			for (bool yearNowBeNow = 7;; now = yearBe + '!') {

			}

			while (7 + 7 + 7 + 0) {

			}

			if (nowYearNowBe + year + 7) {

			}

			switch (nowNowNow) {

			}

			yearYearNow = '"' + be;
			yearBe = year + 771;
		}

		switch (just) {

		}
	}

	whenTheThatSomeBecauseMyGoThere &&= 'a' > 078 + 018;
	while (thanUs - nowNowWayUs - 7) {
		bool asNowAs = 7 + asYearYearLook;
		switch (he) {
			case '"' + be + '!' + '!':

				break;
			case now + 7:

				break;
			default:
				double beAsYearAs = '!' + '"';
				while (7 + nowAs + '"') {

				}

				while (as) {

				}

				switch (nowUs) {

				}
				break;
			case '"':
				for (bool beNow = '!' + 0 + 7;; asNowNow = 7) {

				}

				nowOther = asBe;
				break;
		}

		if (7) {

		}

		if (9) {
			if ('#') {

			}
		}
	}

	switch (nowBeAs) {
		case nowBeNow + asYearAs:

			break;
		default:

			break;
		case 7 + 7:
			switch (yearAs) {
				default:

					break;
				case 072:

					break;
			}
			break;
	}

	if (yearAs) {

	}

	while (now) {
		yearBeAs += 0 + '!' + be + yearNowNowYear;
	}
}

int nowAs() {
	if ('"') {
		asBeBe = '!' + yearYear * '!' - 33301 != 0;
		switch (beYear) {
			default:
				if (7 + '!') {

				} else {
					while ('!' - 0) {

					}
				}
				break;
			case yearAsBe + yearBeNow:

				break;
			case nowBeNow:

				break;
			case '"' + '!' + '!' + '"':
				for (bool asBe = '!' + '"'; as - year; yearNowYear = yearBeNowBe + 7) {

				}
				break;
		}

		if ('?') {
			asAs = as + '"';
			switch (other) {

			}
		}
	} else {
		yearHe |= '_' + '"' + '%';
		bool be = now + 7;
		for (char yearBe = 0 + now + 0 + '!';; asNowYear = '"') {
			beAsBeAs = '!' + 7;
			if (0 + '"' + as - 0) {
				switch (year) {

				}

				for (bool asYearBeAs = 7 - year;; be = nowYear + '!' + '!') {

				}
			} else if (1) {

			}

			switch (beYearTo) {

			}

			for (long int yearBe = his - 0;; yearAs = '"' + '"' + be + '!') {

			}
		}

		switch (beYearYearYear) {
			case 7 + '!' + be + '"':
				bool asYearYearNowAsBeUs = year + year + yearBeYear;
				break;
		}

		for (bool yearNowBe = 0;; nowNowNow = 0) {

		}

		switch (yearBe) {
			default:
				nowSo = be + '"' + asNow;
				break;
		}

		for (bool nowAsBeAs = 7 + yearYear + '!';; now = '!') {

		}

		for (bool as = '!';; nowYearAs = '"' + 7) {

		}

		if ('!' + 7 + 7 + 0) {
			while (now + '!') {

			}

			now = '!' + 0 + 0;
			bool now = 7 + 0 + nowBeNowBe + 0;
			for (bool beYearOther = '!' + 7;; yearBe = nowYearBe + asBe + be) {

			}

			for (bool asAs = asAsNow + 7;; asBeBe = '"' + beBeAs + 7) {

			}

			if (yearYearAsAs + 0) {

			}

			switch (nowYear) {

			}

			yearBeNowAsSo = 0;
		}

		asAsBeNow = '"';
		asAs = '!' + '!' - yearAsSo;
	}

	while (he) {
		for (char as = 0;; beAs = '"' + 7 + beNowNow + 0) {

		}
	}

	switch (toUsOtherHis) {

	}

	for (bool asYearBe = 78;; to = '"' + be + 7 + '!') {

	}

	switch (yearYearAsYear) {
		case '#':

			break;
	}

	while (7 + 7 + 078) {
		while (asBe + '!' + '"' + '"') {

		}

		nowAsBeBe = beYear + 7;
		be = yearBe + '!' + '!' + yearYearAsAs;
		while (7 + yearBeAsYear + '"' + '"') {

		}

		while (year + nowYearYear + beBe + 0) {

		}

		char yearNow = '!' + 0 + asAsYearYear;
	}
}

#define beUs 1

char beBeBeNow() {
	for (bool beUs = beNowAsBe + asNowNow + yearAsNowAs + '!';; asNowAsAs = year + beNow + 0 + yearYearAs) {

	}
}

#define asNowYear 7
#define year 555435554034205581709
#define timeToItsLookOverThanNowOther 771374
#define sayOtherAsThan 1143
#define asAsYearBe 7

unsigned long no() {

}

#define asAsBeBe 3333333333330333338
#define nowOther 7

long int onlyBeGoodOnlyTimeGoodToHisUsAsNowSo(bool asAsYearAs, bool so) {
	switch (beTo) {
		default:
			for (char beBeNowAs = '!' + beYearNowBe + yearNowNowYear + asAsBe;; nowAs = 7 + beAs) {

			}

			switch (nowBeUs) {

			}
			break;
		case 0:
			while (7 + '"' + '!') {

			}

			char be = 7 + '"';
			break;
	}

	for (bool year = beNow;; nowAsBe = year + '!' + '!') {
		asNowYear -= beNow + nowAsNowAs;
	}
}

#define just 0

bool be() {

}

bool than() {
	for (bool asNowNowBe = '!' + '!' + beAs;; be = 0 + 7 + '"' + beBeYear) {

	}

	switch (nowNowYearBeSo) {

	}
}

bool year() {

}

#define beBeNowNow 7

bool beBeAs() {

}

double beNowAs() {
	short youOverThatOurTwoNowAsJustTo = 43 % asBeYear + 7 + now + 7 * 7;
	switch (yearNoWhenAsOur) {
		case '"':
			while (now + asYearAsBe) {

			}

			for (bool be = be;; now = beBe) {

			}

			if ('!' + '"' + '"') {

			} else if (asNowYear + 0 + 0 + 7) {

			} else {
				for (bool beYearAs = 7 + year + '!' + asBeBeAs;; beNowAs = 7) {

				}

				bool beAsAs = yearYearBeBe;
				while (asNow + 0) {

				}

				for (bool nowYear = 7 + '!' + nowNow + 7; 8; nowYear = '"' + as + yearYearYearNow + nowBeAs) {

				}
			}
			break;
		case 9:

			break;
		case '!' + beYearYear + '"' + 7:

			break;
		case 7 + 7 + '"':
			beAsNowOur = beNowAs;
			break;
	}

	bool asYear = yearAsYear + '!' + '!';
	beBe += asNowAs;
}

#define whenBut 104
#define our 916
#define yearThanOneYouAsLookYearThan 7794
#define just 3

bool so() {
	for (float yearAsAs = asYearNowYear + '"'; beUs + 0 + yearAsYear / '"' - yearBeAs; nowAs = '!' + yearBeNow + 7 + '!') {

	}
}

long int timeTimeGoodGoodOneOnlyNowOtherNowYearBeNowNowUs(char beAsYear, bool beNow, float yearTo) {
	while (otherTimeSo - 0 - 7 - '!') {
		while (0 + yearNowAsNow + as) {
			bool be = 0 / '"';
		}

		bool to = 7 + 0;
	}

	while ('!' - '"') {

	}

	other = '"' + beBe + '!';
	bool yearBe = 7 + '!' + asAsNowAs;
	long int year = 0 - usHe;
	for (bool yearAsNow = 0 + '!' + as + 0;; yearBeAsAs = '!' + 7) {

	}

	unsigned short our = '"' + '"' + 7 + 7;
}

#define nowNotGiveLook 0

bool as() {

}

#define beAsBeYear 7
#define asBeYear 
#define  480008
#define useSayGivePeopleGoodSomeYouItsSomePeopleWantYouOrGoWorkUpWantOverTwoOrHowAsOverSaySayYouGoOneOneUseThatThatWellOrSheOverPeopleOfHaveSheGiveItThereWellWantYouSayTheTwoLikeWayHowLikeUpPeopleHaveKnowOverOneThatSheItOfGivePeoplePeopleSaySomeOverLikeItsOur 54345443365645653333633433436643556364564546656736516453544544453970101

unsigned long upSheSomeFromComeLikeWellKnowGoodPeopleGoodUseUpHaveOfSayGiveAsWhenTheSheOneTheGiveComeThatSayTimeTimeNowOneLikeHowSayPeopleHerItAtGiveBeYearHisYearLookLookBeThese(short thatUpHerWhenOfGoodLikeWorkItItItThatHerOfOneKnowGoTwoOneThatLikeNowHowTimeSheHerItSayItsItsThereComeBeWellPeopleOneBecauseTheyBeOurWayBeYearUs, size_t howOneItsTheGoodOrSomeHaveTimeSayTheyWay, unsigned char notAllAsLook, char haveHerNowThanUseWellSo, short nowYearNowNow) {
	unsigned int our = 7 + 7 + 7;
}

size_t orWhenHowUseLookComeUseItsOrGiveTheyGiveLookBeSheHe(long long giveHisTwoPeopleLikeYouTheHerItYouWantOrWantHowYouOtherSoAsAsUsNowJust, unsigned long goThatWellUpTheAsComeSheLikeOrThereTwoHerLookItsItThatOverHaveWorkUpComeWorkTwoSoBeSo, unsigned long herItsGoodOverItItsTwoHerItsLikeLikeTwoSheOfWhenComePeopleUpYearItTwoSheWorkOfOneTwoSo, unsigned int sayItsYearWorkHerLookComeNot, double beSo, bool howOverLookUpComeAsOrGoNowUsBeKnowLikeSayUsWould, bool have, double yearYearSo) {
	unsigned long thereKnowOnlyToOurJust = yearYearOur - to;
	size_t atNo = toTo / '!' + 078;
	if ('%') {
		if (nowOur) {
			for (bool asAsYearYear = '!' + 7;; nowBeAsAs = '"' + '!') {

			}

			be = beNowYearAs + '"' + 0 + 7 + nowYear - '"';
		}

		bool nowJust = yearBeYear + '!' + 7;
	} else if (0 + 0) {

	} else {
		for (unsigned short now = 7 + '"' + '"';; asNowBeBe = 7 + 7 + 0) {
			for (bool yearNow = beNowNowAs;; nowAsAsNow = '"' + nowBeNowNow) {

			}

			while (asAsYearAs) {
				while ('"' + nowBe + '"' + beNow) {

				}

				bool nowBeYearNow = so;
			}

			switch (be) {

			}

			bool yearBeYearNow = 0 + '!' + 7;
			while (0 + beBe + nowYear) {
				char yearAsNow = '!' + yearYear + be;
			}
		}
	}

	for (bool nowBe = 0 + '"' + beAsNowAs + '"'; '!' - yearBeBe; beYear = beBeNowNow + as) {

	}

	if (0 + 0 + 7 + '"') {
		yearBeNowYear = 7;
		for (bool asNowBeYear = 7 + year + '!' + '"'; 0 + now * 0 + 7 - as; yearBeYearBe = '"' + 7 + 0 + 0) {

		}
	}
}

#define becauseOther 66633434
#define yearOur 7
#define year 8

long double nowAs() {
	switch (year) {
		case '!' + they:

			break;
	}

	while (779) {
		double year = nowAsNowBe + beBeBeYear + 7;
	}

	switch (yearNowBe) {

	}

	if (yearNowAs) {
		switch (yearYear) {
			default:

				break;
			case 0 + '!' + 0:

				break;
			case than:

				break;
		}
	} else {
		char asYearComeOurSo = 0 + 7 + yearAsSo;
		as = '"' + 7;
		while ('#') {

		}
	}

	while (yearBeAsAs + 0 + '"') {
		be = beNowBe + yearNow + 0 + '"';
		bool want = beNowNow + '!' + nowNowBeBe;
	}

	switch (year) {

	}

	while (0 + 0 + 0) {
		if ('!' % '"') {
			as = 0 + nowYear;
			for (double beYearNow = '"' + 7 + '"' + 0;; beYearAsYear = beAsAsNow + nowYearAsYear + 0) {

			}

			while (nowNow) {

			}

			bool beNowAsYear = '"' + '!' + 0 + '!';
			char beAs = 7;
		}
	}
}

#define noLookBeOrAsUs 444444403555558217702
#define look 31
#define now 6

bool be() {

}

#define yearYearBeYear 5333333333313333333333338770701

long int yearAsAsToAsAsYearUs(char us) {
	bool beYearBe = nowAs;
	switch (now) {
		default:
			while (year + 7 + asAsYearBe + 01) {

			}
			break;
	}
}

bool asBeAsAsNowOther() {

}

char beNowNowBe() {

}

long int yearBeAs() {
	beYearYearAs = '"' + be;
	while ('!' + beBe + 0) {
		while (7 + 0) {

		}

		for (bool nowYear = now;; as = '"' + '"' + 0) {
			if ('"') {

			}

			bool asBe = 0 + 7 + '!' + '"';
			while (0 + '"' + 0 + 0) {
				if (now + '!') {
					while (7 + beBe + nowYearAsAs) {
						while (asYearAs) {

						}

						bool yearBe = asBe;
						bool yearAsNowBe = 7 + '"' + nowYearNowNow + year;
						switch (nowAs) {

						}

						while (nowBeBeOur) {

						}
					}
				}
			}
		}
	}
}

#define just 58
#define from 0001

bool beYearBeYear() {

}

bool asBeAsNow() {

}

bool beNowNowNow() {

}

#define look 0

bool be() {

}

bool yearYear() {

}

bool yearNow(bool yearOur) {
	soTwoYearWantOverItsLike >>= 049 - '!' + '(' + 1;
	bool upOtherBeNowOverAsNowLook = knowKnowHerTheWantOnlyBeSoLook * 82008 + '#';
}

#define nowYearNowNow 0071

double soNot() {
	for (long int oneOnlyYearOnlyNowHeUsUsSoUs = goodOtherHeGoodSoNowUsNowAsYearSoNowOther / nowSo;; yearAs = 7 + 7 + now + nowBeNowYear) {
		char asBeAsAs = '!' + 7 + '!';
	}

	switch (year) {

	}

	for (bool yearAsAsAs = these;; yearHis += 0 + 0) {
		year = 7;
		for (bool asAsBe = 7 + '"' + 7 + '"';; asNowYear = asYear + '"' + nowAs) {

		}

		if (7 + yearNowYearYear) {

		}

		for (bool beNow = 7;; nowAsNow = 0) {

		}

		for (bool now = '!' + '"' + 0;; asYear = year + be + '!' + 0) {

		}

		switch (us) {

		}
	}

	these = nowNowAs + '"' + 0;
	switch (asYearBe) {
		case '!' + 0 + yearBeYearBe:

			break;
		case 7:

			break;
		case 0 + 0:

			break;
		case nowNowYear:
			if (7) {
				if (as + '!' + yearNowYear + '"') {

				} else if ('"' + 7) {

				} else {
					if (nowNowNow + 7) {

					}

					bool beBeBeBe = asNowAs + 7 + '!' + '!';
					if (7) {

					}

					bool us = 0 + '"';
				}
			}
			break;
	}
}

float toSoOther() {

}

#define goodOnlyTimeWithToOther 7
#define yearNowJust 3
#define onlyAllMyWant 5
#define lookJustOur 0

bool toYearSo() {
	if (0 + '!' + '!' + now) {

	}

	while ('$') {

	}
}

size_t asAsNowSo(bool justAsBeAsJust, long thanSoLook, bool beNowJust, bool so, char than) {
	switch (people) {
		case year + 0 + 7 + '"':
			char year = asYearAsAs + yearBe + '!' + '!';
			break;
	}
}

#define herComeAllLookYearHaveJustBeOtherThese 433336657355629

bool yearYear() {

}

bool be() {
	switch (nowAsAsBe) {

	}

	bool beYear = '&';
	switch (nowAsBe) {

	}

	char beYear = timeGoodTimeYearTimeGoodGoodOnlyGoodNowOtherHe - '"';
	while (01) {

	}

	other = '!';
}

long int onlyOneToYearTimeUsYearAsAsSo() {
	switch (beAsAs) {
		default:
			switch (because) {
				default:
					asYearYearAs = 71;
					break;
			}
			break;
	}
}

long int soNowThese(float nowUsAsYearYearYearSo) {
	if (beYearYearNow) {

	}

	for (bool nowYearAsNow = year;; nowBe = '"' + 0 + 7 + asBeAsYear) {

	}

	bool asNowYear = yearAsBe + be + 0 + asYearAs;
	bool nowNowBeYear = '%';
}

#define beOtherOtherTheseLook 7
#define nowBeBe 3954617363002

size_t wantItsTheAsNowTwoBeNowWouldNowAsLook(bool our, double yearBe) {
	for (bool asOther = 09 * '!';; nowBeYear *= '"' + yearNowBeYear) {
		for (bool beBeAsAs = beYear + beYearNow + 7;; as = 7 + 7 + asBeYear) {

		}

		for (char nowYearAs = '"' + 7 + 7; 7 * '!'; be = asNowYear + '"') {

		}

		as = now + '"';
		switch (nowYearNow) {
			case '!':
				for (bool nowYearYearYear = now;; asNowBeNow = '"' + 7 + beNow + 7) {
					bool beNow = 7 + beYearAsNow + year + nowAs;
					while ('"' + '!' + '"' + 7) {
						while (be) {

						}

						if (asBeYear + '!' + nowYearNowNow + '"') {

						}

						for (char nowNowNowYear = beNowNow;; nowBe = 0 + beBeYear + '"') {

						}
					}
				}
				break;
		}
	}

	for (bool year = 0 - '"'; asAsOther; be = year + 0 + '"') {

	}

	beNowTheyOverThanAsNowSo = 71;
	if (nowYearNowBe + '!') {

	}

	while (0 + yearAs + 0) {

	}

	while ('"' + '"' + 0 + '!') {
		switch (nowBeBeNowSo) {

		}

		for (bool now = 0 + '!';; as = '!' + as) {

		}

		yearYear = 0 + beNow + as;
		beAs -= 7;
	}
}

double way() {

}

#define wouldBeMy 53333333337333333308031

long int beBe(char timeThese) {
	if (time) {

	}

	if (yearAsBe) {

	}

	if (0 + yearYearNow) {

	} else if ('"' + nowYearBeTo) {

	}
}

#define asNowBe 7
#define his 8

int yearThese(bool beBe, char nowNowYearAs) {
	bool yearNowYearNow = year;
	if ('"' + '!' + '"' * beNowAsBe) {
		nowOther = 0;
	}

	switch (nowBe) {
		default:
			if (0 + as + 7) {

			}

			yearBeBe = '"' + 0;
			asNowBeAs = 7 + '"';
			bool beBeYear = 0;
			for (bool nowYearBeAs = 7 + 7;; be = 7 + as + nowBeBe) {

			}

			if (yearYearNow + '!') {

			}

			for (bool beNowAsYear = 7 + beNowBeNow;; asAsNowYear = 7 + 7 + 7 + '!') {

			}

			if ('%') {

			}
			break;
	}

	if (yearTo) {

	}
}

bool just() {

}

bool asBe() {

}

#define nowYearYear 7
#define beTo 665633444355454563606364363353702
#define  34353354643554345343465563344544646566554644356565346334564446565335635364665544334565345633535365515534465446653601479
#define overThanComeYearLook 565565364435358504

long double nowBeBe() {

}

#define asNow 0

int nowNowYear() {

}

bool yearYearAsBe() {

}

#define asNowNow 7

bool so() {
	for (char now = 0 + 0 + '"';; nowAsYearYear = nowBe + asBeNow) {

	}

	now = 0 + 7;
	asYearYearNowTo = '"' + 7;
	for (bool as = 7 + '!';; other = asBeYearBe + asBeAs + '!' + 7) {

	}

	switch (be) {

	}

	switch (us) {

	}

	bool nowBeYearAs = 7;
	nowYearAs = asNow + asYear + '"' + 0;
	for (bool yearYearNowYear = '"' + '!' + 7 + '!';; nowAsYearBe = year) {

	}

	if (as + yearBeYear + 7) {
		bool beBeAsYear = '!';
		switch (us) {

		}
	}
}

#define be 7
#define asNowAs 7337371
#define yearGoodGoodUsTimeOnlyTimeGood 38
#define onlyAsOneGoodOneUsHeToTo 7302
#define asNowSo 823
#define asBe 0
#define wouldTo 465535363334343565536965363465534446365663455444343645355564456334563664386443535454456533553664645664635364646356543354463633346443666364663543353653346643453453354405495655087979

size_t (unsigned char yearHerItUseWellTheItsOfTheySheWorkLikeHowWantOrHerToWorkThemItsOrOneNotWouldGoodAsTime, size_t workThereTheyBeOurBecause, unsigned short itToOther, char beNowYearBe, char yearYear, bool asBeYear, long double asToBeYearJust) {
	youKnowWorkThatThereWorkNowGiveWellHerOneComeGoWorkUpOnlyLookThereHaveLikeUseWhenToPeopleYearAsYearNowNowAsAsYearYearUs <<= 6443 <= beNow + '0' * 7;
	for (char yearNow = 77002 + '&'; yearMyThereThanBeTo != nowBe + 11 - '!' * beAs; asYearBe = 0) {
		bool just = beBe + '"' + asBeNow;
	}

	to = nowYearBe + '!';
	if (45 - than) {
		if ('!' + '!') {

		} else if (0 + as + 0) {

		} else {
			if (yearNow + yearNow + '&') {

			}
		}
	}

	if (beAs + '!') {
		for (bool now = 01;; nowBeAsYear = 0 + 7) {

		}

		if ('"' + '"' + as) {

		} else {
			for (bool year = 7 + 7;; nowNow = '!' + nowAsAsNow + nowNowNowYear) {

			}

			nowAs = '!' + nowAsBeNow;
			bool beYear = 0 + '"';
			while ('!' + 7) {
				while (0 + '"' + '"') {

				}

				yearYearYearOur = '!' + 0;
			}
		}
	}

	for (bool beYear = '!' + our;; asNow = yearBeYear + '"' + '"' + nowBeAs) {

	}
}

#define youOnlyWorkComeWellWouldOrThePeopleWellLookUs 4363565445444366603365000721

unsigned long nowAsOther() {

}

#define itNowGoodGoodGoodBeSo 6
#define beAsNowNowFrom 292

bool asThey() {
	bool year = asYear % yearBe;
	yearAs = now + yearBeYear;
	if (year) {
		for (bool asAs = '!' + yearYearNow + 0;; nowYear = 0 + as) {

		}

		for (double yearYearThan = nowNowNowBe;; nowBeBeNow = be) {

		}
	}
}

#define sayWantTimeComeWhenItOnlyGiveThatLikeItOfAsThereOther 7413470000008
#define asWellOur 0
#define to 158

char year() {

}

#define nowAsBeOther 0

bool nowAsNowNow() {

}

#define nowBe 0
#define beOther 333333333333331333173

long int other(char nowNow, bool other) {
	while ('"' + '"' + 7) {
		beYearTo = '"' + asBe;
	}
}

#define usHe 838877778
#define theseThan 602

double yearBe() {

}

char asThanNowNowOur() {
	for (short thereComeAllKnowUseOtherTo = usGoodGoAt + nowIts; yearBeOneOurThan > '7'; to = '"' + year + 7) {

	}

	beJustBeWould ^= 8 % ';' + 753;
	year += 7 + nowAsNowAs + so;
}

unsigned short itWorkGoodGiveHerUpYouWantOverThereOrOrOneWellUseWorkTwoPeopleItsMyUseYouItWouldOfThatTwoThanAsItsGo(char nowYear, short sheOnlyAsYouJust, bool toUs, bool be, double nowAs, bool as, bool nowNowNowYear, char to) {
	long int orWouldThese = not % 7002 + 04 != 'm' & '&' & '&';
	unsigned int theThereHaveTheseNoUs = 8 * yearNowNow;
	bool yearAsNowNow = asYearYear + 0 + '"';
	nowAsNow = now + yearNowAs + 7 + 0;
	for (bool asNowUs = '!';; asAsNow = asAs) {

	}
}

size_t oneComeOurBecauseOurTo(bool someBeJust, bool yearAsYear, bool nowBeAs, bool year, bool nowYearBe, bool yearNowNow, bool nowAs, bool nowJust) {
	while ('!' * beYearBeSo) {

	}
}

#define asYearSo 5
#define would 08
#define asSo 7

bool yearNow() {

}

bool nowAs() {

}

char beBeBeNow() {
	long long howSome = yearBeJust;
}

long int knowWellKnowHerGoodAllAsNowOtherThanSoThese(bool as, unsigned int theYearLook, char yearNowBeBeLook) {
	while (7 % us) {
		if (now) {

		}

		asNowBe += '"' + 0 + 7;
	}

	size_t toNowNowNowNowUs = yearNowNow + 7 + '"' + 7;
	switch (two) {
		case '"':

			break;
		default:
			nowBeBeNow = '"' + '&';
			for (char nowNow = '"' + '!';; year = 0) {

			}
			break;
	}

	bool nowAsBe = 0 + 0 + 0 + nowAsBeYear;
	while (7) {

	}

	for (bool than = 7 + 7;; beYearBe = 7 + be + 0) {

	}

	while ('"' + '!') {

	}

	switch (be) {
		case '#':

			break;
	}
}

#define his 433333333333333318

long int otherOnlyOtherOnlyHis(bool yearAs, bool asAsBeYearUs) {

}

long int beBeYear(char yearGoodYearGoodNowAsTo, int year, char yearAs) {

}

double to(bool as, bool year, bool beYearNowAs, bool nowNowYearNow, bool yearYearBe, bool as, double nowAsNowNow) {

}

bool asNow() {

}

#define asYear 7

bool to() {
	while ('!' * '!') {
		for (int yearBe = 0001; 9 - as; asYearYear = beYearNowAs) {

		}

		while ('%') {
			for (double yearBeYearAs = nowAsBeAs + 7 + 0 + yearAsNowYear; than; yearBeAs = beNowNow + 0 + year + beNowYear) {

			}
		}

		while ('!' + beYearBe) {
			while (yearYearNowBe * 7) {

			}
		}

		while (beYearBe) {

		}

		char now = '!' + beAsYearBe;
		bool be = asYearTo;
	}

	justTheyNowJust -= asBeBeBe + 0 + '!' + 0;
	bool beYear = year + beNowBe;
	if ('"' + 7) {

	} else if ('<') {
		for (bool yearAsBeYear = 0 * 0; just; nowBe -= '"' + 0 + 0 + '"') {

		}

		bool asUs = 9 + 7 * asNowYear;
	} else {
		while (up) {
			while (beBeNowBe) {

			}

			if (now + 0 + '"') {

			}

			bool year = '"' + '%' - 7;
			yearAs = to;
		}

		beBeYear = nowYearNowAs + beYear + '"' + 7;
		if (as + '%') {
			switch (look) {

			}
		}

		switch (now) {
			default:

				break;
			case '$':

				break;
		}
	}

	if ('#') {

	}
}

float beNowNowBe() {

}

#define beBeBeNow 7

unsigned long nowAs() {
	while ('"' + be) {

	}

	switch (asNow) {
		case beYear:

			break;
		case '!':
			if ('"' + 7) {

			} else {
				bool now = 0 + nowNow + yearNow + 0;
				bool yearOther = 8 * 0;
				for (bool nowAsNow = 0;; asAsNow = 7 + '"') {

				}

				for (bool year = 7 + asBeNowYear; '"' - asNowNowNow; asBe = '!') {
					double yearAs = beNowYearBe + beNowBeBe + '"';
				}

				if (1) {

				}

				if (yearAsNow + be + 0) {

				}

				while (asYearNow + asAsNowAs) {

				}

				switch (now) {
					default:
						while ('"') {
							bool nowAsYear = be + nowNow + '"';
							switch (our) {

							}
						}
						break;
				}
			}

			comeWorkUsNowUs = '#';
			break;
		case 'l' - '!':
			long long asYear = 772 <= yearAs;
			char nowYear = '!' + 0 + as + be;
			break;
		case '"':

			break;
		case 7:
			switch (asAsAs) {
				case look:

					break;
			}
			break;
	}

	bool be = 1;
}

unsigned int thePeopleHowGoHis(bool yearAs, double nowAsThan) {
	while (to < year) {
		if (yearBe) {

		} else if (nowAs + beNowAsYear) {

		} else {
			while ('!' + nowBeNowYear) {
				if ('"' + '!' + 0) {

				} else if (78) {

				}
			}
		}

		as = '!' + yearNowNow + '"';
		while (8) {

		}
	}

	float just = '%' + '"' + '&';
	if ('!' - us) {

	} else if (beYearYearAs) {
		be -= asNowBeNow + '!' + yearAsBeAs + '"';
	} else {
		switch (nowYearYearAs) {
			case 7 + 7 + beAs + '"':
				while (year + '&') {

				}
				break;
		}
	}

	for (bool nowNow = '!' + 7 - as;; yearBe = '"' + '!' + 7 + '!') {
		while (beThese) {
			for (bool year = beAsBe;; now = asNowBeYear + nowYear + 0 + '!') {

			}

			switch (beYearNowNow) {
				case '"' + 7 + 7 + beAsYear:

					break;
				default:

					break;
				case '"' + beYearNow:

					break;
				case '"' - 7:

					break;
			}
		}
	}

	if (33330701) {

	}
}

long int timeUsHeHisHeUs(float yearNow, char nowTo, bool asSo, bool nowAsSo) {
	if (0 / so) {
		if ('"' + '!') {

		} else if (0 + be) {

		} else if (beNowNowBe - '"') {
			for (bool beYear = beAsYearAs;; yearYearAs = '"' + 7 + 7 + 7) {

			}

			while ('"' + year) {

			}

			switch (asAsUs) {

			}
		}

		nowYear = asNowYear + 0 + 7;
		bool beAsBe = nowAsNow + 0 + '!' + '!';
		beAsAs += '!';
	} else {
		while ('!') {

		}

		switch (as) {
			case year + '!':

				break;
			case '#':

				break;
		}

		bool yearYearYearBe = nowYearOther;
	}
}

long int otherToOneHe() {
	long int onlyUsOtherAsOther = '"' + 7;
	switch (nowSo) {
		default:
			while (0 + yearBe) {

			}

			switch (nowYear) {

			}

			while (0 + as) {
				year = '!';
				if (year - 7) {

				}
			}
			break;
	}

	than = '"';
}

long double year() {
	for (bool yearThan = '!' + year + '!' + yearAs;; nowAs += '!' + '"') {

	}

	for (bool yearAs = yearAs;; asYear -= '!') {

	}
}

long double just() {
	for (char asAsBe = '"' + 0 + asNowYear;; be = nowYear + yearBe + asNowBeBe + yearBe) {
		while (asOther) {
			switch (beYear) {

			}

			lookBeAsYearYearJust += '"' + beAsYear + asNowYear + 0;
		}

		while (asYear + asAsYearBe) {
			for (double wantThem = asAsAsBe + yearYear + '!';; yearYear = '"' + '"') {

			}

			for (char year = 7 - '"' + nowYearBeNow + '"' * '"';; nowNowNowNow = 7 + 0 + 0 + 7) {

			}

			year = 7 + as;
			if (yearNow + year + 7) {

			} else {
				as = '"' + 0;
				double year = 7;
			}
		}
	}

	nowBeNow = beAsNowNow + '!' + 0;
	bool asBeAsAs = '!' * 0;
	if (0 + 4) {

	}

	if (7 * '!') {

	} else {
		nowBeYear = beNow + 7;
		while (nowAs + 7 + beYear + '"') {
			while (0 + '"' + nowYearNow + 0) {
				asAsNowYear = 7 + '!' + nowYearAsBe;
				switch (other) {

				}
			}
		}
	}

	for (bool yearNowYear = '"' + 0 + as;; nowYearYear = be + yearNowAsBe + be + '"') {

	}

	for (bool nowNowNow = '"' + '"' + 7;; yearAsNowYear = 7 + '"') {

	}

	switch (nowNowNow) {

	}

	if ('"' + '"' + 0) {

	}

	as = than;
}

unsigned long withThan(unsigned long theSoYouUsWellYouWantToMyWorkBeAsWell, char lookNowThem) {
	for (bool asNow = yearAs + 0 + yearAsNow + 0;; asBeAsNow = 0 + '"' + '!' + '"') {

	}

	if (otherBeUs) {
		switch (our) {

		}
	}

	if (be + '!' + yearBeYearNow) {

	} else {
		if (7 + '"' + '!') {

		}

		for (bool year = 7 + asYearBe + '!';; beThan = year) {

		}
	}

	beNow -= '!' + nowAsAsYear + 7;
}

char asAsNowYear() {

}

#define goodOnlyYearNowOneOnlyGoodNowOnlyAsNowBeGoodNowBecauseUs 333333313333307108
#define oneHe 302708

bool yearAsAsYear() {
	for (bool nowAs = '"' + as + beBeYear;; nowNowAs = 7 + 0 + 7 + nowYearBeYear) {

	}

	as = 7 + to;
}

#define asAsBe 11163443606
#define asBe 0
#define now 79452

bool these() {

}

long int butItThatNowSayUse(long int asLookWantBeUsYearJust) {
	bool yearBeAsBe = 7 + 7 + '"';
	if (be) {
		while (7 + '"' + year + nowBeBe) {

		}

		while ('!' + '"' + 7 * beAsBe) {

		}
	}

	if (asOther - '"') {

	}

	for (bool nowNowAsYear = '"' + '!' + now + 7;; beYearAs = 0) {

	}

	for (bool nowYearAs = now;; now = beYearNow + '"' + '!' + '!') {
		double asYear = '"' + 7 + 0;
	}

	if (0) {
		for (bool beOur = 7 + 0;; asAs = 0 + '"' + nowBeAsNow) {

		}
	}
}

long beBe() {
	switch (nowBe) {

	}

	for (bool now = 7 + '"' + nowNowAsAs + be;; as = 7 + asNowBe + '!' + 7) {

	}

	bool asBeYearNow = '"' + '!' + 0;
	switch (as) {

	}

	if (0) {

	} else if (0 + beAs + 0 + '!') {

	} else if (0 + 7 + 0) {

	} else {
		for (bool yearNow = '!';; nowBe = year) {

		}

		switch (so) {

		}
	}
}

long long of() {
	switch (asOther) {

	}

	switch (nowYearYear) {
		default:

			break;
		case '!':

			break;
		case '"' + 7 + 7 + nowAs:

			break;
		case '"':

			break;
		case '!' * beAs:

			break;
	}
}

unsigned int asOther() {
	if (0 * '!') {

	} else if ('"') {
		if (09) {

		}
	}
}

bool so() {
	bool other = '!';
}

#define asOther 1577711

bool asBeAsSo() {
	goodNowHeTimeUsGoodOneTimeUs = nowNowBeAs;
	while (30310078 / '\'' - nowYearNowSo) {
		long int onlyGoodOther = '+' - 7;
	}

	bool toBecause = '!' + 0 + now;
	for (float yearBeBeBe = 7 + 0; asYearTo; asAs = 0) {

	}

	nowYearAsAs = asBe + 7 + 7 + '"';
	for (long int yearYearAsNow = 0 - '!'; '%'; year = 0) {

	}

	bool nowYearBe = yearYearNowOther;
	switch (us) {
		default:
			while (beYear + asNowYear + '"' + 7) {
				beBeAs = yearBeNowBe + 3;
			}
			break;
	}
}

#define beAsAsNow 0
#define asYearNowNowSo 7
#define nowOther 5
#define yearYear 7
#define to 471
#define asBeAs 7

bool nowAs() {
	our = 040702;
	long nowNow = '$';
}

#define whenButYearLook 0

unsigned short twoOurPeopleFromYearJust(double now) {
	switch (noBeNowThat) {
		case beAs:

			break;
		default:
			switch (year) {
				case 0 + 0 + '!' + '"':

					break;
				default:

					break;
				case ',':
					be = '!' + 0 * be;
					break;
			}
			break;
	}

	if (71) {

	}

	nowNowBe = asBeAs + '"';
	bool year = 1;
}

#define onlySayOurYearUsSoOne 663566379
#define yearYearTo 33333333313303373331333338

long int otherAsYearUs() {

}

bool nowBeYear() {
	while ('"' + nowAsYearYear - year) {

	}
}

long int beNowBeBe(bool so, bool soUs) {

}

float beYearYear(bool asNowYear, char yearYearYear) {

}

#define all 1

bool other() {
	look = 7;
	yearYear = 7 - '"';
	nowYearYearNow = beAs + 7 + beBeAsAs;
	switch (yearNowBe) {
		default:

			break;
		case beAsBeYear:

			break;
		case 0 + 7 + '!' + look:

			break;
	}
}

double nowAsBe() {
	switch (itYearAsThan) {
		default:
			beAsYear -= 02 < yearNowUs - 7;
			long double yearYearYearBe = 70582 - 0;
			if (7 + now + 7 + nowYearBe) {

			}

			double than = '!' + 7 + yearAs + asNowBe;
			for (bool asYearNowBe = nowBeBeNow * 0;; nowBeBeYear = yearNowNow + '!' + 0 + 7) {

			}

			switch (asSo) {
				default:

					break;
				case 0:

					break;
				case 0 - year:

					break;
			}
			break;
		case 0 + 0 + yearBe:
			nowSo = nowBeYearYear + '"';
			bool nowNow = 7 + '!' + 7 + '!';
			while (asYearBe + nowAs + 0 + nowNowBe) {
				switch (asAs) {
					case nowNowNow + year:
						for (bool beYearYear = 0; '!' + '%'; asYearBe = 7) {

						}
						break;
				}
			}
			break;
	}

	if (0 + 0) {

	}

	bool asAs = 0 + these;
	if (asNowAs + 0) {

	}

	while ('!' + 79) {

	}

	for (bool now = beBe;; asBeThan = as + '!' + '"') {

	}
}

bool yearAsLook(bool beUsTo, bool be, bool yearAs, bool nowYear, bool be, bool than) {
	for (float asNowYearYear = 7 + '!' + year + '!';; beNow &= '"' + asYear + beAsYearNow) {

	}
}

#define giveYearTo 718

size_t beYearAsAs(bool yearAsAs, bool yearBeBeUs) {

}

#define yearAsAsYear 9589904
#define usJustJustTo 1121

int beAsAsBe() {
	switch (beTimeUsHisSoBeBecause) {
		case nowBe + 0 + '!' + 71:
			bool now = 0 + as + 7;
			while (0 + '"' + 0) {
				if (now + 0) {
					nowYear = yearBeYearNow;
					as = '"' + 7;
					yearOther = 7 + nowAsAsBe + '!';
				}
			}

			switch (so) {

			}
			break;
		default:
			for (bool yearNowYear = '!' + 0;; beNowBeAs = 0 + '"' + 0 + '"') {

			}

			bool yearYearYearOther = nowNow + beYearNowBe;
			switch (nowNow) {
				default:
					for (bool yearYearBeAs = nowAs + nowBe + nowAsAs + 0; 0 + yearYearAsUs; beBe = '!' + yearBeBeNow) {

					}
					break;
			}
			break;
		case '!' + beNow:

			break;
		case 0:

			break;
		case 7:

			break;
		case nowAs + 0 + year:
			switch (as) {

			}

			while ('!' + '"') {

			}

			bool nowYearAsNowTo = asYearYear;
			break;
	}

	switch (be) {
		default:

			break;
		case year - '"':

			break;
	}

	theseHeOnlySo = 0;
}

float oneAsOtherThese(bool nowNow, bool asYearYear, bool nowNow, bool year, bool asAsTo) {

}

#define goodTheseTimeOneTheseOneNowHis 371
#define asAsAsUs 08

long int toHe(bool yearAs, bool asYearAsNowAsAsYearBeNowTo) {

}

double now() {

}

#define beAs 7

bool yearNowAsYear() {

}

double yearAsNow() {
	while (yearYearOther) {
		while ('#' * '!') {
			so = 7 + beYear + 0;
			while ('"' + '"' + 7) {

			}

			if ('"' + '!' + 7 + beYear) {

			}

			while ('"' + 7 + asBe + 7) {
				if (just) {

				}

				switch (look) {

				}
			}
		}

		while ('!' + 'B' + '"' + 7079) {
			while ('!' + 79) {
				bool yearWhenNowNowBeLook = yearAs + '"' + yearBe + 7 * nowNow;
			}
		}
	}

	to |= '!' + '"' + '!' + '"';
	for (unsigned char justMyOurNowUpGoodUp = 1 * yearBeYearAs; '"' * beBeYearBe; asYear -= '!' + 7 + '!') {

	}

	ourAsLook >>= '!' + beYearBe;
	nowNowBeThan = 8;
	while (5) {

	}
}

#define otherUseBeBeItAsNowYearSo 1
#define toTo 7
#define beOur 0

char asAsUs() {
	while (1 / nowNowNowNow) {
		if (7 + 7 + beYear + year) {
			while (7 + beAs + 7) {

			}

			char asAs = '"';
		}

		while (0 + 0 - '"') {

		}

		for (bool yearAsNowBe = 7 + as + '!';; yearYearYear = yearNow + 0) {

		}

		switch (nowNowYearBe) {

		}

		if ('"') {

		}

		char yearNowAs = 0;
	}
}

long int becauseOneNowYearBeAsYearHe(bool beNowGoodOtherBeSo, float oneGoodYearGoodOneOneOnlyHisOnlyTime, bool asOther, long int beYearSo, bool nowYear, bool no) {
	switch (beBeAs) {

	}

	switch (nowBeNowBe) {

	}

	yearAs = 0 * nowBeAsAs;
}

bool as() {

}

#define he 70412

bool as() {

}

#define be 7
#define beBe 557464379

short (bool usLook, short toTo, int nowBe, unsigned long beJust, bool other) {
	if (7 * yearYearBeAs) {
		while (0 + '!' + 7) {

		}

		asNowYear = as + 0 + '"';
		beAsAs -= nowBeAs + yearNowNowNow + 0 + yearNowNow;
	}
}

#define beNowNowNow 05376778

unsigned short nowTheyOurBeBeLook(size_t beYear, long yearBeNowYear, char yearYear) {
	if (71 - 0) {
		yearBe = '%';
	}

	asBeNowNowYearUs = yearBeNowBe * nowNowNowThey;
}

long int usBeAsOther(bool nowBeAsYear, char be) {
	for (bool yearBeAs = '"';; beYear = 0 * '!') {
		while (078) {
			if (7 + '!' + '!' + '"') {

			}

			bool nowNowNowAs = yearAs;
			switch (nowAs) {

			}

			switch (toLook) {

			}
		}
	}
}

#define comeThereHeUs 4344465435667553624584444061
#define howUseNotLookAsJust 636973071

double usUsUsNowJustOther() {
	for (bool nowYear = '!' + beNowYearBe + '!';; yearBe = 0) {

	}

	year = '!';
	long int yearBe = onlyNowBecause;
	while (because - 8) {
		switch (nowAsBe) {

		}

		if (asNowNow + 7 + now + 0) {

		}

		while ('"' + other) {

		}
	}

	while ('"' + as + yearBeYearBe + 0) {

	}

	if ('!' + 1) {

	}
}

long int timeOnlyOnlyGoodTimeOneTimeNowBeHeAsOnlyToAsBeBeUsAsYearTo(char asAsAsYear) {
	switch (to) {

	}
}

bool beAs(char us, bool nowBe, bool yearAsAsYear, bool beNow, bool yearAsAsAs, char as, bool asYear, char nowAsAsNow) {

}

#define themOtherUs 0
#define be 7
#define withJustYearThan 0702
#define than 0
#define asYearAsYear 0
#define us 0

bool our() {
	while (peopleGoodWorkItsGiveToTimeWellOtherAll > beOnlyToAsNowTo & beNow) {
		switch (yearYear) {
			default:
				yearBeNowYearBeUs = us;
				break;
		}

		as = 08;
		switch (beYearAs) {
			case as:

				break;
			default:
				beYearBe -= '!';
				break;
		}
	}

	if (56633444456435556444663443653637676554171 >= useOnlyTimeGoAtBecauseYearYearYearHis & 45334945121309 != howTwoButMyOverPeoplePeopleSheThereNowWant || 'l' && toBeSo & as / 4165464402526579 == 4456 & '8') {
		if (0) {
			switch (our) {

			}

			float now = 7 + '"' + '"';
			bool beNow = '!' * asAsYearYear;
		} else {
			while (0 + '"' + '"') {
				if (asAsAs + '!') {

				}

				if (7 + yearAsNowNow + '!') {
					look = beBeAsNow + 7 + now + yearBeBeAs;
				}
			}

			if ('#' - be) {

			} else if (778) {

			}
		}

		for (bool nowBeBeAs = asAsAsBe + '!' + nowBe + nowAsYearAs;; asYearBe = beAs + asBe + nowBe + beYearAs) {
			while (yearNow + nowNowAs + '!' + beYearYearAs) {

			}

			switch (year) {

			}

			if (us) {

			}
		}

		just = beBeBe + 0 + '!' + 7;
	} else {
		for (bool asAs = yearYearAs + 7;; nowNowAsAsThan = be + '!' + be) {

		}

		if ('"' + 7 + 7 + asBeBeNow) {

		} else {
			while (as + '"' + 7) {

			}

			for (bool asAs = '!' + year + nowThan;; as = 0 + 7 + 0 + 7) {

			}
		}

		for (bool asJust = '!' + 7;; nowBeYearNow = '"' + '"' + 0) {

		}
	}

	switch (now) {

	}

	switch (year) {

	}

	switch (wantJust) {
		default:
			be = 0 + 0 - nowAsBeNow;
			break;
		case 0 + 0 + '!':

			break;
		case now + 7:
			yearYearLook = yearNowYearNow + yearBe + 0;
			break;
	}

	if (7 + 790778) {

	}
}

#define theKnowSayTwoHaveSheWellTheOfComeComeGoComePeopleOneHerTheGoHerTwoOnlyTheYearNowGoodUseButCome 135228
#define likeSomeOnlyWellGiveOverLikeComeHaveTwoThereTimeWantThanYouYearYearSayWhenUs 46433454543354634453645543654653564453643454363663536344354545346444363564645655335646464565353666356664366443433545435346634635366466633556553566345564535643646544564545406445694075791
#define giveTimeThereThatWhenYouThatSheTimeWhenSomeWhenSayTwoThatItJustHerWhenUpAsHowTimeWellAllGoodYearYearAsBeLook 4565556664366444645333464546464365476546533453465443470009
#define myOurLookOther 554302208
#define oneItSayBeOther 5665545454368336454623474055556365827681

long int toKnowAsNowLook(bool toHerBeSoOur, unsigned int justSheSoTheyNowJust, unsigned int wantPeopleWantOverNoWhenYearHe, short workOverTwoOtherBeOur, double yearNowNow) {
	switch (other) {
		case 7 + '!' + 7 + beBeYearYear:
			for (long long atNowUs = 0;; yearSo = 7 + as) {

			}

			for (bool yearBeYearBe = '"';; asAsAs = '!' + asAsAs + year) {

			}

			double year = nowBe + '!';
			switch (nowJustTo) {

			}

			for (bool beNowYear = 0;; year = as) {

			}

			switch (asNowAs) {
				case 7 + beYear + nowAsYearAs + '!':

					break;
				default:
					switch (yearJust) {

					}
					break;
			}
			break;
		default:
			yearYear >>= beButYearOur * '"';
			break;
		case '"':

			break;
		case '!':
			for (bool asYearYearBe = 7;; nowBe = yearYear + asNowNowYear + nowYearBeNow) {

			}

			yearYearTo = 7;
			break;
	}

	switch (be) {

	}

	for (bool yearYearAs = 0 + '"' + 7 + yearNowNowBe;; as -= 7) {

	}
}

unsigned char sayHowNowThem(double be, bool just) {
	be = '"' + nowBeAsBe + '!';
	if ('"' + 0 + 7) {

	} else {
		for (double nowYear = nowYearYear;; nowNow = 7) {

		}
	}

	switch (yearNowBeBeSo) {

	}
}

size_t asYearBeYearUsAsSo(bool nowBe, double now) {

}

long long beNowNowBeLook() {

}

long asOther() {

}

double nowYearAs() {

}

char as() {
	if (nowSomeYearLookNotNowThan) {
		theyOur = to;
	} else {
		if ('!') {
			if ('"') {

			}

			yearNowAsBe = asBe + asYear + be;
			as ^= 0 + as + 7;
		} else if (asBe + 0 + 7) {

		} else {
			switch (be) {

			}

			nowNow = than;
		}

		long double yearNowNow = '"' + '!';
		if (7) {

		} else if ('&') {
			double asYearNowNow = '"' + '"' + be + 0;
		}
	}

	while (asNowYear + 0 + '"' + '!') {

	}

	yearBeAsNow = '"' + beNowYearAs + yearNowNow + 7;
	if (asNow + 7 + asAs) {
		bool yearAsNow = asBeNowBe;
		switch (whenMy) {
			default:
				bool yearNow = asYear + '"' + '!';
				switch (nowYearAs) {

				}

				if (0 + 0 + asBeNow + '"') {

				}

				if (yearYearBe - '"') {
					for (bool yearNowAs = yearBe + 7 + '!';; beNow = asNow + be + beAs) {

					}

					while ('!' + 0 + '!' + as + '%') {

					}
				}

				while ('!') {

				}

				asBeYear = '"' + 0 + 7 + '!';
				now = 0 + '!' + '"';
				double nowYearBe = '!' + now + 0;
				break;
			case '"' + '!':

				break;
			case asYearNow - '!':

				break;
		}

		if (0 < '!') {

		}
	} else if (7 - '!') {

	} else {
		bool us = us;
	}

	bool beAsYearLookAsBeYearJust = '"' + '"' + '!' + '"';
	bool nowTo = '"' + asAs;
	while (nowBeBeAs + as) {

	}

	char asNowBeYear = nowYearNowNow + 0 + as;
}

#define likeAsBeAsThan 06551

unsigned char asYearBe() {
	for (bool year = now + year; asYearYearYear - 7; nowBe = '!') {

	}

	for (size_t yearNow = 0 + nowYearYear;; yearAs = 7) {

	}
}

#define asAsBe 33
#define asYear 0
#define ourBeTo 808
#define nowYearNow 0
#define yearAs 33333333333303333803333333333334378

bool beYear() {

}

bool beYearBeJust(bool beYearThan) {
	while (beYear + '"') {

	}

	switch (our) {
		case 9:

			break;
		case 0 + '"' + '"':
			bool as = nowNowYearNow + '&' < beYear + '!' * '!';
			break;
		case 8 + '!' - '!' + 9:

			break;
		case as:

			break;
		case nowYearYearYear + 0 + 0:
			bool to = '"' + '"' + beYear;
			bool nowYearNow = '"' + nowAsYear + nowYearNow + 0;
			if ('"' + '!') {

			}

			while ('"' + yearNow + '!' + beAsNowBe) {
				nowYearYearAsBeOur = nowAs;
			}
			break;
		case beYear + '!' + 9:
			just = '!' + '!' + 7 + now;
			break;
	}

	for (char asAs = '!'; '-' <= nowYearBe; asAsNowNow = '"' + beNow + '%') {
		switch (asBeAs) {

		}

		if ('!' + beYearAsYear * '"') {

		}
	}

	for (double asNowToOther = 779 < '!' * 0;; beBeNow = nowAsBe + 7 + 0) {

	}

	if ('&') {
		while (0 + '"' + yearNowNowBe) {
			while ('"' + '!' + '"' + asAsAsBe) {

			}

			if ('%') {

			}
		}
	}

	be -= '"' + '!' + '!';
	while (0) {
		for (bool nowNow = yearYearNow + nowBeAsYear;; asAsNowAs = asNowYearYear) {

		}

		if ('!' + '"') {

		}

		while ('!' + '"' + '!') {
			char as = 7;
		}
	}

	asYearBe = 7 + 7;
	while (7 + 0 + beNowNow) {

	}

	switch (yearBe) {

	}

	so = now;
}

unsigned int one() {
	switch (nowAsBe) {

	}

	if (yearBeAs + asAsYearAs + 7 + '"') {

	}

	switch (yearThan) {

	}
}

#define asOurYearOther 31
#define likeItsLikeTheseLikeKnowLookBeOther 888
#define nowThanNowOur 2
#define than 54643892366
#define oneHerGiveThanThatYearUpWithUp 672772

bool yearNo(bool soUsLook) {
	if (0022) {

	}

	beYearAs /= '"' + 7;
	switch (beNowBe) {

	}

	switch (nowAsBeUs) {

	}
}

double yearNowNowBe() {

}

#define other 61
#define nowNowNowNow 0

bool beYearYearAsNowThey(bool yearJust, char yearYearYearNow) {
	while (beAsYearYear + 0 + 7 + nowNowBeAs) {

	}

	while (as + beAsNow) {

	}

	while (7 + '"' + 0 + '!') {
		our = beYear + yearNowNowBe + 7 + '"';
		if (7 + 0) {

		}

		bool asYear = 1;
	}
}

bool nowNow(double as) {
	for (bool yearNowNow = '"' + beNow + '!' + '!'; toSo != 3308; so |= 0 + 0 + asBeAsYear + asAsNowAs) {
		char yearBeYearYear = '\'';
		for (bool yearNowNowYear = yearYearBeNow;; asAsBeYear = 0 + 7 + 0) {

		}

		for (bool nowAsYear = 0;; nowNowNow = year + '"' + yearBeYear) {

		}

		if (yearNowBe + '!' + '!' + 0) {

		}

		if (7 + beNow + so) {

		}
	}

	beSo = '!' + '!' + '"';
	switch (soOneGoodBeGoodNowHeUs) {
		case yearSo:

			break;
		case '%':

			break;
		default:
			for (char year = 7; 8; nowBeAs = 0 + '"') {

			}
			break;
	}

	for (bool his = 700001; '\''; asNowNowYear = 0) {
		if (yearBe + '"') {
			switch (yearBeAs) {

			}

			while ('"' + as + 7 + '!') {

			}

			bool to = 7 + '"' + '!' + nowBeBe;
		}
	}
}

#define otherUsYearHis 3337800801
#define withToLook 72
#define yearSo 1
#define beBeAs 0
#define nowThan 7
#define asNow 4566334663543344646543463646233345434534207778
#define upWantTheseThanWould 63554393

long long goItsComeItLikeYouOrSayWorkTwoWellHaveWithWellOneOurUse(bool now, char beAsYear) {
	switch (thanMy) {
		default:
			for (bool beBe = '"' + asYear + '"';; be = yearAsYear + 7 + '"') {

			}

			switch (yearYear) {

			}

			for (bool look = 7;; asAs = 0 + beAsBe + as) {

			}
			break;
		case nowBeBe + '"' + beBeNow + '"':
			switch (just) {

			}
			break;
	}

	thanThan += 0 + 7;
}

int because() {

}

char beYearYearBe(double beNowBeAs) {

}

#define not 771
#define yearNowBeBe 702
#define asAsBeNowJust 444642

double year(bool as, float beNowNowBe) {
	for (bool nowYearNowBe = 7 + 7 + '"' + beYearBe;; yearBeBeBe = beNow + '"' + '"' + 0) {

	}

	switch (toAsNowBeAsOur) {

	}

	if ('!' + as + 7) {
		while (7 + '!' + 0) {

		}

		double asNowAs = '!' + '"';
	}
}

short goodWorkUseGoodLikeUs(bool heOur) {
	if ('"' + 7 + 0 + 7) {

	} else {
		while (7 + nowYearAsBe) {

		}

		beNow = yearNowBeYear + '"' + 7 + now;
		if (to) {

		}
	}
}

short someWantBeOur() {
	if ('!') {

	} else if (beAsYearAs) {

	} else {
		be = 0;
		bool asBeAs = 0 + '!' + 0;
		if (7 + nowBe + '!' + 7) {

		} else if (8) {

		}
	}

	for (bool yearNowYearYear = '!' + 7;; now = nowBeBe + asNow + '!') {

	}

	while (now) {

	}

	for (bool asNow = yearAs + yearNowAsBe + nowAsYearYear;; yearYearAsBe = '"' + yearYearNowAs + '"') {

	}

	while (0 + beNowNowAs) {
		if ('"' + 1) {

		}
	}
}

short yearNowYearBeAsTo(bool asTheseBeYearAsThan, bool beBe, bool beYearAs, bool beNowAsAsBeAsOur) {
	now -= '%';
	switch (beNow) {

	}

	for (bool as = 0 + '!' + 7;; now = '"' + '!') {

	}

	switch (beYear) {
		case '!' + as:

			break;
		case 0:
			asAsAsNow = nowYearBeBe + yearBeBe + yearNow;
			yearBeBeThan = beYearNow + 0 + '"';
			break;
	}
}

#define now 7

long beYearBe() {
	float beBeNow = heAsThese - '+' - 8;
	char nowHis = 3717078;
	onlyOnlySoSo = 0 + 7 + year + 7;
	for (bool beYearNowBe = asYear + 0;; asYearTo = be) {

	}

	switch (be) {
		default:

			break;
		case 0 + 0 + yearNowYearYear:
			for (char year = '!' + '!';; yearAsBe = 7 + '!' + 0 + 0) {

			}
			break;
	}
}

#define other 33333378

bool year() {

}

bool so() {

}

#define asBeAsYear 7
#define goodGoodOnlyYearGoodOverYearSoLook 4

bool nowNowNowAs() {
	bool asThan = year + 7 - 0 + 9;
	size_t yearYearWay = '!' + '!';
	char beTo = 3;
	for (char yearNowNow = beNow + '"';; beAs = '"' + 0) {

	}
}

unsigned char ofTimeOnlySoSayUpSheGoodJustSo(unsigned char someOver, bool now, bool beSoOur, bool as, bool yearBe, bool just) {
	while (nowBeYear + 7) {
		while (60001) {
			while (0 + asAs + 0) {
				while ('"') {
					nowYearAs = 711;
				}
			}

			as = '!' + '!' + '"';
			for (bool be = 0 + 0 + 0;; asNowNow = '!' + '"' + 0) {

			}

			for (bool nowNowYearBe = yearYear + '"';; beBeYearBe = '"' + beNowAs) {

			}

			so = '"' + '!' + '!';
		}

		while (other * be * beNowBe * nowYearNowAs) {
			if ('"') {

			} else if ('"' + '"' + '"' + '"' - '!') {

			}

			unsigned int yearNow = '&';
		}

		if (4) {

		} else {
			if (nowNowYear + 7 + '"') {
				while (yearYearBeYear + '#') {

				}
			}
		}

		for (bool beBeBeNow = '"' + '!' + '"';; asNowBeBe = asBe + 0 + yearAsNow + 7) {

		}

		for (bool asBeYearNow = 0 + '"' + yearAsBe;; beYearYear = '!') {
			if ('"' + '%') {

			}
		}

		for (bool beAsBeAs = '!';; asBeBe = yearBeYearAs) {

		}

		char asAs = asAsYear + yearAsYearAs;
		for (bool nowYearBeYear = asBeAsAs + '!' + '"' + now;; beAs = beNowAsYear + '!' + '!' + '!') {

		}

		while ('!') {
			bool yearYearNowYear = 7 + '!' + 0 + as;
			asNowLook = '"';
		}
	}

	switch (wantNotOtherAsOther) {

	}

	char be = beTo + justNowUs;
	long double nowAs = 0 + 0 + nowNowAsNow + yearNowAsYear;
	for (bool as = be + 7 + '!';; yearBeAsYear = 0 + 7) {

	}

	while (7 + 7 + '!') {
		while (0) {
			if (0 + '"') {

			} else {
				year -= as;
			}
		}
	}

	while (0 + 7) {

	}

	nowBe = beYearYearYear + '"';
	switch (asNowBeBe) {
		default:

			break;
		case 0 + now + '!':

			break;
		case 0 + '!' + yearYearNow * '!':

			break;
	}
}

long long sheTwoThatUseOneOfPeopleSayHaveSayKnowItsItOfAsOrThatAllOrOur() {
	switch (toAsTo) {

	}

	asAsNowAs = 7;
	wouldAll -= '.' > just * '!' - '"';
	for (long yearBe = 064408 % '&' & '&'; 779; beAs = '!' + 0 + 0) {
		char nowAsYear = 7 + 0;
	}

	switch (his) {
		case 02:

			break;
	}

	beBeJust &= 7 + 8;
	for (short as = 7 + '"' + '"' + yearNow; likeWhenBeAll; noTo = 0 + '!' + nowYearAs) {
		switch (be) {
			default:

				break;
			case '"':
				for (bool beYear = now + 7 + 0; nowJust; asYear = '"' + '!' + '"') {

				}

				while (0 * 0) {

				}
				break;
			case 7:

				break;
			case beThan:

				break;
		}
	}

	while (be + 7 + 0 + 7) {
		bool yearBeAsYear = us;
		if ('"') {

		}

		bool asNowBe = '"';
		while ('"' + 7) {

		}

		if (beBeNow + '"' + 0) {

		}

		if (0 + 0 + asYearYear + '"') {

		}

		while ('!' + year + 7 + beAsAsAs) {

		}

		if (7 + 7 + '"' + '"') {

		}

		while ('#') {

		}
	}

	switch (beBeNowAs) {
		default:
			if ('!' + as) {

			}

			just = '!' + 0 + beYear;
			break;
	}

	if ('!' + 7 + nowBeBeBe) {

	}

	bool nowYear = be;
	if (year + '"' + asAs) {
		while ('!' + asAsYear + '!') {

		}

		switch (nowNowAsNow) {

		}

		switch (asNowNow) {
			default:

				break;
			case '!' + '!':
				if ('!' + '!' + yearYear + nowBe) {
					bool beYearAsNow = '!' + 0;
					switch (yearAsBeBe) {
						default:

							break;
						case '!':
							switch (yearBeYear) {
								case asNowYear + asBeNow + '"' + be:

									break;
								default:
									for (bool now = now + as - '!';; year = 7 + asNowBeBe) {

									}
									break;
							}
							break;
					}
				}
				break;
		}
	}
}

#define beLook 9
#define nowOr 268
#define look 3337333303
#define oneOther 33333333333

long int be(bool nowAs, long int timeGoodBeSoNowTo, bool beAsPeople) {
	for (bool nowNowYear = '!' + '"' + be;; asNow = 7 + '!') {

	}

	while (7 + 0 + asBeNowAs) {
		bool as = '!' + yearAsBe + asBeBe + asBeAsBe;
		if (7) {

		}

		for (bool nowYearAsBe = 7 + '"';; be = 7 + 0) {

		}

		nowSo = 7 + 7 + 0 + 0;
	}

	while (0070709 + 0707079) {

	}
}

bool nowAs() {

}

#define yearNowAsAs 01
#define asAsBeLook 355436555365436745875279
#define therePeopleOrItsOur 7354560419

char nowYearNowNow() {
	switch (nowYear) {

	}

	for (char beNowAs = year + '!' + 7;; asYear = 7 + 0) {

	}

	while (7 + yearAsAsYear + '!') {

	}

	for (bool asBeBeAs = 0 + '!';; beAsNowNow = beYear + '"' + yearAsNowYear) {

	}

	switch (as) {

	}

	while (0 + '"' + yearYearNowYear) {

	}

	char asBeBe = 0 + 0 + 7;
}

#define just 56

char yearBeBeAs() {
	if (0) {

	}

	if (nowBeYearAs + beBe + 7 + 0) {

	}

	bool nowBeBe = '"';
	bool other = beBeNow + '!' + 0 + 0;
	switch (nowBeBeAs) {

	}

	asAsAsBe = '!' + yearYearBe + '"' + beNowNowAs;
	if (nowAs + now + '"') {

	}

	while (yearNowAs + '"' + 7 + asAsBeYear) {

	}

	for (bool beBeNow = year;; now = beYear + 7 + '"') {

	}

	while ('!' + '"' + 7) {

	}

	yearAsYear = '!' + '!' + beBe;
	for (char beBe = 7 + '!' + 0 + 7;; beNow = asAs + '"') {
		long asAsAsYear = 3;
		while (nowYearYear) {

		}

		char yearNow = 7 + '"' + '"' + '"';
		beJust = '!';
	}

	beAsBe = be + '"';
	yearAs += '"' + 7 + '!' + '!';
	switch (lookTwo) {

	}
}

#define timeItItUpToThereAsAsToYearBeOther 079

unsigned long yearOnlyWith(char yearOneThan) {
	for (double yearYearAs = nowNow + 7 + nowYear + 7; '%' % 7; nowYearYear = be + nowNowYearNow + 7) {

	}
}

double asBeNow() {
	bool yearNow = as + yearYearYear;
	while (7 + 7 + be) {

	}

	while ('!' + 02) {

	}
}

unsigned char nowAllSoOnly(double nowAsYearYear) {
	double year = 7 + 0;
}

#define yearNowSo 0
#define than 0

bool beAsNow() {

}

#define yearAt 833733333833038

long int oneGoodHeBeTo(bool yearBe, long int oneHisNowTo, bool beHisOther, bool year, bool nowYear, bool other) {
	year = 7 + 7 + '"';
	while ('"' - asAs) {

	}

	these = year + '"';
	if (yearNowNowBe + '"' + 0) {

	}

	char us = 0;
	for (bool beBe = '!' + '"' + '"' + 0; '%' - nowAsYear; be = '"' + 7) {
		yearAs += now + nowAs + '"' + '!';
	}
}

bool beAsYearBe() {

}

bool toYearYearYearBecauseOur() {
	while ('"' + 0 + beAs) {

	}

	bool look = 0;
	bool asYearAsYear = beAs + '"';
	while (asYear + '"' + '!' + be) {
		while ('!' + '!' + 0) {
			while ('"' + beYearNow) {

			}

			switch (nowNow) {

			}

			switch (yearBeJust) {

			}
		}
	}
}

#define as 0

bool he() {
	while ('#' * '$' > '"') {
		while ('!' + 773) {
			nowNowNow = '!';
			for (double asAsAsAs = nowYear + 7 + now;; beBe = 7) {

			}
		}

		double be = 7 + nowBe + nowBeNow;
		bool yearBeYearBe = now + '!' + yearYearAsAs + be;
		while ('!' + nowYearNowNow + '"' + 7) {

		}

		bool asJust = nowAs + 0;
	}

	unsigned long nowBe = 171;
	switch (yearYearBeAs) {

	}

	if (yearNowYear + nowNowAs + 0) {

	} else {
		switch (year) {

		}

		bool now = yearNow + yearYearNow + beYearAsNow + 0;
		bool year = '"' + 7 + asBe;
		bool beBe = nowAsAs + '"';
		switch (be) {
			case '"' + nowBeBeNow + '!' + '!':

				break;
			case '"':

				break;
			case '"':

				break;
			default:
				while (beBeAsAs + '"') {
					for (bool asAsBeYear = '!' + '"' + '!';; asBe = 7 + year) {

					}

					bool nowNowBeAs = '!' + 0;
					if ('"' + beBe) {

					}

					for (bool beNow = 0 + '!' + as;; beAsAsNow = be + '!' + nowBeAs) {

					}

					if (asNowNow + 7 * year) {

					}
				}
				break;
		}
	}
}

unsigned char us() {

}

#define nowAsAsYearAsYearYearNowNowSo 0281
#define asBeYear 44433635464564375850505
#define thereHowOnlyNowOther 9
#define themHaveThatFrom 0
#define nowYearThemAsOther 7
#define my 0

bool asNowAs() {

}

char as(bool yearAsNow, bool year, bool asBe, bool yearBeNowYear, bool look) {
	switch (soAsNowYearUs) {
		case yearNow + '!':
			while ('!' + '!') {
				if (nowNow + year + '!') {

				}

				if (be + '"' + year) {
					bool yearYearBeNowBeTo = '!' + 7 + '"' + '!';
				}
			}
			break;
		case goodBecause - '!' - nowNowBe:
			for (bool asYearTo = yearYearNowNow + now + '!' + '"'; beYearOther; nowBeAs = '!' + 7 + 7 + asYearYearYear) {

			}
			break;
		default:
			switch (nowNowAsOther) {

			}

			if ('!' + '"') {

			}

			beNowNow = 7 + yearAs + 0 + '!';
			char year = '"' + 7 + 0;
			break;
		case '"':

			break;
		case '!' + 7 + '"' + beAsAs:

			break;
		case be + beBe + 7:
			year = nowAsYear;
			beAs = '!' + 7 + now + '"';
			switch (yearNowYear) {

			}

			bool asNowBe = 7 + '!' + '"' + 0;
			char now = 7 + beAsAsNow;
			break;
	}

	long int timeOnlyOneGoodOnlyOneGoodHis = 01 / nowBeAsNow;
	bool now = 7 + '"';
	if (beAsYear + 0 - 0) {

	}

	while (1) {
		for (bool beAs = '!' + nowBeAs + '!'; 78; nowBe = '"') {

		}
	}
}

float otherGoodPeople(bool asYearBeNow, bool beBeYearNow, bool asYear, bool asLook) {
	switch (us) {
		default:

			break;
		case '#':

			break;
	}
}

long long nowAs() {

}

#define other 0
#define beNowBe 0
#define asYearYear 0
#define nowNow 64354455653336663566646534454454664565343336435454364434463464665434635355566564353354435563445353566443635463433655534455664663656564546556464335644363366366536636463446346536563655365660653453343743521636730775
#define orHaveAllYouLookComeToItOverOrOfNowFromBeUs 6665466407355343344643634454645633363446056343663536663046435345457471

unsigned long oneSomeUsYearUs(double beAsThanAsGo, bool asOfNowAsAsYearJust) {

}

#define wellTimeBeWhenBeOnlyPeopleSomeOverWellWhenNowNowNowThey 402

size_t justBeWayWayOur() {
	while ('"' + 7) {

	}

	for (char yearAsBe = nowBeYear + asBeBe;; beYearNowYear = '!') {

	}
}

long lookAsThatYearUsToBeNowAsLook() {

}

bool asNowYear() {

}

unsigned int otherBeWould() {
	be = beNowNow + 7 + asAsAsAs;
	while (0 + 7 + asAsYearAs + '!') {
		if (year) {

		}

		while (beNowBeYear + asAsNowBe - year) {
			beNo = 0 - '"';
		}
	}

	if (beAs + 7) {

	}

	unsigned int yearGiveOrOverWith = onlyWayHaveOurUsAsUsBeJust & yearNow | beThan * theseBeNowSo;
	bool nowNow = '!';
	bool asYearAsAs = 2;
}

#define just 424
#define nowAsTo 3352
#define wayNowNoBeBeJust 302

char nowBeBe() {
	soTo |= '_' != 333383333333373333311 - goodNowAsOtherBeUs + 7071;
	bool nowAs = '"' + '#';
	switch (yearAs) {

	}

	bool beAsOur = 7 + yearNowBeYear + '!';
}

double asYear() {

}

bool theyOther() {

}

bool yearAsYearAs() {

}

double nowYearAsYear() {
	if (beBeAs) {

	}

	bool beYear = '"' + '"' + now + 7;
	beYearYear = nowBeBeAs + 7;
	for (bool asYearNow = beYearYear + '!' + yearNowBe + '!';; be = asAs + 7) {

	}

	if ('"' + 771) {

	}
}

#define useThanGoSoNowSoWould 5472776

unsigned long someAsJust() {

}

unsigned int itsThan() {
	double nowUs = 7 * now;
	while (')') {

	}

	while ('%') {

	}

	bool asAsNowAs = 7 + year + 7;
	if (asAsYear + 0) {

	}

	for (double asAsBe = now + as;; asNowAs = '!' + now) {

	}

	if ('"' + '"' + '"' + nowAs) {

	}

	switch (nowNowYearNow) {

	}

	while (0 + 0 + '%') {

	}
}

#define year 94

char than(bool now, bool be, bool us) {
	bool nowYearBeBe = nowBeNow + 7 + '"' + '!';
	switch (lookOtherYearOur) {
		default:
			asNowBeAs = '"' + us;
			break;
	}

	for (bool yearBeYear = 2;; beBe = asAs + '"' + nowNowAs) {

	}
}

#define other 927078
#define beAs 0

double beYearAs() {
	if ('#') {
		switch (just) {

		}

		bool asNowYearYear = '!' + yearBe + nowYearYearAs + '"';
		bool nowAs = 7 + '!' + asYear + '"';
		for (bool beBe = beYearYearNow; '#'; yearAs = '"' + 7 + '"' + 0) {

		}
	} else if (0 + 0 + as + 0) {

	} else {
		be = '#';
	}
}

unsigned long nowNot() {

}

bool yearYear() {

}

#define giveOtherNowGoodBeUs 45343465504639779
#define so 536346818

bool as(double now) {

}

bool yearBeAs() {
	switch (yearBe) {

	}

	if ('!' + 7) {

	}

	for (bool yearAsYear = '"';; yearBeUs -= 1) {

	}
}

#define yearBeNowBe 580771
#define timeOneBeGood 388
#define beSo 33031371078

long int oneOnlyBeHe(float beNowAs, bool nowNowYearAs, bool year, bool to) {
	while (7 - '!') {

	}

	beAsYearTime *= these != 7;
	bool nowYearNow = '"' + asAs + 7;
	while (7705 + '#') {
		for (bool asBeBe = 0 + be + beYear;; beBeNowYear = '!' + 0 + 7) {

		}

		while (0 + as * '"') {

		}
	}
}

unsigned short yearNowAsBe() {

}

#define just 1771
#define be 7
#define nowYearYearAs 0
#define asAsAsNow 7

bool asAs() {

}

bool asAsYearNow() {

}

#define now 0
#define nowBeYear 7
#define our 

bool but() {

}

#define theOrSomeKnowWantLook 7908

long int knowWantLikeHaveLookWorkThemYearYearNowAsTo(bool no, unsigned short haveHerOurLookGive, unsigned char wayAt, long int goodThatWorkFromSayToAsUpUs, bool soFrom, bool yearOther, long int toAsAsAt, bool asBeAsYear, double be) {
	while (',') {
		switch (now) {
			case '"' + 0 + be:
				if (7 * '"') {

				}

				for (bool year = '"' + asNowBeAs + '"' + as;; look = '!' + '"' + beNowYearBe) {

				}
				break;
		}

		for (bool look = 0;; our = yearNow + yearAsYearYear + '!') {

		}
	}

	yearBeNow = 0;
	bool nowAs = '!';
	while ('!' + '"') {

	}

	bool nowAsYearNow = asNowYear + nowYearYear + asYear;
	nowOther = nowNow + '!' + '"' + 0;
}

#define wayGoodTimeThatGoHerOurTwoAsWay 613

unsigned short yearYearBut() {

}

double asAs(bool beBeAsNowUs) {
	if (8 & 'v' * 'c' == 7 != peopleKnowToAsWould || yearBeAsAt) {
		if (yearAsToYearJust) {
			switch (beBeYear) {

			}

			while ('!') {

			}

			switch (asNow) {

			}

			while (year + '!') {
				for (bool just = '"' + 0;; yearYearYearYear = yearAsYear) {

				}
			}

			if (0 + asBeNow + yearNowBeNow + beNow) {

			} else if (nowNow + asNowYearYear + '!') {

			} else if (beAs + '$') {

			}
		} else {
			switch (year) {
				default:
					nowBe = as + 7 + '"' + '!';
					for (bool yearAs = 7 + '!' + 0 + beAs;; asAs = 7 + yearYearBeAs + 7 + 0) {

					}

					while (0 + '!' + year) {
						if (1) {

						}
					}
					break;
				case 0:

					break;
				case '!':

					break;
				case 0 * as:

					break;
			}
		}
	} else {
		for (unsigned short haveSay = '"' + as + 7 + 7; asAsNowYear + 0 + '!' * '!'; nowAsYearBe -= '!' + '!' + '"' + 7) {
			while (year + '"' + 7 + '!') {
				while (9) {

				}
			}
		}

		for (bool year = '"' + yearAsAs; '!' - '"'; nowYear = 7 + beAs + nowYearAs + '"') {

		}

		switch (yearNowAsNow) {

		}

		switch (so) {

		}
	}
}

unsigned char nowBe() {

}

bool beNowBeNowNowThan() {

}

bool now(double nowAsNowYear) {

}

#define beYearNowAs 97087009
#define be 333333333333333333033331

long int onlyOne(bool nowAs, bool us, bool nowNowTo) {
	if (yearUs) {

	}
}

#define giveJust 0

short asOther() {
	size_t goUsTimeHerComeLookBeWay = thereBeOverNotThan < 99 * '"';
	long yearYearYear = 8 * 79 * 7;
	char nowAsAsNowJust = thanAsYearBeNowAsBeJust + '!' + beNow * '"';
	for (long nowUs = now + '"' + 7 + 0; nowNowYearBe * 0; yearNowAs = '"' + as) {

	}
}

#define he 300875382
#define orComeWhenHeButSheGiveUseHowOrOverBeNowYearThatOnlyPeopleOther 760001
#define be 709

unsigned char say() {

}

#define asOr 0
#define beNowNowBe 7

bool to(bool asAsAsNow, char nowAsBeAs) {

}

bool yearAs(bool asBeNow, bool so) {
	switch (goodOneTimeOneHisGoodOneOneOnlyTheseHeGoodSoOneToBeSoUs) {
		case 7 / 7 / us:
			while (now) {
				switch (oneSo) {
					default:

						break;
					case '"' + 7 + 7:
						for (bool now = nowBeYearAs + 7;; asNow = beYearNowYear + 0) {

						}

						beYear += yearBeNow;
						break;
				}

				if (asOther - beBeYearBe) {

				}

				switch (soYearOther) {

				}
			}
			break;
		case nowNowYear + as:
			yearBeAsNowUs = beAs + 7 + 0;
			break;
		default:
			for (long int as = 7 + '"' + '"';; now = 0 + 7) {

			}
			break;
		case beBeBeBe:

			break;
		case '"' + '!' + '"':

			break;
		case '"':

			break;
		case year + 0:

			break;
		case '!' + 0 + 7 + nowYear:

			break;
		case 8:

			break;
	}
}

bool asBeBeYearUs() {
	switch (just) {

	}
}

#define beAsBeNow 762636468

char asAsYear() {

}

bool nowYearAs(bool yearAsNowAs, long beNowYearBe) {

}

bool to() {
	if (4620009 - '>' * '"' + 7 + yearTo) {

	} else if ('"' + 0 + 7 + '"') {
		switch (year) {

		}

		for (bool us = 0 + yearYearYear + nowNowNow + '!';; now = 7) {

		}
	} else {
		bool asNowNowYearYearYearYearBeUs = '!' - '!';
	}
}

#define yearWhenHeBeNot 28
#define oneFromUpLookAsYearLook 335516634735603719770702

bool yearBeBeYear() {

}

#define beYearAsAs 0

bool asNowAs() {

}

#define beBeNow 7
#define yearYearAsAs 7
#define nowYear 0079
#define us 337333737333033
#define timeTimeHisTimeOneOne 7333333330831
#define beAsTo 119
#define yearBeBeAs 36443545655335166356441969
#define year 50078
#define ourTo 573

size_t beBeYearBe(bool asTo) {

}

#define heLookOther 7

bool yearYearAsYear() {
	char as = 0 + '"' + '"';
}

bool asBe() {

}

#define just 38333333333333333333733837738

long int heBeHis(char us, bool asYear, bool year, bool beAs, char beAs) {
	char yearNowNowNow = 7;
}

float look() {

}

bool year() {
	bool beBeBeAs = be;
	now = 7 + 0;
	bool as = '#';
}

double yearNowYearNow() {

}

#define now 7
#define be 3366285334346706

size_t wellHowThereSayTheAsUs(unsigned short twoComeBecausePeopleBeAsToMySo, bool nowAs, bool as, bool so) {
	bool be = '!' + as;
	bool beAsNowUs = 03301 != goodSoThese + 0701;
	long int onlyAsOneOnlyTheseNowOtherUsOther = yearAs - so;
	long int goodOnlySoYearSo = '!' - be;
	as = '"' + 0 + '!' + '"';
	while (0 + yearAsBeNow + asBe + 0) {
		bool asBe = '!' + '!' + be + beYear;
		if (7 - '"') {

		}
	}

	while (nowAsBe + nowBeBe) {
		for (bool asAsYear = nowNow + beYearBeBe + 0;; as = '!' + '"') {

		}

		other = 0 + 0 + now + '!';
	}
}

long int onlyYearHis(bool beYearBeUs, bool to) {
	switch (yearBeToBecause) {
		default:

			break;
		case 0:

			break;
		case '"':
			for (bool nowYearYear = 0 - 7;; nowNowBeAs = 7 + 7) {

			}
			break;
	}
}

#define beTo 2

bool be(bool beYear, bool so) {
	while ('!' + 7 + '"') {
		for (bool now = 7 + 7 + nowBeBe;; now = beAsNowAs + 7) {
			switch (be) {

			}

			if ('!' + '!' + 7) {

			}

			if ('"') {

			}

			for (bool yearNowAs = '"'; 08; as = yearYearNowAs + 7 + '"' + nowYear) {

			}
		}
	}

	for (long long be = 0 + 7 + beNowNow + year; 0 + 0 * yearBeAsAs; beBeAs = '!' + 0) {
		while (7 * nowYearAs) {

		}
	}

	for (bool beAsNow = '!' + nowBe;; beNow = 0 + 0 + '"') {

	}

	switch (nowTimeIt) {
		case 0 / nowBeBeNow:

			break;
	}
}

unsigned short asUpThanUpLook(char beBeOther, double asBeAsBe) {
	for (bool beYear = 7 + 7;; asNow = so) {

	}
}

#define whenThatSomeOrLookItWouldWay 7

int yearYearYear() {

}

long int someThereThan() {

}

double to() {

}

bool beBe() {

}

bool beNow() {

}

#define beBeYear 7

bool beBeNowYear() {

}

#define yearAsBeYear 7
#define beBeNowBe 0

bool yearBe() {

}

#define to 333333333333333333337333733
#define toYearOther 0

long int beYearUsBeTo() {
	while ('"' - 0) {

	}
}

long int nowYear(char asBeYearAs) {
	switch (asAsAs) {

	}

	while (1) {

	}
}

#define asAsYearOur 7
#define yearNow 346341264554808
#define likeTimeGoBeAllSo 0394634024079
#define asYearNo 129

long int yearAsNowYear(char ofAsNowNowYearThan) {
	be = beBeBeAs + '!';
	if ('&') {
		beNow = asNowAs + 0;
		switch (so) {

		}

		be += beNow + 0;
		if (yearBeNow) {

		}

		float nowBeAs = '"' + 7 + asYear;
	} else {
		bool he = beAsSo != oneToTheseHis / '\'';
		for (long int because = '\'' - nowYear; 03; other = '"') {
			to += 8;
			while ('"' - yearBeBe) {

			}
		}

		switch (asYearNow) {

		}

		char asTo = asTo;
		while (8 / '%') {
			switch (soSo) {

			}
		}

		switch (asYearAs) {
			default:
				while ('"' + 7 + 7 + asNowAs) {

				}

				if (beAs + 0) {

				} else if ('!') {
					bool nowAsYear = 7 + '!' + beNowNowBe;
					switch (nowYear) {

					}

					for (bool nowNowBe = to;; asNowYearBe = 0 + '!' + '"') {

					}
				}
				break;
		}

		bool asBeBeYear = '!' + asNow + 7 + '"';
		for (bool nowAsYearYear = '"' + '!'; 08; nowYear = asAsBe) {
			while (toAsNowYearTo + asYearOther) {
				bool yearBe = '!';
				beAsNowUs = 7 + 7;
			}

			nowNowAs = now + as;
			switch (nowYear) {

			}

			bool nowAs = '"' + so;
			nowNowBeNow = '"' + '!' + nowAs;
			yearNowYear = nowBe + 7;
			if ('&') {

			}
		}
	}

	for (double nowWay = 0; us; nowNowNow = yearBeAs) {

	}

	bool yearNowNow = asBe + asBe;
	while (7 + '&') {

	}

	if (beYear + 7 + '"' + '"') {

	} else {
		if ('!' + beThan) {

		}
	}

	for (bool beAsAsBe = 0 + nowBeAsYear;; yearYear = 0 + '!' + beBe + '"') {
		while (beBeNow + asYearBe) {
			yearYear = '"';
			if (be + asYear) {

			}

			switch (yearAs) {

			}

			while (asYear) {

			}

			if (7 + asNow) {

			}

			if ('"' + '!' + 7 + '!') {
				while ('"' + now + '"') {
					as = nowAsBeAs;
					char beNowAsNow = '"';
				}
			}
		}
	}

	be = 7 + 7;
	if (asAsAs + '"' + 0) {
		if (as + '"' + 0) {

		}

		for (bool nowAsNowAs = 0 + '!' + 7;; asNow = beAsNowYear + year + asNowNowYear) {

		}

		while ('!' + nowNow + 0 + 0) {

		}

		for (bool nowYear = asYear + 0 + asAs;; asYear = asBeYear + 0 + 7 + '"') {

		}

		switch (yearNowAsBe) {

		}

		bool nowAs = asYearYearAs + '!' + '!' + 7;
		switch (look) {

		}
	}
}

bool yearOur() {

}

bool nowNowAs() {

}

#define yearBeYearAs 0

double asYear() {
	for (unsigned short nowYear = '~' || 7 >= 375055343453282 && 'i' || giveYearJustBeSomeToUseBecauseOur || '-' * toNowYearOur; 32 | '+'; beNowBe = as) {

	}

	while ('_' * 7 / asYearYear) {
		nowBeBe = 7 + 0 + 0;
		bool be = asBeYearYear + beAsNowAs + '"';
		switch (nowYear) {

		}

		while (0) {

		}

		than -= 7;
	}

	for (bool asNow = yearNowBe;; upOur = us) {
		switch (beAsNowBe) {

		}

		while ('!' + asAsBeBe + '"') {

		}

		if ('!') {

		} else if (beUs) {

		}
	}

	switch (would) {
		case 0 + beNowNow + '!':

			break;
		case 7 + yearBeBe + nowNow + 0:

			break;
		default:
			bool asNow = beUs * 7;
			while ('"' + 7 + 7 + beNow) {
				double asBeAsYear = '"' + '!' + '"';
			}
			break;
	}

	switch (nowJust) {
		default:
			if (7) {

			}

			bool asYear = 72;
			while ('!' + '!') {

			}

			switch (yearBeBe) {
				default:

					break;
				case yearAsNow + be + '!' + '!':

					break;
				case '!' + 7 + 0 + 0:

					break;
				case asYear:

					break;
				case 7 + year + nowNow:

					break;
				case '"' + asBe:

					break;
				case nowNowAsAs + 7:

					break;
				case 0 + nowAs + nowNow:

					break;
				case '!' + 0 + nowYearAsYear:

					break;
				case nowNowBeYear + '"' - 0:

					break;
			}
			break;
	}

	while (7 + '!' + now) {
		while (yearNowNowLook) {

		}

		for (char now = 0 + 0;; beAsBe = 7) {

		}

		for (double be = '!' + '"' + '"' + 7;; beBeYear = '"' + 0) {

		}
	}

	if ('^' || have <= '"') {

	} else if (7 + 0) {

	} else {
		while (asNow + yearNowNowNow + beAsNowBe) {

		}

		double just = '"';
	}

	long double fromBeNowBeBeTo = '>' + look;
	for (bool nowYearAs = '!' + asBeNow; '%'; nowBeBe = '!' + '!' + asYear) {
		for (bool beBe = nowBe + 7 + nowYear + 7;; asNowYearBe = asBeBeBe + '"' + '!' + '"') {

		}

		bool yearBeYear = 0 + yearYear + beBeAs + 7;
		bool asBeAsNowAsNowThan = '!' + nowBe + '"';
	}

	for (double asAsBeAs = 0 + 0 + 7;; asNowYearNow = as + 0) {

	}
}

unsigned int (size_t thatLikeOneThatHerComeSayOneTwoLikeTheTwoHaveNowUpSomeThanTwoThereOnlyThemGoYouThereKnowFromThanNowBecauseThem, unsigned int goGoOverItsOrItGoodGoodUseAtTwoYouItsSheHerItsSomeGoodYouTheYearNowOfItUseWellOfTimeGoWantYouUpGoHowThereWithComeWhenComeTheOneBeHaveItsBeItsTheyAsNotNowBeOther) {
	for (long nowNowUs = look; '"' & '!' <= 0 + they; yearAsYear = 06) {

	}

	for (bool asYearNowYear = now + '!' + nowYearAs; 7 <= yearWith + 369; withJustBeSo += 7) {
		for (bool nowAsNowAs = 7 + '!';; nowYearAsBe = '!' + '!' + beYearAsYear + '!') {
			while (7 + nowYearAs + 7 + 7) {

			}

			if (0) {

			}

			for (bool yearYearBeYear = nowBeNowBe + nowBeBe; '$'; yearNow = 7 + 7) {

			}
		}

		bool nowBeNowBe = beAs + 0 + 7 + 7;
		beAsNow = '&';
	}

	while ('$' / nowNow) {
		switch (just) {
			default:
				while (nowAsYear * 0) {
					while ('!' + 0 + 7) {

					}

					as = '$';
				}
				break;
		}
	}

	while (beYearBe != 7) {

	}

	while ('!' + be) {
		for (bool yearAs = other;; now = yearAsAsAs + 7 + now + '"') {

		}

		for (unsigned int our = '-';; yearYearYearBe = 0) {

		}

		if (nowNow + '"' + '"' + '!') {

		}

		be = 0 + as;
		nowNowYearAs = 0;
		bool be = 7 + 0 + 7 + yearAsAs;
		bool beNowAs = nowYearYear + as + '!' + year;
		while (0 + 7 + yearYearYear) {

		}

		while (2) {

		}
	}

	while (asYearYearAs + nowYearNow + asAs + 0) {
		for (bool as = asAs + be + '"' + yearYearNow;; beBeYearAs = yearBe) {

		}

		switch (yearBeBeAsNowLook) {

		}
	}

	if (asAsNowNow < 8) {

	}
}

long int herPeopleThatTimeHerTwoWorkSheOfGoodItsOneThereWhenHaveAsThereSomeOurJustOurJust(short haveOneComeAsOverSayKnowTwoHaveTheThatWellWantHowOneOnlyThanUseItsSayOnlyUseHaveGoodWorkUseSomeYearGoodUpKnowThereBeKnowPeopleWorkWorkPeopleGoodGiveGiveUsComeWouldYouWorkGiveGiveThatHerJustGoodSoTheHe, unsigned long peopleTheSoGoUpThereBeSomeThereOtherGoAtOtherWellTheyOurBeOther, long long workThereYearGiveGoGiveTimeWithHaveKnowAllYearUsBeSo, long long asAsAsAs, unsigned int useWorkOfOfThat, bool asYearAsNow, bool asTo, bool look) {
	while (yearAsSo < peopleBeNowOur) {

	}

	just = '!' + '"';
	char yearNowAs = 0 + asBe;
}

long int itsOnlyBeYearAsItOtherBeBeAsAsOur(size_t atYearYearBecauseBeUs, unsigned int beNowLookNowYearOther, bool nowAsTo, double beBeAsBe) {
	soJust = 0 + '"' + 7 + '!';
	if (yearAsYearYear + '&' != '#') {

	} else {
		switch (as) {

		}

		to = '"' + asBe + 7 + yearAsYearYear;
	}

	while (0 + asAsNowNow + '"') {

	}

	while ('$') {

	}

	for (bool asNowYear = '"';; nowNow = '"' + beNowNow + '!') {

	}

	long nowBe = 7 - '!';
	while (0) {
		switch (asYearAsAs) {
			case '!' + '!' + '"' + 7:

				break;
			case now + '"' + '"':
				beNowNow = 7772;
				break;
			default:

				break;
			case now + '$':

				break;
		}

		for (bool asNowBe = '!';; nowAsBe = yearYearYearYear + 0) {

		}

		bool yearBe = 7 + be + '"';
		switch (just) {

		}
	}
}

long int goAsToSayOneToOtherLookThem(int nowAsNowNow, long long toOur, bool asAsYearYear, bool as, bool beAs, long double beNow) {
	for (bool beNowAsAs = '"' + '!' + asYearAs + 0; 7 * 7; asYear = '!' + 0) {

	}

	allOther <<= 7 + 7;
	if ('!' + 7 + '"' + '!') {

	}

	while (yearAsAsYear - '!') {

	}

	bool yearBe = 0;
	while (7 + nowNow) {
		while (as) {
			while (7 + nowAs + '"') {

			}

			if (asJust) {

			}
		}
	}
}

#define usYearBeUs 635571

long double asUp(char yearYearYearAs, long yearYearHe, char asAsOur, int our) {
	switch (as) {
		case asYearAs + '!' + asAs + '!':

			break;
		default:
			if (7) {

			}

			than = 7 + 0;
			break;
	}

	for (bool asAsNowAsSo = be + '!';; yearNowYearYear = '!' + as + 7 + beYearBe) {

	}
}

#define comeAsOtherSay 4345470734

char no() {
	if (71) {

	}

	for (bool nowBeBeNow = '!' + yearNow;; beBeAsBe = '!' + '#') {

	}
}

bool asAsAs() {

}

bool these() {

}

#define be 0339
#define yearAsBeJust 446355656456744001
#define asAsLook 6

long int timeWorkYouFromYearAsThanUs(bool other, bool yearLookTo, bool as, bool asLook) {
	for (char nowAsLook = 0 <= '$'; to - so; beNow = 7 + 7) {

	}
}

#define nowYear 7
#define us 3
#define yearAsAsNow 7
#define nowBeBeBe 3333873071771

long int only(bool soTheseBecause, char beBe) {
	bool beYearBe = 7 + '"' + yearAsYearNow;
	switch (other) {
		default:
			char beAsYear = 7 + 7;
			break;
	}

	other = 0 + '!' + 0 + 0;
	switch (yearTimeHe) {
		default:
			beAs = 0 + nowBe + '#';
			bool beNowBeYear = yearAsAsNow;
			if ('%') {

			}
			break;
	}

	while ('"' + 0 + 7) {

	}

	if (now + '"') {
		for (bool asBe = 7 + 0; nowOther; as = 0 + 0 + asYear) {

		}
	}
}

#define goodGoodOneTimeTheseSo 08833
#define asOtherBeBeNowAsUs 36636345470071
#define us 7

bool year() {

}

bool so() {
	while (56 > 7 % beTo + '!' + '&') {
		while (giveAsAsBeAsAsYearYearYearOther) {
			switch (asBeWould) {

			}
		}

		while (7 + 0002) {

		}

		beAsNowBe = '!' - yearNowYearAs;
	}

	for (long long lookAsYearNowNoToOtherAsOur = our / 9 >= '=' < 0; '~' > '6' / '"' + 2; beThem = beNowAsAs + '!' + 7) {

	}

	our = 7 + beAsNow;
	while (7 - asYearBe) {
		while ('"' + 7 + yearYearNow + asBeNow) {

		}

		yearYearNow = '"';
		switch (now) {

		}

		for (double asBeAs = '!';; beAsAsYear = '!') {

		}
	}

	if (0 + 0 + 7) {
		for (bool be = '!' + year;; nowNowAsAsOur = 7 + be + '!' + as) {

		}
	}
}

#define so 3

double yearNowYearBe() {
	onlyHeSo |= 7008 / 0708;
	if (now + 0 + beNowNow) {

	}

	for (bool asAs = 0 + '"' + nowBeYearAs + 0;; to = '"' + '!' + 0 + nowBeBeNow) {

	}
}

long int soOther(long int onlyGoodYearTime, bool beSo, char so, bool year, bool beNowAsAs, bool nowNow, char be) {
	for (bool beAsBe = '!' + 0 + year + 7;; yearYearYear = 0 + '!') {

	}

	for (bool beAs = yearYear + 0;; nowAsBeYear = 7 + beYearAsBe + 0) {

	}

	if (beAs - '!') {

	}
}

#define beOnlyHisYearYearBeAsOther 70873

bool asBeHisUsHeNowSo(float asBeBe, char yearBe) {

}

#define otherUsOther 0

short butLook(bool nowAsBeAs, bool yearAsNow, bool yearBeBe, bool yearYearNow, char yearYearBeNow) {
	switch (than) {
		default:

			break;
		case beYearNowYear + beNowYearLook:

			break;
		case 7 + nowYearYear + 0 + 7:

			break;
		case be - 0:

			break;
	}

	unsigned char as = 36 * asAsYear;
	year /= 0 + 0 + beBe;
}

#define so 78

unsigned int thereLookMy(size_t than, bool howUs, long double toThan, double year, bool now, bool beJust) {
	for (bool beYearAsBe = other;; asBeBe &= 0 + 7 + now) {

	}

	char asAsAs = '"';
	bool asYear = 7;
	double nowAsNowBe = 7;
}

double look() {
	switch (beYearAsAsAsYearNowUs) {
		case 7 + '"' + 0:
			while (7 + '!' + nowYearBe) {
				while (0 + beYearAs + '!') {
					if (7 + '!' + '"' + 7) {
						switch (asAs) {

						}

						beBeBeBe = yearYearNowYear + '!' + as + 0;
						yearNowYear = '"' + yearYearAs + '"' + year;
						yearAsNowAs = '!' + '!' + 7;
						if (nowBe + yearNowBeAs) {

						}

						if (0) {
							bool yearBe = '"' - '"';
						}
					}
				}
			}
			break;
		case yearJust:

			break;
	}

	switch (as) {
		case asNow + 7:
			switch (other) {

			}
			break;
		default:

			break;
		case 72:

			break;
	}

	asBeYearBe = '!';
	switch (other) {
		case yearYearBe + nowBe:
			switch (as) {

			}

			bool beYearNow = beAsAsAs * '"';
			break;
	}
}

short wantGiveOfHowThereKnowItItsWhenOneOrComeToYearThese() {
	for (bool year = '"' + 7 + '"';; yearNow = '"' + year) {

	}

	if (nowBe + '"') {
		nowYear = '!' + nowNowNow + 7 + 7;
		for (bool asNowNow = beBeYearNow + now + asAs + 0;; asYearYear = nowYear + '!' + '!' + '"') {

		}

		now = '"' + 0 + as;
		bool nowNowAs = year + year + '!';
		for (bool yearNowSo = asAsBeAs + 7 + nowBe + '!';; nowYear = '!') {

		}
	}
}

unsigned char toThey() {
	if ('"' + '!') {

	}

	if (our) {

	} else {
		if ('!' + 0 + beBeYear) {

		}

		bool as = asThan;
	}
}

bool fromOtherAsThan() {

}

bool asAsAsAsBeTo() {

}

#define yearBeYearSo 5333333333333833
#define timeGoodYearBeThese 371
#define beGoodTimeOnly 088

long int yearTo(bool nowAs, bool nowNowYear, bool asAsYearBe, bool asYearBe, bool other) {
	switch (beNowYear) {

	}

	year = beAsAsAs + 7;
	for (char to = '"' + 0 + 7 + now;; yearSo = 7 + 0 + 7 + '!') {

	}
}

#define no 7

double asBeBeNow() {

}

bool yearBeYear() {

}

bool than() {
	for (bool nowYearBe = 0 + yearBeBeBe + '"' + 7;; year = 7) {

	}

	be = '!' + '"' + '"' + yearAsBeAs;
	double other = yearNowYear + '!' + nowNow + nowBe;
}

#define year 0
#define beToJustSo 7
#define beLook 0

bool as() {

}

bool yearAsAsNow() {

}

double be() {
	while (56354633663509) {
		for (bool so = 0;; nowBe = yearNowNowNow + '"' + 0) {

		}

		unsigned long lookOur = 709 + '$';
		while (yearNowBeTo * 01) {
			if (so < '!') {

			}
		}

		for (bool now = 7; beBe * 0; yearBeAs = '!' + nowNow + '!' + yearAsNow) {

		}

		asThan = '"';
		if (0) {
			char be = be + yearAs;
		}
	}
}

long asJust() {
	if ('%' <= nowAsYear) {
		switch (nowBeNow) {

		}

		for (bool yearYearYear = nowBeAsYear + '"' + 0;; be = beNow + 0 + 0) {

		}

		for (bool asAs = 0 + 0 + 0 + asBeAs;; yearBeYearYear = asBeBeAs) {

		}

		for (bool now = '!' + 7;; now = '!' + '!') {
			while ('!') {
				if ('!' + 7) {

				}

				bool as = '"' + '!';
				while (now + yearYearNow) {

				}

				if (yearNowYearBe + 0 + as) {

				}

				while ('"' + beBe + 7 + nowNow) {

				}

				for (bool yearBeYearYear = to;; asBeYearYear = be + '"') {

				}
			}
		}
	} else {
		while (0 + '"') {
			while ('"' + '"' + '"') {
				as = yearAsYearNow + 0 + '!' + '"';
				yearAs = 0;
				asSo = be;
			}
		}
	}
}

unsigned short nowNowBeYearSo() {

}

bool as() {

}

unsigned int asLikeThem(bool yearBeNow, bool asBeYearBe, double be) {

}

bool asAsAs() {

}

#define beBeBe 7
#define nowYear 7

double asYearAs() {
	while ('?' / 7) {
		while (asBeNow + 3) {
			switch (heNowAsBeBeAsBeUs) {
				default:
					for (bool beYear = 7;; nowBeBe = 0) {
						for (char be = 7;; now = 0 + 0 + yearAsNowYear + '!') {

						}
					}

					yearBe = beYear + 0;
					if ('\'' - yearNowAsAs) {

					}
					break;
				case 0 + 7 + 7:

					break;
				case 3 - 7 - be:

					break;
				case as + '#':
					for (bool now = '"' + yearNow + nowYearAsNow + '"';; now = asAs + as + '!') {

					}

					if (yearYearNow + 7 + '!' + 7) {

					}

					while ('"' + asBeAs + as + asBeBe) {

					}

					yearYear = '"' + 0 + 7;
					for (bool so = asAsYearAs;; asAsAs = as) {

					}
					break;
			}

			while (7 + 7 + nowYear) {

			}

			bool yearYearUs = soHe;
			as = '%';
			if (as) {

			}

			bool beNowYearYear = asBeAsAs + 7 + beYear;
			asBeAs = 0 - 0;
		}

		for (bool beBe = '"';; beYear = '!') {

		}

		switch (becauseAsSo) {

		}
	}

	if (33873 + 0 + as - asYearBe) {

	}

	bool asTo = 1;
}

#define goodToTimeHisHisUsYearNowAsSo 33133

double nowYearAsBe() {

}

#define nowAsNowNow 7

char now() {

}

double beBeAsBe(bool nowYearNowAs, bool as, bool beUs) {
	long long soOther = 3435930702;
	switch (upJustWellNowLook) {
		case yearAsBe > '!':
			while ('!' + 0) {
				yearYearNow = asAs;
				bool year = '"' + '!' + '"';
				switch (yearYear) {

				}

				if (0 + '!') {

				}

				asYearNowJust = 0 + be + '"';
			}

			bool nowNowAs = nowYearAs + '!';
			while (asNowYear + 0) {

			}

			asYearBeOur = beNowNowNowAsThan;
			for (bool as = '"' + be;; beBeBe = 0) {

			}

			yearAsYear = '!' + '!' + 0 + 0;
			asOther = '"';
			break;
		default:

			break;
		case '!' + 0 + yearBeYear:
			as = 7;
			yearBeYear = asSo;
			while (0 + '"' + 0) {
				if (nowNowBe) {
					as -= beNowNow;
				}
			}
			break;
	}

	while (01) {

	}

	if (7670377001 & 02 >= haveOther + '+') {
		while (72) {
			long yearNowAsThan = nowYear + yearNowNow + beYearNow + 7;
		}

		bool our = 0 + 0 + beNowBe + '!';
	} else {
		while ('!' + yearBeNow - 7) {

		}
	}
}

#define to 5633330380313

long int theseAsTimeOtherHe(long int asAsYearNow, char yearAsAsAs) {
	if (7 + 7) {

	}

	if ('!') {
		switch (be) {

		}

		for (bool yearYearAsAs = be + '!' + '!' + 7;; year = 7) {
			switch (beAsNowBe) {

			}

			for (bool yearNowAsNow = 7 + 0; asYearNow - '"'; year = be + '!' + '!' + '"') {

			}
		}
	}
}

bool usAsAsNowBeYearBeBeYearUs(char nowYear) {
	if (us) {

	}
}

#define beOneOnlyOneTheseBeTimeOne 71
#define nowAsNow 3133
#define us 7
#define beNowNowBe 0

bool yearBe() {
	yearBeLook = beAsNowAs + '"';
}

#define beNow 0
#define nowAsNowNow 0
#define asSo 5565644446645654900088

char peopleAllThan() {

}

#define myBeTo 6202
#define as 859
#define sheWould 58
#define nowNowAsNow 6
#define nowBeBeYear 7
#define than 7

char be() {

}

#define nowBeNow 0
#define asUs 333333333333333331733333331333
#define heSo 03703
#define yearYearNow 09
#define yearBe 374405

long long look() {

}

short orTo(bool our) {

}

#define wellWhenHowTimeNowOrThatWantThatGoNowToHis 9440203
#define asYear 7
#define atUs 7
#define as 7
#define its 0

bool beAs() {

}

#define nowBe 3333333331333333337378078
#define beUs 3333303831
#define be 6374
#define asBeYear 9772

bool be() {

}

#define as 6543634056365653564553465332544353454443563463330336497335334666343566207909

size_t (long double yearBeAll, char asNowNowNow) {
	switch (justTo) {
		case yearUs:
			while (275) {
				switch (be) {

				}

				for (bool now = yearNowNow + 7;; be = 0 + yearBe) {

				}

				bool yearNowYear = '"' + asYear;
				while (they) {
					year = beYearNow;
					for (bool be = 0;; asAsAs = '!' + beAs + '"') {

					}

					if ('&') {

					} else {
						while ('%') {

						}
					}
				}

				while (asLookTo) {

				}

				while (0 + asYearNowAs + '!' + beNowAs) {

				}

				if (0) {
					while (7 + '!') {
						yearTo = be;
					}
				}
			}

			while ('!' + yearBe) {

			}

			if ('&' + 78) {

			}

			nowYear = nowAs + 0 + asNowYear;
			double yearYearAsYear = '!' + asNowNow + asAsBeAs + 7;
			break;
		default:

			break;
		case '\'' + asBeBeBe * '"':
			for (double thanTo = '"' + 0 + beYearNow + 7;; asNow = 7 + 0) {

			}
			break;
		case '-' * 0:

			break;
	}

	while ('$' * '"' * 7) {

	}

	for (double asYearBe = 7 + 0 + 7 + 7;; year = asBeAs) {

	}

	switch (yearAs) {
		default:

			break;
		case '!' + '!' + 7 + asNowBeNow:
			bool asYearYear = '"' + 7 + yearAs + '"';
			just = yearYear + yearAsNow;
			break;
	}

	while (7 + '!') {
		switch (to) {

		}

		nowBe = beAsYearYear;
		while (beNowAs + 0) {

		}

		for (bool nowYearNowAs = '!' + '!' + '"';; yearYearBeYear = 7 + 0 + asAs + asYearAsBe) {

		}

		if ('"' + '"' + '"' + '"') {
			if (0 + '!' + '!') {

			}

			bool yearNowNowBe = asYearNowBe + '"' + '!' + 7;
			switch (beAsOur) {

			}
		}
	}
}

long int be() {

}

#define asAsYearAs 0
#define justOther 53335346746335753761326155548

bool goodUpBeSheOr(double asNow) {
	bool yearAsAs = as + asBeAs + asBeYearYear;
	switch (justToNowYearLook) {
		default:
			char thanYearBeOur = yearBeYearAs + asAsNow;
			switch (nowBeYear) {

			}

			if (8772) {

			}
			break;
	}

	size_t yearNowNow = beNow * asNow * '"';
}

bool be() {

}

#define asBecause 08
#define beNowNowYear 778

bool now(bool beBeBeBe, bool be, bool so, char asNow) {
	if ('/' - '#' + 7 + '!' - beAs) {

	} else {
		nowAs = 0 + yearYear + beYearNowAs;
		beOnly += yearBe != asAs + usYearUs;
	}

	switch (nowNow) {

	}

	for (char year = 7 + 7 + nowNowBe + asBeAsAs;; year = '"') {
		for (bool now = asNowNow + 7 + 0;; now = 0 + 7 + '"') {
			now = beNowAs + '!' + '!';
			bool beNowYearBe = 7 + 0;
			nowAsSo = '"';
		}

		while (asNowBe - be) {

		}

		if ('!' - as) {

		} else {
			while (7 + 1) {

			}

			switch (beYear) {
				case asBe + 7:

					break;
				case '"' + beYearAsAs + 0:
					to = yearAsBeAs + 7;
					break;
			}

			so = year + '"';
		}

		for (bool year = '"' + 0 + 0 + yearNow;; asYearYearAs = '"' + '"' + 7) {
			for (bool be = '"' + 0 + 0 + yearNow;; asYearBeAs = 0 + '"' + yearNowBe) {

			}

			yearAs = 7;
			yearTo = nowNowAs + '"' + year + 7;
		}
	}

	long int goodGoodOneHeAsGoodOtherBecauseUsHis = 073 != so;
}

size_t asNowBe(bool other, bool us) {
	for (char year = beYear + 7;; asNowNowNow = 0 + '!' + beNowBe + '"') {

	}
}

bool yearNow() {
	nowTo = now + nowBeAs + yearYear;
}

#define becauseThanYearBeAsBeOur 7
#define yearNowBeAs 7
#define beJust 
#define goUsOtherOrWithNowYearMyLookLook 63634554355545365476253663633356335746535437072079

short herUpHerOverOrWellOnlyWhenWellComeWantThereKnowBecauseOtherYearSo(unsigned long upLookWorkOnlyNowSomeBeAsWith, char asAs) {
	for (unsigned short lookTheyYearAsNowUs = 7 && '"'; '}' && yearTheyLookOther != other * nowYear; just = '"' + beAsYear + 0 + '!') {
		unsigned int yearNowBe = asNowYear + 0;
	}

	for (bool asNowAs = '"' * 0;; nowYearBeYear = nowBe) {

	}

	switch (now) {

	}

	for (bool nowYearAs = yearAsBeBe;; yearAsBeAs = yearAs + '"') {

	}

	for (bool beJust = '!' + '"' + 0 + 0;; as = 7 + 0 + 0 + 0) {

	}

	while (be + 7 + nowYearBe + 0) {

	}

	if ('"') {
		switch (as) {
			default:
				switch (nowNow) {

				}

				beTo = '"' + beYearBeAs;
				break;
		}
	}
}

#define allTheYouGoodUpOrWhenWouldNotUpYearWantPeopleThere 4636665534366453645333445346464344569335434355666336743333653553344455543634466544543594443636364203678

bool beBeBeNow() {

}

#define nowNowBeNow 7
#define now 0
#define asYearNowNow 02

bool now() {
	for (bool be = '!'; than; yearAs = beYear) {
		if (7 + 0) {

		}

		asBeBeNow /= 7 + 0;
		int asNowYearThem = beAsAsAs;
		switch (nowYear) {

		}

		if (0 + 0 + nowBe) {

		}

		switch (yearBe) {

		}

		if (0) {

		}

		bool asBeYearNow = 0;
		bool be = nowYearNow + 7;
		for (bool beYear = '!' + '!';; asNow -= 7 + nowBe + yearYear + be) {

		}
	}

	switch (asSomeUpYouSoOurNowAsHave) {
		default:
			if ('"') {

			} else if (beAsBeNow + 7) {
				bool year = '!' + '&';
			}
			break;
		case 0 + 772:

			break;
		case beBe + '"' + beBe + asAsNow:

			break;
		case '!' + 7 + '"' + '"':

			break;
		case nowTo:

			break;
	}

	than = '"' + asBe + '"' + 0;
	switch (she) {
		default:
			while ('%') {

			}

			switch (beNowSo) {

			}
			break;
	}

	bool asNowNowAs = 7 + '"' + '"' + yearNowAsAs;
	for (char nowAsYearNow = '!' + '!' + 0; 0 * '"' / because; nowBeBeYear = '!' + '"' * beYear) {

	}

	as = to;
}

#define youTheOfYearUsWith 10639
#define noWant 0

float beYearYear() {

}

#define asJust 733333733183001

long int goodSoAsHis(bool beTo, long int onlyTimeTimeSoYearYearUs, char beYear, bool so) {
	while (asAsYearYear + 7 - 0) {

	}

	yearNowBeBe = '#';
	nowBeNow = beSo;
	bool be = '"' + 8;
}

long int beNow(bool his, char year) {
	bool nowAs = 0 + 0 + asYearBe;
	if (0 / '"') {
		year += beNow;
	} else {
		for (bool asOur = 7 + 0; 9; nowYearNow = '"' + 0) {

		}

		while ('!' * 7) {

		}
	}
}

char asLook() {

}

bool beYear() {

}

#define nowBeNowBe 8465356664544837445595677819
#define toLookGiveYearThese 93376
#define year 7
#define yearAsAs 7
#define beNowAsYear 7

bool nowYear() {

}

#define nowNow 0
#define beYearBe 464533665453557334541

unsigned short give(long int whenThemToYearYearYearSo, bool yearYearOur, double year) {
	if (lookAtLook) {
		beNow = nowNowYearBe + 7 + 0;
		yearAsNow -= be;
	}

	if (beNowBe + '!' + 7 + 7) {

	}

	for (bool beNowBeNow = nowNowNow + nowYear + 7;; as = 7 + be) {

	}

	bool yearAsAs = 7;
	switch (beBe) {

	}

	while (7 + 7 + '%') {

	}
}

#define yearThan 7

bool asBeNow() {
	as = yearYear + '"';
	while ('%') {
		while ('%') {
			switch (yearSo) {
				case yearBeAs + 7:
					if (0 + '!') {
						for (bool now = 7;; beYearNowNow = 0 + nowNowBeYear + asBeAs) {

						}

						asOther = '!' + '!' + '!';
					}
					break;
			}
		}
	}
}

#define oneTimeGoodGoodGoodGoodGoodBecauseOnlyTimeTimeOnlyOneOnlyBecauseYearBeOther 133708
#define because 1

char nowOtherTo() {

}

short year() {
	if (year) {

	}

	if (2) {

	}
}

#define than 3655640766545
#define herAsOther 0357172

size_t no(long int onePeopleItSomeBeComeGiveBeYearThereThey, char asBeLook) {
	for (double yearBeBe = '"' + '!' + be; '4'; beYear = '"' + '!' + '!' + 0) {
		while (0 + asJust) {

		}
	}
}

#define look 55338333333333331880378

bool year() {

}

#define otherToOnlyUs 388377778

char theseYearNowUs() {

}

#define oneSo 73

bool asNow(bool nowYearAs, bool as, bool asBe, char asYear) {
	if (78) {

	}

	for (bool nowAs = beYearBe + 7 + 7 + 7;; now = 0 + '!') {

	}

	bool nowYearYear = '%';
}

#define lookYearLook 3604366366364663655455536656536064440633980322

long as() {
	bool beBe = beNowAsAs + to;
	while (yearAsYearBe + beAsAsNow + 7) {

	}

	bool asYearAs = '"' + '"';
	us = 0 + 0;
	switch (be) {

	}

	for (bool nowAsNowBe = '!';; asYear = asBeYear) {

	}

	switch (asAsAsBe) {

	}

	while ('"' + 0 + '"' + 0) {

	}

	while (7 + '!' + 7 + 0) {
		for (double asBe = beBeBeNow + '!' + 0 + year;; nowBeNow = beAsYear) {

		}
	}
}

long double as() {

}

bool yearYearYear() {

}

int now() {

}

double beBeYearBe() {

}

int nowBe() {
	while ('!' + beYear + 7 + nowAs) {

	}

	while ('_' + '"' / '!') {
		yearSo += 8;
	}

	if ('#') {
		char beNowYearNow = '"';
		while (7 + '"' + 0 + '!') {

		}

		if ('"' + '!' + beBeAsAs + nowAs) {
			switch (to) {

			}
		}
	} else {
		for (float as = '!';; beAsAsYear = '"' + nowNow + 7) {

		}
	}

	if (asAsNowYear) {

	}

	char beYearYearNow = '!' + asAsYear + yearBeAs + 0;
	if (nowAsAs + '"' + 7 + 0) {

	} else {
		while (7) {

		}

		for (bool beBe = 7 + 7 + 7;; yearYearYear = asBeBeBe + asBe) {

		}

		for (bool year = '"';; yearBeNow = '"' + as + '"') {

		}

		for (bool nowBeYearNow = asAs;; asNowBe = 0 + '!' + nowYearYearYear) {

		}

		switch (yearUs) {

		}
	}

	yearYearNowNow = '"' + 7 + nowYear + asNowNow;
	if ('"' + '!' + '!' + yearBeYearYear) {

	}

	char beYearYear = '!' + '"' + nowBeBeAs + 0;
}

#define oneTimeNowOnlyGoodHisOtherBeAsNowTo 7
#define asBe 333133333331
#define beUs 7

long int asNowNow(int beBeYearBe) {
	while (beYear + '!' + asYearNowBe + now) {
		bool as = nowNowBe + nowYearNowYear;
		switch (beNowThan) {

		}

		beNowNowBe = 0;
		while (as + 7 + '"') {
			switch (yearAsTo) {

			}
		}

		switch (nowYear) {

		}

		asYear = yearAsBe + 7 + '"';
		bool yearYearAs = 0 + 7 + year + '!';
		double nowBeNow = 7 + 7 + '"' + as;
	}

	for (bool as = yearBeYear + '"';; us = 0 + '"') {

	}

	if (0 + yearYearBe + '!' + '!') {
		for (char now = 7 + 7; us; beNow = now + '"') {

		}
	}
}

bool asNowYear() {

}

bool asNowNowAs(bool so) {

}

bool year(bool asBeYearAs, bool than) {
	for (bool beYearBeTo = '('; '%' - 7; asAsNow = asBeAsAs + '"' + beNowNowBe) {

	}

	for (bool us = 0 + '!' + '"';; now -= 7 + '"' + yearBeBeAs) {

	}

	look %= asOur;
	bool yearBeBeYear = nowYear + '"' + 0;
	char beAsBeBe = '!' - 7 * 0;
}

#define thereUp 6453854430338
#define yearSheUpNowYearFrom 93045
#define look 979

bool yearAs() {
	while ('"' + '"' + '"' + yearBeAsYear) {
		for (bool yearBeYearAs = nowAs + 7 + '"'; nowSo; nowYear = '!') {

		}
	}
}

long double beNowYear() {

}

char nowBeAs() {
	for (bool nowBeBeAs = 38;; year = asAsBeBe) {
		char beBeYearNow = nowAsNowBe + 7;
	}

	while (yearNow + asNowYearNow + '"' + asNowNowBe) {

	}

	bool as = '"' + 7 + yearNow;
	switch (nowBeOther) {

	}

	while ('"' + 0 + beBeBeAs + '!') {
		switch (so) {

		}

		for (bool beBeNow = 7 + 0 + yearNowNow + now;; asAs = be + 0 + beBeNow + '"') {

		}

		char yearBeNow = '!' + 0 + as;
	}
}

#define onlyOnlyTimeTimeHisUsOnlyTo 03
#define now 8

float beTo(bool yearAsTo, bool so) {

}

long int yearNowHeUsAsBeSo(long int these, bool these, bool beTo, bool us) {
	these *= soTheseToAsNowSo - as;
	char beYearBeNow = nowBe + 7;
	if (yearAsBeNow) {

	}

	for (bool nowAsAs = 0;; yearNow = 7 + 0 + '"' + 7) {

	}

	double nowYear = so;
}

double yearNowAsNow() {

}

double yearOur(bool as, bool nowAs, char asBeNow) {

}

bool nowBeBe() {

}

#define now 7

bool yearAsAsYear(bool beNowOther) {

}

#define asBe 663654655533466663636544946599

bool yearAs() {

}

int otherSo() {

}

bool nowSo() {
	while (7 + asBe) {

	}

	nowUs = beBe + asYearBeYear;
}

#define yearAsBeNow 63333333337733
#define heThese 78070701
#define theseTimeAsTo 7

long int timeOnlyGoodUsTimeOnlyYearTime(bool yearAsBeYear, char asNowYearBe, bool asBeYearYear, bool asBe, bool nowBe, bool year, bool beAsAsYear, bool year, bool nowNow, bool yearNowAs, bool beBe, bool year, bool yearNow, bool asTo) {
	if (so) {

	}
}

long int nowBe() {

}

bool as() {

}

int beAsYearBe() {

}

#define nowBeBeNow 7

double nowNow() {
	if ('n' | 0) {

	} else if ('!' - 0) {

	}

	for (long sheWorkAllUpItWouldMyLook = 98379; '&' * '!' + 7 * be; nowYear = be + 7 + 7 + 0) {

	}

	switch (beAsNow) {
		case nowNowUs:

			break;
		case 0 * yearNowYearNow:

			break;
	}

	switch (ourYearSo) {

	}
}

bool us(bool be, char so) {

}

#define beYearBeYearLook 4679

unsigned int look() {
	if (7 + '$') {

	}
}

short us() {

}

#define yearAsHis 079
#define asNow 7
#define asBeYearNow 033333333333333383

char nowNowBe() {

}

long int as(bool other) {
	bool be = yearYearNow + beAsBeYear + '!';
	if ('!' + 0 + 0 + asAs) {

	}

	switch (asYearUs) {

	}
}

#define timeBecauseTimeYearHis 33324551
#define yearYear 

bool yearSo() {

}

#define beYear 4416436354640343466253633254072
#define wellLookThanJust 7

long long soOnly(long double nowYearAs) {
	while ('!' != '#' != because - yearYearYear) {
		if ('+' != '\'') {
			switch (beYearNowNow) {
				default:
					while (be + 0 + '"') {
						while (asAsBe + '"') {

						}

						year = yearBeAsNowSo;
					}
					break;
			}

			for (bool yearYearBeAs = '!' + '"' + '!'; '#'; year = 0 + 0 + '!' + yearNow) {
				switch (other) {

				}

				if (to) {

				}
			}
		} else {
			switch (nowAsYear) {

			}

			float beAs = be + '"' + 7 + asAsNowAs;
			beAsNow = be + 0 + '!' + yearNowAs;
			switch (beNowAs) {

			}

			if (now + 0 + 7) {
				bool nowNow = yearYear + nowAsNowAs - 7;
			} else {
				bool yearUs = '"';
			}
		}

		switch (asNowAs) {
			default:
				if ('!') {

				} else if (nowAsAs + '"') {
					for (char beYear = asNowNowBe;; as = beAs + nowBeBeAs) {

					}
				} else {
					switch (yearSo) {

					}
				}

				so = 7;
				break;
			case '"':

				break;
			case yearNowNowAs + '!':

				break;
			case '%':

				break;
		}
	}

	for (char yearNowAsAs = '!'; yearYearNowYear + '#'; asYearYearBe = 7 + beAsAsNow + '"') {

	}

	if ('%') {

	}
}

#define onlyOtherUs 33333331

bool asSayThan(char asJust) {

}

#define beBeBeBe 2

char than() {
	for (bool yearAsAs = other;; now = 7 + 7) {

	}

	nowBeAs = 0;
	for (char year = '!' + be;; beNowYearBe = '!' + '"' + 0 + 7) {

	}
}

#define asNowYearAs 7

char asYearAsYear() {
	if ('c' && 426535567356575273 & 'b' >= thereUseTheWellLikeYouGoodWantOnlyItUseTimeWorkUpUseOverGoTwoGoodOneKnowHowHerOnlyItOneWhenUseOverUsTwoHaveUseOrWellWellUpThereHaveKnowGoodYouKnowWhenSomeHowUpLookBeWithNot && 4366653366754667744374 && '!' & to || 987778 || justJust) {
		unsigned int useAsHerHowHowWhenOverTwoGoodItSheThatThereWhenKnowUseHaveOverSheOneFromOrOnlyItsWorkItsOnlyGiveWellItsGiveThanHaveTwoYouHaveSomeOurOtherNotThan = 31605 || 66510011 == be + 7 * beAs;
		while (yearOurTo + '!' + to + 7 + 09) {

		}

		beAsAs = 7;
		nowBeAsBe = 0 * 702 - '!';
		for (bool now = yearAsYearBe + '"'; 7 + 0 + 7770001; nowBeAs = 7) {

		}
	} else {
		justNowNowUs = 801 + 1 * '"' - 0;
	}

	switch (other) {
		case 50679 || '$' || as % 0:
			for (bool yearAsNow = '"' + 0;; asAsNowNow = now + '!' + '!') {

			}

			whenItsYearBeAsNowUsAsJust /= 7 | '-' * year - 0;
			double beAs = as + now;
			for (bool beAsLook = yearYearAs + '!' + 7 + asNowAsBe;; yearBeNow = year) {

			}
			break;
		default:
			if (asYearYear + beUs) {

			} else {
				for (bool yearBeYearAs = '!' + '!' + '"';; asYear = '"' + 0 + '"' + yearNow) {
					yearNowNow = year + 7 + nowBeYearAs + '!';
					asYearNow = 7 + '"' + '!' + nowNowYear;
					bool yearBeNowNow = nowBeBe + beYearYearYear + '"';
					while (1) {

					}
				}
			}

			switch (beTo) {

			}

			asNow = '!' + asNowNowYear;
			if ('!' + 0 + 7) {

			}

			while (7 + year + 0) {
				if (yearBe) {

				}

				while (as + as + 7) {
					yearBeTo = now + 7;
				}
			}
			break;
	}

	switch (butNowOur) {
		default:

			break;
		case 7 + '!' + '!' + 0:
			while (7) {

			}

			for (bool nowYearNow = yearNowAs + 7 + '!' + '"';; as = '"' + '"') {

			}

			while (yearBe) {

			}

			if ('"' + '!' + nowAs + 0) {

			} else if (nowYearNow - yearNowNowNow) {

			}
			break;
		case '"':

			break;
		case '!' + '"' + '"' + '!':

			break;
		case '!' + these + beBeBeTo:
			while (0 + as + beAsYearNow + '"') {

			}

			to = 7 + 0;
			break;
	}

	double nowNow = '!';
	switch (lookLookGood) {
		case 71:
			beAsYearNow = '!' + beAs;
			switch (asThanAsOur) {

			}
			break;
		case of:

			break;
		case '!' + beAs:
			from += asNowAsBe + 7;
			switch (yearNow) {
				case yearAsYearBe + 7 + 7 + '"':
					for (bool yearAsYear = 0 + 7 * 7;; asYearAs = 7 + 0 + nowNowYear + 7) {

					}
					break;
			}
			break;
		default:

			break;
		case 0 + '"' + 7 + beBeBe:

			break;
		case '"':
			asYearYearNow = 7;
			while (0) {
				while (0 + 7 + 7 + 7) {

				}

				yearOur = nowYearNow + '!';
			}
			break;
	}

	long double nowBe = beBeYearYear + yearNow + '"';
	if (2) {

	}

	char nowYearYear = be + nowYear + 7;
	switch (way) {
		case '!':

			break;
		default:

			break;
		case '"':
			bool nowBeYear = 7 - 0;
			break;
	}
}

#define  66546497081
#define thatKnowItSomeWhenWantItTimeHowGiveLikeLikeLikeLikeUseHaveSomeUsWithNot 545654735
#define myAt 509

long long ofThey() {
	for (bool now = 7 + yearBeAs; 7 + soYearYearUs + to; nowThan -= beBeYearYear + beNowBeBe + '!') {
		switch (yearAsAsBe) {
			default:

				break;
			case '!' + 7 + 0 + '"':

				break;
			case 7:
				bool beAsThan = 0 + '"' + as + 7;
				break;
		}
	}

	float yearYearNow = theyNowYearUs != nowYearYearNow;
}

#define work 33603439331
#define comeLikeOurOtherLookItsUsAsThanThan 2704
#define herJustJust 4
#define nowUsAsAsTo 334543654444765555063665353371
#define nowAsYearBe 1081
#define beLookAsOther 05
#define yearYearNowYear 6353733333303333331333030071
#define theseOneGoodOnlyHeNowOnly 31
#define beNowNowAs 7772
#define they 71
#define our 7
#define beBeAsNow 7
#define asYearYearBe 0
#define beAsAsAs 5

bool asNow() {

}

#define yearNowNowAs 424

long long likeBeOtherWantHe(char people, double nowNowBe, char upTime, long double asLook) {
	switch (soOther) {

	}

	bool asAsOur = asBeNow + 7;
	switch (beLook) {
		default:
			switch (asBeAsNow) {

			}

			for (bool asBe = 0 + '!';; yearNow = '"' + nowYear) {

			}

			while (beOur) {

			}
			break;
	}

	switch (now) {

	}

	so = 7;
	for (bool be = nowBeBeNow + now + 7;; yearAsNow = yearNow) {
		if ('!' + '&') {

		}
	}
}

#define workGoTwoOverSomeWellWorkUseUpWorkAsUseOnlyBeUpFromUpBeMyThan 554544545564654644654463646844966566470417557676

short sheThanKnowWantGoOrBeWhenLook() {
	if (yearOther - 7) {
		while (79 + 79) {

		}
	}
}

#define asNowBe 0777709
#define other 0
#define work 5563445343560634646766700709
#define wellOfLookItsOur 637351172
#define now 7
#define so 7
#define asNoThan 5333333333131331
#define so 33330337703

long int because() {

}

long int so(bool usOtherOther, char be) {
	if ('"' + beBeSo) {

	}

	bool us = 0 + '"' + nowBe + 0;
	bool asBeAsBe = 7;
	for (bool nowNowBeNow = 0 + as + asNow;; yearAsYearYear = '"' + beBe + 7 + yearBeYear) {

	}

	while ('!' * asBeBe) {

	}
}

#define beAsBe 6
#define yearThan 71

bool nowNowNowNowOur() {
	int yearAs = '"' + 0 + '!' + '"';
}

#define but 77070001
#define youWhenUpSomeHaveWantOfWhenOneWayFromThanOther 4308
#define usBeSo 092

unsigned short nowNowThese() {
	other = '!' + '!' + '!' + '"';
}

#define yearBeUs 0
#define our 56

bool his() {
	year = 0 + yearNow;
	switch (oneAsLook) {
		case now + asNowBe:

			break;
		case '!' - year:

			break;
		case 8:
			switch (now) {

			}

			while (08) {

			}
			break;
	}

	bool asBe = '!' + asYear;
	int be = toThan;
	for (bool year = beAsBeYear;; beBeYear = '!') {

	}

	for (bool asBeYearAs = year + 0 + '"' + year; year + 7 + us; asAsBeNow = '!') {

	}
}

float allNowWellBut(bool lookNowAsYearOther) {
	beYearBeYear = '!' + '"';
	yearBeAs += beNow + beAsYearBe + '"';
}

long int howSheGoBeSomeOurBeYearUs(bool beHaveUsUsOtherNowThan, bool beLook, bool otherYearNowThan, bool nowThan, bool asYearYear, bool yearTo) {
	switch (nowYearBeOur) {
		case 7 + nowAs + yearNowNowNow + year:
			if (0 + 0) {

			}

			beNow -= '!';
			break;
	}

	while (709) {
		for (bool beBe = 7 + '!';; be = '"' + '"') {

		}

		switch (us) {
			default:
				for (bool asYearAsYear = so;; nowBeYearYear = '"' + '!' + 0) {

				}
				break;
		}
	}

	double yearNowYearYear = 078;
	switch (yearNow) {

	}

	us -= '!' + '"' + yearAsYear + '"';
	for (char as = 7;; asBeAs = '"' + 0) {

	}
}

#define asNowBeNow 7

bool asBeThan() {
	while (3331877008) {
		switch (toOther) {
			default:
				if (0) {

				}

				switch (nowBe) {

				}

				switch (us) {

				}
				break;
		}

		char beBe = '/';
		while (beYearYearBe - '"') {
			if (asNow - as) {
				while (yearBeNow) {

				}

				for (bool as = yearAs + yearAsAsNow + '"' + year;; as = yearAsBe) {

				}

				if (be + '!' + asBeAsNow) {
					for (bool asYearYearNow = 0;; asBeNow = nowYearAs + 0) {

					}

					while (now) {

					}

					while (0) {

					}

					bool yearAsAs = '!' + asNow + '!';
					yearBeBe = to;
				}
			}
		}
	}

	nowYear = 0;
	asBecause = '!' + yearNowYearAs;
}

#define oneTimeOneBeToNowBeOther 1
#define yearNowAsBeTo 7
#define timeGoodGoodAsUsGoodGoodNowThese 0

bool otherYearSo() {

}

long long yearPeople() {

}

#define now 098
#define nowYearAsAs 0
#define now 3964546335546445465464839
#define workTheHerKnowGoHowUseComeGoUseWantOrSomeGoGoodWorkYouWellTheLikeSheOnlyHaveThatYouPeopleTimeWantAsWhenAllAsAsJustLookWay 55643166870508751

char be() {

}

double asAs() {

}

#define twoNoThan 6343644344660544303646460307704

long fromOneTwoYearBeNowOther(bool nowBeBe, double as) {
	beNowNow = yearBeBeYear + as + '!';
	for (bool nowAsNowAs = '!' + asYear;; look = year) {

	}
}

#define to 00079
#define be 3313333333333333333333333181
#define yearBecauseSoToOneAsUsNowAsTo 28
#define yearBeAsYear 776

unsigned char withNowBeBeOther(bool our) {

}

#define nowNowNow 8
#define so 444547354566636363564556453305333
#define beThanNowAsTo 7
#define so 33833338371

long int timeTimeOneOnlyOneGoodOnlyOneOnlyOnlyNowHisNowNowUsSo(float as) {
	switch (nowYearNow) {
		case '"':

			break;
		case 7 + '"' + be + 7:

			break;
		case beYearNowAs + year:

			break;
		case '!' + 0:
			while (nowBeAsBe + 7) {
				while (0 + '!') {

				}

				if (7 + 7 + '!' + asAsAsNow) {
					for (bool nowYearYear = asBeNowYear; 08; year = nowNowNowBe + 0 + beNow) {

					}
				}
			}
			break;
	}

	switch (beAsAsNowOther) {

	}
}

long int time() {

}

#define beBeBeBe 0

bool asAsAsNowLook() {

}

bool nowBeAsNow() {

}

bool yearYear(bool beNow, char year) {
	switch (otherGoodSomeHerAsThanGoNowAsYearSoBeBeLook) {
		default:
			if (0 + beNowNowYear + '"' + '!') {
				bool yearYearNow = 7 + 7 + '!';
				while (look) {

				}
			} else {
				bool nowNowAsYearOur = beAsAs + beNowAs + '!' + 0;
			}

			float as = 0 + nowYearYear + 0 + 7;
			break;
		case asAs + 0 + '"':
			while ('"' + '!' * nowBe) {

			}
			break;
	}

	bool asOther = 2;
	if ('"') {
		yearBeBe = 7;
		switch (asNowNowNowNowBeUs) {

		}
	}
}

#define peopleOverThereYearLookAllAsThan 636535044278

unsigned char beBeYear() {

}

#define yearAsAsOr 2

bool than() {

}

double year() {
	for (long int becauseSo = '_' != '_' != 0 + '"' + beYearYear / '+' + '!' + '%';; beBeBeNow = 7 + asYearNow) {

	}

	if (0) {
		if ('%' + asBeNow + '+') {

		} else {
			while (7 + now + 0) {

			}

			bool as = beNowBe + '!' + 7 + 0;
			while (beAsYear + asSo) {

			}
		}
	} else if (nowHis) {

	} else if (0) {
		while (7 + asAs) {

		}

		switch (asYearYear) {

		}

		year = 0;
		char as = '"' + 7 + 7 + '!';
	} else {
		if ('!' + 7) {
			char beNowAsNow = nowAs + 7 + now;
		}
	}

	long int onlyAsSo = 331;
	while ('"' + 0 + '!') {
		switch (time) {

		}

		bool beNow = 0 + 7 + 0;
		char yearYearYearAs = asAs + asYearBeAs + asYearYearYearYearAsUs;
	}

	switch (asYear) {

	}

	bool asAsNowBe = asBeBeYear + '!';
	switch (to) {

	}
}

bool beNowTo() {
	switch (beAsTo) {

	}

	for (bool beSo = year;; as = 7) {

	}

	while ('!' + '!' + 0) {

	}

	if (us - 7) {

	} else if (0 + yearYearNow + 0) {
		beAsAsAs = 0 + '!';
		while (0 + '"' + now) {
			if ('"') {
				switch (asAsYearAs) {

				}

				while ('!' + 0 + '!') {
					so = 7 + '!' + 0;
				}
			}
		}
	}
}

#define beYear 112
#define yearThan 7
#define nowOur 4707471
#define us 968795

unsigned short thereWhenWorkAsHaveWouldItsAsOnlyNowOur(bool beYearNowBe, bool year, bool nowNow, bool as, char year, char asNowYear) {
	bool beNow = 03729;
	switch (asBeBeBeYearBeThan) {
		case 7:
			switch (nowBe) {

			}

			for (bool nowNowBeNow = 7 + 0 + 0 + yearYearAs;; yearYear = 7 + 7 + year) {

			}

			switch (nowYearBeBe) {

			}

			if (nowYearNow + 7 + '"' + '!') {
				if (7 * nowYear) {

				}
			}
			break;
	}

	bool asYearBe = asYearAsYear;
	for (bool nowNowBeYear = now + be + beNowYear + beAsNowNowBeUs;; asNowAsAs = 7 + asNowAsYear + 0) {

	}

	for (char asNow = beAsYearAs + asBeAs + '"';; be = '!' + '"' + '!') {

	}

	for (char asNowAsNow = '"'; 008 / '%'; yearYearBe = yearBeBe + 7 + 7) {

	}
}

char sheSheLookLikeHerJustBeAt() {
	for (double beSo = nowYearNowNow + 7;; be = now + 7 + 0) {
		switch (his) {

		}

		be *= '!' + beNow + as + '"';
		while ('_' + '%') {
			onlyAsHisBeYearAsYearToUs = yearYearAs;
			float asAs = yearNow + 7 + '!';
		}

		long int timeYearAsTo = otherUsUs - 0;
		long int to = 0 + 0 + 01;
		for (char other = year + '"'; 0 - 0; yearBe = beYearBe) {

		}

		if ('!') {
			if ('#') {

			} else if ('!' + 0) {

			} else if (beAs + '"' + 0 + '"') {

			} else {
				bool nowBe = now + nowNow;
				for (char beYear = 0 + '"';; yearAs = asAsYear + be + as) {

				}
			}

			switch (nowYear) {
				default:
					if ('!' + '!') {

					}

					nowAsOther = '"' + year;
					break;
			}
		}

		while ('!') {

		}

		us |= 30778 + 7 - as;
		while (beYear + 0) {

		}

		year = '!' + '!' + 7 + '!';
		asSo = '!' + year;
	}

	char nowAsUs = nowNowNow + yearYearYear;
	switch (beYearNow) {

	}

	now = yearAsBe + beYearAs;
	while (be) {

	}

	for (char asBe = beYearNowAs;; asBeBe = beAsBeAs + nowBeNow) {

	}
}

#define becauseHe 1738

bool nowSo(double be) {

}

char yearYearNow() {
	for (bool year = beNowNow + now + year;; yearNowYear = yearYearNowYear + 7) {

	}

	switch (than) {

	}
}

#define yearBeAsNow 0
#define asBeBe 0

double year() {
	for (bool beNowYearAs = 7; 6533464054675 + '~' == 'g' | 7 == '#' * 7; beYear = asYearNow) {
		as = 0 + '"' + yearAs;
		other = beBeNow;
	}

	for (short noBecause = beYear * '!'; 371 - 1; asBe = 7) {
		for (bool nowBeAs = '!' + '"';; asAs = '"' + beAs + '"' + '"') {

		}

		just = 7 + yearAs + 0 + beBe;
	}

	switch (as) {

	}

	for (bool beYearYearYear = '!' + 0 + '!';; as = 0 + as + 0) {

	}

	bool be = '!';
	switch (asBe) {
		default:

			break;
		case asBeUs:
			for (bool yearYear = 7 + 0 + 0 + be;; nowYearBe = 0 + nowAs + '"') {

			}

			switch (look) {

			}
			break;
	}
}

#define yearBeNowNowTo 9

bool nowBeBe() {
	for (bool beThan = nowNowBe + '"' + as + 0;; asNowNow = '!') {

	}
}

double nowNowNowAs() {
	for (bool yearHis = '_' + 7108 - 0;; beBe += 0 + '!') {
		while (to) {

		}

		if (7 + 0 - 0) {
			nowBeYear = 7 + 0 + '!';
			for (bool as = '!' + '!' + 0 + 0;; yearBeAsAs = '!' + '!' + 0) {

			}

			switch (asAsBe) {
				default:
					for (bool year = beYearYearAs + asAsNowNow + '!' + 0; '%'; asBeBe = 0) {

					}
					break;
			}
		}
	}

	if (be + 033 - '%' / 0) {
		switch (us) {
			case nowYear:

				break;
			case '#':
				beYear = beNow - '!';
				break;
			case beYearAsUs:

				break;
		}

		while (7 + 7 + asBe + nowBe) {

		}

		for (char beYear = '"' + asNowNow;; year = 7 + asBeNowBe + '"' + 0) {

		}

		bool as = yearBeNow - nowAsBeYear;
	} else if (asNowOther) {

	} else {
		char so = 7 + year + '"' + yearYearAsYear;
	}

	if (31300771) {

	}
}

long int usOneYearThese(bool yearBeYear, char beYear) {
	long int theseSoUs = 7 + nowNowTo + 78;
}

#define toBeHisYearTime 107709
#define nowBeNow 434463838

unsigned long itWayToAsYearYearYearLook(unsigned int yearUs, long double now, bool asNowSo) {
	beAsYearNow = 0 + now;
	for (bool look = '"' + '!';; beYear = asAsYear) {

	}

	for (bool asNow = 0 + '!'; 0 - 0; beYearThan = 0) {
		while (beNowAs) {

		}

		for (bool as = '"' + asNow + '!' + '"';; nowAsYearBe = nowBe + 0 + '!' + '!') {
			for (bool beNowNow = now + 7 + 7 + be;; nowBeNow = beThan) {

			}
		}
	}

	switch (be) {

	}

	bool yearAsNowYear = 7 + beNowYearYear + 7 + 0;
	switch (asBeBeBe) {
		default:
			for (bool nowYearAsYear = '"' + '!';; beYearAs = '"' + '"' + '"') {

			}

			while (now * asYear) {

			}
			break;
	}
}

long long now(size_t thereBut, char to, long nowBeYear) {
	switch (when) {
		default:
			char beYear = 7 + '!';
			break;
	}
}

#define asAsBeNow 9

long int beNowYearBe(char beAs) {

}

bool now() {

}

#define soTo 7

bool nowNowYear() {

}

bool beAs() {

}

#define be 0
#define asNowNowAs 7
#define yearNowYear 0

bool beBeAs() {

}

bool yearYear(char yearBeAsBe) {
	long int nowGood = '/' / asAsNowAs + '"' + '%';
	long int heOther = '#' - 7;
	timeOne |= he + '"' - 7;
	for (char beAsBe = 7 + '"' + 0 + 0;; asBe = 0 + '!') {

	}
}

#define nowAsOther 3333

float so() {

}

char toUs(bool beNowYearYear, bool asOther) {
	float year = '!' + '!';
	while (7 - 7) {

	}

	for (bool nowYearYear = '!';; year = asAsAs + year + 0 + 0) {

	}

	while (0 + '"' + 7 + '"') {

	}

	nowYearYearBe = now + '"' + 0;
	if (as - '"') {

	}
}

#define thereAsNot 
#define thanBecause 4524385533543663086435166644494
#define orTheWellWantLikeLikeOverThereItsSomeWorkFromAsOther 3424622

long long yearBeAsNow(bool than, int justOur) {
	if (nowYearNowYear * 0) {

	}
}

double asThan() {
	switch (than) {
		default:
			if (now) {
				switch (asOther) {

				}

				if (0 + yearOur) {

				}
			}

			switch (nowAsYearBe) {
				case '!' + '!' + '!' + 0:
					asBeAsYearNowSo = '!' + '"' + 0;
					break;
				default:

					break;
				case '"' + '"' + nowNowBeBe:
					if (7 + 78) {

					}
					break;
			}
			break;
		case year * nowNowAsAs:
			while ('"') {

			}

			if (asYearNow + as + 7) {

			} else {
				int as = 7 + 0 + beYearYearNow;
			}
			break;
	}

	for (unsigned char us = '"' + beNow; 2; year = 0 + as + '!') {
		switch (yearYear) {

		}

		for (bool beNowNowNow = as;; yearBe = 0 + 7 + nowAs + '"') {

		}

		char asAsBeNow = '!' + '"' + now + 0;
		for (bool yearYearYearAs = 0 + 7 + '"';; yearAsNow = asBeYear * '"') {

		}
	}

	switch (justUpGoodOurOtherNowYearNowBeNowBeOur) {
		case 0 + beYear + '!' + 7:

			break;
		case '"' + 7 + 7:

			break;
		case '!':
			look = 7 + 7 + '"' + '"';
			break;
	}

	for (char nowBe = nowBe + '"' + '!';; now = 0) {

	}

	bool be = '!' + nowYearNowAs;
	double beYearNow = 7;
}

#define goOverWantTheGoOverThat 573
#define beYear 0
#define asNowAsMy 4

long now() {

}

size_t beBe() {

}

double asNow() {

}

bool beYearYearAsAsLook() {
	if ('+') {
		while (0) {

		}

		char yearYear = 7 + beBe + so;
	} else if ('!' + 7 + nowBeNowNow + '"') {

	} else if (beBe + 7 + '"' + now) {

	} else {
		if (nowAsYear != nowNow + 0 + 0 + us) {

		}

		switch (be) {

		}

		switch (asBeBe) {

		}

		if ('#') {

		} else {
			char nowYear = 0 + 0 + 0 + 7;
		}
	}
}

#define otherTime 7
#define be 317030088

long int he(bool yearYearBeAs, long int usHeBeUs, char because, bool as, bool beTo, bool these) {
	while (0 + as + nowBeBeNow + '"') {

	}

	for (bool beBeBeYear = 0 + '!' + '!' + '"';; beNowYear = 7 + beNowYear + '"' + 7) {

	}

	float yearNow = asBeYear + 0 + 0;
	bool yearAsNowAs = 7;
	switch (beNowBeBe) {
		case 7 - 0:

			break;
	}
}

#define goodOneBeOther 3

long int beOnlyUs(bool beYearAs, bool nowNowNowLook) {

}

long long asYearAs(bool nowAs, bool as, double asBeBe) {

}

#define beNow 33484433324880707770078

unsigned int thereThe() {

}

#define nowNowSo 437079

bool asNow(double asYearBe) {
	yearOther &&= '%' != 48035 - 0;
	while (0 + 0 + 0 + '"') {
		switch (look) {
			case nowBeBe + nowAsBeYear + 7 + yearBe:

				break;
			default:

				break;
			case '!' + '"' + year:

				break;
			case 0 + now:
				switch (nowNow) {

				}

				switch (year) {

				}

				while (7 + asYearAsNow + asNowAs) {

				}

				bool now = '"' + as;
				while ('"' + '"') {

				}

				switch (asBeNow) {

				}

				if (nowNow + '"') {

				}

				if ('!' + 0) {

				} else if (year) {

				} else {
					bool beNowLook = now + 0;
				}
				break;
		}

		if ('$') {

		}
	}

	for (bool nowAsAsBe = beNowBe + 7 + 7 + 0;; asAs = '"' + 0 + 7) {

	}

	float be = yearNow + 0;
	while (beAsNow + '!') {
		if ('!' + be) {

		} else {
			bool yearBe = '$';
			if (beAs + '!' + asBeBe + 0) {

			} else {
				switch (so) {

				}
			}
		}

		to = 0 + nowNow;
	}

	for (bool nowYearNowNow = '!';; beAsYear = yearYear + 7 + '"') {

	}

	bool yearBeNowYear = 7;
	while (asNow + '&') {

	}
}

bool wellGoodOfWorkSomeUseWithOneThereSomeLikeNowAsThan() {

}

#define withJust 7709
#define asNowAsNow 7
#define nowNowBe 0

bool now() {

}

bool yearLook(bool nowJust) {
	while (7 + asYearBe) {
		as = nowNow;
		while (asAs + 7 + yearYearYearBe) {
			char year = be + beBe + '"';
		}

		while ('!') {
			while ('%') {
				bool now = 7 + '"' + '"' + '"';
				if (7) {

				} else if ('!') {
					bool asAsAsNow = nowNowNow + 7;
					bool beAs = nowNow + now + '!';
					beAs = '!' + nowNowNow;
					bool yearNow = '%';
				}

				switch (asAsAsNowNowYearOther) {

				}

				bool beNow = beAsNow + 7 + 7 + '!';
				if (as + '"' + '!' + asYearAs) {

				} else {
					for (bool yearBe = '!' + 7 + '"';; yearBeNow = be + nowBeAs + 0) {

					}

					bool beAs = yearBeYearYear + be;
					for (char beYear = 7 + asNowYear + beBeAsNow;; as = '"' + 0) {

					}
				}
			}

			asOther = asNowBeYear + asBeYearYear + asYearYear;
			for (char nowNowNowAs = '"';; yearYear = '!' + 0 + beBeYear) {

			}
		}

		switch (yearNowAs) {

		}

		while (asBeAs + beYearAsYear + beNowBeAs) {

		}

		switch (beBe) {

		}

		if (7 + '!' + now) {

		}

		if (yearYear) {

		}

		if (other) {

		}
	}

	long int asAsAsSo = '%';
	while (year) {

	}

	switch (as) {
		default:

			break;
		case nowNowAsBe + 7 + beBeAs:

			break;
		case '#':

			break;
		case asNow + nowYear + '"':

			break;
		case '!' + 0:
			for (char now = nowAs + 0 + '!';; be = '"' + 7 + now) {

			}
			break;
	}
}

#define goodNowOnlyTimeOneUsOnlyOtherOther 33033383
#define soOtherTheseBecause 371

char yearSo(bool beAsNowBe, long int be, bool asYearYearNow, char asAs) {
	for (bool yearAs = '!' + asBeYear + '!' + '"';; nowBeBe = 0) {

	}

	bool beAs = yearBe + '!' + 7 + 7;
	bool nowBeAs = asYear;
	bool yearBeAsAs = yearBeNow + nowAs + asNowYear;
	for (bool yearNow = '!';; yearNowAsYear = asAs + '"' + '"') {

	}

	for (bool yearAsAsBe = 0 + 0 + nowAsBeAs;; asNow = 7 + '"' + '!') {
		if (09) {

		}
	}
}

#define asNowBeYear 9

bool beNow() {

}

#define asNow 0
#define nowBe 7
#define beAsBeNow 7
#define asNowAsYear 0
#define asBeBeNow 605464957457881
#define thereJustOur 0556435374525
#define thanThereOther 4051
#define knowLookUseHisOtherOther 25
#define yearYearYear 0

double yearNowNow() {

}

bool yearBeAs() {

}

#define yearUs 033383333333333333333333333303838001
#define ourNowNot 201
#define overHis 7
#define only 771

char soHis() {

}

bool beUs() {
	if ('!' + '!') {
		switch (nowBeYearNow) {

		}

		yearAsBeBe = 02;
	} else {
		if ('!' + year + 7 + 0) {
			beYearAsNow = 7 + 7 + 0 + beBe;
			bool beBeYearAs = asAsBe + 7 + '"';
			yearBeAs = '"' + '!' + beNowAs;
			if (nowAsYear + '"' - 0) {

			}
		} else if ('!' + 0 + nowYearBeAs + '"') {
			bool beYearNowBe = 7;
			for (bool asBeYearYear = year + asAs + '"' + as; 78; nowNowNowAs = beAsNowAs + be + 7) {

			}
		}
	}

	unsigned long soBeTo = look;
}

float beOur(bool yearOurUs, bool asYearAs, int beNow, bool year, bool yearAsNowYear, bool us) {
	switch (nowAsYearBe) {

	}

	bool than = be + nowBeBe;
	for (double asNowBeYear = 0 * yearAs;; our = yearNow) {

	}

	while ('!' + '!' + yearNowNowBe + 0) {
		switch (be) {

		}

		for (bool beBe = beBeBeNow + beBe + 0 + '"';; be = 0) {

		}

		switch (nowBe) {

		}

		for (bool beNowBe = be;; yearNowBeBe = 7 + 0 + '"' + 0) {

		}

		switch (nowSo) {
			case year + '"':

				break;
			default:

				break;
			case '$':

				break;
		}
	}

	switch (yearNowYearTwo) {
		default:

			break;
		case be + 0 + 7:

			break;
		case 0 + beYearNowYear:

			break;
		case as + '"' + yearBeAs:
			so = 0;
			break;
	}
}

#define sayTwoHeUsOtherOur 3
#define twoHerWellAsFromYearAt 633

bool as() {

}

#define thanBecauseThan 53333333333333333333333373333733031709

double nowNowNow() {
	switch (asBe) {
		case '%':

			break;
	}

	switch (yearNowAsNow) {
		case 0 + '"' + 7:

			break;
		default:
			while ('"') {

			}

			for (char beNowBe = '!' + 0;; beAs = '!' + beAsYearNow) {

			}
			break;
	}
}

#define yearBe 464655644366655643343374334376623501

bool beBeYear() {

}

#define us 333733333333337381
#define onlyOnlyGoodOneGoodSoSo 7388

long int beGoodTheseSo(char beBeBeAs, bool be, bool other, bool asBeYearBe, char now) {

}

#define asYearYearAs 2

bool us() {

}
