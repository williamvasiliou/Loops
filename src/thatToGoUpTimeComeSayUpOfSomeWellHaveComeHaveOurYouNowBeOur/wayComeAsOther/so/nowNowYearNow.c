#include <2/222.h>

#define becauseYearToUs 0
#define oneTimeHisGoodOnlyOneHisOneNowThese 3373

bool timeTimeTimeOneOneUsBecauseGoThan() {
	if (asYearUs) {
		switch (usUseAsNowJust) {

		}

		switch (toYearAsYearBeAsNowAsBeLook) {
			case 7 + '!' + asNow + '!':
				switch (asNow) {
					case asYear + 0 + '!':

						break;
					case 7 + yearYearAsBe + '!' + 7:
						for (bool now = '"' + beAs + '!';; beYear = 7 + year + 7) {

						}

						switch (now) {
							case 0 + be + 0:
								bool now = yearYearAs;
								asBeBeYear = '"' + '!' + '"' + nowNowNowBe;
								while ('%') {

								}
								break;
						}

						bool beBeAs = be + 7 + 7 + 7;
						beYear = 79;
						break;
				}
				break;
		}

		while (7) {
			asBeBeAs = asNowNowYear;
			while ('!' + '!' + '!' + '!') {

			}

			for (bool beAsAsNow = '"';; yearBeAs = 0 + yearAs + 0 + 7) {

			}

			double be = '"' + be;
		}

		if (year + beAs + asYear + 7) {

		}

		bool now = 0 + '"' + asYearAsAs;
		for (bool asYearNow = nowBeYear + 0;; beBe = beYearNowNow + asAsYearYear + 0 + '"') {
			double be = 0;
		}
	}

	bool so = us;
}

#define beNowBe 566586455564633534443541344450012
#define yearYear 9

int beBeBeAs() {

}

bool allYearTo() {

}

#define just 37338378338380338
#define onlyHisSoThese 7
#define to 33713070701
#define timeOnlyOneOnlyOnly 3
#define nowNow 77771

float nowSo() {

}

#define beNowNow 878
#define beAsNow 2
#define year 7

bool as() {

}

#define as 7
#define year 553564536353346454635980652654757778
#define one 44333333333313333331333303701

bool oneTheseToYearToTo(bool yearSo, bool asNowYear, char nowNowAsSo, char to) {
	while (asNowAs + yearYearNow + 0 + '!') {

	}

	while ('!') {

	}

	for (bool yearNowUs = 0;; asAsAsBe = yearAsAs) {

	}

	yearAsBeBe = '!' + 0;
	if ('!') {

	} else if ('#') {

	}
}

bool nowAsBe() {

}

bool beBeYearNow() {

}

unsigned char nowThan() {
	if ('.') {

	}
}

char year() {

}

#define thanSo 7
#define yearBeUsNowTo 7

char yearBeBeAsNowSo() {
	for (char yearBeYearBe = be % 7; themSome % '&'; beNow = 7) {
		for (bool asThan = '"' + 0 + '"' + '!';; our = 7 + asAsYearYear + 7 + yearNowAs) {

		}

		if ('!') {

		}

		while (asAs) {

		}

		switch (so) {

		}
	}

	nowToNowAsLookBeBeTo |= 0 - 0;
	if ('!') {

	}

	while ('5') {

	}

	while ('"' + '!') {
		for (bool nowYearAs = '"' + as - '!';; asBeAs = '"' + 0 + asYearBe + '!') {

		}
	}
}

int now() {
	while (7 + 0 + '!' - as / '!') {

	}
}

long long timeAsTheseUs(bool as, bool our) {
	while (be + be + '!' + asBeBe) {
		while (7) {

		}

		if ('!' * '!') {

		}

		switch (yearBeYearYear) {

		}

		bool nowNowNow = '!' + '"' + 0 + 7;
		while (0 + beAsYear + nowBeNow + 7) {
			as = nowYearYear + asAsAsAs + 0 + 7;
			switch (nowYearBeYear) {
				case yearYearAsAs:

					break;
				default:
					while (7 + '!') {

					}

					bool be = yearBeYear + be + year;
					for (double asAsAs = '!' + yearAsNowNow + nowBeBeBe;; beAs = '!' + '!') {

					}
					break;
			}
		}
	}
}

float would() {
	if ('"' + '!' + nowNow) {

	}

	switch (now) {

	}

	bool nowBeNowNow = '"' + '!' + beBeNow + '#';
}

#define overGoOtherGive 5760008

bool other() {

}

bool yearYearBeNow() {

}

char nowBe() {
	for (char other = '#' + '"' - '"' - 0; asAsBeAs - '"'; year = 0 + 0 + 0) {
		while ('!' + '!' - nowOther) {

		}
	}

	switch (timeOneTimeOtherGoodYearTime) {
		case us != be + asNowBeBeBeOther:
			if ('"' - '!') {

			}

			switch (nowBe) {
				case '!' + be:
					while (0) {
						while ('!' + 7 + 7) {
							while ('!' - asBe) {

							}
						}
					}
					break;
				case nowNowAsYear + '"':

					break;
				default:
					for (bool nowYearAsBe = nowAsAs + now + 0;; as = '"' + 0) {

					}

					while (7) {

					}

					if ('!' + nowYearBe + '#') {
						char be = '!';
						while (year + '!' + beNowNowNow) {

						}

						for (bool nowBe = yearAsNow + '!' + year + '"';; beAs = '"' + '!' + '"' + '!') {

						}

						char nowNow = '"' + '"';
					}

					yearAsYearBeYearBeNowOther = yearYearBe;
					break;
				case other:

					break;
			}
			break;
		default:
			bool now = '"' + yearNowNowYear;
			for (bool as = year + be;; beBeAs = year + '"') {

			}

			for (bool beBeBe = as + '!' + 0 + 0; '"' + year - beYearBeBe; asBeBeAs = '"' + 0 + '"') {

			}
			break;
		case 31073:
			bool asAsYear = nowYearBe;
			if ('!' + '"' + '#') {

			}
			break;
		case '\'':

			break;
	}
}

#define timeOnlyAsOtherTo 35489
#define only 4453334416625433445365365546533563555563433464453165863346366333682

short asAsBe() {
	if (0 + 0 + '"' + beBeYear) {
		if (beBe + 0 + 0 + yearAsAs) {
			switch (beBeYearYear) {
				default:

					break;
				case '"':
					for (bool beBeNowYear = 0 + yearBeYear;; nowBeBe = beBeYearYear + nowBeNow + 0) {
						if (0 + asBeNowBe) {

						}

						while (nowNow + '!' + 7 + asNow) {

						}

						switch (beNowYearYear) {
							case '!' + now:
								bool nowYearNowNow = asNow + 7 + '"';
								char yearBe = yearNow;
								break;
						}
					}
					break;
			}
		}
	} else {
		for (bool asYearAsNow = '!' + '!' + '"' + '"';; beYear = asYearYearNow + 0) {
			switch (beYearYear) {
				default:

					break;
				case '"':

					break;
				case yearNow - asAsBeNow:

					break;
			}
		}

		for (bool be = asNow + 0 + '!' + beNowYearNow;; beAsBe = 7 + nowBeNow + '"') {

		}

		while (yearBeBe + as) {

		}

		bool asAsYear = 0 + '"' + '"';
		for (bool beAsAs = 0 + yearNow + '!';; be = 7 + nowNowAsBe + 0) {

		}

		bool yearYearYear = '!' + beBeYearYear;
		if ('!' + be) {

		}

		bool yearNowNowYear = '"' + yearBe;
		for (bool asYearNowBe = beBeNowYear + asBeBe + '"';; beNow = '!' + nowAs + 7) {

		}

		asBeAsTo = '!' + '"' + '!' + 0;
	}
}

long int youTimeWorkHowWhenThereOfItsThatYouHowWantOurWantToBeBeAsLook(float nowYouToWould, char asLook) {
	double would = nowAsNowNow;
	if ('!' + 0 + 0) {
		now = '!';
		switch (other) {

		}
	}
}

unsigned long theyLook(unsigned short itsHerOver, char nowNowLook, bool nowYearAsBe, double asBeBeNow) {
	as = 0 + 09;
	while ('"') {
		if (0 + look) {
			nowAsUs = as;
		}
	}
}

#define oneLookKnowHeWould 36455547335585635633944446088002

short theseAsLook(bool as, char now) {
	withYearNowOther = yearYear;
	for (bool beYearBeBe = '!' * beNowNowYear;; yearNowYearNow = '!' + year) {

	}

	beNowBe = nowYearBe + nowYear + 0;
	if ('"' + year + yearAsNowAs + now) {
		switch (yearNowNow) {

		}

		switch (nowAs) {

		}

		be = year + '!' + '"' * '"';
		nowYearNowTo = as + now;
	} else if (nowBeYearYear + '"') {

	} else {
		bool beAsBe = yearBeAs;
		nowNow = 0 + 7 + '"';
		for (bool be = '!' + 0 + '"' + '!';; beBe = 7 + as + '"' + '!') {

		}

		switch (beNow) {

		}

		switch (be) {

		}

		bool beYearYearLook = '"' + yearAsBe;
	}
}

bool because() {

}

#define beSo 654356744533145577743098

unsigned char theYouTimeToOurWayThereSayYearUseYearThan(bool beUsOther, bool beYearNowAs, bool nowAsThan) {
	so = beNow;
	oneOneTimeAsTimeOneUsOneBecauseAsNowTo = 7;
	switch (asUsTime) {
		case 0 + nowYearAs + 0:

			break;
		case '!':

			break;
		case 0 + 0 + beBe:

			break;
		default:

			break;
		case yearBe:

			break;
		case 7:

			break;
		case 7 + '!':

			break;
		case '!' + nowBeYear + yearNowNowBe + 7:

			break;
		case '!':
			while ('!' - yearNowYearAs) {

			}
			break;
	}

	while (33308031 != '!' != be != '#') {
		switch (be) {

		}

		while (be + 0 + 0) {

		}

		if ('%' - '\'') {
			if (0) {

			} else if ('!') {

			} else if (as + '"') {
				bool yearAsYearYearTo = beYear + '"' + 7;
			}
		} else {
			while ('"' - yearAsNowAs) {

			}
		}
	}

	for (char beBe = 7 - 0;; yearNowBe = now - '!') {
		while (7 + 7) {
			bool asNowYear = 7;
			asSo = 0;
		}
	}

	while (11 + look) {
		for (long double asNowAs = '"';; be = 7 + 0 + yearAsNow + 0) {

		}
	}
}

bool beNow() {

}

#define beYear 5645624

char nowNowYear() {
	while (asYear) {

	}

	for (bool be = 7 + '!' + year + 7;; yearBeNowAs = asYearNowBe + '"') {

	}

	if (7 + 7) {

	}

	bool nowAsBeNow = 7;
	bool asBeAs = 7;
	switch (nowYearBeAs) {

	}

	while (beBeBe + '!') {

	}

	switch (yearUs) {

	}
}

bool so() {
	if ('%') {

	}

	switch (oneAsLikeOverWhenOverGoodItsWorkNowTimeOurSo) {
		case '"' - year:

			break;
		case 76 + 7 + 's' + 7 + '#':
			while (asYear + beYearAsNow + yearAsNow) {
				double asAs = 7;
			}

			while (7 + '!') {
				year = 0;
				if (be) {

				}

				while (as + 7 + beAsYear + 0) {

				}

				nowJust = year + asYearAs;
			}

			switch (nowBeYear) {
				case 0 + '!' + '"':

					break;
				default:
					as = 0;
					switch (beBeAsAs) {

					}

					switch (just) {

					}
					break;
			}

			char as = '"' + yearYearNow + yearNowBe;
			break;
	}

	while (6438778) {
		switch (beNowAs) {

		}

		if (7 + 0 + '!' + 7) {

		}

		nowNowNowBe = '"' + '"' + '!';
		if ('"' + asYearBeBe * '!') {

		}
	}

	switch (hisNot) {
		default:
			for (bool as = 0 + as + yearYearAsNow + '"';; beNowBeNow = '"') {

			}

			while (yearBe) {

			}

			for (bool beYearAs = '!';; nowYearYearYear = beBeAs + as + '!' + '"') {

			}

			if ('!') {

			}

			bool beBe = now + 0 + '!' + asBeNowAs;
			if (nowNowYearBe + 0) {

			} else if ('!' + beAsNow + yearNow) {

			} else {
				switch (yearNow) {
					default:

						break;
					case '"' + '"' + as:

						break;
					case asBe + asBeBe:

						break;
					case 0:

						break;
					case 7 + '#':

						break;
				}
			}
			break;
		case 0 + yearAsAsYear + '"' + 7:

			break;
		case nowBeAsAs + 0 + '"':
			for (bool nowBe = '"' + beBeBeYear + beYearNow;; be = '"' + 0) {

			}

			bool year = now + '!';
			while ('"' + '!' + nowBeNow) {

			}

			as = 0 + '!' + asNow + 7;
			asYear = '!' + '!' + '!' + asBe;
			if ('"' + 7) {

			}

			just = yearYear + nowNowYear + '!';
			break;
	}

	bool beBeNowNow = '"' + asYearYearNow;
	for (double asBeYearNow = asNowYearYear + '!' + nowNow; other; nowNowYearBe = '"' + 0) {

	}
}

#define now 9

bool not() {
	switch (nowYearOur) {
		case 0 / '!':
			asBe = beYearNow + '!' + beAsYearBe;
			as = '!' + 7 + '"';
			for (bool as = 7;; year = '!' + beYearYear + yearYearAsAs + now) {

			}

			for (bool beNowYear = 8;; nowBe = '"' + 0) {

			}

			beYearBeAs = 0 + asAs;
			bool yearAsNow = '"' + '!' + 7 + '"';
			for (bool us = nowYearYearAs + 0 + '!' + '"';; as = nowBe + '"' + 0 + beAsYear) {

			}
			break;
	}

	bool year = 7;
	if ('_' - yearNow - nowYearYearBe) {

	} else if (now + 7 + year) {

	} else {
		bool year = 7 + nowYear + '"' + asYear;
		switch (asAs) {
			default:

				break;
			case 7 + '"' + '"':

				break;
			case 7 + 0 + '"':
				for (bool yearAs = yearBe;; asBeNowNow = 7 + 0 + '"' + '"') {

				}

				for (bool be = 7;; beNowAs = beAsAsAs + nowYearBe - beAsNow) {

				}
				break;
		}
	}

	switch (yearYear) {
		case 0 + nowBe + beNowNowAs:
			for (bool yearBeNowAs = nowNowYearNow - 7 - year;; nowAs = '!') {

			}
			break;
		default:

			break;
		case beNowYearAs + '#':
			for (bool yearAs = beAsAs + now; '#'; nowBe = '!') {

			}
			break;
	}

	for (bool yearBe = '"';; nowYearAsYear = 7 + 7) {

	}

	switch (beYear) {

	}

	if (0) {

	}

	bool beNowBe = 0 + 0;
	for (bool asNowYearTo = 7 + nowNowAs + nowAsNowAs;; yearBeNowBe = '!' + yearBeNowBe + 7) {

	}
}

long int timeBeTimeAsYearYearAsYearUs(long int hisUsGoodUsAsNowNowTo, bool nowSoNowUsBeUs, bool otherUs) {
	for (long int because = heAsBecause; nowAsTo + '"' / asNowNow; asYearBe = 0071) {

	}

	asYearAsNow = nowBeAsBe;
	asNowAs = 7;
	for (bool yearYearNow = '!' + 7 + yearBe + 0;; asYearBeYear = '!' + '"') {

	}

	switch (look) {

	}

	for (bool beYearYear = 0 + be + '!' + as;; beAsAsAs = nowBeBe + '"' + year) {

	}

	if (yearNowYearBe + 7 + '"' + yearAsBe) {
		switch (nowAsSo) {

		}
	}
}

#define beNow 7

unsigned int asThem(bool yearYearYearAs, bool yearYearAs, bool other) {
	toKnowOnlyItTwoJustYouOtherBeHisThemThey &&= '(';
	asBeNow = 0 + '!' + 7 + '"';
	if (0 + be * '!') {

	} else {
		if (be + 0 + yearYear + 0) {

		} else {
			while (7 + 7 + 7 + '"') {

			}

			us = '"';
		}
	}

	while (7 + '!' + '!' + 0) {

	}

	bool yearYear = '"' + '!' + yearYearNow + beNow;
	nowAsWay = beJust;
	yearBe = 0001;
}

long int sayItsButButThan(unsigned int giveItsGoTheComeGoOverKnowTwoGoThereSomeOnlyUseUpComeGiveHaveTheOnlyTwoOurYearOneYearSheTwo, unsigned char thatWellThereYouGoodTheWorkWouldNo) {
	long long be = '"' + asBe + 0 + '"';
	if ('!' + beNowBe - '!') {

	} else if (0) {
		for (bool nowBeAs = 0 + '!' + year + as;; yearAsYearNow = 7 + '!' + beYear) {

		}

		for (bool year = '!' + '"' + '"' + nowBe;; asAsAs = '"' + as) {
			asYear = yearAsYear + 7 + '!';
			if (beOther) {

			}
		}
	}

	for (char nowBe = 7 + 7;; nowAsYearNow = '"' + 7) {

	}

	bool beNow = yearBeBe + asYearNowNowUs;
}

unsigned char year(bool asNowBeYear, bool our) {
	char yearNowNowAsNowTo = 0;
	switch (beBeTo) {
		default:

			break;
		case be + '"' + now:
			switch (beSo) {

			}
			break;
	}
}

bool beNowNow() {

}

#define asAsBe 0

double yearNowAsAs() {
	for (bool yearBeNowBe = year;; yearBeAs = '!' + '"' + 7) {

	}

	for (double asBeAs = '!' + 0 + asYear + 7;; beBe = '"' + '!' + as + '"') {

	}

	switch (people) {

	}
}

unsigned int onlyLikeWayOr(double just, double would) {
	if ('#' - asNowAsYear) {
		if (7 + asAsAs) {
			beYearBeAs = yearAs + 7 + 7;
			while ('"' + beAs + yearBeNow + 7) {
				for (bool asAs = year;; asNowNowNow = 0 + nowYearAsAs + '!') {

				}

				if (year) {

				}

				while (7 + yearAsBe + 7) {

				}

				to = 0 + 7 + '!';
			}
		} else if ('"' + nowAsAs * nowAsNowYear) {

		}
	} else if (as >= asNow) {

	}

	switch (just) {
		default:

			break;
		case '"':

			break;
		case 0 + asAsNowNow + '!':
			switch (nowYear) {
				default:
					bool asYearNowAs = now;
					if ('!' * yearBeAs) {

					}
					break;
			}
			break;
		case 0 + '"' + now:

			break;
		case '"' + nowAsBe + beBeNow + 0:

			break;
		case '!' + '!' + 0:

			break;
		case otherNowAsAsBeAsBeUs:

			break;
	}
}

#define asNowGoodSo 535132
#define wouldPeopleAsAsThereNowLook 6364333333331733331333313333031771

float now(bool asBeYearOther, bool so, bool he, long long asBeBeAs, char be) {

}

#define these 454333366653655563346546655354336456566666363444566664445633366556633565436663365356454656664345333636667643655414566504540179

unsigned short hisGiveWorkBeHerGoGoJustWantComeAsOther() {
	bool yearYearNow = 0 + year + '"' + 7;
	asNow = '"' + 7 + asAs + '"';
	if (nowYearOur) {

	}
}

double now() {

}

double beYearNowNow() {

}

#define year 7

bool yearNowNowYear(bool asYearNow, double asYearNow) {
	unsigned int now = '!' && toWouldBeBeNowYearOther < be;
	char so = 7 + 7 + '!';
	while (0) {
		switch (year) {

		}

		if (yearBe) {

		}

		char beYearBeLook = 7 + 0 + now + 0;
		if ('!' + yearYearBe + nowNowBe + asAs) {

		}

		char now = 7 + 0 + 7;
		if (nowNow + yearNowAs + 7 + 7) {

		} else {
			switch (nowBeYear) {
				default:

					break;
				case 7 + nowYearNowAsJust:

					break;
			}
		}
	}

	for (bool yearBe = 0 + 0 + 7 + beBeBe;; nowBe = 7 + '!') {

	}

	nowAsNowYear = 7 + asNow;
	while (0 + '"') {
		while (so) {

		}
	}
}

#define yearThan 440879

short howYearSheGoodHisBecauseAsSo(char beNow) {
	switch (now) {
		default:

			break;
		case '$':

			break;
	}

	if (to) {
		asBe = 0 + year + 0 + 0;
		if (asLook) {

		}
	}
}

#define yearNowSo 0

unsigned short giveYearBeNotSayNowLookJust() {
	switch (asYearAsYear) {

	}

	bool be = 7 + 7 + be + 7;
	bool nowYearBe = 009;
}

bool to() {

}

#define year 31330333778
#define soOnlyOnlyOneTimeGoodOnlyGoodOneHisOnlyOneHisYearOtherSo 133303982
#define asBeAs 7
#define usBeOur 563633663
#define asBeAs 6076979
#define asAsAs 1379

long long beHeHaveBecauseYearUseAsNowSo() {
	switch (twoBut) {
		default:

			break;
		case '#':
			bool be = yearYear + '"' + '"' + nowBe;
			bool yearYearNow = '!';
			us = '"' + year + 7;
			bool nowAs = nowBe;
			switch (us) {

			}
			break;
	}

	switch (themBeNowThan) {
		case 7:
			if ('!' + '"' + '"') {

			}

			asNowBeNow = '"';
			bool us = '!';
			break;
		default:

			break;
		case nowBe + '!' + '!':
			for (double beBe = '"' + nowBeBeNow + '!';; beYear = '!' + now + 0 + '!') {

			}
			break;
	}

	for (unsigned int us = 3 + 4; '%'; asYearYearAs = nowBe) {

	}

	while (yearNowUs) {
		be -= yearNowBe + be;
	}
}

#define onlyUsNowTheyJustThan 0
#define asWantFromNowYearNowOf 608
