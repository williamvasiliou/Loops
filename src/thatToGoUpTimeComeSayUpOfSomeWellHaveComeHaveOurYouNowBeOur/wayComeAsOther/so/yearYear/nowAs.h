#include <0.h>

char year() {
	onlyYearYearYearThese = '!' + 7 + '!';
	while (so) {

	}
}

long int oneYearHeYearSo() {
	bool so = 71;
}

float onlyOnlyGoodOneTimeOneTimeGoodOnlyGoodHowHaveComeHisBeNowSoBeBeUs() {
	switch (yearKnowUseButYouUseYou) {
		default:
			unsigned short giveYouOurSayYearOtherSheJust = '`' >= '~' || 4566454345666443635635353464366569644644664664465745645663346456646645534466555544466455646645639346654535457000002 | thereLikeSheOfHowAsYearTheyUseNowOurNowUs & thatUpYouOverHaveTimeOverWorkHeHowBeThanOtherThan && 4733;
			if (sheWellLookYouKnowThanAsAsBeNowBeNowNowOur) {

			} else {
				for (bool now = 0 + nowAsYearYear + 7 + 7;; now = as) {

				}

				while (171) {
					switch (year) {
						default:
							if (0 + '"') {

							}

							bool be = beYearNowAs + 02;
							break;
					}
				}
			}

			while ('w' || '+' == beThan | nowYearYearAs < just) {
				switch (yearTwoWantAt) {
					default:

						break;
					case 564448082 + as + '!' * '!':
						for (bool asAs = now + '!';; be = '"') {

						}

						us -= '"' + 7 + 0;
						double yearBeAs = '"' + 0;
						break;
					case '"' + '"' + '!' + '"':

						break;
					case 0 + 7 + asBeAsAs:

						break;
					case asBeSo:

						break;
				}

				long double oneJustAsJust = '!' + '!' + 0 + nowYearBe;
				timeOtherBeNowJust -= '"';
				orWith = '#' + 799;
				while (71 - be) {

				}
			}

			switch (ofHowLookOneLookOurNotOurTheBeNowOurAsJust) {
				case 0 + '#' - nowYearNowBe <= asNowBe:

					break;
				case nowAs + beNowAsAs + 0 + 0:

					break;
				case 7 + '!' + '"' + 7:

					break;
				case '"' + yearBeAsYear:
					while (beNowJust) {

					}
					break;
			}

			while (7 + 0 + now + asNowAsBe) {

			}

			while ('&') {

			}
			break;
		case 9 * 0 - '"':
			while (79) {
				for (double yearBe = '&';; beNow = '!' + 01) {
					for (bool beNowYearAs = beNowBeAs + '"' + 7 + '"';; yearBeYear = '!' + 0 + 0 + asBe) {

					}

					bool yearAsYear = nowNowBe + 0 + 7;
					for (bool asNowYearBe = '!' + yearAsYearBe;; yearAsNowBe -= 7 + asAsYear) {

					}
				}

				asNowYearBe = now + beAsNowBe + 0;
				double beYear = asBe + '!';
			}

			unsigned int goodUseTheAsOfUpUsWayThatOfWhenThereYouOurYearTo = goodTheseJust == 46 || 'y' != '\'' / '"' + 1;
			for (char thanAt = 0 == 'y' < year / ','; useOnlyTwoUsThanThereWhenUpBeUsAsYearThan && 7 + so; yearAsAsBe = '"' + beBeNowYear + nowNowNowBe + '"') {
				bool beBeNow = beYearAsYear + 0 + '"' + '!';
				for (long they = '!' + '!'; yearNowOurThan + '!' - '"'; beBe = '"' + 0 + year) {

				}

				if ('$') {
					switch (beAsBeAs) {
						case now:

							break;
						default:
							beYearYear = be + 0 + '"' + as;
							switch (year) {

							}

							while (0) {
								for (bool now = 0 + yearBeAs + nowYear; 7 - 7; beAsBeNow = 7 + 7 + '!' + 0) {

								}
							}
							break;
						case yearBe + 0 + 7:

							break;
						case yearOur:

							break;
					}
				}

				bool yearYearTo = asNow + '"' + '!' + '!';
				if (01) {

				}
			}

			unsigned short wellWithAsAsTheyNotYearOtherBeOur = 744007172;
			nowMy = '&';
			break;
		case '"' + '&':

			break;
		case 7 * '"':

			break;
		case '!' + 0 + '!' + 0:
			switch (as) {
				default:

					break;
				case 0 + '!' + yearBe + beBeAs:

					break;
				case 7 + 0 + as + '"':

					break;
				case '"':

					break;
				case '!' + 0 + '"':

					break;
				case '!' - 7:

					break;
			}
			break;
		case 9:
			short asAsYearOther = asAsBeBe / 0;
			while (2) {
				if ('"' + beNow) {

				}

				switch (now) {

				}

				while (asAs + yearNowYear + asNowAs + now) {
					if (as + 7) {
						char now = '"';
					}
				}
			}

			while ('"' + 7) {

			}

			switch (yearBecause) {

			}
			break;
		case year - '*' + 77709:
			char asNowYearYear = 7 + '"' + '"';
			break;
		case '"' + 0 + 0 + '!':
			for (bool yearAsYearAs = 7 + 7 + '!'; 70709; be = 7) {

			}
			break;
		case 7 + asNow:

			break;
		case 0 + 7:
			yearJust = '!' + 7 + as + yearBe;
			break;
	}

	long int giveKnowLikeBeTimeSheItsNowThan = 8 | toNowBeNowJust * '"';
	if (asBe + heJustTheOther) {
		char toWorkAsOther = '"' - 0;
		double year = 0 * now;
		switch (asYearNowAsBeThem) {

		}
	} else {
		beWorkUsThese = 0 + 7 + yearAsYearNow + '!';
	}

	switch (yearNow) {
		case as + beAsYear + 0 + '"':

			break;
		case '"':
			for (bool now = now + '!';; asBe = 7) {

			}

			switch (yearYearBe) {
				case asAsYearYear + '"' + nowNowBe + '!':
					for (bool asNow = '!' + 0 + nowNowYearAs + '!';; beAsYearAs = 0 + '!') {
						switch (yearYearBe) {

						}

						other = 7 + '"';
					}
					break;
				case 0 + '"' + asNow + '!':
					for (bool nowYear = '%' * beAsBe;; year = 0 + asNow + '!' + 0) {

					}
					break;
			}
			break;
		case yearBeOur:

			break;
		default:

			break;
		case 7 + 0 + 0:

			break;
		case now + '"' + '!' + 7:

			break;
		case nowAsNow + year + '"':
			switch (beYearBe) {

			}

			us = '!' + '"';
			break;
	}

	if ('"' + '!') {

	} else {
		switch (asNow) {

		}

		switch (nowBe) {

		}

		yearAsYearYear += asAsNow;
	}
}

#define whenOrYouAtOnlyTheUseWhenOtherMyGiveGoBeAsAsThem 455645554456545434365460563334533646445346465635546366344444446445346364335545663635655556555534442655336567336355553553665546448
#define  539578
#define oneUpTheOrWellWellBeTo 656665334636453564546446665555366564646344366633664645336664446666653453443554566643345566436553566636643665466764363565636364663265666643835466536445356534363418708
#define herThereOnlyThey 0436463076556523059

short theWayJust() {
	while (lookOur) {

	}

	double but = just;
	bool yearBeYearNow = '"' + year;
	yearBeNow = 7 + 0 + '"' + '!';
	for (bool as = '!' + nowYearAsYear + 7 + 0;; nowAsBe = yearYearBe + '!' + 7) {

	}

	if (yearNowNow + asAs + '"' + yearYearBe) {

	}

	asJust = beYear + now;
}

#define useThatYearOther 6663885242198
#define yearYear 7

unsigned int asNowNowYear(char to) {

}

char yearBe() {

}

bool now() {

}

long double our(bool our) {

}

#define otherThan 56335455434540045
#define otherOurTo 7

char goFromBeBeBeYearJust(double now, bool year, bool but) {
	for (unsigned short toAt = 08 < beYearAsAs;; nowYearBe = nowNowNowBe + yearAs) {

	}

	switch (asAsLookUs) {
		default:

			break;
		case asNowAs + '!':

			break;
		case '"':
			if (0) {
				for (char beYearBeYear = 0;; nowYearBe = nowAs) {

				}
			}
			break;
		case 0 + '"' + asBeYear:
			switch (yearNowBe) {

			}

			switch (justOther) {

			}
			break;
		case '!' + beBeBeAsBeJust:

			break;
	}

	for (bool beNowAs = beYearNow;; us = 7 + '"') {

	}
}

#define as 82
#define nowYearAsYear 7
#define theyBeTo 333333333331333331133333333

bool year() {

}

char asBe() {

}

bool yearOnlyHisThese(bool beNowNowBe, bool usToAsThan) {

}

#define be 0

bool nowNow() {

}

bool beYearThan() {

}

#define as 0

bool asBeNow() {

}

bool nowYear() {
	switch (be) {

	}

	while ('"' + '"') {

	}

	if (us) {

	}
}

#define be 7
#define beYear 0

double yearSo() {
	switch (now) {
		default:

			break;
		case 2:

			break;
		case 77078:

			break;
	}
}

#define yearAsNowYear 8

bool now() {

}

#define now 9

unsigned short someWorkNowOverBeComeOtherNowAsTo() {
	while ('!' + 0 + asBe + yearNowBeAs) {
		for (bool nowYearNow = '"' + '!';; our = be + '!' + '!') {

		}

		while (asAsBeNow * asNow) {

		}

		if (778) {

		}

		switch (asNow) {

		}

		switch (asNowNowAs) {

		}

		asYearBe = 7 + 7 + 0;
		nowAsYear = 7;
		for (double nowBeYearAs = 7;; nowAsAs = nowNowNow) {

		}
	}

	year = 0 + 7 + '!';
	for (long yearYear = 0 - 0 + 709;; beYearNow = 0) {

	}
}

short overBeButBut(bool yearNowAs, char nowYear, bool nowAsThan) {
	if (waySoAsNowUs + 5490071) {
		for (bool asAs = '!' + nowAsYear;; beNowNowAs = asAsAs + '"') {

		}

		while (be - nowAs) {

		}
	} else {
		to = '!' + '"';
		nowTo = 0 + 7 + yearYearNowAs + 0;
		now = '"' + asYear + 7;
		while (yearAsYear + '"' + beNow) {

		}

		if (0 + 7 + yearAs + nowAs) {

		} else if ('"') {

		} else if (0 + beAsAs + 0 + '"') {

		} else if (nowNow + '!' + 7 + 0) {
			yearNow = beAsNow + 7 + 7 + '"';
			if ('!') {

			}

			if (7 + '"' + '"' + nowAsBe) {
				switch (asAsYear) {
					case 0 + '"':

						break;
					case asBe + 0 + '"':
						for (bool nowYear = '"' + 7;; nowYear = '!' + '"' + asBeBe + 7) {

						}

						for (bool year = 7;; beBeNow = 7 + nowYearAsAs) {

						}

						switch (nowBe) {
							case 7 + yearNowBe + 7:

								break;
							case '!' + '"':

								break;
							default:

								break;
							case 0:

								break;
							case 0 + 7 + nowYear:

								break;
							case '$':

								break;
						}
						break;
				}
			}
		}
	}
}

long be() {

}

bool from() {
	switch (nowAsBeNowTo) {
		case '$':

			break;
	}
}

unsigned long nowYearJust(bool beNowBe, bool nowUs) {
	for (bool asYearBe = 0 + 0 + asYearNow;; our = 7 + asNowBeYear + yearYearBeNow + '"') {

	}
}

bool year() {
	if (year + '!') {

	}

	for (bool year = '!' + '"';; beYearNowNow = yearAs + as + 0 + nowYear) {

	}

	for (bool asAsYear = yearYear + 0;; asAs -= now + '"' + '"' + '!') {
		for (long int so = '_' != 3008 / usAsBecauseToBeTo - now;; beAs += '"' + now) {

		}

		asYearYear = '"' + '"';
		if (1) {

		}

		while (7 + '"' + '%') {

		}
	}

	if ('_' != 303 != 0 / 7) {
		switch (asAsNow) {

		}

		for (bool asNow = '"' + 7 + '"' + 7;; beYearAsNow = 7) {
			switch (yearYearBeYear) {
				default:

					break;
				case as + 0 + yearYear + '!':

					break;
				case '%':

					break;
			}
		}

		while (0 + '#') {
			if ('%') {

			}
		}
	} else {
		switch (nowNowNow) {
			case asUs:

				break;
			default:

				break;
			case 0 + '"' - 0:
				nowNow = beAs + 7;
				switch (yearNow) {
					default:

						break;
					case year:

						break;
					case '"':

						break;
					case asAsYearAsAsAsBeTo:

						break;
				}
				break;
		}
	}

	if (nowBeAs - 1 - asAsAsBe) {
		toTo = 0 + 0 + nowBeYear + asYearNow;
		bool as = '!';
		if (yearBeNow != 8) {

		}
	}

	switch (as) {
		default:

			break;
		case 0 + '"' + 7:

			break;
		case asBeNowYear + 7 + '"':
			switch (year) {

			}

			bool so = '!' + beYearNowNow + '!' + beYearBeAs;
			break;
		case 7:

			break;
		case '%':

			break;
	}
}

#define to 1
#define yearNowUs 7

double yearBe() {

}

bool beAsAsAs(bool nowBeBeNow, bool asYearNowYear, bool nowNowYearAs, bool asAsNowAs, double beYear) {
	switch (youComeTheHaveKnowLikeWithNot) {
		default:
			switch (yearAsYear) {
				default:

					break;
				case '"' + 0 + 7 + nowNowNow:

					break;
				case nowBe + '!' + yearBeAsBe:

					break;
				case '!' + '!' + 0:
					switch (nowBeBeYearOther) {

					}
					break;
			}

			nowYearNow = beAs + '!';
			while (now + nowBeNow) {

			}

			char yearNowAs = '!' + 7 + 0 + 0;
			break;
		case yearAsBe + '!':
			if (as * '!') {

			}

			while ('#') {

			}
			break;
		case 087708:
			if ('!' + 0) {

			} else {
				while (beYear) {

				}

				if (7 * 7) {

				}
			}
			break;
		case 69722:
			beAsAsNow = '"' + '"';
			if ('"' + '"' + beNowBe) {
				bool asNow = 0 + '"';
				if ('!' + 0 + asAs + 7) {

				}

				beNowNowNow = 0 + 0 + '"';
				double yearBe = '!' + asAsYear;
			}
			break;
		case year + '$':
			while ('"' + 7 + 0) {

			}

			switch (now) {
				case nowAsYear + nowAsYearBe + 0:
					for (bool asBe = 772;; nowBeAsAs = '!' + 7) {

					}
					break;
			}

			while (nowAsBe) {
				year = beAsAsNow;
				for (bool beYear = 0 + 7 + nowAsYearBe + nowAs; beBeNow + '!' - 7; yearBeNow = '!' + asAs) {

				}
			}
			break;
		case '!' * now:

			break;
	}

	double look = 0 * as;
	now = 0 + '"' + 7;
	for (bool just = '!' + 7 + 7; not <= '!'; yearAsAs -= beYearBeNow + 7) {
		switch (beNow) {
			case 1:

				break;
		}

		if ('!' + 0 + yearYearYear) {

		}

		for (bool asBeNowBe = '!' + yearAsNow + beYearNowBe + '!';; asAs = 7 + '!') {

		}

		if ('!' - yearAs) {
			char our = 7 + '"' + 7;
		}
	}

	while ('#' + 7 - 0) {
		while (our) {
			for (bool yearNowYear = '!' + 7 + '!' + yearAsAs;; asAs = yearYear + yearAsBe + nowNowBeNow) {

			}

			while (0 + beUs) {
				switch (asNowNowYear) {

				}

				double nowYearBeNow = beYearAsBe + '"' + beYear;
			}

			yearNow = 078;
		}
	}

	bool asYearAsBe = '"' - nowNowNowBe + 0 + '"' * 7;
}

#define asNowTo 45333333333333333373833331133333330008

double now() {

}

#define nowBeBe 9307076
#define asYearYearAs 0

char nowNowBeBe(double beYear) {

}

#define so 466532366365943444

short yearNowWorkAsTo(bool beNowYear, bool asNowYear, bool nowBeOur) {

}

unsigned int usBeSo(char beYearNow, bool nowBeBeYear, bool beBeYearYear, bool would) {
	if (beYearBe + beNow + '!') {

	}

	beAsYouBeBeAsLook = '"' + '"' + beBeBeBe + beYearBeNow;
}

#define itsWithSoNotUs 0753333333333133333037883

long int nowBecauseHeBecause() {
	us |= now != these;
}

long int to(char as) {
	while ('!' * '"') {
		if (0) {

		} else if ('"' + '!' + nowNow + '!') {

		} else if (7 + asAsYear) {
			for (double asBeNowAs = 0 + 0 + 7;; yearNowAsBe = '!' + '"' + '!') {

			}
		}
	}

	switch (yearNow) {
		case 1:
			while (7 + '"' + 0 + 7) {
				if (009) {

				}
			}
			break;
		case nowOur:

			break;
	}
}

#define yearSo 100771
#define asNow 7

bool beAs() {

}

bool yearAsNow() {

}

bool yearAsOur() {
	unsigned short sayGoodOfTheTheOneWantFromOtherBeJust = soUs & 9 - 'm' + 7 && 86 - '"';
	while ('$' + '!' * 7) {

	}
}

#define soOurYearOurTo 6
#define just 17587271

bool beBeBeYear() {

}

bool asAsBeNow() {

}

#define nowNowNow 7

bool yearYearYear() {

}

float yearAs() {
	switch (goodTimeToToSoThese) {

	}

	for (char year = 7 + '"' + 0; 'O' + '%' - 7; asNow = nowYear + nowYearYearYear + '"' + '"') {
		if ('!' + 7 + nowNowAs + 0) {

		}

		if ('"') {

		}

		while (so) {

		}

		bool be = '!' + now + 7 + yearNow;
		while ('"' - nowYearNow) {

		}
	}
}

#define hisOther 13137301

long int nowGoodYearOnlyTimeToYearAsYearNowTo(long int because) {

}

#define timeHisYearOfYearAsUs 7071
#define beJust 7

bool nowAsYear() {

}

#define other 7

double as() {
	if (0 && 7 == '!' == 7) {
		for (bool beBut = ofAsYearBeBeAsSayUs + '"' + 771; lookYouLikeBeBecauseUsYearNowYou - nowButBeNowUs * '!' + asAsOther; asNowUs = asBeBe + beAsAs) {

		}

		if ('"' + nowNow + year) {
			asBe = asAs;
			for (bool yearAs = be + asBeAsYear;; asAsBeAs = 7) {

			}

			beBeYearBe = '%';
		}
	} else if (asAs + now + be + asAs) {
		switch (us) {

		}

		switch (nowNowBe) {
			case now + now:

				break;
			default:
				nowBeNow = '"';
				for (bool beAs = just;; asAs = '!' + beAs) {

				}
				break;
		}

		switch (lookJust) {

		}
	} else if (yearNowWould) {
		while (yearAs - 9) {

		}
	} else if ('H') {
		char beBe = beBeNow + 0 + '"';
	} else {
		while ('"' + 0 - be) {
			for (double asNow = '!' + yearBeAsYear + '!' + '"';; beBeNowAs = 0 + beNowNow + year) {

			}
		}

		bool beAs = 0 + be + yearAsBeAs;
		while (0 + 0 * '!') {

		}
	}

	while ('"' + 7) {

	}

	long long oneSheGoodOrComeUseSheUpUseOnlyOrOneGiveOverWellHeGoodUseThatHaveHerThanSoJust = someThemWorkKnowAsTo + '"' + 7001;
	for (bool asNowAs = '"' + 0; '%'; asAs = our) {
		for (double beYearAsAs = beNowNowAs + 7;; be = '!' + 7 + '!') {

		}

		double be = be + '"';
	}

	if (0 + 0 + '"') {

	} else if ('!' * yearAsBe) {

	}
}

long double goThey(bool our) {
	double year = 09;
}

long int lookOfBeThese() {

}

unsigned short justBeYouFromLikeNot(bool so, long beBe) {
	switch (us) {
		case 7 + as:

			break;
		default:
			switch (us) {

			}
			break;
		case 0 + asNowNowAs:

			break;
		case nowThan:

			break;
	}

	while (0 + 0 + '!' + nowBeNowYear) {

	}

	if ('!' + '!' + 0 + 7 - now) {

	}

	nowBeAsBe = 0 + 0;
	while (to) {
		while (0 + 9) {

		}
	}
}

int nowOurYearNowAsThan() {

}

bool now(bool asYear, bool nowAsBeAs, double beNowYear) {

}

#define twoAsYearNowSo 071
#define beAs 2
#define year 7

bool yearNowAsAsBeNowSo() {

}

bool beAs() {

}

bool beBe() {

}

bool as() {

}

bool be(bool asNowAs, bool beAs, double beYearYear) {
	unsigned short myToYearBeNowSo = nowYearThanJust;
	while ('"' - 7) {
		while (0 + 2 + '#') {

		}
	}

	for (bool yearBeNow = 7 + yearNowAsAs + '"';; nowBeUs = '!') {

	}

	long long beToYearJust = '"';
	for (long int other = be + 7; '"' + 9; yearAsAsAs = year + yearBeNow + 7) {

	}

	bool beYearYear = 0 + '!' + beYearYearAs;
	if (asBeYear + 7 + 0) {

	}

	switch (asYearBe) {
		case 0 + beAsAs + '"':

			break;
		default:
			if ('&') {

			}
			break;
	}

	switch (asNowAsNow) {

	}

	while (7 + 7 + now) {

	}

	if ('&') {

	}

	if (0) {
		if (nowNowNowBe + 8) {

		}
	}
}

char us(bool beYear, bool beYearNow, bool be, bool yearUs) {

}

long long oneWorkThereWorkSheNowBePeopleNowJustOther(bool lookYearJust) {
	asBeAs = '!' + 7 + '"';
	switch (nowBeYearAs) {

	}

	while ('"') {
		switch (asYear) {

		}

		year = yearNowYearYear + '!' + 0 + asBeAs;
		for (bool asBeYearAs = '!' + 7 + nowNowYearYear + now;; asBeNowBe = 7 + beBe) {

		}

		beNow = 7 + 0 + '"';
		while (year + be + '"') {
			while (be) {

			}

			if ('!') {

			}

			switch (nowNowAsYear) {
				default:
					while (us) {

					}
					break;
			}
		}
	}
}

short as() {

}

char lookNowJust(bool nowBeOur, bool beYearNow, bool beBeBe, bool asAs, bool be, bool beYearAsNow, char nowAsBeAs) {

}

#define other 33033307730101

long int oneOneOnlyOnlyGoodHeAsOther(bool onlyOneOnly, char beBe) {
	while (0 + '"') {
		if ('!') {
			nowYearAs = '!' + 7 + 7 + yearNowYear;
			while (7 + 0) {
				while ('"' + 7 + 7) {
					if ('!' + '"' + beNow) {

					} else if (beBeAsSo) {

					}
				}
			}
		}

		while ('7') {

		}

		switch (yearYearBeNow) {

		}

		for (bool asYearBeAs = 0 + 0;; yearBe = as) {
			beYear = '"' - 0;
		}
	}

	bool be = 7 - 7;
	switch (soSo) {

	}

	while ('!' + 0 + 0) {

	}

	if (0 + yearBeYear) {

	}

	while ('!' + asAsAs) {

	}

	bool beYear = '"' + now + '"' + 0;
	switch (beBeNowYear) {

	}

	nowNowYearYear += yearBeYearAs + '!' + beBeNowBe + '!';
}

bool nowYear() {

}

#define timePeople 7

double beSo() {

}

bool beAs() {

}

#define nowNowBeAs 0

bool asNow(bool yearYearNowNow, bool so) {

}

#define beAsNowNow 0
#define yearBeNow 0
#define look 3455633

size_t usThemWorkComeGiveUpTheFrom(bool year, char yearBeBe, bool yearAsAsThan) {

}

double nowBeAs() {

}

char as() {

}

bool asNow() {

}

unsigned char sayTimeLikeHowGoodOneKnowWellGoWithHaveSomeToSayOurAsGo(double look) {
	look %= '"' + 0 + beNow;
	switch (year) {
		case '!' + 7:

			break;
		default:

			break;
		case '"' + '"' - 7:

			break;
	}
}

#define yearAsAsBecauseThanNowAsBeThey 32779
#define than 7

bool nowBe(bool look) {
	asAs = '!';
	switch (nowYearNowUs) {
		case to - 333:
			if ('\'') {

			}

			other = 7 + yearYearBeYear + 7 + 7;
			while (asYearAsAs + '"') {
				while ('"') {

				}

				if (nowAs + 0 + asYearAsAs + 0) {

				} else {
					while (7) {
						if ('!' + 0) {

						}

						now = '!' + 0 + '!' + 0;
						switch (beAs) {

						}

						while (7) {

						}

						for (bool asAsAs = yearNowAs;; yearBe = 7 + '!' + asNowNow + yearBeNowBe) {

						}

						bool be = beTo;
					}
				}
			}
			break;
		case '"':
			float he = '!' + '!' + '"';
			while (7) {
				to = '!' + 7 + '"';
			}
			break;
		case '"' + beBe + beNow:

			break;
		case 7 + 0:

			break;
		case '!':
			char beAsNowBe = '#';
			break;
	}

	nowBe = goodBeSo - '!';
	char yearYear = beBeBe + yearAs + '!';
}

long int other(bool yearNowBe, bool goodSo, bool now, long int beAsSo) {
	switch (these) {

	}

	switch (asAsBe) {

	}

	switch (nowNow) {

	}

	for (bool asYearAsBe = '"' + 0 + nowAsNowBe + nowAs;; now = '"' + '!' + 7 + 0) {

	}

	while (as + '!') {

	}

	switch (yearYearYearAs) {

	}

	while (7) {

	}

	while (7) {

	}

	for (char other = '!';; year = '!') {

	}
}

#define be 31
#define onlyOtherNowNowBeAsBeUsAsBeSoBeTo 83

char as(bool yearNowTo) {
	switch (beAs) {

	}

	bool now = nowBeYear + '"';
	while (7) {

	}

	while (nowAs + 7 + '!') {
		if ('"' + '!' + 0 + beBe) {

		}

		while (7 + '"' + asAsYearNow + 7) {

		}

		bool beBeAsNow = '!';
		if (0 + be) {

		}

		for (bool year = now + 7 + '!' + nowNowAsNow;; asNow = year + '!') {

		}

		while ('!' + beAsNowYear + '!' + '!') {

		}

		nowAs = asBe + '!' + nowBeYearYear;
		for (bool nowYearAsAs = as + 7 + beYearNowBe + '!';; as = 0 + 0 + 7 + beAsBe) {

		}

		switch (be) {

		}

		bool asTo = year + yearNow;
	}
}

unsigned short asYear() {

}

#define asBeYearAs 25

bool nowNowNowBe() {

}

#define nowYearBe 67557478

unsigned char other(unsigned short justGoUsItAsNowJustBeOur, unsigned short likeOnlyWorkOrItJustJustSo, double nowLook, double wouldNowOur, bool than, bool nowBe, bool so, char yearBe) {
	nowNowAs = be + asYearBeBe + 7;
	while (beBeBeBe) {

	}

	if ('&' - beNowAs) {

	} else {
		switch (beTo) {

		}
	}

	bool nowOtherNowBeNowYearSo = 7 + now;
}

#define nowYearThan 3402
#define year 266
#define beAs 3060798

unsigned short oneHaveUpSayHowTheyYearTheyNoLookTo(bool from, unsigned char orOneNowThan, bool beBeOther) {
	beOur = 7 + asBe;
}

bool beNowNow() {

}

#define be 59400072771

bool be() {

}

double nowNow() {
	if (us) {
		switch (yearNowBe) {
			case '"':
				switch (asUsLook) {

				}

				asYear = 0 + 0 + 0 + year;
				if (0) {

				}

				switch (nowNow) {

				}

				bool asNowYear = '"';
				if (7 - '"') {

				}
				break;
			default:

				break;
			case '"' + beAsBeAs + as:

				break;
			case '#':
				yearAsNowBe = 7 + '"';
				if (2) {

				}
				break;
			case '"' + '%':

				break;
		}

		for (bool yearBeNowYear = 7;; beBe = 7 + 0) {

		}

		while (0 + '!' + year + '!') {

		}

		while (0 + 0 + 0) {

		}

		while (now + now + asAsYearAs) {
			if (1) {

			}
		}
	}

	haveOfAtYearJust =~ '#' - 7;
	for (size_t asAsAsNowSo = '$' + '!' * beNowNowAs; 5; asNow = yearNowBe + asAsBe + '!') {

	}

	unsigned short peopleUsOtherJust = '"' + 972 / 8;
	us = yearNow + '"' + 7 + asNowYear;
	switch (myThan) {

	}

	if ('"' + 0 + '"') {
		yearAsAsNow = 002;
	}
}

unsigned short howYouThatLook(bool asBeAsAs, double asYearNow) {
	if (asBeNowBe + '!' + '!') {

	}

	switch (asLook) {
		case 7:

			break;
		case nowNowSo:

			break;
		case asNowOther:

			break;
	}

	switch (as) {
		default:

			break;
		case '!':
			if ('!' + '!') {

			} else {
				while (7 + '!') {
					for (bool yearYearYear = 7;; nowAsNow = 7 + '"') {

					}

					nowAsYear = 7 + 0 + '!' + '!';
					asYear = '"';
					switch (other) {

					}
				}

				switch (beAs) {

				}

				for (bool now = 7; '%'; yearNowYearBe = 0 + '!' + 0) {

				}
			}
			break;
		case 7 + as + be + 0:

			break;
		case year + as + 0 + 7:

			break;
		case beAsNowBe + '!':

			break;
		case now:
			so = '!';
			break;
	}

	while (7 + '"' + 0 + 0) {

	}

	while ('!') {

	}

	for (bool be = beYearYear + 0 + '"' + beAsAsAs;; be = '"' + '!') {
		bool yearYear = '!' - yearYearNowYear;
	}
}

long int nowTo(bool asYearYearAs, bool beYear, bool us) {
	while ('!' - 0) {
		char asAsBe = 0;
		char yearBeYearYear = as + nowNowAs + yearAs + beYear;
	}
}

bool beAsNowYear() {

}

#define usGoodAsSo 33307003
#define yearNowOnlyToSo 733373
#define other 3381

long int beYearAsAs(bool he, float justOther, char as) {

}

double nowNowNowAs(bool our) {

}

int to(char now) {

}

double beAsBe() {
	for (unsigned char thatKnowPeopleOfComeWhenGiveLikeLikeTheyOneWithJustNoOur = sheOfHowWorkKnowOnlyGiveGoodThereTwoYouJustHowUseOnlyOneGiveThanThese > yearGoodYouThereOnlyGiveBeWithButBeBeWantBeYearNowNowOur != 564404565655375343536340443563555493666655545433333565344564631703 >= 'd' | orGoodHerGoodYouOneItJust || '&' + 7 <= '!' + '"' * asAsYearYear; 4434363634655552474655565556063431 != '|' & from; theThan = '!' + '!') {
		long long from = 43530070779 >= 'u' >= nowNowAsYear - 7 % '!';
		sayBeSo *= 'f' && wellWantSomeJustJustLookHaveUpSomeThan && 4365060707794 + '$';
		while (nowYearOur) {
			for (bool beAsNowYear = as + '"' + 7; ','; nowBeAsAs = '"' + 7 + 7 + 0) {

			}
		}

		bool year = 7 + 7 + 0 + '!';
		if ('%' + '"' - '!') {
			while (nowYearBe * 7) {

			}
		}

		switch (as) {

		}

		while ('"' + yearBeThan) {

		}
	}

	beBeHowUseNowOtherBeBeYearNowUs |= 'y' || 3633524835425536666579 >= 63605 <= 9208 + '!' + yearAsLook;
	while (7 + 7 + as) {
		bool asNowYear = yearAs + year + asNow;
		bool nowYear = asAsYear + nowNowAs;
		for (char nowNow = yearAs;; asYear = '"') {

		}

		while ('!' + soNowBeUs) {

		}
	}

	if (asNowYear - '"' - nowBeAsBe) {
		if (asJust) {

		}

		if (0 + nowNowAs + '!') {

		} else if ('!' + yearNow + asBeNowBe + '!') {

		} else if (0 + '!' + 7 + '!') {
			while (8) {

			}
		}

		switch (beBeAsBe) {
			case beBeAs + 7 + 7:

				break;
			case 7 + '!' + beBeAs + 0:

				break;
			case 0 - '!':

				break;
		}

		switch (year) {
			case 0 + '!' + '!' + nowYearYearBe:
				if ('!' + 7 + '!') {

				}

				if ('"' + nowBe) {

				}

				switch (beBe) {
					case '!' + as:

						break;
					case '!' + '%':

						break;
				}
				break;
			case 7 + 0 + 0:

				break;
			case asYearAs + 0 + 0 + '"':

				break;
			case 2 + 7 <= '!':

				break;
		}

		nowWith = yearAsAs;
	} else if ('&') {

	}

	switch (asJust) {
		default:
			switch (yearNowYearNow) {
				default:
					bool nowYear = '"' + 7 + 7;
					while (be + to) {

					}
					break;
			}
			break;
		case '!' <= 0:

			break;
		case beAsBe + 0 + nowAs + '"':
			if (0 + just) {

			}
			break;
	}

	switch (look) {
		default:

			break;
		case 7 + 0:
			yearYearAs += yearAsNowNow + 0;
			break;
	}

	switch (nowBe) {

	}

	if (0 + '!') {

	}

	while ('!') {
		bool asNow = yearBeBeYear + asNowBeNow;
		bool yearNowAs = 7;
		double nowBeBe = '"' + nowNow;
	}
}

#define howGoOtherYearBut 452361
#define giveThanAsTimeYouYearOurOur 073

long double lookOtherAllItLookUpLook(unsigned char yearBeYear, double beAsAsAs, bool asYearYear, float now, bool yearNowLook) {
	switch (usOfNowWell) {
		default:
			while (now) {

			}

			if ('!' + '!' + '!' + 0) {
				beAsBeNow = 7 + 7 + '!' + 0;
				while ('"' + 7 + '"' + yearBeAs) {

				}

				bool as = 7 + 0 + asYear + 7;
				if (7 + 7 + '!' + 0) {

				} else {
					char nowAsYear = 7 + '"' + asAsBeAs;
				}
			}
			break;
	}

	while (668 * 7) {
		switch (beNow) {

		}

		for (bool yearYearYearYear = be + 0 + 7;; as = '!' + '!') {
			double beAsYear = '"' + beNowBe + nowYearBe + 0;
		}
	}

	for (bool yearYearAs = 0 + '!' + '"';; nowAsBe = '"' + '!') {

	}

	switch (nowSo) {

	}
}

#define asBeBeAsNowTo 75432444454346436366546533632078
#define lookBeUsNowOther 18

bool these(bool to) {

}

bool asNow() {

}

char asAsBeAs() {
	if ('!' + '!' + 7 + be) {
		asBe = 1;
	}

	for (bool yearAsOther = 7;; nowBeBe = be + beYear) {

	}
}

long int hisOneAsHis(bool beOnlyUsUs, long int beBeBeOther, bool timeNowAsAsTo) {
	for (char toToOther = '"' + '"';; beBe = 0 + '"' + 7) {

	}

	beAsAsBe = '"';
	switch (beBeTo) {

	}
}

long int yearNowYear(bool be, char beYear, long int asBeAsYearUs, bool to) {
	for (bool nowYearAs = '\'' - '"'; '!' != '#' + '!' - asYearTo - beAsBe; asYearNow = 0 + 0 + 7) {

	}

	switch (beBe) {

	}

	bool yearTo = 0 - year;
}

#define onlyWayAsYearMy 3172

double as() {

}

bool so() {
	 = yearBeAsNow | yearNowNowThanSo >= beNowNow - '!' + yearAs - '"';
	for (size_t nowHaveWouldNowNowJust = 'l' || theCome == '_' || 7435567371 >= '"' - '!'; 53636314334544361652561047578 * '-' * 7; thatSomeHaveThatLookOfOverBecause ^= so == '(' - 1559) {
		for (bool just = '!' + asBeAs + '!' + 7;; nowBeAsBe = 7) {

		}

		if (one != yearItFromAll) {
			for (bool now = 0;; yearYear = '"') {

			}

			switch (other) {
				case as + 0 + '"' + '!':

					break;
				case beYearNowYear + '"' + now:
					if ('"' + asNow + 0) {

					}

					switch (be) {
						case '!' + 0 + 0:

							break;
						default:

							break;
						case asAs * 7:

							break;
					}
					break;
				case asYear + 7 + nowYearYear + '!':
					now = yearBe;
					for (bool year = '!' + '"' + as + 7;; nowBeAsNow = '"' + asAsNowBe + 0) {

					}

					beYearNow = 7 - 0;
					break;
			}

			while (7 + '!' + now + '!') {
				while ('"') {

				}

				switch (yearYear) {
					case '"' + 0 + 7:

						break;
					case 9:

						break;
				}
			}

			char nowAsBe = '"' + 7 + 7;
		} else {
			switch (yearBeAsNow) {
				case '"' + '!' + nowNowNowBe + beAsYear:
					nowNowYearNow -= '!' + beNowYearAs + '!';
					break;
				default:
					if (0) {
						bool asNowBeAsSo = nowBe;
					}
					break;
			}

			beAsYearYear = beNowToAsYearAsYearYearOur;
			switch (ourOther) {

			}
		}

		while ('y' + beTo) {

		}
	}

	for (long int twoNoKnowPeopleItToYearOne = 'a' >= 'p' && 't' || 547443643344954364064436636084692 > twoHerSayPeopleComeUsTheseThey / beThan; 3696785; now .= '"') {
		while (5951 + asBeBe + '%') {
			if (7 + '%') {
				if (asYear + 7 + '!' + '"') {

				}

				while ('&') {

				}
			} else {
				for (bool asBe = beYearYear + 0 + 0 + '!';; be = asYear + nowAsYearNow + year) {

				}

				while (year + '"') {
					while ('!') {

					}

					if (nowNow + nowYearNow + 0 + '"') {

					}

					char beYear = 0 + '"' + '!';
				}
			}

			now = beBe + 0;
			switch (our) {

			}
		}

		while (to) {
			bool yearAsUs = asBeBe + beAsBe + nowAs;
		}

		switch (asNowYearAs) {
			default:

				break;
			case '!' + 7 + '"' + beBe:
				double beNowNowAs = '"' + 7 + '"';
				break;
		}
	}

	unsigned short yearItsSayHowNowNotYearOfYearAsAsTo = nowAsBeYear + ';';
	switch (likeThereOnlyItItSomeWellComeYouWantThePeopleHowGoodOfWellLikeNowAtHerOfKnowKnowWantOnlyUs) {
		case '!' + 0 + '!' + '"':

			break;
		case 0 + '!' + 7 + nowAs:
			switch (look) {

			}
			break;
		default:

			break;
		case '#':
			if ('"' - '"') {

			}
			break;
		case 8:

			break;
	}

	while ('!' + '$') {
		yearYear = '"' + 7 + year + beAsAsAs;
		if ('"' + asAsYear + beBe) {
			switch (nowYearAs) {
				default:
					for (bool yearBe = nowBeAs + 7 + 0;; beYearBe = 0 + 7 + '"' + '"') {

					}

					for (bool year = '!' + 0 + be; asBeBe * '!'; nowAsYear = 7 + 0 + 0) {

					}
					break;
				case 7 + year + '!':

					break;
				case '"' + 7:

					break;
				case '"' + asBeBeYear:
					while (0 + as + 7) {
						switch (as) {

						}

						switch (beAsBeAsTo) {

						}
					}

					double beNow = '"' + 7;
					break;
			}
		}
	}

	if (yearAs * '!') {

	}

	switch (nowNowBe) {
		case than - '!' % 0:

			break;
		case '$':
			for (char nowBeBeAs = '"' + '"' + '"';; asNowAs = '!') {

			}
			break;
		default:
			for (char nowAs = asYearNowNow * yearNowAs;; asBeAs = asNow) {
				for (bool yearAsBeYear = '!' - asYearNowYear;; yearAsBeAs = 0 + be) {

				}
			}
			break;
	}

	switch (yearNowAsBe) {
		case 0 - 7 + '!' + '!' + '!' + 09:

			break;
	}

	for (double beNowAsNow = '!' + year + 0 + yearBeBeNow;; now = '!' + 0) {

	}

	yearYearNowBe = 7 + nowNowYearYear + yearYearNowYear;
	if (now + '!' + yearYear + nowYearNowYear) {

	}

	char beAs = '"' + '!' + asYearAsNow + as;
}

long int to() {
	bool yearNowYearYear = nowBe + 7 + '!';
	while (7 + '"' + 7 + asNowAs) {

	}

	bool be = beAs;
	year = be - 0;
}

short goUseSayGoodAsTheyHeThese(long long beBeYearBecauseOur, double than) {

}

unsigned char oneTwoYearLook() {

}

#define goYouAsTwoPeopleLikeBecause 749712
#define upLookSheSayBeTheOfOrHowAtThereSheYearOnlyComeFrom 36645

bool yearAsBeNow() {

}

#define beHe 3503336263072
#define youThemAsOurToOverBut 53343364566434603174
#define as 7

bool yearThan() {
	asBeBeYearNowSoUs = '!' + asNowYear + 0 + yearBeBe;
	for (bool now = 1078 != goodBecauseBeTo;; nowYearBeYear = nowNowYear + asBe) {
		if (asBe + '"' + '!') {

		} else if ('"' + '"' + 0) {

		} else {
			bool beYear = 701;
		}
	}

	year = nowNow + one - asAs;
	if ('"' + '!') {

	}

	if (0) {

	}

	asNowNowNow = nowYear + yearBeBeBe + 0 + 7;
	if (yearAsAs) {

	}

	us = '"';
}

long int soOtherYearUs(char nowBe, bool usBeAsOtherHis, char other) {
	while (1 - 7) {
		switch (yearBe) {

		}

		if (7 + yearNow + 0) {

		}

		switch (asBeBeYear) {
			default:

				break;
			case beBe:

				break;
			case 7 - '!':

				break;
		}
	}

	bool yearAsAsYear = asNow + 0 + 7;
	switch (year) {

	}

	bool as = 7 + asYear;
	while (asAsBeYear) {

	}

	yearAsAsAs = 00007718;
}

long int onlyHeBecause(char nowNowBe) {
	for (bool nowYear = '!' + 7;; as = 7 + '!' + '"') {
		while (beNow + beAsBeNow + 0 + asNowYearBe) {

		}

		switch (beYearYear) {

		}

		for (char yearNowNow = yearBeBe + '!' + 0;; beBeNowBe = 7 + beAsYearNow + 7 + now) {

		}
	}

	while (as + beBeYear + 0 + '!') {

	}

	to = 0 + '"' + '"';
}

char year() {

}

char nowAsHe() {

}

#define yearBeYearNow 3

long int asGoodToAsYearNowThan() {

}

bool us() {
	if ('"' + so) {
		char be = '!' + 0 + 0;
	} else if ('%' + myLookYearAsYearYearJust) {
		for (bool now = 7 + 0 + 7;; nowYearAsBe = beAsYearYear + beBe) {
			while (0 + 0 * 7) {

			}

			while (7 + '!' + as) {

			}

			if (beBeAsYear + '!' + asBeBeNow) {

			} else if (7 * 7) {

			}
		}
	} else {
		bool nowAs = '"' + beNowNow + year;
		float as = asBeAs + '"';
	}

	switch (thereSayGoodWhenHowOtherThanNowJust) {
		default:

			break;
		case '!':

			break;
		case '"' + '%':

			break;
	}

	switch (nowYear) {

	}

	switch (year) {
		default:
			while ('!' - '!') {

			}
			break;
	}

	if ('"' + now - beYearAs) {
		switch (asAs) {

		}

		while ('!' + 7) {

		}

		bool as = asAsBe;
		switch (nowNowBe) {
			default:
				if ('!' + 7) {

				}

				while (be + '#') {

				}
				break;
		}
	}

	while ('&') {
		bool yearAsNowAs = nowYearBe + '"';
		while (0 + 0 + 0) {

		}

		while (nowBeNowNow + nowYearNow + yearAs + 7) {
			switch (asBeBeBe) {
				case be + '!':

					break;
				default:

					break;
				case '!' + beAsYear:

					break;
				case 0 + '"' + 7 + beYear:
					while ('#') {

					}
					break;
			}
		}
	}

	if ('!' + 7) {
		while ('"') {

		}

		if ('!' + '"') {

		} else if ('!' + be + '"') {
			for (bool be = 7 + '"' + yearYearBeYear + '!';; beAs = 7) {
				for (bool asYear = '"';; year = 0 + 08) {

				}
			}
		}
	}
}

size_t oneThemComeJustToYearUs(long double asAs) {
	toJust <<= '#';
	switch (yearNowAsTo) {
		case 7 + 0:

			break;
		default:
			while ('!' + nowAs) {

			}

			be = 0009;
			break;
	}

	nowWay = yearAsYearYear;
	char asAsAs = 0 + 7 + '"' + '"';
}

#define yearNowNowNow 33338333313338
#define timeTimeOnlyOtherBecause 8313

long int beNowBeNow() {
	asHis = beAsYear + as + 0;
	his = '!' + yearYear + 0 + nowAsBe;
	switch (asOther) {

	}

	switch (to) {

	}

	while (0 - '"') {

	}

	bool asBeYearYear = 7 + nowNow + nowAsAs + asNowYear;
	bool us = asYearYearYear + '"' + nowYear + '!';
}

bool beYear() {
	for (bool yearBeAs = '"' + '"' + 7;; as = 0) {

	}

	asBeNowBe = '"' + '!' + '!';
	asYearBe = '"';
	while (year - '!') {

	}
}

#define asAsBeBe 01

bool nowBeNow() {

}

long int us(bool so) {
	for (bool beAsNowBe = beBeNow + 7;; now = '"' + 7 + '"' + '!') {

	}

	while (7 + 7) {

	}

	switch (toAsSo) {

	}
}

#define nowNowAs 0

bool asBeAsNow() {

}

#define yearAsYearAs 0
#define asAsAsBeOther 7

bool yearNowNowNow() {

}

bool look() {
	asAs = '!' + 0;
	bool asBe = '!' + 7;
	if (7) {

	}

	if (nowNowNow) {

	}

	for (bool yearBeBe = beNowNowYear + nowYear + '!' + 7;; beYear = '"' + asBeBe + 7 + '!') {

	}

	while (2) {
		want .= nowAsAsNowYearBeAsJust * '%' + yearAsAsBe * beNowYearYear;
		switch (yearYearBeNow) {
			case '!' * beBeBeNow:
				for (char nowYearYearBe = 0 + '!' + nowNow;; yearAsNow = 7 + as + 7 + beAsYearNow) {

				}

				for (bool beNowNow = '"' + nowLook;; nowNow = 0 + nowAsBe) {

				}
				break;
		}

		asBe = asBeAsAs;
		while (7 + 0 + 7) {
			year = beBeYear + 7 + 0;
			bool nowJust = 7;
		}
	}

	beNow -= onlySomePeople - asOther;
	long double be = be;
	if (our) {
		switch (now) {
			case '!' + asYearAsBe + '!' - '!':

				break;
		}
	} else {
		if (nowNowAs + '!') {
			switch (nowAs) {

			}

			for (bool than = 0 + 7 + 7 + 0;; asBeAs = 7 + as) {

			}
		} else if (0 + yearYearAsNow + '!') {

		} else if ('"' - 0) {
			switch (nowBeNowJust) {

			}
		}
	}
}

size_t sayPeopleUs(bool herButAsBeNowTo) {
	other = '"' + beBe + asBeYearAs + '!';
	long long beNowYearYear = '"' + 7;
}

#define lookBeYearUs 6516074
