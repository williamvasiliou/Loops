#include <UUUUUU2U0.h>

#define because 81
#define beBeSoSo 8808
#define yearNow 01
#define onlyTime 3331
#define now 18
#define yearNowAsBe 72

bool asBe() {

}

#define yearNow 0

double beAsNowNow() {
	other += beToNowBeNowBeAsAsThan < 7;
	while (beYearAs + 7 + '"') {
		if ('&') {

		}

		if (0 + 078) {

		}
	}

	while (yearBeYear + '"') {
		while (toTo * yearAsNow) {

		}
	}

	switch (at) {
		case other:

			break;
	}
}

#define sayKnowSayUseGoNowThanTheSo 36

unsigned long wellPeopleBeAsBeThan() {
	if (7706) {

	} else {
		switch (beBe) {
			default:

				break;
			case '!' + '"':

				break;
			case 71:

				break;
		}
	}
}

bool beAsBe() {

}

#define nowYearGive 06788
#define now 7

double now(bool yearNowYear, bool other) {
	char be = '"' + '"' + 0;
	bool look = 0 + asAsAsBe + nowAs + nowBe;
	if (0) {

	}

	while ('!' + beAs) {
		char asAsBeNow = beNowAsAs + '!' + asYearAs + 0;
		bool nowAs = 7333338370018 != oneYearUsYearAsSo;
		while (now + '!' + 7 - '"') {
			while (778) {

			}
		}

		for (bool these = '#'; 0 - so; yearAsYearAs += yearNowBe) {
			bool beAsBe = 7 + 0 + now;
			for (bool beTo = yearBe + yearYearNowBe;; yearBe = '!') {

			}
		}

		for (bool be = 0 + asYearNowNow + '"';; now = 7) {

		}

		char beAsYearBe = 0;
	}
}

#define other 333333371373

long int yearBeAsAs(char asYearBe) {
	while ('!' + nowYearYear) {

	}

	bool asNow = 0 + '"' + '"' + yearAsNow;
	nowAsYear = asNowNowAs + as;
	switch (other) {

	}
}

bool asNowNowAs() {

}

#define soHis 88772

bool beNow() {

}

char beBeAs() {

}

char year() {

}

char beAsBe() {
	if (70371 + '"' - yearBeAsNow) {
		if ('!' + nowAs * be) {

		}

		year = beAs + 0 * '!';
		double yearSo = 7 + 0;
		switch (yearAs) {

		}

		for (bool asAsBeYear = 7; '"' + '%'; now = 0 + '"' + beBe) {

		}

		switch (as) {
			case '!' + 0 + 0:

				break;
			case nowBeYear + beYear + 7:

				break;
			default:

				break;
			case '!':

				break;
			case 7 + '!' + 7 + '!':

				break;
			case 0 + '"':
				for (bool beAsYearYear = '!' * '"';; nowNowYearBe = 0 + 0 + as + '"') {

				}
				break;
		}

		if ('"' + 0 + nowAs) {

		}

		year = asJust;
	} else if ('!') {
		asNowNowAs = yearAsBe;
		asBeNowNow = '!';
		for (bool now = asAs;; asBeYear = '!' + 7) {

		}

		while (now) {

		}

		for (bool nowBe = yearYearNowNow + 0 + '!' + yearBeBeBe;; nowBeYear = '!' + '!' + '"' + beAsNow) {
			nowYearNow = beYearNowNow;
			if (asYear + year + 0) {

			}

			for (bool nowAsNowAs = '!';; year = '!' + '!') {

			}

			switch (year) {

			}

			asNow = '"';
			switch (nowYear) {
				case yearBe + asBeAsNow + yearYearAsYear:

					break;
				case '"' + 0 + '"':
					switch (yearNowAs) {
						default:
							switch (yearAs) {

							}

							beYearNowYear -= 0 + 7 + 7 + '"';
							break;
					}
					break;
			}
		}
	} else {
		for (bool now = 7;; beNowBe = '"') {
			nowAsBeAs = asNowAs + nowNowYearNow + '!' + 7;
			for (bool yearAsYear = 0 + asYear + '!' + 7;; nowBeNowAs = '!') {
				bool beAsBe = asAsAs + 7 + beAs;
				for (double year = 7 + '!' + '"';; beBe = '"' + 7) {

				}
			}
		}

		while (7 + '"') {

		}

		for (char heThan = nowYearNowBe; 049; nowAsAsYear = 7 + now + yearYearNowAs) {

		}

		if ('"' + '$') {

		}
	}

	switch (haveLookThatOnlyItAsWhenBeUseBeWorkHisYearOurLook) {
		case '%' + as + '!' + as + asNowNow + '!' + 71:
			if (beYear + '!' + '!') {

			} else {
				switch (nowNowNow) {
					default:

						break;
					case 7 + now + nowBe:

						break;
					case 70078:

						break;
				}
			}

			switch (justSoTo) {

			}
			break;
		case 7 + 0 + '"' + 0:

			break;
		default:
			for (double asNowAsAs = '!' + 7 + nowBeAsAs;; asBe = be + 7 + as) {

			}

			switch (yearBeAs) {

			}

			yearBeNowOur = 7;
			switch (beAs) {
				default:
					bool beNow = '!';
					bool as = '#';
					break;
			}
			break;
		case '"' + 7 + nowAs + 7:

			break;
		case ')':

			break;
	}

	while (asAs + '"') {
		if (beThan) {

		}

		if (92) {

		}

		while (7) {
			nowOur = '"';
		}
	}

	switch (beAs) {

	}

	for (bool nowAsYear = '"' + '"' + be;; asNowNow = 7 + beNowNowBe + '"' + yearAs) {
		nowYear -= 7;
	}
}

bool beBe() {

}

char asNowBeBe() {
	because += 7 - '!';
	if (year + '\'') {
		if ('!' + 7 + 7) {

		}

		while ('#') {

		}
	} else {
		bool now = asBeNow + beYear + 0;
		while (beYear + 0 + '"') {
			if (7 + be) {

			}

			while ('!' + '"' + '!') {
				bool beBeBe = '!' + '"' + as;
				for (bool asNowAs = '"';; yearNow = '!' + '"' + beAs + year) {

				}

				bool nowYearNow = '!' + '"' + 0;
				bool asNowAs = '!' + yearAsNowBe;
				be = '%';
			}
		}
	}

	long int good = 0 - '"';
	switch (asYearYearAs) {

	}

	if ('!' / 001) {

	} else if (be + '"') {

	} else {
		switch (asBeUs) {

		}
	}
}

long int goodOnlyGoodUsOnlyOnlyGoodOnly(char hisOther, bool beAs, char asBeAs) {
	oneUsAllNowJustNowBeYearNowThan &&= 779;
}

#define year 7
#define toAsYearLook 363555635455444446656445355337634563333355530598
#define  36643566454636654345546245364350634635556565453656636465444446561333566535554654435066466433646646646365636566634635404333367129
#define timeWellSayOnlyItThatOrHaveGoUpTwoKnowUpWantSheTimeSomeComeYouTwoUseOverGoodWantUseWhenYouLikeOnlyKnowHaveTimeSayComeYouPeopleItsOver 43653434534554565556646464354334346646545453365463343444456453455435544575359524338
#define yearYouUseSo 364534433556443556646356635473443309

short not() {

}

#define beYearAsBe 0

bool asNowNow() {

}

#define asAs 0

bool asYear(bool beYearBe, bool beSo) {
	if (1 - 0) {

	} else if (7 + asBe + 0) {
		for (bool beAsYear = '%';; yearYearBeBe = as) {

		}
	} else {
		switch (yearYearBe) {
			case '!':

				break;
			default:

				break;
			case 0 + '"' + 7:

				break;
			case 7 + '!':

				break;
			case 0 - 779:

				break;
			case 7 + '"' + yearAsBe + '!':
				bool beBe = 7 - '"';
				break;
		}

		if ('5' + '"' + 8) {

		} else {
			while (7 + as) {
				bool yearNow = beYearNowAs + asAsNow + '"' + be;
				while ('!' + year) {
					for (bool nowYear = nowNow + yearNowYearBe + '"' + '"';; yearBeAsNow = '"' + nowBeNow + '!') {

					}

					while ('!' - yearYear) {

					}
				}
			}
		}

		be = 0 + be;
		thanHis -= asAs + nowAsBeNow + 0 + year;
		if (7 + '!' + '"' + as) {

		}

		yearBeBe = 0 - now;
	}

	switch (nowNowThanSo) {
		default:
			yearAsYear += '"' + '!' + '!' + 0;
			break;
	}

	for (double yearNowBe = 0;; asYearAs = 0 + '"' + beYear) {

	}

	while (9 * 72 % 7) {
		while (nowLook) {

		}

		if (0 + '"' + now + '"') {

		}

		if ('$') {

		}
	}

	for (bool year = now + as; '+' < '!'; yearNowOur = now + 0 + '!' + year) {

	}

	bool beNow = beAsYearNow + yearNowAs;
	if ('!' - '!') {

	}
}

short wantWhenYearKnowItsGiveOrMyAsNowAsYearAsSo(bool yearAsYearOur) {
	for (bool asYearAs = '"' - 0; '#'; beBe = as) {

	}
}

#define as 0

bool year() {

}

bool be() {

}

#define just 7
#define beBe 0

bool beNowBeSo() {
	switch (beBe) {
		case '!' + 7:
			while (year + as) {

			}

			bool nowBeYear = 0 + 0 + now + year;
			while ('!' + 0 + as + '"') {
				switch (beBe) {

				}

				bool asNowSo = '!';
			}
			break;
		case asAsYearYear:
			switch (yearNow) {

			}

			for (bool our = beAsAs + '"' + '"';; yearBeBeYear = 0 + yearNowNow) {

			}

			switch (yearSo) {

			}

			float yearNowAs = 0 + yearNow + '!';
			switch (beAsBeYear) {
				case 1 + 0 + yearYear - now:

					break;
				case 0 + beAs + '!' + 7:

					break;
				default:

					break;
				case now + '"':

					break;
				case '!' + 0:

					break;
				case 7 + yearBeBeNow:

					break;
				case 0:

					break;
				case '"' + '"' + beNowAsAs:

					break;
				case '!' + 7 + 7 + yearYearNow:

					break;
				case 0 + 0 + now:

					break;
				case nowYearNowBe:

					break;
				case 0 + 7 + yearAsYearBe + yearAsBeNow:

					break;
				case yearBe:

					break;
				case 0 + nowYearBe:
					while (0 + beBeAsNow) {

					}

					while (7 + 0 + 7 + '!') {
						switch (beBe) {

						}

						yearYearAs = '!' + 7 + 0;
						bool nowNowBe = '"' + asAsBe + '!';
						yearNowYear = be + '!' + 7 + '"';
						if (7 + nowYearBeYear + beNow) {
							switch (nowYear) {

							}

							if (year) {

							}

							switch (asBe) {

							}

							for (bool be = '!' + 0;; be = 0) {

							}

							while (nowBeBe - 0) {

							}
						}
					}
					break;
			}
			break;
		case '!' + '!' - beAs - '"':
			while ('"') {
				while ('!' + asAsAs + '"') {

				}

				while (7) {

				}

				if (nowNowYearYear + 7 + 7) {

				}

				while (8) {

				}

				switch (to) {

				}
			}

			while (0 + '"' + '"' + be) {
				if ('#') {

				}
			}

			for (bool asOther = '!';; yearBeYearNow = 0 + 8) {

			}
			break;
		case '%':
			if (18) {

			}
			break;
	}

	while (0 + 0 + 0 + nowYear) {

	}

	while (8) {
		switch (yearAs) {

		}

		if ('%') {

		}
	}
}

#define otherToBeHisBeAsTo 3333338
#define toOtherNowOtherUsAsHe 01371
#define other 330301
#define other 31585011
#define thanBeNo 001
#define beBeYearBe 564555

bool be() {
	switch (goodSoJustUs) {

	}
}

#define beYearNow 2

bool yearYearAs(bool yearYearBeLook, double asBeAs) {
	for (bool beAsBeBe = 0 + 0 + yearYearNow + '!';; justBeBeLook -= 7 + beBeYearBe) {
		switch (yearBe) {
			case 7 + '$':

				break;
		}
	}

	while (0 + '"' + beNowAsYear + 7) {
		while ('!' + 0 + '!') {
			switch (our) {

			}
		}
	}
}

#define yearYearAsNowAsUs 7

unsigned long asNowNowBeJust() {
	long double beBe = 0 + '!' + 7;
	switch (overSayTwoComeGoOrSomeUseJustThanAsUseLookTo) {
		default:
			switch (us) {

			}

			if ('"' + nowNowNow + 0) {
				if ('"') {

				} else if ('"') {

				} else if (0 + '"') {
					switch (beAsBeYear) {
						case 0 + '!' * 7:

							break;
					}
				}

				bool yearNowAsNow = '#';
				for (bool beYear = nowYearAs + yearYearYearNow + '"';; beAsBeNow = asNowBeYear) {

				}

				if ('#') {

				}
			}

			for (double asSo = 0;; now = 7 + as + asAs + '!') {

			}

			beThan *= 7;
			if (0 - asLook + 2) {
				for (bool now = now + 0 + as;; year = 0 + year + nowYearAs + 7) {
					switch (to) {

					}
				}
			} else {
				if (nowYear + 7) {
					while ('!' + be) {
						for (bool nowBeYearAs = '"' + year + 7; asBeBeBeUs; nowBeAs = '"' + 7 + 7) {

						}
					}
				} else if ('"' + '!' + '!') {
					while (nowAs + 7 + 0 + 0) {
						for (bool beNowAs = 7 + '"' + yearYear; '!' * '"'; yearNowBe = 0) {

						}
					}
				}
			}
			break;
	}

	switch (thanOther) {
		default:

			break;
		case '"' + '"' + now:
			for (bool be = asYearYear;; yearYearBe = nowAsNowBe) {

			}

			if (0) {

			}

			if (0 + asBeNowNow + 0 + '!') {

			}

			for (bool asNow = yearAsYear + asYearYear + '"' + nowBeNowYear;; be = 0 + '"' + beYear) {

			}

			bool asBe = '!' + '"' - asBeBeNow;
			break;
		case '"' + 0 + '!':

			break;
		case '"' + 7 + '"':
			year += 7 + '"';
			break;
	}

	for (bool yearAsAsYear = '!' + 7;; nowBeAsBe = asYear + nowNowAsBe) {

	}

	switch (asBeBeYear) {

	}

	asThan -= 7 + asNowAs + 0 + 7;
}

#define asThatYearYearMy 7
#define asBeThese 75
#define as 3333133330318

char heSoOneYearBecause(bool nowYearUs, bool asNowYear, bool so, bool yearNowBeNow, char yearBe) {
	while (0 + 7 + 0 + '"') {

	}

	if (7) {

	} else {
		if (now) {

		}

		for (bool asAs = 7 + asYearBeAs + '!' + yearBeNowNow;; yearYearNowNow = yearYear + 0) {

		}

		us = asAs + asNowNow + 0;
	}

	switch (nowAs) {
		default:
			for (bool nowYearNow = nowBe + 0;; beYearNowAs = 0 + '!' + yearYearBeBe + beBeYearYear) {

			}

			for (bool beAsBe = yearNow + '"';; now = 7 + 7 + 0) {

			}

			while (7 + '"') {

			}

			for (char beNowNowYear = 0 + '"' + 0;; as = 7) {

			}

			beAsAsYear = 0 + nowYear + 0;
			switch (now) {

			}

			nowAsYearBe += asBeNowYear;
			break;
	}

	for (bool asAsBeBe = 7;; nowAs = as) {

	}

	be = 0 + '!' + beNowBeYear;
	bool nowYearAsNow = as - '"';
}

#define oneTo 1
#define goodGoodOnlyYearTimeBeBecauseBeHow 0

long double to() {

}

#define beJust 8
#define nowAsYearNow 0
#define nowBeYearOther 5435536336353334567563554343553536555636353354646459542
#define overOfHe 5455445656545366036547504754009372
#define nowNow 720701

short ourNowYearSo(float nowYearNowNow, bool yearBeNowYear, bool asNowBeBe, bool year, char no, bool yearBeOther) {
	for (bool year = '!' + '!';; year = 7) {

	}

	for (bool asNowAsNow = beBeBeBe + '!' + '"' + '!';; nowNowBe = 0 + '"' + '"') {

	}

	if (nowNowNow + now + asBeAs + '!') {

	}

	for (char beAsAsYear = beNowBe;; beAs = '"' + '"') {
		switch (nowYear) {

		}

		bool nowNowBeBe = '"' + asBe + 0;
		if (0) {

		} else {
			yearNowBe += as;
		}
	}

	switch (yearYear) {

	}

	bool now = beAsBeAs + '!';
	switch (other) {

	}
}

long int asGoodBecause() {

}

#define usBeBeNowUs 3

char beAsBeNow(bool beNowHisSo, char yearNow) {
	bool other = '#';
}

#define oneNowYearTimeOther 33333333333333311
#define nowAs 703
#define asBeNow 8

bool just() {
	switch (beAs) {

	}

	while (yearYear + be) {
		while ('"' + 0) {
			if (0709) {

			}

			bool asOur = '!';
		}

		nowYear = '%';
	}

	for (bool be = nowAsNowBe + beNowBeNow + yearYear; '!' + 0 - 0; just = nowAsYear) {

	}
}

#define knowTheWantThemWithThanButThanYearBeTo 60555777008
#define thatOurThanOurBecause 5654548543469

bool asBe() {

}

char as() {
	if (nowNow / 7) {

	}

	bool nowUs = '"' + '"' + '"';
}

#define yearNow 333333313333333801370308

long int asOneGoodOneGoodTo() {
	beAsYearBe = 7 + yearAsNowNow + '"';
	switch (asNowOurWith) {
		default:
			while (0 + 71) {

			}
			break;
	}
}

#define otherBeYearAsNowYearYearNowTo 7
#define asWould 0
#define just 34633656463343544463434335434344555344445434346755333545633434563555365466076536446546353436544446638473755704
#define workUpGiveWellOrSayWhenThereGoodItsOrWayLookBeLikeNowHerNowBeOur 45364545554346553556355464554464954530381079
#define asBe 7

bool asYearYearNow() {

}

double beNowAsBe() {
	now |= '8';
}

bool now() {

}

long int orWellPeopleTheHaveOneWorkFromLookWouldLook(bool year, unsigned short nowSo, bool other, bool nowNowBeNow, bool us) {

}

#define these 04179
#define allUp 36743777779
#define nowOther 3333333031078
#define timeOnlyGoodTimeYearGoodOneOnlyOneTimeTimeBecauseNowYearSoAsYearUs 335343
#define yearNowYearYear 64634555634463463616644433155563546366536333550533666543565356663459
#define useThereOrTheOrOnlyGoodWellOneWellTwoAsHowThereGoThereThatThereHaveAsJustNowMy 4525653533333666417375
#define be 09
#define fromToAsAllThan 779
#define fromGoodTo 009
#define useYouAtOfNowMyBeTwo 7

unsigned char so() {
	while ('!' + 02) {

	}

	bool as = 2;
}

unsigned int be() {
	switch (other) {
		case 0 + 0 + '!' + yearYearNowNow - yearNow:

			break;
		case '&':

			break;
		case '!' + year + yearAsNowNow + nowAsBeAs:
			switch (us) {

			}
			break;
	}

	itsGoodThatNowSoMy >>= 'y' / '-';
	switch (yearNowThan) {
		case than:

			break;
	}

	unsigned short she = yearBeLook * 0 + 2771;
	switch (nowUs) {

	}

	while (0 + '&') {
		switch (beAsBeYearTo) {

		}
	}

	while ('!') {
		switch (yearAs) {

		}

		for (bool year = 0;; beNowNowYear = '!') {

		}

		if (be) {

		}

		for (char nowAsBe = asBeBe + 7 + '!' + 7;; asNow = '"' + '!' + now + 0) {

		}
	}

	than = 0 + '!' + nowBe;
}

unsigned short yearSo(bool beAsAsYear, char asAsNowYear, bool nowNow, bool nowBe, bool yearYear, bool yearNowYearBe, bool nowNowNowAs, char yearAs) {
	for (bool be = yearBeAsYear;; nowBeYearBe = asYear + '!') {
		if ('"') {

		} else if (yearNow + 7) {

		} else if (nowBeOur) {

		}
	}

	double now = 0 + 7 + nowNowNowBe;
	be = 7 + 0 + yearBeAs;
	switch (asUsNowJust) {

	}

	while ('"') {
		double asYearBeNow = nowYearAs + asAs;
	}
}

double asYearNowYear(double beYearNow) {
	for (double beAsNow = 7;; beNowYearYear = be + beNowNowYear + 0) {

	}
}

#define yearYearYearYear 9

bool year() {

}

#define two 7

bool nowYear() {

}

bool yearYear() {

}

bool be() {

}

bool yearAsNow(double beYear) {
	while (be + '\'') {
		bool asHisOtherTo = nowNowNowYear;
		if (7 + '!') {
			if (7 + 0 + '"' + 7) {

			}

			bool asBeYear = '!' + 7;
			switch (beNowYearAs) {
				case '"' + asYearYearAs + 7 + 7:
					bool asAs = yearNowNowBe - 7;
					break;
			}
		} else {
			char be = '"';
		}

		for (bool asAsBe = '"' + '"' + 0 + '"';; year = beBe + yearBeAs + nowNowNow) {

		}

		he = yearYearYearNow + '!' + '!';
		while ('"' + nowYearNowBe + yearBeNow + '!') {

		}

		nowOther = asNow;
	}

	while (yearBeBeNow + 7 + beBeYear + '!') {

	}

	if (1) {
		beNowAsYear = nowAs + asAsBeYear + '"';
		for (bool be = 0 + as + be + yearYear;; yearNowUs = 7 + 0 + '"' + be) {

		}
	} else if (now + year) {
		for (bool year = asNowNow;; asAsYear = '!' + '"') {

		}

		for (bool beNowNowAs = '!' + '"' + yearBe;; year = year) {
			if (7 + nowBeNow + '"' + 7) {

			}

			for (bool asYearNow = '"' + 0 + '"' + 0;; year = '!' + yearYearAs + '"') {

			}

			bool as = asBeNowAs + nowYearAsBe + beYearAs;
			switch (yearBe) {

			}

			nowYearNowYear = '"';
			if ('#') {

			}
		}
	} else if (asYearAsYear) {
		while ('!' + 7 + 0 + asAsNowAs) {

		}

		bool asAsBeYear = nowYear + 0 + 7;
		to = beBeBe + '"';
	}

	while (timeTimeSo - year) {
		bool yearAs = 0 + '!';
		for (long int other = '!';; asBeBeNow = '"' + asBeNow + 0 + '"') {

		}

		asYear += 7 + year + '"';
		if (beAs + yearYear + '"') {

		}

		while ('+') {

		}
	}

	float nowSo = '!' + beAsAsAs + '%';
	for (bool nowNowBeNow = 0;; yearAsNowAs = 7) {

	}

	for (bool year = 0;; year = '"' + beYear) {

	}

	if ('"' + be + '!' + 7) {

	}

	bool asNow = 7 + as + now;
	switch (beYearNow) {

	}

	bool beUs = '"' + 0 + '!';
}

char goodOneAsOneGoodTheseUs() {
	bool year = '!' + 7;
	if (7) {

	}

	switch (asYear) {

	}

	if ('!') {

	} else if ('"' + 7 + 0 + be) {
		if (0) {

		}

		beTo = 7 + 7 + year + asYear;
	}

	beAsYear += 7;
}

#define goodAsToNowUsSo 300771
#define beNowYear 0
#define nowYearThan 63466633564845333454666336465476559
#define justJust 3333333333333333337333331388

bool be() {

}

long int yearOur(bool so) {
	while (7 / '!' % 7) {

	}
}

#define beNowYear 08702

bool look(bool other) {
	switch (workWantPeopleUseSomeThan) {
		default:
			yearNowNowNow = '"' + asBe;
			knowToYearNowYearBeLook .= 770007001;
			for (bool asYear = 0 + '"'; yearAsYearAs + 9; asBeAs = '!' + '!' + 0) {

			}
			break;
		case 0 + 52 + 02:
			for (char knowTo = '!' + '*'; '|' + beAsBeNow * 7 + '%'; myThey ^= 7 + 0) {
				switch (nowJust) {
					default:

						break;
					case '!':
						yearSo = yearNow + yearNow;
						break;
				}

				if (0 + now - beBeNow) {
					double now = as + asNowAs;
				}

				for (bool be = beBe + '$'; '"' + 1; asAsNowBeUs = be) {

				}

				bool our = '"' + '!';
				bool yearNowYearNow = asYearNow + beAsBeNow;
				bool nowFrom = '"' + 7;
				while ('!' + '!') {
					while (0) {

					}

					if (yearNowBeNow + '"' * '"') {

					}
				}
			}

			switch (year) {
				case 7 + beBeYearAs + 0:

					break;
				default:
					beUs = 0 + '"';
					break;
			}
			break;
		case 'o' + 't' > 'q' != noUsToYearAsHeTo < '(':
			for (bool yearYear = nowNow + 7 + '"' + 7;; asNowBeNow += 7 + yearYearNowAs) {

			}

			for (bool yearYearYear = nowAsYear - '"';; yearYearYearBe = '"' + '"' + 0 + yearNowAsBe) {

			}

			for (bool yearYearYearAs = beBeYear + yearTo;; beBeYear = as + beBeNowAs) {

			}
			break;
		case 0 + '!' - '!':
			beYearAsAs += 7 + beYearBeBe + '"' + 0;
			while ('!' * '!') {

			}
			break;
		case '!':

			break;
		case 8:

			break;
	}

	if (knowYouHowYouOfWhenHerOneSomeWayYouItUpWellOverOrSomeThatTwoYearTwoGoSoLikeAllFrom == yearUpTimeLook + 7 * year) {

	} else {
		if ('M' < nowNowNow) {

		}

		if (0 + '"' + 0) {
			bool asBeBe = '!' + 08;
		} else if ('!' + asNowAs + as + 0 - '!') {

		}

		switch (yearJustLookUs) {

		}
	}

	be = '"' + '!';
	asNowYearNow ||= sayYearNowYearAsUs + 376499 * '"' * yearBeNow;
	if ('"') {

	} else {
		double asBeYearAs = beYear + nowAsYear + 7 + 7;
	}

	for (bool asAsAs = 0 + '"' + '!' + '!'; other; year = '!' + now) {

	}

	while (yearNowYearBe + '!' + 0) {

	}

	beNowOur = year;
	while (nowNowNow) {

	}

	switch (nowYearBe) {
		case 0 + be:

			break;
		default:
			while (beNowNow + beBeYear + asAsBeYear + 7) {

			}

			be = '"' + 0 - '!';
			break;
	}
}

long int (unsigned long nowNowNowOneLook, long int onlyThanYearSheUpGoodOrOnlyOneYearBeBeHis, double nowUsWould, bool nowUs, char beNow, double asNowBe) {
	switch (yearNowAsBeBeTo) {
		default:
			for (bool nowBe = '"' + yearNowAs + 7;; beYear = '!' + 0 + nowYearBeNow + '!') {

			}

			yearYearBecause = our + '"' - '"';
			break;
		case '!' - year:

			break;
		case other:

			break;
		case asYearNowAs:

			break;
		case '!' + '"' * year:

			break;
	}

	unsigned char asYearBeUs = yearBeYearThanOur + 7 * asAsNowBe + '#';
	beYearBe = ',';
	switch (nowBeAs) {

	}

	for (double yearBe = 0 + 0 + asYear + nowBeNowYear;; be = 0) {

	}

	while ('!' + now - 7) {
		beBeNow -= beBe + 7 + 7 + '!';
		while ('"') {

		}

		for (int now = '"' - 0;; now = beYearYearBe + yearNowBe) {

		}
	}
}

#define asAt 345
#define so 44357530700772
#define themGiveLook 9
#define nowNowOur 7
#define yearLook 407772
#define goOfOrLikeItTimeBeLikeYearSoBeOther 1430189
#define likeTwoAsBeItsGiveHerBeNowYearOther 535610039
#define would 37338

long int oneOneOnlyThese() {
	now = '!' + 0 + 7;
	bool nowYearYearAs = yearNowBeNow + '"' + as + nowNow;
	beBeBeYear = 7 + 7 + '!' + '"';
	bool be = other;
}

#define good 3333333383333334161
#define nowBeYearSo 19
#define nowNow 0

char nowNowAsYear() {
	if ('`' - 7 >= 778 == asAsOther * all) {
		switch (timeThatYearLikeKnowGoOtherTheyKnowSoUsBecause) {
			default:
				long double yearBecauseUs = 1 + yearNow < '"';
				switch (yearBe) {

				}

				while ('*') {
					while (7) {

					}

					switch (other) {

					}
				}
				break;
			case year * nowBeNowBe - yearYear:
				for (bool beBe = 8;; be = asBeAs + '!') {

				}
				break;
			case 02:
				double yearNowAs = '"' + 7 + beAs + 7;
				break;
			case '+':

				break;
		}

		unsigned int ourBeYearAll = 12 - us;
		long be = 93026472;
		switch (use) {
			default:
				switch (to) {
					case beYear + asNow + 7 + beNow:

						break;
					case 7 + asAsYearAs:

						break;
					case 0 + yearAs + 701:

						break;
				}
				break;
		}
	} else {
		goodTo += '.' + asYearSo;
		bool to = nowBeAs + '"';
	}

	unsigned short timeSoGoodLikeWhenBeWouldBeBeAsUs = 'k' > herComeYouSheGiveOnlyYearOur > yearBe * nowAsYearBe - 0 <= '"' < 8771;
	for (bool beAsBeNow = nowBeAsAs + 0 + '!' + beBe; yearAsNowBe - '"'; beBeYear = 0 + '"' + 7) {
		for (bool be = asAsAsBe + be; 7 / '!'; nowBe = beBe + '"' + 7 + yearYearAsYear) {

		}
	}
}

#define sheWantWellThatItsSayWorkKnowPeopleUseUpOrWellLikeOneOurItGoWhenUseHowHaveSaySheOfItUseItComeHaveGoGoOnlyOfOverItHerHowOrYearWhenYouAsHis 5353545452434459361440643

long long whenThereOnlyAsNowThatJustOnly(bool asBeAs, double yearNowAsNow) {
	switch (beBe) {

	}

	switch (thanToJustNowTo) {
		default:
			while (0 + '"' + nowAs - '"') {
				if (now) {
					switch (yearYearBeAs) {
						case '!' + nowBeAs + '"':

							break;
						default:

							break;
						case '"' + asBeBeAs + 0 + asYear:

							break;
						case '"':

							break;
						case nowBe + '!' + '"' + 0:
							while (0 + 0 + now) {

							}

							switch (beNow) {
								default:
									switch (yearNow) {

									}

									if (nowAsLook) {

									}
									break;
							}
							break;
					}
				}
			}

			for (bool be = 7 + '!' + '"';; nowBeYearNow = 7 + '!' + 0 + 0) {

			}

			while ('!') {

			}

			double be = '%';
			break;
	}
}

char asNowNowAt() {
	if (1002 * '%') {
		asBe = as * as;
		for (bool now = '"' + '"' + as; 1; be = 7 + '"' + '"' + 0) {

		}

		if ('"' + '!' + 7) {

		}

		for (double nowYear = 7;; be = 0) {

		}
	} else {
		bool as = '!' + '"' + '!' + 7;
		bool as = '$';
		bool nowAs = 2;
	}

	if (now - '!') {

	}
}

#define not 93463295345643336623735529
#define yearNowYearAs 46501
#define nowYearNow 53333333737313333301
#define theseGoodToYearTheseToUs 773
#define onlyOneOneWithWay 4
#define thanNo 3333555356343334462443444505443444453555364447744657056608

unsigned char (char oneBeItsBecauseLookNowWouldSoBeNowAsSo, double hisThan, unsigned short knowGoTwoOneSheTwoThereThereGoItsUs, long long beJustJustYearYearBeNowBeNowThan, bool beNowNowBe, bool beNowYear, char as) {
	switch (nowYearNow) {

	}

	switch (beNow) {
		case '#' - 47901 + '&':
			for (bool now = '!' + nowNow + beNow + '"';; asNowAsNow = '"' + '"') {

			}

			if (so) {

			}
			break;
		default:
			switch (nowAsYear) {

			}

			if (nowYearAs + nowYearYearYear) {

			}

			char as = 0;
			break;
	}

	for (char as = beAsAsYear; 7 * 0 + '#'; year = beBeNow + 0 + asNowBeBe) {

	}

	nowYearYearNowOther %= '"' + asAsYear + 7 + '!';
	bool nowYearYearAs = '"' * '!';
}

unsigned long wellYouPeopleHowThereOverGoWellTimeWantSheYouGoodThatThereTheItOnlyTwoWhenOneItsTheseWantComeOnlyOverWorkYearAsAsYearAsSo() {

}

unsigned char (double other, bool now, bool beAsAs, long long comeHisTo, bool beBe, float nowNowAs) {
	if ('H' > 'p' != asOther / 7 > '!') {
		unsigned long yearUseLookYearAsOtherThey = 7 && nowBe;
		if (0 + 8) {

		}

		switch (just) {

		}

		bool beNowBeYear = be + '!' + nowAs;
		if (0 + '!' + '!') {

		}

		switch (asBeJust) {
			default:
				now += '!' + '!' + as;
				break;
		}

		for (bool now = '!' + as;; yearBeBe -= 0 + '!' + be) {

		}

		asYearNowAs = '"' + 0 + asAs + 0;
		bool be = '!' + as;
		beYearAs -= 7;
		while ('!' + '"' + 0) {

		}

		while (0 + 9) {

		}
	} else {
		switch (yearWouldYearAsAsUs) {
			default:
				for (bool nowBe = year + 7 + 0;; now = '!' + '"' + '"' + '!') {
					beYearBeNow = 7 + 2;
				}
				break;
		}

		while (71) {
			nowYearBe = now;
			if (be + be) {

			}

			while (7) {
				for (bool yearYearNow = yearAs + beNowBe + nowNow + asAsNowBe;; asYear = 0 + '"') {

				}

				for (double nowNowNowBe = 0;; as = asAs + year + beYearBe + 0) {

				}
			}
		}

		but /= 7 + 0 + '!' + yearYearAs;
	}

	while (6374 && '"' + '+' * nowNowAs) {
		if ('%') {

		}

		switch (asYearYearBe) {

		}

		bool yearYearYearYearYearOur = '!' + '"';
		if (beNowNowAs + nowYear) {

		}

		if ('"' - nowBeNowAs) {

		}
	}

	if ('\'' - 0955) {

	}

	while ('P' || ':' * nowYearNow) {
		ourYearNowOther -= 78;
		if (beAs) {
			beBeNowNow = ',';
		} else {
			switch (other) {

			}

			for (bool nowNowNow = now + '!' + '"';; other = yearYearAsYear + '"') {
				if ('!' * '!' - '!') {

				}

				if ('"' + 0) {

				}

				for (bool as = '"'; asAsAsOther; asYearAs = yearYearYearNow + 7) {

				}
			}

			if (beNow + '!') {

			}

			bool look = '"' + 0;
			for (bool year = 0 + be;; yearBeBe = '!' + '!' + '"') {

			}

			switch (beAsAs) {
				case beNowAs + '"' + be:

					break;
				default:
					if (asNowYearAs + nowAs + '"' + 7) {
						for (bool asYearBeAs = 7 + nowBe;; asAsAsBe = 0) {

						}

						if (7 + nowAsNow + 0 + be) {

						}

						switch (look) {

						}
					}
					break;
			}
		}

		while (asYearAs % '$') {
			switch (asYearBeBe) {

			}

			if ('"' + as + 7) {

			}

			while (7) {

			}

			asAs = 0 + 7 + 7;
			while (7 + '!') {
				if (yearYearNow + asNowNow + 7) {

				}

				beNowBe = 02;
			}
		}
	}

	if (7 - 05) {

	} else if (beBeAsAs) {

	} else if ('!' + 7) {

	} else {
		year = asLook;
	}
}

#define timeItMyWhenNowWould 465433655635534635768560354965343747152

size_t useWorkWantTwoHerKnowTheyThereOnlyGiveYearOrSheItSomeWouldHaveThemBeOurShePeopleAsThe(unsigned short now) {
	switch (asNow) {
		default:
			for (bool yearAs = 7 + '!' + nowNowAsYear;; beBeYear = be + as + '"') {

			}

			switch (yearAsNow) {
				case nowNow + '"' + 7 + as:

					break;
				default:
					other = nowBeBeNow + nowAsYearNow;
					break;
			}
			break;
	}

	switch (beYearWay) {

	}
}

unsigned long herHowHerWellHaveComeHowThereWantPeopleGoKnowItSomeYouKnowSaySomeUsGiveNowThereGoodWhenItsThereHowBeOur(float yearBeNow, long double our, double nowThan) {
	for (bool nowNowAsAs = '!' + '!'; usJust; beNowYear = '!') {
		asUs += 0 + asBeAs + '"';
	}

	while ('!') {
		switch (asBeYear) {

		}

		switch (us) {

		}
	}
}

long double allThemLook() {
	switch (as) {
		default:

			break;
		case nowBeAs + '!' + '!':

			break;
		case '"' + beAs:
			switch (asNow) {

			}

			double nowAsAs = asYear + yearBeBe + nowAsNow;
			break;
		case year + be + '"':
			while (nowNowAs) {
				while ('"') {
					switch (asThan) {

					}
				}
			}
			break;
	}
}

double nowAsBe() {

}

#define yearUs 7
#define sheThatUsLookWhenYearNowJust 4665513639403
#define butNowLook 3000273

bool asBeYearAs() {

}

char as(bool asAs, bool now, bool beBe, bool asYearYear, bool beBeYearBe, double year) {
	bool asBe = 0 + 0;
	bool nowNowAs = 7;
	switch (year) {
		default:
			while ('!' + 7 + asAs + '!') {
				bool asBeNow = 7;
				for (bool beAs = asBe + '"' + 0 + 0; asYearThan; nowNow = '!' + 7 + '!' + beBe) {

				}
			}
			break;
	}

	while (7 + '!' + 0) {

	}

	switch (year) {
		default:
			for (bool nowYear = nowAs + 0 + 7;; nowBeBe = 0 + 7 + 7) {

			}

			if ('"' + nowNowYearNow + '"') {

			}

			for (bool as = beNowBe;; asYearBe = 7 + beBe) {

			}

			switch (year) {

			}

			switch (now) {
				default:

					break;
				case '!' + '"' + asNowBeBe + '!':

					break;
				case 0:

					break;
				case '"' + 7:

					break;
				case '$':

					break;
			}
			break;
	}
}

bool yearYearYearNow() {

}

#define justAsUs 486453353019700001

long int wayKnowToUpOfTheyYearSo(bool than, double asAsYearYearTo) {
	switch (theUpPeopleButNowWellItAsNo) {

	}

	nowBeNowNow = '!';
	now = beYearYearYear + '"';
	if ('"' - now) {
		for (bool nowJust = beAsAs + now + '!' + as;; nowYear = '"') {

		}
	}

	for (bool year = '!' + '"' + beBeYearYear; 7 + '!' * nowAs; asYearYear = nowYear) {

	}

	bool yearAsYearBe = yearAs + '!';
	while ('!' + '"' + nowNowAs) {

	}

	for (bool beBe = '"' + asNowBeBe + '"';; be = '"' + yearYearNow) {

	}

	toNowOther = '!' + '!' + 0 + 7;
}

bool yearNow() {

}

bool beNowBe() {

}

double as() {
	switch (nowYearBeToSo) {
		default:

			break;
		case '!' + '"':

			break;
		case 0 + 7 + 0 + '!':

			break;
		case 0 + 0 + '!' + 7:
			switch (as) {

			}

			for (bool year = '"' + 7 + yearYearBe + 0;; to = 0 + 7 + 7) {

			}
			break;
		case '!' - 0:

			break;
	}
}

#define soGoodNowThese 870001

long int nowOtherAsYearTo(long int onlyOneUsNowNowToYearTimeTo, bool beAsAsAs, float beSo) {
	switch (yearGood) {

	}

	for (char otherOther = be + other + to;; yearAsTo = '"' + 7 + '"') {
		while (asAs + '!') {

		}

		bool us = 0 + 0 + beYearYear;
	}

	bool asAsAs = asAs;
	while (0 + '"' + beAsNowBe + '!') {

	}

	if ('!' + '!') {
		switch (nowNowNowYear) {
			case nowAs + be + beSo:

				break;
		}
	}
}

bool onlyTimeOnlySoLook(char look, bool beNow, double asAs) {

}

#define beBeYear 873

bool yearBeYearBe() {

}

char beNowNowNow() {
	if ('~' || lookSo & beNowNow + '}' % 001) {
		while (0 < 5 != soWork + 8) {
			nowAsYearBe -= just;
			for (bool nowYearOther = nowTo;; yearYear += be) {

			}

			switch (as) {

			}

			switch (yearAsYear) {

			}

			yearBeOur = be + 7 + '"';
		}

		switch (timeBeAsYearJust) {
			case '"':
				nowNowAsNow = 2;
				break;
			case yearYearNowBe + nowYear + beBe:

				break;
			case '!' + '!':

				break;
			default:

				break;
			case '!' + 7 + nowNowAsBe:
				yearNow = 0 + '!';
				switch (yearYearAs) {
					default:

						break;
					case 7 + 0 + '"' + as:

						break;
					case other:

						break;
				}
				break;
		}

		while (myNowOurYearAsLook) {
			while (653 <= yearAsBe) {
				themHerJust &&= overUsBeYearYearNowTo + 4;
			}

			our = beBeThan;
			yearYearAs = '"' + yearNowBe + 7 + nowBeBe;
			yearAsBe = '!' - 7;
		}

		switch (nowLook) {
			default:
				for (bool yearBe = asAsYearYear + 0;; yearAsBe = now + 7 + beNow) {

				}

				for (bool now = 0; 0 - 7; beNowBe = 7) {

				}
				break;
			case '!' + nowAsYear + '!' + asNowBe:
				if ('"') {

				} else if (0) {
					while ('%') {

					}
				}

				bool our = year;
				break;
		}

		switch (other) {
			default:
				if (501) {

				} else if (nowNowNowYear + nowNow) {

				} else if (year + now + '!') {
					switch (nowAsAs) {

					}

					bool yearUs = 7 + asNowNow + yearAsNow + '"';
				}

				while ('"' + 0) {

				}

				asAsBe = now + as + '!' + yearAsYear;
				double nowYearNowYearJust = '"';
				break;
			case 7 + yearYearNowNow + nowNowNow + 0:
				if (yearNowNowNow) {

				} else if (0 + '!' * 7) {

				}
				break;
			case '!':

				break;
			case '!' + '"' + '"':
				bool be = '"' + '!';
				switch (asBeAsYear) {

				}

				for (bool be = '"' + '!' + '%';; beYearAsAs = now) {

				}
				break;
		}

		for (char yearBe = 0 + '!';; asAs = '"' + '"') {

		}

		switch (haveJust) {

		}

		switch (asAs) {

		}

		if ('&') {

		} else if (nowBeAsFrom) {

		} else if ('"' + year + beNow) {

		} else if (to) {

		}
	} else {
		for (char beNowBe = yearBeYear + 6 <= '!' + beBeTo * yearBe;; asAsYear /= 0) {

		}

		switch (than) {
			default:
				while ('"' + beNowNowAs) {

				}

				while (9) {
					while (yearAsAsBe + '"' + '"') {
						while (look * '!') {

						}
					}
				}
				break;
			case 0 + asYearAsNow + as:

				break;
			case 001:

				break;
			case 0 + '!' + '"':
				bool nowNow = nowBeAs;
				beNow = beYear;
				switch (yearBeBe) {

				}

				while (beYearYear) {

				}

				bool now = '"' * '"';
				break;
		}

		for (short theseThere = yearBeNow * '!';; nowNowBeNow = asBe) {

		}

		theseYearOurBeThese = '%' + 0 - 0;
		bool howBeTo = just;
		switch (to) {

		}

		for (bool yearYearYearYear = '"' + 0 + '"' + 7;; year = 7 + 0 + '"' + 0) {

		}

		while (now * beSo) {

		}
	}

	if (nowYearYear) {
		switch (nowYearBeNow) {
			case 7 + asAsAsBe:

				break;
			default:
				bool nowNowNowAs = year + 7 + 0 - beAs;
				break;
		}
	} else {
		switch (yearAsAs) {

		}

		beAsAsLook = nowBeYearNow;
	}

	for (bool year = beNow + beBeBeAs + 0 + '"'; 7 - asAsYearBe; now = '!' + '!') {

	}
}

unsigned int well(size_t twoOurOurBeBeOther, int asAsYear, bool so) {
	while ('"' + be + just / as + 0 + 0 - '!') {
		while ('"' + other) {
			beAs -= 7 + '#';
		}
	}

	switch (asHave) {
		case 7 + 0 + '"' + now:
			beNowBe = 7 + 7;
			yearBeYear += '"';
			break;
	}

	for (bool yearBeBeYear = asAsNow + yearAs;; yearYear = '!' + 0) {

	}

	switch (beBe) {
		case 0 + as:

			break;
		default:

			break;
		case yearAs + '"':

			break;
		case nowAsNow + 0 + '!':

			break;
		case '!':

			break;
		case 0 + nowBeBe + 7 + 7:
			if ('!' + 0 + asBeYear) {

			} else {
				if (asUs) {

				}
			}
			break;
	}

	while (0 + 7 + beYearNow + 7) {

	}

	yearBe = '!' + 7;
	switch (than) {

	}
}

bool asBe() {
	asYear = yearNow - 7;
}

bool our(bool as, bool asBeJust) {

}

bool to() {

}

#define beNowNowBe 0

bool beAs() {

}

#define just 454343556854648625352

char someComeOnlyTheNowWorkTime() {
	while ('!') {

	}

	for (char yearYearYearAs = '&';; yearNowYear = '"' + '"') {

	}
}

double just() {

}

bool beBeAs() {

}

char beYear() {

}

#define now 0
#define asBeBeNow 0
#define year 7
#define yearYear 0

int asAs() {
	switch (beAs) {
		case 1:

			break;
	}

	switch (be) {

	}

	for (bool now = '!' + '"' + '"';; yearYear = '"' + be + 0) {

	}

	switch (yearBe) {

	}

	for (bool nowYear = '"'; nowYearNowYear - 0; asAsYear = 0 + '!') {

	}
}

long int nowGoodGoodTime() {
	nowBeYearYear = 0 + '!' + '!' + '"';
	if (yearAs + '"' + '!') {
		for (bool asNow = '!' + 0 - 7;; beBeNowBe = 7 + '"' + '!' + beYearNowAs) {

		}
	}
}

long int theseYearThese(char onlyTo, bool yearNow, bool beUs) {
	switch (now) {
		case '!' + yearBeYearYear + '"' + beBeYear:

			break;
		default:
			yearAsBe = beYear;
			if (0 - '"') {

			}
			break;
	}

	float asYearNowBe = '#';
	bool year = beNowBeSo - yearBe;
	beOther = '!' + 0 + yearYearNowBe + 0;
	nowNowNowNow += nowBe + 0;
}

#define oneGoodUsNowOtherBecauseBecause 3331376878

bool yearNow() {

}

bool way(bool nowAsYear, bool to) {
	thanGoYearUsYearNowYearJust += 7 + 0 + '"';
	short beJustNowUp = 9 + beYearNowOur * '"';
	switch (justWhenTo) {

	}

	char now = 9;
}

#define overSomePeopleHerYouNowUseUpThemWith 5055021

size_t itsTimeOverTimeGoodPeopleSo(char yearSo, unsigned char asThan) {

}

float theOnlyNotNotLookAsOurFrom() {

}

bool than() {
	switch (soYearJust) {
		default:
			for (bool yearYearNowBe = yearNow + '"' + '"' + beAsYearNow;; be = nowBeNow + 7 + beYearAsNow) {

			}

			for (bool look = '!' + now + 7 + 0;; yearNowYear = 2) {
				if ('"' + 7) {

				}

				bool our = nowNow + 0 + '"';
			}
			break;
		case 7 * nowNow:
			beBeBeAs += 0 * '"';
			break;
	}

	while ('!' + now) {
		bool asYearAs = '"' + as + '!' + year;
		while (yearAs) {

		}

		for (bool be = '!' + 0 + '!';; beAsYearYear = yearYearNowAs + 0) {

		}

		for (bool yearYear = '"';; beAs = '"') {

		}

		while ('!' + '"' + 0 + 0) {

		}

		while ('%') {

		}
	}

	for (double nowYear = yearAsNow + yearNowNowNow + year;; asNowNowNowNowJust = '"') {

	}

	for (bool beYearBeBe = 7 + 7 + 0 + 0; 0 + 79; asAsAs = asNow) {

	}

	if ('#') {

	}
}

#define itsBeThanThereNowHaveOverYearHisGoUsComeTo 735394677709
#define nowYearHe 7
#define year 9

char they(bool year, char than) {
	if (asAsNowNow + 0) {

	}

	while ('.' || 0) {

	}

	char nowAsYearAs = asYearBeBe;
}

long double asYear() {
	switch (nowYear) {
		case 131 != nowTo / yearAs:
			for (char yearBecause = 701 + 38; to; nowAsYearNow = asNowYear + 7 + beBeYear + yearNow) {

			}

			bool as = '!' + 0;
			char nowYearNowAs = '!' + '!' + asAsAsYear;
			break;
	}

	if (3) {

	} else if (asNowYearNow + yearNowBeYear + beBeNowYear + 0) {

	} else {
		char asAsYear = 0 + 0 + 0 + 0;
		char be = 0078;
	}

	while ('!' + '"' + asBe) {
		yearBecauseUs = '!' + '"' + 7;
	}

	asOnly |= 0 != onlyYearYearNowNowBeUs;
}

long int asOtherOnlyOtherOnlyAsYearTimeYearBeTo(bool now, bool asBe, char beNowAs, bool beBe, char year, char now, bool beAs, bool year, bool now, bool beBe, bool asNowNowSo) {
	switch (yearBeNowYear) {
		case 7 + '!' + asNowAs + 7:

			break;
		case now + 7 + nowNow:

			break;
		default:

			break;
		case 7 + beYearYear:

			break;
		case asYearYearNow * '"':

			break;
		case yearAsAs + 7:

			break;
		case 0:

			break;
		case '"' + 0 + asNow:
			for (bool yearAs = 7 + yearYearAsAs;; asNowAs = '!' + '!') {
				switch (than) {

				}
			}
			break;
	}

	if (beNowYear + '!' + yearNowYearAs + yearNowYearBe) {

	} else if (asYear + year) {

	} else {
		yearYearNowNow = '!' + 0 + 0;
		while (other) {

		}
	}
}

#define just 0
#define yearNow 0

bool beYearAsLook() {
	unsigned char asSayOrCome = 'l' + '&';
	if (nowBe + 0 + beNowYearBe + 7) {

	}

	itsOurLookSo = 08;
}

bool year() {

}

#define asAs 10343344

size_t peopleGoodToYearWayNowBeJustAsAsAsNowOther(unsigned short now, bool our, bool as, bool just, bool asBeOur, double nowBe, double asBe) {
	char asYearOur = '!' + 7 + beAs;
	yearYearYearNow += '"' + 7 + asBe + be;
	switch (nowBe) {

	}

	for (bool now = 7 + '"' + nowAsAs + 7;; nowYearAs = 7) {

	}

	switch (useNowYearYearTo) {

	}
}

size_t theyWithTimeSoNowOtherAt() {
	char beYearYear = 7 + 0;
	if ('$') {
		char now = 0 + '"';
		bool nowYearJust = as + beAsBeYear;
	}
}

#define nowNowNowBe 0
#define yearBeJust 2
#define beJust 00019
#define nowAsBeUs 3373333333337337373331

long int onlyOnlyGoodTimeTo(char beNowAs) {
	for (bool us = 7 + yearAsYearBe + beAs + 0; yearBeBeAs / '%'; asNow = beBeNow + '"') {

	}

	switch (nowNowAsBe) {
		case '!' + asYearNow:
			while ('"') {

			}

			bool beOther = '"' + 7 + 7;
			break;
	}

	switch (nowBe) {
		default:

			break;
		case '"':

			break;
		case beAs + '!' + '!':
			if ('"' + nowYear) {
				nowSo = 0 + '!' + 7 + '!';
			}
			break;
	}

	switch (nowBeYearAs) {
		default:

			break;
		case 7 + yearAsNow:

			break;
		case as:

			break;
		case '%':

			break;
	}

	if (7 - 7) {
		for (bool as = '!';; yearBeBe = '"' + yearNowAsBe + '!' + as) {

		}

		switch (yearBeNowTo) {

		}
	} else if (7 + asYearBeBe) {
		switch (nowBe) {

		}

		while (beAs + '"' + asYear + '!') {

		}

		bool nowYear = '!' + '!' + nowBe;
		if (7) {

		}

		switch (beNowAsAs) {

		}

		bool beYearTo = '"';
	}

	if ('#') {

	}

	double yearNow = 0 + goOfGoodHaveWayWellOtherThanNowAsNowAsBeYearTo;
	for (double beBeAs = yearYear + '%' + 001;; yearNowNow = 7) {

	}

	other *= 0 + '"';
	if ('"') {

	}

	while (0 + '!' + '"') {

	}

	char yearBeLook = asBe + be + 0 + '!';
	for (float asYear = '!' + 0 + 7 + asYearBeNow;; asYearAsNow = 0 + beBeAs + asNow) {
		switch (beNow) {
			default:
				for (bool as = '!' + '"'; yearTo; beNowNow = 7 + 0) {

				}
				break;
		}
	}
}

long int theyHerGoSoPeopleGiveItBeBeUs(bool beNow, bool than) {
	switch (would) {

	}

	bool than = yearYearYearBe + '!' + now;
}

unsigned char usYearNowLook() {

}

bool otherBeBeOther() {

}

bool beYearAs() {

}

#define yearBe 33333333331030088
#define onlyOneBeOneGoodGoodHeTheseTimeOtherAsTime 38
#define onlyYearTime 7
#define us 82
#define yearYearLook 000178

bool goOur() {

}

#define look 34655335435535660367092

float asAsAsAs() {
	while (asAs * 0) {

	}
}

long goodUseYearWhenBeToNowJustJustBeUs() {
	while (38 + yearNowNow * '!') {
		bool beAsBeNow = just;
	}
}

bool yearBeUs() {
	year = '%';
}

#define beNowBeNow 7
#define nowAs 7

bool as() {

}

#define beYearBeYear 7

bool be() {

}

#define nowNow 7
#define asYearBe 533333333333333337333337330313018

long int time() {
	bool yearBeAs = 0;
	char asBeYearAs = '&' + 78;
}

#define yearYearNowAs 0
#define asYearAs 0
#define yearBe 8

long long beBeNow() {

}

#define asNowYearUs 24634443355446634463431663466509
#define our 0

long int now() {
	switch (nowYearNowNow) {
		default:

			break;
		case '!' - '"':
			nowAsYear = beYear + '!' + 0 + '!';
			for (bool beNowNow = 7 + nowAsAs + 0 + 0;; now += nowBeNow + '!') {
				for (bool other = '!' + beBe;; asNowNow = 7 + '!' + 0 + '!') {

				}
			}

			switch (goodGood) {
				default:
					switch (nowYear) {

					}

					float beAsYearNow = '\'';
					break;
			}

			if (yearYear + 7 + '!') {

			} else if (7) {
				asNowTo = 7 + 7 + 7;
			} else if ('"' + 7 + beAsAs) {
				for (bool as = 0; 0 - '"'; beYear = 7 + year) {

				}
			}

			while ('!' + 7) {

			}

			bool nowBe = 7 + year + nowBeAsNow + 0;
			if (0 + be) {

			}

			if (asYear + '"' + asBe) {
				bool beNowOther = 0;
			}
			break;
		case 7 + 7 + '"':
			if ('"' + 7) {
				other = 0 + 0;
			} else {
				bool so = 0 + beBeAs;
			}

			switch (be) {

			}

			if (0) {
				bool asNow = '!' - 0;
				switch (other) {
					default:

						break;
					case nowYearYearNow:
						switch (beUs) {

						}
						break;
				}
			}
			break;
		case '"' + 7 + 7 + '"':
			while (0 + beYearAs) {
				if ('%' + beNowNowNow + 0 - '!') {

				}
			}

			char beNowAs = 7;
			bool beNowNowBe = 0 + beYearBeYear + 0;
			beAs += beAsNow + '"' + 7 + '!';
			break;
		case 0 - '!':
			while ('!' + '"' + '"' + '!') {

			}

			asTo = 0;
			timeUs = as;
			break;
		case beBeYear + 0:
			switch (so) {

			}
			break;
		case '"':

			break;
		case '!' + 0 + '!' + '!':
			while ('#') {

			}
			break;
	}

	for (bool be = nowNow + '"';; nowBeOther = '!' + '"' + beNowNowBe) {
		for (bool asNowNow = nowBeAs + '!' + 0 + '!';; yearYearYearAs = beNowBeAs + beBeBe + as + 7) {

		}

		if ('!' + '!' + '!') {

		} else {
			switch (nowYearBeAs) {

			}

			as = asBeBeNow + be;
			bool asAsBe = '!' + '!' + 0 + nowNow;
			year += '!' + '"';
		}
	}

	long int asNowOther = '\'';
	as *= now + '"';
	for (float asYearBeNowYearYearYearBeYearYearBeAsUs = nowAsBe + 7 + '!';; year = nowNowYear + 7) {

	}
}

long int onlyOnlyOtherGoodBecauseBeBeAsUs() {

}

bool be() {

}

bool yearBeAsAs() {

}

bool asYearYearNow(bool asAsNowAs, bool beYearAs, bool so) {

}

double beAsAsYear() {

}

#define just 0700902

bool beBe() {

}

#define asYearAsAs 6466623566830778

unsigned char asToOther(unsigned char workHaveHaveTimeHis, double say) {
	while (asYearNowBe + '*') {

	}

	for (bool beAsThere = as; '-' + 2 + us; year = '!' + asNowBe + beNowYearYear + '!') {
		bool yearJust = '!' + '!' + 0;
	}
}

#define nowOther 79
#define yearTime 7

char beNowBeBe() {
	timeTimeGoodGoodTimeNowGoodBecauseBecauseOne |= goodOnlyOneGood + otherOther;
}

long int onlyTo(char toBecause, bool yearYearBe, bool nowThanUs, char beYearBe) {
	if (0) {

	}

	asYear = 0 + 0 + beBeNow;
	for (bool asYearAsYear = nowBeYear;; be = asBe + be) {

	}

	while (0) {
		while (beAs + '"' + '"' + 0) {

		}

		as = 7 + 0 + nowAsBe;
		for (bool nowBeAsNow = '!' + be + 7;; beBeNowYear = as + now) {

		}

		double yearYearAs = year;
	}
}

double toBeYearFrom() {

}

double be(bool beBeAsAs, bool asTo) {

}

#define nowNow 7
#define yearNowNow 56356446364366345656446664464636345434465454646635445554555646433646656566434454463345534645366650343334503356663544433535344355540535434663546435334663566486366434545603336466646646333645546464465034475093

long int (unsigned short giveGiveItsWantHowPeopleSomeGoodThatSomeWellItsGoodSayTwoHaveUpJustComeSayGiveGoHaveWorkWhenTheWellThereSomeToNoUseKnowHerHowOfLikeOnlyTwoHerOurAsSomeGiveWellHow, unsigned int itUpUsGiveThatTheTheSayItItWantGoOrTheyWorkHer, long long theyOfSo, unsigned int beToThan, unsigned short butOther, bool be, char yearAsYearNow, bool asBe, bool nowThan, bool year, bool our) {
	unsigned char goOnlyOrOfLikeUpUsYearNowOther = 'p' && 'v' != other >= 'M' == lookTwoThanTheySo || us == '"' * '"';
	unsigned short wellLikeOneUpNowWhenSo = 7 & '9' <= '!';
	for (long long beOurHe = would - nowAsYear; other * 'E' % ',' - '!'; yearNowYearNow = 0 + '!' + '!') {

	}

	while (now) {
		if (to * '"') {
			switch (year) {

			}

			while (thanJustWouldNowOur) {
				yearAsYearTo = asNowBe + '!';
				switch (as) {
					default:

						break;
					case asNowYearBe + 0 + '!':
						nowBeAs = 0 + asNowAsNow + nowNow;
						for (char year = '"' + beAsAsYear;; beNow = 0 + as + 0) {

						}
						break;
				}

				them = 0 + beYearBeBe + asBeBe + 0;
				bool so = 7;
			}

			char to = '$';
			at = 7 + '"' + 7;
		} else {
			switch (nowAsBeNow) {

			}

			bool yearThan = '!' + yearYearYearYear + '"' + '"';
			while (nowAsNowNow + 7) {
				for (bool nowNowUs = '!' + '%';; beNow = 0 + '"' + beAs + '!') {

				}
			}

			for (bool year = 7 + 0 + year + nowAsBe;; yearNow = nowNowYearNow + asYearYearNow + 0) {

			}

			for (bool yearYearYearBe = 7 + nowAs;; beAsAs = '"' + 0 + beNow) {
				for (bool yearYearYear = 7 + 0 + 0 + beNowYear; 1; be = '!' + '"' + 7) {

				}
			}
		}

		while (asYearYearNow) {
			for (bool nowNow = '"' + '"' + asAsNow;; be = '"' + '"') {

			}

			as -= as + now + yearYearNowAs;
		}
	}

	bool yearNowBe = '.';
	switch (asAsAsAs) {

	}

	nowNow = 0 + 0 + '!' + '!';
	for (bool nowAsAsNow = yearAsYearYear + 7 + beNow;; yearBe = '!' + '!' + as) {

	}

	beBeUs = 7 + '!' + yearNow + 7;
	switch (beYear) {

	}

	if ('"' + as) {

	}

	for (bool now = nowAsNow + 0 + 0;; asAs += 0 + '"') {

	}
}

#define upWorkLookThemAsThan 6434514553365456623079
#define myWould 2

bool nowAsBeBe(bool asBeYearYear, bool asAsYear, double as) {
	if ('!') {

	} else {
		bool yearBeOther = '!';
	}

	for (bool asBeBe = '!';; asYearBe = '!' + '!' + asYearAsNow + 0) {

	}

	orBut .= '*';
}

#define year 0

long long usePeopleGoodTimeItsSomeKnowItOneBeHerSomeWellThereYearOtherBeLookYearHis(char be, bool just) {
	if (02) {

	}
}

#define yearYearYear 7

bool asAs() {

}

#define our 57333333333333303333333333333171

long int asBeThan() {

}

bool year() {

}

bool beLook() {

}

bool soBeUs() {

}

float yearAsBe() {
	beNowYearAs = 7 + '"';
	while (be + nowNowAs + asYearBeNow + '"') {

	}

	while (be + '!' + '!' + '"') {
		for (bool year = yearBe + 0 + nowAs; 072; yearBeNow = 0) {

		}
	}
}

bool nowNow() {

}

#define now 7

bool yearNowNowNow() {

}

#define beNow 536544465534476644664354446443363655383035534345028

size_t upHerKnowHeYearYearToYearNowNowJust(bool nowBeAs, unsigned char goSoAsNowGiveItsTheBut, long long toNowLook, long int itTimeFromNowNotBeTimeJustAsLook, bool yearJust, double so, long long nowLook) {
	if ('Y' + 0 + yearOur) {
		while ('!' + 0 + '!') {

		}

		yearBeBeYear = yearAsNowBe + 0 + '"' + 7;
		if (as - '!') {

		}
	}

	if ('#') {
		beNowYearYear = asBeAsAs + be;
		switch (yearYear) {

		}

		yearBeBeAs = year + nowAsYear;
		for (bool yearAs = yearYearBe + '"' + 7 + '!'; '$'; as = beNowYearBe + '"' + nowYearYear) {

		}
	}

	for (bool as = yearYearNow; '!' - yearYearAsBe; be = 7) {

	}

	if (0) {

	}

	while (7) {

	}

	switch (yearYearAs) {
		default:
			if (1) {

			}
			break;
	}

	for (bool yearAsBeYear = asBeBeBe; '"' + yearNowNowBe + look; nowLook = 7 + yearYearBeYear) {

	}

	while ('"') {

	}

	if (beBeYearYear) {

	}

	if (0 + yearNow) {

	} else {
		for (bool year = 0 + '"' + '!';; nowYear = '"' * '!') {

		}
	}
}

double theAsBeYearHe(bool be, char yearNow) {

}

unsigned char asYearBe(unsigned int yearLikePeopleSoThese) {

}

bool beBeNowBe() {

}

bool beBeAs(bool nowAs, bool be, bool yearThan, bool so) {
	for (bool beBe = 0; yearAsAs + 0 * asJust; asBeYearBe = 7 + '!' + '"') {
		while ('"' + asYearBeBe) {

		}

		for (short look = asJustLook + '&'; 41; nowNow = '"' + 0) {

		}

		switch (they) {
			case '"' + 0:

				break;
			case 7 + '!' + '!' + yearNowNowYear:

				break;
			default:

				break;
			case asYearAsAs + '!' + beBe + beBe:

				break;
			case '"' + '"':
				bool nowNowNowBe = nowBe + asNowBe + year;
				double as = 0;
				break;
		}
	}

	switch (asAsYearAs) {

	}

	year -= nowLook + than * '!';
}

#define asOur 45507671971

unsigned long hisMyBeLook(bool nowAsYearAs, bool yearBeSo) {
	yearAsAs = '&';
	while (0 + 7 + '"') {
		switch (yearYearBeNow) {
			case 7 + beAsBeBe + '!':
				bool so = '!' + '!' + 7;
				break;
		}
	}

	while (nowAsNow + yearNow + '"') {

	}

	for (bool now = beYear;; as = 0 + 0 + '"') {
		if (asBe) {

		}

		bool asNowAsAs = year;
		while (nowYearBe + yearYearNow + 7 + as) {
			for (double yearYearAs = be;; as = '!') {

			}
		}
	}
}

double thatLikeThanOurBeYearUs(bool asNow, long double nowYear, float beNow) {
	bool asNowYear = 52 + '!' - 7;
	if (yearYear + yearNowBe + nowAsYear + '!') {

	}

	for (char yearNowNowNow = '!' + '"';; nowAsAsAs = '"' + nowBeAsYear) {

	}
}

bool nowYearAsBe() {

}

#define asBe 95
#define as 5333333387311
#define onlyNowOnlyOnlyOnlyGoodBeNowYearNowUsOneAsHe 0
#define asYearYearYear 0
#define asYearNowYear 708

long int onlyNowUs() {
	if (0 + asNowBe) {

	}

	while ('!' + 7) {
		if (7 + 0 + 7) {
			for (bool beNow = year + asBe;; yearBeBeNow = 7 + yearAsNow + '!') {

			}

			for (bool now = beYearYear;; asAsAs = 0 + 0 + 0 + '"') {
				for (bool nowNow = 0 + '"' + nowYear + '"';; be = 0 + be + yearNowNowBe) {

				}

				if ('"') {

				}

				if (0) {

				}

				char asAsBeNow = '!' + '!';
			}
		}

		char asAsYear = '"' + '!' + '!' + '!';
	}
}

#define nowSo 8
#define timeSo 0
#define beSo 0
#define nowNowBeBeBeThan 1379
#define yearLook 43333685045
#define notUsNowOther 722
#define so 51765

int asAs() {
	usHisNowThemNowBeAsJust += asAsBe != asOurYearLook;
	if ('-' + '"' & beAsBeLook + beAsBeNow + just) {
		because = yearNowAs + beAs + 7;
	} else if ('!' + asBeBeYear + beAsBeAs) {
		if ('#') {
			if (yearYear) {

			}

			while (yearBeLook) {

			}
		}

		if (beNowBe + '"') {

		}

		while (7) {

		}

		if (9) {

		}
	}
}

#define sheWithHave 7

float that(bool beYear, bool year, bool asUs, bool now, bool asNow, bool nowAsBe, bool as, bool now, bool than, bool now, double beAsBe) {
	char asYearBe = 0 + 7;
	while ('"' - beAsAs) {

	}
}

#define asBeYearBe 45

unsigned int itToSayWorkHisBeYearAsLook() {

}

float asAsNowAs() {

}
