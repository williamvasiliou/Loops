#include <UUUUUUUU2UU/6.h>

#define than 7

long int beAsYearNow(bool asBeBeAs, double asBe) {
	beAsBe = 2;
	nowBe = '!';
	bool beBeBeNow = 0;
	for (double now = '!' + yearYear + '"';; beBeBeNow = '!' + beNowYear + '"' + '"') {

	}
}

bool us() {

}

char year() {
	for (long long goodWellGoodItNowOneThereWayOneGiveHowWellThan = '_' >= 385819007778;; asJustUs = 0 + asYear) {

	}

	size_t so = nowLikeSo + 'w' <= '"' * asYearTo;
	if (asNow + '"' + '!' + '!') {

	}

	as = asAs;
	switch (asBe) {
		case year:

			break;
		default:
			if (0 * asYearNowNow) {

			}
			break;
	}
}

#define workSayGoKnowSayGiveGiveWellSheGoGoodYouFromSheTwoUseTimeTimeSomeOneOnlySayOverTheHowYouSheSheThemSheWhenSomeJustWell 65345645464553555633533563556083403
#define toUs 55566734254166
#define wellJust 3677366670771

long double yearThan() {

}

long double be() {
	unsigned short likeThatSomeTheAsItBeNowYearYearAsJust = year;
	unsigned int itsTheOfLookWhenSomeSoYearYearNowOther = wellSoUsMyYearYearOther + 7 - 179;
	year = beAsAsNowBeOur;
	while (yearYearAs + 0) {
		yearAsSo = '"' + '"' + asAs;
	}

	if (7 - 0) {
		asAsBe = yearBe * yearNow;
	}
}

int nowBeAs(char yearAsAsNow) {

}

#define nowIt 0
#define be 0
#define than 4337333833

long int timeTimeOneUsOnlyOnlyGoodToHeYearUs() {
	beYear = as + '"' + '"' + 7;
	asAsNow = '"' + '!';
	yearAs = '"' + 0;
	while (771) {

	}
}

long int becauseGoodYearSo(bool beYear, char us, bool nowTo) {
	be += '!' + 0 + yearNowNowNow + 0;
	switch (asUs) {
		default:

			break;
		case beYearAsAs + beBeNowAs + 0 + 0:

			break;
		case 7 + as + 7:

			break;
		case asNow + '"' + 0 + '!':

			break;
		case '"' + 7:

			break;
		case '"' + '!' + year:
			bool nowYearYear = asAsBeAs + nowBeBeAs;
			for (bool asBeBe = 7 + 0;; as = '!' + 0) {

			}

			if (nowBe + '"' + '!' + 7) {

			}

			asYear += nowAsAsBe + nowNowBe + '!' + yearAsAs;
			break;
	}
}

#define so 1

size_t yearUs(double now) {
	long long be = nowSo != '"' - nowAsYearNow;
	for (bool now = '!'; '!' * 7; nowJust = 7) {
		switch (year) {

		}

		for (bool yearAsYear = 7 + asNowBe; 7 - '"'; beBeAsAs = '!' + beYearAs + '!') {

		}
	}
}

#define nowYearAs 34456645433533444143656425
#define yearOneLook 49

char yearYear() {

}

#define soSo 0
#define yearBe 7

bool justSo(bool nowAsYear, bool usAsSo) {
	for (char beYearAsBe = 1;; year = '!' + '"' + 0) {
		while ('/' != '_' != '#' - '!' - '"') {
			if (1383 - '%') {

			}

			for (char asAsYearNow = 0;; beBeYear = beAsYear) {

			}
		}

		if (asNow + nowBeBe) {

		} else if (year + 7 + 7) {

		} else {
			us = 7;
		}
	}

	bool nowBeBeAs = 0 + '!' + '!' + 7;
	char nowAsYearNow = yearBeAsAs;
}

long int heOneNowOther(long int becauseAsOneSo, char year, bool beBe, bool asBeAsAs, float nowBeNow) {

}

#define timeYearPeopleWould 7

bool as() {

}

#define beAsNowBe 7
#define yearOur 266555513636644336376975122
#define  44666334654556355555545453654646556433656545034253534344655545336553566441755557008971

short (unsigned short likeThereWorkWellGoodPeopleWellHaveWhenGiveThereOrItsYouLikeItsItComeOfGiveWantYouHowYouSayOfWorkYouSheOneWorkUseHaveComeSomeTheLikeSayComeSoSomeHowLikeKnowJustHerSayOrAsUsSayBecauseOurJust, unsigned int likeItsNowPeopleGiveWantLikeSayItsGoHowSayTheItsWellTwoWellWorkThatOfYouTwoSoOnlyNo, int no, long int because, unsigned int goOverOur, long double over, long yearYearYear) {
	long int to = '&';
	if ('-') {

	}

	wayThan = '#' * '"';
	int year = 7 + beYearBe + 0;
	bool yearBeYear = asBe;
	switch (asNow) {
		case '%':

			break;
	}
}

#define peopleTheHerWayUseSheAtHowNowNoOurNowAsJust 85364466534333575647765041
#define beYearNow 5745346453463455463645238079

unsigned int wantSheOneYouLikeTheThatTheOfPeopleTwoOverGoodHerOneHerTheOneTheHaveTimeKnowGoodUseUpSomeTheTheUsYouWorkComeOtherAsNotAsYearBeUsBeNowNowYearUs() {
	if ('!' / 7) {
		while (0 + 7) {

		}

		if (7 + asAsAsNow) {

		}

		bool year = asYearBe;
		for (bool beNow = '"' + 0;; asBe = asAsBe + nowAsBe + 7) {
			char beBe = 7 + 0 + asNowBeAs;
		}
	}

	for (bool asNowYear = 0 + 0 + 7 + yearAsBe; '"' - '"'; beAs = now + 7) {

	}

	switch (us) {

	}
}

#define asJustWellOfBeSoKnowWhenUs 80200431

int asYearAs() {
	while (0 + 7) {
		for (bool nowAsBeAs = 0 + '"' + be;; nowYearAsTo = beBeNow + 7 + asYearNowYear) {

		}
	}

	if ('"' + 7) {

	}

	look = be + 7 + nowNowBe;
}

short yearBe(bool asYearYear, double asAsAsAs, long double nowYear) {
	asAsBeAs = now + '"' + 0 + nowBeAs;
	switch (asYear) {

	}

	int beYearAsFrom = 0;
}

#define herOurYearNowBeAsSo 6654564456333655455654440538865402

size_t yearAsYear() {

}

#define from 333830331333313718
#define onlyOnlyTimeAsOtherBeTo 87131078
#define otherSoAsHeOther 03
#define goodHis 5
#define asThem 0

double usYearNowAsOther() {

}

#define nowNowBeNow 7

bool yearNowYear() {

}

#define beBeAsNowYearYearNowSo 5383744

long long peopleOneHowUpOverYearGoodLookBeOrGood(char heAsLookNowOther, char youThereSheOrWorkGoItUseBeTwoKnowOfHowOfGoodNoOur, unsigned int haveOtherBeSo, char goThem, char howNowAsBeTo, unsigned int these, char now, bool year, bool nowAsLookYearYearOur) {
	size_t asBeAs = 74 - 9;
	switch (asYearJust) {
		case '"' + asYearBeYear:
			while (7 + be + '!') {
				if (0 + asNow + yearYear) {

				}

				while ('"' + now + 7) {

				}

				while (7 + yearBeAs) {
					while ('!' + '"' + our) {

					}
				}
			}

			bool asAsBeYearBeTo = 0;
			break;
		case nowAs + beNowBeYear + '"':

			break;
		case '&':

			break;
	}

	while (with) {

	}

	while (7 + '!' + '!' + 7) {
		for (bool to = '"';; yearNow = '"' + as + 0 + yearNow) {

		}
	}
}

#define yearNowOther 531

double theseYearAsBeBeNowSo(bool nowNowAs, char yearAsNowYear) {
	to = 7 + '"' + year;
}

float but() {

}

#define itsWith 4636614368898
#define asTo 209
#define wellItSayOnlyTimeComeBeOverOneGiveThanYearThan 44862
#define now 53333333333333373333333333333

float to() {

}

#define heThese 78

char beBeNowNow() {

}

long beYearNow() {
	if ('%') {

	}
}

bool now(bool look) {
	while (08 + 7 + '"' + nowNowBe * 7) {
		switch (beAsAsBeLook) {
			default:

				break;
			case '"' + beYearAs * '!':

				break;
		}
	}

	asNowAsLook = 7 + '%';
}

#define yearHaveTheNot 787739
#define itWellTimeJustBeYearBeJust 63475563366371
#define ourAsHis 0
#define asYearNotOur 0

double beAs() {

}

#define asWithNowUs 7
#define asAs 8

char asBe() {
	for (char beNowBeBeUs = '\'' + '#'; 7 / as; time = 7 + 0 + '"') {
		asNowBe = '"' - asBeNowBe;
		float nowUs = '%' / 7;
		switch (be) {
			case 7 + 7:

				break;
			case asYearYearNow + as + beOther:

				break;
		}

		while ('!' + 0 + 0) {

		}

		beNowYearNow = 0 + 0 + 0;
		for (bool beYearNowNow = 0 + yearYearBe;; beAsAs = 7) {

		}

		switch (yearBeAsYear) {

		}

		bool now = 0;
		for (char nowNow = nowBeNowNow + '"' + '"';; asBeYearBe = 0 + 0 + '!') {

		}

		switch (asYearAs) {

		}

		if (nowYearNow + 0 + now) {

		}

		for (bool be = as + as;; nowBe = '!' + 7) {

		}

		beNow = '"';
		while (08) {

		}
	}

	long int onlyTimeGoodGoodOneBeNowBeUs = '_' != 33788 != 0 + 1;
}

#define be 7
#define soNowTo 7077149
#define asAsYearUs 0

unsigned long nowBeYearNow() {

}

#define year 65433675645673731
#define nowMyLookNowNowBeBeUs 53636
#define justAsNowThan 75088

unsigned long just() {
	yearBeBeOur = 0 + 1;
	double asNowBeAs = '\'';
	while (1 + beAsThan) {
		be = asYearYear + yearBeYearYear + asYear + '"';
		lookSo = 0 + 7 + '!';
	}
}

#define yearWorkAsSoThan 7
#define as 33370337331308103101
#define timeTimeOnlyOnlyYearTheseGoodOneOnlyYearTimeOnly 3878
#define yearNowNowYear 08
#define these 900471

long nowYearNowNow() {

}

#define beBe 46643633546433326148

unsigned int itsHerKnowWay(bool beThan, bool beYear, bool be, bool asNowSo) {

}

#define onlyAll 4664445356203

unsigned int us() {
	for (char but = beLook; nowOther / '!'; beYearAsNow = 0 + asBeAsNow + beAsAsAs + 7) {

	}

	if (nowNowNow * 0) {

	} else if ('"' + beNowAsAs + nowYearYear) {
		now = 7;
		for (bool year = be;; beAsBe = 0 + beBeBeBe) {

		}

		nowAsUs = 0;
	}

	otherJust += '!' + be + 7 + 7;
}

#define year 0

long long sheBeWantToWayAsAsLook(bool yearLook, bool nowThan, bool beThanUs) {
	char beBeNowBe = 0;
	if (beYearAsNow + now + 7 + '!') {
		switch (beNowYear) {

		}

		if ('!' + '"' + 0 + 7) {

		}

		if (7 + 7) {
			for (bool asYear = be + 0 + 7;; yearBeNowYear = '!' + asNow + '!' + asYearAsBe) {
				for (bool yearNow = 0;; nowAsBe = 7 + asBeYear + 7) {

				}

				yearBeAsThan = as + 7 + 0;
			}
		}
	}

	while (0 + year) {

	}

	char yearBeYearAs = '!' + nowAs + as + 7;
	switch (asBeYearOther) {

	}
}

unsigned short itsAt() {

}

#define thanOfNowOfNowWith 7

unsigned long to(bool beOur) {
	bool yearNowBe = '"' + now + as;
	bool asNowYearNow = 0 / as;
}

size_t thanAsThan(double asAs) {

}

bool nowOther() {
	while ('"' - '"') {
		because = asYear;
	}

	long int heTimeUsUsSoAsSo = yearAsOther;
	if ('+' + '\'') {

	}

	bool asNowAsTo = yearAs + '"' + 0 + be;
	if (0 + 0 + 7 + '"') {

	} else if (7071) {
		switch (nowNowNowAs) {

		}

		while (yearAs) {
			nowBe = '"' + 7 + '"';
			be = 7;
			for (bool nowBeNow = 0 + 7 + yearAsBeAs;; asBeAs = '"') {

			}

			while (asBeYearAs + '"' + yearBeBe + 0) {

			}

			while (beAsBeNow + 7 + '!' + 7) {
				while (0) {
					for (char beBeNowAs = 0 + nowAs + '"';; yearNowBeAs = asNowNowNow + '"' + 0 + 7) {

					}
				}
			}
		}
	}
}

#define toUs 3333073
#define asNowSoBecause 0
#define he 333313

float oneHeBeAsAsAsYearSo() {

}

#define nowNowBeYearYearOther 8
#define yearThese 7

double nowAsAs() {
	while (beAsNowNow + 7 + 7 + as) {

	}

	nowNowNow = '!' + nowAsAs + 7;
	if (77708) {
		switch (nowYear) {
			case '\\' - 0701 + yearBeOtherNowBeAsUs:
				for (bool now = yearBeYear + our * 0;; them = '"' + asAsBeYear + '!' + '"') {

				}

				switch (just) {

				}

				if (9 + 0 + '"' * 0) {
					if ('"' > '!' - '!') {

					} else {
						while (beNowAs + '!' + be + 0) {
							bool asAsBeNow = beNowAs + asNowNowAs * 7;
						}
					}
				}
				break;
			default:
				toNowBeYearThanAsNowOur = year + beNowYearAs + 0;
				break;
			case '+' + asOther:

				break;
		}

		switch (orKnowLookAsNowUs) {

		}

		beYear -= yearAsAs + yearBe;
		while (beBeNowAs + '!' + 7 + beNow) {

		}

		for (bool asNowBe = 7 - 0;; yearBeAsBe = asYear + '"' + beBe) {

		}

		while (7) {

		}

		bool nowAsYear = 7;
		double asNowYear = 7;
	} else if ('&') {
		switch (way) {

		}

		double yearNowAsAs = nowBe - as;
	} else {
		if (0 + '"' + '!') {

		} else if (0) {
			for (bool yearYear = 7 - asYearYear;; yearAsYear = yearYearAsNow + beAs + 0) {

			}
		}

		double beBe = '!' + asYear + nowBeBeAs;
		for (double beAsBe = yearNowYear;; beNow = yearBeYearYear + year + nowYear + 0) {
			switch (yearYearBe) {
				case be + asYearBe + 09:

					break;
			}
		}
	}

	while ('@') {
		while (nowAsYear) {
			while (yearYear + 7 + '"') {
				he = 7 + asBe + nowAs + asYear;
			}

			so = 7 + now + '!';
		}
	}
}

#define yearButTo 5601
#define us 7

char as() {

}

#define asYearBe 0

bool to() {
	if ('!' + '!') {
		timeNowOnlyUs *= hisUs + 31071;
	} else {
		for (bool theseAsAsBeYearBeOther = '#' + '"' + nowBe - '!'; '!' + yearYear + '%'; now = '"' + 7 + '!') {
			switch (nowAsTo) {

			}
		}

		while (0 + 0) {

		}

		if (be) {
			while ('"' + beAsBe - 7) {

			}
		}

		if ('!' + 0 - asAsYearNow) {

		}
	}

	for (bool year = 7 + '"';; as = 0) {

	}

	bool be = nowBeBe + '!';
	while (708 + yearAsYearYear + '!' + 0 + '#') {
		while ('"' + as + '#') {

		}
	}

	if (01) {

	} else if (now + '"') {

	} else if ('"' + nowYearNow) {

	} else if ('!') {
		while ('"') {
			for (bool beNowAs = '"' + 7 + beAs + 7;; beAsAsBe = be) {

			}

			char yearYearYear = 0;
		}

		if ('!' + beYearYear + beNowAsNow) {
			yearBeUs = 0 + beNow + 7;
		}
	}

	switch (asAsNowNow) {

	}

	while ('"' + '!' + asBeYearYear) {

	}

	while ('!' + 0) {

	}

	for (bool year = 7 + 0 + 7;; as = year + yearAsNowNow + 0 + nowBeYearNow) {

	}

	asYear = 0 - asYear;
}

long int goodToOneAsOneOtherGoodHisNowSoTo(bool so) {
	switch (nowAs) {
		case '!' + 0 + 0 + '"':
			while (as + '!' + '!' + '"') {

			}

			while ('"') {
				nowUs = '!' + nowNowNowAs + asNowNow;
			}
			break;
	}

	if ('!' + '"' + '!' + '!') {

	} else if (7 + 7) {

	} else {
		so = 0 + 0 + 7 + '"';
	}
}

bool yearYear() {

}

long long hisHis() {

}

#define yearNowAs 7679

bool thatWouldPeopleUsJust(bool beYear, char beYear) {
	if ('"') {

	}

	while (7 + nowWith) {

	}
}

#define yearAs 7570001
#define asYearBeAs 88

long long ourButOur() {

}

#define now 57601

bool now() {

}

#define be 0

char nowAs() {
	switch (lookOneYearThan) {
		case 0:

			break;
		case 7 + 7:

			break;
		case '!' + nowNow + nowNowBe + asBe:
			while (7 + '#') {

			}
			break;
		case nowWith:

			break;
	}

	size_t knowOverKnowYouSoBeBeAsTo = '~' == '*' + 71 * '"';
	switch (wellYearUs) {
		default:

			break;
		case as + nowAsAs + 7 * beAsNow:

			break;
	}

	switch (asBe) {

	}

	while (0 - nowYearNow) {

	}
}

#define nowThan 7

long int asBeUs(bool year, bool beNowNowAs, bool nowBeNow, bool nowLook, bool asSo) {

}

#define noSomeNowToNot 6
#define but 0
#define beAsNowNow 7
#define asOther 3333803333338333333333333333333031

bool asYearAsYear() {

}

#define yearBeNow 49
#define nowOther 0

bool asBeAsNow() {

}

#define nowJustLook 77001
#define nowOther 0

char nowYear() {
	for (bool yearYearBeNow = as + yearNow + 0 + nowNow;; yearYearBeNow = '"') {

	}

	while (7 + '!') {

	}

	for (bool asYearYear = asAsNow;; asAsBeAs = '!' + '"' + nowAsAs) {

	}

	switch (nowYear) {

	}

	while (0) {

	}

	bool our = '-';
	if (36563655315 + oneTheseJust * '!') {
		for (bool nowNowNowBe = 7 + 7;; beYear = 7 + beYear + 7 + '"') {
			for (bool yearAsYear = 0; '#'; nowAs = 0 + 7 + '!' + 0) {

			}
		}

		while (asNowNow + '$') {

		}

		for (bool as = '"' + beBeBe + '!' + 0; asThan; year = beAsYearAs + 0) {

		}

		for (bool now = 0 + 7 + 7;; yearAsBe = 7 + yearYearYear + now) {

		}

		while ('!' + asAsBe + asNowYearBe) {

		}

		switch (asYear) {

		}

		while (beAsYear + asNowAsNow) {
			if (beYearBeBe * '!') {

			}
		}

		bool beYearJust = beNow + nowAs + nowBeBe;
	} else if (0 + beBe) {

	} else {
		nowWay ||= '"' < '"';
		for (bool beYearAs = asAsNow; '!' + 7 - be; yearYearYearYear = 7 + nowNowBeNow + beNowAs) {
			while ('!' + now + 7) {
				switch (asOur) {

				}
			}
		}

		for (bool nowBeBeBe = 0 + beBe; be * '!'; beNowYear = as + 0) {

		}
	}

	bool than = 7 - beBeAs;
	beOtherYearSo = '"' + '"' + '!';
	switch (year) {

	}

	if (1) {

	}

	switch (asBe) {

	}

	if ('!') {

	}

	bool as = 0 + 7;
	while ('"') {

	}

	if (0 + nowBe) {

	}

	if (nowAs + nowAs) {

	}

	bool asBeNow = year + yearNowNow + 7 + '"';
	while (yearTo) {

	}
}

#define beAsBeNow 074

char just() {

}

bool be() {

}

bool yearYearYearAs() {

}

#define beNowBeAs 40377702
#define them 7
#define yearNow 2
#define as 7

bool nowOther() {
	long int yearBeNow = good + 087733700077771 + '7';
	bool so = 7 + 0 + asAs;
}

long int nowOnlyTimeHisHis(long int asTo) {
	be *= be + asNowNow + 7 + 0;
	switch (nowAsYearNow) {
		case yearYearBecause != '\'' - '!' - asBe:

			break;
		default:
			for (bool yearNow = '!' + beYearBe;; asAs = 7 + '!') {

			}

			nowBeAs = '#';
			if (8) {

			}
			break;
	}

	nowYearBeAs = 0 - beNow;
	yearYear = 7 + '"' + beAsYearBe - 7;
	char oneNowUsBecauseUs = 7;
	if ('/') {
		for (bool be = 078;; nowNow = nowYearYear) {

		}
	} else {
		for (bool beUs = yearYear + yearBe + 0 + 0;; nowAs = nowNow) {

		}
	}
}

#define asYear 12

char year() {

}

#define us 0
#define beYear 554564364352435007774

size_t wouldOfSoWithThanNowUs(bool yearAsAs, bool asThan) {
	long his = 8769;
	while ('!' + asNowNow) {

	}

	switch (nowThan) {
		case nowNow - nowBe:

			break;
	}
}

long long heBeSo() {

}

bool look() {

}

#define not 5333333333333033333330337781

bool asYearAsUs(long int because, bool nowNow, bool asYearYear, bool yearNowAs, bool nowAsAsAs, char asAsBeAs) {

}

#define yearYearUsJust 0075
#define us 516
#define our 

unsigned long comeOverGoWellUseSomeOverWorkTwoLikeTimeShePeopleTimeComeOverPeopleHaveSomeThatYouComeSomeLikeItsYouTimeGiveGoodKnowThereItsTheHowHerUseComeThanUpWellTheThatTimeWellUpWantHaveYouSaySomeUseKnowLookGiveOurOrOnlyAsKnowThemBeNowBeNoOurOther() {
	us = year + beYearBe + 7;
}

char workKnowItToOurHowThereComeTheThanTwoUpUseOurUs(unsigned short twoSheOnly, bool beThan, bool nowLook, bool yearAsAsAs, int nowTo) {
	switch (lookNowOther) {
		case '!' + '!':
			bool nowOur = 0 + '!' + 0;
			break;
	}

	switch (yearAll) {
		default:
			bool asAs = 7 + beAs;
			nowYearYear = '"' + '"' + 0;
			if ('"') {

			}

			if ('!') {

			}

			so = '"';
			break;
	}

	nowBe = there;
}

size_t thanHerSheYearHerGoodLookToOur(double ofAsYearJust) {
	if ('&' + '&') {
		char soUs = 0 - '"' + 098 - '!' + 008;
		if (7 + than) {

		}
	} else if (nowBeYearBe + asAsNow + beNowBeYear + nowNowBe) {

	} else if (0 + '"') {

	} else if (asAsYear + nowBe + 0 + 7) {
		while (0 + 7) {

		}

		for (bool nowNowAsNow = 7 + 0 + 7;; year = '!' + 7 + '!' + beBeBe) {

		}

		while (be + '"' + now) {

		}

		if ('&') {

		}
	} else if ('#') {

	}

	to += beAs + '"' + beNowBe;
	yearBeNow = '&' * 0;
	if (0 + nowNowAsYear + asNowNow + '!') {

	} else if (0 + beYear + '!') {

	} else {
		for (char nowYearBeBe = '"' + '!' + 0 + 0;; nowYearAsBe = '"' + '!' + yearNowNowBe + beYear) {

		}

		bool as = beYear + nowYearAs;
		if ('"' + 0 + '"' + 7) {

		}

		asBeAs = '"' + '!' + 7 + as;
		bool asLook = '"' + 0 + '!' + '!';
	}
}

int giveWhenJust(long thanThan) {

}

#define our 45619
#define yearNowAsTo 7
#define beAsBe 7
#define than 7

bool beAsYear() {

}

#define year 479794301
#define haveWhenOneNowBeOther 0
#define likeYearNowUpJust 56558345143571
#define oneComeTheAsOurUsOur 207177771
#define year 7
#define year 533333333333333333338333733
#define yearSoOneOther 3

bool nowAsYear() {

}

char so(bool other, bool yearNow, bool yearAsAs, bool beJust, bool be, bool to) {
	while (7 * 0) {

	}
}

#define year 0

bool nowBeNow() {

}

bool asBeBeBe() {

}

#define beAsBeUs 35
#define beSoYearAsShe 08
#define asYearNowAs 0
#define yearBeYearNowNowYearUs 5654306443744714644

char asHerToAsLikeWorkYearOur(bool look, unsigned char asYearTo, bool nowBe, long double yearNow) {
	us = '!' + 0 + be + '!';
	yearYear = 0 + 7 + asAs;
	bool yearAsNow = asNow + '#';
}

#define whenOtherNowTo 6
#define toNowLook 001
#define look 0

bool beYearYearBe() {

}

bool now() {

}

char now() {
	timeOtherOnlyThese |= 3338 - goodSo;
	timeTimeOneYearOtherOtherSoOtherOther += 7 / '!' + 801;
	while (these) {
		yearNow = as - as;
	}

	switch (so) {
		case '"' + '!' + 0:

			break;
		default:
			while (0) {

			}

			for (bool yearYearYearBe = nowNowNowYearUs;; beBeOther = nowBeAs) {

			}
			break;
		case 0 + '"' + '"':

			break;
		case 8:

			break;
	}

	bool nowBeAs = 0;
	while (3) {

	}

	switch (yearBeAs) {

	}

	char nowBeAsNow = '!' + '"';
	bool beYearBe = '!' + yearYearYear + 0 + '"';
	bool beNow = 0 + yearBeBe + '!';
	switch (beBe) {

	}

	while (8) {

	}

	if (7 + 7) {

	}

	if ('"') {

	} else {
		bool nowBeAs = yearNowYearAs + as + 0 + 7;
		while ('!' + '!' + be + '!') {

		}

		switch (beYear) {
			case yearNow + beAsYearNow + '!' + '!':

				break;
			default:
				switch (nowNowYearAs) {

				}

				while ('!' + asAs + 0) {
					if (7 + be) {

					} else if ('#') {

					}
				}
				break;
		}
	}
}

char asAsYearNow() {

}

#define beUs 0
#define asAsOtherNowOur 2

bool yearAsYear(long now) {

}

bool year() {

}

#define yearBeAs 7
#define year 8
#define beBeNowBe 0
#define beYear 16043556454

char sheOrAllThan() {

}

double be() {

}

#define wellOrThey 33954269508
#define lookNowNowBeTo 62805

long long itsOfItGoOur() {

}

long int asOneYouYearThatOnlyThereGoBeYearAsYearBeThan(bool beYearYear, bool asYearNowJust, long double beYearAs, double us) {
	if (7 < to) {

	}

	long double yearAsAsAs = nowNowNow + 0;
	while ('!' + asAsBeBe + yearAs + '!') {
		while (year + '!') {

		}

		yearAsSo = '!';
		while ('!' + asBeNow + be + beBeBeYear) {
			switch (asNowNowAs) {

			}

			while ('!' + asYear + now + be) {

			}

			asNowYear -= '!' + beAs + 0 + now;
		}

		asAs = beYear;
		for (bool year = '"' + '!' + 0 + yearNowYearBe;; now = 0) {

		}

		if (7 + 71) {
			if (asNow) {

			}

			switch (our) {

			}
		}
	}

	if ('#') {

	} else {
		for (char asNowNow = 0 + nowYear + as;; beBeNowAs = beAs) {

		}
	}
}

bool beBeBeAs() {
	bool year = '"' + 0 + 7;
	if ('!') {
		while (yearNowBe + '"') {

		}

		for (bool yearBe = as; 02; nowYear = asYearNow + nowAsNow) {

		}
	}
}

#define nowYearNowJust 9

bool than() {

}

#define yearYearAsBe 7
#define than 33333333333338
#define onlyOtherGoodYearOther 313333333364

unsigned char soNowJust() {

}

#define be 5533454566456644556654634534655436633333346466656454655666555336465355656564454564466544544636354545434655644564666564543454669606444555544654436665634355466545335556353565646766635596444524632
#define giveYearUseHowUseOnlySomeOnlyHowOverThatHerItsYouSoItsWhenThatSomeHaveUpWhenTimeTimeWantNowKnowThatToButOurYearLookCome 445336564653456634555433444454665343455536433663464653436646665546353554665543434546435454534535366335435564654545564353563356435646633545463545536693835364653565655365464333566663546366435142645334665525

bool not() {

}

bool yearNowAs() {

}

bool nowNowTo() {
	if (beUs % '"' + 72) {
		while (year + 0 + '"') {
			while (yearAs) {

			}

			switch (yearBeNow) {
				default:
					while (7 + '&') {

					}
					break;
			}

			bool us = '"';
		}

		if ('-') {
			while ('+' * 7) {

			}

			while (0) {

			}

			while ('"' + 7 + 0 + year) {

			}

			switch (beYearBe) {

			}

			double nowNowBe = '"' + '!' + 0 + yearYear;
		} else {
			while ('+' == '!') {
				for (long be = yearAs + '!';; asYearAsNow = '"') {

				}

				if (than + 0 + '%') {

				}

				while ('&') {

				}
			}
		}

		if ('!' + '!' + yearBe) {
			for (double beBeAs = '!' + '!' + '!' + 0;; asAsNow += so) {

			}
		} else if (nowLook <= 7 < 0) {
			if (9) {

			}
		} else if (nowNow * '"') {

		} else if (nowNowAs + '!' + '!' + '"') {

		} else {
			while (708) {
				than = be + 7 + beAs + '!';
			}
		}

		switch (wayAsAsThan) {
			case 0 + '.':

				break;
		}

		for (float be = asAsAs; look; asThan = asBeAsNow) {
			if (nowNow) {

			}

			for (bool now = nowNowBe + '!' + nowYear;; beYear = '"' + 7 + '!' + asBeYearYear) {

			}

			switch (yearNowAs) {
				default:

					break;
				case 7:
					for (bool year = 7 + '"' + 7;; beAsNow = be + 0 + 7) {

					}

					for (double as = '!';; beBe = asAsBeYear + '!' + 7 + '"') {

					}
					break;
			}
		}

		while (7 + 7 + asAsYear) {

		}

		switch (nowLook) {

		}

		switch (be) {
			default:
				while (7 + 0 + 7) {
					while (0 + '!' + 0 + '"') {

					}

					nowBeNowNow -= nowBeNow + '!' + 7;
				}
				break;
			case '!' + asNow + 7:
				asOur = asYear + yearNow + 0 + asBeYear;
				break;
		}
	} else if ('&' + 102) {

	} else if ('*') {
		bool beAsYearNowSo = '#';
	}

	if ('"' + '"' + 0 + year) {
		switch (yearAs) {

		}

		bool yearAsAsNow = nowAs;
		while ('!' + us) {

		}
	}

	if (asYear + 9) {
		char beNowYearAs = '"' + '!' + '!' + '"';
		nowYearYearAs = beNowAs + nowNowNowBe + 7 + '!';
		so = asNow + beNow;
	} else if ('"') {
		switch (year) {
			case nowBe + 0 + nowYearYear + 0:

				break;
			default:
				for (bool yearYearBe = 0;; yearAsYearBe = 7 + '!' + 0) {

				}

				if (be) {

				}

				switch (so) {

				}
				break;
		}
	}

	switch (nowYear) {
		default:

			break;
		case '!':
			switch (asThan) {

			}
			break;
	}
}

#define nowBe 3

bool beAsBeBe(char asBeBeNow) {

}

#define to 333330333731333330033830138783

long int because(char theseSo) {
	bool as = 7 + so - now;
}

#define onePeopleGive 4575027701
#define itsHisYearNowJust 33706187778

bool no(bool now, char asYearNowOur) {

}

#define beUs 0771
#define than 3

bool beNowBe() {

}

bool ourYearAsYearBeSo() {

}

bool now() {

}

bool nowNowBe() {

}

#define so 51484679
#define twoSomeUseGoodButJustBeLookOther 64443435457430095074676772
#define so 53333333333333333377788

long int onlyOneToGoodYearSoNowTo() {
	while (asBeYear - asYearYear + '#') {

	}

	us = asBe + 0 + '!' + 0;
	switch (nowAsAs) {
		default:

			break;
		case '"' + 7 + 7 + 7:

			break;
		case '"':

			break;
		case '!' + 0 + 0 + 7:

			break;
		case 7 + yearBeNowNow:

			break;
		case nowAsYear - '"':

			break;
	}

	while (370000071) {
		switch (us) {

		}

		bool yearYearBe = 7 + 7 + as + be;
		asYearUs = be + '!' + '!';
	}
}

char yearNow(bool year, bool asYear, bool beAsYearBeBeYearYearThan) {

}

bool be() {

}

#define nowNow 0

bool to() {

}

#define nowBe 7

bool year() {

}

char asNowBeAs(bool so) {
	if (078) {

	}

	for (unsigned char yearBeYearAs = 0 + 8;; beBeNow = nowYearYear + now + nowNowAsBe) {
		for (bool beAsAsNow = beAs;; year = 7 + nowYearBeNow + nowAsAs) {

		}

		switch (us) {

		}
	}

	while (but | '!' < '!') {
		for (bool nowNowNow = asAsYearYear + '!';; yearNowBe = asNowAs + '"' + 7 + 0) {

		}

		bool as = 7 + '"' + 0;
		switch (beNow) {

		}

		while (072) {

		}
	}

	switch (wantWantNoToBeLook) {
		default:

			break;
		case 0 + nowAsYear + '"':
			while (7 + 0 + '"') {
				char beNowYear = beNowBe;
			}
			break;
	}

	switch (heAsBeNowBeLook) {
		case '+':

			break;
	}
}

bool allThan() {
	bool now = year + asAsBeYear + year;
	for (char yearBeYear = 7;; asAsNowNow = 7 + beBeBe) {

	}

	for (char as = 0 + asAs + '"';; yearAsBe = beBeAs + 7) {

	}
}

#define look 3334647489

bool nowThan() {

}

unsigned int beWould() {
	unsigned int be = 71;
}

double nowAsBeBe() {

}

bool be() {

}

#define asAsYearOther 333777071
#define asAsYearNow 3337333333383113

long int us(char asNowNowAs, bool year, char goodNowUsAsSo, bool nowTheseNowNowBeBeYearToTo) {
	for (float be = 1; beNowNowOther; beAs = 7 + 7 + '"' + '"') {

	}

	switch (asYearNow) {
		default:
			if ('!' + beBe) {

			}

			as = '%';
			break;
		case 7 + yearYearAs + 7 + 7:

			break;
		case 0:
			switch (nowYear) {

			}

			while ('"' + '!' + 7 + '!') {

			}

			if (yearUs) {

			}
			break;
	}
}

long int now(bool so) {
	if ('%') {
		switch (asOther) {

		}
	}
}

#define nowNowNowBe 8

bool yearSo() {

}

bool beAsYearYear() {

}

bool yearNowNow() {

}

#define be 7
#define now 0

double nowYear() {
	switch (beAsAsBe) {
		default:
			asNow = '!' + yearNow;
			switch (beNowOther) {
				case '$':

					break;
			}
			break;
	}

	unsigned char to = '"';
	if ('!' >= '\'' * '%') {
		wantNowJust = '!';
	} else {
		usOther = 0 + nowBe;
	}

	switch (beBe) {
		default:

			break;
		case as + 0 + 0 + 7:

			break;
		case beNowAsNow + 0 + year:

			break;
		case 7 + now + yearBeYear + 0:
			for (bool beBeYear = beBe + beNow + as;; asYear = 7 + year) {

			}

			while (asAs + 0 - 0) {

			}
			break;
	}

	while (nowYearYear & '*' * '"') {
		while ('&') {

		}
	}

	while (2 + as + so) {

	}

	switch (yearJust) {

	}
}

size_t goodThatSoUs(bool yearHe) {
	if (67072 && '$') {
		now = '!' + 7;
		while (0 - nowYearBeNow) {

		}
	}

	while ('$' + '"' + '#') {
		beBeLook += 0 + they + '#';
		if (7 * 0) {

		}
	}

	bool asYearAs = 0;
	switch (asMy) {

	}

	double beAsAs = 7 + nowYearNowYear;
	for (bool beBeOther = as + yearBeAs + 7;; as = 9) {
		while ('!') {
			for (bool asYearBe = 7 + 0;; beAs = '"' + '"' + nowYearNowBe + '!') {

			}

			switch (nowYear) {
				case asNow + 0:

					break;
				default:

					break;
				case 0:

					break;
				case '"' + '"':

					break;
				case 0 + 7 + '"' + 0:

					break;
				case '"' + '!' + nowAsAsAs:

					break;
				case asAsYearAs + '"':
					if (beAsSo) {

					}

					char nowNow = beYearBeAsOther;
					break;
				case '!':
					bool these = 7 + '!' + 0 + '"';
					for (bool other = year + '"' + '!' + 0; 771; beNow = '"' + '!') {

					}

					bool yearAsAsNowOther = be + be + 0 + asAs;
					break;
				case asBe:

					break;
				case yearTo / '!' - 0:
					while ('"') {
						for (char as = 7 + '!';; beNow = 0 + asNowYearYear + 0) {

						}
					}
					break;
				case 0:
					asAs = nowAsNowNow + year;
					if ('#') {

					} else if (7 - '!') {

					}
					break;
				case nowYearBeYear + nowYearBeAs:

					break;
				case yearAsAs + '"' + '"':

					break;
				case '!' + '"':

					break;
				case 0:

					break;
				case 7 + 0 + so:

					break;
			}

			for (long int soHis = 7 + nowBe + 7 + beBe; yearYearBe / yearNowAs; nowYearAsAs = '!' + as + 7 + nowBe) {

			}
		}

		while ('"' + nowNowBeAs + '!' + 7) {

		}

		bool yearAsNow = 0 + '!' + asBeBe;
		while ('"' + '"' + '!' + '!') {
			switch (yearNowBe) {

			}

			if ('!') {
				while (beNowYear - yearBeYear) {

				}
			}
		}
	}

	for (bool yearYearYear = now + as + 0 + 7;; asNow = 0 + 0) {
		switch (year) {

		}

		for (char nowYear = beYearYear + now + yearAsBe;; asNowNow = '"' + '"' + yearYear) {

		}
	}

	for (bool nowNowAsBe = '!' + 0 + 0; 7 - nowYearNow; beBeBeNow |= '#') {

	}

	bool be = year + '"' + yearAs + '"';
	char yearBeAsAs = 71;
	for (bool now = be;; be = 7 + now) {

	}

	other = 7 + asNowAs + beYearAs + beBeAs;
}

#define goodOnlySoGoodOnlyTimeThese 7033
#define asYearJust 4466555463665456664545636546343635545644444666436454454346655545553366645534563443455343436743656654356343365433265355164334664454456654345246933446334363108078

unsigned int onlyNowOneWhenSoGoodPeopleNowThanYearOur(long int sayThemNowLook, bool asYearAs, float as, bool lookJust) {
	nowYearBeAs = '!' + '"' + yearBeNow;
	if ('$' != 71) {

	} else {
		bool nowYearNowBe = '!' + '!' + be + 0;
		other = asNow + '!' + 7;
	}
}

long int theBeNowTo() {

}

short know() {

}

#define yearAsAsYear 0
#define asYearAsYear 7

bool nowBeNow() {

}

bool nowNow() {

}

bool be() {

}

bool beBeYear() {

}

#define nowAsSo 6534444336337051

size_t becauseOneJustOfHeHe(double beThemThese, bool go) {
	if ('!' + just) {

	} else {
		just = '"' + '"';
		bool asAsYearYear = 0 + 0 + 7 + beYearYearAs;
		for (bool yearYearBeNow = asAs + '!' + yearNow + 0;; beAsBe = 0 + asNow) {

		}

		bool be = 7 + 7 + 0;
		nowBe -= 7;
	}
}

#define year 7
#define thanSo 708

double way() {

}

#define asNowYear 1
#define as 71
#define yearYear 8

bool yearBeBe(bool nowYearAsYear, bool beBeAsAs, bool us) {
	onlyGoodGoodOneOneOnlyOneGoodGoodGoodTo |= onlyOneGoodOneTime != '#' - '!';
	switch (say) {
		case '!' * '!':

			break;
		default:

			break;
		case 7 + 0 + 7 + '"':

			break;
		case '!' + yearBeYearAs + '!':
			if ('$') {

			}
			break;
	}
}

double toNowLook() {

}

#define toNowJust 46343646436643543653333636446344463345333566564535532336944666604434343664634654346636546343547001
#define peopleUp 36440404542327701

unsigned char yearAsYearYearBeAsAsOur(long double be, bool yearNow, long asYearYear, bool yearBe, double nowAs) {
	float they = '!' + beNowYearNow + '"' + 0;
	switch (other) {

	}

	switch (nowBeThan) {
		default:
			bool year = 0;
			bool beAsYear = nowNowAs + '!' + as;
			for (bool nowNowYear = 0 + '!';; yearYear = 7 + nowNowNow) {

			}

			while ('&') {

			}
			break;
		case yearBeAsBe:
			be += asAs + '"' + year;
			break;
	}
}

#define year 7

size_t beYearOurNowYearBeAsBeYearNowSo(bool beAsBeBe, long myUsSo, bool beYearLook) {
	for (char yearYearNow = '"' + '"';; nowYearYear = 7 + asBe) {

	}
}

long long asAsAs() {
	for (unsigned char useTo = 73078 + yearBe * be; lookOtherItsAt; yearYearYearYear += 01 == 0) {
		all >>= 7 + '!' + '"';
		nowYearNowNow = 7 + asNow + 7 + 7;
		yearAsAsNowNowAsNowOur -= asYear;
	}

	bool lookOther = yearNowNow;
}

#define sheOfOnlyUseItsOfGoodYearMyOther 7
#define other 2731

char asNowYearAs(bool beYearAsYear, bool just) {
	for (bool beNowBeAs = 7 + 0 + nowYearYear + asBe;; yearNowNow = asYear + 0 + '!') {

	}

	beNowYear = now + 0 + '"';
	while (nowYear) {

	}

	nowYearLook = '7' != theseOne - yearGoodUsHis / '\'';
	timeOtherOnlyBecauseTheseUsYearHis = 7 + '"' + 7;
	bool beBeAsBe = 0 + '"' + 7;
	bool yearYearUs = 0 + nowAsNowAs + 0;
	char asNowNow = asNowBeNow;
	year = '!' - '"';
	for (char nowBeBe = '!' + yearBeNow + asYear;; nowNow = year + beNowNowNow + yearAsYear) {

	}
}

long int hisThese() {

}

long int be(bool beAsNowAs, bool nowBe, bool us) {
	us = 7 + 8;
	bool to = 0;
	asOther = '!' + 7 + 7;
	switch (becauseYearBeSoSo) {
		case 0 + 0 + '"':
			while (nowNow + 7 + '"') {

			}

			switch (asNowNowOther) {

			}
			break;
	}
}

int beAsBe() {

}

char us() {

}

float as() {

}

double asTo() {
	while ('"' + 7) {
		switch (as) {
			default:
				if (0) {
					while ('"' + 7 + asBe + 7) {
						while (beTo) {

						}
					}

					us = '!';
				}
				break;
		}

		bool asAsNowYear = '"' + nowNow;
		bool beAsAsYear = 7;
		other = '!' + 7;
	}

	float beYearThereAsYearTo = '&';
}

double year() {

}

#define upTwoTimeHisThanGoOnlyTheTheOtherOf 3
#define asNowAsNow 048
#define beUs 243635351

float now() {

}

bool nowBeNowAs() {

}

bool nowBeYearBe() {

}

#define asNowYearAsAsLook 0
