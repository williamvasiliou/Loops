#include <UUUUU2//00.h>
#include <U/0E.h>
#include <U/60EUV///0.h>
#include </.h>

#define asUs 7

bool year() {

}

bool ourOur() {

}

#define ourAsOur 305641636376360336074
#define peopleWorkGoSomeNowThatOtherToBeYearSoJust 5453556387333313033

long int onlyOtherTheseYearBeAsBeNowSo(bool beNowYearYear, char theseSo, float asYearNowNow) {
	bool asNowAsBe = 7;
	while ('!' + '"') {
		for (bool other = '!';; yearAsBe = year) {

		}
	}
}

#define toBecause 333330833708
#define his 7

size_t as() {
	lookTo = 0;
	bool yearNowAs = 7 + 0 + 0;
	for (bool beNowYear = 0;; yearYearNow = nowYear) {

	}

	while (yearYearAs + '!') {

	}

	switch (beNowBe) {

	}

	be = '"' + 0;
	yearNow -= asNowYear + 0 + 7;
}

bool nowNowBeYear() {

}

bool nowYearNowBe() {

}

bool nowThan() {

}

bool asYearAs() {

}

#define beAs 0586364

unsigned long haveHowOrOverTheWhenSomeSayLikeLookOrLookSheItSomeOrGoodGiveGoodGoOnlyUpGoodTheNoNowWouldSheWell(bool yearBeYear, size_t asSoOur) {
	for (int likeNowSo = beJust + '$' + yearTo; ',' - '!'; be = 7) {

	}

	bool look = yearYear + yearBeAs + '"' + '!';
	for (bool other = 0;; beNowYearBe = beBe + 7) {

	}
}

long int asBeYearNow() {
	switch (so) {

	}
}

#define beUsTo 008
#define asBeYear 1
#define as 7

bool look() {
	switch (our) {
		case 0 + 7 - '!':

			break;
		default:
			switch (nowAsNowYear) {
				default:

					break;
				case 7 + '"' + 7 + '"':

					break;
				case '"' + 7 + as:
					for (bool year = '"' + asAsBe + '"' + 7;; asNow = asYear + '!' + now) {

					}

					beBeYearBeOtherAsJust = asBeBe;
					break;
			}

			nowNow = be + 2;
			break;
		case beBeYearBe + as + 0 + beNowAsAs:
			for (bool asYearBe = 7 + 7 + '"'; '$'; year = yearNowAsNow + '"') {

			}
			break;
	}

	while ('6') {
		for (bool beBeBe = 0 + 0 + '"' + '"';; nowAs = 7 + year) {

		}

		while (0 + 0 + '"' + be) {
			if (0 + yearBe + 7) {

			}

			char as = '"';
		}

		while (look) {

		}

		switch (beYearNowBeYearLook) {

		}
	}

	while (nowYearYearNow + be) {
		while (0 + 0 + asYearBeYear) {

		}

		switch (asAsNowAs) {

		}

		nowAsNowAs = 7 + year + now;
		switch (be) {

		}

		nowNowBe = asNowYear;
		for (bool as = yearBe;; now = asAsAs + asBeNow + '"' + asYear) {

		}

		bool nowYearLook = asYearYear;
	}
}

long long upThereOverYearGoThem(unsigned short peopleSayGoItsOnlyHe) {
	switch (beBeNowAs) {
		default:
			ourSoSoYearOther = 7 + 7 + '"';
			break;
	}
}

#define than 9

unsigned short than(bool beYearNowBe, bool nowYear, bool nowNow, char asYearAs) {

}

bool beNowBe() {

}

size_t nowYearNowAs() {
	for (char asNowNowAs = yearNow + '!' + 7;; nowAs = asYearAsAs + '"') {
		nowBeAsAs = 7 + asBeYearAs;
		if (beNowNowBe + 7) {

		}

		switch (as) {

		}

		if (0) {

		}

		bool be = '!' - asAsAsYear;
	}

	soOneBeHeYearToBeAsSoTo = yearAs + as;
}

#define otherUs 3303333300337001
#define beToAsOneTheseOther 7
#define onlyBecauseUsYearUsToBeSoUs 33133183
#define as 7
#define nowYear 79

bool other() {
	if (7 + '"' + '!') {

	}

	bool look = '"' + '"';
}

bool nowAsNowNow() {

}

#define as 0

bool nowBeAs() {

}

bool asAsBeBe() {

}

bool yearNowYearYear() {

}

double yearNowNow() {
	heNowYearHisJust = yearAs;
	if ('!' + asBeYearAs + '"') {
		while (9) {

		}
	} else {
		now += 7 + as + '!' + '!';
		bool beWith = '"' + 0 + year + '!';
	}

	bool beBeAsNowOther = 7;
	if ('/') {
		bool beNowAsAs = nowAsAs + 7;
		if ('!') {

		}

		if ('"' + beBeNow + beBe + '"') {
			switch (asBe) {

			}

			if (7) {

			}

			beNowAsBe += 0 + asBe;
		}
	} else {
		for (bool yearYearBeSo = '"';; beYearYear = beNow + 7 + '"') {

		}

		haveLookThey += 7 + yearBe + yearYear + '!';
	}
}

unsigned int knowHaveOtherKnow(long int yearBeGo) {
	bool asBeOne = 7 + asBeNow;
	switch (be) {

	}

	so -= '#';
}

#define ourJust 4033352

bool year() {

}

char ourAsOur() {

}

#define yearYearJust 3333333337333333377138338

char toSo(char asTo, bool he, char asNowYear) {

}

#define toGoodOnlyBecause 001

char now() {

}

#define year 9
#define asAs 56633
#define give 1
#define nowYearNow 7

bool asLook() {

}

#define asBeBe 650705439

bool beNow() {

}

#define or 44433353306453414600108
#define us 56404580721
#define yearNowNow 0
#define so 7

bool asNowAs() {

}

#define be 7
#define look 38
#define oneOneUsOnlyGoodHisGoodAsToOtherOther 3338373373383138
#define goodOnlyOnlyOther 0
#define beHis 3807079
#define asYearBeAs 7

bool beNowBeNow() {

}

bool be() {

}

bool yearNowYearBe() {

}

bool beNow(bool as, double yearBeNowNow) {
	for (float year = yearNowBeAs; '~' & yearBeAsBeLookThan && 63466275909 | asUsOur || not - '!' + 2; lookAt = '"' + '!' + '!') {
		if (now) {
			for (bool yearNowNowBe = 0 + yearBeNow + '!' + 7;; asAsBeNow = '"' + '"' + 0) {
				if ('!' * asNow) {

				}
			}
		}

		if ('!' + 7) {
			for (bool yearBeYear = '"' + '"' + beNowBe; than; be = asBeNowYear) {

			}
		}

		bool beYearYear = beYear + 0 + '"';
		yearNowYearAs = 7 - yearNow;
		year = beAs + '"';
		switch (beAsYearBe) {

		}

		if (to) {

		}
	}

	year =~ '"' + now + 171;
}

char us() {

}

size_t yearThanOther(double beNowAs) {

}

#define nowAsYearSo 5333333013333333333371003338

long int so() {
	for (bool asNowNow = '"';; nowYear = 0 + now) {

	}

	switch (yearAs) {
		default:

			break;
		case '!':
			switch (beBe) {

			}

			while (asUs) {

			}
			break;
	}
}

#define so 7
#define beTheseThese 7
#define as 4079

long double ourOurUsYearUs() {
	nowAsAs = '&' % 645463020007002 && 9 & 573484 + 0 + beYear * '"' + beAsAs <= asBeBe;
	switch () {
		default:
			long double look = 'a' % 7 - 1;
			if (3099 - 701) {

			} else if (as + '"') {

			} else if (asNowAsAs + nowAs + now) {

			} else if (7 + yearYearNowNow + 7 + '"') {

			} else if (beAsAsNow + asAsNow + '&') {

			} else if (7 + 7 + 0) {

			} else if ('"') {

			} else if (yearBe + 7) {

			} else {
				for (bool beYearYearBe = 0; us; beBeBe = '"' + nowBeBe + nowBeAs) {

				}
			}

			if (0 + '!' + nowAsBe + nowBeNowYear) {

			} else if (asAsNow + 7 + 0) {

			} else if (7 + '"') {

			} else {
				look = asAsNow + asNowNowNow + asNowYear;
			}
			break;
		case 3774:
			asNow += '"' + '"' + 7 + 0;
			break;
		case beBeOther - 9:
			for (bool year = '"' + 0;; beNowYear = nowNow + 0 + yearBeYearAs + 0) {

			}

			for (bool other = '!';; beYearBe = '!' + yearYear + 7 + '"') {
				for (bool year = nowBe; beBeYear + '!' + asYear + '%'; be = yearBeAsBe + 7 + 0) {

				}
			}
			break;
		case wellJustJust + yearNowYearNowBeUs + 0 + yearNowBeNow + nowAsBeNow + 072:
			double beAsNowNow = 7 + yearBe - yearYearAs;
			for (char be = beNowBe + '!' + nowNowAs; 'p' - '"' + '!' * year; asNowAsBe += 0 + 7 + now + '!') {

			}

			for (bool to = '"'; 7 != 8 * nowYearAs; yearBeAsYear = asBe) {

			}

			for (bool yearYearBeAs = 7 + 0 + '"';; now = asAsNowNow + '!' + year + yearBe) {

			}

			asBe = '#';
			break;
		case 0:
			be = '"' + '"';
			for (bool yearNowAsNow = 0;; be = 7 + yearBeAsAs) {

			}

			char year = 7 + 7 + asAsBeBe + '"';
			break;
		case '+' * asNowNowAs:

			break;
		case 7:
			if ('$') {
				switch (yearYear) {

				}

				asNow = yearNow;
				if (7 + '!') {
					while (1) {

					}
				}
			}
			break;
	}

	for (short asAsBeYear = yearWould * 0; 03734 - asAsYearAsAsYearSo - '"'; itThatSayItsOfJustHisGiveFromAsAsLookNowTo *= at) {
		switch (asAsAsAs) {
			default:
				while (0 + as) {

				}

				while ('&') {

				}
				break;
			case 7 + '!':
				usThan = 7 + '"';
				if (0 + 7 + 7) {
					switch (now) {

					}

					switch (beAsAsBe) {

					}

					for (bool yearNowBe = 0 + '"';; now = 7) {

					}

					if ('"' + '"' + '"') {

					}

					for (bool beYearAs = now + '!' + '!';; yearYearAs = '"') {

					}

					bool nowTo = '"' + '!' + '!' + 7;
				}
				break;
		}

		if (asBe + 7 + nowYear + beBeNow) {

		}

		for (bool nowNowNow = '!';; nowAsAs = '"' + 0 + now) {

		}

		if ('!') {
			if (7 + 0 * 0 - asBeYearAs) {

			}
		} else {
			just = '"' + nowNowYear;
		}
	}

	switch (heHisHaveYearNowThemSo) {
		case 03 - be:

			break;
		default:

			break;
		case '0' * '#' + '&':
			beAsAs += 7;
			break;
		case '!':

			break;
		case '"' - 7:
			for (char beNowNowYear = 7;; nowAsNowAs = '"') {
				if (7 + 7) {

				} else {
					bool beBeAs = year;
					if (look) {

					}
				}
			}
			break;
	}

	switch (workTheUs) {
		case 770707071:

			break;
		case 0 + 7:
			switch (so) {

			}

			switch (beNow) {
				default:
					while (beNowYearBe + 0) {
						if (asBeNow + nowBeNowYear + now + asNowAsBe) {
							switch (asThan) {

							}
						}
					}
					break;
			}

			for (char nowBeAsAs = year + be + asYearAs + 0;; nowNowAsYear = 0) {

			}
			break;
		default:
			switch (yearNowAsYear) {

			}

			char now = nowYear + asAs + 0;
			break;
	}

	bool howOnlyTo = yearNowNowYear + '"' + 7 + 0;
	if (7 + 0) {
		switch (look) {
			default:

				break;
			case 5:

				break;
		}
	} else {
		while (004 - be) {
			yearYearYear = '$';
			bool now = yearAsNowAs + '"';
			bool look = 0;
		}

		if ('"') {

		}

		while (nowNowBe + 7 + 0) {

		}

		while ('!') {
			double as = 7 + beYearAs + asNowYear;
		}

		for (bool beNowBe = 7; other; now = yearYear + now) {

		}

		switch (so) {

		}
	}

	while (yearYearYear + year - 0) {

	}
}

#define themOrThereGoodYouHowOverYearWhenSomeHerAsNowOnlyHerGoodWorkThatThere 6087098

double us(bool now, bool beAs, bool be, bool be, bool yearAsJust) {
	switch (asNowYear) {

	}

	if (09) {
		long be = '!' + '!' + 7 + 7;
	}

	bool some = look;
}

#define giveTimeWayAllAsComeHis 779

long long yearJustYearNowAsNowSo(bool so, bool asBeLook, bool nowBeSo) {
	asTo = '"' + nowYearBeYear + '"';
}

#define asAsYear 7

bool as() {

}

#define be 0
#define beBeAsUs 643755335343765369615228

bool year() {

}

unsigned int wouldTheyOverOtherNowUs() {

}

short just() {
	while ('!' + asNowAs) {

	}

	switch (year) {

	}

	switch (as) {

	}

	for (bool asNowYearAs = 0 + yearAs + year + '"';; yearBe = yearYearAsYear) {

	}

	double be = 7 + as + '"';
}

#define so 9
#define so 0
#define asThese 65333313333
#define goodOneOnlyYearTimeHeOneOnlyUs 3373333381

long int nowSome() {
	double asYearYear = yearAs + as + 0 + yearNow;
}

bool yearNowYearAs() {

}

bool nowBeNow() {

}

long asNow() {
	for (long long beBeNow = nowLook;; our = '%') {
		while (0) {

		}

		while ('!' + '"' + 0) {

		}

		if (beYear + beBeAs) {

		}

		asNowNow = 7 + '!' + '"' + '"';
		double now = yearAsYearYear + 7;
	}

	if (to) {

	}

	switch (asAsTo) {
		default:

			break;
		case yearTo - '"':
			nowYear = '!' + 0 + '"' + nowNowBeNow;
			as = '!' + '"' + 7 + as;
			bool nowNowBe = '"' + '!';
			for (bool be = '!';; yearAs = '"') {

			}

			yearBeAsAs += '!';
			break;
		case '#':
			if (yearNowAsBe + yearAsNow) {

			}

			so = beNow;
			break;
		case as + '!' + as + asBeAsAs:

			break;
		case '%':

			break;
	}

	switch (nowNowYearNow) {
		default:
			switch (yearNowAsAs) {

			}

			while (yearSo - '!') {

			}
			break;
	}
}

#define nowWithJust 6655335344564543436645454447339789
#define beAsAsAsOther 7
#define nowNowYear 8

unsigned char other(bool asYear, bool as, bool nowYearBeNow, bool asAsNow, bool asNow, bool yearYearAsYear, bool beBeYear, bool asYearNow, double yearYearNow) {
	switch (us) {

	}
}

#define otherYearThan 240770007771
#define onlyOnlyTimeOneGoodYearHeHe 13878

long int oneOneGoodTimeTimeToHisUs(bool nowNowAs, bool yearYearSo, char now, bool asAsYear, char beAsAsNow) {

}

char other(bool nowNow, bool beBeYearBe, char asYearYearYear) {
	switch (now) {

	}

	long long beBeAs = '%';
}

#define howNoNowYearYearAsYearJust 0

bool asYearAs() {

}

#define yearYearAs 7

bool beYear() {

}

bool beUs() {
	switch (giveBeLikeToSayWellNowThem) {
		case 37798 / beYearBeYear:
			if (')') {

			}

			so = 0;
			for (bool yearNow = 7 + 7; so; be = '"') {

			}

			switch (beNowOurAsYearNowUs) {

			}
			break;
		default:
			double be = nowBeAs + beNowAs;
			long beBeAs = 0;
			if (7 + '"' + asYear) {
				if ('!' + '"' + yearNowNow) {

				}

				switch (yearYearAsNow) {

				}

				if ('!') {

				}

				char nowNow = 7 + asBe + '"';
			}
			break;
		case be:
			double nowNowNow = '!' + yearBeBeYear + yearYearBe;
			if ('!' + '"' + 7 + asYear) {

			}

			while ('!' + look) {

			}
			break;
		case 7 + 7 + '"' + 7:

			break;
		case 000009:

			break;
		case nowAsAs:

			break;
		case '"' + 7:

			break;
		case beNow:
			for (bool nowSo = 0;; as = asYearAsAs + 0 + nowYearYearYear + 0) {

			}
			break;
	}

	for (long long wouldOfThereLikeHowJust = 'a' <= 3601 + '&';; beAs = year + now + 7) {
		if (0 + asNow + 7) {

		}

		double yearAsBeNow = beYear + 7;
	}

	toOtherThatSheKnow >>= 555734553146464677766155 + 7769;
	if (beNow + '"' + 0) {

	} else {
		nowBeNowBe += 7 + '"' + beNowBe + 7;
		year = '!' + nowAs + '!';
		for (bool asAsAsYear = 0; '"' - '"'; yearNow = 0 + '"' + yearBeBe) {

		}

		switch (asAs) {

		}

		switch (be) {
			default:
				switch (asAs) {

				}

				bool yearYearBeAs = 0;
				if ('"' + 7) {

				}

				while ('"' + yearAsNowAs + '"') {
					for (double yearBeYear = yearAsNowNow + 7;; now = 0) {

					}
				}
				break;
		}
	}

	asBe = '!';
	asBeTo = '"' + be + 0;
}

long int twoGiveGoOfSoSomeItsYearThanOtherWith(bool over) {
	bool atJustYearAsAsNowNowYearNowAsLook = no;
	switch (yearYear) {

	}

	if (year - 7) {

	}
}

long long herOnePeopleOfWellHerUpGiveHaveComeWhenThatSayWhenThatOverKnowUpWantPeopleUsWantWellWhenToLook() {
	for (int nowAs = 514 - 7;; year = 09) {

	}
}

long long ofBecauseOtherSo(bool nowNowAs, bool nowBeNowYear, bool asSo) {
	bool beAs = 7 + 0 + 0;
	yearBeBeYear = nowYearLookSo * '"';
}

#define to 0

short yearUs() {
	if (yearNowToUs % asNowBeBe) {
		switch (beBeNow) {

		}

		switch (beNowBeYear) {
			case 0 + '!':

				break;
			case nowYear + 0 + 7:

				break;
			case '"' + asBe + '!' + '!':

				break;
			case yearNowNowLook:

				break;
		}
	}
}

bool asNow() {

}

long int oneThan() {

}

double now() {
	switch (know) {
		case 0 - '"' < '"':
			if (year + 2071) {
				bool asYearAsNow = 7 + beAs + 7;
				bool year = 8 + 779;
			}

			while (7 + 7 + '"') {
				asTo = 0 + '"';
			}
			break;
	}

	bool just = atUsOur * '&' + '$';
	for (bool hisOur = yearUpNowToThan; asYear * '"'; toYearTo = 7) {

	}

	switch (beYearSoJust) {
		default:

			break;
		case 7 + 0 - 0:

			break;
	}
}

unsigned short butBeToToAsOur(bool nowBeYearTo) {
	bool nowNow = 0;
	double asBeNowAs = 0 + 7 + beNowYearBe + '"';
}

unsigned short beOurLook(bool yearBe, long long allUs, bool ourOur) {
	asLook = '!' + 7 + year;
	switch (nowBeYearBe) {
		case 7 + now + 0:

			break;
		case 0 + '!' + be + '"':

			break;
		default:

			break;
		case '!' + asAsBe:

			break;
		case yearYearYear + 0 + beYear + 7:

			break;
		case '"' + 7:
			for (bool beBeYearAs = 7 + nowNow + 7; 709; nowNowNow = yearYear + '!') {

			}
			break;
	}
}

#define yearAsBeBe 3727071
#define yearAsNowYear 7

char beBeAsYear() {
	switch (yearAs) {

	}

	while (now - yearAsYear) {

	}
}

long int oneHe(char nowBeAs, bool beYearYear, bool year, char toBeUs) {
	for (long int us = 7 + be + 7;; beBeYearYear = yearBe + as + beYear) {

	}

	yearBeNowBe = 0 + 0 + '"' + '!';
	if (0 + year) {
		for (bool be = yearNowYearYear + 7 + '"' + 7;; beNowAs = asBeAsBe + asAs + 7) {

		}

		while (year) {
			while (beYearYearAs + '"' + '!' + 7) {

			}

			switch (to) {

			}
		}
	}
}

#define oneYearOnlyOtherTimeOther 383333031083

char beYearAsYearSo() {

}

#define asBeAsBe 3370071

long int nowNowYearYearAsAsAsNowNowBeSo(char yearYearBe, bool so, float nowBe, bool yearOurUs) {
	for (bool now = 0 + 7 + 7 + beNow;; now = 0 + '!' + '!') {
		if (be + 0 + '!' + '!') {

		}

		if ('!' + nowAs) {

		}

		nowSo = asNowNowNow + 0 + 0;
	}

	bool nowBeYear = '!' + asAsNow - than != as + 79;
	double yearBe = beAs;
	for (bool yearAsYear = '"' + '"' + asNowYearYear;; nowYearAs = year + beAs + '"' + '!') {

	}

	switch (be) {
		default:
			bool be = beNowBe + '"' + now + yearNowAsYear;
			switch (us) {

			}
			break;
	}
}

#define yearYearNow 0
#define beNowBe 7
#define asYearNow 7

bool yearNowNowYear() {

}

char asBeAs() {
	long long yearAsNowYear = '.' & overUs && '"' == 7691 * them + beNowAs * yearYearNowAs - yearBeAs;
}

#define be 22

long long knowTimeComeSomeYouThemUpToOneGoUsWantOur() {

}

bool otherYearBeNowSo() {

}

long int goodOne() {
	while (7 + '!' + 7) {

	}

	beBeYear += year + 0 + 7;
	bool nowAs = 7 + year + '!';
	if ('!' / beBeAsNow) {

	}
}

long int asBe(long int becauseAsBecause) {
	for (bool asYear = beBeBeNow;; now = nowYearNow) {

	}

	so = '+';
	while ('%') {
		asYear = 0 + be + 0;
		while (be + '"') {

		}

		while (0) {

		}

		switch (to) {

		}
	}

	switch (nowSo) {
		case beAsBeYear + '!' + 7 + 0:
			other = yearAs + 7 + 0;
			break;
	}
}

#define becauseOneGoodOnlyGoodThese 33302

bool yearBeYear(bool as, double asAsBe, double asAs, bool justBut) {
	if (7 + 7 + '&') {

	}
}

#define yearNowYearBe 7
#define now 8
#define yearYearJust 1
#define now 
#define twoOtherGoThanWithThatLikeHer 6346655554564644343455565454556653335543534634653334536336536364545443363334653556346563664553661345248554635566648694357316854240071
#define  354553665337535666565553543354633543364546754433643353664653

unsigned char howTimeHerHerUsGoUsHowAllOne(unsigned short ofHowWorkHowSheUsPeopleAsWayNowWorkYouJustNowUse, bool beBeYear, bool nowAsNow, bool so) {
	while (beUsThanYearSo) {
		switch (asAsBe) {

		}

		for (bool as = 0 + now;; nowBeNow = beBe + '"') {

		}

		double year = '"' + '%';
	}

	unsigned short no = 7 / us + '#';
	unsigned short just = '!' + 0 + asBe + '!';
	if (5) {
		if (1) {
			while ('!' + yearYearBeBe + 7) {
				switch (beNowYear) {

				}

				nowYearBeNow += 7 + '!';
			}
		} else if ('!') {

		} else {
			char beBeYearNow = 0;
		}
	} else {
		for (bool beYearAsAs = yearNowBe + 0; just * be - yearNowNowBe; beYear = 7 + year + '!' + 0) {

		}

		while (7 + '!' + asBeYear + asBeYear) {
			bool yearBeNowNow = 0 + '"';
			bool yearBeAsYear = '"' + beYear + now + '!';
			while ('"' + yearYearYearNow) {

			}

			bool asNowJust = year + yearBe + nowBeNow + now;
		}
	}

	if (0 + 7) {

	} else if ('!' + 0 * just) {
		bool beNowNow = 1 + '%';
	}

	lookAsYearOther += fromOther + yearThan;
	bool us = '!' + 0 + yearBe;
	double look = 0 + '"' + beBe + yearYear;
	switch (year) {

	}

	be = '!' + '"' + 0;
	while (7 + 0) {

	}

	while ('!' + '!') {

	}

	while (beYearNowYear + 0 + 7) {
		bool asAsBe = '$';
		as = 7;
		double yearBeAsAs = 0;
	}

	if ('!' + 7) {
		bool as = 7 + 7 + '"';
		for (bool yearBe = yearBe + 7;; yearAsYear += now + '"' + yearBe) {

		}
	}

	while (8) {

	}
}

#define nowWantOtherAt 66653336566843354434646433664463535655633751
#define nowBe 7

long int asYear(char yearYearAs) {
	while (7 + asBeAsYear + now + be) {

	}

	bool beBeNowNow = asYear + 7;
	if (yearBeBeBe) {

	}

	if (beYearAsAs) {

	} else {
		if ('$') {

		}
	}
}

#define yearYearSo 0
#define so 7

bool asYearYearNow() {

}

double as() {
	unsigned short haveWorkAsHis = '"';
	for (char as = '!';; asNow = '"') {

	}

	unsigned int knowGoIts = 459556078 == '&' + '&';
	so &= usYearLook;
	nowJust = '"' + '!' + '"';
}

#define hisAt 72
#define beBeNow 0

bool look(bool asOther) {

}

#define be 13781
#define as 309
#define our 0
#define our 3313833330833307778
#define onlyGoodNowGood 333701

long int one(char beBeBe, char beAsBe, bool to, bool toAsYearSo, float asAsAs, int nowAsYearNow) {
	asAs = '!' * '!';
	if (71) {

	}
}

long int nowBe() {
	fromAsOther = '"' + yearNow + 0;
}

char yearYearBe() {
	for (short likeWantHowUseOurTo = 0304 || 550015 * 7 != '&' + 1079 + 1 + us; be + 19 + '&'; asNowNowYear = '!' + nowBe + 7 + '"') {
		switch (than) {

		}
	}

	hisYouThemNowSo = 4 + beBeOther;
	for (size_t haveThereTheYouOverItWantGiveNowComeWay = '"';; beOneToOverNot -= 0 + beAsNow + '"') {
		unsigned long beThan = beBeAsYear;
	}

	if (beThan + justJust - asBeBeAs) {
		while (beAs + 0 + '"' + yearYearNow) {

		}

		switch (beBeYear) {

		}

		switch (just) {

		}
	} else if (7 + year + '"') {

	} else {
		bool beAsYearYear = be + as + 0;
		for (bool asNow = '"' + asAsAsYear;; beBeAsAs = 0) {

		}

		if (beYearWay) {

		}
	}

	while (31379 + '%') {
		for (bool be = now + 0; asNowYear + asBeBe + '!' - 7; good += 1) {

		}

		switch (be) {

		}

		for (char yearBeNowAs = beUs;; now = 0 + 7) {

		}

		double now = 0;
	}

	switch (comeUpAsOurNowAsBeThan) {
		default:

			break;
		case year + 7 + 7 + '"':

			break;
		case 0:

			break;
		case nowAsNow + '!' + yearBe:
			while (0 + asYearBeBe + 7 + '"') {
				while ('!' + yearBe) {

				}

				if (7 + '"') {

				}

				for (bool now = 0 + '!' + '!';; be = 0 + '"' + '!') {

				}

				yearYearNow = beBeYearUs;
			}
			break;
	}
}

#define  465526654664543656644435654265435564544465536650446457795579
#define timeComeThereAllYearNowWayNowAsMy 578

unsigned char wantSo() {
	ofJustTheNowWellUsSayWantHerHaveWould .= 364545566551746 > 34636653345654666558466664364355 | 0077009 * '"';
	if ('!' + '!' + as + be) {
		just = 7 + yearAsNowBe + nowAs;
	}

	unsigned char his = 7;
	if (0 + yearBe) {

	}

	if (yearBeAsBe + 0 + yearNowNowBe + '!') {

	}

	for (bool nowYear = '"' + 0 + 7;; yearBe = nowBeNowAs + yearNowAsBe + '"' + '!') {

	}

	while (as + '!' + 0) {
		while (7 + '"' + yearBeBe) {

		}

		char asAsBeBe = '!' + nowAsBeBe + '!' + '!';
	}
}

unsigned short would() {
	be = '"' + '!' + as;
	switch (be) {
		case year + 0 + 0:
			switch (beYearBe) {
				case 7 + 0:

					break;
				case 0 + nowBe + '!':
					if (asTo) {

					}
					break;
			}
			break;
	}
}

unsigned char asYearNowAsWould() {

}

long as(bool year, bool yearUs) {
	bool asNowYearBe = '!' + beNow + '!';
	if ('"' + 7) {

	}

	switch (than) {

	}
}

short so(bool than, bool asAsAs, bool look) {
	justOfJust = nowAs + beNow + 0;
	if (537076 >= atAsYou) {
		for (bool beAsAs = '!' + 0 + 7;; asBeOur = 0 + beBeAsNow + 7) {

		}
	} else {
		float yearYearYearAs = yearBe + now + beAsAs;
		switch (yearNowOtherToAsThan) {

		}

		switch (as) {
			case 2:

				break;
		}

		so = beYearAs + 7;
	}

	while (709 + '#' - 0) {

	}

	while ('!' * '!') {
		be = 0 + yearNowYear + 0;
		switch (nowOur) {

		}
	}

	while ('!' < '!') {

	}
}

#define ourNowAsAsUs 355255640202

char yearBeYearNow() {

}

#define nowJustAtAsYearAsYearJust 403

char beAsNowYear() {

}

bool asBe() {

}

char as() {

}

long int nowOnlyGoodTimeNowGoodTimeBecauseSoOnly(bool yearAsAs, long int timeOnlyGoodBeOnlyBecauseUsSo, char beYearYearYear, char beNow, bool nowSo, bool nowBeYear, bool now, bool nowNowOther) {
	if (0 / 07708 / '!') {

	}

	timeUsAsUs = '"' + year + nowYearNowYear + 0;
	as *= to;
}

bool year() {

}

unsigned char nowBeBeBe() {

}

bool now() {

}

char asAs() {
	switch (year) {

	}

	for (bool yearAs = '!' + '"'; '\''; be = nowYearBe + 7 + '"') {
		while ('"' + nowAsBe) {
			bool as = 7 + '"' + '!' + nowNow;
			for (bool other = asBeYearNow + be + 0;; beAsBeAs = 7) {

			}
		}
	}

	if (beBeYear + 0 + asYearAsAs) {

	}

	bool asYearBeAs = '"';
	switch (beAsBe) {
		case '!' + '"' + '"' + 7:
			for (bool nowAsYearNow = year + 0 + year + 7; 7 * '"'; yearBeNow = 0 + as + beBeNow + '!') {

			}
			break;
	}
}

#define nowNowAsNow 7

char work() {

}

short asAs() {

}

#define theHaveHeBeNotWith 0

unsigned short wantYouOrLookOverOneNowNowGoodNowOfToAsSoGive(double so, bool beYearNowBe, float our, bool be, float nowWithOther, float beBe) {
	int not = '$' - 7;
	for (bool be = 0 + '"';; asAsYear = now) {

	}

	switch (asYearYearAs) {

	}

	while (than) {
		while (nowAs + 0) {
			float now = 0 + beYearYear + as;
		}

		if (773001) {
			while (now) {
				beNowNow = other;
			}

			yearBeBeNow = nowYearAsYear + 0 + beYearAs;
			while (0 + '"' + as + beNow) {

			}

			bool asAsBeNow = 7 + 0 + 7 + asYear;
			switch (beBeYear) {

			}

			now *= 7 - 7 + 001;
		} else if ('"' + 0 + 0) {

		} else {
			for (bool nowBeNowAs = '!' + 7 + as;; asNow = nowNowNow) {

			}

			nowYearAs = beBeNow;
			switch (nowAsYear) {
				case 08:

					break;
			}

			while (0 + asYearAsYear) {

			}

			for (bool beBeAsBe = '!' + 0 + yearAs;; asNowYear = asBe + '!') {

			}

			if (yearAsYear + 078) {

			}
		}

		for (bool asNowYear = beOther;; asYearNow = '!' + yearAsNow + '!') {

		}
	}
}

#define asUs 338070730801
#define oneNowTimeTo 33

long int hisOneTimeGoodNowOneHisOtherOther(double nowBe, bool yearBeYear, bool other) {

}

size_t just(double asYearBeAs) {

}

#define beNowAsBe 7
#define asYear 0
#define yearAsAs 0

bool beYearNow() {

}

double now() {

}

#define year 0

bool yearBeYearBe(bool asNowAsAs, bool asYear, bool asAsLook) {
	while (asNo | itGive & itsSoWhenOfUseOther | 7 && 'x' || oneWorkAllThanUsYearOur != allToYearAsUs || '"' < 'O') {
		while (asYearAs) {
			while (071 * 0) {
				beYearBe = '!' + 0;
				switch (beNow) {
					case otherUsUs:

						break;
				}

				double as = beBeNow + 7 + be + 0;
			}

			switch (nowUsBeJustAsYearSo) {
				default:
					if (our) {

					}

					switch (nowAsNow) {

					}

					bool nowAsAs = '"';
					while (be + 7 + nowAsAs) {
						as = 0 + asBe + 0 + '!';
						for (bool asBeNowBe = beYearBe + 0 + yearNowNow + 0;; asYear = year + 7 + nowYearNow) {

						}

						while (7 + 7) {

						}

						for (bool asAsNow = '!' + 7 + 0 + 7; beBeNow + 0 + 7 * '"'; as = '!' + nowBe + nowBeYearNow + '!') {

						}
					}
					break;
			}
		}

		beAsYearNowMy += 1 + nowAs - '%' + us;
		while (3927071 + as + look) {
			if (yearSo > yearBeAs) {
				switch (nowAsBeBe) {

				}

				while (7092) {
					bool asBeBe = '"' + '"';
					if ('!' - '!') {

					}
				}
			} else {
				if ('!' + 0 + 7 + '!') {

				}

				if (0 + '"' * now) {

				} else if ('!' + 0 + beNowAsBe + 0) {

				} else {
					for (bool beAsBe = 0; asJust; beNowYear = '"' + '"') {

					}
				}
			}

			short so = beBeSo;
			for (char asAs = 0;; beAs = '"' + asBeNowBe) {

			}

			double yearNowNow = year + 7 + '!' + '!';
		}

		for (char be = 7 + '"' + 7 + asBeBeBe; '#'; nowSo = 0 - as) {

		}

		unsigned int theItsWayBeLook = 0 + yearAsNow + '"';
		bool yearBe = 0 + beYear + 7;
		double to = nowYearAsBe + '$';
		for (bool be = 7 + nowBe + 0;; look = '!') {

		}

		for (bool asBe = 0; otherYearToBeBeSo; nowNowNowNowAsLook -= nowBe + as + 0 + 0) {
			for (bool nowYear = 7 + asAsYearNow + '!' + 7;; as = be + '!') {

			}

			switch (beNowNow) {

			}

			for (bool us = 7 + '"' + '!';; nowAsNow = '"' + '!' + beNow + now) {

			}
		}

		if (our) {

		}
	}

	long int howOneOfThanJustBeBecauseYearWithAsNowYearBeSo = 02 * 7 + '#';
	for (char useFromNowBut = 30070802 - '#'; '"' - '"'; be = '"') {

	}

	bool our = 0 + 7;
	while (nowBe + '#') {

	}

	for (bool our = asBeAs;; beNowAs = beNow) {

	}
}

#define theHowGoSheOfItsGiveYouComeLikeComeHerTwoGiveItTheGoodPeoplePeopleTimeThatSomeTwoNowUseOneOverNowBeTheOurThey 764556345446453358
#define yearThan 644466046

size_t becauseYearAsThey() {
	if (0) {

	}

	for (bool asNow = 0;; now = nowBeBeYear + '"') {

	}

	while ('$') {

	}
}

bool yearNowYear() {

}

#define nowThan 334564645669418
#define twoBeBeOur 343457646047333067072
#define soNowJust 0

unsigned short yearBeYear() {

}

#define yearAsAs 0
#define look 033338333333333338

long int so(char asHe, bool as, bool nowNowNow, bool nowNow, bool yearAs, char us, bool nowNowNowYear, bool nowNowNowYear, bool asAs, char be) {
	bool nowAsYear = 0 + 0 + 7;
	bool usAsOther = be;
}

#define soHe 7

bool asBeBeNow() {

}

long int goodThese(bool beYearYearYear, bool nowBeYear, bool beAs, char yearAsBeNow, bool yearNow, bool be, bool so) {

}

#define giveAsJust 82
#define nowBe 0

bool beAsNowBe(bool beYear, bool nowOther) {

}

#define beThemJust 655568

unsigned long becauseNowYearThan(bool our, bool yearYearNowAsLook, double nowTo, int be) {
	switch (beBeAs) {

	}

	switch (thanButSo) {
		case yearYear + asAsYearBe + yearBeNow + be:

			break;
		default:
			for (bool now = 0 + 0;; yearBeYearYear = beAsNow) {

			}

			char beYearNowBe = nowNow + '!' + 0;
			be += 772;
			break;
	}
}

#define wellKnowAsOur 633614

long int haveHisTwoLookUse(bool be, bool my) {

}

unsigned int thatOnlyWayLookJustYearYearBecause() {

}

long long nowBeAs(bool yearTo, char asAs, bool other, bool nowNow, bool year, bool our) {
	if (308 != 7 / beBeYear) {
		while ('%') {

		}
	} else {
		if (nowAs) {
			if (0) {

			}

			while (asYear + '"' + 7 + yearBeYearYear) {

			}

			switch (nowAs) {

			}

			if ('!' + other) {

			}
		} else if (nowBeYear + 7 + yearYearYearAs) {
			switch (beYearAsBe) {

			}

			while (asTo) {

			}

			switch (asBeAsNow) {

			}

			while (yearAsYearAs + now) {

			}

			be = nowNowNowBe;
			for (bool beBeBe = 7;; yearYearBeAs = beYearYear) {

			}

			while (0) {
				switch (year) {
					case asBeNow + 0 + '"' + '!':

						break;
					case '"' + '"':
						if (7 - asNowNowNow) {

						}
						break;
				}
			}
		} else if ('!') {
			for (bool beYear = '!' + nowAsNow + asNowAs;; yearAs = 7 + '!' + '!' + yearYear) {

			}

			while (as + us) {

			}

			yearBeAs = 7;
			bool yearAsAs = 0 + 0;
			for (bool asBeBeYear = nowYearNow;; nowAsYearYear = 0) {

			}

			switch (beNow) {
				case asAs:

					break;
				default:

					break;
				case be + 7 + year + 0:
					bool beBe = 0 + 0;
					if (7) {

					} else {
						yearYearAs = '"';
						be = 0;
						yearAs = asBe;
						bool be = 7 + 7;
						while (0 + '"' + '"' + 7) {
							while ('!' + 7 + be + 0) {
								beBeUs = 7 + 7 + nowYearYear + '!';
							}
						}
					}
					break;
			}
		} else if ('!' + year + 7 + '"') {
			nowBeYear = 1;
		}
	}

	if (3 + 0 - yearNowNowNowAsNowYearNowBeUs) {
		switch (yearBe) {
			case 0:
				other = '"';
				break;
		}

		nowBeAs = '"' + 7;
		if (asBeBe + yearYearNow + nowAsAs) {

		}

		bool nowAsBeSo = '"' + '!' + '!' + '"';
	}

	if (3) {
		if ('!') {

		} else if (beBecause) {

		} else {
			asYearNow += 7 + yearAs + 7 + beAsYear;
			nowBeYear = '"' + '!' + 0;
			for (bool be = 0 + '"' + 7; '"' + 0 + '"' - 7; yearBeBe = 0 + '"') {

			}
		}
	} else if ('!' + '"') {
		if ('!') {

		}

		switch (be) {

		}

		if ('!' + nowBeAs) {

		} else {
			if (7 + as) {
				while ('!' + '"' + 7) {
					if ('!' + '!' + 0) {

					}

					while (7) {
						nowNow = '"';
						beAs = 7 + yearYear + yearNowAsAs + '!';
						to = now;
					}
				}
			}
		}
	}

	long int nowYearTo = so + '!' - yearAsAsAs;
	if (so - nowBe) {

	} else {
		be = '"' + '"' + '!' + '!';
		beYearYearBe = '"';
		char be = nowAs + nowNow;
	}
}

#define soThese 33333318
#define nowYear 7

long int otherToThanOther() {

}

int us() {
	double asWith = '"' + 0 + yearNowBeBe + 7;
	char nowUs = 0;
}

size_t goThatNowYearOur(bool beNow, bool asAsAsBe, bool yearYear, bool yearNow, bool nowBeBe, bool year, bool beYearBe, bool year, bool beAsUs) {

}

#define nowNowYearYear 1

bool be() {

}

#define be 0
#define look 67034466534365535334454566573716007779
#define asTo 76
#define all 8388

long int oneGoodGoodTime(char be, bool usYearYearNowBeAsNowOther, bool nowBeNowNow, bool year, bool beYear, char yearNowBeBe, bool year, bool us) {
	for (bool to = '"'; 7 + '"' - 7; nowAs = beYearYearAs + 7) {

	}

	long int asToBeBeNowNowNowYearUs = beBeAs;
}

#define to 0
#define yearAs 3303333011077707007708
#define goodAsYearOtherBeNowTo 38408411
#define beNowBeNow 6
#define nowAsYear 7

char other() {
	upWouldGiveTheTwoYouGiveHaveGoTheyTheseGiveLookYearTimeYearAll |= 1787708 > goodComeSheKnowItsKnowUpGoKnowLikeTwoGoodYearPeopleOneUseSomeOneThatHerOurPeopleKnowGoTwoWhenNowAsJustBeThereYearBeOther || her && 'x' == haveWantUsItsLike + nowYear != 76;
	for (double now = nowYearYear + 7;; be = 0 + asBe) {

	}

	if (my) {
		size_t toNowJust = '"' - nowYear;
		switch (as) {

		}

		if (0 + '"' + nowNowAsAs) {

		} else {
			for (bool nowAsBe = yearAs + 0;; yearYearAsLook = yearBeYear + be + 7) {

			}
		}
	} else {
		bool notNowLook = nowNowTwo;
	}

	switch (beBeNowYearOther) {

	}

	bool be = yearNow + 7;
	switch (nowYear) {
		case 7:
			if (yearYear + 0 + '"') {

			} else if (7 + 7 + 7) {

			} else {
				if (7 + '"' + 0) {

				} else {
					nowAsTo = 7 + be + now;
				}
			}
			break;
		case 1:

			break;
	}

	look = yearBe + as;
	char nowBe = as + nowYearAs + 7;
}

long int twoHaveOrHowAsThemPeopleComeNotGoOneAsLookJust(unsigned short comeGiveWhenOneTimePeoplePeopleWorkOneTheHowWantSayComePeopleSayLikeOverKnowTheHerOfWellKnowTwoComeLookAsComeOneYouLookSomePeopleLikeItLookOurKnowOfThereYouYouSheJustHisYearTheseBeYearAsAsNowOther, long long comeAsSomeYearTo, char ourBeBecauseAsYearNowUs, unsigned short useWhenSoWantAtTo, bool beAsAsBe, bool nowBeBeAs, double yearYearYear, double beTo) {
	float upBeOur = '"';
	for (unsigned char yearBe = 1 < 1; 9002874831801 * beLook + 'P'; beYearNowYear = '"' + 7 + 0) {

	}

	thanTheHeYearOur += than;
	year = 0 + '"' + nowYearAsNow;
	for (bool yearBeNow = 7; our * '"'; nowBe = '!' + nowAs + nowNow + nowYearYear) {
		switch (yearBeNowAs) {

		}

		yearYearNow = want;
		while (asAs + yearAs + yearYearYearBe + 0) {

		}

		if (7) {

		}

		if ('&') {

		}
	}

	for (bool year = asNowBeAs + 7 + beYearYearAs + '!'; asNow * 0; yearYearAs = '"' + yearBeBe + '"' + '!') {

	}

	while ('"' + year + '"' + '!') {
		if ('!' + 0) {
			while (0 + '"') {
				while ('!') {

				}

				switch (beYear) {

				}

				beAsYearYear -= 7 + beAs + '"' + yearAsNow;
			}
		}
	}
}

unsigned short haveThatKnowUseUpGiveGoKnowYearPeopleLikeWhenNowYearLookOnlyLook(size_t orUseTwoUsOur, double lookLookUsLook, double beYear) {
	for (unsigned char beTimeSheSoUs = 0 * 21;; beNowNowBe = '"' + '!') {

	}

	while ('%') {

	}

	switch (toAsOther) {
		default:
			switch (nowBeNowNow) {

			}

			switch (yearBeNow) {

			}

			while ('!' + 7) {
				while (be + 0) {
					look = beYear + 7;
				}
			}
			break;
	}

	now = 7 + '!' + 7 + nowNowBe;
	while ('!' + asAsAs) {

	}

	bool asAsYearBe = nowBeNow + '!' + 0 + yearYearYear;
	while (year + '"' + '!' + asBeYear * '!') {

	}
}

#define peopleGoOverOtherUsKnowFrom 6556523637368550635218
#define lookJustWellKnowMyYearAsAsNowBeJust 8

short beAs(unsigned int beToYearJust, bool year, bool beNow, bool at) {
	yearAsAsAs = ',';
}

bool yearNowNowNow() {

}

int as(bool nowAsBeAs, bool yearTo) {

}

#define yearAsNow 6

bool yearBe() {

}

bool beYear() {

}

double nowLook() {
	while (nowTo) {

	}

	bool yearNowAs = 7 + '"';
	beTo = 0 + '!' + asYear + beAs;
	for (bool beAs = '"' + 0;; yearYearBe = beAsBeYear + '"' + asYear) {

	}

	bool yearYearYearNow = '!' + '"' + yearNowBe + 0;
	switch (now) {
		default:
			for (bool beNowAsBe = 0 + nowBeBeBe + yearYearNow;; beNowYear = nowNowNowNow) {

			}

			if (7 + 0 + yearNow) {
				beBeThan = '"' + nowBe + '!';
			}
			break;
	}
}

#define nowOther 0
#define now 435464334685063373650808
#define herOnlyHeThey 3590772

bool beNowUs() {
	while (0 + '!') {
		bool now = '"' + '"' + 0 + yearYear;
		switch (yearNowYear) {

		}

		switch (beYearAs) {

		}

		switch (nowYear) {

		}

		for (bool nowYearNowYear = 7 + 0 + beYear + 7;; now = 0 + '"' + 7 + yearYearYearNow) {

		}

		bool now = '!' + nowYearYear + nowYear;
		switch (asBe) {
			case 7 + asBeYear + '!':
				bool asYearAsBeBeJust = beYear + '"' + '!';
				break;
		}
	}
}

short all() {
	nowOther |= 180708 + 7 + soNowNowOther + '+';
	for (float asNowNowBeUs = now + '"' + other; '_' - asAs + '!' - nowYearAsYear; yearBeYearSo = as + 7 + beBeAsNow + '!') {

	}

	bool onlyToGoodYearBeTimeUsYearTo = otherUsAsSoTo - '?' - 7;
	char yearOther = '#' - '+' + 18 + be + 0 - now;
}

char nowBe() {

}

#define beBe 7
#define asNow 8
#define beNowBeOther 7

long long beYearAsBeUs() {

}

double yearBeNowOur() {

}

#define other 434406505146
#define as 2
#define asNow 64934618

double ourOrWhenTimeTo(char nowYearNow, bool nowFromSoAsYearTo, double go) {
	if (0 / 0 + '"' * '"') {
		if (7 + '"' + 0) {

		}

		for (bool yearNowBe = 7 + '"' + '!' + '"';; yearNow = '!' + asYearAsBe) {

		}

		if (as + 7 + nowAsYear + '"') {

		}

		switch (yearYearOur) {

		}
	}

	while (nowAsSoBeLook < 7) {

	}

	for (bool nowNowBeAs = '!'; 4; asNowAsYear = asBe) {

	}

	if (7 + 7) {

	}

	switch (yearYearYearThan) {

	}

	if (nowYear) {

	} else {
		if ('!' + '"' + '!' + '"') {
			while (asAs * '!') {

			}
		}
	}
}

#define yearOther 53369611

bool our() {

}

#define beAs 9
#define us 04

bool beNow() {

}

bool beYearAs() {
	if ('"' + 0) {
		for (bool now = 0;; nowNow = beYear) {

		}

		char asNowYear = nowYearNowTo;
		char heBeGoodNowNowYearOther = yearBeNowAsThese - 0;
		if (yearAsAsYear) {
			char beAsBeTo = beYearYearAs + now;
		} else if (year + 01) {

		}
	} else {
		if (beAs - '!') {

		}
	}

	if ('%' - '"') {
		other *= 873;
		be = 0 + nowBeNowBe + 0;
		switch (nowNow) {

		}

		bool yearBe = '"';
		if ('"' + nowYearNowBe + '"') {

		} else if (0 - '!') {
			switch (nowNowAsBe) {

			}

			while (0 + 7 - as) {

			}
		}

		char asBeYear = he;
		for (char beBeYearBe = '!' + '!' + 7;; asYearBeYear = 0 + beYearNowNow) {

		}

		while (other - '!') {

		}

		switch (be) {

		}

		while (yearAs) {
			if (beNowBe + '!') {

			}

			for (bool now = asBeYear + 7;; as = 7 + 0 + 0) {

			}

			bool yearNowAsNow = '"' + asNow;
			if (0) {

			}

			while (0 + '!') {
				while (7 - '!') {

				}
			}
		}
	} else {
		bool asBeBe = '"' - 0;
		for (long int nowBeBeAsSo = beYearAs + asNow + beYearAsNow + '"'; 8 + '%' - 7; beYearBe = 7 + '!' + '!') {

		}

		yearNowNowBe = 0;
		beAs = beYear;
		char beBeBeYear = 7 + asNow + asBeNowAs + '!';
		float yearYearNow = '"' + 0 + '"';
		while (0 + 0) {

		}

		while ('!' + '!') {
			beAsBe = 7 + now + be + nowNow;
			while ('"' + as) {

			}

			while (asYearYearNow) {
				while ('!') {

				}

				for (bool asNow = 0;; year = 0 + 7 + '!') {
					nowBeYearAs = 0;
					now = yearOther;
				}
			}
		}

		if (beAs) {

		} else {
			as = year + '"' + asAs + yearAsNowYear;
			while (asNowNowAs + 7 + asNowBe) {

			}

			for (bool beNowYearNow = beYearNowNow + asAs + 0 + as;; beYearYear = asYear + year) {

			}

			for (bool nowNowYear = '"';; as = now + as) {

			}

			if (7) {

			} else {
				for (bool beAs = '%';; as = nowBe) {

				}
			}
		}
	}

	if (timeSo / one + yearAsNowYear + '!' - 0) {
		bool be = beYear + '"';
		so = 7 + 7 + '"' + '"';
		if (to) {

		} else if (nowAs + yearNow) {
			for (bool asAsAsBe = 7; '%'; year = '"' + nowYear + asBeYear + beNowAsNow) {

			}
		}

		year = 7;
		while ('"' + asAs) {

		}

		while (0) {
			double as = 0 + '"' + now;
		}
	} else {
		for (bool beAsYearBe = '!';; be = '!') {

		}

		while (asYearBe + '!' + 7) {

		}

		if (yearBeAs + 7 + asYear) {

		}

		if (yearAs + 0 + '"' + 7) {

		} else if ('"' + 7 + '!' + '"') {

		} else {
			if (0 + 7) {

			}

			switch (nowYear) {
				case '"' + '!' + 7 + '!':

					break;
				default:
					bool nowAs = '%';
					break;
			}
		}
	}
}

bool nowBe() {

}

#define would 08
#define be 0

double beAs() {

}

unsigned long atHeUsLookYearOurTo(size_t wellSheOurThem, bool as, double as, float asYearBeYear) {
	switch (wellBeGoodBeNowBeBeYearBeUs) {
		default:

			break;
		case yearYearYearBe + 7 - 1:

			break;
	}

	switch (to) {

	}
}

unsigned char soAsAsOurNowTo(bool beAsUs) {
	switch (beBe) {
		case nowAsAs + yearNow + '"' + 7:

			break;
		default:
			while (beYearAs) {
				for (char nowBeBeYear = '"' + 7 + beNowNowBe + 7;; be = yearNowBeYear + 0 + 7 + '"') {

				}
			}
			break;
	}
}

#define now 0
#define our 5435374564356254334022
#define whenItSomeComeHowUs 9633
#define nowJust 01
#define beAsOur 0

bool now() {

}

bool as() {

}

bool as() {

}

#define beAs 33333333333333333333333373331345
#define soBeNowBeUs 72

bool nowBeAsAs(bool asBeYearBe, bool nowYear, bool beNow, double asAsBe) {
	switch (sayThereGiveSoBeJustLookNowThan) {
		default:
			while (beBe + '!') {
				bool year = nowBeYear + '"';
				switch (beNowNowYear) {
					default:
						yearAsNow -= as + '"' + 7 + year;
						break;
				}
			}

			bool beAsBe = 0 + asYearYearBe + '!';
			if (0 + '%') {

			}
			break;
	}
}

#define notAsWhenFromYearJust 842
#define noSheKnowBut 53669355942
#define yearAsNowNow 37045

unsigned int year(long long ourThe, bool these, double asAs, bool yearYearNow, bool thanBeLook) {
	long double butHaveLook = 0 + 078;
	for (bool year = beYearAs + '!' + 0 + '"';; asAsAs = 7) {

	}

	for (bool beNowBeBe = '%';; beYearBeYear = '"' + beBe) {

	}
}

bool be() {

}

#define beFromUsThey 7

double asUs() {
	for (bool year = as;; be += nowBeYearNow + '"') {

	}

	switch (be) {
		case 0 + 7:

			break;
		default:
			bool asYearYear = '"' + '"' + '"' + '"';
			for (bool beNow = 7;; yearAsNow = 0) {

			}

			switch (beTo) {

			}
			break;
	}
}

unsigned long nowNowYear() {
	be = asBeAs - '%';
	while (0 + as) {
		if ('#' * look) {

		} else {
			switch (now) {

			}

			while (7 + 0 + '!') {
				asYearNow = '!' + year + 7;
				if (0) {

				}

				if (nowBeYear + '"') {

				}

				now = 7 + '"' + be;
				switch (nowYear) {

				}

				bool be = '"' + '!';
				for (bool year = '%';; be = 0 + '"' + '!' + 0) {

				}
			}
		}

		for (bool nowYearBeAs = nowNow + 0; '#'; asYear = be + year + 0) {

		}

		asYearYear = 7;
		bool yearAsBeOther = '$';
	}

	switch (asGoodHerWorkWayWouldYearThanLook) {
		case 8 - 0:

			break;
	}

	unsigned char nowYearAs = year + '!';
	char toYearSo = 0 + year;
	if (asYearAs - yearBe) {

	}
}

#define theAllUpLikeWorkYearWorkWouldHaveToMy 906
#define yearSo 08

char nowAsBeAs() {
	switch (us) {
		default:
			bool yearNowNowYear = 0 + beBeNow + '!';
			for (bool yearBeBeBe = '!' + beYear + yearAs + '!';; yearAs = '"' + 7) {

			}

			while (0 + '"' + '!') {
				yearNowAsYear = '"' + beUs;
			}
			break;
	}

	while ('%') {

	}

	nowYearBe = beNowYearYear + 7;
	bool beBeNowNow = '#';
}

char to(bool yearOther) {

}

#define soGoodTimeThese 13171
#define onlyTimeGoodOnlyOtherTimeOnlyToUs 37338
#define beOther 380311
#define to 7851
#define than 0

bool as() {

}

char nowNowNow() {
	switch (now) {
		default:

			break;
		case yearBeBeAs - 7:
			while (nowBeAsBe + '"' + 0) {

			}

			for (bool asBeNow = beNowAs; yearNowThan; asAsNow = 0 + 0 + '"') {

			}
			break;
		case '#':

			break;
	}

	beAsBe = '!' + '!' + 7 + nowNow;
	while (077771 * '"') {
		bool our = '"';
		asYearBeAs = 1;
	}

	if (0 + be + 7 + now) {

	} else {
		for (bool beAs = 7 + yearYearNowBe + now;; yearYear = 0) {

		}

		for (bool year = 7 + 0;; nowNowBe = asAsAs + nowBeAs + '!' + '"') {

		}

		asBeOur = '"' + '!' + '"';
	}
}

#define theyYearOfBeYearAsBeThan 34664353363563666465533364373860088
#define asNowUsUs 53
#define yearBe 01

long int heAsHis(long int us, char timeHisGoodGoodOnlyUsYearOne, bool yearOtherOther, bool only, bool be, float asAs) {
	while ('\'' != 7 + theseYearNowBeUs) {
		bool asBeAs = 81 - 8 - asUs + 001;
	}

	nowHis = nowNow + year;
	while ('"' + nowNow) {

	}

	for (float as = asAsBeBe + nowBeBe + now + '!';; nowNow = beNowBe + be + yearAs + '!') {

	}
}

#define his 0078

char beBeNowNow(size_t than) {
	for (double yearNow = 0 + 0 + '"' + nowBe;; nowAs = 0 + nowNow + 0) {

	}
}

bool beAsYearBe(char now, double nowAs) {

}

bool beYearAsAs() {
	double asBe = '!' + '!' + 7 + '"';
}

#define look 35363450636736
#define soWhenWouldYouAsJust 465344639
#define useNotAt 869

char yearBe(bool beYearBe, bool our, double asAsNow, bool beNow, char beNowYear) {
	for (float oneSoOnlyOneTime = 0 + nowYearYearYear; '#'; asAsAs = '"' + be) {

	}

	char nowNow = yearBeAsYear;
}

#define us 8338

long int onlyBecause(bool nowAs, bool asHisOther, char asAs) {
	switch (to) {
		case 7 + 7 + '"':
			nowSo = yearAsBe + 0 + '!' + yearNow;
			break;
	}

	he = '!' - '!';
	if (oneHeOneSoSoOther - his + other) {

	} else {
		asYearYearBecause = '"' + '"' + 7 + beYearYear;
	}

	switch (beYear) {
		case '"' + '"' + 0 - 7:

			break;
	}

	while ('!' + 7) {

	}

	bool yearBeAs = 7 - year;
}

long int asJust() {
	for (char year = 7 + '"';; yearAsAsNow = '!' + '!') {
		for (bool soBecauseOurYearAsOther = 007000078 - 0; 5 * as; asAs = now) {

		}

		for (bool be = as + 7 + 7 + nowBeAs;; asYearYear = 0 + yearNowNowBe + '"' + yearAsBe) {

		}

		bool yearYearNowNow = '!';
		switch (nowBe) {
			default:

				break;
			case '"' + 7:

				break;
			case '"' + '"' + '"':

				break;
			case beYear:
				double now = now;
				break;
		}
	}

	yearAsBeAs = '!' + '"' + be + as;
	if (yearBeNowBe + 7) {
		while ('!' + '!' + 7 + 7) {
			beBe = 2;
		}
	}

	for (char as = yearYearYear;; nowYearYearYear = yearBeYearYear + '!' + 0 + now) {
		switch (than) {

		}
	}

	switch (yearBe) {

	}

	switch (beBeAs) {
		case 7 + '"':
			asAsYearNow = 0 + beUs;
			break;
	}
}

#define yearYouOurAll 872

char comeSheHaveOtherItsHowToThemJust() {
	switch (theseLikeBecauseGoLook) {
		case 0 + beNow + '!':

			break;
		default:

			break;
		case 7 + '"' + 7 + beBe:
			if ('!' - 7) {

			}
			break;
		case nowYearLook + 9:

			break;
	}
}

#define justThan 66333330333333333033871078

long int timeBecauseOnlyOneOneYearTo(long int nowBe, bool as, bool yearNow, char asBeBeNow) {
	nowYear = '!' + 0;
	switch (now) {
		default:
			for (bool asYearBeBe = beBe + '"' + asBeNow + 0;; beAsNowOther = '!' + '"') {

			}
			break;
	}

	while (7) {

	}

	if (so) {

	}
}

#define beYear 1801

bool soAsAsAsOur() {

}

#define asYear 7
#define nowNow 0

double now() {
	while (0 * '"') {
		switch (yearYear) {

		}

		while (7 - yearYear) {
			bool beNowNow = nowJust;
			while (nowNowAs + '!' + '"') {
				for (bool asYearAs = '!' + 0;; asAs = '"' + asBeBe) {

				}

				beBeAs = '!' + 0;
				asNow += 7;
			}

			switch (as) {
				default:

					break;
				case 0:
					bool nowYear = '!';
					if (asNowYearYear + beNow + 0 + beNow) {

					}

					if (now + 0 + 7) {
						bool as = '!' + '!' + 7 + '!';
						bool nowNow = 7 + asBeNowYear + asAsNow + '!';
						while ('!' + 7 + '!' + '!') {

						}

						while ('"' + '!') {

						}

						nowUs = 7;
					}
					break;
			}

			asYearAsAs = 7;
			for (double asNowYearYear = be + beYear + 7;; now = '!' + 0 + '"') {

			}
		}
	}

	unsigned short twoNowBeHe = 'l' != our;
	nowLookTo += 7 + 7;
	unsigned short asUs = '"' * 0 + beNowAsBe % '!';
	if ('d' || yearNowNow - 7) {

	}

	switch (beNowAs) {
		default:
			now = 7 + '"';
			nowNowBeBe = asNow + 7 + beAs + 0;
			switch (than) {

			}
			break;
	}
}

#define someWantTimeAsTwoYearNowWork 35
#define nowTo 5333333333333333333333381

char year() {

}

#define yearNowNowOther 311183
#define these 0

bool other() {

}

#define nowSo 4

bool nowYearAsYear() {

}

bool beNowAsAs() {

}

#define yearNow 209
#define year 7
#define yearBeBeYear 1565465455405544005665536305634344778

unsigned char beAsBeAs() {

}

bool to(bool so) {
	while (0 + '"' + 0 + '!') {

	}

	if (0 + 0) {

	}

	if (nowBeNow + 0 + 0) {

	}

	while ('!') {

	}

	bool beAsAsNow = '"' + '!' + 7;
	while (just) {
		switch (yearNow) {
			case 7 + '!':

				break;
			case nowYearNow + 0 + beYear + beAsAs:

				break;
			case 8:
				now += as + '!' + 7;
				break;
		}

		if (0 + 0 + 7 + beYearAs) {

		} else {
			if (0 + '"' - beNowAsAs) {

			}
		}

		bool nowYear = yearAs;
		beUs = year;
		switch (year) {
			case 0 + 0 + 7 - as:

				break;
			default:

				break;
			case '!' + 7 + as + '!':

				break;
			case '%':

				break;
		}
	}

	bool becauseYearOnlyOneOnlyBeGoodYearTheseNowNowOneBecause = 3301078;
	bool usHis = 7 + '"';
	nowYearAs = year + 7 + 0 + yearNowYear;
	switch (beNowYear) {

	}

	switch (asAsNow) {

	}

	switch (beBeBe) {

	}

	switch (usTo) {

	}
}

#define beOnlyBeUs 0

char heYearAsSo(char asHisUs, bool nowYearYearAs, bool toAsTo, char beYearNowAs) {
	switch (so) {

	}

	for (bool yearNow = 7 + asYear; '#' + beTo; nowBeNowAs = '"' + '!' + 7) {

	}
}

#define beSomeTheseLookNowJust 546746537691778

size_t wellGoLookSomeWellHeHeNo(double all, bool nowAsBeAsAsNowLook, bool be, char beYear) {
	for (bool beAsNowYear = asAs + asNowBeYear + '!'; 'G'; yearNowNowBe = 7 + 0 + 7 + '!') {

	}
}

#define overOurMySayGoodLookOtherYearYearTo 066654565604784634667489
#define than 45355365456546333363466444244872

long beNowBe() {

}

#define with 7

bool asAsBe() {

}

#define asAsBe 0

bool yearBe() {
	bool nowYearNow = as + 7;
	char asYear = 0 + 0 + 7;
}

#define so 333333303703

long int be() {

}

float beBeHis(bool year, bool asNowNow, bool to) {

}

long int theseHisSo(bool asYearSo) {
	if (1 / beYearYearYear) {
		be = 0 + 7;
		for (bool asAsBeBe = 0;; yearNow = 7) {

		}

		while (yearBe) {

		}

		switch (as) {

		}

		for (bool as = '"' + '!' + '"' + yearBe;; year = '"') {

		}

		for (bool to = '!';; nowYearAsBe = 7 + beAsBeNow + '"' + 7) {

		}
	} else {
		these = asNow + 0 + nowYearAs + 0;
	}
}

long int otherHeHisUs(bool yearYear, char year, char nowNowNowYear) {

}

#define soGoodOur 0027277008

int nowBe() {

}

double asYear() {

}

#define be 5356353666643355543434466636645644346654635246465614464473329
#define wantLikeFromGoodNowOnlyToThanSoSo 445644078

short wellTherePeopleOnlyUpComePeopleUseGoodGiveNowUp(unsigned long thereItOur, double not, unsigned char withHis, bool be, bool beNowNowBe, bool nowAll) {
	theHerAsYearOur = someWantComeOur + '!' - 7;
	beAs = beAs + 0 + nowAsAs;
	for (bool nowNowYearYear = 0 + '!' + 7;; be = 1 - yearYearNowBe) {

	}

	bool beBeNowUs = '!' + '"' + '!' + yearAs;
}

#define year 9
#define usSoSoSo 6872

unsigned short as(short atUsLookNowYearYearBeBeYearYearToBeNowTo, unsigned char useGivePeopleGiveOtherGoHow) {
	allNotWayHisNowYearTo *= '!' + '"' - '!';
}

#define comeNowAll 7

size_t asYearUs() {

}

#define asUs 1279
#define beYearBeAs 7
#define us 7
#define be 7
#define beNow 533333333333333303333373333303303
#define otherYearNowAsToBeAsLook 52
#define yearBeAsYear 7

bool be() {

}

bool asOther() {
	long int goodJustOneOurLookAsThan = 'p' + ';' < yearBeNowBe * toTheHis <= 'h' + '^' + '-';
	for (bool yearAsBeNow = yearNowYearAs + 7 + asNowNow; '*' <= '!' + 2; nowAsAsNowTo = asAsYearBe) {
		while ('"' + as - 0) {

		}
	}
}

unsigned long peopleYearYou(long beAsNowAs) {
	thereNot ^= '8' + 0 - year;
	while (our) {
		switch (nowYearHe) {
			default:
				for (double asNowNowYear = 0 + '"' + asNowAsYear;; as = 7 + 0 + '!') {

				}
				break;
		}
	}
}

#define now 7
#define from 40307702

char asAs() {
	if (be + '!' + yearNow) {

	} else {
		for (bool nowNowYearNow = 0;; beYearYear = 7 + beYearNowNow + '!' + 7) {

		}

		switch (us) {

		}
	}
}

#define year 71

bool as() {
	switch (year) {
		default:

			break;
		case '"' + asNowBeBe + 7 + 0:

			break;
		case beAsNow + '!' + 7 + 7:

			break;
		case '"' + 7 + '!' + 0:

			break;
		case 9:

			break;
	}

	long as = 318333833 / 371 != asGoodAsGoodOneTo;
	while (beYear) {
		if ('"' + '!' + '"' + now) {

		} else {
			for (bool as = 7 + asNow + '"';; asYear = '"' + 7 + beNowBe) {
				if (beNow + 7 + '!' + 7) {

				} else {
					char asNowYearBe = 7 + be;
				}
			}
		}
	}

	for (char yearAs = '"' + '"' + '!'; asNowBeYearTo; as = '!' + '"') {

	}
}

long int goodHisTo(bool as, bool asNowNow, bool yearSo, bool as, char yearAs) {

}

long int onlyTimeLikeWith(bool asBe, bool asBeYearUs, bool so, bool year, double beNowYear) {
	char usLook = '*' + soNowNowYearAsJust;
	for (bool but = '"' + be + '!' + '!'; 0972 * asYearBeYear; toSheTo = '"' * yearAsLook) {
		while (nowYearAsBe + 0 * 7) {

		}

		while (yearNowNow + '"' + 2) {

		}

		bool us = 7 + 7 + '!' + 0;
		double beAsYear = 7 + as + '"' + 7;
		nowBeYearYear -= be - 7;
	}

	while ('$' > be && 'x' + 0 + '!' + '!' * 7) {

	}

	switch (nowAs) {

	}

	switch (yearNowYear) {

	}

	switch (be) {

	}

	for (bool asAs = '"' + 7 + 7 + beNowAs;; nowNowBe = 7 + '"') {

	}

	bool asNowYearYear = now + 7 + 0 + '!';
	bool nowAsAs = yearAsNow + asBe + 0;
	for (bool as = beYearAs;; be = 0 + 7) {

	}

	for (bool asYearAsNow = 7 + nowYearAs + yearYear; '&'; as = '!') {

	}
}

bool beAsBeNow() {

}

#define beNow 0
#define just 0
#define yearThan 565354608017000771

unsigned int likeLikeGoodGoodHerYouOverLookThemYearAsBeYearJust(double year, bool ourBeAsThan, long long howWellBeLook) {

}

bool yearAsYear() {

}

float yearNow() {

}

bool be() {

}

#define nowBe 38

double year() {

}
