#define oneTimeOneOnlyOneGoodOnlyGoodOneOneOnlyGoodUs 33873337333475478
#define yearNowBeAs 7
#define as 701

char nowBeNowYearAsOur() {
	while (onlyNowLookJustNot) {
		useGiveUpOnlyPeopleGoodTimeNowOnlyBeAsSo = 07071 - '!';
		as = '!' + '!';
		bool be = '!';
		switch (asHeUs) {

		}

		switch (yearNowAsBeOther) {

		}

		for (bool yearBeAs = '"';; nowBeBe = '!' + beBeNowNow + nowBe + '"') {

		}

		if (yearAsAsBe + '!' + '"' + 7) {
			while (beYearBeNow + 7) {

			}

			switch (beYearAs) {
				default:

					break;
				case 0 + be:

					break;
				case beAsTo:

					break;
			}
		}
	}

	for (bool asAs = 0 + nowYear;; nowAs = 7 + beYear) {

	}

	while (3 + 8) {

	}
}

long int toGoodYearAsBeUs(long int goodTo, char asBeAsNow) {
	char hisBeSo = '#';
	while (81) {
		for (char asBeNow = 7 + '!' + '!';; yearNowNowYear = 0) {

		}
	}

	switch (asAsYearBe) {

	}

	if (nowAsNow + '!' + '"') {

	}

	for (char nowAsAsYear = '"' + yearNowAsYear + 0 + '!';; yearAsAsAs = 7 + year + 0) {

	}

	nowNowYearAs = yearAsAs + beBe + '"';
	for (bool now = 7; '!' + '#'; yearNowAsNow = 0 + '"' + as + '"') {

	}
}

#define timeTimeTimeUpUseUpYouSheHaveWorkTheGiveNowComeThatTheyOfOfOfOrUpOverKnowOverSayYouItsWantTheTwoWhenGoItWhenKnowTimeSayPeopleUpSheSayOrOfOfGiveTherePeopleWhenOnlyItsGiveOnlyOnlyOfItsThereTheBeItItsOfOfTwoYouOfUpHowTwoTheLookNowNowYearSomeYearLike 336544665685645565536643553356346443656464551653463346655331107209
#define nowAsBeYear 544434355559645545564464734972

char year() {

}

#define nowHaveBut 7

bool yearNowOur() {
	bool year = 7 + 0;
	bool beWay = '"' + 0 + '!' + 7;
}

#define nowAsAsBe 98

long long year(bool yearNow, double yearAs, bool other) {
	for (bool as = 7;; asBe = 7) {

	}

	while (0 + '!' + 0 + '"') {

	}

	switch (be) {

	}

	for (bool than = '"' + 7 + '"' + year;; nowNow = beNow + '"') {
		while (asBe) {

		}

		asAsYear = 0 + 7 + beAsAsNow;
		switch (asYear) {

		}

		now = 7 - asYear + 701;
	}

	long int onlyOnlyUsBecauseYearOtherBeTo = 0;
	for (bool asNowAsYear = nowNowBe;; other = '"' + beYear + yearYearAs + 0) {
		if ('!' + yearNowAs + '!' + 0) {

		}

		switch (to) {
			default:

				break;
			case 7 - 7:

				break;
		}
	}

	while (as + asYearBe) {
		while ('"' - '!') {

		}
	}
}

long int to(bool timeHisSoYearUs, char asAs) {
	while (11 + nowBe + '%') {
		if (be + '#') {

		}
	}

	bool yearUs = '+' - beNow;
}

#define becauseHisToGoodUsGoodGiveHow 92

long double beBe(bool yearAsYearBe, bool asYearBeBeUs) {
	bool year = 7 + asYearBe + '!';
	switch (yearBeNow) {
		case year + '"':
			double asBe = '"' + year + '!' + 7;
			break;
	}

	switch (asYearNow) {
		case 9 != '&' * '!':

			break;
		case now % '}' < 0:
			be = '"';
			for (bool nowNow = '!' + '"';; beAsAs = '"' + yearYearYear) {

			}

			for (bool beNowAs = '"' + yearYearBeNow + 0;; beBeAsAs = '"' + 0 + yearYearAs + year) {

			}

			us = asYearBeAs * '"';
			break;
		case '"' + 7 + nowAsYearBe + '!':

			break;
		default:
			while (7 + nowBe) {
				if ('!') {

				}

				switch (year) {
					default:
						char beAs = 0 + as + '!';
						break;
				}
			}
			break;
		case '!':

			break;
		case '"' + '!':

			break;
		case 0 + '!':

			break;
		case 7:

			break;
		case 0:
			if ('!' + 7) {

			}

			while (0 + asNowBeNow + '"' + 7) {

			}

			while ('"' + be + 0) {

			}

			switch (nowNowBeBe) {
				case yearUs:

					break;
			}
			break;
	}

	for (unsigned short beAsBe = nowBeOur;; herLookBeThem += 7) {
		beBe -= asNowYearNow + '!' + asBe;
	}

	switch (comeOverThatWellYearWhenSoWorkSayJustThereUseThatOneHaveHeHerNowOurAsOurUs) {
		default:
			while (0) {
				nowNowAsYear = '!' + 7 + '!' + beAsAs;
				switch (asYear) {

				}

				for (bool yearNowNow = '"';; nowBeBeBe = 0 + '"' + '"' + beYear) {

				}

				double yearBe = beAsAsYear + '"';
			}
			break;
	}

	switch (beNow) {
		default:

			break;
		case '!':

			break;
		case yearBeYear:
			switch (beJust) {

			}
			break;
		case '!' + beAs + nowAsAsAs + '"':

			break;
		case 0 - '"':

			break;
	}
}

unsigned long haveHerTheSomeUseItAtUpAllBeUs(unsigned int knowSoOnlyOnlyOurOverOur, unsigned int sheAsGoodLookUsLook, bool beYearBe, bool just, char yearBeYearYear, bool as, bool his) {
	unsigned char knowKnowPeopleGoGoGoTwoGiveWantHowSheOurOneUsComeHaveLikeTheGiveGiveGoodOfOnlyFromNotLookJust = 5633654545436556364846 | upMyBeUsUpJustUsUs || timeOfHerAsSoKnow | 44636440508022075 & 7 > ',';
	if (0 + yearAs) {
		for (bool asYearNowAs = beAsBe - '!';; be = 0 + 0 + 7 + nowAsAs) {

		}

		nowBeYear += '"' + asYearYear + 0 + '"';
	}

	unsigned int itYearAsOtherAll = nowNow + asJustOur + 7 * yearBeNowAs;
	switch (other) {
		default:
			if (yearYearYear + '"' + 0 + '!') {

			}

			switch (yearBeThan) {

			}
			break;
		case '$':

			break;
	}
}

#define someBeSoNowSoYearTo 4445366563464956643333333663466436464666844633556343556344433434446552

size_t goOverSheGoodTwoWhenPeopleWantItHowLookWantWellHaveGoHowTheLikeBecauseTwoUpKnowGoWorkLikeSomeOverWouldOneWhenHowNowToUsYearToUs(unsigned short peopleOneBeUpAsUs, bool would, double know, bool yearOur, bool nowBeNowWith, bool year, bool asNowYearYear, bool nowYear, char be, double yearBe) {
	for (unsigned short yearAsAs = 01 + 000709; 0 * '!'; nowSo = asNowAsNow + 7 + '!' + '!') {
		while ('!' + beAs + '"') {

		}

		switch (year) {

		}

		for (bool yearYearNowAs = 0; '#'; beAsAsBe = now + '!' + beNowBe + asBe) {

		}
	}

	ourLookBeOther = beYearAsBeOther + 7779;
	so *= '"' + '"';
}

#define sheOur 14313070701
#define beYearAsSo 3
#define nowNow 002
#define beAsYear 0
#define be 336556433545474344045129

size_t wantThanSoOfUs(unsigned int asAs) {

}

#define beYear 8
#define heNowOther 1

double now() {

}

bool now() {
	if (asYear + 0) {

	} else if (beYearYearAs - as) {

	}
}

#define yearOther 333333333133333333333333088
#define nowNowNowAs 33333378
#define beYearBeAs 001

double nowTo() {
	while ('!' + beYearAsYear) {

	}

	while (0 + 8) {

	}

	for (double beBeBeNow = 7 + 0 + 7;; as = 0 + beNowYear) {
		while (be) {

		}

		if ('!' + nowNow) {

		}

		while (0 - 7) {

		}
	}

	if ('a' == '!' + asAsBeNowOur) {

	} else if (beNowNow + 7) {

	} else {
		long double us = 702;
	}

	to -= 7 - beFrom;
	yearYearAs = 0;
	for (bool asYearNow = nowAs + '!' + 7;; nowNowAs = '!' + '"') {

	}

	while (asBeYear + '"') {
		now = '"' + '"' + nowBe + 7;
		while (just) {

		}
	}
}

#define than 46687304464654436771
#define goFromSoUs 7

bool asUs(char beAsNow) {

}

long yearNow() {

}

bool nowNow() {

}

int yearBe() {
	yearAsNow = 0 - be;
	if (173) {
		for (bool now = asAsAs + 0 + beYearNow + now;; us = '"') {

		}

		if (as + '!' + 0 + 0) {

		} else {
			asYearOther = 7;
		}

		timeYearHe = 0 + 0 + '"';
		for (bool be = '!';; asYearAsYear = '!' + 7) {

		}

		bool now = yearTo;
		switch (beOtherYearOther) {
			case nowAsYearYear:

				break;
			case asAs + 73 + '!' + 1:

				break;
		}
	} else {
		while ('!' + 0) {

		}

		if (his + '%' + 0 + '%') {

		}

		if (to + 0 + year / 7) {
			switch (onlyThese) {
				default:

					break;
				case 0 + yearYearYearAs:
					for (char nowAs = yearAsAs;; beAsNowAs = '!' + beNow + yearNow + 0) {

					}
					break;
			}
		} else {
			if ('"' - asYear) {

			}

			other = '"' + 0 + yearYearYearBe + beAs;
		}

		if ('"' + beAs) {

		}

		if (7) {
			switch (asYearAsBe) {
				case beAsBeYear + '!':

					break;
				default:

					break;
				case '!' + 0:

					break;
				case '!' + asAsYear + '"':
					switch (asAsNow) {
						default:

							break;
						case 7 + be + '!' + '"':

							break;
						case '!' + asYearNow + '!' + '!':
							if (0 + '"' + yearNowBe) {
								if (beAsNowYear + '!' + beYearNowBe + '"') {
									while ('!' + '"') {
										while (beBeBeBe) {

										}

										for (bool asYearYearAsAsSo = 0 + 7 + 0;; beNowBeYear = nowNowYear + nowYearYear + '!') {

										}
									}
								}
							}
							break;
					}
					break;
			}
		}

		if ('!' + 0) {

		}

		asYearBeBe += '"' + '!';
	}

	while ('"' + '"' + asBeAsNow + 0) {
		if (0 + '"' + 7) {

		}

		long int soAsOtherSo = '!' + 7 + beAsNow;
		beSo *= beAsBeAsUs;
		if ('\'') {

		}
	}

	bool be = 8;
	bool otherBecause = 0 + 7;
	switch (beYear) {

	}

	if (beNowAs + '!' + '"') {

	}

	while ('"' + 0) {
		switch (so) {

		}
	}
}

long int his(bool beOther, bool beAs, bool asOur) {
	be = 0 + 7;
	long int asBeAsYear = '"' + 7 + 0 + '"';
}

#define so 0
#define so 654468219

bool be() {

}

#define lookAsAsBeOtherTwo 475

int beBe() {

}

unsigned short timeOfOnlyGiveUs(bool as, bool nowBeNow, long yearBeYearNowNot, bool hisBeTo, bool asSo, bool nowUs) {
	char beSoBeBeOther = '%';
	while ('"' + nowNowNow + now) {
		asNow = 7;
		char be = now + yearBeAs + be;
	}

	if ('!' + '!' + year + '"') {
		us = 0 + 0 + asAsBe;
		if (asNowNow + '"' + 7 + asAs) {
			if (asAsNowAs + asYearAsYear + '!' + 0) {
				switch (beNowBe) {

				}

				for (bool asNowAs = 0 + be;; asNowBeYear = '"') {

				}

				while (yearYear - now) {

				}
			}
		}
	}

	bool nowBe = '!';
	bool year = 7 + '!' * 0;
}

#define now 0

long double beBe() {

}

#define orNowNowBeSo 8

double be() {
	if ('!' + beBeNow + nowSo) {
		if (40702 && '>' + '%') {
			theyBeLook = 18;
		} else if ('!' + 0) {
			while ('!' * 7) {
				nowBeTo = '!' + '"' + yearBeYearBe;
			}
		} else if (7) {
			for (bool asYear = '!';; nowAsAsYear = 7 - '"') {

			}
		} else if ('"' + yearBe) {

		} else if (0 + nowYearNow + '!') {

		} else if (7 + 0) {

		} else {
			while (as + '"' + yearBeAsYear) {
				switch (as) {
					default:
						if ('!' + beBeBeYear + 7 + '!') {

						} else if (asYearNowBe + 7 + 0) {

						} else if (asNowNow + nowYearNowYear + '"') {

						} else {
							while (7 + 7 + 7) {

							}

							asNowBe = 0 + beYearYear + 0;
							if (0 + '"') {

							}

							switch (yearBeAs) {

							}

							bool nowYear = beThan;
						}
						break;
				}
			}
		}

		for (bool now = '!';; now = 7 + yearYearYearAs + nowAsAsAs) {

		}

		for (short yearAsBeBe = year + beOur; asNowYear - as; beBeUs = asNowNowBe + '!') {

		}

		for (bool asBeYear = 0 + yearNow + be; '"' + '"' + 71; asAs = 7 + '!') {

		}
	} else {
		switch (beNow) {

		}

		bool beAsBe = now + beNowYearNow + asYearAs + yearBeBe;
		for (double nowNowAsNow = '"';; beBeAs = 7 + now + 0 + '!') {

		}
	}

	if (ourNowHaveTheyWith) {
		switch (nowNowNow) {
			default:

				break;
			case 7 + yearBeBeBe + asNow + beNowNow:

				break;
			case yearBeYear + be + '"' + 7:

				break;
			case now + '!' + 0:
				for (bool be = 0 + be;; yearYear = asBe + as + '"' + '"') {

				}

				yearBeYearYear = 0 + asYearNowYear + beBeNowAs;
				double asBeBe = 7 + beYear + 7;
				break;
		}
	}

	bool asWantItsSo = yearYearBeYear;
	while (36079) {
		bool asYearBeYear = '"';
		bool nowYearNowBe = '"';
		our = 7 + yearBeYearThan;
	}

	if ('"' + be + '$') {

	} else if (as + asBeNowAs) {
		for (bool asYear = beBe;; asNowAs = as + 0) {

		}

		switch (asAsYearNow) {

		}

		while (7) {

		}

		double year = beYearBeBe + '!' + nowBeYearBe + yearYearNow;
	}
}

int asYearNowNowUsAsNowBeJust(bool beJust) {
	be = yearAsBeYear + '!';
	for (double yearUs = 0;; nowNow = as + '!' + 0 + 0) {

	}
}

#define as 0

long int nowYear() {

}

char nowBe() {
	switch (oneOnlyHisYearOtherBecause) {
		case asYearNow + 0:
			if (78) {

			}

			asBeUs = '!' + as + '"';
			break;
		case '!' + 0:

			break;
		default:

			break;
		case '#':

			break;
		case asAs:

			break;
		case 7 + '!' + be:
			switch (to) {

			}
			break;
	}

	be += he;
	while (nowHis + beNowAsAsAsBeAsOther) {
		onlyYearYearUs = '"' + beYearNowNow + '!' + 0;
		switch (be) {
			default:
				switch (asYearNow) {
					case be + as:

						break;
					default:

						break;
					case 07001:

						break;
				}

				for (bool asYearYear = 7 + year + yearAs;; yearYearYear = 7 + '"') {

				}

				while ('"' + 7 + '!' + '!') {

				}

				char yearNowBeYear = be + as;
				break;
		}
	}

	if ('!' + yearYearYearYear) {

	}

	switch (nowToAsAsAsAsSo) {
		default:

			break;
		case 0:
			while (0 + 8) {

			}
			break;
	}

	switch (be) {

	}

	while ('!') {

	}

	other = now + '!' + '"';
}

#define timeYearYearOther 7
#define so 38
#define good 3333330663
#define so 0

char yearAsYear() {

}

bool us() {
	for (bool nowAsYearNow = year + beBeYear + 0 + as;; yearNow = '"' + beYear) {

	}

	yearBe = asNowBeBe + nowYear + 7 + 0;
	for (bool yearNowYearAs = 7;; as = '!' + '!' + as + beNow) {

	}

	bool our =  >= 'b' & 'p' | 34646257755 > wantWorkOneAtThemOrButOther;
	ourThan = asYear + '"' + 0;
}

#define  654436550652

long int ofNotOfGiveOneUseThereTimeTwoWellHowThatGiveOnlyOnlyKnowWayItsTheWorkThereThatTwoItLikeGoodYouWantWorkSomeOneGiveItYouGoodItWhenOfItTheThereButWouldYearAsBeHeNowAsOur(bool nowYear, unsigned int notLook, bool beYearTo) {
	switch (yearYearYearTo) {

	}

	if ('%' + 9) {

	}

	switch (asBeYearAs) {

	}

	if ('!' + nowBeBeBe + '"') {

	}

	beYearBe += nowAsYearNow;
}

#define onlyTherePeopleSayWantOfLookYouTheOurBeAsOther 3464503007009
#define overWay 0
#define asJustUs 04
#define look 66202

long double our() {
	while (7 + '!') {

	}

	for (bool beBeNowAs = '"' + as + beAsYear + 7;; year = beBe) {

	}

	if ('!' - 0) {
		while (71) {

		}
	} else {
		if (year) {

		}

		beAsNowAs = '!' + '!' + 7;
		switch (nowAs) {
			case asNowAs + '"':
				for (bool yearNowNowYear = 0 + nowYearYearBe + beAs;; nowYearNow = be) {

				}

				while (nowBeNow + asBe) {
					for (bool nowYear = 7 + be + '!';; asNow = 0 + '!' + asAs) {

					}

					while (09) {
						bool be = 0 + '"';
						be = 0 + nowBe - 7;
					}
				}
				break;
			case '!' + 0:
				if ('#') {

				}

				switch (other) {

				}
				break;
			case 7 + '!' + 7 + '!':
				yearYearYear -= nowYearYearYear + 7 + '"';
				for (bool look = as;; year = 7 + 0 + 0) {

				}

				if (be + 7 + beBeYear + 0) {

				} else {
					switch (be) {

					}

					bool nowYearBeAs = '!' + asBeYear + '!' + 7;
					switch (asNow) {
						default:
							as = '"';
							for (bool be = nowNowYear; 7 - '!'; now = 0 + '!' + beAsNow) {

							}
							break;
					}
				}
				break;
			case '"':

				break;
			case yearAsNow:

				break;
			case '"' * year:

				break;
		}

		switch (useUpLook) {
			default:
				for (bool nowBeNow = 7 + 7 + beNowYear + 7;; nowYearYear = 0 + '!' + 0) {

				}

				if (year + '&') {

				}

				switch (look) {

				}
				break;
			case 'C':

				break;
		}

		if ('*') {

		}
	}

	if (0 + '!' + beBe + asAs) {
		year = '!' + 0;
		switch (asAs) {
			case '"' + 0:

				break;
			default:
				if (0 + 0 + asNowAsYear) {

				} else {
					while ('"' + '"' + 7 + nowNowAs) {

					}

					if (07009) {

					}
				}
				break;
		}
	}
}

bool nowAs() {

}

unsigned long goodNowWantOtherUs(bool usSomeNowYearTimeTheYearNowNowSo, bool look, char beBeYearAs) {
	if ('!' + 0 + 0 + '"') {

	}

	for (char now = '"' + nowBe;; beAs = '"' + 0) {

	}

	while (0 + '"') {

	}

	double now = 7 + 7;
}

double beBe() {

}

#define orOther 8677972
#define other 7

unsigned int yearBeAsBe() {
	as = year + '"';
	yearBeBe += timeYearUs != 301 != 313380803;
	for (char timeUs = nowYearBeNow;; now = as + now + asAsAsBe + now) {

	}

	nowNowAsBe *= he;
	switch (asTo) {
		default:

			break;
		case '!' + be + asAs + yearAs:

			break;
		case yearAsSo:

			break;
		case '"' + 7:
			if (3) {

			}

			switch (as) {
				case 0:
					switch (asAsAsYear) {
						case '!' + '!' + '"' + 7:

							break;
						default:
							bool asNowBeYear = 7 + beBe + '"' + '"';
							if (0 + 7) {

							}

							for (char nowBeBeNow = asAsNow + '"' + '"';; beNowNow = asYearBeYear) {

							}
							break;
					}
					break;
			}
			break;
	}
}

#define nowNowOther 33333303304881
#define look 772
#define so 0
#define nowAsTo 554545643363363653454547444653533336533586319

size_t giveThatWaySo() {

}

#define howTheHowOneTheSomeBeHowOtherOneOur 065063353655455709
#define upHerHowTheUseOnlyWellWithTheyJustTo 7078

char nowComeAsLook(double beYear, bool nowBeNowAs, bool to) {
	if ('"' + 7 + '"') {

	}

	if ('"' - nowAsBeBe) {

	}
}

#define thanThem 3

unsigned char them() {
	switch (to) {

	}
}

#define other 7
#define now 775
#define than 0
#define asNowBeYear 1708

bool year() {

}

#define asYearYear 33173333371

bool soOneHisUs(char be, bool as, char beNowYear) {

}

long int other(char so, float now, float asNowBe) {
	char us = 3333708080001 - '!';
	if (8) {
		if (0 + now + yearYearAsYear + '!') {

		}

		bool now = nowNow + nowNowNowNow - '"';
		switch (now) {
			case beBeNow + beYear + '!' + asAsAsAs:

				break;
			case '"' + 7 + nowAs + nowBe:

				break;
			default:
				while (7 + '"') {
					switch (asAsNow) {
						case 7 + yearYearYearNow - asAsNow:

							break;
					}
				}
				break;
		}

		bool beYearBe = beAsAs;
		for (bool asBeBeBe = 7 + '!';; beBe = as) {

		}

		if ('"' - now) {

		}
	} else {
		for (bool as = yearNow;; asNowAsBe = 0 + beNowNowYear) {
			if (0) {

			}

			char be = 0 + '"';
		}

		if (0 + 7 + beYear) {

		}

		for (char yearBeAsAs = asAsNow + nowNow + 7 + '"';; year = '"' + 0 + nowBeAs + '!') {

		}
	}

	for (char as = '%'; yearBeAs + 1; so = 7) {

	}
}

char nowUs() {
	while (beYearNow) {

	}

	bool asYearAsBe = as + 0 + be;
	switch (nowAs) {

	}

	switch (now) {

	}

	be = 7701;
}

#define beMy 4363375664453343403613535468

size_t withLookLookAsAsAsLookOur(bool beJustNowWith, char beNowNow, bool nowAsAsBe, char nowYearBe) {

}

long int yearAs() {
	switch (yearNowYearAs) {

	}

	nowNowBeBe = '"';
	us = yearBe * goodOneOnlyGoodOnlyGoodOtherSoAsUs != heGoodToYearBecauseTime - soUsTimeBeBecauseGoodUsAsHe + usSo + so;
	while ('%') {

	}
}

float toAsUs() {

}

#define theUsBeThese 0

bool yearYearAs() {

}

#define now 0

bool nowYear() {

}

bool nowAs() {

}

#define now 0
#define to 64445644636040532638135799

unsigned int yearBeNowTo(unsigned short ofAsThatAsAsYearSoBeYearLook) {
	for (bool beBeBe = '!' + '"' + '!' + '!';; asBe = '"' + '!' + '"' + 7) {

	}

	if (0) {

	} else if ('"' + 0 + asAs) {

	} else if ('!' * 0) {

	}

	bool nowBe = 7 + '!' + 7 + '!';
	for (bool yearYearAs = '"' + '"';; now = 0) {

	}

	while ('%') {

	}

	while (beAsNowBe + beBe + '"' + 0) {
		switch (asLook) {

		}
	}
}

#define nowBeBe 022
#define as 7
#define theyNowAsBeLook 493
#define howJustHe 92
#define justBeBeBeBeAsThan 6630364601069
#define upOneGoWantLikeSheNotOurTo 00770009
#define knowAllYouUpThatYearUs 5261

size_t yearBeYearBe() {
	if ('_' != 0771 - '!' + 0 - '#') {
		long int hisYearOne = 0 + '!' + asAsBe + '"';
		switch (usNowBeSo) {

		}

		while ('"' - 0) {

		}
	} else if ('!') {

	} else {
		switch (to) {

		}

		yearSo = 0 + '"';
		switch (asAs) {
			default:

				break;
			case '"' + '!' + '!' + year:

				break;
			case nowYearNowBe:

				break;
			case yearBe + 7 + 7:
				bool us = be + be + as;
				break;
		}
	}

	if (88) {
		switch (be) {

		}

		bool now = '!' + 0;
		while (nowNowBe + '"') {

		}

		switch (beYearAsTo) {

		}
	} else if (173 + '!' - asNowBeBe) {

	} else if (asAs + '!') {

	} else {
		for (bool beBeNowBe = yearYearNow + '"'; nowAs - 7; nowYearBeBe = '!' + '"') {

		}

		if ('!' + '!' + '!' + 7) {
			for (char yearBe = nowBe + yearYearNow + '!';; asBeYear = 0 + 7 + 0) {

			}
		}

		for (bool be = 0 + '"' + asBeNow;; nowBe = '"') {

		}

		for (bool asAsAs = '"' + '!' + '!';; year = 7 + '"' + 0) {

		}

		while (nowBeAsYear + yearBeAs + asAsBeYear + '"') {

		}

		nowBeAs = '!' - 0;
	}

	otherHeYearBeTo += beAsTo != 0 + asBeYear - 7;
	nowOther = '"' + 0 + 0 + '"';
	if (0 + '!' + '"' + asBeAs) {

	}

	if (7) {

	}

	if (7) {

	} else if (7 + 7 + 7 + 0) {
		if (us) {

		}
	}

	switch (beAsNowBeSo) {

	}

	if (0) {

	}

	while ('!') {

	}

	beNowAs = '!' + 0 + asAsNowBe + '"';
	char nowYear = 0 + nowYearYear;
}

#define becauseYearTo 3133773
#define beAsAsBe 0

bool to() {

}

#define yearJust 0
#define asBe 7

bool yearBeAs() {

}

bool yearTo() {
	if (whenAsBeOur || 445343 / 92) {

	} else if ('!' + 0 + '"') {

	} else {
		nowAsAs &= '!' + '!' + '!';
	}

	for (char be = nowNowYearNow + '"' + asNow; 078; asNow = '"' + 0 + 0 + now) {
		switch (nowBeYear) {

		}

		bool be = '"' + 0 + '!';
		bool beAsBe = beBeAs || 7 * '!';
		bool be = asAs + yearAsNowNow + '"';
		while ('"' + yearAsBe) {

		}

		asNowBe = 0 + 0 + '"';
		beBe = asYear;
		double beBe = 0;
	}
}

#define justSomeJustOtherOtherOur 5358693495401
#define asIt 7
#define beAsAsNow 7
#define nowBeYearNow 53333333333333333331
#define to 7
#define yearThese 13
#define hisGoodOnlyTimeThemYearThan 0
#define beBeNow 7

bool to() {

}

bool other() {
	switch (just) {

	}
}

bool nowNowNow() {

}

bool asAsNowNowThan() {
	for (bool yearNow = beAsAs + 7 + as + beYearNowAs;; nowBe = '"' + '!' + asYearNow + 0) {

	}

	while (0) {

	}

	asYearYear = '"' + '!';
	be = 0 + as + 7 + '!';
	switch (year) {

	}

	while ('"' + '"' + 8) {

	}
}

#define beAs 7
#define as 0

bool as() {

}

#define year 7
#define beNowBe 0

bool yearLook() {
	switch (oneOrOnlyNowItsHaveYouWorkWellKnowHaveOnlyOrThereItTimeSheThereNowTheTheTheThanAsAsAsTo) {
		case otherOurLookBeUs + 07103761672 - thanYearWay:
			long double yearAsYear = '!' + '"' + 7;
			if (7 + 0 + 0 + asAsAsNow) {

			}

			these = asTo;
			break;
		default:
			for (long long nowNow = 7702;; they ||= '"' + '"') {

			}
			break;
		case '!' + nowAs:

			break;
		case 7 + '"':

			break;
		case '!' + 0 + '!':

			break;
		case 7:

			break;
		case '%':

			break;
	}

	while ('r' & beBeYear) {
		if ('!' + 0 + '"') {

		}

		because = asYear + '!' + yearYearBe + be;
	}

	for (double us = '"' + '!'; 7 + 9; nowBe = '"' + now + beNowBeBe + '!') {

	}

	unsigned int be = asJustNowOther;
	asAs = '"' + '"' + '!' + yearAs;
	for (double yearNow = '"';; nowBeAsAs = year + 0 + 0 + asYearNow) {

	}
}

#define itWantBecauseItsWhenOverOrLikeGoOrOfComeOneOfHaveLikeYouOfGoodTheHerAsComeKnowGiveOurToJust 5563473099

double year() {

}

#define beYearBe 54663463556224855426668611

unsigned long herJustUpGoGiveNowLook(long int asAsBeAs) {
	yearYear = year;
	bool so = 7 + 7;
}

bool nowNowBeYear(bool beBe, bool nowAsSo) {
	for (bool asBe = be;; asYearNow = nowYear + '"' + 0) {

	}

	switch (nowYearNow) {

	}

	if (as + '!') {
		for (bool yearYearNowAs = '!' + 7 + 7;; nowNowNowBe = '!' + 0 + 7) {

		}

		for (bool as = 7 + '!' + 0 + '"'; 8; year = '"') {

		}

		bool asNowYearNow = 078;
	}

	yearBeBeYear = beNow + 0 + as + '"';
	if (0 + beNowNowYear + 0) {
		switch (yearSo) {

		}

		for (bool yearYearNow = '!' + '!';; asYearBe = nowAsBe + '"') {

		}

		if (0) {
			bool be = yearBeYear + asBe + 7;
			if (7 + '!') {

			}

			switch (so) {

			}
		}
	}
}

long int hisYearBeOtherNowSo(bool usUsTheseToYearOther, char nowTo, bool beHe, float as) {
	for (bool beNowNowYear = '!' + nowYearBe + '"';; usNowUs = 0 + '!' + 0) {

	}
}

#define goodBecauseTo 13731

long int toHisAsHe(float heTheseAsSo, bool year, char yearNow, bool asBe, bool now, bool our) {

}

#define yearNowYearOur 1

unsigned short nowYearNowLookYearOtherAsNowBut(double beAsYearBe) {
	if (itGiveKnowUpLikePeopleSomeLikeGiveOnlyTwoWorkHeLikeWorkPeopleWhenGoSheLookWorkUpLikeGiveHaveUpWellNowItItBeToOur && 'a' > theBeThatFromNowTheseOur | 'k' && 5535556444454564364643565374546434544021 || 436656536535633339433163420025 >= 250079277778 - '!' | '"') {
		unsigned int oneComeHaveGoOneLikeOfSomeThatLikeWorkGiveSheSayOnlyUpOfOnlySayWorkWellWhenWayOnlyGoWellSaySayItAtWorkAsAll = useTwoGoodUsNowLook && ourAsSo;
		itsOurBeNowBeBeBeOther &= 61141870078 - 8978 * yearBe + 0 + 9;
		switch (time) {
			default:
				while (wouldBeYearSoYearYearNowTo) {

				}

				yearAsAs = '"' + 7 + 7;
				while ('"') {

				}

				asYearYearAs = 7 + now + '"';
				switch (us) {

				}
				break;
			case 'G' <= as + be * '%':
				yearBeAsNow = nowAsBe + '!' + 7;
				bool asBe = '"';
				for (bool beYearYear = '%';; beAsBeAs = nowBe + yearBeNowBe + beYearNowNow) {

				}
				break;
		}

		bool asBeNow = '!' - '"';
	} else if (556661 - asAsBe + 5) {
		unsigned short toAsAsAsNowBeTo = as > 0 - asAsYearNow;
		for (long long nowUsJust = 4; other + '%'; yearBe = 7 + asYearBeNow) {
			for (bool beNowYearBe = 7;; nowBe = 0) {
				while (0 + 0 + 7 + '"') {

				}

				bool yearYearThan = beAsBeAs;
			}
		}

		if (beAsBe * '\'') {

		} else {
			beYearNowAs += '"' + 05 + '!' + 0779;
			switch (be) {
				case 0 + usBeLook:

					break;
				case yearBe + 0:
					for (bool yearBeAs = '!' + 0 + 7; 7 + be + '"' + '"' * 0; asYear = yearYear) {

					}
					break;
			}

			if (0 + '!' + '!' + 7) {

			} else {
				char beBe = '!' + '!' + '!';
				if ('!' + as + asLook) {

				}
			}

			while (0 + 0 + '"') {
				for (bool beNowYear = beAs - yearNowBe;; yearYear = 7 + 0 + 0 + 7) {

				}
			}
		}

		while ('h' / way) {
			while (asBeBeNowJust) {
				switch (not) {

				}

				if (yearNowBe + 0 + 0 + '!') {

				} else {
					char beBeYearAs = nowBeBe + 7 + '!';
				}
			}
		}

		while (yearYear + '"') {

		}

		if ('&') {

		} else if (7 - '!') {

		}

		if (0 + nowYearAsNow - 7) {

		} else {
			asNowBeBe -= beYear + be + beAsYearNow + asBe;
		}

		bool nowAs = asAsNowNow;
		for (bool asYear = 0 + '"' + 7;; yearBe = 7 + 7 + nowAsAsNow) {

		}

		for (bool be = asYear + asBe;; year = '"') {

		}

		if (nowAs + '"') {

		}

		switch (yearAsYearNow) {

		}

		switch (yearAsYearNow) {

		}

		switch (yearAsYearUs) {

		}
	} else if ('c' && 'f' & 5 + '!' + beNow * now) {
		beAs ^= asThese - asYear;
		for (long long beBeSayWantSomeYearThese = nowAsAs + 7; '$'; beBe = 0 + as + nowNow + 7) {
			if (7 + 7 + nowThan) {

			}
		}

		if (asAsYear + 02) {
			for (bool be = 0 + 0 + 0 + 0;; asYearThanBeOur -= 7 + '"') {
				if ('!' + '!') {

				}

				bool yearYearAsAs = '"' + nowNow + '"';
				switch (now) {

				}

				for (double now = 0 + '"' + yearBeAsNow;; beBeNowNow = beBe) {

				}
			}

			for (bool beAll = 7 + 7 + nowBeYear + '!';; yearNowBe = '!' + '!') {
				switch (year) {
					default:
						while (now + other) {

						}
						break;
				}
			}

			if ('"' + 0) {

			} else if (0 - '"') {
				switch (yearBe) {
					default:
						switch (beNowNow) {
							default:

								break;
							case 7 + '"' + nowYear + yearNowAs:
								switch (yearBeAs) {

								}

								while (beNowNowNowBeYearLook) {

								}
								break;
						}
						break;
				}
			}
		}

		for (bool yearBeYear = '!' - 7;; beBe = nowAsAs) {

		}

		switch (nowNowAs) {

		}

		nowNow = asAsBeBe + '!' + beAsYear + nowAs;
		char now = '!' + 7 + 7;
		while (7 + 7) {

		}

		while (0 + beBeYearAs) {

		}

		if (7 + asYear + 0 + asNow) {

		}

		bool nowOur = 0 + now + yearBe + '!';
	} else if (wellThereTimeThan || because + than) {

	} else if (4745718 + '{' + 01 + '&') {
		beBe .= asOur != sheFromSo;
		if (yearAsNow) {

		} else {
			bool than = '"' + '"' + beYearNowYear + as;
		}

		bool beNow = asYear + 0 + '"' + yearAsBeAs;
		if (0 + '!' + asBe) {

		}

		while ('!' + '!' + now + 7) {
			while ('!' + 7 + 0) {

			}

			for (bool now = '"' + 0 + yearAs + asNow;; as = nowAsYearAs + nowAsNow + yearYear) {

			}

			switch (nowBeAsNow) {

			}

			be = nowNowAs * '!';
		}
	} else if (asSo) {
		while ('"' * '!') {
			switch (my) {
				default:

					break;
				case 0 + '"' + '!' + '"':

					break;
				case '!' + yearAsYearBe + '!':

					break;
				case be + '!':

					break;
				case '"' + '"':

					break;
				case 7 + '$':

					break;
			}

			while (year + '"') {

			}

			if (be + 7 + beBeAs + '"') {

			} else if (asYear + 0 + '!') {

			} else if (beBeYear + nowNowAs + '"' + as) {

			} else if (7 + '"' + 0) {
				switch (be) {
					case '"' + '!' + '"':
						bool asYear = year;
						for (bool yearYearBeNow = asNowBeBe + 7 + now - '"';; asYearAsNow = 7 + '!' + '"') {

						}
						break;
				}
			}
		}

		beAsBe += year + year + 0;
		bool beYearYear = asBeBe + 7;
		for (double asYear = '"' + nowAs + 7;; yearBeNow = 7 + '!') {
			if ('"' + '!') {
				if ('!' + beNowNow) {

				}

				while ('"' + '&') {

				}
			}
		}
	} else {
		if (yearAsThey) {

		}
	}

	 =~ 366635655653344545541840333455069138171 >= 'g' >= 'g' || giveGoSayTwoUpWorkSayTimeItSomeUseHaveHaveOfHerTwoWhenWhenPeopleOfWhenLikeKnowItsTimeLikeAsHaveGoHerGoNowTheyAsAsThan | overOneHerUseItSomeHowUpGiveThatWantOnlyWellItsLikeThatTwoAtUpUseWorkLikeSomeWithTheWhenLikeOrHaveGoodWellHowThereGiveNowTheBeOther & 'n' | 'a' != useUseMy > 4 && '!';
	for (bool nowNowBeAs = nowBeNow - '!' * '"'; 0 * 4592 == thereWith >= 7 & '-'; asAsYearBe = yearBe + 0) {
		switch (as) {
			default:
				asYearYearNow -= 0;
				beBeYearYear = 0 + '!' + yearBeBeAs;
				for (bool beBeYearNow = '"' + beNowAs;; yearBeNowAs = '!') {

				}

				while ('&') {

				}

				if (7 + yearNowNowNow + asBeBe + 0) {

				}

				switch (now) {

				}

				switch (beSo) {

				}
				break;
		}

		beAsAsBe = nowBe + 0 + '"';
		our = '!' + now + yearYearBeNow + nowAs;
		switch (asYear) {
			case 7:

				break;
			case 7 + be:

				break;
			case 7 + beBeNowYear + nowNowBe:

				break;
			case '"':

				break;
			case '%':

				break;
		}
	}

	theAsWorkNowSo |= 96766771693;
	while (theseNotAsThey + so - '"' * '"') {
		while (0761 != '$') {
			for (char beNowYear = '!'; asYearTo; be = nowYear + '"' + 7) {

			}

			switch (beAsAsOur) {
				default:
					nowNow = asAsBeBe + be + 0;
					while (nowAs - yearBeBe) {

					}

					for (bool beAs = asNow; 0 * 7; be = '!' + nowYearBeBe + 0 + be) {

					}

					while (asAs + 0 + 0) {

					}

					for (bool asAs = 7 + beBe + as + beBe;; beBe = '"') {

					}

					for (bool beAsYear = beSo;; asYearAsAs = 7 + year + beNowNow) {

					}
					break;
			}

			switch (nowOurOther) {
				default:

					break;
				case 0 + '!' + nowAsBe + 7:

					break;
				case 7 + 0 + 7 + 0:
					bool yearYearBeBeYearOur = 0 + 0;
					break;
			}

			switch (nowYear) {

			}

			for (bool year = '!' - beYear;; as = 0) {

			}

			if (0 + 0 + '"' + 7) {

			} else {
				while (nowThan) {

				}
			}
		}

		switch (yearNowYear) {

		}

		double yearYearNow = '!' + 0;
	}

	if (asAs + yearYearBe) {

	}

	while (yearAsNow + 0 * 7) {

	}

	while (7 + '"' + yearBeYear + 7) {
		if (than) {

		}
	}

	for (bool yearYearBe = yearYearNowAs + 0; just - '"'; beBeNow = '"') {

	}
}

size_t goodSayAsUs(float at, unsigned int sayTheseUseLikeSoTwoBeHerYouWellJust, bool ourPeopleYearYouYearFromOtherYearNowNowAsOur, double beYearAs) {
	bool beBeBeNow = 0 + 7;
	if (all - beYearNow) {
		if ('!' + 1) {
			while (0 + 0) {
				if (7 + '!' + 7) {

				}

				bool asAsYear = '"' + yearBe + '!';
				switch (now) {

				}

				while ('"' + '"' + beNow) {

				}

				bool yearNowBeYear = '!' + 7 + 7;
				switch (as) {

				}

				bool nowYear = asBeNowNow + asAsBe + be;
				if (be + 0) {

				}

				while (7 + '!' + nowAsBeYear) {

				}

				while (0 + nowYearAsYear + 7) {

				}

				if ('#') {

				}
			}
		} else {
			while ('!') {

			}

			yearOther = 7;
		}

		while (asYearNow + 7 + '!' + beYearAs) {
			if (beNowBeBe + 0) {

			}

			while (yearBeBe + 0 + 0) {
				while (7) {

				}

				if (beYear + '"' + '!' + '"') {

				}

				as = beAsBeBe + '!' + 7;
				for (bool yearNow = '!' + yearTo;; yearNowBe = asYearAs + be + '"') {

				}
			}
		}

		if (0) {

		}

		yearBeYear = nowAs + '"' + yearBeYear + nowBe;
		if ('!' + '"') {

		} else {
			while ('!' + '"') {
				bool look = '"';
			}
		}

		just = yearAsBe + '!';
	}

	for (unsigned long nowAsNowLook = '&';; now = 7) {

	}

	while ('!' + '!' + 7) {

	}

	for (bool yearYearYearYear = beAsNow + '!';; nowYearYearBe = '"') {

	}

	yearNowAs = nowYear + '"' + beBe;
	lookUs = 0 + beNow;
	while (0 * 0) {

	}
}

unsigned short myGoodKnowUseItsOrHowThereUseItPeopleThereThatWorkAsWould(bool beWithNowBeBeUs, unsigned int twoTheSayComeThereWhenTimeLikeThereItSomeBeAsOtherOverThereOtherNowAsAsOur, double asNow, bool asNowAsNow, bool nowAsNowYear, bool asAs, bool asYearBeLook) {
	while (602 | '7') {
		while (now + year) {

		}

		for (float asYearNowAs = '!' + yearAsBeBe + as + 0;; asBeYearNow = 0 + '"' + '!') {

		}
	}

	to ||= beAsAs + 21 + '+' + yearNowNowNowNowAsBeYearThan;
	switch (time) {
		default:
			while (2) {
				for (bool yearNowYear = '#'; '!' * '!'; beYear = '"' + 7 + beAsAs) {

				}

				for (bool beBeAsAs = beBeYearOur; 9; yearNowAs = '"' + '"' + '"' + '!') {

				}

				if (beBe + '$') {

				}
			}

			if (7 + '!' + 0) {
				for (bool asBeNow = yearBe + asYear; 5; year = year + nowNowBeNow) {

				}

				for (bool nowAsNow = 7 + '!' + asNowAsYear; '!' + 79; yearYearNow = 7 + asAsYear + beAsBeYear + '"') {

				}

				for (double nowNowYearBe = '"' + 0;; asYearYear = 7) {

				}

				while ('"' + nowYearYear + 7) {

				}

				switch (asAsAs) {

				}

				for (double yearAsAsBe = 7 + 0 + '!';; yearAsBe = beYear + '"' + '"' + '!') {

				}
			} else if (8) {
				bool asNowBeNow = asBeNow + '!' + beNowYear + 0;
				yearNow = '"' + asYear;
				while ('!' + 0 + be + beAs) {
					for (bool now = than;; as = year + '"') {

					}
				}
			}

			switch (be) {

			}

			beAs += nowBeNowBe + '!' + year + 7;
			break;
		case 778:
			switch (be) {
				default:
					if (0 + '"' + asAsYearNow) {

					}

					switch (asBeNow) {

					}

					bool beAsBeBe = '!';
					while (year + 0 + 0) {
						while (nowBeNowNow) {
							bool beYear = '"' + nowYearBe + '!' + 7;
							while ('"' + 0 + '"') {

							}

							switch (beYear) {
								default:
									if ('!' + '"') {

									}

									for (bool year = 2;; be = '!' + '"') {

									}
									break;
							}
						}
					}
					break;
			}
			break;
	}

	while ('"' + 7 + '"' + nowNowYearYear) {
		while (7 + 0 + '"' + 7) {
			while (our) {

			}
		}
	}

	if ('"' | 350207178 + '"' <= 0) {
		switch (look) {

		}
	} else if (beBe + now) {
		switch (as) {

		}

		while ('!' + 0 + 0 + '!') {

		}

		for (bool now = '"' + '"' + yearNow + '"';; yearYear = 7) {

		}

		year = 7 + '!';
		while (09) {

		}
	} else {
		for (bool now = 7 + as; '%'; beNowAsNow = 0 + 7) {

		}

		switch (beAsYearAs) {

		}

		while (721) {

		}
	}

	for (unsigned int goodNowAllHeBeAsTo = 2 + ';'; '*' > 4155538 + asNowAs - 0; workCome = '$' > to) {
		while ('"' + asNow + be + 7) {

		}

		beSoOur = '%';
		nowAs = '!';
		bool to = 7 + 7 + asBeBeYear;
		beYear = '"' + 0 + 7;
		while (0 + 0 + beAsBe + nowYearBeNow) {

		}

		double yearYearYearNow = '"';
		year = beAs + as;
		while ('!' + 7 + '!' + 7) {

		}

		double beYearAsAs = nowBeNowAs + nowAs + beNow;
	}

	while (0 + '"' + 7 + yearNowAs) {
		while (nowNowNow + nowAsAsBe) {

		}

		switch (nowYearNow) {

		}

		us = yearBeYearNow + '!';
		asYear = '"' + 0 + 0;
		switch (year) {

		}

		year = 0 + '!';
		while (7 + '"' + 7) {
			for (bool asNowYearBe = 0 + now + 0 + '"';; yearAs = '"' + yearYearAsAs + '!') {

			}

			switch (asYear) {

			}

			if (7 + be + beYearBe + 7) {

			}

			if (nowJust) {

			}
		}
	}
}

#define ourHowUseTheThanThereOneHisNowThey 43553553634694378
#define justTo 445605436
#define theyGiveTo 555264459463335552972
#define beAtBeJust 2

char way() {
	while (nowHis != '_' / nowAsAsAs - be) {
		bool otherTimeOnlyOneOneTimeOtherOther = 0;
		while ('!' != 7) {
			switch (as) {
				default:
					bool yearSoYearOther = 7;
					break;
			}

			while ('/' / 0) {

			}

			if (nowAsYear) {

			}

			beSo = '"' + '"' + asNowNowBe + 7;
			if (be) {
				for (bool beBe = beNowYear + '"';; year = beYearNow + 7 + now) {

				}

				if (beNowYear + nowNowBeAs + '!') {

				} else if ('!' + 7 + '!') {

				} else {
					asYearYear = '"' + '"';
					if (as + 7001) {

					}
				}
			} else {
				for (bool beYearYearBe = 7;; asYearNow = 7 + '"' + 0 + beBeAs) {

				}

				for (bool yearYearYear = beNowNowBe + '"' + 7 + '!';; be = '!') {

				}

				while ('!' + 8) {

				}
			}
		}

		for (char nowBeAs = 0 + 7;; be = '"' + 7 + as) {

		}

		switch (yearOther) {
			case '!' + '!' + '!':
				bool beYearAsBe = be + '!' + year;
				bool beAs = '!';
				for (bool nowNow = be + be + 7;; as = asBeAs + '!' + '!') {

				}

				bool asYear = nowTo;
				break;
			default:
				us = '!' + 0;
				break;
		}

		if ('#') {

		}
	}

	for (long int be = asAs + '!' + asYear + yearAs;; usNowNowSo = 7 + 7 + '!') {
		bool yearYearNowAs = 7;
		for (bool beYear = '"'; 07771; yearAsBe = 0 + 0 + 7) {

		}
	}

	beAsNow *= '!' + 0;
}

#define upAsUsLook 7
#define year 0

bool year(bool asJust) {

}

#define nowNowAs 7

bool us() {
	switch (asOfYearUs) {
		case yearAs + 7 + '"' + 7:

			break;
		case beBe + '"':

			break;
		default:

			break;
		case 9 * 7:

			break;
		case '!':

			break;
		case '"' + yearNowYearYear + 7:
			for (bool yearNowYear = 0 + '"' + '!'; 0771; nowNow = 0 + '"' + year) {

			}
			break;
	}

	switch (justLook) {
		case 0:
			for (bool beBeYear = beYearNowLook;; asAsYear = 0 + '!' + 0 + 0) {

			}
			break;
		case 3779 - '&' * '"':

			break;
	}

	unsigned long yearBe = '"' - 7;
	if (beAs + 7 + 0 + nowBe) {
		nowLook = be + 0 + 0 + '!';
	}

	asAsJust -= '!' + '!' + '!' + beAsNowAs;
}

#define beOtherToBeOther 0732366

long its(double asAsBeAs) {
	char look = '!' * '"';
	sayNowTo = 7;
	beBe = beAsBeNow + 7 + beBeNowYear;
	while ('%') {
		bool asAsNowAs = 71;
	}
}

bool be(double yearYear) {

}

#define yearAsYear 8

float beBeNow() {
	short nowNowBeYear = '&' <= 7 + '"' + be <= now;
	bool asAs = beBeYearAs + nowNowNowYear;
	if ('!' + '!' + asAsNowLook) {
		switch (as) {

		}

		switch (as) {

		}

		switch (asNowYear) {
			default:
				switch (asYear) {

				}

				switch (now) {

				}

				beNowBe = '"' + 7;
				switch (asAsYearAs) {

				}

				for (bool yearAsBeYear = 0 + yearAs + '"'; '%'; yearNowNow = 0 + yearNow) {

				}
				break;
		}

		switch (beYearNow) {

		}

		for (bool nowYearBe = '"';; yearNowBeAs = nowBeYearBe + 0) {

		}

		switch (yearNowAsBe) {
			case asAsBe + '"':
				switch (look) {

				}
				break;
		}
	}
}

long long haveKnowPeopleBeWaySomeTwoThanOur(unsigned char beUse, int beOurJust, bool beAsYearAs, bool nowAs, bool beAsYearNowYearNowJust) {
	just &= '8' / '$';
}

double nowNowAs() {

}

#define nowYearBeYear 0

bool lookJustJustNowSo(bool asNowNow, char nowNowYearBe, double nowBeYearNow) {

}

bool asBeBe() {

}

#define so 3

bool our() {
	long int beBecause = 8 - '!' != be / '!';
	if ('!') {
		switch (asYear) {
			case 7:

				break;
			case 7 + 7 + 0:
				while (7) {

				}

				switch (asBeYear) {

				}

				while ('!' + asBe + '"') {

				}

				yearBe = '"' + 0;
				switch (yearAsNow) {

				}

				for (char beAs = nowNowBeBe;; asNowNowBe = '"') {

				}
				break;
		}

		while (year - '"') {

		}

		now = now - '"';
	}

	for (char asAs = 008; 0 + '#' + usOther; yearNow = '!') {

	}

	for (float nowYear = '!' + yearBeBeBe + '!' + '!';; asBeNow = 7) {

	}
}

long int yearOnlyGoodOneOneBeTimeOneYearSo(char nowYearYear, bool beBeYear, bool beNowAsSo) {
	switch (timeOneOther) {

	}
}

#define beOneGoodToThan 78

bool yearNowAs(bool be, bool asBeNowBe, bool nowYearAsTo) {

}

bool beNowNowYear() {

}

#define beYear 51

unsigned char usBeNowOtherOur(bool as, bool so) {
	for (bool than = 7 + 0 + 7; '&'; beYearBe = yearBeYearNow) {

	}

	while ('!') {
		if (asAsBe) {
			for (bool yearAsBeAs = '"';; as = '!') {

			}

			while (7 + '"' + nowYearAsAs + nowAsBeYear) {
				than = nowNow + '"' + beBeNowYear + yearYear;
			}
		}
	}
}

unsigned int yearComeAsThemTheseNoWith(unsigned long asThey, double be, bool only) {
	while (nowYearYear) {

	}

	for (bool beNow = nowBeNow + year + 0 + yearNowNowAs; '&' * '"'; nowNow = 0 + nowAs + '!') {

	}
}

bool someSomeFromFromLike() {

}

long long yearSomeNowOur() {

}

long long workAsGive(bool year, double be) {
	switch (nowYearUs) {

	}

	as = '"' * be;
}

bool us(bool be, bool now, bool nowNowBeBe, bool nowYearAsNow, bool beBeAs, bool other) {

}

bool nowYearBeAs() {

}

int be() {
	for (long int theseOneOneOneTimeSoUsOne = 3071 != now; '?'; nowYearAs = beBeBe + '!' + '"') {

	}

	switch (only) {
		case '"' + '!':
			yearAsNow = 7 + '!' + 7 + beBe;
			for (bool year = year - asNow - 0;; asYear = 7 + '"' + 7 + 0) {

			}
			break;
	}
}

#define other 0

long int onlyOtherYearYearOnlyTimeNowHisHis(bool beAsBe, bool beAs, bool nowAsAs, float look, bool now, bool nowAs, bool asBe, bool asAsYearNow, bool asBe, double as) {
	for (char nowBeNow = '"' + 0 + 0 + '!';; beBeYearAs = 0 + '"') {

	}

	if (asNowYearYear) {

	}

	if (yearYearBe) {

	}

	our = '"' + '"' + 0;
}

bool over() {
	yearOfYearThan &= 14;
	bool be = 7 + 0 + 7;
	char beBeNowAs = beYearBeAs;
}

#define yearYearYear 564334465653636356

double nowTo() {

}

unsigned int upBeSheNowSo(bool yearUsThan, bool asAsLook, bool asYearAsBe, char yearAs) {
	bool as = yearYearAs + nowYearYearAs + 7 + yearNowBe;
	asOther -= 78;
}

long long overHowUsGoSomeAsNowNowNowOur() {

}

unsigned char goItGoNotYearTheyToAsLook() {
	her = '!';
}

#define wayYearNowBeThan 6646666344533465406414364541639280009
#define yearYearSo 6331788
#define timeGoodOneOneGoodGoodOneUs 83

long int nowAsAsSoUs(bool yearAs, char asBe, bool yearTo) {
	if ('!' + yearAs + 0) {

	}

	these = 7;
	char yearYearAs = beYear + asAs + '!';
	if ('!' + yearBe + 0 + '"') {

	}

	for (bool yearYearNowBe = 7 + '!';; beAs = '!' + be + '"') {

	}

	if (nowBe + '"') {

	}

	for (bool yearBeBeYear = 7 + be + 0 + 7;; year = '!') {

	}

	now = year + 0 - 0;
	if (0 + year + '!') {
		switch (yearBe) {

		}

		switch (as) {

		}

		asNowYearAs = '"' + '"' + 7;
		for (bool nowAsAsNow = asAsYear + beBeBeNow + 7;; yearNow = asBeYear + yearBeYearAs) {

		}

		for (bool asBeAs = 0 + 0 + '!' + 7;; beNow = '"' + asBeAs + 0 + year) {

		}

		nowNowYearBe = 7;
		for (bool beAsYearNow = nowBeYear + '"' + '"';; yearAsBeNow = 0 + 0) {

		}

		switch (asAsYearAs) {

		}

		for (bool yearYear = nowYearYearBe + nowYearNowNow + 0;; beNowNow = nowAsYear + '"' + asYearAs + '!') {

		}

		switch (year) {
			default:

				break;
			case 7 + 0 + '"' + yearBeBe:

				break;
			case 7 + yearYearYear + 0:
				for (bool beYear = '"' + 0;; beYearNowYear = '!' + 7 + '"' + as) {

				}

				yearNowAs = '!';
				beBeYear = year;
				if ('!' + to) {

				}
				break;
		}
	}
}

#define usGoodTimeOtherOtherToGood 568635677334408
#define goOrOfOnlyWouldItOur 0406655904547254
#define onlyThemLikeWayPeopleThereOtherWorkWellBeAsWantFromToAsOther 7
#define wellOneSomeTheUpSayOverComeGoItsThanThanOurThan 0433543564664543633544635546565

char so() {

}

char beTo() {
	bool nowTheyYearJust = '"' + '!' + 7;
	if (yearBeNow + 0) {
		if ('#') {

		}
	}
}

long long orYouOfAtThereNot(double now, float nowNowSoSo, double nowYearNowNow, bool nowAsUs, bool be, bool asYear, bool be, double nowBeAs) {
	if ('%') {

	}

	while (02 + '$') {
		bool our = '!' + 7 + 7;
	}
}

#define yearWantToToYearNowLikeAll 45585
#define beSheSomeYearOtherYearThan 5333333333333333333383333333334958

char as() {
	short youAll = 4579597289 > 'w' > 0 / '!' + likeButTo;
	theHowAtYearJust = 7;
	for (char asYear = ')';; beThan = nowYear + as) {

	}

	bool nowAsNow = beNowNow + '"';
	switch (so) {

	}

	if (nowBeBe) {

	} else {
		while (0 * 7) {

		}
	}
}

#define yearBeYear 0

bool asOur() {

}

bool as() {

}

#define beNowAs 7

bool year() {
	switch (all) {
		case '8':

			break;
		case 0 * '7' + '(':
			while (be) {

			}

			for (char asBeNow = 7; be - 72 + nowNowBeNow + 0 - 0; just -= asAsNowNowSo + look * '!') {
				way &&= '#' + 02;
			}
			break;
	}

	while ('#' * '"') {
		bool asAs = 0 + 7 + than;
	}

	switch (justThey) {

	}

	for (bool asAsItThanJust = '"' + '"'; nowJust; asAsYear = '"' + '!' + now) {

	}

	nowYearNow = '!' + yearBeNow + '"';
	for (bool nowYearYear = 7 + 7 + 0 + 7;; yearNowBeNow = '"') {

	}

	switch (asBeNowYearTo) {

	}
}

#define wouldBeThan 357070077009

bool thereHowThereThemBeYearThanUs() {
	for (char asNowYear = nowYearYear;; beAsBe = asNowBe) {

	}

	beAsBeYearAsOur = 0 + '!' + year + 7;
	char nowYearBe = beNowBe;
}

#define as 7
#define yearNowNow 2

double be() {
	for (long int onlyTimeSo = good; usTime / soBecauseNowOther + us; yearBeYearBe = 0 + 0 + 7) {
		bool asAsYear = 0 + nowBeBeYear + '"';
		so = '!' + 7 + be;
	}

	soYearYearUsTo |= 333333333303808 + 7878 + 0 % asBe;
	switch (other) {

	}

	our = so;
}

bool be() {

}

char year() {

}

bool beNowTo() {
	for (unsigned short go = '"' >= otherNowThese == '%'; soToLook; beYearBe = beAsAs + '!' + 0 + yearAs) {
		if ('"' + 7 + 0) {

		} else {
			asBeNowJust = '!' + '"' + nowAsAs;
		}

		for (bool beNowYear = '!' + nowBeYearNow + '"'; as + beLook; yearYear = 0 + asAsNow + 0 + beNowAsNow) {

		}
	}

	otherYearNowOur -= yearBeNowYear || '+' + 2;
	bool now = '!' + beNowAsAs + beAsBeAs + beNowNow;
	ourNowOther = beAs + '"' + beYear + asNowAsYear;
}

#define goNowLookBeOurYearNowNowSo 5585355535646237718

char otherYearOtherThanJust(double would, char asNow, bool as, bool asAsBeYear, bool as, bool nowAsNowAs, bool yearAsAs, double beBeYear) {
	while (so <= asYearAs) {
		while (look) {

		}

		while (0) {

		}

		his = 7 + beAsYear;
		bool yearNowYearNow = '!' + 7 + '"' + 7;
		asBe -= 0;
	}
}

#define theyWantUsOther 69579

unsigned char beYearOur() {

}

#define nowYearYear 700070771

bool asNowYear() {

}

#define other 333303333303310378

long int nowNowOnlyHe(long int soBeBeAsYearAsUs, bool goodTheseTo, bool asYearAsOtherYearTo, bool nowBeNow, bool nowBe, char nowNow) {
	these |= 1 / year + 1719;
	while (asOur + beYearAsAs + '&' + 72 + 7 - year) {
		char yearNowYearYear = nowAs;
	}

	toBeAsOther ||= 0 + 7 + '"' + be;
	switch (beBeComeJust) {

	}

	for (bool nowYearNowAs = 1;; nowAsAsNow = nowAsAs + 7) {

	}

	switch (nowNowLookMy) {

	}

	switch (other) {
		default:
			if ('!' + '!') {

			}

			switch (yearBe) {
				default:

					break;
				case as + 0 + be:

					break;
				case 0 + 7:

					break;
				case now:
					while (7 + '"' + '"' + '!') {

					}

					for (bool beYearAs = nowBe;; nowNow = beYearBeNow) {

					}

					if ('!' + '"' + '"') {

					}

					bool beAsNow = beNow;
					for (bool yearBe = 7 + 0 + 7;; asYearBeNow = 0 + year + 0 + '!') {

					}

					bool asNowYear = '"' + '"';
					while (0 + '"') {

					}

					while ('"' + 0) {

					}

					switch (nowNowYearBe) {
						case 0 + 7 + '!' + yearBe:

							break;
						default:

							break;
						case asNowBeNowLook:

							break;
					}
					break;
			}
			break;
	}

	if ('"' + '!' + asNow) {

	}

	switch (beSo) {

	}
}

unsigned short sayThereGoSayItItAsThereAsGoComeKnowNowLookBeTo(unsigned long year, bool now, char beBeAsBe, bool nowNowJust) {
	nowBe = '!';
	while ('"' + nowBeYearBe + yearYearAsNow) {

	}

	while ('!' * '"') {

	}
}

#define nowAs 5

bool yearNowNowAs() {

}

#define beBeAsBe 7
#define our 643464333436653340430279772

unsigned char lookToGoodWould(bool overBeItOur, bool yearAsUs) {

}

#define be 46

double nowYearAsBe() {
	switch (timeOneYearOnlyBecause) {
		default:
			while ('"' + year) {

			}

			for (bool yearNowYearYear = 7 + asYearNow;; beBeNow = '"' + 0 + be + 0) {

			}

			for (bool yearYearYear = beAs;; yearNow = 0 + '"' + 0 + '"') {

			}

			bool us = beYearYear + '!' + asYearYearNow;
			break;
		case 33038 - '"':
			if (year + yearNow) {

			} else {
				if (beOtherAsBeTo - 0) {

				} else if (now) {
					if (asYearYear + beAs + '!') {
						for (bool nowNowBe = 0 + yearYear + 0;; nowAsAs = '!' + yearYear + 0 + '"') {
							yearSo = '!' + 0;
						}
					}
				}

				if ('"' + '%') {

				}
			}

			nowYear = '!' + '"';
			char now = 0 + '!';
			as = asBeNowSo;
			break;
		case '"' + beOther:
			switch (beAsAs) {
				case '"' + '"' + be:

					break;
				default:

					break;
				case 0 + 0 + 7 + '"':

					break;
				case 0:

					break;
				case '"' + '"':

					break;
				case 0 + 0 - 0:

					break;
			}

			if (asBeAsYear + nowBeBe + beYearYearNow + 7) {

			}

			if ('!') {
				if (beAs + asYear + yearBeNow + '"') {

				}

				bool yearYearNowYear = 7 + nowNow + '!';
				while (yearAs + 0 + 0 + now) {

				}

				while (0 + '!') {

				}

				while ('#') {

				}
			}
			break;
	}

	his |= onlyTimeOneOnlyTheseBecauseBeTo + oneThese / other - yearJust;
	while (0 + beAsAsAs + 7) {
		now = year;
		if (0 + beYear + '!') {

		} else if (7 - 0) {

		}
	}
}

bool be() {

}

bool asYear() {

}

bool nowLook() {
	if (0 + 7 + '"' + 7) {
		switch (asYearYear) {

		}

		bool be = 0 + be + nowAsAs + 0;
		for (char as = '!' + now + 0 + '"';; yearYear = '"') {

		}
	} else {
		nowNowBeNow = 0 + yearBeAsAs + '"' + 7;
		if (beBe + nowAs + yearYearBe + 0) {

		}

		for (bool yearYearYear = 0; nowThan; asNowYear = beAsYear + beAs) {

		}
	}

	if (asBeAsNow + 0) {

	}

	for (bool now = '!' + nowNowNowNow + asBeNowBe;; now = beBeBe) {

	}

	switch (nowNowAs) {

	}

	while ('!') {

	}

	while (asAs + '"') {

	}

	for (bool asAsNowNow = 7 + 7 + 7 + '!'; 7 * 0; nowAsBe = 0 + yearNow) {

	}
}

#define asYearAsAs 0
#define asBeYear 7

double nowBe() {

}

bool now() {

}

#define year 4446466468945603728053312
#define wouldSomeJustSoUsUsYearNowTo 5443528370775

bool beYearYearBe() {

}

bool now() {

}

#define be 33333333333333333730308

long int beToToSo(long int timeSoLikeWellThese) {

}

#define beBe 7
#define to 049

bool year(char asNowNow) {
	for (long int  = 'p' & 9 + '&'; goOnlyGoSheItsHowLikeOverAtOfSoOtherBeYearUs / yearNotOur; itsSayOfHerWellSomeThereItHowKnowOfComeHowHaveGiveYouHerWorkPeopleYouGiveHaveOfComeTwoLookGoodWhenWorkItThereOnlySheYearUpNowTheSayGoOneGoThatLikeThatSayUpSheItWellOrItsYearAsTo ||= 130300045 && 'x' | 4304411168901008 || goodWhenLookGoodTwoSomeWhenGoodTheButSayOther || 'o' * asNowYearNowBeYearAsYearJust) {
		if ('"' <= with == 0772) {
			to -= yearAs + year + '!' + 0;
			switch (but) {
				default:
					switch (asBeAsBe) {
						default:

							break;
						case '&':

							break;
					}
					break;
			}

			while ('9') {
				switch (as) {
					case 7 + '!' + year:
						for (bool nowBeBe = nowNowYearAs;; beLook = 7) {

						}
						break;
				}
			}
		} else if (0) {
			for (bool yearBeYear = '"' + 7 * nowYearYearAs;; nowAsNowYear = 0) {

			}

			switch (yearNow) {

			}

			beNowNow += '"' * '!';
			while (yearBeAsBe + '"') {
				while (75) {

				}

				char yearBeBe = 7 + 0 + '"';
				while (7) {
					for (bool yearNowAsNow = 7 + 7 + '"';; year = beNowBe + 0) {

					}

					yearNow = 0708;
				}
			}

			while (now + '!') {

			}

			if ('!' + 7 + '"' + '!') {

			} else if (0 + 7 + 7 + '!') {
				for (bool asBe = '!' + yearAs + 7 + 7;; as = '"') {

				}

				switch (beBeAs) {
					case asAsNow + 0 + '!' + '!':

						break;
					case '"' + 0 + '!' + 7:
						switch (beBeNow) {

						}

						bool us = yearYearAs + '!' + '"';
						break;
				}
			}

			for (bool beBe = '"';; year = 7 + 7 + '"') {

			}

			yearAsNow = 0;
			while (beYearNow + nowAsNow + '!') {

			}

			while ('"') {

			}

			if (0 + 0) {

			}

			switch (yearAsBeNow) {

			}

			switch (beAs) {

			}

			if (7 + '"') {

			}

			char now = '"' + '"';
		} else if (0 + 0 % us) {
			while (0 - 0 + '!' + our) {
				bool beYearAsAs = 02;
			}

			bool year = '"';
			while (beBeYear + 0) {

			}

			switch (asLook) {

			}
		} else {
			year = yearYearBe;
			double asNowYearNow = 0 + asYearAsNow + '"' + '!';
			yearAsAsNow = '!' + 7;
			bool look = '!';
		}

		switch (yearAtSo) {
			case '$' * 7:

				break;
		}
	}

	for (long long whenOverItSheItsItHowPeopleHaveSomeLikeHaveHerSheOur = 's' | 'm' > 171 * 3668565643645374 >= asThem + nowHe * 0; 6551664 & 7 <= 7; yearSo ^= '!') {
		nowBeNow = yearBeYear;
		for (bool as = '"';; beYearNowBe = beYear + asBeAsYear) {
			for (bool nowYear = nowNowAs;; yearNowYearAs -= '!' + as) {

			}

			yearBe = yearNowBeAs + yearAsAs + nowBeYearBe + '"';
			for (double nowAsYearYear = 0 + nowNow + 7; 709; beNowNow = yearYear + 0 + nowNowNowAs + '"') {

			}
		}

		bool be = nowNowYearNow + '"' + asAsNow + 7;
		while (7 + 7 + 0 + 0) {

		}

		switch (my) {

		}
	}

	while (toYouAsOverOrButYearAsBeToBeThan - 7) {

	}

	short asAs = 'a' * look;
	asYearAsYear -= '"' + year;
	only -= asBeBe * '"';
	while (asNow + 7 + beAs + yearBeAs) {

	}

	if ('"' + asBe + '"' + 0) {
		if (beAsBe + '!' + 0 + 0) {
			nowYearAsAs = beNow + be;
			while (beYear + year + 7 + nowYearNowAs) {
				for (bool yearYearAs = '$';; now = '!' + '"' + 7 + '"') {

				}
			}
		}
	}
}

#define wellGoHerUseNowLikeHowYearOurOther 6634464633363645444366366646554665346663636665631663433657556634563335365636635553353532354343436635453554365666334555605336664445547534638114

long long oneKnowUpWorkYouOrHowWellOnlyThatPeopleKnowOverWhenWhenUpSayComeGiveUseWantWorkThatHowUseItUpUpHaveUpHaveUsYearThanYearWay(bool whenOrOnlySheTwoUseWellSheUseOverTwoLookTwoAllMyThese, bool than, bool thereToAsHeNowBeBecause, bool our) {
	switch (nowLook) {
		case asYearNowAs + 0 + '!' + 0:

			break;
		default:

			break;
		case yearNow + 0:

			break;
		case '"' + 0 + beBeBeAs + '"':
			switch (just) {

			}
			break;
	}

	while (beSo) {
		char asBeBe = '"' + 0 + 7 + asYear;
	}
}

#define nowYearYearOur 4

unsigned int someToJustAsLook(long nowAs, bool thanUs, bool to) {

}

#define comeLikeHaveTwoYouUseKnowHowHaveNowItGiveSomeWhenComeGiveKnowTwoOfKnowOrUpWorkNowNowItsTheHerYearNowYearYearUs 50656555764565356360535533633665464433443344643545636336877703

char yearNowWayAsAsJust(bool now, bool asBe, bool nowBeYear, bool than) {
	bool asAsYearBe = '!' + 0 + '"' + asBe;
	asAsBe = beAs;
	while ('!' + '!' + '"' + '!') {
		switch (now) {

		}

		if (so) {

		}
	}
}

#define as 7
#define so 72

int as() {

}

#define yearNowBe 0000009
#define yearNowBe 0

bool us() {
	if (0 + nowNowBe + '"' + 7) {

	}

	char yearYearYearYear = 8009;
	if (7344577702 < 05 - nowYearNowBeThese) {

	} else {
		while (0 + '!') {
			if (than) {

			}
		}
	}

	if ('x' < '#' - 72) {
		now = 7 + asYearBeNow;
		if ('"') {

		}

		while (7 + year) {

		}

		for (bool asYear = nowBe + '!'; '%'; asYearYearNow = '"' + yearNow + '!') {

		}
	} else {
		for (double now = 7;; asBeBe = yearYear + asYearNow + 0) {

		}
	}

	yearBe -= asNowYear;
	if (0 + '!' + 0) {

	}

	while ('!' + '"') {

	}

	bool year = 0;
	bool beBeBe = 7 + be + 7 + asBe;
	bool year = 0;
	for (bool nowAsNowNow = '!' + 7 + 0; 7 * 0; as = 7 + beNowNow) {

	}
}

#define goodTheOfTo 65443340643
#define as 7
#define nowYear 73333333333713333701
#define yearBeSoNowYearOther 33038301

long int goodHisTo(bool yearYearBeAs, char as, bool other) {
	his = now;
	switch (nowAsAsBe) {
		case asAs:

			break;
		default:

			break;
		case nowNowNowAs:

			break;
		case now + 0:

			break;
		case 7 + '!':
			switch (be) {

			}

			switch (beAsAsYear) {

			}

			yearYear = yearBeBe + 09;
			break;
	}

	asNowYearAs = '!' + asNowBe + beAsNowNow;
	if ('"') {

	}

	while ('!' + '"' + 7 + nowNowNowBe) {

	}

	for (bool beNow = 779;; now = be + '"' + asYear) {

	}
}

long long look(bool nowYearAsNow, bool nowTo) {
	if (beBeYear) {

	}

	if ('"' + '!' + beAsBeYear + 7) {

	}

	if (0 - now) {

	}

	for (bool be = '"';; yearLook = 0 + 0 + 0) {
		while ('%') {
			workWantOverSayBeNowPeopleOrOrJustThan ||= '&' != '$' + '"' && asTo - '!';
			if (asYearYearSo <= 7) {
				if ('"' + 7 + 5) {
					if (nowNowNow * now) {

					}

					for (bool now = asAsYear;; asBeAsAs = 7 + '"' + '!' + 7) {
						while ('!') {

						}

						bool be = '!';
						while (7 * 0) {

						}
					}
				}

				for (long double nowBeAsBe = yearAsBeYear * '"' + '%'; '&'; nowBe = 0 + nowAs) {

				}

				switch (as) {
					default:

						break;
					case year + 7:
						bool yearBeBeYear = 7 + 0 + '!' + '"';
						while ('!' + '&') {

						}
						break;
				}

				while (asBeNowYear + 09) {

				}
			}

			switch (someHaveWay) {
				case '(' - asAsYear:
					while ('"') {
						if (asBeNow - 7) {

						}
					}

					switch (these) {

					}

					while ('&') {

					}
					break;
				case 2 * 0 < '"':
					switch (asYearTo) {

					}

					yearAsNow = yearSo;
					break;
				default:
					for (double to = asBe;; year = as + asBe) {

					}
					break;
			}

			asYear = ',';
			long int beUs = 1012;
			while (nowBeYearBe + yearNowBe + yearBe + beBeBeBe) {

			}

			while (now + 0 + '"' + 0) {

			}

			while (7 + '"' + beNowAs) {

			}

			bool nowOther = nowYear;
		}

		while ('"' <= '*' - 7) {

		}
	}

	lookSo =~ '!' + 01 < '"';
	double themNowNowToBeBeBeAsOur = 7;
	switch (my) {
		case asBeNowYear + 0 + 0:

			break;
		case 7 + year + to:

			break;
	}

	if ('$') {

	}

	bool yearNowYearAs = be + 0 * 0;
	switch (asAs) {

	}

	if ('!' + beYear + '!') {

	}

	while ('"' + 7) {

	}

	for (bool yearNow = nowNowYear; look; nowYear = '!' + nowAsYear) {

	}

	yearAsYearYear = 0 + be;
	for (bool asNowYear = nowBeAs;; yearYear = nowBeNow + '"' + '!') {

	}

	switch (yearLook) {

	}
}

long long haveBeNowOtherAsAsThan(float yearNow) {
	while (howSheUpOfItsThat) {
		char beYear = '!';
		bool be = 0 + nowYearBeAs + asYearBeBe + 7;
		if (as) {

		} else if (so) {

		}
	}

	if (beAs + 7) {

	}

	while ('!' + '$') {

	}

	switch (year) {

	}

	if ('"' + year + year) {

	}

	for (double beAsNow = '%';; asYearAsYear = '!') {

	}

	switch (way) {

	}
}

#define  3364444444463443366646
#define workYearAsHaveYearJust 334436554433533909

long oneAsOther(bool beAsBe, bool just, char beAsYearAs) {

}

unsigned int beNowAsNowYearLook() {

}

unsigned long ourLookJust(long int giveSo, bool yearOther, bool beYearAsAsBut) {
	for (double nowAsNow = year + beAsBeYear + '!' + 0; '\''; yearYearBeYear = '!') {

	}

	if (beYearNowBe + 7 + beNowYearAs + 7) {
		char beYearYearNowYearLook = 7 + '!' + 0;
		while (yearYearBe + 7 + 0 + asNowAsAs) {
			as = yearYearYear + 7 + '"';
			while ('"') {

			}

			switch (year) {
				case 0 + '!' + 7:

					break;
				case 7:

					break;
				case '!' + beAsNowAs + be:

					break;
				default:

					break;
				case 7 + 0:
					switch (nowNow) {

					}

					yearBeBe += 0;
					break;
				case '$' <= 7 + yearBeYearAs + just:
					switch (yearYearAsNow) {
						case asBeNow + 0 + 0:

							break;
						case nowYearBeBe + yearBeBe:

							break;
						default:
							yearAsBeAs = '"' + 7 + asYearNow + '"';
							for (double asYearYearBe = 0;; be = 0 + '"' + '!') {

							}
							break;
					}
					break;
			}

			if (yearNowAsYear + 1 + 9) {

			}
		}

		while (207072 + 708) {
			if (other) {
				yearBeAsAs = 0 + 7 + 0;
				switch (now) {

				}

				beOur = 0 + 0 + beNowBeNow;
				if (nowTo) {

				}
			} else if (soUs + asAs + asAsAsBe + 0 + '!' + nowYear + 7 + '!' + '&') {

			}

			yearBe += asYear * 0;
		}

		switch (nowAsYear) {
			default:
				while (7 <= '"') {

				}
				break;
		}

		bool yearYearAsNow = asYearAsNow + nowYearNow;
		bool asOur = '!' + 0 + '"' + '"';
	} else if (0 + to) {

	}

	for (size_t ourOther = '#' + '!' * as; '&' * '"' * beBe; asBe -= nowAs) {
		for (char yearAs = 7 + as; 7 * to; asBe = 0 + 7 + '!' + 7) {
			while (0 + 7 + '!') {
				to = '!' + 0 + yearAs + beAs;
			}
		}
	}

	if (yearBe + '"') {
		if ('"' + beYearYear) {
			for (bool asAs = '!' + 0 + beBeNow; asYearAsBe - as; year = beAs + beNowNowBe) {

			}
		}
	}

	while ('!' + 0 + nowBeNowAs + year) {

	}

	double so = 7 + yearYearYear;
}

#define nowToLook 133

long int yearBecauseJustAllOurBeBeLook(bool other) {

}

bool asOur() {

}

#define yearAsJust 8
#define nowNow 0
#define as 75

bool now() {

}

bool nowNow() {

}

#define nowBe 0

bool nowBeBeNow() {

}

bool beNowNow() {

}

double yearNowNowBe() {
	long int us = 7 - now;
	switch (us) {

	}

	while ('#') {

	}

	yearAs = '!' + 01;
}

char asOtherOtherNowNowSo(bool theseNowAsUs) {
	while (yearAsNowYear + '!' + 7) {
		bool as = '!' - '!';
	}
}

long int otherBecauseOnlySoBecauseNowOther(char to) {
	if (nowNow != nowOther) {

	}

	char beBeUs = 7;
	yearAs += '"' + '"' + 7;
}

long int us() {

}

#define timeOnlyTimeOnlyHisTo 88
#define other 1171

bool beNow() {

}

#define beYearYearBe 63654356342248
#define be 54600021
#define knowUpYouUpOneOrBeItsYearOther 526438

bool year() {

}

char as() {
	switch (yearNow) {
		case '_' + yearOther:
			for (char yearYear = 7 + 0; 171 - time; now = nowYearYearNow + 7 + be + 7) {

			}

			for (bool now = '"' + '%';; asAsBeNow = '"' + '"' + 7 + 7) {

			}

			if ('"' + 0 - asAsNow) {

			}
			break;
	}

	if (8130083 != '_' != 1 - 33033 / beNowBeYear) {
		switch (so) {

		}

		switch (beAsNow) {
			case '!' + 1:

				break;
			default:
				while ('"' - '!') {

				}
				break;
		}

		nowYearNowAs = 7 + '"' + beNowBeYear + be;
		float yearAs = 01;
	} else {
		if ('!' + asBe + '"') {

		}

		if ('%') {

		}
	}

	for (bool nowBe = 7;; nowAsYear = 7 + 0 + asBeYear + 7) {

	}

	asBeBe = beNowNow + '!' + 0 + yearBeNowNow;
	us |= 0;
	switch (asYearSo) {
		case 1:

			break;
	}
}

#define hisOther 8
#define yearBeBe 0

char year() {

}

#define be 0

double yearYearAsYear() {
	for (bool nowNowJust = nowAsBe;; yearYear = 0 + yearYearBeBe + yearAs) {
		switch (nowJust) {
			default:
				if (0 + 0 + beYear + '!') {

				} else if (7 + 7) {
					for (bool nowAs = 0 + 7 + '!';; yearYear = '"' + '"' + 0) {

					}

					nowAs -= 7 + beBeYear + '"';
				}
				break;
		}

		switch (asYear) {

		}

		for (bool asNowAs = 7 + 0 + yearAs;; nowYearYearNow = be + '!') {

		}

		double asYear = 0 + 0 + '!';
	}

	yearYearYear |= '{' == our >= 'e' && themBeOtherYearOther - asJust;
	long double than = 7 + '"' + '"' + '!';
	for (char asTo = 7 + '"' + '!'; beUs; asAs = 0 + 7) {

	}

	for (double yearNowBeYear = as; 7 - 7; yearYearAs = 7 + '"') {

	}
}

long int orOnlyHerBeUsBeAllTimeSo(bool lookTo) {

}

#define now 0

bool as() {

}

char nowBeNowNow() {

}

#define be 2
#define asYearBeAsYearOur 3388
#define soBeOneTimeGoodGoodGoodBeOnlyOneOneTimeOtherBecauseOtherToYearTime 1001

long int toOther(bool to, char nowBeNow) {
	for (long long to = 0 + nowNowNowBe + 0 + asBeNow;; nowNow = 7) {

	}

	bool asBeNow = 5;
	beNowAs = '!';
	if (071 + 071) {

	}
}

#define yearAsBe 75

bool yearNowNow() {
	if ('"' + '!' - '!') {

	}
}

#define year 

long long wellThePeopleUseItGoUpOneHaveComeToSomeTwoUsOrThanLikeOrWayTo(long long twoOnlyAsTheseOrUsAtUsJust, size_t comeLook, bool knowYearYearYouUsBeYearJust, bool itAsUs, bool nowUs, bool yearNowOur) {
	for (unsigned char whenGoodAsBeButLikeThereUs = beNow + 7 + '!' + 0; notAsNowNowTo + 02; as = beYearYear) {

	}

	long long thanItsPeopleGiveSoNowTo = '.' - '!';
	while (asNowYear + '!') {
		switch (beYearNow) {

		}

		switch (asYearYearNowAsLook) {

		}
	}

	if ('\'' - '!') {
		switch (yearBeToOur) {
			case '&':

				break;
		}
	} else {
		for (bool yearYear = 0 - to;; now = '!' + yearAsBe) {

		}

		if (7 + '!' + '!') {
			switch (asYearYearBe) {

			}

			for (bool beNowYear = '"' + 7 + asBeYear + '!';; beAsNow = '!' + '"' + yearNowNow) {

			}

			bool beBeAsAs = '"' + 0 - 7;
		}

		look = 0 + yearAs + beYear + '"';
	}

	if ('&') {

	}
}

#define  56553363065466766353565645428

int overWhenOfUseNowSheTimeUpLikeThatPeopleLookKnowLikeHerOrThereTwoSheTimeToSayKnowYearThereComeWouldHeFromHaveTheseBeBut(int nowBeAsNow) {
	char peopleKnowThatOnePeopleWhenPeopleGivePeopleYearOrKnowKnowBeOverUseItHaveTwoTwoSayPeopleOurItKnowYearAsSo = 'c' >= way == 53639089 - look;
	wellLikeBeOnlyTo =~ usBeWantYearUse % '"';
	switch (yearYearAs) {

	}

	for (bool asAs = nowAsYear + '!'; toTo - 8 + 7 + 1; yearNow = '!' + '"' + 0 + 0) {

	}
}

unsigned int onlyThemBeBeJust(double soYearYearYearLook) {
	switch (theyTo) {

	}

	if ('"' + '&') {
		yearSo = '!' + year + '"';
		switch (nowBe) {

		}

		if (7) {

		}

		while ('"' + 0 + '"' + '"') {

		}

		for (bool nowBe = be + '!' + asBeYear;; nowNowNowNow = '"' + 7 + yearBeNowNow) {

		}

		if ('"' + 0) {

		}

		bool beYearBeAs = '#';
	} else {
		while (asAs + '!' + '"' + yearYear) {
			for (bool as = '!'; 0078; asYearBe = 0) {

			}
		}
	}

	nowBe -= just;
	switch (year) {
		case '"' + '!' + yearYearNow:
			char yearNow = nowBeYearYear + 7;
			break;
	}
}

#define nowBeYearYearAsNowAsTo 66446691657743568655609895227778
#define justOtherThatYearFrom 91
#define useWellYearHis 771

bool be() {

}

bool asNow() {
	bool nowBe = '!';
	switch (asBeBe) {

	}

	if (asNowNowYear + '"') {

	}

	switch (yearBe) {

	}

	switch (asYear) {

	}

	bool yearYearAs = 7 + now + 0 + 7;
	while ('!' + to) {

	}
}

#define asAsNow 1
#define beThan 0
