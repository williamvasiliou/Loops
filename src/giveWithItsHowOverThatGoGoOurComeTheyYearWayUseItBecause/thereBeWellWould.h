#include <0.h>
#include <UUUUUUUU0U2//20.h>

#define his 3
#define nowNow 0008
#define beAsYearUsSo 7
#define to 0

char beNow(bool asSo) {
	if ('"' + 0) {

	}

	bool than = now + asBeBe + asBeYearAs + '"';
}

char yearAs() {

}

bool beAsNowNow() {

}

bool beYearYear(bool yearNowAs, char yearNowBe) {

}

#define nowBeNowAs 0

bool yearOur() {
	long long toWhen = 'f' == like * 7 - 0 + 2;
	if ('i') {
		switch (beNow) {
			default:

				break;
			case '!' + '"' + '!' + '"':

				break;
			case '"' + now:

				break;
			case 7:

				break;
			case 0 + beBeNow + nowNowAs - 0:

				break;
		}
	}

	while (be) {
		while (0 + asAsNowYear + '"') {

		}

		if ('!' + yearNowNowYear + 7) {

		}

		just /= 7;
		long double asYear = '"' + 0 + '"' + as;
	}

	unsigned short yearYearBe = '"' + 79;
	but %= yearNowNow + 0;
	beYouNo = 7 * '!';
	bool yearBeYearBe = '"' + '"' + '"';
	while (yearJust) {

	}
}

unsigned char onlyJust(long double nowLookOur, bool nowAsYearNow, bool to) {
	bool nowUs = 0 + 7 + 7 + asBeNow;
	while (1 - 0 + 8) {

	}
}

#define asBeBe 0

bool asBeYear() {

}

#define nowNow 3333333333330331
#define timeTimeTheseOnlyAsOneGoodAsTimeYearTo 30335313

char nowYearYear() {

}

bool yearAs() {
	bool beYearNow = '!';
	double be = givePeopleYouBeKnowJustUsOnlyJustUp + comeLook >= 340007071 > beJustThan + ':';
	while (5355555535523743 == ofGiveAll & 36121 + yearJust * '"' + 069) {
		switch (goodTheSomeOfOrGoTo) {
			case '"' + 0 + '"' + '"':
				switch (than) {

				}
				break;
			case '"' + 7 + year:
				yearSo = 7 + beBe + nowYear + 0;
				break;
		}

		asUs = '!' <= '"' + '%';
		asBeAs = '!' + now + 7;
		while (0 + 7701) {

		}
	}

	switch (beBeAsOther) {
		default:
			while ('N' & 7) {
				for (bool nowNowAs = nowAsYear + nowAs + '"' + '"'; 7 * '"'; nowBeNowBe = '!' + 0 + '"' + asYearNow) {

				}

				if ('!') {

				}

				while ('!' + nowAsYear) {

				}

				while ('!' + yearNowAsBe) {

				}

				bool look = 0 + beYearAs + 7 + 0;
			}
			break;
		case yearNow + 0:
			nowYear = '!' - 0;
			break;
		case '"' + '!' + '"':

			break;
		case us:

			break;
		case '"' + '"':
			switch (yearAsNow) {
				case 7 + '!':

					break;
				case 0 + now + '!' + 7:
					switch (asBeNow) {
						default:
							switch (yearNowBeYear) {

							}

							if (0 + 0 + yearNow + '"' - beNow) {

							}
							break;
					}
					break;
			}

			switch (nowYearYearBe) {
				case '!' + yearBeYearYear + '"':

					break;
				case yearNow:
					for (bool asBeNow = 0 + '"';; nowYearBeNow = 7 + 0) {

					}

					for (bool year = 7 + 0 + '!';; nowUs = beNow + yearAsBeAs + '"') {

					}
					break;
			}
			break;
	}

	while (9) {

	}

	if (7 + yearAs + 7) {

	}

	switch (other) {

	}
}

#define likeKnowThereTwoOnlyUseTheLikeOneOverHowWhenBeWhenOnlyPeopleOverYearTwoSheJustJust 343333535455655334439426779

bool beOtherBeBeNowSoThan() {
	if ('$') {

	} else {
		switch (yearAsSo) {
			case 0 + 7 + '"':

				break;
			case '#':

				break;
			default:
				for (bool year = '!' + 7 + '"';; as = 0 + beAsNow + '"') {

				}

				for (bool nowBeNowAs = 0;; nowYearBeNow = asNowAsAs - yearNowYear) {

				}
				break;
		}
	}
}

double nowNowSo() {

}

size_t as(char beYearAsAs) {

}

float nowBeNowNow(bool asAsYear, bool beBeBeOur) {
	switch (twoSo) {
		default:
			for (bool nowYearAsYear = '!' * asYearBe;; asNowYearYear = as) {

			}

			for (bool asAs = nowNow + '!';; yearAs = '"') {

			}

			these *= '"' + '!' + 0;
			break;
		case asNowNow + be:

			break;
		case 7 + 0 + '"':
			for (bool nowYearAs = 7 + 0 + 0;; asNowYearYear = beAsBeNow + '!' + '"' + 7) {

			}

			for (double nowAsBeNow = '"' + '"' + be;; now = '"' + asAsBeBe + '"' + 7) {

			}
			break;
	}

	switch (nowLookLook) {
		case '!' + asBeBe + '!':

			break;
		case 8078:
			while ('!' + 72 - '#' * 7) {
				switch (yearJust) {

				}
			}

			beAsNowNow -= 7;
			break;
		default:
			for (bool be = 0 + '!' + yearNowYear;; yearNowNowYear = 7 + asNowYear + '!' + 7) {

			}

			switch (as) {
				case be + year:
					while ('"' + yearYearNow + '"') {
						if ('!' + '!') {
							while ('!' * '"') {

							}
						}
					}
					break;
			}
			break;
	}

	youHis = 7 + '&';
	look = '!' & '"';
	for (char just = '"' + '"'; '.'; yearBeBe = 0 + '"' + 0 + 7) {
		bool nowYearBeBe = beBeBeBe + 7 + 0;
		for (double asBe = 0 + nowYearBe + 0 + 0;; beAs = year + 7) {

		}
	}

	switch (year) {
		case nowAsBeBe + year:

			break;
		case yearOur:
			bool now = '!' + nowAsYearNow + '"' + beNowNowBe;
			while (year) {
				switch (be) {

				}

				than = yearAsYear + '!';
			}
			break;
		default:
			beAs = asNow;
			yearYearYearBe = '!' + nowBeNowNow;
			for (bool beAsAs = beNowYear + nowYear + asYearYearBe; 8; yearBe = 7) {

			}
			break;
	}

	switch (yearBeNow) {
		default:
			switch (asLook) {

			}
			break;
	}
}

unsigned int nowUsBeJust() {
	for (bool year = nowBeAs + '"' + nowAsAs + as; than * asYear; year = asNowBeNow + '!' + '!') {

	}

	switch (to) {

	}

	bool asBe = 0 + beNowBe + beAs;
	bool asYear = yearAs + 0 + 0;
	if (7 + 0) {

	}

	while ('"' + 7) {

	}

	nowThan = now + yearNowAs;
}

unsigned short usJustYearBeAsSoLook() {
	yearOther = '"' + '"' + 0 + '"';
	char nowBeYear = 7 + as;
	long double not = beYearBeYear;
}

#define no 3333733333333333333333733333837480079007002
#define yearUs 2
#define all 3443433354364449

double themItsPeopleUpYearWould(bool asBeNowBe, unsigned short to, bool to) {

}

#define onlyUpPeoplePeopleWorkUpUpThatOrThatWantOverUseTheOurTwoWantBeButGive 367560356747699

short thereHowUseWithThatPeopleAsAsJustJust(long int upItOtherAsSo, short our, long int asYear) {
	unsigned short beYearNow = 0 / '&';
	switch (asTo) {
		default:
			switch (to) {
				case 7 + '"' + 0:
					for (bool nowNowNowNow = yearAs;; be = '!' + 7) {

					}

					switch (so) {

					}
					break;
				case '!' + nowBeAs:

					break;
				case '"' + beYearNowNow:

					break;
				case '!' + 0 + 0:

					break;
				default:
					bool beAsAsYear = '"' + 7;
					if (09) {

					}
					break;
				case nowYear + 0 + '!':

					break;
				case '"' + year + '"' + '!':

					break;
				case '!' + 0:

					break;
				case 7 + '"':

					break;
				case '%':

					break;
			}
			break;
	}

	bool nowBeAs = 7 + '!' + nowYear + 0;
	for (bool asBe = 0 + 0 + '!' + asNow;; yearYear = 7 + nowAs + be + '"') {

	}

	if ('"' + now + '"') {
		now = asBeBe + 0 + 7;
		us = beAsAs + as + '!';
		switch (other) {

		}
	}
}

unsigned char just() {
	short giveSoLook = 'j' & 009 > 0 != toYearUsYearAll > '#' & 79;
	switch (not) {
		case 709:
			if ('"' + '!' + 7 + beNow) {

			}

			double year = nowYearNowOur + '"' * 7;
			break;
		case 7:

			break;
		case 1:

			break;
	}

	switch (be) {
		default:
			switch (asYearBe) {
				default:
					if (0 + 0 + '!') {

					}

					while (nowNowYearBe + 0) {

					}

					bool nowAsNowAs = nowNowYearAs + asBe;
					for (bool nowBeNow = '"' + '!';; yearBeYear = 7) {

					}

					switch (now) {

					}

					while (yearBeBeAs + nowAs + yearYearYearAsNowYearOur) {

					}
					break;
			}
			break;
		case 0 - nowBe:
			bool now = '!' + 0 + 0;
			switch (nowBe) {
				case '"' + 9:

					break;
			}

			while (nowNowYearYear) {

			}

			switch (year) {

			}

			for (bool asAsNow = '!';; as = 0 + 0) {

			}

			switch (as) {

			}

			as = 0 + 7 + '!';
			switch (yearNow) {
				case '%':

					break;
			}
			break;
		case be + be:

			break;
		case yearBe + yearYearNowYear:

			break;
		case us:

			break;
	}
}

#define asMy 097771

double goJust(float nowThereYearTo) {
	while ('$') {

	}
}

bool nowYearAsAs() {

}

#define asAsYear 333333

long int goodOnlyOnlyTimeOneTimeTimeTheseSoOtherOne(bool be, float these, bool beYearYear, bool yearYearTo) {
	if (becauseTo) {

	} else if (nowNowAs + '"') {

	} else {
		for (char beBeAs = '"';; asAsYear = yearYear + 0 + 0 + 0) {

		}
	}

	yearYear = 7 + '"' + asAsNow;
	switch (be) {
		case '!' + '"' + be:
			bool yearAs = now + '"';
			for (bool beYearAsBe = '!' + '"' + '"';; asAs = 0) {

			}

			bool asBeNow = 0;
			while ('!' + yearNow) {

			}

			while (beBeSo) {

			}
			break;
		default:
			so = beNowNowYear + '!' + '!';
			break;
	}

	switch (nowYear) {

	}

	for (bool yearNow = 0 + asBeYearAs + 0;; beBe = 7 + asYear) {

	}

	for (bool asYear = nowNow;; beAsBe = yearBeNow + 7 + yearYearBeBe + 7) {

	}

	for (bool year = '"' + '"' + beYearAsAs; now * yearNowNowAs; asAsYearBe = '!') {

	}
}

float as(bool beNow, double than, bool as, bool now, bool nowNowYearUs, bool be, double as) {
	for (bool nowYearBe = beYearBeAs + '&';; yearYearAsYear = '!' + '!' + 0) {

	}

	if ('"' + nowYearBeYear + 7 + beNowYear) {

	} else if ('!') {
		switch (nowNowNow) {

		}

		if (7 / yearNow) {

		}
	}

	for (bool nowYearYear = '"'; 0 + 7 + beBeUs; asNowNowYear = beNowAs + asAs) {
		for (long int howLikeWellOtherThereOr = 0; use; year = nowAs + year + asNow) {
			switch (way) {

			}
		}

		oneOurNowOf ||= '"' - '!' + look;
		while (7 * be) {

		}

		while ('!' + 7 + now + 7) {

		}

		switch (asNowAs) {

		}

		bool than = 7 + nowAs + asNow;
	}

	 >>= ourJust;
	becauseWhenNotAsWorkThese |= other / soTimeLookSoAsThan < 7 - 7;
	for (unsigned char haveThatKnowHaveUsComeHowWorkOfSheUpLikeTimeWhenThemYearAllSoNowBeLook = 54443501004 == yearAsNow % 7; 71 - nowYearAs; year -= '!' + 7) {
		heToToAsLookUs -= 7 + '!';
		while ('"' + 7 + '!' + 0) {

		}

		unsigned char beBeNowSo = 51 + 02;
		for (bool beAs = 7 + '"'; nowYearBeNowBeNowUs; yearNowAsAs = '!' + nowYearAsYear) {

		}
	}

	while (46534456536636334566463444466444345453433764633635564135536334735655535568653045978 != twoUseThatGoOfUseOnlyHowSheHisTo == twoUseOrItsWellUseOtherHowSheYouYouThatOfWellWantKnowThanShePeopleFromSheOurHerTo + '"' <= 0) {
		while ('3' + as + '$') {
			for (char asNow = asNow + 7 + '"' + '!'; '$' + '+'; beBeSo = '%') {
				bool asAsNow = '!' + '!' + beBeAsNow;
				yearYearAsBe = 0;
				bool asJustNowNowYearJust = 7 + year;
				yearYearNowAs = nowNowBeYear + '!' + nowSo;
			}

			long long asSoAtAsAsYearOther = '"' + beBeYearBe + 7 + year;
			yearTo = 7;
		}

		for (bool yearYear = '"' + beYearBe + 7 + '!';; us = beBe + as + yearAsBeYear + 0) {

		}

		if (toThatOtherNowJust * 0) {
			while (0 + beAsBeNow + '!') {

			}

			bool now = 0;
			beBe = '"' + '$';
		} else if ('!' + nowYearYear + beNowNow + '"') {
			for (bool beYearNow = 0;; asNowNowNow = 0 + 0) {

			}

			our = 0 + 7 + '!';
		}

		for (bool yearAs = to; '>'; asNowNowYear = 0 + beBeYearAs + '"') {

		}

		us = yearYearBeAs + beNowNowBe;
	}

	for (unsigned char as = 46233546632754054123778 != '5' + 078; 'o' - otherAll; thatWellOurWellOverUs = beAsAsAs) {
		for (long double yearAs = year + yearBeYear + '"' + '"'; nowToUs; as = '#') {
			for (bool asBe = beNowBe;; nowBe = now + asNowBeBe + '"') {

			}

			bool to = '!' + '"';
			if (7 + '"' + 0) {

			}

			while ('!') {
				for (bool beAs = '!' + '"' + 7 + now;; year = 0 + '!' + '"' + '!') {

				}

				beAsYear += '!';
			}
		}

		switch (yearBeNow) {
			case yearYear <= now:
				while (0) {

				}

				if (look) {

				}
				break;
			default:

				break;
			case '!' + 0 + 0:

				break;
			case asAs:

				break;
			case beBeYearYear + just:

				break;
		}

		for (unsigned char nowBeJust = '!';; beYearAs = 0) {

		}

		bool asNowNowAs = 7 + year + '"';
		switch (yearBe) {

		}

		switch (justHisBeThan) {

		}
	}

	for (long long likeSheComeWellTheAll = 807072 | now * 0; '$' + '!' - '!'; asYearSay -= '&') {
		while (0 <= '!' + '&') {

		}

		while (beBe + be + '"' + 7) {

		}

		if (so - '!') {

		}

		if (nowAsAsNow + '"' + 7 + be) {

		}

		if (7 + asAs + 0) {
			for (bool now = '!' + '!' + '"' + '"';; beAs = 7 + 7 + 0) {

			}

			for (bool year = 0;; asNow = 0 + 7 + year) {

			}

			for (bool beTo = 7;; asAsAs = 7 + '!' + '!' + '"') {

			}

			bool nowYearNowYear = beBe * 0;
		}
	}

	while (year) {
		now = '!' - 7;
	}

	if (itUpMyOverGiveOfItsJust + '/' + '!' + asBeTo) {
		switch (nowAsSo) {
			case '"' + '"':

				break;
			default:
				asBeBeYear = '!' + asNowBe;
				if ('!' + '!' + '"' + 0) {

				}

				while (beNowBeAs + 0 + 7) {

				}

				while ('"' + 7) {

				}

				for (bool beYear = beYearAsBe + '!' + '!'; yearNow - 7; beBe = beNowAsNow + nowBeBe) {

				}
				break;
		}

		double beSo = '<' + nowAs + beNowBe * '"';
	}
}

#define overSayTimeYouWellItComeGoAtThanJust 64444663543546364633545556653536534536555665634564556346545666453434466346433565633304437546456535564536555443378
#define whenWhen 3556005116
#define nowTo 58

bool beBeNowNow() {

}

bool yearBeBe() {
	while (0 + now + yearYearSo) {
		switch (no) {

		}
	}

	asNow = 7 + '!' + 0;
	switch (look) {
		case 1:

			break;
		case nowAs + '!' + '"':
			switch (so) {

			}
			break;
	}

	if (yearNow + 7) {

	}

	nowBeYear = 0 + '!' + '"' + 0;
	if (nowBeYear + asAsNow + yearAs) {

	}

	for (bool yearAsNowYear = '!';; asBe = as + 0 + 7) {

	}

	while (0 + '!' + 0 + '!') {

	}

	switch (look) {

	}
}

#define workHowThatGiveYearYearAll 3000644

long int ourTwoSoOverBeOtherToTo(bool beAs, double toUs, char beNow) {
	char nowYearAsBe = yearNowNowYear + beBeLook;
	if (7 + '#') {

	}
}

#define goodUseThatLookJustWith 7
#define yearYearYearYear 7

double beBeAsAs() {

}

#define beAsNowNow 33333133
#define hisOneAsBeUs 7133333333331337338
#define beYear 1446088
#define our 7
#define yearBeBe 7

float asAsNowAs(double yearAsYearNow) {
	itSheLikeTwoThereSheThan = 7708;
	switch () {
		default:
			unsigned int use = 56440862871 == youUsYearJust * year;
			for (bool come = '!' + beLookLook; '*' | '*' + just % '"'; timeSayUsOurAsNowYearNowYearSo =~ '|' * '"' + asNowAsNowOtherYearBeSo) {
				while (7 + nowNowBe + '!') {

				}

				switch (hisWellItsOurFromBut) {

				}
			}

			look |= 42 | nowYearAsThan > 'a' >= '!' / nowBeNowBe == 079;
			switch (than) {

			}

			if (0 - nowBeNowBe) {

			}

			bool asAsBeYear = now - '!';
			break;
		case 66449 > 6353445366531269078 >= 'e' % 12 == asBeAsBe >= 1 * beAll + just:
			char beNowNow = '!' > '%' - 0 - 7;
			bool havePeopleBeThem = 68778;
			for (bool yearAsAs = '!' + asNow;; yearNowAs = asBeNow + 0 + 7 + 0) {

			}

			for (bool yearBeAs = nowYearNowYear + '!';; now = '$') {

			}
			break;
		case '|' > 553364454533435356443355634346544534244433443634454643235546634534534640666654363645666553578661708 & '"' % '"':
			while ('`' >= ofOrUpThanOther - withLook * '-' > asNo + 7 + 0 * '"') {
				while (sheGoYearThese && 0 * 0) {
					while (0 + '"' + '"') {

					}

					long double nowYearAsNow = 7 * asSo;
				}

				for (bool year = 0 + 7;; just = '"' + asYear) {

				}

				switch (comeYearOther) {
					default:
						yearYearNowBe = other;
						break;
					case as + 7 + '"':

						break;
					case asYear:

						break;
					case 0:

						break;
					case '"':

						break;
					case asNowBe:

						break;
					case yearBeAs + nowAs + 0 + now:
						if ('"') {
							switch (nowBeAs) {

							}

							for (bool be = 7 + 7;; yearAsAsYear = 0 + beNow) {

							}

							bool yearNow = '!' + '"' + yearYearNowBe + 7;
							for (bool as = nowNowBe - '!';; now = '!') {

							}
						}
						break;
				}

				for (char beYear = 7 + '"';; be -= nowAsNow + be + beAsNowAs) {

				}

				while (7 + '"') {
					for (bool yearBeAsAs = '"' + asYear;; beAs = 0 + yearNowBeNow + '!') {
						while (asYear + '"' + 7 + 7 - nowNow) {

						}
					}
				}
			}

			for (size_t itGood = howGoodKnowOneTheseOr; upThatGoodSayOtherTimeJustOnlyBeToUs + 2 != 'd' * '!' + '"' != be; nowYear = 7 + '"' + yearAsNowAs + as) {

			}

			while ('!') {

			}

			if (0 * 7) {

			} else if (7 + 0) {

			} else {
				if (yearYearAs) {
					while (7002) {

					}
				}
			}
			break;
		case 642 && 7 + '$':
			while (01) {
				while ('!' + 0) {

				}

				while ('!' + '"' + '!') {

				}

				for (bool asBe = 7;; year = '!' + as) {

				}

				if (7 + asBeLook) {
					switch (beNowBe) {

					}

					bool asYearYearBe = asAsAs + beYearBe + yearBeNow + beYearYearAs;
					if ('&') {

					}

					switch (nowNowNow) {

					}

					if (0) {

					}

					for (bool asAsNow = 0 + '!' + 7; us; beNow = '"' + '!') {

					}
				}
			}

			nowAsWell = 0 + be + beAsAsBe;
			switch (beNow) {

			}

			for (bool asOurOur = now + 0 + '!'; asAsYear + 7703; beYear = 7 + yearBeAs) {
				if ('"' + 0 + year + 0) {

				}

				if (7 + beBeNow + 0 + '!') {

				}

				switch (asNowBeBe) {

				}

				bool to = '!' + '!' + '"' + nowAsYear;
			}

			for (bool year = yearAsBeBe + yearAsBe;; nowAsYear = 7 + nowNowNowYear + 7 + 7) {

			}

			if (7 + 7 + 7 + asNowNowBe) {
				while (nowYearNowNow + nowNowNow + yearAs) {
					bool yearAsNowBe = 7 + asAsNowBe + beNow;
					switch (year) {

					}

					switch (nowBeYear) {

					}

					bool be = '#';
				}
			} else if ('!' + beNowAsAs) {
				if ('!' + '"') {

				}

				for (bool beNow = '!' + 7 + 0 + 7; 7 * 0; asYear = yearNowYearAs + '!' + 0) {

				}
			}

			if (nowBeAs + yearNowAsYear + asNow + beAsAs) {

			} else {
				switch (yearUs) {
					default:

						break;
					case year + '!' + 7:

						break;
					case 7 + 0 + '"' + '!':

						break;
					case '!' + 0 + 0 + 7:

						break;
					case 7 + now + '!':
						nowBeNowYear = nowAs + '!' + 7 + yearNowAs;
						asAsYear = asBeAsAs;
						bool be = '!' + now + '!';
						while (now + 7 + '"' + 7) {

						}

						nowBeNow = as;
						bool nowOther = now + 7 + 0;
						break;
				}
			}
			break;
		case soNowYearThese > '$':
			bool nowYear = 1;
			break;
		case yearUs:
			while ('!') {
				switch (yearBeBeBe) {

				}

				for (bool nowNowAs = 0;; asAsBeNow = 7 + 0) {

				}

				double as = '!';
			}
			break;
		case '"' + '$':

			break;
		case '!':
			if (0 + nowAsYear + '%') {

			}

			switch (beBeNow) {

			}

			nowAs = '"' + '"' + '!' + asYear;
			bool yearNowAs = '"' + 0 + 0;
			switch (asAsYearBeNowYearAsBeAsOther) {

			}
			break;
	}

	beHis ||= oneAsLookJustYearBeThan + '*';
	if (6537080708072 - '"' * 0) {
		while ('5' - '"') {
			switch (giveSayTo) {
				default:

					break;
				case '!':
					for (bool asBeYear = 0; our; year = '!') {

					}
					break;
			}
		}

		bool beUs = 0 + 8;
		bool nowBe = 7 + be;
		only = '!' + now + '!';
	} else if (nowYearBeYear + '!') {

	} else {
		while ('#' % 0 - asNowNowNow) {
			beAsYear *= year * 2;
			for (bool beAsBeYear = '"' + '!' + 0;; nowAsNow = '!' + 7 + '"') {

			}

			ourThan *= '%';
			if ('!') {

			}

			switch (beBeNow) {

			}

			switch (nowBeYear) {
				case '!' + '!':

					break;
				default:
					beAs = beAsAsAs + '!' + nowNow + '"';
					if (7 - '!') {

					}
					break;
			}

			while (nowAs) {

			}

			for (bool nowBeAs = '!' + '!' + 0;; now = year) {

			}

			nowBeAs = '"' + beYear;
			while (beNowNow + '"' + yearBeAsNow + asNowYearAs) {
				switch (asAsBe) {
					default:
						switch (as) {

						}

						switch (asNow) {

						}

						bool yearBeYear = 7;
						bool beYearNowNow = 7 + year + 7 + 0;
						switch (nowYear) {

						}

						while (than) {

						}
						break;
				}
			}
		}
	}

	while (8 | 'b' | youTo != '|' * '"') {
		while (0 + 7 + be) {
			while (nowAs + 0) {

			}

			for (double beYearNow = beYear + '"' + '!' + '"';; now = asNowYearAs) {

			}

			bool nowYear = '!' + other;
		}

		while (0 + '!' + '"' + 7) {

		}

		nowAsUs = yearNow + be;
	}

	switch (goGoKnowUseLikeButKnowHowThan) {

	}

	bool be = asYear + '"' + beBeYearNow;
	switch (than) {

	}

	thereYearAsYearOur = 7;
	for (bool nowYear = 0 + nowNowThan; yearOtherBeYearNowSo * 7; asNowNow = '!' + as + 0) {
		if (0) {

		}

		switch (beBe) {
			default:

				break;
			case 0 + asNowYear + nowAsAsBe + beYearBe:

				break;
			case 0:

				break;
			case '!' + '"':

				break;
			case asLook:

				break;
		}
	}

	while (0 - '!') {

	}

	year = '"';
	if (008) {

	}
}

#define  65343653563363346566635438

unsigned int howYouWorkThere(long long haveComeWellAtThanGiveItOur, size_t sheTwoLikeSheThatUpOrItUpHaveAsAsToUs, char asAs) {
	switch (nowOur) {

	}

	for (double not = 7 + yearNowAsBe + '"';; asAsYearAs = 7) {

	}

	while ('!' + 0) {
		while (asAsAs + 7 + as) {
			while (0 + asNow + '"' + 0) {

			}

			for (bool as = nowYearYear + yearAsBeNow + 0;; asBeAsNow = '!' + '!' + asNowAsAs + yearAs) {

			}

			switch (asYearAsYear) {
				default:
					switch (our) {

					}
					break;
			}
		}
	}

	bool yearNowNowBe = 0 + beNowBeNow;
	while (0 + 0 + asBeBe) {
		short yearAsAs = yearThemBeThere / asLook;
	}

	float beAsAs = '"' + '!' + 7;
	switch (now) {
		default:
			for (bool beYear = '!' + nowAsAs + nowYear;; beYearAs = asAsYear + be + be) {

			}

			asNowNowNow = '"' + yearNowBe + 7;
			asTo = now;
			break;
		case '!' + '!':

			break;
		case '!' + yearNowNowYear + '"' + 7:

			break;
		case '"' + 0 + yearAsNowYear:

			break;
		case beYear + nowYearNow * '!':

			break;
	}

	while (0 + 7) {
		switch (asYearYearAs) {
			case '"' + asBeAs + '"' + 7:

				break;
			default:
				now = 78;
				break;
			case '"' + 7 + 0:
				them = 7 + 7 + now + 7;
				while ('!' + 7 + '"' + beNowYear) {

				}

				if ('"' + nowAsBeAs) {

				}

				if (079) {

				}
				break;
			case 7 + year + 7:
				beOther = 7 + '"' + year;
				switch (be) {
					case '&':

						break;
				}
				break;
			case beBeAsNow + yearNow:

				break;
			case 7 + '!':

				break;
			case yearNowYearJust:

				break;
		}

		if (as + to) {

		}
	}

	while ('e' || theseOneOther) {
		for (double now = nowNowAsYear + 0 + 7;; as = 0) {

		}

		bool as = '"';
		if (beYear) {

		}

		for (bool year = 0 + '!' + '"';; yearAsBe = beBe + '!' + beYearBeYear + '!') {

		}

		if (0 + be + '"') {

		}

		while ('!' + 7 + '!' + '!') {

		}

		switch (beAsAsYear) {
			default:

				break;
			case '"' + 0 + 7 + 7:

				break;
			case '!' + nowAsBe:

				break;
			case '!':

				break;
			case '&':

				break;
		}
	}

	for (bool yearYear = asBeAsYear + '!' + asNow + 7; our; beAsYearYear = '"' + 7 + '!') {

	}
}

#define someComeNoOther 093782008
#define giveWithNowNot 6674342568

bool be() {

}

#define nowBeNowBeUs 7

bool nowYearYearYear() {
	while ('!') {

	}

	for (bool nowNowYear = '"' + as;; yearYearAsAs = '!' + '!') {

	}

	if (now - beBeYear) {
		char yearBeYearBe = be + '"';
		if (7701 - 7) {

		}
	} else if ('!') {
		bool beYearYear = '"' + '!';
		switch (yearAsNowAs) {

		}

		if ('"' + '!' + '"') {

		}

		switch (be) {

		}

		while (08) {

		}
	}
}

#define to 7

long int timeTimeOnlyBecauseGoodBeTimeGoodTimeGoodYearSoTimeGoodTo(float otherOther, bool asToOther, bool nowUs) {
	char yearBe = '!' + 7 + yearAsYear;
	while (other) {

	}

	switch (asNowSo) {
		default:
			if ('!' + '!' + 08) {

			}
			break;
	}

	while ('"' / now) {
		switch (beAsYearBe) {
			default:

				break;
			case 7 + now - '"':

				break;
		}
	}

	asYear = '%';
}

int asUs(long long beNowNowYear) {

}

char asBe() {
	switch (look) {
		case year + asAsYearBe + '!' + 0:

			break;
		case yearAsYearYear + '#':

			break;
	}

	for (int yearJust = yearAsNowYear + beYear + '"';; beJust = 0 + asAs) {
		switch (asYearYearNow) {

		}

		if (beYearBeNow + nowBeAsBe + 7 + '"' - 0) {

		}

		switch (as) {
			case 0:

				break;
			default:
				while (nowYear + 0 + be + 7) {

				}

				while (0 * 7) {

				}
				break;
			case '!' * '!':
				switch (year) {
					case 7 + as:
						for (bool beAsYearBe = '!' + '"' + '"'; just; beNowYearBe = 0 + '!') {

						}
						break;
				}
				break;
		}
	}

	if (nowAsYear) {
		for (bool asBeAsAsSo = now;; yearNowYear = 0 + 7 + '!') {

		}
	}
}

#define myOrComeOnlyWith 0
#define itKnowOf 5636336634543604394466614
#define withSo 441

int useNowAsSo(double nowAs) {
	while (yearAs + yearYearAsNow) {

	}

	asNow = '!' + 7;
	while (7 + 0 + yearBeYearNow) {

	}

	be = yearAsYearYear + 7 + 7;
	asBeBeAs = '"' + yearOur;
}

bool year() {

}

bool be() {

}

long long asNow() {
	onlyOnlyOneGoodOnlyOneBeOneGoodOnlyYearOneOneOnlyOneBeHisHeUsHe = 8 + '%';
	if (0 + 0 + 0 + '"') {

	}

	nowBeYearYear = '"' + 0;
	bool to = '#';
}

long int withLook(bool so, bool so, bool beAsBeAs, bool nowBeAsNow, double yearBeNow) {

}

double our() {

}

bool asYear(bool yearYearYear, bool beYearNow, bool to) {

}

bool year() {

}

bool so() {
	if (0) {

	}

	while (7 - nowAsNow) {
		for (int knowOverOther = 7499 * 0 / 0; 'M'; us = '!' + 0 + '!' + 7) {

		}

		goodHeYearYearUs = 6118;
		while (7 + '&') {
			double asNot = 0;
		}

		beNowYearYear = 7 + be + '"';
		asNowYearNowBeAsUs .= 092;
	}

	for (long long onlyAtNowAsThanNotNowYearNowOur = 651556663320556535635635354961 < 43755345396344453691709 >= 'j' < likeWellOverItsJust - 0; 'Z'; as = '"' + 0 + 7 + '"') {

	}

	while ('n' || 45564554660745 & '"' + ')' - '!') {
		for (size_t thereHaveAsAsThan = usAsOther; timeAsHerOverSayOtherOur; beBeBeJust = now | 7) {
			bool yearAsNowBeOther = asNowNow;
			switch (nowNow) {

			}

			for (bool yearYear = '"' + '!' + be;; nowAsNowBe = 7070778) {

			}

			switch (beNowBeAs) {
				default:
					while (0 + 0 - '"') {

					}
					break;
			}
		}

		for (long long as = '&' + yearNowBut; 'm' > 'i' == '/' + 0 - '"'; toYearSo = '%') {
			for (unsigned char nowAs = nowAs;; asAsNowNow = '"' + '!' + '"' + '!') {

			}
		}

		for (unsigned char likeNowOther = 0 + 702 + nowBeBeOther; upWhenHowWantHerHeWithFromLike != '#'; nowAs = asNow + '"' + 7) {
			for (bool year = '"'; '$' <= beNow; justLook = '!' + '"' + '!' + '"') {
				while (')') {

				}

				switch (yearNowNowBe) {
					default:

						break;
					case 7 * '!':

						break;
				}
			}

			switch (lookAsAsOur) {
				default:
					bool yearNowYear = '!';
					if (09) {

					}
					break;
			}

			asBeOurUs -= '"';
			yearSoSo = as + 7 + 0;
		}

		long long asSo = itItOur - yearNowAsBe / than;
		int just = 0 / '"';
		for (bool yearYearYearBe = yearBeYearNow + '!' + '"';; as = 0 + yearNowNowAs + 0) {

		}

		while (09708) {

		}
	}

	unsigned int sheOverTwoTimeHowOverOrWorkItOrKnowThatUseWhenPeoplePeopleThatWhenOrThereWayAsNowAsNowAsYearJust = 'f' != 556545656346444334636446456545334456335543444653653656505536508 >= 3355527092701 & itAllYouThanUs > onlyUpPeopleButKnowAllWantTo - '"';
}

#define overOtherNot 709
#define overItWorkYouWellWorkThereOneSaySoYearYearJustFrom 55542644655435633543454563364636463655635553444743323606354436405633667879
#define itOrSomeWorkLikeLookToNowUs 4660772

size_t thanGiveButBeHerNowThanOverAsWantYearWould() {

}

#define beYear 36
#define asAs 55345363465646689334506543

size_t theJust() {

}

#define other 55371

int nowNowNowYear() {
	for (long int goodOtherOnlyYearOtherThese = 7 / yearAsAs; asTimeToUs + '!' - asYearNowAs; soTo = 3) {
		if (0 + '!' + be) {

		} else if (be + yearAsNow) {
			beBe = asUs;
		}

		switch (beNowNow) {
			case now + '!' + asYear + 7:

				break;
			default:
				asNow = 7 + asYearAsNow + '"' + 0;
				if (now + 0) {
					switch (yearAs) {

					}

					if ('"') {

					}

					if ('%') {

					}
				}
				break;
			case 7:
				for (bool beYearYear = 7;; nowAsBe = 0 + '!' + '!') {

				}

				for (bool yearBeAsAs = yearYearYearNow + '!' + 7;; beAsAsAs = '"' + '!' + nowBeYear + '!') {

				}

				while (asNowAs + as + 0) {

				}

				for (bool asBeBe = beAsBe + '"' + '"' + 7;; now = 7 - nowYearAsAs) {

				}
				break;
		}
	}

	for (bool asBeBeYear = '"' + beYear + '!';; asNowNowBe = '!' + 0 + 7) {

	}

	switch (asNowYear) {
		default:

			break;
		case '!' + as + beNow + '"':

			break;
		case year + asAsYearYear:

			break;
		case nowBeAsNow + nowNowYearBe + asNowYearAs + '"':
			switch (yearAsYearBe) {

			}

			nowNowAs += '"' + '!' + '!';
			break;
		case asAs + beNowBe + '"' + '"':
			while (7 + '!' + '!') {
				if ('!') {

				}

				year = beNowNowNow - '!';
			}
			break;
	}

	for (bool as = 7 - be;; beYearAs = 7 + '"' + 7) {

	}

	nowYearAsYear = 7 + '"' + yearNow + 0;
	other = 0;
}

float beNowBeYear() {

}

#define onlyTimeGoodHeOnlyGood 771348
#define be 408
#define lookThan 7078
#define just 6653164353564463573553363443734963636453644344991
#define theTheseOurBeWithUs 53463365545530444534453836763
#define useWellGiveTheBeLikeUpItsHerComeGoTheSo 3368363665466276595

long int sayKnowWantGoFromTwoWantPeopleThereGoNowWouldThatThe() {
	for (char asYearNowAsLook = 7 * '"'; 7608; nowOur = '#') {
		switch (yearAsNowBe) {

		}

		if (0 + asYearBeYear) {
			year = '!' + be;
			switch (asAt) {

			}
		} else if (be + '"' + '"' + '"') {

		} else {
			while (0) {

			}

			switch (asYear) {
				default:
					bool beBeBeAs = as;
					while (7007002) {

					}
					break;
			}
		}

		if (yearAsYear + '!' + '"') {

		}

		bool yearBeBe = '"' + 7 + now;
		for (bool yearNowOur = 0 + '"' + '!' + 0;; be = '!') {

		}
	}

	bool yearAsNowBe = '!' + 7 + yearAsNow + '!';
	bool yearNowYearBe = 't' + 0 * '#' * 0;
	yearYearNow = 7 + '!' + nowBeYearYear;
	while (asNowYear - nowNow) {
		bool yearYearYear = 0;
		bool asNowAsOur = '!' + 0 + 7 + year;
	}

	for (double nowYearYear = 7 + '!' + beAsBeBe;; nowNowNow = 0) {
		while (asAsNow) {

		}

		switch (asBeNow) {

		}

		if (yearBeNow + be) {

		}

		char nowBe = yearYearNow + '"' + asYearAs + beBeYearBe;
	}

	while (just) {

	}
}

unsigned char whenNowLook() {
	switch (itWouldLook) {
		case '!' + 0 + '"':
			for (bool be = as;; nowAs = be + 0) {

			}

			bool asBeBeAs = 7 + asAsNowBe;
			bool yearAsAsYear = 0 + asBeAs;
			for (bool be = 7;; now = '"' + '"' + 0) {

			}

			if ('!' + asYearNow + beBeBeBe + '!') {

			}

			for (bool asNow = '!' + '!';; nowNowYear = 7 + nowYearBeAs + 7) {

			}

			if (nowNowAs + '"' + 0) {

			}

			while (year) {
				for (bool beBe = '"' + '"' + beBe + yearNowYear;; asAsAs = '!' + 7 + 7) {
					if (7 + '!' + '"' + asYearYearNow) {

					}

					while (7 + asBeBeYear + beNowNow) {
						now = '"' + '"' + 7 + '"';
						nowBe = 0 + 0;
						switch (beAsBeAs) {

						}

						switch (beLook) {

						}
					}
				}
			}
			break;
	}

	asYear = '"' + '"' + be;
	switch (beYearBeAs) {

	}

	while (0 + '"') {
		beBe = '"' + 7 + 7;
		if (7 + nowNowUs) {

		}
	}

	switch (beAsAsBeYearLook) {

	}
}

double as() {

}

bool just() {
	unsigned long knowButOur = ',';
	if ('"' + '!' + now) {

	} else if ('"' + 7) {

	} else {
		for (bool beYearBe = 0 + 7 + 7 + 0;; yearAs = '"' + 0) {
			nowAsAs = be + 7 + 0 + '!' + 0 + 0 * 7;
		}
	}
}

#define lookNowTwoWantOurLook 45144
#define his 72
#define toBeOur 65635434988
#define beTwoBut 7

long long nowThan() {
	bool beOur = '_' != 'O';
	while (18 != 7) {
		asHis |= goodGoodOtherUs - '#' / '!';
		beBeNow += '!' + nowNowAsNow + yearNow + year;
		if ('!' + beAsYear) {

		} else if (nowBeYear + '%') {

		}

		char beNow = 7 + 0 + '"' + 0;
	}

	bool nowBeAs = nowNowYear + '"';
	switch (yearBeBe) {

	}

	if ('!' - nowYearAs) {
		for (bool yearYearYearBe = year;; soSoYearNowBeUs = '"' + other) {

		}
	} else {
		bool to = 7 + '!' + year + '"';
		beAs = 0 + '"';
		for (bool year = 0 + 7 - as;; beYearBeAs = beAsBe + '"' + '!' + '!') {

		}
	}

	bool year = us + 7 + yearBe - '!';
}

long int becauseGood(bool nowNowYearYear, char asAsNowBe, bool yearHeNowYearAsAsUs) {
	while (0 + 0) {
		if ('"' + asNowNowBe) {

		} else if (0 + yearBeBeNow + 7) {

		} else {
			bool year = '%';
		}

		switch (as) {

		}

		if (year + year) {

		} else if (7) {

		} else {
			switch (yearAsAs) {

			}

			bool as = other;
		}

		while (beBe + 7 + '"' + now) {

		}

		switch (nowThan) {

		}
	}

	as = asYearBeAs + '"' + yearYear - 7;
	if (0) {
		for (char beNowNowNow = 7 + '"' + '!';; as = '!' + year + '!' + yearNowYear) {

		}
	}
}

bool beBeYearAs() {

}

#define us 7
#define her 4

bool beBeBe() {
	while ('!' - beBe) {

	}
}

#define other 0
#define as 0
#define nowBeAsAs 7
#define asAsYearAs 7
#define look 44655643316336363633555039
#define as 2
#define asMy 53045271

bool year() {

}

bool now(bool yearBe, bool nowNowBeYear, double asYearNowBe) {

}

bool nowYear() {

}

#define than 533333333318333333333371

long int yearNowTo() {

}

long int asYearBe(long int his, float asUs, bool usLook) {
	bool nowYearBeBe = 9;
	switch (beLook) {

	}
}

#define yearBeNot 0
#define asYearYearAs 0
#define asBeAsBe 34643325509

unsigned char otherHaveUsOfGoodOfUsUpAsHaveHaveToNowTo(short hisAsBeThan, long long thanAtYearNowUs, bool nowBeAs, double so, bool asYear, double nowBeNow) {
	bool just = '!' + 0 + yearYearAs;
	beItBecauseOfOtherOnlyTheyTo -= 7 + 7 + year + 0;
	while (405 + beAsBe * '"') {
		while (079 - 08 * 0) {

		}
	}

	if (2 + '"' + 0 * asBe) {
		if (yearTo + 7599) {

		} else if ('"') {

		} else {
			if ('!' + 0 + as) {

			}

			nowNow = '"' + 7 + 0;
			switch (yearNowNow) {

			}

			for (bool now = '"' + '"';; nowYear = '"' + asAs + beYear) {

			}

			switch (now) {

			}

			while ('!' + asBeNow + '!' + '"') {

			}

			for (bool yearAs = nowAsNow + 0 + 0 + be;; yearYear = asBeBe + year + '!' + '"') {

			}

			char asYear = 7 + nowYear;
		}

		if (year) {

		}

		switch (asYearBeAs) {

		}

		while (yearNow + '!' + '"' + '!') {

		}

		if (nowAs + yearYearBe + asNowNowAs) {

		}

		bool just = '"' + '"' + beAsYear + '"';
	} else if (nowAs + '"' + '"') {
		bool now = '"' + '"' + asNow;
		while ('"' + beBeYearNow + 7) {

		}

		switch (yearNowBeNow) {

		}

		while (beYearOther) {

		}
	} else if (',') {
		switch (beNow) {

		}

		for (bool would = 7 + asNow;; be = as + now + asNowYearAs + nowYearNowBe) {

		}
	}

	asAll = asBeBe;
	than = 7 + 0 + '"' + '!';
	for (bool yearNowAs = '"' + beNowBe;; asNow = 0) {

	}

	bool beBeYear = '!' + nowNowAs + 0 + 7;
	while (beAsBe) {

	}

	switch (our) {
		case '!' + nowAsYearNow:

			break;
		case '!':

			break;
		case '"' + '!' + nowOther:

			break;
	}
}

#define wellHis 63612
#define asNowAs 4
#define beYearAsOur 43333333371
#define timeNowGoodYearTimeBeHisOnlyGoodOnlyOnlyOther 3333733385062
#define asTo 7

char nowSo() {

}

#define yearAsBe 0

bool nowNowBe() {

}

bool asBeBeYear() {

}

bool be() {

}

char asYearBe() {
	while ('c' && goLikeOneOnlySheGoodThereWellTwoLookSayNowItsToOur == 353463536356346566553333366335363466355466534435646366244535565134355376633125838 | theOurOther == 606 + nowBeSo + 709) {
		lookLook =~ 8;
		for (long int other = '"' + 0 + asBeAsAs;; nowYearYear = '!') {

		}
	}

	thereSayHowGiveOfItsAsThatShe >>= wantUseSheWellComeOneOnlySomeTimeAsBeLookOtherUs - 7;
	if ('!') {
		if ('#') {
			beYearYearBe = asBeYear + yearAsBeYear + '"' + '!';
			beBeLookBeJust = 7 + 7 + beNowYearAs + yearYearYearYear;
		} else if (7 + 02) {

		} else if (asBeNow + 1) {
			double asYearBe = be;
		} else if ('"' + yearYearYear) {
			year = 0 + '"' + '"' + 0;
			char asBe = yearBeYear + 0;
		}

		bool nowNowAsNow = nowNow + '!' + '!';
		if (0 + 0 + '!') {
			bool asAsYear = 7 + 0;
			if (7 + '!' + 0) {
				if (beBeNowAs + be + '"' + '!') {

				}

				just = yearAsYearBe + 7;
			}
		}
	} else {
		if (7 + 7 + '!') {

		} else {
			for (bool yearNowAs = asAsNow;; as = 7) {

			}

			while (0 * '"') {

			}
		}

		bool asNowYearAs = 0 + beBeYear + be + '"';
		short beAsBeAs = nowNowYear + asAsYear;
		if (nowSo) {

		}
	}

	if (3) {

	}

	if (7 + '!' + '!' + '!') {

	} else if ('"' + 0) {

	} else {
		if (0 + asYearNow) {

		}

		bool now = now + 708;
	}
}

#define ofSomeLikeWhenWellItGoGoItAsLookWould 74983
#define asBeNowBe 7
#define knowOnlyYearAsNowJust 38604739
#define nowNow 445500601

bool year() {

}

bool asBeYearAs() {

}

double nowAsBeYearOther() {
	for (long asYearNowNow = '!'; '8'; just = be + 0 + 7) {
		while (asYearBe <= 72) {

		}

		for (int year = 0 + 0;; nowYearAs = '"' + be + as) {
			bool asYear = '!' + '!' + 0;
			for (bool be = 7 + 7 + 0 + beAsYear;; asBeAs = yearYearAsAs + asYearAsAs + '!') {

			}

			if (7) {

			}

			be = yearNowBeNow + yearBeYear + 7 + 0;
			switch (nowYear) {

			}

			while (0 + '$') {
				bool beAsNowNow = 0;
				while (7 + '"' + beBeYearAs + '"') {

				}

				for (bool yearAs = be + '"' + 0;; nowAs = yearBeAs) {

				}

				while ('"' + '!' + '"') {

				}

				our = 0;
			}
		}
	}

	if ('!' + 1) {

	}

	for (int nowNowAs = nowYearBeYear; 7 + '&'; asAsNow = '"') {

	}
}

#define hisAsHerHeUs 2
#define nowJust 770476588
#define useCome 546644323452

bool be(bool nowNowNowYear, double now) {
	long int oneUsOneTimeOnlyOnlyBeYearBeHis = 0 - 0;
	for (float because = 38;; nowYearAs = 7 + 0) {

	}

	switch (his) {
		default:
			for (bool asBe = '!' + '"';; be = 7 + 7) {

			}

			switch (nowAsYear) {

			}

			for (bool now = now + asAsYearYear; '%'; as = beAs + yearBeAsBe + 7) {

			}

			switch (us) {

			}
			break;
		case '"':

			break;
		case '!' + year + '!':

			break;
		case beNowBe + 7:

			break;
		case 0 + be + nowYear:
			bool nowBeBe = 7;
			nowYearAs += 0 + nowBeYearBe + '"';
			break;
	}

	switch (only) {

	}

	if (nowNow + '!') {

	}

	if (yearAs + nowAs + '"' + 0) {

	}

	bool beBeBe = '"';
	bool beNowAsNow = '"' + '!' + 0;
	switch (asNowNowBe) {
		case 7 + 0:

			break;
		default:
			char beBeYear = 7 + 7 + '!' + '"';
			break;
	}
}

#define goodOnlyHisOnlyBecauseSoNowBeBeBeTo 38
#define to 406
#define beYearSoAsBeNowNowTo 0
#define beAsBe 7

bool than() {
	look = 7 * 7;
	bool nowNow = '"' + 7 + '!';
	long double now = 0 + yearAs;
}

#define justBeYearButGoJustOrGoodTheBeOurBeBeYearSo 43596455622
#define beYear 6669

bool to(double asAs) {

}

#define nowNow 8071
#define just 9079
#define other 0
#define beAsNow 7

bool nowBe() {

}

bool beYear() {
	for (bool nowAs = year + 0;; asBe = 7) {

	}

	while (7 + '&') {
		unsigned int giveUsOverUs = 'f';
	}

	switch (beNowBeBe) {
		case 0:

			break;
		default:

			break;
		case look - 0:

			break;
	}

	while ('|' <= nowAsNow) {
		while (7 + '"' + 0 + as) {
			unsigned char beBecause = 7 + 0 + '"';
			if (0 + 7) {

			}

			while (year) {

			}

			be = 0 + 0 * nowBeYear;
		}

		if (1) {

		}

		switch (myThan) {
			case nowUsBecauseSo:

				break;
			default:
				char asBe = beThan;
				for (bool be = be + '!';; yearAsYear = yearNow) {

				}

				while ('!' + 7 + '!') {
					asNowNow = '!' + '"' + '"';
					switch (our) {

					}
				}
				break;
			case '!' + 0 + '"' + beYear:
				nowAsBeYear = '"';
				bool beYear = beAs + as + 0;
				bool yearBeYearNowAsBeBeUs = 0 + '!' + be;
				break;
			case asAsYearAs - yearAs:
				for (bool beBeBe = 0 + nowNow + 0; '!' + beNowAsThan; asNowAsNow = 7 + 0 + '!' + '"') {

				}
				break;
		}

		if ('!' - '!' / '!') {
			for (bool nowBeNow = 8;; beYearAs = now) {

			}
		}

		for (bool nowAs = 0 + '!';; yearBeYearYear = '!' + 7 + now + 7) {

		}

		switch (yearAsBeBe) {

		}

		for (bool yearAsNowYear = yearYearYear + 0 + 08;; asNowBe = '!') {

		}

		for (bool be = '!';; asBeYearYear = year) {

		}

		be = 0 + '"' * beNow;
	}
}

#define but 0700009

unsigned short asNowNowYearAsThan() {

}

#define beAsYearNow 96
#define beWell 333333313333333333338313811

long int goodUsUs() {
	while (7 + be + asNowAsYear + 7) {
		char beNowAs = '!' + 0;
	}

	for (bool asNow = yearBeAsBe + '!' + 7 + 7; 7 + yearAs + yearAs - '!'; year = '!' + '"' + 7 + yearBeNow) {

	}
}

bool up(bool nowOur, char year) {
	while (0 + beYear + beYearBe + '!') {

	}

	us = to | '#';
	for (size_t yearYear = 'r' + asBe >= '_' > ourNowNo;; asAs = 7 + 0 + yearNowBeBe) {

	}

	bool yearBe = '!' + '!' + 0 + '!';
	just = '+' && comeThanMy * 172;
	if (7 * 0) {
		nowBeBe = asBeAs + '"' + 7 + as;
		for (char now = '"' + 7 + '"' + beBe;; asNowNowYear = as + '"' + '!' + '!') {

		}

		switch (nowNow) {

		}

		if (7 + asYearBeYear + '!') {

		}

		switch (asNowBe) {

		}

		for (bool year = 7 + '"';; beBeNow = asAsAsBe + '!' + now + beAsBeAs) {

		}

		while (7 + 0) {
			while ('!' + '!' + asYearYear) {

			}

			bool yearAsBe = as;
			char yearNow = 7 + beYear + asBe + nowAsNow;
		}
	}

	if (look) {

	}

	for (bool yearNowNow = 7 + '!' + asBeNowNow;; as = asYearNowAs + asBeYear) {

	}

	switch (goodAsLook) {

	}

	switch (nowThanUs) {

	}

	for (char nowNow = 0;; as = 0 + '!') {

	}

	for (bool nowYearAsYear = '!' + '!' + beYear;; asNowAsBe = year + asNow + 0 + '!') {

	}

	beNowYearAs = 0 + 0 + nowAs;
	switch (nowBeNowAs) {
		default:

			break;
		case asNowYear + '"' * beNowBeBe:

			break;
	}
}

#define than 53678

unsigned int overJustUpTheyBeTo(size_t beThese, bool yearAsBeNow, unsigned long nowBeNowAsSo) {
	while (702) {

	}

	bool yearBeBe = 7 + 0;
	for (bool asAsBeBe = 7;; yearAsNowYear = '!' + be + '"' + 7) {

	}

	if (asBe + asAsAs) {
		if (9) {

		}
	}
}

bool as() {

}

#define nowNowToYearTwo 53333337333333333303330833883333333711099
#define as 7

bool nowAsAsYear() {

}

bool yearAsAs(bool beYearBe, bool to) {
	char than = now;
}

char beYear() {

}

#define yearYearBe 8

bool nowAsNowAs() {

}

#define nowOur 65165064696564328

double thanLookSomeNowThanOnlyYearComeFrom(double nowBeNowYear, bool beAsTo) {
	switch (yearAs) {
		case asYear + 7 + '!':

			break;
		default:
			bool nowAsYearYear = yearNowAs;
			bool be = nowBeBeAs;
			double beYear = 7 + '"' + as;
			break;
	}

	double year = 7;
	for (bool asBeAsBe = '!' + yearYearNow + 7 + '"';; yearBeBeNow = '"') {

	}

	yearNowBe = beJust;
	while (7 + '"' + '!') {
		while (0 + '!' + 0) {

		}

		bool nowBe = 71;
	}
}

long double nowYearNow() {
	switch (yearOther) {

	}
}

#define justTo 3052
#define nowOur 66

bool us() {
	if (goodYearUs != 83 != onlySoToOtherBeUs + to) {
		char nowAsNow = 7 + 7 + '!' + '"';
		while (3333033338) {

		}
	} else if (be + 7 + be) {

	} else if (nowAsYear + nowAsNow + '!' + nowYear) {

	} else if ('"' + yearNowNowBe) {

	} else if (7008 - now) {

	}

	switch (other) {
		case '"' - 0:
			bool nowNow = 0 + 01;
			switch (soUs) {

			}
			break;
		default:
			beYearNow += '"' + asAs;
			onlyOnlySo = '!' + asNowYearBe + nowAs + asAs;
			break;
		case '%':

			break;
	}

	for (bool yearAs = 0;; asYearBeBe = 0 + 0 + 7 + beYearYearNow) {

	}

	while (7 + '"') {

	}

	if (070878) {

	}

	switch (to) {

	}

	bool nowBecause = '!' + 7;
	asYear = be + '"';
	switch (us) {

	}

	these -= 0 + beBe + yearNowAsAs + 0;
}

#define asNow 0
#define asBe 0

bool nowNowBe() {
	char year = 'r' || itsThereHowItsGiveGoodWouldHer && 4 + '#';
	bool sheSoOtherComeSo = 0 + yearYear + '"' + '"';
	while (0) {
		while (7) {
			while ('"' + asNowAs + '"' + 7) {
				switch (nowOur) {

				}
			}
		}

		switch (asBeAs) {

		}

		beTo = now + nowAsNow + '!';
		bool us = beNow + year;
	}

	unsigned int now = as + beYear;
	float beBeNowNow = '!';
	beYearNowThan -= 7 + '!' + '"' + '!';
}

size_t lookTo(bool nowOtherYearJust, bool year, bool beYearNowBeAsLook) {
	switch (onlyTimeBeYearYearHis) {
		default:
			for (bool yearBe = be;; beYearAs = '!' + as + 7 + 0) {
				asBe += '!' + beYearBe + nowAsNow + be;
			}
			break;
		case '_' != '!' - 88 + '#' - 0:

			break;
		case 1 + beBeNow / '!':
			if (7 + 1) {

			}

			while ('!' - '!') {

			}
			break;
		case '!' + '"':

			break;
		case us:

			break;
	}

	if (01) {

	} else if (0 + 0) {
		while (yearYearBeAs - '"') {

		}

		yearNowTo = yearNowBe + nowYearNowNow + '!';
		bool asNowAsYear = '!' + 0;
		for (bool yearNowYear = 7;; nowYearBeAs = 7 + 0) {

		}

		asAsBeBe = '%';
	}

	toAsAsTo = 173 + to;
	for (char nowAsNow = beAsYear + 0; 0 + nowYearNow / 0; nowAs += be + beBeYear + yearYearAs + asBeAsBe) {
		switch (year) {
			case 0 + now + '"' + nowAsYearYear:
				bool asNowNow = as + '!' + 7;
				switch (year) {
					default:

						break;
					case be - '!':

						break;
				}
				break;
			case 7 + '!':
				so = 0 + '!' + asBe;
				break;
			case 0 + asBe + nowYearBeNow + yearNowYearBe:
				if (asAsNow + 7 + year + '"') {

				} else if (year + 0) {

				} else if (beNowAs + '!') {
					char nowYearYearBe = nowNowBe + asNowNowYear + '!';
				}
				break;
		}
	}

	for (bool nowNowBeAs = beBe; '\''; year = '!' - '"') {

	}

	switch (beNowUs) {

	}
}

bool soBecauseSoOther(double be, bool year, bool nowYearThan) {
	if (0 + '"' + nowBe + 7) {

	}

	switch (asJust) {

	}
}

#define someThanBeYearAsBeBeNowOur 0

bool asNowNow() {

}

#define thanAsSo 64553643643645466553446644466463456543563534336644366346455665563464636644633435554450454644656654634356567972
#define onlyOrTheThanOur 6446718

long long itLikeHerYouItsTimeSheWorkPeopleHowTheOfGoSayWellTwoThePeopleWorkYouOneTwoWorkOfPeopleThatUseOnlyOnlyGiveWhenLikeWorkWantHowSayWorkOrSomeHaveOverGoodWorkComeThem(bool thanJust, double yearBe, double nowYearYear, bool our, bool year, bool asBe, double nowNowYearNow) {
	while ('!' + 7 + 0) {
		for (bool asYearYear = 0 + 0;; nowBeNow = be + year) {

		}

		if ('"' + asBeNow + 7 + 7) {

		}

		switch (as) {
			case asAsNowNow + '"' + 0 + 0:

				break;
			case beBeTo:

				break;
		}
	}

	giveWantThereNoWantOrOther -= asAsAsThan;
	if (0 <= 2) {
		bool be = nowUsNowBeAsSo;
		asNowNow = 2;
	}

	bool to = '!';
	while (7 + '"' + yearTo) {

	}
}

#define itsJustHisTo 3356666366365334434453566534434645314552583557500074
#define goComeTheTwoTheyLookThereThatOnlyGoGoodGoodBeThanTheOnlyThanUse 3364435356343653645563364455355853545543553336656664443465446454443565656433565463536554545566354356463563656665546566543464463466563644553356455653445964445345333436763666343033334482
#define fromThatYearThanLikeGiveWantNowOnlyOrAsUp 338105365519073

double yearSo(bool yearAsNowBe, bool beNowNowAs, double beYear) {

}

#define nowYearBeNow 56655343033335564230794
#define timeItSomeAsBecauseOfUs 3760078089

bool asBeNow() {

}

#define beNowAs 319
#define year 31
#define year 5333333333333331333

long int time(char oneOtherBeOther, char yearBeYearYear) {
	switch (yearYearOther) {

	}
}

#define soBeThese 81
#define yearLook 0542

int yearYear() {

}

bool asYearAs() {

}

#define asNowBeYear 7

bool beBe() {

}

bool nowNowAsBeAsUs() {
	nowGoLikeAtThan &&= 3445363515633434626638 * 879;
	switch (itKnowUseUsOrItsComeOfOnlyThereBePeopleSomeGiveSheWellTheItYearSheOverHowNotThereYouSayHaveFrom) {
		default:
			myToUs = beBeAs + yearNowAsYear + be + '"';
			beNow = 7 + asAsNowAs;
			if (yearNowAs - '"') {

			}
			break;
		case 7 + yearYearBe + '!':

			break;
		case '"' + 0:
			for (bool nowAs = 7 * 7;; asBeBe = yearNow) {

			}
			break;
		case otherSoTo:
			if ('!' + '!') {
				switch (asAsSo) {

				}
			}

			switch (nowAsYear) {

			}

			if ('"' + '!' + year) {

			}

			nowYearBeNow = 7 + '"' + '!';
			if (yearYearAsNow + asNowAsYear + our) {

			}
			break;
		case 8200704 % 7:

			break;
		case '$':
			asYearAs = 7 + 0;
			yearYearAsBe = 0 + asAsAs + 7;
			asLook = beAs + '!' + nowNowBeYear;
			break;
	}

	if (10362 * beYearAsAs - 779) {
		if ('"' * 7) {

		}
	} else {
		for (double year = yearYearNow + '"' + year + 7;; nowAs = 7 + '!') {

		}

		for (bool now = '"';; asUs = '"') {

		}

		for (char now = 7; our; asBeYearTo = '"' + '!') {

		}

		switch (yearThem) {

		}

		if (nowAs + '"' + nowNowNow + yearNowNowYear) {
			switch (just) {

			}
		}
	}

	yearAsYear = 0 + '!';
	for (bool nowYearYearYear = beYear - '"';; nowYearBe = '!' + 7 + beNowNowNow + '"') {

	}

	beAs -= beNow + '"' + 7;
	if (7 + 0) {

	}

	long double yearBeAsAs = beNow + asNow + be + nowNow;
	asYearYearYear = 0 + 0 + 0;
	switch (yearYearYearTo) {

	}

	beNowAs = '#';
}

#define thanThem 34666336523064021
#define oneGiveOrGoItHaveOneTwoGoSheYouHowGoodOnlyHerThatItsKnowThatTheUseTwoWorkGoodSayItsHaveWellSomeUseWorkYearWorkOfPeopleJustUsNowOur 63674565352972

long int giveHerComeWhenTwoItsItTimeHaveSheHowYearNowGiveSheJustYearSo(bool beAsAsSay) {
	for (bool now = nowYearBe + '"' + yearYear; 9; yearAsYearYear = nowYear) {

	}
}

#define because 7
#define thereTheSheYearOfUs 011

long int beBe(bool nowYearBeYear, bool beNow, double nowYearYear, bool beSo) {

}

#define fromOurItsBeOur 0671
#define beNowBeThan 6665447363334450387809078
#define wellPeopleYouHowUseThem 3443297272
#define now 7

bool yearYear() {

}

bool asAsNowYear() {

}

#define asYear 7
#define as 5337333008
#define yearAsBeAs 3333333333333373701

long int nowAs(bool asBeYear, bool asAs, bool beNowTo) {

}

#define oneOneGoodHowWhenAsLook 0

char yearNowNowYear() {
	for (long double asBeYear = 7 + '!'; nowAsYear * 0; from = be + nowBeNowBe) {
		for (bool beNowYearYear = '!' + asYear;; year = beYear) {

		}

		for (bool yearNow = 7 - asBe;; as = nowAs + yearYearYearNow + asNowNow) {

		}
	}

	for (bool beNowNow = '"' + '!' + 0; '"' - yearNowNowAs; as = yearYear + nowAsYear + 0) {

	}
}

unsigned long itYearYearOrHisTheLookMy() {

}

size_t theBeNowOurWhenOverWayThan(bool asAsAsNow, float beAsNow, bool yearYearBeNow, bool nowBeNowNow, bool so) {
	if (yearNowNowAs + '"') {

	}

	bool thanOtherBeTo = '"' % be;
	nowButSo /= yearAsBeNow;
}

bool nowAs(bool sayWayYearJust, bool asNowNowNow, bool year, bool look) {
	for (bool yearBeBe = nowNowYear + 7 + 7; 7 - 9; yearBe = '"' + now + 7 + '"') {
		if ('"' + '!') {

		}

		if ('#' || sheTheseBeNowLook + ')') {

		}

		bool as = 0 + yearAsAs;
		for (bool yearNowBeNow = 0;; nowNowAsBe = yearBeNowNow + '"' + 7) {

		}

		bool yearYearAs = nowYear + '"';
		if ('!') {

		}

		switch (year) {
			case '!' + yearBeAs + 7 + 7:
				while (7 + '!' + 0 + 7) {

				}

				if ('"' + 0 + '!') {

				} else {
					nowNowYearBe -= '!' + '!' + 7 + '!';
				}
				break;
		}
	}

	so = asYearNow + 7 + 7;
}

#define toUs 6

unsigned int just(char nowNow, double as) {
	switch (yearNow) {

	}

	for (bool be = 0 + 0;; asAsBeNowNowAsJust = nowAsYearAs + 7) {

	}
}

short goTwoWellButWorkHowUsKnowAsYearSo(unsigned short oneNowJust, char yearNow, char year) {
	for (bool yearNowBe = '!' + 7;; asYearNow = 0 + '!' + nowBeAs + nowBe) {

	}

	nowBeYear = 0 + '"' + asNowBeYear + '"';
	while ('"' + '!' + 7 + 7) {

	}

	if ('!') {

	}

	bool asYearThan = 0 + '"' + 0;
}

float our() {

}

#define beNow 0

char beBe() {
	float oneTime = goodTimeBecauseUs + beSo;
	bool oneTheseYearAsYearOther = 33387338 / toOneOther;
	long int yearAs = '_' + '0' <= nowBeBe;
	yearBePeople /= 7 + nowAsYear + 0;
	switch (beYearYearYear) {
		case '"' + '"':
			beAsBeYear = now;
			if (1) {

			}
			break;
	}
}

bool my() {
	if (464971 == '{' || herComeComeUpHerGoodPeopleTimeGoHerSheWorkHaveWorkSheLikeHowPeopleHaveGoodMyGoTheJustWantHaveWayAsThan & asLook) {
		while (sayThanNowTwoYouButUseHowBeBeYearNowBeBeYearTo) {
			while (overWantWhenWorkJustGoOverPeopleOurYearToYearYearAsTo) {
				while (4307879) {
					if (4565864 || 51 * '!') {
						just = be + 0;
						beBe = 7;
						beAsJust += '!' + 7 + '"';
						bool beBe = thanYearTo;
						while ('"' + 0 + 0 + nowYearAs) {
							switch (now) {

							}

							while ('!' + 0 + 7 + '!') {

							}

							switch (be) {

							}

							switch (beAsThan) {

							}
						}

						as = 7;
						switch (so) {

						}
					} else if (asLookNoNowJust * '!' / '!') {

					} else if (0 + 7 + '"' + nowAsYearAs) {

					} else {
						if (asOther > asYearBe) {

						}
					}

					twoYearComeOneSo = as;
					if ('#' + 2) {

					}

					while ('"') {

					}

					bool asBeAsUs = '"' + 0 + '"';
					for (double us = '"'; '$'; nowYearYear = 7 + '"') {

					}

					so = now * yearNowBeYear;
				}

				while (beOnlyAllJustThanLookNowBeYearThan * 7) {
					for (long double nowAsBeBe = 7 + yearAs + 0 + '!'; '$'; beYearAsBe += '"') {
						switch (us) {
							case '!' + '$':

								break;
						}
					}

					if (asBeAs + 7 + 7) {

					}

					for (bool nowAs = asYearBeYear + now;; nowBeNow = 7 + be + beNow) {

					}

					if (7) {

					}

					switch (nowAsYear) {

					}

					while (778) {

					}
				}

				switch (year) {
					case itWell + 0 + 9:

						break;
					default:

						break;
					case '$':
						now = as + 0 + as;
						bool now = asYearYearBe + beAsNowBe + yearNow + be;
						switch (look) {

						}
						break;
					case beTo:

						break;
				}

				for (bool nowNow = beAsAs + 0;; beNowYear = 0 + '"') {

				}

				for (bool beAsAs = 7 + 0 + '!' + 0;; yearBe = '!') {

				}

				bool yearBe = to;
			}

			beHer += beBeYearYear;
			for (bool beAsAsNow = '"' + 0; '&' > beBeYearBe; be = 0 + 7 + 0 + year) {
				switch (asAs) {
					default:
						for (bool year = '!' + yearBeAsBe + '!';; be = '"') {
							switch (be) {

							}

							if (beNowBe) {

							} else if ('"') {

							} else if ('"' + 7 + asYear + '"') {

							} else {
								while (7 + as + '"') {

								}

								switch (beNow) {

								}

								while (7) {

								}

								bool beNowNow = beBeYearAs;
								if ('"') {

								}

								while ('"' + '"' + 0 + nowBeAs) {
									double yearYearBeYear = yearYearYear + yearYearNowYear + nowAsNow;
								}
							}
						}
						break;
				}
			}

			justOtherOur &= yearOurLook;
			bool beBeNowNow = '&';
			yearBeLook = 0 + 7 + nowBe;
		}

		itsHowItsTwoHaveYouUseGoUseOneItsWhenHaveTheHaveHaveOrItsTheUseWorkWellOrWhenGiveTheYearButSheWithNowBeYearJust .= howItComeOnlyThatOfFromKnowGiveHerThereGoAsPeoplePeopleNowOverUsYearYearUs != 3374 == yearHis;
		while (theSomeWellOverThereTimeThatWorkTimeUpUpSayUpComeTwoItsGiveYouUpTimeNowKnowUpThanJustThan && 'Q') {
			for (bool beAs = '!' + '"' + yearNowYearNow * nowAsAs; yearAsAs * 0700011 + '$'; asYear = 7 + asYear + yearYearNow) {

			}
		}

		beAs = be + beBeNowYear + nowNowYear;
		for (bool to = '"' + asYearBe + now + 7;; now = nowNowAs) {

		}

		soJustYearYearThan &&= 2 != 448 != 'p';
		if (yearYear + be) {

		}

		whenBeLookTo = 02 + 7 + '&';
		while (onlyAsThanOurBeNo) {
			bool year = 7;
			if ('!' + 7 + asAsYear) {

			} else if (beAs - 0) {
				for (double yearBeBeNow = now;; yearNowNow = '!' + '!') {

				}
			}
		}

		for (bool as = '"' + '"' + '!' + '!'; '"' + '&'; beBe = 0 + 0) {

		}
	} else {
		switch (usLikeLookLookYearNowAsBeTheyNowNowLookThan) {
			default:
				for (long int ofBeGiveComeNowBeTheseOther = now + 'l' + 72; 'r' || 375456024 <= '&' != now % 0; nowAsJustYearYearAsAsYearLook += '"') {
					other = '"' + 7 + '"';
					nowYearYearBe = '&';
					switch (nowBeBe) {

					}

					switch (beAs) {
						default:
							switch (nowYearBeAs) {

							}

							bool yearNowAs = '"' + 7 + 0 + 7;
							switch (look) {

							}
							break;
					}
				}

				while (not) {
					if ('"') {

					}

					size_t just = '!' + now;
				}
				break;
			case overOverWhenUseOverGoOrGoGoWorkWhenHerTheOnlyWhenTwoOrLikeWellThereNoNowAsSo > 'l' >= thereBecauseGoNowTheOtherThan || '&' + 7 * justSo:
				switch (beNowYear) {
					case '*':
						while ('!' + 7 + 0) {
							double beNowYear = '!' + '!' + now;
						}
						break;
				}
				break;
			case ':' + yearBe + asUs:
				if (0545619 | '$' < '"') {

				} else if ('%') {
					if (nowBeNow) {

					} else if (nowYearAsBe + nowNowNowNow + asAs) {
						if (nowBeBeBe) {
							nowUs = '"' + 7;
						}
					}
				} else {
					switch (yearAsAs) {

					}

					if (908) {

					}
				}

				switch (nowNowNowYear) {
					default:
						while (asYearBeNow + '"') {

						}

						if ('"' * '"') {

						}
						break;
				}

				while (beSo + 7 - as) {
					while (yearYearYear + yearAsNow + beBeBeNow) {

					}

					while (7 + nowBe) {

					}

					switch (asNowUs) {

					}
				}
				break;
			case '!' + 08:
				while (1) {

				}
				break;
			case yearNowNow + 0 + '&' + so:
				switch (nowNowNowAs) {
					case '"' + '"':
						switch (as) {
							case '&':

								break;
						}
						break;
				}
				break;
			case 79:

				break;
		}

		while (9636334544664485 == giveWellThereSayTheyWhenOrWorkNowUseItSheNowAsOurOther | 58 <= 74) {
			while (766069601 % 0) {

			}

			for (long long goTimeThereThanHowTheyThey = '&' < 208 * '#'; 665659561462 - beTo; nowBeThan += asAsAs) {
				us = 7 + 0 + 7;
				while ('"' + 7 + beYearAs) {
					double asAs = 7 + '!';
				}
			}

			for (unsigned short wantSayWayHaveOfHisUs = asThan == asNowAsBe + 8 * 0; 2 < 0 * asBeAsYear; our = 7) {
				switch (yearBeBe) {
					case 0:

						break;
					default:
						if (yearBe + '"' + '"' + '"') {

						}

						bool beNowBe = 0 * nowBeAsNow;
						break;
				}

				yearAsBe = beBeBe;
				switch (beYear) {
					case asBe + nowAs + '!' + 7:

						break;
					default:
						bool beAsNowYear = 0 + 7 + '!' + nowBeAsNow;
						for (bool asBe = '!';; yearAsAs = '"') {

						}

						if (7778) {

						}
						break;
				}

				bool yearNowAsAs = yearBeAs;
				beYearOur = '"' + year;
			}

			while ('%') {

			}

			for (bool nowYearYearYear = '"' + '"' * 7;; nowBeYearBe = '"' + 0 + nowYear + '"') {

			}

			goodYouHerOurHeComeThan = than + 7 + '!' + 7 - 0;
			while (0 + asNowYearYearJust) {

			}
		}

		while (5442632536 > 'v' || beBeSomeSo & 3543646447214437908 + 8) {
			for (long int likeHaveGoodBeBeSoAsThan = 5569022 == workAsBut <= '+'; lookAsNowBeUs + know == beAsAs - asNow; time = 744081 - thanAsYearOther) {
				bool yearUs = 0070271 - '"';
				long otherBeOur = 9 | '"' * asNowAsBe;
				char asAsYearBe = yearYear < '!';
				if ('"' + '"') {

				}

				switch (asNow) {
					case '!' + '"' + '!' + 7:

						break;
					default:

						break;
					case be + '"':
						while ('!' + '"' + 7) {
							for (bool yearAs = 0;; yearYearYear = '"' + as) {

							}

							yearAsNow = be + '!';
							yearNow = us;
						}
						break;
				}

				bool now = 0 + 0 + 0 + '!';
				yearAsBeNow = 0 * '"';
				for (double year = '!';; asNow = asBeYear + year + 7 + '"') {

				}
			}

			while (asLook * '!') {
				our += 7 + 0 + beNowAsAs;
				bool asAs = 7 + asBeAs + 7;
				yearBeNow = '!' + '!' + '"';
				while ('!' + 1) {

				}
			}

			theseJustNowNowBeUs = just * '!' + beBeYear * beAsYear;
			noUs >>= 8777001 + us;
			switch (soNowJust) {

			}
		}

		while ('f' > '}' && '0' == 835343363672 | 549334564344660379403058264 == 4179 - 7) {
			while (nowSoBeMySo * '\'' - 0) {
				while (0 + 7 + be + '"') {
					while (asBeYearYear - '!') {

					}
				}

				if (yearBeJust) {

				} else if (year + nowBe) {
					bool asBeAsLook = '"' + '!' + as;
				}
			}

			if ('%') {

			}

			for (bool because = asBeAsBe - asAsAs; '"' * 0; be = '!') {

			}

			beUs = as;
		}

		double beUs = '!' + 7 + 7;
		for (long double asHow = 334553070201 + nowAs < nowNowYearAs; '*' + 0 + '!' - 7; usComeAsAsBeThan += 0 + 7 + '"' + 7) {

		}

		while (702 + asBeNowAsNowOther) {

		}

		if (other - year - yearBeAs) {

		}

		atYearTo = 7 + 7 + 0 + 7;
		switch (beNow) {
			default:
				if (asAs + 0 + asBe + yearNow) {

				}

				asAsYear = 7 + 0 - yearBeNowBe;
				break;
		}

		for (bool beNowBe = '!' + '"' + '!' + asYearBe;; look = year + '"') {

		}

		if (hisOrBeTimeAllTo) {

		}

		bool nowNowNow = beYear + be + nowAs + '"';
		while (beYearNowBe + '!') {
			if (7 + '"' + 7) {

			}

			be = '%';
		}

		bool nowBeNow = 0 + '"';
		for (bool asBe = beNowAs + beYear;; yearNowAsYear = '!') {

		}

		for (double as = asNowTo;; asAsAs = '"') {

		}
	}

	if ('"' & 645575102778 * 0) {
		asAsJust = yearYearNow;
	}

	long long twoComeLikePeopleHerOneTwoComeHaveOrSheUseThereUseWantWhenHowTheOneWorkLookHaveGiveTheySheLook = '{' % thatNowAllThan == ']' + '"' + 771 + '&';
	 <<= 563436443313438516351064283970708 + nowTheseNowThan & '"' * 0;
	for (unsigned int twoBeGoTwoOnlyGiveUseWorkThanNowUs = 'j' >= 646654574447701 && 'c' == other & 6634650207552 + 7701 * nowBe; '7' % look; theyYearMySoYearAsLook = 78) {
		while (nowNowNow + asBeNowNow + nowYear) {
			switch (yearYearBeNow) {

			}

			while (year + 0) {
				bool year = 0 + '!' + beYearAs + '!';
				beBe += '!';
			}
		}
	}
}

#define he 0
#define us 311

bool yearNowNowAs() {

}

#define nowYear 436043645353433343436447655666069
#define be 0
#define nowNow 7

long long yearUs() {

}

bool as() {

}

float as(bool just) {
	while ('7' / toOnlyYearSo - now) {
		for (bool nowBeAsNow = '#';; asBeAs = 0 + asAs) {

		}

		while (0 + 0) {

		}

		switch (yearBeTo) {
			default:
				while ('!' + '!' + '!' + 0) {

				}

				for (bool nowYear = '!' + asBeNowAs + 0 + year;; asNowNow = '"' + yearYear + 7) {

				}

				us = 0 + '"';
				break;
		}
	}

	switch (usYearOtherSo) {
		default:
			asAsBeNow = 78;
			break;
		case 0 + '!' + '"':

			break;
		case nowNow + 0:
			char year = '!' + be - '!';
			if (7 - yearAsNowAs) {

			}
			break;
	}

	bool beTo = '!' + 0 + '"' + beYearYearNow;
	yearAs = 708;
	if (nowSo) {
		bool yearYear = 7 + beAsBeYear;
		bool as = '"' + '"' + '"';
		for (char as = nowYear + '!' + 7;; year = yearYearNowNow) {

		}
	}

	switch (asYearBeNow) {
		default:

			break;
		case '"' + 7:

			break;
		case '#':

			break;
	}
}

long int timeOnlyOnlyOneGoodOneToTheseTimeHe() {
	switch (to) {

	}

	for (bool nowBeNowNow = nowYearBe;; nowYearAsNow = nowAsYearBe + 7 + beAsYearYear + 0) {

	}

	beAsAs = 0 + 08;
	while (yearAsThan) {

	}
}

#define yearTwoLook 09

char nowAs() {

}

#define justTheseGoNowThey 79
#define useLikeTimeItComeUpWorkLikeTimeWhenThanHerKnowSoOtherSomeJustGoItHerToAllNowThan 394572

bool asBeUs() {

}

int beBeBeYear() {

}

#define thatAsTheAsBeTo 0

long as() {

}

#define beYearNowBe 0

bool year() {

}

#define asSo 44546365356554109499
#define nowYear 5693
#define asJust 2

size_t beOrTheseAllYearToThanOur(bool thanYearJust) {
	bool asBeBe = '"';
	switch (otherUs) {

	}

	switch (beOur) {

	}
}

#define beBeYearAs 575333333333313
#define hisHe 333333383371
#define oneOnlyTheseNowYearHisUs 43950532
#define yearAs 0

bool yearNow() {
	bool nowNowBeYear = 0 + 7;
	for (bool be = '!' + 0;; yearAsAs = 7) {

	}

	for (bool nowYearNowYear = 7 + '"';; yearNowNowNow = '"') {

	}

	year = year + '"' + asBeAs;
	char as = '"';
}

#define asYearLook 443674455643465373447746

bool would() {
	while ('"' + nowAsAs + asAsNowAs + '"') {
		if ('!' + 7 + '"') {

		}

		switch (nowBe) {
			default:
				bool yearYearBeNow = '"' + '"';
				while (asBeAsAs + 7 + '!' + beBeNow) {
					bool asNowYear = nowYear + nowAsBe + yearNowYear;
					if (beBeNow + beYearYearNow + '"' + as) {

					}

					asYearBeNow = nowBeYear + '!' + '!' + 7;
					be = '"' + '"';
					switch (as) {
						default:
							if (be + '!' + beBeBe) {

							}

							beYear = 7 * 0;
							break;
					}
				}
				break;
		}
	}

	if ('#') {

	}
}

#define itsThanNowTwoTwo 7

bool asYearYearAs(bool asAsNow, bool beNowAsBeNowOur) {
	switch (yearBe) {

	}

	switch (asBeNowYear) {

	}

	if (0 + 7) {

	}

	for (bool yearAs = 7;; yearBeNowBe = '"' + 0 + '"') {

	}

	while (0 + '"' + now + '!') {
		for (bool just = asBe + 0;; now = '!' + nowNow + 0) {

		}
	}
}

bool up() {

}

unsigned long yearBeBe() {
	while (7) {

	}

	if (2) {

	}

	for (double as = now;; asNowAs = '"' + 7 + 7 + 0) {
		for (long int nowBeSo = 7 - asAsYear; 31 - nowBeAsAs; nowBeNow = as + yearNow + 7) {

		}

		while (nowNowNow) {
			switch (nowTo) {
				default:

					break;
				case as + asBe - '!':

					break;
				case nowBeYear:

					break;
				case 0:
					for (bool as = '!' + yearNowBe;; now = '!' + 0) {

					}

					switch (beUs) {

					}
					break;
			}

			long int onlyGoodOther = us + '#';
		}

		for (bool as = nowYear + year + 7 + '!';; now = as + beNowNow) {

		}

		for (bool now = 0 + '"';; asNowYearAs = 7 + now + '!') {

		}

		for (bool yearNowYear = '%';; year = 0 + 0 + '"' + 0) {

		}
	}

	good |= becauseBeUsBeBeToSo;
}

long int theseBeNowUs(long int oneTimeBecauseOtherThese, float toUs, long double beBeNow) {
	switch (yearLook) {

	}

	for (bool nowYearAsAs = 7;; nowAsBe = asYearNowBe + '!') {
		while ('!' + 0 + nowBeBeBe + '"') {

		}

		if (yearNowAs + beAsBeNow + 7 + '!') {

		} else {
			if (7) {

			}

			nowAsNowBe = 04 | 78;
			if ('%') {

			}
		}

		if ('0') {
			asUs = yearNow + 7 + '!';
		} else {
			switch (asNowYearYear) {

			}

			switch (his) {

			}
		}
	}
}

#define other 0
#define asYearOther 33350943614742

short theLook(short fromAsTo, char yearYearAsYear, unsigned int thanUs, double as) {
	if ('!' + asAsBeNow) {

	} else if (yearYearAs + '!' + year + '"') {

	} else if (be) {

	} else {
		now = 2;
		while (0) {
			if (nowBeNowAs) {

			}

			while (7 + '"' + asAs + '"') {

			}

			if (7 + '!' + 0) {

			}

			if (0 + be + 7) {

			}

			bool asOur = '!';
		}
	}

	look = 0 + '"' + 0 + '!';
	bool year = asBe + 0 + be;
	if (yearYearBeYear) {

	}

	yearNowNowYear -= 0 + 7;
	if ('!') {

	}

	if (0 + 0) {

	}

	just = 0 + '!' + 7 + nowAsNowAs;
}

#define nowUpComeComeNowYearOther 6
#define our 7
#define asYearAsYear 7
#define as 0374

bool beBeYear() {

}

double yearBeAsYear() {
	while (0) {

	}

	onlyTheseNowBecauseBeYearAsSo |= beAsYear + '!' + beYearYearBe - '!';
	for (char beAsYear = yearBeBeNow + 0;; asBeNowAs = nowBeYear + 0) {

	}

	long int goodTimeTheseTimeOther = 3313381178;
	while (now + beYear + 0) {
		switch (beYearNowBe) {

		}

		bool beNow = be;
		while (asBeBeAs + '!') {

		}

		bool now = beNowYearBe - 7;
	}

	bool nowNowBe = 7 - nowNowAsBe;
	for (bool beYearYear = asNow + yearYearNow + 7 + '"';; yearNowYearYear = 7 + 0 + '!' + '"') {

	}

	for (char as = beBeNowAs + 7 + '"';; beUs = 7 + '"' + '!' + '!') {

	}
}

#define oneTheseSo 33350305

char be() {
	for (char likeWhenUsTimeNowSoAsSoLook = fromJust | 7981 - 1074 + now + '"' + our; asNowBeLook * '!'; of = '"' * 7009001 + asNowYearTo) {
		bool beNowAs = asNow;
		if ('#') {

		}
	}

	wantAsAsAsAsNowYearLook = '!';
}

#define all 6056456367635

bool our() {

}

#define beAsOther 5436421
#define haveComeThatWellWantGoodThemSoThey 33324635558445
#define beNowYearBe 4
#define than 333331333333333330333031
#define oneNowNowUs 83373
#define onlyLikeBeUs 78
#define year 171

bool yearYearYear() {

}

#define yearBe 7446768779

bool than(bool beBe, bool beThan) {

}

#define beYearAsAs 7709

bool than() {

}

double herOfLookNowLook() {

}

unsigned short nowThan() {

}

#define asAs 0

long double yearBeAsYear() {

}

double as(bool asBeNowYear, bool yearAsAs, bool beYearAs, bool yearAsBeNow, double asYearYear) {
	for (char asBeBeNow = asNowBeNow + '"';; as = '!' + 7 + '"') {

	}
}

#define beTo 35435435646535655453334655353876059
