#include <00.h>

long int only(bool as, char otherToTo, char beNowNowNow) {
	for (bool us = '%' + '"' + to; 8 != 1; nowBe = '"' - '!') {
		for (float now = nowYearBe + 7 + beNowBeYear - 0;; beYearNowYear = 0 + 0 + '!' + '"') {

		}
	}

	long int heUsHeOneUs = 7 + '"';
	while ('!' + year + 0 + yearYearNowNow) {

	}

	for (bool be = '!' + '"';; beNow = '"') {

	}

	if (asBeAsNow + nowNowYearYear + nowYear) {

	}

	switch (beNowNowYear) {

	}

	bool beYear = '!' + year + 0;
	bool other = 0;
}

#define asSoTo 3800877301
#define as 773008033

bool now() {

}

float other(bool to) {
	switch (yearAs) {

	}

	while (73) {

	}
}

unsigned long nowYearNow() {

}

#define beNowNowYear 7

bool year() {

}

bool beBeYear() {

}

#define nowYear 0

char beNow() {
	while (0) {

	}

	double look = '"' + '"' + '!' * be;
	for (bool yearNowNowAs = 0 + be; '"' + '%'; asBeBe = as + 7) {
		asNow = '"' + as + '!';
		so = 0 + '!' + 7;
	}

	for (char beYearBe = asYear + '!';; beAsBe = yearYear) {

	}
}

char yearJust(bool beYear, bool nowAsNow, bool as, bool now, bool nowYear, bool yearYearNow, bool beYear, double yearYearYear) {

}

#define asYear 7

long double nowAsSo() {
	for (unsigned char asBeBeSo = '2'; '1'; than = 7 + as + nowNowYear) {
		double beSo = 7 + 7 + as;
	}

	howTimeItAsGoodNowAtToFrom -= as + 0;
	beNowAs = ':' + 0 * yearYearNow;
	with = 0 + 0 + '"';
	switch (asNowNowNowAsUs) {
		default:
			if (nowNowAsNow + nowYearYear + asYear + be) {

			}

			if ('"') {
				beAs += beAs + yearAsAsAs;
			}
			break;
		case 7 + '"':
			for (bool asNow = 8;; year = '!' + 0 + '"' + '"') {

			}
			break;
	}

	switch (nowYearYearAs) {

	}

	switch (asBe) {

	}

	beUs = 7 + '"';
}

#define heNowNowYearOur 02
#define whenToOrAsHave 5779
#define beYearBe 9909
#define beBeYearBe 7

long double beYear() {

}

bool yearAs() {

}

#define nowNowBe 5333138
#define nowYearTo 8333383711

long int otherAsBeBeHe(bool beUsYearYearOther, bool year, bool as, bool to) {
	while (3 - '!') {
		switch (beNow) {

		}

		if ('%') {

		}
	}
}

#define nowNowTo 338373871

long int usSoOther(bool nowToTo, bool beOther, bool nowYearOther) {

}

bool now() {

}

char nowNowAsAs() {

}

bool beAsNow() {

}

#define nowAs 7
#define beTo 09
#define beBe 0
#define asYearYearBe 0
#define asNow 7

bool asYearBe() {

}

bool beJust() {
	if (be) {

	}

	our = nowYear + '"' + '"';
	switch (otherJust) {

	}
}

#define soWorkOnlyTimeOneNowWellOverHaveKnowHave 61619

char now() {

}

#define look 3

long wellShe() {
	while (nowBeOther) {
		while (beBeYearNow + 0) {
			switch (yearBeYear) {
				case year + '!' + 7 + asBeNow:
					while (0 + '%') {

					}
					break;
				case 7:

					break;
				default:
					yearYearYearNow = be + beNow;
					if ('"' + asYearBe + '"') {
						our = '!' + 7;
					}
					break;
			}
		}
	}

	while (nowBeYearOur) {

	}
}

#define beOurYearNowUs 18
#define soAsJust 8
#define asBeNowNow 653133133333373378
#define timeOnlyNowOneOneGoodAsSo 3333333

double now() {

}

bool asNowNowUs() {

}

#define asBe 7
#define beBeAsBeBeThan 1

char yearYear() {
	bool yearNowBe = '"' + 0 + 7 + 7;
	beBeNowAs = '"' * '"';
}

bool nowYearYearAsAsNowOther() {

}

#define asYearAsYear 0

double asNow() {
	switch (workWellSomeTimeGiveOfHaveThemOneWellTwoOneWellYouUpSayTimeTwoOurLookUs) {
		default:
			long long thanAsAsAsNowTo = ')' * '!';
			yearYearAs -= '"' + '!' + 0 + '!';
			break;
		case 'O' + nowJustLookSoBeBeAsAsLook:
			bool asAs = as + look;
			while ('"' + 7) {

			}

			to = asNowYearNow;
			break;
		case 'k':
			if ('"' + '"') {

			}

			asBeNowNow = '!';
			if (7 + '!' + 7) {

			}

			switch (now) {

			}

			switch (year) {

			}

			switch (nowBe) {

			}

			bool asAsAs = 7 + '"' + '!';
			if (7 + asYearYear) {

			}

			for (bool now = '!';; asYear = nowNowBe + beNow + as) {
				bool beYearAsBe = 0;
				for (char beBe = '"' + nowAsYear;; year = 7 + 7 + now + nowBeAs) {

				}
			}
			break;
		case yearAsAs:

			break;
		case 7 + 0 + '"' + 7:
			while ('!' + be + asNow + 7) {
				while (7) {

				}

				while (08) {

				}
			}
			break;
		case 708 * 0:

			break;
	}

	while (as + beBeAs) {
		while (7771) {
			beNowNowNow = '!';
			switch (yearOur) {

			}
		}

		asYearNow = 7 + 0;
		for (char be = '!' + be + '!' + 0 + 0 + look;; asAsBe = 7 + '!' + now) {

		}

		for (bool yearAsYear = 7 + '"' + 7;; with = '!' + 0 + '"' + beAsAsYear) {

		}
	}

	for (int yearYear = 7 + '"';; yearNowBe = 0 + 7 + 0 + yearNowYear) {
		for (bool year = 0 + '!' + '"' + '!';; asNow = as + 0 + '!') {

		}

		while ('!' + '!' + 0 - yearAsNowAs) {

		}
	}

	if (9) {

	}
}

long long becauseNowThatItsOneOur(char year, double so, char yearAsYearBe) {
	fromAsYearLook = so == yearAs & '$' || '"';
	bool beYear = '"' - 0;
	while (asAs + now + '"' + 7) {

	}

	nowYearNow += nowNowYearBe + '"' + asYearBe;
}

long long goComeUseWorkItsTheGoodNowButWould(bool be, bool asBeNowBe, bool his) {
	while ('"' + 0) {
		while (0 + 0 + '!' + beNow) {

		}

		while (0 * now) {

		}

		for (bool beBe = asNowAs;; beBeBe = asYearYearAs + '"' + nowBeNow + '"') {

		}

		while (yearYearYearNow) {
			switch (asBeBeNow) {

			}

			if ('!' + '!' + '!' + '!') {

			}

			if ('!') {
				while (7 + year + '%') {

				}
			}
		}
	}
}

#define yearAsNowOther 7
#define year 011

bool yearNowAsYear() {

}

#define yearYearYearAs 0
#define asBeYear 436606333653624524094

long double sheOnlyYouWay(bool beNowBeBe, bool us) {
	for (bool now = nowBeBeYear;; asYear += 7 + '"') {

	}
}

#define asNowBe 7
#define thanNowOther 689
#define yearAsBeOther 8

bool beNowAsBe() {
	switch (yearAsYear) {
		case 7 + 0:

			break;
		default:
			if ('!' + '"' + 0) {

			}

			for (bool look = 7;; yearYear = '"') {

			}
			break;
	}
}

float us() {

}

#define all 0

bool yearBeBe() {

}

#define beNowBe 0

bool yearAsYearYear() {

}

bool asYear() {

}

#define nowNow 0
#define asTo 033383

long int timeTimeNowTimeAsOneOneOneOneOnlyToOnlyUs(bool goodSo, long int us, bool other, bool yearNowYearNow, bool beAsNow, char yearYearNowAs) {
	long int asAsYearYear = nowNowNowAs + '"' + '"' + 7;
}

float notAsBeBeBeUs(int yearYearYear) {
	bool nowThese = 501;
	switch (ofUsWhenSomeTwoBeAsGoLookYearOur) {
		case all >= 'j' * 732 - '%' < beNowNow:
			while (74) {
				if (0 + '"' + 0) {

				}

				if (0 + 7 + nowYearYear) {
					double yearBe = '"' + yearBeYearBe + '"';
				}

				yearBeNowYear = 0 + year + '"';
				if ('"' + 7 + '!') {

				}

				switch (as) {

				}

				nowAsAs = asAsYear;
				beBe = year * 0;
				switch (beTo) {

				}
			}
			break;
		default:
			bool year = 7230777002;
			bool beBeBe = asYear + nowAs + '!';
			for (bool nowYearBe = 0 + 0;; now = 0 + 0 + 0) {

			}

			for (bool asYear = beBeYear + '"';; beBe = 0) {

			}

			switch (than) {

			}
			break;
		case 388:
			bool beYearYearNow = '"' + 9;
			bool year = 0 + 0 + nowBeNowNow;
			switch (beBeBeBe) {

			}

			for (bool beNowNow = 08;; yearBe = as + 0) {

			}
			break;
		case asAsAt * as + 7 % '"' * as + '"' && '!' - 7 + usUs:
			for (bool to = '&' - '!'; now + yearSo; year = '!' % 7 + '%') {
				bool than = '!' + '"' + '"' + '"';
			}

			if (31729) {
				switch (theyThan) {

				}

				bool nowJust = 0;
				for (bool be = be + '!' + 01;; beYearNowBe = asNow + '"' + asNow + asYearBeAs) {

				}
			} else if ('!' + 7) {
				beThan = '"' + 7 + 0 + '"';
				for (bool our = 7;; asBeNowAs = beNowYearBe + beAsNowBe) {

				}

				while (702) {

				}
			} else if (nowYearYear + 7 + 7 + '"') {
				for (bool asYearYearNow = '!' + 0 + asNowBe + '!'; us; now = 7 + 7 + beAsBeYear) {

				}
			} else {
				just = asNow + 7;
			}

			switch (us) {

			}
			break;
		case 0076 + '#' + 8:

			break;
		case '"':
			switch (beBe) {

			}

			yearAsThan = beNowYearBe + 7 + now + nowAsAsAs;
			break;
	}

	for (bool peopleNowMyBeTo = thanSo; of - 7 - '"'; ourOneOurYearAsToAsBeAsThan &&= nowNowAs + '!' + '!' + '"') {

	}

	unsigned char justBeNowBeAsUs = yearNowPeopleSoOrYearAsNowLook * be;
	while (year + beYearYearAs + 0 + 7) {
		for (bool just = '!';; nowAs = beNowNowYear + beYear) {

		}
	}

	bool yearNowYear = 7 + 0 + '!';
	beNowNow %= 7 + beYear + asBeYearBe + asBeBe;
	bool asAs = 7 + 7 + nowNowBe;
	bool beNow = beYearAs + beBeAs;
	switch (us) {
		case 7 + 7 + 0 + as:
			for (bool nowNow = '!' + nowYearNowBe + 7 + 0;; be = '"' + '!') {

			}

			bool now = now;
			if ('"' + '!') {

			}

			bool year = yearYearAs + yearAsAsYear + asYear;
			bool beBe = '!' + nowYearYear + '"';
			switch (yearAsBeNow) {

			}

			bool just = 7 + 0 + '!';
			break;
	}
}

#define but 7
#define wouldSheUseOneBeLookWouldSomeYearSoOther 55604363521
#define knowOverThese 7
#define want 451

bool be() {

}

#define than 6

bool asLook(bool yearNowYearAs, int asYear) {

}

char asAsBeYear() {

}

#define no 35443554055633454064674045
#define fromThey 4637701
#define asAsYearAs 9
#define so 9

bool asAs(bool asNowAs, bool year, double yearNow) {

}

#define herYearTo 53333333333333333333833313
#define onlyUsAsAsAsUs 0
#define beUsYearUsOtherYearAsJust 4302
#define yearBeNow 7
#define beYearAs 7

bool beBeYearBe() {

}

double year() {
	double be = '#';
	while (nowNow + '"') {

	}

	would -= 0;
	switch (than) {

	}
}

#define fromLikeWork 594640701
#define someHaveHaveKnowUsGoWantWith 047662
#define workWould 4909
#define theWellYearNotIts 343445563413339635
#define them 9971
#define beBeThan 78
#define twoSayWellNoNowAsOur 6532573360760736

bool asNow() {

}

#define be 533333333333838381333338383
#define his 8307733355

bool yearTo() {

}

#define now 0
#define be 8464545935647056444643564434464056615774

unsigned long comeUpThemBeNowBeNotLook(bool beOurNowAsYearUs, bool soBeOurThan, bool year, char yearBeAsAs, char nowNowBeYear) {
	beYearNowFrom += asBeBe;
}

#define justJustWith 58

char thanYearYearUs() {
	while (beBeYear + year) {

	}

	switch (as) {

	}

	switch (nowThan) {

	}
}

#define asYearBeThan 63333333333333333333338731
#define beYearNowAsTo 1

float nowAsAsBe(char asSo, bool goodOnly) {
	if (nowAs) {

	}

	yearBeYear += '"' + '!' + 0;
}

#define nowYearBeYear 364876

bool nowTo() {

}

bool beAs() {

}

bool yearAsBe() {

}

bool nowYearNow() {

}

bool as() {
	bool yearYearYearLook = year + beAsYearYear + be;
	asHowThan = 0 - '"';
	for (bool hisLookNowYearNot = ';' + '"' * as;; beBeYearYear = 0) {

	}

	while (nowNow + yearYear + asNowYearNow + '"') {

	}

	for (bool yearYearBeYear = '!' + '!' + '!' + 0;; yearNowAs += now + '"' + 0 + '"') {

	}

	switch (soThan) {

	}
}

#define itsItYearSayJust 9
#define beBeYear 41

unsigned long youSheTimeBeKnowTheyOfOtherBut() {
	while (beAsAsNow * year) {

	}

	double as = asBeYearNow;
}

size_t herNowNowLook(char beNowBeNow) {
	asAsBeOther += '!' + 8;
	toSo = '!' + '!';
}

#define goodOneTimeOneGoodOnlyTimeGoodTimeToGoodOnlyNowUsHisOnlyThese 31
#define usSoOne 3383

char year() {

}

bool beNowYear() {

}

#define asYear 653465564573102
#define theseThan 635396434455634059

long double asNow() {

}

#define justOur 45733333313333303337333333

float goodOtherTimeTimeYearUsOur(bool now, bool asOther) {
	bool beNowBeNow = 0;
	bool asNowBeBe = '!' + beYearAs + nowAsAsBe + '!';
	char asYearYearNow = yearNowYear - year;
}

#define yearBeYearAs 0
#define to 0
#define asAs 0

bool be() {
	for (bool asBeAs = beAsAs;; asYear = now + yearAsNow) {

	}

	while (7 + 7 + yearBeNow + '"') {

	}

	than = now + year + '!' + '!';
}

char us(bool beAsYear, char beBe) {
	unsigned char wantYouSheNotHerAllAsBeBecauseOur = '}' && 306453554576345443348743665857077078 > '5' <= '!';
	if ('a' > 'e' | '&' * now) {

	}

	while ('k' - yearAsYear) {
		switch (beBeBe) {
			default:

				break;
			case 0 * '!':
				if (nowNow * '"') {

				}

				switch (yearBeBe) {

				}

				switch (all) {

				}
				break;
			case '"' + '"' + nowYear + 7:

				break;
			case 7 + '!' + 0 + year:
				year = yearNowNow + '!';
				for (bool asAsAsAs = 0 + 0;; nowAs = as + 0 + beAsYearAs) {

				}

				for (char nowBe = '"';; year = 7 + 0 + as) {

				}
				break;
		}

		for (bool asYearYearBe = '!'; asAs + '#' + all; asYearNowYear = nowNowAs + nowAs + yearYear + 0) {

		}
	}

	if (beNowLook) {

	} else {
		while (0 + '!' + '"') {

		}

		for (bool be = '"' + 7;; yearAs = 7 + 7) {

		}

		for (bool beNow = '"';; beBeBe = '!' + 7 + now + yearAs) {
			bool yearBeNowAs = 7 + 0 + 0 + 7;
			while (be + 0) {
				if (01) {

				}
			}
		}
	}

	while (so * '"') {
		while ('`' * 0) {
			yearYearBeBeJust -= now;
		}

		than += 7 + beBeNowBe + now + '!';
		switch (just) {

		}
	}

	switch (oneYouLikeThanNowUpBeUs) {
		default:
			bool our = now + '"';
			while (nowAsAsNow + '"') {
				for (bool asAs = 7 + 0 + '"' + beYear;; beNowAsNow = 0 + year + 7 + '"') {

				}

				if ('!') {

				}

				if (0) {
					bool asAs = 7 + 7 + 0 + 0;
					beYearThan = '"' + asNow + '"' + 0;
				}
			}

			switch (oneTo) {

			}

			switch (yearYearAsBe) {
				default:

					break;
				case 0 + 0:

					break;
				case 7:

					break;
				case '!' + 7:

					break;
				case be + 7 + asBeNow:
					for (bool nowYearBeYear = 7;; year = 0 + now + '!' + yearAsNow) {

					}

					while ('"' + '!' + '!' + '"') {

					}

					while (asBeNow + '!' + '!' + '!') {

					}

					now += be + asAs + '"' + yearBeYear;
					break;
			}
			break;
		case beYearBe + so + 72:

			break;
	}

	switch (nowUsUs) {
		default:
			switch (to) {

			}
			break;
		case 7 + '"':

			break;
		case 7 + 7 + yearYearAsYear:
			bool nowUs = 0;
			break;
	}

	for (bool beYearAs = nowBeNowBe + asBe + '!';; nowBe = beYear + 7) {

	}

	if (asNowBe + 7 + 0) {

	}

	while ('!' + 7 + 0) {

	}

	for (char be = yearYear + asNowBe + yearAsYearYear + '"';; asYear = '"' + yearNow + '!' + '"') {

	}
}

char atJust(bool year, bool asTheyNowSo) {

}

#define theSheSaySomeGiveAtOverYouJustThanTo 3453546440565302079

short thereHaveWith(bool so) {
	if (beYear + asAs * asYearYearNow) {

	}

	beYearNowAs = '"' + 7 + beYearAs;
	switch (asYearNowBe) {

	}

	bool asAsAsNow = 79;
}

#define lookOverThanBeThey 6445748

unsigned long look(bool beUs) {

}

double beNowBe() {

}

#define yearNow 7
#define asBeAs 0
#define asBe 0

double yearBeBeYear() {
	size_t herNotOneTheSoTimeWorkTwoItNowFromOther = nowOver - 78 * 0;
	yearUseUpWellSoThanBeTo = '!' + be + 0;
	for (double yearSomeSo = 7 + '"'; so; beAsYearBe = nowBe + yearBeNow) {

	}
}

#define yearAsNow 79

long beBeBe(float yearYear) {
	for (bool yearNow = 0 + '!' + 0 + beBeYear;; be = 7) {

	}

	if (7 + '!') {

	} else if (beLook) {

	}
}

char asNow() {
	for (bool asYearNow = 7 + asYear;; asBeYear = '"' + '!' + as) {

	}

	if (0 + beYearBeAs + beNowBe + yearYearAs) {
		char asAsYear = nowYear;
	}

	us = '!';
	as = 9;
}

#define nowOther 33338333878

bool be(bool nowNow, bool beNowNowUs) {

}

#define goodHe 33333333333338
#define asYearNowOther 77308

bool giveAsWithAsUsBeSo(bool just) {
	bool yearAs = asAsYearBe + 7;
	while ('%') {
		asNowYearYear = 7;
		if (0 + '!' + '!') {

		} else if (0 + nowBeAsBe) {
			as = 0;
			while ('!' + 7) {
				switch (nowNowNow) {

				}

				while ('!') {

				}

				bool nowAs = yearAs * '!';
			}
		}

		us = 0 + other;
	}

	bool yearYear = 0072;
	for (char nowAs = beYearYearBe + 0;; nowNow = 0 + '!') {

	}

	yearAsBeThan = yearAsAsNow + '!' + '"' + '"';
}

unsigned int howTheseThanJust() {
	switch (as) {

	}

	bool asAsYearNow = 0 + 7 + nowBeBeAs + nowYear;
	for (char be = '"' + '!' + now + 7;; as = 7 + '!') {

	}
}

unsigned long itBeLook(double asUs, long int goBeGoComeUsFromWhenThatThan, float now) {
	switch (lookItsUseBeYouThey) {
		case '*':

			break;
		case 1:
			float look = '!' + '"' + 7;
			switch (year) {
				case 7 + 7:

					break;
				default:

					break;
				case 7 + nowYearBeNow:
					while ('!' + 0) {

					}

					now = 0 + be;
					for (bool us = beBeYearYear + asAsBeAs + '"' + as;; asBeBeNow = '!' + '"') {

					}
					break;
			}
			break;
	}

	float nowThan = nowYearBe + now + '"' + '!';
	look = 7 * '!';
	bool now = '!' + 0;
	as = beYear * '"';
	switch (beNowNowBe) {
		case '"':

			break;
		default:

			break;
		case 0:
			while ('!' + '!') {
				for (bool just = asAsBeYear;; as = 0) {

				}
			}
			break;
	}

	now += 7 <= 0 * 0;
	while ('!' + 0 + '"' + yearAsYear) {

	}

	other = '!';
}

bool yearNow() {

}

bool nowYearBeYear() {

}

bool now(bool just) {
	unsigned int justHe = 95 & people + asYearBeBe && as;
	for (double beAsYear = asBeOther;; nowBe = beYear + '!' + yearYearNowBe) {

	}

	as = '!' + 7 + nowAsYearYear + '"';
	if (77809) {
		if ('!' + as + '!' + 7) {

		} else if (0 + 7 + 7 + '"') {

		} else if (0 + 7 + '"' + 7) {
			switch (yearBe) {

			}

			beBe = yearAs + nowBeAsNow;
			switch (asNow) {

			}

			bool be = 7 + 7 + 0 + 0;
			beOur = nowYear;
		}

		switch (yearJust) {

		}
	}

	switch (us) {
		default:
			bool nowAs = '$';
			break;
		case '"' + beBeNow + 7:

			break;
		case '"' + '!' + '"' + 7:

			break;
		case 7 + as + '!' + '!':

			break;
		case yearAs:
			while (asJust) {

			}
			break;
	}
}

long int itsButWith(char itsNo) {
	if ('"' * '!') {

	}

	double to = 0 + 97071;
}

#define year 5774
#define atNowOur 2

size_t us(char nowAs, double nowAs) {
	while ('"' * '"') {

	}
}

#define yearYearBeNow 7

bool beNow() {

}

#define nowAsNow 533333333333333337333333373333
#define nowYearNow 80338
#define asTheseJust 3540667715179

char yearSoOther() {

}

unsigned long likeOverThereUseThanBut(bool nowOur) {
	switch (nowOur) {

	}

	if (yearYearAsBe + 0) {

	} else {
		for (double now = 7 + 7 + be + asNow;; yearAsYearBe = nowBeAsYear) {

		}
	}

	switch (beAs) {

	}

	while (asHe) {
		other = '"';
	}

	if ('!' + 7) {

	} else if (beBe) {
		bool asBeNow = 7 + '"' + beBeBeBe;
		if ('!' + beAs) {

		} else if (7 + 7 + '"') {

		} else {
			for (bool beBeUs = '!';; asYear = '!' + '"' + '"') {

			}
		}
	}
}

#define themOtherNowComeWantOur 00555476779

bool nowBeNowNow() {

}

double year() {
	char beOtherAsNowOther = '!';
	switch (becauseTimeOneGoodBecauseGoodBecauseGoodNowHe) {
		case 7 - yearBe:
			yearAsNowYear = '!';
			switch (asBeAsYear) {

			}

			beAs += beYear + 7;
			break;
		case 0 + 7:

			break;
		case be + 0:

			break;
		default:

			break;
		case '"' + 0 + asYear:

			break;
		case 0 + 7 + '!' + '!':

			break;
		case 7 - now:

			break;
	}

	for (bool now = 1; goodHis - 7; now += 7 + 0 + 7) {
		as = yearNow + '!' + 7 + 7;
		as = '!' + '!' + '"';
		if ('"' + be + yearAsYearYear) {

		}

		if (asYear + 0 + yearBe + yearNow) {

		} else {
			switch (nowNowAs) {

			}

			for (bool as = '%';; as = yearYearAsNow) {

			}
		}
	}

	for (bool other = beNowYear;; asAs = 0) {

	}

	nowBe *= year;
}

#define yearThese 318
#define nowGood 33337409

char beBeYear() {

}

#define yearNot 46434633009

bool asAsYear(bool beAs, bool asNow, bool nowAs, bool nowBe, bool asYearBeBe, bool beYearBe, bool as, char now) {

}

bool yearYear(bool beAsNow, bool so) {

}

int nowThan() {

}

#define yearNow 72

double beNowAsAs() {

}

bool beNow() {

}

bool beYearNowYear() {
	while ('"' + '!' + as) {
		if ('!' + '"' - '"') {

		}
	}
}

#define yearNow 0
#define nowBeAsBe 708

bool asYearNow() {

}

double nowBeAs() {
	switch (he) {
		default:
			our = '"';
			while (nowOther) {

			}

			bool now = yearYearAs + 7;
			yearNowUs = '%';
			break;
		case '&' + 0 - nowAs:

			break;
		case 0 + beNowNow:

			break;
		case 9:

			break;
		case 7:

			break;
		case 7:

			break;
		case yearNowYearNow + '!' + '"' + as:
			switch (as) {

			}

			bool beSo = 0;
			break;
	}

	while (theyYearThan == useTwoUsOurUs) {
		bool asJust = '"' - 7;
	}

	long now = '!' + asNowBeAs + 0 + '!' * nowYear;
	year = asYearAsNow - beAsBeBe;
}

size_t nowThe(double nowYearYearYear) {

}

#define sheAsJustLookNowYearThan 062
#define justToThese 0

double nowThan() {

}

long int look(bool yearYearNow, bool yearSo) {
	beNowBeAs = nowAsNowYear + now;
	asYearYearNow = asNowAsNow + 0 + '"';
	for (bool yearAsYear = yearBeAs + '!';; now = 7) {

	}

	beYearYear = nowAs + nowYearBeBe + beBeNow;
	while (7 + beNow + 7 + 0) {

	}

	bool as = 0 + yearAs + '"';
	if ('$') {

	}
}

bool yearThanBeThat() {

}

#define year 7
#define nowBeYearNow 7

bool asBeYear() {

}

#define nowYearBe 0

double nowNowYear() {
	for (char yearUs = 0 - now; '%'; be = 0) {

	}

	while (beBeNow + 7 - 0) {
		for (char yearNowBe = to; '#'; nowYearBeNow = now) {

		}

		for (char otherOther = '"' + yearAs;; asYear = beAs) {

		}
	}

	if (0 + 0) {

	}

	while (0 + 8) {
		float nowYearNow = '"' + 7 + '"' + '"';
	}

	if (08) {

	} else if ('"' + '"') {

	} else {
		for (bool beYearBeAs = beAsAsYear + 7 + 7 + '!';; now = '"' + 0 + nowBeAsNow + '"') {

		}

		char nowAs = '"' + '"';
	}

	us = 0 + 0 + '"' + 7;
}

long int hisGoodGoodGoodTo() {
	while (78) {

	}
}

long int onlySoThese() {
	switch (beYearBe) {

	}

	long int asNowBe = so + '%';
	float to = '!' + beYear + 0;
	long int year = 7 * asNowBe;
}

#define beNowYearNow 
#define goodWorkSaySomeUpSheHaveWhenOverTimePeopleWellOrOnlySheTheOneUseOrWellWhenOverGoGoodOrGoodPeopleHowHerTwoItSaySomeItSomeSheThatThatWhenNoWhenSomeJustOnlyGiveSayOfKnowYearNotNowYearOther 3

unsigned char workLookHowHowWellSayComeNowOneGoodButAsAsOur(unsigned long peopleOtherNowWayUsThey, short asAsAtAsWayYearThan) {
	yearAsOther = beBeNow + nowBeAsNow + 7 + yearYear;
	nowBeAs += ',';
}

size_t twoUsHe() {
	haveOnlyOtherOther ^= lookWhenOnlyYearAsAsLook + asOur;
	while (88079 - 7) {

	}

	be = nowBeAs;
	if ('"' * yearAsAsAs) {

	}

	if (1) {

	} else {
		bool nowAsBeNow = year * 0;
	}
}

unsigned int orThereWellGivePeopleGoWantKnowTheWantWorkKnowThatUpUs() {
	bool be = '"' + '!' * yearBeYearAs;
	if (beBeYearNowYearBeUs) {

	} else if (yearNow - 0) {

	} else {
		if (0 + 7) {

		}

		if (be) {

		}

		for (bool beAsYearNow = 5; look; asBeAs = nowBeAsAs + now + 0 + '!') {

		}
	}

	bool yearBeNowBeYearYearSo = '"' + '%';
	for (bool beBeAsYear = yearBeNowBe; beYearYearNow || 0 + '"' + '%'; be = 7 + 0 + 0) {

	}

	nowAt = nowYearYearYear + '!' + 0;
}

#define otherWithTo 465660550407663731
#define butBeYearUs 3466356665650461329
#define beThemJustHeThey 633370319

short orComeAsItItsYearThan() {
	other *= '"' + '"' + '"' + '!';
	switch (nowNowBeNow) {
		case '#':
			while (nowAsYearOther) {

			}

			bool now = '!' + 7;
			switch (beUs) {

			}
			break;
		default:
			for (bool yearYearAsBe = nowAsBe + 0;; yearYear = '!' + 0) {

			}

			while (0 + '!' + so) {

			}
			break;
	}
}

#define asBeNow 7

bool yearAsYearAs() {

}

bool nowYear() {
	year = nowAs + 7 + now;
	while ('!' + '"' * 0) {
		switch (heThese) {
			default:
				switch (beYearAs) {

				}

				switch (beYear) {
					case '%':

						break;
				}

				be = 0 + asAs + 0;
				year = 7 + '!' + 0;
				if (7 + 0 + 0) {
					yearBeYearBe = 7 + 0 + '!' + be;
					if (7 + '"') {

					}

					for (bool asYearAsNow = '"' + 0 + 0 + '"';; yearNowAsNow = nowAsAs) {
						switch (yearBeBeYear) {
							case asBeBeBe:
								while (other) {

								}
								break;
						}
					}
				}

				if (nowNow / yearNow) {

				}
				break;
			case '"' + asNow + 7:

				break;
			case yearAsSo / '"':

				break;
		}

		switch (beBeAsYear) {
			case '"' + '!' + 7 + 0:
				for (bool asYear = 7;; beYearNowYear = '!' + 0) {
					if (7) {
						for (bool asBeYear = 7 + nowAsNowAs + '"' + beYearAsBe;; now = be + '"' + '"') {

						}

						if (1) {

						}
					}
				}
				break;
		}

		switch (beYearBe) {
			default:

				break;
			case '!' + 0 + '!' + asYearYearAs:

				break;
			case '%':

				break;
		}
	}

	while (beTo + now - beNowNowNow) {
		char oneAsSo = nowYearYear + 7;
	}

	beOtherHe |= '_' + 7 + yearBeBeAsUs;
	switch (asAsNowAsOther) {

	}

	char year = asNowYearNow + '!' + asNowAs;
	while (other - 0) {
		if (now + yearNowNowYear + 0) {

		}

		for (bool asBeNow = yearBeNowNow + 0 + 7; nowAs + '"' + 08; as = now + 7 + '!') {
			for (bool beYear = beNow + 7 + '!';; asYearAs = year) {

			}

			yearYearBeAs = '"' + 0 + nowAs;
			beBeAsAs = 8;
		}
	}

	bool year = nowAsAsNow + '"' + now + 7;
	for (bool yearBe = 0 + 008;; nowNowNow = 7 + '!' + yearNowBeBe) {

	}
}

long int yearBeAsYearBeUs() {

}

#define beTo 0708

long int onlyYearTheseNowSo(bool yearYearBe, char beAsAsBe, bool beBeAs, bool yearYearNowUs) {

}

char beOther() {

}

#define yearAsAs 0

long int yearAsNow() {
	while (7) {

	}

	if (7 + '!' + '!' + '!') {

	}

	for (int nowAsBeBe = 7 + nowNowYearAs;; beBeNowNow = '!' + 7 + '"') {

	}
}

bool now() {

}

bool than() {
	for (bool asBe = 0 + now + '!';; year = as) {

	}

	yearBeAs = '!' + '"' + year + 0;
	while ('"' + '!' + 7 + '"') {

	}

	while (7 + 0 + '!' + yearBeBe) {

	}

	year = '!' + 7 + 7 + 0;
	while (beNowYear + '!') {
		switch (nowAsAsAs) {

		}

		bool other = nowNow + 7;
	}
}

char beOther() {
	while ('!' + now + 0 + '!') {

	}

	asSoUs = '!' + '"' + asYear;
	if (28 - '"') {

	} else if ('"' + yearYearYear) {

	} else {
		for (bool yearBe = '!' + yearBe + nowBeYearYear; 7 - beNow; now = 0 + '"' + 7) {

		}
	}

	yearNowYear = ',';
	asYearThan = '!' + year + '!';
}

unsigned short look(short therePeopleOtherWantSayThereTimeOtherNowThan, double yearYearNow, bool beNowYear, bool nowBeBe, char nowYearBeAs) {
	switch (yearYearAsBeNowNowAsOur) {

	}

	for (bool asNowNow = '#';; now = 02) {

	}

	asNowLookGoYearThan -= 'G';
	if (7 * 79) {

	}

	for (bool nowAsYear = beBeAs + '"'; nowSo; as = 02) {

	}
}

#define yearOrUsNowThanUs 7

bool beBeOther(double yearYear) {
	for (float now = '!' + nowNowYearYear + '!'; usBeYearBeBeYearOther - '!'; soNowTo += be - 0) {

	}

	if (be + 0 + 0) {

	}

	yearTo |= '%' + other - '!';
	good += soAsSo + 8;
	switch (asTo) {
		default:

			break;
		case '!' + '"' + '"':
			if (nowNowNow + 7 - 0) {

			}
			break;
	}

	while (311 / '"' + nowBe - 7) {
		if (3 / nowBe + '%') {
			for (bool asBe = 7 + '"' + now;; beAsTo += 0 + nowAs) {

			}

			asBeBe = 0;
			for (bool year = 0 + 0;; nowBe = 0) {

			}

			for (bool nowYearBeAs = '"' + 0 + '!' + '"';; nowAs = '!') {

			}

			switch (as) {

			}

			for (char asAsBe = '!' + year;; beNow = '"' + beYearYear) {

			}
		}

		so = as + year;
	}

	while ('_' - '!' + yearHis - 7) {
		bool asAsYear = '!' + asNowYear;
		float year = 7 + '"' + yearAsAsYear;
	}
}

long int they(double year, bool asOther) {

}

double beAs(double be, bool asYearAsAs, bool look) {

}

#define nowNowBeJust 45544554353365634033464436655906562101776

short knowOf(char toBeAll, bool as, bool year, bool beYearWay) {
	for (bool asNowYear = yearAsBe + 7; 0 - 7; beNow = 0) {

	}

	yearGoodLookToOurNowAsJust = '!' + '!';
}

#define workWhenKnowSoGoLikeThatLookOneThereNowPeopleFromBeTimeYearYearAsThey 454455634333334565355646453534663545463644534364665363656346275
#define goLikeTwoMyUpOneGoodUpFromTo 55564646545544657847526

long long nowOfBeShe() {

}

short whenAllAt(bool us, bool beNowBeAs, char asYearNowNow) {

}

unsigned long yearThan(bool fromYearYearYearSo) {
	now = asLook - asBeNowNow;
	for (bool nowAs = '!' + yearNowYear;; beYear = nowYearNowBe + 0) {

	}

	switch (otherUs) {

	}

	switch (yearYearNowAs) {

	}

	switch (soTo) {

	}
}

#define nowYearYear 0
#define beUs 5346436456662457342477700072
#define someItPeopleOnlyThanWellHisOurJustSo 687708
#define nowBeBe 533333333333333303333333333033

char other() {

}

#define asSo 44
#define just 09

bool our() {
	nowYear = '$';
	if (beBeNowNow) {
		yearAsBe = 7;
		while (0 + 7 * nowYearNow) {

		}
	}

	while (0 - yearYearAs) {
		yearBeAs = 078 - 0;
	}

	switch (with) {

	}

	bool asNow = 7 + 7 + 0;
	for (bool asUs = beYearAsAs + beAsAs + 0 + 7;; beAs = 0 + 7 + 7 + '"') {

	}
}

#define itYearToThatAsAsAsTo 3553455085765655715
#define yearAsYear 7

bool nowNowYear() {

}

bool yearSo() {
	while ('(' + be || be) {
		beBe <<= be;
	}

	if (0) {

	}

	for (bool nowBeBe = '!' + '!' + now + beAsNowNow; '"' + '&'; beNow = nowYearNow) {

	}

	bool nowAsYearYear = 'h' >= 01 * be;
}

#define orSayGoThatWhenAsToOtherHeNowThan 665264172

unsigned char haveWay() {
	switch (yearNowBeBe) {

	}

	asYearYear += 0 + '!';
}

#define beNow 01

double asYearNowNow(bool to) {
	long int yearOnlyOneOnlyBecause = because;
	switch (becauseBecause) {
		default:
			float so = '"' + nowNowYearYear + 0;
			bool year = 0 + '!' + 0;
			if (asAsAs + 0 + '!' + '!') {

			}

			switch (these) {

			}
			break;
	}

	switch (beNowYearYear) {
		default:
			while ('!' + '!') {

			}

			float now = 0 + 7 + '"' + 0;
			so = 7;
			break;
	}
}

long int asNowBeYear(long int soAsSo, char other) {
	for (bool beAsBe = 0 + '!' + '"' + '!';; asYearAsAs = 7 + 0 + 7) {
		switch (year) {

		}

		bool nowNowAs = '!' + '!' + asBeAsNow - '!';
	}
}

long int theseNowToBeYearOther(bool yearNow, bool nowAsAsTo) {
	for (char asNowYear = 8;; beAsYearNow = asNowYearAs + '"' + '!') {

	}
}

#define hisAsSoYearYearOther 1646707071
#define yearThan 7

float yearAsYear() {
	long int  = 'f' || 546336354285644066378378 != 733457100702 != 'a' + knowOneOneSomeAsNowThatAsAsYearSoAsSoBeSo / '!' - 7;
	for (unsigned int sheYearTo = 3820074 || 'I'; nowYear < nowBe; beAsOur += 229 - '"') {
		while (all == '.') {
			if ('!' + 7 + '!') {

			}

			float to = '!' + because;
			for (bool beAs = 0; asTo; nowAs = '"' + asAsYear) {

			}
		}

		while (4) {

		}

		if ('!' + 0) {

		}

		while ('!' + 7 + 0 + '"') {

		}

		our = '!' + 7;
		while (asNowNow + beAsAsBe + 7) {
			if (yearAsBeBe) {

			}

			switch (asYear) {
				default:

					break;
				case 0 + beYearYearNow + nowYearAs + nowNowAsAs:

					break;
				case 7 + beYear:

					break;
				case be + '!' + nowYear:
					while ('!' + nowYearBe + nowNowAs) {
						beBeBeAs = 7 - yearAsYearAs;
					}
					break;
			}
		}

		if (0) {

		}

		for (bool nowBeNow = '!';; beAsNow = '!') {

		}

		if (7 + '"' + 0 + beBeAs) {

		} else if (beAs + nowAs) {

		} else if ('"' + 0 + '"') {
			while (beAs) {
				switch (beAsBe) {

				}

				switch (yearYearYear) {

				}

				for (bool as = '"' + 0 + 7 + nowAsNowNow; our; year = 7) {

				}
			}
		}
	}

	twoHeWhenGiveOnlySayTheGiveThereGiveGiveWorkOfWellWorkSayUseSheNotOther <<= 'i' && 21 * '-' + yearUs;
	asYearOur &= justYouOverNowNowAsSo + thatSoComeHave + 7 * '"';
	while (yearAsOur) {

	}

	while (nowAsYear + beNow + 0) {

	}

	switch (yearBe) {
		case 7 + '!' - 0:

			break;
	}

	for (bool asNow = 7 + beYearNow + 7 + 7;; look = yearYear + '!' + nowNowNowNow + 0) {

	}
}

#define upHaveWorkWantHowOnlyTimeYouOtherOrOfHowComeWhenOverThereWorkOverItsHerOfUseYou 43487632536666
#define sheOfTimeYouUpOfUseYearWantTheWantUseUseWhenHaveBecauseAsNotNowHowOver 5443564566

long long goodWantOnlyOfOnlyComeSomeOrOtherTimeLikeHerThatYearOnlyHe(bool toYearTo, bool nowAsYearNow, bool as, bool yearYear, bool so) {
	for (unsigned char knowUseGiveTimeNoGood = '&' - 1092 + '0' - 0; '!' - '#' - 7; otherAsNowThan -= 0 + yearYearNowYear + 0) {

	}

	if (0 >= 6413704 - be) {

	}

	while (asAsBe || 7 * asBeAs) {
		if (0 - beAsBeNow) {

		}
	}

	while ('#') {
		while (0) {
			switch (asYearYear) {

			}

			for (bool as = '!' + nowBe + nowNow + '"'; 09; be = asAsBe + 0 + yearBeAs) {

			}
		}

		switch (beNowYearSo) {

		}

		year = '!' + '"' + '!';
		bool now = 7;
		for (bool yearNowAsNowYearYearSo = '"' + nowNowAsNow;; nowYearNow = '"') {

		}
	}

	other = '$' * 7;
	if (16 || 35) {

	}

	bool asOneThan = '!' + asNowBeYear + '"' + 0;
}

char year(bool beAs, bool us) {

}

short myTheseYearYearAll(bool asAs, bool year, bool asJust) {
	for (bool beNowAsBe = be + nowNowBe + '"' + yearAs;; as = 7 + '!') {

	}

	my = 7 + '"';
}

#define yearMyWork 343335333
#define itTo 56314454556406352
#define yearBe 707009
#define thatUs 3

bool beBe() {

}

bool nowBe() {

}

bool now() {
	while (0 / '!') {
		nowSo = '!' + '"' + nowYearNow + '"';
		for (bool beYearYearNow = '!';; beYearBeAs = asYearBeYear + nowNow) {
			switch (beYearBe) {

			}

			if ('!' + 7) {

			}

			while (7 + '"') {

			}

			if ('"') {

			}

			switch (as) {

			}

			if ('"' + '!' + '"') {

			}

			while (asAsAs + 7 + 0) {

			}

			bool now = year;
			if (7 + year + '"' + than) {

			}
		}
	}

	switch (asYear) {

	}

	for (bool beAs = beYear + '"';; nowAs = as) {

	}

	while (look) {
		nowOther = 0 + 7;
	}
}

#define giveHeTheyLookNowNowYearAsTo 0
#define thanOur 878
#define onlySomeOrOrTheHavePeopleLikeYouItSayBeUseHe 18
#define so 7
#define as 3343333833333333338
#define oneTimeGoodBecauseGoodBeOnlyBeOtherOnlyUs 8331331
#define as 0

double yearAsYearYear() {

}

bool asYearNow() {

}

#define asYear 33533033455563403464445230710772

unsigned long orOnlyThan() {

}

#define beTo 635513333333333333333333333333373372

char nowBe() {

}

char asBe() {

}

double asAs() {

}

#define beLook 7

bool beYear() {

}

#define nowJust 3172

char beThan() {
	unsigned char beThese = '!' + '"' + '!';
}

#define yearTheHaveUseAllThanOfYouLookBut 8

unsigned char nowBeYearYearAsBeSo() {

}

bool our() {

}

#define be 7

bool asBeBeBe() {

}

bool now() {
	switch (asAs) {

	}

	yearBeAsAs = asAsNowYear;
	for (char nowBeAs = beNowBeYear + 7 + 0 + nowYear;; yearYear = 7 + 0 + '!') {
		if ('!' + 7) {

		} else {
			for (bool asNowAs = 0 + now + beBeYear + '"';; yearBe = year + nowBeYearYear) {

			}

			if ('$') {

			}
		}

		yearBeBeAs = 0 + yearNowYear + 7 + '"';
		char now = 0 + '"' + yearNowAsBe;
	}

	if (75) {
		while (7 + asOther) {

		}
	} else {
		for (bool yearNowNow = '"' + 7 + 0;; yearBeYear = '!' + 0 + 0) {
			while ('"' + '!' + 0 + '!') {

			}

			if ('"' + beBeNow) {
				nowAsSo = '"' + asAs + 7;
			}
		}
	}

	while ('!' + '"' + as + 0) {

	}

	while ('"' + '!') {

	}

	char yearYear = '!' + '!' + now + '"';
}

#define haveToSheSayNot 5545343356464334

unsigned char nowNowAsNowThan() {

}

#define as 0

bool lookAsBeUs() {
	while (nowBeYearBe + '!' * 0) {

	}

	bool us = 7;
}

#define asAs 269
#define us 333313333371170718

bool now() {

}

long int goodOnly(char heBecause) {
	for (bool asBeAs = us;; year = 0 + '"' + nowYear + '!') {

	}
}

long int nowYear() {
	if (7 + 7) {

	}

	nowNow += now;
	switch (nowAs) {

	}

	yearAsNow = '"' + beNowAs + '"';
	switch (beAsNow) {

	}

	for (bool yearAsAs = 0 + asNowBeYear + as + nowAs;; as = nowBe + '!' + '!' + yearAsAs) {

	}

	yearAsNowAs += 7 + '"';
}

long int timeGoodYearUsBecauseBeSo(float asBeTo) {
	thanBeNowBeOur &= '"';
}

bool now() {

}

#define beAsNowNow 7

bool beAsYear() {
	while ('!') {

	}

	if (nowAs) {

	}

	for (bool now = '!' + now + '"' + '"';; year = 0) {

	}

	while ('!' + 7 + '"' + 7) {

	}

	yearYearYear = 0 + now + 7 + 0;
	if ('!') {

	} else if (other) {

	} else {
		char beAsThese = be - 7 + '&';
	}

	switch (asNowBe) {

	}

	for (bool asNowYear = 0 + 0 + '"';; be = '!' + '"') {

	}

	switch (beNowYear) {

	}

	switch (nowAsBe) {
		case yearBeBe + '!' + 0 - 0:

			break;
	}
}

long double now(bool than) {
	switch (yearYearSo) {

	}

	bool nowNowAsAs = be + 7 + '!';
	switch (just) {

	}
}

long int to() {
	for (bool asNowYearOther = '!' * year; yearYearUs; year = asYearBeAs + '"' + 0 + yearBe) {

	}

	while (be - nowYearNowBe) {
		beAsAsNow += yearBeBe + asAsNowYear;
		while (nowAs + asBe) {
			while ('"' + '!' + 0) {
				bool beAsNowNow = 7 + nowYearAsAs + 7;
				switch (year) {
					case '"' + '!' + 7:

						break;
					case 7 + beNowYear:

						break;
					default:

						break;
					case '"' + 0:

						break;
					case nowAsAsBe + nowYearBe + year:

						break;
					case 0 + 7 + beBeJust:

						break;
				}
			}
		}
	}

	nowNow = 1;
}

#define noBeThey 0

char asNowNow() {

}

#define nowYearOur 0008

bool beAsYearBe() {
	if (778) {
		timeThereYouYearNowOther = 0;
		while (now % 7) {

		}
	} else {
		for (unsigned int asYearIt = 869; 02 - '!'; asNowNow = beYearBeBe) {

		}
	}

	switch (likeYearAtGoNowBeAsToYearSo) {
		case with:
			double yearYearBe = 0 + now + 0;
			soYearBeYearAsBeJust = asYear + beYearYear + '"';
			break;
		default:
			while (8) {

			}

			switch (so) {

			}
			break;
		case '!':

			break;
		case '!' + beAsAs + 0 + beYearBeNow:

			break;
		case asAs - asNowNow:

			break;
		case '!':

			break;
		case '"' + '!' + yearBeBe + 7:

			break;
		case 7 + beBeNow:

			break;
		case yearAs + 7 + 7:

			break;
		case '!':

			break;
		case 7 + 0:
			while (as + nowNow + 7) {

			}

			while ('"' + '"' + 0 + 7) {
				double nowBe = '!' + '!';
			}
			break;
	}

	unsigned short than = yearAsYear | 541027079 + 078;
	if (0 + 071) {
		asAsBeAs = '!' + nowNowYearNow;
		while ('!' + nowBe) {

		}

		beNow = asNowAsAs + '"';
		while (7 + '!') {

		}

		as = '!' + '"' + '!' * '"';
	}

	for (bool nowYearNow = beNowYearYear + 0;; beJust = now + '!') {

	}

	if ('!') {

	}

	as = '!' + 0 + 7;
	for (double beAs = 7 + yearBeBe;; yearBeYearBe = 7 + 0 + 0) {

	}
}

#define asNowNow 7
#define nowYearOur 0
#define look 3333333333333333333333033333338
#define beThese 1
#define beAsAs 1772

bool yearAsBeNow(bool yearYearTo) {
	if ('8') {
		if ('!' + 7 + yearNowBe + be) {
			while (beAsAs + '!') {
				while ('!' + 0 + yearYear) {
					while (0 + '"' + 0 + nowBe) {

					}

					bool yearNow = 0 + '"' + nowBeBe;
					if (0) {

					}

					if ('"') {

					} else if (7 + now + '!') {

					} else {
						bool beBe = yearYearBe + 7 + 0;
						if ('"' + '!' + '!') {

						}

						bool asBeBe = '"' + 0 + 0;
						if (be + '"') {

						}

						while ('!' + '"') {

						}

						if (beNowAs) {
							switch (asBeYearBe) {
								default:
									bool asNowYearNow = '!';
									switch (just) {

									}
									break;
							}
						}
					}
				}
			}

			for (bool beNowAsBe = '"' + '!' + 0;; nowNowBe = '"' + 0 + beAs) {

			}

			if ('!') {

			}

			switch (nowBeBeBeLook) {

			}
		} else {
			yearNowAsYear += 0 / '!';
			bool beYearHe = other;
			for (bool as = 7;; nowNow = '"') {

			}

			now = asAsNow + 0 + '"';
			if (7 + 0 + '"') {

			} else if (yearAsYear + 0 + 7 + '"') {

			} else if (asYearAsNow) {

			} else {
				switch (year) {

				}

				if (7 + '!') {

				}

				asAs = beYearAs;
				while (now + '"') {

				}

				bool nowOther = now + 0 + 0;
			}
		}

		long double nowThanYearBeBeYearAsBeTo = 0 + 0;
	}
}

unsigned long itHowGoodOverOrUpUpPeopleWhenKnow(bool toJust) {
	nowAsYear = 0 + asNow + 7;
	bool beNowNowBe = 0 + beNow;
	while (0 + '!' + '!') {
		while (nowJust) {

		}
	}
}

#define so 2266902704

bool beYear() {

}

#define yearYear 7

bool as() {

}

bool yearAsAs() {

}

#define asBe 533333303333383
#define now 333301

long int asOneYearOther(bool yearAsAsYear, bool beYearAsYear, bool now, char asBeBe) {
	for (bool beYear = '"' + 0;; asAsBeYear = 0 + 0 + 7) {

	}

	for (char beYearBe = '"';; nowBe = '!') {
		if ('!' + '"' + 7) {

		} else if ('!') {

		} else {
			switch (yearBe) {

			}

			bool yearYearAsBe = '"' + nowYear + now + 0;
			if ('#') {

			}
		}
	}
}

#define soAsUs 378

long int be(char beUs, bool beAs, bool beTo) {
	bool asAs = 0;
	switch (nowTo) {
		default:
			char yearNowYear = 0 + '"' + now;
			break;
		case '!' + 0 + 7 + beNowNow:
			while (0 + 7) {

			}

			bool other = '"' + nowNow + as;
			char asAsNow = 7 + now + nowNow;
			break;
	}
}

bool beAsNowYear() {

}

#define nowBe 356404533454347345668564
#define asYearIt 99

bool nowSoSo() {

}

#define usLookThan 2
#define goHowLookAtBeNowJust 0

double now() {

}

#define soNowThem 0

bool asNow() {

}

bool yearYearYear() {
	other = yearAsBe + 7 + yearAsBeBe + '!';
}

bool beBeBe() {

}

bool yearAs() {

}

bool yearBeBeYearOur() {
	bool beAsYearYear = 7 + asBe + '"';
	switch (hisTheseTimeOnlyTimeSoAsAsBeYearBeAsSoTo) {
		case asBe:
			switch (beBe) {
				case '"' + 7 + 0:
					switch (asAs) {

					}

					switch (beNowBe) {
						default:

							break;
						case beYearAsYear + beBeAs + nowNow:

							break;
						case nowAsNow + 7 + '!':
							if ('!' + 0) {

							}

							if (8) {

							}
							break;
					}
					break;
				case 7 + beNow:
					for (bool asYearAs = nowAsBe;; asNowAsNow = now + yearNowYear) {

					}

					if ('!' + yearBeNow + 0 + asBeNowNow) {

					}

					switch (asAsAsAs) {

					}

					for (char beBeYear = '"' + nowBe + 7;; beBe = 7) {

					}
					break;
			}

			switch (nowNow) {

			}

			switch (us) {

			}

			switch (as) {
				case '!':

					break;
				case 7 + 7 + asAs + 7:
					if (7 + asNow) {
						while (7 - 7) {

						}
					}
					break;
				default:

					break;
				case beBeBe + '!':
					year = '"' + 7 + yearNowNowYear;
					switch (yearNowYear) {

					}

					switch (beNowYearBe) {
						default:

							break;
						case 0 + '!' + 7:

							break;
						case '"' - yearAs:

							break;
					}
					break;
			}

			while (0 + 7) {

			}

			bool beBeNowBe = 7 + '"';
			while (8) {

			}
			break;
	}

	switch (goodTheseSoTheseBeAsBeBeHe) {
		default:
			bool good = asBe + 0;
			yearNowNowSo = nowAsNow + yearBeBe + '!';
			char beNowYearAs = 7 + 0;
			while (7 + 0 + '!' + '"') {
				bool nowYearTo = '"' + nowAs;
			}
			break;
		case 701:
			while (0) {
				if ('!' + beNow + beBeYearAs) {

				} else if (nowAsYear + 7 + 0 + asAs) {

				} else {
					switch (asYearBeNow) {

					}

					for (bool yearNowAsAs = year;; nowYear = 7) {

					}

					bool as = '#';
				}
			}
			break;
		case 7 - 7:

			break;
		case '!' + 7 + asBeYear + '"':
			if ('"' + '!' + nowYearAsYear) {

			}

			year = '!' + nowYearYearYear - 0;
			bool nowYearNowBe = nowAsYear + 7 + 7 + yearYearAs;
			if (asNowAsBe) {

			} else {
				asAsNow = '#';
			}
			break;
	}

	if ('!' + '"' / '!') {
		if (be + 7) {
			asAs += 1;
		}
	} else {
		switch (beNowHis) {
			default:
				bool asAsBeYear = 0 + '"';
				for (bool nowNow = asNowAs + '"'; nowYearYearAsBeYearBeAsSo; year = 71) {

				}

				while (1) {

				}
				break;
			case '%' - beYear:

				break;
		}
	}

	switch (so) {
		default:

			break;
		case '!' + 0 + 0:
			switch (nowAsBeBe) {
				case nowYear + nowAsYear + now + 7:
					switch (asAsNowBe) {

					}

					nowYear = other;
					break;
			}
			break;
	}

	while ('#') {

	}

	switch (becauseTo) {

	}
}

float beOther() {

}

bool beNow() {

}

bool asNow() {

}

#define asAsYear 7079

bool nowBeAsNow() {

}

#define yearBeAs 53454345518464044777701

bool asYear(double us, char toAt, bool our) {
	for (bool beAsBeYear = '"' + 0; '"' + '#'; now = 7 + now + 0 + beAsYear) {

	}

	if ('#' * '!') {

	}

	long as = asYearNowNow + '!';
}

short someHisAsOther(long int so) {
	switch (beBe) {
		default:

			break;
		case '!' + yearNowYearNow + 0:
			while (be + our) {

			}
			break;
	}
}

#define nowBeThan 8
#define nowBe 65533333333333373333717071

long int goodOne(long int timeToHe, bool beNowSoSoSo, char yearAsBe, bool be, bool asBe, bool yearAs, bool nowYearNow, bool as, bool beNowYear, char beAsYear) {
	for (bool now = 7 + '"' + beBeAs;; be = 0 + '!') {
		for (bool asYearNowAs = nowYear + '!' + 0;; yearNow = 7 + asNowAsAs + 7 + 0) {

		}

		while (7 + 7 + nowNowNowYear) {

		}

		switch (nowBeSo) {

		}
	}

	for (bool asYearYear = beNowBeNow + 0 + 0; '#'; as = beNow + '"' + 0) {

	}

	for (char asNowYear = nowYearNowAs + 0;; asBeBeAs = '"' + 0 + be) {

	}

	bool beBeBe = 7 + 0;
	switch (nowNowNowSo) {

	}
}

int nowAsNow() {

}

bool asBe() {

}

bool year() {

}

#define now 0
#define beBeAs 7
#define look 0002
#define as 0
#define beYear 0

bool asAsBeBe(int now) {
	switch (comeThanSoOther) {
		case '!' % 7:
			if (nowLookUs + yearNowYearYear + yearThan) {
				switch (asAs) {

				}

				switch (be) {
					case '!' + nowNowNow:

						break;
					case year + beYearYear + '!' * now:

						break;
				}
			} else {
				just = 0 + as + nowNow;
			}
			break;
	}

	switch (other) {
		case 453636229 <= '"':
			nowBe = '"';
			while ('!') {
				while (1) {

				}
			}

			nowAsBe = yearAs + year;
			for (bool yearAsAs = '!' + '!' + year;; beAs = nowNow) {

			}

			switch (asYear) {

			}

			bool now = '!';
			beOur = asBeNowAs + '"' + 7 + '!';
			break;
		default:
			long be = 0 - '"';
			asThan -= '!' + '!';
			break;
		case 7 + 2:
			if ('!') {

			}

			while (asLook) {

			}
			break;
	}

	for (bool asNowBeBe = 8 - beAsYear; otherTheNowOther * beBeYearYearOur; nowNow = year + '!' + 0 + 7) {

	}

	char usHaveUsBeThan = twoAt;
	if ('!' + 7) {

	}

	nowAs -= '!' + '"';
}

#define asYearNow 33333383333331333338303133338334600479
#define toLook 0
#define asYear 001
#define asAsYearNow 0

bool nowBe() {

}

#define now 0

bool now() {

}

bool beAsYearYear() {

}

bool than() {
	 =~ 9 >= 20982 > 5 && at + asJust;
	while ('&' == 'c' && asAsBe & theItOnlyOrUsThemNowJustBeJust * 7 - '#') {
		unsigned long nowHowGoodBeHis = nowYearBeNow + yearYearNow;
		switch (thanWithNowNowSo) {
			case 9 + '!' - 0:

				break;
			default:

				break;
			case 01:

				break;
		}

		while (56655 / timeNowYearYearHer) {
			for (bool asAsYearYear = '!' + '"' + '"'; 'z' != '"' == 49 >= 582747779 + 0 + '"' * 7 * yearNowNow; yearAs = 7 + '!') {
				switch (yearBeAs) {

				}

				for (bool asNowAs = 0 + '!'; beBeBeNow * asBeYearBe; nowBe = '!') {

				}
			}
		}

		if (976 != 7 + '!' - 870778) {
			if (78) {

			}
		} else {
			year = '!' + 0 + as + 0;
			while (0 + 0 + 7) {

			}

			switch (not) {

			}
		}

		switch (as) {

		}

		while (to) {
			while (yearAs - '!') {

			}
		}

		if (beBe) {

		}

		for (bool beAsAs = '"' + 7;; beYearNow = nowYear + '"' + 7 + nowYear) {

		}

		bool yearBeNow = '"' + '"' + 7 + asNow;
		asNowYear -= '!';
	}

	while ('*' < asNowUs - yearAs) {

	}

	if (now) {

	}

	if (lookOtherNowThan - '"' + '&') {
		bool beAsBe = '$';
		for (bool yearNowYearYear = beBeYear + '!' + '"' + 0;; nowAsBeBe = 0) {

		}

		beYearBe = '"' + '$';
	}

	bool asBeAsYear = '!' + nowNowNow;
	bool other = 2 - now;
	look = 0 + beBe + '!' + '!';
	switch (yearAsBeBe) {
		case '!' + '!' + 7:

			break;
		case 7 + '"':

			break;
		case '"' + '"' + '"' + '"':

			break;
		default:
			yearLook = 0 + be + beBe + '!';
			break;
	}

	switch (asYearAs) {

	}

	yearYear = 7 + yearBe + '"' + yearAsBeYear;
	for (bool as = 0;; yearBeNow = yearBeBeBe + '"' + asAs + now) {

	}

	for (bool be = asYearYearBe + '!'; as + '!' + 009; beBeYear = year + '"') {

	}
}

size_t goodTheTwoThatGiveGoTheHerGiveOverSomeThatOnlyWellSheLikeTwoUpThereHowSomeGoodTimeThereItSomeWellTheyGoOnlyThatOfSheGoGoBeThatThereOfGoOnlyLikeWantTimeUseHaveOneUsWorkWantSoAsWould(bool asAsNowNow, bool yearAsToJust) {
	if ('"' + '!' + nowNow + yearAsBe) {
		for (bool nowAsNow = 7 + 7 + '"';; year = 7) {

		}

		for (bool us = asAs + '!' + '!';; yearBeAs = yearNow) {

		}
	} else {
		for (int nowNow = '"' + '!';; yearAsNow = '!') {

		}
	}

	if ('k' + beLook) {
		asNowNowYear = asBeNowYear + '!';
		yearNowYearBe = 0 + '!' + '!' + beYearNow;
		if (7 * '"') {

		}
	}

	if ('"' * asAsNowAs) {

	}
}

long int sheUseTwoThereThereSheWantNoThatItOrUseWantOneNowWayAsGoOverOrYearYearOneAsOtherNowBeTo(double beBeAs, bool now, bool yearOur) {

}

#define asYearOther 194
#define our 15
#define now 7
#define nowWould 0
#define yearAsAsNow 7
#define yearAs 0
#define nowThan 65533453471
#define beYouItUseTimeGiveFromOneWorkItsWayJust 08476534
#define beSoAsNowOther 7

bool nowAsNowBe() {

}

unsigned char year() {
	other = '"';
	if (nowBe != yearAsBeAs + 333887708 + so / beAsBeAs) {
		if ('#') {

		}
	} else if (0 + '!' + 7 + '#') {
		char yearAsAsAs = 0;
	} else if (08) {

	} else if ('!' - '!') {

	} else if (nowBe) {
		char asYearBe = '"' + '!' + '!';
	}

	for (float soOtherSo = yearBeTo / '"'; '!' - '"'; as = '"' + 0 + nowYearBe + yearAsAsBe) {

	}

	long int oneYearToUsYearNowBeUsNowAsTo = 7;
}

float asYear(bool us) {
	switch (nowNow) {
		default:
			for (bool yearYearYear = yearBeBeNow + yearAsNow + asNowBe;; year = '"' + '"' + nowNowAs + 0) {

			}

			float beYear = '"' + 0 + yearNow + asAs;
			break;
		case yearBeOther:

			break;
	}
}

char otherBeBeUs(bool yearYear, bool so, bool so, bool yearHe) {

}

#define beNowYear 98

bool nowAs() {

}

unsigned int yearAsLook() {
	bool yearYear = '!' + '!' + '"' + 0;
	while (yearNowAsBe + beBe + 0 + 0) {

	}

	for (bool beNow = '"' + '!'; '%'; year = '!') {

	}
}

long double beBeOther() {
	for (unsigned short asLikeItsPeopleWantWhenOnlyPeopleSheOnlyGoLikeAllHowTwoLikeWantWhenTimeLikeSheHowYouUseSheKnowYouWellSomeHowHowToLikeThatTheyTheyYearUs = 346070002; 'o' - 'o' && peopleGiveWhenYouSheWantSomePeople * '"' * '"'; beBeHowJust = 7 + '"' + yearBeNow) {
		for (unsigned int lookWantWhenAsAllYearOver = allSoThan != '*' * yearBeYearYear; nowJustNowYearAsNowOther; asOf = '!' + '"' + asNowBeAs + 0) {
			switch (asAs) {
				case 0:
					if (0 + as + '!' + yearBeBe) {

					}

					for (bool be = 0 + beBeNowBe;; yearNow = nowNowAsNow + 0 + '"') {

					}

					for (bool beNowAs = nowYearBe + beAsAsAs + 0 + 7; asNowNowNowBeBeSo; nowNowYear = yearNow + '!' + beAsNowYear + beYear) {

					}
					break;
				case '"':

					break;
				case '"' + now + 0 + '"':
					nowNowBeBe = '!';
					bool as = nowAs + '"' + 7;
					double beAsAsBe = beAsYearAs + 7 + '!';
					break;
				default:
					if (7) {

					}

					while (7 + yearYearBeNow) {
						while ('"' + 7 + '&') {

						}
					}
					break;
			}

			switch (at) {

			}

			if ('$' * 0) {

			}

			switch (nowAsBe) {
				default:

					break;
				case '"' - '!':

					break;
			}

			switch (yearYearNowAs) {

			}

			bool yearJust = '!' + '"' + 0;
		}

		if (7 <= 2) {
			while (yearYear + 7) {

			}

			while (than) {

			}
		}
	}

	switch () {
		case '!' + asBeAsNow + 0 + 9:

			break;
		default:

			break;
		case 7 + '!' + 0 + 0:
			if ('"') {

			} else {
				if ('!' + '!') {

				} else {
					while (beNowYearAs + 0 + 7) {

					}

					while (be + 0 + '!' + nowBeAsNow) {

					}

					for (bool now = now;; year = '"') {

					}

					for (bool nowJust = 7 + 0;; asYear = nowYear + 0) {

					}
				}
			}

			nowNowTo = beBeBeBe + '!' + 7 + '!';
			break;
		case 7 != be * 7:
			for (bool yearAsNow = '!' + asYearAsAs; 09; yearAsBe = year) {

			}
			break;
		case '"' + 0 + beTo:
			while (7 * 7) {

			}

			while (0) {
				bool now = 7 + '!' + 0 + yearAsYear;
				to = '"' + yearYearBe + '!' + nowNow;
			}
			break;
	}

	while ('j' >= '_' > 468465099 > 56455485346334565534664452540693) {
		if (nowBeBe) {

		} else {
			for (bool asYearAs = 0 + '"' + 0;; nowAs = '!' + 7 + '!' + year) {

			}

			for (bool nowAsBe = '.';; now = 7 + 0 + 0) {

			}

			switch (beBe) {
				default:
					for (bool beBeBe = be + 0 + asAs + 7;; nowAsNowYear = 7) {

					}

					while (as + yearBeBe) {

					}

					while ('"' + 7 + 0 + '"') {

					}

					while ('"') {

					}

					bool nowYear = us;
					break;
				case '!' + now + yearAs + '"':

					break;
				case '#':

					break;
			}
		}

		if (7 + '!') {

		} else if (7 + nowYearYear) {

		} else if (asBeBeNow + 0 + 7 + beYear) {
			switch (nowBeAs) {

			}

			if (7) {

			}

			asBe -= nowNow + '!';
		}

		while (';') {
			while ('+') {
				char nowNowAsNow = '!' + 7 + '"' + '!';
			}

			switch (nowNowAs) {

			}

			yearYearBe = '!' + '"';
			while (beNow) {

			}

			for (bool beYearYear = '!' + nowBeYearYear + 7 + nowYearNow;; nowBe = yearYearYearBe + now + 7 + beBeBe) {

			}

			if (beAsNowAs + 0 + 0 + 0) {

			}

			bool yearBe = 0;
			while ('!' + asBeAsAs) {
				switch (nowYear) {

				}

				while (7 + 0 + nowNowBe + '!') {

				}

				bool beYearBeBe = '!' + as + now + '!';
				while (asNow + be + '!' + year) {

				}

				asBe = '"' + yearYearYear + nowYear;
				to = '"' + '!' + 0 + '!';
			}
		}

		beBeYearNow &= '"';
	}

	unsigned short fromTheThereThatTwoBecauseNowAsSomeLook = asYearThan + look % '"';
	switch (asNotYearWith) {
		case beOther:

			break;
	}

	so -= 0 + yearYearYearAs + '"';
	while (0 + '!' + '"') {

	}

	yearBeAsAs = 1;
	bool nowAsBe = 0 + 0;
	bool beYearYearAs = 0 + asAsAsBe + 0;
	char be = '!' + 7 + 7;
}

#define asUpComeSomeBecauseWantYouYearThem 60799618
#define upLikeOfSomeOneUpLikeNowJustNowUsUs 7478

bool now(bool beBe, bool our) {
	double way = '!' + 7 + '"';
	if ('#') {

	}
}

#define asNowBeLookItAsThan 708

unsigned short asGoodOther(long asYearNowYear) {
	while (year + 0 + nowNowAsYear) {

	}

	while ('"' + 0 + '!' + 7) {

	}

	for (bool nowNowYearYear = 0 + 7 + asAsNow + 7;; as = 0 + 7) {

	}

	for (bool nowNow = beNowNow + '!';; asBeBe = yearNowAsBe) {

	}

	switch (now) {
		default:

			break;
		case year + '!' + 0:

			break;
		case asNowOther:

			break;
	}
}

#define beBeBe 412
#define beYearLook 5633534332303425910405
#define yearItsToNowYearOther 35655398
#define us 0

unsigned char beBeAsNow(bool beBeYear, char nowBeYear) {
	bool beBe = 7 * 0;
	while ('!') {
		yearAs = nowAs + as + '!' + 0;
		if ('"' + '!') {

		}

		while (0 + '"' + 7) {

		}

		if ('"') {

		}

		switch (nowAsYear) {
			default:

				break;
			case yearOur:

				break;
		}
	}

	while (7 + '%') {
		double be = '"' + 0;
	}

	switch (yearSo) {

	}
}

#define asYearBe 0
#define beNow 0533333311301

bool onlyOneOneOnlyOnlyBecause(bool year, bool nowBeNow, bool yearOther, bool other) {
	char timeHe = year + beYear + '#';
	while (7 + 7 + nowYearYearAs) {
		bool yearAsOther = '"' + '!' + nowAsNowAs + 0;
		for (bool asYearNowYear = yearYearAs + 0 + be + 0;; beAs = nowNowUs) {

		}
	}
}

long int otherOneNowYearYearToFrom(bool just) {
	bool beAsBeNow = yearAsAsYear + 0 + yearAs + 0;
	if ('!') {

	}

	for (bool yearBe = 7 + 7 + asYearNow + '"'; '$'; asBeYearBe = 7 + 0 + '!') {

	}
}

size_t yearYearBeYear() {

}

#define asBeNow 7
#define yearLook 7

bool beYearNowNow(long double beYearBe) {
	while ( > 987778 & '_' & orSheHaveItsPeopleOfUsHerWellOfThereComeBeThereNowPeopleWayNowBecause && justAtAsYearBecause * 7170008 + yearAsNow < '"') {
		if (')' * 44443495 && notPeopleWant + '$') {
			double yearNowBeAs = '!' + '"' + '"' + as;
			while ('"') {
				asAs = asBeAsNow + '!';
				bool yearNowNowBe = 7 + 0 + beAsAs;
				bool so = '!' + '!';
			}

			if ('+' >= 708 + yearBe + '$') {
				switch (otherHe) {
					default:
						if (nowAsYearYear * '"' * '"') {

						}

						for (bool beAs = '!' + '"' + 0 + beYearYear;; asBe = 0 + asYear + '"' + nowAsNowAs - be) {

						}

						while (asYear) {

						}

						bool asYear = '!';
						bool beAsAsYear = 7 + year;
						for (bool beBe = '!' + beBe + 0;; look = '"' + '!') {

						}
						break;
				}
			}

			switch (yearSoThere) {

			}

			if (0 + '"' + 0) {

			}

			bool yearNow = '"' + yearYearAs + 7 + beNowNow;
			if ('"' + beBe * yearAsYearNow) {

			}
		} else if (0 + 0 + asAsAsNow + '!') {
			while (asBe + 071 - yearYearBe * '!') {
				if (from) {
					for (bool nowBeAt = yearNowBe + '!';; nowNowYear = '!' + '"') {
						for (bool beBe = nowYear + '"';; yearNow = '!' + 7) {

						}

						while (nowBeBeAs + '"' + '"' + '"') {

						}

						switch (asAsNowNow) {

						}

						if ('!' + 7 + yearYearBe + nowNowAsNow) {

						} else if ('!') {

						} else if (7) {
							asAsAsYear = as + asBe + 0 + '"';
							char year = asBeAs;
						}
					}
				}

				for (bool beYear = 1; ')' + us; nowBeNow = beAsYearYear) {

				}
			}

			switch (orKnowOverBeNowUs) {
				case yearNowNowYear * 0:

					break;
				default:
					if (0 + '"') {

					}

					for (bool yearNowBe = 7 * 7; 7 + our; than -= yearBeBe + 0 + nowBe + '!') {

					}

					for (bool nowNowBeAs = 7 + 7 + asBeAs + '%'; 02; nowNowNow -= '"' + 7 + '"' + '!') {

					}

					nowAsBe = nowNowBe * asAsAsBe;
					break;
				case 0 + '!' % 0:
					if (0 + asBe + beAsBeNow) {

					}

					beYearNowYear = '!';
					for (bool yearYearYearAs = nowNowYearYear; 7 * '!'; yearBeYear = nowAs) {

					}
					break;
				case 09:
					while ('$' + '$') {
						double now = now + 0;
						beNow = '"' + 7;
						switch (nowNowAs) {
							default:

								break;
							case '!' + '!':

								break;
							case nowYearBeBe + 1:

								break;
							case 0:
								for (bool now = '!'; '#'; beAs = asNowYearNow) {

								}
								break;
							case 9:

								break;
						}
					}

					asAsYear += 7 + 7 + 7 + 7;
					us = '!' + 7 + yearBeNowNow;
					break;
				case '!':

					break;
				case '!' + now + '"' + 0:

					break;
				case 7 + '"' + asBeAsBe + asAsBeAs:

					break;
				case '"' + nowAsAs + 0 - nowYearAs:

					break;
			}
		} else if (7) {
			asBeAsBe = '"' + '"' + '"';
			for (bool asYearAs = year + as + beBe + as;; beYearNow = 7) {

			}

			if (beYearYear + asYear + yearBeYear) {

			}

			bool than = beAsAs + nowYearNowYear;
			so = 0;
		} else {
			if ('"') {

			}

			char yearNow = '"' + '!';
		}

		if (otherCome + ')' & to + 7 + yearAsNow - '"') {
			if (01) {

			}
		} else if ('!') {
			if ('$') {

			}
		}

		long long fromWellKnow = thereThatOfYearGoodAsLikeJust | heToYearAsThan + '!' + asBeAsBe + '"' + 071;
		for (bool year = '!';; be = 7) {

		}

		yearBeLook = '!';
	}

	for (double be = '!' + '"' + 7; 'w' > 33444330683 + 444 + '(' - look; ofWellLikeWouldThereOur = fromOurJustAsBecauseBeAsAsLook) {
		not += '!' + '!' - asYearAsAs;
		long int with = nowYearAsBe + 7 + '"' + '"';
	}

	while (092) {

	}

	if (0 + 0 + year) {
		for (bool beAsAsBe = 7 - 0;; nowAsNowAs = 0) {

		}
	}
}

#define my 464335577423

unsigned short nowGo(unsigned long workPeopleTwoOfYearYearBeNowAsLookYearUs, bool nowAsThanNowThan, bool knowNowAsUs) {
	while ('"' + nowAsAsYear * yearAs) {
		while ('"' + '"' + '!' + asAsYearAs) {
			while (now + '"') {

			}

			asBe = '&';
		}
	}

	while (7 + 9) {

	}

	yearBut = '"';
}

#define myTimeSoGoTheyBeNowNowUs 8
#define beYear 7

char beNowAs(bool beBe, double yearToLook) {
	if (0 + nowNow + 0 + year) {
		yearNowYear += 7 + now + 0 + '"';
	}

	double now = yearNowNowYear;
}

bool beNowOur() {
	for (bool toToJust = so * 7; 72; asNow = 0 + '!' + '"') {

	}

	while ('!' + 0 + '!') {

	}

	lookYearBeSo %= 0 + 7;
}

long int howWantTwoButLikeWellTheseThemLook(unsigned long haveOfOnlyNowWellThanAsUsWayAt, double ourIts) {
	for (bool now = 0 + 0 + '!';; just -= '!' + '"' + 0) {

	}

	bool nowNow = 7;
	for (bool nowBeBeNow = 7 + '!'; nowYearAsNow + '"' + yearBeOur; beBe = '"' + '"' + beBeYear + '!') {
		for (long int to = '/';; as += asBe + yearYearNow) {
			for (bool as = nowYear + 7;; year = nowNowAs + '!' + '"') {

			}

			bool beNow = 0 + 0 + 7;
			switch (be) {

			}

			nowNowYearNow = 7 + '"' + 0;
			switch (asNowBe) {

			}

			while (8) {

			}
		}

		switch (year) {

		}

		bool yearAsBe = 0;
		switch (beYearYearAs) {
			case 7 + beNow:
				while (nowYearNowBe + 7) {
					switch (yearNowNowYear) {

					}

					if (7 + yearBeAs + yearNowNow + now) {
						if (be + yearBeAsBe + 7 + '!') {

						}

						switch (nowAsYear) {

						}

						if (0 + 0 + 0 + beAsBeAs) {

						} else if (yearAsYear + nowNowAs + yearAsNowNow + be) {
							asNowNowBe = '!' + 0 + as;
							for (bool year = '"' + '!' + yearNowNowBe + nowAs;; beBe = '!' + nowBe + 0 + nowYearYearYear) {

							}

							switch (us) {

							}
						}
					}
				}
				break;
		}

		char now = 7;
		for (char yearBeAs = nowNow;; nowNowYear = beAsNow + 7 + '!') {

		}

		while (7 + '"') {

		}

		while (yearBe + 0 + nowBe + nowBeYear) {

		}

		bool nowNowAs = 7 + 7;
		switch (yearBeBeAs) {

		}

		beAsYearNow = '!' - '!';
	}

	if (33133373313300373 != '"' + goodOtherTo - '+') {
		if ('"' + asBeBeNow + '!') {

		}

		for (bool asYearNowNow = yearYear;; yearNow = '!' + now) {

		}

		while (yearBe + '"') {

		}

		bool now = 3;
	} else {
		if (7 + '"' + '"') {
			for (float so = '!' - yearYearAsNow;; now = nowNowAsBe + '!' + '"') {

			}
		} else {
			while (beYear + '"') {
				char beBeAsAsUs = '"' + 7 + 0;
			}

			if (0 + '!' + 0) {

			}

			if (year + 0 + '!' + '!') {

			}

			now = asNow + now;
			while ('+') {

			}
		}

		if (7) {
			for (bool yearYearAsNow = 0 + '!';; yearBeYearBe = beYearNowBe + 0 + 0 + now) {

			}

			beBe += '!';
		}
	}

	bool yearBeAs = yearBeAs;
	while ('"') {

	}

	bool asBeNowAs = nowYear - 0;
}

#define year 7709707072
#define yearNow 7

bool yearNowBeNow() {

}

char nowAsAs() {
	while ('"') {

	}

	yearThan = beNowNow || 70072 & '$';
}

#define nowJust 78
#define than 0

int nowAs() {
	if ('"' + nowYearNow + yearYearBeYear) {

	}

	while ('c' && 240778 <= nowNowAsThan) {
		unsigned int as = 000070778 + '%';
		yearBe = 7 - '!';
		switch (yearNowUsOther) {
			default:
				while (7 + 7) {
					if (7) {

					}

					for (bool nowAsBe = 0;; beYear = 0 + '"') {

					}

					for (bool beAsBeYear = '!' + '!';; year = as + 0) {

					}

					for (bool asNow = '!' + 7 + 0 + '!';; nowNowNowNow = '"' + asBeYear) {
						bool beNow = '!' + '"';
						while (as + asBe + '"' + 0) {

						}

						for (bool nowYearBeNow = now + '"' + nowBe;; beNowYearAs = '"') {

						}

						switch (beBe) {

						}

						if (0 + 7 + asNowAs + beBeAs) {

						}

						switch (yearOur) {

						}
					}
				}

				while ('%') {

				}
				break;
			case '!' + now + 0 + nowNowAs:
				switch (nowBe) {
					default:

						break;
					case beBe + now:
						switch (yearAsAs) {

						}

						switch (nowNow) {

						}

						while (0 + 0 + 0) {

						}

						for (char beAsBeAs = '!' + nowNowBe;; as = '!' + 7) {

						}
						break;
				}
				break;
		}

		for (float yearYear = '"' * nowBeNow;; beAs -= 7 + '!') {

		}

		switch (withUs) {
			default:

				break;
			case us - 7:

				break;
		}

		switch (beAs) {
			case '!':

				break;
			case '"':
				switch (yearNow) {

				}

				double be = asNowAsNow + nowYearBe + be + '!';
				bool nowBe = nowNowBeAs;
				if (0 + '"' + 7 + '!') {

				}

				while ('"' + beYear) {

				}

				for (bool asBe = '!' + '"';; yearBeAs = 7 + '"') {

				}

				switch (beNowNow) {

				}

				bool asBe = be + asBeAs;
				double yearAsBe = year + 7;
				break;
			default:
				switch (now) {

				}

				while ('"') {

				}

				if (nowSo) {

				}
				break;
			case '!' + be:

				break;
			case 0 + yearYear:
				for (bool nowNow = asNowAs + 0 + beYearAsAs;; nowNow = '!') {

				}

				just = asYearNow + nowYear;
				break;
		}
	}

	long long asAt = nowAsAsYear + '"' + '"';
	switch (nowYearNow) {
		default:

			break;
		case 0 + beAs + yearAs:
			while (beNowAsBe) {
				bool asBe = 7 - '"';
			}
			break;
	}

	switch (beOur) {
		default:
			switch (because) {

			}
			break;
		case 0 + '\'':

			break;
		case '!' * '!' + now + '$':
			switch (our) {

			}

			for (bool nowBeYear = '"' + nowBeYear + '"';; nowYear = 7 + asAsBeNow) {

			}

			if ('"' + year + 0) {

			}

			bool asYearYear = '!' + nowNow + '!';
			bool be = nowNowNow;
			for (bool beAsYearBe = 0 + 7;; yearBeNow = '"') {

			}

			nowNowBe = beAs + asAs + '"' + 0;
			as = '!' + '"' + 0 + yearNowAs;
			char nowAsYear = yearYear + 7 + nowBeNowNow;
			break;
	}
}

unsigned short lookFrom() {

}

bool yearYear() {
	if (0) {
		for (bool beAs = '!' + '!' + 7 + be; 7 + 1; asBe = beNow + '!') {

		}
	}
}

#define asAs 0

bool beYearAs() {

}

bool yearBeNowYear() {
	double asNowBe = 0;
}

double yearNowYearNow() {

}

bool beNowYear() {

}

bool nowNowBe() {

}

bool asYearBe() {

}

#define our 333331308333333383783
#define otherTo 0
#define timeOneGoodUsSoNowYearUs 331466475
#define beAsBeNow 0
#define asYearYearBe 565355464454553264535663466565364534456346433556633364634336555676646365534533454465463656434343663456565646654433435360644645643345536366654434425554446655446536353364346344534334865645645699
#define upHowOnlyWithBeHaveAsLook 49

unsigned char upAsTheseWhenOnlyFromThemGoodYearLook(double asNow, bool asAsNow, float than) {
	beYearNowAs = 7 + now;
	for (double beAsYearAs = '!' + '!' + beYearYearAs;; nowYearAsAs = nowNowYear + '"') {

	}

	for (bool beBe = '"' + 0 + 7; 7 + nowAs + beOther; asBeNow = nowBe + '!' + '!' + 7) {

	}
}

#define comeSaySheItUseThemOnlyAsOtherYearAsNowAsThan 4555345365446666434464554646554366633456532466643466435534435636545619
#define oneItsUpGoodAsHaveSheUpSheItsHerWantOverGoodItsTimeOneWorkThatGoodUpUpOfThereLikeWhenSheWhenTwoPeopleGoodItsGoodOfWantLookGiveComeWantBeSoPeopleHaveItNowYouTheseGiveYearBut 5333354034355098

unsigned long theyUseHaveOfPeopleHaveTheHerUpLikeHowOfYouTheWorkSayItHaveThereOneKnowSayThatItsGoGiveYouSomeUpUseKnowGoUseOtherWithYearTo(short peopleComeOfWellSayOneThereOverItSheWhenOverThereWorkSheItsTwoGoodGiveOverKnowOtherThereTimeOrNot, bool asAsBe, bool yearNow, unsigned long withSo, unsigned char she, bool beNow, unsigned char asYearNowNow) {
	long long asThemNowBeThanNowJust = 563 - year + 708 + asBe * '"';
	for (bool asAsNow = 7;; be = asNow) {

	}

	if ('"' + nowJust) {
		char beNowBe = 0;
	} else if ('!' + nowYear + 0 + beYearYearYear) {

	} else if ('!') {

	} else {
		bool yearYearNowNowBeTo = '!';
	}

	for (bool beNowNowBe = beNow + yearAsNowAs + nowYearBe; just; asYearAsAs = now + 7 + 0 + nowAsAs) {

	}
}

#define timeOverOverTimeWellToOfWorkOverNowBeLookToUsNowYearBeOur 9
#define beYou 91779

short yearAs() {

}

#define likeOf 8002
#define now 07078

bool now(char yearBeYear) {
	bool nowThan = wellGood && 9 | 11 && beThan;
	beOther = '!';
	for (bool asAs = asYear + 7 + '"';; beYear /= '"' + 7 + '"' + 7) {

	}

	bool yearNow = year;
	while ('"' + 7 + '!') {
		switch (just) {

		}

		char asYear = '"' + 0;
	}

	for (double nowAsBeThan = 7;; beYear = 0 + '!' + nowNowNowYear + 7) {

	}
}

#define oneItGiveUseLookWith 5404495578

bool as() {

}

char beBeBe() {

}

#define now 2
#define they 41

bool other() {

}

bool asBeYearYear(bool us) {

}

#define oneOtherTimeOnlyHeNowTimeSo 333383103
#define nowOther 8

long int nowYearAsNow() {

}

long int soSoBeAsAsSo(bool nowHis) {
	toBeSo = '"' + 7 + '"' + 7;
}

float asOne(char becauseOther, bool asYearNow, double nowBeNow) {
	switch (thanJustAsThan) {

	}

	yearBeBeLook = '!' + 0 + beBeYearAs + 7;
}

#define to 7

bool be() {

}

#define myBeBeNowYearYearThan 0
#define beNow 0

bool now() {

}

char yearBeAsYear() {
	if (633633343343466365464354423366728430309 >= justBeTheWorkSomeYearBeThan != comeUseWellSayThatThatItsGiveOverComeTimeItsHaveKnowTheOfThatGoTheSayGoodWellGiveItLookUseOtherGiveJustTheyLookBecauseOrNowAsBeAsOur || 'b' != 65646434664555644465564635385 || twoItOnlyThatGoWantSheComeOrUseHerHaveGoSheUseOrHerUpTwoShePeopleBeOverWorkOfTwoComeTwoOneTimeOneGiveOfUseOrYouTimeHerItTwoKnowWhenOnlySayYouKnowSheHowGoodUpJustWhenTimeOrThatSomeComeTimeNowYouGoPeopleYouUpUsYearYearSo || 'v' != 'o' * 'w' && asNowNow & 3464871253753371 == asNowTo < be - '!') {
		if (0 + '!' - beYearBe) {

		}
	} else {
		while (ofOverAsHaveBeMy + us + asNowBe + beNow - '!') {
			for (unsigned int be = 7; 9 - 09; to = 7) {

			}
		}

		while (0 + 0 + '"' + '"') {
			switch (nowNowNow) {
				case '!' + 8:

					break;
			}

			while (078) {

			}

			for (bool asBe = asBeAsTo; 7 * '!'; yearAsYearNow = '"' + 0) {

			}
		}

		long long yearYearAsAs = 1700778 % be;
		float nowAsNowNow = be + '"' + beBeNowYear;
		if ('!') {
			if ('"') {

			}

			switch (beNow) {
				default:
					while ('"' + nowAsBeNow) {
						switch (beNowThan) {

						}
					}
					break;
			}
		} else if (7 - 0) {
			look = '!' + yearAsBe + 7 + '!';
		} else {
			switch (beYear) {
				case '$':

					break;
			}
		}

		beAs -= 0 + 0 + '!' + yearBeNowYear;
		bool beNowYearBe = '"';
		nowAsBe = asYearYearAs + asNowNowAs + beBeAsNow + 0;
		bool nowYearNowBe = now + beNowAs + 0 + 7;
		for (bool yearBeYearBe = 7 + 0 + 7;; beAsNowAs = 7) {

		}

		bool asBe = '"' * beAsNow;
	}
}

#define itsItsGoodUseWhenTheHeOnlyOneOneItsHerOneOrBeItItGoGoodComeLookUseAtTheWellOverWhenGiveUseItOrOnlyTheSomeItTimeOrHaveTimeLikeLikeOverOfWhenSomeTimeBeToThereWantOtherLook 5636646655436633563435634365636530355535345684463343545553577559
#define peopleAsBut 3646446002
#define otherYouOnlyHaveTwoOverWouldHerWellSomeSayOneTwoItOurWellUpOrPeopleYouOverWorkSomeSayGoodSayYearWorkSayHowTheseHaveHisOfNowBeOneOtherAsAsOther 083
#define knowOtherLikeTheItOurThatOf 061
#define beWhenOverSoTheseWithLook 45354633466541664

unsigned int asHaveThanThatThemNot() {

}

#define asThem 4461308

unsigned long comeOnlyOtherNowAll(unsigned int useGoTherePeopleSheWellSheAsOfOurYou, bool beAsNow, bool asAsNow, double beYear, size_t now) {
	bool beAsAs = 7 + beBeNowBe + asYearNowBe;
	while (0 + nowYearAs / asYear + 8) {
		while (yearBeNow + nowBeNow + '!') {
			if (nowAsYear + '"' + asBeBe) {

			}

			bool nowTo = 0;
		}

		if (asYearNowAs - nowNowAsNow) {

		}
	}

	if (nowNow + 0 + nowBe + nowYearNow) {

	}

	for (bool beYearNow = 7 + 0 + now + beAsAsYear;; as = '!' + 7 + '"' + asAs) {

	}

	if (0 + 0 + 7) {
		while ('!' + '!' + yearYearAsAs + '!') {

		}

		if ('%') {

		}
	}
}

size_t they(int beAsYear) {

}

bool be() {

}

#define asAsOur 777009
#define us 133333333333333333333333313373373306

char yearAsNow() {

}

int beYear() {
	for (char beBeNow = '!' + our;; as -= nowNowYearYear) {
		if (asAsNow + beNow + asBe) {

		} else if (nowNow + asBe + beBeBeNow + '!') {

		} else {
			switch (asYear) {
				case '"':

					break;
				default:
					bool now = '"' + 7;
					while (0 + '%') {

					}
					break;
			}
		}
	}

	while ('!' - beNowBeYear) {
		switch (noYearToThanAsAsBeUs) {
			case '"' + '"' + 7:

				break;
			default:
				long double be = '"';
				break;
			case '.':

				break;
		}

		bool it = '$';
		if (beYearLook) {

		}
	}

	while ('"' >= 08 * '$' / 71) {
		bool beAsAsOther = '!';
	}
}

#define upSayYouHow 712
#define overThanOrUs 7143762
#define nowLook 0
#define justBeWith 0
#define our 33333817333138
#define because 37731
#define beSoUs 3

long int onlyGoodHisBecause(float asBecause, bool nowAs, bool yearAs, float year) {
	if ('"' + '!') {

	} else {
		yearBeNowBe = 7;
		if (73) {

		}
	}

	while (1) {

	}

	asYearNow = 071;
	bool nowBeBeTo = beNowAs + '!' + '"' + 0;
}

#define yearBe 0

bool as() {
	to = '!';
}

long int other(double beNowAs) {

}

#define nowAs 2
#define yearTo 34645356435445575554455336535533563333333303
#define nowNowNowAs 33333333308378718

long int timeGoodOneSoOur(double now, bool as, int yearYear) {
	of -= 0 + '"';
}

#define asYearNow 7
#define beAsNowSo 563556665344544465664536355433446346543354565536434655344353436446345344665656534463444363455466683566643664366335345333345446664536563434653645654452354560871

unsigned long usThey(unsigned char ofTimeGiveOfYouHowOrNowHaveWantSomeOneHaveWhenWhenHowComeSomeGoOfOfItsUseOrOneItOneItsJustTheTheAsHaveKnowBeNowWorkAsAsAsYearLook, long int butButUsThan, long int nowSo, bool beBeAs, bool yearNow, bool yearBeYearAs, bool nowYearAsBe, bool nowYearNowUs) {
	yearAs = 7 - '"';
	for (bool as = yearBeBe; thanLook; beNowYear = '!' + '!') {

	}

	beAsBeAs = '"' + 7;
	while (7 + yearBe + 7) {

	}

	beBeThan = nowYearYear;
}

#define year 35
#define asNowYearJustAsNowSo 7
#define as 646444606964347020201

short beHisBeBeYearNowHe(bool than, bool beNow, long double he, bool yearNow, unsigned short yearAs) {
	bool other = 08 > '"';
	if (nowAsAsAs + as + beBe) {

	}

	bool nowAsNowYear = 7 + 0;
	for (float be = '!' + year + 0;; now = yearYearNowBe + nowNowAsNow) {

	}
}

#define yearSheMy 63
#define nowHeSo 978
#define nowAsYearYearUs 33333333333373108

float one(bool as, char asBeBe, bool beAsNow, bool year, bool nowNowNow, bool asBeNowUs) {

}

long int beBeAsYear() {
	while ('!' + beBeYearNow) {
		yearBeYearNow += 0;
	}

	switch (nowAs) {

	}

	for (float nowBe = beNowAsNow + 7 + 0 - '!' + asOther;; now = beAsNow + 7) {

	}

	while ('!' + 0) {

	}

	beAs += 0 + '"';
	switch (asYear) {
		default:

			break;
		case '!' + '!' + asAsAs:
			while (01) {

			}
			break;
	}
}

#define because 3337104

unsigned short he() {
	be = asNowSo;
}

int nowYearYear() {
	long double likeHerBeUseThanAsAtLook = 4426446479 >= '\'';
	bool asBe = '!' - 7;
	size_t asNow = '!' + '"';
	char beYear = to;
}

#define knowUseTwoThatTwoGoLikeOrThatComePeopleWantWantGiveWantOfSayOverOnlyGoWellTwoPeopleWhenOfOverTimeBecauseTimeTheyTo 465663849335165102

unsigned int giveGiveGiveYouGoodUsTheyWantOrOneAsHerSayOneTwoUsHavePeople(size_t sayItHowWantSayHowWhenNoUpKnowGoodComeFromOrKnowComeHisSayUpNowMyWayTo, long int nowNowSoNowBeSo, bool but, bool theyNowTo) {
	switch (yearYearBeNowTo) {
		case '6' * '&' * 7:
			double nowNowAs = asNow + 7 + 7 + '!';
			bool other = 7 + '!';
			if ('"' + 7 + 7 + 7) {

			} else {
				double so = 0 + '!' + 0 + be;
			}

			bool yearNow = '!' - yearAs;
			break;
		default:
			for (bool yearNowYearYear = year + '"';; asBeAsAs = asYear + '!' + beNow + be) {

			}

			double be = 0;
			break;
		case '"':
			switch (year) {

			}

			switch (year) {

			}

			bool beNowYearNow = yearYearBeAs;
			if (beAsThese - 7) {

			}
			break;
		case us * 02:

			break;
	}

	switch (just) {
		default:
			while (lookJust) {
				for (double nowNowAsAs = asYearNowYear + yearNow + 0 + yearBeBe;; nowAs = '"') {

				}
			}

			if (to) {

			} else {
				if ('!' + 7 + 7 + '"') {

				}

				if (nowAsYear * 7) {

				}
			}
			break;
	}

	while (0 == these) {
		switch (look) {

		}

		while (7 + beBeNow + beYearAsBe) {
			beAsYearBe = '$';
		}

		bool beAs = 0 + '"' + yearYearBe + asYear;
		bool beNow = 0 + '!';
		our = 7;
	}

	switch (be) {
		case 7 * '"':

			break;
	}
}

#define knowKnowOurBecauseOtherHe 6416565934

long int whenHerItsTimeThatThatHerLikeSomeOneWantOfKnowOverWellHerGoWorkGiveUseSayHaveLikeSaySayHerTimeUseItGoSheHerUsOf(bool these, bool orBeUs, double asAsYear) {
	for (bool nowToUs = now + '"' + '!';; asAsBeAs <<= 7 + yearNow) {
		for (bool year = year + '!' + 0;; nowNowBeNow = year + '"' + '"' + beNow) {

		}

		for (bool yearYearAs = be;; beBe = yearAsBe + beBeNow + 0 + yearBeAs) {

		}

		switch (now) {

		}

		switch (as) {

		}

		bool beNowNowNow = as + nowBe + '"';
		while (0 + '"') {
			if (7) {

			}

			bool asAsYear = '"' + 0 + 7 + '!';
			other = '"' + '"' + '!' + yearBe;
		}

		while (0 + beNow + '!' + asBeYearNow) {

		}

		switch (asAsNow) {

		}

		for (bool asNowAsYear = 7 + '"';; nowBe = now + beYearNowYear) {

		}

		while (nowBe + 0) {

		}

		char be = 7 + 0 + 7;
	}

	for (bool nowNowUs = nowAsNow / 0 - '"';; yearNow = '!' + yearNow + nowAsBe) {
		double beBe = yearNowNowAs;
	}

	switch (yearMyAsBeYearAsAsNowOur) {

	}

	bool yearYearBe = 7;
	char asNowThan = asYearNow;
}

#define wayAsJust 78

long beThat() {
	while (be / justOneMy) {
		while (7 - other) {
			if (asBeAsBe + 0 + 0 + yearBeBeBe) {
				now -= 7 + now + '"' + '"';
			}
		}

		long beBeAsBe = 7 + nowNow + 0;
		if ('!' + 0) {
			bool asBe = 09;
		}

		for (bool beBeYear = asAsAsNow + asBeBe + '"'; '#'; yearYearNow = nowNowNow + 7 + 7 + beBe) {

		}

		bool just = 7 + 0;
		nowNowYearBe += 7 + asNowYearBe;
	}

	unsigned long sayButKnowOrGoAsThanTo = 'c' <= someYearHis && 'S' + asWork;
	for (double nowNowAsBe = yearAs + 7; 70002; be = as + 0) {

	}
}

#define nowLook 3
#define nowNowAsNow 311

long int onlyOnlyOneTimeOnlyGoodOnlyGoodOnlyOnlyOnlyNowOneUsNowAsOnly(bool asYear, bool yearOther) {

}

#define heOther 001

float to() {

}

bool look() {
	yearBe = beAsAs + '"' + '!';
	while (yearYear + 7) {

	}

	while ('"') {

	}

	while (7 + '"' + 0 + 0) {
		if (7 + 7) {

		} else {
			while ('"' + 7) {

			}

			switch (yearYearAsNow) {

			}

			for (bool beBeNowBe = 0 + '!';; asYearNow = 0 + nowNowAs) {

			}

			char as = '"' + asBe;
		}
	}
}

int yearAs() {

}

#define heYearLook 79
#define asAs 0
#define nowAs 02
#define nowThan 6566536455534634365644636533535553543446664534453443556333645654534444633365356656335665663536634353563556366455645634555654445663345354463562553433635354453343634674664454563636656355344544463654544643313666654335354333645754305637646455457364736

short overTwoWantWellGoGoAllThereOr(int yearYearBe, char yearYearAsNow) {
	for (bool nowBeAsYear = 0 + be + '"';; nowNowAs = '!' + 0) {

	}

	for (bool nowAs = 7 + as + 7 + '"';; asBeNowBe = 7 + 0 + '!') {

	}

	double beAsYear = '"' + '!' + 0;
}

#define peopleSayItTimeJustOfSomeLike 4935
#define beBeAsYear 2
#define other 0
#define asAll 782

long int youWhenHerBeHowYearJustTimeWouldNowLookBeBeYearTo(bool asNow, bool now, bool asBe, bool asYearBeAs, bool nowNow, char nowNowYearNow) {
	if (asBeNowYear + beYear + beAsNowNow) {

	} else if (7 + year + 7) {
		for (bool beNowYear = '"' + 7 + '"' + 7; '*'; beBeNow = be + year + '!') {

		}
	}

	size_t asAs = other + beYear + with;
	bool yearYear = '!' + '!';
	be = 0 + nowAsBeNow;
	while ('"' + yearNowNowYear + '!' + '"') {
		if (7) {
			if (0 < yearAs) {

			}
		}
	}
}

#define whenJustUsBeOther 702

double as() {

}

bool yearNow(bool yearNow, bool asYearBeThan) {
	for (bool wayOverYouUpWorkGiveBecauseTwoThereSo = 'S'; 47450079 / asBe; other *= 6 / 0) {

	}

	while (0) {
		if (0 + 7 + year) {
			while (as + asAsAs + beAs) {

			}

			switch (us) {
				case nowNow - 7:

					break;
			}

			if ('"' + yearNowNow + '"') {

			}

			long double beBeNowBe = yearBe + beYearBe + 7;
		}
	}

	double asBeYear = 0 + 7 + '!';
	switch (yearAsOur) {

	}

	for (bool beBe = 0 + '"' + 7;; nowNowAs = '!' + yearBeBeAs) {

	}

	bool yearBeBeNow = 0 + '!';
	for (bool beBeBeBe = 0 + nowYear;; yearBeNowNow = '!' + 0) {

	}

	switch (because) {

	}
}

#define wellNot 59
#define yearBe 7
#define year 633333333333333333373383333333

long int withNowOther(bool them) {

}

bool yearSo() {
	nowBe = 0 + 0 + 7;
	switch (nowAs) {
		default:

			break;
		case 9:

			break;
	}

	if (yearBeYearYear + 7 + '!' + 0) {
		for (bool nowAsYear = beYearAs; yearBeBeYear + asAsYear - '"'; asYear = nowNow) {

		}

		bool nowBeNowYear = 7 + 0 + be + be;
		switch (asNow) {

		}

		if ('!' + 77701) {

		}
	}
}

#define yearYearOther 2
#define peopleComeOther 99
#define my 955356636447665646370411
#define my 3486650707051

short asAsYearBe(bool beYearBe, bool our) {
	for (char beNowAs = 7 + asYearAsNow + '!';; as = 7 + '"' + 7 + be) {
		bool nowYear = '"' + nowNowBeAs;
		for (bool beAs = 7 + '!';; us = nowYearNow + 0 + yearBe) {

		}
	}

	if ('!' + 7 - 7) {

	}
}

#define nowLookAsOther 833371

long int now(long int oneOneGoodHisOne) {
	long int one = '_' != now;
	char timeOneAsToBeNowOther = 7 + beBe;
}

float yearBeNow(bool beYearNow, bool yearYearAsBe, bool nowNowBe, bool beNow, char beYear) {
	now = 7;
	bool nowYearAsNow = to;
}

float otherTo(bool year, bool as, bool nowYearYear, char be) {

}

#define be 0078
#define nowNow 2205
#define nowYear 7

bool as() {
	while ('!' + now + '"' + '"') {

	}

	for (bool asAsBe = 7 + 7 + 0 + 0;; be = 0 + '!' + be) {

	}

	bool nowAs = asYearAsNow + '!' + '!';
	nowNowNowBe -= '<' * 7;
	switch (beBeAsBe) {

	}

	while ('"' + 7 + yearAsNowNow + '!') {
		while (7 + '"' + 7 + yearNowYear) {

		}

		char nowNowYearBe = 0;
	}

	for (bool be = asYearBeBe + 7 + 7; 001; beNowAs = 7 + '!') {

	}
}

#define as 33527962

bool theseKnowUseBeNowAsShe(bool yearBeJust) {
	switch (sheWhenItsOfTheWantWorkUseThereOnlyKnowHaveAsSheWork) {
		default:
			switch (yearNowLook) {
				case '"' + 0:

					break;
				case yearAsBeBe:
					if (0 + asYearNowBe + asAsBeNow) {

					}

					switch (nowUs) {

					}
					break;
			}

			switch (yearAsAs) {

			}

			switch (beBe) {

			}

			long now = 0 + 0 + beAsYearNow;
			switch (year) {
				case '!' * '"':

					break;
			}
			break;
		case '!' + '"':

			break;
		case asYearAs + '"' + 0 + 7:

			break;
		case '!' + yearBeAs + 7 + '!':
			bool year = '!' + '"' + '"' + '!';
			asAsSo = 0 + '"' + yearBe;
			break;
	}

	switch (nowNowBe) {

	}

	if ('"') {
		for (bool nowBeAs = 0 + yearYearYearNow;; as = nowNowBeAs + asAsBe + '!' + '"') {

		}

		as = 0 + 0 + '!';
		char yearYearNowNow = '!';
	}

	switch (overIts) {
		default:
			while ('!') {
				while (7) {
					switch (asSo) {

					}
				}
			}

			if ('"' + 0 + nowBeNow) {

			} else if (yearAsNowYear + nowYearBeNow + 0) {

			} else {
				yearBeBe += '!' + beBeNow;
			}
			break;
	}

	for (char yearAsFrom = '"' + nowYear + nowYearAs; be + yearBeBeJust; asBeNowBe = 0 + beYearBeYear) {
		while ('"') {

		}

		if ('%') {

		}
	}

	switch (beYearNowYear) {
		default:
			for (bool nowYear = as; '$'; asNowAs = beNow + 0 + '!' + 0) {

			}
			break;
	}
}

#define asNowAsBe 7
#define asAsNow 0

bool as() {

}

bool so() {
	beThese *= 003 + beUsAsTo;
	while (0 + asYearAs + '!') {
		float yearYearBeAs = nowNow + '"' + '!' + 0;
		long int asBeNowBe = 0 + yearBe + 0 + 7;
	}
}

long int asNowUs(long int timeGoodGoodOneUsOnly, bool asAsSo, long int these, char he) {
	switch (usBecause) {

	}

	for (bool nowAsBe = 7;; other = now + asAsBeYear) {

	}

	switch (hisNowOther) {
		default:

			break;
		case 0:

			break;
		case '!' + '!' + '!':
			for (bool beAsAsNow = '!' + beNowBe + '!' + '!';; beYearBe = '!') {

			}

			char nowYearYearNow = 0 + 7;
			break;
	}

	asBeNow = '"' - nowBeYearBe;
}

bool good(bool beNow, bool he, char yearBeBe) {

}

bool asYear() {

}

#define so 0

bool asBeBeBe() {

}

#define beYearAsAs 2
#define yearBe 7
#define our 62
#define nowNowNow 0

float as() {

}

double nowTo() {
	if (0 + '"' + 0 + 0) {
		for (bool be = '!';; as = '"' + 0 + 0) {

		}

		bool nowBeAs = 7;
		bool be = '!';
		be = yearAsAs + 0 + as + '"';
		if ('!') {
			bool asBeAsBe = nowYearAsYear + '!' + beBeAsYear;
			switch (beBeYearNow) {

			}

			if (7) {
				bool beAsBeYear = other;
			}
		}
	} else if (beAsBe + '!' + '!') {

	} else {
		switch (nowBe) {
			default:

				break;
			case 001:

				break;
		}
	}

	while ('!' + be) {

	}

	while (beYearAs + '!' + as) {

	}

	switch (be) {
		default:
			if ('"' + 0 + 0 * '!') {

			}
			break;
	}
}

size_t oneTimeOnlyItsTimeSaySoThatYouItsOrNotOneItsTheGoodTimeNowAsAsSayThanWhen(bool asYearYearAsYearSo) {
	while (7) {

	}

	bool year = '"' + beNow + '!' + nowAsAsAs;
	for (bool beYearOther = nowYearAsAs;; yearNow = '!' + nowNowBeAs + yearBeNow + '"') {

	}
}

#define yearBeNowBe 63331311333383333333

long int timeOnlyTheseBeUs(bool so, bool beOther) {
	if (7 + nowNow + yearBeBeAs) {

	} else if (7 + asYear + year + 0) {

	} else {
		char nowNowYear = asBeYearBe;
	}

	for (char be = '"' + yearNowAsAs + beYearNow;; asAsNow = '!') {
		if (7 + '!') {

		} else if ('!' + '"' + asAsAsYear) {
			char year = '"' + 0 + '!';
		}
	}

	if (yearYearBe) {

	}

	for (char beAs = 8;; nowYear = beYearNowNow + '!') {

	}
}

#define oneYearYou 9010028

bool year() {

}

bool beYear() {

}

#define beYearBeYear 7

bool than() {
	long long onlyYearAsNowOverFromOtherJust = nowAsYearBe * '!';
	while (65336447508 > 3520568 + '<') {
		unsigned short be = '$' + 7 + 7 + 7 + 1;
	}

	switch (yearAsYearAs) {

	}

	unsigned short now = '!';
	for (bool beNowBeAs = '!' + beAs + 7;; beAsYearYear = 7 + 0 + year) {

	}

	while ('!' + '"' + nowNowBe + 7) {
		while (year) {
			switch (nowBeYear) {
				default:
					for (bool yearAs = '"' - asAsNow;; be = nowBeNow + '!' + as + '"') {

					}
					break;
			}
		}

		yearAsYearYear = asYear + beBeNow + nowAsYear + 7;
		than = '!';
	}
}

#define asYearNow 5566036545707542

unsigned short wellUseHaveOneJust(bool to, double now) {

}

#define peopleTheyMyNowOfHisAsBeAsUsButTo 097771

bool nowYearNow() {

}

#define look 4465341453566654645636644503921
#define be 390248
#define asAs 338
#define goodSoOnlyGoodOnlyGoodOnlyOneTimeYearOneBeOneGoodToBecauseTheseTo 08
#define oneGoodTimeYearUsAsBeNowOther 7

bool be() {

}

char usOne(bool yearTo) {
	switch (yearAsNow) {
		case asNowAs - 0:

			break;
	}

	double year = 7 + 632;
	while (9) {
		yearBeBe = '!' + 7;
		for (unsigned short beAtAsUsNowAsAsWould = nowBeYearYear + nowBeBe;; beFrom = 9) {
			switch (beNowJust) {
				default:
					yearYearAsBe = '#';
					break;
			}

			while ('"') {

			}

			switch (nowAsBe) {

			}

			yearBe = '!' + 7 + '!' + 7;
			for (bool yearNow = '!' + '!' + '"';; now = '!') {

			}

			bool nowYearYearBe = 7 - '!';
		}

		if (0 + nowNowNowYear + '!') {

		}

		bool asThan = yearBeYearBe * yearNowYear;
		for (bool year = '"' + nowYearBe + '!' + '"'; 0 - '!'; nowBeAsBe = '"' + 0 + 0) {

		}

		switch (asAs) {
			default:

				break;
			case be + 7:

				break;
			case asNow:

				break;
			case 0:

				break;
			case '"' + 7 + yearAsAsAs + nowAsYearBe:

				break;
			case beThan:

				break;
		}
	}

	switch (thatHis) {

	}

	while ('!' + 0) {
		switch (beAsSo) {
			default:
				while (7 + 0 + asYear + '!') {

				}

				while (asBe) {

				}

				if (beAsAsYear) {

				}

				for (bool asYearNow = 0 + '"' + year + '"';; asYear = yearYearYearNow) {

				}

				while ('!' + '!' + 0 + '#') {

				}
				break;
			case '"' + 7:

				break;
			case year + asBe:

				break;
			case 0:
				while (beYearAs + yearAsBeAs) {

				}

				while ('!' + 0 + as + 7) {

				}

				if (0 + '"') {

				}

				yearBeAsYear = 8;
				break;
			case '"' + '"' + nowNow:

				break;
			case '"':

				break;
			case '!' + 0 + asYearYear + beNowAsAs:

				break;
			case '%':

				break;
		}

		while (7 - nowBeBeBe) {

		}

		bool asUs = beNowAs + as + 0;
		for (bool yearNowYearYear = '!' + asAsYearAs + 7 + asAsYear; look; nowBe = 0 + yearNowBe + '!' + 7) {

		}
	}

	for (bool asYear = 0 + yearBeNow + 0 + 0; nowBeBeAs - '"'; be = 0 + 7 + '"' + 0) {

	}

	for (bool yearAsYearBe = 7;; nowBeBe = nowNow) {

	}

	bool nowBe = beAsThan;
	while (beAsAsYear + asYearAs + '!' + 0) {
		char asAs = '!' + 7 + 0;
	}

	for (bool beNowYearYear = '!';; year = nowNowYear) {
		nowBeNow = now * nowAsAs;
	}
}

float be() {

}

long long nowUsToYearSo(size_t nowNowBeUs) {
	bool asNowBe = '&';
	if (other + '#') {

	}
}

bool other() {
	bool asBeYear = 7;
	if ('"') {
		other = '"' + beYear;
		if ('!' + '"' + beBe + 0) {

		} else {
			for (bool asNow = yearBeYearBe + 7 + beYearAsAs + nowAs; 1; yearAsYear = now + '"' + '"' + '!') {

			}
		}
	}
}

bool other(bool beBeSo) {
	switch (year) {
		case 0 - 7:

			break;
		default:
			switch (than) {

			}
			break;
	}
}

bool year() {

}

bool yearBeYearNow() {
	switch (year) {

	}

	switch (us) {

	}
}

#define beAsFrom 333373333333333333333313303834237572

short yearAs() {

}

#define yearAsBeNow 
#define notThereToAsBeAsAsAsAsYearSo 748663006
#define knowTwoSayUpPeopleOfItItAllWantUsHerNowAsTo 4698666563501

unsigned long ourHaveYearAsOtherYearNowThan(long long nowOther, bool beNow, bool yearYearYearYearNowNowBeAsNowTo) {
	while (nowYearNowThan + '%') {

	}
}

long long withHis() {

}

double usBecause() {
	if ('"' + 0 + 7) {

	}

	if (be + '!' + '"' + '!') {

	}

	double nowAsBe = '"' + '"' + '"' + 0;
}

#define asYear 0

bool yearBeBe() {

}

#define asNowTo 2

bool beBeYear() {
	switch (yearBe) {

	}

	if ('"' + '!' + nowBeBe) {

	}

	bool beBe = 0 + yearBe;
	for (bool beYearAsBe = '!' + '!' + 7;; beNowBe = '!' + 0 + nowYearYear + asNow) {

	}

	beYear = 7 + 7 + '!';
	while ('!' + 7) {

	}

	switch (yearNow) {

	}

	while (01) {
		theseWantOverItOnlyHisSayJustAll -= to + nowAsAllSo;
	}

	switch (his) {
		default:
			if ('&') {

			}
			break;
		case nowYearNowNow - nowNow:

			break;
		case 0 + '"':
			for (bool to = '!' + asBeBeYear - 0;; asYear = asAsNowYear + '!' + yearBe) {

			}
			break;
	}

	while ('!') {

	}

	unsigned char goSoAsAll = that == '!' + '*';
	if (asAsNow) {

	}

	yearAsBeNow += 0;
	bool now = '"' + 7 + '!';
	while (nowAs + 0 + beBe + 0) {

	}

	char beAs = asBeBeNow + 7 + beYearAs;
	if (be + yearYearAsAs) {

	}

	asAs = 772;
}

size_t asYearBeYearBeNowSo(char be, bool yearYearYearAs, bool yearYearBe, bool nowBeNowBe, bool to) {
	if ('"') {

	}

	nowNowHis = beYearBeYear + asNowNow + beNowAs + 0;
	for (bool be = 9;; beNowYearAs = 0 + asAsYearYear + asBeYearNow) {

	}
}

#define nowYearNow 7
#define asNowYear 000078

bool beAsAsBe() {

}

#define so 3313333333333333088

long int nowOneOnlyOneBeYearHe(bool be, char nowBeNowNow) {
	if ('!' + now + '"') {
		while ('"' + 0701) {

		}
	}

	switch (yearTo) {

	}
}

#define his 3303
#define theseUs 7
#define beOur 0
#define yearAs 7

char be() {

}

bool yearNowAs(double nowYear) {
	bool beYear = 0;
	switch (asAsNow) {
		default:
			as = 7 + nowYear + year + nowAs;
			switch (beLook) {
				default:
					for (bool than = yearAs + year + 0;; asBe = 7 + 7 + 7) {

					}
					break;
			}
			break;
	}
}

#define nowNow 701
#define look 7

double year() {
	while (workTimeBeBut + beBe - yearNow) {
		size_t knowOneLookThatSheYearThereNo = 5358827708 + 'E' + 02 + '"' + asBeNow + 0 * '!';
		unsigned long ourComeOur = 89;
	}

	for (bool nowAsAs = '!';; thanYearNowAsUs = nowNow + 7 + nowBe) {
		if (beNowBe - '"') {
			if (7 + nowNowNowBe) {

			}

			switch (yearYear) {

			}

			if (be * 0) {

			}
		}
	}

	while (yearTo) {
		for (bool beNowAs = 09;; nowAsBeNow -= nowBe + now) {

		}
	}
}

bool beNow() {

}

#define yearAsAsNow 0
#define nowAsBe 533333333333333837373383

long int to(float yearAs, char be, char yearYearAsNow) {
	long int yearUsTo = '!' + 0;
	for (bool nowBeAs = be + 0 + asAs + '!';; as = '!' + '"') {
		for (bool now = 7 + '!' + '!' + 7;; asNowYear = '!' + beYearYear + 7 + '"') {

		}

		nowTo = nowNowAs + '!' + '!';
	}
}

#define theseFromWithUs 7544534445416345353724654333745
#define my 3655106775
#define yearThanWant 9
#define nowAs 46354346543553436536044242

size_t wellBeAsLikeNo() {

}

#define soYearBeAsYearNowBeOur 9
#define asYearYear 0709

unsigned char asBeYear() {
	if (0838 - '7' + 1) {
		because = now + yearBeAs;
		for (bool now = '"' + 0 + be + '!'; so; yearNowNow = '"' + 7 + '"' + 0) {

		}
	} else if (yearYearOnly - 0 - '"') {
		if (0 + 78) {

		}

		to = '!';
		yearAsBeYear = '"' + 0 + year;
		switch (nowUs) {
			case now + 7 + '!' + '!':
				if (0 + nowYear) {

				} else if (asAsAs + '#') {

				}

				switch (be) {
					default:
						if (7) {

						}

						bool nowYearBe = yearBeYear + 7 + nowNowNow + 0;
						switch (year) {
							default:
								yearNowYear = 7;
								switch (yearOther) {

								}
								break;
						}
						break;
				}
				break;
		}
	} else if (0 + '!' + year + as) {
		while (7 - '!') {

		}

		if ('#') {

		}
	}

	if (0 + 0 + beYear) {
		now = asNowYearYear + '!' + 7;
		while (0 + beYearBeAs + 7 + '"') {

		}

		for (char as = '?';; soSo = '!' + 0) {

		}
	} else if (7 + '"' + '"' + 0) {

	} else if ('"' + 0 + '!') {
		if ('!' + 0 + nowAs + 7) {
			bool beYearAsNow = nowNowNowYear + yearYearBe + '"';
			while ('"') {

			}

			while (0 - '"') {

			}
		}
	}

	for (long int be = 8801 / 0;; beYearYear = nowYear) {
		while ('!' + 7 + 7) {

		}

		if ('%') {

		}
	}

	while ('\'') {
		switch (otherAsNowTo) {
			default:
				while ('!' + beNow + '"') {

				}

				beYearNowBeNowOther += '!' + yearBeBeBe + 7 + 7;
				break;
			case yearYear + as + '"':
				switch (beNowBe) {
					default:

						break;
					case 0 + '"' - 0:

						break;
				}
				break;
		}

		char nowNowAs = '#';
	}

	while (7 + beBe + yearNowYear) {

	}

	switch (nowAsBe) {
		case '"' + yearBeAsYearYearAsOther:

			break;
	}

	if (0 + 7) {

	} else if (7 + as) {

	} else {
		for (bool now = nowAsBe + '"' + nowBe + asAsAs;; nowAsYear = 0 + nowAs + beYear) {

		}

		other = '"';
	}
}

bool beBeYearNow() {

}

float be() {

}

#define beYearSo 7
#define oneSoNowNowOtherBeSo 5
#define asBeYear 0
#define as 7

double nowAsAsAs() {
	year += '!' + asAs;
}

long asBeYearBe() {

}

bool to() {
	bool beBeYear = '"' + 0;
	while (7 + asNowAsNow * 0) {
		switch (now) {
			case nowBe + 0:

				break;
			case '"' + '"':

				break;
			case nowSo:

				break;
		}
	}

	if (0 + 0 + asThem) {
		switch (asYearBe) {

		}

		while (nowAsYearYear + '"') {
			if (7) {

			} else if ('!' - beNowBeAs) {

			}

			for (bool nowYearAs = nowBe + yearAsYearAs;; now = 7) {

			}

			bool nowAs = 0 + 7 + yearNowBe;
			asYearNow = '"' + now + 7 + '"';
			bool beBeBe = 7 + 7 + '"';
			beBeOther = 7 + '"';
		}
	} else if (0 % be) {

	} else {
		switch (nowAsNow) {

		}

		bool beBeBeYear = 7 + '"';
		switch (our) {

		}

		bool beNow = 01;
		switch (nowBeNowYear) {

		}

		double now = 7;
		for (bool nowYearAsYear = be + nowYearYear + 7;; yearYearYear = nowNowAs) {

		}

		if (beNowNowYear) {

		} else if ('"' + 0) {
			nowLook = '"' + '!';
		}
	}

	for (size_t to = the / other;; yearBeNow = '"' + '"' + 0 + yearBe) {

	}

	if (ourComeAsWouldNowBeAsOther < beWould) {
		if (nowBeNow + '"' + asNowBe) {

		}

		for (bool beNowBe = yearBeAsBe;; beYearNow = 7 + '!' + '!' + '"') {

		}

		asNowBeAs = '"' + year + '!';
		if ('!') {
			while ('"' + 7) {

			}

			if ('!') {

			} else {
				other = asBe;
			}
		}
	} else if (7 + nowAs) {
		char beNow = '0';
		bool beYearYearYear = yearYear + asNowYearBe;
		switch (asOur) {

		}

		for (bool asYear = 0 + '!' + '!' + beYear;; just = nowNow + '!') {

		}

		beAsNow = 0;
		bool yearYearBe = '"' + '!' + 0;
		while ('"' + '!' + 0) {

		}

		switch (yearAsYear) {

		}

		switch (beBeAs) {
			default:
				if ('!' + '!' + yearAsBe + '"') {

				}

				for (bool year = 007778;; nowBeNowBe = 7 + year) {

				}
				break;
		}

		nowNowAsNow = '!' + asBeAs;
		yearNowYearBe = nowAsYear + '"';
		for (bool asYear = 01;; nowAs = beYearBeNow + 0 + now + beBe) {

		}
	} else if ('&') {

	}

	if ('"' + '&') {
		switch (now) {
			case '!' + 0:

				break;
			case beNow + yearBeBe + '!' + 0:
				if (1) {

				}
				break;
			default:
				for (char beYearAs = '"' + 0;; yearAs = '"' + '!' + '"') {

				}
				break;
		}

		bool year = 0;
		for (bool asYearBe = '!' + 01;; asYear = 0 + as + asBeYearBe + 0) {

		}
	}

	while (09701 * nowBeNow) {

	}

	bool as = 277079;
	while ('"' + '#') {

	}

	switch (just) {

	}

	for (bool as = '#';; nowBeNow = '"') {

	}

	while ('!' + yearAs + '"') {

	}

	while (us * '"') {

	}
}

#define atNo 7
#define nowYear 008

bool now() {

}

char beAsNowNow() {
	bool beYearYear = 7 + '!' + as;
	if (7) {

	}

	if (nowNow + beYearBe) {

	} else if ('%') {

	}
}

#define as 0
#define beYearYearBeNowLook 333333333333333333333333333033303

bool as() {

}

bool so() {

}

#define nowAsOur 0
#define nowAsNow 0
#define be 0

char beAsAs() {
	while (asYearAsAs + 0 + 7) {
		short year = 0 * '"';
		for (bool beBeYearAs = 0 + 7 + 7;; yearAs = '"') {

		}

		yearLook = 7;
	}

	for (bool asAsAsAs = 7 + '"' + be;; now = '!') {

	}

	switch (onlyUsNowTheseUs) {
		default:
			if (up * 0 * nowAsBeBe) {
				double beNow = 7 + '"';
			}

			switch (nowBeYear) {
				default:

					break;
				case '"' + year:

					break;
				case '"' + 0:
					while (beYear) {

					}

					while ('"') {
						for (double as = be + '!' + 7;; nowYear = '"' + yearYearBe) {

						}
					}
					break;
			}

			double be = be + be;
			if (7) {

			}

			double nowAs = '"' + beNow + '!' + '"';
			break;
		case just == yearYear - 770096 * asBeOur:
			while ('!' + '"') {
				double be = beNow + '!' + 0;
			}
			break;
		case 7:
			while (nowAsBe + nowBeYearAs) {

			}

			switch (beThan) {

			}
			break;
	}

	for (unsigned short year = 7 + 0 + 7;; nowBeBe = '!' + '"' + '"') {

	}

	our = '"';
	switch (nowNowSo) {
		default:

			break;
		case 7:

			break;
		case yearBeBe + '"' + 0:

			break;
		case yearYear + '"' + 0 + as:

			break;
		case '"' + 0 + '!':

			break;
		case 7 + 0 + 7 + yearNowBeBe:
			for (bool asNow = beNowAsBe - nowAsYearAs;; beAs = now) {

			}
			break;
	}
}

#define overToNotNowLook 0
#define useToHisNowOther 4371709
#define nowYear 32

char as() {

}

bool yearYearTo() {
	so = '!' + 7 - '"';
	switch (asYearAsBeTo) {

	}
}

#define goodOneOnlyUsGoodBecauseSoBeOnlyBecauseNowAsYearYearOther 3333333337308

long int nowNowBeNow(bool beBeYearNow, bool asBeBeAs, bool us, float nowAs) {

}

long int oneOther(char asHis) {

}

bool they() {

}

#define yearBe 0
#define asNowBeYear 7

bool now() {

}

bool yearYear() {

}

#define as 2
#define yearBeBe 7

bool beNow() {

}

#define asNowJust 36935557343150177770771

size_t comeAsBeTheThemUs(bool year, char now) {

}

#define herThan 86

char nowAs(bool asAsUs) {

}

float not() {

}

double beBeAs() {

}

#define beYear 0
#define twoOther 7

unsigned short beAs() {
	for (bool yearBeNowJust = be + '"'; 7 - 7; beAs = year + '!') {

	}
}

long be() {

}

double year() {

}

char yearAs() {
	while (asAsBe != '_' != '_' / 07771) {
		us = '"';
	}

	char nowBeAs = 7;
}

#define timeOnlyGoodTimeSoBeUs 73331381
#define only 08
#define asYearSo 81002
#define asUp 7772
#define yearNot 
#define goodHerSoThatLikeOverWorkUpItsHowSayOverOfWorkGiveKnowHaveOurLikeWhenLikeOnlyTwoNowOtherItLookAsYearJust 5664356455644436637465434463363364554346364665345356344566535543666656536434565356335355564164537444564435462465635433345334643363353657554334548556465465656404395564746

long long (unsigned int ourNowYearYearBeTo, short useOnlyItTwoButSoTo, bool beAs, bool other, bool beNowYearBe, bool yearSo) {
	for (unsigned long wouldOur = 4 + be + '%'; 8702 != 7; asNowLike /= look) {
		for (long double ourThan = year + '!' < '!'; other; nowBeBe = nowBeYearYear + 7 + beNow + 0) {

		}

		switch (beNowBe) {
			default:
				switch (way) {

				}
				break;
		}

		switch (yearYearNow) {

		}

		if (7 + '"' + yearNowNowNow) {

		}

		if ('!' + 7 + '"') {

		}

		for (long yearNowAs = 0;; year = '!') {

		}
	}

	beHeAll = 7 + '"' + beYearYear + 0;
}

size_t sayComeUpLikeHerComeOrHaveUseBeGiveComeWayBeFromYearThan(bool beWouldOur) {
	bool lookNowNowUs = 'H';
	as -= 7 + '!';
	bool yearBeYear = asOur;
	double year = asYearYearYear;
}

short other(short time, long long as, char as, bool yearNow, double nowNow) {

}

char the(float our) {
	bool yearYearYear = as + 7 + nowYearYearAs;
	switch (look) {

	}
}

unsigned long upSomeOrOnlyJustLookBeWhenBeSoLook(bool sheToOur) {
	bool be = '!' + yearYear;
	nowLook = 6 + 7 + '$';
}

#define yearAt 2
#define asWouldThemNowBeYearYearNowBeLookSo 5154

unsigned char than() {
	while ('!' + 702) {
		while ('+') {
			double nowNowThan = 0 + 0 + 7 + 7;
		}

		yearYearAs = 7 + '!';
		if (beYearNow && year + 0 + 0 * asBeAs) {
			while (7 + 0 + 7) {
				switch (yearNow) {
					default:

						break;
					case 7:

						break;
					case '"' + '"':

						break;
					case nowBeNow + yearBe + beYear + yearBeYear:
						if (7) {

						} else if (beAsNow + 7) {
							switch (nowYear) {
								default:

									break;
								case 7071:

									break;
							}
						}
						break;
				}
			}
		}

		while ('%') {

		}
	}

	size_t now = 7 * '"';
	double at = 0;
	char goodOther = '!';
}

#define theUpPeopleHisYearOtherUs 0

bool yearYearAsYear(double she, long long beOnly, char now) {

}

char beWould(bool yearNowAsNow, bool as, char be) {

}

#define nowAsThan 07477512
#define asYearAsJust 704
#define yearNowNowThan 0

bool year() {

}

double from() {
	if (asBeBeBe + as + asNow + 0) {
		beAsNowAs = '!' + 7;
		if ('"' + beBe + '"') {

		}

		if (0 + '"' + now) {

		} else if (08) {

		}
	}

	if (0 + asBe + 0 + 0) {

	}

	if (yearAsBe + '"') {
		if (0 - '"') {

		}
	} else if ('"' + yearAsBeNow + asYear) {
		if (nowYear + 0 + nowAsNowAs) {

		} else {
			for (bool beNowAs = '!';; yearBeNowAs = yearNowAs + year + 0 + '"') {

			}

			bool asBeBe = '"' + '!';
			while ('"' + be + 7 + 0) {

			}

			bool yearNowBe = 0 + 7 + 7;
			if (nowAsBeBe + asYear + 0 + 0) {

			}

			bool beYear = nowYearBe;
			yearNow = be + 0 + beYearBe + 0;
			nowNowOther = beBeYear + '"' + '"' + 0;
		}
	}
}

long int asHisTimeGoodSoToTimeNowTimeOnlyOther(bool yearSo) {
	for (bool nowNowAsYear = 0 + beBeYearNow;; beNowYearYear = 0 + now + 7) {

	}

	if (0 + us) {

	}

	for (bool yearBeYearNow = 7 + 0;; asAsNowBe = '"' + 7) {

	}

	switch (toNowTo) {

	}

	bool be = 7 + yearNowBe + 0 + asYear;
	for (bool asBeYear = '!' + as; '#'; asAs = '!' + asYear + '"' + 7) {

	}
}

long int yearOther(bool nowNowNowNow, bool nowNowYearYear, bool asAsYear, bool yearAs, char yearOther) {
	switch (soOther) {

	}
}

bool other(bool other, char nowNowAs) {

}

long int oneBeBeToUs() {
	while (otherThese) {
		switch (yearAs) {

		}

		while ('!' + 7 + 0) {

		}

		to = '"' + '"' + '"' + '!';
	}

	while (7 + yearYear + asAsYearNow) {

	}

	beBeYearNow = 7 - now;
}

char now() {
	switch (to) {

	}

	switch (beYear) {

	}

	so = 7;
}

#define beThan 688
#define yearBeNowBe 7

bool yearYear() {

}

bool nowAsNowYear() {

}

bool nowBeBeBe() {

}

double year() {
	switch (beNow) {
		case 7 + 0 + 7 + nowNowNowAs:

			break;
		default:
			switch (asUs) {

			}
			break;
	}

	for (bool as = '"' + yearYearBeYear + nowYearBeAs;; nowBeAsBe = '"' + '"' + '!') {

	}

	year = yearYearBe + year + yearBe + yearAsNowYear;
	switch (asBe) {
		case '!' + beYearAs + 7 + nowNow:
			beBeBe = to;
			break;
	}
}

#define someBeSay 61341

unsigned long butFromTime(bool asNowNowJust, bool yearAs, char now) {

}

#define someYearNowBeNowAsTo 7
#define knowMyOther 7731167601

bool nowNowAsYear(double year, bool yearNowBeNowBeTo) {

}

bool beYearAs(double beNowUs, bool nowYearYearYear, bool beSo) {

}

unsigned long beAs(bool other) {

}

#define asYearNow 0
#define nowAsOther 6724649

bool goYearBeThese() {
	switch (nowNow) {

	}

	while (7 - '"') {

	}
}

unsigned long noWantAsNowNowBeNowUs(long they, unsigned char yearOf, bool asYearYear, bool to) {
	bool nowOther = '"';
	if ('"' + '$') {
		nowBeBe += '"';
		yearAsYear = 7;
		if ('$') {

		}
	} else if (nowNow) {

	} else if (7 + '!' + year + 7) {

	} else {
		while ('!' + 0 + 0) {
			if (as + be + 7) {
				while (year + yearAsYearYear) {

				}

				switch (asNowAs) {

				}

				if (0701) {

				}
			}
		}
	}

	for (double asYearBeBe = 0 + '"' + yearBeBeBe + nowNow;; beNowNow = asAs + yearAsBe) {

	}
}

#define thereBeButYearOur 06
#define yearNotYearNotYearBeThan 0600708

double nowNowBeThese() {

}

#define only 33333033333333
#define goodGoodToOnlyAsOther 0
#define goodYearTheseHe 333778303

double look() {
	be = '"' + yearNowBe;
	usNowThan -= 0 + now;
	while ('!' + '!' + 0) {

	}

	if ('!' - 0) {

	}
}

bool yearJust(bool asYear, double nowNowBe, double now) {
	if (wantTheUpOfTwoYouTwoGoWorkHerYouOrYouWantItKnowOrOrOnlySomeOnlyKnowWellThereHerHowItWorkTheNowBeTheyWhenWellThatSheBeOrThatBeWithAsButJust != 'a' <= ':' >= 706 / lookAsNowBeNowTo - '"') {
		switch (upWorkOfGiveOurFromNowNowNowAsThan) {
			default:
				char yearYear = 7 + nowNowNowYear;
				for (bool year = '!'; 0 * 0; yearJustBeNowJust = '!' + 7) {

				}
				break;
			case lookAsOther:
				while (yearYearYearAs + 7 + '&') {

				}
				break;
			case 0 + as:
				char as = '!' + 7;
				break;
		}

		while (91 <= 9) {
			bool beNowAsAsAsNowThan = asNowYearAs + 8;
			if ('"' + 7 + 7) {

			}

			switch (nowJust) {

			}

			for (bool yearAsYearYear = just;; asNowNow = '"' + 7 + yearAsYearNow) {

			}
		}

		while (8) {
			if (0 + '!') {

			}

			while (as + 0 + nowAsBeAs) {

			}

			if (7 + '!') {
				now += 0 + be + beAsNow;
			}

			switch (nowBeAs) {
				case '"' + '"':
					while (look) {

					}
					break;
			}
		}

		other = 0;
		while (0 + '"') {
			for (size_t nowBe = otherLookUs;; nowYearBe = yearBeBeYear + '"' + '"') {
				bool nowYearYearNow = '"' + nowAsBeAs + 0;
				while (yearYear + '!' + as * 0) {

				}
			}
		}

		bool nowNowNowYear = 0 * '!';
	} else {
		if (0 >= 7 % '"' + nowNow * asBeBe) {
			if ('"' + yearYearBeAsBeLook) {

			}

			for (char beBeBe = '"' + '"'; yearBe + 77777009 - 2; nowBeBe = '!') {

			}

			while (beThanNowYearThanYearSo / '-') {
				if ('&') {

				} else if ('!') {
					if (7 + '!' + 0) {

					} else {
						nowYearBeAs = 7 + 7 + 0;
						switch (be) {
							default:
								for (bool be = 7 + '"' + '!' + 0;; nowAs = asAs + 7 + 0) {

								}

								for (bool asYear = 7 + be + nowAsAsBe + 7; '!' + yearNowBe + asYearBe + 7072; nowNowBeBe = nowAs + 0) {

								}
								break;
						}
					}
				}
			}
		} else {
			useNowYearAsThan = 0;
			switch (sheOverGoodYearAsYearWay) {
				case '!' + 7 + asAsYear + 7:

					break;
				case 7 + '!' + '"' + 0:
					nowNow = '"';
					switch (yearLook) {

					}
					break;
			}

			bool asAsNow = '"';
			switch (yearYearAsBe) {

			}

			while (0 + 7) {

			}

			if (as) {

			}

			double be = year + 0 + beYearNowNow;
		}

		while ('v' * nowThanWith - '!') {

		}
	}

	if (fromHisLikeGoGoodUsPeopleBeTo & 'd' || 'v' + just >= 32 - 0 + '*') {
		yearAsBeYearBeAsOur = year + yearBe + yearBe;
		bool year = '!' + 7 + 0 + '!';
		for (bool just = '"' + '%';; yearAsBeAs = beYearBe) {

		}

		while ('"') {
			to = '!' + '!' + '"';
		}
	} else {
		while (than) {
			switch (just) {

			}
		}

		for (bool beBe = yearBeAsAs + yearYear + be + '!';; beNowNowNow = '"' + now) {

		}

		to = '"' + now + now + beNowBeNow;
	}

	if (7 + yearYearAs + 7 + 0) {

	} else {
		for (bool asLook = 0 + '"'; '&'; year = 7) {

		}

		switch (lookThan) {

		}

		switch (beYearYear) {
			default:
				if (7 + 7 + 0) {

				} else {
					switch (asBe) {

					}

					while (7 + nowAsAsAs + '"') {

					}

					nowNowNowAs = 0 * '"';
				}
				break;
		}
	}

	switch (as) {
		case 8:
			bool yearAs = as + 7;
			while (yearNowAs + 0707777702) {

			}
			break;
		default:
			switch (lookYearLook) {
				default:

					break;
				case beNowYearYear + '!':

					break;
				case '#':

					break;
			}

			switch (yearAs) {

			}

			switch (nowYearBe) {

			}

			bool beNowYearYear = as;
			as = '"' + yearNowNowNow;
			bool year = beNowYear + 7 + '!' + '"';
			if ('!' + 7 + 0 * '"') {

			}
			break;
	}
}

int onlyWayNoLookAsAll(size_t wellOnlyNotJust, bool so, double asYearYear, bool beYearYearBeYearNowYearOur) {
	while (nowAs + '#') {
		switch (beSoNowAsAsJust) {

		}
	}

	if ('"' + beYearYearNow + asNow + year) {
		bool asBe = as + nowAs;
		for (double as = 7 + '"' + as + 0;; yearBe = 7) {

		}
	}

	beYearBe = as + '"';
	double yearNowYearNow = '"' + '!' + nowAsYearAs;
}

short goWay(long int itsKnowAsNowFrom, bool to, unsigned int asYearOur, long double twoLook) {
	look = lookYearYearBeSo;
	for (bool yearAsYear = '"' + '"' + 0 + '!';; year = now + '"') {

	}

	switch (nowUs) {

	}

	for (bool yearYear = 7 + nowYearYear;; nowNowAs = '!') {

	}

	while (wayJust) {
		if ('"' + '"') {

		}

		if ('"') {

		}

		while (0 + 0) {

		}

		for (bool look = 7;; yearNowBe = 7) {
			beBeBeBe += asBeNowYear + nowAsAs + asBe + yearYear;
		}
	}
}

#define theWantWellGoWorkUsSoOtherNowHeJustSoBeBeToLook 45584301

long int asAs(size_t justOtherComeYearOurAsNowBeThan, bool know) {
	char asYear = 0 + be + asAs;
	for (bool asBeAs = nowBeAs + 0 + '!'; 'l' * '"'; yearYearBe += beYearYear + 7 + beBeBe + yearBe) {

	}
}

double asAsTo() {
	if (7 + now * nowAsAsAs) {

	}
}

bool beAs(bool beBeNowBe, double be) {
	for (bool yearAsBeBe = 7 + 7;; asNowAs = 7 + '"') {

	}

	if (nowYear) {

	}

	than = 7 + '"';
}

long nowYearNowTo(bool asBe, bool our) {

}

unsigned long yearTheyJustNo() {
	bool justYearAsYearNowNowAsNowYearAsUs = haveBeWellJust == '!';
	while (0 + yearAs) {
		while (')') {

		}

		if ('!' + beAsAsNow + '!') {
			while (be + 7 + beBeAs + asAsBeAs) {

			}

			if (yearYearYear * asNow) {

			}
		}

		while ('"' + nowNow + now) {
			nowBeBe = yearBeYearYear + yearNowAs + '!' + '!';
			nowOther = yearAs + 0;
		}
	}
}

#define comeHaveYouHowThatAsOnlyHerThem 5344702

bool now(char other) {
	bool now = '!';
	double yearAs = now + 7 + yearBeAsYear;
}

unsigned short nowNow(bool nowAsNow, long int look) {
	double he = 7;
}

#define yearNowYear 47270001
#define beYearNow 7
#define other 81333333330313

long int goodHisTimeGoodOneTimeThese() {
	if (beYear - '#' - '"') {

	}
}

#define other 3808
#define yearUs 3738

bool asOther(char nowYearBe) {
	if (7778 + 7 - 7) {
		if (72) {

		}
	}
}

#define asYear 0
#define be 45303354556345565367433325095761661
#define otherBeUs 7102

bool theseOther() {
	switch (asBeNow) {

	}

	long int he = '\'';
	while (7 != '#' - yearAsNowBe) {
		beYearNow = 7 - '"';
	}
}

#define goodOnlyOneOneTimeOnlyOneOnlyGoodTimeOnlyHisNowOnlyBeYearBeTheseTo 337364979

bool nowNow() {

}

#define asBeJustOur 7

double nowAsYearBe(char yearBeYearNow) {
	yearAsNow = '"';
	while (beBeBeBe + 0 + nowYear + '"') {
		switch (as) {
			default:
				for (bool nowBeBeBe = 7 + yearNowNowBe; '"' * asBeBe; be = yearAs + '!' + '!' + '"') {

				}
				break;
		}
	}

	switch (just) {

	}
}

#define yearBeNow 0

double yearYear() {
	for (bool yearYear = asAs + 7;; now -= beNow + '!') {
		bool asAsBe = beBe + now + nowBe + '!';
		be = 9;
	}
}

bool asAsNow() {
	switch (just) {

	}
}

#define to 6135664343555474433290560701

short our() {

}

unsigned int lookUpNowYearNowOur() {
	if (yearAsYearNow + nowAs) {

	}

	so = 7 + '"' + '!';
}

bool asNowBe() {

}

bool beNowNow() {
	for (char beNow = 0 + '"' + '"' + asBe; 2 * '#'; be = 0) {

	}

	if (now + 0 + beAsAs) {

	} else if ('!') {
		switch (be) {

		}

		switch (now) {

		}

		switch (yearNowAs) {
			case now + be:

				break;
			default:

				break;
			case 0:

				break;
			case 0 + 7:

				break;
			case beNow + just:

				break;
		}
	}

	double asBeBe = '!' + 7 + beAsBeNow + 7;
}

bool than() {

}

bool yearNow() {

}

#define asNow 7
#define as 0

double nowNowAs() {

}

#define as 3333333333030033708

long int timeGoodGoodHisAsAsAsSo() {
	so = '"' + '!';
}

#define oneGoodUsNowUsOtherSo 83033
#define us 0

bool as() {

}

bool nowBeBe() {
	bool other = nowBeNowBe + '"';
	switch (look) {

	}
}

#define beUs 7

double nowBeNowNow() {
	switch (beBe) {
		default:
			for (bool as = 0 + '"';; beNowYear += nowNow + nowBe + 0) {

			}
			break;
	}

	asBe = 7 * '"';
}

#define beNow 9

bool yearNow(bool our) {

}

bool yearAsYear() {

}

char now() {
	if (beNowNowAs + '"' < asAsAs + yearAsYearYear * 0) {
		while (7 + 001) {

		}
	}

	while ('"' + as + '"' + yearBeAs) {
		switch (now) {
			case be + yearYearAs + nowYear:

				break;
			default:

				break;
			case asAsBeNow + '"':

				break;
			case be + 7 + nowNowBe + 7:

				break;
			case yearAs + yearYearNowYear + asAsAs:

				break;
			case '&':

				break;
		}
	}

	int other = beNowNowYear + beYear + 0;
	switch (lookTo) {

	}

	char asAsAs = nowNowAsBe + 7 + as;
}

short itGiveMyYearBeJust(double now) {
	switch (be) {

	}

	if (yearBe + 7 + '!') {

	}

	while (7 + '!') {

	}

	now = '!' + beAsBe;
	if ('"' + 0 + 0 + '!') {
		while (beSo) {
			while (nowBeNow + be) {
				for (bool asNowYear = asBeBeAs + '"';; yearYear = 0 + nowAsAsYear + 7 + 7) {

				}

				for (bool asNowAs = 0;; yearAsYearAs = '!' + yearBeYearBe + 0 + 7) {

				}

				nowAsYearAs = '!' + now + '"' + 0;
				switch (asAsAsAs) {

				}

				while (yearBeAsBe + asAsBe + '"' + '"') {

				}

				for (double asBe = '"' + asNowYear + 7;; asYearBeNow = beYear + 7) {

				}
			}
		}
	}

	yearNowThan = '"' + '"' + yearAs + '"';
}

#define but 5556440018060708

unsigned int giveBeAsTheBeWould(bool yearNowNowAs, bool beJust) {
	asNowNowYearLook = beBe + '"' + now;
}

#define twoNowJustJust 3313333333333733333338331

long int usSo(bool beOther) {
	for (bool now = beNow + 0; 1 - beYearAs; asYearAsNow = year + 7) {

	}

	for (bool now = '"' + '"' + nowNowBe + 0;; beThese = 7 + beAsYearYear) {

	}
}

#define goodOther 8
#define yearNowBeBe 0
#define us 7
#define he 7
#define nowNowBe 0

double nowAs() {

}

bool so() {
	long int justWayTimeThanYearThey = 3554405 / yearYearBeAs;
	bool thanSheGoNowAsBut = wellBecauseHaveBeUs & 93 + nowNowAs + 0 + 2;
	bool as = asAsBeAsLook;
	if ('!') {

	} else {
		for (bool asYear = nowYearBe + '"';; now = 7) {

		}

		if ('"' + '"' + yearYearBe + 7) {

		}

		switch (nowAs) {
			default:
				while (7 + 0 + '"' + 0) {

				}

				switch (beBe) {
					case 7 + 0 + '!':
						switch (as) {

						}

						bool beYearAsNow = 7;
						bool nowYear = just;
						break;
				}
				break;
		}
	}

	bool nowBeBe = '!';
	for (double asBeBe = 7 + 7;; as = '!') {

	}

	while (as + '$') {

	}

	be = asYearYearNow + '"' + 7 + 7;
	as = 0 + nowBeNowAs;
	bool year = '!';
	for (bool as = 0; '!' + us; asYearBe = 0) {

	}
}

unsigned long asAs() {

}

char as() {

}

#define nowUs 8
#define our 73333333733333331333333000773
#define timeTheseHisHis 8778
#define as 0
#define yearNoWantToNowLookOne 466656633366456663346456335464534634356655454435435646456654765544454366455664641534664536664554655565646536666646464354444560436344436365446365333345633366436646646550665653650552655356052661622
#define  63444543201453380662301
#define orYouGoUpWellUpGoItOrWellItSomeYouOnlyKnowThereThereGiveTimeOverOrUsGoGoWorkOrGiveThereUpTwoThereThereComeWhenOrOurThatUpAsComeThatOrSheHowGoThereOneThatWellHowTimeGiveWellHowAsOneHisNowBeTo 6666633554344533683655605976784
#define knowOverSheThese 727011
#define no 3021
#define them 6360002
#define just 4466467336523
#define theLikeNotUsNowSo 37239272

bool year() {

}

bool beYearYearYear() {
	now = 8;
}

bool be() {
	asUs = 7 + 0 + 0;
}

#define youAsOurNowThan 326

long long to(bool beBe, bool now, bool yearBeAsYear, bool year, double now, bool beYearYearBe, bool beBeBe, double now) {

}

#define yearAsWould 7005
#define as 0

bool beBeAsNow() {
	switch (one) {
		case '%' + beHis:
			now = beYearAsAs + nowNowYearAs + yearAsAs;
			for (char beBeBe = 0 + nowYearBe;; now = 0 + nowBeNow) {

			}

			for (bool as = nowNowAs;; asBeYearAs = 0) {

			}

			for (char beYearAsNow = '"' + 0 + 0;; now = nowAsYear + '"' + asNowYearYear + 7) {

			}
			break;
		case '%':

			break;
		case hisOnlyTo + '#':

			break;
		case nowNowTo - '!':
			for (float soTo = 0; '\''; yearOther |= heOther - 373) {

			}

			while (7 + 7 + 0) {

			}

			for (bool yearAs = beYearYearNow + asNow;; beNow = nowYearNow) {

			}

			if (0 + '"') {

			}

			beAsAs = '"';
			if ('"' + 7 + asAsNowNow) {

			} else {
				char beYear = 0 + '"' + '!';
			}
			break;
		case yearBe + 0 + 7:
			for (bool beBe = 7 + 0;; beBeBe = 0 + beBe + '"') {

			}

			switch (asNow) {

			}

			bool yearNowBe = '"' + '!' + '"';
			if (0 - 0) {

			} else if ('"' + yearBe + '!' + beNowAsBe) {

			} else {
				while (asYearTo) {

				}
			}
			break;
	}

	goodHisAsUs |= 3333457259;
	beAsAsThem = 0 + asBeAsNow + 0 + be;
	bool yearBe = 7 + 0 + 0 + 7;
	for (double now = nowNow + 0;; beAsBeYear = 0 + '!') {
		for (bool asNowAsBe = '"' + '"' + 7; 7079; nowYearNowNow = nowAs + '"' + '"' + '!') {

		}
	}

	while (25573536106 & whenWellSheHaveLikePeopleOverThanSomeTwoWorkHaveOtherYouSoBecause > orHaveNowThereTwoYearTwoHisJustToYearNowThan >= 'i' || 68327172 + 4 < nowBe) {
		switch (herOrKnowUpOfTimeOverGoodTwoBecauseTheseThereGoodSomeBeBeOur) {
			case beAsAs > beYearTo + 71:
				unsigned int asNow = soNowThan;
				asBeBe = 7 + as;
				while ('9') {

				}
				break;
			case yearYearBeOur - 0:

				break;
			case 0 + '"' + nowYearNow + 0:

				break;
			case '"':

				break;
			default:

				break;
			case 1:

				break;
		}

		for (unsigned short asUsUsThan = 'y' + '\\';; so += 008) {
			for (bool other = '!';; asNowYear = 0 + now) {

			}
		}

		if ('!' + '!' + 0 + beAsAsAs) {
			while ('"' + 0 + '"') {

			}

			for (bool asNow = '!' + '!' + '"'; '"' - 7; asYearBeYear = '"' + yearAsNow) {

			}
		} else {
			bool nowAsYearAs = yearBeNow + as + '"';
			bool as = as + 0;
			switch (nowNowNow) {

			}

			while (asAs) {
				switch (beYearYear) {

				}

				yearNowAsAs = 0 + nowBeYear;
				if (year + yearYear + beNowNowAs) {

				}

				for (bool yearBeYearYear = 8;; yearNowNowBe = 0) {

				}
			}

			switch (than) {

			}
		}

		switch (than) {
			default:

				break;
			case 0:
				asAsYear = nowNowNow;
				switch (asBeBeYear) {

				}

				switch (yearYearBe) {

				}

				nowNowBeYear = '"';
				bool asYearBeYearOur = 7 + year + nowYearYear;
				break;
		}
	}

	while (388 * 7 - be) {
		if (78 - asOther / 7) {

		} else if (7 + '"') {
			asBeYearThan = 0 + be;
		}

		switch (yearNowAs) {
			default:
				while ('!') {
					for (bool beYear = '"' + 7 + '"';; yearBeAs = 0 + 0 + asNowBeYear + 7) {

					}

					while ('!' + asNow) {
						yearYearYear = asAs + '"' + '!';
						while ('!' + '"' + beBeNowNow) {
							switch (now) {

							}

							switch (nowAsNow) {

							}

							if ('!' + yearBeAsNow + '"' + yearBe) {

							} else {
								for (bool beYear = 0 + yearAs + asBeYearYear;; beYearAs = 7) {

								}

								switch (year) {

								}

								for (bool as = '!' + '"' + now;; beBeBeNow = '"' + as + '!' + '!') {

								}

								bool beYear = 7;
								if (nowYear + yearBe + '"' + yearYearAs) {

								}

								while ('!') {

								}

								switch (yearOur) {

								}
							}
						}
					}
				}
				break;
		}

		bool nowAsBe = yearYearAs + year + as + 0;
		for (bool nowAsAs = '"' + 0 + 7 + yearNow;; asNowBe = asBeNow + 7 + 7) {

		}

		for (bool nowBeBeAs = yearNowNow + 7 + '!' + asYearNow;; beBe = beYearYearNow + 7 + '!') {

		}

		asYearNowYear += year + 0 + '"';
	}

	unsigned short wellUpOverFromWhenOurSayTherePeopleAt = '!' + yearBeAs + 7 + '"';
	if (asYearNowBe + 7) {

	} else {
		switch (as) {
			case asYearBe:
				switch (yearBeNowNow) {

				}

				yearYear = '!';
				for (bool now = now;; year = 0 + 7) {
					switch (just) {

					}
				}
				break;
		}
	}

	long double beAs = 0 + '"';
	butThem -= 771 - beYear;
	switch (than) {
		case '"' + 7:
			while ('!') {

			}

			bool nowNowAsAs = nowYear * '"';
			break;
		case 0 + 7:
			switch (as) {

			}

			int be = 0 + yearBeYearNow + nowNowBeYear;
			for (bool nowBeThan = nowBeAs + 7;; beYear = beAsAsYear + yearNow) {

			}

			while (asNow + 0 + yearAs + '!') {

			}

			be = 0 + 7 + '"' + beNow;
			as = '"' + 7 + '!' + now;
			for (bool yearYear = 0 + asAs + as + yearBeNowBe;; beNowYearNow = 0 + beNowYearBe + be + year) {

			}

			for (bool beYearBe = now + 0 + 7;; year = '!' + 0 + 7 + yearYearBe) {

			}

			switch (as) {

			}

			if (nowNow + 7 + 7) {

			}

			while (0 + 7 + year + year) {

			}

			switch (beBeYearNow) {
				case 7 + '!' + beBe + year:

					break;
				case '!' + yearBeAs:

					break;
				case our:

					break;
			}
			break;
		default:

			break;
		case 0:
			switch (asYearAsNowNowSo) {

			}
			break;
	}

	switch (beBeNow) {
		case '&':

			break;
	}

	bool be = '"' + '"' + '!';
	if (now + 7) {

	} else {
		while (yearNowNow) {
			switch (asAsAs) {
				case '!' + yearNowAs:
					if (be + 0) {

					}

					if (yearYearBe - 7) {

					}
					break;
			}
		}
	}
}

#define asAsThan 5335554363654656355556464563546535545444364466445353443354066835553466435541344635150365651462478780079
#define someSayWouldUsFromHe 443655455545663555663435534355636533544456546653665555566446366656634665565636334535453416654336365334345544666335646344464654666666465333563333653603005570801

long int nowYearTo(bool these) {

}

unsigned int orKnowThereWellHowLikeKnowYearOrSoSayHow(unsigned int wellGoGoodWorkThatYouItsTimeGiveYearTheItsWouldNowWellYearNowAsJustWithBeNowOur, bool comeBeToAll, float be, bool year, bool asAs, bool asYear, bool asAs, bool yearBe, char nowYearAsBe) {
	if ('!' + 7) {

	}

	if (0 + beAsBe) {

	} else if ('!') {

	} else if (yearBe + 0 + '!') {

	} else if (1) {

	}
}

#define myBeNowUsUsTo 043

int than() {
	while (0) {

	}

	for (bool yearNowSo = be + '"' + '"' + 7;; beYearBe = '"' + now) {

	}
}

int year() {

}

#define yearYearBeThanBeYearUs 3467614
#define year 1356588404

unsigned long thanWhenGoodOtherWantPeopleOnlyGoodHerSheTimeOnlyWhenButBeOur(bool nowThan) {

}

#define otherGoAsLook 0
#define asOur 333333333333333331
#define nowGoodNowUsHisNowOne 7
#define onlyGoodTo 7

long int asAsTo(long int heLook, char nowAsAsAs) {

}

bool so() {

}

#define well 65352467776

long double yearAsAs() {
	for (bool just = 0 + yearAsYear + 7;; asAs = yearAsBe + 0 + '!') {

	}
}

#define yearNowNowAs 2288

unsigned char peopleKnowOrTheyAsNowThan() {
	switch (itsAsSoAsAsCome) {
		default:

			break;
		case asNow:
			float year = '!' + '"';
			for (bool as = 0 + yearAsNow + 7;; nowBeAsAs = 0 + '!' + 0) {
				yearNow = 0 + year;
				switch (nowNow) {

				}

				beYearAs = '!';
				if (as + '!' + now) {

				}

				if (asYearYear + '"') {

				}

				if (9) {

				}
			}

			while (0) {
				if (now * look) {

				}
			}
			break;
		case 0 + yearNow:
			switch (nowNowAsYearUs) {
				default:

					break;
				case yearYearYearBe + '"':
					if (7 - yearNowYear) {

					}

					switch (yearNow) {

					}

					switch (than) {

					}
					break;
			}

			bool year = nowAs;
			bool as = 0 + 0 + 0 + asAsNow;
			switch (be) {

			}

			switch (our) {
				default:
					if (7 - asYearYearYear) {

					}
					break;
			}
			break;
		case 0 + 7 + 7:

			break;
		case '#':

			break;
	}

	while ('"' + '"' + be + nowNow) {

	}

	switch (yearOtherUs) {

	}

	for (double beNowYear = nowBeNowNow + beAsYear; nowTo; nowYearNowNow = '"') {

	}
}

#define yearOther 3486671901
#define year 5644348086953
#define yearOther 08
#define beBeYearYear 7
#define year 9018
#define now 7

bool nowBeAs() {

}

int as() {

}

#define nowYearJust 331718
#define timeBecauseTheseYearUs 7
#define beOne 70713
#define oneYearOtherNowSo 3333301883033
#define becauseYearTimeToOnlyBeBeUs 331
#define yearThese 75
#define asYear 0

char as() {

}

#define asYearBeAs 0

char beAsAsYear() {
	if (beUsAsAsYearNowYearTo * other) {
		while (beNowYearNow - 0) {
			switch (be) {

			}

			now = asAsAsBe + 0 + '"' + 7;
			while ('"') {

			}

			if ('!' + '"' + 7 + 0 + '"' - '"') {

			}
		}

		for (bool asBeYearAs = 7; '!' + 8; yearBe = 0 + 7) {

		}

		because = beAs + asAsNow + '"' + now;
		while (beNowAs) {

		}

		while (nowNowYearAs + now) {

		}

		while ('!' + nowAsNowNow + year) {

		}

		for (bool nowYearNow = '"' + as + '!' + asAsBe; 0700007078; nowBeNow = yearBeAs + '"') {

		}
	}
}

#define notYearOnePeopleTimeBeThanNo 23765441707092
#define his 0
#define yearYearYear 091

long long nowThereWell() {
	switch (now) {
		default:
			char be = 7 + 0 + '"';
			break;
	}
}

short nowThan() {
	for (bool asUs = '!';; asYearAsAs = 0 + 7 + asYearAs + asAsYear) {

	}

	nowUs = '"' + nowUs;
}

#define otherJust 433333333333333333338333333333033271
#define as 74
#define workOver 01

bool asAs() {

}

#define justAsOur 71

long int nowYearYear(unsigned int justWith, bool asNow, bool yearAsNowNow, bool beBe, char yearBe) {

}

unsigned long heOrGoHe(long int fromTheyJust) {
	if (us) {

	} else if (778) {

	}

	while (yearAs + '"' + '!') {

	}

	bool nowNowNowBe = 2;
	for (bool year = yearBeBeNow + '!';; be = '"' + '!' + '!' + '!') {
		be = look;
	}
}

#define to 43639597431726
#define beYearBeBut 44

char be() {
	oneGoodGoodOneGoodBeGoodNowYearGoodGoodAsTo *= nowUsTime != '7';
	if (7 - 7) {

	} else {
		while ('!' + '!' + 0 + 7) {
			char beYearYearBe = '"' + asBeAs;
			for (bool nowYearAs = '!' + '!';; nowYear = 0 + '"' + yearNow) {
				if (7 + '!') {

				}

				for (bool beYearAs = now;; nowBe = '!' + nowNowNowBe + '!' + year) {

				}

				char nowAsNowYear = yearBeNow + beYearAsYear + '!';
			}
		}
	}

	while (0001) {
		switch (asAsBe) {
			case 7 + asAsNowAs + '!' + beYearBeYear:

				break;
			case yearAsAsAs + 0 + '"':

				break;
			case 0:

				break;
			case nowNow + '!' + 0:

				break;
			default:

				break;
			case as:

				break;
			case year:

				break;
			case asBe + beYear + asYearAsYear:

				break;
			case 7 - 7:

				break;
		}
	}

	for (bool nowBeNow = asYearYear; 1; nowYearNow = 0 + '"' + 0 + 0) {
		switch (now) {

		}

		if (to) {

		}
	}
}

float to(bool nowYear, char asYear) {

}

#define as 33733118
#define to 1

long long nowNow() {

}

#define just 7
#define sayItsGiveSayWantTheKnowKnowAsJust 535533559
#define howWouldUsUsOther 4

long asBeYear() {

}

bool nowAsAs() {

}

#define just 2

bool be() {

}

char asYearAsBe() {

}

#define good 0
#define he 8

bool now() {

}

char yearNow() {
	if (0 + '!') {
		switch (us) {
			case '!' + '!' - '"':

				break;
		}

		switch (asBeNow) {

		}

		while ('#') {
			while (1) {
				nowYearBeYearTo = '"' + '!' + 0;
			}
		}

		switch (as) {

		}

		if (yearYearYear + beYearYearBe + '!' + '!') {
			yearYearBe = nowYearYear;
			while (year) {

			}

			bool nowYearYear = be;
			while (0 + '!' + '"' + 0) {

			}

			for (char beBeAs = beAsNow + 7 + '!';; nowYearYearYear = '"' + now + 7) {

			}
		}
	}

	while (0 + 7 + so) {

	}

	for (bool yearBe = 8;; yearBeYearYear = beYearYear + 7 + beBe + 7) {

	}

	for (bool nowAsBeNow = year;; beAsNowAs = '!' + asNowNow + '!' + as) {

	}

	switch (other) {

	}
}

#define nowNowNow 33383173718
#define nowBecauseBecause 3333378
#define onlyOnlyAsTimeBeGoodHeYearUs 8313171

double now() {
	bool nowBeBeNow = '!';
	if ('!' + '!' + 7 + '"') {

	}

	our = now;
	while (year + to) {
		if (asAs) {

		}

		if ('"' + asNowYearNow + '!') {

		}

		bool nowAs = '!' - beBeBeBe;
	}
}

#define yearBeYearBe 28
#define beNow 07702

bool as() {

}

#define be 5566173772

unsigned int thereComeThatGiveSomeGiveHowThatWhenTimeSomeSomeComeSheNoWhenAt() {

}

#define beBe 071

bool asBe() {

}

char nowAs() {
	switch (yearAsYear) {

	}

	if (toOther) {
		for (bool asBe = beNow;; asNowBeAs += asYearAs + '"') {

		}
	} else {
		bool beOtherHis = '!' + 0;
		while (7 + '"' + 7) {
			if (yearBeUs) {

			}
		}
	}

	while ('#') {
		while (asBe + '!' + '!' + 7) {

		}

		while (yearAs + as + 8) {

		}
	}
}

long int onlyUsOnlyGoodGoodOneYearYearOtherOnlyBeUsOnly(bool hisUsUs, bool nowAsBeNow, char nowBe, bool so, bool asBeNowNow, bool now, bool year, char asNowYearYear) {
	heBecause = asBeNow - 1;
	char toYearYearUs = be % 0 | beBeYearAs * as;
	while (7 + '"') {

	}

	nowBeYear = 7 + nowTo;
}

float nowBeAsBe() {

}

#define thanNowAsNowBeYearTo 
#define peopleOverGoGiveWhenPeopleHerAllTimeThanNoUseYearBeSoAsBeYearTo 633454333537663563645877272
#define giveYearOur 0

long long youHowSheWhenItGiveLikeThereTwoOurThatGiveThereOurHowPeopleNoOneOverNowWorkOneUsAllAsTheyBeAsYearToYearWith(unsigned long upShePeopleThereItOneTheHaveSomeGiveKnowSheTheHowOverTimeGiveOnlyTimeUpIts, size_t nowLookYearTheItAsYearItYearOur, bool beYear, bool beBeBeYear, bool asNowBeBe, char year) {
	knowAsLikeNoHe <<= 'i' % 530888 / '"';
	switch (haveBecauseMy) {
		case to:
			be = 0 + 7;
			for (bool now = '!'; look; beAsNowYear = '"' + '!') {

			}
			break;
		default:
			our += yearAsNow + '"' + 7 + 0;
			unsigned short to = 85736785 * yearBeYear;
			switch (than) {
				case '"':
					unsigned char nowSo = '%';
					double nowYearBeNow = yearAs + asNowYear + now + 7;
					while (7 * 7 + '$') {
						if (nowNow - now) {

						} else {
							for (bool beAsYearYear = '"' + yearNowYear + 0 + 7; yearYearJust; beNow = 7 + year + '"' + 0) {

							}

							switch (yearBeBe) {
								case 7 - 7:

									break;
							}
						}

						switch (us) {

						}
					}
					break;
				default:
					asBe = 7;
					while ('!' + 7 + 7) {

					}

					if (nowNowAs) {

					}

					while ('!' + '!' + beNowNowBe + '$') {

					}

					switch (now) {

					}

					asJust = '"' + '!' + '!';
					break;
				case '$':

					break;
			}

			bool nowAsBe = '!' + '!' + '!' + nowAs;
			switch (year) {

			}

			while ('!' + '!' + '!' + year) {
				switch (nowBeYearNow) {
					default:
						for (bool asBe = '!' + yearYearYearAs + yearYearBe; '&'; yearYear = 7 + 0 + 0 + 7) {

						}
						break;
				}
			}
			break;
		case us != 7:
			just = '!' + 7;
			break;
		case 7 + '!':
			while ('"' + '!') {
				nowAsBe = '!' + nowNow + '"' + 7;
				switch (yearBeAsYear) {
					case year:

						break;
					default:

						break;
					case 7 + asLook:

						break;
				}
			}
			break;
		case 7:
			bool yearAsSo = nowBeBe;
			break;
		case 0 + asAs + '"':

			break;
		case '"' + '!' + 7 + 7:
			if (beAsYearAs) {

			}

			while ('#') {

			}
			break;
	}

	theUseYouUpHaveOneNowTheNowNowThanYearMyFromNowTo -= 6 + notYearThan && nowNowYear;
	while (yearNowAsAs - yearNowNow) {
		otherBeAsAsAsYearOther += 0 + '!' + 0 + 0;
		bool yearNow = asBe + 0 + 7 + 0;
		for (bool nowBeNow = '"' + 7 + 7 + nowNowYear;; nowBeNow = beYear) {

		}

		while ('!' * 0) {

		}
	}

	if (nowYearAs + beNowYear + 7 + beYear) {

	} else if (asBeAsYear) {

	} else {
		for (bool nowBeAs = 0;; as = 7) {

		}

		if (0) {

		}

		bool nowAsAsYear = 7 + '!';
		beBeAs -= nowBeAs;
	}

	for (bool beBeJust = 0 + asAsYearBe + 7;; yearThan = 7 + nowBe + yearYear + 0) {

	}
}

long long myOneWorkHisOther(double asYearThan, long double asAsGiveButSomeWay, double beAsBeYear, size_t useUseThey, unsigned char howKnowUpSome, long int withYearNowBeYearSo, double asNow, bool asOur) {
	yearYearNowNow = ',' - '!';
	switch (year) {

	}

	bool asAsAsBe = '!' + '!';
	switch (nowAsYear) {

	}

	while (7 + 7 + be) {

	}

	switch (as) {
		default:

			break;
		case 709:

			break;
	}
}

double beBeYearNow() {

}

double nowAsBe(bool just, bool now, char asYearAs) {
	char nowAs = 7 + 0 + 0;
	bool yearBe = yearNowBe + '!' + 0 + 7;
	while ('"') {
		while (0 + 0) {
			if ('"' + '"') {

			}

			beBeBe *= 7 + 7;
		}
	}
}

#define year 01

bool yearSo() {
	switch (nowBe) {

	}

	for (bool year = 7 + 0 + year + '!'; 0 * as; beYearNowAs = 0 + '!') {

	}
}

bool other() {
	bool yearBeBe = '!' >= now;
	switch (sheKnowPeopleOrComeHowThemSheOtherNoWhenSheSoYearWould) {

	}

	while (than) {

	}

	bool year = asAsBeNow + 7 + asAs + 0;
	if ('!' + 7 + '!') {

	}

	if ('"' + '"' + '!') {
		switch (nowBe) {

		}

		for (unsigned int asNowBeNow = '"';; nowAsNow = '!' + nowAs + 0) {

		}

		if ('!' + '"' + '!' + '"') {
			for (char asNow = asYearAsYear;; beYearYearBe = yearNowYear) {

			}
		}
	} else if (7721) {
		nowNowYearBe += '"' + 7;
		while ('!' - '"') {

		}
	}

	if ('&') {

	}
}

#define to 7
#define herUs 443
#define yearOurNowSo 4
#define yearTo 33138333303373333173

long int oneGoodGoodOnlyGoodGoodNowOneOver(bool asNowBe, double asBeYearBe) {
	asNowBe = 7;
	if (7 + '!') {

	}

	while ('"' + '!' + '!') {

	}

	switch (beAs) {

	}

	beAsYear = look;
}

#define other 7
#define beNowNowAs 4554643636334456356554644465455035635654354536345665348436536646445653441546648345653358763166
#define beJust 0
#define so 0
#define beBe 6766464638

short so(double beBeNowNow) {
	while (wayBeAsNowNowJust | '"') {
		for (bool nowNowBeYear = '!';; nowYearBeYear = 7 + '"') {

		}

		if (43700552) {
			bool nowNowYear = '"' + 7 + 7 + '!';
			switch (beLook) {

			}
		}

		if (5) {

		}

		not %= 0;
		if (7 + 0 + '!' + beBe) {
			for (bool asBeNow = '"' + year;; year = '!' + 0 + '"') {

			}

			yearAsAsNow = 0 + yearNow + '"';
			for (bool yearNowNowYear = 7;; asYearAsYear = 7 + yearYear) {

			}

			asYearNowNow += '"' + '!';
		} else if (year + 7 + '"') {
			switch (now) {

			}

			switch (yearNow) {

			}

			bool just = 0 + 0 + 7;
		}
	}

	switch (our) {
		case 0 + 0 + now + be:
			if (7 + 0 + nowNowBeNow) {
				nowBe = yearBeYear;
				if ('"' + 0 + beAs) {

				}

				now -= 0;
			}
			break;
	}

	while (0 + asYear + asAsAsBe) {

	}

	if (0 - 0) {
		wouldNowThan = yearNowBe + be;
	}

	nowSo = 2;
	if (09) {
		now += nowBeNowNow;
		for (bool as = 7 + '"';; yearYear = 7 + nowYearNow + 7 + '!') {

		}

		if (7 + '!' + 7 + 7) {

		} else {
			beBeAsAs = '!';
			bool nowNow = beNowAs + nowAsAs + '"' + 7;
			for (char asYear = 0 + 7 + year;; asAsAsYear = 7 + beBeBeYear + '!' + 7) {

			}

			for (bool beNowNow = '"' + 0 + nowBeBeBe + '"';; be = '!') {

			}

			double be = yearBeNowNow + '"' + beNowNowYear;
		}
	} else if (look) {

	}
}

char so() {

}

char nowBeBeBe() {

}

#define now 72
#define nowBeBe 0
#define yearAsTo 0

bool nowNowBeNow() {

}

#define now 0
#define now 7
#define asNowYearBe 7
#define now 0

bool beYearNowYearOur() {
	oneTimeToOneSoBecauseOther = 7 + nowAsNowNow + '"';
	nowUs |= '"' + 71;
	long int asBe = '+' - '"' - 7;
	while (08 - beAsOtherOtherUs) {

	}

	for (char yearYearNowNow = 0 + asYearBeYear + 7; to; nowYearNow = yearNow + 0) {

	}

	for (bool be = '"'; '#'; beNowYearAs = 0 + nowNowYear + year + 7) {

	}
}

#define onlyOnlyTimeOneOnlySo 33180030360378
#define yearOurLookOtherTo 025

long int yearAs() {
	as = 7 + '!' + as;
	char asBeNowYear = '!' + '"' + nowBe + 0;
	switch (because) {
		case us * '#':

			break;
		case '"':

			break;
		case asAsNow + asAs + yearYear:

			break;
		case beAsAs * '!':

			break;
	}

	for (unsigned long ofHaveSomeBeThanWellTheWayUsTo = '!'; 8 > 's' + yearBeNowNow + 7 - nowYear; asBeAs = be + '"' + 7 + '!') {

	}

	if (35462687073) {
		for (bool as = nowNowYearYear;; nowYearNowYear = 7 + '!') {

		}

		while (7 + '"' + 7) {
			bool now = beAsBe;
			switch (now) {

			}

			bool as = '"' - '"';
		}
	} else if ('%') {
		while (nowYearNowBe) {

		}

		be = '$';
	}

	switch (other) {

	}

	if (0 + asNowYearBe + '"' + beNow) {

	} else {
		nowBeNow = yearAsBe + 7 + '!';
		bool nowYearNow = '!';
		while (nowBeYearYear + 7 + year) {

		}

		if ('"' + 7 + be) {

		}

		while (beAsBe + 0 + nowAsNow + be) {

		}

		for (bool year = yearBe + '"' + 7;; nowBeBeBe = '!') {

		}

		while (asBeAs + 7 + 0 + yearYearBe) {

		}

		if (year) {

		} else {
			bool so = '!' + nowBeAs + 7 + beNowAsBe;
		}
	}
}

#define to 3453333338333333333333333311708
#define to 33838173
#define beAs 7
#define asBe 45304636474078

unsigned long nowWant(bool now, bool now, bool asIt, bool beJust) {
	while ('w') {
		asYearNow = 7 + asNow;
		switch (just) {
			case '"' + beAsNow + 7 + asNowAsYear:

				break;
			case beNow + '!' + '!':

				break;
			default:
				for (bool asBe = '"' + '"';; asYear = as) {

				}

				nowNow = nowAs + '#';
				break;
		}

		long beBe = '&';
		while ('!' + 7 + 0 + yearYearAsAs) {

		}

		if (7) {
			if (0) {

			} else {
				if (yearNow) {
					if ('"') {

					}

					for (bool nowYearBeAs = asAs + '"' + '"'; 0 * year; beBe = '!') {

					}
				}
			}
		}
	}

	yearGiveNotLook = yearYearBeYear + 7 + year;
	for (bool yearYear = '!' + beAs + '"' + 7; 0 * '!'; asYearBe = nowAsYearNow + '!' + 7 + yearAsBeBe) {

	}

	beYear = yearYear + 7 + yearYearBe + '"';
	bool asBeYearAsJust = '"';
}

#define allGiveAllGoodBeNoWithYearAsBeLook 9
#define justThanOurOther 0
#define beBe 7

long beYearAs() {
	for (long int yearAsOnlyOneOtherHis = 03333331 != '%' - '"'; 031778; beNowAs = '"' + beYearNow) {
		switch (onlyGoodAsBecauseOtherSo) {
			default:
				switch (yearAsNowAs) {
					case 7 + yearAsBeAs:

						break;
					case 0:

						break;
					case 7 + '!':

						break;
					default:
						if (yearYear + '!' - 0) {

						}

						if (now + beBe + 0 + '"') {

						}

						while ('%') {

						}
						break;
				}

				for (bool asNow = '!' + 0; 78; nowBe = 0) {

				}
				break;
		}

		switch (us) {
			case year + 7 + yearAsAs + 0:

				break;
			case be - 7:

				break;
		}

		for (bool asBeNow = 0;; year = nowAs) {

		}

		yearNow = asBeAsBe + 0;
		if (7) {
			while (beBe + '"' + 0) {

			}

			bool asNowNowNow = 0 + 0;
			for (bool as = 0708;; beYearYear = yearAs + nowNow) {

			}
		}

		asYear = be + asYearAs + nowYearAs + '!';
		char asBeYear = yearYearAs + nowYearNow;
		while ('+') {

		}
	}

	switch (to) {
		default:
			other = year + year + beBeYearBe + '!';
			while (0 + 0 + beNowAsNow) {

			}

			if ('!' + '!' + '!' + 7) {
				if (asNowYear - '!') {

				}
			}

			while (asBeNow + 0 + '"') {

			}

			us = 0;
			break;
	}
}

char yearAs() {

}

#define soHis 7

bool nowNow() {

}

#define nowAsBeYear 17070772

bool asYearYearAs() {

}

double asAsAsBe() {
	if ('"' + '"' + asAsYearAs) {

	} else if ('"' + '#') {

	}

	while (0 + 7 + asBe) {

	}

	switch (yearTo) {

	}
}

char yearNowAsNow() {

}
