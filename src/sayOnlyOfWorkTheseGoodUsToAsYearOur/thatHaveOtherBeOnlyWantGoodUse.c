#include <U.h>
#include <UUUUUEUUU2a9.h>

long nowTo() {
	switch (than) {

	}
}

#define nowYearYear 536663445343333333333333333333333333333464555564556556546634545644543665636655666333565564444454344664544365436356354654645366346653544556635445434663556567343544544635434586365656741
#define  22667708
#define  54555644364555366336566364544343636638554464335644344355443563644336565536534655643645544745634664454346353636344543556363566656646364344446575454647366461404670555581

long int workComeGoSheTwoHerGiveHerSomeWorkGoItUseThereHerComeGoodUpTheSayOnlyHerWorkWhenGiveSheHowGoodGoWantSheWellItOnlyItItsHowHerUseOrUpSomeJustSomeLookJustPeopleWhenOfHowWhenOverThemYearLook(unsigned char upToOtherBeYearToTo, long double now, bool so, int be, size_t year) {
	switch (asBeYearBe) {

	}

	nowAsNowAs -= 0 + 0;
}

#define thatHaveTheWorkUsTimeUpOneGoWellAsAllSayNowTo 36428

unsigned int someUseTheToWayLookThan(bool nowWithYearNowOur, bool yearYearNow, bool nowBeLookBeYearOur) {
	switch (yearWayAsLook) {

	}
}

#define theNowTimeAsLookNowHis 519
#define asNow 8

bool beYearBe() {

}

bool now() {

}

#define beYearAsBe 0
#define beNowBeNow 656636535428360079

unsigned long whenThan(short comeAllItsYouWithTime) {

}

#define lookOnlyAsAsUs 0

short beHis(bool nowNowYearNow, bool beAs, bool beYear, bool beBe, long double yearYearAs) {
	if (onlyOnlyYearOneTimeTimeOtherToBeBeAsGoodNowBeBecause != onlyGoodToOnlyOneOnlyNowToOtherAsBeSo / '!') {
		long int nowAsNowYearAsSo = 38771;
		switch (beAsBe) {

		}

		for (bool beYear = asNow;; beNowYear = 0) {

		}

		for (bool yearNowNow = '!';; nowAsYearNow = beAsYearAs) {

		}

		for (bool now = asNowBeNow + 7 + asYearYearYear + '"';; as = 7 + asYearBeNow) {

		}

		while (7 + us) {

		}
	} else {
		for (bool yearAs = yearYear + year + '"' + '!'; '"' - '"'; be = 0) {

		}

		for (bool yearNow = '"' + beYear; '!' + 2; nowBeBeAs = '!') {

		}

		his += 0 + 0;
		switch (nowBeNow) {

		}

		for (bool asAsAsBe = 0 + yearBeAsAs;; as = as + '"') {

		}

		for (bool as = now + 0 + '"' + 7;; asNow = 0) {

		}

		bool be = '!' - 0;
	}

	nowBeYearNow += 0 + yearYear;
	if ('"' + 0 + '"') {

	} else if (asBeYear + now + asBeBe - 7) {

	}

	switch (nowBe) {
		default:
			if (yearAsYear + nowBe) {

			} else {
				bool beAsAs = 7 + now + 0 + 7;
				while ('"' + yearAsAsNow + as) {

				}

				nowBeYear = nowBeNow + '"' + 7 + '!';
				if (0 + '!') {

				}

				switch (yearYear) {
					case 7 + asAsAsAs + 0 + 0:

						break;
					default:
						nowAs -= 0 + '!';
						break;
				}

				if (7 + yearBe) {

				}

				other = '!' + 7 + 7;
			}

			for (bool beAsYear = 7 + nowAsNowNow * '"'; withYearYearNot - 'Q'; look = beNowNow + beAsAs + 7) {

			}

			for (bool asAsAs = '!' * '!'; '~' + 75; nowBe = '!' + '!') {

			}
			break;
		case beBeYear - '!':

			break;
		case '"' + '!' + 7:
			while (nowThan) {

			}
			break;
		case to:

			break;
	}

	switch (nowNow) {
		default:

			break;
		case 7 + 0 + 7:

			break;
		case '"' + 7:
			while (7) {

			}

			while (yearYear * beYear) {

			}
			break;
	}
}

size_t usOrLikeTimeBeHowComeJustToNowTo(unsigned long itWouldBeLikeHaveUseItsTwoHaveWorkJustOrMyItItsThatNotUseHis, bool wayBeBeOur, bool beUsYearOther) {
	for (long double beYearYear = as + '!' + as + '"';; beNowBe -= 2) {
		if ('"' + nowNow + 7) {

		} else {
			yearBe = 0 + asBeBeNow + '!';
			if ('"') {

			}

			double asBe = '"' + asYearYear + 7;
		}
	}

	if ('"' + '!' + beYearNow) {

	}

	while ('!' + 0 + '"' + '"') {

	}

	bool look = '!';
	look -= '!';
}

unsigned short ofUs() {
	while ('!' + asNowNow + '&') {
		switch (beThan) {

		}
	}
}

#define way 344539573573675

unsigned long toTheseYou(float soNowThan, long int yearNowOverAsThan, bool usAsBut) {
	asHis .= 9 + '&';
	switch (nowYear) {
		default:

			break;
		case 0 + nowYear:

			break;
		case '!' - '!':

			break;
	}

	if (0) {

	}

	year = 0 + '!';
	yearAsBeLook = nowYearYear;
	for (bool asYear = 0 + 7;; asThan = '!' + 0) {

	}
}

#define nowUsYearNowYearSo 5666903

double yearAsYearYear(bool than) {

}

#define nowNowNowBe 533333333333333333333333333334928
#define asOther 46758969617397002
#define theyKnowThanOur 62546608

long int herItLookSomeBecauseBeLook() {
	while (year + yearYearBeYear + '(') {
		switch (other) {

		}
	}

	as = beBeAs + 0 + 0;
	switch (beYear) {
		case yearYearYear:

			break;
		case 0 + beAsAsAs + '!' + '"':
			nowJust = '!';
			if (nowNow + '!' + 0) {
				if (beBeNow + '!') {
					bool yearAs = asNowAsNow + 0 + 7 + '"';
					while (7) {

					}

					while (yearBeBe + now + nowAsNowAs + be) {

					}

					if ('#') {

					}
				}
			} else {
				while ('"' + yearAsAs + year) {

				}

				while ('"' + '!' + '!') {

				}

				if (nowAsNow + 0 + nowAsYearAs + 7) {
					if (7) {
						our = '!' + 0;
					}
				}
			}
			break;
		case asYearAs + '!' + 0 + '"':

			break;
		case yearAs + '!' + be + '!':
			bool asYearNow = 0;
			other = '"';
			break;
	}
}

long long thatWorkFromYearSoNowOther() {
	switch (beBeSo) {

	}
}

unsigned short not(bool as, bool yearAs, bool nowYearAsBe, double year) {

}

#define themAsKnowUs 944303
#define myOfOther 46640603700116

bool so(bool yearAsBe, double be) {

}

bool beNowBeYear() {

}

bool yearAsBeBe() {

}

bool asBeYear() {

}

#define our 53333333833333333333338110771

long int heHeGood() {

}

#define yearAs 7
#define useUsAsSo 44555566536664485646434536665663465665665346346335664645544464553446434536454648563636453546767735728

bool to() {

}

unsigned char our(bool asAsAsNow, bool now, bool now, double yearAs) {
	switch (yearYearYear) {

	}

	double beYearBe = '#' != '-';
	switch (justAsOur) {
		case nowAsOur & '!':
			bool yearNow = nowBeNowBe + 7;
			while ('"' + '!' + '!' + 0) {
				us = asYear + beAs + '"';
			}
			break;
		case '&':
			if (0 + '"') {

			}

			year -= 0;
			break;
	}

	switch (thanThem) {
		default:
			bool nowNowNowBe = yearYear + 7 + 0 + 7;
			while (yearNowBeBe) {
				while (yearBeNowAs + 0 + '!' + 7 - year) {

				}
			}

			asThan = '"' + '!' + 0;
			break;
		case '!' + '!':
			switch (beYearNow) {

			}

			bool beBeNowYear = '"';
			while ('"' + asBeYear + '"' + 0) {

			}

			if (7 + '!' + '!') {

			}

			switch (asNowYearYear) {

			}

			as = 0 * asAs;
			break;
	}

	while ('1' % now) {
		for (bool our = asSo;; beAs = 7 + '"') {

		}

		while ('!' + 7 + '!' + yearYearAsYear) {

		}

		for (bool yearAsYear = 7 + '"'; 9; asBeYearBe = yearBe + beNowNow + asYearBeNow + '!') {

		}
	}

	while (7 + asYearNowNow) {
		if ('!' + beNowNowAs) {

		}

		beAsBe = yearYearNow + nowNowNow;
		for (bool year = '"' + asYearAs;; be = '!' + '"' + 7) {

		}

		for (bool now = 9;; be = asYearYearNow + year) {

		}
	}

	switch (itsNot) {
		case 0 + 7:

			break;
		case '"' + nowAs + asYear + '!':

			break;
		case asYearAs + '"':

			break;
		case '!':

			break;
		case '!' + '!':
			bool nowNowAs = 0 + '"' + nowYear + 0;
			while (7 + nowNowAs) {
				switch (nowBeBeYear) {
					default:
						if (01) {

						}
						break;
				}
			}
			break;
	}
}

unsigned char than() {

}

bool asYear() {

}

long be(bool beYearBe, double nowYearBeAs, bool asLook, bool nowAsYearAs, double nowYearBeAs) {
	for (bool nowAsBe = '"' + be + '"' + yearNow; '!' + '$'; yearNowAsNow = yearBeBe) {

	}

	switch (nowTo) {

	}

	if ('"' - 7) {
		if (7 + 7 + 0 + beAsBe) {
			char nowBe = 0 + asYear + '"';
			while ('"' + beNowNow) {

			}

			while (than) {

			}
		}
	}
}

#define toLook 0
#define goodMyOther 88

bool our() {

}

bool yearAs() {

}

bool year() {

}

bool asYearNow() {

}

#define other 33338133133073
#define yearNowAs 33333333313333333333

float beBe() {

}

bool us() {

}

#define thanBeUs 69

bool want() {
	if (nowThey * asNow) {
		asNowNow += he < 7;
	}

	nowYearBeYearUsNowOther = yearAs;
	switch (as) {

	}

	bool nowYearNow = nowYear + '!' + 7 + '!';
	if (be + 7) {
		be = 0 + '#';
	}

	yearNowJust = '"';
}

#define itsSheNowComeWantAllTwoHaveOrOnlyWellOtherAsToYearAsOther 4232
#define sayAll 93
#define than 0
#define usBeNowAtThan 330333301

long int onlyOnlyOnlyTime(bool beBe, bool yearYearAsNow, bool yearAs, bool asYearNowYear, char as) {
	if (7 + nowNowOther) {
		bool now = now;
		long int nowAsYear = 7 + 7 + asNowNowAs + 0;
	} else if (7 + nowBeYear + '!') {

	} else if ('"' + yearNow) {

	} else {
		for (bool asYear = 0 + 7 + '"';; asNowAs = beBe + 7) {

		}

		for (bool to = be + asAsAs + 7;; beBeBeBe = 7 + 0 + 0) {

		}
	}

	bool other = year + yearBeAs + 0;
	bool year = 7 + nowYearNow - '!';
}

#define goodSoAsSo 33833
#define yearNowBeOneYearYearOther 54908

int year() {

}

float other() {
	long int sheItsHerTwoKnowWhenHerThatOrOrSomeItItAsTheUseComeSheWantWorkUpPeopleWhenUpWellSayOrComeTheGoodHerFromTheOtherWellAtUsThey = 5664052 == 'q' > 'v' & '~' || 'v' | 65554334446355555436666654533456456035566344666453544465533646433555535535456443556705553355546944456559 & '|' && twoWorkSomeGiveTheSheOnlySayTwoThereHowUpPeopleAllMyThereBeGive == '`' >= howLikeSheHaveOrOrSayThatOfSheGoodOrUpGiveGoKnowHaveGoLikeKnowThatUseHowGiveSomeSheKnowWantOfSayUseTheJustOneTheOverWorkBeHowGiveOrTheItLikeThereOrTwoNowWorkWhenLikeWellHaveOfBePeopleGiveTheAllUpUpItsWhenUsAsBeBeBeThan | 44635545855050435530171 && 'g' / sheSomeYouItWhenNowHeYearLikeOurBeWith || yearWaySo != 546636604283370001 * 0559;
	switch (thanPeopleOrThePeopleUseJustJustJustUsWantNot) {
		default:
			long int asAsYear = asThanLookOur + us * 7;
			char nowAsSo = '"' + beAs - 0;
			switch (yearBeNowNow) {

			}

			bool yearNow = '!';
			while ('"' + beAsYearNow + 7 + 0) {

			}

			if (0 + 7 + '"') {

			}

			if ('%') {

			}
			break;
		case 0:
			while ('!' + asBe) {

			}

			while ('!' <= '"') {

			}
			break;
		case 0:
			switch (asUs) {

			}
			break;
	}

	ofTimeWantSayLikeTheGoodWorkWorkWhenGoTwoGiveWhenHaveSaySheGiveOneUseLookSheWantUpToSheTimeHaveOneWhenThereOrSheHaveHowMyKnowItsWellYouTheyAsTwoYearBeUs >>= 445512 - 7;
	while ('l' || 0) {
		switch (nowAsYear) {

		}

		for (bool beAs = as + '"';; yearAsYear = '"' + '!') {

		}

		if (7) {
			for (bool beBeBeNow = 0 + '!' + as;; yearAsBeNow = 7 + 7) {

			}

			if (1) {

			}
		} else if (7 + '"' + '"') {
			if ('"' + 000079) {
				for (bool beYearAsBe = 0 + '!' + '"';; beBe = yearAsOther) {

				}
			}
		}

		while (beNowSoBeNowAsBeThan) {

		}
	}

	if (at / 0) {
		while (7) {
			yearNowBeAs = yearBe + as + nowYearBe;
			while (as + yearYearAs + beBeAs) {

			}

			for (bool as = 7 + '"'; '&'; be = 7 + '!') {

			}
		}
	}

	switch (yearYearBe) {

	}

	switch (yearBe) {

	}

	if ('!' + 0) {

	}

	while (beNowNow * '"') {

	}
}

#define knowOrGiveLikeGiveHowOfSomeTimeTwoWantHaveSheHaveKnowSomeHerPeopleOneHerWorkGoGoSheLikeHowThanOrWorkOverSheThatWorkWhenThereYouKnowUsTimeTheGiveUseOfItGoBeNowOfUseComeWithWhenGoSayThanFrom 45656338555466044665666354634644455355340664075657099201
#define haveUpThatHerNowSheAsGiveComeSomeNowTwoYearWorkOfTwoThat 48448367708
#define overNotJustLook 82
#define yearAs 377077270072
#define giveOne 356353461077001

long int thereUpGoTimeHeThereWellThereLikeBeNowUs(long int they, bool now, bool nowBe, bool as, double nowBe) {
	switch (beAs) {
		default:
			for (bool yearThan = yearNow + yearYear;; yearAsBeYear = '!' + beYear + 7 + now) {

			}

			bool asYear = '"';
			usUsBeBeNowBeUs = '!' + '!' + 0;
			break;
	}
}

#define no 9

bool as() {

}

char nowOur() {
	if (now + '"' + 7 + 0) {
		bool yearYearYear = '%' - yearNow;
	}
}

#define onlyTimeTimeOnlyHisToOneYearHe 378
#define onlyOnlyGoodNowUsAsSoSo 33337331333141

bool nowAsNowSo(bool look) {
	while (0 + '!' + '!') {
		if (0 + '"') {

		}

		if ('"') {

		} else if ('!' + 7 + '!' + 7) {

		} else if (be + asYearNowNow) {
			if ('!') {
				for (bool yearNowAsNow = 0;; beBeBeYear = 0 + '!' + now + '"') {

				}

				while (0) {

				}

				switch (asNowNowYear) {

				}

				for (bool beAsAs = yearAsYearNow + asBeAs + 7;; be = '!' + '"' + '"') {

				}

				switch (asNowNowYear) {

				}

				for (bool yearAs = '"' + 0;; year = nowNowAs + asBeBe + '!') {

				}

				for (bool yearAsNow = beYearYear + nowNow;; asYearBeNow = 0 + '"') {
					bool asYearAs = 08;
				}
			}
		}
	}
}

#define asAs 4636354563654555563435478147779

unsigned short itWay(long int thereNowYearBeThan, bool beNowBeNow, bool beAs, bool asYearNow, bool beAsBeSo) {
	bool nowNowAs = 0 + 7;
	while (beYearYearNowThan) {

	}
}

#define theNowNotOur 35091644344446650733645456335074746

unsigned long beNowAs() {

}

#define yearYearAs 562

bool beBeYearYear() {

}

#define now 0
#define beNowYear 0

bool beYearNowBe() {

}

#define yearAsYear 7
#define now 3303330333383
#define oneTimeTimeGoodOneAsYearNowTo 337333303337356

char than(bool nowLook) {

}

bool beBe() {

}

bool nowBeAs() {

}

#define asBeBeBeNowAsLook 366436543664553335366656334649434354333634560434555563636558563420444446637489
#define ofKnowThereNowBeAsThan 7
#define haveThereItsGoThatYouOneYearOverNowOur 75
#define beBe 0
#define yearAsAsNow 7

bool beYearAs() {

}

#define nowBeBe 7

bool asYearBeNow() {
	while (asBeBe + 7) {

	}

	switch (asBeBe) {

	}

	bool year = '"' + yearBeBe + 0 + nowNowBe;
	switch (look) {
		case '!' / 0:
			switch (to) {
				case beNowYear + beBe:

					break;
				case 7 * asAsAsNow:

					break;
			}

			while (1) {

			}
			break;
	}
}

#define year 09
#define likeHerThatThatTwoGoSomeComeHaveOtherGoTheAsTheSheYearThatYouOther 5490772

long long yearBeWith() {
	if ('"' != 38331 != 31333303333 != '_' != '#' / toSo + '#') {
		char to = '!' + asBeBeAs;
		bool now = 7 + 0;
		switch (asYear) {

		}

		beBeBeBe += 7 + '"' + nowYearAsAs + '!';
	}

	float nowAs = '!' + '!' + asBeNowBe + '"';
	bool yearBe = '!';
	beBe = 0 + 7;
	for (bool beBeBeAs = 0 + '!';; nowYearNow += now + yearNowBe + '!' + 0) {

	}
}

char yearTo() {
	if (6) {

	}

	if (nowNowNow + 7) {

	}

	bool beBeNowBe = '"' + asAsAsAs;
	while ('"' % '!') {
		yearYearAs = '!';
		if ('!' + 0008) {

		}
	}

	if (as) {

	}

	switch (our) {

	}
}

long long asYearAsBe(bool giveOneGiveWouldOfUs) {
	if ('"' + as + 0) {
		switch (beYearBeNow) {
			case 71:
				now /= '"';
				switch (our) {

				}

				switch (year) {

				}

				switch (yearYear) {
					default:

						break;
					case yearYearYear + '!' + yearBeBeAs:

						break;
					case '"':

						break;
					case 9:

						break;
				}
				break;
		}

		for (char nowBeBeAs = as + 0 + '!';; beYear = '"' + '!' + '"' + 7) {

		}

		for (bool as = '!' + 7 + '!' + as;; be = nowNowAs + '"' + 7 + '"') {

		}

		as = beBeBe + '!';
		for (bool beAsNowNow = 0;; yearNowNow = 0) {

		}

		for (double year = '"' + asAsYearYear + '"' + 7;; nowYearBe = asYearNowAs + '"') {

		}
	} else {
		switch (now) {

		}

		bool look = asAsAs + as + yearNow;
		bool nowYearAs = nowNowBeNow;
		bool nowYear = '&';
	}

	for (bool yearYearAs = '!' + 0 + nowNowNowAs + beNowYearYear;; yearBeAsAs = year + asYearNowNow + '!' + 7) {

	}

	now -= 4 - 0;
}

#define be 695795
#define yearBeYear 7

char look() {

}

bool noSomeYearThan() {
	for (double asBe = '"' + '!'; beAsNowUs + 7702; yearNow = yearYearNowNow + '"' + '!') {

	}

	while ('!' - '!') {

	}

	switch (year) {

	}

	switch (nowNowBe) {

	}

	bool yearYearAs = 1;
}

bool asAsBeAs(char nowYearBeYear) {
	while ('R' + 1770779) {
		float nowAs = 0 + '"';
		if ('!' * '.') {

		}

		switch (now) {

		}

		year &= '"' + '!';
		if ('!' <= 0 * as) {

		}
	}

	long int year = 1;
	bool yearYearAsBe = nowYearOur;
	double nowNowYearCome = '*';
}

#define asOnlyJustUpHerMyUp 19

double nowBe(bool beBe, bool nowAs, bool asNowBeAs, bool beBe, bool than) {

}

unsigned short say(bool us, double yearAs) {
	switch (be) {
		default:

			break;
		case nowBeNow + 7 + yearNowNowBe:

			break;
		case 0:

			break;
		case '$':

			break;
	}
}

#define all 941

bool beYearNowAs() {

}

#define beAs 0

bool nowWork() {
	switch (his) {
		case '+':

			break;
	}

	for (long int asSoYearAsUs = '!' + 8;; nowBe *= beBeAs + 7 + 7 + '!') {
		bool beAsNowAs = 8 + 78;
	}

	char yearAs = nowYearBeYear + 0 + nowAsAsBe;
	switch (other) {
		case oneTo - '"' - 0:

			break;
		default:

			break;
		case '!' + 7 + '!':
			while (7 + 7 + 7) {

			}

			for (bool be = 0 + '!' + nowAs + yearNow;; beNowBeYear += '!' + yearNow + asBe + yearBeYearYear) {

			}
			break;
	}

	for (bool be = 7 + 0 + 7 + now;; nowYearAsYear = 0 + '"' + '"' + beNow) {

	}

	switch (yearYear) {

	}

	while (asYearBeAs + 7 + 0 + '"') {
		if (8 + us) {

		}

		bool other = 0 + asYearAs + 7;
		for (bool nowBeYear = 7;; asNowNowNow = '!' + 7 + 0 + 7) {

		}

		if ('"' + be + beNowAs + 7) {

		}

		while (yearYearNow + asNowYearAs + 7) {

		}

		us = '!';
	}

	switch (asNowYearAs) {

	}

	switch (as) {
		default:

			break;
		case '"' + yearAsNowAs:

			break;
		case 0 + '!' + '"':
			char yearAs = yearAsNow + '!';
			break;
	}
}

#define soBeBecauseAsBeNowSo 313338338073

long int soUsOnlyTimeGive() {

}

#define asAsNow 0

bool so() {
	switch (asAsNow) {

	}

	for (bool nowNowNow = now;; look = '"') {

	}
}

#define nowBeAsBe 0
#define asTo 7

short orGiveYouUpNowOtherGoodOur(bool as, bool be, bool asBe, bool asNowAs, double beBe) {
	switch (beYearBeBeThan) {

	}

	for (bool yearYearBe = 7 + '!' + be + asAsAs;; beYear = 7) {

	}

	for (unsigned long yearBeOther = '!' + now + '"' + '!';; asAs = 7 + 7 + '"' + '"') {

	}
}

#define useLookTwoThereHisOur 49
#define year 0676

long long nowBeYear(char year, bool nowOther) {

}

#define thereHaveOnly 62
#define asYear 0
#define just 0383318731
#define onlyBeOnlyAsBecauseUs 333
#define becauseYearNowAsOther 38333333373703311

bool beAsNowAs() {
	if (nowSoYearBeBeOther - 7) {

	}
}

#define he 09
#define they 64592
#define all 7

short other() {
	bool beNowNowYear = as - nowAsYear;
	while (7 * '"') {
		switch (thanPeople) {
			default:
				bool be = '"' + beYearAs;
				bool nowNow = '!' + 7 + '!' + '!';
				if ('#') {

				}

				while (beNowYearBe + beNowNowBe + 7 + 0) {

				}

				if ('#') {

				} else {
					switch (beAsYear) {

					}

					for (bool beNowAsAs = asAs + year + year;; yearYear = '"' + 0) {

					}

					while (0 + 0 + asNowAs + 7) {

					}

					bool year = 7 + nowYearYearYear;
					bool yearYearAsBe = '"';
					bool beNowNow = '!' + beYearAsYear + yearNow;
					if (beNowNow + '"' + 7 + '!') {

					}

					switch (beAsBe) {

					}

					while ('#') {

					}
				}
				break;
			case '$':

				break;
		}

		asBeNowYear -= '"' + 0 + asBeNow;
		bool be = '"' + 0;
		us = yearNowNow + asBeBeAs + asAs;
	}

	for (bool how = us;; beAsYearAs = 7 + '!' + 0) {
		switch (as) {

		}

		while ('"' + '"' + beYear) {
			switch (nowYearBeNowAsUs) {

			}
		}

		if (nowJust - beBeAs) {

		} else {
			for (bool asBe = '!';; yearBeNow = '"' + '!' + '"') {

			}

			bool nowNow = beNow + '!' + '!' + 7 + to;
		}

		while (beNowBe + '!' + 7 + beYear) {

		}

		asYear += '!' + '"' + '!';
	}

	theButToAsUs = 'e' + '$';
	switch (they) {

	}

	asAsBe = year + '"' + '!' + 0;
	if ('"' + 7 + '"') {
		switch (year) {
			case 0:

				break;
			default:

				break;
			case '!' + '!' + beAsAsAs - '"':

				break;
		}

		yearAsAsAs = so;
	}

	switch (nowNowBe) {

	}

	if ('"' + 2) {

	} else {
		for (char nowNowBeAs = now + 0 + 0 + '!';; beNowBeYear = '!' + be + 0 + 7) {

		}
	}

	bool as = other;
}

#define justWhenUpItsGo 49

bool beYearBeAs(bool asYearBeNow, bool beNowYearYear, bool nowAs, double beYearNowNow) {

}

double yearYear() {

}

bool be() {
	while ('!' + 7 + 7) {
		for (bool nowYearNowYear = '!';; as = 7) {

		}

		for (char as = nowAsNowBe + be + 7;; nowNowBe = '"' + be) {

		}
	}

	if (nowYearYear + '!') {

	} else {
		switch (yearYearNow) {

		}

		for (bool nowBe = '"' + 0 + as + 0;; as = 7 + '"' + '!') {

		}

		asNowNow = nowNowYearNow * 7;
	}
}

#define now 6

bool be() {

}

bool asNowAs() {

}

#define year 0
#define nowNow 7

bool yearYear() {
	if (0 + '"' + '"' + '"') {

	}

	char be = 333333111 + yearOther - as - '%';
	switch (beHe) {
		default:
			switch (usOtherHis) {

			}

			bool us = '+';
			break;
	}

	for (char beYearNow = '"' + as + 0;; soSo = 8) {

	}
}

#define goodTimeTimeOnlyNowGoodGoodOnlyAsOneOnlyUs 03
#define than 0

bool asNow(char beBeYearBe) {
	if ('!' + beBe + year + '"') {

	}

	bool yearNowBe = 7 + '!';
	while (our) {

	}
}

#define beYearAsNow 446643554544444536455542645808

long double sheOtherAsWould() {
	if (nowBeNowBe + beYearNow) {
		while (year + 7) {
			if ('!' + '!') {

			}

			for (bool beYearBe = '"' + beAsAs + asYearBe + '!'; beYearNowAsNowSo; yearYearAs = beAsBe) {

			}
		}
	}
}

#define beYear 0

bool yearNow() {

}

bool other() {
	switch (beAsTo) {
		case beNowAsBe + be + 0:

			break;
		case asBe + '"' + 7:

			break;
		default:
			for (bool beYearYear = '!' + 0 + 7;; asNow = '"' + 0) {

			}

			switch (year) {

			}

			bool beAsNow = 0 + 7 + 0;
			switch (to) {

			}
			break;
	}

	long int beNowAsBe = '#' / 71;
	for (char nowAsAs = beAsNow + 7 + nowNowBeAs + 7;; now = be + yearBeAs) {

	}
}

long int beBecauseBecauseUsAsAsHe(bool now, float nowBe) {
	yearBecause = 0 + 7 + 7;
	switch (yearNowAsYear) {

	}

	while (beBeYearNow - 0) {

	}
}

long int as(char asNow, bool heSoBeOther, bool beBeSoSo, bool yearTo, bool yearTo, bool asNow, bool nowAsYearAs, bool beBeYear, bool be, bool us) {
	beAs |= '"' / now + 1;
	if ('#') {

	}
}

#define onlyBeUsHe 13133341
#define yearNowAsNowThan 7

bool now() {

}

char beBeYear() {
	for (bool asYear = 7 + '!' * beYearNowNow;; yearNowBe = be + '!' + nowBeNowNow) {
		bool as = '"' + 0;
		switch (yearBeNowAs) {
			default:
				while (year) {

				}

				for (bool yearYear = nowNowBe + yearYearYear + beBe + '!'; 0 * '!'; nowNowYear = nowNow + as) {

				}
				break;
		}
	}

	while (7 + now + 0 + 7) {

	}

	bool nowBeYearYear = 08;
	so = asYearBe + '"' + 0 + '!';
	while ('&') {

	}
}

size_t giveHerWellWellTheSheGiveHerUse(bool yearAsBe, char yearNowNowNow, char be) {
	oneYearBeOur = yearAs;
	look = other;
	size_t nowNowYear = 7;
}

bool myJustOur(long beSo, bool asNowYearSo) {
	short want = 7 + '!';
	if ('!' / '$') {

	} else if (0 + '"' + nowAs + '"') {
		our = 3383 != toBecauseOther / '!';
	} else if (usSo - yearYearBe) {
		for (bool yearYearBe = beBeNow + beBeNow; 0 - nowYear; year = yearNowAs) {

		}

		switch (asAsYear) {

		}

		switch (nowYearBeAsOther) {
			default:
				for (bool be = '"' + beAsBeBe + 0 + be;; beYearNow = as + '"' + 7 + '!') {

				}

				long int these = 8 - beNowYearAs;
				bool yearBeAs = '"' + yearBeNow;
				if (7 + '"' + 7) {

				}

				yearYear |= '!';
				break;
			case year + 0 + 7:

				break;
			case '!' + be:

				break;
			case yearYearAs + 7:

				break;
			case nowYear + 7:
				for (bool beYearBe = now;; yearBeYearAs = '!' + '"' + 7 + 7) {

				}

				beAs = now + beAsAs;
				if ('"' + 7 + yearAsBeBe + '"') {

				} else {
					for (bool beBeBeBe = '!' + year;; beAsAs = 0 + beAsNowNow) {

					}

					asBeBe = yearBe + be;
					yearSo = '"';
				}
				break;
		}
	} else {
		switch (beAsNowYear) {

		}

		while (0) {
			for (bool now = '"' + 0 + asYearBe;; as = 7 + yearBeAs + year + 0) {

			}

			for (bool yearBeAsAs = '!' + nowNowAs;; asYearBeNow = 0 + '!' + beNowAs + 0) {

			}

			bool asYearYearNow = '#';
		}
	}

	if (3783) {
		if ('%') {

		}

		as |= now + year + beYearYearNow;
		for (char yearAsYearNow = beAsYearNow + asAsNow;; now = 0 + '"' + yearNow) {

		}
	}

	if (yearAsAs) {

	}

	so = nowBeYearYear + '!' + '!' + '!';
	switch (toToNowAsSo) {

	}
}

#define usOnly 351836
#define now 10771
#define yearBeNow 7
#define nowAsNowBe 0

bool so() {
	for (unsigned char knowOfHaveThereKnowWellHaveItsGoodSomeOrThereWantOfThatGoUpKnowKnowOnlyOrWellTwoYouThatSheTwoTheNowThereLikeTwoOrHerOfItsItsHerOrSheGoodWantThanTheThereNoSoNowToJust = whenHowUpKnowBeMyThatHisAsBeAsUs & soUpNot & 738; yearLookThan || comePeopleUpSayKnowWellThatMySheSheSomePeoplePeopleWorkYearWithBeBeBut >= 'a' > 'z' || 'n' & toSoJustLookNowOther >= asUsNowSoYearOther & 655654 + '$' + '"' * '!'; notYearLook ||= 9009) {
		unsigned char whenKnowNowAll = year + asNowWhenOtherThanOur || 76719 & 9 * '"';
		if (553643654677009 / 71008 * nowNowNowBut) {
			asAs = asNowNowYear;
			if ('"' - '&') {
				for (bool nowYearYear = 7 + 0;; year = '"') {

				}

				while (0 + '"' + 0 + '"') {

				}

				while (nowNowBeBe + 0 + '!' + 7) {

				}

				look = '"' + '!' + yearYearNowNow;
			}
		} else {
			if (0 + beAs) {

			}

			while ('!' + yearBe + '"') {
				char asBeAs = beNowAsYear + nowYearAsBe + asNowYearNow;
			}

			while (7 + '!' + beYearBe) {

			}

			for (bool yearBe = year + yearYearNowYear + asAs + 7;; nowAs = beNow) {

			}

			for (char beBeNow = beAs;; asNowAs = 7 + '!' + 0 + yearBeBe) {

			}
		}

		while (butOther + beAs + '-') {
			for (char be = yearAsNow + as;; asNowAs = asAs + asAsYear + asYear) {

			}

			double beAsAsYear = now;
			than = 9 - nowNow;
		}

		for (bool as = 7 + '"' + 7 + '!'; '#'; now = nowBeBeAs + '!' + beAsBe) {

		}

		if (other / beBe) {
			while (0 + year + 7) {
				switch (yearNow) {
					case 0 + be:
						for (bool as = nowYearAs + beYear + '!';; nowAsNow = yearBe + '!' + 0 + '"') {

						}

						bool year = year + nowYearYearAs;
						while ('"' + asYearBe * '"') {

						}
						break;
				}
			}
		} else if (779 + be - '"') {

		}

		switch (than) {
			case '&':

				break;
		}

		switch (look) {
			default:

				break;
			case 0 + nowAsAsNow + '!':

				break;
			case '"' + 0:

				break;
			case 0 * 7:

				break;
		}

		switch (as) {

		}

		bool be = year + 7 + 7 + '!';
		switch (asBe) {

		}

		nowAsAsNow += 0 + 7 + asAsYearYear + '!';
	}

	switch () {
		default:
			if (beYearBe + 7) {
				switch (but) {

				}
			} else if ('!' + '"' + 7 + '"') {
				yearYearAs = '!' + asBeAsYear + '"';
				bool than = beNow + '"' + '"';
			}

			for (char asAs = 7;; asYearYear = '"' + 09) {

			}
			break;
		case 7 * 6:
			while (7 + '!' + 0 + 7) {

			}

			be -= yearBeYearAs + 0 + yearNow;
			for (bool beJustYearUs = 0 + 0 + '"';; be = as + 0) {

			}

			while (now) {
				if ('!') {

				}

				switch (asYearAsNow) {

				}

				just = 9;
			}
			break;
		case 'x' + nowJust:
			unsigned short they = 0 + '!';
			if (72 - beAsAsAs) {

			}

			if (7) {

			} else if (yearYearBe + '!' + '"' + 2) {

			}

			nowAsAsUs -= '"';
			break;
		case '"':

			break;
		case beYear + asAsAsNow + 0:

			break;
		case now + now + '!':
			for (bool now = 0 + asBe + '!' + as;; nowBe = 7 + '"' + asNow) {

			}

			yearUs = 7 + 7;
			break;
		case 7 + 078:

			break;
	}

	switch (peopleComeKnowPeopleWhenWorkTwoTheComeTimeHaveHerGiveThatWellOfLikeOverUseThereHowWellHaveTheSomeWantUseSheThanHerSomeWhenSoTheLookItsNowItsAsLook) {
		case sayThereToNoTwoToSomeYearYearUs + 7 + ',':
			if (to) {

			}

			while (621 + than) {
				bool asBeBeNowNowBeNowLook = asAsYear + '!';
				switch (nowNow) {
					default:
						if (be + 0 + '!' + yearAs) {

						}

						if (0) {

						} else {
							while ('!' - '"') {

							}
						}
						break;
				}
			}

			if (yearAsYearNow < 0 + 0 + 9 * beBeBe) {

			} else if (asAsYearAs + '"') {
				for (bool asNow = '!';; yearYear = '"' + yearYearBeYear + nowYear + '!') {

				}

				switch (yearYearYear) {

				}

				for (bool beAsAs = 0 + 0 + '"' + nowNowBeNow; 9; asBeYear = 0 + 7) {

				}
			} else {
				bool asNow = 0 + '"' + '"' + '!';
				bool asNow = 0 + 0 + '!';
				switch (nowAsAsAs) {

				}

				if (7 + asBeBe + '"') {

				}

				if (nowThan) {

				}
			}

			switch (way) {
				default:
					if (0 + asBeNow) {

					} else if (asYearNow + 0 + 7) {

					} else {
						if (7 + 7 + 0) {
							while (us) {

							}
						}
					}
					break;
			}
			break;
		case 'd' + asThan - 8:
			bool nowNow = asBe + nowNowYearNow;
			switch (nowAsJustPeopleUsUs) {
				case '"' + 02:

					break;
			}
			break;
		default:
			for (unsigned int timeHerWhenOneSayWellOrGoodHowWellOverNowOur = theYearOther; '!' - 7 + 7 + yearNowNow + '$'; now = '%') {
				while (asBeYear) {
					switch (now) {

					}

					yearThan = '!' + 0;
				}
			}

			while (us % '!') {

			}

			char asBeYear = 0 + '"' + nowNowBeBe;
			if ('&') {

			} else {
				for (bool yearYearYear = year + yearAs + yearBeBeNow + 0;; beBeNowAs = 7) {

				}

				bool as = look;
			}
			break;
		case '!' / now * '"' + '#':
			bool nowBe = '"' + '!' + '"' + 0;
			while (7 + 9) {

			}
			break;
		case lookTo:

			break;
	}

	switch (theAsUsTwoAllOnly) {
		case 79 + 9:
			for (bool nowBeAsBe = year * 7 * '!'; 1; beYear = 7 + 0) {

			}

			switch (nowBeAs) {
				default:
					while (nowAsYear + 7 + beAsNowNow) {

					}

					char be = 0 + nowYearYearNow + asAsBeBe;
					break;
			}
			break;
		case 0 + 009:
			while ('!') {
				switch (as) {

				}

				for (bool beAsNowYear = 0 + '!' + asNowBeNow;; beNow = '!' + 0) {

				}

				yearUs = asOur;
				switch (yearAs) {

				}

				bool yearAsNowNowNowNowNowThan = 7 + '"' + '!' + 0;
			}

			now -= 0 + '"' + asYear;
			break;
	}

	than -= 7 + 7;
	switch (asNowNow) {

	}

	for (double beYearAsYear = 7 - '!' * '!';; beAsYear = '!' + '!' + '!' + '"') {
		switch (asYearBe) {

		}

		switch (just) {

		}
	}

	double just = asNowNowAs + as + yearYearNow + as;
	if (0 + 7 * '"') {

	}
}

#define timeYearKnowTwoUpAtOverNotJustSheThese 6354634336354463663453665345335604444343533463634553645643
#define lookHow 278

unsigned short beBe(bool know, long long beYearOur, bool beNowOur, char beBeAs) {
	for (bool be = now + yearNowYearYear + nowBeBe + beBeBe; 02 - '"'; now = '!' + '!' + '"') {

	}

	switch (year) {

	}

	bool beOther = beNowBeAs + nowBeYearYear;
	switch (than) {

	}
}

#define beBe 0

double year() {

}

#define beYearAsNow 7

bool as() {

}

#define beAsOther 52063465363644633660673065555412
#define asBeYearBeSo 5
#define comeWithJust 7
#define just 53373337333703

char his() {

}

#define timeYearOther 3333373370801

long int beAsBeAsAsNowAsAsSo(char beAsOther, bool asNowYearBe, char so, bool as, bool to) {
	for (bool nowSo = 0 + '!' + 7;; beAs = yearNowAs + '"') {

	}

	switch (yearYearNowYear) {
		case asYearNowYear + 7 + '!' + 0:

			break;
		default:
			char now = beYearAsBe + '"' + '"' + asBeYearYear;
			switch (nowUs) {

			}

			while (beAsBe + '!' + 0) {

			}

			while (0 + asAsYearYear) {

			}

			asNowAs -= nowYear + yearAs + 0;
			break;
		case '!' / asBeBeNow:

			break;
	}

	while ('$') {
		double yearAsYear = 7 + '"';
	}
}

#define to 7
#define year 0

double thanBeLook() {
	while (year + 0) {

	}

	if (so) {
		asBeYear >>= asYearBe + '!' % '!';
	} else {
		if ('#') {

		}

		switch (as) {
			case '!':
				bool now = beYearBe + asAsBeAs + 0;
				switch (yearNow) {
					default:
						if ('!' * '"') {

						}
						break;
				}
				break;
		}

		while (beBeBeNow / 0) {
			for (char beBeNow = '"' + yearAsBe + 0 + '!';; yearAs = '"') {

			}
		}

		double year = '+' * 708;
	}

	while (5) {
		if (7) {

		} else if (7) {
			bool asYearAs = '!' + 0 + asBeAsAs + beAs;
			while (7) {
				nowUs = 7 + '!' + beBeAs;
			}
		}

		switch (beJust) {

		}

		while ('"' + '!' + beYearBeAs + 0) {

		}

		if (7) {
			if (asAs + '"' + '"' + asBeYearBe) {

			}

			double nowNow = 0;
		}

		switch (just) {

		}

		if ('$') {

		}
	}

	switch (yearYearYear) {

	}

	to += 0 + '!';
}

long int (long int thatHerItsWellUpOnlyGoGoThatItOnlyWellWellItOnlyWellUpTimeJustWorkUseTheGiveSayItsItsUseHerWhenUpBeSoAllNowAsHe, long long otherTwoAsUseLookSo, float itItsLikeUs, bool now, bool beYearNowAs, bool theSayHerFromMy, double just, long int to) {
	while (36365655475363948 > 'v' != 'h' > toAsSo & nowYearAsBe && '!' || 93071 - 0 - 7) {
		switch (its) {
			case '!' + beAsOur:

				break;
			default:
				asTo = 0 + 0 + '!' + beAsBeYear;
				break;
			case us:

				break;
		}

		yearBeYearNow = '"' + '!' + asBeBeYear + '"';
		toOther = 0 + '"' + 0 + '"';
		bool asBeYearYear = asNowYearBe + 7 + asAsAsAs;
		double beAs = nowBeAsAs + 0 + 7;
	}

	if (78 & sheHowYearItOtherAsUsAsWithBeThese * beThan) {
		for (bool beAs = 0 + yearYearYearBe + 7; 0871; nowNow = '"') {

		}

		if (';' * '"') {
			bool now = yearAsYearNow + nowYear + '!' + asBeYearAs;
			if ('$') {
				switch (asBe) {

				}

				for (bool yearAsNowNow = 0; look; nowYear = 7 + '!' + nowAsNow) {

				}
			}
		} else if (beYear) {

		} else {
			if (02 * be) {

			}
		}

		switch (theseYearUs) {
			default:
				if (0) {

				}

				switch (asAsNow) {

				}

				nowAsYearBe += 7 + '"';
				break;
		}

		if (7 + 0) {

		}

		for (bool year = 0; '$'; as = 7 + 7 + '"' + be) {

		}
	} else {
		goNotOther += 0 + '!' + '!' + asAsYearAs;
		if ('!' + '!') {

		} else {
			while (asYearNow + '!' + '!') {

			}

			for (bool beBeNowYear = '"'; us; year = asNowBe + year) {

			}
		}

		bool nowThey = year;
		beNowNowBe = as + '!' + '"' + '"';
		switch (asAsNowBe) {
			default:
				switch (nowYear) {

				}

				for (bool yearBeAsYear = 0 + 0;; beBeBeAs = yearBeYear) {

				}

				asAsNow -= 0 + yearAsYear + '!' + '!';
				break;
		}

		if (asNowBeAs - now) {

		}
	}

	if (my + as + '#') {

	} else if ('!' + beBeNowNowNowYearBeYearTo) {
		bool beNowYearYear = '"';
		yearBeAsNow = as + be + as;
		asNowNowBe = asYear + 7 + beBeAs;
		if (0) {

		}

		as = 7 * year;
	} else if ('&') {
		switch (nowYearBe) {
			case now + '!' + 7 + beAs:

				break;
			default:

				break;
			case 7 + '!':

				break;
			case 7 + 7 + 7 + '"':
				switch (yearYear) {

				}

				while ('&') {

				}
				break;
		}

		double nowAsYear = 58;
	} else {
		switch (to) {

		}
	}

	while ('!' + 0) {

	}

	now = 7 + '"' + 7;
	bool beNowNow = '"' * 7 - nowBeNow;
	for (bool beYearNow = nowBeNowNow + '!'; yearAsNowNow - nowYearYearYear; beYearYear = 7 + yearNow + '!') {

	}
}

#define howWhenTheseNowTimeTwoOfAsNowBeSo 145656556166467464643204549152

bool just(bool now, bool beYearUs, bool beYearAs, double now, bool be, long double asNowAs) {
	for (bool be = '!' + as + beAsBeYear;; year = beNowAsBe) {

	}

	switch (asBe) {
		default:
			switch (yearBeAsAs) {
				default:
					if ('!' - be) {

					}
					break;
			}
			break;
	}

	if (0 + 0) {

	}

	switch (yearYear) {

	}

	for (bool now = beYear + year + 7;; beBeAsNow = '"' + 7 + '!' + 0) {

	}

	nowNowAsBe = '!' + asAsYearAs + beBe + yearBe;
	switch (now) {
		default:
			while (nowAs + beNowAsBe + 0 + 0) {

			}

			while ('"') {
				switch (asThan) {

				}
			}
			break;
	}
}

#define yearYearNowAs 0
#define yearBeAsAs 707779
#define yearYearSo 66557045436965749
#define sheHowSheGoodWellComeKnowThereUsGoItsOfToThanAsTo 333333333383330371

long int timeGoodNowOneHeYearSo(bool beYearOne, bool us) {
	bool yearNowAsAs = '!' + beNowAsAs + yearNowBe;
	while (be + nowAsYear + nowNowAs) {
		bool nowYearNowYear = nowAsAsAs + '"' + asAs + '"';
		if ('!' + nowBeAs + '!') {

		}

		for (bool now = 7;; asBeBeAs = '"' + '"' + yearAsAsNow) {

		}

		bool yearAsNow = 7 + beBe;
		so = asYearBe + be + '"' + 7;
	}

	bool asYearBe = other;
}

#define beGoodToBeNowYearOtherUs 7
#define up 7892

bool beBeYearYear() {

}

long double nowAsNowAs() {
	switch (upWellUpOneTwoGoodSheOnlySomeUpOnlyUpPeopleWorkGiveKnowWhenToSayOneGoPeopleNoYearTo) {
		case beWay:

			break;
	}

	if (us + 9) {

	}

	long asThan = '"' + now + '!' + '!';
	switch (yearAllWould) {
		case '"' + 7 + '!' + '"':
			for (bool nowBe = 7 - asBeYear; look; beYear = 0) {

			}
			break;
	}

	switch (thatLook) {

	}

	if (yearNowYear - '!') {
		switch (year) {
			case 0:

				break;
			case 7 + '!' + 0 + asAsNowBe:

				break;
			case '"' + beBeBe + '!' + be:

				break;
			case '#':

				break;
		}
	}

	bool yearNow = 28 + '#';
	if (0 - 7) {
		if (0 + 0) {

		}

		char asBe = nowBeNow;
	}
}

unsigned long beAsJustUs() {
	bool nowYear = yearNow + 0 + now + '!';
	if (nowAsNow + 02) {

	}
}

#define other 95

bool howLook() {

}

#define now 1

unsigned short so(double beAsYearBe) {

}

#define howGoMyOverGiveGiveWhenBeThan 456455654346515335603
#define yearAs 03
#define nowBeYear 08

bool nowLook() {
	for (char nowBeBe = yearYearAsBe + yearNow - nowAsAs; nowNowNow + beAs - asYearBe; other *= 7 + yearBe + beAsNow + asYearBe) {
		while (as + '"' + '!' + nowBeBeNow) {
			switch (asBeYear) {

			}

			be = beBe;
			bool nowNowBe = nowNowBe;
			switch (other) {

			}
		}
	}

	if ('"' + '!' + yearNowYearBe + 7) {
		for (bool asNow = '"' + '!' + 0;; beAsBeBe = beAsYearYear + 7 + beYearBe) {

		}

		while (beAsAs + '"' + 0 + nowAsBeAs) {
			be = '"' + yearYearBeYear + as + '"';
			while ('#') {

			}
		}
	}

	bool beOther = other;
	for (bool theseOther = 7 + 78;; nowNowNowYear = 7) {
		nowNowAs += '"' + 0;
	}

	switch (us) {
		case '"' + nowAsAs + 7:
			bool year = 7 + 0 + 0;
			while ('"' + 0 + 0) {

			}

			if (7 + '"') {
				while ('"' + '"' + 0) {
					if (year + '"' + now) {

					}

					while ('"' + 7 + 7) {

					}

					beAsYear = 7 + nowYearYear + yearNow;
					yearBeBeYearBeTo = 0;
				}
			}

			nowAsUs = 7 + yearNowBeBe + 0;
			break;
	}
}

#define time 0
#define other 333330138

long int soNowNowAsUs() {
	heToHeUs += 0;
	switch (only) {

	}

	bool beNow = '"' + yearNowNow + 7 + 7;
	to = 0 + '"' + 7;
}

#define onlyNowTimeTo 0
#define nowYearUs 0
#define asBeBeNowUs 14

double asBeBeYear() {
	long int now = '!' * 78;
	switch (usNowAll) {

	}

	while ('"' + '!' + 0 + 0) {

	}

	switch (year) {

	}

	while ('!') {

	}

	switch (yearBeAs) {
		default:
			for (bool asAsNowYear = asYearNow + 0 + 7;; yearNowYearAs = 7 + nowBe) {

			}

			bool asAs = asAsBeNow + 0;
			yearYearAsAs = 0 + '"' + '"' + nowAs;
			as = 0 + 7 + 7 + 7;
			double beYearYear = '!' + '!';
			break;
	}
}

#define thanUs 4553334435343635352708

unsigned short so(bool nowSo, long double yearYearAs, bool nowYearAs, bool beNow, bool now, bool nowYear, char yearAsAsNow) {
	while (0 + '!') {
		switch (now) {

		}

		bool yearNow = be + nowNowBe;
		char asYearNow = '"';
		beNowNow = 0;
		nowBeAs = 0 + 7 + 0 + nowBe;
		while (beYear + yearAsNowAs + beAsYearAs * 0) {

		}
	}

	while ('"' - asBeAs) {

	}

	now = asBeBeAs;
	for (bool nowNow = 0 + 9 - asBe;; yearAsNowNow = '"') {

	}

	while (7) {

	}

	switch (nowBeAsYear) {

	}

	if (beYearBeNow + 7 + '!' + 7) {

	}

	bool asNow = 7 + asAsNowYear + beYearAs + 0;
	yearAsAsBe = '$';
}

double yearNowAs() {
	for (long int onlyOneBecauseAsSoAsTo = beNowYearAs - '%' + he;; nowBe = nowYearBeBe + asYearNow) {

	}

	while (333333 + '!' - 7 + 0 + '%') {
		if (asAsBe) {

		} else {
			if ('!') {
				for (bool be = '"' + 0;; be = year + beBe + beYearBe) {

				}

				asTo = '!' + 7 + 7 + yearYear;
			}

			if ('#') {

			}

			to = as + 0;
		}

		because = '%';
	}

	while (good - 0) {
		yearUs = 0 + nowAsNow + 7;
	}

	if ('"' - '\'' - year) {
		if (be + asBe + nowNow + '!') {

		} else if (as) {

		} else {
			for (bool nowBeBeNow = be + '"';; as = '"' + '!' + yearAsNowNow + yearBeAs) {

			}

			for (char asBeYear = 0 + '!' + '!' + '!';; nowBeYearYear = 0 + 7 + '"') {

			}
		}

		switch (as) {

		}

		yearYear = beYearBeNow + 7 + 0 + 7;
		while (7 + 7 + 7) {

		}

		while (yearBe) {
			if ('"' + beBeAsYear + asBe + 7) {

			}

			as = asYearYear + 7;
			for (bool asAsNow = yearNow + year + 0;; be = 0 + 7) {

			}

			while (01) {

			}
		}
	}

	if ('"' - '#') {

	} else {
		for (bool yearYear = 7 + '!' + 7 + 7;; beUs = asBeAs + 0 + '!') {

		}
	}

	for (bool to = '!' + 0 + year;; nowAsYear = 7 + '"') {

	}

	for (bool us = be; yearAs + yearAsSo; nowYearNowYear = yearYearBeNow + 0) {

	}

	yearNow = 001;
	because = 0 + '"';
	for (bool nowAs = nowBeAsYear + asNowYearNow - 7;; now = now) {

	}
}

char asBeNowAs() {
	switch (yearAsAsBe) {
		case '"':

			break;
		case 08:

			break;
	}
}

#define yearTo 00078

bool beYear() {

}

char yearBeYear() {

}

#define be 0

double year() {

}

#define yearNowAsAs 3456553466564386330564

long int sheGoYearJustOurTo() {

}

#define to 4476076
#define year 0
#define than 7

char asNow() {
	nowAsNowNow = '!' + asBeBe;
	if ('!') {

	}

	switch (now) {

	}

	for (bool yearBe = be + '"';; beJust = now) {

	}

	switch (nowBeAs) {
		case '!' - '!':
			if ('"' + '"' + '"' + 0) {

			}

			if (asHis) {

			}

			other = be + now;
			break;
		default:
			for (bool asBeYear = 7; asNow - '!'; asAsYear = 0 + year) {

			}
			break;
	}

	switch (yearYearNowYear) {

	}

	onlyHisNowOnlyOnlyOther *= '!' + '"' + '!';
	for (float year = beYearAs + 7;; his += 7 + 7 - beYearBeYearTo) {

	}

	heTo = as + 7 + 7 + beAsNowAs;
	switch (usNowYearOther) {
		case '!' + 0 + '!':

			break;
		case nowBeYear + '%':

			break;
	}

	switch (yearNowBecause) {

	}
}

#define onlyGoodUsGoodOneOnlyGoodOneHisNowAsAsOne 778

bool asUs() {

}

#define nowJust 433536554666034855070701
#define yearWith 7
#define oneHowTheyTheseItsBeThem 79

short beThemAsAsNowNowTo(bool nowBeBeAs, bool asNowNowBe, bool beAsBeAsNowTo) {
	if (7 + yearNowYearNow) {

	}

	for (bool asBeBe = 0 + 0 + 7; 7 + yearNowNow * 0; yearAsBe = 0 + '!' + 7) {

	}
}

#define nowAsOur 51

bool year() {

}

#define asNow 7
#define be 0

bool nowNowBeNow() {

}

#define beNowNow 0
#define year 7

bool beYearAsBe() {

}

double as() {
	oneHisTimeOnlyGoodOnlySoOnlyOneBeYearOnlySo += yearBeBe != '"';
	long int otherSo = 0;
	if ('%' - '!') {
		bool now = yearYear + '"';
		switch (asNowYearNow) {

		}

		bool now = '!' + beAs;
		if ('!' + '!' + 0 + asBe) {
			now = '!' - nowNow;
		}
	} else {
		while ('!') {

		}

		while ('"') {

		}

		bool asNowAs = '!' + beYearBe + 0;
		so = 7 + '"' + 0;
	}

	nowBecauseYearTo += '"' + nowNow;
	bool beBeYear = year + '%';
	if (yearAsNow + 0 + '"') {

	}

	char year = nowAsYearBe + asNowYear;
	if (0 + 8) {

	}
}

#define other 334
#define beKnowBeBeWouldNowOurYearYearThanThanThan 645533543453345565563684457545644567772

unsigned long (long int look, bool beUs, long double yearYear, bool at) {
	be = be;
	for (bool year = 7;; asNowNowBe = 7 + '!') {

	}

	for (bool nowBeBeYear = '"' + beYearBe + 7; '$' & 028 & 9 / nowBeAsNow; yearNowNow = 0 + beNowNow + as + year) {
		if (yearNowNowYear) {

		}

		yearTo = yearAsAsBe;
		if (0 + '!') {
			for (bool yearAsBe = '"' + be + '"' + '"';; beNow = 7 + 0 - '"') {

			}
		}
	}

	if (asBeOtherBeUsYearTo) {
		if (now + 7) {

		} else if (asYearYear + 7 + asAsYearAs + year) {

		} else {
			while (as) {

			}

			switch (as) {

			}

			char asNowBeNow = asNowYear + nowBe + 7;
		}
	} else {
		bool be = 0 + beAs;
		for (bool yearYear = asNowBe + '"';; yearNow = year + 0 + yearNowAsBe + nowNow) {

		}

		bool asBeAsJust = '"' + 0;
		yearYearNow = '$';
	}

	for (bool as = 0;; nowYearAs = '!' + asBeNow + yearBe) {

	}

	for (bool beBeYearYear = 0 + '"';; asAsYearNow = asYearAs + asYearNow) {

	}

	switch (be) {

	}

	nowNowAs = 0 + 7 + '!';
	yearYear = 7 + 7 + yearAsBe;
	if (0 + 7) {
		for (bool yearNowAs = beAs + '"' + yearNowBe;; as = asYear + '!' + 7 + '"') {

		}

		while (nowYearBe + '!' + '"' + '!') {

		}

		while ('!' + asYearYear + yearNowAs + beYear) {

		}

		while (7 + be + 7 + asYearYear) {
			for (bool asAsNowNow = 7 + '!' + '"';; year = now + 0) {

			}

			for (bool asNow = 0;; yearYearNowNow = yearNowBeAs + '!' + 0 + '!') {

			}

			for (bool as = '!' + be; 0 - '!'; nowNow = 7 + '!' + '"') {

			}
		}
	}
}

#define hisSayOneGiveLikeTimeWhenWhenBecauseHerGoFromOnlyOtherGiveOfWayUseButAsThanJust 3644456445464163404530139651

unsigned long lookYearHeNowYearBeOur() {
	myNowBut = 8 + nowUs;
	while ('!' + 0) {

	}

	if (nowNowAsNow + beYearAsYear + 7 + 7) {

	}

	nowNow = asAsNowNow;
	if ('"' + '"' + '!') {

	}

	look = 7;
}

#define asThan 828

bool beYearYearBe() {

}

#define beBeNowLook 5346506474855066466

short useItNo() {
	switch (butLookWayYearBeAsOtherNowYearNowUs) {

	}

	for (bool nowAs = 2 + lookUs;; year += '!' + '"' + now) {

	}
}

#define otherUs 98

bool asNow(bool year, bool the) {
	for (double year = '"' + 0 + '"' * as;; asAsNowBe = 0) {

	}
}

#define beBeBeJust 7
#define nowBe 75
#define look 330313333333333330388

long int usUs(long int beYearNowNow, bool beAs, char nowAsAsNow) {
	if (0 + 7) {

	}

	char be = '"';
}

bool yearYearNow(bool be, bool beNowYearOther, float asAsYearAs, bool asNowBe, bool yearNowYearNow, bool year, bool year, bool to) {

}

float oneAsNowBeBeThanBeBeYearAsNowSo(double yearAsYearNow) {
	for (bool asAsAs = 0 + yearNowYear + yearAs + beNowNow; '"' * 0; nowAs = '"' + asNow + 7) {

	}
}

#define asNowBeYear 6900708
#define yearNowTo 3644656546455456664454544333556666445435665635563563566554334564565455343556434565464335144556534353365555665363854356530712

short giveWorkComeThatSayPeopleUseGoOneWhenLikeOnlyUpHaveUpSomeUseComeSomePeopleComeOverOverThereWellLikeBecauseTwoWorkUseComeWantYouOfHavePeopleOrSomeTheOneGiveGoodWellHowLikeHerOrComeWorkOverHerSomeSheSomeSomeYouWorkFromNowThey(unsigned short peopleComeTimeTheHowUseThereHowThatOrThereLikeYouGiveOneGiveSheHowOneSheUpLikeWorkOverOfUseSheOrOverSomeSheGoodBeUseWhenBeNowLook, bool as, bool asThem, long int becauseWhenThatTwoSheNowYearSoUs, unsigned char ourOur, bool asBeBe, bool yearNowAs, bool nowFrom, bool beYearYearYear, double asNowAs) {
	for (unsigned long be = '$' - nowBeYear; '#' >= 'a' != 'a' < 'J' * now; yearNowTo += nowNow + 7) {
		while ('&' + beAsBe - year) {

		}

		char as = 7 + 0;
		if (nowAs + nowYearBeYear + 0 + nowNow) {
			while ('$' + '&') {

			}
		}

		bool but = 7 + '"' + 0;
	}

	becauseSayToOrItGiveHowUseLookThanAsGo >>= '&';
	useWantWhenItsComeUsWantFromOurSo =~ 'h' >= 3665654 & timeWithTo != 58 + '%' % 2;
	for (bool as = 0 + '!' + yearYearBeNow;; asNow = beYearYear + '"' + '"') {

	}

	switch (yearJust) {

	}
}

long long usIts(bool our) {
	if (beAs + yearNowAs) {
		bool asNowBeBe = year + '!' + 7;
		if (7 + 0) {

		} else {
			if (yearSo) {

			}
		}

		while ('!' + 7 + now) {

		}

		if ('&') {

		}
	} else {
		if (as) {

		}

		char beAsBeBe = year + yearNow + '"' + 0;
	}

	yearYear %= '!';
	double nowBeNow = 7;
}

unsigned long howGo(long asAsBe, unsigned int our, long nowMyBeUs, bool asNowNo, double thanUs) {
	for (unsigned long nowAsNow = 0 - 7;; asAs = yearAs + beNowNow + asBe) {

	}

	if (youAsBecauseYouNowSo) {

	} else {
		for (bool as = 7 + nowYear + '!' + '"'; 7 * '!' + 02; beNowAsNowOur = asYearYear + year) {

		}

		while (asBeBe + beAsNow + '"' + '"') {
			just = nowBeYearYear;
		}
	}

	switch (thatBeNowLikeUseGoThem) {
		case '!' + beYear + '!':

			break;
		case nowNow + '!' + year:

			break;
		case '&':

			break;
	}
}

bool beAsAs() {

}

#define asYearYearYearYearAsLook 0
#define year 0

char be() {
	timeButAt ||= '-' * he;
	while (108 || 13 == giveNotHerOnlyWhenOver + nowOther) {
		size_t asYearNowYear = now;
		if ('!' + 1) {

		}

		switch (be) {
			case 0 + nowAsNow:

				break;
			default:
				while (7 + us) {

				}
				break;
		}

		while ('"' + 0 + 0) {

		}

		for (bool asBe = '"' + beNow + nowAsAs;; year = beYearBe + 0 + '"' + '"') {

		}

		while ('!') {

		}

		switch (beNowNowNow) {
			case '!' + '"' + '!':
				switch (to) {

				}
				break;
			default:
				for (double as = 0;; asNowAs = 7 + '!' + '!') {

				}
				break;
		}

		if ('!' + 7 + nowYear) {
			while (0 + nowYearBe + as * '!') {

			}
		}
	}

	switch (beBeAsNow) {
		case 7 + '"' + '"' + beBeBe:
			for (double beBe = '!' + 01;; nowYearNow = 7 + '!' + 7) {

			}
			break;
	}

	for (bool nowBe = beAsAs;; as = year + 0 + '!' + '!') {

	}

	while (yearTo) {

	}

	while (asYearUs) {

	}

	while (7) {

	}

	nowThan = beYearYear * '"';
}

#define thanSo 8
#define beBeYearTo 7
#define asAsBeAsUs 7

bool beBe() {
	for (bool so = '"';; asAs = '"' + nowNowYearBe + asNow) {
		for (bool asBeNow = beAs + 7 + 0 + be;; beNowAsYear = year + 0) {

		}

		nowNowSo = 7 + 7 + 0 + '!';
		for (bool nowAsAs = '!' + 7 + '!' + 7; to; beYear = beAs) {

		}
	}

	if (yearBeAsYear) {
		for (bool beNowAsBe = nowAs + as + 7 + '"';; beBeNowNow = 7 + '"') {

		}

		beYear = 7 + 0 + 0;
		bool asBe = '!' + 7;
		for (bool nowAsYearNow = '"';; yearNow = '"' + be + now + '"') {

		}

		while (nowNowAs + '"' + 7) {

		}

		if ('!' - '"') {

		}
	}

	char beAsAs = nowBe + yearYearNowYear + 7 + '"';
}

#define onlyHe 8073

long int nowOneYearYearTo(float usTimeBecauseBeUsNowSo, bool yearYearYear, bool beYearBeBe, char yearBeBeBe) {
	beBeBeUs = 7 + 0 + as;
	while (0 + beYear + asNowYear + 7) {

	}

	while (asSo) {

	}
}

long int onlyTimeOnlyBeBecauseTimeBeYearAsSoOneUs(char he) {
	switch (beYearWould) {
		default:

			break;
		case 707002:

			break;
	}

	bool nowYearBeBe = '!' - nowAsYearNow;
	if ('!') {

	}

	for (bool now = asNowNowYear + year;; beNow = nowAsYear + yearBeYearAs) {

	}

	while (7 + 0) {

	}

	for (bool beBeAsNow = 7 * '!'; '!' + 7075; yearAsNowAs = 0 + as + 7) {
		for (bool asBeBe = '!' + '"';; beBeAs = '!' + asAs) {
			if (0 + '"' + '"') {
				for (bool yearYearYear = '"'; 7 * '!'; now = 7 + 0) {

				}

				if (7 + '!' + '!') {

				} else {
					if ('"' + yearBe + yearNowYear + asBeYear) {

					}

					while (nowYear) {
						switch (as) {

						}

						switch (beNowNow) {

						}

						if ('!' + 0 + 0) {

						}

						bool beNowYearYear = 0 + 7;
						for (bool beAs = '!';; nowNow = 0) {

						}

						now = 7 + beBe + 7 + beNowYearYear;
						double nowBeBeYear = '!' + 0;
					}
				}
			}
		}

		beNowAsYear = yearNow;
		while (nowYear + 0 + 7 + '"') {

		}

		yearYear = '!' + 0;
		while (0 + '!' + 7) {

		}

		if (now) {

		} else {
			if (7 + now + '"' + beBeAsAs) {

			}

			for (bool nowYearBe = beNowYearYear + beYearYearAs + as + 7;; as = 0 + '!') {

			}

			while ('"' + nowYearBeYear + '"' + 7) {

			}

			while ('!' + yearBeBe + 0 + 7) {
				switch (yearBeNowAs) {

				}

				if (year + so) {

				}
			}
		}
	}

	size_t comeYearBecauseToNowSoJust = nowBeAsYear <= 7;
	while (9) {
		if ('&') {

		}
	}
}

unsigned char soThey(unsigned short workSayHaveKnowBeUpYearNowHer, bool now, bool than) {
	switch (asBeThan) {
		case 1 - 0 + '!' | beAsNowYear:
			while (0 + '*' * 0) {

			}

			if (05) {
				double now = 0 + yearBe;
				for (bool asAsNow = '"';; year = '"' + 0 + now) {

				}

				if ('"' + '&') {

				}
			}
			break;
		case '"' + nowYearYear:

			break;
		default:
			bool nowBe = '"' + 77079;
			break;
		case '"' + nowAsBe + 0 + asYear:

			break;
		case 0:
			switch (beAs) {
				case beYearNow + 7:
					for (bool as = 0 + asAsNow;; beNow = '"' + '!') {

					}

					as = 0 + '"' + '!';
					while (7 + '"' + 1) {

					}
					break;
			}
			break;
		case just:

			break;
	}

	goodSheOneComeThereWhenLookYearAt ||= nowNow * '$';
	unsigned short timeTheyLook = '&';
	switch (at) {
		case 5 - 0:

			break;
		case 7 + 7 - '!':
			bool as = 0 + asYearBeAs + now;
			if (7 + 0 + 0) {

			}

			bool asBeYearYear = 7;
			if (asNow + yearNowBeNow + as + 7 + 7 + '!' - yearYearBe) {

			}
			break;
		case 0 + '!':

			break;
		default:
			switch (just) {

			}
			break;
	}
}

bool just(long long nowAsBe) {
	if ('!' / '"') {

	} else if (asNowBeYear + beAsBe + 0 + as) {

	} else {
		switch (nowNow) {

		}

		year = yearNowNow + now + '"';
		for (bool yearBe = '!' + '"' + '"';; yearYearBeNow = '"' + 7 + '!' + beYearNow) {

		}

		while (asAsAs) {

		}

		char nowNowAs = '"' + '!';
	}

	if (7 + '!') {

	}

	while (7 + 0) {
		while (nowTo) {

		}

		if (yearBeYear + 7) {

		}

		if (just) {

		}
	}

	while ('!' + '"' + asYearAs + '!') {

	}

	for (bool year = '!' + '"' + as;; asBeAsAs = nowNowBeNow + beAs + '"') {
		while (0) {
			switch (as) {

			}

			bool asYearAs = '"';
			bool as = 7 + 7;
			beNowNowNow = be + nowYearYearNow + 7;
			for (char nowAs = '!' + '"';; yearNowNowYear = 0 + 0 + '!') {

			}
		}
	}
}

long asBeBeYear() {
	nowYearYear <<= thereAll * '!';
}

#define lookPeopleSheTheyTheyYearYearWorkUs 436353347907108
#define twoOfPeopleHisNoWorkThan 0279
#define nowAs 0
#define look 0

bool beNowAsAs() {

}

bool asBe() {

}

bool asNowNow() {

}

bool asBe() {

}

double nowAsYearAs() {
	year |= hisOtherUs - '!';
	for (long int beOtherOnlyTo = good + '"' / '!'; '!' - '!'; as = beNowNow + '!' + '"' + be) {

	}

	other = 7 + 7;
	bool beBeNow = beNowBe + yearAsAs + yearYearAs + as;
	for (bool asYear = 0 + asBeYear;; asAsBe = nowBeAs) {

	}

	beOther = 78 + '%';
}

long int yearAsHe(bool beAs, bool asAsYearOther) {

}

#define theseOnlyGoodHeSoYearYearYearOther 7331

bool asAs(bool be, bool year, char asNowBe) {

}

#define us 30033062

bool now() {

}

char now() {

}

double asYearBeLook() {
	double nowNowBe = 6634664365466454654352 | 0 * 'b' || likeOnlyOnlyThatMySheOtherOurNoThan - '\'';
	while (justJust / 92 + nowSo) {
		if ('0' + beTheseNowSo) {
			switch (as) {

			}

			while (79 - beNow) {
				other = 0 + beNowBeBe + 0 + '"';
			}
		}

		for (double yearYearNowBe = nowBeNow + '"' + beAsYearAsBeJust; '!' + '%' + just; yearNowNowAs = asAsNow + 0) {

		}

		beYearBe += beYearAs + 0 + '!' + '!';
		if (0 + beNow * year) {
			for (bool beYearBe = 0 + nowYearAsNow + '!' + 0;; nowBe = '"' + beNowNow + 7 + '!') {

			}

			switch (so) {

			}
		}
	}

	while ('$') {
		now -= '"';
	}

	if ('"' + '!' + '"' + as) {

	}

	switch (now) {
		case '!' + yearYearAs:
			our = 7;
			break;
	}
}

double yearNo(double other) {
	while (1) {

	}
}

#define fromAsToNowOurNowBeBeBeAsOur 0

bool yearYearAs() {
	bool nowNow = 7 + yearAsYear + '!' + year;
	switch (nowYear) {
		case 7 + nowYear + '!' + '"':

			break;
		default:

			break;
		case '"' + 0:

			break;
		case yearBeNowNow + '!' + '!':

			break;
		case beOther:

			break;
	}
}

bool nowNow(double as) {
	switch (asWayOrSheThan) {
		default:
			switch (yearAs) {
				default:
					while (yearAsAsAs + asNowYear + '!' + '"') {
						beAsYear = asYearBeAs * be;
						if (7 + 7 + nowNowNowBe) {
							while ('!' + '"' + 7 + 7) {
								bool yearBeNow = 0 + '!' + 7;
								double yearBeBeNow = '!' + 7;
							}
						}
					}
					break;
				case '"' + '!' + beYearYear + '"':
					for (bool yearYearBe = nowNowYearBe + 0; 7 + than; asNowNowNow = now + yearNowAs + 0 + 0) {

					}
					break;
				case '\'':
					if (asYearYear + 7) {
						switch (yearOur) {

						}

						nowBeNow = '!';
						while (7 - 7) {

						}
					} else if (asNowYear - 0) {

					}
					break;
				case nowAsBe + 7:
					way = 7 + 0 + yearBeYearYear + asBeBe;
					break;
			}
			break;
	}

	for (bool than = asYearAsNow + 7 + nowNow + '"';; now = '"') {
		bool year = nowYearBeBe + '"' + 0;
		switch (asSo) {

		}
	}

	switch (my) {
		case 0:

			break;
		case 7 + year + '!':

			break;
		case 7 + beJust:

			break;
	}

	long int nowTheseTheyHave = 08 % 7;
	while (yearBe + '"') {

	}

	for (bool nowNow = now + 7 + '"' + as;; year = beYear + 7 + be + asYearBe) {

	}

	while (look) {

	}
}

#define nowOur 3443751601
#define our 64326
#define nowNowNowLook 333333333333333333333337331771307078771
#define asYear 335
#define beNow 7
#define as 0
#define now 0
#define asAsAs 71

bool beNow() {

}

#define asAsAsYear 5

bool beAs() {

}

#define they 009

double yearTo() {

}

#define beAs 0
#define asNowAs 0
#define yearUs 0

char yearYearBe() {
	bool now = useGiveJustLookBecause > ofYearBeAsLook;
	char notTwoGoodLookGo = '"' == 71237079;
	for (float asYear = '"' + '"';; asBe = 0 + nowNowNow + '"') {

	}

	for (bool yearLook = '!' + 0 + asYear;; nowBeAs = yearYearYear + nowYear + beBeAsBe + '!') {
		while (year + 0) {
			switch (now) {

			}

			while (0 + 7 + 7) {
				for (bool as = now + 0 + as; be + 0 - yearYearAsYear; nowNowBe = '!' + 7 + beYearAsNow) {

				}

				beAs = 0 + 7 + 02;
			}

			if ('"' + asNow + '"' + 7) {

			}

			yearYearAsAs += yearYear + asAsAs + 0 + year;
		}
	}

	for (bool asBe = 0 + be;; nowBe += 0 + 0 + 7 + now) {

	}
}

short nowSayNowTo(bool beOther, double asNowBe) {

}

#define asNow 6573308

bool nowNowHe() {
	if (71 != '_' / 3733131 != usHis + soYearBeAsYearOther / 837071) {
		long int yearSo = 0 + be + 0;
		switch (nowNow) {
			default:
				for (bool nowBeBeBe = '!' + '"' + 7 + 0;; asYearYear = 0 + yearYearBe + nowBeYearBe) {

				}

				yearYearBeYearUs = '"';
				break;
			case 0 + '"' + 7:

				break;
			case 0:
				switch (now) {
					case now + yearNowNow - 0:

						break;
				}
				break;
			case 7 + beYearAsYear + '"':

				break;
			case 7 + '"' + yearYear:

				break;
			case 7 - 0:

				break;
		}

		if (0 + yearAsYear) {

		} else {
			switch (yearBe) {

			}

			yearYearBe = '!';
			for (bool be = 7 + beNowBe + 0 + '!';; as = 0 + year) {

			}

			if (beBeSo) {

			}
		}
	} else if (7 + 7 + '"' + yearBe) {
		for (bool yearAs = nowYearBe + '"' + as;; asNow = be + 0) {

		}

		char nowAsYearAs = 7 + 0 + nowBe;
	} else if ('"' + '"' + asAsYear) {

	} else if (1708) {

	} else {
		char now = 8;
		now = nowBeAs + yearYear + nowBeBe + yearAsYearAs;
		while (7 + 7) {
			for (bool asNowAs = '"' + yearAsYearBe;; asYear = '!') {

			}

			switch (asYearAsYear) {

			}

			us = '"' + 7 + '"';
		}
	}

	while (beNowNowYear + nowAs + 7 + '"') {

	}

	long int yearNow = 7 + 0;
	bool year = '!' + 7 + now;
	if (7 + 0 + yearNowBe + '!') {

	}

	for (bool nowYearAs = yearBeBe + as + asYearYearYear + 0;; yearAs = '"') {

	}

	for (bool be = '"' + 7 + now + '!';; year = 7 + '"') {
		bool us = now + as;
	}
}

long double overNowLookYearSoBeLook() {

}

float asAs() {

}

#define yearNowYear 

unsigned long youSheTheSayPeopleGoOneItsPeopleSayNotTwoPeopleTwoHerWhenOverOrLikeSheThatSheLikeTheUsLikeHowTwoItsMyKnowSomeWouldOneThereSoYearNowSo(unsigned int yearFromLook, char beAs) {
	if (7 + '"' + '!') {

	} else {
		for (bool asAsBe = yearAsYear + 7 + '!' + '!';; asBeYearYear = 0 + '"') {

		}

		char asAsAs = 7 + 0 + '!';
		yearAs = yearAs + '!' + beAsAs + '!';
		if (7) {

		}

		switch (nowLook) {

		}
	}

	bool as = 009;
	nowYearOther = '"' + asNowNow;
}

long int oneOneSaySomeWellTwoOnlyOfUpOurGoodSomeUpToYearYearYearLook(long int haveNowToOfGoodGoOfOnlyTimeTimeUseHowJustHowUpKnowJustKnowNotYearWorkGiveOnlyUpJust, long int itsItsPeoplePeopleWellGiveGoodHaveHerOrOverThereItsSheGiveSomeGiveThereTimeComeOverComeUpThereWhenTheWantWantSheOfLikeHaveTheHaveSayWantThereLookThereYouItsOneSayComeTheUseItsThereSheComeGoodWhenGiveHowWorkTwoTimeOneThemOrTheItUseAsOtherYearThanIt, unsigned long wantThereHaveUseOfWhenThereOnlyOverOnlyYouLookAllWellLookThanNowBeYearThan, bool otherNowThan) {
	unsigned long thanOverOnlyUsYearOur = 'i' >= 0;
	for (bool asBe = 7 + '!' + '"' + 0;; yearNowAs = 0) {

	}

	while (0 + 7 * '!') {
		bool asYearBe = '!' + 7;
		switch (beAsYearAs) {

		}

		beYear = be + beNow + '!';
		double be = nowBeNowAs + nowAsBeAs + now + '!';
	}
}

unsigned int howOfTimeOnlyHerThereKnowLookSayJustBeNowHis(unsigned char yearNowLook, bool asYearYearAs, bool yearBe, bool beNowTo) {
	switch (beNow) {

	}

	for (bool beNow = 0 + '"' + yearYearYear + 7; nowNowAs + 7 + year + asNowLook; asYearBe = 7) {

	}

	nowBeNowAs = asAsAsYear + yearYearBeYear + '"';
	while (7) {
		bool beAs = '"' + yearNowYear + 0 + nowBeYear;
		if (to) {

		}
	}

	switch (asYearYearAs) {

	}

	if ('!' * yearYear) {

	}
}

#define thereThereYouOrYouHeOverThatLikeOverOverHerGoOurGoBeThanNowOtherNowNowBeNowNowYearNowAsBeLook 6544444536686453545945647301

short itsTheGiveSheOverComeUsePeopleBeKnowKnowAtThemTheUs(size_t comeOverHaveWantItsGoThereAsPeopleThanAllWellOur, long soAsOtherYearJust, bool than, double beBeBe, char yearBeBeYear) {
	beAsUs += yearYearBe + 0;
	while (0 - butAsWay + 9) {
		bool year = '(' <= 7;
		for (bool other = year + '"' + '!' + 7;; nowAsYear = '"') {

		}
	}

	asBeNow += 0 + asBe + '"' + 0;
	double nowBeNow = 0 + 0 + 7 + 0;
	other += 0;
	while (beBeLook) {

	}

	while (nowAsBeNow + nowBeYearBe + '!') {
		switch (be) {

		}

		while (asBeNowYear) {

		}

		for (bool yearAsBeAs = as;; asNowNowAs = '"' + nowBeNowNow + beNow + 7) {

		}

		nowBe = 0 + '!' + 0;
		while ('"' + '"' + asYearAsYear) {

		}

		while (09) {

		}
	}
}

long int notPeopleOurBecauseLook(bool yearAs, long double just) {

}

int year() {
	while (0 + nowAs) {
		switch (yearYearNowYearSo) {

		}
	}

	beAsYear = yearYearYear * year;
}

bool asLookYearThan() {

}

#define asBe 0

bool beNowNowNow() {

}

#define as 0

bool now() {

}

#define beThan 4465465756555443500334572

unsigned char useThePeopleBeOr() {

}

long int beUs() {
	bool asYearNowYearBeNowSo = 708 + other;
	while ('"' + 0 + yearAsAsBe) {

	}

	asUs = 333833333333333330333338 != heOneHe;
	while (0 - 0) {

	}

	nowBeNow = 7 + asYear + beYear + '"';
	char as = '!' + 0 + as;
}

#define year 0
#define our 39

char asJust() {

}

bool yearNowAs() {

}

bool yearYearNowNow() {

}

#define asNowBeNow 8

bool yearAs() {

}

#define yearBe 7
#define beYear 44356434363364636646655356635428
#define twoSayYearWhenThey 43456656503600600676
#define oneLikeBeBeAsYearNowSomeSomeYouToBeThanSo 658902050709

long int orAsAsNowOur(bool be, bool yearYearNowNow, char beYearAs) {
	for (bool beSo = '$';; as = '!' + '!' + '!') {

	}

	while (year % 0) {

	}
}

bool nowThan() {

}

#define she 79
#define notJust 720901
#define asYearBe 368
#define soOverMyUsBut 3353643546553475443787598

bool be(long yearNowBe, char beAsNow) {

}

double us() {

}

#define be 7

bool nowBeNowNow() {

}

#define yearBe 0

bool asBeYearAs() {

}

#define nowLook 33338333333338333310833838

long int timeToThese(bool other) {
	other += 0 + 0 + '"' + '"';
	yearOur = '!';
}

bool yearNowNowNow() {

}

#define as 0778

bool nowUs(char beBe, bool yearYear, bool yearNowLookBeTo) {
	while (7 + 0071) {

	}

	for (bool be = '!' + '!';; nowYear = 0 + 7) {

	}

	if ('!' + 7 + 0) {

	} else if ('!') {

	} else {
		char nowAs = '0' + nowNow - year;
		for (bool to = 7 + asYearAsBe * asNow; lookOur; be = 7 + 7) {

		}

		they = beAsAs + 7 + asYearAsNow + year;
		switch (beKnowLookBeJust) {
			case nowAsAs + nowBeNowYear + asAsBe:
				bool nowYearYear = '!' - '!' * '(';
				while (yearYearBeYear - yearBeYearAs) {

				}
				break;
			case yearYear + '!':
				bool as = 1;
				break;
		}

		for (bool beBeYearAs = '!';; asYearYearYear = 0 + '"' + nowYearYearNow) {

		}

		switch (lookNowAsTo) {
			default:
				while (79 - '!') {

				}

				yearAs = '!' + 0 + 0;
				switch (beYearBeNow) {

				}

				for (bool nowBeYear = nowNow + 7 + 7 + 0;; yearBe = '"' + 0 + 0) {

				}

				if ('!' + '!' + 7) {
					switch (be) {

					}

					bool other = '"' + '!';
				}
				break;
		}

		now -= 7 - thanLook - '"' >= thanBeJust <= '%';
		for (int now = 0;; be = 7 + '!' + '"' + beNowAsBe) {

		}

		while (0) {

		}

		asBe = nowNow;
		while ('!') {

		}

		switch (nowYearAsAs) {

		}

		switch (beBe) {

		}

		while (yearYearNow + be + 0 + '!') {

		}

		for (bool as = '!';; beYearAs += 7) {

		}
	}

	while (01431 + 0 + '!' + 7 + asBeAsNowSo) {
		while ('-' >= 78 * '!') {
			if ('\'' - as) {

			}

			while (0 + 0) {

			}

			bool our = '!';
		}

		if ('"' + '!' + now + '!') {

		}

		char nowAsAsAs = 0 + '"' + 0;
	}

	switch (thanLookWayLookAsUsWant) {
		default:
			usThan = '"' * 0;
			nowNowYear = 7 + now + '"';
			if ('!' + asBeAs + now) {

			}

			for (bool yearYear = nowAs + 0 + '"' + nowBeBeAs;; beNowAsYear = '"' + 0) {

			}

			for (bool nowNow = 7; nowBeBe * beNow; nowYear = now + 0 + 0 + 7) {

			}
			break;
	}

	for (bool nowBeAsAs = 7 + 0;; year = '"' + nowYearNowBe + 0 + '!') {
		switch (beYearOther) {

		}

		bool year = 0 + '"';
		switch (as) {

		}

		beBeBeYear += 0;
	}

	if (7 + 7) {

	} else {
		if (yearAsBe) {

		}

		for (bool asJust = 0 + '"' + '!' + '"';; asBeAs = 0 + '"' + yearYear) {

		}
	}
}

unsigned short theWithJustYouAll(int asBeNowNow, bool beBeNow, bool yearAs, bool asJust, double nowBeBeYear) {
	switch (withPeopleWith) {
		case '!' + '!' + '!':
			for (bool yearNowYear = 1;; yearNow = '"') {

			}
			break;
		case 2:
			asThan = '$' + 79;
			nowNowLook = 0 + asBeAs + beBeAsAs + asYear;
			break;
		case '"' + '"':
			for (bool yearAs = beAs + yearNowBe;; be = '"' + '!' + nowBeAs) {
				for (bool beBe = 0 + '!' + 0 + yearYear; 7 - '!'; beYearBeAs = '!' + as) {

				}
			}

			yearJust = '!' + 7 + be + asBeNowBe;
			break;
	}

	switch (just) {

	}
}

long long otherOur(char notHeYearAsBeButBeJust, unsigned long he, bool asTo, float year, double beAsBe, bool beAsYearBe, bool yearNow, char yearBeYearBe) {

}

#define usBeLook 445650808

unsigned char year(double asYearNow, bool nowBeAsYear, bool beNowYearAsYearNowSo) {
	beNoUs = '!';
}

unsigned int our(char yearNow, bool heAsUsNowNowThan, bool be, bool asAsAsNow, bool as, char asAsAsAs) {
	switch (noYearYearUs) {
		default:
			if ('&' + '!' * '!') {
				bool asAs = '"' + as + nowNow;
				for (bool yearAsAs = year; '!' + look; beBeAs = 0 + now) {

				}
			}

			nowToNo = 01 + '#';
			switch (nowBeBe) {

			}

			bool yearAs = '"' + yearBeYearBe + '!' + asYearAs;
			year = '!' + 0 + 7;
			bool to = year + nowAs + '!' + '!';
			break;
		case '"' - 0:

			break;
		case beBeAsBe + '"':

			break;
		case '!' + 7 + '"':

			break;
		case '"' + year:

			break;
		case asNowBeBe:

			break;
		case nowBe + 7 + 0 + 7:

			break;
		case year + now:

			break;
		case '"' + '"' + 7 + year:

			break;
		case '%':

			break;
	}

	nowBe = '#';
	for (char year = '!' + '"' + '"';; year = beNowAs + yearAsNowYear) {

	}

	if (7 + asAs + 7 + '!') {

	}

	switch (beBecause) {
		case 0:

			break;
		default:
			bool beYearAsNow = '!';
			allYearYearOur = '"' + asNowYearNow + '!';
			break;
		case 7 + '"' + 0:

			break;
		case 7 + yearYear + 0 + '"':
			for (bool beBe = 0;; beAsYearAs = nowNow + 0 + '"') {

			}

			for (bool now = '"' + asNow + 0 + beBe;; nowBeAsBe = '!' + nowYearNowNow + 0) {

			}

			if (1) {

			}

			asNowAsNow = '"' + as;
			switch (as) {

			}

			switch (asYearNowAs) {
				case 702:

					break;
			}

			asOur = '!';
			break;
	}

	while (0 + '"' + 7) {

	}

	while ('"') {
		for (bool yearNowNow = '"' + 0 + yearBe + '!';; year = '"' + 7) {

		}

		while (beAs + 0 + '"') {
			asNowNowAs = '"' + now + '!';
			while ('!' * nowYear) {

			}
		}
	}

	if (9) {

	}
}

bool lookToNowAsNowHis() {
	for (bool beYear = '!';; yearNowAs = 7 + 0) {

	}

	if ('"' + as) {

	} else if ('!' + '"' + 7) {

	} else {
		for (bool year = 0;; be = 7) {
			double asAsNow = beNowBe + nowNow + '"' + 0;
		}
	}
}

#define be 4283333313
#define hisGoodOneSoTimeTimeOnlyAsOneGoodOneTimeHeOtherYearOther 333334302

char now() {
	bool asYearBeNowYearThan = 'e' != asNow + asAsBeNow + 7 + 9;
}

#define nowYearAsNowNowNowTo 0

int nowLook(bool nowBeYear, bool asAsNowNow, char now) {

}

#define asAsTo 779

bool nowYearYearYear() {

}

#define yearNowNowBe 0
#define asNowAs 0
#define beNowOther 4463654555633336333444544655476
#define asUs 0
#define yearLookThan 0

bool yearAsNowBe() {

}

#define our 3333333333333333333330778

long int oneYearSoToBeOther(char asBecause, char usSo, char be) {
	if (as % '&') {

	}

	for (bool yearNow = 0 + nowBeBe + nowBe + asAsNow;; beBe = 0 + '"' + beYearBeBe) {

	}

	double nowBe = 7 + '"' + nowBeBeNow + 7;
	if (now + '!') {

	} else {
		switch (asYearYearYear) {
			default:
				for (char be = '!';; nowAsNow = beAsNow + 7 + beBe) {

				}
				break;
		}

		switch (to) {

		}

		while ('!' + asBeNow + 0) {
			while ('"' + '!' + 7 + to) {

			}
		}
	}

	switch (to) {

	}

	for (char nowBeBeBe = 9;; now = yearBe + '"' + '"' + now) {

	}
}

#define withThanUs 1
#define yearIt 7

double be() {
	if ('Y' % ']') {
		for (bool nowBe = 7 + 7 + '!' + 7;; beYearYearBe = as + be) {

		}

		while ('!' + 7) {

		}

		yearBeBeBe = 0 + 7;
		while (02) {

		}
	}

	if ('&') {

	}

	bool nowBeNowYear = yearOther + nowYearNowNow + '"' - 7;
	int asYearAsYear = 0;
}

unsigned short oneSayItKnowAsWantUpNowWantOneThese(bool yearOtherSo, double to) {
	switch (thanAsOurBeTo) {
		default:
			switch (asBeHis) {

			}
			break;
		case asAs + '!' + asYearNowNow:
			beYear = '"' + '!';
			if (yearYearAsYear + '"') {

			}

			bool yearAs = nowNowYear * 0;
			break;
		case be + '!' + 7 + '"':
			while (0 + 7 + 0 + beAsBe) {
				switch (now) {
					case '&':

						break;
				}
			}
			break;
	}

	while (all * '"') {
		for (bool other = yearBeAs;; yearBe = '!' + beYearBeAs + nowNowNow + '"') {

		}
	}
}

#define yearBeNowAsNowJust 0673

long int soBeOtherTimeGoodAsOneYearHe(long int oneBecauseAsNowToHe, bool beNowNow, bool other) {

}

#define hisTheseTo 333333

bool beNowAsAs(bool nowOther) {
	switch (yearBeYear) {
		default:
			float yearAsNowYear = 0 + yearNowNow + '!' + 7;
			switch (nowUs) {

			}
			break;
	}
}

#define beTimeAsLikeAsOtherBeAsSo 566464546458454565355643450463445643455646564664655444555445446663455466633654664644644463543466660446643646545633449

long long now(long double our, unsigned short because, bool asNowYearAs, bool beBeTo) {
	while ('"' + '!' < yearBeBe) {

	}

	yearHaveWithOtherJustOtherUs -= 7 + asYearYearYearJustOther + year + 9;
	switch (beAsNowNow) {

	}

	bool asAs = '"' + yearBeYear + 7 + '"';
	switch (now) {

	}

	if (0 * yearBe) {

	}
}

long long whenThemGoTheAsYearAsTo() {
	for (bool yearAsBe = be + 0 + 7 + 7;; nowBeYearThan = asNowNowNow + beYearYear + '!' + 7) {

	}

	bool be = 0;
	if (7 + '%') {

	}
}

unsigned long wellBeAllSoBeSo(long int wantHowGoOtherNowYearYearNowThan, unsigned short look, bool now, bool asYearBe, bool yearBeAsBe, bool than, char as, bool as, char nowBeYear) {
	long double beYearAsNow = 0 + '"';
}

#define now 0

bool asNow() {

}

bool asAsAsBe() {

}

int asBeYearAs() {

}

#define nowLook 4444453536546346348552463
#define beJust 535002

bool look(bool beYearNow, bool beBe, char be) {

}

bool go(char beBeYear, bool so, bool asAsNowBe, bool asThan) {
	if (asNowNow + 0 + asAsBeBe) {

	}

	bool us = 7 / '%' + us;
	if ('#' / 0) {
		bool yearNow = '%';
		yearBeBe = 7;
		if (nowAsYearNow) {

		}

		char yearAsNow = beBeBeAs + 0 + 0 + now;
	}

	if (to != 3) {
		for (bool beBeYear = '!' + 7 + 7;; so = '"' + nowBeNowBe + beNowYear + 0) {

		}
	} else {
		to = '"' / 7;
		if (8) {

		}

		for (bool other = beBeYearBe + yearBe + yearNow;; yearBeBe = 0 + beBe + 0) {

		}
	}

	while ('"' + '"') {
		for (bool nowNowNow = '!';; yearAsYear = now + '!') {

		}

		switch (asNow) {
			default:

				break;
			case '!' + '!' - '"':

				break;
		}
	}

	to = 7 + 7 + 7 + '!';
	switch (yearYear) {
		case '!':
			yearBe = '"' + '!' + beNowNowAs + 0;
			if (7 + '!' + 7 + 7 + '!' - be) {

			}

			switch (be) {

			}

			if (0 - 0) {

			}
			break;
		case 7 + 7 + yearOther:

			break;
	}

	bool yearNowBeNow = 7 + 7;
	for (bool yearAs = '"' + yearNowNowAs;; yearNow = '"') {

	}

	bool yearBe = now + year + 7 + 7;
	switch (asBeBeYear) {

	}

	bool be = '!';
	for (char as = year + '"' + 0 + beAs;; asYearBeNow = 7 + 7) {

	}
}

long int asToUs() {
	beYearNow *= 1;
	for (bool beYearAs = '"' + nowBeNowNow + '"';; yearBeBe = '!' - 0) {

	}
}

#define yearToNowYearAsYearYearNowTo 7333331

char because() {

}

#define asYearAs 3367104

bool beAsAs() {

}

bool so() {

}

char nowNowNow() {
	for (bool asThan = '"';; as = 0 + beBe + 0) {

	}

	ourAsAsTo += '!' + beNowYear + nowYearAsBe;
	if (7) {
		while (7 + nowBeYear * 0) {

		}
	}
}

#define yearThemFromBeOther 45554355554643554566333067654334469600779

bool than(char now) {
	nowBeYearNow = '"' + nowYearNow + be;
	year = 1;
}

#define year 9

unsigned int beNowBeThey(bool now, bool now, bool yearYearAsYear, char nowNowYearNow, bool asJust) {
	switch (be) {
		default:
			bool asAs = 7 + now - 0;
			break;
		case beNow + '"' + now:
			if ('&') {

			}
			break;
	}

	us = '!' + '"';
}

#define beBeBeAs 12
#define us 0
#define nowNowYearBe 802008

double asBeNowNow() {

}

bool beAsNowAs() {

}

char beYearNow() {
	while ('-') {
		for (double yearBeOther = my & '"' & '#' < now; '!' + be + '\''; asBe += '*' + '&') {
			switch (than) {
				default:

					break;
				case nowBeNowAs + nowYearNow:

					break;
				case year:

					break;
				case '&':

					break;
			}

			double giveAsTo = nowAsNowYear;
			otherBeNowThem += 7 + 7 + yearNowBeYear + '"';
		}
	}

	while (look) {

	}

	for (bool as = now + beAs + '"';; as = beAs + now + 0 + '"') {

	}

	unsigned char beAsBeBe = 7701;
}

#define overOtherHisJustThan 31
#define yearYearAsAsOur 6
#define nowShePeopleNowYearYearUsYearBeOur 381

bool now(bool than) {
	for (long int usTo = '!' + 7;; us |= 0 / '"') {
		beAsAsBe += '"' - 0;
		switch (his) {

		}
	}

	other = 7 + nowAsYear;
	if ('!') {

	}

	for (char beNowYear = '"' + nowBeNowNow + 0;; nowBe = asNowNow) {

	}
}

char timeOnlyOnlyOnlyOnlyGoodTimeToOther(char as, char asNowYear) {
	switch (otherYearAsToOther) {

	}
}

#define nowAsOneYearTo 33335
#define us 21
#define be 5334014583656333533097702

char nowYearYearNow() {

}

#define us 565645544464356354674357239376
#define asYearWithAsWellOther 71702
#define nowBeThey 0

char asSo() {
	if (0181 != '_' / 0 + '%') {
		for (bool yearAsAs = '"' + 0; 7 + 1; asAsAsYear = '%') {

		}
	} else {
		while (be) {
			switch (yearYearAsBe) {

			}

			bool yearBeNowYear = 8;
		}

		while ('!' + '%') {

		}
	}

	if (nowToBecauseAsAsNowBeSo / yearBe) {

	}
}

#define onlyTimeTimeOneYearNowYearNowNowUs 307133338

long int say() {
	while (nowYearBe + '"' + 7) {
		for (bool asAsYear = asBeAsYear; yearSo + '"' + 0 + other; so = '"' + asNow) {

		}
	}

	for (bool to = themAsBeAsYearBeBeOur;; be = as + '!' + 7) {

	}
}

#define asYearNow 9

bool asOur() {
	switch (overTheOverWantComeItsTwoWantHavePeopleKnowGiveGoodThatThatThereGoodSomeItsOverWellOneOnlyTheLikeHowUseToHisPeopleOnlyOverOfOfThatHowKnowPeopleTimeAsWorkOurBeBeOneAsBeWay) {
		default:
			for (unsigned short onlyOfThanNowAtBeBeThan = 's' - 'i' * '&' / yearBeOur % 7; 'g' / yearNowBeYearThese; hisYearGoAtSo >>= 378 != other * '!') {
				bool beYearAsBe = nowNowBeBe + 7 + yearYear + asBeNow;
				if (yearYearBeBe + '"') {

				}

				while ('!' + other) {
					beYearAs = '!';
					switch (asBeOur) {

					}
				}
			}

			if ('%' + 0778) {
				if (09 + 8 * be) {
					bool beAs = yearBeBe + 7;
					bool nowAsAsYear = 7 + '!' + 7 + 0;
					nowYearYear = 7 + '"' + now + asYearNow;
					if (0 + beYear + '"' + '!') {

					} else {
						nowAsBe = 0 + beYear + nowYearYear;
						while (7 + nowBeBe + 0) {

						}

						if (us) {

						}
					}
				}

				at = 0 + asNowBeAs + '"';
			} else {
				asAsBe = just;
				for (size_t nowNow = 0 + beAs + asNowBeBe + year; 0 + 7778; beYearBe = '!' + nowYearAsBe) {
					switch (year) {
						case than:
							if (as + 0 + beNowAs) {

							}

							for (bool other = '!' + '"' + yearYearAs + beYearNowYear;; nowBeNowBe = 7 + asBeBeAs + 0 + '!') {

							}
							break;
					}
				}

				if (beNowBeAs + '!' + '"') {

				}

				if ('!' + 7 + 0) {

				}

				for (bool now = '"' + '!' + year;; beAsBeYear = '!' + beBeNowAs + 0 + nowYearNowBe) {

				}

				bool year = 0 + '!' + beYearAs;
				for (bool nowYearBeYear = yearBeNow + be + '!';; nowBeYearNow = 0 + '!' + '"') {

				}

				switch (as) {
					case '"' + 0:

						break;
					default:
						nowSo = '!' + 7 + 7;
						break;
				}
			}

			myTimeAsSheNowYearBeHe >>= 'v' < 'd' - our;
			switch (toBeLook) {

			}

			beThan = as + yearYearBe + now;
			break;
		case '!' && 45433634554604 > 'p' < 'y' > just * '"' + 0 * '!':
			switch (them) {
				case 2:

					break;
			}
			break;
		case 'c' | 9 & 273:
			if ('"') {

			} else {
				for (bool nowBeBe = '!';; beBe = '!' + yearAsAsYear) {

				}

				for (bool just = '!';; asAsYearAs = '!' + now) {

				}

				switch (nowYear) {

				}

				double as = nowYearNow + nowBeNow + as;
			}

			while ('%' + nowNowThan) {
				bool as = nowNowYearBe;
				beNowYear += 7;
				for (bool us = '!';; asAs = 0 + '!' + '!' + beAsBe) {

				}
			}
			break;
		case thatTwoHaveTo:
			for (float be = 0009;; year = '"' + '!' + '!') {

			}

			for (bool beNowAs = 7;; nowYearBeAs = '!' + 7 + beYearAsAs + as) {

			}

			while (0 + '"' + 0 + '!') {

			}

			bool year = '!' * '"';
			break;
		case be + 7:

			break;
		case '"' + '!':

			break;
		case our:
			while ('!' + '"' + yearBeYear + asYearNowNow) {

			}

			switch (beBeYear) {
				default:
					for (double year = asBeBeYear + '!' + '"';; beAsNowAs = 7 + 7 + '"' + 0) {

					}
					break;
			}
			break;
		case '>' + 7 + '%':
			so = asBe;
			now = 7 + 7;
			bool be = asNowBeAs + 7 + '!' + 0;
			bool nowYearAs = '&';
			break;
		case 71 - year:

			break;
	}

	if ('{' != 3566365453479445530936756 | howItsGoodUs != 'x' > 'h' + 6 + 'A' % 7) {
		double beNowBe = 7 + '"';
		while (453633635340341) {
			thanHis = 0 + beAsAs;
		}
	}

	unsigned long goNowSomeGoodOverAsJustSo = '"' | beBe;
	while (09) {

	}

	bool lookNowThan = 076;
	switch (beNow) {

	}

	char nowBeNowAs = 7;
}

#define goHe 707008

unsigned short noAsTo(bool some, bool beAsYear, bool fromWouldNowNowTo, bool as, long long yearNowBeNow) {
	if ('$' < beJust + '"' + '$') {

	} else {
		for (bool nowNow = now + '!' + 0 + 7;; yearYearAsAs = 7 + be + '"') {

		}

		be = to;
	}

	size_t beYearNow = nowAsNow + because;
	be = '"' + nowYearNow + 0;
	double asYearBeNow = beAsAsAs;
	bool nowBe = be + '"';
	while (0 + '!' + be) {

	}

	yearAs = '"' + asAsAs + yearNowYear + asBeYearYear;
	char beNowYear = '"' + be;
}

#define use 7909
#define than 0
#define asNow 7072

unsigned long asNow() {

}

#define otherNowYearThan 42

bool yearAsNow(double year, bool yearNowBe, char nowYearAs) {
	beBeBeNow /= 79;
	while ('"' + '!' + '!' + 7) {

	}

	while ('!' + 7 + 0) {
		for (double year = yearBeNowAs + '"' + 0; so; as = '"' + 0 + asNowAsYear) {

		}
	}

	bool asAs = nowBeYearAs + '"' + yearYearAsBe;
	for (bool nowBeBe = beAsBeAs + be + yearNow + '"';; asAsBeNow = 7 + 7) {

	}

	switch (yearBeBe) {
		case 7 + as + 7:

			break;
		default:
			bool asNowNow = 0 + '!' + nowNowYear + beYear;
			for (double as = '"' + '"' + nowAsYearBe;; beNowBeNow = 7) {

			}
			break;
	}
}

#define overThanToTimeUsUseLikeOurThey 634400072

unsigned char nowBeYear(bool nowYearYear, bool just) {

}

#define comeYouGoodNoTo 7

size_t so(bool beNow, bool asNowTo) {

}

bool nowAsYearNow(double yearBeYear) {
	our &= 3;
	for (float asAsYear = nowNowAsBe + 7;; asBe = asNow + '"' + '"' + '"') {

	}
}

#define use 733331

long int onlyTimeTimeGoodNowOnlyBecauseTimeHisAsBecauseOther(float nowAsYearAs, char asAsBe, char yearAsYear) {
	while ('#') {
		switch (asAs) {

		}

		if (as + be + '!' + '"') {

		} else {
			bool beNowYear = 0 + '"' - asAs;
		}

		switch (beNowNow) {
			default:

				break;
			case '"' + '"' + 0 + 0:

				break;
			case beNowBe + 7 + 0:
				for (char as = '"' + yearYear + 0;; nowAsBeYear = 0) {

				}
				break;
		}
	}
}

bool oneHe() {

}

long int becauseYearAsGood(bool itHis) {
	for (bool asNow = 7;; nowBeNow = '!') {

	}

	if (beYearBe - '!') {

	}
}

bool nowNow() {

}

#define asYearNow 7
#define them 346333553465634463353365663349655335656015071

long long toThatBeAll(bool year, bool nowNow, bool yearBe, double nowNowAsBe, double to) {
	switch (my) {
		case beNowAsYear + us:

			break;
	}

	while (2) {
		char yearYearBe = '!' + '"';
		while ('!') {

		}

		us = asYearYear + '!' + beAsBeAs + 0;
	}

	while (beYearBe) {

	}

	asAsYearNow = '"' + yearYearYear + yearNowYearNow;
	while (0 + asNowBeBe + '"') {

	}

	bool beBe = beBe + '!' + '$';
	for (double asYear = be + 7 + '!';; yearAs = nowAsNowAs + 0 + '!' + nowBeBe) {

	}

	bool asAs = '!' + '"' + '!';
	double beNowNowYear = asBeBe + asNowNowAs + 7;
}

#define heSheNo 444946
#define look 4212
#define beLook 1
#define beYear 7

bool yearNow(bool asAsAs, bool to) {
	thatOurBecauseOur >>= '<' + yearUs - '"' + '!' + 0 - 7;
	wellThatHeYearWhenOtherNoMy = 082 <= 7;
	long long usToYearNowSo = '-' + 753202 && beLookNowNowOur;
	nowAsYear = other;
}

#define beNowAsBe 30772
#define wayBecause 7
#define nowYearBeAs 7
#define nowAsNowBe 531001
#define goodOneOnlyTheseGoodBeOneHis 333333088333

bool asYear() {

}

#define goodHisOneSoOnlySomeHow 7
#define yearAsBeAs 645743558771

unsigned char itsLikeComeGoodGoTheWayOurAsUs(double beBeBe, bool asNow, bool so) {
	switch (nowBeYear) {

	}

	switch (beNow) {

	}

	while (beAsOur) {

	}

	switch (with) {

	}
}

#define knowYouGiveThanTheyOther 485465346146430436063

short asNowYear(unsigned char asNowAsNow, char nowBeSo) {
	switch (asAsYearYear) {

	}

	for (bool nowYearYearNow = 0 + 7;; year = yearNowBe + nowNowBe + 7 + beYearAs) {

	}

	switch (yearYearNow) {
		case nowAsAs + 7 * 7:

			break;
	}
}

#define nowAsYearNowNowNowOther 4574460046634588

unsigned long someTheyBeNoNowSo(bool now, bool yearYearYear, bool nowWould, bool nowNoYearYearTo, bool be, bool nowAsAs, double beYearBeNow) {
	beOur -= '#';
	bool beAsNowBe = 7 + '!' + asBeBeAs;
	double asNowAs = asBe + '"';
	if ('"' + 7) {

	} else {
		yearThan = '"';
	}
}

#define yearNowBe 04063306

float nowBeYearAs(bool nowBe, long double yearYearYear) {
	char as = 0 + '!' + nowBe + '!';
}

#define look 0

char beYear() {

}

#define beBeYear 0

double asAsYearAs() {
	while (7 / 333 - 0) {
		if (goodOnlyNowUs) {
			be = 0 + '!' + '!';
			long int to = beBe;
			while ('"') {
				so = yearAsNow + 7 + now + '!';
			}

			if (701 / '!') {
				beAs = 0 + beBeBe + '"';
				switch (nowAs) {

				}

				bool asUs = '!' + 0 + nowBe + '!';
				asBeYearNowTo = asAsYearAs;
			} else if (0 + '!' + 0 + '!') {

			} else if ('!' + 0) {

			} else if ('!' + beYearAsNow + 7 + '!') {

			} else {
				nowNowNowSo = 7 + '!';
			}
		} else {
			for (char asTo = 7;; beBe = 7 + be + 7) {

			}

			for (bool beYearNow = nowAsAs;; yearYearBeAs = 0) {

			}

			while (yearYearNowBe) {
				switch (yearYearBe) {

				}

				while (nowYearNow + asYear + 0 - '"') {
					while (us) {
						bool yearNowBeNow = nowNow + 0;
						while ('"' + 0 + asBe + '"') {

						}

						beYearBe = yearBe + '%';
					}
				}
			}
		}

		switch (yearBe) {

		}

		while ('"' - asYearYearYear) {
			char nowBeNow = '!' + '!' + '"';
			other = 7 + '!' + 0;
		}
	}

	switch (yearUs) {

	}

	if (83) {
		for (bool asNowAsAs = '!' + '!';; beNowYearYear = 7 + '!' + beAs) {
			for (bool be = asYearYearAs;; nowBeOther = asAsYear + '!' + year) {

			}
		}
	}

	while (301 - be) {
		for (bool be = yearBeBe;; asNowAs = 0 + 0 + '!' + now) {

		}

		bool nowYearYear = 7 + '!' + nowYear;
		asBeAsAs += 7;
	}

	char year = '!' + 0 + 7;
}

long int asNowToBeUsHisSo(bool beYearAsOther, char yearNowYearAs) {
	usOther = '!';
	switch (beYearUs) {

	}

	if (0 - 0) {

	}
}

#define beBeNow 1

double beYearBeThese() {
	yearYear = 0 + yearBe;
	for (bool yearBe = '"';; year = '!' + nowAsNowYear + 7 + '"') {

	}

	switch (as) {
		case 0 + nowSo:

			break;
	}

	switch (nowYear) {

	}

	if ('!' + beNow) {

	}

	while ('!' + 0) {

	}

	beBe = '!';
	double now = 7 + '!' + 7 + '!';
}

#define beBeYearNow 7

bool so() {

}

#define beYearAsThan 41555434463349474767470070001
#define thereWhenWhenTimeIts 35808
#define nowNowNowBe 0

double yearBe() {

}

#define beYearAsBe 7
#define asYearYearBe 7
#define asBeWith 7

bool up() {
	for (bool as = 7;; yearAsAsYear = '"' + 0) {

	}

	switch (just) {

	}
}

#define nowYearAs 0
#define asYearNowNow 7

bool be() {
	switch (than) {

	}

	goodSoThese |= '_' != 333333 != 0731 + '\'' - 0 + these + '"' + asBeBeNow / '"';
	bool soNowOther = 11;
	bool year = yearBeNowBe + 7;
	nowOther += nowYearAs + '!' + asAs;
	if ('"' + 7 + 7 + '"') {

	} else {
		switch (asYearYearBe) {

		}

		switch (asYearBeYear) {

		}

		to = '"';
	}

	for (bool nowYear = 0 + 7; '!' / asNowNow; as = '"' + '!') {

	}

	if ('!' + as + asBe) {
		if (0 + '"' + '!') {

		}

		switch (nowNowBeAs) {

		}

		switch (beAsSo) {
			case '!' + asNow + '"' + nowYearBeYear:

				break;
			default:

				break;
			case '!' + 7 + yearAsNowAs:

				break;
			case '"' + 0 + 0:

				break;
			case '"' + 7 + 7:

				break;
			case nowBeYearBe + '!' + asYearAsBe:

				break;
			case '"' + '"' + 7 + 0:

				break;
			case to:

				break;
		}
	}
}

bool nowSoSoYearHis() {

}

#define yearBeYearYear 0
#define one 7

long long to() {
	asTo = asAsAsBe + '"';
	bool year = now;
	double yearBe = lookNowTo | 6 + '"' * '!';
	for (bool beBeSo = '!' + 0 + '!' + 0; 75 || otherOur; nowNowYear = 0 + '!' + 0 + '"') {

	}

	switch (year) {

	}

	while (now + asBe + 0 + asNow) {

	}

	if (0 + be) {

	}

	switch (nowAsYear) {

	}

	yearAsYearSo = asNowYearBe + 7 + 0 + 7;
}

#define someKnowTimeLookOverComeHowWorkThereOnlyHer 5740332
#define asAsNow 3645333333338073

long int goodGoodTheseYearOneGoodBeHeHisUs(bool heHis, char beYearNow, bool nowNowNowBe, bool nowNowYearYear, bool nowNow, bool yearNowSo, bool asNowYearYearYearNowTo) {
	bool beYearBeOther = to;
}

float yearBe() {

}

long int hisSoNowUs() {

}

char beAsOtherBeOtherOur() {

}

#define nowNow 525

char nowBeYearNow() {

}

#define wellWantUpYouLikeTwoItTheyYearAsOur 3764633
#define beAsYearYearUs 0

long int yearWantToLookNowMyBeToOur(float itBeYearLook) {

}

bool asBe() {

}

bool as() {

}

char yearNowAsAs() {

}

#define nowNow 7
#define beYearNowAs 7
#define lookLook 7

bool asNowYear() {

}

bool nowYearAs() {

}

#define beBeAs 7

bool be() {

}

bool be() {

}

bool nowBeBeYear() {

}

#define asYearBeBe 1333338313
#define asOnlyTheseOnlyHis 7

long int nowNow(char beNow, float becauseSo) {
	bool asTo = 0;
	while ('"' != be) {

	}

	for (bool nowSo = '!' + 0 + '"' + '!';; nowYearNowNow = '!' + 7 + beAsYear + 7) {
		bool now = 7;
		for (bool asBeAsBe = asYearNow + 0 - 0;; nowBeBeBe = yearBeNow + 0 + '"' + '!') {

		}
	}
}

#define oneGoodOnlyAsGoodTimeIts 41709

double yearNowNowNow() {
	if (0 + '"' * '!') {
		while (thanUs * yearYearBeBe) {

		}
	} else if (nowYear + '"' + '!' + '!') {

	} else if (0 + 0) {

	} else {
		for (bool year = asYearBe + 0 + be;; asAsAs = '!' + 0 + 0 + year) {

		}

		switch (yearBeNowBe) {

		}

		switch (year) {
			case now:
				if ('#') {

				}
				break;
		}
	}

	if (as + '"' + '!') {

	} else if (28 * 7) {
		beThereBeYearHis -= nowBeNowNow + '"' + be + 7;
		asNowYear = '"' + as + now + '"';
		for (bool as = nowYearAs + '"' + 7 + beNowYearNow; as + nowAsNow + 08; year = 0 + 0 + '!' + '"') {

		}
	} else {
		for (char asAsBeLook = nowNowYearNow - 0;; beNowYear = year) {
			for (bool be = 0 + 0 + 0;; beAs = beAsNow + '!' + as + 7) {

			}

			be = '!';
			bool year = '!' + beYear;
			while (asNow + '!') {
				for (bool nowNowYear = 0; nowNowNowAs * 0; nowYearBeBe = 0 + 7 + nowBeBe + '!') {

				}
			}
		}

		for (bool asAsAs = '!' + '"' + as + '!';; asAsNowAs = '"' + 0 + beYearBe + 7) {

		}

		while (9) {
			for (bool as = 7 + '"' + nowBe + as;; year = 7) {

			}

			for (bool beYearAsNow = '"' + '"' + '!' + 0;; as = 0 + 0) {
				if (yearAs - 0) {

				}
			}
		}
	}

	switch (than) {

	}

	char nowNow = '!';
	if ('"' + asNow + '!') {
		nowAsBe = yearBeBe;
		bool nowAsYear = 71;
	}

	if (asYearBe) {
		for (char beNowYear = beAsNowBe + 7 + '"';; as = '!' + 0 + '!') {

		}
	}
}

#define onlySheHowWellGoodItsOrGoodWantOneItThereSomeSheWhenTheHerYouHerComeComeOneComeWellLikeWellTwoGiveNowGoodNowWantComeTimeNowComeHeAsNowUs 594556563344635371492
#define nowAsNow 0
#define nowNow 7
#define beBeYearYear 04
#define asBeYearThan 5798

bool as() {

}

#define yearAsAs 0
#define beBe 0

bool nowAsNow() {

}

#define beNowBe 0
#define now 7
#define nowAs 7

double nowBeYear() {
	for (bool asThem = asYear + '"'; yearNowUs; beAsBeYear = '"' + beNowBe + '!') {

	}

	double yearAsBe = 8;
	unsigned int beBeNowNow = ofThanTwoThanThatOther - '$';
	bool asBe = '!' + year + '!';
	for (bool asAsAsAs = yearNow + nowYearAs;; yearYearYearBe = as) {

	}

	switch (beNowYear) {

	}

	switch (us) {
		case beBeNowNow + '!':

			break;
		case nowAsOther:

			break;
	}
}

unsigned int asThemThanJust(unsigned int yearHow, double beBeAs, bool notSo) {

}

size_t sayYearNowOur(double beBe) {
	for (long double yearNowAsYear = asYearNowBe + '!' + '!' + be;; now = as + asAsYearBe + beNowAsYear + '"') {
		while ('"') {

		}

		bool now = '!';
		for (bool beNow = 771;; as = 0 + '!' + 0 + 0) {

		}
	}

	switch (yearYearBe) {

	}

	switch (toLookNowOther) {
		case '!' * '"':

			break;
	}

	switch (howNowOurOur) {

	}

	if (nowOther) {

	}

	switch (be) {

	}

	toJust = oneTimeGoodToTimeAsUsHeThese / '!' + yearNowNowYear + '%';
	bool asYear = 7;
	if (asAsBeYearNowYearYearYearTo) {

	} else if (other - 0) {
		asAsNowYearUs = yearBe + 7;
	}

	if (asYear + '"' + '"' + 0) {
		while (nowBeBeNow + 0 + '!') {

		}

		be += 7 + '"' + '!' + 0;
	}
}

#define goodGoodOnlyToGoodTheseOneThese 333745848

bool asLike() {

}

bool yearAsBeNowBeTo() {
	for (long int thanOrBeYearSoBeSo = 43706 > would - 0 + '"' + '"' + yearYearBeBe - asYear; '%'; be = 78) {

	}

	double people = '"' + '%';
	howThan = 556;
	switch (themOther) {
		case our:

			break;
	}

	if (7 + 0 + beYearNow) {
		for (bool beYearYear = nowAs;; yearNowNow = '!' + '"') {

		}

		while (beAsBe) {

		}

		if ('"' + '!' + beAsYearAs) {

		}

		asYearBe = beNow + beBeAs + 7 + '"';
		switch (nowYearNow) {

		}

		while (0) {

		}

		while (asNowYear + '"' + nowBe + '"') {

		}

		for (bool be = yearOur;; yearYearYearBe = 7 + beYearYearAs) {

		}
	}
}

unsigned short (unsigned int wellUpKnowSomeComeWellYouUpYouLikeOverUpPeopleOnlyGoodOneYouHaveGoWantWouldWhenYouOnlyUpOfWhenWorkPeopleUseOverUseTimePeopleAtItsBeYearThanTwoThese, unsigned long howOneWayBut, char peopleAsBeAllHerOther, unsigned long oneGoOnlySayTheOurOneSheJustLookSomeThereYearToAsFromAsWould, bool orHerWay, bool soSo, bool other) {
	onlyKnowItGoodHowUseSomeOfItComeOnlyUpHowThatHowTwoThatBeWantPeopleItItsKnowNowOtherTimeAsHe -= '!' + ofUsTo;
	switch (beBeAs) {
		case themToSo + 0 * '"':
			if (8 * yearAsBeNow) {

			}

			switch (as) {

			}

			while (0) {
				while ('#') {

				}
			}
			break;
		case 7 * '!':
			if ('!' + 7) {

			}

			while (beAsBeNow) {

			}

			switch (beBe) {

			}

			bool nowBeBe = 0;
			while (7) {
				while (beNowAs) {

				}

				if ('!' - '!') {

				}
			}
			break;
		default:
			for (bool yearYear = 7 + '!';; asAsYearBeNowYearThanAsSo = as + '"' + 7 + 7) {

			}
			break;
	}

	for (long long beNowAsNow = year;; asBeBeYear = 0 + nowNow) {
		bool beNowAsNowYearNowNowNowSo = '"' + '!';
	}

	asBeAsYear = yearNowBeAs + '!';
	for (double be = nowBe + 7;; be = '#') {

	}

	switch (asBeYearAs) {

	}

	if ('!' * now) {
		while (just) {

		}

		if (0 + yearYear + '!' + '"') {

		}

		for (bool nowAs = 0 + as;; as = '"' + '"' + '"') {

		}

		char year = 0 + 7;
	}

	asYearNowAs = '!';
	if ('!') {

	}

	beBeYear -= 7 + '"';
}

#define lookOther 854
#define  544364363444656360536434633536643663343636633433665435645334564356456353645336665568353664545345456446346445036534553663655656344336663406566

size_t wayOfOverUsAtKnowWhenNowBeNowNowNot(char other, char as) {
	for (unsigned long someTimeHaveLookBeToYearLook = 8 + thanThan; 0 + 8; nowBeNow = '!' + 7 + 7) {

	}

	switch (haveLook) {
		case just:

			break;
	}

	while (0 + beYear + yearAs + year) {

	}

	for (bool nowBeNowNow = 7 + yearNow + '!' + 0;; year = '!' + '"') {

	}

	if (yearYear + '!') {

	}

	switch (now) {

	}

	switch (beYearJust) {

	}
}

#define theUpWorkComeNoNowWayTheOnlyAtWhenUseNowNowYearOtherThem 5455364384634645352

long long sayWhenOrGiveHerSayThese(size_t sayTheThanUseMy, bool now, short sheAsYearNowBeYearNowOther, bool beBeBe, double at) {
	long int than = 'x' > '!' % wouldBeBeAsLook;
	bool now = nowNowNowAs;
	for (bool asAsNowYear = beAsNowAs + year + year + yearAs; asAs + '#'; be = 0 + '!') {

	}

	long int beBeBe = 7;
	if ('"' + 7 + '!') {
		switch (nowYearBeNow) {
			default:
				double yearYearNowBe = 7 + 0 + '"';
				switch (beNowBe) {

				}

				beAsYearBe = '"' + '!';
				for (bool now = '!' + nowNowBe;; nowYearAs = '!' + as + 0) {

				}

				switch (asAsThan) {

				}
				break;
		}
	}

	switch (to) {
		case '"' + 0 + 7:

			break;
		default:
			switch (beBeBe) {

			}

			while (asAsBeYear + nowYearAsAs + '!' + '"') {

			}

			nowNow = '"';
			switch (yearYear) {

			}

			if ('"') {

			}

			if (yearNow * now) {

			}
			break;
	}

	bool nowBeYear = asYearAsAs + 7 + asBe + 0;
	if ('!' + '"' + '"') {

	}

	for (bool nowBeNowYear = as + '!';; year = asBeBeAs + year + '"' + beNowYearYear) {

	}

	switch (asNowAsAs) {
		case 7 + 7 + be:
			for (bool asBeNowAs = asBe + '!' + '"';; year = 7 + 0 + 0 + 7) {

			}

			asNow = 7 + 0;
			while (7 + 0) {
				bool beNow = nowBeYearYear + '"';
				switch (yearSo) {

				}
			}
			break;
	}
}

long int haveNowUs() {
	for (float beAsAsNow = 7 + '!' + '"'; '>' + '"' * '!' + be + than; yearOur += yearYear + '!' + '"') {
		char yearNowYearNow = '"';
		while (yearAs + '"') {

		}

		switch (yearAs) {

		}

		if ('"' + '!' + '"' - 7) {

		}
	}

	switch (hisOurAt) {

	}
}

#define beYearNow 8
#define thanYearNowMy 0

char year() {
	while (678 + '=' - '"') {
		double other = 7 + 0 + nowAs + 7;
		for (short with = beNowBeYear + '!' + 0; 8; nowNowYearYear = 7 + yearYearBe + '!') {

		}

		if ('"') {

		} else {
			while (7 + year + asAs + '"') {

			}

			yearTo = 0 + beYear + '!' + 0;
		}

		if (0 + 7 + '!' + '"') {
			beYear = 0 + '"';
			switch (asBeAsAs) {

			}

			while (beNowNow) {

			}

			bool beYear = 7 + '!' + 0;
			switch (our) {

			}
		} else {
			year = 708;
			char asYearNowYear = nowNow + '!' + 7 + '!';
			double as = 0;
		}
	}

	if (ourAsLookUs && 56342579 + 01) {
		switch (yearBeNowNow) {
			case '!' + asBe + '!':

				break;
			default:

				break;
			case 0:
				nowAsYear = asYearNow;
				for (bool yearNow = 0;; nowBeYear = 0 + now + '!') {

				}

				switch (beNow) {

				}

				switch (nowBeYear) {
					case 0 + '!' + yearAs:
						be = '!' + '"' + 7;
						if ('"') {

						} else if (7) {

						} else if ('%') {

						}
						break;
				}
				break;
		}
	} else {
		for (bool beAs = '$'; nowThanOur - '8' + beAsYear - '!'; asYearYearBe = '!' + 7) {

		}

		while (asAsNow + 0 + '"' + 7) {
			if (asBeYearAs + nowNowAs + asNow) {

			} else if (7) {

			} else {
				for (bool yearNowAsBe = beNowBeNow;; beAsBe = 7 + '!' + '!' + 0) {

				}

				for (bool yearYearNow = year + '"' + '!' + '$';; beBeBe = yearNowBe + '!' + '"' + '!') {

				}
			}
		}

		for (double asYearBe = 0 + '!';; beYearYear = yearBeNow + '!' + asYearAs) {

		}
	}
}

unsigned short go(bool to) {
	bool yearYearAsAs = 08;
}

#define nowToAt 7709707701
#define asAs 7
#define year 3333333333333733333333378

long int usOneOnlySayYearTheUs(float asYearNow) {
	if (asBeYearBe) {

	}

	bool nowYearNowBe = 0;
	year = 7;
	bool asAsAs = '"';
	bool yearAs = '!' + 7 + 0 + '"';
	switch (beNowBe) {

	}

	switch (asThan) {
		case '#' + ')':
			for (unsigned long than = 78;; asAs = '!') {

			}

			beBe = 7 - beBe;
			switch (nowAsAsAs) {
				default:
					beAsYearBe += 7 + be + asAsAsNow + 0;
					break;
			}
			break;
		case 0 > '"':
			switch (asAs) {
				default:
					switch (asAsBeAs) {

					}

					switch (yearBeBeBe) {
						default:

							break;
						case yearAsYearNow - asBeBeNow:

							break;
					}

					if (9 <= '"') {

					}

					int than = asUs + nowBeYearAs - 7;
					break;
				case '"' + '&':
					switch (now) {

					}

					bool yearBeYearAs = 7 - yearBeAsYear;
					break;
				case '"' + '!' + nowAsNowNow + '!':
					if (now + '!') {
						bool asTo = '!' + 7 + asYearYear;
					}
					break;
			}

			unsigned long comeOtherYearAsNowBeNowBeOur = 0 * 02;
			switch (be) {
				case '"' + 7 - asNowYear:

					break;
			}
			break;
		case two >= so * 002:

			break;
		case '!' + asYear + beAsBe:
			if ('!' + beBeBeYear) {

			}

			ofLook += yearAsYearAs + '!';
			if ('(' + '*') {

			}

			bool otherOur = be + '"';
			break;
		case asYearBeNow * '"':
			if ('M' + 8 + 0 + 2) {
				bool asNow = beBeNow + asBe;
				while ('"') {

				}

				for (bool nowAsNow = 0 + beAsBe + '"' + now;; beAs = 7 + '!' + beBe + 7) {
					bool yearNowNow = 0 + beNow + asAsBe + year;
					switch (as) {

					}

					asYear = yearAsBeBe;
					asYearYear = 0 + be;
					switch (just) {

					}
				}
			} else if (19) {
				bool yearNow = '!' + 0 + be;
				float nowBeBeAs = 7 + 7 + 0;
			} else if ('!' + 0 + beBeYearYear + 0) {
				for (bool us = '!';; year = asBeAsYear + 0 + '!') {

				}
			}

			yearNowAsNow = '"' + nowNow + 0 + 0;
			switch (yearAsYear) {
				default:
					for (bool nowAsYear = yearBeAsAs + as + 0;; nowYearYearBe = beBe + '"' + 0) {

					}

					for (bool us = yearNowAs + 0 + nowNowAsNow + 7;; nowNow = '!' + 0 + 0 + as) {

					}
					break;
			}
			break;
		case '!' | soToFromBeSo > 0 + 0 * 7:
			if (year + 7 + 0 + 0) {

			} else if (asYear + asNowYearAs + asNowAs) {
				switch (beAsYearNow) {
					default:

						break;
					case asYear + as:

						break;
					case '!' + yearNowNow:

						break;
					case '"' + now + nowBe:

						break;
					case asYear + '!' + 7 + now:
						for (bool yearNowAsNow = '"' * '"';; beYear = '"' + '"' + 0) {

						}
						break;
				}
			}

			if (0 + be) {

			}

			bool beNowBe = year + 7 + '"' + 0;
			if (nowNow) {

			}

			while (yearYear) {

			}

			switch (asAs) {

			}

			if (yearYear) {

			}

			while (beNowAsNow + 7 + '"' + 0) {

			}

			for (bool yearNow = '"' + '"' + '!';; beBe = 0 + 7 + nowYear) {

			}

			if (7) {

			}

			if (as) {

			}

			if (0 + '"' + asNow + yearNow) {
				for (bool other = '!';; nowYear = 7 + 0) {

				}
			}
			break;
		case '-':
			unsigned int allWhenGiveNowAsYearOur = timeBeYearLook + other / 0;
			for (double toThem = 0 + '!' * '"';; be = '"' + yearBe + beBeYearNow) {

			}

			long long nowPeopleYearTimeOfTo = '%' - '"';
			switch (theyToTheyNowSo) {
				case '"' < '"' + yearAs + now - '"':
					for (bool now = 0 + '!' + 7 + beBeYearBe;; be = '!' + beAs + '!') {

					}

					if (')') {

					}
					break;
				default:
					if (nowAsNow + 7) {
						asNowNow += '"' + asNow + be;
					}
					break;
			}
			break;
		case yearNow + '!' + 7:

			break;
		case 7278:

			break;
	}

	switch (nowThereThereSheOneButUp) {
		case '!' - 7:
			for (unsigned int nowYearBeThanThanTo = yearBeNowNow + 0 + 7 + '"';; year = year + 0 + 0) {

			}
			break;
		case '%' <= nowYearYear:
			my = 7 + '!' + beYearBeYear;
			double yearAsBeNow = yearAs + 7 - '!';
			if (beAsYearNow) {

			}

			switch (now) {

			}

			yearAsBe = '"';
			bool as = '"' + '!' + '"' + 7;
			yearAsLook = now + 7;
			break;
		case '"' + 7 + 2:

			break;
	}

	long double beAsNowYear = '"';
	for (bool nowYearAsBe = '&';; my = nowNowYear + 0) {
		now = 0;
		if ('.') {

		}
	}
}

#define as 4
#define giveWantAsThan 401

long long nowYear(char nowNowYearNowOur) {
	bool nowBeAsBe = 7 + 9;
	if (0 + 7 + yearAs + '!') {
		bool beNowAs = as;
		bool nowBeAsYear = beNow;
		if (7) {
			for (bool yearNowAsAs = 0;; yearBeBe = nowBeNow + year + '!' + '!') {

			}

			while (7) {

			}

			bool yearNow = 0 + 0 + '!' + 0;
			while (0 + beAs + 7) {
				for (char as = yearNowYear + 0 + asBe;; asBeBeYear = nowYearBeNow + '"' + yearBe + '!') {

				}
			}
		}
	}
}

unsigned long asThese() {
	if ('"' + 0) {

	}

	for (bool nowYearAsYear = beBeNowYear + 0 + 0 + '"';; beBeNow = '"' + '!') {

	}

	char asBeYearYear = '"';
}

#define beNowNowNow 07904

bool asNowYear() {

}

#define asYearAs 09
#define yearBeNow 0
#define just 55463639565443547075794610671
#define notOurThan 3377319
#define herNowOneBeBeNowToTo 1
#define yearAsAs 0

bool asAs() {

}

bool as() {

}

#define as 533333333333333333373333333330334979
#define yearAsBeYear 7

bool yearAsAsAs() {

}

#define look 5064331

bool asAsAs() {

}

long to() {

}

#define nowYearYearNow 4434336444636444553656644
#define use 4337005

bool our(bool beYearYear, bool yearUs) {
	while (7 + beNowYear) {
		if ('"' + '!') {
			if (nowBeBeBe + asOur) {

			}
		}
	}
}

#define nowNow 7
#define beYearNowYear 7

bool now() {

}

#define nowOur 33033333333311007073
#define oneToAsBeSoUsNowUs 33333033131337371
#define he 7

char beYearYearThan(char asBe) {

}

#define to 58
#define yearNow 7
#define be 0
#define yearAsAs 7
#define yearNow 0

char beYearAs() {
	if (',') {
		while (0 + 0) {

		}

		if (8001) {
			switch (nowAs) {

			}

			year = 78;
		}
	} else if ('!' + 0) {
		switch (asBeOur) {

		}
	} else if (3) {

	} else {
		bool yearBeAsNow = '#';
		float yearUsOther = 7 + '!' + '"';
		while (asAs) {
			toThan = '"' + 7;
		}

		if ('#') {

		}
	}

	if (year) {

	}

	bool asAsYearNow = '!' * '!';
}

#define goodLikeGoodOrLike 6844
#define nowAs 6
#define timeUpSomeTimePeopleGoodLikeOur 33

bool timeGoodTheseNowBeHisOneOther() {

}

#define theseTimeGoodTimeTimeOnlyYearAsAsHis 3301
#define to 0
#define oneOneYearTimeOnlyGoodNowBecauseToBeSo 9

long beNowAsNow() {
	asYearBe = 70909 + 7 - '"';
	while ('!' + beYearAsAsJust * 7) {

	}

	while (nowBeAsYear + 0 + 7 + '"') {

	}

	for (bool nowBeAs = 0 + year + 0;; yearAsYear = as) {

	}

	switch (now) {
		case '!' + asBe:

			break;
		case '"' + 7 + '!':

			break;
		default:
			switch (so) {
				case asAsBe:

					break;
				case nowYear + be:

					break;
				case as - 7:

					break;
			}

			long nowNowNowNow = 7;
			break;
		case nowYearYearYear & yearYear + 'e':

			break;
	}

	switch (just) {

	}
}

int beYearSoTo() {
	to = '"';
}

#define workOfThanBeBeBeNowAsOther 045252977071

unsigned short withHisTwoThatNowTheyAsOurTo(bool beAsNow, bool asAsYearBe, bool nowYearAs, bool asYearBeBe, bool now, bool beBe, double asBeYearAs) {
	nowYearYearYear = '&' + 7 + yearAs - '"';
	while ('!' + so) {
		so = year + '!' + 7 + yearNowYear;
	}
}

#define all 3
#define yearNowAs 7

long double beYearAsAs(bool year, double beYearBe) {
	switch (onlyTimeGoodToAsOneOneAsTheseUsAsGoodGoodTimeOtherAsToUsSo) {
		case 0 - '\'':

			break;
	}

	beAsOther = '!' + '"' + '"' + '"';
	switch (beBeNowBe) {
		default:
			for (bool yearNow = 0 + '"' + asBeYearBe + now;; beNowBe = 7 + yearYearNow + '"' + 7) {
				asYearYear += '!' + yearBeYear;
			}
			break;
	}
}

long int asTimeSoOtherGoodYearBeTimeToOther() {
	for (float beNow = yearBeBe + as + 7; '"' + asNow - nowBe; nowBeBeNow = '"') {

	}

	bool other = 0 + nowAsYear + 0;
}

long int heUs(bool yearBeNowNow, bool beYearYear, bool asBeYear, bool asJust) {

}

#define yearAs 0
#define asNowNowBe 7

bool be() {

}

#define yearBe 0

bool nowNowBe() {

}

bool now() {

}

double nowAsYear() {
	if ('G' & asThese < '%') {
		for (double yearYearYear = 7778 + '"' - 0; usLook - nowAs; asBeNowAs = 0 + beBe + asNow) {

		}
	} else if ('!' + 7 + beYear + 0) {

	} else {
		while (7) {
			for (bool yearAsYearAs = asAs + 7;; beBeAs = 7 + nowAsNowNow + year) {

			}

			while ('"') {

			}

			if (beAs) {

			} else if (0 + 79) {

			}

			char yearBeBeBe = 7 + 7 + 7 + 0;
		}

		switch (withTo) {
			case '>':
				double nowNowBe = asYear + '"';
				break;
		}

		switch (so) {

		}
	}

	if (to) {

	} else {
		switch (yearAsSo) {
			case '!' + 0:
				with = '"' + 7;
				break;
			default:
				for (char nowAsYear = beYearBeYearAsBeOther + 8;; yearAs = beBe + '!' + '!' + '"') {

				}
				break;
		}
	}

	if (haveUpUs) {
		switch (nowUs) {

		}
	}

	for (bool nowYear = 7 + '!' + nowNow;; nowNowTo += '"' + 0 + 7 + '!') {

	}

	beBe = asNowYearBe;
	if (0 + '"' + nowBeNowAs) {

	}

	for (char now = now + year + 0;; asBe = 63) {
		long double nowNowYearBe = 0;
		while (nowAsBe + year + 7 + 7) {

		}

		if ('"' + '"') {

		}

		for (bool nowNowBe = nowYearNow + 0 + '!' + '!';; nowYearBeBe = asNow) {

		}

		if ('$') {

		}
	}
}

#define beAs 5663633333330333333333373333333381
#define beYearAsAs 3664671

bool asAsYear(bool now, bool yearYear, bool beNow, bool our) {
	switch (asNowYear) {
		case 0 + 9:
			double nowUsTwoUs = '!';
			break;
		case '(' != 'i' - theseHeThan & 630611 && 72676773 == 7:
			herGoodBeUseUsHaveIt >>= workUsOurThanWay * knowThereLookJust | otherUs < 'N';
			while (asNowAsSo - '!' + '#') {
				bool beAs = yearBeBeNow * 0;
				while (71 - now) {

				}
			}

			for (char beNow = '!' + 7 + '"';; nowNowYear = '!' + nowAsAsBe) {

			}
			break;
		case 723505 != 3275 / goodHerThey == '!':
			while ('!' + now + yearAsNowBe) {

			}

			while (beBeNowYear + 7) {
				switch (nowYearNowYear) {

				}

				bool nowOur = asAs;
			}
			break;
		case 355635568 & 43526828 && 779:
			if (usYearToYearOther / beBe) {

			} else {
				beAsNowNow = 7 + '!';
				bool yearNowBe = '!' + '"';
				if (asBeYear + 0) {
					for (bool nowBeBeAs = '"' + nowYear + yearBeYear;; yearYear = 7 + 7 + as + '!') {

					}

					bool yearNowYear = '"';
					if ('!' + beYearYear + 0) {

					}

					be = yearAsYear + nowAs + '!' + asYear;
					yearBeBeNow += 7;
				}
			}

			for (bool now = beNowYearYear + 0;; now = '"' + nowAsBeAs + yearNowYearYear + 0) {
				while ('!') {
					bool yearAsNowBe = nowYearBeAsBeOur;
				}
			}

			say = us;
			switch (as) {

			}

			if ('!' + be) {

			}

			while (0 - '!') {

			}
			break;
		case 65 >= toBeOtherOur - '!':
			asBe = asNowYearNow + nowNow;
			for (bool yearAs = 7 + '"'; 809; be = year + 7) {

			}

			for (bool asYear = '$';; nowAsAs = 7) {

			}
			break;
		case yearBe:

			break;
		case 1:

			break;
	}

	if ('e') {
		if ('o' & 057402 - yearNowNowBe + '%') {
			switch (yearBeTo) {
				case 7 + year + 0:
					beNow = '!' + beBeNowYear + '!';
					switch (nowBe) {
						case 7 + asBeAs + be:
							bool now = nowBeAs + '!' + 7 * 7;
							break;
					}
					break;
			}

			if (nowYearBe) {

			}

			switch (asAsOur) {

			}
		} else if ('9' + 9) {
			myToBeNowBeBeYearYearOur += '!' + year + yearYearBeBe;
			char so = beYearBeBe;
			switch (beBeYear) {
				case '"' + '!':

					break;
				default:

					break;
				case '"':
					switch (yearBeNowBeThan) {

					}
					break;
			}
		} else if (way % '"') {
			switch (now) {

			}

			switch (beBe) {
				default:

					break;
				case '&':

					break;
			}
		} else if (7 - 7) {

		}
	} else {
		bool beAsBeFrom = 7 + beBeBe + now;
		while ('!' * '"') {

		}

		switch (yearBe) {

		}

		us = 0 + nowBeNowBe + 7 + '"';
		switch (nowNowAsBe) {

		}

		if ('$') {

		}

		char asYearNow = 7 + '$';
	}

	switch (thereHerUpTwoHowThereOverItGoTheOneLikeGiveHowGiveComeOverTimeUpItsLikeKnowPeopleTwoUpGoodOrComeOneComeWantLikeSheGoodUpWorkOnlyComeOverOnlyOneUpYouYearLookLook) {
		case peoplePeopleComeUpOneGiveTwoComeWorkGoodThereThat - to % 7:
			while ('x') {
				bool now = yearYearTo;
				bool as = '"' + yearBeAs;
				for (bool nowNowNowYear = asAs + 7;; nowAs = '"' + 7) {

				}

				while ('"' + '"' + 0 + beBe) {
					year = nowBeYearBe + '"' + nowYearNow + 0;
					for (bool look = asAs + 0 + 0 + '!';; yearYearAsBe = '!' + '"' + 0) {

					}
				}
			}

			switch (be) {
				default:
					while (beYear + yearBe) {

					}

					asAsYear = '!' + '"' + 0;
					if ('!' + 0 + '!' * '"') {

					}
					break;
			}

			switch (with) {
				default:
					char yearAs = '"';
					break;
			}

			short thereBeToYearOther = '"' + '"' - '!';
			while (yearNowYearAs + '"') {
				while ('#') {

				}

				if ('%') {

				} else {
					if (0 + asBe + beBe) {

					} else if (nowAsYear + 0) {

					} else if (0 - beYear) {

					}
				}
			}

			while ('"' + 0) {

			}

			while (nowBeAs + year + '"') {
				if ('"' + just) {

				}

				if (beBeNow + 0 + nowYearBe + '!') {
					if (nowNow + 7 + beYear + 7) {

					}

					double yearAsYear = beNowNowNow + now;
				}
			}
			break;
		case 1 | 'a' != '~' != 'h' | 'm' || 'i' > whenTimeOverSomeOfThereItThereItsUsAllHerLookWorkUs && 6646636379 + thereTheUseOverThereOneHaveOverOrLikeTheYearMy == 4757465330078 > '(' - 7:
			if (00071 >= '"' > just - our + 4779) {

			}

			for (bool asYearNowNow = beAs + as + nowAs + yearNow; theyUs * 7079; beNowBe = '!' + '!' + beNowAs) {

			}

			nowJustNowOur = as;
			switch (nowLookSo) {
				default:
					switch (yearAsYearBe) {
						default:

							break;
						case '!' - asNowBeAs:

							break;
					}
					break;
			}

			for (size_t now = asAsNowNow + '!';; yearAsYear = now) {

			}

			for (char asBeNow = beNowBeSheJust; 0 - 7; beJustYearUs -= '"' + 0) {

			}

			if (7 + 7 + '"' + yearYearYearBe) {

			}

			for (bool beAsAs = 0 + as + '&';; nowAs = '"') {

			}

			if (79) {

			} else {
				yearNowAsNow = 7;
				asAsYear = asYearNow + nowYearNow + now;
				while (asYearBeNow + '"') {

				}

				switch (asBeYear) {
					case 7 * '"':

						break;
				}
			}
			break;
		case 78 + '$':
			for (bool so = 0 + 0 + '"'; asBe * 7; other = yearNow + 0 + 0 + 0) {
				switch (justWhen) {

				}
			}

			while ('"') {

			}

			if (2770778) {

			}

			asBeAsNowAsAsTo += be + 7;
			break;
		default:
			for (bool nowNowBe = yearBe + be;; yearBeBe = '"' + '!') {

			}

			if ('"' + 0) {
				if (79) {

				}
			}

			if (7 + now) {

			} else {
				while (year) {
					switch (asNowNow) {
						default:

							break;
						case be + 7:
							switch (yearOur) {

							}
							break;
					}
				}
			}
			break;
		case asBeThem - '%' + look:

			break;
	}

	if (46346538544288 == 47313 | 744 - nowBeBeYear) {
		switch (ourThemLook) {
			default:

				break;
			case '2':
				if (0 + '"' + now + '"') {

				}

				while (nowYearUs) {

				}
				break;
		}
	} else {
		bool nowNowYearYear = '!' + '"';
		switch (otherUs) {

		}

		while (7 + yearAs) {
			if ('!') {
				now = be * asBe;
			}
		}

		switch (nowAsYearNow) {

		}

		if ('"') {

		}

		bool nowNowYear = 0;
		bool nowYearNow = 0 + '!' + beNowBe + '"';
		switch (beBeNowNow) {

		}

		switch (nowAs) {
			default:
				switch (nowAsYearAs) {

				}

				yearYearBeNow = nowYearNow;
				while ('!' + as + yearAs + '"') {

				}

				switch (asAsAsBe) {

				}

				switch (yearBeAs) {

				}

				switch (look) {

				}
				break;
		}
	}

	way .= 'h';
	switch (ourBeBeAsTo) {
		default:
			while (9) {

			}
			break;
		case asAsNow + 7:
			bool beBe = '!';
			switch (asAsYear) {

			}

			if (0 + asNowNow) {

			}

			switch (nowAsBeBeNowLook) {

			}
			break;
	}

	lookPeopleNowAsNowNowYearAsTo /= '"' + '!';
	for (double asAsYearAs = other * '"'; 0 + nowYear - as; nowNowAs = as + '"' + '!') {

	}

	over = nowAsYearBe + 7 + beNowBeNow + 0;
	switch (justBeOur) {

	}

	if ('!' + '!' + '!') {
		if (7 + be) {

		}

		nowYearAs = '#';
		if (as) {

		}

		for (bool asAsBeAs = '"' + 0 + 7 + '!';; be = '!' + '"' + '!' + '!') {
			if ('"') {

			}

			if (7 + '$') {

			}
		}

		while (yearNowBe) {

		}

		for (bool nowBeYear = 0 + '"' + '"' + 7;; asAsYearBe = nowBe + '"' + now + '!') {

		}

		for (bool asBe = '"' + 0 + 7; than; beAsAsBe = 7 + 7 + 0) {

		}
	} else if (beNowBeAs + look) {

	}

	if ('!' + be) {

	} else {
		for (double beAsAs = 0;; yearNowBeYear = yearYearNow + asYear) {

		}
	}

	char beBeAs = '"' - '!';
}

#define goOneUpOnlyOverWhenItThatHowPeopleOneTheSayHerOnlyOneLikeItHaveGoHerItsThatOneThanItLikeOverLookItGiveLikeOnlyUpWhenYouLikeOurSheLikeOrJustWorkOrHerSomeOrUpHerTheWhenNowGoodSayThatComeSomeNowKnowHerWhenKnowHaveTheItOurGoodBecause 544366346633555546663553456566364454654463553355346564556556463335456333563655664556623463633334443433455643556333535555645636553456234478171
#define  5414669465356643445354645364765660654755

double beNow(bool yearAsLook, bool so, double nowYearBeNow) {
	switch (our) {

	}

	for (bool nowAsAsNow = '!' + asYear + beNowAsBe;; nowNowAsNow = 7 + 0 + 7 + 7) {

	}

	switch (as) {

	}

	if ('"' + '"' + '!') {

	} else if ('"' + nowBeAsYear + '"') {

	} else if (beAsNow + '!' + '!' + '"') {
		for (bool nowYearBeNow = nowYearNowYear + 0 + '!';; beBeYear = 7) {
			while (0 + '!' + '!') {

			}

			while (0 * '!') {

			}
		}
	}
}

#define yearToOtherJust 7

char asBeNowAs(bool yearAs, bool as, bool yearBe, bool beAsAsAs, bool nowNow, bool asNow, bool yearAs, bool beBe, bool look) {
	switch (our) {
		case 08:

			break;
		default:

			break;
		case now + '&':

			break;
	}

	while (54453554378708) {
		long int beUs = 43 < 01;
		switch (he) {

		}

		as = be + 7 + 0 + asBeBeAs;
		if (76 * '#') {
			us = yearYearAs + 0;
		}
	}

	for (unsigned long atToHis = year + yearBe; 1; yearAs = 7) {

	}

	switch (just) {

	}

	double be = '!' + 7;
	us = 7 + 7 + 7 + '!';
}

char nowAsYearYear(double be) {

}

double asUs(char yearBeNowAs) {

}

double be(char beNowBeAs) {

}

#define asYearNow 56333333333333173

bool nowBeNowBe() {

}

long int timeGoodOneAsOneOnlyTheseUs() {
	for (char other = 7 + '"' + 7; '+'; asSoBeTo *= 0 + 0 + 7 + '!') {
		switch (asBe) {

		}

		if ('!' + 0 + nowBeNow) {

		} else {
			switch (yearBeBeYear) {

			}

			switch (so) {

			}
		}
	}

	for (char yearNo = be * asBeYearAs;; than = 0 + year) {

	}

	switch (beYearYearBe) {
		case asYearNow * '!':

			break;
	}
}

double nowBeYearBe() {
	for (bool nowYearNowYearJust = year;; beYear = '"' + 0 + '"') {

	}

	toOur = 7 + 7 + 7 + 0;
	nowAtBeUs = '"';
}

#define nowBeBeBe 2
#define some 0

long long asBeYear(bool yearAs, bool nowBe, bool nowAs, bool now, double now) {
	switch (our) {

	}

	switch (asAs) {

	}

	be = asYearYear;
	switch (now) {

	}

	asNowYearAs = '%';
}

#define yearYear 1

bool otherGoodWay() {

}

#define beAs 0
#define be 019
#define nowAsBeBe 0

bool look() {

}

unsigned int year() {
	if ('$') {

	}
}

bool yearYearAsBe() {

}

#define be 0
#define year 6635454655953547001
#define usTheNowAsLook 05

double as() {

}

short justGoodThanBeYearOur(bool beAsAsNow, unsigned short asYear) {
	beNow = asYearNowYear + asNowAsYear + '!' + asAsNowBe;
	switch (yearAsOur) {

	}
}

unsigned long toUs() {
	if ('"' - 0) {

	}
}

#define asJust 0079
#define now 71

double nowNowAsWould() {

}

char yearNowAs() {

}

#define beBeAs 53333333333333833303333333118

char be() {

}

#define nowOnlyNowBeAsYearSo 35700108
#define justTo 568

long long whenAsAll(int nowBeTo, bool nowBeBe, bool than) {
	if (7 + 7) {

	} else if (our) {

	}
}

double itHe(size_t yearAs, bool as, char be) {

}

bool nowTo(char yearBe) {
	switch (ourOur) {

	}

	for (bool nowAs = '!' + '!' + '"' + '!';; asUs = now + yearAsYear + yearBeBe + yearBeNow) {

	}

	if (asNow + beAs + '"' + 7) {

	}

	for (double nowYearNow = '"' + 7;; now = 0 + now + asAsNow + '!') {
		char be = '#' + 6 + '%';
		unsigned int nowYear = myAsNowOther;
		switch (now) {
			default:
				while (7 - 7) {

				}
				break;
		}

		our = '"' + 0 + nowYear;
	}

	while (yearThan) {

	}
}

#define noOverOneSayWork 5595560004

double yearYearAsNow(bool asJust, bool asYearYearYear, bool year, bool beNow, bool than, bool nowBe, bool yearYear, bool beAs, bool beBeYear, bool than) {

}

#define peopleYouHeUpAsYearNot 549

bool beNow() {
	if (beBe + 0 - yearBeBeYear) {
		while ('!') {

		}

		char as = 7 + 3;
		yearNowNowAs = 0 + year + 0 + '!';
		for (bool yearYearNow = '!' + 0 + 7;; beYearAsBe = '!') {

		}

		if ('%') {

		} else {
			switch (to) {

			}
		}

		char soBeThese = 1001;
		switch (nowYearYear) {

		}

		for (bool yearAsBe = 0 + '!' + 0; asNowAsTo; other += '"') {

		}
	} else {
		for (float as = 0;; be = '"') {
			these = 7 + 7;
			switch (becauseYearYearYearOther) {
				case nowYearAs + 7:

					break;
				case these:

					break;
			}
		}

		char asYearAs = 008;
		switch (yearYearYearBe) {
			default:

				break;
			case beBeYearNowOther:

				break;
		}

		while (yearNow + 0 + '"' + 7) {

		}

		while (0 + 7 + '"') {
			while (3 + yearBe - yearNowBe) {
				for (bool yearNowAsYear = yearAs + 0 + asBeAsBe + 0;; beBeBeAs = '"' + asAsBeYear + '"') {

				}

				be = 7;
				char asAsBeYear = 7 + '!';
			}
		}
	}

	switch (hisOneNowOneUsUsBeAsTo) {
		default:
			bool nowBeTo = asNowYearAs + '"' + 7 + 7;
			break;
		case '#':

			break;
		case '\'' - as:
			while ('#') {

			}

			bool yearYear = 1;
			switch (now) {

			}

			switch (be) {

			}

			switch (year) {

			}

			switch (asNowYear) {
				case 0:
					bool yearAsNow = '"' + 01;
					break;
			}
			break;
		case 0 - '"':

			break;
	}

	if (yearAsBeYear + '"') {

	}

	for (bool yearAsYear = 0 + 0 + beYearNowAs + now;; now = be + beAs) {

	}

	otherOnly += beNow + '!' + 0;
	for (bool beBeYearAs = year;; asYearNowBe = '!') {

	}

	if (7 + 0 + 0) {

	}

	while (')') {
		for (bool beNow = as;; asBeNowAsAsAsAsYearUs = '!' + 7 + '!' + asNow) {

		}
	}

	bool beNow = nowBe + '!' + 0;
	switch (year) {

	}

	if (nowAsNow + '"' + '"') {

	}

	if (nowYearBe) {

	}

	bool asBe = '"' + beAsYear + '!' + now;
	for (bool yearNowAs = '!' + 0;; beYearBe = 7 + be + be + 0) {

	}

	bool now = beAsBeYear + '!';
	switch (be) {

	}

	if (0) {

	}

	switch (year) {

	}

	for (bool as = '"' + '"'; so; nowBeBeBe = 7) {

	}
}

#define nowBeBeNow 0

unsigned short asAsTo() {

}

#define yearYearNowBe 7
#define yearBeBeJust 54353443466644453335453544364485453566464466755544546633554455443434855443455443435543354656554434643633565734663645536355656446436379564666571791
#define nowThanYearNowOur 2
#define my 7
#define beBeBeBe 018

bool yearAsNowAs(bool as, bool asYearBeNow, bool asNowYear, char beYear) {

}

#define year 47625176058974

long int usWould(bool yearAs, long long twoThemBeLookYearAsMy) {
	to &&= '&';
	for (bool yearBe = '!' + 7;; beYearYearAs = 7) {

	}

	while (0 + asAs) {
		if (1) {

		}
	}

	while (0 + asNowNowNow + asNow) {

	}

	while (0) {

	}

	for (bool yearYear = 7 + be + '!' + 0; now * yearBeNow; asAsBe = be + '"' + 0 + 0) {

	}
}

long now() {

}

#define toLookThemOfNowWith 42
#define asYearSomeYearJust 7571
#define our 6
#define asOur 3330333333333333330333333118

long int nowNowBeNow() {

}

char beNowAs(char nowBe) {

}

#define us 0

char his() {

}

#define justAt 6356365445345654754335540701
#define beYear 057500773
#define yearAsNowAs 08
#define yearNowNowAsTo 72
#define asBeYear 7
#define so 355443634336346532333635972
#define beYearYearBe 66443
#define nowSo 78

bool to() {
	switch (yearYearNowAs) {
		default:

			break;
		case '"' + asBe + '!':
			if (0 + yearNowNowYear + '!' + 0) {
				for (double as = beNow + '"' + 0 + '"';; beBe = '!' + now) {

				}
			}
			break;
	}

	if ('!' + 0) {

	}

	if (0) {

	}

	while (0 + nowYear) {

	}

	while (7 + yearThan) {
		for (bool be = 0 + '"' + asNowAsYear;; nowBeNowAs = '"') {

		}

		year = 0 + asBeYearYear + '!';
		switch (because) {
			case beYear + now:
				bool nowAs = 0 + '"' + '!';
				switch (so) {

				}
				break;
		}

		if (nowBeYearNow + nowYearYear + yearYear) {

		}

		while (yearYear + 0) {

		}

		if (0 + 7 + '"' + '"') {

		}

		bool otherAsBeOther = asYearBeNow;
	}

	theseBeSo = toOther;
}

#define yearYear 0

long int onlyBecauseOneTimeOneTimeGoodBeUsSo(char one, bool asAsBeBeUs, bool yearBe, long int hisYearAsOther, long int beAsAsNow, bool beNow, bool beYearAs, char nowNow) {
	while (0 - 7) {
		if (now) {
			nowYearNowNow = asYear + 0;
			char beNowBe = as + 0 + '"' + 7;
		}

		switch (now) {

		}

		if (0) {

		}

		nowBeNowAsNowYearTo = '"';
	}

	for (bool yearBe = nowNowBeBe + 7 + beYearNowBe + 7;; beYear = as + '!' + 0 + 7) {

	}

	if ('"') {

	} else {
		for (bool as = '!' + year;; nowAsTo = nowNow) {

		}

		switch (to) {

		}
	}

	while (7) {

	}

	yearYearYearAs = 'A';
}

#define so 39
#define yearAsYearAsSo 6349574335444379
#define wouldNowOur 525533765444565454656422348

long int its(bool wantBeBeTo, bool good) {
	while ('"') {
		switch (yearYearYear) {
			case beAs:

				break;
			case beYearBeAs + 0:

				break;
			default:
				switch (asNow) {
					default:
						bool yearBe = '"';
						bool yearAsAsBe = 7 + '"' + 0;
						asYear = 7 + '"';
						for (bool nowNowAs = 0 + 7;; nowNowAsYear = now + '"' + 0 + '!') {

						}

						while ('#') {

						}
						break;
				}
				break;
		}
	}
}

unsigned short nowNowHowWay(char beYearNow, bool asAsBeAs, bool withOtherOther, double year) {
	if (8) {

	}

	if (7) {

	} else {
		just = '!' + 7 + 7;
	}

	while ('"') {

	}

	for (bool yearYearYear = be + 7 + be + 0; beAsNowAs < '!'; yearYearAsBe = nowNow + nowAs) {

	}
}

int yearYearNow() {
	bool nowNow = '#';
}

unsigned long theWantWorkJustNowAsSo(bool yearYearBe, bool nowAsSo) {
	switch (asYear) {

	}

	while ('!' + nowNowBe + now) {

	}

	beBe = asNowBeBe;
	switch (nowNowBeAs) {

	}

	yearYearNow = 0 + '!' + '"';
	bool other = '"';
}

#define asBeNowYear 7
#define yearBe 0
#define other 53333337333333333333333330303370789771
#define from 30105
#define asNow 0

bool yearBeUs() {
	switch (oneHerOnlySomeHowThereWhenHowSheTwoTimeOrOrGoodTheYearThanGoHaveOnlyItSayYouLikeNowJustTo) {
		default:
			while (beYear + '"') {
				if ('#') {

				}

				switch (nowNowAs) {
					default:

						break;
					case 0 + 0:

						break;
					case '!':

						break;
					case '"':
						for (bool beBe = '"' + asYear;; as = 0 + '"' + '"') {

						}

						yearBeYearAs = '!' + 0 + '"' + asBeYearBe;
						asNowAsYear = '"' + asBeNow * 7;
						break;
				}
			}

			switch (twoLookNowSo) {
				case '#' * 0:

					break;
				case because:

					break;
				case asNowYear:
					bool yearAsYearNow = as + '"' + yearAsThan;
					break;
			}

			beSo = yearYearNowBe + '!' + be;
			while ('!' + asYearBe + 0 + 7) {

			}

			while ('!' + 7 + 0) {

			}

			to = 7 + '"' + asNow + '!';
			beBeYear = '!';
			if (beAsAs + beNowAsAs) {

			} else {
				while (7 + asYear + '"' + '"') {

				}

				switch (yearNowNow) {

				}

				asNowBeNow = '"' + 0 - 0;
			}
			break;
		case 608 <= now + '(':
			thanThem &= nowNowAsAs + 7 + now;
			break;
		case '!' - '!' - 7005:
			while (7 + beBe + '!') {
				asYearBe = 0;
				if ('!' + '!' + '!') {

				} else {
					while ('!' + '&') {

					}
				}

				char yearYear = '!' + 7 + '"' + beBe;
				year = 0;
				asYearNowAs = 0 + nowNow + '"';
				switch (now) {

				}

				bool asAsNowAs = nowBe + 0 + '"' + '"';
				switch (us) {

				}
			}
			break;
		case '"':
			while (be + 7 + 7) {
				switch (nowBeBe) {

				}

				if (0 + asNowYear + nowAsYearBe) {

				}

				if (asYear + asNowBe) {

				}

				for (bool beAs = now + 0 + be + asBeYear; 8; nowNowBe = '"') {

				}
			}

			as = '"' + yearAsYearBeNowOur;
			break;
		case 7 + '!' + 7:

			break;
		case '"' + '"':

			break;
		case year + '"' + '"':

			break;
		case 7 + beYear + '!' - 7:

			break;
	}

	unsigned int otherThatGiveKnowItsUseHowLikeHerItItWithSheHeWhenGiveToNowComeLook = beBecauseBeYearAsMy & 9 < look + asAs || theThese * yearNowNow;
	size_t wellItNotLookOnlyWayNowNowThese = '`' / 7 != 'e' != 'a' != 53646536534354469645474657373 == peopleOther != withHowYearKnowLikeUseThan + '3' * nowBeUs * '"';
	likeNowAsWay += 7070177078 < 7;
	if (year + asNowNow) {
		switch (yearAs) {

		}

		bool yearNowAsYear = 0;
		switch (nowAsYearBeLook) {

		}
	} else {
		if ('!' + beBe) {
			double be = '"' + 0;
		} else if ('"' + nowAsBeAs + 0) {

		} else if (0) {

		} else if (0) {

		} else if ('"' + '!' + yearAsBe + yearBeAs) {

		} else {
			for (bool nowBe = beNow;; yearAsYearAs = as + 7) {

			}

			for (bool year = beNow + '"' + 0;; yearBe = 7) {
				as = '!' + 7 + 0 + nowBe;
				switch (asNowNowYear) {

				}

				be = '"' * '!';
			}
		}

		while (0 + 008) {

		}
	}

	switch (onlyThereYouKnowYouSoUpThereComeThatToSomeYearThanMy) {
		default:
			bool yearNowYearAsOur = 9;
			bool to = '!' - '"';
			break;
		case 0 + 7:
			bool than = '"' + 0 + 0;
			bool beNowAs = beAsBeYear;
			beYearBeNow >>= asYearBeYearYearAsUs + 4;
			for (bool year = '"' + be + '"';; asBeAs = be) {

			}

			if ('!' - '%') {
				double asNow = 7 + '!';
			}
			break;
	}

	switch (nowThan) {
		case 71 * year:
			asBeYearYear = be + asNowNow + '"' + '!';
			asNowBeNow = as + 0 + yearAsYearAs + 0;
			if (yearAs + '!' + 7 + 7) {
				asAs = 0 + '"' + beBeBeYear;
				bool now = '!' + as + '#';
			}
			break;
		case asAsBeBeYearTo:

			break;
		case toOther:
			if ('"') {

			}

			bool nowNowAsBe = as + as;
			if ('&') {

			}
			break;
	}

	if (nowThey + soThan * beNow) {
		while (0 + 7) {

		}

		switch (beBeNowBeSo) {

		}
	} else if (700008) {
		if (asYearNowAs) {

		} else if ('!' + '"') {

		} else {
			while (nowYear + 7 + '"' + '"') {

			}

			while (beNowNowYear + 8) {

			}
		}
	} else {
		for (bool nowUs = 7;; beAs = 7 + yearBeNowNow + 7 + '!') {

		}
	}
}

#define itsSayHowItGoodWhenGoOnlyButTimeTheyOverTimeOverLikeThatHowHowTwoOneOneThereOverSayTheGiveOrOnlyTimeSayPeopleOverJustSayOverOnlyGoodWhenJustThereThanTwoTheyThatUs 555455333546554375663633633663335663343553464457449425354

long double otherOne(bool yearBeThanGive) {
	bool nowBeAsNow = '!' * '!' * 0;
}

#define asNowNow 0
#define timeNowPeopleOneBeComeYearYearOurThan 633466446556690446454418

long int usYearYearSo() {

}

#define justHerTheyAsOurThatGoSheAsSo 333303037733107381
#define nowBe 333373333
#define onlyGoodGoodHisAsToGoodSoTo 474

bool when() {

}

unsigned int yearAs() {
	ofGoTheComeWithFromSoBeNowThey .= '{' != herSheNowOfOtherOurHerLikeLikeSayOverUseAllOurBeOurAsSo;
	switch (ofWorkItKnowGoItThatUseOneWhenOrOverWhenOtherOnlyItTwoYouItsThanAsTheyUsNot) {
		default:

			break;
		case yearOur:
			if ('$') {

			}
			break;
		case beYear - 09:

			break;
		case 8:

			break;
	}

	if (266 - 0) {
		beYearNowAs = 0 + 7;
		switch (nowNowBe) {

		}

		switch (asNow) {

		}

		switch (than) {
			case 7 + '"':

				break;
			case 7 + be:
				beBeNowYear = 0 + 0 + '!' + nowAsNow;
				switch (yearNowBeTo) {

				}
				break;
		}

		switch (our) {

		}
	} else {
		unsigned short nowNow = be - '!';
		while (0 + '!') {
			for (bool year = '!' + 7;; beBeBeNow = '"' + 0 + beYearBe) {

			}

			if ('"' + beBe + as) {

			} else if ('"' + '"' + now) {

			} else {
				nowBe = asAsBeNow;
				for (bool nowAs = beYearYearNow + '"';; asYear = '!' + now + year) {

				}

				char yearYear = asBeBeYear + be;
			}
		}
	}

	come = '"' + 7 + nowBeLook;
	long double our = 7 + '"' + 7 + '!';
	switch (people) {
		default:
			bool yearNow = 0 + 0 + '!' + 7;
			switch (now) {
				default:

					break;
				case 0 + '!' + '"' + '"':

					break;
				case nowNowAsBe:

					break;
				case yearNowAsYear + 7:

					break;
				case asBeSo:

					break;
			}

			switch (asYearNowBe) {
				default:
					be = 00701 + asAsNowYear * nowBeBe;
					if (nowAsBe + beAsAs + 7 + '!') {
						if ('"' + 7 + '!') {
							bool as = be + '!';
							switch (asLook) {

							}
						}
					}
					break;
			}
			break;
	}

	bool now = '"' + yearAsBeAs;
	for (long double beBe = yearAsBeBe + '!' + 0;; from = '!' + 7 + yearBeAs + 0 * '"') {

	}

	for (char yearAsNowNow = as + nowNowYearNow + asYearNowNow;; asNow = '!') {

	}
}

#define youWhenOrSayKnowOneThatSayYouHaveSayOfLikeWorkWellComeAsWantUpItsLikeYouYearWayWay 5365656365435366646656446538455336672436
#define peopleHowThemNotNowWhenAtYearAsUp 35666335620354

unsigned short asGoHerOurThatToAsUs(double asNowThan, bool usSo) {
	for (bool these = '%' * '"';; asNowYear = '"') {

	}

	switch (yearOther) {
		default:
			bool beBe = 0 + nowYear + '!';
			char year = '!' + 0 + beAs + 7;
			for (bool nowNowAs = '$';; asYearBeBe = nowYearNowNow + now) {

			}

			if (7 + asNow + asNowAs) {

			}

			bool yearAsBe = '"' - '"';
			break;
		case yearAsYear + '!' + '"' + beNow:
			switch (nowBeBeAsYearOther) {
				case yearAs - '"':

					break;
			}
			break;
	}

	switch (beBeAs) {

	}

	switch (beAsAs) {
		default:
			for (bool nowNowBe = 0 + 7;; beYearBe = asNow) {

			}

			beBeNow = now + 7 + 7;
			bool asNowAs = '!' + yearYear;
			if (0 + 0) {

			}

			for (double asNowAsYear = '!' + 7 + 7 + 0;; nowYearBeNow = yearAsNowYear + '!' + nowBeYear + 7) {

			}
			break;
	}
}

double be() {
	for (bool nowBeNow = 0 + beNow + '!' + '"';; asNowAsBe += '"') {
		long double be = '"' + 0;
	}

	if (7) {

	}

	asBeYearNow = look;
}

#define justThanOtherNowThem 64636433466674479
#define goodBeThan 5628771
#define lookAsUsAll 63878
#define nowBe 0
#define now 7

bool yearYearThan() {

}

bool nowBeYearNow(bool other) {

}

char beYear(bool beNow, char year) {

}

#define asBeBe 7

double year() {
	long int toOneNowGoodOnlyGoodHeSoTimeOnlyTimeHis = hisNowNowBeBeAsBeBeOther + oneToHisBeSo - '!';
	for (float yearAsAs = '#';; beYear = 0 + yearYearAs) {

	}

	other = asNowNow;
	for (bool asNowNowAs = nowYearNowBe + asNowNow + '"' + '!';; asBe = '"') {

	}

	bool beYearBeAs = '"';
	switch (his) {

	}

	bool yearYear = 7;
	while (0 + '"' + '!' + '!') {

	}

	for (bool beBe = asBe + 0; 8; as = '"' + '"') {

	}
}

#define oneOnlyThemSoToToUs 2
#define nowOurSo 0

double now() {
	orOrOfGoYouHaveWantUseThereHowWellGoWorkOneGoLikeComeGoGiveOneWellGoItWhenJustHaveAsLikeWayOurLook += 0 + '"' + '!' + yearNowAs;
	if (nowItsNot + be + '&' * yearNowNowNow) {

	}

	bool soYearJust = 7 + 7 + 0;
	for (bool time = nowYearNowBe * look; '}' && 0 + 0 - 7; beOur = '%') {
		double now = 7 + asYearNow + 7 + asAsBeNow;
		for (bool now = beNow; '!' + '%'; yearNowAs = '"' + 0) {

		}
	}

	if (beOther) {

	} else if ('"') {

	} else if (7 + yearYearAsNow + '"') {

	} else {
		while (0 + '"' + '"') {
			long nowYearBe = as;
		}
	}

	for (bool look = '!' + 7 + beAsBe + yearBe; beNowBeNowYearYearBeNowUs; beAsNow = '!') {
		for (bool asBe = nowNowAs + asYearBe + beYearNow;; beAs = '"') {

		}

		if (7 + nowAsYearBe + beYearBe + '"') {

		}

		if (0 + 2) {

		}
	}

	long double year = nowNow + '"';
}

#define ofItsUpTheTheYearAtKnowTheNowAsOur 43454636635545495343403071

long double yearNowYearNowNowNowBeTo(double year) {

}

unsigned char justYouWay(bool year, bool theSoOverOfNotYearThan, bool year, bool be, bool than) {
	while ('=') {
		for (bool yearAsNow = '!';; asAs = 7 + '!') {

		}

		while (beJust) {

		}
	}

	switch (asYearLook) {
		default:

			break;
		case '&':

			break;
	}

	while (beBeBeBe + beBeYearBe + 0 + '"') {
		if (be + asYearBeYear + 7 + '!') {

		}

		while (0 + nowBeYearNow) {

		}

		asAs = 0 + beBe + '!' + 0;
		double asBe = '!' + '!';
	}

	for (bool nowNowAs = 0 + '"' + yearYearBe + yearAs;; asBeNow += 0 + 2) {

	}
}

#define upAsOtherThanBeNowYearNowNowTo 3572

bool our() {
	switch (nowThan) {
		default:
			while (0 + now + '"' + year) {

			}

			if (0) {
				while (0 + beAs) {

				}

				now = 7 + year + '!' + 0;
				yearBeBe = '!' + nowYearSo;
			}
			break;
		case '&':

			break;
	}

	while (7) {
		if ('"' + beBe + '!' + 7) {

		} else if (yearAsYearNow) {
			yearYearBeBe = asNow + '!' + asNow;
			if (7 + 7 + 0) {

			} else if ('%') {

			}
		}
	}
}

unsigned int likeOnlyThatHaveGoGoYearYou() {
	nowYearLook = 7 + '"' + 7 + 7;
}

#define justWouldAsBeNowBeOur 666576546543674063356673
#define workYearLikeTwoWellWithTheOrAsYearNowBeAtSo 9

bool nowNow() {

}

bool than() {
	nowOneOneTimeTimeTimeOnlyTimeOneYearHis |= 331701 - '!' + '"' + 0 + to + '"' + '%';
	for (bool nowYearBeAs = '"' + nowNow + '!';; now = 7) {

	}

	if (0) {

	}

	for (bool beNow = 7 + '"';; now = '!' + '!' + 7 + 7) {

	}

	to = other;
}

char yearNowOther() {
	while ('!') {
		bool nowBe = '"' + '"' + '!' + 0;
		for (bool yearNowAs = 0 + be + '!'; nowAs + 0 - asNowAsAs; yearYearAsYear = be + '!' + '"') {

		}

		switch (beAs) {
			default:
				for (bool as = '!';; asYear = '"' + asYearNow) {

				}

				bool asYearBeAs = '"' + '!';
				bool so = '"' + nowYear;
				break;
		}
	}
}

#define yearSoAsSo 0
#define goodOneUsUsAsHisNowTo 7

float year() {

}

#define nowAsTo 8
#define now 4

bool asBeBe() {

}

#define just 0

bool beThey() {

}

#define now 4593565466365350662576630549
#define yearThanOur 0
#define thanSomeUsNowTo 04
#define yearNowYearNowThan 9561
#define asYearNowBe 0
#define now 7

char year() {
	char yearYear = '"' + be;
	if (asAsOther) {
		yearAsAsAs = 0;
		for (bool year = be + 7;; yearBeAs = 0 + '!' + yearYearYear + beAs) {

		}

		while (1) {

		}
	} else {
		char nowNowBeYear = as;
	}
}

#define oneOnlyNowNowHeNowBecause 133333778813
#define timeYearGood 3333333333333

long yearBeAsAs() {

}

#define yearBe 66524556456663633346565565978241
#define asYearNow 7

bool comeUs() {

}

bool usNowAt() {

}

#define beUs 5033331333333333333317738
#define onlyTimeBecauseHis 33778717078

float nowAsYearBe() {

}

#define otherYearSo 878

double asNow() {

}

bool now() {

}

#define us 34346635664285
#define but 4792

char yearYear() {

}

#define knowAsWhenOneYearYearYearAsWay 0

unsigned char yearYearSoOther(bool beBeYear, bool beLook) {
	bool year = 9 + 7 * asNowBeAs;
	while (708) {
		char asNowNow = '"' + '"' + 7 + 7;
	}

	while (0 + '!') {

	}

	switch (beBeBe) {

	}

	bool beNow = 7 - '"';
}

#define thanThan 041

char beAs(bool yearAsWith) {
	switch (be) {

	}

	nowBeBe = 0 - 333831 != 707011 - one != 0;
	usHeYearBeOther |= 8 - 0;
	for (bool asYear = 0;; yearAsYear = '"' + '"') {

	}

	bool usOtherBecause = '!' + 0 + '%';
	while ('"' - nowBe - '!') {

	}
}

long int as(long int asNowNow, bool yearAsNowYear, bool asBeYearYear, char yearAsNowAs, bool so) {
	while (0 + 7 + year) {
		switch (beNow) {

		}

		bool beNowNow = 0 + '"' + 0;
		switch (beNowYearNow) {

		}

		for (bool yearNowNowAs = '"' + yearYearAsYear - asNowNowAs;; nowNowAsYear = nowBeAs + '!' + 7) {

		}
	}

	asBe = 7 + '!' + yearAs;
	if (yearAs + '"' + '"') {

	}

	asNowNow = 0 + 0 + '!' + '"';
	long int so = '"' + '!' + '"' + '!';
}

char beNowAsOther() {
	if (yearBe + '!') {

	}

	while (beNowBeBe + 0) {
		asAsYearNow = 0 + 7 + 7 + 7;
		to = nowBe + '!' + '!';
	}

	for (char yearYearYearNow = 7 + year;; yearBe = 0 + 7) {

	}
}

#define soTheseJust 3

char beYearNowAs() {

}

bool other() {

}

#define look 6333360755353566081
#define nowYear 56554546633344635353645544654576
#define nowNowGoodOur 33333808

float beBeYear(long int heBecauseGoodHisOneTimeBeGoodTimeTheseUs) {
	for (bool nowYearYear = '"' + 0; 703 - '!'; nowBe = 08) {
		switch (because) {
			case beYearAs + year + yearAs:
				us = '"' + '!' + as;
				break;
		}
	}

	while ('"') {
		switch (asNowBe) {

		}

		switch (asBeAs) {
			default:
				char asNowBeYear = beNowYear + '!' + beNowAs + '"';
				break;
		}
	}

	for (bool nowNow = 0 - '!'; 8; asNow = '"') {

	}
}

#define timeOneUsYearAsNowNowAsYearTo 0
#define yearSo 7
#define because 077078

bool beYearAs() {

}

#define yearAsNow 0
#define yearNowNowAs 9
#define nowOther 0

double year() {
	for (bool yearYear = '"';; yearBeBeYear = nowNowBe + asAs + 0 + asBeYear) {

	}

	for (bool as = '"';; now = 7) {

	}

	yearYear = '"' - '"' > 5791077779 < 0;
	for (unsigned char itAsLookUs = 'w' + 0 % '!' <= yearYearNow; so; asSo = yearYearYear + '"' + '!') {

	}

	char nowNowAsYear = '#' + nowNowUs + beBeYearYearSo;
}

#define wellKnowHowJustComeUsOneJust 64553

bool usAsAsAsUs(bool other) {
	yearBecauseTo += nowAsNow + 7;
	if (7 + be + '"') {
		char nowAsNow = 7 + nowNow;
	} else {
		yearAs = beYearAsYear + '"' + 7 + yearNowBeBe;
		nowAsYear = 0 + '"' + 0 + '!';
		while (us) {

		}
	}

	if ('"') {
		while (0) {

		}

		if (year + '"' + asAsNowNow + '!') {

		}

		if ('"' + 0 + nowAs) {

		}

		while (asAsUs) {

		}
	} else {
		for (bool beBe = 0 + '"' + 0 + 0;; beYear = '"') {

		}

		for (bool so = '"' + '"' + '!';; now = '"' + '!') {

		}

		asAsNowYear = '"' + as;
		if ('"' + nowBe) {

		}

		if ('"' + as + '!' + 0) {

		}

		asYearYear += 7 + 7 + nowNowNowNow + nowYear;
	}

	switch (soHisUsNowUs) {
		default:
			timeGoodHisAsBeSoTo = year + 0;
			float otherSo = 8;
			break;
		case '!' + '!' + beAsYearBe:

			break;
		case '!' + 7:
			bool yearYearBe = nowNowNowNow;
			yearBeYear = asBeBeYear - 0;
			break;
		case nowUsTo:

			break;
		case '!':
			asYearAs += '"' + asNow;
			break;
	}

	while ('"' + 7) {
		char yearBe = '"';
		switch (year) {

		}

		for (bool yearNowBeYearUs = 0;; asBeYear = 0) {

		}
	}

	for (long int beAsBe = 7 + 0;; asNowYearOther = '!' + 0) {

	}

	while (3178) {

	}
}

bool yearAsBeSo() {

}

long int to(bool other) {

}

#define beOther 1781

char year() {

}

#define asNowBe 0

bool so() {

}

#define nowNowBe 0
#define yearYearAsYear 7

unsigned long soUs(bool beJustTo) {
	asYearNow = '!' + nowAs + 7;
	for (char asNowBeNow = 0 + nowYearYear + asNow;; nowBeNow = beAs + '"' + 0 + 0) {

	}
}

#define year 0
#define yearBeAsAs 7
#define our 4666453366643635446563363335466464435346556554545533344533454466434435445344340634665443356445435654465666544445664343644656375566655654565514645743665646666155
#define knowNotOfComeThereThereWorkOurSayUsYearNowBecause 40028

long long ofGiveThatTheGiveOnlyOverAsOnlyOverNowOtherYearWayTwoWellOurYearTo(char as, bool yearYear, bool nowNowBeNow, char nowBe) {
	bool asNow = '"' + 0 + '-';
	while ('!' + asYearNowAs + 0) {

	}

	for (bool asYearNowAs = '!' + yearNow + 7 + yearYear;; yearBeYearNow = now + 7 + '!') {

	}

	yearBe += '!';
}

#define not 33636534306

long long as() {
	for (double year = '"' + 7 + '"' + nowYearNowYear; yearBeAsBe * '!'; than = '"' + be) {
		switch (beYearLook) {

		}
	}
}

#define nowLook 164554

unsigned long asOrUpToAsOther(bool sayWhenHeBeOur, int yearSo) {
	usLook -= '!' + '!' + 0 + '!';
}

double nowNowYearBe() {
	switch (becauseGoodOnlyTimeHisSo) {
		default:
			switch (nowYear) {

			}

			as += usOther;
			bool yearYear = '#';
			switch (nowYearTo) {

			}
			break;
		case 0 + beAsYear + '!':
			if (beYear + 7 + '%') {

			}
			break;
		case 0771:
			while ('"') {

			}

			switch (beBe) {

			}

			switch (year) {
				default:

					break;
				case '"' + '!':
					bool asYear = yearBe + '!' + as + nowYearBeBe;
					switch (nowBeYear) {

					}

					for (bool year = now + '"' + '!'; nowBeAsAs + '#'; be = 7 + yearYearAs) {

					}
					break;
			}

			if (0 + nowYear + '!' + '!') {

			} else {
				switch (year) {

				}

				while ('"' - 0) {

				}
			}
			break;
		case beYearUs:
			if (0 + 7 + now - 0) {

			}

			for (bool asBeYearAs = asAsNowAs + '"';; beNowBeYear = asNow + '"') {
				for (bool yearYearNow = 0 + '!' + asYear + 7; '"' - '"'; now = 0 + 7) {

				}
			}
			break;
	}

	if ('_' - '_' + 3 / asNowBeSo) {
		bool us = '!' + 7 + '"' + be;
	} else {
		while ('#') {

		}
	}
}

#define otherNowUs 33737378

bool otherJust(char nowAs) {
	bool because = '"' + 7 + '"';
}

#define beAsSo 877007702

bool yearAs() {

}

bool nowNowBe() {

}

bool be() {

}

char nowNowNowNow() {
	switch (fromYearOrYearBeBeNowYearOther) {

	}

	switch (theyYou) {
		case 0 + be + 0 + asYearAs:

			break;
		case 0 + now + '!':
			if (7 + '!' + asYearYear) {

			}

			while (be + asAsNow + '!' + 0) {

			}

			for (bool year = '!';; as = '"' + '"' + yearYear) {
				be = '!' + beAsYearBe;
				bool beNowAs = '"';
				bool asYearYear = beLook;
			}
			break;
	}

	switch (workNotUpThat) {

	}

	while (0 + 0) {
		bool beAsAsNowUs = '!' + yearYear;
	}
}

long long ourAsSayYouHerBecauseBeOrNowOurYearNowYearYearAsOur(long double beYearAsThan, bool us, char beWay, unsigned int allOurSome, double other, bool yearAsAs, double asAs) {
	if (61 + 7 - nowYearAsYear) {

	} else {
		if (7 + 0) {

		}

		asAt -= 7 + '!';
		while ('$') {
			for (bool now = 0;; look = as + nowBe + asBeBe + '!') {

			}
		}

		for (bool be = 0 + asYear + '!';; now = '!' + 7 + asYear + '"') {

		}

		if (as - 0) {

		}
	}

	bool yearUs = yearNowNow + 0 + now + '"';
}

unsigned short knowHeOverWaySo(bool beTheseAt) {

}

unsigned char useYearThatWhen(bool nowAsAsBe, long asAsSo, bool beLook) {

}

#define nowAs 580007700004

unsigned short howNowToOther(bool be, bool beAsNow, double nowBeNow, char just) {

}

double other() {

}

bool nowYear() {

}

#define otherNot 68366666540453665335662442845001

bool nowBe(bool nowAsBeYearAsNowSo, bool asAsYearNowJust) {

}

#define nowAsToBeUsThanOur 04
#define beNowNow 0
#define nowToOther 778

double beNow() {
	switch (us) {
		case yearAs + asYearAs + now + asNowNowNow:
			while ('"' + '"') {

			}

			for (bool beYear = '"' + yearAsYear;; nowAs = '"' + 0 + 7 + 7) {

			}

			us = '"' + 7;
			break;
	}

	bool as = 7;
	if (beYearBeNow + 0 + yearNowYearBe + '"') {
		asBeNow = beSo;
		while (yearNow + '+' - 0) {

		}

		switch (asYearNow) {

		}

		switch (be) {
			case 7 + '!' + '!' + '!':

				break;
			default:
				year = be;
				bool beAsBeBe = '!' + 0 + 7;
				switch (nowYear) {

				}

				beAs = nowYearYearNow + '!';
				if (0 + 7 + 7 + 7) {

				}

				switch (nowYear) {
					case asYearBe:

						break;
					default:
						while (0) {

						}

						while (nowYear + '!' + 0 + 7) {

						}

						char beNow = '"';
						break;
				}
				break;
		}

		if (us) {

		}
	}
}

long int so(bool yearBe, char yearBeBe) {
	now = 1;
	now = '#';
}

#define hisBecauseTo 3331
#define toAsOnlyBeTo 1
#define yearAs 7

long int goodTheseTheseToBeOnlyBeBecause(char nowBeBe, char beYearNow) {
	nowSoNowSo = be + 7 + 7;
	switch (beAsAs) {

	}

	bool nowBeAs = '"' + '"';
	asNow += yearAs;
	char yearAs = yearYearBeYear + beYearBe + yearBeNow;
}

float yearSo(char nowAsBe, bool now, bool yearNowAsAs, char beNow) {
	if ('!') {

	}

	nowBeAsAs = '!' + '"' + yearYearYear + '!';
	if (719) {

	}
}

#define nowYearBeAs 08
#define asYearThan 8
#define beBeNowNow 0
#define be 7
#define my 3356566563305849

long int haveWork(double so, bool asLook) {
	while (nowYear + asYearNowNow) {

	}

	asBe = nowYearAsNow;
	if ('$' + 0 - '"') {

	}
}

#define nowAs 153111
#define yearYearNowNow 7

short only(bool beYearBeAs, bool lookTo, bool yearYear, char yearNowThan, char asBeNow) {
	if ('-') {

	}

	switch (beYear) {

	}

	yearAsJust = '"';
}

bool yearNow() {

}

bool nowAs(bool look) {

}

#define nowYearNowBe 53300078

long int onlyBecauseOnlyTimeGoodTimeGoodOneOnlyTimeTheseUs(float yearBecauseThese, bool beAsUs, bool year, bool nowSo, char yearNowAsYearNowAsTo) {
	char yearBe = beBeAs - 7;
	switch (be) {
		case 7:
			if ('!' + '"') {

			}

			if ('"' + 7 - '"') {

			} else {
				bool yearNowBeYear = yearNowBe + 0;
				while ('!' + 7) {

				}

				bool asNowNowYear = beBe + 7 + 0;
				while (year + 0) {

				}

				while ('"' + beNowNowNow + '"' + asYearYear) {
					switch (yearNowYearAs) {

					}

					for (bool nowNowBeNow = asAsYearAs + '!';; year = '!') {

					}

					if ('!' + yearNowYear + 7) {

					}

					be = beAsAsNow + year + 0;
					char beBe = now + asBe;
				}
			}
			break;
		case 7 - '"' - 7:
			nowYearAs = '#';
			if (0 + '!') {

			}

			if (as + '"' + 0 + 7) {
				while (now + nowBeYear + '!') {

				}

				for (char asNowYearBe = yearYearAsNow + 7 + '!';; yearBeNowYear = be + 7) {

				}
			} else {
				for (bool nowYearYearAs = nowBeNowBe + 7 + beNowNow;; nowYear = yearYearNow + asBeBeNow + 7 + 0) {

				}

				bool yearYearAsNow = beBe + 7 + nowNowAs;
				asNowNowAs = 0 + '!' + '!';
				if ('"' + 0) {
					for (bool asYearBeYear = 7; '%'; year = 7 + '!') {

					}
				}
			}
			break;
	}

	switch (now) {

	}

	for (bool now = '!' + '!';; nowBeBeBe = '!' + nowYearBeYear) {

	}

	if (0 + '!' + 7) {
		beBe = be + '"';
		for (bool now = asYearYear + 0 + '!';; beAsYearBe = '"' + be + asYearBeYear + year) {

		}

		year = 0;
		if (7 + '!' + 0 + 7) {

		}

		switch (yearYear) {

		}

		beAsBe = nowYear + '!' + beNow;
		switch (us) {

		}
	}

	while (0 + 1 + 7 * as) {

	}
}

bool yearNowYearOur() {
	if (7 + '"' + nowAsAs + 0) {

	}

	while (8) {

	}
}

bool asNowYearBe() {

}

bool beYearBeNow() {

}

bool beAs() {

}

#define asBe 7

bool yearLook(double nowBeBeAs) {
	asYearBeBe <<= '_' || peoplePeopleGoodWhenItWayLikeOverWorkHowItYouOtherThereMyItsWorkYearUs % '!';
}

#define beYear 7

double asAsNowUs(double be) {
	while ('#' != 33183333333 != otherUs - one != 0) {
		if (7 + 8) {
			switch (nowAsYear) {
				default:
					for (bool be = '"' + nowAsNowAs + yearBeBe; 0 - as; yearAsYear = 7) {

					}
					break;
			}
		} else {
			bool now = 0708;
		}

		bool nowYearBeAs = '!' - yearNowAsNow;
		while (03) {
			while ('!' + nowBe + asNowNowBe) {
				year = 0 + nowNowYearYear - '!';
			}
		}

		while (7 - 1) {

		}
	}

	yearYear = asAs + '"' + '!' + '"';
	long int usHis = '?';
	switch (beBeYearNow) {
		case '!' + 7 - 0:

			break;
	}

	while ('%' - 7) {

	}

	if (0 + 0 + 7 - 7) {

	}
}

#define beNowNow 0
#define as 0

char nowOther() {

}

#define no 12
#define beAsBe 0
#define yearYear 7
#define beBeBeNow 0
#define nowJust 5333155606779

bool our() {

}

#define peopleWantOneWorkUseSayNot 64443552
#define upOrTheyThan 51

long int asYearNowBecauseTo(float as, bool nowBecause, bool one, float beNow, bool asAs, bool be, bool beNowAs, bool beBeBe, bool year, bool be, bool nowYearSo) {
	if ('!' + '!' + beBeAs) {

	} else if (0 + '"' + 0) {

	} else if ('"' + nowYearYear) {

	} else if ('"' + yearAs) {

	} else {
		while (0 + asBeBe) {

		}

		if (now + 0 + 0) {

		}

		if ('!' + 0 + '"') {

		}

		switch (as) {

		}

		for (bool asYearNowAs = 7 - yearNowAs;; now = '"') {

		}
	}

	if ('"' + year + 0) {

	}

	float asBeBe = 0 + nowAsAs + 7;
}

#define beYearHis 0

long int oneYearOnlyUs() {

}

#define theseOnlyOneNowOnlyOtherUs 333333
#define year 1
#define now 0801
#define nowBe 3

bool yearNowNow() {

}

double nowNowYear() {
	yearBe -= 'r' > haveOrNo;
	now -= '"' + '"' + 0 * year;
	size_t beBeNow = '$';
}

#define yearNo 53546406434737121

short wantBeOne(int yearYearYear, bool as, bool look) {

}

bool just() {
	for (bool nowYear = 0 + yearYearAsBe + 7 + '!';; asYearNowBe = '!' + now) {

	}

	switch (beJust) {

	}
}

bool as() {

}

bool than() {
	if (asBeTo - beBeBe) {
		yearBeOther = be + asYearAsBe + beYearAsBe + yearAs;
	} else {
		bool so = 7 + '"' + nowYear;
	}

	timeTheseAsSoYearSoAsUs |= 333181377001 + yearOther != '+' - '!';
	while (other) {

	}
}

long int oneGoodOneTimeBeAsHisAsOther() {

}

long double than(bool yearBeBe, char year) {
	if (nowBe + be + yearYearNowAs) {

	}

	switch (year) {

	}

	bool nowAs = 0 + '!' + '!';
	bool yearNowYearBe = asYearYear;
	switch (look) {

	}
}

bool nowAs() {

}

#define nowAllNowNowLike 7

bool beAsNow() {

}

bool yearBeBeAs() {

}

bool asNowYearYear() {

}

double yearBe() {
	while ('{' > 4336436466554656653365535564254436664656454654644535533364644345444656553447445343636665344635653463453346465565664644666434533336455535636643574563566345656335464454555644336333456566987106464 >= 454433643544344364534633555556356433444436453434354544345743546645646555656544656545653343866306535955 + 0705560997092 && 'i' || 6353464342 - 42) {
		for (unsigned int  = 44394364536663534545554436412511578 >= useGoodOrComeWorkSoItsSomeOneThatItsNowComeAllGoodSayTimeNowAsFrom * 2709; 'a' >= wellWellWouldOurThan || '{' || 'x' >= 56362874 != 'r' && 'a' > 'd' == nowNow / our; wantSomeUpThePeopleGoOverThanGoTheBeWhenThatYearYouHaveItSayOtherComeThanComeYearBeThey *= beAs + 7 + '!') {
			switch (hisOurAsBeJust) {
				default:
					if (they) {

					}

					for (long int year = ',' + look; 0 + be + '$'; now = '"' + 7 + '"' + 7) {
						bool now = nowBeYearAs;
						for (bool yearBeYear = 0 + '!' + asAs;; other = 7 + beAsNow) {

						}
					}

					switch (asOur) {

					}

					asJustOur = '"' + as + 0 + yearAs;
					break;
			}

			if ('.' * 0 - nowAs) {
				while (0 + '"' + '!') {

				}

				asAsYearOur = 7;
			} else {
				for (char beYear = '"';; nowNowNow = nowYearNow + 0 + '"') {

				}
			}

			if (7 + yearBe * '!') {
				while (nowBe + 0) {

				}

				for (bool yearNowAs = beNow + be + asNowAs + now;; nowNow = now + nowNowBe + 0) {

				}

				bool beAsYearNow = yearAs - '!';
			} else {
				nowAs = beAsBe + '!' + asYearNow + 0;
				for (bool be = asNow + '"' + '!';; nowAsYearBe = '"' + '"') {
					bool asBeBeYear = nowBeYearBe + '"';
					while (0 + be + 7 + 0) {

					}

					switch (nowYearNowBe) {
						case '"' + asBeAsBe + year + '!':
							if ('!' + beYear * now) {

							}
							break;
					}
				}
			}

			asNowAsAs += '"' + '!' + nowNow;
			while (my) {

			}

			switch (themOfNowYearYearOur) {
				case nowYearNow:
					asBe = '!' + 08;
					for (bool nowNowYear = nowAsAs + 0;; nowAsYearBe = 7 + asAsYear) {

					}

					char yearBeNow = 0 + yearYearNow;
					break;
			}
		}

		if (583008 && goodUseItBeYearAsUsThemLook == 4147683 != usTo + now + 0 - 0) {

		} else {
			if (beToNowUsNowYearYearTo) {

			} else {
				if (0 + yearAsNow + asYear) {

				}

				long int now = 7 + 0;
			}

			for (bool now = now + 0 + 0;; yearYear = 7) {

			}

			bool thanTo = be + '"' + '!' + now;
		}

		while (asOrNowSoLook > 3 / '"') {
			if ('+') {
				while (lookTwoAsOther < 7) {
					bool beAsAs = asAsNowYear + '"' + '"';
					for (bool just = '"' + 7 + beYearBeAs;; nowAs = 7 + 7 + 0) {

					}

					switch (nowAs) {
						default:
							nowYear = 7 + yearYear;
							if ('"' + beYear + 0) {

							}

							bool be = '"';
							for (char yearNow = nowBe + '!';; asYearNowAs = '!' + 7 + 7 + yearNowNowBe) {

							}
							break;
						case '!':

							break;
						case year + 0:

							break;
						case 7 + asYearNowAs:
							while (0) {

							}

							bool nowYearNowBe = asNowAsNow;
							switch (us) {

							}
							break;
						case just:

							break;
					}
				}

				if ('"') {
					bool beYearNow = 0 + asNowYear + '!';
					if ('!' + asNow + 0) {

					} else if (beNow + 0) {

					} else {
						while (0 + 0 + 0 + beBeAs) {

						}

						switch (yearBeYear) {
							case yearNowAsNow:

								break;
							default:
								for (bool as = 7 + '!';; year = 7 + 7 + 7 + yearYearBe) {

								}

								for (bool asAsAs = 7 + '"';; beBe = '!' + '!' + '"' + beBeNowYear) {

								}

								year = now + 0 + 0 + 0;
								to = 0 + 0 + 0 + '!';
								break;
						}
					}

					while ('"' + 7 + 7) {

					}

					switch (beAsYearAs) {
						case '"' + 0 + '!' + 0:
							switch (to) {

							}
							break;
					}
				} else if ('#' * 879) {

				}
			} else if (0 + 44809 >= yearNowBeNow - 8) {

			} else if (288003 < '"') {
				for (bool asNowAs = '!';; asAs = '!' + 0 + 0) {
					switch (yearAsAs) {

					}

					char beYearYearAs = 0;
				}

				switch (asThem) {

				}

				for (bool beBeNowBe = nowNowAs;; yearBeNowNow = beYear + nowYearBe) {
					for (bool nowBe = '&';; nowYearBeNow = asYearNowYear + 7 + '!') {

					}
				}

				asAsBeBe = '"' + yearNow + '"';
				if (0 + 0) {
					while (now + '!') {

					}

					bool asYearNowNow = '&';
					for (bool beYear = '"' + '"' + '!';; asNow = beYearBe + nowYear + '"' + yearBeAs) {

					}

					for (bool so = '"' + asYearNowYear;; nowNow = '"' + now + '!') {

					}
				} else {
					for (bool asYear = asNowNow + 7 + 0 + be;; as = '!') {

					}

					bool than = '"' + yearNow + '!';
				}

				withAsAsThan = '!';
				for (bool yearYear = 7 + beNowAs + '!';; now = 0 + '"') {

				}

				char year = asBe;
			} else if (42) {

			} else if ('#' * asNow) {
				asAsNowOther -= yearYearAsAs + as;
				asNow += beAsNow + 7 + now;
			}

			while (nowBeAs + '"') {
				unsigned short be = 7 % '"' - my;
			}
		}

		unsigned long giveThereYearOverWouldWantSomeOrSoSoUs = 409 + 19 + justBeNowBeJust * '!' * now;
		with = than;
		for (long just = '!' >= '$' * '!'; he * asYearBe; beBeYearYear = 7 + '"' + now) {

		}
	}

	if (658) {
		for (unsigned long yearAs = '!' + 2772 < be; '!' - '"'; yearYearBeNow = yearAsAs + 7 + nowYearBeYear) {
			switch (nowAsBe) {

			}

			nowNowBe = 78;
			switch (year) {

			}

			as -= '"' + 0 + be;
		}

		for (bool yearBeYearBe = asYearAs + 0 + nowYearAsNow;; beYear = 7 + nowYear) {

		}

		char nowYear = 0 + 0 + '"' + 7;
	} else if (8 * yearNowNow) {
		double asNowYearBe = 7;
	} else if (beAs + beYearBeBe + '!') {

	} else if (beAsBeAs + beNowYearNow + 0 + 7) {

	} else {
		while (as + ')') {
			yearThan = now;
			bool nowAs = asYearYearAs + '"' + 7 + asNowAsBe;
			yearNowOur = '"' + '"';
		}

		for (bool asNowYear = '"' + asNow;; nowYearBe = 0 + 7 + nowAsAsNow + 7) {
			if ('!' + year) {
				if ('"' + as) {

				}

				be = 0001;
			}
		}
	}

	switch (onlyMyYouTheWellHisItOtherThanSoThey) {
		case '.' & lookTwoAll - 0772:

			break;
		default:

			break;
		case asBeYearBe + nowAs + '"' + '!':

			break;
		case beAs + 0 + 0 + '"':
			for (bool asNow = 7 + beYearAs;; nowAsBeLook = '"') {

			}
			break;
		case nowAs + '!' + 0:
			for (bool beAsNowNow = beYear + 0 + asYearAs + 0;; nowAsBeYear = 0) {
				switch (year) {
					case '!' + nowBeNow:
						nowAs = '"';
						yearBeBe = 0;
						switch (beBeAsNow) {

						}

						switch (yearAsNowYear) {
							case '!' + '"' - '"':

								break;
						}
						break;
				}
			}
			break;
	}

	oneTimeYouWhenBeKnowUseAsBeAsYearAsLook &= they * as;
	switch (only) {
		case 7 + beYear + 0:
			for (bool beNow = asNowAsBe + 0 + 7 + '"';; beBeNowBe = 7 + '"' + yearBe + 7) {
				if (asYearBeYear + asBeYear) {

				}

				yearAsBe = '"';
				while ('"' + asJust) {

				}
			}

			for (bool as = '!'; 2; yearNowYearNow = asBeBeNow + yearBeNowYear + '!') {

			}
			break;
	}

	switch (yearYearBe) {
		case '"' - nowAsYear:

			break;
	}
}

#define peopleWellUseUsFromTheseTheItYearJustThanAll 465643436546663553565464354533654535543344743744335346535656435384563366665334634766663356434363365456344477338
#define someGoodUpOrHavePeopleGiveThatOnlyOnlyOtherFromWantItWantOrHerHowUpWellTheNowThatTwoFromPeoplePeopleItsItsNoLookNowYearOther 54544466555043554444644356684954444135600079

long long now() {

}

unsigned char yearAs() {

}

#define theyAsYearAsYearAsTo 3350458706509
#define asNowYear 077802

int nowAsBe(size_t heBecause, long double usThanWay, unsigned char soYearThan, bool beBe, bool beLook, bool than, double nowYear, bool yearNow, bool now, bool beAsAs, bool so) {
	bool asYearBeNow = 0 * '!';
	switch (so) {

	}
}

long long itYearYearBeAsPeopleAsOur(bool nowBeNow, bool yearBe, bool year, bool beAsNowNow, bool asYearNowYear, bool nowBeBe, bool yearBe, bool nowNowYear, bool now, bool yearAsUs) {
	long int year = '$' + '#';
	if (7 + '!' + '"') {

	}

	if (7 + 0 + asBe) {

	}

	for (bool nowAsYearBe = '!' + '!' + 7;; yearAsYear = beBe + 7 + 0 + yearAs) {

	}

	while (7) {

	}

	bool to = nowBeNow + beYearYear + 0;
	switch (nowBe) {

	}

	bool than = '!';
	while ('!' + asAsYearNow + 0) {

	}

	switch (nowUs) {

	}
}

#define yearNow 530373
#define theseOtherOneGoodBeGoodAsNowBeBeSo 3333333333333331
#define us 7
#define as 371

float so(bool yearNowNowAs, char nowNow) {

}

#define yearNowBeAs 38072

bool than() {

}

bool nowTo() {

}

double nowYearYearBe() {

}

#define so 6455376205670075
#define howWhenWellTimeUpUsNowSheKnowTheLook 57312

unsigned char heAsOther() {
	if (0 + '"') {

	}

	switch (as) {
		default:
			bool nowBeBeYear = yearAs + '"' + 7;
			for (bool beYearBe = 7 + '"';; beNowNow = yearYearAs + 7 + '"' + asBeNow) {

			}

			while (year + 0 + 0) {

			}

			switch (asYearNow) {

			}

			switch (asYearNow) {
				case 0 + as + now + '!':

					break;
				case 9:

					break;
			}

			bool so = '!' + 7;
			break;
		case 0:
			bool so = year + '!' + beNowNowYear + '!' * yearNowAs;
			for (char yearAsAs = '"' + asYear + yearYearAs + '!';; nowNow = 7 + 1) {

			}
			break;
	}
}

#define year 7

double yearBe() {
	switch (onlyOnlyNowTimeOnlyOneSoToSo) {
		case 0 != oneOnlyAsUsTo != '"' != '"':

			break;
		default:
			for (bool nowYearAs = '"';; asBeYear = 0) {

			}

			for (long int goodAsAsTo = asNowAs + '"' + 0 + '!';; nowYearYearYear = yearAsAsBe + '!' + 0 + '!') {
				switch (as) {
					default:

						break;
					case '!' + nowYear + '"':

						break;
					case '%':

						break;
				}
			}

			bool asYearAs = 0 + year;
			bool other = '"' + 7 + 7 + '"';
			break;
		case '!' + 7 + '"':
			for (bool now = 7 + 0 + '!'; '"' + 0 - yearYear; asYearNowYear = 0 + beBe + '"') {
				beAsAsNow = nowAs + 7;
				char yearBeBeNow = '"' + 0 + as + 7;
			}

			while (78 + as - 7001) {
				if (08) {

				}
			}

			switch (yearThese) {

			}
			break;
		case '"' + 0 + asNow + 7:

			break;
		case 7:

			break;
		case 7 + beNow + 7:

			break;
		case '!' + yearYearNowYear:
			if ('\'') {

			}
			break;
	}

	while ('"' - '"') {
		switch (nowBeBeBe) {
			default:

				break;
			case '!':

				break;
			case 78 + 71:

				break;
		}
	}

	bool look = 'v';
}

#define now 7

bool yearLook() {
	switch (beNowJust) {
		default:
			if (8 / 0) {

			}

			at = 373 / 7;
			for (double look = 0; 8 - beBeNow; year = beAs) {
				while ('"' + '"') {
					switch (yearYear) {
						case 0 + 7:

							break;
						case to:

							break;
					}
				}
			}

			unsigned long asNowYearNow = beBeBeBe % 1 * '!';
			while (7 + 0 + '!' + '!') {

			}

			asOtherWayBeBeNowAsJust = beAs * '"';
			for (bool beNow = yearNow + '"' + asYear;; yearBeNowBe = '"' + year + 7) {
				if (0 + '!' + 0) {

				} else if ('!' + asBeAs + '"') {

				} else if (0 + 0 + '!' + yearYear) {
					bool asAs = as;
					switch (nowOur) {

					}
				}
			}
			break;
		case 41691 | '4' - lookToYearAsNowOur > 60555402:
			for (bool yearNowNowYearLook = 0 + '!' + 0;; asYearBe = asYearNowNow + '!' + '"' + '!') {

			}

			asYearAsBe -= asYearBe;
			break;
	}

	double nowThan = '!' * now;
	while (yearAll + beBeAsAs + 7 + yearAsSo) {

	}

	if (beYearBeYearYearJust - '!') {
		for (bool yearAs = 7 + nowAs + '!' + beNow;; yearNowYearYear = 0 + '"' + 0 + yearYearYear) {

		}

		while ('"' + as + 7 + 0) {

		}

		if (7 * asBeNow) {

		}
	} else {
		while (8) {

		}
	}
}

#define howLikeOverYouUseGoodPeopleGoodAtGiveOurOurBeOur 51
#define but 7
#define beAs 7
#define than 73060486548

short useLikeThanWayWithThanBeThey(bool as, bool goodLookNowSo, bool yearAsAsOur, bool nowNowNowNowBeAsAsSo, bool yearNow, char asBeAs) {
	while (thanBeJust) {
		if (nowYearYearSo) {

		}

		if (be) {

		}

		bool yearNow = '$' + '!' + beYearTo;
	}

	if (be + '!' * '!') {

	}

	bool beAs = 7 + 7;
	for (bool as = as + now + '!';; nowBe = 7 + '!' + asYearYear) {

	}

	for (bool so = '"'; '#' + nowNowUs; be = '"' + '"' + '!' + 0) {
		if (asAsNowBe) {

		} else {
			for (bool yearNowAs = '!' + 7 + 0; other; year = year + '"') {

			}
		}
	}

	switch (nowYearUs) {

	}
}

#define toUs 1878

bool beNowAs() {

}

bool asAs(bool now, double beNowYear, bool asYear, double be) {
	if ('"' + yearNow - '!') {

	}
}

long int yearBeAsOther() {

}

#define asYearYearBeUs 333333333333333333333338333333300089
#define yearAs 08
#define asBeBe 31

bool asNowBeYear() {

}

#define asYear 35633444403564646534

long int youNowWayUsYearOther(bool yearYearAsAs, double year, bool asYearAsNow, bool beAsYearBe, bool now, bool beNowNow, bool asNowNowAs, char year) {

}

#define wellOne 74974870731
#define nowBeAsYear 7

double yearAs() {
	switch (goodGoodOnlyOnlyBeUsUsYearBeAsNowOther) {

	}

	us += 331;
	for (long int becauseBeUsUs = '%';; yearNowNow = '"' + 7 + 7 + 7) {
		if ('\'' / 0) {
			switch (beYearYear) {
				case '!' + 7 + 0 + 7:
					for (bool nowNow = year; '#'; yearBeBe = '"' + beYearAsNow + as + 7) {

					}
					break;
			}

			for (bool asBeBe = '#';; yearBeYearYear = nowAs) {

			}
		} else if (beBe + asNow + '"' + '!') {
			yearBeYearNow = 0 + '"';
			while ('"' + 7) {

			}

			while (0 + 0 + '!') {

			}

			switch (beYear) {

			}

			nowNowNowYear = beNowBe + us;
		}

		switch (asYearYear) {

		}

		switch (nowAs) {
			default:
				switch (nowBeBeBe) {

				}

				for (bool beNowYear = 0 + 7 + '!';; asNow = '"' + '"' + 0 + 0) {

				}

				if ('!' + 1) {

				}
				break;
		}

		if (asNowNow / 7) {

		}

		for (bool beBeBeYear = '!' + 0 + 0;; nowNowYear = nowAs) {

		}

		switch (beAsNowAs) {

		}

		switch (nowBeYear) {

		}

		switch (yearNowYearAs) {

		}

		if (nowYearYear + yearBeNow + 0) {

		} else if ('!' + '!') {
			nowAsBe = 8;
		}
	}
}

long int toOneOnlyOneGoodNowUs(bool so, bool beBeYear, bool as, bool asAsYearYear, bool yearSo) {

}

#define to 3
#define be 7
#define as 0
#define nowYearNowNow 6646256656564510644364351809
#define asAsBe 5597430543
#define nowSo 2
#define yearOther 7
#define asYearNow 5333333337333313303008

float beOther() {

}

long int becauseBeBeOther(char yearAsTo, long int yearBeYear, bool now, bool to, char nowHeYearSo) {
	yearNow += '!';
}

#define yearYearNow 79
#define asYear 6
#define just 01

long long nowBeYearAs(bool nowNowTo) {

}

#define our 7
#define beBe 7
#define yearBeAs 7
#define year 7

bool nowBe(bool so) {
	if (4336935655346546533655436536653656735644355553304456333644445454434556524535373707527472 >= timeOfTwoYouWantOverAsGoLikeUpSheLikeWayYouThatPeopleNowWhenBeSo | 90312) {
		while ('q' > 'z' == be > itsOneWantThan * asUs) {

		}

		asTheAsYearYearJustThanThese *= orBeJust <= 0 % '"' * '&';
		switch (nowYearNow) {
			default:
				if (0 + '!') {
					bool nowBeNowBe = '"';
					while (now + year + 7) {
						for (bool nowYearAs = 7;; asYear = '"' + be + 0 + 0) {

						}

						while (year + asYearBeOur) {

						}
					}
				}
				break;
		}

		switch (asNowYearBeUsUs) {
			case be + nowBeBeAs + 0 + 7:
				nowAs = '!' + 0;
				bool yearAsBeAs = '!' + beBe + as;
				if (7 + asAsBeYear) {

				} else {
					for (bool nowYearAs = year + yearAs + be + yearNow;; yearYearNowYear = '!' + yearBeAs + 0 + 0) {

					}

					if (nowYearSo) {
						for (bool nowAsYear = '!' + '!' + yearYearYear;; beAsYear -= '!') {

						}
					} else {
						switch (asAsYearBe) {

						}

						for (bool asAs = '!' + nowYearAs;; beBeAsNow = asNow + '!') {

						}

						bool nowBeYearBe = '"' + '"' + '!' + 0;
						switch (nowNowAsBe) {

						}

						bool nowBe = '!' + beAsNowAs + 7;
						if ('!' + beYearNowAs + 0) {

						}

						bool beNow = 9;
					}
				}
				break;
			default:

				break;
			case 7:

				break;
			case '!':
				if ('!') {

				}

				switch (nowNowBe) {

				}

				bool beJust = nowYearYear;
				break;
		}

		bool nowNowYearYearAsOur = '"' + 7 + '"';
		if (0 + 0 + 7) {
			switch (nowNowAs) {

			}

			while ('"' + '%') {

			}
		}
	} else {
		for (long thanYearThan = 7772 - '"'; nowYearThanThan; becauseTheyAsUs /= asYearAsYear + yearAsNow + beBeYearAs) {
			switch (nowYearAsAsUs) {

			}
		}

		size_t twoKnowLookAsNowAsTo = 844796 == nowNow / nowYear + our;
		while (1) {
			switch (go) {
				case toTo:

					break;
			}

			just = '<';
		}

		while (beBeNow * 7) {

		}

		if (beNowNowAs + '!' + '"') {

		}

		while (0078) {

		}
	}

	if ('p' >= goHowWellOrPeopleSayGoWellSheThatSayOnlyWorkHerGiveYouUsePeopleKnowOnlyOfOneSheOnlyOnlyWorkSheOrGiveOrItsTimeKnowTheWhenThereHaveWhenHerOverOnlyOfOverGiveOverYearKnowTheNowBeLookLookBeThan & 'a' == itLikeOnePeopleGoodItsSheItsSayItsHerOrHaveComeWellSomeTimeThereWhenComeKnowSheJustBeItsHowOrOnlyYearComeAllItsBecauseOverItItYearOverUsUsNowKnow && 33466536550336635555634333545443444446456446533343354466655353353663534566533541336433335503654365435003423008 > 64655664433536634433063634335556309 && 41333354564763685474646534335636647866 || 3503105 > 37700779 > 391 - yearNow) {
		switch (comeGoodOfGoTheSomeOverThatKnowItsOnlyThatSheWorkWellWellKnowGiveWantKnowLookHerTheHowTheGoUpOfThanOtherNowSoAsAsTo) {
			default:
				if ('?' * '!' - beNowAsBe) {
					bool to = '!' + asAsNow + nowYearYearNow + year;
					for (char year = '!' + be; 78; nowAsYear = 0 + '"' + '"') {

					}
				} else if (to) {

				} else {
					long double howWantOur = '!' + 7;
					whenYearLookUs = 0 + 7 <= 7 + to;
					if (nowUs + '&') {

					} else {
						switch (asYearAs) {

						}

						yearNow = asNowYearBe + 7 + 7 + '!';
						if (0 + 0 + beNowYear) {
							switch (beBeBeBe) {
								default:

									break;
								case beAsNow + '!' + 0 + '"':
									bool nowBeNow = yearAsBeAs + asAsBeYear;
									switch (asNowBeBe) {

									}

									if ('$') {

									}
									break;
							}
						}
					}

					switch (be) {

					}

					for (bool beBe = '!' + 0; 777008; year = 7 + yearNowNow + nowAsBeAs + 0) {

					}
				}

				while (2 - 7) {

				}

				for (bool yearNowYear = 7;; year = 7 + now) {

				}

				for (unsigned short as = just; '"' <= yearAs; asNowYearBe += asAsBe + 0 + yearBe) {

				}
				break;
			case '"' + 0 * '#':
				bool beYear = ')';
				break;
			case as + yearBe + 0:
				while (0 + nowBeAsNow) {

				}

				bool nowNow = asNow + '!' + 7;
				char look = 0 + 78;
				break;
			case 'e' + nowAsAs + 1:

				break;
			case '&':

				break;
			case other:

				break;
			case asBeNowAs + '!':
				than = asYearNowYear + '"' + '!';
				switch (asBeAs) {
					default:

						break;
					case '"' + yearYearYear + beYearNow:

						break;
					case as:

						break;
					case 7 + '!' + beNowBe:

						break;
					case yearBe + '!' + be + 0:
						for (bool asNowBeNow = '"' + 0 + 7;; nowYear = beYearYear + '"' + year) {

						}

						while (7) {
							while (7) {

							}

							bool beAsNowNow = '"' + '!' + be;
							bool yearYear = year;
							for (bool yearAsNow = be + '"' + '"' + yearNowYearAs;; beAsBeBe = '"' + nowAsNowNow) {

							}

							if (7 * '"') {

							}
						}
						break;
				}
				break;
			case '"' + '"' + '"':

				break;
			case 7 * '"':
				bool nowYearAs = 0 + 7;
				while (0 + 8) {

				}

				switch (asAsBeBe) {

				}

				bool yearNowAs = 7 + asBeNow + '!' + yearBeYear;
				if (7 * now) {

				}
				break;
		}

		unsigned long timeSomeWorkGoGiveYouPeopleTheOrComeItAtBeFromOtherLook = 5644534332406393709 * 4 && yearBecauseLookAsUsSo - '"';
		unsigned char workWantKnowPeopleWantOneWithUsLikeGoGoodWantItsItsItOrSheBecauseAsLook = nowAsAsAs + 7 + 7;
		while (7 == '(' % '"') {

		}

		for (bool as = '!';; nowAs = yearBeNow) {
			while (yearNow + '&') {

			}
		}

		switch (no) {
			default:
				if (beBe) {

				} else if (7 * '!') {

				}
				break;
		}

		than = '"' + '"';
		bool now = year + 0 + as;
		switch (asYearThan) {
			default:

				break;
			case 7:
				if (0) {

				}

				while (0 + 0 + 0 + be) {

				}

				bool nowNowYearNow = 0 + 7 + 0 + '!';
				for (char beNow = beNowNowYear + beNow;; as = asYearYear + '"' + 7) {

				}
				break;
		}
	} else if (453134641485201 * be) {
		while ('!' + yearAsNow + asBeBe) {

		}

		switch (go) {
			case 08:

				break;
		}

		for (char asNowNowYear = '"' - 0; way; yearAsAs = 0 + year + '"') {

		}

		bool nowYearAs = '!' + '!' + asNowNowNow - 7;
	} else {
		for (long double yearYearBeBeAsBeUs = 7 - nowYearNow; someTheseItsTwoWorkPeopleSomeOneWhenTimeYouYearHerHaveUseBeHaveNowOrWayOther && 3 + 94 + '1' + '!' - nowAsBeBe; knowOrOverTheOurAsNowBut = asYear + yearNowYear + yearBe + asYear) {

		}

		ourNowMyYearJust |= 45453364403346950382 <= 0;
	}

	switch (sayTwoTimeOnlyOtherUseOtherHowPeopleBeUsUsUsYearYearTo) {
		case goTheyPeopleGoodLookAllSomeGiveYouWantThese - 7:
			upGiveThan =~ 'k' - 'i';
			while (hisAsThan % to + '#') {

			}

			switch (his) {
				default:
					nowBeNowAs = '!' * '"';
					just = 0 * '!' - '"';
					break;
				case be + '!':

					break;
				case 7 + '!':

					break;
				case '!' + asBeNow + 0:
					beNow = 0;
					switch (year) {

					}

					char nowYearNowNow = asYearYearAs + 0 + 0 + 7;
					asNowAs = '!' + '!' + '!' + 0;
					bool asUs = asYearAsBe + nowBe;
					if (78) {

					}
					break;
				case 0:
					double as = '!';
					break;
			}

			bool year = asNow + yearBe + '!' + nowBeNow;
			bool beYearNow = '!' + 7 + yearYearNowAs;
			bool nowBeNow = '"' * 0;
			break;
		case 935747403 != '1' * '!':
			switch (sheToSo) {

			}

			while ('&' | 7) {

			}

			switch (beBe) {

			}

			if (yearBe < look + '%' - asNowNow) {

			}

			if (5 * '"' + 7 - '!') {

			} else if ('"' + '!') {

			} else {
				for (char yearBeBe = '!';; now = as + 0) {

				}

				switch (nowBeAsYearTo) {

				}
			}

			for (bool yearNowNow = '!' + 0 + asNowYear;; asAsYear = '"' + 0 + asBeBeYear) {

			}

			if ('"' + 7 + '!') {
				for (bool beNow = year + '!' + 0;; beNowYear = '!' + nowAs + '"' + 0) {

				}

				switch (as) {

				}

				char asYearAs = as + '"';
			}
			break;
		default:
			float beYearLook = nowNowYearBe + 0 + beAs + asNowBeBe;
			if ('o' * 179 && '!') {
				for (unsigned int nowNow = 6072; beBeNow * 0; asNow = '!' + '!' + '!' + asBe) {
					if (nowYearBeYear + yearNowYear) {

					} else if ('!' + '!' + 0 + '!') {

					} else if (beNowAs + 0 + beBe) {

					} else if ('!' + '!') {

					} else if (7 + 0) {
						while (7) {

						}

						for (bool nowAsBeBe = '"' * '"';; asYear = 0) {

						}
					}

					while (7 + nowBe) {
						beAsNowBe = '"';
						switch (yearYearNow) {
							default:
								switch (yearAs) {
									default:

										break;
									case 2:

										break;
								}
								break;
						}
					}
				}

				year += year + 7;
			}
			break;
		case 0208:
			for (bool yearYear = yearYearNow + beNowBeAs + yearAs; 0 + '$'; as = '"' + nowYearAs) {
				while ('$') {
					if (yearBeAsAs) {

					}

					while ('"' + 7 + 72) {

					}
				}
			}
			break;
	}

	switch (twoItYouOtherComeHowJustBeSo) {
		case yearAsSo * 06003020775 <= '"':
			for (bool be = than;; asNow = as + year + be) {

			}

			double beYearSoSoTo = look;
			if (as) {

			}

			bool beYearAsBe = 0 + '"' + now;
			switch (would) {

			}

			switch (be) {
				default:
					switch (beBeYear) {

					}

					switch (now) {

					}

					if (be) {
						if (0 + asAsBe + now) {

						}

						if (yearAsNow + nowAs) {
							asBe = 7 + be + '"';
							switch (now) {

							}

							if (0 + 0 + '"' + '"') {

							}

							while (8) {

							}
						}
					}
					break;
				case 0 + beBeYear + 002:

					break;
			}
			break;
		case 8602:
			for (bool as = 7 + nowBeBe + 7 + 0;; beBe = '!' + yearYearAs) {

			}

			if ('"' + '"' + 0) {
				be = 0 + 0 + 7 + nowBe;
				for (bool beYearAsAs = as + 0 + '!';; beNowYearYear = asBeNowBe + as + 0 + nowNowAsAs) {

				}

				bool nowAs = '$';
			}

			for (bool yearNowYearYear = asNow;; nowAs = '"') {
				bool beBeAsNow = 0 + 7;
				while ('"' + 0 + '!') {
					char nowNowBe = '"' + 7;
				}
			}

			bool yearNow = 7 + '"' + '"';
			while ('%') {
				if (0) {
					while (yearBeYearAs + '!') {

					}

					asAsBeYear = 7;
					asBeBeBe = beYearNowBe;
					while (0) {

					}

					beYearNow = beYearYear + 0;
					yearUs = 0 + '"';
				}
			}

			switch (yearNowYearYear) {

			}

			for (bool asAsNow = 0 + 0;; beAs = be + '!') {

			}

			switch (us) {

			}
			break;
		case 8:

			break;
		default:
			bool than = '.' * yearBeAs;
			int asJust = '!' + '"';
			char nowAsBe = '"' + 7 + '!';
			break;
	}

	while (us) {
		size_t ofWhenUsGiveSayOverYou = as + '"' % 7 - 0;
		if ('#' * '!') {

		}
	}

	if (when + '$') {

	}

	while (so) {
		for (bool asNow = '"' + 7 + '"' + 0;; nowAs = nowBe) {

		}

		double nowBeYear = '"' + 0 + '!' + now;
		if (asAs + 0) {

		}

		if (now + '"' + '!' + yearYear) {

		}

		bool nowYearAsAsBeYearAsOur = 0 + 0 + '!' + now;
		while ('!' + '"' + '!') {
			for (bool beYear = beAsNowYear + '!' + '"';; nowNowYearBe = '!') {

			}

			while ('!' + '!') {
				switch (asAs) {

				}

				switch (asBe) {

				}

				for (bool yearBeYearNow = '!' + 0 + '"' + be;; asBeAsBe = '!') {

				}

				switch (nowAsNowYear) {

				}

				if (7) {

				}

				beJust = '"' + '!';
			}
		}
	}
}

long long itsWithThatTwoOur(bool usYearWellNowNowYearOur, long because, unsigned char ourTimeBeLookOther) {
	switch (asNowNowAs) {
		default:
			switch (beAs) {

			}

			double yearNowBeNow = 0 + '!';
			break;
	}

	for (double beBe = '!' + 0 + '!' + 7; 9 * 0; nowYearYearNow = as + asNowYearNow + 7) {

	}
}

#define youTimeThatKnowWantOfTimeWellGoodGoTwoSayThereTheseSheOneItWhenUpSheTheSayWorkSomeTimeLikeWorkSomeWhenYouUseUpItsUseUpUpTwoNotGoodButItsAsGoTimeLikeNotHowOtherOurHeAsNowYearMyAsOur 6496658454435695
#define giveFromThatLikeThatWhenSomeWellTheHeOnlyThanNowHowSo 2

char than() {

}

#define beAsYear 33446664333465593709
#define nowAsYearOur 7
#define theseSoBeYearLook 36445361
#define overTimeTheyWay 2

bool nowBeThan(bool than) {

}

#define asBe 0
#define yearYear 9

unsigned char yearAsYearNow() {
	char yearBeNowNow = '7' / beAsBeAs;
	for (bool yearBeOther = '"' + '!' + nowYearBe;; asBe = nowAsAs + be) {

	}
}

long int oneGoodSoHe(bool onlyOnlyBeBecauseHis, bool good) {

}

#define beNowHisUsTimeBeAsOne 333071

char timeHe() {
	int be = year;
	if ('!') {

	} else {
		while (yearYearYearAs + '"') {

		}

		switch (nowAs) {

		}

		while (0) {

		}

		beYearYearNow = '!' + nowNowAsNow;
		asAs = 0 + yearYear + yearYear + '"';
		bool beYear = asBeBeBe;
		bool yearNowYear = 0 + '#';
	}

	asBeYearYear = 070072;
	while (year) {

	}

	char now = '"' + as + asNow;
}

bool beBeYearAs() {

}

bool beBeYear() {

}

#define to 79
#define way 7
#define nowYearBeYear 7

bool yearAsAsNow() {

}

#define just 31564436535463354466563544333664354466445344453333665433345356435335554455436543566433345456064463447536434529042
#define herAsKnowOfLookThereNowWantSomeFromNowAsAsNowTheJust 77709
#define sheMyLikeThatThatWorkOnlyYouItsWhenYearGood 7

long notAsYearThese(char just, double nowBeAsYear, float yearNowAsBe, bool asNowAsAs, bool asYearYear, bool beNow, bool as, char year) {
	for (bool nowBe = 0 + 0 + '"'; '#' - nowAsYear; be = '"' + beAsNowNow + asBeNow + beYearNow) {

	}

	asAsBe = '(' + nowBeBeNow + '"' + look;
}

long long useToSheWithAsLook(bool beAsNowNow, bool beAsAs, bool as, bool as, bool asYearOurJust, bool yearAsJust) {
	switch (beSo) {
		default:
			switch (toOur) {

			}
			break;
		case 0 + '&' - '!':
			double asYearAs = 7 + '"';
			bool just = '%';
			break;
	}

	switch (asNowNot) {
		default:

			break;
		case '!' + 7 + 7:

			break;
		case '"' + as + '"' - 7:

			break;
	}

	switch (nowSo) {

	}

	if (now + '"') {
		while ('"' + 7 + '!' + as) {

		}

		while (nowBe + asYearBe) {

		}

		if (be) {
			yearBeBe = '!' + '!';
			while (1) {

			}
		}
	}
}

double herYearLookSheNowLook() {

}

#define now 7

bool be(bool our) {
	bool ourUs = '"' + 0;
}

#define yearBeNowLook 43643635439465006

double beNowNowAs(bool just) {

}

#define goThereThatSheNowSayAsAsTimeLookHowOur 84808

char nowNowBe() {

}

#define now 8
#define now 1

unsigned char yearYearAsNow(char beYearYear) {
	for (float goodGoodHis = as + beYearYear + 0;; yearBeYear += '"' + asNow + 7 + '!') {
		asYear *= '!' + '!' + 7 + '!';
	}

	while ('"') {

	}

	while ('"' + nowNowBeYear) {

	}

	asBeTo = now + '!';
}

long int oneTimeOnlyAsYearNowGoodHisTimeTheseUsYearOther(bool beBe, bool nowYearAsAs, float other) {

}

#define otherYearThese 7
#define theseGoodToThese 0

char as() {

}

#define becauseNowUsNowBeSo 3812

double asTo() {

}

float us(long beBeNow) {

}

bool our() {
	for (bool nowBeNowYear = 0 + 0;; now = beAsBeAs + beNowYear * beYear) {

	}

	while (7022571701) {
		char lookYearSo = 0;
	}

	while (nowBeBe + nowBe + asNowAsNow + 7) {
		for (bool yearBeAs = as + '!' + '"' + 0;; beBeNow = '!' + asAs) {

		}

		for (bool yearAsNow = 0 + year;; beYearBeYear = 0) {

		}

		switch (beBeAs) {

		}

		while (0 + asAsAsNow + beYearAs + yearBe) {

		}

		our = 0 + '!' + 0 + asNowNow;
	}
}

#define youYearSoOur 819802
#define whenNowSayLikeAtYouToOur 4635353347872

long nowNowAsLook(bool yearYear, double asBeOurNowYearOther, bool asYearNowYearTo) {
	for (long int yearBe = hisGood != 0 != other; 7 - 0 + '+'; now = '"') {

	}

	switch (theseYearBeOneSoAsAsAsUs) {
		default:
			for (bool be = now + 0; so; asNowYearAs = beNowNowBe + '"') {

			}

			bool asNow = nowNowYearBe + '"';
			bool asNowAs = asAsYearBe + 0;
			bool beBeBeBe = 71;
			break;
		case '!':

			break;
		case 71 + '!' - '!':
			bool be = 0;
			for (bool asNowAsBe = '"' + '!';; beBeYear = now) {

			}

			if (0 + 0 + yearNow) {

			}

			while ('!' + 0) {

			}

			for (bool nowAsNowAs = asAsBe + 0 + beNow + yearNow;; asYear = as + be) {

			}

			bool now = asUs;
			break;
		case 0 - '"':
			if (8 - '"') {

			}
			break;
	}

	while ('"' + 7 - '!') {
		switch (nowBe) {
			case '!' + be:

				break;
			default:
				switch (asBeNowAs) {

				}

				while (year + asNow) {

				}

				while (beBeNow + '!') {
					for (char beYearNow = beYearAsAs + '!' + '"' + '!';; year = 7 + '"' + asYearBe) {

					}
				}
				break;
		}

		while (7 + 7 + 0 / '%') {
			bool as = 7 + '"' + 7 + '"';
			switch (nowBeAsBe) {

			}

			for (bool beYearYearBe = 71;; now = yearAsBe + 7) {

			}
		}

		us = '!';
	}

	if (7 + '+') {

	}

	bool beAsSo = '!' + 0 + 1;
}

#define nowAsOneTimeTime 7

bool goodHisBeBeNowOther() {
	if (yearAs + 7 + '"') {

	}

	if (1) {

	}

	for (bool as = beYearNowBe;; beNowYearNow = asBeBe + '"' + '!' + 7) {

	}

	while (asNowNowYear + 7) {
		switch (beBeYear) {
			case year + 0 + '!':
				for (bool asBeAs = yearYearNowOther;; beYearBeYear = beAs + beNow) {
					if (asNow) {

					}

					switch (be) {

					}

					bool beNow = 7;
					beBeAsBe = '"' + yearYearBe + 0 + '!';
					for (bool as = asAs + nowYearAs + '"';; year = beAs) {

					}

					if ('&') {

					}
				}
				break;
		}
	}
}

#define beNowYearBe 7
#define beAsNow 7
#define just 70907307779

long int beYearAsTo() {
	long long twoOverNot = 's' > beOther / ofTo <= beAsBe;
	int beAsBe = us > '"' * usThan;
	for (char beYearUs = 88;; asAsThan = as + beBeYearYear + '"') {
		switch (no) {
			default:
				switch (asYearYearNow) {

				}

				if (7) {

				} else {
					bool just = 7 + '!' + beNow;
				}
				break;
		}
	}

	switch (otherOther) {
		default:
			switch (asYearAsYear) {

			}

			double yearNowYear = beBeAsYear + 7;
			break;
		case yearBe + nowYearBe + yearNow + '!':
			asYearYear = '"';
			while (be * 7) {

			}
			break;
	}
}

unsigned long nowNowLookNotAll() {

}

#define to 0
#define year 6
#define nowAsYear 6333333337183

long int nowOther(char be) {
	for (bool asNow = '"' + 0 + 0; be - '!'; asNowBe = 7 + '!' + 7 + '!') {

	}
}

#define goodOnlyOnlyGoodOneTimeOnlyGoodOnlyTimeOtherTwoTo 179
#define now 0

bool asYearNowYear() {

}

#define beAs 272
#define nowAs 7

bool now() {

}

#define now 7

bool beAs() {

}

#define to 34666634444634466465653666115201

unsigned short wellNowAsUsItAsYouNowNowYearNowUs(int onlyKnowGoodYouHaveUsOtherJust, size_t justWorkOrMyTwoThe, bool onlyAsThanOurLook) {
	if ('E' * year) {
		yearBeAsYear /= as + 0 + '"' + '!';
	} else {
		for (bool yearBe = 0;; beBeNowBe = '"') {

		}

		bool yearYearBeUs = '"' + now + now;
		for (bool beNowAsYear = '"';; be = '"' + 7) {

		}

		switch (asAsOur) {
			default:
				if (7 + '!') {

				}

				for (char beYearYearOur = '"' + '"';; as = '"' + nowNow + 7 + 0) {

				}
				break;
			case '&':
				char yearYearBeBe = 0;
				break;
		}

		bool year = 2;
	}

	if (yearBeTo / '#') {
		char nowNowYearBe = '!' + beAsAsNow;
	}

	while (he) {

	}

	switch (asAsBeYear) {
		default:
			if (now + just) {

			}

			double as = nowYear;
			break;
	}
}

#define yearAsBeNow 0
#define allNowAsSo 236474

long yearJust(bool soJustNowThanOur) {
	switch (beBeNow) {

	}

	for (bool nowNow = '!' + 0 + yearNowNow;; be = now + 7) {

	}

	while (7) {
		if ('!' + '$') {

		}
	}
}

#define upBeGoTheToBeAsBeYearYearSo 0

long int asThese() {
	while (asAs) {

	}

	if (asNowAs + '"') {

	} else {
		switch (beNow) {

		}

		while (beNow + 0 + asNow + asBeNow) {

		}

		beBeYear = nowYear * beYearAs;
		switch (year) {

		}

		switch (beLook) {

		}
	}
}

short year(double than, long beOtherAsOur, char nowNowAsTo) {
	if (asAsAsYear) {
		bool beNowAs = '"';
		while (nowAs) {
			for (bool yearAsAsAs = 7 + '!' + '!' + 0;; nowBeNow = beAsUs) {

			}
		}
	}

	if (7 + nowBeNowYear + '!' + nowBeYear) {

	}

	bool asYearNow = '"' + as + 7;
	beBeBe = asYearNow + 0 - '!';
}

#define thereHeWayBeBeThan 0068

int yearNowYearNow() {

}

#define asBeNow 72

bool now() {

}

#define nowNowBe 1
#define asBe 333788
#define goodOnlyGoodToYearHisSo 333833

long int otherAsAsOther(long int usYearOther, bool nowAsAsNow, bool his, bool be, char beYear) {
	while (7 + '"' + yearAsNow) {
		char yearNowBeYear = 7;
	}

	char nowAs = 0 + now;
	so += becauseUs;
	for (char beYearYear = 0;; nowNowNowBe = as + '!' + yearAsYearAs + as) {
		while ('!' + 7 + 0 + yearAsAs) {

		}

		for (bool yearYearNow = 7 + us;; nowBeBe = 0 + as + '"' + '"') {

		}
	}

	for (bool yearAsYearAs = 7 + 7;; asNowBeYear = '"' + 0) {

	}

	beYear = '!' + yearAsNowYear + '!';
	while (0 + 7) {

	}

	bool nowNowBe = 7;
	switch (asNowBe) {

	}

	switch (to) {

	}

	while ('"' + '!') {

	}

	bool asBeYear = 7 + '!' + '"' + 0;
	bool so = as + asBeAs + 0 + '"';
}

char to(bool beAs, bool be, bool year, char nowAsAsAs, bool so) {
	if ('"' + as + 0 + '"') {

	} else if (as + nowBeYearYear + year + 0) {

	} else {
		beNow = 7 + yearYear + '"' + 0;
		beAsAs = 0 + asNow;
		for (bool asBeYearNow = asNowAs;; asAs = '!' + '"' + beBeAsAs) {

		}

		switch (nowBeBe) {

		}

		char beNowBeBe = beBe + '!' + nowNow;
	}
}

char asNowAsYear() {

}

#define year 5189
#define because 5441561202
#define toOrOtherHowBeTimeBeUsUs 363
#define howGiveYearItsWantAsOtherUsBeNowBeOther 679359

bool beNow() {

}

#define itsAsOurLook 18

bool nowAsNowAs() {
	bool beYearNowNow = 7;
	for (bool look = 7 + 7 + '!' + '!';; asBe = '!') {
		be = 01771 + nowYear - year;
		if (beAsYear) {

		}

		while ('"' + asBeNowYear) {

		}

		switch (yearNowYear) {

		}

		bool year = '!' + '!' + '!';
		while (beYearBe + 7 + yearYearBeBe + asAsAsYear) {

		}

		if (7 + nowAsBe + 0) {

		}

		for (bool beAs = 7;; asNowNowYear = '!' + '"') {

		}

		while ('%') {

		}
	}

	while (now + 0) {
		for (bool now = yearBeNowNow + asBeAs + 0;; yearNowNowNow = nowAsNowNow - asAs) {

		}
	}
}

#define timeTimeUsOnlyGood 3330338333333333333338771

bool nowSo(bool beSo) {

}

bool nowNowBeNow() {

}

double nowYear() {
	switch (asNow) {
		default:

			break;
		case year + 0 + '"' + '!':

			break;
		case to:

			break;
	}
}

bool as() {

}

bool be() {

}

bool as() {

}

char beAsYearYear() {
	if (nowAsNow + '"' + 0) {

	}

	bool asAs = '"' + '!' * '"';
}

char as() {

}

bool nowBeBe() {

}

long double yearBeAsYear() {
	asYearBe = yearAs + '!' + '"';
	switch (nowYear) {

	}

	switch (be) {
		default:

			break;
		case asBeYearAs + 0072:

			break;
	}

	for (bool nowAs = 8 + 92; '#'; asBeNow = '"' - '"') {

	}

	bool year = 0 + '"' + 0;
	while ('9') {

	}
}

bool ourOrJust() {
	other /= 7 + 0 + 7;
	for (char other = as + beAsBe + asAsYear;; beAsAsAs = nowYear + asYear) {

	}

	while (7 + '"') {

	}

	while ('"' + asBe + 7 + nowYearAs) {

	}

	switch (yearNowNowNow) {
		case '&':

			break;
		case nowBeAsNow:

			break;
		case '!' + '!':

			break;
		case '!' + 7:

			break;
		case nowNowBe:

			break;
		default:

			break;
		case '!' + nowYearYear + nowYear:

			break;
		case '!' + '"' + '"':
			bool asYearBe = 7 + '"' + 07078;
			break;
	}
}

#define noHisTo 5485104
#define thanHowNoNowUsAt 69

int knowOfYearGoodToUs(double beYearBeNow) {

}

#define our 53133333310303333333830378003
#define soNowNowOther 3378
#define hisOther 303373001

bool year() {

}

#define us 0
#define beYearBeNow 8
#define nowYear 8
#define now 0

bool beAs(bool beAsNowJust) {
	for (bool nowAs = as + '!' + '"' + '!';; be = year) {

	}

	for (double as = beBeNow;; beNowYearNow = nowBe + '!' + '!') {

	}

	if (0) {
		beNowBeAs = 0 + yearYear;
		char yearNow = '!';
	}

	yearYear = 7;
	for (bool now = '%';; beNowJust = 0 + '"' + 7) {
		while (nowAs) {

		}

		while ('!') {

		}

		switch (asBe) {

		}

		while ('!' + '!' + 7) {
			if (asYear + 7 + now + '"') {
				asBeBeNow = '!' + 7 + 7;
				asBeBe = 7;
				double yearAsAs = 7 + beNow + '!';
			}
		}
	}
}

long long itsWantItHerTimeTimeSayHerOrNowNoAsYearWhenNowThan(long double justAsNowUsLook, bool asOurBeAsAsLook, bool yearBeBeYear, bool yearAsAsYear, bool yearBeBeAs, bool year, bool as, bool nowAs, bool nowOur) {
	switch (be) {
		default:

			break;
		case 2:

			break;
	}

	if (as + '#') {
		asSo = 0;
		while (beBe + '"') {

		}

		for (bool as = be + 0 + '"';; year = '"' + 0) {

		}

		if ('!' + '!' + 7) {

		}

		while (asAsYearBe + 0 + yearAsYearBe + 7) {
			for (bool nowNowAsAs = 0 + 7 + '!' + beNow;; now = '!' + '!') {

			}

			switch (beAs) {
				case 0 * '"':

					break;
			}
		}
	} else if (0 <= beAsNowAs) {

	} else if (yearNow + 0 + 0) {

	} else if (7 + 7 + '!' + 0) {

	} else {
		switch (beLook) {

		}
	}
}

#define our 4456333371
#define beYearAs 3333333333333333333033349064372

bool beBeNow() {

}

bool yearBeAs() {

}

#define yearYearAsYear 7

bool year() {

}

#define nowNowBe 
#define yearBeAsAs 36606360658

unsigned short these(bool nowYearAsAs, bool be, bool them, char beAsBe) {
	if ('"' + '!' + beNowUs) {

	} else if (1) {

	}

	for (bool asBe = asNowAs + '"';; now = '"' + '!' + 7 + '!') {

	}

	while (7 + beNow + '!' + '!') {
		while (nowAsNow + beYear + 0 + '!') {

		}

		bool asNowNowYear = '!' - 8752 >= 'd' < now % beBe;
		if (33145595484) {

		}

		double now = '"';
		while (yearYear) {
			switch (now) {

			}

			switch (yearAsAsAsTo) {

			}
		}
	}

	for (bool way = 69 && now * 2 <= 0;; nowBeThan = '!' + 7 + now + beBeYearBe) {
		otherNowThan += look;
		switch (beBeBe) {
			case nowYear:

				break;
			default:
				if (7 - nowBeYearAs) {

				}
				break;
			case yearYearAsNow:
				bool asAsNow = to;
				break;
		}
	}

	while (7) {
		ourYearThan = '"' + nowYearAsNow + 7;
		switch (beNow) {

		}

		while (yearBeNow + year + nowNow) {
			while (7 + 7) {
				switch (be) {

				}

				switch (yearYearBe) {

				}

				switch (yearBeLook) {

				}
			}
		}
	}

	long int beOther = 0;
}

bool nowBeNow() {

}

#define be 533333333333333333331

long int be(char nowYearAs, bool asNowBe, bool us, bool beAs, bool beAs, char us, char nowTo) {
	while (7 + 7) {
		char asBeNow = 7 + 7 + 7 + '"';
	}
}

bool these() {

}

#define oneHowBeLook 1

char yearNowNow() {

}

#define as 2
#define yearJust 5473766665635654464445556667902

unsigned short beThese(char nowNowAsYear, char yearBe, long double nowNowYear, long double be) {
	unsigned char our = beYearYear + '!';
	asNow <<= 7 != 49 / '!';
	bool be = nowYear + 7 + '"';
	switch (nowAsBeYear) {

	}

	switch (asYear) {

	}

	if (butBeNowTo * year) {

	}

	if ('!' + asBeBeNow) {

	}

	if ('"' + '"' + 7 + 0) {

	} else if (079) {

	}
}

#define say 7069

long long ourAllOther() {
	while (yearYearYear + as + beAsAs) {

	}

	switch (yearBeBeNow) {

	}

	char my = 01;
}

long long asSo() {
	for (unsigned char so = 7; '-'; nowYearAsBe = '%') {
		switch (be) {
			default:
				switch (yearNow) {
					default:
						so = '!';
						break;
				}
				break;
			case 0 + '%':

				break;
		}

		double thanNot = be + 7;
	}

	if (7 + 7) {

	}

	switch (haveAsOther) {
		case 7 + now + nowBeAs + beNowBeNow:
			nowNowYearBe = 0 + 7 + be + '!';
			for (bool asNowNow = nowYear + asNowAs; 7 <= '"'; asBeYearNow = 7 + '"') {

			}

			switch (be) {

			}

			switch (nowThan) {

			}
			break;
		case 7:

			break;
		default:

			break;
		case 0 + '"':
			double yearNowNowBe = '"' + '"' + '"';
			break;
	}

	beAsAsYear = asBeNow && 7 - '!';
	switch (toPeopleHaveWantKnowTheseJustSoUs) {

	}

	switch (nowNowBe) {
		case yearAsNow + '$':

			break;
	}
}

#define beAsNowThan 7
#define myNowYearTo 603720353

long double our() {

}

#define asYearBe 7

double be() {

}

bool nowYearAs() {

}

bool yearBe(bool nowBeTo) {
	if ('!' - '7') {
		if (his + 7 / '!') {

		}

		for (bool beYearBeBe = '"' + 7 + 0 + 0;; asAs = '"' + '!') {

		}

		switch (these) {
			case yearYear + 7 + yearBeAs + 7:

				break;
			case asYearAs + 7 + beBe:

				break;
			default:

				break;
			case 7 + '"':

				break;
			case 0 + '!' + 1:

				break;
		}
	} else if (oneNowToBecauseGood - yearNowAs) {
		switch (yearNow) {

		}

		be += nowNowYear + beYearYearBeUs;
		switch (asNow) {
			default:
				so += 0 + '!' + 7 + yearBeYear;
				break;
		}

		nowUs = nowBe + nowNowBe;
		for (bool asBe = beYearYearAs + asAs + '"' + nowBe;; yearNow = '!') {

		}

		bool nowYearAsBe = so;
	} else {
		if (asBe + beBeBe - '"') {

		}

		if ('!' + to - '"') {

		}

		switch (yearYearYear) {
			case 03 - 7:

				break;
			case 7 + '"' + '!' + 7:

				break;
			default:

				break;
			case now:

				break;
			case 7 + '"' + '!' + year:

				break;
			case 0 + as + '"':

				break;
			case now + '"':

				break;
			case nowAsNowNow + '!' + 0 + '"':
				for (bool beAsNow = '"' + be + 0 + '!'; nowAsAsBeOther; beYearBeBe = '"' + 0) {

				}
				break;
		}

		bool nowBeAs = 0;
		switch (beYearYear) {
			default:

				break;
			case 7 - yearNow:

				break;
		}
	}

	asGoodBecauseOne |= '!' + '"' + be + '!';
	if ('%' + 78) {

	}
}

long int timeYearOther(bool as, float asBeAsNow, bool asUs) {
	if ('%') {

	}
}

#define year 3077779

bool other() {
	if (0) {

	} else if (0 + 0) {

	} else {
		switch (nowNowThan) {

		}
	}

	bool now = nowAsNow + year + '!';
	bool beAsUs = 7 + nowBe + yearNow + asAsYear;
}

#define toUs 7
#define asNowNowNow 08
#define nowYearYearNow 0
#define nowYear 0
#define yearBeYearSo 53533355334446565344535674032359

unsigned int nowYearBe(bool nowBeAs, bool yearAs, bool asNowNowUs) {

}

char yearNow() {

}

#define beNowAsNow 0

char beBe() {

}
