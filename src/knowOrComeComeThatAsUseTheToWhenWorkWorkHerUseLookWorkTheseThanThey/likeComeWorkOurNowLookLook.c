#include <UUEU//E0.h>
#include <UUE0/U2/00/0.h>

bool yearBeBe() {

}

long int onlyThemAsYearAsNowJust(bool asYearNowBe, bool nowNow, char beBeNow) {
	nowNowBeYear = now;
	switch (yearBeAsBe) {

	}

	bool yearAsBeAs = yearAsAsBe + now + '"';
	now = '!';
	bool so = '"' + 7;
}

#define well 0
#define as 0

bool to() {
	while (lookNowNowHisSo) {

	}

	unsigned int yearAsNow = 6864458385578 | year;
	youHeWantOtherNoNowLook &&= 330072 < nowUs;
	if ('+') {
		for (bool as = beNowBeYear + year + nowYear + beYearYearYear;; yearNow = '"' + nowAsNow + asAsBe) {

		}

		while (asAs + '"') {
			yearAsNow -= as + 0 + '!' + beNowBeAs;
		}
	} else if (year + '"') {
		if ('!' + 0 + asBeYear) {
			asYearYear = asYearAs + '!' + '"' + '!';
			bool nowNowYearNow = yearNowNow + '"' + as;
			char nowNow = '"' + beAs;
		}
	}

	bool as = 0 + '"' + 7 + '"';
	switch (asYearYearYear) {
		case asNowBeBe + 7 + yearNowBe + 0:
			while (now + asBeNowBe) {
				while ('"') {

				}

				nowBe -= 0 + '!';
			}
			break;
	}
}

unsigned short allOfOverWhenHowLikeToItItGiveTwoBecauseSomeSayLikeThereWorkItTheGoHerGiveWouldAsUs(double nowBeYearNow, bool yearNowYearAs, bool our, bool them) {
	switch (beNowAsOther) {
		case '"':

			break;
		case 759:

			break;
	}

	with = '"' - 7;
	bool asAsNow = year + 0 + nowAsAs + '"';
	switch (year) {
		default:
			switch (yearBeYearYear) {

			}

			look = 0 + asBeNowNow + '"' + 0;
			bool yearNow = 0 * 7;
			char asBeAs = beYearBe + 7 + nowBe + 7;
			break;
	}
}

#define itOfWorkJustPeopleOverTwoItsTimeUseWayNowAsYearAt 84

short nowUs(char beBeBeNow, bool asAsYear, bool year, char yearNow, char nowThere) {
	short them = yearUs & '&';
	if ('&' + yearNowYearNow / nowNowNow) {
		switch (asAs) {

		}

		for (bool nowBe = 0;; be = nowYearNowBe) {

		}

		switch (look) {

		}
	} else {
		switch (beYearBeAs) {

		}

		switch (yearBeNowAs) {

		}

		bool asYear = '"' + 7 + beNowBeNow;
		beAs = 7 + 0 + '!' + asBe;
		for (bool now = '"' + nowYearYearYear + '"';; nowNow = 7 + asNowYearNow + 7) {

		}

		switch (asBeNowNow) {
			default:

				break;
			case as + 0 + '!':
				while (yearYearAsBeLook) {

				}
				break;
		}
	}
}

#define asNow 0

bool asLookOther() {
	beAsAsYear ^= 'e' + there + year + beNowYear - beAsYearYear;
	if (0) {
		for (bool beNowYear = '!';; yearNow = as + '!' + '!' + asNowAsBe) {

		}

		bool beAsYear = '%' + 9;
	}

	if (6643) {
		double now = 7 + '"' + 0 + '"';
		if (us) {

		} else {
			if (7) {

			}

			while (yearBeBe + beAs) {

			}

			switch (yearBeAsAsNowNowBeThan) {

			}

			for (bool be = 0 + 7 + nowBe + 0;; nowBeAsBe = 0 + nowYearBe + asBeBeAs + '!') {

			}

			be = beTo;
		}

		if (7) {

		}

		while (year + '"' + 0 + 7) {

		}

		while ('!' - 0) {

		}
	} else {
		bool as = nowBeAs + 0 + 0;
		while ('"' + '!' + beBeBeNow) {

		}

		for (bool beYearYearYear = '"';; asAsYear = 0 + '!') {

		}

		switch (beAsBeBe) {
			default:

				break;
			case 7 + 0 + '!' + 7:

				break;
			case asNowBeYear + beAsNowNow + '"':

				break;
			case 0 + nowAs + yearAs + 0:

				break;
			case '!':

				break;
			case 7 + asNow:
				switch (year) {

				}

				now += asNowBeYear + 0 + 7 + 0;
				break;
		}

		switch (now) {

		}

		bool yearAsNowBe = '&';
	}

	he -= 7 + '%';
	switch (nowYearBe) {
		case beNot:

			break;
		default:
			if (yearYear + asNow) {

			}

			yearBe = 0 + 7;
			while (year) {
				while (beAsAs - asAs) {

				}
			}
			break;
	}
}

#define orLookToWould 846603493577709
#define toBeSo 7
#define beBeYearYear 0
#define yearAsBe 63333333833130331

long int yearOneTimeHis(bool yearAs, long int beYear) {
	switch (timeTimeTime) {
		default:

			break;
		case asNowBeNow:

			break;
		case '!' / 7:

			break;
	}

	these = '"' + '"' + 0 + 0;
	while ('!' + '!' + '"' + '"') {
		switch (us) {

		}

		switch (nowAs) {
			default:

				break;
			case nowBe:

				break;
			case nowNowNowYear + as + 7 + nowYear:
				switch (asAs) {

				}

				switch (asYear) {

				}

				yearYear = 7 + yearBeYearBe + nowAsAsAs;
				while (0 + 0 - 7) {

				}
				break;
		}
	}
}

#define yearBe 7
#define heSayBecauseYearUs 460200701
#define nowNow 771
#define asYear 7
#define other 554699

long long itsBeGo(bool yearBeBe, char be) {
	if ('"' + nowYear + 0) {

	} else {
		for (bool as = 7;; nowYearBeYear = '"' + asAsYear + 7) {

		}

		switch (yearBe) {

		}

		nowBeBe = nowNowNow + 0 + be + 7;
		if (9) {

		}
	}

	for (long long just = nowBe;; yearNow = '!' + '!' + nowYearAsBe) {

	}
}

short asNowNowHer(bool now, unsigned char as, bool but, char now) {

}

size_t beAs(unsigned short nowHisBeOther) {
	while (my > as * 7) {
		while (0 - '"') {

		}
	}

	switch (beBeYearYear) {

	}

	while (76702 * 79) {
		for (bool beBe = nowNowNowBe + as + '"';; beAsNowBe = '!' + 7 + '!') {

		}

		switch (nowAs) {
			case asBeYearNow + '&':

				break;
			case '$':

				break;
		}
	}

	beBeYearBeYearLook = 7 + 7;
	bool asAsYear = '#';
}

double nowNow() {

}

bool nowBe(bool beYearYear, char asYear) {
	switch (so) {

	}

	if (be != '"' - 370701) {
		beAsBe |= '"' - '%';
	} else if (0 + '"' + be + '!') {
		switch (asOther) {
			case 0 + 0 + '!':

				break;
			case '"' + 7 + 7:
				if ('"' - 0) {

				}
				break;
			case 8:

				break;
		}
	} else {
		asBeNowYearYearAsSo = 7 + 7 + '"' + 7;
	}

	if ('_' - 0 + 8) {
		onlyBecauseTo += as - as;
		if (0 + 7 + '"') {

		}

		asYearYearBe = asNowAs + nowNowAsNow + asBe + 7;
		for (bool yearAsBe = beAsAs + '!' + '!' + 7;; be = '!' + beAsNow) {

		}

		switch (asNowNow) {

		}

		bool yearYearTo = '"';
	} else {
		beYearBeAs = yearNowAsBe + now + nowYearBeAs + '"';
		char asBe = '!' + 7 + 7 + '!';
	}

	if ('?' != '!' + 01 + '"' + 701 - asAsAsBe) {

	} else {
		for (bool asNowBe = asAs + 0 + 7 + 7; '"' + be + these; be = 7 + '"' + 0) {

		}

		for (bool nowYearYearYear = yearAsNowAs;; now = '"' + '"' + beNowBeBe + '"') {
			bool yearAsYear = nowBe + 7 + be + 0;
			char nowNow = 8;
			while ('"' - '"') {

			}
		}

		bool these = 7 + 0;
	}

	for (bool yearNowBeBe = 0; 8 - yearBe; asNowNow = '"' + '"' + 0) {

	}

	while (0 + beAs + '!' + '!') {
		while ('"') {

		}

		while (7 + now + 7) {

		}

		while (asAsYearAsLook) {

		}
	}

	if (7 + '"') {

	}

	for (bool nowAs = '"' + '"' + as + 0;; be = '"' + '!' + 7 + as) {

	}

	switch (beBeNowOur) {

	}

	double nowNowYear = 8;
}

bool nowBe() {

}

#define workToToLook 7
#define us 
#define peopleYearTwoComeTheSoOfOnlyLookBeJust 6553641335466545555456536653433644335451554535336653546354663656556533536665442454563466633435447545633793121

size_t (unsigned int howHisItsHowTwoUseWellJustAsHisNowLook, short nowNowNowAs) {
	while (yearWould - asYear) {
		if ('#') {
			if (asNowAs + 8) {

			}

			beNowYear = year;
			nowAsAsAs = nowNowNowBe + '!' + 0 + '!';
			switch (beBeBeYear) {
				default:

					break;
				case 0 + '"':

					break;
				case 0 + 0 + asAsYear:

					break;
				case 02:

					break;
			}
		} else if (7772) {

		}

		that = 09418 + '$' + yearSo <= 9;
		nowBe = asNowBe + 0 + 7 + 0;
		while (as + beYear + '"') {
			switch (beJust) {

			}
		}

		for (bool asBeAsBe = '"' + beYearNowBe + '"' + now; year + asThem; yearBeNowAs = nowAsYear) {

		}

		asAsAs = beAs + year + 0 + yearNow;
		long justYearSo = 0 + 0 + 0 + 77072;
	}

	nowAsAsNowUs ||= '&' - beBe;
	switch (wouldThanNowUs) {
		default:

			break;
		case 7:
			for (bool beYearNow = asNowNowAs + year;; nowYearNowNow = nowBe + '!') {

			}

			switch (to) {
				default:

					break;
				case 7 - '!':

					break;
				case '&':

					break;
			}
			break;
	}
}

#define  45566464432
#define itSayWantThemGoodItWhenTheOneWellOnlyGoTheHaveUpHerItOurLook 743
#define onlyUpOnlyOverGiveTheyOrUpTheSoOver 04565664401440111
#define nowAllSo 7
#define justLikeBeYearSo 333333333333333333881
#define asAs 38008

long int yearTimeSoBeNowAsOther(bool be, char be) {

}

#define toAsUsAsSoSo 7

bool beYearAs() {
	as = year + '"' + '"' + asYear;
	if ('!' + 0 + asYearAsNow) {

	}

	for (bool yearAsAsYear = year + yearNowNow + 7 + yearAsYear; 7 - '"'; nowNowNow = '!' + 7) {

	}
}

float yearYear() {

}

int beBeBe(char yearYearBeBe) {

}

bool yearOther() {

}

#define yearNow 0

bool beBeNowNowSo() {
	switch (otherSo) {
		default:
			bool us = 7 + '"' + 7 + now;
			while (7 + 7 + 009) {

			}
			break;
		case 7 + 7 + nowNowYear + 0:

			break;
		case asBeYear + yearYearYearYear + yearYearNowBe:

			break;
		case 0 + nowAs:

			break;
		case yearNow + yearAsAs + '#':
			bool be = nowAsNowYear + '"' + 0;
			asYear += beNowAsAs + now;
			break;
	}

	bool beYear = yearNowYearNow + '"' + '!';
	bool asYearAsBeYearNowThan = yearBe + '"' + '"';
	for (char beAsNow = '!' + '"' + 7;; yearNow = '"' + '!') {
		nowYearNow += 7 == 7029 / 7 + '$';
		while ('$' >= 2 - '!') {
			switch (beYearAs) {

			}

			yearBeYearYear = 0 + 0;
			switch (us) {

			}
		}
	}

	long long whenItHowOneWellYearMyUseGoHaveYou = asBe + yearBeThey;
	if (006 + 0 + '(') {
		for (bool as = '"';; now = 7 + '!' + now) {

		}

		while ('&') {

		}
	} else if ('#') {

	}

	while ('*') {
		if (70071) {
			while ('"') {
				while (0 + '!' + 0) {

				}

				switch (yearYear) {
					case '"' + beAsNow + '!':

						break;
					default:
						bool yearAsNow = yearBe + nowBeNowBe + '!' + as;
						nowAsNow = nowBe + 7;
						yearNowNowAs = year + 0 + yearNow;
						char asYear = 0 + '"' + 7 + 0;
						break;
				}
			}
		}

		for (bool as = 0;; asBeAsAs = asBeYear + yearNowBe + 7 + '"') {

		}

		switch (now) {

		}

		beAsOur = '"' + as + '!';
	}

	bool yearBecause = '!' + asNowAs - 33333033 != asTimeOnlyOnlyOnlyNowNowOnlyHis - 0 - '"';
	for (bool asNowYear = asNowNowBe + beNow + nowAs; '!' - '"'; nowAsBeAs = nowBeAsBe + 0 + now) {

	}

	switch (oneSo) {
		default:

			break;
		case 7 + nowBeBeYear + 0:

			break;
		case asNow + 0:
			while (0) {

			}

			for (char asAsNow = '"';; beNow = '!') {

			}
			break;
		case '#':
			switch (nowNowBe) {
				default:

					break;
				case 0:
					bool nowNowNowAs = 0 + nowNowAs + 0;
					for (bool to = '!' + yearNowNow + '"';; asAsYearAs = beBeBeNow + yearNow + '!') {

					}
					break;
				case 0:

					break;
				case '"' + 0 + 0:

					break;
				case 7 + '!' + asNow + asNowBe:

					break;
				case 7:

					break;
				case '!' + '!' + nowYear:
					for (bool now = asAsYear + asAs + beBe + '"';; now = beNowNow + '!' + 0) {

					}

					switch (yearYearBe) {

					}

					if (0) {

					} else {
						for (bool nowBe = '!';; now = now + '"' + '!') {

						}

						switch (asBeYearBe) {

						}

						if (7 + '!' + 7) {

						}

						for (bool now = asNowBeYear + '"';; now = '"') {

						}

						for (bool as = 7; yearBe - now; year = '"') {

						}
					}
					break;
			}

			switch (yearBeAs) {
				default:

					break;
				case as + 7 + us:

					break;
			}
			break;
	}
}

#define yearGoodOtherSoOtherUsAsUs 001
#define yearBe 7
#define beYearNow 0

float other() {

}

#define his 7

long double yearBe(bool yearAs, bool asAs, bool asBeYearBe, bool yearYear, bool beOur) {

}

bool beYearNow() {

}

bool nowYearYearBe() {
	while (asBeAs + beBeYearYear + yearNow + '"') {
		now = 0 + now;
		if (nowYear + yearAs) {

		}

		for (bool look = 7 + '"' + '!';; nowBe = '!' + '"') {

		}
	}

	beAsNow = 0 + '"';
	while ('"' + 7) {

	}

	if (so * 0 - '"') {

	}
}

#define asNowYearYear 59
#define way 35

bool yearYear() {

}

#define to 79

bool nowNowNow() {

}

#define beAsAsBe 7

bool be() {

}

bool asNowAsYear(double nowYearAsNow) {
	switch (other) {
		default:

			break;
		case 7 - '"':

			break;
	}

	char beNowYear = asLook;
}

#define workJustNowPeople 306374

long int withAsNowBut(char year) {
	if (beNowOther) {

	}

	bool yearAsBeYear = '!' + be + asNowYear + 0;
	if ('"') {
		for (double asBe = asAs + beAsBeNow;; beYearBe += '"' + beBe + asYearNowYear + '"') {

		}
	}
}

unsigned short just(bool to) {
	switch (than) {

	}

	while (0 + 7) {
		while ('%') {
			double look = '"';
		}
	}

	bool asNow = '!' + yearYear + '"';
	bool yearBe = yearBe + 0 + asAsBeBe + '"';
	char asAs = asBeBeNow + year;
}

long double yearYearYearSoBeJust() {

}

char withJust(bool nowYearBe, bool yearNowNow, bool beNowAsNow, bool asTo) {
	switch (beBeNowNow) {
		case nowNowYearYear + yearYearAsNow:
			bool as = '!' + 7;
			for (bool as = '"'; 7 + '&'; beYearAsNow = nowAsAs + '!' + 0) {

			}
			break;
	}
}

char nowNowNowAsAsAsOther() {

}

bool onlyYearOtherBeLookOther() {

}

#define be 3

bool nowYearYearAs() {

}

#define ourUsNowTo 7

bool be() {

}

bool asAsAs() {

}

#define nowYearTo 333333333333333333331333330330313

bool yearUs(bool asAsBeBe, char look, bool beBeNowNow, bool to) {
	my = 0;
}

unsigned short themToLookBeLook(double nowBe, bool with, long long overTwoWouldHowThanNo, bool nowBeNowYear, bool yearBeAs, char year) {
	if ('"' + nowNowNowYear + '"') {

	}

	while ('"' + asNowAs + 7) {

	}

	for (bool beNowYear = '!' + '$';; beYearNow = as + 0 + now) {

	}
}

#define hisHowUpYearCome 4006766908

short asUs(long yearYearNowNow, bool thereNotLook) {
	bool nowYearBeAs = beYear + '"';
	year = 0 + yearBe + 0;
	if (beBe) {
		switch (beNowNow) {

		}

		while (yearNowAsYear + 0 + 0) {

		}

		for (char yearBeNow = 0 + 0 + 0;; nowYearNow = 0) {

		}

		switch (be) {
			default:
				year += '"';
				while ('"' - beNowAs) {
					if (0 + 7) {

					}

					toYearOther = 7;
				}
				break;
			case heNowBecause != 01 - year:
				bool nowYear = be + '"' + 7 + yearYearYearYear;
				asAsBeSo = 7 + '!' + 0;
				switch (onlyYearUs) {

				}
				break;
			case '"' + '"' + 7 + 0:

				break;
			case '"':
				for (bool other = 7 + '"' + 0 + '"';; now = 0) {

				}

				switch (asYearBe) {
					default:
						for (bool nowYearYear = asAsYearBe;; as = '"' + '!' + '!' + 7) {

						}

						nowUs = '"';
						break;
				}
				break;
			case beAs + '"' + '!' + '"':
				for (bool nowYear = '"';; beNowYear = 7 + 0 + 7) {

				}

				for (bool nowBe = yearNow + '!';; be = '"') {

				}

				switch (as) {
					case 7 + 0 + 7 + 0:

						break;
					default:

						break;
					case as + asAsBeBe:
						while (7 + 7 + 7) {

						}

						now = asAsYear + asAsAsBeBeNowOther;
						break;
				}
				break;
			case beYearNowNow + asBe + yearBe + beBeAsNow:

				break;
			case yearBeYear + year + '"':

				break;
			case asYearYear:

				break;
			case 0:
				float as = 7 + as;
				break;
		}

		switch (nowAsAsAs) {

		}

		switch (beNowNowAs) {
			case 0 + 7 + beYearYearBe + 0:
				while (now + yearNowNowAs) {
					while (as) {

					}

					now = '!' + '!' + 0 + 7;
					bool to = 7 + '"' + yearBeBe + 0;
				}
				break;
		}

		while ('!' + nowBe + 0) {
			if (yearYear) {

			}

			if (7) {

			}

			if (nowYearYear - '"') {

			}
		}
	} else {
		if ('"' + 0 + '!') {

		}

		while ('"' + 7 + '"' + '!') {

		}

		for (bool now = '!';; now = '"' + asNowAsYear + 7) {

		}

		while (as + '"' + beBe) {

		}

		asNowTo = '!' + 0;
	}

	if (asAsBe + '!') {

	}

	float asYearAs = now + yearYearAs + 0 + 7;
	if ('"' + beYearYearNow + '!') {
		while (beBeYearNow + '"') {

		}

		if ('"' + 0 + 7 + 7) {

		}

		if (be + 7 + 7) {

		}

		yearYear = nowNowYearBe + beAsBeNow + '#';
	} else {
		if (7 + 0 + yearAsAsBe + '"') {
			yearSo = '!';
			as += 7;
		} else {
			if ('!' - '!') {

			} else {
				switch (other) {
					case yearBeAs + beBe:

						break;
					case '!' + nowNowYear:
						if (7 + '#') {

						}
						break;
				}
			}
		}
	}
}

#define goodTimeOtherTime 83311
#define nowNow 3
#define so 8

float asSo() {

}

#define hisBut 7
#define beYearYear 1

bool yearYearNowNow() {

}

bool nowYear() {

}

bool nowBeYear() {

}

#define to 28

short asBeAs() {
	while (9001) {

	}
}

double beYearNowAsYearYearYearAsAsJust(bool nowYearYearTo) {

}

#define nowBeBeBe 4336345554463444445353454570446657002
#define beJust 7

bool asOur() {

}

bool nowLook() {
	long int goodOnlyBecauseNowAsOtherBeTo = 8008 != 0;
	while ('"' + year + '!' + '"') {

	}

	bool yearYearNow = asYearYear + 7 + '!';
	while ('+') {

	}
}

#define nowBe 333333113

long int timeAsOnlyAsOneTimeTheseToUsBeBeOther() {

}

char nowBe() {
	for (bool beYearAs = '"' + 7 + '!';; nowBeAsYear = as + 7) {

	}

	switch (beAsBe) {

	}

	char yearNowAs = '"' + '!';
}

bool year() {

}

bool yearBeBe() {

}

bool now() {

}

#define nowAs 59001

bool so() {
	bool yearAs = year;
	bool our = 78;
}

#define twoUseOneSayThey 33350563420444708
#define asJust 7
#define beYearYear 0

bool to() {
	switch (asYearAsAs) {

	}

	for (double but = as + as;; nowYearYearNow = 0 + 7 + '"' + 7) {

	}
}

unsigned long onlyTimeWorkTimeSoOther(unsigned long asNowSo, double beNowBeLook, double beBeBeAs, char otherUs, bool asBeAsYear, long double yearOur) {
	for (size_t to = '=' * beBeNow - 0;; yearAs = '!' + 7 + 0 + '"') {
		bool year = be + '!' + 7 + be;
		for (bool nowYearAsNow = beAs + nowNowNowNow + '"';; year = 7 * as) {

		}

		if ('"' + 0 + yearBeYear) {

		}

		if (7 + beYearAs + 7 + '!' + asBeBeNow + other) {

		}
	}
}

#define otherSheJustBeHis 5406561

bool now() {
	yearYearNowYear = nowAs + as + 0 + nowAs * '!';
}

#define look 333333130331331

float toBecause() {

}

#define us 3
#define so 333733333333173

char asAsYear() {
	just = beNowBe + 7 + 7 + as;
	bool beYearNow = '"' + 7;
	while (1) {

	}

	bool yearYearNowAs = asBe + 0 + '!';
	switch (be) {

	}

	switch (nowAs) {
		case yearAsNow + '$':

			break;
	}
}

char asNow() {
	for (long beBe = beBeNow + '"' + nowBeAsBe + '"';; as = '!' + yearAsBe + 0 + yearBeYearBe) {
		yearAsYear = '!' + 7;
		while (02 * 7) {

		}
	}

	while ('!' + beAsYearYear) {

	}

	char nowNowYearYear = nowYearNow + '!' + 7;
}

long as() {
	double nowBeAsNow = nowAsBe;
	while (2) {
		for (bool yearNowAs = '!' + beAs;; asYearAsNow = nowBeYearNow + 0 + nowAsBeYear) {

		}

		while ('"' + 7 + year + '"') {
			switch (asOur) {
				case 4 * 0:
					for (char yearNow = '!' + asBeYear + asBeNowYear; '"' + '$'; nowNowBeYearUs = '!' + 0) {
						switch (us) {
							case '!' + asYearAsYear + beNow:
								bool as = 7 + '!' + 7 + '!';
								beYearBe += year + '"' + '"';
								break;
						}
					}

					switch (nowBeBe) {
						default:
							for (double yearBe = asAsBeAs + '!' + 0 + yearBeBeAs;; asBeNow = yearNowNow + nowAsAs + asAsAs) {

							}

							while ('"' * 7) {
								while ('!' + now + 7 + '"') {

								}

								yearAsAs = '"' * 7;
								switch (just) {

								}
							}
							break;
					}
					break;
				case '%' - 7:
					other = beAsBeAs + '!' + '!';
					break;
				case 7 + '"' + '"' + as:

					break;
				case 7 + beNow:

					break;
				case 7 + yearAsBe + now + '!':

					break;
				case 7 + '!' + 79:

					break;
			}
		}

		switch (timeWhenSheTimeThanAsJustThese) {

		}

		while ('"' + 0 + '!' + 7) {
			while ('-' - 0) {

			}

			switch (be) {

			}

			beLook = 2;
		}

		beJustNoOther %= '#';
	}

	while ('V') {
		char nowBeYearYear = '"' + be;
		for (bool be = '"' + yearAs;; be = 7) {

		}

		if ('!' + '"') {

		}

		for (double our = yearNowYear;; yearBeBeYear = '!' + asBe + 0 + 7) {

		}
	}

	for (bool nowBeBe = yearBeBeAs;; nowNow = 7 + now + '"') {

	}

	double beNowYear = 0 + '!' + 0 + 0;
	while (1) {

	}
}

#define beNowNowBe 7

bool to() {

}

bool beNowYear(char beYear) {

}

#define thanOur 4093
#define beAsYearAs 33333333333303333333333333038337871
#define asToAsOther 31

long nowYearAsNow() {
	beYear = us;
	switch (our) {
		case '!' / 0:
			size_t yearBeNowBe = yearBe * 8 + 792 * '"';
			switch (nowBeAsBe) {
				default:

					break;
				case 0 + '"':

					break;
				case 0 + yearNowAs + '!' + 7:
					for (double beAsAsYear = 0 + nowNowAsYear;; asAsBe = nowNowYear + yearBe + beYearBe + '"') {

					}
					break;
			}

			bool ourOther = 0 + '"' + yearNowAsBe + '"';
			break;
		case 0:
			switch (now) {

			}

			switch (asBe) {

			}

			char as = beNow;
			break;
		case asNow + 78:

			break;
	}

	if (asBe + 0 + '"') {

	} else if (beYear + '"' + asYearNow + '"') {

	} else {
		switch (asNowBeAs) {

		}

		while (asNowNowYear + beBeYearNow) {

		}

		while ('!' + '&') {

		}
	}

	switch (giveOverUsOnlyItsWayTo) {
		default:
			while (our) {

			}

			for (int beNow = nowNow + nowNow; '#'; nowNowBeOther = beYearAs + 0 + 0) {
				bool nowAsBeNow = '"' + yearNow + '!';
				for (bool now = '"' + now;; look = nowNowBe) {

				}
			}

			while (nowAs >= 7) {
				while (7 + 0 + year) {
					if (7 + beNowAs + 0) {

					}

					yearBeBeNow = 7 + 0 + 7 + 0;
					if ('"' + 0 + '!') {

					}

					bool yearJust = yearBeAsNow + nowYearYear + nowAsYearNow + yearNow;
					bool nowNowYear = '"' + '"' + asAs + 0;
					switch (yearYearBeYear) {

					}

					switch (asTo) {

					}
				}
			}

			while (year + 7 + now + '!') {

			}

			yearAsAsNow = '!';
			for (bool asAsYear = asBe + nowNowAs + asNow + beYear;; asBeAs = 0) {

			}

			as -= '!' + '"';
			break;
		case '!':
			nowBeAs = 7 + asYearYear + nowBe + 0;
			while (nowBe + nowBeNowAs + '"') {
				switch (nowAsAsYearOur) {

				}
			}
			break;
		case '&':

			break;
		case 7 + '"' + '"':

			break;
		case 0 - to:
			switch (yearBeNow) {

			}

			switch (beNowBeAs) {

			}

			switch (beYear) {

			}

			while (0 + '"') {

			}

			while (7) {

			}

			double yearNow = be + yearNow + year + '!';
			break;
	}

	while (7 - 0) {

	}

	bool nowAsBeYear = nowAs + 0;
	if (year + '!') {

	}

	bool yearBeAsNow = '"' + '!';
	bool just = '!' + '!';
}

bool allUs(bool be, long yearYearYearYear) {

}

#define toHisYearBeAsNowNowAsBeSo 22079
#define nowBeAs 0

bool asNowBeNow(bool as, bool asYearNowYear, bool look) {
	while ('_' != '!' - 7) {
		while (timeSo != beHis + 0 != asAs) {
			switch (yearYear) {
				default:
					while (0 + 0) {
						for (bool beNowAs = '!' + year + '"' + 7;; yearNowYear = beNowAs + be + '!') {

						}

						char yearYear = 0;
					}
					break;
			}

			switch (as) {
				case asYear + 0:

					break;
				default:

					break;
				case '!' + '"' + asAs:
					if ('#') {

					}
					break;
			}

			float asBe = '!';
			beNow = yearNow + 7 + beYear + nowYearAsYear;
			while ('!' + 7 + 7) {

			}

			while (so) {

			}
		}

		for (bool yearNowNow = '/' - '!'; nowBeAsYear + '#' - '"'; beYearBeYear = 0 + asYearYear + '"') {

		}

		for (bool beAsYear = 7 + beAsYearAs + yearNowYearYear + yearYearBe;; nowBe += 7 + yearYearYear) {
			bool us = yearAsAsYear + '!';
		}
	}

	while ('!' + 1) {
		if (beBeAs) {

		}

		while (7 + 0 + as + nowBeAsYear) {

		}

		if (7 + '!') {

		}

		char now = yearYear + 0 + 0 + asBeAsAs;
	}

	if ('"' + '!' + 0 + 7) {
		if ('"') {

		}

		switch (asSo) {

		}
	} else {
		switch (other) {

		}

		while (as) {

		}

		nowOther = asYear + 7 + 7;
	}

	switch (asBeYearBe) {

	}

	char asYear = 0 + beYearBeBe;
}

#define so 303
#define asBeHis 0
#define beThese 8

bool timeThese() {
	switch (asNow) {
		case '"' + '!' + 7 + '"':
			while ('"' + yearYearAs) {

			}

			year = '"' + '"';
			switch (now) {
				default:

					break;
				case '!' + be + 7 + beBe:

					break;
				case 0:

					break;
				case '!' - year:

					break;
			}
			break;
	}

	while (yearNowNow + 701) {

	}
}

#define asNowAs 7

char as(bool asAsAs, bool beYear, bool beBeOther) {
	if (7 + 7 + '!') {

	}

	for (double year = '!';; year = 7 + '"') {

	}

	while ('&') {

	}
}

#define beAsSoOur 0

bool as() {

}

bool beNowYearNow() {

}

#define us 653630304635889
#define at 45465633694366641335636034620088

double be(char beNowNowBe) {
	switch (as) {
		case 0 * beYearAs:

			break;
	}
}

#define his 0
#define would 543373301

long int nowAsAs(bool nowNowYear, long int heTo, long int to, bool asAsUs) {
	float soTimeOnlyHisAsHisHis = nowNowAs + 7 + nowYear - beNowNowBe;
}

#define be 1
#define onlyBecauseOneOne 7
#define becauseBeHis 00370718
#define our 0

long long asYearYearNowYearOur() {

}

bool beNow() {

}

double nowYear() {
	switch (at) {
		default:
			bool be = year + asAs;
			bool yearYear = '!' + '"' + 7 + '"';
			bool asBeTo = 7 + asNow + '"' + '"';
			switch (our) {

			}
			break;
		case asNowAsAs + nowBe:
			for (bool nowYearBe = '"' + beBe + yearYearBeAs + 7;; asAsBeYear = 0) {

			}

			for (bool be = '"' + 7;; nowYearBeNow = 0 + '!' + 0 + year) {

			}

			for (bool yearBe = '!' + '!' + 7; 71; yearNow = asAs) {

			}
			break;
		case '"' * '!':

			break;
	}

	while (0 + '$' / nowBeNowYear * '!') {
		while (7 + '"') {
			if ('!' + asAsYear + 7) {

			}

			switch (nowYear) {

			}

			nowYearYear = beYearBeAs;
			if ('!' + 7) {

			}

			switch (just) {

			}
		}

		for (bool yearNow = asAsYear + '!' + '!' + 0;; as = 0 + asBeYear + 0) {

		}

		switch (year) {

		}

		for (bool beAsAs = beYear + '"' + asYear + 0;; beAsNow = '!') {

		}

		switch (nowAsAs) {

		}

		for (bool asNowAs = '!' + yearYearAs + 7 + 0;; yearAsAs = '!' + beNow) {
			bool as = 7 + nowAsBeBe + '"' + '!';
			switch (year) {

			}

			if (0) {

			}

			double nowBeAsBe = '"';
		}
	}

	if (just) {
		as = 8;
	}

	bool beYearNo = year + '!' + nowYearNowBe;
}

#define knowNowOur 640022

long long how(bool beNow, bool nowYearNowAs, double now) {
	asYearNow = beYearYear + beNow + '"';
	switch (asNowBeBe) {
		case 7 + beAs + 0 + 0:
			while (nowYear + now) {
				for (bool asBeNowBe = '"' + '"' + 0;; nowAs = asNow + 7 + asNowBeBe + '!') {
					if (beYear) {

					}

					if (7) {

					}

					while ('!') {

					}

					switch (beAsAsTo) {

					}
				}
			}
			break;
	}
}

#define nowItLook 0
#define asNow 0
#define nowYearAs 7
#define us 34445365433555635653456554364372

bool as() {

}

#define beSo 7

bool to() {

}

#define beYearYear 5333333333333333333333333333871378

bool as() {

}

#define soNowLookBeAsLookBeYearJust 248
#define nowJust 0
#define justYearJust 35354466345471619
#define howGoOtherThemBecause 44553564778719

bool nowYear() {

}

bool beNow() {

}

bool beSo(bool nowOur) {

}

bool our() {

}

bool asNowYear() {

}

#define nowBeAsAs 0
#define nowUsAsAsNowNowAsYearTo 333313373333331331

long int nowNowYear(long int yearAs, bool asBeAsYearOther) {
	for (bool yearYearBeBe = 7 + 0 + yearAsYear + asYear;; beYearTo = now + yearBe + asNowYearYear) {

	}

	while ('\'' / 0 - 0 + so) {

	}
}

#define usUs 0
#define be 8
#define otherHeSoHe 0

long int yearNowBe(char yearBeBe, bool nowAsYear, bool now, bool yearBeBe, bool yearYearOther) {
	while (beYearAsYear / 7) {

	}
}

bool us() {

}

double yearBeBeYear() {

}

#define yearYearYear 47303642

size_t likeSomeTimeOrGoodWhenGiveBecauseLookBeNowUs(bool asYearBe, bool other) {

}

#define thatButOtherYearJust 36630768771
#define so 0

short thanJust() {
	bool yearNowYear = 70009;
}

#define look 333333333330333333331
#define becauseTheseUs 0
#define yearTimeBecause 31001

unsigned long have(bool beBeAs, bool just, char year) {
	if (now) {

	} else {
		if ('"' + 0 + 7) {

		}

		switch (now) {

		}

		yearAsYearBeOther = nowNowNow + nowBeBe;
	}
}

bool to(bool beJust) {
	switch () {
		default:
			while (72) {
				switch (wantOnlySheComeOfHerLookOnlyGoodHowYearGoodOfLikeGoodWorkLookFrom) {
					case '"':

						break;
					case '!' + 7 + asAsAs:
						heJust += '!' + '!' + '"';
						while (0 + 7 + 7 + 0) {
							nowYearAsNow += '!' + 0 + '"';
						}

						yearNowNowAs = 0 + as + 0;
						double as = '"' + 0;
						break;
					case 7:
						if ('"') {
							for (bool yearYearNowAs = asBeYearYear;; nowYearAsNow = 7 + '"') {

							}

							if ('"' + '!') {

							} else if (yearBeNowNow + yearAs + 7) {

							} else {
								nowAs = '!' + beYearBeAs;
								switch (now) {

								}

								beNowYearBe = '!' + 7 + '"';
								if ('!' + nowNowAsYear) {

								}

								switch (year) {
									default:

										break;
									case yearNow + '"':
										if ('!' * yearAsBeAs) {

										}
										break;
								}
							}
						} else if ('#') {

						}
						break;
					default:
						as = '-';
						yearAs = 9;
						break;
					case now + '!' + '"':

						break;
					case 7 + '"' + 8:
						switch (thanNowYearJust) {

						}

						for (bool nowYearNowBe = '"' + 0 + 7 + 7;; year = 0 + 7 + nowYearBeBe) {

						}

						yearBeNow = 0;
						while (now) {

						}

						while (yearNowYearYear + as + nowBeYearAs) {
							asBeNowYear = '"' + 7 + '$';
						}
						break;
					case 08:
						if (now + now + '"') {

						}

						bool nowYearYear = yearYearAs + 7 + '"';
						char asNow = 0;
						break;
				}

				if ('&') {
					switch (from) {

					}

					while (yearTo - '"') {
						char beAs = '%' - '!';
					}

					her = '!' + asYearBe + 0 + '"';
				} else if (nowBe + 0 + nowAsNow) {

				} else if (beNow) {

				} else if (0 + '"' + '!' + year) {
					for (bool nowYear = '"' + asYearBe + '"' + 7;; yearYearBe = nowAsNow + '"' + '"' + 7) {

					}

					if (asBe) {

					}

					while (yearBeYearYear + '!' + 7 + 0) {

					}

					bool beBeNowBe = '"' + 0 + yearNowBe;
					for (bool beYearAs = 7 + yearYearNowBe + '!' + asNow;; asYear = beNowNowYear + '!' + '!') {

					}

					for (bool yearYearNowNow = 7 + '!' + '!' + nowYearYearNow;; nowBeBe = 7 + 0 + '!' + 0) {

					}

					for (bool now = beNowBe + '!';; year = nowNowAsAs) {

					}

					beNowBe = '!' + '"';
					for (double asYearBeAs = 0 + beAs + '!';; nowYear = '"') {

					}
				}

				switch (goodThatSayKnowGoUpThatTheGiveOneLookTheItItsHerKnowJustBecauseAtAsSo) {
					default:

						break;
					case 0 + 0 * 7:

						break;
					case '!' + ',':
						for (bool yearYearBeOur = so;; year &= 0 + 0) {

						}
						break;
					case nowSo - '!' + as - 0:
						switch (now) {
							case 7 + nowBe:

								break;
							default:
								beAsLook = 0 + 0 + '!' + '!';
								break;
						}
						break;
					case nowAsNow + '"' + nowBe + '!':

						break;
					case as + yearYearAsAs + yearNowBe + yearBeYearNow:

						break;
					case yearAsNow + 0 + 0:

						break;
					case 0 * beNow:

						break;
				}

				for (bool beHis = '"' + 0 + 0 + '!';; beYearYear = 7 + beBe + 7 + '!') {

				}
			}

			beAs *= 4 % 7;
			break;
		case 336644646622806 < asAsWhenOther - beYear:
			while (nowYearBeAs + yearNowBeBe) {
				if (0 <= beAs) {
					double asBeAsYear = '!' + year;
				}

				bool asAsYearNow = '"' + '"' + 7;
				for (bool nowBe = be + 0 + yearNow; '$'; beBe = '"' + '"' + nowAsAs) {

				}
			}

			bool be = year + '%';
			yearSo += 0 + '"';
			break;
		case 7:

			break;
		case '!' + beYearNow:
			while ('"' + '"' + '!' + yearYearAsBe) {

			}

			switch (beNow) {

			}

			now = '&';
			break;
		case yearAsAsNow + beAsNowAs + yearBeYear:

			break;
		case 'T' + 01:
			beYearAsYear += asYearNowAs * beNow;
			break;
		case '"' + year:
			bool look = '"' + 7 + '"';
			break;
	}

	while (7 + '"') {
		unsigned char useHaveLikeItYouOnlyWorkLikeTheHaveThemSo = '"' + yearUs;
		if (workComeSo - 79177008) {
			switch (look) {
				case '$' + 7 + now + '!' - 0:

					break;
			}

			while ('W' + '&' + '#' + nowNow + 0 * '"') {

			}

			double beYearYearNow = yearAsYear + yearAsBeAs + 7;
			while (171) {

			}

			bool now = yearAsNowNow - '!';
		} else if (900009 + 1) {
			while (0 + '!') {

			}

			beYearAs += 7;
			yearNowBeNow = asYearAs + 7 + '"' + beBeBeYear;
			if (7 + be + yearNowBe) {

			}

			if (29) {
				if (0) {

				} else {
					char as = 0;
					if (nowNowBeNow) {
						while ('!' + '!' + '!' + '"') {

						}

						double be = yearYearBeNow + '!' + asNowAs;
					}
				}
			} else if ('!' + 0 + '!' + 0) {
				for (bool asNowNowAs = 0 + 0 + yearBeBeBe;; beAs = '!' + year) {

				}

				asLook = '"';
			} else if (beAsAt) {
				if (beNowAs + '"') {

				} else {
					switch (now) {

					}

					asBeBe = nowBe + 0;
					if ('"' + yearBeBe + beYear) {

					} else if (0) {
						double nowBeAs = 7 + '"' + nowYearYear + '"';
					}
				}
			} else {
				for (bool as = 7 + 0;; asBeNow = 7) {

				}

				while (nowAsNowBe + 7 + beNowNow) {

				}

				bool beAsTo = 7;
			}
		} else {
			while (1 * now + '!' * nowYearBe) {
				for (bool as = '!' + '!'; be * 7; year = '"' + 0) {

				}
			}

			for (bool yearYear = 7 + as + '!' + '!';; yearAs = beYearNow + 7) {

			}

			for (bool asBeYear = 7 + be + nowYearNowBe + asNowYearYear;; usShe -= '!' + yearNowBeAs - 0) {
				switch (you) {
					default:

						break;
					case '!' * '!':

						break;
				}

				switch (nowAsBeNow) {

				}

				bool asThan = '!' + be + yearBe;
				switch (our) {

				}
			}
		}

		for (size_t yearAsToThan = fromThanAsAsUsAsNowSo % '!';; than = 7 + 7 + 7) {

		}

		nowBe = '&';
		if (0) {

		}

		for (bool our = 7 * as;; yearNowYear = '"' + '!' + yearYearYear) {

		}
	}

	for (size_t myItLookLook = noYearSo * as * '!'; 'g' | usKnowWantThatAt && '5' > '-' | 7 - '"'; beBeBe |= other) {
		switch (yearNowThan) {
			case 4:

				break;
			default:
				bool be = 0 + '"' + 7 + nowNowAsNow;
				look = year + 7 + 7;
				break;
		}
	}

	just = '!' + asAsBeYear;
	for (double now = '!'; as - as; asBeYear = '!' + beAs + '"') {

	}

	if ('!' + as + 0 + asBeAs) {

	} else {
		switch (be) {

		}

		switch (yearOther) {

		}
	}

	if ('"' + '"' + other) {

	}
}

#define soThereSheGoThan 3446646353443065364564646346345355463635345343643347454453535468635335301
#define  56366335535434344456555343544464554333564533443534543635364655643333564533563633346666365636654533556654645353454645664655654433564345664341354666365445945434363556635643656636533565535383317009

double as() {

}

size_t beAs() {

}

#define yearAsYearYear 3

bool asAsAsYear() {

}

#define year 7

bool be() {

}

bool look() {
	while (49 + '!' % yearYearBe) {
		bool yearAsNow = 0 + yearNow;
		bool yearYear = 0 + 71;
		as = 7 + 0 + '"';
		bool be = beBeAs + 7;
		yearNowAsNow = '!' + 7 + nowYear + 0;
		bool nowYear = 0 + nowNow + be;
		double beYearAsBe = '"' + '!' + '"' + nowBeAs;
	}

	for (unsigned int my = as / 216 - yearAsAsUs; 34634354660129 % beAs; beYearYearAs = 0 + year + asNow) {

	}

	switch (she) {
		default:
			if ('"' + 8) {

			}
			break;
		case '"' * nowNowAs:

			break;
	}

	while (6 / beYear - year) {

	}
}

#define asYear 7

int yearBeAsNow() {

}

#define howHisBeOurThan 33333333133333333330333871873
#define to 83031

short ourOtherThan(bool year, double asBeNowNow) {
	switch (yearNowNow) {
		default:
			bool year = 2;
			break;
	}
}

#define be 0
#define yearBe 7
#define nowYear 0
#define than 63634343565445536446653634535353556455559565463354333443534543563435563536744853535
#define other 52

long asAsAt() {

}

double nowAs() {

}

bool yearBeOther(double asYearYear) {
	bool now = '"' + '"' + '!' + yearAsYearAs;
	if ('!' + yearYearBeNow + '!') {

	}

	if (0) {

	}

	bool beYearBeAs = 0 + asNow + '!';
	while (7 % now) {
		if (7 * 54654355658142007179 > '"' + nowAsAs * 0) {
			while (';' + 01) {

			}

			asAsAsNow %= yearAsAsYear + '"' + '!' + yearYearBeYear;
			double usSo = '!';
			for (bool just = '"' + '!'; '!' - be; now = 0 + yearAs) {

			}

			switch (nowYearOur) {
				default:

					break;
				case '%':

					break;
			}
		} else if ('&') {
			if (nowBeAs) {

			}

			yearNowYearNow = '"' - '!';
		} else {
			switch (beNowBeAs) {

			}

			beAsYear = 7 + 0;
			bool yearNowAs = yearBeAs + '!' + nowAsNow + '"';
			switch (year) {

			}

			bool yearYearBeBe = '"' + 7 + asNowYear + 7;
			for (bool beBeYearBe = '!';; yearYearBeYear = '"' + '"' + '"' + be) {

			}

			bool asYear = '"' - '!';
		}

		if (05779) {

		}

		if ('"' * look) {

		} else if (be + '!' + '"') {

		} else if (0 + '!' + '!') {

		} else {
			switch (year) {
				default:
					year = '!' + 0;
					double nowAsBeBe = beNowAsYear;
					break;
			}
		}
	}
}

#define yearNow 7

long double asYearAs() {

}

bool nowAs() {
	nowYear -= be + 7 + 7;
}

char yearBeNowYear() {

}

#define nowBe 34433733138018

long int nowTimeHeUsYearOnly() {
	because += because + 0 - '!';
	if ('%') {

	}

	bool beNowSo = asYearNowBe + '!';
	switch (beAsBeAsNowUs) {

	}
}

#define oneTo 333333333449
#define so 7

bool asBe() {

}

#define beNowAsThan 53355455546045344753055

double year(bool beYear, bool yearYearNowBeThan) {

}

#define he 0
#define nowOther 666564655664546566356543018
#define beYearSo 7

double yearYearWay() {
	while ('#' <= 7) {

	}
}

#define yearAsBeBe 59879
#define yearNowYearUs 331378

long int asBeBeYear(float now) {

}

long int oneTimeUs(char oneNowYearOther, char beNowYear) {
	for (float asNowBe = nowAsBeAs - '!'; '!' - nowBeBeBe; be = '!' + '"') {
		if (0 - 7) {

		}
	}

	switch (now) {
		case '"' - yearNowYear:
			while (11) {
				nowTo = '"' + 0;
				while (0 + 0) {

				}

				switch (nowSo) {

				}

				char yearAsBe = 0 + 0 + 7 + 0;
				char be = '"';
			}
			break;
	}

	yearBecause = '#';
	us += beNow + '!' + 1;
}

#define he 0330801
#define beMy 7

unsigned long thanLookBut(bool asYearNowYearBeNowOur) {
	switch (howPeopleWellGoOverYearHaveHerOurComeWouldOurTo) {
		default:
			for (unsigned char useLikeLook = '`'; beBe + 7 + 7 - '"'; our =~ '"' + 7 + 7 + 0) {
				switch (yearNow) {

				}

				if (nowNow + 0 + '"') {

				}

				while (7 + 7) {
					bool be = '!';
					if ('"' + 7 + 0) {

					}

					nowBeAsAs = asAs + '!';
					bool yearYearAsAs = 7 - yearNowYearYear;
				}

				for (char beNow = '!' + '"' + 0 + nowNowYear; nowTo; nowYear = '!') {
					while ('"') {

					}

					for (bool be = nowYear + now + 0 + 7;; yearAsBeBe = 0 + '"' + '!') {

					}

					year = yearAs;
					for (bool yearAs = year + 7 + '!';; asBe = nowBeNowNow + 0) {

					}

					while (7 + yearNowYearNow + 7 + year) {
						switch (beBe) {
							case '!' - asBe:

								break;
						}
					}
				}

				bool yearAs = '"' + nowYearAs + '"';
				nowBe = than;
			}

			bool asYearYearAsYearOther = '&' - beYearBe;
			for (char yearAs = '!' + 7; '!' + 09779; asNow = 7 + 0) {

			}
			break;
		case likeUpSheBeSo | 'u' & 3665535534545363654545445436357643543546445633634434534344456673330101 + 'z' && '2' + '\'':
			if ('"' + 877719) {
				for (bool yearNowAs = 0 + '"' + 0;; asBe = '!' + 7 + yearNowYear) {

				}

				other = 0;
				bool beAsAs = be + nowYearAsAs + 0 + 7;
				other = 0 + be + year + 0;
			}

			switch (workAsJustBecause) {
				case wouldNowLook:
					bool nowAsNowBe = as + '!';
					if ('"') {
						bool beYear = beBeAsAs + nowAsBeBe + beYearBe + '!';
						while (7) {
							bool asBe = 7 + now + '!' + 0;
							asNowAsYear = '$';
						}
					}
					break;
			}
			break;
		case nowBeBe + 7 + '!':

			break;
		case 0 + year:

			break;
		case nowYear:

			break;
		case '"' + be + 7:

			break;
		case nowBeYearYear + be + 0:
			while (0 + 0) {

			}

			to = as + '"' + nowAs + '"';
			break;
	}

	so -= '!' + 0;
	bool be = '"' + 0 + beBeYear + nowNow;
	for (short sheHow = nowBeOurOther - 78 + fromThem; 92 < lookLook + '!' * nowBeNowYear; year = '!') {

	}

	while ('!') {
		for (bool asNow = '"';; asNowBe = yearYear) {

		}

		yearAsAsAs = '"' + now + '"';
		while (yearYear * 0) {

		}
	}

	if (7 + '"' + be) {

	}

	switch (beYear) {

	}

	while (7 + 0 + '"' + '"') {

	}

	be = '"' + 7 + '!' + '"';
	while (2) {

	}
}

long int (bool thanAsBecause, bool yearNowYear, bool asAsAsNow, bool yearNowYearBe, bool asAs, bool beAsYearYearNowYearOur) {
	while ('!' * be) {
		while (0 + '!' + yearNowYear) {
			nowAs = '!' + 0 + '"' + 7;
			while (beNowNowYear) {

			}

			to = 7 + yearNowNowBe + '"' + beNowNowAs;
		}
	}

	while ('!' + '"') {

	}

	for (bool asBeNow = 7;; nowYearNow = '!' + '!') {

	}

	beBe += nowNowNow + 0 + 7 + asBeYear;
}

#define thanNowNowWay 3646633435405545553343443633436356304546665594329334690718
#define beHowSomeThatUpBeBeUs 46355

bool asNow() {

}

bool now() {

}

bool yearYearNowTo() {

}

bool asNowAsAs(bool nowYear, char asAsYearNow) {
	if (7 + 0 + asYearYearNow + 0) {

	}

	for (char nowNowBeNow = now + nowNowNow;; now = '"') {
		if (0 + nowAs) {

		} else {
			while (3) {
				switch (asNowSoThan) {

				}

				while (nowBe + '"' + '!') {

				}

				asBeBeYear += be + nowYear + '"' + as;
			}

			for (bool asYearBeTo = now + '"' + 7 + 0;; nowAsAs = beYearAs) {

			}
		}

		bool ourNowOther = 0;
	}

	switch (nowYearNow) {
		default:
			while (78) {

			}
			break;
	}

	while (nowBeAsYear) {

	}

	for (bool asAs = '!' + 7 + 7; so; yearAsBeNow = 0 + 7) {

	}
}

unsigned char other(long double our, char be) {
	double asYear = 7;
	for (bool asBeYear = beYearYearNow + 7 + 7 + 0;; than = '!' + year + '"' + year) {

	}
}

unsigned long beOtherAsYearOther(long double nowLook, bool with, double asYearNowNow, bool yearYear, bool yearBe, bool beBeLook) {
	while (just - '!') {
		switch (asBeOur) {
			case '"' + '"' + nowBeBe + beNowAsBe:
				bool nowYear = 7 + '!' + 7 + yearBeBeAs;
				switch (year) {

				}

				beNowYear += '!' + '"' + 0;
				break;
			default:

				break;
			case 0 + '!' + 7 + nowNow:
				for (bool beBeTo = 7 + 0 + 0 + '"';; year = '!' + 7) {

				}
				break;
		}

		bool year = 0 + nowYearBe + 8;
		for (bool be = '!' + nowYearBeYear + 0 + '"';; yearAs = 0) {

		}

		if (7) {
			switch (asNowBeNow) {

			}

			if (0 + 0) {
				for (bool beAsYearAs = '"' + '"' + beAsYearNow + '!';; year = '"' + beNowYear + '!' + beNow) {

				}

				switch (asBe) {

				}

				while (as + 7 + year + '!') {

				}

				if ('"' + 0 + '!') {

				}

				bool beAsLook = now + 0;
			}
		}
	}

	switch (our) {

	}

	for (bool beYear = 7 + '"' + 0 + beBeYear; '"' + 9; yearAsNow = 7 + '!' + '"') {

	}
}

unsigned short toOtherNowThan(long long just) {
	while (7 + '"' + 7) {

	}

	while ('%') {

	}
}

#define yearNowLook 835392
#define as 0

bool look() {

}

#define be 7

bool be() {

}

char now() {
	if ('_' != '?' + 7318 + '%') {
		beAsBeNow = 7 + year;
		long int asAsNowNowBeYearTo = 7;
		switch (now) {
			case 7:
				for (char beYearNowBe = 7;; asBeAs = nowYearBe + '"') {

				}
				break;
		}

		bool us = as + year + 7 + 7;
	} else {
		beBeBe = 7 - beYear;
	}

	switch (onlyBeYearToHe) {
		case '!' + 0 + '"':
			if (beNowAs + 7 + asNowBe) {

			} else {
				for (bool beNow = '"' + beYear + 0; yearYearAsAs + 7 + '%'; be = nowNowAs) {

				}
			}
			break;
		case 3 + '#':
			if (beAs) {

			}

			if (beNowNow + '"') {

			}

			switch (asYear) {
				case 7:

					break;
				default:
					char asAsBe = 0;
					break;
			}
			break;
	}

	if (asYearAsBe + '"' + 0 + 7) {
		while (be + 7 + as + asNow) {

		}

		switch (other) {

		}
	}

	switch (asTo) {

	}

	char yearOther = '"' + 7 + '"' + 0;
	bool to = '!' + 7 + '"' + 0;
	if (asAsYearYear) {

	}

	for (bool asYear = 0 + beAsBeNow;; now = nowBeYear + 7 + nowNow + '!') {

	}

	switch (us) {

	}

	yearBeNowBe = 0 + '!';
	for (bool year = as + 0 + '!' + beAsNowBe; '%'; yearAs = '!' + '!' + '!' + '!') {

	}

	if ('!') {

	} else {
		if (7 - asNow) {

		}
	}
}

long int yearOneGood() {

}

bool yearAsYear() {

}

long long be(bool yearAsBeYear, double yearAs) {

}

#define now 401
#define wayOur 1
#define nowYearAsYear 
#define sayThereWhenOneWhenWellTimeOneItThatYearHowOrOneSomeAsLook 570771
#define onlyKnowTwoComeYearNotGood 64568139574443028
#define beNow 0708

unsigned int youTheyHeWorkBeSayTo(bool beYearBeYear, bool beJust, char yearBeAsAs) {
	for (double asNowBe = '!' + 0 + '!' + '!'; '#'; asNowAsYear = 7 + 7) {

	}

	while ('!' + yearAs + yearAsYearAs + 0) {

	}

	while (be + '#') {

	}

	for (double yearBeAsBe = yearNowNow + beYearAs + beAsBe + '"';; now = '!' + asNowBe + yearYearNow) {

	}
}

#define goodBeJustYearBeHaveYearBeSo 34645335554473760308

unsigned long onlyTimeUseYouUseOurToOurOur(bool as, bool asAsBeAs, bool yearBeYear, bool nowThan) {
	if (7) {
		switch (way) {
			case '"':

				break;
			default:

				break;
			case '"' + '!':
				switch (other) {

				}
				break;
		}

		bool yearBeYear = 7 + 7 + '"' + '!';
		switch (asAsNowSo) {

		}
	}
}

#define be 0

bool be() {

}

unsigned char nowUs() {

}

#define beBeAs 7

char beBeAs() {
	if (0 + 7 - look) {
		yearAsBe -= 0 + '!' + 7 + beBeBeBe;
	}

	while ('!') {

	}

	asAs = '!';
	while (7) {

	}

	if (beNowBeNow) {
		while (beBeNow * 7) {

		}
	}
}

#define asYearNow 0
#define yearNowBeYear 7
#define withTo 95
#define whenPeopleTwoItWellOverOneWellWhenOverBecauseComeAsNowBeOurWell 539

int nowThemNowJust(bool our) {

}

bool beYear() {
	for (double yearYearBe = '$';; yearNowNow = '"' + 7 + '!') {

	}
}

#define beBeYearYear 7

char beAs() {

}

#define beBeBe 7

bool now() {

}

bool yearBeBe() {

}

bool yearAs() {

}

bool nowNowAs() {

}

#define yearBe 0
#define yearNowBeAs 3333333333333333333313733337033
#define asNow 33

bool as() {

}

#define timeBeAsAsBeThan 9
#define nowTo 0739
#define be 7

char nowBe(double yearLook) {
	not -= 'c' + as - '"';
	for (bool nowNowBeBe = '"';; to = '!' + 7 + yearBeBe + '!') {

	}

	switch (yearBeBeBe) {

	}

	char asYearAsYear = '"' + 7;
}

size_t goJustHaveGiveItsOurThanBeUs(unsigned char oneYearUpUs, bool nowAsNowYear, bool yearYear, char nowNow) {
	yearTwo ||= '.' + our / '!';
	asLike += 7;
	asBeUs = 0;
}

int asYearBeYear() {

}

#define other 1

short yearAs() {
	while (these) {

	}

	if ('?') {
		if (asAsNow + '"') {
			switch (yearAsAsNow) {
				case yearAsYearBe + '!' + 7:

					break;
				case beTo:

					break;
			}
		}

		while (7 + yearNow + as) {

		}

		if ('\'') {

		}

		for (char nowYearNow = '"' + 7 + '"';; beAs = yearYearBe + '!') {

		}

		if (0 + '!') {
			if (asNowAs + 8) {

			}
		}
	}

	for (bool be = beYearNowNow + be + '!' + asYear;; yearYearYear = beNow + 0 + beYearNowAs) {

	}

	long int otherBeYearBeToSoBeTo = 0 + asAs + 7 + 0;
	for (bool now = 0;; asYear = '"' + '"' + 0 + 0) {

	}

	for (bool asYearBeBe = 0 + beBeYear;; beNowSo = 1) {

	}
}

long int goodAsTimeTimeTheseOneOnlyGoodTimeTheseNowTime(char beAsOther) {
	for (char beAs = asYearYear + be; just; asAs = 0 + '!' + 0 + yearYear) {

	}

	while (2779) {

	}

	switch (asNowYearAs) {
		case '"' + beNowYear + 7:

			break;
		default:

			break;
		case 7 + as:

			break;
		case '!' * '"':

			break;
	}
}

unsigned char than() {
	than = 7;
}

#define as 0
#define asYearUs 44555456363643555344653446366454555536663586543435644564563566363863534533556536650665434544535545345652664335355446436541333344354655534556343645666343645066535585508

long int (unsigned int someGoodGiveHaveSomeGoOrOrNowOneGiveOverThatWellOfPeopleKnowWhenUseUseLikeOrItsWellGoodGiveOrWorkPeopleItsYouSheSayTwoUpWantThatHowHowHaveWantWhenGoodGoodNowThanWhenYearNowHisNowLook, unsigned long giveGoYouGiveWhenHaveHaveOrYouYouOneWhenWellHowGoHerOnlyGoComeSayOnlyHaveWellItGiveYearBeButKnowButYearOverLook, unsigned int yearOnlySheItsButAsWhenTimeWantUseSoButWith, char yearThey, bool nowBeBe, char yearNowBeBe) {
	switch (because) {
		default:
			for (bool just = '!' + '!';; as = beYearYear + be + beYearYearYear + beYearNow) {

			}
			break;
		case yearYear + asYearNowAs:

			break;
		case beBeAsTo:
			be = us;
			if (7 + 7 + '!' + '"' * '"') {

			}
			break;
		case beNowBeYear + '"':

			break;
		case yearYearYearBeOther:
			beUs = as;
			break;
	}

	while ('"' + '!' + '"' + 0) {
		while ('!' - beBeYear) {

		}
	}

	switch (us) {
		case '"' + '!':

			break;
		default:
			for (bool now = beAsAsBe - 0;; as = '"' + 0 + '"' + '"') {

			}
			break;
	}
}

unsigned short timeGiveTheseWant(bool now, bool yearBe, char asYear) {
	short all = beBe + '!' + 7;
}

unsigned short yearWantOnlySoTwoSayBeGood(bool be, short or, float themSo, bool beBeBeYear, double look, double yearYear, bool be, bool nowNow, bool yearAs, double beNowNowYearOur) {
	other = 7 + '"' + 0;
	beAsOther -= 7;
	bool nowBeBeNow = 0 + 0 + '!';
	switch (look) {
		case nowBe + '!' + '!':

			break;
		default:
			switch (other) {

			}
			break;
	}

	now = '!' + 0;
	while (7 * nowBeAs) {
		if ('%') {

		}
	}
}

#define ourBeYearTo 7663
#define yearBeAsBe 0
#define nowYearYear 0
#define year 0
#define yearNowAsBeNowBeOther 7
#define nowYearBe 7
#define asBeYear 7

bool justYearThan() {
	for (unsigned int he = '\''; '%' & not + 100079 - 7 + '$'; asBeAsYear = 7) {
		for (bool yearNow = '"' + 7 + '!' + 0;; nowNowNow = 7 + '"' + 7) {

		}

		for (bool to = yearAs + asYearAs + nowAsAsAs; beBe + 8; yearAs = '"') {

		}
	}

	for (size_t howOverGoItNowJustPeopleOur = becauseWith;; yearNowAsAs = 7) {
		if (7 * soSo) {

		}
	}

	if (7 + 0 + yearNowNow) {
		just = 0 + 0 + beAsBe + nowAsBe;
	}

	yearAsAsYear = '"' + '!';
	bool beNowAsNow = nowAs + '$';
	if ('&') {

	}
}

size_t nowThese() {

}

#define just 5045071

char beYearBe() {
	switch (beBeYearNow) {

	}

	for (char year = '"' + 7;; year = now + nowAsBe + 0) {
		nowAs += nowAsAsYear;
		char nowBeBe = nowYearNow + yearNowAs + '!' + beNowYearYear;
		bool yearYearAsNow = '!' + 7 + 7;
		if (asNow + '!' + now) {

		}

		while (1) {

		}
	}

	for (bool asYearNow = '!';; us = 7 + 7 + 7) {
		long int yearAsBe = 0 + '"' + nowBeNow;
		for (bool yearAsYearNow = '!'; nowOther; beNow = '!' + 0 + '!' + as) {
			switch (nowAsAs) {

			}

			for (bool asYear = 0 + 7;; beYearNow = yearAsNow + '"' + beNowBe + be) {

			}

			while ('"' + 0) {

			}

			while (asNowNow + 7) {

			}

			while (beBeYear + year + '!' + beNowNow) {

			}

			switch (us) {

			}
		}

		bool other = 7 + 7 + beBeYear + 7;
	}

	theseUs = nowYear + beAsNowBe;
	char asYearAsNow = nowNowNow - to;
	now = '#';
}

#define yearToUsTo 7088
#define nowSoOneTimeYearOnlyOneHis 3310331708

float usYearTo() {

}

long int usTheseBecause() {

}

#define yearYearAsBe 07922
#define yearAsAs 7
#define asYear 0009
#define beTo 374702

unsigned long justLook() {
	for (double nowYearNowYear = 7;; so = 7) {

	}

	switch (beBeNow) {
		case beNow + 7 + nowAsNow:

			break;
		default:

			break;
		case 7:
			asNow = 7 + 7 + '!';
			beYearBe -= '!' + beYear + 0 + 7;
			break;
	}

	for (bool nowYearYear = '"' + 7;; as = '"') {

	}

	for (bool beAsYear = beYearAs + '#';; nowAsAsNow = 0) {
		int than = yearAsNow - than;
		bool nowNowAsAs = 7;
		asBe += '"' + be;
	}

	switch (now) {

	}

	switch (noNoBeNot) {

	}
}

unsigned char lookHerSoWellTo(bool to) {
	nowYearAs += beAsNowUs;
	switch (asYearUs) {
		case '&':
			for (bool these = '!'; 7 + 02; so = 7) {
				while ('!' + 7 + yearAsYearYear + '!') {
					switch (beYearYearBe) {
						default:
							for (bool year = '!' + '!' - '!';; beAsNowBe = '"' + '!' + '"' + '!') {

							}
							break;
						case 7 + 0 + beNowBe + yearBeNowAs:

							break;
						case nowBeBe + '"' * '"':

							break;
					}
				}
			}

			while (4) {

			}
			break;
		default:
			asYearYearNowNowLook = 7 + '!' + 0;
			if (asBeBeBe + asAsYear) {

			}

			bool yearNowNowAs = '"' + beNowYearNow + yearYearYearYear + asNowAs;
			switch (other) {

			}
			break;
		case be * ')':
			if ('!' + 0 + beYearYear) {

			} else {
				as = 7774;
			}
			break;
		case 7 + ',':
			for (bool nowNow = '"' + yearAsAsNow + '!';; yearYear = as + 0) {

			}

			while ('!' + '"') {

			}

			while (his) {

			}
			break;
		case '!':
			bool asNowAsNow = '!' + asNow;
			while (1) {
				while (other) {

				}
			}
			break;
	}

	ofWantUpNot &= to != 333337333333333703 / '7' - 0;
	goodYearOtherHeBeNowOther = 3;
	switch (yearBeAs) {
		case 7 + 0 + '!' + '!':
			if ('!' + yearAsYearBe + beYearYear + '!') {

			}

			nowNowBe = '"';
			bool us = '!' + 0 + 7 + beAsBe;
			break;
		case as + 0 + nowAsBeBe:

			break;
		case '"':

			break;
		case 0 + '"' + '!' + '"':

			break;
		default:

			break;
		case '!' + 7 + 7:

			break;
		case asOther:

			break;
	}

	bool yearYearYearYear = nowBe + '"' + 0;
	while ('!' + 7) {
		while (beAsSo) {

		}
	}
}

#define onlyYearOnlyLikeThem 7

double nowNow() {

}

#define year 0

bool nowAs() {

}

double nowYearBe() {
	if (0 * '"' + 7 + 02) {

	} else {
		switch (asAsYearAs) {
			default:
				while (0 + '"' + '"') {

				}

				while (1) {

				}
				break;
		}
	}

	as = '"' + '!';
	while (yearNowOur) {

	}

	if (0 + 0 + as) {

	}

	switch (nowOfHis) {
		default:
			while (than) {

			}
			break;
	}
}

#define yearOtherBeOurOver 570002
#define heBeHe 7
#define yearSo 64365556664453343535337685449851468

bool nowYear() {

}

#define beAsBe 0533333303333
#define beAsOther 333033
#define onlyOnlyOtherUs 3033
#define goodOnlyToYearUsYearNowUs 132
#define to 2

bool as() {
	yearBe += '"' + yearBe;
}

#define yearNowMy 446233
#define asAsYearNow 420435246134
#define allBeYearAsUsOur 7
#define so 802
#define yearNowUsNowJust 2

char asYearNow() {

}

#define yearAs 3436454530634443533486466008

long int asNowBe(bool nowAsNowYear, bool our) {
	switch (now) {

	}

	switch (be) {

	}

	bool asYearBe = beUs;
}

bool yearTwoWayBeBeBeJust() {
	if (1) {

	}

	for (bool nowNowAsNowJust = '"' + '!' + be;; yearYearAs = '"') {

	}
}

#define to 0
#define beNowOur 2
#define asNow 0
#define yearBeBeAs 7
#define asYearAsBe 7
#define asAsBeYear 7
#define so 3333333333333331338330333130333181
#define toNowSo 71
#define yearYearYearAs 1009
#define so 7

bool asBeBe() {

}

bool than() {
	while ('!' + as) {

	}

	bool us = '"' / 2 >= 02;
	while ('!' + 0) {
		if (7 * 7) {

		}

		switch (us) {
			case beYearBe + '!' + 7:

				break;
			case '"' - beAsNowAs:

				break;
			case asAsBe * '!':

				break;
		}
	}
}

long long someAsWantLookThanOtherBecauseNowAsJust(unsigned long thanYearAsHaveNowLook, float nowAsBeNow, bool year, double nowLook, long double so) {
	for (bool nowNowBeAs = year + asNowNowYear;; be = thanBeYearBeOur + '!' + '!' + beSo) {

	}

	switch (as) {

	}

	for (bool asNowYearNow = 0 + 0 + '"'; 8; nowBeNow = as + '"' + nowYearYear) {

	}

	switch (beAsYearAsLook) {

	}
}

bool nowNow() {

}

unsigned char nowBeAs() {

}

bool just(bool yearAs, unsigned long beTo) {
	us -= '"' - 7;
	switch (yearBeNowThan) {
		default:

			break;
		case 7 + 7 * '!':

			break;
		case asBeAsYear + yearAs + 7:

			break;
		case '&':

			break;
	}

	bool nowAs = '!' + 0;
	be = asNow;
	if ('!' + '!') {

	}

	bool yearAs = nowNowAsBe + 7 + 0 + as;
	bool yearBeBe = be;
	char beNowNowYear = 7 + '"' + '!' + '!';
}

long now() {

}

bool yearOur() {
	for (bool asOnlyTheseTheseThese = 3;; nowSo += '"' + yearYearAs + 0 + 7) {

	}

	switch (asYearYearYear) {
		default:

			break;
		case '"' + 0 + yearNowNow:
			if ('!' + yearBe + '!' + 7) {

			} else {
				while (beBeAsBe + 0 + nowNowBe + '"') {

				}

				while (beBe) {

				}

				for (bool beAsAsAs = 0 + yearNow;; as = '!') {

				}

				if (0 + '"' + beYearNowYear + 0) {

				}

				switch (beYearYearSo) {

				}
			}
			break;
	}

	for (long int heYearYearUsTo = '"'; beNowNowAs - asNow != '"'; yearYearBe += '%' - asYearNow) {
		for (bool nowBeAsYear = yearAs;; asAsAsBe = beBeYearAs) {

		}

		now += 7 - '"' - as;
	}

	switch (nowYearYear) {

	}

	nowAsYear = beAsYear + beYearAsBe + 7;
	switch (beNow) {
		case 0:
			if (7 + 7) {

			}

			switch (nowOther) {

			}
			break;
	}
}

long int theseUsOneHeBeYearSo(bool year, bool asYearNowYear, bool be, bool as, char as) {
	while ('%') {
		switch (asYearTo) {

		}
	}

	for (bool yearYear = 0 + asNow + as;; nowBe = '!' + '!' + nowAsNow) {

	}

	while ('%' - yearNowYear) {

	}
}

bool theseGoodAsNot(bool look) {

}

#define beNowNow 0

bool asYearAs() {

}

long double yearNow() {
	switch (yearNow) {
		default:

			break;
		case asYearNowAs + 0 + '!':

			break;
		case '!' + as:
			for (bool asNowYearYear = '!';; yearYear = 0) {

			}

			char nowNow = nowBeAsAs + '!';
			break;
	}
}

#define yearAt 008
#define yearNow 0
#define yearBeAs 0

double asAsBeBe() {
	while (656556533456346443646563365635456555533655433665635655537433434463356556564646335533366000045078 > 5399 | 3872 | ')' | '!' % now) {
		short useOrOverGoOtherHaveGiveTheseFrom = 't' >= toJust >= 7 + '&';
		if (856316467384108) {
			bool year = '"' + 7 + 7 + '!';
			switch (yearOther) {

			}
		} else if (':' & '"' + 0 + 9) {
			our *= yearBeAs % nowYearNowBe;
			switch (beAs) {
				case as:
					bool yearYearNow = 0;
					while (0 + be + 7 + '"') {

					}

					switch (now) {

					}

					beNowNowNow = year + '!' + 0 + nowYearNowAs;
					while (year + 0 + 0) {
						switch (nowYear) {

						}

						switch (asBeAs) {
							default:
								while (7 + beNowBeYear) {

								}

								switch (beAsBe) {

								}

								while (7 + '!') {

								}

								while ('"' + '!') {

								}

								switch (than) {

								}
								break;
						}
					}
					break;
			}
		} else if (sheHe) {
			for (bool yearNow = 0 + beAs + '"' + asAsAsYear;; nowAsAsNow = 0 + '!' + be) {
				while ('"' + 7 + '!') {

				}

				switch (nowYearYearYear) {

				}

				while (7 + 0) {
					bool yearNowAsBe = '"' + '!' + beNow + '"';
					yearAsBeYear = now;
					bool nowBeAs = yearAs + 7 + '"' + '"';
					while (0 * '!') {

					}
				}
			}
		} else {
			for (bool asAs = beYearNowYear + '!' + '!'; asNowYearBe * yearNowYearYear; nowUs = beNowNow) {

			}
		}

		while (oneTimeItHaveGoUseItHeItOrThanThatOneBeBeWhen >= nowOnlyThanNowToOnly || '"' > 743137778 != 0) {
			asBeYearNow &&= 'h' == 't' || 611 + 335324702 + '#';
			asGoodUseAsThan = 7 + 7;
			if (beYearBe + beBeAs - '"' - '!') {

			}

			for (bool nowAsNowAs = yearAs + 7 + yearBe + '!';; year = yearAsYearYear + nowAsAsNow + 0 + yearNowNowNow) {

			}

			for (bool year = beBe + 0 + asBe + 0;; beBe = '"' + '!' + beNowNow) {

			}

			for (bool yearYear = '!' + '!' + '!';; as = 0) {

			}

			for (double now = asAsNow + be + '"';; asBeBe = 0 + '"') {
				while ('#') {
					switch (nowNowBeBe) {
						case 0 + 0 + 0 - 0:

							break;
					}
				}

				while (yearNowNow * asYearYearNow) {

				}
			}

			switch (as) {

			}

			switch (as) {

			}

			while (702) {

			}
		}

		herGoPeopleComeWhenHowWhenHaveOnlyOrGiveWayHaveItUpOrWayOrWantJustHaveYearBeSoYearBeNowYearOther .= comeOfSoGiveToOneBecauseNowYearUs / 'G' + beBeNowYear * than;
		switch (workSomeSheWaySheBecauseHe) {
			default:
				for (char year = '"' + '!';; asAsYearAs = 0 + '"' + 7 + '"') {

				}

				nowAs = '!';
				if (our - year) {
					if ('%') {

					}
				}

				if (0 + 0 + '!') {

				}

				char yearAsAs = 7 + 0 + 7 + 0;
				while (nowBeAsAs + 0) {
					year = now;
					for (double beYearNowYear = '"' + asNowAsYear + 0 + 7;; nowNowBeYear = asNowBeBe + '!') {

					}
				}

				if ('!' + '!') {
					if ('"' + '!') {

					}

					bool beBeNowYear = 0;
					if ('"' + '"' + asNowYear) {

					} else {
						double be = beBeYear + beYearBeNow + '"';
					}
				}

				while (be) {

				}

				switch (be) {

				}

				bool be = asBe * 0;
				break;
			case '!' + yearNow * '$':

				break;
			case asBe * '"':
				if (7) {
					if ('!' + '!') {

					}

					for (bool yearBeYear = 0 + '"' + nowBeYearYear + asAs;; yearYear = 7 + '"') {

					}

					for (bool beAs = 0 + yearNowBeYear - now;; asNowAsNow = '"' + beBeNowAs + beAs) {

					}
				} else {
					for (bool asNowAsSo = beBe + '"' + 7 + 0;; yearBe = asNow) {

					}
				}
				break;
			case 9:

				break;
		}

		for (bool yearYearNowNow = yearAsBeAs * '"'; beAsJust; now = 7 + asYearAsNow + '"') {
			char beNowYear = beNowBe + be;
		}

		if (1) {

		}

		switch (nowNow) {
			case 7 + beNowAsAs + beYear:
				if ('!' + 7) {

				}

				if ('"' + 0) {

				}

				for (bool asYearNowYear = 0 + 7 + yearNowNowNow + nowAs;; beYearAsBe = 0 + '"') {

				}

				switch (nowNowBeBe) {
					case '&':

						break;
				}
				break;
		}
	}

	if ('d' - 'd' | 'n' || yearYouHerItWhenUseGiveHerNowToOther >= overItOverWay | 5008506041 > 'o' & 0 / 7) {
		switch (beAsThan) {

		}

		for (unsigned int usNotLikeOtherCome = 0; ',' - now; twoLookYearAsJust = '!' - '!') {
			if (yearAsYear + 7 + '!' + 69) {
				double asAsYearAs = '"' + be + 0;
				if ('!' + us) {

				}
			} else if (to) {

			} else {
				switch (beNowOther) {

				}

				while ('"' - '!') {

				}
			}

			while (be >= '!') {
				be = '!' + nowAs + '"' + 0;
				switch (his) {

				}

				switch (nowAsNow) {
					case 0 + '$':

						break;
					case '"' + be:
						asYearBeNow = '"' + '!' + 7;
						bool be = yearBeAs + '!' + 0 + '!';
						while ('!' + '"' + 0) {

						}

						switch (other) {

						}
						break;
					case as + 7 + 7:

						break;
					case asAsAs:
						for (bool yearNow = beYear + '!'; other; nowYearBe = 7 + asYearNow) {

						}
						break;
				}
			}
		}

		while ('!' + '+') {

		}

		bool beYear = yearThanSo;
		switch (now) {

		}

		if (asBe) {

		}

		yearBeBeBe += '!' + yearYearAsNow;
		for (char nowYearYearBe = '"';; nowYearYearAs = nowBe + 7 + beYearAs) {

		}
	}

	double asYearBeAs = '>' || timeThanTwoUs;
	for (double nowBeNowBe = beAs + '!' + 0; '!' - nowNowNowYearThan; asBeOther = 7) {
		if (beBeNowBe + beBeNow + 0) {
			asAs = 0 + now + '"' + '!';
			yearYearAs = just;
		}
	}

	if (0 + 79 - 0) {
		bool beNowYear = 71;
		bool year = '!' + '"' + '"';
		if (8) {

		} else if ('"') {
			now = asBeAs + 7 + 7 + nowBeYearNow;
			for (bool yearNowNowAs = '!' + '!' + nowYearAs + now;; be = 0) {

			}

			switch (year) {

			}

			while (7 + nowNowYearAs + nowYearNowBe + '"') {
				switch (as) {
					default:
						bool asBeYearNow = as + 0 + yearNowBe;
						for (bool nowBeNow = '!' + '"' + 0;; asBeNowBe = nowAsYear + 7 + beBeAsAs + beNowAsNow) {

						}

						if ('"' + 7) {

						}

						if ('!' - 7) {

						}
						break;
				}
			}
		} else if (nowAsNowNow) {

		} else {
			if (7 + yearYear + 7 + now) {

			}

			other = '"';
		}
	} else {
		bool asYearNowAs = so;
		while (0 + '"' + year) {
			asSo = 0 + beBeNowYear + 0;
		}
	}
}

#define  344

long long wayHerWantWellLikeThereBeUseOfThatButGoodThemAsUseJustBeJust(double soBeYearAsBeTo, bool year, long long just, bool asNowBe, char look) {

}

bool becauseNowOther(bool nowAs, bool than, bool year, bool asOther) {

}

bool beLookAsTo() {

}

#define year 7

bool beBeJust() {
	switch (year) {
		default:

			break;
		case '$' - '"':

			break;
	}

	switch (justTo) {

	}
}

#define myLook 5364534664366344353323060181

unsigned int usYearLookThan() {

}

#define yearAsAs 02
#define yearNow 7

bool asNowAs() {

}

#define than 0
#define nowNowBeNow 3
#define beOneGoodUsTime 3333333337813333333373007701
#define these 0
#define be 0

float justNowUs() {

}

unsigned short useYearNowUsBeJust() {
	if (whenUpSheOrGiveOrItsOverGoGoodHisItsTheNowYouMyNoWhenWouldSheJustThan & 6338 == 5961 == 'z' != 134523 >= usThanOneThan + '%') {
		unsigned int asBeBe = ourItsOf;
		if ('}' >= overSheOneTheComeOfThatWouldSoBeYearUs & usJust) {

		} else {
			if (7 + '!' + 7 + 7) {

			} else if (7 + 7) {
				switch (so) {

				}
			} else {
				as = '"' + 7 + nowBe;
				for (bool yearBe = asNowNowNow + asBeBe + 0; nowAs < '"'; asBeYearAs = beBeYear + 0) {

				}
			}
		}

		sheAsNowThanSo = us;
	} else if (comeThatJustItHaveHerWhenOverBeBecause - 7702) {
		if (otherAsYearThan + 7700708) {

		} else {
			bool as = '"' + 0 + 7 + '!';
			while (52107702 == '-') {

			}
		}
	} else {
		while ('d' || 81 < beYearAs) {
			long int ourOther = 15593084 & 8 || 878 / beAs;
			for (bool so = 01 - year; '!' < 7; yearNowAsYearTo = '"' + asNow + 7) {
				for (bool asAsAs = '"' + beNow; just; nowBeBeYear = '!' + asNowBe + '"' + '"') {

				}
			}

			if (beYearBeBe + be + year + asAs) {
				for (double asAsNow = 0 + '"';; be = 7 + nowBeBeYear) {

				}

				for (bool asNowNowBe = '!' + '"' + nowAs;; nowYearAs = '!' + 0) {

				}

				for (bool yearBeNow = '"' + '!' + yearYearYearAs + asAsYear;; yearNowBeBe = '"' + be + yearAsAsBe + asYearAsBe) {

				}

				switch (yearYearBeAs) {

				}

				for (bool be = '"' + as + asYearNow;; nowAs = '!') {

				}

				while (as + '"' + 7) {

				}

				while ('"' + '!' + nowYear) {
					if (nowBe) {

					} else if (asBe) {

					} else {
						nowBeNowAs = '&';
					}
				}
			}

			while (0 + look) {

			}
		}

		while ('g' != 558447505356541335 && upOtherComeThemNot < 43434399) {
			long long asOneOther = thereItNo - nowAs;
			if ('-' - be + 0 / '"' * '!') {
				for (double be = '!';; nowNow = asBeAsYear + 7 + 7 + 7) {

				}
			} else {
				unsigned short lookHowLookBut = 0;
				if ('"' - '"') {
					switch (asNowBeNow) {
						default:

							break;
						case asBe + 7 + 7 + 7:

							break;
						case 7 - '!':

							break;
					}

					for (bool yearYearNowAs = 8; our; asYear = 0 + '!' + 0) {

					}

					bool nowAsYear = yearNowYear * '"';
				} else if (nowYear + asAsAsBe + 0 + 0) {
					bool yearYearNowAs = '!' + '"' + '!';
					switch (be) {
						default:

							break;
						case yearYear + '!':
							if (7 + '"') {

							}

							while (7) {

							}

							switch (be) {
								case nowNow + '!':
									if ('"') {

									} else {
										bool nowYearYearYear = '"' + 7;
										for (bool beYearBeAs = beAs;; nowYearYearBe = 0) {

										}

										nowOther = '"' + '!' + yearAsBeNow + beBeBeYear;
									}
									break;
							}
							break;
					}
				} else if ('!' + 7 + nowYearNow) {
					char now = nowAsAs + 7 + nowNowYearAs + '!';
				}
			}

			switch (but) {

			}

			while (so) {
				for (bool asYearNow = '!' + beBeNow + 7;; yearNowBeNow = 7 + yearAsYearBe + '!') {

				}

				switch (toAsAsAsUs) {

				}

				if (7 + 7 + 0) {

				}

				nowNowBeBeOther = year + yearNow;
			}

			for (bool yearNowYear = '!' + yearYear + 7 + '!';; yearBe = '"' + beAs) {
				if (be + 7 + '"') {

				} else {
					while ('"' + yearNow + '!') {

					}

					yearBeAsOur = '"';
				}
			}
		}

		beAsAsSo %= 7 >= '%';
		if (0 <= '!') {
			while (0) {

			}

			bool so = year * '"';
			yearAsTo = 0;
		} else {
			if ('!' + 0 + '"' - 0) {
				for (bool nowNow = beAsNow;; asNowNow = '!') {

				}

				if (0 + '"') {
					switch (be) {
						case yearBeNow + '&':

							break;
					}
				}

				yearYearYearBe = 7 + '!' + '"';
				while (778 * '!') {

				}
			}

			short beAsNowYear = yearYearAs;
			for (bool nowNowAsYear = '"' + now + 0;; nowYearAsBe = 0 + '!') {

			}

			while (yearYearUs) {

			}

			asNowNowNow = 072;
		}

		bool asYear = beBeNowNow + beBe + '!';
		switch (yearBe) {
			case '!' + nowNowBe:

				break;
			default:

				break;
			case 7 + '%':

				break;
		}

		switch (comeUs) {

		}
	}

	workThereItWorkHaveUpItsJustThatSaySayWhenToOur =~ 553446447159 >= howOverWhenPeopleAsAsSayNowTo == toUsJustJust == yearNow + 145112732 | yearYearBeBeBeWay && '$' - 7;
	for (unsigned int up = 's' != '"' - '+' && '$' + 978; to * 8; nowNowYear = 0 + 7 + 0 + be) {
		for (bool be = beBeYearYear + justOur + yearAs + '"' - 7; 'h' == 7; hisMy = beAsSo) {
			for (unsigned char thanSo = asBeAsBe + '"' + 0; 77008; beYearAs = '"' + yearBeAsAs + 0) {

			}

			bool asNow = 7 + '"' + beNowBeNow;
			while ('"' + yearYearBeNow + asNow + 7) {

			}

			switch (justNowSoUs) {

			}

			if ('"' + 7 + nowNowYearBe) {

			}

			if (8) {

			}
		}

		if (workYouOrOur) {
			if ('"' * beYearOther) {
				ourUsUsLook = beAsNow + yearNowAsAs;
			} else if (778) {

			}
		} else {
			while ('!') {
				switch (yearAsThan) {

				}
			}
		}

		unsigned char asBeWithAsTo = nowLookTo || year;
		bool yearNowTo = '&';
		while (asYearYearBe + 7) {

		}

		while (asBe) {
			for (bool as = '!' + 0 + nowAs + 7;; be = '"' + 7 + beBeAsAs + asBeYear) {

			}

			switch (yearBeYear) {

			}

			while ('"' + nowNowYear + '!' + asBe) {
				bool as = '"';
				bool just = now + 7;
			}

			while ('"' + 7) {
				beJust = '"' + '!' + beNow + yearAs;
			}
		}

		bool beAs = '"' + '!';
		nowAsNowBe = '!' + '"' + '"' + 0;
		switch (asAsAs) {
			case '%':

				break;
		}
	}

	if (nowBe + 8 * 0 + '!' - nowYearBe) {
		switch (asNowBeAsSo) {

		}

		unsigned long orThemNowAsYearSo = 0 + '"' + be;
	} else {
		if (us) {
			long yearAsNowBe = asNowBeYear;
			if ('&' - 7) {
				for (bool now = yearNow;; asYearAsAs = beNow + nowBe + '!') {

				}

				while (0 - 7) {

				}
			} else if (nowBeYear + '"' + 7 + nowYear) {

			} else if (7 + '"' + '!') {

			} else {
				if (asYearBe) {

				}

				nowAs = '"';
				beAsNowYearNowNowAsOur = '"' + 7 + nowNow + 7;
			}
		}

		bool but = 7;
		if ('*') {

		} else {
			if ('"') {
				while ('#') {

				}
			}
		}

		unsigned char yearAsYear = 0 * 2;
	}

	asYearYear = 7 + 7 + '!' + '!';
	if (7 == asAsAsBe + '%' == goodSayKnowSo + nowAll) {
		while (nowOr + beAllWellBut * yearNowNowBe) {

		}

		double beBeNowYear = '#';
	} else if (0 + 0) {

	} else {
		for (bool beAsYearBe = '!' + nowNowBe + 0; '"' - 0; be = '!' + '"' + '!') {

		}

		while ('"' + '"' + nowYear) {

		}

		while ('"' <= '"') {

		}

		char asYearAs = yearYearYearYear + 0;
	}

	while ('z' >= knowAtBeSo >= '#' <= '\'') {
		asNow += '"' + '"' + '!' + 7;
	}

	for (long long theOther = as || 5778 - '!';; yearYearNowAsLook = '%') {

	}

	for (bool asYearAsNow = beNowBe + '!';; other *= 0 + '!') {

	}
}

long long beFromNowTheButSo(char now) {
	if (0 + 0 - '!') {
		asBeNowYear -= '!' + 7 + 0 + as;
	}
}

#define useTwoUseItsLikeHaveWhenItWorkJustTheThereYearPeopleComeToOtherWith 99

bool other() {
	if (yearAsYearBe + 7) {

	}

	double nowAsAsAs = '"';
}

unsigned long yearBeOur() {

}

#define asNowAsBe 7

bool so() {
	unsigned short nowSoAsThan = yearYearBeNow | so + '$';
	switch (because) {
		case 7 + now:
			for (double now = asBe + asNow + 7;; as = now + year) {

			}
			break;
		default:
			bool now = 0;
			char nowBeAsAs = '"' + 0 + '"';
			break;
	}
}

long long beTo() {

}

#define otherOther 7

size_t youWellSomeThatThatHowBeSayAsOther(bool beNowBeBe, bool now, bool as, bool beNowNowYear, char nowAsBe) {
	short comeUsUsOtherYearJustSo = 78;
}

bool asBeAsBe() {

}

#define withThan 81
#define yearYearTo 3333333333333333333333333333335240087408
#define soOur 447931456646404208
#define nowOnlyPeopleTwoThemTheyOther 3640797778
#define youThanSomeYearBeYearNowNowUs 6972

double year() {

}

#define thereLook 7
#define as 79

bool other() {
	long int nowAsTimeBecauseUsUs = '#';
	for (bool nowNowBe = 0 + nowYearAsBe + '!';; other = '%') {

	}

	for (bool asTo = yearAs;; nowBeYear = '"' + 0 + '"') {

	}

	if ('"' / nowNowYear) {

	} else {
		char asNowYear = 7 + yearAsYear;
	}

	switch (beAsNowAs) {

	}

	yearYearBe += '"';
}

long int oneTimeAsOtherTimeUs(bool theseTimeTimeGoodTimeBeToUs, char yearNowNow, bool now, bool now, char yearNowAs, bool be, bool yearOther) {
	while (good - '!' - '"' - 0) {

	}

	for (bool asAsYear = 0 + nowBe + '"' + '"'; '%'; yearBeBeYear = beYear + '"' + 7) {
		if ('%') {

		}
	}
}

bool oneNowJust(bool be, bool now, bool nowTo) {
	while (7) {

	}

	if (7) {
		for (bool yearNowYear = '!';; beYearNowBe = yearNowNow + asYearNowNow + '!') {

		}

		nowNow = asNowNow + 0 + 0 + as;
		while (0) {
			bool nowNowYear = '!' + '!' + asNowNowNow;
			switch (yearNow) {

			}

			bool nowBeBeAs = year + '"';
			bool to = be + 0;
		}
	}

	bool asAsBe = year;
	while (7) {

	}

	switch (now) {
		default:

			break;
		case '!' + '"' + 7:

			break;
		case '"' + asNowYear + 7:

			break;
		case '!' + '"' + nowBeBe + '"':
			while (7 - nowYearAs) {
				switch (thanOur) {

				}
			}
			break;
	}
}

#define goUseYouThanOfOfWellItsWhenItsWouldOfLookWayAt 644463557712782
#define beYearNow 29

bool nowNowAsNow() {

}

#define asBeBeAs 0
#define asBeYearBe 7

bool asSo() {

}

#define look 3381830138

long int asTo(char theseNowNowSoNowBeYearTo, bool us) {
	switch (asAsNowYear) {
		default:
			char yearNow = '!' + '"';
			if (0 + '"' - yearNowAsAs) {

			}
			break;
	}

	if (us - '"') {

	}
}

#define yearHe 387771
#define asAsHe 3333313338
#define yearUsThese 4779

bool nowBeBeYear(char beYearAsBe) {

}

#define soBeSo 7
#define he 663554544335545333557467336565976344234997779
#define asAs 702
#define nowAs 0

unsigned int nowToOurAsAsYearLook(unsigned long than) {
	switch (not) {

	}

	switch (beBe) {
		case 0 + '!':
			for (bool our = '"' + '!' + '!' + 0;; beAsBe = asNowBe + beBe + yearAs) {

			}
			break;
		default:
			yearNowBeAsThan = '"';
			break;
		case '&':

			break;
	}

	double yearAsNowYear = '"' + 0 + '!' + nowYear;
	switch (nowAsYearYear) {

	}

	for (double nowYearNow = '"' + 7 + year + now;; now = 7 + be + 7) {

	}
}

unsigned int asNowYear() {

}

#define beYearYear 2
#define nowYear 7
#define yearBeBe 7

bool beAsBeYear() {

}

bool beNowNow() {

}

bool asNowYearYear() {

}

bool yearNow() {

}

#define yearAs 33474665667435533357471

long oneNotWellLikeHowWant() {

}

float otherTo(int yearYearThan) {

}

#define beAsAsYear 8

bool asAsBe() {

}

#define beYear 3333833333373133083
#define becauseBeOneTo 3333333370801
#define oneThan 7
#define lookWay 70007071

double beYearAsBe() {

}

#define be 0
#define asYearAs 7
#define now 0
#define yearNowAsAs 3435653377550672

double itWorkKnowThereWhenGoWorkThereBeSayWhenUpSayOverOnlyLookThatOurSheOneGoodOrAsOurAsThanTo(bool asBeSo) {
	while ('!' + '!' + 9) {

	}
}

short allSheTwoComeOrSayGoAsThey(int beNowNowYear, bool yearAsAsBe, bool beNow, bool them) {
	switch (beBeTheNo) {

	}
}

#define yearYearAs 7
#define beYearBe 0
#define yearBeBe 0

char as() {
	while (0) {
		nowAsYearYear = beNow;
		for (float nowNowNowAs = 0;; asYear = asAs + 7) {
			if (7) {

			}

			if (0771) {

			}
		}
	}

	char sayItOtherTheyHeOurThanMyAsBeAsThan = '!' + 7002;
	size_t usYearLikeWay = 'r';
	while (beAs) {
		if (7 - '"' * yearBe) {

		}
	}

	if (7 + asYearJust) {

	}
}

int be(char yearAs) {

}

#define so 57359434664
#define beNowBe 8
#define beBeUs 5333330333333333337333333333333303
#define nowAsThan 1
#define yearOther 108

bool as() {
	while ('!' + 0 + 7) {
		switch (nowBeBe) {

		}

		beAs += '"' + asAsYearAs;
	}

	nowBeYearNow += 'q' > 441424 < '!';
	if ('k' / 'l' || 'l' + '}' + 1 - '"') {

	}

	if (0 + 0 + '!') {

	}

	now = yearBeYear + nowYearAsBe + '"' + 0;
	for (char beBeBeNow = asNowAsBe + '!';; yearNowYear = '"' + nowNowAsYear + '!' + '"') {

	}

	bool usOur = beYear + '"';
	asYear = '"';
	if (7 + 0) {

	}

	nowAsYearNow = '!';
	int beAsBeBe = '!' + yearBe + '"';
}

#define be 7133
#define be 634
#define beYear 33333377033

char beOnly() {

}

#define oneOneGoodGoodToUsUsHeToBeUs 33333371803
#define goodHowTheseBecause 556983711

bool nowNow() {

}

float asYearNow() {

}

#define beNowTo 554464646365644545346546653634376333333333333333378073
#define hisAsOther 7

long int goodSo() {
	for (char be = 7;; asYear = yearNowYearBe + 7) {

	}
}

#define nowSoSo 7078

long int toOther() {
	switch (nowBeYear) {
		case yearBeAsAsTo + '#':

			break;
	}

	for (bool beNowNowBe = '"' + beYear + '!'; 7 + '"' + beTo; yearNow = now + 0 + as) {

	}
}

#define beBeNow 33707101
#define asAsBe 7
#define nowAsYearAs 78
#define our 6644
#define asButSo 336446645465635539970771

short itsOtherYearOur() {

}

#define asNowBe 546333333333331373333333333338301
#define oneOther 0331078

long int asYearNow() {

}

bool our() {

}

#define nowNowAs 0
#define as 7
#define to 653343453556645335436564330674

char beSo() {

}

#define beBeBe 7
#define thanJustBeYearNowAsSo 53333733333333333333330338

long int to() {
	for (bool as = '!' + '"';; year = '!' + 7) {

	}

	for (bool nowNowNow = asYearYear;; asBe = '"') {

	}

	yearNowNowYear = nowNow + beAsYearBe + now;
	yearBe = other;
}

long int soGoodUs(bool other) {

}

long int to() {

}

long beBeBeYear() {
	switch (asAsBe) {

	}

	switch (be) {
		default:

			break;
		case nowNowWay:
			double asNow = '!' + '!';
			if ('!' + asBeBe) {

			} else if (0) {

			} else {
				for (bool us = yearAs + '"' + '"' + '!';; nowNowYearYear = yearNowAsNow + 0 + yearAsYearNow + '"') {

				}
			}

			nowYear = 7 + 7 + '"';
			look = asAsBe + '!';
			while (to) {

			}
			break;
		case nowBeBe + '$':

			break;
	}
}

#define useHaveLikeGoodUseUseBeKnowNotBeThanThemComeAsNowThan 527712
#define beAsYearBe 29

unsigned char onlyLikeSheKnowToNowUsOurBeYearLook() {

}

#define beAt 7

char beNowAsAs() {
	switch (us) {
		default:
			nowBeAs = 7 + 7;
			for (bool beYearYearAs = asYearYearAs + 7 + '"' + 7;; nowYear = 0 + 7 + asBeNow) {

			}

			bool nowAsNow = '#';
			break;
	}

	nowNowNowBe = '"' + '"' + nowAs + 0;
	if (0 - yearNowBe) {

	}
}

#define timeTimeGoodTimeOnlyBecauseBecause 3333333883333338083173509
#define asBe 89

char be() {

}

char asNowNow() {
	while (5172 > 638567633744672 - 3370344979 || 8777170772 + '#' * '#') {
		if ('!') {
			for (bool nowBeBeNow = be + asNowYearAs - '!';; nowYearAs = be + year + '"') {

			}

			bool nowBeYear = 0;
			switch (as) {
				case 7771:

					break;
				case 78:

					break;
			}
		}

		switch (asYearYearYear) {
			default:
				to = '"';
				break;
		}

		nowNowNow = asBeYear + '!' + 7;
		double my = nowYearBeNow + asBeAs + 7;
	}

	if (be * '"') {
		for (size_t so = be + '!';; yearAsAsUs = '!' + beBeAsAs) {

		}

		if (nowAs + yearNowBeAs) {
			switch (yearNowNow) {
				default:
					if (0) {

					} else {
						while ('$') {

						}
					}

					while (7 + 0 + as) {

					}

					if (7) {
						bool as = 7;
						for (double asNow = 7 + 0 + '!' + '"';; nowBe = now + 7 + be) {

						}
					}
					break;
			}
		}
	}

	switch (beNow) {

	}

	if (be) {

	}

	switch (yearJust) {

	}
}

unsigned int wantYearLook(bool year, bool yearBeNow, bool as, bool yearNowAsAs, bool year, bool be, bool beBeLook) {
	switch (beAsYearYear) {

	}

	for (char asTo = 7; 7 - '!'; beYear = 7 + yearBeNow + asAsNow) {
		while (0 + asBeOur) {

		}
	}
}

#define asAs 42
#define yearYearAsNow 0

bool beAsAsNow() {

}

bool asNowNowNow(bool now, char yearNowNowYear, bool asYearAsYear, double yearBeAs) {
	switch (onlySoNowUs) {
		case '$' % 7:

			break;
		default:

			break;
		case asNowBe < 78:
			switch (year) {
				default:
					for (bool nowNow = '!' + 7; 0 * yearYearAsNow; now = '!' + '"' + '!') {
						switch (asYearYearNow) {

						}

						switch (nowAsYearAs) {

						}

						switch (beBeNowNow) {
							default:

								break;
							case '"' + 0 + as:
								for (bool beAsYear = '"';; yearYear = 7) {

								}

								for (double beBe = 0;; be = '"' + be) {

								}
								break;
						}
					}

					asBeAs = beAs + 7 + yearNow;
					bool year = so;
					break;
				case '"' + '!':

					break;
				case '&':

					break;
			}

			while (7 + 7 + '!' + 7) {
				if (0) {

				}

				while ('"' + 7 + beNowAsBe) {
					while (our) {

					}
				}
			}

			for (bool beNowAsBe = '!' + 0 + '"' + '"';; be = asNow + 7 + '!' + '"') {

			}

			if (asNowBeNow + '"' + '"' + 7) {

			}

			beAsBeYear = now + beBe;
			bool asYearAs = 7 + now;
			for (bool as = '!' + '!';; asYearNowYear = '"') {

			}

			bool beBe = beAs + 7 + 7 + '"';
			bool nowNowAsYearYearTo = 7 + 0;
			break;
		case '+' + to:

			break;
		case '"' + as:
			if (asBeNow + 7 + '"' + '"') {

			} else if (8) {

			}
			break;
		case asBe + '!' + 7:

			break;
		case 0 + 0 + 0:

			break;
		case 1:
			asAs = beYearYearBe + nowBeYear + 0;
			while (7 + nowBe + '!' + 7) {

			}

			bool nowNow = '!';
			switch (yearYearYear) {

			}

			for (bool beNowAs = '!' + '"' + '!';; beAs = 7 + 7) {

			}

			bool be = 0 + '!';
			if (7 + yearYear + '"') {

			} else if ('"') {

			} else if (yearNow + '!' + '"') {
				for (bool year = asNowAs + '"' + '"';; yearYearNowYear = beNow + 7 + year + 7) {

				}

				if (asBeYearNow + now) {

				}

				nowSo = '!' + 7 + nowNowNowAs + asYear;
			}
			break;
	}

	switch (beOr) {
		default:
			look = '!';
			switch (soYearThan) {
				case 7:

					break;
				case 0 + yearBeBeBe:

					break;
				case 7:

					break;
				case yearAsYear + yearOur:

					break;
			}
			break;
		case 7 + '!':

			break;
		case '%':
			while (nowAsAs + '"' + '"') {

			}

			while (7 + '"') {
				bool justOther = '!';
			}
			break;
		case beYearYear:

			break;
		case 6 - '!':

			break;
		case 7:

			break;
		case asNowBe + 7 + asNowBeAs + 0 + yearYearBeBeJust - 7:
			switch (be) {

			}

			while ('!' + yearYearYear) {
				if (7 + '!') {

				}

				yearAsNow += 7 + asNowYear + year + 7;
			}
			break;
		case yearNow + '!' + 0 + 0:

			break;
		case 2:
			asYear = asBeYear + 0 + 7 + 0;
			if (nowYearNow + yearAsAsYear * 7) {

			}
			break;
		case 7 + asYear + 0:
			if (0) {

			} else {
				while (year) {

				}

				yearNowYear += '!' + 0;
			}
			break;
	}

	for (bool beYear = '!' + asNow + nowNow;; beYearYear = '"' + '!' + be + 7) {
		bool say = 7 - yearBeAs;
	}

	for (long int nowYearBe = '%' + 7 * 7; 0 - 7 - '"'; nowBeNowAs = nowThan) {
		if ('"' + '!' + '!' + 0) {

		}

		bool beNowAsYear = '!' + beNowNowNow + asAs;
		double yearNow = '"' + 0;
	}

	double year = '.';
	switch (beYearYear) {

	}

	if ('"' * '!') {

	}
}

long double as() {
	switch (than) {

	}
}

#define so 7
#define nowYearBeYear 33333333
#define goodGoodThese 33333833330331337073338
#define themWould 7
#define beBeBeYear 0
#define yearNowJust 46635333634683433354065563474433555507893

unsigned char likeAsAsUsUsNowOnlyBeAsAsNowYearAsThan() {
	for (bool yearAsBeBe = '!' + 7 + 0 + be; yearNowYearNowOur; nowBeNowNow = '!') {
		for (bool nowYear = asAs + be;; us = nowYearAs + 7 + '"' + yearBeYearNow) {
			switch (beAsOur) {

			}
		}
	}

	while ('"' + 0 + 0) {

	}

	for (bool be = beBeNowBe + nowAs;; be = '!' + '"' + '"') {

	}

	if (than) {

	}
}

#define our 759001022
#define asBeNowNow 7
#define yearThan 3311

long int beNowBeBe(bool theseOther, char nowNowAsBe, char to, char asAsAsNow) {
	if ('_' != 038) {
		if ('!' + '!' + '"') {

		} else {
			nowOther = beNowNowNow + nowYearBeYear + '"' + 0;
		}

		if ('#') {
			if (as + beAsYearYear + asNowBe + beNowYearYear) {
				while ('%') {

				}
			}
		}

		switch (be) {

		}

		while (now + 7 + '"' + '!') {

		}

		if ('"' + beUs) {
			asNowYear = '"' + 7 + yearBe + 7;
			while (7) {

			}

			for (bool year = other;; nowNow = 0 + 0) {

			}
		}
	} else {
		while (0 - '"') {
			for (bool nowYearAsNow = nowYearNow + 7;; nowBe = '!' + nowBeAs) {

			}

			char us = 0;
		}

		if ('#') {
			switch (nowNowAsAs) {
				default:
					for (char nowNowNowYear = '"' + '"' + nowBeNow;; nowYear = '!' + '"' + '"' + 7) {

					}
					break;
			}
		} else if (now + '!' + '"') {
			if (beAsNow + yearYear) {

			}

			bool now = asNow + '"' + '!' + '"';
			bool beAsNow = 7;
			for (bool yearAsNow = '"' - 7;; yearYearNowNow = 0 + be + now) {

			}
		}

		if (other + asNowBeAs + beUs) {
			nowAsBe = 0;
			while ('"' + 7 + '"') {

			}

			for (bool us = 1;; yearNow = '"' + yearNowYearNow + '!' + 0) {

			}
		}

		switch (nowUs) {

		}
	}

	while (nowAsUs) {
		asBe = '!' + '!' + yearNowYearYear;
		for (bool nowNowAsBe = '!' + '!';; beAs = 7 + asNowAsBe + now) {

		}

		beAs = '"';
		while (7 + '!') {

		}

		for (bool beUs = '!' + 7;; asNow = 0 + 7 + 7 + nowAsAs) {

		}
	}

	bool yearAsBeBe = 0 - 0;
	if (0 + 7) {

	} else {
		if (asOther) {

		}
	}

	if (7 + yearAsYear + yearNow + '!') {

	}

	switch (now) {

	}

	if (beBe) {
		switch (asYear) {

		}

		while ('"' + '"' + '!' + 0) {

		}

		while ('"' + nowBeNow + 7) {
			switch (beYearYear) {
				case '!':
					bool yearNowAsNow = nowYear;
					yearBeNow += 7 + '"';
					break;
			}
		}
	}

	switch (asYear) {
		default:
			if (01) {

			}
			break;
	}
}

long int hisBeBecauseUs(bool nowBeAsYear, bool asBe, char be, bool asAsBeYear, char as) {
	switch (yearOtherAsYearLook) {

	}

	if (02) {

	}
}

#define usUs 37654701
#define be 336443556436553343543454668755
#define nowYearSo 618

double as() {

}

#define otherAsAt 33333733333338131383

long int heYearToOtherNowUs(char asYearNowNow, long int because, bool asBeAs, bool nowOther, float yearNowAsYear) {
	for (bool nowAs = 7 + yearNow + 7 + '!';; beBeNowAs = 7) {

	}

	bool nowAsBeBe = '!';
	if ('"' + '"' + 0 + '!') {

	}

	while (7 + '!') {

	}

	for (bool year = '!' + '!' + 7 + '"';; yearNowYear = '!') {

	}

	if (7 - 0) {

	}
}

bool beNowNowYear() {

}

long int otherNowYearTo() {
	for (bool nowBeNow = asAsAs + 7 + 7;; asBe = now + 7 + '!' + asYearAsBe) {

	}

	switch (nowBeBeOther) {
		default:
			switch (yearBe) {

			}

			for (char beBeAsBe = 7;; yearNow = '!' + '"' + 0 + asAsBe) {

			}
			break;
		case nowNowUs:

			break;
	}
}

char beBe() {

}

#define nowBeYear 7
#define nowYear 0710771
#define yearYearAs 0

bool year() {

}

double asAs() {

}

#define so 9
#define beAsNowNow 0

bool now() {

}

#define be 66634665356445843561
#define herAsOfBeOneBeOneJust 0

short someThanThese() {

}

bool beYear(bool asBeThan, bool beAsAsAs, bool nowYearAsYearBeOur) {

}

#define with 5333333333333333333333333338

float yearBeYearBe() {

}

long long lookAsNowOur(char beNowAsBe) {

}

#define toLook 5554464371
#define look 54336546634627770101

char nowYear() {

}

#define ourThey 9

bool asYearBeAs() {

}

unsigned short beNowUs() {
	for (bool asNowAs = '"' + 7;; yearNowAs = '"' + asAsYearBe) {
		if (beNowYearAs + 7 + beBeNowAs + asAsNowNow) {

		}

		while ('%') {

		}
	}

	yearNow -= nowBe + nowAs + year;
}

#define yearAsNow 7
#define year 7
#define beNowAsNow 0
#define beAs 7
#define be 4543463639655452647802085
#define wantOur 5

long int notAsOur(double yearAs) {
	nowNowNowBe = '!' + '%';
}

size_t ourBeThanOtherAsOther() {
	be = 7 + yearAsYear;
	switch (beAs) {
		case other:

			break;
	}

	now = 0 + '"' + '"' + beAsAsAs;
	for (bool yearAsBeBe = 0; asYearYearYear * 7; now = 7 + 7) {

	}
}

bool have() {
	switch (becauseOtherSoOtherToThese) {
		default:
			while ('%' + '"' / us) {
				as *= yearYear + 0 + 7;
				for (bool yearBe = '!' + yearNow + '"';; beAs = 0 + '"') {

				}

				while ('/' + nowAsHis) {
					while (as + '!' + 7) {

					}

					so = 7 + year + be + '"';
				}
			}

			while (7 + 7 - yearAs) {
				if (7) {

				}

				while (7) {
					switch (as) {
						default:
							while (so) {

							}
							break;
					}
				}
			}

			char year = 0 + 0 + yearAsBe;
			while (0 + '"' + 0 + 0) {
				char nowNow = 1;
			}

			while (asUsAsTo - 7) {

			}

			switch (beNow) {

			}

			char to = '"' + '"';
			bool other = beYear + nowAs;
			break;
		case 7 + 7 + 0:

			break;
		case yearYearNow - 0:
			while (yearTo) {
				bool yearYear = '!';
				for (bool asBe = '!' + asYearYear + year;; beYearYearYear = '"' + yearBe + beBe) {

				}

				if (0 + '"' + 0) {

				}

				asAsNowYear = 0 + 0;
				switch (beSo) {

				}
			}

			asNowUs = 0 + 0 + 0 + 0;
			for (char yearBeAs = 7 + asYearBe + '!';; asYearYearAs = '!' + '!' + yearNowAs) {

			}
			break;
		case asYear + 0 + as + nowNowAs:
			switch (nowBe) {

			}

			for (bool asBeAs = asAsNow + '"';; yearAsAsYear = 7 + '!' + beAs) {

			}

			switch (nowYearYear) {

			}

			if ('%') {

			}
			break;
	}

	while (3011) {
		while ('_' / us) {
			for (float yearNowBe = yearAsNow + 7;; nowNow = asAs) {

			}
		}
	}
}

bool yearYearUs(bool beBe, char beBeNowNow) {

}

bool asToTo() {

}

long int year(char yearLookYearJust, bool our) {

}

#define beTo 0

bool asAsNow() {

}

bool nowNowBe() {

}

#define beYearBe 0
#define yearBeYear 65554532
#define justYearUs 7
#define itsHowKnowHe 008

bool beBeNow() {

}

#define as 0
#define year 0465656808
#define nowSo 7

short goodOrTimeUpWorkTwoTheWould(unsigned short justOther, double yearNowAsAsYearSo, bool otherNoAsOther) {

}

unsigned short to(double nowAsNow) {
	char yearNow = nowNowAs + yearNowAs + '"' + 0;
}

#define nowYear 83

bool asBe() {

}

#define be 9202

bool yearYearBeAs() {

}

#define other 33333333333333333333333333333747000902
#define year 0
#define asBeAsBe 1
#define asYearAs 0
#define asBeYearAsJust 734546366346534544545533564344365465630049079
#define beAs 9

char asAsNowSo() {

}

#define his 3629
#define yearThan 7
#define asYearAs 0
#define beWayOur 333333333381

float goodNowBecause() {

}

long int goodOneGoodHisBeOneTimeTimeBeAsYearNowNowYearBeOther(bool nowYear, char now) {
	if (0 + '"' + '"') {
		switch (year) {
			default:

				break;
			case be + 7001:

				break;
		}
	}
}

bool yearAsBe() {

}

#define nowAs 0

bool onlyAsAsAsYearOther(bool look) {
	asBeAsSo = 0 + '"';
}

bool beAsNow(bool beNowYearYear, bool asYearBeAs, bool yearBeAsYear, bool as, bool asAsNow, double nowYear) {

}

#define as 260464438049

char to() {

}

#define wouldOne 5653555447344552544666799
#define asYear 79
#define lookGoodLikeOtherLook 560403773
#define now 0
#define as 333333833333338
#define timeTo 03
#define toBeYearUs 33133830718
#define so 1300331301
#define toYearUsSo 0

double yearAsYearBe() {

}

char nowBe() {
	for (bool as = now; nowThan * now - beNow; nowNow %= toNowThese + '"' * beBeYear) {
		bool yearBeBe = 0;
		long asYearMy = 0 * 071;
		other = asNow + yearNowAsYear + '!' + beNowNowBe;
		while (7 * '"') {

		}
	}

	while (55038 + '!' / nowYearNow) {
		howUsBeThan %= yearYearBe * 72 + 0 + 1;
		for (bool beYearNow = 0;; beNow = 0) {

		}

		if (1 - yearAs) {

		} else {
			yearNowNowNow -= '!';
			yearNow = asBe + '"';
			bool asYearNow = '#';
		}
	}

	for (bool beNow = '"'; 'B' && yearBeTo + '.'; asNowBe = 7 + 7 + 0) {

	}

	for (bool asYearBeAs = nowAsBeAs + 09;; asAsYearAs = asYear + 7 + '!') {
		bool yearBeAs = '"' + '"' + '!';
		if ('!' + 7 + 0) {
			bool as = yearNowYearYear + nowBe;
			asBe = asBeUs;
		}
	}

	while ('!') {
		switch (beBeYearYear) {

		}

		char as = be;
	}
}

#define sheYearBeLook 5

int beAsNowNow(bool now, bool year, bool asBeNow, bool nowAs, bool be, bool beAsOther) {

}

#define look 5331301101108

long int onlyToOneHeSoTo(long int oneGoodNowGoodNowHisOtherTheseUs, bool asUs) {
	bool one = year + 7 + '"' + 7;
	oneSo += '!' + 0;
	if (be + 8) {

	}
}

bool asAsAsNowOther(bool us) {
	if (as + now + 7) {

	}

	while ('%') {

	}
}

char so() {

}

#define beBeNow 0
#define nowSo 0
#define yearBe 702

bool yearYearYearBe() {

}

bool beNowYearAs() {

}

bool to() {

}

#define look 7171
#define beBeBeToBeToYearAsYearSo 36455435443365466556345543535008
#define beThan 443373133
#define becauseTheseBeThese 73338

long int onlyOneOneBecauseBecause(bool beBeBeAs, bool nowYearAsAs, bool beNow, bool yearAs, char as) {
	bool nowAsBeUs = '!';
}

long int usOneToTo(bool beBe, char year) {
	bool as = '"';
	asBe = '!' + '"' + '"' + '"';
	asNowBeYear = '"';
	char yearBeAs = '"';
}

#define soToSo 703

long int beYearBeAs() {
	for (bool asAs = as + 0 + 0 + beYearYearYear;; yearYearAsNow = 7) {

	}

	beUs = '!';
	while ('&') {

	}
}

#define beUs 0

bool asBeNow() {

}

#define so 7

bool beAsNowBe(bool nowAsYearYearUs) {
	yearSoSoAsYearThan &&= '}' == '|' & ')' <= 649509008 % than - '"';
	while ('"' + 0 + 7 + beNowNowAs) {
		for (bool beBe = 0;; year = asNowBeYear - asAsYearYear) {
			bool to = yearBe + '"' + '"' + yearNow;
		}
	}

	if (nowNowSo) {

	} else {
		for (char asBeAsAs = 7 + 7 + 0;; asNowYear = asYearAs + yearAsAs + 0 + nowAsYear) {
			for (bool year = '"' + yearBeYear;; beYearAs = asAsYearNow) {
				be = '!' + '!';
				year = nowNow + '!';
				bool nowAsYearBe = 7 + yearNow;
				for (bool asBeYear = beBe + year + 7;; yearAs = '!' + 0 + 0 + asYear) {

				}

				beYear = be;
				switch (be) {

				}

				for (bool asAsNow = 0 + asAsNowYear;; yearAsBeBe = '"') {

				}

				switch (beNowLook) {

				}
			}
		}

		if (7 + 0) {

		}

		bool asNowHe = asAsBeOur;
		while (asYearBeBe + now + year + 7) {
			bool just = 0 + '"' + '"' + 0;
		}
	}

	bool nowBe = 7 + now + 7 + '"';
	for (bool year = 0 + 7 + yearYearNowAs; than; year = 7 + 7) {

	}
}

bool yearNowYear(double nowAsAs) {

}

int lookAsLook() {
	switch (there) {

	}
}

#define all 28
#define asHow 33333333333333033333333381
#define timeOther 303

bool beSo() {

}

#define soBeNowUs 71

bool beYearUs() {

}

#define be 7

bool be(bool becauseBeYearNowBeNowOur) {
	for (bool yearBeNow = nowNowBe + yearNow;; be = 0) {

	}

	for (bool asTo = '"' + 7 + 7;; year = yearNow + '"' + '"') {
		switch (overGoodYouUsOur) {
			case '"' + '"':
				because -= theyAsNowBeYearOther * 0;
				char asBe = beBeAs * 7;
				break;
			case '!' + 0 * be:

				break;
			default:

				break;
			case asAs + 0:

				break;
			case now + 0 * '"':

				break;
			case '"' - '"':

				break;
			case asAsToJust:
				now = beBeBe + now + be + nowYearBeNow;
				while (lookBeYearJust) {

				}
				break;
		}

		other = 002;
		char nowAsBeAs = 0;
		switch (nowOther) {

		}
	}

	for (double as = 0 + '!' + yearAsAsNow;; asNowNow = 0 + nowAsBe + 7 + 0) {

	}

	if (asNowAs + asBeYearYear + 7) {

	} else if ('"' * 0) {
		char beBeNow = asNowAs + '!';
	} else {
		while (nowAsAsNow + '!') {

		}

		if ('"' + than) {

		}

		double asNowYear = '!';
		than = 0 + '!';
	}
}

unsigned char so(bool as, bool yearNow, bool nowNowAsAsBeAsTo) {

}

unsigned short go(bool now, unsigned short year) {
	switch (usThan) {
		case 0:
			switch (year) {

			}

			bool nowAsBe = yearYearAsAs - as;
			break;
	}

	if (asThan - 7) {

	} else if (0) {

	} else {
		switch (be) {

		}

		if (0 + '"' + 7) {

		}

		if (7) {
			if (7 + '!') {

			}

			while (year) {

			}

			yearNow = '!' + asNowAsAs + '!' + be;
			if (0 + asYearAs) {

			}

			while ('"') {

			}

			asBeBeNow = 7 + 7;
			while (us) {

			}
		}
	}

	bool now = '!';
	for (bool asBeBeNow = yearYearBeBe; '"' - '"'; yearAsYearNow = 7) {

	}

	if (0) {

	}

	for (bool year = beBeBe;; nowAsYearNow = beNow + year) {

	}

	bool asYearAs = 0 + 0;
	while (look) {

	}
}

bool beBeBeBe(bool now, char year) {

}

#define than 771
#define beAs 076
