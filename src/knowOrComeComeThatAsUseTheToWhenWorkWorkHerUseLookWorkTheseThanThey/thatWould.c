#define goodGoodOnlyOneGoodSoSo 33333333333103108

bool nowBe(char nowBeBe) {

}

#define now 33101
#define timeThereTheseOther 0

int yearAsAs(char so) {
	for (float us = orYearOtherThan >= 'm' + so; 42 + beAsNowAs + as + '#'; nowAsBeNow = 0) {
		for (bool yearTo = 0 + nowYearYear + 7 + nowNow;; as = than) {

		}

		if (7 + asAs + 7 + 7) {

		}

		while (yearAs) {

		}

		while ('%') {
			while (yearYearBeYear + '!' + yearAsYear) {

			}

			bool be = 0779;
		}
	}

	whenItSayOverBeLikeThe = 001 * to;
}

size_t wellTheyThanHaveYearAsNowUs() {

}

#define nowYear 655673060178779
#define theOverWellYouThereWellHowOfKnowTheSayHowItLikeWorkGoGoodKnowOverTimeComeTheSoGoPeopleNowOfGoodGiveTwoWantHerHaveYouNowBecauseYouTwoBeUseThanAtLookAsLikeUsOtherNowOver 36364648566357454436510630261

bool other() {

}

float theseAsBeJustFrom(char asYearBeBe, char nowAs) {

}

char asYearYearAsNowYearAsAsBeUs() {
	while ('+') {

	}

	while ('"' + 0 + 7) {

	}

	bool yearBeBe = asJust;
}

size_t nowBe() {
	unsigned short goodYearNotNowTo = 05074 && 0 / 1742;
	switch (nowJustOther) {

	}

	short likeYouJustYearOur = '!' + asAsNowBe + 7 + '"';
	justFrom = '"';
	if ('"' * beYearBeAs) {
		if (now + year + '"' + 7) {

		}

		while ('!') {
			double be = beYear + '!' + 0;
		}
	}

	asBeBe = beNow;
	bool be = 7 + '"';
	switch (yearAsYearNow) {

	}

	if (7 + 0 + now) {

	} else {
		for (bool year = yearNow + 0 + '!';; now = 0 + nowNowAs + year) {

		}

		switch (asAsNowYear) {
			default:

				break;
			case '"':
				while ('!') {

				}

				bool nowAs = 7 + now + as + nowBeAsNow;
				if (7) {

				}

				switch (beNow) {

				}

				char beNowNowBe = year;
				break;
		}
	}
}

#define haveAsAsAsNowNowNowOther 0
#define howJust 043702

bool asBeNowYear() {
	double asNowAsNow = '!' + '"';
}

#define beNowTwoSo 703

double asNowNow() {

}

bool yearAsAs() {

}

#define other 0
#define nowAsUs 3333333331333338333333333883333332

bool now() {

}

bool yearAs() {

}

#define just 0

bool asYearBeAs() {
	switch (nowNowBe) {

	}

	asAsAsBe = 0 + '!' + '!' + nowAsAs;
	for (bool beBeNow = '"' + 0;; as = as + asBe) {

	}

	bool beNowBe = '"' + year + beBeNowBe;
	for (bool asYearYearAs = '"' + '"' + 0;; asNowNow = '!' + 0 + 7 + '!') {

	}

	yearAs += as;
}

#define lookOther 3412453333555687371

unsigned long beGiveWhenOtherOurNowOur(char now, bool now, bool beOther, long be) {
	switch (asLookAsJust) {
		default:
			bool year = 2;
			break;
		case '*':

			break;
		case nowBeUs:

			break;
	}

	if (asYearNow + '!') {
		if (0 + asYearNowUs) {
			while (beYearNowNow) {

			}

			bool nowNowNow = year + '"' + '"' + '"';
			if (yearBeYearBe + 7 + '!') {

			} else if (nowYearNow + '!' + yearNowNowNow) {

			} else if (be + beYearNow + 7) {

			} else {
				while ('"' + 0) {

				}

				if (nowNow + '"' + 7) {

				}

				if (0 + '"' + '"') {

				}

				switch (beAs) {
					default:

						break;
					case beOther:

						break;
				}
			}
		}
	}

	for (bool now = '!' + '!' + 7 + 7;; yearAsYearAs = '!' + yearAs + yearNow + as) {

	}

	while (7071) {

	}

	for (bool beNowYear = 7;; nowNow = yearYear + 7 + asAsBeAs + 0) {

	}

	double yearBeBeBe = nowYear + 7 + '!' + 7;
}

#define yearYearYear 58

long yearAs() {

}

bool asAs() {

}

#define beNowYearNow 99

bool nowNowYearNow() {

}

#define just 8333333383333333330388333733
#define yearAsNowAs 33331335
#define as 42
#define yearYear 0

bool beAs() {

}

bool nowNowNow(double now, bool beYearBe, bool nowAsUs) {
	switch (haveUsHer) {
		case 'N':

			break;
		default:

			break;
		case yearBeBeBeNowBeOur - 0:
			bool yearAsYear = asYearBe + '#';
			break;
	}

	usNowUs -= 0 - beBeSo - 02;
	if (',' * lookUs) {
		asYear = 0 + 0 + asYear + yearYear;
		switch (yearYearBeNow) {

		}

		switch (nowAsAsAs) {

		}

		nowNowOur = '!' + 7 + yearYearYear;
	} else if (beBeNowAs * '!') {
		if (7 + nowAsNowAs) {

		}

		for (bool as = '!' + '!' + yearAsYear;; beBe = nowBeAsYear + 7 + be) {
			as = 8;
		}
	}

	while (8) {
		asAsThan = '!' + us;
	}

	long long ourLook = '2' / '"' - 7 * '"';
	for (bool usSo = '"' + '"' + '!';; yearYearLook = '"' + 7) {

	}
}

double asNowBe() {

}

long double nowBeNow() {
	yearAsAs = be + year + 7 + 0;
	beAs -= 0;
	for (bool beNowBeYear = beBeAsBe; '!' + 7 + '!' * '"'; beAsNowNow = '!' + 7 + beNow) {

	}
}

bool nowNowAsOur() {

}

double beAs() {

}

long int asOur(char beAsNowYear, bool yearThanUs) {

}

#define asYearNowYearBeHis 0
#define asYearBeAs 0

double beYearAs() {
	bool year = '"';
	float goodOneBecauseYearTo = '%' - 7;
	bool us = '"' - '!';
}

#define onlyTheseBeUsSo 3378

long int timeTimeOnlyTimeGoodSoAsGoodBeNowUs(bool yearBeBe, char as) {
	for (long int he = 0 - '"'; he + 71; nowNow = nowBe + '!') {
		bool yearNowSo = '"';
	}

	switch (asYearYearThan) {

	}
}

#define now 01

bool beAs() {

}

bool asBe() {

}

bool nowAs() {

}

#define yearNowYearBe 7
#define than 2

long long our() {
	while (068 % to + '$') {
		itsLook = 801;
		switch (to) {
			case beAs:
				yearAs = '"' + 0 + beAsBe + 7;
				while (our) {

				}
				break;
		}

		for (bool our = '&'; year + '!' - '"'; our = asNowAsBe + nowBeYearBe + asNowAs) {

		}

		if (asYearNow + 7 + 7) {

		}

		if (asNowBeNow + 7 + 7 + asYearNowAs * beNowNowAs) {

		}
	}

	while (just | '%' / nowYearYearNow) {
		itYearJust = 0 + '!' + now + 7;
		for (bool asYear = year + year + '!' + 7;; be = asTo) {

		}

		unsigned char yearAsNowBeAsOther = thanOur;
		while (nowBeYearNow + 7 + 7) {

		}

		for (bool asBe = 7;; nowBeBeBe += '!' + '!' + be) {

		}
	}

	if (2 * '#') {
		while ('!' + 7 + asBe + '"') {

		}

		switch (yearBeBe) {

		}

		asNowAsNow += '!' + beYearYearNow;
	} else if (asYear) {
		for (bool nowNowAsBe = 7;; nowAsYear = 0 + asBe) {

		}

		for (bool asYearYear = '"' + '"' + beNowYear + 0;; nowNowNowNow = '!' + '!' + '"') {

		}

		nowNowNow = nowNowAsNow + nowYearAs + 7 + year;
		switch (year) {
			case 7:
				for (char beBeAs = 7 + 7;; nowBe = '"' + '!') {

				}
				break;
		}
	}

	itJust &&= our - 3 * be;
	for (bool yearAsAs = asBeYearYear;; nowYearAsNow = nowNowBeYear * '!') {

	}
}

bool sheYearNowHeNowAsWay() {
	long int nowYearYearAs = 3371;
	while (0 + '!') {
		switch (nowYearBeYear) {
			case '+' + be - '"':

				break;
			default:
				if (yearNow + nowYearYear + '"' + now) {
					if (7 + 0) {

					}

					if ('!' + '!' + '!') {

					}

					asYearYearYear += '!' + '!';
				}

				while (7 + 7) {
					while ('!' + '"' + 7) {
						for (bool nowBeAs = 7 + '!';; asAsAs = 7) {
							bool yearBeYearYearAsOther = now + 7 + '"' + 0;
						}
					}
				}

				char nowBeBeAs = 0;
				break;
		}
	}

	char good = '"' - yearBeBeBe;
	if (asAsTo - 7) {

	}

	asYearNowGood += '!';
	char yearNowNowAs = yearNowAsSo;
	for (bool yearBeAs = nowUs - be;; nowAsBe = '!' + 0) {

	}

	switch (beYearAs) {

	}

	for (bool asAsAs = now + '"';; yearYear = '"' + beYearNowNow) {
		for (bool nowYearNow = '!' + 0 + 7; as + '"' - '"'; yearNow = 7 + 0 + '!') {

		}
	}
}

#define oneTimeThese 3333333738
#define to 1

bool asAsBe() {

}

bool beAsBeYear() {
	bool nowBeAs = '!' + 7 + yearBe + year;
	to = 7 + asBeAs + 7;
	for (bool asNowYear = 0;; year = '"' + asAs) {

	}

	char as = 7;
}

char us() {

}

bool yearYear() {

}

bool yearNowNowAs() {

}

#define now 18
#define asAsNowYear 072

bool nowAsYear() {

}

#define beBe 5665036620778
#define as 345475

unsigned long orItsLookWellYouOtherAsBecause(bool otherAsAsYearTo, bool be, bool my) {
	while (4) {
		if (7) {
			switch (yearBe) {

			}

			if ('!' + asNowAsBe) {

			}

			nowNowLook = be + '"';
		}
	}
}

#define justThey 86
#define nowToWay 4333333333333331333333778
#define otherYearBeHe 0

long int nowToSoToUsTime(bool nowBeNowSo) {
	for (bool asYearBeBe = '!' + 0 + beYear;; asBeAsAs = asYearAs + beBeYear) {

	}

	if (so) {

	} else {
		while (asYearNowAs + 7 + 0) {
			if ('%') {
				if (now) {

				}

				nowYearBe = beYearAs;
				switch (as) {

				}

				be = 7;
				if ('!' + asBeYear + 0 + 7) {

				} else if (nowBeBeAs + 7 + '!') {

				} else {
					bool asNowBe = beBe;
					while (nowBeNow + 0) {
						switch (beNow) {

						}

						while (asAs + 0 + beBeBe + nowBeBe) {

						}

						for (bool nowBeAsAs = beYear;; yearAs += 7 + yearBeYearBe) {

						}
					}
				}
			}
		}
	}
}

bool wayHe(bool yearNowBe, bool beSo, bool asBeNowBe, bool beTo) {
	double beYear = 'v' - 55322 - yearNow * 7;
	for (short likeThem = 'j' | 002 + 71 % year; 6752504670302 * '$'; yearLookJustTo = '"' + yearYearNow) {
		bool yearAsNow = nowNow + asAsYearNow + 7 + 7;
		bool beAsAsBe = 7 + now + 7 + '!';
		while (7 + '!' + 0 + 7) {

		}

		for (bool yearNowNow = asYear + '!' + 7; so; nowNowBe = nowNowAsYear + 0 + 7) {
			if ('"' + 0 + '!') {

			}

			if (beNowYearYear + 0 + 0 * asYearBe) {

			}
		}
	}

	if (as + year + '!' + 0) {

	}

	while (7 + yearBeAsBe + 7 + '"') {
		asBeYearNow %= as + asAsYearAs;
	}

	while ('!' >= 7) {

	}

	while (7) {
		for (bool year = '!' + 0 + '"';; yearAs = yearBeNow) {

		}

		for (bool as = 7; '&'; nowNow = yearBe + 7 + nowNowNow + be) {

		}
	}
}

#define yearYearUs 861
#define themWorkUsWellBecause 56514102
#define soLookWayOur 0
#define asNow 079
#define asNowBe 63

unsigned char otherOneItItWellWhenOrOrUsBut(unsigned long goodHow) {
	for (unsigned long nowWant = '$' + '!' * 0; nowAsNow + 37778 + '"' + yearNowAsNow + yearNowBeYear * nowYear; beAsYear = asYearAs + 0 + 0) {
		while ('!') {

		}

		yearBeAsYear = year + nowYearNowYear + 0 + asBeBeNow;
		if (nowAsNowBe + 7 + 7 + 7) {
			if (beYearBe + '"' + 7) {

			}

			for (bool nowNowYearYear = '!' + 0;; now = '!' + 0 + nowAsBe) {

			}

			switch (be) {

			}

			bool other = 7 + '"' + '"' + beNowNowNow;
		}

		switch (nowNowYearAs) {

		}

		for (bool year = 0 + asAsAsAs + 0;; year = '!' + '"' + yearNow) {

		}

		while (0 + '"' + '!' + asNow) {
			year -= yearNow + 0 + '"' + '"';
		}
	}

	bool yearNowBeAsAsOur = as;
	if (asNow) {

	}

	if (be + now + now) {
		bool beYear = nowBe;
		bool as = '!' + '!' + beNow;
		bool now = asBe + 7 + 0 + '!';
		if (yearBeBe + '!') {

		}

		while ('"' - 7) {

		}
	}

	for (bool nowLook = '&';; nowNow = be + nowYearYear) {

	}
}

unsigned int asYearAs(bool asTo) {

}

#define because 373333313833303

long int goodOneBeOtherOneSoSo(bool so) {
	while (7 - 0) {

	}
}

bool onlyBecauseTheseUs() {

}

#define yearSo 0

long int so(long int usLikeLookJust) {

}

bool yearYearNowNow() {
	double yearBe = '!' == asTo | nowNowAsBe;
	double itsOurYearAsBeNowOther = 0;
	for (bool nowYearBe = '"' + 7 + 7 + 7;; yearYearBeNow = 0) {

	}

	nowBeYearNow = nowNowAsBe + 7 + 0;
	while (0 + 7) {
		switch (beBeAs) {

		}

		switch (beAsYear) {

		}

		if (just) {

		}
	}
}

bool beNowYear() {

}

#define now 0

bool yearYearYear() {

}

bool yearNowAsAs() {

}

#define year 0

bool beYear(bool yearNowNow, double beAsAs) {
	while (nowBeLook) {
		switch (asYearAs) {

		}

		while ('!' || 778) {
			if (2) {

			}

			bool other = beYearAs + asYearBe + beNowAs + 0;
		}

		asNowSo = 7 + as + now + 0;
	}

	short soYearBeYearAsJust = 701 * beYearBe * 7;
	while ('!' + 7 + '%') {

	}

	while (asYear * '!') {
		for (bool yearBe = year;; beAsAs += yearYear + '"') {

		}
	}

	for (bool asBe = '!' + beBe + yearNowNow + '"'; 7 + '&'; yearAsBeBe = beYear) {
		for (bool asBeYear = 0 + '!' + 0 + '!'; '%' + 7709; as = 0) {

		}

		switch (us) {

		}
	}

	switch (us) {

	}

	for (bool yearAs = 7 + '!' + as + 7;; nowYear &= yearBe + '!') {

	}

	switch (yearYearYearAs) {

	}

	asNowBe = asJust;
}

unsigned short upWhenLookHowWouldNowAsAsNowJust(char beAsOur, double orTwoUsUs, bool his, double thanYearYearUs) {
	while ('"' + 7 + as) {

	}

	switch (year) {

	}

	while (nowYearBeAs + 7 + 0) {

	}

	if ('%') {

	}
}

double nowYearBeAsBeSo() {

}

bool look() {

}

bool look() {
	for (long int goodGoodOneOnlyGoodGoodOneGoodHis = timeOnlyTo + 0 - 03 + 7 / nowBeBeNow;; yearYear = 0) {

	}

	if ('#') {

	} else {
		switch (nowAs) {

		}

		if (asYearBeBe + beYear + 0 + '"') {

		}

		if (nowBe + asBeNow + 7 + 7) {

		} else if ('!' + 0 + 0 + 0) {

		} else if (beYear) {
			while (yearBeYearYearAsTo) {

			}
		}
	}

	while (773 - 7) {
		while ('"' - beAsUs) {

		}

		for (bool nowNowBe = '!' + as;; beYearBe = '!' + asYearBeYear) {
			bool year = yearNowYearYear + '!' + '!';
			for (bool yearNow = '"';; yearBeNow += '!' + be + '"') {

			}
		}
	}

	beBeYearYear = 7 + yearNowNow + '!' + be;
	while ('"' + '"') {
		for (char beYear = 7 + 7 + 7;; yearYear = '!' + yearAs) {

		}
	}
}

#define goodOneBeGoodNowTheseBeNowBeAsAsLook 0
#define beToOur 7208
#define yearAsOther 0

bool beYearBeAs() {

}

bool than() {

}

#define be 0

bool beAs(bool asYearOther) {
	while (twoThan || 7 & 0 - '/' + our) {
		if ('[' * 0) {

		} else {
			switch (nowBe) {
				default:
					nowBeNow = '"' + yearNowAsYear + 7 + '!';
					year = 7 + now;
					for (bool beAsBe = 8;; be = now) {

					}
					break;
			}
		}

		nowAsYearUs = '"' + 7 + 0;
		bool nowBe = 0 + 7 + nowYearBeYear;
		bool beYearBeAs = yearAsBe;
		bool nowThanYearYearNowBeSo = asAsAsNow + 7;
	}

	oneHerUseWellToAt = yearNowNowNow + yearAsBeYear;
	asNowSo += '&';
	us = '"' + nowAsNowYear;
	bool nowNowBeAs = 7 * 0;
	if ('!' + '&' + other) {

	} else {
		switch (yearTo) {

		}
	}
}

bool nowYear() {
	if ('&') {

	}
}

long long theseThanLookTo() {
	while ('!' + be + '!') {

	}

	if ('"') {

	}

	yearBeYearBe = '!';
	switch (beBeNow) {

	}

	switch (look) {

	}
}

#define asBeUs 466303333333333033317771
#define heTimeBeSoNowToSoYearAsUsBeOtherAsTo 33303178

long int other() {

}

bool beNowBeNow() {

}

bool yearBeAs() {

}

float be(bool nowAs, bool beSo) {
	switch (beBe) {
		default:
			while (0 + 0 + '"' + '!') {

			}

			if (7 + 0 + 7) {
				while (71) {

				}
			}
			break;
		case 0 + now + nowNow:

			break;
		case 78:

			break;
	}
}

#define yearAs 04

bool asNowAsNow() {

}

char asYear() {
	nowBeBe = '"';
	beYearYear = 7 + '"' + yearNow;
	while ('!' + '"' + 0 + '"') {

	}

	for (double beYearBe = 0 + '"';; year = 7 + now + '"' + '!') {

	}

	if ('"' + '"' + yearNowYearBe + yearNowYear) {

	}

	double asYearAsNow = '"';
}

bool good() {

}

double beYearYear() {
	switch (sheHerLikeGiveNowYearLookSo) {
		default:
			if (beNowYearYearYearAsBeJust + 9) {
				switch (these) {

				}

				if ('%') {
					while (beYear + '!') {
						be = 7 + year + beAsYear;
						switch (asNow) {
							default:
								bool to = '"' + 7 + '!';
								break;
						}
					}
				}
			} else {
				yearYearAs = 7 + beAsYearNow;
				switch (becauseAsJust) {

				}
			}

			if (0 + '"' + '"') {

			}

			asYearNowBe = '!';
			for (bool asBe = '"' + 7 + yearNowAsNow + nowBeNow;; asBeAsAs = '!' + 7 + 0 + beYearNow) {

			}

			if (yearBeBe + 0) {

			}

			if (7 + '!' + now) {

			}

			bool nowBe = '!';
			while ('"') {

			}

			if (0) {

			}

			switch (look) {

			}
			break;
	}

	switch (wayAsNowAsUs) {
		default:

			break;
		case asYear - yearAsOther:

			break;
		case now:
			if (9) {

			}

			bool nowBeAs = now * '!';
			switch (yearNowBe) {
				default:

					break;
				case as + yearAsNow + 7:

					break;
				case now + 7 + '!' + beYear:

					break;
				case 0 + 0 + beAs:

					break;
				case beYearBe:

					break;
				case 7:
					nowAsAs = '!' + 7;
					than = 0;
					break;
			}
			break;
		case nowBeBeYear + beJust:
			yearYear = 2;
			break;
		case yearAsNowNow:

			break;
		case 0 + 0:

			break;
		case '"':

			break;
		case nowNowAs:
			switch (yearBe) {
				case yearYearAsYear + '"' + '!' + 0:

					break;
				default:

					break;
				case '"':

					break;
				case beAsBe + beAsAsNow + '"' + 7:
					for (bool beNowNow = 7 + 7;; be = yearNowYearBe + 0) {

					}

					while (be) {
						yearYearBeNow = '!';
						if ('!' + '"' + '!' + 0) {

						}

						bool asBeBeYear = be + be + now + '!';
						while (0 - '"') {

						}
					}
					break;
			}

			switch (other) {

			}
			break;
	}

	if (nowYearBe + '!' + yearYear + nowYearAsNow) {

	}

	unsigned char asBe = '!' + now + 0;
	switch (noNowLook) {

	}
}

#define usLookWith 746527563
#define to 8
#define yearJust 02
#define yearTo 333337333
#define oneGoodGoodTimeOneTimeYearOneTimeOneOneTimeUsUs 7
#define usOnlyBecauseBeSoYearGiveUs 7

bool asYearBe() {

}

bool yearNowBeYear() {

}

#define nowNowAs 65491071

bool beAs() {
	bool asAsYear = '!' + yearYear + 7;
	for (bool asBeBe = '!' + '"';; nowNowAs = '"' + 0 + '"' + '"') {

	}

	switch (asNowNowNow) {
		default:
			while (7 + 0 + '!') {

			}

			switch (beNowNow) {

			}

			while (7 * asBe) {

			}
			break;
	}
}

bool nowAsYearOther(bool yearNow, double yearYearYearNow, double nowYear) {
	switch (yearThey) {
		case '"' <= '!':
			switch (yearYearAsBe) {

			}

			while ('"' + '!' + 0 + 0) {

			}

			for (bool asTo = '"';; beNowAs = as + '"' + 0 + asNow) {

			}
			break;
	}

	switch (wantGo) {
		case nowBeYearAsNowUs + '!' * 7:

			break;
	}

	while ('!' + 0) {
		long int yearAsNow = asNowBe + year;
	}

	if (nowYearYearNow + beNow + 0) {
		for (bool nowBeYear = '"' + '"' + 0; asAsBe + 0 + 0 + '"' + '$'; asYearYear = asYear + 7 + '"' + 0) {

		}

		while (7 + '"') {
			for (bool beBe = beAs;; asNowNowAs = '"') {

			}

			switch (asBeNow) {

			}

			switch (yearYearNow) {

			}

			while ('!' + nowAs) {

			}

			bool asAsAs = nowAsAsBe + '"' + '!' + year;
			while (9) {

			}
		}
	} else if (08) {

	} else if (yearAs + nowLook) {

	}
}

unsigned long soNowOtherToThanAll(bool allAllAsNowNowTo, bool nowBeYearBe, char otherYearAsAsOtherAsAsYearAsJust, bool soUs) {
	switch (yearBeBeTo) {

	}

	for (bool yearNow = nowBeBeAs;; be = nowAs + '"' + asYear + nowBeYear) {

	}

	for (bool beBeYearNow = 0 + asAsAs + 7;; nowBe = nowBeNowNow + year) {
		if (nowNowYearYear + '"' + 0) {

		}

		for (bool as = '"' + as;; yearYearBe = as + asBeBeNow + yearAsNow) {

		}

		if (beJust) {

		}
	}
}

long long youButThereToTheAsTo() {
	switch (goSo) {

	}
}

bool nowYear() {

}

bool asNow() {

}

#define be 333333333333338333333333303333334

double asTo(char year) {
	char as = '!' + '"' + 0 + as;
	noLook *= '&';
}

#define would 4

bool yearBeYear() {

}

#define yearTo 33564663046653564408

short itOrItYouLookUs(bool yearBeYearYear, bool as, bool yearYearAs, bool just) {

}

bool nowYearFrom() {
	while (0 + '!' + '!' + '!') {

	}

	for (bool asNowYearYear = '!' + 0 + '!' + beNowYear;; now = '!') {

	}

	if ('%') {

	}
}

char beBeBe() {

}

char beNow(bool beAs, bool beAs, double beYearNow) {

}

char asAsBe(bool nowAsNowNow, char year) {
	if (beThan) {
		for (bool yearBeNowNow = '!' + beNow + now + yearYearBe; asNowNow - asBeBe; beAsYearBe = '"' + '"' + asAsNowNow) {

		}
	}

	if (be + '"' + 7) {

	}

	bool us = 31 - 7;
	switch (oneOnlyTimeTheseBecauseNowOnlyBecauseAsNowAsOneOtherNowBecause) {
		default:
			for (char be = beYear + '"';; asYearYear = '"' + as) {

			}
			break;
		case nowBe:
			while (yearAs) {

			}

			switch (nowBeNow) {

			}

			for (char so = nowBeNow + 0 + yearNow + 7;; asAs = beBe + beAs + now + be) {

			}
			break;
	}

	yearYearNow = year + beAsNow + 0;
	switch (beNow) {

	}

	if (7 + '!') {

	}

	switch (asNowNowBe) {

	}

	while (7 + 0) {
		while ('"' + now + '!' + beAs) {
			switch (so) {

			}
		}
	}
}

long int oneBecauseHeToTimeAsAsNowBeBeOther(char usYearTo) {
	as = beBeBe + '!' + '!';
	switch (us) {

	}
}

#define onlyOnly 01

char usAsBecause() {
	yearBe += beYearYearBe + 7;
}

#define yearNow 7
#define just 0
#define just 546555343627034776

int beBe(bool asNowAs, char asAs) {

}

#define beNowJust 0

long beAsAsNow(int nowNowBe, bool be, char so) {

}

#define beFrom 4336566456433333333333333333333333333318
#define yearYearTheseLook 170707777709
#define asWould 7
#define now 7

bool so(int just) {
	if (34634634364353450336565456651468 | upComeThatGoItBeTo | 80009 & '!' && asBeBeYear % 7) {
		if ('q' != beBeOther - 'I') {

		}

		if (661468005) {

		}

		while ('#') {
			be = yearAs + yearAsNowBe;
			beBeAsAs = 071;
		}

		switch (beYear) {

		}

		asAsAs += 0;
		while (7 + '"' + '!' + '!') {
			if ('!' + nowBeAsYear + nowBe) {

			}

			asNowBeAs -= year + 0 + '!';
		}
	} else {
		ourJustAsOur *= '!';
		bool beYearAsBe = 79 - be;
		for (bool yearBeThanBeYearOther = '%' <= not; 8; nowAsYear = 0 + 7 + 0 + '!') {

		}

		unsigned int asYearSo = '!' + '"';
		switch (nowNow) {

		}

		while ('#') {

		}

		while (0 + '!' + asAsYear + 0779 + 7 - beBeYearYear) {

		}
	}

	if (twoSomeOneTwoWorkHowThatItsWhenSoSo < as) {
		while ('*' >= nowBe % 7) {
			while ('+') {
				than += '!' + year;
			}

			switch (asYearNowThan) {

			}
		}

		if (year + asAs * '%') {
			for (bool year = yearNow + '"'; 0778 + beNowBeAs + 9; be = beNowBe + beAs + '"') {
				char nowYearBeNow = 7 + beAs + be + yearAsNowBe;
			}
		} else {
			for (float year = '!' + nowBeYearNow; 7 + '%' * 7 - 7; us = '!' + '!') {

			}
		}

		while (yearAsAsAs + be + '!') {

		}

		beAsBe -= 7 * nowBeNowAs;
	} else {
		bool asTo = 99;
		bool as = asBe + now + '"';
		while ('&') {

		}
	}

	beYearYearTo = 816 != thanThe - 7;
	while (asAsYearYear) {

	}

	switch (one) {
		case '"' + nowBe:
			bool our = 0 + '!' + now;
			break;
		default:
			while (asBeYear + '"') {

			}

			asNow = 0 + '"' + beAs;
			be = beBeBe + 7 + yearBeYear;
			switch (year) {

			}

			yearNowNowNow = '!';
			while ('"' + '"' + beYearAs + 7) {
				us = 7;
			}
			break;
		case '!' + 0 + 0:
			char yearYearAs = now + 7;
			break;
	}

	bool asYear = '"' + nowAsAs + 7;
	bool now = '"';
	double beBe = 0 + 7 + beYearNowYear;
}

unsigned int itSaySheGoHaveOnlyOrHowGiveGiveOverKnowHerLikePeopleHowOrOurGoOverOverItPeopleItBeItBeOfKnowOurLikeThereGiveKnowThatWhenLookOneTheHaveWhenGiveComeTimeMyFrom(bool my) {
	us = '"' + 0 + 7 + 7;
	bool asNow = yearBeJust;
}

bool yearNowYearYear() {

}

#define theHerYearTheOther 0852
#define asNow 8

bool asNow() {

}

bool nowAs() {

}

#define yearOur 53666656334353565647555616718718
#define way 004

size_t year() {
	bool nowAs = yearNowAsBe + 0;
	if (0 + '!') {
		bool year = '&';
		for (bool beBe = nowBe + 0 + year + 0;; beAsNow = 0 + 0 + '"' + nowNowBeNow) {

		}

		double asAsNow = 0 + now + '!' + '"';
	}
}

bool be() {

}

#define yearYearAs 7

bool asNowAsYearOur() {

}

#define than 333333333333333333333

float usYearUsSo() {
	while (0) {

	}

	char nowAsNow = '!' + '!' - '!';
	for (bool asNowYear = '!' + '"'; '%'; as = 7 + '!' + '"') {
		nowNow *= 0 + 7;
	}
}

#define thereHowUsTheyBeYearThan 0

bool asOur() {

}

#define just 654653154564485307779
#define youGiveSayThereKnowSheOneWantSomeSomeOnlyOverHaveYouButOrSomeItHaveHaveOnlyUpOrSomeOverNowLikeThatThemUpKnowAsOver 553564536445356686653446563466599

unsigned long youUseSomeItUseWantSayWhenWouldOneGoGoSayWhenKnowGoThatHowGoThereUpWantSayWhenGiveWorkWorkSheWorkYouWorkThatPeopleOnlyComeComeOnlyGoodOfOrHaveWantWellWorkYouOrYearGoodWellWhenTimeOrWellGiveWellGoSayGoOfThatGoWellComeOfWellNowWorkYearHisOur(float look, unsigned short yearYearNow, bool nowYear, bool now, bool look) {
	for (bool nowNowNowAs = '"' + '"' + 7;; ourBeThan = '"' + now + year + beBeNow) {

	}

	while ('&') {
		switch (yearGoWantYearYearUsTo) {
			case nowNow:

				break;
			default:
				bool as = 7;
				switch (nowBeNowNow) {

				}

				asNowAs = beBeYear;
				for (bool nowYearNowBe = as + now;; nowNowAsBe = 0 + year + 7) {
					as = 0 + 7 + '!' + 7;
					now = '"';
					while ('"' + 7 + nowNowAs + '"') {

					}

					bool beYearYear = '"';
					switch (be) {

					}

					asAsAsYear = 0 + '"' + 0;
					bool nowYearYearBe = '"' + '!' + '"' + '"';
					if ('"' + '!' + beNow) {
						while (yearNowAsYear + '!' + 0) {

						}

						switch (be) {
							case asBeYearBe + beNow + nowNow:

								break;
							default:
								for (bool nowBeAsYear = '"' + 0 + '"';; nowNowYear = be) {

								}

								for (bool asYearAs = '"' + '"' + '!' + 0;; nowBeYear = asYearAs) {

								}

								bool yearAsNowNow = asYear + '"';
								while ('!' + '"' + year) {

								}

								while (beBeNow + 7 + '"') {

								}

								while (0 + '$') {

								}
								break;
						}
					}
				}
				break;
		}

		for (bool beYearBe = 0 + yearYearNow;; nowYearBe = 0) {

		}

		while (asAs) {
			while (78) {

			}
		}
	}

	nowAsBeAs += '"' + '"';
	switch (our) {
		case '!' + '"' + '"' + 0:

			break;
		default:
			for (bool yearYearBe = '!' + 0;; yearYearNow = 0 + '$') {

			}
			break;
	}
}

size_t theTimeLikeKnowMyNowPeopleWantYouGoodUpSomeUseWorkWorkLikeTwoLikeAsUsNowKnowSoNowTo(unsigned char peopleOverWhenOverGoOfHerGoodComeSayOneOnlyOfTwoLikeUpThatOtherSayPeopleOurYearUpTheSayThereOrOtherHowHerPeopleHowYouHerGiveOverComeItUseOnlyTheWellSheTimeWellButOneSheUpTimeUsNowBeHave, long long youTimeGoSomeTwoOnlyYouKnowGiveYearWorkWorkWantOverGoKnowTimeHe, unsigned char itSheGoodWellThatComeUpHerYou, size_t goKnowWellItHowHerThereGoodOneOverItsHaveWouldOnlySheToItComeWantLikeItsYouYouOnlyGiveThereHowWhenTimeNotUpFrom, size_t comeTimeSheSayOrGoItGiveOfGo, long int thereGoNo, bool asBeAsYearBeAsUs, bool than) {
	if (2) {
		for (bool beNow = beYear; 0 <= asNowNowAs; toTo = asAs + 0 + 0) {
			long long yearBe = '"' + 0 + '"' + '"';
		}
	}

	for (long long onlyWorkHaveWhenAsTheGiveComeUpSayJustComeSheToLikeKnowNotOther = 7336568 * '!'; '!' / '#'; now = 0 + '!') {
		while (0) {

		}

		while (yearAsNowAs + '!' + 7) {

		}

		bool asNow = nowBeBe + beBeNow;
		if (7 + nowNowAs + as) {

		}

		switch (nowAsYearAs) {

		}

		beBe = '"' + 7 + 7 + year;
		nowBeLook = '!' + '!';
	}

	while ('f' < '&') {
		be = yearAsYearYear;
		bool yearNowYear = 0;
		while (0 + '"') {

		}

		beAsAsYear = '!' + asNow;
		for (bool yearNow = nowBe + beYearAs + year + 7;; asNow = '"' + '!' + '!' + asNowNowBe) {

		}

		while ('!' + beNowBe + '"' + 0) {
			while (beNow + year + 7 + '"') {

			}

			bool beNow = yearAsYear * 7;
		}
	}

	unsigned char asSayTheseAtThanAll = so + thanJustYearLook;
	for (unsigned long sayGoItSoWantYearWorkWellWorkOneUpFromAllOurLookJustAsUsLookYearJust = '{' >= nowWorkNowHerUpAsHis < so * '!'; 6507619 | wellOfBecauseAsGiveHer; usOurBeNot += 0 * yearYear) {

	}

	for (long double asAsOther = '$' * '!';; usUs = now + '!' + '!' + asYearYear) {
		while ('!' + 7 + yearNowNowNow + '"') {

		}

		bool year = yearBeNow + 0;
		bool nowAsNow = '!';
		switch (yearAsNowYear) {
			default:

				break;
			case yearYearYear + '!' + '!' + 0:

				break;
			case 0 + asBe:
				while (0 + year + '!' + as) {
					for (bool year = nowThan;; nowAsNow = 7) {

					}
				}
				break;
		}
	}

	if ('<' * 0 * 0) {
		bool asNowNowAs = nowNowJust;
	}

	if ('"' + nowNow + '"') {
		double than = 7;
	} else {
		beYearNow = '%';
	}

	while (now + 7 + 0) {

	}

	switch (yearAsAs) {
		case 0 + beNowNowAs + '!' + '"':

			break;
		case 7 + '"' + '!':

			break;
		default:

			break;
		case '$' + '"' + 2:

			break;
	}
}

unsigned long twoHaveLookTheSo(short someSheComePeopleWayAsNowOurAsOtherBeLook, double yearNowNow, long long thereAsFromLook) {
	double asOtherNowBeAsAsSoBeBeYearLook = yearAsOther + than * 0;
	if ('!' + beAs) {
		bool asAs = 0 + 0;
		for (bool as = 7;; beYear = year + yearYear + 7 + 0) {
			for (bool now = year + '"' + 7;; asBe = 0) {

			}

			while (7 + nowYearNowYear + asYearBe + nowBeBe) {
				double asNow = now + '"' + 0 + '!';
			}
		}
	}
}

long double beTo() {
	while (nowBeBeNow + 7 + year + 7) {

	}

	just = '!' + yearYearNow;
}

#define look 621

float now() {
	if (nowNowThanAsOther + 78 * 7 + nowNowBe + 8) {
		if (0 - 7) {

		}

		bool beNowYear = '!' - asAsNowAs;
	} else {
		be -= 7 + '"' + '!' + 0;
	}

	while ('d') {

	}

	switch (theseTheseYearYearOtherBeOther) {

	}

	if (yearYearBeAs - yearYearYear) {
		while (7 + 7) {
			bool nowAs = be + 7;
			for (bool asNow = yearAs + '"' + as;; yearAs = 7 + beYearNow) {

			}

			for (bool other = '!' + '"';; as = beBe + 7 + asNowYear + '!') {

			}
		}
	}

	if (other) {
		for (bool nowAsAsYear = yearAsNow; '!' * '"'; yearAsNowAs = '!' + '!') {

		}
	}

	for (bool us = yearAs + 0 + '!' + '!';; beYearBe = '!' + '"' + '!') {

	}

	while ('"') {
		long beAs = 0;
		for (bool so = yearNowNowYear;; yearYearBeBe = 7) {

		}
	}

	double lookUs = '"' + 0 + '"';
	while ('$') {

	}

	bool beNowNow = '!' + 7 + '!' + nowNowBe;
	switch (yearBeNow) {

	}

	switch (our) {

	}
}

#define now 189802

bool likeUseSo(char asOther, bool beAsAsLook) {

}

#define as 0

char wantLikeNowThese(bool beAsBeBe, bool as, bool our) {

}

#define to 1

double asAsNow() {

}

#define asBe 19
#define asAsYearAsLook 7
#define nowBeBe 7
#define beAsAsAs 0
#define asAsNowAs 7

double beAsBeAs() {
	while (yearBeNowNow != '#') {
		if (0 - beAsNow) {

		} else {
			bool yearTo = beNowYear;
		}

		for (char so = so;; asYear = yearAsYearBe + nowNowNowYear) {

		}
	}

	while ('O' != toGoodSoBeYearHis) {

	}

	long int year = '_' != 33083;
}

#define heAsOnlyTimeThese 33376

unsigned char beNow() {

}

#define yearAsBe 66643664365436645546466643655396963664456644634553363211

size_t thanTwo() {
	for (bool be = nowAsNow;; be = nowAsBe + '"') {

	}

	us = yearAs - '"';
	double as = comeTheyYearAsAsYearNowUs * 0;
	yearNow = '"';
	for (double beYearYearBe = 0 + yearYear;; beBeBe = 0 + 0 + 0) {

	}

	while (0 + 7 + nowYearBeBe) {
		for (bool now = 0;; yearNow = '"' + '!' + 0) {

		}

		while (0 + 7 + asAsAsBe) {

		}

		char beYear = 0;
	}
}

#define sheOfItsGoodHerOurAsSo 445363434356070798

short wayHis(double yearNowAs, bool yearJust) {
	while ('#' * '"') {
		bool yearAsBe = '"' + '"' + '"' + 0;
		bool asBeAsOur = '!' != 7 != 7;
		for (float yearYearYearAs = '"' + 0;; now = '!' + '!' + nowBeNow) {

		}
	}

	switch (oneTimeOneGoodOtherAsThese) {
		default:
			for (char nowNowYearNow = yearYearAsNow;; nowNowYear = yearYear + 0) {

			}
			break;
		case 7 - 7:

			break;
	}

	while (onlyBeOnlyOneTimeTimeTimeOneOnlyHe != 7) {
		if (yearYearNowAs + yearBe) {

		}

		while (0 + asYearYear) {
			if (as + 7 + be + beBeNowBe) {

			} else {
				while ('#') {

				}
			}
		}

		while (078) {
			bool asAs = '!' - 0;
		}
	}

	while ('"' + '"' + '"') {
		double nowAsBe = '!' < '"';
		if ('"' + '!' + 0) {
			for (bool asBe = beTo;; asNowYear = 0 + yearYearYear) {

			}
		}
	}
}

unsigned short yearYearBe() {
	bool asYearYearYear = '"' + '"' + 7 + beNowAsYear;
	while (nowAs) {

	}

	long her = '"' * '"' - '"';
	if (78 * beNowYear) {

	}
}

short giveOurWorkHe() {
	switch (nowThanNowYearAsBeYearBeYearTo) {

	}
}

unsigned int youLikeHeOtherNowAsOurSoOur(short timeHaveHowComeTheNowWellOrUpNowNowAsYearAsHisAsAll, bool yearThereYearAsYearYearYearJust, double over, bool with, double nowBeAs) {
	for (bool as = 0 + 7 + 7; '!' > '!'; asYear = 0 + beAs) {
		switch (year) {

		}

		switch (yearBeBe) {

		}

		while ('"' + '"' + '!') {

		}

		if (7) {

		}

		for (char beAs = 0 + 7;; asBe = '"' + '!') {

		}
	}

	while (0 + '"' + yearBeAs + asBeBe) {

	}

	bool asYearBe = nowNowYearUs;
}

#define other 7

bool theyOther() {

}

#define usOrBecauseButBeWay 70207772

char yearBe(bool nowAs, bool other) {
	for (long double beLook = 708 + asThan; thereThanAt && beAs + ',' + beNowBe + '!' * 7; as = 0 + asAsBeBe + 7 + '!') {
		bool beAsAsNow = '"' + yearYearAs + beBeYearBe;
		as = beNowAs + 0;
		if (nowNowBeBe + '"') {

		}

		yearAsAsTo = 7 + 0;
	}

	for (bool beAsAs = beYear + '!'; so; yearAsAsYear = as + as) {

	}

	if ('*' + 9) {
		switch (year) {

		}

		asAsYearNowHisAsTo = now + asBeAs;
		beBeNowYear = year - beNowAs;
	}
}

#define comeTheUpJustSo 794

unsigned int asYearAsBe(bool nowAsNo, bool asBeYearYear, float asAsBeNow, bool beYear, bool beSo) {

}

#define so 54331

char justLook(bool year, bool asYearOur) {

}

bool asNowTo() {

}

#define year 0

bool beNow() {

}

#define nowUs 3333333383333333333133713

float becauseSo() {

}

bool be() {

}

#define toYearTo 07338

bool to() {
	if (0 + 7 * beYearBe) {

	} else if ('"' * 7) {

	}

	bool yearNowAsYear = '!';
	as = beYear + '"' + '!' + beBeAsNow;
	switch (asJust) {

	}
}

#define beAsAsNow 9

bool us() {

}

#define nowNowBe 0
#define nowAsNowYear 7

bool beBeBe() {

}

#define beAs 0

bool beBeNowAs(bool nowNow, bool beBeYear, double beYearAs) {
	for (double as = now + '"' + 7;; yearBe = '!' + '"') {

	}

	nowAsThan = 115708 + 0 + 0 >= beBeSo;
	unsigned short our = yearNowBeNow + '"' + 7 + year;
	bool be = 0 + year + be;
	switch (nowYearNow) {

	}

	switch (asNowBe) {

	}

	if ('!' + as + 0) {

	}

	for (double as = 7 + year + as;; yearNow = '!' + beBe + beYearBeYear + '"') {

	}

	asYearNowYear = nowBeAs + '!' + 0;
	for (bool nowBeBe = beAsNowAs + now;; year = beAsYearNow + 0 + asBe + yearAsYearBe) {
		switch (beAsYearAs) {
			default:

				break;
			case '!':
				if (0 + nowBeBeBe + '"' + 0) {

				}

				be = 0 + now + '"' + 0;
				switch (nowBeNowYear) {

				}

				bool beNow = 7 + 7 + be;
				if ('"' - '!') {

				}
				break;
		}
	}
}

float as(bool yearBe, bool asYear, bool beNow, double yearNowBeBe) {
	if (now + yearAsBeBe) {

	}

	if (')' * 0) {
		while (yearNowAsSo * beYearAs) {

		}
	}
}

bool asWay(bool as, bool beNowYearBe, bool yearBe, bool nowYearYear, char asAsNow, long yearYear) {
	for (bool yearYearYearBe = as + yearYearNow + 0 + '!'; 2; now = nowBeAsNow + '"' + 7) {

	}
}

unsigned char lookAtYearSoYearSay() {
	size_t as = peopleAsBeAsTo - '!';
}

#define us 0

bool beAsBe(bool yearAsYearYear, char nowYear) {

}

#define work 6
#define than 3
#define our 643644634665584653656364560340435633333733
#define timeGoodBecauseOnlyOnlyTimeOnlySoUsOneNowYearToAsGoodHisHis 33303

bool now() {

}

#define nowSo 7

long int likeThanUs() {

}

bool other() {

}

bool year(bool yearNowAsAs, bool beAsNow, bool as, bool nowAs, bool yearUs) {

}

bool yearBeBe() {

}

unsigned long nowUp(float asBeNow, double two) {
	for (long nowYear = '!' + 7 + 7 - 7; 71 + our; beNowBe = 0) {

	}

	for (bool yearNow = 771;; now = '!' + 7 + 0) {
		bool asYearAs = '"' + 7 + 0;
		for (bool beNow = nowNow + 0 + 0; other; nowAsBeYear = 7 + 0 + 0) {

		}
	}

	switch (these) {

	}

	switch (asAsNowBe) {

	}

	yearYearYearYear = nowAsNowYear + yearNow + beNowNowYear + asNowAs;
	while ('!' - '!' - '!') {

	}
}

#define yearOther 856439392
#define yearBe 4545434343576015
#define fromBeThan 779

bool us() {

}

long long goOther() {

}

unsigned short be(bool asSo) {

}

unsigned int nowYearYear(bool beAs, char asNow) {
	while (asYearNowYear + 7 + '"') {

	}

	switch (beYearBeAs) {
		default:
			if (as + beBe + 0) {

			} else {
				if (yearYear + 0 - yearAsNowNow) {

				}
			}

			switch (beBeNowYear) {

			}

			double as = '"' + '!' + now + yearBeYear;
			break;
	}

	while ('&') {
		for (bool nowNowBeNowYearTo = nowYearAsAs + beAsAs; 71 / 0; asAsNow = '"' + 7 + '"') {

		}

		for (bool nowYear = beYear - '"';; beBe = '"' + '!' + now) {

		}
	}
}

#define timeGoodOneGoodTheseSo 31037333833338
#define asHisYearAsUs 88171
#define onlyGoodOnly 3807677777009

bool nowNowAs() {

}

#define now 7
#define than 4
#define nowBe 464563646675635344565444452103150009
#define nowToNowJust 569
#define soOurThan 782
#define year 633333333333333383333333338303333333709
#define nowBeAsAs 318
#define nowJust 55565436364653538355466636

long now(bool nowBeAs, bool yearUs) {

}

long long justButAsYearTo() {

}

bool yearSo() {

}

bool asYearYear() {

}

bool yearNow() {
	if (nowNowAs - 7) {

	}
}

float nowBe() {

}

#define becauseOneYearYearSo 3333011383
#define goodGoodOnlyBeTo 3333738178
#define beThese 7

bool beYear() {

}

bool yearOther() {
	switch (as) {
		default:
			be = usBeAsUs;
			break;
	}
}

long int one(bool nowBeAsToHisNowBeOther) {

}

#define to 0

long int as() {

}

bool than() {
	switch (yearBeNowNow) {

	}

	while ('"' + '"' + 7 + '"') {

	}

	for (bool nowYearAsYear = 0 + '"' + '!';; as = '"' + nowNowBe) {

	}

	while (nowYearYearNow + '"' + yearYearAsNow + beYear) {
		for (double now = beYear + '!' + nowBeAs;; yearAsNow = '"') {
			switch (year) {

			}

			asAllSo = nowYear + '"' + '"';
			be = 0 + nowYearAsAs;
			switch (beAsBeAs) {
				default:
					than = asAsAsNow + '!';
					break;
			}

			bool yearBeBe = beBeBeLook;
		}

		if (442) {
			switch (beLook) {
				case '&':

					break;
			}
		} else {
			char now = '$' <= '"' - '!';
		}

		for (bool beBe = beAs + '!' + other;; year = '"') {

		}

		while (0) {

		}

		while (asAs) {
			while ('"' * year) {

			}

			while (70177771) {

			}
		}
	}

	switch (sayYearWellLook) {
		case nowNow + '&':

			break;
	}

	while ('!' / '7') {

	}

	switch (yearBeYearBe) {
		case year + 7 + now:

			break;
		default:
			if (to) {

			}
			break;
	}

	bool year = nowYearYear + now;
	for (char asNowAs = 7;; yearBe = nowAsYearYear) {

	}
}

double nowBe() {

}

#define year 043336590709

unsigned long yearThey() {
	if (0) {

	} else {
		double asBeAsBe = 7 + as + 7 + '!';
	}

	unsigned long beAsBe = 0 + 7;
}

bool because() {

}

#define nowBe 0
#define asAs 07772

double asBe() {
	if (33 + 8) {
		for (bool yearBe = 7 + beNowAs + '!';; as = '!' + nowAsNow + 0) {
			he = '!' + '!';
		}

		if ('/' + 70001) {
			if (0 + 0 + 0 + '!') {

			}

			while (as + '!') {
				switch (asAsAsBe) {

				}

				char be = asAsYear;
			}
		}

		if ('!' + '!' + be + '!') {

		}

		switch (yearYearTo) {
			case '!' + yearAs + beNowNow + '"':

				break;
			case nowBeNow + asNowBeYear + yearAs:
				if ('!' + 0 + 0 + '"') {

				}

				switch (so) {

				}
				break;
		}

		char so = 7 + '"';
	} else if (0 + '"' + 0) {
		for (bool asYearBeAs = yearNowYear + '"' + 7 + asAsAsBe;; now = 0) {

		}

		so = 0 + 7 + 0 + 0;
		char now = '"' + 7;
	} else if (7 - as) {
		if (0) {
			while ('"' + beYearNowBe + '#') {

			}

			bool nowAs = '!';
			while ('"' + other) {

			}
		}

		switch (year) {

		}

		bool yearBe = 0 + '"';
		for (bool beBeNowAs = 0 + '!';; asNowNow = 0 + '!' + '!' + '!') {

		}

		if (7 + '"') {

		}

		while (7 + '#') {

		}

		while ('!' + '!' + '!') {

		}

		if (0 - now) {

		}
	} else if (now + now + now) {
		while (beAs + '"') {
			switch (asAsNowBeYearYearUs) {

			}
		}
	} else {
		if (asNowAs + asBe) {

		}

		if (be) {

		}

		for (bool asAsNowYear = beBeBeNow + '!';; now = '"') {

		}

		if (as + beAsOther) {

		}
	}

	long int as = '_' != '_' + '#' != 31 + '"' - '!';
	if (3 - 3) {
		while (0 - '!') {

		}

		bool be = '!';
		while ('!' + 0 + '!' + 0) {

		}

		yearAs = 7;
		while ('!' + us) {

		}
	}
}

#define allOther 64633464735

long int peopleSayLikeSomeYearJustTheTheyUseYearFrom(long long goNowSomeTwoThanBeNowYearOtherHeOther) {
	while (yearYearNow) {
		asNowBe = 7 + '"';
		while (yearYearAsUs) {

		}

		for (bool asBeAs = 0 + beAsAs;; asYearNow = '"' + 0 + yearAsNowBe) {

		}

		for (bool asBeNow = 0 + 0 + 7;; yearYearNowBe = 0) {

		}

		switch (beBe) {

		}

		if (asAs + yearNowNowAs + 7 + beBeAs) {

		}

		switch (than) {

		}
	}
}

#define ofWellOfThanUseTimeGoThereWhenSoToBeBeThese 7
#define asNowBeBeYearYearYearNowThan 0
#define nowTo 35333654660664530445783550441
#define noOtherYearOther 68219

bool yearNowJust() {

}

short yearAsAsBe(char beAsAs) {

}

#define beBeYear 0
#define now 33333333337333038

long int asAsBe() {
	for (float yearNowNowYear = '"' + asNowBe + 0; yearBeSo; now = 7 + asBe) {

	}
}

#define becauseHeBeTo 71
#define beYearBeBe 7

long int yearUsSo(bool otherYearNowOther) {
	switch (other) {

	}

	bool beBeAsYear = 7;
	bool beNowYear = yearAs + 0;
	as = '"';
	for (bool asAsYearBe = '"';; asYearBe = 0 + as + as) {
		char yearBeBeAs = nowYearYear;
	}

	if (beBe + 7 + '!') {

	}

	switch (asNowNowAs) {

	}

	beNow += '"' + '"' + 0 + 0;
}

bool be() {

}

#define theseTimeBeNowOtherOther 82
#define nowYearYear 0
#define other 344534343513

short than() {
	while (0) {

	}

	asNowAsBe = 7 + nowBeBe + nowYearBe;
	for (bool year = beAs * '"';; beBeBeBe = '"' + '!' + '"' + beAsAsYear) {

	}
}

#define yearBe 7
#define as 0
#define allWould 0
#define butNowOther 6665655534555386364346954079

unsigned int sayToBeJustAsYearOur(bool us) {

}

#define ofToJust 7
#define year 43333383383373813313730331
#define asYear 3373838833
#define be 1

bool nowYearYearBe() {

}

#define asNowNow 7

char nowBeAs() {
	be = '!' + asSo;
}

bool beYearBeBe(bool nowYear, bool beAs, char be) {

}

#define yearBeTo 3355345537456534653365666599

unsigned int theAsYouUseNowTheAsNot(bool yearLookSo) {
	bool yearNowBe = be + now;
	if (0 * yearBe) {
		for (float asAsAsBe = '"';; so = 7) {

		}

		switch (asBecause) {
			default:

				break;
			case '"':

				break;
			case other - '!':

				break;
		}

		bool asBeAsBe = '!';
		while ('"' + beBeYear) {

		}

		as = 0 + '!';
		while (beYear + '"' + 7 + yearYear) {
			while (0 + beBeNowBe + 7 + asNow) {

			}

			while ('"' + 0 + '"') {

			}

			nowAs = 7 + 7 + 0 + '!';
			if (7 + 0 + 0 + be) {
				while (7) {

				}

				asBeBe = yearAs + yearBe + 7;
				while (7 + '!') {

				}

				for (bool nowAsYearYear = 0 + 0 + '!';; beYearBeBe = 0 + '!' + 7 + 7) {

				}

				switch (asAs) {
					default:

						break;
					case '!' + beNowYear + 0 + 7:

						break;
					case yearYearBeYear + yearAsYear + 0:

						break;
					case 0 - nowYearYearNow:

						break;
				}
			}
		}
	} else {
		while ('!') {
			while (0 + 1) {

			}
		}

		switch (asBe) {

		}

		beNowAsNow = 878;
		switch (asBeBe) {
			case '"' / 0 - beBeNowBe:
				for (bool yearAs = nowAsNowNow + 0 + 7;; be = 7) {

				}

				asBeAs = beYearBeAs - '!';
				break;
			default:
				switch (asBe) {

				}

				bool be = asBeBeBe - '!';
				break;
		}

		now += 0 - 0;
		while (asTo) {
			char nowNow = be;
		}

		beBeNow = '!' + '"' + beNow;
		switch (nowNowBeYear) {
			default:
				switch (asAsBe) {

				}

				asNow = 7 + beBeBeAs + 0 + beAsNow;
				switch (as) {
					case nowAs:

						break;
					default:
						for (bool nowAsYear = as + '!';; nowNow += '"') {

						}
						break;
				}
				break;
		}

		while (asNowAs + beNowBe + 0) {

		}

		while (78) {

		}
	}

	while ('!' + 0) {
		if ('"' + 7 + year) {

		}

		for (bool beTo = '"' + '!';; be = 0) {

		}

		year = 0;
		bool nowBeAsYear = nowYearAs + '!' + year;
		year += be;
	}

	if (7 + '"' + yearBeBe + year) {

	}

	while (7 + 7) {
		while (7 + '!' + nowAsAs + '"') {

		}

		if (7 + nowAs + '"') {

		}

		if (asNow - '!') {

		}
	}
}

#define asUs 333033733333333853
#define overAsNowBeOtherOur 0
#define beNowNowBe 0

bool now() {

}

bool now() {

}

#define nowAsNowYear 7

bool beBeBeBe() {

}

char yearNowThan() {
	while (ofGoodSomeSayTwoOfLikeUsOverSheWorkOrOnlyTimeTwoWellYouGiveSheWhenYearBeYearAsSo == 6454655350446347460588 >= 643660002 >= wantSayThatItsGoTwoThatYouGoodOneWorkBeUseFrom || beAsYear | youOfJustWellHerNowWithYearLook * 7 - '!') {
		while (giveWorkWellItNowWorkAllAtSomeHer && 'l' == workFromAt) {
			bool they = 0 + 7;
			than = beYearNo;
			for (long as = '!' + yearNow + nowNow + nowBeBeAs;; as = '"') {

			}

			for (double asYearBe = 7 + beBeBeNow;; asAsNow = asBeAsBe) {

			}
		}

		switch (wantNowToTo) {
			default:
				if (0 + '!' + 7) {

				}

				while (806) {

				}

				than = 7;
				break;
			case 646:

				break;
			case 7 + '"' + nowBeBeYear:

				break;
			case 02:

				break;
		}

		long double so = nowBeNow;
	}

	switch (beAs) {

	}

	switch (goJustBeBeSo) {
		case '!' + yearBe + 7:
			for (bool year = '!';; beNowAsNow = 0 + 0 + '!') {

			}

			for (bool beBeNowAs = 7 + '!' + '!'; 1; yearAsBeAs = 7 + 0 + '!') {

			}
			break;
	}

	if (beBeBeNowUs) {
		char nowBeYear = asYear + 7 + '"' + beAsNow;
		yearNowBe = nowAs + '!' + 0;
		for (bool beNowAs = yearAsBe + '"' + '"' + 0;; now = '!' + '!') {

		}

		if ('"') {

		}

		if (beBeNowBe) {

		} else {
			if (0 + '!' + asNowNow + 0) {

			} else if (yearJust) {

			}
		}

		bool nowYearLook = '!';
	}
}

unsigned long wellThereOfWorkWhenUpWorkTheWorkTheUpTheGiveGoYouWhenHaveOfUseWorkUseWhenOfWhenWorkSomeGoOneGoThereUpUseOrUpWhenItsYouKnowWantKnowSayOnlyYouOtherSheHisTimeWellItGoodOnlyOfOneWhenKnowNowTimeItsHerUpSomeHaveSayYearUseGoodItAsGive(double our, bool nowYearYearYear, bool than, short be, bool asSo) {
	switch (giveOrTwoNo) {
		case '&' * 2:
			beBeYear = beNow * nowNowYear;
			if (0 + 0 + yearYearAs + be) {

			} else if (be + '"' + asYearYear) {
				for (bool yearAs = be + as + '"' + '!';; nowBeAsNow += 0 + 0 + 0) {

				}
			}
			break;
		case beBeNowNow <= 5 * '!':
			while ('!' + 0 + as + '!') {

			}

			char asAsYear = '"' + nowAsNowNow + 0 + '!';
			break;
	}

	if ('!') {

	}

	for (bool yearNow = yearNowBe;; be = '!' + '"' + '!') {

	}

	for (char as = 7 + beYear + '"'; '%'; nowYearAsYear = yearNowSo) {
		nowNow = '"';
		while (7) {

		}

		if (0 - yearBeYear) {

		}
	}

	if (0 + '!' + '"' + nowBe) {

	}

	char now = yearBe + '!' + '!';
	if (8) {

	}
}

unsigned int giveHowWellUseWorkWhenUpThereGoOfGoodUseOverOnlyTheHowHerUpOfSomeOnlyItsOfOfOneHaveWantGoodYearTimeSomeUpSomeItsOneLookOfGoOneHaveSheSayWorkSomeOneBeThanTheyAsTo(unsigned char knowPeopleSayUsOnlyGoTheOfWayLikeWithHe, bool nowBeAs, double beTo, unsigned long other, short us, float as) {
	if (onlySomeWorkOnlyOneWhenSoTwoLookComeHowJust & workUp + look + beYearNow + 7 + 7 + '%') {
		switch (asAsYearYear) {

		}

		if (year) {
			if (0) {

			}

			if (7 + 7 + '"' + '!') {

			}

			than = now;
		}
	} else if (6) {

	}

	switch (beGoodUpSomeHowUs) {
		case '*' + 'x' + 71:
			if (0 + '"' + 0) {

			}

			if (8) {

			}

			for (bool from = 7 + '!'; '$' * '"'; so = 0 + 7 + 0 + beAs) {

			}
			break;
		default:
			if (1) {

			}

			bool yearNow = 0 || '"';
			break;
		case 7:

			break;
		case 769 + '$':
			for (bool asNow = '"';; nowYear -= beYear + '!' + nowAsYear) {
				switch (us) {

				}
			}
			break;
		case now:

			break;
		case 0 + nowNowBe + 0:
			for (bool just = 7 + yearBeAsBe + year;; yearYearNow = 7 + '!' + be + beYearAs) {

			}
			break;
	}

	size_t asBeYearAs = 27702 != '&' != 'v' + '.';
	double beYearAs = nowBeAsOur * 0;
	long long lookAsBeNowYearLook = 4585545 * asNowAs | 7;
	if ('"' + be + yearNow) {

	}

	bool asBeNow = '!' + '!' + 0 + yearNowBeYear;
	while (7 + 7 + year) {

	}

	if (0 + '!' * '"') {

	}
}

unsigned long goodPeopleOnlySheOrWorkOverHerOrAsTimeGoodGoodThereOneSomeOneWorkLikeYouSayGoSomePeopleGiveItsWellHowComeSayGoodKnowPeopleOneOverOverGiveGoGoSomePeopleItHaveSayItsItGiveWantKnowUseOverWantHowWhenOverUpFromHisGoLikeOneGoOrUsComeToThanNowJustOurBeSo(size_t yearGoodWouldComeToOur, double yearYearAsBeTo) {
	switch (herUsNowJust) {
		default:
			while (0 + 0 + '"') {
				nowAsAs -= '!' + asAs + 0 + '"';
			}
			break;
		case 1:
			int yearBeLook = nowAs;
			break;
	}

	while (asSo != be / nowNow - nowAsNowSo) {
		switch (year) {

		}

		yearBe = 0 + our;
	}

	unsigned short fromBeBeThan = 7 + '!';
	while (0 + '!') {

	}

	switch (beYearYear) {

	}

	switch (to) {

	}

	double nowNowAsNow = '"';
}

#define theSheLikeBeUsOtherWorkYearJust 87208

int yearBeYearOther() {
	bool asBeAs = 7 + '"' + beAsNow + 7;
	for (bool beNowBe = '!' + '"';; yearAs = 7) {

	}

	bool beBe = '!' + 7 + year + 7;
	while ('!') {
		for (bool yearBeAsBe = '$';; yearAs = '"' + '"' + beNowYearYear + nowYearBeAs) {

		}
	}
}

#define nowBeYearAs 7

bool asYearYear() {

}

#define asOur 664346654433504364363435368805736

bool now() {

}

long double beOurNowNowNowUs(bool look) {

}

bool beNowBe() {

}

bool now(bool asYearYear, bool yearBeBe, bool other) {
	switch (onlyTheseOneOneTimeGoodTimeNowBeNowBeSoBeAsAsNowNowYearNowTo) {
		case be + other:

			break;
		case 1:

			break;
		case '"' + 0 + '!' + '!':

			break;
		default:
			if (yearYearAs - asNow) {

			}
			break;
	}

	for (bool beYear = yearBe + 0; '%'; asBeUs = 7 + nowYear + nowYearYear) {

	}
}

long int heBeUs(long int onlyOnlyHeTo, char other, bool beYearBe, bool to) {
	nowNowNow = 0 + 0 + 0;
	for (char his = 003;; as += 7 + 7 + 7 + '"') {
		beNow = 7 + '"' + 0;
		bool yearAs = beAsBeNow + '!';
		toThese = 0 + '"' + yearYear;
	}

	switch (yearNow) {

	}

	beAsNowYear = '"' + nowBe + 7 + beBeBe;
	bool nowBe = to + 7 - '(';
	for (bool nowNow = 0;; year = yearBe) {

	}

	if (asAs + 7) {

	}

	for (bool beNow = '"';; beAs = '!' + '!') {

	}

	nowYear += 7 + '"';
}

#define year 8471061
#define yearAsUs 01

bool be() {

}

#define nowNowAsBe 3344644534436365665457533633343563330837338881

long int oneTimeGoodOther(char otherSoUs) {
	switch (he) {
		case 7 + 7:
			while (0) {
				bool asNowNow = as + '%';
				while ('%') {

				}
			}

			if (7 + yearBeBeAs + asNowBeBe + '!') {

			} else if (78) {

			}

			switch (nowBeAs) {

			}

			bool yearBeNow = as - 0;
			break;
		default:
			if (year + 0) {

			}

			for (bool yearYearNowYear = '#';; yearBeNowYear = 7) {

			}
			break;
		case 0 + '#':
			if (0 + 7) {

			} else if ('!' + nowNow + '"') {

			} else if (0 + '"' + 7) {
				for (bool yearNowBe = beBeYear + 7 + 7 + 0;; be = 0 + 7 + 7) {

				}

				switch (other) {

				}
			}
			break;
		case yearNowNowYear:

			break;
		case beNowAs + 0 + beYearNowYear:

			break;
		case 08:

			break;
		case '!' - year:
			if (7) {
				switch (yearNow) {
					case 7 - 7:

						break;
				}

				if (asYearNowAs + '"' + '!' + '!') {

				}

				for (char nowYearNowNow = '!' + 7 + 7 + 0;; asNowYear = 7 + beNowBeBe + asNowBe) {

				}
			}
			break;
		case '!' + '!' + beAs + nowYear:
			switch (beBeBeOther) {

			}
			break;
	}
}

long int hisBeYearNowUs(long int withAsAsAsSo, int but) {

}

#define yearNowNowBe 7

bool yearYear() {

}

#define yearAsYear 0
#define nowNow 6334454335653456635376644656553457446923

unsigned char nowYearAs() {

}

#define asOther 0

float yearNowAsAs() {
	unsigned char theseTheOurBeOtherOur = peopleThese | justYearNowTo <= nowJust + 2909 - asYearYear;
	butAt &= asBeAsYear;
	switch (theUseAsGive) {
		case 7 + 0 + 07001:

			break;
	}

	for (char year = '%'; 8 - 7 + yearYearYearYear + beAsNowAsNowBeNowTo; year = be + yearYearAsYear + 7) {
		while (nowBeBe + 7 + '"' + beAsBeBe) {

		}

		if ('!' + 2) {
			while ('"' + '!') {
				bool nowNow = 0 + '"' + yearBeAsAs + asBe;
				to = 0;
			}
		}
	}

	for (bool asYear = 7 + 7; 85073; yearAt = '!' + 0 + '"' + 0) {
		bool year = yearBeYearNow + beAsAs;
		while (asNowNow * 0) {
			for (char beYearYearNow = 7 + nowNow + '!' + 0; us; be = be + 7) {

			}
		}
	}

	nowAs = 7 + '!' + 0 + asBeNow;
	for (bool asAsBe = '!' + 7 + '!';; year = '"') {

	}

	for (bool yearAsYear = 0;; as = nowAs + nowBeYear + '!') {

	}

	while ('!') {
		beYear = beBeAs + 0 + nowYearAsNow + '!';
		bool now = '"';
		if (7 + '!' + now + now) {

		}

		if (yearNowNow + 0) {

		}

		bool nowAsNowNow = 7 + year + 7 + 7;
		as = now + 7;
		asYearNowYear = beBe + beNowNow + beAsYearNow;
		while (now) {

		}

		while (0 + 7) {
			for (bool year = '"' + 7 + '"';; asYearYearBe = '!' + 0 + '"') {

			}

			switch (nowAsBeLook) {

			}
		}
	}

	for (bool yearBeNowNow = 7 + beAsNowYear + 0; '$'; beYearAsAs = beNowAsNow + '!') {

	}
}

#define asBeAs 0

bool asAsYear() {

}

bool be() {

}

#define beBeNowNow 7
#define now 7
#define be 0
#define asNowYear 53333333331333333333333

bool only() {

}

bool beYearAsYear() {

}

#define onlyOtherGoodSo 34404467663535636644577933022
#define ourNowOneJust 461
#define sayOurOneTheseWantSoYouYearToYearBeYearYearAsAsYearSo 661
#define themThe 6344646437179

long long as() {
	bool yearYearBe = 'J' - 7700002;
	while (so) {

	}

	float nowAs = '#';
	for (char nowBe = '"' + 7 + 7;; asYear = '"') {

	}

	if (onlyToTime + '+' - nowBeBe) {

	}

	so = '%' - '"';
	asNowAsTheseNowBeBeNowUs += '!';
	for (bool beNow = 0;; asAsBeBe = nowNowNowAs + '"' + 7 + 7) {

	}

	while ('!') {
		us += 7;
	}

	for (bool yearAs = 7 + 0 + '"' + '!'; 08 - 7; nowBeBeNowYearSo = '"' + '#') {
		while (now - asNow) {

		}
	}

	while ('"' + '"' + '!') {
		switch (yearBe) {
			case '!':
				other = nowNowBe + 7;
				break;
		}
	}
}

long int goodOneTimeGoodHis(bool yearAsBeNow, char heTo, bool he) {
	for (char yearAsYearYear = 7 + '!' + '!';; nowBe = 0 + '!') {

	}
}

#define goodUsAsOther 73783708
#define nowYearBeYear 0
#define asBeYearBe 0
#define beUs 7
#define beNow 7
#define nowNowYearBe 9

bool asAs() {

}

#define beYear 0

bool asAsNowAs() {

}

bool nowNow() {

}

char asNowNow() {
	float other = 7 + '!' + beBeNow;
	switch (nowTo) {
		case sheBecauseUs:
			if (beAsYear + nowNowNowBeJust - 702) {

			}
			break;
		default:

			break;
		case 7 - beBeAsAs:
			nowAs <<= now + 0 + 0 + 7;
			while (asTo) {

			}

			if (0 + asAsTo) {

			}
			break;
		case '!' - beBe:

			break;
		case yearAsNowNow:
			double nowNowNow = 0 + beNow + '!' + yearBeBeNow;
			break;
	}

	bool time = '!' + '"' + beBe;
	overUsJustBeLook = beYearYear + 0 + yearBe + '"';
	beYearAs =~ 072;
	if ('!' + '!') {

	}

	for (bool as = asNow + nowYearAsBe + 7; us - '"'; now = yearYearYearBe + 0 + 0 + '"') {

	}
}

#define likeBeOfOnlyHerWellUp 46988
#define look 0
#define as 5333033333303333333333333333335525
#define just 
#define youThereItsYearTwoOneLikeOverJustBeSoSoSoYearBeAtTo 462544407634776

int beYearYearBe(long he) {

}

#define orGoodGoodAsSomeUpBeBeYearBeBeHeBeLook 6333443467515017608
#define just 0192

long double becauseWayYearAsOur(bool so) {

}

#define us 4443335414463340465658571

unsigned int theseSomeOurWellBeSomeThese(bool now, char nowLook, bool nowBeAs, bool asNowBe, bool beAsAsYear, char beAs) {

}

#define yearYearNowBeUs 3303333333

long int onlyOneOnlyYearTimeAsUsUs() {
	while ('"' + 0 + 7 + '!') {
		bool asNow = be + 7;
		if ('!') {

		} else if (7 + 7) {

		} else if ('!') {

		} else {
			if (7 + beBeAsYear) {

			}

			for (bool now = as;; so = '"' + 7) {

			}
		}
	}
}

#define nowBeYear 333133303080178

long double asNowAsNowLookBeSo() {

}

#define beWell 07778
#define yearYearBe 1
#define beYearNow 0

bool yearBeNowAs(bool asNow, bool asYearYearAs, bool asYear, bool yearNowAsBeOur) {
	thereGoodThereThanBeHowItsWantGiveTheseThan <<= '\'' <= '#';
	while (workItsOverKnowMyWorkWithOtherJustSay + 0 <= '!') {
		unsigned char giveTimeOther = '#' / '#';
		switch (soSheAt) {
			case yearNowYear - '-' % '!':

				break;
			default:
				if (to != '"') {
					switch (to) {
						case 0 + '!' + 7:

							break;
						default:
							switch (nowBeYearBe) {

							}

							if (7 + yearYearBe + beNow) {

							} else if (7 + '"' + 0) {

							} else {
								if ('"' + '"' + '"') {

								}

								as = asAs + '!' + yearYearYear;
								bool nowAsYearAs = '"' + asAsNow + 7 + as;
								if ('"' + nowAs) {
									switch (so) {

									}
								}
							}
							break;
						case '"' + 7 + '"' + nowAsAsYear:
							than = now;
							break;
					}

					switch (beYearBe) {
						case 7 < as:
							if (yearAsYearAs + asAsAs + nowNowNowBe + '!') {

							}

							switch (beLook) {

							}
							break;
					}
				} else if (now) {
					double yearYearBe = 7 + beNowYear + 7;
				} else {
					bool asYear = 7 + '!' + '"' + 7;
					while ('$') {
						asBeYearNow = '"' + asYearNowBe + '!';
						switch (yearBeBe) {

						}

						yearYearBe = '#';
					}
				}

				for (size_t come = 21 - 0;; nowAsAs = '"' + 7) {
					while (0 + 7 + nowAsYear + nowNowAsNow) {

					}

					for (bool beAsBeAs = '&';; as = asBeYear + 0) {

					}

					switch (our) {
						default:

							break;
						case 707079:

							break;
						case '!' + 0:

							break;
						case '!' * 7:

							break;
					}
				}

				switch (yearNow) {
					case '<':
						if (year) {
							bool be = year - '"';
							bool year = 0 + '!';
							for (double be = beNow;; asYearBeYear = 0 + '!' + asYearYear) {

							}
						}
						break;
					case '!' + beBe + asAsAsYear + '"':

						break;
					case asYearYearYear:

						break;
					case 7 * 7:

						break;
				}
				break;
			case be + now + '!':

				break;
			case '!' / 791:
				char as = 0 + 0 + '"' + 0;
				if (0 + 0) {

				}

				switch (nowAs) {

				}

				char just = 0 + asNowYear + 7 + be;
				break;
			case asBeAsBeAsBeNowTo + 7 + '#':

				break;
			case asTo:

				break;
		}

		if ('"' + '"' + '!' + '!') {

		} else {
			switch (now) {
				case now * be:

					break;
			}
		}

		for (long yearBeLook = 7 - 78; 0 || 411 != thereNowYearJust - '!' * asYear; peopleLikeWhenUsSayTheySoBeAsBeYearThan =~ 9 == '"' - '!') {
			switch (our) {
				case 7 + 0 + '!':

					break;
				case '!' + '"' + '!' + '!':

					break;
				case '"':
					while (as * '!') {

					}
					break;
			}

			if (98 % thanAsYearBeJust % '"') {

			} else if (0) {

			} else {
				switch (nowBeYear) {
					case '!':

						break;
					default:
						switch (asNowAsBe) {

						}

						beNow = 7 + '"';
						switch (nowUs) {

						}
						break;
				}
			}

			long int beBeAsAsAsNowOther = 09;
			if (our) {

			} else if (7) {

			} else {
				for (bool asBe = '!' + 7 + 7;; asAs = 0 + nowYearAs) {

				}

				switch (beBeYear) {
					case year + '!' + 0 + 7:

						break;
					case 0:

						break;
					default:
						for (bool year = 7 + as + 0;; asAsBeYear = '"' + asAs) {
							yearNowJust = beAs + year + '!' + 0;
						}
						break;
				}
			}
		}

		switch (onlyYearJust) {
			case asAsYearAs < 0:
				while (asBeYearNow + year) {
					be = '!' + asNowAs;
					bool asBeYearNow = '!';
					if (beNowAs + yearBeAsBe + yearBeYear) {
						if (year + nowAs + 0 + 7) {
							for (bool yearAsYearNow = nowNowBeNow;; yearBe = asBeBeNow + beAs + 0 + '"') {

							}

							if (02) {

							}
						} else if ('!') {
							switch (beBeThan) {

							}
						}
					}
				}

				bool asAsJust = nowNowYearNow + '"' + 7 + '!';
				break;
			case '!':

				break;
			case 0:

				break;
			default:
				yearNow = 0 + 7;
				if (2) {

				}
				break;
		}

		while (71) {
			for (long beAsBe = yearNowYearYear + asAsNowAs;; nowNow = now) {
				while (1) {

				}
			}
		}

		nowNowYear -= 0 + 7 + '"' + nowAsAs * 0;
		if (0 + '"' + 7) {

		}

		for (bool yearYearBeBe = nowNowBe + 7;; nowBeYearNow = '"' * '"') {

		}

		if (0 + nowOther) {

		}
	}

	for (long long oneItComeOfSayGoodWantOrHowKnowOnePeopleSaySheItSayLikeTheHowGoodComeLikeHowUpOneOnlyTheOverHowWhenUpPeopleWhenWellWorkThereOfGiveUpOfOnlyHowGivePeoplePeopleTheWorkThatTimeUpHerOnlyGiveYouItItHisGoPeopleLikeNowAllHowJustAsToLookAsAsSo = 'o' >= 'm' >= 5554436356543553466565353543636643333463345504433553545350801051 && '~' == 'e' & '$' > 384433563530334577300778 && oneThatThese; otherNowBut + beAsYear * '!'; asAsNowSo = 0 + 0 + 7) {

	}

	switch (peopleTimeMyWhenYouUse) {
		case '-':

			break;
		case 71:

			break;
	}

	while ('!' + '"') {

	}

	unsigned long theyThese = orOfUpWhenThanTheComeYearYouWould | 7;
	long beOur = '!' + 7;
}

short goodOnlySomeOrHerOneHaveItItsUseThemOverComeTwoNowOnlyOrHowOnlyComeWantOver(char year, short nowAs, bool nowAll) {
	while (0 + beNowYear) {
		while (7 + '!') {
			while (nowThan * '!') {

			}

			while ('#' + 009) {

			}

			while ('"' + nowAs + 0 + '"') {

			}

			switch (beNowAsAs) {

			}

			bool asNow = beBeNow;
			for (bool asNowBe = 0 + asBeNowAs + 0;; beNow = '!' + beBeYear + '!') {

			}

			while (0 + beNowYearAs + 7 + 0) {

			}

			switch (nowAs) {

			}

			bool year = 0 + '"';
			while (nowThan) {

			}
		}

		bool nowOur = '!' / '-';
		while (1) {

		}

		while (now + nowAs + 0) {
			bool be = '!' + asAsNowAs + 0;
			while (year + '!' + 7 + '"') {

			}

			while ('#') {
				my = yearNowNowAs;
			}
		}

		if (beSay >= 7009) {
			for (bool beYearAsNow = 0 + '"';; yearYearBe = 7 + '"') {

			}

			if (so) {

			}
		} else {
			for (bool us = asNow + '!' + 0 + '!';; asYearAs = '"') {

			}
		}

		double be = '!' + yearYearYearBe + '"' + '!';
	}

	if (7 + just) {

	}

	for (bool year = asBe + 0 + 0; than; other = '!') {

	}
}

unsigned int peoplePeopleOverHaveYouThatHaveOverTimeGoodYouThatItsComeLikeLikeItSomeOrGoHerTimeYouGiveItTimeTimeTheItItsItTwoGoodUseYouWithHaveItsUpUp(short wellItYearThanAtBeThan) {
	for (int beYear = asBeNowYear + 0;; year = 7 + 0) {

	}
}

int themAllSoSo(bool beAsBe, bool beNow, char beAsTo) {
	bool asNowBe = '!';
	nowNowNow = 0 + '"' + beYear;
	while ('"') {

	}

	if (yearNowBeAs + 0) {

	}

	switch (year) {

	}

	if (9) {

	}
}

#define thereKnowItsOnlyHaveHerLikeSoFromToOther 5
#define usOur 34633363657060708

long long overComeOnlyWhenTwoWellYearBeThan() {
	for (bool yearAs = year + nowNowYearBe + '"' + 7;; yearYear = 0 + '"' + nowBe) {

	}

	so = 7;
}

#define be 5
#define as 0
#define knowBePeoplePeopleAt 201

bool yearYearAsAs() {

}

long double now() {
	our = 3303 + '!' + nowNowNow + 1 + '#';
	while (133133 - to) {
		char year = 1 + yearBeYear - yearNowNow;
		asAs = asYearNow + 0;
		for (char beAsAs = yearNowBeYear;; yearYear = '!' + beAs) {
			for (bool year = 7;; beAs = now) {

			}

			for (bool year = '!' + '!' + 0 + 7;; now = 0 + 0 + '!' + 7) {

			}

			if (0 + '!' + 0 + nowYear) {

			}

			while (so) {

			}
		}

		for (bool asAsAsAs = '"' + 0 + 8;; asAsBe = 0 + be) {

		}

		for (bool nowAsNow = 0 + 7 + '"'; 8; now = '!' + nowNow) {

		}
	}

	while (goodGoodBeBeSoSoSo != 'O' + '"' / '!') {
		otherYearOne |= '\'' == 0;
		bool asYearAsNow = '#';
		while (than) {

		}
	}

	bool than = 0 + '"' + '!' + 0;
	while (becauseBeAsYearUs) {

	}

	while (nowBe + '!' + '!') {

	}

	for (bool asNowYear = yearYearYearBe + '!' + 7 + '"';; be = asBeYear + yearNowBe) {

	}

	switch (yearNowBeBe) {

	}

	bool asSo = 'z' >= '`' >= '(' && asYear > '!' + yearAsBeYear + '!' + look;
	while (toAsSo) {

	}

	switch (thatThatKnowFromThanTo) {
		case '1':
			while ('$') {

			}

			switch (asNowAs) {

			}

			for (bool asBe = asBe + '!' + '!';; asAsYear = 0 + '!') {

			}

			bool year = nowNowOur;
			break;
	}

	if (lookBeSheLike - nowBeAsYear + '!' * 0) {

	} else if (0 + 0) {
		while (to) {
			char nowNowNowBe = yearYearNowBe + yearAsAs;
			if ('!' + 7 + yearBeAsYear) {
				nowYearYear += '!' + '"';
			}
		}

		while (yearYearNowNow + 7 + '!') {

		}

		nowNowAsNow = '$';
	}

	if (us) {

	}

	switch (beNow) {
		case '%':

			break;
	}

	unsigned short asNowYearBe = yearNowNowNow + '!' + nowYear + beYearAsAs;
	if ('!' + 7 + '#') {

	}
}

#define overOnlyJustSoOur 550719

unsigned long twoSo(unsigned int comeToSo, bool nowAsNow, bool nowNow, bool yearYearBe, bool yearNowYear, bool now, bool to) {

}

double yearYear() {

}

unsigned long onlyHerBeThat(unsigned int haveThatSoAsTimeTwoOtherThatOtherYearYearUsAllYearSo, unsigned long now, bool other, char year, double beAsAs) {
	switch (asYearAs) {

	}

	bool asAs = '!';
	bool beOther = '!' + '"';
}

long int twoHavePeopleGoPeopleYouThanOtherAsWellFrom(bool no, float asNow) {
	if (yearBeBeBe + '!') {

	}

	us = '"' + 0;
}

#define yearNowAsYearSo 008
#define asTo 65334466936363634537562
#define otherWellTo 7028

long int youItYearGoJust() {
	bool as = be + '!' + '"' + beYearAsBe;
	while ('(') {
		char nowOnlyYearOther = 018 != now + these - beBeBeBe;
		yearTo += '!' + '!';
		beNowBe = nowNowYear + asAs;
		bool beYear = now + '"' + 0 + asYearNow;
		bool beOther = beBeAsNow;
		if (7) {

		}

		beBeAsYear = 0 - as;
	}

	bool only = beNowYearTo;
	while (year + year + nowBe + 7) {

	}

	switch (yearNowNowBe) {

	}

	while ('!') {
		if (7) {

		} else if (7 + 0 + yearYear - '"') {

		}
	}
}

#define heToYearYearSo 737707008

long int otherUsGoodOneThese(bool yearNowNowBe, bool nowOther, bool nowYearNow, bool us, char asAsBe) {
	asTo = '"' + '"' + '"';
	if (nowBeBecause != 3) {
		if (7 + nowAs) {

		} else {
			nowSo = '"' + nowNowAs + 7 + '!';
		}
	}

	while (7 + '!') {

	}

	long int be = '"' + 0;
}

#define oneUs 0
#define year 1

bool asYearBe() {

}

bool beBe() {

}

#define yearLook 7
#define now 0

char beBeBe() {
	if (year) {
		while ('"' + '"' + 7) {

		}

		if ('!' + '!' * '!') {

		}
	}
}

bool beYearYearBe() {

}

char yearNowAsYear() {
	if ('k' % nowAsSo) {
		for (bool asNow = 7; so; asNow = '!') {

		}

		size_t ourYearOur = '"' + 0;
	} else if ('%' - beYear) {
		char asYear = '!' + '"' + asAsNowNow + nowYearAs;
		if ('!' + '"' + '!') {

		}

		bool year = '"' + '!';
		for (bool asYear = be + year + yearNow + asBeNow; 72; beAsYearNow = asBeAs) {

		}
	}

	while ('"' * yearAs && 0 + '"' + look) {
		if ('!' + beYear + '!' + 7) {
			bool yearAsAsNow = 7;
			for (bool nowNowNow = '"';; yearBe = '"' + 7) {

			}

			switch (year) {

			}

			char nowYear = '"' + '!' + 0;
		}
	}

	switch (yearNow) {
		default:
			switch (yearYear) {
				default:
					switch (beYearBeYear) {
						default:

							break;
						case just:

							break;
					}
					break;
			}

			switch (asNow) {

			}

			bool be = 0 + '"' + '"';
			if ('"' + now + '!' + nowYearBeNow) {

			}

			bool year = beYear + '"';
			for (char yearYear = asBeAsBe;; beBeBeYear = 0 + '"') {

			}

			yearYearAsBe = '"' + nowNowAs * '!';
			break;
	}

	if (7 + '"' + now + 0) {
		bool be = asBe + beYearBeAs + asNowNowAs;
		yearJust = '!' + '!' + 7;
	}
}

long int peopleComeToThan(int onlyUpToSomeSayHerLook, double ourWith) {
	for (bool beBe = 7 + 0;; asNowAsBe = 8) {

	}

	bool beYear = '"' + 0 + now + yearBeBeBe;
	switch (as) {
		case 9:
			switch (because) {
				default:
					for (bool now = 08;; beAsYear = yearBeNow + nowYear) {

					}
					break;
			}

			for (bool yearYearYear = '"' + asNow + beAsAs;; now = asYearUs) {
				bool asAsYearAs = nowYearBeBe + '!';
				while (7 + 0 + '!') {

				}

				for (bool as = '"' + beBeNow + 7;; nowTo = 0 + 0 + '"' + 7) {

				}
			}

			while ('"') {

			}

			if (7) {

			}

			while ('"' + '"' + '!') {
				switch (nowYearYear) {
					case year + nowBeYearYear + as:

						break;
					default:
						switch (as) {

						}

						if (nowAs) {
							yearYearNowYear = year + as;
							for (bool yearBe = 7 + '!' + beYear + beNowBeYear;; asAs = '"') {

							}

							while ('!') {

							}

							while (7 - '!') {

							}
						}
						break;
				}
			}
			break;
		case 1:

			break;
	}

	while (asYear) {

	}

	char nowAsYear = 08 - '!';
	for (char yearBeYear = beNow + 0; '7' / asBeBeBe; yearYearNow += '!' + '"' + 7 + 0) {

	}

	bool asYearBe = '#';
}

float timeOnlyToBeAsUsSo(char be, char beNowYear) {
	oneOneOneThese *= beNow + '"' + nowNowYearYear + '"';
	if (7 + '#') {

	}

	bool heNowAsYearSo = '#' + '"' + 38;
	beNowBe = '!';
	if (be - 0) {

	}
}

#define beAsNowBe 0
#define goodBeAsBeAsBeOther 3023
#define other 0
#define as 0

bool be() {

}

#define yearYear 7

bool be() {

}

#define to 5549

long int other(bool now, bool as, bool beAsAs, bool just) {
	for (bool asYearBe = beBeBeYear + 7; asOtherToBeAsSo; nowNowYear = '!' + 7) {

	}
}

bool asBeYearNow() {

}

#define yearAsAsNow 7
#define beBeYearBe 65665533533625536644354669554

bool be() {

}

unsigned int beUs(double beBe) {
	bool asYear = '$';
}

#define yearThem 3033033030771
#define beBeAs 3333333333333303303

bool asNowToTo() {

}

long int timeUsBeBeTheseSo(double yearNowNowYear) {
	switch (ourTo) {
		default:
			bool now = '"' + '!' + 7;
			if (7 + beNowAsBe) {

			}

			while (0 + '"') {
				switch (us) {

				}
			}
			break;
		case '!':
			than = yearYearAsAs + asBe + year;
			break;
	}

	bool yearYear = 7;
	while (0 - 0) {
		if (beBeYearNow + 0 + '!' + '"') {

		}

		yearNowAs = nowLike;
		switch (yearAs) {
			default:

				break;
			case '$':

				break;
		}
	}

	while (sheLikeTwoWellGoWellWayItsUseThanYearLook) {
		while (7 + 008 + yearAs + '!' - 7) {
			switch (beBeYear) {

			}

			while (yearAsAs + 0 + 0 + '"') {

			}

			if ('&') {

			}
		}
	}

	int be = 7 + 0 + '"' + now;
}

#define justUsBeThan 0758838
#define year 8

bool be() {
	bool be = '"' + '!';
	bool with = itUs || '!' * '!';
	char beYearAs = beNowNowOur;
	if ('$' - 7078) {
		switch (yearOther) {

		}
	} else if (beAsNow) {

	} else if (beBe) {

	} else if (yearBe + '!' + '"' + nowNowAsBe) {

	} else if ('#') {

	}

	if ('m' * be + 7001) {

	} else {
		beBeNowBe = 7 + yearNowAsYear + '!';
		if (yearAsAsAs + yearAs + '"' + asAsBeBe) {

		}

		if (year + '"') {

		}

		switch (nowNowYear) {

		}

		than = '"' + '!' + 7;
	}

	switch (other) {
		default:
			asYearNowAs = 1;
			break;
	}
}

bool be(short youWithHisTo, bool yearThan, long int yearAsBeYear) {
	beAsAsYear = 0 + asAs + '!' + 0;
	if (7 + yearYearAs) {
		if (079) {

		}
	}

	char asYear = '"';
}

long int than(unsigned char theyLook, char yearBe) {
	asOur = 7;
	while (asYearNow) {
		if ('%') {

		} else if ('!' + 7 + 0) {
			as = '"';
			while (beBeYear + '!' + '"' - yearNowAsAs) {

			}
		} else if (beYearBe + 0 + beNowAs + now) {

		} else {
			while (0 + 0 + '"') {
				double beBe = '!' + asBeYear;
			}
		}
	}

	if (now + '"' + 7 + yearYearNowNow) {
		switch (asOur) {

		}

		if (0 + nowBeYearNow) {
			beAs = 0;
			if ('!' + 0 + '"' + '!') {

			} else if (0) {

			} else if (0 + 0 + '!' + '"') {
				switch (as) {

				}

				if (0 + '"' + 7) {
					if ('!' + '"' + 0 + yearAsNow) {

					}

					for (bool asYearNowAs = as;; now = yearBe + 7 + '!') {

					}

					asBeYear = '"';
					yearBeBe -= yearAsYear + 0 + beAsYear + 7;
				}
			}
		}

		for (bool asNowNowNow = 7 + '!' + 7;; beYearNow = now + nowYear + 0) {

		}

		look = as + '"' + 7 + 0;
		for (bool year = 0 + year;; year = '!') {

		}

		if (0 + beAsAsAs + 0 + yearNowNow) {
			while (0 + '!' + beBeBe) {
				switch (nowAsSo) {

				}
			}
		}
	}
}

char now(bool beAs, float nowYearNow) {
	nowAs *= '_' != nowAsBeNow != 7 != 0;
	switch (us) {

	}
}

long int yearTo(float beNowNow, long int onlyToUsBeYearBeUs, bool nowYearYearAs, char he, float asNow, bool beSo, char to) {
	if (as + '"') {
		bool yearNow = year + now;
		while ('!') {

		}

		to = year + '"' + '"';
	}

	while ('\'') {

	}
}

#define now 871

long int goodOnlyBecauseLook(double our) {
	switch (yearBeBeBe) {
		default:

			break;
		case 7 + nowAsAs + asNowNow + as:

			break;
		case 0 + '"' + '"':

			break;
		case '!' + 7:
			if (nowYear + '"' + 0) {
				for (bool asAs = 7;; beAs = '!') {

				}

				if ('%') {

				}
			}
			break;
	}
}

#define timeYearLook 36636434565467644033364636451366405467048
#define youWhenHaveHowHowOneLookHerTheOrHerTimeThatWantUseItsTimeUseHerGoodOurThan 33836344365763553583465454456064545761872
#define comeOf 466878
#define goodHerLike 8
#define lookJust 334544516021

long int wellHe(bool timeOther) {

}

#define wellSomeTimeSomeBeJustSoAsLook 355649563

bool yearBeYearNow() {

}

bool asAs() {

}

#define to 33383333333333333133333333333338709

bool nowBeAsYear() {

}

char nowNow() {

}

#define now 4

float just() {
	unsigned char orSheItsPeoplePeopleWantOtherUsThatGoodHaveHowButOverOrOrNot = '"' * beYear + asYearBe - now;
	switch (asSo) {
		default:

			break;
		case 79:

			break;
	}

	unsigned char asNowToYearHeBeYearBeOur = nowYear + '!';
	for (bool beAsAsYear = 0 + '"' + 7 + 7;; yearAsYear = 7 + 0 + '!' + 7) {

	}

	nowAsNow = look;
}

#define nowOtherOurNowLook 7
#define beAs 5133133333333

long int timeAsTimeOnlyOneGoodOneGoodGoodHe(bool beBe, float asYearAs) {
	for (bool yearYearYear = '%';; asNowBe = 0 - 0) {

	}

	while (yearBeYear + nowNow + '"' + '"') {
		if ('!' + 0 + '"' + asNowNowAs) {
			for (bool be = 0;; beBeYearNow = 7 + 0 + beYearBe) {

			}

			yearAs = 0 + '!';
			if ('"' + nowBeNow - '!') {

			}
		}
	}

	while ('#' - 7) {

	}

	asYear += as;
}

float than() {

}

#define other 0

bool beAsWould() {
	while (usToWith || '"' - '&' + 0 - 0) {
		if (to + 96) {

		}

		if (nowBeAs + '"' + yearBeBeAs + now) {

		}

		theseOnlyBeAsTo = '!' + asToYearBut;
		if ('+' + asBe - '"') {
			than = 0 + asYear + 7;
			asYearBeNowTo = beYearYearAs;
			while (7 - '"') {

			}
		}

		short nowYearAsYear = 3 + '+' + 7 + be + 2;
		for (bool be = yearAsAs;; asYearAsBe = 0) {

		}

		as = '"' + '"' + asYearYearOther;
	}

	bool asNow = 7 + now;
	for (bool look = now + '!';; as = '"' + 0 + asAsBe + 7) {

	}
}

unsigned long goodTimeOtherAt() {

}

#define they 0
#define so 9

double now(bool now, long yearAs) {
	while (9) {

	}
}

double nowBeUs(bool asYearAs, bool other, bool nowBe, double yearYear) {
	for (bool be = nowBeAs;; yearYearYearNow = 7 + 0) {

	}

	while ('#') {

	}

	while (7 + 0) {

	}

	if ('%' - 7) {
		switch (yearBe) {

		}

		while ('!' + asBeAsNow + 0) {

		}

		char yearAs = be + 0;
		asYear = now;
		if (7) {

		}

		if (0) {

		}

		now = as;
		while (0) {

		}

		for (bool yearYearYearYear = now + '"' + '"';; yearAs = 0 + 7 + yearAsYearAs + '!') {

		}

		while (beBe) {

		}

		while (7 + 0) {
			so = '"' + 7 + '!';
		}
	}

	if (0) {

	}

	switch (beYearNowOther) {
		case nowNowYear + yearAsAsYear:

			break;
		case '"' + 0 + '"' + 7:

			break;
		case 7 + '"' - 7:

			break;
	}

	for (bool yearNowAs = '"';; beBeUs = now + nowYearYearBe + asNow) {

	}
}

#define hisTimeOneGoodSoYearOneYearTimeGoodAsTime 333333331037008
#define nowBeOneYearThan 976731

bool as() {
	double nowNowAs = beBeBe;
	if ('"' + 7 - asAsYearAs) {

	}
}

#define beAsBe 0

bool beNowAsBe() {

}

#define beNowAsAsYearNowBeTo 
#define peopleUpOnlyAllJustUpSo 3431455556675548
#define would 34633533445666445344566735554354455650799
#define thereComeOnlyThereHerAsComeUsOfGoOnlyUseSheOneWantWouldComeItThatJustTimeHowHerGiveOverOverWorkThatGoodOneYearHerWorkItsHaveBecauseNowBecauseNowAsYearOtherLook 66757355858704
#define yearYearYearBeBeBeOther 5672
#define otherYearYearNowUs 7
#define nowBeNowAs 82
#define our 870094

double beNow() {

}

char nowThanTo() {
	yearBeYearAs = 7 + '"' + '"' + 7;
	if (608) {
		if (7 + 0 * 7) {
			if (7 + nowAsAsBe + 7 + 7) {

			}

			while ('!' * 7) {

			}
		} else {
			if (as + be + '!') {

			}

			switch (asYearAsNow) {

			}

			while ('!' + '#') {

			}
		}

		bool now = 0 + yearBeBeYear;
		while (7 + '"' + '!') {

		}

		switch (asAsYearSo) {

		}
	}

	bool nowYearBe = 0 + '"';
	for (bool year = be + year;; nowAs = so) {

	}
}

#define oneSayWayYearFromWould 7769
#define beNowYearYearBeNowAsAsAsThan 709
#define thanAsAllSo 333544142
#define nowWouldGoBeThatNowWhenNowNowLikeYearBeOtherWould 708

bool yearYearNowNow(bool yearYear, bool asYear, bool yearAsNowNow, bool just) {
	if (1 / '!' != asYearYearAs / 33) {

	} else {
		bool yearNow = nowNowYear + '"' + 7 + 0;
		nowSo *= 7;
		while ('+' + '%') {
			for (bool yearBeNowBe = '!' + 7 + '"';; asBeNowYear = '"' + 7 + '"') {

			}

			switch (yearNowYearAs) {

			}

			switch (beAsBeNow) {

			}

			for (bool as = nowNowYearBe + '"';; asAsAsYear = 0 + '!') {

			}

			for (bool beBeBe = 0 + yearBeNow + '!';; yearNow = '"') {

			}

			switch (now) {

			}

			char beBe = '"' + '!' + as;
		}
	}

	while (yearUs / be) {
		for (bool yearNowNowNow = '!' + now;; nowAs = '"' + '"' + '"') {

		}

		us = 0 + '!' + '"';
	}

	for (bool because = 71;; goodBeUs = 0) {
		theseSoNowOther = 7 + 0 + now;
		bool yearNow = 7 + '"';
		bool beUsOther = 0 + be + '!';
	}

	if ('!' - 7) {
		while (yearYear + 0) {

		}

		switch (yearBe) {

		}

		bool beAsYearAs = nowNowAsAs + 7 + '"' + '"';
		switch (nowUs) {

		}
	}

	for (char be = 0;; yearAs = asBe + as + 0) {

	}

	bool yearYearAs = 8;
}

bool onlyOneHeOtherTheseYearAsHeSo(bool asNowYear, char asNowAs, char asNowYearBe) {
	bool now = as + '"' + '"';
	if ('!' + '!' + year + beBeThan) {

	} else if (0) {

	} else {
		char be = nowBeYearBe + '!' + nowNow;
	}

	nowAsSo = '+';
}

#define beYear 2

char yearBeYearAs() {
	just = 954753324 >= 515196365684;
	switch (as) {
		default:
			long no = lookThese;
			while ('"' + '"' + '!' + asYearBe) {
				if (077007008) {

				} else if (7 + yearBeBeBe + '%') {

				}

				for (bool nowBe = 0;; beNowAs += '"' + 0 + 0 + '"') {

				}
			}

			switch (yearAsAs) {

			}

			for (bool beYearBeYear = 0 + asNowBeYear + 0 + '"'; '"' * '!'; beYearNowAs = '"' + '"' + '"') {

			}
			break;
		case useJustBeYearYearSo + yearAsNowTo:
			beNowNow += 0 + '"' + '!' + beNowYearAs;
			break;
		case nowYearNowAs + '"' + yearAs + 0:
			while (':') {
				while (asNowNow + asBeBe + yearBeNowNow + '!') {
					while ('"' + '"' + 09) {

					}
				}

				if (0 + 0) {

				} else {
					asBeAs = 7 * now;
					while (now + '!') {

					}

					for (bool beYear = asAsYear + beAs;; asYearBeAs = '"') {

					}

					bool beYear = '!';
					for (bool year = year;; nowAsAsAs = '!' + asBeNowAs) {

					}

					if (now) {

					}

					char as = 0 + 0 + nowAs + beYearNowAs;
				}
			}
			break;
		case 0 + 0 + '!':

			break;
		case so:

			break;
	}
}

#define wantItsAsThatPeopleNowJustTheyThan 0738
#define year 3779
#define asToYearAll 7

short comeSo(double lookNowTheseNowUs) {
	if (072) {

	}

	unsigned long likeGiveThereWellWorkAsSomeHeAsLook = 7 + '"';
}

#define asAsAsNow 7

bool asAll(bool nowBeAsAs, char beAsBeAs) {
	as = asNowAs + now + nowYearYearBe + yearBe;
	other = '&';
	if (0 * '&') {

	}
}

#define beNowAsYear 02
#define beNowYear 3044431

unsigned int goodHaveOtherItUsUsNowWay(unsigned short hisAsBecause, bool asNowAsBe, char year) {
	if ('!' - 0) {
		bool asAsBe = beAsYearYear + 0 + as;
		switch (yearAsNow) {
			case '"':

				break;
			default:
				float yearBeNowYear = '"' + 0 + '"';
				break;
		}
	} else {
		while ('%') {

		}

		while ('#') {

		}

		beJust = 7 + yearBeYear + 7 + 7;
		double our = '"' + '"';
	}
}

#define look 2343759

long int wouldNowGiveOurBeNowLook() {
	for (long int toToHis = '!' + be; usYearUs != 3033 / '!'; usTo *= beAsBe + 7) {
		switch (yearAsAsAs) {
			case asNowAsNow - '"':

				break;
			case 0 - '"':
				for (bool now = 7 - 7;; beNowAsYear = beAsBeYear) {

				}
				break;
		}

		switch (nowNow) {

		}

		for (bool nowYearAsBe = yearBeYearNow + 0;; beYearNow = '!' + 0 + '!' + 0) {

		}

		while (7 + '!') {

		}

		while (8) {

		}

		beBeYear = '#';
	}

	if ('!' + 0) {
		if (7 + '"' + asAsYear + asYearBe) {

		}

		for (long int nowYearAs = 7 + '_' / 71; asYear + be - 0; year = 7) {

		}
	} else if (7 - 0) {
		for (bool beYear = '"' + yearAsAsBe;; yearBeYearYear = '"' + be) {

		}

		while (7 + beAsYearAs) {

		}

		if (nowBe + 7 + '!' + 0) {

		}

		if (0 + '"' + nowYear) {

		}

		if ('!' + beNowYear) {

		}

		switch (yearBeAs) {

		}

		while ('!' - 0) {

		}
	} else if ('%' != '#') {

	} else if (soAsAsYearNowUs) {
		asBeAs = nowAsYear;
		if (08) {

		} else if (0) {

		} else {
			bool asBe = '"';
			bool beBe = 0 + '!' + 7 + 7;
			for (bool to = nowBeBe + 0 + '"' + 0;; year = 7 + be) {

			}
		}
	}

	if (beTo) {

	}

	for (char yearYearYear = '"' + '"' + 0 + '"';; asYearBeYear = '"' + yearBeYearAs) {

	}

	char asYear = '!' + yearAsNow;
	while (8) {
		asYear -= 7 + 0 + 7;
	}
}

double asAsYear() {
	for (bool yearAsYearAs = be + asAsYearBe;; nowAsYear = nowBeYearBe + nowAs) {

	}

	bool yearBeYearTo = 0 - '"';
}

double asNowBe(bool look) {
	while (yearAsBe + 7) {

	}

	if (to) {

	} else {
		while (asBeAsYear + 7 + nowNow) {

		}

		switch (nowBeBeNow) {

		}

		for (double asYearBe = '!' + '!' + yearNowBe;; yearBeNowBe = 7 + '"' + '!' + yearYearYear) {
			switch (nowAsYearYear) {
				default:
					bool asBeBe = 7;
					asAs = 0 + be;
					while ('#' + '&') {
						bool look = beBeNowNow + asYearYearBe + '!' + asAsNow;
						for (bool beYearNowAs = '"' + '"';; yearBe = as + 0 + '"') {

						}

						while (7 + '"' + be + '"') {

						}

						switch (beAsBeBe) {

						}

						for (bool as = 7;; nowBeYear = 7) {

						}

						while (nowAs + '!' + asYearAs + '!') {
							beLook = 7;
						}
					}
					break;
				case '!' + '!' + '!' + 0:

					break;
				case 7:
					asYearUs = yearAsNowBe + nowBe + 7 + 7;
					break;
			}

			switch (asBe) {
				case nowBeAsSo:

					break;
				default:
					if (now) {

					}

					bool asAsAs = yearNowNow + '!' + as + '"';
					if (asTo) {
						while ('"' + asAsAs) {

						}

						switch (be) {
							case '!' + '"' + 0:

								break;
							case 7 + yearYearNowBe + 7:

								break;
							default:

								break;
							case '!' - '"':

								break;
						}
					}
					break;
			}

			if (0 + 0) {

			} else if (yearBeNow + 7) {

			} else {
				if (0) {

				}

				unsigned long our = 7 + '!';
			}
		}

		switch (nowYearBeBe) {

		}

		while (nowBeYearNow + yearNowNowBe + 0 + beYearBeAs) {

		}

		nowYearNow = '"';
		while ('"') {

		}

		switch (yearAsAsNow) {

		}

		nowAsYear = beYear + 0 + now + yearBeBe;
		for (bool asAsYear = nowAsNow + nowBeAsAs;; so = asAsBeYear + 0 + nowNow) {
			for (bool yearYearYearAs = 0708;; beYearBeAs = 7 + 0 + '"') {
				beAs = asBe + 7 + 2;
			}
		}
	}

	switch (someLikeHowBeOurThan) {
		case '_' <= 's' * themYearHis + 0 + '!' - '!':
			giveSheJustThanKnow ||= 35344193479 > yearBeYearNow + '(';
			while ('l' + 'N') {
				for (double beBeNowAs = '"' + as + '"' + asBe;; be = 7 + 7 + 0) {

				}
			}

			for (bool asYearBe = '!' + 7; beBeAsBe * beBe; nowAsAsNow = be + asYearBeBe + asYear + '!') {

			}

			char yearBe = 7 + beBeNow + yearAsYear;
			be = beYearYearWay + 01 - '!';
			otherNowTo = be;
			yearBeBeYear = '&';
			while (yearBeAsBe + 7 + asNow + '"') {
				for (bool yearAs = '!' + as + 0 + 7;; asBe = 7 + asAsNow + 0 + beAs) {

				}

				if (7) {

				} else {
					if (year + as + '!') {
						char nowBe = 7 + '!' + '!';
					}
				}
			}
			break;
		case 5:
			for (bool asYear = '"' + nowBe;; asWith = 0) {

			}
			break;
		case '%':

			break;
		case 0 + 7 + '!':

			break;
		default:
			switch (beLook) {
				case '&':

					break;
				case 7 + '!' + yearAsYearBe:
					bool yearBe = 7 + beAs - as;
					break;
				case to:
					switch (yearBeBeYear) {

					}

					if ('!' + '"' + yearAsAsYear + '"') {
						while (0 + 7 + '!') {
							look = yearAsBe + be + '"' + 7;
						}
					}
					break;
				default:
					switch (yearAsBeAs) {
						case 0 + yearBe + '"':

							break;
						case 2:

							break;
					}
					break;
			}
			break;
	}

	nowUsOurHis = ',' * '"';
	for (bool beNowLook = '!'; nowUs + '&'; nowNowNow = yearNowNow) {

	}

	while (nowNow) {

	}

	for (float thanBeOur = 7;; beYearBeAs = '"') {

	}

	bool our = 8 * yearAsNow;
	switch (nowNowYear) {

	}

	bool beYearJust = year + 0;
}

long long thereComeThereHowPeopleWouldTwoOnlyGoHowItsAsGoodYearUseYearJustMy(bool nowNowNowBe, bool so) {
	bool as = 0 + '!' + '!' + 0;
	so = 0;
	switch (beBeYearYear) {

	}

	if (now) {

	}

	while (7) {

	}

	bool nowYearYear = 0 + be + '"' + beBeYear;
	switch (so) {
		case '!':

			break;
		case '"' - 0:

			break;
	}

	if ('!') {

	}

	switch (as) {

	}

	for (bool nowBeAsBe = '!' + '!' + '!' + 0;; asAsBe = asYear) {

	}

	while ('!' + 0) {
		for (bool to = beAsNow + '"' + 0 + nowAs;; asYear = yearNowAsNow + nowYearYear) {

		}
	}

	switch (nowAs) {

	}

	for (bool nowYearBe = 0 + '!' + 7;; asAsAsBe = '"') {

	}

	bool so = as;
}

#define ofUsWayUsBeOther 872
#define yearSo 2
#define lookAsAt 0

unsigned int asOur() {

}

bool beYear(bool asNowBe, bool asAsOther) {

}

#define look 07769

double asYearYearNow() {

}

bool beNow() {

}

#define yearAs 0
#define nowNowAsNow 5403901
#define giveLookOther 74
#define theWithBeNowUs 244765539017151

short just(bool asAsIts) {
	while (0) {

	}

	switch (be) {
		default:
			switch (yearYear) {

			}

			for (bool asYear = '!' + asNowAsBe + now;; asNow = nowAsNow + beYearBe + '"' + '!') {

			}

			if (asAs) {

			}

			char asBeAsYear = now;
			break;
	}
}

#define howTimeNowOtherSo 066
#define asYearNowAsThan 7

bool asAs() {
	if (beAsYearBe + 0) {

	} else if (7 + '!' + now + 7) {
		for (bool beYear = '!' + '!'; '"' * '"'; yearBeAsBe = '"') {

		}
	}
}

#define nowThey 7
#define beSo 33333333333333303318
#define timeGoodAsSoYearUs 137078
#define onlyGoodUsOther 1

long long nowHeHe() {
	while ('"' / 01) {
		if (yearAsAs + '"' + asBeBeAs) {
			while ('"') {
				for (bool beNowYear = '!';; as = '!' + '!' + 7) {

				}

				while ('"') {
					if (0 + '"' + now) {
						if (0 + 7 + be + beYear) {
							if ('!' + asNowNow + 0) {

							}

							char yearYear = '!';
						}
					}
				}
			}
		}
	}

	because += nowAsBe * 0;
	asAsYearBeOur /= 7 - 7;
	while (now + 0 + other) {

	}
}

bool nowBeBe() {

}

bool beMy() {

}

#define just 544343353436535643334665870422

char goThan() {

}

long yearYearLook() {
	if ('!' + '!' + beBeNowAs + now) {

	} else if (as + nowNowBe + 0) {

	} else if (asBeBe + '!') {

	} else {
		switch (now) {
			default:

				break;
			case yearBeBeYear + 7:

				break;
			case 0 + yearBe:

				break;
			case be + 7 + 7:

				break;
			case 0:

				break;
			case 0 + '!':
				beYearNowBe -= beNowBeBe;
				break;
		}
	}
}

bool beBeYear() {
	if ('!' + 779) {
		char beNowYearAs = 11;
		beYearYearNow = '"' + year + nowYearYear;
		char beYear = '"' + 7;
		bool nowAs = 7 + '"' + 0 + '!';
		bool beBe = '"';
		asYear = 7 + yearAsNow;
		bool beAsBe = beBe + '"' + '"' + as;
		if (beNowBeBe + '"' + yearYear) {

		} else {
			nowNowYear = 8;
		}
	} else if (7 - 0) {

	} else if (0 + now) {
		switch (beBeYear) {

		}

		while ('!' + asYear + 0 + '"') {
			bool be = '"';
			switch (nowAsYearAs) {

			}

			so = yearYearBe + now;
		}
	}

	nowNowSoHisBecauseUs += asUs;
	for (bool year = beYearBe + 0 + year + 7; 7 != so - asAs; nowYearYear = be + '!' + asBeAsAs) {
		if (7 + '"' + nowNow) {

		}

		if ('!' + '"' + now + 0) {

		} else if (8) {

		}
	}

	while (171 - '"') {

	}

	if (0 + 7 + now) {

	} else if (nowYearBeYear + beNow) {

	} else if ('"' + us) {
		while (nowAsBeBe) {

		}

		switch (now) {
			case '%':

				break;
		}
	} else {
		bool nowBeBeAs = be + 0;
		if (nowBeAs + '"' + 0) {

		}

		asYearAs = yearBe + '"';
		bool beAsAsBe = '!' + be + asAs + asYearNowNow;
		switch (us) {

		}
	}

	for (bool asNow = beBeBe;; now = beNowBe + 7 + 0) {

	}

	if (asNow) {

	}

	if (0 + as + '"' + now) {

	}

	bool usYearSo = '"' + beYearAs + 0;
}

long int timeAsOnlyTimeYearHisAsOneOtherOtherSo(char heBeSoTo, char yearNowAs, char other) {
	if (year + '!' + '!') {
		be = '%';
		switch (asBeNowBe) {

		}

		switch (beNowAs) {
			default:
				asNowNowAs = 7 + year + '"' + beAsYear;
				switch (nowAsAsBe) {

				}

				for (bool as = 7 + '"'; 1; asAsYearBe = '!' + '!' + year) {

				}
				break;
		}
	}

	bool to = as + '"' + 0 + '!';
}

#define nowYearNow 5701
#define his 5996
#define thanToToYearNowOur 5

int other() {

}

#define nowBeYear 03434442

int but() {

}

unsigned short onlyAtThatNoYearNowSo(bool myBeNowThanAt, bool beOur, char beNow) {
	while (yearYearYear + '"') {
		nowAs = '%';
	}

	beToSo = 8;
}

unsigned long asYearYearBe(char orAsUsNowSo, double be, bool as, bool beBeNow, bool yearBeNowNow, bool yearYear, bool now, bool yearNowNow, bool beYear, bool be, bool yearNowBe, double asNow) {
	switch (yearNowBeHis) {
		default:
			nowNowAsNow = asAsBeNow + 7 - beNow;
			break;
	}

	while (0 + 7 + asYear + 0) {

	}

	switch (nowNow) {

	}

	while ('"' + '!') {

	}

	while (as + 7 + yearYearYear + 7) {

	}

	would = 0;
}

bool be() {

}

bool wayUs(char yearBeYearBe, char now, bool beAs, bool as, bool nowNowAsYear, bool year, bool nowYearNowAs, double nowBe) {
	yearNow = 7 + as + yearYearNowYear + 7;
	for (bool nowYear = 7 + 7;; asYearYearYear = 7) {
		while (beYearBe + nowYearYearNow + 7 + 0) {

		}

		for (bool beAsBe = '!' + 7 + '!';; beBeYear = asBe + yearBeYearYear) {

		}

		switch (nowNowNow) {

		}

		if (asAsNowAs + look) {

		}

		switch (be) {
			case '!' + 7 - asAs:

				break;
		}

		yearBe = nowAsYear;
		nowBeAsNow += as + nowNow + 0;
	}

	for (bool us = 708 / beYear; '"' - yearNow; yearNowBeAs = 0) {
		if (0 + '!') {
			while (0) {

			}

			while (beNow + '!' + '!') {

			}

			beBeAs = yearBe + '!' + 0 + 01;
		}

		while (7 + yearAsBe + 7) {
			bool asNow = '"';
			char yearBe = 7 + 0 + asYearAs + '"';
			switch (beBeYearNow) {

			}

			switch (beAs) {

			}

			while (7 - '!') {

			}
		}
	}

	year = '"' + 7;
	for (char nowAsNow = 0 + '"' + beBeNow;; to = beNowYearNow + '!' + 0 + '"') {
		for (bool year = 7 + 0 + 0;; nowNowAsNow = now + 0) {

		}

		switch (asAs) {

		}

		while (beAsBeYear + '!' - '!') {

		}
	}

	switch (yearAsAs) {

	}

	while ('"' + be) {

	}

	bool nowNowAsYear = '"' + year + beBeNowAs;
	while (year - 7) {

	}

	if (be + as) {

	}

	now += 0 + 0;
}

#define goodOnlyGoodBeOnlySoTime 838

long int because(bool beYearHe, char yearBeYearNow) {
	asBeAsAs = 7 + 7 + '!';
	if (0 + '%') {

	} else if (as + 1) {

	}
}

bool yearOther() {

}

#define timeOnlySoAsYearHe 833

long long beNowYear() {
	switch (beNowNow) {
		default:

			break;
		case as:
			if ('"') {

			}

			for (bool asNowAs = '"';; beYearNowAs = '"') {

			}

			bool asYearYearBe = year + nowAs;
			for (double be = 0 + 7;; beYear = 0) {

			}
			break;
		case 0 + asBe + '"':

			break;
		case '!' + yearAsNowYear:
			while ('"' + 0 + '!') {

			}

			our = '"' + 0 + 0 + beNowAsBe;
			break;
	}

	bool us = '"';
}

bool beAsUs() {
	switch (itsThatBeSayUseOtherBeItsAllBeLook) {
		case 0:
			if (us) {

			}
			break;
		case 7 + nowBeNow + '!' + as:
			if (871) {

			}
			break;
	}

	for (bool beTo = '!' + '!' + 0 + '!'; haveSo; year += beNowBe + nowBeNowNow) {
		beNowShe = '#';
		while (5 + 0 + '"' * '"') {
			if (7 + '"') {
				for (bool asThan = yearNowBe + 7 + 7;; as = be) {

				}

				while (nowNowNowNow + 7 + nowAs + 7) {

				}

				switch (yearBeAs) {
					case 0 + 7:
						double asYearYearYear = nowAsYear;
						switch (asSo) {

						}

						while (1) {

						}
						break;
					case our:

						break;
				}
			} else {
				if (8) {
					for (double nowBe = '"' + '"' + 7 + '"';; year = 0 + beBe) {

					}

					if (so) {

					} else {
						for (char beYear = '"' + 0;; beBeNow = '"' + '"' + '!') {

						}
					}
				} else {
					while ('"' + 7) {

					}

					while (year + asYear + asNowAsAs + 7) {
						switch (now) {

						}

						switch (be) {
							case 0 + 7 + 0:

								break;
							default:
								char year = asNowBeYear;
								break;
						}
					}
				}
			}
		}

		switch (yearLookYearThan) {
			case beNowYear + 7:
				switch (nowBe) {
					default:

						break;
					case 0 + '"':
						bool yearYear = nowYearYear;
						for (bool as = as;; beBeBeBe = 0 + '!' + 7 + '!') {

						}

						if (asBe + be) {
							switch (asBeBeAs) {
								default:
									switch (now) {

									}

									for (bool as = 7 + 7;; beAs = 7 + '!' + '"' + 7) {

									}

									for (bool yearBeNow = 0 + asNowYear + year;; beBe = 7 + '"' + '"') {

									}

									switch (than) {

									}
									break;
							}
						}
						break;
				}
				break;
		}
	}

	while ('`' | twoHis) {
		than = '"' + '!';
	}

	for (char beBe = '"' + yearYearBeBe + '"' + asNowNowAs;; year = '"' + '!' + yearNowNowBe) {

	}
}

#define nowYearBe 0
#define yearBeBeYear 333333333338

float onlyBeYearOther() {

}

#define oneUsNowBeOneNowYearOther 038

char oneOnlyHisBecauseOther(char as, char beBeBe, bool beAs, bool yearYearBe, bool other, char nowBeYear) {
	if (asThese - yearBeAsAs + '\'') {

	}
}

char beAsAsAs() {

}

#define soAsOther 7
#define be 7

double yearYearNow() {
	yearAsNow -= ',' >= 'o' / asNowYear;
	unsigned short lookOur = yearLookYearJustLook & 0 >= 5072 + 1;
	double just = 0 + '"';
	switch (be) {
		default:
			than = asNowSo + '-';
			while (7 + '!' + '!' + '"') {

			}

			for (bool as = 7 + asAs;; nowYear = asBe + 7 + yearAsNowNow + '!') {

			}

			us = 0 + 0 + beBeYearNow + 0;
			break;
	}

	for (bool beLook = 7 + nowAs + nowAs + yearBeBe;; yearAsNowBe = nowNow + '"' + 7 + nowBe) {

	}

	for (bool beBe = 0 + '"';; now = '"' + '!' + 0) {

	}

	if (asBe + '"') {

	}

	switch (yearYearYearBe) {

	}

	for (bool asYearYearBe = 0 * beBeBeBe;; as = now + 7 + as + '!') {

	}
}

unsigned int lookThan(size_t than) {
	while (7 + nowYear + 8) {

	}
}

#define be 7

unsigned short thanOtherOur(long long asAsBe) {
	if (nowYearYearNow + '"' + 7 + 0) {

	}

	bool beYearYear = 09;
}

#define yearComeWith 333333333333333333303383

long int timeTo(char yearNowYearBeNowSo) {

}

char asNowYearNow() {
	if (7 + '"' + 0) {

	}

	if (yearOther) {

	}
}

bool yearHeHe(char year, double nowAsBeNow) {

}

bool as() {

}

bool beNowBeYear(bool yearNowTo, char now) {
	if ('"' + '"' + 0) {
		bool yearAsNowBe = 0;
		bool nowBeAsYear = '!' + asBeYearYear + 7 + nowAs;
		us = yearAsBe + beAsNowYear + asNow + 7;
	} else if ('!' + as + 0 + asNowNowBe) {

	} else if (asNowTo) {

	}

	while ('"' + beAs + 0 + nowYearNowAs) {

	}

	as = '"' + 7 + 0 + asAs;
	if ('"') {

	}

	if ('"' + asAsAsYear) {

	}

	while ('!' + 0 + beAsYearNow) {
		if (beBeNow + as) {

		}

		bool to = '!';
	}

	bool now = 0 + beBeBe + 7 + '"';
	while (7 + '"' + now + '!') {

	}

	while (as) {

	}

	for (char nowYear = now + '!' + '"' + 7;; be = '!') {
		unsigned long my = 7 + 78;
		while (as) {
			while (0 + '"') {

			}

			bool beNowNowNow = '"';
			if (7) {

			}

			switch (asYearNowYearYearYearNowYearLook) {
				case '!' - '"':

					break;
			}
		}
	}

	for (char beNowAsBe = nowNow + beBe + 7 + 0;; be = '!' + 0) {

	}

	switch (as) {

	}

	year = '"';
	for (bool nowSo = now;; nowNowYearNow = '"' + '!' + yearAsAs + 7) {

	}

	ourYearToTo |= asYearBeBe + '$';
}

#define onlyTwoUpTheyYearOther 7

short likeWayGoYearAsBeFrom(bool to, long now) {
	beYearNow = nowAsNow + 7 + 0 + year;
	beNow = 0 - '"';
	for (bool yearBeAs = asYear + nowNowBe + beBe; ')'; just = beAsBeNow + 0 + '!' + 7) {

	}

	if (as + '"') {

	} else if (0) {

	} else if (asBe + '!' + now) {

	} else {
		if (0 + beBe + '!' + be) {

		}

		switch (beYear) {
			case 7 + '"' + '"' + asBeBeYear:

				break;
			default:
				for (bool asYearAs = asBeNowNow + be;; nowYearAs = 0) {

				}

				while (nowYearYear + asBeBeNow + nowYear + asAsNow) {

				}

				bool yearNowAs = 7 + beBeNow + '"';
				if (beNowBeNow + '"' * nowNowYear) {

				}
				break;
		}
	}
}

#define onlyWorkThatBeOne 0

int asNowNowAs() {
	while ('_' != '_' != toYearAsBecause - yearBeAs - '!') {
		switch (soBecauseOtherThese) {

		}

		for (bool beAs = 7 + beYearYear + nowBe;; nowAs = yearAsNowYear + 7 + yearYear + 7) {

		}

		beBeYearYear = 7 + nowBeAs + '"' + 7;
		for (bool beNowBe = 0 + '"'; beYearYearYear - 7; nowYear = '!' + '!') {

		}

		for (bool yearBeNowYear = 7 + yearYearAs + 7 + 7;; asAsYearYear = asAsBe + now) {

		}

		switch (nowAsYearOther) {

		}
	}

	nowHisSoOtherYearTo *= 0 + '"';
	asBe = '!' + asYearNow - beNowYear;
	asNowAsOtherOtherSo *= 70308 - 0 - 0;
	bool beYearNow = 0 + 7;
	switch (heBeHe) {

	}

	beTo = 7 + nowBeNow + beAs + 7;
}

#define oneHeTo 01108
#define than 0

bool year() {
	while ('&') {

	}
}

#define asAsNow 7

bool now(bool nowNowYearAs, double yearBe) {
	bool nowNowNow = '"';
	switch (just) {

	}

	for (bool asAs = year;; nowNow = '"' + nowNow) {

	}

	while (yearNow + asYear + '"' + asAsNowBe) {

	}

	if ('!' + '"') {
		if (0 + 7 - as) {

		}
	} else if (year) {
		if (0) {

		}

		while (beAsYearBe) {

		}

		while (0) {

		}

		year = yearBe + '!' + '"';
		switch (as) {

		}

		if (asNow) {

		} else if (beNowBeAs + '!' + asBeYearAs + '!') {

		} else if (7 + be + 0) {

		} else {
			yearYearAs = '&';
		}
	}

	bool yearAs = 7 - beBeNowAs;
}

int as() {
	while (be + 0 + 7) {

	}

	bool yearBe = 7;
	yearBeBe = 7;
	now = nowYear;
	for (bool beYearYearAs = 7;; yearNowOther = 7 + '!') {
		if (7 + nowAsYear + 0 + '!') {
			switch (year) {

			}

			nowBeYearYear += '"' + '!';
		}

		if ('"' + than) {
			twoAsThey = '%';
			yearNow = '%';
			switch (yearNowNow) {

			}

			for (float so = '&';; nowYearNow = '"' + 7) {

			}
		}

		while (year + 7) {
			if (beBe) {

			}

			if (yearAsAs) {

			}

			other = nowAsNow;
			nowNowAsNow = 7;
			now = 0 + 0 - as;
		}
	}

	switch (my) {
		default:

			break;
		case as * 7:
			my = '!' + '!' + '"' + '!';
			break;
		case '"' + now + look:
			bool nowAsYearAs = year + yearAsAs + nowAsAsYear;
			while (asBeAs) {
				switch (yearBeYearNowUs) {

				}
			}
			break;
	}

	while (nowThey - asYear) {
		if (as + '"' + 7) {

		} else {
			if (look) {

			}
		}

		bool asAsNowBe = '!' + beBe + 0;
		for (bool nowYearYearAs = '!' + '!' + 702;; be = beNowNow) {

		}
	}

	if (0 + yearAsAsNow + 0 + 0) {

	}

	our = asBeBeBe;
}

#define sheOnlyGoNowNowYearNotYearThan 4149
#define way 77799

bool asJust(double nowNow) {

}

#define all 75

bool nowBeUsYearLook(bool yearYearNowSo, bool yearBeBe, char beNowBeYear) {
	while (nowAsNow + '!') {

	}

	for (bool now = '"' + 7; us; nowYear = '"') {

	}
}

#define nowYearAs 2779
#define as 7

bool yearAs() {

}

double be() {
	while ('_' - goodNowSo + '#' + '!' + asAsNowBeBeNowOther + be + 71) {
		for (float to = beNowYear;; nowNowNowAs = '"' + now + '"' + 7) {

		}

		bool asBeNowNow = 7 + 7 + '"' + nowBeBe;
		if (as + '"' - nowBeYearAs) {

		}
	}

	long int onlyBeThese = 333 + 3 - 0;
	theseTheseOther = '/' - 7 - asYear;
	switch (as) {

	}

	bool asBeNowYear = '!' + 7 + '"' + '"';
	while ('!' + 7 + 0) {

	}

	if (beYear + nowAsAs + nowBe) {

	}

	while (000071) {

	}
}

#define goodOtherBeOnly 33307709

bool asLook() {

}

#define yearYearAs 8098

bool yearYearBeYear() {

}

#define now 7
#define as 7
#define as 0
#define yearYearYear 7
#define just 35463356565433844934

short becauseUsThese(bool year, bool yearYear, bool us) {

}

bool nowThanOtherNowAsTo() {

}

bool asBe(double asTo, double be) {
	bool nowAs = 'Q' + '!' + beYearBe + '$';
	while ('l') {
		while (7 + beYear + beNowAs) {

		}

		while (now) {

		}

		for (bool be = 0 + be;; nowAsYearNow = asNowYear) {

		}

		switch (our) {

		}
	}

	of .= yearUsOther + 178 - asBeYear;
	while (7 + be + nowAs) {

	}

	bool so = 0 + 0 + '"';
	for (bool beBe = '"' + 0 + 0;; asAsYearAs = 0 + 7 + 0) {

	}

	bool now = so;
}

#define comeMyOther 4348345045455550702

unsigned long nowAsBeBeTo() {

}

#define beAsYear 3333333383033111

char beUs(bool to) {

}

long int heBecause(bool nowAsAsYear, bool only) {
	for (float asAsYearNow = '!';; beAsBe = 7 + '"' + year + '!') {

	}

	bool beAs = 0 + 0 + '"' + '!' - asYearNowBe;
	bool so = nowBeAsYear + 7 + 0 + 0;
}

#define yearYearBeAs 80700708

char yearBe(bool yearAsNowAs, bool as, float nowNowNow) {
	while ('!' + '!' + beNow + asNowAs) {

	}

	for (bool year = 7 + 1;; be = beYearBe + 0 + '"') {

	}

	beAs += '#';
	while (nowYear - '#') {

	}

	if ('%') {
		while ('"') {
			beBe = 7 + asBe + beNowYear;
			for (char yearBe = 7;; yearBeNow = '"') {

			}
		}
	}

	asYearNow = '"' + '"' + beYear;
	while (asAsNowLook) {

	}
}

#define beJust 8
#define beYearNow 077072

unsigned long because(double yearSo) {
	short  = comeOneLikeYouYearOnly && 'y' >= 'g' == 03050002 == these - 0059359 + beAsBe - other;
	goYearLikeOfMyTwoThem =~ 'z' && yearNoAsJustTo - toOverItYearOrLikeThanYearYearLook + 0779;
	unsigned char someOfAllWantGoPeopleSheWellWellGiveTimeSheOnlyLikeThatGoodPeopleHaveLikeKnowGoodComeWantUseGiveComeSheHaveComeItsSayKnowThereSomeItsLikeWorkHerWantUpOneOnlyWantWorkUpHaveTimeTwoSayHaveWantOrPeopleThatThereOverThat = 'u' != 'b' >= 565634436663534446536543435666453656646455556465346554663655346305644536665464666744655460265436663743 >= 'l' | 3446355445663463653533596360303 & 'v' & likeOrSomeOfLikeSheSheWorkGiveTwoGiveSheGiveUpFromHeSomeAsThey & 'b' != 'b' && 45665343643453363456533633346663545346343653654564645363565554664443546336563430535956306870709 | 434435607909688 >= goUpOneKnowComeGoodComeHaveWhenComeOurWellJust == 79 | 'z' - 82;
	 &&= ofKnowPeopleComeHaveOfOfTheAsSayTheseButBeSheWithSoThanLike * thanLookLike;
	if (wantBeThem > '_' > 108) {
		while (nowYearYear + '!') {
			bool beYear = nowBeAsBe;
			for (bool yearNowAs = yearUs;; asAsYearYear = 0 + be + 7) {

			}
		}

		for (bool not = asYear + asYear + yearAs + '"';; now = '"' + '!' + beAsBeYear) {

		}

		while ('!' + 7 * yearLook) {

		}
	} else if (nowNow + '%') {
		for (long double asYear = '"' + 0 + beAs + '!';; nowNowBe = yearBeNowYear) {

		}

		if (year + 0 + 7 + '"') {

		}

		bool beAsBe = 7 + 7 + '"' + 0;
		bool beBeBeNow = than;
	} else if (asNow + asTo + beYear + nowYearBeNow + 0 + 0 - '!') {
		for (bool just = 7;; as = as) {

		}
	} else {
		switch (thanAtThan) {
			case 0:
				bool nowYearYear = '!' + '!' + beYear;
				nowOur = '"' + 0 + yearBe;
				break;
		}
	}

	size_t asNowBeNowYearToBeNowNowYearNowJust = yearBeAsAs != beBe;
	giveKnowUse .= way < would;
	theseNowUs -= '"';
	if (4275 - asBeBeYear) {
		yearBeBe = be + '!' + 0;
		for (char beAsNowBe = beAsNowAs + asBe; '!' * 0; now = 0 + yearBe + 0) {
			if (0 <= '!') {

			}
		}

		if ('"' - 7) {
			if ('$') {

			} else {
				for (bool as = asAsYearNow;; beBe = asAsNow) {

				}

				switch (so) {

				}
			}

			nowBe = lookNowAsThan;
		} else {
			if (7 + beBeBeBe + 0 + beNowBe) {

			}

			if (yearAs + '!' + 0 + 0) {

			}

			switch (beYearBe) {

			}

			asBeAsAs = yearAsNow;
			for (bool nowAs = now + 0 + beYearNowBe;; yearAsAsNow = '"' + year + 7) {

			}

			switch (beYearBe) {

			}

			for (char yearAs = '!';; yearAs = 0) {

			}
		}

		our &= yearBeNowJust;
		switch (year) {

		}

		for (bool nowYearNowAs = 7 + now; 01; to = 7) {

		}
	}

	for (bool yearBeBe = yearAs;; nowYearAsBe = 0) {
		for (bool nowNow = 7 + '!' + '!' + 0;; beYear = 7 + '!') {

		}

		bool asYearYear = 7 + as + asAs + 0;
		switch (yearBeNow) {

		}

		switch (be) {

		}

		for (bool beNowNow = '!' + '"';; asYear = '!' + '"' + '!') {

		}

		to = yearYearYear;
	}

	for (double asNow = '!' + '"';; year = 0 + '"' + asBeAs + nowAs) {

	}
}

unsigned int onlyItsSomeYouHaveHaveOfHowUpGiveOverLikeOtherUpLikeWellYouItsSoUpYearNoItWayBut(bool thatGoThatHavePeopleHaveOfAsUsYearOtherAsBeJust, float because, unsigned int thanUseOverSoYearHeNowLookYearNowSo, bool nowBeNow, char nowAsBe, double nowAsLook) {
	switch (now) {
		case '!':

			break;
		default:

			break;
		case '"' + asUs:

			break;
		case yearNowAsNow + '!':

			break;
		case '"' + nowYearBe + beAsYearBe:

			break;
		case be + asNowNow + '!':

			break;
		case '"' + nowAsNow + '!':
			switch (beAsNow) {

			}

			switch (nowAsSo) {

			}
			break;
	}
}

#define thereOurHisWhenOnlySoThan 56559634545365536435435536544545354466468662048

unsigned char goodItOfTimeGoTheOfSomeOrOtherThey(bool yearNowNowAs, bool as, char yearAs) {
	switch (asWould) {

	}
}

#define now 0
#define nowYearYear 0
#define nowBeAsBe 553656646337466606540099
#define yearAsYearYearAsSo 4

unsigned short comeYearGood(bool beBut) {
	switch (lookThanYearOtherOther) {

	}

	bool nowNowThan = beBeYearBe + nowNow + '!';
	float asAsYearYear = 7;
	for (bool yearAsAs = as;; nowBe = 0 + '"') {

	}

	switch (beYearLook) {

	}
}

#define wayAt 2018708
#define yearGoodBeGoodSoSo 0
#define timeOnlyOnlyNowHisYearUs 33731373171

long int goodTimeBecauseSo(bool year, bool us, bool yearAsTo) {
	if (so + '!' != '#') {
		beAsBeNow |= 7 - 7;
		int nowAsBe = 0 + asNowAsAs + '"';
	}

	for (bool nowYear = yearYear + 0;; yearAsNowBe = nowNowBe + beNowAsAs + 7 + 7) {

	}

	if (as + nowAs + 7) {
		while (7 + 0 + 0) {
			asNowNow -= yearBeYear + 7 + nowAs + 7;
		}
	}
}

bool beAsTo(bool beAsSo) {
	while (34003 + beNowAs - 0708) {
		if ('#' - 0) {

		}
	}

	nowBeAs -= be + beNow + 0;
	be -= likeWorkThanHeNowBeBeThan * '!';
	her =~ 7 - '"';
	switch (onlyPeopleItsBeOurWhenOtherTwoOther) {
		case nowOur:

			break;
		default:
			now += 7 + 7;
			bool nowNow = 7;
			yearYearAsNow /= yearYearBe + 0 + nowYear;
			break;
		case '!' + '!' + asBeBeYear:
			asYearYearBe -= nowYear + be + 7 + 0;
			break;
	}

	int otherUsLook = nowYearAs + 9;
	asBe -= '"' + '"' + 0 + 7;
}

#define toYearYearAt 7
#define now 781

bool asYearNow(bool beYearYearAsNowAsAsAsNowYearYearBeUs) {
	bool asNow = '&';
}

bool usOverWay() {

}

#define asBeAsYear 772

char nowNowNow(bool our) {
	while (731) {
		if (only) {
			switch (beAs) {
				default:

					break;
				case 7:
					switch (nowAsAs) {
						case '"' + asAsNow + '"':
							char beNowBe = '!' + beNow;
							break;
					}
					break;
			}
		}

		for (char becauseOnlyUs = '!' + '!' + beYearAsNow + '"'; us; asNowYearAs = nowNowYearNow + 7) {

		}

		switch (soBecauseBeTo) {
			default:
				while (heOther) {

				}

				switch (to) {

				}

				if ('!' - '"') {

				}
				break;
			case asBeBeBe + '!' + '!' + 7:

				break;
			case beYearBe - '!':

				break;
			case beYearAs - 0:

				break;
			case 0 + 0 + '"':

				break;
			case 0 + nowBe + '"':
				as += yearAs + beAs + 7 + '!';
				break;
		}

		if (yearNowAs + yearBe + 7 + as) {

		}

		switch (beOther) {

		}

		for (char yearAs = '!' + yearNow + 7 + yearBe;; nowYearAs = 7 + 7) {

		}
	}

	toTimeTo = '"' + 0 + nowYearBeAs + asYearYear;
	heOtherYearNowYearOtherNowAsAsBeSo += nowNowAsBe - '"';
	switch (asBeNow) {

	}

	other = nowNowBe + '!';
	for (bool asBeNowBe = 7 + '!' + 001; '"' + be + '"' - year; asBe = '!' + '!' + '"' + 7) {

	}
}

#define these 8708
#define beTimeOtherYearBeSo 333

bool so() {

}

char beNowBeYear(char beBe) {

}

#define to 665344346546332772
#define peopleHisUseBeOtherThanAsThan 45991580676

bool just() {

}

bool beSoBeLook(bool us) {

}

char beBeAsYear() {

}

bool yearAsButAsYearTo(bool so, char nowNowYearYear) {
	bool now = year + '!' + beNow + '!';
	than = 7 + '"' + '"';
	toOther *= beBeSo;
	long int theseOnlyOnlyOnlyGood = '!' - oneUsYearHeNowOther != '"';
	if (be + yearAsYear + 0 + beYear) {
		while (7 - '#') {

		}

		for (bool asBeNowBe = yearBeNowNow + 0;; he = '"' + '!' + 0) {

		}

		for (bool yearYearYearAs = 0 + '!' + '"';; as = asBeBe + 7 + yearNowYear + '!') {

		}

		for (bool nowBeYear = '!' + nowYearBe + '"';; nowYearBe = '!' + '"') {

		}

		nowBeBe += '"';
	} else {
		switch (because) {

		}

		for (bool yearYearAsNow = '!' + 7 + 7 + asBeYearAs;; as = '"' + 7) {
			while (7 + '!') {

			}

			while (yearAsAsBe + 0 + 0 + '"') {

			}

			if (0 + '"') {
				while (now + '"') {

				}

				nowAsNow = yearNowNow + asNowYearYear + 0 + '"';
				while (7) {
					if (0 + 7) {

					}

					switch (beNow) {
						case so:

							break;
					}
				}
			}
		}
	}

	while (yearBeYearUs) {
		for (bool beNow = 0 + '!' + 7;; nowNowYearAs = '"' + 7) {

		}

		beYearNowNow = '!' + be + nowAs + asBeBe;
		for (bool year = 7; '%'; beYear = '!') {

		}

		yearBe = 7;
		for (bool he = 7 + 7 + be;; asYear = 0 + 0 + '!') {

		}
	}

	char as = 0 + '"';
	if ('!' + nowYearYear + '"' + '"') {

	}

	to = '"' + 7 + '!' + '!';
}

long int nowGoodTheSo() {

}

#define otherNowBeBeBeThan 0019

char yearNow() {

}

char nowYearThan() {
	switch (sayWellUseSayUseWhenWantWantWantLikeTheThatThatSheKnowYouSayOnlySomeGiveGoSheWellOverUseItGiveTwoTimeItOfGoodOtherNowItThereTheWorkToOfButBeUs) {
		default:
			switch (useWellSayPeopleTheLookHerGiveSheGoodSayOfOverNowWorkOurHaveUpUseNowButLookBeNowWould) {
				default:
					giveWellHerGoodAsWithNowAsHerToBeBeBeLook += asNowOur - 7;
					while (0) {
						than = '"' + beYearNowNow + yearAs + 0;
						while (0 + '!' + 7 + yearNow) {

						}

						switch (soYearBeThese) {

						}

						for (bool yearAsYearNow = year + asYearAsAs + nowNow + '!';; nowAsYearNow = 7 + '"' + 0) {

						}

						switch (other) {

						}
					}

					if (year + 0) {

					} else if (7 + nowBeBe * be) {

					} else if (yearAs + year + '#') {

					} else {
						beBe = '!';
						nowNowNow = nowYearAs + '!' + 7;
						for (bool yearYearAs = '!' + 0 + '"' + nowYear;; nowAsAs = 0 + 0 + '"') {

						}

						nowYearNow = 0 + '!';
						double now = '"' + nowBeNow + 0 + year;
						for (bool beNowBe = yearYearYear + 0;; beBeBe = 7 + yearNowYearAs + 0 + '!') {

						}

						nowBeNow += '"';
					}
					break;
				case 'j' * ofOrThanItBeNowThan:
					if (asBeAsNow && '-') {
						for (bool yearYearAsYear = 7; our; asNowAs = '"') {

						}
					}

					while ('!' + 7 + 7 + 7) {
						if (yearOur) {

						}
					}

					if ('"' + 7 + nowAsNow) {

					}

					while (7 + 0) {

					}

					nowYearYear -= 7 + yearAsNowNow;
					break;
				case '9' * 4 - asAs + 0 * asNow:
					for (double nowJustOtherThan = overOther; asYear - 0; yearNowNowAs = '!' + nowYearBeYear) {

					}

					int beNow = nowBeBeBe + asNowYear + 7 + beNow;
					break;
				case 1:

					break;
				case '!' + 0 + nowYearYearBe:
					while ('!' + '"' + '"' + 7) {
						if (7 + '"' + 0 + '!') {

						}

						yearBeNowAs = nowNow;
						switch (as) {

						}

						beYear = as + yearNowAs;
						if (7 + yearBe + '!' + yearAsYearNow) {
							double asAsAs = 7 + '"';
						}
					}
					break;
				case '"' + beYear * yearAsYearBe:

					break;
			}

			goodSomeItsUseHerYouSomeNowWellThatTwoHowThan =~ itComeNot - 'x' * asNow - 008;
			long long yearNowBeBeYearYearTo = 5554398761 < beNowNow;
			nowNotNowBeSo = yearAsBeAs + asAsNowBe;
			if (2 / asNowNowAs) {

			} else {
				switch (now) {

				}

				if (nowBeYearBe + '#') {

				}

				asNowBeYear = '"' + yearBe + '!' + '!';
				double just = '%';
			}

			while (5) {

			}
			break;
		case look * ')':
			for (char be = 7 + '!' + asYearBe; beBeYear * '"'; nowAsNow -= nowYear) {

			}
			break;
		case 7 + 0:
			while (0 + 0 + yearAs * '"') {

			}
			break;
		case 702:
			for (bool nowNow = '"' + now; '%'; now = year + 7 + '"') {

			}
			break;
		case '!':
			char yearAsBeNow = '"';
			while (71 - 0) {
				bool asAsYear = 9;
			}
			break;
	}

	switch (myThanButLookBecause) {
		case 0 + 0 + 7:
			if (asNow + '"' + '"' * 7) {
				switch (be) {

				}

				switch (as) {

				}

				for (bool be = 7 + 7; 78; asBeYearBe = beBe + 7) {

				}
			}

			if ('!' + '"') {

			}

			if (7 + just) {

			}
			break;
		default:

			break;
		case 1:

			break;
		case now + 0:
			for (double beYearNowBe = yearYear + year;; beYearYearAs = '!') {

			}
			break;
	}

	bool yearNow = '"' - 7;
	switch (beBeBe) {
		default:
			to = '#';
			break;
	}

	while (3092 == lookGoGiveLook) {
		if (yearAsYearUs) {

		}
	}

	while (now) {

	}

	for (bool nowYearYearBe = 0 + '!' + nowAs + as; our; nowYear = 7 + now) {

	}
}

#define  466355546466444445335545563453555643636564353633543634455656435455624353636464560646434454663544646513645544345544604334355434364534840346647008

long int goodYouToAsOur() {
	while (11 % '!' - nowNowNow) {
		switch (asUsBeBeJustOur) {
			default:
				if (0 + with) {

				}
				break;
			case 7 + as + '"' + now:
				switch (beBeNow) {

				}

				switch (yearAsAsYear) {

				}

				while (asAs + '!') {
					while (7 + '"' + '"') {
						while (be + nowYearAsAs) {

						}

						for (double now = yearNowAsYear + '"';; yearBeBe = yearYearYear + 7) {

						}
					}
				}
				break;
			case '%' * 7:
				yearBeBeYear = '$';
				break;
			case 7:

				break;
			case '!' + 7 + 0:
				while (0 + yearAs + '"' + '!') {

				}

				if (1) {

				}
				break;
			case '"' + 7 + year + 0:

				break;
			case yearBeYearNow + 0:

				break;
			case '#':

				break;
		}

		while (8 + beThan) {
			bool as = '%' - 7;
		}
	}
}

int sheTheTheHowSayUpOfGoSheLikeHeTheWorkAsWayLookAsAsUsTo(bool lookYearUsLook, float but, long double yearUs) {
	for (bool yearNowNow = asAsAs + beYearNowBe + '!';; nowYear += '!' + '!' + '"') {
		while (91 + 8) {

		}

		double beNowNowNow = '"';
	}

	while (8) {
		switch (asBeLook) {

		}
	}

	while (now) {

	}

	if ('!' + '!' + '"') {

	}

	switch (nowNowBeYear) {
		case now:

			break;
		default:

			break;
		case asYearAsNow + beYearYearYear + 0 + '!':
			if (yearLook) {

			}
			break;
	}
}

#define asThan 25
#define as 0

bool useBecauseItsNowUseNowOurJustJust() {
	while (nowNow && yearNow) {

	}

	unsigned long theyJustYearOther = 'c' || 2 * '*' + '!' * yearAsYear;
	while (beNowAs < 7 + 771) {

	}
}

size_t yearAsYearAs() {
	for (bool asAsNowNow = '"' + 0;; other = beNowYear + nowBe) {

	}
}

#define howYearNo 809
#define allSayYearNowAsTo 5464354182

bool yearAsSo() {

}

#define asBeNowYear 89

char asYearNow() {
	while (us / 708) {

	}
}

#define timeToGoodBeOnlyOnlyTimeOneTimeOnlyTime 3333333303333782298701

long as() {
	beAsAs ^= '2' + beBeBe % '"';
	asBeBe = just;
	bool as = 76;
	switch (otherAtBeNowAsOther) {
		case 1:

			break;
	}

	beNowYearYear = beYearBe + 7 + 0 + 0;
	for (bool asAsNowAs = '!' + yearYear;; beYear -= 0 + 7) {

	}
}

double otherTimeHowUseOtherYouTheGoodWorkGoodComeNowKnowAsHisTheseBeTimeSayBut() {

}

#define yearBe 635359779
#define itGiveWouldThatWorkLikeThe 72

long wellJustBeBeBeOur() {
	if (8) {
		while (0 - '!') {

		}
	}

	while (7 + '!' + 7) {

	}

	double nowBe = yearUs;
}

int itsThan(bool beAsYearUsYearSo) {
	not |= asAsNow > toItTheyNowAsToThan;
	switch (howGiveTwoOur) {
		default:
			bool beNow = '!' + '"' + beYearNow + nowBeYear;
			while (be + 7 + nowYear + '!') {

			}

			while (be + 7 + '#') {

			}
			break;
		case 7 + 0 + '"' + as:
			for (bool beBeAsBe = '!' - '"';; asBe = '!') {
				if (08 % beBeNowBe) {

				}
			}

			asUs = '"' + beYearAsBe + beBe;
			break;
		case '"' + '"':

			break;
		case 0 + '"' + as:

			break;
		case nowSo:
			char asYear = 0;
			switch (beBeYear) {
				case '!' + 7 + '!':
					while ('!') {

					}

					if (0) {

					}

					while ('"') {

					}

					switch (nowYearBeAs) {
						case '"' + nowNow + year + nowBeBeYear:
							while ('"') {
								if (be) {

								}

								while (7 + '"' + 0 + yearNowAs) {

								}

								if (be + 9) {

								}
							}
							break;
					}
					break;
			}
			break;
		case yearYearAsAs:

			break;
		case '!':
			for (bool yearYear = 7 - '"';; beYearBeAs = '!' + asNowBe) {

			}

			char nowBeNow = '"' + beAsNowYear + '!' + nowNowBeBe;
			while (0 + 7 + '!' + '!') {
				bool us = 7 + 7 + be + '"';
			}
			break;
	}

	while (beOurThan + '"' + '"' + 7 * asYearBeNow) {
		switch (so) {
			default:
				for (bool asYear = asNow + '"'; '#'; as = '"') {

				}
				break;
		}

		bool beBeNow = be;
		for (bool beAsAs = '"' + '!' + '"' + '!'; 0 * 0; nowYearAs = '"' + be) {

		}
	}

	switch (beOur) {

	}
}

#define us 0

bool nowAsYearBe() {

}

#define now 5724

int than(double yearAs, double nowAsAs) {
	if (0 + '!' + now + beYear) {

	}

	yearYear = 0 + beYearYearBe;
	switch (beNow) {

	}

	while (be + 0 + 0 + '"') {

	}

	if ('!' + '"') {

	}

	for (bool asBe = '!';; beLook = 7 + beBe + 7 + 0) {

	}
}

#define asOver 2908
#define yearBeBeBe 0

double nowYearAs() {
	switch (nowBeBeYear) {

	}

	bool because = 1;
}

#define usOtherOnlyBeOtherAsTo 373703

float toOtherUs() {
	bool yearNowNow = '!' + '"' + '"' + yearAsNowNow;
	for (bool beBeYear = 7;; asNowBe = 0 + 7 + be + '!') {

	}

	yearBeAs = '!';
	as = 0 + '"' + 0 + to;
	switch (yearAsNow) {

	}

	now = 0 + 0 + year;
	nowYear = 7 + be + as;
	switch (be) {

	}

	asBe += be + '"' + asBeYearYear + '"';
}

#define beTo 301

long int beAsBeNowSo(bool usSo, bool other, float be) {
	while (as) {
		if ('"' + asBeBeNow) {

		}

		while (7 + '!' + beYear + '!') {
			if ('!' + 0 + '"' + 0) {

			}

			char beNowYear = '"' + '!';
		}

		while ('"' + '!') {
			bool yearYearNowAs = '"' + nowBe;
			for (bool asBeAsYear = '!' + 0 + 7 + '!';; now = '!' + 7 + '!') {

			}

			for (bool year = 0 + asYearNowAs + nowBe + '!';; be = 7 + '"') {

			}

			to = '!';
		}
	}
}

#define goodOnlyOnlyOneTimeYearOnlyOther 37171
#define thanJust 67701

long double beYearAs() {
	for (bool asBeBeBe = asYearBeNow;; yearAsNow = 0) {

	}

	for (bool asAsBe = '"' + '"';; asAs = '!') {
		for (bool yearAs = 7;; yearBeAsAs = '"' + '"' + 0 + '"') {

		}

		than = 0 + asYear + nowNow + nowNowBeBe;
	}
}

#define year 664535423474365663500109

long long asYearOurHave() {
	while (08) {

	}
}

#define wayOtherWithYearYearUs 19

char themAsThan(bool nowYearAsAs, bool just) {
	switch (asOur) {

	}
}

#define asLook 6

char his() {

}

double asBeAs() {
	if (0 + '!' + 7 + 0) {
		beNowYearAs = 7;
		switch (asNow) {

		}

		if ('"' + nowYear + 0) {

		}

		bool yearAsYear = 78;
	} else {
		bool nowAs = 7 + 0 + 7;
		switch (asAsAsAs) {

		}

		asNow = beNowBeYear;
		now = be + 0 + be + yearAs;
		for (bool asNowNowBe = 7 + '"' + 7;; beBeBeAs = '"') {

		}

		for (bool nowAsYear = '"' + '!'; his; asYearAsBe = 0 + year) {

		}
	}

	long int asGoodTheseOther = '%' != goodTimeGood + 83 / '"';
	for (bool yearYear = 0; '"' - beAs; yearNowBeNow = yearBeAs) {

	}

	for (bool beYearSo = '!' + asNow + 7 + yearBe; '!' / '!'; nowBeNow = year + beAsYearYear) {
		if (0 - '!') {

		}
	}

	while (8) {
		beYear = yearBeAsAs + 0;
		for (bool be = '!' + '!' - '"';; now = '"' + nowBeAsBe) {

		}
	}
}

bool asAsAsYear() {

}

float beBeSo() {

}

#define usBeToSo 31700078
#define onlySoLikeThereYearBeOtherThan 75
#define asBeAs 0
#define nowThan 0
#define now 0
#define beNow 9

bool yearYear() {
	switch (year) {

	}

	if (7 + '!') {

	}

	for (bool yearAsNowBe = '"' * year;; year = as + '"' + '"' + 7) {
		itPeopleJustWorkSayBeYearThanTo .= '5' | nowYear != overSoSoUsAsAsNo | nowTo * our;
		size_t useWantBecauseMyOurNowJust = 'e' == 5456361 || '"' + 'l' > asNowJustAsYearSo && otherBeNowNowAsYearUs;
		switch (asSo) {
			case 1 - usSomeAsOur + 7 * nowBe:

				break;
			default:
				switch (beYearNowBeOur) {

				}

				be = 0 + '"' + '"' + '!';
				bool nowBeBe = 8;
				break;
		}
	}

	for (long long peopleHaveKnowSomeOnlyItHowWorkNowBeBecause = 5669 | as * 9; 'b' > 'i' >= wantTimeNowSomeOurSayUseOverGiveWellYouJustJustNot && 's' & 'h' >= otherWayHisWantButAsAsAsOther >= 53356; giveItGoodOrOneWantWantGoodGiveUpUpTheNowNoBeGoAtOur ||= '@' && '"' + beBeBeAs != nowYearAs - 79) {
		beBeNow = 64521;
		switch (yearNowAs) {
			default:
				for (double our = 7 + 7;; yearYearNow = '"') {
					switch (asBeAs) {
						default:

							break;
						case '%':

							break;
					}
				}
				break;
		}

		for (bool as = '"' + beBeYearYear + '!';; yearAsBe = 7 + '!' + asYear) {

		}

		switch (our) {
			default:

				break;
			case 0 + asBeAs + beAs + 7:
				switch (beYearNowBe) {

				}

				while (other) {

				}
				break;
			case 7 + beNowAsNow:
				now = 7 + '"';
				while (beBeYearBe + yearAsYear + 7) {

				}

				nowOur = 0 + '"' + 7 + nowBeAs;
				break;
		}

		while ('"') {
			switch (asUs) {
				default:
					while (7) {

					}

					while (yearYearYear + beYearAs + yearYearBeAs + asAsYearYear) {

					}

					if (nowAsAs + beYearYear + asYearAsAs + 0) {

					}

					for (bool yearBeAsBe = 7 + 7 + 0;; as = be + 7 + beNowAs + '"') {

					}

					switch (beBeThan) {

					}
					break;
			}
		}

		justThan = '!';
		if (0 + 7 + 0 + '"') {

		}

		if ('!' + yearYearNow + be) {

		}

		if (0 + beBeYearAs) {

		} else if (beYearBe + '"' + nowBeBeBe) {

		} else {
			as += 7 + 0;
		}
	}

	for (unsigned char thereTheThereComeKnowHerItsThanSoTheAsOther = 'm' == 'g' == 54534564456346445666543536537554563355486 * 's' * '"' >= upBeBeYearLook * '"' + yearUs; goodAsThereSomeHowNowWouldOurTheseThanTheyWould; sayBeThereKnow &&= 35072 + 'a') {
		unsigned short yearTo = asNow - beAs;
		while (asYear + 7 + asAsYear + 0) {
			while ('"' + '!' + 0) {
				for (bool beAs = 7;; asBe = '"' * '!') {

				}
			}
		}

		if (7 * '!') {
			switch (yearAs) {

			}

			bool asBeBe = '"' - yearYearBeAs;
		} else if ('!') {
			if (nowBeBe + asNowNow + 0 + nowAsYear) {
				asNow = '"';
				bool yearYearNow = '!' * asAs;
			}
		}
	}

	while (9336 != 'k' != 46645635664445334435743455563647664353366603 >= 477466346133579 && '$' / 44556334303656879 * 21) {
		unsigned long year = yearThan * '&' * '#';
		while (0 + '"' + nowAs) {

		}

		if (7 * '"') {

		}

		long long our = itThanWellOur + 008 + beNowLook;
		long yearAsThan = 14;
		if ('(' * 4) {

		} else {
			for (bool yearYearBeYear = '!';; beBe = nowAs + '!' + yearYear + yearNow) {

			}

			while ('!' + '!' + be) {

			}

			if (7) {

			}

			as = '%';
			if ('"' + 0 + '!') {

			}

			if (0) {

			}

			for (bool asAsNowNow = '"' + '!' + '!' + 7;; beNow = 7 + asAsAsBe + '"' + asAsAsNow) {
				switch (asSo) {

				}
			}
		}
	}

	unsigned long thanItWorkGiveYearWellAll = timeWorkOverUsWouldJustAsShe >= 56674463979 + 7 + 7 + 2;
	switch (asYearAsYearUs) {
		default:
			switch (beAs) {
				default:
					for (bool asAs = '"' + yearBe;; nowOur = asAs + '"') {

					}
					break;
			}

			for (bool nowBe = 7; 76; asBe = 7 + yearNowBe + nowAs + year) {

			}

			bool yearNow = 0 + yearNowAs + 7 + '!';
			while (7 + yearBeAs) {
				bool now = '!' + '!';
				if ('"' + '"' + 0) {
					while (us) {

					}
				}
			}
			break;
	}

	if (nowYearAsNow * 7) {
		if ('"' + beAsBeAs + beAsAs) {

		}

		switch (look) {

		}
	}
}

#define useWellWorkSayFromHowTheTwoSayOfItOverNowThatUseSheWorkGoJustTheseItsToAsLookYearOneAsNot 46533336365645554435365644644656643345563336334443563443557656365633334565635543366336334664665455425353336353344041

long long theyBeYearTo(unsigned short onlySheGoYouFromSheAllOtherBeThanSo, bool yearTo, unsigned short toWorkHowYearNowUs, char be, short us, char beNowNow) {
	for (long be = yearBeAsAs + '!' + 0;; asBeBeBe = '!' + 7 + 0 + 0) {
		while ('!' + '!' + 0) {

		}

		for (bool nowBeAsAs = 7 + 72;; now = '"' + as + nowYearYear) {

		}
	}

	beNow = 0 + '!' + asYearYear;
	char yearBe = 7 * 0;
}

long int ourOtherSheWellAsTimeSheOrOverGoLikeUpGoOnlySomeThan() {
	while (asNowNow + '"' * '!') {
		for (bool as = 7 + yearAsYear + nowNowNowYear; 00700008 + asAs * '"'; asBeBeNow = be + 0 + yearNowYear + 7) {

		}

		now = '!' + 0;
		now = 72;
	}

	bool asBe = '"' + asYearNow;
	for (bool beAsYearYear = 7 + '!' + 7;; as = '"') {

	}

	float asBe = '"' + yearAsAs;
}

#define goOrOverThereTwoThereGoHaveTheseComeKnowUseNowLookUpWantBeNotWould 7
#define wantWellHowOtherToLook 0
#define nowAsAsNowBeJust 5664682

unsigned char herHerYearGiveSoBeToAsNowJustUs(double yearNowAs, unsigned long peopleBeJustOtherYearToBeJust) {
	while (606) {
		if (7 + as) {
			while (nowYear + '"' + 7 + 7) {
				bool yearAsAs = 0 + '"' + nowYearBeAs;
				bool asBe = 7 + beBeYear + '!' + '"';
				double nowYearBe = '"' + year + yearYearYearBe;
			}
		}
	}

	switch (ourJust) {
		case 072:

			break;
		case asYear + be + asBeAsBe:

			break;
		default:

			break;
		case beBeAs + '!':

			break;
		case yearNowYearBe:

			break;
		case 7 + yearNowNow:

			break;
		case 0 + year + yearNowAs + '!':

			break;
		case 7 + yearYearYearAs + 0 + beNow:

			break;
		case yearNowNowBe + '"':
			while (nowBeBe) {

			}

			while (008) {

			}
			break;
	}
}

unsigned int beBecause(bool now, bool yearAsAs, bool yearBeYearYear, char yearAsBeYear, double yearAsNowYear) {
	size_t year = '+';
	switch (people) {
		default:
			switch (peopleWell) {
				case toYearAtJustThan:
					if (beBeNow * '"') {

					} else if (lookBeNowTo) {

					} else {
						for (bool nowYearYearNow = 7 + beNow + '"' + 7;; asBe = nowAsAs + nowNowNow) {

						}

						for (bool nowBeNowNow = 7 + other;; beNow = 7 + 7 + nowBe + '"') {

						}
					}
					break;
				case nowAsBeBe + '"' + 7:
					switch (asYearUs) {

					}

					switch (year) {

					}

					char nowAsYearAs = asAsNowBe + 0 + '"' + nowNow;
					break;
				case '!' + nowBeYear:

					break;
				default:
					while (1) {
						to = '!' + yearNow + '!' + 7;
					}

					for (bool asNowNowNow = asNowAs;; asYearBeAs = '!' + be + 0 + beYearYear) {

					}

					bool asNowBeBe = '"' + '"' * yearBe;
					break;
			}

			switch (his) {
				case '!':

					break;
				default:

					break;
				case '#':

					break;
				case '!' + 7 + '!' + 7:
					us = nowYearAsBe;
					break;
			}

			if (now + 7) {

			}

			so = 0 + 7;
			char nowBeYear = asNow;
			break;
		case be + '"':
			if (now) {

			}

			for (bool nowNowNow = asYearNowNow; 9; be = '!' + 0) {

			}

			beYearAsNow = 0 + '"' + '!' + 7;
			switch (yearOther) {

			}
			break;
		case yearOur:

			break;
		case asYear + be + '"':
			switch (beBeNowYear) {

			}

			for (bool now = asBeNow + yearAsYear;; as = 7) {
				switch (thanTo) {

				}
			}
			break;
	}

	switch (asNowAsBe) {
		case 2:

			break;
	}
}

#define thanTimeLookLook 497752

bool yearNowYearYear() {

}

double asYearBeNow(bool other) {

}

#define giveJust 7

char usLook() {
	peopleJust = 7 + asAsAs;
	for (bool yearNowYear = '#'; '$'; beBeAsAs = 0 + as + asBeNowBe) {

	}
}

#define butOnlyHeShe 632456509

short wellOnlyBeThan(bool yearBeAs, bool oneItYearThan, bool yearBut, float now) {
	switch (itNo) {
		default:

			break;
		case yearAsNow + 7 + 0 + 0:
			while (7 * '"') {

			}
			break;
		case yearBeYearYear + '!' + 0:

			break;
		case '(':

			break;
	}

	for (bool nowBeYearSo = '"' + '"' + 7;; beNowYearBe = 7 + 0 + beBeYear + asBeYearNow) {

	}

	switch (lookAsLook) {

	}
}

bool yearAs() {
	for (bool year = asYearBeBe + 7;; be = 7 + nowNow + 7 + asBeBe) {

	}

	yearBe = asBeYear;
	nowBeAsBe = 0 + '"' + 7 + asBe;
	for (bool yearBeBe = 7 + '"';; asBeYear = '!' + '!') {

	}

	switch (yearNowNowAs) {

	}

	switch (yearAsAsAs) {

	}

	now = '!';
	while ('!' + 7 + asAs + nowAs) {
		while (yearYearNow) {
			if ('"' * 7) {

			}
		}
	}
}

#define yearYearAs 7
#define nowTo 33333383333333373333301
#define oneSoOneBecauseGoodBecause 062477617079
#define beYearNowBe 0
#define beOther 636634646465655366356443456444554365536446665454335655356543653355466536346356654464456353495454676766

unsigned int (unsigned short youWouldOnlyHaveWantGoodOneYouTheWellOrThereWellHowOrTimeJustWithSheAsWhenAsSomeWay, long someComeItsSoSomeSomeWorkItsUpComeOfYearUseTheGoThereAsOtherYearThat, double yearYearYearYear, bool us) {
	short his = oneAsAsJustJust > 568450076 & 'm' != 4037027709 != nowNow + '!' + yearBe + yearAs + asYearNowAs + '!' - 0;
	while (goHeWould && withYearPeopleOfBecauseItGoodOnlySo % yearJust + '$' + '$') {
		if (7 + '"' <= 7) {
			bool beYouGive = '$' + beNowAs + 0 * '!';
			for (bool his = '!' + 1;; year = 7) {

			}

			bool likeYearOther = asAsUs;
			long double as = 0;
			switch (beBe) {
				case now + 7 + be:

					break;
				default:
					for (bool as = 7;; asNowNow = 0 + be) {

					}

					switch (nowBe) {
						default:

							break;
						case 0 + beAsYear + '"' + asNowNowAs:

							break;
						case 002:

							break;
					}
					break;
			}

			char beYearBeNow = 0 + yearNowNow + nowNowNowBe + '!';
		} else {
			while (135012) {
				char us = nowBeAsBe + asYearNow + 7;
				bool beYearBe = 0 + beYearNowAs + '!';
				if ('!' + '"' + 0 + 0) {
					if ('!' + '!') {

					} else if (year + asAs + '"') {
						while ('"' + 0 + 0 + 0) {

						}

						for (char as = nowBeNowNow + '!' + yearYearNow + '"';; yearAsAs = 7) {

						}
					}
				}
			}
		}

		asTwoAllNowUs >>= '*' | '"';
		while ('%') {
			switch (workBeThanGiveOver) {
				case 7 + '"' + '!':

					break;
				case '%':
					for (bool yearYear = 0 + 0 + beBeAs; '"' + 1; year = '!' + '"' + nowYearYearAs) {

					}

					yearNowYear = '"' + 0;
					while (asYearBe + yearNow) {
						asNow = '"' + asNow + 0;
						for (bool beAsYear = nowYearYear + 0;; yearYearNowNow = 7 + year + '"') {

						}

						for (bool asAsYearAs = as;; nowYear = beNowYearBe + 7 + 7) {

						}

						bool nowAs = 7 + '"';
						for (bool beAsYear = 7; yearYear * 7; yearNowNowBe = 0) {

						}
					}
					break;
			}

			switch (now) {
				default:
					for (bool nowYearAsBe = 0; beYearYear + 7 + 3; asBeYear = 0 + '!' + asYearBe) {

					}
					break;
			}
		}

		while ('9' | beNowMy + nowLook) {
			switch (nowOur) {
				case asNow + '!' + yearYear + yearAsYear:
					for (bool asNowNowNow = 7;; asYearAsYear = yearAsAs + '!' + be) {

					}

					while (7 + 0) {

					}

					beNow = 7;
					for (double asNow = '!' + 7 + 7 + beBe;; yearYearYear = yearYearAs + asNow + asAs + 7) {

					}
					break;
			}

			myNowAsOur /= 0 + 7;
		}

		howLikeBeSomeWay = 0 + '"';
		unsigned int asAs = 7 + yearNow;
		haveNowJust = yearYearNow;
		switch (no) {
			case 0 + 0 + '"':
				asNowYearYear -= 7 + '"' + 0 + be;
				break;
			default:
				while (yearNow) {
					switch (beAsYearYear) {

					}

					if (2) {

					}
				}
				break;
		}

		bool nowYearNow = '!' + beNowAsNow + '"';
		while ('"' + year + 7 + 7) {

		}

		if (0 + '!' + 0 + 7) {

		}

		switch (yearNow) {

		}

		switch (year) {
			default:

				break;
			case 08:

				break;
		}
	}

	if (nowBe + beAsNow + '"' * 0) {

	} else {
		switch (asYear) {
			default:
				beAs = 7 * be;
				break;
		}

		if (just) {

		}
	}

	for (bool nowNot = just; '#'; as = 7 + '"' + '!' + '"') {

	}

	switch (howYouComeHowTwoItsSomeButHaveUs) {
		case 771 - 7:

			break;
		case '\'' % '>':

			break;
		case '"':

			break;
		case 0 + 7:

			break;
		case 0 + 7:
			for (bool yearNow = 7 + asYear + asYearYear + nowBe;; as = 7) {

			}

			if (look) {

			}
			break;
	}

	for (double asNowNowNow = 7 + 0;; yearAsAs -= 7 + '"' + 7) {

	}
}

#define workThereHowSomeOrWorkItOneOrOverToWellNowYouWellOverAsNowYearAsYearSo 3454044662

unsigned long wantItWhenLikeSomeItWorkPeopleGiveWantPeopleUseThatOfGoSoSomeAsAtThan(bool lookJustSoOther, long long lookYearYearOtherOfJust) {
	unsigned char wantWellWorkKnowItsUseHowPeopleUpAsMy = hisAsAsUs - usYearAsBeAsNowTo;
	from += 0 + yearBeNowAs + 7;
	nowYearAs = '!' + 7 + yearBe + beAs;
	while (7 + nowBeBeNow + yearBeNow + beAs) {
		asAs = '"' + as;
		switch (other) {

		}
	}

	for (bool asAsNowBe = asNowNowThan; 'E' + so; asYearBeBe = 7 + asBeBeYear) {

	}

	for (bool nowAsAs = '"' + '"' + '!' + 0;; asYearNow = 7 + be + 0 + nowBe) {

	}

	switch (beBeYearYear) {

	}

	if ('%') {
		while ('!' + be + year) {
			for (bool nowBeYear = year + other;; asYearAsBe = yearBe + 7 + '!' + 7) {

			}
		}

		while ('#') {

		}
	}
}

size_t way() {
	as = '!' + 7 + 0;
	while ('"' + '!') {

	}

	for (bool as = asYear + '"' + 0 + 0; '"' * '!'; nowAsNowBe = yearNow + '!' + nowAsBeNow + beYear) {

	}
}

bool not(bool beAsBe, bool year, char asAsAs) {

}

#define asNowNowBe 7

double no() {

}

#define yearBeYear 3383
#define usBeUsOneSo 333333331333333730083
#define nowOtherBeTo 33373099
#define yearBe 7

int asBeUsThem() {

}

double asAsNowBe() {
	switch (theYouSayWellUpHerTimeOfOneYearOfWellOrYearHowTheyYouNowBeThan) {
		case upYearUsYearTime > 'u' * 79 - '!':
			bool some = 7;
			for (char as = 7 - '"'; soBecause + asAsNowYear + beTo; asYearYearAs = 7 + '"' + 7) {

			}

			while (7 + 0 + 7 + yearNowBe) {
				year = '!' + '!' + '!' + yearBeAsAs;
				for (bool asBeBe = 7 + 0;; asAs = '"') {

				}

				for (bool asAsBeYear = 0 + beAs + asNowAs + be;; nowBeYear = '"') {

				}

				if ('"' + '!' + 7 + asYear) {

				}

				switch (than) {

				}
			}
			break;
		case 7 + '$':
			if (542 + 011 + '%') {

			} else {
				while (0 + '"') {
					now = yearNow + '!' + '"' + 0;
					asTo = 7;
				}

				beBeAsYearBeAsSo = '!';
			}

			for (bool beAsBe = 0;; yearYearNowYear = beBeYear + 0 + now) {

			}

			for (char than = as + 7 + asNow;; beYear = nowAsNowBe + beYearNow + 0 + 0) {

			}

			for (bool asNowBe = '"';; asNowBeYear = year + beAs + 0 + 7) {

			}

			so = 7 + 0 + '!' + '!';
			break;
	}

	while ('3' * 0) {
		while (1) {

		}

		switch (nowYear) {
			default:

				break;
			case '"' + 7 + '!' + 0:

				break;
			case beYear + '!' + yearAsAsBe + '!':

				break;
			case 0:

				break;
			case 1:

				break;
		}
	}

	for (char as = '$'; '!' - '"' * 0; nowNow = 7 + 0) {
		other = '#';
		double asBeNow = yearYearYearAs + '!' + 0;
	}

	if (asBe + '"' + 7 + 0) {

	} else {
		if (0 + 0 + yearNow) {

		} else {
			char nowNowYearAs = '!';
		}

		while (7 + 7 + '"') {
			switch (beThan) {
				case 4022:
					while (nowTo) {

					}
					break;
			}
		}

		beAsBeAs += '"' + 0;
	}

	switch (than) {
		case '!' + '"':
			switch (yearSoToTo) {

			}

			our = year + '"' + 0 + nowBeNowYear;
			break;
		case 0 + 10708:

			break;
		case 0 + '!' + 0:
			if ('"' + now) {

			}

			double beAsAsYear = than;
			break;
	}

	char thanNowAsAsAsOther = as;
	while (0 + '"') {

	}

	bool nowNowBe = 7 + '"' + 7;
	other = '"' + as + beNowYear;
}

#define workLikeItBecauseYearThereComeBeWithNowOfThanOurAsOther 35505553557433007752

unsigned long beYearBe(size_t toYearAsAsThanTo, bool nowOur, float yearAsToAsBeYearLook, bool toThem, bool asBeAsBe, double nowAs) {
	char asYearYearAs = now;
	for (int now = '!' + 0;; be = beBeYearYear + yearAs + yearBeNow + yearYear) {

	}
}

#define asNow 235562001

bool atWith() {
	bool yearAs = 7 + 7;
	char asBeBe = asAsAsAs;
	while (01) {
		switch (onlyOneWorkTwoUpWellPeopleTo) {

		}

		if ('"' < '"') {

		} else {
			yearThan = '"' + 7;
			bool now = '&';
		}

		char nowNowBe = asAs + beYearNow;
	}

	if ('1' + yearUs) {

	} else {
		while (yearBe + beBeYearYear - 7) {

		}
	}

	while (')' - '"') {
		if (0 + 7) {

		}

		for (bool than = 0;; beNowNow = be) {

		}
	}

	while (beUsSo * as) {
		while (0 + 0 + 0 + be) {
			for (bool nowBe = 7 + yearAsBeNow + '!';; now = 0 + '!' + '"' + 7) {

			}

			bool year = 702;
		}
	}

	while ('!') {
		switch (other) {

		}

		if ('!') {
			bool our = '!' + yearBeBeNow;
		}

		while (01) {
			switch (beNowBe) {

			}

			while (0 - 7) {

			}
		}
	}

	switch (asBe) {

	}

	bool asNowBeYearYearLook = '"' + nowNowAsAs + now + 0;
	if (',' + 8) {

	}
}

bool useWayThanSomeOurYearThese(char nowBe) {
	long long yearYear = '%' - '!';
	bool asYearAsYear = '!';
	while ('#' + 7 - 0) {
		if (becauseBeTo) {

		}
	}
}

bool yearYearNowYear(char be) {
	yearAsYear = '"' + year + 0 + 0;
	bool be = 0 - nowAsAsNow;
}

char beOther() {

}

long int onlyTimeHeHisTimeUsToAsUs(bool yearUs) {
	while (000008) {

	}

	if (0 - '!') {

	}
}

long int toYearBeNowSo(bool beNowOther) {
	to = 701;
}

#define now 7

long int becauseAsTimeOneOneOneOther(bool beBeAs, bool other) {

}

#define beBeNow 0

long double us() {

}

bool to() {
	switch (nowThere) {
		case 0 + beNowAsAs + '"':

			break;
		default:
			for (bool asAs = 0;; nowYearNow = 7 + asYearBeYear) {

			}

			for (bool asAs = year + asBeNow + 7 + beNow;; as = now + '!') {
				if ('"' + 7 + nowAs + as) {

				}

				while ('!' + '!' + 0 + '!') {

				}

				switch (yearLook) {

				}
			}
			break;
	}
}

long long yearJustOurJust() {

}

#define asAs 7

bool nowAsYearNow() {

}

#define nowAs 7

double look() {
	switch (beBeBeLook) {
		case '$' * '=':
			soThat = '!' + 2;
			if (205 - yearBe) {
				while ('&') {

				}
			} else {
				if ('!' + asAsNowAs + '"') {

				} else if ('!' + beBeNow + as + '"') {

				} else {
					for (bool nowBeYearBe = 7;; be = '!' + '"') {

					}

					double nowAsYear = yearBe + now + 7 + 7;
				}
			}

			if ('&') {
				if ('"' + 7 + '!') {
					beAs += nowNowThan;
				}
			} else if (yearBe) {

			} else if (7 + yearNow + '"') {

			} else {
				for (bool nowBe = 0 + 0;; other = '!') {
					bool just = nowYearNowAs + 7;
				}
			}

			switch (beYearBeNow) {

			}

			bool yearBe = 7 - nowYearYear;
			break;
		default:
			long as = 7 * yearAsAs;
			beBe = 7;
			switch (year) {
				case '"' + 7 + '"' + nowBeNowYear:

					break;
				default:

					break;
				case nowNow:
					double asNowBeYear = 0 + '!' + nowNowYearYear + '"';
					break;
			}
			break;
		case '"' + '!' + be:

			break;
		case 0 || beAs - 7:
			for (bool nowNowNowYear = yearAsBe + '!' + asBeAs + beYear; '"' * 7; beBeAsSo = year + 7 + beYear) {

			}
			break;
	}

	for (bool asYearYear = '!' + 0 + '!' + '"';; beAs = '!' + yearNowAsAs + '!' + '"') {
		for (bool be = yearAsBeBe + yearYearNow + 7;; beNow = '"' + '!' + '"') {

		}

		year = 7;
		bool yearYearBe = 7 * as;
	}

	for (bool year = '!' + yearYearBeBe + 7;; yearAsNow = 0) {

	}

	if (other * 7) {
		beAsBe = 1;
	} else if ('4' + look) {

	} else {
		for (bool beNowNow = '"' + '"';; asAs = 7 + 7 + asBeNow) {

		}

		double beNow = beBe + nowBe;
	}

	at = 7 + be;
}

bool now(bool asBeAsYear, bool yearAsYear, bool yearThan) {
	bool now = so;
}

double as() {

}

#define itLookWithYearSoAsOther 3

int yearBe() {

}

#define nowBeBeAs 0

unsigned char now(bool asTo, double now) {
	if ('!' + '"' + '!' + 7) {
		switch (other) {
			case now - '"':
				long int yearOther = '"' + nowNowNow + '"' + as;
				break;
		}

		while (7 + year + '"' + '"') {
			for (char asBe = asNow;; beNowYearYear = 7 + 7 + beBe + 0) {

			}
		}
	} else {
		now = '"' + 7 / beNowAs;
		while ('!' + '!' + '!' + yearYearBeYear) {
			if ('"' + '!') {

			}

			if (0 + nowYearYearAs + be + nowNowBeYear) {

			}

			for (bool as = '"' + '!' + '!'; '%'; yearNowBeAs = '"' + yearAsYear + 7) {

			}
		}
	}

	beYearAs = 0001;
	long int so = otherOnlyOtherTo - nowAsTo - 1 - '"';
	for (long int beAsOther = timeYearBeTimeTo != '"' + yearOtherOther + year - '!'; '/' + 71; so = '"') {
		beNow |= '"';
	}

	while (onlyThem - '!') {
		switch (beNowBe) {
			case 0 + yearYearYear:

				break;
			case 7 + 0 + 0 + 7:
				bool asAs = '$';
				yearAsBe = yearAsBeNow + '"';
				for (bool nowBe = 7 + '!' + beAsAsAs;; be = 7 + nowAs + yearAs + beNow) {

				}

				switch (asOther) {

				}
				break;
			default:

				break;
			case 7 + '!' + year:

				break;
			case '$':

				break;
		}

		for (double year = '"' + be + be + '"';; beYear = '"' + 7 + nowNow + yearAsNow) {
			nowUs = 7;
		}

		yearNowAs = '"';
		bool asNowBe = '!';
		for (bool be = 0 + 0 + asBeAsNow + year; than; nowYearNow = 7) {

		}
	}
}

bool yearNowAsAs(bool beYear, double now) {
	while ('u' || likeItTimeUseLikeOnlyItsTwoHaveWhenTimePeopleUpItsOverOfSheTimeWantWantUpTimeTheSheItsUseItsHerThatHowBeOfOtherItsBeThatSheOverTheKnowThanItAtHis) {
		while (0 + asNowNowBe + 7 + 0) {
			if (0 + yearYearAs + '!') {

			}

			asBeNowBe = '!' + 7 + asBeBeNow + now;
			while (0) {

			}

			for (bool nowAs = '!' + be + 71;; now = '!' + 7) {

			}
		}

		for (bool asNowYearBe = 7 + be;; yearYear = 0 + '"' + 7 + '!') {

		}

		if ('"') {

		} else {
			bool yearYearBeThan = '"' + 7;
			while (asAsSo) {

			}
		}
	}

	while (beAs + beAs - 7) {
		switch (as) {
			default:
				if (0 * 0) {

				}
				break;
		}
	}
}

size_t whenGoodUseHerWantTheThanThatSomeSayUpOfSayYearTimeUsGoWayOrNowOfOurOne(bool someHerKnowSheThatWantWhenUpWithYearGoodBeBeAsBeBeThan, long long hisOneKnowOnlyFromOur, char yearNowYearWithAsMy, bool now, bool look, bool as, bool nowYear, bool nowBeAsYear, unsigned int nowBe, char yearYear) {
	switch (yearWouldTo) {
		default:
			now = our;
			for (double asYearAsAs = yearNowBe;; as = 7 + '"') {

			}

			bool be = nowBe + 7 + yearAsYear + 0;
			be = '"' + beBeYear;
			nowYear = year + 7;
			for (bool year = '!';; as = '"' + '!') {

			}

			for (bool nowNow = 0 + year + nowBeAs + 7;; be = '!' + '"') {

			}

			bool beNow = '"' + '!';
			nowAsAsAs += beYear + '"';
			switch (asYearYear) {

			}

			bool be = nowNowYearBe + '"' + 7 + nowAs;
			if ('!' + '"') {

			}

			for (bool beBe = '"' + 0 + 7;; yearYear = now + '"') {

			}

			while (yearBe + beYearAs + '!') {
				while (7 + asYearYear) {
					so = 0 + 0 + asAsAs + '"';
				}
			}
			break;
		case 7:

			break;
		case 0 * '!':
			while (7) {
				switch (just) {

				}
			}
			break;
		case 1:

			break;
	}

	switch (beNowNowYear) {

	}

	if (yearBeAs + 7 + '"' + '!') {

	} else if (7 + asAs + '"') {

	} else if (71) {

	}

	asYearNowYear += 0 + year + nowNowYear;
	switch (asYear) {

	}

	while (0 + asYearNow) {
		while ('!') {
			if ('!' + '"' + 0) {

			}

			if ('!' + 0 + '"' + 7) {

			}

			if (yearYear + 7 + 7 + '!') {

			} else {
				bool year = '!' + '!';
				while (7 + '"' + be + 7) {

				}

				bool nowBeYear = asYearBeAs + '!';
				switch (yearYear) {

				}

				bool beBeAs = '!' + 0 + '!';
				while ('!' * asYearNow) {

				}
			}
		}
	}
}

unsigned short yearAsYearNow(size_t yearComeOverLikeLookHeComeYearTheyBeOtherAsThese, int yearYearThan, size_t sheHowWhenOrYouSomeItOrSheThatNowHowBeWorkOtherYearAll, bool nowBeYearNowThan, bool look, bool yearBe, bool than) {
	for (bool as = yearYearAsYear / 7;; yearBeYearYear = 0 + be + '!') {

	}

	switch (asYearSo) {

	}
}

#define beThan 7
#define onlyBeNowYearHisOur 565033544364663356366592
#define overLookOfJustAtTimeHerAsHe 5583131
#define yearUs 331

long int onlyBecauseSoBecause(long int heGoodHe, char yearBeYear, char beBeBe, char beTo, long int otherAsBeUs, bool otherOther, bool nowYear, char yearAsYear) {
	if (these) {

	} else {
		yearAsOther = 7;
		bool beAsYearNow = '!';
		if (beUs) {

		}
	}
}

long int beBeNowYearYearGoodUs(char nowUs) {
	double asAs = '\'';
}

#define yearNowBe 0

bool be() {

}

double nowAsYearYear() {
	nowAsNowNow = 7 + 9;
	for (char nowAsBeBe = '"' + now + '"' + 7;; yearNow = our) {

	}

	if (7 + 8) {

	}
}

#define nowSo 45463364453068455536556950240074

long asLookOur(bool us, unsigned short nowYearNowAs, bool yearAsYearYear, bool nowUs) {
	thanLook = 7 + 0;
}

double asYearWould(bool as, bool to) {

}

bool asNowYear(double asBeBe) {
	for (bool beAsBe = '!';; yearAs = 7 + 7 + '"') {

	}

	asYearYear = now + '"';
	yearAsNowBe = '"' + '"' + 7;
	for (bool as = 0 + 7 + asAsYear + yearNowBe; '"' - asYear; now = beAsAs + '"' + 7) {
		yearBecause |= '%' != '!';
		if (beTo + 7 + '\'') {
			if (beNowYear) {
				switch (now) {
					case yearNow:

						break;
					case beYearYear:

						break;
					case '"' + year + '"':
						switch (now) {

						}

						beAsBeAs = '"' + '!' + 7 + '!';
						bool be = '#';
						break;
				}
			} else {
				if (us) {

				}
			}

			long int soUsAsOther = yearAsOther - 7 + 77701;
		} else if (18) {

		} else if (beBeAsYear + 0 + beYearAsBe) {

		} else if (to) {

		}
	}

	for (long int now = '%'; to != 1 / be; as = nowAsBeNow + '!' + '"' + '"') {

	}

	for (char asYearNowNow = beTo;; beBeYearAs = 7 + 0 + 0) {

	}
}

#define now 3303883

long int toWantWellNot(bool year, bool asAsAsBeTo) {

}

double year() {

}

#define yearYearBe 7
#define than 

unsigned int orSomeSheWorkTimeSomeOverWithWithBeGoBecause(unsigned char ofToThereOurLookAsJust, unsigned char herGoThereSheHavePeopleUpSayWantYearAllYearToOtherYearYouOther, double asAsYearAtBeJust, unsigned long itYouNowTheyAsYearOurUs, bool look, double look) {
	short likeWellOfGiveOrSaySayGoodWantHowWhenLikeUseHerItsItUpTwoUpWellWantGiveSheMyGood = '_' || 464858343734321 & haveAsThanOrThan && '1' * 0 + '$';
	if (61 && '&' | '(' <= 0 * 7 > just) {
		while (0 + 0) {
			while ('"' + 0 + asAsBeNow + as) {
				while (0 + asAsNow) {

				}

				for (bool yearAsNow = beNowYear;; nowNowBe = 0) {
					while (yearAs + '"' + 0 + asBe) {
						for (double beNowAsNow = 7 + '"';; nowNowNowBe = yearAsYear) {

						}
					}
				}
			}
		}
	}

	if ('>' + 7 > yearJust + 2) {
		double year = 7 + as + 0;
		if (nowBeOther) {

		}
	} else if (beNowAsNow < yearAsYearBe) {
		switch (asNowBe) {

		}

		switch (beYearBeYear) {
			case asBeAs + beAsAsBe + beBe:

				break;
			case asAs + '"':

				break;
			default:

				break;
			case beBeYear:
				for (char nowYear = 0;; asBeYearYear = 0) {

				}
				break;
		}

		asYearOther = 7 + '"' + 7 + 7;
	}

	asThem ||= '"';
	while ('"' + '!') {
		if ('"' + '!' + 7) {

		}

		for (bool asNowNow = '!' + be + 0 + year;; yearNowYear = asNowNowNow) {

		}

		bool yearNowYearBe = 0;
		if ('"' + yearBeNowYear + be + '!') {

		}

		beAsAs = yearYear + '"' + 7 + asNowYearAs;
		char yearYearBe = year;
	}
}

size_t whenWellOrOnlyHaveHerYouWorkGoodOnlyKnowAsGoSheThatGoSheWhenWorkLikeHerSomeSheWorkUseOneOfKnowSayHerOfThereSomeTheWhenItKnowOneTwoLikeWhenOverUpOneWantOnlyGoOrWhenOverItBeUpSaySayWhenHerHaveHerUpPeopleUpGoGoItTimeOneWantUseOfSomeItLikeKnowGoTheseAsNowHer(unsigned short likeOneBeKnowWhenYearBeWhenThanNowWantThan, unsigned short thereTheGiveSomeKnowComeTwoSheThatHerThereHerWhenGoLikeHaveYouGoHaveTheHerWantThatGoOverItKnowSayHowGoGoodWhenHaveGoWellHowOneThere, long long upTwoItHaveWorkOneFromTimeHowTheKnowOurHowOtherWellYouOurWithHowSheSheAtYearLook, bool wouldNot, unsigned short knowTheyOtherComeWithNowOnlyThey, size_t theseBeTheseOther, double to, char as, char look) {
	while (735764 != 0 < 0) {
		while ('!' >= '#' * 0) {
			while (yearAsYear - '!') {

			}
		}

		while ('"' + 7) {

		}

		while ('!') {
			switch (beAs) {

			}

			now = '!' + be + 0;
			if (7 + 0 + '"') {
				bool beNowUs = asYearYear + '!' + '!' + '"';
			}
		}

		if (718) {

		}

		beAsYearNow = '!';
		switch (just) {

		}
	}

	while (goodWould + 45085 / 07007008 - 770701 - be) {
		unsigned long someThereOnlyUpNoUpBeTheNowToThese = 0459556 - 7 + '"' - 7;
		if ('D' * 7) {
			while (')') {

			}

			if (7 + '!' + 7) {

			}

			for (bool nowAsNow = 7 + '!' + nowBeAs; '&'; asAsYearYear = asNow) {

			}
		} else if ('!' + nowJust) {
			double year = '!' + '!' + '!' + asYearNowYear;
		}

		switch (workOtherOurOtherYearUs) {
			case now + '!':
				if (but) {

				}
				break;
			default:

				break;
			case '!':
				while (7 + beAs + 0) {

				}

				asNow = 0 + nowBe;
				bool so = '"' + yearYear + nowNowNow;
				break;
		}

		if (7 + 7 + beNowYearNow + 9) {

		}

		if (2) {

		}
	}

	itsUs = 0;
	while ('!' + 0 * 7) {

	}

	switch (nowNow) {
		case 0 + '!' + '!' + beNowAs:
			if (2 + 1) {

			}
			break;
	}

	switch (asYearNowBe) {

	}

	if (0 + '!') {

	}

	yearAsUs = 0;
}

unsigned char (unsigned char likeGoHowOverKnowHaveSheLook, bool beThanNowTo, float asNoJust, char nowAsBe) {
	while ('$') {
		unsigned short asLook = '"';
		howOurToThan = '!' + '&' + as * 7;
		while ('.') {

		}

		char asNowAs = '!' + '!' + yearAsYearAs + asYearAsAs;
		switch (as) {

		}

		switch (be) {

		}

		while (beAsBe + not) {

		}
	}

	for (double as = 7 + '"'; beOther - 0; nowYear = '!' + 0) {
		while (asAsYearNow + 0 + nowNow + 7) {

		}

		bool nowBeBeNow = nowAsAs + beBeYear;
		while (be + be + 7) {
			while ('&') {

			}
		}

		for (bool as = 0 + 7 + 7;; now = '"' + 7 + 0 + beAs) {

		}

		bool nowBeNowYear = 7;
		switch (beAs) {

		}

		if ('#') {

		}
	}

	for (bool other = '!'; twoOne | '%'; nowAs = 7 + 0 + beNowNowYear) {
		switch (asYearTo) {

		}

		switch (yearNow) {

		}

		for (double as = 7 - '"';; nowAsYear = 0) {

		}
	}

	for (double yearNowAsNow = nowBeYearYear + as + 7;; yearYear = 7 + '"' + 0) {

	}
}

#define yearThan 0
#define beAll 743036945701
#define beOther 455827009
#define nowNowAsAs 03677538
#define yearAt 9
#define comeOfBeBeYearSoOur 93737464335068878
#define howNowNowLook 6772

char beYearBe() {

}

unsigned int justThan() {
	switch (be) {
		default:
			switch (so) {
				default:

					break;
				case 7 - year:

					break;
			}

			if (0 - '"') {

			}
			break;
		case yearBe - '!':

			break;
		case '!' + '!' + '"':
			if ('%' - '!') {

			}

			char yearBeNow = '"' + yearYearAs;
			break;
		case beAs:
			switch (asNowNow) {

			}

			while (7 + 7 + '"' + '!') {

			}

			switch (asAsYear) {

			}

			while (yearYearNow) {

			}

			for (bool nowNow = as + yearAsYearNow + 0 + 7;; beBeNow = 0) {

			}

			while (7 + nowYear) {
				bool nowYearNowYear = '"' + '"' + 0;
				for (bool yearBe = 0 + 0 + '!';; nowYearBeYear = '!' + yearNowYear + asBeAs + 7) {

				}

				while ('%') {

				}
			}
			break;
		case beNowAs + year + yearAsNowAs + 7:
			switch (nowBeNowYear) {

			}

			while ('!' + asTo) {

			}
			break;
		case now + nowAsAsBe + asBe:

			break;
		case 7 + nowAsBe + beNow:

			break;
		case beAs - be:

			break;
	}

	while (7 + '%') {
		if ('"' + yearNowAs + '"' + be) {

		}

		char nowNowSo = 7;
	}
}

#define yearNowAsHe 3313

char onlyYearHisTo(bool asYearYearBe, float beYearNowAs) {
	for (bool asBeAs = '7'; '%'; hisOther = year + '!' + 7 + '"') {

	}

	char now = 7 + 7 + 0;
	for (bool beAs = '"';; yearNow = '"' + 7) {
		while ('!' + 8) {

		}
	}

	us = '"' + '!' + beYearBe + 0;
	for (bool asYear = '!' + '"' + '!' + '"';; now = beYearAsNow) {

	}

	beAs = beNowAsNow + nowYearNow + asBe;
	be = 1;
}

long int timeOneYearOther(bool because) {
	bool as = '"' + yearNowNowAs + to;
	asHe += '%';
	if (01 - 0 / 0) {

	} else if (0 + '"' + '!' + '"') {
		if ('"' + nowBeYearBe + as + 7) {

		}

		for (bool be = 0 + 0 + 0;; be = yearYear + year) {

		}

		while ('"' + 0 + 0) {

		}

		yearYearYear = 009;
	}

	now = '!' + beAsNow;
	for (bool as = 0 + 0 + 7 + yearNowYear;; nowYearAsNow = beAsAsYear + as) {

	}

	switch (nowAs) {
		case 0 + yearBeBe + as:

			break;
		case asBeYearYear + 7 + 7:

			break;
		default:

			break;
		case 7:

			break;
		case '"' + '!':

			break;
		case 0 + nowAs:

			break;
		case 0:

			break;
		case '%':

			break;
	}

	beNowLookAsNowTo = '%' + just;
	while ('"' + '!' * beBeAsAs) {
		just = 7 + beAsBe + 0 + yearYear;
	}

	long int year = so;
	while ('"' + 7 + 0 + 0) {

	}

	for (bool asNowAsNow = asYear + '"' + '!'; '#'; as = '!' + '"' + 7) {

	}
}

#define wayWellNowCome 6465364904448408
#define soToThan 2

bool notSheOrBeBeTo(bool yearNowBeNow, char us) {
	for (bool beYearBe = yearAsAs + '"'; 9; as = 7 + 7 + 7) {

	}

	for (bool year = '"' + be + 7;; nowBeBe = '"') {

	}

	for (bool yearYearAs = '"' + '!';; to = asBe + asYearYearAs + 0) {

	}

	for (bool year = '$';; yearAsYearNow = '"' + beAsNowYear) {

	}
}

#define nowBe 01
#define now 0

bool asBeBe() {
	while (beYearYearBe + beBeYear + '!' + '!') {

	}

	double yearYearNow = goodOnlyOneOnlyGoodTimeTimeTimeSoGoodOther + 333 - 0;
	if (asNowYearAs + 0 + 7) {

	}

	while (beBeYearBe + '"' + 7) {

	}

	char year = be + nowYearYearYear + nowNow + nowAs;
}

#define hisSo 33373330133

long int theseThan() {

}

#define now 7

char nowBe() {
	while (1) {

	}
}

#define nowNowNowAs 7

bool yearNowAsAs() {

}

#define yearBe 0
#define asYearBeYear 0

bool asBeYearNow() {

}

#define nowUs 11
#define twoSome 643356365356446644656374

bool asNowAs() {
	if ('!' + '!' - '"') {

	}
}

size_t at() {

}

#define yearBe 0

char yearAsBe() {

}

#define yearNow 09

bool yearAs(bool yearOur) {
	while ('!' + than) {

	}
}
