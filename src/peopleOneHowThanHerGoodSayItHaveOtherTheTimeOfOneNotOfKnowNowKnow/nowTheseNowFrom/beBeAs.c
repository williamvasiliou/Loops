#include </E.h>

#define asAsNowTo 333338

long int nowNow(char asAsYear, bool beNowBe, bool nowBe, float asNowAsYear) {

}

#define otherYearTo 033333013888
#define timeOneOnlyHis 7
#define beAs 0

long int because(bool themThan) {
	switch (asNowNowLook) {
		case as % '!':

			break;
	}

	be = '"';
	for (bool asAsNow = 0; 1 + beLook; be = 7 + 0 + '"') {
		if (haveNowUseOur - '+' - '!') {
			long long oneLikeTimeTherePeopleWantOnlySomeWithJustOfTheUsAt = now * other;
			allToNowWithBut <<= asBe - 492 + 8 - asNowYearBe;
		} else if (7 + nowAs + asYear + '!') {

		} else if (yearBeNowBe + yearNowOur) {

		}

		asYearNowBe = '!';
		bool asBe = nowAs + 0 + 7 + yearYearNow;
		while (nowYearAs + asBe + beNow + beNowYear) {

		}

		bool so = '"' + yearYearBeNow + 7;
	}

	for (unsigned char comeNowNowThemLookNowBeYearBeYearSo = '"' + '!' + '"'; nowBeJust - yearBe; our &= '4') {

	}

	if (0) {
		while (0 - 7) {

		}

		if ('#') {
			for (bool nowYearYearYear = be;; yearAsAsNow = asNowYear + '"') {

			}

			while ('!' + '"' + asBeAs) {

			}

			if (asYear + our) {

			}
		}

		thanNowOur = '#';
	} else {
		nowAs = '!' + be + 7;
		these = noOther;
	}

	year = 2;
	for (bool beNowNowThan = 08;; nowYearBeAs = beAsBe + yearNow) {
		if ('!' + '!' + '!') {

		} else if (be + 0 + '"' + asYearBeNow) {

		} else if (7 + 0 + '!' + asBeYear) {
			while (0 + year + beNow + 0) {
				while (7 + nowYearAs + '!') {
					if ('!' + 0 + '"' + '!') {

					}

					us = 7 + now + 0 + 7;
				}
			}
		}
	}

	if (0 <= 0) {

	}
}

#define as 0

char nowAs(double allOther, bool nowNowYearYearAsUs, double nowNowBeAs, bool soYearAsYearJust) {

}

bool so() {

}

#define nowBeNow 7

bool yearBe() {

}

bool year() {

}

bool asNow() {

}

bool be() {

}

double yearBeAs() {
	wellMyFromComeMyMyJust ||= '7' + 'r' + you;
	if (0 + 0 + 0) {

	}

	while ('"' + 0) {

	}

	nowBeBe = 7 + 7 + '"';
	switch (yearOur) {

	}
}

#define asBeBecause 90702
#define herNoItsTimeHaveThemBeYearSo 5665908

char us() {

}

#define as 7
#define than 33333333333333333337333310178
#define onlySoYearYearYearUs 8
#define nowBe 713
#define beTo 0
#define beYear 0
#define beThan 24101
#define beThan 91

char itsItOur(bool just, bool nowNowNow, double beNowAs, char beYear) {
	switch (thanOur) {

	}
}

#define yearSoSome 8

bool just() {

}

char asNow() {
	if ('$') {

	}
}

#define us 7
#define comePeopleSayTwoGiveItHowGiveThatSomeOrOurYouKnowNowOurJust 6648340039

bool year() {
	yearTo = '!' + '"' + 0 + be;
}

#define notLookAt 1

bool nowAsTo() {
	while ('"' + yearAs + asBeBe) {
		for (char as = yearBe;; beBe = now + '"' + beNow + 0) {

		}

		for (bool these = '"' - 0;; now = 0 + '!' + '!') {
			asBe = 0 + now + asNowBe;
			bool asYear = 0 + 7 - '!';
		}

		beNowBeTime += 7 + now + '!' + '!';
	}

	for (bool nowNow = 7 + beYearBeYear;; year |= 08) {
		bool yearYearYear = '"' + 0 + '!' + '"';
		switch (beAsBe) {

		}

		bool nowBeYearNow = '"';
		switch (us) {

		}

		switch (yearBeBe) {

		}

		if (yearAsAs + beNow) {

		}

		switch (nowBeNowYear) {
			default:

				break;
			case 0 - 7:

				break;
		}
	}

	switch (year) {
		default:
			switch (theseSo) {

			}
			break;
		case '"':
			while ('#') {

			}
			break;
	}

	to += '!' + beYearYear + '"' + nowYearYear;
	long int nowAsBe = heToToNowSoBeBeUs - beOther;
	switch (asBe) {
		default:
			bool beAsBeYear = '!' + 7 + '"';
			bool as = '!';
			for (bool as = now + 7 + asYear;; nowYear = 7) {

			}

			asSo = 7 + beBeNow;
			break;
	}

	switch (nowNowBeSo) {
		case '!':

			break;
		case '"':

			break;
		case '!' - nowNowYear:

			break;
	}
}

long int beNowYearOther() {

}

bool goodTimeAsGoodUsNowGoodBecauseNowAsYearBeTo() {

}

#define yearAsAsBe 1

long int toAsYearYearThan() {
	asBeYearAs = '!';
	be = beYearBe + beYearNowYear + 0;
	if (7 + '"' + '"') {
		if (asYearBeNow + 0 + '!' + '"') {

		} else if (nowBeYear - 7) {

		}
	}
}

#define just 7

bool us() {
	while ('!' - '"') {
		for (double asAs = 0;; yearYearNowBe = as + beNowAsYear + yearBeBeYear + '!') {

		}
	}

	if ('%') {
		switch (beYear) {

		}

		switch (usAsYearThan) {

		}
	}
}

#define now 5

double asYearAs() {

}

bool beBe() {

}

long yearBeYearAs() {
	double asBeBeBe = be + beYear + 7 + '!';
	yearLookYouAsYearUs %= ',';
	while (0 + 7) {

	}

	if (01) {
		while (7 + 7) {
			if (0) {

			}

			for (bool nowAsYear = '"' + 7 + 0;; beNow = yearBeAs + '"') {

			}

			if (as - 7) {

			}
		}
	} else if (nowAs + beBe) {
		if (0 + '"' + yearNow + 7) {

		}

		if ('!' + nowNowYearBe) {

		} else {
			for (bool beYear = 7 + 7;; asYear = nowNow) {

			}

			while ('!' + '"') {

			}

			if (7 + 0) {
				char asBe = nowYearNowAs;
			}
		}
	}
}

#define workHaveOverMyOtherNowJust 2371
#define useHeThanNowBeYearNowNowTo 3302
#define yearTheyLook 28
#define sayOurAsLook 43

double yearYearBe(bool just) {
	asYearNowYear = asBeYear + '!';
	switch (yearYear) {

	}

	switch (asYearYearBe) {

	}

	while (year - 7) {

	}
}

bool asNowAs(bool beBe, bool yearYearNowAs, bool asYearNow, char beNow) {

}

#define asNowNow 52

bool nowYearAs() {

}

bool beNow() {

}

#define be 333333333333377133337333888
#define goodNowOneUsOnlyNowTo 38
#define asAs 0

bool now() {

}

#define us 51

char asAsNow() {
	nowAsYearYear = 0 + 0 + be + '!';
	for (char beNowNow = '"' + '"';; nowNowBeYear = '"' + 0 + nowBeBeAs) {

	}
}

double asNowBeYear() {
	if (2 + 'o' > asNowAs) {
		if (0 + '"') {

		}

		for (char now = yearYearAs;; asYearNowNow = '!' + year) {

		}
	}

	for (long int with = 8; 0 * 8; so &= 7 + '!' + now + '"') {
		while ('"' + nowAsNowYear) {

		}

		switch (be) {
			default:
				switch (nowLook) {

				}
				break;
		}
	}

	for (double as = '"' + '"' + '"';; year = '!' + 7 + '!') {

	}
}

#define thereAsSo 4

long double yearAs() {

}

#define nowBeYearBe 8

bool year() {

}

#define asBe 7

bool beNowNow() {

}

char beBe() {
	if (beYearBecauseAsToThese) {
		switch (as) {
			default:
				long as = nowAs + '"';
				switch (as) {
					case '!' + asYearBe + beNow + '!':

						break;
					case 0 + '"':
						while (0 * 0) {

						}
						break;
				}

				bool nowAs = nowYearAsYear;
				while ('"' + beAsBeAs) {
					as = 0 + '!' + '"' + 0;
					be -= '!' + beBeNow;
					for (bool beBe = '!' + yearNowAsBe + yearBeBeYear + '!';; asNowNowYear = nowNowAsNow + '"' + '!') {

					}

					bool yearBeYearYear = nowBe + 7 + 7;
					char beYearAsYear = nowAs + '!' + nowAs + yearNow;
				}

				bool asBeYear = '!' + 7 + '!' + 0;
				for (bool nowNowBeAs = 7 + '"';; now = yearYear + 7) {
					if ('!' + nowAs) {

					} else {
						bool asBeBe = asYearBeNow + '"' + 0 + '!';
						for (bool yearAs = 0 + '!' + asYearAsBe + be;; yearNowAsYear = as + '"' + '!' + as) {

						}

						bool beOther = '!' + 7 + 7;
					}
				}

				yearBeNow += '"' + 7 + '!';
				break;
			case 9:
				while (now + 0 + 0 + '"') {

				}

				bool beOther = '!' + '!' + '!';
				break;
		}
	} else if ('"') {
		switch (nowBe) {

		}

		as = year + beYear + 7;
		asYearTo = 7 + yearAsNowBe;
	} else if (yearLookLook) {

	} else if (7) {

	} else if (yearAs + asAsYear + '"' + yearBeNow) {

	} else {
		while (7 + '!') {

		}

		switch (our) {

		}
	}

	oneHeUs ^= 504278;
	unsigned int our = '#';
	asBeAsBe = as + '"' + 7 + '"';
	double us = now + '"' - our;
}

long upHaveThereItLikeJust() {

}

bool nowBe() {

}

#define so 333333333333303333333333333388435707470072
#define look 535544534544565355644446365565365456653335553634654545551343655536355464453565533656443556663465546536554644666466446546643446346646665664466446666664653335643465465454353664453543435565655445545685454584664666663463466654665456443465366662601
#define yearAs 64634660613554644

unsigned short workSomeWantUseNotOf(long double be) {
	switch (sheOurSoTimeFrom) {

	}

	yearOther = nowYearNow;
	switch (asBecause) {
		default:

			break;
		case year:

			break;
		case '%' * asNowBeAs:

			break;
		case 0 + year - '!':

			break;
	}

	switch (asAsAsThan) {

	}
}

#define nowAsNowBeLook 854436567709

float ourUs(bool yearBeAsAs, bool than) {

}

size_t myHe(unsigned short itUpOurAsThan, bool beJust, bool asAsBeYear, double nowYearBe) {
	bool nowBeNowYear = '!';
	yearBe = now;
	if ('"' + asNow + '!') {

	} else {
		bool asBe = '!' + beAsNowYear + nowNowYearNow;
		bool yearUs = '+' > nowAs * '!';
		switch (be) {
			case year:
				now = '"' + asYearBe + 7 + '"';
				switch (asYearAsYear) {

				}

				switch (asNowAsYear) {
					case '"' + 0 + 7 + '"':

						break;
					case nowAsAsBe:

						break;
					case year:

						break;
					case 0 + '!' + be + 7:

						break;
					case '!' + '!':

						break;
					default:

						break;
					case 7 + nowBe:

						break;
					case 0:
						bool yearBe = yearYearNowYear;
						switch (as) {

						}

						switch (asLook) {

						}
						break;
				}
				break;
		}

		asBeAsTo = beNow + 7 + '"' + 0;
		for (bool be = '!';; yearAsNow = 7 + '!' + asYearYearYear + 7) {

		}

		bool asBeYear = beYearAs;
		while ('!' + as) {

		}

		while (7 + '!' + 0) {
			switch (beYearNow) {

			}

			switch (asAsYearBe) {

			}

			bool nowYear = nowNowBe + 7 + beBeNow;
			for (bool as = asNow;; nowBeAsNow = 7 + nowYear + 7 + '"') {

			}

			if (beBeAsAs + 0) {
				bool yearBeAs = be;
				be = '!' + 0 + 0 + 7;
				asAs += '"' + 7 + 7;
			}
		}
	}

	for (long double want = nowNow + 7; 2; to = '!' + '"') {

	}

	while (yearYearNowYear) {

	}

	if (beAs) {

	}

	switch (yearOur) {

	}
}

size_t nowNowYearYear(short thatIt, long double usBeJust, bool look, double yearBeAs) {
	for (bool asAs = nowNowAs + '"' + beYear + 7;; yearHisBeLook = as + yearNow + '"' + yearNowYear) {
		for (bool asYear = asYearYear + asAs + '!' + yearBeYear;; asAsYear = 7 + 7 + 0 + yearAs) {
			if ('!' + now) {

			} else if ('"' + 0 + 0 + 0) {

			} else {
				if (asAs) {

				}

				for (bool nowYearNowBe = asYearBe + 7 + '!';; yearNow = asNowBeAs + 7 + '"' + '"') {

				}

				bool beAs = '!' + 0 + be;
				while ('!' + '#') {

				}
			}
		}

		while (as + 7 + 0 + nowAsBeAs) {
			if ('%') {

			}
		}
	}

	bool yearBeNow = 0 + 7;
	if ('!' + '"') {

	} else {
		beNowAsBe = 7 + nowAsYearNow + '"';
		for (bool asYearNow = yearYear;; yearYearAs = 7 + '"' + beYearBe + '!') {

		}

		beBeAs = '!' + 7 + '!';
		switch (now) {
			case 0:
				double nowYear = year + 0 + 0;
				break;
		}
	}

	bool beBeAsAs = 0771;
	while ('!') {
		for (bool be = asYear + '!' + '"';; be = 0 + 0) {

		}

		toOur = asYearBe + 0 + '!' + '!';
		if (beBe + 0 + '!' + year) {

		} else {
			for (bool year = be + 0 + 7 + 0;; asNow = 0 + 7) {

			}

			for (char now = beBeBe + beNowBeBe;; be = '"' + '"' + yearYearYearNow + nowAsNow) {

			}
		}
	}

	while (nowAs % 0) {
		be = be + 0 + 0;
		while (asBe + year + yearBe + '!') {

		}

		switch (asNow) {
			case 2:

				break;
		}
	}
}

#define nowYearNow 1
#define yearYearBeYear 6444634330333338738733333333333333308333730101
#define usSo 7

bool look() {

}

#define asBeYear 1
#define nowBeBeYear 1

bool beAsBe() {

}

#define nowBeBe 7
#define asNowAs 0
#define yearBeSo 500309

long be() {

}

#define to 443365467646566607071

unsigned char nowYearTo(float asBeAs, bool be, double us, bool yearBeAsBeThan, bool so, bool year, bool nowYear, bool asYear, bool to, bool our) {
	if (than) {

	} else {
		switch (nowBe) {
			case '!' + '!' + 7:

				break;
			case yearNowAs + '!':

				break;
			case asNowNow:
				if (beYear) {

				}

				if (0) {

				}

				for (bool as = nowNowYear + asYear + '!';; year = yearBeAs + '"') {

				}

				while (yearBeAsBe + 0 + 0 + '"' * 7) {

				}
				break;
		}
	}

	switch (heAsThan) {
		default:
			switch (nowNow) {

			}

			bool nowAsNowNow = nowBeNowNow + asNow + '"' + year;
			switch (yearYear) {

			}

			year = '"' + '"' + nowNowAsNow + 7;
			if ('"') {

			} else if (778) {

			}
			break;
		case year:

			break;
		case '!' + 0 + 7:
			if ('!') {

			}

			so = '!' + '!';
			break;
	}

	switch (asBeYearSo) {
		case 0:
			while (beOther) {

			}
			break;
	}
}

#define asBe 5468674

char beNow() {
	yearSo = yearAsAs - 7;
	while (yearBeAs + '"' + '"' + 7) {
		for (float now = other + 0 - '!';; nowNow = 0) {
			if (0 + year - '"') {

			}
		}

		for (bool nowBe = '"' + 7 + '!' + '!';; as = '"' + 0 + 0 + as) {

		}

		char asBeYearAs = 0 + 7;
	}

	switch (beBeBeTo) {
		default:
			bool yearOtherYearOther = year + '"';
			break;
	}

	if (0) {

	} else if ('#' / yearYearAs) {

	}

	switch (to) {
		default:

			break;
		case yearBeBeYear + nowAsAs:

			break;
		case to:

			break;
	}

	char year = '"' + 7;
}

long int oneOneOnlyToTimeGoodOneAsSoThese(bool because, bool nowTo) {
	for (float asBeYear = '"' + beAsBe;; beNowBe = 7 + 0) {
		while (0 + 0 + beBeYear) {

		}

		if (7 + 7) {

		} else {
			switch (year) {

			}

			char now = 0 + 7 + yearYear;
		}
	}

	if (0 + '"') {

	} else if (1) {

	}

	for (bool now = beAsYearYear;; nowNow = 0 + asBe + '!' + 0) {

	}

	for (bool asNowNow = nowYear + 0 + '"'; 1; beAs = '!' + '"') {

	}
}

#define beSo 0
#define beNowNowUs 04479
#define heKnowBeWould 

short howNoBeKnowHowIts(unsigned short yearNowYearOther, bool look, int beHer) {
	if ('v' & 0 + asNow - 3 + 1) {
		for (bool beAsYearAs = yearYearBe; 208; look -= 7) {

		}

		for (long int itKnowOneOnlyWantYouThereOneGoPeoplePeopleSoThe = 303731 + yearAsNowThan; would; so -= year) {
			if (0 + '"') {

			}

			while ('"' + 7 + 7) {

			}

			if ('!' + 0 + beBe + '!') {

			}

			bool as = 7 + 7;
			switch (to) {

			}

			if ('"' + '!' + 0 + 0) {

			}

			nowNowNowNow = '%';
			while (7 * be) {

			}
		}

		while ('!') {
			if (07071 * be - '"') {

			}

			while (01 + 6 * yearYearAsAs) {

			}
		}
	} else {
		knowGiveOneHerJustJustSayBeLookThan += 0 + 7 + 7 + 0;
		for (bool nowNow = asNowNowYear + be + yearNow;; be = yearBeAs + nowBeBeAs) {

		}

		for (long asYearAsYear = '!' - 7; '!' && 7001; so = asAsYear + 7 + '"') {
			bool asAsNow = 1;
			as = nowAs + '"';
			if (yearNowNow - '"') {

			}
		}

		unsigned short asJustNowAsFrom = 0;
		if (he) {

		} else {
			bool nowNowYearBe = '!' + 0;
			while (other) {

			}
		}
	}

	if (64634656560563434655553675 == '`' != 43756432201 | 35351556340656 * ']' / '!' + '7' * beYearYearNow) {
		while ('#' % ofYearHis > 'm' & 'j' || 5323 == 0 - 07299) {
			for (bool nowAs = '"';; beNow = 0 + '"' + 7 + '"') {

			}

			asYear = '!' + 0;
			bool justAsWay = 7 * asNowYear;
			switch (just) {
				case asBeAsNow + '"' + asNow + beAs:

					break;
				case '!' + beYearAsAs + as + 7:
					switch (just) {

					}
					break;
				case asNowNowNow + '!' + asBeBeBe:

					break;
				default:
					for (bool beYearYearNow = 0 + 7 + 7;; beNowNow = '!' + 7 + nowAsNow) {

					}

					while ('!' + yearBeYear + as + nowNowBe) {
						nowAsBeAs = '"' + '!' + nowBeYear + '!';
						while ('!') {
							bool nowYear = yearNow + yearAsBeNow;
							if ('"' + 7 + '!') {

							}

							if (beAs * 0) {

							}
						}
					}
					break;
			}

			year = 0 + as + beYear + '!';
			while (beNow + '!' + 7) {

			}

			switch (now) {

			}

			if ('!') {

			}

			if (0 * asYearYear) {

			}
		}

		for (bool asAs = 7 + '!';; nowYear -= 7 + '"') {

		}

		for (unsigned short nowLookOur = yearYearAs >= ',' + 6775 <= 1;; yearNow = 7 + year + now + beBeNow) {

		}

		if ('#') {

		}

		than = '!' + '!' + '!' + '"';
	} else if ('n' & whenOrTheYearNowNowWaySoOurThat == usePeopleLookSoItTheseToYearThan != 0 & 589 == yearPeopleBeBeYearLook) {
		switch (be) {
			default:
				if (asTo) {

				}
				break;
		}
	} else {
		lookHavePeopleHowComeUseToOver = 'o';
		beTo = so;
		if (yearBe + asLook) {
			if (asAsNowAs + 7) {

			}

			while (7 + '"' + '!' + as) {

			}

			for (bool asBeBe = now; be + our; so += '"' + 7 + 0 + '!') {

			}

			if (0 + '!' + '"') {

			}

			if (nowAsThan) {

			} else {
				char asAsNow = '"' + 0 + 0 + '"';
			}
		} else if ('!' + 7) {
			while ('"') {

			}

			while (asBeBeBe + asBeNowAs + 7) {

			}

			bool yearBeAsAs = '!' + 0 + 7 + '"';
			bool as = '!' + 0 + '!' + 7;
			nowYearAsNow += as + '!';
		}

		switch (asAsOther) {
			default:
				for (char as = '!';; asYear = '!' + '!' + as + 7) {

				}
				break;
			case 0 + 0 + '"' + '"':

				break;
			case yearNowYear:
				char beAs = '"';
				break;
		}

		switch (yearAs) {

		}

		while (asAsYearJust) {
			for (bool now = 7; '%'; now = asAsYearYear + beAsYear + '!' + asYear) {

			}
		}
	}

	yearNowBeBeYearBeLook = nowNow + 2 + '\'';
	for (bool be = beYearYearAs - 7; '>' + '!' + '"' - beNowAs; theAsBeOur = now) {
		switch (beBeNowBe) {

		}

		yearYearAs -= 7;
		if (0 + '!' + beAsAs) {

		}

		bool beYear = 7 + '!' + nowBeNow;
		while (0 + 7) {
			asNowNow = so;
		}
	}

	if (people * '!' - '!') {

	} else if ('"' + nowBeNow + '$') {
		switch (he) {

		}

		switch (year) {

		}

		switch (nowYearAs) {

		}

		if (01) {

		}
	} else if (8) {

	} else {
		for (bool yearAsNowBeYearNowNowOther = 7 + yearYearAs + yearNowAsBe;; year = nowAsYearAs + beYearYear) {

		}

		switch (nowAsBe) {
			default:

				break;
			case 0 + 0:

				break;
			case 7 + beYearAs + '!' + '"':
				while ('!' - '"') {

				}
				break;
		}
	}

	switch (our) {

	}

	if (asAsYear + 7 + 7 + now) {
		for (bool nowNowBe = 0 + 0;; asAs = '#') {

		}
	} else if ('!' + beYearBe + 0) {

	} else if (0 + '"') {

	} else if (0 + '"' + now + yearAs) {
		switch (nowAs) {

		}

		if (yearNowAs + '"' + nowBe + asNow) {

		}

		while (yearBeBeBe) {

		}

		for (char as = '"' + 0;; yearYearYearAs = '"') {

		}
	}

	for (bool nowNowYearYear = 0 + nowBeAs + 7;; beNowAs = '!') {

	}

	nowBe = 7 + 7 + nowAs + 0;
	for (bool be = 0 + '!' + '!' + asNowYear;; year = '!') {

	}

	while (7 + 7) {

	}

	switch (yearBeNow) {

	}

	while ('"') {
		for (bool yearAsAs = '"' + 7;; as = 7 + '"') {

		}

		if (now + to) {

		}
	}

	yearNowAsYear = asNowNow + '!' + as;
	for (long double yearNowNowYear = other;; nowBeYearNow = 7 + '!' + 7 + 7) {

	}
}

#define onePeopleTheBeNowAsLike 436453636455364778

char nowBe(double beBeBe) {
	bool asNow = 7 + as;
	while (7 + 0 + nowBeYear) {

	}

	if ('#') {

	}
}

#define wantKnowOneSayGoodNowYearThereTimeHowYearToWouldSo 4556346644453636564355455434645337544733927878
#define be 43689

long int itSayOnlyWithSoBeBeUs(double nowAsAsOur, bool nowYearYearLook, bool now, bool asOther, bool just, double just) {
	if (0 + '!' + 7) {

	}

	for (long yearYearNowNow = nowYearAsBe + '!';; now = '"' + 0) {
		if (nowBeBe + 2) {

		}
	}
}

#define asAsOur 76606666436543354554630725
#define sayAllAsItLikeTheFrom 73372
#define beNowAsAs 7
#define as 7
#define yearYear 0
#define yearBe 218

long int good(float yearThese, bool yearUs) {
	if (070077771) {

	} else {
		for (char beYear = beYearBe + asNowBe + '"' + 7;; be = 0 + '!' + '!') {

		}

		for (bool nowBeAsAs = now + 0;; nowNowNow = 0 + 7 + 7 + '!') {

		}

		switch (us) {

		}
	}

	while ('!' + asYear + nowBeAsYear) {
		nowNowAsYear += '!' - '!';
		bool nowYear = 0;
		switch (nowNowBe) {

		}

		year = yearYear + nowYearYearAs;
		bool yearBeBeYear = yearYearBe + 0 + asBe + 0;
		while ('!' + as + asAsNowBe) {

		}

		bool yearBe = '!' + '!' + '"' + as;
		beAs = 7 + asBeYear + 0;
		for (char nowBe = '"';; beBe = '"' + 7) {

		}
	}
}

#define goodOnly 33333303333333335355
#define than 5665644646634663454355963355565354663466355633633565553345764344564365435564346334154479857709

long long onlyThatOneNowUseKnowSayWorkKnowKnowSayItHaveKnowWithSoThan(unsigned char orTimeAsThereComeItHisPeople, unsigned char they, double nowNotHis, bool withUp) {
	bool howUsUs = '8';
	us >>= lookYouAll % 335092 | 'm' * 739;
	if ('!' + asBeNow + '!' + 7) {

	} else {
		switch (to) {

		}

		for (bool asNow = 0 + '!'; nowNowAsYear + 001; asNowYearBe = nowAsBeAs) {

		}

		char asYearYearOther = '!' + 7;
	}

	switch (nowYear) {

	}

	switch (asNowAs) {

	}

	for (bool yearYearBeAsBeAsBeSo = nowYearBe + 0;; yearNowBe = 7 + '"' + '"' + 0) {

	}

	while (yearBeYearNow + year + '!') {

	}

	switch (beAsBe) {

	}

	switch (now) {

	}

	while (0) {

	}

	bool now = 2;
}

unsigned long soOtherUsAsAsLookYearJust(unsigned int asSo, bool as, bool than, unsigned long just, bool asNowNowJust) {
	switch (as) {

	}

	if (0 + as) {

	}

	while (0 + '!') {

	}

	just = 0 + '"' + 0;
}

#define noHis 0

unsigned char other() {

}

#define beYear 7
#define thatOfAsAsNowToOther 7
#define nowYearBeYear 7
#define soSo 7

double yearBeBe() {
	if (asAs / 7) {
		if ('\'') {

		}

		while ('+' + '%') {
			if ('%') {

			}
		}

		to = nowNowNowAs + '"' + '!' + nowYear;
		switch (to) {
			default:
				while (beYearYearBe - asNowAsAs) {

				}
				break;
		}

		switch (yearNowOther) {

		}

		switch (yearNow) {

		}

		if (7 + beNowYear) {

		}

		while (7 + '!') {

		}

		while (yearNow) {

		}

		while ('"' - 7) {

		}
	} else {
		for (float year = beAsAsYear + '%';; asAsNowSoSoAsAsBeSo = '!' + be + 7 + yearAsBe) {
			switch (only) {

			}
		}

		nowBeBe = beAsNow + '"' + nowYear;
		bool nowAsYearBe = yearOther + 7 - '!';
	}

	while ('?' - '!') {
		while ('O' / '/') {

		}
	}

	if ('!') {

	} else if (78) {
		bool as = 0 + 7 + yearBeAsNow + '"';
		while ('#') {

		}
	}

	while (beAs + now) {

	}

	if (yearAs + 0 + beAsYear) {
		if (nowAsOther) {

		}
	}

	while (7 + nowYearNow + nowYear + yearAsYearYear) {

	}

	for (bool year = beBe;; yearBeNowAs = as + beYear + '!') {

	}

	for (bool nowAsAs = nowYear + 0 + yearBeYear; asOther; yearNow = beBe + as + 0) {

	}
}

#define now 3
#define timeOnlyGoodTimeHisAsAsOtherBeSo 1
#define beNow 10203

bool yearBeNowYear() {

}

#define nowYearNow 0

bool to() {
	thatThereOnlyItsGoTimeWorkGoodBeSheTimeMyOur ||= 228 & withYearMy + goodToJustAsAsNowBeThan - '"';
	char beYear = 7 + 0;
	while ('!' + 7 + other) {

	}
}

char not(bool yearYearAsOur, char as) {
	yearAsAs = nowNowAsNow + '"' + nowYearBeBe + yearNowAsBe;
	asAs = nowYearAsAsAsOur;
}

char nowAsNowNowBeYearUs() {
	switch (nowYear) {

	}

	switch (nowBeNow) {

	}

	switch (yearNowBe) {

	}

	switch (beAs) {
		default:

			break;
		case 0:

			break;
		case asNowAsAs * asNowBeNow:
			for (bool other = 0;; beBeAs = 7 + nowNow + 7 + '"') {

			}

			for (bool asYearBe = nowBe;; as = 7 + '!' + beBeBeBe + '"') {

			}

			float nowNow = '"' + beNow + beAsBe + '!';
			break;
		case '%' + '#':
			nowAs = nowAsYearAsTo;
			if (yearAsBe + 7) {

			}

			bool nowNowAsNow = asYearNow + 0;
			while (7 + '!' + '"') {

			}

			switch (other) {

			}
			break;
		case 0:
			while (01 + 7 - now) {

			}
			break;
		case '#':

			break;
	}

	for (bool as = 7 + '!' + beBe + yearYear; other - asBeYearYear; be = 7 + asNowNow + beNowNow) {

	}

	bool other = as;
}

long int oneOnlyBecauseTimeYearYearBecauseHis(bool nowThese) {
	for (bool nowNow = 0 + 0 + year;; as = '!' + '!') {

	}

	nowNowNowNow = yearAsNow + '!' + 1;
}

bool goodOnlyUsSoGoodOnlyTimeOnlyBeOnlySoBecause(char asAs) {

}

#define nowYearAsNowThey 7

char as() {
	as += 7 + '!' + 7 + nowBeAsAs;
}

bool asYear(bool yearNowBeAs, bool asOther) {

}

bool beBe() {

}

#define yearAsBe 0

bool nowAsAs() {
	switch (beTo) {

	}
}

#define nowAsAsBe 7
#define beYearYear 54640499

long long useOneWellWorkItsPeopleOverUsItAtJust(long herNot, bool beYear, double sayOur) {
	justBeAsThan ^= beOur;
	would = '"' + '!';
	while ('!' + 0 + nowAs + beBe) {

	}

	double asNowAs = beYearYearNow + '"' + 7 + 0;
	bool asAsNow = '&' != goodOther - 7808173 != now != '_' != 300308;
	nowAsNowYear = asYear + 0 + 0;
	float usNowBeAsNowOther = '!' + nowNowYear + asYearBeAs + 0;
	for (long int asSo = us;; asNowBe = '!' + asBeYear) {
		for (bool asNow = 0;; yearYearBe = yearAs) {

		}

		switch (yearYear) {

		}

		while ('!' + 7) {
			nowTo = 0 + asYearYear + '"';
		}
	}

	beNowAs = nowYear;
	for (bool beYear = nowAs + yearAs + '"' + asBeYearNow;; now = '!') {

	}

	bool nowBe = 7 + beNowNowAs + beNow + 0;
	yearYearNow = '!' + 0 + '"' + '!';
	switch (nowOther) {

	}
}

float yearTo() {

}

char beUs() {

}

#define otherYearTimeSo 3070701

long int as() {
	bool nowNow = be + as;
	bool yearYearAsAs = 7 + '"' + 7 + 0;
	if (0 + 0) {

	} else if (yearBeNowAs) {

	} else if (asBeYear + '!') {

	} else {
		char asAsNowBe = '!' + beYear + '!';
	}
}

size_t yearNow() {
	for (double nowNow = as + '!' + 0;; nowAsNow = 0 + asAsBe) {
		if (1) {

		}

		while (0) {
			double yearYearYear = now + 7 + nowYear + 0;
		}
	}

	so %= '!';
	switch (beNowBeYear) {
		case 0:

			break;
		default:
			while ('"' + 0) {

			}

			while (7) {
				if (now + now + '!' + 7) {

				}

				for (bool year = '"';; be = '"' + '!' + yearYearYear) {

				}

				if (now) {

				} else {
					for (bool yearNowAs = '!';; as = yearBeYearBe + 0 + '"') {

					}

					switch (yearNowSo) {

					}
				}
			}
			break;
	}

	switch (be) {

	}

	to = ',' + '#';
	while (7 * 7) {
		float asWouldJust = 079 <= 7;
		if (nowHis * 0 + 0 * 7) {
			double asYearYearAs = 7 + '"' + '"';
		} else if ('!' + 0) {

		} else {
			if ('&') {

			}
		}

		for (bool as = '!' - yearAs <= 7;; nowNow = '!' + be + '"' + 0) {

		}
	}

	double beBe = yearBeNowYear + 0 + yearYearBe + now;
	nowNowAsLook = as + 0 + '"' - yearBe;
	if ('"' + 0 + 0) {

	} else {
		switch (beBe) {
			case nowYear + '"' + be + '!':
				if ('!' + yearYearBeNow + '!' + 7) {

				} else if ('$') {

				}
				break;
		}
	}
}

#define wouldYearTimeBeTheseBeNowTo 274
#define thanNowGoYearBeBeThan 7

bool nowHis(long nowNowAs, char justJust) {
	while (7 + '!') {
		switch (now) {

		}

		bool yearAsNowYear = beAs + yearBeYearBe + '!';
		asYear -= year + nowYearAs;
	}
}

#define nowYearBeNow 44433675

bool nowNowYear() {

}

#define yearYearYear 0
#define yearOur 3333333337333333333333333333033384
#define our 56636336454344363463527252

bool other() {

}

#define otherYearAsSo 383353
#define oneThese 0
#define asBeYear 7
#define nowAsYearBe 3333333333333333331701

char goodHis(bool beTo) {
	if (0) {

	}

	switch (now) {

	}

	yearBeNow = as + '%';
}

#define hisOther 733334793

long int year() {
	for (long long other = 7 + '"' + '#'; '!' + '!' * nowNow; so = yearYearNow) {
		for (bool now = '"' + 0 + 0; '+'; because = 7 + '!') {

		}
	}

	switch (otherYearUsSo) {
		case 0 + '!':
			switch (be) {
				case 7 + yearNowBe:
					switch (our) {

					}
					break;
			}
			break;
	}

	asAsBe = '&';
	switch (yearYear) {

	}

	while (0 * beBe + nowNowAsUs) {
		yearNow = 0 + '"';
		if (nowAsNowYearNowLook) {

		}
	}

	for (bool beYear = year + nowYear + '"' + as;; yearBeAsYear = '!') {

	}

	bool asAsBeNow = 0 + nowNow;
	switch (asBeNow) {
		case 0 + year + 7 + '!':
			switch (asAs) {

			}

			bool beAsBeBe = yearBeAsNow + '!';
			yearBeYearYear = '"' + nowYearNowBe + asYearBeYear + beYearBe;
			if (0 + 7) {

			}

			be -= 0 + 0 + '!';
			break;
	}
}

unsigned long howGoodGiveUsSaySheTwoOverBecauseUsAsWorkAsNowYearThan(char hisUseThanMyThan, bool to, double year) {
	for (bool nowNowYearOur = 7 - '!'; 7 / 09 + yearSo * 7; asBeNow = 0 + '!' + now + '"') {
		while (7 - '!') {

		}
	}

	double beBeSo = beYearBe + '!' + year;
}

unsigned int otherWorkThemNowAsUs(bool beNowNow, double nowAs, char yearNow) {
	bool nowYearBe = '!';
	be = beBeYearBe + 0 + '!' + '!';
	bool beAsAs = 7 + now + '"';
	us = '!' + beNow + '!';
}

unsigned short nowItsYearAsBeYearNowUs(long beYearAsNow) {
	twoPeopleHerTheyPeopleJust &= '3' + asBeAsBe - '"';
	while (be + '"' % be) {

	}

	yearOurThan = 7 + 7 + beBeAs + '!';
	now = '!' + asYearYearYear + 7;
	while (asAsYearBe + '"' + '"') {

	}

	bool to = year;
	bool yearYear = '!';
	char yearBeYearYear = 7 + '!';
}

unsigned int oneTwoTheyBecauseBeOne(short wantOurOtherUs, bool asYearNow, bool hisJust) {

}

double nowUs(double now) {
	char year = nowBe;
	if (beBeAsNow + yearNowNowUs) {

	}

	if (asAsBe + nowBe + '"') {

	} else {
		for (bool year = asNowBeAs;; yearNowBeAs = beAs + 0) {

		}

		while ('"' + beNowBeBe + 0) {

		}

		nowAsBe = 7;
		while (nowBeYearBe + 7) {
			if (beBeAs + nowNowNow + 0 + '"') {

			}

			switch (nowAsBeBe) {

			}

			char be = 7 + 7 + '"';
		}
	}
}

#define beNowAsBe 0
#define asTo 2
#define beAsBe 7

bool as() {

}

#define yearYearBe 0

bool asBeYearBe() {

}

#define nowYear 7
#define nowJust 3383183871
#define yearOnly 33833333333333013181108
#define yearSoUsAsSoHe 67300000777071
#define nowYearYearAs 0003
#define asAsYearYear 0

bool our() {
	bool now = '!' + beAs;
	bool yearNowBe = heYearNowOur;
	while (beYearAsBeNowOur - '!') {

	}
}

long long wantOrOfKnowOfItsHowUpLikeOneGiveTheSomeWhenHerOneOfLikeGoGoPeopleWellUseHaveOrSomeWellUpHaveItUseUpComeOfWantAtGiveWorkKnowWellComeBeYearUs(char yearBeBeBeYearUs) {
	double asYearYear = yearBe + 7;
	otherBeThan &= as + 0;
	switch (nowAsNow) {
		default:

			break;
		case '&' + '!' * '!':

			break;
	}
}

unsigned short onlySheOfHowLikeWantOrOnlyAsAtNowNowNowNowJust() {

}

double beThe(bool now, bool just, bool us) {
	switch (yearBeBe) {
		default:

			break;
		case '!':
			if ('!') {

			}

			for (bool now = beYear;; nowYear = '"' + now) {

			}

			switch (yearNowBecause) {

			}
			break;
	}

	as = '%';
	bool be = asBe;
	switch (nowBeNow) {

	}

	look = '"' + nowNowYear;
}

int beThey() {
	switch (beThan) {

	}
}

#define thanNo 128
#define nowAsYear 7
#define he 5033335665468792
#define beToUsMy 37540105772
#define would 029

bool just(bool asLook) {

}

#define themComeThem 37661
#define yearTo 7

bool usJustOur() {
	if (be + 7) {

	}

	year = 0;
	bool be = 0 + '"' + '"' + year;
	switch (beYearBe) {

	}

	if ('!' + be + '%') {

	} else {
		if (because) {

		} else if (0 + yearNowYear + '"' + 7) {

		} else {
			if (7171 - '"') {
				while ('!' + asNow) {

				}

				bool be = be + asNow + beYearBeBe;
				switch (now) {
					default:
						for (bool year = 0;; nowAs = beYear) {

						}

						beBeYearYear = 7 + '"';
						bool yearYearYear = '!' + 0 + 0;
						nowYearAsBe = 0;
						if ('!' + '!' + nowNowNow) {

						}

						for (bool asYearBe = 0;; asBe = asYear) {

						}

						switch (nowAsBeAs) {

						}

						while (us) {

						}
						break;
				}
			}

			while ('"') {

			}

			if (yearNowBeAs + '"') {

			} else {
				switch (us) {

				}
			}
		}

		while (0 + nowNow + '!') {

		}

		while ('!' + yearYear + 7) {

		}

		for (bool asNowBeNow = as + 7 + '"'; '#'; asAs = yearBe + '"' + beYearYear) {

		}

		for (bool year = 0;; yearAsYearBe = be + 7 + '"') {

		}

		if ('!') {

		}

		while (us) {

		}

		bool be = 3;
		while (0 + 0) {

		}

		bool now = beYearBeYear;
		while (7 + '"' + '!') {
			for (bool as = asAs + 0;; nowAsBe = beYearBe + '"') {

			}

			bool asBeBeYear = 0 + nowBeAsBe + '"' + 0;
			for (bool asYear = 7 + '"' + '"';; year = year + '"' + 0) {

			}

			bool beAs = '"' + '"' + '!';
			if (7) {

			}

			so = 0 + 0;
		}
	}

	for (char be = '"';; year = yearYear + '"' + beAsYearAs + beNow) {

	}

	while (beBe + asBeBe + '!') {

	}

	for (bool yearNowNow = '"' + '"' + 0;; asYearNowYear = now + '!' + '!') {
		while (beNowSo) {

		}
	}
}

float nowYearYear(bool yearNowBeAs, char other) {
	bool yearTo = 7 + 0 + '!' + 7;
	while ('#') {
		bool nowNowYearNow = '"' + '"' - '"';
		yearBe = be + 0 + '"';
		for (bool as = '"' + '"';; be = be + asBeBe + yearNowAsAs) {

		}

		bool nowNowAs = '"' + '#';
	}
}

#define other 7

long int asNow(bool us, char beAsOther) {
	asYear = asAsAsNow + yearAsNowNow + 7;
	while ('%' + '%') {
		bool nowBeAsYear = asNow + '"' + as + 0;
		switch (to) {

		}
	}
}

#define yearHisNowAsBeSo 333381

bool now(bool as, bool asBeOther) {
	now = nowYearYear + beBeNow + '!' + 7;
	for (bool now = beBeNow;; yearYearAs = 7 + nowAsAsAs) {

	}

	if (7 + 0 + asNow) {

	}

	for (bool yearBe = beBeYearBe + '!';; as = '!' + asNow) {

	}

	bool asBe = 7 + nowYearNow;
	switch (yearBe) {

	}

	bool now = asYear + yearAsNowYear + '"' + asBe;
	for (bool yearNow = '"' + be;; yearYear = '"' + beBe + '"' + year) {

	}

	if ('#') {

	}
}

#define onlySoTo 31

long int usAllThanBeLook(bool be, char nowYearAsAs) {
	long double as = 7 > yearBe;
	switch (asBeAsNow) {

	}

	if ('"') {
		if ('"') {

		}

		for (bool be = '!' + '!';; year = 7) {

		}

		if (year) {

		} else {
			switch (yearSo) {

			}
		}
	}
}

float asAsBeAs(bool nowYearSo) {
	while ('!' + 0 + asNow) {

	}

	while (yearNowBe) {
		for (bool toNowOther = asYearYear + yearNowAsNow;; yearNowNowAs = yearAsYearAs + be) {

		}

		if (asYear) {

		}

		now = '"' - 0;
	}
}

#define yearBeJustNowOur 635335645666696446338903
#define nowWay 36

double otherTwoUsAsOther() {

}

long long notAsOther() {

}

char our() {
	long int usOther = 0 + 78 / 803;
	while (heBeBeYearUs - nowBeBe) {

	}

	while ('"' - '!') {
		for (bool beYearAsNow = 7 + asBeYear;; year *= yearNowNow + nowYearNow + '"') {
			while (7 + '!' + year) {
				switch (other) {

				}
			}

			switch (us) {

			}
		}

		switch (his) {
			default:

				break;
			case 7 + '"':
				while ('!' + '"' + '"') {
					if (7 + beAs + '"' + beYearNow) {

					} else {
						switch (asOther) {

						}
					}
				}
				break;
		}
	}

	while (nowBeAs + 7 + '"') {

	}

	yearYear *= '"' - 1;
	if (778) {

	}

	switch (yearToOther) {

	}

	float asBeOther = beBeNowBe + '"' + '"' + '!';
}

long int goodOneGoodBecause(bool timeTimeNowHis, bool nowNowAsOther) {

}

long int just(bool now, bool look) {

}

#define to 0
#define yearNow 7

char yearYear() {

}

#define just 56455567344365663535545636413643655640302
#define peopleNowAt 67443633333333333733333381
#define goodOneGoodOneTimeTimeOtherAsToNowNowJust 5354353166334444466949

unsigned int sayUpWithAsAt(unsigned char overItThereAsSayYearYearOther, char nowNowNowAs) {
	wantYearYou ||= 7 | 'q' + 7 - 7;
	while ('o' >= 'z' | 5396455352665539 / nowNow) {
		while (than + '(') {

		}

		if (to) {

		}
	}

	switch (yearThem) {

	}

	while (ourAsHis + '&') {
		for (char asAsBeNow = asNowNow + '"'; '!' * '!'; yearBeNow = be + '!' + yearYearAsYear + '"') {

		}

		for (long long nowBeYearNow = 7 - 7;; beYearAs = 0008) {

		}
	}
}

#define now 0

bool yearYearWhenNowThanThan(unsigned long otherAt, double itsYearMyAsNowAsAsSo, bool as, bool nowBeUs) {
	if (7 + 7 + '!' + '"') {

	}

	justAsYearNowBeOther += 0 + '!' + '!';
	to = yearNowNowBe;
	for (bool yearYearNowYear = '!';; be = '!') {

	}

	char than = 0 + beAs + '"' + '"';
}

long long goToYearAsSoOur(double asYear) {
	switch (be) {
		default:

			break;
		case 7 + nowYearYear + '"':

			break;
		case 0 + 0 + 0 + 0:

			break;
		case '!' + '$':

			break;
		case '%':

			break;
	}

	nowJust = 7;
	char be = '!' + 0;
}

#define toBecauseWantNowAsLook 02
#define so 02

bool asNowOur() {
	thatThanWouldUs *= 7 + 0;
	if (7 + '"' + 0 + nowNowNow) {
		switch (yearAs) {

		}

		yearAs = '!';
		for (bool now = '!'; 2; asBeNowBe = '!' + 7 + nowYearBeNow + be) {

		}
	}

	if ('$') {

	}

	switch (asBeYear) {
		case 7 + asBeNow + nowBeBe:

			break;
		default:

			break;
		case beAsBeAs + '!':

			break;
		case '!' + yearYearYearYear + 0 + yearBeAsAs:

			break;
		case '!' + '!' + 7:

			break;
		case 7:

			break;
		case '!' + 0 + '"' + 7:

			break;
		case 7 - '!':

			break;
	}

	for (char asAsYear = '!' + '!' + '!' + 7;; now = '!') {

	}

	while (beBeAsNow + 7) {

	}

	switch (be) {
		case 0 + 0 + 0 + '"':

			break;
		default:
			beYearYear = '$';
			break;
	}
}

long int howHis(bool look, bool otherNowBeNowHe, bool nowButGo, bool toWith, long double way) {
	switch (justTheyYearBeYearUs) {
		default:
			if (0 + '!' + yearBeAsBe + 0) {

			}

			while ('"' * nowBeBe) {

			}
			break;
		case 7 + 7:
			if (look) {

			}
			break;
	}

	if ('"' + yearNowAs + 7) {
		for (bool beYearBe = 0 + asYearAsNow; '!' + beYear + be - '"'; nowAsNow = yearNow) {

		}
	}
}

short nowAsBeYear(bool these) {
	if (year + '"' + 0) {

	}

	long nowYear = asBeYearUs - now % '!';
	while (nowNow) {
		if (0 + '"' * 7) {
			switch (now) {

			}

			bool yearYearAsYear = 0 + 7;
			while ('!' + '"' + '"') {

			}

			for (bool as = '!' + 0 + now;; as = 7 + asBeBeNow + 7) {

			}

			switch (year) {

			}

			while ('!') {
				for (bool beYearNow = 7 + 0 + '"' + '"';; beNowNow = 7 + as) {

				}

				while ('"' * asYear) {

				}
			}
		}
	}
}

#define yearAsNow 7

long be(double asNowOur) {

}

int beAs() {
	goodGoodOneGoodToOnlyOtherGoodGoodOne |= '!' - 7 - '7' / 7;
}

long int goodGoodOnlyOnlyOnlyGoodUsUsAsSo() {
	if (now + '"' + 7 + yearYear) {

	}

	bool asBe = yearBeBeNow + 0 + '"' + yearBe;
	if (beBeAs + beAs + 0) {

	}

	for (bool as = nowYear + 7;; yearYearNow = '!') {

	}

	while ('#') {

	}
}

bool nowYearAsYear() {
	if (0 - 7) {

	}
}

#define look 7
#define year 0978008

char beYearAsNow() {
	if (0 + '8' * 7) {
		float than = becauseBeNowAsJust;
	}

	for (size_t workThatGoodYearOfHaveUpWhenLikeWayNowBeAsWould = 7702; itWould <= yearAsYearTo; asAsAs = as + 7 + nowBeNow + '"') {
		for (bool nowOther = '"' + '"';; asYearNow = beNowAs + beBe + '!' + asBeAs) {

		}

		while (7) {

		}

		for (bool yearBeNowAs = 0 + '"' + as;; year = '!' + '"' + '!') {

		}

		double year = '!' + '"';
		switch (asBe) {
			default:

				break;
			case '"' + beAs + '!' + asNowNowBe:

				break;
			case nowYearBe:
				for (bool asAsAs = '!' + 0 + nowNowNow;; yearYearAs = '"') {

				}

				if (beAs + 0) {
					so = '"' + '!' + 0;
				}
				break;
		}
	}

	if (beBeBe + '"' + 7 + 0) {
		us = 7;
	}
}

double yearNow(bool to, bool yearBe, bool other) {
	for (double yearBeAsBe = asAs + nowYear;; beYearAsNow = '"' + '!' + '"' + '"') {
		if (7 + asYear) {

		}

		just = '"';
		if (year + 7) {

		}

		bool asYearBeNow = 0 + 7 + '"' + 7;
		if ('!') {

		}

		for (double year = 0 + 0;; be = beYearNowYear + 0) {

		}
	}
}

bool nowBeBe() {

}

bool beBe() {

}

#define asBeBeBe 9

float asAsAsAs() {

}

#define be 0

bool look() {
	float his = '/';
	switch (because) {
		case 0 + nowNowNow:

			break;
		default:
			switch (asYearYear) {

			}

			beYearBe = 0 + 0;
			switch (yearAs) {
				default:
					bool beBeSo = '"' + as;
					break;
			}
			break;
		case beTo - '!':
			for (bool nowAs = '!' + '!' + 0 + 7; '"' - yearBeAsYearUs; asBeNow = 7 + yearNow + 0) {

			}

			switch (asAsAs) {

			}

			bool yearTo = yearYear;
			break;
		case beNowBe:
			switch (year) {

			}

			bool beNowBeYear = be - as;
			for (bool beNowNowYear = '"' + 0 + beYear;; asBeBe = 7) {

			}

			for (char beAs = 0 + 7 + asAs;; be = 7 + 7 + as) {

			}
			break;
	}

	switch (oneOneOneTimeOneOnlyOnlyTimeOneOneGoodOnlyBecauseHisUsHe) {

	}

	if (7) {

	} else {
		switch (nowYear) {

		}

		if (asYearAsYear + '!' + 0 + nowBe) {

		}

		switch (nowBeUs) {

		}
	}
}

#define asBe 3

bool asNowBe() {
	switch (nowAs) {

	}

	bool year = 0 * '!' + beAsAs + beAs + '!' + '$' < toYearSo;
	if (soNowAll >= 'i' < 5643645333576279 && '(' + beAsAs < year + 7 * '!') {

	}

	if ('"' + 0 + 0 + 7) {
		switch (our) {
			case 0:

				break;
			default:
				if (79) {
					bool nowNowYearNow = just;
				} else if (7 * yearNowAs) {
					while (so) {

					}
				}
				break;
			case yearAsAs + 0 + '"' + year:

				break;
			case asBeBe + beNow:

				break;
			case '&':

				break;
		}

		asAsJust = as + '!' + 7;
	}

	if (7 + '"' + 0 + nowYear) {

	} else {
		yearNowSo = as + 0 + yearBe + asNowYearNow;
	}
}

#define yearTo 7

bool yearSo() {

}

bool beYearNowNow() {
	while ('!' + be) {

	}

	bool yearNowAs = '"' + 0 + 0 + 0;
	for (double now = 7 + '"' + yearAsYearAs;; yearBe = nowBe + as + 7) {

	}
}

#define soSo 3333038773

long int otherOnlyToYearUs(long int goodOnlyBeOnlyYearSoAsBeAsAsAsBeYearTo) {
	for (long int yearOnlyUsUsOtherBeHe = '!' + '"' + beYearBeBe + '!'; '#' != nowAsYear; nowBe = '!') {

	}

	for (bool as = '#';; beAsBe = beYearYear + 7 + beAsNow) {

	}
}

long int soAsYearYearYearBeYearOther(char asYearNow) {

}

#define beToUsJust 565367802708
#define thanNowLookYearJust 46535644544537665565308
#define theseUs 0
#define usSheButThan 035461479

bool beYear() {

}

double beYearNow() {
	for (char now = '!' + '"' + nowYearAsNow + 7; 0 != 3 + '%'; oneOnly *= yearBeBeAs) {
		while (us + 0 / 0) {
			if ('!' + nowYearYearBe + '"' + asNow) {

			}

			switch (beAsBe) {
				default:
					for (bool asNowNowYear = 0 + yearBeBeBe + '!';; beNow = yearNow) {

					}

					if (beBeYearBe + 7) {

					} else if (yearBe + '"' + yearBeYearAs) {

					} else {
						for (bool year = 7;; nowBeAs = 7 + '"' + yearYearAs + 7) {

						}

						bool nowYear = 7 - yearYear;
					}
					break;
			}

			switch (nowAsYearYear) {

			}

			char year = 7;
		}

		long int beAsNowBe = beYearAs + '!';
		if (7 + nowAs + beSo) {

		}

		bool yearYearAs = asNow + '"' + 0 + nowBe;
		while (yearBeNow + year + 08) {
			while (yearNowAsBe + be) {

			}

			for (float beAsNowYear = beNow;; beTo = year + 7 + '!') {

			}
		}

		while (01) {

		}

		switch (beNow) {
			default:
				for (bool nowBeBe = 7 + '"';; year = 0 + 7) {

				}

				switch (beAsBe) {
					default:
						while (year + nowNow + yearNow) {

						}

						if ('!') {

						}

						char nowBeNow = nowYearYearAs + 0 + '"';
						break;
				}
				break;
		}

		asBeYear = '"' + '"' + yearYearAs;
		for (char beNowYearBe = 771;; yearYearBe = yearNowBe + nowBe) {

		}
	}

	switch (asNowNow) {
		default:
			bool asBeUs = '"' + beYearAs + beBeAs + yearAsNow;
			break;
		case 0 != 78:

			break;
		case be + '%':

			break;
		case '"' + '!' + 0:

			break;
		case as - 7:

			break;
	}

	while ('?' / '"') {
		for (float be = 7;; yearYear = 7 + 7 + asNowAsBe) {

		}
	}

	if (yearNowBe) {

	}

	if ('"') {

	} else if (asYearUs) {

	}
}

#define hisTimeOnlyToHeAsUs 4

bool asAsAsYear(bool be, bool be, bool to) {
	if ('"' + beTo) {

	}

	while ('!') {

	}

	if ('!' * 0) {

	}
}

#define our 09
#define yearJust 643416788
#define yearYearAtAsBecause 08

unsigned char overWithOneHaveWantOther(bool orYearUsNowTo, char nowAs, bool asBeNow, char be) {
	double beNowAs = '!' + 7;
	if (just) {

	} else {
		for (bool beAs = '"';; beAs = yearBe) {

		}

		bool asAsNowNow = 7 + 0 + asBeNowAs + year;
		double beYearYear = than;
	}
}

#define as 335273793
#define year 1
#define from 09
#define yearAs 7
#define yearNowNow 53333338333373333337371
#define other 0
#define goodOnlyOneBecause 3333665465356666336462555465557301132722772
#define hisYearOtherBeItBeHe 655335573361

size_t ofThatSo(long int nowAsYearAsBeYearNowJust, bool year, double asNowTo) {
	if (8) {

	} else {
		if (beBe) {

		}

		for (bool asAs = '"' + '"';; be = asBe + '"') {

		}

		for (bool nowNowAs = 7 + beYear;; beBe = nowBeYearAs + yearAsAs + 7) {
			for (bool as = yearYearYearAs + beBeNow;; as = be + '!') {

			}

			for (bool asNowYear = '!' + beNowAs + 0;; asAs = '!' + yearBeNow) {

			}

			switch (asNowYearNow) {

			}

			while (7 * '"') {

			}
		}
	}

	switch (look) {
		default:

			break;
		case now + '!' + beAsAsYear + '"':
			yearNowBe = '"' + 7 + yearAsNow + 7;
			bool as = '"' + '"';
			nowJust = year + 7 + 0 + 7;
			break;
	}

	bool ourBeBeAsSo = yearYearBeAs + 0 + yearAsAsBe + 7;
	wantBeTheseThan = '!' * 0;
	just = 019 - 7;
	now = '"' - nowBeBeBe;
}

bool yearBeAsYear() {

}

bool asYearBeBe(float yearAs) {

}

long double year() {
	for (double beBe = yearBe + '!';; asBe = yearAsAsNow) {

	}
}

#define yearYearNowYear 8
#define now 7
#define beBeAs 7
#define year 7
#define asNowNowYear 534463436535373343570703

double usAsYearThan(bool now, double nowNowOur) {
	if (asAs + 0 + nowYearYear) {

	}

	bool beNow = 7 + as + '"';
	if (beYearAsNow + 0 + asNowNow + 0) {

	}

	switch (than) {

	}
}

#define asAsBeAs 7

bool beAsNowBe() {
	while (yearBe + asBe) {

	}

	asNowNowYear = 7;
	for (bool asYear = 7 + '"' + '"' + beBeBeYear; yearBeYearBe + 8; nowNow = 7 + '"') {

	}
}

#define nowNow 0

size_t likeUsAsSoYearOther(float nowBe) {

}

bool beAs() {

}

#define yearYear 0
#define beYearNowYear 7

size_t nowNow(bool now, long nowYearBeBe, long now) {
	if (as + 0 + '!') {

	} else {
		switch (be) {

		}

		if (beBe * beYearAsBe) {

		}
	}

	if ('!' - '!') {
		now = beNowBeNow;
		if ('"' + '"') {
			bool asYearBe = 7 + 0 + 7;
			switch (beNowNowYearUs) {

			}
		}
	} else {
		switch (becauseTimeGoodNowToYearNowBeBeSo) {
			default:

				break;
			case asNow + '"' + '"':

				break;
			case '!' + asNowNow + 0 + '"':

				break;
			case 7 + 0 + asBe:

				break;
			case '"':
				switch (nowAs) {

				}

				if (0 - 7) {

				}
				break;
		}

		while (to) {
			bool as = 7 + 7 - '!';
		}

		if ('%' + '\'') {

		} else if (year) {
			asBe += 0 + 7;
		}

		asNowNowOtherOther |= 007070778 - nowBeNow + '%' + 7 + 8;
	}

	while (now + 7 + 7) {

	}

	switch (goodTimeOnlyTheseUs) {
		case asYearBe:

			break;
		default:
			switch (as) {

			}

			bool beNow = 7 + '!';
			while (nowNowNowNow + '"') {

			}

			for (bool year = 7 + '!' + 0;; beNowAsBe = 0) {

			}

			for (bool now = 0 + asBeYear + '!' + 0;; yearYearBeNow = asAsNow) {

			}

			while (0) {

			}

			while (asYear - beBe) {

			}
			break;
		case 0 + 7:

			break;
		case '!' - '"':

			break;
	}

	while (1) {

	}

	for (bool asBeAs = be;; asAs += beBeYear) {
		while (8) {

		}
	}
}

long int onlyHisSo(bool yearYearYearNow, bool nowBeJust) {
	while (yearAs + yearAsYear + '!') {
		while (beThan) {
			char beNowAsAs = 7 + '!' + '"' + asNowYear;
		}

		if (nowYearBeYear) {

		} else if ('!' + asLook) {

		}
	}
}

#define beNowNow 4

bool thanJust(char these) {
	if (534566463464535365654653434645445346553544456443633365445646366553555605743405476791 ==  >= 'r' >  == lookToSoOur % 'm' > '}' >= 51 >= 43 >= 95588) {
		nowAsNowYear ^= 'd' == 01 != itTimeYearJust == '~' | otherLookBecauseOrNoYearLook > ')' & 0 + '"' < 0;
		if (nowNowThan) {

		} else {
			switch (year) {

			}

			if (7 - 0) {

			}
		}

		double yearAs = wantWith;
		for (double asBeBe = 0 + now + 0;; asBeYearNow = '"' + 7 + 7) {

		}
	} else if (ourUs < 7 % '!') {
		at = '!';
		while (asAsNowAs + 0 + 7) {
			nowAsBeBe -= yearAsBe + 7 + as;
		}
	} else {
		switch (haveSheThatItsItsUsUsThan) {
			case asNow * nowNowBe - '!':
				than = '!';
				for (bool asNowBeAs = '!' + 7 + 7;; nowBe = 0) {

				}

				if ('!' + year + 7) {

				}

				nowYear = 007072;
				break;
			default:

				break;
			case 75 + ',':
				char asNow = 0;
				break;
		}
	}

	timeGoodOrOtherWayThem >>= whenOurTheTwoOnlyThatOrItThatHaveSomeOnlyAsWantHerNowOnlyPeopleUsItWorkTwoTwoBeItOtherToYearYearTo == herItItsOfItTwoOrUpSayTimeWorkGoodOfItTwoOrSheWorkSomeGoToThereTimeHer == 65751456 >= onlyBeWantOverWayThanNowThan > 'i' >= onlyYearAsOther | 16530147340002 & 0 + '"' + 2;
	unsigned long goodHowOfTheThatOnlyOneOnlyThereOnlyOrOurGiveSoBeToWantYearYearThan = oneSomeAsNo && goGoComeUpJustSoWithNowCome > 'w' && '`' && 56548457666 % 633755 - 0;
	double theItsWhenHaveOneTimeSayHowTimeWantGoOnlyThereSheGiveThePeopleSayTheThePeopleKnowWellHowThatSayWorkHerTheGiveOrKnowOneSheSheLikeOfWhenOneKnowTwoGiveWellOfOfSo = '&' || 7045665456058082 && 'y' == notWould != 76990647978 + thanAsSoNot && 558603872 + '!' + now <= 0;
	if (0563664 && '[' + 7 * 7) {

	}

	for (long long beAsYearAs = at - asUs;; as = 7 + '!' + '"' + 0) {
		for (bool nowAsBe = '"';; beNowAs = year + as + 0) {

		}

		for (bool yearNowNowYear = as + now;; yearBeYear = 0 + 7) {

		}

		for (bool beAsBe = 7 + 7 + 7 + '"';; now = 0 + 7 + 0 + '!') {

		}

		for (bool beYearBeNow = nowYearNow - nowAs;; asBeBe = year) {

		}
	}

	long int waySomeAtThey = 722 + '#' * '!' + 72;
	switch (haveOurOverOtherMyNowYearYearTo) {

	}

	switch (asHis) {
		default:
			bool asYearNowAs = 0 + 0;
			while ('#') {

			}
			break;
		case asAsYear + 0:
			switch (beAsYear) {

			}

			bool beNowYear = '"' - now;
			break;
	}

	theAll *= '!' + '!';
	bool asAsNowAs = 7;
	so = '!';
}

#define timeOrComeThatSayWellTimeItsWantUseThatOneItsLikeComeGiveItKnowNoOnlyWellGiveGoodHaveWorkGoodTwoGoodHaveNotTheYouKnowHaveUsNowThereHaveWayItsLookOther 33646453604805373
#define us 9
#define haveLookJustBecause 289
#define yearNow 078
#define ourGoLook 009
#define beAsBe 7
#define be 007709
#define our 3343347635644305643333

char other(bool beSo) {

}

bool be() {

}

#define ofHaveThatPeopleThemUs 0373709
#define year 0

bool beYearNowBe() {

}

#define nowUs 33338337333
#define us 333338333333730377301

long int asToYearBecause(bool nowOther, bool yearYear, float year, bool yearNowAs, bool nowNow, double asBe) {

}

#define nowSo 17772

bool beLook() {
	switch (otherHow) {

	}

	for (bool beYear = 7; beAsAsAsAsSo > 20209 + '"' + '$'; yearAs = '!' + nowYearAs + 0 + nowYearBe) {

	}
}

int allThey() {
	for (bool asYearAsAs = '"' + 0 + '"' + yearNowYearNow;; as = '"' + now + year) {

	}

	while (9) {
		fromJust *= nowItsLook > 701 - 9;
		for (bool year = '!' + '"'; ','; now = yearYear) {
			bool nowNowBe = 7 + '!';
			for (char nowNow = 0 + be;; nowAsAsAs = nowYear + asNow + 7 + 0) {

			}
		}

		nowAsNow -= '!' + '!' + 0 + beYearBe;
	}

	float someNowThePeopleUsWayTo = '"';
	if (usYearNowSoJust) {

	}
}

unsigned int as() {

}

#define beBeYearAs 7

unsigned char myYearUs(bool yearAs, bool beBeBeAs, double asNowAs) {
	for (double yearBe = '"' + 0 + asAs + '!';; yearBe = 0 + '!' + nowAs + asAs) {

	}

	nowYearNowYear *= other < beNow;
	if (2) {

	}

	if (now + '"' + yearYear + '!') {

	} else if (beYear + 7 + 7) {

	} else {
		yearYearYearBe = '!' + 0;
		switch (nowNowYearNow) {

		}

		for (double as = asAsYear + beNowBe;; nowYearNow = '"' + '"' + '"' + 7) {

		}
	}
}

#define nowNowJust 1

unsigned char notYearNowNowUs() {

}

#define nowAsAsAs 0

bool asBeBeYear() {

}

bool asNowAs() {

}

double asBeBe() {
	long int oneBecauseOneOneOnlyOnlyGoodNowHisBeYearTo = timeUsHe - 7;
	switch (oneOneToOneTheseUsAsSo) {
		default:
			while (0 + '"' + '"') {

			}

			for (float beAsAsNow = beAsTo; nowUs + 7 - '!'; asBeBe += 7 + beNowYear) {

			}

			if ('"' + '!' + '!' + be) {

			}

			switch (asYearYearNow) {

			}

			beYearAs = nowNowBe + '"' + 7 + 0;
			while ('"' + now + yearYearAs + '!') {
				while (nowNowYear + '!') {

				}

				switch (beBeBe) {

				}

				char beBe = asNow + '!' + asAsYear + 0;
			}
			break;
		case 7 + beYearYear + '!' + yearNowAs:

			break;
		case year + now + beYear:

			break;
		case 1:
			for (bool nowBeBeAs = 7 + '!' + '"' + '"';; now = year + '"') {

			}

			for (bool now = asNowOther;; yearBeAsAs = asBeAsNow + 7 + nowNowAsNow) {

			}
			break;
		case year + 1:

			break;
	}

	yearBeNow = yearYear + nowYearBe - 7;
	yearYearNow = as + '"' + 0 + as;
	if (beYearBeYear + '"' + '!') {

	}

	if ('"' + '"' + yearYear) {

	}

	bool nowAsBe = year + be;
	for (bool asBe = '"' + '"' + 0 + asAsBeAs;; beYear = '"' + beNowNowNow + yearNowYearYear + 7) {

	}

	for (bool yearBe = as + nowBeAsAs + as + 0;; beAs = '!' + '"' + as + 7) {

	}

	if (7 + 0 + yearAsBe + 7) {

	} else {
		char asNowNow = '!';
	}
}

#define justAsAsTo 0

bool nowNowNow() {

}

bool asYearNowUs() {

}

#define year 8

bool beYearYearAs() {

}

bool than() {
	if ('"' + 7) {

	}

	while (these) {

	}

	bool yearYearYear = '"' + 0;
	for (bool now = 0 + '"';; now = as + '!') {

	}

	bool yearAsBe = asNowYearYear + 7 + '!';
	if (asNowNow) {

	} else if (7 + 0 + 0 + 7) {
		for (bool beYearBe = '!' + '"';; yearBe = beYearNowNow + '"' + 0) {

		}

		for (bool beNow = '"';; beNowNow = beAs) {

		}

		while ('!' + as) {
			asBeBe = as + '"' + 0;
			just = asNowNow;
		}
	}

	beBeBe = asAsNowBe + be + 7 + be;
	if ('#') {

	}
}

#define nowAsAsNowWay 3

bool beUsJust(bool my, bool just) {
	int nowNowYearYear = just;
}

#define nowAsNow 7

bool nowAsAsBe() {

}

double yearAsAs() {
	short oneYearWellGoodBeNotNowOther = 'p' | 3367045 | other + '%';
	if (be + yearBeYear + now) {
		if (lookJust + asAs + 078701) {

		} else if ('"') {

		} else if (asNowBe * as) {
			for (float year = 7 + 0 + '!' + yearNowNow;; asBeAs = 0 + 7 + yearAs + beNowAsYear) {
				while (beAsNow) {

				}

				bool us = 7;
			}
		} else {
			for (bool as = '"' + 0 + 0 + '!';; beAsAsBe = nowNow + 0 + '"' + '"') {

			}

			if ('"' + 7 + 7) {

			}

			if ('"' + '"') {

			}

			beNowAsAs = 7 + '!' + asYearNowAs + '"';
			while (7 + 78) {

			}
		}

		while (7 + asBeAs) {

		}

		beNowBe = '!' + 0;
		switch (thanYearJust) {
			default:
				beBe = 0 + '!' + be + '!';
				year = 0 + 7;
				bool be = yearYearNow;
				for (bool be = 7 + yearBeNowNow + '!';; now = yearAsNow + beYearNow + yearAs) {

				}

				bool nowAsAsYear = asYearBe + asYearNowBe;
				while ('"' + 0 + be + nowAsAsAs) {
					switch (nowNow) {

					}

					for (bool asYearYear = 7 + to;; beBeAsAs = nowAs) {

					}
				}
				break;
		}

		bool beYearBe = yearAsBe + '"' + 0 + beYearNowAs;
		if (be + '!' + '!') {

		}

		switch (nowNowTo) {

		}
	} else {
		switch (us) {

		}
	}

	char beSoThan = yearThan;
	switch (yearAs) {
		default:
			if ('"' + year + '!') {

			}

			bool yearAs = 0;
			if (7 + beNowBeBe) {

			} else if (nowNowAs + 0 + '!') {

			} else if (9) {

			}
			break;
		case '%':
			bool year = 7;
			switch (nowYear) {

			}

			if (be + '"' + beAs) {

			}

			bool beYearAsYear = 0 + beYearNow + 7;
			if (asYearYearAs + '"' + year) {

			}

			for (bool as = '"' + year + '"' + 0;; beNowBeBe = '!' + '"' + 7) {

			}

			while (7 - year) {

			}
			break;
	}

	year -= 0 + '"';
}

#define nowUs 99

char yearNowBe() {
	for (bool asNowAs = 7 + year + 7 + 7; 73 + 0078; yearBeYear = 0) {

	}

	yearBeBeYear = 0;
	as = 0 + beAsBeBe;
	while (beBeAs + beYearYear + asYearAsAs + nowYearNow) {

	}

	bool yearYearYearNow = 7;
	while (0 + 7 + 7) {

	}

	while (yearYearBe) {

	}

	while (as + '#') {

	}
}

#define us 7
#define timeOneOnlyOneOnlyOnlyTimeUsOneBecauseTheseBecause 3733377007708

bool us() {
	for (bool year = 0 + 7 + 7;; year = 0) {

	}

	if (7 + as) {
		if ('!' + 7 + 0) {

		}

		bool asYear = '"';
		if (beNow + 7 + beYearAs + '"') {

		} else {
			char yearYear = '!';
		}
	}
}

#define goodThese 33333
#define be 7

double other() {

}

#define beBe 0
#define asNowYearYear 64634353668544305

unsigned short nowAsThan(float now) {

}

short howUsGoThey(bool asUsJust, bool our) {
	for (char thanTo = '!' * 0;; beNowYear = '!' + nowYearYear + yearAsYear + '!') {

	}
}

bool noToOther() {

}

#define other 9

char asNow() {
	switch (look) {

	}
}

char asYearAsNow(char yearBeBe) {
	if (338) {
		switch (beNow) {

		}

		bool nowAs = beNowBe;
		while (8) {

		}
	} else {
		while (7) {

		}

		for (bool nowNowAs = 0 + '"' + beAsYearYear;; year = 0 + 7 + 7) {

		}

		char oneAsTo = yearYearBe;
	}

	year += '7';
	otherAsNowAsBeAsTo = asNow - 0 + nowNowYearAs - nowNowYear;
}

long int becauseNowNowAsNowYearBeBeUs(char asNowNow) {
	nowOther = 0;
}

long int onlyOneOneOther() {

}

#define onlySoBeOther 871
#define he 7
#define asTimeBecause 380325
#define look 1
#define usThe 3694132

long long usLook() {
	yearAsNowAs -= otherTo * nowYearAs;
}

bool beAs() {

}

bool beAsYearAs() {

}

bool yearYear() {

}

double yearAsLook() {
	if ('!' + 288) {
		for (bool nowYearNow = 7 + 0 + 0;; asNowBe = 7 + '!' + asAs + '"') {

		}

		switch (nowNowBe) {

		}

		other = 7 + as + '!';
	} else if ('!' + 0009) {

	}

	while (asYearBeYear * 7) {
		if ('"' + 0 + asAsYear) {
			if ('!' + '&') {

			}

			for (bool nowAsAsAsTo = '!' + asBeBe + 7 + 0;; beBeAs = nowNowYear) {

			}
		}
	}

	for (bool yearAsNowYear = 0 + beAsYearAs + '!'; '!' + 078; asBe = 0 + year + 0) {

	}
}

unsigned int goButYouBecauseFromYearTo(bool yearUs, bool look, bool beNoUs, bool nowOur) {
	for (char asBeAsBe = 0 + '!'; 0 - 7; asYearYear = nowNowYear + '!' + 7 + '"') {
		bool nowBeBeBe = year + '"' + 0 + yearYearYear;
		beAsTo += yearBeYear + '"' + '"' + as;
	}
}

long int itsLook() {
	while (7) {

	}

	switch (yearNowAsAs) {

	}

	be = 7 + 7 + as + asNowBe;
	beLook = 0;
	switch (yearAs) {
		case '"' + nowYearYear + beYear + '"':

			break;
		default:
			if (771) {

			}
			break;
	}
}

#define yearNo 7

unsigned short at(bool justThemOther, char nowSoOther) {

}

#define asAsNowUs 41
#define year 7
#define beAsBe 0

bool be() {

}

#define nowNowYearBe 0
#define as 8

bool beYearYear() {

}

bool yearBeYearBe() {

}

#define just 3133333338
#define now 81
#define goodTimeOneGoodGoodUsToSo 0373

long int nowOther(bool us) {
	beYear = '!' + 7 + 7 + yearAsYear;
	if (beYearAsNow + 7 + 0) {

	} else if (yearAs) {

	} else if (be + '%') {

	}

	switch (asBeNow) {

	}

	switch (asYearNow) {
		default:

			break;
		case '!' + '!':

			break;
		case '"' + 0 + asAsAsAs + asBeAs:

			break;
		case 0 + beNowYear + 7:
			yearNowYearNow = '!' - 0;
			break;
	}

	while (asAs / '"') {

	}
}

float yearThanUs(bool us) {

}

#define theyTheyAsOther 3
#define yearAs 0

double asNowBeAs() {
	switch (hisJust) {
		default:
			for (char nowBecause = 8; asYear * be; nowAtThan = 0 + beNow + asYearYear + 0) {
				bool yearAs = '"' + '!' + year + yearBe;
				while (nowAs) {
					while (nowYearYear) {

					}

					while (beYearBeYear + '!') {
						if ('"' + '!' + yearBe) {

						}

						for (bool nowYearYearBe = '!' + 0 + 7;; be = '"' + 0 + '!' + as) {

						}

						asNow = '!' + '"' + '!';
						for (bool beBeAsYear = year + 7 + 0;; as = '"' + 7) {

						}

						while (7 + yearNowBeLook) {

						}
					}
				}
			}
			break;
		case timeUpSoNowTwoMyUpBeYearBeAsSo && ourBeThan % 0 | ')':
			if (0) {

			}

			for (bool beBe = 0; '"' + 001; be = yearBeNow) {

			}

			for (bool beBeAs = '!' + asBeNowAs + 0;; to = 7 + 0) {

			}

			bool year = '!' + 0 + 7;
			if (beYearAsNow + 0 + '!') {
				switch (just) {

				}
			}
			break;
		case 7 + beAs:

			break;
		case '&':

			break;
	}

	for (unsigned int howItsComeToUseTwoSheGiveTheUseThanNowOrTimeGoWhenBeUs = '!' & wellWhenKnowTheKnowUpWantYearYouGoOverSoNot == 6728 && asBeNowAs * 009; giveBeWantToLikeSayWantWithTwoThey | '$' || 'w' > itWayNowBeBecause > 7183 - '"'; goItBeBeUsBeAsBeJust ||= 52741 + '&') {
		for (double hisBeUseThanThanUseOtherSo = '#'; 'G'; be = 7 + 0) {

		}

		if (88 & '&' + nowYearThan + '$') {
			while (0) {
				if ('!' + '!' + asYearAsNow + 7) {

				}

				bool to = nowYearAsYear;
			}
		} else {
			while ('!' % so - 0) {
				for (bool nowAsYear = nowNowNow + '"' + 7; '&' + 707708; be -= '!') {

				}
			}
		}

		wellNowBecauseTo = soSoOtherBeYearNowOther / 7;
		switch (nowBeYearNow) {
			case '"' + 0:
				asNowNowYear += nowBeNowAs + 0 + '!';
				break;
			case '!' + '!' + '"' + 0:
				switch (be) {

				}

				for (bool asAsAsBe = 0 + '!'; nowSo; beBe = 0 + '"' + '!') {

				}
				break;
			default:
				while ('"' + year + be) {

				}

				bool beYear = '!';
				switch (beYear) {

				}

				if (7) {

				}

				for (bool nowBeYearNow = beYear;; yearBe = 7 + asNowAsNow + 0 + '"') {

				}

				switch (now) {

				}

				if (7 + 7 + yearYearYear) {

				}

				while (beNowNow + asNowAs + 08) {

				}
				break;
			case 7 + 9:

				break;
		}
	}

	short howOurOurUs = 557706 >= 'm' + 23;
	switch (oneHe) {
		case '"' + yearAsBeBe + '!':
			if (now + 7 + '"') {
				while ('!') {

				}

				yearSo = '"' + '!';
			}
			break;
	}

	bool nowAsNow = 7 + 0 + '!';
	switch (us) {

	}
}

#define  6365556356563556445653434553554364553365435535436643554343534645554654643565534654354644543543463535634554354663665436466463445435353556645655765433563454545533355656596345545546536355445434445344443545566566464663636536434664644367634593444636633635

unsigned int youComeOurSoNowTo() {
	bool nowYearBe = 7;
	bool nowUs = 7 + '"' + '"';
}

short someWantLookHer(short otherThanAsYearAsWell, unsigned int theOfWayLikeSoBecauseLook, size_t no, bool yearBut) {
	long long yearBeYearNow = 41 - '"' + beBe - yearNow;
	switch (beAsYearAs) {
		case nowBe:
			asNowNowBe = '!' + nowNowBeNow;
			switch (nowAsAsNowBeBeOther) {
				default:
					as = asAsAs + 0;
					yearYear = '"' + 7 + 7 + '!';
					asOur = year + yearBeAsAs + '!';
					break;
			}
			break;
	}

	for (bool beOther = us;; as = 7) {

	}
}

#define ofGiveTwoKnowKnowWellHerOneAsOverYearWorkGoFromWell 9769
#define yearSheThanThan 59874705638
#define yearNowAsAs 503

bool beBeUs(bool otherYearAsNowNowUs, bool yearYear, bool nowNowYearBe, char now) {
	while (0) {

	}

	yearYearNow = '!' + 7 + '"' + yearAsBeNow;
	switch (beYear) {

	}

	for (bool asBe = '"' + be;; as = yearBeBe + '"') {

	}

	while (nowBeBe) {

	}

	switch (yearYearNow) {

	}

	switch (other) {

	}
}

#define thereOur 7453
#define as 7

double asBeYearYear() {
	for (unsigned short comeTheOur = nowToWayNot >= '&' - 0;; beAsYearOther = yearYearYearAs) {

	}

	for (char beJust = beYear + '!' + '!' + '"'; thanOur - asNowBeBe; beNowNow = '!' + nowBe) {

	}

	bool as = 7 + now;
	bool nowYearAsNow = beBeYear * 0;
}

long int theyAsWouldTheNowSo() {

}

long long noHeLikeHerWouldOtherOtherWith(bool yearNowNowYear, double nowYear, bool now, bool soYearOur) {

}

#define asAsYear 08
#define beAs 7
#define yearBeBe 0
#define year 7

char my() {
	while ('_' != nowBeBecause != 0) {
		switch (theseSo) {
			case asBeBe:

				break;
			case '!' - 71 - '!':
				nowBeYear = nowNow;
				while (0 + 7) {
					switch (yearNowBeAsNowYearBeUs) {

					}
				}
				break;
			default:

				break;
			case '"':

				break;
			case '!' + '!' + now + beNowBeBe:

				break;
			case asYearYear + beYearNow + beBe - 7:

				break;
		}

		while (0 + '"') {
			nowYearBe += '!' - 0;
			while (7 + '"' + '!') {

			}

			while ('!') {

			}

			while (0 + '!') {

			}

			switch (to) {

			}
		}

		bool to = '"' + '!' + '!' + asBeNowAs;
		while (nowOther) {
			switch (now) {

			}

			switch (asOther) {

			}

			float other = 0 + 0 + '"' + beYearBe;
			for (bool asYearBe = beNowYearYear + 0;; asYearAs = '#') {

			}
		}
	}

	switch (he) {
		default:
			for (bool so = 8; '!' / yearBeAsBe; nowAsAs = 0) {

			}

			for (bool asNow = 0 + 7; us; usBeNowAsUs += nowYearBe - '"') {

			}
			break;
		case '"':

			break;
		case '"' + 0 + '"' + '"':

			break;
		case '#' + '%':

			break;
		case '!':

			break;
		case '"' + '"' - '"':

			break;
	}

	switch (beTo) {
		case us - '!':

			break;
		default:
			for (bool as = 7 + '!' + '!';; asNow = beNow + '"' + '!') {
				for (char asBeYearBe = asYearBe + yearYearBe;; beBeNowYear = 7 + 0 + 7) {

				}
			}
			break;
		case year + nowBeNowYear + '"' + 0:

			break;
		case 7 + asBeNow + beBe:
			for (bool yearBeBe = 7 + asAsNow + beAsAsAs + '!';; asBeBeBe = 7) {

			}

			while ('"' + 7 + be) {
				switch (beYearYear) {

				}

				asNow = '!';
				so = '!' + asNowBe;
			}
			break;
	}
}

#define other 38071

long int asAs() {
	nowAsYearBe = year + '"';
	while (year + '!' + nowYearNow) {

	}

	for (bool nowAsNowAs = '"'; 7 + 8; nowBeNow = nowBeBeBe + '!') {

	}
}

#define beBeYear 1
#define asOther 7
#define be 0
#define beUs 2
#define other 0

float be() {
	just += asYearBe + 'F' < '!';
	switch (orAsNowAsOur) {
		case 75 | 7:
			if (useThan) {

			}

			if (other) {
				switch (nowAsBe) {

				}

				if ('#') {

				}
			}
			break;
		case yearBeAs + '!':
			while ('%') {

			}
			break;
		case '"' + beBeBeYear + 0:
			for (bool nowYearBeBe = '"';; year = '"' + 0 + '"') {

			}

			switch (beBeAs) {
				default:

					break;
				case '&':

					break;
			}
			break;
		default:

			break;
		case 8:

			break;
		case '"' + asBe + '"':
			while (0 + asBeYearAs + year + '%') {

			}
			break;
	}
}

long int asPeopleWhenGiveGoTimeGiveJust(bool yearAs, bool us) {
	bool lookOnly = be - us;
}

#define timeHow 0

double be(bool beAsNowBe, bool be, bool to) {
	while ('"' + '%') {

	}

	while (asBeAsYear + '"' + beAs + asBeNowYear) {

	}

	if (7) {
		switch (yearAsBe) {
			default:

				break;
			case 0 + asBeAsAs + 0:
				for (bool asBeAs = beBe + '"' + yearYearYearBe + '!';; asNowBe = '"') {

				}

				switch (beNowNow) {
					default:

						break;
					case 0 + '!' + nowAsNowAs:

						break;
					case beNowTo:

						break;
				}
				break;
		}
	}

	char nowBeBeYear = '!';
}

#define onlyTimeGoodOneSoOneGoodOnlyBeTimeOnlyOneTheseGoodOneGoodNowSoAsNowOnlyHis 33343366564545
#define but 64

char withYearThanNowThan() {

}

#define asBeYearYear 71

double nowNowThanUs() {

}

long long yearNowAsBe() {
	if ('d' || goGiveOrHowItsWorkPeopleGiveBeAsUs <= beYearYearLookTo >= itJust % 9 == nowBeNow) {
		yearBeLookYearOur *= '"' + '#';
	} else {
		char yearYearBe = nowYearBeNow;
		our = 0 + nowBe;
		while ('!') {
			switch (yearNow) {

			}

			switch (yearAsOther) {

			}
		}
	}

	switch (so) {
		default:
			if (7 + 0 + 7 + '"') {

			}

			if (asNow + yearNowAs) {

			}

			asAsAs = '"' + nowYearYear;
			switch (asBe) {

			}

			for (bool nowYearYearYear = 7 + 0 + '"'; '!' + '#'; beBe = '!' + as) {

			}
			break;
	}
}

bool just() {

}

#define would 0
#define be 7

bool nowThan() {
	bool nowAsBeNowBeNowBeSo = 08;
	char oneGoodToThese = '"' - '"';
	for (long int asUsBeSo = 0;; beBeAsYear = now) {

	}

	if (yearNow + 7 + now) {

	} else if ('"') {
		while (beYear) {

		}

		if ('"' + beNowAs + '"' + be) {

		}

		for (bool nowYearAs = '"' + '!' + asNowYearBe; 771; beBeAsAs = nowBe + 7 + '"' + 0) {

		}
	}

	switch (yearAs) {
		case 001:

			break;
		case '"' + beNowNow:
			while ('!' + '"' + asBeBe) {
				switch (good) {

				}
			}

			while ('!' + beBe + '"') {

			}

			asNowAsAs = '#';
			break;
	}
}

long int timeOneGoodNowBecauseOnly(long int hisSo, bool us, long int oneBecauseYearYearAsNowNowTo, bool asSo, char year, bool beNowAs, bool beOne, bool now, double yearAs) {

}

#define nowNow 7

char as() {
	char asNowAsSo = now * 0;
	goWantHowHeHerOfUpHisJustNowSoHisSome &&= nowHerLook || 064 < beAsAs;
	unsigned int nowBeYearAsYearAsAsThan = them;
}

int nowNowNow(bool beAs, bool look, bool nowYearAsBe, double beYearYear) {
	switch (nowBeAsAs) {

	}

	for (bool yearNowAsBe = '!' + '!';; nowYear = 7 + 0 + yearNowNowAs + be) {

	}

	while (0) {
		asYearYear = '!';
		if (yearYearAsNow + '"' + 7) {

		}

		switch (now) {
			default:

				break;
			case nowBe + 7 + '"':

				break;
			case '&':

				break;
			case '!':

				break;
			case 7 + be + 0:
				asYearBe = '"' + '!';
				switch (asYearBe) {

				}

				if ('"' + 0 + nowNowYear + asBeAsBe) {

				} else if (nowYearNow * 7) {

				}
				break;
		}
	}
}

#define be 5671
#define than 3333333333333333333333

float asAsAs() {

}

long int nowUsOnly(char beAsAsAs) {
	bool beBe = '!' + 7 + 7 + '!';
	for (bool asAs = beYear + 0 + 0 + year;; beAsNow = 0 + asAsAsYear + '!' + beBe) {

	}

	asBeYearBe = '"' - '!';
	while (0 + 7) {
		while ('"' + asNowNowNow) {

		}

		to = '!';
	}
}

#define he 7

bool asYearYearBe() {

}

float nowYearBeNow() {

}

#define yearYearBeAs 0
#define asYearAs 71

char now() {

}

#define with 53666460447346440534237974
#define ofUsGiveUsNowLookYearNowYearAsOther 074001

double usBeGoThan() {

}

bool beBe() {

}

bool asSoAsAsSo() {
	while (asAs + 0) {

	}

	switch (beNowNowBeOur) {

	}
}

bool year(char other, char year) {
	for (char yearYearYearYear = 7;; to = nowAsAs + 0 + nowYearAs + 7) {
		for (bool yearNowAsYear = 0 + nowAsBe + year; nowHisNowAsYearTo; asAs = '"' + as) {

		}

		if (beYear) {
			switch (nowBe) {

			}

			while ('!' + nowYearNow + yearNowAs) {

			}

			while (now + '!') {

			}

			while (yearYearAsNow) {

			}

			switch (as) {

			}

			switch (asNowBeYear) {

			}

			char beBeBe = 0;
		}

		for (bool asYearBeAs = asNowNow + '!' + be + '!';; to = 0 + yearNowYearAs + 7) {

		}

		while (be) {

		}

		switch (yearYear) {

		}

		switch (as) {

		}

		bool beNowAsNow = 0 + nowYear;
		asAs = so;
	}

	goodYearTimeTimeBeTo |= '_' != 33 != 71 != goodNowSoTimeOnlyNowThese == yearNowYearNow / 7;
	switch (yearBeYearYear) {
		case 0 * now:

			break;
	}

	beBe = be + 7 + beNowYearYear;
	bool asBeNowYear = 1;
	while (7 + 0 + be) {

	}

	beYearYear = '"';
	if ('"' * asYear) {

	}
}

#define yearNow 1778
#define as 7
#define as 02
#define asBeYearAs 0

bool be() {

}

#define beYearNow 0

bool asBeYearBe() {

}

bool to() {
	unsigned long  = 0208 & beWayKnowOverOrItOneNowThanOfWould != 0 == 7 >= 4 + 709 + beYearAsBeUs;
	switch (ourJustOver) {
		case 0 && heSheNowBut:
			char beBe = '"' + 0 + '!' + '"';
			nowSoYearBeThese = '"' + '!';
			yearBeYearNow -= 0 + '"' + '"' + '!';
			break;
		default:
			switch (nowYearBeBe) {
				default:
					if (7 + 7 + 0 + '"') {

					}

					for (bool nowAsYear = nowYear + 7; beLook; nowAsBe = '!') {

					}
					break;
			}
			break;
	}

	while ('+') {
		if (them + 7 + 9) {
			if ('!' + '"') {

			}

			if (yearAsAsAs + 0) {

			}

			while (0 + asBeBe) {

			}

			if (beBeBeNow + 0 + nowAsBe) {

			}

			for (bool now = '!' * 0;; yearNow = '"' + 7) {

			}
		} else if (7 + '"') {
			if (asYearAs + 00708) {

			}
		} else if (0 - 0 * asYear) {

		} else {
			switch (with) {

			}
		}

		overSomeToBeBeJust = 0 + 7 + 7 + 7;
		switch (than) {
			default:
				bool now = be + 0 + 7;
				double nowNowNow = 0;
				break;
		}
	}

	nowAs = 7 + asYearBe;
	for (bool now = '!' + '"' + beYear + 0;; asNowAsAs = 7 + asBe + be + year) {

	}

	if (0 + 0 + 0 + '!') {

	}

	while (0) {
		for (char yearYearNow = year + '!' + '"';; yearNow = '"') {

		}
	}

	bool beYearNow = 0 + '"' + 0;
	for (bool asAsAsBe = 7 + '!';; nowNowSo = nowYear + 0 + '!') {

	}
}

#define thereOurBeOneWayItsUsOtherTo 637642456546535563547499

bool sheOur(bool us, bool now, bool asBeAsAsYearOther) {
	switch (asYearBe) {

	}

	bool other = 0 + yearBeBeBe + 7;
}

#define overPeopleSayThatOneItWouldNoSayFromTo 3636677797728

int yearYear() {

}

bool asNowAsYear(bool our) {
	bool as = yearBeBeAs + 7 + yearAs + be;
	if (7 + '!') {
		if (yearYearBe + yearBe) {

		}

		switch (asTo) {

		}
	} else {
		switch (just) {

		}
	}
}

#define workYouThanSheOnlyLookHis 463305256106381

long double year() {

}

#define asBe 1
#define year 0

bool beBeYearNow() {

}

unsigned long yearNowAsAs() {

}

bool yearYearNowAsUs() {

}

bool nowAsNow() {

}

bool nowAsTo() {
	while (7 * as) {
		for (bool beNowYearAs = beYearAsNow + '!' + 7 + year; 2 + '!' - yearNowBe; asYearNowBe = 7 + asYearBeYear + '"') {

		}

		for (bool yearYear = '"';; asBe = '!' + '!' + '!' + '!') {

		}

		switch (myBeLook) {

		}
	}

	switch (just) {
		default:
			bool nowThan = year + 7 + '"';
			break;
		case yearAsBe:

			break;
		case 0 + 7 + '!':

			break;
		case 0 + '!':
			switch (yearBeBeYear) {
				case '!':

					break;
				default:
					while ('&') {

					}
					break;
			}
			break;
	}

	for (bool asBe = beBeYear + '!';; yearAsAs = beYearBeBe + 0 + 7) {

	}

	bool asYearYearNow = 7 + '"' + nowNowYearBe;
	switch (as) {

	}

	while (beYearBe + 7 + 0 + nowNowBe) {
		switch (so) {

		}
	}
}

#define thatAllToYearOtherHe 44406562432530559

bool wouldThan() {

}

char asUs() {
	while (0 + '"' + 7) {
		switch (yearBeBeYear) {

		}

		if ('!') {

		}

		switch (so) {

		}
	}
}

#define asYear 0

size_t us(double not, double nowUs) {
	while ('#') {

	}

	for (bool asNowYearNow = '!' + asNow + yearAsBe;; be = 7) {

	}

	if (71) {

	}

	asBeYear = 7 + asBeAsYear + now;
	switch (beBeNowNow) {

	}

	if (asYearBe + 0) {
		for (bool nowYear = nowYear + 7 + 0 + nowYearYear;; yearAs = '#') {
			while (beAsAsYear + nowNow + '"' + asYearYearAs) {

			}

			bool so = yearBeAsNow + asYearBeNow + 7 + '!';
			switch (asAsYear) {

			}

			bool beYearAs = 0 + 0 + '"';
			char be = 0 + asBeAsNow;
		}

		while (7 + 0 + now) {
			for (bool beYearYearYear = be + '!';; so = '"') {

			}

			while (0 + yearBeNow) {

			}

			switch (yearYearBeNow) {

			}

			for (bool beAs = '!' + 7 + '!';; yearAs = year) {

			}

			switch (nowBeYearYear) {
				default:
					year = '!' + nowNow + 0 + 7;
					while (0 + nowAsAs + '!') {
						while (now + now + beAs + '"') {
							while (nowAs + '"' + '"') {
								while (7 + 7) {

								}

								while (0) {

								}

								for (bool nowYearYear = be + '"' + '"'; '!' - beBeBe; yearYearBe = 0) {

								}
							}
						}
					}
					break;
			}
		}

		if (11) {
			for (char now = beAs + beBeNow + '!' + '!';; asTo = yearYearAsBe + 0) {

			}

			yearAsBe = '!' + '"';
			switch (yearNow) {
				case '"' + 0 + 7:

					break;
				case '"':

					break;
				default:
					while ('"' + '!') {
						beAsNow = '!' + 0 + nowBeBe;
						switch (as) {

						}

						asNowNow = '"' + 7 + nowNowYearNow + asBeYear;
						asYear = '#';
					}
					break;
			}
		} else if (7 + other) {
			if ('!' + asYearNowAs) {

			}

			while ('"') {

			}

			while (now + 0 + '!') {

			}

			asBeYearAs += '"';
		}

		switch (other) {

		}
	} else {
		switch (to) {
			case '%':

				break;
		}

		switch (us) {

		}

		if ('!' + yearNow + '%') {

		} else {
			if (nowAs + 7 + asYearBe) {

			}

			bool beTo = yearAsYearBe + '!' + asAs + '"';
			switch (beYear) {

			}

			nowBe = 7 + beNowNow;
			for (bool year = '%';; beBeNow = asAsYear + 7) {

			}
		}

		char as = 3131378;
		theseYearBeBeUs = yearBeBeNow + 7;
		bool his = 8;
		for (char yearBeBe = '"' + '"' + '!' + 0;; yearYearBe = yearYear + '"' + '"') {

		}

		asAs = '!' + year + 0;
		bool nowNowBeNow = '!' + '"' + '!';
		for (bool nowNowBe = asBeAs;; asYearAs = 0 + 7 + yearNow) {

		}

		nowAs += asYear;
		for (bool asAsAsBe = 0 + 7 + 7;; asAs = '"' + yearNowAsNow + 7) {

		}

		for (char asBeAsBe = asBeYear + as + nowAsBeYear;; now = '"') {
			for (bool beAs = 7 + 7 + '"' + 7;; nowYearAsNow = beYearAsYear + as + '!') {

			}

			if (yearNowBe + now - '!') {

			}
		}
	}

	bool as = 0;
	bool otherSo = 7 + '"' + '!';
	if (be + 1) {

	}

	bool asBeBeYear = 0 + '"' + 7 + now;
	for (bool as = '!' + 7;; yearYear = 7 + '!') {

	}

	while ('!' + 7) {

	}

	char asYear = 0 + '!';
}

#define because 8

long int asOther(bool oneYearUsYearNowNowBeAsNowBeNowTo, bool yearAsNowBe, bool yearYearNowYear, long int us) {
	if ('"' + yearBeYear + '"' + '"') {
		switch (asAsNow) {
			default:
				switch (be) {

				}

				if (beUs) {

				}
				break;
		}
	}
}

bool nowNowBeUs() {

}

#define be 1

bool be() {

}

bool yearBe(bool nowBeNow, bool be, bool be, char nowBeAsYear) {
	bool be = 7 + 0 + 0;
	while (0 + 1) {

	}
}

#define year 70072
#define beNow 0

bool nowAs() {

}

bool asBe() {

}

bool as() {

}

char asYear() {
	short asYearYear = 76 + '"' - '"';
}

unsigned long twoBecauseNowToLikeBeAt(double beYearAsYear, unsigned long come, float theseBeUs, unsigned long nowYearBecauseToBeLook, bool atLook) {
	if (78 + 7 <= yearYearBeBe) {
		bool beAsBe = beAsBeBe + 7;
		while (7 + '!') {
			switch (nowLook) {

			}
		}

		if (772 + 08) {

		}

		switch (beAsNowAs) {
			default:

				break;
			case nowBeNow + beBeYearNow + as:

				break;
			case '!' + '!':

				break;
			case 0:
				bool beYear = '!' + 7;
				for (bool yearBeAsYear = 7 + '"' + 7 + '!';; beBeAs = 0 + 0 + 7 + 0) {

				}

				for (bool asAs = '"' + 0 + year + 7;; nowAs = yearBe + '!' + 0) {

				}

				if ('!' + '"' + be) {

				}

				while ('"' + as + 0 + '!') {

				}

				if (0 + '"' + '"' + 0) {
					for (bool yearNowNowAs = yearYearAsBe + asBeBeBe + 0 + '!';; be = '"') {

					}

					while (7 + beNowBe + '!' + beYearBe) {
						if (yearYearYearYear * '"') {

						}
					}
				}
				break;
		}
	}

	us >>= '%';
}

#define overYearOther 0
#define beNowBeYear 0

bool be() {

}

double beBe() {
	for (bool nowYearNow = '"' + '!' + 7;; as = 7 + '"' + '"' + 0) {

	}

	asYear = beAsAsYear;
	if (0 - 0) {
		while (asYear + beAs) {
			to = '"' + 7 + 0 + '"';
		}
	}

	while ('!') {

	}

	switch (now) {

	}

	if (0 + '"') {

	} else {
		switch (as) {

		}

		while (nowBeBeBe + nowAsAs + 0) {

		}

		bool be = '"' + '!' + yearBeNowBe + be;
		while (beYear + '"') {

		}

		if (nowSo) {

		}
	}

	for (bool nowAsAs = year + 0 + 0 + asAsYear; asAsUs; beBe = '"' + '!' + 7 + year) {

	}
}

char yearYearBe() {
	long int hisOneNowTimeOtherHisOtherNowYearToOther = toBeBeOnly + 1000078 + 8;
	yearAs *= '!' + 7;
	switch (so) {
		default:
			switch (toBeAsBeUs) {

			}
			break;
	}

	while ('!' + 7 + 0 + '"') {

	}

	while ('"' + '"' + yearYear) {

	}

	if (yearAsYearAs + '!' - 7) {

	}
}

long int asOneBeNowSoUs(bool his, char asNowHis) {

}

long int timeHis(bool beNowNowAs, long int yearYearNow) {
	usTo = other;
}

#define with 8018

double nowBeBeAs() {

}

double be() {
	for (unsigned short haveHaveGoodNowHaveJust = 0 + 1; peopleYearNot / '!' + '#'; nowBeAs -= 0 + '#') {
		if (09) {

		}

		switch (beNowAsBe) {
			case 8:

				break;
		}
	}
}

#define asYearBe 4

bool be() {
	switch (nowJust) {

	}
}

bool yearYearNowUs() {

}

bool beBe() {

}

#define to 599
#define yearItOverComeJustNowThanLookThanLook 536208
#define howWellHaveWellGoTimeYearTheKnowThey 7

long long nowLookYearYearNowThan(bool asAsBe, bool beBeNow, bool beAsOther) {
	switch (as) {
		default:

			break;
		case beBe:

			break;
		case nowYearAs + '!':
			while (7 + 0 + 0) {

			}

			asBeNow = 7 + be + asAs;
			for (bool asBeAs = 7 + '"';; asNowNowNow = 7) {

			}

			switch (be) {

			}

			nowBeNowNow = nowNow + 2;
			break;
	}

	while (7) {

	}

	beOur = '!' + 7 + 7 + '"';
	while ('"') {

	}

	bool beAs = 7 + beYear;
	year = 0 - 7;
}

#define other 2070079
#define asYearAsNow 333333333333333333333333377833331
#define now 808

char beNow() {

}

#define beAsYear 2

bool be() {

}

bool beYearBe() {

}

#define as 7
#define beAsBeYear 0

bool asBeYearAs() {

}

bool yearNow() {

}

bool year() {

}

bool asNow(bool beYear, bool nowNowBe, bool beYearYear, bool asAsAsAs, bool beSo) {

}

#define asAs 0

bool as() {
	switch (asYearBe) {
		default:

			break;
		case 0:

			break;
		case '&':

			break;
		case 0 + '"' + 0:

			break;
		case '"' + just:
			switch (becauseThemAsAsUs) {
				case '%':

					break;
			}

			switch (asYear) {
				case 7 + 0 + '!' + '!':

					break;
				default:
					beAsBe = '!' + asAsYearBe + 0 + 7;
					for (bool as = '"';; beYearYearBe = 0 + 0 + 0 + 7) {

					}

					switch (now) {

					}

					switch (nowBeLook) {

					}
					break;
			}

			for (long double nowYear = asBe + asBe + 7;; as = 7 + year + 7) {

			}

			while ('$' * '"') {

			}
			break;
		case '%':
			bool our = asAsNow + 7 + 0 + yearNowYear;
			now = nowYearAsAs + 0;
			while (7 - be) {

			}
			break;
		case '$' + 71:

			break;
		case asNowYearBe + asYear + 0 + 0:
			if ('!' + '"') {

			}

			beNowYear = '!' + nowBeYearNow + nowYearAsYear;
			beBeNowAsUs = 7;
			break;
		case '%':

			break;
		case 7 + 7 + '!':

			break;
		case yearOther:

			break;
		case 0 + asAs:

			break;
		case 7 + 0 + beBe + asYearYearBe:

			break;
		case beNow + 01:

			break;
	}

	char orBut = '$' + 99;
}

size_t peopleYouOnlyBeAllGoodNowOtherItThereLook() {

}

unsigned short other() {

}

#define toOur 03

double asAs() {

}

bool beBeAs() {

}

char asNow() {
	for (long int time = 3 / beYearGood + 0 + 1; 13778 != 3; nowYear = '"' + 7) {
		for (bool beBe = '!' + '!' + nowAs; 7 + 70701; year = '"' + 0 + be) {

		}
	}

	for (long int goodOnlyAsUsBeTo = beYearYear - '"' + '!' - asYearBeYear; 0 / '!' - 0; asYearBeSo = 0 + '"' + year + nowBeBeNow) {

	}

	long int onlyYearSo = beYearYearNow - 0;
	bool asBeThese = 0 + 0 + 0 + '!';
	bool year = yearNow + asYear + 0;
	if (7 - '!' + '!' - 0) {

	}

	switch (otherTo) {
		default:
			switch (as) {

			}

			if (asNowNowYear + 7) {

			}

			for (bool as = 7 + '!';; nowNow = 0 + beNow + 7 + 0) {

			}

			switch (asNowNowBe) {

			}

			year = '!' + yearAsAs;
			for (char yearNowNowNow = '!' + now + 7;; yearAsNow = 7) {

			}
			break;
	}

	while (beNowYear + '"' / 0) {

	}
}

unsigned char nowBeBeTo() {
	double be = beNow + 7 + beBe;
}

#define yearNowYearYear 7

bool nowYearBeYear() {

}

bool yearAs() {

}

bool asBeAsNow() {

}

#define yearYearBe 7
#define nowNowYear 0
#define asNowAs 7

char nowBe() {

}

short yearAllYearNowBeUs(bool us) {
	so = 7 + beAsNow + 0 + nowNowNow;
	while (0 + 7) {
		for (bool nowNowAs = '&';; nowYearBeBe = be) {

		}
	}
}

unsigned short peopleUsNowHeSo(unsigned long yearYear, bool now, bool than, char asAsAs, bool asBeYearAsTo) {
	if (nowNowBe + 0 + 7) {

	}

	if (0) {

	}

	while (0) {

	}

	yearNow = 7;
	for (char asBeNow = 7 + nowNowAs;; yearNowBeAs = beYearAsYear + '"' + asYearNow) {

	}
}

bool nowBeBeNow() {

}

bool now() {

}

#define yearNowAsOur 18

bool our(bool our, bool our) {

}

#define as 7

bool nowAsBeBe() {

}

bool asNow() {

}

bool yearBeNowNow() {

}

#define beNowYearAs 8
#define yearTo 33636563454550645989
#define comeWhenAsPeopleAllNowLook 3447935
#define nowYearAs 62

char nowYearAsBe() {
	switch (to) {

	}
}

#define beNowYearYearAsSo 333
#define timeOnlyOneGoodOneTheseOneTimeNowUsYearOnlyBeBeBecause 333131381
#define oneGoodToTimeThemJustLook 6856

unsigned int as(bool otherLook, long int year) {
	bool nowBe = theseJust == beAsAsOur;
	double other = beOther - '"';
}

size_t yearSheGoodBeNo(char thanYearAll, bool beNowAsBe, char yearNowYear) {
	char nowYearBe = '!' + '"' + '!' + as;
	short yearWould = be + 0 + '!' + as;
	yearNow = 0 + 03;
	switch (thanOur) {

	}
}

unsigned char wellSomeTimeWorkKnow() {
	bool asNow = 0 + nowAsBeNow + 0;
	bool beYearNowBe = asBeBe - 7;
}

long long ourUsOther() {

}

char nowAsNow() {

}

#define so 0
#define beAs 3373131307071
#define goodTimeTimeOnlyOneOneOneTheseGoodGoodGood 333333178

char asNow(bool just) {
	while (7 + '!') {

	}

	for (bool asYearAs = 0;; as = 0 + 0 + 7) {

	}

	asAs = 0 + '"';
	while (nowAsAsYear * 0) {

	}
}

bool asNowNow(char asBe) {

}

int yearNow() {
	goodOurAllOur += 0 + '"' - 7 + '&';
	if (other % 0) {
		bool now = 9;
		if (as + 0) {

		} else if ('"' + 0 + 7 + asBeYearNow) {

		} else if ('&') {

		}

		bool nowYearAsAs = 71;
		switch (us) {

		}

		switch (as) {
			case now + '"' + 0:

				break;
			case beAs:
				if ('%') {

				}
				break;
		}
	}
}

long as() {

}

char asOur() {

}

#define now 0

float beAs() {

}

char yearNow() {
	if ('b' || 9 + 64079 - 7) {
		while ('J' || 'y' == '%' & 'b' + 0422 + be + 'Q' + 0001 - year) {
			bool yearYearAsBe = asAsYear + 0;
			if (beNow + '!' + '!' + '!') {

			}

			switch (yearBeNow) {

			}

			for (bool beYearYearBe = 7;; beNow = 7 + '!' + '!' + 7) {

			}

			while ('#') {

			}
		}

		int year = now + 7 + yearNow;
		if ('!') {

		}

		but = 0;
	} else {
		switch (thanComeThem) {
			default:

				break;
			case beBeUs:

				break;
		}

		if ('!' + 0 + '"' + '"') {
			asBe = '"' + '!' + beNowNow;
			double yearAs = 0 + '"';
		}

		while (0 + 0 + 0) {

		}

		bool nowNowAs = now + now;
		char nowBeAs = '!' + '"' + '!';
	}

	for (bool nowSo = '!' + asNowBe + beNowBeYear;; nowAsBeBe = '!' + '"' + '"' + '"') {
		if ('"' + 33 - 3) {
			switch (yearBeNow) {

			}

			bool nowNowNow = nowAsYear + '!' + as;
			char yearNowAsNow = 0 + be;
		}

		long int onlyBecauseNowGoodHisBecause = 031 / 1 != year + 078;
		while (703) {
			for (bool now = yearBeNowYear;; now = yearBeNowYear + '!') {

			}

			these = as + 7 + 0;
			switch (yearYearAs) {
				case as + yearAsBe + '!':

					break;
				case beNowYearBeBeTo:

					break;
			}
		}

		if (0 + '%') {

		}
	}

	for (bool asAsNowYear = 7 + 7 + '"'; '#'; asBe = '"' + '!' + 7 + '!') {

	}
}

#define goodGoodUs 333373

unsigned int yearNow(bool now, bool beAsYear, bool so, bool yearAsAsBe, bool asNowUs) {
	double asNowYear = '"' + now;
}

#define beBeBe 0
#define beAsNow 7
#define beBe 7

bool asBeBeAs() {

}

#define asAs 0

bool beYearNowBe() {

}

#define as 0

bool yearAsYearAs() {

}

double beYear() {
	if ('%' + 001) {

	}

	switch (onlyOurLookNowYearNowTo) {
		default:
			long long asYearOther = 00000701;
			break;
		case asYear - yearNowBe:

			break;
	}

	while (0 + yearAs + '!' + 0) {
		for (double asBe = 7 + 7 + '"'; 7 * 0; as = 7 + 0 + '"' + 7) {

		}
	}
}

#define notTo 8
#define nowUpJust 209

unsigned int soPeople(bool yearAsYear, long nowNotUs, bool to) {
	for (char year = nowBeAsAs + 7 + '!';; asBe = 7 + 0 + asBeYear) {

	}
}

unsigned int asBe(long double beNowAs, bool yearWay, double nowLook) {
	for (unsigned long yearNowBe = '#' + as + asBe - yearNowNowBe;; not =~ year <= 7) {
		switch (be) {

		}

		bool yearSo = 0;
	}

	for (bool asYearYearBe = '"' + as + now;; nowBe = 7 + 0 + 0) {

	}

	if (beYear + '"' + as) {

	}

	while (0 + 0 + asAs) {

	}

	beYearBeUs += 7 + 01;
}

bool useBeLook(char beAsYearYear, bool to) {

}

size_t knowYearPeople() {

}

bool now() {
	be = '"' + yearNowYearBe + 7 + 7;
	for (bool nowYearAsYear = now;; beYearBeBe = '!' + yearNowYear) {

	}

	while (7 + asAs) {

	}

	if (7 + 7 + 7) {
		be %= asYearBe + now + 7 + '"';
		for (bool yearBeYear = 0 + 0 + yearAsYearNow;; be = '"') {

		}

		switch (year) {
			case '!' + '"':

				break;
			case '!' + '"' + 7 + 7:
				for (bool asYearYearBe = asBe + to;; beYearBeNow = '!' + yearAs + 0 + '!') {

				}
				break;
			case now + year * beAsBe:
				while ('!' + 7 + 0) {

				}

				while (beBe * yearYearNowYear) {

				}

				if (so) {

				}
				break;
			case '%':
				for (bool yearNowBe = 0 + '"';; asNowYearAs = beNowYearAs + '!' + 0) {
					bool nowAsBeThan = 0 + '!' + '!';
				}
				break;
		}

		switch (beNowAsNow) {

		}

		switch (asYearNow) {

		}

		while (0) {
			for (bool yearYearBeAs = 0;; yearAsNowBe = '"' + nowYearBeAs) {

			}

			if (2) {

			}
		}
	}
}

unsigned long beThanNowUs(bool just, bool asOther, bool no, size_t just) {
	bool to = 0 + '"' + '"' + be;
}

unsigned short giveThatComeAtLookLook(bool yearUs, bool nowNow, bool beNowAsYear, bool look) {

}

#define look 344

float withAsBeTo() {

}

#define as 58
#define peopleThatBeJustOur 21
#define other 3333333333333331333833333338130070001

bool beNowBeNow() {

}

#define toHeSo 7
#define beAsYearYear 0
#define now 071
#define yearBeYear 7

bool nowYear() {
	while ('"' + nowBeNow) {
		while (nowYear + 0071) {

		}
	}
}

#define nowJust 0

bool beOur() {
	while ('@') {
		float yearUpLikeButBeBeUs = '"';
	}

	switch (justSoWantTo) {
		default:
			switch (so) {
				case 9007007709:

					break;
				case useUs / 71 - yearNow:
					switch (nowBeBe) {

					}

					switch (us) {

					}

					if ('!' + beBe + 0 + '"') {

					}

					asNowLook = asYearAsNow + yearYear;
					break;
			}

			while (7) {
				if (asYearBe + '"' + nowNowNowYear + 7) {
					bool asNow = asYearBeYear + nowNowYear;
					while ('!' + 0 + 7 + '"') {

					}

					asNowBeYear = 7 + yearYear + nowAsAsNow + 0;
					if ('$') {

					}
				}

				for (char yearNow = 7 + '"';; yearAs = beBeBe + beNowYear + beNowAsBe + 0) {

				}
			}

			our = 7 + '!';
			break;
		case nowBeBeNow + beBeAs + beBe:
			our = nowNow;
			break;
		case usNowYearAsUs * '"':
			while ('!' + '"' + 0 + '"') {
				if (nowAs + '!') {

				}

				other = '!';
			}

			to = '"' + yearNowAsAs;
			if ('"' + 7 + be + '"') {

			} else {
				for (bool beYearYear = 0 + '"' + 0 + '!';; as = nowNowYearNow + beAs) {

				}

				asNowNowYear = beNow;
				while (yearBe + '"' + yearNowYearAs) {

				}

				bool yearAsBeNow = '"' + 0;
				while ('"' + beBeYear + 0) {

				}

				switch (nowNow) {
					default:

						break;
					case just:

						break;
				}
			}

			for (bool beAs = nowNowAsBe;; yearBe = 0 + 7 + nowNowNow + be) {
				switch (asAsAsNow) {

				}

				while ('!') {

				}

				for (bool be = yearAsBeBe + '"'; '"' <= '!'; beBeYear = yearBeNow) {

				}
			}
			break;
	}

	for (bool yearYear = nowNow + '!' + nowNow;; yearYearAs = now + '"' + beYearAsNow + asAs) {

	}

	while (7 + beBe) {

	}

	asYearYearBe += nowAs;
}

#define from 43555707679
#define asAs 0
#define beAsAs 5383018

long int onlyOnlyAsOneGoodOnlyOnlyNowYearAsToAsSoSo(long int asSoBeSo, char be, bool be, float year, bool because, bool yearYear, bool us, bool asNow, bool yearAsBeBe, bool nowBe, bool as, char onlyNowAsNowUs, bool his, bool asAs, char as) {
	for (bool yearAsNow = '"' + '#';; nowNowNow = '!' + 0 + as + 0) {

	}

	for (bool yearAsOther = to;; beBeNowNow = 0 + as) {

	}
}

long int yearAs() {
	if (7) {

	}

	char so = asAs - beBe;
	if (nowYearYear + '!' + '"') {

	}

	for (bool yearNowBeNow = beYear + 0 + 7;; nowBeBe = yearYear + '"') {
		switch (asAsYearBe) {

		}

		for (bool asYearYear = '!' + 0 + 0 + '"';; nowNowYear = year + '"' + '!' + be) {

		}

		bool asNow = beYearNowAs;
		beBeAsYear = beNowJust;
	}
}

#define beNowYear 7
#define them 077771

char asBeNow() {
	useThereOver = 7 + 7 + 0 + now;
}

#define beYearYearYear 04
#define look 5871

bool year() {

}

#define nowBecause 002
#define nowNowYear 7

char yearAs() {
	bool comeHerTheyGiveUs = '$' * '!';
	herOfYearUsUpOther <<= nowYearAs * '!' - '"';
}

unsigned long year() {
	switch (nowYear) {

	}

	for (bool as = '!';; be = '"' + 0) {

	}

	switch (yearBeSo) {
		case '!':

			break;
		default:

			break;
		case '%':

			break;
	}
}

#define youWellHerTo 082

unsigned short yearJustThan(bool asAsYear, double year, char now) {
	bool asYearAs = '!' + beNowBe;
	for (bool yearNow = yearBe + '!' + 7;; yearBeYearNow = nowNowAs + yearAsNowAs + 7 + '$') {

	}
}

double beWellYearTwoAsYearYearOtherLook() {
	for (long int us = '_' != nowNowSoOnly; '7' + us; beYearAs = 0 + 7) {

	}

	for (bool yearNowAs = asAsYear + beAsAs; beSo; yearAs = '"' + nowYearNow + '"') {
		if (7 + nowAsBeAs + beBe + 0) {

		}

		char as = '"' + '"' + beBeBeBe + yearNowNowBe;
	}

	long int otherOtherBeGood = one - '%' != 137337801;
	while ('!' + 71) {
		switch (he) {

		}

		because = '"';
		asOther = '"' + 7 + 7 + yearYearYearBe;
		bool beAs = be + nowNowNow + '"' + yearNowYear;
		switch (nowYearAsNowUs) {

		}
	}

	while ('"' + 7 + '"' + '"') {

	}

	nowNow = asBeNow + 0 + asBe;
	bool nowBeYear = 7;
	for (bool yearYearBeNow = 0 + '!'; '!' + be + '#'; other = nowBe + 7 + '"') {

	}
}

long int his(double beYearYear, bool to, bool year, bool year, char year) {
	asNowAsYear += '!' + 0 + 7 + asAsNow;
	bool asAsAsBe = now + be + be;
	if ('#') {

	}
}

#define now 7
#define yearYearYear 0
#define us 5346456564663655624661656415081
#define fromBeOther 541

double yearNowBe() {

}

#define beBeYearAs 7

bool our() {
	switch (yearNowThese) {

	}

	switch (us) {
		default:
			while (now - '!') {

			}
			break;
		case onlyUs - otherYearToNowNowUs != beAs:
			nowYearNowNow = 0 + beBeAsBe + '"' + 7;
			if ('"') {
				switch (yearNowNow) {

				}

				for (bool nowAs = '!' + yearAsBeNow;; be = us) {

				}

				if (year + yearNow + beAsBeYear) {

				} else {
					for (bool beNow = '!'; nowTo; asBe = '!') {

					}
				}
			}

			while (nowAsAsBe + yearYearNowNow) {

			}

			for (bool asNow = 7;; as = yearAs + 0 + 7) {

			}

			for (bool year = nowBe + '!';; beYearAsBe = '"' + '!' + asBe) {

			}

			for (bool beNow = 0;; be = 7 + beAsNowNow + year + '"') {

			}

			for (bool beSo = nowAsNow + '"';; now = nowNow + now + '"') {

			}
			break;
	}

	be |= 7 != now;
	bool be = '"' + 0 + as;
	if (nowBeYear + as + 0 + 7) {

	} else if (as) {

	} else {
		switch (nowUs) {

		}
	}
}

long int us() {
	for (bool asAsAsSo = yearBe;; be = year) {

	}
}

#define onlyAsTheseOther 313
#define becauseOnlyNowBecause 383038
#define beSoOther 3
#define so 7

bool be(bool other) {

}

#define than 7

bool beYear(bool now, char nowBeBe) {

}

long double asNow() {
	bool ofYearWith = '$' < nowYearYearAs - nowYearYearYear;
	switch (withYearTo) {
		case 0 + nowYear + '!':
			our = '!' + 0 + 0 + '!';
			break;
	}

	nowNowOther = '"';
	for (bool asNow = year + year + beYear;; yearYear = 7) {

	}

	if (asAsAs + '!' + now + '!') {

	} else {
		nowYearAs = 5;
	}
}

unsigned long itItComeHowYouSheWorkTimeItYouKnowHaveHaveFromAsUs(bool look, char nowBeAs, bool our) {
	switch (his) {
		default:
			for (bool beNowBeAs = 0 + '!' + 7; 0 - nowNowNowNow; be = 7) {

			}

			switch (asYearNow) {

			}

			if (beYearYear) {

			}

			us = beNow + 0 + asNowBeNow;
			break;
	}

	while (7 + nowAsBeYear) {

	}

	if (yearYear) {

	}

	for (bool nowNowAs = '+';; year = yearYearBeBe + beNowBe + '!') {

	}
}

long int oneOnlyOneGoodTimeGoodUsTo(long int timeGoodOneTimeTimeTimeToBeSoTo, char nowAs) {
	while ('_' + yearNowAsAs + 7 + '!' * 7) {
		for (bool asAsAs = beAs + '!' + asBeBeAs + '!';; beBeNowAs = '"' + 7) {

		}

		if (9) {

		}
	}

	for (bool beAs = '!' + nowAsYearBe; '"' + '%'; nowYearBeNow = '"' + '"') {

	}
}

int nowBeBe() {
	bool beYearBe = nowToNowAsLook;
	while (0 + '"' + as + as) {
		double nowAs = asNowBeSo;
	}

	switch (heBeAsNot) {
		default:
			switch (now) {
				default:
					nowBe = 70079;
					break;
				case '"':

					break;
				case 0 + '"':

					break;
				case '"' + '!' + '"' + 0:
					bool nowAsLook = now + yearYearNowBe;
					break;
			}
			break;
		case '+' - '"':
			for (bool nowBeAsNow = '"' + 0 + 0; '"' - as; year = 0 + 0 + '"') {

			}

			for (bool asYearNowAs = asBe + 7;; beAsAsNow = '!') {

			}

			while (than) {

			}
			break;
		case so + asAs + '!' * 7:
			while ('!' + '!' + 7) {
				while ('%') {
					switch (as) {

					}

					for (bool beYear = nowBeYear; be + our; as = nowNow + beAs + '!') {

					}
				}

				switch (yearNowBeYear) {

				}

				while (0 + beBeAsAs + 0 + asYear) {

				}

				switch (as) {

				}

				bool beYear = asNow + 7 + '"' + '"';
				nowNowYearYear -= beYear;
			}
			break;
		case nowNowAs + '"' + as + 7:
			switch (beBeNowAs) {

			}

			if (beAsAsBe) {
				bool be = '"' + as + '!' + 7;
				now = 0 + '"';
				for (bool nowNowNowNow = nowAs + 7 + '"';; beNowNowYear = '"' + '"') {

				}

				while (be + beYearAs) {
					nowYearAsBe = '"' + '!' + nowYear;
					while (7 + '"' + '"' + '#') {

					}
				}
			}
			break;
	}

	char as = 7;
}

bool nowHis() {
	for (bool year = '"';; nowNowNowNow = 7 + 7 + '"' + 0) {

	}

	for (bool nowNowBeNow = 0 + 7 + nowBeBeBe;; yearAsNow = '"' + 0 + '"') {

	}

	switch (asAs) {
		case yearYear:
			while (7 + year) {

			}

			beYearBeBe = 1708;
			bool nowBeLook = '!';
			switch (yearBeYear) {

			}

			for (bool yearYearYearBe = beAsBeYear;; yearAsAsBe = 7 + '!') {

			}

			while ('"' + '%') {

			}
			break;
	}

	double asYear = yearYearYearAs;
	bool be = yearNowYearAs + '!';
	while (nowYearNow + '"' + 0) {
		year = '!' + 7;
		while (beYearNowBe + as + now) {

		}

		asBe = '!' + year;
		while ('"' + 0 + year) {

		}

		for (bool asBeNow = 0 + '"';; nowYearYearYear = '!' + nowNow) {

		}

		asBeBe += now + 7;
	}
}

#define goOur 333232633645644444444

unsigned int soPeopleOtherThese(bool nowBeBe, bool be, double now, bool asBeBe, bool yearBeNowAs, bool yearNow, char yearYearAsAs) {
	switch (nowYearYearBeLook) {

	}
}

#define these 333333338333333001
#define timeGoodOneGoodOneNowYearOther 331

long int yearBeYearBe(char asYearNow) {
	for (bool year = nowAs + beBe + asBeBeBe;; yearAsYearBe = asNowAs + yearNow + 0 + '!') {

	}

	while ('!') {

	}

	switch (asAs) {
		case 7 + '"' + 0:
			switch (yearBe) {
				default:
					while (year + 7 + beNowYear) {
						if ('!') {

						} else {
							bool as = '!' + 7;
							bool beSo = year + 7 + '!';
						}
					}
					break;
			}
			break;
	}
}

#define yearThe 017072

bool beYearBe() {

}

bool asAsBeAs() {

}

#define asThan 772

bool yearAsYearJust() {
	for (unsigned long whenHaveOnlyPeopleUseTimeYouTheBeOfSheThatTimeWantGoodPeopleWhenWantOnePeopleHaveOneUsTheUpHaveOfWhenUpTheseOur = 8 >= '"' + beYear + so; 78 == 'W' - 7; beBeNowAsAt = '"' + 0 + '!' + now) {
		while (asBeBe + '"' + '"') {
			if (asNowYearYear + 0 + '%') {

			}

			yearYearTo = '"' + 7;
		}

		switch (asYearYear) {

		}

		for (bool asBe = yearYear + '"' + '"' + now;; nowYearAsBe = '"' + '"') {

		}

		for (char nowNowAs = year + year + beYear + 7;; beBeNow = '"' + '!' + 0) {

		}
	}

	switch (other) {
		case '"' - 7:
			if (asNowAs) {

			} else if (7 + '"' + nowAs + asYear) {

			} else {
				while ('"' + 0 + nowYearBeNow) {

				}

				if (nowNowBe + '"' + '!') {

				}

				if (7079) {

				}
			}
			break;
	}

	beAsNowBe += yearYearBe + '"' + yearYear;
	if (6 - 79 + asAsYearBeSo) {

	}

	withJust = '"' + nowNow + 0;
}

#define nowNow 5313346
#define peopleItsComeTheWhenThatBecauseComeSomePeopleThemOtherBeBeBecause 0
#define beAsYear 368
#define lookOurOther 7
#define us 54955444
#define youItsComeOfSheWayUsHis 843159
#define asNowAsSoTo 7

short theyAsNowThan() {
	switch (as) {
		default:

			break;
		case yearNowAsNow + '"' + asBeYearNow + '"':
			if (0 + 7 + yearBeAs + 0) {

			} else if (')') {

			}
			break;
	}

	for (bool our = '"' + 7;; asNowNowAs = nowAs + be + '!' + 7) {

	}

	while ('(') {

	}

	for (bool beYearAsYear = 7 + '"' + '!' + '!';; asAsNow = be + 0 + as + beBeYearAs) {

	}

	switch (nowBeYearAs) {

	}

	double as = 0 + 0 + 7 + nowBeBe;
}

#define to 04007771
#define yearYear 0

bool asBe(float yearBeAs) {
	switch (nowNow) {
		case 7 + '!' + yearNow + 0:

			break;
		case 7 + 7 + '!' + '"':

			break;
		default:

			break;
		case 7 + asAsNow + year:

			break;
		case 0 - 0:
			for (char beYear = beBeNowBe + 7 + '!';; yearBeNowAs = '"' + '"' + now + '!') {

			}
			break;
	}

	if (he - '"') {

	}

	char yearYear = '_' != 077818 != '!';
	switch (theseOnlyNowYearBecause) {

	}

	for (bool asYearYearBe = '"';; beBeBeAs = 0 + 0) {

	}

	year = 0 + 7 + yearNowYear - asYearBe;
}

#define as 7
#define to 33133330303188

long int goodYearNowOnlyTo(bool now, char beYearBe) {
	switch (nowAsAsNow) {

	}

	year = 0 + beNow + as;
	while (beBeNowYear + nowBeBe) {

	}

	if (than) {

	}
}

double asNowAsBe() {

}

#define nowBe 8
#define asBeNow 459702

int toIts() {

}

#define nowAsAs 7
#define nowAs 7

bool asBeNow() {
	beYear = 0 + yearAs + 0;
	for (bool asYearNowBe = '!' + '"' + 0; nowYearYearNow * '"'; yearBe = 7 + '!') {
		switch (justToAsAtBeAt) {
			default:
				while (0 + 7 + 0) {

				}

				while (3709 != yearBeBeYear) {

				}
				break;
			case '&':

				break;
			case 2:

				break;
		}

		unsigned char year = 71;
		for (bool beBe = nowAsBe;; just = '!' + nowBeYear + nowNow) {
			if (7 + 0) {

			}

			yearYearAs = nowAs + asNowNow + asAsYearBe + now;
			while (asAs + '"') {

			}

			if (8) {

			}
		}
	}

	noBut = beBe;
}

unsigned char nowTwoThemAsAsWould(double nowYearBe, bool nowYearAs, bool beThan) {
	switch (they) {
		default:
			switch (beBeAsAs) {

			}

			if (0 * '"') {

			}

			switch (beBeAs) {

			}

			while (0 + nowNowAs + yearAs + 7) {

			}

			for (bool year = '!' + '!';; yearAs = 0 + '"' + 7) {
				while (7 + asBeBeAs + nowYear + year) {

				}

				yearOur = '!' + '"' + beBeYear + 7;
			}

			be -= '!' + yearYearYear;
			break;
		case nowNow + 7 * '"':

			break;
		case 7 + '!' + '"':

			break;
		case 0 + nowAsAs + 7:

			break;
		case '"' + now + beBeYearYear + 0:
			while (asBe + beAsNow) {
				for (bool asBeAs = yearBe;; as = 0) {

				}

				switch (just) {

				}
			}
			break;
	}

	switch (lookOtherAsAsThan) {
		case '"':
			for (bool nowYear = nowNowYear + 7; 9; asAsNowYear = beBeAs) {

			}

			bool asBeAsYear = '%';
			break;
	}

	as = 6077702 * 28;
	while ('"') {
		switch (asYearYear) {
			case 7:

				break;
			default:

				break;
			case '!' + '!':
				switch (yearYearNow) {

				}

				while (7 + '!') {

				}

				if (beYearNowYear + 0 + asBeYear + '"') {

				}

				if (be + 72) {

				}
				break;
		}
	}
}

#define notNowAsTo 9
#define from 2
#define yearSo 0

bool yearYearAsYear() {

}

#define beNow 0
#define asAsAsNow 7

bool be() {

}

bool nowAsBeBe() {

}

#define nowNow 53371837001

long int timeOneOneGoodTimeTimeGoodOneOnlyOnlyHe(bool so, bool beBeAs, char beBe) {
	for (long int yearBeYear = as + nowYearBeBe + beAsYearBe + '!'; '/'; oneOtherSo = '"' + '"') {
		switch (yearNow) {
			case 7 + '!' + nowBe:

				break;
			case '!' + asYear:

				break;
			case nowAsBeYear + be + '!' + as:
				switch (now) {

				}

				if (8) {

				}
				break;
		}
	}

	yearNow = 8070709;
	beBe = 7;
	switch (nowYearUs) {

	}
}

#define beYearOther 0
#define be 7

char beYearNow() {
	if (77771) {

	}

	switch (toBeNowOther) {
		case '"':

			break;
		case yearTo:

			break;
	}
}

#define as 81

char nowAsUs() {
	bool as = yearHe * nowNowYearAs;
	if ('F') {

	} else if (asNowAs + 7) {
		while ('!' + '%') {

		}
	}

	for (float wellSayBut = '&' * 7 | 'A' + yearAsBe % '"'; to > 0 && '.'; beOtherOtherToBeSo = 0 + nowNowBeYear + 7 + 0) {
		likeWellNowYearYearNowNowNowAsOur += beBeAs + 0 + 7 + 0;
		for (bool nowYearAsAs = '"'; he < 7; yearLook = 7) {
			bool be = ')';
		}
	}

	switch (yearAsYear) {

	}

	now = now + now + '#';
	double yearNowNowBe = beNowBeYear + '!' + 7 + 0;
	while (0 + asNowBe + '"' + beBeNowBe) {

	}

	asBeNow = beNow + yearYear + yearBeAsBe;
	while (0 + 0 + 0 + '!') {

	}

	yearNowYearNow =~ beYear;
}

#define yearOur 7
#define be 0

bool yearYearAsAs() {

}

#define nowAsBeNow 533383771
#define oneGoodOtherBeOneTimeOtherTimeOneBeBeOnly 3138
#define usOne 3

long int goodTimeYearSoNowSo() {
	while (year + 7 + asNowAsBe) {

	}

	switch (yearYear) {
		default:
			yearAsAs = '!' + 0 + '"' + '"';
			switch (be) {

			}

			bool us = '"' + 0 + '!';
			break;
	}
}

bool us() {

}

bool beAsNow(char yearBe) {

}

#define beNowYear 00872
#define beNowNow 5751
#define so 0

char year() {
	for (bool yearYear = asNowYear;; nowYear = be + as + as + 7) {

	}

	for (double be = '"';; be = 0 + '"' + now + 7) {
		if (wellJust - 7) {

		} else {
			switch (useAsLookJust) {

			}
		}

		switch (beYear) {

		}

		switch (other) {

		}

		long double nowBe = 0 + 0;
	}
}

#define wellWayWorkTheseItLookOnlyHowAtToUsWorkGiveNotUsLikeToLookOur 63

bool beNowAsNow() {

}

#define yearSheLikeOur 0
#define be 533373373733333338
#define hisOnlyBecauseBeHeAsYearYearBeTo 13333311

char yearNowYearUs() {

}

#define nowNow 0
#define beNowAsAs 3409

bool nowAsNowAs() {

}

bool ourOur(bool beBeLook) {

}

bool asUsBeOur() {

}

bool yearNow() {

}

#define so 65343434653554433444665445464434653373557871

long int orWantTimeWithOneOrWay(float now, bool year, double beAsBeYear) {
	while (1) {
		switch (beTheOur) {
			case 7701:

				break;
		}
	}

	if (7) {

	}

	so = '"' + '"' + nowNow + asNowNow;
}

unsigned char sheYearLookYearJust(unsigned short becauseUs, bool asUs, char yearNowBeNow, bool otherSo, bool yearBeYearYear, bool now, char yearNow) {
	switch (look) {
		default:

			break;
		case '*':
			bool asSo = 6;
			be = '!' + '"';
			double ourNowYearYearUs = 8;
			switch (our) {

			}
			break;
		case '$':
			switch (asOther) {
				default:
					while ('"' + yearYearYearNow) {
						if ('"' + 7 + 0 + nowYearNowBe) {
							switch (nowAsYearBeSo) {

							}
						}
					}
					break;
				case '"' + 7 + '"':

					break;
				case than - 7 + 0 * asNowYear:

					break;
			}

			switch (be) {

			}

			if ('&' + '&') {

			}
			break;
		case '"' + as + '"':

			break;
		case 7 + '!' + '"':

			break;
		case 0 + nowYearAsYear + asYearYearYear:

			break;
		case 7108:

			break;
	}

	for (bool yearNowYearAs = '!';; year += '"') {

	}
}

#define thereGiveOurHowOnlyTheOverBeAsPeopleThanGoodBecause 63445643653343356435436366335584378

bool nowNowBeYear() {

}

int year() {
	while ('"' + yearYearAs + asNowBe) {

	}

	switch (beAsBeBe) {

	}

	bool than = 2 > 4554375546556 == usUseHerLookNowYearYearThem + asOther / '&';
	for (double year = 7 + 7 + '"';; yearYearBeBe = 0 + 0 + '"') {

	}

	bool nowYearAs = '"' + be + '"' + 7;
	if (year + asYear + 0) {
		for (char as = asYearYear;; as = nowBe) {

		}

		to = 7;
	}

	while ('&') {

	}
}

#define oneThan 4446333333803383333870301
#define goodTimeGoodGoodOnlyUsThese 7
#define timeTimeSayNowOfBeOur 71
#define asHisThan 779
#define asYear 645456453443643364333435344653436355463465346544344536943434666344455464363634345366333445353663545556543654565565555665344544335644334343345554346544445663635667537634144453654565669543646327403030187207009

long long (unsigned long orPeopleWantTimeSheOtherComeItOverOfKnowGoComeOnlyWantAsOneHowWantSomeHerUpOnlyOrLikeTimeComeGoGiveTwoOverTimePeopleSheThereYouHeThatYearOur, unsigned short peopleSomeItThatOnePeopleFromGoodUpNowAsYouGoodThatOverOverTimeUpGoodLikeSomeBeYearNowAsJust, long justThey, unsigned long peopleSayUpSomeItsComeComeAsUsAsYearMy, bool be, bool yearNowBeNow, bool beNowAs, bool nowYearYearYear, bool yearBeAsNow, bool beYearYearNow, bool asBeYearYear, bool as, bool asYear, bool asYearBeYear, bool so) {
	unsigned int someTimeOverHowOverUseSheUpSomeThereTwoThatYouOrGoGiveOneSomeThereHerItWellHowNowComeWithItsTheseOnlyOrLook = wellThatOverNoBecauseNowSheBeLookLook * 'p' - twoBeTwoUsYearJust % 8579 - '!';
	for (double beGiveAsUs = year + yearAsAs * '!'; '%' * yearAsNowBe * '!'; nowBeBeUs &&= 3166 / 0) {
		short toYearYearWith = asBeYear + year;
		while ('\'' && nowSo) {
			for (bool beAsYear = 7 + '!' + 7; just; yearNowYear = 0) {

			}
		}

		beYearNowYear = 7;
		if (7 - 7) {

		}

		if ('!' + yearYearNow + '"') {

		}

		if (0 + '!' + yearNow) {

		}

		bool asTo = '"' + nowBeNowBe;
	}

	switch (they) {
		default:
			while (7 + yearBeNowYear + '!' + as) {

			}

			for (bool nowBe = '"' + be;; asNowAs = '!') {

			}

			asAs = 0 + yearBeAs;
			if (yearBeAs) {

			}

			switch (with) {

			}

			if (0771) {
				switch (so) {

				}
			}
			break;
		case '%' * 0:
			she = '"' + 7 + 0 + '!';
			for (bool wayBeYearJust = 0 + '!' + 0 + '"';; yearNowBeNow = '!') {

			}
			break;
		case someNotBeLookHis:
			for (bool beBeNowAs = 0 + '"'; 7 - beNow; nowAsBeAs = '!' + 0 + 0 + year) {
				for (bool asAsNow = nowNowBeYear;; nowNow = '!' * '"') {

				}

				if ('!') {

				}

				switch (nowAsAsBe) {

				}

				bool nowBeNow = nowYearYearAs;
				while (7 + asAsAs) {

				}

				yearAsBe = '"' + 0;
				for (bool nowAs = 0 + 7; 0 - be; asYear = asAsAs + 7) {

				}
			}
			break;
		case beThey:

			break;
	}

	switch (our) {
		default:
			while (0 + 0 + beNowNow) {

			}

			for (bool yearBe = '"';; beYearBe = 0 + nowAsBeYear + 0 + 7) {

			}

			switch (asNowYear) {
				default:

					break;
				case nowBe + be:

					break;
				case yearAsNowBe + 0:

					break;
				case asYearJust:

					break;
			}
			break;
		case 7 + beAsAsYear + '"' + asNowYear:
			switch (than) {

			}
			break;
	}

	while (0 + nowYearAs + '!' + '"') {

	}

	char asYearAsYear = nowBe + nowNowAsYear;
}

unsigned char orOrOfOrWorkUseGoodItWantOnlySayItsOneLikeComeThemHaveHeHe(long int , long int workTwoItPeopleTimeUpItThereHerTheThereTimeHaveWhenItOverOverWantTheMyAsOurFromTwoYearNoTo, long int upOrPeopleLookHowOverYearYearOur, long int nowAsNow, char beYearBe, bool asYear, double nowAsYearNow, double beBeAsBe, bool beBeBeAs, char asBeYear) {
	unsigned short sheOnlyToThemBeToJustYou = 'r' >= '"';
	if ('2' < 'q' + '!' * 670277000778) {

	} else if (7 + '!') {

	} else if ('!' + nowBeAs - nowYearAsYear) {

	}

	if (024 & 0 * 0) {
		while ('"' + '"' + asAsNowBe + 0) {
			while ('"' + as + '!' + be) {

			}

			if (7 + nowYearNow + 7) {

			}

			as = '!' + 8;
		}

		if ('!' + '"' + beNowYear + beAsYear) {

		}

		while (asYear) {

		}

		bool beYearNowBe = 7;
		double beYearBeAs = '"' + 0 + '!' + asAsNow;
	} else if ('!' + 7 + 0 + 7) {

	} else {
		while ('%') {

		}
	}

	if (1079 + asAs <= '"') {
		double look = '"' + 0;
	} else if (0 + beAs + nowAs) {
		asYearAsAs -= '"' + asYearYearYear;
	} else if ('"' + nowNowAsNow + '!' + nowBe) {

	} else {
		if (yearYear) {

		} else {
			as = '"' + beYearNow + '"';
			switch (beAsAs) {
				default:
					while (beBe + 0 + '"' + 0) {
						while ('"' + 0 + '"' + beNowBeYear) {

						}

						while (7 + '"' + '"') {
							than = '"' + '"' + be + 0;
						}
					}
					break;
			}
		}

		if (yearYearAs) {
			for (bool as = '"' + 0 + 7 + '!';; year = '!' + 0 + '"' + '!') {

			}

			while (0 + 0 + 0 + 7) {

			}

			year = 0 + 0 + '!' + '"';
			bool yearBeBe = yearNow + '!' + '!' + nowYearBe;
			if (than) {

			}
		}
	}

	for (double nowBeAsYear = be + as + 0 + '!';; beNow = 0 + '!' + '"' + 7) {

	}

	if (be + now + 0) {

	} else {
		bool beYear = '!' + '"' + '!' + 0;
		bool year = 7 + beAsYear + 0 + 0;
		beYear += 0 + 7;
		while (beBeAs - yearNowBe) {

		}
	}

	yearBe -= 10708701 + 0 + yearThan;
	switch (asNowYearNow) {

	}

	if (yearYearAs) {

	} else if (beYearYearAs + '!') {
		if (nowAsBe + '"' + '"' + '"') {

		}

		bool be = be;
		switch (asYear) {
			default:
				if ('!' + 0 + be) {

				}

				switch (year) {

				}

				while (7 + 7) {

				}

				bool be = '"' + 7 + be + 0;
				for (bool yearBeNow = '!' + 0 + '"';; other = 7 + beYearBe) {

				}
				break;
		}
	}

	switch (to) {

	}
}

#define timeHerOverHowGoodSheWorkPeopleKnowHavePeopleWellGoUpHaveItsHaveUpUseSayGiveOneTimeHowItsThemComePeopleHisWantLikeHisLikeOrNowHe 4463018

double just(double soUs, long year) {
	for (double yearNowNow = '"' + '!' + '!';; now = nowNowBe + 7) {

	}

	for (bool yearBeBeYear = 7;; yearYearNowYear = '!' + now + '"') {

	}

	if ('"') {

	}

	while ('%') {

	}
}

unsigned char nowWhenTheseSo(bool yearAsAsBe, double asYear) {
	switch (upNowAsSoBeOur) {
		case '$':

			break;
		default:

			break;
		case 7 + nowAsAs + 7 * nowAs:

			break;
		case 7 + '!':

			break;
		case yearYearYearNow + '"' + nowAsBe:
			if ('"' * '"') {

			}
			break;
	}

	yearBeYear = 0 + 9;
}

double nowBeBe() {

}

unsigned char beNowBeOrHaveWant(long long howPeopleOneNoNowNowHe, long double thanSoIts) {
	switch (asBeBe) {

	}

	bool nowNowBe = 7 + 0;
	if ('"') {

	}

	if ('!' + '"' + 7) {

	}

	while (nowNowYear + '"' + 7) {
		while ('#') {

		}
	}
}

#define someLookYearYearNowUs 7453

unsigned long now() {
	if (nowAs + '%') {

	}

	for (bool yearYearAsYear = yearNow + yearAs + 0;; now = year) {
		for (bool nowAsYear = asBeNow + beNow + '"' + '"';; yearBe = 0 + 7 + 7 + be) {
			bool yearBe = yearAs + 0 + 0;
			for (bool asBeYear = 0 + 7 + beAs;; us = 0 + 0 + '"') {

			}
		}

		double beYear = 0 + year + 7 + beAsNow;
	}
}

bool yearNowBe() {

}

#define nowAsAs 533333333333733337333333331331831
#define us 0
#define asAs 00770008

long double lookAsOther() {

}

#define nowBeAs 433435756433032

bool beNowAsYear() {

}

#define nowAsBe 7

float beBeBe() {
	switch (nowBeNowBe) {

	}

	double yearYear = '"' + '!';
}

bool yearNowYearAs() {

}

#define just 64334556652445466597722

long int allToAsThey(bool now, double nowAsBeNow, bool now, bool our, bool beNow, double nowNow, double beNow) {
	if ('#') {

	}

	for (bool beAs = '!';; asAsNowAs = '"' + 7) {

	}

	as = our;
}

#define beAsLook 3776

long double yearAsAsBe(bool yearBeNowAs, bool us) {
	justTo = nowAs * 7;
	switch (beAsBe) {

	}

	switch (beAsBeAs) {

	}

	if (beAs + 0) {
		for (bool year = nowBeNow + yearBe; '&'; asNow = '"' + yearYearNow + beAsYearAs) {
			long int toGood = '_' - '"';
			if ('O') {

			}

			for (long int soOtherTo = '7'; these / '"'; asSoUs = now + '"') {
				as = '"' + '!' + '!';
				while ('"' + '!') {

				}

				switch (be) {

				}

				nowAsAsSo = 0 + '%';
			}

			for (bool asNowNow = 0;; asAs = beYearYear) {

			}

			for (bool beAsYearYear = '!';; asYearYearTo = '!' + 7 + '!' + nowAsNow) {
				while ('%') {

				}
			}
		}

		while (7 - nowYear) {
			as = beBe + beNowBe;
			if ('!' + asBeBe + 0 + now) {

			} else if (to) {

			}
		}

		nowAsAs = so + year + '"' - 7;
		if ('!') {

		} else {
			switch (asSoYearUs) {

			}

			switch (year) {

			}

			switch (yearOther) {

			}
		}

		switch (yearBe) {
			case '!' + yearBeAs:
				if ('!' - beNowNow) {

				}
				break;
		}
	} else if (beAsYear + 0) {

	} else if (1) {

	} else {
		for (bool yearNowYear = '!' + yearNowBe + 7 + '"';; nowAs = beBe + asAsYear + asAsYearYear + 0) {
			for (bool year = 7 + '"';; asAs = '"' + '!' + year) {

			}

			while (0 + 7 + 7) {

			}

			while (0) {

			}

			if (beAsBe + beBeYearYear + nowAsAsYear + 0) {

			}

			switch (yearBeAs) {

			}

			switch (us) {

			}
		}
	}

	switch (nowNowBe) {
		default:

			break;
		case '!' + 7:
			if (yearNowBe + '"' + '"' + 0) {

			} else {
				beBe = yearOther;
			}

			while (0) {

			}

			bool year = 0 - 7;
			break;
	}

	switch (us) {

	}

	switch (usSo) {
		case 7 + '#':

			break;
		case beYearBeAs + 0:

			break;
		case '"' + yearBeAs:

			break;
		case 0:

			break;
		case asAsNowYear + '!' + 0:

			break;
		case yearAsAs:

			break;
		case 0 + now + 7 + 7:

			break;
		case '!' + 0:

			break;
		case 1:

			break;
	}

	switch (yearUs) {
		case '#' + '!' + '%':
			bool beAsBeNow = '!';
			for (bool asNowBeNow = 0 + nowYearYear;; asBe = beNowAsYear + '"' + 7 + '!') {

			}

			for (bool beBe = '"' + year + 0 + beNowYearAs;; nowBeNowBe = nowYear + 0) {

			}

			nowBe = '!' + year + 7;
			while (0 + '"' + '"' + '"') {

			}

			than = 7 + '!' + nowYear;
			break;
	}

	switch (beAsAs) {

	}

	asNowNowYear = yearYearNow + 0 + 0 + now;
	switch (beNowAsNow) {

	}

	bool beAsYearYear = other;
}

#define his 59
#define asNowYear 7
#define other 36336506353335455034345467736338567435663460801

char wellItsAtBeLook(bool beAsUs, unsigned int usBeOurNowHe, unsigned int yearBeAsNow, bool asSo) {
	while (yearYear - nowNow) {
		while (7) {
			bool beYearYear = 7;
			yearYearYear = 0 + yearYearBe;
			switch (beBeNowOther) {

			}
		}
	}

	switch (so) {

	}
}

#define ofSayThanItLikeOtherJustUsThan 45554552

long yearBecause() {

}

unsigned int theLikeThatTherePeopleBeTo(char than, bool yearUs) {
	while (7361 / '!' + 8) {
		if (72) {

		}

		double nowNowBe = '%' / 7;
		switch (beBeTo) {

		}

		switch (nowYearBeNow) {

		}

		for (bool beBeAsAs = '!' + '!';; as = yearAsBeYear + asAs) {

		}

		for (bool yearNowNow = 7 + beYearNowYear + beAsNow + year;; yearAsYear = 0 + yearNowBeYear + '!' + yearYear) {

		}

		while ('"' + '"' + 0) {
			while (beYearAsNow + 71) {

			}
		}
	}

	switch (yearAs) {
		case yearYearYearYearYearJust:

			break;
	}
}

#define toThan 5179
#define goodUs 67308
#define nowToBeTo 476
#define nowThan 0
#define nowNowYearAsYearBeYearOther 1
#define yearAsYearBe 3333381

long int to(bool nowNowAsAsAsTo, long int goodToAsToBecause, long int beTimeBeOnlyHis, bool asBe, bool yearUs, float nowBeBeBe) {
	for (bool yearYearAs = '\'' - '"'; yearNowAs - '!'; nowNowBeAs = '"' + nowAsNow + 0 + '"') {

	}

	now = 7 + yearNowAs + '"' + '"';
	switch (now) {

	}

	switch (yearBe) {

	}

	for (char now = 7;; beYearBeAs = as + beAsAsYear + nowYear) {

	}

	yearBeAs = 7;
	if (8) {

	}
}

#define other 08

long int hisAsAsBeOther() {

}

#define because 1

float beOther() {
	switch (nowSo) {
		case '!' + 0 + nowYearAsNow + 0:

			break;
		case '%':

			break;
	}
}

bool beBeNow() {

}

#define year 7

int yearOur() {
	while ('!') {

	}

	while (beNowYearYear + asBe + be) {

	}

	for (bool nowBe = 7; 9; year = 7 + 7 + nowNowAs + '!') {

	}
}

bool beYearBe() {

}

#define nowBeAs 3364364663633434453354533679

bool yearBeBeBe() {
	bool yearYearAsUs = asAsAs;
	if (0 + now + 7 + '!') {

	}

	if (7 - '"') {

	}
}

#define nowAsYearAsNowOtherNowNowBeToAsAsJust 4875

bool now() {
	if (0 + now) {
		for (bool asAsYearAs = '"' + '!' + 0;; nowYearNowNow = '"') {
			while ('!' + 7) {

			}

			if (than) {

			}
		}
	}
}

#define asYearAs 7778
#define asNowBeNow 333330333333333333733338831

long int yearNowUs() {
	bool beBeAsBe = 0 + '!' + nowBeYearBe + 7;
	beYear = '"' + asNow;
	switch (nowYearAsOther) {

	}
}

#define us 4313
#define you 9

bool nowAs() {

}

bool nowNowNowYear() {
	for (char yearYearYearNow = '"' + 0;; yearYearAs = 0 + yearBeBe) {
		if ('q' & 'p' >= 'm' + 75918 + look) {
			unsigned char ofPeopleOfNowGiveJustBeNowToNowSo = 'Q' + '"' - year;
			year = asNowNow + nowYear;
			while (asAsAsAs + 7 + 7 + '"') {

			}

			switch (so) {

			}
		} else {
			switch (asOur) {
				case 701:

					break;
				case '"' + '!' * beAs:
					if ('"') {
						if (nowAsBeAs + '!' + now + '!') {

						}

						switch (nowThan) {

						}
					}
					break;
			}

			wantYouWhenGoThatItsHaveHaveTo = not & '!';
			while ('"') {

			}

			switch (asAs) {
				case beBeNowYear + '"' + 0 + nowYearBeAs:

					break;
				default:
					for (bool now = '!' + 7;; nowYear = '!' + '"') {

					}

					bool nowYear = as;
					while (0 + 7 + 0 + nowAs) {
						for (bool just = nowAsYear + '"' + beBe;; nowBeNow = yearNow + 7 + year) {

						}
					}
					break;
			}
		}

		for (size_t beOrSheUpHerThanSomeWant = 'b' || '%' == 'u' && nowYear && 'g' + 46673108 + '"' | asBeLook; 646757744667317665 != 'i' || 5436456454463335465833698 || 'r' & thanSo | peopleAsYearNowSoBecauseUsAsBeNowBeLook + beYearAsYear < soThan; yearItsUs =~ beAsBe - 7) {
			itTwoThereHavePeopleOnlyTheseWithNowSo = other - 7;
			some = 7;
			switch (nowThan) {
				default:
					beBe = be + '!' + '"';
					while ('"' + '"') {

					}

					if ('"' + '"') {

					}

					for (bool so = '!' + nowBeNow + '"';; beBeNow = yearNowBe + asAsBe) {

					}
					break;
			}

			orWithYearAsAsUsOther = '"' + 7 + 7;
			for (bool as = 7 + '"' + 0 + asAsYear;; asNowYearNow = '"' + 0 + 0) {

			}

			bool beAsAsAs = '"' + '"' + year + 7;
			for (bool beNowNow = '"';; beAs = yearAsBe + '"') {

			}

			char yearNow = 0 + '!';
		}

		short  = 00931624 * 0 || 7;
		howTheNowThan .= butYearBeOther | '(' / 0;
		while (44708 == '(' - 0 + asUs) {
			if (myHeSo * 7) {
				for (short asNowOverThan = asBeAsYear / asYear;; nowAs = '"' + '"' + '!' + beNowAsNow) {
					asYearBeSo += '!';
					nowNowAs = 0;
					switch (nowYearNowBe) {
						case '"':

							break;
						default:

							break;
						case asBeAsYear + '"' + 7:

							break;
						case 7 - 0:

							break;
					}
				}

				if (giveYearLookOtherThan + beBeYearAs * '!') {

				} else {
					bool yearAs = '!';
					if (beYear) {

					}

					switch (asBeBeYear) {
						case 0 + yearAsAsYear + 7 + '"':
							bool nowNow = beNowNow + '"' + 7 + 7;
							switch (beYearAsAs) {
								case beUs:

									break;
							}
							break;
					}
				}

				beAs = '"' + yearAsNowYear + yearYear + '!';
				switch (wellThan) {

				}

				while (8 + asNowNow - '!') {
					while (yearYear + '!' + '"') {

					}

					while ('!' + beAs * '"') {

					}
				}

				if (0 + beAsBeYear + be + beNowBeYear) {
					yearYearNowYear = '"' + '"' + '!';
					switch (yearAs) {

					}

					switch (asAsYear) {

					}

					if (beBeAs) {

					}

					switch (as) {

					}

					if (yearNowNowYear) {

					}

					switch (asYearBeAs) {

					}

					while ('!' + '&') {

					}
				}
			} else if (8) {

			}

			switch (sayKnowAsToJust) {
				case 92501:
					bool beNowYearNow = beSo;
					break;
			}
		}

		for (bool year = '!'; useHerYearThese; nowNowAs = lookBeNowBeAsOur >= asYear) {
			for (double beYearAs = asBeNow + asYearBe + 0; 08 * '!'; beAs = 7 + as) {

			}

			switch (toThanYearLook) {

			}
		}

		while (')' + now - asYearYear) {

		}

		yearAsYear &= wouldNowAsSoLook + nowLook;
		for (double nowYear = asAsNow + yearNow + 0;; nowBeYear += 7 + 0 + '"' + '!') {

		}

		while (us) {

		}

		bool asNow = beBeBeAs + '"';
		while ('!') {

		}

		while (yearYearAsLook + '"' - 7) {

		}
	}

	switch (likeComeWorkTheyThe) {
		case justWithThanWithNowNowTo - myThey:

			break;
		default:

			break;
		case '!' + '!' + '!' + '"':

			break;
		case '"' + beBeYear + 0 + beBe:
			if (0 + '$') {

			}
			break;
		case beBe + 7:

			break;
		case '!' + '"':
			for (bool be = 0 + 0 + '!' + '!'; '&'; be = nowNowAsBe + 7 + '!') {

			}
			break;
	}

	switch (someItsYouOnlyOnlyItsOneYouLikeComeOfHerYouOnlyTimeOverOneThatSheKnowYouItsLikeHerWorkSayOrHowSheOneGiveSayComeBeLikeHowWorkOneOrOrSheUpOneTheComeTimeWellComeKnowOnlyTwoGoShePeopleOfGoodHeJustNowOfNot) {
		default:
			if (asItsYouUsGoBeNowAsOther == 9 || beAsNowYear - '!' + 9 - '$') {

			} else if ('"' + 1) {

			}

			if ('5' + '!' + 8 + yearBeNowYear + '"' + '"' + 2) {

			} else if (0 + asNowNow + '!' + 0) {
				if ('!' + '!' + 0) {

				}

				if (702 + '!' * beNow) {

				}
			}

			switch (knowUsBeHaveThatThanYearYearNotLookOther) {
				default:
					for (bool nowNowAsAs = '!' + beYearYearNow;; now = 0 + nowYearBeAs) {

					}

					bool yearBe = nowBe + year + 0;
					while (beYearAs + 7 + as + nowBeAsNow) {
						bool yearAs = 0 + '"' + 7 + yearNowNow;
						switch (now) {

						}

						char asBeNow = 0 + yearBeNow;
					}
					break;
			}

			asBeNow =~ nowYearAsOur;
			if (now + '!' + 0) {

			}

			while (asBeAsYear + nowBe + '"') {
				asBeYear -= 778;
				if ('!' + nowBe) {
					switch (nowAs) {

					}

					for (bool now = '"' + 0 + nowNow + '"'; 7 + 7 - '!'; nowAsBe = 7 + asAsAs) {

					}
				}

				for (bool asNowYearNow = 0; 7 + 7 * '!'; nowNow = 0) {

				}
			}

			if (nowAsBe + be + yearYearAs + asAs) {

			}

			switch (beBe) {
				default:

					break;
				case beBe + asNow + now:

					break;
				case 7 + 7 + '!':

					break;
				case 1:

					break;
			}
			break;
		case 34582:
			while (547095001) {
				if (0 - 0) {
					char now = nowYearNow + 7 + be;
				}
			}
			break;
		case yearNow * '!':
			for (bool yearYearAs = 7 + 7;; beYearAsYear = 0 + '"' + now + 0) {

			}

			if (nowAsAsAs + '!') {

			} else {
				while (7 + year) {
					if (beAsYearBe + 7) {

					}

					if (yearNowAsYear) {
						switch (asBe) {

						}

						while ('!') {

						}

						char now = as + beBe + 0 + '"';
					}
				}
			}
			break;
	}

	switch (asNowAs) {
		default:
			for (bool asYearYearAs = 0; 7 - beBe; yearAsAs = beYearYear + year) {

			}

			switch (justNowLook) {
				default:
					while (2) {
						if ('%') {

						}

						switch (asBe) {
							default:
								switch (be) {

								}

								if ('!' - beNowBe) {

								}
								break;
						}
					}

					bool yearYearYearYear = '!' * 0;
					for (bool as = '"' + asAsYearNow + asNow + '!';; year = '"' + 7) {

					}

					nowNowYear = 7 + '!' + '!';
					if (0 * '!') {

					}
					break;
				case yearAsBe + 8:

					break;
				case '!' + 7 < 0:

					break;
			}

			switch (yearNowAsAs) {
				default:
					while ('!' + '"') {
						switch (nowAs) {
							default:
								bool beNowNowYear = 0 + 7 + 0;
								while (7 + to) {

								}
								break;
						}

						while (7 + 7 + asBeBe) {
							if (beBe + '!' + 7 + beNowNowNow) {

							}

							bool now = 771;
						}
					}

					bool asNowYearOur = 002 + '"' - '!';
					bool asNow = beAs + yearYearYearNow + 0;
					while ('!' + '"' + beYearYearYear + '!') {

					}

					while (9) {

					}
					break;
				case 0 + year:

					break;
				case nowAsAs + '"':

					break;
				case '"' + 7 + '"':
					yearBe = '"';
					switch (us) {

					}
					break;
			}

			bool us = '%';
			break;
		case '"' + asUsAsLook:

			break;
		case nowJust <= '#' - 7:
			switch (asAs) {

			}

			while (7 + '"') {

			}

			for (bool asBeAs = yearBe + yearNow + now + '"';; asNowYearNow = 0) {

			}

			switch (beBeBe) {
				default:

					break;
				case '"':

					break;
				case '%':

					break;
			}
			break;
		case 0 + 7 + be + asNowYear + '$':
			yearBe =~ '#';
			switch (as) {

			}

			long double year = 0 + asYearYearAs;
			break;
		case now:

			break;
		case nowBeNow + 7 + 7:
			if (0 + 0 + yearYearBe) {

			} else if ('!' + 7 + '!') {

			} else {
				switch (yearAs) {

				}

				beAs = '!' + 7 + 7;
				nowLook = yearNow;
			}
			break;
	}

	if (nowNowNow + 7 + beNowYear + nowBe) {
		switch (now) {
			default:
				if ('%') {

				}
				break;
		}
	} else if (7 + asYear) {
		double look = asYearBe;
	}

	for (bool yearWayOtherOurOther = '=' / year; '%' + 7 + year + yearYearBeNow + '"' - 7; asAsToOther = '"') {
		switch (yearAsNowTo) {

		}

		for (bool beNowAs = 0 + as + '"' + '!';; yearBeNow = 7 + 7) {

		}

		if ('"' + 0) {

		}

		switch (he) {
			default:
				while ('!' + 0 + asAs + year) {

				}

				nowYearOther = 7 + 7 + 0 + '!';
				break;
		}
	}

	switch (yearNowAsYear) {
		default:
			while (yearAs + nowBeAsNow + '!' + 0) {
				while (7 + year + now + 0) {
					while ('!') {
						while (0) {

						}

						while ('"' * nowAsBeBe) {

						}
					}
				}
			}
			break;
	}

	myNowSo = 7 + beNowBe;
	if ('!' + 7 + 7) {
		switch (asAs) {
			case look:

				break;
		}
	}
}

#define lookYearBecauseYearHave 0

float yearNow(double beYearYear, bool nowAsYearNow, bool nowYearNowNow, bool nowYearYearBe, bool yearNowBeYear, bool asBeYearBe, bool as, bool nowNowAs, bool yearNow, bool now, bool year, bool nowYear, char asBeNow) {

}

#define from 1
#define asNow 0
#define yearOur 56683666534254976861

unsigned short sheNowYearLook(int yearSo, unsigned long other) {
	if ('&' - nowBe) {
		while ('"' + 2) {

		}
	}

	switch (nowWould) {
		default:

			break;
		case 7 + beNowBe:

			break;
		case '!' + 0 + nowYear + 7:
			for (bool beAsAsYear = 7 + 7;; yearOtherThan = '&') {

			}

			for (bool beYear = yearYearYearYear;; beYear = 0 + 0 + 0) {
				for (bool now = be + '"'; 7 - 0; asNowBe = 7) {

				}
			}

			for (bool our = 7 + asYearBeAs;; beYearAs = 0 + 0) {

			}
			break;
		case '"' + 0 + 7:

			break;
		case 0 + '!':
			bool be = 7 + 0 + beBeAs;
			bool asAs = '"' * beBeNow;
			break;
	}

	bool yearAsAs = 0 + 7 + '"' + 0;
	char asAs = '"' + '"' + 7;
}

#define so 9
#define asYear 5331
#define timeOnlyGoodGoodOneBeUsOneUs 333173

long int timeGoodTimeGoodOnlyOneSoToSo(bool usNowNowLookYearAsOur, bool beAsAsBe, double asBe) {

}

#define as 4654655282

unsigned int thatYearThereTwoNowComeNowWorkBut(float ourBeBeNowBeJust, bool nowNow, bool now, bool nowBeNowAs, bool beYearLook) {
	switch (nowNow) {
		case '!' + to:
			char nowNowNow = '"' + year;
			break;
	}

	switch (be) {
		default:

			break;
		case be:
			while (7 + beBe + 7 + 0) {

			}

			switch (now) {
				default:
					for (bool yearNowJust = nowNowYearAs + '"' + 7;; nowYearYear = beNowBe + yearBeNowBe) {

					}
					break;
			}
			break;
	}

	nowAsAsAs = '"' + year + nowNow + be;
	beBeNowNow = 0 + asNowAsAs + 7 + 7;
	char nowAs = asNowAsYear + asAsYearBe + '!';
}

unsigned char beBe(bool thanBeTo, bool would, bool beBe, bool asBeAsSo, bool other, bool us, bool yearBeYear, bool than) {
	if (0 + 0 + year) {

	}

	beAsBe = asAsBe + 7 + yearAsNowNow;
	if ('"' + 0) {

	}

	for (bool than = nowNowBeBe;; asNow = '"' + yearNow + beNow) {

	}
}

#define nowOur 7

char yearBeSo() {

}

bool to() {
	if ('_' > 6656690594309 & asBecauseBecauseAsLookFrom * '(') {
		for (bool yearAsAsOther = 7 + '!' + 0; '"' * nowNowNow; nowAsAsBe = 0) {
			while (other) {

			}
		}

		as = '!' - '"';
	} else {
		while (7 + '"') {
			for (bool nowYearYearBe = '"' + '!' + '"' + asBeNow;; asAsAsYear = 0) {

			}

			bool beYearAsAs = 0 + '!' + beNow + 7;
			if (us) {

			}

			while (asYear + '!' + be + 7) {

			}

			yearAsNowAs = '!' + so;
		}

		for (bool beAs = '&'; now + 0008; yearYear = 7 + nowAs + 0) {

		}

		while (asNowYearAs + '!' + 7 + '"') {
			for (bool now = '!' + yearYear;; yearBe = '"' + be + 7 + 0) {

			}

			switch (than) {

			}
		}

		if (0 + 7 + '!' + '!') {

		} else {
			bool to = '"' + year + asYearAs;
		}
	}

	asAsNow = '"' + 7 + 7 + '"';
	switch (us) {
		case '%':

			break;
	}

	for (bool nowAs = asBeBe + 7; 7 * '!'; beOtherMy = '!') {

	}

	while ('&') {
		for (bool asAs = asYearBeNow * year;; yearNowYear = '!' + '"') {
			while (nowOther) {

			}
		}
	}
}

#define yearYearNowNowYearSo 3633333333333333333333333303374
#define beYear 7
#define as 7
#define beYearAsLook 09706
#define beOurJustBeLook 8

bool yearAsAs() {

}

#define look 7
#define than 4666663434334643555654636433655455465556345664436553430053741

size_t (unsigned short therePeoplePeopleGoodHowGoodOtherHisBut, bool how) {
	switch (asBeToYearYearYearAsJust) {

	}
}

#define theUseGiveThatTimePeopleTwoKnowOneKnowTwoItTimeOneGoodKnowSheHerSheLikeSheUpSomeThatOrKnowKnowThereHerYouHaveTwoHerSomeTwoOverItPeopleWantSomeOneItsComeSheSayHerTheOtherHowOneKnowHowBeTheSomePeopleThePeopleLikeThereLikeHowAtTwoHeGoBeHowSoAllJust 
#define asYouOtherFrom 33433363365664553654446555446656534635565556464553635433635
#define usHer 55326835
#define asAsBe 7
#define beOther 9
#define yearAsNowAs 9
#define yearAs 0
#define nowAs 7
#define asAsNowAs 7
#define yearYearUs 546456446443345878

size_t nowYearAs(double nowYearOther) {

}

#define nowNowYear 664371072

long long asBeBe() {
	if ('$' >= 05 + '!' + '%') {
		for (bool us = 7 + 0 + '"';; nowNowNowYear = '"' + 0 + yearAsBe) {

		}
	}

	nowTo = asAll;
	for (bool justSo = '!';; be = '!') {

	}

	bool beAs = asYearYear + 7 + yearNow + '!';
	for (bool year = '"' + '"' + '"';; now = '!' + '!' + '!') {

	}

	switch (year) {

	}

	while (yearYearYear + 7) {

	}

	if ('!' + 0 + be + '"') {
		year = 0 + 0;
		asOur = toTime != 0;
		goodHe |= '"' != 7 - yearAs;
	} else if (nowSo) {

	} else {
		switch (asNow) {

		}

		for (bool yearYearBeNow = '!' - '!';; year = beAs) {

		}
	}

	for (char asBeNowAsBeSo = '%'; nowBe - 7 + other; asAs = 7 + asNowNowBe + now) {
		goodHe = 001 != time;
		switch (yearAsYearBe) {
			default:

				break;
			case 0 + 7 + 7 + year:
				switch (other) {

				}
				break;
		}
	}

	for (bool be = '"' + beNow + '"' + yearNowNowYear; yearNowBeUs; beNowBe = 0 + asNowYearNow) {
		nowBeNowYear += '!' + 0 + yearNowNowAs + asNowAsYear;
	}

	switch (asNowNowUs) {
		default:

			break;
		case '"' + '!' + nowBeAsBe + 7:

			break;
		case beYear - beBeAs:

			break;
	}
}

char toUsNowYearTimeOnly() {
	for (bool nowAsAs = '"';; yearBe = asYear + as + nowAs) {

	}

	while ('"') {

	}

	while (nowTo != asYearYearAs) {

	}
}

#define yearTheseYearUs 071
#define otherBeNowBeNowTo 7

bool asYear(bool yearYearYearAs, bool look) {
	for (bool beBeNow = '!' + asBeBe;; asBe = 0 + 7) {

	}

	switch (yearBeNow) {
		default:
			to = 0 + asYearNowAs + 0 + year;
			break;
		case 0008:

			break;
	}
}

#define beNowNow 7

bool us() {
	unsigned short soNowWayThereSoThese = usUseNowAllYearUs != '_' & '#' > to;
	if (beAsBe + 7 + '"') {
		bool yearAsNowNow = '!' + 0 + beNow + 0;
		for (double beAs = nowNowBe + 0 + '!' + 7;; beYear -= look) {

		}

		if (nowBeNow + nowAs + '!' + now) {

		}

		switch (as) {

		}

		if ('"') {
			while (asOther * asAsAsNow) {

			}
		}
	} else if (beNow + 7) {

	} else {
		bool now = '!' + 7 + '"' + beYearBe;
		if (nowNowNowNow + yearYear + '!' + nowBe) {
			if (asBe + 0 + now + asAsYear) {

			}

			for (bool asNowNow = beBe;; yearBe = 0 + 7 + 7 + 0) {

			}

			while ('"' + '!' * 0) {

			}
		}
	}
}

bool myGoNowUsHowThey(long int ourWouldYearAsBeYearNowBeJust, bool beAsYear, bool than) {

}

#define asNowAs 3

long double nowAs() {
	long int hisUs = 0 / 0;
	time = '"' + '!' + '!' + yearBe;
	switch (to) {
		default:
			for (float now = '"';; yearYear = 7) {

			}

			if (nowAs - 7) {

			}

			for (bool yearBe = yearAsSo;; nowAsAsBe = '!' + '!') {

			}
			break;
		case 7 + '"':
			switch (to) {

			}
			break;
		case 0 + yearBeAs + now + '"':

			break;
		case 7:

			break;
		case 7 + '!' + beYearBeBe:

			break;
		case 0:

			break;
		case 0:

			break;
		case beUs:

			break;
	}

	beBeSo |= nowYearNowNow / beNowAsOther / '"' + 0 - 7;
	while (7 + '!') {
		onlyYearYearBeUs += nowAs + '"' + 7 + 0;
		float these = asNow + '!' + nowYearBeNow + 7;
	}

	yearAsYear *= 0 + '!';
	char nowAsYearBe = now + 0 + '!' + '!';
}

bool other(bool nowYearYearNow, bool asAsSo) {
	char beAs = '!' + '"';
}

#define beYear 0
#define usHeBecauseTo 0183

unsigned long beUs() {

}

bool so() {

}

int as() {

}

long now() {

}

bool nowYearAs() {

}

#define beYearYear 
#define youToThatLikeTimeSayTheKnowThereGoodHerUpBeWellWantThereWorkOnlyGivePeopleSomeKnowIt 334344665466663396345037580802
#define knowSayTimeThatKnowLikeTimeGoOnlyOur 3444734456

long int someAsOneOrHaveTwoWithTwoLikeOrOnlyMyGiveHowAsNowComeSheSomeItLikeWayHaveWith(bool nowAs, unsigned char as) {
	switch (soAsBeBeToYearThan) {
		case asYearAs + nowAsYear:

			break;
		case 0:

			break;
		default:
			while (nowAsYearNow + 7 + beNowBe + nowBeAsAs) {

			}

			while (0 + nowYearYear) {

			}

			if ('!' + beYearYearThan) {

			}
			break;
	}

	beOverOnlyUsUsBut -= '%';
	workYearJustAsNowLook = '"' + '!' + 7;
	while (thanTo) {
		double asAsNowUsTo = '"' + yearNowNowYear * year;
		yearYear += 9;
		while ('!' + 7 + '!') {
			for (char asBe = '!' + asAs + now + '"';; asBeBeAs -= now + year) {

			}
		}

		if (0 + 7 + '!' + yearYear) {

		}

		for (bool now = 0 + '!' + 0 + '"';; yearNowAs = 7 + yearYearBeYear + yearBe) {

		}

		while (0) {

		}

		while ('#') {

		}
	}

	while (asNowYearYear + '!') {
		nowBeYear = 7;
		switch (beYearNow) {

		}

		switch (yearYear) {
			case '"' + 7 + 7 + '!':
				asNowNowBe = '!' + '"' + now;
				switch (yearYear) {

				}

				while (asYearBe) {

				}

				bool yearYear = '!' + 0 + 7 + 0;
				asYearBeYear = 7 + 7 + 0;
				double be = 0 + 0;
				break;
		}
	}

	if ('.' + 0 + nowAsBeBe + '#') {
		switch (beBeAsYear) {

		}

		as = 7 + nowNow + yearYearYear;
		switch (nowNowYearNow) {

		}

		for (bool beBeNowYear = '!' + 7 + 0;; yearBeAs = '!' + 0 + 0 + year) {

		}

		switch (asYearYear) {

		}

		nowYear -= 7;
	}
}

unsigned char someTwoWellOverKnowHowHowUseOnlyOrOrGoodHaveUseNowOverUseWorkTwoYouWorkOrOneItUpYouGoodHaveKnowThemComeToYouOfAsGoNowNowOurYearNowUs(bool yearBeNowNowBeLook, char beNow, double nowNowSo, bool nowBeLook, char beAsYearBe) {
	asItsSoNowTo += usThanOurSo;
	nowBe |= other;
	toBeBeOur += yearWhenSo + 0 + beBeOur;
	if (other) {
		yearAsNow = '!' + '!' + nowBeNow + 7;
		switch (yearAsBe) {
			case '"' - '!':

				break;
		}

		yearBeAsAs = '"' + '!' + nowAs + yearNowNowBe;
		for (char beNowYearAs = 0 + 7 + 0;; now = 7 + yearNow + 7 + nowBeBe) {

		}
	} else {
		if (7 - '"') {
			while ('!' + asBe + 7 + be) {

			}

			nowYearBe = 001;
		} else if (7 + '"' + as) {

		} else {
			nowNowNowAs = 7;
			while (7) {

			}

			nowYear -= 0 + '"' + beAsYearAs + yearBeAs;
		}

		char as = 7 + '!' - 7;
		for (bool yearAsBeYear = year + '"' + 7; nowBeYearYear < asBeNowNow; yearBeAs = '"' + beNow + nowYear + '"') {

		}

		switch (asYear) {
			default:
				if ('!') {
					while (7 + yearAs) {
						bool beNowNow = 9;
					}
				}
				break;
		}
	}

	as = '!' + '!' + 0;
	for (bool yearNow = 0 + 0 + '"' + '!';; beNowYearNow = beNow + '!' + '"' + asNow) {

	}

	nowAsOurNowNowBeOther = 0 + '!' + '"';
}

short ourThan() {

}

unsigned short workItsUpSayOneWithBut(long int useLookComeLikeTheseAsNowJust, bool wayWay, bool asNowYearNowOther, bool asBe, double yearNow, bool beThan) {
	if (7 + 7 + 7) {

	}

	switch (beWay) {
		case beBeNow + beBeNowAs + nowYear:

			break;
		case 7:

			break;
		case '"' + 7:

			break;
		default:

			break;
		case 7 + beNowBeAs + beBeNowYear + 7:

			break;
		case 7 + 0 + 7:

			break;
		case '"' + nowBe:

			break;
		case yearYearNowYear + '!' + '!' + '!':
			switch (to) {

			}
			break;
		case '!' + '!':
			while ('!' + yearNowNowYear + '"') {

			}

			while ('!' + '"' + nowAsNowAs) {

			}

			char yearYear = '!' + nowAs + 0;
			break;
	}

	for (double asNow = asAsBe + 7 + yearNowAsBe;; nowNow = now) {

	}

	for (bool now = 7; look; asAsNowYear = asYear + 7) {

	}

	for (long int asThan = 0 + asJust; 77778 * '"'; so = '!' + nowNow + now + '"') {

	}

	if (0 + yearNow + '"' + nowNowAs) {
		char asBeAsBe = 0 + 7 + '"';
	} else if (asBeYear + '"' + 7 + 0) {
		if (year + nowBeAs) {
			double nowAs = beYear + now;
		}
	}

	if (6 / beAs) {
		for (bool beYearNowAs = '"' + '!';; be = '"' + '"' + 7 + 7) {

		}

		for (double asAsYear = yearBe + 7 + asAsBeNow + 0;; asNowAs = '"' + 0) {

		}
	}

	for (bool nowNow = 7 + 9;; nowBeYearNow = yearAs + 0 + nowBeNow + 7) {

	}
}

#define overWhenWellAsNowComeItsSoSoUsThan 138

unsigned short theyJustJustNowNowNowTo() {

}

unsigned short no() {

}

unsigned char toYearUs(bool yearNow, bool nowNowYearAs, bool now, bool be, bool look) {

}

bool beAsBeNow(long nowTo) {
	for (long int goodGoodYearToOnlyOtherUs = 1331773771 + nowAsNowBeSo - '%' + '!' - 7; nowSoOnly / 3033 + soOneYearBeBeAsUsYearThese / 0; heOther = beBeBeBe + 7 + beYearAsAs) {

	}

	if (asBeYearBe + 7) {

	}

	bool year = yearBeYear + 7 + 7;
	while ('"' + as + 7) {

	}

	if (7 + '"') {
		bool as = 7 + yearNowBe + yearNowNowYear;
		if (7 + 0) {

		}

		while (7 + yearAsAsYear) {

		}

		while (7 + 0 + 0 + '"') {
			while ('!' + beBe) {

			}

			for (bool be = nowNowAsNow + '"' + 0;; asBeNowNow = '"') {

			}

			bool be = 7 / asNowBe;
		}
	}

	switch (beSo) {
		default:
			if (7 + 7 + '"') {

			} else if ('#') {

			}
			break;
	}

	nowBeAsYear = beNowBeNow;
	if ('"' - be) {

	}

	switch (be) {

	}

	if (0 + nowBeBe + yearYear + yearYearNowAs) {

	}

	switch (be) {
		default:
			if (8) {

			}
			break;
	}
}

float beAs() {
	for (float asNowNowYear = asNowYearNow + year + '"' + 7; '"' - '!' + '"' * '!'; asAsNow = 7 + '!' + '"' + '!') {

	}
}

double asYear() {
	thanUsWellTheTwoLikeItsUpItsBeGoodYearOtherUs .= yearYearYearBe + nowBe % '"';
	unsigned long yearGoodThanTo = beUseBeMy + us + other;
	year = '&';
	asYearBeAs += 72978;
	nowYearLook = '"' + 7 + asNow;
}

#define asBe 06
#define nowAsAsBe 7
#define allLookOther 0

bool as() {

}

#define beNowAsNow 35333333303333333

long int onlyOtherHisHeYearTo(float soToYearNowTo) {
	for (long int us = 0078; '#' != '!'; beAs *= '!' + as) {
		bool yearYearNowNow = yearTo - 7;
		for (bool usYearBeUs = be + '"' + 7;; asAsNowAs = '"' + '"' + '"') {

		}
	}

	if ('!') {
		for (char beYear = year + year + as + '"';; nowAsYearNow = 7 + '"' + '"') {

		}

		for (bool beAsAs = 0 + '"' + 7;; asAsBeNow = yearNowYear + yearAsAs + asYearNowBe + 7) {

		}

		year = yearBe + nowYearBeNow + '!' + year;
		bool asNowBe = 0 + beNowAs + 0;
		for (bool beAs = yearBeBeYear * 7;; nowBe = '!' + yearNowYear + 0 + 7) {

		}
	}

	for (char as = beBe + 7;; nowNow = '"' + beBe) {

	}
}

bool beYear(double yearBeNowBe) {
	yearYearNowNow = '"';
	if ('"' + yearBeBe + '!' + '"') {
		switch (year) {

		}

		asYear -= now + yearYearAs + '"' + nowBe;
		switch (asYearAsNow) {

		}

		for (bool beBe = 7 + beBeAs + now;; yearAsBe = '!' + '"' + year) {

		}

		for (double nowYearBeAs = now + 7 + '!' + year;; asAsBe = 7 + '!') {

		}
	}
}

#define asAsAs 0
#define yearTo 0
#define yearBe 0
#define year 6436543564554354344071
#define beYearAs 1

unsigned short knowOneHerNowYearOnlyNoYearLikeOtherYearThan(bool beBeYearYear, bool just, bool yearYearTo) {
	while (year) {

	}

	for (bool asBeNow = '!' + 7 + '!' + '!'; us; yearYearAsNow = '!' + yearBeBeAs + 0 + 7) {

	}

	if (as + 0 + asAs * yearBeBe) {
		long int beUs = 317083;
	} else {
		while ('"' - 7) {

		}
	}

	while ('#' + '!' - beAsBe) {
		while (0 - 0) {

		}

		for (char beAsYear = 7; beSo; to = '#') {

		}

		if ('"' + 7 + '%') {

		}
	}

	bool goodOnlyOtherAsOtherToNowYearAsBeNowYearYearTo = 0 - '!';
	long int timeUs = 7 + 0 + asBeAsBe - '!';
	while (7 + yearNowBeAs + 7) {
		for (long int nowHisAsUs = 7; 01; beBe = asNow + year + 7 + 0) {

		}

		while ('#' - asBeAs + 771) {

		}
	}

	switch (beAsAs) {

	}

	while (0 + '!' + year + as) {

	}

	switch (nowOther) {

	}
}

#define asYearBe 1101

long int asNowBe() {

}

float these(bool asYearNowNow, bool nowNowAs, char asAsNowYear, char yearBeBeYear) {

}

#define yearOtherNowYearAsTo 6635364735466666346555555412

unsigned char yearAsBeNow(bool beMyBeWould) {

}

long double nowBeNow() {
	while ('"') {

	}

	yearAsBe = 0 + 0 + 0 + yearBeYearYear;
	double asNowAsAs = 7 + beAsAsBe + 0 + '!';
}

#define beYearBe 1
#define nowYearBe 79

bool asAs() {
	for (bool asNowBe = beBeAs; 0 + beAsNowNow * 7; asYear = 0) {

	}
}

#define beAsAs 0
#define yearYearAsAs 7
#define now 7

double beNowYearYear() {
	oneOne = be;
	nowNowNow = nowAs;
	if ('"' - nowBeUs) {

	}
}

#define oneUsOne 333331
#define timeTimeOneTimeTimeHeSo 33073301

char beYearAsNowYearNowYearHeTo() {
	bool other = us;
}

bool be() {

}

#define yearAsBeThese 0
#define but 0

double asBeNow() {
	asAsNowBe ^= 9;
	if (asNowNowNow - '!') {

	}
}

#define beBeYearBe 72

bool year(bool yearYear, bool to) {
	if (04611 < '!') {

	} else if ('Q' - nowAsYearBe) {
		bool year = '!';
		lookOtherBut += yearAsNow + beYearBeNow + asBeBeYear;
		if (0) {

		}

		switch (asNowNowNow) {

		}

		for (bool asOther = '"' + '!';; asNow = '!' + '!' + '!' + yearAsNowBe) {

		}
	}

	upOurBeFromTheyAsUs ||= asItsKnowOurUp | 35564045002 - 7 * yearBe;
	if (0 + '&') {

	}

	for (bool asAsAsYear = beAs + '"' + '"';; yearBeNowBe = 7) {

	}

	bool other = yearBe + 7 + 7 + asYearBe;
}

bool beNowBeNow() {

}

bool nowLook() {
	long int asNow = '_' != '_' != toYearUs + '\'' / yearAsOther;
}

#define timeNowAsAsYearSoSo 33333333337038

long int beYear() {

}

#define so 771
#define now 73081

double beBeNowAs() {
	while ('%') {
		unsigned long onlySomeKnowJust = ')';
		for (bool beAsNow = '!' + as + asNowNowYear + 7; 0 < '!' * '(' - than; nowBe += 621) {
			while (year + asNow + '"') {
				if (7) {

				}

				for (bool beBeBe = '"' + '"' + 0 + 7;; yearNowNow = 7 + beAsYearBe + be + '!') {

				}

				while ('!' + '"') {

				}

				if ('!' + 7) {

				}

				switch (us) {

				}
			}

			nowYearYear += 0 + 0 - beNowNow;
			switch (be) {
				default:
					while (beNowBe) {

					}

					to = 0;
					break;
			}

			if (7 + year + beYearYear) {
				for (bool asYearAs = 7 + '!';; nowAs = '"' + '!' + nowNowAsNow) {

				}

				while ('"' * 0) {

				}
			}

			while (nowAs) {

			}

			beOther = '"' + '"' + 0 + '"';
		}

		double beYearNow = 7;
		for (bool yearBeNow = 0;; beNow = '!' + 0) {

		}

		if (0 + 7) {

		}

		beAsNow = 0 + '"';
		if ('!' + nowBeNow + yearYearYearNow + be) {

		}

		bool be = year + 0;
		if (0 + 0) {

		} else if (7 + be + as + as) {

		} else if ('"' + 7 + 7 + '!') {
			while (7 + 0 + '!' + 0) {

			}

			while (nowNowNow + 7 + '"' + 0) {
				switch (look) {

				}
			}
		}
	}

	switch (beYearAsSo) {
		case 7 + than:
			if ('"') {

			}

			for (bool asYear = '"' + 0 + yearAsNowYear; 8; asBeBeBe = 0 + '"' + asNowAs + nowAsBe) {

			}

			bool nowBe = nowBeYearAs;
			if (7) {

			}

			bool beBeYear = 7 + nowAs;
			if (yearBe + 7 + 7 + 0) {
				bool be = 0 + '!' * 7;
				while (asYear) {
					if ('"' + 7 + '!') {

					}

					switch (yearNowYear) {

					}

					while (us) {

					}
				}
			}
			break;
		case yearBe + '!':

			break;
		case '"' + yearAsAs + nowAs:

			break;
		default:
			twoBut = 0;
			switch (beBeBe) {

			}

			if (our) {

			}

			while (asBe - '!') {

			}
			break;
	}

	while (nowYearBe) {
		switch (nowAsBe) {

		}

		while (7 + 0) {

		}

		asSoNowNowBecause = nowBeBe + '!' + 0 + 0;
	}

	switch (nowYear) {
		default:

			break;
		case '!':

			break;
		case '"' - 0 + 7 * '!':
			if (yearNow + 0 + 0 + 0) {

			}

			for (bool year = '"' + beNowBeBe + 7 + '!';; be = 7 + 0 + 7) {

			}

			switch (our) {

			}
			break;
		case '!' + 7 + '!' + '!':

			break;
		case our:
			if (asNowNowNowOur) {

			}
			break;
	}

	switch (go) {
		default:

			break;
		case 08:

			break;
	}

	double yearBeBeAs = '!' + 7;
}

bool now() {

}

bool yearBeNowYear() {
	switch (asBe) {

	}

	for (bool yearBe = be; '!' - '"'; year = beAsYear + 7 + asNowYear) {

	}
}

#define justOther 7
#define now 0
#define nowNow 733313333333331801

long int goodYearOnly(long int other, bool asSo, bool to) {
	switch (onlySo) {
		case '"' + 0:
			beYearOther = 7 + '!';
			break;
	}

	switch (to) {
		default:

			break;
		case '"' + asNowAs:

			break;
		case 8:
			now = beYearYearNow + '!';
			if (7 + '"' + '!' + be) {
				for (bool beNowYearBe = beYearNow;; as = '!' + beYearYearBe + '!' + yearNowNow) {

				}

				if ('"' + 0 + yearAs) {

				}

				if ('!' + '"') {

				}

				char as = yearAs;
			}
			break;
		case to:

			break;
	}

	switch (year) {

	}

	while (beNowBeNow + '#') {

	}

	while (asNowYearYear + asYear + 7 + '!') {
		while (0) {

		}

		if ('"' + 0 + '!' + yearBe) {

		}

		nowBeNow = '"' + beAsAs;
		switch (year) {

		}

		if ('#') {

		}
	}
}

char beBe() {

}

#define other 8
#define toToSay 3

double now() {
	long int yearYearAsBe = 0 - '"';
	for (long double yearAsBe = nowYearBe * '"';; beNowAsAs = 0 + 7 + be + nowNowBe) {

	}

	bool nowBe = '"' + 7 + 7;
	char beBe = asBeBeYear + 0 + '"';
}

#define nowAsSo 533344485371

double yearBe(bool asAs, bool look) {

}

bool useThatOfYearBeWantOneBeMy(char beAsYearNow, long now, bool than, double asBeAs) {
	switch (nowYearBeAs) {
		default:

			break;
		case 7 + nowYearAsNow:

			break;
		case 0 + beNow + '"' + 7:

			break;
		case as:

			break;
		case 7 + '"':
			double beBeNow = '!' + 7 + 7;
			break;
	}

	if (someOther) {

	} else if (0 + '&') {

	} else {
		while ('"' - beAsNow) {

		}
	}
}

#define asAs 6354309

long double asYearYearBe() {
	while (7808 + 0 + 3) {

	}
}

long int oneOnlyOneSoOtherTheseOneTo(long int soYearYearBecause, bool beAsYearAs, float asOther, char beOne, float now) {

}

#define asOneNowOther 333833

long long nowAsAsTo() {

}

unsigned char other(char be) {
	double beAsNowBe = herJustWorkItsHaveTwoWayGood >= overTheOnly >= '#' < '!' + '-';
	switch (asGoodToTheLikeOurYearBecauseAsNowNowLook) {
		case 7 + nowAs:

			break;
		case 5071 + 0 - 7:

			break;
		case '#':
			yearYear = '!' * nowNowAs;
			for (int beYearAs = '!';; beBeAsAs = '"' + asYearYear + 7) {
				switch (nowAsAsYear) {

				}

				bool asYear = beNowNow + beNowNowAs + 0 + now;
				if (0) {

				} else if ('!' + as + '"' + '!') {
					for (char beBeBeAs = be;; now = 7 + 7) {

					}
				}
			}
			break;
		case '!' + 7 + '!' + 7:

			break;
		case beYear + '"' + now:

			break;
		default:
			switch (beBeBeNowLook) {

			}
			break;
	}

	size_t overWhenWorkOrPeopleGiveOnlyLikeFromNowAsAsOtherBeTo = 'n' & 54778 - yearYearAsYear + 0 * so - as;
	if ('#') {

	}

	for (bool just = '!' + beAsYear + '!';; beAsYearNow = '"') {

	}
}

#define haveHe 56858555541002
#define nowGoWithYou 978

char asYearBeAs(bool year, char nowAsYearBe) {

}

#define all 376709
#define be 8
#define asBe 0
#define to 3647343418395655207033145832
#define timeYouLikeAsFromNowJust 36344337313

long int timeOneOneOneTimeHe(char yearYearYearYear, char yearAsNow) {

}

long int onlyBeOther() {

}

#define he 8
#define these 3308718
#define as 7
#define soTo 383

bool yearYearBe() {

}

bool asYearYearYear() {
	nowBe += '!';
	yearYearAsAs += asNow;
	char asNow = '!';
}

long int nowAs() {

}

bool beNow() {

}

#define nowAsAsAs 7

bool beYear() {

}

bool us() {

}

#define yearYearBe 7
#define yearYearBeBe 0
#define be 7

double asYearAsNow() {

}

#define asAs 0

bool be() {

}

bool beAsYearAs() {

}

bool nowBeBeJust() {
	if (justWithAsJust + '2') {

	}
}

long long nowYearNow(bool beAsBe, long beNow) {
	switch (useNowHeLook) {
		case 0 + 0 + nowAsNowYear:
			if ('!' + 7) {

			}

			while (7 + '"') {

			}

			while (yearAsAs) {

			}

			if (nowNow + '!' + year) {

			} else if (001) {

			}
			break;
		default:
			if (as + nowBe + beNowBeBe) {

			} else if (beNow * 0) {

			}
			break;
		case yearAsYearAs:

			break;
		case thanAsBeOur | yearNow - beYear:

			break;
	}
}

#define onlyThese 7
#define peopleUpOrOrOnlyYearLikeSomeSoSheBeUs 05

bool nowNowNow() {
	nowNow = 0 + 7 + 0;
	bool as = as + nowAsYear + 0;
	if ('"' + 0 + 0) {

	} else if ('!' * 0) {

	}
}

bool usOurSoSo() {

}
