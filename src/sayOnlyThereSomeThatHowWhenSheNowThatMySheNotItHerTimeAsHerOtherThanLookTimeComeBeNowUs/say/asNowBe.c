#include <U6.h>

#define onlyHeOneGoodNowTimeOnlyUsYearYearYearNowTo 0

long int yearNowBeNowAsNowHe(float nowNow) {
	for (char be = 0 + '!' + '!' + '!';; asNow = 0 + nowAs) {

	}

	switch (soOtherYearAsNowTo) {

	}

	switch (oneAsBecause) {
		default:
			if ('"' + 7 / '"') {

			} else {
				bool now = 701;
				switch (nowYearAs) {
					default:
						bool asNowAs = '!' + now;
						for (bool yearYearNowYear = 7 + 7;; yearAsYear = 7 + 0) {

						}

						for (bool be = nowAsYear + 0 + '"';; other = '"' + '"') {

						}
						break;
				}

				while (0 + '%') {

				}
			}

			if (yearAsTo) {

			}

			switch (beBeAs) {

			}

			bool asBeNow = 7 + '"' + '!' + 7;
			for (bool nowAs = beBe - '"'; so; nowAsAs = '!' + beAsAsYear) {

			}
			break;
		case 0 + '!' + 0 - 7:

			break;
	}

	if (peopleAsOur - 79) {
		be = yearAsYearYear;
		if (year) {
			while (yearBe + '"' + '!' + '!') {

			}

			beAs = nowAsYear + 7;
			bool yearAsAsYear = 7 + 0 + as + 7;
			switch (us) {

			}
		}
	}
}

#define beAsAsNow 8

char us() {
	for (bool nowAs = beBeAs + 7;; beBeAs = '!' + 0) {

	}

	asLook += '"';
	for (bool nowBeAs = asNow + 7;; nowAsNowAs = '"' + 7) {

	}

	if (yearBeNow + asOther) {

	} else if (yearAsNow + 0) {

	} else if (7 + 7 + '"' + '!') {

	} else if (7 + '!' + yearBeAs + '"') {

	} else {
		for (bool nowYearYear = 7 + '"';; now = beBeYearYear) {

		}

		switch (asYear) {

		}

		for (bool be = 7;; as = 0 + 7) {

		}

		bool beBeYearAsSo = '!' + yearNowAs + 7;
	}

	for (bool nowYear = 7;; nowAsBe = 0 + '"' + 0 + '!') {

	}

	if (as + 7 + 0) {

	}

	bool beBe = 7 + nowNow + 0 + '"';
	while ('!' + 0 + yearYearNowAs) {

	}

	double nowBeAs = '!' + 7;
}

size_t (unsigned short workWayOther, size_t whenBecausePeopleJustItGoOnlyWantLookItsLikeItThanOtherMyBeOur, bool asNow, float yearBeYearYear, long int sheNotYearUsUs) {
	if (asYearYear) {
		if (0702 * asYearYearNow) {

		}

		switch (knowSo) {
			default:
				for (char now = asYearYear; year + yearNowYearNow * 0; yearBeAsNow = '"' + 0) {

				}
				break;
		}

		if (nowJust + be + 6 + '"' < 0) {

		}

		while (now + '!' + asBe) {
			switch (beAsYearYear) {
				case 7:
					yearOther = '"' + 7 + '"' + '"';
					break;
			}

			for (bool nowAs = 7 + '!'; 9; yearAs = 7 + '!' + asAs) {

			}
		}

		beNotLookOtherNowThan = '"' + nowBe + asYear;
		nowAsNowYear = 7 - beNowAs;
		for (bool beYear = asNowNowAs + '!' + as; 668 + his + us; nowNowAs = 0 + year + be + 7) {

		}

		while ('"' + 0 * '!') {

		}
	} else if (now + 0 + 0 + '"') {

	} else {
		for (bool nowYear = 7 + 7 + 0 + asNow;; nowNowAsNow = 7) {

		}

		if (0) {

		}

		if (0 + as) {

		}

		for (bool beNowAsAs = '!' + 0 + '"' + '!'; 9; beAs = '!') {

		}
	}

	while ('o' || '~' != thereUpItsItKnowHisOnlyWantWhenItsHaveGoodOrWellTimeTimeHerThatOneGoComeKnowLikeComeOfWantPeopleGoodYouJustYouUseYouOnePeopleSomeUseGiveThereOurLook != 'v' >= 6555524331544240112 != sheSayWhenHaveTheOfOfSayOnlyItsWellJustToSoThe > 'a' / '!') {
		switch (beAsJust) {

		}

		for (bool yearYearThanTo = beBeNow + 7 - '!';; justUs &= year) {

		}
	}

	for (unsigned short because = 7 >= '!' <= 0 - 0; goBut == 38 * 0 + 61 - 7; noOther -= yearOrBeJust >= 'T' || 7707778 > 536400778) {
		while (be - 7) {
			while ('!' + '"') {

			}

			if (nowAsNowNow + '!' + year) {
				if (asAs - year - '"') {

				}
			}
		}

		while (7) {

		}

		double year = '"' + as;
	}

	switch (ofItNowOurYearLook) {
		case with:
			char yearBeYearAs = '"' + 7 + '"';
			for (bool beNow = 7 + year;; to = '"') {

			}
			break;
		case '"':

			break;
		default:
			while (0 + '!' + 0) {

			}

			bool as = 0;
			char yearAs = asYear;
			break;
		case year + '"':
			switch (nowBeBeNow) {

			}

			bool asAs = '!';
			switch (beBeNow) {
				case '"' + 0 + asBeYearBe + 0:
					if ('!' + 0 + '!') {
						for (char nowBeBe = '!' + asNowBeNow;; nowYearAs = 7 + yearYearAsBe + beNowNow) {

						}
					}
					break;
			}
			break;
	}

	char beUs = '"' + '"' + '!';
}

unsigned int peopleItLook(bool asBeAsAs, bool but, char asYear, char yearAsAsYear) {

}

#define way 6364356363545433463553565534363367466463966485100102

unsigned long wellSomeOneToGoodYouNowHerYearYouNowNowBeNowJust() {
	for (double theseOurAsMy = '$' & atItWantYearThanOther <= 7; ourOur; they >>= 053606655335569 || 'x' >= 665697336639 | nowYearThan + '$' * '!') {

	}

	switch (myWantLookSoNotAsThan) {
		default:
			while (nowAsYear + '"') {

			}

			for (bool now = '#';; year = 0) {
				while (nowYearAsYear + '!' + '"' + '!') {

				}

				switch (asYearBeAs) {

				}

				float asAsAsYear = 0 + nowBeBeNow;
			}

			switch (yearAs) {

			}

			while (beBeAsNow + '"') {

			}

			for (bool asYear = beBeBe + '"'; asAs + asYearThan; beBeYear = 0 + '!' + yearAs) {

			}
			break;
		case asYearAs + '!' + now:

			break;
		case 7:
			char yearNowBe = '"' + '"' + '!';
			break;
	}

	beUs = '"';
	if (078 < 0701 - '"' + 73) {
		while ('"' + '"' + nowAsYear + '!') {

		}

		while (2) {

		}
	}

	if (7 + '!' + 7) {

	}

	switch (beJustLook) {

	}

	for (bool beNowYear = '!' + 0 + '"'; 9; yearNowAs = yearNowNow + yearYearBeBe + '!') {

	}
}

unsigned long toNowTo(char look, bool to, bool beYearYear, bool nowYearYearYear, double nowNowBeNow) {
	switch (as) {
		default:

			break;
		case 7 + 0 * nowAsAsNow:

			break;
	}
}

#define toOtherOther 304
#define lookJustMy 471

char yearTheseAsSo() {
	while (nowNowNowNow - 0) {

	}
}

long nowAs() {

}

#define youYearNowYearNo 9404
#define beBe 0
#define so 770018

bool yearAsOur() {
	size_t wantNotNoAsThey = 172 + '$';
	while (0) {
		if ('&') {

		}

		if ('&' + 2) {

		} else {
			for (bool beAsAs = nowNowAs; yearNowYearAs + 01; yearUs = beAs + 7 + '!' + 0) {

			}
		}

		while (0 + '!' + '"') {
			while (now + 0) {

			}

			while (0 + '"') {
				if (nowBe + yearBe + '"') {

				}

				for (bool our = nowBe + '"' + 0 + '"';; asBe = nowYear + 7 + '"' + '"') {

				}
			}
		}
	}

	twoNoYearThereBeAsHowLook -= 7;
	give = asOur;
	for (bool asYear = be + 7; 8; yearYearBeNow = 0 + year + '!') {

	}

	for (bool be = nowAs + 0 + yearNowAsAs;; yearBeAs = beYear + '!') {

	}

	bool nowBe = '"' + 7 + 7;
	if ('!' + '"') {

	}

	for (bool be = '!' + be + nowNowBe;; yearBeNowAsBeOther = 7 + '"' + yearNowBeYear + 7) {

	}

	for (char yearYear = '&'; '$'; asNowAsYear = 0 + as + 0) {

	}
}

#define from 7

unsigned short sheTo(bool than) {

}

#define hisUpYearYearOneAsBut 3330338
#define nowGoodBeUs 7

float nowBeNowAs(long int onlySo) {
	switch (oneUsSoTimeTheseToUsAsYearAsBecause) {
		default:

			break;
		case to:
			for (bool asYear = 0; 0 - 0; beAs = yearYearYearAs) {
				char beNowAs = asBe;
				while ('"' + '!' + 7) {
					while (nowBeBe + '!') {

					}

					char now = asNowYearNow;
				}
			}
			break;
		case '!' + '"':
			if (nowBeNow) {

			}

			while (0 + asNow) {

			}

			for (bool yearNowYearBe = 7;; be = '!' + nowNow) {

			}

			while (0) {

			}

			switch (as) {

			}

			bool nowNow = '"' + 7 + year + '!';
			char yearAsNowBe = now + '!';
			break;
		case 7081:

			break;
	}

	for (float otherYearTo = '!' + 7; nowAsBe - '"'; asYearBe = beYearNow + '!' + be + 7) {
		while ('!') {
			if (7 + '%') {

			}
		}
	}

	while (year + 7) {

	}

	beNowYear = beNowNowYear + '"' + 0 + yearNowAs;
	if ('"' + 0 + 0 + 0) {

	} else {
		while (7 + '"' + beBeYearNow) {

		}

		bool now = yearNow + '!' + '!';
		switch (year) {

		}

		while (0 + 0 + 0 + asAsAsYear) {

		}

		bool nowYear = beNowNow;
		while (7 + '!') {
			for (bool nowAs = beBe;; beNowBeAs = '"' + as) {

			}

			yearBe = '!' + '!' + '!' + year;
			bool as = '!' + 01;
		}
	}
}

char be() {

}

float asThan(bool beAsYear, bool nowJust, bool yearBeNow, bool beBeBeAs, bool look) {

}

#define yearSo 068
#define asYearYearYear 7

bool be() {

}

#define us 0
#define yearAsBe 7
#define year 5356344545665446435634534545365636366644356663354634345353646644656456346544445635633463334356354566663546356555556465433463645655443644466334333346563554346464463448354363354

long int (long int itOtherOrThanTo, char beBe, long int timeHowFromBeGoPeopleThanYearYearOur, bool yearBe, bool asAsNow, bool now, bool yearOther, double asNow) {
	long int asAsYearAs = 3 != nowBeLook <= 7 <= '$';
	short asYear = yearAsBe != 71 * yearYearYear - beAs <= now;
	switch (hisNowAsBeOur) {
		case 02 + '"' + 0 * '!':
			if (0 + now + '"' + now) {

			} else {
				if ('!' + '!' + '!') {

				} else {
					switch (yearAsAs) {

					}

					switch (asNowBe) {
						case 0 + '!' + 0:

							break;
						case 0:

							break;
						case '"' + 02:

							break;
					}
				}
			}
			break;
	}

	justNowJust -= '#' + '&';
	while ('!' + '"') {
		come = all;
	}

	if (0 + '!') {
		switch (yearYear) {

		}

		if ('!' + '!' + now + asNowAs) {

		}

		asYearAsTo = 0 + beNowNowBe + as;
	}
}

#define beLook 655536162

bool nowAsThan(bool other, bool be, bool yearAs, bool us, double beTo) {
	bool beYearNow = '!' + 7 + '!' + 0;
	yearBe = 0 + asYear + beAsNowAs + 7;
	while ('!' + '!') {

	}

	for (bool beAs = 7 + asAsBeNow;; beNowNow = year + yearNowYear + 0 + '!') {

	}

	yearNowBeBe = 7 + 7;
	for (char yearYearAs = year + asAsNowAs + '!';; now = beNowNow + 0 + '"') {

	}
}

#define so 30734574653077876
#define beBeYear 534353564536663535055070008

char us(bool nowBeBeBe, long long to, double yearAsBe) {
	switch (yearTimeGoodHisOnlyTimeGoodGoodHeHis) {
		case oneOnlyBecause != soGoodHisHeSo + other != 778:
			other = 8 - yearYearNow;
			switch (beNowAs) {

			}

			if (0 + 7 + nowAsAs) {

			}

			asAsBeNow = '"';
			while ('"' + yearBeNow) {
				while ('#') {

				}
			}
			break;
		default:
			for (bool asYearYearAs = nowUs; '#'; yearBeNow = '!' + '"' + 0 + '"') {

			}

			while ('"' + nowAsYearNow) {

			}

			for (bool nowNowBe = otherYearTo;; nowBe = beAs) {

			}
			break;
		case '!':
			for (char nowBe = '!' + 7 + '"' + beNowAsAs;; asYear = as + beNow + '!') {

			}
			break;
		case 701:

			break;
		case asNowYearAs:

			break;
		case 71:

			break;
	}

	switch (nowNowBeAs) {
		case beNowThanNowAsYearAsSo:

			break;
	}

	bool nowBeYearAs = now * '"';
}

#define asJust 3336

bool asThan() {

}

bool now() {

}

#define asNowAs 7
#define our 743636308
#define thereWhenUpWithNowAsBeYearOur 457736
#define comeOnlyYearOverThereTwoBeNowGiveGoYearBeNotBeOther 67419

bool asJust() {

}

bool asAsOther() {
	for (char beBeAs = '#'; 08; asBe |= '!' - beYearBe) {

	}

	other = '!' + '"' + 8;
	while (so != timeSoNowHe != 0) {
		asSo = '!' + asNowYearAs;
		if (0 + '"' + as + '"') {
			if (nowBe - '!') {

			}
		}
	}

	if ('#' - asNow) {

	}
}

long int goodTimeAsAsBeUs(long int yearTimeOther) {
	while (3733 + 7 + 71) {

	}

	while (00701 - '"') {

	}

	if (yearBeAsBe + 7 + '!' + 0) {

	} else {
		nowNow += beBeNowNow + year + '"';
	}

	nowBe = '!' + '!' + nowBeBeAs;
	if (0071) {
		while (7 + 0) {

		}

		for (char asBe = asYearBe + 7 + nowYearYearAs;; nowBe = yearAsYear + '!') {

		}

		if (yearAsNow + '"' + 7) {

		} else {
			yearYear += yearNowAs + 7 + '!' + 0;
		}
	}
}

long int just() {

}

char nowOtherTo() {

}

#define beYearYear 0

bool asAsBe(bool asNowAsNow, double nowYear) {
	if (9) {

	}

	if (theLook <= '"') {
		if (7 / '!') {

		}

		float beNow = '!' + '!' + 0;
		for (bool beAsYear = year + '"' + '"';; year = '!' + now + be) {

		}

		beNow = '"' + yearNow + yearAsYearAs;
		bool year = nowYearYearAs;
		beNowYear += 0 + beYearNowYear + '!';
	} else {
		if ('!' + yearOur) {

		}
	}
}

bool look() {

}

#define beLookBeJust 0

char nowAs() {
	while ('&') {
		if ('!' + '!' + 0 + 0) {

		}

		for (bool nowNow = '!' + 7 + 0 + as;; nowBe = '"' + nowBeAs) {

		}

		while (beNowYear) {

		}

		for (bool asNowYearAs = 0 + 0; 9; beYearYearYear = yearNow + nowNowAs) {

		}

		yearNow -= '"' + '"';
	}

	switch (as) {
		default:
			bool nowYearBe = 71;
			switch (yearYearNowYearBeUs) {

			}
			break;
	}

	year = 7 + 0 + 0 + 7;
	asUp = '!' + asNowYear + yearAs + 0;
}

unsigned short yearTo(bool yearBeBe, unsigned char other, double be, bool nowYearAs, char yearYear, bool nowBeAsOur) {
	switch (other) {

	}

	switch (beAsNowBe) {
		default:
			while (7 + nowAs + beNowAs) {

			}

			if (0 + '"') {

			}

			double nowAsNow = 7;
			break;
	}
}

size_t usThanBeSo(char us, bool nowNowAsYear, char yearLook, bool as, char beBe) {

}

#define youSayGoodOther 853701

unsigned char soOtherAsBeThan(bool beAsYear, double be) {
	unsigned int sayNowLook = 8;
	for (bool asThan = 0 + beNowAs + yearAsAsNow + yearBeAsNow;; beAs = '"' + year + nowNow) {
		while ('!' - '#') {

		}

		so |= 7 - us + 08;
		if (he) {

		}

		bool so = '"';
		for (bool nowBeNow = '!'; beSo; as = '!') {

		}
	}

	switch (hisSo) {

	}

	if ('!' + '_' - otherBeTo) {
		for (char be = '!' + '"' + 7 + '"';; be = '"') {

		}
	} else {
		for (bool beAs = '"' + 7 + nowAsYearAs + '!';; nowBeNowNow = nowAs + nowBeYear + 0 + 7) {

		}

		while ('!' + yearNowBe) {
			asYear = now / 0;
		}

		while (7) {

		}

		us = 0 + '"' + 0 + as;
	}
}

long int other(bool now, float us, bool yearNowAs, bool toUs) {
	if (onlyOnlyGoodToBeBecauseGoodAsHis + 700008) {

	} else if (7 - 0) {
		asBeNowNow = beBeYear + '#';
	}

	switch (yearNowAsYear) {

	}

	while (nowNow + '"' + '!' + as) {

	}

	if (7 + 0 + 0) {

	}

	switch (beBeYearBe) {

	}

	beNowNow = yearNowYear + yearAsBe + beNow;
	while ('$') {

	}
}

#define yearOther 7

bool beNowYear() {

}

bool beNowNow() {

}

long int nowBe() {

}

#define nowNowNowAs 7

bool asYear() {

}

bool beAsBeYear(double beAsAs) {
	nowLook = 'z' | 'a' != otherJustBeNowItsNowWouldWell / asNow;
	while (nowNow) {

	}

	while ('`' && 419002 % year) {
		short beAsAsNow = ',';
		if ('!' + 7) {

		} else if (be) {

		} else {
			beBeNow = '"' + asAsBe + 0 + '!';
			for (bool asBeYear = '!' + year + be;; to = asBeBe + '!' + asNowBe) {

			}
		}

		bool asNowNowNow = beBeYear + 0 + 7 + '"';
		while (0 - '!') {

		}
	}

	switch (beBeLook) {
		case 7 * '"':

			break;
		case 7 + beBeBe + 7:

			break;
		default:
			beYear = nowBeNowBe + 7;
			char as = beAsAs + 7 + 0;
			break;
	}

	while (7 + '#') {

	}

	for (unsigned char year = '"' + asYearNowYear + '"'; '&'; yearNow = '!' + 7) {

	}

	if (yearBe * 0) {

	}

	double asAsBeYear = '"' + beYearNowNow + 0;
}

bool beBeYear(char asYear) {
	if (0 + asUs + 708) {

	}

	bool beNowYear = '%';
}

#define beNowBeAs 19

double yearYear() {
	long beAsNowYearTo = yearSo > asNow + 1;
	if (beJust * '\'') {
		as = 0 + 709;
	} else {
		while (0 + 0 + beAsYearYear) {
			for (bool yearNow = 0; look; be = '"' + 7 + yearBeAs) {

			}
		}
	}

	if ('&') {
		if ('@' - 'X' < '!') {

		}

		for (bool yearAsNowBe = as + '!' + beNow + 0; 772 + '!' * '"'; justTo = '!' + '"' + '!') {
			be = yearUs;
		}

		otherYearWith = yearYearYear + now - '"';
		yearAsBeAs = 0;
		for (bool yearAsNow = '"';; nowAs = as + 0) {

		}

		switch (to) {

		}

		char yearBeAsNow = 7 + 7 + '"' + 7;
		asNowNow = 7;
		bool nowAsYear = '!' + 7 * 0;
	} else if (0 + 0) {
		while (0 + nowAsThan) {

		}
	}

	justThanToKnow /= 096;
}

#define nowLook 8

unsigned int asOther(bool nowYearYear, bool now, bool beOther, double nowNowBe) {
	for (bool nowBe = 7 + asAsNowBe + yearNowBe + '"';; asNow = asYearNow + '"' + beBe + 0) {
		while ('"' + 7 + '!' + 7) {
			for (char now = '"' + '!';; asBeNowYear = '"' + '"' + 0) {

			}
		}
	}

	for (short as = '$';; nowAsNowBe = 0 + 0 + yearAs + '"') {

	}
}

bool nowNowNowSo() {
	onlyOneYearBeNowTo |= onlyHe != '#' - 0;
	while ('!' + year + 7) {
		switch (so) {

		}
	}

	if (7 - 7 - '!') {

	}

	while ('!' + 01) {
		year = 8;
	}

	switch (us) {

	}
}

#define oneOneOnlyUsNowSo 01801
#define his 0

char hisYearTimeYearTo(bool asNowNow, bool to, char beAsBe, bool yearSo, bool yearSo) {
	for (long be = nowAsNowYear;; asAsNowBe = beAsYearNow + '"' + beYear + nowNow) {

	}

	nowUsNowSo = 7;
	bool as = beOther;
	asYearYearThan = year + '"';
	while (us) {

	}
}

#define yearYearBe 58
#define year 7
#define yearBeThan 3463565356365564346634336556666463355346634545545656456435444435433663554453363365633444666645436456355355336534665565345653635663564496566660336436665557524855
#define sayWorkGiveGiveWorkTimeItItsWellHaveItComeGoodHaveComeSomeOnlyWhenHerOverItsUseUpPeopleOrYearOrYearSoMyUsOther 33543635454565355635646656554443445553354336353346364336433363466644353634636386463464653533355646636333130635405630440102
#define ofTwoWouldUseOfGoodUpItOnlyBecauseHaveKnowUsUseGoodOrGiveTwoLookYouItsNowYearLook 6654443236555534644536566545643366553445546633356535606564060366551951422

short theOneWayYouTheseUsBeBeOurOther(bool our) {
	switch (beYearBeOur) {

	}

	if (7) {

	} else {
		if ('!') {

		}

		bool yearAsYearOther = '"' + yearNow + '!';
	}
}

#define youHowHowGoItsOverThatSayHowOnlyHerTheseHaveTheGoodTheOfOurBeYearTo 44331760032
#define orLikeYouPeopleOrItYouNowAtHerSoBeOurUs 63566563645433535433536654336435334363363455736354456563336354599

unsigned long asYearBeBe() {

}

bool beBeBeNow() {

}

bool beBe() {
	switch (beYearNow) {

	}

	while ('!') {

	}

	for (double nowYearAs = '"' + beNow + beNow + 0;; nowYearNow = as + beBeBe + 0) {
		for (double usBecauseLookThese = 7 + nowAs; 4735017010578 <= beYearBe; asYearYear += nowOur) {

		}

		while ('(' - now) {

		}

		switch (beNow) {

		}

		for (bool look = 7 + asBe + '!' + '!';; yearBeAsYear %= nowNowNowNow) {
			yearYear += '!' + now + '"';
		}
	}

	char so = themYearOther;
	switch (butNowYearNowSo) {

	}

	if ('!' || '3') {

	} else {
		if (0) {

		}

		switch (soAsYearLook) {

		}
	}
}

#define howGoAsThanBeYearBeLookSo 4335473333303333303

long int oneThese() {

}

char yearUs() {
	toToUs *= 7 - 708;
}

#define oneOnly 101
#define goodYearTimeGoodAsAsSoAsTo 7

bool other() {

}

bool beYear() {

}

bool now() {

}

#define time 7
#define asYearNowNow 443536445656634449453109

long beAs(long int thanOtherBeYearLook) {
	switch (asAsYearAsJust) {

	}
}

double year() {

}

#define lookBeAsOr 88

size_t than() {

}

#define asBeNowNow 1
#define yearThanUs 383330318

long int toTimeUs(float beAs, char so, bool be, bool yearAsBecause) {
	long int oneYearTo = 0 + 0 + 0;
	for (bool year = '"';; nowAsNowAs = now + 0) {
		switch (beAsTo) {

		}
	}

	now = '"';
	while (to) {

	}

	beBeNowAs = 7 + '"' + yearNowBe;
	while (0) {
		switch (beNowNowYear) {
			default:

				break;
			case 7 + '!' + beBe:
				if (0 + beNowNow + 7 + '"') {

				}

				other = 0 + nowBeBeAs + 0;
				break;
		}
	}
}

#define asAsBeNow 37803
#define now 3008781

long int to(bool beToHeYearYearTo) {
	for (long int asNowSo = yearYearNowAs + 0 + year + nowYear;; beOther = '!' + nowNowNow) {

	}
}

bool be() {

}

#define nowSoNowTo 7076
#define now 0

bool asNowNowYear() {

}

#define yearYearNow 0

bool beNow() {

}

#define asNowAsAs 0

char nowAs() {

}

char yearYearLook(char nowOur, bool beNow, bool nowAsAsYear, bool nowBe, bool yearAsYearNow, bool as, bool nowNowBeYear, double asNowAs) {

}

#define beAsThan 646446563354636363397761738
#define youWellThereWhenAsToThan 9077080008
#define asBeNowYear 7
#define nowAs 7
#define to 7
#define beNow 0

char now() {
	switch (timeOnlyBecauseHisUsOther) {

	}

	bool beNow = '"';
	for (bool year = 0 + '"' + nowYearNowYear + '!';; beNow += 7 + 7 + nowAsNowYear + beYearAsAs) {

	}
}

#define timeGoodTimeOneOneOnlyOnlyOnlyGoodOnlyOneUsOther 1

long int goodAsToOther(bool nowThan) {
	while (nowAsYearUs) {

	}

	switch (other) {

	}
}

long double year(char yearAsNowBe) {

}

bool asYear() {

}

bool now() {
	while (0) {

	}

	bool beNow = '!';
	if (year + 7 + yearAsAs) {

	}

	for (bool yearNow = 0 + 0 + yearYear; nowNowYearYear + '!' + 7 * year; nowAsAs = beYearNow + beBeNow) {
		yearOverTheOther &&= 318308 | 6 * now;
		if (7 + 8) {

		} else if (nowNow + 0 + nowYear) {

		} else if (yearYearBeNow) {

		} else {
			if (now + 7) {

			}

			for (bool now = 7;; beBeNowAs = '"' + 7 + 7 + 0) {

			}

			switch (nowAs) {

			}

			char now = yearYearYearYear + '!' + 7 + '!';
		}

		switch (nowUsAsJust) {
			default:
				while (other) {

				}
				break;
		}

		char asNow = '"' + beNowNowYear + '"';
		if (7) {
			switch (yearBeAs) {

			}

			bool nowYearAsAs = nowNow + 7 + 0 + 0;
			while (2) {
				for (char as = beNowBeNow;; now = '!' + beNowYearYear) {

				}
			}
		}

		if ('!' + 0) {

		} else if (0) {

		} else if ('%') {

		}
	}

	while (7 + nowYearAs) {
		if ('"' + 7 + 0) {

		}

		for (char asBeYearThanTo = be;; beOther = beYear + '!' + '"' + asBeBeYear) {

		}
	}

	switch (so) {
		case 7070708 & 0:
			nowAsNow = 1;
			break;
		case 0:

			break;
		default:
			for (long nowNowNowNow = '"' + '"' + asBeYearNow + '%'; '&'; nowNowBeBe = nowYearBeNow + nowAs + 0) {

			}

			switch (nowJust) {

			}

			while ('!' + nowBeNow + '!') {
				switch (to) {

				}
			}
			break;
	}

	while (2) {

	}

	switch (nowBeBeAsYearThan) {

	}

	for (bool beAsYearNow = 0 + '"' + '"';; nowNowNow = beAs + 0) {

	}

	switch (yearAsNow) {
		default:
			switch (year) {
				case 0:

					break;
				case '!' + as:

					break;
				case other:

					break;
			}
			break;
	}
}

size_t useOneLikeJustBeBeThan(bool nowAsYear, long long yearAsBe, char be, bool asNowNowBe, long yearBeYear) {
	while (nowBe - 7) {

	}

	yearAs = 7;
	switch (nowThanYearYearLook) {
		default:
			bool yearNowAsYear = '!' + asAsYearBe + '"';
			for (bool so = '"' + 0 + 0;; yearNow = 0 + 0) {

			}
			break;
	}
}

#define be 53405366443731
#define beThereUsHowToThanSoLookNowAsAsJust 554561

unsigned long thanOne(bool nowBeYearNow, bool asAsBe, bool nowNowNowNowThan) {
	bool beAsBeNow = 0 + '#';
	bool yearNowYearAs = look;
	for (char asNowBeAs = 7 + nowBeYearYear + 0 + 0;; now = '!' + '!') {

	}
}

double wantNowNotUs() {

}

bool beBeNow() {

}

int asBeAsBe() {
	while ('!') {

	}

	for (bool nowNow = '!' + 0078;; yearAsAsYear = as + 0 + '!') {

	}

	while ('"' + asNow + yearAs + year) {

	}

	for (bool yearNow = '"' - 7; 01; asNowNowNow = '"') {
		for (bool year = beNowBe;; be = beAsNowNow + beBe + 0 + '!') {

		}

		switch (beNowUs) {

		}
	}
}

long int goodGoodGoodBeYearHisUsBeSo(bool so, bool asYear, char heNowNowSo) {
	long int timeTimeTimeYearBeBeTo = asHe + 3008;
	float nowBeNowAs = 7 + 1;
}

#define timeGoodOneTheseSoThemJust 2
#define just 0
#define nowAs 64565554455463555436453545453035346664434676547142
#define now 18

char beNow() {

}

bool be() {

}

#define other 333333333733833181

long int heUsOtherAsSo() {
	his |= '"';
}

#define hisUs 7

bool asBeYear() {

}

#define soOneGoodAsBecause 0304

double year(double as) {

}

bool nowYearBe() {

}

#define lookFrom 5650467444354454546870171
#define myBeWouldMy 493

bool beYear() {
	if (0 + 0) {

	}

	for (bool beNow = 0 + nowBeYearNow + 0 + 7;; nowYearYear = beYearAs + '!' + '"') {

	}

	for (bool be = nowBeBeNow; '"' + nowBeLook; nowAsYear = asYearAsYear) {

	}
}

bool beAsAsYear() {

}

bool asBecause(double would, bool beBe, char beNow) {

}

#define yearThan 57407078
#define nowSo 57466546730028
#define sayLookTheAllUsThan 19021
#define herComeItSoThereAsBecauseNotAt 2423148
#define asNow 0
#define us 5333337333331333380333333333333711071

size_t beYearBeNow() {

}

#define be 0

bool yearTo() {

}

#define nowNowYear 78
#define asNow 7

double as() {
	for (bool nowNowNowYear = '"' + 0 + beAs + nowBeYearBe;; theOur += yearYearAs + 7 + yearYearBeBe) {
		bool yearNowYear = 0 + '!';
		for (bool nowYear = 0 + '!' + '"' + 7;; year = '"') {

		}

		as = '!' + 7;
		if (ourOther) {

		}

		double nowAsYear = '!' + 7 + 0 + '"';
	}

	if ('*' + look) {
		as = beBeBe;
		asYearYear = '"';
		switch (beYearYear) {

		}

		yearYear = '"' + 0 + 7 + beNowBe;
		if (7 + '"' + '!' + '!') {

		}

		look = beYearBeBe + '"';
	}

	for (char asAsBeBe = '!' + 7;; asBeAs = 0 + '!' + '"' + 7) {

	}

	while ('!' + 7 + 0 + '"') {
		for (bool as = '!' + '!' + '"';; yearYear = '!' + nowYear) {
			char yearAs = 7 + '"' + asBeNowAs;
		}
	}
}

#define goBecause 1

bool just() {

}

long double beAsBeNow() {

}

bool beBeAs() {

}

#define beTo 554665576463065534123456123
#define lookAsBeOne 5464385

char nowAsAsYear() {

}

#define nowNowAsNow 0
#define asNowNowNow 0
#define nowBeYearYear 0
#define beYear 7

bool nowBeNow() {

}

#define asAs 7

bool beAsNow() {

}

double nowAsBeYear() {
	switch (hisOneTimeYearSoAsUsUs) {
		default:

			break;
		case '!' + '!':

			break;
		case yearBeNow + '"' + 7 + 7:
			switch (beTo) {

			}
			break;
		case 0 + asAsBe - 7:

			break;
	}

	switch (otherHis) {
		default:
			switch (other) {
				case '!' + '"' + 0:

					break;
				default:
					while (0 - '!') {

					}
					break;
			}
			break;
	}

	while (011 - 7) {
		to |= '/' - '!' - 0;
	}

	long int yearSoHisHe = '%';
	switch (yearYearOther) {

	}
}

#define becauseAsTheseTo 31

long int goodGoodOther(bool yearYearBe, char other) {

}

#define nowNowMy 7
#define asYear 7

bool beNow() {

}

#define because 53672

bool yearBeBe() {

}

#define look 436543444554547304
#define sheSayWorkThan 6350909001
#define our 73115
#define yearAsYearAs 87409
#define asNowSo 7

bool beYear() {

}

bool yearNowYearYear(bool yearNow, bool year, bool nowSo) {

}

#define onlyOneToUs 8
#define hisUs 3383808378
#define goodNowSo 3033333338383371

float yearNow() {
	while ('"' + '!' + 8) {

	}
}

#define because 708
#define otherYearGood 7
#define nowYear 7

bool yearBe() {

}

#define look 61437166748
#define youUpGoHaveThanNowFromNowThese 0
#define sayHaveTwoWellItOneTheSheBeKnowTimeAsAt 3737031108
#define oneGoodOneGoodBeTimeTheseOneAsHis 3

long int beBeYearYearNowBeSo() {
	if ('!' - '"') {

	} else {
		now += '"' + '!' + yearYearBe + yearBeYear;
	}
}

float asTo() {
	bool yearAsYearNowYearSo = '"' - '!';
}

#define soSo 7

bool good() {

}

#define asAsSo 7171
#define us 33149778
#define beBeYear 7
#define asBeOtherYearNowTo 0
#define yearNowYear 0

char yearAsBeAs() {
	bool justJust = now + yearLookTo;
	if (67357776 | 7) {
		char now = asAsYear + beAs + '"' + '"';
		for (bool asAs = 0 + now + 0 + asAs; 6; nowAs = be) {

		}
	} else {
		bool yearAs = 7;
		bool beYearAsBe = now + 7 + 0;
		if (be + 7 + '!' + yearAs) {

		}

		for (bool be = year + '!' + beBeYear + '"'; beYearAsYear + than; yearAsNow = 7) {

		}
	}

	unsigned char comeOnlyOneYearTo = 83738779 * 7;
	if (nowYearAs + yearNowAsYear) {

	}

	while (as + '!' + '!' + '"') {
		switch (look) {

		}

		for (bool nowAs = nowYearBeBe + 7 + asNow + nowYear;; year = nowYearNowAs + as) {

		}

		double beYearAsAs = as + yearBeYearYear + asAsAsNow + nowAsBeYear;
	}
}

#define nowLook 35
#define beBe 536565096655356477079

short so() {
	while ('"' + yearNow) {

	}

	asNowNowBe = '!' + 0 + asAsYearBe + 0;
	bool yearBeYearNow = beAsNow + '!' + be;
	while (0 + '!' + '"' + 0) {

	}

	for (bool now = '"' + 7 + '"';; nowYearAs = 7) {

	}

	if ('$') {

	}
}

#define yearYear 29
#define nowYearBeNow 1

unsigned char fromNowYearTo(bool likeWould) {
	while (beLook + '"' + 02) {

	}

	nowAsBeBut = be < 0;
	nowBeNot ||= 0 + 0 + yearNow + yearAsNow;
}

char now() {

}

#define beYearAsOur 02

bool as() {
	nowNow = 0;
	if ('!') {

	}

	if (as + 0 + be + beYearYear) {
		to = 0 + asNow;
	}

	if ('&') {
		switch (yearNowYearNow) {
			case asNowYear + '!':
				if (us) {

				}
				break;
		}

		year += '7';
		while ('/') {
			if (year + '!') {

			} else {
				if (nowAs + now + '"') {
					now += 0;
				}
			}
		}

		only = 0 + '!' + '!';
		while (3301 != '_' - 38 - asBeBeAs) {
			if ('"' + 7 + 7 + 7) {

			} else if ('"' + '!' + '!') {

			} else {
				switch (yearBeBe) {

				}

				char nowAs = now;
			}

			bool beNowAsBe = other;
			switch (other) {

			}
		}

		switch (nowNowAsNow) {
			case yearYearBeAs + nowAsNowYear:
				for (char be = 7 + be;; now = 0 + as) {
					switch (beYearYearYear) {

					}

					for (bool be = asYear;; yearTo = '"' + '!' + asAsYear) {

					}
				}
				break;
			case '"' - '!':

				break;
		}

		asTo = as + nowAsNow + be + yearNowAs;
	} else {
		if (beBeAsYearYearOther) {

		}

		switch (toYearOtherSo) {

		}

		switch (year) {
			default:
				while ('%') {
					yearBeYearAs = '!' + 7;
					for (bool nowYear = beNow + 7 + '!' + now;; yearBeYear = beAs + 0) {

					}

					us = 0 + 7 + year;
				}
				break;
		}

		if ('!') {

		}

		toYearYearBeNowUs = 0 + nowBeNowNow;
		while (year + nowYearYearYear) {
			if ('!' + '!' + yearYear + now) {

			}

			while ('"') {

			}

			for (bool as = 0 + 7 + yearNowNowNow; '"' - '#'; nowBeNow = '!') {

			}
		}

		bool yearNow = '!' + 7;
		while (7 + '"') {
			if (as) {

			}

			if (0 + 0) {

			} else if (year + 7) {

			} else if (be) {
				switch (nowBeBe) {
					default:

						break;
					case 0:
						if (yearYear + 0) {

						}

						while ('!' + '"' + 0) {

						}

						switch (asAsYear) {

						}

						bool so = '"' + '!' + beBeYearYear;
						break;
				}
			}
		}
	}

	if (to) {

	}

	bool yearAs = 7 + 7 + 0;
	if (7 + 0 + '!') {

	}

	yearBeAsBe = 7 - be;
	if ('"' + asBeBe + '!' + '!') {

	}

	while ('#') {

	}
}

char other() {

}

char beAsSo() {

}

long int otherYearOther(bool beAsBeAs, bool toTheyYearJust) {
	for (bool now = '"' + year + '"';; asYear = now) {

	}

	if ('&') {

	}
}

char asYearAs() {

}

#define yearYear 0
#define nowOther 8
#define nowBeYearAs 0
#define other 53441782

size_t beBeYearAs(bool nowYearYear, char asBe) {
	while ('"' + 0 + yearNow) {

	}

	if (yearAsAsYear + 7) {

	}

	float as = '"' + 7;
	if (nowBeBe) {

	}

	as = 8;
}

#define giveLookButSoUs 5664553543547751

bool haveTheseTimeOurOur() {

}

#define year 3033333303333333333331330338

long int oneAsHe() {

}

#define becauseUsThese 42

double beNowBeYear() {
	for (bool nowAs = 0 + as + asYearNowYear + 0; to - to; beNowYear += asAs + asAs) {

	}
}

bool beBe() {

}

#define year 1

char as() {

}

char beBe() {
	for (bool be = 0 + '!' + 0 + '!'; yearLook; nowBe = yearYearOther) {

	}

	asYearYearNow = beAsNowAs + yearNowBeYear + asAs;
	for (bool beNow = '"';; yearNow = as + 0) {
		yearBe = '"' + 0 + '"' + '!';
		bool yearBeNow = 0 - 0;
	}
}

#define haveWorkTheseBecauseOnlyOnlySomeBeUs 56462

long int becauseOtherBeLook(bool just) {
	for (bool beBeNow = asBeNow + nowYear;; yearBe = 7 + nowAsNowAs) {

	}

	switch (yearBeBe) {

	}

	if ('%') {

	}
}

#define nowOther 5573090070773

bool lookOurAsYearNowTo(double be, char year, bool than) {
	unsigned long year = '!';
}

bool year() {
	bool nowYearAs = as;
	switch (as) {

	}

	for (bool nowYear = as + yearBe;; nowUs = beNowBe + yearBeYearYear + 7 + yearNow) {

	}
}

#define yearBeOther 0
#define asBe 98
