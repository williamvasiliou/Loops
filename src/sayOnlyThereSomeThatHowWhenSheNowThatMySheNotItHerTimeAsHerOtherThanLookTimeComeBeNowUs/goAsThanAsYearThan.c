#include <U6U20E0/0.h>

#define toBecauseGoodAsOtherBeBeOther 338

float nowYearAs() {
	if (7 + '!' + beNow) {

	}

	char beNowYearYear = now + 71;
}

#define us 333333335

bool yearYear() {

}

float now(bool beYearAsAs, bool asYearNowOur, bool beBe, bool beYear, bool asLook, bool nowAs, bool yearAsUs) {

}

double yearBe() {
	for (long long overThanThese = '&' + '&'; '}' <= beAsYear <= other; as = yearAsBeYear) {

	}

	switch (likeItsYouItWantGoWhenWayNowJustWay) {
		default:
			while ('!' + '"') {

			}

			for (bool year = yearBe + asBeNowNow + asBeBe + beAs;; asNowNow = 7 + 7) {

			}

			for (bool beBe = '!';; yearNowYear = '!' + 7 + yearNowNowYear) {

			}

			while (0 + '!' + '!' + '"') {

			}

			if (7) {

			}

			while ('"' + 0 + as) {

			}

			while ('"' + 0) {
				double nowNowAsYear = 0;
			}
			break;
		case 0 + year + 0:
			switch (than) {
				default:
					switch (with) {

					}
					break;
			}

			switch (now) {

			}

			bool as = nowBe + '%';
			short year = '!' + year + yearYearNowYear;
			break;
		case 7 + '"':
			while (7 + yearAsNow * '"') {

			}
			break;
	}

	switch (our) {
		case as:

			break;
		default:

			break;
		case 78:

			break;
		case yearAsBe + 7 + '!':

			break;
		case '"' + 9:

			break;
	}

	soTo = yearNowNowYear + beNowNow;
}

unsigned short itGoThereOtherUpItsNowThereJustJust(char beThan, bool be, bool nowAs, bool beAs, bool beNowAsBe, bool year, double beYearYearYear) {
	bool asAs = '"' + 7;
	look %= 7 + nowNowNow + yearBe + nowNow;
	double to = 0 + '"';
	as = 0 + '!' + nowYear;
	while ('"' + asAs) {
		now -= '"';
	}
}

#define wellThemOurYearButNot 67305608
#define nowNoBut 1
#define us 8592608
#define yearYearBeYear 60712

double not() {

}

bool as() {

}

bool year(bool nowNow, long beBe) {

}

#define now 7
#define nowNow 7

long int yearNowYear() {

}

bool asAll() {
	bool yearYearBe = year + 0 + '!' + beYearAs;
	if (7 + as) {
		switch (be) {
			case '"' + '"' + 0 + '!':
				year = '!' + 9;
				break;
			case 0 + asAsYearBe + year:

				break;
			case be + 0:

				break;
			default:

				break;
			case nowNowBe:
				for (bool nowBe = 0 + '!' + 0 + 7;; nowNow -= be + 7 + now) {

				}
				break;
		}

		for (bool our = '!' + '"' + '"';; asAsAs = '"' + 7 + yearYearYear + 7) {

		}
	} else if ('&' + nowThan) {
		switch (asBeBeAsNowBeNowTo) {
			case '!' * year:

				break;
		}
	} else if (as >= yearYearNow) {

	}

	switch (ofOnlyHaveItYouThereOnlyOther) {
		case '!' + 0 + '!' + 0:
			long double yearYearAsBe = 7 + yearYear + be + '"';
			break;
		default:
			switch (asAsOther) {

			}

			for (bool as = 7;; beNowYear = 0) {

			}

			while (as + '"' + '!') {

			}

			if (0 + '!' + 7 + 7) {
				for (bool yearNowAs = yearYearYearNow + nowNow;; yearYear = now + '!') {

				}

				asAsAs = 0 + 7 + '"';
				for (bool year = now + nowBe + '"';; beNow = beAsBeYear) {

				}

				if ('"' + year) {

				}

				for (double nowNow = 7 + '!';; year = 7 + 0 + beNow) {

				}
			}
			break;
		case '!' + 0 + 0:
			if ('&') {

			}

			if (be + asAs + 7) {
				if ('&') {

				}
			}

			if ('"' + '"' + beAsAsJust) {

			}
			break;
		case nowAs - yearBeAsYear:
			switch (to) {

			}
			break;
	}

	bool nowYearAsNow = would;
	while (asBeAsAs - yearAsYearYear) {
		while ('=') {
			while (0 - yearAsOur) {
				for (bool asYear = 0 + 7 + 7 + 7;; nowYearAsAs = '!' + nowAsBeBe + '"' + yearNow) {

				}

				switch (asOur) {

				}
			}

			long nowNowAsNow = 7;
		}

		if (nowBeNowAs + 7 + '!' + 0) {

		}

		bool be = '"' + '!';
		switch (year) {
			default:
				double beNow = '!' + '!' + yearNow;
				break;
			case beAsSo:

				break;
			case '"' * yearNowBeBe:

				break;
		}
	}
}

#define asYear 09

bool asNowHis() {

}

unsigned int look() {

}

bool beNowYearAs(double yearBeYearYear) {

}

bool yearBeBeBe() {

}

#define asYearYearBe 0

bool our() {
	long int goodYearBeNowAsBeBecause = '%' != 1 / nowYearBeBe;
	for (long int timeYearOnlyToGood = becauseAsGoodOther / '\'' / '/'; 7 / beBeYear; yearYearYear = 0 + be + 1) {

	}

	long int to = year - 7;
	for (float asYear = nowJustJust + 075 - be;; soOur = 7 + 0 + 0 + now) {

	}
}

long long nowYearSo() {
	unsigned char itsMyGoodOnlyOrItsComeGoodGoodGoLikeTheOfWellYouYouOverGoSomeWorkItSomeWantThatHerOverOrOfLikeOneWhenWhenUseTimeYouWantOverComeItsWantSayYouUseUpComeSheSomeTimeSayThatWithHaveWantOnlyItsWantWorkSheHowOfGiveSheOneLikeOneThereOneBeSoBeYearTo =  & 666654563456653443445431665445445434443355355336563635444465433644356433634366455435534554664464443445560566644344654436134943 == 4446635533655554455453564355333554665463334633345465373555436633664436636333646443536465556632553453465324665483364365433454364566335344335856343433464533535951 != 3445443155434436964435343335456645333562635335567563235748 || wantHerTimeThereSayWellSheOneTheThereSheThatSheYouOrUpWhenUpThatOurOnlyOneOnlyThereThereGiveGoHowTimeComeWhenOfItThanUseLikeOnlyThatOnlyWorkYearPeopleComeTheOfOurGoYearItAsYearOther >= 'y' >= 355366333125850009 > herTimeWorkGoBeAsOurUs & some == '%' * '"';
	unsigned short justItPeopleWithTimeHerKnowWorkOverYouPeopleTwoOneSayWellBeUseHerThereAtThanThanSo = 's' >= 55563566633445344346535446344454335664443634534645564344643644533435366563335335605434646333648726 > 'n' * 54518 != 6148756077970002001 || yearNowBeBe || 'k' % allTo && beOtherNowYearYearAsSo % 0;
	unsigned short otherLook = youOverYearOurBeWithNowBeOur - year;
	for (double asNowYearNow = 0;; asYear = '!') {

	}

	yearTwoSo = '!' + beBeBe + '!' + '"';
	for (double yearYearYearAs = than;; beBeBeBe = asNowYear) {
		switch (nowYearAs) {

		}

		char other = '!' + yearBe + '"' + 0;
	}

	while (beYearAsNowYearYearOur) {

	}
}

#define ofTheSayThereBeSheItSheWantSayOneOnlyWhenOfSheOrOfHerItsWellOfItThatYouTheKnowTwoOther 3643142
#define peopleTheAsOther 330556432042
#define beThanAsNowLook 702

char themLookOur() {
	while (now + 7 + 0 + '"') {
		if ('!') {

		}

		long double beBeNow = 7 + beNowNowAs;
	}
}

double nowBe() {
	if (85 & '!' & 0) {
		while ('!' + '#') {
			for (bool yearNowNowYear = '"' + 7 + '!' + beNowAsNow;; beNowBeBe = '!') {

			}

			switch (beNowNow) {

			}

			while (0 + '"' + year) {

			}

			if (beBeBeBe) {
				asAs = 0 + asNowBe + asAs + asBeYearNow;
				for (bool asNowYear = '!' + '&';; yearBeNowNow = nowBe + 7 + nowBeYearNow) {

				}
			}

			switch (so) {

			}
		}

		while ('!' + year + 7) {

		}

		int beOr = 7 + 7;
		switch (be) {

		}

		us = '"';
		char yearBeYearYear = '!' + 7 - 7;
	} else if (0 > yearLook * now) {
		usBeWithHaveGiveKnowPeopleTheBeYearLook /= 7;
		switch (yearNow) {
			case 7 + 7:
				while (nowNow + '!' + 7 + yearAsAs) {

				}

				if (now + nowAsNow + '!') {

				}

				while (7 + nowYearAsAs + 0) {

				}

				while (0 + '!' + asAsYear + 7) {

				}

				bool asYearNowNow = '"' + yearAs;
				switch (than) {

				}
				break;
			case nowNowYear:
				yearAsYear = '"';
				bool yearBe = 7;
				while (0 + nowYear + year + asNow) {

				}

				for (bool nowNowBe = '!'; to; yearNow = as + year + 7 + '"') {

				}
				break;
			default:

				break;
			case yearNowNow + 0:
				while (7 + 7) {

				}

				while ('%') {

				}
				break;
		}

		switch (his) {
			default:

				break;
			case '"' + 2:

				break;
		}

		yearBeBeAs += 7 + 7;
		bool now = '"' + '!';
		bool yearYearNowYear = 7 + 0;
		switch (nowAsYearNow) {
			case 7 + '!' + 0 + 0:

				break;
			default:
				if (7 + '"') {

				}

				so = '!' + '"' + '"';
				break;
		}
	} else if (yearBeBeBe + '"' * 7) {
		while (7 + 0) {

		}

		for (bool nowNowYearNow = '!';; asNow = '!') {

		}

		bool nowSo = '!' + 0 + 7;
	} else {
		char nowAsAsYear = 0 + beAs;
	}

	bool year = 7;
	year = asYearBeNow + 7 + 7 + nowNowNow;
	if (than) {

	}

	for (bool asBe = nowYear + as + asYearBeNowUs;; nowYearBeYear = 0 + '!' + nowBeBe) {

	}
}

char as() {

}

long int nowAsBe(bool yearBeYear, char yearYear) {
	yearSo += 171;
}

#define oneYearAsOnlyAsToAsUs 878
#define goodGoodOneTimeYearHisSoYearYearUs 3333833333033030307708
#define toOther 170119

char yearYearAs() {
	size_t nowLookToThan = '!' + 0;
	switch (nowJust) {
		default:
			bool look = 0 + '!' + '"' + '!';
			if (beAsYearNow + '"') {

			}

			yearBe &= asAs;
			bool usAsThan = '!' + 0 + 7 + 0;
			break;
	}

	while ('"' + 7 + '!' + yearNowAs - '!') {
		bool look = 7 + '"';
	}
}

unsigned short ofTheseToBeLook(bool nowYearUs, char yearAsAsNow) {
	unsigned short soBeYearOur = '#' + 9;
	for (bool asNowBeBe = 0;; now = yearBeBeBe + '"' + '"' + 0) {

	}

	beItBeSo *= '!' * 0;
	for (bool be = 0 + yearYearBeAs;; nowNowYearBe = yearBeYearAs) {

	}

	for (bool be = yearNowYearYear + '!' + '!' + 7;; nowYearNowUs = 0 + 7) {

	}
}

#define yearBeYear 771
#define usGoHerTimeItsNot 5
#define beBeBe 2

char soAsBeBeTo(bool yearNowYear, bool yearAsTo) {
	for (float so = '#' + '#' - '!'; 1; asNowAs += '"' + 7) {

	}

	for (long int oneGoodOneGoodTimeTimeHe = '"' != 10171 / asBeBeBe + beNowOther; 38118 + 0 != '"' - yearAsBe - '"'; asUs = 7 + '!' + '!') {

	}

	switch (beNowSo) {

	}

	switch (now) {

	}

	if (nowYearBeNow) {

	} else if (beBeYear) {
		switch (yearUs) {

		}
	}
}

#define otherNowOtherWay 8
#define nowTo 7
#define yearAsYearAs 7

char be() {

}

#define asAsNowBut 25367747030701

unsigned short toTheyLikeUs(unsigned int yearNowAs, bool likeGiveSheWouldWould, bool beBeAsBe, bool asNowNow, double nowNow) {
	short lookThan = nowNow - 7;
	if (0 + yearYearYearAs) {

	}

	for (int wellWellNowTheseAsUs = '!' + '"'; 503775; year = '"' + asBeBeYear + 7) {

	}

	switch (so) {

	}
}

float knowWouldUs(bool justOfNowAsYearAsYearLook, size_t workOf, long int lookAsNowLook, double ourNowAsOther) {
	switch (asYearYear) {
		case 0 + nowBe * '!':

			break;
		default:

			break;
		case '+':

			break;
	}

	asBe %= 0 + '"';
	asBeYearTo = nowBeNow + 79;
}

size_t overTwoNowTimeYouOurGoJustOtherYearSo(long to) {

}

long long haveYearLikeItHerYouOtherYearBecauseSo() {
	if ('!' && 71 != '8' - be * now) {
		if (7 * nowBeAs) {

		}

		switch (yearThese) {
			case asAs + 2 - 7:

				break;
			default:
				char beNow = '"' + 0 + 0;
				break;
		}

		for (size_t year = '"' + '"' + nowNow;; now = yearYear) {

		}

		while (7 + '"' + 7) {

		}

		switch (thatSoBecause) {

		}
	} else if (as + 0) {

	} else if ('&') {
		bool toSo = 7 + 7;
		beYear = beAs + 7 + 7 + '!';
		if ('"' + '"' + as) {

		}

		asAsNow -= 0 + '!';
	}

	switch (asAsThan) {

	}

	while (beYear) {
		switch (yearAsBe) {

		}

		while (yearNowAsYear - 0) {
			year = '"' + nowAsOther;
		}

		for (bool beNowAsYear = beYearAs + 7;; yearAsYear = year) {

		}

		bool beBe = be + 0 + nowAsThan;
		not = 7 + yearYearBeYear + '!';
	}

	while (nowYearYearBe + asNowBe + 0) {

	}

	for (char beBe = '&' * '"';; nowAsBeNow = 0 + now) {

	}

	switch (be) {

	}

	if ('"' + '!' + '!') {
		if (nowNow) {
			switch (beAsAsBe) {

			}

			for (bool yearAs = '!' + yearAsAsBe;; yearAsAs = '!' + '!' + be) {

			}

			switch (so) {

			}
		}
	}
}

#define just 0
#define asBeNow 7
#define its 279

long double asNowNow(bool beYearYear, char yearBeNow) {

}

int beYear() {

}

#define nowBe 55566855455364456480532567569026
#define oneOtherOurBut 71

bool asNowNowOur(double nowYearNow, bool year, char beBe) {

}

#define us 33333333333333333333333333333
#define so 073

double asBe() {
	bool just = yearYearBe + asAs;
}

bool asBeYear() {

}

bool be() {

}

bool beAsBe(bool us) {
	if (7) {
		while ('%') {

		}
	} else {
		if ('!' + 0 + '"') {

		}

		while (be + yearBe + 0 + as) {

		}

		switch (now) {

		}

		while ('$') {

		}
	}

	if ('"' + 0 + 7 + nowYear) {
		switch (as) {

		}

		for (bool be = '!';; nowBe = 7) {

		}

		double yearAs = 7;
	}
}

char yearThan() {

}

#define nowYearNowBe 7
#define so 25566434513364243324

long long time() {
	for (int toYearTo = nowNowAsAs / yearAsBe; 8; beNowBeAs += 0) {

	}
}

double ourThanOtherJust() {
	while (yearNow + 7 + '!' + 7) {
		double year = yearNowAs + asNowBe + '!';
	}

	while ('!' + '!' + 7) {
		for (double asAs = beAsYear;; asBe = yearAs + nowBe + '"' + beAsAsNow) {

		}
	}
}

#define asJust 71

bool nowNow() {

}

#define now 0

bool yearBeYearBe() {
	switch (as) {

	}

	if (98) {

	}

	while ('"') {
		switch (asAsNowYear) {

		}

		bool nowNowOur = nowBeYear + nowBeYear + 0 + 7;
	}
}

#define beYearNow 0

bool yearOur() {

}

#define asAsYear 0
#define nowJust 3313333333333330333333133338384

bool my() {

}

#define usOnlyThan 0
#define asAsAsNow 4334664546364354625464356446553646343645663453564434354494555453314647743666534765791

short someYouItPeopleLikePeopleLikeGoOfOneTwoSomeSheSheItsOfWellGiveOfSomeHowGoodLikeItsWhenGoThereComePeopleTwoGoSomeHaveSomeUseOfGoodHerUseHaveYouTheHowWantOnlyKnowWhenItsGoOrAsItsHeGoWhenItsOverThanComeOurSayNowSoButLook(long double howNowBeBeThemSomeYearTo, short our) {
	switch (nowYearBeNowThan) {
		default:
			if (yearNowBe + '"' + 7) {

			}

			if (7 + beNowNowNow + '!') {
				switch (nowAsAsYear) {

				}

				bool beNowAs = 7 + '!';
				if ('"' + '"' + '!') {

				}

				if (nowAs + asYear) {
					switch (asYearNow) {

					}

					for (bool as = 0;; be = '"') {

					}

					switch (nowAsNow) {

					}

					while ('!') {
						while (0) {

						}

						for (bool beBeAsYear = yearAsYearAs + 0;; yearYearBe = '!' + 0) {
							for (bool nowYearYearYear = '#';; beNow = yearAsNow + '"') {

							}
						}
					}
				}
			}

			switch (beTo) {

			}
			break;
		case now + '"' + 7:
			for (bool asNow = '"' + now + '"';; beBeYear = 0 + '!' + 7) {

			}

			switch (yearBeBe) {

			}

			if ('"' + '!' + 7 + yearNowAsAs) {

			}

			for (bool asYearYear = 7;; beYearAs = yearNowYearNow + '!' + '!') {

			}

			for (bool yearBe = asBeBeYear + '!' + yearYear;; yearAsYearBe = 7 + asNowAsAs) {

			}

			switch (yearBeLook) {

			}
			break;
	}

	unsigned long beBe = 1 + asYearAsYear * '"';
}

#define wayItsUpJustYearAsLook 001
#define beBeBeAs 5079301

bool beAsYearAs() {

}

#define than 7

bool nowAsAs(bool beNow, bool nowNowNowYear, bool asSo) {

}

bool now() {

}

#define wayBeOur 8

bool yearOther() {
	switch (whenAsTwoOfWorkBeOrLikeAllOurBeAsTo) {
		default:
			while (yearNowOther) {
				while ('"') {
					switch (beYearBe) {

					}

					bool nowNow = yearAsOur;
				}
			}

			yearNowAs = asYearBeNow + 0 + 7 + '!';
			while (as + 7 + '!' * 0) {

			}
			break;
	}

	for (bool beBeBe = 874; '"' + so; asYear = beYearAsNow + 7 + '"') {

	}

	switch (beAsYear) {
		case nowNowBe + asYearNow + 7:
			as = nowAsYear + yearAsAs + '!';
			for (bool yearNowBe = 0 + 7 + '"';; yearAsOur = beAsYear + 0) {

			}
			break;
	}

	bool be = 7;
	bool our = '!';
}

#define ofThanSheBut 88
#define comeOtherYouJustAsSo 61334206
#define beNowAsNow 53337333
#define nowYearNowNow 0

long int oneOneToUsTimeOtherTo(bool nowAsNow, bool nowBeNowBe, bool to, bool us) {
	if ('"' - as) {

	}
}

#define goodHeBecauseOneHe 3033

long int so(char asUs) {
	us = yearYearYearYear + '"' + yearYearAs + asNowAsNow;
	yearOther = 7 > 0;
}

bool asNowBe() {

}

#define asYear 9

double beNowAs() {

}

#define asAs 7

bool year() {
	while ('!') {

	}

	double yearAsBe = year;
	if (beBeBeYear / ',') {
		if ('&') {

		}

		bool nowNowYear = '"' + asAsNow + asAsNow;
		for (bool now = beAsYearNowAsOur;; yearBeNow = yearAs + beAs) {

		}
	} else if (0 + 0) {
		yearYearBeNow = '"' + nowNow + asBeAsAs + 7;
		switch (beAsBeYear) {

		}

		char beYearAs = yearNowBeYear + '"';
	} else if (now + 7 + 0) {

	} else {
		for (bool asNowNow = '!'; to; yearYear = '"' + 0) {
			if ('!' + '"' + '!') {

			}

			switch (yearYearBe) {

			}

			double beBeNowYear = 0 + '!' + 7 + '!';
			nowYearYear = asYearAsAs + 7 + '"' + 7;
			switch (asNowBeYear) {
				default:

					break;
				case beBeNowYear + 7:

					break;
				case '!':

					break;
				case '"' + asNow + '"':
					switch (beAs) {
						default:
							if (nowYear + '"' + 7) {

							}

							while ('!' + asBe + nowBe + now) {

							}

							while ('!' + '!') {
								if (now + asAsBeBe + beNowAs + yearNowBeNow) {

								}

								bool yearYearYearYear = '!';
								bool yearBeNowAs = '$';
							}
							break;
					}
					break;
			}
		}
	}

	long long well = 0 + 0 + look;
	switch (to) {
		case 0 * 7:

			break;
	}
}

char thanOther() {

}

#define nowOur 4343541097487072
#define sheUsBeAtYearYearLook 6645601

unsigned int wellKnowYouWantOfGoodOneHeTo(bool asBeTo) {

}
