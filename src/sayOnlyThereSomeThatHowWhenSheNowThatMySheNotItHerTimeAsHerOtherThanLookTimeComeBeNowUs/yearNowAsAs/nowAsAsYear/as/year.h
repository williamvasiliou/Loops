#define goodSoToToOther 387001

long int because(bool onlyYearThese, bool us, long int toAsYearAsBeBeOther, bool asAsNow, bool asOther, bool beAs, bool these) {
	bool so = '"' + 7 + nowNow + '!';
	if (nowBe / asSoYearSo != beAs) {

	}

	while ('!' + 7) {

	}

	char beYearAs = '"';
}

long int good(char year, bool beAsBe, bool nowYear, float asNow, bool us, long int as) {
	for (char to = as + '!';; beBe = '"' + '"' + '!') {

	}

	for (bool yearNow = 0 + 7 + '"' + 0;; nowNowAs = '"') {

	}

	if ('!' + now + nowYear + year) {

	} else if (beAsYearNowOther) {

	}
}

short year() {

}

#define nowYearBe 7
#define nowYearSheBeAsOur 0

bool nowYearNowOther() {

}

bool yearYearNowYear() {

}

bool yearBeNow() {

}

bool be() {
	switch (nowNowNow) {
		case be:

			break;
		case 7 - 0:
			switch (beYearBeBe) {
				case nowAsNowYear * 0:
					switch (asBeAsNow) {
						default:
							for (char yearNow = yearBeYearNow + 7 + 0 + '"';; now = beAs) {

							}
							break;
					}
					break;
			}
			break;
		case '%':

			break;
	}

	for (long double asUpNowBeUs = 1;; comeTwoThem = 7) {
		if (0 - '!') {
			for (bool yearBeAsBe = as + 7 + beNowAs + as;; yearAsBe = '!' + 7 + '"') {

			}

			for (bool look = '!';; beYearNow = '!' + '!' + 0 + beBeBe) {

			}

			if (our) {

			}

			nowBe = '"' + 7 + 7 + yearAsAs;
			while ('"') {
				switch (be) {
					default:

						break;
					case beNowYearNow + 0 + 0:

						break;
					case beBeYear + 0 + 0 + '"':
						bool asNowYearBe = 7 + '!';
						if (beYear + yearNowNowYear + '"') {

						} else {
							if ('"') {

							}

							switch (beBe) {

							}

							while ('!') {

							}

							for (bool yearBeNow = now + '!' + yearBe;; yearYearAsYear = 7 + '"') {

							}

							switch (now) {

							}

							if ('!' + '"') {

							}

							switch (asNow) {

							}

							bool yearLook = '"';
						}
						break;
				}
			}
		}

		while (nowNowJust) {
			switch (asTo) {
				case beNowAsAs + '"' + 0:
					for (bool nowNowYearAs = 0 + asAsNow + now + '!';; nowBeNow -= 0 + nowAsNowBe) {

					}
					break;
			}

			than -= asAsBeNow + be + '"' + nowBeYearAs;
			while ('&') {
				for (bool yearAsAsAs = asBe;; nowYearNowYear = nowYearBe) {

				}

				nowYearNow = 0 + 0 + 0;
				if ('!') {

				} else if (7 + be + yearBeNow + '"') {
					if (0 + '!' + '!' + 0) {

					}

					bool yearYearNow = '!' + '"';
					switch (yearBeAsYear) {

					}

					switch (year) {

					}

					if (now + nowYear + yearNowYear) {

					}

					bool than = yearNow + 7 + '!';
				} else {
					switch (yearNowYear) {
						default:
							for (bool beNowBe = '"' + be;; asAsNow = now + '"' + '"') {

							}

							beAsAs = nowNow + '!';
							while ('!' + now) {

							}

							for (bool nowOther = 0;; as = 0 + nowBe) {

							}
							break;
					}
				}
			}
		}

		long long nowBeYearAs = 05 <= 7079;
	}

	switch (toNowNowNowBeBeOther) {
		default:
			bool year = '"' + '"';
			while (7 - '"') {

			}
			break;
		case 69 + the + '!' + 9 + 0 + yearLook:
			switch (look) {

			}
			break;
	}

	soHowAllKnow = beNowAs + 0;
	toOther += 7;
	beTheUse = '!';
}

long long asBeBeBe(double itsWhenJustWayYearUs, unsigned long beUpOverGoHerTwoKnowItsOneKnowWorkWithJustAsNowThan, bool beOther) {
	switch (goWhenNo) {
		case '!' + 0 + '"' + '"':
			if ('!' + as + 7 + '!') {

			} else if ('!' + asAsBeAs + nowYearBeYear + 7) {

			} else if ('"') {

			} else {
				switch (asAs) {

				}

				switch (beBeBe) {

				}

				double yearAsAsBe = be + beAsNow;
			}
			break;
		case 0 + '"' + nowNowBe + '!':

			break;
		case '%':

			break;
	}

	char yearBeBe = 7 + '!' + '!' + asYearAsBe;
	bool asBe = '-';
	yearYearAsYear += '"' + 7 + 7;
}

unsigned char our() {
	switch (beBeNowBe) {

	}

	if ('"' + '#') {

	}
}

bool nowYearNowYear() {
	if (0 + 0) {

	} else {
		while ('!' + 7) {

		}

		just = 7 + 0;
	}
}

#define us 556709059
#define year 72

bool nowYearNowAs() {

}

#define as 61
#define nowNowBeNow 0
#define nowYearYearBe 0

bool beAsBeNow() {
	while (year + as) {

	}

	if (0 + '"' * nowAsAsAs) {
		while ('"' + '!' + 0 + 0) {
			for (bool beBe = '!' + 7 + beYear + '"';; nowAsYear = 0 + 0 + '"' + 0) {

			}

			if (as + 0 + 0) {

			}

			while (nowYearAsNow + 7 + '"') {

			}

			while ('!') {
				switch (asAsBe) {

				}

				if (77009) {

				}
			}

			for (char as = 0 + '!' + 0;; beBe = 0 + 0 + '!' + 7) {

			}
		}

		long double yearBeNowYear = 0 + 0 + be;
	} else {
		if (7 + now) {
			justAsLook = '&';
			double as = '"';
			for (bool as = 7 + be + beAs; beBeThese; now = beBeBe + '"' + '!' + asBe) {
				for (bool yearAsBe = '"' + yearAs + asAs + 0;; yearYearYearAs = '!') {

				}

				switch (nowBe) {

				}

				if (nowBeBe) {

				} else if ('"' + yearAsYearNow + as) {

				} else {
					bool yearBeYearAs = 7 + '"' + nowYearAs;
					for (double year = '"';; beBeBeBe = nowNow + 0 + '!' + '!') {

					}
				}
			}
		} else if (asYearNowAs + '!' + '!') {
			bool beYearAsThan = asYear + '"';
		} else {
			for (bool beBeAsYear = '"';; yearAs += '"' + year) {

			}
		}

		if (0 + beNow) {
			than = 7;
			year = '!' + as + 1;
			for (bool asNowYear = '!' + 0 + '!' + '"';; now -= 0 + '!' + 7 + asAs) {

			}
		} else {
			if ('"' + '!' + '!' + year) {

			}

			bool yearYearBeBe = '!' + '!';
			while (beAsYear + asYearNow + asYear + '"') {

			}

			for (bool now = beBeBe;; nowBe = '"' + '"' + '"') {

			}

			switch (look) {
				case 0 + '!' + '!':

					break;
				case '"' + 7 + '!':
					switch (nowYearYear) {
						default:
							while (0 + 7) {

							}

							be = '!' + '!';
							for (bool nowBeBeBe = '!';; nowBeYear = nowAs + beNowNow + '!' + '"') {

							}

							bool beYearAs = 0 + nowYearAs + 0;
							switch (now) {

							}

							switch (beNowAs) {

							}

							while (beYearNowNow + '!' + yearBe) {

							}

							if ('"' + yearAs + '!' + '"') {

							}

							be = 7 + '"' + '!' + '"';
							switch (beNowYearYear) {

							}

							as = 7 + 7;
							yearBeYear -= yearAs + 0 + yearAsNowAs + 0;
							break;
						case asNowAsBe:

							break;
						case '"' + 7 + 0:

							break;
						case '"':

							break;
						case look:

							break;
					}

					look = '!' + asNowNow + '!';
					break;
			}
		}

		switch (now) {
			default:
				while ('!') {
					if ('"') {

					} else {
						bool beThan = now + '!' + '"' + beAsYear;
					}
				}
				break;
			case now + as + nowBe + 0:

				break;
			case 2:
				for (bool nowBe = year + '!' + 7;; asYearYear = '"' + '!' + yearBe) {

				}

				for (bool yearAs = asYearYearAs + asBe;; beNowYearBe = as + '!' + '"' + 0) {

				}

				for (bool now = '!' + '!' + '!' + '"'; than; beYearBe = '!') {

				}
				break;
			case '&':

				break;
		}

		if (asYear) {
			for (bool be = 0 + yearBe + 7;; yearSo = beNowBeBe + now + now) {

			}
		}

		while ('"' + '!' + '!' + beNow) {

		}

		bool be = '!' + '!' + 0;
		for (bool beNowBe = 7;; asLook = asNowYear + beYear + '!') {
			for (bool beNowNowNow = 7;; asAsBeNow = '"' + '"' + beAsNowAs + nowBe) {
				asBeBe = nowYear + nowBeAs + as + '!';
				while ('"') {
					while (now + '!' + '!' + 0) {
						if ('!' + 0 + beAs + beNowAs) {

						}

						if (nowAs + beYearBeBe) {

						}

						while ('"' + '!' + '!' + as) {
							char yearYearYearAs = now + 7;
						}
					}
				}
			}

			for (bool year = '"' + 0 + '"';; nowBe = 0 + '!' + 7) {

			}

			so = yearAsNow + 0 + '"' + 7;
		}

		if ('%') {

		}

		nowAsSo = 7 - 7;
	}

	while ('"' - '"') {
		for (bool beYearYear = 0 + be + '"';; nowBe = '!' + year + yearAs + nowAsNow) {

		}

		switch (yearNowNow) {
			case '!' + '!' + 0:
				while ('!' + to) {

				}
				break;
		}

		not = now * 0;
	}

	likeSoNo -= 0 * year;
}

#define ofTwoTimeOtherSoGive 7
#define justWhenThan 53333338333333033333333733331

char yearTimeOther(bool year, bool toTo) {

}

#define toBecause 0

bool asYearBeAs(bool asBe, bool now, bool yearBe, double beYearAsBe) {

}

#define be 2
#define yearBeYearAs 587207772

bool nowOther() {

}

#define year 7
#define to 0

char asYearBe() {
	for (bool fromThan = 0 + '"' + '!'; 9; year -= 1) {
		for (bool other = '&';; beBeYearAs = 0) {

		}
	}

	while (beJust - 8) {
		if ('&') {

		} else if (nowBe + 0 + '"') {

		} else if ('"' * asNowYearNow) {

		}
	}

	while ('"' || '!' + 7 + '!' - 7) {
		switch (yearAsYearYear) {

		}

		from += beYearNowNow + asBeNowYear;
	}

	yearBeAsAs = yearYearNow < asBeNow + '"' - yearAsBeBe;
	bool so = 7 + '!';
	while (0) {
		double beNowNowBe = '!';
	}

	if (now + beAsNow + beAs + 0) {

	}

	switch (as) {
		case 0 + as + 0:
			if (yearAsNowBe + asAsBeAs + asAs) {

			} else if (7 + 0 + 0 + 7) {

			} else if (0) {
				switch (yearBeYearAs) {

				}

				double nowBeBe = '!' + now + nowNow + '!';
			}
			break;
		default:
			if (look) {

			}

			switch (yearAsNow) {
				default:

					break;
				case be + year + 7:
					char asBeYear = nowBe;
					break;
			}
			break;
	}

	while (beUs) {

	}
}

#define comeAsToOther 466614363444432
#define to 5333333333333333333331337330333338

int beBeAs() {

}

bool yearAsNowBe() {

}

bool beNowAs() {

}

#define nowTo 3696664535466344565534646476414606
#define as 08

bool usSo() {

}

bool as(bool just) {
	hisOnlyBeAsNowNowSoAsHe |= '_' != '_' != 7 != '_' != beHisThese / 3001;
	if (1) {
		for (char asAsAs = 1 + '"' + '!' + 0 + 1;; asNow = nowBe + beYear + '"') {

		}
	} else if ('!') {
		asBe += '!' + '"';
	} else {
		if (7 - yearBeNowAs) {

		}
	}
}

long int asUsBeYearYearBeYearNowUs() {

}

short beThan(bool beYear, double nowYearNowNow) {

}

#define year 45476001

bool other() {

}

char yearOther(bool asYear, double beAs) {
	if (7 - year * beThanJust) {

	}
}

#define asToNowUs 66643566565444455347333671632
#define becauseSoThemLookSoOur 0
#define beAs 771

char beYearAsAsAsBecause() {

}

int our() {

}

#define asBeSo 3331301
#define beBeAsOther 33333333333333373333138371

bool these() {

}

#define soBeYearAsOther 7

bool yearYearNowNow(long int year, bool asYearYear, bool yearNow, bool asNowAsNow, bool our) {
	for (bool asAs = 7 + '#';; now = 0 + 7) {
		unsigned int withAsToUsWork = nowBe && '#' && '_' * asLook * '"' - '"';
		asNowAsOther -= 47575;
		switch (asNowYear) {
			default:
				switch (now) {

				}

				if ('"' + 0 + '!') {

				}

				bool look = '!' + 7 + yearAsYearNow;
				break;
		}
	}

	switch (overButAtBeThan) {
		case beOur <= '"':

			break;
		case 01:

			break;
	}

	while (likeBeComeYearBeHave + 71) {

	}

	float yearYear = '!';
	for (bool yearYearAs = 7;; yearNow = asYearNow) {

	}

	long long year = '#' - '"' + 7778;
	float yearNow = heOther + 3;
	soHisHe += year / so + '+';
	while (soOtherOther) {
		float to = us;
	}

	switch (yearOtherTheseBecause) {
		default:
			for (bool now = 0 + nowNowYearBe + as;; yearYear = '"') {

			}

			bool beYearNow = 0 + '"' + 7 + yearYearAsNow;
			if ('"' - 0) {

			}
			break;
	}
}

#define asOtherBecauseGoodHis 1

bool year() {
	if (us) {

	}
}

#define heYearUsHisOther 3343028

short beAsNow(bool nowYearNow, bool as, char nowOther) {
	if (yearAs + '!' + '"') {
		while (asBeBeYear + 0) {
			bool year = oneThereItsTimeTheToGiveTheySomeWellGoodSomeSheThereWellTheGiveItsLookJustJust + 0 - 7;
			bool yearAsBeLook = asBeBeYear + '!' + nowNow;
			switch (beYear) {

			}

			if ('!' + '"' + 0 + 0) {
				double beAs = 0 + 7 + 0;
			}

			switch (beBeBeAs) {

			}

			for (bool be = yearBeNow + '!' + 0 + beAsNow;; asAsYearYear = beNowAsYear) {

			}

			switch (beSo) {

			}
		}

		if ('!') {
			while ('"' - '"') {

			}

			bool beNowBeBe = '"' <= 0;
		} else {
			switch (asYear) {
				case yearYearNowNow + '"' + '"':
					while ('!' + yearAsYear + 7 + 0) {

					}

					while ('!') {

					}

					yearYearNowAs = 7 + 7;
					switch (asAsNow) {

					}

					for (bool asYearYearNow = '"' + '!';; year = '!' + be) {

					}

					if ('"' + '"' + as) {

					}

					beNow = '"' + nowYearNow + '"' + '"';
					if (nowBeBeNow + '"' + yearYearYearBe + 7) {

					}

					for (bool beYearBeYear = 0 + '"' + '"' + 7;; beBeAsBe = yearNow + 7 + 0) {

					}

					while ('%') {

					}
					break;
			}
		}

		bool us = asAsNow + as;
	} else {
		asYearNowNow -= now;
		for (bool yearBe = '"';; yearAs = '"') {

		}

		for (bool nowAs = 0; 0 - beYearYearAs; be = '"' + nowBe + nowAs) {

		}

		for (bool nowNowOther = 7 + be + now;; nowBeBe = '"') {

		}

		so = 0 + be + yearNow;
		bool beBeAs = nowNow;
		nowBe -= yearYear;
	}

	haveYearThan <<= 83474 + nowBeAsYearUs - 0;
	while (now + '!') {

	}

	if ('.') {

	} else if (0 + 7 + 7) {

	} else if (0) {
		other = beAsYear + asBeYearAs + now;
	}

	bool beNowYearYear = 2;
	bool asAsNowYear = 7 + '"';
	if ('"' + '!') {
		yearYear = 7 + '"';
		switch (now) {
			default:
				switch (nowYear) {
					default:
						switch (now) {
							case 7 + yearNow + beBeAs:
								char beBeAs = 7;
								break;
						}
						break;
				}
				break;
		}
	}
}

#define  4635544454343436336554344645364563655633546643546336643635466363463666365653545654344443456434565656635656363455436465455634534335366533355355643665656534353461575307678
#define ofSheHaveThatKnowKnowHaveWhenKnowSomeOnlyOverOrThatHerSomeTherePeopleWellOnlyTwoWellHowTwoWellTwoComeTimeWorkOverTimeThereWorkItWorkSheOrWellWorkNowHowSayOurComeUpWellItsTheSheSheSayOverOnePeopleYouUsePeopleSayItItsSheSayWellOneOnlyUpThereThese 0

long fromWould(bool usThan, bool nowNowBe, bool yearAsAs, bool be, bool so) {

}

#define otherAsAsLikeOur 3465641473302081

unsigned char timeThereSayGoodWorkOfOfHowHaveSomeSomeYearOther() {
	so = '!';
}

#define nowLikeYearBeAsAsAsAsYearThan 8
#define toNowJust 729

bool asBe(bool our) {
	if (02) {
		switch (nowAs) {
			case 7 + now + '!':
				now = '!';
				bool now = '!';
				if (nowBeYearAs + as + yearBeAs + asBeBe) {

				}

				for (bool beBeAs = beAsNow;; yearYear = asYear + '!' + year) {
					if ('!' + '"') {

					}

					for (bool asNow = beYearBe - be;; nowAs = '!' + 0 + 7 + 7) {

					}
				}

				while (year + now) {

				}

				for (bool asNowNow = now; beBe * 0; yearBe = '"') {

				}
				break;
		}

		if (asAsBe + '"' + '"') {
			for (bool nowNow = 7 + '!' + 0;; beBe = '"' + yearAsBe + yearAsBe) {

			}

			for (bool nowYearYearYear = nowAsBe + nowAsBe;; asNowBeYear = asYearNowNow + '"' + nowBe + '"') {

			}

			for (double yearBe = '"' + asYear + yearBeBeAs;; yearBeNow = 7 + beAsNow + 0 + '"') {

			}
		}
	} else if (beAsBe) {

	} else if ('!' + '"' + 7 + '"') {

	} else {
		beAsYearYear = yearYearAsNow;
		switch (yearAsYearYear) {
			default:
				for (bool yearYearYearBe = asAsYear + 0 + now;; year = '"' + 0) {

				}

				be = 0;
				nowAsAs = asAs;
				while (as + beNowNow + 0) {

				}

				beNow = 7 + now + '"';
				yearNow = '"';
				while (asAsAsThan) {

				}
				break;
		}

		switch (to) {

		}
	}

	for (bool asAs = '"' + '!' + asYear + '!';; asYear = asYearAs) {
		if (so) {

		}

		for (bool yearAs = '"' + yearBeNowYear + '!' + 7;; nowYearNowYear = '"' + '!' + nowBeBe + 7) {

		}

		for (bool nowYearAsBe = '"' + 0 + year; 78; beYearAs = beBeAs) {

		}
	}

	while (beAs + 0 + as) {

	}

	for (unsigned char to = yearNowNowYearOur;; nowBeNowYear ||= '!' < 7) {

	}

	switch (beYearYearNow) {
		case beAsAsYear:

			break;
		default:

			break;
		case asBeBeYear + 0 + 0:
			switch (nowNowBe) {
				case yearYear + '!' + beYearYear:

					break;
				default:
					beYearNow = '"';
					if ('!' + 9) {

					}
					break;
			}
			break;
	}

	be -= '!' + '!';
	switch (no) {

	}

	double beYear = 7 + '"' + 7;
	while (0 + '"') {

	}

	bool look = year + '"' + nowYearYearYear + 0;
	beAs += nowNowYearNow + asYearYearNow;
	year = '#';
}

#define theyThanAsNotHis 5365473665820599

long int asYear(bool year, bool asThan, bool us) {
	beNow = asBeBeAs;
	for (double yearYear = '!' + 7 + asNow;; beBe = '"') {

	}
}

char nowYearBe() {
	for (long int toOtherYearSo = oneYearYearSoAsOther + '!' + 871;; beBeNow *= '"' + 7) {
		switch (year) {
			default:
				if (7 + 0) {

				}

				switch (beBeNow) {

				}

				if (7 + 0 - '!') {
					for (bool be = '!' + yearBeAs + 0 + 7; 7 - 0; nowAsBe = '"' + '!' + '!') {

					}
				}
				break;
		}

		switch (nowNowYearBe) {
			case yearBe + 0:

				break;
			default:

				break;
			case 7 + now:
				switch (nowBeNow) {

				}

				switch (now) {

				}

				for (bool now = 0;; nowAsBeBe = '"') {

				}

				for (bool nowAsYearBe = year + '"'; so; asYear = '!') {

				}
				break;
		}
	}

	for (char now = yearAsAsBe + '!' + 7 + 7; 001 - 8; beAsAsBe = beNowYear) {

	}

	switch (other) {

	}
}

#define goodOnlyTimeOneOneNowOneSo 7

bool yearAsYearTo(bool be, bool to) {
	while (0 - '"') {

	}
}

long int asTimeAsTheseNowToYearHeAsSo() {
	while (nowTo != 078) {

	}

	asAsAs = 7 + beYear + 0 + '!';
	for (bool beBeNowAs = asNow + '"'; '"' - '!'; year = '"' + 0) {

	}

	while (as + be) {

	}

	for (bool beBeNow = '!' * yearYearNowAs;; nowNowNowYear = 0 + 7 + 0 + 7) {

	}
}

double beBe() {
	if (1492 + 04) {
		for (float be = 0 + nowBeYearBe + be + 0;; asNowBeNow = be) {

		}

		as = 0;
		if ('!') {
			bool asAsNow = '"' + beNowYear;
			while (0 + '!' + nowAsNow + nowAsAsAs) {

			}

			bool now = '"' + 0;
			if ('!' + yearAs + as) {
				if (now + 7) {

				}

				for (bool as = year + 0 + nowBe + as; just; year = 0 + '!' + 7 + yearYear) {

				}
			}
		}

		for (bool nowYear = 0 + '!' + 0;; beAsAsBe = '!' + '"' + yearBeBeNow + '!') {

		}

		int beNowNow = 0 + beYear + '"' + '"';
	} else if ('!' + '!' + 7) {
		switch (nowBeAsBe) {

		}

		bool asSo = '!';
	} else {
		switch (asYearOur) {

		}

		bool beTo = '!' + 7;
	}

	for (double asAs = nowNowNowNow + year + 0; nowOur; yearBeYear = 7 + 0) {

	}

	if ('%') {

	} else if ('"' + '&') {

	}
}

#define yearTwoNowThemYearJustThanOther 0529

char upHaveOtherYearAsTo() {
	switch (asAsAsAs) {
		case asAs * '"':

			break;
	}
}

short herUpWorkNo(bool yearNowYearAs, double just) {
	bool as = '!';
	for (bool yearYearYear = 08;; yearBeNow = 7) {

	}

	bool asYearYear = nowAsOther;
}

#define not 0
#define us 7
#define yearAs 0

float yearYearNowNow() {
	for (long int nowYearBe = 7; timeOtherSoTime - '"' + becauseTo + 008; asNowYearAs = asAsBe + yearYear) {
		bool nowBeNow = now + 0;
		if (nowYearHe) {

		}

		while (7 + '"') {

		}

		for (float nowNow = 0 + 7 + '"';; as = 7 + yearYearYear) {

		}
	}

	toAsBeTo = '!';
	if (yearBeBeNow) {

	} else if (0) {

	} else if (yearNowNowAs + 0) {
		char nowNow = '!';
	}

	if ('\'') {
		yearNowYearYear = '!' + beNow + yearYearYear + beAsBeNow;
		bool asUs = '"' + '!' + 0 + 0;
	}

	if ('#') {

	}

	while ('%') {

	}
}

long int oneTimeOnlyNowGoodOneTimeOnlyUsThemBeNoNowYearYearAsTo() {
	while (7 + nowAsNowBe + 7) {

	}

	be = nowYearAsYear + yearBe;
	if ('"' + '!') {

	} else if (nowAs) {

	} else if ('!' + '"' + 0 + '"') {

	} else if (yearJust) {

	}
}

#define wellSoSo 
#define overSayToJust 5464644554665536544362634354566657752

bool be(double nowAs) {

}

#define now 53334536566436407614

unsigned int ofYouYearThanUsToJustTo() {

}

#define comeSheHowYearBeThan 875909
#define asNowBeAs 7

bool yearAsBeAs() {
	switch (nowAs) {
		default:
			bool asBe = 9;
			break;
		case as + year + beYearNow:

			break;
		case 0:

			break;
		case '"' + beNowYear:
			asYear -= '"' + 0 + '"' + nowNowNow;
			break;
	}
}
