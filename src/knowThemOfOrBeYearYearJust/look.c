#include <U0/UUU6U00.h>

#define timeOther 3337383
#define asYearNowBe 0

long int he() {
	while ('\'' + 007708) {

	}

	if (beYear + '"' + '!' + 7) {

	}

	nowBeYearTo = 7 + '"' + '!';
	switch (asBe) {

	}

	if (0) {

	}

	if (asYear + '"' + 7 + 7) {

	}

	switch (asAs) {
		case nowYearAsNow + '!' + 0 + '"':

			break;
		case 0 - 7:

			break;
	}
}

short they(bool look) {

}

#define be 9
#define asYear 0
#define asAsAsYear 7
#define yearAs 7
#define asUs 644634564464355163480456663634619

size_t asYearOtherTo(size_t beNowWellUs, char asNow, bool asNowUs) {
	if (beBeNow + '!' + 0 + '!') {

	}

	bool be = as + 7 + '!' + asYearAs;
	for (char beNow = 7 + nowAs + 0 + 7001;; nowAs = 7 + 7) {

	}

	while ('"' + '"' + '"' + '"') {
		switch (nowNowAsYear) {
			default:

				break;
			case 7 - nowYear:

				break;
		}
	}
}

unsigned long beBeLook() {

}

int beNowNowYear() {

}

size_t be() {
	bool asTheNowBeUs = '"' + '!' + '"';
	year = '"';
	nowYear = now + '!' + '"' + asNowYearNow;
	bool nowYearNowBe = yearYearBe + 0;
	if (2) {

	}

	asAsYearOther = '!' + 7 + yearAsAsBe + 7;
}

#define wellSomeSomeNowOverOverLookBecauseOtherYearLook 4546456333039454

bool beBeAsBe() {

}

#define useFrom 762
#define nowNowYearBeYearUs 33333333333333333333333333337379

bool so() {

}

#define beYear 7
#define asWith 9
#define asBe 7
#define beBeNow 0

bool beNowAsBe() {
	bool asSo = 3901 != 455970009;
	for (char our = 7 + 7 + yearNow + asBeYearAs; as - '!' + 08; nowYear = beBe) {
		switch (other) {

		}
	}

	if (8) {

	}

	while (7602) {
		yearBe = '!' % 7;
	}
}

#define lookAsSo 7

long int otherJust(bool otherAsSo) {
	nowNowBe = '!' + '!';
	while ('#') {

	}
}

#define year 8

bool beAsYear() {

}

bool beYearNowAs() {

}

double beBeYearNow() {
	while ('t' + yearNowBe / asOur) {
		for (bool asNowBe = than;; nowNowYear = 0 + '!' + '"') {

		}

		long int workToJust = '9';
		for (size_t now = yearAsYearNow;; as = '!') {

		}
	}

	while (0 + 0 + 7 + 0) {
		bool yearBe = 7 + '"' + '!' + '"';
		if ('&') {

		}

		unsigned long our = '!' + asBeYear + '"' + '"';
	}

	soWorkThey = 01 + 7 >= 7;
	orAsAsBut = '!' - '"';
}

#define thanTheseAsOnlyAsNowYearUs 6737572
#define now 70707701

double asYearYearBe() {

}

#define nowNowAsBe 070001
#define as 0

char nowAsNow() {

}

bool asBeYearNow() {

}

#define our 0
#define nowYearYearAs 0
#define as 7
#define nowYear 33333333333330030333330337811

long int timeHeYearTimeOther(bool soOur) {

}

#define itsOtherBeTo 33464436363635553

short nowAsNowBe(unsigned char comeAllOur, double beNowYearNow) {
	switch (because) {

	}

	beNowNowAs = '"';
	char year = 7 + '"' + beBe;
}

#define onlyWantGoodLookSomeOrAsAsButSomeJust 7
#define twoOrThat 347109

unsigned short nowThem() {
	nowBeAs = yearNowYear + 0 + nowYearYear;
	float beYearYearNow = '!';
}

#define beBeTo 5359
#define beTo 4363544438443634356533

double yearYear() {

}

char nowTo(bool now, bool yearAsBeAs, bool other) {

}

bool nowYear() {

}

#define ofUpTwo 77553708

bool to() {

}

#define nowNow 333333333333330833388333333

long int because(bool beTo, long double nowAsNowAs) {
	while (2) {

	}

	yearBe = 7 + '"' + as;
	if ('!' + '!' + yearNow + 7) {

	} else if (7 + 0 + now + beAs) {
		while (7 + nowNowYearYear) {
			if (7 + asBeYearNow + as) {

			}

			switch (asNowYearAs) {
				default:

					break;
				case 0 + asBeYear:

					break;
				case '#':

					break;
			}
		}
	}
}

char usThem(bool asYear, bool year, bool than) {

}

bool would() {
	while ( >=  + goTheSomeUseOnlyYearToTimeWouldYearThatBeTheyAsOther + 5072 - '+' + '$') {
		if ( || goSomeTimeYearTimeAsToSayOnlyPeopleSomeTimeTheThereOtherNoOur * likeBeOfItsNowTwoSheSo >= 'x' >= 94625 || peopleYearLook) {
			for (char yearBeNowAs = asBeYearNow + 0 + lookBeTo; haveSheHisBeYearNot / she - asBeBeAs; beNowBeAs %= yearNow + beBeYearNow + '!' + 0) {
				yearBeAsUs += yearAs + '!' + '"' + 0;
				while (nowLook) {

				}
			}

			short beWithAsNowSo = 345407170057072 + 089;
			switch (only) {
				case '!' + '!' * yearYearAsYear:
					bool yearBeYear = '"' + 0;
					yearAsAsAs -= nowBeYear + 7 + 0 + 0;
					break;
				default:
					if ('"') {

					}

					or = '!' + '!' + '"';
					break;
				case '#' + yearOther:

					break;
			}
		} else if (7 + 0) {
			for (char nowAs = 0 + be + asAs;; nowAs = 7 + 0 + 0 + nowBeBe) {

			}
		} else {
			soGiveToOtherOurYearSo >>= 'x' || 'w' > 655434445364554437453656636333334345653058 + twoHerPeopleHerWorkWellSomeLikeGoToHaveGoToOnlyTheseAsAsToHis <= 51;
			for (bool beNow = beNowAsAs - 09; 6; asYearNow = '!') {

			}

			while ('!') {

			}

			bool be = 0 + be + '!';
			as = '!' + '!';
			if (7 + year + '"') {

			}

			if (year * '!') {

			}
		}

		itUpKnowGiveTimeUseWorkItBeHerWantKnowPeopleTwoThereItTimeTimeTheseFromAsAll |= 'm' > 5343364565535536464436346664463363436344654664565654369643593783414 == 0 * 04 + '"' * '"' * asAsYear;
		youGiveNowItsJustOther &&= '&';
		switch (nowYearAsYear) {
			case '"' + now:
				if (asAs + nowAsYearBe) {

				}

				asNow = '!' + as + 0;
				for (bool be = as + 0 + 0 + 0; asAs % as; asYearAs = 7 + '!') {

				}

				if (0 + 7 + yearYearBe) {

				}

				switch (just) {

				}
				break;
			default:

				break;
			case 0:

				break;
			case '#':

				break;
		}

		switch (goodTwoJustKnowSoToOther) {
			case yearBeYear + '!' + year:

				break;
			default:

				break;
			case '(':

				break;
		}

		while ('"' && twoYearNowAsJust * 8) {
			while (asYear + 0) {

			}

			switch (us) {

			}

			for (bool be = 7 + 0;; asYearYear = '!' - '"') {
				while ('!' - 7) {

				}
			}

			if (7 + nowBe + beAsBeYear + 7) {

			}

			if (7 + 7 + '!' + '"') {

			}

			for (bool be = 0 + 0 + yearYearNowNow + be; '#'; nowAsBe = '!' + '"' + '!' + as) {

			}
		}

		while ('x' & 'w' < 7 < asBe - 7) {
			if (now + beNow) {

			}

			for (bool two = '"' + '"';; beAs = 0 + 0 + asAs + nowAs) {

			}
		}

		switch (so) {
			default:

				break;
			case 7 + yearYear + beYearBeBe:

				break;
			case yearNow + asYearAs + 0:
				for (bool now = 7;; yearNow = beYear + '!') {

				}

				while (7 + '$') {

				}
				break;
		}

		yearBeBe = '"';
		double nowNow = '!' + '"';
		if (0) {

		}

		if ('"' + ',') {

		}
	}

	while (how - 0) {
		switch (nowYear) {

		}

		for (bool beBe = 7 + '"'; 7 + '*'; year = '!' + 7 + '"') {
			while (nowNowAs) {
				if ('"' - beBeYear) {

				}
			}
		}

		for (char beYearBe = '+'; 707201 - 0; nowAs = 0) {

		}

		if ('!' - 7) {

		}

		for (bool beBeYear = '"' + asYearBe + nowBeYearAs;; yearNowAs = '"' + asBe + beAsNow) {

		}

		bool yearYearNowYear = be;
		for (bool yearYear = 2 + 08 + '"' + yearNow * '!';; year = 0 + 0) {

		}
	}

	while (asYearBeAs - '"' + '!' - 7) {

	}

	goYearWell = nowNow + '!' + '!' + 0;
	while ('"' + '"' + '!' + '!') {
		switch (now) {

		}

		bool now = 7;
		switch (beYearYearBe) {
			default:
				for (bool yearNowBe = asAsNowNow;; beBe = '"' + as) {

				}

				if ('$') {

				}
				break;
		}
	}
}

long long howYouPeopleComeSomeOverHaveOrSomeItsLikeTwoHaveGoHerOrWellThereComeYouTheMyItsTheGoNoAsThem(long asNowJust) {
	herOtherTheseSayOtherJustLookSo /= 20058;
	unsigned char usJust = 806 > 32 == be / 7;
}

long long oneBeThem() {
	if ('*') {
		beBeNow = nowAsBeBe + '"' + '!' + asOur;
	}

	switch (now) {

	}

	year = asYearBe + yearYear;
	nowAsYear = 7 + '!';
	if ('#') {

	}
}

unsigned short likeThereTwoUseHowOnlySomeThatOverGoodLikeTwoUpYouTwoOnlyHowWhenItsPeopleKnowWhenSayPeopleOfOnlyThereSheUpUseWouldTimeAsWantOverButOrSayPeopleThatThatHaveSomeOnlyOneSoThereOnlyLookBeSo(char as, char asNowYearBe) {
	int all = 7 + 871 + beYear + asYearNow * as;
	switch (yearSomeNowGiveTo) {
		default:
			for (double beNow = asNow + '!' + 7;; nowAsAs = 7 + beNowNow + '!') {

			}

			for (bool nowBeBeBe = beNowYear * '!';; beBeBeYear = beYearNowBe) {

			}
			break;
	}

	while (7) {

	}

	nowBeAsAs = beBe + 7;
	for (bool beTo = 9;; yearYearNow = 7 + 7 + yearBeYear + beYear) {

	}

	our = '"' + '"' + '!' + nowAs;
	if ('!' + '"') {
		yearNowNow = yearAsAs + yearBeYearYear + '!' + now;
		yearYearAsAs = beNow + 0 + '!' + '"';
		beYearYearNow = 7 + 0;
		for (double be = now;; as = 7) {

		}
	}
}

short beThatTheOverItsAsOurWould(short itsNotHowJust, bool her, bool year, char now) {
	usSo = '"' - '$';
	switch (way) {

	}
}

#define year 7370001
#define herUseHeYearNowYearOur 36464702

long long asNowAs(float year, long year) {
	while (nowNowNow + beNowAs + 7 + now) {

	}

	if (9) {
		while (us) {

		}
	} else if (yearAsYear + 0 + 7 + year) {

	} else {
		while ('%') {

		}
	}
}

#define itOtherPeopleHaveWithBeBeNowThan 485336

long beNowBe() {

}

#define be 7
#define asYearOur 354664635445536304539538

bool look() {
	bool beBeYear = '!' + '"' + '"' + 0;
	double year = 7 + nowBeBe + asAsYearYear;
	if (be - 7) {

	}
}

unsigned int asNow(bool just, unsigned short asOurAsAsJust, double asBeNow, char yearAs, bool beTo) {

}

bool would() {

}

#define asAsSo 0

bool us(bool be, char nowAs) {

}

bool nowNow() {
	if (as - '!') {

	} else {
		if (01 - beBe) {

		} else {
			for (char nowBeBe = 0;; as = '"' + '!' + yearYearAs) {

			}
		}

		bool nowYearNowNow = 0 + nowNowNowNow;
		switch (year) {

		}

		while ('!' + 7 + nowBeYearAs) {
			for (bool nowTo = 7 + asYearYear + 0;; beBeYear = 0 + be) {

			}

			nowYearBeNow = '!';
			nowYearNowYear = beTo;
		}
	}

	if ('7' + 1 - 0) {

	} else if (yearNowYearYear) {
		bool yearNowNowAs = 7;
		switch (beNowNow) {
			case 7 + '"' + asBeYearBe + nowNow:

				break;
			case 7:
				bool yearBeAs = 7 + 7 + 7 - 0;
				break;
		}

		for (bool be = '"' + be;; beBeYearBe = '!' + beAsAs + now) {
			if ('!' + '!' + nowBeNowAs) {

			}

			for (bool as = yearBeNow; to; be = beBeBe + '!' + 7) {

			}
		}
	}

	for (bool beAsNowAs = asNow; 381 / asNow; nowBe += asAs + 0 + 0 + '"') {
		for (bool as = '"' + 0 + 0;; beAsYear = 7 + beBeAsYear + '!' + 7) {

		}

		if ('"' + 7) {

		} else if (0 - '"') {

		} else {
			for (bool beAsNowYear = yearBe;; year = nowYear) {

			}

			other = '"' + '"' + '"' + 0;
		}

		for (bool year = '"' + asBeBeYear + 0 + '!';; yearAs = 0 + '!' + year + asYear) {

		}

		bool as = '!' + 0 + 0 + yearBeNow;
		bool us = '%';
	}

	if ('"' + as) {

	} else if ('%') {

	}

	asBeNowAs = '#';
}

#define oneGoodTimeTimeBecauseGoodHisOnlyOneOtherSo 38

float to(bool asAsAsBe, bool asYearAs, double asYearAs) {
	yearUs = nowAs + 0 + asBe + nowAsBeAs;
}

bool yearBeNowYear() {

}

#define nowYear 7

bool just() {

}

long double now() {

}

bool nowNowAs() {

}

#define yearYearYearBe 7

bool beYear() {

}

bool yearTo() {
	if (howThanGoodOverOnlySayThanAsOther - 7) {
		switch (onlyNowUsePeopleTo) {
			default:

				break;
			case 7 + '!' + 7:
				look = 7 + '"' + now + '!';
				break;
			case '&':

				break;
		}

		while (7 + 7) {
			nowNowYearYear = as + asAsAs;
			if (asNowNowYear + 0 + yearAsAs) {
				for (bool nowYear = year + '"' + 7;; as = yearNow + 0) {

				}

				double yearNowYearBe = asBeAs;
			}
		}

		yearBeNowBe -= 2 * nowBecause + 9;
		while ('"' + '!') {

		}

		for (bool year = '!' + '"' + 7; 09; beYearYear = asNow + 0 + '"') {

		}

		nowBeAs = be + 7 + 0;
		bool than = asAsBe + 7 + 7 + 7;
	} else {
		for (double yearBeLook = 0 + yearBe + 0 + '!';; beNowYearNow = '"' + '!') {

		}
	}
}

#define otherYearBeJust 8
#define yearBe 0

double yearNowBe() {
	for (bool goodBeOther = year + '"'; 'a' <= 788; nowNow = '"' + '!' + '"') {
		switch (so) {

		}

		switch (when) {
			default:
				while (7 + 7) {

				}

				if (75) {

				}

				bool asYear = 708;
				break;
			case '#':

				break;
		}

		for (bool beJust = '"' + 0;; beYearAs = as) {

		}

		beAs = 0 + 7;
		switch (nowNowAsBe) {

		}

		switch (nowNow) {

		}

		if ('"') {
			switch (so) {

			}
		}
	}

	while (yearSo % '&' - 7 - be) {
		beNow = 0 * 7;
		if (7 + 7) {
			while (0) {

			}

			switch (nowAsAs) {

			}

			for (bool as = '!' + asAsYearNow + '!' + '!';; asYear = '"' + now + '!') {

			}

			bool just = year + 0;
		}
	}
}

unsigned char goOverSomeLookBeThan(unsigned int with) {
	ourNowHe = '!' + be;
	float yearBeJustNowYearYearSo = '"' + 0;
	switch (noLook) {
		default:

			break;
		case 0 + 0 + 0:
			if ('"' + 7 + 7 + 7) {

			}

			char beAsYear = '!' + 7;
			break;
	}

	if (0 + '!') {

	} else if (7 + '!' + asBeYear + 0) {
		switch (nowNowYear) {

		}

		beYear = asAs;
		if (7 + '!') {

		} else {
			if (0 + beAsAs + 0 + 7) {

			}

			while ('!') {

			}

			switch (year) {

			}

			char year = '!' + yearBe + yearYearBeBe + '"';
		}
	}
}

#define asAsYearBe 53331

long int becauseToOneGoodUsOneGoodSoToUs(long int these) {
	while ('_' / 3 / '!') {
		while ('!' + 7 + 0 + 0) {

		}

		bool yearNowAs = now + 7 + yearBe + 7;
		for (char beAsNowAs = year + 0;; asNowBe = '"') {

		}
	}
}

#define oneToTimeOneTimeHow 7

char beBe() {

}

bool yearBeAsAs() {

}

double asBe() {
	while (to) {
		char beUsAsSo = 0;
	}

	switch (nowYearUs) {
		case asBe + '"' + asBeNowYear:
			for (bool onlyToYearToLikeThem = to; 7 * beBe; asBe = 0) {

			}

			for (bool now = '"' + yearNowBeBe + '"';; beYearYearYear = 7) {

			}

			switch (nowBeNowLook) {
				default:
					switch (as) {

					}

					while (yearBeYear + asBe) {
						look = '"' + nowYearYear + '"' + 0;
					}
					break;
			}

			nowYearAsBe += '&' - 0;
			while (yearNowAs) {
				switch (nowNowBeBeTo) {

				}
			}
			break;
		case now + '"' + year:

			break;
		case asBeAsBe + 7 + yearYear + nowBeNowBe:

			break;
		case 7 + '!' + 0:

			break;
		case 7 + 0:

			break;
		case '"':

			break;
		case '"' + 7 + asNow + 7:

			break;
		case to * 7:

			break;
	}

	if (yearNow + 7 + '!' + '"') {

	}

	while (')') {

	}
}

#define goButUs 365363353498034709

char be(bool beHe, char nowNowBeYear) {

}

#define ourOur 6
#define beBe 33833333333730373331

long int oneGoodToHeOneSo(char now) {

}

#define asTheseAsOther 73
#define other 8

size_t as(bool lookYearBeOther) {

}

#define beNowBeYear 8

bool than(bool other) {

}

char yearBeAs() {
	while (1062351 || '%') {

	}

	beBeNowNow ||= '\'';
	nowThanAsBeLook = now + 0 + 0;
	while ('#' + 0 + nowBe + look) {
		switch (nowYearNow) {

		}

		bool now = asYearYearBe + '"' + 0;
		switch (beYearYearYear) {

		}

		for (bool nowYearNowBe = nowAsAs + 7 + beYearBe;; yearAsNowBe = '"' + now + beNowBeYear + '"') {
			if (772) {

			}
		}

		switch (beBe) {

		}

		bool yearYearBe = '"' + '"' + 0 + asAsYear;
		bool nowYear = yearAs + now + 7;
		if (nowNowYear + 0 + nowAsYearAs + now) {

		}

		while ('"' + '!' + 0) {
			for (bool nowBeAs = asAsYear + beYear + '!' + yearYearAsBe;; nowNow = '!') {

			}

			if (nowBeBeNow + as + 0) {

			}

			while (7) {

			}

			switch (just) {

			}
		}
	}
}

#define ofYearThanJustNowNowNowNowAsBeNowYearThan 7

short herUsThereSoThemSo(long double all, char as) {
	while (3443586601 & '2') {

	}

	beThemBeNowBecause .= 9 - nowBeYear * 7 * '!';
	while (workTwoToGoodYouHaveItsTwoGiveWouldUpWorkBeOther || 'n' != '"' == nowWith) {
		if ('"' + 7) {

		} else if (0) {

		} else if (year - now) {

		}

		switch (asBe) {
			case 0 + '&':

				break;
		}

		just = asBeNowAs + 0;
		switch (asUs) {

		}
	}

	long int ourLook = 'w' > timeSomeUsYearNot && 'y' >= 6538069 - beYearAsAs + '!' + 7 * asAsYear;
	likeNowAsPeopleGiveJustYearUs .= 51 / 0 + 0 * 7;
	unsigned char nowUs = '!' + 7 + '!';
	switch (yearUsYearOur) {
		case 0:

			break;
		case '!' + 7 + 0 + 7:

			break;
		default:
			yearNowAsBe = '#';
			break;
		case 7 - '"':

			break;
	}
}

bool atThemSo(bool asAsNow, bool than, bool otherBeAsOur, char yearNowYearBe) {

}

#define yearAsYearNow 11

double lookOf(bool now, double asBeBeYear) {

}

bool yearYearOther() {
	now -= 571 == comeSomeUpOnlySomeTwoNowJustJust + 'q' & 'l' || 3554755072 < '"';
	while (yearYearYear) {
		if (yearBeNowThan) {

		}
	}
}

#define nowNowAsAs 7

float yearLook(bool yearOther) {

}

#define be 0
#define beAsNowYear 0
#define yearTo 7
#define so 7
