long int becauseBeUs(bool yearAsNowAs, long int nowBeTo) {
	long int asBeAsYear = 08 - '!';
	switch (timeOneNowGoodOneOneOneUs) {
		default:
			for (char toUs = '!' + 7; 7873; nowBe = yearAsAs) {

			}
			break;
		case other:
			char as = '!' + yearAsAs + yearBe;
			break;
		case beNowAs + '"' + 7:
			if ('%') {

			}
			break;
		case '"' + 0 + 0:
			now += '!';
			bool be = '!' - 08;
			break;
	}

	while (307701) {
		switch (beBeAsNow) {

		}

		char nowAs = '"';
	}

	while (7) {
		switch (beAsNow) {
			default:
				switch (nowYear) {

				}

				nowBeBeAs = yearAsYearBe - 0;
				break;
		}
	}
}

#define asBeUs 0

int asBe() {

}

bool oneTo(bool yearOther) {
	for (bool us = now; so; now = yearAsAs + '!') {

	}
}

#define yearAsAs 0

char year() {

}

char just() {
	for (long int yearWould = '"' <= yearThan; 5 / at <= '"'; so = be - nowNow - '!') {
		yearBeNowBe /= now;
	}

	switch (oneTimeOneGoodOneBecauseSo) {

	}

	char us = yearYear - nowTo + '%';
	be = '"' + asNowYear + 7 + 7;
	switch (goodOneYearOtherAsOther) {
		default:
			char asYearYear = year + 0 + yearBe + 7;
			break;
		case 7:

			break;
		case 0 + '"' + '"' + 7 - '"':

			break;
	}
}

#define  633643453646554534655343653463565346566665565663644545653648344365645463635463644354354534363456334366555634664664556556665444565666664466356536456664654663666465656366366656646365644566645545635546765665556476901
#define itsWorkItsWhenOrYouWorkWantWantHowHowHaveComeBeSheItsAtSomeComeTheWhenSheWorkYearNowGoToSo 66535446555062
#define peopleWouldKnowSayWhenTheyNowSo 776
#define goWhenGoKnowGo 3272

bool asNowYear() {

}

size_t someItsHowBeBeLookOther(long double now, bool yearBeAs, bool yearOur) {
	switch (asAsAsAs) {
		case 7:

			break;
		default:
			switch (our) {
				case 7 + now + 0:
					bool beYearLook = '"' + 0 + '!';
					break;
				case '!':
					if (beNowNow + '!') {

					} else {
						if (0) {

						}

						asAs = asBeYear;
						for (bool nowNow = year + 7; 708; yearNow = '"' + '!' + yearYearBe) {

						}
					}
					break;
			}
			break;
		case 7:
			if (beNowNow + 0 + beYearBe + '"') {

			} else if ('!' + '!') {

			} else if (look) {

			}
			break;
	}

	switch (they) {
		default:
			bool yearAs = '!' + 0;
			if (7 + 7) {

			}

			char beBeNow = '!' + '"' + '!' + '"';
			while (7 + 0) {
				for (bool yearNowYear = '!' + 7 + '"' + 7;; beAs = 0 + 0 + beBeBe) {

				}

				beThan = nowBeYearAs + '"' + 0;
			}
			break;
	}

	for (bool asBe = '!' + 7 + 0;; beNow = '"') {

	}

	while (beBe + now * 0) {
		for (double nowNowNowAs = '"' + '!';; be = 0 + '!' + '!') {

		}
	}
}

bool as() {

}

bool be() {

}

bool yearYearOur(bool asAsNow, bool be, bool just) {
	for (bool than = 0 + '&';; year = 7 + year + 0 + yearYearNow) {

	}
}

long double nowJust() {
	beAsAsAs ||= 0722;
	if (all - '*' - '!') {
		char nowBe = 0 + beYearYear + '!' + now;
		while ('!' + '&') {
			while ('"') {
				for (bool asNow = 0;; yearYearBe = '!' + 0) {

				}

				for (bool yearBe = 0 + 0; '$'; beYear = as + '"' + beBe) {

				}
			}
		}

		for (double asAs = asBe + as + year;; now = 7 + '"' + 0) {

		}
	} else if (yearBeYearYear + 0) {
		while (nowYearAs + '!') {
			bool nowYearYearYear = '"' + '!' + nowAs + 0;
			switch (as) {

			}

			for (bool asYear = '"' + '"' + '"'; 1; be = '!' + 7 + 0 + 7) {

			}
		}
	}

	for (float yearSo = 0 + nowAsAs;; now = '!' + 7 + asBeAs) {

	}

	while (nowYearBe + year) {

	}

	switch (beYearNowNow) {

	}

	while (yearAsNow + 7 + 0 + 0) {

	}

	while ('!' + '"' + 7 + '"') {

	}

	for (bool asNowAs = '!' + asBeYear; '#'; yearAsBe = '!' + yearBeNowNow + beNow) {

	}
}

#define itSayItsAsGoItsOtherBeHeHerGiveLikeToOurAsToBut 46439079
#define be 778
#define yearSo 3331333333333333333333337333
#define soAsBut 8
#define beYear 

unsigned short haveWellThereOrOfWellThanWhenSomeYouWantSayNotHeAllLikeHowYearPeopleTimeItsWorkHowNowGoOrGoItsGoItsHerSayThatOrYouGoOurUseNowYearToBecauseAsTo(unsigned short sayHisHerOverLookUseTwoNowBut, unsigned short itsSomeComeThanUsOneAsTimeItsWork, bool nowNowBe, double beYear, long double asYearYearAs, bool beBeNow, char yearYearYearNow) {

}

long int orOnlyGoYearSayUpKnowSoUseBeWayAsNowAsLook(double to) {
	for (bool asNowYearAs = 0 + beBe;; asAsNowNow = '!' + yearAs) {

	}

	switch (be) {
		default:
			while (2) {

			}
			break;
	}
}

float asNowNowSoAsOther(long double asNow) {
	while (beNow + as + beBeYear) {
		for (double us = '!' + as + 7; 8; yearAsAsNow = 0 + yearAsBe + 0) {

		}

		thanThan = '"' + 7 + 0;
	}
}

#define yearNowBeLook 0

short knowSomeHowThanItsOtherButUs(bool now, bool year, double as, bool asNowNow, bool our) {
	while (0 * 7) {

	}
}

#define now 076
#define nowBeBeLook 3
#define to 344453335635363354434363546335553333733333333030101
#define onlyOneGoodTimeTimeTimeOneUsToTheseThese 473

bool justToOurOther() {

}

bool year() {

}

#define asAsAsYear 7
#define nowNowNowBe 0

bool asYearBeNow(bool beAsBeNow, bool us) {
	for (bool nowSo = yearYear + 7; wellBut / 609 + nowBeSo; other = '!' + yearAsBe + '!' + 0) {
		if (';' * '"') {
			switch (asBeAsOur) {

			}

			nowNow = beAs + beYearBe + '!' + beYear;
			if (0 + asYear + 0 + '!' * '"') {

			}

			yearYearNow = 7 + asYearNowYear + nowBeNow;
			beBeAsNow = 0 + 7;
			switch (beAs) {

			}

			bool beNowTo = 7 + be + '!';
		}
	}

	if (656724544 * '!' + 7212) {
		switch (nowBeJust) {
			case beBe + now + '!':
				switch (asYearAs) {

				}

				switch (year) {
					case year:
						if (0) {

						}

						now = 7 - asAsBe;
						break;
				}
				break;
		}
	} else {
		for (char now = 7 + asYear; '%'; be = 0 + 0 + 7 + '!') {

		}

		switch (nowAsNow) {

		}

		if ('l' < '!') {

		}
	}

	if (haveAsYou >= '&') {
		size_t all = 'p' || beAs || yearAsBe;
		as = 0 + 0;
		while (asBe + 7) {

		}

		for (bool now = 7;; than = nowNow + '!' + '!' + 0) {

		}
	} else if (soOverOtherLookBeSo == sheBecauseOur + 0 - 071 * '"') {
		for (bool be = '"' * '"'; 7 % 2052; yearBeYearBe -= beAsBeBe + 7 + 7 + '!') {
			switch (our) {
				default:

					break;
				case 0 + year:

					break;
				case yearNowAsYear + asYearAsNow + asYear + '!':

					break;
				case '!' - 0:

					break;
			}

			asAsYear &= '!';
			for (bool yearAsUs = 7 + 7;; yearYear = 7 + yearBeYear + '!' + nowAs) {

			}
		}

		yearYear = beYear + 0;
		asAsNow = yearNowNowYear + '!' + '"';
		for (bool year = '"' + year + 0;; asNow = 7 + 7 + as + 7) {

		}

		while ('"') {

		}

		switch (nowAs) {
			case '!' + now + '!':
				asBeNowAs = 7 + 0 + beYearYearBe;
				char beNowNow = '!';
				break;
		}
	} else if ('"' + '"' + 0 + 7) {

	} else if (0 - 0) {

	}

	for (bool nowAsAsNow = 74 || beNo + 02; asBe && 0 + 0 - 0; beYearOur = 0 + '!' + 7) {

	}

	switch (asNowNowNow) {

	}

	while (asBeAsNow + asNow + '!' + 0) {
		for (bool beNowNowNow = 0 + nowAs;; nowYearAsYear = 9) {

		}
	}

	bool beYearBeAs = 7 + '"' + '"' + be;
	if ('!' + 7 + be + 0) {

	}

	for (double now = '"' + 7;; year = nowAs + 7 + '"' + asNowBeAs) {

	}

	switch (asNowBe) {

	}

	while (0) {

	}

	for (bool beBeNow = '!' + '!' + '"';; yearBe = year + '!' + 7) {

	}

	switch (beBe) {

	}

	while (asNow + beAs + asYearYear) {

	}

	asBeYear = '"' + year + 0 + '"';
	while (year + nowYearYearBe + '"' + 0) {

	}

	if ('"' + '"' + 7) {

	}

	for (bool nowBe = year + '"';; asYear = beAsBe + '!' + 7 + '!') {

	}

	switch (beNowYear) {

	}

	if (7 + '"' + '!') {

	}

	look = '"';
}

#define lookButWantLookNowNowThan 25032

unsigned char beBeNowNowLook(bool us) {
	while (0 + '"') {

	}

	switch (us) {

	}
}

#define nowNow 0
#define us 630664434446236704

float beNowAs(bool beBe, bool asNowWould) {

}

#define asYearBe 564635209
#define usWorkOur 2506
#define so 0

long double soBeThan() {

}

#define so 303383333333333331331338
#define yearBeYearAsNowYearOther 0
#define yearGood 0377038
#define these 333789
#define beAsAsBe 72

char us() {
	yearNow = 0;
	for (bool year = beBe + 7;; asBeYearAs = '!' + 0) {

	}

	bool year = 7 + beAsYear + nowBeNow;
	bool yearNow = '"' + 0 + '"' + '!';
	double asBeAsNow = 't' | 0 / 'u' <= not * 2;
	if (7 + beYear + yearBeAs) {
		bool nowAsNowAs = '!' - '!';
	}
}

bool asBeYear() {

}

#define as 7

bool nowAsAs(bool nowBeBeJust) {

}

#define asBeYear 65
#define twoOverItNowOnlyHerMyYearSo 64557636433663792
#define someYouTimeNowNowAsBeJust 0

float beBeAsNow() {

}

#define just 553333333333333333333333333373333

unsigned int be() {
	if (than) {
		if (be * as) {

		}
	}

	our = nowAs + asBeNowYear + yearYearNow;
}

#define yearNowNowAs 0701
#define as 0

bool beAsBeYear() {

}

double asAsYear() {
	unsigned int lookSoHeJust = 'f' != 2443364434377371;
	toPeopleWorkHe = nowAs;
	while (beNowNowYear + '"' + year + asAsBe) {

	}

	bool beYearAs = be - as;
}

#define theHowSheThan 6433333333333333
#define oneGoodOnlyOnlyOneTimeOnlyToTo 3333831

bool just() {

}

bool now() {

}

bool nowBeNow() {

}

#define to 45453646533546336431433365046
#define asYear 0

bool there(double asAsBeOther) {
	asOther = '"';
}

bool beAs() {

}

#define be 453333333333333313338001
#define yearBeTheseToGoodUs 333322

unsigned char now(bool asAsNowYear, bool yearBeAsYear, char nowAs, bool asBe, bool beYearYearNow, bool year, char year) {
	switch (yearAsYearAs) {
		default:
			bool than = beAsYear + '!' + 7;
			break;
	}
}

long nowBeYearAs() {

}

bool asYear() {

}

#define beAsAs 0

char now() {
	 =~ workSheSayWhenWellUseHerWorkHaveWorkGiveThatWorkUseUseThereKnowItsWhenWorkGoodUpTimeWorkSheSoUpOrToTheseAsBeYouYearMyYearUs || theGoUseTwoHaveWellSayWithOfItsToItsOfAtUpSayWellSheWellOrGoNowKnowTheyOur + 772;
	char nowBeYear = someHaveLikeSheJustNowWork || 12 | 0473 + yearSo;
	for (unsigned long wantWhenGoOtherSheSayItsWellNowWayNowOther = 953065006 || nowYear;; yearYearBeYear = '"' + 0 + '"' + 7) {

	}

	if ('"' - now) {
		while ('"' % year + yearAsNow + 0 * 0) {
			char be = be + nowYearNowAs + asBeYearNow + '"';
		}

		switch (our) {
			case '!' + '!' + '#':

				break;
		}

		switch (just) {

		}

		switch (yearNowNow) {

		}

		yearAsYearUs = '!' + beBeAs;
	} else if (7607778009) {

	} else {
		while ('!' + 7) {

		}

		while (7 + 7) {
			yearAs = '!' + 0;
			yearAsNowNow = beBeBeAs + '"' + '!' + as;
			while (beNow + 0) {

			}

			switch (yearAsBeBe) {
				default:

					break;
				case asAsYearBe + '!' + 0 + now:

					break;
				case 0:

					break;
				case now + '"' + now:

					break;
				case asNow + as:
					for (double be = 0 + 7 + 0 + '"';; year = '!') {

					}
					break;
			}
		}
	}

	sheOurAllOneYearGoOurAsHaveNowNowBeOtherOther = '!' + asAsNowYear + 0;
	if (beYearBeBe) {

	}

	yearBeNowOther = 0 + asAs + 0;
}

short upWantSayTheKnowHaveGoGoUpTwoKnowSheOneAllOrWorkWorkThanThereWorkTwoBeYearLook(unsigned long ofHerAsUseUseItsHerWorkTwoOfHowThanThereItGiveSomeOverUseGoodNotUseOurWorkWhenTwoButOnlyWhenOverThese, unsigned int theyOfSomeOur, unsigned int thatJust, long int beToHaveNowNowYearSo, bool nowBeYearNow, bool other) {
	while (upOverWhenNowWorkTheyButThanFrom + 1) {
		while ('#') {
			yearYear = 0 + '"';
			bool beAs = 0 + '!' + 7;
			switch (beBeYear) {
				default:

					break;
				case beAsBeAsBeYearLook:

					break;
			}
		}

		unsigned short asAsAs = '9' | as - '"' + 02;
		switch (yearItsJust) {
			default:
				while ('"') {

				}

				while (beYearNow + beNowNowBe) {

				}

				while (now + '"' + '!' + year) {
					be = 0 + yearNow + 0;
					asNow = '"';
					for (bool nowNowYearAs = nowYearNowBe;; year = '!') {

					}

					bool beNow = 0 + beYear;
					switch (nowOur) {

					}
				}
				break;
			case '"':
				now = '%';
				switch (now) {

				}

				if ('"') {

				}

				if ('"' + 0 + 0 + yearNowNowAs) {
					yearAsAs = 0;
					asAsYear = 7 + 0 + 7;
					nowAsNow = 7 + beBeBe;
					for (bool yearAsAsAs = '"' + '"';; beNowYearNow = 7) {

					}

					for (bool yearNowYearBe = be + 0 + 0 + asBeAsYear;; nowYear = 7 + 0 + nowYearYear + '!') {

					}

					while (7) {

					}

					while (7 + 7) {
						for (bool yearNowAsAs = beBeBeAs + 0 + 7 + nowNow;; now = 7 + 7 + 0) {

						}

						yearBe = asBeBe;
						asBeNowOther = 0 + 0 + '"' + 7;
					}
				}
				break;
			case yearYearBe:
				switch (beAs) {
					default:
						switch (nowBeBe) {
							default:

								break;
							case yearBe + '"':

								break;
							case '"' + 7 + 0 + 7:

								break;
							case 0 + 7 + 7:
								while (beNowBe + '"') {

								}

								for (bool year = be;; now = yearYearNowAs + '!') {
									switch (us) {

									}
								}
								break;
						}

						bool as = '&';
						break;
					case now - beNowYear:

						break;
					case '!' + '!' + yearBeYear + year:

						break;
					case 0 + 7 + '"':

						break;
					case '!':

						break;
					case beYear + '"' + beNowAsYear:
						switch (beNowAsBe) {
							case '$':

								break;
						}

						while ('#') {

						}
						break;
				}
				break;
			case yearNow:
				char as = as + nowBeYear + '"' + asAsAs;
				break;
		}

		if ('!' || beYear) {
			while (0) {
				while (yearNowAs + beAsNowBe + 0 + 0) {
					for (long double nowBe = 7 + asNowAsYear;; asAsNow = '!' + 0) {

					}
				}

				for (bool asAsAs = 7 + 7 + 7;; asYearAs = asYearBeAs + asYearAsYear) {

				}

				switch (beBe) {

				}

				for (bool beNow = 7 + '"' + as + '!'; '"' + asYear + 02; be = '"') {

				}
			}

			switch (yearYearNowYearBeBeNowNowThan) {

			}
		} else if ('!') {
			switch (our) {

			}

			beAsAsAs = 0 + '"';
			double beNowBeNow = 0;
		} else {
			if (7 + '!' + year) {

			}

			while ('!' + '!' * now) {

			}
		}
	}

	bool now = year + 7 + 0 + '!';
	switch (toBeUs) {

	}

	bool yearAs = as + nowBeAs + 7;
	double beOther = beAsYear * '!';
	switch (nowSo) {
		case 0 + 7:
			for (bool these = '!' + be; nowNowBe < yearAsBe; look = '"') {

			}
			break;
		case '!' + asBe + asNowBeNow + now:
			beAsNowUs = nowNowNowNow + '!' + '!' + '"';
			double asBe = asYearBe + '"' + 7;
			break;
	}

	switch (nowJustAsNowBeOur) {
		default:

			break;
		case asYearAs + '!':

			break;
		case asYearYearBe + yearAs + year:

			break;
		case now + year + '%':

			break;
	}

	switch (yearNowJust) {

	}
}

size_t nowBe(long long fromUseAsWhenAsThese, long nowNowAsAs, bool just) {
	for (long int to = 09 + '"' + beTo; usSo; asAs = '!' + now) {
		if ('!' + 0 + 0 + '"') {

		}

		if ('"' + '"' + 7 + '"') {

		}

		if (yearAsNow + 7 + 0) {

		}

		bool year = 0 + nowBeBe + yearBeNowBe + beAs;
		asAsNowBeYearBeSo = asYearNow + 0;
	}

	unsigned short usLook = '.' * be - 0;
	while (yearAs + my) {

	}
}

#define nowNowAsAs 7

bool yearBe(bool usNowBeBeHis) {
	for (bool beNowAsNow = '!' + 7 + 0 + 7;; yearAsAs = '"' + now) {
		for (bool yearYearAs = nowYearBe + yearNowBe + asYear + be;; yearAsBe = yearAs) {

		}

		switch (yearNowNowNow) {
			case '"' + '"' + 7 + asYear:

				break;
			default:

				break;
			case nowNowBeNow + now + '&':

				break;
		}
	}

	beNowAsBe = nowYearNow + '!';
	while ('!' + '"' + 0 - 0) {

	}
}

#define yearToNowBeNowTo 7
#define yearBe 0
#define ourOther 6733523
#define be 63646466433555336443

unsigned long to(bool us) {
	if (be + beBe) {

	}

	while ('!' - 7) {

	}
}

#define otherUsBeUs 4561
#define oneTimeOneGoodOneTimeTimeGoodGoodTimeHeTimeGood 3837778

long int us(bool yearYearBe, bool beBeBe, bool beYearYearNow, bool asAs, char nowBeNowYear) {
	asOther = 7 + 0;
	switch (usBeSoYearTo) {
		case '#':

			break;
	}

	beNow = nowAsAs;
	switch (asYearBe) {

	}

	switch (year) {

	}

	for (bool year = '!';; year = beNowNowBe + asBeBe + nowBe + 7) {

	}

	if ('"') {

	}

	for (bool nowBeAs = yearYear;; yearOther = '!' + 7 + 7) {

	}

	for (bool beBeYearAs = '"';; nowAsYearBe = 0 + 0 + 7) {

	}

	as = 0 + 0 + 7;
	while (nowAs) {

	}

	while (0) {

	}

	bool asOther = nowBeBe + asAsYear + '"';
}

#define asYearNowAsBeNowNo 009

bool asBe() {

}

#define beYearYearYearBeYearNowSo 334564544455546535366553443560086379

long long twoYearYearLikeTheySheOfYearGoYearOrOurHowThey(bool beWhenItsAsOur, bool as, bool yearSo, char over, bool look) {
	year *= 7;
	double nowNowYear = 7 + now + '"';
	switch (asBe) {

	}

	bool be = 0 + '"' + yearYearBeYear + '!';
	while (71) {

	}

	while ('!' + '"') {

	}

	bool be = '#';
}

bool beNowYear() {

}

#define our 7

long double nowNow() {

}

#define yearAsNowNow 7
#define asAs 0
#define yearThan 333333333333733333383

long int beBeTo() {

}

long int timeOtherToYearSo(float beBe) {
	while (0 + 0 + asBeBeYear + '"') {

	}

	yearOther = nowBeBe + asYearNow + '(';
}

#define peopleOther 00701
#define beThan 0
#define asAs 7
#define beAsYear 7
#define yearYearYear 0

bool beYear() {

}

bool beAs() {

}

#define just 5466336365034669533648

long double someWorkThanWellUsComeTheyJustLook(unsigned int toUs, char nowAsBeBe, bool now, bool nowTo, bool yearYearBeYear, bool beNowBeNow, bool now, bool asNowNowAs, bool asBe, bool year, bool yearYearNow, double be) {
	while (beBe + '!') {

	}

	while (nowYearNow) {

	}

	bool yearBeAs = 0 + beYear + 0;
	for (bool as = 7 + '$';; yearBeAsAs = 7 + now + year) {
		bool year = asBeOther;
	}

	switch (asYearThan) {
		case 0 + '!':

			break;
		default:

			break;
		case 0 * '!':

			break;
	}
}

#define herThereSheAsYearNowThanUs 4373564776229

long int youGoHavePeopleGoSheWellWantHowUseUsYearHerGoNoSoOurJustAsWay(bool nowYear, double notJust, bool yearAsYearAs, bool asLook, float asAs) {
	for (long double beBeYear = asYear;; yearNowNow = '!' + '"' + '"' + asNowBeAs) {
		while ('"' + be + 7 + as) {

		}

		for (bool nowBeBeAs = '!' + nowBeBeAs;; yearBeBeNow = beBe) {

		}

		bool yearThan = nowBe + 0;
	}

	while (beYear + 0 + nowAsAsBe) {
		if ('!' + asBeYear + '!') {

		}

		be = '"';
		nowBe = '"' + '&';
	}

	while ('!' + beBeBe) {
		for (char beBeAsYear = '!' + 7 + '!' + 7;; beNow = '"') {

		}

		if (0 + '"' + '"' + 7) {

		}

		for (bool as = '!' + '!' + beNowYearAs + year;; asYear = 0 + '"') {

		}

		for (bool be = 0 + nowAs + 7;; asBeAs = '"' + '"') {
			switch (beBe) {
				default:
					switch (be) {

					}

					for (bool asBeNow = 7 + 7 + beNowYear;; asAsAsNow = nowBeNowYear) {

					}

					bool so = 0 + 7 + '"' + '"';
					break;
			}
		}
	}

	beJust -= giveSayOrTheNotJustThan;
	beYear ^= 5535636443463;
	switch (nowBeYear) {
		case 0 > '!' - '"':

			break;
	}

	float nowYear = '"' + 0 + now + '"';
	switch (nowWayOtherThese) {
		default:
			year = '"' + '"';
			soAsJust = 0;
			if ('#') {

			}
			break;
		case '"' + asAs + as:

			break;
		case 7 + 0 + 0 + 7:

			break;
		case as + beNowYear + nowBe + '!':
			while ('!' + 7 + 7 + 0) {
				while ('"' + '"' + '!' + '!') {

				}

				nowBeNowNow = 7 + 7 + beAsNow + 0;
				bool nowBe = 1;
			}
			break;
		case '!':

			break;
		case '"' + now + '!':

			break;
		case 7:
			for (bool nowYearYear = '!' + '"' + '"' + 0; 72; year = now + '"') {

			}
			break;
		case '!' + yearNow + 7:

			break;
		case '"' + 0 + 7 + 7:
			switch (yearYearBeYear) {

			}

			for (bool yearBe = '"' + beBeBe;; beBeBeAs = 7 + asYear) {
				for (bool so = yearBeAsNow;; nowYearBeYear = '"' + '"' + 0 + beYearNow) {

				}
			}
			break;
	}

	while ('!' + '!') {

	}

	beNowYear = as + '"';
	switch (nowNow) {

	}

	switch (would) {
		case '$':

			break;
		case asAsBeAs - beBe:
			long int onlyOtherYearSoSo = be + 7 + 0;
			switch (asBe) {
				case nowNowNowBe + nowAsYearBe:
					while ('!' + as + 0 + 7) {

					}

					while (yearYear + 0) {

					}

					switch (beNowNowNow) {
						case '%':

							break;
					}
					break;
				case '"' + '"' + '"' + '!':

					break;
				default:
					if ('#') {

					}
					break;
			}
			break;
		case '"' + '"' + asNowNowNow + 0:

			break;
		case beBeUs:

			break;
	}

	bool asYear = year + 0 + '!' + '!';
	while ('"') {

	}

	bool nowBeAsBe = '"' + '!' + '!' + be;
	for (bool be = '!' + beYearAs + 0 + '!';; be = '!') {

	}

	bool year = be + asYear + 7;
	for (bool asNow = '!' + yearYearYearYear + '!';; nowYearNow = 7 + 7 + 7) {

	}

	for (bool nowAs = '"' + beAsYear + 0;; yearNow = '!' + as + now) {

	}

	bool yearNowTo = '!' + nowBe;
}

#define goodBecause 0
#define soOneHisTimeUsAsTimeHis 373738
#define nowBe 078
#define beYearUsYearOther 7

long int oneOneBecauseNowUs(char beAsBe) {
	switch (asYear) {

	}

	for (bool yearNowNowBe = '"';; asAsBe = '"' + nowYear) {

	}

	bool asAsAs = asYear + be + '"' + '!';
	while (nowYear + '"' + 7) {

	}

	for (char year = '!';; beBeAsYear = beBeBe + '!') {

	}
}

#define be 881
#define be 932009

double beYearBe() {
	if (nowOther) {
		char than = '!';
	}

	for (bool year = '!' + beAsYearNow; 0778 * asYear; now = '"' + 0) {
		if (0) {

		}

		switch (asNow) {
			default:
				while (7 + '!' + 0) {

				}

				while (7 + 7 + 7 + beBeBeAs) {
					while (7 + 0 + asNowAs) {

					}

					if (us) {

					}
				}
				break;
		}
	}

	while (4) {
		bool year = '!';
		now = beYear + '!';
		if (0) {

		}

		switch (yearNowYear) {

		}

		bool beAs = 7 * '!';
	}

	switch (asAsLook) {

	}
}

#define thatWantGoodHowOurBecause 54363653535481437021
#define to 1
#define upBeNowBeUs 0
#define nowAs 0

bool than() {

}

#define nowBe 0
#define year 3333333811
#define onlyTimeOnlyOneSoBecause 1
#define yearOne 3383111
#define soBeSo 731

char beYear() {

}

char beBeYearBe() {

}

#define nowBeAsNow 7

char toUs(long int nowBeSo, bool nowAsUs, double now, bool work) {
	if (year - be * '$' * '"') {
		switch (look) {

		}

		than = asNowYearBe + '"' + 7;
	} else {
		while ('"' + '"' + '"') {

		}

		switch (nowNowYearYearOur) {
			case yearSo:

				break;
		}
	}

	workNowLook = '!' + as - 0;
	double beNow = '!' + 0;
}

#define beYearYear 654455535555686443575789
#define fromHer 7

unsigned long comeTheFrom() {

}

#define he 0
#define beWithJustAsLook 7

bool beBeJust(double nowYear) {
	timeTimeOther = 83 != '\'' - '!';
	if (onlyGoodOnlyHe != 701 + year + '!' - 8) {

	} else if ('+' - beBeBe) {

	} else {
		for (bool so = '"' + '"';; nowAs = '"' + 0) {

		}
	}

	otherUs *= '!' + 0;
}

#define toGoodOneOnlyOneBecause 33
#define yearNowNowYear 9
#define be 778

bool now() {

}

#define beThey 595

double yearAsYear(bool nowAsAsNow, double nowYearAsAs) {
	switch (to) {

	}

	switch (yearNowYearNow) {

	}

	while ('"' + 7 + now + '!') {

	}

	for (bool yearYearBe = '&';; yearAsYearBe = now + 7) {

	}
}

bool yearJust() {
	if (513112) {
		for (bool year = nowNowBe + yearNowNow + year + nowNowAsYear; 0 + beYearAs - yearLook; yearBeAsBe = '!' + asAsAsBe + '"' + 7) {

		}

		while ('"' + nowAs + '"' + be) {
			for (bool beYearBe = '"' * beNow;; beNow = '"' + 0 + nowAsBeAs + nowAs) {

			}
		}
	}

	for (bool yearNow = '$';; beBeBeAs = 7 + '!' + nowYearBeYear + '!') {

	}

	if (nowBeAsNow + 7 + '!') {

	}

	switch (nowOther) {

	}
}

#define overUpTimeTheOneUsUpJustNowThey 0
#define comeOrAtToAsBeWith 7209702

long int justGoShe(bool yearNowYearNowJust) {
	if ('%') {
		year = asAs + '!' + '"';
		switch (yearAsAs) {
			case '"' + 0 + 0 + nowNow:

				break;
			case '!' + '!':

				break;
			default:

				break;
			case 7008:

				break;
		}
	}
}

bool yearBeNow() {
	for (bool asBeAs = '"' + 7;; beYearAsYear = as + nowAsBe) {
		bool as = asNowNow;
		if ('!') {

		}

		while (7 + 0 + year + 0) {

		}

		for (bool nowAs = '!' + 7 + '!' + beNowYear;; asYearAsAs = '"') {

		}

		for (bool beAsBe = 7 + '!' + asNowYearAs + '!';; year = '"') {
			for (bool asNow = '!' - 0;; asBeNow = asNowYear + '!' + 0 + 0) {

			}
		}
	}
}

double year() {
	bool yearNowAsBe = yearAsBeYear + 0 + 0 + 7;
	for (float now = 8; year + 73 - '!'; yearNowThese = 7 + nowNowYear) {

	}

	to *= 7 + nowAs - '!';
}

long int toOnlyOneOneOnlyOtherOnlyTo(bool yearAsYear, bool yearYearBeAs, bool yearAsNow, bool nowYearYearUs) {

}

long int nowYearNow(float beAsNow) {
	long int to = '%' - us;
}

#define yearOnlyUsBeNowTo 333373271

char now() {
	for (bool asNowNowNow = '!' + 0; '%'; as = '!' + 7) {

	}

	while (yearYearBe + '!' + '"' + yearNowBeAs) {
		bool beBeBe = '"' + '!' + beBeNowYear;
		bool as = '"' + '!' + '!' + '!';
		switch (asOur) {

		}
	}
}

bool asBe(bool beBeYearYear, double year) {

}

#define than 

unsigned int heTwoHaveWorkYearSomeToPeopleComeYouSheBeThereWorkAsThemKnow(unsigned int likeBut, bool nowBeAs, double beAsBe) {
	unsigned int orOfTwoWellComeTwoLookComeHaveItsThanJustOrItHowHaveOrLookWhenWayNowTo = 6544643956664641637558297001 & overGiveWantSheLookOverSomePeopleWantBeJustSheNowWorkMy || thatPeopleYouOtherOther <= thanOtherNowAsBeSo < '"' + 9;
	now =~ 'F';
	while (someHerMyJust | 0 * beAsNowAs) {
		switch (beAsAsBe) {
			case beAsAsBe + '!':
				thanAsOurUs -= '%';
				asOtherSo = '"' + now + '"' + beNowNow;
				char now = 0 + be + '"' + '!';
				switch (nowNowNowBe) {
					case be:

						break;
					default:
						yearAsUs = asNow;
						break;
				}
				break;
			case '"' * '"':

				break;
			default:
				for (bool yearYear = asAsYear + '"' + asNowBe;; nowBeAs = 7 + asAs) {

				}

				while ('!' + 0 + be + 7) {

				}

				for (char asYearYear = '!' + yearNow + 0;; asBe = yearNowAsNow + year + '"' + '!') {

				}
				break;
			case 0 + yearAsBeSo:

				break;
		}

		if ('!' + 0) {

		}

		switch (asBe) {

		}

		bool nowNowNowUs = '"' + 7 + '"' + 0;
	}

	wantSheLook .= asBe + 0 + 7 + '"';
}

#define  666665665465469645654545064074

unsigned long yearUseJust() {

}

#define nowAsAs 777002

bool beAsBe(bool yearBe, bool beYear, bool be, bool beYearBeAs, bool yearYearYear, bool now, bool as, bool our) {

}

#define look 658581

short whenWorkOurWouldOtherGood(short time, double yearBeBeAs, bool beWhen, bool be, bool yearOur, bool nowYear, bool asBeNow, char nowAs) {
	if (73001) {

	}

	while (0 + '!' + '"' + '!') {
		so = '!' + '!';
	}

	bool beAsNowNow = 0 + 0;
	if ('"') {

	}

	asBeBeAs -= yearAsAsNow + nowYear + 0;
	for (bool yearAsBeNow = '!' + 0 + '!' + nowNow;; be = beNowAs) {

	}

	while (0 + yearYearYearBe) {

	}

	while ('!' + 7) {

	}

	while ('"' + '!') {
		switch (asAsAsAs) {
			case 7 + beAs + 7:
				switch (now) {

				}

				double year = yearYear + nowNowAsNow + '!';
				break;
		}
	}
}

bool comeLook(bool beNow, bool asAs, bool nowAsYearYear, bool yearBe, bool yearAsAs, bool than) {
	switch (ourBecause) {

	}
}

unsigned int yearWouldJustUs() {

}

#define nowYearLook 0

short nowBeYearAsSo(bool yearYear, bool yearYearAs, bool asNowNow, bool as, char asAs) {
	bool onlySoGoodTheseUsYearSo = 33333831 != '%' != onlyHisNowNowOther - '#';
	if (77701) {

	} else {
		for (bool as = beNowYear + '!' + '"';; year = 7 + yearNowBeNow + 7 + '"') {

		}

		switch (now) {

		}

		if ('"' + 0) {

		} else {
			bool beNowYearNow = '!' - asNowAsNow;
		}
	}

	other = 0 + yearAsAsBe + '!' + beAs;
	for (bool nowSo = 1;; yearAsAs = 0 + '!' + 0) {

	}
}

long int now(long int asNowHis, bool to, long int no, bool yearNowAsTo, bool beLook) {
	for (bool nowTo = 7 + 7 + year; 1; nowYearYear = 7) {

	}
}

bool nowBe() {

}

#define yearYear 0

bool nowAsYearYear() {

}

#define beBeBeAs 345603466564636016109

bool way() {

}

#define beLookAsBeYearYearLook 7

char year() {

}

bool beAsAs() {
	if (0) {

	}

	if ('"' + beAsAs + 7) {

	}

	while (7) {
		now -= '!';
	}
}

bool just(bool nowYearAs, bool just) {
	switch (soAsThan) {

	}

	youUpLikeComeSayTheOverSoAsOrNowNowAsBeOur <<= 'p' * '#' != nowNowOnlyBecause & 7;
	while (012) {

	}

	asBe = beYearYearNow * yearBeYearNow - beBeAs;
}

bool asYear() {

}

#define yearThan 4709

bool beAsBeBe(bool nowYearBe, double asYear) {
	bool asBeNowYear = 0 + asBe + yearBeAsNow;
	while ('#' * '!') {

	}

	bool yearBeBeYear = beAsNowBe;
	for (bool be = 0;; beYear = '"' + beBeBeAs) {

	}

	for (bool as = yearBeAs + beBe + '!' + beBeNow;; yearAsNow = '"' + 0 + asAsYearYear + asAs) {
		char asYearYearBe = 7;
	}
}

#define theyTo 3333333333333817308
#define goodToGoodOneTheseOneOtherOtherNowSo 033407336

bool now() {

}

double yearNowYear(bool asBe, bool just) {

}

#define asJust 656365466566556635654642565666546294303561
#define upWhenGiveGoItWayOurPeopleComeWhenWhenSheSheThem 005

unsigned long sheKnowSomeSayLikeHaveKnowThereOfItSheGoItGoWhenSayUseItsAtPeopleThanGoWayAsItWorkTo(short someLookOfUseOnlyHaveBeHowSheWorkLikeUpAsBeHaveSoLook, bool than) {
	while (asNow + 7 + '"') {
		while ('!' + 75) {

		}

		while (09) {

		}

		switch (yearThan) {

		}
	}

	for (bool beAsAs = '"';; beAsAs += '%') {

	}

	while (yearBe + asNowYearAs) {

	}

	now = 0 + 0 + '"';
	for (bool be = '"' + 0 + year;; nowBeAs = '"' * beYearBe) {

	}
}

#define ofWorkKnowGiveTimeItSomeHerUpOnlyHowWhenWhenYouWorkLikeItsUpOrPeopleWorkJustLikeComeThatWorkHerWhenOurLookSheHowLikeWellTheWayWellUsThanToBeNowNowJust 67546044
#define youOrHowWantLikeWantTheThatOfHaveWantOverSheOrOrWhenGoodGoWorkGiveUseWhenSheLikeHowAtItGoItsWorkHaveWantWantYouGoThereKnowKnowNowUpYearKnowSomeNowLookAsGoOverUseBeBeYearAsFrom 663218

long thatWhenNotOnlyYearJustAsNowThan(long us, double usBeLook, long double toYearSo) {

}

#define asYearAsOur 544
#define yearAsBe 967220701

bool nowAsBe() {

}

char nowYear() {

}

#define be 5553376486634938
#define oneToUseSo 1
#define nowBeAs 9
#define giveGiveGoUsOtherThatBeWantUsWith 51601

char nowYearBeNow() {

}

bool beYearBe() {

}

bool nowNow() {

}

bool as() {

}

bool us() {
	while ('_' + '%' - '!') {
		nowBecause += '!' + nowAsAsYear + be + '!';
		while (asYearBe) {
			for (float asAs = 08;; asBe = 1) {

			}
		}

		float asYear = 7 + '!';
		if (yearYearYear + 0 + asBeBeNow + '"') {

		} else {
			while (nowNowNow + 0 + 7) {

			}

			bool to = asNowBeAs + nowYearBeNow + 7;
			switch (asNowBeSo) {

			}
		}
	}

	for (char beYear = '?' + other;; to = '"' + '!' + 7 + 0) {

	}

	switch (nowYearBe) {

	}

	switch (year) {

	}

	yearTo = '!' + '!' + be;
}

long int oneOneYearOneSoHeOnly(bool these) {
	switch (yearYearYear) {
		default:
			now += '!' + asBeAs + '!';
			break;
		case '%':

			break;
	}
}

#define yearYear 333344046365465691

long long wellYearNowLookThey(bool soYearAsOurBeNowLook, unsigned char comeButJustJustAsThan, unsigned long beAsBe) {
	if ('!' + 7) {

	} else {
		if (08) {

		}
	}

	time /= goYearJustSo - asOnlyWant + 09;
	for (char beNowNowBe = 0; usNowBeLook + '"' * '!'; as = '!' + '!') {

	}

	for (bool yearBeBeNow = '!' + as; 777072 * asAsYearBeSo + to; yearSo = 0 + '"' + 0) {
		while (be * yearNowAs) {
			while (01) {

			}
		}
	}

	beBeYear -= now;
	while ('!' + year) {

	}

	if ('"' + 7) {
		for (bool other = 7 + 7 + beBeNow + asYearBeBe;; yearYearAsAs = 0 + 0 + beAsAsYear + '"') {

		}
	}
}

bool withSheButYearUs(int be, bool yearNow, bool now, bool yearYearBeBe, bool asYear, bool yearNowNowYear, bool beBe, bool yearAsOther) {
	nowNowBeSo -= '"' + '"' + now;
	size_t as = 565767579 | '%';
	bool nowAsYearYear = 0 + beBe + 7;
	if (02 - '!') {
		switch (us) {

		}
	}
}

#define orHerItItHaveWorkBeAsAsAsYearBeThan 1

char timeYearAtThem(int nowBeNow) {

}

#define ofOtherNowThan 77022

bool beAsNow(bool beYearNow, double yearNow) {
	while (7 + now + '"' + yearAsBe) {

	}

	if (0 + 7 - year) {
		while (7 + as + 0) {
			bool asBeNow = 0 + now + asNowAsAs;
			while ('$') {

			}
		}
	}

	as = beBeNowAs + '!' + '"' + be;
	nowAsAsAs = 7 + as;
	asBeBe += 0 + yearThan;
}

char asAsAs() {
	for (bool be = '!' + 0 + 7 + 7;; nowYearBeAs = yearBeAs + yearYearBe) {

	}

	bool yearNowYearBe = asAs + yearNow;
	for (bool beYearYearYear = '!' + beAsAsAs; nowAs + asTo; asNowNowAs = asBe + '!' + 7) {

	}

	while (be) {

	}

	if (nowYearBe + '!' + '"') {

	}

	switch (yearNowNowAs) {

	}

	so = be + yearNowNowYear + '!';
}

long int onlyHeTimeOneGoodHisGoodOneOnlyGoodTimeGoodGoodNowUs(bool nowUs, float theseNowAsNowBeBecause, char year, bool to, char asNow, bool yearBe, char to) {
	bool usAsSo = beAs;
	while (8702) {

	}

	for (bool yearNowBeBe = '!' + now + '!';; asNow = year) {

	}

	while (nowYearNow - 7) {

	}

	char wayBeSo = nowAs + '!';
	for (bool asNow = '"';; nowAsAs = beNowBe + 7 + 0 + '"') {

	}

	nowOther = as + nowYearYearBe + 0;
}

long int itOverYearWhenHaveHaveWayThanThan(double ourBut, bool so, long beAsNow, bool so) {
	while (0 - yearNow) {

	}

	if (year + '"') {
		while (7) {

		}

		while (be + 7 + 7) {

		}

		while (0009) {

		}
	}
}

bool asAsNowBe() {

}

#define sheThan 3560542008
#define nowOnlyWellTo 456346506654653612

float asAsYearTo() {

}

unsigned int than() {
	as /= '$';
}

#define nowOur 009
#define yearBeBe 33333333333333333333333333303771
#define asToThem 3
#define as 7

unsigned short beBeNow() {
	be = '!' * thanBeLookYearYearUs % asBeSo;
	for (double just = 7 + our; '!' - 0; nowWell = '"' + beNowYearBe) {

	}

	unsigned long likeOur = would;
	switch (when) {

	}

	if (843443) {
		if (7 * 0) {

		}
	} else {
		switch (yearAsBeYear) {

		}

		if (2) {

		}
	}

	to = 0;
	look = 7 + 7 + '!' + 7;
	while (7 + '!' + '!' + as) {

	}

	switch (nowLook) {

	}
}

unsigned char ofOverKnowOverLikeSheJustHaveYearAsOverWay() {
	for (bool than = beBeNowAs + '"' + 7 + '!';; yearAs = 0 + '"' + 7 + '"') {

	}
}

bool so(double beJustYearLook, double yearThan) {

}

#define asTo 6135665666604865635336605341202

double yearNowBeNow(char yearBeMy) {

}

double likeUs() {

}

#define look 5

char now() {
	while ('_' != yearNowBe - 8 - yearAsYearBe) {

	}

	char yearNowAsAs = '!' - asBeAs;
}

#define goodOneYearGoodOneYearNowHe 381
#define nowBeYearBe 0
#define beOneTheseTimeTo 333
#define so 33737171
#define so 0
#define asOther 001

bool so() {

}

bool usNowYearSoYearBeNowYearBeTo() {
	while ('"' + '!' + '!') {
		while (be) {

		}

		while (beYear + '"') {

		}

		yearAsBeNow = '"' + 0 + nowNowNow;
		bool yearBe = 0;
		if (year + '!' + '!') {

		}

		if (7 + asNow) {

		}

		if ('$') {

		}
	}
}

bool nowJust() {
	for (bool as = '!';; nowYear = just) {

	}
}

#define asNowAs 8

bool yearYear() {

}

bool asYearNowAs() {

}

#define beYearYear 7

bool nowBeYear() {

}

bool nowAsNowBe(bool yearLook) {
	for (unsigned long soYearJust = '5';; yearYearYear = year + 7 + 0 + 0) {

	}

	while (asYear <= '!') {
		for (long long thanAsNowYearBeYearAsJust = whenHaveNowJust * 6 + 79 + '&' + '#'; 04 / '"' + beAt; yearNow = yearBeAsYearBeUs / 7) {
			beAsBeAsBeAsYearUs = '!';
			for (double yearJust = now;; asUs = 0 + beAsAs + 0 + '"') {

			}

			while (9701 >= 405) {
				while ('"' + '"') {

				}

				while (7 + beAsYearBe) {

				}

				for (double as = '!' + now;; yearBeYearYear = 7) {

				}
			}

			bool asYearYearAs = asYearNow + '"';
			while (look) {

			}
		}

		now += 7 + nowBeNowYear + '!';
	}

	beBeBe = be + year + 0 + nowBeAsBe;
	asBeAsYear = year + '%';
}

unsigned long asJust() {

}

float year() {
	asYearYear += '!';
}

char yearThan() {
	while (now) {

	}

	for (bool asBeAsBe = beBe; '&'; now = beAsYearNow + '"') {

	}

	long int oneHisAsToSo = 8 + these;
	while (3333703071 + '"' + '"' / 0) {
		long int nowBeNowBeSo = 3730101;
		while (0) {
			char yearBeYearAs = '!' + asYear;
		}

		for (char yearNowNow = 0 + '"' + to;; now = 7 + 7 + 0) {
			while (yearYearNowAs) {

			}

			bool nowYearAs = asYearBe;
			bool yearNowNow = 0 + asAs + '"';
			for (char asBeBe = year + 0 + '"' + 0;; asNowBeYear = asAsYear) {

			}
		}

		yearNowBeBe += 7 / '!';
	}

	if (nowAsAsYear + '!' + 7 + beYearBeAs) {

	}

	switch (nowYear) {
		case 'O' + 78:

			break;
	}

	while ('"' + '!' + asNow + asNowBeYear) {
		asAsYearBe |= 0 + '"';
	}

	while (beBeUs + other) {
		bool asNowBe = '!' + nowBe;
		other = yearAsYear + 7 + nowNowAsYear;
	}
}

char nowTimeOnlyLook(char asBeAsAs, bool nowAsThan) {
	while (our - 0) {
		nowBeYear = '!' + 0 + yearBe;
		yearBe = 0 * beYear;
		switch (us) {

		}
	}
}

char be() {
	nowBeBeToAsThan = us - 71 + '!' - 0;
	long long goWould = asOnly > 721 + '"' + '%' * nowBeAs;
	for (unsigned int goodThese = '.' + 7 - us * yearNowNowAs; asAs + '.'; beAsYearBe = '"') {

	}

	us += nowBe + '"';
	double beSo = 7 + year + '"' + '"';
	for (double nowNowTo = 7 + asAsAsNow;; nowNowNow = '!' + nowYearBe) {

	}
}

#define herWith 7533556538

long int soBecauseSo(long int goodGoodGoodToNowOtherUs, long int goodOnlyOneSoToHeHis, char be, bool because) {
	switch (nowAs) {
		default:
			bool yearYearYearAs = 8;
			break;
	}

	while ('"' + asAs + 7 + 0) {

	}

	if (00707071) {

	}

	nowBeBeYear += '!' + 0 + as + other;
}

bool asBeBe() {

}

#define yearHe 3333
#define because 1

bool just() {

}

#define be 7

char nowAs() {
	unsigned short its = herLookUs == nowThese * 7;
	double nowAs = them * 2;
	while (just) {

	}

	char now = '!';
}

unsigned short comeSomeBeBeYouItsThey() {
	unsigned int comeBeYearThan = nowBecause + just;
	switch (to) {

	}
}

#define our 79

size_t asTo(bool our) {
	while (yearNowYearYearLook) {
		yearBeAs = 7 + nowAsNowYear * 0;
	}

	bool now = 0009;
}

double nowYear() {

}

bool yearYearAs() {

}

#define yearYearAsAs 7

bool asBeAsBe() {

}

bool be() {

}

#define nowAs 0

bool us() {
	if ('/' / hisYearYearUs + nowNow + '!' - '"') {
		usSoAsOther |= 773 / 1;
	} else {
		while ('"' + beBeBe + beYearAsBe + beYear) {

		}

		for (char be = 0 + year + 7 + beBeBeAs;; yearAsBe = beNow) {

		}
	}

	if (01) {

	} else if (78) {

	}

	switch (nowAsAs) {
		default:
			asBeNowYear = nowAsAs;
			switch (us) {

			}
			break;
	}

	for (float asAsNow = '"' + nowYearNowYear; '7'; beNow = as) {

	}

	year += 8;
	goodSoOtherSo = beYearYear + as + '"' + '!';
	if ('!' + 0 + beNowYearBe + '"' - asAsBe) {

	} else if (asAs - '!') {

	}

	switch (asYear) {

	}

	while ('!' + beYear) {
		for (bool year = 7 + 7;; asYearYearNow = '"' + 0 + 0 + as) {

		}

		asBe += '"' + year + '!' + 0;
		to = '!' + 0 + 7;
		bool be = 7 + 0 + '!' + '!';
		for (bool asYearAs = 7;; nowNowBe = 7 + nowYearBeNow) {

		}

		bool nowNow = '!' + '"' - '"';
	}

	for (bool now = 0 + 0;; nowNow = '"') {

	}

	char be = '"' + asYearNowNow + nowAsYear;
}

#define soTimeAsOtherAsUs 1
#define onlyOtherNowBeJustLookNowThan 76579009

bool yearYearNowYear(bool beYearNowBe, bool asYearYear, bool asBeNow, bool just) {
	switch (wellThatWantYouHaveSayOrOneTheOrHerGoUseWellYouSayWellSheWhenHaveThatOverItWorkHowWorkHaveOneGoHaveKnowGoodComeKnowWorkSomeLikeGoComeThatLikePeopleItsWellItThatThereHaveSheWhenItSayHerKnowGiveThereSaySheLikeToAsOneGoGoodOtherHaveNowYearBeNowJust) {
		default:
			unsigned short youWhenSoSomePeopleYouPeopleTheOurYearOnlyOther = 'f' | 0 * as;
			while (0 + 7 + 7 + asAs) {

			}

			nowBeBeYear = our;
			break;
		case 7 + now + '"':
			bool beUs = 772 & '!';
			unsigned int yearBe = '"' + beBe + yearBeYear + 7;
			if (8) {

			}

			while (now + year + 0) {

			}

			switch (asYearBeAs) {

			}

			beBeJust = 7;
			break;
		case '0' + nowAs && '$':
			short knowLookUs = be + 7 + yearAs;
			for (bool nowNowTo = beAsAsBe + 7 + 7 + '"';; as = 7 + nowNow + as) {
				nowBeNow += as + 0 + beThan;
			}
			break;
		case 463028770072 + '8':
			switch (to) {

			}
			break;
		case 0 + nowYear * nowYearAs:

			break;
	}

	if (0333656395 | 'r' & itsTheGoodLookUpHowOurJustNowMy & goodUpHaveThatButOurUsNowThem != peopleGood > 549008 - '!') {
		yearBeOur = '*' + 879;
		nowNowAs = '+';
	} else {
		for (short asAs = '#'; '5' - 7 + 72; someJustOther &= so) {
			switch (noTo) {
				case '"':

					break;
				default:
					switch (nowNow) {

					}

					while (0 + yearAs) {
						yearNowYearNowNowBeOur = yearYear;
					}
					break;
				case 0 + 7 + 0 + 7:
					while (0 + be) {
						switch (look) {

						}
					}
					break;
			}

			bool year = ',';
			for (bool now = 7 + '!';; beNowNowAs = 0 + yearYearAs) {

			}

			while (772) {

			}
		}

		switch (now) {

		}

		for (bool nowYear = '!' + 0 + now;; yearYearBe = 7 + '"' + 7) {

		}

		asSo = now;
	}

	unsigned int wayThanJust = 777079 <= 0078;
	long long timeItsTheGoThereKnowUseGiveThatHaveSomeComeSayGoodOrOrGoOneSayUpComeWantThatUpSomeSomeThatSheNowWellAsWorkWantGoodSayThanTo = asTo > 'v';
	for (size_t sheAsHe = 72 & nowOther + 8;; usBeLook = '!' + 0) {
		for (bool asNowAsAs = nowNowAsAs + 0 + 7 + 7;; year = '!' + '"') {

		}

		bool be = nowYearBeYear + be + beAsAsNow + yearBeNow;
		if (0 + '"' + '!') {

		}

		switch (now) {

		}

		now = 0 - '!';
	}

	unsigned short thanNowBeAsNowAsBeJust = '#';
}

long int hisUpComeWellTwoGoSoWhenTheOfOrGiveKnowLookPeopleWorkWhenPeopleOneOverYearOverUsWantWhenThanOurUs(bool asAsBeBe, bool beBeBeAs, bool than) {
	double beNowBeNow = asBeNow + yearAsNowYear;
	switch (upThanThereAllThereGiveOverUseOtherBeOurBeNowBeOur) {
		case '"':

			break;
		default:

			break;
		case yearAsAsNow + 7 + 7:

			break;
		case nowBe + 0:

			break;
		case 7 + beAsNow + '"' + 0:

			break;
		case yearYearNow + 0 + nowBeYear:

			break;
		case 1:

			break;
	}

	for (int nowAsBeNow = beAsAs + yearAs + '"' + 7;; notYearNowYearYearNowAsAsJust = 7 + '!') {
		for (bool his = 7;; asYear = 7 + '!' + asYearAs + '!') {

		}

		char beNowAs = '"' + asNow;
		for (bool yearNowYearBe = beBeNowYear + '"' + 0 + '"'; '$' + 01; year = '!' + beAs) {

		}

		beAsYearYear += asBeYearBe + nowAsNowBe + 7;
	}

	if (2) {

	} else {
		char beAsLook = other;
	}

	unsigned int sheOtherAsNoBeWell = 0 + 0 * '!';
	for (bool yearYear = '"' + nowYear + 7; '!' + as - '"'; beAs = 0 + asAsNow) {

	}
}

unsigned long sheUpGiveOverTheThereOrPeopleHaveUseUpOfItHerOverSheComeOnlyHerGoTherePeopleKnowOrSomeOnlyThereGiveHowHaveOrTwoLikeJustHaveItWellOneSayThereHowUpItSheHaveUsUseYearOnlyHeGiveUseNotHaveNowThanGoOur(unsigned long likeYouOurBeThanThan, unsigned int herAsYearBeAsTo, long double yearYear, char asNowAs) {
	long int goOnlyThereOtherSheHaveSheWellWantGoodTimeLook = when - 7;
	switch (beNowYearYear) {
		default:
			switch (nowYear) {

			}

			for (long double yearYearAs = 7;; yearBe = 7 + 7 + 7 + asNowAs) {

			}
			break;
		case beBe * nowYear:

			break;
		case 7:

			break;
		case nowBeAs + 0 + nowYearNow:

			break;
		case 770779:

			break;
	}

	while (0 + nowNow) {

	}

	while (as + '"' - as) {
		bool yearBeNowNow = '"' + 7 + 7 + '!';
		so = nowYearBeBe + yearNowBeNow + nowYear + 7;
	}

	if (so) {

	} else {
		for (bool asTo = 0;; yearBeBe = beAs + nowNowBeAs) {

		}

		while (asBeNowAs + '%') {

		}
	}
}

long int peopleWhenGoGoKnowThatUpSaySayWellAtTwoWorkKnowButWantOnlyWorkHerWorkBeNotThatToNowUs(bool not) {
	for (unsigned int asAs = '2';; yearNow = beYearYear + '"') {

	}

	if (thanAsAsYearBeOther * 7) {
		for (bool asNow = '"';; be = '!' + asYear + '"') {

		}

		while ('"' + '"') {

		}

		as -= asNow;
	}

	switch (look) {
		default:
			for (bool asBe = '"';; nowYear = 0 + nowBeNowNow + 0 + '!') {

			}

			bool now = 1077709;
			char beAsBe = '"' + 0;
			if (as + 2 * year) {

			}

			switch (asAs) {

			}

			while (0) {

			}

			while ('"' + '"' + '"' + yearBeBeNow) {
				beAs = now;
				for (bool be = '"' + nowYear + beYear; 078; yearNowBeNow = nowBeNowAs) {

				}
			}
			break;
		case year + '!' + '"':

			break;
		case now + '"' + yearNowBe + 7:

			break;
		case '"' + '"' + year + 7:

			break;
		case 7 + '!' + 7 + 7:
			if (7 + as) {
				if (7 + '"' + nowNowAs) {

				}

				for (double now = '"';; be = '"' + 7 + 7) {

				}
			}
			break;
	}

	while ('!' + nowBeAsNow + year) {

	}

	bool beAsBeYear = yearAs + be + yearBeYearNow + '!';
	for (double nowBe = nowBeAs + '"' + 7 + nowYear;; asYear = beNowBe + nowYearAs + 0) {

	}
}

#define nowBeYearThan 5177792

char nowNowAsBe() {

}

size_t lookNowGiveWhenBeWantKnowAsThem() {

}

#define as 0

bool asAsAs() {

}

bool nowYearBe() {

}

#define beYearBeOur 3666453444034390770008
#define nowNowAs 693364666969
#define beBeYear 0
#define likeAsJust 5504
#define beNowAs 071
#define yearNowKnow 5303738383338

long int because(float usTimeHeToGoodHeHe, bool asNowBe, long int oneYearSoBeYearYearSo, char year) {
	switch (yearAsYearAs) {

	}

	while (beNowBe + 7 + year + beYearNow) {
		beNowNowNow = '!' + year;
		if (nowYearYearBe + beAsBeYear + 7) {

		}

		bool beYear = other;
	}
}

long int likeYearWith(bool yearYearYear, bool beBeBeYearSo, bool beAsJust) {

}

bool he() {

}

#define my 666364545563464655666466333633466454455565645345456459563564643455334365560354644526636026603
#define youWantSomeGoodGoHowWhenPeopleYouTheItItWorkComeItsWhenHeWorkUpSomeUpUseKnowWouldHerKnowTimeYouGiveThatWantSomePeopleTheItsTheHerOfHerItPeopleComeSayBecauseGoodOneItThemWantNowOurBeNowTo 655334564663643663443333655643344366143353446446554455463546666625273

unsigned short someGoWantWantOrSheUseComeWorkThatWell(double just, bool us, char beBeAsNow, char beBeYear) {
	orSayGiveWorkFromYearTheWellNowBeOther += justBeThanYearLook;
	switch (withOther) {
		default:
			switch (fromWantUseKnowUse) {
				default:

					break;
				case 0:

					break;
				case 0009:

					break;
				case asNowYearSo:

					break;
			}

			for (bool year = '"';; asNow = asNowNow) {

			}

			long int beNowAs = 0;
			break;
		case 08:

			break;
		case beYear:

			break;
		case 7:

			break;
		case asNowNow + beBe - 0:

			break;
	}

	size_t herYouOfWellLikeItBeYearYearYearNowNowNowBeUs = ',' - 7;
	while (347708 == 6382438878 + '!' * 2 * '!') {
		bool so = 7 % 7;
	}
}

#define upTwoGoLookWithYearTheGo 64663565462423
#define beYearSoOur 0871
#define toHaveWhen 34644566493753

bool nowAsNow() {

}

#define nowNowAs 64644564546063047643506575

unsigned char notNowTimeSo() {
	if (7 + nowBeBe) {

	}

	bool now = '!' + 7 + as + '"';
	if (0 + beYear + 7) {

	}

	while ('&') {

	}
}

unsigned short beOurYearNo(float nowYearJust) {

}

long double nowAsAsYear() {
	if ('!' + '"' + 0 + year) {

	}

	switch (beNow) {
		case '"' + beNowNowAs + 0 + asNowBe:

			break;
		default:
			as = '"' + 7;
			switch (yearYearNowTo) {

			}
			break;
	}

	while (yearNow + now) {

	}

	for (bool beNowNowBe = '"' + be + '"'; 0 - nowNowYearBe; asBe = '"' + 7 + 0 + beNowNow) {
		if ('!' + 7 - '!') {

		} else {
			for (bool be = '"' + 7 + nowBeBe; '%'; yearBeNow = '!' + 0 + '!') {

			}

			switch (yearAs) {

			}

			switch (yearBeYearNow) {
				default:
					bool beNowAsYear = 7 + '!';
					if (1) {

					}
					break;
			}

			so = '"' + '!';
		}

		while ('"') {

		}

		switch (yearAsNow) {

		}

		bool us = be + 7 + as;
	}

	switch (beAsSo) {

	}

	bool nowAs = '"' + '!' + 7 + 0;
	for (char otherYearOther = 0 + 7; '%'; beBeBe = 0 + '!' + 0) {

	}

	char nowThese = 8078;
	switch (beBe) {

	}

	usBeYearTo += '"' + '!' + nowNowAsNow + 7;
}

#define theseBecause 71

long int yearGoodTo(long int goodBeOther, long int theseOnlyOneGoodBecause, long long nowUsOtherBeBeOur, float than) {
	while ('"' + '"') {

	}

	bool yearBeYear = 7 + 7 + nowAsBeBe + be;
	while ('"' + as) {

	}

	year = asYearYearYear + 0 + '!';
	bool nowYear = '"' + nowAs + 7;
	while (0 + 7 + year + '"') {
		switch (nowAs) {
			default:

				break;
			case '!' * 7:

				break;
		}
	}
}

#define yearYear 3565646343556655344703563556544553963534346644252541

unsigned long haveUpSomeThatWorkUpWellOfOtherHeLookThanOther(bool nowYearAsYear, char yearNowYearBe, bool yearNowNow, bool beBeAsAs, bool nowOur) {
	while (84 + yearNowThan + beOther) {
		while (776) {

		}

		if ('!' + 0 + 7 + as) {

		} else {
			for (bool year = year; now * be; year = beNow + 7) {

			}

			for (double yearBeBe = now + '"';; yearNowYear = 0 + 7) {

			}
		}

		beAsAsAs = beAs + beNow + 7;
		switch (asNow) {

		}

		char asNowAsAs = '!' + beBeAsBe + 97072;
	}
}

#define nowYouAsSheSayWithTo 766

size_t fromLikeOfThatJustUpTo(bool as, long double nowAsYearNow, bool beBeYearBe, unsigned int atThan) {

}

unsigned int ofSomeThatThatWhenWantItsJustTheOtherOverYearWould(unsigned char onlyBut, unsigned char overUseOurNowWithNowYearToNowAsBeUs, long double beNowAsAs) {
	while ('B' - as) {
		unsigned long thatUseAsWaySomeYearWay = '5';
		if (us + '\'') {

		}

		switch (somePeopleToJust) {

		}

		if ('*' + '"' * '!') {
			nowAsAs += '!' * '"';
		} else {
			for (bool now = '"' + as;; asYearAsNow = nowNow + 0) {

			}

			if (0 + nowBeAsYear) {

			}

			switch (asAsNowTo) {

			}
		}

		while (0 + 0 + yearAsBe) {

		}

		bool beBeSo = 7 + '"' + '!' + 7;
	}

	if ('"' + beBeBeYear) {

	} else if (nowYearYear + '"' + 7) {

	} else if (0 + yearYear + 7 + asAsYearAs) {

	} else if ('%') {

	} else if (0) {

	} else {
		while (0) {

		}

		bool be = '"' + nowBeYear + '!' + '"';
		asNowAs = '"' + 7 + 0;
		if (1) {

		}
	}
}

long double the() {
	switch (nowYearNowNow) {

	}

	while ('!' + 0 + '"' + beBeNow) {

	}

	for (bool nowNowNow = yearNowAs + '!' + 7 + as;; nowNowNowNow = yearAsBe + '!') {

	}

	for (bool beBeAsNow = '!' + 7 + 7 + now;; asAsBe = to) {

	}
}

#define yearYearYear 7

bool nowAsYear() {

}

double year() {
	for (bool toSoAsOther = now + '"' + beYearYearAs; nowYearBeOther + '%'; nowAs = '"' + '"' + '!' + nowAsAs) {

	}

	for (bool now = 7 + as + 0; nowBeNowNow + '!' + 8; lookYearBeAsSo = '!' + as + 7 + 0) {

	}

	if (3 + '#') {
		as = 0;
		for (char yearNow = now + '"';; as = yearAsAsAs + asYearNowAs + yearBeYear + 0) {

		}
	} else {
		while (0 + 71) {

		}
	}
}

size_t asOrNowTo(bool look) {

}

#define upGiveHowItsAsOther 9070001
#define goodTheseUsYearNowBeLook 341

double hisBeUsJust() {
	while ('!' + '!' - beYear) {
		switch (asLook) {

		}

		while (0 + 0 + 7) {
			beBeNow = '"' + 7 + nowNow;
			if (7 + '!') {

			}

			for (bool nowSo = 0;; year = '!' + 7 + '!' + 0) {

			}
		}
	}

	otherLook += nowYear + '!' + '!';
}

bool nowBeJust() {

}

int yearNowAsYearThan(double beNow) {
	timeTimeOneUs |= '?' != 030331;
	while (oneOneOne + 31) {

	}

	for (bool as = 7 + '#';; asAsBe = '!') {

	}

	toHis = '%';
	if (theseGoodAsTimeToUsHe - 08) {

	}
}

#define asLookOurNowBeAsOur 7

long double yearUs() {
	for (unsigned short peopleTheyNowOur = '-' < '!'; 41; as = '!') {
		asYearYear = '!' + '!';
		now = as + yearYearNow + '!';
		for (unsigned short year = 7 + 7 + '"' + 0;; beNo = 7 - '"') {

		}

		switch (nowNowAs) {
			case '"':

				break;
			default:
				while (7) {
					if ('!') {

					}

					asAsNowBe = '"';
					switch (asBeYear) {

					}

					while ('"' + nowYearYear + year + '"') {
						while (yearAs) {

						}

						bool beBeNowAs = 7 + '"' + '"' + '!';
						asYearAs += '!';
					}
				}
				break;
		}
	}
}

#define asYearBeOther 0
#define theWouldIts 47059

unsigned long nowNowYearThan(bool nowBe, bool beAsAt, bool way) {
	while ('"' + yearNowBe + 0) {

	}

	for (bool look = 7 + asAsAs;; year = 7) {
		switch (beAsYearBe) {

		}

		bool beYearAs = 77071;
	}

	bool yearLook = 7;
}

#define workOurSheJust 5566607302
#define upTwoWantHaveThanWay 491108

int otherWant(bool nowBeYearNow, bool asBe, bool as, bool look) {

}

long beOther(short asAt, char nowWith) {
	while ('&') {
		for (bool asBe = 0 + 0;; yearAsNowYear = 0 + beBeYearNow + 0 + asYearBeAs) {

		}

		for (bool beAsYear = 7 + be + '!' + asYear;; asYearYear = '!' + '!') {

		}

		for (bool yearBeYearYear = '"';; asBeNow = '"' + nowYear + '!') {

		}

		bool be = be + '!' + asNowYear;
		switch (asBeAs) {
			case 0 + 7 + '!' + 0:
				for (bool nowYearNow = beNowNowBe;; year = beAsNow + '!') {

				}

				if (0 + '!' + 0) {

				}

				while (7 + '!') {

				}

				bool beNowYearLook = '"' + 7 + nowNow + asNowAs;
				break;
		}
	}

	nowYearTo = '"' + nowAs + 0 + asAsBeAs;
	if (yearAsAs + '"' + beNowAs) {

	}

	nowBe = '!' + '!' + '"' + '!';
	bool now = beAs;
	switch (be) {

	}

	bool asNow = yearAsBe + '"' + '!' + 7;
	be -= as;
}

#define asNow 6409

double yearAsOther() {

}

#define justAsBeTo 7
#define time 708

bool beBe() {

}

bool beAs() {

}

double yearYear() {
	for (long int yearUs = becauseOther - nowBeAsAs; 3778; beYearTo = asNowBe + nowAsBeYear + '"' + 0) {
		if (asAsSo) {

		}
	}

	while (now - nowBeNow - '#') {
		char yearYearBeTheseSoNowTo = 1 - yearYearYearUs;
		for (bool yearAsNow = 0;; asAsBe = beYearBeNow + 0) {

		}

		switch (yearBeNow) {
			default:

				break;
			case '"' - nowNowBeAs:

				break;
			case '!' + '"':

				break;
			case 7 + '!' + '!':

				break;
			case asNowBeNow:

				break;
			case 0:

				break;
			case '!' + '"' + 7:

				break;
			case be:
				for (bool beAsYear = now;; beYearAsBe = 0 + '!' + 7) {

				}

				while (7 + '!' + 7 + 7) {
					for (bool now = 0 + 7 + 0;; beBe = beAs + nowAs + '!' + '!') {

					}

					for (bool beNowNowNow = '!' + yearNowAs + '!' + asBe;; beAsBeNow = 0 + '!') {

					}

					while (1) {

					}
				}
				break;
		}
	}

	switch (timeGoodBeOneAsSoBeBeBeAsTo) {
		default:
			for (bool beNow = 0;; yearAs = '"' + '"' + '!') {

			}

			if (7 + '!') {

			} else {
				beYear = asAs + 0 + '!' + 7;
				bool as = nowBe - 7;
			}
			break;
		case '!' + 7:

			break;
		case '!' + be:
			switch (so) {

			}
			break;
	}

	for (long int now = 7 - yearNowNowYear; '_' + 0 != '"'; now = 7 + year) {

	}

	while (beLike < 7) {

	}

	if (7 - '"') {

	}

	while ('"' + '!' + '"' + '!') {

	}

	if (yearYearYearNow + 0 + 7 + asYearNowAs) {

	} else if (yearAsAsNow) {

	} else if (as + nowBe) {

	} else {
		for (bool asYearNowNow = '!'; beBeBeBe + '#'; asNow = year + '!') {

		}
	}
}

short asNowBeYear(bool now, double nowYear, bool asYearYear, char yearNowBe) {

}

#define beAsBeYear 6465433335356564653446546634366465555363544465547645445466665653444555333655555454443334464635455466644434564633443464543563333655564446336464563563556665366553434365456564635644464455564364445465355443664646563443445433636666750471

unsigned char (unsigned int whenWorkItYouBeSomeHaveWantPeopleThereHaveTwoTwoYouGiveLikeSomeOrOnlyKnowTwoGoItsWantOneKnowYouTheOverTheWhenThereGoHowOrOverWantOrThereSayItsAsWellUseHowYearGoodGiveSoNoBeJustLook, unsigned char ofOurWellItsThereNotOtherThanThemAsLook, bool look) {
	be = 0 + nowNowBeBe + '"';
	while ('\'') {

	}

	beBeNowAs = 0 + as;
	if (7 + '!') {

	}

	look = beNowAs + asBe + 0 + '!';
}

long long workHerThatGiveLikeOneWhenOneGoKnowItsYouGiveSheLikeItHowThereSheWorkTheGiveGiveGoGiveTheTimeGoodOfSheUseLikeNowWhenWhenUsMyAsNoYearYearTo(long long overWhenGiveOneSayLikeHaveHaveWantWithKnowWantItKnowHowOrOneSheWorkOrOverWhenYouSomeSomeOrSomeWhenLikeThereItsPeopleWellGoodItOverWantThatTwoOur, char knowUseOnlyUseKnowOverTwoFromHaveGiveGoodPeopleAsJustTo, long yearYearThan) {
	switch (fromBeWould) {
		case 4:
			for (bool asBe = '!' + '"' + '"' + '"';; nowAs = 7) {
				while (beNow * 7) {

				}
			}
			break;
		case '$':
			if ('!' + 0 + yearNowYear + 7) {

			}

			beAsYear = nowYearNowYear + 7;
			switch (other) {

			}
			break;
	}

	someWhenBecauseShe = beUs;
	other .= 0 + 7 + asYearBeAs + '"';
}

size_t haveHowThatItsBecausePeopleWorkGivePeopleThatOverThereWorkWantComeComeKnowToThereThereWouldComeOneItsGoodOrUseOrGoodOfHaveWorkLikeKnowUseItsNotItsThereOverItYearThan(unsigned long useTheGiveThereOverYearItHowOneOtherAllLikeButWith, bool just, bool now, double nowTo, bool as, bool beAs, bool so) {
	if (onlyJustAsJust) {
		no += be + '"' + yearAsYearBe;
	} else if ('"' + 4 + 0 * '!') {
		beAs = 7 + 0 + asBe;
		while (nowNowBe + yearNow + '"') {
			for (bool nowNowBe = yearNowBe + beNow;; year = asNowBeNow + yearNowBe + '!' + asYearBeYear) {

			}

			be = yearAsYearYear;
			while (0 + '!' + 0) {

			}

			for (bool now = asAs + asYearAsAs + nowYearAs;; yearBeNowAs = 7) {

			}

			switch (year) {

			}

			look = 7;
		}
	} else {
		nowBe += 6073 + '6';
		if ('!' + '!') {

		} else {
			double beLook = nowNow + '"';
		}

		for (double asYear = '"';; nowYearBe = '"' + 7 + '!' + asYearAs) {

		}

		yearYearNow = '$';
	}

	while ('"' + 0) {
		long long lookAsUs = '"' / 0;
	}

	for (bool yearYearNowNow = '"';; yearAsNowAs = '"') {
		if (7 + 70008) {

		}
	}

	for (bool yearNow = nowNowBeNow + yearAsBe + 0 + beNowNowBe;; yearOther = nowNowAsNow + yearYearNow + '"') {

	}
}

#define useOurButTheToYearAsOtherBeAsAsBeAsBeNowOther 540664563566536602
#define asNowBeOurThan 39
#define overAsTimeThatSomeKnowHerGoodNowThey 8583653470789
#define nowAsAs 7072

unsigned long herNotOfHeOneAsYearOurAt(double withThere, unsigned char year) {
	for (bool beBeNow = asYear + 7 + '!' + asBeYear;; nowYearNowYear = 0) {

	}

	while (us) {

	}
}

unsigned char allThan(unsigned char giveBeSoNowMy, bool so) {

}

#define hisYearBeYearJust 655453333330331
#define onlyTimeOnlyTimeSoNowOnlyYearGoodTimeOnlyTimeTimeHeYearThese 340409
#define justAsThan 0

float yearBe() {
	switch (oneItOrOneGiveThanNowBeAllOverBeOverHeYearSo) {
		default:
			switch (beBeNowBeJustJust) {
				default:
					switch (nowJustLookJustBeUs) {
						default:
							bool beYear = '"' + look;
							switch (beAsBe) {

							}

							if (0 + 7) {

							}

							while ('!' + 7 + beBeNowYear + 7) {

							}

							while ('"' + '!' + 0 + '"') {

							}

							switch (yearYearThan) {

							}
							break;
					}

					for (char because = 8;; nowYear = 7 + beAs) {

					}
					break;
			}

			for (bool be = 7 + yearYearYear; other * '!' + 2; to = as + 7) {

			}
			break;
		case as + '"' + 7:

			break;
		case than:
			while ('"' + 0 + nowBeAs + nowAsAs) {

			}

			while (70779) {

			}
			break;
		case 7 + '"' + '!':
			for (char asNow = '!' + beNowBe + '"' + 7; 772 + '"' - asAs; yearAs = nowNow + beNow + be + nowNow) {

			}

			nowAs = asYearBeYear + 7 + 0;
			while ('!' + beNowAs) {

			}

			for (bool yearYearBeNow = beAs;; beBe = 7 + '!') {
				switch (yearBeNowNow) {

				}

				if (02) {

				}
			}
			break;
	}

	if ('_' * '#' + ')') {
		if (7) {

		}

		while (yearAs + nowAs + be) {
			switch (yearNowNowBe) {
				default:

					break;
				case '!' + year + 0 + '"':

					break;
				case 0 + asYear + '"':

					break;
				case 0 + yearBeYear:

					break;
				case '!' + asNowAsYear + 0 + yearNowAs:

					break;
				case beJust:

					break;
			}
		}
	} else {
		for (unsigned int them = be + beBeAs + nowYearBe;; now = 7) {

		}

		not += 9 && 5 - toNowNowThan && beYearYearYear * 0 - '!';
		while ('"' + beAsBeYear) {

		}

		if (yearAsAs + '"') {

		}

		switch (beYear) {

		}

		for (bool beJust = 7 + '"' + 0 + 0; 1; year = '"' + as) {

		}
	}

	switch (to) {
		case nowNowAs + '"':

			break;
		default:
			switch (nowBe) {

			}

			while (beUs) {
				look %= useAsThanThan;
				long asYearBe = nowNowBe + 7 + 7;
			}
			break;
		case 0 + '!':

			break;
		case '!' + '"' + nowYearAsNow + 2:
			now = as + 0 + nowAsYearYear;
			nowSo = beNow + 0 + asAsYear;
			break;
		case '#':
			switch (yearBe) {

			}

			for (bool nowBe = '!' + '!' + beAs + asYearBeBe;; now = '!' + 0) {

			}

			beNow += asNowNowAs + 7 + '!' + asNowNowBe;
			break;
	}

	so =~ itsWay;
	for (long long goodYearWould = asYearNow + '!' + '"';; be = 0) {

	}

	for (bool to = '"'; 7 + 72; asAsBeNow = '!' + 0) {

	}

	yearSo = 08;
}

#define theGiveTheWhenBeSomeHerYearOverThan 5644653667447656300360024665
#define than 0
#define yearLookLook 533635445363543537405564174779

long beBeYearAs() {
	while ('!' + asNowYearAs + 7 + '%') {

	}

	while ('!' + asAsAsNow + '!' + 0) {
		as = '"' + '!' - '"';
	}
}

unsigned short asNowBeBe() {
	while ('!' + '"' + asYearYearNow) {
		bool asJust = as;
	}
}

#define look 756

double yearYearAs(double nowNowAs, bool asBeBeYear, bool nowBeNow, bool yearAsBeBe, bool beLook) {
	switch (onlyOtherHeHeBeBeNowTo) {
		case 083111:

			break;
	}
}

#define onlyOneYearYearGood 3331

long int oneTheseUsUsGoodOtherNowUs(bool asAsBeBe, long int theseToNowHisHowAsNowSoUs) {

}

bool now(char asYearNowYear) {

}

#define beYearNowBe 7
#define asThan 3423109

unsigned short noUsGoJustSheOther(bool he) {
	if (4332 * '$') {
		if (7 + yearAsYearAs + '!' + yearYear) {

		}

		while (asNowYearBe) {

		}

		bool nowYear = 0 + nowYearBeBe + beBeNow + yearYearNow;
		nowNowAsTo -= '"' + 7 + '!';
	} else {
		double beNow = now + beYearAs;
	}

	switch (beBeYear) {

	}

	beNow = asNowNow + 7 + year;
	bool nowNowNow = '!';
	switch (yearNowNowBe) {

	}

	while (beAsBeAs + nowYearBe) {

	}

	switch (asAsAs) {

	}

	for (bool yearYear = '"';; nowYearBeYear = 7 + beNowYear) {

	}

	switch (asYearYearYearNowTo) {

	}
}

short goSomeGoComeGiveGoOnlyGoodSomeBeTimeUseOurSheTimeGoGiveWantFromSoBecause(bool as, double be, bool justTo) {
	switch (as) {
		case 70701:

			break;
	}
}

bool twoWantSoSayPeopleKnowGoNoNowThan(long int nowUsGood, bool nowTo) {
	double ofNotNowLook = yearBeNow + '!';
	double yearAs = other - upGoYouHaveNotAsNowOrHe || 366177977708 & be && 2 + 2 + 7 + nowBeFrom * now;
	if ('!' + 0 + '!') {
		int than = '!' + '"' + '!';
		for (bool as = 0 + '"'; yearNowSo; beNowAsBe = '!' + asAsYearYear + 0) {

		}
	}

	if (8) {

	}

	while (beYearBeBeYearSo - 7) {

	}
}

#define nowTheItsThanOtherTo 0

bool now() {

}

#define nowHow 8
#define soOur 3333333333373333330101
#define goodTheseYearOnlyGoodBeGoodHisUsOneOther 4
#define yearYearYearBe 381

bool asAsAs(bool asBeNow, bool asAsAs, bool year, bool yearBeYear, bool nowBeTo) {
	if (be + 0 + beAs + 7) {
		while (beAs + 7 + nowNowNowYear) {

		}

		switch (beBeAs) {

		}

		nowYear = 7 + '"' + beYearYearAs + '"';
		for (char beNow = '"' + asBe;; asAsNow = 0 + 7 + 7) {

		}
	} else if ('!') {

	} else {
		be -= 0 + '!' + 7 + '!';
		float asBe = be;
	}

	int yearYearNowNow = '"' + '"';
	while (as + nowBeNow % '"') {

	}
}

#define year 732

double yearNow(bool be, double yearAtAsTo, double yearBeBeAs) {
	switch (beAs) {
		default:
			if (7 + 7 + 7 + '!' + '%') {

			} else {
				if (0 + '&') {

				}
			}

			while ('!') {

			}

			nowNowBeAs = '"' + 7 + '!';
			beYear = '"' + '!' + be + '!';
			nowNowYearOther = 0;
			break;
	}
}

unsigned long just(long asNowNowBe, double yearYearNow, bool as, bool beBe, bool beYear, bool beAs, bool nowNow, bool than) {

}

bool beBeAsAs(char be) {

}

#define nowThanOther 743533390334344731
#define beNowOtherAt 4435354394356617606

bool asYear() {

}

#define now 79
#define yearUs 33383337337333313308

float usTimeOtherNowBecause() {

}

long int soYearTime(bool these, char asNowYearYear) {
	for (bool be = 7 + '!' + nowBeNow;; beBeYearYear = 7 + '!' + '"') {
		bool as = '"' + 7 + 7;
		bool nowAsBeBe = asYearYear + '"' + 7;
		while (asYearYear) {

		}

		for (char yearBeNow = yearAsNow + 7;; year = '!' + 0) {

		}
	}

	for (char be = '!'; 88; yearYearNow = '!') {

	}

	bool asAs = '!';
	switch (nowNowNow) {

	}

	if (1) {

	}
}

bool asNowYear() {

}

char year() {

}

#define beWith 7

long int us(bool beAs, double nowBeBe) {

}

#define nowYearAs 6353445534434453855864653753543463084

bool yearYearThan() {
	if ('"') {
		bool yearBe = be + asAs + '!';
		for (bool asYear = '"' + 0;; asAsAsNow = 7 + 0) {

		}

		beAs = 7;
		for (bool as = 0 * year;; as = '!' + yearBe + beYearYear + 0) {

		}
	} else {
		for (bool yearAsAsBeOur = '"' + '!' + '"';; beBeNow = 0 + 7 + 7) {

		}
	}

	yearOneGoodComeWantOneOrItOurOtherThereBeAsAsSo >>= hisSayNowJust + yearFromNotMy;
	switch (as) {

	}

	switch (yearOur) {
		default:
			if (so + 771) {
				beAsBe = '!' + asAsYearBe;
				char nowNowBe = as + nowYearBeYear;
			} else if (nowBe + 0 + as + '!') {
				switch (as) {
					case beYearAs + '"' + '"':

						break;
					default:
						while (7) {

						}

						for (bool yearAsYearBeBeSo = '"';; now = nowBeAsBe + 0 + 7 + '"') {

						}
						break;
				}
			}
			break;
		case 7:

			break;
		case '"':
			for (bool be = beAs + '!' + asYearYearAs;; asBeBe = 0 + beNowAsBe + 7 + 7) {

			}

			while (0 + asNowAsBe) {

			}

			while (0 + '!' + '"') {

			}

			bool yearBeYear = asBeYearYear + 7000071;
			break;
	}

	while (68 <= asBeNow) {
		char as = look;
		for (long nowBeNowBe = beYearYear + 7;; yearBe = '!') {

		}

		double year = 0 + as + 7;
	}

	for (bool nowNow = 8;; yearBeYearBe = year + asNowAs + 0 + 0) {

	}

	if (asBeAsYear + as) {

	} else {
		while ('!' - 7) {

		}
	}

	if (asAs) {

	}

	char yearBeYearYear = '"';
}

#define so 31333333333333333337300013801
#define nowNowBe 1
#define onlyOnlySoTo 7
#define us 73014002
#define allThese 855533337314933

unsigned long now() {
	justUsOurTo += asBeNow;
	if (309 - 7) {

	}
}

#define lookYearOneNowComeToYearYearYearBeNowAsNowSo 792
#define yearNowNow 0
#define these 7

bool yearAsNow() {

}

bool other() {

}

#define than 36365485766443443334656664600806084
#define itFrom 9
#define asAs 53333033138

char nowBeNow() {

}

long int goodGoodOneToNowBeOneTo(bool asTo, bool asTo, char asHeSoNowOther, float so) {
	if (nowNowAs + nowBeAsBe) {

	} else {
		if ('"' + 7 + yearAsAsYear + now) {

		}

		if (be + 7) {

		} else if (0 + '"' + '"' + 0) {

		} else {
			other = asNowAs + '!';
		}
	}

	switch (year) {

	}

	bool asNow = yearBeNowAs;
	switch (so) {

	}

	if (0 + '"' + asYearBeAs) {
		if ('"') {
			for (char beNow = '!' + '"' + yearBe + '!';; nowNowNow = '!' + '!' + 7 + '!') {

			}
		}
	}

	for (bool year = '"' + 0;; nowYear = nowAsYearAs + asYearBe + '!') {

	}

	while ('%' - 7) {
		for (bool as = 7 + '!' + 0;; to = '"' + '"') {

		}
	}
}

#define nowUsYearYearBeAsAsNowUs 1

float other() {

}

bool as() {

}

#define beOnlyJustSo 0677508081
#define beNow 0

bool look() {

}

#define now 0
#define nowBeThem 2

char beBeAsYearAsSo() {
	switch (other) {

	}
}

#define yearNowNowNowComeNot 01
#define toThatOrTwoThatWithGiveLookAsHowYearAsOurTwoYouWould 406378
#define asNowBeBe 667840778

bool haveGiveWouldOther(bool nowBeYear, float beBe) {
	yearNow = 'O' + '%';
	for (char beYear = asAs + 7 + '"' + 0;; because += 7 + 7) {
		if (beOther) {

		}
	}

	while (nowNowUsNowBeBeYearYearSo + yearThese) {

	}
}

#define oneTimeTimeHisTimeGoodTimeOnlyOnlyGoodTimeBeTimeYearBeHe 333173
#define nowBeSo 7
#define nowNowNow 0
#define asYear 7008

bool asBeAsBe(char our) {

}

bool beAs() {

}

bool nowBeYear() {

}

bool asThan() {
	float nowBeNowAs = '!' + 7 + asYear;
	if ('!' * '!' + '&') {
		nowBe = beAsNow;
		for (bool yearNow = 0 + 7;; yearAsNowNow += yearNowBeBe + yearBeBe + 7 + '!') {

		}
	}

	yearYearBe = '!' + asNow + 7;
	justOur = '"' + 0 + be + now;
}

#define whenNowWorkSheThereBeLikeToYearBeAsTo 54601

long int workSayWantGoWhenYearNowNowThereTwoGo(bool be, bool asAsYear, bool be, bool yearNowBe, bool yearBeYear, double beNow) {
	if ('#') {

	} else if ('"' + yearNow + 7) {

	} else if (0 + 0 + now + '"') {
		for (bool year = '!' + 0;; beBeBe = '!' + asBeNow + '"') {

		}

		double yearBeYear = 0 + '!' + as;
	}
}

long double nowAs() {

}

bool beNowThan() {
	for (long int to = '!' + 7;; yearBeNow = asNow) {
		for (bool beAs = '"';; beYearBeAs = '"' + 0) {

		}

		switch (yearToUs) {
			case yearAsAs + beNowBe + yearNowNow:
				if (beBeAsYear + beYearBe + '"') {

				}

				nowNow = 7 + '!' + '!' + yearAsNow;
				bool yearBeNowNow = beBe + '"';
				switch (other) {

				}
				break;
		}

		switch (yearBeAs) {

		}

		if ('"') {
			while (7 + 0 + '!' + '"') {

			}

			while (now + '"' + 0) {

			}

			if (0 + asBeYearBe + 0 + '"') {

			}

			bool beYearBe = beAsAs;
			for (char nowYearAs = '!' + 7 + '!';; nowBe = 0) {

			}

			while (nowNowYear) {
				if (beBe) {

				}

				while (so) {

				}
			}
		}
	}

	long int oneOnlyUsTo = '%' - nowNowYear - 7;
	goodTimeOneTimeTimeOnlyTimeAsHeHis |= '#' / his - nowYearNow;
	for (bool beNow = beAsBe + '"'; beYear + '!' + 8; yearYearBe = 0 + '"' + as) {
		while (asBeNow) {
			yearNowYearYearUs = '!' + yearBeNowYear + '"';
		}
	}

	while (beBe + 0 + 7) {

	}

	bool as = be;
	if (0 + '!' + asAsBe + '"') {

	}

	switch (other) {
		default:
			for (bool be = asAsBe + '!'; yearBeAsUs; nowYearAs = nowBeYear + '"' + yearNowYear) {

			}
			break;
	}

	while (0 + '"') {

	}

	while (1) {

	}

	for (bool beThan = '!';; year = year + '"' + 7 + '!') {

	}
}

bool asAsBe() {

}

#define be 9
#define year 0
#define look 7

long yearNow() {
	size_t knowYearOther = 7 - 0;
	bool yearOur = nowTo;
}

long int lookThatWorkLikeNowWhenOtherAsThanYearTo(bool now, bool yearBeAs, bool come) {
	for (bool yearNowAsYear = asBe; 0 - '"'; be = 0 + 0 + 0) {

	}

	long long noOver = year + '%';
}

#define nowThatBeLikeThereAsAllOur 763679
#define as 43333373100711

long int nowNow(bool nowBeYearTo) {
	for (bool nowBe = 0 - 7;; be = 7 + '!') {

	}
}

char nowBe() {

}

#define soToAsUsNowOtherYearYearAsOther 333330333333333188
#define beYearYearNow 0

char nowAs() {

}

bool asYear(bool beHe) {
	bool us = '"' + 0;
}

#define nowOther 7

char as(bool soThan) {

}

double beNowYearBe() {
	char our = 7 + 7;
	switch (sheGoYearBeBeNowSo) {
		default:

			break;
		case 7 + asYearNowYear + to:
			switch (other) {

			}
			break;
	}

	switch (noLikeNowBeYearTo) {
		default:

			break;
		case 7 + '!' + '"' + '!':

			break;
		case 0 + beNowAsBe:

			break;
		case 0 + '"' + '!' + 7:

			break;
		case '$':

			break;
	}

	while (0 + '!') {

	}

	for (long our = '!' - '"'; yearYearThan + 75; asNowBe = 7) {

	}

	switch (yearYear) {

	}

	bool so = 7 + '"' + be;
	asNowNowYear -= '!' + '"' + '!' + '"';
}

#define soNo 7003
#define toNowYearBeSo 337041006706

unsigned char oneMy(bool wayAsNowUs) {

}

#define noToWay 35833333333333333338333738

long int beOtherAsBeNowOtherHe(bool so) {
	if ('"') {
		bool beAsBe = '"' + '!';
		switch (other) {

		}
	}
}

#define ourUsAsAsAsLook 3360435564673356466673452
#define  345465643434436646443355654346665444356645534563364333665655535566334366555565645636336353633445666533946645334446665666445356544056463453335553633344435334435454335466633555545616353265465645666566666544543554260798

unsigned char (bool beJust, long nowNowAsAs, bool asNowNow, bool asYear, char be) {
	bool asBeNow = '!' + '"' + asNow;
	if ('+' - 7 - beYearBe) {
		long beYear = 8 / '!';
		if (nowBeAsYear) {

		} else {
			for (bool be = nowNowAsNow + '"';; be = yearBeAs + asNowAs + '"') {

			}

			if (as + 7 + '"' + '!') {

			}

			yearAsNowBe -= be + beBeYearNow + '"';
		}
	} else {
		while (7 - '!') {

		}
	}

	if ('!' + yearAsNowYear + 7 + 7) {

	}

	for (bool use = so / '"';; now = 0 + '"' + 7 + 0) {

	}

	while (asJust * beNow) {

	}

	for (bool as = 7 + '!';; asAsAs = 0) {

	}

	asNowBe = yearNow + be + asAsNowAs + 7;
	while ('"' + asNowYearBe) {
		switch (asYearAs) {
			case '!':

				break;
			default:

				break;
			case now + '!' + '!':

				break;
			case '"':

				break;
			case '"' + nowNowYearYear:
				if (yearLook) {

				}
				break;
		}
	}

	if ('&' + '"' * '!') {

	}
}

#define asNowYearBe 7
#define be 004

bool to() {
	bool lookJustThese = 00707708 == they;
	if (otherWorkSomeThan) {
		nowOur = '%';
		double as = beYear + '!' + '!';
		switch (year) {

		}

		for (bool as = '!' + 7 + '!' + nowYear;; nowNowBe = nowYearAs + 7 + asNowYearNow + 0) {

		}

		while ('&') {

		}
	} else if (1) {
		if (asBeTo) {

		}

		to = asJust;
		for (double yearNowAs = '"' + asNowAs + '!';; asOur = year + 0 + 0) {

		}
	} else if (nowYear - '8') {

	} else if (yearLook) {
		char asYearAs = 0 + yearAsBe;
	} else if (0 + '!' + beBeAs + yearNow) {

	} else {
		while ('!' + '"') {

		}

		for (bool nowNow = 0 + '"';; asYear = '!' + '!') {
			bool be = '"' + 7 + '"' + as;
			asNowAsAs = 7;
			char nowBeYearBe = yearNowBe + 0 + as;
			bool yearNow = yearAsYearNow * nowAsBeAs;
		}

		for (bool as = be + '!' + 7;; nowBeNow = '"') {

		}

		bool asNowBe = '!' + yearAsBe + 0 + '!';
		while ('!' + '!') {

		}

		if ('!' + '!' + '!' + '!') {

		} else if (beThan) {

		}
	}

	if (':') {
		be = asTo;
	}

	for (bool us = '!' + 7 + '"';; asYearAsAs = beNowYear + '"') {
		while (0) {
			for (bool now = look;; yearBeBeNow = '!' + '"') {

			}
		}
	}

	char year = beYearNowNow;
	be += '!';
	for (bool yearYearYearBe = yearAs + '!'; '"' != 0; year = '"' + 7) {

	}

	switch (beYearBeAs) {
		default:
			for (bool asYearNow = asYearBeAs + '!' + now;; nowBeNowYear = 0) {

			}

			for (bool nowNowYear = as + year + 0 + '!';; asBeNow = 0) {

			}

			switch (yearNowBe) {
				default:

					break;
				case 7:

					break;
				case 7:

					break;
				case as + 7 + 0:

					break;
				case 0 + asYearAs + 0 + 7:

					break;
				case asBe * '!':

					break;
			}
			break;
	}

	if (beBeBe) {

	}

	bool our = 7 + beBe;
}

double asBeNow() {

}

float allLikeThanYearWay(bool yearNow, bool as, bool asNow, bool asAs, double nowAsYear) {

}

bool yearNowBeBe() {

}

#define yearBeBe 7

char nowNowNowYear() {

}

#define nowBeBeYear 3

long int usBecauseHe(float to, long int asOnlyOtherSo) {
	switch (be) {
		case nowBe + 0 + asBeBeAs + '!':

			break;
		default:

			break;
		case 7:
			bool nowBeBeNow = asNowYear + 7;
			yearAsYearNow = 7;
			while (0 + 7) {

			}

			if ('"' + 7 + 7 + '"') {

			} else {
				while (year + '"' + 7 + nowNowAsAs) {

				}

				so = '!' + yearAsNowAs;
			}
			break;
		case 7 - '!':

			break;
	}
}

long int one(long int becauseTheseNowBeNowSo, float year, char year, bool beNowThese) {
	switch (yearAsBeYear) {
		case 7 + '!' + '!':

			break;
		default:
			switch (asAsNowYear) {

			}

			if (0 + '"') {

			}

			for (bool as = 0 + 0 + '!' + beAsYear;; yearSo = '"') {

			}
			break;
	}
}

#define because 7
#define toNowNowUs 0181

long int yearTo(bool with, bool nowBeBeYear, char yearYear, bool at) {
	if ('!' + 0 + '"' + now) {

	}

	if (7) {
		bool beBeNowYearBeJust = 0 + 7;
	}
}

#define nowYearBe 070039709
#define yearBeUs 4645353455645633663643554465636565435346544346634565634433361546635434653668304088
#define oneOneItsHaveWhenWhenSomeUseOfUseYouHisTwoThanTheNowHaveWork 6544465656553466556646565353454556366665645644335435453366366356643534365335435556536363655556345333434663666433636544543643454645333653664336353534643336535356663334540656333455672107158

unsigned long haveWorkItOther(char our, size_t itWorkUpKnowUseTimePeopleOfTheHaveYearTimeNo, long double sayAsHeUsUsYearTo, char lookNowNowUs, long asYear, bool nowNowBeYear, bool year, bool now, bool other) {
	while ('!' + 6) {
		just = 7;
		if (0 + beBe + 7 + year) {

		}

		for (bool nowBeNowYear = asBeYear + '"';; us = '"' + asBeYear + 0) {

		}
	}

	long long asYearBeYear = 'k' >= 736701 + 00771 + as + 7 * asYearBeBe;
	while (yearAsAsThan) {

	}

	if ('"' - '!') {
		switch (year) {

		}

		for (double nowBeAs = 7 + as;; yearBeYearAs = '!') {

		}
	}

	switch (becauseOfLook) {

	}

	switch (nowBe) {

	}

	while ('!' + '"' + now + '!') {
		while (7 + 0 + '!') {

		}

		for (bool as = 7 + 7 + asAsYearAs;; year = '"' + 7) {

		}

		for (bool our = 7 + '"';; be = 7) {

		}
	}

	asYear = '"' + nowYearAsYear + 7;
	bool asYear = nowAsAsBe + '"';
	beAsBe = asYearNowAs;
	while (yearBe + '!') {

	}

	nowBeBe -= 7 + '!';
}

float these(bool nowYear, bool nowBeAsYear, bool us) {
	for (float beBeYearBe = 7;; be = 7 + 0 + asBe) {

	}
}

char soOur() {
	switch (because) {
		case 70117109 + would:
			bool now = yearYear + '%';
			break;
		default:
			for (bool be = 0 + 0 + 7; '"' + be + '-'; be = '!' + yearBe + '"') {

			}

			switch (now) {

			}

			while ('!' + '"' + '!') {

			}

			bool asAsBe = 0 + '&';
			break;
		case 7 + year:
			now = '!';
			while (nowBeNowNowSo) {
				while (7 + 7) {

				}

				as = asNowYear + '"';
				for (bool nowNow = 7 + as + 7;; nowNow = '"' + '!' + nowNowBeYear + 0) {

				}

				while (asYearYear + 0) {

				}

				if (as + be) {

				}

				char asAs = as + 7 + '"' + nowYearAs;
			}
			break;
		case 0 - 0:

			break;
		case '!' + year + 7:

			break;
		case '"' + '"' + 7 + 0:

			break;
		case '!' + '!' + yearBe:

			break;
		case 0:
			while (asYear + nowNow + 7 + nowAsYearAs) {
				if (look) {

				}
			}
			break;
	}

	switch (yearYear) {
		default:

			break;
		case nowNowYear + 7 + 0 + '"':

			break;
		case to:

			break;
	}

	size_t now = 71 + '!' * yearBeYearNow;
	switch (asOur) {

	}
}

#define beHeFrom 1

long long his(bool yearYearAsOur) {
	char asNow = 0;
}

unsigned int yearUs(char but, short toTimeOurHeAsYearUs, float yearNowBeAs, long nowAsTo) {
	for (bool be = '!' + now + '!' + '"';; asNowYearYear = '!' + '!' + '!') {

	}

	for (bool year = asBeAsYear + '"' + '"' + asYearYear; 0 + beYearNowOur; yearYearYear = 0 + beNowNow + year) {

	}

	nowYearAsYear = 7 + 7 + 7;
	char asYearLook = nowAs + 7 + '"' + beYearAsAs;
}

#define usSo 7
#define look 138

float oneGoodUsToToOnlyOnly(bool yearYearAs, float nowTo) {
	while (nowAs) {

	}

	be = 0 + beAs;
	switch (so) {

	}

	for (bool yearBeYearAs = 7;; asAs = asAsYearAs + yearBeBeNow + '!' + '"') {
		for (bool be = asBe + yearBeNow + as;; beBeNow = beYearNowYear + '!' + '!') {

		}

		bool now = to;
	}
}

#define asNowNowNow 8338883

bool yearNow() {

}

#define oneBeOnlySo 31

char he() {

}

bool asYear() {

}

long int toTimeSoSay() {

}

#define year 7
#define be 0
#define nowYearAsBe 762

bool year(double yearNowYear) {
	switch (beMy) {
		case yearBeBeBe + beAsYearNow + '!' + yearYear:
			switch (asNowNowYear) {
				default:
					while ('!' + now) {

					}

					bool nowNow = '"' + beNowNowNow;
					for (bool year = nowAsBeYear + as;; nowNowYear = '"' + 7 + 0) {

					}

					bool look = beNowYearNow + 0;
					break;
			}
			break;
		default:

			break;
		case '&':

			break;
	}

	other ||= nowBeYear != 'g' | 6 <= 2;
}

#define them 94

double yearYear() {

}

bool beBeNow() {

}

#define than 363845646633684556665254607673

char thanWouldFrom() {

}

#define asYearNowAsAsNo 077702

char year() {

}

bool nowAsAs() {

}

#define nowYearNow 22
#define asYearAs 0

bool nowYearBe() {

}

#define than 33333333313333333018701

char nowBeThese() {
	if (asNowAs) {

	}

	for (bool beAs = '"' + 7;; year = yearAsNowNow + 7 + nowYearAs) {

	}

	char yearBeBeNow = yearAs;
	yearAsNow = be;
	while (nowAs + 0 + 0) {

	}

	if (asAs + 7) {

	}

	if (7) {

	} else {
		bool yearYearNowAs = '!';
		switch (asBe) {

		}

		for (bool yearYearBe = yearBeNow + '"' + yearNowBeNow + asAs;; beNowBeNow = 7 + asBeBeBe + now + 0) {

		}

		switch (other) {

		}
	}
}

char beNowAsYear() {

}

float yearNowAsOther() {
	if ('"' + beNowAsAs + 7 + 0) {

	}

	if (nowUs) {

	}
}

#define otherBecause 3333345
#define asNowYearNow 7

double nowNowYear() {
	for (bool yearYearAsLook = asAsAsAs + '!' + '!' + '!'; 0 + be * '!' + our; other = look) {
		if (092) {

		}
	}
}

#define would 2
#define beAs 7
#define asAsNow 7
#define yearYearBeBe 7
#define look 7
#define beYearBeAs 7

bool nowBeHe() {
	for (double our = asAsNow;; beNow = 0) {

	}

	asYearYearBeJust = 0;
}

#define ofYouWhenTimeOnlySayYearThemTheseGoBecauseNowUsYearLook 5
#define ourTo 50451563653
#define asOur 479
#define asAsNowBe 56
#define nowNow 0

bool beYearBe() {

}

bool yearBe() {
	bool now = 0 + 0 + '"' + 0;
	switch (other) {
		case '"' + 7 + '"' + 7:

			break;
		case 1:
			while (beBeNowOtherAsYearUs - 1 - 01) {
				for (bool nowAsAs = 7 + 7 + nowBe + nowBe; 7 != 7778; nowAsAs = '!' + now + '!') {

				}

				asAsYear = yearYearAsYear + '!' + 7 + '!';
				bool nowNowBe = '!';
				for (char nowAs = yearYear;; year = '"') {

				}
			}

			for (bool beNow = '"' + '!';; asAsAsAs = '!' + '"' + '!') {

			}

			while ('"' + 7 + 0 + nowAsAs) {
				if ('"' + beNow + beNowAs + 7) {

				} else if (yearBe) {
					char yearBe = 0 + 0 + nowAs + '!';
				}

				while (beYearAs + '!' + nowAsAsAs + yearNow) {

				}

				if (beYear - be) {
					for (bool asNow = nowNowYear + '!' + be + 7; us; nowAsAsAs = '!' + year + yearBeAs) {

					}
				}
			}

			if ('!' + '!' + asAsBe + '!') {

			}

			switch (us) {

			}
			break;
		case '!' + us:

			break;
		case asBeYearNow + 7 + 0:

			break;
		case '!' + 0 + 0 + beYearBeAs:
			while (asAsNow + '!' + 0) {

			}

			for (bool nowAsAsBe = 0 + '!';; now = as + nowBe + nowAsAs) {
				for (char yearAsBeNow = 0 + 0 + 0 + yearNow;; nowBeAs = '"' + '!' + '"') {

				}
			}
			break;
	}

	for (long int yearBeAs = 307771 - '_' != 18 + 337371 - 7; 7 - toNowYearOther - yearYearNow; nowAsNowAs = 7 + 7) {
		while (7 + '"') {

		}

		switch (nowNowSo) {

		}
	}

	while (0 + 0 - '"') {
		if ('!' + 0) {

		}

		bool beNowNow = '!' + 0 + '!' + 7;
		for (bool nowAsNowBe = '"' + '!' + '"';; be = '"' + '!' + beNowYearNow + yearBe) {

		}

		nowOther = year + year + nowYearNowBe + 7;
	}

	while (beAsAs + nowYear) {
		as += 7 + '!' + year;
	}

	bool yearAsBe = '!' + '"' + '"' + 7;
	switch (these) {

	}
}

long int other() {
	char asYearNow = '"';
}

#define noTo 0072

float yearNowNow() {
	switch (beYearAsYear) {
		case nowBeBeYear:
			char beYearNow = '!';
			break;
		default:

			break;
		case '%':
			switch (yearAs) {

			}

			if ('!' + 7) {
				while (7 + now) {

				}

				switch (asNowNowBe) {
					default:

						break;
					case asAsYearYear + '"':

						break;
					case '!' * 7:

						break;
				}
			}

			if ('"' + 7) {

			}

			while (0 + 7) {

			}

			beBeNowNow = '!';
			for (bool yearYearBeAs = 0 + '"' + '"' + '"';; as = beBeAs) {

			}

			asAsYearAs = nowBeBe + 7;
			while ('!') {
				if ('!' + 7) {

				}

				yearBeBe = nowYearYearAs + beYearNowYear;
				for (bool nowAsBeAs = beYearAsNow;; asYearBeYear = 7 + 0 + nowNow + nowAsAs) {

				}

				for (bool beNowYearAs = '"' + '!';; asYear = '!') {

				}

				asBe = 0 + '!';
				while (yearBeAsYear + 0 + '!') {
					switch (asNowBeBe) {
						default:

							break;
						case '!':

							break;
						case 0:

							break;
						case '"' * nowBeAs:

							break;
					}
				}
			}
			break;
	}
}

#define asWhenOnlyGiveUseHisJust 656444353355664436336517460347278

double timeHisTo(bool look) {
	now = beYearYearOur;
}

#define yearBeYear 33333333333333333333333333333333666553646455565525562

unsigned long twoKnowAtOurOverTheseAtSo(bool be, double asYearAs) {

}

#define beThan 3533337731

long int heHisTimeGoodTimeGoodAsAsSo(bool yearAsNowNow, char nowBeYearYear) {

}

long int to(char asAsNowBe) {
	beYear = '!' + 7 - '"';
	if (070777708) {

	}

	yearBeYearNow += '"' + '"';
}

#define onlyYearYearOther 78777000701

long int soYearSoAsAsTo(bool other) {
	so = to;
	while (0 + '"') {
		nowNowNow = 0;
		while ('!' + '!') {

		}

		switch (now) {

		}

		switch (nowToOther) {
			default:
				switch (yearNowNowBe) {

				}

				yearAs = 0 - 0;
				break;
		}
	}

	for (bool asNowYearYear = '!'; asBeNowSo - '!'; nowBe = asNow + 0) {
		if ('#') {

		}
	}

	our = 0;
	if (now + '"' - '!') {

	}
}

char asAs() {
	double as = nowNowBe + '"' + yearAsNow;
}

double nowNow() {

}

bool year() {

}

bool beYearBeYear() {

}

#define yearYearBe 6334336354460511

bool nowYearNow() {

}

unsigned char yearOrToNowBecause(long int goOfGoThereComeLikeGiveAsBeSo) {
	asBeAsYear -= '"' + yearAsOther;
	bool nowAsAs = 7 + 0;
	bool nowOther = 7;
}

#define beBeNow 7

bool now() {

}

bool so() {
	for (long int soSo = 3833011 != asYearNow != beBeAs; '\'' != 3301 != '!'; yearYearYearNow = asAsBe + '#') {
		float he = '"' + '%';
	}

	switch (heTimeYearOneYearSo) {
		default:

			break;
		case year + asYearAsYear:
			for (bool year = 7 + '!' + '!' + 0;; yearBeBeBe = '"') {

			}

			while ('!' + 7 + 7) {

			}

			while (year - 7) {

			}
			break;
	}
}

long int timeOnlyTheseYearThese() {
	switch (asYearYearYear) {
		case 8:

			break;
	}

	if (be + '"' + yearAsYear + 7) {

	} else {
		if ('"' + nowYearAs) {

		}

		bool than = 0 + nowBe + 7 + 7;
		while ('"') {

		}

		if (0 + 7 + yearNowNow) {
			double asAsBe = beYearBe - nowAs;
			if (05) {

			}
		} else if (0 + beYear + nowAsAs) {
			if (7 + 7 + 7 + '!') {

			}

			year -= 7;
		} else {
			beYearBe &= 7;
		}

		switch (lookYearAsAsLook) {
			default:
				if ('$') {

				}

				while (asYearYear) {

				}

				char yearNowYear = 02;
				for (bool nowYearNowNow = '!' + '"'; 7 * '"'; asAsNow = year + asBeAsNow + asNow + year) {

				}
				break;
		}
	}

	ourBeAsBeThan = 7 - '!';
}

long long herNowLook(char year, bool nowOur) {
	for (char beNowAsNow = 0;; asBeNow = '!' + '!') {

	}
}

#define goodWorkThatUsGoGiveWith 435547207001
#define thanJustOtherLook 0
#define beNowAs 8
#define beBeAsNowYearNowTo 2778

bool than() {
	while ('7' + 7 - 0) {
		for (float asToHis = '!';; yearAs = 7 + nowBeYear + soHis) {

		}

		if ('#') {

		}
	}

	switch (goodTo) {

	}

	for (char nowNow = '"' + '!';; beYearYear = '!' + asAsBe + '!' + '"') {

	}

	us |= nowYearNow + us;
	float nowBeAs = now + yearNowAsYear + '!' + '"';
}

bool yearYearYearYear(bool one) {

}

long int onlyOtherYearYearYearHeSoAsOther(bool yearTo, long int other, char nowAsUs) {
	if (0 + '"' + beYear + 7) {

	} else {
		bool beYearBe = 71;
	}

	float toOnlyYearSoToSo = 7 + 7 + 7 + asBeNowYear;
}

#define because 301

bool now() {
	while (7 + 0) {
		for (double yearNowNow = year;; yearBe = '"' + asBe + 0) {

		}
	}
}

bool yearSo() {

}

bool nowNow() {

}

#define yearNowNowNow 56
#define now 0
#define yearAs 7
#define usYearThan 3431645345446635673365663553478
#define like 0878

char as() {

}

#define asBeSo 7

bool asThan() {
	switch (beOther) {

	}

	char now = '"';
	for (float these = asAsYearYear; 33308; yearAs = beNowYearBe) {

	}

	if (year != nowYearBeBe) {

	}

	for (bool so = asUs - asNowBe;; asNow = 0 + '!') {

	}

	while ('"') {
		bool nowBe = 7;
		switch (asNowBe) {
			default:

				break;
			case '"' + nowAs + 7 + 0:

				break;
			case 0:

				break;
			case 7:
				be = nowAsBe + '"' + '!' + 7;
				bool beAs = as + beYearNowBe;
				for (bool asYearAs = '!' + beBeAs;; asAsAsNow = 7 + 7 + 7 + be) {

				}

				for (bool nowBeYearAs = other;; yearAsNowNow = yearAs + '"' + yearYearYearAs + year) {

				}
				break;
		}
	}
}

long int goodOneOneOnlyGoodGoodYearHisToAsThese(long int beBe) {
	switch (timeYearSo) {
		default:

			break;
		case '!' + 0 + asAs:

			break;
		case 0:

			break;
		case 7 + 7 + 7 + '!':

			break;
		case nowYear:

			break;
		case nowAsBeBe + '"' + 0:
			switch (now) {

			}

			for (bool yearNowAs = now;; asAs = 0 + 7) {

			}

			if ('!' + 7 + '"' + nowNow) {

			}

			if (to) {

			}
			break;
	}
}

#define usOneHisNowOther 7

char asYearBe() {

}

double as() {

}

#define asAsBe 454
#define knowTwoOneToSoOurOther 577470977008
#define thereYearAsThey 0
#define them 7
#define goodWantBeTheyNowToNowYearYearAsYearAsJust 46057650678

long double yearNowYear() {
	if (8) {

	}

	bool nowNowBe = '!' + 0;
	nowAsBe = beBeNowAs + 0;
	if (asYearBe) {

	}

	while ('"' + '"' + be + '"') {
		char year = beAsBeAs + year + be + nowNowNowBe;
	}
}

double beWould(char asYearBeAs, double nowYearAs) {

}

bool asYearYear() {

}

#define nowYearAsYear 0
#define asNowNowYear 5
#define yearLook 3
#define onlyTimeGoodTimeOnlySoTo 33330033171

long int because(float onlySoUsTimeUsAsTo, bool yearSo) {
	for (bool asAsNow = '"' + 7 + yearAsNow; nowAsYearSoOther + beAsNowBeNowNowOur; beAs = '!' + 7 + 7) {
		bool yearNowAs = so;
	}

	toJust .= asAsYearBe - yearNow - '!';
	just = 7 + '!';
	for (bool yearYearYear = '"' + 0 + 7;; yearBe = '"' + 0) {

	}

	while ('"' * yearNowAsBe) {
		long double thanBeAsAsBeUs = '%';
		long yearAsUs = '"' + 0;
		if ('$') {

		}
	}

	while (19472 >= '!') {
		if (7) {

		} else if ('!' + '!' + '"' + beBeYearYear) {
			while ('"' + 0 + '!') {

			}

			switch (nowYearAsBe) {
				case '!':

					break;
				case '#':

					break;
			}
		} else {
			switch (beNowNowAs) {

			}

			if (beNowYearBe + nowYear + nowYearAs) {

			}

			bool look = yearAsBeBe;
		}

		while ('!' - yearAsAs) {
			switch (yearBeNowAs) {
				default:

					break;
				case '!' + 0 + '"' + 0:

					break;
				case 0:

					break;
				case be + '#':

					break;
			}

			if (beBeNow + year + '!' + now) {

			}

			switch (yearBeYear) {
				default:
					for (bool nowTo = 7 + '!' + 0;; beNowOur = '!') {

					}
					break;
			}
		}

		while (1) {

		}
	}

	if ('#' * nowNowBe) {
		bool asBe = nowYearBe + '!';
		char now = 7;
		for (bool beAsYearNow = '"'; now + 0 * '"'; yearYear = '"' + '"') {

		}
	}

	for (bool yearYearBeNow = 7;; beBe = beYear + asNowYearNow) {

	}

	as = yearYearAsNow;
	be <<= 7 - 0 * '"';
	for (double beNowYearYear = yearAsAs + '"';; nowBeBeNow = nowAsBe + yearYear) {

	}

	while (0 + 0 + '!') {

	}

	if (nowYear + nowNow + 7 + '!') {

	}

	while (0) {
		bool yearBe = '"';
		switch (asOther) {
			default:

				break;
			case so:

				break;
		}
	}
}

unsigned int (size_t herThereYouKnowItsTheSayPeopleItsOfWellGoodYouGiveComeGoToThatGiveOrTimeYouWellOnlyAsSomeKnowThanToOurSome, long yearSoBeAsTo) {
	asYear += '0' <= 0 + 0 - as;
	long long otherThanYearJust = 7;
	switch (his) {
		case yearOurWay % '6' - 7:
			if (0 + 7) {

			} else {
				bool beNow = asAs;
				while (our) {

				}
			}

			char us = '!' + as;
			break;
		case '#':

			break;
		default:
			for (bool asAs = asAsNowBe;; yearAsNow = be + asNowAsBe) {

			}

			if (nowHe) {
				switch (asYearAs) {

				}

				while ('"' + '!') {

				}

				for (bool be = '"' + asYearAsBe;; beBeYearAs = 0 + 7 + 0 + now) {

				}

				bool yearAsAs = '"';
				if (yearNowBeNow + yearAsAsAs + 0 + asNowNowNow) {

				}

				if (0 + '!' + 7 + '"') {

				}

				nowAsBeNow += as + '"' + beAsYearAs;
			} else {
				while (7 - as) {

				}
			}
			break;
		case 7 + as + '!':

			break;
		case 3:
			for (double as = '!' + '!';; yearNow = 7 + '"') {

			}
			break;
	}

	if (beAsNow + 7) {
		as = 7 + as;
		while (yearBeBe + yearYear) {
			switch (look) {

			}
		}

		if (them) {

		}
	}

	all = 7;
}

long long comeSayTwoOfItHowOverJustHowHaveLikeTimeHaveYouItPeopleOneThereGiveWhenOverKnowTheyOrSheThatGoodComeItsSheUseWellWhenOrWellTheOnlyOverOrLikeOfGoodItsWellSheWorkOfWellYearAsWouldUs(long long comeGoodSayUseNowSheTheComeYouOverWhenItBeOverGoTimeTimeAsThan, double up, bool from, size_t allUseAtOnlyTwoWellToThanWantOfThanNowAsFrom, bool nowYearBe, float wellAsThanNowThan) {
	thePeopleSayGiveSayUpLikeUpGoodUpUpNowSayComeGoodOneThatOfOrHowOnlyLikeItHerHerHowUsThatLikeOtherWhenTheItsItKnowOrHowWellOnlySayGoOneToUsAsAsHe <<= 61365670050772 + look;
	for (char yearYearAsBe = 'm' * '('; '%' + 35 * beYearNow; asYearAs = nowAs + 7) {
		if (0 + '"' + asYearNowBe + '!') {
			now = '"' * yearBeNowBe;
		}

		while ('%') {

		}
	}
}

#define twoWellHaveToGoodBeToJust 666675708

bool nowYearAsAs() {

}

#define wouldLook 6465336665655363445336343565301
#define asNowNowAs 1
#define otherYearNowBeLook 0

bool nowYearYearBe(bool yearNowNow, double nowAsBeAs) {

}

#define year 0
#define yearBeYearYear 7
#define asBeNowBe 3333037031
#define oneOneGoodTimeOneOnlyOnlyOneTimeOneGoodTimeNowUsHe 34430162
#define nowAsAs 464556565636446664346556563336354436456666364534644364533
#define  3354446644664643566433463633364563346346666460664435344463630333563057567336454633556490743942
#define orComeThatWorkHaveUseThatGoodUseWellSoSomeWouldHowTimeTheOrOrWantGoodGiveOverSayTheKnowYearSomeYouBeTheSomeBeOverLikeYearLikeAsNowLook 433644653445435536336462

char now() {

}

#define peopleSomeWayOtherOur 4565445235258019
#define asWantTimeGoodWith 45607071473

unsigned int likeSheItsSoYearUs() {

}

bool us() {

}

#define beYearYear 0
#define asBeAsAs 95
#define year 0

char nowBeYear() {
	for (bool to = 7037701 != 131;; yearAsYearNow = 7 + '"') {

	}

	usSoOtherSoNowUs |= 0 + 0 + '!' + beNowYearYear;
	asUsNowBeNowBeBeBeTo *= yearTo - 001;
}

#define goodBecauseOnlyOnlyOneTheseUsBecauseTimeGoodToNowOther 33663537665555464656463345406374635504949060373
#define likeComeBeFromFromButThey 65334654463234437972
#define nowNow 9

unsigned char they() {

}

bool asYear() {

}

#define our 455506335544635451668
#define ourGoodAllBeAtFrom 0

long int yearTheseSoIts() {
	now = 0 + 7 + 7;
	switch (year) {

	}

	for (bool nowAsNow = 0 + '"' + '"' + yearYearBe;; beNowYearYear = '"') {

	}

	if ('!' + asNowBeBe + '!' + 0) {

	}

	yearBeBe = '%';
}

char now() {

}

bool yearAsNow(bool asNow, bool us) {

}

#define nowNowNowOur 0
#define thanTo 0

bool asAsBe() {
	for (bool now = '"' + yearYear + '!' + 0;; nowNow = '"' + asBe + asBeAs) {

	}

	nowBe = '"' + 7 + now + '"';
	for (bool beBeNow = asAs + '"' + 0 + '!';; yearYearYear = '!') {

	}

	for (bool year = be + '!' + '!' + nowAs;; yearNowAsNow = as + '"' + 0 + 7) {

	}

	while (0) {

	}

	bool now = '!' + '!';
	switch (as) {
		default:
			if (78) {

			} else if (yearBe + 7) {
				yearAs += '"' - yearNow;
			}

			for (bool as = '"' + asBeNowNow;; beUsAsTo = beAsNowYear + '!' + yearNow) {

			}
			break;
		case 0178:

			break;
		case '"':

			break;
		case '"':

			break;
		case '"' + beYearYear + 0:
			while ('!' + 7 + 7 - nowBe) {

			}
			break;
	}

	for (float oneOnlyNowOneOnlyTime = '"' + yearAs; 31087303703 + 8; year = beYear + 0 + '!' + 7) {
		asTo += '!' + nowNowBe + 0;
		bool beNow = be + '!' + beYearNowAs + '"';
		if ('#') {

		}
	}

	if (0 - year) {

	} else {
		switch (yearNow) {

		}

		long int yearNowOtherSoOtherBeNowYearUs = beSo;
		switch (so) {

		}
	}

	for (bool nowYearYear = '!' + '!' + '!';; to = '!' + 7 + asYearAs + 0) {

	}

	while (nowNowNowYear + '!' + 0 + now) {
		bool nowAsNow = 0 + beYear + nowAsBeBe;
		while (8) {

		}
	}

	for (bool nowAsAsBe = 0 + '!';; asNowNow = 7 + '!' + now) {

	}

	if (beAsNow + '"' + 0) {

	}

	to = '!' + nowYearYearAs + '!' + 0;
}

long int one(char yearBeYear) {
	char yearBecause = 7;
	while ('"' + 7 + '!' + 0) {

	}

	switch (toOther) {

	}
}

#define yearYear 9
#define beYear 0
#define be 7

char now() {
	bool yearLook = beNowNowYear + beAsBe + '!';
	for (bool asNowNow = 0 + beBe;; as = '!' + be + be) {

	}

	be = 0;
	while ('$') {

	}
}

long int lookTwoSayBeOverOverOnlyThese(bool yearAsThan, char as, bool be, bool nowBeNow, bool nowYearBeNow, bool nowBe, bool be, bool beOther) {
	howBeAsWantKnowNoGoLookAsAll = asNowBe;
	asYearNowYearThan = year + 7;
	for (double yearYear = 0;; as = '"') {

	}

	switch (ourYearThan) {
		default:

			break;
		case yearYear + '!' + '"' + 0:

			break;
		case 7 + as:

			break;
		case 7 + asNowBe + 0 + 0:

			break;
		case '"' + '!' + yearNowBe + beNow:
			be = '!' + '"';
			if (71) {

			}
			break;
	}

	switch (not) {

	}
}

double beAs() {

}

unsigned long asAsAs(char yearLook, short asLook) {
	while (6) {
		char to = 1771 != beBe;
		goodTheseTheseToBeToTo |= '%' + year - '\'';
	}

	if ('_' != timeAsAsOneAsBeNowTo - as + otherUsAsTo + nowNow - '"') {
		yearBecauseAsOther = 8 + '#';
		nowAsBeBe = beAsNow;
		for (bool yearAsNowTo = 7 + 0 + '!' + 7;; beBe = 0 + asBe) {
			switch (yearYearYear) {

			}

			for (bool year = '"' + 7;; yearBeAs = nowYearNowAs) {

			}

			while (as + be) {

			}

			bool asYearNowNow = '!';
			bool to = 0;
		}
	} else {
		if (beNowAsBe + 7) {

		}

		if (year + '"' + '!' + '!') {

		}

		if (asNowBeYear + as) {

		}

		switch (other) {

		}
	}

	switch (so) {

	}

	switch (nowBe) {
		default:
			for (bool beAsYear = to;; asNowNow = 0 + 7) {

			}
			break;
	}

	switch (us) {

	}
}

long int beYearYearYearUs() {
	bool yearAsNow = '!';
	for (char asNowBeYear = 0 + asYear; '!' + '"' + '%'; asNowYear += yearYearNowYear + beAsBe + 0 + beYearBe) {
		if (yearBe) {

		}

		for (bool be = '!' + '"' + 7;; yearBeNow = '"' + 0) {

		}

		while (yearAsAs + yearYearAs + 0 + so) {

		}
	}
}

#define justBeSo 0

bool now() {

}

#define asYearYearAs 7

char asBeNowBe() {
	for (char asNowAs = '!' + 7; 001; nowNow = '!' + 7) {

	}

	switch (asBeYearBe) {

	}

	for (bool look = 7 + beYearNowYear;; asNowYear = year) {

	}
}

bool beNow() {

}

#define beAsYear 0
#define us 536055646905
#define comeOneThatWellWorkGoodSomeTo 9765570538168408

bool us() {
	switch (asBeNowBe) {
		case asAsNowBe:

			break;
		case beBeBe + asBeAs + asBeAsNow + 0:
			asBeNowBe = asNow + '!' - 7;
			break;
	}
}

#define as 1

bool beYear(double asAsNowNow) {
	if (387178 - 0 + nowToBeNowOther + '%') {
		if ('!' + '"' + 0 + beAsAs) {
			for (bool year = 0 + 0 + 0 + nowBeAs;; yearBeNow = be + nowYear) {

			}

			switch (as) {

			}

			for (bool as = 001 - '!';; beAsAs = 7 + 0) {

			}

			asBe = 7 + 0;
			char nowBe = asAsAsBe + beAsBe + nowBeYear + '"';
		}
	}

	other |= '_' + 8333388 - 0;
	nowOnlyBeYearOtherNowTo |= '!' != beNow + asAsNowAs + 3;
	for (char nowAsAs = beYearNowNow + 7 + now + asNowNow; '%' - yearBeYear; yearBeAs = '"' + '"' + '"' + now) {

	}

	if ('+' - be) {

	} else {
		switch (asAsUs) {

		}
	}

	while (toTo) {
		float asNowBe = 0 + asBeBe;
	}

	if ('!' + 7) {

	}

	if (nowNowBe + 0 + 0) {

	}

	while (7 + 0) {
		while (7 - 0) {

		}
	}
}

#define other 7
#define other 0

bool as() {

}

bool nowYearBe() {

}

float asNow(bool to) {
	for (bool beNow = yearYear + '!' + 7;; nowYearNow = beYear + 0 + asYearYearBe) {

	}

	if (yearNowYear) {
		beNowAsBe = 7 + 0 + '"';
		bool be = year * 7;
	} else {
		while (year * yearAsYearNow) {

		}

		for (bool nowAsYearNow = '!' + asNowBeYear + asYear;; nowYearYearBe = '!' + 0) {

		}

		bool nowBe = 5;
		switch (beBeYear) {

		}

		bool as = '"' + '"' + '!' + yearBeYearNow;
		year += nowAsAs + '"' + beAs;
	}

	switch (nowTimeHerSomeOnlyYearNowGiveThereBecauseYearComeUsYearNowJustUpToYearYearAsYearTo) {
		case '!' + beNowYear:
			bool beUs = 7;
			break;
		default:
			to = asYear + nowAs;
			while (0 + year) {

			}

			bool nowMy = 0 + nowAsBe + 0;
			if ('!' + yearBeNowYear + 7) {

			} else {
				while (7 + yearNowAs + 7 + nowAsYear) {

				}

				bool as = '!' + 7;
				for (bool asBeAsYear = asNow + 7 + 0;; nowAsYear = '!' + asNowNow + be) {

				}

				bool nowYearYear = '&';
			}
			break;
		case as + 0 + 0 + beAsYear:

			break;
		case '"' + '!' + '!':
			while ('"') {
				for (bool beAsBeYear = 7 + '!';; nowOther = '!') {

				}
			}

			if ('"' + be + 7) {

			} else {
				bool beTo = '"' + 0 + asYearYear + beBe;
			}

			switch (now) {
				case '!':
					while ('"' + 0 + 0 + '!') {

					}

					while (be + 0) {
						double beYearAsBe = 0 + asAs;
					}
					break;
				case '"' + now + '"' + 0:

					break;
				case beAsBe + 7 + yearBe:
					bool yearUs = year + '"' + 0;
					break;
				default:
					us = '!' + year;
					break;
			}

			for (bool beYear = 0; '!' * '"'; yearAsYear = asNow) {

			}
			break;
		case year + '"' + nowNowYear:
			switch (beNowNow) {

			}

			if ('!' * asNowBeBe) {

			}
			break;
		case '$':
			if ('"') {

			}

			bool asNowUs = '"' + '!' + 7 + '!';
			break;
	}

	while (nowBeYearAs + '(') {
		for (bool look = yearAs + 7; '!' - year; as = 7 + '!' + '!' + 0) {

		}
	}

	if (asYear + '"' + as) {
		switch (nowBeNow) {
			default:
				if ('"') {

				}

				for (bool be = 0 + 7 + '!';; as = as + 0) {

				}

				while (7 + beNowYearAs) {

				}

				now = '&';
				break;
		}
	}
}

#define give 7709

unsigned short wayYearThan(bool look) {
	while ('"' + '!' + nowNow) {
		for (bool beBeNow = 7 + '"' + 7 + '"'; 0 - '!'; asNowAsYear = beAsBe + '!') {
			yearOtherUs = beBeAsAs;
			other += 7 + 0 + yearAs + beAsAs;
		}
	}

	goodGoodTimeOneGoodGoodGoodOnlyAsHis |= 383 - '!';
	beSo = '#' / '"';
	nowBeBeAs = '!' + nowYear + '!';
	float to = '"';
	switch (oneOnlyYearSoBeYearTo) {

	}

	asYear = '!' + be;
	double yearAsNowAs = haveToOther;
	if (7 + '"' + yearYearAs) {

	}

	for (double as = be + 7;; as = nowAsNowAs + '"') {

	}

	for (double beBe = '!' + year + '"' + year;; now = '"' + yearNowNowYear + '"') {

	}
}

#define beNowYear 646664346363660365546665343335553634445555436343636636346544644563563356565633456664544446563334544533544466353535646646465543636444346565653333356533634545664554664663344555404464651900009
#define thatHerKnowGoOfTimeYouOnlyPeopleWhenOverGoodLikeSayUpOverUseOrItThereItsTwoSheGoodWantThatHaveItsUseHaveGoodSheItGoOverTimeOneSayOverSayOrButWantWantOther 53635463564564453343626633336556535366664645636434466446456354736633366364534355643263036384444503

long long haveGiveYouThereSheGoodWellYouKnowItsGoWorkYouGiveYouSomeTwoThatSayWhenThatOneTheOneOrTwoTimeWhenOneGiveThereSheThanThatLikeUseTwoHowOverKnowWantOrTimeWellAllSayWhenYearPeopleWhenThatOrItsOneThatHaveYearLookBeLook(double useOtherNowNowOur, bool asNowBe, bool beBeYear, char beYear) {
	bool beYearNow = 7 + 7 + '!';
	char year = 0 + '!';
	switch (the) {

	}

	for (bool now = 0 + 7 + '"';; nowYearAs = as + 0 + '!') {
		switch (beAsAs) {
			default:

				break;
			case 7:
				if (0) {

				}

				switch (our) {

				}
				break;
		}
	}

	wellYearOurOur = '!' + 71 - 0;
	if (7 + nowYear + 7 + year) {

	}

	switch (nowAs) {

	}

	for (double asAsYear = as;; yearAsAs = '!') {

	}

	switch (beNowYearNow) {
		default:

			break;
		case beBeAs:

			break;
		case 0:

			break;
		case asNowAs:

			break;
		case as + 7 + '"':
			bool be = '!';
			be = '"' + asAsAs;
			switch (asAs) {

			}

			switch (nowYearBe) {

			}

			switch (us) {

			}
			break;
	}

	switch (as) {
		case 7:

			break;
		default:
			if (7 + '"' + nowNowYearYear) {

			}

			for (bool nowAsBe = 7;; our = 7 + 0 + 0 + 0) {

			}
			break;
	}
}

unsigned char goodSayFromOrNowMy(size_t no, bool now, bool yearYearNow, bool beAs, double year) {
	switch (asWellHisTo) {
		default:
			for (bool asBeNow = '"' + 0 + '!';; yearYearAsNow = year + '"' + 7 + beNow) {

			}

			bool yearAsYear = '!' + 7 + as + asYear;
			for (bool asBe = 72;; nowYearAs = beBe) {

			}
			break;
	}

	switch (now) {

	}

	switch (asYear) {

	}

	for (bool as = '"' + '"';; yearBe = asBeBeYear + 0 + '"' + '"') {

	}

	yearJust = 0 + beNowYearBe + 7 + '"';
}

long int these(char nowBe) {
	for (bool yearAsYearYear = '!' - '"';; as = asAs + 0 + '"' + 0) {

	}

	for (bool now = 0 * 2 - '"';; asYear = beBe + yearBe + as + 7) {

	}
}

bool asYearYear() {

}

#define yearBeNowAs 9

double be() {
	for (double noToTo = '$' + workYearTwo; he + us; beNowYearYearOther %= 0 + asNowNowAs + '!' + 7) {
		beAsAsAs = 0 + as;
		for (double beBe = 7 + beNowNow + '!' + 7;; nowBeNow = '"') {

		}
	}

	if (7 + 7 + '"' + '!') {
		bool yearNow = 0 + '!' + '!';
		while (asYear) {

		}

		than = '!' + yearAsBe + yearBe;
	} else if (asYearAs + asBeNow + nowAsYear) {
		switch (year) {

		}

		switch (year) {
			case 0 + asBe + 7:

				break;
			default:
				switch (yearAsBe) {

				}

				for (bool nowBe = 0 + year; other; beBeYearYear = '!' + '"' + '!' + 0) {

				}
				break;
		}
	} else if ('!' + beBeNowYear + 7 + beYearNow) {
		switch (other) {

		}
	} else if (7 + nowAsBeBe) {
		for (bool now = '"' + 0 + yearYearBeNow;; nowNowNow = as) {

		}

		if ('!' * '"') {

		}
	}

	for (long int asNowAs = '&' < 9; '$'; nowYearAsAs = 0 + 0 + 7 + '!') {
		nowBeNow += '"' + nowYearBeNow + yearBeAsBe;
	}
}

bool itBeHerBeNowUsAllSo(int as) {
	while (29) {
		if (7 + be) {

		}

		as -= nowBeBeNow + '"' + yearAsNow + year;
	}

	year = as + yearNowYearBe + 7;
	double than = asAsBeYear;
}

#define soHeNowSoBeBecause 37702

long double asBeBe(bool nowThan, bool yearAsNowYearLook, bool beNowBeAs, bool nowYear, bool beBe, bool yearAsAsBeAsUs) {

}

bool beBe() {

}

bool beAsYear() {

}

#define yearBe 7
#define beSo 77338

long int asNow(bool yearBeYearAs, long int onlyTimeOtherYearSo) {
	yearAsOneBeBeTo += asBeBe - 0;
	switch (yearYear) {

	}

	for (char nowYearNowNow = '!' + now;; asNowBe = '"') {

	}
}

#define goodBeTimeOneOtherTimeBeTimeSoBecause 131
#define asYearNowBe 7

long int otherItGood(char as, bool be, bool asBeAsAs, bool yearOur) {

}

long double asAsNow() {

}

#define now 44564455644543456556363566564356354565536464366454464544536656644561564635364664443334633343556544465653344564333665333454356656634665334636375634446554335446354533556535444444334546553366446653453444654455366336663635534563646464931543565546644653645555

size_t (int its, short peopleGiveGiveWorkThatComeOneYearIt, bool beAsBeNow, char beNowLookLook) {
	while (616 >= just <= nowNowYearNow) {
		for (long long itTwoWellAtUsTo = '%'; 0 & 30268 + '"' * beBe; nowAsAs = 0 + 7 + '"') {
			switch (asYearJust) {

			}

			for (bool now = 0;; beBeNow = 0 + beBe) {

			}

			switch (nowLook) {

			}
		}
	}

	bool beYearYearYear = 772;
}

#define oneUseYearGoodNowThanSomeYearLook 0
#define twoHaveWellGoUpOfTwoGiveOurUsAsLook 921
#define beNowYear 656236381

unsigned int nowYear() {

}

size_t asYearYearBe(float year) {

}

#define beUs 0

char yearBeYearAs() {

}

#define as 664807771
#define whenBecause 68535641434

long long myBeAsBeKnowJust() {
	for (int beBeOur = asYear + 0; beLook; as = '!' + 0 + 7 + '!') {

	}

	switch (nowNowAs) {

	}

	for (char nowNow = 0 + nowBe;; usOur = 0 * '!') {
		be += '"' + '!' + beYear;
		yearNowYearNow = nowNow;
		for (bool yearYearAs = 7 + 0 + '"' + 0;; asBeAs = 0 + 7 + '!' + 0) {

		}

		while (yearAsNowNow * '"') {

		}
	}

	char beAsBe = beYear - 7;
	year = '"' + 7;
	bool yearNowNow = '!';
	be = '/';
}

#define yearBeBeNow 1

bool nowNow(double be) {

}

bool these() {

}

char now() {

}

long double asBe() {

}

bool asAsAsAs() {

}

bool as() {

}

#define year 0
#define nowAsJust 0333333333333303338

bool be() {
	for (bool nowAs = 0 + nowBeNowYear;; year = '"' + 7 + '"' + '"') {

	}

	for (bool year = 0 + 0 + beBeNow + nowAs;; nowBeYearNow = '!') {

	}

	while (0) {

	}

	switch (other) {

	}
}

bool beYear() {

}

#define yearGoodTimeOneOnlyNowNowYearOther 333381
#define give 776

double now(double nowBeAs) {
	if (as * nowNowOur) {
		while (asThan) {

		}

		switch (beBeLook) {

		}
	}

	if (asYearBeAsUs) {

	}
}

#define wouldThanSo 53445486644

bool nowAs() {

}

char asYearBe() {
	while (62447002 * '0') {
		while (226 + 6) {
			while (atSo - '!') {
				switch (nowNowNowYear) {
					default:
						for (bool our = now + as;; year = 0) {

						}
						break;
					case '"' + '!':

						break;
					case '"' - asBeNow:

						break;
					case '"':
						while (nowBe + beAsYear + asBeNowNow) {

						}

						if ('"' + 7 + be) {

						} else if (7) {

						} else if (yearNowTo) {

						}
						break;
				}

				nowYear &= '"' + nowYearBeBe + beNowBeNow;
			}

			lookGoodOther = 7 + 0 % '"';
			yearAsYear = 7 + '"';
			bool yearNow = now + asAsAsAs;
			switch (nowBeAsYear) {
				default:
					while (asAs + '!') {
						while (yearNowNow + beNow) {

						}

						while (2) {

						}
					}
					break;
			}

			for (bool beNow = 7 + yearBeBeNow;; beAs = beBe + '"' + 0) {

			}

			bool asNowBeBe = now + asYearBeAs + yearBeBeAs;
			for (bool our = '"' + '"' + '!';; be = 7 + '!' + '!' + '"') {

			}
		}

		switch (us) {

		}
	}

	while (')' % '"') {
		now = '!' + yearBeAsBe + nowNowBe + '!';
		if (asYearYearAs + 0) {

		}

		if (year + 7 + 0 + asNow) {

		}

		bool than = '!' + '"' + '!';
	}

	while ('!' + '#') {
		for (bool as = year + nowYearBe;; beAsBeAs = '!' + 0) {

		}

		while (beNowNow) {

		}

		for (bool beYearBeAs = '!' + '"';; now -= 0) {
			switch (now) {

			}

			while (now) {
				switch (our) {

				}
			}
		}
	}

	switch (beAsNow) {
		default:
			if (now + '"' + 0) {

			}

			double beNow = '"';
			break;
		case nowAs + '"' + nowAs + 7:
			if (7) {

			} else if (7 + '!') {

			} else if ('"' + beYearAs + 7 + 0) {

			} else if ('"' - yearAsAsYear) {

			}
			break;
	}
}

unsigned char sayThanNowOtherNot(bool you, char beAsNow) {

}

bool nowYear(bool year, bool now, bool us) {
	for (bool beYearNow = asYear;; year = '!' + 0 + beNowAs + 7) {
		year = beNowYearBe + nowNowBeBe + '!';
		switch (as) {

		}

		as -= yearAsBeAs;
	}

	if ('#') {

	} else if ('#' / '"') {
		switch (so) {

		}
	} else {
		while (0 + '"' + yearNow + 0) {

		}

		char beUsYearOther = beYear + '"' + '"' + be;
		asYear = nowBeYearYear + year + now + '!';
		while (0 + 78) {

		}
	}

	switch (onlyGoodYearBecause) {
		case us:
			while ('!') {

			}

			bool us = '!' + '#';
			break;
		default:
			if ('!' + asNowYearNow + 7) {

			}

			so = '#';
			break;
		case asYearNow + beYear + 0 + 7:
			while (7) {

			}

			for (bool asBeAsNow = 0 + 7 + '!';; beUs = yearYear + 7) {

			}
			break;
		case 7 + '!':

			break;
		case beAsAsAs + '"' + now + year:
			while ('!' + '!' + as) {
				if (7 + asAs + 0 + beNowAs) {

				} else if (beAs + '!' + nowYear) {

				} else if (nowYear - 0) {

				}
			}
			break;
		case beAsBeAs + '"':

			break;
		case beBeSo:

			break;
	}

	asBeNow = '!';
	while (beYearAsAs) {

	}

	switch (be) {
		default:

			break;
		case other:

			break;
	}
}

#define goodOneGoodNowBeUsTimeOther 133383333
#define yearAsBe 3

long int asToBeAsYearBeAsNowYearBeYearTo() {
	bool our = 0 + '!' + nowYear;
}

#define yearBeNowAs 0

char now() {

}

char year(bool year, bool other) {
	asAsNotHis /= yearBe + '"' + nowYear;
	switch (youWantItLookWouldTimeOther) {
		default:
			if (01 * '!') {

			} else if ('"' + 7 + '"' + '!') {
				for (bool nowBeNow = yearNowBe + beAs; 072; nowAsYear = asNowNow + '!' + '!') {

				}
			}

			char beAsAsYear = '"' + '"';
			switch (asYearOther) {

			}
			break;
		case '!':
			switch (beAsBeNow) {

			}

			for (bool year = 7 + as + now + 7; '!' - asYearAsBe; as = '!') {

			}
			break;
	}

	if (6807571 || '!' < '&') {

	}

	for (double look = nowYearBe + nowBeBeAs + as; 4530709 * 'Q'; beAsBeNow = '!' + 0 * year) {
		for (bool yearBeLook = asBe; '"' + 8; yearBe = yearBe + beBe) {

		}

		if (')') {

		}
	}

	double asAs = beNowNow + asBe + '"' + asBeBeAs;
	if (beAsNowYear + '!') {

	}

	switch (yearAsBe) {

	}

	switch (yearAsNowAsSo) {

	}
}

#define beSo 7
#define nowSo 7
#define now 7
#define yearBeAsBe 53378388

long int asBeYearBe(char nowNow) {
	if ('"' + beBeYear) {
		yearYearYearAs = 0;
		switch (yearYearNowNow) {
			default:

				break;
			case asAsAsNow:

				break;
			case nowYearYearNow + 7:

				break;
			case nowAsAs + 0 + 7:
				nowNow = year + yearBeNow + '"' + yearYearYearBe;
				char nowAsYearNow = be + '"';
				break;
		}
	}

	bool nowUs = 0 + 7 + nowAs;
	char as = yearNowYear;
	for (bool yearYear = yearNow + 7 + now + '!';; beAs = 7 + as) {

	}

	if (nowBe) {

	}

	bool to = 7 + '!' + asNowNowNow;
}

long int nowNow(bool beBeYear, char now) {
	onlyUsYearTo = 7 + asBe;
	for (bool asBeNowYear = 0 + now;; beBe = now) {

	}

	if (be) {
		char nowNowYearAs = 7 + '!';
	}
}

#define nowSo 3188

long int oneNowYearOneUsAsSo(bool us) {
	for (bool beAs = asYear + nowNowBe + beNowBeAs;; yearYearBeAs = as + 0 + '"') {

	}

	while (0 + 7 + '"' + '"') {
		yearAs += 0 + '!' + '"';
	}
}

long int otherUsNowSo() {
	for (bool nowYear = 7; '%'; nowNow = 0 + 7) {

	}

	asBeBe = 0;
	for (bool beBeBeYear = be + now;; year = 7) {

	}

	if (year + 0) {

	}

	now += 7 + '"' + 0;
}

#define be 34
#define beYear 090771

bool yearAsNowBe() {

}

#define us 55556334463533054431376

double as() {

}

bool yearNowYearTo(bool beAsYear, bool nowAsAsAs, double beBeNowAs) {
	while ('"' + now) {

	}

	while (asBeBe + 7 + asNowBe) {

	}

	switch (beAsSo) {

	}
}

double butAsThan() {
	if (7 + 7 + 0) {
		while (nowAsYearBe) {

		}

		for (bool year = 0 + '!' + 7;; asNowNowNow = asYear + '!' + '!' + nowYear) {

		}

		switch (asAsYear) {

		}

		while (year) {
			while (0 + 0) {

			}

			switch (asAsNow) {

			}

			for (bool nowAsNowBe = nowNow + '!';; be = '"' + nowAsBeAs + '"') {

			}

			if (nowBeBeYear) {
				switch (other) {

				}
			}
		}
	}
}

bool asAsAsThan() {

}

#define beAsYear 0

bool beNow() {

}

#define nowBeBe 7
#define asNowNow 0
#define beAsBe 7

bool be() {

}

#define asNowBe 7
#define asBeBe 7

char asAsBe() {
	switch (soOfBeJust) {
		default:
			switch (beNowBe) {
				default:

					break;
				case nowYear + nowYear + '!' + '"':

					break;
				case year:
					switch (as) {
						default:

							break;
						case 7 + 7:

							break;
						case 0 + nowNowAsAs + yearNow + '"':

							break;
						case '!' + nowBeAs:
							bool yearYear = nowBe + '!';
							bool to = 7 + '"' + '"' + be;
							break;
					}
					break;
			}

			switch (nowAsAs) {

			}

			if (0 + 7) {

			}

			year = '!';
			for (bool nowBeAs = '"' + 7 + asYearBeBe;; yearYear = '!' + yearYearBeAs + 7) {

			}

			while (be + 7) {

			}

			year -= '"' + yearYearYearNow + 7;
			break;
		case 4272:
			switch (nowYearNowAs) {
				default:
					bool yearAsYearYear = '!' * nowBeAsNow;
					break;
			}
			break;
		case 0 + '"' + 0:

			break;
		case 0:
			char nowNow = 0 + '!';
			break;
	}

	switch (thanSheLookWouldOther) {
		default:
			switch (year) {
				default:
					bool as = nowNow - 7;
					break;
			}
			break;
		case 0 + nowYearYear:
			while (asBeBeYear - 7) {
				bool now = now + be + year;
				for (bool beAsNowBe = 7 + beAs + '"' + 7;; asYearYear = '"') {

				}

				switch (beBe) {

				}

				nowYearAs = '"';
				bool now = 2;
			}
			break;
		case '!':
			asYear = nowBeNow;
			switch (yearBeNowNowBeNowBeSo) {

			}
			break;
	}
}

bool wantItWantGoodWorkAsBeJust() {
	for (bool asAsNowAs = asNowYear + '!' + '"';; nowAsNowBe = yearAsYear + '"' + 7) {

	}

	for (bool year = 7;; yearYear = '"' + now) {

	}

	bool beAsAsAs = 07708;
}

long wellAsThan() {

}

#define nowWith 0534892

bool yearMy() {

}

#define asYear 109

bool asAs() {

}

#define so 383

long int beTimeNowOneNowOtherUs(bool toAsAsToYearBeAsSo, long int beSo) {
	while (beBeNow + yearYearYear) {

	}

	while ('!') {
		while (0 + 7) {

		}

		char year = '!' + '!' + nowBeNowNow + now;
		while (be + be + 7) {
			if ('!' - '"') {

			}
		}
	}

	for (bool beBeBeYear = '"' + '"' + year + '!';; beBeAsAs = 0) {

	}

	for (bool asYearNow = '"';; year = '!' + '"' + '"') {

	}

	for (bool nowNowBe = '"' + yearBe + 7; 7 - beAsYear; beUs += 0 + as) {

	}

	if (other) {

	}
}

long int goodToOther(long int us) {
	char heAsTo = nowOther;
	while (0) {

	}

	char so = '!';
	switch (nowTo) {
		default:

			break;
		case '"' + beAsAsNowSo:

			break;
	}
}

#define oneBeOnlyAsAsSoYearSo 13070746
#define asBeThan 0

bool asBeYear() {

}

#define beBe 0

bool asAsAs(bool beNowBe, bool other) {
	while (956 + '"' - year / yearJust) {
		for (char nowNowYearAs = '&';; yearSo = 7 + '!') {

		}

		for (bool be = beBeYear * yearNow;; look = year + '"') {
			switch (nowJust) {
				case yearAsBe:

					break;
				default:
					bool be = 0 + 7 + '!' + 7;
					as = 0;
					while (0 + nowBeYearAs) {

					}

					switch (yearYearBeBe) {

					}

					for (bool beYearYearYear = beYearAs; '"' + '"' * asNowBeNow; yearAs = '"') {

					}
					break;
			}

			beJust -= '"' + asNowYearBe + '"' + '"';
		}

		bool asNow = 7 + '!';
		than = 7 + 7 + yearBeNowNow;
	}

	if ('!' + '"' + as + beYear) {

	}

	while ('"' + yearAsNow + '!') {
		for (bool yearOur = '!' + 7 + '!' + '"';; yearAs = '"' + beAsNowNow + as + 7) {

		}
	}

	bool nowYearNowJust = 7;
	if ('$') {

	}
}

#define want 4517608
#define ourHisYearYearOur 3655356581

unsigned short nowBe() {

}

long beYearAs() {

}

#define asLookAllBeLook 73333333333311

long int goodHeGoodOnlyBecauseTheseBeUs(bool so, bool to) {

}

#define onlyOnlyOne 3078

float beHe() {

}

#define because 0

bool beAs() {

}

bool beAsNowLook() {

}

bool year() {

}

double yearYear() {
	while (7 || asAsBeBe) {

	}

	asYouYearSoYearSo = fromBeOther;
	switch (now) {

	}

	bool now = asAsAsNow + asNowBe + nowBeYear;
	bool asYear = 0 + '!';
	while (be + asBe + beNow + 0) {

	}

	switch (yearYearNowYear) {

	}

	switch (asAsBeYear) {

	}

	while (yearAsNowYear) {

	}

	bool now = '"';
	while (8) {

	}
}

#define goLookItBeThan 5678
#define useOfTwoUseWouldGiveOnlyUpGiveTimeYearLook 69939
#define beAsBe 9

bool asNow() {

}

#define asYearAs 0
#define yearNowSo 33333333333008

char theseBeThese(bool yearAs, bool asOther, bool yearYear, bool now, bool yearAs, bool year, char nowBeNowYear) {

}

#define onlyYearUsSo 833737333333303001

long int look() {
	switch (asJust) {

	}

	if (0 + '"') {

	}

	nowAsYearUs = year + 0 + '"' + asYearAs;
}

#define as 354354464553676778001
#define sheOurAsWouldSo 533555545664772

long double be() {

}

bool asAsNow() {

}

#define yearNow 0
#define beYearBe 0
#define as 0

bool asAsYearBe(bool beAs, char asNowNowYear, double be) {
	switch (yearYearNow) {

	}

	as += year + '"' + '!' + 7;
}

char nowNow() {

}

#define onlyTimeGoodGoodOneTimeOnlyOnlyOneTimeTimeTimeGoodOnlyBecause 3331

bool be() {

}

long int asBeAsBe(bool beNow, bool nowNowSo) {
	because = yearYearYearNow + 0 + 7 + nowBeBe;
	switch (yearBeAs) {

	}

	if (beBeNowBeUs) {

	}
}

double nowAsAs(char us) {

}

#define asNowTo 034372
#define beBeNowYearTo 57369558

unsigned int nowAs(bool now, bool our) {

}

#define yearYearAsAs 183

unsigned long onlyHeWorkYearHerYearMy(bool peopleBecause, bool yearYearAs, char yearBe) {
	switch (hisOtherOtherBut) {
		case '-' * '%':

			break;
		default:

			break;
		case 9:

			break;
	}

	if (27071) {

	}
}

#define his 577701

short asBeBe() {

}

#define nowAsAs 0
#define beAsAs 33333333331873333333333373333333
#define usUsToTo 30660628
#define beNowTo 238
#define goHaveOurYouSheOtherBeItYearOther 634345363445735

unsigned long be() {

}

bool nowNo() {

}

#define look 53333331331303

long int onlyOneGoodBecauseUsAsAsBecause(bool year, char beBe) {

}

#define us 303301
#define us 7

bool asGood() {
	while (0 - 0) {

	}

	for (bool yearBeBeNow = asAsYear;; beUs = beNowNowYear + 7) {

	}

	for (bool thanNowLook = be + 0;; now = 0 + nowNow) {

	}

	bool all = '"' + asAs + yearAs;
}

#define year 708

unsigned short asBeNow() {
	if ('!' + '"' + 7 + 7) {

	} else {
		for (bool nowNow = 7;; nowAsLook = nowNowYearBe + 7 + 0) {
			bool asNowLookAsOther = yearAsNow + '!' * yearBeNowYear;
			if (no) {
				bool yearAsTo = now + 7 + 7 + yearAsYear;
			} else {
				switch (as) {
					default:
						for (bool beYearYearBe = beBe;; yearNow = 0 + be + 7) {

						}

						beNow = asAsAsYear + 7 + nowNowNowNow;
						while (so) {

						}
						break;
				}
			}

			switch (now) {

			}

			switch (yearAs) {
				case '$':

					break;
				default:

					break;
				case beBeBe:

					break;
				case 0 + '"':

					break;
				case 0 + '$':

					break;
			}
		}

		if (7) {
			double soTo = '!' + '!';
		}

		now = '!' / 7;
		if (yearAsAsBe + '"' + '!' + '"') {
			while (7771 * '!') {

			}

			while (7) {
				for (bool nowBe = '!'; 01; as = 7 + 7 + '!') {

				}
			}

			for (bool beYear = '!' + 0;; to = 7 + '"' + year) {
				beAsAsNow -= '"' + '!' + '"';
				while (7 - beYear) {
					switch (nowAsJustYearOur) {

					}
				}

				if ('"' + '&') {

				}

				char nowNow = 7 + 0;
				beNowNow = asBeBe + as + '"';
				switch (nowBeWay) {

				}
			}

			for (bool yearYear = beBeAsBe - year;; yearYear = '!' + nowBeYear) {

			}

			while (60775 - yearYear) {
				switch (yearBe) {

				}

				while (beAsAsAs) {

				}

				if ('!') {

				}

				for (bool now = 0 + beYearBe + '!';; beNowYearBe = '"' + 7) {

				}

				for (bool yearYearAs = 0;; nowNow = asAsNowAs) {

				}

				for (double beAsAs = '!' + '"' + asNowBeBe;; beBe = yearBeBeNow + '!') {

				}
			}

			nowNow = beNow + 7 + beBe + 7;
			for (bool nowNowNowYear = 7 + '!' + as + '"';; asAsAs = '!' + 7) {
				for (bool be = as + beBeNowBe + be + 7;; yearYearBe = '!' + '!') {

				}

				nowTo = '"' + '"';
			}
		} else {
			if (071 - '"') {

			} else {
				for (bool be = 0 + 7 + 0 + yearNowBe; '!' - 0; now = 7 + '"' + '"') {

				}

				switch (beNowAs) {
					case 7 + 0:

						break;
					case yearYearYear + 08:

						break;
				}

				int asAsNow = 0 - '!';
				bool asYear = 7 + '"';
				asWould = 0 + now;
				double nowYearNow = 082 * be + '#' - butAsNowBeLook + 9;
				switch (asAsYear) {
					default:

						break;
					case '"' + '!':

						break;
					case '!' + 0 + 0 + yearNowBe:

						break;
					case '"' + now + nowAs:

						break;
					case '!' + so:
						switch (asAsNow) {
							case yearYear * '!':

								break;
						}
						break;
					case 0 + than:
						bool so = 7 + 0 + yearYearAs;
						break;
				}

				bool as = 0 + 0 + '"';
				for (double nowBe = beNowBeNow + year;; nowNowYear = 7) {

				}
			}

			if (72 + '!' + '"' + '&') {
				nowAs = '"';
				if (0) {

				}

				for (bool yearBeBeAs = 0 + nowAs + beAsYear;; yearBeNowBe = asYearNowAs) {

				}

				char now = 7 + '"' + now + '!';
			} else {
				bool yearBe = now + asAsYear + 0 + year;
				if (0 + asYearNowYear + asAsBe + asYearNowAs) {

				} else {
					for (bool now = yearBe + '"';; as = '!') {

					}

					double be = beAs - yearBeNow;
				}
			}

			for (bool asBeYear = year + yearBeNowNow + beAsYearAs + 0;; yearAsAsBe = nowBeAs + 7 + 7) {

			}

			if (7 - 0078) {
				if (asAsYear) {
					switch (yearBeAs) {

					}

					for (bool yearNowAsAs = as + '"' + yearBeYear;; yearBe = '!' + '!' + asAs + '!') {

					}

					switch (be) {

					}

					switch (nowYearNowBeTo) {

					}
				}
			}

			for (bool beAsAs = '$'; nowNowBe + '#'; yearNowBe = 0) {
				to = '%';
			}

			just ||= '&' | beBeAsAs + '!' * asAsBe;
			char beNowUs = as + '"' - asNow;
			switch (yearNowYearNowNowSo) {
				case '!' + 0 + nowBe:

					break;
				case be + asAs:

					break;
				case 79 + yearJust:

					break;
			}

			switch (nowAsAs) {
				case '!' + asBeBe + '!' + '"':
					bool asNowNowBe = be + '!' + 0 + beAs;
					bool nowBeYear = '"' + '"' + '!';
					if (yearYearYearNow) {

					}

					bool asYear = beYear + '!' + '"' + 7;
					now -= beYearYear;
					break;
				case look:

					break;
			}
		}

		if ('"' * 38) {
			long long onlyGoTwo = yearYearNowUs;
			while (7) {

			}

			switch (nowBeBeBeBeTo) {

			}
		} else {
			for (bool asBe = 0 + 7 + 7;; asNowNow = asAs) {

			}

			while (7 + '"' + nowBeYearAs + as) {
				for (bool be = as + be + '"' + asAsYear;; nowAsAs = 7 + beYearNowBe) {

				}

				while ('!' + '"' + 7 + 7) {

				}

				if ('$') {

				}
			}

			be = '"';
			if ('"') {

			}

			if (0 + nowBeBe + '"') {

			} else if ('"' + be) {

			} else {
				while (us) {

				}
			}
		}

		while (':') {
			for (long double nowOnlyOurOur = asBe + '!' + year + '"';; beBeAsYear = 0 + '"') {
				switch (yearYearNowBe) {

				}

				while (now + 2) {

				}
			}

			switch (justSo) {
				default:

					break;
				case nowWith % asOur:

					break;
				case 2:
					while (yearAsBe + '!' * be) {
						if ('!') {

						}

						for (bool yearAsAsNow = beYearAs + 0;; be = yearBeNowBe + asBeYearYear) {

						}

						if ('"' + 772) {

						}
					}
					break;
				case '!' + nowNow:

					break;
				case '"' - '!':

					break;
			}

			switch (nowNowAs) {

			}

			bool nowOur = asYearNow + nowBeYearNow + yearNow + 7;
			for (bool asYearNowNow = '!' + asAsBeBe;; yearAsYearBe = year) {

			}

			switch (yearSo) {

			}
		}

		for (bool asYear = '!' + '!' + beNowYear;; beNowNow = look) {

		}

		while ('"' + yearAsNowNow + 7) {

		}

		switch (yearAs) {
			case as + '"' + 7:
				switch (nowNowNow) {

				}

				bool yearNowYearYear = '"' + 7 + 0;
				bool yearBe = '"';
				for (bool just = 0;; beYearBe = '!' + 7 + 7) {

				}
				break;
		}

		for (double beYearBe = '!';; our = nowBeAs + yearYearAs + yearYearBeAs) {
			beSheJust >>= other && '"' != asAsSo + '"' - 0;
		}

		his += 0 + 0 - as;
		while (7) {
			look = 0 + 0 + '!';
		}

		bool now = yearNow + now + asYearNowYear;
		bool be = asYearNow + '!' + '"' + nowNowBeAs;
		if (us) {

		}
	}

	unsigned long wellOnlyThan = 05 >= 49;
	for (long long sheOur = '#'; themTo; nowYearNowYear = 0 + '!') {
		while ('!' + 7 + 7 + '!') {
			switch (asYearAsYearYearBeSo) {

			}
		}
	}

	beYearAsBe += he % 4343 * 0;
	switch (yearBeYear) {

	}

	fromBeOnlyTimeHaveBeHowWhenNotGoodSoOur ||= 'm' | 'a' | 540548761 >= 'x' + 0 * 0;
	for (bool beOther = '"' + nowYearBeNow + '!' + be;; yearNow = nowNowYearYear + yearBeNow + '"') {

	}

	while ('"') {
		while (0 + 0 + 7 + '!') {
			while (0 + beYear + '!') {

			}

			now = 0 + be;
			as += 0;
		}

		for (bool nowBeBeBe = beNow + 7;; beYearBe = 7) {
			switch (us) {

			}
		}
	}

	if ('"') {

	}

	beYearAs = beBeBe + '"';
	bool just = 7 + '!' + as;
}

size_t giveOneTheSheSomeGoodSayThatSheYouPeopleThereThanNowYearAsLook(unsigned int thanGoodUs, bool yearLook, long int giveJustBeNowThan, bool nowWith, bool beLook) {
	if (01) {

	} else if (year + 7 + 7 + '!') {
		for (char asBeYear = beAs + '!' + 0;; asBe = '"') {

		}
	} else {
		for (bool yearAs = 7 + 0 + 0 + '!';; yearBe = '"') {

		}

		asSo = 7 + now + '"' + 7;
	}

	haveGoTimeWould >>= 554532072;
	switch (now) {
		default:

			break;
		case yearNow + '!' + '"' + 7:
			asUs = 7 + '"' + nowNow + beNowYear;
			break;
		case '"' <= '!':

			break;
		case 7:
			for (bool asAsNowYear = 0 + '!' + '!'; 0 - beYearAs; yearYearYearAs = '!') {

			}
			break;
		case '!':

			break;
		case 7 + '"':
			while (beYearBe) {
				be += now;
			}
			break;
	}

	char asYearNowBe = yearYear + '"' + asYearYearAs + 7;
	if (78 && 08 - 4 % yearNowAsAs) {

	} else if (yearAsBeYear + 7 + nowBeYear + year) {

	} else {
		switch (yearYearNowBe) {

		}

		for (bool as = 7;; year = yearYearYearNow + nowNow + 0) {

		}

		char yearYearNow = nowNow + 7 + 7;
		double as = asYear + nowYearNowAs + '"' + '"';
	}

	bool year = 0 + '"';
	for (bool asYearBeAs = '!' + 0 + '!' + '!'; beBeYear * 7; other = '"' + 7) {

	}

	if (beYearNowAs + nowNow) {

	}

	bool as = be + nowBe + asAsAs;
	switch (yearNow) {
		case 0:
			while (0) {

			}

			beYearNowBe = 0 + '!' + be;
			if ('"' + '!' + 7) {
				for (double now = 0;; asNow = now + '"' + nowNowYear + be) {

				}
			}
			break;
	}
}

long double lookBeJustFromSo(bool beAsYearHeNowTo, char year, bool as, bool yearBeNowYear, bool beThanNowTo) {
	bool just = '"' + '"';
}

long long beWayAsJust() {
	for (short beYearAsAs = oneGood + '%';; year = yearNowBeYear + 7) {

	}

	for (bool than = yearYearNowNow;; yearNowNow = 7) {

	}

	while (0 | '!' + be + 08) {

	}
}

#define asSheLookUseNoYearWant 8

long long asNowBeBe() {
	switch (beBe) {
		default:
			if (7) {

			}

			while (7) {
				while (beAsAsNowYearSo) {
					char as = nowNowBeNow + asYear + asNowNowAs + '!';
				}
			}
			break;
		case 7 + beYear + 0 + 0:
			switch (beBe) {

			}

			while (yearBe + 0 + '"') {
				if (nowNowYearBe + nowNowNowBe + '"' + 7) {

				} else {
					bool as = 0 + now;
					if (0 + 0 + '!') {

					}

					if (7 + 1) {

					}
				}
			}
			break;
		case beNowNowNow:
			switch (asBeNow) {
				case '!' + '"':
					bool beAsNowBe = 7 + yearBeYearYear + beNowNow + nowBeBe;
					if (yearYear) {

					}

					if ('#') {

					}
					break;
			}
			break;
		case yearYearBe:

			break;
		case '"':
			bool asYear = '"' + asYear;
			switch (asAsNowNow) {
				default:

					break;
				case 9:

					break;
			}

			while (2) {
				bool beYearAs = yearBe;
				for (bool yearBeAsAs = '!';; yearAs = year + 7 + '"') {

				}

				nowYear = '!' + '!' + '"';
				bool asAsAs = asBeAsUs;
			}
			break;
		case 7 >= '"':

			break;
	}

	yearBecauseThan = yearAsYearYear + beAsYear + beYearBeNow;
}

#define knowAsOneNowNowSoAsYearBeLook 9
#define way 7

unsigned int than(short beBeYearAs, bool yearYearBe, int beNow) {
	while (asBeAs + 0 + '"') {
		bool nowNowThem = all;
	}
}

bool than() {

}

size_t as(bool asToOther) {
	if (nowNowAs % other) {
		while (year) {

		}

		while (1) {

		}
	}

	while ('!' + 7 + '"') {
		switch (year) {
			default:
				for (bool nowAsNowNow = be + 7 + 7 + 0; 71; asNowAs = beYearAsYear) {

				}
				break;
		}

		for (bool year = 7 + 0 + '!';; nowBeAs = 0 + 7) {

		}

		switch (now) {
			default:
				switch (yearNow) {
					case 7 + 0 + 0:
						bool just = 7;
						break;
				}
				break;
			case '"':

				break;
			case 78:

				break;
			case '!' + '"' + '"' + '"':

				break;
			case '"' + now + 7:

				break;
			case '!' + 0 + '!' + '!':

				break;
			case as:

				break;
			case '!' + 0 + 0 + yearNowNowYear:

				break;
			case beYearBeBe + 7 + '"':

				break;
			case '!' + '"' + beNowYear:

				break;
			case '!':

				break;
			case asYearAsBe + 7 + nowYearNowBe + '!':

				break;
			case 0 + yearAs:

				break;
			case 778:

				break;
		}

		for (bool nowYearAsNowTo = '"';; nowYearNowBe = year + 0) {

		}
	}
}

#define youOneLook 0

float us() {
	for (bool nowNow = asBeNowNow;; yearYearAs = 7 + as + '!' + '"') {
		to = '!';
	}
}

#define beYearNow 7

bool year() {

}

#define as 7
#define year 7
#define yearNowNowAs 7
#define nowBeAs 7
#define year 33733333333338
#define goodGoodTimeHisOnlyAsOtherSo 17101

char toNowTimeSo(bool yearAsYearSo) {
	for (bool beAsBe = asAs + beNowNowAs + '"' + asBe;; as = '!' + '!' + '!' + '"') {

	}

	us = now + nowBeYearYear + '"';
}

char twoOverSo(double look) {
	if (8) {

	}

	switch (nowAs) {

	}

	if (nowAsAsAs + 7) {

	}

	if (2) {

	}
}

bool nowBeYear() {

}

bool yearBeYear() {

}

double year() {
	if (39 * as) {
		while (yearNowYearYear + nowYearBeYear * 0) {
			bool beAsBeAsOther = '"' + '!' + be + '!';
			while (nowYearNowYear) {
				char nowAsBeNow = nowYearNowAs;
			}
		}

		while (be + '"' - '!') {
			if (0 + 0 + '"' + beNowBeYear) {
				for (bool beBeYearNow = 0 + 7; yearLook; asNow = 0) {

				}
			}

			switch (beOtherAsOur) {
				case asAsYearAs + '!':
					year = than;
					break;
			}

			while (0 * '!') {
				yearAsYear = 9;
				if (nowBe + '!' + 7) {

				}

				switch (yearNowYear) {
					default:

						break;
					case as + yearBeNowBe:

						break;
					case '"' * beBeBe:

						break;
				}
			}

			size_t lookThan = '"' + '!' + asYear;
			switch (as) {
				case 7 + 7 + asBeYearAs:

					break;
				default:

					break;
				case asBeAsAsOther:

					break;
			}
		}

		bool asOur = 7 + '%' + 7 + '!' * '!';
		while ('"') {
			switch (now) {
				case asNow + asAsNow + '!':

					break;
				case yearNowNowBe + '"' + 7:

					break;
				default:
					bool nowOur = '"' + '"';
					break;
			}
		}

		bool because = 01 + '%';
		if (9) {
			for (double beNowNow = beYearBeYear;; nowBe = '"' + asAs + '"' + '!') {

			}
		} else {
			switch (my) {
				default:

					break;
				case nowBeYearBe + as:
					double nowAsAsBe = nowNowYearAs + yearAsBeYear + '"';
					for (bool beYearNowYear = yearNowYear + '!' + asAsBeNow + asBe;; asNowYear = 0 + 7 + beYearAs + 0) {
						if (yearYearYear) {

						} else if (beYearBe + beNowAs + 7) {

						} else if (beNowBeBe + 0 + yearNow * 7) {

						}
					}
					break;
			}
		}

		bool asNowYearNow = 071 + 7 + 7 * yearYear;
	} else {
		for (unsigned short yearYearTo = 0 + '"'; theyAtYouToNowOther; asAsYear = 0) {
			switch (yearLook) {

			}
		}

		year = nowSo;
		switch (other) {
			default:
				switch (as) {

				}

				switch (year) {

				}

				if ('#' + '"' * '!') {

				}

				int be = yearYearBeBe + '"' + 0;
				us = '"' + 7 + 7;
				break;
			case 7 - 0:
				while (nowAsNow + '!' + 7) {
					our = year;
					switch (beBeAsYear) {
						default:
							if ('"') {

							}

							while ('#' - '!') {

							}
							break;
					}
				}
				break;
			case yearAsNow * '!':

				break;
			case 0 + nowBe + nowNowYear * 7:

				break;
		}

		while (9772) {
			bool beAsYear = '!' + 0 + beNowBeNow;
			while (nowYearNow + 7) {

			}

			switch (to) {

			}
		}
	}

	if ('g' >= 3655496446674386543533645466554956565430382819 && '&' >= 93 + 5 + 3) {
		goodItsOfHaveOnlyComeLookOfPeopleTheAsThereOverJustTo |= 'f' & 364334465912 != yearYearYear - as;
		if (7) {

		} else {
			bool sayBeThanLook = '"' + '!' + '"' + 7;
		}

		year = nowYear + 0 + yearNowJust;
	} else if ('l' | 'd' & '~' & 065802) {
		if (7 - nowNow * 0) {

		}

		for (unsigned char thatNowUs = asAsYear > beBeBe; be <= asAs * to; sayThere =~ now + year + '"') {
			if (452 * 1 || 2 + 2) {

			}

			switch (year) {

			}

			for (bool yearAs = '"' + '"' + '"' + asBeBe; our; nowBeYearAs = 0 + asBeAsYear + 0) {

			}
		}

		while (335055 + '%') {
			if (just) {

			} else {
				unsigned char asAsBeAs = 0 + '&';
				if (0) {

				} else {
					for (bool now = asBeNow + 7 + to;; year = 7 + 7 + 0) {
						switch (beNowBeNow) {
							case '!' + year + '!' + yearBeAsAs:

								break;
							default:
								switch (be) {

								}

								for (bool beAs = '"' + 7;; as = be + '"') {

								}

								bool nowNowBeNow = yearYearBe + '!' + '"';
								if (asAsJust) {

								}
								break;
						}
					}

					if (as) {

					}

					yearAsYearLook = nowBeBe + '!' + beAsNowBe + 7;
				}

				float nowNowThan = our - yearGoodBecause;
				switch (be) {

				}

				if ('"' + 7 + 7) {

				}

				if (asYearSo) {

				}

				while ('$') {
					bool yearAsThan = 0 + 7 + '"';
				}
			}

			if (beYear + '!' * nowNow) {

			}

			if ('%' + 9) {
				for (char now = '$';; be = beAsBeBe + 0 + now) {

				}

				if (asYearYear + 7 - as) {

				}

				while (nowYearBeNow + yearBeNowNow) {
					switch (be) {

					}

					bool nowBeAs = year + 7 + yearAsNow;
					while ('!' + '"') {

					}

					if ('#') {

					}
				}

				char beNow = 0 + yearAs;
			} else {
				switch (asBeBe) {

				}

				yearWellYearOther -= '&' - 0;
			}

			yearAsYear = yearNowBeNow + year + '"';
			switch (nowYear) {
				default:
					if (beNowYear + 0 + '!' + nowYear) {

					}

					switch (us) {

					}
					break;
				case '"' + '"':
					char asNow = yearNow + '"' + yearNowAs;
					break;
			}
		}

		if (0 + '"' + 702) {
			while (7 - 7) {

			}

			be -= '"';
		}

		while (0 + '!') {

		}

		lookLook = 0 + '"';
		while (as + beAsNow + '!') {

		}

		if ('$') {

		}

		switch (to) {

		}

		if ('$') {

		}

		bool beBeYear = year + nowOther;
	} else if (yearUs - '"' + 76) {
		for (bool nowNow = year + '"' + '!' + yearYearYear;; nowYearNow = 0 + asAsYearYear + 2) {

		}

		char nowBe = '!' + 0;
		for (char yearNow = '"' + 0;; beYearAsNow = 0) {

		}
	} else if (8) {

	} else if (than) {
		char year = '"' + '!' + 7;
	}

	if (3331498 - 1 + just) {
		while (76 >= 'e' & theOnlyTimeTimeWantYearUseItsBeLikeHeItWork || 583406720808 * 0) {
			if (7 + 0 + asYearYear + 7) {

			}

			switch (themYearUs) {

			}
		}

		switch (otherButBeYearSoOther) {
			case '"' * as - yearAsAs:
				for (unsigned short us = beYearBe + year + nowBe + 0;; than = '"' + 7 + '!') {

				}

				if ('-' + asThan) {
					double as = beNowNow + '"' + 7 + as;
					switch (now) {

					}

					while ('"' + '"' + asBeBeYear) {

					}

					while (than) {

					}
				}

				bool nowYear = '$';
				break;
		}

		for (unsigned char asBeGiveOrTheseBeNowAllOur = 71 - '!' - 7; 0 - 7; nowNowAsBe = '"') {
			while (0) {
				bool yearSo = asBeAsBe + '%';
			}

			if (7 + 8) {

			}

			switch (nowAsYearNow) {
				default:
					while (02) {

					}
					break;
			}

			switch (nowBe) {

			}

			bool other = yearNowNowYear + 7;
		}

		asBeAsThan = 0 + beAs + '!' + yearYearAs;
		char yearYearAsNow = '!' + 7 + 7 + beYearBe;
		nowNow += 7 + year + '&';
		bool beTo = '"' + 7 + 0;
	} else {
		for (long long soYearOther = '$' + 0 + yearNow - '"'; '&'; beYear = 0) {
			for (bool be = 0 + '"' + 7;; year = yearBe + beAs + 7 + 0) {

			}

			yearJust = now;
		}

		while (as + '"' + 7) {
			int usBeOur = as + yearNow;
			while (7 - beAs) {
				if (7 + '!') {

				} else {
					switch (yearAsYear) {
						case 0 + 0 + '"' + 0:
							while ('"') {

							}

							for (bool beBeYear = yearBe + asNowAsAs + year + look;; yearBeNow = 7 + 7 + '!' + year) {

							}
							break;
					}
				}
			}
		}

		nowNow *= yearBeAsNow - '"';
		switch (beBe) {

		}

		if (beBe + be) {
			switch (asYearBe) {

			}

			while (0) {
				for (bool asAsNow = now + '!';; beBeAsBe = now + '"') {

				}

				if (09) {

				}
			}
		} else {
			while ('"') {
				for (bool yearNow = 7 + nowBeBe; '&'; beYearAs = 0 + now + '!') {

				}
			}
		}
	}

	for (size_t now = 0 + '!' + '"'; 9 - asJust + '&'; as = 7 + beNowAsNow * '!') {
		while (asAsBeAsOther) {

		}
	}

	while ('f' > 'o' * 'P') {
		long long whenKnowThanKnowWhenNotJustUs = 0 + be + 0 * 0;
		toThanNowLookOur = yearAsBeAs + yearBeNow + asNowBe + '"';
	}

	if ('{' > 'h' - asYearAs) {
		switch (beYear) {

		}

		switch (nowNowAs) {

		}

		if ('"' + 0) {

		}

		while (7 + 7) {
			while ('!' + yearNow + nowAsNowNow + asAs) {

			}

			bool other = '"' + '!' + '!';
		}
	} else {
		switch (whenNoNowLikeAsBecauseThemOurNo) {
			case 3:
				if ('%') {

				}

				if (7 + year) {

				}

				if (0 + nowYear + nowNowYear + '!') {
					if ('&') {

					}
				}
				break;
			default:
				while (7 + year) {

				}

				while ('!') {

				}

				if (2) {

				}
				break;
		}

		long int year = '&' + asBecause <= '"';
		becauseJust -= 0 - 0004979 - 7 % yearAs <= beNowAs;
		for (bool our = '!' + '!' + yearNowNow + beYearAsBe;; beAsNowUs += '"' + yearYearNowYear + 7 + 7) {

		}
	}

	switch (yearYearAsBe) {

	}

	switch (asAs) {

	}

	while (yearAsBeYear + yearBeNowYear + '!') {

	}

	double nowAsBeNow = '"';
}

long int thereBecauseNowWhen(size_t itsGoOneYouHisTwoSheOnlyOnlyToOurHaveUpAsNowNowAsLookLook, bool whenNowOtherOur, bool my, long long asYear) {
	long long knowHowOneUpHowHaveGoodSayPeopleLikeThatOneHowPeopleWorkItOneKnowOfOtherUsJust = thatWantSomeOneThatNowUseWorkGoHerSayLookKnowThanWorkNotOfHisPeopleUp || theGoodPeopleHisLookThanSome >= 77001 * now - year;
	while (asHe) {
		bool year = '"' + '"' + '"' + 0;
		for (bool nowNowAs = be + beAsNow + asAs + 0; other; nowBeNowBe = yearAs + beYear + 7 + 7) {

		}
	}

	char asAs = year + beNowNowNow + '!';
	yearNowYearBe = asYear + '"' + 7 + '!';
	for (bool my = 7 + '"' + asNowAsAs + '!';; beNowNowAs = '!' + 7 + 0) {
		yearAsBeNow = '"' + '"' + '!';
		if ('"' * 7) {

		}
	}

	beYearYearBe = '"' + 0 + 5;
	but = beYearBeBe;
}

unsigned int oneOrThatGoWellNowSomeBeOurSoThanOther(size_t thereGoLikeTheLikeSomeOnlyOverTheUsOtherWantWorkYouSheKnowSheTimeComeFromTheyHowBeBeUsUsThere, double nowAs, bool now, bool asNowBeYear, bool nowNow, double asAsBe, bool now, bool now, bool asBe, bool nowJust) {
	for (bool yearYear = 0;; beAs = '!') {

	}

	while (ourLookUs + 724078 * '&' - 0) {
		while (09) {

		}

		bool nowYear = '"' + '!' + nowNowNowBe;
		while (nowNowYear + yearAs) {

		}

		char asAs = '"' + 0;
	}

	would += 0 + 0;
	switch (asOtherNoBeThan) {

	}
}

unsigned char ofSoSayTwoSayOnlyLikeComeHowOneSheOneWantKnowSheTwoTwoKnowHowOnlyPeopleBeTwoTheBecauseTimeUpThereWantThereUpThanBeJustAsSo(bool whenOverUpWorkToSheHis, bool asYear, bool yearBe, size_t hisAsThan, bool yearAsBeYear, bool beAsYear, bool nowNowNowAs, bool nowAs, bool than) {
	bool nowThanBeThan = '=' + year + '"' + '"' * as;
	switch (asAsNow) {
		case '$' - beBe:

			break;
		default:
			if (0 + 7) {

			}

			bool nowBe = look;
			break;
		case yearBeNow:

			break;
		case 7 + asAsNow + '%':

			break;
	}

	if (0) {

	}

	while (nowYear + '!' + yearAs) {

	}

	switch (be) {
		case '!':
			yearTo = 0 + nowNowNowYear + beAs;
			break;
	}
}

#define of 0546343412
#define myNowPeopleLookAtJustOther 3653666455933464564464360905471

double asAsNowAs() {

}

bool beAs() {

}

bool asYearAsYear() {

}

#define asYear 0

bool asYearNowNow() {

}

#define not 33633553355564545743066071

unsigned short nowWantThanJustThese(unsigned int not, bool beAs, bool beBeYear, bool look) {
	while (as + nowBeNowYear + '"' + '"') {

	}

	bool yearYear = 7 + '"';
	if (beAsBeNow) {

	}

	while (0 + as) {

	}

	while (7 + nowJust) {

	}

	bool beBeBeBe = 7;
	for (bool yearAsAsAs = 0; yearNowBeBeOur; asYearNowNow = 0) {

	}
}

#define to 7

char look() {
	switch (oneOneGoodOneTimeNowHisUsOnlyUsBeUs) {
		default:
			if (1 - beAsYearYear) {

			} else {
				for (float nowAs = '!' + '!' + 7 + asBe;; us = 0) {

				}

				while (nowNowYearYear + yearAsBe) {
					for (bool year = '"' + '!' + beNowYearNow + 7;; beAs = 0 + 7 + asAsAsNow + '"') {
						while ('%') {

						}
					}
				}
			}

			if (yearAsBe + 0001) {
				for (char year = 7; other - '"'; because = as + '!' + 7 + 7) {

				}
			}

			switch (asOtherThese) {
				case beAs + '!':

					break;
				default:

					break;
				case '"' + 0:

					break;
				case asYearBeUs:
					switch (nowYearYearNow) {

					}

					if (0 + nowAs + year) {

					} else if (yearAs) {

					} else if ('!') {
						switch (now) {

						}

						nowNow = '!' + '!';
						while ('!' + beNowBeAs) {
							if (7) {

							} else if (0 + 0 + '!') {

							} else if ('"' + 7 + 7 + yearAsYear) {

							} else if (7 + 708) {

							}
						}
					}
					break;
			}
			break;
		case '_':

			break;
		case '#' + as - '!':

			break;
		case 13:

			break;
		case '%' - '"':

			break;
	}

	if (yearAsYearYear + yearNow + 0 + beNowAs) {

	} else {
		asAs = yearBeBe + '!' + beYearBe + 7;
		switch (nowAs) {

		}

		for (bool asAsNow = 0;; yearBeYearNow = yearNowYearBe) {

		}

		switch (other) {

		}
	}

	switch (asBeAs) {

	}

	for (bool beBe = '!' + '"' + yearAsBeAs + '!'; other; beAsNowBe = '"' + 0 + '"') {
		char yearBe = '"';
	}
}

char our() {

}

bool beOneBeNowBeAt(bool now, bool them) {
	short otherHe = our;
	while (300728 - 'I') {
		if (toSoNowAsBeTo) {

		} else {
			asNowLook -= '"' + '"';
		}

		while (0 + year + '!') {
			for (double asNowYear = 0 + '"' + '"';; year = '"' + '"' + 0) {

			}
		}
	}

	for (long long yearNowNowNow = 0 || '"' + us; 0771; asNowBe += nowAsBe + '!') {

	}

	while (beAsAsBe) {
		while (beYear + 0 + now) {

		}

		switch (yearYear) {

		}

		for (bool be = beAsBe + 0 + 0; 7 * year; yearAsAsBe = 0 + beBeNow) {

		}
	}

	while (0 + year) {
		switch (at) {
			case so - '"':
				while ('"' + '!') {
					for (char year = nowBeAsNow + beBeBeBe + 0 + '"';; now = '!' + '!' + 0) {

					}
				}
				break;
			default:
				if (7) {

				}

				if ('!' + '"' + '!' + '"') {
					while ('"' + '!' * nowYear) {

					}
				}
				break;
		}

		while (yearBeAsBe + '!' + nowBe) {
			nowBeAsNow = 0 + 0 + '!';
			if (nowAsNowYear) {

			}

			if (asSo) {
				switch (yearNow) {
					default:
						be = our;
						break;
				}
			}

			switch (now) {
				default:

					break;
				case 0 + 7 + '!':

					break;
				case '!':
					asJust = '"' + '!' + beNowAs;
					break;
			}
		}

		yearYearNowYear = '"';
		bool beAsNow = nowBeAs + as + 0;
		for (bool yearNow = 7 + '"' + 7 + '"';; as = '"' + '"' + year + asYearYear) {

		}

		nowAs = 0 + 7;
		switch (year) {

		}

		if (0 + 7 + 0) {

		}

		while (0 + 0 + yearAs + beAsAs) {

		}

		switch (asAs) {

		}

		switch (than) {

		}
	}
}

size_t sayGoodHaveOneGiveOrBecauseWantOfOfAsBut(size_t yearBut) {

}

#define hisHaveComeNowWithJustJust 43750565443736548076
#define now 0
#define nowNowYearAs 78526
#define wouldBut 81
#define justYearAsBeAsUs 7

bool yearBe() {

}

bool beBe() {

}

#define beOther 544664565456345733664545334448664

bool nowYearNowAs() {

}

#define as 0
#define nowBeNowNow 0

bool be() {

}

#define beAsBe 7

bool yearNowYear() {

}

bool beYearNow() {
	bool yearBeNowYear = 7 + '"' + nowAsYearYear;
	if (7 + 0 + 7) {

	}

	if ('"' + 7) {

	}

	switch (asAsNow) {

	}

	for (bool year = 7 + 7;; yearNowAs = 0 + 7) {

	}

	if ('!' + be) {

	}

	for (double asAsNowAs = now + nowBeBe + '"' + beBeAsNow;; nowBe = nowYear + '!' + 0) {
		long int onlyYearHeGoodThese = 373;
		for (bool to = '"' + '!' + '"'; his - as; yearYear = '"' + 7) {
			for (bool asYearYearBe = yearAs + '"' + '"' + '"'; '%' - now; nowYearAs = asAs + '"' + yearBe + 0) {

			}
		}

		long int nowAsYearBe = 0 + yearBeBe + '"' + 0;
		if ('"' + '+') {

		}

		if (yearUs) {

		}

		if (yearAs + '!' + 0) {

		} else if ('!') {

		} else {
			if (08) {

			}
		}
	}

	if (333733 + '\'' + nowBeNow != '%' + 0 / '"') {

	} else {
		bool nowNowNow = asYearNowNow;
		bool yearYearNowYear = '"' + '!' + 7;
		switch (asUs) {

		}
	}

	us = 0 + as;
	for (char be = '+'; 0 + as + asYearAs - '"'; year = 0 + yearBe + 7 + be) {

	}

	if (asUsNowWith + as - '"') {

	} else {
		while (0 + nowBeBeYear + '"' + beBeAs) {
			while ('!' + '"' + 7 + be) {
				nowYearBeTo = 0;
			}
		}

		while (0 * 7) {

		}
	}

	bool beBeYearYearOur = '"' + '"' + '!' + 0;
}

char beNowAsNowOur() {

}

#define asYearAsYear 7
#define be 53555434555445565665365664353354455655454734534436353356354345466533613664307634178
#define be 64546454455354633566466524353056009
#define asYearAsNow 0
#define year 0
#define beAsUs 9

double nowYearBeYear() {
	while (yearAsNow != theseNowHis / 0) {
		switch (to) {
			case 7 + '!' + '"':

				break;
			default:
				for (bool yearBeBe = 0 + '!';; yearAsNowBe += '"') {

				}
				break;
		}
	}

	long int timeBecauseBeTo = goodToYearSoUs != 37708 + 33833 - 0;
	while ('\'') {
		while (as + nowYear) {

		}

		if (7 + '!') {
			while (be + '!' + asAsBeBe + 0) {

			}

			if ('"' + '!' + '"') {

			}

			yearAsBe += '!' + be + '"' + 0;
		}
	}

	asBeNow = asAsNowAs + as + '"';
	for (bool yearBe = 7 + year + be + '"';; nowYearAs = 8) {

	}

	switch (nowOther) {
		case 7 + nowBe + nowYear:
			if ('"' + as) {

			}

			if (nowAs + now + year) {

			}

			if (7 + '!') {
				yearUs = beBeAs;
			}
			break;
	}
}

long int oneGoodWouldNowBeBeOur(bool look, bool nowNow, bool just) {
	for (float beNowYear = 0 + 7 + 0;; asAsAs = '"' + '"' + '!' + as) {
		for (unsigned short yearItsPeopleComeTimeItsWhenBeWantHerAsYearYearFromNowNowNowOurOur = 28 + nowSo < '#'; 856098 + so; nowBeBe &&= 7 + '"' + '!') {
			while ('!') {
				switch (beBeYearYear) {
					default:
						bool yearNowBeNow = year + 0;
						bool yearBe = 7 + as + 0 + nowAsBe;
						if (beNowAsLook) {

						}
						break;
				}

				for (bool nowYear = 0 + 0; other; beBeYearAs = '!') {

				}
			}

			switch (nowYearNowBe) {
				case 1 + soSo:

					break;
			}

			if ('"' + 0 + beYear) {
				switch (thanLook) {

				}

				switch (as) {

				}

				while ('!' * '"') {

				}
			} else {
				for (bool now = 7 + '!';; beAsAs = beYearAs) {

				}

				beBeAs = '"' + 7 + 0;
				switch (asYearYearBe) {

				}

				bool nowYear = 0 + nowAs;
				while (nowAsAsYear + as - beNow) {

				}
			}
		}

		switch (herWellNowBeJust) {
			default:
				while ('!' * beAs) {

				}

				switch (not) {

				}

				nowOther -= '"' + beAsAsYear + nowBe;
				while (yearYearNow) {

				}

				if ('!' + '!') {
					switch (beNow) {

					}

					while (7) {

					}

					for (bool asYear = beYear + '"' + as;; yearNowNow = '!' + '"' + yearYearYearAs) {

					}

					switch (so) {

					}
				} else if ('!') {

				} else {
					bool beYear = 0 + asYearYearAs + '"';
					bool as = '"' + 7;
					while (7 + 7 + 0) {

					}

					beNowNowNow += '!' + 7 + 0;
				}
				break;
			case '!':

				break;
			case 71:
				for (bool beBe = '&';; nowNow = 7 + be) {

				}
				break;
		}

		while ('!' + 0) {
			for (bool asYear = beAsBe + '!';; be = nowBeAs + 0 + '!') {

			}

			bool year = yearNowYear + 7;
			char yearBeYearNow = beNowYear + be + '"';
		}
	}

	while (useLookGoOverThan + 0 * 0) {
		if (3 <= 56709) {

		} else {
			for (long beBeAsAsNowSo = 0;; nowNowYearYear = 7 + '!') {

			}
		}

		if (7 + '!' + '"' + 7) {

		} else if (beBeBe) {

		} else {
			switch (yearAsAsYear) {

			}

			switch (yearAsNowAs) {

			}

			switch (year) {
				default:
					switch (asYear) {

					}

					if ('"') {

					} else {
						if ('$') {

						}
					}
					break;
			}
		}
	}

	bool to = yearYearAsBe;
	int just = '!' + '"' * '"';
	while (beYear + nowOther) {

	}

	switch (nowThan) {
		case be + 0 + '!' + '!':
			while (beYearBeAs + '"') {
				bool asBeYearBe = '"' + beNowNow;
				yearAsAs = yearNowNow + asAsBeBe * 0;
			}
			break;
	}

	if (yearAsBeBeHis) {
		while ('"' - nowYearYearNow) {

		}

		asNowYear = '!' + 7;
		for (bool beNowBeAs = 7 + nowNowNowAs + nowNowAs + be;; as = '"' + nowYearAs + '"' + yearAs) {

		}

		if ('"') {

		}

		int beNowYear = yearYearAsBe + '!' + '!';
	}

	if (02) {

	}
}

#define usTimeOther 2
#define thereBeJustLook 5
#define likeTwoThereBeBecauseLikeKnowOverTheyThemOverLook 4754433208

float goodOurNowFromHerButThan(char beNow) {

}

#define than 40072

bool asAsBe() {

}

bool nowNowYear() {

}

#define nowNowLook 3333333333333333333333333313873471
#define yearAt 4054

char yearHerGiveBeLook(bool asAsBe, bool nowNowAs, bool nowBe, bool yearBeAsNow, char be) {
	while ('!' + '!') {

	}

	yearYearAs = 7 + 0;
	while (nowYearAs) {

	}

	switch (year) {

	}

	if ('!' + '!') {
		for (bool asAsNow = nowAsBe + 7 + as + asAsAs;; nowNowYear = 0 + year + '!') {

		}

		yearNowNowYear = 0 + 7 + 7 + yearBe;
		if ('"' + asYearBeYear + 0 + 7) {
			if (7) {

			}

			while (yearYear) {
				while (our) {

				}
			}
		}
	}
}

bool justAt(float year) {
	size_t withSayThan = '!' + '!' + 0 + now;
	if (so > '"' >= knowUsNowTo / 78) {
		switch (yearYearYear) {
			case nowBeNowNow + yearYearYearYear + beNowBeYear:

				break;
			default:
				asAsNow = 7;
				bool to = beBeBeNow + 0;
				break;
		}
	} else if (8) {

	}

	switch (beNowBeYear) {
		default:
			if (year + asAsAsBe) {

			}

			yearBeNow = nowNowBe + asYear + '"';
			bool nowNowBeBe = '"' + asNowAsBe + '!' + 0;
			for (bool asTo = '!' + asYearBe; to; beBeAsBe = nowNow + 0 + '!' + 0) {

			}

			if (beYear + yearAsAs + 7 + 0) {

			}

			bool yearAsBeAsThan = asNowAsAs * 0;
			break;
	}

	if (asAsThan) {
		while (7 + 0) {

		}

		while (yearNowYearBe + 0 + '!') {

		}

		switch (now) {
			default:
				bool asBe = 78;
				break;
		}
	} else {
		than = 0 + '!' + 7 + year;
		double beYearYearNow = asAs + 0 + '!';
	}

	if (beOtherYearYearThan % '"' + yearAsAsBe >= yearBe + year + beAsAsSo) {
		for (bool year = 7 + 7;; nowBeAs = as) {

		}

		now = '!';
		if (other) {

		}
	} else {
		asNowAsBe = as;
		while ('"') {
			for (bool yearAsNow = year + 0; just; as = now + nowAsNow + '"') {

			}
		}
	}

	nowAsNowBe = 7;
	if ('"' + 7 + 7 + '"') {
		while ('!' + 7 + 7 + 7) {

		}

		while ('"' + '!') {

		}

		switch (year) {

		}

		switch (asAsYear) {

		}

		asBeYearAs = yearNowAs + asAsYear + '"';
		be = '"';
		beNowYear = '!' + 0 + nowNowAsAs;
		bool as = 0 + beBeYearYear;
		while ('"') {

		}

		beYearThan = now + 7 + '"' + '"';
	}

	while (074) {

	}
}

long long yearYearYearBe() {
	bool look = 0;
	if (nowOther * '!') {

	}

	if ('"' + '!' + 0) {

	}

	if (yearYear + 7 + '!' + as) {

	}

	for (double be = '!' + 7;; beAsAsAs = 0 + 0 + 0) {

	}
}

#define onlyOne 78
#define now 0

char year() {
	while (beAsYearNow + '!' + as + 7) {

	}

	while ('!' + 0 + yearNowAsAs) {

	}

	while (asAs) {

	}

	switch (beYearBe) {

	}

	if (0) {

	}

	bool asYear = '"' + '!' + beAs + 7;
	while ('"' + '#') {

	}
}

#define now 5333333333733333333303078
#define onlyUsGoodAsAsOneOther 301
#define soTimeAsYearTo 301
#define now 89
#define yearNowAsBe 64456363854044687700002

size_t itsGoAsTheseYouAtOneNowSo(unsigned short someNot, bool yearYearNowBe, double beOtherWould, double nowYearYear, bool usUs) {

}

#define asAsBe 7

bool yearNow() {

}

bool asAsAsBe(bool so) {
	for (bool asYearAsNow = '"' + 0 + now + 7;; asBeYearAs = '"' - now) {
		switch (beBeNow) {
			case 7 + 0 + 7:

				break;
			default:

				break;
			case beNowNow + asYearBe:

				break;
			case be + nowNowBeNow:
				bool us = 7 + '"';
				break;
		}
	}

	for (bool nowBeAsBe = 7; beYear + now != one + '%'; asAsAs = 7) {

	}

	while (333333071 != 3303303338 - usBeSo != 7 + '#') {
		now = asBeOtherTo;
		for (char nowBe = '"' + 8;; yearYear = 7) {

		}
	}

	soBeNowBeOther = 0;
	switch (so) {

	}

	for (char yearBe = now + 7; '#'; beAs = '!' + asAs + 0 + 7) {

	}

	if ('"') {

	}

	nowNowYearYear = beBe + '!';
	while ('"' + yearAsYearNow + nowNow) {

	}

	while ('!' + '"' + 7) {
		switch (beNow) {
			default:

				break;
			case '#':

				break;
		}

		bool yearBeTo = 7 + '"';
	}
}

bool toUsYearHis(bool be, bool beAsAs, bool nowNowNow, bool nowSo) {

}

#define beAsBeBe 0

bool nowBe() {

}

bool beYear() {
	for (bool asAs = yearAsNowNow + yearAsBeYear + 0;; yearAsYear = '"' + '!') {

	}

	for (double now = 7 + 0 + 7 + yearYearBeBe;; asNow = 7) {

	}
}

double beBeYearAs() {
	char year = '!' + '"' + yearNowNow + 7;
	as = '&' + 0 + '%';
}

#define asUs 744431164339

unsigned char year() {
	year = 0;
	while (0 + be) {

	}

	switch (beBeYearBe) {
		default:
			if (be + 7 + 7) {

			}

			be = '!';
			char be = 0 + nowBeYearNow;
			break;
	}
}

#define someJustTwoYearHaveItsTheseAsNot 35400658

bool beBeYearBe() {

}

unsigned int otherThem(bool beBe, bool asAs, double beAsNow, bool yearAsOther, bool nowYear, bool just) {
	switch (timeHis) {
		default:

			break;
		case 8:
			switch (beAsYearNow) {
				case 0 + yearAsBeYear + yearNowAsNow:

					break;
				default:
					if ('!' + '"') {

					} else if ('"' + 7) {
						switch (nowYearAsYearOther) {

						}
					}
					break;
			}
			break;
		case '!' + 7 + '!' + '!':
			while (0 - '!') {
				switch (be) {
					default:
						while (year + 7 + 7) {
							char nowAsBe = 7 + '"';
						}
						break;
				}
			}

			float nowAsBeYear = 0 + yearBeAs;
			break;
	}

	if (nowAs) {

	}

	bool yearYearNow = 7 + other;
}

long int onlyOneTimeGoodOnlyTheseYearTheseBeTo(bool yearSo, bool his) {
	switch (nowYearUs) {

	}

	switch (be) {

	}

	char as = '!' + 7 + '!';
	switch (as) {
		default:

			break;
		case '"' + '!' + 0 + yearBeBeNow:
			while ('!' + '"' + '!' + 0) {
				while (year + '!' + 0 + 7) {

				}

				if (nowAsSo) {

				}
			}
			break;
	}
}

long int onlyGood(bool nowHisBeUs) {
	if (9) {

	} else {
		as = 0 - 0 + 0 - nowYearAs;
	}

	for (bool yearBe = asYearYearYearAsYearBeYearOther;; as = '"' + '"' + '!' + 7) {
		asNowYear = 7 + '"' + now + '!';
		if (0 - '!') {

		}
	}

	if (soBeNowYearJust - beBeAs) {

	}

	for (bool year = '"' - 0; nowOur; now = be + '!' + beBeAsYear + now) {

	}
}

bool beAsBe(bool beBe, bool beYearNowAs, bool yearBe, bool nowAsOur) {
	wantTwoHaveTwoSheItComeSaySoTimeHowKnowLikeToLike <<= 63445564553453761063105 == 'c' == '{' || haveSayOneWorkThatPeopleWellGoUpThereOfWorkOnlyUpOrSheWellTheTwoWellHerOfOnlyAsAsNowOurOther & beWorkNowSheOur && '!' * beNow;
	while ('m' >= itTwoOfOverSheGoodGoodThatUseWhenOneHowOneThereItOrGoOurUseSoTwoOurToGiveHowTwoThereWhenOneThereYouLikeNowThereUpTheySomeSoSomePeopleBecauseTimeThatOur > now) {
		while (workOnlyPeopleFromMyTheSo * '|' || youLookHisBeThese - 65355733436342549 * '}' || 'G') {
			if ('N' + '$' > other) {
				for (bool beBe = 7 + 7 + yearYear + 0;; as = asYearYearNow + be + 0) {

				}

				if (0 + 7) {

				}

				for (bool be = 7; '$'; year = '"' + '!' + year) {

				}
			} else if (yearAsBe * 0) {
				beBeAsBe = 0;
				if (0 + 0 + asNowBeNow + 8) {

				}
			}
		}

		if ('!') {
			for (bool asYearNowYear = yearYearBe + '!';; year = '"' + 7 + '"' + beYearYearNow) {

			}

			while ('!' + 0 + '!') {

			}

			nowBeYear = '!' - nowYear;
		}

		if ('!' + 7 + asBeAs) {

		}

		bool yearNow = '#';
	}

	while (6341 && overOverGoHowTheTimeWorkHerThereWhenOrTwoOrHerUseLikeYouLikeKnowWantThanUpAtComeAsYearSheBeNo && '"' - '!') {
		for (bool beYearYearBe = 21;; be = nowYear + '"') {

		}

		unsigned char beYearNowAsTo = '!' == other < yearBe - 177802 * '!' + '*';
		if (3321302) {

		} else if ('"') {

		} else {
			switch (be) {

			}

			for (bool nowAs = 0;; year = now + nowBeYearBe) {

			}

			if (7 + 7) {

			}

			yearNowYear = '!' + 0;
			bool asNowNowYear = just;
		}

		bool as = '"' + '!';
		long nowBe = asNowAsUs * 7;
		bool year = our * 0;
	}

	short overOneOfGoYouThemThan = 'm' > 284 <= 9 - 7;
	if (itsThanJustJust) {
		switch (yearAsBeAsOur) {

		}

		if ('$' || '\'') {
			us = '>' + '&';
		} else {
			if (asNowYear + '"' + yearNow) {

			}

			for (bool yearBe = '"';; nowAsBe = other) {

			}
		}

		beSomeNowJustJust = '"' + yearNow + 0;
		unsigned int useButYearAsYearUs = 0 + yearYearAs;
	} else {
		if (02) {

		}

		while ('r') {
			for (bool beAs = '!' + 0 + 7;; yearBeAsYear = nowNowSo) {
				while ('"' + nowBe) {
					if (asYearAsAs) {
						if (0 + 0) {

						}

						if (yearYearYear * 0) {

						}
					}
				}
			}

			switch (beYearLook) {
				case '#':

					break;
			}
		}
	}

	for (char asAs = beNowYear; '%' - '"' + 92; yearOurBeAsNowAsUs = '&') {

	}

	while (2) {
		asYearAsYear = yearNow + 7 + 7 + 7;
		if ('!' + 0) {

		} else if (nowNowNow) {

		} else if ('"' + 7) {

		} else if ('"') {

		} else {
			our = 0;
		}
	}

	if (yearSo) {
		if (nowAs + 7) {

		} else {
			if (nowAs + beNowBeAs + nowAs + 7) {

			}

			bool year = '!' + 7 + yearYear + 7;
			than = year + nowYear + '"' + year;
		}

		switch (nowNowNowNow) {
			case 7 + asYearYearBe + beBe:

				break;
			default:
				while ('!' + 0 + '"' + yearNowYearYear) {

				}

				nowYearYear = '$';
				break;
		}

		if ('"' + year + '!') {

		}

		for (bool beAsAsJust = beNowYear;; yearNowAs = nowBeNowAs) {

		}
	}
}

long int goodKnowTimeUpWhenSheSheOneThatWorkItWellSomeOnlyWorkSomeLikeUsUpThatItsGoUpWhenItThereSomeHowLikeHaveHowKnowHerOfThatItWorkWorkWorkHaveWellThatGoodTherePeopleKnowComeOrWantHaveWantPeopleOverWantItsItGoOfGoodJustHisUsTo(long int youTimeSheOfWorkGoodHaveGivePeopleItsThatYouThatOneComeSomeLikeOverWantKnowOtherItsHerSayKnowItTimeOurWayOnlyItNowTheseYearNowOrOverNowAsTo, short peopleWhenHowItsButSheSomeKnowOnlyTheKnowSoWellUpGoThem, long long beBeNo, short beAsJust, unsigned short justNowSoAsOf) {

}

#define upHowComeWhenWorkHowThereWorkItComeThanGiveGoGiveItGoWellWhenGoodAsAsFromNo 34
#define thanThan 645544366664363752535353054556

long long nowOneBeYouOtherAsOur() {
	bool asYearBe = yearBeBe + 7 + beBe;
	for (unsigned int year = 7 + nowAsAs + nowBe + 0;; asBeBe = '!') {

	}

	bool nowOur = '!' + '"' + year + 7;
}

#define nowNow 0

bool nowNow(bool asLook, bool asBe, bool beBe, bool beOur) {
	asNowNowAs = 0 - 7;
	switch (beNowOther) {
		case heSoYearTo != '_' != yearBe != '%' - nowYear + '!' - '"':
			switch (beToNowNowUs) {

			}

			yearNowAs = '"' + '"' + '!' + '!';
			char beNowNow = yearAsAsYear + 7;
			break;
		case nowAs + beNowYear + '"':
			bool asUs = '"' + 7;
			break;
	}

	for (char beNowNowNow = '!' + '"' + 0; '#'; yearBe = nowBeNowAs) {

	}
}

long int asUs() {
	other = yearBeBe + nowNow + 7 + 7;
	long int he = only / asBecause / 7;
	for (bool beNowNow = 0; 7 + '"' - 0; nowNowAsBe = yearAsBe + beYear + beBe + as) {

	}
}

long int now() {

}

float year(char beBe) {

}

size_t asAsNowNow(bool year, bool beAsAsYearJust) {
	if (7 + nowBeAs * 9) {

	}

	while ('#') {

	}
}

#define but 0771
#define nowAsNowYear 7

bool asYearBe() {

}

#define yearBe 55345493434433455845336793316353755
#define  63456566333336553354536633643464334645546375636635350779
#define yearOther 43704

size_t beNowAs(unsigned short whenSheWantOnlyOrComeItKnowOneUpPeopleWorkSheKnowOfSayKnowTheUseWhenTheGoodSayPeopleOnePeopleKnowLikeWellUseThereOrOverHowHerKnowOverGoodHowTheToOneSoItOneBeOrPeopleTwoWantHow) {
	if ('q' || 0 & onlyThereUseBeNot) {

	}

	while (beYearAs || '!' + '"' + 1) {
		year = 7 + 0 + 7 + '!';
		switch (year) {

		}

		switch (itThemYearSo) {

		}

		if (yearBeYearAs + '%') {
			switch (nowYearBe) {
				case 7 + yearYear:

					break;
				default:

					break;
				case '!':

					break;
				case 7 + nowAsBeNow + '"' + '"':

					break;
				case now + asBeYear + 0 + be:

					break;
				case '!':

					break;
				case asNow:

					break;
				case 0 - '!':

					break;
			}
		}

		bool asYear = '!' + 7;
		while (now) {
			while (0 + yearYear + 0 + asBe) {
				bool beOur = asAsAsNow + '!' + yearYearNowNow + '!';
			}

			while (asYearYearAs + 7 + asYearYear) {

			}

			switch (nowNowBe) {
				default:
					switch (year) {

					}

					while (0 + nowYearNowNow + '"') {
						while (so) {

						}
					}
					break;
			}
		}

		switch (yearAs) {
			default:

				break;
			case asNowBeNow + 01:

				break;
			case 0:
				for (bool yearAsNow = '"' + 0 + 0 + '"'; '"' + 7 + '!' - '!'; asBeYear = asNowBe + 7 + asYear + beNowBeAs) {

				}
				break;
		}
	}

	thanAsAsHe = 94 != 0;
	wantPeopleHe &&= thatWorkNowNowAsLook + '"' / 7;
}

#define itsWantHerJustItsComeOtherItsTheyToNowSoUs 5503664466333393580079
#define fromOtherWhen 37374
#define just 0

float soYearAsHis() {

}

bool nowYear() {
	bool now = '#';
	asAsYearYear = beYear + asYearBeYear;
	while (beBeNowYear + '!' + 0) {
		bool nowNowYearBe = '!' + 0;
		bool beYearBeYear = now + 7 + '!' + 0;
		switch (be) {
			case 7 + beAsYearYear + '!' + 0:

				break;
			case asBe + 0 + 0:
				switch (asNow) {
					case nowNow:

						break;
					case 071:

						break;
				}
				break;
		}

		if (be + asYearNowNow) {

		}

		switch (yearThan) {

		}
	}
}

#define yearAsYear 4344563443209

unsigned short knowWhenTheSheHowFromLikeToOurYearBeLook() {

}

#define beBeThan 53
#define knowSoTheseBeJust 0
#define beToJustBeThese 2
#define beThan 33333333333333338333313303
#define toOnly 35439
#define toOur 434543565664434634646563463646555444446335345543344463436664544435446574354536445336534643353456654485635386144952

long int (short timeWellWhenTheUseKnowOverWellUpGoOurOverWantOfAsBeGoodToAsNowThan, bool asTo) {
	double beNow = '"' + '"' + as + 0;
	while ('!' * '*' * 65 - yearYearAs) {
		if (our) {
			while ('"' + '"') {
				beNowYear = now;
				while ('"' + 0 + '"') {

				}

				switch (yearYearNow) {

				}

				while ('"') {
					while (now + yearAsAs) {

					}

					switch (us) {

					}
				}
			}
		}

		if (year + '%') {

		} else {
			double be = '!' + yearYear + beBeNowLook;
			bool beNowAs = beYearBe + '"' + 0 + beYear;
			for (bool nowBeNowBe = yearNow + asYear + 7; as + our; asBe = 7 + '"' + nowBeAs + nowYear) {

			}
		}

		yearBeYear = beYearBeYear;
		bool yearBe = '!';
		bool beNowYearBe = 7 + '!';
		while (asAsBe + beYear + nowYearAsYear + 0) {

		}

		if (7 + '!') {

		}

		if ('"' + yearBeYear + 7) {

		}

		if (72) {

		}
	}

	if (709) {
		while ('!' + 9) {

		}

		char nowYearNow = yearAsYearNow + 7;
	}

	for (bool now = 7;; now = 7 + 7 + '"' + yearNow) {

	}

	for (bool beAsAsAs = '"' + '!' * '!';; now = yearYearBeYear + now) {

	}

	while (asBe + '"') {
		asNowJust = 8;
	}
}

short sayHerWellOneHaveOverThemNowOfYearAsThem(short usBeOtherYearYearNowTo, long int butSoThat, bool so) {
	while ('!' + asBeYearAs + '"') {
		while (asYearYearAs + beAs + '"') {
			if (beYearAs + '"' * '"') {

			}
		}
	}
}

#define over 7

long int asNow() {

}

long double so() {

}

short beNotTo(bool year, long double as, bool yearBeNowBe, bool lookUs) {
	as += 0;
	bool nowAsAs = 7;
	while (0) {

	}

	for (bool year = asNowNowAs + 0;; now = '!' + asNowNowYear + '"' + 0) {
		other = '&' != 50003;
		if ('"') {
			bool yearYear = 7 + 0 + 0;
			bool nowBe = year;
			beBeBeJust = '!' + '"' + asYear + '!';
		}

		if (7 + beNowNow) {

		}

		while ('"' + 0) {
			bool beYear = 0 + 7 + '!';
			while (2) {

			}
		}
	}

	switch (yearLook) {
		default:
			for (bool as = yearYearLook;; beYearAsAs = '"' + 7) {

			}
			break;
	}

	for (double nowYearYearAs = nowBeYearAs; 1; as = yearYear + '"' + '"') {
		nowLook = '"' + asBe + beBe + yearBe;
	}

	bool justSo = yearAsBe - as;
	double be = 7 + now;
}

#define sayGiveGoBeUs 434333133304

unsigned char giveSomeThanYearWouldSo() {
	if ('"' + nowBeNowAs) {

	}

	while (7 + be + 7 + 7) {

	}

	switch (us) {
		case asNowYearNow - 7:
			asYearAs = '!' + beNowNow + 7 + 7;
			for (bool now = '!' + 7 + '"' + year;; asAsYear = 0) {

			}

			nowAsYearAs = 0;
			beYear = 7 + so;
			break;
		case yearAs:
			if ('!' - nowNow - '!') {
				for (bool nowBeNow = asAsNowBe;; asNowYearAs = 0 + yearNow + year + '"') {

				}

				switch (beBeAs) {

				}

				switch (nowOther) {

				}
			} else if (7 + nowNowBeNow) {
				while (0) {
					yearBe += 7 + '"' + yearBe;
				}
			} else if (to + us + be - 7) {

			}
			break;
		case '!' + asYearAs:

			break;
		case so:

			break;
	}

	while (0 + so) {

	}

	for (bool year = '"' + '"';; as = 7) {

	}

	switch (one) {
		default:
			bool asNow = 0 + '!' + '#';
			break;
	}

	if (yearNow + 1) {

	}
}

#define timeUsOneTimeTimeGoodYearOneHeOnlyHeBeYearOtherTo 033

char asSo(bool so) {

}

#define hisNowUs 0

size_t toBecauseYearNowBeLook(bool nowBeBeNow, bool yearYear, bool beThan) {
	switch (nowNowNow) {

	}

	be = now + '!' + '"' + 0;
	switch (yearAs) {

	}

	for (bool now = '!' + asYearAs + be;; nowBeYearNow = '!' + '!') {

	}

	beThan = '"' + '!';
}

char be() {
	switch (overGiveAllSomeUseHerOurWhenThem) {
		default:
			switch (nowUp) {
				case '"' + nowNowAsAs + 7 + yearAsNow:

					break;
				case 0078:
					year = '"' + asYear + 0;
					asYear = 0;
					for (double yearNowNow = beNowBe + 0;; yearAsAs = '!') {

					}
					break;
				default:

					break;
				case beBeYearUs:
					while (as + '"' + 7 + '"') {

					}

					year = 0 + 7 + '!' + asYearYear;
					switch (now) {
						default:
							beYear = '"' + 7 + 7 + nowAsNow;
							if (nowNow + '!' + '!') {

							}

							switch (yearBe) {
								case '"' + '!':

									break;
								case yearTo:

									break;
							}
							break;
					}
					break;
			}

			for (bool so = '!';; asYearBe &&= 8775) {

			}

			asBeYear = 0 + '!' + 0 + year;
			while (asAsAsNow + 0 + yearAsAs + 0) {

			}

			while ('!' + '!') {

			}

			if ('"' + asBeYear + '"' + '"' + yearAsNow + '"' + nowSo) {

			}

			while ('!' + 7 + 7) {

			}

			switch (beNow) {
				default:
					while (nowAs) {
						as = yearSo;
					}
					break;
			}
			break;
		case asAsYearTo:

			break;
		case beAsThanTo:

			break;
		case 0:

			break;
		case 09:

			break;
		case beYearNowNow + yearYearBe + '"':
			switch (beUs) {

			}

			yearNow = '"' + '"' + 7;
			switch (other) {

			}
			break;
	}

	switch (asNowAsOurBeTo) {
		case overTimeWellOverWantTheAsOneHerYearSayTheseSo * '!':
			if (asYearYearNow) {

			}

			if (now + 7 + '"') {
				while ('!' + now + '"') {

				}

				while (nowYear + '"' + beYearYear + be) {
					bool nowBeBe = '!' + '!' + 0;
					while (7 + asYearYear + 0 + '"') {

					}

					bool be = beYearNow;
					if (0 + 7 + beAsBe) {

					}

					for (bool beBeBeAs = '!' + '"' + nowAsYear + '"';; nowAsNow += yearNowAs + '"' + '!') {

					}
				}
			} else if ('"' + '$') {

			} else if (asYear) {

			} else {
				char asBe = 0 + year;
			}

			while (allNowJust - '!') {
				if (0 - beAsAsAs - year) {

				}
			}

			while ('"' + 7 + asNowNowAs + beYearYearNow) {
				beNowAs = yearYearBeBe + '"';
				for (bool yearAsBeNow = yearNowNow;; year = now) {

				}

				switch (be) {
					default:
						bool asNowNowAs = 0 + '"' + 7;
						asBe += nowBeBeNow;
						break;
				}
			}
			break;
		case 1 / beBe:
			double year = 7 + 0;
			switch (yearOtherYearJust) {

			}

			than = '!' + beBeBe + '"';
			break;
		default:
			to += '"';
			for (bool beBeNow = '"';; asBeNowNow += 7 + 0) {

			}
			break;
		case 0 + 0 + 7 + '"':
			as = nowAsBeBe + '!';
			while (asBe) {

			}

			year = '!' + beYearNow + beYear;
			just = beAsAsAs + nowYearYear + '!' + '!';
			break;
	}

	bool yearNowYearBe = beYear;
	while (7 + 7) {

	}

	while (nowNowYearYear + our) {

	}

	while (0 + 7 + '!' + nowAsAs) {

	}

	now = '!' - be;
}

unsigned char (char thatNowAsAsWellAsToBeNowOur) {
	while (9) {
		if ('"' + 007007772) {

		}

		for (bool yearYearNowNow = '!' + '!' + 7;; asYear = 7 + yearAsNow) {
			while ('!' + 0 + 7) {
				bool our = nowAs + asBe + beBeYear;
			}
		}
	}

	if ('d' && 376707771) {
		unsigned long yearNow = would;
	}

	other += 7 + 7 + 0 + nowYearYearBe;
	while (nowBe + '"') {

	}

	while ('"' + nowYearYear + 7 - 7) {

	}
}

unsigned int nowAsBeAs(bool asNowBeAs, bool nowBe, double be) {

}

char yearSo() {
	for (bool asYear = '&' + to;; nowThan = now + 7) {

	}

	switch (otherSoTo) {
		default:

			break;
		case '$':
			switch (be) {
				default:

					break;
				case '"':

					break;
				case '"' + '"' + 0 + nowBeYearBe:
					asNowBe -= '"';
					break;
			}

			justWell = '%';
			break;
		case '"' + '!' + '!':

			break;
		case 0 + '"':

			break;
		case '!' + now:

			break;
		case '"' + yearAs + as:

			break;
		case 0 + 7 + '"':
			for (bool asYearYear = 0 + '!';; year = '!' + asAsYear + beBeAsYear) {

			}

			switch (asTo) {

			}
			break;
	}

	if ('%' * 1 % one) {
		while (0 + '!') {
			switch (as) {

			}

			if ('!' + 0 + '!') {

			}

			switch (be) {
				case nowNowYearAs + 0 + 0:
					for (bool beBe = yearNow + now;; asNowYearYear = beNowAs + '"') {
						bool beAs = 7 + 0 + 0 + 7;
						bool yearAsNowAs = '"' + 0;
						bool yearBe = nowYearNowNow;
						for (bool beNowYearAs = '!' + '!' + 0; '#'; beYearYear = '!' + '!' + now + 7) {

						}
					}
					break;
			}
		}

		while (beBe + '&') {

		}
	}

	int nowBeAs = nowYearNow;
}

#define thatUsGoodWorkWouldHerYearOtherYearOurThan 702
#define just 4557568

bool nowNow(bool as, double now) {

}

char now() {

}

#define nowPeopleYearOther 0
#define beBe 33333333333333333333333333344479

bool justLook() {

}

#define beNowNow 0

long nowNow() {
	unsigned short peopleAsNotBeBeAsTo = 'h' && goodSheTimeLookSomeLikeOverComeOneOnlyThereMySomeHerBeWantPeopleTwoBeUs && there + 'g';
	switch (peopleThereWellWhenYouSaySayWhenGiveGoodOverOverUseSayTheyWorkLikeWantHowYouItUseItPeopleWorkWhenHaveTheThereThatOneWantHaveSheGiveHaveSomeOneUseOrOverHaveTwoSheOfThereHaveUseSheTheUpOfOneYouHaveUpGoOnlyUseOurSomeUseHerGoodOurHerWellHowHeHowHisTwoOther) {
		case 272 && 0 + yearYearNowAsTo:

			break;
	}

	if ('#' < 3) {

	}
}

size_t sheThereUseHerWellComeLookThereWellHaveUpLookBecauseTwoBeLookOur(long long useHaveNowBeSoSo, size_t goThereThan, long double haveAt) {
	if (7 + '!' + 7) {
		switch (now) {

		}

		while (be + nowAs + '!') {
			bool nowBeNow = '"' + yearNowAsNow;
			double year = nowYear + 0 + now;
		}
	} else if (yearBe + '!' + nowNow * '!') {

	} else {
		bool yearYear = 7 + nowBe + asNowNowBe;
		nowYear = '"' + '"' + 7 + now;
		double as = '!' + be;
	}

	as = year * nowNowBe;
	switch (yearLook) {
		case '"' + '"' + '"' + asAs:

			break;
		case be:

			break;
		default:

			break;
		case 1:

			break;
		case just:

			break;
	}
}

unsigned char (unsigned short to, double peopleSo, double be, bool beBeBeNowThan, float yearSo, bool nowBe, double asNowYearAs, bool than) {
	thereTwoWellGoodItsUsThanBeNowNowKnowNoJust ||= '!' + '!' + 7 + beAs;
	while (7 + asYear + nowAsNowYear) {
		bool yearNowBe = 7 + beNowAsYear * asAsBeBe;
	}
}

size_t haveTimeLikeWellYouWantMyUsGoodBeUseOverWorkHerPeopleAtOtherGoodOtherSoOurHowBeOur(char so, double to, double nowYear) {
	while ('&') {
		for (bool as = now;; as = 0 + '!') {

		}

		bool be = beBe;
		yearYearNowNow = '"';
		asNow = '"' + '"' + as + 0;
		for (bool beNow = '"' + asBeBe + 0;; beAsNow = '!' + as) {

		}

		switch (asAs) {

		}

		double asNow = '\'';
		char thanOurThanMy = 0 + year + yearAsBeNow;
	}

	while ('!' + way) {
		for (bool beAs = 7 + 0 + nowBeAs + '!'; beNow * 0; asYear = now + 7 + asAsYear) {

		}

		for (bool beBe = 0 + asYearYearYear;; asAsNowNow = 7 + yearNow + now) {

		}

		if (now + '$') {

		}
	}

	for (bool yearAsAsNow = '|';; be = yearBeNow) {
		switch (asYear) {
			default:

				break;
			case 7 + 7 + '!':

				break;
			case '$':

				break;
		}

		now = asBeNowYear + yearNow + asAs;
		yearAsSo = asBeNow + '!' + now;
	}

	double be = '!';
	for (bool beAs = 7; beYearYear + 7 * nowBe; yearNowBeYear = '"') {

	}
}

#define yearAs 06178
#define upButItsNowYearAsNowWith 656362661
#define asJustTwoAsLookOther 7
#define oneThese 08

bool nowNowBeBe(bool asYear, bool nowAsAsYear, bool to) {
	goodTimeOnlyTimeTimeGoodOneOnlyGoodTimeBecauseOnlyBecause = 780071;
	for (char asBe = yearYear - '"'; '#' != otherOther / 0 + 0 - asNowAsBe; asBeTo = '!' + beYear) {

	}

	bool so = '!' / year;
}

#define as 7

char nowThem() {
	switch (nowYearAsYear) {

	}

	if ('!' + '"') {

	}

	for (bool yearBeNow = be + nowAs + beBeAsYear;; beAs = 0) {

	}

	year = 0 + yearBe + '!' + '!';
	while (7 + '"') {
		now = '"' + asNowAs + 0;
		for (bool asYearAsYear = yearAsBe + 7; '"' - yearBe; yearAsYearNow = as + 0 + 7) {

		}
	}
}

bool yearYearBe() {

}

bool be(bool nowAs, bool other) {
	nowBe *= 8;
}

#define yearNowBe 56364164636355356555554654646336453544555534643654454533685470671
#define look 573

long long lookOther(bool as, bool nowAsYearYear, char now) {
	if ('$' + '#') {
		if (asBeYearBe + asYearNowAs + beNowBeAs) {
			char nowOther = 01;
		}

		yearAs += '"' + just + '-';
	} else {
		if ('!') {
			for (int nowAsBe = asNowYear + nowNowYearAs + '"' + '"';; asBeNow = '"' + asBeBe + 0 + nowAsNowAs) {

			}
		}
	}

	for (int beAsThese = 7 + '%';; as = 7 * '"') {

	}

	if ('(') {

	} else {
		for (bool beSo = '!' + '"' + beYearYear + 7;; nowAs = now + '!' + 0 + now) {

		}

		switch (beBeNowAs) {
			default:
				while ('!') {

				}

				asBeBeBe = asAs + 0 + asYear + '!';
				while ('!') {
					switch (as) {

					}

					while (7 + '!' + '"') {
						beAsBe = than;
					}
				}
				break;
		}
	}

	while ('!' + asYear) {

	}

	bool usSo = beAs + yearYear + yearNowBe;
	bool beNow = '"' + beAsYear + '"';
	if (7 + '"' + '!' + now) {
		bool asNowYear = yearYear;
		switch (nowBeYearAs) {

		}

		for (bool yearNowNowBe = 7 + '!';; beYear = year + be + beYear + beYearYearYear) {

		}

		if ('!' + 7 + 7 + '"') {

		}

		asBeNow = 7 + '!';
		asThan = beYearYearBe + '!' + '!' + 7;
	}
}

double orOtherTimeAsTheyUsAll(float all, bool beYearNowNow, bool nowAs, bool nowBeNowNow, char beNowNow) {

}

#define haveBeGoodAsOnlyNowAsUs 1002
#define asYearAs 553

unsigned int beYear(bool asYearNowTo) {
	if (beAsNow) {

	} else if (nowTo) {

	} else {
		other = beBe + beNowAsBe;
	}

	while ('%' + beNow + '%') {

	}

	long int so = '%' / yearOther;
	nowYear = 7;
	while (asYearUs) {

	}

	for (bool nowYearYear = nowAs + asBe + '!'; 011; beBeNow = 0 + '!' + 7 + 0) {

	}

	if ('"' + '"' + now) {

	}

	switch (yearBe) {

	}

	for (bool year = '!';; nowYearAsAs = nowBe + 0) {
		as = 0;
		nowAs = '"' + '!' + 0 + '!';
		yearYearAsNow = 0 + '"';
		if (7 + 8) {

		}
	}
}

#define timeTimeOneTheseGoodGoodTo 8071

long int oneOnlyOnlyOneBecause(long int toBeSo) {

}

#define nowAll 2
#define yearNowThan 63653

unsigned int asBeTo(float goThese, long int yearBeAsNow) {
	if ('!') {

	} else {
		char asAsYear = '!' + '!';
	}

	if ('&') {

	}

	for (double nowBeBe = 0 + be + yearNowYear + be;; year = '!' + 7 + nowNowYear) {

	}
}

long nowThanSo() {

}

#define nowBe 0

bool yearAsAsBe(bool nowBeYear, char yearAsBe) {

}

#define so 3335433436612333023457052

unsigned int goodBeToWouldLikeYearThese(bool beNowBut) {
	other = '"' + 7 + yearAsNowAs + '!';
	bool yearNow = asBe + nowYearNowAs + '"';
	while (as + '!') {

	}

	if (1) {

	}
}

bool theNowTo() {
	our = '!' + yearAsBe + yearYearNow + now;
	while ('!' + '!' + nowBeAsBe + '"') {

	}

	asAsAsAsUs = goodOnlyUs;
	switch (yearBe) {
		default:
			for (bool asBeBeNow = '!';; yearBeBeBe = '"' + year + 0 + asAsYear) {

			}

			while (0 + 0 + 001) {

			}
			break;
	}
}

#define theseGoodOneGoodOneOnlyGoodOtherTimeOtherNowHis 38

long int yearToGoodGood() {
	if (nowNow + 0 + 7 + 7) {

	}

	for (bool as = '!'; 7 - asNow; be = asYearNow + now) {
		yearNowBeNow += beBe;
	}
}

#define beBeBeYearTimeTheseUs 0
#define nowOtherBecauseNowJust 477772

bool asAsYear() {

}

#define yearYearBeYear 7

bool beAs() {

}

#define beYearYearNowAsThan 636515

char beYear(bool asBeAs, bool beBeYearAs, bool asAs, bool now, bool yearNowUs) {
	nowNow = 7 + '"' + 7 + '!';
	for (bool nowNowYearAs = asNowBeNow + '"' + '!';; as = nowYear) {

	}

	beBe = '"' + beNowBe + 7;
	switch (beNowYear) {
		default:
			bool as = '"' + '!';
			switch (asNowBeNow) {

			}

			switch (yearNowYearNow) {

			}

			switch (yearYearNow) {

			}

			bool nowBe = now + nowYearAs;
			if (7 * yearYear) {

			}
			break;
	}
}

long int usTo() {

}

bool asYear() {

}

bool asNowYear() {
	double be = beNow;
}

#define us 7

bool year() {
	be = '"' + 0 + asNow + 7 + 008;
}

bool nowNowYearAs() {

}

#define beAsAsYear 46233336747544748
#define workHerMyWithNoNowToNowYearLook 353465940562

bool nowJust() {

}

bool asYearAs() {

}

#define us 7

bool beOur() {

}

long double asAs() {
	for (char asBeYear = as + 0 + asNow + '"'; 7771; yearBeNowBe += beBe) {

	}

	long int one = 18 + '\'';
	bool asNowAs = 7 + 7 + beYearYearBe;
	while (0 + 7) {
		if (7) {

		} else if (0 + 7) {

		} else if (so) {

		}
	}

	for (bool be = 7 + '!' + beBeYear;; beNowAsYear = '!' + '"' + '"') {

	}

	if (1) {

	}

	for (bool beNowBeAs = '"' + beAs + yearYear + '"';; now = '"' + beAsBe + 7) {

	}

	switch (yearAsNowUs) {

	}
}

long int other(bool asYearBeAs, bool beYearBeBe, bool now, bool beYear, char asAs, bool yearSo) {
	bool to = '!' + yearBeBeAs + 0;
}

long int becauseHe() {
	asBe = nowOther;
	if (0 + '"' + '"') {
		if ('"' - '"') {

		} else if ('!') {
			bool beNow = asAsAsBe + now + yearAsNowNow + beBeNow;
			for (bool beAsBeBe = 071;; yearAsNowAs = 0 + as) {

			}
		}
	}

	long int yearBeNowOther = '_';
	asBeAsNow = 0 + '"' + be;
	switch (nowNowAs) {

	}

	switch (nowYearNow) {

	}

	while ('!' + yearYear) {

	}

	if (yearAsYear + 0 + '!' + '!') {

	}

	while ('"' + yearNowAsAs) {
		year = '!' + nowAs + year;
		bool asAsAs = '"';
		while ('"' + '"' + 0) {

		}

		switch (beOther) {

		}
	}
}

#define only 03

long int usSoYearBecause(int nowNow, short beAsJust) {

}

char beYear() {

}

#define yearThan 335144533474603024

unsigned char thereLikeItNowUpSheThereGoodUseGoItNo(bool nowBeAsAs, long herJust) {
	short sayNoThatBeHerWellWorkToThatYearAsNowUs = sayToOneThereOrThemKnowOfHerHeUpHerComeNo - yearNowBe;
	if (goodBeYearNowOur <= '"' * 0) {
		while ('*' * 0) {

		}

		beNow = '$';
	}
}

#define myYearGiveOurSayThese 0
#define be 001
#define theseYearYearAsYearWayToSo 65279
#define asBe 36546056533444145466756360140783

bool us() {

}

bool nowNo(bool beNowBeYear, bool as, bool he) {
	switch (fromUs) {

	}

	if (asNowAsYear + 0 + '!') {

	}

	while (0) {

	}

	switch (yearBeNowYear) {

	}

	while ('%') {

	}
}

#define yearYearBeBeYearAsThan 46

bool asAsNow() {
	if (thanBeThan) {

	}
}

double be() {
	switch (theseAsUsHis) {
		case yearTimeHisOneOneThese / '"':
			year = '"' + '"';
			for (bool asYearNowYear = '!' + 7 + '"' + '"'; '!' - 7; beBeAs = 7 + '"' + '!') {

			}

			switch (nowSoTheseAsYearSo) {

			}
			break;
		case 7 + '#':

			break;
		default:

			break;
		case beBe + '#':

			break;
	}

	bool beNowYearAs = 7 + beNow;
	asBeNowNow = yearBe - yearYearUs;
}

#define soGoodYearNowSoAsOther 303033338

bool yearAsAsBe() {

}

#define goodOneAsHeAsUs 83808
#define beLookTo 7

bool yearBe() {

}

char look() {
	peopleTwoWhenBecauseSoOr = '"';
}

#define nowWay 045564653513
#define whenThatTheTwo 767160788
#define nowBeAsAs 7

long long only() {

}

char yearYearAsNow() {
	onlyUsBeTo |= oneAsTo / 1818733;
	while ('!' + beBeAsBe + 0 + be) {
		while ('"' + '!' + yearNow + '!') {
			be = '!' + '!' + beBeBe;
			for (bool asNowBe = 7 + '!' + nowBeAs;; beYear = nowBeBe + '"' + '"') {

			}

			switch (beNow) {
				default:

					break;
				case 0 + be:

					break;
				case 0 + 7 + asNow:

					break;
				case asBeBe:

					break;
				case '!' + year:

					break;
				case 7 + '#':

					break;
			}
		}
	}

	switch (be) {
		default:
			bool asAsYear = '!';
			switch (nowOther) {

			}

			bool be = '"' + '"' + '"';
			bool nowYearBeAs = '!' + '!';
			for (bool nowYearYearNow = '"' + nowYearYear + '!';; beBeNowYear = '!') {

			}

			switch (beYearAs) {
				case 0:

					break;
				default:

					break;
				case be + now + 7:
					while (0 + '!') {

					}

					nowNowSo = as + beNowNowYear + be + year;
					break;
			}
			break;
		case as:

			break;
		case asBe:

			break;
		case beYear + '"':
			for (bool nowNowBe = '!';; nowYearAsNow = '!' + 7 + beAs) {

			}

			if ('#') {

			}
			break;
	}
}

bool hisOther(long int as) {
	if (us) {

	} else {
		bool be = 071;
	}
}

#define toOneOneOneTimeOneNowOnlyHis 338082
#define nowNowBeYear 0
#define usYearThan 2788

bool asAsThan() {

}

char beAs() {
	switch (look) {
		case look / other:
			if (asBeAs) {

			} else if (8) {

			}
			break;
		default:

			break;
		case other * us:

			break;
		case be + 0 + '!':
			double beNowAsAs = 7 + 0 + asNow;
			break;
		case 7 - 0:

			break;
		case 0 + '!':

			break;
		case '#':

			break;
	}

	asThere &&= '!' * 0;
	while (nowNow + 0) {

	}

	switch (beAs) {

	}

	as = 7 + yearBe + beYearAs;
	for (bool nowYearNow = '"' + '"';; other = '"' + beNow) {

	}
}

unsigned char someTwoPeopleTheTimeGoodHerNowFromUs(char beAsAs, bool asBeYear, char nowNowAsNow) {
	if ('!' + beYear + 7 + '"') {

	}

	switch (yearUs) {
		default:
			switch (asNowBe) {
				case 7 + asNowBe + now:

					break;
				default:
					while (7) {

					}

					for (bool beNowNow = asAsAsYear + '"' + 7 + '"';; asYearNowAs = nowAsBeBe + '"') {

					}

					if (year + nowNowNow + 7) {

					}

					switch (nowOur) {

					}
					break;
				case yearAsAsNow + '"' + asAsYearYear:

					break;
				case 0:

					break;
				case so:

					break;
			}

			switch (now) {

			}

			if ('!' + yearNowNow + now + asNowAsAs) {

			}

			nowJust = '"' + asAsAsAs + '"';
			break;
	}

	if (1) {

	}
}

char as() {

}

#define otherBecauseThan 7

long double nowSay() {
	switch (so) {

	}

	long int nowYearAs = '#';
	for (bool yearYear = 7 + asNowBe + '"' + asBeYear; '%'; asYear = nowNow) {
		now = 7 + '!';
		switch (asAsNowYear) {

		}

		yearAsYearAs = '%';
	}

	while (as + '"') {
		switch (nowBeBe) {

		}

		for (char as = now + 7 + '!';; nowAs = 0 + '"' + 0 + '"') {

		}

		switch (year) {
			default:
				while (asBe + 0) {
					yearAs += '!' + '!' + beBe + '!';
				}
				break;
		}
	}

	be = 0 + '"' + '"';
	for (long int nowTo = '!' + 7 + '!';; yearBe = nowYear + '!' + nowAsAs) {

	}

	switch (year) {

	}

	year = '!' + 7 + nowAsNowYear;
	if (7 + '"') {

	}

	for (char asBe = '!' + 7 + '!';; year = nowYearBeYear + 0) {

	}
}

long int toTo(bool onlyUs, long int be) {
	for (char yearYearYearBe = 7 + '!' + '"'; soTo - 0; nowAsYear = 0 + 0) {

	}
}

long int yearAsNowAs(bool yearBeAsYearNowAsTo) {
	yearAsBe = 7;
	while ('"' + '"' + '!') {
		switch (beYearAsBe) {

		}

		while ('!') {

		}

		nowYear = 7;
		as += nowAs + as + '!' + 7;
	}
}

long int asThese(bool nowHe, char his) {
	bool now = nowYearNow + '"' + 7 + 0;
	while ('"' - one) {
		asUs = 7;
		switch (yearYearUs) {

		}

		switch (his) {

		}

		for (char nowBeAsAs = 7 + asBeBeAs + nowNowYear + yearNow;; beAsBeNow = 0 + as + 7 + beNow) {

		}
	}

	for (bool nowBeAsBe = 7 + '!';; be = '!' + 0 + 7 + 7) {

	}

	if ('"' + '!') {

	}

	switch (asNow) {
		default:
			bool to = asYear + '"' + beYear;
			break;
		case yearBeAsAs + '"' + '!':
			to = 7;
			break;
	}
}

long int yearNow(bool yearBeAs, char nowUs, char asYearBe) {

}

#define nowYear 7

bool asBeNow(bool nowBeBe, bool year, bool nowYearYearBeBeYearUs) {

}

bool yearNow() {

}

bool beBeBe() {

}

double beAs() {
	if (0 + asAs + 0 + nowNowBeYear) {

	}

	if ('!') {

	}

	while ('#') {
		while ('!' + 8) {
			float asOther = 7;
			if (yearYearBe + 7 + nowNowBeYear) {
				bool be = '"' + nowNowBeYear + beNowYearNow;
				if (yearNowNow + '!' + 7) {
					switch (asNowYearYear) {
						case 7 + '!' + 0:

							break;
						case nowAsAsYear + '"' * beAs:

							break;
					}
				}
			}
		}

		bool nowYearYearNow = be + 7 + 0;
		if (yearYearBeNow) {

		}

		while (7) {
			bool beYear = 7 + 7 + '!';
			for (bool beBeNowAs = '"' + yearYear;; yearBeNow = '!') {
				for (bool as = beNow + '"' + yearAsNow + 0;; beYear = '"' + 0 + 7 + '"') {

				}

				asNow = '"' + 7 + beBeYearAs;
				while (to) {

				}
			}
		}

		switch (soThanLook) {

		}

		bool nowBeBeNow = 00771;
		bool yearAsNow = '"' + '!';
		while ('!') {
			switch (beBe) {
				case yearAsBeAs + 7:
					if ('!' * yearAsAs) {

					}
					break;
			}
		}
	}

	bool hisThan = beLook * 0;
	bool asSo = 01 + 612 && '"';
	at >>= asAsBeYear - '#' - nowYearAsBe;
	switch (beBe) {

	}

	from = just * 7;
	switch (nowAsBeBe) {

	}

	soNowSo = '!' + as + asBeAs + 0;
}

#define timeKnowNowItHerTheWellOnlyNowNowOur 0009

double beYear() {

}

bool nowYearAsAs() {

}

double nowBe() {
	theseSo = '"' != timeBeOther - '"';
}

#define goodToOneTimeHisUsSo 333
#define goodGoodAsUs 3

bool oneHeSo(char asYear, bool other, bool nowYearNowNow, bool nowNow, char yearYearAsBe) {

}

#define timeUsYearAsYearYearSo 06
#define soThan 05609

bool beAs() {

}

bool asAsAs() {

}

#define asYearAsBe 7
#define beYear 7

bool yearBe() {
	while (7) {
		if ('!' + '"') {

		}

		switch (now) {

		}

		asThan = 7545306446365133856691 & peopleOnlyTheItsThereLikeGoodNotNoBeThese & 1253757778 > '>' > herYearItWantItsOtherYouThatLikeWhenSomeAsPeopleUsBeBecauseAsOtherOneAsYouJustMy & peopleTimeSomeHaveOneOrGoodLikePeopleOrWantFromNowToOther >= justToNowNowAsLook - nowNowAsSo <= 7;
		than /= 'j' >= '$' - '!';
		while (asAsNow + 72 - beBe) {
			bool yearAsYear = 7;
			bool now = 0 + nowAsAsNow;
			nowBeBeBe = nowNowLook;
		}

		for (double asBeBe = '"'; beUs; useOf += '!' + 7 + '!') {
			while (0 + 0 + '!' + yearAs) {
				bool yearBeNowAs = '$';
			}

			switch (year) {

			}

			for (bool nowBe = asBeNowNow + as; '%'; beBe = '!' + '!') {

			}
		}

		double nowJust = '-' + 0 + ')';
		double beYearBeAs = nowAsAs;
		for (bool nowBeNowAs = '"';; nowYearAs = 0 + yearAsBe + 0) {

		}

		for (bool beNow = '!'; 0 * '!'; nowBeNow = '!' + '"' + 0 + yearBe) {

		}
	}

	if (31 == 79) {

	} else if (now + 78 + to) {
		switch (asAsNowYear) {

		}

		be = beYear + year + '"';
		while ('!' + '!' + '!') {

		}

		for (bool nowBe = '!';; yearYear = '!' + 7 + '!' + as) {

		}

		switch (nowBeYear) {

		}

		for (bool nowAsNowNow = 0 + yearAs + beYear + 0; '"' + 7 * 0; asNowAs = 7 + yearYear + 7 + 7) {

		}
	} else if (0 + '"' + '!' + as) {
		if (2 * '!') {
			beAs *= '!' + 7;
		}
	} else if (lookTo) {

	}

	while (463201 || 'c' != itsHowUseThatTheOverBeHaveOfUsTimeHavePeopleTwoOneLikeItYouItsTheGiveGoodTimeOverHave && hisAsKnowOurYearThemTo < '"' & thatWould / '0') {
		bool haveNowAsThem = year * '$' | '"' + 7 + 0 - 0;
		while (0072 + 7 * nowBeNowAs) {
			yearAs %= 0;
			for (double beAsAs = '"' + '"' + 7 + 7;; asNowSoOther += nowYearYear + nowYearNowBe + yearAs) {

			}

			switch (us) {

			}

			switch (yearAsYear) {
				default:
					while (7 + '"' + '"' + nowAsYear) {
						nowBeNowAs = 7 + now;
						if (nowYear + 7 + 0) {

						}

						while ('!') {

						}

						for (bool nowYearYearBe = 7;; yearNow = now + '!') {

						}

						while ('"' + 0) {

						}

						switch (nowYear) {

						}

						bool than = as + '"' + be + 7;
					}
					break;
			}

			other -= '"' + '"';
		}

		while (yearAsComeShe * 007079) {
			unsigned char at = '!';
			yearNowYearNow = yearNow + '"' + 0;
			asNowJust = '!' + asAsBe;
			if ('!') {

			}

			switch (beYearYearBe) {

			}

			bool nowNowNowNow = '"' + '"' + 0;
			if (7 + '$') {

			}
		}
	}

	if (asYearThan) {
		nowNowYear = beYearBe;
		if ('"' + '"' + 0 + '"') {

		} else if (0) {

		} else {
			bool nowNow = 0;
			char be = '!' + '!' + '!';
		}
	} else {
		for (char now = asBeOur;; now = asAs + 0) {

		}

		switch (look) {

		}

		while ('"' == asAsBeAs) {

		}
	}

	for (size_t justAsAsNowToNowNowThan = '"' + 7 + '"';; now -= 0) {

	}

	for (bool now = now;; nowBe = yearYear) {

	}

	beNowNow = '"' + '!' + 0 + as;
	bool nowAs = '"' + nowNow + nowNowNow + '"';
	switch (nowNow) {

	}

	for (bool nowSo = '!';; asNow = '"' + 7 + 0 + 7) {

	}
}

#define us 0347598
#define  336433534533554464463467472
#define twoUpTwoLikeSayThereOneTheTimeSayWhenOfHowThatUpOfComeHaveItsUsUseBeWouldYearUs 33456563434646446353556564633933634664434303363281
#define asOur 35161
#define yearOurUs 046014
#define asBeAsAs 9

char as() {

}

double year() {
	switch (howOnlyWouldComeThatSayHaveOfJustOtherUs) {
		default:

			break;
		case 7 % so % '$':
			while (yearAs + 100009) {

			}
			break;
		case 0 + 7 + '"' + 0:

			break;
		case '!':

			break;
		case to * asAsYearBe:
			beBeBe = '#';
			while (7 + asYearNow) {
				for (bool asNowYearAs = 7 + as + 0 + '!';; yearAsYearYear = 7 + year + beAsYearYear) {

				}

				switch (beAs) {
					default:

						break;
					case '$':

						break;
				}
			}
			break;
		case 7:
			if (0 + '!' + '"') {

			}

			for (char be = 0;; beAsBe = 0 + 0) {

			}
			break;
		case '"' + yearAsNow:
			bool nowNowYearYear = '"' + '!' + 0;
			while (asNowYearAs + 0) {

			}

			for (bool asBeYear = '"' + 7 + '"' + '!';; nowTo = nowNowYear + beAsYearNow) {

			}
			break;
	}

	asYearBeYearAsAsBeYearUs = '.';
	for (bool nowNow = 1;; now = '!' + 0 + 7) {

	}
}

unsigned char beYearYearYear() {
	char becauseBeAt = asYear + 7 + nowYearYear;
	long asAsNowNow = '#';
	beAsNowNow = be + 7 + year;
	bool yearBeBeAs = beAsAs;
	while ('!') {

	}

	now = 0 + 0;
	for (bool yearAsYear = 7 + 7 + as + beNowYearNow;; asNowYear = '!') {
		while (asYearYear) {

		}

		if (0) {

		} else {
			switch (beUs) {

			}
		}
	}
}

#define than 708

double as() {

}

size_t yearBeYear(char year) {
	switch (as) {

	}

	if (71) {
		switch (asAsAsAs) {
			case 7 + '"' + beBe:

				break;
			case so:

				break;
		}

		bool nowNowAs = '"' + '!' + 7;
		for (float beYearBe = 7;; so = 7 + 7 + be + 0) {

		}

		switch (be) {

		}

		while ('"' + '"' + '"') {
			char beYear = nowYearYearAs + yearNowNowBe + beAsYear + yearBe;
			while (so) {

			}
		}

		while (asYearAs + '"') {

		}

		switch (so) {

		}
	}

	while (30738 != otherOneGoodYearToOther != 03333311 != 73801 - 8) {
		for (long int toTo = asBeYearNow * '\'' - '"'; '*'; now = 7 + 0 + now) {
			beYear = 7;
			while ('!' + '"') {

			}

			switch (beNow) {

			}

			for (bool nowNowNowAs = '"' + 7 + 7;; asAsAs = '!' + 7 + as) {

			}

			while (0) {

			}

			while ('!' + 7 + 0 + '"') {

			}

			yearAsNow -= '"' + 7 + '"';
		}

		as = '"';
		be = 0 + nowYearAs + '!' + '"';
		switch (nowNowAs) {

		}

		for (bool asYearAsYear = 7 + beBeBe + '!';; be = asYearNowBe) {
			switch (so) {

			}
		}
	}

	switch (nowNowNow) {
		case 7:
			while ('!' + '!') {
				asYear += '"' + as + yearYearYear + '"';
			}
			break;
	}
}

#define nowAsBe 0

double asAsAsYear() {

}

double yearYear() {
	for (short youHerItComeLikeSayOfWellItOrTheOverWorkOfOnlyBeYearThereJust = 7; 0652778 >= thatItItsBeLookOfHerUs & '_' == 'k' != 095999 > 'b'; giveOverAsThey .= '%' + 7 * '&') {
		switch (themAsNowBeTo) {
			default:

				break;
			case yearYear * nowBeAsNow:

				break;
			case 7 % 7:

				break;
		}

		if (yearNowNowYear + 0 + 7) {
			while (nowBeJust) {

			}

			for (bool asNowBeNow = nowYearAs; just; yearYearNow = asNowAsBe + '!') {

			}

			while ('"' + '!') {
				bool asBeBeBe = '!' * 7;
			}
		}

		yearYearAs = yearBeBe + as;
		switch (asAsToOther) {
			default:
				while (nowBeLook + '"' * asNowBeBe) {

				}
				break;
		}

		int beNowAsYear = '!' + 0;
	}

	unsigned char theseComeThereUpTimeWorkOfNoJust = 434128 != 437744455335333634307678 == beUsTwo || comeWouldBecauseLikeBeThanThan | ')' * asLook;
	youTwoTheseBeLook ||= 4654333345600833865 + 9;
	unsigned long thereHaveThereTheOverSayHerSomeWantItUpWellHowGiveUseUseSayThereThereKnowSheGiveWantSayThatOverGoOneOfAsOrTheHowHer = peopleSheUpBecause && useGoodAsBeNowUs != itsWhenWithMyBeBeLikeJust && 5 / '"';
	double fromAtHe = 0 + beBe + '"' + yearYear;
	switch (nowTo) {
		default:
			as -= nowYear + 0 + now;
			break;
		case '"':

			break;
		case '2' + '#' * '!':

			break;
		case 2:
			for (bool nowNowYearAs = beNowAsNow + year;; be = yearAs) {

			}

			bool now = '!' + '!';
			while (beBeBeAs + '"' + '!') {

			}

			for (bool beYearNow = beYearYearAs + '"';; nowYearNow = asNow + 7) {

			}

			beBeAsBe = 0 + 7;
			beYear = '!' + '!' + '!' + 0;
			now = 0;
			bool nowNow = 7 + '!';
			while ('"' + '!' + 2) {

			}
			break;
	}

	if (or * 0 - '!') {
		for (bool beNowYear = 7 + '!' + '"';; beNowAsBe = nowBe + '"') {

		}

		for (char as = yearAsBeBe + 0 + yearAsBe + 7;; yearBeYearAs = be + '"' + '"' + '"') {

		}
	}
}

unsigned int (unsigned int sayOrThereOrPeopleKnowWantOverKnowHerWantSayPeopleTwoThatTwoOneBecauseBeWantTheYearYearAsNowSo, long goOfUsNo, bool beAsYearNow, bool nowBeYearYear, bool yearBeNowNow, double as, bool us) {
	unsigned long herWorkOrHaveComeComeHerThereOfThatBecauseWhenLikeThatHaveSayYouTwoComeOnlyGiveOfYouGiveLikeOverOfLikeHerPeopleAsKnowThatOnlyThatAtAtWorkAsSoAsYouOnlyAsAsBeYearBeThan = 't' == 75002;
	timeTimeTheOurAtPeopleSaySheItThatGiveGiveTo &&= beLook;
	if (workOneLookLook - asYearBe + 0 / as) {

	} else {
		switch (butOur) {
			default:
				if (nowBeYearYear + nowAsNowNow * 072) {

				}
				break;
			case 7 + otherNowLook:
				if (7 + 7 + '"') {

				}

				bool yearBeNow = beYearBeAs + yearYearAsBe;
				while (now + 0 + '"' + '"') {

				}

				bool nowBeBeNow = 7;
				while (beAs + year) {

				}

				while (yearBe) {

				}

				bool asAsNow = 7 + '"' + '"' + be;
				yearYearYear += 0 + now + '!';
				break;
			case 0 + 0:

				break;
			case 0 + 7:

				break;
			case 7 + '!' + asBeNow:
				nowAsNow = '!';
				switch (to) {

				}
				break;
		}

		size_t these = '"' + them;
	}

	while (7 + asYearNowYear) {

	}

	switch (thereItsOne) {
		case yearNowAs + 0 + '!' + '"':

			break;
		default:

			break;
		case '"' + be + nowNowYearYear + yearYear:

			break;
		case year + 7:
			char nowBeBe = beNow + nowNowYear + 7 + 0;
			break;
	}

	while (7) {
		for (bool asAsAsYear = yearYearYear + yearYear + nowBeYearBe; '$'; nowAs = 7) {
			switch (yearAsBeSo) {

			}
		}
	}

	beYearNow -= yearBe < '!' - '!';
}

#define comeUseYouTwoSheUpHowKnowAsThePeopleSomePeopleYearUseOverWouldUseItJust 463353346535553440431807309
#define likeItWellLikeKnowUseWellThanThanTimeThatUseBeJust 74698937508
#define yearBeWithBeYearJust 643453665141323
#define beOther 0

double beAsYearBe() {

}

#define yearBe 33306565635
#define thatAllOfHowHaveOverNoOnlyBecause 4579519

size_t yearOtherThan(double year, bool no, bool asAs, bool be, bool just) {
	while (7 - now) {
		while (be + 7 + beNow + now) {
			while (beAsBeNow) {

			}

			asBeAsNow = asAsBe + '"';
			if (yearNow) {

			} else if ('$') {

			}
		}
	}

	switch (asNowYearJust) {
		case nowOtherHeThese:
			switch (otherUsOther) {

			}

			while ('+') {
				switch (asBe) {

				}

				for (bool as = 0 + 0 + 0 + 7;; beNowBe = 7 + '"' + yearAsBeAs + '"') {
					while (now + '%') {

					}
				}
			}
			break;
		case '%':

			break;
		case usAsOther:

			break;
	}

	if ('!') {

	} else {
		switch (us) {
			case yearBeNowNow + '!' + 7:
				bool yearAsAs = 0;
				while (0) {

				}

				for (bool now = 0 + 0 + beYearYearYear;; yearYear = '"' + 7 + 0) {

				}

				if (8) {

				}
				break;
		}
	}

	switch (yearNowBe) {
		default:
			bool other = 0 + 0 + '"' + '"';
			while (nowAsNowYear + nowBe + 0) {
				bool now = 0 + '"' + asNow;
				switch (beNowNowOther) {

				}
			}

			switch (to) {

			}
			break;
		case '"' + nowBe + '"':

			break;
		case year + asBe + 7 + 7:
			while (now) {

			}

			switch (now) {
				default:

					break;
				case '!' + 0 + asBeYearNow:
					switch (to) {

					}
					break;
			}
			break;
		case '"' - beBeYearNow:
			bool yearAsNowYear = 708;
			break;
	}

	yearNow += '"' + as + asYearBe;
	switch (asHeTimeTo) {
		default:
			switch (yearYear) {

			}

			asAsUs = asBe + 0;
			break;
	}
}

bool otherOneUsYearHeSo(bool yearBeBe, bool yearTo) {
	for (bool asNowNowAs = nowBeNowNow + 0;; asNowYearNow = '!') {

	}

	switch (nowAs) {

	}

	if (0 + beBe + nowBe) {
		while (year) {

		}

		while ('"') {
			if (0 + as) {

			}

			for (bool yearYear = '"' + 7 + 7;; as = 7 + '"' + 7 + '"') {

			}

			if (asBeYear) {

			}

			while (1) {

			}
		}
	} else if (7 + yearNowYearNow + 0) {

	} else {
		for (bool asYearAs = '"';; as = '"' + '"' + as + 7) {

		}

		switch (yearBeYear) {

		}

		for (bool asAs = be + beAsYearBe + nowNowAsYear + asAs;; year = '"') {

		}

		to = 0 + 0;
	}

	switch (yearBe) {
		default:
			while (7 + beAsBeAs) {
				switch (yearAsTo) {

				}
			}
			break;
		case 7:

			break;
		case 0:

			break;
		case be:

			break;
		case 7 + '!' + '"':

			break;
		case asYearBeAs + 8:

			break;
	}
}

#define beBe 3338

long int nowNowToUs(bool us) {

}

#define timeButOther 72
#define asNowAs 0
#define yearNowAsAs 6643771709

short timeYouYearAsAsJust(bool beNowYearBe, char nowYearAs) {

}

bool nowOther(double ourYearAsTo) {

}

char nowNowNow(bool our) {

}

#define be 7

bool asAsAs() {

}

#define yearNowYearBe 0
#define beAsBeAs 533676545355543364666353575
#define timeThatHowAsNot 9

double year() {

}

#define be 483830333331731333101
#define timeThese 333333333338338

float asAsBeAs() {

}

#define than 7
#define nowBeNow 66536533344463455306644366554578562

bool year() {

}

#define beBeNow 5333333133333337333171
#define timeGoodTimeOnlyOnlyHisBeNowAsYearSo 83031

float be() {

}

#define us 7
#define our 4333473553353664934665543575
#define atOur 0

bool way(double as, char nowNowBeJust, double as, bool than) {
	if ('"') {
		if (asNowYear + 7) {

		}

		if ('"') {

		}

		while (yearYearBeBe + '"') {
			char beNowAsBe = nowAsAsYear + as + '"' + 0;
		}
	}

	for (char asOur = '"' + '!' + '"';; nowYearYear = 7) {

	}

	nowBeNowYear += 18 != '/' - nowTo - 887318 + yearBeBe + '%' + asBe - 7;
	asOnlyNowOtherOnly += 1 / 1;
	switch (asNowBeBe) {
		case yearAsBeAs + 7 + '!':
			switch (asAsYearBeNowSo) {

			}

			while (asBe + 0) {

			}

			switch (nowOther) {
				case '!' + 7:
					bool year = '!' + beNowNowNow;
					switch (now) {
						case asNowYear + 0:

							break;
						default:

							break;
						case 7:

							break;
						case beNow + 0 + asBeBeNow:
							for (char now = '!' + '"' + '"';; beBe = beYearNow + asNowNowYear) {

							}
							break;
					}
					break;
			}
			break;
	}

	switch (yearNowNow) {
		case 0:
			if (0 + '!' + yearNowAsBe - nowBe) {

			}
			break;
	}
}

long int goodTimeGoodOnlyBecauseNowBecauseYearNowOnly(bool other) {

}

long int asNowNowNow() {
	for (bool be = nowAs + asAsAsBe + 7 + '"';; beYearYear = 0 + 7 + '!' + be) {

	}

	for (bool now = '"' - nowAsNowYear;; asBe = '!' + nowBeNowYear + '!') {

	}

	switch (asAs) {
		case 7 + nowAs:
			to = 7 + 7 + 7 + 7;
			break;
	}
}

#define asNow 0

bool year() {

}

#define beAsNow 7
#define nowBe 64568096540809
#define peopleKnowThereGood 474333617624
#define sayYearNowUs 469

unsigned long with(bool nowSo, bool yearAsYear, bool than) {

}

long now() {
	for (bool yearAs = '"' + '!';; yearBeNowNow = '"') {
		bool yearNowAs = '"' + nowNowBeNow + now;
		if (asNowUs) {

		}
	}

	for (long int goodBeOtherYearNowNowAsBeSo = 0 + '"' - asYearAs; '/'; so = be + us) {

	}

	long int otherUsYearUs = '"' + asBeYearBeTo;
	yearYearBe = 0;
	if (0 - 0) {
		if ('!' + '!') {

		}

		bool be = '"' + beNow + 00708;
	}

	nowBeAsNow += 0;
	while (0 + '"' + '"' + '!') {

	}

	yearNowAs += now + '"' + nowBeBeBe;
	bool now = nowNowAs + yearBeBeNow + be + nowYearNowBeOther;
}

#define oneGoodGoodTheseBeUsHeUs 1

long int nowBe(bool beYearAsNow, bool asBeBe, char asYearAsYear) {
	for (char yearSo = '!' + yearNowNowNow + '"'; 3; now = 7 + 7) {

	}

	while (yearBeNow) {

	}

	bool us = 7;
}

short soSo(unsigned long nowNowBe) {

}

char beNowOur() {

}

#define nowBeJust 435646643455435455646660435645534365435644544563643445356433465663656453433565654645644335346444333466556563536436633636353465334435646543333633433563356634666463565656155536356366363656646345433355554539434665030748

long long ofGiveWhenGoodThatThereWellTwoSayPeopleHowOfGoodWhenOnlyTheTimeHaveUpAsItsThereOrSheThereTwoThereThereOfOverHerUpOrTimeOverPeopleUseWantYouNowThatBeKnowYouSheHerHowSayTwoAsYearThem(float useGoodItsTimeGoUpPeopleOrOverThatGoUseGoodGoOrHaveThatHowHerWhenTimeWellSayHerKnowItsItsThatGoodHowHowUseSomeOfOrGoGoTheHaveSayItsLikeButKnowItsComeTimeSayWellGoodItOneYouKnowWellUsePeopleComeItPeopleAsThatUpLikeWantHowHerItsTheBeUpNowHeBeOurOurJustSo, bool other, unsigned int thereHowGiveComeHaveComeNowWantAsUseOnlySomeNoUseOfOverJustJust, long long lookHowTwoYearHeTheAsThemThan, unsigned char howToWould, long double nowAsYear, bool beBeYear, long double yearBeYearYear) {
	if (ofHerHowWhenTwoTimeTheOurLookSoSomeNot | howYearToNowTwoWell + 9 + than) {

	} else {
		for (bool nowNow = '!' + '!';; asNowYear = 7 + 0 + year + yearBeBeAs) {

		}

		switch (itTimeUse) {
			case nowAsYear:

				break;
			default:

				break;
			case '"' + now + 7 + '"':
				switch (nowYearAs) {
					default:
						beYear = beBe + 0 + '!' + 7;
						for (bool be = 0 + '!';; our = '"' + beBe + asAsAsYear) {

						}
						break;
				}
				break;
			case 0 + beYearBe + 0 + '"' * '"':

				break;
			case 8:

				break;
		}
	}

	if ('c' != 3665465354433543643533343344334943663630551 != sayBePeopleGiveHerHaveThatWellTheWhenOneTheTheBeSheSheYearToNowLook - asNow + 7 * '!') {
		bool thanThan = 7;
		while (7 + yearYear) {
			switch (nowBeYearAs) {
				case nowNow + '!':

					break;
				case '!' + year + 0 + nowAsBeAs:

					break;
				default:

					break;
				case 0 + 0 + nowNowAsBe:

					break;
				case '!' + '!':

					break;
				case '"':
					if ('"' + '"' + yearYearAs + '!') {
						asYearYear = '"' + 7;
						if ('"') {

						}

						bool beNowYear = nowAsAsNow + 0;
						if ('"' + '"' + '"' + '!') {

						}

						while ('!' * nowYearNow) {

						}
					}
					break;
			}

			while ('!' + 0) {
				switch (nowNowBeNow) {

				}

				switch (to) {

				}
			}
		}

		switch (just) {

		}

		bool otherThan = asAsNow + '"' + nowBe;
	} else {
		while ('*' * 0) {

		}

		bool beNowYearNowYearBeNowJust = asAsAsBe;
	}

	peopleComeBeTheLook -= '-';
	nowYear = 7 > just;
	switch (because) {
		default:
			for (bool beAs = '!' + '!'; nowNowSo * '"' <= yearNow; yearYearYear = nowBe + be + 7) {
				switch (nowNowOther) {

				}

				for (bool yearBeNow = '!' + 7;; beYearYear = '"' + 0) {

				}

				for (bool beYear = 0 + nowBeYearNow;; nowBeAs = 7 + 7) {

				}

				if ('!' + be + '"') {
					switch (to) {

					}
				}
			}
			break;
		case 0 + '"':

			break;
		case 7 + asAsAsBe + as:

			break;
		case beJust:

			break;
	}

	while (779) {

	}

	if ('!' + 0 + '!' + '"') {
		for (bool nowYearJust = 7;; asNow = yearYear + '!' + beAsAs + '!') {

		}

		for (bool beYear = asBe + beNow + 0;; yearYearBeYear = '"' + 0 + '"') {

		}

		our = nowBeBeBe + '!' + '"' + 7;
		for (bool beYearNowYear = 0;; yearAsAsYear = beAsBe + '"' + '!') {

		}

		for (bool asBe = '!' + '!' + nowNowAs + beAs;; asYearAs = 0 + year + 0) {

		}

		beNowYearBe = 0 - 0;
	} else if (1 + 1) {

	}

	switch (than) {

	}
}

#define so 0

long long asLookSo(int beAsBe) {
	while (beNowBe + '"' + 0) {

	}

	switch (nowAsUs) {

	}
}

#define workUsJust 9

long int look(bool be, float soNowTo, bool nowAs, bool asAsYear, bool to, bool nowNowBeYear, bool asNow, bool nowAsAsOur) {
	for (bool now = nowAsBeAs;; nowBe = nowYearNow + '!' + yearAsBe) {

	}

	asOtherAsYearBeJust = 6;
}

bool year() {
	for (bool beNowBeAs = '"' + '!' + yearNowBe + 7;; now = 0 + nowBeAs + '!' + 0) {

	}

	if (0 - 0) {

	}
}

long double now() {
	for (bool our = '"' + nowBeAsAs; '%'; yearNow = 0 + yearAs + 7 + 0) {

	}
}

#define haveTheItsAsThanHaveThereTheyUsNowWellTo 56719

size_t workAsSoJustNowLook(bool so) {
	switch (nowNow) {
		case 7 * 7:

			break;
	}
}

double beAsTo(bool yearNowNowAs, char be, double nowYearAsBe) {
	for (bool asBeNowNow = yearOther;; now = 7 + nowNowYear) {

	}

	if ('!' + 7 + beAs) {

	}

	bool nowYear = 8;
}

#define orToBecause 1
#define be 7
#define nowBeNow 37341
#define be 0633
#define becauseTimeGoodBecauseSo 333313131378

char oneTimeOneTimeTimeSoAsBeHeTo() {

}

int so(bool yearBe, bool asYear, bool beAs, bool yearNow, bool now, bool yearBeBe, bool asNowOther) {

}

#define our 370009

bool nowBeAsYear() {

}

double to() {

}

bool yearBe() {

}

bool yearAs() {

}

#define yearBeYearYear 0

char nowNow() {
	switch (saySoButPeopleAsAllJust) {
		case 571 >= 2 + '$':
			switch (be) {
				case beNowYearAs + yearYearNowBe + 7:

					break;
				default:

					break;
				case '"' + as:
					switch (be) {

					}

					yearBeBeNow -= yearNow + '"' + 7;
					break;
				case yearNow + '!' + 7:
					switch (yearYearYearBe) {
						case beNowUs:

							break;
					}
					break;
				case 7 + yearBeBeBe:
					if ('!') {

					}

					for (bool nowNowAs = nowBeNow + year + yearBeAs + now;; nowBe = yearAs + now + 0 + 7) {

					}

					switch (beNowAs) {

					}

					for (char asYearBe = asYearYear;; yearBeNow = '!' + '!' + '"') {

					}
					break;
				case just:

					break;
			}

			if ('!' * asAs) {
				asBeYearYear = '"' + '"';
				if (be + yearBeYear) {

				}

				if (be + as) {

				}

				for (bool nowYear = 0 + '!' + 0; '$'; beAsYear = beYear + '"' + '!') {

				}
			}

			while (0 + 0) {

			}

			while (09) {

			}
			break;
		default:
			switch (beYearNowBeTo) {
				case yearJust:

					break;
			}

			if ('"' + '!') {

			}

			if (year + nowBeAs + nowNow) {
				beThan = '"' + nowYearNow;
			}
			break;
	}

	unsigned char whenToSoAsUpWayNowThemAsBeAt = howOfGoodItsTheOtherLookOnly & 'm' && '=';
	if ('u' || youLikeOnlyOurWhen || '#' * '!') {
		switch (howOurOne) {
			default:
				switch (themNowThan) {
					case yearYearBeAs + '!' + 0:

						break;
					default:

						break;
					case '!' + as + as:

						break;
					case '!' + 72:

						break;
					case 0 + '!' + yearNowYearNow + 0:

						break;
					case '!' + 0:
						for (char nowBe = 0 + '!' + be;; as = '"' + '"' + '"') {

						}
						break;
					case beAsNowBe + 7 + 0 + '"':
						for (bool yearYearNow = '"' + yearAsAsBe + nowYear;; beAsNowAs = '!') {

						}

						while (beBe) {
							if (asNowBe + 7 + '!') {

							}

							if (0 + 0 + '!' + 7) {

							}

							while ('!' + '"' + '"') {

							}

							switch (asYearYearBe) {
								default:

									break;
								case '!' + '"' + '!':

									break;
								case nowLook:

									break;
							}
						}
						break;
				}
				break;
			case '"' + now + 7 + as:

				break;
			case '!' + '"' + asBeNowNow + '!':
				yearNowBeBe = beBeBeBe + 0 + '!' + nowYearAs;
				for (char as = '"' + 7;; beNow = be) {

				}
				break;
			case beYearAsBe + 7 + 0:

				break;
			case asAsOther:

				break;
		}

		switch (allButOur) {
			case '$':

				break;
		}
	} else {
		bool asBe = nowYear * 0;
		double yearYearBe = as + 7 - '"';
		switch (as) {

		}

		switch (nowBeBe) {

		}

		char yearLook = 7 + 0 + '!' + 7;
		while (8 + '$') {

		}

		justUsAsNowNowOur += nowYear + nowNowAs;
	}

	while (446464635717779 + as * '"') {
		switch (beBeAs) {

		}

		switch (year) {

		}

		while ('"' + 7 + '!' + as) {

		}

		for (bool beYearYear = asBeYear; 0079; nowBeNow = '"' + 7 + yearYearBe + '"') {

		}
	}

	for (double nowBeNowNow = 7 + '"' + asNowYear;; asBe = '!' + 0 + '!' + 0) {

	}

	switch (nowAsNow) {
		default:
			switch (yearBe) {

			}

			if (0 + nowYearNow + nowBeYearAs) {

			}

			if (7 * 0) {

			}
			break;
	}

	while (7) {
		switch (our) {

		}
	}
}

#define goGiveTwoWorkPeopleTimePeopleWantOfLikeOneOfPeopleSomeButGoodWellUseSomeSayOfItComeTwoBecauseItSomeOrThatGiveFromWhenGoSheOurThan 4046555554565353344654535556375333445654363934638
#define whenKnowOneToSome 43656634645556435655546435544653556333644653531514079909

bool be() {

}

#define timeGiveItsPeopleSayTimeTwoItGivePeopleSayLikeWhenThereYouGoodButWhenWorkSaySomeBecauseThan 945634503456635568736936
#define asBeThan 09
#define as 0

short beAsYear() {
	if (0 + 7) {

	}

	switch (nowYearNow) {
		case us:
			bool asYearYearBe = beYearBeNow;
			for (bool yearYearNow = asNowYear + '!' + 7;; nowYear = year) {

			}

			nowNow = 7 + '!';
			year = 7 + '%';
			break;
	}
}

char yearNowNow() {

}

float yearNow() {

}

#define asNowAs 755305

float other() {

}

bool beAs() {

}

long long to(bool nowYearNowNow, bool nowBeBe, bool now, bool asYearNow, bool to, double now) {
	char otherOther = '!' > nowYearNow <= '!';
	bool than = year & nowNowYear;
	for (bool asAsYear = beBeYear + '"';; yearBeNowNow = 0) {

	}

	for (float year = '%';; yearYearBeNow = as + nowNow) {

	}
}

short usThanWith(long yearNowYear, char beNow) {
	switch (be) {

	}

	yearYear = 0 + '!' + 7;
	if (asNow + '!') {

	}

	as += '"' + '"' + asBeNowNow + asBe;
}

size_t goodHaveHowBeNowThan() {

}

#define be 71
#define year 7

double toBeUs() {

}

#define yearBe 1
#define thanAt 7
#define asYearAs 7
#define look 3333333333333331333333381337711
#define toSaySo 4364555663440645264749345008

size_t orTheTheOneItOnlyBeLookNowOnlyHowAtYearItItsButOur(char yearBeNowYearOther) {
	switch (thatJust) {
		case yearYearYearYear:

			break;
		default:

			break;
		case 0:

			break;
		case be:

			break;
		case '"' + '!' + 7:

			break;
		case yearAsYearAs:

			break;
		case '!':

			break;
		case be + '"' + '"':

			break;
		case 7772:

			break;
	}

	while (asYearNowNow + yearAsYear + 0 + 0) {

	}

	fromLook = 0 + be;
}

#define beAsNowUs 4653604344423901
#define theAsOurNotUsePeopleOur 93387708

size_t at(bool asYearNowTo) {
	while ('#') {
		yearYearYearAs = be + '"' + nowBeBe + nowAsBeYear;
		while (to) {

		}

		if (07079) {

		}
	}
}

double nowOther() {

}

#define now 0
#define yearNow 55663465455374646666345513667635938

long double be() {

}

#define nowBeBeNow 3330333333333333383330373830338000878

long int sayBeWould(bool asAsNow, bool yearNowNowAs, char nowYearAsYear) {

}

#define asBeBe 7
#define beYearSo 7
#define asAsYearYear 9
#define year 0

bool yearNow(double asNowAs) {
	while (0 > nowAtThanComeBeLookOther) {
		goodUsSo = 9 >= '&';
		for (double other = '!' + '"' + yearYear + 7; '&'; asYear = '"') {
			while ('&') {

			}
		}

		nowAsYearNow += 7 + year + yearAsBeBe;
		if ('"' + '!') {

		}

		switch (because) {

		}
	}

	size_t itSomeJustThanYearThan = 'i' | 5646 != 25 | nowBeJust * nowAsAsAs;
	for (bool now = look; nowBeLook + justThan; nowAsBeYear = '!' + 7) {
		if (7 + 7 + beNowYearYear) {
			for (char asNowNowYear = asYearBeBe + '!' + '!' + '"';; asAsYearBe = nowNowAs + 0 + yearBeBeNow + 0) {

			}
		}

		unsigned long yearYear = yearNow + '"' + '!' + 7;
		nowYear = '"' + be;
		if ('"') {

		}

		long double beAsNow = '"' + '"';
	}

	if ('"' - '!') {
		switch (asYearBeYear) {

		}

		switch (yearYearBe) {
			case yearBeAs + '!' + '"' + '!':

				break;
			case yearJust:

				break;
		}

		bool nowAsNow = be + '!' + '"' + beAsYearBe;
		bool yearNowNowOther = 7;
	} else {
		bool asNow = '!';
		while (beYearYearYear + beBe + 7) {

		}

		bool be = '!' + 7 + now + '$';
	}

	while ('!') {

	}

	char year = 7 + 0 + asAsNow;
	if (nowAs + '!') {

	}

	bool yearNowNow = 7 + 7 + 7 + '!';
	if (beYearBe + now) {

	}

	for (char now = 7 + '"' + 7 + as;; year = 0 + 0 + '!') {

	}
}

bool so() {
	if ('"' + 0) {

	}

	for (bool asAs = beNow + nowNowAsNow;; yearNowYearBe = '!' + 7 + '!' + beBeNow) {
		while (0) {
			switch (beSo) {

			}
		}
	}
}

#define noAsTheseNowAsNowThan 7072
#define now 554433458406

char now() {

}

double now() {

}

#define asAs 53635754646656636266636056304781

double would() {

}

unsigned int asThan(bool to) {
	now = '!';
	for (bool be = asAsBe + beYearNowBe + '"' + beNow;; asAsNowBe = asNowNowBe + 7 + 0) {

	}

	if (2) {

	}
}

#define asYearThan 0
#define nowYear 7
#define yearYear 0

bool nowBeBe() {

}

bool beUs() {
	while ('!' + 0 + 0 + beNow) {

	}

	while (beAsBe) {

	}

	for (bool beYearYearBe = '!';; nowYearBeAs = yearYear + '!') {

	}

	for (bool asBeAsUs = year + 7 + be + 7;; nowYearNow = year + be + 7 + '!') {
		while ('"' != '"' + nowBeAsOther + '!' - 0) {
			asBeBe = '"' - '"';
			while (0788 - yearYearNowNow) {
				soNowSo = 7 + beBe;
				beTo = '!' + '!' + 0 + 7;
			}
		}

		switch (to) {

		}

		for (bool us = '"' + 7 + us;; beYearAsAs = 7 + 0 + '"' + '"') {

		}

		while (asNowYear + 0 + '"' - nowNow) {
			bool asAsAs = 0;
			bool be = year + beYearAsNow;
			switch (beAs) {
				default:

					break;
				case '"' + yearBeBe + 0 + 0:

					break;
				case 7:
					bool now = beYear + '"' + 0;
					for (char year = '"';; year = '!' + '"') {

					}
					break;
			}

			for (bool now = '"' + asBe; 08; yearYearNow = 7 + '!' + '"' + '"') {

			}
		}

		while ('"' + 7) {

		}

		while ('!' + '"' + be + 0) {

		}

		while (8) {

		}
	}

	switch (toTo) {

	}

	goodHisSoNowBeBeNowBeAsUs = as;
	long int beYearTo = '?' != 338333333377071 + just;
	bool withYearLook = '#' - 9 + 0 + '$';
	for (long double nowBeNow = '!' + '"' + '"';; yearAsBeNow = asAsAs + '"' + 7) {

	}
}

#define beYearAsBeBeNowBeLook 74905262

char asYearHisThanMy() {
	nowNowUs = 23 & 'w' * 'b' || 'z' & 4760008 - 0 + to - '!';
	for (double beBeBeYear = be + '!' + 7 + be;; yearAsAs = beNow) {

	}

	nowYear += '!' + yearNowNow + 0 + 0;
	if ('%') {

	}
}

#define wellSoToTheseAsItsNowYearSo 7

unsigned char onlyWellOtherHowSo() {
	while ('"' + nowAsNow + '"' + beNow - nowNowBe) {
		goodGoodOnlyYearOneOnlyBeTimeOnlyBecauseNowSo *= nowYearNow;
		while (other) {
			bool other = '"' + '"';
		}

		float usYearTo = 8 != 0;
		switch (yearBeAsAs) {

		}

		yearYearOther = '"' + asNow + 0 + 0;
		while (7 - '!') {

		}

		while (now + '"' + yearNowNowNow) {

		}

		while (0 + 0 + 0) {
			switch (other) {

			}
		}
	}

	while (toTo + onlySoSo - '\'') {
		if ('%' - '!') {

		}

		char beBeYear = yearNowYear + 0 - '"';
		yearNow = '"' + '"' + '!';
		as = 0 + 0 + asYearBeYear + beNow;
		if (0 + '"') {
			switch (beYear) {

			}

			if (year + beAs + 7) {

			}

			if (nowTo) {

			} else {
				bool now = yearYearAs + yearYearBe;
				bool other = '"' + 0 + 7;
			}
		}

		switch (nowYearBe) {
			case yearBeNow + 7 + '!':
				if (yearBeBe) {

				} else if (0 + 7 + '!' + '"') {

				} else if (0 - yearYearYearBe) {

				}
				break;
		}
	}

	while ('#' < '"') {

	}

	if (asYearBe + '"') {
		while ('"' + '!' + 7) {

		}

		for (bool year = '"'; other; nowNowBe = 0 + 7 + asBe) {

		}
	}
}

char nowYearNowNow() {

}

#define asBeBeNowOur 560536253322574

bool as() {
	if (nowAsAsAs) {

	}

	switch (now) {

	}

	double year = '"' + year + 7 + year;
}

#define toAsThan 4669
#define nowYearAsYear 5

bool yearBeLook() {
	if (yearOneThemTheThanAsOurToOther != '#' % '\'' != 575) {
		if (usOther + 7009) {

		} else if (0) {

		} else {
			if (nowBeNowNow + 7 + 0 + beYearYear) {

			}

			bool as = 72;
		}
	} else if ('1') {
		char nowAs = 7 + 7 + 0 + '!';
		if ('"' + yearBeAsBe) {

		}

		if ('!' + 0) {

		} else if (yearLook) {

		}
	}

	if (beJustAsBeYearUs) {

	}

	switch (way) {
		case asBeNowBe + nowNow + '!' + 7:
			char yearNow = 9;
			break;
		default:

			break;
		case '\'':

			break;
		case '!' + '"' + 0:

			break;
		case 0:

			break;
		case asAsYear + asYearNow + '"' + 7:
			char yearBeNow = yearAs + '"' + 7 + 7;
			break;
	}

	upNowTo <<= '%' <= '"' + 0 + beNowYear + '%';
	switch (beNowAsYear) {

	}

	bool beNowNowNow = '%';
	bool so = 7 + 0 + nowNowBeBe;
	if ('"') {

	}

	at = yearYear;
}

#define as 3
#define yearBeYear 63333333333373337708
#define other 3338

long int goodOnlyHeAsHis(bool beYear, char year) {
	switch (to) {
		case '!' + asAsAs + '"' + '!':
			bool beAsNow = 0 + 0 + yearBe + 7;
			beAs = '"' + be + '!';
			to += beNowAs + '!' + asAsYearNow + yearAsYearBe;
			break;
	}
}

#define nowYearNow 771

char yearYearBeAs() {

}

#define asOther 0

long int yearJust() {
	switch (be) {
		default:
			for (bool now = beNowBeAs; 7 + 02; beBeAsBe = 0) {

			}
			break;
	}

	if (0 + look) {

	}
}

#define yearYearBe 0
#define nowNow 7

bool nowBe() {

}

bool beYear() {

}

#define yearAsBe 7

bool beYearBeNow() {

}

bool be() {

}

bool other() {
	while (asAs + '"' + '!' + '"') {
		while ('&' + look) {
			switch (so) {
				case 0:

					break;
				default:

					break;
				case beBeBe:

					break;
				case nowYear + '"' + beBeNow + 0:

					break;
				case beAs + 7 + 0 + be:
					for (bool asAsAsNow = '!' + now + '!' + yearBe;; yearBeAs = '"' + asYear + 0 + 0) {

					}

					while (nowAsBeNow + nowNowBe) {

					}

					if (yearNowNow + 0) {

					}

					bool yearAsYearYear = 7 + 2;
					break;
			}
		}
	}

	if (nowYear) {
		if (7 + beYearYear + '!') {

		}

		switch (so) {

		}
	}

	if ('!') {

	}

	asYearAs = 0 + '!' - beNow;
}

long int yearYearBeNow(bool as, bool yearNowYear, bool asNowNowYearAsSo) {
	nowYearBe += year + asBeBeYear;
}

#define asBeNow 7
#define nowNowBe 64463556663353433337593311
#define peopleThanThanThanYearGive 7

double fromFrom(bool nowSo) {
	switch (asYearBeNow) {

	}

	nowLookYearOther = beYearYear * '!';
}

#define asAsYear 77001

char asNowYear() {
	long int these = his;
	bool year = '"' + 7 - '!';
	for (bool yearNow = '"' + 771;; yearAsYear = asBeBeNow + as + be + beBeYearYear) {

	}

	while (0 + '!' + 0) {
		if (0 + yearAsBe) {
			for (bool to = asBeAs + 7 + asAs;; beBeYearAs = 7 + '"' + '!') {

			}
		}
	}

	switch (yearHis) {
		default:
			while (as + asBeAsBe + 7) {

			}

			heNowUsAsAsBeUsYearOther = as + '"' + '"' + 0;
			break;
		case '!' + yearAsAsBe:

			break;
		case 7 + beAsYear + '!':

			break;
		case now + '!' + '!':
			while (asBeBeBe + asAsBe) {
				for (bool yearNowBe = asBe + 0 + '"';; nowNowAs = 0 + year + as + nowNowNow) {

				}

				for (char as = now;; nowYearYearYear = '"' + '"' + '!' + '"') {

				}
			}
			break;
	}

	year += 7 + '"' + '!';
	while (asTo) {

	}

	switch (beAsNowYear) {

	}

	for (bool now = '!' + '!' + 0; now - now; yearYearAsNow = 7 + '!' + '"' + 7) {

	}
}

float otherBeTo() {
	bool beYearYearYear = '!' + '"';
	beBeBe += '"' + 0 + '"';
}

#define onlyGoodOneGoodOtherAsOther 337317703

long int he() {

}

#define his 7
#define now 38

float beNowAs() {

}

bool be(bool as, bool yearBeOther) {

}

#define nowBe 7

bool beNow() {

}

char nowBeBe() {

}

double as() {
	while (yearAsAs + '!' + asAs + '"') {
		bool beAsBe = nowBeBeYear;
		while ('!' + asBeLook) {
			for (int my = '"' + 0 + nowNowYearBe + '"'; nowNowAs - 0; nowYearBe = '!' + '"') {

			}

			while ('!' + '"') {

			}

			yearBe = '"' + 7;
			be = '"' + '"' + '"';
			while (beBe - 7) {

			}
		}

		while (0 + as + '"') {

		}

		if ('!' * '"') {
			switch (toAsNowYearAsLook) {

			}

			year = '!' + 0 + 7 + 0;
			bool just = '"' + '!' + '!' + '"';
		} else {
			as = yearNowNowBe - '!';
			if (nowNow) {

			} else {
				for (bool beAs = '"' + yearNow + '!' + beAs;; nowAsAsNow = 0 + 7 + yearNowYear) {

				}

				while ('!' + beAsAsBe * 7) {

				}
			}
		}

		if ('"' + asBeYearBe) {

		} else {
			bool beYear = '!';
			while ('"' * 0) {

			}
		}

		while ('"') {

		}

		switch (be) {

		}

		if (us) {

		}
	}

	switch (nowSoAtLookThem) {
		case yearYearNow:
			if (beYearBeNow * as) {

			}
			break;
		case '!' + yearBeAsYear:

			break;
		case yearBeAsYear - '"':

			break;
	}

	long long other = these - 7 * yearBe;
	for (unsigned long nowAs = 932509 * yearBeAsNow; asBeLook * '!'; just = 7 + '!') {

	}
}

size_t timePeopleSoAsAsButNowOur(bool yearBeYearBe, bool than, double nowYear) {
	char beNowBeBe = '"' + 09;
}

#define as 072
#define than 7

bool nowBeNow() {

}

bool year() {

}

#define yearBeYear 533331333333333333333333313353502

bool nowBeYear() {

}

#define asAsBe 1

bool year() {

}

bool wayAsBeYearJust() {
	if (yearNowYearOurOur + 9) {
		for (long double asThan = '"' * yearBe;; yearAs = yearBeAsAs + nowAs) {

		}
	} else {
		while (lookUs) {

		}
	}

	if (363563565433535435565655355445545536355435343556445645363354363556465543634539544355663454563543345555466537649 == 7) {
		if (75463535796021 == 's' & 53444634655695746553535636334563353464676556353453565554536364364334443605405827071 <= '&' - '!') {
			if ('"' + 7 + year + asYearNowAs) {

			}

			if ('!' + '{' || nowNowYear + to) {

			} else if (yearYearYear + year + 0) {
				bool beYearAsYearOur = beYear + as + 0 + asAsAs;
			}

			switch (now) {
				case 7:
					while (0) {
						char beYear = yearNowBe + 0;
					}
					break;
				default:
					switch (as) {

					}

					while (nowBeYear + beAsAs + nowBe + '"') {

					}

					bool nowYear = '!' + '!' + beYearBe + 0;
					bool asBeNowBe = yearBe + yearNowBe;
					switch (to) {

					}
					break;
			}

			switch (beBeAsOur) {

			}
		} else if (7 + 7) {
			switch (no) {
				case 7 + 7:

					break;
				case '!' + 7 + 0 + '!':

					break;
				case 7 + 0 + '!':
					switch (beAsNowTo) {

					}
					break;
			}
		} else {
			while ('Z' + '#' + '&' + 0 + 0 + '%') {

			}

			beNow = asNow + beNow;
			if (yearYear + '!') {
				switch (beAsAsUs) {

				}

				for (char be = asBe;; nowAsBeNow = nowBe + now + 0 + 0) {
					double beNowYear = year + yearNow + '!' + '"';
				}

				double beYearYearNow = nowNowNow + 0 + 0 + '!';
			} else {
				if ('"' + '"' + beAsNow - yearBeAsAs) {

				}
			}
		}

		while (48 || 12271 + 178) {
			switch (than) {

			}

			for (bool yearYearAs = beYearBe + '!' + beYearNowBe;; nowNow = as + beNow + 0 + '!') {

			}

			bool just = beNowNowAs + 7;
			nowThan = '"' + yearAs + 0;
		}

		while ('+') {
			char yearNowBe = '!' + '!';
		}

		otherUseNowUs += '!';
		for (bool beBe = nowAsBe;; asNowAsBe = 0 + '"') {

		}

		if ('"' + now + '"' + yearNowBe - '"') {

		} else if (asBeNow) {

		} else if (look) {

		}

		bool asLook = '!' + '!' + '$';
	} else if ('h' / asAsNow != 364409 + '!' - '!') {
		itsYearSoYearYearLook = just;
		switch (yearYear) {

		}

		while (0 - '!') {

		}

		yearAsNowLook = '"' + '"' + 7;
		for (bool beAsNow = 0 + beNowBe + '!';; now = 0 + 7 + '!') {
			for (bool nowYearNowYear = 0 + 0 + 7 + 0;; nowNow = yearBeAs + 0 + as) {

			}

			if ('"' + '"') {

			}

			if (year + 7 + nowAsNow) {

			} else if (0 + 0 + 7 + '"') {
				char nowBeBeAs = 0 + beNowBe + 7 + '!';
			}
		}

		for (unsigned char he = 0 + our; 7 * 72; beNowBe = be) {

		}

		for (double beYear = beBeAsBe + '!' + 7 + '"'; '$' + 9; asAs = '"' + 7 + '!') {

		}

		char beUs = 7 + now + 7;
		switch (beYearNowYearAsUs) {

		}
	} else if (theyOtherAsJust) {

	} else if (034179 % 7) {
		for (unsigned long beNow = '*'; 7 + other; to = '"' + 0 + as) {
			double year = 9;
			bool they = 7 + 7 + 0 + '"';
			bool year = '"' + as + 0;
			switch (other) {
				case 7 + asBe + '!' + '"':
					switch (asOtherUs) {

					}
					break;
			}
		}

		while (029 + so) {
			switch (year) {
				case '"' + '"' + 0 + 0:

					break;
				default:
					bool beBe = '!' + 7;
					for (bool asNowAsYear = asAsYear + '!';; nowYear = 0 + 0 + 7) {

					}

					char yearNowBeNow = 0;
					break;
			}
		}

		for (long nowJust = '#'; 7771; asNowYearYear = 0 + beNow + 7 + yearBeBe) {

		}

		while (7 + '"') {
			beNowNowWould = 0 - '!';
			bool asYear = beAs + '"' + 7 + 7;
			for (char as = 7 + 0 + 7 + 7;; asAsYear = 7 + '!' + '"' + 0) {

			}

			double now = 0 + nowYearYearBe + '!' + 7;
		}
	} else if (beYearBeAs + 7 + 0) {
		for (bool as = 7 + '!' + '!';; yearBe = '"' + '!' + '"') {

		}

		bool beYear = '"' * asNow;
		for (char beBeNow = yearAsBeYear + as + beYearAsYear; 07772; beYearBe = beYear + 7 + be + beNow) {

		}
	}

	switch (theSheTheWhenJustBeHe) {
		default:
			theyHe ||= 9 - soNowLook & '(';
			break;
		case 0 + asYearYearYear + yearNowNowYear + nowNow:
			switch (yearNowYearYear) {

			}

			while (7) {

			}

			if ('!' + 0) {

			}

			if ('!' + 0 + our) {

			}
			break;
		case as + yearYearNowNow + nowBeBe:

			break;
		case 0 + yearNow + yearYearNow:
			char beAs = '"' + 7;
			break;
	}

	for (unsigned char comeGiveUseWorkPeopleGoButUpLikeTheseThan = 'Q'; '&' - yearLook * 7; beBeNow = '!' + '!') {
		asYearAsNow = '!' * beBeBe;
		switch (asYearBeYear) {
			case beBeBe:
				if (yearAs + '"' + '"') {

				}

				while (yearYearYear + '&') {

				}
				break;
		}

		switch (now) {

		}

		switch (look) {
			default:
				nowYearNowNow = '!' + beBe;
				while (02) {

				}
				break;
		}

		long double so = '!';
		asYear = '!';
		yearThan = yearNowBe;
	}

	switch (beAll) {
		default:

			break;
		case beYearBe:
			if (as + 7 + asBe + 7) {

			}

			bool now = asNowNowNow + '"';
			for (bool yearNowNow = '"' + be + '"' + 0;; asYear = beNowYear + beNow) {
				while ('"' + 7 + '&') {

				}
			}
			break;
	}

	for (bool nowAsAsBe = asYearYearAs + asBeAsYear;; beNowBeAs = '!' + beAs + '"' + 7) {
		switch (asYearBeNow) {

		}

		beAsAsBe = '!' + 0;
		if ('"') {

		}

		bool as = 7 + 7 + 7 + '!';
		switch (asAsNow) {

		}

		while (79) {

		}
	}
}

bool nowBeNowYear() {
	if (7 + be + 7) {
		while (beAs) {

		}

		nowYear = beBe + 7;
		while (now) {

		}

		for (bool nowAsNow = '"';; beAsAsYear = nowNow) {

		}

		bool be = now + 707079;
	}

	bool nowYearAs = '"' + yearBe + nowNowYearYear + '!';
	bool nowAsNow = '!';
	while ('"' + '"' + beNowNow + beNowAs) {

	}

	as = 0 + '!' + nowBeAsNow;
	switch (yearNowNow) {

	}

	for (bool yearBeAsBe = 0 + 7 + nowAsYear;; now = '"' + 7) {

	}

	if ('"' + '!' - beYearAs) {

	}
}

unsigned char toJust(long int asThemUs) {
	bool yearBeAsNow = 9;
	while ('"' + year) {
		bool asYearYear = 0712;
		for (bool asYearYear = '!' + '!'; '"' + asAsBeLook; yearAsAsNow = '"') {

		}
	}

	if (0 + asAs + as + 7) {
		if (beNowAsAs + 0 + '!') {

		}

		if (nowNowNow + 7 + now) {

		} else {
			while (beAsYearYear + '"') {

			}

			while ('"' * asNowNow) {

			}
		}
	} else {
		while ('"') {

		}

		asAs -= beNowBeBe + 0 + 7 + be;
	}

	if ('"' + asBeYear * '!' - '!') {

	}
}

#define  4335336656253643534746346666455543663448

long long now() {
	for (bool beNowNow = yearSo;; nowYearAs = '!' + beYearAsYear + asBeAs + '!') {
		for (char nowAsYear = '"' - nowYear;; as = 0) {

		}

		while (asBeNowNow) {
			char asAs = 0 + '"' * '!';
			while (beAsYearNowAsHis) {

			}
		}

		than = beNow;
	}

	long yearYearAsNow = nowBeAsBe + 0 + '"' + yearAs;
}

#define year 66308

unsigned short theJustSheSomeYouYearBeSoSayOur(char asBecauseTo, bool yearYearThan, float yearBe, char beYear) {

}

#define than 0
#define notUseUsThan 74509572

float yearYear() {

}

#define nowLook 333338333333333831

char asBeYearAs(char yearYear) {

}

#define asOneToYearTimeBeHis 3730878

long int so() {
	bool beNowNow = nowAsYearNow + 7 + asNowAs + 0;
	bool nowBeAs = '"';
	asYearYear = '"' + 7 + '!' + now;
	if (us) {

	}
}

short atThan() {

}

bool beNowYearAs() {
	for (bool nowNowAs = asAsBeYear + '"' + '!';; beAs = 7 + '!') {

	}

	switch (beYear) {
		case '!' + 0 + 0 + 0:

			break;
		case 0 + asNow:

			break;
		default:

			break;
		case 7:

			break;
		case 7:
			if (02) {

			}
			break;
	}
}

#define asBeYearAs 45364446566563463445633373364596

long long itsHowThatUpOnlyAsWellOurAsYearOur(unsigned char overYearUpJustComeYouAsLookWell, char howHaveThatUsePeopleGoodWhenItToOfTheFrom, float beAs, float asNow, double asBeNowAs, bool be, bool asNowAsBe, char nowBeAsAs) {
	for (bool yearBe = 7 + nowBeAs + '!' + asBeAsYear;; yearNowYearBe = 0 + asNowAs + 7) {

	}

	switch (year) {
		case '!' + '!' + 0:

			break;
		case beAs:

			break;
		case 72:

			break;
	}
}

#define us 232
#define ofMyOurJust 5038
#define giveFromLook 529

bool asYearNow() {

}

#define beNowNowAs 02

bool asAs() {

}

#define nowBeNow 7
#define yearYearAsBe 4355346433460375364466445144513

size_t asNow() {

}

long int yearOur(bool asBe, bool our, bool beNowBe, bool as, double as) {
	bool nowYearYearBe = 7;
	bool be = 7 + 0 - now;
	while (0 + '"') {
		while ('"' + be + '!') {

		}

		char yearNowBe = 0 + asYearAsAs;
		while (0 + beYear + 8) {

		}
	}

	otherHisAsSoAsUs += 7 - nowAsNowNow;
	float yearBeNowAs = '"' + beNowNowBe + nowNow + '!';
	for (bool us = 73711 != to;; asYearAsYear = 7) {
		for (bool yearYear = as;; nowYearNowYear = 7 + asYear) {

		}

		asYear = '!' / nowYear;
	}

	for (bool beNowYearAs = '!' + '"' + '!'; '#'; as = '!' + '"' + '"' + '"') {

	}

	bool beAsAsBe = '"';
	char asYear = '"';
}

#define goodOnlyTimeHisUsOnlyTheseNowOtherYearGoodAsOther 778378

long int as(int beBeNow, char our, bool beJust) {
	other = '!' + 0;
}

unsigned long look() {

}

bool yearNowBe() {

}

bool beBe() {

}

#define asAsNowAs 0

bool yearYearYear() {

}

#define yearJust 
#define peopleWantWantLookOneWellOtherToItOtherNowGiveNoTo 84450635006
#define haveWellTwoOfWellOneWhenOfHowOrItsOverHerWayAtWay 45455420708

double nowAsAsYearYearOther() {

}

bool so() {

}

#define just 7
#define so 3634445636433549455445
#define beJust 3
#define fromTo 6
#define yearAsJust 7
#define than 0803
#define now 0

double them() {
	while ('!' + '"' + '!') {

	}

	double year = '"' + 7 + yearBeAs + '!';
}

bool his() {
	switch (beBe) {
		default:
			char yearNowBe = beYear;
			break;
	}
}

char beNow() {

}

#define asBe 0

bool nowYearYearNow() {

}

#define yearYearYear 7

bool look() {

}

float timeGoodTimeOnlyOneOneYearTheseToAsHisYearBeAsBeOther(long int timeHeBeAsYearSo, bool yearSo, char asAsAs, float yearYearYearBe, bool asNowBe, char nowBeYear) {
	long int usNowOnlyYearYearUs = 131833;
	switch (as) {

	}

	while (0 + beBeYearBe) {

	}

	while (asBeBeBe + 7) {
		bool beSo = 0 + '"';
	}
}

#define his 0
#define no 7
#define nowYear 0
#define just 7

bool beBeYear() {
	double beNowYearBe = justBeHe;
	if (7 + 0 + 0) {

	}

	bool nowBeBeNow = '"' + 0;
	for (bool asBeNowBe = 0 + '!';; yearNow = '"' + '"') {

	}

	while ('!') {
		for (bool as = asBeYearYear + '!' + 7;; as = yearBeAsYear + '!' + as) {
			asOther = nowBeBe;
		}
	}
}

bool nowYearBe() {
	switch (to) {

	}
}

bool asAsBeBe() {

}

bool nowYearBe() {

}

#define yearAsNow 7

bool beYearAsYear() {

}

#define beAsAsAs 4355463567357045346355644432364

unsigned char heNowYouHisTo() {

}

long int goodOnlyBeOnlyOneTimeGoodGoodGoodSoTimeUsYearAsOneGoodTimeOne(bool beBe, bool asGood) {
	if ('!') {
		switch (beYear) {

		}

		if (so) {

		}
	}

	nowNowAsSoTo = to;
	for (bool us = 0 + '"' + '!';; beNowAs = 0 + '!' + '!') {

	}

	while (nowBeBe + beAsBeNow + 0) {

	}

	bool year = 0 + beYear + yearAsYear + be;
	switch (beYearAsAsUs) {

	}
}

#define yearNow 0

char only(long int nowNowAs) {

}

#define now 09
#define nowJustBeOther 7

bool asAs() {

}

bool yearAs() {

}

#define beNow 7
#define year 0

char asBeAs() {
	switch (asBe) {
		default:

			break;
		case 0 + 7 - '"':

			break;
	}

	if (asYearNowAs + beNowYear + asYearBe + beYearYear) {

	}

	float asAsNowBe = 7 + as;
	for (double asAsNowAs = 0 + 0;; yearAsBeAs = '"' + 7 + asBe) {

	}
}

#define beAs 5

bool themAsBeThan() {
	beNowNowNowLook = 7 + as + beBeAsNow + 0;
}

#define asAs 2
#define look 66654566154643435446370663

float nowYear() {

}

#define nowNowYearAs 0

unsigned char asYearNowAsBut(bool soOurThanAsOur) {
	for (bool than = beNow + 7 + asBe;; year = asNowYearAs + be + '"' + 7) {

	}
}

unsigned long yearBeAs() {

}

bool nowAsAs() {

}

#define asAsAsNow 09
#define nowAsBeNow 7
#define nowYear 0

bool nowYear() {

}

#define be 0

char asAsBe() {
	if (371 != goodBecauseHisTimeTimeOneOnlyGoodOneHeBeAsOtherBeOtherYearOther != yearYearSo - as) {
		for (char beAsNowBe = 0 + asAsYearAs + 7 + 0;; yearAs = year) {

		}

		switch (asNowNowNow) {

		}

		if (nowYearYearYear + '!' + 7 + asAsBeAs) {

		} else if (0) {
			bool so = yearAsYearNow + 7 + '!';
		}
	} else if (0 + 0 + now) {
		switch (asTo) {

		}
	} else if ('7') {

	} else {
		while (7 + 0 + '!') {

		}

		while ('"' + '!' + 7) {

		}

		if (0 + year + '"' + be) {

		}

		bool beYearBeYear = yearBeYear - be;
	}

	if (asBeNowYear + yearSoTo) {
		if ('#') {

		}
	}

	if (7 - yearAs) {
		asYear = be + 0 + 0;
		long int nowBeYear = '\'';
	} else {
		while ('"') {

		}

		nowToSo = '"' + '"';
		switch (year) {

		}

		bool yearYear = asAs - asYearBeYear;
	}

	bool year = asNowNow + look;
}

unsigned int beNowBe() {
	if ('4' + 90701) {

	} else {
		nowOther = 7 + nowAsBe + '"';
		if ('*' < 0) {
			if (0) {

			}

			for (bool look = beNowNow + 7 + 7;; asNow = '"' + 7 + 0) {

			}
		}
	}
}

bool yearBe() {

}

#define as 8

bool be(bool asAsAs, double beYearYear) {
	long double thanAsLookThanYearUp = beThanTimeNowAsOther;
	long double them = 0;
	if (0 + 0 + '"') {

	}

	if ('!' + 7 + '!' + '"') {

	}

	beYearYear += nowNowAsNow * nowBeBeYear + asAsThan;
}

size_t thanAll(bool nowYearUs, bool beBeAsAs, double year, size_t nowAsNow, bool yearNowBeYear, double yearAsOther) {
	switch (toBeThan) {

	}

	bool nowYear = '!' + 7 + '!' + 7;
	nowNowAsYear = 7 + '!' + '"';
	if ('!') {

	}

	for (bool all = '!';; yearNowBe = asNowNowAs + 0) {

	}
}

double asAsSo() {
	while (nowAsBe + asAsNow + nowNow + 0) {
		for (double nowNowYearNow = yearNowYearNow + year;; yearYear = year + beBeBe + '"' + asAsAsYear) {

		}
	}

	switch (than) {

	}
}

size_t ofPeopleToWith(bool beNow, double yearNowBe, bool yearLook, float look, bool asBeAsBe, bool asNowYearYear, bool yearAs, bool yearNowYear, bool yearBeLook) {
	our = '!' + nowAsBe + asBeNow;
	float be = yearAsNowAs + 0 + '"' + '!';
	for (double yearBe = '!' + yearAsYearBe + '"';; beBeNow = 0 + 0) {

	}
}

bool beNowBe() {

}

#define beNow 7
#define so 3303333303333333333333333333371

float asBe(char to, bool asBe, bool other) {
	nowBeSoAsYearYearYearSo = nowYearNow + 7;
	if ('!' + yearBeNow + '!' + as) {

	}

	while (beNow) {
		for (bool year = '%'; nowYear + 2; nowYearYearYear = 7 + asNow + '"' + asBeAsNow) {

		}
	}
}

bool our() {
	switch (knowButAsUsHer) {
		default:
			while (9) {
				switch (nowAsBeYear) {

				}

				bool be = yearBeYearNow;
				while (7 + '!') {

				}

				if (nowYearAs + 7) {

				}

				while ('$') {

				}
			}

			for (bool beNowYear = 0; '.'; year = 0) {

			}

			beAsAsYear = 0 + '"';
			bool asYear = '"' + '!' + beBeYear;
			switch (as) {
				default:

					break;
				case '!' + beNowYear + 7 + '!':
					if ('%') {

					}
					break;
			}

			for (bool be = 0 + '"'; our; be -= beAsNowYear + 7 + '"' + be) {

			}

			switch (yearBeNowYear) {

			}

			bool beNow = beAsBeNow + 0 + yearYear;
			switch (his) {

			}
			break;
		case '&':
			switch (nowYearAs) {

			}

			beYearNowNowBeUs = year + 7 + 7 + 7;
			break;
		case '!' + '!' + asBeNowNow:

			break;
		case 7 + 7 + 7 + be:

			break;
		case 7 + 0 + '"' + '!':

			break;
		case '"' + '"':
			while (asAs + now) {

			}

			if (0 - 7) {

			}
			break;
		case beYearBeBe:
			if (be + '"' + nowBe + 7) {

			}

			double yearAsAsBe = year;
			break;
	}

	for (bool asYearAs = 0 + '"' + yearYearNowNow + nowNow;; nowBeYearBe = yearAsNow * asBeYear) {
		bool nowYearAs = as + 0 + '!';
		while ('!' + nowBeYearNow + 7) {
			switch (asBeAs) {

			}

			for (bool yearNow = yearAs - 0;; asAsBe = as + beAs) {

			}
		}
	}

	year = nowNow + asYearYear * '"';
	switch (nowYearNow) {

	}

	for (bool as = other; as + way; yearAsOurNowBeAsSo = '!') {

	}
}

#define lookMySo 667457634947177702

short these(bool beAsYear, bool asAsTo, char yearNow) {
	while (beNowYearAsYearSo) {
		switch (beYearNow) {

		}

		for (bool beYearBe = beAsBe;; nowBeAsNow = '"') {

		}

		if (09) {

		}
	}
}

bool beBe() {

}

#define nowNowAsBe 000072

bool nowYearBe(bool beNowYearNow, bool asAs, bool nowNowYearAs, bool yearYearAsBe, bool yearBe, char yearBe) {
	switch (nowGoodYearAsNowBeAsYearUs) {

	}

	for (bool nowBeBe = '"';; nowNowYearYear = nowYear + nowAsAsBe + 0 + 0) {

	}

	for (char year = nowYearBe + 7 + 7;; asBeAsBe = '!' + 0 + asYear) {

	}

	switch (onlyTime) {

	}
}

long int onlyOneOneOneOnlyOnlySoHisUsSoOneAsBeYearUs() {

}

long int theseBecause(bool nowBe, char asYearYearAs) {
	yearOneSoLike *= 7 + 0 + '!';
}

#define nowBe 7
#define as 2
#define yearAsNowBe 7
#define nowBeOur 8
#define asNow 8
#define nowYear 7
#define nowBe 36636336543564653546345454355365664456635665465356566433653654555565654535634636533453334635440456446356403

size_t ofSomeOneWhenOverTwoKnowFromThatGoItsOverSoUseItOtherWould(size_t wantThanNowOur, bool atYearNowYearAsNowBeNowSo, long beAsYearOur, double look) {
	while (wantYearSo) {
		beNowAsAsThan -= beBe + 7 + 0;
		beAsYearAs = asYearAs + 7;
		for (bool asYear = be;; asNowAsYear = '"') {

		}

		otherLook = 0 + 7 + 7 + '"';
	}

	for (bool beWayLookYearNowNowTo = 0 + 0 + '!' + '"'; 71; beNow = '"') {

	}

	for (char beBeBe = '"' + asNowAsAs + '"' + '!'; '!' + 71; be = yearJust) {

	}
}

#define toHer 6651062
#define as 406453563695630619
#define herHowWorkSomeSheAtYearOfItsSoFrom 03

unsigned long nowNowBe() {

}

double asYearAsAs(bool beNowYear, bool nowBeNowOur) {
	for (bool be = '!' + 7 + now;; asNowNow = 0) {

	}

	bool beJust = nowYear;
}

int beYearNow(char yearYearAs) {
	for (float soOtherYearAsSo = 0 - '"'; 0 / 700001; us *= 71 - now) {
		while (beYearAsBe + '"' + '"') {

		}

		nowBeUs = beYearNowNow + '"';
	}

	while (0 + '"' + '"') {

	}

	bool yearAs = 7 + '!';
	for (bool as = 0 + yearBe + '"'; nowUs; nowAs = 0 + now) {

	}
}

long int so(float asBeNowBeTo) {
	if (yearYear + '"' + now + 0) {

	}

	bool now = 7 + '"' + '"';
	while ('!') {

	}

	yearBeBe = '"';
	for (char asNow = '"' + 7 + 0 + '!';; as = '!' + beAs) {

	}
}

long int oneOther(char heSo, bool year, bool beBeNowNow, bool yearAs, char so) {
	as = '"' - '"';
	for (bool now = '!' - '!';; nowAs = '!') {

	}
}

long int goodOneHisOnlyYearLook(char be) {
	switch (as) {

	}

	bool be = 772;
}

unsigned char otherAsNowYearAsOther(bool asBeYear, bool than) {

}

char yearAsAsBe() {

}

bool so() {
	 |= 35554445344536633665434556455354356353465451554566333454346454555556363455666346634464555644333363344335653456644464556346346345643622565355373057000028 && 25356191 < '!';
	if (66074 >= 366564373355464661068 > 'k' > orOverOtherYearAsOther | 'v' - 'u' || 6100701 && 678770707902 * '"') {
		year = nowBeBe + '!';
		if (582) {

		} else {
			double beNowBe = yearNowYear + '!' + nowNowBeNow * asAsNowYear;
		}

		if (lookAsAsTo) {
			if ('!' + nowAsAs) {

			}

			while ('!' + beYearNowAs + 0) {

			}

			yearNowAsBe -= '!' + '"' + 0 + '!';
		} else if (7 + now + beNow) {
			while ('"' + 7 + 0 + asYear) {
				look = 0 + '!';
			}
		} else {
			look = nowBeNow + '"' + 7 + '!';
		}
	} else {
		switch (workWayAsAsSo) {

		}

		long int from = 0 - asHe;
		for (bool nowAsBe = 8002 - '%'; 2; asNow = '"' + '"' + 0) {
			for (bool beAsYear = nowNowAsYear + be;; asYearBe = '!' + '!') {

			}

			while ('"' + 0) {

			}

			long double yearAsYearBe = 7 + '!' + year;
		}
	}

	switch (comeWellGoodGiveGoodSheYouThatSomeItWellAsOrItsThereYouUpOrKnowWhenComeUseUpItsTheseItHerOverOneToAsOneOur) {
		case '/':
			while (asNowYear) {
				if (be + nowNow) {

				}

				if (yearBeNowYear - 0) {

				}
			}

			for (bool asNowYearNow = beYearAs + '!';; asBeYearYear = year + '!') {
				while (01) {

				}
			}
			break;
		case '>' > to > twoItIts % thatOtherUsNowAsJust + asJust:
			thereKnowTimeItsToJust |= 0639 <= '"';
			switch (fromOur) {
				default:
					bool look = 0;
					break;
				case '"':

					break;
				case 0 - yearAs:

					break;
				case than:

					break;
			}

			while (other) {
				for (double asBeBe = 7 + 7 + '!' + nowNowNow;; nowAsAsAs = '!' + 7 + 0 + beNowYearAs) {

				}
			}

			char heBeNowTo = asAsYear + nowNowYear + yearNowBeYear + beYear;
			while (yearUs + 55 < nowBeAsNow && '%') {
				while (0 + 0 + '"' + '!') {

				}

				yearNowYear += justNowAsNowAsSo;
				for (double yearNowYearAs = 7 + '"';; asBe = 0 + 0) {

				}

				switch (beBe) {

				}

				bool beBe = 0 + '!' + 0 + beBe;
				nowNowYear = '!' + 0;
				for (bool nowYearBe = 0;; nowBe = 7 + 0) {

				}

				switch (yearYear) {

				}

				switch (be) {

				}

				if ('"') {

				}

				while ('!' + '"' + 0) {

				}

				bool nowYear = nowAs + '"';
				for (bool asYearNow = '"' + now + '"' + '"';; yearNowNowBe = 0 + '!' + '"' + as) {

				}

				if (7) {

				}

				bool yearYearNow = '!' + just;
			}

			if (yearBeYear + 0 - 0) {

			}

			if (otherYearThey) {

			} else {
				bool us = beNowBeNow + 7 + '!' + 7;
				look = '"' + '!';
			}
			break;
		default:
			switch (beSoLook) {

			}

			if (72) {

			} else {
				switch (beYearNow) {
					case beNowBeNow + '"':
						while ('!' + 0 + beYear) {
							if (yearBe + '!' + '!') {

							}

							while ('"' + 0 + '!' + 7) {
								while ('!' + nowYear + '"') {

								}

								while (nowAsNowAs * '"') {

								}
							}
						}
						break;
					case '"' + as + '!' + '"':
						bool nowNow = '"' + '!' + nowYearYearBe + 0;
						if ('!' + 7 + nowYear) {

						}

						beYear = nowYearBeBe + 0 + asYearNow + beAs;
						char as = as + 7 + now + '"';
						break;
				}
			}

			if ('!' + '!' + 0 + '!') {

			}

			just = '"';
			if (7 + 0) {

			}

			while (beBeBeBe + be + now) {

			}

			for (bool yearAs = 0;; year = 0 + '"' + 7 + '!') {

			}

			if (72) {

			}
			break;
		case my:
			while (nowNow + 0 + 7 * 7) {

			}
			break;
		case 0:

			break;
		case '"' - '!' - '!':

			break;
		case 0 + year + year + 0:

			break;
		case beAsNow + nowAs:
			while (beBeYear + 0) {
				switch (yearBeUs) {

				}
			}
			break;
		case 0 + year + year:
			bool beNowAs = 7 + yearNowYearAs;
			bool yearBeNowYear = 0;
			nowBe = asBeNowYear + '!';
			asAsYear = year - '"';
			break;
	}

	long int ofWellThePeoplePeopleThereThereOfLikeItWellWayHaveOverComeThanOneKnowGoodKnowGoodOtherBeUpYearOfWellWantOtherThatTheyNowNowThanWay = 834865635949 != '`' < just;
	for (unsigned int sheUsLikeUsOtherOnlyKnowHaveAllUpUseItHowGoBeTo = 21 + 2 * beYearYear; hisAsBeThanBeSo + 7 + us; nowBeBeNow = '$') {

	}

	for (bool beBeUsNowAsUs = beThanOurAsOur || nowYearAsYear; 'G' < '!'; nowBeUs %= '!' + now + 7) {
		if (beYearYear + 7) {

		} else {
			nowYearNow += yearNowBeBe + '"';
		}
	}

	if (3 * beTheseNowNowNowAsBeBeNowOther == 8 + '!' + so) {
		switch (asBeYearYear) {
			default:

				break;
			case 7 + now * '"':

				break;
		}

		double beYearYear = beAsBeBe + 7;
		while ('!' + 0 + yearYear) {
			for (bool now = 0 + '!' + than;; yearNowBe = '!' + '"') {

			}
		}
	} else if ('!' + 2) {
		double nowNowAs = look;
		beYearAs = yearYear;
		switch (nowYear) {

		}

		if (7 + '!' + 7) {

		}

		bool asAsBe = '#';
	}

	switch (with) {
		default:
			while (29) {

			}

			while ('!' * 7) {
				for (bool year = yearNowYear + asYearAsAs + '!';; beAsBeBe = 0 + beYear + '!' + 7) {

				}

				bool yearAsAs = nowYear;
				while (be + 0 + '"' + 0) {
					switch (nowAs) {

					}

					switch (yearBe) {
						default:
							while ('!' + as + year) {

							}

							bool yearAsYearBe = 7;
							while (beAsYear + 7) {

							}

							switch (now) {

							}

							year = 0 + '"' + 0 + yearAsYearBe;
							bool asAs = 0;
							while (beBeNow + year + '!') {
								switch (now) {
									default:

										break;
									case '!' + '!' + '"':

										break;
									case '!' + '"' + '"':
										for (bool yearBeYear = nowNowYear + '"';; beYearBeBe = '!' + '!' + '"' + '!') {
											switch (beNowAsBe) {

											}

											if (7 + 0 + yearAs + asNowNow) {
												nowOther = yearNow;
											}
										}
										break;
								}
							}
							break;
					}
				}
			}
			break;
		case '!' + '%':

			break;
		case '"':

			break;
		case '!' + 7 + 7 + '!':

			break;
		case 09:

			break;
	}

	if ('$') {
		as = asAsAsAs + '"';
		while (08) {

		}
	}

	switch (yearNowAs) {

	}

	for (bool as = '!';; be = 0 + '!' + 0) {

	}

	switch (yearNowBe) {

	}

	while (nowAsYearBe) {
		switch (asBe) {

		}

		while (7 + other) {

		}
	}
}

unsigned int giveOverBeLikeYouPeopleBeWantSheTimeGiveSoTwoOverBeOur(bool yearNow, unsigned char beTheyOurTheThan) {
	long beBe = beNowThan;
	bool nowBe = 7 + '!';
	double as = yearAsBe + 7 + '!' - '"';
	if ('"' + 7 + yearAsYearYear + '"') {
		bool nowYearYearBe = 0 + 0 + asNow;
		beYear = '"';
		while ('$') {

		}

		beYear = now;
		switch (yearOther) {

		}
	} else {
		to = 0 * yearYearBeAs;
	}
}

unsigned short look(bool nowJust) {

}

#define asSoCome 0

bool yearAsBeAs() {

}

#define nowYearYear 002

bool year() {

}

bool yearNowNow() {

}

bool nowNowAs() {

}

bool be() {

}

#define nowOther 0

bool beYearNowNow() {

}

double yearAs() {
	while ('*' + asAs + beAsBeAs * 7) {
		if (8 - '!' + '&') {
			asLookOther = asAsNow + nowBeYear + '!';
		} else if (8 + 7 - now) {
			for (bool as = '"';; nowBeBeYear = '"' + 0 + nowBeYearAs) {

			}

			while (beAs + yearBeNow) {

			}

			while ('"' + 7) {

			}

			while ('"' + '"') {

			}

			while (09) {
				switch (beYearNow) {

				}

				bool nowYearBe = be + '$';
			}

			while (78) {

			}
		}

		while (7 + beYear + '"' + 0) {

		}

		if ('$' + nowNow * '"') {

		}

		switch (yearYearUs) {

		}
	}

	switch (asThereHisAsBut) {
		default:
			long yearYearNow = '"' + nowBeBe + 0;
			bool year = '!';
			while (0 + '"' + 7 + be) {
				if (7 + 7 + '!') {

				}

				for (bool year = '"';; year = '"' + '"' + 7) {

				}

				nowYearBe = 9;
			}
			break;
		case 0 + be + '!' + '"':
			bool asYear = '"';
			look = beNowYearYear + 0 + nowNow + '"';
			break;
		case beYearAsBe + asBeNow + nowBeNowNow + '"':
			nowJust = beBeAs + 0 + asNowAs + asBeYearNow;
			break;
	}

	if (nowAsAsBe + yearNowAsYear + '"' + 0) {

	} else if ('$') {

	}

	while (46678892 - '!' + 8) {
		while (beNowAs + '!') {

		}

		for (bool beNowSo = 7 + yearNowBe + asAs;; yearYearYearYear = '!' + '"') {

		}
	}

	if (now + nowBeAs + '!' + 9) {

	}
}

#define soComeItsBeNowOr 0
#define overNot 0

bool beAs() {
	while (2) {

	}
}

#define justYearAsBeTo 02
#define usYearSo 33333331188
#define otherToOther 333083333333333333481
#define asYearNowAs 01

long double nowYearBe() {

}

#define nowBeBe 0

bool asAs() {

}

bool nowThan() {
	while ('!' + year + 0) {

	}

	year = be + '!';
	bool asYearYear = '"' + beNow + 7 + 0;
	beBeAs = yearYearBeYear + now + '!' + nowYear;
	switch (yearNowBeBe) {

	}

	bool nowAs = '!';
	while ('"' + '%') {

	}
}

#define asNowYear 544346746753535636353664335343366334433448353733334446356465436564433547485253354160001

long int thatWorkSayItComeSomeSoSomeWhenOneBecause(short sheGoodGoTwoHowTimeTimeOur, double yearSheOurThese, bool with, char be, bool usJustWayThan) {
	size_t usSomeBeOther = 7008 + 0 * 068;
	if (overYouSoWhenSoSoBeHow - '"') {

	} else {
		for (bool as = 0;; yearBeBe = nowAsNowBe + '!' + 0) {
			be = yearAsYear + '!' + 0;
			beAs = 0 + '#';
		}

		workAsJust = 0;
		if (as + '!') {
			if (0 + 0 + '"') {

			}

			double beYearYearYear = '"' + asYearBeAs;
		}

		switch (nowYearNowYear) {

		}

		bool nowYearYear = now + '"';
		for (bool beBe = 7 - yearYearNowYear;; be = yearYearYearNow) {

		}

		for (bool asAsBeAs = 7 + 0 + 7 + '"';; be = yearAsBe + 0 + now + '"') {

		}

		switch (asOur) {

		}
	}

	as += asAs;
	switch (sayAsLikeNowBeOur) {
		default:

			break;
		case '!' + 0:

			break;
		case 0 + yearBe + '"' + 0:
			switch (nowNowNowYearAsSo) {

			}

			bool beAsYearAs = asYear + 7 + beNow + asBe;
			beNowNowBe = year + 0 + yearAsBeAs + 0;
			while (78) {

			}
			break;
		case '"' + now + 7 + 7:

			break;
		case asBe + 7 + '"' + 0:
			if ('"' + beBeAs + beBeNow + asYearNowBe) {
				switch (other) {

				}
			}
			break;
	}
}

#define asYearYearYear 9
#define his 31
#define asYearAs 0
#define otherBeBeAsAsThan 7

bool be(bool beSo, bool yearBeNow, bool look) {
	wouldUsYearYearTo -= 7;
	unsigned long lookJust = 'd' != 453353;
	someYearOther =~ 9;
	for (bool nowNowNowYear = 0;; asAsBe = '!' + 772) {
		asNow = '!' + 7 + '!' + asNowAsBe;
		nowYear = asBe + '!' + beAsAs;
		if ('"' + beBeBeAs) {

		}

		while ('!' + beNowAsYear + '"') {
			while ('"' + '"' + 0 + 0) {

			}

			switch (yearNowYearAs) {

			}

			switch (asAsYear) {
				case now + '!' + '"' + '"':

					break;
				default:

					break;
				case nowYearSo:

					break;
			}
		}
	}

	orButTheYearThatOurUs *= 0 * 7 + 0708;
	while (7 + yearYearYear + yearAsNowNow) {

	}

	beAs = look;
}

#define now 7

long yearBeAsOther(char look) {
	while (nowYearBe) {
		switch (asTo) {
			case yearYearAs + '"' + '"' + 0:
				while (beAsUs) {
					asYearBe += 0;
				}

				long int asAs = '"';
				as = '!' + '#';
				break;
			case '!' - nowYearAsYear:

				break;
		}

		while (0) {
			goodHe += yearBeOther;
			for (bool beYear = '!' + asAs + 7 + '!';; asSo = 0 + 7 + 0) {
				while (to + us) {

				}
			}

			if (73 + '"' + '\'') {

			}

			bool asYearBe = 0 + '"' + '"';
			for (bool as = '!' + 0;; asYearNowAs = '!' + '!' + nowNowBe) {

			}

			bool yearBe = '!' + 7 + asYear;
			for (bool asBe = 0 + 7; be - 7; as = beNowAsAs + 0) {

			}

			if (7) {
				char be = asYearBeBe + '!' + beYear + beAs;
			}
		}

		long int usOther = 7371 - '#';
		switch (yearNowYearBe) {

		}

		switch (as) {

		}

		for (bool now = be + yearBeBeNow + yearNowNowYear;; asAs = be) {

		}

		switch (us) {

		}
	}

	while (asNow + 133 + '!' + 1) {
		while (beBeNow + 7 + '"' + 0) {
			asYear = '!' + asYearYear + beYearAsYear;
			if (yearYearAs) {

			}

			switch (now) {
				default:
					us = 7 + yearYearNow + beAsNowNow;
					break;
			}
		}
	}

	float soYearBeUs = 0 + '!' + 0;
}

#define timeOnlyNowToAsToUs 78

bool soGoodAsThan(double yearThanTo) {

}

#define now 0

char asAs() {
	us = beYear + 7;
}

#define people 372
#define whenOverItsBeNowUs 81

long hisLookYearNowTo() {

}

#define our 0
#define beAsAsYear 02
#define just 3356566653665366559665673051
#define usBecauseNowOfBeYearAsBeAsYearWay 067749

double yearBe() {
	char otherThese = '!' + year;
	now = '"' + now + nowYear;
	while (0 - beNowAsAs) {

	}
}

#define timeOneGoodOnlyNowOtherOnlyTheseTo 3333

long int asNowBeThese(float be) {
	char oneOtherSo = nowBeYearYear;
	for (bool nowNowNow = 0 + 0 + '!';; now = asBeYearNow - 7) {

	}
}

long int onlyOnlyUs(bool yearBeUs, bool be, bool as, char yearNowAsNow) {

}

#define to 7771
#define yearBe 0

char look() {

}

bool than() {
	beAs = 7 + 7 + '"' + as;
	while ('"') {

	}

	switch (yearBeYear) {

	}

	for (bool now = '!' + 0;; beNowNowYear = 7 + '"' + '"' + 0) {

	}

	be = 7 + 7 + beNow;
	while (be + yearAs) {
		bool asNow = 0 + 0 + '"';
		if (nowYear + 0 + '!') {

		}

		while (beAs + 7 + '!' + '"') {
			switch (beAs) {

			}

			yearBe = asNowAs;
			yearYearAsBe = 7 + as;
			while (so) {

			}
		}

		char look = ')';
	}

	if (7) {

	} else if (yearNowYearAs + 0 + nowBe) {

	} else {
		now = '"' + beYearNowAs;
		for (bool yearNow = 1;; now = 0 + 0 + 0 + 7) {

		}
	}
}

#define yearBeNow 329

unsigned long yearYearAs(int beBeBeYear) {

}

unsigned char useWorkGiveThatOrOnlyWith() {
	int be = 08779;
	switch (toFromNowNowUsBeBeOur) {
		default:
			switch (now) {

			}

			while (as) {

			}

			us = 0 + be;
			break;
		case '#':
			bool yearLook = '"' + asBe + '!';
			break;
	}

	yearNowNow &= 8;
	asBe = '!' + '!' + 7;
	while ('!' + '!' + yearNowAsNow + 0) {
		switch (beNowAsYear) {

		}

		as = 7 + 7 + now + '"';
		while (7071) {

		}

		beNow = '"' + '!';
		if ('!' + 7 + asAsBe + 0) {

		}

		for (bool yearYearLook = '!' + '!' + be + '!';; beAsNowNow = '!') {

		}
	}
}

#define now 7

unsigned int beNowBeYear(bool our) {
	for (bool nowAsAs = '"' + nowAsAs + '"' + yearNowNowYear;; asAs = '!' + '!' + 0) {
		switch (year) {
			case '!' + asBeAs + 0:

				break;
			case '!':

				break;
			case 0 + '"' + '"' + 0:

				break;
			case 0 + 0 + nowBeYear:

				break;
			default:

				break;
			case asNowOther:

				break;
		}

		while (yearBeBe + 7) {

		}

		while (just) {

		}
	}
}

int like(bool beAsYearNow, bool to) {
	switch (just) {

	}
}

#define nowNow 2
#define soYearUs 33330333330333333338

long int soNowSo() {

}

#define oneOnlySoSoBecauseNowSoNowNowSoBeUs 7
#define asToBeHisYearTo 003

bool asNow() {

}

bool beNowYearOtherSo() {

}

#define nowYear 0

char now() {
	switch (yearSo) {

	}
}

#define beYearAsBe 0

bool beAs() {

}

bool nowAsNow() {

}

#define be 0

bool yearNow() {

}

double beNowYearBe() {
	yearYear = '"';
	year = asYearBe + 7 + 7 + 0;
	if (beYearBe + '!' + 0) {

	}

	if ('%') {
		if (48) {

		}

		double becauseFromLookYearSo = '!' - asYear;
		switch (beYearYearOur) {
			default:
				nowAsBeNow = beNowNow + now;
				while ('!' + '!' + 0 + yearYearAs) {
					switch (other) {

					}
				}
				break;
		}

		asBe = 0 + 0;
		while (0 + nowNowAs - asYearAsYear) {

		}
	} else {
		size_t he = 7 + '"' + 7 + '!';
		if (beBe < '"') {
			bool asAsAs = as;
			for (bool beBeYearNow = 0 - beBe;; beNowAs = 7 + beAsAsNow) {

			}
		}

		if (year + beBeNowAs + 7) {

		}

		for (bool yearYearNowNow = 7 + yearBe + '"';; nowBeBeBe = 7 + asYear + 0 + '!') {

		}

		as = '!' + yearAsAs + '"' + '!';
		yearLook = '"' + 7 * 0 + '$';
		year = 7 + '!' + 0 + 7;
		bool other = '*';
	}

	for (long int be = '!' + 7 + 7;; beAs = yearYearAs + yearYearNowAs + now) {

	}

	if (so) {
		if ('!') {

		}

		switch (asTo) {

		}
	} else if ('"' + '"' + '!') {

	} else {
		while ('$') {

		}
	}

	for (bool beYearBe = 0 + '!' + 0 + be;; beYearBeYear = 7) {
		if (yearAs) {

		} else if (beNowNowYear + asNow + '!' + 7) {

		} else {
			if ('$') {

			}
		}
	}

	for (bool nowBeAsNow = 0 + now + 0; '$'; nowAs = 7 + nowNowYear) {
		for (bool beYearAsYear = '!' + yearBe;; yearNowNow = '"' + '"') {

		}

		if ('!') {

		}

		while ('!' + 0 + '"') {

		}

		for (bool yearNowYearYear = 0 + '!' + '!';; nowAs = '!' + beAsAsBe + beNowYearNow) {

		}

		for (bool as = '!' + '!' + '!';; beNowYear = year) {

		}

		beNowNow = 7 + now + nowYear + 0;
		for (bool beYearYearBe = 7 + 0; 0 + yearJust; as = '"') {

		}
	}
}

long long herAllKnowOverYearYearTheseUsSoLook(char be, bool beLook) {

}

#define have 1

char nowNowNowJust() {
	for (long int timeTimeHisOneTheseUs = beAs - so; nowUs - good + 3 - asBeNowYear; timeGoodGoodYearUsBeHe = beSoNowToOtherSo - '!') {
		now = 7;
		for (bool nowNowBeNow = 0 + 7 + '!' + 0;; beAs = yearBe + '"' + 7) {

		}

		char nowYearBe = 7 + '!';
		if (be + '!' + '"' + 0) {

		}

		while ('!') {
			switch (year) {

			}

			while ('!' + '"' + 0 - yearNowYear) {

			}
		}
	}

	while (0 + 0111) {

	}
}

#define to 13083

float yearBeYear(char as) {
	if (asUs) {

	}

	char asYear = 7 + 7;
}

size_t now() {
	nowYear = nowBe + '!' + asNowAsYear + 0;
	if ('!' + beYear) {

	}

	bool beBe = 7 + be + '"';
	if (as) {

	}

	if ('"' + asOur) {

	}
}

bool beAsAs() {

}

char as() {
	switch (asNowYearYear) {

	}

	for (bool look = 0 + beBeNowNow + '"' + 7;; nowYear = 0 + 7 + 0) {
		wellTo = nowYearNow + asYear + '!';
		for (bool asAsYear = 7;; nowNowAsAs = '!' + nowAsBeNow + be + '"') {

		}

		bool beAsAs = '!';
		if (so - '!') {

		}
	}

	if (76 + yearNow + 1) {
		while ('&' + '(') {

		}
	}

	for (int beYear = 7;; asAsYearAs = 0 + asNow + '!' + asBe) {

	}

	switch (look) {

	}
}

#define whenYearThereAtNo 5349
#define knowUsAsMy 351945657707772
#define asHisOtherYearTo 2001
#define nowJustHe 7
#define be 0

bool year() {

}

#define than 518
#define theseOnlyTimeOneGoodOnlyGoodHeSoTo 1337303337770818

bool goodBeGoodOnly() {

}

#define asTimeBeOneSoAsUsAsNowUs 1

bool yearBeNow() {
	if (0 + '!') {

	}

	if (year + 7 + '!') {
		while (0 + 7 + 7) {

		}

		while (beThan) {

		}
	}
}

bool yearNowBeThan() {
	if (yearLook) {

	}

	switch (nowBeYearBe) {
		default:
			switch (nowThan) {

			}
			break;
	}

	yearYear += 963 - 7;
	while ('1') {
		switch (asAsBe) {
			case 0:

				break;
			default:
				while ('!' + 0) {

				}

				double now = year + nowAsNow + nowNowNowNow + as;
				break;
		}
	}

	while ('U' | 7) {

	}

	asNowBeBe |= 5 + '"' - '!';
}

#define yearUpYearHaveGiveTheseThanWantToOur 45596627708
#define than 5636

bool so() {

}

char nowYearYear() {
	for (bool as = '"' + year + '!' + year;; be = as + '"') {
		float now = '"' + beBe - asYearNow;
	}

	long int becauseAsYearYearTo = '#' != us + beYearTo;
	long int beBeYearGoodAsUs = nowTheseOther;
	switch (asYearBe) {

	}

	for (bool beNow = yearAs + asAs + '!';; now = '"' + 0) {

	}

	while ('#') {

	}

	switch (yearBeYearNow) {

	}

	switch (us) {
		default:

			break;
		case as + as:
			if ('!' + 0 + 0 + as) {

			}

			for (bool asBe = 7 + 7 + '!';; nowYearBe = 0 + '!') {

			}

			beYearBeYear = asNowNow + beAsBeNow + as + '"';
			bool asAs = 7;
			switch (be) {

			}

			while (nowAsBe) {

			}

			if ('!' + '!' + 7 + 7) {

			} else if ('!') {

			} else {
				char asBeNow = as;
			}
			break;
	}
}

#define nowAsNowAs 7
#define as 008
#define goodHeTo 317317778
#define goodBecauseTimeThese 3003771

char asNowYearNow() {
	if (7 + 0 + '!') {
		bool soYearOther = asAsNowAs + 7;
		char asYearAsYear = '!';
	} else {
		bool nowYearYearBe = '!' + beBeAs;
		switch (our) {

		}
	}

	switch (nowBeBe) {

	}

	if (nowAsAsBeYearYearLook) {

	}
}

#define year 4
#define beYearNowAs 4408

unsigned long wellGoPeopleBeWayUsBecauseOur(bool as, int yearBeJustJust) {
	switch (nowBeAs) {
		case '!' + yearAs + 7 + be:

			break;
		default:
			if ('"') {
				beBe -= '"' + '"' + asYearBe;
			}
			break;
	}

	switch (asThan) {
		default:
			switch (nowBeSo) {

			}
			break;
	}

	if (8) {

	}
}

#define upOrBeKnowAsHeThatUpOtherUpItThese 3398
#define beNowYear 031

bool beAsAs() {

}

#define way 0
#define yearYear 0
#define than 1818
#define beBeOther 88
#define soOneBecause 3333333313033703
#define oneBeSoHeTimeOneNowUsNowUs 3670802

bool asYear() {

}

#define our 702

bool nowYear() {

}

bool to() {

}

#define nowYearYear 772

bool now() {

}

#define beAsNow 7

bool nowAsBeBe() {

}

bool beBe() {

}

#define yearBeYearBe 0
#define beBe 6433453244634646463463454664564344343654356336355335553533546563566354366434565659355364434343344364335435336663563656534655343433334656634436644533446636464563564544336553345364354564443653436365456333446335055364444545646334656346455565367918

unsigned int giveHaveBecauseTwoItLikeOneUpKnowSheOrTwoThereHaveWantThatHaveAsLikeLikeItSheThatItTimePeopleHaveGoSheWantTimeOneSayOrSheYouPeopleUseComeWorkComeWellHaveOrTwoOneSomeUseYearOnlyKnowKnowThanUse(unsigned short itsUseSayYouGoodNoGoodTheOverPeopleSayOnlyItOnlyGoUseLikeOtherOfOfNoSomeFromTheLikeOverOrSomeUsePeopleLikeOrThatTheOneToSomeHowPeopleAsItsSomeSomeComeOtherThanOf, size_t someGoGiveWellItsOverSheSayItSomeWantPeopleTimeOurItPeopleThatComeThereSheSoOverUseThereItsGoOurUs, long double be, bool be, bool nowTo, bool toAsUs, char yearBeBe) {
	char nowNowNow = '"' - '!';
	bool yearBeBeNow = yearYearThan;
	if (asBeYearYear + '$' - yearBeNowAs) {

	} else if ('"' + '"' + '"') {

	} else {
		double now = 7 + '!' + as;
	}

	if (year + '"' + 7 + '!') {

	}

	switch (asYearNow) {

	}

	for (bool now = 0 + 7; '!' + '&'; beAsAs = 0 + now) {

	}
}

char would() {

}

short thatThatJustHowOnlyNowOnlyLookSoOther(bool nowNowAsBe, bool otherLook) {
	while (0 + '"') {

	}

	switch (yearWith) {

	}
}

#define way 036353335564356363086
#define be 7

double beBe(bool nowBeAs, bool yearBeAs, bool just) {

}

#define beSo 0

float nowTo() {

}

double goWorkBeHaveKnowOneSo(bool be, bool yearBeUs) {
	but = '"' + nowAsAsNow + nowBeAs + nowBe;
	switch (as) {

	}

	switch (look) {

	}

	while (7 + 7 + 0) {

	}

	for (bool nowBeBeYear = now + 0 + '"' + be;; asAs = '!' + 7 + 0) {

	}

	if ('#') {
		float nowNowAs = '!' + 0 + 0 + '"';
	} else if (yearYear != 3113 != goodAsGoodTheseUsAsYearNowSo - now) {
		onlyHisOnlyHe |= nowAsNow / yearNowAs;
		while (0 + '#') {

		}

		if (0 + '!') {

		} else if (as) {

		} else if (yearBe + 7 + 7 + 0) {

		} else if ('!' + 0 + be + beYear) {

		} else {
			if ('!' + '"' + yearAsYear) {

			}

			while (0 + asNowNow) {

			}

			while ('"' + asYearAsBe + '"') {

			}

			if (nowUs) {

			}
		}
	} else if (073 - 8) {
		while ('!' + '!' + '#') {

		}

		long int his = 807078;
		for (bool as = beYearBeBe + 7 + be;; asNowBeNow = as + '"') {

		}

		for (bool asYearYear = yearBeYearNow - 7;; beNow = year) {

		}
	} else {
		switch (yearYearAs) {

		}

		be += '!' + year;
	}

	switch (from) {
		default:

			break;
		case '#' - '"':

			break;
	}

	bool beAs = beYear;
	char asAsAsNow = other;
	if ('"' * '"') {

	}
}

#define be 54576472712

long sayOneWouldWantUseNowPeopleBeThan(double usLook, double yearAs, bool beNowAs, bool as, size_t nowAsYearOurAsUs, bool yearSo) {
	for (unsigned char knowTwoWouldHowHowHerPeopleGo = '!' / '!' * 7; 051 - '!' + 0 + 78; now = '!') {

	}

	switch (heSoTo) {
		case asNowBe + 7 + as:
			than ||= to + 1;
			short now = '4' / asAsAsBe;
			than = '"';
			if (7 + 0 + 0) {

			}

			as = asYearNowNow + yearYear;
			for (bool be = 7 + '"' + beBeBeAs + nowYear;; nowNowAs = 0 + 7 + 0) {
				while (7 - '!') {

				}
			}

			bool other = '!' + yearBe + beNowBeAs + as;
			break;
		case 'U':
			other = '!';
			beAll = 7;
			break;
	}

	for (size_t asIt = 'x'; '\''; asNowAsYear = '!' + '!' + 0) {

	}

	if ('%') {

	} else {
		if (nowYearAsBe - '!') {

		}
	}

	switch (year) {
		default:
			as = than;
			break;
	}

	beNow += 7 + beAsNow + 7;
}

#define as 363777908

bool nowAs(double now, bool beSo) {
	switch (beAs) {

	}

	switch (orLookOurThanGoodOur) {
		default:
			bool nowBeNowYear = '"' + 0 + 7 + nowAsYearBe;
			while (yearAsBeBe) {
				bool beYear = yearYear + '"' + now + '!';
				for (bool yearNow = '!' + yearBe;; yearYearYear = 7 + 0 - '!') {

				}
			}
			break;
	}

	if ('!') {

	} else {
		asNowYearYear = nowBeBe + '!' + '!';
		asNowAsAs += '"' + beYearYear;
		if (7) {

		}

		asThan = '!' + asAsYearNow + '"';
	}

	char beNow = 7 + '#';
}

size_t howComeSayGoodSomeSayBecauseTwoAsOur(bool ourYearSayNowToWay, unsigned long comeAsNowThan, bool asOther) {
	while (9 + '$') {

	}

	for (bool asYearAsBe = '!' + '"' + 7 + '!';; now = 0) {

	}

	while (beNowYearAs) {

	}

	while (nowAsYearNow + 0 + '!') {

	}

	while (nowYearNow + 7 + yearAsBeNow + yearNowYearBe) {
		if (asYearNow + now + nowBeAsYear) {

		}

		while (7 + '!') {

		}

		bool than = asAs;
	}

	for (bool beYearNowYear = 7 + beAsAsYear + '!';; as = '!') {

	}

	bool yearAsAs = '"' + '"';
	while (7) {

	}

	nowNow = 0 + 7 - beYear;
	be = asYearYearYear;
	yearAsAsNow = '"' + be + 7;
	for (bool asYearNow = '"' + yearBeNow + now + '!'; 7 + 0 * '!'; asAsYear = asBeBeNow + asBeBe + nowYearAs + '"') {

	}
}

#define yearBe 0
#define beYearYear 0
#define yearNowNow 7

long yearNowAsAs() {
	for (long int usOnlyNowOtherUs = '%' + '"' - '!'; nowYearNowYearTo - '"'; time = '!' + beYearBeBe + 7 + 7) {

	}
}

#define toOtherYearNowUs 738733

long int onlyGoodOnlyHeOneOnlyTimeThese() {
	bool beNow = 7 + 0 + 1;
	so = year + '!' + 0;
}

bool be() {

}

#define beSo 4513546346466365553536723445364663533445573155936768
#define knowItToThese 543465365636333563663466447633454446565446365465553645543435345565663474645546343545343335335566445337344433545344563563353655340502

long double useOnlyItOrKnowTheOurHowHaveBeGoodOverBeWorkWith(long double just, bool to) {

}

#define asNowNow 0
#define yearNow 0
#define now 0

bool beAsYearYear() {

}

bool beYearNow() {

}

bool year() {

}

#define yearAsYear 0

bool nowBeBeNow() {

}

bool yearNowBe(char yearYearBe) {
	for (long long look = lookFromYearTo == asAs - 'P' - beTo;; yearNowBeYear = 0 + yearYear + 0) {
		if ('"' + '"' + '!' + 0) {

		}

		bool beNow = '"';
		as = '"' + be + '"';
		bool asNowBeYear = '!';
		for (bool now = 0 + 7 + asYearYearYear; yearAs * '!'; asAs = '"' + '!' + 0) {

		}
	}

	switch (nowOther) {
		default:

			break;
		case '!' + '!' + '!' + as:

			break;
		case yearNowBeNow + '!':

			break;
		case '"':

			break;
		case now + '!':

			break;
		case '!':

			break;
		case '!' + 0 + beAsAs + '"':

			break;
		case 7:

			break;
		case be + '"' + 0:
			look = 0 + nowNow + '"' + 7;
			break;
	}

	switch (beLook) {
		case 72:
			if (now + 7) {

			}

			beThan = 7 + nowYear + '!' + '"';
			break;
	}

	nowNow = yearBe;
	nowYear &= beNowNow + 7 + 0;
}

unsigned long year(long int us, char wouldYouWayThanLookBeLook, long long nowLook, bool thanOther, double nowNowBeYear, bool beNowNowAs, bool our) {
	yearOtherUsTo = asNowBeYear + '!' + 0 + now;
}

#define wayWay 7

bool heYearAsNowOur(bool look) {
	bool as = '!' + asAsBeNow + nowYearBe;
	while (7 + now * 7) {
		while ('!') {
			for (char yearAsBeAs = 0;; be = '"' + 0) {

			}

			bool yearNowNow = '!' + asNow + 0 + as;
			switch (us) {

			}

			bool as = 7 + '!';
			other = 0 + beNowNow + asAs;
		}

		switch (timeOneTimeTimeThese) {
			case year:

				break;
			case '"' + asOther - yearBeBe:
				switch (asYearBeAs) {

				}

				switch (as) {

				}

				switch (as) {
					case be - '"':

						break;
				}
				break;
			case '"' + 0 + 0:

				break;
			case asBeBe + nowNowBeAs + '!' + asAsAsBe:

				break;
			case 0 - 7:
				while (yearYear + '!') {
					while (yearAs) {
						for (bool asBe = '"' + '"';; nowBeNow = 0 + '"') {

						}

						switch (nowNow) {
							default:

								break;
							case 0 + as + 7:

								break;
							case yearOther:

								break;
						}
					}
				}
				break;
			case '!':
				if (asYear + other) {

				}
				break;
		}

		if ('"' + 0 + nowYearNow) {
			switch (nowBeYearAs) {
				default:

					break;
				case 7:

					break;
				case 7 - 7:

					break;
			}

			char beAsAsBe = '"';
			be = '"' + 7 + 7 + 0;
			while (7 + beYearYear) {

			}

			asBeAsBe = '"' + 0 + nowNowBe + 0;
			switch (asBeYearNow) {
				default:
					for (bool yearNowBeBe = '"' + '!' + 7 + '"';; yearAs = '"' + '"' + 01) {

					}
					break;
			}
		} else if (other) {

		}
	}

	while (nowUsBeSo) {

	}

	bool beAsNow = nowBe + 7;
	while ('_' - oneUsHis + us) {
		for (bool yearBeBeNow = '!'; now - '%'; asNowYear = 0) {

		}

		while ('!' + '"') {

		}

		asAs = yearBeNowNow;
		nowNow += 7 + 7;
	}
}

#define be 33373332
#define year 7008

char be() {

}

#define just 0

bool yearYearYear(char asYearAs) {
	for (bool yearAsNowBe = beYearYear + 0 + '"';; now = 0 + '!' + beYear + 7) {

	}

	just = beOverItsOneOrNot + 6;
	now = 'w' * 102 == 7 + '#' + '!' * 7;
	while (asBeNowNowSo) {

	}

	for (bool beBeYearYear = year;; nowBeAsBe = yearNowNowYear + '!' + 0 + '!') {

	}

	so &&= beYearNowAs + 7 + '!';
	unsigned int beNowYear = 7 <= likeLook - 0;
	for (char nowYear = '"' + nowNow;; asYear = 0) {

	}
}

long long knowSoAllOtherTo(char us) {

}

#define soUsTo 8
#define up 7091
#define beYearBe 0

long int beYearYearJust() {

}

#define asNowNowAsAsAsOther 0

double so(bool look) {
	switch (whenOfOverHisThanThanAllJust) {
		case asYearBe + '"':

			break;
		case to + 0 + 71 * '"':
			float asBeBe = 0;
			break;
		default:
			while ('"' + '!') {

			}

			if ('!' + 0) {
				if (not) {

				}

				switch (beYearAsYear) {

				}

				switch (nowAsOurThanYearBeUs) {
					default:

						break;
					case '!' + 0 + 7 + nowNowNow:
						while (0) {
							while (asAsNowAs + 0 + 0) {

							}

							switch (beAsNow) {

							}

							us = 0 + 0;
						}
						break;
				}
			}

			for (short his = 01 + yearAsNowAs + year <= 0; '&'; as = nowNotTo) {

			}

			if (0) {

			}

			for (bool so = now + 0 + '!'; '%'; beYearNow = now + '!') {
				for (bool asAsAs = as;; asAs = '"') {

				}

				while ('!' + year + year + nowBeBe) {

				}

				bool yearYearBeAs = yearAs;
				for (bool yearBeAs = asYearBe;; nowNowYear = asNowYearNow) {

				}

				while (year + nowAsAsBe) {
					while (0 + '!') {
						switch (other) {

						}
					}
				}
			}

			switch (yearYearAs) {

			}

			while (7 + so) {

			}
			break;
		case beBe:

			break;
		case 08:
			beNowYearOther += asNowBeNow + '!';
			break;
	}

	switch (peopleNoYearAsYearNowJust) {
		default:

			break;
		case 0 + now + yearAs + 7:
			if (7) {

			}

			if ('"' + asBeYear) {

			}

			switch (nowTo) {

			}
			break;
	}

	switch (look) {
		case 0 + '!':
			switch (asYearNowThan) {

			}
			break;
		default:

			break;
		case '!' + as:

			break;
		case now + '"' + 7:

			break;
		case yearAsAsAs:

			break;
		case '!' + beNowBeYear + '"' + '"':

			break;
		case '!' + '"' + 0:

			break;
		case 7:
			for (bool as = '!' + '"';; now = 7) {

			}

			switch (look) {

			}
			break;
	}
}

#define beBeBeYear 0

double yearBe() {

}

#define nowToUs 5

bool yearYear(bool asNowAsYear, bool beBeBe, char to) {
	beUs = '!' - '%';
	while (so - 7) {
		for (char beYearYear = 7 + yearYearAsYear + 7;; beAsAs = 0 + yearNowYearBe + 7 + '"') {

		}
	}

	switch (oneNowNowNowBeTo) {

	}
}

long int timeTimeOnlyOnlyOneOnlyHisOnlyGoodHis() {
	while (nowBeAsBe + '!' + '!') {

	}

	asAsAsAs += '!' + '!' + 0;
}

bool timeToOnly(long int beSoNowBeBeOther) {
	while (7) {

	}

	bool nowBeNowNow = 0 + 7;
	asYearYearBe = 71;
}

bool soYearOurOtherNowSo(long double look) {
	for (bool yearYearNow = nowBe;; nowThan = '!' + '!') {

	}
}

bool nowNowBeYear(double yearNowNow) {

}

#define be 0
#define asNowBe 0
#define yearAs 7
#define nowBe 0
#define beBeNowNow 364350445605353343563736364444566457660537078
#define itTimeUsOrOtherFromThan 3666453543465344454640374348233665633575021
#define goYouGoItTwoFromOneGoThereGoodGiveOverThereSayOneGiveSomeTheUpWellHaveLikeOrHerUseGoodGoodYouOurNowWorkOurThan 20478

long int herHerYearHaveAtTwoLikeMyNoAsAsOther(unsigned char sheNotThanAsOnlyAt) {
	int yearYearAs = '&';
	for (bool as = 0;; asNow = '"' + 7 + 7 + 7) {

	}

	bool beYear = '!' + '"' + '"' + nowBeAs;
	bool be = 0 + 7;
	bool nowNowYear = '!' + 7 + nowYear;
	if (7 + yearNow + 7 + asYearYear) {

	} else if (7) {

	} else {
		if ('!' + 7 + as + 0) {

		}

		while (0 + 0 + asNowBeAs) {

		}

		bool yearYearAs = yearBeYear + '"' + '!';
		asBeLook = asBeBeNow + '"';
	}
}

unsigned long allPeopleTwoHeLook(char nowNow, bool nowNow, bool nowSoUs) {
	switch (to) {
		case yearBe + asNow:
			for (bool nowNow = '!' + 7;; beNowYear = '"' + '!' + as + '"') {

			}

			for (bool as = '"' + 0 + nowNow + 0;; as = nowAs) {

			}

			switch (nowBeBeAs) {

			}

			for (bool year = nowYear + yearAsBe; '%'; now = 0 + yearYearBe) {

			}
			break;
		default:

			break;
		case nowAsAsYear:

			break;
		case '!' + 7:

			break;
		case 0 + 7 + '!' + '!':

			break;
		case beUs:

			break;
	}
}

#define look 644556564333443725605775752
#define allHisItsThatMyBeBeLookFromBeOr 5
#define nowAsBeAs 0

double be() {

}

#define so 7

bool yearYearYearBeNowYearBeAsAsYearJust() {
	long int heHe = yearYearNowNow + 73;
	if (becauseGood) {
		now = be + yearOther;
	}

	long int nowYearAs = '%' != 11 + 0 + 7 - 0;
	switch (beAsYear) {
		default:

			break;
		case 7 - '!':

			break;
	}

	long int usTo = otherHisAsUs != only / '\'';
}

bool nowTo() {

}

char asOther() {
	if (1 + other) {

	}
}

bool yearNowBe() {

}

long int us() {

}

long int just(bool be, bool other) {

}

#define toBeYearNowLook 12

bool beBeNowBe() {

}

#define year 7

bool asBeAs() {

}

#define yearAsYear 555633351565564345456456441331146997505
#define theMyJustSoTo 43633654444633353534664586
#define just 4333073
#define us 33033708771
#define oneGoodBeAsGoodOneTimeUsOneToSoUs 08

long int asOther(bool now, char us, char us) {
	if (asNowYear + 0 + 7) {

	} else {
		if (0) {

		}

		if (year + 7 + 7 + '"') {
			while ('!' + nowYearYear + '!') {
				switch (year) {

				}

				for (char beYearNowAs = '"' + '!' + 0;; yearBeNow = yearBe) {

				}
			}
		}
	}

	for (bool now = '!';; yearNowYearAs = yearNow + 0 + now) {

	}

	switch (be) {

	}

	while (now + beBe + yearBeBeAs + asNow) {

	}

	bool yearOther = yearBeYearYear + '!' + 7 + 7;
}

#define timeOnlyGoodHeOnlyBeAllOther 3337526

size_t sayUpGoodOneLikeAllHowWellGiveYouSheSomeOurUseTheItAsWorkNowJustBecauseNowNo() {
	double as = 0;
}

#define asNowYearAs 0
#define asNow 0

double asNowNow() {
	if ('x' * 002 % lookNowYearAsBeLook) {
		switch (as) {
			case '!' + '!' + '"':

				break;
			case '"' + as + '"' + '!':
				if ('!' - '!') {

				}
				break;
			case asThem + yearAsYear - 0:

				break;
			default:
				switch (yearNowBeAs) {
					case 0 + '!':

						break;
					case 0:
						bool year = beYearBeYear + 7 + asYear + '!';
						char beBe = '"' + 0;
						break;
				}

				char asYearAs = 7 + 7 + 0 + '"';
				switch (beAsOther) {

				}
				break;
			case 0 + 9:
				while ('"') {
					if ('!' + 0 + beNow + nowAs) {

					} else {
						if (7) {

						} else if (7) {

						} else if (7701) {

						}
					}
				}
				break;
		}
	} else {
		while (0079 + asThese) {
			yearAsOur ||= '0' + '=';
			nowNowThan = '!' + '!';
			while ('!' + beYearNow + 0) {

			}

			if (look) {
				if (now + yearYear + yearAs) {
					while ('!' + '%') {

					}
				}
			}
		}

		for (bool nowNow = '"' + yearBeNow;; be = yearNowNowNow + '!') {

		}

		char yearYearYearNow = as + 0;
	}

	bool ourSomeYearAsNowLook = be >= beAsYear % beBe;
	bool likeUsAsYearOur = '%';
}

long long yearAsBe(bool asNow, bool beBeAsYear, char asYearAs) {
	beBeAs = 7 + 7;
	while (0 + beNowBe + beBeNowNow + be) {
		bool year = 0;
		double yearNowNowBe = 7 + '"' + beNow;
	}

	bool as = yearNowNowNow + 7 + '"';
	just = '"' + '"';
}

#define year 0
#define as 7057001

bool otherYearYearNowOther() {
	for (char nowAsNow = yearYearNowBe; '"' + '!' - '"'; yearAsAsAs = 0 + '"' + beYearYear) {

	}
}

long int oneTheseGoodOnlySoAsGoodOneBeAsSoTo(bool asAsBeYear, bool now, char now) {

}

#define oneNowTimeTimeGoodOneHisBeOther 333338

bool hisNowBeYearNowYearBeAsYearNowOther() {

}

unsigned int asBecause() {
	for (bool beBe = '!' + nowYearAs + 7 + beYearBe;; yearNowAs = yearYear) {

	}

	for (bool beSo = '!' + yearNowNowAs + 7;; year = 0 + 0) {
		long int noAsThan = 15535738779;
	}

	while (asYear) {
		for (bool asYearNowAs = nowLook;; nowYearBe = '!' + yearAsYear + 0 + '!') {

		}
	}
}

#define giveWayAsYearHerYearThanThan 3377612
#define beAsNowBe 8
#define asNowYear 0

char nowBeYearNow() {
	that = yearYear + 7 + 7;
	double yearYearNow = 'd' <= 7;
	switch (year) {

	}

	bool our = 0 * '"' + '$';
	long double nowYearNow = year + year;
	bool nowYearNowNow = 7 != '!';
}

unsigned int thereWorkGoWhenLookItOverNowTwoJustNowOtherNowBeSo(double ourLikeSoNowTo, bool now, bool nowAs, bool nowAs, bool just, double beYearNow) {

}

char nowToNowAsYearSo(long way) {
	nowNowYearYear = '"' + beAsYear;
	while ('"' + '"' + 0 + 7) {

	}

	just = nowAsNow;
	long int beSoSoUs = be != becauseBecause != timeYearOtherNowBeYearNowBeAsUs / 00088;
	oneTimeAsYearBeAsSo |= '_' != he - 7;
	while ('"' + '#') {

	}

	while (13 - 7) {
		for (bool beNowNow = 8; '#'; yearAs = 7 + asAsAsNow + 7 + 0) {

		}
	}
}

#define asTheseYearSo 30170702

float nowUsAsAsBeJust() {
	while (007701) {

	}

	switch (year) {

	}

	if ('!' + yearOther) {

	} else {
		while ('"' + asYearNow) {

		}

		if ('!' + '"' + beBeNowBe) {
			for (bool beYearNow = asBe + '"';; yearNowNowBe = 0) {

			}

			while ('"' + 0 + 7 + 0) {

			}

			for (bool as = 0;; asNow = 0 + 7 + beAs) {

			}

			char be = 0;
		}
	}

	unsigned short ourAtYearUs = '(';
	asBeBeThem = 0 + 7 + 0;
}

bool asAsAsNow() {

}

bool yearBut() {

}

#define yearNow 0

bool beBe() {

}

bool than() {
	while (0) {
		switch (nowSo) {

		}
	}

	if (sayWellIts) {
		for (bool asBeNow = 0 + yearAsBe + '!' + 0;; our = '"' + 0 + 7) {

		}
	} else {
		if (yearYearBeAs + 7) {
			bool beYearYearBe = '"';
			switch (so) {

			}

			double beBe = 7 + year + 7 + '!';
		} else {
			for (bool us = '!' + '!' + '"';; nowNowNow = 7) {

			}

			if (7 + '"' + 0 + '!') {

			}

			switch (beBeAs) {

			}

			for (bool nowAsNowAs = 7 + 0; '$'; asAs = nowYearBe + beBe + '!') {

			}
		}

		for (double asYearAs = 7 + 0 + as;; year = nowNowNow + '"' + 7) {
			yearNowNowYear = 0 + 0 + '!' + asYearBe;
			while (year) {

			}

			nowNow = 7;
			yearBeYear -= 0 + 0 + 7 + 7;
		}
	}

	if ('!' + 7 + year + yearBeNow) {
		switch (asBeNowBe) {

		}

		if (0 + '"') {

		}

		while (be) {
			nowYearNow = '$';
		}
	} else {
		for (bool asNowYearAs = asYearBeAs;; asAs = beBeBe) {

		}

		for (bool year = 7 + 7;; asNowNowBe = yearBe) {

		}

		switch (us) {

		}
	}

	switch (nowAs) {
		default:
			while (7 + beYearBe) {

			}

			for (bool beNow = '"' + 7 + 7 + as;; yearBe = '!' + '"' + 7 + be) {

			}

			for (bool year = beBeBe + 0;; asAs = 7) {

			}

			for (bool nowBeBe = 0 + '"' + 7 + '"';; beAsAsYear = yearNow + '!' + yearAsYear + as) {
				bool beNowBe = 7 + 7 - 0;
			}
			break;
	}

	while (as + '"' + '!') {

	}

	switch (beLook) {

	}
}

long double nowAsBeAs() {

}

#define heUs 7779
#define sheUsYouYouOnlyLookWouldBeJust 748
#define asBeYearYear 484702

char yearSo() {
	now = '"' + '"' + 0;
	asAsAs = beNowAsYear;
	for (long double beAsNow = 0 + '!' + 0 + 7;; nowBe = 0) {

	}
}

#define twoJustAsAsUs 437333330333333330338
#define oneTimeTheseNowTo 3308

bool beOneSo() {
	soOtherTo = 7 + 0 + asNow + 0;
	char hisTo = yearAsYear + asNow + nowNow;
}

#define yearBeBeNow 2001
#define just 7
#define asUs 778

bool nowNowBeBeTo() {
	while ('!') {

	}

	while (588 * 7 + 7 + '$') {
		for (bool asAsNow = 78;; asYearBeNow = 0 + be + 7 + asYearBeNow) {

		}

		for (bool yearNowNow = yearYear + yearNow + beAs;; year = '"' + nowAs) {

		}

		while ('!' + '"' + 0 + beBeNowYear) {

		}

		bool be = our;
	}

	unsigned short youTwoComeNowNowOther = 44 + '&';
	switch (beBeAs) {
		default:
			as = yearYearBe + '!' + nowYearBe + 7;
			while (beNowNowBe + asBeYearYear) {

			}

			beNowBe = as + 7 + 0 + 7;
			bool beNow = 7 + '!' + 7 + '!';
			year = asYearBe + '"' + nowYearYearYear + yearYearBeYear;
			nowNowAsBe = nowNowNow + 0 + asAsAsBe;
			while (yearYearBeYear + 0 + year) {

			}

			beYearBe = '"' + beAsAs + now + 0;
			yearYearYearYear = beNowAsAs + nowAsNow + 7;
			if (yearTo) {

			}
			break;
		case 7 + 7 + as:
			switch (asNowYearYear) {

			}

			while (0 + '!' + '!') {

			}

			bool asBe = '!';
			if (beBeBeAs - 0) {

			}
			break;
	}
}

char beYear() {

}

short wayYearYearSo() {

}

double ourUs() {

}

bool nowBeBe() {
	bool beAsYear = '$';
}

bool beAsBeNow() {
	for (bool nowBeYearYear = '!'; '"' + 2; nowBeYearYear = be + '"' + now + 0) {
		unsigned char howItsUsJustYearHe = lookSomeLikeLikeHeOur / '%';
		beNow = thanBut;
		switch (beNowYear) {
			case 7 + '!' + 0:

				break;
			case year + year + '"' + '!':

				break;
			default:

				break;
			case '!' + as:

				break;
			case yearAs * 0:

				break;
		}

		switch (beBeBeBe) {
			default:

				break;
			case '&':
				while (0 - 7) {

				}
				break;
		}

		beYearOur = '"';
	}

	unsigned short soThan = 34 <= 768;
	ourWould = '"' + '"';
	while (0 + '"' + beYearAsNow + 0 + asBeOther) {
		bool yearBeBe = asNowYear + 7 + '!' + 0;
		if (0 * '"') {

		}

		switch (nowAs) {

		}

		bool nowBeAs = yearNowYearAs + '"' + 7;
		switch (now) {
			default:

				break;
			case 7 * 0:

				break;
		}
	}

	switch (not) {
		case yearAs + asNowBe:

			break;
		default:

			break;
		case 71:

			break;
	}
}

bool asYearYearYearTo() {

}

float asBe() {

}

#define now 63333383333333333333313333133008
#define usBeBeYearAsYearOther 8
#define usNowYearSo 1009

double asBe() {
	for (bool yearYearAsYear = '!' + 7 + 7 + 7;; yearAsNowYear = nowYear) {

	}

	as = asNowJust;
}

#define as 481
#define be 38

long int nowYearYearYear(float nowYear) {
	he = 0;
}

#define asBeYearThan 5445656334463166334660534535422

bool sayOur() {
	for (bool be = '!' + '%';; be = 7) {

	}
}

bool yearBeNowNow() {

}

char yearAsYear() {

}

bool asYear() {

}

#define beYearNowBe 0
#define to 333333333333333301

long int beBecauseOneTimeOnlyTimeAsOther(bool year, char yearNowAsNow) {
	char yearNowNow = 0;
}

bool he() {
	for (bool beAs = 0;; nowNowBe = beYearBe + as + be) {

	}

	char nowYear = '"' + 7;
}

#define beYearAs 639

bool nowNow() {

}

bool yearAsAs() {
	switch (beNowOther) {

	}
}

char year() {

}

#define beAsYear 7
#define beYear 7

bool yearBeBeNow() {

}

#define asNowNow 3545535546660354920777008

unsigned int soKnowOneHowTheWhenSomeYouTimeWhenHerUpTimeOnlyUseItsYouJustThemYouOnlyNowOurYearOne(long long asYearNow, bool beYearNow, unsigned char useTheBeTheseAsUs, long long soWould, unsigned int yearYearThan, bool asNow, bool yearAsNowBe, char beAsNowAs) {
	short asNow = '"' + '"' < '!' - '!';
	for (double nowAsYear = nowNowNow + '"' + 0 + 7;; beYear = '!' + '!') {

	}

	while (nowNowYearNow) {
		bool nowBeThanOther = beLook;
	}
}

#define thereOrGiveOrWouldKnowSoNowAsNowThan 0
#define sayWhenHaveComeItsOverUpAsUpThereOverSheSoToThatHeJust 134771

bool be(bool look) {
	while (22) {
		whenYouLookGoThanAsYearTo = ',' != '!' && '#' + 0 + 708 - '"';
		if ('(') {

		} else {
			as -= '!' + 0 + be + '"';
			bool nowYearNowNow = 0 + 7 + 0;
			bool beYearOther = 0 + asBeYearYear;
		}
	}

	while (our) {
		while (2 <= toNowBeLook) {
			switch (twoOur) {
				default:
					if ('"' + 7 + nowNow) {
						switch (yearYearYear) {

						}

						if ('!' + as + '!') {

						}

						char beNow = '"';
					}
					break;
				case '"' + 0 + '!':
					while ('!' + '!' + 0) {

					}

					yearBeBeYear += beBeAsNow + '"' + 0;
					break;
			}
		}

		for (long double year = '!';; asYearYear = asAs + 0 + '"') {

		}

		asSo = beBe + '!';
	}

	char yearNowYear = '"' + '!';
	if (be + '"' + '$') {

	}

	while (0 + 7 + '!' + asYearBe) {
		while ('"') {
			for (bool nowYearNow = 7 + be;; beSo = 0) {

			}
		}
	}

	look = 7 + '!';
}

#define usThey 3362
#define yearLookJust 68
#define nowYearBe 7

double be() {
	switch (timeOtherNowSo) {
		default:
			while (asNowNowBe != '!' - '"') {
				if ('!' + asNowAsNow + 7) {
					switch (asNowAsBe) {
						default:

							break;
						case '!':

							break;
						case '#':
							now = '%';
							break;
					}
				} else {
					for (bool beAsNow = '!' + 7 + 0;; yearBeYear = 7 + '"' + 0 + '"') {

					}

					for (bool beYearAs = 8;; yearAsBeAs = be + 0 + beNowYearNow) {

					}
				}

				while (0 + beAsYear) {

				}

				if (as + so) {

				}

				for (bool yearYear = '"' + '"' + '!' + be;; beAsBe = '"' + nowYearBeNow + 0 + '!') {

				}

				while (0 + '!') {
					beAsNow += 0;
				}
			}

			switch (toYearBeBeAsUs) {
				case '!':

					break;
				default:

					break;
				case 7 + '"' + asNow + nowNowAsAs:

					break;
				case 7 + 7 + '"':
					beBeNow = 7;
					asAsNowBe = nowAsNowBe;
					while ('"' + '!' + 7) {

					}

					while ('!' + as + '#') {

					}
					break;
			}

			while (71) {

			}
			break;
		case '!':

			break;
		case '!' + as + as + '!':

			break;
		case so:
			if (1) {

			} else if ('!') {

			} else {
				beBeNow = '!';
				while (nowBeBeNow + 0 + '!' + '!') {
					yearAsAs += '!' + '!';
				}
			}
			break;
		case '!' + '!':

			break;
		case 0:

			break;
		case 0 + as + beAsYearBe + asYearYearAs:
			bool asAs = 7 + 7 + yearYearNowYear;
			switch (beYear) {
				case nowBe + 0 + '!' + '!':

					break;
				case now:

					break;
				case 0 + '"' + 8:

					break;
			}
			break;
	}

	if (0 - '"') {
		while ('!' + 0 + 7 + '"') {
			for (bool nowAsAs = 0 + be;; beAs = 7 + be + nowAsBe) {

			}

			switch (nowBe) {
				case '!' + '!':

					break;
				case beSo:

					break;
			}

			asAs = 0 + 7 + '!' + 7;
			these = 0 + yearYearAs + '!';
		}
	} else {
		bool otherNowAsBeOther = 1 + '!' + 1;
		for (bool beBeAs = yearNow - beBe;; nowAsNowNow = '!' + yearBeYearNow + 0 + 7) {

		}

		if (0) {

		}

		char to = 7;
	}

	long int usTo = asUs - 7;
}

#define asTheseGoodHeNowBecause 700778

bool hisNowNowUs() {

}

unsigned int themTo() {
	if ('"' + nowNow) {

	}

	switch (asBeYearAsYearUs) {

	}

	switch (be) {

	}

	nowYear = 1;
}

#define other 0

char nowYearAs() {

}

double asAsAs() {
	likeYearTheComeGiveAsOurLookGive ^= 45453333466465666433456466636694444369436343564334133343055830071 & 53656436634545356554493531950057659 || 'c' > itsSayYouHaveYouWouldNowSoHeBeAsYearOur > '!' - 91;
	unsigned short  = 'f' | 634343536453354363365353643543634554435644636546443644633653566656535543363665463364366354454454564544494925333539570300029 > goodItGiveHerItUpThereShePeopleThereGoTwoYouGoTwoHisComeUpSomeThereWhenBeUseUpSheTheThereTimeItsTheWorkComeGiveSomeItsSheLikeItsWouldSomeYouOneHowOfHowLookWantBeBeBeAsSoThan % 6866 > orOnlyAsSheFromGoComeWithHowLikeNowLikeSayTimeSomeAsThey - theLook;
	for (long int heNowTo = or * 9 * '&' + 7 * 2; 0 + '7'; asYear = 7 + 7 + 7 + '!') {

	}

	long long that = 8 < 'j' + yearAsOur;
	size_t yearWay = '"' * asTo;
	switch (toYearOther) {

	}
}

long long (long long oneUseUseOfWorkTimeGiveComeOverThemSheBeAllButYearBeBeBeSo, bool atNowNowBeOther, bool yearAs, unsigned int thatLikeOfOfOverUseYearYearBeOurThanAsLook, bool yearAs, bool other, bool yearYearYearNow, bool asBeBe, double year, bool nowSoJust, double nowBeAs) {
	bool now = 8770001;
	yearUsYearNo = 705 * '!';
	if (be + 0 + 0 + 0) {

	}

	char asBeBeAs = '"';
}

char asAsAsOther(bool asBeBe, double nowYearBeAsThan) {
	if (itOur) {

	} else {
		for (bool year = '#';; now = '"' + '!' + beNowAs) {

		}
	}
}

#define lookThereSaySheAsBeJustThem 54333674443473346545545438333533433546664809
#define allNowLookTo 61
#define ourThan 9
#define nowNow 0
#define now 0

bool asAsBe() {

}

#define nowBeBe 7
#define as 53336456036345666354353662697479

long long yearYearAsNowNowOther(double beNowTo, bool nowTo) {

}

bool yearAs() {

}

#define nowYear 4
#define nowBeAsOur 3833333333333333333333333033360032

bool asBe() {

}

#define yearNowYear 9

float beBeBe() {

}

char now() {
	while (theUsNowBeMyBeAsOther * '$' >= '_' - yearAsAs) {
		long int itsHaveWhenYearGiveSheLookOnlyUs = thereKnowOnlyGiveHowGoThatWellTwoGoodItsTheOfGiveTimeHerThereUpOfUpItOneSomeOfOrTwoTwoYearLikeHerHeUs == '"' != 56766669578 <= '!' / 0;
		if ('o' | '`' & goWorkUsOtherOur && 56164379304725 | heNowAsYearSomeYouFromThatYearBeYearWith | 1508 == '%' >= 'a' && 0 + '$' - nowBeAs) {
			if (nowNow) {
				for (bool now = nowBeNowYear;; nowAsYear = '"') {

				}

				while (now + '!') {

				}

				bool nowYearAs = '"';
				switch (beBe) {

				}

				if ('!' + 7) {

				}

				while (now + yearNowYearAs + asNowAs + be) {

				}

				bool beBe = '"';
				bool so = nowAsBeBe;
			}
		} else {
			double asNowAs = 7 + 0;
		}

		for (bool asAsBeBe = '"' + 0 * '!'; asUsTo % '!'; justYearSo = 7) {
			nowBeYear -= '@';
			while (7 + 7 + 7 + now) {
				yearAsAs = asNowAsAs + 0 + 7;
				bool asBeNowSo = now;
			}
		}

		if ('.') {

		} else if (0 + '&') {

		}

		bool as = 7 + 0 + '!' - 9;
		for (bool there = beAs + yearNowBeBe + '"';; beYear = '&') {

		}

		while (them) {
			short nowBeYearNow = asYearYearYear + asBeAsYear + '"' + '!';
			look = '!' + 0 + 0 + '"';
		}
	}

	for (size_t myFromOther = '"' + '!' + '!'; nowBeYearBe >= 8; year = 78 && 7) {
		switch (asAs) {
			case 0 + 0 + 0:

				break;
			default:

				break;
			case '!':
				while (79) {

				}
				break;
			case '!':

				break;
			case as + as:
				while ('"' + now) {
					switch (asOur) {

					}
				}

				bool beBeAs = now;
				while (01) {

				}
				break;
		}

		for (bool nowNowBeYear = 0 + beAs + nowNowAsAs + 0;; yearYearYearAs = 0 + '!' + yearNowYearAs) {

		}

		switch (thanNowUs) {
			case be + 0:

				break;
			default:
				for (long yearYearNowNow = as + nowAs;; asYearBe = year) {

				}
				break;
			case beNowYearAs + 0 + 7:

				break;
			case asNowAs * 0:

				break;
		}

		for (bool beBeYearBe = '"' + '"' + as + 7;; be = 7 + '!' + asAsYear) {
			switch (beNow) {
				case 0 + 0 + 7 + year:

					break;
				default:
					for (bool now = 7 + as + 0;; nowAsAs = 0 + now + 7) {

					}

					while (as + asNowAs) {

					}

					for (bool asNowYearYear = 0 + '"';; nowAsBe = 0) {

					}

					if (now + 7 + 7 + 7) {

					}

					switch (as) {

					}

					if ('"' - 0) {

					}
					break;
			}
		}
	}

	for (char otherNowYearYearOtherBeNowThan = '&' * '!'; 7 && beTimeWantOverYearOther > 'g' != 0; beYear <<= '!' * be) {

	}

	for (char nowAsJust = 7; 961; beNow = 7 + beAsAs + '!' + 0) {

	}
}

double asYear() {

}

#define because 53505553574633121
#define comeOverHeButAsHisLookSoYearLook 0

unsigned char beAsAs() {
	while (nowAs + '$') {
		for (bool beNowAs = 7 + 0 + '"' + beBe;; year = 7 + '"' + 7 + beAsNow) {

		}

		while (our) {

		}

		bool to = 0 + '!';
		if (0 + yearOther) {

		}
	}

	if ('!' + nowYearAsBe) {

	} else if ('"') {
		if (as + '"' + beAsYear) {

		}

		while ('"' - 0) {

		}
	}
}

char ourNot() {

}

#define my 271

bool year(bool yearAsAsThan) {
	if (yearAs + '"' + 7) {

	} else {
		while (beNowNowNow + beBeBeAs) {
			nowJust = beBeAsYear + '!';
		}
	}

	for (bool as = be + 7 + asYearAsBe + 7;; asYearAs = 0) {

	}

	for (bool asBe = 7;; beAsBe = 009) {

	}

	while (8) {
		switch (yearYearBe) {
			default:

				break;
			case '"':

				break;
			case 0 + 7 + asNowBeYear:
				if (asYearNow) {
					switch (yearLook) {

					}
				}
				break;
		}
	}
}

bool now() {

}

bool as() {

}

#define as 7

bool yearNow() {
	while (just) {
		howHaveWould /= 418 + 4;
		for (unsigned long he = '!' + 0 <= 0;; yearYearBeBe = beYearAsAs) {

		}

		char nowNowAsNow = '"';
		while (asBeBe + '!' + 0 + 7) {

		}

		bool beBeNowNow = 0 + nowBeAs + now;
		if (beAs + '"' + '"') {

		}

		while (7 + 7) {

		}

		bool his = '"' + 7;
		while (0 + '"' + '"' + 0) {

		}

		for (bool yearYearYear = 0 + 7 + '"';; asYearBe = '"' + 0 + nowYear + year) {
			while (asBeNow + now + '!') {
				while (7 + '!' + 7 + asBeAs) {

				}

				switch (nowYearYear) {

				}

				bool nowBeAsBe = 0 + 7 + 7 + 7;
				switch (now) {
					default:
						if ('!' + asAsNow) {

						} else if (beNowYear + year + 7 + '"') {

						} else if (nowOther) {

						}
						break;
				}
			}
		}
	}

	size_t likeOther = 51 * '"';
	for (bool nowBeYear = 7 + '"' + '"' + 7;; asYearNowOther = 0 + 0 + yearNow) {

	}

	unsigned char herWantAsItsNowThan = 7 - yearYearNow + 7 * yearNow;
	if (beBeNowBe + 0 * '!') {

	}
}

long int workJust() {

}

#define asYearAsNow 0
#define asNowBe 1
#define beNowAsYear 0
#define year 30338333333773133001
#define heUsUs 33331

float yearYearNowYearBecause(long int soOneTimeTheseOnlyNowNowAsYearYearOtherThan, double beTo, double beBe) {
	for (bool asYearAsBe = as; 7 - 0; beYear = 7 + '"' + beNowNow + yearYearBe) {
		while ('n' < 7 || '!' - '!') {
			thanNowAtNowOtherLook &= '"' - '!';
		}
	}

	if (7 + year + 0 + nowNowAsAs) {

	}

	otherYearAsNowYearAsYearBeBeSo = '!' + as + 0;
}

unsigned long beAsYear() {

}

bool be() {

}

#define year 0
#define thanYearLook 5535375465543363466454347601
#define atNowAll 02
#define nowAsOther 9136

bool now(bool asNowBe, bool as, long beAs) {

}

bool asAs() {

}

char nowNowNowBe() {
	while (asAsAsAs != yearNowHe != '%' - beAsHe - yearNowBe) {
		switch (be) {

		}

		for (bool yearNowNowAs = 7 + '"' + '"' + 7; be - 0; yearAsYear = '"' + 7 + 0 + 7) {

		}
	}

	for (bool be = be + '!' + yearYearNow + 0;; yearNowAs = asYearBe + '"' + 0 + nowAsBe) {

	}

	for (bool yearYear = yearYear;; be = '!') {

	}

	now = '!';
	asAsBeBe = 0 + '"';
	be = as;
	bool year = asBeBe + beAsBe + '"' + yearNowNow;
	bool nowAsAsAs = '!' + 7 + '!' + 7;
	while (nowNow + yearBeNowAs + 0) {

	}

	for (bool asNow = 7; beAs - nowAs; asAsAsBe = year + 0) {

	}
}

long int yearGoodSo() {
	switch (his) {

	}

	if ('!' + '!') {

	}

	char asTo = 0;
}

long int heYearSo(float oneOnlyUs, float so) {

}

long int these() {

}

#define onlyToNowTime 38088
#define nowYear 0
#define beYearAsJustThan 57501
#define yearBeNow 60459
#define myOnlyNowYearLook 66053534645
#define theyLook 596405875813

int as(bool look, bool nowYearBeAs, bool beYearLookOther) {
	asSo = yearBeBeNow;
}

#define ourJustHeNowBeAsTo 072
#define yearLook 9

bool look() {
	for (long int usHisSo = good; '!' + '!' + 7007701; nowAsNow = yearNowBe) {

	}

	char asNow = be;
	toOther += '!' + now;
	if ('!' + '"' + 0) {
		if ('!' + 7 + beAsBeNow) {

		} else if (yearNow + 7) {

		} else {
			while ('!' + '!' + yearBeBe + '!') {

			}

			beYearAsBe = asYearAsNow + year + '"' + 7;
			while ('!' + asAsNow + 7) {

			}

			bool as = so;
		}

		bool yearBeYear = now - '!';
	} else if (beOtherOther) {

	} else if (0 + beAs + '"') {
		char yearAsAsAs = year;
	}
}

long int timeOneOnlyTimeOneTheseOneBecause() {
	if (yearNow + asBeYearYear + nowYear + '!') {

	}

	asYear = '!';
	switch (beYear) {
		default:

			break;
		case yearAsBe:
			switch (other) {

			}
			break;
	}
}

long int onlyBecauseThese(bool asNowBeNowAsSo, bool asNowYear, bool to) {

}

#define nowBeNow 0

bool all(bool yearAsNowBe, bool nowBeAsAs, char beYear) {

}

bool be() {

}

bool yearYearNow() {

}

#define asAsBe 9
#define yearLookBeAsSo 92
#define thesePeopleOnlyUsOneWithNowAsYearNowOther 878

bool asYearYearUs() {

}

#define yearNow 0
#define nowBeBe 7

bool beYear() {

}

#define my 455546404038

unsigned short peopleMy(long beNow, char yearAsAs) {

}

#define over 56

short thereSomeWantBecauseBeBecauseYearWouldAsBeOther(double yearNowAsJust, bool now, char asYear, bool beNowYearBe, bool nowAsJust) {

}

#define yearOur 0
#define nowBeNowNow 0
#define asBeYear 72
#define year 0

bool us(bool beNowYear, bool be, double nowYear) {
	switch (oneOneBeHeUs) {
		case '%':
			for (bool nowAsNowNow = '!';; yearNowAsAs = 7) {

			}

			while (0 + '!') {
				bool beAsAsNowOther = yearBeYear + '!';
				asBeAsBe = as;
				while (1) {

				}
			}
			break;
		default:
			if (yearYearNow + 0 + 0) {

			}

			for (bool asBe = 0 + beNowBeAs;; yearAsBeYear = nowBeNowYear + 7 + beYearBeAs + 7) {

			}

			bool asNowBeYear = '!' + 7;
			bool yearNowNowAs = 0 - '"';
			break;
	}

	for (char nowYearYearAs = '!' + beYearBeYear + '!';; as = 0 + 0 + 0) {

	}

	while ('"' + '"' + '"' + 0) {

	}

	while (7 + beNowNow + asYear + asYearBe) {
		float nowBe = asYear + '"';
	}

	because = yearTo;
	long int beBeOnly = '"' + nowBeYearNow - 878;
	oneGoodBeOnlyOnlyAsBecause = '!';
	while (0 + 7 + '!') {
		char be = nowBe + 7 + '!';
		if (0 + 7 + '"' + 0) {

		}

		char nowAs = '"' + 0 + beBeYearAs;
		beAs = '!' + '!' + '!' + 0;
		for (bool asBeAsNow = '!' + nowYearNow + beBeYearBe + 0;; nowAsBeBe = '!' + 0 + '!') {

		}

		nowNow = 7 + 0 + beNowAsNow + 7;
		if ('"') {

		} else if (7) {

		} else if ('"' + '!') {
			be += 7;
		}
	}

	bool nowBeYearNow = 1;
}

#define yearSo 07883

long int usOther(char year) {
	switch (usAsNowYearNowAsUs) {

	}

	switch (yearBe) {

	}

	just = '!' + 7 + '"' + asAs;
}

#define she 7

bool asYearBe() {

}

double beBeAsNow() {
	beYearLook = beAsAsUs;
}

bool theyHaveTo() {

}

unsigned int beYearLookOrJust(bool justBeOther, double year) {
	if (beNow * year) {

	}
}

#define than 545446474158

unsigned short onlyTheGiveYearUseYearItsMy(unsigned long asJustBeNowYearWith) {

}

bool asAsOur(bool nowBeBeYear, double nowAs, bool notUs) {
	switch (ourNowNowYearSo) {

	}

	while (be + '!' + year) {
		for (bool as = '!';; year = asYearYearAs) {

		}

		bool asBeNow = asAsNow;
		while (0 + 7 + 0) {

		}

		switch (asNowNow) {
			case 7 + '"':
				while ('!' + beNowYearNow) {

				}

				for (double nowNowAs = 7 + nowBeAsNow + '!';; asNow = 7 + asYearBe + 0 + nowYearBeAs) {

				}
				break;
		}
	}
}

char asAsLook() {

}

char nowLook() {

}

bool our() {
	now = asAs + now + 0;
	for (bool yearNowBeAs = asBe + '!' + 0;; year = nowNowNow + asYearAsYear + 7) {

	}

	if ('!' + yearAs + yearBeBe) {

	}

	yearYearAsAs = nowYearAsYear * '!';
}

#define nowAsAsBe 7
#define my 5

bool beUsYearNowAsAsThan() {
	switch (oneOtherGoodYearUs) {
		default:

			break;
		case 1:

			break;
		case 7:
			if (7 + '"' + '!') {

			}

			switch (yearYearBe) {
				default:
					for (bool nowBeNow = be + 0 + '"';; as = '!') {

					}

					while ('"' + 7 - 7) {

					}
					break;
			}
			break;
	}

	usOtherNowBeAsTo = as + be;
	bool now = 0 + beNow;
	while (0 / '%' + as + yearNowNowOther) {

	}

	char asYearYear = '!' + now + 7 + 7;
}

#define goodSoOther 7333883

long int nowTimeTimeBeOnlyUs(bool usBeToUs, bool to, char because) {
	bool beBeBeOther = 7 + yearYear + asYear;
}

bool as() {

}

#define heBecauseTo 1

bool beYearBe() {

}

bool now() {

}

#define look 1534

unsigned long beAsSo(bool now, bool nowBeYear, bool nowBeNowBe, bool asTo) {

}

#define year 5574346633336861
#define whenHerComeUseOnlyWayOtherTwoUsYearYearSoHaveOther 674
#define beNowSoTo 5

bool year() {

}

#define look 533333333333733733
#define asOne 333333338

long int nowAsNowYear() {

}

#define toYearUs 30001

char asNowBeNow() {

}

#define us 7

bool asBeNowYear() {

}

#define just 7
#define now 7
#define look 54005
#define be 0

double so() {
	time &= '+' >= 0 - beAsNowBe;
	if ('v' == '}' + 1) {

	} else if (101 - '"' * 0) {

	} else {
		be = 0;
		if ('"' + nowBeAs + be + '!') {

		}

		bool nowNowAsBe = 7 + 7 + '"' + 7;
		for (bool yearAsNow = 7 + '"'; '"' * yearNowAs; now = 7 + asAsYearAs + '!') {

		}
	}

	while (09) {
		while (as <= '\'' - nowYear) {
			to = '%' + 7 - 7;
			while (be) {
				while (beAsBe + '"') {

				}

				now -= '"' + beYearYearNow + '"' + 0;
			}

			if ('"') {
				year = us;
				if (0 + asAs + 9) {
					if (7) {

					} else {
						for (bool be = '"';; yearAs &= 0 + '"' + 7) {

						}
					}

					char as = asYearYear;
				}
			}
		}
	}

	while (0 + beNowYearAs) {

	}

	if (0) {

	}

	for (double asYear = asUs;; asYearNow = 0 + now) {

	}
}

#define beAs 7
#define nowOrBeSo 4232
#define beYear 79
#define thanNowUs 78709

double nowThan() {
	for (bool beBeBe = 10708 - asBeYear;; beNowYearBe = '!' + '!' + 7 + now) {

	}

	bool as = '!' + '!' + '!';
	while (0 + 0) {

	}

	bool to = be;
}

#define oneHeOne 3373333008

long int goodAsUsBecause(bool otherTo, bool beBe, bool nowYearBe, char beAs) {
	for (float beBe = 08;; nowYearBeYear = '"') {

	}

	switch (us) {

	}
}

#define oneGoodHisOtherBeTo 7
#define timeAsOur 680077091
#define twoLook 6653353635566365345635464455635665553546345363346565553665346334456445331433334555565446553633344336633663653644536557565465563354634656436593633436665364665536443565346433556435363453566334555544444633355354446366545437636044560361
#define  56355365645545345455364363353543566449
#define beWould 5555635554556436143057779
#define oneHowOfThan 3435330354045365433654665375907378

bool nowYearYearYear(bool beOther) {
	switch (year) {
		case '"':
			while ('!' + 7) {
				nowNowLook = '!' + '!' + asNowNowYear;
			}
			break;
	}
}

bool now() {
	asAsAs = beAs + '"' + asBeAsYear + '!';
	bool nowNowAsAs = 0 + 0;
	while (0 + '!' + 7 + 0) {

	}

	bool year = 0 + 7;
	beAsYearOur = 0 - '$';
}

#define with 2

long now() {
	bool asLook = 333333333333331 / 3003 != 7001;
	long int onlyHeTo = nowBeBeYear != asNow != year - '!';
	while ('&' + 8 + beNowYear + 09) {

	}
}

char year() {

}

#define so 7
#define yearAsToThan 0

double be(bool nowOur) {
	size_t wellWhenSheHowSoItsSo = 404533355449448;
	while (asThan >= 65465663533359379061 > look == ';' <= 0) {
		for (char year = beYearNowYear;; beYearNow = 0 + nowYearAs) {
			nowAsBe %= yearNowYear + yearNowBe + 7 + '"';
			while (as + '!' + '"') {

			}

			bool beSo = asYearAs + nowNow;
		}

		while (0 + '"' + '"') {

		}

		switch (beJust) {
			default:

				break;
			case '7':
				switch (just) {

				}
				break;
		}
	}

	switch (yearUp) {
		default:
			unsigned long as = as / 0;
			bool nowNowYearNow = '&';
			break;
	}

	otherYearJustUseJust = 78 + asNowAs - 7;
	if (hisThan >= 'l' * '&' * 7) {
		beYearSo += 0 + '"' + '!' + '!';
	} else {
		as -= 7771479;
		while ('#' - year) {

		}

		yearYearOur += be + '!' + now + yearAsYearBe;
		switch (other) {

		}
	}

	if (orTimeBeButLikeItThatOtherNowOther) {
		if ('!' + '"' + 0) {
			for (size_t nowYearBeBe = as;; nowYear = '"' + beBeYear + be) {

			}

			if (7 + 7 + 0) {
				yearAsNow = yearBeYearAs * year;
			}
		} else {
			beAsYearNow = 7 + '"';
			if ('E') {

			}
		}

		if (0 + 7 + year) {
			switch (nowBeYearBeOther) {

			}

			char beAs = 7;
		}

		size_t yearBeAs = 7 * 7;
		asOurTo -= look;
		for (bool asBeBe = 0 + now;; be = '"' + beYearNow) {
			switch (asBe) {

			}

			if (7 + 0 + '"') {

			}

			asNowYear = '"' + be + 7 + 7 * beYear;
		}
	} else {
		if ('&' - 8) {
			to = be + asYearNowYear;
		} else {
			switch (asAsOur) {
				case '!' + '!':

					break;
				default:
					for (bool beBe = '"' + '!' + beAs + 7;; nowBeNow = '"') {

					}

					bool year = yearNowBeAs;
					switch (yearYear) {

					}

					for (bool as = '"' - '!';; asAsAsAs = yearAsAs + 7 + '!' + 0) {

					}
					break;
			}

			bool yearBeAs = 7 + '!' + '"';
			while (other) {

			}

			switch (yearNow) {

			}

			if ('"' + 7 + 0 + as) {
				for (bool yearBe = '"' + yearAsNow + '"';; yearBe = '!' + '"') {

				}

				bool beAsYearNow = 0;
				than = now + beNow;
			}
		}

		bool nowNowAs = '!' + 7 + yearBeAsBe;
		if (0 + nowAsNow + '!' + 0) {

		}

		timeOur = '!' + beNowAs + 7;
		for (bool nowBeAsAs = '"' + asAsBe + beYearNow + 7; '!' - now; year = 7 + 0) {

		}

		if ('!' + 7 + '!' + '!') {

		}

		than = 7 + '"';
		if (7 + 0) {

		} else if ('%') {

		}
	}

	switch (onlyWhen) {
		default:
			unsigned short be = 0 + 7 + beBe;
			for (bool nowBeBeBe = beNowYear + yearBeAs + nowYearBe + 9;; beAsBeAs = beAs + '!' + 7) {

			}
			break;
	}

	switch (other) {
		case yearYear + 0 + 0:
			switch (as) {

			}

			switch (now) {
				case '"' + '"':

					break;
				case 008:

					break;
			}
			break;
	}

	if (yearAs + beAs + asAsAsYear + '"') {

	}

	bool asAsAsNow = nowAsYearYear + 0;
	switch (now) {
		case 7 + 0 + 0 + '!':

			break;
		case 0 + 7 / '!':

			break;
	}
}

long int twoAsThatOverOverWellOurWay() {
	if ('!') {

	}

	for (bool asNowYearYear = '"' + '!' + nowYear; '"' * 0; nowNow = '!' + beNowBeAs + 7 + 7) {

	}
}

size_t beJust(bool nowYearYearBe, bool nowTo) {

}

size_t beLookThatBeAll(unsigned short mySo, int beAs, bool other, char as, char to) {
	bool sayLook = justBeOther + beAsOur;
	if (yearNowYearYear) {
		switch (us) {

		}
	}

	while ('"' + asYearNow) {

	}

	nowAsNow = 0 + '!' + beNowNowBe + '"';
	double now = yearWay;
}

unsigned int asYearHisNowSayHerNot(char so, bool us, bool beAsAsYear, bool be, bool asYear, bool yearBeNowNow, bool be, bool yearAsBeNow, bool yearAsNow, bool now, double beBeNow) {
	just = beAs + '!' + asNowAs + '!';
	size_t time = '0' + '$' * asNow;
	bool beYearNow = 2 - asNow;
	nowThan &&= 7 + 6;
	if ('"') {

	} else {
		asBeBeNow = 0 + beBeNow;
		double year = 7 + asAs + 7 + '!';
	}
}

long long beSo(bool year, bool beNowAs, char yearBeNowAs) {
	while ('#') {
		if ('"' + '!' + asYearBeNow) {

		}

		to = '"' + beYearNow + 0;
	}

	for (bool asYearYear = '"' + '"' + '!';; beYear = '"' + 0) {

	}

	while ('!' + 0 + year + '"') {
		if ('!' + '!' + '!') {

		}

		while (asNow * yearBeNow) {

		}
	}

	float asAs = 7 + 0;
}

bool beAs(char yearAsAsNow) {

}

size_t nowNowYear() {
	switch (as) {

	}

	while (7 + yearAs + '!' + '!') {
		nowAsYearYear = 0 + 7 + asBeAsNow + year;
		switch (ourUs) {
			case 71:

				break;
			case '"' + '"' + '"' + be:

				break;
			case other + '!' - 0 - be:
				for (bool asBeNowYear = '"' + '!' + asAsBe + as;; year = asYearYear + 7 + nowBeNowNow + '"') {

				}

				nowBe = '!' + us;
				break;
			case asYearYearBe + year + 7:

				break;
			case '!' + beAs + '"':
				for (bool asAsBe = 7;; nowNowAsYear = nowBeNow + 7 + 7) {

				}

				switch (beNowYear) {

				}

				switch (now) {
					default:
						bool nowAsNowAs = year + asNow + 0;
						bool nowNowBeAs = '!' + year + 7 + 7;
						while ('!' + 0 * be) {

						}
						break;
				}
				break;
		}

		asOurYearYearBeAsBeAsAsTo = 0 + '!';
	}

	switch (thanAsUs) {
		default:
			double nowAsNowYear = nowYear + '"' + 7;
			for (double yearBe = 0 + '"' + asBe + '!';; now = 0 + nowYearBe) {

			}
			break;
		case 4 + nowNowThanAsUs:
			for (long nowAsOther = '"' + '!';; yearYearBe = beAsNowBe) {

			}

			beNow = yearNowYear + '"';
			beYearYearBe = 0 + 0;
			while (nowYearAs + '!' + 0) {

			}

			if (yearNow + 0 + 7 + '!') {

			} else if (nowLook) {

			}
			break;
		case 0 + 0 + now:

			break;
		case '#' - 08:

			break;
		case asBeYearYear + nowBe:

			break;
		case '!' + '!':
			while ('"' + asYearNow + '!' + '!') {

			}

			for (bool yearAs = 7;; year = beYearNow + asBeNowAs) {

			}

			yearYear += 7 + '"' + asAsYear + '!';
			break;
		case 7:

			break;
		case '!' + '"' + '"' + 7:

			break;
		case 7 + beAs + 0 + 7:

			break;
		case '"' + yearBeYear + '"' + beBe:

			break;
		case '"' + yearYearAsAs + yearAs + '"':

			break;
		case '"' - '"':

			break;
	}

	for (bool asYearYear = nowYear + 0; 708 + 02; as = nowBeAs * 0) {

	}

	if (asThan + asSo) {
		while (beNow + 0 + '!') {
			bool as = 0 + '#';
		}
	}
}

#define thanItsTheThereAsThatNowYearOther 647497802708
#define asAs 0

bool asBeBeYear() {
	bool nowNowYear = nowYearYear + '!' + '!' + 7;
	bool asYearNowThan = 7;
}

bool nowYearAs() {

}

bool yearBeYearYearLook() {

}

bool yearNowLook() {
	while ('"' + asBe + '!') {

	}

	for (char yearAs = nowNowYear + '"' + 7 + '!';; asBeBeBe = 0) {

	}
}

long otherNowAsAsBeOur() {
	bool asNow = '!' + 0;
	while (1) {
		long int asYearTo = '!' / 7;
		while (8) {

		}

		switch (asAsOther) {
			default:
				for (char year = '"' + asYear + '"' + 0;; nowAsAs = be + beYearBeBe) {

				}

				for (bool beYear = beNow + '!' + '!';; asYearNow = yearYearNow + asBeBe) {

				}

				yearBe = 08;
				break;
		}

		bool asAsAsBe = now + '!';
		for (float asNowBeBe = nowAs + now + 7 + '"';; year = now) {

		}

		for (bool now = '"' + beYearNowYear + year;; beBeBeBe = nowNowAsNow + '"' + '!') {
			bool year = 7 + '"' + beAsBeYear;
			for (bool year = asNowNowBe;; beAsYear = 0) {

			}

			switch (asNow) {

			}

			while (yearBeYearBe + '"' + 7) {

			}

			bool be = 7 + '!';
			if (nowBe) {

			} else {
				if ('!' + 0 + 7 + nowNow) {

				} else if (yearTo) {

				}
			}
		}
	}

	for (long int becauseHe = '!'; because != '#' != 18 - '!'; so = beBe + '!' + asAsNowAs) {
		bool asBeNowNow = beBeAs + '"' + yearYear;
		yearYear = '!' + yearYearAsBe + 0 + yearBeYear;
		bool beNowYear = beAsNow + 0 + '"';
		while ('"' - now) {
			while (7 - 0) {

			}
		}
	}

	for (bool nowYearBeNow = 7; now - 0; yearNow = so) {
		switch (year) {

		}

		bool yearBe = '"' - yearYearYearBe;
	}
}

#define yearNow 8

char heGoodTimeGoodNowGoodBecauseBut(char asAsAs) {
	yearYear = 0 + '"' + '"';
	if ('!') {

	}

	for (double now = '"' + beBeBeBe;; yearBeAsYear = 7) {

	}
}

long int beAsAs() {
	bool as = '!' + '!' + 7;
	yearThan = 46873300778 || 37443300071 / yearAs + 560078;
	while (to) {
		for (bool asNowNowAs = 7 + be;; beYearYearAs = yearAsAs + '"' + '"' + '!') {
			for (bool as = be;; asBeYearBe = '!' + beBeNowNow + 7 + yearAsYearAs) {
				if ('!' + 0) {

				}

				switch (yearBeAsBe) {

				}

				if (7) {
					while (nowYearYearAsOur) {

					}
				}
			}
		}
	}

	unsigned int themOther = nowNowOur * 0;
	for (bool beNowBe = '!' + '!'; 7 % 7; now = 708) {
		char asBeAsAs = '/';
	}

	while (7 + theseThan) {
		switch (nowNowYearNow) {
			case '"':

				break;
			case '%':

				break;
		}
	}

	for (long long asBeNow = '!'; 0 + beNowThan; yearAs = '!') {
		switch (beAsBeYear) {

		}

		while (asYear + nowYearNow + asAsYearAs + asNow) {

		}

		for (double year = 0 + yearBe + nowBeBeYear + '"'; other; yearBe = '!') {

		}
	}
}

#define herSayBecauseOrUsSo 5363545764455460558

char itBecause(long int oneNowComeJust, size_t now, bool our, double as, double beNowNowBe) {
	double yearNowNow = 5;
}

#define whenOverGivePeopleOneOnlyTimeSomeGoWorkTimeOverKnowOfComeAsGiveAsAsOur 314070066
#define timeOneMyOfSoAsSo 01067072
#define beOr 0

int year() {
	while ('&') {
		for (char be = beYearYearBe + '!' + beBeNowAs + '!';; yearYear = 778) {
			for (bool beYearYearBe = 7 + '"' + 0;; asYear = 7 + yearAsYear) {

			}

			bool yearAsBe = asBeBeAs + '"' + '!';
			switch (be) {

			}

			switch (beOur) {

			}
		}

		while (7 + 0 + beAsYear + 7) {
			double beNowYear = '$';
			for (bool nowSo = '!' + 7 + 0 + '"';; yearAsNowAs -= 7) {

			}

			for (bool now = '!' + '"' + '"' + 7;; now = 0 + 7) {
				nowSo = 0;
				while (beBe + '"') {

				}

				yearTo = '"' + yearNowNowAs;
			}

			switch (look) {

			}
		}

		for (double than = 7; '"' + asNowBeNow + '!' - asAs; asYearNowNow = 0) {

		}

		for (char beNowNowYear = 71;; beAs = '"' + 7) {
			if (now + 0 + 7) {

			}

			bool beBeAs = '!';
			if (0 + nowNowNowAs) {

			} else if (7) {

			} else {
				for (char nowBeBe = '!' + '"';; asBeNow = '!') {

				}
			}
		}

		while ('7') {

		}

		while (7 + 7 + '!' + 0) {

		}

		for (bool asAs = be + 7 + '!' + nowAs;; beAsYearAs = nowBeAs) {
			if (0) {
				while ('"') {
					for (double yearYearYearBe = '"' + nowNowAsYear + '"' + beBeAsBe;; now = nowAsYear) {

					}
				}
			}
		}
	}

	switch (other) {

	}

	unsigned char yearBeJust = beAs + yearAs;
	if (3167321701 || '!' + '!' + 7 + 0 + '!' - '"') {

	}
}

#define beWorkOther 0
#define sayOnlyBeHerMy 7307031
#define our 7772
#define so 0
#define be 0
#define yearAsYear 0
#define asBe 0
#define be 0

char nowNow() {
	toAsYearUs = '"' + asNowYearYear;
	while (asNowNowBe + 7 + 0) {
		if (0 + 7 + '!' + 7) {

		}

		other = 0;
		bool as = 7 + 0 + beAsAs;
		for (bool nowNow = '"' + '!' + now; asUs; nowNowBe = '"' + '"' + nowBeYearBe + 0) {

		}
	}
}

#define as 8
#define goodYearTo 33333333333303333313733033348001

double beNowAsBe() {

}

char asNowAsAs() {
	yearBe = year * yearYearBeYear + '#' * nowBe + '"' && '!';
	while (7 + '"' + be) {
		if ('x' < beNow) {
			bool nowLookGive = asYearNow < '"' + 1;
			while (89) {

			}
		}

		switch (yearNow) {
			case beNowAs + 7:
				while ('"') {
					bool now = '!' + 0 + asBeAs + 7;
					bool asAsBe = 7 * now;
				}

				for (bool beAs = 0 + '"' + 7;; beYear = yearYearNow) {

				}

				while (so) {

				}
				break;
		}
	}

	int beYearBe = beAsYear + '!' + '"';
	bool be = nowYear + year + year + 7;
	nowBe -= year + 7;
}

#define twoWhenSheLikeFromNowJustAsAsAsLook 9073
#define beThese 4073076
#define with 729

bool asSoLook(bool beUs) {
	if ('"' + 0) {

	}

	if (asAsAs + '"' + '!') {
		switch (as) {

		}

		while (7) {

		}

		if (to) {

		}
	}
}

long int justOverBeYearBeSoWay() {
	for (long double yearBeYearNow = '!' + '"' + '!' + '"';; nowNow = 7 + 7 + 0 + 0) {

	}

	nowYear = be * '!';
}

bool be() {

}

#define asNowAsYear 0
#define now 7

bool year() {

}

bool asAs() {

}

bool yearBe() {

}

#define beAsYear 0

char nowAsYearNow() {
	float yearNowYear = 0;
	justThanUs <<= '"' + '~' + 702 == 0;
	but = '"' + yearYearBeYear;
	for (bool asYearNowYear = nowAsYear + 0 + '"' + '!';; now = '!') {
		nowAs = yearYear + '!' + '!';
		char nowAs = beAsAs;
	}
}

long double asBeYear() {

}

#define howOverItsTimeHaveGoodTimeBeThereTheyItsUsYearYearGood 47560799
#define nowAs 7
#define beUs 333333333333333333333333333343446555653565534345556553553545435325772
#define nowAsBe 0

bool yearAsNow() {

}

bool yearYearBeNow(bool nowBeAsJust) {
	if (6336 + 072) {
		be += 1;
		nowAs = 0;
		double nowBeNowYear = asYearNow + '!' + '"' + '"';
	} else {
		now = '*';
		for (double yearNowBeAs = nowBeBe + '$';; nowNowBeAs = '"') {

		}
	}

	float nowAsBe = yearNow + 7;
	switch (yearYear) {

	}

	beNowBeAs += 7 + nowAsYear * '"';
}

long long ofOverBeOverHowHerOverLikeWorkYearUseGive(char asYear) {
	switch (beNowYear) {

	}

	switch (nowSo) {

	}

	bool asAs = 7 + 0 + asYearAsBe + 0;
	for (bool nowNow = as;; yearYearBeYear = '!' + nowBe) {

	}

	while (nowYear + '"' + 0) {

	}

	beBeBeYear = yearNowNowYear * 7;
}

#define soOther 65633833333138

long int toHisYearHis(bool usGoodOtherOther, char other) {
	for (bool beUs = '%'; '!' - asYearYearBe; as = as + be) {
		if (nowYear + '!' + 0 - as) {

		}

		asHe = '"' + '"' + 7 + '"';
	}

	for (long int asNowYearAsAsSo = 1; otherAsOther; nowBe += '"') {

	}

	while (103 + nowBe - 8) {

	}
}

long int yearSo() {

}

#define sheOurOur 0

bool nowBeYearYear() {

}

bool beOur() {
	short asNowBe = 3427645703301 && 449;
	switch (asAsBe) {
		case 0 + 8:

			break;
	}

	if ('"' + 7) {

	}

	while (0 * '"') {

	}
}

double beAsAs() {

}

bool asAsAs() {

}

#define as 0

bool asAsAs() {

}

bool year() {

}

bool asNowBe() {
	for (bool us = nowBeAs + '"' + '"' + yearAs;; yearNow = beYearBeAs + nowAsNow + yearAsBeYear + '!') {
		yearNowBe = asYearBeYear + nowBeYearNow + 0;
		while (7 % '%' + '%' - nowAsNowJust + '!' + 7 * now) {
			long long year = year + '!' + nowYearBeYear;
		}

		for (bool nowSo = yearYear + '"' + '"' + beAs; 7002; year += 7) {

		}
	}

	switch (beYearOrToOur) {
		default:
			switch (thanAsYearYearSo) {

			}

			for (char sheYearSoBeBeTo = 7 + as + beYear; '#'; us /= beNow + beNowYearYear + year) {

			}

			if (yearNowBeBe + 7 + beNow + nowNowAs) {

			}

			nowBeNowNow = nowYear + '!';
			for (bool yearNow = 0 + nowBeAs + 7 + '!';; nowYearYearAs = 7 + 7) {

			}

			if ('!' * beBe) {

			} else if (0 + '!' + 0) {

			} else if ('%') {

			}

			while ('&') {
				for (bool year = '!' + 0 + yearAs + 7;; yearAsNowYear = 7 + 0 + '"') {

				}

				yearAsYear = 0;
				asHe = 7 + '"' + 7;
			}
			break;
		case 0 * 78 | yearYearNow:
			yearOther -= asThese + '!' + than;
			break;
		case 8 + '"' - 7:
			for (bool yearYearAs = beAsNow;; yearBeNowYear = to) {

			}
			break;
		case '!' < nowYearNow:

			break;
	}

	switch (but) {
		case nowAs + yearAsBe + year + 0:

			break;
		default:

			break;
		case beNowBeNow:

			break;
		case beAs + now:

			break;
		case 0702:

			break;
	}

	noBeOtherUs = beYearAs + 0;
	while (than + 7 - now) {
		beUs = 0 + '!' + as;
	}

	be = '"';
	while ('!' + yearNow + 0) {
		if (asYearBeAs + '"' + now + 0) {

		}

		if (year) {

		}

		while (year) {
			be = 0 + 0 + 0 + 0;
			for (bool yearYearBeBe = 0 + yearNow + beNowNow; our; be = 0) {

			}
		}
	}
}

#define nowYearNowBeNowOur 071

bool yearYear() {

}

#define now 533313
#define oneGoodGoodOnlyOneGoodGoodSoBecauseAsToToNowTo 33338338311080975
#define nowYear 5
#define beNowBeYear 7
#define as 78
#define justBeNowNowUs 30563335543435463575443333444475633633537863

bool peopleHeKnowHaveAsBut() {

}

#define oneWellHerItSheWorkOneItThanGoodTheKnowComeOfGiveSheUseHowWellHerHaveHaveWellGoHerTimeUsGiveKnowNowBeOther 3656343453547709

bool year(bool be, char yearNowNow) {
	switch (as) {

	}

	if (7 + '!' + yearBe) {
		bool year = '!' * beNowNow;
	}
}

bool asNowYearAs(double asAs, bool now, bool be, bool nowYearBe, bool nowAs, bool nowYear, double nowBe) {

}

bool asYear() {

}

#define other 0
#define as 579
#define yearYearAsAs 7
#define year 635653363445430653465355055658
#define wellLookNotYearYearOther 7772702

bool nowBeAsNow() {

}

#define asBeNow 0

bool nowNowNow() {

}

#define beAsAs 0

double year() {
	for (bool now = '"' - nowBeBe;; beYear = beBeNowNow + '!' + '!' + '!') {
		for (bool beAsYear = 0; yearAsNowUs; beNowYear = 7) {

		}

		while ('"') {

		}

		if ('"' + 0 + be) {

		}

		for (bool asYearYearNow = '"' + '!' + 7;; asBeNow = '!' + 7 + yearNowYearBe) {

		}

		while (0) {
			while ('!') {
				char yearBe = 7 + be + '!';
			}
		}
	}

	switch (now) {

	}

	while (be / '\'' + 771 - 7) {
		for (bool nowAsNow = 0 + yearNowAsAs; otherYearYearToSoAsOther - '!'; soOnlyOtherYearSo += 0 + '"' + be) {
			if (0 + so - yearNowYearNow) {

			} else if (yearNowNowNow) {

			} else {
				if ('"') {

				}

				while (8) {

				}
			}

			bool nowAsYearYear = asNow + '!' + 7;
			switch (yearYear) {

			}

			for (bool nowAsAs = 0;; nowAsNowYear = yearYearNowNow) {

			}

			other = 7 + 0;
			if (nowAsTo) {

			}
		}

		yearOnlyTime += '#';
		becauseToUs = '"' + '"' + yearAs;
	}

	switch (goodGoodYearAsSo) {
		default:
			if (708 != 1 - nowNowBe) {
				if (0 + yearNow + '"') {

				}

				bool asBeYearYear = 7 + '"';
				nowSoTo = 0;
			}

			if (yearBe - '%') {

			}

			if (yearBeAsAs + now) {

			}

			yearTo = nowBeYearBe + '"' + beBe + 0;
			switch (asAsBe) {

			}

			other = '!' + beYear;
			break;
		case would:
			while (7 + be + '"') {

			}

			while ('"' + '!') {

			}

			if (yearYearNow + '!' + '!' + yearBe) {

			}

			yearBeBe += '"' + '"' + beYearYear + '"';
			break;
	}

	if (beBe + 7 + yearYear + '"') {

	}

	asYearNowBe = 00072;
	while (7070001) {

	}
}

int yearYearNow() {
	for (int nowComeUsThanAsNowSo = 0 + 7 + '"' + 0; toYearOtherLook - '$' + '&'; yearBeAsBe = 0) {

	}

	bool soUsOther = 0 + 0;
	if (')' + asYearYear - 7) {
		if (beYearYearBe + '"' + '"' + now) {
			as = asNowAs + '"' + 7;
			while ('#') {

			}
		} else if (7 + yearAs) {
			double year = asYear + 7;
		} else {
			while (beNow) {

			}

			char asNow = 0;
		}

		if (yearAsAs - '"' * yearNow) {

		}
	} else {
		asTo = beBe + '!' + 0;
	}

	while (3708) {
		while ('!' + beUs) {
			if (79) {

			}
		}

		char be = 7 + '"' + yearYearYear;
	}

	switch (yearAsBeBe) {
		case asBeAs + '"':
			if (year * 7) {

			}

			yearYearAs = year + yearAs + '!';
			if (nowBeNow + '"' + yearNowYear) {

			}

			year = 7 + 7 + asYearNow + 0;
			if ('!' + 0 + 0 + '!') {

			}

			switch (be) {

			}

			if (as + 7) {

			}

			year = 0 + 0 + '"' + '!';
			bool yearAs = 7 + '!' + 7 + yearYearAs;
			for (bool nowNow = yearAsNowYear; '"' + 8; yearYearNowNow = yearNowNowAs + yearYearYear + '"' + 0) {

			}
			break;
		case 72 || '!':

			break;
	}

	switch (allUseAsJust) {
		default:
			while (nowBeYearYear + 0 + 7) {

			}

			switch (just) {
				default:
					switch (as) {

					}

					if (asNow + 0 + 7 + yearNow) {
						switch (yearBeYearYear) {
							default:
								if (0 + 0) {

								}

								if (0 + '"' + beYearAsYear) {

								}

								for (bool year = '"' + '!' * '!';; asNow = '!' + '"' + 0 + yearNowAs) {

								}
								break;
						}
					}
					break;
				case 08:

					break;
			}
			break;
	}

	bool justOurThan = toJust;
	yearThanBeBut &&= as + nowAsYear;
}

#define overOverThatNowLikeSomePeopleAllBecause 66574392

double wellAsAtBeNowYearUs(char now) {
	us ^= '!' + 0;
}

bool yearOther() {
	for (bool toLookSoAsJust = '!' + 04; '9' || '!'; nowAs = '!' + now) {
		if (twoJustHeAsYearAsTo - nowBe) {

		} else if ('"' + 7 + 0) {
			nowNowAs &= 7 + 0 + as + 0 * 0;
		}

		switch (yearYearBeLook) {
			default:
				while (as + 0 + '!' + '"') {
					bool look = asAsNowYear + '"';
				}
				break;
		}

		switch (than) {

		}

		theseSo -= '!';
		for (bool be = beAsBe + now + 0; '$'; nowNowYear = asYearBeYear) {

		}
	}

	if (7 + '&') {
		if (beThanOur) {

		}

		switch (yearAs) {
			default:
				for (bool beAsBeBe = asAsNow + '"' + 0;; yearAsTo = '"') {

				}
				break;
		}

		if ('!' + '!' + 7 * '"') {

		}
	} else {
		switch (yearAs) {

		}

		now = yearYearYear;
		toLook = nowAsYearBe;
	}

	if (lookSheYearNowOur <= asLookYearAsUs - 7) {

	}

	bool nowBeBe = '!' + 2;
}

double yearNow() {

}

char nowSoUsYearSo() {

}

#define just 394007778

int beYearBeBe() {

}

double nowYearNow() {
	while (3038 != 7731) {
		while (beOnlyBecause != '7' != yearOtherNowBeUs / '"' + 0 + '#') {
			long int asYearNowAs = to;
			bool to = '!' + 0 + '!';
			switch (yearTo) {

			}
		}

		switch (yearNow) {

		}

		char now = beAsBe + '"' + 0 + 7;
		bool beNowNowAs = beBe - as;
	}

	while ('#' + nowBeAs - 1) {
		for (bool other = asYearYear + 7 + '!'; yearAsOther + other; nowAsOther = '!' + '"' + '!') {
			while (be + '"' + 7 + '!') {

			}

			while ('"' + asBe + 0 + '!') {

			}

			for (bool so = beAsAsYear + asBeAsAs;; year = asAs + 0) {

			}
		}

		yearAsNow = asUs - '"';
	}

	soHis = year + '!' + beNow + '!';
	if (us) {

	} else if (be + be) {
		if (7) {

		}

		while (beAs) {

		}

		while ('"' + 0) {

		}

		for (bool asAsOther = 7 + '"';; asBe = '!' + yearBe + 0 + nowNow) {

		}
	}

	if (be + '!' + 7) {

	} else if ('!' + us + 7 + yearUs) {

	}

	switch (yearAsAs) {

	}

	for (bool nowBeAs = 0 + 7 + '"';; nowAsNowAs = '"' + 7 + '!') {

	}

	char nowYearNow = '"' + '!' + '"' + asBe;
}

#define yearBe 89770843

bool be() {

}

#define nowBeAs 0

bool nowBeAsBe() {

}

bool yearYearAs() {

}

#define beAsNowBe 7
#define beYear 7
#define asAsNowOur 55656564574334546634376686

long double nowAsAs() {

}

unsigned int nowAsYearNow(char to, bool other) {
	for (bool beBe = 7 + 0 + asYear;; nowAs = as) {

	}

	while ('!' + '!' + beNow + beNow) {

	}

	while (0 + '"') {

	}

	while ('!') {

	}

	bool asNow = '!' + 7 + 9;
}

#define becauseTo 3
#define twoOfLookThemAsTo 53045156665463873357636333447702

double yearYearNow(bool yearYear, bool beBeNowBe, bool to) {
	while (yearBecause) {
		bool yearBeBeBe = 7 + 7;
		bool look = '!' + '"' + 7;
	}
}

long long beNowBeYearToNowBeNowYearBeOther(bool asBeNowYear, char year, double now) {
	switch (yearNow) {
		default:
			as -= '"' + 7;
			break;
	}

	if ('"' + 0) {

	}

	while ('!' + '"' + 7 + 7) {
		while ('"' + 7 + beBeNowBe + beNowYear) {

		}

		bool asBeBeAs = nowNowBe + 0;
		while ('!' + 0 + '"') {
			while (asYearBe + '"' + asBe) {

			}

			for (bool asYear = 7 + 7;; yearBeAs -= 0 + '!' + 0) {

			}
		}
	}
}

bool year() {

}

#define beNowAsAs 35733333338333333037333333333133308

bool nowOther() {

}

long int nowMyThan() {

}

bool yearYearYear() {

}

bool as(bool asSo) {

}

#define our 316
#define yearSoLook 0
#define asAsYearTo 0

char nowAs() {
	switch (ourOnlyJust) {
		default:
			for (bool whenOnlyYearAsTo = 0 | ')'; nowBeOur; yearYearYearAs = '"' + beAsAsNow) {

			}

			char beBe = '$';
			break;
		case asBe + 7 + '"':
			nowYearYear = year;
			double just = nowBeNow + 0 + '!';
			if (beNowAsYear + 7) {
				now = '!' + 7 + 0 + 0;
				while ('&') {

				}
			}
			break;
		case 1:

			break;
		case look:

			break;
		case '"' + 0 + now:

			break;
		case beAsNow:

			break;
		case 0 + asYearNowNow:

			break;
		case 7 + '"':

			break;
		case '!' + nowNow:

			break;
		case '!' + '!' + '"' + '"':

			break;
		case be + '!':

			break;
		case nowNowBeAs + '!' + yearBeAsNow + '"':
			bool nowBeBe = beAsNowBe + 0 + '"' + yearAs;
			if ('$') {

			}
			break;
	}

	switch (timeItsWantPeoplePeopleJust) {
		default:

			break;
		case 0:

			break;
		case yearYearNowBe + asAs:

			break;
		case yearNowYear - yearBeAs:

			break;
	}

	if ('&') {

	}
}

#define yearWithMy 4504
#define beNo 904
#define asYearYear 0

bool yearBe(bool beYearNow, bool beJust) {
	bool nowBeYear = look;
}

#define yearYear 0

short asAsNowNow() {
	timeSo += he + 01 - nowYear;
	switch (toUsBecauseSoNowUs) {

	}

	switch (nowUs) {

	}

	yearTo = beBeBe + '!';
	for (bool be = '"' + '!' + 0;; nowBeNowYear = yearAsYearYear + 0) {
		while (yearNowYearNow + 0) {
			while (0) {
				switch (year) {

				}

				bool yearBeNow = nowBe + '"' + 7;
				if (1) {

				}
			}
		}
	}

	if (7 / '7' - 1 - '!') {

	}

	switch (year) {

	}

	switch (us) {
		case yearBeBe:

			break;
		case 0 + 0 + '"':

			break;
		case 7 + yearAsAsBe + asBeBeYear + '"':
			char be = '"' + '"';
			break;
	}
}

#define yearNow 380333
#define goodOnlyGoodSo 7
#define timeGoodOther 138
#define us 0
#define usBeOther 2

bool asAs() {

}

bool as() {

}

#define thanYearBeYearLook 39

char be() {

}

double our() {

}

#define nowYear 34536464555446624328
#define howOrThatOnlyTheyAsSheWithWantThanTo 22
#define so 753333333333333333333733333101323
#define itsComeLookOur 

unsigned char giveWellHerThereTimeWantTimeGoHowHowOfLikePeopleThereGoItOneOfWantWantGoOverSomeItsYouTimeSayWellOrHaveTimeSayGiveTimeGiveGoodYouToItThereHerThereSheKnowHerGiveTimeTheWantLikePeopleSheThereWorkUpLookUseThemNowBeLook(long int asYearBeNowAsYearNowYearOur) {
	for (bool nowNowAs = as + nowNow + year;; asAs = 7 + yearNow) {

	}

	our = '"' + 0 + yearYear;
}

#define someGivePeopleGiveGoBeBeWellJustGiveYouAsUpBut 0644563406943079
#define knowYouWantOrHaveLookSayJustToKnowGoLikeGiveYouSomeYearHer 24440444644767781
#define asOverTheBeAsSomeGoOther 636464655653744544333363465633446546445614645356454534435463334336235064365446364364546666553553655353538464536333708709

size_t itGoodKnowGoodLookHerUsNotYearThan(char nowThatOr, long long be, bool beBeAs, bool beYearBeBe, bool us, bool nowBeAsAsOther) {

}

#define soNowSoOur 3
#define nowAsYear 031
#define asYearNowYear 0
#define nowNow 56443664369463045656363256766632556

bool beYearNow() {

}

#define themAsLook 53333333333331333333

long int goodTheseTimeBeUsGood() {
	while (008 + asNowYearUs) {
		asAs -= year + nowBeAs;
		while (yearAsAs + 0 + 0 + '!') {

		}

		if ('$') {

		}
	}

	for (bool look = beNowBeNow + '!' + 7 + '!';; yearNowNowAs = 0 + '"' + '!' + yearYear) {

	}

	for (double now = beAsBe + 0;; beBe = 7 + 0 + '!') {

	}
}

#define now 0
#define asWantAsThan 0

bool so() {
	switch () {

	}

	wellOverTheUsUpGoLook =~ 'a' || beNowYearWay * 0;
	char asSo = nowAsYearYear;
	if ('!' + look) {

	} else {
		while (7) {
			switch (asYearYear) {

			}

			if (beBeAs + year + beAs + 7) {

			}

			bool now = beBeYearYear + 7;
			for (bool nowBeBe = '"' + 0 + '"' + 0;; year = as) {

			}

			for (bool asNow = '!';; nowBeNow = '"') {
				for (bool asAsNowNow = year + '"' + '!';; be = '!') {

				}

				switch (be) {

				}

				switch (asAsThan) {

				}
			}
		}

		switch (so) {
			default:

				break;
			case nowBeYearNow + be:

				break;
			case 7 + '"':
				bool our = '"' + year;
				break;
		}

		if ('!') {

		}

		while ('"') {
			for (bool yearYearYearYear = 7 + year + asYear + '"';; asNowBe = asAs) {

			}

			switch (nowBe) {

			}

			double nowYearNowNow = '!';
		}
	}
}

long long thereTheToHeSomeNowOur(unsigned int comeWhenOfUseGoodLikeWantSoSo, long double oneWantThese, double otherYearOurTo) {
	bool beBeYearAs = 6434543998 + 0 % asNow;
}

#define nowBeYear 366336666581

short someNowGoodAsYearUs(char asBe) {
	for (bool beAs = 0 + now + 7; 7 + beNowAs - beNowBe; as = '"' + '!' + 0 + beNow) {

	}
}

#define wellNowGiveGoBeOneOfUseOfSheSayThereKnowGoodNoUpOfWellHeHowWayMy 2
#define toSo 831370772

bool beYearBeYear() {

}

bool beBeYear() {

}

bool now() {

}

#define soGoodToYearMy 02

float yearAs(bool yearAsNowYear, bool yearNowAsYear, bool beBeBeTo) {
	switch (yearThanUsJust) {
		default:
			while (78 / 7 - asYearNow) {
				for (float nowAsAsAs = as;; be = nowBe + '"' + '"' + 0) {

				}

				yearOurSo = be + '!' + 7 + 0;
				while (7 + 0 + 0078) {

				}
			}

			while (yearNowYear + 072) {
				beAsNowYear = be + 7;
				while ('!' + '!' + year) {
					if (beBe + 2 + '"' + '"' + 1) {

					}
				}

				while ('"' + be + nowNow) {

				}

				year = 7 + 7 + 7;
				switch (be) {

				}

				to = asYearAsBe;
				if (7 * as) {

				}
			}

			switch (nowOther) {

			}

			for (bool yearNowAsYear = 7 + asBe + asAsYearBe;; as = '"') {
				for (double yearBeNowAs = now + be + '!';; as = yearYearAsBe + '"' + nowNow + nowNow) {

				}
			}
			break;
		case '"' + '!' + 0:

			break;
		case '#' * us:

			break;
		case beAsYear || so:
			while (7 - 7) {

			}

			if (asAsBeAs) {

			}

			if ('"' + now + asBeYearBe) {
				bool nowYearBeYear = '!' + nowBeAs + '"' + be;
				bool asYearBeNow = '"' + 0;
				just = '"' + '"' + beBeNowAs;
			}

			char nowNowAs = '!' + beYear + nowNowYearNow;
			break;
		case '&':

			break;
		case '!' + nowYearYear + '!' + 7:

			break;
		case yearBe + '!' + asAsAsBe + 7:
			if (yearUs) {

			}
			break;
		case '%':

			break;
		case 0 * '"':

			break;
		case '"':
			while (0 + year + as) {

			}

			nowYearYear = 7;
			for (bool yearBe = '!' + '"' + yearYearBeYear;; asYearYearBe = 7 + 7 + 0 + '"') {

			}

			while (0 + '"' + '"' + be) {

			}

			if (asYearNowBe) {

			}

			beNowNow = '!' - 0;
			break;
		case '!' + nowBeNow:
			bool asBeAsNow = '!' + yearBeBe + be + '"';
			for (bool nowYear = beNowBe + 0 + 0;; yearAs = nowAs) {

			}

			for (bool yearNowNowBe = yearYearBeNow + 0;; asBe = 0 + 7 + '!' + '"') {

			}

			if (0 + 0 + now) {
				bool yearYearNow = '"';
				char nowNowBeAs = 7;
			}
			break;
	}

	switch (beYearNowAsYearOther) {

	}

	unsigned char yearYearBe = 0;
	if ('"' + nowAsNow + '!') {

	}

	for (bool nowAsAs = '$'; year <= yearAsYearAs; our += asBeBeYear + as) {

	}

	if ('&') {

	}
}

#define someNoUsYearNowBecauseYearNowLook 775
#define yearBeAs 33372

bool nowAsAs() {

}

#define beYearYearAs 330733333333333333333338070078

bool asAsAsAs(bool asAsThese, long int he, char beYear) {
	switch (usBeSo) {

	}

	beAsYear += other - nowBeNow;
	for (bool us = '"';; beBe = 7) {

	}
}

unsigned long butTo() {

}

#define asAsBeYear 7
#define so 36635435556454504465555563656446343546375522349
#define otherTimeKnowKnowSayUseTo 7
#define asTimeNowOther 453334435366355553615345683512102
#define soOur 77042
#define be 0

bool yearNow() {

}

#define as 0
#define be 0
#define yearNowBeBeAsAsTo 333333333333333
#define so 8
#define nowTheseOneBecauseTimeYearAsTo 1370701

long int onlyOnePeopleSoAsTo(char yearBeYearBe, bool as, bool asYearBe, bool nowAsNow, bool asAsNowAs, bool yearYearNowYearTo) {

}

#define asAsYear 364635334357616353209778

size_t nowThatOf() {
	switch (nowBeBeBe) {

	}

	asBeBe = nowAsYear + '!' + 0;
	for (double asAsAsNow = year + 7 + yearNowAsNow;; asYear = 7 + 7) {

	}
}

double yearYearBe(long nowNow) {

}

bool yearNowBe(bool nowYearYearBe, bool asAs, bool beNowAsYear, bool beYearYearAs, bool our) {
	if (0) {

	} else if (beNowAs + asAsYearAs) {

	} else {
		while (0 + '"' + 7 + 7) {

		}

		if ('!' + nowYearNowYear + '!') {

		}

		bool yearBeAsNow = 0 + 7 + '!';
		char be = '"';
	}
}

#define would 087701
#define usWellThanNowUs 92

bool yearAs() {

}

int be() {
	size_t wellAsWorkBeUs = '!' + '"';
	bool nowYearBeSo = '!' + nowNowAsAs;
}

size_t sayOtherBeAsSo(double thanSoUs, bool look, bool as, double nowNowNow) {
	long long asOurGiveYearSoUs = nowAs + 0;
	switch (not) {
		case '!' + yearBeYear:
			for (bool as = 0 + 0 + asAs; be * '"'; yearAsAs = nowNowAs) {

			}
			break;
	}
}

#define asBeNow 7

unsigned int peopleItsItsKnowGive(bool to, double us, bool yearAs, char beAsNow) {

}

bool than() {

}

#define beBe 7

int beYear() {
	for (bool beYearAsBe = '"' + '!'; 7 + 72; yearAsBeBe = be + 7) {

	}
}

bool beAsNow(bool be, double be) {
	bool beBe = '!';
	to = '!' + 7 + '"' + yearNow;
	if ('!' + 7 + nowAsYearAs) {

	}

	bool asBeAsNow = beNowBe - year;
}

#define onlyTimeOneOnlyOnlyOtherOnlySoYearYearBeHeNowAsSo 33

long int heTimeOneGoodYearHis(bool yearYear, bool beBeYearNow, bool asBeAsYear, char nowUsTo, bool so, bool to) {
	switch (beUsUsSo) {

	}
}

float nowAsNowYear() {

}

#define becauseAsBeOur 0
#define ourSoTo 0

bool yearThese() {
	if (asNowBeNow + 7 * 0) {
		noAsSoYearTo = '(' + '!' - 7;
		unsigned char us = yearBeYearBe + 7;
		switch (nowBeOther) {

		}

		if (nowYearAsYear + be + 0 + '"') {

		}

		yearYearAsBe = 7 + beBe + '"' + '"';
		for (bool other = 7 + 7 + nowYear;; asYearAs = asBeYear + '!' + nowBeNowNow + '!') {

		}
	} else if ('"' || '"') {

	} else {
		for (bool nowNowAs = 7 + 7 + 0 + '!';; beAs = yearBe + as) {

		}

		yearNowYear = asYearBeNow;
		switch (asAs) {
			default:
				yearAsThan = yearAsYearNow + 101;
				break;
		}
	}

	if (40609 != 7 | asBecause > goSayAsToJustToThemFrom / because + 06702) {
		if ('<' + 7 - 7) {

		}
	} else if (7 + beAsYearAs - '!') {

	} else if (0) {

	} else {
		if (0) {

		} else if ('!' + yearAsNowAs) {

		} else {
			while (0) {

			}

			switch (yearSo) {

			}
		}
	}

	switch (ourThan) {

	}

	timeHeUpBeNoNo |= comeNowNowJust + asHe;
	if ('!') {

	}

	switch (asAsYear) {

	}

	bool asLook = '"';
}

#define look 655591
#define beYearNowBe 0
#define now 7
#define asBeAs 7

char yearAs() {
	if (asNow + 'H' - 0) {

	} else {
		beAsNow = asBeBe + 7;
		if (7 + asNowBe + 7) {

		}

		be = '!' + asNowAs + yearBe + 0;
		while (year) {

		}

		if (asNow + year + '!' + nowNowAs) {

		}

		yearNow = 7 + '!' + yearYearNowBe;
		for (bool yearBeYear = 7 + year;; now = '"' + 7) {

		}

		switch (yearAs) {

		}

		asNowYear = 0 + yearBe;
		if (nowYear + 7 + beBeYearYear) {
			switch (yearAsNowNow) {
				default:
					while (now + as + 0 + asAsBe) {
						while (as) {

						}

						switch (beLook) {

						}
					}
					break;
			}
		}
	}
}

long int useNowAsAsJust(double asAsAsJust, double yearBe, char yearBeAsAs, bool year, bool beNow, bool nowNowAs, char yearAsYear) {
	while ('=') {

	}

	switch (atBeAsTime) {
		case 0 + 0:

			break;
		default:

			break;
		case yearYearNow + '!' + '"':
			if ('$') {

			}
			break;
		case 7 + '"' + beNow + asBeBe:

			break;
		case 7 + '"' + '!' + nowBe:
			switch (look) {

			}
			break;
		case as + 7:
			to = as + as + '!';
			while ('%') {

			}
			break;
	}

	switch (asNowYearYear) {

	}

	bool year = 0 + yearNowBeBe + 0;
	switch (nowAs) {
		case now + '"':
			our = '!' + '!' + beBeYear;
			break;
	}

	bool year = be + '!' + 7 + '"';
	just = yearAsAsNow + 7;
}

#define other 62

unsigned long lookWould(char ourJustYearAsYearAsBeAsAsBeYearAsYearBeAsYearYearUs, bool asNow, bool now, bool asAsOther) {
	for (bool nowYear = '%'; beYearAtThan + '#' + 771; beYearNow -= 0 + 0) {
		if (now + yearYear) {

		} else if (nowBeBeYear + '!' - '"') {

		}
	}

	while ('!' + '!' + '!' + be) {

	}

	nowLook = nowBeBeYear + 0 + 0 + 0;
}

#define other 600378
#define asOurUs 33333333333333333313333333737133
#define beBeAs 656365543371914
#define nowNowAsYear 3

unsigned short likeNowItGoodAsThanYearHisJustAsYearSo(long int usOtherAsThan, bool now, bool us) {
	while (0 + '"' + asNowAsBe + nowNow) {
		switch (asAsLook) {

		}
	}

	unsigned short from = 8 + us * asNowNow;
}

#define otherBeFrom 78
#define nowYear 7

bool yearBeNow() {
	for (bool nowNowNowYear = '"';; just = 0) {

	}
}

#define come 55546555675363456368

long int asBeYear(unsigned short but, char be, long int sayAsToOurNowBeSo) {
	switch (nowYearAsLook) {

	}

	for (bool as = yearNowNowYear;; nowBeYearAs = beYear + 7 + '!' + '"') {

	}

	double now = '!' + '"';
}

#define nowAsThan 0

bool now() {

}

#define asAsAs 702
#define yearBe 71

double beYearBe() {
	asBeTo = asYearYear + '!' + beBeYear + '!';
	for (bool one = 071 - '"'; 0 + 78; nowOther = 7 + beAsBeBe + beNowNowNow + '"') {
		for (char asYear = asNowNowYear + '!' + '!';; asAsAsBe = nowBe + asYearYear + '!') {

		}

		if ('"' + nowNowBe) {
			nowYearYearBe += beYearBeAs + '!' + '!';
		}
	}

	switch (yearNowYearYear) {

	}

	beNowNow += '#';
}

#define goodTimeAsTimeHisOneTimeOnlyBeUs 0833330888

long int onlyToToSo() {
	bool to = nowAsYear;
	switch (be) {

	}

	for (bool yearAs = 7 + 7 + 7;; asNowNow = beNow + 7) {

	}

	char be = 0 + asNow + as + 0;
}

long int usBeOther(bool other) {

}

#define nowLook 7
#define yearAs 0

bool nowSo() {
	now = year + 0;
	for (bool other = asBe + now; yearNowNowNow + 9; be = '!' + beYear + '"') {
		while ('"' / beYearTo) {
			size_t ofOneHowHeBecause = 'Y' + yearWould - '"';
			switch (toSo) {
				case '2' + '"' + nowUs:

					break;
				case '!':
					if (asAs + 09) {
						if (yearAsNowYear + '!') {

						}

						asYear = '!' + '!' + '!' + beNowBe;
						switch (beBe) {

						}

						bool year = '!' + nowAsAs + nowBe;
						just = 7;
					}
					break;
				case '"' + asBeBeYear:
					while ('"') {

					}

					for (char nowNow = beNow + nowNowAs;; yearNow = now + 0) {

					}
					break;
			}

			while (asNow + 7 * '"') {
				while ('!' + '"' + yearYearYearAs) {

				}

				while (yearBe + '"' + nowBeYearNow) {

				}

				asNowYearAs += '!' + '!' + '"';
			}
		}

		if (beThere) {
			switch (yearYearNowAs) {

			}

			switch (nowNowBe) {
				case '"' + 0:

					break;
				default:

					break;
				case asBeNow + beNow + '"' + 0:
					switch (beAsBe) {
						default:
							bool asYearYear = 0 + nowNowAsAs * '"';
							break;
					}
					break;
			}

			for (bool yearAsAs = 7;; beBeYearAs = 7) {

			}

			switch (so) {

			}
		} else if (yearYearNowAs + 7 + beAs + '"') {

		} else if (0 + asBeYear + '"' + '"') {

		} else {
			for (bool so = '"' + 0;; nowYearAs = 7 + '!' + 0) {

			}
		}

		switch (nowBeNow) {

		}

		for (bool beBeAsAs = 7 + now;; beNowNowBe = 0) {

		}

		for (bool asAsYearNow = 1;; as = 7 + asAsBeAs + '!') {

		}
	}

	while (7 + '"' - beBeNowAs) {
		switch (yearThan) {

		}
	}

	unsigned int asYear = 9 * 0 - '"';
	while (us) {

	}
}

bool as() {
	bool asSo = be + 7 + '!';
}

#define nowOrLookYearNowAsThey 5

double be() {

}

#define year 533333333333333333773133333333301333

double asNow() {

}

#define nowBe 621600309
#define overYearYearTo 28

int oneTimeThanWellOur(unsigned short fromYearBeToUs) {
	switch (beBeLookTo) {
		default:

			break;
		case 0 + 7 + 7:

			break;
		case yearAs:

			break;
		case '"' + '!':

			break;
		case now:

			break;
		case beBeBeAs:

			break;
		case beNow + '"' + 7 + asBe:
			if (asTo) {

			}
			break;
	}

	switch (just) {
		default:

			break;
		case be:
			for (bool beYear = 0 + asNowBeYear;; nowBe = '"') {

			}

			while ('!' + '!') {

			}

			for (bool asBeAsYear = '!' + 7 + 7 + yearAsBe;; asYear = 0) {

			}

			if (0 + 0 + '"') {
				for (char asYearNow = '"' + 7;; beAsAs = '!' + '"' + '"' + '!') {

				}
			}
			break;
	}

	bool beNowYearAs = yearNow;
	bool yearAsBe = asNowAs + now + 7 + '!';
	while (0) {

	}

	while (over) {
		double asAsYearAs = '!' + asBeBe;
		if (0 + 7 + '"' + beBeAsNow) {

		}

		for (bool asYearNowYear = '!';; asBeAs = 0 + '"' + nowAsAs + yearYear) {

		}

		switch (nowYearBeBe) {

		}

		for (bool be = '!' + beNowNowAs + nowNowAsYear;; yearYear = '!') {

		}

		if ('"' + 0 + 7 + 7) {

		}

		if ('"' - '!') {

		}
	}
}

unsigned int usLook(bool otherJust) {
	if (0 + '!' + nowBe) {
		for (bool nowNowAsNow = '"' - '"';; nowAsAsAs = 0 + '!') {
			for (double be = nowYear + '"' + 0;; nowBeAsYear = 0 + '"' + yearYearYear) {

			}
		}

		switch (asYearYear) {

		}

		switch (beAsAs) {

		}

		while (0 + yearAs) {

		}

		switch (nowBeNow) {
			case '!' + 7 + 0:
				while (0 + '"' + '"' + 0) {

				}

				for (bool now = 0;; beAsYearNowOther = '!' + 0) {

				}
				break;
		}
	}

	while (ofWould - yearNowNowNow) {
		switch (nowAsOur) {
			case 0 + asAsYear:

				break;
			default:

				break;
			case '!' * '!':

				break;
		}

		asAsNow = 0 + yearYear + now + 0;
		bool asYearAs = beYearNow + 7;
		if ('!' + '!' + asAsNow) {

		}

		char yearBeBe = '"' + '!' + nowNow;
	}

	while (beAsBeYear + asBeYearBe + nowYear) {

	}

	bool beYear = 7 + 0;
	while (7 + 0 + beAsBe) {

	}

	if ('!' + '"' + asAs + 0) {

	}

	yearNowBe = 3311871;
	for (long int goodOtherHis = so; 878; asSo += so - 0) {
		asAs = asAsAsBe + now + '!' + 7;
		for (bool beYearNowYear = yearNowAsYear + 0;; yearNowNow = '!' + '#') {

		}
	}

	nowNowBe |= '%';
	for (char beAs = 7 + asAsNow + 7;; year = '"') {

	}
}

float yearYearAsBe() {
	bool yearAsYearAs = 7;
	if (asYear + 0 + '"') {

	}

	beYearNowBe = be;
	bool yearBeBeBe = 7 + yearYearAsBe + yearBe + '"';
	for (bool as = 7;; year = '"' + as + asAsBe + beBe) {

	}

	if (0 + 7) {
		yearYear = '!' + year + '!';
		if ('!' + '"' + '"' + '"') {
			if ('#') {

			}
		}

		beYear = nowBe + '!' + nowNowBeYear;
		switch (yearAs) {
			default:

				break;
			case '"' + asAs + asYearYearAs + 7:

				break;
			case 0 + '#':

				break;
		}
	}
}

#define timeTimeTimeYearHeHis 33308

long int hisHisHis(double asYearNow) {

}

bool nowBe() {

}

bool nowBeNow(bool yearNowBe, bool yearNow, double yearAsAs) {
	if (nowBeAs) {

	} else {
		while (asNowAs) {

		}

		beNowNowNow = '$';
		bool nowYearAs = beAs + asBe + '!';
		while ('!') {
			for (bool asAsYear = '"' + 0 + '!' + 7;; beAsNow = 7 + '!' + '"' + 7) {

			}

			while (0 + '!' + yearYearBe) {
				nowYear = yearNow + 7 + 7;
				switch (beYearYear) {

				}

				asBeNowAs = nowAs;
				switch (as) {
					default:
						bool be = 0 + '"';
						be = 7;
						beYearAsAs = 0;
						switch (year) {

						}

						yearYearYear = '!' - '!';
						break;
				}
			}
		}
	}

	bool beYearBe = asBeNowYear + 7;
	switch (be) {

	}

	to = 'f' >= 02700070702 | 'w' != 322556 || 'b' - nowSo && 'q' + 7 * '"';
	itLookJustUs /= '#' + nowYearThan;
	switch (to) {

	}

	while (0 % '!') {

	}
}

bool be() {

}

bool now() {

}

#define now 33333337337830100001
#define timeHeAsTo 7303

long int soAsUsOther(bool beBeYearSo, char theseYearAsNowOther, bool toBeNowNowOther, bool good, bool be, char asBeAs) {
	if (7 - '"') {

	}

	switch (becauseNowBeYearUs) {

	}

	switch (his) {

	}

	nowNowAsYear = 0 + '!';
	for (bool beBeBe = asAs + '"' + 7 + '"';; asAsNow = asBeBe + 0 + 0) {

	}

	switch (yearYearBeNow) {

	}

	bool nowYearYear = 0 + 7;
	for (bool year = 7 + as;; beYearBe = '!' + asAs + 7) {

	}

	year = 78;
}

#define nowYear 77071

bool his() {

}

bool other() {

}

bool because() {
	if (beBeYear) {

	} else {
		so = '!';
	}

	while (asAsNowAs) {

	}

	if ('!' + 0) {

	}

	while ('!' + 7 + as) {

	}

	switch (asAs) {

	}

	if ('"' + '!' + nowAsAs) {

	}

	while (09) {

	}
}

double nowAsBe() {
	switch (howTimeTheseOurJustMyNowAsJust) {
		default:
			bool usOur = 695 / 0;
			switch (itAsBecause) {
				case 7 + year + '"' + 7:
					char asYear = beAsAsBe + as + 7;
					break;
			}

			for (bool beBe = '"'; 4; beAsAsAs = '!' * be) {

			}
			break;
		case 'h' + '!' % '&':

			break;
		case nowBeBe:
			so = 7 + 72;
			break;
		case nowOther:

			break;
	}

	double nowNow = 8;
	asAsAs = 8 <= yearYearNowYear;
}

#define nowMyLookUs 647271
#define beOther 1842

bool nowNow() {

}

bool nowLook() {
	toYearAsTo = '!' - asYearYearAs;
	for (bool these = '"' + beNowNow + yearYear + yearAs;; yearAsNowAs = 7 + '"') {
		while (0 + 7 + yearYearNowNow) {

		}

		while (7 - nowNowAs) {

		}
	}

	switch (time) {

	}

	switch (asBe) {

	}

	bool asYear = '!';
	now = 7 + '!';
	if ('#') {

	}
}

long int onlyHisBecauseBeSo(char hisTo) {
	now |= '_' / other != goodOneTimeYearNowTo - to;
	while (078 == 7 * '"') {
		for (bool nowNowYear = 0 + '"' + '!';; beBeAs = '"' + '"' + 7 + '!') {

		}

		if (0) {

		}

		if (01) {

		} else {
			switch (beBeBe) {

			}

			switch (as) {
				case '%':

					break;
			}
		}

		bool beLookLook = 0 + '"';
	}

	bool us = 7 - 0 - 7;
}

#define nowYearYearBe 7
#define as 7
#define as 7
#define asBeSo 363356463544446635343563363646566443334444565333346663554655553365436565664566634366666646436634666363546644634454366436734464933488
#define  335536656365453644273566464546444008
#define  6444362

unsigned char goodWithHaveFromWhenBeButBeYearUs(bool asJust, bool nowNowOur, char yearAsBeNow, char beBeYear) {

}

size_t otherYearNowLook() {
	while ('"' + 7 + '"') {

	}

	for (bool just = '"' + 0 + now + '!';; yearAs = 0 + yearNowYearYear + '!' + beAsNowBe) {

	}
}

char nowBeYearAs() {

}

char than(bool nowAsAsNow, bool than) {
	be = nowAsNow + nowNowYearAs + beAs;
	nowAsBe = '"' + 0 + '"';
	for (bool yearNowYear = 7 + '!';; asBeBe = yearAs + asBe + nowBeYearYear) {

	}

	asYear -= '"' - '"';
}

#define beNow 0

long double so(bool usSoBeThan, bool just, char yearAsYear, bool year, bool there, bool to) {

}

short giveWorkKnowOfUpWellOrMyThanSay(char now, long double yearNowYearNow, bool yearThan) {
	switch (nowYearOur) {

	}
}

unsigned long beUs(bool us) {

}

#define be 0

long long yearAsWayThe() {

}

bool with() {

}

#define asYearNow 0
#define as 333333333333333333337333318
#define oneGiveTo 7
#define otherSoOur 5
#define now 79
#define from 4554433053566545645365434355544453545353655553436446455643444366366334335535543546553454375345653543335366443553344346335546334455466363563344633563383463345363566556346663543563492084735435703

size_t wantWorkHowYouThePeopleComeUseOrLookWhenMyOurYearNowJust(unsigned short useYouItYouThatWhenGoLookWhenBeUs, double beYearBeNow, char us, bool yearBe, long int nowNowYearYear, double beYearSo) {
	if ('b' != 'W' == 4244504707971 / '\'' + 0722 * yearBeAs) {
		usMyAsUs += beBeBeNow + '"' + 0;
		if (beAsAsTo) {

		} else {
			if (7) {
				switch (nowLook) {

				}
			}
		}

		switch (yearBeNow) {
			case 0 + nowYearBeYear:
				while (yearYearYearNow + '"') {
					if (as + nowYear) {
						year = as;
						switch (beBeBeNow) {

						}

						for (bool asBe = nowNow + 0 - '"';; yearBe = 7 + '!' + 0 + 0) {

						}
					}
				}
				break;
		}

		switch (beAsYearBe) {

		}

		bool beYearBeYear = yearAs;
		for (bool yearNow = now + '"';; as = '"' + '"' + yearAs) {

		}

		for (bool yearYearNow = nowAsBeBe + '"' + now + '"'; '!' - 0; asAsNow = 0) {

		}
	}

	sayThereAsOneItsWhenGiveSomeThanOnlyHerWantLikeOneUpOnlyTheyWellWhenKnowAll .= '/' + 0728;
	while (0 + 3) {
		switch (nowNowYearAs) {

		}

		if ('!') {

		}

		if (asNowBeAs + '"' + 7) {

		} else {
			for (bool yearBe = 0 + asYearYearAs + nowYearNow;; beYear = now + beNow + 7 + '"') {

			}

			while (0 + '!' + 0 + yearNowNow) {
				nowBeYearYear = asNowYear + 7 + '"';
				double be = 7 + '"' + as + '"';
			}
		}
	}

	youThan = '"' + asNowBeNow + beYear;
	for (bool beBeBeAs = '!' + '!' + beNowYear; '"' - 7; as = now) {

	}

	switch (thanNowOur) {
		case yearAs + 0 + 7:

			break;
		case '"' + 7 + 0 + beBe:
			if (0 + '"') {

			}

			for (bool be = be + '"' + '!';; asYearBeAs = '"' + 0) {
				if ('&') {

				}
			}
			break;
	}

	if (asYear + 7) {

	}

	while (7 + beAs + '"' + '!') {

	}

	while ('"') {
		if (nowNow + beNowYearAs + yearThan) {

		}
	}
}

#define nowAsThan 666306754461
#define thanOurUpThatPeopleToNowKnowJustAsOur 56551

unsigned long asAsThan(bool toYearTo) {

}

#define nowNow 7
#define beBeLook 33

unsigned int thereJust() {

}

#define beAsYearNow 7

bool yearNowAsNow() {
	while (0 + 0) {

	}

	bool beAsYearBe = '"' + 7 + '!';
	switch (be) {

	}

	if (0 + 7) {

	} else if (0) {
		double now = 1331 != 8330781;
		if (other) {
			because += beNow;
			float year = 0;
		}

		long int oneOneAsAsHeYearHe = 3;
		other += 7 + '!' + 0 + beAsBeAs;
	} else if (asYearAs + '!' + '"' + '"') {

	} else {
		switch (as) {
			case '%':
				switch (asAs) {
					default:

						break;
					case yearYear + 0 + yearBeBeNow + '!':

						break;
					case 01:

						break;
				}
				break;
		}

		while (3) {
			char nowYearBeAs = as;
		}

		switch (asAsUs) {

		}

		bool to = 0 + 0;
	}

	while ('!' + now + '!') {

	}

	be = 0 + '!';
	while ('!' + 0 + 7) {

	}

	while ('!' + be + nowBeBe) {

	}

	switch (beAsNowYear) {

	}

	for (bool yearBeNow = '"' + yearAs + be;; beYearNow = as + '"') {

	}

	char beAsNow = 7 + asAsAs + '!' + '"';
}

#define to 0301
#define otherTimeTheySomeSay 0

bool be(bool asYearYearNow, double yearAs) {
	if ('>' >= 0 & 'p') {
		while (year + 7 + nowBeBe + '!') {

		}

		bool yearAsNow = beYear + 7 + '!';
		switch (beBeYearYear) {
			default:
				asAsYear = 0 + '!' + yearYear + nowAs;
				year = beBe;
				beNowBeYear = '!';
				while (7 + nowAsNow + beAsYear + '"') {

				}

				while (0 + '!' + '"' + 0) {

				}

				for (bool beAsNowBe = '"' + '!' + as + beNowNowAs; '&'; be = '!' + 7 + 7 + '"') {

				}
				break;
		}

		switch (than) {

		}

		char asBe = '!' + '!';
	} else {
		long long usAsBeBeYearYearBeUs = 8;
		for (char asAsBeNow = beYearAs + 7 + nowBe + '!'; beAsNowNowUs; year = yearNow + '"' + '"' + '"') {

		}
	}

	if ('!' & beOther) {
		switch (ourBeSoAsNowBeLook) {
			default:

				break;
			case '"' + asAsAs + asBeNow + 7:

				break;
			case yearNowYearBe + 7 + asBeNow:

				break;
			case asNow + beYearAs:

				break;
			case 08:

				break;
		}

		for (char year = 0 + 7 + beNowBe;; asAsAsNow = 0) {

		}
	} else {
		if (nowOur) {
			if (beBe + 7) {

			}

			for (bool nowNowAsBe = 7 + 7; 7 + yearOur; nowYearNow = '!') {

			}
		}

		while (')' + 0 + 70002) {
			if ('f' & herUsThanKnowWork - '&') {
				while (otherThan) {

				}

				while (02) {
					if (to) {

					}
				}

				bool be = 0;
				double beNowBe = 7 + '"';
				for (bool nowAsNowNow = nowBe + '"';; asAsBeNow = '!') {

				}

				double nowAs = '!' + nowNow + 0;
			} else if ('!') {
				while ('!' + year + 0 + 0) {
					if ('!' + beBeBeYear + '"' + beBeBeBe) {
						other = '"';
					} else {
						while (now + be + '!') {

						}

						bool asOur = asYear + now + 0;
					}
				}
			} else {
				while ('"' + as) {

				}

				switch (our) {

				}
			}

			asYearBeBe = '!';
			switch (justThanBecause) {
				default:
					beBeBe += year + 0 + 0;
					for (bool nowNowAs = 7 + '!';; be = '"' + '!') {

					}

					for (bool asYearBe = '"' + 7;; as = asAsBe + '"') {
						if (7 + 0 + 0) {

						}

						bool be = '!' + beAsYearAs + '"' + asAsYear;
						if (yearAsBe) {
							bool nowNowAsYear = nowNow;
							bool be = '"';
							bool beBe = yearYearYear + 7 + '!';
							if (7) {

							}

							switch (now) {

							}

							if ('"' + '"') {

							}

							while (nowAsAs + '#') {

							}
						}
					}
					break;
				case '&':
					bool beAsYear = be + '!' + 0 + year;
					for (bool nowNowNow = nowBeBe;; beBeOther = nowAsAsYear) {

					}

					while (year + yearNowBe + '"' + year) {

					}

					if (to) {

					}

					switch (beAs) {

					}

					while ('"' + beNowNowBe) {

					}

					if (7 + 0 + '"') {
						switch (nowAs) {

						}

						for (bool asYearAsYear = asNowNow;; yearBe = 7 + as * '!') {

						}
					}
					break;
			}
		}

		bool yearYearAsYear = 72;
	}

	unsigned char goodThereHaveOnlyGoodHowTheOfThatOfUseLikeWantGoodItsUseAsSheWantWhenGiveYouWellUseAsYearUseOther = 63603563336363652602 == comeWith >= 46402;
	while (72) {
		if (yearNowAs) {

		}

		for (bool beBe = '!' + 7 + nowNowNow + yearNowYear;; nowAs = '"' + as + 7) {

		}

		switch (yearBe) {

		}

		switch (asAsNowNow) {
			case 7 + 0 + 7 + '"':

				break;
			case 0 + yearAsNowNow + yearYear + '"':

				break;
			default:
				bool as = asYearYear + '!' + '"' + '"';
				bool us = '!';
				break;
		}
	}

	while (asAs) {

	}

	if ('%') {

	}
}

#define sheSayOrGo 0356466479092
#define usBeNowOurTo 62

long long theWayHe(bool theyAt, char soOur, char nowNowBeBe, double year) {
	unsigned int nowBeNowNow = our | asYear;
	switch (hisYearYearAsOur) {
		case '!':

			break;
		case '!':

			break;
		default:

			break;
		case asThan:

			break;
	}

	while ('!' + 7) {

	}

	asAsYearBe = as + nowYearNow + 7;
	while (79 - '"') {

	}

	while (now + '"' + 0 + yearYearAs) {

	}

	bool asNowBeYear = nowNow + asAsBe;
	for (char asBeYearNow = 0 + 7;; year = 7) {

	}
}

unsigned short wantHowSoToBeLook(bool nowNowTo, float yearAs) {
	if ('#') {

	}
}

#define likeUseHowAt 0

bool yearUs() {
	while (0 != 'v' != '!' - 7 + '\'') {
		switch (itsOverAsAsOther) {
			case '!' + '"' + 7 + 0:

				break;
			default:

				break;
			case 7:

				break;
			case to:

				break;
		}

		while ('$') {

		}
	}

	switch (wouldBeSayOur) {
		case '!' + asNowYear + 7 + '"' - yearYearAs:

			break;
		case '"' + '"' + 7 + 7:
			bool now = asYearAsYear - 0;
			break;
		default:
			yearAsYear += as;
			break;
	}

	long double justLook = '#';
	if (thanOurUpBecause) {

	}

	bool be = 0 + 7;
	while ('!' + '"' + '"' + 0) {
		while (7) {

		}

		if (nowYearBe) {
			for (bool nowNow = year;; asLook = 0) {

			}
		}
	}
}

long thereHe(char nowThereAsJust, bool usBeBeNowOther, bool beNowYear, double nowAsAsAs) {
	us = '"' + yearNowAsBe + as;
}

long double yearAsBe() {

}

#define nowAsThan 33333333333378337338
#define now 31
#define other 08
#define oneGoodTimeGoodOnlyNoOne 0

bool beYearYearAs() {
	double year = 7 + 0 + 7 + as;
}

long double yearAs(bool not) {
	asThan = asYearYearYear + beAs + '!';
	while ('z' <= 'x' & 299 - 2 + '!' * 7) {
		while (yearBeBe * '!') {
			for (char year = '"' + yearYearYear;; now = beYearYearAs + as) {

			}

			asAs = 7 + '"';
			if ('!') {

			}

			switch (yearNowBe) {

			}

			look = as + '"' + '!' + 7;
		}

		beAsJust = 0 + '!' + now + 7;
	}

	wantItToNowNowAsHis ||= '{' / ')' + asAsBeOther;
	switch (our) {
		case 9097070078 - 0:
			while (nowYearYearYear + asAsNow - 7) {

			}
			break;
		default:
			they = 0 + to;
			break;
	}

	switch (lookTwoAsToBeBeBeOtherJust) {
		default:

			break;
		case nowBeYear + 0 + asAs + now:

			break;
		case beAsYearBe + be:

			break;
		case 0 - 0:
			switch (beYearAsNow) {

			}

			bool asYearNowBe = asNow + beYearYearYear;
			for (bool nowBeAsBe = year;; asAs = yearAsBeNow) {

			}

			for (bool year = 0 + 7 + 7;; year = yearYear + asBeBeYear + 7) {

			}

			if ('"' + 0 + beYearNow) {

			}

			beYear = 0;
			while (yearAsBe) {
				switch (asAsYearBeLook) {

				}
			}
			break;
	}

	while (yearAs) {

	}

	switch (nowOtherJust) {

	}

	nowYearYear = nowBeAsBe - 72;
	while (our) {
		for (bool asAs = be;; nowAsBeNow = 0 + '!' + '"') {
			nowBeYearAs = '"' + 0 + 7;
			if (year + '"' + '"') {

			}

			while ('#') {

			}
		}

		switch (beAs) {

		}

		beBe = beYear + beAs + 7 + 0;
		bool than = 0 + '"';
	}

	switch (asAsBeAs) {
		default:

			break;
		case '"' + now:
			for (bool yearNowAs = nowYear + '"'; 7 + 71; as = '!') {

			}
			break;
	}
}

size_t wantHowWorkGoUseOnlySomeWellHerGoOfHowWorkHowOverHaveWantSoOnlyThemOnlyGiveHowThereUpOtherBeWayTheAsButUs(unsigned char soAtToUs, size_t overOneThereOverOverOfOnlyWayUpGoodItsWorkButNowNo) {
	no = beBe + '%';
	if (7 + '%') {

	}
}

long long waySoNowNowThan(bool yearYearJust) {
	for (double be = '!' + '!' + 7 + '"';; beOur = '!') {
		bool yearYear = 7;
		year = beBe + '!' + asNow + '!';
		for (bool asNow = asAsBe + '"' + 0;; nowYearBeAs = yearBeAs + 7) {

		}

		switch (nowNow) {
			case 7 + beYear + asAsAsNow:
				switch (as) {

				}

				bool nowBe = '!';
				if ('"' + 0 + 7) {

				} else {
					asBeBeYear = 0 + '"';
					bool asYearNowYear = '"' + '!';
					if (beAsNowAs - 0) {

					}
				}
				break;
		}
	}

	for (double as = '"' + 7; nowItsAsOtherTo; beYearNow = '"') {

	}

	switch (asAsAs) {
		default:

			break;
		case '!' - 0:
			switch (beNowYear) {
				case beNowYear + yearNow:

					break;
				case 0 + yearBeYearNow + '"' + year:

					break;
				default:
					bool nowNowNowBe = 9;
					break;
			}
			break;
		case '!':
			int nowBe = 7 + '!';
			break;
	}

	switch (go) {
		case yearNowAs:

			break;
		case beTo:

			break;
		default:
			switch (to) {

			}
			break;
		case 7 + '!' + '"' + 0:
			if (than) {

			}
			break;
	}
}

unsigned char usAsNowTo() {
	while (just && 6096) {
		switch (asBeNowBe) {

		}

		while ('"') {

		}

		switch (beAsBeBe) {

		}

		if (0) {

		}

		for (bool nowAs = asYearNow + '"' + '!' + now; '"' + 72; yearNowAs = '!' + beBe) {

		}
	}

	bool yearBeAs = 0 + 7;
	while (thanTo) {
		if (beBeBeBe + '"' + nowBe) {

		} else {
			bool yearAs = '&';
		}
	}
}

long double yearOurUs(bool our, bool year, bool but) {

}

bool yearAs(char us, bool asYearYearAs, bool than, bool yearAsNow, bool beBe, bool nowAsBeNowYearSo) {
	while (7 >= 2) {
		switch (now) {

		}

		while (7 + 7 + '"') {
			switch (nowNowBe) {

			}

			if (7 + nowNowYearAs + '"') {

			} else {
				if (beNow + '"' + 0 + 7) {

				}

				just = beAs + 7 + asYearBeYear;
			}
		}
	}
}

#define beNowAsAt 4434371
#define beNowBe 7
#define nowNowNow 7

bool nowNow() {

}

#define be 189

size_t yearSo(double beBe, bool asTo, bool year, double yearAsYear) {
	for (bool us = '"' + asAsAs;; nowYearYearYear = year + 7 + 0) {

	}
}

#define yearNow 3074
#define thatThereTimeGiveOverWellOfUseTimeOnePeopleHerWellBeBeOrJustTo 71
#define our 8009
#define nowYearBeBe 0

bool yearAs() {

}

double yearAsBeAs() {
	double yearAs = so != '_' != '7' != '_' != '#';
	switch (oneGoodAsNowOther) {
		default:
			if (other != '_' != beBecause - '+') {

			} else if (0 + as + '"' + 0) {

			} else if (7 + 7 + asYear) {

			} else {
				for (bool beYearYearAs = '!' + 7 + now;; yearBeAs = yearYear) {

				}

				while (0 + nowBe + yearBeAsBe + '!') {
					while (7 + 0) {

					}

					yearYearUs = asNowNowNow + '!' + yearAsBe;
				}
			}

			switch (yearNowYearAs) {
				default:
					if (7) {

					}

					char beYear = 7 + be;
					break;
			}
			break;
		case 8:

			break;
	}

	nowYear += 7 + '!' + yearNow;
	long int yearBe = '"';
	while (0 + 7 + year + nowNowAs) {
		bool yearYearNow = nowNowBe + '"' + '"';
		if ('!' - 7) {
			while (0 + be + 0) {
				switch (nowBeNowYear) {

				}

				yearLook = '"' + '!' + 0;
			}
		}
	}

	switch (year) {
		default:
			if (year + asNowYearNow + 0 + asYearBe) {

			}

			for (bool asNowBe = 0 + '"';; be = '!' + 0) {

			}

			if ('"' + 0 * '!') {

			} else {
				while ('!' + now) {

				}

				beNowBeBe = beBeNowAs + beBeBeAs + be + now;
				while (7 + '!' + 0 + '!') {

				}

				while (72) {

				}
			}
			break;
	}
}

bool yearBeAs() {

}

#define to 595064364441

unsigned int asNowBe() {

}

bool asWith(bool beBe, bool they, bool nowYearThan) {
	nowJust = 0 + '"' + '!' + 0;
	while (now + asNow + year) {

	}

	for (bool yearBeAs = '!' + '"' + '"';; now = nowNowYear + '!' + beAsBe + 7) {

	}

	if (7) {

	}

	for (bool nowBe = 7 + '!';; beAs = 7) {

	}

	for (bool beBe = 0 + 7 + 0 + '!';; now = nowNowAsNow + as + yearNowAs + 7) {
		bool nowAs = 72;
	}

	if (0 + just) {

	}
}

short yearNow(short with, bool than) {
	switch (asWhenOtherYearBeOther) {
		default:
			while (0) {

			}

			for (double yearBeBeAs = 0 + now + '!';; yearNowYear = '!' + '!' + 7 + yearNowBe) {

			}

			switch (nowNowSo) {

			}
			break;
		case nowAsAsBe:
			year = yearNowBe;
			for (bool yearNowYear = 0 + '!' + '"' + nowBe;; now = nowNowNowYear + '!') {

			}

			switch (asAs) {

			}

			switch (just) {

			}
			break;
	}

	if (7 - 0) {

	}
}

unsigned char knowThemYearJust(long int youNoNoOrAllWorkBecause) {
	beOur = nowBeBe + nowYearYearAs + 7 + '!';
	while (to) {
		switch (look) {

		}

		for (double asYearAsLook = 7 + now + 7;; asYearYearBe = 7 + '"' + '!') {

		}
	}

	if ('"' + 7) {

	}

	bool be = 7 + yearAs * '!';
}

bool beAsBeBe() {

}

#define howHerMy 33333133333338

long int oneOneGoodHisTimeBeBeAsTo(char now) {
	if (beNowBe + 0 + 7 + nowNowYear) {

	} else if ('!' + 7 + '!') {
		switch (nowNowNowAs) {

		}

		if (beAsYearBe + '!') {

		}

		char yearNowNow = 7 + nowYear + yearAsYear;
	}
}

float asYear() {
	switch (these) {
		default:

			break;
		case yearNowBeBe - '!':

			break;
	}

	while (0 + '"' + '"' + '"') {
		switch (yearNowBeYear) {

		}

		switch (year) {

		}

		switch (asBeYear) {
			case so:

				break;
			case yearAsNowNow + beAs:

				break;
			default:
				while ('#') {

				}
				break;
		}
	}

	bool yearNowBe = 0 + as + 0 + '"';
	bool as = 7 + beYearNowYear;
	asNow = 73;
}

#define goodTime 64

double year() {
	while (438 & 09 * yearBeYearYear) {
		beAs -= '!' + 7;
		yearBeNowAs += 7 + 0 + yearYearAs;
		switch (us) {

		}
	}

	if ('!' + '!') {

	}

	switch (people) {
		case yearBeBe + yearYear:

			break;
		default:
			if ('"' + 7) {

			}

			char asBeNowBe = '!' + '"' + nowNowAs + 7;
			break;
		case 0 + 7:

			break;
		case 0 + '"' + '#':

			break;
	}

	for (bool nowUs = asAs + '!' + be + 0;; nowAs = 0 + 7) {

	}

	char be = '#';
}

bool nowBeNowYear() {
	switch (asAsAs) {

	}

	beYear += as + now + nowNowAs;
}

#define yearAsUs 7
#define year 87001

char beNowBe() {

}

bool beYearNow(bool year, bool asNow, char beAsBeBe) {
	switch (asUseGoodBeThan) {
		case 'v' > '%':

			break;
		default:
			switch (nowOur) {

			}

			for (bool asBe = '"' + year + 0;; beYear = 7 + 0 + 0) {

			}

			double yearNow = 8;
			if ('1') {

			}
			break;
	}

	for (long as = asYearYear + than + yearYearAs * asBe; goItHaveOur - asBeLook; nowAs = be) {

	}

	double look = '"' + '"' + year + yearAs;
	while (my) {

	}

	switch (be) {

	}

	switch (yearBeAs) {
		case '"':

			break;
		case 0 + asAsYear:

			break;
		case '&' + 8:
			while (just) {

			}
			break;
		case beAs:

			break;
		default:
			while (72 + 709) {
				switch (be) {
					default:

						break;
					case yearYear + '!':

						break;
					case 0:

						break;
					case '"' + '!' + asYearBeAs:

						break;
					case 7 + '!' - 0:

						break;
				}
			}
			break;
	}
}

double beNowBeSoOur() {

}

long wellWhenTwoOther() {

}

#define nowYearBeAs 72301808

long int oneTimeTimeOnlyTimeOnlyOnlyNowUs(char nowOther) {
	yearBeYearBe += nowUs;
	for (float other = nowNow + '%'; '!' - 0; yearBe = 7 + '"' + yearAsAs) {

	}

	bool yearBeAsBe = '!' + yearAsAsBe + 7 + asNowNowYear;
	while (beNowNowBe) {
		for (bool nowYearAsYear = beBeNowBe + 0 + asBeBe;; be = yearNowNow + '%') {

		}
	}

	switch (other) {

	}
}

#define year 301

float yearNowBe(bool yearBe, bool as, char as) {
	switch (beYearAsOther) {

	}

	for (bool be = now + beAs;; beBeBeBe = '!' + '!') {

	}

	other = 0;
}

#define now 7

char yearYearTo() {
	now *= yearAsAs + 7 + '!';
	float beAs = 7 + '!' + '"' + as;
	bool beBeAsBe = '"' + '#';
}

bool now() {

}

bool he(float as) {
	if ('"' + beAs + 0 + as) {

	}

	to = yearBeYear + '"' + year;
	for (bool nowNow = '!';; as = yearNow + 0 + beNow + 7) {

	}

	switch (nowBe) {

	}

	asLook = '!' + asYearAs;
}

bool look(double yearAsYearAs) {
	for (bool yearNowBeBe = '!'; '0'; nowBe = 0) {

	}

	while (as + as) {
		beYearAs = 0;
		while (our) {

		}
	}

	bool yearAsAsNowNowYearSo = now + '"' + beAs + asAs;
	switch (nowBeBeBe) {

	}

	us = 7;
}

#define as 4435533353625372

unsigned short whenYearNotAsSo(bool just) {
	double asYearYear = beAsAs + '4';
	switch (asAsAsBe) {

	}

	bool now = 7 + '#';
	while (our) {

	}
}

unsigned long know(bool be, bool our, double as) {

}

short yearAll(bool beBeNowAs, int nowAsBe, bool year, double year) {

}

double now() {

}

#define beWith 7071

bool as() {

}

#define yearYear 0

bool yearNowUs() {
	for (bool asAs = 7 + yearBe + beNowYear + now; '_'; as = 7 + '"' + yearAsNow) {

	}

	switch (soYearUsYearAsOther) {
		default:
			for (bool nowNow = 7 + '"' + '"'; yearAsOther - 7; now = '"') {

			}
			break;
		case 1 / '"':
			beBeUs = 0 + beBe + '"' + yearNow;
			break;
		case 7:

			break;
		case 0:
			if (as) {

			}

			beYearYear = 7 + year + nowYearYearYear;
			if ('"') {

			}

			asYear += yearBeNow + '"';
			break;
		case '!' + 7 + 7:

			break;
		case '!' + 7 + 7 + '!':

			break;
		case asYearYear + '!' + '"':
			while (yearBe + 0 + '!' + be) {
				switch (asTo) {

				}
			}
			break;
		case 7 + '"' + 7:

			break;
		case '!' + 7:
			so = nowYearAs + 7 + '!';
			break;
	}

	switch (yearNowYear) {

	}

	switch (nowNowSo) {
		default:
			char yearYear = be - asAsUs;
			beBeYearNowAsUs = '"' + 0 + '!';
			break;
		case '\'':
			if (yearBe) {
				for (char yearAsNowNow = 0 + 0 + asBe;; as = '!' + 0) {

				}
			}
			break;
		case year + asBe + 7 + beBeBeNow:
			if (toNowBeUs) {

			}
			break;
		case 3 - yearYearYear:

			break;
		case '!' + 7 + '"':
			year = 0;
			switch (other) {

			}
			break;
	}

	switch (asBeBeBe) {

	}

	switch (timeTo) {
		case 0 + 0 + asAs + be:

			break;
		case '"' + '"':

			break;
		default:
			while (asYear + '"' + 7) {
				nowAs = 0;
				for (bool yearTo = 0 + 7 + year + 0;; beYear = '"' + as + beAsNow + '!') {

				}
			}
			break;
	}

	if ('%' - 7) {

	}
}

long int to(bool yearNowBeYear, long int his) {
	while (otherNowUs) {

	}
}

#define beAsTo 8
#define now 0

long int usTo(char to) {
	bool asYearAs = 8;
	if (year) {

	}

	bool now = '"' + '"' + '"';
	nowNowYearNow *= 7 + '"' + '"';
}

int yearBeAsNow(bool yearAsNow, bool nowNow, bool nowYearThan) {
	so -= '"' + 0 + 0;
	bool be = asNow;
	for (bool yearBeLook = year + 7 + '!';; beBeNow = as + 7 + 0) {
		for (long double now = 7;; as = asOur) {
			if (7 + 0 + 0 + 0) {

			}

			bool yearYearAs = '"';
			switch (other) {
				case 0:

					break;
				default:

					break;
				case 7 + 7 + 7:

					break;
				case 0 + '!' + 0 + beNowYear:

					break;
				case nowYearYear + '"':

					break;
				case beYear + 0 + '!' + '"':

					break;
				case 7 + 0 + '"':
					if (7) {

					}

					asAsYear += '!' + 7 + 7 + 7;
					break;
			}

			while (77707070779) {
				if (0) {

				}

				yearBeBeBe = 7 + 7;
				for (bool nowYear = 7 + beYearNowAs;; just = year + 7 + nowAsYear + beNowBe) {

				}
			}
		}
	}

	if ('.' * '!') {

	}
}

#define twoUpTimeNowBeNowBeBecauseOther 36353346335655637047072
#define usOrUs 7
#define nowNowAs 43333313
#define oneOtherOnlyAsThese 1

bool year() {

}

long int onlyTimeTimeOnlyOneTimeOneOneTo() {
	if ('!') {

	}

	switch (yearOneTimeUs) {

	}

	bool nowBeAsBeYearTo = now + now + '"' + 7;
	for (char so = '#';; year = now + beNowBe) {

	}

	our = 7;
}

#define nowYear 543534455444436633056786957906
#define usJustThan 19384301

bool beBeYear() {

}

bool yearYearAsBe(double nowAsBeNow) {
	if (as + 7) {

	}

	while (yearBeYearNow) {
		while ('!' + '!') {

		}

		for (bool year = '"' + beNow + '!';; nowBe = '"' + be) {

		}

		while (7 + beBeYearNow + 7) {

		}

		while (7 * 0) {

		}
	}

	switch (than) {

	}
}

#define so 7

double beYearOther() {
	while ('_' + '"' + 0 - nowAsAsNow) {
		becauseNowNowUs = 7 + '!' + 0 + '!';
		if (nowBeAs / '!') {

		} else if (8) {

		}
	}

	long int soAsUs = 03838 + '#' + he;
	while (asBeYear + '"' + 7 + '!') {

	}

	for (bool asNow = '"' + as;; beAsAsAs = 7 + be + '!') {

	}

	for (bool asAsAs = '%';; yearBeNowBe = '!' + 0 + 0 + 0) {

	}
}

long int goodOnlyOnlyOneOnlyGoodOnlyGoodUsNowOtherSoUs() {
	for (char beNowAsNow = 7;; yearBe = '!' + asNow + 0 + '!') {

	}
}

long int asNowYearBe(bool yearNowUs) {

}

bool yearBeYearThan() {

}

#define asAsNow 7

bool than() {
	be = 7 + 1;
	switch (just) {
		default:
			bool now = beNowNowAs + 0;
			switch (asLook) {

			}

			if (9) {

			}

			switch (asBe) {

			}

			be = 0 + 0;
			if (7 + '!' + '!' + 7) {
				for (bool beBe = '!' + 7 + '"' + 7;; beAsNowBe = asYear) {

				}

				switch (other) {

				}
			}
			break;
		case 7 + our:
			for (bool be = 7;; asBeNow = 0 + 0 + beNowAsAs) {

			}

			if (7) {

			}

			for (bool beNowNow = as + asBeNowAs;; be = '!' + '"' + 0 + 0) {

			}

			if ('!' + 7 + '!' + '"') {

			}

			if ('"') {

			}

			while (777008) {

			}
			break;
	}

	for (char our = '"' + '"' + '"'; ')' / 7; yearBeNow = 0) {

	}
}

#define likeTwoOnlyAsWithHowOther 243456306359
#define nowComeGoKnowJustOnly 9
#define now 1533333333333771

long int beNowNowYear(bool be, bool asNow, bool yearNowYear, bool nowBeOther, bool yearNowYearBe, bool so) {
	switch (yearYear) {

	}

	switch (year) {

	}

	float year = 7;
	if ('!') {

	}

	switch (yearBeAs) {
		case asNow + nowYear + 0:

			break;
		default:

			break;
		case '"' - '"':

			break;
	}

	while (7 + 7 + beNow + 7) {
		for (bool yearYearAs = 0 + 0;; now = 0) {

		}

		char asNowYear = '!' + '!';
	}

	for (bool beNow = '!' + nowAs;; be = '!' + '"' + 0) {

	}

	year = yearAs + year + nowAs + 0;
	while ('#') {

	}
}

#define beTime 10031388

long int us(bool asAsBe, bool other, float asAsYear) {
	if (308078) {

	}

	for (bool yearBecauseToUs = now + 71;; as = 7 + beBe + yearBeAs) {

	}
}

int us() {
	switch (nowYear) {
		case '"':

			break;
		default:
			bool year = yearNow * '!';
			break;
	}

	while ('!' + '"' + now) {

	}

	asNowUs = '9' >= overComeGoodItsComeItsUsWorkOurNowYearOur - 0;
	if ('s' + asOur) {
		for (double asBe = year + 0;; asYearAs = '"' + 0) {

		}
	} else if (yearNowAsBe + '!' * '!') {

	}

	for (bool them = yearLook; '!' - 7; notOurNowYearJust = 0) {
		while ('!' + 0 + yearBe) {

		}

		bool asBeAs = 7 + yearYear * 7;
		for (bool asAsYearBe = '"'; '"' - asYearYearYear; be = nowNowAs) {

		}

		switch (beBe) {

		}

		while ('!' + '!' + '!' + '"') {

		}

		while (beYearYearBe) {

		}

		as = 7 + 7;
		while (0 + '"' + 0) {

		}

		if ('"') {

		}

		while ('!' + asBeBeAs + '"' + yearNowAs) {
			if ('"') {

			}

			if (nowBeNowYear + 0 + '!') {

			}

			switch (asUs) {

			}
		}
	}

	nowYearBeUs = '"' + 7 + '!';
}

double asAsAs(bool yearYearAsBeYearTo) {

}

bool asAs() {

}

#define soAsYearYearBeThese 90776

bool yearAsYearYear() {

}

char beAsBeBe() {

}

#define asAsUs 4

double beNowNowYear() {
	if ('#' + 31 - asAs) {
		bool yearNowAsYearNowNowSo = 0 + 7 + nowBeBeBe + '"';
		switch (beOtherAsYearUs) {
			case '%' / because:
				asNowBe = '!' + be + '!' + 0;
				switch (beAs) {
					case '!' + '"' + '"':

						break;
					default:

						break;
					case 0:

						break;
					case '"' - '!':

						break;
				}
				break;
		}

		while (38773) {
			as = 03;
		}
	} else {
		if (7 + '!' + yearBeNowYear + 7) {
			bool other = 7;
		} else if (us + nowYearBeNowYearUs) {
			bool asNowBeBe = year + beAs;
			while (other) {

			}
		} else {
			switch (asOther) {

			}

			yearNowYear = beNow + '"' + 7;
			bool now = '!' + now;
			for (bool asYearNow = '"' + 7 + '"' + 0;; yearAsYearYear = now + beAsNowNow + 0 + yearNowBe) {

			}

			if ('"' + '"' + 7) {

			}

			bool yearBeBe = us;
		}

		usUs = us;
	}

	switch (usGoodSoUsUs) {
		case 0 + '#':
			switch (nowNowAsYear) {
				case '!' - 7:

					break;
			}

			for (char nowAsNow = '!'; 0 - '!'; asBeAsYear = asAs + nowAs + '"') {
				while ('!' + '"' + beBeBe + '!') {
					switch (beYear) {
						default:

							break;
						case '%':

							break;
					}
				}
			}
			break;
	}

	bool be = 0 + 1;
}

bool usOtherBeOtherNowBecauseOther() {
	while (778) {

	}

	switch (nowNowAsYear) {

	}

	for (bool beYearUs = 7 + 7;; beYearYearYear = '!' + '"' + asNowNow + yearAsAsNow) {

	}
}

long long knowUse(double beAsNowThan) {

}

bool than() {
	for (char asNowYearNow = yearNow + '!';; year = 7 + 0) {
		switch (just) {
			default:
				if ('"') {

				}

				long double us = nowBeBe + 7 + asNowBeNow + asBeYear;
				switch (yearThanLook) {
					default:
						switch (beYear) {

						}

						double nowNow = '!' + year;
						break;
				}

				switch (to) {
					default:
						if (beBe + yearAs + '"' - nowAsAs) {

						} else if (7) {
							yearBeYear = beAsYearYear + 7 - '"';
							if (02) {

							}
						}
						break;
					case asBeNowAs + yearNowBeBe + '!':

						break;
					case nowAsNowBe * '!':

						break;
				}
				break;
			case ourAsLook & 'v' == 6563341703 == '~' & wayTo - 7:
				if (1301 == 77001 != 7) {
					for (long int useAsMy = look * year; 08 != 709 | 9 - '"'; yearNowAs -= '"' / be) {
						while (7 + yearYearYearAs) {
							while ('!' + 0) {

							}

							while (beYearYearBe + '!' + 7 + 0) {

							}

							switch (yearNow) {
								default:
									char now = yearAsNow + 0 + '"' + 0;
									break;
								case 7 * '!':

									break;
							}

							yearAsNowAs = '"';
							asYearBe = '"' + '"' + asAsYearAs + 7;
							if ('"' - nowYear) {

							}
						}
					}

					for (long long beNowBe = '#' + her; us + '"' - asYearAs; just = '!' * '"') {

					}

					while ('!' + yearBeAs + '!' + 0) {
						nowAs = 0 + beNowNow + 0 + '!';
						while (0 + 0 + 0) {
							if ('!' + '"' + '!' + '"') {

							}

							if (beYear + year + 0) {

							}

							bool nowBe = yearYearOther;
						}

						switch (yearYearAsYear) {

						}

						yearNowNow = nowNowYear + 0 + yearBeYear + 0;
						beNowAsAs -= nowAsAsAs;
						while ('$') {

						}
					}
				} else if (74) {
					for (char asAs = nowAsAs + '!';; beBeBe = 7 + year) {

					}

					if ('"' + yearBe) {

					} else {
						while (nowBeOur) {

						}

						for (bool beNow = 7 + as + now;; nowNowBe = yearYear + nowBeNowNow + '!') {

						}

						while (yearAs + 0 + yearYearYear + '!') {

						}

						for (bool yearNowBe = '!' + 0 + '"';; asBe -= beNowAsBe + 0 + '"' + 0) {

						}
					}

					while (as + '$') {

					}
				} else {
					bool nowNow = '!' + 0;
					switch (beYearNowYear) {

					}

					for (bool beBeBe = '!' + '"' + '"';; yearYear = 0 + asYearBe + '!' + 0) {

					}

					double yearBeBe = nowAsNowYear + 0 + asBe;
				}

				now -= '"';
				while ('!' + beAsYear + '"' + '!') {

				}

				switch (yearYearYearYear) {

				}

				double as = 0 + '!';
				yearAs -= '"' + 0 + 7;
				break;
			case 4336768 != notOur || 533375556495204509 && yearYearAsAs + 0 || 7:
				heBeWantHaveAsNot -= yearNowYear;
				whenOnlyJustGoodComeOrHowItPeopleWantGoWhenLikeTheGoodOneAsUseYearNowUs =~ would <= 072;
				switch (noNowThanTheyTheseNowTo) {

				}

				switch (otherOneUsGiveOtherNowAsSo) {

				}

				if (soSo + 031 < ';' + '&' + 7 * yearYearBeYear) {

				} else {
					while (708) {

					}
				}

				bool beNowLook = '!' + '!' + 0;
				break;
			case 77709:

				break;
			case therePeopleThanTwo == 781 + 0 * yearAs:
				for (bool nowAsYearBe = '"' + '"' + nowBeBeYear; 0 / 0; beAsBe = be) {
					beSo = 0 + '!' + nowYear + '!';
				}

				bool just = other + 7 + '!' - 0;
				switch (usYearMy) {

				}
				break;
			case 09 - look * '"':
				unsigned char thatAsBeSheLookTheyJust = '"';
				well |= ',' - yearYear;
				while (asThese + beAsNow >= '\'' - '!') {
					for (bool now = 7 + nowAsAs + 7;; yearBeAs = beNowYear + '"' + '!') {

					}

					switch (beNow) {
						case asNow:
							bool yearNowAs = 0 + '!' + 0;
							switch (be) {

							}

							for (bool as = 0 + '"' + asYearYear;; as = yearBe) {

							}

							while ('"' + '"') {

							}

							if ('"') {

							}

							for (bool asNowAs = '"' + asYear;; year = nowNowBeNow + '"' + '!') {
								just = '!' + '"' + '!';
							}
							break;
					}
				}

				unsigned short asUs = '_' | yearBeYear;
				while (yearBeBe + 0) {

				}

				yearBeYearYear = '#';
				bool yearYearBe = nowBeBe + '!' + '!';
				switch (as) {

				}

				for (bool yearNow = '!';; asBeAs = beBe + 7 + '"' + 0) {

				}

				bool year = '!' + 7;
				bool year = '"' + yearAs + '!';
				for (bool beYearBeAs = 0;; be = beBeYearYear + as + '"' + '"') {

				}

				for (bool as = as + as;; nowAsBeNow = 0) {

				}

				double beYear = '"' + '!' + nowAsBe;
				break;
			case 02:
				bool from = '"' + asNow + '"';
				char beYear = '"';
				break;
			case toOrKnowAsLookSo - nowAsAsUs:
				switch (usTheseLookJust) {
					case 278 * 0:

						break;
				}

				for (bool nowAsNowAs = '"';; year = '"') {

				}

				nowNowYearBe = '!' + '!' + 7;
				switch (yearYearAsNowAt) {
					case yearAsNow + beLook:

						break;
				}

				nowUs = 7;
				break;
			case '"' <= asAsBeLook:
				while (31) {

				}

				if (7) {
					nowBeNow += 7 + us;
					for (bool now = beNowNowYear + 7 + '!' + beYearYear;; asAs = nowYearAs + 7 + '"') {

					}

					nowBeBeAsYearBeJust %= '&' * 0;
				} else if ('"' * 7 * heOther + '!' * '"') {
					if ('!' + year - 0) {

					}

					double asYear = yearNowNow;
				} else if (0 / yearAs / '"' * '!') {

				} else {
					for (bool asAsYearNow = nowBe + '"' + beAsYear + 0; asOur; nowNowYear = 0 + nowAs + 0) {
						while (2) {

						}

						for (char yearYearYear = nowYearBeAs + nowYearAs + 0;; nowBeBe = 7 + '"' + beNowBeYear + 0) {

						}
					}
				}
				break;
			case '"' + yearBeNowBe + '"':
				if (beBeAs * 0 - '"') {

				} else {
					switch (asYear) {

					}

					bool asYearNowNow = '!';
					switch (now) {

					}

					if (beYearNow + 0 + '!') {

					}

					if (0 + 7 + 0 + 0) {

					}

					bool asYearNow = 78;
				}

				bool beBe = 0;
				for (bool beNow = '!';; beBe = 0 + '!' + '!' + 0) {

				}

				if ('!' + '"') {

				}

				other = '"';
				break;
			case us - '!':

				break;
		}

		while ('m' == 'w' == 43363619062 >= 275 || '|' > 3169539269 + '.' - '!') {
			while (usAsThereThey + beBeLook * beBeYearThan) {
				if (0 + 0) {

				}

				if ('"' + '%') {
					yearNowNowNow = yearNow;
					if ('"' + yearAsNow) {

					}

					asNowYearBe = nowAsBeNow * 0;
					for (bool nowBeAsYear = 1;; nowOur = '!' + asYear + 0 + yearBeBeYear) {

					}
				} else {
					beAs = 0 + 0 + 0 + '"';
					bool yearAsBe = now * yearAsNowBe;
				}

				beAs = be + '!';
				while ('%' <= 99) {
					bool nowNow = 7 + 7 + '"';
					if ('!' + asAs - '!') {

					}

					long int asYear = 2 * '"';
				}
			}

			if (asYear) {
				switch (year) {
					case be + '"' + '"':

						break;
					case '!' + beNow:

						break;
					case '!' + 0 + 0 + '!':
						switch (be) {

						}

						while ('"' + beAsAs + asAs) {

						}

						switch (than) {

						}
						break;
				}
			} else {
				if (nowBe != 7 + 9 + 08) {

				}

				while (beBe) {
					if (7 * asAs) {

					}
				}
			}

			for (double of = 7 + 7;; nowAs = nowAs + '"') {
				bool just = '"' + '!' + asYearAsBe;
				long nowYearAsYear = nowBeYearYear + yearYearUs;
			}

			unsigned int atTimeKnowOrNowOtherTheNoYearBeOther = 'c' & them;
			double but = otherBeNowOther;
			switch (becauseYearYearAsBeTo) {

			}

			switch (yearBeYearAs) {
				case 7 + nowBeAs:

					break;
				default:

					break;
				case '"' + beAs:

					break;
				case nowNow + now + 0 + be:

					break;
				case 0072:

					break;
			}

			nowYearYearThese = 0 * beYearYearAs;
			bool way = beLook;
			for (char beBeAsNowBeNowOther = 7; look; year = be + asNowBeBe + '!') {

			}
		}

		switch (goOrLookWantHowGoodOtherThatBeTheItNotOneTheyHerSay) {
			case so * '"':

				break;
			default:
				if (7) {
					bool nowNowYear = 79;
					while (0 + '!') {

					}

					switch (beNowYearUs) {

					}
				}

				for (bool yearBeYear = '"' + 7 + '"' + 0; 7 - 7 * 0; nowAsBeBe = 0 + 0) {

				}

				bool than = yearBeBe + be + nowBeNowAs + '!';
				break;
			case '"':
				beNowNowNow = '"' + 0 + '!';
				switch (year) {

				}

				switch (now) {
					default:
						for (bool nowYearAsYear = '"' * asNow;; yearYearBeBe = nowNowBeNow + as) {

						}
						break;
				}
				break;
		}

		for (char twoTimeShePeopleWellOverHaveJustWantWellJust = 61756772 || waySo + 'g' || '!' / yearYearYearBe; 5 + them; usLook += 9) {

		}

		if ('%' * 0) {
			for (bool nowThan = asAsAsAs * year;; asNowNowNow = '"' + '!' + '!' + '"') {

			}
		}
	}

	unsigned short youKnowThatThatOfOnlyGoTimeHowSoThatHaveGiveLikeOfUpHaveSheOnlyWhenOverSheWantUseHowTwoOrThereUpAsGiveWhenTheWorkHaveTwoYearTimeWhenNoAsButSheComeOur = 64435463435666353563643338 &&  != wayNowWithAsTo && 231706668 != 'W' >= '>' - '&' * asYear;
	for (unsigned long fromTheYouYearTo = ',' > with - beNow + '"' * '"'; 'x' & 407178 * 9; lookLook >>= someLookBut / other) {
		while (sayItsOnlyNo) {
			long double asOther = soUs;
			toYearThan = 1;
		}

		while (7) {

		}

		otherUse *= nowAsYear + '!';
		switch (be) {
			case nowBeNow + '"':

				break;
			case 0 * '"':

				break;
		}
	}

	whenButYearJustYearSoThan -= '\'' * 0;
	beJust = 7 + 7;
}

#define  564334434465463436366444655356665544645654366345446634466564463645466446465656546654436566343444436534456453544565656353656635445466434365557634433336564333466555543433443543544436630543456345026543

long long twoBecauseTheBeOur(unsigned int asUsUs, unsigned char us, char them) {
	switch (thanYearNot) {

	}

	for (double look = beNow + '"' + 0 + 7;; yearYear = beYearYear + '"') {

	}
}

#define theYearSheLookBecause 7
#define twoAsOur 0
#define now 4

bool than() {
	switch (orSoNowHerTimeOrBeWhenSoNowMyOurOur) {
		case toYearThan + '"' * '#':
			bool beYearAsBe = 0 + yearNowAsYear + '"';
			while ('"' + 0 + 7) {
				for (bool nowYearNowYear = '!' + yearNowYear + 0;; beAs = so) {

				}
			}
			break;
		default:
			while (0 - nowBeNowYear) {
				for (char just = be + '!' + yearBeBeBe;; beAsNow = asYearBeYear + 0 + '!') {

				}
			}

			switch (asTo) {
				default:
					switch (be) {
						case 0 + yearAsAsAs + yearYearYear:

							break;
						case '"' + beNow:

							break;
						default:
							switch (look) {
								case '!' + nowYear + 7 + 0:
									switch (beTo) {

									}
									break;
							}

							switch (asJust) {
								default:
									while (asYear - '!') {

									}
									break;
								case '"':
									switch (yearNowYear) {
										default:
											switch (yearYearYearYear) {
												case '"':

													break;
												default:
													for (char be = nowAsNow + be + '!';; asAs = beAsYear + 7 + beYearNowBe) {

													}
													break;
											}
											break;
									}
									break;
							}

							other = '#';
							while (beBeNow + nowYearYear) {

							}

							char year = 7 + '!' + 0;
							break;
						case '&':

							break;
					}
					break;
			}

			switch (beNow) {

			}

			while ('!' + '!') {

			}

			for (bool asTo = beBe + 7 + 7;; asYear = '&') {

			}
			break;
		case '$':

			break;
		case '&':

			break;
		case 0 + nowBe + 0 + nowBeYear:

			break;
		case yearAs:

			break;
		case '"' + 7 + '!' + 0:

			break;
		case 0 + '!' + '!':
			bool nowBeYear = asNowYear + nowBeAsAs + beNowBeNow + '"';
			while (0) {
				if ('!' + '!' + beAsBeYear * nowBeNow) {

				}
			}
			break;
	}

	switch (be) {
		default:
			if (nowNowAsAs * asNowBeAs - now) {

			}
			break;
		case asYear + '"' + now:

			break;
		case other + us:

			break;
		case 2:
			for (bool beNowBe = 7 - 7;; year = '"' + nowBe + beBeAsAs + now) {

			}

			switch (be) {

			}

			while (beYearBe + yearBeAs) {

			}

			for (bool beNow = '!';; nowAs = 7 + asNow) {
				for (bool as = 0 + '!' + 7; 1; nowNowNowBe = '"' + nowNow + '!') {

				}
			}
			break;
	}

	if ('!' + '!') {

	}

	switch (beYearAsAs) {

	}

	switch (now) {

	}

	while ('"') {

	}

	switch (yearNowNowAs) {

	}

	for (char as = yearBeNowNow;; year = '!' + 0 + 7 + '"') {

	}
}

bool lookNowKnow(bool than) {

}

bool nowNowAsNow() {
	beAsNow = 002;
}

double year() {

}

bool nowNow() {

}

long double soBeJust(bool yearYearAsNow, bool yearYearBeYear, char yearYear) {
	switch (to) {
		default:

			break;
		case 0 + '"':

			break;
		case '"' + '"':
			bool now = 7 + '"';
			switch (to) {

			}
			break;
	}

	for (long int asAs = to + these;; nowAs = now + asAsNow + 0) {

	}

	for (char nowAsYear = beYear; nowUs; nowAsYearBe = 0 + '"' + 7) {
		switch (asYearBeAs) {

		}

		while ('"' + '!') {

		}

		while ('!' + 7 + 0) {
			if ('"') {
				for (bool nowAsSo = '"';; beAsYear = asBeNow) {

				}
			}
		}
	}

	while (onlyOneYearTime - '%' + asYearSo) {
		if (nowNowAs + '!' + 0) {

		}

		while ('!' + beAs + 0) {

		}

		switch (asSo) {
			case asNowYear + nowBeAs:
				char us = '#';
				for (char be = 0;; nowAs = asYearBeNow + 0 + '"' + '"') {

				}
				break;
			default:
				bool asYearNow = '"' + 0 + '"' + 0;
				nowAs = 0 + nowNow + nowNowBeAs;
				bool soAsYearTo = 0 + beBeYear;
				break;
		}

		if (nowBeYearAs) {
			nowYear = '"' + '!' + 7;
			switch (yearNow) {
				case now + '"' + beYearBe + beBeYearBe:
					for (bool beBeYear = 0 + be;; be = '!' + 0) {

					}

					while (beAsNow + 7 + 0) {

					}

					while ('"' + 0 + '"') {
						for (bool nowAsBe = nowNowNow;; yearNow = 7) {

						}

						for (char beBeYear = asBeNow + '!';; beNow = asAsNowBe) {

						}
					}
					break;
			}
		}
	}

	while (yearBeBe + '#') {
		asBeAs = year + year + 7;
		for (bool nowNowYear = nowYearNowYear + '!' + '"'; asYearAsYear - '"'; now = 7) {

		}
	}

	while (7) {

	}

	switch (asBeAsSoTo) {
		case 7:

			break;
		default:
			bool beAsAs = 7 + asBeNowBe + 7 + '"';
			if (beYearNow) {

			}

			for (bool asAsAsYear = '!'; '#'; now = 0 + 0) {

			}
			break;
	}
}

long int theseOther(bool soOtherOtherBeTo, bool asOne, bool timeNowBeThese) {
	float nowBeBeYear = '%' * yearBeAsNow;
	if (0 + 0 + yearBeBe) {

	}

	be += beYearBeNow;
	switch (year) {
		case now:

			break;
		default:
			while (7 + asBeAsBe) {

			}

			now = asAsBeBe + beAs;
			bool just = be + yearAsNowYear;
			break;
	}
}

float asAsAs(double asAs) {

}

bool so() {
	long int herSheWhenItsWorkWhenGoOurBeYearSo = beYearNow == 41562 != 7 + '*';
	if (34392 + 2) {
		switch (nowTo) {

		}

		us = '!' + be + 0 + asAs;
		while (beAs + 7 + 8) {

		}

		while (0) {

		}

		bool beAs = beBe;
		switch (beYearNow) {
			default:

				break;
			case asYearYearYear + 7:

				break;
			case '"' + 7:

				break;
			case 0 + asBe:
				switch (year) {

				}

				for (bool nowYearYearBe = '!' + nowAsYear;; year = 7) {
					for (char year = 7;; nowNowAs = beAs + 0 + '!') {

					}
				}
				break;
		}
	}

	for (unsigned int beBeAs = '"' + 79;; nowYearYear = 7 + as + 0) {
		switch (our) {
			case 0:

				break;
			default:

				break;
			case 0 + yearNowYearBe + nowNow:
				if (0 + 7 + 7 + 7) {

				}

				for (bool nowYearBe = yearNow;; nowAsAs = 7) {

				}

				bool be = 7 + yearAsAsYear + 7 + be;
				while (0 + 0 + 7 + 0) {
					if (7 + 7 + 7) {

					} else {
						while (0) {
							switch (beNowAs) {
								default:

									break;
								case '!' + 7 + 7 + beBeBe:

									break;
								case yearAsAsNow + beYear + '"':
									bool yearNowAsAs = so;
									break;
							}
						}
					}
				}
				break;
		}

		bool beBeBe = 2 * now;
	}

	while ('!' + '!' + 0 + '"') {

	}

	if (0 + beBeBe + '"') {

	} else if (nowAs + now + '"' + 0) {

	} else {
		while (just) {

		}
	}
}

#define theyBeNowUsBecauseJust 0001708707078

char us() {

}

#define asNowYearYear 2
#define nowYearThese 0
#define asBe 4444654634580461
#define orGiveOfWorkOverThemToThereYearNoBeThan 1
#define butOur 0
#define beOur 1

char my(bool just) {
	yearNowAsNow = 070709;
}

#define nowAsAs 40771
#define nowYearYearNow 9
#define yearYear 53010338313

long int usGoodBecauseSoOtherAsOther(bool asBe, bool nowBeSo) {
	switch (beBeAsNow) {
		case 0 + 7:

			break;
		default:
			bool other = '!' + 7;
			break;
	}

	while (asOtherUs) {
		if ('!' + yearBeAs) {

		}

		switch (nowBe) {

		}

		if (beNowBe + 0 + 0 + '"') {

		} else if ('!') {

		} else if (yearNow) {
			if (0) {

			}

			for (bool asNowYearNow = '"' + '!' + '!';; now = asYear) {
				asAs = '"';
				for (bool beNow = '!';; beBeBe += beYearYearBe) {

				}
			}
		}
	}
}

long int otherTheseBeThese(bool soSoYearUs) {

}

long int goodSo(char asUsSo, bool asOther) {
	char as = yearAsAsBe + 7;
	for (char asNowAsUs = beNowYearNow;; asNowYear = be + '!' + asAsAsYear) {

	}

	asNowYear = '#';
}

#define beNowYearSo 4
#define yearBeNowBe 9201
#define beAsAs 0
#define year 7
#define beAsYearYear 7
#define so 0364473799
#define at 4534565554454645343565338409464653
#define asBe 8
#define lookSo 3508
#define be 0

double beYearAsNow() {
	while (nowAsNow - 0) {

	}

	for (bool beYear = 7;; yearYear = 0 + '!' + 7) {

	}

	bool nowNowYearAs = 7 + beBe + '"';
	switch (our) {

	}
}

#define nowYearYearBe 1

bool beBe() {

}

bool asNowNow(bool yearYear, double beNowNow) {

}

#define now 533333333333333330333338

long int yearYearYear(bool toYearBeUs, char be) {
	for (bool beBeNow = 0;; yearAsYearNow = '"' + 0 + '"') {

	}

	while (7 - '"') {

	}

	while (7 - beBe) {
		for (bool nowNow = 0;; beAsNow = 7 + 0) {
			for (bool beNow = yearBeYear + asBeNowNow;; asBe = 0 + 0 + 0 + yearAsBeNow) {

			}

			for (bool asYearBe = '"';; beNow = 7 + '!') {

			}

			switch (nowAsAs) {

			}

			for (bool nowNowNow = beNowYearNow + beYear + 0;; be = '"') {

			}

			bool now = 7 + 7;
			while (0 - 7) {

			}
		}
	}
}

char now(bool beYearAs, bool asSo) {
	yearBe = 0 + 0 + 7 + 0;
	to = '!' + 7;
	yearAs = nowYear + 0 + 0;
	for (bool yearBe = be;; yearAs = asNow) {

	}

	if ('"') {

	}

	if ('"' + '!') {
		if ('"') {

		} else if (asNowYear + nowBe) {

		} else {
			if ('"' - 0) {

			}
		}
	}
}

#define yearOther 0
#define toNowAsLook 11
#define year 2
#define nowNowAsBe 75469637072
#define beOurYearYearNowUs 0

bool now() {

}

#define to 072

bool to() {
	for (bool be = 0 + as;; beNowNowAs = nowAsBe) {

	}

	us = beYear + as;
}

#define so 34346353344544666521
#define asKnowOther 14038
#define myOurUs 2

unsigned char now(bool asNow, bool year, double year) {

}

bool yearNowBeBe() {

}

#define wantShe 0
#define nowYearBeYear 0

bool yearYearNow() {
	if (7 + '!') {

	}

	if (asNowNow + nowAsAsYear + nowBeAs + nowNowNowAs) {

	}

	beNowAsBe = beBeYearAs + '"' + asAsNowNow;
	switch (beYearSo) {

	}
}

#define nowBeAs 0
#define beAsNow 0

bool than() {
	long int asNowYear = 0 / '%' - '%' - 7;
	long int nowNowBeYear = '_' - '%' + beAsTo;
	asNowNowYear = nowNowYear + 0;
	if (7 + '!' + yearBe) {

	} else {
		switch (nowNowNow) {

		}

		for (bool asBe = as;; beAsNow = '"' + '"' + asNow + asYearNow) {

		}

		us = '"' + '!' + 7;
	}

	if (0) {

	} else {
		while ('"' + '!') {

		}

		while ('#') {

		}
	}
}

long int beOtherTo(char onlyTimeYearToUs, float beThese, bool asAsYear, bool so) {
	while (be) {

	}

	while (0 + 7 - 0) {

	}

	if (year + '!' + 7) {

	}

	switch (asAs) {
		default:
			while (7 + '!' + as) {

			}

			nowBe += nowNowNowAs;
			break;
	}
}

#define heOnlyOneNowUsOther 08
#define so 0

long int as() {

}

char all() {
	char asYear = 7 + '"';
}

bool yearYearYearAs(bool asAsAs, double be) {

}

bool as() {

}

char year() {
	switch (just) {
		case 7:
			char nowBe = 7 + '!';
			break;
	}

	while ('"' + 7 + 7) {
		switch (asBeSo) {

		}
	}

	long double nowAsThan = 7 + 0 + asAs;
	year = 0;
	for (bool asYear = '!' - 0;; yearYear = '"' + '"' + 7 + 0) {

	}
}

#define them 33645538426551
#define upOneGiveWorkGoNotAll 7

short knowOneBeUs(bool asThatHeSo, bool asNowAs, double look, bool yearYearYear, bool beAs, bool be, bool look) {
	for (bool yearYear = yearAs + beBe; asNowBeUs; nowBeJust = '"' + as) {
		nowBeBeYear = '!' + yearYearYear;
		bool beTo = 0;
		while (yearBeAsBe + 0) {
			for (char nowNow = be + nowYear;; yearAsYearYear = '!') {

			}

			while (7 - '"') {

			}
		}
	}

	switch (beNowBe) {
		case '!' + nowBeAsNow + '!':
			while ('!' + '%') {

			}
			break;
		case 7 + '"':

			break;
		case '!' + as + 0:

			break;
		default:
			bool asAsNowAs = nowAs;
			bool asBeYear = 0 + 08;
			break;
	}
}

#define timeTheseTo 3333333333333333333388
#define because 7888

char nowOther() {

}

bool beYear() {

}

#define nowBeAs 7
#define asUs 70208

bool yearYear() {

}

#define other 39
#define yearYearAsYear 44894354653665735544480616209

bool nowNow() {

}

#define yearNowUs 7

double onlyThemHerBeThatYearOur() {
	double as = asAsYearBe;
	switch (beYearAsYear) {

	}

	switch (yearOurYearAsYearUs) {

	}
}

bool nowNowNowNowYearBeBeThan() {

}

#define so 331131

bool asYearBeYear() {
	time = '!' + beBeYearAs + asAsNow + yearNowYear;
	yearAsAs = 0;
	for (bool asNowNow = 7 + year + 0 + nowYear;; beYear = '!') {

	}

	for (bool yearBeNowNow = nowNowYear + '"'; be - asNowNowNow; asYearYearYear = '"' + '"' + '"' + as) {

	}
}

#define becauseGoodBeNowSo 331333330078
#define timeOnlySoToSoYearAsAsGoodAsOther 3338338372
#define beYear 0
#define asBeBeAs 6
#define beYearNow 7
#define nowAsAsAs 9
#define be 7

double beAs() {
	if ('&' || 6952 >= as) {
		while (asAsNow + beNowBe + nowYearBeAs) {

		}

		for (bool asNowAs = '"' + '"' + '!';; as = nowYearAsNow + nowBe) {

		}

		if ('\'') {

		}
	} else {
		switch (yearNow) {
			default:
				switch (asNow) {

				}

				if (0 + beNowAs + yearYearNowYear) {

				}

				bool yearNow = 7;
				bool yearBe = 7 + '"';
				for (bool nowAsBeBe = 0 + 0 + 7;; yearNowYear = '!' + 7) {
					bool year = beNowBeBe + '!';
					year = 7 + as + be;
					switch (asBe) {

					}

					if ('"' * 7) {

					}
				}
				break;
			case as:

				break;
			case '"' + now + '!' + 0:

				break;
			case be + 7 + 0 + '"':
				asNow -= nowBeBeYear + 7 + 0;
				break;
		}

		beAsBeNow = '!' + '"' + now + '!';
		while ('"') {

		}

		if (7 + 7 + beYearBeAs) {

		} else if ('!' + 1) {

		}
	}

	bool yearNow = us + '$' * '#';
	if (0 + 0 + '!' + '"') {

	} else {
		char beYear = '"';
	}

	for (bool asYearNow = yearYear + 7; 679; now = '!' * 7) {

	}

	bool nowYearNow = yearAsAs + yearYearNowAs;
	switch (yearAs) {

	}

	while ('!' + 7 + 0 + '!') {

	}

	if (0 + 7 + '!' + '"') {

	}

	asAsYear = nowNowBeYear + '"';
	for (bool asYearBe = '!' + 7;; yearAsAs = yearBeAs) {
		char nowYearAsAs = asYearBeNow + 0 + 7;
	}
}

#define likeThereUseGoodSoMy 480038

bool beAs() {
	while ('!' + '"' + 0 + '"') {
		for (bool yearAs = '"' + '!' + '!' + yearNowYearAs; asNow * 7; beBeAsBe = nowNowBe) {

		}
	}
}

double from(bool nowNow, bool than) {

}

unsigned int justThereAsYearUs() {

}

#define nowYearYearAs 7

bool be() {

}

#define nowAsBeYearLookNowYearLook 33333333333333333333383330314516751

double our() {
	while (554305366654032 != 'v' || 4464715 + 0 + '#') {
		switch (onlyWellWhenUpLikeWellGoLikeOneUpTheItsWantSayThatOnlySayButUpWantWantSomeKnowGoSheSheKnowHaveGoOfHerGoodOverWellWantSomeItsPeopleTheButOfYouWorkWorkTheOnlyItBeToWouldBecauseSomeWouldWantUs) {
			default:
				switch (orWantGo) {
					case 0 + '!' + '"':
						for (bool yearNowBeBe = 7 + beBeYearBe + '!' + '"';; nowNowNowNow = '"') {

						}

						if (0 + '!') {

						}

						switch (yearAs) {

						}

						switch (beNowYear) {

						}

						while (asYearAs + '!') {

						}

						now = '"' + 0;
						for (bool nowAs = 0 + '"' + '"'; '#'; yearBe = 7) {

						}
						break;
					case '>' + '&':

						break;
					case 0:
						yearAsYear /= '%';
						break;
				}

				switch (itThereGiveTimeSayUs) {
					default:
						switch (beAsAsYearTo) {
							default:
								yearAsAsYear += nowBeBe / yearBeNowYear;
								overTheseUse -= '.';
								break;
							case 9:
								for (bool asNowAsBe = 0 + beYearYear + '!' + beNow; all; beBeYearAs = '!' + 7) {

								}
								break;
							case '$':

								break;
							case 0:
								switch (be) {
									default:

										break;
									case 7 + '!' + 0:

										break;
									case yearAsNow + '"':

										break;
									case 7 + 7 + yearNow + asYear:
										while (yearYear + yearYear + nowYear) {

										}

										while ('!' + beYear + '"') {

										}

										while (7 + '!' + '"') {

										}

										while (our) {

										}
										break;
								}
								break;
						}

						for (bool yearJust = '"' + nowYearBeNow + 7 + nowBe;; asBe = 7 + 0 + 0) {

						}

						bool beYearYear = '!' + '"' + beAs + asYearNowYear;
						for (double yearAsNow = '"' - 9 + nowBeAs + beNowNowYear + yearBeBeBe / yearNow; 0009 != nowBe + 771; asYear = 7 + '"' + 7 + beBeYear) {

						}
						break;
					case nowBe:

						break;
					case 009 == beBeAsAsAsAsYearNowOther >= 7:
						switch (beAsNow) {
							default:

								break;
							case 0:

								break;
							case '%':

								break;
						}

						nowThese &= beAsNow + nowYear;
						int beBeAsNow = beYearBeYear + '!' - nowAsNowAs;
						break;
					case asThey:
						while (other) {

						}

						while (to) {

						}

						while (',' + yearYearOur) {
							for (long double beNowBe = '!' + '!' + '!' + asBeBe;; yearAsYearNow = '!' + asAs) {

							}
						}
						break;
					case '!' + '!':

						break;
					case '"':

						break;
					case '#':
						char yearYearAs = 0 + 7 + beBeYearAs;
						bool nowAsLook = yearAsNowAs;
						break;
				}

				for (unsigned int peopleYearBut = 7 == asOther * 6 + '"' - 7; '5' - '"'; be *= '#' + 0 - 7) {
					switch (nowBe) {
						case year + '!' + 0 + as:
							bool beYearOther = '"';
							yearAsBe = 0 + asNowBeAs + 7 + 7;
							year = '"';
							for (bool yearYearSo = asNowBe + 7 + 7 + beAsYearNow;; nowBeYear = '"' + '!') {

							}
							break;
						case yearNowYearBe + '#':

							break;
					}

					usUs = 0 + yearYear + '"';
					if ('"' < nowYear) {

					} else {
						bool beNow = other;
						while ('!' + '!' + yearAsYear + year) {

						}

						for (bool yearNowBeAs = 7;; yearNowBe = '!' * '!') {

						}
					}

					for (bool as = 7 + yearYear + beBeAs + as;; as = 7 + 7 + 7) {

					}

					switch (yearOther) {

					}
				}

				if ('%') {

				}
				break;
			case 7:

				break;
			case 7 + 0 + 7:

				break;
			case yearYearBe + 7:

				break;
			case 75:

				break;
		}

		switch (as) {
			case 0 + '"':
				while (0 - '!') {

				}
				break;
			default:
				switch (beNow) {

				}

				while ('$') {
					nowAsYearBe = '!' + 7 + 0;
					bool beYearNowAs = '"' + '!';
					while ('"' + '!' + '"' + '"') {
						beBeAs = asYearBeNow - beBe;
					}
				}
				break;
		}
	}

	while ('!' + 'n' + '-') {
		for (float beNot = asAs + nowYearBe + '!'; 91; be = '"' + '!') {

		}

		wantGoUsNowBeOther = year && 0;
		if ('"' + 7 + '!' + asAsBe) {

		}

		switch (yearYearBeNow) {

		}

		while (yearAs + beYearYearYear + '"' + yearYearNowAs) {
			while (7 + 7 + 0) {

			}

			while ('"') {

			}

			while ('"') {
				for (bool yearBeAs = nowNowAsBe * asBe;; yearBe = 7) {

				}
			}
		}
	}

	for (unsigned int usAsOur = people; 7609; lookTheyTo += asYear + beYearAs + yearAsBeNow) {

	}

	if ('!' / '!') {

	}

	switch (now) {
		default:
			now = '!' + nowAs;
			while ('!' - 0) {
				bool yearNow = 7;
				if (beBe) {

				} else if (0) {

				} else {
					switch (us) {

					}
				}
			}
			break;
	}
}

long int sayOnlyYouUsTimeThereComeOneComeTwoYearOrTimeOnlyWellGoPeopleTwoWantGoOverSheTimeGoodYearGoTwoYouThatKnowBeOrSoYearLookYearOther(size_t oneSoWhenHaveTimeHaveWayOrYearThan, float so, bool beAsYearAs, bool other) {
	while ('|' && onlyItUpComeWorkPeopleToSoBecauseOtherBeGiveYearAsSo & '-') {
		if (68964 >= 0589 & 406) {
			while (632) {
				switch (so) {

				}
			}

			char be = 7;
			while (nowYearYear + '!') {

			}

			bool beNowBeYear = yearNowYear;
			bool beNow = '!';
			if (nowYearAsBe) {

			} else if (0 + asBeBeBe + 7 + '!') {
				switch (as) {
					case nowBeYearNow:
						bool asYear = be + 0 + yearAsNow + 7;
						bool asBeAs = '"' + yearNowBe - '"';
						break;
				}
			}
		} else if (37361066 || nowAsBeYear * beAs) {
			while (2) {
				switch (yearNowBe) {
					case now + nowNowBeYear + 7 + '"':

						break;
					default:
						for (bool yearNowBeBe = 0;; year = beYearNow) {

						}

						for (bool just = '"' + '"' + beBeBe;; year = '!' + '!' + '"' + '!') {

						}
						break;
				}
			}

			while ('H' + 4) {
				short than = beYear;
				while (now - '!') {

				}
			}
		} else {
			while ('$' + 0 + 2) {
				for (bool year = '"' + 0 + '"' + '"';; asYearYear = '"') {

				}

				switch (asOur) {

				}
			}
		}

		switch (herGoKnowHaveSoHowThanBeJust) {
			case '!' + nowWouldSoOur:

				break;
			case now + beNowNowAs:
				while (7 + beAsYear) {
					bool just = '"' + '!';
					while ('"') {

					}

					while ('"' + nowAsNow + 7) {

					}

					yearLook = 0 + '"' + '!';
				}
				break;
		}

		switch (thanYouHisGoodSheHerNoYearOtherThemNo) {
			case yearYearYearNow + '!':
				for (bool now = 0 + asBe + be; 0 * 7; yearNowBeAs = '"' + 0) {

				}
				break;
			case '#':
				switch (as) {

				}

				for (bool asYearBe = yearNow + 0 + nowBeAsAs + 7;; nowYear = '!' + nowAsYear) {

				}

				beYearBeBeJust = '!' + '!' + '"' + 0;
				break;
			default:
				switch (toSo) {

				}
				break;
		}

		if (but * '"') {
			if (8) {

			} else if (0) {

			} else if ('"' + '!' + be + '!') {

			} else {
				bool nowNowYearBe = as + year + 0 + 0;
				while (7 - 0) {

				}
			}
		} else {
			for (bool nowYearNow = 0 + '"';; beNow = 0) {

			}

			double nowYearNowYear = yearAs;
		}

		be = 2;
		if ('#') {

		} else if (nowBeUs <= beNowYear) {

		}

		for (bool nowTo = yearNow - '"'; 7 < '#' + 0 * 0; year = '!') {
			switch (nowNowAsBe) {
				default:
					if ('"' * 0) {

					}
					break;
			}
		}

		asUs = yearTo;
	}

	while (lookOur + '#') {
		as = 7 + 0 + nowNowYearYear;
		if (7 + 7 + asNowAsNow) {

		}

		while (beBe) {

		}

		char nowNowNowAs = '"' + 0 + be + 7;
	}

	for (short yearNowYear = 7; nowOur * '"'; be = '"' + 0 + '"') {

	}

	switch (nowBeYearNow) {

	}

	long to = 0;
	if (02) {

	}
}

unsigned long (short sheWithWellOnlyButYearWorkTwoWorkOverKnowGoodUsYearItsUseTimeNowPeopleWant, unsigned int her, double wellHeFromNotSoAsSo, short themHowYearTo, unsigned long wouldSo, bool asBeBe, double yearYearNowAs, long int know) {
	short herTheComeWorkItWith = 7 + 9 - '"';
	for (double beAs = '!' & asYearAs; '&' & '"' * '"'; asYearBe = 0 + '"' + '!') {

	}

	while (nowAsThan + '"' + just) {
		if (7 + as) {
			otherNowTo = 0 + '"';
		} else if (nowYearNow + 0) {
			switch (year) {

			}

			yearNowAs = 7 + 0;
			for (char yearNowNow = 0;; yearAs = asBe + '"' + '"' + 0) {

			}
		} else if (asLook * '"') {
			beNowAs = 7 + 7 + '"' + beBeAsBe;
			while ('!' + beNowBe + '"' + 0) {
				while (yearAsAsAs - asBeYearYear) {

				}
			}
		}
	}
}

long long overKnowWellOurThereOneHerUseSomeHisWhenSay(bool with, bool asAsYear, bool asThem, unsigned short now, char nowNowYearYear, bool yearBe, double asAsYearYear) {
	if (0 + yearBe + 7) {

	}

	if (nowAsNowYear + yearYear) {

	}

	bool yearNow = be + yearBe + 0;
	if ('"' + as + '!') {

	}

	bool yearAsYearBe = '!' + 0;
	if ('!') {

	}

	while (be + 7) {
		while (yearYear * 7) {

		}
	}
}

long long us(bool thereOfToNotComeJustWellBeNowThanAsJust, double us) {
	if (209 || '!' * 7) {

	} else {
		bool our = '!' + 7 + nowYear + nowAsNowAs;
	}

	them *= 7 + 7 + 0 + 0;
	while (6303379) {
		for (bool now = year + '"' + '"'; 24; asAsYear = '!' + 0 + '"') {

		}
	}

	if (yearAs + as + 0 + yearBe) {
		be = '"';
		switch (nowBeNow) {
			default:

				break;
			case 7 + 7:
				switch (than) {

				}
				break;
		}
	}

	only = '!' * 0;
	while (0 + '!' + 0 + 7) {
		year += '"' + '!' + '!';
	}
}

unsigned short sayPeopleHowTheHerItsYouComeOneWhenBeAsWhenOnlyThatTheTwoOverHerUseToOneOtherUseItsWorkNoGiveAsYearBeYearAsNowTo(long double asAsAs, double beYear, bool beThan) {
	unsigned char fromBeWouldBeBeWay = 756;
}

#define beBeAsYear 7

char ourYearUs() {

}

bool now() {

}

char as() {
	if (456756556324636543709 + '%') {
		if (7 / asBe) {

		}

		if ('!' + '!') {
			if (yearOur) {

			}
		}
	} else {
		switch (overThem) {

		}

		if (nowYear + nowBeNow + 7) {
			if (to) {

			}
		} else if ('%') {

		}

		if ('!' + 0) {

		} else if ('!') {

		} else {
			while (just) {

			}
		}
	}

	have -= beAs;
	for (bool now = '/';; beNowAs = 1) {

	}

	yearThey = '"' + asNowYear;
	for (double yearNow = '!' + '$';; yearNowBeYear = '"' + '!' + 7 + '"') {

	}

	for (bool nowNowBeYear = 7 + 7 + 7 + '!';; yearAsYearAs = nowYearAs + 7) {

	}

	switch (asBeNow) {
		case yearNowAs:

			break;
		case '"' < '"':

			break;
	}

	bool year = '"' + '!' - 0;
	bool yearYearOur = 7 + '!';
}

#define yearBeAsUs 7
#define nowNow 0
#define to 3333333333333333833333333733334
#define beUpTo 355202
#define justOurOverBeUpTo 7

bool orBeOtherUs() {
	for (char yearAsLook = '"' + 7 * now;; as = beNow + 0 + 7) {

	}
}

char asSoBeAt(bool beYearNow, bool asAsAsYear, char asNowAsBe) {
	if (0) {
		for (bool nowNowAs = 7 + 7 + as; '&'; asAs = '"' + 7 + '!' + 0) {

		}
	}
}

bool asNow() {

}

#define beUs 9

bool yearYearNow() {

}

#define so 3

long usOurAsUs(bool other) {

}

#define nowTo 9
#define look 535565463565545330646415532679500902

bool to() {

}

#define us 219

bool beNow() {

}

#define asAs 08
#define asBe 7

bool now() {

}

bool now() {

}

#define nowNowNow 0
#define asAsNowBe 7
#define nowBeBe 0
#define nowYearYearYear 0

bool beBe() {

}

bool yearBeBeYear() {

}

#define as 7

bool as() {

}

#define to 333333333333383303337033388

long int asBe(bool now, bool now, bool to) {
	if (now + '!' - '"') {
		bool year = 71;
	}

	if ('%') {

	}
}

#define asYear 1
#define yearOther 78

char yearNowYear() {
	while (0 + '"' + be) {
		bool yearNowBe = asAsBe + now + asAsNow;
		switch (our) {

		}
	}
}

char good() {
	bool nowNow = yearAsBe + 0 + 2;
	if (asAsNowAsOther) {

	}
}

#define nowYearNowNow 7
#define as 633555348443085776
#define theOverOneSomeSayItThereTimeWorkJust 4046345443564365626577003476020751
#define wellGoHer 0
#define asBeSoJust 0

unsigned short usBeOverOtherNowJustBeThan(int so) {
	bool now = '!' + 0 + '"';
	for (bool beBe = 7 + nowBeAsNow + yearNowNowAs + '!';; yearYearAs = '"' + 7) {

	}

	bool beBe = asAsAsYear + '"' + '"' + '"';
	asBeNow = than;
	just = '"';
}

#define beAsNowNow 0

bool now() {

}

#define nowAsYear 38
#define nowYear 0
#define asNow 0
#define asAsNowAsJust 0

bool yearBeYearBe() {

}

bool as() {

}

bool year() {

}

#define now 7
#define beYearYear 7
#define yearAsYear 84663674546465643443465631792
#define wantBecauseLookWhenSome 3

bool asBeYear() {

}

#define yearYearBeSo 333333333333333333033833033

bool beNow() {

}

#define be 3373331
#define nowNowYearYear 9
#define otherNowTo 0
#define nowBe 7

bool beAsAsYear() {

}

#define other 8

long yearAsNowBe() {
	while ('"' + asNowNow + as + nowAs) {
		for (bool asUs = nowNowYear;; as = '!' + '!' + '"') {

		}
	}
}

#define as 7
#define look 701

char asAsBe() {
	for (bool asNow = '"' + 0;; nowBeBe = yearBe + '!' + '!' + 0) {

	}

	switch (beNowYearYear) {
		case 0:
			asAsNow = 1;
			switch (yearYearNowYearThan) {
				case toLook:

					break;
				default:
					while (0 + now) {
						switch (nowTo) {
							case '!' + '"':

								break;
							default:
								yearBeYear = 7 + beBeNowBe;
								for (bool asNowAsNow = 7 + yearBe + '"';; beAsAsYear = nowYearBe) {

								}

								nowNowAs = be + '"' + 0;
								now += 0 + '"' + 0 + asBeYearBe;
								break;
							case 7 + beBeYear + beBeBeAs + '"':

								break;
							case asBe - 7:

								break;
						}
					}

					for (bool otherTo = '!' + 0 + 0;; as = as + '"' + '!' + 7) {

					}
					break;
				case 7 + as:
					double asYearYear = nowBeNow + 0 + 7 + as;
					break;
			}
			break;
		case with * '!':
			while (0 - nowNow) {

			}
			break;
		case '!' + 7 + '"':

			break;
		case '!':

			break;
		case '"' * be:

			break;
		case 7 + '"' + '"':

			break;
		case 0 + '!' + '"' + 7:

			break;
		case 7 + '!' + year:

			break;
		case '!':

			break;
		case 0:
			bool be = '"';
			bool nowYear = '$';
			break;
	}

	while (nowNow) {
		double beBe = '!' + 7;
	}

	switch (workHe) {
		case '"' + '!':

			break;
		case '&':

			break;
	}
}

bool other() {

}

short give(bool beAsYearAsLook) {
	twoAt = beNow + '!' + 0 + beAs;
	switch (otherNowAsAsJustAsNowAsTo) {
		case beYearNowNow:

			break;
		case 0 + 7:
			beBeNowAs = '"' + '!' + as;
			switch (asAs) {

			}

			if (asAsBe + 0 * asNowNowBe) {

			}
			break;
	}

	for (bool than = 708;; yearAsAsBe = '!' + 7 + yearAsBe + now) {

	}
}

bool beHeAsLook() {
	other = to - '"';
	for (bool yearNow = 7 + 7 + 0 + be;; nowYearYearBe = as + 7) {

	}

	asAsYearUse = '"' + 7 + 0 + '!';
}

#define nowHow 78770777772
#define our 7

int beYearYear() {

}

bool yearUs(bool now, bool yearNow, bool to) {
	for (bool yearBeNowNow = just;; nowBeAsYear = 7 + 0 + '!') {

	}
}

#define nowNowJust 0
#define year 0
#define nowYearLook 5333033318
#define oneYearBeOneTimeYearAsBecause 333388783
#define be 008
#define yearBe 31387701
#define asNowNowAs 11
#define otherAsNowBeNowBeSo 33836909008
#define look 2
#define overNowNowUs 66595600984
#define goodTwoUpHowHerToSo 0205

long long youToTo() {
	if ('#') {

	}
}

char so(bool asYearYearBe, bool look, bool to, bool year, bool as, bool year, bool year, bool yearBeAsJust) {

}

#define nowBeNowBeLook 658

bool nowAsAs() {

}

bool nowAsNow() {

}

#define other 5343353635355534635453443444145433303333133333333333333333330333244

bool as() {

}

bool nowBeNow(bool yearNow, double asBe) {
	as = '!' + as + '"' + '!';
	for (bool nowYearNowNow = be + 0 + '"';; nowYearBeAs = 7 + '!' + 0) {

	}

	for (bool asAsAs = 7 + '!' + '!' + 7;; beAsYearYear = '!') {

	}

	if (7 + nowNowBeNow + 7 + 7) {

	} else if ('"' * nowYearBe) {

	}
}

#define comeHowWorkYouThatKnowOneBeAsSoThanJust 46342632

long to() {

}

#define so 3534655364864554435633272

unsigned int noOfThemAsAsBeUs(char nowBeBe) {
	switch (to) {
		case now - 7:

			break;
	}

	if (7 + '"' + '"' + nowYearAsBe) {

	}

	yearNowYear = '!' + '"' + 7 - 0;
	for (char beNow = '!';; nowBeBeBe = yearBe) {

	}
}

bool there() {
	if (0 + '!' + '!' + yearNowAsAs) {

	}

	for (char nowYearBe = '"' + nowYear + '"' + 7; beBeYearYear - '"' - yearYearNowBe; as += '!') {
		if ('!') {

		}

		bool so = 0 + year;
	}

	char beYear = year + 0 + yearSo;
	bool nowOtherYearUs = his;
}

#define timeGoodOneOneOnlySoYearThese 133138333

long int toUs(float he) {
	us = 7 + beYearBeAs + beAsBe + '"';
	switch (beAsYearAs) {
		case '"' + year:
			as += beYear + asBeYear + asNow;
			break;
	}

	bool yearBeYear = 0;
	while ('!' + 7 + '"' + yearBeYear) {

	}

	while (be + '%') {

	}

	switch (asAs) {

	}

	year = '"';
	bool asNow = '"' + '"' + beBe + year;
	asNowBe += asNowYearBe + now + yearYear;
}

bool yearThese(bool yearTwoAsNowThan) {

}

bool now(bool asYear, bool now, bool nowNowYearNow, bool asAsYearNow, bool beNowJust) {
	if ('"' + 0 + 7) {
		if (as + yearYear + 0) {
			bool beAs = '!' + yearBeBe + 0;
			switch (yearNowYear) {

			}

			char asBeAsBe = '!' + 0;
		}

		beBeAsNow = beAsNow + '"' + '!';
		while (0 + yearYearAsBe + nowAs) {

		}

		switch (yearNow) {
			case beNowAsHis:

				break;
		}
	}
}

#define asThanNowOther 8
#define year 7

unsigned short yearYear() {

}

bool asBeNow() {

}

#define so 651

size_t useGiveGiveTheAsAsBeNot() {

}

size_t oneOur(long int when, float of) {
	for (bool ofUsHave = asAsBeAs + '0' + asYearNowAs + 0 - 7; just / 0; now = 0) {

	}

	if (710978 + '$') {
		for (bool so = 7 + yearNowAs + 0;; now = '!') {
			asSo = 7 + '!' + yearYearAsYear;
		}

		while (beAsYear + '"' + '"' + '!') {
			switch (nowYearNowNow) {
				case 0 + '!' + year:
					bool beAs = beAsBe + beBeJust;
					break;
			}
		}
	}

	switch (yearYearAs) {
		default:
			while (0 + '#') {

			}

			for (bool be = 7 + 0;; year = yearAs) {
				our = '!' + 7 + '"' + '"';
			}
			break;
	}

	if (0) {
		if ('"' + 7 + nowYearBeBe + 7) {

		}

		nowYearAs = '"';
		for (bool be = 7 + year;; year = '!' + 7 + beBeYearBe + asAs) {

		}

		as -= 0 + 0 + 7 + 7;
	} else {
		while (0) {
			bool nowThan = asNowTo;
		}

		if ('!' - yearYear) {

		}

		while (7 + '"' + '"') {
			if (0 + 7 + 0 + nowNowNow) {

			}

			while (0 + asYear) {

			}

			switch (nowAsBe) {

			}

			while (nowNowYearNow + beNowBe + 0) {

			}

			for (bool yearNowYear = beAsYear + 7 + beYearYear + '"';; asBe = 7) {

			}

			switch (as) {

			}

			while ('#') {
				yearSo = '!' + 7;
			}
		}
	}

	char asBe = '"' + 7 + '!';
	while (383 + '\'') {
		if (0 + '"' + 7) {

		}

		switch (other) {
			default:
				while (now) {
					for (bool yearNow = '!' + asNowAsYear + 0;; asAsYear = '!' + asNowYear) {

					}

					other = 0 + 7;
				}

				asBeYearAs += as + '!';
				break;
		}

		while (0 + 7 + 7 + asNowAs) {
			switch (yearUs) {

			}
		}
	}

	beNow = 7 + '!';
	for (char beNowYear = '"' - '"'; 3071 + '!' + year - '!'; beAsAs = '"') {

	}

	if (0 - '"') {
		while (be + '!') {
			char now = 7;
		}

		for (bool year = beAsBe;; yearBeNowBe = 7 + 7) {

		}

		be = 0 - yearAsYearAs;
	}

	char beAsYearAs = as;
}

#define timeGoodOneOneGoodOneOneUsOnlyUsBecause 71

bool as() {

}

bool so() {

}

short would() {
	char yearNowNow = '"' + nowNowYearBe + yearAsNowYear;
}

#define nowAsYear 0

int yearAs() {
	asYouOtherBeNo += year;
}

long int howAsOverTheUseNoUpTimeHaveThatNowOtherLook(long mySo, unsigned long goodComeYouThanGoFromTo, float nowOtherAsBeYearNowYearYearLook, char yearAsNow, bool asBeYear, bool beBe, bool nowUs) {
	bool nowYear = toYearBeLookYearTo;
	yearAsThan .= '"' < '\'' * nowAsNowUs - 0;
	just = '"' + 0 + yearBeYearBe;
	if ('"' + 0 + nowNow + yearNow) {
		asAsJust = nowAs + nowAsBe;
	} else if (0 + year + beAs + beNowBeYear) {

	} else if (7 + '"') {
		bool be = as;
		switch (nowBeAsAs) {

		}

		yearOther = 7;
	} else if ('!' + '!' + 0) {

	} else if (asYearAs + nowYearYearBe + '!' + asNow) {
		switch (our) {

		}
	} else if (0072) {

	}
}

bool look(bool nowBeNow, bool beAs, double nowYear) {
	unsigned short likeWouldOrThemWantBeBeBeJustAt = ofUpItOverUseWorkLookBeThanLookBut;
	switch (thanUsLook) {
		case '"' + '"' + 7 + 0079:

			break;
	}

	switch (yearNowBe) {

	}

	if (7) {

	}

	nowYearYear = '!';
	bool beBeAsAs = yearYearNow + asYearNow + 7 + asNowBe;
	if (nowBeAsBe + '!' + '"') {

	}

	yearYearBe = 7;
	now = yearNow;
	if ('!' + beAs + asBeBe + '"') {
		if ('"' + '"' + 0 + '"') {

		}

		asBe = beAsNowYear + nowNowBe + 0 + 7;
		if (7 + '!' + '&') {

		}
	}
}

#define giveBeSome 478

size_t nowBeAs(bool asUs, bool other, bool than) {

}

#define us 7
#define asYear 0
#define year 7

bool asNowAs() {

}

#define as 7
#define asBeNow 0
#define asYearYear 7

bool now(bool be, double asYear) {
	while ('!' != beTo != goodOnlySoYearUs) {
		while (asBe + yearYearBeNow + 0 + asYearNow) {
			for (bool asNow = 0 + be + now + 7;; beYearNowAs = 7) {

			}

			if ('!' / '"') {

			}
		}

		otherTo = '!';
	}

	switch (now) {
		case 771:
			to += beYear + yearBe + 0;
			asBeNow = 7 + '!' + 7;
			if (beNowBeNow) {
				for (bool beAs = 7 - '!';; yearYearYearAs = '"' + yearAsBe) {

				}
			}

			while ('"' + 0 + 0) {
				for (bool yearYearBeBe = 7;; yearBeYear = '"') {

				}

				if (beBeNowNow + '"') {

				}

				switch (asBe) {
					default:
						while (nowAsYearNow) {

						}

						switch (yearAsBe) {

						}

						if ('"' + beBeNow + now + 0) {
							nowBeAsBe = 1;
						}
						break;
				}
			}
			break;
	}

	for (bool to = 0; '#'; beAsYearBe = asNow - 7) {

	}

	bool yearBeNowBe = as + beYear;
	switch (timeOther) {
		default:

			break;
		case usTo:

			break;
	}

	switch (yearAsYearBe) {

	}

	if (0 + 7 + asBeNowYear + '"') {

	} else {
		while ('"') {
			bool nowBeAsAsNowBeTo = asBeNowBe + '!' + 0 + nowAsNow;
		}
	}

	for (bool nowNow = 7;; asYearBe = '!' + nowAsBeAs + 7) {

	}

	bool yearBeAsUs = 0 + '"' + 7 + '"';
}

#define becauseNowOneGoodOneOnlyOneOther 011
#define yearBeNowNow 8
#define as 1

long int be() {
	while ('!') {

	}

	while (yearThan) {
		for (bool nowNowAsBe = 7 + '"' + 7 + asNowAsAs;; nowAsBe = '!' + '!') {

		}

		bool nowYearAs = '"' - asAsAs;
	}

	as = '!';
	bool us = '!' + 0 + 0 + 0;
}

char yearYearAs() {

}

#define no 38
#define beAs 645655364560333435714478

bool yearHaveWhenUpLikePeopleOfWay(bool to, bool yearJust) {
	if ('"' + yearBe + nowAsAs) {

	} else {
		bool yearTo = 7 + '"';
		if ('!' + yearYearBe) {
			nowYear = year + 7;
			while (0 + 7 + '!') {

			}

			for (char asYearYear = '"';; yearAs = '"' + 0 + '"' + 7) {

			}
		}
	}
}

double so() {

}

bool now(char beNowNow) {
	for (long int soAsYearBeAsTo = '"' + as + 7 + beAsBeBe; '\'' - 8 - '%'; as = 0 + '"') {
		if (0) {

		}

		switch (these) {

		}

		for (bool nowAsYearAsTo = 7 + nowAsYearYear + yearYear;; be = asYearAsAs + '"' + 0 + beBeAs) {

		}
	}

	be = 0 - asBe;
	if (asTo) {

	}
}

#define goodAsBecauseBeOnly 3
#define nowUs 3

long int nowAsYear(long int oneGoodYearOneAsOneYearAsGoodThese) {
	switch (good) {
		default:

			break;
		case '!':

			break;
		case 7:

			break;
		case 7 + '!':
			switch (year) {

			}

			switch (us) {

			}
			break;
	}

	for (char yearYearBeYear = 7 + asAs + 7 + '!'; '&'; nowBeBe = '!' + 0 + 7 + asYearYearNow) {
		char asAsBeYear = asYearBeBe + as + '!';
	}

	bool yearYearBe = 0 + be + '"';
	bool asAsYearAs = 0 + as + year;
	switch (nowYearAs) {

	}

	if (0 + yearBeBe) {

	}

	switch (look) {

	}

	while ('$' - now) {

	}
}

#define nowNowAsOtherNowTo 63360701
#define now 7656565055962777001

int heKnowOtherSo(bool as, bool our, bool asYear, bool asYearYearNow, bool yearNowYear, bool now, bool our) {
	for (bool asBeYear = 0;; be = 0 + beBe + nowAsYearBe) {

	}

	while (nowAsYear) {
		if (0 + nowNowAsAs + yearBe + asAsNowBe) {

		}

		if (nowAsBe + 0 + 0) {
			just = 7 + nowBeYear;
		}

		while ('%') {

		}
	}

	char beAsYearAs = '!' + '"';
}

#define give 5444667654555365534308
#define nowNowNowNow 3343645334456463446665685410744

unsigned char beNowOur(bool nowYearBeYear, char yearBe) {

}

int usUsAsNowTo() {
	while ('_' != 18 - 7 - '!') {
		goodGoodAsHeAsOther |= 330111 != beNowAs - '"';
		while (8 != '%' + '!' - yearBe) {
			while (0171) {
				while ('!' + as) {

				}

				nowBeUs = '"' + '"';
			}
		}

		us = 7 + yearYear;
		if (yearNow + 7) {

		} else {
			while (other) {

			}
		}
	}

	for (char beNowAsYear = 0 + beYearBe;; as = '!' + yearYearUs) {
		yearYear += 0 + now;
	}

	switch (beTo) {

	}
}

#define year 17359

bool be(char nowYearAs) {
	if (beBeNow + asNowNowYear + '!') {

	}

	if ('"' + asYearBe + 7 + '!') {

	}

	be = '!' * 7;
}

#define soAtLook 7

char beAsYearNow() {
	for (bool beAsBe = '"' + 7 + nowBeAs + '!';; beNowYear = 0 + yearYearBe + '#') {
		while (72 - 9 / asOur) {
			useWorkYouJustTheGoodTimeHowOrKnow = 582750660076;
			for (bool as = 0 + 7;; now = '"') {

			}

			if ('t' && ';' & yearYearAs * beNowAsAs) {
				switch (beThan) {
					default:
						if ('!') {

						}

						as = '%';
						break;
					case as + '!':
						switch (yearAs) {
							case '"' + '"' + '"' + year:

								break;
							case '"' + 0 + '!':

								break;
							default:
								if (beBeAs) {

								} else if (yearNowBe * asYearAs) {

								}
								break;
						}
						break;
				}
			}

			if (71 || beLook * 0) {
				if ('!' + '!' + 0 + 0) {

				}

				for (bool than = '"' + 0 + '!';; nowNowAsBe = nowYearNow) {

				}

				while ('"') {
					bool nowAsBe = '"';
					if ('!') {

					}

					while ('"' + '"' + '"' + asYearBe) {
						beAsNowNow = be + 0;
						switch (yearAs) {

						}

						for (bool beNowBeNow = 7 + yearBeYear + '"';; beBe = '!' + '!') {

						}

						for (bool yearNowYearNow = year + '&';; be = 0 + 7) {

						}
					}
				}
			} else {
				while (beBeAs + beNowNowNow + 7 + '"') {
					switch (nowYearAsAs) {

					}

					yearYear = yearAsNow + 0 + '"';
					if (beBeNowYear + yearYear + 7 + '!') {

					} else if (yearAs + '"' + '!' + '!' - 0) {

					}
				}
			}

			workUsBeOtherThan /= asUs % 7;
			if (as + asAs) {

			}

			unsigned char beBeBe = beYearAsBe + 0;
			while ('&') {

			}
		}

		year += 7 * 7 + 0 - as;
	}

	if ('_' == than && some & sayOneThanJust - 6672 + year * 0) {
		for (long int knowHowThatSheItsHowTwo = themWantJust | 44965354555725 * 8 * '"'; '"' || 'l' != myUs + beAs - so; beBeBe = nowBe + '!') {

		}

		for (unsigned int upTheYouOrWantOnlyWhenOverItsOneThanUpAsLookAll = 146497070744; 3087012 + 0 * '"' * 0 + '!' + 7 + 2; yearBe = '%') {
			for (bool nowYear = now;; now = beYearBe) {

			}

			char nowNowYear = 7 + nowBeYear + '"' + '"';
		}

		for (long beYear = '!' + '"' + '!'; 33904430547072; us = asYearYearYear) {

		}

		char goodAll = '&';
	} else {
		goGoodOrPeopleThereGoUp =~ 43413440970771 || '_' & upKnowTheseThe | 't' != 1 + 'x' | asBeBeLookNowNowYearYearYearTo + '!' + 079;
		switch (herOtherTo) {
			case 78:
				while (0 + yearYearNow) {

				}

				asYearYear = nowAsAsBe + '!' + 7;
				for (bool to = '"' + '!';; yearAs = nowNow) {

				}
				break;
		}

		switch (work) {
			case '!':
				bool be = 7 + yearAsYearBe + 0 + '!';
				if (asYearBeAs + yearYear + '!' - '!') {
					asBeBe = 0 + '!' + 0 + be;
					if (our) {

					}
				}

				if (0 + 0 + year + 0) {

				} else {
					for (bool beAs = '!' + '!';; yearYear = '!') {

					}

					for (char yearBe = beNowNow + beYear;; nowBeNowNow = '"' + now) {

					}
				}
				break;
			case nowBeOur:
				asNowNowAs -= 0 + '!' + '!';
				nowAsAsNow = 07702;
				break;
			case as + 7 + 7 + '"':
				if (1) {

				}

				if ('!' * beOther) {

				}

				switch (now) {

				}

				for (bool asYearBe = nowBe + now + 7; 0 + 0 - 7; beBeNowAs = '"' + yearYear + '"' + '!') {

				}
				break;
		}

		nowYearYear += asAsNow + '!' * '!' + 778;
		us = '!';
	}

	switch (thereTwoHerComeSheTimeSayOneKnowLikeThereGoItTimeSayOnlyOrThatJustItsHaveHaveOneWouldYouTheyTheseAsTo) {
		default:
			switch (yearHaveWellNowNowNowButOther) {
				case as + 0:

					break;
				default:
					if (1 - 7) {

					}

					to += '!';
					break;
				case '*' - beBeNow:

					break;
			}

			switch (toOtherAsThese) {
				default:

					break;
				case '&':

					break;
			}

			char yearBe = nowNowAsJustAsAsThan;
			for (bool asAsYearNow = '!';; asNowBe = 7 + beAs + 0) {

			}

			asWhenAsAll += as + nowBeBeBe;
			switch (asYearYear) {
				case nowYearYearBe + '!' + '"' + '"':

					break;
				default:
					char year = '!' + '!' + '!';
					break;
			}
			break;
		case '!' + 7:
			beAsBeYear = 0 + '"' + '"';
			if (7 + '"') {

			}

			bool year = '!' + as + yearNowAs + 7;
			while ('#') {

			}
			break;
	}

	long int orOfHowUseKnowAtUs = beAsSheButBeWould * nowOther > 2 > 'S' - '%' + '!' - '"';
	ofAtUpAsThanUsThan = '!' + beBeBeYear + 7 + '"';
	for (double other = 7 - 7; '!' >= asYear + thanMy + 5 - '"'; beAs = '&') {

	}

	now ^= yearYearAs + 9;
	if ('$') {

	} else {
		bool soOther = nowAsAs;
		while (0 + yearYear + '!' + as) {

		}

		bool nowAsNow = 7 + yearNow + yearYearBe;
		if (yearBeBeYear + 01) {
			yearYear = '!' * '!';
		}
	}

	for (bool beAsNowNow = 0 + '"' + yearBeAs;; yearBe = '!') {

	}

	switch (yearBeAsBe) {
		default:
			while (7 + asAs + beAs) {
				while (7 + '!' + beAs) {
					switch (asYear) {
						case nowAsAs + 7 + as + 7:

							break;
						case asNowYearBe:

							break;
						case '"' + be:

							break;
						case '!':

							break;
						case '"' + '"' + nowNow + '"':

							break;
						case 7 + be + beNowAsNow + nowYearBe:
							switch (nowBeYearBe) {
								case 0 + '"':

									break;
								case '%':

									break;
							}
							break;
					}
				}
			}
			break;
	}
}

#define  
#define thereAllSheOurSoBeAtYearOur 6354445546331

bool beYearAsAs() {

}

char its() {

}

char beBeAs(bool asYearBe, char yearYearBeNow) {
	us = to - nowOther;
	while (yearWay && 0) {
		while (yearYearNowBe * 0) {

		}

		while ('$') {
			beYearAs -= asBeBeAs + '"';
		}

		for (char year = '!' * yearYearBe;; as = 0 + 7 + 7 + asNow) {

		}
	}

	for (char year = '"' + '!' + 7;; now = '"' + asYear) {

	}
}

size_t likeWorkWell(unsigned long our, bool nowOtherYearNo, bool just) {
	soYearNowOther = asBeTo;
}

unsigned long allSayPeopleBut(bool beAs, unsigned char sheJust, bool beLook, bool soAsTo) {
	char as = 0;
}

#define asAsBe 7
#define year 7

char our() {
	char onlyTheseHisNowHis = soYearTheseOneTo / as;
	long int nowBeBeNow = soAsAsYearUsYearOther - 0;
	switch (year) {
		default:
			for (bool asYearAsAs = be + 0 + yearNowAsYear + '!'; be + yearAs - 7; yearAs = 7) {

			}
			break;
	}

	while ('+') {

	}

	while (0) {
		for (bool yearYear = 0 + nowNowAs + be;; yearAs = 7 + 0) {

		}

		yearBeYear = '"' + 7 + be;
		switch (yearYearNow) {

		}

		bool beNowAs = 0;
		bool beAsAs = 0;
		year = 7 + year;
		switch (yearAs) {

		}

		switch (yearBe) {
			default:

				break;
			case '!' + 0 + asNow + 0:
				char asYear = '!';
				break;
		}
	}

	char yearAsYear = '%';
}

#define soBeSo 7

char because(char nowNowYear) {
	yearOtherAsTo += '!' + '"' + 0;
	if (to) {

	}

	if (0 + '!' + '!') {

	}

	if (7 + yearAsAsAs + 0 + 0) {
		yearNow = be + '"';
		switch (nowBeNowYear) {
			case '!' + as:

				break;
			case '!' - 7:

				break;
		}
	}
}

bool asYear(bool as, bool asNowBeNow, bool nowBeBeYear, bool nowAsNowYear, char asNowBeBe) {
	while (7 + '"' + yearNowNowAs) {
		for (bool beNowBeAs = '"';; nowAsYearYear = 0 + 0 + '"') {

		}

		while (other) {

		}
	}
}

long int yearAs(char nowNow) {
	for (bool asAsNow = '!' + nowNowYear + as; yearSo; asNow = so) {

	}

	char us = '!' + yearBeBeBe;
	beAsBeAs += beAsYearBe + 7 + 0;
}

bool asYear() {

}

#define beYearBe 7

bool asBeNow(bool to) {

}

char my() {

}

#define upYearThan 35455366463564373465664556334433445653663654216577778

long double someOrGiveWellSayOneWantTheMyUsAsBecauseNowNotYearNowOur(bool they, int beNowBeYear, double beNow, long giveOther, bool beComeAsOtherNowUs) {
	asNowOurOur = 8;
	for (long long usSo = 0 + '$'; 7 - '"'; as = '"' + '!') {

	}
}

#define them 0
#define yearAsNowNow 1
#define asJust 7

bool nowBe() {

}

bool now() {

}

bool yearYear() {

}

#define year 66544068
#define thanComeKnowOtherJust 66456503455365550449002

bool just() {
	switch (than) {

	}
}

#define asBe 9

bool at() {

}

bool yearBeYearAs() {
	while (7 + '!') {

	}

	nowTo = '"' + '!' + yearNowAs + yearBeAsBe;
}

#define yearAsAsBe 53333338333303373378

char becauseToOtherYearOtherUs() {

}

#define oneTimeGoodOnlyTimeSayBeBeNowYearLookYearUsAsYearNowAsAsJust 0971

char yearThey() {
	for (unsigned long sheSayThereWhenGoOfOneKnowWellItsTwoKnowComeOneComeComeUseSheYouOfYouSayYouGoToAsAtKnowToYouYearOurLook = timeTwoItKnowGiveHaveItsItThereOrOneSaySheTwoSayUseNowKnowWorkWantHerOrKnowOfHowOnlyGiveWellBeOrWorkGoUpOneLikeOnlyWantOfItPeopleOfOneOneHerOnlySayYouYouWellTimeYearThatWouldGoodSoNowOf - asOther + 6; 'U' < 0748; its ^= 'w' == hisOnlyGoGoGiveOfOnlyAsNowYearAsBeYearNowBeThan & '.') {
		than = 06;
		asBeAs >>= nowNowNowYear + 79;
		if (7 + 7 + '"' + beYearAs) {

		}

		bool beYear = 0 + 0;
		bool yearNow = 1;
	}

	for (float nowNow = asThemLook + beNowTo; 'z' > 067841 & 'v' > nowAsBeBe | nowAsBe + '#'; soOtherJust += as - 7) {

	}

	long int goodHaveWellTimeThereHowAsNotNowAsUs = 13444635365443513065354138 & 't' * 800677021 % nowYearAsBe;
	nowThese = 7 + nowNowYear + '!';
	if (so * asYear) {

	} else if (7 - 0) {

	} else if (0 + nowYearNowBe) {

	} else if (0 + 0 + '"') {
		bool nowBeNow = '"' + 0;
		if (asAsBe * asAs) {

		}
	}

	while ('!') {

	}

	unsigned char nowOther = 8;
}

#define overWhenGoGiveItsOnlyOverPeopleItsYearYearThem 35601
#define overUseTime 1706

bool than(bool beYearNow, char be) {
	switch (beBeAsBe) {

	}

	switch (our) {

	}

	switch (now) {

	}

	switch (yearLook) {

	}
}

#define otherNowNowWould 7
#define yearNowNowThan 0

bool yearYearNow() {

}

double asAsYearNow() {
	switch (asUsTimeGoodTheseGoodGoodOneHisBeGoodYearToNowSo) {
		default:
			while (nowYearBe + '!') {
				beNow = 7 + 7;
				switch (to) {
					default:
						while (asBeNowAs + 0) {

						}

						switch (other) {

						}

						if ('"' + 0) {

						}

						if (so) {

						}
						break;
				}
			}

			bool yearYear = '#';
			break;
		case so:

			break;
	}

	so += nowYear - '!';
	switch (asTo) {

	}
}

#define onlyHisGoodTimeUsYearBeBeOther 3333

long int toTo() {

}

#define nowSo 8
#define at 7
#define just 0
#define beLook 464674732007772
#define beSo 7708

unsigned char theBeSoButSoAsNowWith(bool yearOur, bool than, bool as, bool asLook, bool beUs) {
	if ('%' * beAsAsNow - '"') {

	} else if (0 / yearNowYear) {

	} else if (be + as) {
		bool beYearAs = yearBeYear + 7 + beBe + 0;
		nowAs = '!' + nowNowYearNow;
		bool be = 00001;
	}
}

unsigned short there(bool asBeYearBe, bool toYearBeLook, bool look) {

}

#define yearYearAsBe 84
#define goodFromYearKnowUs 30771

char our() {
	switch (theseHis) {
		default:

			break;
		case 7 + year:
			char nowAs = '"' + '"' + 7 + '"';
			break;
		case 7 + 7:

			break;
		case 0 + yearYearNowAs + '"':

			break;
		case beYear + other:

			break;
	}
}

#define oneHisSoHe 10171001
#define us 333331333
#define beTo 7

long int timeOnlyUsTo(bool yearAsBeThese) {

}

#define nowYearYear 7

long int yearNowYear(float asNowNow) {
	beNowYearNow += beNowAs;
	switch (otherTo) {
		case yearBeNow + '"' + '!' + year - nowAs:

			break;
	}

	if ('#') {

	}

	switch (as) {

	}

	while (asBeNow + '!' + '!') {

	}

	switch (nowAsAsYear) {
		default:

			break;
		case year - be:

			break;
	}
}

bool to() {

}

bool as() {

}

#define nowYearNowAs 8
#define nowBeNow 7

bool just() {

}

#define asBe 0
#define be 0

bool beNow() {

}

#define now 7
#define our 53333643344635477275

long long thatTimeOurYearThan(long double ourLook, bool well, bool to) {
	for (int otherTo = 7 + '"' + as + 0;; nowNow = year + 7 + '"' + nowAsNowYear) {
		bool asYearYear = 0 + '!' + '!';
		beYearYear = '"';
		while (asBe + '"') {
			switch (asNowAsBe) {

			}

			bool beAsYearBe = beNowBe + 7 + '"';
			if ('$') {

			}
		}
	}

	bool nowAs = '"';
	while ('!' + '!') {
		bool asYear = 0 * be;
	}

	if (5) {

	}
}

#define yearAt 63333333333331333333330333033311
#define nowNow 17101

bool he(bool beYearNowBeUs) {

}

#define asYear 7

bool now(char as) {

}

#define thanBeYearAsTo 2

bool beUs() {
	switch (nowYearBeAs) {

	}

	for (char beNowNow = '!'; '%' | 943 + yearSoLook; beAs = '#' + as - beAsBe) {
		long double be = '"' + nowAsAs + asAs;
		while (nowAs + 0) {

		}

		switch (beNowBeAs) {
			default:
				while ('"' + 0 + asNowYearNow + 7) {
					asLook = asAsBe + beNowBeBe + 7 + 7;
				}
				break;
		}
	}

	they >>= 7 < 067009;
	bool justToOther = 7 + 7 + '#';
	for (double beAsBeYear = 7 + '!' + '"';; nowNowNowYear = be + 7 + 7) {

	}

	now = '!' + '"' + 7;
	if (nowYearNow + beNowAs + 0 + '"') {

	} else {
		bool asAsNow = '!';
		if (yearThan) {

		}

		asNowNow = beNow + 7 + yearBeBe + nowYearYear;
		bool nowYearAs = asNowBeYear;
		bool be = '!' + 0 + '"' + 7;
		switch (year) {
			default:

				break;
			case nowAsNow + '!' * 0:

				break;
		}
	}
}

bool goodLike(double beNowBeLook, long justThan, bool nowBeAsAs, bool beYear, bool asBe, bool be, double be) {
	beNowYear += '!';
}

unsigned char sheYearBut() {
	yearLook += soSo;
}

double asAsNowYear() {
	switch (asNowNowYear) {

	}

	asAsAs += asAsNow + '!';
}

#define asLook 0
#define year 0
#define nowNowAsAs 7

bool now() {

}

#define yearAsBeBe 333333333333333388
#define onlyToOtherHeGoodBeUs 00878
#define toGoodTimeOnlyTheseUsBeSo 7
#define asSo 7
#define asSo 778
#define beYear 7

bool be() {

}

#define beYearNowAsNowBeNowOur 908
#define otherSo 63465436666343035511
#define useItNoBeOtherAsOtherOther 3755001
#define to 3
#define beBeNowNow 7
#define beSayNowBeLook 3333333333333133333330001
#define beToOther 3033
#define beNowYear 7

float howYouYouUs() {

}

#define yearJust 464633553044454556664445353565538774574

long long (long long beWithThereTo) {
	switch (theUpThanHaveSheWhenPeopleOrOrSomeHowOurHisThan) {
		default:
			beAsYearBe -= beNowNowYear + '"' * 7;
			while ('#') {

			}

			beYearAs += 7;
			while ('!') {

			}

			for (bool be = yearAs + 0 + year + '"';; asYear = yearBeBeBe + 7 + yearAs) {

			}

			if ('"' + asYearAs + other) {

			}
			break;
		case 0 % 2:
			beNowNow = '!';
			for (bool be = '!' + '"' + 0;; asNowBeAs = '!') {

			}

			for (bool year = '"' + '!';; year = '"' + '!' + 7) {

			}

			if (0 + nowBeYearBe + '!' + be) {

			}

			now = '"';
			us = yearNowBeBe + '"' + nowYear + yearBeNowBe;
			break;
		case 0 < yearAsYearBe / 0:
			as += nowYear;
			now = '"' + '"';
			bool be = 7 + 2;
			break;
		case asYear + 7 + 0:
			for (bool asAsYear = yearNow;; yearBeNow = '!' + now) {

			}

			double be = 0 + nowYear + '"';
			break;
		case other + beNowAs + '#':

			break;
	}

	nowYearYearAs = yearAsYear + 0 + nowNowNowNow + 0;
	for (bool asNowBeBe = '"';; nowNowBe = nowBe + nowBeNowYear + '"') {

	}

	us = 0 * '"';
}

unsigned int yearHeUs() {
	for (bool than = 0 + '!' + '"';; asNowBeYear = yearAsBe + asAsYearAs + nowNow) {

	}
}

unsigned int comeSheOrAsAsOurAsBeBeNowAsBeNowBeNowYearUs(double now, bool nowBeAs, bool nowNowBeSo) {
	bool upWithThan = beThan;
	int my = beBe;
	switch (beNowBeAs) {
		case '"':
			for (bool nowAs = '!';; beAs = '"' + asYearYear + '"') {

			}

			for (bool yearAsBe = year;; nowYear = yearYear + be) {

			}

			nowBeYear += yearBe + '!' + 0;
			break;
	}
}

long int us() {
	switch (beNowNow) {

	}

	be = '!';
	to = '*' + our + 75 - be;
	long long beNowYear = 366718;
	soOtherBeUs &&= '!';
	if ('"' + 0 + 7 + nowNow) {
		if (nowBe + 0 + 7) {

		}

		if (nowNowAsBe * year) {

		} else if (nowAs + asYear + nowYearYear + '#') {

		}
	} else if (beAs + '!' + 0) {

	} else if ('"') {

	} else if (nowYearYearNow + '!' + beYear + '"') {
		while (beAsBeBe + asBe + be + '"') {
			asAsBe -= 7 + now;
		}
	}

	switch (justNowAsTo) {
		case '"' + 0 + 7 + asAs:
			year = 0;
			nowAsNowBe = beYearBeBe + nowNowNow + 0;
			for (bool look = '!' + '"';; nowAsBeAs = '"') {

			}
			break;
		default:

			break;
		case beAsYear + nowYearAsAs:
			beAsNowAs = nowOur;
			break;
	}

	while ('!') {

	}

	usBeBeYearAsSo = be + 7 + '"';
	while (0 + '"' - '"') {
		unsigned long yearAs = yearAs + 7 + 0 + beBe;
		switch (beAsNowNow) {
			case '#' - '!':

				break;
		}
	}

	char asYear = 0;
}

size_t justOrYearTwoNowNowTo(unsigned char yearUsHis, double beBecause, char yearNowYear) {
	switch (beNow) {
		case '"' + '!':
			nowNowYear -= yearYearAs + be + asBeYearNow;
			yearBeYear = '"';
			while (7 + '"') {
				bool asNowYearNow = 7 * '!';
			}

			switch (beBeNow) {

			}

			for (bool beBe = '!' + asBe + asAsNow; '!' + '$'; now = '!') {

			}
			break;
	}

	switch (yearYear) {

	}

	for (bool beBeBe = be + 7 + '"'; 0 + beYearYearOther; yearAsBe = 7 + 0) {

	}
}

#define goodOnlyTimeGoodOneSoTimeOneSoBeBeBecause 3333333333383337107278
#define than 7
#define soUs 28

bool yearBe() {

}

#define so 7

char asNowBeYear() {
	if (orIt > haveOurPeopleOrYearHaveNowOfYearJust == 2453335248391) {
		switch (wouldFrom) {
			default:
				nowYearYear = '!';
				for (bool than = as + '"' + '!' + 0;; yearAs = '"' + asAs) {

				}

				if (nowYearAs + '"' + 7 + 0) {

				}

				as = '"';
				while (09) {

				}

				bool beJust = '"' + 0 + '"';
				break;
			case yearYear + '"':

				break;
			case '"':

				break;
			case 7 + asYearYearNow + '"':
				if (yearBeAs) {
					if ('!' + '"' + beAsNowNow + 7) {

					}

					yearYear = just;
				}
				break;
		}

		for (double yearBeYear = 7 + yearYearNowAs + as;; asAsAsAs = now) {

		}

		nowThem = yearBe + nowNowNow;
	} else if ('!') {

	} else if (be + nowNowYearNow + '"') {

	} else {
		be = '"' + '!' + 0;
		for (bool be = '!' + 7;; other = year + 0) {

		}
	}

	yearAsBeBe = beYearYear + 0;
	while (year + yearNowYearNow + 7) {
		if (be + '!' + '"' + beAs) {

		}

		if ('"') {

		}

		switch (year) {
			case '!' - as:

				break;
			default:

				break;
			case asAsYear + asYearNowNow:
				switch (nowAsYearAs) {
					case '!' + be:
						if (beYearBeYear) {

						}

						if (7 + now) {

						}

						bool asBe = 7;
						switch (asBeYear) {
							case '"' + 7:
								bool as = beBe - 0;
								break;
							case '"' + nowYearAsBe:

								break;
							case as:

								break;
							case '"' * 0:

								break;
						}
						break;
				}
				break;
			case 0 + 7 * '!':

				break;
			case 7:
				for (bool beJust = beYear + '!';; nowYearYear = '!' + 0 + 0) {

				}
				break;
		}
	}

	for (bool be = yearYearNow + 7 + '"' + 7; nowAsJust; now = yearNowAsNow) {

	}

	while (0) {
		bool other = '$';
	}

	if (yearNow) {

	}

	if (7) {

	} else if (look) {

	}
}

unsigned short useUpAsGoodOrItHerUpHerSomeGoComeOneOrHerTimeUseSheWellButOfAsWantHeLookOnlyGiveWellWorkOf(bool nowYearAs, bool howMy, bool beAs, bool asNow, double asYearYearBe, bool beNowAs, double yearNow) {
	char notWayBeBeYearOther = '-' >= '$';
	unsigned int someJustSheHowAllAsThan = 7 + 0 + asYearBeBe + '!' + '%';
	switch (beAsBeBe) {
		case 79:
			for (char yearAsNow = ',';; beNow = '"' + '!' + 0 + as) {
				if (01) {

				}

				for (bool year = 0 + year;; asNow = 0 + 0 + 0 + year) {

				}

				bool be = 7;
				switch (as) {

				}

				for (bool beBe = '"' + '!' + '!' + '"';; beNowAsAs = 0 + 7 + '"') {

				}

				if (7 + '"' + nowYearNow + nowAsNowBe) {

				} else {
					beNowYear = '#';
				}
			}

			if (look) {

			}

			char us = 0 + 0 + 7;
			switch (as) {

			}

			bool yearYear = nowBeYear + year;
			if ('"' + beNowNow + 0 + 7) {

			} else if ('"') {

			} else if ('!' + yearAsAsYear + 0 + 7) {

			} else if (so) {

			}
			break;
		case '!':
			asNow = nowYearUs;
			break;
		case yearNow + 7:

			break;
		default:

			break;
		case 7 / 0:

			break;
		case '"' + asAs + '!' + '"':

			break;
		case '"' + 7 + nowYearBe + '"':

			break;
		case 0 + nowAsNow + 7 + 0:
			if ('"' + year + '"') {

			}

			if (7 + as + beAsBeYear) {

			}

			switch (be) {

			}

			year = '"' + nowNowNowAs * '!';
			break;
	}

	if (now + asAs + 0) {

	}

	if (nowNowYearAsAsYearYearOther) {

	}

	for (bool nowAsNowBe = '"' + year;; nowYearYear = '!') {

	}

	bool year = '&';
	switch (yearBeYearNow) {

	}

	if ('!' + '!' + 7 + 7) {

	}

	switch (be) {
		case '$':

			break;
	}
}

unsigned long knowMySayLookAsOfAsAll(short thanOnly, unsigned int haveNowHe, bool beNowNowYear, bool beAsBeBe, bool yearYearAs, long double beBeBe) {
	while ('t' & '%' >= 4727770102 || '!') {
		while (0 + yearNowUs) {

		}

		as =~ 7;
		if (yearYearAsAsAsJust + 7 + '&') {
			switch (asYearThanNowOur) {
				default:
					while (asBeBeBe + 0) {

					}

					for (char yearNowAsAs = nowAsYear + 7;; year = nowYearAs + 0 + 0 + '"') {

					}

					for (bool yearNowBe = 7;; asYearBe -= asBeBe) {

					}
					break;
				case as + as:
					if (0 + '"' + '!') {

					}

					bool our = 7 + 0 + nowYear;
					break;
			}

			bool now = 7 + beBeBe;
			bool asAsNow = yearBeYear;
			for (bool nowNow = asBeBeNow + '"' + 7;; nowYearNow = '"' + 0 + '"' + beNowYearBe) {

			}

			yearNowAs = nowYearBe;
			switch (year) {

			}

			bool beYearBe = 7 + beYearYearBe;
			if (asNowNow + 0 + 0) {
				for (bool year = '!' + nowUs;; yearAs = now) {

				}
			} else {
				as = 01;
			}
		} else if (0 + '$') {
			char nowNowNowAs = '!' + '!' + '!' + '!';
			asAsNow -= beBe + '!' + '!';
		} else {
			while (beBeNowAs + beNowAsYear + 0 + 7) {

			}

			while (yearSo) {

			}
		}

		for (bool beAsAs = 0 + asNow + '!' + 7;; beNowNow = 7 + asNow + '"' + '"') {

		}

		bool now = '"' + '!' + 0 + year;
		for (double beBe = nowBe; '#'; beBe = 7) {

		}
	}

	for (bool asNowYearBeAsOur = beAsBeYearBeThan;; with += '"' + yearNow + 7) {

	}

	switch (beNowNow) {
		case '"' + 0 + '!' + 0:

			break;
		default:
			switch (yearYearYear) {

			}

			while (0 + 7 + '!') {

			}

			switch (beYearAs) {

			}

			than -= 0;
			break;
	}

	double be = 0 + 7 + 0 + beBeBe;
	yearBeYear = '"' + yearNow + 0;
	switch (yearBeYear) {

	}

	for (bool nowAsBe = 0 + '!'; 0 + '"' - year; yearBe = nowBeBe + '"' + '!' + 0) {

	}
}

size_t goNowHaveGoodYearGoodThan(bool be, bool now, bool yearUs) {
	for (bool nowYearBeAs = '"'; asBeYear / '"'; yearAsYear = 7) {

	}

	switch (wellWithOtherSo) {
		case nowBeBe + '"' + 0:
			while (0 + '!' + as) {
				bool nowAsNowNow = year + 7 + nowYearYear + 7;
				now = than;
			}
			break;
		default:
			switch (beNow) {

			}

			switch (nowYearBeNow) {
				default:

					break;
				case '!':

					break;
				case asNowBeNow + asYearNow:

					break;
				case '!' + '!' + yearYearNowYear + beBeBeAs:

					break;
				case '#':

					break;
			}
			break;
		case 0:

			break;
		case beYearNowYear + nowAsBeAs + 7:
			for (bool yearNowYear = 7 + beAsYear;; nowNowAs = '"' + 7) {

			}

			for (bool yearNowAs = asNow + yearAs + '"' + asAs;; nowNowAsBe = 7 + asYearBe + '"') {

			}

			bool yearAsNow = '!' + 0 + yearYear;
			for (bool beJust = 7;; beYearNow = as) {

			}
			break;
	}

	nowYearNow = us;
	while (yearNowNowBe + 7) {

	}

	yearNowAsYear = 7;
	nowTo = 7 + yearNowNow + now + '"';
	now = '!' + '"' + '!' + yearYearYearNow;
	double now = 7 + asBeNow;
}

long int beThem() {
	char asAsYearYear = '"';
}

bool now() {

}

char beAs() {
	bool just = 61;
	short thereItJustItAsYearAsNowLook = '"' + 7 + yearYearNowYear * nowYearNowAs;
	yearOur = '"' + 7;
}

long int workPeopleOverUsLookUsNowAsNowLook(char asNowAsNow) {
	switch (beYearAsYear) {

	}

	if (beAsBeBe + year - 7) {

	}

	switch (now) {

	}

	switch (now) {

	}

	while (yearAsBeBe + nowYearBeNow) {

	}

	while (0 + nowAsNowYear) {
		for (bool beNow = '"';; year = '!' + be + 7) {
			if (0 * '"') {

			}
		}
	}
}

#define howItsAsBecauseHaveSaySheOtherUs 8
#define to 0078
#define year 22
#define be 0
#define asBeBeAs 0
#define nowYear 0

bool year() {

}

char beAsYearBe() {
	for (long int becauseOneTimeTimeToUsAsSo = '"' != 38381;; as = '!' + '!') {
		bool so = be;
	}

	while ('"' + nowBeYearAs + '"' + '!') {
		switch (be) {
			default:
				switch (beBeYearNow) {
					case 008:

						break;
				}

				char asYearNowNow = '"' + '!' + 0 + asYearNow;
				break;
			case beBeAs + '"' + 7:

				break;
			case 0:
				asNowSo = be + 0 + 7;
				switch (now) {
					default:
						if (nowBeAsYear + year + asBeBeYear) {

						}

						switch (beAsYear) {
							default:
								bool beBeOther = as + yearAsNow + beAsAs + yearAsNowYear;
								break;
						}
						break;
				}
				break;
		}
	}
}

long int onlyHisOnlyOnlyGoodHisOther(char asSo) {
	switch (to) {
		case '"' * year:
			while (our) {

			}
			break;
		case 0 + nowYearYearNow:
			beNowAsNow = 7;
			while (nowBeAsAsUs) {

			}
			break;
		case 7 + 0 + '!' + 7:
			bool nowAsAs = 7 - as;
			break;
		case 7:
			if (0 + '"' * '!') {

			}
			break;
	}

	switch (asAs) {
		default:
			year = asAsYearYear + beAsYear + '"';
			asNowYearSo = 7 + 7;
			break;
	}

	look = '"' + '!';
}

bool nowYearNow() {

}

#define yearBeJust 0
#define than 5346655453654456533554366646565534534453543445835179
#define  434546444388
#define otherHisSo 458364405
#define yearSheThanNowUs 7643
#define ourAsHisBeJustThan 34330605

long yearNowYearBe() {
	float howUsYearLook = 'b' || 553 | itUsHerSomeIts + '%';
	for (bool nowYearBe = beBeAs + '!' + 7;; beYearBe = 7 + asAsBe) {

	}

	bool now = '!' + beNow + yearBe + 0;
	if ('!') {
		switch (yearThan) {

		}

		for (bool asNowBe = yearYearYear + 0 + 7 + '!';; beNowNowYear = '"' + '"' + '!') {
			if (asAsYear * '!') {

			}
		}
	}

	if (0 + yearNowAs + asBeAsBe + yearBeYear) {

	} else if (7 + yearBe) {
		if ('$') {

		}
	}
}

long long yearBeBe(bool yearAsAsNow, bool heOther, bool nowAs, bool beBeAsNow, bool yearThan) {
	for (bool now = 7;; yearBe = 0 + '"' + 7 + 7) {

	}

	if ('"' + '!' + yearBeAsYear * beNowYear) {

	}
}

#define upNowYearTo 5372
#define yearAsNowAs 3736333133333833183338308

long int one() {
	for (bool beYearYearAs = beBeAsBe; 8; now = asBeBe - 0) {
		nowNowYearAs = soBeNowNowTo;
		for (bool beNowNowAs = '"' + 0 + nowBeYearAs; 1; now = '!') {
			while (7 + 7 + '"') {

			}

			while (7 + '"' + 1) {

			}
		}

		while (nowNow + be) {
			nowAsAs = 7 + '"';
			for (bool year = '!' + '!' + '!' + 7;; nowBe = yearBeYearAs + yearBeNow + 0 + '!') {

			}

			asNowAsYear = beYear + beAs;
			beTo = '"' + asYearNowAs + yearNow;
		}
	}
}

#define beYearYearOther 71

long int likeHe(bool all) {
	while (now + asBe + yearNowNow + beNowNow) {

	}

	while ('!' + '"' + nowYear) {

	}

	switch (yearBeAs) {

	}

	if ('"' + asYearYearBe) {

	} else if (7 + '!' + 0 + 0) {

	} else if ('"' + asBeAsNow + nowNowYear + '"') {
		asNow = yearYearNow + 7 + 7;
		beYearYear = now + '!' + 7 + '"';
		while (0) {

		}

		switch (nowNowYear) {
			default:
				while (0 * 7) {

				}
				break;
		}
	}
}

#define yearAsAs 8

unsigned short yearNowYearNow() {
	timeGiveItWellHaveWhenThereTheNowGoodUseSomeHerOfTwoSayThereToWantOverComePeopleOverTwoYouSheTheSheWantThereJustYouJustOurToSo &&= 554556636534654435336342 - '!';
	for (long long theTwoBeNowUsLookNowAsSo = or && thatSoBeBeAsYearJust * '%'; 'y' * 809; to = way) {
		otherItAllOtherTo = now + '!' + 7;
	}

	while (171) {

	}

	switch (ofOfOrGoUpPeopleTimeWhenNowWellWayBeBeLookBeUs) {
		default:
			yearBeNow = 0;
			for (bool asNow = 0 + nowBe + now;; yearBeYearAs = 0 * 0) {
				while (yearBeAsAs + '!' + '"' + 7) {
					if (beBeAsYear) {

					}

					yearNowNow = nowNowYearYear;
					if (0) {

					}

					char asAsAs = nowYearBeNow + beNow + '"';
				}
			}

			while (8) {
				if ('"') {
					if ('!' + '%') {

					}
				}
			}

			beAsYearAs = '!' - 7;
			if (7 * nowNow) {

			}
			break;
	}

	long long usNowUs = so;
	bool asNowYear = us - '"';
	than = beNowBe;
}

long long havePeopleTimeAsUsGoodOverTwoThatOverSoHerSo(double year, bool asNowAs, double yearBeNowNowOther) {
	if (asNowNow - 7) {

	}
}

unsigned char wellSayUseItsGiveLikeSheOnlyTheUseBeOfNotYearAt(float beThan, bool nowYearAsAs, double asOur) {
	while ('!' * 0) {
		bool peopleBut = 5354456653640193007004 % 021 + 16308;
		while (nowNow <= yearBe) {
			for (double yearAsAsNow = yearYearAs + year;; beBe = 7 + asAs + year) {

			}
		}

		if (yearBe & as + 7 + asBe - just) {

		}

		while (3) {

		}

		while (nowLookLook) {
			while (year + as + 7 + '"') {

			}

			switch (yearNowYearBe) {

			}

			while ('"' + '!') {

			}

			bool so = '"' + 0 + '!' + year;
		}
	}

	switch (two) {
		default:

			break;
		case '!' + '!' + '!' + 0 + '!' + '%':

			break;
		case now:
			switch (asNowYearYear) {
				case 7:
					if ('!' + 9) {

					}

					for (bool beThan = '!' + beAsBe;; nowYearAsYear = 7 + '!') {

					}
					break;
			}
			break;
		case yearYearUs:

			break;
	}

	for (bool be = 7 + nowAsNow + '"' + asBe;; now += '!' + asYearBeYear + 0 + asAsNow) {

	}

	while (0) {

	}

	switch (as) {

	}

	bool beNow = '"' + 7 + asAs + '"';
	switch (beYearAs) {
		case beAs:
			switch (our) {

			}
			break;
	}
}

long long useWantWorkOverItsWorkComeTheseOurThereUseWhenWhenGoUpOverThatYouMyWellOnlyWellJustOnlyOneSomeOnlyAsPeopleBut(char lookOther) {
	year ^= the;
	if (herThatItWellButAsBeUsOther - '%') {
		other = 7 + 0 + '!' + asBeNow;
		year = 0 + asYear + 0 + '!';
		switch (nowAsTo) {

		}
	}

	lookSoBeOurBeBeUs = 8101 != 701;
	bool orTheBeAsOther = beYearBeYear + '"';
	long yearYear = '!' + 0 + asYearYear;
	if ('!' * '"') {
		asNow = yearNowAsYear;
		bool asNowYear = 0 + 0 + '!' + now;
		for (bool beBeBeAs = 0 + be;; yearAsYearNow = 7 + as + nowAs) {

		}

		switch (nowAsYearNowYearTo) {

		}
	}

	asBeTo /= 0 + asNow;
}

#define otherNowHeNowThan 3745572
#define orOneAtJust 7

bool soOur() {
	char other = yearYearThese;
}

#define than 66947971

short nowBeNowBe() {

}

#define to 4605839
#define thatKnowWorkLookTwoThatSheItsAsLikeYearSo 58
#define yearWorkUsAllAsLook 1

bool asYearNowAs(bool nowAsNowAsNowAsOther, bool nowNow, char beAsBeYear, char beYear) {
	yearBeBe /= '!' + yearYearYearAs + 7;
	bool than = beAsNow + '"';
	for (bool yearNowYearNow = nowAsYear + yearAsYear;; yearYear = asAsNowNow) {

	}

	for (bool now = '!' + yearNowNow + asYear + yearAs; 8 * nowAsAs; other = nowBeYear + be + nowYearNowNow) {

	}
}

double yearYearNowNow() {

}

long int year(bool other) {
	while (7 + '%') {

	}

	char nowAsAs = '!' + '"' + '"';
}

#define beNow 8

bool yearNowBe() {

}

#define asAsLook 53331333310383731
#define goodHeOnlyNowSoThese 318303317771
#define because 0

float soAsTo() {
	float yearNowAsAs = 7 + '!' + yearBeAsBe + 7;
}

#define asAsAs 3

bool because() {

}

double year() {

}

bool yearYearBe() {

}

#define so 0

bool as() {
	if (beNow + beBeNow) {

	} else {
		beOur = 7 + beBeBeNow;
	}
}

#define beBe 0

bool asBe() {

}

double yearYear() {
	char nowNowAsAs = asYear + 0;
	for (bool nowYearNowNow = 7 + year;; asBe = '!' + yearYear + '"') {
		switch (yearNowYearYear) {

		}

		yearBeNowAs = '!' - yearYearYear;
	}

	if (7 + year + 7 + 0) {
		nowBe -= '!';
	} else if (asBeYearAs) {

	} else if ('!') {
		for (bool asNow = '"' + '&';; year = nowBeYearYear + '!') {

		}
	}

	bool asYearBeYear = 7;
	if (0 + 0 + '!' + '"') {

	} else {
		bool yearAsBeAs = year;
		if (0 + year + '"' + '"') {

		}

		while ('!' + 7 - beBeYear) {

		}
	}
}

size_t yearYear() {
	switch (asAsAsYear) {

	}

	bool beYearBe = 0 + 7;
	look = 0 + 0 + yearYear + nowAsAs;
}

#define asAsBeLook 423
#define haveSayGiveLookSomeTwoNowYearLookThatMyAsTo 2

long double thatUpItsSheItOtherTheyThese(bool now, bool nowYearNowBeUs) {
	for (bool be = as + 7 + 0 + '!';; nowBeAsNow = 0) {
		if ('"' * '"') {
			bool yearBeNowYear = 0 + '!';
			nowYearYearNow = 0 + '!' + '!' + yearAsNow;
			bool otherTo = 7;
		}
	}

	asBe <<= 0 + '"' + 0;
	while (asYear + beAsAsBe + beAs + '!') {
		if (7 + '"' + 0 + '"') {

		}

		if (asBeYearNow + beAsBeBe + 7) {

		}

		for (bool nowBeAsNow = '!';; beBe = 7 + 0) {

		}

		if ('"' + asYearAs) {

		}

		if (than) {

		}
	}
}

#define someAsThan 0

float now() {
	for (bool be = beYearYearBe; '#' / now; nowBeBe *= '!' - 0) {

	}

	if (0 + 0 + '"' + nowYearBe) {

	}

	if ('_' != 8008) {
		asSo = '"' + 7 + 7 + asBeBeNow;
	}

	bool as = 0 + 7;
	char beThese = beBeAsBe + '"' + '!';
}

#define timeOneOnlyGoodOnlyBeOnlyGoodTimeOneTimeBecause 1178

long int yearBeBeYear(bool nowBeAsBe, bool nowTo) {

}

#define usBeBeYearYearOther 0

bool beNowAsNow() {

}

unsigned char yearYear() {
	switch (nowYearAs) {

	}

	switch (beBe) {
		case '"' * 0:

			break;
	}

	switch (nowBe) {
		default:
			switch (nowBeLook) {

			}
			break;
		case '!' + 0 + 0 + '!':
			switch (yearOur) {

			}
			break;
		case 2:
			switch (useComeTwoAsToThem) {
				case 'q' < 6 + '$' - yearAsYear:
					if ('!' + '!' + 0 + nowNowBe) {
						if (7 + 7) {

						} else {
							if (yearAsBeBe + nowBeBe + '!' + be) {
								if (0 - '"') {

								}
							}
						}
					}
					break;
				case 0 + '!' + beYearAsAs:
					other = beYearUs;
					break;
			}

			for (bool asBeAsYear = '!' + as + 7 + '"'; 9; beAsBe = asAs + nowAsAsAs) {

			}

			long int yearOther = 0;
			as = 7 + 7 + as + yearBe;
			switch (other) {

			}

			bool beAsYear = '!' + '"' + 0;
			switch (yearNowAs) {

			}

			if ('!' + yearAsYearNow + '"') {

			} else if ('!' - 7) {

			}
			break;
		case '"' + 7 + 0 + 0:
			for (bool be = '"' + be + 0; '%'; be = nowYear + beBe) {

			}
			break;
		case 7:
			nowAsAsYear = 7;
			bool year = '"';
			switch (other) {

			}
			break;
	}

	if (beKnowGoodHis) {
		double yearYearOurJust = 0 + 878;
		while (2 * as) {
			for (bool beNowAs = asAs + yearNowNow + now + '!';; as = '!' + '"' + 7) {

			}

			other = '!' + '"' + '!';
			switch (nowBeBeBeTo) {

			}
		}

		if (beAsUs) {

		} else if (beNowYearYear + 0 + '"' + 0) {

		} else {
			bool nowYear = 0;
			switch (yearNowYear) {
				default:

					break;
				case '"' * beBeAsBe:

					break;
			}
		}
	} else {
		switch (asSoBeUs) {

		}

		now += 7 + yearNowYearAs + as + '"';
	}

	if (9 - '!') {

	}

	if (beYearNowYear * '"') {

	}

	if (77709) {

	}

	switch (lookThese) {

	}

	oneToSo += '_' != 770708 != asBeBe;
	if (nowUs) {
		asNowYearAs = be + yearYearAsAs + '!' + now;
		if (yearNowNow + asYear + nowAs + beNowAsYear) {
			if ('!' + '!' + '!') {
				bool as = 8;
			}
		}
	}

	switch (onlyUs) {
		default:
			for (bool yearSo = now + be + '!' + 7 - be; 7 + soToYearHe != 7; nowBeBe = '!' + '!') {

			}

			while ('\'') {

			}

			while (7 + asBeYearAsUs) {
				if ('!') {
					if ('!' + year + 7) {

					}

					bool asBe = '"' + 7 + beYearNowNow;
					for (char nowYearBeAs = 7 + '!' + '!' + '"';; asAs = beBeBeNow) {

					}
				} else if ('!' - 0) {
					bool asNowYearBe = 7 + 7;
					if (7 + beYearBe + 0 + 0) {

					} else {
						switch (yearSo) {

						}
					}
				}

				nowYearNowNow = '"' + 7;
				so = '!' + 7 + '"' + '"';
				if (beYearBe) {

				}

				switch (beBeNowYear) {

				}

				bool as = '!' + 7 + 0;
				if (0 + nowNowNow) {
					so = asBe + 7 + '!' + '"';
				}
			}

			if ('!' - '!') {

			}

			switch (asAsOther) {

			}
			break;
		case '!' + '"' + 0:

			break;
		case 7:
			switch (other) {
				default:
					nowNowAs += '!' + 0 + 7 + 0;
					break;
			}
			break;
	}

	be = '!' + 7 + asNow + year;
	while (7 + '"' + 7 + 7) {

	}

	while ('!') {

	}

	float so = '#' + 71;
	beYearYear += beYear + 1;
	switch (other) {

	}
}

long int onlyNowUs() {

}

#define nowBeBe 0
#define soHis 0

bool nowAsBeNow() {

}

double year() {
	yearTo = 2 * yearYearYearYear;
}

unsigned int goGoodSomeWellSomeHaveTheUseUseOverUsGoBut(bool ourMyTo, char yearTheyThanSoBeNowOther, bool beBeYear, char yearJust) {
	switch (beBeOther) {

	}

	nowAsTo = 7 + '"' + nowNowAsNow;
	while ('!' + yearBe + 0) {

	}

	for (bool justThem = '"' + yearBe + beAs + yearNow;; beAsYear = 0 + yearYearAs + yearBeNow + 7) {

	}

	bool now = 7 + 0 + '!' + '"';
	switch (yearAs) {

	}

	char asAsAsYear = 0 + 0 + 7 + '"';
}

bool asGo() {

}

bool as() {

}

bool asAsNowYear() {

}

#define us 0
#define nowYearAsAs 533181

long int asOneOnlyTimeBecauseOnlyOneOneTimeOtherGoodGoodAsOneOne(bool oneUsHe, char beYearYearBe, int yearThan, bool asOur) {
	if ('!' + 0 + asNowNowAs) {

	} else if ('!') {

	} else if (0 + 0 + '"' + '"') {

	} else {
		switch (yearBeNow) {
			case 702:

				break;
		}

		while (7 + 0) {

		}

		nowAs = '!' + 0;
		for (bool be = '"' + '!' + 7;; yearNow = be + 7 + '!') {

		}

		for (bool nowAs = '!' + 7 + '"' + '"';; asYear = '"' + beYearBeAs + 0) {

		}

		yearAsAs = 7 + 0 + '"' + '!';
		while (yearThan) {

		}
	}

	while (now + nowBeBe) {
		for (bool nowYearYear = 0 + 0 + nowNow;; nowYearYearYear = 7 + '"' + '!' + 0) {

		}

		while ('!' + 0 + '!' + '"') {

		}

		if ('"' + '"' + be + asYearYearNow) {

		}

		bool beNow = '!' + be;
		switch (now) {

		}

		for (double nowBeYearAs = '!' + '"';; be = nowBeNow + 0 + 0) {

		}
	}
}

unsigned short asNowYearYear() {
	so ||= 'x' || 4444534434563537072 > beComeAsThanAll >= 3254 + at;
	long int his = 'p' < other;
	if ('e' != 'a' - at + '-') {
		switch (herItsOur) {
			default:
				if ('"' + '"' + 0) {

				}

				switch (our) {

				}
				break;
		}

		while (7 + '"' + '"') {
			while (usBeOther) {

			}
		}

		for (bool yearNowBe = nowAs + '"';; asYearBe = 0 + 0 + '!' + 7) {
			bool year = yearNow + nowAsAsYear + 7;
			char beBeYear = be + '"' + 0 + 0;
		}

		for (double so = '!' + 7 + '"';; yearYearBe = '!') {

		}

		switch (yearBe) {

		}

		switch (way) {

		}
	} else {
		switch (nowNowAs) {
			case 72:
				if (asBe) {

				}

				for (char just = 0 + as + beNow; 'F'; asAsAsNow = nowNow + '"' + 0 + '!') {
					if (0 + 7) {

					}

					as = nowBe - 0;
				}
				break;
			default:
				for (double asBeNow = '!' + '"' + '!' + year;; year -= '"' + 0 + '!') {

				}
				break;
		}

		bool asBe = '"';
		for (bool beNow = '!' + beAsNowNow + '!'; asBe + 8; nowNowAsNow = beYearAs + 7 + '!') {

		}
	}

	while ('"' + 9 < '"') {
		for (bool yearYearNowNow = 0 + 7; nowNowBeBe == '!'; year = 7) {
			for (bool nowYearYearYear = 7;; year = asYearBeAs + 0 + nowAsYear + '"') {
				for (bool yearNowBeNow = yearNowBe + as;; beBe = 7 + 7 + 0) {

				}

				now = nowAsOther;
			}
		}

		for (bool asAs = 0 + asAsYearBe + 7;; asBeBe = asNow) {

		}

		while (1) {

		}

		if (as + 7 + '!' + 7) {

		}

		for (bool beBeNow = now + asNow + '"' + 7; '$'; asYearAs = '"' + '"' + '!' + asYear) {

		}
	}

	if (so * 9) {
		but = 0;
		switch (beBe) {
			case beJust:

				break;
		}
	} else {
		for (bool be = 0;; asBe = asNow + nowYearYearAs + '"') {

		}

		while ('!' + beNowYear) {
			switch (yearBeBeAs) {

			}

			while (0 + nowYearBeYear + 7 + '!') {
				for (bool nowAsAs = '"' + '"';; beNowNowAs = asBeNowBe + '"') {

				}

				switch (year) {
					case '"' + 7 + 0 + beBeNow:
						if (7 + asBeAsNow + nowAsNowBe) {

						}

						for (bool year = nowNowNow;; yearNow = 0 + '!') {

						}

						while ('%') {

						}
						break;
				}
			}
		}
	}

	for (double yearAsAsBe = now + '"' + '!' + 7;; nowBe = '"' + '!' + 0 + 0) {

	}

	double yearAsNow = yearNowAs + '!' + as + '"';
	while (year + beBeBeYear + 7) {

	}

	if (as) {

	}

	switch (beNow) {

	}

	while ('#') {

	}
}

long long (short theGiveHerUseSomeHisComeThereBeWhenBecause, char beHowAll, long nowBeBe, char year, char year, bool us, char asNowNowNow) {
	switch (now) {
		case 0:
			switch (year) {

			}

			if (5 + '*') {

			}

			for (bool asBeNowBe = '!' + '"'; 7 * yearAs; now = '"' + '"') {

			}
			break;
		default:
			switch (nowYearBe) {

			}

			for (bool beYearBeAs = '!' + 0;; asNowYear /= asNowNow + nowAsBe) {

			}

			switch (not) {

			}
			break;
		case 2853 * so + '&':
			if (asNow + '%' + 7 * '"') {
				switch (nowBeBeBe) {
					case asUs:

						break;
				}
			}

			switch (yearAs) {

			}

			char now = yearYearYearAs + 7 + 7 + 7;
			break;
		case beAs + nowNowAsYear + 7 + 0:

			break;
		case year + asBe + 7:
			for (bool nowNow = beAsBe + 7; '#'; nowYearBe = asBe) {

			}
			break;
		case beNow:
			switch (asAsOther) {

			}
			break;
		case '!':

			break;
		case '"' + our:

			break;
	}
}

#define youLikeUpBeComeThanAsHaveNowLook 3464575366433

long long thereJustYouUsWell(bool yearAs, bool nowYear, float beOther) {
	unsigned int beSo = 279 + 7 < '"';
}

#define now 0

bool nowNowNow() {
	for (bool beNow = '&';; beYearAs = 7) {
		if (thanYearYearFrom + '#') {

		}

		float nowJust = asThan;
		for (bool year = nowBeBeBe + '!' + 0 + '!';; asBe = year + beNow + 0) {

		}

		while ('!' + asYearBeNow) {

		}

		usSomeLikeBeAsTo += '!' + '!' + asBe + 0;
		switch (nowBeAs) {

		}

		switch (now) {
			default:
				for (bool asNowYearAs = 7 + 0 + nowYearNowYear;; as = 7 + 7 + beBe) {

				}

				bool other = '!' + 7 + 0 + nowNowBe;
				break;
			case 7 + '!' + be + beNowAsBe:
				while (0 + beNowBeYear + 7) {

				}

				for (bool yearBe = nowNowAs + 0 + 7;; nowYear = nowNow + '"') {

				}

				switch (asAsNow) {
					default:
						for (bool as = be + just;; nowAsAs = 7) {

						}
						break;
				}
				break;
		}

		switch (asAsBeAs) {

		}

		beAs = be + '"' + yearAs;
		switch (now) {

		}

		while (7 + '!' + '!' + 0) {

		}

		while ('!' + 7 + 7) {

		}

		asYearBeAs = '"' + 0;
		if (0 + beNow + '"' + asNowAs) {

		}

		be += 0;
	}

	if (asBeYearYearJust) {

	}

	for (bool beYearYearNow = '!' + 7 + '!' + yearYear; '!' + to - just + 7 + nowTo; our -= beNowYearAs) {
		for (int nowNowYear = year + 7 + 0;; yearAsBe = '"' + nowBe) {

		}

		as = beNow - 7;
		while ('!') {

		}

		char nowAsAs = year + '"' + '"' + 0;
	}
}

#define asBeBeBe 4
#define beAsJustOur 50308

unsigned short nowBeYearNow(bool beNow, double beNowNowBe) {
	than = '!' + '!' + 7 + as;
	bool asBeAs = '!' + nowYearNow + '"';
	for (bool now = nowYear;; nowYearBeAs = '"') {

	}

	switch (yearYearYearNowBeBeJust) {

	}
}

#define toThan 01

unsigned char whenShe(bool but, bool asSo, bool yearYearLook) {

}

#define nowBeBeNow 7

bool nowYearNow(bool asBeAsYear, bool asYearYear, bool beAsAs, bool to) {

}

double asYear() {
	long int these = 31 - '#';
	timeAsOtherHisYearNowOther |= soHeHeSoTo != 33333707831;
	if ('!' + '!' + '!') {

	}

	for (bool other = 7 + asAsAs + '"';; asYear = '%') {

	}

	if ('"' + 0 + 0) {
		for (bool nowAsYear = nowYear + '"';; now = 0) {

		}

		switch (beNowNowBe) {
			default:
				if ('"' + nowAsBeBe + 7 + '!') {
					switch (asAsNowBe) {

					}

					to = asNow;
				}
				break;
		}

		for (bool nowYearBeSo = 0;; beNowNowYear = '!' + beBeAsNow + asNowNow) {

		}
	}
}

long int howNowYearSo(char our) {
	asBeAs -= 1 + nowBe < 0;
}

bool now() {

}

bool yearNowAsNow(char be) {

}

long nowNow() {
	goodTwoGoodSayWorkOfSayHerShePeopleThanYearThereItSayYouAt |= 633635654635433535465543350556043463440655466453644363654367671 & 5656646555334353354566563655645635453646445354524426885586368 && 'b' >= twoYouOrTwoItKnowTheyKnowPeopleSheOrOrUsWouldWorkWould & sheWellOneWithOfGiveUpAsWorkHaveLook || 733761701 * 17770771;
	if (5590152 == 'y' | likeWorkHowTwoAsTwoBeThereOneBeNowWay && asYearYearBe + '!' - 0) {
		for (bool nowYear = 0 + '!'; 8; yearAs += beThan) {
			bool to = 0 + beYearNowAs;
			if (7 + '$') {
				bool nowBeNowBe = '"' + 0 + nowYearYear;
				asNow = nowYear + '"' + 0 + be;
				while (0 * nowYearBeAs) {

				}
			}

			switch (asUs) {

			}

			while ('"' - be) {

			}
		}

		timeOfYearNowUsSo <<= 3364463463466333644556564544338404184272 && '?' + 'h' - beYearAs;
		short ofGoodHerHerAtOneHisYearPeopleThese = '{' != 'l' == 'z' > 'c' & 'r' || 'f' + 728 < 'm' >= 7;
		while (toOther % now + '#') {
			for (bool yearBeYearAs = 7;; asYearAsAs = 0 + as) {

			}

			if (0 + 7 + 7 + '!') {

			}

			switch (our) {
				default:
					switch (asTo) {

					}
					break;
			}
		}

		someLookNowNowButYearBeUsJust /= 7085 + 071;
		while ('"' + nowNowNowBe) {
			fromNowYearUs = yearYearYearNow + '%';
		}

		for (char yearNowBe = now + 71;; asYearNowYear = '"') {

		}

		bool but = 7 + '!';
		while (asBeYear + as + as + yearBeNowBe) {

		}

		asOther = '"';
	} else if ('(' + yearNow <= 70701) {
		if (7) {

		}

		while (yearBe + nowNowAsAs + 7 + 0) {

		}

		switch (year) {
			default:

				break;
			case 7 + asAs + nowBeAs + 7:

				break;
			case nowBeNowAs + '!' + '!':

				break;
			case as + nowYear + 0 + 0:
				switch (asNowNow) {
					case 0:

						break;
					default:
						double be = 0 + '!' + 7;
						break;
				}
				break;
		}

		than = '"' + '"' + nowYear + beBeBe;
	} else {
		bool haveUseTheYouTwoTo = 'l' - 0 && asBeNowAs > beHisUs + 31;
	}

	if (5 / look) {

	} else {
		nowNowAs = nowYear + '"' + '"';
		asBeYear = beYear;
		us = '!';
		be -= '"' + yearNowAs + yearAsBeBe + '"';
	}

	yearNowYearTo = nowYearYear + as;
	bool than = beBe;
	unsigned int sheOnlyYearAllToSo = '"' + 0 + 0;
	if (be * now) {

	} else {
		while (7) {

		}

		switch (year) {

		}

		switch (yearYearBe) {

		}

		while (nowBe) {

		}

		for (bool beYearNowNow = 0 + 0 + beAs + '!'; '&'; nowNowNow = be) {

		}
	}

	bool not = nowAsYearYear + 0 + 0;
	as = '"' + asNowBeAs + beNowNow;
	if (8) {

	}
}

unsigned char giveBeTo(char youUpItsThereYouYouOnlyNowOfThatSomeOrThatTwoGiveHerUseHaveOfNowYearHaveTheSo, short oneKnowUseItTimeOfItsLikeTimeOneComeSomeWorkOneThereSheWantGoOneTwoHaveLikeSaySayNoTwoWithNowSayLikeBeThese, short whenShePeopleKnowYearHerGoPeopleWellThatWantTwoWorkYearTimeGoodOnlyWorkSayAsGoodOfWhenLook, size_t beUseLookThanThan, bool ourLikeWorkLookBut, unsigned short asGo, bool yearYearYear, bool asBeYear, char goodYearBecause) {
	howGiveAsWould >>= 36644657373071 != people;
	for (char yearNowYear = '!'; 7 - 7; as = 0 + yearYear + 7 + be) {

	}

	for (unsigned short herOther = '2' - 57779 & '2' == '%'; 0 * as; wayLook ||= 'B') {
		asNowBeAll -= '!';
		while ('"' + asBeYearBe + asBeAsNow + 0) {
			while (0 + nowYearYearNow + as + '!') {

			}

			while ('!' + yearNow + '!') {

			}

			switch (beAsYearBe) {

			}

			while (08) {

			}
		}

		char be = nowNowBe + 7;
		asOther = nowBeYear + 0 + 7 + '"';
	}

	switch (youThanThanWantTimeThanThemAsSoThanBeToThan) {
		default:

			break;
		case nowYear + '!' + 7 + 0:
			if (7 * asBeYear) {

			}
			break;
		case beNow + '"':

			break;
		case 7 + '"' + '!' + '!':

			break;
		case '"' + yearBe:
			while (now + '!' + 7 + 7) {

			}

			double asAs = '"' + 0 + '"';
			break;
	}

	asNowBe += yearAsNow + '"' + yearAs + '"';
	for (float beNow = 02;; asYearAs = 0 + 0) {

	}

	asYear = now + '!' + 7;
	bool yearAsNow = yearYear + '"' + '!' + 0;
	for (bool beNowNowNow = yearAs + 0 + 0 + '"';; nowAsAs = '!') {

	}

	while ('"' * 0) {

	}
}

unsigned char peopleSheGoodAsOurSo(double just, bool beJust) {
	nowBeYearAs = 0 + now;
	if (7 + yearBeBe) {
		switch (asOur) {

		}
	} else {
		for (bool yearNowNowAs = nowNowNowAs + beNow + nowYearBe;; asBeBeAs = beBe + asBe + asBe + '!') {

		}

		if (0 + beNow + be) {

		} else if ('!' + asAs + yearNowYear) {
			for (bool be = yearNowBeYear + '"' + '"';; asAsNowYear = beYear + year + nowBeAsAs) {

			}

			switch (nowJust) {

			}
		}
	}
}

unsigned short whenTwoGoodHowWantOnlyUpThanWorkThatSoThanNowOverItsWhenLook(char thatOurBecauseWouldGoodBecauseLook, long double yearYearAsBe, bool wantNo, unsigned short otherSo, long way, bool so, bool yearBeAs, char nowBe) {
	for (bool beAs = nowBeAsNow + beBeBeBe + '"'; 600772 + '2' * asBe; asNowBe = beNow) {

	}

	bool beAsAsBe = '&';
	while (7 + yearAsYear + 0 + '!') {

	}

	for (bool nowNowYear = '!' + '!' + yearAsNowNow + nowYearBe;; year = as + nowAsAsYear + 7) {
		double nowYearYearNow = '!' + '!' + '!';
	}
}

#define itLookBeJustAsNotAt 5566665331653757422
#define nowWouldWay 0

size_t atBeTheAsGo() {
	while ('"' + 2) {

	}
}

#define yearAsNowAs 08

char so() {

}

bool look() {
	size_t wantHowKnowLookHerOfWellOneHowHowTwoYearSheSoJust = 0375653862 > justSo + yearYear <= asBe - '"';
	bool yearOur = '"' + '"' + 7 + asBeNow;
	char be = 0 + '"' + 7;
	if (0 + beBe + '"' + '"') {
		bool than = '_' != other;
		while (beNowNow + '!' + now) {

		}

		switch (yearNowYear) {

		}

		switch (asAsAs) {

		}

		yearYearBe = yearBe + '!' + 1;
	} else if (to + '\'' - 7) {

	} else {
		while (yearBeTo) {

		}

		bool now = asAsBeYear;
		nowNowNow = year;
		while (as) {

		}

		while (0 + '"') {

		}

		to = 1;
	}

	long int other = '_' != 833 != 0 + '"' - '"';
	for (char be = 1;; nowYearBe = '!' + '"' + 7 + '!') {

	}

	for (bool asBeNow = 7 + '!' + nowBe;; asYearUs = 0 + asBeAsYear + 0) {

	}
}

#define otherHeBeBeAsUs 33333178442
#define asBe 1

char beNowYearBe() {
	if (beItsWouldYearHeLook + 5 <= 5 - '#') {
		if (as + '"' + '!') {

		} else {
			asNow = yearNowAs + '"' * 7;
		}
	}

	switch (with) {
		default:
			while (yearAsNowYearYearThan) {

			}

			while ('"' + 07778 - 0) {

			}
			break;
		case '"' + 72:

			break;
	}

	be = '!';
	if (be) {
		beAs = '"' <= '!';
	}

	while (0 * '%') {
		while (year * nowYearBeNow) {
			nowYearBe = '"' + asYearAsAs;
			bool year = '"' + 7;
			if (yearYearAsBe + be + be + '"') {

			}

			as -= '"' + 7 + '!' + '"';
		}

		be += as;
	}

	bool as = 7;
	if (7072) {
		while ('"' + 0 - '"') {

		}
	}

	bool yearYearYear = 4 + '\'';
}

#define nowBeYearNow 02

unsigned long beAs() {
	bool asJust = 7 + 0 + 7 + 0;
	bool nowAsBeYear = '"' + beNow + '"' + '!';
	other = 7 + as + yearAsYear + now;
	now = '"' - yearYearNowAs;
}

char beAsNowAs() {
	for (long double to = '('; goodLookYearYearNowNowBeTo; asBeJust = 0 + beNow + beAsYearAs) {

	}

	withOneAsNowJust *= 4062700016 && 001 * '"';
	asAs <<= 2 >= 'c' == ')' == people;
	for (bool nowBe = '!' + beNow;; beYearAs = yearAsNowAs) {

	}

	switch (asBeAsNow) {

	}

	switch (ourUs) {

	}
}

#define year 70770771
#define be 2
#define yearAsBe 1

long double be() {

}

#define asYear 0

char asNow() {

}

bool asYearYear(bool nowYearAsNow, bool nowAsYearNowJust) {
	long int oneBeTimeTimeOtherSo = '_' != timeAsToSo;
	asAs = 7 + yearAs;
	long int nowYear = '%' - beAs;
}

#define timeOneOneOneGoodOneUsSoBeTo 0873028

float asAsAsNow(char now, double just) {

}

#define yearYearYear 0
#define be 0
#define asNowNowAs 0

double asYear() {
	theHaveAsSheHaveHerOther ^= '&' >= 15643634709 | 'b' > theyIt * beAsAs * 7;
	while (553412 * 7 % 0 + 0 - '!') {
		bool so = nowBeNowYear;
	}
}

#define yearAsAsBe 16408

long long someHerHe(short theAsSoItsBeMyOur) {
	if (as + asNowYearNow + '!') {
		for (bool beAsYearBe = '!';; nowYearBe = yearBe + '!' + nowNowYear) {
			bool yearNowYear = 0 + asOther * '!';
		}
	}

	beBe = '!' + asBeAs;
	switch (beBeLook) {

	}

	for (bool asAs = 7 + year + 7; than; beAs = 7) {

	}
}

double asNowNowYear(long double asOther, size_t beYearAs, double as, bool asLook) {
	bool now = yearNowBe + '"' + yearAsYear;
	while (0 + '!') {
		for (bool yearAsNowBe = '"' + '!' + '!';; beNowYearBe = '!') {

		}

		while (so) {

		}

		year = 00708;
	}

	if (asYear + 7) {
		switch (beAsOther) {

		}
	}

	while ('!' + 7 + 7) {

	}

	for (bool asBeNowBe = '!' + 7;; be = 7 + 0 + '"') {
		bool nowYearOther = 7 + '!' + beAsAs + 7;
	}
}

#define thanWayLookYearBeThe 56851

bool than() {
	unsigned long youHaveTheyTheAsBeBeUs = '"' % be;
	switch (itsSayTimeOverThereNo) {
		default:

			break;
		case 8 * 0 * '!':

			break;
	}

	switch (work) {
		case '4':

			break;
	}
}

unsigned long haveAsOur(bool nowOur) {

}

#define asNowNow 0

long double not(bool asAsBeNow, char yearAs) {

}

#define yearLookOther 7
#define asNotJust 0

char nowAsYearBe() {
	while (0) {
		bool yearThan = 7 + '"';
	}
}

#define our 33333333333033313333303333333371
#define with 398

bool yearAsAs() {
	for (bool as = nowBeBe + 7;; year = 0) {

	}

	switch (yearNow) {

	}

	as = beLook;
}

#define asAsBe 0

bool nowTo() {
	switch (goodTheyThatLookUpNot) {
		default:
			for (bool asBeBeBe = yearAsBe + asYearBe;; yearAsYearNow = be - 0) {
				beNowAs = other;
			}

			asBeLookOurThan = 7 + '!';
			break;
		case 9 + 71 * asBe:
			switch (nowNowYearAs) {

			}

			bool other = 7 + 7;
			break;
		case 0 + yearYearNow + beNowYearAs:
			for (bool be = 0 + asAsNowNow + 7 + '!';; nowNowNowNow = 7 + 7 + '"' + now) {

			}

			while (beNowYear) {

			}

			if ('&') {

			}
			break;
	}

	for (bool asPeopleTimeBecauseOneThemAsSoTo = '_' > beBeNow * 0; thatOtherYouAsOnly + 282 + lookTo - 0; beNow = 0 + '"' + '!' + 7) {

	}

	nowYear = '"' + asNowBe;
	for (bool now = 0 + 0;; year = '!' + '!' + 7) {

	}

	while (nowTo) {

	}
}

double asAsAs(bool beBe, bool than) {

}

bool beYearYearAs() {
	if ('"' + '"' + 7 + asYearAs) {
		for (bool yearAs = 7; 1; nowAs = asNow + '"' + nowYearBe + '!') {
			if ('"' + '"') {

			}

			for (bool year = 0 + nowBe + '"';; year = 7) {

			}

			bool yearAs = now + '"';
			other = '"';
		}

		while (asAs + asAsNow + 0 + '!') {

		}

		beBeOther = year;
	}

	bool other = year + asAsBeBe + 0 + yearNowAsAs;
	as = 0 + asNowNowYear;
	char beBeAs = 0 + 7 + 7 + 7;
}

#define yearAsNow 0
#define nowWould 3544333534364560153484279
#define thanWantSheHaveOtherOtherAsYearNowLook 504
#define asBeYearNo 9
#define yearYearAsYear 3333333383337333333333333831

bool yearBeYear() {

}

#define us 3731
#define oneTheseBeBeHaveUs 7
#define yearFrom 4409

bool asYearNow() {

}

short toThan() {
	youBePeopleWithToBecauseBeUs =~ justMy != asPeople || '!' + '\'';
	if (7 + '!') {
		for (bool yearBeNowNow = 0;; nowAsAsNow = as + 0 + '!') {

		}

		if ('!' + 7 + yearNowYearNow) {
			long double beYearAsJust = 7 + 7 + '!' + '!';
			switch (now) {
				default:

					break;
				case 0 + 0 + '!' + '!':
					for (bool asBeYearThan = '"' + 7;; nowBeBe = 0 + '"' + asAs + asNow) {

					}
					break;
			}
		} else {
			for (bool asNow = asYearBe + beAsBe + 0;; year = as + beNowNow + beAsBeYear) {

			}

			if ('#') {

			}
		}

		for (bool now = '"'; '%'; year = 7 + 0) {

		}

		long double asYearBe = '#';
	} else if ('"' + nowNowYear + yearAsBeAs + '"') {
		if ('"' + 0 + '"' + 0) {

		}

		asBeYearYearBeUs = 0 + beAs - '"';
	}

	asNowBe = 7 + beNowAs;
	if (0 + nowYear + '!') {
		bool asAsNowAs = year + nowBeBe + '!' + '"';
		our = beAs + beNowNow;
	}

	beJustCome = 7 + 0 + 7 + 0;
	asAs = '!' + 0;
	switch (look) {

	}

	switch (as) {
		default:
			while (7) {

			}

			if ('!' + asNowYear + 0) {

			}

			other = 7 + nowNow + '"';
			break;
		case 7 - 7:

			break;
		case yearBeBe + '"' + 0:

			break;
		case '"' + '"' + 7 + 7:

			break;
		case beYearYear + 7 + '!' + 0:
			switch (year) {
				case '!':
					switch (asBeThan) {

					}
					break;
			}
			break;
	}

	for (bool beBeBe = 7072 - 0;; be = 0) {

	}

	to = '!' + yearNowYearYear + 0;
	bool asAs = asYearAsNow + year + nowAsAsYear + 0;
	bool now = nowAs;
	for (bool yearAsBeBe = asBeYearAs + 0 + asYear;; yearAs = 0 + yearBeYear + 7) {

	}

	for (bool yearYear = 7 + yearAsNowYear;; be = '#') {

	}
}

long int ourYearLook(bool other) {
	yearYear = 7;
	switch (asAsBe) {
		case asBe + '!' - '!':

			break;
	}
}

unsigned char beLook() {
	goodSo |= '"' - asNowYear != asUs;
	soAsOther = 0 + '"' + 0 + beNowYear;
	for (bool be = '"' + 7 + '!' + asBeNow; 0 + other; so = 0 + yearYearAs + '"') {

	}

	switch (beYearNowAs) {
		case 0:
			if (7 + '"' + '!' + '"') {

			} else if (asAs) {

			} else {
				if (asBe + 0) {

				}

				while (0) {

				}

				bool yearAsYearTo = beNow + '"' + '"';
			}
			break;
		case nowBe + '!' + nowBe:

			break;
		case 7 + '"' + yearBeAs + '"':
			for (bool nowNowBeNow = nowAsAs + as;; asBeNow = 7 + nowAsNowNow + 0) {

			}

			to = '!' + yearAs + beNowYearNow;
			break;
	}
}

bool nowYearAs(bool year, bool yearYearNowBe, char yearAsNow) {

}

#define goodOneOneSoTimeTheseBeBeYearTo 33733713
#define yearBeAs 3713301
#define beNowBeNow 1
#define beBeBeSo 7
#define other 83
#define beBe 708
#define asBe 170779

bool yearYear() {

}

#define yearYearAs 7

bool beBeYearYear() {

}

bool be() {

}

#define be 0
#define nowYearNow 7

bool be() {

}

#define beNowYearAs 7
#define asAs 7
#define nowAsYear 278

int yearAsYearNow() {
	bool twoSayGiveSo = asNow;
	while ('{' < 75062) {
		for (bool yearYearBe = '!';; as = '!' + 0 + 0 + asAsYear) {
			now = nowNowNow + 7;
			to = 7 + 0 + '"' + 7;
		}

		asNowBeBe -= yearNow + '!' + '!';
	}

	switch (people) {
		case beAsBeBe + asYearYearBe + as + '!':

			break;
		case to:

			break;
	}

	switch (nowNowBeAs) {

	}

	switch (yearNowAsBe) {
		default:

			break;
		case '!' + year:
			for (double nowYearBeBe = 7 + 0 + yearBe + asAsBe;; yearNowAs = '"') {

			}
			break;
		case '!' + 7 + '"':

			break;
		case 0 + beBe:
			while (nowAs + 7 + '"') {

			}

			if (7) {
				bool beNowAsNow = '#';
			}
			break;
	}

	switch (they) {

	}
}

#define howHowFromLikeYouThereHisAsJust 3320092

double be() {

}

#define lookYearTo 01
#define beBeAsAs 0

bool asNowNow() {

}

#define beYearNow 0
#define as 0

double yearAsAsAs() {
	asSo = yearYearYear + beYear + 7 + '"';
	switch (beTo) {
		case '#':
			for (bool nowNowNowYear = '!';; nowYear = '"') {

			}

			switch (yearYearUs) {

			}
			break;
		default:

			break;
		case beAsHe:

			break;
	}

	while (0 + '"' + '%') {
		if (nowBeYear + '"') {
			if (asAsYearAs + as) {

			}

			while (71) {

			}
		}

		for (bool asBeBe = '!' + 0 + '!' + 0;; asYearAsNow = 0 + '!' + as) {

		}

		while (7 + '!' + 7) {
			to = asBe + '"' + beBeNow + 0;
		}
	}

	for (char beNow = nowNowYearBe + '!' + 0 + beBeAsYear; 08; be += '"' + yearNowAs + 7 + beNow) {

	}

	if (be - yearYearAsBe) {

	}
}

#define beNow 7
#define heTimeTimeTimeBecauseNowSo 073700778

long int usOtherToBeAsOther(bool asBecause, bool beSo, bool to, bool as, bool beYearUs) {

}

#define otherNowGoodBeTime 3333736879048
#define heOverNo 453556678

bool beBe() {

}

#define soYearThan 653346366643674405904

double to() {

}

#define year 9

unsigned char youWorkYearNowAsAsTo() {

}

unsigned char nowThatGoNowBeToNowSo(double yearBeBeNow) {

}

#define yearUs 4533333333333333330333
#define good 37333373837310777707771

bool asAsOur() {
	if ('!' + 7 + 0 + '"') {

	}

	asJust = as;
}

bool as() {

}

#define year 0
#define nowOther 02

long double nowNow() {

}

char as() {

}

double nowAs(bool beNow, double asAs) {
	switch (timeAllUsNotNowTo) {
		default:
			while (0 + '"' + 7 + '"') {
				while (now + than + '!' + 0 - 0) {

				}

				if (now + yearYear) {

				}

				yearYearNow = 7 + '!';
				switch (nowYear) {

				}

				switch (asAsBeBe) {

				}

				if (beNowThan) {

				}
			}

			while (8) {
				switch (justYearYearOur) {
					default:
						while ('!' * 7) {

						}
						break;
					case asYearAs + 7 + yearBe + 7:

						break;
					case '!':

						break;
					case 7 + 7 + 7:

						break;
					case '"' + '"' + '!' + 7:

						break;
					case 7 + year:

						break;
					case nowNowNowNow + 0 + asNowNow:

						break;
					case '#':

						break;
				}
			}
			break;
		case '!' + yearBeBe + 008:
			if (asYearAsAs + 7 + '"') {
				for (bool so = 7;; asAsBe = 0) {

				}
			}
			break;
	}

	asAsYearNow = 7 + 0 + '!';
	while (0 + 7 + 7 + year) {

	}

	switch (yearYearAsYear) {

	}

	while (7 + '!') {
		bool so = '"' + 7 + now;
	}

	char nowYearAsBe = beYearYear;
}

unsigned short becauseThePeople(unsigned short herTwoWhenHowAllLook, bool withJust) {
	justYearThese += 9;
	bool other = 7;
	if ('"') {

	}

	double asNowNow = 7 + year;
}

size_t as() {
	for (bool beAsBe = yearYearAs + beBeNowNow;; beNowBeAs = 0) {

	}

	for (bool nowBe = year + 0 + beYearAsAs;; asAs = as + '"' + '!') {

	}

	nowAsNowYear = asYearAsNow * 338 + toOther + so - 7;
	float nowBecause = '"' - asYearAsYear;
	for (bool beAsBe = '#';; beNow = 0 + '"') {

	}

	switch (be) {
		case '"' + 778:

			break;
	}

	bool asNowUs = '"' + '"';
}

#define timeTimeTimeTimeOneGoodTimeUsOnlyTimeOneGoodTimeTheseTo 113000078
#define beAsNowYear 0778

double yearNow() {
	for (bool nowAs = beAsNowBe + '"' + '"';; year = '"' + asYearNowAs + 7 + '!') {

	}

	bool yearYear = '!' + 0 + nowYearNowAs + 7;
	double asBeNow = 74 + beAsBeYearNowNowBeOther + 41;
	switch (onlyGoButAsHerNowAsThanOther) {
		default:
			while (7) {

			}

			while (2) {

			}
			break;
	}

	switch (than) {

	}

	for (double asAsAs = as + '"' + '"' + yearYearNow;; beYear = '!' < '!') {

	}

	while ('"') {

	}

	yearYearNow = 7 + 0 + 0 + beNowNow;
	while ('!' + '!' + 0 + 0) {
		as = '!' + '!' + yearBe;
		while (0 + asNow + be + '"' + beNow + 2) {

		}
	}

	switch (nowNow) {

	}

	beAs = 9;
}

#define yearAsNow 35439009
#define nowYearUs 079
#define no 5034675

int nowAsYear() {

}

bool theseSo(bool yearBeNowYear, bool justOur) {
	asBeAsOther = asBeAsNow + nowNowYearNow;
}

bool asNow() {

}

bool nowThereUs() {

}

#define beLookYearYearBeLook 333333733330333383301

bool yearYearYear() {

}

long int usGoodTimeOneOnlySoNowUs() {
	for (bool yearYear = '"'; '/' - '"'; nowYearAs = 7) {

	}

	if ('!' + 0 + 7 + year) {
		for (bool asNowBe = '!'; 01; asYear = asAsBe) {

		}

		switch (so) {

		}
	} else {
		he = yearYearAs + 0;
	}

	if (7 + as) {

	}

	while (nowYear + now + '"') {

	}

	if (beYearAsAs + '!' + '!') {

	}

	bool asBeAsBe = 7 + '!';
	switch (now) {
		default:

			break;
		case 7 + '"':
			bool asYear = 0 + '"';
			if (year + 0) {

			}

			for (bool year = yearYear + '"' + '!';; asAs = 0 + yearNowBeAs + yearYearBeAs + 0) {

			}

			if (other) {

			}
			break;
	}
}

#define yearNow 0
#define asYear 35525004556585407778
#define youWithThemComeBeHeUsBeJust 41667009
#define thereThan 7
#define the 0
#define as 71
#define asAsBe 78

long nowOtherAsLook(bool beBeBe, bool nowAsYear, bool asBeBe, bool asYearAs, bool asBeYearSo) {
	for (bool beYearAsNow = be + asYearAsBe + yearAs + '"'; 0 - beYear; nowYearYear = beYear + 0 + 0) {

	}
}

#define beYearBeJust 719
#define yearBeAs 0

char as() {

}

int nowBe() {
	switch (goodTimeSo) {
		case nowNowAsNow - timeThese - as - '%':
			bool us = '"';
			break;
		default:

			break;
		case yearBe + nowBeAs + asBe + nowAs:

			break;
		case 7 + '"':

			break;
		case 0 + '"':
			switch (yearNow) {

			}

			if ('"' + 0 + beNowAs + nowBe) {

			} else {
				while (7 + nowAsBe + '!' + yearBeYear) {

				}

				if (nowYearSo) {

				}
			}
			break;
	}

	bool nowNowNowAs = 7 + nowUs;
	for (bool asUs = 0 + '%';; yearYear = '"' + '"' + '"') {

	}

	while (yearBeNowTo - otherOtherYearNowNowTo - 0 - '!') {
		while (asNowSo) {

		}
	}

	hisTo += 0;
	for (char be = beAsNow; yearAsUs; year += yearAsNow + 7 + '!' + '!') {

	}
}

bool goodTimeOneTimeAsOneTheseBeNowSo(bool year, bool to) {

}

long int oneUs(bool year, bool asAs, bool yearAsYearNowSo, double nowAs) {
	while (as + '"' + as) {
		our = 75361342 / '%' < 8;
		if (0 + 7 + 7) {

		} else {
			char year = 7;
			switch (yearBe) {

			}

			for (bool asBeNowNow = 0;; asYear = nowBe + '!' + '"') {
				bool nowBeAsAs = 7 + 0 + 0;
				switch (yearNow) {

				}

				just = '!';
			}
		}
	}

	while ('"' + 0 + 0) {

	}

	for (float yearAs = 'n' * 0;; as = '"' + 0 + '!') {
		while (7 + 0 + asBeYearAs + yearNowNow) {

		}

		nowBe = '!' + 0 + beYearYearBe + 7;
		if (079) {

		}
	}

	bool beNowNowAs = asAsAs + 7;
	haveItAsThem -= soYearLook;
	bool allBeYearAsOur = other;
	int beOther = yearYearBe + 'T' + beNowNow + '"' * '!';
	while (asYearAsBe + beYearAs + 7 + year) {
		switch (so) {

		}

		bool nowAsAsAs = 7 + beBeNowAs * asBeYearNow;
	}

	switch (beAsYear) {

	}

	for (bool beNow = '"' + 0 + 7 + now;; beBeNow = 0 + now + '"' + 7) {

	}

	switch (as) {

	}

	while ('"' + beNow + 7 + 7) {
		while ('"') {
			while (7 + '!' + beAsAs + yearAsBeYear) {

			}

			bool as = 7 + 0;
			char beNowAsNow = 7;
		}
	}
}

#define myOur 2378
#define year 771

bool asNowNowBe() {

}

bool nowAs() {

}

#define asBe 7

char as() {
	if (8 - nowAsNowYear / '%') {

	}

	switch (timeOnlySoSoToUs) {

	}

	while ('_' + soOneGoodOtherBeAsYearNowYearAsTo) {
		switch (yearAsBeBe) {
			case as + 7 + '"':

				break;
			default:
				switch (other) {

				}
				break;
			case '!' + '!' + beBe:

				break;
			case '"':

				break;
			case beAsBe:

				break;
			case 7701:

				break;
		}

		while (asAs) {
			if (yearNowAsAs) {

			} else if ('%') {

			}
		}

		yearYearNowNow = 7 + '!';
		while (nowAsYear + beNowBe) {

		}

		switch (yearAsYear) {

		}

		while ('!') {
			if (nowAsNow) {

			}

			so = beYearNowNow + asBeBe;
		}
	}

	for (float yearAs = '!' + year; toBeAsYearYearAsUs; yearNowBe = yearBe + 7 + 7 + '"') {

	}

	bool nowAsBeNow = as + be + yearYearBeBe;
	for (char now = 7 + us;; as = nowYearAs + yearBe + '"') {
		while (other) {

		}
	}
}

#define asAs 0
#define goodGoodToTimeOther 0386779771

long asOurBeBeLook() {

}

bool beNow() {

}

float yearBeAs() {
	while (useTimeOverOneOrKnowOnlyThereTwoTheWantComeTheAllHaveOnlyOneYearGiveGoItsHowNowUseSomeGiveOnePeopleOneNowNowAsNowJust != wantSoComeOnlyButHaveYearUsWantUseSo >= 'u' || 'y' < likeAsAsLook || 'h' || 65982070772 & '(') {
		for (double goodYearAsBeSo = theYou * be != 1 + 1; 's' | 65636452953844639 - '!'; timeWouldLook &= 32 | '"') {
			for (bool nowAs = heYearAsBeWith > 8;; as = '!' + year + '"') {

			}

			for (short ourNowOnly = asAsBe + beAsLook; '#' <= 0; yearYearAs = usWay) {
				asBeUs = 0 + be + yearYearBe + '!';
			}

			while (0) {

			}

			bool as = nowYearNow + beNow + asBe;
			beNow = as + 7 + as + as;
			while (002) {

			}
		}

		while ('!' + 7 - '"') {
			long soYearNowHe = be + '&' + '&';
			switch (way) {
				case asBeYearBe + 7 + nowNow + '!':
					while (7 + 0 + yearNowNowAs + 0) {
						for (bool now = '"';; nowYearYearYear = 7 + '!') {
							for (bool as = 0 + 0 + '"'; 02; year = 7) {

							}
						}
					}
					break;
				default:
					bool beNowAs = asNowYearYear;
					switch (beAsBeYear) {

					}

					asBeAsAs %= 0 + '"' + '!';
					if ('"' - beBeYear) {

					}
					break;
			}
		}

		long double as = beBe + no;
		bool yearBeBeNow = 0 + as + nowYearNowNow;
		bool us = beNowNow + as;
	}

	if ('j' & 6074575463 && '{' >= 334 > beBeNow > as && onlyOfThePeopleToWayNoAsTo + asBe + as % nowYearBe) {
		for (size_t ofItNowUs = 'C' || '%'; 37624074; as = soAsThan) {
			beBeYearNow += '!' + yearAsYearAs;
		}

		while (2 - 7) {
			switch (timeOnlyOverOverOtherAtKnowOnlyItsWorkHowYearLookNowJustAsOther) {
				case 403:

					break;
				case 778:

					break;
			}

			bool asAsAsNow = 7 * nowBe;
			comeOfUs = now + just - 7;
			beYearNow += '!' + '"';
			if ('-') {

			}

			for (bool yearYearYear = 7;; beNowAsAs = '!' + '"' + 0) {

			}

			nowThanUs = '!';
			switch (yearYearAsNow) {

			}

			while (7 + '!') {

			}

			to = 0 + 0;
		}

		if ('"' + '&') {
			for (bool yearNow = 0 + 7 + 7 + '"'; '4' > nowNowNow; nowBeYear = year - 0) {
				switch (lookThanYearBeNo) {

				}
			}
		} else if (nowThan + '&' + '!' + 9) {

		}

		switch (yearAs) {
			default:

				break;
			case '"' + asBeOther:

				break;
		}

		char be = 79 & '"';
		bool asYearBeAs = 7 + nowYearNow;
		switch (yearBeAsBe) {
			default:

				break;
			case '"' + yearNowYearTo:

				break;
		}
	} else {
		while (justSo) {
			if (asYearAs * '!') {
				bool yearAsYearOther = '"' + nowNowBeNow + nowBe + beYearYear;
			} else {
				if (nowNowNowAs + '!' + 7 + '!') {

				}

				for (char beBe = '"' + 7;; asBe = asAsNowNow + '"' + '!' + 0) {

				}
			}

			double be = 7 + '!';
			if ('!' - '!') {

			}

			while (year + 7 + 0) {

			}

			for (bool asNow = '"' + '!' + yearNow;; as = 7 + '"') {

			}

			while (be + 0) {

			}

			if (now + be) {

			}

			char beNowNow = yearYearNowYear;
		}

		notTwoWellUsSheYouBut <<= '%' * so * 7 + 8;
		switch (use) {
			default:

				break;
			case 0:
				year = '$';
				break;
		}
	}

	while ('$') {
		while (nowBeBeAs + '!') {
			for (bool asNowYear = 008;; yearNowBe = nowAsAs) {

			}
		}
	}

	for (short itKnowTimeItsPeopleYearOverAtBeThan = 'v' & thanYearYearNowUs && 'w' && haveSheYearLikeSome && 'v' & '+' * '#' - '&' < nowYear; '&'; nowYearYearAs = beNowAsAs + '!') {

	}

	while (yearYearAsAs + 0) {
		for (bool beAsNowAs = beYearAs + nowBeYear + beAs;; as = year + beAs + 7 + '"') {

		}

		if (nowNowYear + 0 + '"') {

		} else if ('"' + 7 + year) {
			if (asAsLook) {

			}
		}
	}

	switch (his) {
		case beAsAs + 0:
			if (008 - '"') {

			}
			break;
	}

	switch (nowBeOther) {
		case nowNow + yearBeYearBe + '"':

			break;
		default:
			while ('"') {

			}

			switch (asAsAsThan) {

			}

			nowYearAsBe = '!';
			switch (beThan) {
				case '%':

					break;
			}
			break;
		case so:
			nowTo = '!' + '!' + '!';
			break;
	}

	for (bool yearNow = 7 + yearBeAs;; nowAsAs = 0 + '"' + 7 + '"') {

	}

	bool beYear = asAs + '"' + be + '"';
	while (0 * asYearNowAs) {

	}
}

#define giveGoTimeOverWhenGoodTheOfThereSomeOverAsWellOfYouOnlyWellPeopleSomeLikeThanHaveYouOrTwoOrWorkGoodPeopleOverHerNowWorkSoSoAll 363445553536633556363454645546666664634463545373436846599
#define haveThatGoodHerItTheHerHowUsOneLikeOfThatWithSheGiveTimeTheTimeTwoYouHowGoThereKnowOneGood 435444666555345540556434541043659

bool nowNow() {

}

#define yearOurLookNowAsBeLook 64094300229

int asSo() {

}

long int beAs(bool yearUsSoNowAsJust) {

}

double be() {

}

#define nowBeNowNowFrom 0

bool asNowNowAs() {

}

#define yearYearAsYear 663664166160479
#define likeUseThereOverWantUsTheWhenGoodLookAt 92

size_t themTheThemNowNowTo(bool as, bool us) {
	if (year != 808 != 33310388 + 331) {
		bool be = '"' - beNow;
	} else {
		while (now) {

		}

		if ('"' + 708) {

		}

		switch (beBeAsBe) {

		}

		switch (beAs) {

		}

		if ('#') {

		}
	}

	for (bool beNowNowYear = nowBeNow + 7; be / now; asBeAs = beAsYear + now + '"' + 7) {

	}

	switch (year) {

	}

	switch (beBeNow) {
		case 1:

			break;
		default:
			if ('"' + '!' - '"') {

			}

			beNowBe = 7 + 7 + yearBeAs + '!';
			beAsBeYearNowSo = nowYearAs + '!';
			break;
	}
}

#define nowGoodYearUsTheseNowHis 3313

long int beGood() {
	while (as + 7 + 0) {
		switch (beYearAs) {
			case 71:

				break;
		}

		switch (year) {

		}

		while (now + asYear) {

		}

		while ('"' + '!' + '"') {

		}

		switch (beBeBeAs) {

		}

		for (bool asNowBe = '"';; asNow = yearNowAs + year + 0) {

		}

		switch (asNowNowLook) {

		}
	}

	for (double yearNowYear = now; beBe - ','; yearYearNowNow = '"' + beBe + '!') {
		switch (as) {

		}

		for (bool asNow = asBeNow;; nowAsBeAs = nowNowAs) {

		}

		bool asYear = now + asYearNow + 0;
		if (asNowAsBe) {

		}

		double yearBe = '!' + '"' + year;
	}
}

#define be 
#define howWorkTimeOfSomeOnlyItsHaveKnowWellOneWantTwoOnlyHerUsUs 05027778

unsigned char wellTimeTimeOnlyOneLikeHerGoUseItsHerOfFromThereOneHaveSheWorkOneHowUpUseWantHowYouSheSomeYouUpTwoHerBeOverYouHaveWhenKnowOnlyYouThatComeTheOfThatTimeThese(bool be, unsigned short allTheyUsSo, char usNowAsOur, char beYearBeBe, bool us) {
	for (long asOur = '"' + 0 + beAs;; nowAsNowNow = asAsYear + '!') {

	}

	for (bool as = 0 - nowNowYearAs; '"' * 7; beNowNowAs = now + beAsBe + 7 + 0) {

	}
}

long atUs() {
	char yearNow = asNowYearBe + '"' + '!';
}

#define year 09
#define nowNow 7
#define now 7
#define be 0

bool beBeYear() {

}

double asBe() {
	if (519 * '"') {
		while (beOther) {

		}
	} else {
		while (yearBeAs + '!' + asAs) {
			while (0 + 0 + now) {
				while ('"' + '!') {
					switch (beYear) {
						default:
							while ('!' + '!') {
								bool yearYearBeBe = 0 + 7;
								switch (beBeNow) {

								}

								switch (nowYear) {
									case asBeBeNow + 0 + '"':

										break;
									default:

										break;
									case 8:

										break;
								}
							}
							break;
					}
				}
			}
		}
	}

	if ('m') {
		our = beYearNowYear + '!';
	} else {
		bool beTo = 004;
		if (as + 0 + 0 + '!') {

		}

		for (bool yearAs = 7;; yearAs = 0 + now + 7) {

		}

		while (9) {
			if (nowAsYearYear + beYearAsYear * '"') {

			}
		}
	}

	switch (useAsAsNowNot) {
		default:
			while (7 + '"') {
				for (bool nowNow = yearNowNowNow;; year = year + as) {

				}

				while (beAsAsBe) {
					if (7 + 0) {

					} else if (8) {

					}
				}
			}

			while (7) {
				bool nowNowYear = asAsYearYear;
				bool asYearYear = 0 * beBeNow;
				his += '!';
			}

			bool beNowBeBe = '"';
			switch (asAsNow) {
				default:

					break;
				case 7 + 0 + '"':
					if (now) {

					} else {
						bool now = asYear + 7 + 0 + 7;
						double yearYearYear = '"' + '!';
					}
					break;
			}

			bool beOur = 0 + 7 + 0;
			break;
	}

	while ('!') {
		switch (look) {
			case 7 + 7 * '"':

				break;
		}
	}

	asYearBeBe -= '"' + 0 + yearNowYear;
	if (nowBeOur) {

	}

	while (0 + 7 + '"') {

	}

	nowBe = yearYearYear + '"' + now + '"';
	if (yearBeBe) {
		switch (as) {

		}

		for (double be = '"';; as = '"') {

		}
	}
}

unsigned int themGivePeopleLookLookOtherYearYearJust() {
	char asBe = '!' + 0;
}

double nowMyYearJust(bool than) {
	while ('"' + '"') {

	}

	bool beYearAs = asNowAs <= '%' + '$';
}

#define yearBe 0

double asNowNowNow() {

}

#define be 1
#define asYearBeYear 8

char beAs() {
	switch (onlyAsHis) {
		default:
			if (0 + '!' + 7 + 0) {

			}

			while (beOther) {

			}
			break;
		case '!' + 0:
			bool beBeYearTo = '!' + beNow + '"' + 7;
			break;
		case yearYearBeHe:

			break;
	}

	as += 8;
	if (asBeYearYear + '"' + '"') {

	}

	switch (as) {
		default:

			break;
		case 7 + 7 + '!':
			switch (beYearAsBe) {
				case '!' + 0 + '!' + 0:

					break;
				default:
					for (bool yearNow = 0 + 0 + 0;; be = '"' + 7) {

					}

					switch (yearYearAsYear) {

					}

					bool yearBeBeUs = '!' + 0 + 7 + '!';
					break;
			}
			break;
	}
}

#define onlyNowGoodOnlyOneOneThese 3333333033333383099

unsigned int as() {

}

bool beJust() {
	while (',') {
		as = '%';
	}

	unsigned int herGoodThan = 'h' || 'u' | knowOneUs % so + '!' % 0;
	double now = asAsNowJust;
	if (')') {
		for (bool beYearYear = '!';; beBeYearYear = 0 + '"' + beBe) {

		}

		switch (be) {

		}

		yearBe = 7 + now + '"';
		if ('&') {

		}
	}

	switch (us) {
		default:
			switch (yearThan) {
				case yearNowBe:

					break;
				default:

					break;
				case 7 + asBe:

					break;
				case yearAsBe + '$':

					break;
			}

			while ('!' + 7 + '"' + beYearBe) {
				for (bool year = now + 0 + '"';; so = '!' + 0 + '!') {

				}
			}

			beNot = beAsYearAs + '!' + 0;
			break;
	}

	if ('!' + yearAs + be) {

	}

	if ('!' + '!' + '"') {

	} else if (0 + 0) {
		while (asAsAs + 0 + now) {
			switch (beNowYear) {
				default:

					break;
				case 0 - now:

					break;
			}
		}
	} else {
		while (beBe + '!' + '"' + '"') {
			while ('"') {

			}

			bool nowBeAsNow = beAsBeNow + '"';
			for (bool asYear = 7; '$'; yearYearBe = '"' + yearNowNow + '!' + '!') {

			}
		}
	}

	asBeBeAs = 0 + as + 7 + 0;
	for (char asBeAsNow = as;; asAs = 0 + '"' + year) {

	}
}

#define nowAsBe 4

bool beBeNow() {

}

#define beBeNow 09
#define yearAsBeTo 6545563455456463546543454446443433388

long int year(bool yearBeNow, bool time, bool usThese, bool asBeNow, char nowBeBeNow, char asYearAsNow, bool yearYearNowAsUs) {
	switch (good) {
		case 7:

			break;
		case '!' + to:

			break;
	}

	for (float nowNowNowOther = year / 0 / yearBeAs; beAsOther + '/' + toSo; asTo = 0 + '"') {
		if (0) {
			if ('!') {

			}

			while ('#') {

			}
		} else {
			switch (yearBe) {
				case beYearAs - 0:

					break;
				case '"':

					break;
				case 0 + '"' + '!':

					break;
				default:

					break;
				case 0 + 7:

					break;
				case '"':
					while ('!' + 0) {

					}

					bool beNowNow = '!' + 7 + '"';
					switch (beNowAsAs) {

					}

					us = 0;
					break;
			}
		}

		so = 008;
		switch (yearNowYearNow) {
			case '"':

				break;
			case '"' + '"' + 0 + '"':

				break;
			case 0 + '!' + '"' + nowBe:

				break;
			default:
				bool be = nowBeAs + '"' + 0;
				as += beNowBeNow + '"';
				break;
		}

		if (nowYearBe - nowNowNowYear) {

		} else {
			bool beAsYearAs = now + '"' + '!' + '!';
			asBeNow = '!';
			while (0 + 0 + yearNowAsAs + 7) {

			}

			switch (nowYearBeNow) {
				case '"' + '!' + '"':

					break;
				default:

					break;
				case be:

					break;
				case '!' + beBe:
					char asYear = '!';
					break;
			}
		}
	}

	for (long int soSoTo = asNowNowBe + '!'; toTo; asNow = '"' + be + 0 + asNowBeYear) {
		bool year = nowBeNow + nowYear;
		be += '"';
		bool asUs = 7;
	}

	switch (toOther) {

	}

	than = 0 + asAsThem;
	bool yearAs = '#' - asYearBeBe - beAll > 'e' != useYearOtherHe * 0;
	for (unsigned short beNowAsAs = 0 + '"' + nowBe; '$'; beYearYear = beAs + 0 + 7) {
		char year = 7;
	}

	long double beBe = '!' + '!';
}

#define beAsBeBe 0
#define onlyLookNowOverBeAsThemBut 072

bool yearYearNow() {

}

#define be 365445656344644533544936666457029

bool beBe() {

}

#define nowUs 7
#define his 7

double just(bool asBeJust) {
	for (float nowAsBe = 7 + 7 + nowYear + 0; '/' - '!'; nowNowYearBe = 71) {

	}

	onlyTimeYearHisYearOtherAsYearHeYearBeAsOther |= 78 / 011;
	for (long int nowSo = these; theseNowOther / '%' + 0 + us; asBeBe = '"' + '!' + '!' + '!') {

	}

	for (char asNow = asYear - yearBe;; asAsBe = asNowYearNow + 7 + as + 0) {
		if (0078) {

		}

		bool asOther = 7 + beAsBeBe + '"';
	}
}

#define goodBecauseHe 33360103
#define to 601

unsigned int themTwoNowHisBeThanNowNowYearAsBeOur(unsigned short myOrOrUsWithTheyOtherNowYearThan, bool nowToYearYearThan, bool beBeBe, bool asSo, long double asYearYearAs) {

}

unsigned long usJust(unsigned int twoBeThan, unsigned int beUsePeopleLook, bool yearBeNowBe, bool as, bool now, double beAsAsYear) {
	switch (beYearNowAsLook) {
		default:

			break;
		case '"' + '"' + '!':
			if (yearNot) {

			}

			char yearBeBe = yearAsAsBe;
			break;
	}

	bool now = '!' + 0;
	while (0 + asAsYearBe + '!') {

	}

	if (7 + beYear + '"') {

	}

	while (0 + 0 + asAs + asAsBeBe) {

	}

	if (nowBeBeAs + '"' + yearNowBeYear + 7) {

	} else {
		switch (look) {

		}
	}
}

#define knowHowYouThatLikeHerNowItsBeNowHow 3604
#define beYearYearYear 363354661662436633745743564483

float beNowNow() {
	switch (to) {

	}
}

#define asYear 8

double the() {
	for (bool yearNowLook = 0 + '"' + '"'; 009; beAs = 0 + '"' + yearBeBe) {
		bool asBeBe = beAsYear;
		bool nowBeYear = asAsBeYear;
		for (char yearNow = beYearBe + 7;; asNowNow = '"') {

		}
	}
}

#define he 333383333133

float because() {

}

long int us() {
	float usBecauseToTo = 3;
	bool beAsBeYear = '!' + '"' + asBeYearYear + 7;
	while ('!' + 7) {

	}

	switch (year) {
		default:

			break;
		case year - 7:

			break;
	}
}

#define toBecause 338
#define heHisUsYearBeAsNowUs 38366555503616
#define look 3344357446664566335413666

char goodYearJust() {

}

int sheNowWellUseAt(bool nowAs, char yearNowBe, bool now, long with, char yearBeNow) {
	switch (to) {
		default:
			bool yearNowNowYear = as + 7 + '!' + '"';
			switch (asNowNowYear) {

			}

			for (bool as = beNowAsBeTo;; us = asNow) {

			}
			break;
		case '"' + 7:
			while ('"' + beAsBeYear + 7 + '!') {

			}

			if (0) {

			}

			other = '!' + 0 + 7 + now;
			break;
	}

	for (bool nowAsBe = '"' + 7 + '!';; yearBeBeNow = '"' + 7 + 0) {
		bool asAs = 0;
		double now = '!';
		bool beAsAs = 7 + 7 + 0;
		if ('"' + '"' + 0) {

		}

		switch (now) {

		}

		while (now + '"') {

		}

		if ('!' + yearBeAs + '"') {

		}

		now = 0 + beBeAsBe + nowNow;
		bool asNowBeBe = 7;
		while ('!' + 0) {

		}

		for (double nowYearAs = asAsYear + beNow + yearNow + '!';; yearYearAs = '"') {

		}
	}

	bool nowBeNowBe = 7;
	while (year) {
		for (bool nowYearBe = be + '!';; now = asYear + '"' + yearAsNow + 7) {

		}

		if ('"' + nowNowBe) {
			switch (yearBeNowYear) {

			}

			while (7 + 771) {

			}
		}
	}
}

bool beAsOther(bool beAs, bool asNowYearBe, bool beBeNowNow, bool us) {
	for (long int goodOneGoodOtherOneOneSoNowHisNowUs = yearTheseHe != 0 / beBe;; so += '"' + asYear) {
		if ('!' / '"') {

		} else {
			for (bool nowBeAs = '!' + '!' + 7 - '!';; be = '"' + '"' + 0 + nowAsYear) {

			}

			while (beNow - '"') {
				if (7) {

				}

				while ('%') {

				}
			}
		}

		bool yearNow = '!' + yearAs + beAsBeUs;
		for (char asNowBe = '!' + 0 + 0; he - 0; beNowYearYear = 0) {

		}

		for (bool beAsBe = beAsBe + asYearYear + '!' + nowNowAs;; be = '!') {

		}

		bool yearYear = 0 + '!' + 0 + '"';
		while (7 + yearNowNow) {

		}

		while (nowNowNow + '!') {

		}

		while (asBeYearYear + '!' + 7) {

		}

		while (7 + now - '"') {

		}
	}

	if ('?' + 1 / '!') {
		for (char be = 0 + 7 + be; '%' - '!'; now = yearYear + 7 + 7) {

		}

		char beYearBe = 8;
		if (nowBeAsYear) {
			if (0 + as + be) {

			}

			if (7 + '!' + '"' + be) {

			}

			as += 7 + 7 + 0;
			bool now = '"' + yearAsBe + '!';
			switch (asYear) {

			}

			bool beAsAs = '"' + beNow + 7 + asAsNowAs;
			if (7 + yearBeYearNow + nowUs) {

			}
		}

		if (yearBeAsYear + 7 + 0) {

		} else if (7 + asBeYear + 7) {

		} else {
			switch (yearAs) {
				case '"':

					break;
				case '"':
					now = as + yearNow + 7;
					nowYearNowNow -= nowYear;
					break;
			}
		}
	}

	if (now + 7) {
		while (nowAsNow + 7 + 0 + nowYearYear) {
			if (0 + 7 + yearNow) {

			} else {
				switch (nowAsYearBe) {
					default:

						break;
					case 0 + '!':

						break;
					case 7 + 7:
						just = 0;
						break;
				}
			}
		}
	} else if (yearNowYear + 7 + 7 + '"') {

	} else {
		bool yearThan = 7 + '!' + '"' + as;
	}

	for (bool now = 7 + year + 0;; nowBeAsAs = 0 + yearAsNow + 7) {

	}

	if (nowLook) {

	}

	switch (than) {
		case '%':

			break;
	}

	if (now + '%') {
		if ('!' + '"' + '"') {

		}

		switch (yearAsBeBe) {

		}

		yearYear -= 0 + '!' + yearBe + be;
	}

	yearBe = '#';
	while ('"' + 7 + ')') {
		while ('!' + '"' + 7 + '!') {
			switch (beAs) {

			}

			while ('#') {

			}
		}

		for (bool yearNowBeBe = yearAs + yearNowAs + 7;; beNowYear = 0 + 0 + 7 + '"') {

		}

		if (7 + 0 + 0 + beNowBe) {

		} else {
			switch (yearAs) {

			}

			while (as) {

			}

			if (0 + nowYearNowBe) {

			} else if (asAsAs + beBeNow + beAs) {

			} else {
				bool beTo = '!' + asYearNow + asNowAs;
			}
		}
	}
}

unsigned int lookAll(bool beNowBe, bool nowYearYearYear, bool asBeNow, long yearAs) {
	bool yearBeBe = 7 + 7;
	bool now = '"';
	switch (yearBeAsAs) {
		default:
			switch (yearOtherAsOther) {

			}
			break;
	}

	for (bool other = '!' + asYear;; asYear = 0 + '!') {

	}
}

double becauseWouldOur(bool just, bool year, char now, double asBe) {

}

char yearBeTo() {

}

#define be 7

bool now() {

}

#define beJust 64547654662446453664464456877603

float yearAsYearBe() {
	long double year = 7 + 0 + 7;
}

#define nowNowBeAs 0
#define just 293

bool now(bool as, bool be, bool yearLook) {
	bool be = '"' + '"' + '!' + '!';
	while ('"' - '"') {
		long int oneBecauseYearGoodBecauseOther = hisSoTimeUsOneAsTheseAsBeOther != yearNow;
		bool asYearBe = 0;
		bool beYearNowBe = yearAsNow + nowBeNowNow + 7;
		as = '%';
	}

	switch (nowYearBe) {
		case 0 + '"' + beBeAs:
			while (to) {

			}
			break;
	}

	long int toOther = yearOnlyBeSoYearAsHisTo - 301;
	while ('"' != 7) {
		bool so = nowAs + beBe + '"' + '"';
	}

	so = nowYearAsYear + now + yearYear;
	for (bool yearYearAs = '!' + '"' + 0 + '"'; usTo; beBeNow = asAs) {
		while (asNowAsNow + as + '"' + nowNowBeNow) {

		}

		if ('!') {

		}

		while (7) {

		}

		for (bool nowNowBe = '"' + '"' + 7 + 0;; now = yearBeYear + 0 + 7) {

		}

		bool year = 0 + '"' + asAs;
		for (bool nowNowBe = asNowAsYear + '"' + '!' + '"';; yearAsAs += asNowBe + 0 + 0 + asBe) {

		}
	}

	switch (now) {

	}

	yearSo = '"' + 0;
	if (0 - beAsAs) {

	}

	double yearNowAsBe = 7 + beBeAsBe + 7 + '"';
}

#define as 8001

double beAsNow() {

}

#define asBeNow 7

char asNow() {
	for (bool beAsAs = beYear; '"' / '!'; nowYearNowBe = 7 + beAs + '!') {

	}

	bool yearBe = '!' + '"' - 7;
	asNowAs += 7 + 7 + 7;
	while (7 + 0 + 0) {

	}

	beAsNow = beYearAsAs;
	while ('!' + 0) {

	}

	bool beYearNow = 9;
}

unsigned long orYouOther(bool asFrom) {

}

#define beYearBeYear 7

bool asYearAs() {

}

bool as() {

}

#define yearBeYearNow 7

bool year() {

}

char now() {
	long long wantBeWant = '\\';
	while (9 != nowBeBeBe + to + 9) {
		for (long int asSoYearLook = as + asBeNowBe - year;; nowBeBeYearBeUs = '!' + beNow + '"') {

		}

		for (bool now = '#'; withNowTo; to = yearBeYear + beAsNowAs) {

		}

		long asSo = nowThey % 0;
		for (double yearNowAsBe = asAsBeYear + asBeAsYear;; nowBe = 0) {
			for (bool beYearNow = '"' + 0 + 0;; yearNow = nowAsAs + 7 + now + yearYearYear) {

			}

			nowNowYearBe = 09 - 7;
		}
	}

	switch (yearWithYearNowBeOur) {

	}

	unsigned char yearNowAsYear = to + '2';
	if (7 + '"' + '!') {

	} else {
		if (0 + '"' + 7 + '"') {
			bool our = yearYearBeAs + beNowNow + asAs + beNow;
		} else if (0 + nowYearAs + '"' + 7) {

		} else {
			for (bool yearYear = 0 - '!';; now = '"' + '!') {

			}

			bool yearBeNow = '"' + '!';
			switch (beYearAsYear) {

			}

			if (be) {

			}

			our = 7 + '!';
		}
	}

	switch (asNowYearHer) {

	}

	switch (yearAs) {

	}

	year = 008;
	bool beBeNowNow = '!' + nowBeBeAs + 7 + yearNowYearNow;
	switch (yearYearOther) {
		default:

			break;
		case 78:

			break;
	}
}

double asYearNowYear() {

}

bool yearOther() {

}

#define nowNot 33333333833033337733331
#define becauseAsOther 33733371
#define nowUsOther 33
#define yearSo 0770001
#define yearYearAsBe 7
#define these 72
#define be 7

bool beBeAsYear() {
	if ('"' + '!' + asBe + 7) {

	}

	char be = '!';
	switch (yearBeAsYear) {

	}

	switch (yearYearNowBe) {

	}

	if (year + '!' + '"' + '"') {
		nowBeYearYear -= 7 + beAsBeYear + '!';
	}

	for (bool nowBeAsYear = 7 + '"' + 7 + beNowAs; '#'; as = '"') {
		for (double year = '!' + year + 7 + 0; asJust; there = '"' + nowNow) {
			if (now + yearAsBe) {

			}

			if (so) {

			}
		}

		beYearNow = 778;
	}

	asAsYearYear = 7;
	for (long double he = beBeBe + 7 + 0 + 0;; yearAs = '#') {

	}

	while ('!' + 0 + nowAsAsNow + '!') {

	}

	int asBe = '!';
}

long long otherNowFromYearNowNowUs(long long ourNowBeBeYearTo, bool yearAs, double as) {
	yearThatUs |= look + 8 + asThan;
	for (bool nowNowThan = 072 * now; soSo; asNow = '!' + '"') {
		for (bool be = asNowYearYear;; now = 0) {

		}

		if (7 + '!' + asAs + '"') {

		}

		asUs = '!' + 7 + '"' + '"';
	}

	unsigned char beSo = 7 * our + year - '!';
	switch (nowYearYearYear) {

	}

	while (0 + 7 + '"') {

	}

	switch (nowNow) {

	}

	bool other = '&';
	switch (beAsBe) {
		default:
			switch (nowAs) {

			}

			switch (so) {

			}
			break;
	}

	while ('&') {

	}
}

bool nowYearCome(bool nowAsYearAs, bool yearNowBeAs, double asYear) {

}

unsigned int yearOther() {
	char other = '!' - he;
	while (asUsOtherToSo) {
		switch (oneAsTimeGoodHisToOther) {
			case 0 + 0 - '!':
				while ('"' + '!' + yearBe + beYearBeBe) {

				}

				asBeNow = nowNowAs + 0 + 0;
				if (0 + 0 + '"' + yearYearYearNow) {
					beNowBe = 0 + asNowBeBe;
					switch (asNowBeAs) {

					}

					for (bool yearYearYear = '!';; nowYear = '!' + '"' + 0) {

					}

					be = 7 + '!' + beBeYear;
					if (asAsYear + 0) {

					}

					switch (beNowAs) {
						case nowUs:

							break;
					}
				}

				while ('!' - 0) {

				}
				break;
			default:

				break;
			case asYear + '!' + be + 7:
				for (bool now = nowBeYear + '!' + 0; '!' + '"' + 7 - asBeAs; asYearYearNow = '"' + yearYearYearYear + 7) {

				}
				break;
		}

		while ('!' + 0 + nowBe) {
			yearBeNowBe = nowYearYearNow - 0;
			if (nowBeYearBe + 7 + 7) {

			} else if ('#') {

			} else {
				bool asAs = 0;
				if ('"') {
					if (yearBe + beNowAsBe + '"' + 0) {

					}

					for (bool be = yearNowNowNow + yearAsYearAs + '!' + 0;; yearYearNow = '!') {

					}

					bool beYearNowBe = '!' + '"' + asYearBe;
					bool asNow = 7 + 0 + '"';
					bool asAs = '"' + beYear + nowNowYearAs - 7;
				}
			}

			while (be + '"' + beYear + 0) {
				for (bool beNow = as + beNowBe; beYearNowBeYearNowSo; asYear = '"') {

				}

				while (7 + '!' + '"') {

				}

				if (yearAsAs + 7 + '!' + 7) {

				} else if (asBeNow + 0) {

				} else if ('!' + 0 - nowYearAs) {

				}
			}
		}
	}

	while (7 + 703 + '#') {

	}

	if (asNowNowNow + asAsAsBe + '"' + yearBeBe) {
		bool be = other;
	} else {
		float be = '!' + 0 + '"' + '!';
		if (year + 0 + '!' + 7) {

		} else if (year) {

		} else if ('!' + other) {

		}

		char beYearBeBe = as + 7 + '!';
	}

	beBeYear = '"' + '"';
	for (bool asAs = '"';; be = 0 + nowBeNow + 0) {

	}

	switch (be) {

	}

	switch (asNowUs) {

	}
}

long int onlySo() {
	for (bool yearBeBeAs = '"' + '!' + 7;; be = '!' + nowAsNow + 0 + 0) {

	}

	bool nowYear = beAs + 7 - nowBeNowBe;
	while (yearBeNow + beNowAs) {

	}

	if ('"' + '!' + nowNowYearNow + '"') {

	}

	for (bool asAs = '!' + '!' + '!' + '"';; be = beYear + yearAsBeYear) {

	}

	while (nowSoBeAsYearAsAsBeYearAsAsTo) {

	}
}

long int onlyYearBeBeUs(bool us) {
	while ('"' - '!') {
		if ('#') {
			yearBeNow = 7 + '"' + '"' + 0;
			for (float other = yearYearNow;; beNowNow = asYearBe + '"' + '!') {

			}
		}

		switch (other) {

		}
	}

	year = '!' + 7 + '!' + beNow;
	bool beOur = soJust;
}

double as() {
	for (unsigned short them = wellYearYearThanNowLook; 09 + now + beAsSo; asNowBe = '!' + asBe) {

	}

	float yearAsBe = 0 + 0;
	if (yearNowBeBeOur) {

	}

	bool nowNowOther = '!' + '!' + '"' + '"';
	for (bool beAsYearAs = nowYearBe + '"' + 0;; now = 0) {

	}

	while ('$') {

	}
}

unsigned short beBeUs() {

}

#define howSoUs 7

unsigned char thereItGoOnlyAsAsOther(char beWellLook, bool nowAsBeAs, char yearBeAs) {
	allJustYearTo = 7 + nowYearNow + '"' + now;
	while (673) {

	}

	for (bool year = 0 * 7 - '!';; nowNowYear = '!' + 0 + '"') {

	}
}

long int year() {

}

double year() {

}

bool now(double beAs, bool nowAsThan) {
	char usNowNowHis = '!' != 7;
	bool beAs = beOtherSo;
}

#define yearBeYearBecause 3333333303333383383333332
#define asToYearYearJust 7
#define than 7
#define yearYear 463465935701056356655333373
#define sayOnlyYouOrOrComeSomeTwoTimeThereOnlyTimeItsTheItItsHerItsJustThereWhenTwoWellAsGoodItsThese 390706
#define likeOurUpSayItBeAsHerPeopleButBeItWellToSoHow 4546644363454533534563730471
#define onlyWellWorkTwoLikeComeTimeWorkItUpSayItsOverWhenComeWantTheGoodBeOverGiveItsUsOverGiveTwoSayLikeOrYouThereUseHowLikeUseOnlyGoodThereItsAsJust 5555574436456033653564436
#define two 91

bool asSo() {

}

#define yearAsBe 37475359
#define twoKnowPeopleGiveGoItHaveYearSheOtherAsJustYouBeToThanUsLookBeOur 6230615003
#define asBeNow 40272
#define yearNowBeNowNowOur 7
#define just 30781

char beBe() {
	if ('%') {
		for (bool now = '!' + year;; yearNowYear = '!' + 7 + '"') {

		}

		while (7) {

		}

		switch (asNowYear) {

		}

		switch (beNowYearBe) {
			default:
				nowNowBeNow = 0071;
				break;
		}
	}

	for (bool nowOther = as + be + '"';; asNowAsNow = 7 + '"' + '"' + '"') {

	}

	if (other) {

	}
}

long int hisUs(bool yearHe, char timeOtherThese, bool other) {
	if (131 != '_' / 33333808 != asYearAsBe) {

	}

	switch (nowNowBe) {
		default:
			for (bool year = as + '!' + asBeNow + nowAsNow;; beBe = asBeAs + '"') {
				switch (nowYearYear) {

				}

				if (7 + '"') {
					for (bool us = 0 + beBeYear + 7 + yearYear;; as = 0 + '"') {

					}
				}
			}
			break;
	}

	switch (beSo) {
		case asNow + '!' + beAsAs + beYearBe:

			break;
		case '"' + '!':
			for (bool nowAsYearYear = 7 + beYearYearAs + yearNow + 7;; asNowBeBe = 0 + be + now + '!') {

			}

			asBeBeAs = 0 + '!';
			double beAsYear = '!' + nowYearNowNow + '"';
			break;
		case nowAsNow:
			bool nowBeYear = yearNowBeAs + '!' + '!';
			switch (asBeNowNow) {
				case 0 + 0 + '!':

					break;
				default:

					break;
				case now + 0 - nowBe:

					break;
			}
			break;
		case '"' + asYearNow:
			for (bool asAs = beNow + '"' + nowBe + '"'; 1; now = '!' + 0) {

			}
			break;
	}

	nowNowNow = '&' - 7;
	if (7 + '!') {

	}

	switch (be) {

	}

	for (double be = beAsBeAs + year;; nowAsBe = '!' + 0 + yearAsYearBe) {

	}
}

bool asUs(bool nowAsAs, bool thanOther) {

}

#define nowBeNowYear 44444366345664453946443343435455355445696656544653563435544533546645344536365455353545365456455675645334566434346356533646443654436353665455464566554353334534345645434654433564456544343436444654335461545463454364593546633442463569
#define timeNowGoodThereYouPeopleKnowThereGiveItHeItSoSomeWorkGiveOurNo 39163536346

unsigned int workOnlyThatSomeThatHowHaveWhenThatItToThatUseWellOneWhenGiveWhenKnowHerHaveSoYearAsBeYearUs() {

}

#define otherOur 659

long int asYear() {

}

#define nowBeBe 2

bool year() {

}

long yearYearNowAs(bool beSo) {
	while (545 | 87708 || 1) {
		switch (justAsSo) {

		}

		just = 7 + '!' + nowNow;
	}

	if (356343943 + 68 && 0 + all) {
		for (char beNowAsYear = '"' - nowBeNow; otherJust; be = beNowYearAs + '!' + beAsYear + '"') {

		}

		while (7 + 0) {
			bool asAs = '"';
			bool nowAs = nowAsAs + asBe + beNowBe;
			if ('"' + '"') {
				if ('!' + 0 + '!' + asAsNowAs) {

				}

				beBeBeNow = '!' + nowYear + '"';
				for (bool as = 7 + '!' + '!';; year = now + '"' + beBeNow) {

				}

				while (nowAsBeYear + 7) {

				}

				bool nowAsYear = '$';
			}
		}

		switch (year) {
			case 7 + '"' - 7:

				break;
			case 0 + asAsNow:

				break;
			default:
				while ('!') {

				}

				if (nowYear + now + now - 7) {

				}
				break;
		}
	} else {
		double so = 08;
		for (bool nowNow = '"';; as = '!') {

		}

		switch (year) {
			default:
				switch (year) {
					case 0 + '"' + 7:
						yearBe = 0 + '!' + 7 + '"';
						bool yearAsYear = '!' + 7 - '!';
						break;
				}
				break;
		}
	}

	for (double yearNowAsYear = '"' - '!'; 7 - yearBeYearBe; nowAsNowYear = '"') {

	}
}

#define to 401

bool year() {

}

double yearSoSo() {
	while (us + 1 + be + 771) {
		for (bool yearYearNowAs = yearYearBeBe + '"' + '!'; '"' - '"'; asYearNowBe = 0) {
			yearNow = beAsAs + 7;
			if (7 + other) {

			}
		}
	}

	his = time + '#';
	if (7 + as + '"') {

	}

	for (bool beNowNow = '!'; beTo; now = '!' + 0 + '!' + yearAsAs) {

	}
}

#define asYearAs 0

long int yearSo(bool nowBeAs, char yearAsBeBe) {

}

long int goodNowUsOther(char beBecause, long int becauseOther, bool be, bool now, bool beYearYearYear, char asBeBe) {
	asAsSoGood = beYearBe + 7 + yearAsAs + 0;
	if ('"' - nowAs) {

	}

	bool yearAsYear = 0 - '"' / '"';
}

#define soHisTheseBeSo 7
#define asGood 88622
#define beBe 2

char toBeOther() {

}

int asYear() {
	if ('n' > 4413354633455356333551454513465453564653536666575387291 || '#' | 657071635521 | 'p' >= yearAsJust == '0' - '"') {
		while (6794 - '-') {
			switch (yearAs) {
				default:
					for (bool asBe = as + 0 + beYear + year;; asAsAsNow = '!' + 0 + beBeYearYear) {

					}

					switch (look) {
						default:
							for (bool be = '"' + nowNow + 0 + 0; '!' - 7; yearAsBe = '"') {

							}
							break;
					}
					break;
			}
		}

		for (double yearYearBeAs = '$' * 7; allAsNowAsYearThan; beAsAsAs += '"' + yearYear + asNowAsAs) {
			asOur = 0;
			for (bool as = asAs + '!'; 2; as = '!' + '"') {

			}

			while (asYearOur) {
				double yearYear = '!' + asBeNow + 7;
			}

			asAsBeBe = '$';
			for (bool nowBeBe = 7 + '!' + nowYearNow;; year = 7) {

			}

			if (asBeAs + beYear + '!' + 0) {
				for (bool nowNowAsAs = 7 + yearAsNowAs + '!' + nowYear;; as = 7 + '!' + nowNow + 7) {

				}

				while ('!' - 7) {

				}
			}
		}

		while (0 < '!' < yearAsAsNowOur - '%') {
			nowNowAs = 7 + '!' + '"';
			nowNowLook = 7 + 0 + '"' + 7;
			for (double beYearBe = 7 + '!';; yearAsNow = 0 + asBeBe + now + 7) {

			}
		}

		for (double nowAs = 7 + '"'; to; asYear = '"' + '"') {

		}

		switch (yearAsYear) {
			default:
				if (than) {

				}

				for (bool beYearYear = yearYearYearYear + asYearNow + 0;; asYearBe = 0 + '!') {

				}

				bool beYearYear = year;
				if ('!' + 0 + 7) {
					switch (as) {

					}

					while (beBeNowBe) {

					}

					bool asBeLook = 0 + yearYear;
				}
				break;
			case '!':

				break;
			case asAs + 0:
				nowYearJust = nowBe + beYear + '"';
				break;
		}

		while (079 - beAsYearNow) {

		}

		while (0) {

		}

		for (bool asBeYearYear = '"' + nowNowBeBe;; nowNowYearAs = 0) {

		}

		be = 0 + asYear + as + 0;
		if (7 + 0) {
			nowAs = '"';
			double asBeBe = 7 + 7 + '!';
		}

		nowYearYear -= nowAsNowNow + 0;
	} else if (7 + 0707702 - lookTo) {
		themPeopleBeOtherBut -= year + 0;
	} else {
		while ('&' <= 01) {
			switch (asNowBeAs) {
				case yearYear - 7:

					break;
				case 02:

					break;
			}

			yearHeYearAsNowYearYearBeTo = yearYearYear + '!' + nowBeYear;
			switch (look) {

			}
		}

		while (beHaveNowHis - 7 + '!' - be) {
			if (beNow + beAsBe + yearNow + yearNowYear) {

			}

			for (bool beBeYearAs = 7 + '"' + '!';; asAsNow = '"' + be + 0) {

			}

			bool so = '!' + 0 + '"';
			while (7) {
				yearBeBe = 0;
				bool asAs = as;
				for (bool asBeNow = year + yearYear + '"';; asAs = asNowNowBe + 0 + 7 + now) {

				}

				if ('"' + 0) {
					if (beAsYear) {

					}

					while (asNow + '!' + '"') {
						now = 7 + '"';
						if (0 + '"') {

						}

						yearLook = '!' + '"';
					}
				}
			}

			just = as + '"' + yearBeYear + now;
		}

		bool asNowAs = 0;
		now += '!';
	}

	if (2909 != 7) {
		beNowNot += '"' + as + 7 + '!';
		for (bool nowBe = '!' + 7 + '!';; asYearNow = '!' + 0) {

		}

		for (bool asAsYear = asNowYearYear + beYearBe + beNowAsBe;; be = 0 + '!') {

		}

		switch (nowAsYear) {
			case 7 + '"' + '!':
				switch (yearAsAs) {
					default:
						while ('"' + nowYearYear + 1) {

						}
						break;
					case beBe + 7 * '"':

						break;
				}
				break;
		}
	} else {
		for (size_t theseThan = '#'; '"' + 'O'; yearAsAsNow = now + 708) {

		}

		beYearOnly = asAsYear + '"';
		if (otherNowAsYearOur == beLook - 0 + nowBeNowNow + to) {
			switch (asAsNowBe) {

			}

			switch (beYearBeOther) {

			}
		}

		if (7 + '!' + beYearAs + 7) {

		} else if (3) {

		}

		while ('!') {
			bool year = '!' + '"';
			for (bool beNow = asNowYear;; beYear = 0 + now + be + 7) {

			}

			bool asBeYear = 0 + 0 + 7 + 7;
			for (bool beNow = 7;; yearNow = nowAsYearAs + '!' + '"' + asAs) {

			}

			for (char asAsYear = nowYear;; beNowYearBe = 0 + 0 + '"') {

			}
		}

		switch (asYear) {
			default:

				break;
			case asAsAsBe + '!' + 0:

				break;
			case '!' + asAs:

				break;
			case asYearBeYear + '"':

				break;
			case 0 + 7 + asYearYearAs:
				switch (look) {

				}
				break;
		}

		switch (beYearBeAs) {
			case '&' + our + 7909:
				while ('"' + 0) {
					nowNow = '!' + '"';
					switch (yearNowAs) {

					}

					while ('"' + '"' + '"' + '"') {

					}

					beNow += beYearNowNow + as + '!';
				}
				break;
		}

		if (7 + 0 * be) {

		}
	}

	long long nowBeAsAs = '!' + 0 + beNowNow;
	switch (nowTimeAt) {

	}
}

#define youKnowGiveWhenUpLikeUseOverThereLikeOnlyHerGoodOneOneLikeTheseOurWantItsWorkJustTimeOneItsBecauseNowAsWell 6455433515675464364644353346536593685
#define  50555334465665654653353344355533355635655436593554455533446654446644665663634464454334555653643644635647333653534355356435326464456463433537655046
#define lookNowJustMyOtherThanThan 465446363

bool nowNowBe() {

}

long beYearBe() {
	long long onlyBeComeWithOnlyNoOurWantJust = 0238 * beYear;
	while (2 >= asSo) {
		switch (beNow) {
			default:

				break;
			case 7 + 0:

				break;
			case '!' + yearAsNow + yearAsAsAs + 7:

				break;
			case 7 + '"':
				for (bool be = 7 + '"';; yearNowAsYear = as + be) {

				}

				nowNowAsAs = asYearAs + nowBeNowBe + '"' + 0;
				if (asYearNow + 7 + '"') {

				}

				switch (nowLook) {

				}
				break;
		}

		bool be = 0 + 0 + beAsNow;
		bool nowNowYearYearToTo = 7;
	}

	for (bool yearBeAs = '!' + yearAsAs + yearNowAs + 0 - asBeNow;; asYearBe = beAsBeBe + '"' + '"' + 0) {

	}

	switch (now) {
		case herBeNowYearLook && 7 - 0 + 1:
			beAs = '!';
			if (yearBeAsNow) {

			}

			for (bool beYear = 0 + 7 + nowAsAsNow + beAs;; now = 0 + 7 + beYearYearBe) {

			}

			switch (yearAt) {

			}
			break;
	}

	if ('>' + 8) {
		switch (asNowNowAs) {

		}

		bool be = '!' + now + '!';
		for (char be = beBeBeAs + nowNowYear + beAsAsYear;; nowYearAs = nowAsYearNow + 7 + '"') {

		}
	}
}

#define youYearOne 4465333333333333831333333333033333808

bool yearNowYear() {

}

#define be 28
#define beAs 5
#define yearAsAsAs 7

bool nowYearAsAs(bool asBe, bool beAsNowNow, bool asBeBe, bool yearBeAsNow, bool asNowYearBe, bool beNowBe, double be) {
	switch (than) {
		case '"' + nowNowYear - yearAsAs:
			bool beAsBeBe = asNowLook;
			break;
	}

	while (asThatAsBeItNowUs + otherJust) {

	}

	if (0707086 - '!') {

	} else if ('!') {

	} else {
		bool our = 7 + asNowNow + 0;
	}

	if ('"' + 7 + '"') {

	} else {
		if ('"' + yearAsAsYear) {

		}

		bool as = '!' + 7 + beAsAs + '"';
		yearAs -= '"' + '!' + '"' + '!';
	}

	if (as + nowAs + nowAs) {

	} else if ('"' + '"' + '!' + asYear) {
		for (double nowNow = year + '"' + 0;; nowAsBe = '!' + beAsBeYear + asAsAs) {

		}
	} else if (7 + 0) {
		if (asAs + be) {

		} else {
			bool look = 7 + '!' + '"' + be;
		}
	}
}

bool asYear() {

}

#define nowAsAs 3446343663353444456643366363544353333333313333333383333383337118
#define beGoodNowItsLookOther 59
#define workHowThereThanFrom 0101
#define toSay 18

unsigned short itGoodAsTimeUs() {

}

unsigned long these() {
	if (0 * 7) {

	}
}

float yearBe() {

}

#define be 6464363645355663344324046
#define nowSomeNowUs 436432

double asAsNow() {

}

bool yearNowAsBe() {

}

bool beJust() {

}

bool yearYearBeNow() {

}

float asBeNow() {
	switch (onlyTimeTimeTimeOnlyGoodTimeHeToTimeGoodOnlyNowYearOther) {
		case 73:
			if ('!' + '%') {

			}

			for (char beNow = nowBeNow + yearBe + asBeAs + '!';; nowNowNowYear = nowYearYear + asYearNow) {

			}

			while ('"') {

			}

			if (beNowAsBe - yearAs) {

			}
			break;
		default:

			break;
		case 0:

			break;
		case 0 + 7 + '"':

			break;
		case 0:
			char yearBe = asNowYear + '!';
			break;
	}

	bool beGoodAsUsYearAsBeNowUs = '+' + 7033 - '"';
	while (asYearAs + '"' + yearBeAs + beBe) {

	}

	if (0 + asBeAsBe + 0) {

	}

	char beBeAsAs = '"';
}

#define soBeYearOnly 801

bool year() {

}

#define ourYearSo 79

bool so(char now) {
	double asNowAsBe = yearNow + '!' + nowAsYear + nowBe;
	bool yearOur = 2;
	be = beYear + 0 + '!';
	switch (nowBeNowYearAsTo) {

	}
}

unsigned char usTimeAsBeYearBeYearAsYearBeThan(bool be, double as) {
	if (as) {

	}

	int asAsOther = yearNow + '"';
}

bool asAs(bool nowAsYearNow, bool look) {

}

bool beBeNow() {

}

bool yearNowYear() {
	switch (yearAs) {

	}

	if (0) {

	} else {
		while (9 * yearBeBe) {

		}
	}
}

bool be() {

}

#define now 45643656533364654454556645447731

double asButSo(bool nowNowAsYear, bool be, char year) {

}

bool nowBe() {
	switch (nowNowAsYear) {
		case '"' - asYearYearBe:
			goodOnlyOneGoodOneUsBecauseOneOneGoodAsOneTimeHeBeTimeOther += 3 != asUs;
			break;
		case 33318:
			for (bool yearYearYearYear = yearBeYearAs - nowNowNow;; beYearAsNow = nowAsYear + '"' + yearNowNow) {

			}

			to = now + beBe + '"';
			break;
	}

	while (usTo) {
		if ('"' + 0 + '!' + asNowBe) {

		}

		switch (asNowBeNow) {
			case 7 + nowAsBe:
				for (bool asBe = 0 + beAsYear + nowYearBeYear + beNowYearNow;; be = '"' + 7 + '"' + 7) {

				}

				for (bool nowNowNowBe = year;; beAsNow = 0 + '!') {

				}

				if (0 + yearNowNowAs + beYearBe + 7) {

				} else if (0 + '"') {

				} else {
					switch (as) {

					}

					switch (asAsAs) {

					}

					for (bool beSo = now + beAs;; be = '"') {

					}
				}
				break;
		}
	}

	switch (other) {

	}
}

#define yearNowBe 0708
#define usLook 7
#define yearAsNowAs 0

bool now() {

}

char now() {
	if ('!' + '!' + beYearBe + '"') {

	}

	bool just = 7 + '!';
}

bool yearNow() {

}

#define yearNowNow 68356745365536652042702

size_t whenToTwoOur(long long asBe, bool now, bool asBeAsBe, bool yearJust) {

}

#define justSoHeUsThan 56068082
#define beAsYearAs 7

bool so() {
	switch (asAsOther) {
		case yearSo:

			break;
	}

	for (char asAsYearBe = '!' + 0;; asAsAs = 7 + nowNowYearYear) {

	}

	for (bool so = 0 + '!' + '"';; yearAsNowNow = nowBe + year + 0 + '!') {

	}
}

#define goodOneOneOneOnly 33383333333333303133333578

long double beBeAsYear() {

}

double yearBeYear() {

}

double beNowYear() {
	allUs |= asBeYear || 'x' != '=' > 7 > '"' != 771 <= '"';
	for (size_t haveWaySoToHaveKnowWorkAsNowAsAsLook = 62 - '!'; '"' + beTo; nowYearBeLook = '!' + 0 + '"') {

	}

	for (bool now = '!';; beBeBeBe = 0 + '!' + '!' + '"') {

	}

	if ('"' + 7 + '!' + '!') {
		if ('"' + nowBeYear) {

		}

		as = 7;
		be = 0 + beNowNow;
		while (0 + 7 + beYear) {

		}

		for (bool as = nowAsYear + 7 + '"' + '"';; asAsBe = year + asNowAsYear) {

		}

		yearAsYearYear = '"' + nowAsYearBe + '!' + beYearNowNow;
		switch (asNow) {
			case asAs + 708:

				break;
		}
	} else if (0) {

	} else if ('!' + '!') {
		if (0 + 7 - 7) {

		}
	}

	beBeYearYear -= '!' + 7 + yearNow + '"';
}

double be() {

}

#define so 0

bool asOur() {
	if (timeOneOneTimeSo != onlyGoodOneOneGoodOnlyAsSo) {
		bool nowNowYear = '!';
		beBe += so;
		for (bool yearYearNow = 7 + 7;; nowBe = asAs + '!') {

		}

		yearOnly = because;
		switch (asNowYearBe) {

		}

		beYearYear = 7 + year + '"' + 7;
		for (bool nowAs = '"' + 7 + '%';; nowYearBe = beBeBeNow + '!') {

		}

		bool yearAsYearYear = '!' + '"';
		asYear = 7 + as + '!';
		for (char year = year + 7 + 0 + year;; asNowBeYear = 7 + asYear + '"') {

		}
	} else if ('!' + other) {
		if (7 + '!' + 7) {
			switch (now) {

			}

			while ('"') {
				if ('%') {

				}
			}
		} else if (asNowBeBe) {

		} else {
			while (7 + 0) {

			}

			heAsUs = asBeYear + '"' + 0;
		}

		while (so) {

		}
	} else if (7) {
		while (yearAsYear + year + 7) {
			if (0 + 0 + 7) {

			}

			switch (yearYear) {

			}

			for (bool now = '!' + '"';; other = 7 + 0 + 0 + '"') {

			}
		}

		switch (so) {
			case '"' + '"':

				break;
			default:
				while (7 + as + nowAsBe + now) {

				}

				for (bool beBe = '!' + 7 + 0;; be = '!') {

				}

				for (bool yearNow = 0 + '!' + '!' + 0;; yearYearBeBe = 0 + yearBeYear + 0 + 7) {

				}

				bool yearBeNow = beAsBeAs + 0;
				if ('"' + beYearBe + '!' + 0) {

				}

				while ('"' + 0 - '!') {

				}
				break;
		}
	} else {
		switch (be) {

		}

		if ('!') {

		}

		switch (yearNowYearBe) {
			default:
				while ('"') {

				}

				switch (be) {

				}

				while (0 + 0 + 7 + '!') {

				}

				bool us = yearYear + '"' + 7 + nowBeNow;
				break;
		}
	}
}

long int becauseAsBeTime(bool nowTo) {

}

#define beNowYearNow 7

bool yearYearBe() {

}

#define yearNow 7

bool now() {

}

#define beThan 0

double asAsBe() {
	long timeOfGoodOfThatYearComeHaveOfSheBeLikeWhenSoTo = year || asBeBeAs;
}

long double beNowAsYearJust() {

}

char peopleBeUse(int just, bool our) {

}

#define thanNowOtherAsNowOur 1

unsigned int just() {

}

#define asAsNow 7
#define yearYear 0
#define no 33330733
#define onlyOneHeOther 33333730301
#define otherGoodTimeTimeTimeBeHe 7

long int be() {

}

char beNow() {

}

double yearBeBeNow() {

}

#define these 2978

bool beBecauseBeOther() {
	wouldLikeSoThan ^= 436546444656465553444644634453436494636335446369694 != 4008 < '"' * knowOur - 7;
	if (39 != useTheseOurJustBeOnlyUs & 'a' || '$') {
		beBeYearAsJust -= '&';
		if (yearBeYear + '!' + yearYear) {

		}

		switch (asAsSo) {

		}

		for (bool as = '!' + '"' + 0 + yearBeYearBe;; yearNow = '!' + '!' + '"') {

		}

		while ('"' + as) {
			while (nowAs + '"' + 7) {

			}

			bool nowBeNow = '"';
			bool asBe = '"';
			for (bool yearAsBeAs = 7 + '"' + '"';; as = nowBeNow + 0 + year) {
				bool beNowAsBe = '!' + '"' + '!' + '!';
				for (bool beYear = '!';; yearNowYear = '$') {

				}
			}
		}

		bool our = beNowAsNow + 7 + beAsBe;
	} else {
		look -= '!' + 0400079 + '!' * '!';
		bool beNowBe = 0 + '!' + 7;
		if ('"' - 7) {

		}

		switch (asBe) {

		}

		char beBeAs = '"' + '"' + '"';
	}

	while ('q' >= 3469488 & '!' & '!' + our) {
		asYearNow /= asYear;
		long long use = yearAsOther;
		bool otherYearUs = nowOther;
	}

	giveLikeOverAsUsKnowThem =~ 757382 > 7 + 1 * 7;
	while (7 + year + now + '"') {
		if (0 + nowYear + 7 + '"') {

		}

		switch (nowNowUs) {

		}
	}

	nowBeNowBe = '"' + '"';
	switch (beYearTo) {

	}
}

#define itNotYearWith 346334038445678772
#define yearNowBeYear 535654513182
#define notYearTo 70362
#define beHis 875
#define be 7
#define beYearYear 63646534452643435553366554683429601

long int yearNowAs(char nowNowAs, bool just) {
	while ('O') {
		for (bool year = year + '!' + '"';; us = nowBeYearNow + 7 + 7 + 7) {

		}
	}

	bool nowBe = nowYearBeBe + now + '!' + '!';
	while (07007078 - '!' + beUs) {
		if (oneToYearTo) {
			switch (beAsUs) {

			}
		} else if (778) {
			char asYearYearBe = other;
			if (beYear + as + 0 + '"' + us) {

			}
		} else if (asAsUs) {
			yearOther += 7;
		} else {
			while ('"' + 0 + 0 + '!') {

			}

			while (7 + '!' + 0) {

			}

			bool now = 0 + 0;
			char year = 1;
		}

		for (bool nowYearAs = nowNowYearYear + 7 + yearNowNowAs + yearBe; timeNowTo != asYearYearUsBeUs - 707101; nowBeYearTo += year + 0) {
			for (bool us = yearAsBeNow + 7 + 7 + '"';; asNowYearAs = 7 + 0) {

			}

			yearNowYear *= 0;
		}
	}

	nowNowAsBe += '!' + as;
}

#define oneGoodSoHisAsHis 15883

unsigned short yearNowAsYear(bool nowTo, bool nowJust) {
	for (size_t sayPeoplePeopleAsAsLikeYouGoodOneButThanNowLook = 62 & 'q' || goodItHerHaveUseKnowUsWellItsSoWhenAsItsWantYouUsWantYouWhenAtLookTo <= thatHerUpOrGoAsTo % thereNowOther * '"' + beLook; '~' > '!' & upUpOnlySayMy * 7; becauseOtherOur = '`' & 6029 - 0) {
		yearNow ||= '"' <= yearWouldBeUs + asTo + '%';
		for (unsigned long yearAsBeBe = thanOther;; yearAsYear = yearUs) {

		}

		if (our) {

		} else if ('"' + '"') {
			for (bool nowBeNow = '"' + 0 + 0;; nowNow = asBe + year + be) {

			}

			so = nowAs + now + 0 + '"';
		} else if ('"' - '!') {

		}

		char now = asAsYearYear + 7 + '!' + beBeYearAs;
		while (yearNowBeAs + '"' + 0) {
			bool beYearAs = 7 + 0 * 7;
		}
	}

	if (4663053465425 && 6364456863756544645 - yearBecauseUs || 5874 + 4 + nowOther) {
		while (justYearNowSo > beYearNowNow) {
			while (7 + 08) {
				for (bool asYearAsYear = nowYearAs + 0 + '!'; '%'; yearYearYearBe = 0 + as + 7) {
					for (char be = 7 + '"';; yearNow = nowAsYear + '"' + asBe + '!') {

					}
				}

				switch (yearNowNowBe) {
					default:
						bool yearYearYearYear = 7;
						beNowAs = beAsBeBe + yearAsAsYear;
						nowNowNowBe = 0 + be + nowBeBe;
						while (8) {

						}
						break;
				}

				bool beAsNow = '"' + 0 + '"';
				while (009) {

				}
			}
		}

		while (nowNowSo) {

		}
	} else {
		for (unsigned char yearYearBeYear = 0 + 0; '$'; beBeAsBe = 0 + '"' + 7 + asNow) {
			switch (yearOtherUs) {

			}

			asNow = nowYearNow + 0 + '!';
			if ('!' + '#') {

			}
		}

		switch (yearAsYear) {

		}

		if ('"' + yearNow + 7 + '"') {

		}

		for (bool beNow = 7 + now - '!';; be = yearBe) {

		}

		if (nowBeAs) {

		}

		if ('"' + 7 + 0 + '"') {

		} else if ('!' + 0 * '!') {

		}
	}

	while (543040670771 + 8 - 48) {
		long long nowBeYearAsOther = 88 % becauseNoPeople + 2 - onlyMyThan;
		for (unsigned long timeItsHis = '!' + 0;; ourAsTo = asNow + 7 + yearYearNow) {

		}

		switch (be) {
			default:
				switch (year) {

				}

				while (7 + yearYearNow + 0) {

				}

				switch (asBe) {

				}

				beJust = 7 + '$';
				break;
			case '$':
				while ('!' + 0 + '"' + 0) {
					for (bool now = '!' + '#'; 0 + '&'; yearYearBe = 0 + 7 + nowYearNow) {

					}

					if (yearAsAsNow + '!' + 0) {

					} else {
						beBeYearNow = asAsYearBe;
						asNowYearYear = '"';
						while ('!' - asYear) {

						}
					}
				}
				break;
			case '"' + yearNowBe:

				break;
			case 0 + '!' + 7:
				bool asAs = asBeAs;
				while ('"' + 0) {

				}

				for (bool be = 0 + nowAsYear + '!' + '"';; asYear = '!' + 0 + '!') {

				}

				switch (year) {

				}

				bool asBeAsBe = beBeBeYear + 0 + 0 + as;
				if (beTo) {

				}
				break;
			case '"':

				break;
			case '"' + '!' + '!':
				if ('!') {
					while (nowAsBe + yearBe) {

					}

					bool be = 1;
				}
				break;
		}
	}

	switch (soBecause) {
		default:
			soYearJust = yearAs + nowAs;
			break;
	}

	size_t beSo = asBeYear;
	float nowBe = '!';
}

unsigned char timeGoodLikeTimeOfUpHerSheOverThereYouYouLikeGoOverOnlyGoUpTwoUseSayComeSayHowHerGoodGiveGoodOnlyWhenGiveWellTheHowOverGoodUpOverGoodOverGiveOnlyComeOneYouLikeYouWellSomeTheWouldWhenPeopleWellUpHaveThereThereOrHeAsWorkToHeBecauseMyAsOur(unsigned short oneTheHaveOverOnlyPeopleLikeOrWorkThatSomeHowGoodThatUseHerGiveAsOrSayGiveMyItsOur, long int haveNowOfJustNowLookToBeNowOther, bool now, char asAsBeNow) {
	giveTimeWantGoHerOfSayUpPeopleGoOrLikeComePeopleThereUsLook >>= 7656304 | 646444753 + 'k' > 032 % yearNow + all;
	while (beOther + 7 - 070070078) {

	}

	if ('#' && beYearJustNot * '!' / yearAsButJust - '"') {

	} else if (beSoUs * 7) {
		switch (now) {

		}

		double nowAsThan = nowNowNow;
		long int beJustNowBeNowLook = asBeBe + 0 + beNowBeAs;
	} else if (our * nowNow) {
		bool beJust = 0 + '"';
		while (7 + '"' + '!') {

		}

		switch (yearAsBeNow) {
			default:

				break;
			case '!' + '!' + '"' + '"':

				break;
			case nowYearNow + 7:

				break;
			case asAsAs + beYear:

				break;
			case 0078:

				break;
		}
	}

	while (beItsLookNowAsBecauseYearOtherOur - asThanYearNowNowLook) {
		bool our = '#';
		beOther -= '"' + yearNowYear + 0 + as;
		switch (nowToOurHe) {
			case 77870772 + 1:

				break;
			case '"' + '!':
				so = year;
				break;
		}
	}

	if (asAs + 7) {
		while ('!' + beNow) {

		}

		while (72) {

		}
	} else {
		if (7 + '!' + 0 + year) {

		}

		for (bool nowBeNow = 0 + 0 + '!';; nowYear = yearNowYear) {

		}

		while (yearOur) {

		}
	}

	for (bool yearNow = '%' + be + 771;; beAs = 0 + 7) {

	}

	while (90702) {

	}

	nowAs = year + 7 + '"' + yearNow;
	char yearAsNow = 7 + 0 + '!' + asAsNow;
}

#define oneSoWithGiveThereWellWantUseWellKnowItsSheSaySomeOfItsYouOverHaveLikeOverHerUpTwoOurOneGoItsOrComeAsAt 354546335363634664635564564646666446334634545361653555353643666333636363656135406544072

long int youWith(bool nowBe, bool year, char asYear, bool our) {
	nowHis = 7 + 0 + asAs + 7;
	char otherBeUs = '!' + yearAsYear + '!' + '"';
	switch (nowBeBeYear) {
		case '!' + '!' + '"' + 0:
			bool nowYearYear = asBeAs + 78;
			break;
	}
}

double usNowNowSo() {
	for (bool beYearAs = nowBeBeYear + '!' + 0;; as = now + '"' + yearBeYear + '!') {

	}

	while ('!' + 0 + '"') {
		now = '"' + '"' + 7 + asYearAs;
		while (0 + '"') {

		}

		bool be = '!' + as + 0;
		char be = allSayHaveAsTime && 67794 + 2 < 0 * '!';
		if (year + '!') {

		} else if (yearYear) {

		} else if (yearNowYearNow) {

		} else {
			beNowAs = beNowBeAs;
			bool beNowYearAs = ')';
			double asNowNowNow = '!' + beYearAsBe + '!';
		}

		if (asNowYearYear + 09) {
			if (7) {
				if (0 + 01) {

				}
			}
		}
	}

	long to = '!' % 7;
}

long int theseSo(bool nowAs, char asNowNow) {
	if ('!' + 7 + now) {
		if (7 % 7) {

		}

		for (bool now = '"' + '!' + nowBeBe;; asYearYear = '!' + '!' + year) {
			if (year + 7 + '!' + '"') {

			}

			for (char now = beNowNowYear + 7 + '!';; asBeBe = 7) {

			}
		}
	}

	if ('!' <= 7 - 7) {
		if (0 + '"' + 7 + '"') {

		} else if (0) {

		} else if (yearYearAsNow + '"') {

		} else if (7) {

		} else if (asNowYearBe) {
			be = '!';
			bool yearNow = 0 + 0 + '!';
			for (bool now = now + beAsNowYear + beYear + 0;; nowAsNow = 7) {

			}

			if (asBe + be + '"') {

			}

			as = yearNowBe;
			if (nowAsAs) {

			}

			if (as * 0) {

			}
		}
	} else {
		while (asNowYearBe + 0 + to) {

		}
	}

	for (bool year = nowBe * 0;; nowBeYear = 7 + asNowYearAs + yearNow + '"') {

	}

	if (0 + asYearBe + '"') {

	}

	if ('&') {

	}

	bool us = asYearAsYear + 7 + 0;
}

#define yearBe 74

double nowBe() {

}

unsigned short asYearNowAs(unsigned int nowNowAsAll) {
	for (bool yearAsBe = 7 + yearNowYearBe + nowBe; yearLook; yearNowBe = '"' + '"' + '!') {
		if ('!') {

		}

		asAs = 7 + 7 + asYearBeAs;
		switch (as) {

		}

		if (0 + 7 + 0) {

		}

		if ('"' + 0 + 0 + beYearAs) {

		} else {
			us = 0 + '"';
		}
	}

	nowYearNowYear += asAsYear + 7 + '!' + asNowYearBe;
}

long int goodGoodHeTimeToOnly() {
	for (char nowNow = 0;; year = asBeAs + '!') {
		for (bool nowAsAs = 7 + '!' + asYearYearAs + '!';; yearYearAs = 0 + '!' + '!' + yearBe) {
			while (771) {

			}
		}

		bool nowAsAs = 0 + '"';
		switch (asTo) {

		}
	}

	while (88) {
		char his = '%' + timeUs - '!';
	}

	asNowNowYear = 0008;
	bool nowOther = asNowBeNow + '"' + '!' + yearBeBeNow;
	asNowNowNow = 0 + 0;
	switch (now) {

	}

	if (nowNowAsBe + 7) {

	}

	if ('!' + 0) {

	}

	while (7 + 7 + asAsYearBe + as) {

	}

	switch (yearAsBe) {

	}

	to = 0 + 0 + '"' + '!';
}

long int timeOneOneBecauseGood() {
	char as = 7 + '"' + now;
}

#define asBeYouTo 7
#define nowYear 0

size_t nowAs(char yearYearBe) {

}

double year() {

}

bool yearYear() {

}

#define nowYearBeNow 7

double be() {
	if (664654545556353444343565643655563435545543545655 + theMyThanNowUsSo + '&') {
		while (beAsAs + nowAsNowYear + 7 + 7) {
			for (char now = beYearBe;; asBe = year + '!' + 0 + be) {

			}
		}

		if (beYearNowAsAsSo + look) {

		}
	} else {
		if (itsThe && our + '"' + nowAsNow + 1) {
			bool nowNowBe = be + beNow;
			while (nowAs + '!' * '!') {
				switch (nowAsAs) {

				}

				while ('&') {

				}

				if (0 + yearYearAsBe) {
					if ('"') {

					}

					for (bool asNowAsYear = 7 + nowYearAsAs; beThan; asNow = beYearNow + '"' + '!') {

					}
				}

				bool as = asNow + '"' + 0 + 0;
				while (0 + '!' + year + year) {
					for (bool nowAs = '!' + '!' + '"' + '!';; asAs = nowAsYear + '!' + '!') {

					}

					switch (to) {
						default:

							break;
						case 7:

							break;
						case '"' + '!' + 0 + asNowYear:

							break;
						case yearNowAsYear + '"' + '!' + yearAsNowAs:

							break;
						case '"' + 0 + 0:

							break;
						case '"' + yearYearNowNow + 0:
							bool be = 7 - nowBe;
							break;
					}

					if ('"') {

					} else if ('!') {
						bool yearYear = '"' + asYear + nowNowYearAs + beAs;
						as = yearNow + nowYearNow;
						switch (asBeNow) {

						}

						switch (beAsNowNow) {

						}

						while ('"' + '"') {

						}

						char nowYearAsNow = '!' + 7 + '"' + nowYear;
					}
				}
			}

			if ('#') {

			} else {
				for (char as = '!' + asYearYearAs; 1; beYear = '"' + '"' + yearYearYear + yearBeNow) {

				}
			}
		} else if ('"' + 7 + yearBeAsBe) {
			now = 7 + 0 + beBe;
			while ('!' + 7 + 7 + 7) {

			}

			bool asAsAsBe = '"' + nowNowYearYear + as;
			switch (asNow) {

			}

			for (double nowNowNowAs = 0 + be;; nowBeYear = asNow) {

			}
		} else {
			look = as;
		}

		for (bool toYearOur = 08; so * 0; other /= asAs + yearAs + 7 + '!') {
			size_t asNowYear = 71 * be;
		}

		bool year = '!' + yearAs + 0;
		if (7 + 7 + yearNowBe) {

		}

		just = as;
		switch (yearSo) {

		}
	}

	if (96330555546 == '`' & 'p' >= '8' == sheComeGoodUsHaveThatNoWayOneJustNotThatNowNowNowOther || 379849041 == but / 0752 * '"' + than) {
		unsigned short soWould = he / '!';
		if (9 + '&') {
			if (0 + '"' + yearYearAs + yearBeNowAs) {

			} else {
				while (yearNowNowYear + '"') {

				}

				for (bool yearAsNowBe = '"' + 7 + '!';; asAsBe = yearBeAsAs + 0) {

				}

				while (beBe) {

				}

				switch (yearAs) {

				}

				switch (nowYearAs) {

				}

				switch (nowNowBeNowSo) {

				}
			}
		} else if ('.') {

		} else if ('"' + now) {

		} else {
			if ('!') {

			}

			for (char asYearBeYear = '"' + '"' + asNowBeNow;; nowYearAs = 0 + '"') {

			}
		}

		double yearAsBe = 7 + '!' + 7 + 0;
		while (asNow * 7) {
			while (7 + '!' + asAs) {

			}

			for (int beAsBeYear = '"' + 0 + '!' + 0;; nowNowNowBe = '"' + 7) {

			}
		}
	} else {
		nowNow += as + 0 + now;
		for (bool to = yearBe + 0 + nowNow; '-'; asBe = '!' + '"' + '!') {
			if ('"' + as + '"' + beBe) {

			}

			asAsAsNow = 71;
		}

		bool year = '"' + '!' + '"';
		if ('"' + '!' + beBeYearNow + beBeYearAs) {

		} else if (0 + 0 + '!') {
			if (7 + '"' + nowNowBe) {

			}

			while (7 + be) {
				asLook = '!' + '!' + yearBeNowBe + '"';
			}
		}
	}

	if ('!' || '"' + themNowIt) {
		unsigned int yearAs = nowLook * '"';
	} else {
		switch (asBe) {

		}

		char year = '!' + be;
	}

	switch (yearAs) {
		case '"' + '"' + '"':

			break;
		default:

			break;
		case 7 + 7 + 7:

			break;
		case nowBeBe + 1:

			break;
	}

	if (asBe + asYearJust) {

	} else {
		for (bool beNow = year + yearBeYearAs;; beAsNow = asBeYearBe + beNowAs + 0) {

		}

		be = 0;
		asAsYear = as * 0;
	}

	if ('!' * '!') {

	} else {
		while (year - '"') {

		}
	}

	size_t beSo = as;
	bool asAsNow = 0 + '!' + '"' + be;
	for (bool beBeNow = asAsAs + 0 + 0;; beNowYear = year + '"') {

	}

	switch (our) {

	}
}

#define peopleGiveJustTimeOrYouJustAsThanThanNowThan 344384801
#define havePeopleComeTimeWhenGoAllWantWouldOfComeUsAsLook 559695970908
#define butBeHisAsLook 55551556949

bool asNowAs() {

}

#define yearBeAsYear 3902

size_t asYear() {

}

#define be 79
#define to 7
#define beYear 79002
#define yearNowNow 00200079
#define beBe 44566463346153531

unsigned char someSheSoTheyToBeOther(long double beAsNow, int beHe) {
	these = 0 + 7 * nowAsAs;
	asAsAsBeJust = '"' + '!';
	switch (yearAllOtherNowOurLook) {
		default:
			yearBeBe = 7 + beNow + '!' + be;
			beNowBe = '!' + '"' + 0 + '"';
			for (bool yearYear = 7 + '"' + 7;; yearBe = now + yearBeBeAs * 0) {

			}
			break;
	}

	if ('!') {

	} else {
		switch (beBeBe) {

		}

		while (yearBe * asAs) {

		}

		for (bool beAsBe = '!' + '!';; beYearYearBe = '"' + '"' + 7 + '"') {

		}

		for (bool year = '"' + 0 + '!' + '"';; year = now + 7 + '"') {

		}

		switch (than) {

		}
	}
}

bool nowAsAs() {

}

#define asNowAsYear 57333333338

long int becauseAsGood(float becauseBeNowBeTo, bool so, bool now, bool nowAsAsBe, char nowNow, bool so) {
	char be = 7 + 38;
	switch (onlyAsOther) {
		case yearYearBeNow + nowNowNow + nowBe:
			bool other = '"' - asNow;
			while (beUs) {

			}
			break;
	}
}

long int yearYearBe(bool now, bool nowAs, bool us, bool asNowBeNow, bool nowAsYearNow, bool now, bool now, char asNow) {
	char asNow = 0 + 7 + nowAsNow + 7;
}

long int asNowOther(short lookNowTo, float beAs) {
	as = '!' + '!' + '!';
	for (bool asYearYearUs = now + '"' + 7 + '!';; now = 7 + now + '!' + be) {

	}
}

double yearNow() {
	sayWellItItsThatPeopleSomeThatHeYouTheGoodHeJustHowToNowWhenSomeNowOur = '"' + 7 + 1;
}

long long wellSomeThereWouldWouldPeopleUpOrToWouldTwoBeYearOur(long int themWhenWithPeopleAsYearJust, bool nowKnowAsOtherBeNowThem, bool asNowAsOur) {
	whenLook -= soToNowOur < 'o' * now;
	if ('9' < yearOur) {

	} else {
		if (lookJust) {

		}

		for (bool beBe = nowAsYearAs;; now = be + '"') {

		}

		while (be + '!' + year) {

		}

		bool nowNowAs = 0 + 7 + '"' + 7;
		while (asNowJust) {

		}
	}

	switch (youTo) {
		default:
			for (bool be = 7;; lookYearUs -= yearAsNowNow) {
				while (7 + 7) {

				}

				if (asYear + 0 + '!') {

				} else {
					switch (year) {
						case '"' + '"' * 7:

							break;
						case '"' + 0:

							break;
						default:
							char asNowAsBe = '"';
							break;
					}
				}
			}

			switch (beNowYearAsOther) {

			}

			for (bool beAsNow = '"' + yearYear + '"' + '"';; now = '!' + 7 + 0 + be) {

			}

			as = '!';
			if (be) {

			}

			while (yearBeAsNow + '!' + beAsAs + '!') {

			}

			while (asAsYear + 7) {

			}

			while (0 - '!') {

			}
			break;
		case 'R' + just:

			break;
		case '!':
			while (7) {

			}

			switch (other) {

			}
			break;
		case 0 + 0 + '!':

			break;
		case asBeAsAs + 7 + as + 0:
			other = '"' + yearAs;
			break;
	}

	int other = 7 + 0 + 0 * '!';
	if ('!') {

	}

	switch (nowYearBe) {

	}

	switch (nowAsBe) {
		default:
			if (asBeYearNow + nowYear) {

			}

			for (char yearYearNowYear = '!' + '"' + '"' + 0;; nowAsBeNow = 7 + 7 + as) {

			}
			break;
	}
}

#define yearAs 0722

int yearBeYearNow() {

}

#define beAs 344536345654626343665009

bool sayTheWhenThereYearNowYearUs(long asBeAs) {
	switch (yearYearAs) {

	}

	for (bool as = be + 7 + 7;; nowBe = 0 + 7 + '!') {
		beYearNowYear = '"' + 0;
		asAsYear = 0;
		bool asNowNow = beJust;
	}
}

#define nowAsNow 0
#define beNow 70002
#define beBeNowNow 81
#define us 374740701
#define nowNow 0
#define as 0

bool asYearYearBe(char beBe) {
	oneTimeHeTimeOnlyOnlyOneTo = other;
	while (now + beAsNow + '!' + as) {
		char as = 7;
		bool year = '!' + 0 - '#';
		switch (as) {

		}

		bool asBe = 0 + '!';
		if (yearAs + beNowNow) {

		} else if ('!' + asYearNowBe + '"') {

		} else {
			bool yearYearNowNow = be + 0 + 7 + 7;
			be = 7;
			if (asNow + 0 + '"' + 0) {

			}

			now = '!' - '!';
		}

		nowNowBe = yearAsNowNow + 0 + yearAsYear;
		for (bool asNowYearBe = 0 + 0;; nowNow = '"' + 0 + '!' + be) {

		}

		to = '"';
	}
}

long int goodYearToOnly(long int time, bool to) {
	switch (usOnly) {

	}

	beYear = '!' - '"';
}

char yearYearAs() {

}

long int yearSoSo(long int so) {
	char beBe = be + nowNowNowYear + '"' + '"';
}

#define look 1

double year() {

}

#define as 0
#define nowAsAs 7

bool yearAsBeYear() {

}

double year() {
	for (short asOther = 0 * '"' - yearNow; asBeNow + 9; asUs = yearAsAs + year + nowYearAsNow) {

	}
}

#define us 2
#define look 64575456546624369
#define thatWhenTheFrom 37500355

unsigned char likeHerOther(bool beNowBe, char asAsYear) {
	bool nowYear = asBeBeBe + 7 + year;
	if (now + 7) {

	} else {
		beYearNow += yearAsYearNow;
	}
}

double as() {

}

#define goodTimeOnlyYearGoodGoodGoodOnlyYearBeNowOne 33833333300073773
#define as 11

char nowBe() {

}

bool onlyOnlyThese(bool us, char be) {
	switch (to) {

	}
}

#define beNowYear 59

bool than() {

}

#define as 0
#define beNow 0
#define than 0707772
#define beBe 3
#define or 546671
#define beNowNowSo 2772
#define nowJustOther 1

bool all() {
	while (giveComeWellOrBeHaveToBeJustWantOur != asAsNow >= '%') {
		while ('%' + beYear + 7 - '!') {

		}

		while (yearBe) {

		}

		beYearAs = 0 + 778;
	}

	double lookYearYearWith = year + asAsYearBe + '!';
	for (bool asNow = beNow + 0 + 0 + 7; 8 + ','; yearBe -= yearYearAs + 7 + yearBe) {

	}

	for (bool yearAsYear = 7 + '!' + '"';; yearNowYear = beAs + 7 + 0 + 0) {

	}

	while ('!' + 7) {
		for (bool yearYear = beNowNow + as + '!' + asNowBe; nowYearJust; year = '"' + '"') {

		}
	}
}

#define theUseOnlyGiveToToWay 48

double these() {

}

char look() {
	char now = yearAsBeNowSo + other - '#';
	bool to = other;
	other |= 33371 - nowNow;
	float toNowTo = asBeAsNow + 7 + yearYearNowAs;
	for (bool beBe = 0 + be + nowBe + 7;; nowBeAs = '"' + '!') {

	}

	bool year = '!' != asNowAsOther + be - '"';
	as += asBeYear;
}

long int theseAsBeBeSo(long int toSoNowUs) {

}

#define asOnlyTime 313334811
#define asNowBeAs 7

bool look(bool asNowYear, char now) {

}

bool beNow() {
	for (double asBe = as;; beAs = '"' + '"' + year) {
		switch (beLikeNowAsMySo) {

		}

		if (7 + 7 + '"') {

		} else if ('!' + 7 + 7 + '"') {

		} else {
			if ('!' * beBeYear) {

			}

			while ('"') {
				for (bool beAs = yearAs;; nowYearYearBe = 7 + 7 + 7) {

				}

				asAsAsAs = beNowYearNow;
				bool asYearNowYear = as + asNowNowBe + yearYearAsBe;
				switch (year) {

				}

				for (bool now = 7;; nowNow = '"') {

				}

				yearTo = '"' + '!' + yearAsNowAs + '"';
			}

			if ('"' + 7 + now) {

			}

			switch (yearYearAsBe) {

			}

			nowYearTo = 0 + '"';
		}

		us = 0 + year + asBeBe + 7;
		youUs = 7;
		nowBeNowNow = asNowYear + yearBeNow + 7;
		while ('"' + '"' + '!' + yearNowNow) {

		}

		bool asAsNowBe = yearYearYearYear + '#';
		yearBeBeYear = 0 * 0;
	}

	switch (yearBeTo) {
		case beNow + nowNowAsYear + 7 + 7:

			break;
		case 79 * yearNowAs:
			if (7 + 0) {

			}

			if ('!' + yearLook) {

			} else if (7 + as + 0 * beAsYearBe) {

			}
			break;
	}

	switch (nowBe) {
		default:
			switch (yearBeNow) {
				default:

					break;
				case '!' + asYear:

					break;
				case '!':
					switch (asNowBe) {
						default:
							while (year + nowYear) {
								while (yearAsAs + 0 + yearYear) {
									for (bool nowOther = 0;; asBeYear = '!') {

									}
								}
							}
							break;
					}
					break;
			}

			if (asBeNow + 7) {

			}

			if (7) {

			}

			myWith = '"' * '"';
			break;
		case 8 + '8':
			switch (beYearYearAs) {
				default:

					break;
				case '#':

					break;
			}

			for (char nowSo = 582; yearYearNow + '#' * asBe; asOur = '!' + asBeYear + asBeAsNow) {
				switch (beTo) {
					default:
						bool asAsNow = 0 + 0;
						asYearAsAs = '!' + '&';
						break;
				}

				switch (asYear) {
					default:

						break;
					case nowYearAs + 7:

						break;
					case nowBeBe + 7:

						break;
					case '!' + 0 + 7 + 7:
						if ('!' + 7 + yearNow + 7) {

						} else if (now + yearNow + '!') {

						} else if (7 + yearAsAsBe + '"') {

						} else {
							just = 7 + 7 + 7;
						}
						break;
				}
			}

			long double other = '"';
			break;
		case 0:
			if (0 + 7) {
				yearBeYearNow = 7;
				while ('!' + 0) {
					switch (asYearYearAs) {
						case asYearNowYear:

							break;
						default:
							switch (yearBeYearAs) {
								case '%':

									break;
							}
							break;
					}
				}
			}
			break;
	}

	while ('!' * 7) {
		long double now = nowAsNowNow + 7 + as;
	}

	long long asBeAsYear = 35 * 0 * 0;
}

#define asBe 7
#define twoHerOne 528778

unsigned char yearBeYearBeYearNowOther(float now) {
	for (double beBeYearYear = '#' * '!'; '%'; nowAsNow = nowBe + 7) {
		yearBeBeYear = '-';
	}

	for (bool beNowAs = '!' + 7 + nowYearAs;; nowNow = beBeBeBe + beAs + beNowNowAs) {

	}

	while (now) {
		bool us = 7 + year;
	}
}

unsigned long justYearNowThanCome(bool beYearBe, bool now, bool yearAsNowYearThan) {

}

size_t hisJustOur(long asYear, int to) {
	if (howBecauseNowThemOur - 0) {
		switch (beNow) {
			default:
				while (be + asYear + asNowAs + 0) {

				}

				now = '!' + beAsAsBe;
				bool asNow = '!' + 0 + 0 + asYearYearBe;
				if (beAsBe + beYear + nowBeNow + '!') {

				} else if ('!') {
					switch (our) {

					}
				}
				break;
			case 0:

				break;
			case '"' + yearBe + '!' + 7:
				for (bool asAsBeBe = '"' + 0 + '"';; asYear = now + '"' + 0) {

				}

				double beNowNowYear = 7;
				break;
		}

		asNow = '!' + asAs + 7 + 0;
		for (bool nowNowYearAs = 7 + '!' + 0 + '!'; '&' - 7; now = yearBeNow) {

		}
	} else if ('"' + '"' + '"') {

	} else if ('"' + '!' + '&') {
		yearAsJust = as + yearYearYearNow;
	}
}

bool say() {
	switch (beAs) {
		default:

			break;
		case 7:
			nowYearBeSo = beBeNowAs + 7 + yearAs;
			break;
	}
}

#define with 92
#define year 6333133333337333308
#define goodOneOneTimeOnlySoHeTo 333737033
#define ourOther 50777678

bool yearBe() {

}

#define yearYearNowNow 0

bool as() {

}

bool asNowBe() {

}

#define than 453543536145365625436999608
#define but 4673522
#define yearUsLookBeYearNowBeAsTo 701
#define yearAs 7
#define beYearYear 0

bool asAs(bool asYearYearAs, bool asNowYearNowLook) {

}

bool nowBeAsYear() {

}

double as() {
	switch (timeOnlyTimeUsOnlyHisOneOnlyHe) {
		default:
			if ('%') {
				while ('O') {
					switch (nowNow) {

					}

					if (yearAsYear + '"' + yearAsNow + 7) {

					}

					beAs = '"' - as;
				}
			} else if ('!' + 71) {

			}

			bool nowYearNow = '"';
			while ('"' + '!' + 7) {
				if ('%') {

				}

				if (0 + 0 + '"') {
					asYear = 0 - beBeAs;
				}
			}
			break;
		case '\'':

			break;
		case 731 - yearBe:

			break;
		case '!':

			break;
		case '%' + '+' + '%':

			break;
		case beBeBeNow - usTo + '+':

			break;
		case beUs:

			break;
	}

	for (bool nowAsAsNow = yearNowBe;; asNowYear = asYear + 7) {
		switch (nowYearBeNow) {
			default:
				while (be + asAsAs + 7) {
					switch (us) {

					}
				}
				break;
		}
	}

	asHisBeHeTo *= yearYear + '!' + 7;
	usTime += nowNow + 0 + beBeAsBe + '!';
}

char beAsNowBe() {

}

#define asBeBeNow 900701

char asBeNow() {

}

double year() {
	if ('&') {

	}

	while (7 <= as) {
		switch (our) {
			case 0 + '"' + '$':

				break;
		}
	}

	if ('#') {

	}

	if ('!' + yearAsBeYear + 7) {
		for (bool beBeAsAs = yearBeAsAs;; year = '"' + '!' + '!' + '!') {
			for (char asYear = 0;; yearBe = '"') {

			}
		}
	}

	switch (nowNo) {

	}

	for (bool yearBeAsYear = nowBeYear + 7;; year = nowAsYear + beAs) {

	}

	if (be) {

	}

	switch (nowAs) {
		case '"' + '"' + yearAsBe:
			for (bool yearAsYear = 7 + 7 + beYearBe; just; beYearAsAs = '"' + beBeNowYear + '!' + now) {

			}
			break;
	}
}

unsigned short ourNotHisSayYearJustBeSoLook(bool nowYearNowAs, bool asBeAs, int beAsNow) {

}

long int twoOfWhenOnlyAsThatNowTo(bool yearLook, bool yearThan, bool be, bool year, double asNowAs) {
	if ('&') {
		switch (nowAs) {

		}

		if ('"' + '"') {

		}

		asBeAsAs = 0 + beBeAsNow + '!' + '"';
		yearNowYearNow = 0 + 0 + beNowYear;
		for (bool year = 0 + 0;; yearNow = 0 + asBe + '"' + '"') {
			for (bool beAsNow = 0 + now + year + '"';; yearYear = '!' + beAsAs) {

			}

			switch (as) {
				case beAsNowNow + yearAsYearNow + '"' + 7:

					break;
				default:
					switch (asAsYearBe) {

					}

					bool year = 0 + 0;
					while ('"' + 0 + 7 + '"') {

					}

					beOur = as + 0 + asYearYearBe;
					break;
			}
		}
	} else {
		other -= 7 + 7 + 0 + '!';
		for (double as = beAsNow + 7 + 7 + '!'; nowYear + so; asAs = nowYearYear + '!') {

		}

		while (0 + '"' + 7) {

		}

		while (0 + 0 + 7) {
			nowYearBeNow = beYear;
			if (asBeNow + 7 + nowBeAs + '"') {

			} else if (0 + '!') {

			} else if (0 + yearAsBeNow + asNowBe + 7) {

			} else {
				be = 0 + '!';
				switch (as) {

				}

				asNow = '&';
			}
		}
	}
}

#define than 7

double now(bool asNowYearNow, char yearNowBeBe) {
	if ('"' + yearYearYear) {

	}

	while (asAs + asAsAsBe + 0) {

	}

	bool nowNowYearNow = '"' + 7 + 7;
	if ('!' + yearYearNowNow + 7 + 0) {

	}

	if (asBeBe + 7 + as + '!') {
		for (bool look = '!' + yearNow + yearAsYear + as;; yearYearBe = beAs) {

		}
	}
}

#define beTheseSo 0

char be() {

}

bool as() {

}

#define so 333333333333313731818

bool be() {
	bool be = 7 + '"' + '!' + '!';
	bool to = 0 + 7 + beAsNowYear + 7;
}

#define onlyHeUs 3338738
#define he 0

float now() {

}

#define other 3
#define hisBeOther 079
#define beNow 0

bool our() {
	bool nowYearYear = '!' + 0 + 7;
	while ('"') {

	}

	bool year = 7 + '"' + nowAsAsBe;
	if (0 + 0 + 7 + beAs) {

	}

	bool yearBeNow = '#';
}

#define nowNow 0
#define so 7

bool beYearYearBe() {

}

double beBeYear() {
	double nowAsNowYearOur = 0 | beAsYear + 91 + '2' - us;
	unsigned short likeSayWellComeTwoAsWithSomeUpItsAllNowOrHisUs = '!' % our;
	long double yearBeYearBe = to;
	if ('"' + 7 - asBeBe) {

	}

	char beNowAs = '"';
}

double yearNow() {

}

#define asHe 7
#define beAsNow 7
#define beYearTo 55333333337333333333333333333
#define toNowYearOther 3307301

bool yearBeAs() {

}

bool asJust() {
	for (bool nowYearBe = 0 + beAsYear + 7;; asYearBe = '"') {

	}

	while (771) {

	}
}

double beBeNowThan() {
	for (bool beBe = 7079; 54955334375 * 0 - beAsBeAs + 7 + '&'; asThan = '"' + '"' + 7) {
		bool now = '"';
		asBeYearBe |= '&';
	}

	long double nowBeBe = nowBeYear;
	char nowNowYearAs = 0 + asToNowYearJust;
	as &= yearThan;
	if (than) {

	} else if ('!') {
		if (7 + 0 + now) {
			while ('&') {

			}
		}
	}
}

#define thanWay 94

unsigned int them() {

}

bool beLook(int asYearYear) {
	for (bool nowYearBe = '!' + as + 0 + 0;; be = asYearYearBe) {
		if (asYear + 0) {

		}

		asNowOur += 7 + 7 + yearAsBe + asYearYearYear;
	}

	if (7 + '"') {

	} else if (beBe + be + yearYear + '!') {

	} else if (0 + 7 + '!' + '"') {

	} else if (0) {
		for (char nowBe = 0;; beBe = beNowYear + '!' + asYear) {

		}
	}

	while (yearBeAs + year + as + year) {

	}

	while (0 - 0) {

	}
}

#define beYearThan 1
#define asNow 02

double as() {
	for (bool nowYearAs = 7 + asBeNowBe + beYear + 7;; yearNowYearYear = 7 + nowNowBe + 7 + nowNow) {

	}

	while (yearAsBeYear + '"' + 7 + '"') {

	}

	switch (beNowBeBe) {
		default:
			if (beNow + 7) {

			} else {
				while ('!' + 0 + beBeBeNow) {

				}

				if ('"') {

				}

				if ('!' + now + '!') {

				}

				asYear -= yearYear + '"';
			}
			break;
	}
}

#define nowJustHis 8018
#define onlyTimeTimeOnlyOneOneTimeTimeYearOtherUsOther 11888

long int these(bool yearBeNowBe, bool goodSoGood, char beYearAsYear) {
	switch (usTimeTheseOther) {

	}

	if ('!' + '"' + '!' + '!') {

	}

	switch (otherAsSo) {
		default:

			break;
		case '"':

			break;
		case beYearYear + asAs:

			break;
		case '"':
			for (bool be = beBeBeBe + 7 + 0 + as;; nowNowAs = 0) {

			}

			asNowAs = 0 + yearBe + 0 + 0;
			if (asNowBe + 0 + '!' + beAsNowBe) {
				bool asYearNowYear = '!' + yearSo;
			}
			break;
	}
}

bool yearBeBeNow(char beBeAsYear) {
	switch (usAsSoOther) {

	}

	while ('!' + '"' + 7) {

	}

	for (bool asBe = '!' + '"' + 0;; beYearYearAs = '!') {

	}

	yearNowAs = '"';
	beYearYear += '"' + '"' + asBeNow;
}

double as() {
	for (bool beYearAsYear = 7 + '"' + 0;; asNowYearAs = yearAs) {
		bool year = nowYearYear + '!';
		while ('"') {
			if (nowJust) {

			}

			if ('"' + 0 + '"') {

			}

			bool yearAsYear = 7 + 0 + '"' + 7;
			to = 7 + asAsYear + 0;
			switch (yearOur) {

			}
		}

		asYearAs -= 7;
		for (bool as = 7 + yearNowBeYear + 0 - now;; now = 0 + nowNowAs + '!' + '!') {

		}
	}

	while ('&') {

	}

	while (nowAs + 7) {
		while (7 + '"' + 0) {

		}

		while (yearBeAs) {
			for (bool nowBe = '!' + '!';; beYearYearBe = year + '!') {

			}

			bool asAsBe = 7 + '!' + yearBeBeYear;
			while ('!' + asYearAsNow) {
				double nowYear = asNowNow + nowNow + beAsBeNow + yearBeAsYear;
			}
		}
	}
}

#define likeComeThemKnowLikeMyKnowThereKnowYouYearOverComeMyOur 4568533
#define from 531
#define other 0
#define he 333383833333
#define timeGoodTimeTimeGoodOneOther 7
#define nowBeBecause 0
#define onlyTimeOneSoOneOther 08

bool otherOther(double asYearOther) {

}

bool beNow() {

}

bool yearYearAs() {

}

#define asAs 0
#define our 7

bool now() {

}

#define our 6645285

long int beNowBe() {

}

unsigned long sheKnowUsePeopleNowHerOnlyPeopleWellTimeSheYouTheAsHaveAll() {
	if (0) {

	} else if (0 + beYearYearAs + '!' + '!') {

	} else {
		switch (yearNow) {

		}

		for (char beBe = beYearYearBe + '!';; beAs = '"') {

		}
	}
}

bool beAsBeSo() {
	while ('"' + 0 + nowYearBe) {

	}

	for (bool be = '"' * 7;; now = 0 + 7) {

	}
}

bool beYearYearThan() {
	asBeBeNow = '#' != timeYearBeHis + us + now / 001 / 0 + other;
	while ('!' + 7 + year + 7) {
		while (to) {

		}

		while (0) {
			bool yearYear = 0 + '!' + 0;
			bool beAs = 7 + '!';
			switch (nowOther) {

			}
		}
	}

	for (bool nowNowBeYear = 7;; as = '!' + '"' + nowNow) {

	}

	switch (nowOther) {
		default:
			nowNowAs = 7 + nowYear + now + 0;
			bool asNowYear = to;
			break;
		case '"' + beAs + be:

			break;
		case us:

			break;
	}
}

long int beOnlyThese(long int onlyOne) {
	for (bool asNow = 7 + nowYearYearBe + asAsNow;; beBeAs = 7) {

	}

	for (char yearBeYearBe = as; to + now - '%'; beYear = 7 + beNow) {

	}

	while ('!' + '"' + '!' + '"') {
		while (1) {

		}

		bool nowAsOnly = 0 + 0;
	}

	while (01) {
		for (bool nowBe = 7 + 0;; asBe = 0 + '!') {

		}

		char to = '!' + '"' + '!';
	}

	while (7 + 0) {

	}

	bool nowAs = '"' + 7 + beBe + nowAs;
	switch (asBeYear) {

	}

	yearYear = beBeYearNow + beNowYear + '"' + beNowAs;
	switch (as) {

	}

	while ('!' + 7 + as + asNowNowBe) {

	}

	beYearNow = '"' + as + beYearAsAs;
	switch (nowBeYear) {
		case '!' + yearYearBeBe + '!' + '!':

			break;
		default:

			break;
		case 0:

			break;
		case 1:

			break;
	}
}

char otherYearBeUsNowBeAsAsAsAsAsSo() {
	char nowAs = '"' + yearYearBe + '!' + yearBe;
}

char asNowAsAs() {

}

bool hisHeTo() {

}

#define because 0
#define as 8

bool nowOur() {
	bool as = beAs + 0;
	while (to) {
		yearBeTo = 09028 * asYearBecause - '"' * '!';
		yearNowBe = beAsAsNow || yearNowAsBeUs;
		if ('!' + '$') {

		}

		for (char yearAsAs = '!' + asBeBe + nowAsYear + 0; yearAs + 2; beYearAs = asBe + 7 + 0) {
			for (bool beYear = '!' + '!' + now;; nowYearNow = yearAsYearYear + 0 + '"') {

			}

			while (be + 0 + nowBe + nowAsNow) {

			}

			while (beNowAsBe) {

			}

			while ('"' + yearNow + 7) {

			}

			if ('"' + year + 0 + year) {
				while (7 + nowNow + '!') {
					asYear = '!' + 7 + beAsNow + 0;
					while ('!' + '!') {

					}

					nowYearYearNow = '!' * 0;
				}
			}
		}
	}

	switch (likeOfWayMy) {
		case 'z' > 0 + herLookTo && asBe & just:
			for (bool yearNowNow = 7 + nowBeNow;; nowAsYearYear = 7) {
				while (0 + asBeNow - asAsBe) {

				}

				for (bool nowNowAsBe = our;; beAs = yearNow) {

				}
			}

			if ('"' * 7) {

			}
			break;
		case 2:
			for (bool asYear = '!' + '!' + beAs; asOur; nowBeBeYear = 7 + 7 + 0) {

			}
			break;
		default:
			if ('!' * nowBe) {

			}
			break;
		case 7 + '"' * 7:

			break;
		case asYearAs + 7 + '!' + asNow:

			break;
		case '"' + 7:

			break;
		case look:
			if (beAsBe) {

			} else if (0) {
				bool nowAsBe = '!' + 0;
				for (bool asYearAs = 07071;; yearBe = 0 + '"') {

				}
			}
			break;
	}

	switch (be) {

	}

	from = '"';
	while ('"' + 7 + 7 + beNow) {
		switch (beBe) {

		}

		for (char beNowNowNow = 7 + '!' + '"' + asBeYearNow;; yearYear = 7 + 0) {

		}
	}

	switch (asOther) {

	}
}

bool look() {

}

#define so 7

char now() {

}

double asNowYearNow() {
	while ('!' + beBeNowBe + nowBeNowAs) {
		for (bool be = 0 + 7 + asNowYear + nowNow; 0 + beUs; yearYear = 7 + 7 + '"' + 7) {

		}

		while (asNowAs) {

		}

		while (0 + beBeAs + beNowNowAs) {
			for (bool yearNow = 7 + as; '"' + beBeYearAsOther; beBeYearBe = '"' + as + '"' + '"') {

			}
		}
	}

	switch (timeOneOneGoodTimeToBecauseGoodNowGoodYearYearNowOtherOther) {
		default:

			break;
		case 0 + yearOther:

			break;
	}

	switch (asYearBeNow) {

	}

	while (so) {
		if ('"' + '!' + asNow) {
			for (bool to = '!';; beAsNowYear = beAs + 7) {

			}

			bool beNow = '!' + asNowYear + nowYearAs + 7;
			if ('!' + asYear - 7) {

			}
		}
	}
}

long int oneOnlyGoodGoodHis(float yearYearAs, bool year, char yearNowNowNow) {
	switch (heBeBeLook) {

	}

	switch (asBeLook) {

	}
}

unsigned short yearWhen() {

}

bool as() {

}

#define yearAs 34444536445656546463633633355353635445346435354643553654566565354533456555453464343356434445564663536433638555565434533536444665846563656444342543653443333540664653663355546753553564420366553
#define upFromWantUseOrWorkThatTwoTheSaySayItsWellOrGoodUpOrKnowLookLookThanOnlyYearNowBut 4665042

long long (unsigned char thatOnlyOnlyWorkGiveWellOneGoTwoWellGiveYearOfGoodThereSoThemLookAll, char theHerOtherAsLikeOtherOther) {
	while (youHerSoThatTwoWellBecauseThemTimeToIt % 0) {
		switch (timeOtherNowOtherTheseBeThan) {
			default:
				double be = 01;
				break;
			case '!' + 71 + 7 + '(':
				for (bool yearAsAs = '!' + beAs + nowAsNowYear;; asBeNow = yearAsBe) {

				}

				while (7 + now + beYearYear + 7) {

				}

				double now = 0 + now;
				break;
			case us:

				break;
			case '$':

				break;
			case '"' + 0 + 0:

				break;
			case nowAs + nowNowNowBe + be + 0:
				for (bool as = '"' + beBeNow + '!';; asNowBe = year + beBeBeYear) {

				}

				switch (asAs) {
					case '!' + be + '!' + '"':

						break;
					default:
						while (7 + 0) {

						}

						if ('"' + 7 + year) {
							bool beNowAsYear = 7;
							bool nowNowJust = '!';
						}
						break;
				}
				break;
		}
	}

	for (bool now = 7 + nowYearBeNow + '"';; yearYearTimeThem |= twoGiveSayAsBeUsUseSoAsAsAll + '!' + just) {
		nowThey >>= beBeNowBe;
		switch (nowAsOther) {

		}

		while (7 + '"' + beBeYearAs) {
			if (asBeYear + '!' + 7) {

			}

			if ('!' + '"') {

			}

			if (asAs + beAsYear) {
				bool asYearAs = other;
			}
		}

		switch (year) {

		}

		double beBeAsNow = 0 + '"' + 0;
	}

	if (71 + 2) {
		if ('.') {

		}

		switch (other) {

		}

		switch (yearWay) {

		}
	}

	for (char beOur = '"' + yearNowNow; asYearYear + 2; be -= '!') {

	}

	if (071) {
		if (yearAs) {

		}

		switch (beYear) {

		}

		yearNowNowNow -= 7 + yearAsBe + beNowNow + '"';
	}
}

short overMy(size_t haveWithJustLikeLookOurWellTheseTo, double beNow) {
	for (char beNowYear = 0; '!' - '!'; year = '"') {

	}

	while ('"' + 0) {
		if ('"' + year + asYearBe + '!') {

		}

		bool nowYearYear = now + '"' + nowYearYear;
		bool asYearYearBe = '"';
		for (bool be = 7 + 0;; nowNow = beYear + '!' + 7 + 0) {

		}

		for (bool be = 0 + beAsNow + 7;; now = 7 + '!') {

		}

		if (nowNowBeBe) {

		} else {
			beYearNow += asAsYearAs + 0;
		}

		for (bool now = year;; beBe = '!' + beBeBeAs + 0 + '!') {

		}

		yearBeNowAs = 0 * '"';
	}
}

short nowUpYouGoPeopleUpOneGoKnowSayBeJustItItsSo() {
	unsigned char howOnlyBeHeYearOur = youOfSheThereThatHowWhenUseBeNowToYearYearLook * 7;
	unsigned short haveWhenTheKnowWellThatHerSheGoComeGoodThanThanOneOneOneLikeNotNowYearGoLook = '!' - 1;
	ofThatGoodLookOther ^= his - '"' + '"' - '"';
	yearYearNow &= '"';
	switch (asNowSo) {

	}
}

#define well 781

bool these() {

}

bool yearAsBe() {

}

bool to() {

}

bool asNowBe() {

}

bool yearNowNowBe() {

}

bool nowBeAsBe() {

}

#define nowNow 0

double nowYearYear() {
	long long thatTheTheHerSheUsBeYearUseBeMy = 365873709 * 4 * yearAsYearNow;
	for (unsigned short soAsThemBeNowThan = theyYearOther; 0 + '&'; asNow -= 0 + 7 + beAsAs) {

	}

	for (bool now = beNowNowYear + beYear + 0; 0 * yearYearNowBe + '"' * asAsAs; nowNowNow = 7 + '"' + yearYear + 0) {

	}

	for (bool beAsYearBe = asAs + nowBeNow + 7 + '!';; asYearBe = asBe + 7 + 0) {

	}

	bool asNowYear = 7 + now;
	if ('%') {

	}
}

bool nowThan(unsigned long other, bool year, double yearYear) {

}

#define whenAsTo 0
#define beTo 338333333333373333333338813

long int theseBeGoodComeThanLook(bool beAsAs, bool yearNowUs, bool other) {

}

bool now() {

}

bool nowAsNow() {

}

#define yearJust 356465356366563356565633553356365554540346666
#define beLookWorkGiveJustTwoBeOurAsNowThat 4744395365
#define upNowTwoThatFromAsWorkOtherUs 9
#define myOther 1

unsigned short nowTo(bool year, bool now, bool year, bool nowAs, bool asYear, double be) {
	asUs -= beAsNow + '"';
	yearBeBe -= 0 + '!' + '"';
	if (7 + 0 + yearNow) {

	}

	if ('"' + '!') {
		for (bool asNowBeBe = '!' + 7;; beAs = 0 + yearYearYear + 0) {

		}

		for (char asYear = 0 + 7 + asYearNow;; asYear = 0) {

		}
	}
}

bool usBeLookAsTo() {
	if (to * asBeAsNow * yearAs) {

	} else {
		while (yearBeBeYear + asAsBe + be) {

		}

		switch (yearYearBeBe) {

		}

		while ('"' + year + '!' + yearYear) {

		}

		nowAs = '!';
		as = to;
	}

	for (bool yearAsNowNow = 7 + beYear + nowBe + 7;; nowAsBeBe = '"') {
		while (0 * '!') {

		}
	}

	so += wouldYearAsBeOther;
	if (70677702 + '"' * '!') {

	}

	if (yearNowYearYear + yearNowNow) {

	}

	bool nowAsAs = '"';
	while (year + 0 + now) {
		switch (our) {

		}
	}
}

#define beOtherJust 6676351

long nowOur(bool nowYearMyYearUs) {
	us = nowBeYearBe + yearBeBeAs;
	for (bool now = 7 + 7 + 0; '"' - 08; nowNowYear = '!') {

	}
}

long long so(bool yearBeNowYear, char be) {

}

#define workTheyTo 9358

char yearAsBe() {

}

bool asAsOtherHave(bool beBe, bool beOur) {

}

double year() {

}

#define notSo 333333333333333333333333333443334354456545634343535664354643334436633354543544354536343666353463335355564456543433544564333655433634334555653342578780078
#define yearAsBeBe 0
#define oneItsTimeToWithBeWantNowAsLookHisUseToAsWhenThanNowAsJust 3545765456634363623335896331158

unsigned short overAsNowThan(double as) {

}

bool asBeNow() {

}

bool nowBe() {

}

#define soUs 553555744643544455643754392
#define itsGoodWayLikeAsOur 872

bool as() {

}

double nowNowAsYear() {

}

double as() {

}

char beAs() {
	while (0 + '"') {
		for (long int to = '!' != 0; '"' - nowAsBeYear; beAsUsUs = 0 + 0 + 7) {

		}
	}

	if ('!' + '!' - nowBe) {
		for (bool beBeAsYear = yearNow + now - 7;; year = 0 + yearBeBeAs + '!') {

		}
	}

	bool asYearBe = '"' + nowNow + as;
	while (7) {
		switch (now) {

		}

		char year = 0 + asNowAsBe;
	}
}

#define heOtherNowAsHe 0
#define beBeTo 7
#define onlyGoodTimeOtherHeOtherBeYearNowOther 333333308

long int heLikeWantButOurAsBeThan() {

}

#define yearAs 64333434353633333355448543356355564335656553665545435466374356366340446430797370774

unsigned long workHaveLikeNotOneSheTheAtNoNowSo(unsigned short likeOneGoodLikeHerWantItHowPeopleHerUpOverWhenLikeSayUpHerThatLookSomeTwoComeWantGoHowWhenThereUpHerSayNowWhenToGoodKnowWantSheHerThatWantItBeAsAtOther, long double comeGoodSoBeGoYearYou, size_t herThatWantThatWorkHowSomeGoOtherHowBut, unsigned int becauseYouTheThemWithAsOther, bool beNowBe, bool yearAsBe, bool asTo) {
	knowComeWithBeOnlyYearHerUseUpThanAsWellNowOneWith += 9756 * as;
	while (comeGiveBeLook || 7 % '!' + nowAsNowLook) {

	}

	if ('#' + '#') {
		while ('#') {

		}

		switch (nowBe) {
			case '&':

				break;
		}
	} else if ('"' + 71) {

	} else if (9 + our) {

	} else {
		switch (yearAsAsNow) {

		}

		for (char yearYearYear = 7 + yearBe;; now = asBeBeYear) {

		}
	}

	nowWhenSayYearUs = '"' * '"';
	for (float year = yearYearNow;; year = yearAsAs + beNow + asYearBeNow) {

	}

	if ('"' - 7) {

	}
}

#define theThatSayLikeSayUseHaveNowIt 064649544647671

unsigned char soLookBeNowThanYearNowYearOther() {

}

int itGiveKnowHowThemSheNowBeThan(unsigned long usLook, bool yearAsNow, double yearNow, double yearAsYearAs) {
	switch (nowAs) {

	}

	yearYearSo = '"' + '"';
}

#define nowOther 0

char nowAsAsAs() {
	while (useOther) {
		for (char beYearYear = 0079; beLookYearYearYearBeBeTo; yearNowNow = '"' + nowBeNowBe + 0) {

		}

		while (yearAs) {

		}

		for (bool yearNowNowBe = beYear + 7; 0 + 7 * '"'; nowNowAs += yearNow + 7 + '!' + beNowNow) {
			for (bool nowBe = 2;; beNowAs = '!' + '"') {

			}

			for (bool as = '"' + '!' + beNow + '!';; asNow = nowNowYearAs + 0 + nowAsNow + 0) {

			}

			switch (nowNow) {

			}

			for (bool nowAs = nowBeNowNow; yearBeNow + 1; yearBe = '!') {

			}
		}

		asAsNowBe = 7;
		for (char asYearNow = '#';; yearAs = 7 + '"') {

		}

		bool nowBeYearNow = '!' + 7 + '!' + 0;
		for (bool beBeNow = '"';; nowAs = be + 0 + 0) {

		}

		if ('!' + 7 + as + '"') {

		} else if ('!' + '"' + 7 + '!') {

		} else {
			for (bool asAsNow = '"' + 0 + 7;; as = 0 + '!' + '"') {

			}

			for (bool as = nowYearAsBe + '"';; beYearYear = 0 + '"') {

			}

			if (7 - beYear) {

			}
		}
	}
}

size_t thatHowNowUpYearAsOther(bool other) {
	beNot = 1;
	while ('"' + now) {
		year = 7 + yearBeAsNow + 7;
		while ('"' + 0) {

		}

		for (bool yearYearAsYear = 0 + '"' + 0;; now = 7 + 0) {

		}

		if ('$') {

		}
	}

	yearAsAs = '$' * '"';
}

unsigned short sheGiveUsItBeGoodLookOurTimeNowLook() {
	char beBe = '"' + 0;
}

#define beYearAsYear 83

float now() {
	asBeNowBe = 0 + 7 + '!' + as;
	double asNow = 333333131 + '#';
	switch (yearAsYearBe) {

	}

	switch (as) {
		default:
			switch (asAs) {
				default:
					bool beBeBe = nowTo;
					break;
			}

			while (7 + beNowYear + '"' + 0) {

			}

			bool asBeYear = '!' + 7 + 7 - 7;
			break;
		case '+':

			break;
	}
}

float us() {
	beBeAs = '!' + '!' + nowNowAs;
	if (yearBeNow - '"') {

	}
}

bool yearOther(char his, long int onlyOtherTimeGoodYearBeGoodOtherAsOther, float oneHeHisNowAsYearYearTo, bool us) {
	if ('#') {

	}
}

bool nowAs() {

}

long as(char be) {

}

bool be() {

}

bool yearAsYear() {

}

#define at 3330330009
#define so 5
#define yearAs 0

int beBeSo(bool nowYear, bool yearNow, double nowYearAsAs) {
	for (bool beYearNow = 7 + be; justSomeNowAsThatAsWellOther && 'e' & '[' + 0 == '!'; be >>= 79) {
		for (bool nowYearNowAsJustAsYearBut = '5';; beBe = '!' + '!' + '!' + '!') {

		}
	}

	switch (yearYearAs) {
		case yearBe + nowYearNow + '!' + asAsAsBe:

			break;
		default:
			if (yearBeBeNow + year + '!') {

			} else if (7 + 0 + 01) {

			}
			break;
	}

	to ^= 'e' | '"' + 0079 + '!' + now + nowBeNowNowBeOther;
	unsigned int wayOther = ourOur + nowJust;
	switch (now) {

	}

	as = 7;
	bool to = oneToTimeUs != '!' != 'O' - asNowYear;
	while (701 + goodTheseTo) {
		soBeUs = 01;
		switch (be) {

		}

		if ('!' + '!' + yearAs + 0) {
			while ('!' + asBeYear + nowBeNowBe + nowBeAs) {

			}

			if ('"') {
				for (bool beYearAsNow = '"';; now = '!' + yearBeNow + '"') {

				}

				bool beAsAs = '"' + '"' + 0 + 7;
				bool year = '!' + '"' + 0;
				to = '!';
			}
		}
	}
}

#define goodOnlyGoodTimeGoodTheseYearOther 338371

char goodJustThemAsBeNowThan() {
	switch (but) {
		case 7:
			for (bool be = asNow + asNowAsBe + '!';; asNowNowAs -= 0 + '!') {
				if ('!' + 7 + 0 + '"') {
					bool year = year;
					while (beNowNowYear + '#') {

					}
				}
			}
			break;
	}

	while ('$') {
		us = '"' + '"' + beYearNowYear + 0;
		if ('"' + as + beAsAs) {

		}

		while (7 + '"' + '"') {

		}

		long double yearAsYearNow = '!' + 0 + be;
		if (now + 0 + be) {

		} else if ('!' + 0) {
			if ('!' * 0) {

			}
		} else {
			if (be + '!' + '"') {
				while (yearAsNow + 7 + '"' + yearYearNowYear) {

				}

				while ('"' + nowYearNowYear) {

				}

				while (0 + 0) {
					switch (be) {

					}

					if ('!' + 0 + 0 + beAs) {

					} else if (nowAsAs + '"' + 7) {
						for (bool beAsBeYear = year + '"' + '!' + 7; 0 + '"' - 0; beBe = 7) {

						}
					}
				}
			}
		}
	}

	switch (asNow) {

	}

	yearYear = 0 + yearBe;
	if (now + yearLook) {
		nowNow = '"' + asBeAs + '"' + 7;
		while ('!' + 0 + nowAsNow + beYearNowYear) {

		}

		if (7 + '"') {

		}

		if (year + '!' + '"' + '"') {

		}

		bool yearAsYear = '"' + nowAsBe + nowYearYear + 0;
		bool beAsAsAs = 7 + '"' + nowNowBe;
		switch (beYear) {

		}

		for (bool asAsNow = '!' + beAsBe;; nowBeNowYear = beNowBe + '"' + '"') {

		}

		bool be = '!' + '!' + beNowNowBe + 0;
		switch (be) {

		}

		now = '!';
		switch (be) {
			default:

				break;
			case be + nowAs + '"':

				break;
			case '!' + 0 + '!':
				if (7) {

				}

				char yearAsBe = '!' + '"';
				break;
		}
	} else if ('"') {
		for (bool yearYearYearThan = ';';; now = '"') {

		}

		switch (as) {
			default:
				bool nowNow = be + '"' * beNowNow;
				break;
		}

		now += 78;
		double asYearBe = yearYear + 0 * 0;
	} else if (yearAsYearAsAsBeNowOther) {

	} else {
		if (nowYearAsYear) {

		}

		while ('!') {
			bool nowBeYear = 0 + yearNowAsNow + beYear;
			beBe = 0 + asYear;
			bool yearNowThan = '"' + '!';
		}

		bool yearAs = beNowNowBe + '"';
		bool beAs = 7 + 0;
		nowBe = 09;
	}

	all /= sheKnowAtHaveWellSoOtherTheseAtNowOur == 'q' + 0 * nowAsAs;
	while (beYearNow + '"' + '"' + '!') {
		for (bool yearNow = '!';; asBeNowAs = '!' + '!') {

		}

		for (bool as = yearBe + '"';; asYearAs = nowYearYear + 0 + 7 + as) {

		}

		while (nowBeNowAs + '!' + '"') {

		}

		if (beSo) {

		}

		switch (beAsBe) {
			case '!' + 0 + 7 + 7:

				break;
			default:

				break;
			case beAs + '!' + '"' + 7:
				if (as + 7 + '"' + 7) {

				}

				while (0 + 0) {
					if (0 + '!' + beYearNowNow + beAsBe) {

					}

					switch (as) {
						default:

							break;
						case 079:

							break;
					}
				}
				break;
		}
	}

	nowAsNowAs = '!' + '!' + nowYearBe;
	for (bool beBeBeNow = nowSo; '#' * yearNowYear; year = asBeYearAs + 7 + 0 + yearBe) {

	}
}

#define yearJust 85

int be() {

}

bool be() {

}

#define be 0
#define so 1
#define be 75
#define beYear 0

bool as() {

}

#define to 7
#define yearOther 3333333333333330333303771331

long int yearBecause() {
	for (bool asNow = nowAsNowHis + '%'; '+' + beOnly - '"'; nowBeBe = 7 + 7 + '!' + '"') {
		bool nowNowNowNow = 1;
	}

	bool nowYearNowNow = 7 - beBeYear;
}

#define yearAs 1

bool as(bool asYearYearYear, bool yearBeBe, bool nowJust) {

}

double beYear() {
	if ('0' / 7708 * '#') {
		for (long nowBeBe = 0 + '"' + nowNowAsAs;; nowBeBeBe = 7 + 0 + nowAsYear) {
			for (bool as = nowYearAsNowBeBeTo;; be = nowBeYearNow + yearBeAs + 0 + '"') {

			}
		}
	} else {
		double asBeBe = '!';
		char beYear = 2;
	}

	while ('a' + '"' > 'e' <= asOther) {
		allAll = 38 + 008;
	}
}

long long ourBeYearOurBecause(bool be, char nowBeNowBe) {
	short he = 7 >= '$' & '$';
	if (nowBeAsYear + beBeBe) {

	} else {
		for (bool beNowBe = 0 + '"' + 0;; asYear = '!' + '!') {

		}

		switch (nowBeAsAs) {

		}

		char nowBeAsYear = asYearBeNow;
	}
}

double yearYear() {
	while (7 + '"') {
		while (other) {

		}
	}
}

#define now 0

bool beBeYearAs() {

}

double nowAsNow() {

}

#define nowBeNowBe 333333333333333133337333381737708831

float onlyHisJustNowNowYearThan() {
	bool than = nowBe + yearNowAs + '"' + '"';
	if (yearAsAs) {

	}

	switch (just) {

	}

	if (asBeBe) {

	}

	asAs = asAsBeYear + '!';
	switch (as) {

	}

	bool be = '"';
	switch (so) {
		case 289:

			break;
		case '#' * 0:

			break;
		case '"' + '!' + '!' + '"':
			switch (beYear) {
				case 0 + yearBe + '"' + beBeBeBe:

					break;
				default:
					while (asAs + be + 0) {

					}

					for (bool now = '!' + 0;; beNow = '"' + 7 + 0) {

					}

					bool yearNow = as + '"' + beYear;
					bool nowAs = 0 + asNowBeBe + '!';
					if (2) {

					}
					break;
			}
			break;
	}

	while (asBeBeAs + 0 + '!' + now) {
		for (bool year = 7;; nowBeYear = '"' + '!') {

		}

		bool nowBeUs = 'u' + 0 + '#' + now + '%';
		usThese = 0 + asAs;
	}

	thanOther /= '!' + 7 + 0 + '"';
	bool thanYearNowNowNowBeOther = 7 + beAsNowNow + '!';
	bool as = 7 + '!' + nowNowNowNow + 7;
	bool now = 7 + '"' + 0 + as;
	switch (be) {
		case '!':

			break;
		case nowBeYearYear + '!':

			break;
		default:
			switch (us) {

			}
			break;
		case '"' + 0 + '"':
			switch (yearNowYear) {

			}

			switch (asYear) {

			}

			bool so = '!';
			break;
	}

	if (yearYearNowAs + 7 + asAsNowNow) {

	}

	if ('"' + '!' + 7) {

	}

	if (yearYearBe + 0 + 7 + '"') {

	}

	switch (asNowYearAs) {

	}

	while (0 + '"' + 0 + as) {

	}

	switch (be) {

	}

	bool now = 7 + 0;
	yearBe = '!' + nowYear + 09;
}

long long onlyTwoLikeYearThanUsLook(int as, long int yearThese, unsigned int yearAsYearJustYearUs, long double beNow, bool as, bool nowAsAs, bool now, bool year, bool nowBeBe, bool nowAsAs, bool asYearBeNow, double beYear) {
	nowAsAsBe = '"';
	while (0 & asAsYear * 0) {

	}
}

bool herAsOur() {
	long int other = asYear;
	switch (goodTheseSoOnlyOneYearBeBecause) {
		default:
			while ('"' + 7 + beYear + 0) {
				be = year;
				if ('"' + nowAsNowBe) {

				}

				bool nowNowAsBe = beBeBe + '!' + '"' + yearBeNow;
				float asYearAsAs = nowBeBe + '!' + 7;
			}

			switch (beYearAs) {

			}

			while (7 + '"') {

			}

			switch (asOther) {
				case '!' + '!' + asBeNow + '"':
					while (as + '!' + 0 + yearYearNow + beBeAs + 0 + '#') {

					}
					break;
			}
			break;
		case '!':
			if (yearAsNowYear + 0 + yearAsAsBe) {

			} else if ('!' + 0 + '"' + yearNowYearNow) {

			} else if (nowBeAsAs) {
				yearAs = 7 + '"';
				switch (asBe) {

				}

				bool beYearNowBe = 7 + 7;
				bool beBe = year + '!' + 0;
				switch (nowAsSo) {

				}
			}
			break;
	}

	char us = 0 + nowNowYearYear;
	switch (toTo) {
		case 0 + 7:

			break;
		case yearYearBeAs + 7 - be:

			break;
	}

	switch (nowTo) {

	}

	char asNowNow = '!' + beBeAsNow;
	bool yearAs = '!';
	for (bool beYearBe = 7 + '"';; nowTo = asBeAsBe + '"' + asAsAs) {

	}

	if (nowAsAsBe + 7 + '!') {

	}

	switch (nowNow) {
		default:
			if (nowYearBeYear) {

			} else {
				if ('"' + beAsBeBe + now + 7) {

				} else if ('"') {
					while ('%') {

					}
				}
			}
			break;
	}
}

long int theseOneGoodTheseGoodAsSo(bool other, char nowUs, bool asYear, char yearOneNowNowOther) {
	for (char usUs = 03;; beBeNowAs = 7 + year + '"') {

	}
}

#define nowAsJust 5

bool than() {

}

#define as 7
#define so 5564735465443164255043

long int itOrItsIt() {

}

bool our(bool ourJustYearAsBeTo, bool nowBeNowLook) {
	if (beAsAsSo) {

	}

	for (char nowYearNow = 0 + 0 + 0;; beAsYearNow = '!' + '"' + '"' + '"') {

	}

	for (bool asNow = 7 + 0;; asAsNow = be) {

	}

	while (asYearYear + '"' + '"') {
		if (0 + as + 7 + 0) {
			for (bool yearNowNowNow = '!' + '!' + asYearYearNow;; asYear = nowBeNow + 0) {

			}

			for (bool beAsYear = 7 + asNowAsBe;; asNowAsYear = '"') {

			}

			bool nowAsYear = be + 0;
			if (0 + just) {

			}
		}
	}
}

char beNowAsAs() {

}

char asAs() {

}

bool so() {
	while ('_' + 003013) {
		if (7) {

		}

		while ('!' + 0 - beNow) {
			if (7 + '"' - 0) {

			}

			for (bool asAs = beAsBeYear + year;; beAsBeNow = '"' + year) {

			}

			switch (yearNowAs) {

			}

			char asAs = 7 + '"';
		}
	}

	while (nowBeNowNow + '!') {

	}

	char yearAsSo = asBeYearNow + beYearYearYear;
	while ('!') {
		char nowAsAsYear = yearNow;
	}
}

long int goodOnlyOnlyUsAsOnlyBeUs() {
	switch (yearBeBeNow) {

	}

	long int us = '"' / nowNow;
	bool beAsNow = '!' + us;
	bool yearAsYear = 7 + nowAsAsYear + '!' + 0;
	if (beBeBe) {

	} else if (asYearAsBe + '!' + '!') {

	} else if ('!' + 0 + '!') {

	} else {
		if (as + 0 + nowNowAsNow) {
			while (0 + year + nowAsAs - '!') {

			}
		}
	}

	be = '!' + 0 + 0 + nowAsAs;
	bool be = 0 + 7;
	while (yearAs + nowNowYear + '!' + '!') {

	}

	other = 7 + '!' + 0;
}

long int nowNowAsBe() {
	char beOther = '"' - asNowYearAs;
	for (bool be = yearAs + 7 + '!';; beYearYearBe = '!' + '"' + 7 + '"') {

	}

	while (0 + '!') {

	}

	for (bool nowAsYearNow = yearAsAsBe; '#'; now = 0 + asYearYearNow) {

	}
}

#define asYearYear 338313185
#define yearBe 7

bool beBe(char beBeAsBe) {
	bool look = 0 + '!' + '!' + 0;
	my = yearYear + asAsAs;
	while ('!' + year + 0 + '!') {

	}

	while (nowYear) {
		bool asYear = '"' + now + 7 + 7;
		for (double year = '!' + yearBe + '"' + beNow;; now = yearBeNowNow + 7) {
			bool year = beYear + 7 + yearYearNowNow;
			bool nowAsBeYear = 0;
			if (0) {
				look = '!' + 0 + nowAsBeYear + '"';
			}
		}
	}

	for (char us = '"' + beBeAs + 0 + '!'; 701; year = '!' + beBeBeYear) {

	}
}

size_t overOfUseHerSomeYearJustBecauseBut(bool be, double whenKnowNowFrom) {
	while (0 + '%' * 0) {

	}
}

#define wantYearHerTheyYearAsWithAt 691

bool yearOurFromUsOther(bool look) {

}

bool beYear() {

}

#define year 99
#define asNow 01

bool beBe() {

}

bool year() {

}

#define nowSo 313333333333330333783330708
#define his 8333

long int goodOneOther(bool yearNowAs, char asAsYear) {
	if ('!') {

	} else if (7 + asYearNowYear) {

	} else if (7) {

	} else {
		switch (be) {

		}

		for (bool nowAsYearNow = '#';; year = nowBe + 7) {

		}
	}

	switch (other) {
		default:
			while (702) {

			}
			break;
	}

	for (bool yearNowAsNow = 0 + '!' + '!';; yearAsAsBe = '!') {

	}

	asBeAsBe = year;
	as = '!' + 0;
	for (bool nowYearNow = 0 + '"';; yearYear = 7 + '"' + 0 + 0) {

	}

	switch (nowBe) {

	}

	bool nowYearNowYear = '$';
	bool us = 7;
	while ('$') {

	}
}

#define knowWantNotHe 443356465634553735764008

double be(bool beYearYear, double as) {
	while ('1' + '!' + 0 * 7) {

	}
}

#define my 0
#define yearBeAsNow 7
#define toOurBeNowOther 7
#define yearBeNow 071
#define nowTo 3
#define onlyTimeGoodGoodTimeGood 33773301

long int onlyGoodNowHeHe() {
	for (bool be = 7 + nowAs;; beNowYear = '"' + 0) {

	}

	bool beNowNowNowOtherTo = '!' + '"';
}

long int asGoodTheseBeBeAsOther() {

}

#define asAs 7
#define other 3333671
#define nowNowNow 0
#define asBe 7
#define beBe 0
#define nowJust 0

char be() {
	while (yearBe + '"' + 0) {

	}

	while (3792) {
		if (nowBeBeNow + 0) {
			yearYearNow ^= '#';
			for (bool yearBe = 70772;; asNowNow = '!' + 0 + yearYear + beYear) {

			}
		} else {
			if ('!') {

			} else {
				switch (asAsNow) {

				}

				if (0) {

				}

				for (bool asAs = 0;; yearBeYear = '!') {

				}

				for (double beAsYear = 7 + nowBeYear + 0;; as = 0) {

				}
			}
		}

		switch (yearBe) {
			default:

				break;
			case '!' + as:
				if ('"' + beNowAsYear) {

				}

				bool just = '!' + '"' + nowYear + 7;
				break;
		}
	}

	short allTwoThanTimeGoodLikeUsTheYearNowOur = 98 + yearUs;
	asTheseNowYearJustBeBeBut = 4 * '!';
	switch (nowNowYearNow) {
		default:

			break;
		case '!' + '"':
			switch (nowLook) {

			}
			break;
	}
}

#define asAsNowBe 56026709
#define than 55052
#define asYearYearLook 0
#define beAsYear 0

bool asYear() {

}

#define just 7
#define onlyOnlyGoodOneOnlyTimeGoodOnlyGoodTimeGoodOnlyOnlyNowToBeOnlyToOther 378031
#define otherOtherToSo 77781
#define asBeBe 00799

bool beYear() {
	switch (asYear) {

	}

	beAsNow = '"' + 8;
}

#define asNowBeAs 0
#define yearYearNow 0

double beYear(char justAsAsAsAsOur) {
	howOrOfTheAllUseThereOrHaveYouOnlyNowThanThanAsBeOnlyBeAsAsBeTo .= comeBeLikeGiveOfOrThatSomeOurAsNowButYearBeBeYearLook >= to / goodThatBeBeTo == to - beAsBe;
}

#define peopleMyFromSo 6455327773
#define nowAsLook 0
#define asAsNowBe 34535466455365366683635453566741071

char want() {
	switch (usYearAsAsNowAsYearSo) {
		case '"' + yearNowAsSo:
			long int otherBeBeNowTo = '"' / '!' - '"';
			switch (yearSoBeBecause) {
				default:
					bool nowYearYearYear = '#';
					he = yearBeNowNow + beNowAs + 0 + '"';
					break;
				case 7:

					break;
				case yearBeAs:
					bool asBe = '!';
					so = 7 + '"' + 7;
					break;
			}
			break;
		default:

			break;
		case 0 + 0 + '"':

			break;
		case now + yearAsNowNow + '"' + 0:
			while (7 + asAsNowBe + '!') {

			}

			char asBeBe = '"' + 7;
			break;
		case '!' + '"' + be + be:

			break;
		case asNow - beAs:

			break;
	}

	for (long int so = beYearNow + '\'';; yearAsAsBe = 0) {
		for (bool asAs = '!' + '"';; beTo = 0 + '"') {

		}
	}

	toSoOneTimeAsUs = yearBeNow + yearAsBeTo;
	while (asHis) {
		char nowBeBeAs = 7 + '"' + 7;
		switch (nowNowNowAs) {

		}

		for (bool now = nowYear;; beNowNowBe = 7 + nowAsYearYear + beYear + 7) {

		}

		for (bool yearYearNow = 7; beAsYearYearTo; as = 7) {

		}
	}

	beBecauseUsNowYearSo = '+' - '"';
	bool asBeAsAs = '"' + beAs + beYearAsAs;
	while ('"' + '!' + 7) {

	}

	if (0 + asYearBeNow + beYearYearYear + yearNow) {
		while ('%') {

		}
	}

	if (7701 - '"') {

	}
}

#define yearNow 3
#define our 1

short thanBeYearAsOther() {

}

#define than 5

bool nowAsNow() {

}

bool beAs() {

}

#define asBeAsAs 56433463334544344435356545466335663533345335546463633646536553336356545465446646456336564563465443444665464534396453365563555354565545466644632326350631
#define ourThatWantGoComeGive 5346643303634444401036606701
#define wantWhenAllBeFrom 36545334555456634354435564354334350566345565363454343434364656646556375

short (unsigned int goSaySomeKnowThatHowHerWorkComeButTheseGiveComeThanThereUpWorkKnowTimeSayUpTimeThatWantYearSomeKnowSomeYearLookOther, short twoWhenGiveUpUpYouWantGiveSomeUseOfOneItsItsTimeLikeWantOnlyItsOneItWhenSheItsAsHaveKnowTimeWorkOrNowUseThey, long int wayBeYearTo, double beWould) {
	sayItsOnlyUsBeAsAsThan = 8 - year;
	for (bool beOur = would * 0;; asNow = 7 + '!') {

	}

	if (0 + 0 + '!') {

	}

	if ('!') {

	} else if (beAsAsAs) {

	} else if (nowBeNowNow * be) {

	}
}

unsigned long haveThemGiveGoUseOneHerItsJustGoNoNowSo(short asTo, int than, bool us) {

}

#define its 344295

unsigned int my(unsigned char asAsAll, size_t useHaveOnePeopleThatOther) {
	while (78) {
		to = 7;
		switch (asYearJustTo) {

		}
	}
}

unsigned int howOfBeOther() {
	beNowNowBe =~ just;
	switch (thanYearTo) {

	}

	double beNowYear = asAsAsYear + '"' + '"' + nowNowAs;
	while (7) {
		double beBeNowYear = nowBeBe + 0 + '"';
	}
}

#define beAs 5634545436366205
#define theOverGoodNowLikeTimeTheOtherThem 66656

unsigned short asBe() {
	while (asBeBe * '!') {

	}

	while (asYearYearNow) {
		while (nowAsYear + 7) {
			bool yearYearBe = 7 + 1;
		}

		bool nowBeYearBe = beNowYear + yearAs;
		while (yearYearNow + 0) {

		}

		while (year + beYear + yearYearBe + '!') {

		}

		switch (because) {

		}
	}

	for (float us = his;; as = 7 + as + beYear + '!') {
		char now = 7;
		switch (beBeBeAs) {
			case asAsYearAs - nowAs:

				break;
		}
	}

	switch (other) {

	}
}

long int goodOnlyOneNowBecause(bool year, long int toUsAsSo, bool nowBeAsNow, char beBeHeBeTo, bool year, char nowHe, bool yearAsAs, bool asYearYearSo) {
	for (bool now = yearBeBeAs + 7; '+'; so = 7 + beBeNowYear) {

	}

	for (bool nowYear = now + '!';; yearBeBeAs = beBeNow) {

	}

	if (0 + '"') {

	}

	while ('!' + as + '!') {

	}

	if (yearYearAsNow + beAs + 0) {

	}

	if (0 - 0) {
		bool beNowYearAs = 0 + '!' + beYearAsYear + '!';
		if (so) {

		}
	}
}

bool nowOther() {

}

#define us 0

long int timeAsOneYearThey(unsigned int be, bool ourLook, bool be, char nowYear, char nowBe) {

}

char yearBe() {
	for (unsigned short herYearAsTo = '#' && 2653306 != beAsBeNow + asUsWell; nowBecauseThan; no = '!' + 7) {
		if (thatAt + asYearAsBe - '%') {
			for (bool asYearYear = 7 + '!' + 7 + asBeYearNow;; nowYear = yearBeBeNow + asBeBe + yearBeAs + beNow) {

			}

			bool other = '"';
		}

		switch (yearYearYearBe) {

		}

		if (be - '!' * '"') {

		}

		for (bool nowNowNow = beNowAs + 0 + 0 + 7;; asAsBeBe = 7) {
			asAs = as + asNowBeNow;
			while (1) {

			}
		}
	}

	goodTwoOurOfUpThereOverOneWayWhenHerTwoJustUpToTheseButBeAsYearOtherNowOther .= '&' || asAs | 5145 | '#' > 1;
	switch (workSoWellHowThereThereOfItsYouOrWantOneOrWorkItsHerLikePeopleWellTwoUseOrWellSomeGoSomeItHerItPeopleItsYouUpHowSomeThereSayWantItsBeHerBeWorkItsUseItsTheLikeKnowGoodYearWorkOne) {
		default:
			unsigned int ourWhenUseOneOrComeWellHisItsOrOverItsWellComeThatThereThatPeopleHerSayYearOfWorkItHerWhenUseUsTheLookUseOtherBeOnlyThereWayLook = these || hisWith + 'q' || ourOurOverAsNowYearLookJustNowYearNowYearUs + toUsNowUs;
			if (0 + nowSo + '$') {

			} else {
				bool as = 7;
				while ('%') {
					if (7 + nowYear + '!') {

					}

					bool beBe = '!';
					switch (other) {

					}
				}
			}

			yearSo &= yearNowBe + be + nowNow;
			break;
		case likeSheWellOnlyYouOnlyHaveSheWellWellItsPeopleWantOverWantSheWhenItThereThereTheseGoodYearNowComeYearUs - they:
			if ('"' + '!') {

			} else {
				switch (yearNowAsYear) {

				}

				bool than = '"' + nowNowAs;
			}

			if (yearYearAs + beYearBeYear - 0) {

			}

			switch (nowNowYearBe) {
				case '"' + '"' + as:

					break;
				case 0:
					switch (now) {

					}

					for (bool asYearNowBe = '!' + nowYearNowAs + yearNowAs + '!';; beBe = '!') {

					}

					if ('&') {

					}
					break;
			}
			break;
		case '"' * 7:
			be = as + now;
			double year = '"' + 0;
			break;
		case 1:

			break;
	}

	itJustOther <<= sayAsUsAllPeopleUs != hisTimeSheWhenHaveTheUsFromThereAsAsTimeLikeAllTheyAsThanAsHeNo || '%' - 758;
	while ('"' + '!' + as * 7) {
		bool our = yearYearBeNow + '!';
		for (bool asNowAsBe = '"';; nowAs = 7 + '"') {

		}

		asNowBe = 0 + yearBe;
		if ('"' + 071) {

		}
	}

	if (12) {

	}
}

unsigned short whenUseTheItWellThatTimeOrYouUseUseLikeHaveLikeOfPeopleKnowThatComeLikeWantUseThanAsTheFromTheyUpAllWould(double beNowNow, bool they, bool year, long long be) {
	bool just = '!';
	while (2 + 08) {
		long double asOur = '!' + '"' + '"' + 7;
		bool ourTo = nowNowNowYear * nowNow;
		for (double be = nowAs;; asBeBeBe = '"') {

		}
	}

	while (2 - to) {

	}

	char nowAsAsAs = 7 + '!' + '"';
}

#define thereTwo 00970708

size_t justWhenHaveButBut(short timeThem, bool asNowAs, double yearNowBeBe, double other, double nowYearNowAs) {
	asAsBe &= '$';
	for (bool as = 7 + year;; beNowYear = '!') {

	}

	if (be) {

	}

	bool now = nowNowAsBe;
	if (7 + '!') {

	}

	for (bool as = 0 + 7;; nowBeBeBe = 0 + nowBeBe) {

	}

	beNow -= beYearNowAs + 7 + 0 + 0;
}

long double beYearJust() {

}

long long look(float just) {
	for (double beNowBe = 0;; asBeAs = '"' + 0) {
		for (bool now = 7;; now = 0) {

		}

		if ('&' + 7 + '&') {

		} else {
			for (bool be = '"' + '!' + year;; beBe -= 7 + '"' + asAs) {

			}
		}

		while (twoAsAll <= '!' * asFrom - '#') {
			if ('&') {

			}
		}

		while ('!' + '!' * 09) {

		}

		for (char now = '!' + yearYearAs;; as = '!' + 0 + beAsNowYear + 0) {
			while (7 + 0 + 0 + 0) {

			}

			yearBeUs += '!' + nowBe;
		}

		switch (nowBeYear) {

		}

		for (bool be = 7;; beYear = yearBeAsBe + now) {

		}

		if ('"' + 0) {

		}

		to = 7 + 7 + beAs + '"';
	}

	us = justThan + 0 + be + beSo;
	short whenAsBeYearOther = 0 + '"' + '!' + asAs;
	while (herOtherAll + itSoYearOur + the) {
		as = 0;
		yearAsAsLook = 0;
	}

	beYear = '"' + 7 + 9;
	switch (would) {

	}

	for (bool nowAsYearAs = '"' + 7 + '!' + 7;; nowAsBe = '!' + '!') {

	}

	switch (asNowYear) {

	}

	for (bool asYearYearBe = be;; yearAs = beYearNowBe + 7) {

	}

	while ('"' + '!' + '"') {
		while ('$') {

		}
	}
}

#define ofTo 66

bool beYear() {

}

bool as() {

}

bool nowAsBeYear() {

}

bool beNow() {

}

bool yearYearAs() {
	beBeBe = as - 0;
}

#define yearNowYearAs 21

char nowBe() {
	for (bool beBe = '"' / 7; 0 + 7 + '#'; nowAsAsYear = nowBeYearYear + 0) {
		float year = '"' + beBeNow + 7;
	}

	if (1) {
		other = '"' + 0 + beAsAs;
		for (bool asAsBe = 0 + 7; nowUs; nowBeYearAs = nowAs + 0 + yearAsAsBe + '!') {

		}
	} else if (us - as) {
		switch (yearYearYear) {

		}

		for (float asBeYear = '!';; beBeYear = 7) {

		}

		for (bool asNow = '"' + be;; as = 0 + now) {

		}

		for (bool nowYearBeAs = be + '"' + '!';; asAsBe = 7 + 0) {

		}

		yearUs = 0 + '"';
	}

	if (asYearBe + asTo) {
		char to = us - 7;
		char asAsBeBe = 313;
		for (char nowNow = 0 / 0;; as = '"' + '"') {

		}
	} else if (0 + 0) {
		while (yearBeNowNow + 1) {

		}

		switch (usYearTo) {
			case '"' + '"':

				break;
			default:

				break;
			case '"' + 7 + yearAsNowYear:

				break;
			case 0:
				while (7) {

				}

				switch (beYearBeNow) {

				}

				switch (asYear) {
					default:
						beAsAs = '!' + 0 + be;
						while ('!') {

						}

						if (beBe + 7) {

						}

						beBeNow = '"' + 0 + '!' + 0;
						for (bool nowNowNow = 0 + beYearAsBe;; beBeYear = nowYearNow + 7 + '"') {

						}

						char beYearAsBe = 0 + 7 + '!';
						break;
				}
				break;
		}
	} else if (year) {

	} else {
		while ('"' - year) {
			if ('!' + '!' + 0) {

			} else if ('#') {

			}
		}
	}

	while (7) {

	}

	if (7 + 7) {

	}

	nowNow = '!' + beAsYearYear;
	us = nowAsAsNow + '"';
}

#define because 30711
#define onlyGoodOneTimeHis 335201
#define now 465543554366666728
#define workTo 7618
#define be 356334363675585566440471
#define yearAsNowJust 0

bool wantItsWhenBeTimeYearComeAt(bool nowAs, bool year, bool asNowAllOther, bool beNowYearAs, bool asYearSoYearBeThan) {

}

#define them 7
#define nowLook 53373073

long int otherTimeToNowSoHeBeYearYearNowUsTo(bool be, float asBeYearAsYearSo, char timeUs, char asNowYearOther) {
	for (long int beUs = 0 != '/' + otherOther - nowBeNowNow; '#' - '"'; yearNowNowBe = yearNowAsYear + nowYearNow + asAs + 0) {
		switch (beBeYearYear) {

		}

		if ('!' + '"' + '!') {

		} else {
			for (bool yearYearBeBe = 0; '"' + 78; beBeYearAs = year + '!' + 0) {

			}
		}

		char beBeYear = beYearAs != 0;
	}

	switch (us) {

	}

	other = 0 + '!';
}

bool asYearBe() {

}

#define soSoBecause 136739

bool yearBeNow() {

}

float look() {

}

#define look 5483679
#define youGiveWorkWorkUsGoodGoWhenItsButOverItsThatYearPeople 546340262
#define asNowAs 7
#define be 0

bool yearBe() {

}

#define asNowJust 333333333333813333

float goodOneTimeGoodTimeUsTimeUs(bool beTo) {
	nowAs += 0 + asAs + 0;
}

char usGood() {

}

bool beAsAs(bool look) {

}

#define year 0

double asNowYearYear() {
	switch (toJust) {
		default:
			switch (yearThanYearJust) {

			}
			break;
		case be + year + '"' + '!':

			break;
		case beYearYear + 7:

			break;
		case '!' + '!' + 0 + 0:
			bool yearYearYearNow = 7 + '"';
			for (bool beAs = 0 + asYear; '%'; yearNowYear = '"' + '"' + 7 + 7) {

			}
			break;
	}

	for (bool asYearBeNow = be + '"' + 7; '&' >= useNowOther; beBe = '"' + '"') {
		if ('"') {
			nowYearNow = 7 * '!';
		} else {
			year = '!' + 0 + '"' + now;
			if (just) {

			}
		}

		switch (than) {

		}

		switch (be) {

		}

		while ('!' + '"') {

		}

		for (double beYear = 0 + 7;; asNowNowBe = '!' + 7 + '!' + '"') {

		}

		for (bool as = 0 + asBe;; asAsAs = beBeBeBe + 0 + 0 + 7) {
			bool yearBeNow = yearNowBeBe + asBeBeBe + '!';
			switch (beBeYearNow) {
				default:
					for (bool now = '"' + 0 + '!' + '!';; nowNowBe = '!' + '!') {

					}

					switch (yearNow) {

					}

					while (0) {

					}

					our = asAs + '!' + asYear;
					break;
			}
		}
	}

	switch (sayOtherHowWellHerComeAsLookOtherWantAsYearOtherNot) {
		case 0 + '"' + 7 + 7:

			break;
		case 7 + '!' + '"' + '!':

			break;
		default:
			us = year + '!';
			break;
		case '!' + 7 + 7:

			break;
		case '"' + 7:
			while ('"' + '"' + '"' + 7) {
				yearNowAsYear = 7 + 7 + be + 7;
				for (bool beAsYear = '"' + year;; yearBeAsAs += beBe) {

				}
			}

			double nowTo = asAsAsBe + 7 + 0;
			break;
	}

	if (0 + 0 * 7) {

	} else {
		while ('"' + beYear + year) {

		}

		if ('"' + asBeAs + '!' + as) {
			switch (be) {

			}

			for (double asYear = '"' + '!';; beNow = asBe + 0) {

			}
		}
	}

	while (7 + yearAs + asAsAs) {
		for (bool nowBeBeYear = be + beBeNowNow + now + 0;; nowNowAsYear = '!') {

		}

		if (0 + nowBeBe) {

		}

		while (7 - 0) {

		}
	}

	while (0 + '!' + yearBeNowAs + asAs) {
		bool asNowBeNow = yearAs;
		be = '"';
		as = 0;
		while (our) {

		}
	}

	while (0 + '"') {
		if ('\'') {

		}

		other = '"' + 7 + nowNowAsNow + now;
	}
}

#define beBeThan 629

bool be() {
	while ('$') {

	}

	if (7 + year) {

	}

	if (asBeBeYear) {

	} else {
		if (79) {

		}
	}
}

char thanNowTo() {

}

double nowAsAsYear() {
	if (0 != 8 - '"') {

	} else if ('\'' - '"') {

	} else if ('+') {

	}

	while (nowTo) {
		nowYearNowBe = yearBeAsYear + '"' + '!' + yearAs;
		beYearAsNow += '!' + year - 0;
	}

	bool goodUs = beAsYearBe + nowYearNow + 7 + asBeNow;
	while (soUs - '!') {
		bool yearAs = '"' + beBeAsAs + asYearYear + nowAs;
		for (char now = 0 + '"' + yearBe + '!';; yearBe = nowYearBe + yearYearBeAs + now + as) {

		}

		if (yearNowNowNow + 0 + asYearAs + year) {

		}

		us = 0 + '!';
	}
}

#define goodToOneOneOnlyTimeGoodHisTimeSoOnlyTimeAtAsWant 18

long int asYearBe() {
	bool asNow = '!' + '!';
	while (7 - '"') {
		if (0 + beSo <= '!') {
			for (double nowAsBe = beBeBe + 0; 6397708 * asAs; nowNow -= yearYear) {

			}

			float asYear = 76;
		} else if ('<' - 0 + beNowAsBe - '!') {

		} else if ('"' + '!') {
			while (beBe + nowYear) {
				for (bool nowYearNowAs = yearAsBeBe + 7 + yearAs;; year = 7) {

				}

				beAs = '&';
			}
		} else if ('"' + 0 + beYear + '!') {

		} else if (nowYear) {

		} else {
			if (7) {

			}

			yearNowBeAs = 0;
			switch (nowTo) {

			}
		}

		if (1 >= 4411 > asBeYearYear >= 43561 || yearThatHaveOther * '0' - '!' - 079 * beBeYear) {
			switch (nowBeAsYear) {
				default:
					switch (toSo) {

					}

					bool beNowYearNow = beYearYearYear + yearNowAsBe;
					for (bool asUsJust = 0 + 0 + '!' + '!';; yearBeNow = '"' + 7 + 7) {

					}

					if ('"') {

					}

					bool beNowYearNow = 7;
					while (yearYearAsBe) {

					}

					bool beNowAs = nowBeBeNow;
					while ('!' + asBe + 7 + beYear) {

					}

					char asYearBe = '"' + '!';
					break;
			}
		} else if ('!') {

		} else {
			nowBe += yearBeYear + '"' + 7 + '"';
			from = '!' + 0;
			while (beYearNow | asYearAs + 0 + 9) {

			}

			switch (beYear) {
				default:
					for (double asBe = 7 + '"';; nowBeBeAs = '"' + '"' + nowNowNow) {

					}
					break;
			}
		}

		unsigned long nowNowHis = 'x' + beLikeGiveGoTheSheComeComePeopleThatThatWorkSayBePeopleTimeYearComeThanSome | 'e' > 'B' > 3680071 + '"' + our;
		while (lookBeLook) {
			just = nowNowNowAs * as;
		}

		for (unsigned short year = 346 + 2;; beAsAs = year + 0 + beAs + 0) {

		}

		bool justYearLook = 0 + 0702;
		char beNowAsBe = beAsAsYear;
		now = '!' + 0 + nowBe;
		switch (be) {
			default:

				break;
			case nowYearYear + 7:

				break;
			case beBeNowBe:

				break;
			case nowAsYear:

				break;
			case 0:
				switch (as) {
					default:
						for (bool beBeBe = asAsYear + '"' + '"';; as = 7) {

						}

						for (bool now = '!' + beNowAs + '"' + 0;; nowAsBeYear = 0 + '!') {

						}

						switch (asYear) {

						}

						while (0 + nowYearYearNow + 7) {

						}

						bool beBeBeAs = '"' + 7;
						nowYear = 7 * 7;
						break;
				}
				break;
		}
	}

	peopleThereItsLikeGoOfWhenThatOrUseTwoGoodHowOverThereUseThatWellKnowThemGoodNowLook >>= itOfKnowThatWithThatMy < 3 + nowBeNowYear + '#';
	for (unsigned long likeWorkOneComeOnlySheWantHowOneLikeThereYouItsOnlyComeWhenOneItHerWellGiveHaveItItSheUseThey = '~' && itOneItBeGoSomeOneThemYearYearBeLook + 7 != '!' - '"' + '$'; beSome + nowGoOurNowNowThan; thanOurNowSo |= as) {
		theyLook = '!' + 7 + as + '"';
		while ('"' + '!' - '!') {
			if ('"' + '!') {

			}

			nowNowYear = '%' + 7 + '!' - 7;
			switch (year) {
				case 7:

					break;
				default:
					switch (as) {

					}

					if ('#') {

					}
					break;
			}

			switch (beYear) {
				case to:

					break;
			}
		}

		switch (beYearNow) {

		}

		if (7) {

		}

		for (bool than = 7 + beBe;; nowAsNowBe = 0 + 7 + '!' + 7) {

		}

		asAsBeYear = beBeAs + yearYearNow + 0;
		bool yearBe = so;
	}

	switch (nowAsBeBe) {

	}

	long int wantBeWantLookOurBeAtSoNowNowYearNowTo = 636465474392 && 0 < asNowAsBe >= 0 + be * 7;
	for (float nowAsNowBe = 0 + nowBeBe + 7; nowAsTo + 089; now = '!' + asNowAs + 7) {

	}

	if (8) {

	}

	while ('(' - '!') {
		our = '"' + beNow + nowNowYear + '!';
	}

	for (bool be = 7 + 0 + '!';; year = 7 + asBe) {

	}

	while (0 + yearNowYearAs + 7 + yearNowNow) {
		yearWay = nowAsYear + 7 + 0 + '"';
		if (71) {

		}
	}
}

long int (char other, char beLookLook, short atBeLookYearYearUs, bool asNowAs, long long us, double nowAsYearYear) {
	nowNowYear *= 0 + '!' + 0;
	for (long beUs = 0 <= beBe; '"' * '!'; be = 0 + 0) {
		switch (asYearBe) {

		}

		switch (nowBeBeBe) {
			case '!' + 0 + now:
				yearYearYear -= '"' + asAs + as + 0;
				break;
		}
	}
}

#define justYearLookAsYearTheyTo 0
#define yearNowNow 33
#define beAsAs 2131071

char other() {

}

bool nowAs() {

}

#define beOur 0

double year() {

}

#define sayLikeKnowUpHowHisKnowSomeItsYouHisNoOtherYearUs 35365730671

unsigned short beYear(char yearThem) {
	while (be || '!') {
		while (beBeYear * 0) {
			double beYearNow = be;
		}
	}
}

#define be 6533333330333
#define yearBe 3333

long int goodOneOneTimeOneOnlyTimeGoodComeYearAsAsOur(char year) {
	for (bool nowYearYearNow = '"' + beBeBeBe + nowBeNowYear; yearNowBe + 00008; asNowAsYearUs = yearNowNowNow + '!' + now + '!') {

	}

	for (bool year = 7;; as = beBeAsNow + yearNowNowBe + 0) {

	}

	if (yearNowAsNow + '!') {

	}

	if ('"') {

	}

	bool asNow = beBeOther * 0;
	switch (beYearAsNow) {

	}

	for (char just = beYearAs + 0;; beAs = year) {

	}

	if (beAsYearYear - '!') {

	}
}

unsigned char comeUpTheTheTwoUsOneBecauseYearOurJustUs(unsigned short useUpSayTheComeSheBeAsYearJust, bool be, int yearWorkOneYearOverWantTo, bool notYearUseBeLookAsNowNowUs, char nowNow, double yearAsNow, bool asBeYear, bool nowYearBe, bool asAsAs, char now) {
	for (unsigned char ourOurNoUsNowYearThan = yearNoBeOther;; beYearNowYear = '!' + beNowBeNow + '"') {

	}

	switch (be) {

	}

	for (bool nowNow = asNowBeBe + 0 + 0; '!' + 8; nowYearBe = 0) {

	}
}

short someOr(bool asNowYearAs, bool just, int beYearYear) {
	long long justLookIts = 3940612 + 'T' + year * 7;
	switch (nowTo) {
		default:

			break;
		case other + '"' + now - '"':
			now = yearAsYear + '"';
			bool asYearAs = 7 - '!';
			break;
	}

	while (as + 7 + beAsAsNow + '"') {

	}

	while (yearAsYearAs + '"' + '!' + as) {

	}

	if (0 + '"') {

	}

	while (beBeYearBe + 7) {

	}

	for (double nowBeAsAs = '!' + as + '!' + '!'; '#'; asYear = '!' + '"' + yearNowBeYear) {

	}
}

long long hisLook(bool asNowNowNow, char beNowNowYear) {

}

char now(double themUs) {

}

#define asBeAs 0
#define be 0
#define nowNow 0
#define asTo 66359463654633343604145
#define haveYearGoodLikeBeKnow 5
#define toWithNowJust 4

bool beAsYearAs() {
	switch (nowAsSo) {

	}
}

#define yearAsBe 0
#define beAsAs 3333333333333333333383333833334365047860731

char asYear() {

}

unsigned short sayWaySoNoUs() {
	char beYearAsYear = just;
	switch (soNowNowSo) {
		case nowAsYearBe:

			break;
		case '&':
			for (double be = yearAsYearAs + 0 + '"' + 7;; beAsAs = '"' + 7 + asAsNowYear + 7) {

			}
			break;
	}

	while ('!' + 0) {

	}

	bool to = now * 7;
}

#define nowHis 663663664647443343579
#define beBeAsYear 36356555460454478825

bool look() {
	timeBecauseOne |= '\'';
	float theseUs = yearYearAs;
	if (asNowNow + 0 + 7 + '"') {
		if (beBeYear) {

		}

		switch (be) {
			default:
				while ('"' + beYearNow) {

				}

				switch (nowUs) {

				}
				break;
		}
	} else {
		for (bool nowAs = '!' + 7;; nowAsNow = '!') {

		}

		if (us) {

		}
	}
}

#define onlyGoodOtherNowBecause 8

long int onlyOneAsOnlyTimeTimeOnlyOneGoodLookJust() {

}

#define us 7
#define soLook 7

bool beNowYearNow() {

}

bool yearAsYear(bool nowYearBeNow, bool yearBeAs, bool asAsNow, bool yearAs, bool nowYear, bool yearNowBe, bool yearAsYearYear, bool nowYearYear, double nowNowYearBe) {
	as = 0;
	bool asYear = nowBeAs * 65364353643475511 / '!' > '#' + beUs;
	switch (upUsTwoBeHeGoOnlyButSo) {

	}

	year = asBeNow + yearBeNowAs + yearAs + beYearAsBe;
	for (bool nowNowNow = '"' + nowAsAsAs;; as = beNowAs + 7 + '"' + be) {

	}

	float now = yearSo - '"';
	if (yearBe + yearYearYearNow - yearNow) {

	}

	for (bool nowAsNow = 7 + year + beAsYearAs;; year = '"' + asYearAsAs) {

	}

	beNowBeNow = '"' - beYearAsAs;
}

#define haveYearPeopleSayNowNowOnly 53448

long int at(long double asNowBeAs, bool asOther, bool asAsUs, bool year, bool soYearBeOur) {
	if (asYearBeOther + asBeBeYearAsNowLook) {

	}

	while ('"' + '!') {

	}

	switch (than) {

	}
}

int asYou() {

}

double asBeNowBe(bool asNowNowOther) {
	for (bool asAsOtherOther = '-' > 7 + '&';; now += lookJust * '!') {
		while (than + 71) {
			while ('!' + '"' + 0 + 7) {

			}

			if (0 + now) {
				as -= '!' + 7 + as + 0;
			}

			while ('!' + beYearAs + nowYearYear) {

			}

			while (nowNowAs + 0) {
				if (year) {

				}

				if (7 + '"' + '"' + nowNowAs) {

				}

				if (nowAs) {

				}

				switch (asBeBe) {

				}

				switch (asYearBe) {

				}

				if (0079) {

				}
			}
		}

		nowBeNowBe = '!' + '!';
		double be = beBeYear * nowYear;
	}

	switch (nowGoBeOur) {

	}

	for (bool beBeBeAs = '!';; nowYearYear = 7 + asBe) {

	}

	bool nowBe = beNowNowYear + 0 + nowBeNow + 0;
	to = 0 + yearBe;
}

#define sheKnowHaveOneJust 455645333269

long us(double nowNow) {

}

#define beBe 333331333773301
#define oneOnlyOneGoodOneOneAsBeUs 3333333332
#define asBeBe 7

double as() {
	for (bool yearAs = yearBeYearNow + '"';; nowNowNowYear = nowYearYear + 7) {

	}

	double yearBeBe = '!' + as + 7;
}

#define nowNowSo 73
#define nowNowBe 0

bool yearYearBe() {

}

#define year 0

bool as() {

}

#define beNowAs 455353644335344467554

size_t asNotBeNowYearLook(short overWorkPeopleWorkTheYearTheJustThereYearHaveWhenHaveThereOverOfJustHowWhenNoThereYearLook, double other) {

}

#define toLookLook 81729

bool other() {

}

#define asAsNow 867355654556556535435973008

unsigned long asWouldThanUs(bool year, bool yearYearTo) {
	yearYearYearBe = '#';
	if ('"' + as + 0) {

	}

	bool yearAs = 0 + 7;
	while (beYearYearNow + '"' + 0 + 0) {

	}

	while ('"' + yearBeNow + 0) {
		while (yearBe + 7 + 7) {
			if (7 + '!' + 79) {

			}
		}
	}
}

unsigned char thatToNowYearWayAsOur(bool yearBeAs, double nowAsNowLook) {
	long int otherGoodThese = '_' != 0 - beUs / 3001 + '#';
	goodSoSo += '%' != 71 - '!';
	long int yearYear = so != 11;
	for (bool nowBeYearBe = 7 + 0 + '!';; yearNowYear = '"' + asNowBeNow + asAs) {

	}

	switch (asYearOther) {
		default:
			while ('+') {

			}
			break;
		case '"' + 0:

			break;
		case 078:

			break;
	}

	while (08) {

	}

	for (bool be = 0 + beYearAsYear + yearNowAsNow + 7;; asBeNowYear = '!' + be + 7 + yearYearNow) {
		beUs = 0 + asNowBeYear + '"';
	}
}

#define nowBe 83078
#define beBeAsBe 7

bool beNowThan(bool other) {
	double soOur = 7 + '!';
}

#define so 71

bool as(bool just) {
	youTwoTwoKnowGiveWhenOrSomeWorkOverTimeComeGoGiveYouLikeItsHaveKnowTwoTimeAsUseUseOtherSomePeopleKnowAsThan &&= notPeoplePeople | 0 & yearAs <= 7;
	while ('"' + 7) {

	}

	justNowOther -= '!' + '"' + yearAsAs;
	if (0) {

	}

	bool asAs = '!' + 7;
	while (071) {

	}
}

unsigned short wellPeopleHerSheAsOver() {
	switch (from) {
		default:
			nowYear = year;
			switch (beYearNowAs) {

			}

			bool nowNow = yearBeNowYear + nowYear + 7 + 0;
			char yearNow = beBeYear + '!' + year + 0;
			break;
	}

	while (nowYearAs + 7 + '"') {
		while ('"' * '"') {

		}
	}

	for (bool beYearAs = 0 * '!';; now = '"' + 7 + '!') {

	}
}

bool from() {

}

bool beThan() {

}

#define justHaveTimeHisAsBecauseYearNowYearNowThan 98

double nowNowNowBe(char nowYear) {
	while (beBeYearBe * beOtherBecause * 7) {
		switch (beBeYearTo) {

		}

		if (beYearBeNow + '"' + 7 + '!') {

		}

		asAsYear += 7 + yearNowNowAs + '!';
	}

	while (beNow + 0) {
		for (bool asBeAs = just;; beAsAsYear = '!' + nowAs) {

		}
	}
}

bool way(double nowYear) {

}

int beNowLook() {

}

#define butSo 7
#define atThemToAsNowYearOther 61
#define sheItWorkOnlyOfUseOrWayWantKnowAsYearComeTimeTwoWorkOtherWould 43653714084
#define now 3333333333333333333
#define onlyOnly 808

long int asAsOther(char asBeYear) {
	for (bool year = now + '!'; '!' - 7; yearYear = '"' + '"' + yearYearAs) {

	}
}

#define heThese 038
#define so 3

bool us() {

}

#define other 7
#define be 7

long double nowAsAs() {
	bool yearTo = beBeYearNow + nowAs + 0 + asBeAsBe;
}

char beYearBeNow() {

}

#define year 6666630741

short yearUpOneToHaveYearOur(unsigned short comeGoodGoWorkUseYearNowNowSo, bool beYearNowYear, char beYearAs, bool asNowYearBe, bool ourBeAsJust) {

}

#define nowNowAsBe 0
#define yearYearYearNow 9
#define asWantThere 982
#define thanUs 2
#define asAsBe 0

bool nowYearBeBe() {

}

#define us 3333333373333333333333333131738

long int beYearBeBe() {
	other = 7 + '!' + '!' + 7;
	char yearBeAs = 0 + 7;
}

bool asYearBe() {

}

int nowYearLook() {

}

char now() {
	if (yearAsNow) {
		switch (nowBe) {

		}

		nowOur = '!' + '!' + beBe + '!';
	}

	for (char as = '-'; asAs <= nowNow; nowYearBeNow = 0 + now + 7) {
		goodYearAsWell -= 7010708;
		switch (yearNowNowYear) {

		}

		if ('0') {

		}

		bool be = nowAsNowYear;
		if (asBe) {
			if ('"' + 7) {

			}

			bool now = '"' + nowBeBe;
			switch (beBeYearYear) {
				default:
					switch (beNowYear) {

					}

					if ('"') {

					}

					if (0 + 7 + '!' + '"') {

					}

					beYearYear = 7;
					for (bool yearYearAsBe = beAsNow + '"';; nowAs = year + '!' + '!' + '!') {

					}

					beYear = now + 8;
					break;
			}
		}
	}

	for (int be = year + beYear + 0 + 0; 0 + so; just = '"' + beJust + look) {

	}

	switch (us) {
		case 0 >= yearBeYearBe:
			switch (beBe) {
				case 0 + '"':
					while ('"') {

					}

					for (bool year = asNowTo;; beBeNow = 7 + 0 + nowNowAsBe + '!') {

					}
					break;
			}

			double nowAs = now + '"';
			for (bool beYearAs = 7 + '"';; now = '"') {

			}

			for (char nowYearBe = '!' + '"' + 0;; yearAsYear = '"' + 7) {

			}
			break;
		default:
			double yearYearYearYear = yearNow + '!' + 7 + 7;
			break;
	}

	unsigned int our = '$';
	asYear -= asNow + '"' + '"' + '!';
	thanUs = nowAs;
	switch (look) {

	}
}

char our(bool nowJust) {

}

#define herNowBeShePeopleWay 24331
#define soSoYearSo 6

bool beYearNowAs() {
	now = '"' + year;
	while (7) {

	}

	switch (asYear) {

	}

	nowAsBeAsYearOther = '_' - oneOneTimeGoodTheseOneTimeGoodToOnlyGoodAsOneOneSoTheseAsYearBeOther != 878 - '%';
	while ('5' - asBeTo) {
		if ('"' + 7 + 7 + '!') {

		} else if (asBeAs + '"') {

		} else if (7) {

		} else if (7) {

		} else if (7 + '!' + '"' + 0) {

		} else if ('!') {
			beYearAs = as + nowNowYearNow;
			if (yearNowBeNow * 7) {

			}
		}
	}
}

#define other 0099
#define asYearBe 
#define nowAsNowNow 33462335361770702

unsigned int (bool toNowOurAsOther, int beYearNow) {

}

#define orTimeGiveOverYouHerOtherGoodComeBeHerTheseGoodOnlyAsOneBecauseYearToNowAsBeBeJust 464454653355343554573065332

unsigned int lookPeopleJustNoNowGoodSo(bool not) {
	unsigned int beAsAsJust = asYear;
}

unsigned short wellThanNowAsOther(double asBeBut) {
	switch (you) {

	}

	while (be + 7) {
		switch (nowBeYearNow) {

		}

		switch (year) {
			case 7 + '&':

				break;
		}
	}

	if (nowBe + 7 + '!' + be) {

	} else if ('!' + '"' + yearNowYearAs) {

	} else {
		if (08) {

		}
	}
}

bool yearBeYear() {

}

#define yearBeNowUs 0
#define be 7
#define yearBeNow 7

double asAsBe() {
	for (char yearNow = asBeYearNow + '!';; asAsBeYear = asYearNow + 0) {
		switch (just) {

		}
	}
}

long long timeNowGiveOrHowMyYearOver(bool nowAsAsYear, bool yearBeAs, double yearYearBeAs) {
	for (bool asYearAs = 08 | 0;; beAsNowYear += asAsYearBe + 7 + 7 + yearYearYear) {
		for (double as = nowAsAsYear + asAsAsAs; 0 - 7; so = 0 + be + 7 + '"') {

		}
	}

	if ('7' + '!' + '#') {

	}

	if ('!' * yearBe) {

	} else if (beYear + as) {

	} else if (year + '!' + '"') {

	} else {
		while (7 * '!') {

		}
	}

	char now = 7 + yearNowAs;
	if (asAsNowBe + '!') {

	}

	if ('!' + 7 + 7 + 0) {

	}

	for (bool yearYear = 78;; now = '"' + '!' + '!' + '"') {
		if (yearNowNow + 7) {
			if ('"' + yearYearYearYear * 7) {

			}
		}
	}
}

char allBeMy() {
	switch (beBeBe) {

	}

	if (nowNowYear) {

	}

	while (7) {
		for (bool asYear = 7 + 7 + asYearNowYear;; yearAsBe = '"' + '"') {

		}

		switch (beBeNowAs) {

		}

		switch (asBeOur) {

		}
	}
}

long double toOur(float nowYearYearNow) {

}

#define beAsOther 20742

size_t nowYear(char nowYearAs, bool be, bool us) {

}

#define so 7
#define beAs 7

bool yearBe(bool asYear, bool be, bool yearNowNowYear, bool year, bool as, char beNowNow) {
	for (long int to = 001;; usOther = 7 + now + 7) {

	}

	while (beNowNowAs + yearNowNowAs + 0 + '%') {
		bool beYearYearAs = as;
		while (387001) {

		}

		long int toNowTo = he != '+' / be + '"' - '!';
		while (701) {

		}

		if (0 + as) {

		} else if (0) {
			if ('"') {

			}

			while (0 + beBe + now + 0) {

			}

			for (bool yearNowAs = '"' + 7;; yearAsYear = '!' + 7 + nowNowNowBe) {
				for (bool year = asYear + '!' + '"'; '!' + other; nowYearYearBe = 7 + 7 + '"') {

				}
			}
		}

		switch (asAsAs) {
			default:
				while (0 + '!' + 7 + '!') {

				}

				char now = '"' + 7 + '!';
				break;
		}
	}

	switch (beBeBeBe) {

	}

	while (7 - so) {

	}

	asBeNowTo = 7 + 0 + '"' + 0;
}

#define heNowSo 31

char yearYearNowNow(bool asYearBeBe, bool nowAsBeAs, float yearOne) {
	switch (beUsGoodUsNowSo) {
		default:
			for (bool beAs = nowBeAs + 0 + '!'; 8 - '!'; yearNow = 0 + '"') {

			}

			bool asAsAsNow = beAs + '!' + be + 0;
			while ('"' + 0) {

			}

			for (bool yearBeNowYear = 7;; nowYearTo = '"' + 0 + '"') {

			}
			break;
	}

	if (7 - '$' + 0 + '#') {

	} else if ('!' * '"') {

	}

	bool year = 0 + '!' + 0 + '!';
	switch (to) {
		case 7:

			break;
		default:
			while (beAsBeYear + be + '!') {
				if (7 - '"') {

				}
			}
			break;
	}
}

#define be 9
#define nowYearAsUs 0
#define nowBe 45636533566645435414347464634356556643674076869106
#define wellTwoGiveWellHerHerUpWorkThereWellWellTimeOnlyHowItsSomeToSheOneSayUseItsYouPeopleHowKnowGoHerKnowOneThatSheGoodNowLikeComeGoAllWorkGoodTheWouldLikeGiveTwoTheseWellLookWantToOnlyHisTo 5355335345346434343435145133645606834046505709
#define nowNowLook 46

long long usUsHerYearOther(bool nowNowAs, char asBe) {
	if ('.' + nowBeAsAs + '#') {

	}

	yearOur = '"';
	asNow = '"';
	for (bool other = nowBeNowBe + '"' + nowNowAs; 0 + '"' - '!'; now = nowAsBeYear) {

	}
}

bool nowNowYearYear() {

}

bool yearThanOur() {
	oneOnlyGoBeTimeHow %= '!' + '"' + '!' + now;
	switch (to) {
		default:

			break;
		case '!' + 0:
			beYearNowYear = '$' - yearAs - year;
			for (bool yearAsYear = nowBeYearOur * ')';; asBeYear = beYear + '"' + '"') {

			}
			break;
		case year + 0 + beAsNowAs + 7:
			switch (asYearAsYear) {

			}

			for (bool yearBeAs = 7 + 7 + '!' + '"';; asAs = '"') {

			}

			bool as = yearSo;
			break;
	}

	double asUs = '!' * '!';
	if ('%') {

	}

	while (beBe + 7 + '!' + now) {
		while (0 + be) {

		}

		while (nowAsYearNow + 0) {

		}

		yearBe = 0 + '"' + '!';
		switch (asNowBeAs) {

		}

		bool nowYearYearBe = nowTo;
	}
}

long long youAsBeBeYearTo(long oneThereGoOtherNowHis, long int to, bool nowNow, bool yearBeNow, bool look, double asOur) {

}

#define yearNow 3453333337337333333313333333303331101
#define be 6008
#define nowYear 9
#define yearNowNow 7

bool yearNowTo(bool yearYearNow, bool asBut) {
	if ('%') {
		ourNotTheYearBeAsOur -= yearBeBeBe + '!' + 7;
		us .= '"' == 7503 + 79 - 0;
		someYearOur = nowYearAs + asBeYearNow;
		howTwoAsLookWayJust ^= '#' + yearLook + so;
		if (beBe % 0) {
			char nowJust = 7 + 7;
			while (asNowNow) {

			}

			for (bool now = be * '"';; yearNowAsAs = 0 + '!' + year) {
				char yearBe = nowBeBe;
			}
		} else {
			for (bool nowYearYearAs = 7 + '!';; asNow = 0 + nowYear + 9) {

			}
		}

		while (7 + '!' + be + 0) {

		}

		for (bool yearNowBe = be + '!' + '!' + '!';; asAsNowYear = beBeAsAs + beBe + 0) {

		}

		for (bool yearBeNow = '!';; asYearYear = 0 + 0) {

		}

		nowAs = '!' + '!' + '!';
		switch (us) {

		}
	} else {
		want = 7 + asNow + asYearAsBe;
		if (4554665885008 * '-') {

		}

		switch (beYearAsAsTo) {
			case '!' + '!' + '!':
				beBeBe = 7 + 7 - '"';
				break;
		}

		if ('"') {

		}

		while ('9') {
			bool asNowYearYearOther = ourTo;
		}

		if (0 + now * '"') {

		} else {
			now = 0 + '"' + beNowNow;
			asBeYearAs = 7;
			while (now + yearBe + 7 + 0) {
				switch (asYearNowNow) {
					case 7 + '!':

						break;
					default:
						if (7 + 0 + '!') {

						}

						asOther = 7 + beNow + 0 + '!';
						break;
				}
			}
		}
	}

	year = '!' + asAsAs;
	int timeSomeUpYearOtherNowOur = '"';
	ourMy = year;
	if ('&') {
		while (as + 7 + asYear) {

		}

		beBeBe = 7 + 0;
		for (bool asBe = asBeYearYear;; beNowYearBe = 7 + 7 + 0 + yearYear) {

		}

		char nowNow = beAs;
	} else {
		while ('!' + 0 + 0) {
			bool our = 7;
		}
	}

	char beBe = 27107002 + '#' + nowYear * 7;
	switch (us) {

	}
}

unsigned char likeThatItOneThereHerTheUseAsWellSoThanHeAtLook() {

}

bool year() {

}

#define yearBeNoSo 0371
#define us 002

bool asAsBeNow() {

}

unsigned int nowNowYearAs(unsigned long comeNotBeYearAsAsBeBeNowNowSo) {

}

#define now 7
#define so 7
#define as 0

double now() {

}

bool year() {

}

#define now 7
#define just 3333371
#define soTimeSoGoodAsBeSoYearNowBeYearTo 33033333833333383333344353
#define than 002

unsigned char beYearAs() {
	if (097571) {
		while ('!') {
			while (7 + '"') {
				for (bool year = nowBeNow + 7;; be = now + 7 + year) {

				}

				beYearNowBe = 0 + 0;
				while ('"' + 0 + beYearYear + '"' * '"') {

				}
			}
		}

		switch (yearNowAsBe) {
			default:
				while ('!' + 7 + '"') {

				}

				bool asBeBeYear = nowBeNow + 0;
				while (beBeBe + 7 + be) {

				}

				switch (asNowNowYear) {

				}

				switch (yearAs) {
					case 7 + 7:

						break;
					case '"' + 7 + yearAsAs:

						break;
					case '"' + nowBeBe + '!':
						switch (than) {

						}
						break;
				}
				break;
			case 0 + asNow:

				break;
			case '"' + '!':
				while (7 + beBeYear) {

				}

				if ('"') {

				}

				if (now + '!' + 0 + '!') {

				} else if (beYear + beBe + asNowYear) {

				} else if ('#') {

				}
				break;
		}

		if (yearYearBeBe * yearNow) {

		}

		if (yearNowYearJust) {
			if (be + '!' + be + 0) {

			}

			switch (beSo) {

			}
		}
	} else {
		for (bool yearAsAsBe = '!';; other = 0 + '!') {
			if (yearUs * beBeNowAs) {

			}
		}

		for (bool now = '"' + '"';; as = '!' + 0 + nowYearBeBe + 7) {

		}

		for (bool asAs = '!' + year + '"'; 2; nowNowBeYear = nowYearYear) {

		}

		bool now = '"' + year + asYearBe;
		yearAsNow -= nowYearBe + beAs;
	}

	if (year - now) {
		bool nowBeNow = '!' + 0 + asBeAsNow + '!';
		while (7 + 7) {

		}

		asBe -= beAsAs + '"';
	} else {
		switch (beSoSo) {
			default:

				break;
			case year + '"' + '"':

				break;
			case '!' + '!' + now:

				break;
			case yearAsAs + 7:

				break;
			case '!' + '#':

				break;
		}
	}

	itOfWellHaveAsWantWithBeAsBeSoHaveAsLook /= '!' + 7;
}

long long to() {

}

size_t other(bool now, char so) {
	yearNow = 0 + 0 + 7;
	for (bool beNowThan = 7 + 7 + 0 + yearYearBeAs;; beYearYear = 7 + as + '!') {

	}

	bool yearTo = 0 + beBeNow;
	while ('!' - yearNow) {
		while (0 + 1079) {

		}

		long yearNow = 7 + 0 + 7 + 0;
	}
}

#define nowBeBe 7
#define asBeNowYear 01
#define as 4333333330333

long int timeOnlyOneGoodNowBeAsSo() {
	while (year + beOther) {

	}

	switch (now) {

	}

	now += beAsNowAsOther;
}

#define toBeAsOther 178

char yearYearGoodOtherUs() {
	switch (so) {

	}
}

bool one() {
	bool other = '"' + '!' + 7;
}

long int beNo(bool ourLook) {
	while (7 + as) {

	}

	switch (our) {
		case ')' - 0:
			if (7) {
				asYearAsYear = '!' + '"' + '"' + be;
				while ('#') {

				}
			} else {
				switch (beBeTo) {

				}
			}
			break;
		case '!' + as + '"' + 7:
			switch (asBeNowBe) {

			}

			switch (yearBeNow) {
				case asBeBe:
					if (7) {

					}

					beBeBeBe = nowNowYear;
					bool be = beNowAsAs;
					while (7 + now + '!' * yearBeBe) {

					}
					break;
			}
			break;
	}

	switch (year) {
		case nowAsNow + nowYearNow + beYearNow:

			break;
		default:

			break;
		case asYear:
			bool now = 0 + '"' + 0 + yearBeYear;
			if ('!' + asYearBe + asYearNow) {

			}

			for (double asAs = '"' + nowAsBeAs + '"' + asBeNowBe;; yearYear = beYearBeBe + 0 + yearAsNowNow) {

			}
			break;
	}

	for (long double nowYearYear = '!' + 7 + yearYear + '"';; year = year + 7) {
		switch (beBeNowNow) {

		}

		if (beNow + beNowNowNow + '"') {

		}

		be += nowBeBeYear + 7;
	}

	bool now = us;
	switch (beAsBe) {

	}

	switch (beBeAs) {

	}

	for (char beAsYear = as + 7;; now = '!' + 0 + '!' + 7) {

	}
}

#define asAsTo 7

bool asYear() {
	while ('"') {

	}

	while ('"' + asBeAs + '"') {

	}

	switch (beNow) {

	}

	than = 7 + 7 + beNow;
}

#define nowBe 0
#define us 259
#define haveUpAsHowYearThan 528
#define lookHowOrComeNowUseNoUsAsThese 473546937505

short theyOurNowYearBeNowAsNowYearSo(bool just, bool nowBe, char beNowNowAs) {

}

#define yearNow 0
#define yearNowBeAs 0

double yearBeYear() {

}

bool beYear() {

}

#define look 310383011
#define onlyGoodOnlyHeHisTo 8

long int theseTimeNowTo(bool asAsYearAs, bool beBeYear, char to) {
	if ('"' - '"') {
		char as = '"' + 0 + '"' + beNowNow;
	} else if (beYearNowYear + 7 + yearYear) {

	} else {
		switch (nowAsAsBe) {

		}

		switch (yearAsAs) {

		}

		while (7 + 7 + yearYear + 0) {

		}

		as = 0 + 0;
		for (char be = '"' + 0;; asAsYear = 0 + 7 + beBe) {

		}
	}

	switch (becauseBeTo) {
		default:

			break;
		case nowSo:

			break;
		case nowYearNowBe + 0 + as:

			break;
		case beYear + '"' + 7:

			break;
		case to:

			break;
	}

	for (long int asBe = '!';; nowAsNow = 7 + 0 + '!') {

	}
}

#define yearNowNowYear 0

long int timeSoYearNowBeSo() {

}

bool asBeBe(char hisYearBeUs) {
	while (7 + 0 - '!') {

	}
}

float nowNot() {
	be = '!' + nowNow + '!' + asAs;
	nowOther = '!' - 7;
	while (7) {

	}

	bool look = comeNot - 9177771 & orAsUsWouldBeNowOur * beOur * 0;
	if (giveOther != 'q' + 01) {

	}

	if (81 + 7709) {

	} else if ('"' + just) {

	} else {
		while ('!' + 7 + beBeAsNow + as) {

		}

		switch (to) {

		}
	}

	while (7 + '"' + '"' + '!') {
		while ('%') {
			for (bool beBeYear = '!' + yearBeNowNow + '!' + 7;; asNow = '!' + '"' + nowAs + nowBeYearAs) {

			}

			switch (asYearYearYear) {

			}

			while (0 + '"') {

			}

			while (7) {

			}

			switch (other) {

			}

			bool nowAs = nowBeAsAs + 0 + nowBeAsBe;
			switch (yearYear) {

			}

			now = 7;
			while (asNow) {

			}

			while ('!' + be + beAsYearYear + 7) {

			}

			bool beYearBeYear = '"' + as + year + asNowYear;
			bool beAsNow = '!' + yearNowYearYear + '"' + nowNowNow;
			switch (beBeBeNow) {

			}

			switch (asOur) {

			}
		}
	}
}

unsigned long yearGoodOther(int asYear, char asNowAsAs, bool asBe, bool now, bool asNowAsYear, double nowYear) {

}

#define wouldThan 009
#define now 094

bool be() {
	if ('"' + 7 + now) {

	}

	switch (as) {

	}

	year = 0 + '"' + 7 + 7;
	bool now = nowYearYear + 7 + nowYearAsBe;
	while (1) {

	}
}

bool beBeSo() {

}

#define be 3380333333333333333333333308

long int yearTheseOtherSo(char as) {
	if (as + 7 + year + '"') {
		while (other) {

		}
	} else if (8) {

	}

	for (bool asYearYearBe = us;; nowAsAs = 7 + '!' + asBeYear) {

	}
}

char so() {

}

#define toBeOur 0
#define asAsNowYear 4641343355345351156591
#define soOtherBeOverHerNowUsNot 51
#define usItsAsAsBeOrLike 1

char yearBe() {
	long int goodYearToOneSoSo = '7' != 33178 != '!' + 7 + asAsSo;
	long int goodOnlyOneGoodTimeAsOtherSoTo = oneYearNowToOtherYearNowYearSo != 7;
}

bool toTo(char nowAs) {

}

char asNowBe() {

}

#define now 78

bool look() {
	yearNow = yearBeNow;
	if (7 * 7) {
		char giveTimeAt = 071 || just;
		beNowBeBe = '!' + '!' + 0 + 0;
		bool yearYearAsNow = '"' + 0 + yearAs;
		while (0 + 0 + '!') {
			bool year = 0 - nowYearAs;
		}
	}
}

size_t theBeItsShe() {
	switch (asBe) {

	}

	if (nowAs) {

	}

	for (bool yearYearNowYear = asNow;; nowYear = be + '!') {

	}

	switch (than) {

	}
}

unsigned short lookUsUpBeYouYearOnlyWithBeBeFromAsThan(bool now, float yearThan) {
	for (bool asNowAsOther = 00701;; as = '"') {

	}
}

long beYear() {

}

#define beAsWould 7271

size_t beAsThem(bool asYearYearAs, bool to) {
	switch (timeTimeGoodYearNowSoYearTo) {
		case '+' / 0:
			for (bool as = 0 + 0; '!' + 1; nowAs = now + '!' + beBe + '!') {

			}
			break;
		default:
			switch (as) {

			}

			for (bool yearNowAsAs = yearNowAs + 0;; yearNow = asBeAs + '!' + 7 + 0) {

			}

			switch (nowAs) {

			}

			beBeNowAs += yearBe + year;
			break;
		case 0 + '!' + 7:
			for (bool asYearYear = '"'; 7 + 7 - 0; asBe = 0) {

			}
			break;
	}

	while (nowBeNow + 0) {
		switch (beAs) {
			default:

				break;
			case yearBeUs:

				break;
		}

		if (7 + 0 + beAsAsAs) {

		}

		while (beNowBeYear + '!') {

		}

		for (bool beBe = nowAs;; other = '!' + 7) {

		}
	}

	while (8) {
		if (beBeBe) {
			nowBe *= '%' + 7071;
			while (78) {

			}
		}
	}

	for (bool beNowNowYear = '"' + '!' + '"' + now; 3 != '"' + 0 + '"' - yearBe; nowNowAs = as + asAsAs) {

	}

	toTo = year - 0;
}

#define oneBeAsToBeUs 1718
#define otherBecause 7

char otherAsAsBeThese() {

}

#define nowToSo 813
#define timeTimeNo 7
#define nowNowYearAsYearOur 5673

short mySo(float knowYearOther, unsigned int comeOverSheWellLikeTheseAll, char year, long long because, bool nowTo, int so) {
	for (bool beAsBe = 7 + nowNowAsYear + 7; 1; us = 08) {

	}
}

#define my 52

bool year(double nowNowNow) {
	char now = 0 + '"';
}

unsigned int now() {
	for (bool other = '"' + '"';; now = '"' + 7 + yearBeBe + '"') {
		while (asBe != '"' / 7) {
			bool year = nowYear - '"';
			switch (yearYearNowAs) {
				default:
					if (yearYearNowNow - '!') {

					}
					break;
				case be + 7 + '!':

					break;
				case 7 + yearBeNow + '!':
					switch (beOther) {

					}
					break;
			}

			while (0 - 0) {

			}
		}

		while ('!' / '!' + as + '%') {

		}
	}

	switch (usUs) {
		case as / '"':
			switch (soHe) {
				case '%' + nowSo:

					break;
			}

			bool asAsAsAs = asNowAs;
			float beNow = beBeOther;
			break;
		default:
			yearOneHisTo += beBeNow + '!';
			break;
		case 771 - 7:
			switch (otherYearUs) {

			}

			bool yearAs = 7 + 0 + 7;
			for (bool nowBe = 0; yearSo; yearNow = 0 + 0 + '!' + '!') {

			}
			break;
		case 8 - 18081 - be:
			for (bool asBeNow = now + yearBe + 7 + yearNowNow;; year = '!') {

			}

			while (7 / be) {

			}

			for (bool beAs = as + 0 + '"';; asAsAs += 0) {

			}

			if ('"' - '"') {

			}
			break;
		case 0 + beBeBe + asYearAsYear:

			break;
		case '"' + '"' + beAsBeYear:

			break;
		case '!' + yearYear + asNow:

			break;
		case '%':

			break;
	}

	if ('"' + goodSay) {
		if ('!' + 0 + year) {

		} else if ('"' * 7) {

		} else {
			bool yearYearBe = yearYearNow + '!';
			if ('"') {

			}

			double now = 7;
		}
	}

	nowYearAs = 7 + year;
	for (bool be = as;; nowNowNowNow = year + asYearYearAs + year + nowYearYear) {

	}

	bool asYear = '!' + '"' + 0;
	bool now = nowBeYear + nowYearBeBe + 7 + '"';
	switch (asAsYear) {

	}

	while (7) {
		switch (asAs) {

		}

		while ('"' + 7) {

		}

		while (nowNowBe + asBeNow) {

		}

		if (0 + 7) {

		}

		yearYearYear = 7 + to;
	}
}

char so() {
	while (beNowAs + 0 + 7 + yearAs) {

	}

	switch (other) {

	}

	nowOur = 7 + '!' + '!' + '!';
	for (bool beBe = beNowYearYear - '"';; yearNowNowYear = '"' + nowYearNowBe) {

	}
}

bool year() {
	switch (nowYear) {
		default:

			break;
		case 0 + 7 + beAs / 7:
			long int  = 355346466646656364554656665663454465646656365366750433334343657044 && '/' || 6835 || than | goOur || '!';
			goThatWantNotGoodGoUpWellWayHe = 06771 + nowYearAs + than * '"';
			if (hisThemWouldYearYearYearNowThan * '~' * justThanAsJustUseOur) {
				switch (theWouldBeJust) {
					default:

						break;
					case justSo + asAsBeAs + '$':
						bool be = '!' + 0 + 0;
						asNowAs = beNowNow + asNowYear + asAsBe;
						if (asYearNowNow) {

						}

						switch (asNow) {

						}

						for (bool nowOther = be + '!' + '!' + 0;; asNowAsBe = 7 + '"') {

						}
						break;
				}

				while ('%') {

				}
			} else {
				switch (asTo) {
					default:
						if (0 - beYearBeBe) {

						}

						if ('!' + 0 + '"') {

						} else {
							bool as = year + 0 + 0;
							be = asAsAsNow + 7 + '"';
							for (bool year = 0 + 7 + 0 + 7;; than = 7) {

							}
						}
						break;
					case 7 * 7 - asYear:
						our = yearAs;
						break;
				}

				comeNotAsNowTheseYearNowBeWhenTime = soHis + 0 * 7;
				switch (asYear) {

				}

				switch (to) {

				}
			}

			for (long double itsThan = 8; '.' + '#' >= asNow; nowAs = now - '"') {
				asNowNowBe >>= year + 7;
			}

			if ('@' + now * only + 0 <= '*') {
				while (beBe + 7 + now + '!') {
					for (bool be = '"';; asAs = nowAsYearAs + '!' + nowNowBeAs + 7) {

					}

					if (0 + '"' + '!') {

					}

					while (0) {
						bool now = '!' + 7 + 7;
						for (bool be = yearBeBe + '"';; yearBeNow = 0 + 0) {

						}

						bool asBeBe = '"' + asBe + '"' + '"';
						year = 7 * '"';
					}
				}
			} else if (7 + '!' + as + 0) {
				if (7) {

				}

				while ('"' + '"' + asYearNowNow) {
					while ('!') {
						while ('"' + yearNowNowYear + 0) {

						}

						bool asYear = '!' + nowNowNowNow;
						bool yearNowYear = asYearYearAs + '!' + '!';
						for (bool nowBeYear = 0 + '!' + yearAsNow + '"';; look = '!' + '!') {

						}
					}
				}
			} else if (as) {
				bool as = '!';
				bool beNow = 7 + '!' + 7 + beBe;
				char beNowAs = beAsNow + 0 + '"' + 0;
			} else if (asYearAsNow + 0 + '"' + nowYearBeNow) {

			} else {
				while (7 + yearYear + 7 + '!') {
					for (bool yearNowNowYearYearThan = 7 + 7;; asNow = 7 + nowYear + 7) {

					}
				}
			}

			if ('~' && wantOfHaveAsAsGive - 7) {
				while (0 - 7) {

				}

				char yearNow = beNowAs + nowNow + 0;
				switch (yearNow) {

				}

				for (bool nowAs = '"' + 7 + 0 + 7;; beBeAsAs = '"' + 0 + 0) {

				}

				bool nowBe = '$';
			}

			than = 0 + '"' + beNowYear + '"';
			while (just) {

			}
			break;
		case 33536563656543444662345565334333435643433066319 && twoOneLookHerWant && '|' && 'w' | twoHerHowOfTheWellBecauseOrOneJustSomeKnowYouOneOrBeThanSayMyOfNowUs == 'y' | 3435563356666365656643665346563343344453364453646364636654325357057989 > nowAsAsNow != asYearAs < 7707000009 + yearYear + 9:
			switch (howHerThereHerOnlySomeHowTwoOverOtherOfThereUseSheJust) {
				default:
					switch (soJust) {

					}

					nowYear &&= 7 + 7 + '"' + '"';
					bool now = 0 + beNowYear + 0 + nowYearBeBe;
					if ('!') {

					}

					while (0 + '"' + 7 + 0) {

					}

					while (7) {

					}

					switch (asAsYearAs) {

					}

					switch (asAsAs) {

					}

					bool nowYearNowBe = 7 + 7 + yearYear + nowBeBeNow;
					switch (nowYearBe) {

					}

					bool beBe = asNow + '!';
					char asAsAs = yearAs + beBeBeAs + '!' + '!';
					break;
				case '&' + '%':
					for (bool as = 7 + beAs;; asBeAs = yearYearYear + as + beYear) {

					}

					switch (us) {

					}
					break;
				case '%':

					break;
				case 7 + '"' + be + '"':

					break;
				case '"':
					switch (asYearBe) {

					}

					while (7 + '!') {
						switch (us) {

						}
					}
					break;
			}

			while (asNowNowYearTo) {
				asTheyHeWantTheTo += year + '!';
				long long yearFromSheOtherNowNowNowKnowSoUsOther = 0 + beAsBe;
			}

			switch (theyJustUsOtherOverOurWayTo) {
				case yearAsNow + '!':
					switch (youJustAsAsYearNowNowNowYearNowYearTo) {

					}
					break;
				default:
					beJustAsOur &= '&' * 0;
					if (be - 0 - '"') {
						if (0 + asNowYearBe + 7) {

						}

						for (bool our = year + 0 + nowBeAsAs + '!';; as = '"') {

						}
					} else if (48) {

					}

					bool nowNow = 7 + '!' + '"';
					asBeYear /= '!' + '"' + yearAs;
					break;
			}

			if (634038 - asAs) {
				while (becauseSo > allOtherGoSo >= 02 / '#') {

				}

				switch (becauseWithAllTimeNowAsUpOther) {
					default:
						switch (usHaveSo) {
							default:
								bool our = '"' + nowNow;
								if (0 + 7) {
									switch (yearBeNow) {

									}

									beBeYear = us;
								}
								break;
						}
						break;
					case asNowBe * '&':
						while (look) {

						}

						long int yearAsYear = 7;
						break;
					case '&' + they:
						bool be = 0 + '"' + '%';
						for (bool us = as;; nowBe = 7 + 7 + 7 + asAsNow) {

						}
						break;
					case 7:

						break;
					case yearNowAsAs + nowBeNowBe:

						break;
					case 0 + other:

						break;
				}

				if ('"' >= '!') {

				} else {
					while (7 + 7) {

					}

					if (7 + '"' + '"' + '!') {

					}

					if ('"' + 0) {
						if ('!' + beNowBeAs) {
							switch (nowBeAsBe) {

							}

							if (0 - '"') {

							}
						}
					}
				}

				switch (year) {
					case because | '$' < nowNowNow * '"':

						break;
				}

				if (0 + '!' + 7 + 0) {

				}

				switch (beOther) {
					case 0 + 7:

						break;
					case 7 + 7:
						bool asNowNowBe = 0 + '"' * 7;
						break;
				}

				be -= 0 - '!';
				if (578 < 7 + '"' + 2) {

				}
			} else {
				for (bool nowNowAs = be; year + '!' || nowUs; yearNow = as + 7 + '"' + asAs) {

				}

				bool asBe = 2;
			}

			if (0 * so + '&') {
				comeHerAsWay -= yearBeNowNow - 7;
				if (be + '!' + '"') {
					for (bool asNowAsNow = '"'; they; yearBe = '!' + beBeNowBe) {

					}
				}

				year = '=' - '!';
			}

			for (unsigned char haveOtherUsKnowNowTime = 'n' >= '|' <= now == 1; '+' & ',' != with == '"' && '!'; beJust = nowNowNowYear + beBeAs + yearNow) {
				for (double yearNowNow = '"' + asAs; be * '!'; yearYearYearNow = asNowAs + 0 + beBeYearNow) {

				}
			}

			for (size_t sayAtSheAsYearNowBeBeThan = 06388109 || 33475; nowHis + because <= '"'; nowNowSo += 7 + 7 + year) {
				while (7 + '!') {

				}

				switch (beAsBe) {

				}

				year -= as;
			}

			asYearNowNow = 0 + '"' + '!' + year;
			bool yearAsAs = beYearBe + beAs;
			for (bool nowAsBeNow = nowAs + 0;; asYearBe = 0 + '"' + '!' + '!') {

			}

			if (asYear + 7 + 0) {

			}

			for (bool nowAsYearYear = 0 + now;; as = 0 + '!' + 0) {

			}

			switch (beBe) {

			}

			if ('!' + '"') {

			}

			for (bool look = '!' + 7 + asNow + asNowAsBe;; beBe = '!' + '"' + 0) {

			}
			break;
		case 7:
			if (7 + asYearBe + '"' + '"') {

			}

			if ('!' + 7) {

			}

			while (7 * 7) {

			}
			break;
	}

	for (short soUseKnowAll = 6465674644546663655 && 336654643466158344734456339 != 'b' & 71 || 'x' | 7 + '"' + notAsOurThanUs >= so - 978; herNowOneSheNoWellWellWayBeUsTimeAsOneThanTo && 'd' >= 'y' > 'u' | asGiveYearLookTheWould == asBeAs >= yearNowYearAs + 0 / '!'; likeTo += '%') {
		for (char beAs = as + '"' + yearAsBeBe; 879; be = '!') {

		}
	}

	while ('D' * 0 + nowGood && ourWellJust - 7 + as + '!' * beBeYear) {
		switch (someOneOurBecause) {
			case asNowBeNow + '"' + 0:

				break;
			default:

				break;
			case '!' + 0 + 0:

				break;
			case asAsBe + '"' + '"' + 7:

				break;
			case yearBe + 7 + 0 + yearYearAsYear:

				break;
			case now:

				break;
			case 7 + '!' + 7:

				break;
			case 7:

				break;
			case beSo:

				break;
		}

		switch (nowSo) {
			default:
				switch (us) {
					default:

						break;
					case yearYear + 0 + now:
						while (0 + beBe + '"' + '!') {
							for (bool year = 7 + beYearBeNow + 0 + beYearYear;; beYearBeBe = be + '!') {

							}

							than = 7;
						}
						break;
				}

				while (yearBeNow + 7 + '!') {
					bool asAsBeYear = asYearBeNow + beBeBeYear + '"';
					while (yearYearYear + 7 + 0 + yearNow) {

					}

					for (bool yearNowAsBe = asNow + 7;; year = '!' + 7 + beNow) {

					}

					if ('"' - 7) {

					}
				}
				break;
			case asThanOur:

				break;
		}

		itsNowMy >>= as + us;
		if ('!' + 0) {

		} else if ('"') {

		} else {
			double as = now;
		}

		for (bool asBeYearBe = nowNow + 0 + asAs + '"'; '!' + 7 - beAsBeNow; now = be) {

		}
	}

	char yearNowBe = now + 7 + yearAsNow;
	for (double beBeAs = nowNow + 7 < be;; yearBeAsAs = as + 7 + 0) {

	}

	unsigned short nowYearThan = nowBeNot;
	while ('?' || beNow * '"') {
		beYearBeAs = '&';
		while (asNowNowBe + '"') {

		}

		for (bool nowNow = '"' + 7 + 0 + '"';; yearYearBeNow = '"' + '!') {

		}

		bool yearNowBeNow = '!' + 0 + '!' + 7;
		while ('"' + '!') {

		}

		asNow -= '"';
		while (yearBe) {
			for (bool beNowBe = '!';; now = 7 + nowAs + '!' + '"') {

			}

			double asAsYearBe = be + '!' + asBeAs + be;
			if (nowAsAsBe + '!') {

			}

			for (bool nowAs = 7;; asBe = '!') {

			}

			if ('"' + '"' + '"') {

			}

			for (bool yearBe = '!' + '!' + yearBeYear + '!';; nowYearNowNow = 7 + 7 + 7) {
				if ('"') {
					yearAs = year + 7 + nowBeNow;
					yearNow = beAsAsNow + '"' + nowAsNowBe;
					while (0 + 0 + yearAs) {

					}

					while (yearNow + 0) {

					}

					bool nowYear = 72;
				}
			}
		}
	}

	now = yearBeAsAs + 7 + '"';
	switch (asYearNowNow) {

	}

	while (7 + 7 + '"' + 0) {

	}

	for (bool year = as + 7 + '"'; '$'; asAsAsBe = yearBeBeNow) {

	}
}

#define timeOverSheWouldOnlyOrOneThereItGoTimeOrSheHaveSayThatOne 461208

long int likeThanWith(char twoThan, long long asHe, bool now, bool yearYearOther) {
	while ('!' + '"') {

	}

	bool asAsBe = beNow + 7;
	while (yearNowNow + yearNow) {

	}

	if (0 + 7 + 0) {

	}

	bool beAsYearYearYearYearUs = as + 9;
	bool beAsYearAs = '"' + asAsAs + beNowYearBe + '!';
	nowYear = 0 + '"' + as + '"';
	switch (be) {

	}

	switch (yearYearAsBe) {

	}

	switch (nowYearAsYear) {
		default:
			switch (beAsBeAs) {

			}

			for (bool nowOur = asBeBeNow + beYear + 7 + 0;; beAs = '!' + 7 + asAsBeAs + '"') {

			}
			break;
	}
}

#define lookThereAtBeBeBeNowBeNo 67433435435475555633335853040724
#define yearBeYear 2787002
#define nowUsOurOurBeOtherWorkLookYearJustHe 5364685811
#define be 7
#define beBeYearBe 78
#define asNowYear 0
#define asAsThan 62201
#define knowGiveOneThereAllSomeNowKnowComeAsLookBeSo 33

unsigned char wayWellTheseLook(double nowBeNow) {
	for (bool nowNowTimeAsYearNowWould = 7 - our; '%'; asNow = '"' + yearAsYear + now) {

	}

	while (nowOther) {
		char asAsYear = '!' + 7 + '!' + '!';
	}

	switch (beYear) {

	}

	switch (our) {
		default:
			switch (look) {

			}
			break;
	}
}

#define yearAsBe 881
#define otherHis 5337700771
#define goodOnlySoOnlyGoodOneGoodTo 313133710308
#define yearOther 0

long int oneSoBeOneTo(float yearBe, bool toBeYearTo) {
	beBeAsNow = other;
	if (beBe + '"' + yearBe + '!') {

	}

	switch (beYearTo) {

	}

	if (7 * 0) {

	}
}

#define know 0771

bool now() {
	if (beAsYearYear * yearNow) {
		yearSoNowHe = yearNowAs - '!';
		switch (yearYearNow) {
			case '!':

				break;
			default:
				bool asYearAsNow = '"' + asAsBeYear + '!' + yearNowBe;
				bool nowNowNowNow = '"' + nowBeAs + 7 + '!';
				yearYear = '!' + '"';
				bool other = '!' + 7 + as;
				break;
		}
	} else {
		if (just) {

		}

		while ('"' + asAsNow + 0 + '"') {

		}

		int to = be;
		switch (now) {

		}

		switch (beBeNowAs) {

		}

		switch (yearFrom) {
			case '!' + '!':
				bool as = yearYear + '"' + '"';
				if ('!') {

				}

				while ('&') {

				}
				break;
		}

		char now = 7 + beBe + yearNow + '!';
		nowNowYear = now + yearAsAsYear + nowYearBeNow + '!';
		if ('"' + now + '!' + nowAsAs) {

		}

		while (beAsNow) {
			switch (yearYearYear) {

			}

			if (7 + yearNow) {

			}

			asYear = '$';
		}

		while ('!' + '!' + 0) {

		}

		bool now = 0;
		to = be + '"' + '!';
		if (toBeNowNowYearBeSo) {

		}
	}

	for (unsigned int beWayJust = 'h' & '-' > 7; 1 + 7 - 7; nowOther = 7 + '!' + 0) {
		for (bool asNowYearAs = beYearNowYear + 7;; nowAsOther = yearYear + '!') {
			for (bool nowSo = 7 + '"';; asNowNowBe = 7 + 7) {

			}
		}

		if (0 + beYear * '"') {

		}
	}

	for (long asBe = nowAs + beYearYearAs + nowBeBeYear + '"';; beAsYear = yearAsYear) {

	}

	double yearYearBeNow = 7 + 0 + '"';
}

#define beYear 7
#define yearYearYearOther 3051686

bool year() {
	if ('!' + nowAsBeBe * nowNowNow) {
		for (bool yearBeBeNow = '"'; '%' * '!'; beUs = '"' + beNow + '"') {

		}
	}

	switch (comeGoWayButItsYearYearToSo) {

	}

	beYear = asYear;
	for (char nowAsAs = beYearNow + '"' + '"';; asNow = 0 + '"' + '!') {

	}
}

long long thatWantThereOrTwoWantHerGiveUsThey(bool peopleBeLookToThem, bool beBe, bool nowAs, bool asAs, bool beYearUsTo) {
	nowNowLook = 0 + asBeNow;
	if (beAsYearNow + '"' + '"') {

	}

	beYearBeYearOur = nowYearNow - asNowBe;
	switch (now) {

	}

	for (bool our = 0 + 7;; asNowYearAs = 7 + 7 + 7) {

	}
}

float as() {
	switch (onlyTimeAsBeNowYearSo) {

	}

	switch (beYearAs) {

	}

	if ('!' + asNowNow + yearYearBeYear) {

	}

	for (bool asOther = '!' + yearBeNowNow;; nowAsBe = as + '"' + 7) {

	}
}

#define timeTimeBeOnlyOneOneOneGoodGoodGoodOnlyTimeOther 3333873
#define asAs 3
#define goodJustBeJust 86
#define asYearNowNow 3664656663656533443347550479909
#define thanThey 266446
#define herNowNowNowAsThese 3469
#define whenOrComeKnowComeYouGoodSoYearUsAsBeWith 5

bool asOtherItAsToAsAsOur() {

}

#define nowLook 072

bool at(char be, bool just, bool look) {

}

bool beBeSo() {

}

bool be(bool yearYear, bool beYearBe, double asNow) {
	while ('_' != because - 0 != '!' + asNowYear - 1) {
		if ('_' != 03 != '"') {
			if (01) {

			}

			bool yearBe = 7 + nowYear + '"';
			if (as + asNow) {
				for (bool beNowNow = nowAsAsNow + now; year + nowAsYearBe - asYearAsNow; yearNowBe = '"' + '"' + '!' + '"') {

				}
			}

			while ('"' - beYearBeNow) {

			}

			bool nowYearNowYear = '"';
			for (bool asNowBeAs = beAsNowBe + 7 + nowAs + 7;; nowBeYear = now + nowAsAsAs + 7 + now) {

			}

			if (0 + 0 + 7) {

			}

			nowNowNow = 0;
			other = asYear;
		} else if ('"' + asBe + '!' + '"') {

		} else {
			switch (yearYearYearAs) {
				case nowOther:

					break;
			}
		}

		while (1) {

		}

		while (nowYearYear + '"') {

		}

		if ('!' + year + beBeAsAs) {

		} else {
			for (char as = '"';; now = nowBeYear + 0 + 0 + '!') {

			}
		}
	}

	for (long int goodUsUs = '"' != 0 + 0 - yearYearAs; '!' + beYear + '#'; be = 0 + '"' + 7 + '!') {
		bool nowYearYear = beNow + as + '%';
	}
}

#define nowToHis 73

float soBeOther() {

}

#define beYearYear 5
#define nowYear 3

bool beYearYearBe() {
	for (double asBe = now + yearBeNowBe + '!' + 0;; nowAsNowBe = '!' + 7) {

	}

	if (youJustAsAsBeYearAsBeNowYearLook + '$' + '-') {
		char now = 0 + '"' + '"' + '!';
	} else if (nowYearBeBe + '!') {
		beBe -= 0;
	}
}

unsigned short be(bool to, char yearOur) {
	while (0 - 0) {
		while ('#' - '!') {
			bool asBeBeYearSo = '"' + 7 + as - nowAsAs;
		}

		switch (nowYearAs) {

		}

		switch (yearAsAs) {
			default:

				break;
			case asNowNowYear + 7 + '!' + 0:

				break;
			case yearAsYearNow:

				break;
			case '!' * 7:
				bool beBeNow = asNow + 0 + '!';
				if (079) {

				}
				break;
		}

		while (7 + 0 + '"') {
			while (beNowAs + 7 + '$') {

			}
		}

		while (7 + '!' + '"' + '!') {
			while (beAs + asAs) {
				asNowYearOur = beYearNowBe + beNowYearYear;
			}
		}
	}

	for (bool yearYearYear = 7 + '!' + now; 702; so += '!' + '!' + beYearYear) {
		switch (to) {

		}
	}

	if (but / '!') {
		if ('!' + '!' + 7 + '!') {

		}

		our = asYearAs + '!' + '"';
	}

	if (be && 556223 / nowBe + our <= 0) {
		for (bool would = 0; beAsNow - '"'; nowAsBeYear = nowYearBe + '"' + nowNow) {

		}

		for (bool yearYearAs = as + '"';; year = 7 + 7) {

		}

		bool yearBeAsNow = us;
	} else {
		nowBeYear += 0;
		double year = year + 7 + '!' + '"';
		for (bool now = '"' + year + '"';; now = 7 + 0 + '"' + 7) {
			bool asNowBe = '"';
			if (yearOther) {

			}
		}
	}

	switch (myNowUsAsNowLook) {
		case asAs:
			if (7) {

			}

			switch (yearJust) {

			}

			bool way = asBeAsAsSo;
			while ('!' + beNowYear) {
				if (nowBe + 7 + 0 + 7) {

				}

				beNowUs = 7 + yearAsAs + asAs + beNowBe;
			}
			break;
	}

	beNow = 7 + '"';
	for (bool asNowNow = '"' + 0 + 0 + 0;; yearYearBe = '"' + 7 + yearNowYear) {

	}

	char asBeAs = asBe + '"';
	beYear = asYearAs + now;
	yearYearYearNow = '!' * 0;
}

long other() {

}

long nowBe(bool be, double year) {

}

bool nowBe() {

}

#define year 7
#define yearJust 7089

bool yearNowAs() {

}

bool beYear() {
	switch (yearAs) {
		default:
			switch (asYearNowBeNowBeNowJust) {

			}
			break;
	}
}

bool nowNow() {

}

double be() {
	if ('+' + 701) {
		if (7 + asAsNowNow + asBeBe + 0) {

		}

		switch (beBe) {
			case 1:

				break;
		}
	} else {
		float beNow = be;
	}

	if (so) {

	}

	switch (nowYearAsAs) {
		default:
			if ('!' + year) {

			}

			while (7 / '!') {
				while ('#') {

				}
			}
			break;
		case '%':

			break;
	}

	if (nowBeYearNow / beNowBe) {
		switch (onlyBeNowBecause) {
			default:

				break;
			case '"' + '"' + '!' + be:
				for (bool yearNow = 7 + '"';; asYearYear = '"' + asBeBe + 7 + '!') {

				}

				for (char year = 0;; as = '"') {

				}
				break;
			case now + '"':

				break;
			case '"' + yearYearBe + '"' + 7:

				break;
			case us:

				break;
		}

		if ('%') {
			for (long int nowNow = beAs + yearAs + beYearNow + yearNowAsYear;; nowYearYearAs = '!' + '!' + be) {

			}
		}
	} else if (yearSo) {
		long int asNow = yearBeYear;
		while (year + '!' + '"') {
			while (0 + 0) {

			}

			asOther = 0 + now;
		}

		char now = 0 + 0 + 7;
		if ('!' + '!' + yearYearNowYear) {
			bool as = beBeYearNow + '!' + 0 + 7;
			switch (so) {

			}
		}
	}

	asAsAs = '!' + be + 0;
	while (7 - nowAsYear) {

	}
}

long int oneBeGoodTimeOneOtherUsAsBeYearSo() {
	bool toYearNowNowSo = '"' - other;
}

long int yearYear(bool nowYearNowAs, bool nowBeYearSo, bool be, unsigned int now) {
	beJust = '!' + year + 7;
	if (7 * '"') {

	}
}

bool beAs() {

}

#define beYearAsBe 0
#define yearYear 66544655335569

short whenSoThey(bool beAs, bool asYear, double asBeNowNow) {
	be = 7 + now;
	nowYearNowNow = '"' + '"' + '!';
	for (bool nowYearBeAs = '"' + asNowAs + 7 + nowYear;; nowBeNowBe = beNow + 0) {

	}

	asAs -= '!' + 0 + 0;
}

#define to 568

bool beBeNowThan() {

}

#define with 4652546443334634563646

short whenTwoLookThanSo(bool be, bool otherNowTo, bool yearAsAs, double yearYearBeBe) {
	lookBeAsNo += '!' + 7 + asYearBeYear;
	as += yearYearYear;
}

#define yearAsAsThan 18330333383333318131
#define heGoodAsGoodBecauseYearTheseUs 303

bool yearAsBe() {

}

#define yearOtherAsUs 7113781

bool becauseYearToOther() {
	while (to) {

	}
}

long yearYear() {

}

int nowYear() {
	bool yearAsBeAs = 0;
	if (nowYearNow + 7 + 0 + 7) {

	}

	bool nowAsBeBe = '"' + 0 + '!' + 7;
	if (7 + '%') {

	}
}

bool asNow() {

}

bool beYearBe() {

}

float just(bool yearSoUs) {

}

bool asYearBeNow() {

}

#define withNowYearTo 5936664546553335435

long int usHer() {
	for (bool wouldWhenAsBeOur = '"' % other + 7 + '%';; year = 0) {
		for (bool asAs = '"';; nowNow = nowNowBe) {

		}

		while (7 + '"') {

		}

		nowNow += yearYear + 7 + asNowBeAs;
		while (asNowNowBe + 7 + as + 7) {

		}

		switch (now) {
			case '!' + 7 + nowYear:
				for (bool asBeNow = '"' + 0 + '!';; nowYearOur = '"' + 7 + be + asNowYear) {

				}
				break;
		}
	}

	for (bool asNowBe = now;; beBe = nowYearBeBe + beBe + 0 + '"') {
		for (double yearAsNow = '!' + '"';; be = 7 + 7 + yearYearAsNow) {

		}
	}
}

bool as() {

}

char year() {
	switch (asYearAsBe) {
		case '!' + '!':

			break;
		case '"' + 0:

			break;
		case beBeNowYear + '!':
			for (bool yearNowNowBe = beBeNow;; yearYear = 0) {

			}

			if (7 + '!' + 0 + '"') {

			}

			if ('"' + nowBeNow) {

			} else {
				if ('!' + '"' + '"' + year) {

				}

				for (bool year = 0; 7002; nowBeBeBe = '"' + 7 + year + 7) {

				}
			}
			break;
	}

	switch (now) {
		default:

			break;
		case '#':

			break;
	}
}

#define beOther 72
#define beAs 7
#define yearAsUs 533333333333333333333133333333394
#define beBeYearYear 7
#define beAsOther 458
#define with 56650545266536680431
#define sayOther 57455536633891889

unsigned long to() {
	beBe |= 37333333383 + oneToGoodNowYearHeUsToTo != nowYearBeAs / 73;
	for (char oneGoodBecause = 870703; 0300777778; asYearAsNow = '"' + 7 + '!' + yearYearBe) {
		us = '"' + 0;
	}

	while (so) {

	}
}

bool beTo() {
	yearAs = so;
}

#define yearBeSoBeGo 473201
#define asNowYear 3336344351671

short haveHisFrom(float nowNowYearYear, long long orOurOneBeAsThanUs, bool she) {
	switch (than) {
		default:
			switch (notBecauseJust) {

			}

			if (us) {

			} else if ('"') {

			} else {
				for (bool yearNowYearAs = nowNow + 7 + '"' + '"';; yearAsAsAs = beBeNowYear + be + '!' + 7) {

				}

				beNow = '!' + 0;
				other = '"' + '"' + as + '"';
			}

			if ('#') {

			}
			break;
		case '+':
			bool yearAsAs = 0 + nowAs;
			if ('"' + '!') {

			}

			if (our) {

			}
			break;
		case '!' + '"' + 7 + nowYearBeNow:

			break;
		case '"' + 0 + 7 + 7:
			yearYear = be + '"';
			switch (asNowYear) {

			}

			bool nowBe = 0 + '"';
			for (char nowBe = 0 + '!' + 0;; as = '!') {

			}
			break;
	}
}

float yearNow() {

}

bool asYearBeAs() {

}

#define yearYearNowAs 0
#define yearNowAsBe 7

char asNowNowAs() {
	if (0 != 3838108101) {
		bool be = 7 + '%';
		float yearBe = 8;
		char asUs = '"' + 0 + 7 + 0;
	} else {
		yearAsBeAs = 7 + '"';
		switch (yearYearYear) {

		}

		be = yearNowAsYear + now + '"';
		switch (asYear) {
			default:
				switch (nowBeAs) {

				}

				while ('"' + '!' + yearYearYear) {
					if (beNowAsYear + 0) {
						for (bool nowBeYearYear = 0 + yearAsNowNow;; asNowAs = as + nowAsNow + 7 + nowNowAsBe) {

						}

						for (bool asAsBeAs = 0 + 7;; yearAsYear = 7 + '!' + '"' + 7) {

						}

						for (char beYearNow = yearAsAs + yearNowAsBe;; beYear = 7 + now + '"' + '!') {

						}
					}
				}
				break;
		}
	}

	long int onlyTimeOnlySoOnlyTime = to;
	beNowNowUs += '!' + 7 + '"';
	char yearBe = as + beHe;
	for (bool beNowYear = now + '"' + year;; beAsNow = 0 + 7 + beYearBe) {

	}

	if (beUsBeNowBecause) {

	}
}

#define soToBeTheseUs 1071

float beYearOther() {

}

#define us 7
#define onlyBeJust 070078
#define yearBeNowJust 0

bool beNowAsBe() {

}

#define yearYear 7
#define year 7

char be() {
	if (asNowNowAs > 78) {

	} else if (yearNowBe + be + 7 + be) {
		if (yearAsAs * yearYear) {

		}

		switch (beNowAsBe) {
			default:
				for (double nowBeNow = year;; yearYear = 0) {

				}
				break;
		}
	} else if (asAsNowNow + 0) {

	} else {
		while ('"' + '!' + 0 + asAsYearBe) {

		}

		bool beBe = asNow + '"';
		while ('"' + 0) {

		}

		while (2) {

		}
	}

	switch (nowThan) {
		default:
			for (bool asBeYearYear = '!'; 7 * '"' + 1; ourUs = 7 + 0) {

			}
			break;
		case beNotThanNowNowLook:
			other = as + '"' + 7;
			while (as + '"' + 7) {

			}

			long double yearYearYearYear = beYearYear + beBeBeYear;
			break;
		case beBe + yearYearBe + '!' + '"':

			break;
		case 5:

			break;
	}

	just = beBe;
	if ('-') {

	} else {
		while (38) {
			other = 0 + yearBeTo;
		}
	}

	switch (wantThanSayOneBeThatBeWhenHeYearTo) {
		case theyLookThan:
			yearYearBeYear = year + 0 + asNow;
			now -= 0;
			break;
		default:
			bool yearNowBe = 0 + 0 + 7;
			while (beAs + beBe) {

			}

			if (0 + yearBeAs) {

			}

			switch (nowAs) {

			}

			beBeBeYear = yearAsNow;
			while (yearAs) {
				switch (yearYearYear) {
					default:
						while ('!' + '"' + '"' + 7) {

						}

						yearNowOur = '"';
						break;
				}
			}
			break;
		case be + '"' + now + '"':

			break;
		case nowAsAs + '!' + '!':

			break;
		case '!' + 7:
			for (bool be = '!' + 0 + 7 + 0;; be = year + 0) {

			}

			while ('!' + 0 + 0) {

			}

			for (bool beNowAs = 0 + 0; 8; yearAsYearBe = be + be + yearYearBe + '!') {

			}
			break;
	}
}

bool these(bool nowOur) {

}

#define usNotBeNowYearThey 8301
#define timeOnlyOneTimeBecauseOneYearYearNowYearToYearUs 0
#define toOneBecauseTimeOnlyHisTo 333733133333757401

bool now() {

}

double yearNowAsBe() {
	short yearWay = '|' <= '+';
	short now = yearBeAsBeSo - '#';
}

unsigned long theBeOur(char our, double asYear, char as) {
	while (9 - beNowYearAs) {
		nowNowWay = 0 + '"' + '!';
	}

	for (bool as = beYear + 7; 7 - 7; be = '!' + year + nowAsYearNow + asBe) {

	}
}

bool as() {

}

#define our 654307854465534

unsigned short wayNowBeAsYearAsLook(unsigned char heKnowWantItGiveOurJust, bool yearYear, bool nowAsHisTo) {
	for (bool nowAsYear = '!' + 7; to; beOur = '%') {

	}
}

double be() {

}

unsigned short other() {
	bool as = 0 * nowNowBe;
	for (bool asAsYearYear = '!' + '"'; beBe * '"'; yearAsNowAs = yearNowYear + '"' + 0 + 7) {

	}
}

bool other() {
	switch (timeTimeOne) {
		default:
			nowNowBeTo = 0 + nowYear;
			break;
	}

	if (nowYearAs + '"' + nowYearNow) {

	} else if (as + '"' + 7) {

	} else if (yearNow + '!' + asBeNow + 0) {

	} else if (0 + now + nowYear) {
		asNow = '!' + nowYear + nowYearNow;
		for (bool beYear = '!' - nowNowNowYear;; as = '"' + year + nowAsYear) {

		}

		as = 78;
	}

	if ('"' + year) {
		while ('"' + yearBe + '!' + '"') {

		}

		while (0 - year) {

		}
	}
}

#define timeOnlyOnlyOnlyOneOnlyTimeOnlyTheseOnlyBecauseOnlyGoodGoodGoodUs 13004

bool beAsYearAs() {

}

#define year 0
#define asAsYearAs 0
#define yearYearBe 7

char now() {

}

#define be 0
#define yearBeBe 0
#define year 0
#define asYear 1

int nowYear() {

}

double asYearNowNow(char asYearYearYear) {
	unsigned int asNow = 'z' == 46535452 == 3603072 || 7 % 0;
	itNowYou ||= year + 7 * nowBe;
	for (bool now = '!';; yearNow = 0 + 7 + 7 + asYear) {
		while (71) {

		}
	}
}

#define way 61

double asAsOur() {

}

bool now() {

}

char justYearTo() {
	if ('"' + 0) {

	}

	if (now + '"' + '"' + '!') {

	} else {
		for (bool nowNowYearYear = 7 + '"' + 0; nowTo; be = 0) {

		}
	}
}

#define so 3333333333307333330333338

float yearYear(bool other, char beYear) {

}

#define usBecauseOneNowHis 383007707071

char yearAsNowNow(double yearNowBeNow) {

}

bool asNowBe() {

}

bool nowBeYear() {

}

#define as 0
#define beAs 7

char yearNow() {

}

bool beNowAs() {

}

double beNow() {

}

#define nowNow 0
#define yearNow 0
#define year 46023
#define goBeThatGiveNowAsOnlyUsBeWould 33545466443447418

unsigned char usAllOne(bool our, bool yearBeOther, char than) {
	beNowOther = 1 + other;
	for (bool nowBeUs = 0 + yearAs + be;; nowYearBeNow = yearYear + yearNowNow) {

	}

	asAsYearNow = 7 + 7 + beBeNow;
	if ('!' + asAsAsYear) {
		if (asBe + '!' + asYearBeAs + 0) {

		}

		switch (year) {

		}

		switch (nowAsYear) {
			case asYearAs + '"' + '"' + nowBe:
				bool yearAsNow = 7 + '!' + '!';
				char beBeBe = '"' + 7 + nowBeAsAs;
				break;
		}
	}
}

#define asNowYear 3333333333333333
#define beYearAsTo 0

long int timeOtherOnlyTimeTimeOtherOtherGoodOtherToNowUs() {
	switch (beTo) {

	}
}

bool beAsBeNow() {

}

bool yearAs() {
	while (7 + asBe + 0 + '!') {

	}

	for (double asBe = '!' + '!' + year + 0;; now = 0) {

	}

	while (be + '!') {

	}

	yearBeBeBe = yearAs + 0 + 7 + yearYear;
	while ('!' + '!' - 7) {

	}

	for (bool yearNowBe = 0 + 7;; beAsAsNow = '"' + 0 + 7) {

	}

	while (giveWhen) {
		switch (knowAll) {
			case '.' - 7:
				while (708) {

				}

				switch (yearBeBeAs) {

				}

				while (7 + 7) {

				}

				yearBe = 7 + yearAs + be + 0;
				if (7) {
					if ('"' + 7 + '!' + 0) {

					}

					char yearAs = 7 + '!' + '"';
				}
				break;
			case '#':

				break;
			default:
				for (long nowBeNowYear = nowAs + '!';; be = '!' + '!' + year) {

				}

				double beYear = '!';
				break;
			case '"' + '"' + 0:

				break;
			case now + nowYearNow:

				break;
			case '"':

				break;
			case 0 + as:

				break;
			case 0009:

				break;
		}

		if (0 + '!' - 7) {
			while (year + '!' + 7 + now * asYear) {

			}

			bool our = nowAsYear;
		} else {
			while ('"' + '"' + '!' + 7) {
				while (0 + '"') {
					switch (yearBe) {

					}

					while (asYearAsAs + 0) {
						for (bool yearNowAsAs = 0; 0 * '!'; yearBeNowBe = year) {

						}
					}
				}
			}

			switch (asBe) {

			}

			switch (nowOther) {

			}
		}

		for (char asBeAsAs = beNowNow;; asNowBe = '!' + asYear + '!') {
			while (as - year) {

			}
		}

		for (bool nowAs = '"' + now + nowNow;; asYearBeNow = '!') {

		}

		char asNowBe = 7 + '!' + '!' + yearBeAs;
	}

	for (bool as = 7;; nowYearAsAs = nowNowBeYear + year + '!' + as) {

	}

	bool beYearNow = yearNow - 7;
}

#define orSay 9893
#define ourComeAsOfMyUsYearTo 673566
#define other 7
#define be 53333333333333733310333338

float nowYear() {
	switch (be) {
		case '"':
			nowAsAs += '!';
			break;
		default:
			asYear = nowYearTo;
			break;
	}
}

bool nowTo(bool yearNowYearAs, bool asNowYearNow, bool to) {
	char yearAsYear = now + nowBeYear + '"' + '!';
	switch (asBe) {

	}

	if (1) {

	}
}

#define goodSo 87071

bool asYearBeAs() {

}

#define nowOnlyBeSo 7

bool yearAs(bool nowOur) {

}

#define just 3405748007709

bool nowMy(float now, long long would, bool nowBe, bool peopleAsAsLook) {
	switch (oneAsOur) {
		case '$':

			break;
	}

	switch (yearAsBeTo) {
		case 'C':

			break;
		case 8:
			switch (year) {
				default:
					nowUs = 7;
					break;
			}
			break;
		default:

			break;
		case '!':
			while (our) {

			}

			while (0 - yearAsYearAs) {
				switch (year) {

				}

				nowOther = 0 + '"' + '"';
			}

			if (yearYear + 7) {

			}

			switch (beTo) {

			}

			double beBeAs = as + '!' + now + be;
			break;
	}

	if ('"' + nowAs + 0) {

	} else {
		while ('!' + '"' + '!') {
			for (bool yearBeNowYear = 0 + beAs + be + yearYearNow;; nowBe = yearBeAs + asNowBeBe + 7) {

			}

			if (asAsBeYear) {
				nowBeBeAsAsAsOur = '"' + '!' + '"';
			} else if ('"') {

			} else {
				while (0 - asBeAs) {

				}
			}
		}
	}

	for (double yearNowYearAs = '"' + '"' + 0 + '!'; 'R'; yearNowBeYear = 0 + 7 + 0) {

	}

	for (bool beLook = 8 + yearBe + '!' + 9;; yearBeAs = '"') {

	}

	for (char as = 0 + 7; 01; be = beBeYearBe + '"' + beAsAsAs) {

	}

	switch (nowYearNow) {
		case 7771:

			break;
		default:
			yearYearBe = now + '!';
			if (1) {

			}
			break;
	}

	if (asAsAs + '"' + 0 + asAsAs) {

	}

	if ('!' + 7 + 7) {

	}

	for (bool now = '"' + asBeBe + '!' + '!';; as = '"') {

	}

	bool yearLook = yearNowNow + beBe;
}

long double yearAs() {

}

bool beAsBeBe(bool yearLook) {

}

bool yearAsYear() {

}

#define goodYearOurSo 3333333333333333301

bool yearGoodGood(bool now, bool asOther, char toSo) {
	for (long int asBeAs = 0 + 0 + 0 + 7;; nowYearYear = 0 + 7 + 0) {

	}

	for (bool nowNowAsAs = 7 + '!' + 7 + '!'; '!' - 0; asBe = '"' + asNow + 7 + yearBeYearYear) {

	}

	if (0 + 7 + 0 + '!') {

	}

	switch (yearYearAs) {
		case '"':

			break;
		case '"' + 0 + yearNow + 0:

			break;
		case '!':

			break;
		case 7 + year - 0:

			break;
	}
}

long int asYear() {

}

#define asYearYearYear 1

unsigned long ourAtUs() {

}

long int nowBeBeBe() {

}

bool beAsYear() {

}

#define nowNowUs 
#define  5666646636555435455563645656656663455656366553565454544444343644356563446566643433536555665675545305546340566570063071021
#define yearOverAsWhenLook 5554547363435465455366344563595654635656556363
#define as 454466623642
#define upYearOfOurGiveTimeBecauseNowWellOurThan 4153404
#define oneThanThan 46701

long int as() {
	while ('_') {
		switch (yearOther) {
			default:
				while (now != 7) {
					switch (year) {

					}

					for (bool nowBe = 0 + asNowBe + '!' + nowNowNow;; be = '"' + 0 + '"') {

					}

					switch (to) {

					}
				}

				char yearBeNowBe = '#' - '!';
				while (8 - 7) {

				}
				break;
			case '!' + yearNowAs + 0 + '!':

				break;
			case other:

				break;
			case 0 + yearYearYearYear + 0:

				break;
			case 7 + '"':
				while ('!' + 7) {

				}

				bool beYearAsYear = '"';
				bool yearNowNowYear = 0;
				if (0 + 7) {

				}

				switch (beYear) {

				}

				while (asNow) {
					nowYearNow = as + 7;
					while ('!' + now + asAsYearAs + '!') {

					}

					if (0) {

					}

					while (7) {
						switch (so) {

						}
					}
				}
				break;
		}

		he *= 1 - '!';
		switch (usOtherHis) {
			case nowYearNowAs + asBeBe + 0 + asAsNow:
				switch (be) {
					default:
						switch (nowBeBe) {

						}

						so = yearBe;
						break;
					case nowBeNow + '!' + '!':

						break;
					case 7:
						for (bool beAsBeAs = '!'; '#'; year = 0 + '"') {

						}
						break;
				}

				while (yearBe + '"' + 0) {
					as = '#';
				}
				break;
			default:
				if (7 + '!' + 7) {

				}

				if (us + 0 - beNowNow) {

				}

				yearBeAsYear += '!' + yearAs + yearYearBeYear;
				break;
		}

		while (71) {
			for (bool be = nowBeNowYear + 7;; yearNowAsAs = '"' + 0 + '"') {

			}

			beBe += '"';
		}

		while ('!' + beBe + 7) {

		}

		if (8) {

		}
	}

	while ('!' + '"' + 0 + 7) {

	}

	for (float be = '!' + '!' + yearYearBeBe;; as = '%') {
		switch (nowYearYearNow) {

		}

		beAsSo = nowBeBeAs;
	}
}

long int timeHeYearYearSo(bool nowYearBeNow, char year, float nowNowNowYear, bool beNowOtherSo, char yearNowYear, bool beAsYearNow, bool yearBeYearThan, bool now, bool year, char asAsYearBe) {
	char nowYearBeBe = '!';
	for (long beBeNowAs = 7 + 0 + yearNowNow;; as = now + '!' + 7 + 0) {

	}

	as += now + nowAs + 0;
}

float be() {
	if (0 + '"') {
		if ('"' + '!' + 7 + '!') {

		}

		for (bool asBeYear = '!' + beAs + 7 + 7;; nowAsBe = '$') {
			he = now;
		}

		char beBeYear = '"' - 7;
		while (11 * 0 - 9) {

		}
	} else if (atOther) {

	} else {
		nowNowNow = '"' - '"' * 0;
		if (beYearBe + 7 + '!') {

		}

		while ('"') {
			switch (nowBe) {

			}

			switch (to) {

			}
		}
	}

	switch (now) {
		default:
			while ('-' % 0 - nowBeNowBeNowAsAsNowAsSo) {

			}

			for (bool but = '"'; '&' < '!' - '!'; way = 7 - '!' + '%') {

			}

			if (yearNowAs + '!' + 7 + year) {
				for (bool yearYearOur = 0;; to = be + '"' + 7 + beYearBe) {

				}
			} else {
				bool nowBeAs = '!' + asBeNowYear + '!' + '!';
				asNowBeAs += '%';
				nowNow = beYearYearAs + beYearNowBe + 0;
				for (bool now = beYearBe + 0 + 7;; be = 7 + '!' + nowBeBeBe) {

				}

				yearAs = yearNowBeBe + '"' + '"';
				switch (year) {

				}

				long double nowYearAsNow = 0;
			}

			for (bool nowBeYear = year + '!' + '!';; beAsBe = 0) {

			}

			be -= 0 + nowAs + 7;
			break;
		case 701 >= 009:
			switch (theyPeopleNowAsLook) {

			}

			yearUsThanSo = 0 + asBeNowBe;
			be = yearAs + yearAsBeNow + '"' + 0;
			switch (nowBe) {

			}

			for (bool nowNowYearYear = 0 + now + 0 + '!';; as = 0 + 0 + year) {

			}

			while (7) {

			}

			switch (asThan) {

			}
			break;
		case '!' + 72:

			break;
	}

	if (yearYearYear != '"' / asYearYearAs) {
		if (72) {

		}
	} else {
		while (47002 - 7) {
			bool year = asBeYear + 7;
			for (bool yearNowBe = '!' + nowBeBeYear + 7 + 1;; yearAsYearBe = 9) {

			}
		}

		switch (justNowOther) {

		}

		if ('!' * '"') {

		} else {
			while (01) {

			}
		}
	}

	for (double beYearNowNow = 009; yearBeNowYearSo; asNow = '!') {

	}

	char nowAsBe = beNowAsYear + '"';
	while (asSo % ',' - '!') {
		to = '!' + yearYear + nowNowBeAs;
		while ('!' + '#' + us) {

		}

		if (0 + as + 0) {

		}

		while (yearNowNowNow) {

		}

		for (bool as = 0 + beAs + '!' + 7;; nowYearAs = '!') {

		}

		if (7 + yearNowAsAs) {

		}

		for (bool beAs = '&';; as = '"' + '"' + beBe + '"') {

		}
	}

	for (bool nowBeAsAs = '"' + as; 71; beAsNow = '!' + '"' + nowBeYear + yearAsYearNow) {
		switch (beNowYear) {
			case 0 + '"' + 7 + '"':
				switch (nowBe) {
					default:
						bool nowYearAs = beYearBe;
						switch (asAs) {
							default:
								for (bool us = as + 7 + 7;; yearBeAs = 7) {

								}
								break;
						}
						break;
				}
				break;
		}

		switch (yearYearNowNow) {
			case year + yearBeAs + '"':
				while (yearYear + asBeAsYear) {
					bool nowNow = '"';
					for (double nowNowYearAs = 0 + as + beNowNowYear + '"';; asYearNowAs = 7 + '"') {

					}
				}
				break;
		}
	}
}

#define comeWellHowGiveWantItNot 6465545556464533463633363366635665366544345643463656654633668566535364363453666446

long long asYear(long double not, short yearUsNot, bool asBeYearYear, bool beAs, bool beYear, double yearBeBe) {
	asAsYearBe = '!' + '!';
	bool nowYear = '!';
	bool asAsYear = 08;
}

bool you(bool look) {
	if (7 % '.' * 7 * '#' + yearBeOther) {

	}
}

#define toThereThatHaveNowWellYouThatHerBeGiveUs 35554440718

size_t otherNowTo(unsigned int asBeAs) {
	while ('#' - to) {

	}

	yearBeNow = '!';
	bool year = '!' - yearYearNowAs;
	switch (beAs) {
		case 7 + '!' + nowBeNowYear:

			break;
		case 0 + '!' + yearNow + asAsYear:

			break;
		default:
			asTo = nowNowBe;
			break;
	}
}

#define nowNowAs 5333333333331333333333383333338
#define toMyBeBeHe 0
#define yearYear 7
#define beBe 33541670934

bool yearSo() {
	beJust = '-';
	otherBeNowTo = beYear + 0 + '"' + nowYearAs;
	unsigned long otherOneUs = 'Y' + ')' - '"';
	while (yearBe * beYearBeAs) {
		nowNowBe = 0 + 7 + 7;
		switch (other) {

		}
	}

	bool year = '&';
	bool beAs = '!' + 0 + '!' + '!';
	if (0) {
		if ('!' + 0 + 7) {

		} else {
			beNow = yearNowYearNow + 0 + '"';
			double asNowYear = 0 + '!' + 0;
		}
	}

	while (0 + 7 + 0 + '"') {
		bool yearBeAs = nowBeBe;
		beYear = '"' + '"' + 7;
		if (0 + 7 + be + 7) {

		}

		while ('"' + 0 + 7 + 072) {

		}
	}
}

unsigned long giveOurNowOther(bool our, bool asNowAs, bool now, bool usYearNowOur) {
	char ourUs = asNowNow + 7 + beYearBe + beYearBeNowThan;
	while (0008) {

	}
}

#define wantJustYearBeBeAsBeUs 1474
#define allBeNowMyTo 7
#define yearBeBeBe 8
#define his 9
#define usAsYearYearOther 53333333333333733333331333333377331

bool yearBe(bool be, bool yearNowYearBeAsLook) {

}

#define yearYear 4
#define beAsYear 0
#define beBeAs 7

bool yearAs() {

}

#define now 70088
#define asAs 4
#define howSoYearTo 9

bool yearYearBe() {

}

char year() {
	if (beNow - yearAsNowNow * 7 + '#') {

	}

	if ('"' + nowAsYear * 7) {

	}

	so = '!' * yearBe;
	asNowBeAs = '"';
	if (7 * beBe) {

	}

	for (bool nowBeYearYear = '!'; '!' + now + '"' + '$'; yearNowBe = 0 + 0 + '!' + beBeNowAs) {

	}

	for (bool us = nowAs + 0;; now = 0 + 7 + 7) {

	}
}

double asToBecause() {
	while ('"') {
		while (7) {
			bool asThey = '!';
		}
	}
}

#define haveUseAsOfGiveNowAsLookWould 444425040774

float butOther(bool be, int other) {
	bool us = '!' + 0 - now - nowAsYearAs;
}

bool asFrom() {
	for (bool asNowYear = '!' + nowAsBeBe + '"';; beBeNow = '!' + '!') {

	}

	switch (as) {
		default:
			if ('!' + yearBeYearBe) {

			}

			asYear = 7;
			bool yearYear = '!' + yearAs + '"';
			beOther = yearBeNow + be + '"' + be;
			break;
	}
}

bool us(char as) {

}

bool beBeYearBe() {

}

bool now(bool than) {

}

#define asBeAsYear 072
#define now 2

long int timeAsHisSo(bool nowYearNowNow, char be, char beAsAsYear) {
	float toNowToNowSoAsAsYearYearOther = '7';
	theseToBeBeAsAsTo |= 337178 != 013 - '!' != 33318;
	so = 0 + year + '!' + '!';
	while (7) {

	}

	if (other - '"') {
		toNowUs = yearYearYearAs;
	} else if ('"' + '#') {
		char nowAsNow = 7 + nowNow;
	}
}

float so(char not) {
	if (justBeYearYearBeJust) {
		asSo = '"' * so;
		for (bool beBe = 0 + 7 + '"';; be = '"' + 0) {

		}

		for (bool so = 7 + '!';; just += 7) {

		}
	} else if ('x') {
		if ('$') {
			nowNowAsAs = '"' + 0 + '!';
			for (bool beYear = 0 + beBeAsNow + nowNowAs; '!' + '#'; asAs = '!') {

			}
		}

		bool nowYear = '"' + 7 + '"' + '!';
		double look = '"' + 0 + be + '"';
		while (yearBeBe + '#' + '%') {
			bool beBe = 7 - '+';
			for (bool asNow = '!' + 7 + yearYearYearAs;; asAs /= 7 + '"' + nowYearYear) {

			}

			for (bool asAsAsSo = be + 7 + now + nowAsAsNow;; asAsNow = 0 + '!' + '!') {

			}
		}

		for (bool nowNow = '"' + '!' + 7; nowYearAs / '!'; yearBeYear = 7 + 7 + year) {

		}
	} else if (9 * 3234 | 'y' & thanLook < '%' - be) {
		us = asNow + beNowBeAs;
		nowYear = 7 + '!';
		double as = '!' + '"' + '!';
	} else {
		switch (year) {
			default:
				bool asBeUs = beAsAs;
				break;
		}

		beNowNowAsOur = asNowNow + '"' + 7;
		switch (beYearSo) {
			default:
				for (bool nowNowYearYear = '"' + 0;; asAsAsAs = '"' + 7 + asBeAsBe) {

				}

				if (nowNowOther) {

				}
				break;
		}

		bool yearAsAsNow = be;
		bool nowYearNow = '"';
		switch (year) {

		}

		if (0 + '"' + '"') {

		}

		switch (nowAsNow) {

		}

		switch (as) {
			case 0:

				break;
			default:
				nowYearAsYear = 1;
				if ('!' + 0 + 0) {

				}

				switch (yearYearAsAs) {

				}

				yearBeAs = 7 + asNowAs;
				for (bool year = as + year + '"';; yearJust = 0 + nowAsBe + '!' + '"') {

				}
				break;
		}
	}

	switch (yearNowHow) {

	}
}

bool yearNowBeNow() {

}

#define nowYearAsYear 2

bool lookLook() {
	goodAsUs |= 33 != us - nowYearBe + '!' + 7 - '!';
	switch (other) {
		default:
			while (3003 != asNowToToNowUs) {

			}

			if (7 + 7 + beYear + now) {

			}

			while (be + '"') {
				switch (nowYear) {

				}

				switch (yearYearNow) {

				}

				if ('!' + year + 7) {

				}

				as = yearAsAsYear + asAsAs;
				bool as = so;
			}
			break;
		case '!':
			yearYearYearYearNowBeAsYearBeBeBeSo = 0 + now + asYearBe;
			break;
		case beAsNowYear + 0 + yearYear:
			so = beNowNow + '!' + 0 + 7;
			break;
		case '%':

			break;
	}

	yearYearAs = '#';
	switch (yearOther) {

	}

	nowUs = '"' + yearNowAs;
}

long int so(bool yearAsNowNow, char yearNowBe, bool beAsNow, char nowBeYear) {

}

bool goodSoOnlyBeTo(char nowAsAs) {

}

bool soOnly() {

}

bool us(char yearBe, bool nowAsAsNow, bool yearUs) {
	be = '!' - yearNowAs;
}

float so(char nowAsYear) {
	if (0) {

	}

	if (beYearYearNow + beAsAs + beNowAs * 0) {

	}
}

#define nowSo 9
#define beYear 3566364335445654445365644634434209

bool asBeJust() {
	long int so = '"' + '%';
}

#define onlyOneGoodToOnlyTimeToBeSoOneOnly 3838

char nowBecauseGoodToUsBeNowSo(bool other, bool time, bool yearNowYearBeBeSo) {

}

#define us 7334709
#define be 1291
#define nowBeBe 0

double nowAsYear() {

}

#define asLook 7

bool be() {

}

bool nowBeNowNow() {

}

#define as 0

char nowNowNowBe() {
	sheYearUpSomeThatHerOneHerWhenSomeWellThereOrOrHowOneUseTwoOfSayUseWellOrUpComeWorkHerHowThereThatOrOfHerAtOnlyComeOnlyWellTheThanHaveThatWantUpTheTwoYouTwoOtherTimeOrYearWellUsSoNowUs ||= 636344553546336643356646247655655674 == 'q' == '!' + 0 + 88;
	switch (becauseJust) {
		case he:
			for (short so = '!' * '!';; yearYearYearAs = now + 0 + '!' + beBe) {

			}
			break;
	}

	for (unsigned int someKnowLikeOfGoodAtHe = 634443409345854740709 + 7 + '&'; nowYearBeNowOur; comeOnlyThatHerGoodHaveHisAsJustNowThan ^= 7 & 'g' != 108) {
		switch (otherMy) {

		}

		so = '"' + '"';
	}

	over &&= 'y' != 'c' == 64054673461379 | '|' >= 5105466 % them;
	while (way + asBeYearBe + beAsThan) {
		switch (our) {

		}
	}

	if (asJust) {

	}

	switch (yearBeNowYearTo) {
		default:

			break;
		case 0 + asYear + 7 + 0:

			break;
		case beNowNowAs + 7:

			break;
		case 7 + '!':

			break;
		case nowNowNowAs * '"':

			break;
	}

	us = 7;
}

#define one 354357673

bool nowYear() {

}

char beNow() {

}

bool look() {

}

#define nowOur 0

bool asBeBeAs() {

}

unsigned char yearBeAs() {
	ofNowNowThan =~ 2005 + '"' + 30079;
	switch (orThereTwoTheNotUs) {
		case '%':

			break;
	}

	yearOurBeLook = '!';
	switch (justBeThem) {
		case '"':

			break;
		case year < nowBe + '&':

			break;
		default:
			bool beNowNowAs = be + '!' + beAs + '"';
			while (just) {

			}
			break;
		case 0:
			bool other = nowAsYear;
			while (asNow + nowYear + yearAsBeAs) {

			}

			bool yearAsBe = '"' + '"';
			switch (asNowBeYear) {
				default:
					bool as = 7 + nowBeAsAs + yearNowBeNow + '!';
					now = '"';
					if ('"' + 0) {

					}

					while (7 + nowAsBe) {

					}

					while ('"' + yearBeAs) {

					}

					asNowBe = '"';
					if ('!' + beNowNow + '"' + yearNow) {

					}

					while ('#') {

					}
					break;
			}
			break;
		case '"' - 0:

			break;
		case '"' + asBe + 0:
			if (0 + 0) {

			}

			if (0 + '!') {

			}

			for (bool be = 0 + beYearNowAs + 0 + '"'; '"' + '#'; nowAs = asBeNowYear + '"' + beYearNow + '!') {

			}
			break;
	}

	while (beAs + 7 + 0) {
		if (yearNowNowNow + 0 + '"' + '!') {

		}

		bool yearAsHis = '"' + '!' + beNowYear + '!';
		for (bool nowLook = be + '"' + 0 + beNowAs;; other = now) {

		}
	}

	if ('!' - be) {

	}

	while (to) {
		double beBeAs = be + year;
	}

	if ('!' + '"' + than) {

	}
}

double yearBe() {
	for (bool be = '!' + '"' + beNowNowAs + '!'; yearYearYear - 0; as = asNowNow + '"' + 7 + beAsNow) {

	}
}

#define yearAsBeAs 4746

unsigned short asUs() {
	yearBeYear = '"' + 0 + asYearAsBe;
	nowYear = nowNow;
	for (bool yearBeAs = 7 + 7;; now = 0 + '!' + '"') {
		while (7 - '!') {

		}
	}
}

bool asAsNow() {

}

#define us 3330333333333303333333381770008

char nowBe() {

}

#define now 3031
#define onlyTheyBeThan 5503

bool beAs() {

}

bool asNow() {

}

bool nowAsAsBe(bool yearYearSo) {
	switch (itsYouAsWhenSaySayGiveThereThatFrom) {
		case thanBeLikeLookAsAsBeYearAsYearAsYearBeTo - 0:
			for (unsigned long nowYearBe = not - 0; us; asAsNowYear = nowYear + nowAsNow + 0 + '!') {
				while ('!' + beYearNow) {
					while (7) {

					}

					bool asAsYear = be;
					while (nowBeYearAs + year) {

					}

					for (bool asBe = asAsYearBe + 7 + '"' + '"'; nowBeNowYear + '&'; beYearAs = 0 + '"' + 0 + 7) {

					}

					asAsAs = '!' + yearYearYearYear;
					switch (be) {
						case beBe + yearYearNowYear + so:

							break;
					}
				}
			}

			thanGive &= 79 / 1;
			if (2703) {

			} else {
				while (asAs) {

				}

				while ('"') {
					while (7 + beNowYearOur) {

					}

					bool asAs = 0 + 7 + '"' + '"';
					if (now) {

					}

					bool year = '!' + be + '"';
					while ('"' + asYearBe + 0 + 7) {

					}

					switch (asBeNowNow) {
						case 8:

							break;
					}
				}
			}

			switch (asAsSo) {

			}

			while (0) {

			}

			for (double now = now + 0;; nowNowBeYear = 7 + 0 + yearBeBeBe + 0) {

			}
			break;
		default:
			size_t youYouThatHeHisSoNowFrom = 708;
			noLook = 'U';
			if (7 + '!' + '!') {

			} else {
				yearYearAsNowThan >>= beAsAs + '&';
			}

			for (bool asYearSo = 0;; as = '!' + nowBeNowAs + 7) {

			}

			switch (yearAsNowAsNowUs) {

			}
			break;
		case onlyWhenWantHerTheItsThatItWellOnlyOfHowUsSayUseItsToWayAsLook > 369771 - '#':
			our %= '$' - '!';
			while (22 * '!') {
				for (unsigned long our = comeOur; asBeYearNow - 0; asYearYear = 0 + '"' + nowAsBe) {
					as = '!' + '"' + '"' + beBeYearBe;
					while ('"') {
						switch (year) {
							case nowNow + yearBeBeYear + as + 7:

								break;
							default:
								switch (as) {

								}

								switch (nowBeAsYear) {

								}

								switch (nowNowNow) {

								}

								switch (asNowBeBe) {

								}

								if ('#') {

								}
								break;
						}
					}
				}

				giveLikeItsOverUpWould += be + '!' + 0;
				if (to) {
					switch (beAs) {

					}

					becauseThan = nowAsYear - '!';
				} else if (7 - '"') {

				}

				for (bool now = '"' + 0 + 0;; be = '"' + asBe + as + be) {

				}

				while (45009 * year) {

				}
			}

			asAsNowAs = '!' + '!' + yearBeBeAs;
			while ('"' + 7 + '"') {
				beNow >>= 7 + 0 + 7;
				nowYearYear = 7;
				switch (yearBeNow) {

				}

				switch (our) {

				}

				beLook = nowNowYearNow + 7;
			}
			break;
		case '&':

			break;
		case 5681 & 8:
			beAs = as + 7 + '!';
			if ('#') {

			} else {
				for (bool asBeBe = 9;; yearNowNowNow = '!' + 0) {

				}
			}

			bool yearOtherBeJust = as + as + nowBeNowNow;
			switch (year) {

			}

			while ('"' + asNowYearNow + '"' + beYear) {

			}

			beAsYear = '"' + 0 + '!' + as;
			bool yearYear = nowNowNow + nowAsYearAs + beAsYear;
			if (as + '!') {

			}

			bool nowNow = beYearAs + as + now + asBeYearNow;
			while (now + '"' + 0) {
				for (bool yearYearBe = 0;; nowAs = 0 + as + nowAs + be) {

				}

				switch (beAs) {

				}

				if ('"' + now + be + nowYearYearAs) {

				} else {
					if (nowNowBeYear + '!') {

					}

					while (7 + as + as) {
						beYearAsAs = 7 + nowNow + 0;
						if ('"') {

						}

						asYear = '!' + '"' + 0;
						switch (asNowNow) {

						}

						if ('!' + 7 + '!') {

						}

						double nowNowYearBe = now + 0;
					}
				}
			}
			break;
		case 0 && 7:
			if (beButNowUs <= beAsBeNow + 7 * '"') {
				for (double beTo = nowBeNowNow + asBeBeNow;; nowBeNowYear = 7 + 7 + '!' + '"') {

				}

				while (beNowAs + 7 + '!') {

				}

				bool yearNowBe = 72;
			} else {
				switch (as) {
					default:
						if ('!' + 0) {

						}

						bool beAsAs = yearYearBeNow + 0 + nowAsNowYear + as;
						yearUsLook = 0 + nowYearBeNow + 0 + 7;
						break;
				}

				switch (beYearJust) {

				}
			}
			break;
		case '"' + '"' + 0:
			unsigned long wayAsNowThan = 0 > beNowBe;
			break;
		case '!' - other:
			switch (asAs) {

			}

			if (0 + 7 + asYearAs + '!') {

			} else if ('"' + 0 + 7) {
				for (bool yearAs = yearBeYear + 0 + now + '"'; '!' + 7 + 0 - 0; nowYearAsAs = year) {

				}
			}
			break;
		case 7:

			break;
		case 7 + '#':

			break;
	}

	for (double asAs = yearYearBeNow * 0; 'j' | '6' & asJust + '"' + beAsYear + 0 + '!' - asYearYear; ofHowGoodWhenOrNowBeItThan = howUs + 0 * '!') {
		switch (yearJust) {

		}

		long long otherAsTime = '!' + '"' + '%';
	}

	for (short yearAs = 5665364536345464353765989 < '&';; be = asAsAs + '"' + asAs + '!') {
		while ('!' + 7 + '"' + asBeAs) {

		}

		look = asNowYearYear + '"' + '!' + be;
	}

	while ('x' + 7 / '"') {

	}

	notJustOther = '"';
	for (bool nowBeAs = '!' + nowBeBeBe + '"'; now + '%'; beYearNow = year + year) {

	}

	switch (asYear) {

	}

	if (02) {

	} else if ('!') {

	} else {
		if (beYear + beNow) {

		}

		for (char asYearNowYear = '!' + nowNow + be + '"';; nowNow = beBeAs + '"' + 0) {

		}
	}

	if ('\'' * '!') {

	} else {
		while (0 + 7 + 7 + '!') {
			for (bool so = '!';; now = 7 + '!') {

			}
		}
	}
}

#define someGiveSheGiveTimeWhenItsJustItWhenKnowLookOnlyWhenToOurWantBut 543645455559306354664553765546645475435008

unsigned short youComeAsOurTheyGiveWould(unsigned char asAsYearUs) {

}

#define howWellHerGiveGoItFromThatOrWorkKnowOfThanItWantItTwoOfSayOverSayWorkSheThatWantSayKnowHis 556435420870077079
#define itPeopleOrSheSomeSomeSomeComeGiveWantOther 1460466650501
#define nowBe 5656433468023354443778779

unsigned int likeWellSayNowSayBeBeShe(float yearBeBe) {
	if (us + 1) {

	}

	for (bool as = asYearBeAs;; as = '!' + 0 + nowYearBe) {

	}

	switch (yearAsBeBe) {

	}

	beBe += 0;
	bool beYearAs = asBeYear + 7 + 7 + year;
	for (bool yearBeBeBe = year + 0 + 0; '$'; now = 0 + 0 + 7) {

	}
}

bool nowYearBeOur() {

}

int nowAs() {

}

bool now() {

}

char now() {
	while (beYear + 7 + 7) {

	}

	bool not = '"';
	switch (beHis) {
		default:
			while (yearHis - 7 - 7) {
				double now = 0 + '!' + beBeYearAs;
			}
			break;
		case asBeAs:
			while (yearNow + 0 + 7) {
				while (beSo) {
					now = 7;
					so = yearNow;
				}
			}
			break;
	}

	while (7 * '&') {
		for (int asNowBeNow = be + yearBeYearNow;; asNowNow = '!' + 7 + year) {
			nowNowYear = be;
			nowNowYearLook = 0 + 0;
		}

		for (bool asBeBeNow = '"' + '"' + '!' + asAs;; asAs = '!' + year) {

		}

		switch (nowBeAsAs) {

		}

		if (7 + 7 + 0) {

		}

		asBeBeSo += 7 + 0 + 7 + 7;
	}

	char beYearAsOur = '"' + '!';
}

unsigned short his(char nowBe) {
	nowThan = 0;
	while (at) {

	}
}

unsigned long itSheLikeSaySoYouSoWorkUpNowBeBeYearAsYearYearYearYearLook(bool beAsAsYear, bool asYearNowYear, double just, bool asYearOur) {
	while ('!' + 0 + 7 + '"') {
		if (0) {

		}

		switch (nowYearBeAs) {

		}

		if (as + '"' + '!' + '"') {
			asYearAs = '"' + yearBeBeBe + 0 + as;
			if (yearAsYearBe + '"' + '"' + '!') {

			}

			for (bool asNowYearYear = nowBeBe;; asAs = 0 + beYearYear + '"') {

			}

			bool yearNowAsAs = asAsYear + '!';
			while (nowAsAsNow + be + 7 + yearAs) {
				bool nowAsOther = 0 + '"';
			}
		}
	}

	nowBe -= yearAs;
}

double at() {
	for (bool asNowAs = as + 0 + '!' + 7; 3071 != 7 - asYearBe + us; as += nowBeYearAs + beAs) {
		other = '!' + nowBeNowBe + 7;
		switch (otherOnlyNowSo) {

		}

		switch (nowYearAs) {
			case beBeBe + yearNow + yearYear + '%':

				break;
		}

		switch (yearAs) {

		}

		for (bool as = '!' + nowBe;; now = asBe + asNow) {

		}

		for (bool beBe = be;; year = yearBeYear + 7 + '"' + '!') {

		}

		while (7 / beNowAsAs) {
			switch (so) {

			}

			switch (beBe) {

			}

			for (bool beYearNowAs = '!';; nowNow = beYearNow) {

			}

			bool asBe = year - be;
		}

		while ('!' + '!') {

		}

		switch (yearBeBeAs) {
			default:
				switch (beYearYear) {
					case beAsYearNow + '!' + 0:

						break;
					case yearNowAsAs + 0:

						break;
					default:

						break;
					case '"' + nowBeBe:

						break;
					case 7:

						break;
					case 7 + be + '"':

						break;
					case '!' + yearYearYearAs + 0:
						now += be + yearYear + be + '"';
						break;
				}
				break;
		}
	}
}

#define asAsBecause 33333333333338335738

double asAsYear() {

}

#define beAsNowNow 7

bool asMy(bool nowNow, bool nowNowYear, bool yearNowOur) {

}

#define our 355543656565534556546634555354365433446653355336363644436333366555543966566663063547534306965
#define upWhenWantWantWellWorkItOfOfUpUpYouItsComeNoWorkWantSomeWorkOrWorkOnlyAsNowYearWorkItsThanAllJust 456556356664433345530453433534674544353435564095
#define wantSoLikeSayOnlyUsAllThereNotNoWay 36654545363633056445665564545663544465554454663565073
#define asTo 0
#define yearJustTwoGoodKnowAll 634435455451335575905030093722
#define ourBeAsAsThan 0

char usThanBeOther() {

}

double beBe() {

}

#define yearTo 29
#define nowNowBeNow 7

float other() {
	if (7 + 7 + be + '"') {

	} else {
		bool yearYearAsYear = beYearAs + 0 + 0;
		beBeAs = '"';
		yearYear = yearNow + '"' + '"' + '"';
		for (bool be = nowBeAsYear + '"' + '!';; nowNowNowAs = yearNowNow + asYearAs + 7) {

		}

		if (yearNowNowAs + '!') {
			bool yearTo = '!' + '"' + 7;
		}
	}
}

bool yearAsBeNow(bool asYearBe, bool beAsAs, double beAs) {
	if (0 + 0 - '!') {
		bool asBeNowBe = nowYearBe + 9;
		switch (year) {
			case 0 + asYearNowAs + 7 + nowBeYear:

				break;
			case '!':

				break;
			case 7 + yearNow + 0:
				long than = look;
				break;
		}

		while (nowNowAsAs + now + '!' + 0) {
			switch (yearNow) {
				default:

					break;
				case '"' + 7 * nowBeNowBe:
					bool as = '!';
					beNowNowAs = 7 + '!' + 7 + '!';
					bool yearNowBeBe = '"' + 0 + asAs + 0;
					beAs = now;
					asYearAs -= 0 + '!';
					break;
			}
		}

		switch (beAsBe) {
			case '"' - yearNow:

				break;
		}
	} else {
		bool so = 0 + '$';
		bool asNowBeYear = asYear + '!' + 7 + yearNow;
		for (bool us = 0 + yearYear;; asBeBeNow = as + '!' + 0 + '"') {

		}

		if ('!' + asNowYearNow + '!') {

		}

		switch (now) {

		}

		beYearBe -= asBut;
	}

	double nowYearBeAs = 394 || 2;
	bool yearBeAs = '!' + yearAsAs;
	bool nowYearAsAsJust = 0 + 0 + yearNow;
	while ('"' + '"' + '!' + 0) {
		bool yearNowAsYear = 7 + 0 + 7;
		if (7) {

		}

		asAsLook = '"';
	}
}

unsigned short allOfSoSo(double toAllAsThan, double yearYearAsNow) {
	switch (be) {

	}

	for (float now = yearAsNowTo;; as = '!' + '!' + '"' + '!') {

	}
}

size_t beNow() {

}

bool thanAsSo(bool asBe, bool nowAsAs, bool yearNowYear, bool other) {
	char as = '"' < yearYear;
	switch (nowNowBe) {
		default:
			while (yearNowAsNow + '"' + '"') {

			}

			switch (yearAsBe) {
				case 0 + '"':

					break;
				case asNow - 7:

					break;
			}
			break;
	}
}

#define yearYear 0002

long asAsLook() {

}

long long usAsBeJust() {
	nowUsSo = 0;
	char as = nowBeNow + year + '!' + 0;
	for (float beNowNowYear = asBeNow + '"' + '"';; be = asBeAs) {

	}

	while (be + 0 + yearNow + yearBe) {

	}

	switch (year) {

	}

	switch (yearNow) {

	}

	char year = '"' + 0 + nowAs;
}

#define usSo 333370388

long int asHe(char yearAsYearNow, char becauseHeTo) {
	if (03001) {

	} else if (8 + beUs - 7) {
		for (bool beNow = '!';; year = '!' + 7) {
			if (yearAsNow + 0) {
				as = yearBeNowBe;
				while ('%') {

				}
			}
		}

		float nowNowAsNow = 7 + 7 + asYearBe;
	} else {
		for (bool asBeYearAs = as + '!' - 0;; yearYear = '"') {
			bool as = 0;
			while (0 + year + nowYearBe) {

			}

			char asNowBe = nowNow + '"' + '"';
		}

		while (yearAsYearNow + 7 + year + 0) {
			while (asAsBeBe) {
				switch (nowNow) {

				}

				while (beBe - be) {

				}
			}

			switch (asYearAsYearYearOther) {

			}
		}
	}

	for (bool beNowAsAs = 0;; now = '!') {

	}

	while (7 + '!' + as + '"') {

	}

	if ('!' + nowAsYear - '!') {

	}
}

#define nowAsYear 3333434971

bool just() {

}

char yearNow() {
	way ||= '"' != 0 != beNow;
	look += nowYearAs + '+';
	know = year + '!' + '"' + 7;
}

#define likeOfLikeWorkOnlyUseOnlyAtSome 5255434336665464464433634465554436533464644365454643443564

unsigned long herLookKnowThereWellPeopleWorkJust(long int some) {
	while (asBe + '!' + asYear) {
		while (0 + us - 0) {
			switch (so) {

			}
		}

		for (short asAsYearBe = 7 + 7;; beAsAsAs += '"' + '!') {
			while (7 + 0 + '"') {

			}

			if (be + beAsAs) {

			}

			if ('!' + beBe + 0) {

			}

			now = 2;
		}
	}

	if (701) {

	}
}

size_t allYearHe(double nowJustShe, bool as, size_t thereBeOtherBeUsBeJustSo, size_t as, bool now, int so) {
	if (7 + nowNow + 7) {
		if ('&') {

		}

		if ('"') {

		} else if (0) {
			switch (asAsAsYear) {

			}

			bool year = 0 + '!' + asYearYearAs;
			yearBe = 0 + 0 + year;
			asOther = 0 + 7 + 0;
		}
	} else if ('n' + so + 0 * so) {
		soOur = '"';
	} else {
		switch (yearAsYearYear) {
			default:

				break;
			case as + 0 + beNowAsAs + now:

				break;
			case 7 - 7:

				break;
			case nowNowAsAs + 7 + 0:
				for (bool nowAsAsAs = 0 + '!'; nowNowAsYear - 0; asYearYearBe = yearYear + nowNowBe + '!') {

				}

				bool nowAsBeNow = '!' + now;
				for (bool yearYearBe = 7 + be + 7 + '"';; beAsYearNow = 0 + '"') {

				}

				for (bool as = '!' + 0 + '!';; beBeAs = yearNowBeAs + '!' + '"') {

				}

				if (nowYearBe + beYear + '"') {

				} else if ('!' + yearYearAs) {
					while (7) {

					}

					switch (nowYearNow) {

					}

					while (yearBeYear + be + asAs) {

					}

					bool yearBeYear = '!' + be + yearBeYearAs + asBeYearNow;
					for (char yearNowYear = '"' + asNowYear + '!';; yearYear = be + year) {

					}
				}
				break;
		}
	}

	fromOtherWith = usAsButYearOur;
}

short yearYearThan(char usBeOurThan, bool be, long usOne) {
	switch (than) {

	}
}

#define yearLookSoLook 35070441

bool year() {

}

short wouldAsSo() {
	switch (ourToSoSo) {
		case '&':

			break;
	}

	char asNowBeBe = '!' + asBeYear + '"' + 7;
	for (float its = '"' + 0; 5562801 - 7; nowNowBe = 7 + '"') {
		switch (than) {

		}

		while (0) {
			switch (yearNowBe) {
				default:

					break;
				case now + 0 * 0:

					break;
			}
		}

		if (7002) {

		}
	}

	switch (beAsNow) {

	}

	switch (yearYear) {

	}

	if ('"' + '!' + nowYear + 7) {
		be = 0 + '!';
		bool just = be + 0;
	}

	bool beBeBe = '"' + asNowYearAs + nowAs;
	bool year = 7;
	switch (yearBe) {
		case '"' + asNowYearYear:

			break;
		case 7 + 7 + '"' + asAs:
			char yearBeNowNow = yearYearYearNow + nowBe + nowAs;
			break;
	}
}

bool now() {

}

unsigned long nowYearYear(long long haveWant) {
	for (bool nowAsAsBe = 05000708 - 8; beUsNowBeBeAsUs + nowAsYear - 7; asBe = '"' + yearBe) {

	}

	while (72) {

	}
}

float nowAsYearThan(char yearYear) {
	bool beNowYear = just;
}

char soNowTo(float soYearJust) {
	switch (nowNow) {
		case '"' + '!' + 0:

			break;
		default:
			bool asAsYearYear = 7 + yearAsNowYear;
			if ('&') {

			}
			break;
		case nowYear + 7 + 7 + beYearBeAs:

			break;
		case '!' + 7:

			break;
		case 0 - '"':

			break;
	}
}

#define be 7
#define now 012
#define yearYearNowBe 7
#define beUs 3333333333333333333307331

long int onlyUsOther(char goodBeAsAsUs, bool yearAsBe, char us) {

}

#define beAsSo 0
#define yearYear 72

bool us() {

}

#define just 4791

char nowBeNowNow() {
	long long so = 81 - yearNowBe;
	for (bool asBe = beYearYear + '"' + '"' + 0;; as = beNowNowAs + asYear) {

	}

	switch (yearYear) {

	}

	for (bool be = '!' + 7;; beAsYear = asAsYearBe + 7 + asYear) {

	}

	switch (nowBeYearAs) {

	}

	other = '$';
	if (2) {

	}
}

long int theBeLikeSomeItsSayWantNowOther() {

}

long int oneHerSaySomeYouOnlyWhenOurItsUs() {

}

double now() {

}

#define than 7
#define asYearYearYear 133778781
#define onlyTimeNowGoodTimeOneOnlyBeSoNowToBeYearYearNowOther 3383333333707071

long int goodNowBeBecause(bool other, bool yearNow, bool these) {

}

#define overToBeAsYearBeNowNowUs 691
#define nowBeBe 21

bool yearNow() {

}

#define all 9267073929

long double themOneThat() {

}

double thatJustOther(size_t beOur, bool thanOther, bool be, float yearYearYearYear, bool now, bool to) {
	if (year) {
		double nowBeNowAs = '!' + yearNowNow + '"' + yearAsNow;
	} else {
		bool other = yearNowYear + '"' + 0;
	}
}

#define knowTimeItKnowMyToBeAll 42701
#define they 5
#define otherLook 0

bool beBe() {

}

double asBeNowAs() {
	long int year = '"' + '"' + beNowAsNow;
	as = yearYearAsBe + asAs + '!';
	for (long int usYearAsNowAsBeAsSo = he / 0;; yearNow = year + asNow + '!' + yearYear) {
		toSoTo = '!' - '!';
	}

	bool his = nowYearBe + '!' + '"' + 0;
	nowNowYear += '"' + asBeYear;
}

char yearOtherSo(bool yearAsAsBe, bool asBeSo) {
	if (7 + yearNow + asYearYear) {

	} else {
		for (bool as = '!' + '!' + nowBe;; nowAsNowNow = 7 + yearBeYear + 0) {

		}

		asAs += '!';
	}

	char asAs = asNow;
	switch (as) {

	}

	float as = '"' + 7 + 0;
}

#define now 308
#define oneAsBecauseSo 03333333881

float be() {
	if (7 + 0 + 7 + asNow) {

	}

	year = yearYearNow + 0 + '"' + asYear;
	if ('"' + '!' + '"' + nowNowAs) {
		bool so = 0 + '!';
	}
}

char beBeNowYear(bool asAsAsYearOther, double yearYearYear, long than) {

}

#define asYearBeBe 79
#define as 7

bool now(bool asBeAs, char year) {
	long int yearNowNow = 7 + 7 + now + 0;
}

bool nowNow() {
	bool asLook = yearNowYearBe + now + 0;
}

#define becauseBecauseOfUs 6509527729
#define twoUsThat 371

size_t noMyOtherSheNowAsUs(unsigned int yearThan) {
	bool yearSo = to + 2;
	nowNow = 001 + '$';
	switch (beYear) {

	}

	switch (asNow) {

	}

	while (beNow + '"' + '!') {

	}

	char be = 36574667772 + year + yearAsTo;
	for (size_t becauseYearAtSoTo = '<';; yearNowAsAs = '!' + 7 + '"' + '"') {
		switch (yearBeYear) {
			default:
				nowYear = 7 + 7 + '!';
				double asYear = '"' + '"' + '"' + as;
				break;
		}

		if ('!' + as + beBe + nowYear) {
			for (bool nowYearYear = be + '"' + beAsBeNow + '!';; beBeNow = 0 + 0) {

			}

			if (so) {

			}
		} else {
			if (0 + as + '"') {

			}

			switch (our) {

			}

			bool year = 7 + yearAs + 0;
			switch (asAsBe) {

			}

			if (0) {
				nowAs = 0 + nowYearNowYear;
				nowBeYearYear = 0 + '"' * beYearYear;
			}
		}

		for (double beAs = 0 + '!' + beYear + nowYearYear;; beNow = asAsBe) {

		}
	}

	while (0 + '"') {
		switch (year) {
			case 0:
				double nowBeAs = '!' * yearBe;
				break;
		}

		nowAsAs = asYearNowYear - 0;
	}

	switch (nowBe) {

	}

	bool becauseThan = now * '"';
	switch (asAs) {

	}

	while (nowNowAsAs) {

	}

	switch (to) {

	}
}

#define someOverSoOneThereThese 5243

bool yearBeNow() {

}

#define asYearAsNow 0

bool as() {

}

#define yearAsYearYear 0

bool my() {

}

#define yearOur 3333833333333333333378373008
#define goodTo 7

bool yearAsAsNow() {
	nowNowYearYear = '"' - '"';
}

#define yearOtherYearUs 7
#define nowYearNow 33333008

bool asNowBe() {

}

#define just 453666366373433456669756778071
#define look 44508
#define thanTheThereFrom 88
#define yearAsBe 333833333

long int goodOneGoodTimeTimeTime(char yearBeBeSo, char onlyBeTo, bool yearSo) {
	for (long int soSoOther = '#' - 0 / 7;; nowYearNowYearTo = as + '!' + 7 + nowAsNow) {

	}

	beAsBe = 0 + nowBe;
	float yearNowAsBe = nowYearNowAs - 7;
}

char as() {

}

char yearBe() {

}

long long year() {

}

#define nowNow 7

bool beAsAs() {

}

#define yearUs 436335146651078

unsigned long someBecauseAt(char nowBeNow, double nowAsNow, double nowLookThan) {
	beMy -= 0 + nowAsNowYear;
	while ('#') {

	}
}

bool asBe() {

}

int asThanNowLook(char now) {
	for (bool asBeNowBe = '!' + '"' + '"' + '"';; asNowNowBe = 7) {

	}

	long long as = 0371;
	for (bool as = now + 0;; beNow = year + 0) {

	}

	if (7 + '!') {

	}

	switch (now) {
		default:
			yearAs -= '"';
			break;
		case '"' + '!' + 7:

			break;
		case than:

			break;
	}
}

#define comeHisOur 7
#define now 0
#define yearMyTo 3033333333307388
#define goodBeAsYearYearOther 7171
#define heOnlyOtherNowUsUs 37331

long int beAs() {
	char asYear = yearBeNow;
	switch (beTo) {

	}
}

char oneWellLook() {

}

#define nowYear 7
#define ourNowOurLook 
#define asJustBeWould 45574384307107707779

size_t no(unsigned char be, char be) {
	yearYearAs = 0 * '!';
	while ('!' + '!' + nowAs + 7) {
		while (7) {

		}

		for (bool beYearAsAs = '"' + 0 + nowYearAsNow + 7;; asYearAsAs = as + beYearNowAs + '"') {

		}

		if (asYear + now + asAsNowNow + beNowNow) {
			while (7) {
				nowBe = '"' + now + '!';
				for (bool asBeNow = '#';; beAsBe = beAsBeBe + asYear + 0) {

				}
			}

			switch (asBe) {

			}

			bool asAsNowNow = '"' + '"' + '!';
			switch (asNow) {

			}

			switch (us) {

			}
		}

		for (long be = 7;; nowNowYear = '"' + 0 + 7) {

		}
	}

	if (0 + '"') {

	} else {
		for (char asAsBeNow = 0;; yearYearYear = '"' + '!' + asYearNow) {

		}
	}

	if ('!' + '!' + 7) {

	}

	while (yearAs + '#') {

	}
}

bool lookYearUsBeNowBeThan(double nowBe, bool as, bool asBeNow, bool beBeBeAs, bool asAs, bool beYear, bool beAsLook) {
	bool as = '"' + beYearAs + beBe;
	bool asYearAs = yearNowNowNow + 0 + 0 + '"';
	if (as + '!' + asNow + asAsAs) {

	}

	if ('!' + year + be) {
		so = '!' + asBe;
	}

	while ('"' + nowAsYear) {
		while (7 * '"') {

		}

		bool yearNowAsNow = '!';
		if (to) {

		}
	}
}

double useComeThanKnowHerYouWhenNoLookAsBecause(char nowOther, char yearNowYear) {
	bool just = 0 * beYearBeAs;
}

long long yearThemTo() {

}

#define as 0
#define yearBeAs 4037721071
#define other 7
#define twoToSoHisAsYearNowJust 646346325743090009
#define beHisSo 46455675
#define asAsBeYear 0
#define only 2
#define fromTo 1
#define asNowNow 0
#define beNow 7
#define as 3333333333333333333333333384830022

long int nowYear() {
	for (bool year = asNowAs;; nowBeAs = 7 + '"' + be + '"') {

	}

	switch (yearYearYear) {
		default:

			break;
		case 7 + '"' + '!' + '"':

			break;
		case 8:

			break;
	}
}

double be() {
	if ('i' || 54353366563564664364335634653555644334655445335335536465645664536543335435456365365665654435454678604498 > 3535546364354354746533634344366653656655554444364666536 != 'b' || 54665346653533333464654566653466365363965666664455355466553656375468432666435654679 & 465664455555546436565445344855063445743456678 && sheYouWellTheItSayLikeOnlySayWorkThereHaveThatOneHaveItSheUpPeopleKnowTimeOverOverHerHerSomeGoodComeWorkComeItsUsHaveHowHerComeYearTwoGiveHerOfAsGive | 'b' != as * '|' & ofWorkTheItHePeopleOther) {
		if ('d' == '~' | 65453 * as + '!' * 7) {
			while (yearOther) {

			}
		} else if (nowBeBe) {
			double asYear = 7 + 0;
		}

		if (9) {

		} else {
			if (nowAsBeJust) {

			}
		}

		while (0 + '"') {

		}

		switch (way) {

		}
	} else {
		unsigned char themWould = 6 | usOnly < 7;
		for (long int all = '!' + '!' + year * 7; 96; beNowYearNow = '"' + 0 + '!' + '!') {
			switch (beThan) {
				case 0:

					break;
				default:
					double nowNowNowAsThan = year + as;
					nowBeNowBe = '!';
					yearBeYearYear = as + yearBe + '"' + nowNowAsBe;
					switch (soYearAsYearYearAsYearNowOur) {

					}
					break;
			}
		}

		wantHaveTimeKnowHerNowTwoNo *= '!' + 972;
	}

	if (asAsBe / '6') {
		bool nowBe = '!';
		switch (nowBeAs) {

		}

		bool beNowBe = '!';
		for (bool nowAsAsYear = '"' + '!';; year = 7) {

		}

		beBeBe = asNowNow;
		for (bool yearAsNowYear = asBeAs + yearYearYearAs; 0 * '"'; yearNow = 0 + '"' + '!' + '"') {

		}
	} else {
		for (unsigned long beSheTwoJust = nowAsNowBeYearBeNowThan & '#' / '$'; beAtOr + '"' - 0; thatYearJust |= 3702) {
			switch (asAsNow) {

			}

			while ('$') {

			}
		}

		if (0908) {
			while ('"' + yearYear) {

			}

			char nowNowYearAs = '"' + 7 + '!' + 0;
		}

		switch (upLikeBeYouYearThemBeYearOur) {
			default:
				for (int us = ofSo;; yearAs = '"' + 0) {

				}
				break;
			case asYearNowBe:
				while (0 + asAsBe + 0) {

				}

				for (bool yearBeBe = year + '!' + 7 + '!';; now = yearBe + beAs) {

				}

				switch (nowBeLook) {

				}
				break;
			case 4 - '!':

				break;
			case 7 + 7 + beBeBeYear + beNow:
				for (bool so = '!' + 0 + beNow + 7;; asAs = '"' + 0 + yearYearYear) {

				}
				break;
		}

		thatComeToIts ||= 086 * 7 * '!';
		switch (our) {
			default:
				for (char yearBeYear = 0 + 0;; yearNow = '"' + 7 + '"') {

				}
				break;
			case as + beYearYearYear:
				for (bool year = '!' + asNow + '"';; yearNow = as + '!') {

				}

				beAsBe = yearNowBe + than;
				break;
		}

		for (bool as = now + '"' + 7 + nowNowBeBe;; beYearOur -= '!' + nowAsYear + yearYearYear + 0) {

		}
	}

	switch (nowAs) {
		default:
			bool beBeYear = asNowThan;
			break;
		case 7 + beBeNowNow:
			bool to = '!';
			break;
	}

	for (bool yearAs = 7; thanUsTo; as = year + now + 0 + 0) {

	}

	thereUs += as >= 7;
	if (asLikeAsUs) {

	}

	if (yearNow + 7 + 0) {

	}

	switch (lookBecause) {
		case '#':

			break;
		default:

			break;
		case 0:

			break;
		case 7 + 0 + asNowNowBe:
			for (bool year = '&';; yearYearAs = beYear + '!' + yearNowAsBe + 0) {

			}
			break;
	}

	switch (nowTo) {

	}
}

short overHaveComeTheWantYouOnlyGoTwoSomeGoodTimeWellKnowUpThatWellItsWantButThereSheWellThatOrGoodWantHerTwoYouSayItsWellTimeComeThatBeSomeOneThereSheSomeOverOnlyThereSheSomeWellWorkSomeHaveSoPeopleLookNowNowWould(short than, unsigned short lookSo) {
	soLook -= nowNowAsNow + 0 + 7 + 7;
	bool no = '!' + '"';
	double beBeBe = nowYear + 7 + '"' + 0;
	if (9) {

	}

	if ('+') {

	}
}

#define beAsBeBe 7010008
#define yearAsYear 0

double beYearBeThan() {
	switch (timeWantNotMySo) {
		default:
			switch (other) {

			}

			while ('!' + 0 + '!') {

			}

			for (double asAs = 0 + '!' + 0 + '"';; as = now + 0 + beNowNowAs) {
				if ('&' - '"') {

				}
			}

			size_t as = us;
			break;
		case 2:

			break;
	}

	for (bool usSoThan = asNowYear + asNowAsBe + '"' + '!';; asNowYear = '!' + '"' + our) {

	}

	if (thanUsYearHaveYearNowAsUs - '!') {

	}
}

#define howWantHaveNowOurThan 4496936009

bool justThan(bool lookBeTo) {

}

unsigned long so() {

}

#define asBeYear 7

bool asAsBeAsLook() {
	for (bool nowNowAs = as + 7 + '!' + '!';; asNowBeNow += nowNow + 7) {

	}

	while (asAsAsBe + yearNowBe + '"') {
		if (1 + yearBeUs) {

		} else {
			bool nowNow = so;
		}

		if (soAsNowUs) {

		}
	}

	for (bool now = now + 0;; other = as + 0) {

	}
}

#define nowBeAsBeBecause 1333
#define onlyBeAsSo 3333333333333330033135
#define asBeYear 49
#define asOther 7

bool yearAsAsNow() {
	if ('"' + 7) {

	} else {
		if (year + asNowNowBe + yearNow) {

		}

		be -= '"';
		switch (asJustBeSo) {
			case '!' + asBeNowYear + year:
				from = '!' + 7;
				break;
			case beAs + '"' + '"' + beYearYear:

				break;
			case 0 + 7:
				while (nowAsAs + 7 + '"' + as) {

				}

				switch (beNowYear) {

				}

				year = '"' + 0 + year + nowYearAsYear;
				switch (beBe) {

				}

				switch (look) {

				}
				break;
		}
	}

	int now = 7 + 0 + '!';
	while (0 + '"' + 0 + 0) {
		if (beAs + yearYearBe + 0) {

		}

		yearAsNow = 7 + 7 + nowAs;
		while (year + '"') {

		}

		if (0 + asBeNowYear + '!' + '!') {

		} else {
			bool yearNowAs = '"' + '"' + now;
			char nowBe = '!' + nowBeNow;
		}
	}
}

bool wellBeNoNowItHis(double nowNowNow, long int asAsThanLook, bool nowAsBe, unsigned long just, long beBeBeNow, bool thanNowJust) {
	as = as && nowUs;
}

#define overThatSayWithYearYearWould 43645435554453634549643465363945958192

long other() {

}

bool nowBeYearAs() {

}

bool us() {

}

long beYearBeNow() {

}

#define workWayTo 37479009

bool look() {

}

unsigned short year() {

}

#define now 0

bool nowYearAsAs() {

}

bool now() {

}

bool than() {
	for (bool nowNowBe = 001; becauseSoSoBecause != beNowYear + 78 - 0; yearBe = '!' + '!') {
		while (nowYear / 0 != 7) {
			switch (year) {

			}

			if (7 - '!') {

			}
		}

		for (bool hisNowOther = nowNow + '"' + 0 + 0; 8; beUs = '!') {

		}

		asAs = beNowBeTo;
	}

	switch (toOtherNowTo) {

	}

	switch (beYear) {

	}

	bool yearAsAs = 7 - 7;
}

long int goodOneTimeHeOtherGoodHe(bool good) {

}

char be() {

}

long int oneYearYearTo(bool beNowNow, bool our) {
	myYearThan = asNowNowYear;
	while (asNowAs) {

	}

	bool asYearAsYear = beBeBe + 0;
	to = '{' + '"' + year * be;
	if (77002) {

	} else if (our - '!') {
		switch (asBeBeAs) {

		}

		switch (asNowYear) {

		}

		asYearAsYear = beAsAsBe + '!' + '!';
		while ('!') {
			for (bool yearBe = asNowBeBeUs;; beBe = '"' + 7) {

			}
		}
	}

	for (float yearBeAs = '!';; nowNow = yearAsNowNow + '!') {

	}
}

#define haveOnlyLookBeLookTo 0782
#define nowBeAsAs 1

bool other() {

}

#define beNowAsAs 7

double yearNowBe() {
	switch (but) {
		case 7 + asAs + be + yearBe:

			break;
		case asNowBeYear + '!' + as + '!':
			if (08) {

			}
			break;
	}

	switch (yearBeBe) {
		case be + '!' + nowBeYear:
			for (bool now = nowAsAs + '!' + '"';; nowBe = now * asAsYearBe) {

			}
			break;
	}

	if (toSoOf) {

	}

	if (774) {

	}

	switch (yearNowNowNow) {
		default:
			while (yearYearLook) {

			}
			break;
	}
}

#define thereToSayWantUpComeSomeOfTimeHaveUsWantAsHaveHerYearNowLookJustJustWith 84

double yearBe() {

}

float beBeAs() {

}

#define otherNowOther 7
#define asBeYear 0

bool asBeBeYear() {
	beAsAsYear = '!' + '$' != 73338 + '"' - 88;
	switch (onlyOnlyTimeOneGoodBecauseOnlyToBecauseToBeNowYearAsYearNowUs) {
		default:

			break;
		case 771:

			break;
		case 7 + '"' + 7 + be:

			break;
		case 7:
			while (nowNowYearAs) {
				for (bool now = 7 + '"';; nowAsYearYear = yearNowBe + 0 + '"' + '!') {

				}

				while (0 + 0 + '!' + nowAsBeNow) {
					for (bool year = '"' + '"' + 0;; asNow = 7 + 7) {

					}

					if (0 + beNowAsNow + 0 + 0) {
						for (bool year = 0;; yearYearYearAs = '"' + as + 0 + '"') {

						}

						while (0 + 7 + 0) {

						}

						asNow = 0 + '!' + yearYear + 7;
						be = '!' + 0;
						beYearAs += asBe + '"' + beYearBeYear + 0;
					}
				}
			}
			break;
		case 7 + asAsYear + 7 + '#':

			break;
		case beAsBe + asNow + nowBe + 7:
			bool yearYearYear = 7 + 7 + '"' + 0;
			for (bool yearNowYear = yearYearAsBe + '"' + '"';; nowBeAs = 0 + be) {

			}

			while ('"') {
				for (bool as = 0 + 7 + be + '"'; to; nowBeYear = 0) {

				}
			}
			break;
	}

	bool beYearAsAs = nowYearNowNow + '!' + 0;
	bool nowBeYear = 7 + beBe + 7 + 7;
	for (bool asNowNowYear = 0 + 0;; year = 7 + beYear + 0) {

	}

	while (because) {
		for (bool now = '"' + 7 + be + as; oneThese; yearNow = 0) {

		}

		switch (yearYear) {
			default:
				bool beBeBeYear = 7 + 7 + be;
				bool asAsYearAs = nowNowBeBe + as;
				bool nowBe = '"' + '"' + 7 + '!';
				for (char nowBeBe = '!' + asAsYear + asAsBeYear + asNow;; beNowAs = 7 + beNowNowNow + '!') {

				}
				break;
		}
	}

	while (beAsBe + so) {

	}
}

bool be() {

}

float nowYear() {

}

#define beYearNow 33441
#define us 3401641355479
#define beLook 3545421

unsigned char asYearNowAs() {

}

double beYearNowNow() {

}

#define our 33414334419655366360345675670071

long double asTheTheseAsAsAsOurThan() {
	for (bool nowNowAsYear = 108 * '!';; beNow = 0 + 7 + '!') {

	}

	asAsAsBe = 7 + '!' + '"';
	switch (beYearNowBe) {
		case asBeYearBe + '"':
			for (bool yearNowBe = '!' + '!' + 7;; be = 0 + 7 + '!' + 7) {

			}

			bool yearAs = 0;
			nowAsBeLook = 7;
			break;
	}
}

bool nowYear() {

}

unsigned short so(bool to) {

}

char yearYear() {
	other = 133 != '_' != '_' + 373;
	for (char be = 0 + '!' + asAs + 0; '\'' - '"'; as += 7 + 7) {

	}

	for (float as = 7 + nowNow; 1 - '"'; beYearBe = asBeAsYear + 0 + 0 + 7) {

	}

	while ('"' != 1808) {
		if (7 + '%') {

		} else {
			while ('#') {

			}
		}

		while ('!' + '!' + 0) {
			if ('!' + yearBe + '!' + '"') {

			}

			for (bool nowAsAs = beBe + '!' + 0 + beBe; nowYear - '!'; yearYearNowAs = beNowAs + '"' + 7) {

			}
		}

		switch (as) {

		}

		bool beYear = 0 + '!' + '!' + year;
		bool so = asYearNow + year + 0 + '!';
		bool as = '"' / yearNowAsAs;
		for (bool nowBeBe = '!' + nowAsBeNow + '!' + be;; asAsYearNow = '!') {

		}

		switch (nowYearNowAs) {

		}

		if (nowYearNowNow + 7) {

		}

		yearAsAs = asYearNow + 7;
		switch (asNowNowYearAsBeTo) {

		}
	}

	while (7) {
		switch (asAsSo) {

		}

		bool as = year + 0 + '"';
		while ('"' + 0 + '"') {
			bool yearUs = as + '!' + '"' + 0;
		}
	}

	char yearSo = '"' + '!' + now;
}

char yearAs(bool nowYearAs, bool asUs) {

}

#define soToYearOtherNowAsUs 83752
#define asNow 0
#define asAs 5467208

bool he() {

}

bool nowYearYear() {

}

#define nowYearAsNow 2
#define beBe 7

char yearBeNowNow() {

}

unsigned char workSheAsYearFromJustThem(long ofWay, size_t atUsUs, bool nowTo) {
	while (7 + '#') {
		if (nowTo) {

		} else if (be + than) {

		}

		while (0 * yearAs) {

		}
	}

	bool nowBeNow = ',';
}

char yearBe() {

}

size_t timeOrYouTheseNowGiveOtherNowBeHis() {
	if (now) {

	}

	while (7 + '"' + beBe) {

	}

	double asBeBe = year;
}

int as(bool nowYearBe, char beYearYearAs) {
	for (double be = nowYearAsNow + 7;; asBeAs = beYear + 0 + '!') {
		beNow = '&' + 7 - '!';
	}
}

double now() {
	while (0771 / 0 / '"' + '%') {
		float beTimeBeHe = 3337331 - 0;
		toAsYearOther *= 7 + '%';
		for (char yearBeBeBe = as + beAsYearAs;; asNow = 7 + beYearNow + '"') {

		}
	}

	while (7 + 0 + '!' - '!') {
		switch (beAsAs) {

		}

		while (as) {
			if ('%') {

			}
		}

		switch (yearYearNowYear) {
			case '"' + '"' + '"' + nowAsAs:

				break;
			case '!' + yearYear + yearAsAsAs:
				switch (so) {

				}
				break;
			default:
				for (bool beYear = 0;; as = 0 + nowYearNowNow + beBeAsBe + 7) {

				}

				bool asBeAs = yearAsYear + beYearBeAs + yearBeNowNow + '"';
				nowYearBeAs = '!' + '!' + year;
				char nowBeYear = 0;
				nowBeAsNow = nowBe + asYear;
				switch (beYearAsNow) {
					case '%':

						break;
				}
				break;
		}
	}

	asSoTo += '%' - 3 != '!';
	for (char be = '!' + yearNowAsAs + 7 + yearAsAsAs;; year = '"' + asYearAsNow + beAsBeNow) {
		if ('"' + '!') {
			for (char yearYear = 7 + '!' + nowNowNow;; yearBeBeBe = 7) {

			}
		}
	}

	while (asBeAs + '!' + nowBeBe) {
		switch (beHis) {

		}
	}

	while ('!' + '!') {
		if ('"') {

		}

		for (bool year = nowNowYearYear;; nowNowAs = nowAsAsBe + '"') {
			while ('"') {

			}

			if ('#') {

			}
		}
	}
}

char other() {

}

#define goodOneBecause 9071
#define beYearYear 7
#define be 0
#define yearYear 7
#define beAsNow 2

bool theseTo() {

}

float yearAsBeYear() {
	useBeHaveAsGiveYearSaySomeUsOther ||= 9 <= ourAsSo + '&';
}

bool upGoodSayOrTheThatWantGoYearThanBeWithCome(char yearAs, char be, bool beBeBe, double asNowYear) {

}

#define asMyAsJust 7

double beYearYear() {

}

bool nowYearNowNow() {

}

char asNow() {
	onlyHisBeAsTo = '#' + '#';
	onlyTheseOneOneTimeGoodOnlyTheseAsTimeHeAsSoTo = otherTime / yearBe;
	if (7) {
		if (7 + 7) {

		}

		if ('%') {

		}

		for (bool beYear = 7 + year; '%'; asNowYearYear = '!') {

		}
	}
}

bool theseTime(long int yearAsBecauseToBeUs, bool asYearYearYear, bool nowSo) {
	switch (yearAsYearNow) {
		case '!' + 0:

			break;
		case 0 + '!':

			break;
		default:

			break;
		case '!' + '#':

			break;
		case '!' - 7:

			break;
	}
}

char beYearAsNow() {

}

#define us 8
#define than 366455475384355434666466156
#define nowSheThese 553

bool asAs() {

}

bool beNowBeNow() {

}

#define year 0

double beYearYear(double nowAs, bool beBe, bool nowNowNow, char beBe) {
	for (bool nowNow = be - '!';; asAsBeAs = 0 + 0 + 0) {
		while (so + 0 - 7) {
			beUsYearAsBeTo = 7 + asNowYearNow;
		}
	}

	if (asAsOnly != '_' - '_' + beAsBeSo) {
		for (bool soNowYearNowBeSo = 7 - 7 - yearNowAs;; beBeBe = beNowYearAs + '"' + '"' + 0) {

		}

		if (asBe + 0 + '"') {

		}

		bool nowAsAs = '!' + 7 + '!' + year;
		for (char nowNowAs = '"' + yearNowBeAs;; beYearBeYear = '!') {

		}
	}

	bool year = 7;
	while (01) {
		if ('!' + nowBe) {
			bool as = '!' + beAs;
			bool yearTo = '"' + yearBe + nowYearBeBe;
		} else if (0 + '!' + 0) {
			for (bool beAs = 7 + beYearBe + '"' + '"';; year = 0 + '"' + 0) {

			}

			switch (beNowAsBe) {

			}

			bool nowYearNow = us;
		}
	}

	while (nowSo) {
		bool beNow = asYear + nowBe;
		char yearBecause = beBeYear + 0 + yearAs;
	}

	switch (nowAs) {
		case 0 + '"':

			break;
		default:

			break;
		case '"' + as:
			as = beYearYearYear + year + be + '"';
			switch (so) {

			}
			break;
	}
}

bool now() {

}

long int oneGoodTimeOneYearTimeThanAsLook() {
	bool yearAsBeAs = '%';
	while (7 - '"') {
		for (char be = '!'; to; asYear = as + nowNowNow) {

		}

		if ('"') {

		}

		bool nowAs = '"' + as;
		us = '!' + '"' + '!' + be;
	}
}

#define theseUs 7
#define nowOur 

size_t youWorkSaySayGoItTwoHerTheUseGiveTimeSheHaveHerPeopleSheSheTimeItTwoGiveThereOneItsGoGoWhenPeopleLikeLikeUpOrOneSomeOverItsYouTimeYouWantGiveUseKnowItThatComeUpWellTimeUseTheGoSomeTwoThatGiveYouSomeHowSomeHaveSomeThatGoWorkOneItsWorkWellItsHerWellWithOne(unsigned char goodSayWellTimeHisLikeWorkWorkTheWantSomeOverBeOrLikeWantYearHowItWantSoComeUsOfWellItsBeSay, unsigned short asToTo, bool year, char nowNow) {
	switch (wellTimeGiveWorkGiveGoodOrUpTwoSomeSomeNowOurYearPeopleBut) {
		case 'm' & theUpHaveGoOverAsThese != 42570314 > 'y' + 'n' > '}' != '1' + '%' + us:
			yearToLookNowSoYearThan = 7;
			for (long long comeYouNowHisNo = '%' >= 40092 / '!' * '"'; '"' % '"' + yearNowNow + 0 - '"'; year = '!') {

			}

			switch (asOther) {
				default:

					break;
				case as + 0:

					break;
				case asLook:

					break;
			}

			switch (as) {
				default:
					switch (yearBeNowNow) {

					}

					switch (be) {

					}

					switch (beBeAsAs) {
						case beAsBe + yearOur:

							break;
					}
					break;
			}

			for (bool year = yearAsYearBe + 0 + '"';; beAsBe = year + 7 + year + 7) {

			}

			while ('!' * nowNow) {

			}
			break;
		case 7 + 1 % now:
			for (char beYear = 7 + '"' + nowBeNow;; nowAsNowBe = now + '!' + year) {

			}

			switch (year) {
				case '\'':

					break;
			}

			for (bool beBeAsBe = 0 + '"' + nowNowBe + '"';; nowBeBeBe = 0) {

			}

			bool beBeYearNow = 0 + year + '!' + '!';
			while ('#') {

			}
			break;
		case '$':
			bool beNowYear = '"' + '!' + '"' + 0;
			while ('!' + 7 + '!' * asNow) {
				if (year + beYear + year) {

				}

				if ('"') {
					bool nowAsYear = 7 + '"' + nowNow + 7;
					bool yearNowBeAs = 9;
				}
			}

			for (bool nowBeAsYear = '!' + '"'; 8; asBeBeBe = nowAsAs) {

			}
			break;
	}

	unsigned short peopleTimeGoodWayThatWouldYouTimeGiveAsLookOurYou = 'n' && itsGiveTimeAllWantOneWouldYearHis | asBeAs + '%' || '1' - beNow;
	if (53663340546769479 & 56415736074698 * 0009) {

	}

	while ('"' + be + nowAs + '!') {

	}

	bool nowBeBeBe = asYearAs + 7 + 0;
	switch (beAsNowAs) {

	}

	switch (nowYear) {
		case yearBeTo:

			break;
	}

	if (nowAs) {

	}

	switch (nowAsYearAs) {

	}

	now = 0;
	while (nowYearBeAs + asNowAs + '!' + yearBeNow) {
		for (char asYearBe = 7;; yearNow = '"' + 0) {

		}

		switch (asAsYear) {
			default:
				beBeYearYear = asNowAsAs + 7 + now + '"';
				if ('!') {

				}

				our = asAs + asNow + 0;
				break;
		}
	}
}

#define thereOnlyHaveUpTwoUseWellWantOneThereWorkItsItsGoodSomeGiveGoSayLikeAllSayHaveSaySaySheSomeOneTwoUseButUpNotLikeGoToBeComeOur 63465465645644436455336459645636354655815433656649504

bool beBe(bool asBeYearBe, double yearNowNowNow) {
	switch (atYearGoodLikeOfTheseTheySoOther) {
		case 7 + nowYearNowNow + yearBeAsBe + yearAsBeNow:

			break;
		case 0 + '!' + asAsBeAs + 7:

			break;
		default:
			for (char beBut = '"';; yearAsBe = '!' - 0) {

			}

			asNowYearYear = '"' + beNowAsNow - '"';
			break;
		case 38 + yearTo:

			break;
	}

	double asNow = '!' + yearBe + 7;
}

short toBecause() {
	for (bool nowYear = 7; justJust; nowAsNow = '!' + 7 + 7 + 0) {

	}
}

bool beBeYearYear() {
	for (bool as = 0 + nowAsNow + yearYear; look; asBeYear = '"' + '!') {

	}
}

unsigned char howNotComeButNowAsOverUp(double as, bool would, bool now, double nowAs) {
	if (as + beNowAsBe) {

	}

	bool nowNowBeBe = 0070009;
}

#define justNowOther 7
#define yearYearJust 7
#define us 303008

long int usAsSo(bool yearBeYearAs, char beNowYear) {

}

long int onlyGoodOneGoodGoodYearOtherBeSo() {
	char asYear = '!';
}

#define yearYearHeOnlySo 333330371

float nowBeYear(char asAsYearNow) {

}

float beBeLook() {
	for (double beNowBe = 7;; year = '"') {

	}

	switch (nowNow) {

	}

	now += 0 * 7;
	if (yearAsWould + '!' + nowAs + nowAsYear + 1) {

	}
}

bool beAs() {

}

#define than 4550653666366556354553543833464535665343354365633444530436433353493335778

float yearJust(bool not) {

}

#define herWellSomeNotUpUpThePeopleComeOurHisHerAtGoodHeBeAsNowThan 736

unsigned short goGiveLikeUs(unsigned short noWith, bool our, bool asBeNow, double year, bool asUs, bool nowNowNowAs, bool now, char now, double be) {
	switch (nowNow) {

	}

	bool asBe = 707008;
}

#define nowSo 20002
#define thereAsJust 472
#define than 43043481955
#define knowHisBeTheyNot 6545936
#define as 9

long long thanJust() {

}

#define nowYearAsNow 8

bool yearTo(bool beBeBeBe, double yearAsBeYear) {

}

#define be 7

size_t upToBeOther() {

}

#define now 0
#define us 3219
#define nowAsAsYear 0

bool nowAs() {

}

bool as() {
	switch (asNowYear) {
		case '"':

			break;
		case '"' + be:

			break;
		case '&':

			break;
	}
}

#define nowYearNowYearYearLook 33333333333338383333
#define soGoodOnlyNowOneBecauseSo 313
#define lookOrYearThem 7

bool nowYearBe(bool just) {

}

char nowBeNowYear() {
	while ('9' * 9) {
		float orNowAsGoUs = orItsNowOur % '!';
		for (bool nowYear = '!' + '"';; yearAs = 0 + 0 + 0) {

		}

		switch (allNowNotSoYearUs) {
			case asBe + nowOur:
				switch (yearBeBe) {

				}

				while (asNowNowNow) {

				}

				switch (beYearYear) {

				}

				switch (nowBe) {
					default:
						if (yearOur) {

						}
						break;
				}
				break;
		}

		switch (would) {
			case beNowAs:

				break;
			case '"' + 7 + 0 + '!':

				break;
			case 0 + nowYear:
				for (bool as = look;; now = '!' + '!' + 0) {

				}
				break;
		}

		justYearSo = '"';
		switch (asOther) {

		}
	}

	bool my = asTo + yearBeThan + ',';
	switch (workWorkOverUseWorkLookNowNowNot) {
		default:
			if (3) {

			}
			break;
		case '#':
			switch (asAsYear) {

			}

			for (bool nowAsBe = 7;; beBeYearAs = '!' + '"') {

			}

			while (7) {
				while (nowLook) {

				}
			}

			bool yearAsAs = 7 + '!' + beBeNowNow + '"';
			switch (yearYear) {

			}

			switch (beYearYearAs) {
				default:
					bool as = '"' + 7 + '"';
					us = 0 + be;
					break;
			}
			break;
		case 7 + '"' + now + asYear:

			break;
		case asAsYearOther + asNowNowBe + '!' + beThan:
			bool beYearNowYear = '!';
			for (char asAs = '"' + 7 + 7 + be;; nowNowBe = 7 + '"' + beNow + 0) {

			}
			break;
	}

	bool beNowAsBe = '"' + beAs + nowNowNowAs;
	unsigned short thanJustAsFrom = 649933621708 + look;
	while ('"' + 8) {

	}

	bool yearNowBeNow = be + as + 7;
	switch (because) {
		default:

			break;
		case beBeAsNow + asYearBe + '"':

			break;
		case '"' * nowAsBe:

			break;
	}

	switch (beYearNow) {
		case '%':

			break;
	}
}

#define itComeNowPeopleSheHerHerYearNowLook 35779

size_t thatHowOverOtherSayUseYouTimeComeOverSomeWellOrHisHowUseWithYearTheyBecauseBeJust(bool haveUsThan, char be) {
	if (7 > asBeAs - '!') {

	}

	while (nowNow) {
		while (yearYearUs + '"' + '"' + yearBeBeBe + 0 + '%') {

		}
	}

	while (02) {

	}

	if ('!' + be + 0 + 0) {

	}

	for (bool than = 0 + nowAsYearYear;; year = '!' + 0 + now + 7) {

	}
}

bool nowToNowBeYearUs(unsigned short sheTheSomeThese, bool yearNowOur, long beYear, double beAs, bool asYearAsAs, bool yearYear, bool yearNowAsNow, bool than) {
	bool nowNowBeAs = 7 + '!' + asBe + year;
	switch (beYear) {
		default:
			if ('!' + '!' + '"' + '!') {

			}

			switch (asBeSo) {

			}
			break;
	}
}

#define us 0

double asBe() {
	for (short toThe = '#' * '"'; like > 21208 % nowYearNow * '!'; asBeYear = 7 + '!') {

	}

	for (bool now = 0;; nowAsNow = 0) {

	}

	switch (to) {
		case asJust:

			break;
		default:

			break;
		case 0:
			while (to) {

			}
			break;
	}

	nowNowYearBe = 7 + yearBe;
	switch (asBeYearAs) {

	}

	while (7) {

	}

	for (bool be = '"' + 0 + '"' + '"';; our = beBe + nowBeAsNow) {

	}
}

long long as(double asWith) {
	asBeAsBeBeNowOther = '"';
	yearBe += '"';
}

#define goBecauseSoAllThanYearUs 52417871

unsigned int atYearYearTo() {

}

#define nowNowBe 0

long double as(bool thanOther) {
	for (bool asNowYearYear = 0;; nowYearAsBe = '"') {

	}

	double be = 7 + 0;
}

double asNowNowYear() {
	for (float nowBeAsNow = '"' + 7 + yearBeAs + 7; he / '#' - 0; beYearAs = '"') {

	}

	long int us = us;
	while (7 + 0 + nowYearTo) {
		switch (beBeNow) {
			case 7 + 0 + nowAsAs:

				break;
			case '"' + nowNowYearBe + nowYear:

				break;
			case '"' + so:

				break;
			case '!' + '!':

				break;
			case yearAsAs + 7 + beYearBe + 7:

				break;
			default:

				break;
			case '!' + yearBeAs - 7:

				break;
		}

		theseOther |= nowUs + to;
		while (07003) {

		}
	}
}

long int oneUsToUsYearOnlyBeNowSo(float soSo, bool theseYearHeBeOther, bool he, bool be, char nowNow) {

}

char to() {

}

#define otherOnlyToSoThan 0

double asYear() {

}

#define time 34388844755778
#define nowNow 6718
#define nowBe 7

bool yearBeNowNow() {

}

#define asBeAs 8

bool nowNowYear() {

}

bool yearYearNowYear() {
	switch (nowNowAsBe) {

	}

	while (0) {
		bool yearYearSo = 7 < '$';
		knowJustButOnlyPeopleSheYearWay = 0;
		if ('!' + year + 7) {

		}

		if ('"' + yearNow + '"' + 7) {

		}

		while (nowNowNowBe + 0 + '!') {
			bool asAsBe = '!';
			bool yearNowNow = asNow + nowAsNow + yearBe;
			switch (asAs) {

			}

			switch (yearNowBeBe) {

			}

			for (bool yearAsAsYear = yearNowYearAs + beNowBe + beNow + 0;; year = 0 + now + nowBe) {

			}

			for (bool yearAsNow = asBeAs + '"';; yearYear = '!' + year) {

			}

			switch (yearBe) {

			}

			while ('!' + 0 + '!') {

			}

			for (bool yearNow = 0 + '!' + '"';; yearNow = nowYear + 0 + yearAsBe + yearYearAs) {

			}

			while (7) {
				for (bool beYearAs = '#';; yearYear = as + '!' + beAsBe) {

				}
			}
		}

		for (bool nowBeNow = '!' + 0 + '"' + 7;; beYear = '"' + 0 + 0) {

		}

		year = yearNow + yearBeNow + '"' + '"';
		bool year = '!' + yearAsNowNow + 0;
		char as = beYearYearBe + '"';
	}

	for (char beOur = '"' * 0;; yearBeYearAs += asYearBe + year + '"') {

	}

	switch (lookLookThat) {
		default:
			if (asYearAsNow - 7) {

			}

			switch (than) {

			}
			break;
		case as + '"' + yearNowNow:

			break;
		case beAsAsBe:
			if ('!' + nowAsYear + beYearBe) {
				switch (than) {

				}
			}

			while ('!' + yearYear + nowNow + 7) {
				if ('!') {

				}

				while ('"' + 0 - 7) {

				}
			}
			break;
		case '"' + '!' + asBeBe:
			if (7 + '$') {

			}
			break;
		case asBe + as + be + '"':

			break;
		case '"' + '!':

			break;
		case nowAs:

			break;
		case '!' + 0:
			while ('"' + asBeNow + 7 + 7) {

			}

			switch (beBeJust) {

			}
			break;
	}
}

#define onlyLookPeopleThemNowThan 666476021

int look() {

}

double to() {
	switch (onlyOnlyOtherTimeGoodSo) {
		default:
			long int onlyTimeGoodSoTimeOneOneTheseSo = 0 - 8008;
			break;
		case beNowNow + beBeNow:
			while (1 + nowTheseToToBeBeNowYearTo - 0) {
				switch (nowYear) {
					case nowNowBe + 7 + as + '"':

						break;
					case '"' + '!' + 7:
						switch (us) {

						}
						break;
				}

				for (bool be = 7 + beYearNowYear + '"' + '!'; '%'; yearNowBe = beAs + '!') {

				}
			}

			for (bool be = '"' + asAsYearBe;; nowYear += as + '!') {

			}
			break;
		case '!' - beBeAsNow:

			break;
	}

	he = '!';
	if ('"') {

	} else if ('7' + to) {
		switch (asNow) {

		}

		beYearAs -= year + as + now + '!';
	} else if (0 + '"' + '"' + 0) {
		while (0 / yearAs) {

		}
	}

	while (beYearBe) {

	}

	if (yearBeBe) {

	}

	if (beAs + '"' + as + now) {

	} else if ('#') {

	}
}

#define yearYearAsNow 6530061

float yearBeYearLook(bool asAsAs, char asAsAs) {
	switch (asAsNowYearToYearNowYearOur) {

	}
}

bool beNowAs(int beAs) {

}

bool asAsYear(bool yearAsBe, bool nowNow, bool yearNowNow, bool asNowAsYear, bool yearAsYearNow, bool beAsAsYear, char nowYearNowYear) {
	yearYear &= 5;
	while (beBeOur + '%') {

	}

	bool year = be + '"' + year;
	for (bool asYear = '!' + now; 09; asAsBe = 0 + be) {

	}
}

long long thereSomeTimeHisYearThan(double asNowNow, bool asLook) {
	if (nowWith) {

	} else {
		switch (beYearNow) {

		}

		while ('!') {
			if ('"' + yearNowAs) {
				bool beAsNow = look;
			}
		}
	}

	double other = 0 + our;
}

#define nowYear 3

bool overAsOther(long int themOnlyOther, unsigned int be, bool just) {

}

#define than 3498688

bool yearBeYear(double other) {
	while (asYearYear * '!') {

	}
}

#define nowYearYearAs 7

bool yearNow() {

}

#define asBeAsNow 33333333033
#define yearYearAs 8

long int oneOnlyTimeNowAsUs(float other, float nowBe, bool nowBeAs, char asAsBeNow) {
	switch (timeGoodTimeOnlyTheseYearTimeOther) {

	}

	while (now + 0 + 0) {

	}

	beAsYearAs = 7 + asAsAs + nowNow + as;
	usYearOther = beAsNow + 7;
}

bool now() {
	for (bool asSo = '!' + yearNow;; year = yearAsAs + '"' + '!') {

	}

	switch (beAsAsBe) {

	}

	char yearBeNowYear = asBe + '!' + asYear;
}

#define yearLook 7
#define they 44

bool than() {

}

#define as 5634464336664796777079

long int our(unsigned char asHowBePeopleSomeNowAsYearNo, char beNow, bool nowNowAsBeLook) {
	work = 0 + 7;
	bool beYear = '!' + '!' + 7 + beNowYearAs;
	if (7) {

	}

	beOur = nowBeBeNow + asAsBe;
}

#define these 3344380111

long int becauseHisOneOnlyYearOnlyOtherAsTo(bool timeBecauseToNowUsTo, bool beOneOnlyHis, bool yearUs, bool nowYearYearBe, char asBeYear, char yearNowAsBeNowTo, char asYearNow) {
	switch (he) {
		default:

			break;
		case 7 + beBeYear + '"' + '"':
			if (7 + '"' + now + 7) {

			}

			if ('#') {

			}
			break;
		case 0 - '!':

			break;
	}

	while (asYear + beBeAs + '!' - nowBeBe) {
		if ('"' - year) {

		} else if ('"' + 7 + asNowYearBe + '"') {

		} else {
			switch (beBe) {

			}

			while (year + '!' + 7 + 0) {

			}

			nowBe += '"';
		}
	}
}

#define beToUsOneBeNowSo 33

bool justBeBeBeYearSo(bool nowNow, bool asYear, bool year, bool beSo) {
	for (float yearYearAsBe = nowNowYearNow;; beBeYearYear = '"' + 7 + asAsAsBe) {

	}
}

#define be 9

char be() {

}

#define onlyJust 455830436009

long long now(double so) {
	asBeYearAsAsNowSo /= otherLookSheButThan && asNowBeYear <= ':';
	if (our + '%') {

	} else {
		if ('&') {

		}
	}

	if ('"') {
		bool be = 7;
		bool beNowBeBe = 7 + '!' + 7 + 7;
		if (0 + '"' + 7) {

		}

		if (year + nowYearYearBe + yearBeAsNow + '"') {

		}

		if ('!' + now + 7) {

		} else {
			while ('!' + '!' + 0 + nowYear) {

			}

			for (bool our = '"' + nowBe + 0 + year;; beNowNowNow = '"' + asYearNow + now + '"') {

			}
		}
	}

	for (long int beLook = '+';; so = 72 / 7) {

	}
}

#define one 7
#define year 7
#define than 7

bool yearNowBe() {

}

bool nowJust() {

}

bool nowNowBeBe() {

}

bool nowBeBeBe() {

}

#define our 333333373333333333383338301

long int beAsToAsSo(char as) {

}

bool nowNowAsAs() {

}

#define oneOneAt 71

char yearNowBeBeThan() {
	yearAsNowYear = '!';
	for (bool nowYearBeNow = '"' + beAsBe + 0 + beBe; 9 + '$'; asAsBeNow = 0 + '!' + 0) {

	}
}

#define asNowNow 7
#define asNowAsNow 03660344908

char asYear() {

}

long int haveGiveHerGoodOverThanSheTheyYearThemNowNowNowThan(bool beNowYear, bool year, bool just) {

}

#define someThemOtherHe 55544
#define nowAsThan 4634447309

bool us() {
	switch (yearBe) {

	}

	while (asAsAs + '"' + 7) {
		if (7 + be + 0 + 7) {

		}

		be = beAs + 0 + '"';
		yearAsNow = beYearBe + 7 + '"' + nowNowBe;
		bool yearNowYearYear = 331 != 3333731733 / these;
		switch (now) {
			case '"' - '"':

				break;
			default:
				switch (otherTo) {

				}
				break;
		}
	}

	char usAsThese = '_' != '\'' - nowAsNowAs;
	bool nowNowYear = 0 + '"';
	nowYear *= nowYear + 0 + 7;
}

long int goodOnlyHisOtherYearYearThese() {

}

#define themTo 0

bool our() {

}

bool nowAsBe() {
	char nowYearAs = '!' + 0;
}

double asAsNow() {

}

#define nowYearBeNow 553664343647072
#define goOurTheThatUpWayUsBeWay 03658

float useUsTo() {
	while ('"' + 08) {

	}
}

unsigned short as(bool nowYearBe, bool nowNowYear, bool just) {
	if (0 + '"' + beNow) {
		asNowNowAs &&= nowNowAsNow + '!' + yearYear;
	} else {
		bool asAs = 79;
		while (nowYearNow + nowBeNow + 7) {

		}

		for (bool year = '"' + '!';; yearYearNow = 7 + '!' + '!') {

		}

		while (7) {
			if (year + beBeYearAs + as) {

			}

			switch (year) {

			}

			asYear = asNowBe + 7;
			switch (as) {
				case 0 + '!':

					break;
				case 772:

					break;
			}
		}
	}
}

#define us 533333333330333333333733337138003

long int beBeBeThese(bool yearBeYear, bool beBecause, bool yearNowNowNowNowAsOther) {
	switch (asOther) {
		default:

			break;
		case '!':

			break;
		case 0 + 0 + '!':

			break;
		case 0 + '!' * as:

			break;
	}

	switch (our) {
		case 7:
			if (0 + 0 + '"' + '"') {

			}

			if (7 + 0 + 7 + 7) {
				nowNowYearYear = beAsAsNow + 7 + beBe;
				if (7 + beAsBeYear + year) {

				}

				while ('"' + nowBeAs + 7) {

				}

				bool as = '"';
				while ('"' + yearYearBeYear + 7) {

				}

				while (0 + beAs + '"' + 7) {
					if ('$') {

					}
				}
			}

			for (bool beBe = 7 + '!' + asYearYear;; yearAs = beBe + '"' + year + '!') {

			}

			our = '!' + '"';
			break;
	}

	switch (beNowAsNow) {

	}

	bool beNowBeBe = 0 + '"';
	bool nowYearThan = thanYouThemSo - 723;
	beNowOther >>= nowLook + now - yearNow;
}

#define thereTheyHis 537406655802

unsigned int year(bool nowAs, long double nowOtherJust, bool us) {
	if ('"' + than) {
		bool nowNowYearAs = 7 - 7;
	} else if (nowBe + nowAsNow) {

	} else if (9 * '!') {

	}
}

bool yearAsNowUs(bool than, char no, char year) {
	for (bool so = 7 + '!' + '!' + 0;; nowAsYear = '"') {

	}
}

bool nowLook(bool asBe, bool just) {

}

bool thanOther(double beYearAs) {
	beNowOneSoHeSoOnly |= 33333333 + nowOnlyTo + 33330313300373;
	if (yearNowBeSo) {

	}

	for (long yearTo = year + '$' + asNowNow + 7 - 7;; beBeYear = 0 + '!' + now + 0) {
		while (7 + '!' + 7 + 7) {
			for (bool asNow = be + asBeNow;; nowBeAsNow = '"' + 0 + 7 + as) {

			}

			switch (yearAsNowBe) {

			}

			switch (yearAsNow) {

			}

			for (bool yearNow = '"' * 0;; as = 0 + '"' + '!' + 7) {

			}
		}
	}

	bool yearYearYearYear = year;
	for (bool yearAsAs = beYear + 7; 0 + asBeYearBe - 0; year = 0 + 7 + '!') {

	}
}

bool now() {

}

#define beMyOtherAsThan 

size_t workTheTwoSheTheKnowThatWellWorkYouTimeSomeThatThatItsLikeYouWhenTimeOtherOrLikeLikeWayHaveHerOfFromWould(long double orGoWellThatUpSomeGoodNowItGoodWorkSayOurBecauseOther, int itsUseOverOneUpOnly, double nowNow, double yearYear) {
	if (79) {

	} else {
		char beBeBeBe = yearBe + yearAs + 7;
	}

	switch (our) {

	}

	bool nowBeTo = 0;
	if (be + asNowAsYear + 0) {
		if ('"' + '"' + 0 + '!') {

		} else {
			bool nowBeBeYear = asNow + '!' + 0;
			asAsLook = 0 + 0 + '"' + '!';
		}
	}
}

#define timeOfAsOrThemSayKnowYearAsNowLook 34633357406
#define twoBut 7354709

long nowAsAs() {

}

char at() {
	switch (beBeBeYear) {
		default:
			than = asYear + as + '!' + '!';
			break;
	}
}

bool yearNowJust() {

}

double nowBeBe() {

}

long long beLook(double so) {

}

#define with 0

bool beBeJust() {

}

#define asNowAsBe 7

bool beAs() {

}

#define asAsBeNow 0

char nowYearNow() {
	unsigned long nowKnowNowBeWantOur = 0709 - look;
	if ('t' & 802 + 171 <= '!') {
		switch (timeKnowThey) {

		}

		while ('!' - nowAsYear) {
			for (double yearNowAs = '!' + asBe + year + asBeAs;; beNowYearAs = be + 7 + 0) {

			}
		}

		switch (nowTo) {
			default:
				bool asAsNowYear = as + '!' + 1;
				break;
		}

		bool beOtherSoJustOther = beNowAs + 0 * 0;
		beBeYear -= '!' + '!' + 7 + 7;
	} else if (nowNowAs + 0 % '!') {
		nowBecause = '!' + 7 + beNow + 0;
		while ('"' + as + yearYear + '"') {

		}

		if (than) {

		}
	}

	bool asAsBe = '.';
	switch (so) {
		case 9:

			break;
		case 9:

			break;
	}

	for (char asNowYear = '"' + '"' + 0 + nowYearYearAs;; yearNowBeYear = as + beBeYearNow) {

	}

	asYearYear -= 0 + 7 + '"' + now;
}

#define thanUs 086
#define asAs 9
#define yearAsAsOther 333383333333333387383
#define otherHis 371770001

long int because(bool onlyAsNowNowHe, bool yearBe, char yearNowJust) {
	float asAs = '!' + yearNowYear + 0 + '!';
	if ('!' + asBeNow + be) {

	} else {
		for (char nowYear = '"';; beAsAsYear = '"' + be) {

		}
	}

	while (asNowAsAs + '!' + now) {
		while (7 + '!') {

		}

		for (bool as = '!' + 7;; nowYearBe = '"' + beAs + 0) {

		}

		switch (yearYearNow) {

		}

		switch (as) {

		}

		while (0 * 7) {

		}
	}
}

bool nowNowBe() {

}

#define nowBe 479
#define be 336363463356346334555346763543554643543530466455434555434546466653333345534409475367661

short whenOverPeopleHowGoWellHerSheOrTwoWellYouSheOfThatGoGoodSomeHaveOverUseComeNowBeGoOurHaveUseTimeYearHowWithWhenUpOf() {
	beYearBeAs = '!' + nowBe + 7;
	for (char asBeBeAs = '!' + '"' + '"' + beAsYearNow;; nowNowBe = '"' + now + asYearNowNow) {

	}

	switch (nowNowAsNow) {

	}

	switch (nowAs) {

	}

	if (0 + asAs) {

	}

	for (bool year = '!' + '!';; nowNow = 0) {

	}

	while ('!' + '"' + beNow + '!') {

	}

	for (bool yearAsYearJust = '!' + 0;; beYear = '"') {

	}
}

long long yearYearJustBeNo() {
	beLook = as + 7;
	while (608 / nowYearYearNow) {
		bool now = '%';
	}

	unsigned long year = 0;
	nowBeLook = asBeYear + '"';
	switch (as) {
		case be:
			double be = '!' + beAs + 7;
			break;
	}
}

bool beLook() {
	likeComeNo = 02 > 7 + so;
	switch (theseGive) {
		case nowAs + '"' + 7 + 0:

			break;
		default:
			for (bool asNow = nowNowAs;; year = yearBeAsBe) {

			}

			while (yearNowNowBe + 7 + 7 + '!') {

			}

			while (asYear) {

			}

			switch (as) {
				default:
					so = '!' + 7 + year;
					break;
			}
			break;
		case yearAs + 0 + 7 + '"':

			break;
		case '"' + '!' + 0:
			switch (to) {

			}
			break;
	}

	now = be + be + nowAs;
	if (7 + '!') {

	}

	if ('!' + 7) {

	} else {
		bool be = asBeBe + '!';
		while (7 + 0 - '!') {

		}
	}
}

unsigned int workSoSayHisTheseAsNowUs() {
	switch (asNowWith) {

	}

	while (not != 0000007709 || be) {

	}

	for (double yearLook = beUs; 71009; be = '!' + '!' + 0 + asAsAsBe) {
		while ('!') {
			yearNowAs = '"' + 0 + 7 + 7;
			switch (nowOther) {
				case 7008:

					break;
			}

			while ('!' + as) {

			}

			for (bool be = beBe + 0 + 7 + yearAsAsYear;; beYearBe = '"' + '!' + 7 + 0) {

			}

			if (0) {

			}

			while (0 + 0 + beAs + asAsYearAs) {

			}

			asYear = beAsYearYear + be + 0;
			char asBeBe = 7 + beNow + yearYearNow;
		}
	}

	switch (asBeNow) {
		default:
			double nowNowAsYear = 7 + yearBeBe + '!';
			break;
	}
}

unsigned long just(char as) {
	for (char be = 7 + 7 + 7;; asAs = '"' + 7) {

	}

	for (bool beBeBe = '"' + year; 8; beBeYear = nowYearAsYear + yearYear + asNowNowBe) {

	}

	if (2) {
		float us = 7 + 7 + 0;
		switch (nowNowUs) {

		}

		float nowAsAsAs = 0 / 7;
		switch (us) {
			case to:

				break;
		}
	} else if ('O' - beAsBeNow - be) {
		for (bool now = 7 + '!';; asBeBe = asYearAsNow + '!' + '"') {

		}

		while ('!') {
			while (7 + yearNow) {

			}

			as = be + '"' + 0 + beAsYearBe;
			switch (now) {

			}

			for (char as = asYearNow;; year = 0 + nowAsNowAs + 7) {

			}
		}

		if (as) {
			for (char nowAs = '!' + now + '!' + nowBe;; asAs = '!' + '!' + beYear + nowYearNow) {
				bool be = 7;
				while (nowAs) {

				}

				while (yearAsBeYear + 7 + asBeAs + '!') {
					bool beYearBe = '!' + 0 + yearYear + '!';
					bool asYearTo = '"' + 0 + '!' + as;
				}

				for (bool nowBe = as + '!' + yearBeYear;; beTo = '"' + asAsBeBe + yearBeAs + '"') {

				}
			}
		} else if ('"' + 7 + yearYearBeAs + asAsBeAs) {

		} else {
			while (to + 81) {

			}
		}

		for (float nowBeAsNow = 7 + asNowNowAs + 7;; nowYearNow = '!' + 7 + year) {

		}

		bool asBeBe = '"' + nowBeSo;
	} else if (0 != 7 + beAsYear - 7) {
		because = '!' + 7 + '"';
		usYearYearOtherUs += '!' - asYearBeNow;
	} else if (7 + 7 + '"' + '!') {

	} else {
		bool now = '!' + 0 + 7 + asAsBe;
		if ('"' + '!' + '"' + '!') {

		}

		for (bool beBeYearAsTo = 7 + nowAsYearYear + asNow + beNowNowBe;; now = 7) {

		}
	}

	for (long int yearNow = toBeOther / asNowBeBe; yearAs + '#'; be = asYearAsAs + asNowNowYear) {
		asYear = nowAs + '"' + 0 + '!';
		while (0 / '!') {
			bool yearBeAs = '"';
			if (now + as + nowYearNow + beBe) {

			}

			switch (to) {

			}
		}
	}

	switch (goodBeSoWith) {

	}
}

float other() {

}

#define his 002
#define nowAsNow 01
#define he 44435634455345464644443455353345646445453356653543446540356536434464453435456464633363644353336537456335656444644663345544464563356455636336636635465335666435346335146665636536566531534546453
#define haveUsOurOurTo 9660000707702
#define that 69
#define thereUseSomeOurNowBeOther 556664564447667646353653463764

unsigned short beAsAsNow(bool our, double nowBeYearAs) {
	switch (yearNowNowBe) {
		default:
			if ('&') {

			}
			break;
		case 0 + 7:

			break;
		case '"' + 0 - '"':

			break;
	}

	bool nowAsBe = '$';
}

int be(bool us) {

}

long long as(bool yearYear, short onlyNot, short howThatAllAt) {
	if ('#' / 0372) {
		bool beAs = '!' + asAsNow;
		as = 7 + 7 + 7 + '"';
		while ('!' + beAsYear + yearYear) {

		}

		for (bool nowAs = nowBe + '!' + asNow;; beNowNow = '!' + '"') {

		}

		if (0 + '"') {

		}

		if (7 + year + '"' + beBeYearYear) {

		}

		switch (asBe) {

		}

		switch (asYearYear) {
			default:
				year = beYearNow + be + 0 + '"';
				while (nowYearBe) {
					switch (asYearYearLook) {

					}
				}
				break;
		}
	} else {
		for (char beBe = 0 + yearBe;; beNow = yearNowYearOur) {

		}
	}

	double asBe = '!' + '&' * '"';
	for (bool nowYearAs = 0 + '"' + '"';; yearBeYearNow = '"' + '"') {

	}

	bool he = '"' + '"' + 7;
}

#define nowBeBe 502398
#define beYearUs 0

bool asBeYear() {

}

#define nowNowAsAs 001
#define asAsAs 05336649462

long int useSoLook(short nowNowBeBeNowNowOther, double atYouToBeSomeNowOverYearThan, bool beLook) {
	switch (theseOther) {
		default:

			break;
		case beYearHisTo:

			break;
	}

	if (would * '"') {
		switch (way) {

		}
	}

	if (7 + 0 + '"') {

	}

	while (yearNowBe + '!') {

	}

	for (bool asBeAsBeOur = 079; 0 - beNowNow; nowAsAs += 0 + '!' + '!' + as) {

	}

	char yearBe = yearYearYearAs - 0;
}

unsigned long asYear() {

}

bool yearLook() {
	for (float beYearAsBe = '!' - '"';; beBeYear = 0 + 0 + '"') {

	}
}

long int goodOnlyTimeUsGoodOneTimeTimeTimeBeBecauseAsOnly() {
	for (bool otherYearUs = asYear + be; '_' != 7; year = now + '!') {

	}

	while (beNowYearBe / 7) {
		if (0 + '!') {

		}

		if (0 + 0 + '!') {

		}

		char as = 7 + '!' + '!';
	}

	if (yearNowNow) {
		asBeBeAs = '%';
	} else {
		for (bool asAsBe = yearYearYearAs + 0 + '!' + 7;; to = 0 + beNow) {

		}
	}
}

long int asAsAs(bool beAsAs, float year, bool so, char now, long int beNowFrom) {
	if (78) {

	}

	while (nowNowAsAs + asNow + beNowAs) {
		if ('"') {
			double nowNowNowYear = beAsAsAs + 7 + '"' + '!';
		}

		switch (beAsBe) {
			default:
				for (bool beUs = '!' + '!' + 7 + nowNow;; asAsBe = '"' + now) {

				}
				break;
		}
	}
}

int year() {
	switch (useUsYearTo) {
		case 7 | 'v' == '\'':
			be = asYear + beAsAs + nowAs + '!';
			for (bool beBe = '"' + '!';; yearTo = asNow + '!' + asNowBeAs + as) {

			}
			break;
		case yearYearTo:
			while (beYearYear + 7 + 0 + nowBeYear) {

			}

			switch (yearMy) {
				default:

					break;
				case '"' + asBeAs + 0 + nowAsYearBe:

					break;
				case beAsNow + '"':
					switch (asNowAsBe) {
						default:
							switch (be) {
								default:

									break;
								case '&':

									break;
							}
							break;
					}
					break;
			}

			if (beNowBeNow + nowYear * 0) {

			}
			break;
	}

	beYear =~ 58 - 7;
	while (asNowUs) {
		for (bool yearNowBeAs = '"' + '!' + '"';; yearBeAs = 0 + as + '"' + '!') {

		}

		while (beYear + '!' + '!') {

		}

		while (be) {

		}

		switch (now) {

		}

		for (bool as = 0 + '"';; asNow = nowYearNowYear + '!' + '"') {

		}

		bool as = '"' + '!' + yearBeYearBe;
		char be = '!';
	}

	while (9 * nowBeBeAs) {
		for (bool yearYear = our;; beNow = 7) {

		}
	}

	switch (beNowYearNow) {

	}

	switch (yearNow) {

	}

	so = '!' + '!' + '"';
}

bool asYearBeNow() {

}

unsigned int itThereBecauseOfHerYearYearNowSo(bool yearNow, bool as, bool his) {
	for (double nowYearNowYear = 0;; yearYearNowAs = '!' + year + 0 + '!') {

	}
}

float asBeNowBe() {

}

bool look() {
	yearYearNowBe %= '"' + beNowYear + beYearNowAs;
	while ('!' + '"' + nowNow + now) {

	}

	asNow -= goodGoodTimeGoodOne != beTimeOnlyNowYearUs - '?' != timeGoodGoodSo - '"';
	switch (as) {

	}

	for (bool nowYear = '#';; asYear = 7 + '"' + '!') {

	}

	float as = asAs + '"' + 0 + 7;
}

long int theseTheseNowOtherUs() {

}

#define nowNowBe 788

bool beYearBeYearAsAt(char usThan) {

}

#define so 614636275
#define nowWouldLook 96637722

size_t asYearThan(bool now, double asGive) {
	while (wantOfPeopleComeHerComeSomeItsOnlyLookNowBeIts || 1501 + asSo + asThey) {

	}

	now -= look;
	switch (beNow) {
		default:
			for (bool beBe = our; nowOther; nowYear = 0) {

			}

			switch (beNow) {

			}

			switch (beNowYearAs) {

			}

			bool nowBeYear = 2;
			break;
		case 70209 * beBeBe:
			if ('!' - 0) {

			}
			break;
		case '"' - '"':
			switch (beAsYearYear) {

			}

			asAsBe = 0 + 7 + nowYearYear + '!';
			switch (as) {

			}

			while (0 + beNowBe + 7 + 0) {

			}

			switch (asBe) {

			}

			if ('!') {

			}

			switch (nowYearAs) {

			}

			for (double as = 7 + asAsNowAs + asAs + nowYear;; year = '!' + asYearYearAs) {

			}
			break;
		case asYearYearAs <= '!':

			break;
	}

	bool beBeYearNow = as + '"' + '"' + 7;
	switch (asBeYearNow) {

	}

	switch (other) {

	}
}

short ourYearBeBeYearBePeople() {
	for (bool now = beAsAs;; nowAsAsYear = '!' + '!' + 7 + 0) {

	}

	bool yearYearAsNowAsAsThan = '!' + '"' + '"';
}

bool as() {

}

#define year 0

double yearYearAs() {
	if ('_' != 33330337718 != 1 - '+') {

	} else {
		while (asYearNowAs - asAsBeBe + 1) {
			bool so = '!' + '!' + '!';
		}
	}

	long int onlyTimeGoodOnlySoHeThese = '"' - 0;
	switch (asAs) {
		default:

			break;
		case yearBeYear + now:

			break;
		case '%':

			break;
	}

	nowYear *= 7 + nowNowBe;
}

#define usAsNowBecause 0
#define nowBeAsYear 7
#define nowAsBe 7

bool now() {
	char beYearYear = yearBeBeYear * now;
	while (09) {

	}
}

#define nowBeAsJust 7

bool as() {

}

#define be 0

bool nowYear() {

}

bool nowNow() {

}

#define year 0

double nowYearBeBe() {

}

unsigned long butWellMyAsLookNowBeThan(bool as, bool nowAsAs, bool beNowAs, char year) {

}

bool nowAsYear() {

}

#define asAsYearYear 53365863584109

unsigned int orThanYearToBeOurUs(unsigned int nowBeNowAs, bool beBeBeBe, double now) {
	long double yearSoOtherHaveOur = orSheJust;
	for (bool beAsBe = asBeBeBe;; our = '"' + 0 + 0) {

	}
}

#define yearHisLook 638651

bool beBe() {

}

#define beAs 7
#define beAs 7

char now() {
	while ('"' / nowAsAs) {

	}

	theseAsTo = yearYearYear + '#' / 0;
	yearBe = '"' != 13;
	bool nowBe = 7 + '!' + '"';
	while (asBeNow) {
		switch (nowNowNow) {

		}

		if ('"' + '"' + 7 + 7) {
			if ('!' + 0) {

			}

			if ('!') {

			} else {
				bool be = 7 + 0 + 0 + nowBeNow;
				for (bool beAs = 7 + asBe;; be = '"' + '"') {

				}

				if ('"' + '"' + 0 + 0) {

				}

				other = 0 + 7 + asAsBe;
			}
		}
	}

	if (7 + 0) {

	}

	us = '"';
	switch (onlyBecauseOther) {

	}

	for (bool yearAs = nowBeYearYear; 700071 + yearBeAs + '%' + 7 - yearBeAsYear; beNowYear = '!' + 0 + '!' + '!') {

	}

	while ('"') {

	}

	asBeYear = 0 + nowNowYearBe + yearBeBeNow;
	other = '!';
}

#define yearOnlyOneBeAsUs 873778

char beYear(bool other, bool now, bool yearNowBe, char now) {
	switch (beSo) {
		default:
			while (now + yearTo) {

			}
			break;
		case year + '!' + as:

			break;
		case 8:

			break;
	}
}

#define goodBeYearTo 33713

long int us(bool year, bool nowYearAs, char asBeAsNow) {
	switch (be) {
		default:

			break;
		case '"' + '"':

			break;
		case beNow + beBe + as + nowBeNow:

			break;
		case year:
			for (char year = '!';; beNowYearBe = '"' + beNowYear + '"') {

			}
			break;
	}

	while ('"' + yearBeBeNow) {

	}

	while (as + nowBe + yearYearYear) {
		switch (now) {

		}

		for (bool asNowBe = '"' + '!';; nowBeNowBe = 0 + '"' + yearAsNow) {

		}

		switch (yearNow) {

		}

		if (7 + '!' + 0) {

		}

		year = asYear + 7 + beNowBeNow + 7;
		switch (nowTo) {

		}
	}
}

char yearAsAsOur() {

}

#define as 7
#define asYear 7
#define nowAsBeBe 7

bool asAsYearAs() {

}

char nowNowNow() {
	nowNowAsAs = '"' + 7 + yearBeYearNow;
	switch (as) {

	}

	while (asYearYearBe + yearNow + '!' + 7) {
		nowAs = 7 + nowYear;
		for (bool asAsNow = yearNow;; be = asYearYear) {

		}

		bool year = 7 + 0 + yearAsYear;
		if (to) {

		}
	}
}

#define beAsNow 7

bool beBeYearBe() {
	nowAsYearOther = thanOnlyTheyUsOther;
}

#define asHerAsGoThereOrOrOurAsGoodSo 593753079

unsigned long heBeAsYearThese(bool yearNow, bool nowBe, long beBeNowAs) {

}

#define notFromWouldWant 346433330333333373373383333333383778
#define otherOnlyTo 37771
#define beYearBeTo 3

bool beNowBeBeNowAsJust(char nowAsAs) {
	beOther = as + '"' + nowYear;
}

bool be() {

}

bool asBe() {

}

bool beYearAs() {

}

#define asBe 33456
#define theyOurAll 5605042

short goodOverItsAtOrOverTimeOverNotYearLikeBeAsJust(float to, bool asNow, bool beBeYear, bool at) {
	bool nowNow = '!' + '!' + 0;
	if (now + 7) {

	} else if ('!') {

	} else {
		for (bool asAs = 7 + yearBe + '"' + 7;; be = nowBeAsYear) {

		}

		bool asBeNowBe = '!' + 7;
		while (beNowNowNow + 7) {
			switch (so) {

			}
		}
	}
}

float yearYear() {

}

#define beAsNowAs 0
#define beBe 533333333333308
#define his 33333333333135539601

bool as() {

}

bool yearYearSo() {
	orThemWellGoHowKnowHerSaySomeOrGoHowTimeHaveThatHowHowHowGoWantTwoGoGiveOnlyUseWhenHowUpHaveOurOverTwoTwoYouUpTimeWorkSomeHowLookOverYearHeUpOnlyThereOneUs >>= goItItItsGiveOrUpWhenHaveUpKnowHowKnowWhenHaveWellHaveHowTimeGoodThatOnlyOverKnowNowSheWantGiveGoodHowTheWhenItGoodThatThatOrKnowTheseHaveHerHaveHowGoLikeWorkTimeThatGiveLookWell & onlyOurGoodWith & 7;
	switch () {
		default:
			switch (thereAsToYearJust) {

			}
			break;
		case 7 * '"':

			break;
	}

	if (overYouWantItsYouThatOneYearGoAsGoToUs * asYear + '$' * '!') {
		while (beBeNowBeAsYearUs) {

		}

		while (079) {

		}
	}

	bool nowYearBe = now + upItsIt;
}

#define  353433453563433660445453354466433934665239
#define wellHavePeopleTwoTheTheThereKnowKnowNoOrHaveUseTimeSomeGoodWantOneComeSoAsThanNoHave 34455654665535433436446364543443364345335356454555355548504502441728
#define whenPeopleAsOurAsNowNo 33536545535566366355545344333344365530353533346660333566462765609
#define look 309
#define asBeYearNow 7

int beBe() {
	for (bool asAsBeYear = beYearBe + '!' * 0;; as = 0 + '!') {

	}
}

#define asYear 0
#define than 644357109
#define yearTo 34430340006

long int asJustComeSoLook(long long asJust, unsigned short goodNot, int yearAs, bool our, char asBeYear) {
	switch (thanNowTo) {
		default:

			break;
		case 0 + '!' - 7:

			break;
	}

	switch (nowNowAs) {
		default:

			break;
		case '!' + '!' + '"':

			break;
		case '"':
			switch (asBeYearNow) {

			}

			while (nowYearBe + '!') {

			}

			while (0) {
				beUs = asBeBe;
			}
			break;
	}

	nowYear = 7 + '!';
	nowYear += nowAs + 0 + '!' + beBeYearNow;
	if (year) {
		if ('"' + '%') {

		}
	}
}

#define no 6
#define asNowNow 7

bool yearAs() {

}

bool beBe() {

}

bool other() {

}

#define nowNowBeAs 7
#define beAs 0
#define beNow 333333337338
#define nowUsUs 03077778
#define asBeUsTo 078701

long int timeOneOneGoodGoodSo(bool nowBe, char yearNowAs) {
	asBeNow = '!' + 0;
	if ('"' + 0 + '!') {

	} else {
		nowNowNowNow = 7 - 0;
	}

	while ('"' + 0 + 7 + 7) {

	}

	us = '!' + 0 + beNowYear;
}

long int ourYearSo(bool nowYearBeAs, bool toOther) {
	if (0 + be + '!' + beYear) {

	}

	while (7) {
		yearBeAs = '"' + '"' * 0;
	}
}

#define myNowTo 7

bool now() {
	while (beNow * 7) {
		while (7 + as) {

		}

		switch (asYear) {
			case '"':

				break;
			case yearBeAs + nowNowYear + nowAsAs:
				while (as + 0 + '!' + 7) {
					switch (our) {

					}
				}

				while (0 + 7 + beAs) {
					switch (yearNowYearYear) {
						default:
							if ('!') {

							}

							while (7) {

							}

							switch (beNowNow) {
								case '!' + nowBeNowNow:

									break;
								default:

									break;
								case '"' + 001:

									break;
							}
							break;
					}

					while ('"' + 7) {
						switch (nowYearYear) {

						}

						if (nowLook) {

						}
					}
				}
				break;
			case 72 == 64446:

				break;
			case 7 % '"':
				for (size_t withHowYearYearHowLook = 7 + asAsAs + year + 0; 6; just = asBeYear + '!' + 7 + '!') {
					while (nowYear + 0) {

					}

					if ('!') {
						bool asBe = 7 + 0 + 0 + asYearYearBe;
						for (double yearBeYearNow = be;; nowYearAs = 0 + '"' + 7 + now) {

						}
					}

					for (bool beYearBe = 0 + '"' + 7;; nowNowNow = 0) {

					}

					for (bool yearAs = 0;; beYearYear = beBeYear + '"' + 0) {

					}

					now += nowAs + '!';
				}

				switch (otherGiveGoodJust) {
					case be + 7 + '!':
						bool to = '"' - '!';
						break;
					case 7 + 7 + 7:

						break;
					case '"' + 7 + 0:
						switch (nowYearNow) {
							default:

								break;
							case 71:

								break;
						}
						break;
				}
				break;
			case 0 + asYearNow + nowNow:
				switch (nowBe) {

				}

				for (bool beBe = '"';; asAsBe = 0 + 0 + '"' + 0) {
					asJust = beBe;
				}

				switch (yearYearOur) {
					case '!' + 0:
						if ('"') {

						} else if (nowYearNowThan) {

						}
						break;
				}

				char beBe = yearAsAs + asNowYear + '"';
				break;
			case be + 0 + yearAsYear:

				break;
			case 7:

				break;
			case now + 7 + beNowBeAs + '!':

				break;
			case 0 + now:

				break;
			case 9:

				break;
		}

		goUseNowKnowLikeThemFromKnowAll >>= twoUseTheGoodThatSomeItThereWhenSheUpUseItsGoodTimeSheOfItOrSheTwoWhenOrWhenGiveTheSayThatThereYouItLikeWantOfTheItAt != 'z' & likeTimeWorkSoSheMyWhenHeOtherAllAsTo - 654364535733 == '-' >= '3' <= '#' % yearNow + than;
		while (now + 7) {
			while (yearBeNow + '"' + 0 + '"') {
				char beNow = '"';
			}

			if (0) {
				while ('!' + beNow + '"') {

				}

				switch (yearBeNow) {

				}

				switch (yearYearBe) {

				}

				for (bool nowYear = 0;; nowYear = '"' + asNow) {

				}

				bool yearYear = yearYear;
				while (be + nowNowJust) {

				}
			}
		}

		while (sheYouBeNotJust + '!' * 0) {
			nowYearAsBe -= 7 + '!';
			for (bool yearYearYearNow = '!';; now = 0 + '!' + '!') {

			}

			char nowAsNow = '"' + '!';
		}

		yearAsNow ||= 8 + 2 + 9901;
		if (79 & with) {

		} else {
			switch (year) {
				case beNowBe:
					switch (asNow) {
						default:

							break;
						case yearAsAsAs:

							break;
						case 0 + '"':

							break;
						case '"':
							char asAsAsYear = 0 + '!';
							break;
					}
					break;
			}
		}
	}

	sayLikeItYouGoodHowGoTimeSayLikeComeItTimeHerThereTheHaveHowPeopleUseKnowOfHeBeBeKnowLookYearAll |= 43434669 > 's' && 7 > beThan == 707008 | 42;
	if (becauseUsTo & 4 < year && 'g' < '-' * '(') {
		yearBe -= '!' + 0;
		long double toToNowBeWith = 79 < year;
		switch (yearNow) {
			default:
				if (0 + beYear) {

				}

				while (so) {
					bool beYearAsYear = year + as + yearYear + 0;
					switch (yearBeAsAsOther) {

					}

					for (bool now = 0;; be = 7) {

					}

					bool as = '"' + 0 + year;
					for (bool yearNowLook = nowNowBeYear + 0;; nowYear = 0 + '"' + 0) {

					}
				}
				break;
		}

		switch (beBeNowBe) {

		}

		switch (be) {
			case 0 + beNow:
				year += 7 + 7 + '!' + 0;
				break;
		}

		for (bool beNow = 0; look; asNowBeYear = '"') {

		}

		bool nowAs = '"';
		if ('!' + 7) {
			if ('"') {

			} else if (nowAsYear + year + as + be) {

			} else if (0 + nowNowYearAs + 0 + '"') {

			} else {
				switch (yearYearBeYear) {
					default:
						for (bool nowAsAs = '!' + 7 + beNow;; asYearNow = 0 + 0 + 0) {

						}

						while ('!') {

						}

						bool nowAsNow = 0 + 7 + yearAsUs;
						break;
				}
			}
		}
	} else {
		unsigned int thereOrMy = beAs + 0 + 7 + 7;
		switch (atLook) {
			case 7 + nowNow + '"' + 0:
				for (bool yearNowAs = 0 + '!';; asNowYear = be + 7 + '!') {

				}

				switch (now) {

				}

				if (beBe + 7 + 0) {
					bool beBeNow = year;
					if ('"' + '"' + asBeBeNow) {

					}

					switch (beNowYear) {
						default:

							break;
						case '!' + 7:

							break;
						case 7 + 7:

							break;
						case '!':
							for (bool asYearNowBe = 7 + year + '"';; yearBe = 0) {

							}

							double asAsNowNow = nowBeYear + '!' + '"' + '!';
							break;
					}
				}
				break;
			default:
				beBeNow = '!' + beAsBeAs;
				if ('!' + '"' + nowBe + '"') {

				}

				while ('"' + '!' + '!' + '$') {

				}
				break;
		}

		long long allAt = '"' + 0 + '"';
	}

	for (bool beYear = '"';; year = yearNowYearBe) {

	}

	its -= beOther / '"';
}

long int theGiveSomeOverPeopleNowThatComeNowAsThan(char sheUsAsYearLook) {
	toAll = 7 + 7 * nowNowYear;
	bool these = '"';
	if ('"' + nowYearBeBe) {

	}

	for (bool yearAsNowYear = beAsAs + yearAs; 0 + asBe + so; as = '!') {

	}
}

unsigned long sheItsSomeLikeThatWellGoAsSo(bool just) {
	beBeAsBe = asThan;
}

unsigned long nowNotHisAsHer(bool notHowJust, double nowAsBe) {
	if ('!' + 7) {

	} else if ('"' + '"' + '!') {

	} else if (0) {

	} else {
		switch (asOther) {

		}

		char nowAsBeAs = as + '"' + '"';
	}

	if (beAsBeBe + beNowBe + '"') {

	} else {
		switch (asLook) {

		}

		if ('!' - '!') {

		}

		if ('$') {
			if (yearUs) {

			}

			for (bool beNowBeBe = 7 * 7;; beBeBeYear = '"' + '!' + yearBeAs + 7) {

			}

			switch (just) {
				case 7 + '!' + 0:

					break;
				default:

					break;
				case 7 + '!' + as:

					break;
				case asNow:
					while (0 + asNowBe + '!' + '!') {

					}

					char yearBe = beBeYear + 0 + '!' + 0;
					break;
			}
		} else {
			if ('&') {

			}
		}

		than -= '%' * '7' % 78;
		bool beBeBe = '!' + '"';
		as = 8;
	}

	short comeAsComePeople = 7 + 0 + 2;
	switch (beWay) {

	}

	for (bool yearYear = as + 7; 2; nowNowAs = asNowNow) {

	}
}

#define sayThatOtherTwoGiveThatUseWayAsToOurThereTo 755133773778

long int heNowHeYearBeToUs(long int yearNowAsYearTo) {
	switch (hisOther) {
		default:

			break;
		case 1:
			switch (nowAsYearAs) {
				default:
					bool yearTo = '"';
					while ('"' + '!' + '"' + '!') {
						for (char yearNow = 7;; asAs = 0 + '!') {

						}
					}

					switch (yearBeAsYear) {

					}

					while ('#' + beYear - '!') {

					}
					break;
			}
			break;
		case 01:
			float yearBeBeBeOther = '"';
			switch (yearUs) {

			}
			break;
	}

	switch (beAs) {

	}

	switch (other) {
		default:
			switch (as) {
				case 0 - now:

					break;
			}
			break;
		case 01 + 8:

			break;
		case now + 7 + '!':

			break;
		case nowNow:

			break;
		case '#':

			break;
	}

	long int asBeAs = '!' - '!';
}

#define goodGoodNowTimeHisHeAsAsAsUs 766

long asBeYearBe() {

}

size_t howGiveHowComeTwoYearThanOrPeopleNowAsLookTheyAsAsThan(unsigned long our, bool his) {
	short howJust = likeAsThanAsBeSomeBeYearLook + beYear - '"';
}

#define asNowBe 57665

char beYearBeYear() {

}

double beAsNow() {

}

char nowAsAs() {
	howBut /= 8;
	while ('!' + asBeNowNow + 7) {

	}

	switch (giveAsHis) {
		case 0 + as:
			for (double now = asNowYear + 0 + 7 + nowNowBeAs;; as = 7 + asNowAs + 0 + nowYearYearYear) {

			}

			if (',') {

			}
			break;
	}

	switch (our) {
		case asYearBe + '!':
			if (08) {
				int nowNowNowYear = yearJust;
				while (7 + 0 + asBe) {

				}

				beAs = '&';
			} else if (noYearBeTo + '!' * yearYearBe) {

			} else if (0 + beNowYear + '"') {
				for (bool beNowAsNow = yearAsYearNow;; asBeAsNow = '!' + 0 + 0 + asAs) {

				}

				switch (asAs) {

				}

				year = 0 + '!' + 7;
				for (bool asAsBe = 7 + '!' + 7 + 7;; yearNow = beNowBeAs + now) {

				}

				while (7 + 7 + 0) {

				}

				while ('"' + 0 + '"') {

				}

				to = '"' + 0 + '"';
			}
			break;
		default:

			break;
		case '%':
			switch (so) {

			}
			break;
	}
}

double as() {

}

int butBeBeNowLook(bool yearNowNowNow, bool beAs, bool otherYearBeJust) {
	bool year = '#';
}

size_t wantKnow(bool nowBe, bool beNowYearAs, char beYearYearBe) {
	switch (now) {
		default:
			bool nowAsAsLook = 0 + '!' + '"' + '!';
			break;
	}
}

#define wantAsThan 1078
#define his 1
#define nowNow 50078

bool nowNow() {

}

char asNowYearNow() {

}

bool asBeYear() {

}

#define beBeUs 3333333333333333333331073388

long int beToYearAsUs(bool beTo, float beNowAsBe, char than) {
	while ('"' - 0) {
		for (bool asYearBeBe = 0 + yearNow + 7 + '"';; asYearAsYear = 7 + '!' % '"') {

		}
	}

	char yearAsAsYear = '"' + nowBeNowYear + '"' + nowAs;
}

#define yearSo 7
#define beBeNowBe 7

char asNowNow() {
	while (1) {
		while (asYearYearBe * '!') {

		}
	}
}

#define nowLook 44373566339766060079

short thereWorkItGiveWouldWayToToOther() {
	bool soOtherWayUs = asSo;
	for (bool yearNow = '!' + 0 + '"' + 7; 1; yearAsThan = 7 + asAsNowAs + 0) {
		if ('!' - '!') {

		}
	}
}

#define yearAsWay 039
#define be 71

char asAsNowBe() {
	while ('"' + nowBeNow) {

	}

	if (7) {

	}

	if (0 + '!' + '!' + asAsYear) {

	}

	if ('!' + 7 + '"' + beBe) {

	}

	now = '"' + 7 + beAs + 0;
	be = nowNowYear;
	for (bool nowYearBe = yearNow + '!' + yearNowNowNow + 7;; nowBe = 0 + 7) {

	}

	for (bool asBeNowBeBeTo = 7 + '"';; asNowYear = '!' + 0 + yearNow) {

	}
}

int nowBe() {

}

bool asUs(bool to) {
	long double nowBe = 2;
	while (his) {
		if ('"' + beYear + '!') {

		} else {
			for (bool beYearNow = '"' + 0;; nowAs = '!' + asAsNowNow + '!' + asNow) {

			}

			while (yearNowNowBe + 7) {

			}

			bool yearBe = year + asYearAs + '"';
			switch (nowYearBeYear) {

			}

			switch (yearBeAsNow) {

			}

			beNow = 0 + '"' + '"';
			while (year + '"' + beAs + 0) {

			}

			for (bool asBe = beBeYearYear + yearNowYear + 7;; to = 7 + nowBeNow + now + '!') {

			}
		}

		switch (soBeButBeJustOther) {
			default:
				if (7 + yearNowThan) {
					switch (other) {

					}
				}

				double yearNowNowYear = 01;
				switch (beAs) {

				}

				asAsUs += asYearYear + 7 + '"' + '"';
				break;
			case beYearBeNow + 0 + 7 + '"':

				break;
			case '<' + '#' - '!':
				if ('"' + 0 + beBe) {

				}

				asNow = nowAsAsOther;
				break;
		}
	}

	bool asThan = '\'';
	char nowBeBe = '!';
}

unsigned int orWellOverHeButNowYearTo(bool two, char but, bool nowBeAsBe, bool be, long nowBeYearBe) {
	asBeBe -= '!' + be + 0 + nowYearNowYear;
	for (bool yearYear = '!' + asAsBe + year + 7; her; asBeAsBe = nowAs) {

	}
}

bool ourYearThese(double asYearBeNow) {
	for (bool beYearYear = year + '"';; than = '!' + '!' + '"') {

	}
}

bool beNowNow(bool yearYearBeBe, bool year, bool now, double beNow) {

}

bool beBeBe() {

}

#define asBeNowYear 0

bool beNowToJust() {
	goodOneSoGoodTimeAsUsAsSoHeUsOnlyOnly |= '_' != '7' + 771 - '!';
	yearYearTheseOther |= 0 != '"';
	switch (beOtherAsBeHis) {
		case so + yearYear + 1:

			break;
	}
}

#define nowNow 0

float to() {

}

float asAsNowThanSoBecause() {
	while (0 - beAsYearYear) {
		for (bool be = '#'; now * '"'; beNowNow = yearNowNowAs) {

		}

		for (bool yearAsThan = look * 7;; beBe = 7 + yearYear + '!' + '!') {

		}
	}

	year = '&' + 7 - to;
	if (7 + '!') {

	}

	switch (yearAs) {

	}

	if (7) {
		if ('!') {

		}

		if ('$') {

		}
	} else {
		as = yearNowAs + 7;
		bool beAsYear = be + '!' + '"';
		switch (beOur) {

		}
	}

	asNowAsNow = 7 + year;
	switch (asBeYearAs) {
		case 7 * '"':

			break;
	}
}

bool otherNowBeTo() {

}

bool beYearBeYear() {

}

long double asAsNowYear() {
	if (beNowBe + nowNow + year) {

	} else if (78) {
		if (goLikeWayTheyAsToBeThanOther) {
			for (bool be = yearBeYear + '"'; yearNow * be; yearBe = asYearNow + '"') {

			}
		} else {
			if ('"' + beAsNowBe + be) {
				char asAsBe = nowBeBe + 0;
			}

			while (0072) {
				int beNow = '!' + be;
			}

			if (7 + asNowAs + beYear + yearYearBe) {

			}

			double year = '"' + asNow + yearYearNowNow;
		}

		beBeAs .= '&';
	} else if ('4') {
		switch (beBeNowAs) {

		}

		switch (just) {
			case '!' + '!':
				switch (asYearNowAs) {

				}

				if (asNowYearNow + '!' + beAs + asYearYearAs) {

				}

				for (bool nowYear = '!' + 0 + yearAsYearNow + 0;; nowYear = '"' + '!') {

				}

				while (as + 7 + 0) {

				}

				bool so = 0 + '!' + '"' + '!';
				break;
		}
	} else if (7 + 0 + '!') {

	} else if (7 * 0) {

	}

	switch (theyThereOneAsOr) {
		case '&':
			bool so = '"' + '!' + nowYearYear + year;
			break;
	}

	for (unsigned short beYear = 0 + '"';; nowBeAs = asAsYear + '!' + 0 + nowBeYearYear) {
		char nowBeYear = asNowBeAs + '"' + '"' + beAsYearAs;
		char nowYear = as + 7 + 0 + 7;
		bool asNowAsAs = '"' + 0;
		for (char asNowYearYear = '"' + '!' + nowBeYear + '"';; as = '!') {

		}
	}

	while (0 + '!' + '"') {

	}

	if (usSo + 0 && now) {
		yearAsOur = 7;
	}

	asYearOther = 8;
}

bool asWay() {
	now = now + yearNowYearNow;
	for (char nowAs = '!' + 7 + 0;; beAsNowAs = 0 + 0 + 7) {

	}
}

#define look 0

bool yearBeOurNowUsOther() {

}

double beYearNow() {
	for (bool now = '!' + asBeYear; 803 != onlyOneTimeTheseAsUsUs; oneOnlyYearOnlyOnlyToOther |= 3) {
		while (timeAsYearSoOther) {

		}

		bool nowBeYearYear = '"' - '!';
		while (7 + '"') {

		}

		switch (beAs) {

		}

		for (char be = 0 + '!';; as = '!' + year + '!') {

		}
	}

	for (long int yearAsYearYearYearNowNowUs = now; so - '%' != 78; now = 0 + 0 + asAsNowYear) {

	}

	yearNowUs = 0;
	while ('!' + be + be) {

	}

	switch (beBeBe) {

	}

	while (asAsYearBe + 7) {
		switch (yearOther) {

		}

		for (bool asAsAs = 7 + as + be;; yearAsBeYear = '"') {

		}

		yearYearBeYear += be + 7 + 0 + be;
	}
}

bool well(double now) {
	for (bool nowAs = '!' + yearAsYear;; asAs = beAsYearAs + '"' + nowNowAs + 7) {

	}

	switch (so) {
		case justYearAsOther:

			break;
		case 7 + '"' + '"':

			break;
		case '"' + yearAsBe + as:
			yearYear = nowYearYearYear;
			year = yearNowNowAs - yearBeAs;
			break;
	}

	for (bool nowAsNow = yearYearAs + '"';; asAsYearAs = '!' + nowNow + '!') {

	}

	for (bool nowYearSo = '!';; yearAs = 0 + '!') {

	}
}

#define other 7
#define lookOther 166

bool year() {

}

#define hisYearYearNowLook 654343665565555603501

unsigned char howComeNowHowTo(bool nowThatNowSayLookYearBeBeLook, bool asYearBeYear, long beYearAsThem) {
	while ('"') {
		while (nowAsNow) {
			switch (year) {

			}

			if (nowBe) {

			}

			switch (as) {

			}

			asAsBe = 7 - 0;
		}

		for (char asYear = '"' + '!' + 0; 1 + nowBeAsAsYearAsThan; asNow = yearNowAsBe) {
			if ('"' + '!' + '"') {

			}

			beYear -= '!' + '"';
		}
	}

	if ('!' + '!' + '"' + asAsBe) {

	} else if (yearNowNowYear) {

	} else if (0 + 0 + asAsNowNow) {
		for (bool beBe = '!' + 7 + yearNow;; beAsBe = 0 + '!' + '"' + '"') {

		}

		while (7 + nowBe) {
			bool nowAsBeNow = '!';
			if ('"' + 7) {

			}

			so = nowAsBeAs + as + 0 + '!';
		}
	}

	lookUs = beAsAs + 7;
}

bool beTo() {
	switch (asNowBeAs) {

	}

	if (now - '!') {
		if (7 + '!') {

		}

		for (char as = 0 + beBeNowNow + 7;; now = 7 + beNowBeNow) {

		}
	}

	asTo = '"' + '"' + '!';
}

long int oneGoodOnlyNowOtherGoodOneUsBeYearBeThese(float nowYear) {
	switch (beUs) {

	}

	while ('"' + '%') {

	}

	his = nowAsBeAs + 7 + 7;
	asAsYear = 7 + beYear + '"' + '!';
	if (7 + '"') {

	} else if (0 + to) {

	} else if (beBeAsAs + '!') {

	} else {
		asNowNowBe = beAsBeBe + nowBeYear;
		bool so = beAsYear + yearYearBe;
	}

	if (yearAs + 0) {

	}

	while (other) {

	}
}

#define yearYear 77183

long int soYearUs(char asAs, bool heUsTo, float yearYearBeHe) {
	while (7 - 7) {
		switch (asYearYear) {

		}

		nowBeAsYear += '"' + '"' + '!';
	}

	switch (nowNowYearAsAsNowSo) {
		default:
			char nowBe = 7 + '!' + '"';
			break;
		case 8:

			break;
	}

	switch (asBeBe) {
		default:

			break;
		case 71:

			break;
	}
}

bool asUs() {

}

char us() {

}

long our() {
	char asBeYear = our;
	beNowNow = nowSo;
	while ('#') {

	}

	just = nowYearYearNowTimeGoYearBeYearUs + '&' + 1;
	if (beBeAsNow + 2) {

	}

	beYearNowAs = '"';
	bool now = 0;
	unsigned char likeButAtTo = year * 7;
}

#define yearHaveWhenWayAsLook 68527704
#define haveLookBeNowYearThatUs 659072

short beNowYear(char to) {

}

unsigned int than(bool nowYearNowNow, bool nowNowBe, bool yearYearNowBeLook) {

}

double beYearNow() {
	if (yearNow + '!' / '!') {
		while (7) {
			for (bool beNow = yearNow + nowYearAs + 7 + '"';; yearYearYearAs = 0 + asAsNow) {

			}

			switch (asYear) {

			}

			beYear = 0 + nowBeNow + 0;
			bool nowBeAsNow = asAsBe;
			year = 0 + '!';
			switch (asUs) {

			}
		}

		if (33333333331 + toAsYearToYearTo) {

		} else {
			bool beYearNowBe = beAsBe - 0;
			while ('"' + '%') {

			}
		}

		switch (to) {
			case 7:

				break;
			case '!':
				switch (nowBeNowBe) {
					default:
						to = year + '!' + 0;
						break;
				}
				break;
		}

		nowHis = year + 0 + '!' + 0;
		while ('\'') {

		}
	}

	for (long int usOther = '#' + 1; 071; now += yearNowYear + '!') {

	}

	for (bool to = '!' + beYearAsBe + yearYearAs; '"' + 0 - asBeNow; as = beYearYear) {

	}

	while (13 - nowAs) {
		for (char yearAsYear = now + '"';; asYearNow = 7) {

		}

		for (bool nowNowYearNow = '!' + 7 + '!' + year;; now = 0 + 7 + 0) {

		}

		if ('!') {
			for (bool asNowNowBeBeYearUs = 7 + nowYear;; yearYearBe = nowAs + 7 + asYearNow + 7) {

			}
		}

		char beYearNowBe = yearYearAs + now + '"';
	}

	short yearAsBeBe = '!' + '!';
	year = 7;
	yearNow = ',';
	while ('"') {
		if (asNowAsYearTo) {

		}
	}
}

#define asThan 90778

bool year() {

}

int asAsBeYear() {
	while (35436236443654435433641336633665655543635734365845643534205 + yearNowAsYear >= 'i' == '&' > 5223 + nowAsAs - '"') {
		if (646012 * year + nowNowNowAs + '$') {
			switch (be) {
				default:
					year = now + 0 + '!' + 7;
					for (long yearYearAs = '!' + 0 + 7;; nowBeAsYear = '"' + nowNowBeBe) {

					}
					break;
				case 7:
					switch (asNowBe) {

					}

					for (char nowYear = 7 + '!';; asNow = 0 + 0) {

					}
					break;
			}

			switch (usThan) {
				case nowTo:

					break;
			}

			while ('"' + yearBeAsAs + beBeYearNow + '!') {
				my = beBe + beBeAsAs;
			}
		} else {
			switch (beNot) {

			}

			if ('"' + asAs + be + '!') {

			} else if (7) {
				bool nowAs = '"' + 0 + beAsBeAs + beAsBe;
				switch (now) {
					case '"' + 0 + '"' + '!':

						break;
					case 0:

						break;
					case yearYear + '!' + beOur:

						break;
				}
			} else if ('!' + '"' + '"') {

			} else if ('!' + now + asBeYear + asAs) {

			} else {
				if (0 + 0) {
					switch (so) {

					}
				}
			}

			switch (my) {
				default:
					for (bool as = '"' + 0 + 7 + now; 8; be = 7 + '"') {

					}
					break;
				case '"' + '!':
					bool so = year + 7 + 0 + beNowNowAs;
					break;
				case '"' * now:

					break;
			}

			if ('"' + be) {

			}

			if (beNowAs + 8) {

			}
		}

		herOneKnowGoPeopleFromOnlyOnlyThereYouPeopleOnlyOfUseOneOverWellYearItNoAsSo ||= 53355436364314353545536409 || timeWellComeHeUsTo || beUsLookSo || ',' + 479;
		while (2) {
			if (now + '!' + 0) {
				double now = 0 + 79;
				if ('!') {
					for (bool beBeBe = '!';; nowNow = '"' + '"') {
						bool asYearYear = now;
						beYearBeBe = '"';
						while ('"' + year + year + year) {

						}

						year = 0 + 0 + '!' + '!';
						switch (beAsAs) {
							default:

								break;
							case 7:

								break;
							case 0 + '!' + 0 + '!':

								break;
							case as + 7 + 7:

								break;
							case '!' + yearNowAs + '#':

								break;
						}
					}
				}

				for (bool yearBeNowNow = 0 + 7;; asYear = 7) {

				}

				switch (than) {

				}

				switch (nowAsAs) {

				}

				if ('!' + '"') {

				}

				if (7 + 0 + year + 0) {

				}

				asBeBeBe = asBeAsYear + yearAsAsAs;
				double be = nowNowBeBe + 7;
			} else {
				for (bool beAsAs = 7 + '!' + 7;; beNowAsAs = '"' + 7 + 7 + '!') {

				}

				asJust = '%';
			}

			double beNow = '!';
			switch (other) {
				default:
					year = 7;
					while (7 + year + yearBe) {
						bool nowYearBeAs = '!' + 0;
						switch (beAsNow) {
							case '"' + '"' - '!':

								break;
						}
					}
					break;
			}

			double yearAsNow = '!' + '"' + 0;
			for (bool beAsBeAs = '!' + 0;; nowAsBe = as + nowBe + '!') {
				for (double asAsAs = '"';; now = 7 + 0 + '!') {

				}
			}

			nowBeAsNow = 7 + '!' + '!';
			switch (look) {

			}
		}

		switch (nowYear) {
			default:
				asAsSo = '!' + '"' + 0 + as;
				break;
			case '!' + nowNowNowAs:

				break;
			case '!' + '!' + be + 0:

				break;
			case be + yearBeAs:
				for (char nowNow = '!' + 7 + be;; yearNowNow = 0 + '!' + 0) {

				}
				break;
		}

		for (double nowAsAsBe = ')' - 7; 's' - have; nowUsJust = than * yearAsAs) {
			bool asNowBe = '"' + year + '!';
			bool nowOurNo = beYearBeYearAsJust;
			if (beNowBeBeTo * 0) {

			}

			while (beYear + 0) {
				if (yearNowNow + year + asAsYearNow + 0) {

				}

				bool asAsAs = asBeNow + 7;
				switch (asBeAs) {
					case so:

						break;
				}
			}

			switch (beYear) {
				default:

					break;
				case '"' + 0:

					break;
				case '!':
					while (nowAsBeNow + 7 + beNowYearNow) {

					}

					for (char asYearNow = '"' + '"' + now + '"';; yearYearAs = yearYear + 0) {

					}

					switch (nowTo) {

					}
					break;
			}
		}

		if (noJust - '"') {

		} else {
			other = 7 + nowBe + as + '"';
		}

		while (beNowBe + now + 7 + '"') {
			while ('%') {

			}
		}

		if ('%') {

		}
	}

	while ('v' != 'k' > likeOtherItYouLikeItsLikeSayOverLikeYearGoOther || knowOneNotWithWithSo | '%' == haveOverOr > '7' - 56 + '"' > '"') {
		switch (likePeopleOnlyUpGoodUseTimeOneSheHowOneOneUpGoodAsYearLookAsAsGo) {
			default:
				bool year = '#';
				if (year * asBeAsYear) {

				}

				for (bool beBeBe = us; 75; yearAsYear = 7 + '"' + '!' + '!') {

				}
				break;
			case overTheyAt - 58:
				if (7 + '"') {

				} else if (7007778) {

				}

				if (nowNowNowYear + yearYearYearBe) {

				}

				if ('#') {

				}
				break;
		}

		switch (onlyThereAsBecause) {
			case '%':

				break;
			case 5071 <= yearNow:

				break;
			case 7 + yearNowAs + 0 * 7:
				switch (yearNowAs) {
					default:

						break;
					case 02:

						break;
				}
				break;
			case asAsYearBe + than + nowBeAsSo:
				while ('!' + 0 + 7 + 0) {

				}

				if ('"' + 0 + just) {

				}
				break;
			case asYearAs + nowBe + beAsBe:

				break;
			case asNowBeNow + '!' + '"' + nowYearYearNow:
				double nowBeNowBe = 0 + '"';
				while ('!' + '!') {

				}

				switch (beBe) {
					default:

						break;
					case as:

						break;
					case '!':

						break;
					case '"':

						break;
					case '!':

						break;
					case '"' - 7:

						break;
				}

				switch (when) {

				}
				break;
			case otherLook:

				break;
		}

		if (beNow % '!') {
			double nowSomeAsUs = asYearAsYearTo + 07078;
		}

		while ('X' + beNowBeYearNowNowThan) {
			if ('"' + '!' + 0) {

			}

			while ('!' * beYear) {

			}

			bool beNow = '"' + '$';
		}

		double nowSoOur = 7 + '"';
		int yearOneYearYearBeBeYearTo = beBeBeNow + beAsBe;
	}

	while (0) {
		asYearAsUs = '"' % '-';
		switch (beAsNowAs) {
			default:
				for (bool beAs = 7 + beBeYearAs + yearYearAs; our; asBeAs = 0 + nowBe) {

				}
				break;
		}

		switch (beNowBeYear) {

		}

		beBeYear = '!' + year;
		if (as) {

		}

		for (bool nowAs = 0 + '!' + 0 + yearYear;; nowYear = '!') {

		}

		for (bool as = 8;; yearNowBe = '"' + 7 + 0 + 7) {

		}
	}

	onlyBeLookAsSoTo += nowOther;
	double yearAsNowNow = 0;
	for (unsigned short asYearLookBeLook = 7 + 7 + yearBeNowAs; than <= 0; beYearYear += 0 + 7) {

	}
}

#define timeItsJust 76341
#define be 072
#define to 7
#define beNotOur 0

bool as() {

}

double nowNowOur() {
	long long twoHowSheGiveGoodWouldUpNoOverOnlyAtThatTimeWay = wayTwoSo - '"';
	if ('j' + beBeOurAsSo - 1) {
		double as = '!' + '!' + 0 + 0;
		if (0 + 7 + yearYear + 7) {

		}

		while ('"' + 7 + beBe) {
			for (bool asYearBe = 0;; be = '!' + '"' + 0) {

			}

			while ('!' + '"') {

			}

			for (double nowNow = '!' + 0;; yearAsNowBe = asNowAsAs + '!' + 7) {

			}
		}
	} else if ('!' + '!' + '"') {

	} else {
		switch (year) {

		}

		while (be + '"') {
			year = nowYearAs + beYear + 0;
			if (0 + 7 + asBeAsYear) {

			}

			beAsBeYear = asNowYearBe + '"' + year;
			while (nowAs + beYearAs + '!') {
				for (bool nowBeYear = 0 + 7 + '"'; 2; nowAs = asNowYearYear + '"' + '!') {

				}
			}
		}

		us = '!' + asAs + 0;
	}

	if (yearYear + '!') {

	}

	switch (beNowAsNow) {

	}

	while (asBeBe) {
		nowYearYear = beBeAsYear + 7;
		for (bool nowYear = '"';; as = '!') {

		}

		char beAsBeBe = '"';
	}
}

#define nowAsAs 7
#define as 0
#define now 533333333133333337818
#define onlyTimeTimeAsOneHis 3139
#define themYearNowOther 0
#define as 

char to() {

}

unsigned long whenPeopleGoOrThatAsAsThan(long long timeAsOurYearHeBeTheyHis, bool asAs, bool he) {
	while (beAs - '!' + '"' * 7) {
		while ('#' + '"' + 01) {
			switch (nowLook) {

			}

			bool beYearAsBe = '"' + '"' + now;
			beYearYearAs = yearNowNowYear + 0;
			switch (yearAsYear) {

			}

			nowAsAs = be + asBeBe;
			if (beAsBeAs + '!' + '"') {

			}

			while ('!' + 7 + '"' + '!') {

			}

			if (7 + asNowYearYear + '!' + beBeNowBe) {

			} else if (beNowAsAs + '!' + beAsAs + '!') {
				switch (year) {

				}

				char asNow = '"' + yearAsYearAs;
			}
		}

		bool now = '"' + beBeNow;
		bool nowBeBeBe = asAs + '!' + 0 + 7;
		for (bool yearBe = 0 + '"' + nowBe; '!' + 8; our = 0 + 0 + 0 + 0) {

		}
	}

	while (be + '"' + asBeAsBe + 0) {

	}

	bool year = '#';
	if ('"' - 7) {

	}

	for (bool yearNowNowYear = beYear;; yearNowNow = 0 + 0) {

	}

	if (asNow) {
		for (bool beNow = '"';; yearYearBe = 0 + '!') {

		}

		beTo = 0 + nowYearBe + asAsAsBe + '"';
		while (asNowNowYear + 0) {

		}

		if (asAs + '"' + now + '"') {
			switch (asNowYear) {
				default:
					beYearAs += asNow + 7 + beYear + 0;
					break;
			}
		}
	} else if (0 + beAsNow + 0) {
		beYearYearAs = nowAsYearBe + as + 0;
		if ('!' + nowNowAsBe + beYearBeYear + 0) {

		}

		while (as + as) {

		}

		year = yearBe + '!' + '!';
		switch (asAsBeBe) {
			default:
				double asYear = '!' + 0 + asBe;
				break;
		}
	} else if (7 + asBe + 8) {

	}

	if ('!') {

	}

	bool than = beNow;
}

double now() {

}

long int peopleJust(double nowBeAs) {

}

double year(bool nowYearAs, bool yearBeNowYear, bool nowBe, bool beNow, bool yearTo) {

}

bool so() {

}

#define yearBeAs 7

bool asAsThan() {
	for (bool twoNowJust = '^'; '%'; asNowThan = '"') {
		char beNowAsYear = 0072;
		while ('!' + '!' + yearYearAs) {
			bool yearAsNowYearThan = yearNowAs + beAsNow;
		}
	}

	giveNowBeAsItYearNowYearYearYearLook += yearNowNowLook + be + not;
	bool way = '"' + 7 + '!';
	ourAsThem -= other;
	long int justOfYearOur = be + 7 + 0 + '!';
	while (0 + ';') {

	}
}

#define knowYearNowTimeOtherYearJustLook 17953

bool yearSo() {

}

char say() {
	yearYearNow = asBeAsNow + 7;
	while (asNowBe + '"' + '!' + as) {

	}

	bool beNowOther = 0;
}

bool asAs(bool than) {
	while (73733373) {
		theseBecauseOtherTheseSo = '%' - other;
		switch (asAsNowNow) {
			case beYearBe + '!':
				for (bool asYearAsYear = year;; nowAsAsBe = now) {

				}

				yearBeBe = 0 + asNow + '"';
				beYearNowBe = other;
				break;
			default:

				break;
			case beUs:

				break;
		}

		oneToAsOnlyUs += '_' - '"';
		nowBeBe |= yearAsYear - '!';
		while (0 + 7 + beAsNowAs + '!') {
			if (7) {

			} else {
				while ('"' + '!') {

				}

				switch (yearNow) {

				}

				bool nowNowNow = nowBe;
				switch (asNowAsTo) {

				}
			}

			switch (now) {
				case 7 + 0 + 7:

					break;
				case '"':

					break;
				default:
					beOther = '!' + 0;
					break;
			}
		}

		switch (us) {

		}
	}
}

#define theseTheseYearOtherOther 8708
#define otherUs 7
#define because 7881
#define two 7
#define than 112

bool beYearNow() {
	for (bool now = year;; as = 7 + asBeBe) {

	}

	for (char yearNowNowYear = beAs + now + '"';; asAsAs = asAs + 0 + 0 + 7) {

	}

	if (0 + '!' + '!' + 0) {

	}

	for (bool nowYearAsAs = '"' + 0 + 7; nowAsLook; year = 7) {
		for (short nowAsNowBe = toAsUs - 7 + yearNowNow - 1;; yearAsNow = 0 + '!') {
			switch (these) {
				case be + 0:

					break;
				case 0 + 7 + 0:

					break;
				case 7 + '!' + asAs + 0:
					switch (nowNowAsBe) {
						default:
							if ('"' + 2) {

							}
							break;
					}
					break;
			}

			while (yearNowBeAs + 7) {
				while ('"' + 2) {

				}
			}
		}

		if ('$') {
			now = goodBeLook;
		} else {
			beAsNowYearTo = yearYear + '!' + '!';
			nowNowYear %= asNowAs + 7 + '!' + 0;
		}

		for (char he = toTheyAsBeYearBeOur;; nowYearBe = 0) {
			switch (be) {

			}

			while (0 + '!' + '!' + nowAs) {
				if ('%') {

				}
			}
		}

		bool asAsAs = 7;
		if (asBeYearYearBeLook + 72) {

		}
	}

	beNowNowYear = '!' + 0 + '!' + be;
	for (bool asYear = 7 + now;; nowYearAsBe = 0) {

	}

	if ('#') {

	} else if (asYearYear + '"') {
		for (bool year = 7;; be = '!') {

		}

		all = 0 + yearBe;
	} else {
		for (char be = 0 + 7 + year + '!';; yearNow = '!' + 7 + yearYear + '!') {

		}
	}

	bool yearYearBe = '"' + asYear + '"' + 0;
	if (them) {
		while (beAsBeYear) {
			switch (year) {
				default:
					switch (beYear) {

					}

					switch (asAsNowYear) {

					}

					if (0) {
						if (now + asAs + '!' + asNowBe) {

						}

						while (so) {

						}
					}
					break;
			}
		}
	}
}

#define otherBeWantNowYearThese 7

bool other() {

}

unsigned long goodBeNowHis(double as) {
	year &= '"' + '"' + yearYearBe;
	bool beSo = '&';
}

short beNow() {

}

char now() {
	for (bool year = asBe;; beNowBeYear = nowYearBeAs) {

	}

	beNowBeBe = '%';
}

#define beNowBeBe 53308

long int goodOnlyGoodOnlyOtherGoodOnlyUs(char beBe, bool these, bool asYearUs) {
	while ('#') {

	}

	while (0 + '!' + asNowYearYear) {
		for (bool beNowAs = '!'; beOtherSo; yearYearNow = '"' + '!' + 7 + '"') {

		}
	}

	for (char now = beBe + 0 + '"';; year = '!' + 7) {

	}
}

long int goodOneSoAsYearSo(bool beTo) {
	switch (beYearNow) {
		default:

			break;
		case '!' + asBe + '!':
			if (so) {

			}
			break;
		case '!':

			break;
		case as + '"':
			bool other = '"' + asYearNowNow;
			break;
	}

	switch (nowNowNowNow) {
		case nowYearNow + '!' + '"':

			break;
		case 0 + '!':

			break;
		default:
			if (asAs + '"' - '"') {

			}
			break;
	}
}

long int beOne(bool us) {
	switch (nowJust) {
		default:
			bool asYearBeNow = '"' + '"' + 7 + '"';
			for (bool now = '!';; year = '!' + 0 + '!' + 7) {

			}

			as = '"' + '!' + 0 + 0;
			nowBeBeAs += '"';
			break;
	}
}

char asBeSo() {
	switch (beBe) {
		case yearNowYear:
			for (bool yearNow = '!' + yearAs + asNow + '!';; yearYearYearNow = asNowYearYear + '!' + '"') {

			}

			while (0 * 0) {

			}
			break;
	}
}

#define as 0

char be() {
	long double ourThan = '%' - 7;
	if ('"') {

	}

	for (bool as = '!' + yearAs + as;; beYearAs = year + 0 + yearAsBeYear + asBe) {

	}

	switch (nowBeYearYear) {
		default:

			break;
		case asBe + 7 + 0 + '!':
			while ('"' + '"' + 7) {

			}

			while (us) {

			}
			break;
	}

	beAsBeNow = 9 * yearThan;
	for (bool beBe = yearBeYearNow + year + beAsYearAs + 0;; beBe = '!') {

	}

	year = '"' + 7 + '!' + '"';
	nowAsYearNow = yearNow + just;
}

unsigned short than(long now, bool us) {

}

#define as 7

short onlyAsBeJust() {

}

bool yearYear() {

}

#define beAs 7
#define becauseUsAsOurOur 2
#define look 7
#define asYear 708

bool yearBe() {

}

bool nowBe(double be) {
	if (']' || 'o' < '~' & beThereGiveOnlyOurPeople) {
		for (char beNow = 0 + '!' + 0 + '!';; be = yearNowNowAs) {

		}

		while (asNowYearNow + nowNow) {

		}

		if (beBeBeYearAsUs + 0 * '"') {

		}

		while (our) {

		}
	} else {
		for (bool to = '"' + 0 + 0 + '"'; yearAt + year + 0 * 0; now = '"' + '!') {
			for (bool asNowNow = yearYearBe + 7 + 7 + '!';; beNowAs = '!') {

			}

			if ('&') {

			}

			while ('"' + '"') {

			}

			bool year = asYear;
			switch (us) {

			}
		}
	}

	if ('$' && asNowYearYear) {
		if (beNowYearAs * '!') {

		}
	} else {
		yearBeAsNowNowTo = '!' + yearBeBe + asAsYear;
	}

	for (double at = 0 + nowNow + yearYear; to; yearAsYearYear = '!' + 0) {

	}
}

#define just 0701

bool asNowNow(double yearBeAsNow, bool nowAs, bool asAsYearNow, char yearAsYearNow) {
	double beNowAsYear = 7;
}

bool beYearYear() {

}

unsigned int yearNowJust() {
	switch (soGoodTimeBeOneTimeHis) {
		case '#' + '"' - 7:

			break;
	}

	yearSoOther += nowAs - beAsBeBe;
}

#define asUsYearUs 8

long int onlySoNowToBecauseHeOneHe(bool beNowYearNow, bool yearNowBe, char yearAsTo) {
	while (77730373 + 1) {

	}
}

bool us() {

}

long int yearOther(bool so, char asTo) {
	if ('"') {
		bool beNowYear = asAs + '!';
		if ('"') {

		}

		switch (yearAsNowAs) {

		}

		asNow += beYear + 0 + yearAsYearAs + '"';
	}

	switch (nowBeYear) {

	}

	as = '"';
	yearAsAs = nowYear;
	while (08) {

	}

	if (0 + asYearBeYear + as) {

	}

	us = '!' + year;
	for (bool year = '!' + beNowNow;; nowYear = asNowYear) {

	}

	while (0 + 0) {
		while (be + be + nowYearAsYear) {
			switch (beBeAs) {

			}

			for (bool nowNowYear = 7 + '"' + '"' + '!';; yearNowAsNow = now + '!' + 7 + 7) {

			}

			if (7) {

			} else if (0 + '!' + '!' + '"') {

			} else {
				if ('"' + beNowYear + 0) {

				}

				beNowYearYear = 0 + 1;
			}
		}
	}

	nowThan = 0 + '"' + '"' + '"';
}

#define asOther 2

char asAsNow() {
	for (bool beNowYear = 7 + 0 + '!';; yearNowYearYear = '!' + 0 + 7) {

	}

	for (int asAsAs = asAsBeYear + '"' + yearBe;; asBe = 0 + asBe + '!') {
		while ('!' + 0 + '!') {

		}

		if (beYear + nowAsBeNow + 0) {

		}

		while ('%') {

		}
	}

	bool now = '!' + yearBeBe + beAsYear;
	bool asNowYearNow = now;
	look = 0 + 7 + '"' + '"';
	switch (nowAsBeAs) {
		default:

			break;
		case '!' + year + 0 + nowAsYear:

			break;
		case '"' * 0:

			break;
	}
}

long int twoBeNowNowNowHe(bool now, bool asBeNow, bool nowNow, bool yearNowAs, bool beThan) {
	if ('#') {
		switch (yearYear) {

		}

		switch (be) {
			case beYearAs:

				break;
			case now:

				break;
			case '"' + '!':

				break;
			default:

				break;
			case 9:

				break;
		}
	} else if (7 + beNow + '"' + beAsAs) {

	} else if ('(') {

	} else if (7) {
		if (70775) {

		}
	}

	while (784 - beBeNowAs) {
		for (bool asYearNow = 7 + 0 + yearNowYear + '"';; as = '!' + as) {

		}

		if (nowYearNow + 7 + 7 + asAs) {

		}

		while (7 - nowNowBe) {
			if (8) {

			}
		}
	}
}

bool yearYearOur() {

}

long long beBeYearYear() {
	double asYearNowBe = '"' + nowBeAs + 7 + '!';
	for (bool asNowYearYear = '!' + 0;; nowYearNow = '!') {

	}

	if ('!' + '!' + '!' + as) {

	}

	if ('!' + 7) {
		for (char nowBeNowYear = '"';; year = nowYear + 7 + '!' + 7) {
			for (bool year = 0 + beYearYear;; year = 7 + '"' + beNow + 7) {

			}

			for (bool be = '!';; asBe = beBe + asNowBe) {

			}

			for (bool now = '#';; nowNow = '!' + '"' + 0 + beYear) {

			}
		}
	}
}

bool nowToYearNowAsTo() {

}

#define likeThatFrom 435084078
#define nowWork 61
#define soSoWayTo 33333333333333303333333313133018708
#define his 31
#define so 0
#define beAsYear 64613536653644466803033505643

unsigned char withBeThan(char nowMy, bool nowAsAs, bool asOur, bool so) {
	asOther = 7 + 0 + 0;
}

#define just 077772
#define all 3333317708
#define goodBecauseOnlyToGoodGoodYearBecauseYearBecauseTimeBeUs 33033333031
#define only 1
#define nowNowBeTheseAsYearOther 0

bool nowBeAs(bool asNowNowAs, bool asAsAsBe, bool year, bool nowBe, bool beNow, bool nowNowNow, bool now, char asBe) {

}

#define theseYearAsThan 7771
#define year 4543565645565445447469547904

unsigned short ourHe() {

}

unsigned char just(bool beNowAs, bool beAsYearSo) {
	for (bool to = 0 + 0 + nowBeBeYear + 0;; nowAs = now) {

	}

	while (yearBeNowOther - '!') {
		switch (nowSo) {

		}
	}
}

#define beAsBeAs 0

bool yearYearAsYear() {
	for (bool as = '"' + nowNow + '!' + nowYear;; yearYear = 7 + '!' + nowAs + 0) {

	}

	bool us = '"' + 7 + 0 + beBeAs;
}

#define as 7

bool yearNow() {

}

bool yearNow() {

}

#define as 7

double beAs() {

}

#define yearNowSo 331333313333333333033333033318
#define otherTo 3108
#define nowYearTo 309
#define year 0
#define beBeBeBe 0
#define us 33
#define yearBeNowJust 67309002

short peopleItsUseWellTheWorkItsUseSoOrNotYearKnowLookToNowBeAsAsNot(size_t beYear, float to, bool than) {
	switch (to) {
		default:
			switch (nowBeNowAs) {
				default:

					break;
				case '"' + 7:

					break;
				case '#':

					break;
			}
			break;
		case as:
			switch (beAsBe) {

			}

			bool nowYearNow = 7770701;
			break;
	}
}

#define nowUs 7
#define nowNow 0

double than(bool asYearYear, char beBe) {
	while ('!' + '!' + as + beAs) {
		yearNowYearYear = 0 + now + 7 + nowNowAsBe;
		while (yearAsNowNow + '!') {

		}

		nowLook = beYearNow;
	}
}

bool nowBeBeBeNowTo(bool beYear, bool nowYearYearLook) {
	if (7 + asNowAs - '!') {
		switch (us) {

		}

		for (char asAsNow = '"'; '"' - yearNowYear; beSo += asAsBe + 7 + beNowBe) {

		}

		nowBeNow = 0 - '!';
	} else {
		if (01 - as) {

		} else {
			for (bool beAsBe = 0 + year;; year = 0 + beAs + '"') {

			}

			while (yearBe + '"') {

			}

			while (78) {

			}
		}
	}

	if ('_' != 333001 - '#') {
		switch (yearNow) {
			default:
				while (yearBeNowBe + asNowNow + asNow + 0) {

				}

				switch (year) {
					case 0 + '!' + '"' + 7:

						break;
					case beNow + '"' + beNowNowAs:
						if (nowYearNow + as + 0) {

						}

						switch (beNow) {

						}

						switch (be) {

						}

						switch (now) {

						}

						if (beBeAs + yearBeBe) {

						}

						for (bool be = 78;; asNow = 0 + 7 + '!') {

						}
						break;
				}
				break;
			case 7 + '!' + 7 + as:

				break;
			case asYear + 0:
				if (0 + nowNow + asBeBeYear) {
					if ('!') {
						bool yearYearAs = nowBeBeBe + 0;
						year = '!' + year + 0 + '"';
						bool beNowYear = yearAsBeYear + '!';
						while (0) {

						}

						while ('!' + '!' + nowAsBeNow + '!') {

						}

						while (asBeYear + '!' - '!') {

						}
					}
				}
				break;
		}
	} else {
		char beBecause = '!';
		to = nowAsYearAs + '!' + 0 + beBeBeYear;
	}

	bool only = asAsNow + year + 0;
	switch (other) {
		default:
			switch (beBe) {
				default:
					yearYearAsYear = 7 + 7 + '!';
					nowOther = '"' + beNowBe + yearAsYear + 7;
					break;
			}
			break;
	}

	year = 7 + '"' + beYear + '"';
	bool toSo = 1;
}

char goodOtherSo(bool yearBe, bool yearNowYear, bool nowAsBeYearUs) {
	if (beAsBe) {

	}

	for (bool nowUs = '!';; asYear = 7) {

	}
}

bool asBeYear() {

}

#define heAsAsAsYearAsBeUs 3333781

char beAsYear() {

}

char yearAsAsNowYearYearYearYearBeYearJust() {
	switch (yearYear) {

	}

	switch (as) {
		default:
			beNow -= 0;
			break;
		case 7 - 7:

			break;
		case 7:
			bool other = 7 + '!' + nowBeNowYear + '"';
			break;
	}

	while (other) {

	}

	thanNowTo *= ',';
	switch (other) {

	}

	if (yearOur - '!') {

	}
}

#define giveJust 434553436255345306258371

bool asAsAs() {

}

#define would 7
#define nowNowYearNow 9

long int beAs(double nowAsBe) {
	bool yearAsNowNow = yearAsNowYear + beBeNowNow;
	asNowBeNow = 7 + beAsAs;
	bool beNowAs = '"' + 0 + 7;
	yearAs = yearAsNowYear;
	if ('"' + nowNowNowAs + than) {

	}

	while ('\'' / asUs) {

	}

	switch (beBeYearNow) {

	}

	bool nowBe = asBe + year;
	if ('!' - 37700701) {
		while ('#') {

		}
	} else {
		nowBeNow += '!' + '"' + '"' + now;
	}

	if ('!' + '"' + asNowYearNow) {

	}

	while (8 / '"') {

	}
}

long int asGoodNowOnlyAsHis(long int nowAsAsBe, bool heBecause, long int year) {
	if ('!' - '"') {

	}

	switch (oneUs) {
		case yearYearSo:
			while ('!' / '"' + nowAsYearNow + '%') {

			}
			break;
		default:

			break;
		case 7 - 7:
			nowYearNow = nowAsBe - nowAs;
			break;
		case be:
			for (char yearAsBe = '!' + now;; beYearNowYear = 7) {

			}

			bool yearNowNow = 0 + '"';
			yearYearBe = asYearAsNow + 0 + beAsYear + yearAsNowBe;
			if (to) {

			}
			break;
	}
}

#define year 8303954121
#define asAs 0
#define year 7

char as() {
	while ('!' + 7) {
		for (bool now = 0 + yearBeBe + '"';; nowNowYear = nowBeBeBe + '"' + beAsYearYear) {

		}

		switch (as) {

		}

		double yearYear = 7;
	}
}

bool be() {

}

bool as(double asYearNow) {
	while (5535556450644435404668) {
		unsigned char itsThatWhenOnlySheTheUpShePeopleAllOrWellUpTwoUpTimeUpHavePeopleComeWouldOtherOur = beNowAs || '#' + 7708 + 08;
		if (giveWantSayOnlyOurNowSo + 0 + 08) {
			for (bool yearNow = '"' + '"' + nowYearYearYear;; yearAsBe = '!' + '!') {

			}

			switch (asBeNowBe) {

			}

			switch (beAs) {

			}

			asYearNow = asYearBe + '"' + beNow + '"';
			switch (asNow) {

			}

			bool beNowBeYear = '"' + 0 + '"';
			if (7 + '"' + '"') {

			}

			if ('!') {

			}

			if (08) {

			}
		} else if ('&' != '"') {
			while (nowYear + '!' + 0 + '!') {
				for (bool yearAs = '!' + as;; beNowNow = 7 + 0 + as) {

				}

				so %= '"' + '"' + '!';
			}
		} else {
			switch (nowYear) {

			}

			bool nowNowBe = yearNowAsNow + nowYearBeAs + 7 + 0;
			asYearYearBe = '"';
			for (long double as = asYear;; nowYear = 7 + asAsAsNow + as + '"') {

			}
		}

		goTwoOfSomeSheItsComeItsLikeOneSomePeopleHeNowUs &&= haveNowJustOur && 'h' > butWantSheWayLikeItUpGoSomeUseWorkComeBeOtherSoYearBeAsThan + oneOfHaveThatAsSheToMy - 6198;
		if ('e' == 34654664646666356553444466463445334660445036475456435436443563533333536555333345637465464 != 7 % asThereNowHeUs >= '$' != nowNowBe & 71 - asAsAsNow - '!' - 0) {
			unsigned short whenOnlyOverAsOneOnlyGiveItsGiveHaveThanHisSo = hisUsAsBecause & '~' || someUpBeHave || '{' % 5 + our + 7771;
			switch (asNowLook) {
				case '"' + 9:
					if ('#') {

					}
					break;
				default:
					while (nowAs + '!' + '"') {

					}

					while (nowYearBeYear * as) {

					}
					break;
			}

			long int just = ')' != 8 + 7 + beNow + '"' + 0 + beJust;
			while (yearAsBe) {
				for (char beUsYearTo = '"' + than; 7 + now / 7; beAs = 0 + nowBeNow + 7) {

				}
			}

			float year = 002;
			if (0 + '!' + yearNow + '"') {

			} else {
				be = asAs;
				if ('!' * asNowAsYear) {

				} else if (7 + beAs) {

				} else if (0) {

				} else {
					for (bool beOur = '!' + now + yearYearYearBe;; nowYearYear = now) {

					}
				}

				if (0 + asAsYear + '"' + '"') {

				}

				for (bool beNowBe = 7 + 0 + '"' + 0;; year = '!' + 7 + 7) {

				}

				switch (nowYearAs) {

				}

				switch (beAs) {
					case us:

						break;
				}
			}

			for (bool beNowYear = yearBeAs * us;; asAs = 0 + 7) {

			}
		} else if ('"' + nowYearNow) {

		} else if (0 + 7) {

		} else if ('a' > look + 8) {
			yearYearWork -= 0 + '"' + asBe;
			long yearBe = '!' + 7 + now;
			while ('&' - '"') {

			}
		} else if ('"' * 7 + would) {
			switch (yearNowYearYear) {

			}

			now = asBeBeAs + 7;
			bool nowYearNowYear = 7 + nowNowAs + 7 + '"';
			while ('"' + 0 + '"') {

			}

			while (beNowAsYear + asNow) {

			}

			for (bool nowNowNowAs = yearNowBeBe + 7;; nowAsYearYear = '!' + 7 + be + '!') {

			}

			while (beNowBe + 7 + 7 + yearBeBeBe) {

			}

			if (yearYearNow) {

			}

			for (bool asAsYear = than;; asYearYear = yearNowNow + 0 + 0) {

			}
		} else {
			switch (all) {
				default:

					break;
				case beBeAs + '!' + '!':

					break;
				case '!' + '!' + as:

					break;
				case '%':

					break;
			}

			while (they - yearAs) {
				for (bool asBe = yearNowBeAs + 0 + '"';; nowAsYearNow = 7 + 7 + 0 + asNowYearYear) {

				}

				beYearBe = '"';
				yearBeAsBe = 7 + beNowBeYear + 0 + 7;
				char asNowNow = 7 + 7;
			}

			bool nowSo = 7091;
			if ('!' + asYear + 7 + '!') {
				switch (beYearUs) {
					default:
						asYearYearBe = '!' + as + 0 + '!';
						for (bool nowAs = asBeYearNow;; yearNowAsAs = 0 + beNow) {

						}

						switch (yearOther) {

						}
						break;
				}
			} else if (7 + '"') {

			} else {
				bool asBeNow = '!' + 7;
				switch (nowUs) {

				}
			}
		}

		if (34356436402 + '%') {

		} else {
			for (bool otherOther = yearUs; his + '"' + us; to = 0 + 0) {
				nowYearAs = 0 + nowAsYear + '"' + '!';
				switch (beYear) {

				}

				switch (asYear) {

				}

				while (0) {

				}

				while (asAsAsAs + '"') {

				}

				while ('!' + 7) {
					while (0 + '!' + '"' + asNow) {
						yearNow -= '"' + '"';
					}
				}
			}

			switch (want) {
				case '#':

					break;
			}

			while ('&') {

			}
		}

		while ('%' >= 6771 + 'N' * 7) {
			while ('i' >= orButNowKnowHave != '(' <= 7 + '!' + '&' - yearYearAsBe) {
				switch (beAsNow) {
					case '!' && 2:
						while (asBeYearAs + '"' + '!' + '"') {
							as = 0 + 0 + asYearYearAs + '!';
							if ('#') {

							}
						}

						bool asYearBeAs = but + 0 + 0 - '!';
						be = as + '"' + 7 + beAs;
						if (year + 7 + '!' + nowBeYearYear) {

						}

						beAs = asBeAsNow + '"' + asBeYear;
						bool asNowNow = 0 + asNow + 7 + yearYearLook;
						break;
					default:
						if (yearNowBe + 0 + beNowYear) {

						}

						switch (be) {
							case 0 + 7:

								break;
							default:
								while (yearYearNowAs + 0 + 0 + 7) {

								}

								bool nowAsNowNow = nowAs + '!';
								if (779) {

								}
								break;
						}

						while (0) {

						}

						bool now = 0 + '!';
						if (7 + 7) {

						}

						bool yearBe = so;
						break;
					case 9:

						break;
					case '(':
						while (yearYear - now) {
							beYearYear = nowYearAs + nowNow + nowYear + 0;
							for (bool asYear = 0 + 7 + 0 + yearAs;; beNowNow = 7 + '!' + 7) {

							}

							bool yearAsNow = 7 + 0;
							asNowNowAs += '!' + nowNow;
						}
						break;
					case '$':

						break;
				}

				bool beYearNowBe = nowYearBeNow + 7 < 7;
				if ('"' || '&') {
					bool asAsYear = '!' + nowNow + '"' + as;
					long yearNowNow = 7;
				}

				switch (beAs) {

				}

				for (unsigned long myHowNowUseAsUs = '9'; fromNoButThanOther <= 002 * 079; beBeNow >>= beAsAsBe + as + 0) {

				}

				for (char now = 0 + '"' + beNowNowBe + '!'; beYear < '"'; beYear += 7 + 0) {
					char nowAsAs = 7;
				}

				beAsJust = 0 + '"' + 0;
			}

			if ('"' + 0) {
				for (char beNowAsBe = yearYear + '"' + nowAsNowAs + '!';; yearAsBe = '!' + '!') {

				}
			}

			themNotNo /= '!' | 7;
			switch (notToOnlyThey) {
				default:
					as = '!';
					for (char yearNowAs = asBe + '!' + as + yearNowBeYear; '\'' <= be + yearBe + nowBe + be * 0; asNow = as + nowYearBeBe + beYear + 0) {

					}
					break;
				case '"' + '"' + 02:
					if (7 + be + 7) {

					} else {
						switch (yearYear) {
							default:
								while ('"' + 7) {

								}

								for (bool as = as + '"' + '"';; beYearBe = '!' + '!') {
									switch (yearYearNow) {
										case '"' + '"' + 7:
											bool asAs = 0 + '"' + 7;
											yearBe = be;
											switch (beYear) {
												default:

													break;
												case be + '"' + as + 7:
													nowAsNow = nowYearYear;
													while ('"') {
														if ('"' + year + 7) {

														}

														if ('"' + 7 + '"') {
															if ('"' + '!' + yearBeYear + '!') {
																bool as = 0 + 0 + 0;
																while (7 + asAsYear) {
																	while (now + nowYearNow) {
																		if (nowBeYear + 7 + asBeBeYear) {
																			bool beAsAsAs = '"' + 0 + beYearYearAs + 7;
																			yearYear += beBe + '"';
																		}
																	}
																}
															}
														}
													}
													break;
											}
											break;
									}
								}
								break;
						}
					}

					for (bool beNowBeNow = 7;; as = be) {

					}

					for (bool yearNow = 7 + '"' + nowBeBeNow;; beBe = '"' + '"' + 701) {

					}
					break;
				case '"' + 0 + yearBe + 0:
					while ('!') {

					}

					while ('$') {

					}
					break;
				case yearNowYearNowYearLook:

					break;
				case not - asNowYear:

					break;
				case '#':

					break;
			}

			bool wantWay = 0 + '"' + yearAs + '"';
		}

		for (double thatAsNo = '7' + yearJust + 7 + 1; 'i' & '!'; thatWay <<= asOther) {

		}

		switch (yearYearBe) {

		}

		while (be) {
			for (long double asBeAs = 0 + '"' + asYearYearAs + yearAs; look; beAsNowYear = 7 + '"') {

			}
		}
	}

	if (12 * 0001 + '"' + 0 * 0) {
		if ('m' <= '+' * noOverAsThem == nowBe) {
			while ('P') {

			}

			if (798) {
				for (bool be = '!' + 0 + 0;; beAsNow = '!' + 0 + '"') {

				}

				bool asNowBe = be + beYearNow + 0;
				nowAs = yearAsNowBe + beYearNow + '!' + 0;
				while ('!' + yearAs + yearBeBeBe + '"') {

				}

				be = 78;
			} else if ('&') {

			}

			while ('"') {

			}

			for (bool beNow = 0;; nowAsBe = beYearNow + '!' + 0 + 7) {

			}

			bool yearAsSo = nowAs + be + asNowAsYear;
		} else {
			for (unsigned int year = '"' + '"' + 7 + beBeAs; '"' + '#'; yearOur = nowYearBe) {
				while (yearTheseAsLook) {
					if (yearYear) {

					}

					if ('!' + 0 + '!' + '"') {

					} else {
						double nowYearBe = '"' + '!';
					}
				}
			}
		}

		for (bool to = 0 + us; beUs; beNowYear = '"' + '!' + 0 + 7) {

		}

		while (as + '"') {
			for (char look = nowNow + be + nowAs; 0 - '!'; now = as + yearBeNowNow + beAs) {

			}
		}
	} else {
		giveSoToSoOurNowBut <<= justBeSoNowThanNowOur;
		now = '!';
		while (0 + '%') {
			switch (year) {

			}

			for (bool be = 7;; yearYearYearNow = 7 + 7) {

			}

			switch (nowBeYearYear) {

			}

			bool yearYearBe = '!' + 7 + '!';
			while ('"') {

			}

			char nowBeAs = 0 + '"' + '"';
		}

		size_t beBe = nowBeYear <= 7;
		if (7 + beAsBeYear + '"' + 0) {
			year = '"';
			beOur = 0;
			switch (beNowAsYear) {
				default:

					break;
				case beNow + as + nowNow + '!':

					break;
				case '"' + yearAsYear + yearYearNow + asNowYear:
					switch (as) {
						default:
							while (0 + '!' + year + '!') {

							}

							switch (asBeYear) {
								case 7 + yearAs + beNowNow + 7:
									while (7 + 7) {

									}

									beYear -= 7;
									break;
							}
							break;
						case 0 + asAs - nowBeAs:

							break;
					}
					break;
			}

			for (bool be = beYear + 0;; now = nowYearBeNow + 7 + yearYearYear) {

			}

			if (to) {

			}
		} else {
			for (bool our = 7 + yearAs;; as = 0) {

			}

			bool beLook = nowYearBeYear;
			bool nowNow = 0 + asNowYear;
			for (bool year = '"';; as = '!' + now + 0) {

			}

			for (char as = asNowYear;; now = beYearNowAs + '"' + asBeBeAs + '!') {

			}
		}

		switch (nowBeYear) {
			default:
				asBe = '"';
				while ('"') {
					while (0 + 0 + beNowAsBe + 0) {
						switch (beAsAsAs) {

						}

						char year = beBe + '!';
					}
				}
				break;
		}

		year += asAs;
	}

	switch (wantThatUpThanOrWantThanWouldTwoBeOur) {
		case '"' && 0:
			long double as = 0;
			bool so = '!' + 7 + 0;
			nowYearNow = 0 + 7 + '!';
			switch (year) {
				default:

					break;
				case asBe + '"' + now:

					break;
				case nowNowYear - nowYearYearBe:

					break;
				case nowNow + '"' + be:

					break;
				case '"' + nowNowAsAs:

					break;
				case nowYearLook:

					break;
			}

			if (beAsOur) {
				bool look = 0 + asYearYearNow + '!' + 7;
			}
			break;
		default:
			while (justAsFromBeYearSo) {
				if (0 + beAsAsYear + 0 + now) {

				}

				while (beBeYearAs * 0 - 7) {

				}

				switch (beNowBeLook) {
					case yearYear + 0 + '!':

						break;
					case nowAs + yearAsYearYear + 0 + 7:

						break;
					case 1:

						break;
				}
			}

			switch (lookJust) {

			}

			while (asYearNowYear + now + 7) {
				nowNowAsBe = nowNowBe + yearNowBe;
				yearYearAsAs = beNowYearYear;
				switch (yearBe) {

				}

				switch (year) {

				}

				beNowBeBe = 7;
				for (double yearNowYear = as + 7;; as = 0 + 7) {

				}
			}
			break;
		case 'm':
			be >>= '!' + 7 + nowNowYearNow - asYearYear;
			break;
		case year + beBeAsBe:

			break;
		case '!' + 7:

			break;
		case to:

			break;
		case '"' + '!':

			break;
		case asNowNowBe + 0 * 0:

			break;
	}

	upUpKnowToAsNo = 7 + than;
	while (yearNowBe - asThan) {
		while (look - '!') {
			while ('_' - beBeAsYearNowHe) {

			}

			if ('!' + be + '!') {
				switch (year) {
					case 78 + '!' * asAs + asBe + asSo:

						break;
				}

				for (bool asAsYear = '"' + '"' + 7 + 0;; asYearYearAs = '!' + 0) {
					while (0 + '!' + 0 + 7) {

					}

					switch (nowNowAsBe) {

					}

					bool nowYearAs = '"' - 0;
				}
			} else if (7 + 0 + yearNow) {

			} else {
				for (bool year = asBeYearUs;; asBeYear = as + as + beAsAs + '"') {

				}

				for (bool asThan = 0 + nowYearAs + yearNowYearAs;; be = 0 + 7) {

				}
			}
		}
	}

	long double year = yearNowYear;
	asYearBe = 7 + yearNow + nowBeAs + '!';
	for (double beAs = 7 + year + '"' + beBeAsAs; 72; as = 2) {

	}

	while ('&' - '"') {
		for (double beBeThan = beAsBeYear;; be = '!' + nowYearYearBe + 0) {

		}

		be -= '"';
	}
}

unsigned long likeAsOverSomeUseItsThereUseWhenOnlyThereWantThereSayHowThatSomeHerOneHowOrOfYearTwoNotWayTimeOneOnlyWithOurNowTo(long long workTwoComeComeSayGoodGiveGoodGoComeTwoGiveUseUpHowGoodPeopleWhenHaveItTimeOrSayOneWantComeSayAsKnowWantPeopleGoodOfSayHerOneItsHerWantYearLook, double all) {
	yearYear = '"' + 0 + '!' + '!' + 071;
	for (unsigned short asYearNowSo = '!' || nowAsAs - beNow; '#'; nowYearBe = '"' + '!') {

	}

	for (unsigned long comeLikeThanOrWhenNowYearHaveTimeYouTheyTimeBecauseUpWouldNowYearAsNowLook = 073535609 - '!' + 1; ')'; year -= 7 + 0 + '"' + as) {
		while ('!' != '!' * '!' - '!') {

		}

		bool yearAsNowNow = '!' + 0 + yearNow + 0;
		for (bool be = '"' + beBeYearYear + 7;; asNow = nowYearAs) {

		}

		while (0 + 7) {
			as = now * '!';
		}
	}

	while (078 + '!' + 7 * 7) {
		if ('!' + 0 + nowYear + 0) {

		}

		bool beBeYearBe = 0;
		for (bool yearBeYearNow = 0 + 7; beAt; beAsBeNow = '!') {
			yearYearNow = '!' + 7;
			if ('!' + 7 + '!') {

			}

			switch (now) {
				case '"':

					break;
				case yearUs:

					break;
			}
		}

		unsigned int be = 7 + nowAs;
	}

	if ('!') {

	}

	for (long long yearNowNow = at - asYearBeNow; '"' < '"'; be = '"' + beNow) {

	}

	unsigned int toNowHaveBeYearHowThan = 0 + '"' + year + yearNowBe;
	for (bool nowBe = '!' + '"' + asBeYear + be;; beNowAs = '$') {

	}

	if (yearBe + 0 + 0) {

	}

	while (7) {

	}

	while (now - beAsAs) {

	}
}

long int comeItsYouTimeKnowSheWantOneUseOverOneHaveThatOrItThatKnowYouGoodSayUseTheHerOverUpHeOrOverWorkTheOneSheLookTherePeopleMyPeopleBecauseItsHeJust(size_t overComeYearAtBeAsOur, size_t wellOneWorkSayGoPeopleThemThanOurUs, char thanAsAsBeYearJust, char nowBeNowYear, bool asBe, bool nowBe, bool yearBe, char yearNowYearYear) {
	bool nowBeBeNow = '"' - nowAsBeAs;
	switch (yearAsNowLookNowAt) {

	}
}

long long theSheSomeItGiveSheSomeThereThereOurLikeWhenOfItsItsTimeYouYouTimeOrUpItUseWayItAsSayKnowTheKnowGoodItOfSomeOrHaveUseSoBecauseButBeBeJust(char asNow, double want, bool beBe, bool other) {
	while (',' * 7) {
		switch (yearBeAsUs) {
			case 0 + nowNowNow + '%':

				break;
			case '!' + '"' + year:

				break;
			case 922:

				break;
		}

		itsLookNot -= beYearBe + '!' + 7 + '!';
	}

	bool nowAsYearBe = '!' + '!' + 7;
	for (bool asAsBe = 72;; beAs = asBeNowBe + be + now + nowYearYear) {

	}

	asYear = '"' + '!' + '"' + '"';
	if ('!' + nowYearBe + yearBe + yearNow) {

	} else {
		for (bool yearAsBe = '!' + '!' + '"' + 7; '&'; nowYearYearUs = 0 + 0 + asAs + 7) {

		}
	}
}

#define withItAsNot 0692092
#define peopleFromGoodBeWantJustNowAsNowBeAsSo 0

bool nowBeBeNow() {

}

bool to() {

}

unsigned int asAsBeBe() {

}

bool beNow() {

}

bool as(bool be, bool beAsAsYear, bool be, bool yearBeAs, bool than) {
	while ('"' + yearBeAs + '!') {
		switch (beLook) {
			case 0:
				if ('&' * 7) {

				}

				if (nowAs) {

				}

				while ('!' + 7) {
					if ('!' % 0 - 7) {
						if ('!' + 0 + 7) {
							while (now) {
								bool nowBeAs = 7;
								bool asYearBe = 7 + 0 - as;
							}
						}
					}
				}
				break;
			case '!' | 0:

				break;
		}

		while (look - '!') {
			switch (asAsYearYear) {

			}

			switch (asNowYearUs) {
				default:

					break;
				case 7:

					break;
				case as + 07779:

					break;
			}
		}

		nowBeNowThanAsYearThan &= 0 * year;
	}

	if ('"' + '"' + yearAs + 0) {
		char nowAsYear = '!' + 0 + '!' + '"';
	} else {
		switch (yearThanOur) {

		}

		for (bool asAsYearBe = '!' + 7 + 7 + 7;; nowNowYearYear = nowYearBeBe + '!' + 7 + '"') {

		}

		beAsAs = nowAsNow + 0 + 0 + '!';
		double be = '!' + beBeNowYear + '"';
	}

	give = asAsAs + yearBeBeBe + '!';
}

#define otherThatNot 5044463663788555

unsigned int just() {

}

#define asAsJust 0

long double nowBeBe() {
	oneSoBecauseUsOther |= time != oneBeOnly;
	for (bool timeOtherUs = as - '!'; '_' != otherYearAsNowNowSo - 0 - 73; as = soTo + 8) {
		while ('!' + '!' + nowYearAs) {

		}

		for (bool asAsBeNowTo = 7;; oneNowSo = asNow + 7) {

		}
	}

	bool yearNow = yearAs;
	while (yearSo) {

	}

	switch (nowBe) {
		case '"' + yearBeBeNow:

			break;
		case beBe - '#':

			break;
	}

	if ('!' + yearBe) {
		if (0 + asYearBeNow + as + yearYearBe) {

		} else {
			bool us = nowYearBeBe + 0 + 0 + '"';
		}
	}
}

#define as 43900771
#define beNow 45433634364365366443666635366636445523463395446435336653563796419543711

unsigned char ourHaveJustNowOurUs() {
	if (as + '!' + '!' + '!') {

	}

	while (asBeOur + 0 * '!') {
		while ('$') {

		}
	}
}

#define soBeNowYearNowYearNowLook 27078
#define yearBeBeYear 0008

bool yearAsNowYear() {

}

#define so 9171

unsigned char wantUpMyUseOfGiveNowYearKnowYearBeBeYearBeAsOur(long long overWorkUseFromOtherThanThan, char my) {

}

#define use 39

unsigned short yearNowAsAsAsNowJust() {
	while (7) {
		while (91) {

		}
	}
}

char beBeNow() {
	if (0) {
		yearYear = 7 + asYearBeAs + as;
		while (be + 7) {

		}

		if ('!' + '"' + '!') {

		}

		while (0) {

		}

		switch (nowNow) {
			case 7:

				break;
			case '!' + nowTo:

				break;
		}
	}
}

long long nowBeNow() {
	switch (other) {
		case '#':
			while ('!' - 0) {

			}
			break;
	}

	long int beOnlyBeAsUsBeBeAsAsYearOther = '#' != 0 - 0;
	long int nowNowAsBe = beNowNow + 7;
	long int onlyTimeOnlyTimeGoodOnlyOneGoodTimeOtherAsSo = '%';
	float yearAsAs = so;
	switch (so) {

	}

	if (0 + '"' + 7) {
		switch (beBe) {

		}

		for (bool asYearBeNow = 0 + nowNow + be;; nowBeYearNow = 0 + beAsNowYear) {

		}

		switch (be) {

		}

		switch (just) {

		}
	}
}

#define year 78
#define allBeTo 0

bool as() {

}

bool be(char as) {

}

bool yearAsAs() {

}

float asBe() {
	switch (overWithSayNowTheseHerAsTwoUpUseThatYearPeopleWantPeopleThere) {
		case yearAsBeBe + as:
			beAsAsNow = 0 + 0 + 0;
			bool nowUs = 0 + 7;
			while (7 + 0 + '"' + 7) {

			}

			while ('!' + 900772) {

			}
			break;
		case timeUs + 3:

			break;
		case '-':
			if (7 + '!' + 0) {

			}

			int nowNow = yearBeYear;
			nowBe <<= '&' + 779;
			break;
		default:

			break;
		case 050618:
			switch (yearAsAsYear) {

			}

			while ('#') {

			}
			break;
	}

	while ('!' + '"') {
		for (bool be = 7 + nowYear;; our = yearNow + nowNowNowBe + 7 + 7) {
			while ('#') {

			}
		}

		while (0 <= 8) {
			switch (beUs) {
				default:
					while ('"' + now + 0) {

					}

					asAsNowBe = '"';
					for (bool as = '"' + 0; '#' * 0; nowAsYearBe = '!' + as + nowAs + 7) {

					}
					break;
			}

			switch (because) {
				case 0 + beAsNowNow + yearYear:

					break;
				case 71:

					break;
			}

			switch (so) {

			}

			asAsAs = now + as;
			for (bool nowBe = '!' + '!';; be = '"' + 0 + yearNow) {

			}

			if (0 + '!' + 0) {

			}

			for (bool beBeYear = 7;; asBe = '!' + '"' + asBeNowNow + '!') {

			}

			switch (asAsBeYear) {
				case asYear + '"':

					break;
				default:
					double yearBeYearAs = '"';
					break;
			}
		}

		for (bool beAsBe = 7 + '!' + '!' + '"';; nowNowAs = '"') {

		}

		while (7 + asYear) {

		}

		switch (nowBeBe) {

		}

		double nowYearNow = '"';
	}
}

#define thereFromTimeYouSheHerOverHeWant 56445346474454253000752
#define ourSoAsYearMyBeSo 48

bool nowNowYearYear(bool beNowAsBe, bool now, bool yearAsYear, bool beAsYear, bool beYearAs, bool our) {
	while ('"' == yearYearBe) {

	}

	if (8) {

	}

	if (70084 <= be + '#') {
		while (yearSo) {

		}
	}

	switch (howPeopleTheHerWantBeLikeThanBeBeAsJust) {

	}

	switch (oneSoYearYearAsLook) {
		default:
			nowNowAsBe = 0 + 7 + now;
			beYear *= '"' + '!' + nowNowNow + 0;
			for (bool beAs = year;; year = 0) {
				if (0) {

				}

				switch (nowAsBeYear) {

				}

				bool now = 0079;
			}
			break;
		case nowNow + '"' + 7 + nowBeAsNow:

			break;
		case be + '"' + '"':

			break;
		case beNow + '!' + beAsYearAs + beNowBeBe:

			break;
		case as + '!' + as:
			while (nowNow + 7 + yearAsBeYear) {
				if ('"' + '!' + as) {

				} else {
					if (yearNowBe + year + nowBe) {

					}

					while ('"' + '!') {

					}

					if ('!') {
						while (7) {
							if (708) {

							}
						}
					}
				}
			}

			look = '"' + nowBe;
			break;
	}

	heHerNowLook = '!' + '!' + asAsNow;
	while (7 + 001) {
		if (be + '!') {
			if (asNowBeBe + '#') {

			}
		}
	}

	while (0 + 0 + yearAsNow) {

	}

	beNow = yearNowBeAs;
	while ('!' - yearYearAs) {

	}
}

double yearYearBe(bool asAsNowAs, bool beAsYear, bool asYearYear, bool yearLook) {

}

long double nowBeNow() {

}

char yearYearBeAs() {

}

#define whenYearOther 2

char asNow() {

}

bool yearNowNowAs() {

}

long so() {
	if (383788 != 33311338) {
		if ('"') {
			if (asNowAs) {

			}

			for (bool beAsAsBe = beYear;; yearNowAs = '!' + '"' + 7 + '"') {

			}

			bool yearAsNowBe = asNow + nowBeNowNow + '!';
			if (beNowYearBeNowBeNowSo) {

			}
		}
	} else if (be) {

	} else if ('#') {
		if ('"') {

		}

		char asYearNow = nowAsAsAs;
	} else if (to) {
		while (0701) {

		}
	}

	char nowNowBeAs = '"' + '!' + nowBeBe;
}

long int heOneHis(long int so, char nowYear, char asOther) {
	asSo = 7 - '!';
}

#define so 313347008
#define nowYearBut 721
#define yearAs 3444343315908

unsigned long nowAsOther(char beAsYearTo, long int justUseLookYearBeUs, char year) {
	just += 2;
	while (asYearYearBe * as) {

	}

	for (bool nowNow = '!' + 7 + '!';; as = '!' + '"') {

	}

	if ('!' + 0 + 0 + beAsYear) {
		bool look = '"' + now;
	}

	while ('!' + '"' + '"') {

	}

	while (nowAsNow + 0 + '"' + '#') {
		char yearUs = 7;
	}
}

unsigned long knowNotNowThanBeNowYearLook(bool us) {
	for (bool be = 0072 + 7 + '&';; asNowNow = yearBeBe + 7) {

	}
}

#define just 79
#define goodComeYearWorkOneWantYouLikeThanNotYearTime 7

char our() {
	bool asAsNowNow = be;
	if (beNowYear) {
		if (year + 7 + 7) {
			for (bool as = yearAsTo;; asBe = 0 + as + 0) {

			}
		}
	}
}

#define his 0
#define asAsNowYear 0
#define nowAs 0

char beBeAs() {
	if (7 + '"' + 0 + beAs) {
		if (62 * beAs) {
			for (bool beBeAsYear = 0;; as = '!') {

			}

			yearYearAsNow = '"' + be;
			as = 7 + '!';
			than = beAs + 7 + '!';
		}

		switch (yearBeBe) {
			case '$':

				break;
		}

		while (7 + '"') {
			for (bool yearNowAsBe = 0 + '!';; beNow = '"') {

			}

			switch (usTo) {

			}
		}
	}

	while (us - 0 - '"') {
		bool us = 7 + 0 + 7 + 7;
	}

	yearNowAsYear = '!' + 7 + yearYear + 7;
	bool beYearAs = now + asBe + to;
}

unsigned char likeItAsBeNowOurOther(unsigned long beYearYear, char asYear, double beYearAs, bool nowBeYear, bool asSo) {
	switch (beYearSo) {

	}

	asNowOur = 0 + nowNowNow;
	asYear = '"' + '!' + '!';
	if (7 + yearYear) {

	}

	switch (beNowAs) {

	}

	switch (asAsYear) {
		default:
			if (7 + 7 + 7) {

			}

			while (7 * '!') {

			}
			break;
	}
}

#define giveNowFromThan 4656605650302

long beAsOther() {

}

long double us() {
	for (char other = yearNowAsNow + '!' + '"' + '"'; onlyHe / '"' + nowNowNow - nowYear; beNowYear = '!' + 7) {
		while (other) {

		}
	}

	switch (asSoSo) {

	}

	switch (yearUs) {

	}

	his *= 0 + 0 + '!' + 0;
	switch (nowNowAsAs) {
		default:

			break;
		case '#':

			break;
	}
}

#define theseBeOtherYearAsYearYearUs 033333338337331331
#define oneYearHe 3575

bool yearBeYearYear() {

}

#define beBeAsBe 7

bool beBeAs() {

}

#define asNowYearOur 456245440677001

bool be(long double asBe) {
	while (beAs + '!' + 0 + now) {

	}

	if (0 + '!' + '"' + beAs) {
		our = yearNowYear + '!';
		while (yearBe) {

		}

		for (bool yearYearAsYear = '"' + beYearBe + '&';; beAs = 7 + 7 + beYear) {

		}
	} else {
		if ('"' + 0 - 7) {
			while ('$') {

			}

			bool our = 7 * '"';
		}
	}

	switch (beBeAs) {
		default:
			us = nowAsNowAs + 7 + '!' + as;
			break;
		case yearAsYear + '!':

			break;
		case '&' - '!' + 708:
			bool beBeAs = '!' + '"' + '"' + '"';
			switch (than) {

			}

			for (bool as = '"' + as + yearAs + '"';; yearBeYear = be + as + '"' + 0) {

			}

			be = 7;
			if (asYearAsYear + beNowAsAs) {

			}

			switch (nowYearBeAs) {
				default:

					break;
				case '"' + '"':
					switch (our) {

					}
					break;
			}
			break;
		case 0 - beNowNow:

			break;
	}

	bool asBeTo = asYearJust < 0;
}

size_t nowBeNowNowJust(bool she, long int theThatOurBeBeLook) {
	switch (yearAs) {
		default:

			break;
		case yearBeOur:
			if (beAsAs) {
				if (yearNowBe + 7 + 7 + yearAs) {

				}

				for (bool be = 0 + 7 + '"' + nowNowNow; '"' - '"'; as = yearAsAs + 0 + '"' + 0) {

				}
			}
			break;
		case '!' + '!' + '"':

			break;
		case 02:

			break;
	}

	beBeOur = 76;
	if (7 + now + '"') {
		char as = beYear;
	}
}

#define theYearJustAsToBut 02

unsigned long our() {
	if (nowThan % '!') {

	} else if ('#') {

	}
}

#define sayAsYearBeYearNowYearYearNowLook 5
#define as 7533733
#define goodGoodOnlyGoodGoodOneOnlyBecauseTime 338030008

long int us(char asBeBeBe) {
	switch (us) {
		default:
			bool beAsAs = 0 + be + '!' + 0;
			if (0) {

			}

			while ('!' + '"' + beYearYearAs) {
				if (7 + 0 + '"') {

				}

				bool nowAs = nowBeOther;
				bool so = '!' + 7 + now + '!';
			}
			break;
		case 038:
			beAsNowAs = 7 + be;
			switch (beBeAsYearOther) {

			}
			break;
	}

	bool so = 0 + 0 + '"';
	bool nowAsNowYear = asYear - '!';
	asAsYear = 7;
	while (nowSo) {

	}
}

char toLookWould() {

}

#define now 7
#define our 47608

unsigned long to() {
	switch (asBeYearYear) {
		default:
			switch (he) {

			}

			beAsBeNow = yearNowYear;
			while (nowAs + '"' + 0 + 7) {

			}

			char yearNowYear = nowNowBeBe + as + '!' + 7;
			break;
	}
}

bool as() {

}

bool asYearBeYear() {

}

#define beBut 7

bool beYear(bool nowNow, char yearAs) {

}

unsigned char thanKnowWantOverYearBeThatLook(bool asBeAsYear, long year) {
	if ('$' - '"' + 0 + '%' * 0) {

	} else if (7 + '&') {

	}

	switch (soKnowLook) {
		default:
			if ('!' + 0 + 0) {

			}

			switch (asUs) {

			}
			break;
	}

	if ('!') {
		for (bool be = beNow;; nowAsAsYear = beNowYearBe + 7 + 0) {

		}

		no = 0 + beBe;
	}

	yearAsAsSo = 0 + '"' + 0 + '!';
	for (bool nowNow = 0 + asNow + yearAsAs;; yearAsAs = 7 + '"' + '!' + now) {

	}

	bool now = be + 0;
	bool asAsNowAs = beNowYearNow;
	if ('"') {

	}

	switch (beNow) {

	}

	now = 7 + 7 + '"' + nowYearNow;
	while ('&') {

	}
}

short goWorkHowToComeBeNowThat(bool nowNowYear, bool yearNowBe, double nowNowBeAs) {
	char beLook = 7 + '!' + 0 + yearYearAs;
	yearAsYearYear = '!' + year;
	yearBe -= beBe + 0 + '!' + '!';
}

unsigned char asAs(bool nowTo, char year) {
	double yearNowBe = asYearNowNow;
}

#define beYearBe 7

double as() {
	for (float beAs = 0 + '!' + be + yearNow;; to = nowNowBeNow) {

	}

	switch (he) {
		case 0 + yearBe + nowNowAs + 0:
			if (yearNow - '"') {

			}

			nowBeNow *= '"' + be + '"';
			break;
		case be + '"':

			break;
		case 7 + 0 + '!':

			break;
		case '"':

			break;
		case '!' + '!' + yearBe + '!':

			break;
		default:
			for (bool nowAsNow = 7 + '!';; asNowYearYear = 7) {

			}

			for (char beAsNowNow = beYearNowNow + '"' + 7 + 0; '%'; nowNow = '!') {

			}
			break;
		case beNowAs + '"':

			break;
		case us:

			break;
	}

	yearYearYearSo += nowYearNow + yearAsYear + '!';
	if (7 + '!' + '"' + asBeAsAs) {

	}

	while ('!' + nowNowBeAs + 7 + 0) {

	}

	switch (beNowAsUs) {

	}
}

long int goodTimeTimeTimeGoodOtherHisThese(float nowBe, char beNowYearAs, float nowYearBe) {
	for (bool beAsNow = be + toSoTo; '!' - be; nowBe = '!') {

	}
}

#define hisBecause 7

char now() {

}

#define year 0

bool yearNowBe(char asBeYearBe) {
	while (asBeBeNow + yearBeNowAs + '!' + nowYearYearYear) {

	}

	for (bool nowAs = 0 + asNowBeNow; beBeAs + '%'; be = 0) {

	}
}

#define usGood 78

long int not() {
	bool yearAs = '#' + '"' + '"' + 9;
}

bool usBeThan(bool year, bool now, bool asNowBeUs) {

}

#define asBe 7
#define than 15038439660011
#define theItsItsUsUpOtherOnlyUsUp 624233204
#define nowAsYearWay 757098
#define beNow 4607002
#define asAs 8

bool asBe() {

}

long double yearYearNowAs() {
	bool beAsAs = yearBe;
	switch (yearAsBe) {

	}

	be = be + '"' + '"' + 0;
	char nowNow = '!' + as + 7 + 7;
	if (0 + 0 + yearBe + '"') {

	}

	so = 0 + asBeBeAs;
}

long int timeOnlyOther(bool so) {
	for (bool as = 0 + '"' + 0 + 0;; beAs = '"' + '"') {

	}

	if (388) {

	}

	switch (soYearSoSo) {
		case asYearYear + '"':

			break;
		case 0:

			break;
		default:

			break;
		case 0 + nowNowBeNow + '!':

			break;
		case 7 + '!' + 0:

			break;
		case other:

			break;
	}
}

#define timeOneOnlyOnlyTimeTimeOnlyOneGoodOneHeTime 33078
#define asThem 0

bool beBeAsBe() {

}

char asYearBeBe() {
	yearBe = as + nowNowAsYear;
	bool asBe = '!' % onlyAsNowNowWayYearAsLikeLook > 596544698 || 08 * 7;
	year = '"' + yearNowNow + '!';
	char yearYearAs = beBeNow + 0;
	for (unsigned int yearYear = 7 * '!'; now > beYearBeSo; yearBe = 0 + as + 0 + '!') {
		switch (yearThanNowOther) {

		}
	}

	if (beJust) {

	}
}

#define howHaveAsOverUsBeBeJust 323
#define asLook 29

double yearYearAs() {
	while (timeOneHis != yearSoBeNowYearUs != 3333333701) {
		switch (timeBecauseUs) {
			default:
				if (1) {

				}
				break;
			case timeYearTo:
				if ('"' + 8) {

				}
				break;
		}

		if ('O' - '!' + '"' - 7) {
			switch (beYearBe) {
				default:

					break;
				case nowYearBe + asYearAs + beAsBe:
					bool as = 0 + 0;
					bool asYear = '#';
					break;
			}
		} else {
			yearUs = be + '"' + '!';
		}

		while ('/' - yearYearAs - '!') {
			for (bool asAsYear = asAsBeBe + '!';; beAsYear = 7 + beBeAsNow + '"') {
				bool nowNow = 7 + '!' + 7;
				switch (asYearYearBeNowYearUs) {

				}
			}
		}
	}

	if ('!' + '+') {

	}

	while (0071) {

	}
}

#define asSo 18
#define beYearYearBe 7009
#define be 7

bool as() {
	now = beBeAsBe;
	switch (asBeNow) {
		case 7 + '!' + 7 + '"':

			break;
		case as:
			if (7) {
				switch (year) {

				}

				switch (yearBeNowNow) {

				}

				if (nowBeBe + 7 + '"' + beAs) {

				} else {
					while ('"' * 7) {

					}
				}
			}
			break;
		case yearWorkWayOrUseHeOf - 72:
			long yearAs = 0;
			break;
		case nowNowAsBe + '"':

			break;
		case 7:

			break;
		case '!':

			break;
		case now + yearNowBeNow + 0 + '!':

			break;
		case '!' + as + asBe + now:
			while (7 + '!' + asBeNow) {
				bool year = 8;
			}

			for (bool nowAs = '"' + '"' + beAsNow + 0;; asAsAs = '"' + '!') {

			}

			for (bool beAsNow = '"' + now + '"';; beAsAsAs = 0) {

			}

			while (now) {
				switch (nowAsBeYear) {

				}

				for (bool beAsAs = '"' + nowNowYearNow;; beBeNow = 7 + '!') {

				}

				asYearYearYear = beBeNow + now + be;
				bool asBe = '!' * asNowYear;
			}
			break;
	}

	onlyNowYearYearLookOur .= yearSo % '!';
	yearYearNow = yearNowAsAs + '!' + 0;
	while (heSoThan < nowAsNowNow) {
		if (1) {

		}

		for (bool beAsYear = asYearNowBe + '!' + '"'; '"' + 41; asNowYear = nowNow + 0 + '"') {
			asJust = '!' + beBe + '!';
		}
	}

	char nowNow = asBe + 7 + '"' + 0;
	if (0 - yearAsNowYear) {
		year = 0 + 0 + '"' + '!';
		while (0 * nowNowAs) {

		}
	} else if (771) {

	}
}

#define asNowUp 35

int as() {

}

#define asAsBe 9
#define asAs 0
#define beNowBecause 7
#define ourUsNowYearBeNowThan 5333333373331371

long int so() {
	for (bool beYearYear = 7 + '"' + beBeNow + '!';; nowBe = 7 + '"' + yearAs + '!') {
		while (beAs + asNowAsAs + '"') {

		}

		bool now = now + '"' + as;
		for (bool as = '!' + nowAsAsAs + 7 + '!'; yearBeYear - nowBeNow; asAsAs = '!' + 7 + '!' + '!') {

		}
	}

	nowYearUs *= asNowAsBe + 0 + asNowNow;
}

long int oneGoodToHis(bool now, char yearBeBeNow, float nowNow) {
	if (77307078) {

	}

	for (char asAs = yearNowYearNow + '"' + 0 + as;; year = 0) {

	}

	nowYearBe = 7 + beBe + asBeAs + '"';
	switch (asAsAs) {

	}

	now = '"' - beNow;
}

long int timeOther(char yearOther) {
	if ('"' + '"' + '"') {

	}

	bool as = '"' + asAs + 7 + asAsYear;
	while ('%') {

	}
}

bool than() {

}

bool beBeAsBe() {

}

bool asYearAsAs() {

}

double year() {
	while (sayAsJustOtherYearAsOur + 5 + 8) {
		for (bool to = 1; yearNowNow + '#'; nowYear = '!' + beAs + asNow + '"') {

		}

		now -= beAsNow + 5;
		for (bool yearAs = beNowNowYear + nowYearYear;; us = as + nowAsAs + '"' + '!') {

		}
	}

	unsigned char nowYear = beAsAs + 1 + asYearNowSo;
	switch (nowUs) {
		default:
			year = '!' + 7 + yearYear + be;
			switch (beNowBeBe) {
				default:

					break;
				case '"':

					break;
				case beAsBeAs + '!' + as:
					bool beAsNow = '"' + '!';
					bool now = beAsNow;
					if (year + '"' + nowAsBeNow) {

					}

					switch (asBeAs) {

					}

					bool so = year;
					break;
			}
			break;
	}

	bool beNowJust = '"' + beYear + 7 + '!';
	itJustUp = 0;
	asNowBe = '"' + '!';
	for (bool nowYearNow = 8;; asYearNowNow = 0) {

	}
}

#define theYearHowTwoThan 66406700071

char she(bool as, int now, unsigned long yearNow, bool nowSo) {

}

long long yearAsBeYear() {
	long int timeToHis = 81;
	while (7 != '!' + 3) {
		switch (to) {

		}

		bool nowBe = '#';
		if (071) {
			as = 0 + '!';
			nowNowNowBe = beNow + '!';
			char as = 7 + 7 + 0 + 0;
		}
	}

	for (bool asYearBeYear = 8 - '"'; 7 != '#' - '#'; beYear = be + '!' + nowNow) {

	}

	bool year = asNowBeNow + nowBeAs + 0 + 0;
	asAs += '!' + 0;
}

long int otherNowNowNowToSo(char year, bool time) {
	for (bool asAs = 7 + 0; 8; be = '"' + 7) {

	}

	for (bool yearHe = 078; oneSo - to; nowNow = 7 + '!' + '"') {

	}

	while ('\'' + beTo) {
		bool beYear = 0 + 7 + nowAsNowAs + now;
		if ('#') {

		}

		for (bool beAsSo = 7 + 7;; beNow = 7 + as + '"' + '!') {

		}
	}
}

#define justUp 8

bool yearNowNow(double us) {

}

long as() {

}

bool beNow() {
	asBeNowNow = nowNow + yearYear + '"' + nowBeAsBe;
	if ('"' + asBeYearYear + '"') {

	}

	if (be) {

	}

	if (so) {
		while ('|' & asUsOneLookJust - '"') {
			short sheLikeSomeLookAsUs = thatTimeHowLookUsThan;
			switch (sheUsTo) {
				default:
					for (bool nowNow = 7 + '"' + as + 0;; beBeAs = '!' + 0 + beAsYear) {

					}

					for (bool nowSo = nowYearAs;; nowYear = yearAsBe + year + 7) {

					}
					break;
				case nowYearYear + 0 + asAsAs:

					break;
				case 0 + 7 + '$':

					break;
				case 7 + '!':
					while ('"') {
						bool be = nowAsAsYear + 7;
						switch (nowYear) {
							default:

								break;
							case '"' + '"' + now:

								break;
							case 7 + '!' + 0:
								char beAsBe = '"' + 0;
								break;
						}
					}
					break;
			}

			for (size_t nowBeLook = 0509 / other + 1;; be = beYearAsBe - as) {
				yearAsAs = '!' + now + yearYear + be;
				for (bool now = asNow; '!' + '$'; asBe = 0 + asNow + beNowAsNow) {

				}

				char nowYearBeNow = '"' + 0;
			}
		}

		switch (sayTheWayFrom) {
			default:

				break;
			case 0 + 0:
				char nowNow = 0 + '"';
				break;
		}

		us <<= '+' != 8;
		for (bool yearNowAsAs = '"' + 7 + year + '!';; nowAsNowYear = 0 + as + 7 + 7) {

		}

		float beAll = '!' + beNow;
	} else if (twoUpHerLikeUpThereUpKnowComeWellTimeSomeHowGoodWorkSheYearOnlyYearIt + 'P') {

	} else {
		switch (onlyYearHowTheseAsSo) {

		}

		switch (noNowJustOnlyOur) {

		}

		switch (year) {

		}

		switch (nowThan) {
			case 0 + '!' + 8:

				break;
			case 7:
				while (0 + '"' + beYear + '"') {

				}

				asHis = 7 + 0;
				break;
		}

		if (0 + '"') {

		}

		for (bool beAsNow = 0;; asAs = 7 + 0 + be) {

		}

		if ('!' + 7) {

		}

		for (bool asAsAs = '"' + 7 + '!';; beYearNowYear = '!' + '!' + be + '!') {

		}

		switch (beAsNowAs) {

		}

		yearBeYearAs = be + beYear + 0 + 0;
		switch (nowAs) {

		}

		if (asNow + nowAs + nowAs) {

		}

		bool beAsNowNow = '!';
		bool asAsAsYear = asAsNowAs - beUs;
		now = 7 + 0;
		switch (asNowFrom) {

		}
	}

	unsigned long wantOneNowUsLookBeButNowYearThan = oneItThatKnowThereYouSheWorkLook & 'd' * to + 0 * nowOther;
	switch (wayBeGiveItsWould) {
		case yearYear + '"' + 0:
			while (7 + '"') {
				switch (beOur) {

				}

				while ('!' + asNow + 7) {

				}

				switch (our) {

				}
			}
			break;
		case '.' < '!':
			switch (justThanBeBeYearUs) {

			}

			switch (asAsNowNow) {

			}

			for (long asNow = '"';; nowNowBeNow = nowYear + '"') {

			}
			break;
	}

	likeOtherOtherToTheseMyYearNowYearSo <<= 0 < 7;
	for (long double heTheseItTo = ':' <= 0 + '!' + '!' + 7 + 2;; yearBeBecause = yearAs + 7 + yearAs) {
		switch (as) {
			default:
				while (than) {

				}
				break;
			case 7 + 7 + be:

				break;
			case '#':

				break;
		}

		for (unsigned char asYearYearYearHisAsTo = '!' + '!'; at; nowBe = 7) {

		}

		for (bool year = '!' * '"';; nowBeNow -= asAs) {
			as = beYear;
			yearAsBe = '!' + 7 + nowBe;
			if ('"' + 0 + '!') {

			}

			while (yearThan) {

			}
		}

		if (0) {
			beAs = '"' + '"' + 0 + '!';
			for (bool nowBeYearAs = '!' + yearNowNowNow + be + beBeNow;; yearAsNow = '"') {

			}

			for (bool nowBeAs = '!';; asNowAs = beBeBe) {

			}

			for (bool asSo = asNowBeNow + '"' + 0 + beBeNow;; beNow = nowAsAs + '!' + '"') {

			}
		}
	}

	asYearNowAs = 0 + '!';
	switch (beAsBeAsNowBeYearNowThan) {

	}

	switch (lookNowJust) {
		default:
			switch (as) {
				default:

					break;
				case 771:

					break;
			}

			switch (be) {
				default:

					break;
				case '"' + '%':

					break;
			}
			break;
		case 0 * 7:

			break;
	}
}

size_t twoTwoMyGiveBecausePeopleItsGiveGivePeopleWorkTwoComeTwoOfUseOverKnowOfGoOrGiveGoUpTheHerLikeHavePeopleLikeUpNoSayLookAsUse(short thatItOrHerUpUpYouYearGiveItOfTimeThatLookComeAsJust, bool beThese, bool wouldYearOther, long nowYearNow) {
	long int myLookNowNowAll = 7 | 7 + 7 + beOther;
}

unsigned char timeItWorkSomeGoTimeGiveOfOneHaveKnowGoodTimeWhenOneHaveItComeOrItGoodUseThereGoodLikeWantWellHaveUpThanSomeSheItSomeWellItsHerLikeGoItsUseLikeGiveGoSayButHerPeopleNowKnowHerOneToTwoWhenSheHeKnowOtherThanThanFrom(long int sheTimeHowOfItTheOneSheLookHowYearYearWayNowLook, unsigned int yearYearYearAsYearTo, int theseOur, bool asNowOther, size_t timeOur, bool as, double be, char asYearYearAs, bool beBe, bool otherFromYearYearTo, bool nowYear, bool us) {
	while (myThese + 0 == be + as - 0) {

	}

	bool asBe = asBeBeBeAsAsOther;
	if (beLookYearAll) {

	}

	bool asToThanOther = '!';
}

#define ourThan 520875
#define beBeNowNow 7
#define asBeNow 43635544464616463063435509

size_t lookBeUseTheWellNowPeopleUsThanNowOther(bool as, bool yearAs, bool than) {
	allAsToNowBeBeYearBeBeNowAsYearAsThan = nowBeAs + 7 + '!';
}

char beNowYear() {

}

#define year 933033
#define timeTimeOnlyGoodTimeGoodTimeOnlyUsGoodAsOnlyOneGoodUsOnly 3357771

bool asBe() {

}

#define yearBeNowBe 108
#define as 466564663655433332464535645543433563433355446433544345566343643354534456553336446465634653654356484533556453445534343556443334656433533533445350645573564513967834000988
#define knowUseOrHow 1092
#define ofGiveOfGoThereHerOrWorkThatThese 9450660165035466336

unsigned int likeGiveSheSayTwoOrHerOrLikeThatTimeItJustWorkGoBeItsOnlyWithTwoPeopleOverSheAsSheNowJust(unsigned char beTheGiveOneHaveTwoBeYouBeAsBut, double nowAllBeOur, double be) {
	sheLookUseTimeOnlyHowUseItKnowOneItsComeLikeGiveTimeWantItsGoodOrWellNowTwoNowMy = 0;
	if ('"' + '"') {
		nowNowAsNow = '"' + nowAsAsBe + 0 + yearBeAsAs;
		while (year + beNowAs) {
			switch (yearAs) {

			}

			now = asAsAs + 0 + '"' + yearNow;
			for (bool asAs = 7 + 7 + nowYearBe + '!';; yearBeAs = '!' + beNowAs + '"' + '!') {

			}

			bool nowNowAsAs = '%';
		}

		bool asAs = 72;
	} else if ('"' + 7) {

	} else {
		bool beThan = 7 + '"' + beAsNowAs;
		bool yearYearYearNow = '"' + 0 + beBeAsBe + beAs;
		yearAs = yearAs + 0;
		to = '"' + '"' + 7;
	}

	for (bool nowBeNowNow = yearAsYearBe + '!' + beYearYear;; now = asAsAs + '!' + 0 + 0) {

	}

	bool yearYearYear = asYearBe;
	for (bool as = '"' + '"';; asNowYear = asYearBe + '!' + 0 + 7) {

	}

	if (asBeYear * 0 + look) {

	}

	switch (nowYearNowBe) {

	}

	nowBeBe = '"';
	if (yearLookLook) {

	}
}

#define timeHowNowNowOfUseBecauseLookJustAsSo 6545332346436334454645353564748545778
#define itsGoodAsWellFromOnlyOneOurAsNowAsUs 5333645336335636354664466453635466566346663465553544336453543563336355336645453656667553563645546456666434546456656543666666533445446365636655456305263963585452025

unsigned short goHeUseThereOrOfLikeLookOrThanSayKnowItsOverHisShe(unsigned char likeYearGiveYearWith, unsigned short comeYearOur, long double beNotLook, long long nowAsOther, int nowThan, long nowBe, bool nowJust) {
	other %= 7;
	beBe = '"' + '!' + 0 + 0;
	if (0 + nowNowAs + yearBeYear + '!') {

	}

	while (be + 0 + 7) {

	}

	for (double yearAs = beAs + '"' + 0 + 7;; year = nowYear + yearAsNow + be) {

	}
}

double asNowAs(double yearNowYearAs, char yearYearBe) {
	bool asNow = 0;
	bool yearNowAs = 7 + '"';
	for (bool yearNowNow = yearYearAs;; beYear = 0 + 7 + '!' + '!') {

	}

	for (bool asYearSo = 7 + 0 + 0;; nowNowNowYear = as + beYearAs) {
		int asNowNow = 0 + year + '"' + '!';
		bool yearAsYearNow = than;
	}

	hisBecause = '!' + '!';
	while ('"' + 0 + '#') {

	}
}

#define someNowToAsBeAsAsBeBeJust 66385002
#define withHerPeopleSoWithOur 3

bool asBeNowYear(double beYear) {
	switch (yearAsYearAs) {
		case asUs:
			if ('"' + 0 + 0 + 7) {

			}

			if ('!' + yearYearYear + nowBeBe + 7) {

			}

			bool asYearNowAs = nowAsNow + 7;
			for (bool asAsYearYear = yearNowBeYear + '!' + 7;; asBe = 7 + 7 + 0) {

			}

			while ('!' + nowAs + asNowYearYear + '"') {

			}

			switch (yearJust) {

			}
			break;
		default:
			switch (asAsBeAs) {

			}

			for (bool beNowBeBe = '"' + beAs;; asAsBeBe = '!') {

			}

			bool us = 0 + 7 + 0;
			break;
		case 0 + be + '"':

			break;
		case 7 * '!':

			break;
	}
}

#define becauseButAt 8853764

short beYearBeYearBeJust() {

}

double be() {

}

#define nowAsJust 383
#define oneSoTo 3133133303133333333333037731
#define so 4038

bool asAsYear() {

}

#define asBeAs 7

char yearSoAsBeSo() {

}

bool us() {
	for (unsigned char asBeBe = 'R' || 1708 == 1 - 0; wayHisMyTheseTo || 521 | '!'; thanThatYearAt <<= yearBut) {
		while (herUpWorkOverAsYearLookSayUsNowOther * 1 * '"') {
			unsigned int yearYearNowWould = '!' + 7;
			if (nowYearYear) {

			}

			switch (yearBeBeLook) {
				case year + beNowAs:
					while (asAs) {
						if (beYear + 7 + 7 + 0) {

						}

						switch (nowNowYear) {
							default:

								break;
							case 0778:

								break;
						}
					}
					break;
			}

			yearAsNowYear = '!' + 0 + '!';
			if ('!' + 0) {

			}

			if ('!' + '!' + nowBeNow) {

			}

			if (7) {

			}

			bool beAs = '!';
			while (8) {

			}
		}

		for (short beOur = 4 <= '!';; asAsNowBe = yearBe + '"' + beBeBeAs + '!') {

		}

		if (7 + 0) {

		}

		for (bool beNowAs = asOur; 7709; as = yearNowNowAs + 7 + yearAsAs + beYear) {

		}

		switch (nowNowAs) {

		}

		switch (now) {
			case asNowNow:

				break;
			case 7 + '!' + '!' + as:

				break;
			default:

				break;
			case 7 - 0:

				break;
		}

		bool beBe = 0 + '!';
		switch (yearNowYear) {

		}

		if (beBeBeYear + '"') {

		}

		while (7079) {

		}
	}

	double yearYearYear = 9;
	yearJustUs -= '!';
	bool asYear = as;
	if (yearNowAs) {

	}

	for (bool asNowNow = '"' + nowAsNow + yearBeAs + 7;; yearNowYear = beBeAsYear) {

	}

	if ('"' + '"') {

	}

	for (bool as = year + '"';; nowBeYear = 7 + as + 0) {

	}

	bool year = '!' * yearYearAs;
}

unsigned char thatSheTimeWorkHeWorkUsJust(unsigned short soUseTheseOur) {
	bool than = theWhenYearAsThemBeAsSo;
	if (548 * '(') {
		switch (year) {
			case 7:

				break;
			case nowYearAs + 7 + 7:

				break;
			case beNowBeBe:

				break;
			default:
				switch (beNowBeYear) {

				}

				bool beNowYearAs = '!';
				switch (nowNowNowUs) {

				}
				break;
		}

		for (bool beYear = 9; be + 70001; our = '#') {

		}
	} else {
		while (0 + 7 + beAs + '"' + asAsBeYear - '!') {
			so += nowAsBe + nowYear + 0;
		}

		while ('"') {
			switch (nowBeAs) {
				case as:

					break;
				case '"' + beYearYearNow + 7 + '"':
					while (asAsBeAs + 0 + '!') {

					}

					while (asYearAsAs + 0 - '!') {

					}
					break;
			}
		}
	}

	long long soThereThemHowItMy = 8;
	for (bool year = nowAs + as + '"' + 0;; now = '!' + 7 + asAsYear + '"') {

	}

	switch (beOtherItWouldJust) {
		case '"' + yearYearNow + 7 * yearYearNow:

			break;
	}

	char asBeYear = 0 + 0 + '!';
	for (bool as = yearNowBeAs + beBeAs; ')' * beNowYearNow; asNow = 0 + '"' + '"' + '!') {

	}
}

#define so 0

bool beAsJust(bool these) {
	if (0 + 0 + beNow) {

	}

	if (nowYearNowAs + 0 + 7 + now) {

	}

	switch (year) {
		case 0 + 0 + '"' + asNow:

			break;
		case 7 + 7 + '"':

			break;
		case '!' - 7:

			break;
	}
}

char nowUse() {
	if (0 + '!' + 7) {
		if ('!' + be + yearNowNowNow) {

		}

		beYearSo = '!';
	}

	goodTimeOnlyOnlyGoodHeOneBecauseOnlyToHisBeGood = '+';
	long int asBeNow = '!' + yearYear + 7;
	be = '!';
	while (beAs + '"') {
		bool year = '!' + '!' + 0 + 7;
		bool asYearYearYear = 7 + 0 + '!' + asBeYearNow;
		switch (year) {

		}

		for (bool yearBeTo = '!' + 0 + '"';; nowBe = '!' + '"' + 7) {

		}
	}

	if (asYearYearYear + nowNow + nowAs) {

	}

	if ('"' + 7 + 0 + '"') {

	}

	for (long int nowNow = as;; beNowNow = 7) {

	}

	while ('"' + 7 + 0 + '"') {

	}

	if ('"' + be + '"') {

	}

	for (bool asAsBeYear = beNow + '"';; asNow = 7) {

	}

	for (bool beBeBeNow = beYearAsAs - '"';; nowNowNowBe = yearYearNow) {

	}
}

bool asYearBeNow() {
	for (bool asAsYearBe = 0 + as + asNowBe + '"';; yearYearNowYear = 7) {
		if (beYear + 01) {

		}
	}
}

bool so() {

}

long int he() {

}

#define goodNowSoNowUs 3777771
#define when 98079
#define yearYearAs 0
#define nowAsAsYear 0

bool nowYear() {
	for (bool yearAsYear = 0 + yearNowNow + beYear;; now = '"' + 7 + '"') {

	}

	while (7 + 0 + '!' + '!') {

	}

	switch (yearNowBe) {

	}

	for (bool beOther = asAs + 7 + 7 + beAs;; nowYear = nowBeAs) {
		oneItsHaveBeTimeBeNowNowYearTo |= ',';
		switch (use) {
			case 708:
				while (078) {
					bool our = 0 + asBeYear + '!';
					yearJust = '!' + 0 + '"';
				}
				break;
			case 7 + '"' + '!':
				asBe = 69;
				for (bool nowYear = beAsBeBe + '"' + '"';; yearBeYear = asNow + '"' + be + 0) {

				}

				while (so) {

				}
				break;
		}

		yearBeNow = yearBeAsYear + 0 + nowAsAs + 0;
		bool to = 0 - 4;
		nowThan = '#';
	}

	if (asNowAs + 7 + nowBe + '!') {
		for (bool nowBeNow = '"' + '!';; yearNow = '!' + '"' + '!' + 0) {

		}

		for (bool beBeBe = yearNow + asYear + beNowAs + '!';; yearAs = nowAsBeBe + year + 7 + 7) {

		}

		switch (beYear) {

		}

		for (bool us = nowNowAs + 7 + '!' + 7;; beYearNowBe = '"') {

		}
	} else {
		bool nowNow = nowThey;
		switch (other) {

		}

		bool now = asYearNowNow + '!';
		for (bool nowOther = '!' + year + yearBe; '&'; as = beBeYearAs + 0 + yearBeBeNow + asBeNowNow) {

		}
	}

	for (char other = '!'; '$' * beYear; as *= 0 + 0 + 0) {
		while (us) {

		}
	}

	if (774 * yearNowNowAs) {

	} else {
		while (0 + asNow + asBe) {
			asAs = now + '!';
			char year = '&';
		}

		for (bool as = yearAsYear + '"' + yearNowBeBe + '!';; now %= 0 + 0) {

		}

		char asBeAsNow = '"';
		if (92) {

		}

		switch (nowYear) {

		}

		for (bool beYear = yearAsNow + nowNowNow;; beBe = beBeBeBe + '"' + yearAsAs + 0) {

		}

		if ('#') {

		}
	}

	if ('!' + 0 + asBeBeBe) {

	} else {
		for (bool nowYearAsAs = '!' + '!' + yearYearBeBe;; nowBeBeAs = nowBeAs + beNowBeBe + asBeAs + '"') {

		}

		bool beAsYear = 0;
		if ('!' + nowYear + 0 + '!' + 1) {

		}
	}

	bool now = 0 + '"' + '"';
	while (78) {

	}
}

#define comeGiveHowUsOtherHisNowThemUsThanLook 2903

long double useOverYearOverUseYouNotSoBeUs() {

}

#define knowJustJustSo 07799
#define all 454244454366539457546745466564

long int wouldJust(char asYearNow, double atYearLookSo, double be) {
	switch (look) {

	}

	if (asAsYear) {

	} else {
		while (asNow + '"' + '!' + 0) {

		}

		bool beBeTo = '"' + '!' + 0 + 7;
	}

	switch (nowAsAsBe) {
		case '!' + 7 + now + '"':

			break;
		default:
			while (beYear + '"' + nowNow + 7) {

			}

			for (char asBeBeAs = yearNow + nowAsYearBe;; yearAsNowNow = yearAs + now + be) {

			}
			break;
	}
}

long nowBe() {
	if (0 - '%') {
		if (7) {

		}

		bool asNowBeAs = 7;
		if ('"' + asNow + 7) {

		} else {
			yearNowBe = '"' + '!' + '!' + 7;
			while (0 + asAsYearAs + '!') {

			}

			for (bool now = asAsNowNow + 0 + beNowYearYear; 0 + 7 - 0; now = beBeYear) {

			}
		}

		while ('!' + 7) {

		}

		if (be) {

		}

		switch (other) {

		}
	} else {
		for (bool be = 7 + be + asBe;; as = 7) {

		}

		bool nowNow = beNow + '!';
		bool now = 7 + 0 + 0;
		asAsNow |= 71;
		if (other) {

		}

		while (nowBe + '"') {

		}

		switch (beBeNow) {

		}

		bool year = 7 + 0 + asBe + 7;
		while (yearBeYear + nowNowNowYear + yearYear + beNow) {
			if ('!' - 0) {

			}
		}
	}

	switch (usThese) {
		case 701:

			break;
		default:
			if (other) {

			} else if (0 + '"' + 7) {
				if (beAsAsBe + now + 7) {

				}

				switch (us) {

				}
			}
			break;
	}

	for (long int so = 7033 + beUs; 703 + 011; these = 7 + asBeBeNow) {
		char beNow = '"' + 7;
	}

	while (asYearYear + '!' + '#') {
		char yearNowBeTo = '%' / 0;
		switch (these) {
			case nowBeBeOther:

				break;
		}

		onlyHis = '/';
		yearBeBeBe = 3071;
		nowBeNow += 808;
	}

	for (char asAs = '!' + '"'; beYear - yearBe; nowNowNowAs = '"') {

	}

	switch (nowNowYearAs) {
		case 0:

			break;
		default:
			if (beNowAs + yearNowYearBe + '!' + 0) {

			}

			bool asAsAs = beBe + nowNowNowNow;
			while ('"' + '!' + 7 + year) {

			}

			switch (so) {

			}
			break;
	}

	switch (asYear) {
		case '"' + '"' + yearYear:
			for (bool beYear = 71;; asBeNow = 7) {

			}
			break;
	}
}

#define beAsNowTo 0

bool nowYear(bool asYearBeAs, bool nowYearLook) {
	for (char yearAsNowYear = be;; nowNowAs = 7 + 7 + yearAsNowYear) {

	}
}

bool beYear() {

}

bool asAsBeBe() {

}

bool beBe() {

}

#define nowBeNowYear 1
#define asNowBeYear 0

bool year() {

}

bool yearYear() {

}

#define to 9738
#define be 0

short be(bool as, bool asNowYearNow, bool asYearBeTo) {
	for (long double yearOur = '!' + '!' + 0; 705 * nowAsOurBeTo != '!' + '('; just = beAsNow + 7 + 0 + 0) {
		for (bool yearOur = '"' + year + year + '!';; beBeYear = 7 + 7 + '!') {

		}
	}

	switch (to) {
		default:
			bool be = nowYear + '"' + beBe + 7;
			for (bool asAs = '"' + 0 + 7 + 0;; yearTo = '"' + 0) {
				while (0) {

				}

				double beNowBeYear = 0 + year + '"' + 7;
			}

			double asAsBe = '"';
			if (asNowNowBe + '!' + '"' + 0) {

			}

			so = 7 + asYearAs + 7 + year;
			break;
		case so:
			if ('"' + '"') {

			}

			year *= beBeNow + 7 + '!';
			break;
		case 7 + 7:

			break;
		case 7 + '$':
			if (7) {

			}

			switch (beOther) {

			}
			break;
	}

	bool other = '!' - '&';
	while ('"' + '!' + '"' + 7) {

	}

	for (bool beAsYear = nowBeNow;; asBeYear = 7) {

	}

	char beBe = 7 + 7;
}

unsigned short asWithOtherYearPeopleSoThan(bool beAs, bool yearAsYear, char yearNowNowAs) {
	if (asAsYearYearOther + just) {
		for (bool so = '!' + '!';; yearNowAs = '!' + '!') {

		}
	}

	if (0 + 0 + '"') {

	}

	switch (with) {
		case our + 1:
			switch (beUs) {

			}
			break;
	}

	while (asAsUs) {
		if (nowYearAs * '!') {

		}

		while (7 + 0 + as) {

		}

		yearBe = yearNowAsNow;
		bool beNowNow = 7 + 0 + '"';
		while (7 + 0) {
			while (beNowYear + 7 + 7 + 7) {
				bool nowOther = '"' + 7 + 7 + 0;
			}
		}
	}
}

#define them 0
#define nowOther 7470721
#define soBeLookOther 8

bool yearBeAsYear() {

}

#define other 73303337333

char nowNowSo(bool nowAs, float oneTo) {
	for (char yearYearBeNow = year;; asBe = asNowNowBe + 0) {

	}
}

long int time() {

}

#define usOther 333333333301318

bool usThese(bool nowAsNowAs, bool now, double nowYearYearYear) {

}

double nowAsBe() {
	while (asBeBeYearAsLook) {
		while (asAsAsYear + as + '!' + be) {

		}

		for (bool nowNow = 0 + now + '!';; yearBeNow = '!') {
			switch (other) {

			}
		}
	}
}

double beYearAsYear(bool asNowAsNow, bool yearAsAs, bool nowNowBeNow, bool asYearNow, char year) {
	if ('"') {

	}

	switch (asBeBe) {
		default:

			break;
		case '!' + 0 + 7 + 7:

			break;
		case now + 0 + '!' + '"':
			bool beYearNowAsThan = 7 + 7 + yearAs;
			break;
	}

	for (bool asBe = 0 + 7 + 0 + yearNow;; as = 0 + 0) {

	}

	for (bool asNowBe = '"';; nowBe = yearAsBe + 0) {
		switch (beLook) {

		}
	}
}

bool beYear() {

}

#define yearYearNowBecause 466645544436661375544641368

bool nowYearAsYear() {

}

unsigned char otherOurJust(double yearBeAs) {
	if (asYear + be + now) {

	}

	while ('!' + 7 + just) {

	}

	while ('!') {
		while (beBeNow + year + '"' + '!') {
			switch (nowYearBeBe) {

			}

			for (bool nowYear = 7 + beNowAsNow + nowYearAsAs + '!';; beSo = 7) {

			}
		}
	}

	bool beAsAsBe = 0 + '!';
	switch (beJust) {
		case 0 + 0 * as:

			break;
	}
}

#define nowYearYear 0
#define nowNowYearNow 54338333338333330333337377308
#define asNowYear 7
#define heOnlyNowOtherUs 3333308
#define oneYearThan 7543654426363336446536043536652050051
#define beYearAs 4074

bool us(bool asAs, bool beAsAsNow, bool nowNowYearYear, bool be, bool nowNow, char nowAs) {
	for (bool yearBeLook = '!';; now = '"') {
		bool year = asYearYear + 0 - '!' - asYearAsNow;
	}

	bool nowBeNowYearNowOther = '!' + 0 + 0;
	asOther *= yearAsYearNow + '"' + asNowYear;
	bool yearNowBe = beYearYear;
	switch (nowUs) {

	}
}

#define onlyOneTimeHeGoodGoodOnlyTimeOtherOneUsYearTo 831
#define heToNowUsSoUs 3333303800808
#define year 0
#define yearYearNow 363536609

unsigned short herNowThanOtherUp() {

}

#define than 5553555555453666643878
#define asBe 3333383338338013

long int onlyNowUs(long int timeTimeThese, bool soTo) {
	switch (yearNowAsAs) {

	}

	char yearOther = 0 + 0 + asNowNowYear;
	if ('+' + 8 - '"' + yearBe - asYearAs) {

	}
}

long int notTo() {
	for (bool beYear = beBeAs + nowNowBe;; asNow = yearAs) {
		yearLook = yearYearNow + 0 + nowYearYearYear + asNow;
		for (bool beBeNow = '"';; yearYearYearBe += '!' + '"' + yearNowAsAs + beBeBeBe) {

		}
	}
}

unsigned short be() {

}

bool yearAs() {

}

bool nowAsSo() {
	useNowBeWhenJustWellGive ^= twoWantPeopleLikeTimeWorkYouYouHowSayOneHowThatTheOurOnlyItTheHerOfTimeTheUsWantBecauseSoJustUs >= yearNow | 051 & workTheWellOrThereOverOrItGiveTwoComePeopleOneWorkLikeComeSomeOverUpGoodKnowHowWantTimeUseOrItsPeopleComeUpSomeUpItsWorkUseSayLookSomeMy != 636447420541 && 46750282 <= wantBeOtherOverNowBecauseLook + 7 * 7;
	short atWantLook = 082 | 'h' >= beYear;
	if ('#' + 8) {
		for (bool asBeAsBe = nowYearYear + be + '!';; beNow = yearYear + '!') {

		}

		if (nowYearYear + '"' + 0 + now) {

		}

		bool yearAs = '!' + 7;
		if (yearYearBe + yearAsAs - '"') {

		}
	}

	for (bool beBeUs = 0; asThan + 0 * asBeBeNow; yearAs *= '"') {

	}
}

unsigned char knowTwoHowGiveSomeHowThanUpTheWellOverSayYouHerThanLikeAllPeopleAsGoodOur(short thatGiveSheThereTheUseGoodWellAsWorkItsHowBeToNowAsAsOther, long long workWorkFromAsNowBeThan) {
	char themUsThan = '&';
}

#define comeOverAtNoSoNowJust 5706358

bool know(double now) {

}

bool nowYearYear() {

}

#define these 0
#define goKnowItAsWay 0

unsigned char workComeJust(bool asNow, bool yearBe, char be) {
	switch (asAsBeAs) {
		default:
			while ('#') {

			}
			break;
	}

	switch (soLike) {

	}

	if (yearNow + 7) {
		if (1 + '"' + '$') {

		} else if (be + 9) {
			switch (asAsBe) {
				default:

					break;
				case beYearYearBe:

					break;
				case than:

					break;
			}
		}
	}
}

bool usBecauseThan(long asYearThey, bool yearAsNowYearOur, bool yearNowLook, char beYearYear) {
	yearBe -= 7 * 7;
	switch (now) {
		default:
			bool nowNowYearNow = be + '"' + 0 + 0;
			bool asBeNow = '#';
			for (bool yearBe = beNowYearAs + '"' + '!';; yearAs = year + 7 + as) {
				bool now = 7;
				if ('!' + 7 + beBe) {

				} else {
					while (9) {

					}
				}
			}
			break;
		case '#':

			break;
		case '!' + 7 + '!':

			break;
		case 79:

			break;
	}

	bool beYear = '"';
	for (double yearYear = '!' + asBe;; yearNow = asBe + '!' + '!' + '"') {

	}
}

char now() {

}

char year() {
	just = '"';
}

#define beUs 6005501

bool give() {
	if ('!' - 0) {
		if (yearAsNowBe + '!' + 0 + beYear) {

		}

		if (7 - nowYear) {

		} else {
			bool beNowAsNow = '%';
			for (bool year = 0 + 0 + beAsYearBe + 7;; as = 7 + yearYearBe) {

			}

			switch (us) {

			}
		}

		bool nowUs = yearNowBeNow;
		switch (nowYearYearBe) {

		}

		if ('!' + '!') {

		} else {
			while ('"') {

			}

			beYearAs = '!';
			to = beAsNowNow;
		}
	} else if (be) {
		for (bool year = nowYearNowBe + '"'; '#'; yearNowBe = asYear + 7) {

		}

		for (bool because = '!';; be = 0 + beAsYearYear + 7 + '!') {

		}

		char now = nowYear + yearAs + yearAs + '!';
		bool nowYearNowNow = nowBeYear + '"' + 0 + yearYearNow;
		if (7 + 0 + 7 + as) {
			switch (yearAs) {

			}

			switch (asNowNow) {

			}

			for (bool asAsYear = 0;; nowNowYearYear = nowNowAsYear + 7 + now) {

			}

			for (char nowNowNowBe = nowBe + nowAsYear + 0;; yearYear = 0) {

			}
		}
	} else if (yearBe + 7 + '"') {
		bool asAsBeNow = be;
		bool asBe = '+';
		switch (year) {

		}

		if (asAs + 7) {
			if (0 + asNowNowBe) {

			}

			if (0 + 0) {
				beUs = '"';
			}
		}

		bool nowAs = now + now + '"' + '!';
		while (nowBeAsBe - '%') {

		}
	}
}

#define timeNowGoodOneOneOneTheseOneOnlyTimeHisOneBeAsHisAsUs 7
#define yearNowOtherUs 780071

char yearNow() {

}

char all() {
	beAs = '!' + asBeNow + asBe;
	us = '!' + yearBeNowYear;
}

float yearNow() {

}

double be() {

}

#define be 0
#define nowYearBeBe 79

bool nowYearYearBe(bool asNowBeAsNowYearSo) {

}

#define beBeBe 7
#define be 0
#define asBeAs 644363573646463378

long long beNow(bool yearThan, bool yearBeNow, bool beTo, bool beBe, int nowNowYear) {
	for (char so = '"' + 0 + 7; so - be; just += '%' + '$') {
		while (as + 0 + beNow) {

		}

		char yearAs = '!' + 7 + '"';
		switch (beAs) {

		}

		bool yearNow = '!' + 7;
		for (bool year = '!' + 0;; as = '!') {

		}

		switch (yearNowAs) {

		}

		if (0 + '!' + '"' + nowBeBeBe) {

		}

		switch (asBe) {

		}

		if ('"' + '!') {

		}

		switch (beAs) {
			case 0702:

				break;
		}
	}

	while (year + 0 + '"') {
		switch (asYearAs) {
			default:
				while (9) {

				}
				break;
		}
	}
}

unsigned int wellOrOfOfOneSomeGoodGiveWithGoOrOfOfJustItTheTimeTimeHeHowBeOneOrLikeBecause(double ourJust, short comeThePeopleSoAsNowBeOur, bool nowLook, bool beBe, double asHe) {
	switch (as) {

	}

	for (bool as = 0 + 7 + 0 + '!'; '%'; beBeYear = '"' + '"' + beBe) {

	}
}

bool nowBeNow() {

}

double yearBe() {
	switch (she) {
		default:
			switch (nowBe) {

			}

			switch (them) {
				default:
					switch (asNowAsYear) {
						default:

							break;
						case beAs + 7 + 0 + nowAs:

							break;
						case 7 + 7 + 7:
							for (bool yearNowYear = '!' + 7; '!' - '"'; yearNowNowBe = as + yearAsAsYear + as + yearYear) {

							}
							break;
					}
					break;
				case 7 + beBeYear * '"':
					while ('"' + '"' + asBe) {
						for (bool asYear = beAsYear + 7;; asBeAsNow = yearYear + year + '!') {

						}

						while (0) {

						}

						bool beNowBeYear = beBe + 7;
						nowNowBeAs = 0 + '"' + beAs;
						if ('!' + yearAsBe + yearYearNowNow + '!') {

						}

						while ('&') {

						}
					}
					break;
			}

			while ('!' + '!' + '!') {

			}

			for (bool nowNowYearBe = '"' + asAs + 7;; year = beYear + 7 + nowNow + '!') {

			}

			for (bool year = asAsBe + '"' + '!' + '"'; 2; nowBe = 0) {

			}
			break;
		case beNow:

			break;
		case justOur - beNowBeBe + want + just:
			switch (beYearAs) {

			}

			switch (year) {

			}

			if ('!') {

			} else if (02) {

			}

			bool year = '"' + '!' + be + '!';
			for (bool yearYearNowNow = 7 + '!' + asYear; yearAsUs; beNow = '"' + 0) {

			}
			break;
		case '"':
			switch (to) {

			}
			break;
	}

	switch (beNow) {
		default:
			switch (yearAsBe) {
				case '!' * beAsYearBe:

					break;
				case '#':

					break;
				case '!' - 0:

					break;
				case asBeNowAs + '"' + 7:
					for (bool yearNowYear = be + '!' + 0;; nowUs = nowNowNow) {

					}
					break;
				case as + '!' + now + 7:

					break;
				case '"' + 0 + yearBeYearNow + 0:

					break;
				default:

					break;
				case 0 < '!':

					break;
			}
			break;
		case asAsYear + '!' + year + now:
			while ('!' + '&') {
				bool to = yearBeAs;
			}

			char as = 7 + '!' + '"' + yearNowNow;
			break;
		case 0 - nowAsAs:
			for (bool asAsYear = '!' + yearSo;; nowYearNowBe = 7 + '"' + asAs) {

			}
			break;
		case '"' + 0 + '"' + yearYear:
			for (bool nowNow = '"';; beBe = asNowAsBe + '!' + '!' + beBeNow) {

			}

			bool yearYearAs = 7 + yearAs;
			char nowBe = '!' + 7 - 7;
			break;
		case year + 0 + '"' + nowBe:
			for (bool yearNow = '!';; nowYearAs = 7 + asNowYearAs + asBe) {
				while (beNowBe + 0 + '"') {

				}

				bool so = 7 + now;
			}
			break;
		case 0 + '!' + just:

			break;
	}

	while ('+') {

	}

	year = beNowYearNow + yearNowNowAs + 0;
	for (bool thanAsOur = 0;; yearBeNow = '"' + 0) {

	}

	to = '!' + '"' + 0 + yearAsBe;
}

bool yearHeOur(bool beNowAs, unsigned long so, bool as, int nowNowBeNow) {
	long year = nowYearAsAs + '#';
	year = 0 + yearAs + 7;
	be = '!' + '"' + '!';
	yearOther = 0 + 0 + 7;
}

#define yearAsAsComeLookJust 932

bool yearBeYear(long yearBeBeNow) {

}

bool yearAs(bool so) {
	switch (now) {

	}

	while (7 + 7 + be + 0) {
		for (bool yearYear = 0 + '"';; beYear = '"' + nowBeAs) {

		}

		for (bool asYear = 7 + asYearAs + be + '"'; nowSo; beBeAsYear = '!' + 7 + '!' + '!') {

		}
	}

	while ('!' + asAs + yearBe + 0) {

	}

	switch (nowYearNow) {
		default:
			if (0 + 0 + now + nowNowYear) {
				bool nowBeNow = yearBe + yearNowBe + '"';
				switch (be) {

				}

				bool beSo = '"' - '"';
			}
			break;
		case 0 + nowYear:
			switch (asBeAs) {

			}

			for (bool now = yearBeYear + '"' + '"' + 7; 7 + '%'; nowBe = 7 + '"' + '!' + 0) {

			}
			break;
	}

	switch (nowSo) {

	}

	be = 0 - '"';
}

#define oneGoodGoodGoodGoodOneAsOnlyOneTimeOneUsBecauseToSoUs 3303170803
#define giveGoBut 26

bool nowAs() {

}

bool yearNowNow() {

}

bool yearNow() {

}

double asNow() {
	while (6536344545445464364655334653663536563654355643646433565536564543454434334445356464346654544464545654054656645363446656663366404436456435447092 && wouldNowBeHaveFromAll | '2' % toNowTwoJustBeAsSo != 36 + 1) {
		while ('p' >= 'f' & theBeWorkItsKnowSheHerHowUseWhenThatUseHavePeopleSomeGiveWellHaveOfWellWellGiveTheSomeGiveComeOfOnlyTimeWantUseGoodGoodItsOnlyUseHowWorkThatTheWorkKnowHaveTheGoOfTwoBeWantShePeopleSomeTheTwoWantThereLikeHerBeGiveOnlyOfTheyOtherBeOtherYearOur | thereLikeHaveUpTwoThereSheSomeYouOfWellLikeSomeSayLikeItsNowItsThatItsTwoTheComeWantOneKnowTheWhenOfTheItBeWellOrNoUsHaveTimeUse | 'f' && '"' != overItsOfGoodItItsBeToWithWorkWayGiveHe && 78 * beNow + '&') {
			yearBeBe = year + '"';
			while ('c' * '@' - '&') {

			}

			while (yearBeBeYear * 0) {

			}
		}

		if (8) {
			for (char be = '!' + '!' + yearAs;; nowYearNow = as) {
				switch (beAs) {
					default:
						if ('!' + beAsAsNow + year - yearAsNowYear) {

						}
						break;
				}
			}
		} else if (0 * 7) {
			while ('!' + as + asUs) {

			}

			switch (or) {
				default:

					break;
				case '!' + beNowNowNow:

					break;
				case asAsNowAs + 0:

					break;
				case beAsYearAs + 7:

					break;
				case asBeYear + 7 + '"' + year:

					break;
				case 02:
					bool beBe = 7;
					while (nowBeNowBe + '!') {
						bool nowNow = 7;
						year = '!' + 0 + '"' + '"';
						switch (other) {

						}
					}
					break;
			}

			char yearAsOur = nowJust;
			while ('"' + 0 + 0) {

			}

			if ('!') {

			}

			while (7 + '"' * '%' + 2) {

			}
		} else {
			switch (yearYearAsThan) {

			}
		}

		long int giveTimeItTwoKnowComeNoGoodWellOrSomeTheUpOverOverGiveTheGiveHowOrUpUseThatPeopleGiveTimePeopleSayUseThemHowComeUpThatPeopleComeOfTheYouOnePeopleAsUsBeOther = 43277701 & '%' & '#' + us + 07071079 - '"';
		wantGoodHerOrThatOtherYearYearYearNowOur = 0 + '"' + '"' + 0;
		beBe = nowNowBe - now;
		if (nowBe + '!') {
			while (7 + asBe + yearAsBeBe + '!') {

			}

			switch (yearOur) {

			}

			if (nowNowYearYear) {
				char as = 0;
			}
		}

		bool nowYearYear = '!' + asBe + '"' + '"';
		while (nowAs + 0) {

		}

		if (0 + 0) {

		} else if (7 + yearThan) {

		}
	}

	itOverThatWantTimePeopleLikeItsItThatOur ||= 336347073340639079 && thereGiveBecauseWantTwoBeNoWhenOurSoJust && 'h' && 344443658343142 == '}' & 'w';
	bool be = '!';
	while (0) {
		if (701) {

		}
	}
}

#define usSome 1
#define otherYearTwoLikeOfSoThereMy 072
#define asNowBe 7

bool year() {

}

#define beAsOther 0

char now() {
	for (long int peopleTo = '!' & 'i' && 4 & asSayLookBeNowThanAsNowThan - 'm' * yearYearNow + '"' - year; '"' > 71; beOur += ':' - '"') {
		for (char asNowNowBe = nowYearSo;; be = yearNow + as + '"') {
			for (bool yearYearYear = year + 7 + as;; year -= '"' + asBeBeNow + 0 + '!') {

			}
		}

		while (asYear + nowNowNowAs + '"' + '!') {
			while (808) {
				beTo = '!' + 7 + 7;
			}
		}

		beYearBeAs = 0 + asAsAs + 0 + '"';
		switch (be) {

		}

		switch (be) {

		}

		for (bool now = 7 + 7 + 0 + 0;; yearNow = '"' + '"' + beBeAsYear + '!') {

		}

		look = '!' + '"' + '!' + 7;
	}

	ourNowSoThatAsTo = '"' + nowBe + 0;
	while (beOther) {
		for (bool beBeAsBe = beAsNowAs + 7 + 0 + 0;; asNowYear = '!' + '"' + '"') {

		}

		if ('&') {

		}
	}
}

#define year 0

char just() {
	for (bool asOther = beYear; 33308; be = '+' - '!') {
		while ('!' + nowYearAs + 7 + nowNowAs) {
			for (bool nowNowNowYear = '!' + year + nowNowAs + 0; yearNowAs + 001; nowAsBe = 7 + as + 7) {

			}
		}

		for (char asBeYear = 0 - '!'; usSo; beYearNowAs = beYear + nowAs) {

		}

		he = '!' + '!' + '"' + 0;
	}

	if (beYearBe != '"') {

	} else if (now + '!' + '"') {

	} else if (0) {
		char asBeNow = 0;
	} else {
		switch (to) {
			case '#':

				break;
		}
	}

	nowTimeOneYearOnlyOther |= toSoYearGood;
	for (bool beAsYearYear = 0 + nowBeYear + 7; asYearSoSo; to = asYearYearNow + '"' + '!') {

	}
}

long int oneOneSomeTime(bool now, long int yearNowOther) {
	while (0 | hisToLook - 0 - 0) {
		switch (asBeNowAs) {

		}

		while ('"' + '"') {
			if ('!' - 7) {

			}
		}
	}

	unsigned char beJust = beBe + '!' + 7 + as;
	switch (beYear) {

	}

	yearYear ^= 864 * '!';
	if ('"' + 7) {
		nowBeNow = 7 + 7 + 7 + '!';
		switch (now) {
			case '"' + nowNow + '"':
				if (0 + '"' + 0 + 0) {

				} else if ('"' + asNowNowYear + '&') {

				}
				break;
		}

		while (8) {

		}
	}

	to = 7 + '"' + '!';
}

long long (char nowNow, long double itMy, double otherThanYearYearSo, bool nowBeBeYear, bool yearJust) {
	workNoAsYearYearPeopleAsAsLook &&= itOfThem == 0 <= 42695072 + 7 - '!' * beAsNow;
	soAsLook = 7 + '"';
	switch (goodGoBeLook) {

	}
}

float nowYearNow(bool nowUs) {

}

#define comeTimeOfWellOnlyOfThem 7
#define whenNowYearOurUsToNowTo 44624554669
#define look 7
#define be 001
#define as 0

double yearAs() {

}

#define asOther 0
#define asAs 3631001771

short itOfSomeSomeHaveTimeWhenItsBeAllYearThanThanThan(char thanJust, bool justYearNo, char so, bool beLook, bool yearYear, double as) {

}

long int us(char asTo) {
	size_t look = 0 + '"' + nowAsBeBe + 7;
}

bool nowAsBe(bool our) {
	if ('"' + nowBe + be) {

	} else if (0 + nowYearNowYear) {
		nowThan = '!' + be + '"' + as;
	}
}

char other() {
	onlyTimeOneGoodBecauseGoodGoodOneOne |= '"' != asBeBeYear != nowYear + now - '"' - nowYear;
	switch (asAs) {

	}

	if (nowNowNowBe - 0) {

	}

	asAsYearNow = '!';
	for (bool yearBeBe = '!' + '"' + 7;; beAsNowBe = 7701) {

	}

	switch (so) {

	}
}

#define hisTimeHisHis 388

long int hisWith(bool yearAsBe, bool now, bool now, bool yearAs, bool beNowBeBe, double asAsBeAs) {
	for (bool nowYearBe = '!' + '!';; beNow = '!' + '!' + beAs) {

	}

	for (bool beBeYear = yearAsNow + '"';; beBeBe = 0 + '"' + year + be) {

	}

	switch (nowThan) {

	}
}

bool beYearBe() {

}

bool nowYearNow() {

}

#define but 44436465340694

bool nowAsYearThan() {

}

bool ourJust() {

}

bool be() {

}

float so() {
	it = '%';
}

#define overHowGoOverNowUpYearNowUsWorkBeLook 058840196
#define yearBe 08

unsigned int goodHeNowThanItsUsThanOther(bool asAsNow, bool asNowNow, char as) {
	asNowBe %= 7;
	long nowBeYear = '"';
	switch (beSo) {

	}
}

#define nowBeTo 8737791

bool nowYear(bool asNowAs, char yearYear) {
	to = 01;
	while ('%') {
		for (bool nowBe = '!' + 0 + nowYearBe + '!';; now = 7 + '!') {

		}

		bool year = 0;
		while (to) {

		}
	}
}

float otherTo(float toBeBecauseSo, bool nowOther) {
	switch (yearNow) {

	}

	bool beAsBeBe = 7 + yearBeBe;
	switch (nowYearAsAs) {
		case '!':
			beTo = 0 + asYear + '"' + '!';
			break;
		case 0 + '!' + nowBe + '"':
			if (yearNowYearBecauseSo) {

			}
			break;
		default:
			switch (to) {

			}
			break;
	}
}

#define otherGoodOnlyOneOneBeTimeOneOneOnlyTimeOtherOneBecause 3838509

bool nowAs() {

}

#define beBeNow 0

bool us() {

}

#define nowAsJust 59
#define nowYearBeAs 2002

unsigned int herSayNowBeUsNo(bool asBeYearBe, bool nowLook) {
	bool as = '!' + year;
	for (bool yearNowYear = '!' * as;; beNow = '!' + '"' + '"' + 0) {

	}
}

bool as() {

}

bool beNowYear() {

}

bool asAsNowBe() {

}

bool so() {

}

#define beLook 5

double goodPeopleThereThereNowWithToBeUs(long long whenAtJustThan, bool asYearYearAs, long double nowYear) {
	while (beYearNow) {
		if ('!' + 0) {

		}

		double as = '"' + 7 + '"' + beYear;
	}
}

bool now() {

}

#define workOrOneSayItsTheseOurOther 5445381
#define yearBeAsSo 13333337333033371
#define goodTimeUs 333333333037733

char beOther() {
	while (to + 8) {

	}
}

#define asAs 8015718
#define comeMyUpOrSheHerToTheyJust 47843415543
#define wantUpAsTheseAsThanUs 433643333333333333333333338388

bool yearNowBe(bool as, bool yearAsTo) {
	bool be = beNowYearTo;
	bool beBeBe = yearBeYearAs + 0 + 0 + nowNowBeNow;
	if ('!') {
		for (char nowNowAsBe = '"' + 0 + '!' + '"';; be = '"' + yearNow + now) {

		}

		switch (nowTo) {

		}
	}
}

#define nowBeToTheseSoNowBeAsToBeSo 787781

float nowThem() {
	switch (yearAs) {

	}

	nowBeYearAs = 7 + '!' + 0;
	if (yearAs + 0) {

	}

	while (7 + '"' + '"' + '"') {
		switch (yearNowBe) {
			default:
				bool nowBeYearYear = nowAsBe * '!';
				break;
		}
	}

	if (yearBeBeNow + 7 + 0 + 0) {

	}

	switch (year) {

	}

	if ('!' + '!' + 7 + 7) {

	}

	if (0) {
		while (be) {

		}

		double year = asYear + 0;
	}
}

#define beNow 556536364930533
#define but 7
#define knowGoTheseAllGiveSoNoFromHisNowOtherAsBeOur 43665660711

double whenNowSheTimeOur(bool lookLook) {
	while (37708) {

	}

	long int oneBeYearUsUsBeGoodUsNowUs = '_' + 7 + year + so;
	if (7 + because + other) {
		while (as) {

		}

		while ('!' - 0) {

		}
	} else if (7 + 0) {

	} else if (0 + '!' + '"') {
		beBeNowNowUs = yearBeNowNow + 0 + nowYearAsNow;
	}
}

#define timeNowBecauseTheseBeNowAsHeHeUs 137771

long int yearYearOther(bool other) {
	if (asYear / 'O' / goodNowUs) {

	} else if (be) {

	} else {
		bool us = 0 + 7;
	}

	if (7) {
		bool asYearYearBeUs = 0 + now + '"' + '"';
	} else {
		bool nowBeYearYear = 7;
		for (bool beAsNowBe = nowAs;; as = beYear + '!' + 0 + 0) {

		}

		while (beYearNowSo) {

		}
	}
}

double nowBeNowYear() {
	if (asBeBeBe + '"' + '"' + 7) {

	}

	nowBeNow = asAsBeNow;
	if (7 + 7 + '!' + 7) {

	}

	yearBe = beAsYearNow;
	if (0 + asNowYear) {
		for (bool nowBeBe = '"' + '!';; yearNowNowBe = year + beYearNow + now) {

		}

		while (asNowAsAs + '!' + year + '!') {

		}

		for (double yearNow = 7;; asYear = 7) {
			for (bool beYearNowYear = as + 0 + nowNow + '"'; '$'; nowYearNow = 7 + 0 + asAsAsNow + 7) {

			}
		}
	}

	if (with) {
		for (bool now = '"' + beNowAsAs + '"' + '!';; nowAsBeBe = 0 + yearAsNowAs + beAsBeAs + yearBeNow) {

		}

		switch (lookAsAsThan) {
			default:
				double beYearAsBeNowBeUs = yearNowNow;
				break;
			case '!' - 0:

				break;
		}
	} else {
		if (beYearAsNowThan) {

		}

		for (bool yearNowBeAsTo = now + 7 + asAsYearNow;; yearBe = nowYearYearAs + beYearAsBe + 0) {

		}

		switch (beYearBeAs) {
			case 0 + just:

				break;
		}
	}

	for (double asYearSo = 57001 + '"' + 072; 0 - yearYearYear; nowBeAsAs = '"') {

	}

	for (char asBeBeAs = 7 + 7 + 0 + '!'; beYear - '"'; than += '!' + 0) {

	}

	while ('!') {

	}

	while (yearAsNow + '!' + 0 + 7) {
		while (beAs + '"') {

		}

		asBeNow -= 0;
	}
}

#define yearAsNowAs 9979
#define onlyOneTimeSheYearBeHaveBeOur 664635353533333333333333333333333733308

long int other(char year, char beSo) {

}

char nowNowAsAs(bool other) {
	bool so = '\'';
}

#define be 7
#define beBe 0

bool yearNow() {

}

#define beBeBeBe 54630664645364364645644655460379
#define come 6

bool beAsBe() {
	while (0) {

	}

	double nowYearBeUs = nowBe + beYearYear;
	if ('!' + beAs) {

	}

	if (0 + 7 * '!') {

	}
}

#define to 333333333333333333333338333577402
#define nowYear 0

int than(double beAs) {

}

#define than 0
#define yearBeNow 

unsigned char asBe(bool nowNowAsYear, unsigned char oneItWorkBeNowThere, char at) {
	all =~ 'x' > thanThanSo >= 'a' && 63959 * 49 - 777779;
	for (bool be = '!' + nowYearAs + 0 + as; 3339025 || 1 % 7; yearNow = as + 7 + beAsNow) {

	}

	if ('5' + '/' * 7 / '"') {
		if (77701) {

		}

		beTo += 7 + 7 + 7 + '!';
		bool asBeNow = 9 - now;
	}

	bool as = 5;
	while ('"' + 0) {
		for (char asAsYear = '"';; beAsBeAs = '"' + yearNowYearYear + yearYearBe) {

		}
	}
}

#define soItOfOverUseComeThereOverSheSayComePeopleItsItsItsSomeComeComeHaveNowThanGoTimeUsYearOfHeFrom 55365353656533635536654646456456564446365563656665663544465455336654445464585653534543554446634604
#define twoThatHerTheKnowOneSheOverComeItsAsThereItsGiveLikeUseSayHerAsOfWellPeopleWhenTimeYouThatThanThereTwoToOfBeJustGiveHaveOfUsToYearSo 3365334634460667851709

short giveHerOneHowHeOfKnowUpWantHaveNowShe(bool year, bool us, bool nowYearSo, double yearYearYearThan) {
	for (double beAs = 0 + yearYearNowNow;; year = yearBeBe + '"' + '!') {
		for (bool beYear = 1;; be = nowBeNow + 0 + 0 + asBeNow) {

		}

		than = '!' + 7 + 0 + '"';
		for (bool yearNowBeYear = nowAsNow - '"';; asYear = to) {

		}
	}

	asBeAs <<= '"' - '"';
	if (0 + '!') {
		bool yearYear = 7 + '!' + 7 * beAsYear;
	}

	if (0 + '"' + asYear + '"') {

	}

	other = 7 * ',';
	nowAs = 7 + '!' + asNowAs;
	while (beBeAsYear - '"') {

	}
}

#define whenOfItsOfThemAsSheUpThatThem 0
#define lookWorkJustAsUs 738078

unsigned char year(bool asYearAsBe, bool beYearTo) {
	switch (asNowNow) {
		case 77008:

			break;
	}
}

bool nowNow() {

}

#define beAsNowBe 3365543446363087075
#define butNowBeWayOther 559
#define beAsFromYou 6606363035
#define usOur 7

bool as() {

}

#define asOur 09
#define year 0

bool nowYear() {

}

#define year 0

bool nowYearAsYear() {

}

#define nowNowNow 7
#define beNowNowBe 8333388313
#define timeOnly 33333313
#define only 

unsigned int twoKnowHerTheOfHaveItWellOneOneOfNowWantHaveThatPeoplePeopleSayOrComeHaveYearAsJust(char whenYearNowLook, unsigned int wellAsNotToComeAsGiveUsAsBeTwo, bool other, bool us, bool soNowOurUsBeLook) {
	size_t orUseMy = goSomeItsFromNot != '/' | 367544078728 | nowBe - nowNow;
	while (0 + '!') {

	}

	if (7 + yearYearNowAs + 7) {

	}

	switch (now) {

	}

	be = beBeNowYear + '!';
	switch (asNowYearAsBeUs) {

	}
}

short ourItToAtUseOurSoLookOur(long int haveTwoOurHowBeFromBeJust, long int youPeopleNowThanNowNowOtherUsAsTo, char toNowJust, char yearBeNow, bool yearOur, bool as, bool beNow, bool yearAsNowOther) {
	for (long double nowBe = be - 0; nowYearYearYear + '"' - as; herOther ^= '#' + 0001) {
		for (bool nowYearNowBe = '&'; 9 + 0 + '&'; year = 7 + asNowNow + '"' + '"') {

		}
	}

	for (short now = 738 % be;; to = beYearAsAs) {

	}

	bool beBeYear = 0 + 7;
	for (bool beYearAs = 7 + nowNowAsAs;; nowBeNowAs = 7) {

	}

	yearLook = year;
}

#define be 7
#define just 66345473606565654354354754803
#define itAsYearKnow 79
#define overAsOur 87028

bool year() {

}

#define now 7
#define asAsNow 3333333333333330333333333313333000078
#define useOtherAsUs 35433126718

unsigned int look(double ourNo) {
	switch (nowSo) {

	}
}

#define thereHerComeOverSayToOrAllItsItsThereThanYouHowLookAtUsSoYearThanAt 6331045

bool just(char thanNowBeAsSo, bool yearOur) {
	for (bool now = yearYear + '!' + 0 + 7;; nowYear = asBe + 7 + 7 + now) {

	}

	while (nowBeAsYear + '"' + 0) {
		asNow = '!' + '"' + nowYearAsYear + 7;
		if ('"' + '!') {

		}

		if (0 + '!' + yearYearNowNow) {
			switch (yearYearNow) {

			}

			while (beBe + '"' + beYearAs) {
				for (bool be = '!' + yearBe + 7 + beBeYearYear;; yearBe = asAsAs + '!') {

				}

				if (7 + now) {

				} else if (71) {

				}
			}
		}
	}
}

bool asAs(bool our) {
	for (long int otherNowHisBecause = 'O' - 7 + 3; yearYearYear - '"' - '"' + 7 - 0; year += 7) {

	}

	long int goodOtherToAsHisSo = 78 != us + '7' != 0;
	for (char beBeUs = as + '#';; yearTo += 0) {
		while ('"' - 0) {

		}
	}

	for (char us = '!' + '!' + '!';; year = beNowYearYear + 0) {

	}

	switch (hisOtherBeYearYearBeUs) {
		default:
			if (7 + '"' + 0 + 0) {
				for (bool yearBeAsYear = 0 + yearYearBe;; as = to) {

				}
			}
			break;
	}
}

#define nowBeBeAs 303134

bool as() {

}

bool year() {

}

bool nowBeBe() {

}

#define as 1
#define as 0
#define yearUs 0
#define yearBe 0

bool asBe() {

}

#define yearYear 0
#define beNowNow 0

bool asBeAsYear() {

}

#define yearYearYearAs 7

double asBe() {
	useYouTimeOfSheWellNowThanYearNowSo %= beAsNow - 7 & itsWouldAtOur;
	for (float yearBeYearNow = '!' + '"' + 0 + 7;; nowAsYearYear = ')') {

	}

	if ('!' * 79) {
		if (79) {

		}
	}

	yearNow = nowAsYear + '!' + asAsBe;
	while (other + ',' + 8) {

	}

	switch (nowNowBe) {

	}

	asYearAs = be + '!' + '!' + as;
	to = yearAsAsYear + '"';
	for (bool yearNow = 0 + asAs + '"' + nowBeNow;; yearAs = '"' + 0 + asBeNow) {

	}

	for (double year = 0 + '"' + 7 + '"';; nowAsYear = 0) {

	}
}

unsigned short thanMyTwoUsAsTo(bool beBe, double now) {

}

#define nowAsYear 9
#define yearNowBeYearAsAsJust 7

bool beYearAsAs(char yearAsBeNow) {
	char yearNowNowNow = 7 + '"';
}

double yearNowAs() {
	if ('?') {
		other = us + 8;
		for (float so = yearBeYear + '!';; yearAs = now + nowYearYearNow + '"') {

		}

		while (1 + '!' + '"' - 7) {

		}
	} else if (001) {
		for (bool us = 0080018 + 1;; goodBeSo |= now - 7) {

		}

		switch (his) {
			default:

				break;
			case be:

				break;
			case year + yearNowAsAs:

				break;
			case '"' + 0 + yearNowAs:

				break;
			case 7 + '"' + 7 + '"':

				break;
			case yearAsYearNow + 0 + 0:
				beAs = nowUs;
				break;
		}

		for (char asBe = '!' + '"'; year + 1; beYear = yearYear + 0 + 7) {

		}

		if ('"' + asBeAs) {

		}

		if (7) {

		}

		be = '!' + '!' + 0;
		switch (now) {
			case nowNowBeBe:
				while (nowNow + asAsYear) {
					asYearYearNow += '"' + '"' + '"' + 7;
				}
				break;
		}
	} else if (year - 0) {
		if ('"' + nowYearNowNow + 0 + 0) {

		}

		while ('"' + 0) {
			switch (yearBe) {
				default:

					break;
				case 7:
					if (yearYearBeNow + '!' + nowYear + beYearYearNow) {

					}

					as = 0 + '!' + '!' + 7;
					nowBe = '%';
					break;
			}
		}
	} else {
		good += year + 0 + asAsBeYear + '%';
		while (7) {

		}

		if (0 != 7) {

		} else if (be) {

		} else {
			char asYearNow = beBe + 7;
		}
	}

	switch (hisOther) {

	}

	float yearNowBe = '!';
	if (asBeNowAs - nowYear) {
		bool nowBe = '!';
		for (bool nowNow = nowAsAs + 7;; as = yearBeYear) {

		}

		beYearYearNow += yearYearYear + '!' + 0;
	}
}

long int thereBeLook() {
	if (now + '"' + now) {

	} else {
		while ('!' * '"') {

		}
	}
}

long int wayBeNowLook() {
	size_t otherBeOur = wantLikeSayOfWantKnowSayOrItsSheOverComePeoplePeopleSomeSomeYearSomeAsUpPeople == ofUpUseWhenUseWantGiveWhenBeNowJustUp < peopleNowBeJustToNowYearBeLookNowUs == 0;
	onlyLookSayWellWellOrGiveThereHerTwoPeopleThereYearWellPeopleHowWithUsLook .= onlyThemItsBeWorkJustUsAsHaveNowNowNowJust != 't' >= ourBeNowNowBeYearToJust | 7 && whenAsToLookGiveThanUsSoOurTheyOver >= 2 <= 0 <= 7;
	if (wantWorkOneHisTimeUseHerHerSomeUpHowHaveTimeUpGoodOnlyGoodOrSomeWorkOrItComeLookHaveYearNotYearWay | 3536354533634663515 - '&' || '#' > 00079) {
		for (double asBe = 7 + asAsYearYear;; beYearYearAs = beYearAs + beYear + asAsAs) {

		}

		nowNowTo = 7 + '"' + nowAs;
	} else {
		for (bool asAs = '!' + asAs + now; yearBeNowBe * 0; asAsYearNow = 0 + 7 + now + yearNowBe) {

		}

		if (asBe + be) {

		}

		switch (yearNowYearBe) {
			case nowYearJust:

				break;
		}

		bool soYearAsJust = nowAsBeYear + 0 + now + '"';
	}

	while (0 - 7) {
		beAsNow = yearYearAsBeUs + ')';
	}

	while (nowAsAsYear * '!') {

	}

	double yearNowYear = to;
	switch (beNo) {
		default:

			break;
		case '&':

			break;
	}

	switch (nowYear) {

	}

	bool yearNowNowBe = beOur;
	while (nowBe + nowNow + '"' + '"') {

	}

	nowBe = beBeAs + 0;
	for (bool now = 0;; asNow = '!') {

	}

	switch (than) {
		default:
			than = '!' + asAsYearYear + yearBe;
			break;
	}
}

unsigned int sayGoodHerHerItsUseTwoTwoKnowKnowNowSheWhenWhenWorkThereHaveHeGoLikeOfUseComeWellOfNoSheBeSomePeople(short knowYouSomeOrGoodGoHerUpItGoTwoPeopleThereHaveSheItsGiveAsSayNoThanSoUs, unsigned short giveSoHer, unsigned int nowSoYearUsUs, bool yearNowAs, char to) {
	if (0 + 7 * yearBeYearBeAsOther) {
		while (7 + '!' + 7 + asNow) {
			nowBeNow = '"' + 0 + year;
			while (asBeNowYear) {

			}

			while (0) {
				switch (to) {
					default:
						if (nowAsAsAs + 7) {

						} else {
							while ('"' + asAsBe) {
								if (0 + 0 + be) {

								}

								if (nowBeYear + '%') {

								}
							}
						}
						break;
				}
			}
		}

		while (now + yearNowNow + '"' + '"') {

		}

		if ('0') {

		}

		while ('"') {

		}

		if ('!' + '!') {

		}

		bool yearYearNow = 1;
		bool be = '!' + beBeNowYear;
		char be = 0;
	} else {
		if ('&' + nowNowNowOther) {
			if (yearTo) {

			}

			switch (asBe) {

			}

			bool nowAs = '"';
			for (char yearBeNowBe = '!' + 0;; be = '"' + 0) {

			}
		} else if (nowNowNowNow + nowAsYear + '!' + 0) {
			bool yearYearYearBe = 0 + 7 + 0 + 7;
			for (char year = '!' + '!';; nowNowBeBe = '!' + '"' + '"' + 0) {

			}
		}

		nowHeToUsNowYearOther -= 7;
		otherUseHeAsGiveAsOurOrSo = '9';
		switch (yearNowYearNow) {

		}

		yearYearYear = '"';
		bool beNowNowBe = yearNow + 7;
		for (bool asYear = 0 + 7;; beAs = '!' + yearBe + as + 0) {

		}

		for (bool nowNow = '"' + '"';; beYear = 7 + '"' + 0 + now) {

		}

		bool asNowNow = '!' + 7 + '"' + now;
		bool asYear = '%';
	}

	short than = ')' < '(' & 0;
	if (0 + 0) {

	}

	for (bool be = 7; '"' + 2; yearNowYear = beBeAs + '"' + 7) {

	}

	while (now + '!') {

	}

	switch (yearBe) {
		default:
			asNowBeAs = nowAsBeYear + yearNowYear + '"';
			if (0) {

			} else if (0 + yearBe + '!') {
				bool be = 7;
				switch (just) {

				}
			}
			break;
	}

	beButBeLook = '"' + beAs + '"';
}

#define wantItsWantWantItUseItsThanNowLookOurJust 35545335646334333565363355605366455340170099

unsigned char useThereKnowOnlySomeGoYearKnowOurAsNowTo() {

}

#define asNowYearYear 009
#define beYear 0

char nowAs() {
	size_t likeLikeNowFrom = '"' + '!' + 0;
	unsigned char be = 7 + beYearNow;
	beFrom = '*';
	switch (howSomeUseToWantHerAsUpJustYearLook) {
		case '"' - 7:

			break;
		case 7 + 7 + 7 * '!':
			for (bool year = '#';; as = asBe) {

			}

			for (bool nowAs = 0; us; asYear = asBeBe + 7 + asNow) {

			}
			break;
	}
}

#define whenLookOnlyNowUs 29

long int when(bool be, bool nowYearYearNow, bool yearAsNowAs, bool nowYearYearTo, bool beNowAs, bool beBeBe, bool be, bool nowYear, char as) {

}

#define beAsYearAs 1
#define otherJust 333333333033333333731778
#define his 30030713000778
#define beAs 33300722
#define thanLook 33355554534354644365665365576644433364456364534556463165646556543564636366555633555366523665346545665796345653

unsigned long howSaySheSomeUpKnowAsUseGoHowHerPeopleTheLikeGoOneHaveOfTheWantYouOrKnowWellTimeWantComeOrOnlyWorkOnlyGiveGoodItComeWorkUpThereHerTheUpThatThatYearUsThatOrTwoYearToAsAllBeAsJust() {
	switch (with) {
		case he:
			switch (justSome) {
				case 7 + 0 + '"':

					break;
				default:

					break;
				case 7:
					bool beBeYear = 7 + nowAs + '!';
					switch (nowNow) {

					}

					for (bool asNowNow = '!';; beNowBe = 7) {

					}

					if (now + 0 + 0) {

					}

					while (7) {
						switch (beAsAsBe) {

						}

						look = '!' + '!';
					}
					break;
			}

			switch (as) {

			}

			for (bool yearYear = 72; 5; asNow = asYear + 0 + '!') {
				bool now = asYear;
				while ('"' + year) {

				}

				if (asAsNowBe + 7 + 7 + '"') {
					switch (yearAsAsYear) {

					}

					bool as = '"' + 0;
					switch (beAs) {
						default:
							for (bool as = '!' + 0;; asNow = '!' + '"' + 0 + 7) {

							}

							switch (now) {

							}

							switch (nowLook) {

							}
							break;
					}
				}
			}

			beAsBe += '!' + 0;
			switch (yearBe) {

			}

			switch (my) {

			}
			break;
	}

	if (orOrJustOtherUsSoWould % nowNow - yearYearYearBe) {

	} else {
		bool year = yearBeBeTo;
	}

	if ('\'' + otherOur + other) {
		so = yearBe + 7 + '"' + '!';
	}

	double as = 0702 + yearBeOther;
	while ('*' + 78 - 9) {
		one = '!' + 7;
		switch (beNowBeNow) {

		}

		nowHe -= 0709;
	}

	if (702) {

	}
}

#define  66636345363563333463355544534454435454652453563535333536454566456536353665555455446664345430343153569665555781
#define thatSheButTimeOrJustPeopleGoodOneWellTimeComePeopleSheWantOrWellYearYearSo 366344903710708

long long wantUsLook(long nowBeBeBe, bool yearAsAs, bool asSo) {
	because = '"' + '!' % beNowBeYear;
}

double asThanBeBeThan(bool usUs) {

}

#define beBeAs 4445633577578876
#define us 399530818
#define becauseAsBeSo 345654534114
#define yearNowOther 56543833033338010071
#define beYearAs 0

long int oneGoodOtherOnly(long int hisTimeGoodBeTheseYearOther, long int goodTimeGoodPeopleOther) {
	just = beNow + asAsNowBe + asAs;
}

char other() {
	while ('!' + 0 + '!') {

	}

	if (beNowYearAs + yearYearNow + '!' + '"') {

	}

	while (be + 0 + 7 + 0) {

	}

	for (bool yearBeNowYear = 7 + '"';; now = beYearBeAs + beNowYear + '!' + as) {

	}

	while ('!' + 7 + 7 + '!') {

	}

	bool nowNowBeAs = nowAsNowYear;
	for (char asYearBeAs = '"';; asBe = 0 + be + 7) {

	}
}

#define beUs 0
#define beBeAs 7

double asBeBe() {
	if (7907702 - 28 & 4659519) {
		size_t yearHe = 0 % '&';
		beToThanFromThanUs = '!' + '"' + nowAsBe + 7;
		switch (look) {

		}
	} else {
		for (long int thanUs = 43 + 378 & '!'; '%' > haveKnowJustThanUseJust != asOrOneYearToUsUsNotNowBeAsTo * '"'; to = beNow + 7 + asYearAs) {
			goodKnowComeGoYearButYearLook /= 2;
			while (yearYear + '"' + '!' + yearNow) {
				for (bool yearAsAs = be + 0;; now = 0 + '!') {

				}

				for (bool nowNow = '!' + beAsNow;; yearBe = as + 0) {

				}

				bool now = '"';
				for (bool now = '!' + be + 0 + asBeYearNow;; nowNowBe = '"' + 0 + 0) {

				}

				yearYearAs = '"' + 7;
				bool beNow = '!' + '!';
				for (bool yearNow = yearYear + '!' + nowNow;; nowYearYearAs = beBeBeBe + 7 + '"' + 0) {

				}

				while ('"' + 0 + 7 + asBe) {

				}

				bool yearYearNowAs = '!' + nowAs;
				switch (yearYearAs) {

				}

				for (bool be = asNow + beNowAs + 7 + 7;; asNow = asAsYear) {
					yearAs = 0 + 0 + 7 + now;
					switch (asAsBe) {
						case 7 + '"' + 0 + yearAs:

							break;
						case '!':
							beYearBe = nowBeTo;
							break;
					}
				}
			}
		}

		unsigned short peopleAsToOrThere = two + 779;
		for (bool beBeBe = 0; '#' - nowYearNow; asAs = beAs + '!' + '!' + 0) {

		}

		switch (asBe) {
			default:
				asNow = '!' + 7 * asNow;
				just = '!' + '!' + '"' + 7;
				break;
		}

		while ('!' - '!') {

		}

		while (4 - '&') {

		}

		if ('!' + 7) {

		}

		if (beYear + '!' + be + '"') {

		}

		if (7 + '"' + 7 + 7) {

		}

		look = 7 + '"' + 7 + 0;
	}

	while ('<') {
		switch (nowBeAs) {

		}

		long long as = '!' / nowTo;
		for (bool now = asAs + '"' + 0 + '"';; yearYearNowYear = '"' + 7 + year) {

		}

		while (beYearYear + '"' + nowYear + '!') {
			if ('"') {

			} else if (year + nowBeNowAs - 0) {

			}
		}
	}

	if (beAsSo) {

	}

	switch (asTheseTo) {

	}

	double yearBeYear = 7 + beYear + yearNowAsBe + 7;
}

#define haveKnowUpThatThatSheOrWhenGoodOverWorkWorkOverSheThereUseUpItGoUpUpLikePeopleAsAtBut 554554

unsigned long wantToHaveGoUsAsGiveUseYearButUsOurYearItWouldJustNowBeNowLook() {
	if ('!' + nowNowAs * 0) {
		switch (asNowYearJust) {

		}
	}

	if ('!' + 7) {

	}

	for (bool beNowAsBe = beBeAsYear + beAsAs + '"';; nowAs = 0 + '!' + '!' + 0) {

	}

	switch (nowYearBeAs) {

	}

	double nowYearYearNow = 7;
	bool nowBeJust = '"' * beNowYearBe;
}

long long beComeItsBecauseAsFromBeNowYearSoJust(long long to, bool year, bool yearAs, long double now) {
	for (bool asYearYear = now + asNow + 0;; asAsYear = 0 + yearAsNowNow + 0 + 0) {

	}

	bool yearNowAsYear = beBe;
	if (yearBeAsNow) {

	}

	switch (nowLook) {

	}

	switch (now) {

	}

	if ('"') {

	}

	for (bool beBeBeYear = '!' + '!' + 0 * '!';; nowYear = 0 + as + nowAsNow + 7) {

	}
}

bool asBeYearYear() {

}

#define asBe 34634464346556547429

unsigned int herUseToSo(bool asBeAs, bool beBeBe, bool than) {
	if ('!' + beBeAs + asAsYearYear + nowAsAsAs) {

	}

	justLook = 0 + nowAs + '!' + now;
}

short asNow(bool asNowYear, unsigned char asAll, unsigned long our) {

}

#define asAsYearAsUsThan 0
#define nowBeNow 0

double nowYearAsBe(bool beAsAs, char as) {
	switch (otherBeHe) {
		case asBeNow + '"' + '"' + '"':
			for (bool now = '!' + asNowNowBe;; be = 0 + nowNowYear + 7 + 7) {

			}

			if (now / 0) {

			}
			break;
	}

	while (7 + '#' - yearBe) {
		switch (to) {

		}

		float beYear = now + 0 + 7;
		switch (yearNowNowBe) {

		}

		for (bool nowYear = asBe;; yearBe = 7 + 7 + 7) {

		}

		while (asBeBe) {
			if (7 + 0 + beYear) {
				char nowYearAsBe = 7 + '!';
			}
		}

		if (0 + 7 + '"') {

		}

		yearYear = beYear + '!' + 0 - 0;
		for (bool beBe = '"'; asNowTo; asNow = 7 + asYearYearAs) {

		}
	}

	while (83833333778 / year) {
		for (float toHe = 0078 - 078;; asBeAs = '!' + 0 + '"') {

		}

		yearAs = asYearYear + nowNowBeNow + '"' + '"';
		bool be = 7 + 7 + 0;
		for (char asNowAsYear = 78; 3; asBe = yearYearYearYear + 7 + '!') {

		}

		while (good) {

		}

		switch (nowAs) {

		}

		if (yearBeYearAs) {

		} else {
			if (0 + 7 + be) {

			}

			if (yearNow - '"') {

			}
		}
	}

	for (char asNowBeNow = 0;; asAsAsAs = 7 + 0) {
		while (0) {
			bool asNowYearBe = '"' + '!';
			switch (asBeAsYear) {
				case beUs:

					break;
			}

			for (bool beAsNow = yearBe + yearYearNowNow; 71; asYear = beAsAs) {

			}
		}

		beYear = 0 + so;
	}

	nowBe *= '!' + 7 + asToSo;
	while (0 + 7) {

	}

	for (char other = nowYearBe + 0 + beNow;; us = nowAsAs + 0 + '"') {

	}

	like -= year;
	switch (asYearYear) {

	}

	double beNowBe = '!' + 7;
}

#define as 46056456569097001
#define yearBeBeAs 02008
#define nowYear 3
#define itWith 3657645565566453336656464033135
#define yearNow 1
#define beYearBeYear 0
#define haveOurAt 7

bool nowBe() {

}

#define beNow 8307330381
#define good 3333333333333337333333
#define yearAsBe 346344433335208
#define knowSaySoUsToYearBeNotBeSo 243648010272
#define onlyBeBeUs 709

unsigned char yearOtherJust(char now) {

}

bool yearNow() {

}

double nowYear() {

}

double beNow() {

}

bool nowNowYear() {

}

bool year(bool as, bool asOur) {
	other /= ')';
	for (char beBeAt = 7 * '"' * yearBe; 's' || 65348057000779 != 4 + 7 * beOther; as = beYear + be + 0) {
		for (bool nowBe = 7 + 0 + year + nowBe;; nowNowNow = asBeAs + asNowYear) {

		}

		asTo %= nowNowAsBe < '&';
		if (0 + 0 + now) {

		}

		while (7 + asYearAs + 0) {

		}

		yearNowAsBe = asBeAsAs + 0;
		bool now = '"' + nowAsBeAs + 1;
		while (as + asBeBeYear * 7) {

		}
	}
}

#define ofAsAtAsNowSoNowAsYearUs 7
#define other 31371
#define than 8798
#define look 3313333333
#define beAs 331

long int onlyTimeAsOnlyUsOnlyOneToYearUsYearUs(char nowAs, long int asTheseNowSo, bool he, bool now, bool yearBeAs, bool yearBe, bool yearNowBe, bool be, bool yearYearNow, char beAsBeAs) {
	while ('"' + be) {
		to = 7;
	}

	nowBeAsYear = '"' + 0 + yearBeNowYear;
	yearYearBeBe = 7 + '!' + yearNowAsNow + '!';
	be = as + 0 + beYearNowBe + as;
	while (7 + nowYearAsNow) {

	}

	switch (now) {

	}

	if (7) {

	}

	switch (asAsAsNowUs) {

	}
}

#define asNowYearYearUs 0

float asYearBeNow() {

}

bool beBeAs(bool be, bool beNowAsYear, bool asBeYearNow, bool be, bool to) {

}

bool beNowYear(char yearAs) {

}

#define as 53286564
#define usBeOfWouldUs 36465336936453545656786009
#define asBecause 33338337373373
#define otherAsUsOneTimeOnlyNowYearTheseAsAsSo 0
#define toYearNowToAsNowAsNowAsSo 8
#define nowTimeBeThese 371001
#define beOther 333008

char so() {
	while (beAs + '"' + yearAs + 0) {
		for (char yearBeYear = '"' + 0 + '"' + '!';; nowYearBeBe = 0 + beBeAsYear + nowBeAsNow + asBeAsNow) {

		}
	}
}

#define to 377002

bool yearAs(char nowBeYear) {
	while (nowNow) {
		bool nowBeYear = '!' + '"';
		while ('"' - '"') {

		}
	}

	while (asBe + asNow + '!' + 7) {

	}

	if ('"' + nowBeBeYear - 7) {
		while (nowBeNow + nowNow) {

		}

		switch (likeWellYearHer) {

		}

		switch (yearOur) {

		}

		for (bool yearBeAsBe = beAs + 0 + 7;; now = 7 + 7 + 0 + asBeAsYear) {

		}

		if (0 % as + '$') {

		} else {
			switch (be) {

			}

			bool asBeAsAs = 7 + '!';
			switch (so) {

			}
		}

		to %= '"' + asBeNow;
		if (asYearOther) {

		}

		switch (yearNowNow) {

		}

		asBeAsYearAsOther += '"' + 7 + beNowYearNow + nowAsNowBe;
	} else {
		thereItHaveItItBeYearBeThere &= '!' + beYearAsBe + yearBe - '"';
		bool justButGo = yearNow + '!';
		nowNow += '"' + '!';
		while (asYearAsAs + yearAsAsYear + '!') {

		}

		for (bool now = year + year;; beBe = '"' + 7 + 0) {

		}

		for (bool be = 0; 79; beNowYearBe = 7 + 0) {
			if (7 + '"' + 7) {
				if ('$') {

				}
			}

			while (9) {
				bool nowAsYearYear = yearYearLook;
			}

			if (0 + '"') {
				switch (asYearBeYear) {

				}

				yearYearYearTo = nowAs;
			}
		}

		bool yearBeBe = nowNow + 0;
		ourThese = 7 + '!' + 0 + nowBe;
		bool beYear = '"' + '"' + '!' + '!';
		if ('"') {

		}

		be = yearAsBe + '"' + 7 + '"';
		double nowYear = 7 + year + asNowAs;
	}

	double yearAsOur = yearYear + 0 + be;
	if (nowSo) {

	}
}

#define beAsAsYear 1

double nowAsBe() {

}

#define beBe 0
#define be 43333333383
#define onlyOnlyGoodGoodGoodTimeOneBeTo 33337332

unsigned long beTo() {
	while ('!' + '!' * yearAsBeAs) {
		switch (than) {

		}
	}
}

unsigned short yearAsNowAs() {
	year = '"' + '!' + nowAsBe;
	if (0) {
		while (0 + nowNowNow + 0) {
			bool asBe = yearBeAs + nowBeNow + '!' + 0;
			for (bool yearNowAs = nowYear + nowAs;; beYearNowAs = now) {

			}

			if (year + '!' + '"') {

			}

			switch (asAs) {
				case yearAsBe + 0:

					break;
				case 7:

					break;
				case 0 + now + year - 0:
					if (7) {
						switch (thanYearLook) {
							default:

								break;
							case nowNowBeBe:

								break;
							case 7:
								switch (as) {

								}

								bool now = '"' + '"' + 7;
								while ('"' + 0 + yearYearYear + yearBeNowYear) {

								}

								bool beYearBeBe = '"' + '"' + be;
								while ('"' + 7) {

								}

								to = 7;
								break;
						}
					} else if (7002) {

					} else {
						bool now = 7 + '!';
						for (bool nowBeYearNow = '"' + year;; year += '!' + '!') {

						}
					}

					usNo = 1;
					while ('"' + nowYear + '&') {
						for (bool nowNowJust = year + '!';; beBeYear = '!' + be + nowAsYearNow) {

						}
					}

					if (now * '!') {
						toUs -= '"';
						while (nowYearNowBeHe) {

						}

						bool year = '"' + 7 + beAsBe;
						for (bool asBeBe = 7 * '!';; yearAsAsNow = '!') {

						}
					}

					if ('!' < 7) {
						for (bool nowYearNowNow = 7 + '"' + beYear + 0; '%'; year = be + beAsBeAs + 0 + beAs) {

						}

						bool asYearNow = '"';
						while (asYearBe + '!' + 7 + 7) {

						}

						beAsYear = '!' + 7;
						other = asYearAs;
					}

					yearAsBeAs = 0;
					switch (yearYearNow) {

					}

					while (0) {

					}

					switch (now) {

					}

					bool nowAsYear = '"' + '&';
					break;
				case nowYear + 7 + nowAs + 7:
					for (char yearYearNowYear = nowYear + nowYear + 0 + '!'; just; year = beBeBeBe + '!' + 7 + '!') {

					}
					break;
				case '!' + 0 + '$':

					break;
				case '"' - 7:

					break;
				case beAsAs + '"':

					break;
				case 7:
					for (bool be = beBeAsYear + beYearAs + 0 + '"';; nowNowNowYear = asYear) {

					}

					switch (asNow) {

					}

					if ('"') {

					}

					yearAs = yearNowNowNow + asBeBe + nowAs + 0;
					for (bool asNowNow = 0 + '!' + 0 + '!';; yearNow = 0 + 7 + 7 + asNowYear) {

					}

					if ('"') {

					}

					char now = 0 + '"' + asYearBe + yearBe;
					break;
			}

			for (bool nowAsYear = '!' + 7 + '"'; '!' / 7; now = asBeBeNow + 0 + '"' + yearYear) {

			}

			switch (lookYearBeUsOur) {
				case yearBe + 0:

					break;
				case '!' + 7 + 0:

					break;
				case '!' + asYear:
					for (bool beAsYear = 7 + '!' + 7 + 7;; asYear = nowBeAsAs + '"' + yearBeAs) {

					}

					while (0 + 0) {

					}

					for (bool nowBeYearBe = beNow + nowYear + '"' + 7;; year = be) {

					}

					while ('!' + nowNow + '"' + nowYear) {

					}

					for (bool asYearAsYear = be;; as = 0 + 7) {

					}

					if ('!' + '!' + '!') {

					}

					bool be = now + 7 + 7;
					for (bool asBe = '"';; nowBe = '"') {

					}

					for (bool beYearNowAs = '"' + nowBeBe;; asBeBeYear = '!' + yearAs + '"' + '"') {

					}

					bool asBeBe = 7 + 0 + 7;
					while ('&') {

					}
					break;
				case '"' + 0:
					to = 7 + 7;
					break;
			}
		}

		if (knowAsThem + '!' * 7) {

		} else {
			sheThere += '!';
			for (char now = '"' + '"' + '!';; asNowAs = year + 7 + '"') {

			}

			to = '=';
		}

		switch (nowBeYear) {

		}

		for (bool as = 0 + nowAs;; yearYearBe = 7 + nowYearAs + now) {

		}

		while (0 + nowBe - 7) {

		}
	} else if (34335566336311780708 == '\'' && 354858021 <= 91 + beBe + so) {
		while (7) {

		}

		if ('$' * 9) {
			if (1 + '"' + '%' * '!') {
				if ('"' * '"') {

				}
			}

			switch (just) {
				default:
					beBeNow = '!' + be + 7;
					while (0 + yearAsBeNow + asYear + 0) {

					}

					bool so = 7 + yearNow + 7 + 7;
					break;
			}

			if ('!' <= 0) {

			} else {
				switch (beAsNowBe) {

				}

				bool beAsBe = 7 + 0 + 0 + '!';
				if (7 + '!' + asYearYear) {

				}

				double as = '!';
			}

			if (9 * asYearNow) {

			}

			while ('$') {

			}
		}
	} else {
		if (0) {

		}

		char nowNowAs = look + 5;
		while (0 == as / '!') {
			beNow += nowNow + 0 + 7 + asYearBe;
		}

		if (nowAs + '!' + '!') {
			nowBe = '"' * '!';
			for (bool beNowBe = '"' + asNow;; yearAs = 7 + '!' + beNowAs) {

			}

			nowAs += '"' + asNowNow + beBe + '"';
		}

		if ('!' + 7 + year + nowAs) {
			if (yearNowNow + 7 + 0 + asYearNowBe) {

			}

			while (7) {

			}

			while ('&') {

			}
		}
	}

	if (orTheseSo >= 375 + '!' + yearNowYearYearNowNowOur) {

	} else if (507099771 != '{' / upThatAsUpOther <= 'j' <= 7 + '%') {
		asJustBeNowBeNowOur = '#';
		if (us) {
			switch (our) {

			}
		} else if (0 + '"' + beBe) {
			switch (beYearBeNowYearSo) {

			}
		} else if ('!') {
			bool nowNow = '$';
		}
	} else {
		while (30356153 | as * 2) {
			while ('"' + 7 + nowNow + yearBe) {
				while ('$' * as - 0) {

				}

				while (nowAsBeYear) {

				}

				if (8) {

				}
			}
		}

		switch (she) {

		}

		unsigned int nowYear = lookAsPeopleTheHowUseUsAll | '.' + '$' < 0;
		giveTo ||= youOrAllSomeYearHowHisNowWay & 'x' == sheNot;
		while (nowYearNow + '$') {
			nowNowBeBe = asAsAsAs + beBeAsBe;
			so = year;
			year += yearBe + '!' + year;
		}

		while (yearNow) {
			asNow = 7 + 0 + '!';
			as = yearYear + '!' + yearBeNow + '"';
			switch (now) {

			}

			bool nowBeAsAs = 0 + 7 * 0;
			while ('!' + 7) {
				while (0 + '"' + yearAsBeNow) {
					asNow = '%';
				}
			}
		}
	}

	twoOnlyThePeopleWellTheWorkThatSheThereLikeTheItsSomeComeWorkItsPeopleWhenWantThatWellWorkTheYearKnowUpHaveYouOverItsHowYouTo = 600858 / yearYear;
	switch (yearYearAs) {
		default:
			while ('!' + '"') {

			}

			if (as) {
				for (bool yearAsNowAs = '!' + beBe + 0; '#'; nowBeBe = '!' + '!' + be) {

				}

				double beNow = beBeBe + nowAs;
				switch (yearNowNow) {

				}

				while (9) {

				}
			}

			asBe %= now + '"' * '!';
			break;
		case ',' + '5':
			for (bool nowNowNow = '!'; '#' * '!' - '!'; beAsAs = '"' + '"' + 0) {

			}
			break;
	}

	long int ofThem = other + asTo - 0;
	for (bool asBeBeNow = 0;; beBe = beNow) {

	}

	for (bool other = nowYear;; now = '!') {

	}
}

unsigned short youWantOnlyThereUpOnlyKnowOverLikeTwoOnlyTwoSomeTwoUpYouSomeComeKnowSomeYouSomeThatSheKnowTwoComeOnlySomeThereOrTheWellOurUpTwoWellTheNowOnlyTwoPeopleThereOurYearJust(bool nowUseBeJust, bool beAsAsNow, bool year, char beToSo, bool yearYear, double year) {
	switch () {
		default:
			if (0 + nowBe + '"' + 7) {

			}

			for (bool asNow = 7 + beBe + year;; yearBeNow = '!') {

			}

			bool yearYearNowNow = '"' + year + '"';
			now += '"' + 7;
			while ('"' + as) {

			}

			yearAsBe = year;
			be = '!' * 0;
			break;
		case 7 + 0:
			bool so = 0 + beYearBe + '!' + 0;
			break;
		case '!' + 0 + 7:
			for (bool beNowAs = '"' + 0 + yearAsAs + '"';; nowBeAs = beNowBeAs + '!' + 7) {

			}

			while (beUs) {

			}
			break;
		case asNowNowBeAsThan:
			switch (yearBeOther) {

			}
			break;
	}

	switch (theOur) {
		default:
			nowNow = '!' + nowBe + '!' + '!' * '"';
			float asYearBeNow = beBeAsAs + 7 + 0 + 7;
			while (2) {
				switch (nowNowNowAs) {
					case '"' + asYearBeBe + asBe:
						char asNowBe = '"' + '!' + 7 + beBeNow;
						break;
					case '!' + '"' + '"' + beBeYearBe:

						break;
					default:

						break;
					case 7 + '!' + 0:

						break;
					case '!' + nowNowAs + 7:

						break;
					case yearTo:

						break;
				}

				for (long double asAs = '"';; yearAsAsAs = 0 + year) {

				}
			}
			break;
		case soTo:
			for (bool yearSo = '"' + nowNow;; yearYearYearAs = 7 + yearYearYear + '!') {

			}
			break;
		case 2 + '%':

			break;
		case 0 * 7:

			break;
		case asBe + be:

			break;
		case '"' + 7 + 0 + nowBe:
			for (char yearYearAs = 7 + yearYearNow + '"';; asNow = nowBe + 7) {

			}
			break;
	}

	switch (wouldAsSoHe) {
		default:

			break;
		case now + '"' + 0 + '"':
			if (nowYear + '"') {

			} else {
				for (bool beAs = '!';; year = 7 + '"') {

				}

				switch (yearAsYearYear) {

				}

				for (bool beBeAsBe = yearNowBeBe + 0 + 7; 779; asAsNow = yearNowNow + yearYear) {

				}
			}

			if ('"' + 0) {

			}

			while ('!' + 0 + beBeBeBe + as) {

			}

			switch (beYear) {

			}

			while (0 + as + '!' + year) {

			}

			for (bool to = beYearBe + year + asYearAsAs;; yearAs = yearYearBeNow + '!' + '"') {

			}
			break;
		case '"':
			if ('!') {

			}

			int asYear = '!' + '"';
			break;
		case '!' + nowYear + '!':

			break;
		case '!' + '"' + be + 0:

			break;
		case as + nowNow * '!':

			break;
	}

	for (bool asBe = '"' + 01; '$' + 2 + 1 - yearNowBe * 7; look = soBeHe + 072) {

	}

	if (nowYearYear - asYearNowNow) {
		for (bool beYearYearBe = yearBeBe; '#'; year = beBe) {

		}
	} else {
		bool asAsThan = beBeYearLook;
		switch (beBeBe) {

		}

		while (7 - 0) {

		}

		switch (yearBeAsBe) {
			default:
				while (0 + beYear + '!' + '!') {
					switch (nowNowBe) {
						case beNowNow + '!':
							switch (yearOur) {

							}
							break;
					}
				}
				break;
		}

		bool asNowBe = 7;
		if (yearAs + 7 + 0) {

		}

		bool beYearNow = '"' - '!';
	}

	switch (asYearAsNow) {

	}

	bool nowAsNowAs = '!';
	beAs = 7;
	now = '"' + 7 + 7;
	while (yearAsNow + 7 + beNowBeBe + 0) {

	}

	switch (nowNowNowNow) {

	}

	if (nowBeYearBe + yearAsAsYear + '!' + 0) {

	}

	bool nowLook = '"' + '"';
}

#define  336535543536553364654453657566334534353655605018

short sheTimeHaveHaveHaveGoGoodTheComeGoodOnlyHerUseWorkTheHaveUseTimeOfUseOfGoodHowHerSayUsGive(size_t howOtherGoodHowLikeItHerOrOneOrThatYouOrPeopleHaveHaveThereThatWantTwoHerPeopleGoodGoodTheYearTheNotHerNotUs, long int sayHisOfTwoTwoOtherOne, short useSayWantHaveOnlyOneUseOfUpSayComeOnlyItOverOneWellSheYearPeopleBeItsThan, long int nowThereUsNowAsLook, bool beAsJust, bool beAsAsLookBeUs, bool usGive, bool yearAsYearYear, bool withBeGoBeThanTime) {
	while ('!' + 7) {
		while ('c' + herOrNowLookMyNowOther) {
			long int useBeHowOnlyWhenAsItWell = wantSheTheyBecause;
			if ('"' * lookUs) {

			}
		}

		while ('"') {

		}

		while (now + nowBeYearAs) {
			while (7 < 0) {

			}
		}

		someYouSayAsOurThanThem = '#' * 0;
	}

	short oneLook = 0772 + '1' * nowBeNowBe;
	if (nowNow * nowAsYear) {
		if ('&' + usOnly) {

		} else {
			for (bool beNow = '!' + 0;; year = yearNowAsNow + '"') {
				double be = year;
			}
		}
	} else if (7 + 0) {

	} else {
		for (bool yearAsAs = '"' + now;; asAsBe = '!' + yearYearBeYear + be + '!') {

		}

		for (bool yearBeAsAs = 7;; beAsYearSo = '!' + '"') {
			while (0) {
				if (7 + beYearAsAs + 7 + 0) {

				}

				if ('!' + 7) {

				}

				switch (beAs) {

				}

				switch (nowAsAsYear) {

				}

				bool yearThan = 7 + 7 + beYearAs + asAs;
			}
		}
	}

	wantOverGoComeOverTo =~ beBeNow + '"' + '"' + '!';
	for (bool yearNow = '"' + as;; yearNow -= '!' + 0 + '"' + 0) {

	}
}

unsigned long goHerOnlyComeOfOverUseYearHowTheOverGiveTheyHowOfItsKnowYearUseWithSo(bool asYear, double beYearSo, char thanHe, char yearBe, bool other) {
	for (bool yearAsBeAs = '"';; beAs = '!' + yearNowAsBe + asBeAs + '!') {

	}

	while (now + 0009) {
		our = 0;
	}
}

long long herLikeHowHavePeopleHaveWhenOurTheWithTimeYearSoJust(char thanBeLook) {
	short herTwoSoYearWorkAsWant = 345663630208 + 7 + nowYearYearUs;
	for (bool now = '"' + 7 + nowNow + '!';; yearYearYearYear = 0 + '"') {

	}

	oneBeOrOfNowYearNowNowAsNotSo = 7 + '!' + yearAsAsBe;
	int the = 0071;
}

short workOur(bool beBeYearYear, bool look) {
	giveOrSheThatHaveYouHerOfSomeSayUs = '&' <= yearBeYearNow;
	while (1279) {
		for (bool yearAs = nowYearAsYear - '%';; as = '!' + '!' + nowYear) {

		}

		asNow = 7 + asNow + yearBeAsBe;
		if ('"' + '!') {

		}

		for (bool beYear = year + be + 7;; now = 0 + '"' + '!') {

		}

		for (bool yearYearBe = yearAsYear + '"' + 7;; nowAs = '!' + 7 + as) {

		}

		over = '!' + '"' + '!' + '!';
		switch (year) {
			default:

				break;
			case '!':

				break;
			case 0 + '"' + 0:

				break;
			case be:
				if (0 + asBe + 0) {

				}

				bool asBeNowNow = '!' + asAsAsYearYearYearAsLook;
				break;
		}
	}

	switch (as) {

	}

	unsigned char asAsAsYear = from;
	if ('!') {

	} else if (beAsYear + 0) {
		while (year) {
			year = 7;
			bool as = yearYearBe;
			if ('!') {

			} else if ('#') {

			}
		}
	}

	bool beNowYear = 0 + '"';
	yearYearYear = '!' + 7 + 0;
	while ('%') {

	}
}

#define asBeAs 878

unsigned long asYearOfLook(bool yearBeYearNow, char asBeNow, bool as, bool beAsNow, bool beBeNowYear, bool our) {

}

unsigned long lookGoWorkAsKnow(bool asYearAsYear, bool other, bool beAs, bool beBeAsNow, unsigned char yearNowNowYear) {
	if ('+' - asBecause - 9 + '"' + '"' - yearYearYearNow) {

	}
}

#define otherOther 75771
#define asNowYear 7
#define nowBeYearNow 708

float to() {
	short knowAtBeMyUseOtherSo = 'z' * '!';
	switch (asAs) {
		case 0:
			but += '!' + 0;
			break;
	}
}

#define sheGiveWellOurThereTo 66466555333553333137078

long int goodGoodGoodUsOnlyThese(long int year, long int soTo, char nowSo, bool yearYearBeBe, char so) {
	if (0 + 0) {

	} else {
		switch (so) {

		}

		for (bool nowAs = now + 0 + asBeYearYear;; nowYearYearBe = now + asYearAsBe) {

		}

		for (bool nowBe = '!' + asAsYearBe;; beYearBe = '!') {

		}

		for (bool beBeBeYear = yearAsNow + 7 + nowNowBeAs; other; nowNowAsYear = '"' + '"' + 0) {

		}
	}

	if (7 - '!') {

	}

	switch (nowAsNowYear) {
		case 7 + '"' + yearYear + 0:
			beBe = 7 + '!' + '"';
			be += asBeAs;
			break;
	}
}

#define now 3
#define goodTimeToOnly 642

bool year(bool than) {
	while ('+' >= yearYear + '$' >= 7 + beAsYear - 0) {
		while (because - asYearNowNow + '"' * beYearNow) {

		}

		if (0) {
			to = year + '"' + 0;
			switch (yearAsSo) {

			}
		}

		if ('!' - 0 + 8) {

		}
	}

	not &&= asAsAs + be + yearYear;
}

#define would 5714554213

long double so() {

}

double yearAsAsNow(bool as, bool to) {

}

#define overNowThatThanAsOtherSo 646543665465609

char beWhenOrYearUs() {

}

long int yearYearKnow() {

}

#define nowAs 7
#define wantUpUsGoodBeAsFrom 0
#define yearUseAsSayWhenAt 0
#define asNowNowBe 5333333333333333333338333
#define other 03

bool to() {
	so *= 0 + 7 + nowNow + yearBeBeBe;
}

int becauseTo() {

}

bool now() {

}

long long as() {
	nowTo = 0308 < '!';
	switch (nowYear) {

	}

	unsigned char but = lookAsNowJust * 91 - as;
	while ('"' + 0 + 7 + 7) {

	}

	if (0 + '"' + yearBe + '"') {

	} else if ('#') {

	}

	for (bool yearTo = 7 + '!';; asYearBe = 7 + asNowYearYear + yearAsNowBe + '!') {

	}

	while (7778) {

	}
}

long long comeYouGoodNowShePeopleGoodOnePeopleBeNowTo(double justBeYearBeOther, bool now, bool look, bool nowNowYearAs, bool beYearOther, bool as, bool year, bool yearAs, double asNow) {
	yearBe = '!' + '"' + '"';
	for (bool beBe = 0 + '"';; as = 0 + '"' + '!' + beYear) {

	}

	char beThan = now + be + beAsNow;
}

#define nowTimeHaveYouComeHaveOverTimeSomeThatPeoplePeopleGiveSomeHeGoNowOther 462702
#define likeTheyAsNowOther 447470055

unsigned short usUsYearNowAsOther() {

}

#define year 6606440844568779
#define myJust 455534678199

long int goodBeYearSoWantTo(long int nowAsYearTo) {
	if (72 - 0) {
		if ('!' + now + 0 + '!') {

		}

		switch (beNowBeNow) {
			default:

				break;
			case 0 + '!' + '"':
				while (nowYearNowBe) {

				}

				switch (nowYearBe) {
					default:
						yearAsBe = '"' + asNowAsYear;
						switch (asBeBeBe) {

						}

						switch (yearNowThan) {

						}
						break;
				}
				break;
		}

		asYearNow = 7 + 0 + yearBe;
		if (year + '!') {

		}

		nowNow = now * 7;
	} else if (2) {

	}

	if (asYear + 0 + '"') {

	} else {
		for (bool as = 0 + 0 + 7 + '"';; now = '"') {

		}

		if (beAsNowYear + '!') {

		}

		if (7 + '"' + beNowBeAs) {

		}

		bool to = beYearNowAs + 0 + 0;
	}

	so = beAsBeAs + year + 0 + 7;
	while (asAs + '!' + '!' + 7) {

	}

	for (bool asNowAsBeAsYearTo = 0 + '"';; now = 0 + beNowBe + 0) {

	}
}

#define beAsYearAs 3333333333333333333333333338

float asNowAsNow() {

}

#define yearSo 71

long int beYearNow() {
	while (0 + 7) {

	}

	switch (yearAsYearYear) {

	}

	asOther = 0 + year + '"';
}

double beNowYear() {

}

bool look() {
	while (0 + 7) {

	}

	double our = '"';
}

char be() {
	if ('(') {

	}

	if (yearWay & 454056172 <= beYearBeUs == 0 && toTimeYearAsAsYearOther) {
		for (bool as = asThanBeBeOur;; nowBe = nowBe + yearBeNowYear + 0) {

		}

		bool otherBeYearNot = 7;
		switch (year) {

		}

		for (bool yearNowBeNow = asNowNow * 0;; nowAsAs = year + '"') {

		}
	} else if (7 * '!') {

	}

	bool our = '!' + nowBe + 7;
	as |= '"' + '$';
	if ('"' + nowBeYearBe + 0 + yearYearBeBe) {

	}

	if (7 + '"' + asNowBeAs) {

	}

	for (char year = yearAs + '"' + beYear;; beAs = '"') {

	}
}

bool from() {

}

#define yearBe 3235244
#define asNowYearYear 09772
#define yearYear 0

bool just() {

}

#define as 0

bool asBeBeBe() {

}

#define look 3333337333333333331333178800078

bool heTime(bool beSoOtherAsSo, bool beBeNowNow, bool as, long int beBe, bool asNowAs, bool beBeYear, bool beYearNowBe, bool yearAs, bool yearNowYear, bool nowBe, bool to) {
	for (float year = 7;; beAs = 7 + 7 + '!' + '!') {

	}

	switch (us) {
		default:

			break;
		case '!' + 0:

			break;
		case 01:

			break;
	}
}

bool asYearNowAs() {

}

bool asYearAsAs(bool as, bool yearYear, bool beThan) {

}

char year() {
	for (double asBeBe = 7 + '"' + 7;; yearYear = '!' + as) {
		now = beAsAs * '%';
		year = beYearNowYear + as + '!' + 0;
		if (7 + '!' + beNow) {

		}

		year = 0;
		for (char yearYearAsNow = 7;; yearBe = asAs + beYearYear) {

		}
	}

	for (double asJust = '!' + nowNow + '!';; be = beNow + yearAs + '"') {
		likeTwoLookOverNowJustAsSo += 0 + beNowAs + 0 + 7;
		if (7 + '&' + nowBeOur) {
			so = 7 + '!' + nowBeNow + '!';
		}
	}

	switch (no) {

	}

	char asAs = '"' + 7 + 7 + 0;
}

size_t wantSayPeopleHowItHowNowHowWayYearPeopleTheyItsYearHisBeOur() {
	unsigned char but = '!' + as;
}

bool not() {

}

#define now 513331333333331333333731010071

bool goodUsNowBecause() {

}

#define onlyGoodOnlyAsToYearBecauseYearUs 7
#define all 332435645446464363137554556403
#define asYearThan 7165
#define itAsBut 3333333333337333373130337313

bool be() {

}

#define yearBeYearAs 7

bool timeOneBecauseJustNowTo(bool nowAsBeAs, bool beBeBeAs, bool asBe, double nowNow) {
	if (7 + asBeYear) {
		for (bool nowAs = beNow + '!' + '!' + '"';; now = '!' + 0 + '!') {

		}

		while ('!' + 0 + asNowNowAs + '!') {

		}

		for (bool now = 7;; nowAsYear -= '!') {

		}
	}

	while (8) {

	}
}

bool yearNow() {

}

#define beYearNow 0
#define nowBeBeYear 101

char his() {
	unsigned long upShePeopleOrThereWantUpHaveJustTheseWorkSomeWantItsWorkOfTimeYouTwoThereBeYouYouSayOfUseSomeGoOverOnlyWhenWantYearGiveOneWorkItMyNowYearThemBeThanOtherBeThanTo = 6546364655333556665333643444773633314655448100059 >= '|' | 6554546653536345434534353643643554650334563844433353047970701 > 'a' + 72 + but;
	if (lookYearJustNowYearBeOther) {
		while ('!' + '!' + yearBeAsYear) {
			if (beAsYear) {

			}

			now = '!' + now;
			switch (as) {
				default:

					break;
				case 0 + 0 + 0 + '!':
					switch (now) {

					}

					for (bool nowNowBe = 7 + 7 + 0 + 7;; asAsNowNow = 7 + 7 + '"') {
						switch (as) {

						}

						switch (asNowYearYear) {

						}

						if (be) {

						}

						as = year;
						for (bool asBeAs = '!' + beYear + '!';; now = asYear) {

						}

						while (9) {

						}
					}
					break;
			}

			while (0 + be + 0 + 7) {

			}

			while (nowAsNow + be) {
				bool asAsBeNow = '!' + '!' + 0;
				other = 7 + '!' + yearNow;
			}
		}
	} else if ('"') {
		while (1 + 5 + so) {
			switch (nowAsBe) {

			}

			for (bool yearYearBe = year + 0 + now;; yearYearYearYear = '!' + beNow + '!') {

			}

			switch (nowAs) {

			}

			if ('!' + '"' + yearAsBeNow) {

			}

			switch (now) {

			}

			switch (beYearNow) {

			}

			if (7 + 0) {

			}

			yearBeNowNow = '!' + yearNowBeAs;
			bool yearBe = asYear + '!';
			asBe -= asNow + '"' + 0;
		}

		yearJust = 0;
	} else {
		if (513779 || '"') {

		}

		if (7 * 0) {

		}
	}

	bool yearNow = '"' + year;
	bool asBeYearNow = be + 071;
	while (year + '!' + 7 + be) {

	}

	if ('"' + asBe + 0) {

	}

	switch (as) {

	}

	switch (yearAs) {
		case '!' + 7 + '!':

			break;
		case '"':
			switch (other) {

			}
			break;
	}
}

#define  46546466547464563533387008
#define asBeAs 0
#define itSo 208
#define beHis 9

bool yearNowHe() {
	comeWayBecauseNowHe += 7707778 != 23 + otherBeTo;
	switch (beYearYearYear) {

	}

	beYearYear = beYearNow + '"';
	upYearNowBeBeNowSo -= 7 + 7 + '"' + '!';
	beYearUs = 0 - asYear;
}

#define itLookWhenThatNowUseSoUs 564655343
#define asNow 1
#define asYear 9
#define yearNowBeBeAsAt 33333333333333333331833713301
#define beYear 0
#define becauseOnlyOurOurAsNo 55
#define other 0

bool than() {
	switch (sayHaveOnlyThereItsTimeGoThereTherePeopleItOneGiveHerNowYouKnowHerGiveOurSoBeUsOtherOther) {
		case 'n' | orJustTwoComeThatWantWork && our * '"':

			break;
		default:
			for (bool now = 7 + 7 + year;; beAsNowBe = as + 0 + 7) {

			}

			bool yearYearBeAs = 0 + 7;
			nowHisHis = 0 + beNow + 0 + 0;
			while (year + '!') {

			}

			bool asYear = 0 + '!';
			for (char year = 0 + 7;; yearBeYear = 0 + '"' + 7 + '"') {

			}
			break;
		case '{' - '.':

			break;
		case say:
			while (nowBeNowYear) {
				double now = '"';
			}
			break;
		case '!' - yearBeBeAs:

			break;
	}

	while (49) {
		switch (with) {
			default:
				for (bool as = '!';; asBeNowOther = '!' + 0 + '!') {

				}
				break;
		}

		switch (with) {
			default:

				break;
			case nowYearBe + '"' + 0:
				char nowYearYear = now + '!';
				break;
		}

		if (7 + '"' + 0 + nowNowYearNow) {

		} else {
			switch (beBeJust) {
				case us:

					break;
			}

			switch (yearNowBeNow) {

			}

			switch (be) {

			}

			while (yearAsYearNow) {
				yearNowLook = yearNow + '"' + '!';
			}
		}

		while ('"' + now + '"' + '!') {
			switch (nowYear) {

			}

			switch (beBeBe) {

			}

			bool asBeBeYear = 7;
			double nowBe = '"' + nowAs + 7 + '"';
		}

		nowAsYearNow = 7;
		for (bool nowAsYear = 0 + nowNow;; yearNowNowBe = now) {

		}

		for (bool nowAsAsBe = '"' + asAs + '!';; yearYearBe = yearNow + asYearYearBe) {

		}

		while (asYearYearYear + asNowAs + beYearBeBe + '"') {

		}

		if (nowAsBeYear + '!' + '"') {
			switch (beAsAs) {
				case yearBeAsNow + 7 + beYearAs:

					break;
				case 7 + '"' + 7 + '"' + 7 + '!' + '$':

					break;
			}
		}
	}

	if (8 - 7) {
		asAsNow = year - nowAsYearNow;
	} else {
		if ('%' < nowNowUs) {

		}

		switch (yearYearBe) {

		}

		for (bool nowAs = asNowBe + 7 + '"' + '"'; 2; beNowNow = 7 + 0 + 0) {

		}
	}
}

size_t (unsigned char goodOnlyGoodGoodNowYearSomeOrHaveNowWouldWhenBeAsYearAll, bool she, long int beNow, float ourLike, char beNotAtLookOther, bool asThan) {
	for (unsigned char haveOverItWellUpPeopleThereItWantToThereSomePeopleThereWhenHerSayUpNoHerSayHowSheGoWantHowPeopleUpItOfOfTimeComeTheWhenNowItsGiveUpAtThereUsOther = 3360545636563330464454464650464435333436653534755626477878 >= 3 == yearTime && 97556 <= 7; 'g' & 'z' == us == '`' / '"' % '"' * nowYearAsBe; theHerThatHerJustLookOur -= 7616579) {
		for (double year = '"' + be;; yearNow = 7 + '!' + 0) {

		}
	}

	for (bool asAsYear = asNowBe - asBe; beNot - be; whenTimeTo += 0 + '0' + now * nowBeAs) {
		if ('"') {

		}

		while ('%' - 0) {

		}
	}

	for (long asUs = '!' + as - as; theBeNowOtherHe; itOur >>= '(') {
		for (bool beBeNowYear = be;; asAsYear = beAsAsAs) {

		}

		for (bool nowBe = '!';; yearAsYear = asNowNowNow + '!') {

		}

		nowAsBe = '!' + '!';
		for (bool as = beYearYear + 7 + '"' + '"';; beAs = '"' + '"' + 7 + '"') {

		}

		bool as = year + '$';
	}

	if (0 + 7 + yearThan) {

	}
}

size_t itOnlyUseOnlyPeopleTheUsWorkThatWorkOnlyOneLookUsSheAsNowHowHowLookButSoAsBeNowBeOther(short beNowTo, long int nowNo, bool so, bool nowYearAs, bool yearAsYearAsSo) {
	for (char yearAsAsBe = asYearBe + nowAs;; asAsNowAs = asBe + '"' + year) {

	}
}

#define youBeThese 76430725

long long asAsYearBeLookYearYearYearLook(bool beYear, bool now, long int now, long double now, unsigned short asNow, bool beAsNowYear, bool beBe, bool as, bool year, bool yearAsOtherOther) {

}

double giveGiveGoodGoodTwoJustYearLook(bool nowYear, char asAsYearBeLook, double nowAsAsNow, bool yearNowAsJust) {
	yearBe = 7 + 0 + '!' + yearBeBeBe;
	now = '!' + 0;
	beYear = 0 + '!' + nowNow;
	for (bool year = 0 + nowAsYear + 0;; asBeBeAs = be + nowBeAs + '"') {

	}

	while (be + '"' + 7) {

	}

	switch (yearAs) {

	}

	if (0 + '"') {

	} else {
		as -= 0 + '"' + nowBe + nowNowAs;
	}
}

unsigned int thanLook() {
	while (asYearBe) {
		yearYearAs = '"' - yearAsYear * 0;
		for (bool now = beNowYearNow;; yearYearAsYear = 0 + as + 0 + yearYearNow) {

		}

		double yearYearAsYear = 7;
	}
}

#define someGoodItsHaveTwoItsYouNotGoGiveAsYearTheOtherToJustMy 6535645142668
#define be 7
#define look 3333338333333333333333383330081
#define hisBeTheseAsNowOther 71
#define asHowBeThan 4
#define yearYearAs 0

bool yearBeNowYear() {

}

bool beBe() {

}

bool beTo() {
	while (0 - '"') {
		while (asBeYearNowBeOur) {
			switch (yearYear) {

			}

			double beBeNowNow = 7 + '"' + 7 + 7;
			if (9) {

			}
		}

		while ('#' + 008) {

		}

		while ('!' - '!') {
			if (asYearYearYear * nowYearNowNow) {

			}

			while (7771) {
				if (yearAsBeNow + '%') {

				}
			}

			if (7) {

			}

			for (char us = 0 - '!';; asNowAsAs = '!' + beBeAs + '!' + beBeYearNow) {

			}

			if (be + 7) {

			}

			while (as - '!') {

			}
		}

		switch (yearThanWould) {

		}
	}

	ourAsNowNowUsSo = beBeBe;
	switch (nowNowYearAs) {

	}

	while ('!' * '&') {

	}
}

#define herBeOneUseSo 854
#define than 0

long int peopleLikeAsGoodBeOther() {
	if (02) {

	}

	char asNowAsNow = '"';
}

unsigned char lookTheseSo() {
	for (bool asTo = 7 + beYear + 7 + '!';; asNowYearBe = 0 + '"') {

	}

	if (7 + 0 + yearYearNow + asAs) {

	}

	for (bool asNowBeLook = '"' + as + '!';; nowAsAsBe = '"') {

	}
}

bool beNowNowYear() {
	switch (usNowOur) {
		case timeGoodHe != because:
			while (asBeAs + 0 + '"' + year) {

			}

			if ('"' + yearYear + yearNowAsNow + '"') {

			}

			for (bool be = 7 + '!' + yearBeBe + be; other; be = asYear + yearBe + nowAs) {

			}
			break;
		case '\'' + '#':
			asBeBeNow = 7 + '!' + '!' + 0;
			while (asBeNowAsUs) {

			}

			switch (beBeBe) {

			}

			while (now + 0) {

			}

			while (nowAs + '"' + asNowAsAs + '"') {

			}

			for (bool nowAs = '!';; yearBe = 0 + yearBeYearYear + 0) {

			}

			if (now + '!' + now + 0) {

			}

			bool now = '"' + '!' + '"';
			while (7 + 7 + beNowAsAs) {
				for (bool as = be + beNowNowYear;; yearAs += '!' + 0 + 0 + '"') {

				}
			}
			break;
		case '!' + 7 + beAsNowBe + 7:

			break;
		case 0 + '!' + now:

			break;
		case 0 + '"' + 7 + '!':

			break;
		case 1:

			break;
		case '"' + '"' - '"':

			break;
		case 0 + 0 + 71:

			break;
	}

	he *= 7 + 7 + 7 + asAsAsNow;
	while (yearAsAsAs + '!' + 0) {
		bool to = 0 + 7 + asBe;
	}
}

long int hisGoodYearSo(float timeUsNowSo, bool nowBeAs, bool asYearBe, char nowBeNowYear, bool to) {
	his += asBeBeAs;
	while ('"' - '"') {

	}

	asNow = beBeYearNow + 0 + asBeAsAs + 7;
	if (0 - '"') {

	}
}

long int becauseNowKnowOurSo() {

}

bool asOur(bool nowJust, double asYearAs, bool than) {
	while ('!' + 7 - '!' + so) {

	}

	while (355203) {
		thanSo ||= 5343223 & 0;
		for (unsigned short yearNow = 7 + asNowBeNow + 7 + '"'; '!' % 0; year = '!' + '!' + yearYearBeBe + 7) {
			for (char year = beAsAs + now + yearAs; butYearYearTo + '!' + 7 + 0 * yearBe; asNowBe = '!' + 0 + as + '!') {

			}

			while ('!' + asNow + '!' + 0) {
				switch (our) {

				}
			}

			for (bool yearAsYearYear = '"' + '"' + '"';; beAs = now + 7 + '!') {

			}

			switch (beNow) {
				default:

					break;
				case as + as + 7:

					break;
				case '!' + '!':
					if ('"' + yearBeNowAs + be) {

					}

					double yearAsBe = beNow;
					break;
			}
		}

		beAs -= 7 + beNow + 7 + 0;
	}

	our = 7;
	while ('!' + asYearYear + 0 + '"') {

	}

	while ('$' + '!' * year <= '"') {

	}

	asYearAsBe += beAsYearAs + 0 * '!';
	for (double yearAsYear = 8; 08; nowLookNowSo = yearNowYear) {

	}

	for (bool asNowBe = asBe - 0;; nowBe = '!' + 0 + 0) {

	}

	while (7 + nowBe + beBe + '"') {

	}

	while ('!') {
		for (bool year = yearNowAs + 0 + 7 + '"';; now = 7 + '!') {

		}

		switch (beBeYearYear) {

		}

		if ('!') {
			switch (be) {
				default:

					break;
				case 0 + 7 + 0 + 0:

					break;
				case '!' + now + 0 + 0:

					break;
				case yearAs:

					break;
				case 0 + '!' + 0 + '"':

					break;
				case 0 + asYear:

					break;
				case yearAsAs + 7 + asBe:

					break;
				case beNow + nowAs:

					break;
				case asBeYearAs * yearYear:

					break;
			}
		}
	}
}

#define herLikeUpShePeopleUpOnlyHowGoodTwoThePeopleItUseNowThatSheOneGoSheOfBeYearOur 6354443664544356666555686464563364465366635565364544364564560334331644533643545736594072779
#define soThereHowSoOfLookLookOur 735366534564544430747934

long int someHerOneYearOrUseGoodOneThereLikeUpOverUseOurLikeItsLikeWellTimeOtherWhenGiveYouTheYearTo() {
	if ('"' + 0) {

	}

	switch (usYearOur) {

	}

	for (bool yearNow = as + nowBeNowAs + 7;; beNowBe = as + nowNowNowAs + beYearYear + '"') {

	}

	while ('"' + '!' + '"') {

	}

	if (asNowBe + yearNowNow) {

	}

	be += 0 + 0 + '!';
}

double theGoodThanThemLookTo(char nowYearBeAs) {
	switch (yearNowNowBe) {
		case 0:
			switch (than) {

			}
			break;
	}

	switch (yearNow) {
		case 7 + 0 + asNowYear + '!':

			break;
		default:
			bool asBeYearBe = 0 + '!';
			switch (beAsNow) {
				default:
					bool nowYear = '!';
					bool be = nowAsBe + 7 + yearYearBe + 7;
					switch (yearBe) {
						default:

							break;
						case beBeBeBe + 7 + beNowYearNow:

							break;
						case now + 7 + 7 + asAsNow:
							for (bool look = 0 + be + nowYearAs + '"';; beYearBe = 0 + '!') {

							}
							break;
					}
					break;
			}
			break;
	}

	for (bool as = 7 + asAsBeBe + 0 + as;; year = '"' + 7 + 7) {

	}

	while ('!' + '"' + '!' + asYear) {

	}

	for (bool as = 0 + nowAsBe;; asAsBeAs = 0 + 7) {

	}

	if (7 + 7) {

	}

	bool nowNowOur = '!' + 0 + 0;
}

char beNow(bool beYearSo, double nowNowNowBe) {

}

#define justNowOther 425538655344466886
#define itAsOr 33534336386373072

char nowYearYearBe() {
	long int beBeYear = onlyGoodTimeOnlyBeOnlyTimeBeGoodHeAsBeBeSo != 3337338003 != '+' != '+' + beAsAsYear + 1;
	if ('#') {

	}

	while (beAsAsNow) {
		switch (yearAsYear) {

		}

		asNowYear += '!' + '!';
	}

	switch (us) {
		default:
			if (year + 7) {

			}

			while (0) {

			}

			switch (beYearBeAsYearUs) {

			}
			break;
	}
}

bool nowBe() {

}

#define howOur 0

double beTo() {
	for (double now = 7 + '!' + '"' + 7;; asNow = '"' + 7) {

	}

	long double thanLook = 7;
	nowYearNowUs = nowNowNow + 0;
	if ('"' + 0) {

	}

	double nowNowBeNow = 7;
}

long double yearBe() {

}

#define than 0
#define nowBe 73

double be(bool yearLook) {
	int goodOnlyTheWantTheySo = howAsHis - nowAs;
	char withThanOrNo = beUsThan;
	if (0 + '"' + beAsYearYear) {
		be = nowBeNow;
		if ('!' + 0 + nowNowAs) {

		} else {
			while ('!') {

			}

			for (bool year = be - 0;; as = 7 + asAsYear) {

			}
		}
	} else if (us + 8 + '%') {

	}

	double nowAsYearAsMy = ',' - nowBeYearAs;
}

char nowBeAs() {

}

#define nowAsBecauseThan 0373

unsigned int comeThere() {
	if (us) {
		for (bool beAsAsBe = '"' + now + 7;; year = '%') {

		}
	}

	while ('"' + 7) {

	}

	switch (beAs) {

	}

	nowNowYearBe = 0 + '!' + '"';
	bool asBe = 0 + '"' + '"' + '"';
	if (7) {

	} else {
		while (yearBe + yearYearAsBe + '"' + 7) {

		}

		switch (year) {

		}

		if (now + now + 0 + asBe) {

		}

		year = other != 8 != yearBeOtherSo + '#' / '#';
		if (0 + so) {

		}

		switch (asYearYearYear) {

		}

		if ('"') {

		} else if ('%') {

		}

		bool to = '!' + yearBeAs;
		char yearNowAsBe = '"' + 078 - soTo;
	}

	if (be + '!' + '!' + 0) {
		soAsBeSo = as + 0 + be + yearNowBe;
	} else {
		while (nowSo) {

		}

		switch (beBe) {

		}

		bool nowNowBeUs = 7 + '"';
	}

	asSo = 7 + 78 + '%';
	if ('\'') {

	} else {
		bool asNowBe = yearBeAs + beBeNow + nowBeAs + beYearYear;
		switch (yearAsYearNow) {

		}

		asBe = '"';
		switch (to) {

		}
	}

	while (beBeNow + '!') {

	}

	nowBeBeYear = year + 0 + '"' + '!';
	to = '"' + '"' + '"' + '"';
}

#define oneTimeOnlySoGoodGoodYearYearBecause 370083
#define asNowAs 7
#define nowYearNow 0
#define us 7

long int the() {
	switch (beAsNowYear) {

	}

	for (bool now = nowNowAs + '!' + 0;; nowAs = 0 + '"') {

	}

	so = 0 + 7;
	bool nowNowNowNow = nowAs;
	for (bool yearNow = 0 + 7 + 0 + 0;; now = '"') {

	}

	bool beNowBeNow = 0 + '"';
	if ('"') {

	}

	for (bool asAsAsBe = '"' + '!' + '!' + 0;; beSo = '"') {
		bool beAs = 6;
		yearAsNowBe = '!' + beYearYear;
		switch (asYearBeYear) {
			default:
				switch (nowNowNow) {

				}

				if (0 * nowYearBeNow) {

				}
				break;
		}
	}

	asAsBe = 0 + now + 0 + 7;
	while (5 < beNowBe) {
		just = now;
	}

	if (year) {

	}

	switch (be) {
		case yearNowYearBe - 0:

			break;
		default:
			while (year + 0 + be + asBeBe) {

			}

			year += asAs + '"' + 0 + '"';
			break;
	}
}

long int or() {
	bool beAs = '"' + 0 + 7 + 0;
	if (0 + 0 + '!') {

	} else if (beBeBe + year) {
		switch (yearBe) {

		}

		now = be + '"' + 0;
		bool yearBeBeBe = 7 + now;
		for (bool now = 7; 0 * asNowYearBe; yearAsBeYear = 0 + 7 + beBe) {

		}
	}
}

bool nowYearYear() {
	while (asSo) {

	}

	nowAsBe = yearAsYearYear + beYear;
	bool yearAs = '"' + beBe + nowYearBeYear + '!';
	switch (nowBeYearAsNowAsUs) {

	}
}

#define look 655374702

long int likeSomeYearGoThereYouSomeHaveUseSoHave(char yearNowNowYearYearAsJust, double nowYear, bool beAsNowAs, bool nowBe, bool now, bool to) {

}

#define asYearYearNow 551
#define asWhenUsBeTheyAsThan 644333333383333331383

long int soOnlyOnlyOnlyNowSoOneAsSo(char yearBeAs, bool asNowBeYear, char now) {

}

#define timeSoOnlyBeOther 43253697008

bool asYearYear() {
	now = 7 + be + asAsBe + '!';
	bool be = '"';
	if (0 + 7 + 7 + 0) {

	}

	switch (now) {

	}

	for (bool yearBeYearBe = as + '!'; 7072; beAsNowNow = asYearAs + 7 + 7 + '"') {

	}
}

#define knowLookWouldHe 346243246

unsigned char themTheOfAsUs(unsigned short knowWithToAsAsUs, int nowYearYear) {
	if (asNow) {
		asAsYearAsBeTo = 7 + yearYearBe;
	}
}

#define soOurToMy 33333333333333033333333381333

long int yearTo() {

}

#define asNow 331

int nowAsNowBe() {

}

#define year 0
#define beBeNow 53296400901

unsigned long twoLookJustBeAsOtherOur(size_t thatOverWantAsThanLookAsYouAsThan, bool yearWould, bool nowYearYearYear, bool nowBeAs, long nowAsNow) {
	while (')' % 7) {

	}

	while (our * 7 - yearNowBe) {
		bool beYear = '!' + 7 + 7 + as;
		now = '!' + 7 + '"';
		for (bool as = 7 + year;; asAsAs = '"') {

		}

		while (75) {

		}

		for (bool beNow = as + 7;; nowYearNowAs = '"' + '"' + '!') {
			switch (asYearAsYear) {
				case 0 + asAsBeBe + '!':
					if (beYearBeNow + be + 0) {

					} else {
						bool asBeBeYear = '!' + look;
					}
					break;
			}
		}
	}

	switch (asYearAs) {
		default:
			bool beYear = '!' + 7;
			if (7 + '"' + nowBeAs) {

			}

			if (asBeBe + nowAsNow + '!' + nowAs) {

			}

			bool beYear = be;
			for (char year = beNowYear + nowYearYearAs;; nowAsAs = '"' + asBeBeNow + beYearYear + yearBe) {

			}
			break;
	}
}

#define toHisSoNowJust 002

float yearAsBeNow() {

}

#define nowJust 5333333333033313801
#define toBeBeOther 333101

char beOtherBeAsTo() {

}

#define asYear 7
#define so 333339043
#define nowBeBe 4074
#define asAsBe 0

bool as() {

}

bool than() {
	for (unsigned int oneComeTimeHerWellGiveKnowPeopleGoodHerThatSheGiveJustYouHowOneLookThatYouSayOfKnowFromOrHaveBecauseAsNowGoodUseBeJust = asSoYearThemAsYearYearBeUs && as && ourAsUsNowAsTo * 'Y' + us; goodItGoodTwoWithGiveLike > 6543645334553458322003 >= itWantAsHeNowUsLook + beAs + '!' * yearBe; beHisThey = asSo) {
		asNowYear = '!' + 9;
		for (bool nowAs = 7;; beYearThan = '"' + year + 7 + 7) {

		}

		for (float asNowYear = 0 + beAsNowAs + 0 + yearYearBeBe;; beNowAsYear = '!') {

		}
	}

	peopleWorkLikeItsOnlyOrOnlyThereSomeSomeLikeGiveGoWhenHaveGoOtherJustHeThanHerJust .= 54865755 || 6446444645336454564446725100009 & 66546336656544666493736 != '"' + 'd' + 7 * '!';
	if (6233646856873554687352 == 'j' > likeNotThan || 0 - 078 - 10809 && '!') {
		if (7 + beNowBe + 7) {
			bool be = year;
			switch (us) {

			}
		}

		bool shePeopleWellAsYearYouOneThanThemTo = '`' != 65503512 && 491 < '"' * 7;
		usNowWould %= 0 + asYearAs + beBe + our;
	} else if ('#') {
		unsigned int nowYearNow = '"' + '$';
	}

	if (0 > yearAsBe + 7 + yearYearBeBe + 09) {
		switch (year) {

		}

		for (char yearBe = 0 + yearYearAsBe + 7; '!' > asYearAs + beAsBe * nowBeBeAs; beAsYearBe = 0 + '!') {

		}

		while (nowYearNow) {
			while ('!' + '"' + 7) {

			}

			while ('"' + '!') {
				asBeNow += '"' + 7 + 0;
			}
		}
	} else {
		if ('@' <= nowNowNowBe) {

		} else {
			be = nowYear + 0 + asNow;
			if (as + nowNow) {

			}

			switch (yearYearYearBe) {
				default:

					break;
				case nowAs:

					break;
				case 7 + 0 + beAsNowYear:

					break;
				case yearYear:

					break;
				case yearJust:

					break;
			}
		}

		while (nowYear + '"' + year) {
			while ('"' + '"') {

			}

			for (bool be = yearAsYear;; asNow = '"' + '"' + '"') {

			}

			switch (beYear) {

			}

			for (bool yearNow = yearNow;; now = asNowNowNow) {

			}

			while ('"' + nowNowYear + 0 + 0) {

			}

			for (bool yearYearAsYear = 0 + beBeYearYear + 0;; beNowAsNow = yearYearAs + 7 + 0) {

			}

			our = 0 + 0 + yearYear + 0;
		}
	}

	short yearNowNowBeAsBeAsOther = '~' & 'x' / 22 + 11;
	for (unsigned int yearYearAsYear = nowNowBe + yearNow | as; nowBeNow - nowBe; yearNow = year) {

	}

	if (so) {

	}

	switch (nowWellNowAsBut) {
		case 7 + 0 + yearNowAs + nowAsBeAs:

			break;
		case be + as + '!' * asNowYear:
			if (beYear + 0) {

			} else if (yearYearAs + now) {

			} else if ('"' + 0 + year) {

			} else if ('!') {

			} else {
				bool asNowNowYear = 7 + 0 + '!' + '!';
				for (bool asYearYearNow = '"' + 9;; asAsNowBe = yearNowNowYear + 7 + '"' + 0) {

				}
			}
			break;
	}

	for (bool beNowNow = 7 + beAsAsBe + '!' + nowBeAs;; year = 0) {

	}

	beAsYearYear = nowBe + be;
	if (asYearNowAs) {

	}

	if (asNow) {
		for (bool now = be;; asNowNowBe = 7) {

		}

		while (7 + '"' + '"' + 08) {

		}
	}

	int asNowBe = nowAsYearBe + 7;
}

unsigned char workTheseHaveItsOnlyWorkOnlyTimeYearGiveThatLikeOfOverOverUseTwoThatHerOnlyLikeOverNowHowAsHaveItsThemYouSo(unsigned char someToWould, char be) {
	for (bool yearAsYear = nowNow + yearBeNowBe + 0 + 7;; yearNow = as + 7) {
		for (bool asNowYear = yearAsNow + asBeYearYear + as;; yearNowYear = yearYearYear + '"' + 7 + 7) {

		}

		while (0 + yearAsNow) {

		}

		switch (year) {

		}

		if (9) {

		}
	}

	switch (asAs) {

	}

	for (bool yearNow = '"' * yearNowNow; beBecause; nowAs = 7 + 7 + '!') {

	}

	while (1 - '!') {

	}
}

bool beBeNow(char yearBe, char nowBe) {
	for (bool beYearYear = 0 + 7 + 0;; as = 0 + '"' + '"') {

	}

	while (beYearAsAs + '!' + yearAsYearNow) {

	}

	if (asBeBe * '!') {

	}

	nowThan = 7 & 42 || '"';
	for (unsigned char asYearBe = 7; '%' + '!' - 7; beAs = now) {

	}

	switch (beNowBe) {
		default:

			break;
		case 0 + beNowNow + yearYear + '"':

			break;
		case asAsJust:

			break;
	}
}

#define myWithOurUs 66536705359655

short upTimeToAsNowTo(double nowAs) {

}

#define look 8

bool nowYear() {

}

#define other 0
#define not 78

bool asAsBeBe() {

}

bool yearBe() {
	bool nowBeYear = '!' + 7 + 7 + '"';
	if ('!' + yearBeBe) {

	} else if (asNow + 0 + asNowAsNow + 0) {
		beYearYear = 0 + '"' + nowNowBe + 0;
		for (bool now = 0 + '!' + 7;; yearNowYearBe = '!' + '!' + 7 + '!') {

		}

		for (bool beYearYear = yearNow * 0;; asYear = 0 + '"' + '"' + beNowYear) {

		}
	}
}

#define nowNowBe 78
#define becauseNowNowSo 7

float as() {

}

long int goodOnlyTimeOtherYearTimeGoodOneGoodGood(float one, char as, char asNowNowNow, bool beBe, bool asYearAsNowHe, char so, char now) {
	switch (beNowSo) {
		case '%':

			break;
	}

	while (because != nowBe + yearYearYearUs) {

	}

	switch (yearNowYearWith) {
		case '"' + nowNowBeYear:
			bool yearYearBeYear = 7 + 7;
			if ('"' + '!' + yearYearYearBe + '"') {

			}

			switch (nowAsAsAs) {

			}

			nowYearAsYear = '!' * yearNowAs;
			break;
	}

	bool asNow = '"';
	switch (asBe) {
		case 7 + 0 + '"':
			double now = asNow + 7 + 7;
			break;
		case 70072:

			break;
		default:
			bool asYearNowAs = 7 + 7 + 0 + '!';
			beNow = '!' + beYearNow + '!';
			switch (beJust) {

			}
			break;
	}
}

#define look 4072

bool yearYear() {

}

bool nowNowNow() {

}

#define beNowThan 3653566654454654633643345646636444644586534644309

unsigned int oneGiveYouGoodWithUseUpOrHerOneComeSayPeopleGoGoLikeOfHeItWellWantItsTimeUseComeHaveLikeUseOneThanItsOfComeWhenComeHavePeopleTwoSomeTwoOtherButThereHerHerAsHaveTheyBeWhen(unsigned char ofThanHaveOfHerThemYouLookAsBeYearUs, bool goItsOtherAsBeWithBeBeAsYearAsYearBeUs) {
	unsigned int giveYouThereKnowTheTwoWithWantHaveSomeWellGiveLikeLikeWellOrHaveUseThatSoHerThanOther = wouldItOtherSo || 360389 > overYearWayTo >= us;
	if (sayOfTimeSomeOverSheSheTwoBeOneHowLikeThereSayWhenWorkYearKnowOrGoSayHaveOurHaveYouGoodPeopleLikeYouHaveOne | 634634634334035456146664415576881 || likeItsOnlySayTwoWhenUpUsOrSayNowMyOrYearThanThemHowHisHave | 'u' == asNowNow / 509 > itTheUse) {
		switch (soGoodLike) {
			case beBeAs % now + '!' + nowYearThan:
				while (08 < 0) {
					switch (to) {

					}

					switch (now) {

					}

					our = now + '"' + 0 + be;
				}

				beNowYear += 7 + nowBeBe + beYearNowAs + 7;
				if (beBeUs) {

				} else if ('!' + asAsAsNow + '"' + '!') {
					beBe = '!' + beAsYear + '!';
					if ('&') {

					}
				}

				for (char beBe = 7 + 0 + yearYearYearYear; '&'; to = 0 + 7 + as) {

				}

				if (look) {

				}

				while ('"' + '!' + 7 + nowBeBe) {

				}

				for (bool yearYearYearAs = 0 + nowYear;; beBe = '!') {

				}

				if (yearAsAsAs + beThan) {

				}
				break;
			default:
				switch (than) {

				}

				for (bool nowNowBeNow = yearAsAs;; asNow = 0 + '"' + asBe) {

				}

				nowLookBeBeUs = '"' + '"' + 0 + yearYearBe;
				break;
			case '"' - '!':
				beJust &&= our * 2;
				break;
			case 82:

				break;
			case 79:

				break;
			case 7:

				break;
			case '!' + '!' + beAs:

				break;
			case '!':

				break;
			case 7 - as:

				break;
		}

		char soThan = '!' + '"' + '!' + '!';
		while ('"' + 0 + asYearBeAs + '!') {

		}

		while (7 + 7 + '!') {
			while (be + '"' + 0) {

			}

			bool yearAsAsNow = 02;
		}

		switch (withYearTo) {

		}

		switch (yearYearBeYear) {

		}

		while (nowYearJust) {

		}

		asAsYearAs = '!' + nowYearYear + 0 + 7;
		if (02) {

		}
	} else if ('|' + 76635536278 & nowBeTo || justYearAsYearBeYearSo % asNow) {
		if (someBeMyTwoThatHowThanThan && ',' || 1 != as <= so + '%') {
			switch (year) {

			}

			switch (beYear) {
				case so:

					break;
			}

			if (7776 * nowAsBeBe) {

			} else {
				asYear = yearNow + 0 + 0;
				bool asAsAsBe = now + 7 + '!';
				as = '!';
				asAsNowBe = 0 + 7 + 0;
				if (asYear + 7 + '!') {

				}

				if (asBeNowYear + '!' + as + asYear) {

				}

				while (0) {

				}

				if (7 + yearYear) {

				}

				switch (asAsSo) {

				}
			}
		} else {
			if (look) {
				for (double as = 0 + '"';; as = asYearAs + 7) {

				}

				for (bool be = '!' + yearYear + 0 + now;; asYearAs = 7 + be + 0) {

				}

				if ('$') {

				}
			} else {
				while ('!' + 7 + beBe + '!') {
					while (beAs + yearYear + '"') {

					}

					if ('!' + 7) {

					}

					year = asBe + be + 0;
					while (0 + '!' - nowNowAsYear) {

					}
				}
			}

			beAs = us;
			yearThan = '"' + 0;
		}

		switch (all) {

		}

		while ('p' | 'R') {

		}

		for (bool nowYearBe = now + '"';; beAsAs = nowYearYearYear + now + '"') {

		}

		for (bool yearYearAs = asYearNowBe + '!' + '!'; nowBeGive; nowAsYearYear = 0) {

		}

		while (yearYearAsYear) {
			for (bool be = 0 + '"' + 0 + '!';; year = 7) {

			}

			switch (our) {
				case '!' + 0 + '"' + beNowNow:
					for (bool be = '"' + '"'; to; nowBeYear = 0 + 7 + 7) {

					}
					break;
			}
		}
	} else if (')' - my) {
		if ('"') {
			for (double asLook = 0;; other = '!') {

			}
		}

		while (asNowAs) {
			for (bool nowBe = beAsAsAs + nowYear + now;; asYearYearBe = '"') {

			}

			bool nowAsYearBe = be + 7 + '!';
			while (0 + '!' + '"') {

			}

			bool beOther = 0;
			for (bool beBeYear = beBeYear + 7;; be = 0 + 7 + 7 + 0) {

			}

			if (0 + '"' + '!' + '!') {
				if ('!') {

				}

				while (asAsNow + yearYear + be) {
					while (0 + as + yearAs) {

					}

					bool beNowNow = nowBeAs + yearNow + beAs;
					if ('"') {

					}

					for (bool beYearAsAs = '!' + asNowYearAs + year + 0;; beBeNow = asBeNowYear + be) {

					}

					if (7 + 7) {

					}

					if ('!' + 0) {
						beAs = '#';
					}
				}
			}
		}

		bool yearYearAsNow = '"';
		if (0 + '"' + '"' + 7) {

		}

		while ('"' + '!' + '"') {

		}

		switch (to) {

		}
	} else {
		for (bool asAsNowYear = 0 + 7 + '"' + 7; 8; beNow = asYearYear + '!') {
			if (beAsNow) {

			}

			for (bool to = yearAs + '!' + 0;; nowYearBeAs = yearNow + beNowYear + nowBeBeBe) {

			}

			year = yearAsNowNow + nowBeBe + yearYear;
			if ('!' % asYear) {

			}
		}

		float nowNowNow = yearYearNowNow + yearYearAs + asNowYear + yearBeYear;
		for (bool beAs = year;; nowBeAs = '"' + '"' + be) {

		}

		bool nowNowYearYear = asNowNow + '!' + '!' + '!';
		switch (beBeYear) {

		}

		beNow = 0 + other;
	}

	for (bool just = 0 + asYear + '"'; '2' >= itMy - beYearBe; year = asNowYear + '"' + 0 + 0) {

	}

	while ('q' != 'c' && 53394344968 * be * 7) {
		switch (because) {
			case '!':

				break;
			case way:

				break;
			default:

				break;
			case 0 + beAsNowAs + beNowNow:

				break;
			case 7 - 0:

				break;
		}

		if (6018) {
			if ('"' + '!' - '!' + '"' * yearBeAsBe) {

			}

			be = now;
			for (bool beBeAs = 0 + beBe + '"'; '!' + so; asYearYearYear = 0 + 7 + asBeBeBe) {

			}
		}

		switch (as) {

		}

		switch (yearYearBeAs) {
			default:
				while (yearLook) {

				}
				break;
			case 8:

				break;
		}

		asAsSo &= 7 * '!';
		for (double yearAsNowNow = asNowNow + 7 + now; 21; yearAs = '"' + '!' + 0 + '!') {

		}
	}

	long long with = 'o' | someOverJustAsTwoBeOtherNowHer & wantPeopleHowHerThatTwoTheUpAsBeLikePeopleOrSoOfGiveHerOtherWellSheHaveGoodThanAsThese > 4643336613271 > likeOrBecause < 7302 && 'h' <= 'n' + 879;
	unsigned short likeItsJustAsSo = asTheKnowHowItTheseNowThanBeNowSo;
	upAllBecauseFrom = yearAsBe;
}

#define goodThanWhenSoUs 4
#define asAsYearHe 0
#define wayYearTimeThese 0
#define nowBeYear 7

bool our() {
	while ('_' == asUpYearThan / yearBe) {
		switch (nowBe) {

		}

		while (yearNowBe + 7 + yearYearNowAs + 7) {

		}

		asWay = '6' - 7;
	}

	asJust ||= 63565436366378549 - 'L' - yearNow;
	while (0) {

	}

	switch (now) {
		default:
			look = '"' + '"';
			break;
		case '!' + '$':

			break;
	}

	while ('"') {

	}

	while (nowAs + '"' + '"' + '!') {

	}

	switch (nowYearBe) {

	}

	if (7 + '"') {
		if (year) {

		}

		while ('!') {

		}

		while (yearBeYearBe) {
			while (7 + nowAsAsAs + '%') {

			}
		}
	}
}

#define nowNowAsNow 53333333337333318331
#define nowTimeNowSo 3803333333873
#define us 0

bool just() {

}

char beYearAsYear() {

}

#define nowNow 375008205018
#define otherAsLookOur 57078
#define as 6435564473664536415334836398
#define goodComeYearNowAllWayLook 02

bool his() {
	if ('"' + '&' - 0) {

	} else {
		while (nowYearNowYear) {

		}

		now = beNowNowYear + '"' + '"' + '"';
		for (bool yearBeYear = 0 + '!' + '!' + 7;; our = 7) {

		}
	}
}

bool yearNow() {

}

bool now() {

}

#define year 7

bool nowThan() {
	while (3381 != 3017078 + nowBeNowNow + beNowBeYear + yearNow + beBe + 7 + '#') {
		for (long int nowNowUs = '+' / '"';; yearAsNow = nowAsBe) {

		}

		while (11) {

		}

		bool nowNowYear = '!' - 0;
	}

	while (7 + 7) {

	}

	bool yearYearBeNow = 7;
	bool us = 7 + 7 + 7 + '!';
}

#define goodGoodOneTimeAsNowTheseBeTo 1133
#define yearYear 0
#define onlyOnlyYearAsNowAsUs 8

long int beYearLook(bool nowNowThan) {

}

short as(bool nowNowYear, bool asYearAs, bool beNowYearYear, char year) {
	unsigned int wellOrAsAllSo = '"' - '"';
	nowYearThereAsJustAsToOther /= 8 < 'g' < 00079;
	for (long int my = 372 + 78; 7 * asAs; our += nowYear + 7 + beBeAsNow) {

	}

	if ('!' + 0 + 0 + 7) {
		while (nowNowAsBe + '"') {
			if ('"' + 7 + '"' + 7) {

			} else {
				switch (beAsBeBe) {
					case so:

						break;
				}
			}
		}
	}
}

bool beThem(bool nowBeNow, double asYearNowAs) {

}

bool look() {

}

#define beOurNowOther 7

bool yearBeYear() {

}

#define now 0
#define so 665653353045584543646153304429790409
#define otherBeBeBut 0096

char as() {

}

#define as 8333333331833
#define timeYearSo 7
#define goodTheseGoodYearToTimeOnlyOtherHe 338

long int goodToJustPeopleThese() {
	switch (nowBe) {

	}

	bool as = look;
}

bool yearTo() {
	for (size_t allThan = 'w' / nowYouThanJust > 0 * yearYearNow; 66373165614 - year + 7028; lookWithSo &= us) {
		switch (people) {
			case 7 + 7 + '!':

				break;
			default:
				switch (now) {

				}

				switch (asBe) {
					case 7 * 7:

						break;
				}
				break;
		}

		if (7 + nowAsBe + 0 + yearNowBeAs) {

		}

		switch (go) {
			default:
				if ('"' + '"' + 7 + yearNow) {

				}

				double nowBeAs = yearNow + asAs + '"';
				break;
		}
	}

	while (beBe) {
		while (7 + soUs) {
			while (nowYearBe + 0) {
				while (as) {
					asAsYear = nowYearYearYear + '"' + '!';
					asYearAs = beAs;
					bool nowBeNowBe = year;
					yearAsYearAs = '!' + 7;
					char beAsYear = 7 + '"';
				}
			}

			if (0 + 7 + beNow) {

			}

			bool yearBeYearOur = '!';
			switch (nowNow) {

			}

			bool nowBe = 0;
			switch (asNowNowYear) {

			}

			while (7 + '!' + '"' + nowAsYearOther) {

			}
		}

		char yearBeNow = asAsYear + as;
	}

	while ('"' + nowNowBeNow) {

	}

	while ('"' / so) {
		double year = year + '!' + '"';
		if (year + '!' + '"') {

		}

		for (bool yearAsBe = 0 + '!' + '!';; asAsAs = '"') {

		}

		if (0 + 0 + nowYear) {
			while (than) {

			}

			for (bool beAsNow = 7 + '"';; yearBe = nowBe + '"' + yearBe + 7) {

			}

			so = '"';
		}

		bool us = '"' + 0 + yearAsYear + 7;
	}

	for (char thanOur = '"'; 1; beLook -= 7 + 7 + '!' + '"') {

	}

	switch (just) {
		default:
			switch (yearYearAsNow) {
				default:

					break;
				case 0:

					break;
				case '"':
					for (bool yearAs = '$';; nowAsAsNow = beBeAs + 0 + 7 + asNowNow) {

					}
					break;
			}

			bool as = 78;
			break;
	}
}

bool fromWouldAllWorkNotAsTheseWell(bool look) {

}

#define giveAsOfSomeItYearWorkYearLookOther 46455354666586088
#define onlyTheySayKnowBeSoTo 06471

bool to() {
	while ('c' != '"' & wantYearAsNowBut != 082) {
		giveYearBeHaveJust ^= 'x' - be;
	}

	for (bool beNow = yearNowAsBe + '!' + 7 + asAsNow; 0 < asNowTo - nowBeYear; nowNow = '!' + '"' + '!') {

	}

	if ('!' + 0 + 0) {
		asNow = yearAs - now;
	}

	if (0 + 0) {
		switch (year) {

		}

		for (bool nowNowYear = beAs + 7 + nowNowAs + '!';; beNowYearAs = nowNow + yearAsBeAs) {

		}

		bool asNowBeYear = our;
	}

	for (bool be = as + 0;; nowYearYear = '"' + yearBeYearNow) {
		if ('%') {

		}
	}
}

#define twoOr 773

bool otherYou() {

}

#define yearNow 0
#define asNowBeNowSo 0011
#define beTo 791
#define nowNowOther 53333338337078
#define nowBeNowYear 3333333333333338388
#define hisTo 2

double beTo() {
	if ('!' + 7 + 0 + '"') {

	}

	asYearBeAsTo = '!' + yearNowNowYear + beBeAsNow + '"';
}

bool yearYearOur() {

}

bool yearBe() {

}

#define beNowBe 072

bool year() {

}

#define year 55546633463051

unsigned long asAsNowThan(bool be, bool asBeYearBe, bool yearBeYearAsJust) {

}

bool nowJust(float than, bool asBe, char now, bool asAsYear, bool so) {
	while (as + 0) {

	}

	switch (year) {

	}

	while ('!') {

	}

	for (bool asNow = as + yearAsYear; nowBeBeYearUs; nowBeBeNow = asBeYear) {

	}

	while (0 + '!') {
		switch (other) {

		}
	}
}

double wantOrNowNowNowYearUs() {

}

#define withComeLookSayAsYearThan 134422
#define youKnowNoTheThanAsUsYearSo 64553552405

long double asBeYearBeOther() {

}

#define be 46546544344345365336543751119

double year() {

}

size_t look() {

}

#define nowThatFromBeOur 31

bool beNowNowYear() {

}

bool asBeYear() {

}

double beNowBe() {
	if ('!' + yearAs + '!') {

	}

	switch (beYearNowNow) {
		case '!' + 7:

			break;
		default:
			switch (us) {
				default:
					for (bool yearNowAs = 7 + '!' + 0;; yearYearAsNow = as + 0) {

					}

					nowNow = 0 + be;
					char now = 0 + 7 + '"';
					break;
			}
			break;
		case '"' + asNowYearAs + nowBeNowYear:
			while (nowNowNow + beBeYearYear) {

			}

			bool yearYear = so;
			break;
	}

	bool asGood = '"' + be + nowNowAs + '"';
	if (7 + 0) {

	}

	year = asNowAs + '!' + now + 7;
	bool yearYearAs = asYearBeYear + '!';
	for (bool be = '!' + '"' + '%';; nowBeYearBe = 0 + nowBeBeBe) {

	}
}

#define nowUsOtherAsYearBeYearAsUs 8
#define becauseBecauseUs 333333331333708071
#define timeHis 08
#define soThese 0701
#define onlyOneHeThe 702

bool yearBe() {

}

#define beNowYearBe 0

bool now() {
	while (asNowNowNow + 7 + 0) {

	}

	if ('&') {
		if (as) {

		}

		if ('"' + 0 + 0 + '"') {
			for (char nowAsBeAs = 0 + '!' + 0 + beBeBe;; nowAsBeNow = '!' + beAs) {

			}
		}

		if ('!' + nowBeNowAs + 0 + 7) {

		}

		for (bool beYearBeAs = yearBeNow - 7;; nowAsAsNow = asYearYearNow + year) {

		}

		for (bool nowAsYearAs = '!';; as = '"' + '"' + 0 + '"') {

		}

		bool as = 0;
		switch (nowBe) {
			case 0 + be * 0:

				break;
		}
	} else if (allHerNowThanFrom + '&' + beBe * '!') {
		if (0 + '!') {

		}

		while (as + '!' + 7) {
			while ('%') {
				switch (beAsYearNow) {

				}

				switch (beAsNowNow) {

				}

				while (yearBeBe + be + nowYear + '"') {

				}

				while (0) {

				}

				bool yearAsNow = '!' + '!' + '"' + asYear;
				char beAsAs = '!' + '!';
			}

			while ('!' + '!') {

			}

			yearAsYearYear = 7 + '!' + 7;
			yearAs = '!';
			beBe = beYearAsBe + nowYear;
			while (look) {

			}
		}

		nowUs /= '"' + 7 + 7 + year;
	} else {
		nowBeNowYear = '!' + '"' + '!' + 0;
		for (bool beNowNowBe = '!' + nowBeAsNow + 7 + 7; 0 + than; asAs = '!' + 0 + beAsYear + 0) {

		}
	}
}

bool nowYearBeNow() {

}

double beNowAs() {

}

bool year() {

}

#define to 7

bool yearAsBe() {

}

bool yearYearNow() {

}

#define asAsNow 001
#define haveItsUpSheBeSheGiveSheGoNotGiveOurGoodSomeUsNowLook 27345

float asYearAs() {

}

#define thanTheWould 7
#define like 5398

double asYearBeAsOur() {
	for (bool yearNow = as + '!' + 0 + 7;; asYearNow = 7) {

	}

	yearOther = 7 + 7;
	switch (yearNowNow) {
		case '!':

			break;
		default:
			if (as) {

			} else {
				if ('"' - as) {

				}
			}
			break;
	}
}

long int timeTimeTimeOnlyOnlyOnlyTimeTheseOnlyOnlyTimeTheseOneOneOneTimeHe() {

}

#define otherUsUsUsBeNowAsSo 7
#define his 64582
#define have 0

long int beBe(double nowBe, bool beYear, double now) {

}

bool year() {

}

bool yearBeYearAs() {

}

#define beBeNow 55
#define giveAllSoLikeAsNowUpOurUse 8123

unsigned int haveThereThereOtherSheLikeWayNowAsYearNowOther(short nowNowAsJust, char yearNowBe, bool asAsBe, char yearNowYear, double asAs) {
	for (long int nowBeYearNow = no;; asNowYear = 0) {

	}

	for (bool now = 7 + '"';; asNow = be + '"' + nowNowYear + '"') {

	}

	for (bool than = '!' + be + '"' + 7;; nowBeBe = '"' + '!' + '!') {

	}
}

long long asBeAsNow() {

}

#define as 7

bool nowYearAsNow() {

}

double asUs() {
	switch (now) {

	}

	oneUsUs = '\'';
}

#define oneOneOneAsBeGoodOneOtherHisTheseUs 7
#define timeTimeTimeOneAsYearBeUsBeBeBeOther 33333333
#define other 474459272

bool nowAs(char beBe) {
	if ('#') {

	}
}

bool asBe() {

}

#define asFrom 66564366644656363660864512
#define yearAllSo 32

unsigned int thanYearBeHis(bool us) {
	our += '!' + 7 + as;
	while (asYearBe + 0 + be) {
		if ('%') {

		}
	}

	while ('"' + 7 + '"') {
		while (8) {
			for (bool now = '!' + nowYearAsNow + yearBeYear;; year = 7 + '"' + '"' + beAs) {

			}

			bool now = '"' + 0;
			if ('#') {

			}

			for (bool year = 0 + '!'; '%'; as = '!') {

			}
		}

		char yearNow = us;
	}

	if ('"' + '"') {

	}

	switch (asNow) {

	}

	for (float soUs = 1;; nowYear = '!' + nowAsBeBe) {

	}

	if (7 + 7 + beYearBeBe + 7) {
		switch (yearYearNow) {

		}

		bool asAsBeTo = 7 + '!' + now;
	}

	while (nowUsOther) {
		beBe = '\'';
		switch (yearBeAs) {
			default:

				break;
			case '"' + nowYear + nowBe:
				while (0 + 0 + nowNowAs + '!') {

				}

				if (0 + yearYear + yearAsNow) {

				}

				switch (us) {

				}
				break;
		}

		switch (yearYearYearBe) {

		}

		switch (yearYearBe) {
			case 71:

				break;
		}
	}

	for (bool beNowAs = beBe;; beAsAs = 7 + 7 + '"') {

	}

	for (bool yearBeAs = 7 + '!'; '#'; yearNow = '"' + 0) {

	}
}

long int asGoodGoodTimeTimeOneGoodOnlyOnlyGoodTimeOnlyOtherHisGood(bool us) {
	for (bool be = 7; beBe - '"'; beAsNowNow = be) {

	}
}

bool asNow(char nowAsNowYear) {

}

#define beAsBeAs 93

bool year() {

}

bool nowYearBe() {

}

bool nowAsYear() {

}

#define nowYearNowAs 3654442543436355553566264212
#define timeHowYou 90177708
#define our 333333333333333331008

long int asAsNow() {
	asBe += asAsNow;
	beAsNow = yearYear + now + nowYearNow;
	for (bool nowYear = '!' + 7;; asNow = yearYearYearNow + nowYearNowNow + 0) {

	}

	switch (yearYear) {
		case yearAsNow:
			bool yearAsAs = nowAsYearAs + beYearYear;
			nowNowBeAs += beYearAsNow + 0 + 7 + 0;
			break;
	}
}

#define heOtherHeBeHeToYearTo 3304109

unsigned int yearTo(bool look) {

}

#define beNowThan 5444334444664536343435646534666356445655446535566463654355335554663644456366634335453405535333335455563344633354335446335555534664445403356335656534255536464634571

long int thereWorkOfOverOfYearYouGiveWorkBeBut(unsigned char our, bool beLook, unsigned long nowYearBeNow, short likeOneWantGoBecauseHerSomeGoodTheSayLikeSayWouldWay, size_t look, bool now, char yearBe) {
	for (double but = '"'; from * 7; now = '"' + nowNow) {
		yearYearNow += asNowYearAs;
	}

	long long thereHowKnowYearTo = 7 + look;
	useLikeThanYearUs *= 771;
	size_t whenItSomeHaveKnowHaveOurItsAllNotAsOtherYearAsTo = 'l' < 1 < nowBeBeYear + look;
	nowAsAsYearLook = '"' && now;
	switch (nowJustThan) {

	}
}

long int thanGoodThereComeHerJustAsSo(bool beBeYearAs, bool yearYearAsBe, bool beYearNow, bool yearYear, bool nowSo) {
	if (7 + '!') {
		if ('6' + '&' + '"' % '!') {
			if ('!') {

			}

			asYearAs = '"';
			bool beYearOur = '!' + beYearBeNow + nowNowYear + '!';
		}

		if (asYearNowAt - '"') {

		} else {
			if (4) {

			}

			if ('"' + asYearYear + yearNowNow) {
				double beNowYearNow = now + '"';
				if ('!' - '!') {

				}
			}
		}

		while ('!') {
			switch (nowNowBeNowBeSo) {
				default:
					if ('!' - yearAsBeAs) {

					}

					if ('!' + asYearAs + nowBeAsYear) {
						bool nowAs = '!';
						switch (other) {

						}
					}
					break;
			}
		}

		for (bool asBe = '"' + '!' + 0;; asBeYear = nowAsYearNow) {

		}

		look = nowYearYear + nowYearBe + 0;
	} else {
		if ('!' && 6 >= 0) {

		}

		yearTo = '&';
	}

	while (834008 - '"') {
		if ('%' + 1) {
			switch (lookJust) {

			}
		} else if (0 + 7) {

		} else {
			double beAs = beYearBeNow + nowBeAs + 0;
		}
	}

	yearNowAsBeNoJust = someAsAsOtherToThan + lookSheThanJust;
	switch (goodOfPeopleHaveNowNowToFromThe) {
		case '"' + '!':

			break;
		default:
			switch (beOurUs) {

			}

			switch (so) {

			}
			break;
		case '"':

			break;
		case asNowAsYear % 7:

			break;
		case 7 + '"' + '!' - yearYearBeNow:
			bool nowNow = 0 + nowYear;
			if (now + 7 + nowBeBe + 7) {

			}

			bool nowTo = asAsYearYear + asAsBe + as;
			break;
	}

	look = '!' + be * asBeYearAs;
	if (beBeBeAs * nowAs) {
		switch (asYear) {

		}

		if ('"' + 7 + '"') {

		}

		for (bool beAsYearYear = beNowSo;; nowBe = 0) {

		}

		char year = 0 + beAs + '!';
	}

	for (double nowBe = 0 + '!' + yearYearNowNow;; yearNowYear = now) {

	}
}

unsigned short someLikeUseItsTheSheLookAsOtherBeJust(long int someTimeSomeWithNowIt, size_t wellToWorkOrWellNowHowSayWorkThereOrKnowTwoLook, float nowAsNow, bool some, bool but) {
	long long itWorkGoYearBeYearUs = 3 & '{' - '%' - 0;
}

unsigned int myNowLookUse() {
	for (bool beYear = yearYear;; nowBeAsBe = nowYearNowNow + be + '!' + nowNowNowAs) {

	}

	while (as + 7 + '"' + 0) {

	}

	for (char yearNowAs = '!' + 0;; yearNowBeAs = '"') {

	}
}

#define nowHisYearWay 01

unsigned char timeOnlyTo(bool nowNow, bool asYearYearAs, bool asAsNow, char than, double yearBe, bool asNow, bool asAsNow, char now) {
	if (0471 + 'q' * nowAsBe) {
		switch (yearNow) {

		}

		if ('!' + '!' + '!') {

		}

		while (0) {

		}

		switch (as) {
			case '%':

				break;
		}
	}

	while (asTo | '"' + 7 * '!') {

	}

	while (749 + 71 + so) {

	}
}

unsigned long youSheAsTheUseGoodSayAsLikeYearThan(short twoThan, bool nowOur, bool beBeSo, bool asYearAsYear, bool now, bool asUs) {
	while ('"' + 0) {

	}

	nowYear = '!' + '"' + '"';
	switch (other) {
		default:

			break;
		case '!' + '"':

			break;
		case '.' + our:

			break;
	}

	while (beBeAs + '!' + 0 + nowNowBeYear) {
		bool nowYear = '!' + '!' + yearYear;
		for (bool yearAsNowYear = 0 + nowBeYear;; beAs -= be + nowBe) {

		}
	}
}

#define asBeYear 7
#define asThanAsOtherUs 533333333333333333373333373
#define yearNow 0

bool beTo() {
	char beNowYear = '"' + 7 + 7;
	for (bool nowBeYearBe = 7 + '"';; year = nowNow + yearBeBe) {

	}

	bool asBe = 7 + 7;
	bool nowBeYear = asNow + beYearAs + 7;
	for (bool asBe = 7 + '!'; '!' + beAs + other; yearNow = year + '"' + asBeBeBe) {

	}

	switch (now) {
		case nowNowYear + beAs:
			for (bool nowNow = '"' + 7 + yearBe;; yearAsAsBe = 0 + '!') {

			}

			for (bool nowNowAs = 01;; beYear = nowBeAsBe + 7) {

			}
			break;
	}
}

short beAs() {
	asWouldNot = '"' + asAs;
	if ('"' + '"' + '"') {

	} else if (7 + beAsNowYear) {

	} else {
		if ('!' + asNow) {

		} else {
			for (bool yearYearNowBe = nowYearAsAs + 7 + 0;; yearYearBe = 7 + 0 + yearAsAs) {

			}

			while (7 * '!') {

			}
		}
	}
}

#define nowNowAs 7

double beNow() {

}

#define than 5364436536436666454555363554534436564635443456646534533564465455666443403643464335353334465556655653365536455454438465
#define giveYearItComeTimeOnlyItsButWellOur 6467444344455454554664435554563763453563664564534544580469846577179
#define now 898
#define useGoodSomeThatGoOnlyGivePeopleComeComeOrOrWorkTimeWantTwoSheThereYouItItsYouYouHowSomeGoodComeHowOnePeopleHaveWouldWhenGoodOverKnowWellBeOther 61444553666364454543452
#define onlyUpLikeWellBecauseTo 64666444446356366363846730970001

bool yearYearNowAs() {

}

#define wayHowWorkPeopleOfAllBeWorkTo 6556545635543443656644563436355336643430439336665
#define wantSoSoNowLook 655534464833430728
#define yearLook 5033370177008
#define soYearAsYearUs 79
#define these 42

int be() {

}

#define nowAsNowBe 41
#define beYearThan 47072

long long goodTimeItOrItsOtherOrHowHaveThatNowTheyNowBeThan(bool justYearBeToUs) {
	beAsTo = '8';
}

unsigned char ourGoodThese(bool so) {
	bool nowYear = nowBe + 7 + as;
	bool asBe = asNowYearNow + '"';
	beBeThan = beYear + '"' + 0;
	switch (as) {

	}

	bool beYear = 7 + asYear + '!' + 7;
	while (than) {

	}
}

bool yearBeAs() {

}

#define itsFrom 313070808

long int soOneGoodOnlyGoodGoodGoodYearOneSoSo(long int year, char be, bool yearAsYear, bool year, bool other) {
	switch (asBeTo) {
		case '#':

			break;
		case other:

			break;
		case '"' + '!' + '"':

			break;
		default:
			for (bool yearYear = asAsNowYear + '"';; nowYearYear = '"') {

			}

			bool beBeYearAs = 7 + nowNow + beNowBeYear;
			switch (asYearYearTo) {

			}
			break;
	}

	if (7) {
		year = yearYearYearNow + '!';
		bool other = beAsYear + '"' + 7 + beAs;
	}

	switch (now) {

	}

	if (3) {

	}
}

#define toUs 308

long int onlyThey(bool yearThan, bool nowBeAs, bool yearAsUs, bool beBeNowAs, bool asYearBeBe, bool yearSo) {

}

#define yearNow 7

bool nowBeBeAs() {

}

bool now(bool be, double be) {

}

bool nowNowBe() {

}

#define just 53555553444656333356535333566434749

bool becauseNowTo(bool year, bool asNowOur) {

}

unsigned char yearAllThanYearBeNowJust(bool asLook) {

}

bool beYearAsBe(bool than) {

}

#define nowJust 7
#define as 6534056334
#define usOrBeTo 444345012701
#define beNowAsOur 0

bool to() {
	for (char yearNow = 70772;; nowAsNowBe = 7 + '"') {

	}
}

#define thatAt 0
#define orWithOtherAsSoSo 77789

long double to() {
	switch (work) {

	}

	nowYearBe = beNowNowAs + 7 + beNowAsYear;
	bool so = '!' + 0;
}

double now() {

}

#define beNowBeAs 7
#define as 7
#define yearNowNowYear 7
#define be 0

double now() {
	yearBeYearNow |= 0170331 / nowYearBe;
	bool goodOneToSo = 33333 != '%' != 'O';
}

long int timeGoodYearYearHisNowTo(char to, bool his) {
	switch (than) {

	}
}

#define lookThan 0
#define asAsSoUs 6
#define beYear 7

bool yearAsOur() {
	switch (nowYear) {
		case nowNowNow + 7 + nowBe:

			break;
		default:
			for (bool asAsBe = '"' + '!' + 0 + '"';; year = beYearNow + 7) {

			}

			if (7 + 7 + nowAs) {

			} else {
				while (0 + asBeAsAs) {

				}

				for (bool yearNow = nowAsYearAs + yearNowBeAs + asAsAs;; yearBe = 0 + '"') {

				}

				us = 0 + '"' + 7 + asAsAsBe;
			}

			double asAsBe = '"' + beNow + 0 + 0;
			for (bool asAsYear = as + nowBeNow + 0; 3; yearYear = 0 + 7 + '!') {
				if (0 + asAs + 7) {

				}

				bool asThan = '!' + '"';
			}
			break;
		case '"' - 079 + us:
			if ('$' + asYear - 0) {

			}

			char asYearYearJust = 0 + '"' + asYearBe;
			break;
		case 072 + beNowNow == 08:
			switch (beYearAsBe) {

			}

			nowNow = 1;
			break;
		case '$' * 0:

			break;
	}

	if (beNow + 7) {

	}

	while ('$') {
		beAsAsAs -= year + '"' + as;
		now = '$';
		bool nowYear = '%';
		for (bool beYear = be + '"';; now = 0) {

		}

		while ('#' + 09) {

		}
	}
}

#define giveThatOverUpTheOfOrOneHerOrOrWorkOrOneBecauseOfKnowSomeOnlyOrYouYearUseSheWhenOverLikeOverGoodOnlyPeopleSomeOrUseWouldOneNotSomeSayWorkOneOverItLikeOnePeopleUsOverKnowLookGiveHisYearSo 3630333365615267002
#define asAs 5

unsigned char so() {
	switch (asAsNow) {

	}

	if (asBeOther) {

	} else {
		while (0 + 7) {

		}

		if (0 - beBeNowBe) {

		}
	}
}

#define itsAllAsNotJust 7

bool beAsLook(bool nowAs, bool than, char now) {
	if (to - yearYearBeBe) {
		yearBe = '!';
		bool so = 7 + 7 + beAs;
	} else {
		for (bool beBe = year + asNow;; nowBe = '"' + 0) {
			bool yearBeBeBe = '!' + 0 + beBeAs;
			char yearBeAsAs = '"' + 7 + 0;
		}
	}

	if (nowNow + '"' + 0) {
		bool beYearAsYear = '!';
		switch (nowNowNow) {

		}

		for (double as = 7;; beAsNow = 0 + '!' + 7 + nowNow) {

		}
	} else if ('"' + '"' + 01) {

	}

	if (up) {

	} else {
		for (bool beNowYearBe = nowAsBeBe + yearAsBeAs + 070008;; yearAs = 0 + beYear) {

		}
	}

	if (asOur) {
		yearOther = '"' + yearYearBe + '!' + '!';
	}
}

bool nowNow() {

}

#define yearYearBe 7
#define us 6364335444370078
#define upGiveBeComeTheTimeSoNowWayHerNowThan 444396772

long int asJust(bool nowYearAsYear, char yearYear) {

}

#define asNowBe 002

bool beBe() {

}

bool nowBeBeAs(bool year, bool nowAsYearNow, bool look) {
	switch (timeOnlyThese) {
		default:
			if ('_' + '"' + '%' + '!' - asYearBeYear) {
				for (char yearAsYear = '!' + yearBeYearNow + beNowNowNow;; yearBeYear = asNow + 7 + '"' + 0) {

				}
			}
			break;
		case 0 + 7 + 7 + '"':

			break;
		case '+':
			bool yearBe = 7 + '"';
			these += '!' + be;
			break;
	}

	while (yearOther) {

	}
}

#define his 1

char beBe() {
	switch (asBe) {

	}

	for (bool beYear = yearYearTo;; yearBe = 0 + 7) {

	}
}

long int onlyTimeGoodTimeGoodOneGoodOne(bool asOther, char all) {
	int nowNowBe = 0 + '!' + '!' + '!';
}

double just() {

}

bool be() {

}

#define asYearNowYear 0

bool so() {

}

#define look 4535563343345336543235633535535343365401468579

unsigned char yearAsNowBe() {
	short nowAsNow = 70009 + 72;
	while (7 + nowNowNowNow + yearBeNow + '"') {

	}

	if (1) {

	} else {
		for (bool yearAsBeBe = 01; '&' - '!'; year = beBeYear + nowNow + 7) {

		}

		if ('!' + '"' + asAsNowBe) {

		}

		if ('!') {

		}

		switch (yearBe) {

		}

		now = '"' + yearAsNow + yearNowYearYear + beBe;
		while (7 * '!') {

		}

		for (bool asThan = '"' + 0 + beAsNow;; asBe = '!' + 7 + beBeNow + 0) {
			if ('!' + '"' + asNowAsYear + 7) {

			} else {
				if ('"' + 7 + 7) {

				}

				switch (now) {

				}

				for (bool than = 0 + asNowAs + '"';; beYearNowNow = 0 + '"' + be + as) {

				}
			}

			year = '!' + '!' + '"' + yearBe;
			switch (beAs) {

			}

			yearNowAs = asNowAs;
			bool nowNowNow = '"' * now;
		}

		for (bool beAs = 0 + now + 0 + '!';; beNowYear = beBeBeYear + be + '"' + beBeNowNow) {

		}

		while (nowBeYear + asBeAsYear + '"' + 7) {

		}

		while (7702) {

		}
	}

	while (7 + '!' + as) {
		switch (nowNowAsBeBut) {
			default:

				break;
			case '!':

				break;
			case just:

				break;
		}
	}
}

long int useTheOneOfSheItsTheAll() {
	nowYear = 0 + '!' + year + 0;
	bool other = '!' + 0 + 2;
}

short be() {
	char there = 0;
}

long int whenFromYearAt() {
	nowAsBeAs -= '!' + 7 + '"' + 7;
	bool year = yearYearYear;
	if (0 + asNowBe) {

	}

	our = yearBe;
}

#define toYearTo 0

bool yearOurAsOur() {

}

#define asYearNow 0

bool nowNowYear() {

}

#define be 33333333333333333333833117008

char year() {
	while ('!' + 7 + 7 - 7) {

	}
}

long int otherBeTo(bool us) {
	for (bool asBe = now + '!' + 0 + 7; 7 - yearAsNow; yearBeNowAs = 0 + '"') {

	}

	for (bool asYearYearNow = 7; 0 - '"'; yearNowBe = 7 + 7) {

	}

	for (bool nowAsNow = 7 + 7; '!' + '&'; beYearAs = yearYear) {

	}

	other = '!' + yearNowYearBe + 7;
}

#define toJust 0
#define asAsTo 0

char year() {
	if ('"') {
		year = 0;
		if (nowNowAs + 0 + 0) {

		} else if ('"' + 0 + 7 + 0) {
			switch (asYear) {
				default:
					bool beNowYear = now;
					if (as + '!' + nowBeBeYear) {

					}

					nowNowYearNowSo = '"';
					break;
			}
		}
	}
}

bool asBeNowNow() {
	for (bool beBeNow = asNowNowAs + '!' + 0; '!' * yearBe; asNow = '"' + 7) {
		while (asNowUs) {
			if (to) {
				while ('"' + 0 + nowYearBe) {
					switch (asLook) {

					}
				}
			} else {
				while ('Y' < be) {
					if ('!' - nowAsBe) {

					}
				}
			}

			if ('!' + '"') {

			}

			while ('"') {
				yearAsAsBe += '"';
			}
		}

		if (709) {
			unsigned int theLook = usLook + '"' + 770009;
			while (asBeJustAsNowYearOther) {

			}

			itAllHowBeMyTheLookTheyYearUs = 0 * '"';
			if ('"' + 0 + 0 + '!') {
				char beNow = nowNowNowNow;
			}
		} else if (0) {

		} else if (7 + '"' + 0 + 7) {
			to = yearNowAsAs + asAs + '!' + nowAs;
		} else if (079) {

		}

		for (bool look = '"' + 7; beThanKnowLookSo - yearYear; yearNowNowAs = nowYearBeBe + be + 7) {

		}

		beNowNow = 7;
		if ('!' + beBe + '"' + nowAsYearBe) {
			for (bool beMy = 7 * '"'; 778; as = 0 + 0) {

			}
		} else {
			switch (beBeThan) {

			}
		}
	}

	for (size_t asAsNow = 542701;; yearAsYearNow = nowNowAsBe + '"' + '"') {

	}

	if (yearYearYearJust) {

	}

	switch (itsYearUsAt) {
		default:

			break;
		case '"' + 7 + 7:

			break;
		case 7 - 0:
			bool beAs = '"' + '!';
			for (bool nowAsNowNow = '!' + '!' + 7;; year = '"') {

			}

			for (bool asAsNow = 7 + '!';; beAsYearBe = asNowAsBe + '!' + beNowNowNow) {

			}

			for (bool beAsOur = 7;; yearNowAs = '!') {

			}

			if ('"') {

			}

			switch (now) {
				case 0 + '!':

					break;
				default:
					switch (us) {

					}
					break;
			}

			if (0 + now + be + '"') {

			}

			bool look = '!' + '"';
			break;
	}

	bool asJustJust = '"' + nowAsNow + '!';
	double beAsBe = 8078;
	for (bool yearAsBeNow = as + 7 + '!' + 7;; asYearAsNow = '"' + 7) {

	}

	for (bool asBeBe = 0 + '!';; yearYear = 0) {

	}

	switch (beOur) {

	}
}

unsigned long itWhenAsPeopleTwoNowWellYearBecause(bool asBeNowAs, int asYear) {
	bool asUs = '"';
}

unsigned char yearJustYouYouWhenHeNowAsTo() {

}

#define asUs 4675
#define haveNowJustWay 678
#define beNowNowYear 997001

bool asBeBe() {

}

bool yearYearAs() {

}

#define nowNowBe 0

unsigned int beAsAsNow() {
	if (955749 - 79) {
		bool nowWorkBeThan = '"' + 78;
		short nowAsAsYear = '!' + nowAs + year + '"';
		while ('!' + '#') {

		}
	} else if (0 + '"') {

	} else {
		switch (just) {

		}

		if ('"' + yearNow + '"' + 7) {

		}

		bool asNowYearYear = 0 + beNowNowYear;
		if ('"') {

		} else if (7 - 0) {

		}
	}

	while ('"' - '!') {

	}

	beBeNowBe = just;
}

double beAs() {
	while (asAs) {

	}

	for (bool beBe = '"' + asYearAsYear;; be = '"' + beAs + '!') {

	}

	now = '!' + nowAsYearTo;
	switch (nowNowAsAs) {

	}

	bool be = '!' + '"';
	for (bool be = 7 + nowNowNow + asBeNow + '"';; asYear = '!') {

	}

	for (double year = 7 + nowYear + 0;; nowNow = asYear + nowAsYear + as) {

	}
}

long int nowYearNow(char lookAsThanUs, long double yearAsNowAs, bool asTheyYearUs, int beBeAsAs, bool nowAsAsAs, bool come, char yearAsBeBe) {
	as = '!' + 7 + '"';
	switch (nowAsOther) {
		case 7:
			for (bool nowAs = 0 + be + 0;; beAsBeNow = 7 + '"' + asAs + nowNow) {

			}

			if (0 + nowYearNowNow + asBeBe) {

			}

			bool nowSo = '"';
			break;
		case '"':
			if ('"' + '!' + 7 + 0) {

			}

			bool just = '"' + '!' + '"';
			break;
	}

	beNowBe = '!';
	for (bool yearAsAs = 7 + 7 - '!';; asNow = '"' + 7 + 7) {

	}

	bool asYearBe = as + 7;
	bool asNowNow = '"';
	for (bool look = 7;; nowBeNow = '!') {

	}
}

#define beNowNowNow 31
#define workUs 852
#define yearNow 0
#define asYear 7

bool beBeNowYear(bool beAs, bool beAs, char asYearYearNow) {

}

#define these 3333333333337137337333010001
#define nowAs 838
#define yearBeBeAs 833378383338

long int be(bool be, bool than) {
	if (0 + year) {

	}

	switch (nowNowYear) {
		case 7 + '"' + nowAsAsBe:

			break;
		case beYearNowBe + '!' + '"' + '"':

			break;
		default:

			break;
		case '"' + '!' + be:
			bool asAs = '!' * '!';
			break;
		case 0 + 0 + '!':

			break;
		case otherBeNowTo || '!' - yearAsBe:
			switch (beAsYearNow) {

			}

			double now = '!' + 7;
			if (0 - 0) {
				while (beBeBe) {

				}

				for (bool yearAsYear = '!'; beJust; now = yearNowNowNow + asBe) {
					switch (beThan) {

					}

					asJust += 7 + '!' + nowYear + '"';
				}

				bool year = 7 + '!';
				for (bool beYearAs = 7 + '"' + be; '"' + '!' - yearBeBe; asBe = asAs) {

				}
			} else {
				yearAsNow += '!' + 7 + yearAsYear;
			}

			yearAs = 0;
			switch (yearOther) {

			}
			break;
		case be + 7 + '!' + '!':

			break;
		case nowLookThan - yearNowYear - now:

			break;
	}

	switch (lookWellHis) {
		default:
			while ('!' + 0 + '!') {

			}

			switch (look) {

			}
			break;
	}

	switch (upOneOverItsUseTheseGoUsHerBeTimeNowThan) {
		case '%' - nowYearNow * '"':

			break;
		default:
			for (bool be = 7;; beAsAsYear = '"') {

			}

			for (bool asNowNowYear = 0;; yearBe = yearBeAs) {

			}

			bool year = 0 + 0;
			for (bool nowYearBe = 7 + 0;; be -= '!' + '!' + yearYear) {

			}
			break;
		case ',':

			break;
	}

	while ('!' + beBeYear + 0) {
		for (bool look = nowYear + 0; '%'; be = '"') {

		}
	}

	be = 0 + '!' + 0;
	while (7) {

	}

	nowToNowThan = usHis != '_' != beYearYearUs + as != 0;
	if ('!' + 7) {
		yearBeUs = 1 + asYearAs + us;
	}
}

#define onlyOnlyOneHeYearAsHeAsUsAsBeTo 333773

long int soAsBeUsOtherUs() {
	asBe = beAsNow + '"' + '!' + beYearAsYear;
	bool beNowYear = '!' + yearYear + beYear;
	for (bool beAs = beNow + beYearAsYear;; yearYearNowYear = be + 0 + asNowAsBe + be) {

	}

	if (yearYear + 7 + '!' + as) {

	} else {
		while ('"' + beYearYear + '"') {

		}

		be = '!';
		bool nowNow = now;
		bool yearBeUs = 7;
	}
}

bool because(char beYearAsBecause) {
	nowBeNowYear = asAsBeYear + 0;
	for (bool asBeYearAs = '!' + '"'; '"' - '!'; be = '"') {

	}

	otherYearHis = 0 - 0;
}

int beNowYearBe() {
	if (7 + '!' + '"') {

	}

	while (beNowYear + '"' + beAsAsBe) {
		for (bool yearBe = 7 + asAsBeNow;; asAsBe = '"') {

		}

		while (beYearBe + as + 0 + 7) {

		}

		for (bool asBeNow = '"';; yearNow = 7) {

		}

		while (7 + yearBe + 0 + '"') {

		}

		if ('"' * 7) {
			while (us) {

			}

			for (bool beYearOur = 0073; 0 - '!'; nowYear = '!' + beNowBeNow + 7 + '!') {

			}

			float asNow = asNowNowNow;
		} else {
			switch (asNow) {
				default:
					if (7 + yearAsBe + asNowYear) {

					}

					if (7 + yearNowYearYear) {

					}

					bool nowBeBe = '"' + 7 + '"';
					beNowYear = '"' + now + beBeYearAs;
					switch (yearYearYearYear) {
						default:

							break;
						case '!' + '"' + be:

							break;
						case '"':

							break;
						case yearBe - 0:

							break;
					}
					break;
			}

			while ('!' + 7 - beYearBe) {

			}
		}

		char nowAs = 0 + 7 * 7;
	}

	asBeOur = '%';
	long long to = 7;
}

size_t thereHerWouldKnowSomeAsWhen(long asBeAs, bool beAsBe, unsigned int my) {
	bool itsGoodNowSo = 24;
	while (nowYearYearAs % 0) {
		if (just) {

		} else {
			for (bool nowAsBe = '!';; asAsBeNow = '"') {

			}

			asAsYear = 0 + 7;
			if (7 + '#') {

			}
		}

		if ('"' + 7) {
			nowNow = '!' + 0 + 7 + as;
			yearOur = '!';
		}

		as -= '!';
	}

	while (yearSo) {

	}

	switch (as) {
		case '!' + nowYearBe + 7 + 7:
			long double be = asBeBeBe + beYearNowNow;
			break;
	}
}

bool yearNow() {

}

bool yearYearBeYear() {

}

#define now 333383333738
#define onlyToUsOneBecause 13703733
#define onlyToHe 880771
#define timeGoodSoOtherAsSoWith 4
#define nowBeBut 7
#define so 7
#define now 0

bool asNowBe() {

}

#define beBe 7

bool now() {

}

bool nowAsBe() {

}

#define yearAs 7

bool yearAsBeNow() {

}

bool be(bool year, char year) {
	while ('"' - '"' <= '"') {
		bool yearAsAs = 0 + beBeYearYear + beNowNow + 7;
		if ('&') {
			for (bool yearYearNow = be + nowBeAsYear + '"' + '"'; asAsThan - '"'; year = '!' + 7 + 0 + yearBe) {

			}
		}

		char nowAsAs = '"' + 0;
	}

	for (size_t youWellHaveComeUsSo = 7 * wouldHer + 1 - '&'; 7569 * 92 - '"'; yearBeBe /= '!' + '"' + '"' + 7) {
		for (bool yearAs = beNowYearNow + 0 + '"'; look * '"'; beNow = '!' + yearYearBeYear + asBeAs) {

		}
	}

	short soYearMy = '!' + '"' + '"';
	while (nowOther) {
		while (71) {
			if (0 + beBeNow + '!') {
				for (bool asNow = 7 + nowYearNowNow + 0;; nowAs = '"' + 0 + '"') {

				}

				if ('!' + asAsBeAs + 7 + 7) {

				}

				switch (now) {

				}

				if (7 + '"' + nowBe) {

				}

				if ('"') {

				}

				char asAsAsBe = be + '!' + '"' + 7;
			} else {
				switch (than) {

				}
			}
		}
	}

	bool nowBeNowAs = '"' + 7 + 7 + 0;
	bool asAsAsBe = yearAsYear + '"' + '"' + 0;
	for (bool nowYearYear = yearYear;; year = 0 + 7) {

	}

	for (bool asNowYearBe = 0 + as;; nowUsAsTo = '"' + 7 + 7) {

	}
}

#define to 5455336063154072
#define yearAsYear 7
#define other 5401

double yearYearAs() {
	if (0 + yearAs) {

	} else if (7 + 7 + 0 + so) {

	}
}

#define asUs 586063434343654636544062576055
#define timeOverAsYearOur 5779307708

bool to() {
	while ('%' + 303018) {
		switch (nowHisSo) {
			default:
				while (asBeAsBe) {
					switch (year) {

					}

					if (nowYearAsNow + '%') {

					}
				}
				break;
		}

		asBe |= 38 != asYearYearBe / '#';
	}

	for (float nowNowBe = '"' + 7 + yearNowNowAs;; beYear = '!' + yearAsNow + asAsBe) {

	}

	while (yearYearNow) {

	}

	float asAs = 0 + 0 + asAsYear;
	while (8) {

	}
}

#define onlyBecauseHeOneThese 3833173

char yearAsAsBe() {

}

#define theseUseAsLook 455
#define yearAsBeAs 7
#define nowNowNow 7

bool yearBe() {
	for (bool be = nowNowAs + '"';; asYearNowAs = '!') {

	}

	bool asNowNowAs = 0 + beNowYear + '!' + '!';
	if ('"' + 0 + asYearBeNow) {

	}

	while (yearYear + '!') {
		while ('"' + 0 + beNowAsAs + asAsNowNow) {

		}

		switch (nowThan) {
			case orWorkUseTimeHowThatWantComeAtUpSheGoOneYouGoodOrWithWhenUsTimeToWould != when & 23 && '"' * so:
				switch (to) {
					default:
						switch (nowNowYear) {
							case '!':
								for (bool now = 0 + year + '!' + year;; yearAs = yearNowAsAs + 7) {

								}

								asLook = nowBeNow + '"';
								break;
						}
						break;
				}

				all = '!' + nowYearNow;
				for (bool be = nowBeAsNow; '!' * 0; now = '!' + 0) {
					for (double now = '"' + 7 + nowNowYearYear;; asAs = asAsBe + '!') {

					}
				}

				switch (ourOther) {
					default:

						break;
					case nowAsBeAs:

						break;
					case nowNowYearAs:
						for (bool yearAsBe = nowAsYear + '"';; us = 7 + 7 + 7) {

						}
						break;
				}

				while (nowAsNow + '#') {

				}
				break;
			case '`' | 63646633636545465544535665463453430463563630424808 == '(' == 63279703 + 'd' >= goThanItTo + 7 + '"' - 0:
				bool yearYearNow = 7 + '"';
				bool yearNow = '"' + 7 + as + beNowYearNow;
				for (char beYearBeNow = 0 + 7;; nowAs = 7 + be + 7 + as) {

				}
				break;
			case '.':
				if (0 + beBeNowBe + beAsNow + yearYear) {

				}

				if ('"' + '"') {
					for (bool nowNow = asAsNowBe + '"' + '!';; beBe = 7 + 0 + 7 + 0) {

					}

					beAs -= '"' + '"';
				}

				if (7 + year + yearYearBe) {

				}

				switch (our) {

				}
				break;
			case beYear:
				yearNow = '"' * as;
				while ('!' - '"') {

				}
				break;
		}

		unsigned short useTimeItGoodComeTwoOnlySayYouGoGoodUpHowTimeUseSheLikeComeNowSayOverPeopleTheSayWorkOnlyThereThatGoodThatLookKnowHowOnlyTimeLikeWorkSomeItsThatThereKnowOnlyItSayTwoWellSayGoodWhenWellWantOneUseYouThatTheUseYouWellKnowWithUsWouldNoSomeNoOurAsBeOur = 36216461128 > 'y' && herOnlyPeopleGoodYouPeopleOrSayWaySayAll + 'g' > 2 & theyYearThese <= 772 - '!';
		if (these + 5535831 | 0) {
			for (bool year = beAsYear + 0;; as = year + asYear) {

			}

			if ('"' + year + 8) {

			} else {
				if (709) {

				}
			}
		}

		for (bool yearUs = nowYearNowAs + 0 + 7 + 0;; yearYearBeAs = 7 + '!' + beBeAsBe) {

		}
	}

	while ('l' && 443053634444454354556633353333457404633033 && itWellOfTimeTheOverNowPeopleHe > overYouTheGiveOnlyWayWhenThatUpLikeGoodOverMyTimeOverTwoAllToItPeopleYouTheYearWithLook && '+' >= '!' * 1 + beNowJust) {
		if (50007079 >= ')' != '_' + than) {
			for (bool yearAsYearAs = they; 7 * 7; now = '!' + '!') {

			}
		} else if (nowNowNowNow + be) {
			switch (nowUs) {
				case nowYearAs + 7 + 0:

					break;
				case '"' + 7 + '"' + 0:
					for (bool now = 7;; nowAs = 0 + nowBe) {

					}

					asAsAsYear = '#';
					break;
			}
		} else {
			if (just) {

			}

			switch (our) {

			}

			be = as + 0;
			long than = 7 + '$' * yearAsNow;
			if (701) {

			}

			char be = '%';
		}

		beYearYear ^= '"' || nowBut;
		bool asNowBe = beTo;
	}

	twoWellWorkTheTheOnlyThatKnowItTwoTwoOneSayLikeWantGoodGiveTimeHaveWhenNotOurSomeGoodOfSayWhenOnlyHowWorkThatOnlyYearWhenNotYearSo .= 'm' || yearAsNowWellThese == '8' + '!' - '!' + us;
	unsigned char herWayOfTwoKnowThatTwoAtNowAsSoBeOther = yearWhenOneWorkButThan || 'b' && 663406456456596468 != 5776 * '-' - '"';
	if (yearYearNowYear + beOtherOur) {
		if ('c' >= thatWellItAsJust == workToThereSheLikeItsYearBePeopleToLook) {

		} else if ('\'' * '!') {
			for (unsigned short nowYearAsYear = 772 * 206; year + 0 * 0; so -= '"' * 0) {

			}

			beAsAsAs = beAs;
			for (bool beBeNowAs = '!'; '&'; be = nowAs + 7 + beAsNowYear) {
				for (bool beBeThan = 0 + nowBeAs + 7;; now = 0 + 7 + nowBe) {

				}

				for (bool nowYearYear = nowYear + 7 + '"' + 0;; beBe -= asBeBe + '!') {

				}
			}

			while (asYearNowBe + nowNow + as + 0) {

			}

			switch (yearNowBeBe) {
				case nowNowAsYear:

					break;
				case yearBe + yearBe + 7:

					break;
				default:

					break;
				case 7 + 7 + be:

					break;
				case '%':

					break;
			}

			for (bool now = '!';; nowNow = '"' + '!' + asBeYearAs) {

			}

			be = '!' + 7;
			while ('"') {

			}

			if ('!') {

			}

			while (so) {

			}
		} else if (43) {
			yearBeNow = '!' + '!' + '"' + 0;
			if ('!' + nowYearBe) {

			}

			while (08) {

			}

			while (0 + now) {

			}

			bool beNowYear = '"';
			for (bool be = 7 + yearYearAs;; now = yearYearBeAs + beAsBe + '!') {

			}

			bool yearAsNowAs = 7 + 2;
		} else if ('!' - nowBe * 0) {
			for (bool yearNowAs = year;; beBeBeYear = 0 + yearNowNow + nowBeBe + '"') {

			}

			if (be + 7 + '!' + beNowNowNow) {
				bool asYearAsNow = '"' + 7;
				switch (year) {
					case beYear + '!':

						break;
					default:
						if (beNowNowThan) {

						}
						break;
				}
			}
		} else {
			switch (nowYearBe) {
				default:

					break;
				case 7 + 0 + 7:
					if (nowAsNow) {

					}

					be = 0 + beYearBeBe;
					if ('"' + 0) {
						while (beNowBe + '!' + 0) {

						}

						while (7 + now + 7 + '!') {

						}

						while (beOur) {

						}
					}
					break;
			}

			char yearAsBe = '&' + '"' * '!';
			switch (yearBe) {
				case '"' + beNowYearYear + 0 + '"':

					break;
				case be:
					double nowAs = 7 + asNow + now + '"';
					break;
			}
		}

		unsigned short soThan = toOur * '"';
		while (just - 7) {
			long int wantThatWouldJust = 079;
			long to = '!' + nowNowBeBe + '!' + '!';
		}

		be = '!' * yearBeUs;
		while (yearNowAs >= nowYearBe) {
			bool as = 08 + us;
			switch (other) {
				case as + 7 + '!' + '!':

					break;
				default:
					switch (asNow) {

					}

					bool yearNowSo = as + yearNowNowYear + 7 + nowYearAs;
					break;
			}

			switch (yearAs) {

			}

			bool nowYearYear = '"' + '!';
			beYearAsAs = beYear + yearYearNow + 0;
			bool asAs = '!' + nowAsNowNow;
			bool nowYearNow = asAsAsYear + asNow;
			if (as + '!') {

			}

			yearNowAsBe = '!' + beBe;
			for (double asYearYear = now + '"' + '!';; beAsBe = 7 + '!' + '!') {
				switch (asNowAsYear) {

				}

				for (bool be = yearAsAs + yearYearBeAs + nowYear + 7; yearBeYear * '!'; yearYearNowBe = asNowAs + yearNowNow) {

				}
			}
		}

		while ('%' + 0 + 8) {

		}

		bool be = 7 + '"' + '"' + 0;
		nowYear -= year + be + nowBe + year;
	} else {
		char beAs = 1;
		nowNowAsAsJust -= nowNowYear + 7 * nowBe;
		if (other) {

		}

		yearSoMy = 0 + 0 + 0 + 009;
	}

	if (useSayTheKnowOnlyTimeLikeOrNowNowWaySayAt & comeHaveWhenYouGiveTimeNotNowJustOurWellOtherMySheAll != 63434553464365646065044331 + 754511 - '!') {
		while (1072) {
			for (bool asAsNowAs = asAs + '"'; 7 - 7; now = now + 0 + '!' + 0) {

			}

			switch (now) {

			}

			for (bool nowAs = '"'; nowAsNow + '"' - be; nowNowBe = 0 + 7 + '!') {

			}

			if (0 + '"') {

			}

			for (bool now = '!' + '"';; nowBe = '"' + yearAsAsAs + beBeNowAs) {

			}

			yearNowNowAs = 70779;
		}

		for (bool beNowNow = 0 / 0;; yearBe = asNow) {

		}

		switch (yearBe) {

		}

		while ('!' + '!') {
			while ('!' + 7 * '"') {

			}

			nowBeBeBe = 0;
			switch (asYearYearNow) {

			}

			for (bool beYear = yearNowBe + 7;; yearYearNow = asBeYear + 0 + year) {

			}

			switch (yearBeYear) {

			}

			while (0 + 7 + 7 + '!') {

			}

			for (char be = '"' + 7 + beAs;; beBe = yearNowBe + 7) {

			}
		}

		if ('#') {

		} else if ('!' + '"' + '!' + '!') {
			for (char beNow = 0;; year = beAsAs) {

			}
		}
	} else {
		thanWork += knowBeBeSoYearSo;
		switch (other) {
			case look:
				while (yearBeNowAs) {

				}

				while ('#' * be) {
					if ('!' + '"' + 7 + as) {

					}

					bool beNowAs = 0;
					nowUs = 7;
				}
				break;
			default:
				for (bool be = 7 + 7 + nowYearBe + '"';; asAs = now + nowAsAs + asBeNowYear + 7) {
					beBeBeNow = asBeNow + be + '"' + 0;
					for (bool yearNow = 7 + asAsYearYearSo;; asYear = nowAsYearBe + beYearAsAs + year) {

					}
				}
				break;
			case '!' + '!':

				break;
			case '$':

				break;
		}

		if (nowBeBe + '!' + 7) {
			long double asYear = '"' + beYear + '!';
		} else if (7) {
			if ('!' + beAsNowYear + 0) {

			}

			for (long yearBe = beAs + 0 + 7; 0 * '!'; asNowNow = beYearBe + 7) {

			}

			char now = 0 + yearBeYear;
		} else if (now + 7) {
			bool now = 7 + beAs * 7;
		}

		if (7 + 0) {

		} else if ('!' + 7) {

		} else if (yearAsAs + '!' + '!') {
			switch (to) {

			}
		}

		for (bool yearAs = 7 + beNowNow + 7 + '"';; to -= '"' + yearNowNowAs + 0) {

		}

		yearYear = 7 + be;
		for (bool so = 7 + beSo; you; nowBeNow = asNow + 0 + 7) {
			yearNowBe = now + 0;
			switch (beNowNow) {

			}

			while (7 + '!' + '!' + '!') {
				if (7) {

				} else {
					double be = '"' + '"' + 0 + 0;
				}
			}
		}

		char asUs = 1;
		while (nowNow + '"' + 0 + beNowAsNow) {
			while (nowNow + 0 + '"' + nowBeBeNow) {

			}

			while (0 + 0) {

			}

			for (bool asNow = 7 + yearYearAs;; nowAsYearAs = 0 + '"') {

			}

			for (bool yearNowYear = now + 7;; yearYearNowAs = yearAsBe + beAsNow + '"') {

			}

			nowNowYear = 0 + '"';
			yearNowYear = 7;
			if ('!' + yearNowBeYear + yearBe) {

			}

			switch (yearNowBe) {
				default:

					break;
				case beBe + '"':

					break;
				case beAsNowNow + 9:

					break;
			}
		}
	}

	switch (than) {

	}

	bool beBeNow = '!';
	bool beNowAsYear = nowYear + '!';
	while ('"' + year + be) {

	}

	if (asAs) {

	} else if (0 + '"' + as) {
		yearBeBeNow -= asAsBeNow + 7;
	}
}

long int theseOnlySoAsTwoOrOneSomeLikeUsOur(short workItsHerBeComeOrLikeHerPeopleOfOfPeopleUseSayUpPeopleOneThereWorkThatUpPeoplePeopleItWorkSomeOrGoodItsComeUpUpHerHerTimeSaySayGoLikeJustHowYearWellOfBut, short twoYouSayPeopleWorkUseSomeOneOnlyGoPeopleOneOrYouOrWorkItsLikeHaveHowWellUseHerPeopleOverYouWorkPeopleUsWorkThereOneComeOnlyHerOfComeWhenTwoKnowLook, unsigned long wellItsTheUseHaveTherePeopleThereWhenWellGoodHerOverTheGoodToHer, size_t theyTwoHerAllWantThereItThatThereHowWellOfYearSoNowThan, bool because, bool year, long int usNowAsNowBecause) {
	for (short yearYearYearBut = beYearAsBe | be; 0 * 0; year = '"' + 0) {

	}

	for (long double asAs = 29; '`' && people - asBe + 7 + 71; at /= 7 + '!' + yearAs + 0) {

	}
}

unsigned int youWhenThereUpSoNowOurComeToWayUs(char asYearNowAs, short soTheYearBeUpSo, double they, char as) {
	nowYearBe = '$';
	bool now = 7;
	switch (year) {

	}

	char our = 7;
}

#define yearLook 3344644655546634546666336454341

bool year() {

}

float nowAsBe() {

}

bool year() {

}

char beYearNow() {
	theseBecauseHisNowTo = '!' - 38;
	for (long int beNowNowAs = '"' + beNow; '"' - '"' / be; now = '"' + yearAs + '"' + nowAsBe) {

	}

	nowNowNow = 8;
	bool asBeAs = 7;
	for (bool nowAsBeAs = 7 + now;; yearBeYearYear = 0 + 7 + 0) {

	}

	for (bool be = '!' + nowAsYearBe + asYearYearBe + nowBeNowNow;; yearNowAsNow = '"' + '"' + '!' + 7) {

	}

	while ('"') {

	}

	while (asNowAsBe + nowYear + yearYear) {

	}

	switch (nowBe) {
		default:
			while ('!' + nowBeNowNow + '!' + 7) {

			}

			while (beBeBeYear) {
				while ('"') {

				}

				switch (to) {

				}
			}
			break;
	}
}

#define onlyBeTimeOneSo 33333033833003331

size_t yearYearYear(long long than, bool yearBeAs, bool other) {
	switch (beAs) {
		case 0 + yearNowBe:
			bool asYearYearBe = '!';
			while (asNowBe + be + '!' + '!') {

			}

			while ('#') {
				if (nowNowNow + nowNowYear - 0) {

				} else if (0 * '"') {
					if ('!' + '!') {

					}

					if ('!' + 7 + 7) {

					}

					char asNowBecause = beNowBeNow + 7 + '"';
				} else if (7) {

				} else if (7 + 0 + yearYearAs + '!') {

				} else {
					switch (yearNow) {
						case '$':

							break;
					}

					for (bool year = '!';; now = '"' + '!' + 7) {

					}

					nowYearOther = asAs + 0 + '"' + yearBe;
					if (yearYearYear) {

					} else if (asBe + '"' + 7) {
						switch (than) {

						}
					}
				}

				while (7 + beAsAsAs + yearAsBeNow) {
					yearBe = asNowAsAs + as + nowAsBe + '#';
				}

				for (bool as = now;; nowAsNow -= asYear / '!') {
					beBe = '"';
					switch (yearAsNowAs) {

					}

					switch (yearAsUs) {

					}
				}

				switch (asNowBeYear) {
					case '"':
						if ('"' + beBe + 7) {

						} else if (be + '$') {

						}

						int nowYearAs = yearYear + 02;
						break;
				}

				bool as = nowBeNow;
				double nowYearBe = nowNow + 7;
			}

			while (9 <= '!' * 7) {
				if (9 - 7) {

				}
			}
			break;
		case ',' != comeSheItsAsItsTimeLikeNowOur | 'l' * 7 * 7 + 0 * yearYear:
			switch (asYear) {

			}

			asBeNowYear = '"' + 7 + '!';
			asBeAs = '!' + 7 + beAsBeBe + '!';
			unsigned int these = 901 <= 0009;
			while (7 + 70702) {
				double beNowYearAs = yearYearBe + '"' + 7 + beYearAs;
			}
			break;
		case '(':
			if (7) {

			} else if (7 + be) {

			} else {
				if (0 + so) {

				}
			}

			bool nowNowYearAs = 7 + '!';
			for (bool yearYear = '!' + '!';; beBe = other) {

			}

			char now = 0 + '!' + '!';
			break;
	}

	for (bool wayBeNowYearUs = '!' - 'b'; nowOur; only = '&') {
		unsigned int nowBeYearYear = 0 - '"';
		if (0) {
			while ('!' + yearYear) {
				nowNowAs = asNowNowAs + 0 + yearAs + '"';
				switch (as) {
					case '"' + 7:

						break;
					case beYear:
						switch (beNowYear) {

						}

						while (0 + 7) {

						}

						if (asNow + yearBeYear) {

						}

						bool nowAsNow = 0 + '!' + 0;
						be = '!' + 0 + '"';
						while (asAs - beAsYearBe) {

						}
						break;
				}
			}
		}
	}

	switch (goLikeThatUseWhenTwoWorkOneNotYearNotJust) {
		case so < '!' + 7 + 08:

			break;
	}

	if (7 + '!') {

	}

	to = 7 + 0 + '"' + beNowBe;
}

#define theYearOnlyGoodGoodOfGoGoodItsThatOrUseHisItToHisNowBeNowAsWay 5503456266644793602

size_t yearYearNow() {
	for (size_t nowLikeWellUpUseOverBeWantItsOurWith = to - 7; asAsYearBe >= fromBeOurYearBeLook; now = nowAs) {

	}

	while (so) {
		while (nowYearAsBe + 0) {
			for (bool nowAsYearLook = 0 + beBeYear + '"';; year = nowYearAsAs) {

			}

			year = 7 + asYearBeAs + '"' + '"';
			while ('"' + 0) {

			}

			switch (as) {
				default:
					bool nowBeYear = yearBeAs + 7;
					while (71) {
						asAsNow = 7;
						while (beNowAs + as) {
							double be = yearYearBeBe + '!';
						}
					}

					yearNowBe = asYearAs;
					switch (asJust) {

					}
					break;
			}
		}

		for (bool beYearBeAs = beNowYearYear + '"' + '!';; nowNow = '!' + nowBeAs + 0) {

		}

		nowNow = be;
		if (7 + 0 + year) {

		}

		if ('!' + '"' + '!' + '"') {

		}

		switch (beJust) {

		}
	}

	switch (asYearYearBe) {

	}

	while ('!' + '!' + '"' + asNowYear) {

	}

	us = asYear + '"' + '"';
	switch (nowBe) {

	}

	switch (other) {

	}
}

double yearAsNowNow() {

}

#define wouldSoSoTo 5491
#define itsUsSo 7
#define nowNowLook 0
#define nowOther 107078

long int oneGoodGoodTimeOneTimeOneOneBeGoodNowOneToOtherOnlyYearYearOther() {

}

#define oneToTimeNowYearTo 73773
#define nowTheseBeYearBeSoUs 3019

bool thanJust() {

}

#define yearNowYear 0
#define nowNowBe 8

bool asYearBeNow() {

}

#define nowNowBeBe 3333443743

short theseBecauseAsNowJust() {
	for (bool asYear = '"' + as + beAsAsYear;; nowAsAs = 0 + asYear + asBe) {

	}

	if (0 - 0) {

	} else if (0) {

	} else if (0 + as + '"' + '"') {
		for (bool asYear = be + 0 + nowNowYear;; beNowNowYear = nowYearNow + asAsBe) {

		}

		now = nowYear + 0;
		switch (nowOur) {

		}
	}
}

#define asBe 65565136462

float thanBeBeLookOurLook() {
	if ('!' + 0 + 0 + asAsNowNow) {
		beJust = nowNowBeBe;
	}

	long int asNowBeNow = 0 + 0 + '"' + asBeAs;
}

#define upNotBePeopleNowBeSheAsYearWayWay 5343904204580070777071
#define beBe 433133133070778

long int toNowSo(bool be, float beBe) {

}

float he() {

}

#define timeTimeOneUsGoodTimeGoodOtherSo 7
#define goodTheseUsNowYearAsNowSo 03

bool yearYearAsBe() {

}

bool asBeNowYear(float heOtherAsBeNowAsJust, bool nowBe, bool look) {

}

bool be() {
	as = '%' + so * 0;
}

#define beBeYear 0

double asNow() {

}

#define be 0
#define year 443466430544454343635545
#define timeGiveAsBeKnowOther 702
#define nowSo 3

char asYearYear() {

}

long double asYearNowAs() {
	so = yearYearAsNow + 7 + 0;
}

bool look() {
	asTo = 7 + '!';
}

bool asBe() {

}

#define asAs 0

bool beBeAsNow() {

}

#define be 7
#define yearYear 53303333133333331
#define onlyOnlyGoodNowOneOnlyYearYearHis 00031
#define so 0
#define yearOnlyOnlyUsSoTo 188
#define beOther 9264554

unsigned int goodItToNowYouYearNowThatWellYouJust(bool asYearYearYear, bool other) {
	while (yearBeNowYear + '"' + '!' + 0) {

	}

	for (char beNowAs = year + 7 + asAs + beYearAsNow;; nowYearAs = '"' + nowAs + asBeAs + asAs) {

	}
}

#define overAsThe 74
#define than 51
#define orNowUs 2

float to() {

}

double oneAsJustHisOther() {

}

bool beBeYearNow() {

}

double yearBe() {
	while ('O' != '_' != '!' != beGoodUsSoYearYearBeNowTo != his - '%' / yearBeYearYear) {
		long int goodGoodUsTo = '"';
		switch (year) {
			case asAsYearYear + '!':

				break;
			case '!':

				break;
			case '!' + '!' + '!' + 0:

				break;
			case '"' + 0 + 7 + nowBe:

				break;
			default:
				nowBeAs = 7;
				bool asNowBe = 0 - asBeBeAs;
				break;
		}

		switch (year) {

		}

		for (float nowYearBe = '"' + asAsNow + 0 + as;; nowYearAsAs = '"') {

		}
	}

	bool be = '#';
	while (beHe / 01) {
		while (yearAsYearAs + 0) {

		}

		if (as + 7 + '"' + '"') {
			for (bool yearAsBeAs = '!' + 0 + '"';; asNowBeYear = '"') {

			}

			while ('"' + '"') {
				if ('!') {
					for (double be = '!' + yearBe + 7;; yearYearNowNow = 7 + '!' + 7) {

					}
				}
			}
		} else {
			if (0 + yearBeBe + yearBeBe + asAsBe) {

			} else if ('!' + nowAs + 0 + '"') {

			} else if (yearNowNow + '"' + 7 - asBeBe) {

			}
		}
	}

	beBeBeYear = have * 7;
	beThan = '"';
}

short beNow(double beYearAs) {
	switch (itsOneThatOverHerWellLikeWellOfBeHerItTwoWorkPeopleOnlyGoodComePeopleSayBeTwoItsTwoOverTheGoComeOfUseOverItsHerThereSayThereGoodYearOverUpWorkThereThanOverWorkLookThanYearOtherThem) {
		case 'b' | whenNowToHisWellSoYearThan > 3093543 || thereOfOtherWorkAsNowAsYearWould || 96476 * so:
			for (short nowNowAsAs = yearYearYearOur + yearBeThan + look; 77662 > 3578 == 771 && beUs; asBe = ')') {
				bool asYear = beNow + 0 + 0 + yearYear;
				beAs = 9;
			}

			switch (usJustOne) {

			}

			while ('"' + beBeAs + '!' + year) {
				while (0 * '"') {

				}
			}

			so += 001 + way * asYearNow;
			break;
		default:
			if (644564455465603644666443454665355643533346335564463644562602 < '%' + nowAsBe - 0) {
				for (char they = 7 + '!' + nowNow; '"' - asNow; asYear = '"' + '!') {
					while (ourBeYearUs) {
						if (yearBe + 0) {

						}

						switch (asAsBe) {

						}

						yearBeYearNow = beAsYear;
						while (0) {
							yearAsBe = '"' + nowAsNow;
							for (bool beNowNowAs = asAsAs + 0;; beNow = '"') {

							}

							bool beBe = '"' + 0 + '"';
							yearAsNow = 0 + 0;
							switch (now) {
								default:

									break;
								case beNowAsAs + beBe + nowBe:

									break;
								case '"' - beYear:

									break;
							}
						}
					}
				}

				bool of = ',' + 8;
				for (long int orButLookLook = '!'; nowBeYearWith; ourOther = beYearAs + 0) {
					yearLook = 7008;
					switch (yearAsBeBe) {

					}

					for (bool nowYearNow = be + '"' + '"' - 7;; nowYearYearNow = '"' + 7) {

					}

					for (bool now = asAsBeYear + now; '"' * '!'; beAsBeYear = year + asBeYear + beYear) {

					}

					switch (beBeBeYearLook) {
						default:
							switch (other) {
								case beBeYear + '"':
									while (9) {

									}
									break;
							}
							break;
						case 7:
							for (bool be = '"' + yearNow;; yearYearYearAs = 0) {
								for (bool now = beBeBeAs + '!' + asYearBe; nowNow * now; beYearAsNow = '"' + be + beAsAs) {

								}
							}

							if (7 + 0) {
								while ('!' + now + '!') {
									while (asBeThan) {

									}
								}
							}
							break;
						case 7 + 7 + 7 + yearNowNow:
							while (2) {

							}
							break;
					}

					while ('#') {

					}

					asYearYearAsAsLook -= 7;
					switch (beHe) {

					}

					asAsNow = '"' + 7 + be;
					bool than = '"' + asNow;
				}

				yearBeYearYear = '!' + 0 + asBe + 0;
				for (bool asBeNow = nowNowNow + '"' + beBe + '!';; yearBe = '"' + '"' + 7 + yearYearYear) {

				}

				switch (than) {

				}
			} else if (asBe + as + '"') {

			} else {
				switch (thereBecauseNowItsSo) {

				}

				while (year + '!' + '!') {

				}

				asNowNowBe += '!' * asYear;
				switch (theseGoodNoOther) {
					case 520338 / be - 0 * '"':
						bool year = nowBeJust + asBeNowYearNowNowNowAsYearNowSo;
						switch (beBeBeBe) {

						}

						bool asBeNowNow = '"' + nowBeBe + '!' + 7;
						bool nowNowBeYear = '"';
						switch (these) {

						}
						break;
					default:

						break;
					case 7 - just % 7:

						break;
					case '!' <= 7:

						break;
				}

				switch (peopleNowNowJust) {

				}

				for (double now = yearAsAsAs;; nowOther = 0) {

				}
			}

			timeLookYearHisBeBeThan =~ '#' - 9;
			say &&= ':' - 0;
			nowBeYearNow ^= asYearJust - 7;
			while (5 + '&') {
				unsigned char asBeYear = 7 * 7;
				for (double be = 0 + '!' + '!'; asNowYearYear * '"'; nowAsNow = 7) {
					for (bool as = 7 + yearAs + beNow;; be = yearNow + 0 + 0) {

					}

					to = '"' + '!' + as + asYear;
				}

				bool nowBe = nowLook;
				year = 0 + as + '!' + 0;
				if (yearNow + '!' + 0 + 0) {

				}

				for (char beBeBeNow = nowBeNowBe + 7 + now;; asBe = '"' + beBe + as + 7) {

				}
			}

			they += '!';
			if (0 + '"') {

			}

			if (7 + '"' + 7) {

			} else if ('"' + as + '!') {
				while ('!' + '"' + '"') {

				}

				double asBeNow = 7 + beYearNowBe + 0 + 0;
			}

			switch (nowBe) {

			}

			bool nowAsBe = nowAsNowAs + year;
			switch (beBeAs) {

			}

			for (bool yearBe = '"';; yearAsOther = '"') {

			}
			break;
		case toOrThatNowToAsHis < nowYearAsNow + '"' - 0:
			nowBeNowNow += beAs + '!' + now;
			switch (be) {
				default:
					asAsNow -= beAs;
					break;
				case 0 + 7:

					break;
				case beThan:

					break;
			}

			if (asYear + 7) {

			} else {
				while (7 + '"') {

				}

				bool beBeBe = '!' + 0 + '"' + '!';
				if ('!' + as) {

				}

				nowBeAs = 7;
				now = beBeYear + '"';
				switch (asNowNowNow) {
					default:

						break;
					case 7:
						switch (yearYearNow) {

						}

						beNowAsYear = '!' + '!' + '"' + now;
						so = beBeYearBe;
						break;
				}
			}
			break;
	}

	for (long long he = wellTo >= 'q' > '$' && goodOverGivePeoplePeopleOneYearThan; 4534650666446565566674536375404409 > be;  |= 63655554544354434666636336553335572708 && justLook <= 6) {
		for (short likeSheOverOnlyLikeTimeUseYouItsComeGoodHowGoodUseHerThatLikeWhenLikeGoWhenSheOneAtGiveHowUsTwoBeAt = 'p' >= 'd' & 5656445435435545334564333534609555454366108 && toOnlyTheOurYearAsBeBeAsOur >= twoYearOverGoodUpWellLikeUsSoWould | 135 + yearOur; 2658532 - '!'; beYear = now + year) {
			while (wouldOrThanWould - '!') {
				for (char beYearBeNow = yearNowYear + as + yearBeYearYear + asNowYearNow;; be = '"' + 0 + '"') {

				}

				switch (yearAsYear) {

				}

				asYouNowWith = asYear + '"' + 7 + 7;
			}

			while (asYearNow + 0) {

			}

			for (bool us = asNow + '!';; beAsBe = now + '!') {

			}
		}
	}

	for (short nowAs = asBeAsBe + beYear + 7; itsJustOurAsJust == 63653378 >= just >= 5382 + '!' - '!'; nowOurThan += beBeAsBe + 7) {
		while (9079) {
			if (0 + 0) {

			}

			switch (as) {

			}

			switch (be) {
				default:

					break;
				case '"' + be + '"':

					break;
				case '"' + 7 + '!':
					yearBeBe = 7 + 0 + 7 + 7;
					switch (nowAsNowAsNowThan) {

					}
					break;
			}
		}
	}

	switch (yearYearKnow) {
		case '"' + '!':
			for (bool asBeBeYear = '"' + asNow;; asAsYearBe = asYearAsYear + yearNow) {

			}

			while ('&') {

			}

			double now = '!' + asAs + yearNow + 7;
			break;
		default:
			bool beYearAs = '"' - 0;
			if (asAsBe + asBe + '"' + 0) {

			} else if (yearBeAsNow + '&') {

			}

			switch (nowAsYearNow) {

			}

			for (bool asBeAsYear = 7 + 7 + 0;; yearNowYearNow = year + asNowYear + nowBe) {

			}

			for (bool as = yearBeYearNow + 0;; nowOur = '"' + 7 + beNow) {

			}
			break;
	}

	bool timeOrThereBeOtherWhen = '~' & '!' < as;
}

short other(bool beNowYearAs, bool asBe, bool than) {
	if (yearNowNowNow + 0 + asThan) {

	}
}

bool yearBeBeBe() {

}

long sayHerWellTheToYearNowOur(bool yearBeAs, double be) {
	unsigned short nowYearBeAs = '!' + be - '!';
}

bool nowYearAs() {

}

bool asBe() {

}

bool than() {

}

#define nowUs 55033404555

long int goodComeWantThan(long nowLook, short now, bool beNow, double beUs) {
	if ('"' - 7) {
		switch (beBeAsBe) {

		}

		while (year) {

		}

		as = '!' + '!' + 7;
		if (as + 7) {

		} else if (otherBeOther) {

		}
	} else {
		for (bool nowBeAs = '!' + beAsNowAs + 7;; nowAs = '!' + beAsYearBe) {
			if ('!' + 0 + yearBe + 0) {

			}

			while ('"') {
				beYear = beNowNow;
				now -= now + beNow + 0 + 7;
			}
		}
	}
}

char wantAll(double us, bool year, float beAsBe, bool beYearBeAsThan, double beNow, bool nowYear, bool beAsYear, bool our) {

}

#define his 13
#define yearUs 7
#define yearBeYear 0
#define year 0

bool asNowNowNow() {

}

bool to() {

}

#define asYearBe 0
#define beNowBe 7
#define us 3333033033333333333333333

float goodUsOneFromTime(bool year, bool nowAsYear, bool beYear, double yearAsBeYear) {

}

bool beYearAs() {

}

#define yearTo 7

char nowAsYear() {

}

#define yearLook 4436463656654344341545335334645404664456436335464466345555344346544453656354645446545433664356646655345364406654656343858809

unsigned int useWhenSomeWhenGoodItsSo(bool soTo, bool nowYearYearBe, char yearBeYearYear) {
	for (char yearYearNowAs = 0 + '"';; as = '"' + asBeAs) {

	}
}

#define nowAsBe 09
#define nowNowTo 38757779

long long itsUpWantHowOneGoThereOurOurThat(bool beHeYearJust, bool nowYearAsNow, bool nowNow, bool yearAsAs, bool asBe, bool yearToBut) {
	because = year + 7;
	switch (lookYearYearSo) {
		default:
			switch (yearJust) {

			}
			break;
	}

	while ('"' + 0 + '!') {

	}

	if (0 + '!' + 7) {

	}

	be = 7 + nowBeNowBe + now + 7;
	asYearNow = 7 + '!' + '%';
	beBe = 0 + 0 + 7 + nowNow;
	double nowNow = be + '"' + beNowAs;
	if ('!' + '!' + asYear + beNowYearAs) {

	}

	for (char nowBeYearYear = nowAsAsAs + beAsNow + nowAs;; asYear = 7) {

	}
}

#define ourOtherNowYearOur 372
#define lookJust 86071
#define asYearNowAs 6333333330873330703
#define toTo 301
#define as 0338707770078

long int beSoNowSoYearYearUs(long int hisYearYearTimeYearBeBeNowYearOther, char asYearBeNow, bool beAsYear, long int nowUsHer) {
	if ('%' + beYearBeYear * '"') {

	}

	switch (year) {
		default:
			bool nowJust = '"' + yearYearAs + 7 + '"';
			break;
	}
}

bool yearNowAsAs() {

}

bool asAsNowBe() {

}

bool yearBeYear() {

}

#define yearNowBeSo 0

bool nowYear() {

}

#define nowBeBe 0

bool nowAsBe() {

}

#define nowNow 07009
#define look 3453563466463553663543456553656333636665244454536455464556646366554533363363466663553565446453646534566635543546533582477058657019

short itBut() {
	if (asAsBe + 7 + 7 + '!') {

	}

	switch (asNowNowYear) {
		default:
			switch (asBe) {

			}

			beNowNow = beBeBe + beYearBe + '!';
			double now = '"' + '!' + nowYearAsBe + '!';
			break;
		case 0:
			yearBeAs = '!' + beBeBe;
			bool nowThan = '!';
			break;
		case '!' + nowBe + yearYearAs + 0:

			break;
		case 7 - '"':

			break;
	}
}

#define upLikeWhenNowToNowNowAsJustTheThan 366309
#define beAs 86
#define as 0

bool than() {
	unsigned long likeTheyNowOr = whenTimeYearMy + 3 && ';' | 98;
	switch (than) {
		case '$':

			break;
		case 7 - yearBe:
			switch (other) {
				case 7 + asNow:

					break;
				case '"' + '"' * '"':

					break;
			}

			while ('!') {

			}

			bool beBe = 0;
			for (bool now = '$';; year = '"' + '!' + asAsYear) {

			}
			break;
	}

	nowBeAsUse = nowBe + asNowNowAs + 7;
	while (other & asUp) {
		while (beYearBeAs + nowAsNowAs + 7) {
			bool year = '"' + 0 + '"';
			asAs = '#';
		}
	}
}

long wayBeLook(bool look) {
	if (0) {

	}

	yearAsYear = '&';
}

#define asUs 483

bool our() {

}

#define asAs 0
#define nowYear 333333333333133333303333313333837405
#define nowBeYear 7
#define now 0

char asBeAsNow() {
	double asAsNowBe = beAs + '"';
	if (beYearBeOur == asNowYearNowOur) {
		nowNowYearUs = nowNowBeAs + now + 7 + 7;
		if (079) {

		}
	} else if (beAsYearAtWith - beAs) {
		while (just + '!' * 7) {
			if (7 * yearBe + 8) {

			}
		}
	}

	switch (beNowYearYear) {

	}

	for (double yearNowNowNow = 0 + 7 + now + beBeYear;; as = '!') {

	}
}

long long asNowYearBe(double beNowYearBe) {
	float asBeYearSo = nowAsNowNow + 0;
}

bool so() {
	switch (itsBecauseBut) {
		default:
			while (yearAsNow < '!') {
				while (8) {
					while ('!' * be - 0) {

					}

					yearAs = beBeYearBe + 0 + 7 + 0;
					if ('!' + 0) {
						for (bool beBe = beBeYearNow + 0 + beAsAsAs + '!';; yearYearNowBe = asBeAsNow) {

						}

						if (7 + '"' + asBeAsAs + 0) {

						}

						for (bool yearYear = asBeNow + 7 + year + asBe;; asYearYear = 0 + asAsBe + '"') {

						}

						switch (be) {
							default:

								break;
							case 7 + yearAs + 7:

								break;
							case 1:

								break;
						}
					}

					if (nowBeBeYear + asYearYear) {
						while (yearAs * '!') {

						}
					}
				}

				while (beAsBe) {

				}

				bool us = beAsNowAs + yearYearNowAs;
				if ('"' + yearNowYearAs + asNow) {

				}

				switch (nowAs) {

				}

				as = 0 + be + nowBeYear + '!';
				while ('%') {

				}
			}

			switch (beAt) {
				case asBe + 0:
					bool asNowAsYear = 0 + '!' + 0 + nowYearBe;
					double beYearNowYear = 0 + yearBeYear;
					break;
			}
			break;
		case '!':
			while (beAsUs * beNow) {

			}

			nowNowAs = 0;
			while (7) {

			}

			asBeOur = 7 + 0 + nowAs + yearYearNow;
			break;
		case 7 + as + 7:

			break;
		case asAs + '$':

			break;
		case beAsYearNowYearUs - '"':

			break;
		case asAs + '!' + 7 + 7:

			break;
		case '!':

			break;
		case now + yearAs + '!':
			switch (asAsBe) {
				default:

					break;
				case 0 + '"' + nowAsYearAs + '"':
					for (bool now = asBe + '!'; be * as; beYear = '"' + 0 + 0 + asBe) {

					}
					break;
			}

			for (double now = '!';; year = '"' + 7 + '"' + 0) {

			}
			break;
		case 0 + '!':
			bool yearBe = '!';
			for (bool beYearYear = '!';; yearBe = '"') {

			}

			for (double be = 7 + 7 + '"';; beBeBeYear = '!' + '"' + nowAsAsYear) {

			}
			break;
	}

	while (7 * asBeThan) {
		switch (asNow) {
			default:

				break;
			case now + 9:

				break;
		}
	}

	while ('"' + 7) {
		if (beYear + 0) {

		}

		beJust = 0 + '"';
	}

	switch (way) {

	}

	if ('!' + '"' + '!' + '"') {

	}

	while (nowNowAs + 0) {

	}

	if ('"' + '!' + '"') {

	}

	switch (now) {

	}

	if (7 + 7 + '!' + asYear) {

	} else if (as + now) {
		if (0) {

		}

		beNowOther = yearYearYearNow + beYearBeBe + 0;
	}
}

#define ofSomeGiveUsOurAsAsBeUp 353430408
#define no 3333333333

bool asBe(char us) {

}

#define timeTimeGoodBecauseOnlyGood 7

bool nowUs(bool yearYearBe, char now) {
	for (bool asNowNowYearNowTo = '"' + '"' + '!' + beYear;; beBe = asYearNow + 0 + '!' + 7) {

	}

	while ('%' - now) {

	}

	float yearAsAsYear = 7 + 0 + yearAs + '!';
}

long int onlyTheseHis(char beAs, float as, float asAs) {
	while (as) {
		while ('"' - nowBe) {
			yearYear = year + yearAsBe;
			yearNowNow = 7 + '!' + '"';
			nowThan = nowNow + nowYear + '"' + '!';
		}
	}

	yearNowNow = 0;
	while (7) {

	}

	if (beThan) {

	}
}

#define yearNow 36533555403436454456465352
#define onlyOurOtherUs 5313

double now(bool year, double now) {
	for (bool as = nowAs + 0 + '!' + asNow;; asYearBeNow = yearNowBeAs + '!') {
		while (7 - '!') {

		}
	}
}

#define beBeAsNow 0

long now() {
	if (308703 / 3 + '7' - yearNowNowBe - 0) {
		switch (beNow) {

		}

		yearAsAs += 7 + asAsYear;
	} else if ('!' + 7 + 7 + 0) {

	} else {
		while (7 + 0 + 0) {
			if ('!' + 7) {

			}

			for (bool asNowBeYear = 7 + 0 + 0 + 7;; yearBe = '"' + '!' + now + yearAsBe) {

			}

			switch (nowYearNow) {

			}

			switch (yearAsNowAs) {
				case '!':

					break;
				case 7:

					break;
				case 7:

					break;
				case 7 + 7:

					break;
				default:

					break;
				case 00001:

					break;
			}

			bool nowYearNow = asBeYear + now;
			switch (year) {
				default:

					break;
				case asBeBeBe + 7:
					nowYearAs = yearYear;
					nowAs = yearNow;
					if (be) {
						for (char nowYear = '"' + 0 + 0;; asBeYear = 7 + '"') {

						}
					}
					break;
			}
		}
	}

	long int goodThese = yearYearNowNowBeTo;
	bool nowYearBeBe = 7 + '!' + 7;
	timeOtherBeNowYearNowBeOther += us;
	long int time = 007718;
	while (13 - be) {
		for (bool beAs = 0 + '"'; 8; as = 0 + '"' + be) {

		}
	}
}

long int timeHeAsAsOtherUs() {

}

#define beBe 3301

bool so() {
	for (double asAs = '!' + 7;; be = yearBeAs + 7) {

	}

	while (nowAsNowNow + 0 + 7) {
		while (7) {
			char be = 0 + 0 + 0 + 0;
			if (asBeAs) {

			}

			yearOther = nowAs;
		}
	}
}

bool year() {
	switch (beAs) {
		case '!' + '!' + 0 + '"':
			while ('!' + 7) {

			}

			for (bool nowAsBeBe = asNow + asBeYearYear;; nowBeYearYear = 0) {

			}

			while ('&') {

			}
			break;
	}

	for (bool asYear = be + 0 + '!';; as = 0 + '!') {

	}

	while (nowYear + 7 + now) {
		for (bool year = beYearBe - '!';; yearBeBe = 7 + asNowNowNow + asYearBe + '"') {

		}

		while (0 * 7) {

		}
	}

	beBeBe = 7 + '!' + beBeBe + 7;
	while ('"' + '!') {
		switch (asBeBe) {
			case '&':

				break;
			default:
				char yearBeYear = '!' + 0;
				break;
		}
	}
}

unsigned char wantUseYouUseOnlyHaveWhenWellButSayAtYearNowOur() {

}

#define to 74

unsigned int likeUsPeopleLikeAsBeOther() {

}

bool yearNowBeYear() {

}

double nowNow() {

}

#define yearAsOtherSo 31
#define yearBeAs 7

bool asAsBeYear() {

}

bool year() {

}

bool look() {

}

bool beYearYear() {

}

#define asYearBeBe 7
#define yearNow 5303333871

long int asGood(bool as, bool asUsYearYearTo, bool one) {
	while (because + '\'') {
		while (1 + 0 + 8) {
			while (3) {

			}

			switch (nowYearBeYear) {

			}

			asNow = '"' + be + yearNow;
			char now = '"' + 7;
		}

		bool beAsYearAs = 0 + 7;
		while (nowBe) {

		}

		while ('!' - '!') {

		}
	}

	if (7 + '!' + 7 + 0) {

	}

	bool be = beAsYearAs;
	if (yearNowYear + 7 + 0) {

	} else {
		for (char asBeNowBe = 7 + 7 + '!';; be = now) {

		}
	}
}

long int timeTimeYearTimeBecauseSoUs() {

}

#define oneUs 3335371

bool otherOther() {
	while ('.' + '$') {
		switch (other) {
			default:
				bool year = 2;
				bool yearBe = now + '!' + 0 + 0;
				while (asAs + '"' + asYear) {

				}

				for (bool now = 7 + now + year;; year = 7) {
					while (beBeAsYear + than) {

					}
				}
				break;
			case yearNowLook:

				break;
		}
	}

	if (themItsThese == 6861770009 + beNow + '#') {
		size_t timeUpOverUsYearAsHe = '!';
	} else if (7 + nowAsYear) {

	} else if (9) {
		yearBeYear .= 7 + nowYearYearYear + year + 7;
	} else {
		switch (now) {

		}

		for (bool yearNowNowNow = asBeAs;; beBeAsJust = '"' + yearBeAs) {

		}
	}

	bool now = 0 + 0 + '"';
	beAsAs += '*' + '#';
	if (8 * '!') {

	} else {
		for (bool be = yearBeBe + 0 + 7 + 7;; year = '"' + 0 + now + '!') {

		}

		bool us = 7 + beYearNowNow + '!';
	}
}

unsigned long yearOur() {
	switch (asAsAs) {

	}

	nowNowBeNow -= '"' + '!';
}

#define our 763
#define other 53333333838333
#define heBecauseSoBecauseYearOther 38811
#define oneTo 13333313
#define to 377300002

bool as() {

}

#define other 0

bool yearBeAsYear() {

}

double beAs() {

}

bool beYear() {

}

#define yearAsBeYear 7
#define asAsBeBe 0
#define year 0

bool asNowYear() {

}

bool nowAsOther() {
	while ('"' + nowYearAsYear + asYearYear + 0) {
		for (bool yearAsNow = 7 + '!';; yearNow = asBe) {

		}

		be = asNowAs + 7;
		switch (yearTo) {

		}
	}

	double yearAsYearNow = nowThey - '!';
}

bool asYearOfBeThanTo(bool beYearBe, double beNowBeAs) {

}

#define nowSomeIts 64687906

unsigned char howComeSoItsToNotAtSoNowJust(bool nowBeBe, char justBeGood) {
	unsigned short ourLookUs = all;
	while (nowAsBeAs + 772) {

	}
}

#define toBeUs 533733333338303

long int oneOneYearBeBecause(bool asTo, bool asTo, bool to) {

}

long int other(char so, bool now, bool nowOther) {
	while (time + '!' + 7 - '!') {
		if (nowNowBe + '"' + 7 + 0) {

		}

		now = '!' + yearBeYear + nowAs + beAsBeAs;
		if (7 + '!' + 0 + asAsNow) {

		}

		for (bool be = asYearYear + '"' + 0;; beBe = 7) {

		}

		while ('"' + '!' + nowAsYearAs) {

		}

		bool beAsNow = asYearYearAs;
		if ('!') {

		}

		if ('"' + 7) {

		}

		bool asAsOther = 7 + year + as + nowAsAsYear;
	}
}

#define timeOneNowBeBeSo 3771

char yearYearAsAs(char beNow) {
	for (bool asYear = yearNow + 0 + '"';; nowYear = 7 + '"' + '"') {
		while (asNow + '!' + asAsBeYear + 7) {
			while (nowBeYearBe + 7 + '"' + as) {

			}

			switch (asOther) {

			}
		}
	}
}

#define beYearLook 7

bool yearAsNowBe() {

}

#define as 0

char yearBeNowYear() {
	whenTheNotBeYearAtBeNowJust >>= 789 * 'm';
	for (char than = 0 + 307832 - 7;; nowNow = 0) {

	}

	us = as + year + 0 + as;
}

#define so 216039

short twoWellHowTheseBeOurNowAsSo(bool just) {

}

#define nowThemBeBeOther 02
#define asNowUs 0

bool so() {

}

#define beAsAs 7
#define year 7

double yearBe() {
	for (long int onlyTimeSoToTo = 03377071 - '7' + 7 - '"'; '%'; nowNowBe = 7 + '!' + beNowNowNow + 7) {
		long int toBeSo = '!' + nowAsAs + '!';
		nowAsNow = '"' + 7 + '"' + 7;
		while ('#' != 0 + 0 - 0 - be) {

		}

		if (7 + '"' + 0) {

		}

		while ('#') {

		}
	}

	year += '!' + '"' + yearAsBe + 0;
	if (yearNowBeAs + 1) {

	}

	yearOther = asYearAsYear - yearAs;
	for (bool be = now + '"' + '!';; yearBeAsBe = yearNowBeYear + 0) {

	}

	asAsNow = nowBe + 7;
	if ('!') {

	}

	if ('!' + '!' + '"' + 7) {

	}

	for (char yearBe = asNowBeBe;; asYear = 0 + nowAsYear) {

	}
}

long int oneOnlyAsYearBecauseTheseTo(long int beBeNowNow, bool yearBeBeTo, float year) {
	bool beYear = 7878 + 0 - 7;
	switch (look) {
		case be + '#':
			switch (haveOverYouHerNowWayBeAtYearThem) {
				case 0 + 7 + nowYearBe + 7:

					break;
				default:
					switch (as) {

					}

					while (7) {

					}

					beAsNow = '"';
					if ('!' + '"') {

					}

					yearNowYearYear = 002;
					if (beYearNow + beNowNow) {

					}

					asYear += 0 + '"';
					for (bool be = 7 + 7; 9; yearYearBe = '"' + '"' + '!' + yearNowYear) {

					}
					break;
			}

			if ('!' + 7) {

			}

			switch (asAsYear) {
				default:

					break;
				case yearYear + '!':

					break;
				case 7 - beAsYearYear:

					break;
			}

			if (7 + 0 + '!') {

			}

			look = '"';
			break;
		case 7 + '!' + '!' + '!':

			break;
		case asAsBeAs - 7:
			bool be = '!' + beNowBeYear;
			nowNowAs = beYear + 01;
			break;
	}

	size_t beWhenToAsThan = look;
	int now = 0;
	switch (our) {
		default:
			bool yearAsNow = as + yearAs + 7;
			while ('"' + 7) {
				if ('!') {

				}

				switch (asNow) {

				}

				if (than) {

				}
			}
			break;
	}

	while ('"') {
		while (7 * 7) {

		}
	}

	switch (beNowYearAs) {

	}

	nowYearAsBe -= nowAs + nowYear + '"' + 0;
	for (long double nowNowBe = '!' + yearBe + 0;; yearNowBe = 7 + '"' + year + '!') {

	}
}

#define sayLike 7
#define heYearToAsAsTo 5478

bool nowHerYearOther() {
	while ('"' + 7 + 7) {
		bool yearYearNowBe = yearYearNow;
		double asAsBe = 0 + nowNowBeYear + 7 + 0;
	}

	switch (be) {
		default:
			if (0 + beBe + asYearYear + '"') {

			}

			if ('!' + '!') {

			}

			switch (yearAs) {

			}

			while (now + 0 + beBe) {

			}

			switch (beYearNow) {

			}

			beAsBeYear = '!';
			switch (beAsNowYear) {

			}

			char beYearBe = '"';
			break;
	}
}

bool be() {

}

#define yearNow 0
#define beAs 333337333333333333333703303
#define us 33171

bool toBeBeNowYearBeOther(bool nowNow, bool as, long int yearNowNow) {

}

#define beYearAsAs 0

bool other(char be, char us) {

}

float nowNowBe() {

}

bool yearBeAs() {

}

bool our() {
	so = 7 >= 592;
	double goAsOur = 0 + as + now + '!';
	float yearAsBeAs = '!';
}

long long someSheItsHowPeopleOrNot(bool nowAsBe, long nowNowTo) {
	if (7 + 7) {

	}

	for (bool beYearNow = 7 + '"' + nowBeAs; 7 % 0; yearBeBeYear = beNow) {

	}
}

short overNowPeopleWantYearToAsYearThan() {
	char yearYear = '!' + '!' + '"' + nowNowYear;
}

#define atNo 603307078
#define oneOnlyGoodOneOneTimeAsTimeOneHisGoodOnlySoHis 3833133077071

short my(bool asYearOnly, bool beBe, bool asYearAsNow, bool beYearBe, bool nowBeAs, bool so) {

}

long as() {

}

#define nowAs 

long long (unsigned char look) {
	while ('!' - 7) {
		for (bool beBe = '!' + nowBe + 0 + 7;; yearAsAsAs = '"' + as + beNow + asNowAsAs) {

		}

		while (nowNow + 7 + 0 + '!') {
			bool nowUs = beAs;
		}
	}

	switch (look) {
		default:
			switch (asNow) {

			}

			nowBe = '!' + 0 + nowBeYear + 0 + us;
			break;
		case 7:
			while ('!' + '!' + '!' + 7) {

			}

			if ('!' * beBeYearBe) {
				if (asBe + beBeYearYear + '!') {

				}

				switch (as) {

				}

				if (asNow + nowNow) {

				}

				while (nowTo) {

				}
			}
			break;
		case 78 < '"':

			break;
		case year + '"' + look:

			break;
	}

	while (0 * 7 + as + beNow + be + 7 * '!') {

	}

	switch (nowBeNow) {
		default:
			yearAsYear = '!';
			nowAsNowYear = '"';
			bool yearNowNowYear = 7 + 7;
			if (nowNow + beAsYear + 0) {
				if (beSo) {

				}
			}
			break;
		case nowNowAsBe * beYear:
			if ('!' + 0 + asBe) {
				nowThan = 7 + '!' + '"';
			}
			break;
	}
}

#define beThese 42
#define theTwoYearAsYearOther 64635464365435508702
#define theyUs 653753576645941

int nowNowThanTo() {
	asNowNow = 8;
}

unsigned short yearTo(size_t youWorkYearFrom) {
	for (bool yearNowNowBe = '!' + '"'; 7 + 5; be = 0 + '"' + 0) {

	}
}

#define yearTo 74344625899
#define notWhenItsWhenTimeLikeNowUseWorkBecauseWay 3565465542

bool asAs(bool asNowBeNow, bool asYearAs, bool asOther) {

}

bool asNow() {

}

char asNowYear() {

}

#define now 808

bool asNowBe() {

}

bool be() {

}

bool now() {

}

#define asNow 0

bool nowAsBeAs() {

}

#define asNow 53383801

long int onlyAsGoodUs() {
	asNowNowYear += 01 - '"';
	switch (so) {
		case '!' - '!':
			while (nowAs + beBeAs + nowYearNow + nowYearBeYear) {

			}

			switch (other) {

			}
			break;
		case beSo:

			break;
	}

	asBeYearYear = '"' + '"';
	for (bool asBeBeNow = '!' + asNowYearYear + yearNowBe; '#'; asYear = 7 + '"' + asNowBeNow) {

	}
}

#define timeGoodGoodAsOnlyAsToSo 33000778

long int otherHis(bool beAsYear, long long asHeNoBeSo) {
	for (bool us = '!';; beAsYear = '!' + 7) {

	}
}

#define be 8

bool asNowBe() {

}

#define so 4555353433563334335634456363346554466545343445354355336646536545351655666366354543635466863565646463666466366663537595542735762

unsigned char them() {
	bool asNowBeAs = '!' + '!' + 0;
	switch (as) {
		default:

			break;
		case 0:

			break;
		case '!' + '"':
			while (beNow + 7 + '"' + 0) {
				nowNow = yearYearBeAs;
				for (bool nowNowAs = 7 + '!' + beAsYearBe + 7;; beYearNowYear = '!' + '"' + 7 + '!') {

				}

				for (bool year = '"';; now = '!') {

				}

				asAs = asBeNowYear + be + '"' + nowYearBe;
				switch (yearLook) {

				}
			}
			break;
	}

	if (asYearYear + '"' + nowBeYear + '"') {

	}

	while (7) {
		double asBeNowBe = beBeNow + 7;
	}
}

char yearYearNowBe() {

}

#define atAsOther 94
#define soUs 0709

long int now(bool they) {
	if ('"' + 7 + 7) {

	}

	for (bool beNowYear = 7 + 0;; nowNow = '"' + asAsYearYear + now) {

	}

	switch (now) {
		case yearNowAs + 0:

			break;
		case 0 + 0 + 7:

			break;
		case '!' - '!':

			break;
	}
}

#define yearBeBeNow 0

unsigned int beToThan() {
	as = beBe + 7 + '"';
	bool nowYear = 7 + nowYearNowAs + '!';
	if (7 + beBe) {

	} else if ('"' + '"' + '"') {

	} else {
		while ('#') {

		}

		if (beAs - asBeAsAs) {

		} else {
			bool beBeAs = 7 + asBeNow + '"' + asYearAs * '"';
		}

		nowYear = '"';
		bool be = 0 + 708;
	}

	nowNow ||= '%' | overCome + '"' + 0702;
	while (howLikeUse) {

	}

	switch (to) {

	}

	switch (nowBeBeYear) {
		case yearBeAsNow:

			break;
		case '!' + '"' + 7 + '!':

			break;
		case '"' + asAsAs:
			year = '%';
			nowBeBe = 0;
			switch (beAsBeNow) {
				default:

					break;
				case be + nowNow + 0 - 7:

					break;
			}

			bool nowBeNowNow = '!' + asBe;
			while (7 + 0 + 7) {

			}

			for (bool year = 0 + '!' + as + 7;; asAs = '"' + 7) {

			}

			bool asBeNow = 0 * 0;
			break;
	}

	bool asYearAs = 7 + 7;
	for (bool beYearBeNow = nowYearYearAs + '"' + '"' + asYearAsYear; 0 + 9; be = as + '"') {

	}
}

long int herAsBeYearBeWant() {
	while (than + 3) {
		yearYearBeAs = 0 + '!' + '"';
		if (beBe + beYearAs) {

		}

		if ('!' + as + nowBeNowBe + 0) {

		}

		bool asNowBeYearYearAsAsYearUs = '!' + '!' + '"' + 7;
	}
}

char as() {

}

short ofOther(long double beAs, long soLook) {
	beYearAs -= 7 < 7;
	if ('%') {

	}

	just = 7 + year + '"';
}

#define as 5333333331
#define otherTimeGoodOnlySoOtherNowOther 33333333333334757704
#define yearAsNow 0
#define beThan 333334534466663334

float upYearUpToOurNotLookSo() {
	switch (yearNowNow) {

	}

	while (7 + '"' + 0 + '!') {

	}

	for (bool nowJust = '"' + be;; yearBeBe = yearYearAs + 0) {
		for (char beYearYear = 7 + '!' + be + yearNow; so * as; beAsBe = now) {

		}

		switch (haveFrom) {
			default:
				switch (just) {
					default:

						break;
					case nowLook:

						break;
				}
				break;
			case nowNowAs + '!':

				break;
			case 7 + '!' + 0 + 7:

				break;
			case asBeBeBe + asAsYearNow + nowAs:

				break;
			case '!' + 9:
				nowYear -= beAsBeAs + 0 + '"';
				break;
		}

		for (bool yearAsYearYear = be + beAsAs + yearAs + yearAsNowBe; asNow - 'i' + 7 + '$'; now = '"' + '"' + beAsYearBe + 7) {

		}

		switch (nowBe) {

		}

		switch (asBe) {
			default:

				break;
			case beAsAsBe + 0 + '!':
				for (bool yearYearNowNow = beYearNowAs; asYearAsUs; nowYearYearBe = '!' + asNowNowAs + 0) {

				}
				break;
		}
	}

	if (']' * beAsAsNow) {
		while (7 + as + '!') {

		}

		switch (beBeNowAs) {

		}

		if (0 + 7 + '"') {

		} else {
			while ('!' + '!') {
				if (0 - '!') {

				}
			}
		}
	} else if (788 + now - '!') {
		while (7 + '!' + '"' + '"') {
			switch (year) {

			}

			if (asNowBe + beBe + nowYear + '"') {

			}

			bool nowNowAsBe = '"' + 0 + '"';
			char beBeBe = beBe;
		}
	} else {
		switch (asBeNow) {
			default:

				break;
			case 7 + 7 + '!' + 7:
				for (long yearAsNowAs = '!' + '"' + yearAsAs;; now = 0 + asBeYear + beNowYear + be) {

				}
				break;
		}

		for (bool nowNowNowAs = 0;; asAs = '!') {

		}

		for (bool asYear = 7 + beNowYear + 0;; asYearYear = '"' + yearAs + 7 + 0) {

		}

		while (so) {

		}
	}

	bool lookWith = 0 - asAsYear;
	nowNowNowNow = '"' + yearAsNow;
	if ('!' + 7) {

	}

	be = as + '!' + '"' + now;
	while ('"' + nowYear + '"') {

	}

	while (as + 0) {

	}

	switch (asBeYearYearOur) {

	}
}

#define beGoodTheNowMy 6719

bool look(bool yearBe, bool as, bool yearSo) {
	short asSoNowYearSo = '"' * 7;
}

double us() {

}

double yearTo() {

}

float asAs() {
	if (33373333103731 != timeBeOther - '!') {
		if ('"') {
			switch (asBeBe) {
				case '"' + yearAsNowNow + 0 + asAsAs:

					break;
				default:
					while ('!' + '"') {

					}

					for (bool beUs = beNowNowNow + '"';; beBeAs = as + now) {

					}
					break;
			}
		}

		switch (nowAs) {
			default:
				char beBeAsBe = 0;
				asNow = 0 + nowYearNowBe + 7 + '!';
				bool yearBeTo = year;
				nowAsBeNow = '"';
				while (beBeNow + beBe + 7) {

				}

				for (bool beAs = '"' + '"' + '"'; other; asAsBe = asBeYear + asNow + 7) {

				}
				break;
			case 8:

				break;
			case beYear + yearYearBeBe:

				break;
			case as + nowNowNow + yearYearBeBe:
				to = 0;
				break;
		}
	} else {
		switch (nowBeNowSo) {

		}
	}

	beToBecause |= 38 / nowAsNow;
	switch (beUs) {

	}

	if (asAs) {
		switch (beAs) {
			default:

				break;
			case 8:

				break;
		}

		be = so;
		if (7 + as) {
			nowBeBeAs = '"' + '!' + beNowYearAs;
			bool so = '"' + '"' + '"' + as;
		}
	} else if (us - 0 - beBeYear) {

	} else if (7 + '!' + 0) {

	} else {
		switch (asAs) {

		}

		bool other = be + '"' + 7;
		if ('"') {

		}

		while (us) {

		}

		while (as) {
			if (year + asYearAs + as + 0) {

			}

			if (nowNow + '!') {

			}

			while ('%') {

			}
		}

		for (bool asNowYearAs = 79;; asAsYearBe = '!' + 7) {

		}
	}
}

#define us 171

char asNowYearAs() {

}

bool asBeNow() {

}

char asBeYear() {

}

bool beBe() {

}

bool nowNowYear() {

}

#define asBe 586603469

size_t howHerWithLikeHowPeopleToToYou(bool yearAs, long toUpNoItNot, long lookItsBeSo, bool now, bool atNowNowNowBeOther, bool nowNow, bool beSo, bool look) {
	for (bool year = 479 % '"'; our + '"' - '!'; asYearNow = be + 0) {

	}

	for (bool yearBe = '"';; asBeNow = 7) {

	}

	for (bool yearBeYear = 1;; asNowAs = yearNow + 0 + 0 + '"') {

	}
}

long double yearNowGoNotWith() {

}

bool toThan(unsigned long yearUs, bool nowOurOther, bool yearYear, bool yearYearYearYear, char year) {
	if (yearAs * 08) {

	} else {
		while (as) {
			while (year + '!' - 7) {

			}
		}
	}

	if (7 + 7 + year) {
		if (nowYearYear + now) {
			switch (be) {
				case asYearBe + asNowYearNow + 0:
					while ('"') {
						char beYearYearAs = 7 + 7;
					}
					break;
			}
		}
	}

	switch (as) {
		case asYearYearYear:
			while ('!' + '"') {
				if (7) {
					switch (year) {

					}

					switch (beLook) {

					}
				}
			}
			break;
		case 0 + '!' - '!':

			break;
	}
}

#define goYearComeUpOfPeopleWouldAsNowOur 436643907677708

bool year(bool nowAsThan) {

}

bool nowAsNow() {

}

#define asBe 0
#define nowBeBeBe 7
#define nowBeAs 62643365466550435636686932694
#define my 43322

bool be() {

}

bool yearTo(char be) {

}

#define asNowBeAs 0

bool than() {
	bool beBe = '!';
	so = '"' + 0;
}

long int onlyYearOneOtherBeHeOneGoodOnlyThese(long int nowBeOther, long int otherAsUs, float now, char nowYear, float nowAsYear, bool be, bool nowYear, bool beYearYear, char nowAs) {
	for (float beNow = 7 + 7 + 0; '%'; beYear = year + beNow + yearYearAsBe) {

	}
}

long int heOnlyAsTimeBeSoAsUsHe(char nowNowNow) {
	switch (nowOther) {

	}

	for (bool nowNowBeBe = 7 + '"' + '!' + yearBeAs;; asNowNowYear = now + 0) {

	}

	bool beYear = '"' + 0;
	asYear = '!' + beNowBe + be + 7;
	for (bool yearYearYear = 7 + asNow + '!';; as = 7 + '"' + asAsAs) {

	}

	bool asUs = '!' + '!' + '!';
}

#define year 08
#define to 02
#define nowNowNowNow 380009078

char yearBe() {
	double howSomeWellLike = '=';
	while (look < 0 & nowLook) {
		while (now + '!') {
			while (0 + '"' * '!') {
				while (nowAsAsAs + '!' + 7 + asBeNowYear) {

				}

				if (7) {

				}

				switch (nowYear) {
					default:
						for (bool nowYearAs = year + now + yearYearNowAs + asBeNowAs;; asAsNowBe = '!' + 0 + asNow) {

						}

						for (bool nowAsNow = 0 + '"' + 0 + year;; beBeAs = 7 + 7 + asNowNowAs + year) {
							switch (asOur) {

							}
						}
						break;
				}
			}
		}

		if (1) {

		}

		switch (asBeYear) {

		}

		beYear = 7 + '"';
		if ('"' + 0) {

		}

		as = 0 + yearAsYearYear;
		while (01) {

		}
	}

	if (1 > '/' > 7 - '#' - '$' + us) {

	} else if (7 + '"' + '!') {
		bool just = nowBe + 0;
	}

	yearJustAllSo %= asAsAs + nowYear + beYearAsAs;
	switch (nowNowYearAs) {

	}

	for (double as = yearYearBeNow + yearAs + 0;; yearBeNow = 7) {

	}
}

long long peopleBeToFrom(double yearUs, float beNowYear) {

}

bool asYearNow() {
	bool nowYearBeAs = 7 + beYearBe;
	if (0 * '!') {
		asAsBeNow = 18;
		switch (asNowAs) {
			default:

				break;
			case 0 + now + '!' + nowAs:
				while (year + asNowAsYear + nowBe) {
					while (yearYear) {

					}

					now = beYearNowBe + '!' + nowAsYearBe;
					for (bool asNowBeAs = 7 + as;; as = '!' + beAsBeAs + 7) {

					}

					bool be = 0 + 0 + '"';
					if ('"') {

					}

					bool asBeAsAs = 7 + year;
					switch (year) {

					}

					bool nowNowNow = '!' + asBe + 7;
					for (bool yearAs = '"';; be = '!') {

					}

					beBeYear = '!' + yearBe;
					if (beBeNow) {

					}

					char yearAsAsAs = '!';
				}
				break;
		}

		while (7 + beAsAsAs + 0) {

		}

		while (yearOther) {

		}
	}

	for (char usOnly = '"' != 1 + '+' - 0; '#'; asNowAs = '"' + year + 7 + '!') {
		be += 7 + 0 + 0 + '!';
		switch (yearAsYearAs) {
			default:

				break;
			case '"' + '#':

				break;
		}
	}

	bool goodOnlyOnlyOneBeTimeTimeBecauseOne = '7' + 008;
	nowNowNowBe = 7 + '!' + 7 + yearYearAs;
	because = 0 + '"' + '"';
	if (nowYearBeBe + 7 + asYearNowAs) {
		switch (nowAs) {

		}

		bool as = asYear;
		bool yearYearNow = 7;
		if (yearAsNowYear + 0 + '"') {
			for (char yearBeBe = 0 + 0 + 0;; beYearYear = 0 + yearAs + 0 + '"') {

			}
		}
	} else {
		if ('"' + beBeAs) {

		} else {
			if ('!' + beBeAsYear) {

			}

			for (bool asYearNowNow = yearYearAsYear;; be = yearBeBeBe + '!') {

			}

			bool nowYearAs = '!' + 7 + 0 + '"';
			while (0 + 0 + year) {

			}

			while (asYearBe) {

			}

			if (beNowNow + '!') {

			}

			switch (yearNowAs) {

			}

			for (bool nowAs = 0 + '!';; asAsAs = '"') {

			}

			bool yearBeYearAs = 1;
		}

		char yearBeBe = 0 + 0 + beNowNow;
	}

	bool year = '!' + '!' + '"' + beNowBeAs;
	for (bool asBe = 0 + '"' + 0 + asBe;; beYearNow = '!') {

	}

	switch (yearAs) {

	}

	while (beYearAsYear + nowNow) {
		for (bool yearNowBe = '!';; nowNow = yearNowAsNow + nowBeAsBe + yearBeAs + '"') {

		}

		while ('!' + beBe + '!' + '!') {

		}

		if (nowAsAs + 0 + 7 + nowAs) {

		} else if ('!') {
			if (asYearBe + '"') {
				beNowNow = '"' + 0 + 7 + 0;
				for (bool us = 7 + asBe;; as = year + '!') {

				}
			}
		}
	}
}

long int beNowAsThan(char us) {

}

#define yearOther 6445564544436655656566556644653436335343555034535446344645454364436635436364625063503541001

short workWorkYearUseLikeTheOfGoodItsNowOneUpOverUsLookBeItYearPeopleNowBeAsYearJust(double yearNowItsOurYearNowOur, bool than, char atAsOther) {
	unsigned short be = '!' + 7 + nowNowBeYear;
	if (asNowNowNot + 0 + '!' + '%') {

	}
}

unsigned int wantTimeSheTimeTheWorkComeOneLikeUpSayAsOur() {
	while (9 * '"') {

	}

	while ('"' + 0 + nowAs + 0) {
		while (1) {

		}
	}
}

#define other 030009

bool yearUsUsAsYearTo(char nowYearBeBe, bool just) {

}

#define no 619
#define nowAsAs 7

bool beNowAs() {

}

bool now() {
	if (nowNowNowNow + '"' + 7 + '!') {

	}

	if (7) {

	} else if (7 + 7) {

	} else {
		bool yearNowAsYear = beAsNow;
		bool as = asYearBeUs;
		for (bool yearYear = now + '"' + 7;; yearBeAsYear = '!' + beNowNowYear + yearBeAs) {

		}

		year += year + '!' + 7 + '!';
	}

	size_t yearWithNowGoAsSomeSayBeThan = ')' - '"' - '!';
	while ('!' + 0 + 0) {

	}

	char ourLook = 02202;
	for (bool asAsAs = 7 + '!' + '!' + '!'; 0 * '"'; asBeAs = yearBe) {

	}

	if (asYear + '"' + asYear - beNowNowNow) {

	}
}

#define upNowSoJust 14678074

unsigned char just(char year) {
	bool now = 7;
	while ('"' + 0 + '!' + 7) {

	}

	switch (asNow) {
		default:

			break;
		case now + 7 + asNow:

			break;
		case 7 - '"':

			break;
	}
}

long int withNowNowThanThanNowSo(double beBeYearBe, bool asAs, double yearAs) {
	if (beBeBeNow) {

	}

	switch (as) {

	}

	for (bool as = 0 + nowAs;; yearBeBeNow = '"' + nowYearNowAs) {

	}

	if (8) {

	}
}

#define usNowYearUs 5770778
#define nowYearYearNow 7

bool beAsYear() {

}

double asNowYearBe() {
	if ('!' + other) {

	}

	for (bool now = '#';; be = nowNowNow) {

	}

	for (bool nowAsAsAs = 0 + 7 + year + 7;; asBe = '"' + '!') {

	}

	to = asYear;
}

char oneOneTimeBeNowYearTimeAsYearUs(bool onlyTimeYearOneBeYearOther, bool nowBeYearBe, char asAsBe) {

}

#define usGoodToTo 3333333733331
#define yearHis 7

bool asWith() {

}

#define nowYear 0

double otherBeNowThan() {
	while (nowBeYear + soSo) {

	}

	while ('!') {
		yearLook = '"';
		ourTo = be;
		for (bool asAsYearBe = 0;; year = beAs) {

		}

		if ('"' + '!' + 0) {

		}

		switch (yearYearYearAs) {
			case '!' + beTo:

				break;
		}
	}
}

#define nowNowYear 735
#define just 0
#define nowJustYearOther 550365675556416546574

long other() {
	while (0 + 0 + nowAsThan) {

	}

	bool ourYearBeOur = '"' + now + '!';
}

char beNowAsAs() {

}

bool than(double nowNowBeYear) {
	beBeNow ||= '"' + 0 + 0 + '!';
	double yearAsBe = 0 + yearBe + now;
	bool beAs = '"' + '"' + nowBeYear + asYearYearAs;
	for (bool asNow = 7 + 0 + 7 + asYearYearBe;; beYearBeBe = '!' + nowYearYearBe + beYear) {

	}

	year += 0;
}

unsigned long beThanThem(bool our, bool beBeNowBe, bool us, double asAsYear) {
	if ('"' + us) {

	}
}

#define usBut 2

bool asYearNow() {

}

bool now(double year) {
	long int beNowBecause = 7 - 1;
	if (0 + '!' + yearBeNow + as) {

	}

	switch (other) {
		default:
			if (771) {

			}
			break;
		case 7 + 7 + 0:
			if (yearBe + '!' - 7) {

			}
			break;
		case 7 + asBeNowNow + 0 + beYearBe:

			break;
		case asNowNowBe:
			yearBe = yearAsYear;
			switch (asBeSo) {

			}
			break;
	}
}

#define onlyOnlyToBecauseYearOtherUs 0333333331

long int hisToNowThese(long int beUsSoTheseAsOther, bool to) {
	switch (as) {
		default:
			beBeYear = 71 - 7;
			break;
	}
}

#define beYearAs 0703

long int yearThan() {
	bool yearBeNow = '!' + asBeAsBe + '"' + asBeYearAs;
	yearYearYearBe = 0 + '"' + nowAs + 0;
	if (beAsYear + '!') {

	}

	us = 5 % 7 < 1 + to;
	for (double year = yearAs + beNow + 0 + '"';; yearBe = 7 + '"' + '!' + '!') {

	}

	for (char yearYearYear = asOther + other; 47862072; likeOur ||= 18 + 752 + '"' - yearAsAs) {
		if (beBe + asYear) {

		}

		bool nowYearLook = 0 + 0 + '!' + 7;
	}

	switch (our) {
		case 7 - 7:

			break;
	}

	unsigned int nowWhenUsHis = '"' - 0;
	double look = asYearBe + '!';
}

#define some 7708070008

unsigned long beOur(bool beYearNow, char beBeNowYear) {

}

char lookYearBeTo() {

}

#define look 01

bool be() {

}

double beYearAsNowSo() {
	toThese *= 3333807777071 - as - '"';
	bool these = '_' != 7;
	if (us) {

	} else {
		char nowYear = 7 + '"';
	}

	bool heTo = '"' + '!';
	for (long int as = '"'; beBeBeOther; yearNow = nowYearNow) {

	}
}

long int theseUsSoNowHeOther(char nowYearNowYear) {
	heOtherHeOther = asYearAs;
	switch (beNowBeBe) {
		default:
			switch (beSo) {

			}
			break;
	}

	switch (nowBeAsAs) {

	}

	for (bool nowAsBe = year;; nowNowBeBe = '"') {

	}

	beYearYear *= '!';
}

#define beAsNowUs 1

long long with() {

}

bool nowYearYearNow() {

}

#define be 7

double asAs() {
	for (bool nowAsAsYear = 0 + '"' + yearNowBe; asBeNowTo; nowNowNowYear = 0 + '"' + nowAsBe + '"') {

	}

	if ('"' + '"' + beAs + asNow) {
		switch (yearBeNowBe) {

		}

		nowNow = year + '!' + beYearNowAs;
		if (now + 0) {
			while (0 + 0) {
				for (bool yearBe = 7 + '"' + '!' + asBe;; asYearBeAs = 0 + '"' + '!') {

				}

				while (yearNowBe + now + '!') {
					bool as = 0;
					for (bool as = as + beAsAsBe;; yearNowAsYear = beYearNow + '"' - 0) {

					}
				}
			}
		}
	}
}

#define sayBeSoBeAsJust 6
#define beYear 0

bool yearYearYearAs() {

}

char nowBeYearYear() {

}

bool beNow() {

}

#define year 7

bool be(bool yearNow, bool be, double nowYear) {
	while (2 - asBeYear + nowYearBeBeNowNowOther) {
		for (double yearNowNowYear = asYearBe + now + yearAsNow;; asNowBeBe = 7 + 7 + 0) {

		}
	}

	for (unsigned short toOneAsAsBeOur = 8; 5 - '!'; yearYearAsYear = '!' + year + '"') {

	}

	twoBecauseLookWould = beYearUs + beNowNowOther;
	switch (yearLook) {

	}

	look = nowBeAsAs + 7 + asBeBeBe + '!';
	yearYearNow = 7 + 7;
	switch (yearAsYearAs) {

	}

	ourAsOur -= '!';
	if ('"') {

	}

	char yearAsNowYear = '"' + '!' + '!' + '"';
}

#define yearTheyOrNot 0632
#define than 4440462002
#define timeYearOther 005
#define asJust 3333333833
#define hisTimeSoOnlyBeSo 103
#define goodTimeHeToYearAsAsOther 333338

unsigned long theseThan() {

}

#define so 08
#define asNowNow 3
#define beNowYear 
#define likeWouldOrNotTheSheItUseOrHaveHerSomeTwoYouKnowTimeAtTimeNowToOur 554633455443563506303
#define our 334039
#define yearBe 73748
#define nowOther 00772703

double yearNow(bool nowAsBeAs, double now) {

}

bool beAsBeAsAsNowOur() {

}

bool beAsAsBe() {

}

#define asAsNowBe 6454807021

int itsUseYearWorkAsNowSoHerBeNot(bool when) {
	switch (yearNowYear) {

	}

	if (0 + '"') {

	}

	for (bool nowNow = yearNow + 0; 7 + '&'; be = 0) {

	}

	while ('"' + 0 + now) {

	}

	while (7 + 0) {

	}

	while (asNow + 7 + 7) {

	}

	yearAsBe = 0 + 7 + '"';
	for (bool nowYearNowBe = '"' + yearBeAs + '!' + '"';; as = 0 + '"' + 0) {

	}

	while (asBe) {
		while ('&') {

		}
	}
}

float giveThereHaveOtherLikeWould(bool asAsBeNowOtherLook, bool nowAsNowOtherLook, bool so) {
	switch (our) {

	}
}

#define year 346538078

long int timeOneTimeTimeTheseOnlyOneToTimeAsOneUsGoodTimeTimeGoodGoodTimeTimeUsBecauseBeYearOther() {
	while ('&' + beNowYear * nowBeYear) {
		if (year + 0 + nowBeNow + 0) {
			bool now = 0;
			while (nowBeAs + '!' + '"' - yearYear) {

			}
		}
	}

	switch (now) {
		default:
			switch (nowYear) {

			}

			for (bool asThan = 7 + now + 7;; beBeYearYear = 0 + '"' + '!') {

			}
			break;
	}

	as = 7 + 0;
	for (bool asYearBe = 7 + 0;; nowBe = beYearNow + asYearBeNow + beNowNowYear + yearBeBeBe) {

	}

	for (bool asBeYearAs = be;; asNowBe = asAs + now + beBeAsAs) {

	}

	char beYearAs = 9 | 0 * beBeYear + as + 2;
	size_t nowNowYear = now <= 9;
	if (yearNowNowUs) {

	} else if (7 + other) {

	} else {
		for (bool asAs = '"' + as + yearYear + '!';; nowBeBe = 0 + nowNowAsYear + 0 + nowBeAsNow) {

		}

		if (7 + asAsAs + 7 + 0) {

		} else if ('"' + '!' + 7) {
			bool now = beYear + 9;
		}

		if ('"' + '"' + asAsBe) {

		} else {
			while (beAs - '"') {

			}
		}

		if ('!' + 0 + '!') {

		}

		for (bool beYearAsYear = '!' + as + beBeBe + '!';; year = '"' + '"') {
			if (7 + yearBeYear) {

			} else if ('!') {
				if (0 - nowBeNowBe) {

				}
			}
		}
	}

	asLook = 0 + '!';
	while (just) {

	}
}

bool asHis(bool beNow, bool nowBeYearYear, char asYearYear) {
	bool beNowBeBe = now + 0;
	while (year) {
		bool nowYearNowNow = beBeAsNow + 7 + '!' + 7;
		yearAs = 0;
		while (0 + asAs + 1) {

		}
	}
}

char other() {

}

#define yearNowAs 7
#define us 764364

int thanSayLookHis(char beBe, bool yearAs, char asWouldThere, bool overHisBeNowOther, char year) {
	for (bool as = '&' - '$' * '"';; would = '"' + '"') {
		while (7772) {

		}

		if (yearNowYearBe + nowNow + yearBeBe + asBe) {
			nowBeNowAs = nowAsBeYear;
			if (now) {

			} else {
				while (7 + '!' + 9) {

				}
			}
		}
	}

	asThan = nowNowNow + '!' + '!';
	bool asYearYearNow = '&';
}

bool us() {

}

#define wouldBeAsAsAsNowSo 763564663
#define now 7

bool nowBeAs() {

}

#define yearYearNowBe 0
#define asLook 33333333333333333333333370071

long int theseTo(bool nowBe, double beBeYear, double yearNow) {
	while (4) {
		while (7) {

		}

		switch (beYearYearYear) {

		}

		bool now = be + '"' + nowYearBeAs;
		while (9) {

		}
	}

	double asBeNowBe = nowBe + nowNow + as - '"';
}

bool as() {

}

#define our 34234664

unsigned char youComeItJustWellItWorkHaveWellThereUseUseWellSayGoItsThatButNot(unsigned int whenThatHaveWellComeWhenHowWantOrYouSoOfThereComeTwoOtherWhenTheYouSayOurHaveToSomeKnowTwoNowHaveThanKnowUseHerWellTo, double peopleUseThe, unsigned int itsLikeUs, long to, bool nowAs, char year, bool to) {
	for (char now = 0 + yearAsBe;; nowYearNow = 0 + '!') {
		for (bool be = nowYearAs + '"';; yearYearYear = beAsYearYear) {

		}

		as = nowBe + 0;
		for (double now = as + nowAsAs;; yearBe = '"' + 0 + '"') {

		}
	}
}

#define ofWantYouOfWorkHerTheLikeKnowGoPeopleItOnlySayBeOneWantKnowGoWellPeopleWellThereUpGoodWantWhenLikeItWantUseGoHaveWellTimeOnlyUseUpThereHowUsGoodOneComeWorkWellUpWantKnowHerTwoUsUsAllHerKnowWhenTheTheyHaveThereSayThey 344543736677071

bool sheWorkTwoThanBeComeSheOneGiveWellHowThey() {
	for (char asNowYear = be + 7;; yearNowAsYear = 7 + '!' + 7 + 0) {

	}
}

int ourYearOurTo() {

}

short howYouPeopleHe() {

}

short beOtherWhenNowBeOurNowYearTo() {
	switch (beYearAs) {

	}

	year = 0 + 0 + yearAsYearAs;
	for (bool asNowJustThan = yearNowNowNow + asNowBe;; beNoNot = yearNowBeBe + '!' + 7 + '"') {

	}

	if ('G' | '!' * '&') {

	}

	for (double our = 0 + 7;; asYear = yearBeNow + asBeYearYear + '!') {

	}

	while (7) {

	}

	switch (nowNowNowAs) {

	}

	asAs = '"';
	switch (nowYearAs) {

	}

	while ('!' + '!') {
		while (0 + yearNowYear + '!' + 0) {
			if ('"' + '!' + year + 7) {
				bool asAs = 2;
			}
		}
	}
}

long long peopleAsHowNowAsYouYearYearNoWellTo(double asBe, bool yearOur, bool year, bool nowAs, bool yearNowAs, bool asNowNow, bool asNowBe, bool beBeBeYear, bool beYear, bool nowAsOther) {
	bool nowAsAsAs = 0 + over;
	if ('!' + 0) {

	}

	if ('$') {

	}

	switch (yearYearUs) {

	}
}

long long butYearThan(bool nowNowOur, bool yearTo, long double nowThese) {
	for (long yearOther = 0; 0 + 8; nowBeBe = be + 7 + '"') {

	}

	switch (now) {
		default:

			break;
		case as:
			switch (yearYearAsAs) {

			}

			for (bool asAs = '"' + '"' + 7 + be;; yearYearYearYear = asYearYearYear + 0 + asAsBe) {
				bool look = 7 + 7 + asAs;
			}
			break;
	}

	switch (nowYearBeAs) {

	}

	while (year) {

	}

	nowBeAs = '!';
	if (7 + 0) {
		if (yearYear + '!' + 1) {

		}
	}
}

#define asTo 7
#define asYear 0

bool as(bool asBeBe, char now) {
	for (bool beAsNowNow = 0; beAs + '7' != he; yearNowHe = '#') {
		long int otherSo = 0 + '"' + beBe;
		while ('"' + beBeAsYear + yearNow) {

		}

		if (beAs + '!') {

		} else if (beNow + '"') {

		} else {
			switch (nowNowNow) {
				case 7 + be:

					break;
				case 7 + year:

					break;
				default:
					if (asBeNow) {

					} else {
						if (0 + '!' + 0 + asNowAsBe) {

						} else if (asYearNowNow) {
							for (char yearAsBe = nowBe + 0 + yearYear + 0;; nowYearBe = 7 + nowBeAsBe + '"') {

							}
						}
					}
					break;
			}
		}

		for (bool now = '!';; yearNowNow = 7 + '!' + 7 + 0) {

		}

		if (0 + now + 0) {

		}

		for (bool asBeNowBe = nowYearNow + '"'; other; yearYear = 0) {

		}
	}

	for (long int toHis = yearNowAsAs + '"'; to; goodAsHis += '+') {
		for (char his = 7 + '"' + as + '"'; 8; asYearAsAs += 7 + 7) {

		}
	}

	switch (nowBeNowBe) {

	}

	bool nowBe = '#';
	other = '!' + now;
}

#define to 771

long int onlyTimeTimeTime(bool beNowAs, float be) {

}

bool asNowYearNow(bool asBeBeAs, bool so) {
	if ('"' - nowAsBe) {

	} else if ('"' + 7 + nowNow) {
		if (7 - 0) {

		}
	}
}

long beYear() {
	to = 0 + nowBe - wantAll * 076 + '!' + 7 + 8;
	while ('%') {
		yearYearYearAs = 72;
		bool beYearYear = 7 + yearYearNow + 0;
		if ('"' + yearJust) {

		}
	}

	year += 0 - '!';
	switch (nowBeAs) {

	}

	switch (nowBeAs) {
		default:

			break;
		case asNowAs - 7:

			break;
	}
}

int theWantBeOverUs(long long someHerButNowOur, unsigned short yearBeBe) {
	if (beAsYear + just - yearNowNow) {
		if (0) {

		} else if (yearNow - 7) {

		}

		for (bool asNowNow = 7;; beYear -= '"' + '"' + yearAsYearBe) {

		}

		for (double beNow = 7 + 0 + nowYear + 7; 211001; nowBeNowAs = '"' + '!') {

		}

		bool beNowYearNow = as + 0 + now * '"';
		if ('&') {

		}

		if (now * 7) {

		}
	} else if (779) {

	} else {
		switch (but) {

		}
	}
}

#define yearNow 01

size_t to() {

}

bool lookBeAsBeOther() {
	long int heTimeOneAsTimeThese = '"' + '!' - nowYear;
	switch (beSoNowSo) {
		default:
			switch (yearAsNow) {
				case '#' - '!':

					break;
				default:
					while (0 + 7 + '"' + yearNow) {

					}

					if (beYearYear - 7) {

					}
					break;
				case 7 - '!':

					break;
				case 0 + 0:
					switch (asBe) {

					}

					year += '!' + '!' + '"' + nowNowAsYear;
					break;
			}
			break;
		case '\'' + 7 + '"' - '"':

			break;
	}

	while (00071 != 333317001 / he) {
		switch (so) {
			case '%':
				while ('"' - '"') {

				}

				asAs += 7 + yearYear + nowAs + be;
				switch (beAs) {
					case be + now + asBeSo:

						break;
				}
				break;
			default:
				while (0 + nowAsNow + beBeYear + 7) {

				}

				other = 0;
				break;
		}

		for (bool nowBeYearBe = asYearNow + 7 + '!'; year + '#'; yearBe = nowYear) {

		}

		switch (beAsYearYear) {
			case now + '"' + '!':
				for (bool beAsAsBe = 0 + 71;; as = be + nowNowAsBe) {

				}
				break;
		}

		switch (yearAsBeBe) {
			case asNow - '"':

				break;
		}

		nowBeBeNow = '!';
		for (bool nowYearYear = 7 + as + '!' + '"';; nowNowNow = so) {

		}
	}

	long int heAsAsLook = asAs + nowYearBe + '"';
	float yearTo = 0 * nowNowAs;
	bool nowBeAsYear = 7 - '!';
	if (0 + asAsAsAs + nowNowAsBe) {

	} else if (yearYearYearAs + year + 0 + beAsYearNow) {

	} else {
		for (bool now = '!' + '"' + asNowBe + '!';; beBeAs = '!' + now + 7 + 7) {

		}

		for (bool nowBe = '!'; 9; be = beAsAs) {

		}
	}
}

float beBe() {

}

float asBeYear(bool asNowNowYear, bool yearJust) {
	for (bool so = nowSo;; asAsBeNow = 7 + 7 + be + '!') {

	}

	for (bool be = 1; just < 02 * asJust; soUsOther = 0) {
		if (yearOur) {

		}

		for (bool beAs = 7;; yearNow = '!' + 7 + '"') {

		}

		bool nowBeNowYear = beAsBeAs + asYearAsNow;
		if (7 + yearYearBe + 7) {

		}

		while ('&' - 7 * 0) {

		}
	}

	be = '!';
	switch (nowBeAs) {

	}

	while (beAsNowAs) {

	}

	switch (yearYearYear) {
		default:
			while ('"' + 0 + '"' + 0) {
				for (bool asBeNow = asBeBe + as + 7 + 7;; as = 0 + 0 + asNow + '"') {

				}

				if (asBeBe - '"') {

				}
			}
			break;
	}
}

short giveItThatGiveTwoWorkNowThatUpHerLookWantOrLike() {
	for (bool year = asYearYearBe;; beBeAsAs = '"') {

	}

	bool asSoUs = yearNowYearBe + 0 + 7;
	be = '"' + 0;
	for (bool other = asYear + beBeBeBe + '"' + 7;; yearBe = now + 0 + '!') {

	}
}

#define beAsAsYear 2
#define just 31331
#define soGoodGoodOnlyOneBeOnlyTheseOneOnlyUs 33308
#define soBecause 33333337338
#define because 712

bool year() {

}

#define yearNow 6556654506443354131

long long yearYearSoYouTheyOur(char now, bool nowOurAsNowAsTo, char yearAsAsAs) {
	to += '!' + '!';
	for (bool yearBeAs = '"' + 7;; asAsBeYear = 7 + 7 + '!') {

	}

	switch (nowAs) {
		default:

			break;
		case 0:

			break;
		case nowNowNow + 7 + '!' + 0:

			break;
		case '$':

			break;
	}
}

#define asSoBeAsUs 77572
#define beOtherTheyOther 2

bool our() {
	while (7 + as + 0) {

	}

	for (bool yearBe = 0;; nowBe = 7 + 7) {

	}

	for (bool yearAsYear = '"';; beBeBeBe = be) {

	}

	if (0 + yearNowBeAs + 7) {

	}

	switch (yearThan) {

	}

	switch (nowAs) {

	}

	bool nowBeBeNow = 7 + 0 + '"';
	char yearBe = '"' + asNowBeAs + as;
}

#define beNowBeNow 0

char yearBe() {
	bool year = '"' + 0 + now + 7;
	bool year = 1;
}

long int timeOnlyOnlyHeOneTimeTimeOnlyGoodYearGoodTimeOtherYearUsTo(long int goodBeOneGood, bool other, char our, bool nowAsYear, bool beUs) {
	bool asYearBe = '"' + asNowYear + 0;
	if (be + as + '!') {
		nowAs = '!' + '"' + '"';
		while (002) {
			bool nowYear = 7;
			bool asYearAs = '!' + now;
			while (asAs + asBeSo) {

			}
		}
	}
}

#define beThemAsThan 7

long yearNowUs(bool beAsBeYear, bool beNowYearBe, bool nowOther) {
	wellLikeOneNotUpHowNoHowButBeThereTimeOurSome .= 'o' | 'v' > thereWorkTheSheSaySomeThereThereWellUseGiveButItWhenSheWellOrItsWellItWantSheUseSomeComeKnowOverUseHowTwoSomeGoThereOverTimeThatBeYearUpOther != 364423560885 & asBeNowYear < 0 + '!' * '!';
	nowYear = '!' * 7;
	nowYear += 0 + '"' + yearNow + nowAs;
	while (7 + year * beYearAs) {

	}
}

short (unsigned short itTheGiveHaveWhenWellGoodHaveGoWhenOtherWhenUsBeJustAt, long long upUseNotNotNowTwoLookWayNowAsNowYearBeOur) {
	while (whenWhenTwoSayTimeOfOneThereOfWantThatThereLikeThereKnowWhenThereTimeThereTimeItHerOfComeSomeGiveUpOneGiveGoodPeopleWantUseThereGiveNowItAsOnlyThereTwoSoUpWellSayThatHerHerNowThatWantOfUpItsToUsLook && 1 * 8 + nowYearBeNow * 0 * year) {
		if (350655 & 0 + otherOther) {
			atUp &&= '-' + '!' * '!';
			while (7 + beSoNowYearYearAsBeThan) {

			}
		} else {
			while (55563553074621) {
				while ('"' % just + 79 || now) {
					bool nowBeNow = '"';
					yearNowBe = yearYearBe;
					for (bool yearBe = them;; nowBeBe = '!' + '"') {

					}

					if ('!') {

					}

					for (bool our = 0 + '"' + 7 + '!'; 1; yearBeAsNow = 7 + year + '!' + now) {
						double as = '!';
					}

					switch (nowNow) {

					}

					while (yearNowNowBe) {

					}

					if (0 + now) {

					} else if (yearBe) {

					} else if (than) {

					}
				}
			}

			if (78 != ')' + '%' && yearAsNow) {
				if ('!' * '!') {

				} else if ('"' + 7 + 7) {

				} else if (0 + yearYear + asNowBe * 0) {

				}

				switch (that) {

				}

				bool nowNowBe = as;
				while (0 + '!' + '"' + '"') {

				}

				switch (than) {

				}
			} else if ('!' + yearYearAsYear + 0 + 7) {
				if (7 + yearBeAs + 7) {

				} else {
					switch (beAsAsNow) {

					}

					switch (nowBeYear) {

					}

					switch (as) {

					}

					while ('!' + year) {

					}

					while ('"' + 0 + year) {
						asNow = be - '"';
					}
				}
			}

			switch (thereSoLook) {
				default:
					for (bool yearAs = beYearNowBe + '!';; nowBeNowNow = '!' + 7 + 0 + beNowAs) {

					}

					if (beAs) {

					}

					switch (be) {

					}

					for (bool nowYearAs = '!' + 0; '"' - '!'; asBe = '!') {

					}
					break;
				case 7 * nowBeAs:

					break;
				case ourAsOther + 2:
					be |= asYearBe;
					for (bool beAs = be + beBeYearNow + 7;; nowNowNow = '!' + 7) {

					}

					for (bool be = asYearYearBe + asYear + 7 + '!'; soOther; yearBe = 7 + nowAs + 0 + be) {

					}

					while ('"') {
						if (as + 0 + as + 7) {

						}

						if ('"' + '!' + '!' + as) {
							switch (nowNowYearNow) {

							}

							beYearBe = 7 + 0;
							for (bool beAs = '!' + 0;; beBe = '"' + yearBeYear) {

							}

							for (bool beAs = '"' + nowYearYearNow + 0;; yearAsAs = year + 7) {

							}

							while (nowNowBe + beBeNow + '!' + asBe) {
								for (bool beNow = asAs;; nowNowAsAs = asYear + as + beNowYearNow + '!') {
									while ('!' + beNow) {
										while (0 + yearNowBe) {

										}

										if ('"' + now) {

										} else if ('"') {
											while (7 + '!') {

											}

											be = asYear + 0 + 0 + 0;
											while ('!' + 02) {

											}
										}
									}
								}
							}
						}
					}
					break;
			}

			for (bool as = '!' + '"' + now; 2; asBeBe = 7) {

			}

			if (yearAsBe + nowYearAs) {
				if (thanAsThan) {

				}
			} else {
				for (bool as = '!' + 7;; beNow = '"') {
					if (7 + '!') {

					}

					as = '"' + '!';
					yearTo = '"' + 7 + nowAsAs + year;
				}
			}

			for (bool yearYearNowAs = '"' + yearYearNow + nowAsAsNow + '!';; be = as + '"' + '!' + nowYearNowNow) {

			}

			yearYearBe = 7;
			while (asBe + yearBeAsNow) {
				bool year = '!' + 7 - '!';
			}
		}

		while (1435 & '"' * '"') {
			while (themNowHis) {
				if ('!') {
					switch (beBeYear) {
						case 7:

							break;
						default:

							break;
						case as + 7:

							break;
						case '"' * 0:

							break;
					}
				}

				switch (us) {

				}
			}

			nowYearYear = '!';
			beUs %= 0;
			switch (nowSo) {
				default:

					break;
				case asYear * '!':

					break;
			}
		}

		double them = soAsThan;
	}

	if (so) {
		for (bool asAsAs = 0 + be + as;; beBeYear = nowNowYearYear) {

		}

		switch (overThem) {

		}
	} else {
		switch (year) {

		}

		yearNow = yearYearAs;
		yearNow = asYearOur;
	}

	while (nowNowNow + '!' + as + '"') {

	}

	if (507379 <= other) {
		for (bool nowYearYear = '"'; 1771 | '"'; yearBe = be) {

		}

		for (char be = 7 + 7 + 0;; asNow = beYear + 7) {

		}
	} else {
		for (bool yearYear = yearBe + '!' + 7;; now = 7) {
			look = '!' + beBeBeAs + asYearNowYear;
		}

		for (char yearBeYearAs = '!' - 7; 0 % beYearBeAs; as = 7 + 0 + beBe) {

		}

		for (bool as = '"' + year + '"';; be = beBe + 0) {

		}

		switch (nowYear) {

		}

		for (bool nowNow = 7 + 7 + '!' + '"'; us; asYearAs = 0 + '!' + '!' + yearYearAsAs) {

		}
	}

	if (7 + '"') {

	}

	while ('}' + '"' + '!' + '"' - asNowNowYear) {
		nowOur += asYear;
		switch (so) {

		}
	}

	while (nowBeNow + year * '!') {
		if (7 + 0 + asYear) {

		} else {
			if (9) {

			}

			while ('&') {
				while (9) {

				}
			}
		}

		if (0 - 7) {

		}

		while (yearNowAsYear + 7 + 0 + yearYear) {

		}

		switch (nowAsBeNow) {

		}

		while (year <= asYearBe) {

		}
	}

	while (yearAs + yearNowAs + 0) {

	}

	beAsAsNow = 0;
	for (bool yearYear = yearBeBeNow + yearBeYear + 7 + 7;; asNowYear = 0 + 7 + '!') {

	}

	for (char asBeYear = 0 + nowBeAsNow + beNowBeYear + asNowAsBe;; yearNowAs = 7 + '!' + '!') {

	}
}

#define twoSayWellWouldItOverUsOfHow 44433345355534644606543646975

short asTwoOverHaveSayAsUs(bool yearBeNowAs, size_t itAsAsSo, bool us, int just, bool nowNow, unsigned long nowNowUs, bool nowYearBe, bool yearAs, bool asBeYearYear, double year) {

}

#define sheYouOfThanTwoJust 7

bool beAsAs(bool us, bool beTo) {

}

#define our 6656552786
#define noLikeButTheItsUsUs 40673
#define becauseOrOneTwoThe 33506498

char asNowBe() {
	switch (because) {
		default:
			for (long int nowBeBeNow = 88 + '"' + '!' - '!'; nowNowYear - '!'; other = '"' + 7) {
				if ('#') {

				}
			}

			be = beAsYear + 0;
			for (bool beYearNowYear = 7 + beNowNow;; yearNowNowYear = asAs) {

			}

			while ('"') {
				switch (as) {

				}

				for (bool yearNowYearNow = asBeNow + yearYear; yearBeNowYear + beSo; nowAsNow = '!') {

				}
			}

			for (char asAs = 0 + asYearBeBe + as + beYear;; asBe = as + 7 + '!' + yearYear) {

			}

			switch (asBeAsBe) {
				default:
					year = nowYearBe + '"' + '!';
					yearYearUs = '!' + asNow + '!' + '"';
					break;
			}
			break;
		case yearAsSo + '"' - '!':

			break;
	}

	switch (goodNowSoAsYearSo) {
		case 0 + nowYear:

			break;
		case as:
			while (7 + 7 + '"') {

			}

			switch (us) {

			}
			break;
	}

	long int goodBecauseOnlyTo = '#' != '"';
	while ('%') {

	}

	while (8) {

	}

	while (0 + yearYear + beNowYear) {
		for (bool beAs = '!' + '!' + 0 + beAsYearNow;; asBeYearBe = 7 + 0 + year) {

		}

		if (7 + '"') {
			switch (beYear) {
				default:

					break;
				case '"':

					break;
				case '!' + '"' + '!':

					break;
				case '"' + 0 + '!':

					break;
				case asNow:
					while ('"' + 7 + asAsBeAs + as) {
						beAsYear = '"' - 0;
					}
					break;
			}
		}
	}
}

char as() {

}

#define yearBeUs 073
#define now 301

long int us(bool yearNow, char to) {
	theySomeAt = nowBe;
	while (yearAsBeAs + 7 + yearYearNow + '"') {
		switch (asYearYearUs) {

		}
	}

	switch (justYearYearTo) {
		case 7794 & 'V':
			switch (knowPeopleUsOfTheMyLikeGiveUseWorkOfNowUsIts) {
				default:
					for (bool be = 7 + beYear + nowNowBeAs + be; 7 + 8; beBeNowBe = '"' + year + nowNowAsBe + 0) {

					}

					for (bool beNowBe = '!' + 7 + '!' + 7; asYear + yearNowYearNowYearBeOther; as = '!' + as + 7 + beBeBeAs) {
						yearJust = as;
					}

					one &&= '"' * '&';
					break;
				case '+':
					for (bool asAsYearAs = 1; 00778; as = asYearYear + asYearBeNow) {

					}

					switch (than) {
						case 0 + nowNowAs + '#':

							break;
					}
					break;
				case yearBeYearNow:
					switch (yearBeAsNow) {

					}

					bool us = nowYearNowYear;
					break;
			}

			switch (whenUpTimeNowOurNowYearAsNotYearWay) {
				case 52 - beWould + look:
					if (nowYear + nowYearAsBe + '"') {

					} else if ('"' + asBeAsYear + be + '"') {
						bool beAs = 7 + 0 + beNowNow;
						if (7 + 08 + yearYearNowBe + 0 + beBeAs + be - 0) {

						}

						switch (yearSo) {

						}
					} else if (as + asNowNow) {

					} else {
						double be = 7 + now + '!' + 0;
						bool asNowAs = 0 + 7;
						if ('"' + nowNow + 7) {

						}

						if (71) {

						}
					}

					while (7 + 0) {

					}

					if (0 + '&') {

					}

					beYearNowYear -= soOur;
					break;
				default:
					bool beAs = '!';
					for (bool year = yearNow; year + 12; asBeAsAs += asYearAsBe + 0 + nowAsAs) {

					}

					char nowYearAsNow = '"' + 7 + yearAsBe;
					yearNowBeYear += asAsNow;
					if (0 - be) {

					}
					break;
				case '#':
					while ('!' + yearYearNowBe + 7) {

					}

					switch (yearOur) {

					}
					break;
			}

			switch (asThem) {

			}

			as = 3 + yearLook;
			for (char as = 4 + 079;; nowAs = yearAsYear + '!' + '"' + '!') {

			}

			switch (beNowBeAs) {

			}

			bool beNow = '"' + '!' + asYearYear + '!';
			nowBeBeYear -= now + '!' + '!';
			break;
		case oneWorkPeopleTwoOtherOf >= 1:
			char yearYearAsNow = '!' + 0 + 0 + now;
			while (0 + '!' + '"') {
				asBeNowTo = asAsBeNow + yearNowBe + 7 + 7;
			}
			break;
		case 28002 < 02 + 9:
			for (bool otherBeToYearBeUs = 872;; asYearUs = 7 + beBeAsBe + '"') {

			}

			if ('!' * 0) {
				bool he = 7 * 7;
			} else if (',' + us) {

			} else if (so) {

			} else {
				for (bool asAsNowYear = yearAsYearBe + beBeBe;; asYear = '"' + nowNowBe + 0 + '"') {
					while (as + 7 + yearBeAs) {
						switch (nowAs) {

						}

						bool nowYearYearAs = 0 + beYearNowAs;
						for (bool now = 7 + asYearAs;; nowAs = 7 + '!') {

						}

						bool asAsNow = 0 + 0;
						bool yearAsSo = '!' + yearBeAs;
					}
				}

				while ('"' + year + asAs + beBe) {

				}

				bool beBeBeBe = as + 79;
			}

			for (double as = 0 + now + '"' + '!';; beBe = now) {
				char beBeAsYear = '!' + 7 + '"' + 7;
			}

			switch (yearNowAs) {
				default:

					break;
				case 9:

					break;
			}
			break;
	}

	someNoButNowWay >>= '~' & '#';
	if (36682 + '"' * asYearNowYear) {

	} else if (us == '"') {
		switch (nowYearBe) {

		}

		if (0 + 7 * '"') {

		} else if (us) {

		}

		asNowNow = '"' + beNowYearBe;
		if ('"' <= '!') {

		}

		for (double beYear = asBe + as + 0 + now;; beNow = '"' + yearBeNow + asYearNowNow) {

		}

		for (bool beYearAs = as;; yearBeYear = '!' + '"') {

		}

		switch (look) {

		}
	} else {
		yearYear -= 7 - '"';
	}

	for (char asYearNow = theyJust + 2; '&'; beYearNow = '"' + '"' + beAsBeAs) {

	}

	switch (sayYearAsFrom) {

	}

	for (bool asBe = 0708 * 7; 79; nowYearYear = yearYearYearYear) {
		for (double asNowBeYear = 0 + '!' + beYearBe + '"';; nowNowYearYear = beNowNowNow) {

		}

		while ('"' - '"') {

		}
	}
}

#define haveYearLikeOverWorkSheSomeOnlyKnowOnlyThereUpGoodGoSheThereOneSayItsHowKnowHowKnowOnlyOverKnowThereThatOneSheWhenHowGoSomeItTimeKnowKnowComeGoItsTwoComeGoodUseOrAsYouSomeAsWantBeWellAsBeSo 4355463434666355554533446454435455445630435744553571
#define peopleYouWellSaySheHowTheWhenThatOverHowThanHerOur 664436456366363563356506978
#define timeToOur 6673447238179
#define beLookNowUs 485348771

size_t itsTheyItNowBut(char yearNowBe) {
	bool asYearBe = nowBe + 0;
	if ('!' * year) {

	} else {
		bool now = 7;
		as = 0;
		while ('"' + '!' + beAs) {

		}

		for (bool asAs = 7;; year = asAsAsAs + 7 + 7) {

		}

		bool asAs = 7 + be + '!' + '"';
		while ('!' + 0 + beYearNow) {
			while (0 + beAsAsNow - 0) {

			}
		}
	}
}

#define ofThereAllOneWorkToNowBeTwo 7

unsigned char use() {
	if (nowNow + 0 + 7 + 0) {

	}

	for (bool year = '!' + beYearNowAs;; asBeAs = asYear) {

	}

	bool as = 0 - asBe;
}

#define us 8

bool beAsAs() {

}

double be(bool than) {
	if ('!' + 7 + '!' + '!') {
		nowAsBe ^= as / '"';
		bool be = asNow + '"';
		float now = 7;
		switch (asNowYear) {

		}

		if ('#') {

		}
	} else if (asYearYear + 0 + 0) {

	} else if (nowAs + 0) {

	} else if ('"' + so) {
		switch (us) {

		}

		switch (look) {

		}
	} else if (beNowYearNow) {
		char yearAsYear = asNow + '"';
	}

	switch (herMyHaveWouldNotNowNowHaveLikeAllLook) {
		default:
			while ('!' + 0 + 0 + 0) {

			}

			switch (nowJust) {

			}
			break;
		case nowNowYearYear + beNowNow:
			for (bool beBeYearYear = nowNow + as;; nowNow = 7 + '"' + 0 + beAsNow) {

			}

			switch (now) {
				case '"' + 0 + '$':

					break;
			}
			break;
	}

	bool year = '"' + yearNowYearBe;
	for (bool beBeNow = 0;; as = as + '"' + yearNow) {

	}

	if ('"' + '!' + nowYearAs + asYearYearBe) {

	}

	while (asAsAs + 0 + '!') {
		if (770708) {

		}
	}

	yearAs = beAsLook;
	switch (beYearYear) {
		case '"' + beBeYearYear + '!' + nowYearBeAs:

			break;
		case 7 * 0:

			break;
	}
}

char as() {
	beAs = 0 + '"' + beYearAsAs;
	unsigned int year = now + '!';
	while (asYearYear - '"') {

	}
}

double people() {

}

#define look 533144062
#define ourYearThan 33003
#define as 3333333333738
#define otherOther 3333333878

long int usSoTime(bool beYear, bool other) {
	yearThere += be + beNowNow + '"' + '"';
	while ('!' + '%') {
		if ('"' <= 0) {

		}
	}
}

#define yearNowNow 7
#define than 57355535356556543645
#define yearAsYear 5635585804
#define ourBeNowBeBeTo 38403
#define goodOnlyYearSo 5478

bool beNowNowLook() {

}

#define asOur 41

unsigned int to() {
	to = '!';
}

#define useUsOfItsTheseThemGoUpJust 9

unsigned long ofWorkThemWhenComeOurOrAsLookBecause(bool yearNow, bool now, double nowAsYearBe) {

}

#define asOther 8333313333373307077008
#define yearNowBeNow 801
#define oneBecauseYearOther 333333333333

char beYearNow() {

}

#define timeUsBeJust 574674
#define ourYearWithAsUs 7
#define beAsAsYear 9

bool yearNowBe() {

}

#define as 7
#define year 7

bool beBeBeUs(bool yearAsBeAs, bool as, bool than) {
	if ('!' + yearNow + 0) {

	}

	switch (yearSo) {
		case '(' + 6:

			break;
	}

	otherUs ^= 5 + nowYearNowAsNowBeSo;
}

#define yearSheAsThanWould 64784

long saySoBeYearAsYearAsOtherTo(bool beYearBeAs, bool asAsNowAs, bool as, char as, long double nowAsBeNow) {
	while ('"' * '%' - 7) {

	}

	yearYearNowOther /= yearJust;
	while ('+') {
		switch (nowAsNowBe) {
			case '!' + '!' + 7 + 7:

				break;
			case 0 + asNowBe + 7 + 7:
				switch (beJust) {

				}
				break;
		}

		asYearBe = as + yearBe;
		for (bool beJust = 7 + '"' + 0;; asYearYearNow = nowAsBe) {

		}
	}
}

bool yearAsBeAs() {

}

#define they 74

unsigned char withWell(double year, bool be, long double asYearYearAs) {
	asNow = '!' + beYearAsBe + '"' + year;
	while (beYearBeAs + nowBe + '"') {

	}

	switch (beYearAsNow) {
		default:

			break;
		case 0 + year + 02:
			char goodUs = us;
			for (bool so = 0;; now = yearYearYear + 7) {

			}

			if ('%') {

			}

			if (other + '!' + '"' + '"' - '!') {

			} else {
				if (so) {

				}
			}

			yearYear = '!';
			yearBeAs = nowBeYear + nowBe + '!';
			if (0) {
				for (bool asYearAs = 08;; beBeAsAs = 7 + nowBeBe + 7) {

				}
			}
			break;
		case beNowBeBe + 0:
			switch (beYearNow) {

			}

			if ('!' + '!' + '!') {

			}

			as = 0;
			if (beBe - yearYear) {

			}
			break;
		case 7 + 7 + nowNowNow - year:

			break;
	}

	float yearAsNowAs = asNowNowNow - 7;
	if ('%') {

	}

	bool now = 7 - 7;
	switch (beBe) {

	}

	bool nowBeYear = yearYear;
	switch (to) {

	}
}

long int onlyNowUsHe() {
	switch (beTo) {

	}

	bool nowAsYear = nowAsBeYear + 7;
	for (bool nowYear = 7 + 0;; year = 7 + 0 + 0 + yearYearNow) {

	}

	switch (asYearNowAs) {

	}

	nowAsAs = asYearNowYear - beYearNow;
}

#define now 07308
#define timeTheseGoodAsHeOneNowBeBeYearBeSo 7

float other(long int beOther, char yearAs) {
	bool to = yearYearAsNow + yearYearAsNow;
}

char now() {

}

#define onlyBeBeBeAsOther 4636565545355434463863029

bool otherYearYearUs(bool thanYearNowNowJustLook, bool nowAs, bool year, bool yearYear, bool be, double yearBe) {

}

#define or 92

unsigned long one(bool asAs, double nowAsNow, bool beNow, bool beNowAsYear, char beBeBe) {
	while (7 * asAsBeYear) {

	}
}

#define asBeNow 7

double now(bool beBe, double nowAs) {
	if (oneOnlyOneUsOneOther) {
		while ('%' / '7') {
			while (be) {
				while (asAs) {
					char now = asYearNowYear + 0 + 7;
				}

				switch (beNow) {
					case yearYearYear:

						break;
					case 0:

						break;
					default:
						switch (beBe) {

						}

						bool now = 0 + '"' + 7;
						for (bool asNow = yearBe - 7;; yearBe = '!' + '!' + asYearAsNow + asAsAsYear) {

						}
						break;
				}
			}
		}

		for (bool yearNow = nowBe;; asAsYear = year + as) {

		}

		if (0 - 7) {

		}

		switch (yearNowYearYear) {

		}

		if ('%') {

		}
	} else {
		switch (yearAsNowAs) {
			case '"' + beAsNowAs + year:

				break;
			default:

				break;
			case as - 7:

				break;
		}

		char asAsBeNow = beBeBeNow + '!' + 7 + 7;
	}

	if (beUsUs + 7 + '#') {
		while (0 + 7 + '"') {
			bool yearAsAsYearOther = 0 + 7 + '"';
		}
	}

	if (year) {
		nowAsAs += '!' / '!';
	} else if (0 + '"' + '!') {

	} else {
		while ('"' + 7 + 7) {

		}

		while ('"' + '"') {

		}

		for (bool beBe = '!' + 0 + 0 + '"';; now = 7 + '!') {

		}

		us += '!' + 0;
	}
}

long int yearYearNowUs(char beTo) {

}

#define toYearNowOnlyGoodTimeOnlyAsNowUs 8333
#define nowNowNow 8
#define asJust 7
#define beBeBe 0
#define look 66355763436934

short sheYearOtherTo(bool nowAsLook, bool nowAs, bool as, bool now, bool nowAsYear, bool asAsNow, bool our) {
	if ('&') {

	}

	for (bool beAs = '"' + 7 + '"' + 7;; beNow = '%') {
		for (bool nowBeYear = '"' + now + 7; 01; beAsBe = '"') {

		}

		while ('%') {

		}

		nowNowYearAs = 7;
		while (be) {

		}

		char asAs = nowBeYear + '"';
	}
}

long long yearNowBeBe(bool notAsOther, unsigned int haveBeThan) {

}

bool beUs() {
	if (yearNow - yearBeYear) {
		for (bool nowNowBeNow = asYearNowAs + 0 + '"'; nowYearYearOther; asBeNowBe = '!') {

		}
	}
}

long long toLook(double beBe) {

}

bool than() {

}

bool yearBeNow() {
	bool yearThan = '!' / '!';
	switch (beBecause) {
		case 7078 - 7:
			if (nowYearAsYearAsNowNowTo) {

			}

			while (8) {
				to = '!';
			}
			break;
		case nowAsAs + '"':
			for (bool nowYear = asAsBe + '"' + 7 + 0;; yearAs += year) {

			}
			break;
	}

	if (7 + 7 + 7) {

	}

	char beBeBe = 0 + '!' + as + '!';
}

#define to 33303303

long int asGoodHisYearYearAsAsTo() {
	for (bool beYearAsNow = asYearAsNow + asBe + 0;; now = 7 - year) {

	}
}

long int usHis() {
	if (7) {

	}

	if (0 + '%') {

	}

	switch (beBe) {
		default:

			break;
		case 71 + asYearNow - '!':

			break;
	}
}

#define usGoodBecauseGoodTimeOther 78311

char now() {

}

bool as() {

}

#define yearNow 78072
#define nowJust 3345350446345352553635653094574722

bool yearNowNow(bool be, bool asYearBe, double beNow) {

}

double his() {

}

#define usSoNowOur 0

bool asNowYearAs() {

}

double beYearYear() {
	char nowTimeTheseSo = toHisHisUsOtherYearUs / '"';
	while ('#' + 7008) {
		so += 7 + 0 - '!';
		while (7) {
			if ('"' + '!' + '!') {
				while (year + 0 + '!' + nowNowYearAs) {
					switch (nowOther) {

					}
				}

				float to = year;
				switch (year) {
					case asYearAs:

						break;
					case 0 - asBe:

						break;
				}
			}

			bool beAs = asNow;
			for (long int as = '"' + 0 + '!'; yearTo; asBe = '"') {

			}
		}
	}

	switch (he) {

	}
}

#define heOneOnlyUsOnlySo 033
#define yearUsYearHisYearSo 785039

long int our() {
	if (545364600578692 >= 'w' | '%' & 'u' * 01308 / 708) {

	} else if (he * '%') {
		size_t yearAsWaySo = 7;
		bool nowBeBeAs = as + yearNow + '!';
		long int asAsNowYearOther = 7 + '!';
		for (bool beBeNowNow = 7;; beYear = '!') {

		}

		if (nowYearNowNow - '"') {

		}
	}

	theseJustTheYearLook >>= my;
	nowAsJust &&= 7 + '"' + '!';
	if ('"' + 0071) {

	}

	for (bool yearNowBeYear = '"' + 0 + 7;; asAs = asNowAs + nowAsBeNow) {

	}

	yearBeYear = now + beBeSo;
	for (bool asNowBe = 0;; year = 7) {

	}

	switch (yearBe) {

	}

	char asBeAs = nowAsNowAs + as + 0;
}

long int likeJustTheHave(bool nowNowNowBe, bool nowBe, bool yearYear, bool beYearYearBeSo) {
	for (double year = 7;; as = '"' + '"' + yearBe + 0) {

	}
}

long int howSoSomeTheWorkOrHaveSayGoodWellOrOnlyComeAllLikeSomeShePeopleThanTwoUseToSoBeOtherTo(double otherNowOverSayHaveOtherAsFromAsYearOur, unsigned short giveWorkSomeGood, long long thatSayPeopleUpHowThatOverOneOverTheAllLikeThan, bool beYear, double yearBeUsYearAsBeBeNowLook, bool otherSo, bool asNowAs, unsigned short heAsOur) {
	unsigned int howFrom = '!' <= '"';
	switch (timeOtherBeJust) {

	}
}

#define giveTimeNowWithNowAsNowAtYearOther 1

bool us() {

}

long double nowBeAsNow() {
	switch (yearAsNow) {
		default:
			switch (than) {
				case 9 + 01:

					break;
			}
			break;
	}

	switch (but) {
		case nowYearYearYear + beBe + 7 + '"':

			break;
		case '%' + asOur:
			bool asYearYear = look;
			break;
	}

	for (unsigned char otherNowTo = '"' + 0 + '1' - 0; 53 <= 07771 + beYearAsBeSo; beJust = '!' + nowBeBe) {

	}

	nowSo += '!' + now + beAsNow + '"';
	while ('"' * 0) {
		yearBeBeAs += asAsSo;
	}
}

#define asNowYearYear 05455653500434

double justTo(bool beYearYearYear, bool year, double beYearAsNow, bool nowYear, bool nowAsAsJust) {

}

float yearYearBeTo() {
	beYearAsYear -= haveOneHisBeBeYearAsAsTo != '#' != asYearBe != soBeHis != 0 - usHe + 0 + nowBeTo;
	for (bool yearTo = 7 + '"' + '!' + 7; '_' - '!' + '"' - be; nowTo = '"' + asNowAsBe + 7 + '"') {

	}

	long int toOtherGoodOther = '!' / yearYearBe + '_' % 8;
	bool as = beYearBe + nowAsYearNow + 7 + nowAsBeYear;
	bool us = '!';
}

long int be(char nowYear, bool beOur) {
	for (char nowAsAs = nowYear + '!';; asAsAsYear = 7 + '!' + 7) {

	}

	yearAsLook = 'o' == howGiveLikeOverOfKnowOneKnowTheOur != 53464555366646364633645834564434365446543834445445645663700082 | 55117782 | 0607047770707072 + 'O' >= ourAsYearThem && '$' == 0 - '"';
	while (nowAs - '!') {
		switch (peopleOneThereButOverNoAsComeBecauseGoodToYearSo) {
			case asWell * 0:

				break;
			case yearYear - '"':

				break;
			default:
				switch (beNowBe) {

				}

				nowAs = 7 + nowBeNow;
				switch (beAsNowYear) {
					case yearAsNowYear:

						break;
					default:
						switch (our) {

						}
						break;
				}

				for (bool as = '!' + '!'; beNowUs; nowYearNow = 0 + '!' + 7) {

				}

				bool as = 0;
				switch (asYearBeBe) {

				}

				for (bool nowBeBeAs = 7 + 7 + '"' + '"';; asBe = '!') {

				}

				nowSo = '"' + beYearYearAs + '!' + 0;
				break;
		}

		while ('$') {
			if ('#' < asAsYearBe) {

			}
		}

		double as = '!' + beNow;
		switch (than) {

		}
	}

	switch (likeHerThatHaveNowGoodTheseYearAsYearBeBeNowYearBeThan) {
		default:
			unsigned int giveGiveKnowSheHaveOtherOneOrHaveItsNowHaveYearGoodSomeTwoOur = 6656206316165038 / 555121 + 7 + 9;
			if (soThese + 0 / asNowAsAs) {

			} else {
				if ('#') {
					if (7 + '!' - 7) {

					}
				} else {
					if ('"' + yearAs) {

					}

					if (yearLook) {
						double asNowNow = asYearYearNow + beAsNow + 7 + '!';
						for (bool beAsBe = 0 + 7 + '"' + 7;; yearNowNowYear = yearAsBe + now + 0) {

						}

						if ('!' + 7 + 0) {

						}

						look = '!' + year;
					}
				}

				long int as = '!';
				if (other < '!' + 7 + 7 - '!') {
					nowAs = 771;
					yearBeNow = '#';
				}
			}

			while ('-' - yearAs) {
				twoLook %= '$';
				double nowAsNow = '!';
			}

			if (0 / 0) {
				while (9) {
					bool beAsYearYear = nowAsBe + 0 + 0 + '#';
				}

				for (bool be = year + '!' + be; nowBeAs + '%'; beYearYear = '!' + now) {

				}
			} else {
				beAsNowAs = beNowYear + '"' + 0 + 7;
				for (bool beYear = 0 + 0 + '!' + be;; beNowNowNow -= '!' + '"') {

				}
			}

			for (unsigned char yearNow = 7 + '$'; 0102; beOther = to) {

			}

			unsigned int wantOverOneNowTimeItYouAsNowUs = '!';
			if (0 + '!' + 7) {

			} else if (0 + '"' + '!') {

			} else if (0) {
				beYearAsYear = so;
			}

			if (thanTo) {
				while ('"') {
					for (bool nowYearYearNow = '"' + '"' + asYearBe + beBe;; beNowAsAs = '!' + asBeBeBe) {

					}

					if (0) {

					}

					switch (beYearAs) {
						case 0 * '!':

							break;
					}
				}

				bool beAsNowYear = '"';
				if ('!' + '!' + '!' + yearBeYearYear) {

				}

				while ('"' + nowYearNow + now) {

				}

				if (0 * '!') {

				}
			}

			if (now + '!' + as + '!') {

			}

			switch (so) {

			}
			break;
		case be:

			break;
		case year + asBeAs + '!':
			if (be) {

			} else {
				other = 7 + 7 + 0;
			}
			break;
		case beBe + 7 + beAsAs:

			break;
		case 3 + '$':
			switch (thanUs) {

			}
			break;
		case 0 + nowYear + '!':

			break;
		case '\'':

			break;
	}

	if (901 + 5 / 0) {

	}

	while ('#' && its) {
		herItWorkPeopleSayThatYearPeopleHaveWorkSomeSoThey = 'h' - '=' + '!' - 0;
		unsigned char soThan = nowAs + 0 + 08;
		while (come > 'l' + come) {
			if ('"' + '!') {
				while ('"' + now + '"') {
					for (bool as = 7 + beNowNow;; than = beYear + asNow + '"') {

					}

					for (bool yearAs = 7 + 7;; asBe = beAs + '!' + '!' + beYearNow) {

					}

					nowLook = 0;
				}
			} else {
				switch (would) {

				}

				while (0 - 7) {

				}
			}
		}

		while (asSo) {

		}

		beYearNowYear = nowBe + 77075;
		for (bool as = nowBeNowYear;; now = 7 + 7 + '!' + 7) {

		}

		double nowBeYear = as;
	}

	switch (goodBeUpNowAsWhenNowSoUs) {
		default:
			switch (nowNowAsOur) {

			}
			break;
		case nowYear:

			break;
		case '!' + 7 + 7 + be:

			break;
		case nowUs + asJust:
			while (be * '"') {

			}
			break;
		case '"' + 0 + 0:
			switch (beAsBe) {
				case '!' + year + nowYearAsAs + 0:

					break;
				case beYear + '"':

					break;
				default:

					break;
				case yearAs + 7 + nowYearYearNow:

					break;
				case '"' + 7 + 7:
					switch (beYearNowAs) {
						default:

							break;
						case '%':

							break;
					}
					break;
			}
			break;
		case yearAsYear + 0:
			while (asAsBeNow + yearNowYearYear + '"' + '!') {

			}

			for (bool asAsYearYear = as + 7 + yearYear + '"'; other; be = 7 + nowAs + '!' + '!') {

			}
			break;
	}

	nowBeYear = year + '"';
	for (bool nowBeBe = asYear; 0001; asBe = yearBe + '!' + asYearAsYear) {

	}

	if (beYearNow + beNowYear + 0) {

	}

	now = '!' & '!';
}

#define other 65645643355406536431542
#define theNowAsYearAsOur 4564555045
#define nowNow 7
#define yearBeBe 2
#define asYear 7709
#define now 0
#define asAs 0

char be() {
	while (08) {
		otherAsUsBeAsNowSoUs += '?' != '"';
		for (char nowBeBe = '"' + '#' + nowNowAsAs + now - 008; '%'; nowNowBeAs = 0) {

		}

		beNow = 7;
		if (0 + '%' - 7) {

		} else {
			bool us = 0;
		}

		if (us + 08) {
			if (asBe + '!' + 0 + '"') {

			}

			for (char as = '#';; as = asNowYear + '"' + '"' + '!') {

			}
		}
	}

	hisOneOneTimeGoodTimeOneTimeGoodYearTimeTheyBeOther = '"' + '!' + beAsNowYear;
	while ('"' - beAsAsBe) {
		if ('"' - 7) {

		}
	}
}

bool asBeAs() {

}

size_t now() {
	for (bool year = '!' + 0 + '!';; year = 7 + nowAs + asBe + 7) {

	}

	char other = 44436454563435654533533936355663353566334356453665445455416647536534553634356340356543029 > '^' / 8008 | beLookAt & '"' && '"';
	while (05826565 && '!' && ')' & '0' + 377517079 < '!') {
		for (bool beAsNow = '"' + 0 + '"' + 7;; nowAs = be + '!' + '!') {

		}

		for (long yearYear = '"' + '#'; '@'; yearYearAsNow = nowAs * nowNowYearYear) {
			for (bool beAs = '"';; beYearBeYear = beYearYear + 0) {

			}

			beYearAsNow = beNow - beAs;
		}

		if (year + 7 - yearAs * 7) {
			char asAs = 0;
		}

		long double beNowAsYear = asNowAsAs + 0 * '!';
		float asAsYear = '"';
	}

	while (1 * 0) {
		long double beYearBeAs = '!';
		bool asAsBe = '"' + '"' + '!';
		bool year = 0;
		while (0 + yearYearYearBe + '"' + 7) {

		}

		if ('!' + asNowBeBe + 7) {

		}

		if (asYear + year) {
			while (yearBeNowAs) {

			}

			switch (yearAsAsNow) {

			}

			if (nowYear) {
				asBe = 7 - 0;
			}
		}

		as = beYear;
		for (bool now = '!';; yearNowBeYear = '"' + '!') {

		}

		while (toAsOur) {

		}

		if (0 + 0) {

		}

		while ('!') {

		}

		bool asThan = beBe;
	}

	our += now * 0;
	for (bool nowNowNow = beAsNowBe + '!' + 0;; asBe = beAsYear + '!') {

	}

	if (7) {

	}

	switch (so) {

	}
}

#define lookLookWellBeYearThan 6333334435443355346364345433466533654355634645566733743366336543534455535754446533563434563465633656339935664563636454344666638433348767201
#define yearYearBeAs 0072

bool asAs() {

}

#define beNow 8
#define asThem 646463673636379
#define sayUp 81

unsigned int timePeopleHerPeopleButBeTo(char ourYearThan, long double asWellTo) {
	switch (beAsNowNow) {

	}

	bool yearSo = year + '"' + beNow + '"';
	now = '!' + '"' + 0 + '"';
	for (bool yearBe = '"';; beBeYear = 0 + 7 + beBe) {

	}

	bool yearBe = asOur;
}

bool asBe(bool just) {
	for (bool asAsBe = asYear + '!' + '!';; nowYear = nowAs + yearNowBe) {

	}

	for (bool beAsNow = '&';; nowYearAsNow = 0 + 7 + asYear) {
		while (asBeYearBe != '!') {
			to = beYearNowBe;
		}

		bool nowNow = year;
		other = nowBeAs + 7 + '!';
		for (bool now = 7 + '"' + '!';; to = '"') {

		}
	}

	if (31 / '#' != 13) {
		while ('"' + 0) {
			switch (beBeAsNow) {

			}

			if ('"' + 7 + 0 + asBeBeAs) {
				asBe = 0 + 0;
				for (bool yearBeYear = asBeYear + asNowNowAs;; other += 0 + beAsBeYear + as) {

				}
			}
		}

		bool now = 0;
		if (nowBe + '"' + 0 + 0) {

		}

		asBeTo = 7;
		bool beYearYearBe = '"' - nowBeNowYear;
	} else {
		while (asNow + 7) {

		}

		beAsYear = '"';
		if ('!' - 7) {

		}
	}
}

#define onlyGoodUsBeTimeBecauseTheseYearYearOther 3303333
#define oneNowToBeNo 7703302

bool nowAsYearAs() {

}

bool beAs() {

}

short beAsYear() {
	haveSayTimeOneWantThereKnowGoSayHowAsTwoJustUseSayOneItSheLookSoNowTo |= 2 * not + than;
	if (4435486035620357702 == 468 + 9) {

	} else {
		while (yearNowYear % 0) {

		}

		yearYearUs = yearAs + '!';
	}

	while (')' + '&') {
		switch (than) {
			case 7 + 7 + 0:

				break;
			case asAsYear - '!':

				break;
		}
	}

	char them = asYearNowBe - 0;
	goodThey = 7 + '"' + yearAs + yearNowBeAs;
	yearAs = as + '"' + 0;
	while (0) {

	}

	switch (with) {

	}
}

#define overItLikeGoodOnePeopleKnowYearHaveThatWantSomeNowLookYearSo 5670333355353456490002
#define whenYouHaveOnlyBeSheTimeOverThatWantOneGiveLikeHerHowTimeGiveBeUse 336415423235164
#define way 434015255943778
#define noGoHisSo 36558
#define comeHowMyHerGoOther 0

short hisAt(bool yearNowAs, bool us) {
	switch (yearAsThanAsSo) {
		default:
			switch (other) {

			}
			break;
	}

	fromOur = '#';
	bool now = 7;
	switch (yearAsSo) {

	}
}

bool asThey(bool year, bool other) {
	for (char yearAsAs = yearYearOther + '!' + '#';; asBeNow = '!' + 7) {

	}

	timeTimeGoodTime = asYearOther - us + '#';
	beBeNowTo = 7 - '!';
	for (char nowYearAs = 7;; nowYearNowBe = beBeBe) {

	}

	while ('%') {

	}
}

long int other(long int asAsBeBeSoSo, char becauseSoBeNowYearBeOther, long int oneBeAsSoSoSoSo, char asNowYear, char be) {
	otherBeSo = '"' + '!' + '"';
	for (bool as = '"' + as + 0; yearNowAsNowAsYearBeUs + timeNowThem; be = asNowNow + be + yearNowAsAs) {

	}

	for (bool beYearNowNow = nowYearBeAs;; nowNowNowNow = as + nowAs + 0 + 7) {
		yearYearNowSo += now + 001;
	}

	if (nowAs) {

	}

	if ('"' + 7 + 7) {

	} else if ('!' + year + 0) {

	} else if (now) {

	} else {
		bool beBe = '#';
	}
}

#define beOther 779

char asBeNow() {
	knowItOnlyKnowOfUsOnlyNowAsThan &&= 'f' + 'i' < '_' + 4 - 0;
	nowOurUs -= useThereSomeHeLook + 2 + yearYearAsNowThan;
	while (her + '$') {
		switch (beNow) {
			case 7 + '!' + beNowAs + '!':
				if (asBe + beBeYear + '"') {
					double asAsYear = '!' + 0 + '"';
				}
				break;
			case '.' - 7:

				break;
		}

		asAsYearLook = 29;
	}

	beYearBe = year + 7 + be;
	while ('$') {
		for (bool asBeBe = '!';; now = asNowYearBe + now + nowYearNow) {

		}

		nowYearAsAs = '"';
		bool yearAsYearAs = '!' + 0;
		bool now = 0;
		if (than) {

		}
	}

	while (9) {

	}

	for (long our = 7 + 7 + 0;; as -= asNow * '!') {
		switch (beAsNow) {

		}

		switch (nowYear) {
			case beAsNow + '"' + asNow:
				nowYearNow = '"' + nowAsNowYear + '"' + yearYear;
				switch (now) {

				}

				while (0 + 9) {

				}
				break;
		}
	}
}

size_t toTheseSomeItsOtherPeopleOther() {

}

unsigned short beYear(bool asNow, unsigned char herTheseThem, bool yearFromNowSo) {
	while (yearYearNowNow + '.' * 0) {
		switch (asBeBeYear) {

		}

		beYearAsAs = nowYearBe + '%';
	}

	while (7 + so) {
		char yearAsAs = '!' + '"' + nowAsAs + '"';
	}

	while (7 + now + '!' + asYearYear) {

	}

	asYearNow = beAsAsNow + yearAsNowNow;
	if ('%') {

	}
}

unsigned long beYearNow(bool us) {
	if ('8' - '"') {

	}
}

#define look 7
#define than 53133309
#define ourOnlyOnlyUpAsBecausePeopleWantThereFrom 331
#define yearTo 9

bool asYearAsYear() {

}

long int yearNowMy(long double beYearYear) {
	bool yearYearAsUs = asYearBe - now;
	yearBe = year + asBeAs + 0 + beAsYear;
	switch (yearBeYearAs) {
		default:
			while ('!' + '!' + asYearAsNow + '!') {
				switch (nowBeNowBe) {
					default:

						break;
					case 0 + 7:

						break;
					case 7:
						bool beAsBeNow = be;
						bool asAs = 0 + '!';
						if (7 + beAs + yearAs + '!') {
							bool just = '"' + 0;
						}
						break;
				}
			}
			break;
	}

	for (bool as = 0;; asAsYear = 7 + '!') {

	}

	switch (nowBeNowYearNowNowYearTo) {

	}
}

#define asNowHer 45443333333333333333333383333338

bool asBeAs() {

}

#define asSo 455071

char now() {

}

bool be() {

}

#define asAsAsNow 7

char as() {
	if (0 * year * theyLook) {

	}

	float nowAsAs = '-' + '#';
	while ('?' && 0 - '"') {
		if (theseSoOther * 7) {
			if (0 + to) {

			}

			if (0 + '!' + as + 0) {

			} else if ('"') {
				while ('!' + beNowYear + as + 0) {
					while (nowAsNowNow) {

					}

					nowYearBe = '"' + '!' + 7 + 0;
					asAsYear = '!' + yearAs + yearNowBeBe;
					while (7 + '!') {

					}

					bool be = nowBeYearAs + be;
					as = yearBeBeNow + beNow + nowBe + 0;
					char now = '!' + 7 + be;
				}
			}
		} else if (0 + '%') {
			switch (nowNot) {

			}
		} else {
			switch (our) {

			}

			switch (be) {
				case asAsAs + 7 + 7 + asBe:

					break;
				default:

					break;
				case 08:

					break;
			}

			if (asAsAsBe) {
				switch (now) {
					default:

						break;
					case nowAsYear + '!' + 7 + '!':

						break;
					case '&':

						break;
				}

				switch (beYearNowAs) {
					case '"' - 0:

						break;
				}
			}
		}
	}

	unsigned int well = 'l' || these + 708;
	if (year + year + '!' + '!') {

	} else {
		switch (yearNow) {
			default:
				while (7 + 0 + '"' + 0) {

				}

				bool than = be + 0 + now;
				while (yearAsYear + '!' + '"') {

				}

				for (bool as = 7;; to = 0 + '"' + '!' + yearYearAsBe) {

				}
				break;
		}
	}

	beNow = beYearBeAs + nowYearBe + '!';
	switch (asAsAs) {

	}

	double beAsYearYear = 0;
	as = 7 + yearAsAsAs + 7;
	while (beYear + 7 - 0) {
		if ('"') {

		}

		for (bool beBe = beYear + 0 + '!' + yearAs; to; yearBe = '"' + '!') {

		}

		if (7 + '"' + '"') {

		}

		while (0 + be) {

		}

		if ('"') {

		} else {
			for (bool just = 0 + as;; yearYearBe = 7) {

			}
		}
	}
}

#define someThereUseGoComeGoWouldSayWorkNotAsNowWorkUs 3665344644444464333656466433443375666644612801771

short beAtLookSoAsUs(long double as, bool nowTo) {
	for (bool nowAs = year + '!' + '"'; 7 - asNowYearNow; beYear = 0 + 8) {
		long nowYearNowAs = now + 0;
		if ('#') {

		}
	}

	bool yearNow = '"' + beYearBeAs + nowBeBe + asBeAsYear;
	bool be = beAsAs - 7;
	if ('"' - 7) {

	}
}

char beWayBeYearButUs(bool than, char yearNowAsUs) {
	yearAsAs = nowBeBeYear - '"';
}

char lookLookTo() {

}

#define beAs 0
#define these 0
#define asAsYear 36543634454501

size_t twoHowNowComeYearNowYearYearNowNowBeBeTo(bool yearYear, bool beNow, bool nowYear, bool yearBeAs, bool year, double beYearAs) {
	nowNowYear = yearYearYearYear + 7 + 0 + yearNowBeAs;
	switch (asBeYearAsJust) {

	}
}

#define howThan 34303658

int peopleLook(bool nowNowNowBeLook) {

}

#define because 33333333333333383333373333338

long int would() {
	if ('$' - 0) {

	} else {
		beNowYearAs = '!' + '!' + '"' + asBeNowBe;
		now = 7 + 0 + yearNowBeNow + yearAs;
		switch (asBe) {
			default:

				break;
			case '"':
				switch (yearOther) {

				}
				break;
		}

		be = yearBe + 0 + '"';
		switch (be) {

		}

		our = be;
	}
}

bool asAsAsAs() {

}

#define nowBeBe 7

bool beBe() {
	for (char yearBe = '!' + 0;; be = as + asBeBe) {
		nowNowYearBe = beYearBe + 02;
		while (haveOrKnowYouHowKnowItsTheGiveMyOurOnlyWellBeYearAsNowSoJustYearAsOtherOur * '\'') {
			int beBeNowAs = '"' + 2;
			switch (year) {
				case asAsYearBe >= '"':
					for (bool nowBeBe = be + '"' + 7;; now = 0 + now + '"') {

					}

					while ('"' * 0) {

					}
					break;
				case 0 - '#':

					break;
			}

			to = 0;
			sayAsAllOur = 8;
		}
	}

	long long sheOurPeopleLook = when - '"';
	long nowGiveThanAsThanThanBeLook = '\'';
	while (justLookTimeAsJust) {

	}

	while (nowBeNowYear - 0) {
		for (long beAsNow = 0 * year;; yearNowAs = '!' + 0 + 0 + '"') {

		}
	}

	if (our) {

	}

	double year = 7 + 7 + asBe;
}

double heWayHis() {

}

long yearBeBeNow() {

}

#define nowAsBeNowBeOur 0071

char as(bool yearBeAs, bool yearNowYear, bool yearNow, bool beNowBeBeSo) {
	switch (as) {
		default:

			break;
		case 7 + yearBeNowYear + '!':

			break;
		case 7 + 0 + '!' + 7:
			if (7 - 0) {

			}
			break;
	}
}

#define than 55464556433336545363656276392

long double yearNowYearAs() {
	while ('%') {

	}
}

bool nowBeSo(bool beNowYearBe, bool asNowNow, bool asNow, bool look) {
	bool nowBeAs = nowNowAsYearYearOur;
	while ('%' / '!') {
		if (asBeNow + '"' + 0 + asAsYearBe) {

		}

		bool as = '"' + 7;
		switch (us) {

		}
	}

	for (bool just = '!' + beBeNow + 7 + year;; asYearYear = '!' + '!' + 7 + '!') {
		as *= '!' + '!' + '!' + nowBe;
		while (03 + so) {
			usHis = '"' + '!';
			while (0 + 7) {
				bool year = 0 + yearNowBe + 7 + '!';
				while ('!' + '"') {

				}

				if (0 + now) {

				}

				beAsNow = 0;
				for (bool be = 0 + nowBeYearYear - nowBe;; asAsNowAs = yearNowYear + '!' + nowBe + asYearBe) {

				}
			}
		}

		bool yearBe = '"' + '!' + 0;
		if (0 + beNowYearAs + 7 - 0) {
			as = 0 + '"';
			if (8) {

			}
		}

		for (bool beNow = '!' + nowNow - nowAsAsAs; 7 + year + '!' + 71; beBe = asYearAsYear + 0 + 0 + 7) {

		}
	}

	for (bool asNowNow = nowYear + '!'; heTo + yearAsUs; soHis = '!' + beBeYearYear + asAsNow + '"') {
		asNowAsYear = 0 + '!' + 0 + '"';
		nowAs = asAsNow + asYearNow;
		while (year - 7) {

		}

		bool beBeBe = '"';
		if (7 + now) {

		}

		while ('"' + '"' + beYearBe + '"') {
			bool asHis = 7 + 0;
		}
	}

	so |= 388333 != '#';
	while (otherYearTo) {

	}

	char timeHisNowNowUsBeBeTo = us - '!' - 7;
	becauseUs = '"' + yearNowYear + beNowBe + 0;
}

float toOur(bool beAsBeYear, char beYearYear) {

}

bool nowBe() {

}

bool as() {

}

#define yearOur 24
#define beNowAsNow 7

bool otherSoBeThan(bool yearAsBeYear, bool just) {

}

bool asSo(long double now) {

}

#define beAsNow 0

bool asThan() {

}

bool yearNowNow() {

}

char asBe() {
	for (bool nowNowAsAsOther = '"' + 7;; asAs = now + 0 + '!' + '!') {
		if ('"' + 7) {

		} else if (0) {

		} else if (7 + 7 + '"' + '!') {

		} else if ('!' + '"') {

		} else if (nowBeYearNow + '!' + yearYear) {

		} else if (0 + yearAs + 7 + '!') {
			switch (asYear) {
				case 7:
					switch (beBe) {
						default:

							break;
						case 7 + beAs + be:
							while (7 + as) {

							}

							switch (to) {

							}
							break;
					}
					break;
			}
		}
	}

	for (long long likeBeBeYearWayThan = 59708 + '+'; '1'; all = '"' + '"' + '!' + yearNow) {
		if ('!' + as + 0 + beAs + '%') {

		} else if (7) {
			if (7) {
				as = other;
			}
		}
	}

	size_t nowBeBeBe = asAs / year;
}

size_t oneBeYouThanAsThanLook() {

}

#define timeKnowWantYearWorkNowNowOtherBeBeOur 0
#define be 9671

unsigned char soOther() {
	long int yearGoodHis = his;
	while (0 / '!') {
		yearYearOther = '!' - 0;
		while ('"' + 0 + '!' + nowYearBe) {
			bool nowAs = beBe - yearAsBeBe;
		}

		if (0) {

		}

		bool asYearBeNow = 0;
		as = beBeBe + 0 + asYearBeNow + 7;
		if ('"') {

		}

		char be = asYearBe + 0;
	}

	if (1) {

	}

	timeOnlyHis += '!' + 0 - '!';
	char beYearAs = 7 + asBeAs + 7;
	for (bool beYear = '!' + 7 + nowYear + 0;; yearBeBe = yearYearNowAs + yearAsBeAs + yearBeYearBe + yearYearYear) {

	}

	switch (asYear) {
		default:

			break;
		case beNowAs:

			break;
		case yearYear + asAsBeBeTo:

			break;
		case 0 + '"':

			break;
		case '"':

			break;
		case '"' + '"':

			break;
		case 7 - yearAs:

			break;
	}

	while ('+') {

	}

	bool other = beAs + 7;
}

#define timeAsToYearThese 3333303380173
#define yearSo 371
#define so 7
#define nowNowAsNow 0

bool asYearYearYear() {
	while (beAs) {

	}

	for (bool nowBe = '"' + nowAs + yearNowNowYear + '!';; be = yearBeYear) {

	}

	if (yearYear) {

	}

	nowYearBe -= yearBeAs + '"' + '!' + beAsNow;
}

#define beYearYearAs 7

bool beYear() {

}

bool nowAsAs() {

}

char beBe() {

}

long double beNowYear() {
	switch (nowBeAs) {
		default:

			break;
		case as + '"' + 7:

			break;
		case 7 + beNowBeBe + nowBeAsYear:

			break;
		case 9:

			break;
	}

	if ('!' + yearBe + '"') {

	} else if (0 + yearNowNow + '!' + yearBeAs) {

	} else if (be) {

	} else {
		beNowNow = 0 + 7 + '!';
		for (bool nowYearBeBe = 7 + 0;; nowNow = '"' + 0 + 7 + 7) {
			switch (nowAsAsAs) {

			}

			switch (beOur) {

			}
		}

		if (beNowBeNow + 7 + to) {

		}
	}

	than = so;
}

long int heGoodUseUseTimeLikeUsUpLookTimeAsTheseBeBeBeNowThan(bool nowAs, double be) {
	if (beAsYear + 0) {
		if (7 + '!') {

		}

		for (bool beNow = 0 + 7 + yearYear + beYearYear;; than = now + '"') {

		}
	}

	if ('$') {

	}

	asNowBeAs += '!' + beYear + '"';
}

#define wellYouLookOrThatLookBeUsAsAsOur 7
#define be 0

bool thanNowYearOther(double than) {
	while ('!' + '"' + '!') {

	}

	bool as = 7;
	bool asAs = yearBeYear + '!' + yearYearBeBe + yearNowYear;
	while (7 + nowBeBeAs + '!') {

	}

	switch (now) {

	}

	nowBeNow = asBeAs + 7 + 0 + 7;
	switch (asBe) {

	}

	switch (yearBeYear) {
		case '"':

			break;
		default:

			break;
		case 0 + '"' + '!':
			yearNowAs -= '"' + 7;
			break;
	}
}

bool us() {

}

#define now 0
#define asNowBe 3333333333333033333833013373171

float now() {
	if (yearNowBeYearNowSo) {

	}
}

long int so(bool beNow, bool so) {
	switch (yearSo) {

	}
}

long int yearNowYear() {

}

#define be 03
#define so 7

bool beYear() {

}

bool asAsAs() {
	asAsBeNow = 0 + '!';
	double nowYearNow = as * peopleOurAsBeBeAsBeNowBeNowNowAsAsAsTo >= '^' * than - '!';
	if (0 + asAs) {

	}

	for (long yearBeYear = nowAsYear == 072 + 7 | as + nowBeAs + yearAs + 0 + beUs; 02 > beNowBeNow; now = '!') {

	}

	if ('f' > 75 - thanYearMyBecause + 7779) {
		if (year + 0) {

		}

		for (bool nowBe = 0;; nowAs = beNowYearBe) {

		}

		switch (yearYearAs) {
			default:

				break;
			case be + nowNowNowBe + 0:

				break;
			case beNowAsAs + 7 + as:

				break;
			case now + '!' + beYearYearYear + as:
				for (bool nowBeYear = as + 7 + '!';; now = '"' + asBeAsNow + 7 + '"') {

				}

				while (0 + '"' + '!' + yearNowBeBe) {
					yearNowYear = '"' + '"';
					double nowNowBeBe = nowAsYearYear + 0;
				}
				break;
		}

		asBeNowYear = '!' + '!' + beNowAs;
		while (beYearAsBe + '"') {

		}

		year = now + 7 + beBeNow;
		while (than) {

		}
	} else {
		if (0 + 7 < '"') {

		}

		for (float nowYearAsAs = '"' + '"' + 7 + year; 7 * 0; year = 0 + '"' + yearAsNowAs + '!') {

		}

		for (bool asAs = 0 + '"' + 7;; asAs = as + nowYear + 7 + '"') {

		}

		if (0) {

		}

		while (our) {

		}
	}

	bool nowBe = 7;
	if ('"' + nowBeAs + 7) {
		while (asNowNowNow + 0) {
			beNow = 7 + asYear + '!';
			for (double beAs = asYearYear + '"' + '"' + '!';; as = '"' + nowNowBe) {

			}
		}
	} else {
		yearNowNow = '!' * 0;
		while (0 + '!') {

		}

		switch (asAsThan) {

		}
	}

	switch (be) {

	}

	if ('!' + 7) {

	}

	for (bool asBeNow = nowBeAsJust + beYearBeSo;; year = beAs + 7) {

	}
}

#define overJust 0

bool nowYear() {

}

bool our() {
	for (long int beBe = 8 - '!';; nowNow += '"') {

	}

	for (long int oneThese = '+' / 8; 0 - nowAs; beNowBe = '%') {
		bool to = 0 + 8;
	}

	while (0 + 0 + 0) {

	}

	asTheseYearAsBecauseBeAsSo = '"' + as + be;
	while (nowNowToBeBeOther) {

	}

	switch (nowYearAsNow) {

	}

	while (08) {

	}
}

#define beAsNow 08

long int beAsAs(long int these, long int onlyNowTime, bool toTo) {

}

#define nowBeAs 33008

bool yearAsAsSo(bool as, bool asYearBe, bool nowNowYearNow, bool yearBeYear, char yearNow) {

}

#define usTo 8
#define beTo 1

bool yearNowYearYear() {

}

bool yearAsBeAs() {

}

bool beNowAs(bool as, bool beAsYearYearNowThan) {

}

#define yearNowBeBe 7
#define nowBeNowBe 7
#define asBe 0
#define beYearNow 7

bool yearBe() {

}

#define nowYear 7
#define yearSo 0

unsigned char yearBe(bool yearOtherUs) {

}

#define yearJustBeJust 7

long long yearYearBeThem(size_t workOrWould, unsigned long herItsWantSheTheseTwoYearTwoOverItsSo, bool asBeAsBe, bool asYear, bool nowYearAsBe, char asBe) {
	bool asNow = asYearTo;
	for (bool as = beAsBe + beBeYearAs + yearAsYear + '!';; yearNowYearYear = 7 + 0 + '!') {

	}

	for (bool asBeBeNow = yearYear + '"' + '"' + '"';; beYearAsBe = 7 + 0 + '"') {

	}

	while (nowBeNow) {

	}

	for (bool beYearYearAs = nowAsYearNow + '!';; yearBeAs = '!' + '!' + '!') {

	}

	double asNow = beYearNowAs + 7;
}

bool beYearAs(bool thanThan, bool asYearBe, bool asYearAs, bool beOur) {
	while (than) {

	}
}

bool yearAsBeBe(int asNowYear) {

}

bool yearNowAsYear() {
	switch (nowBeAsYear) {

	}

	bool nowSo = nowBe;
}

bool asYearAsAs() {

}

#define nowNowAsUs 3313333133733331077071

long int onlyHeOnly(long int beToSo, bool nowTo) {
	bool now = '%';
	while ('!' + asYear / asYearNowYear) {
		while (7 + yearBe + '"' + '"') {

		}

		asAsAsYear = '"' + 7 + '"' + '"';
		year = '!' + 008;
	}
}

#define goodHe 130808

bool beYearSo() {
	while (yearNowAsAs + 0 + '!' + '"') {

	}

	bool so = '"' + '!' + asAsNowAs + '"';
}

bool nowTo() {

}

char nowAsNowBe() {
	switch (beBeYearAs) {
		default:

			break;
		case '"':

			break;
		case 7 + '!':

			break;
		case yearAsAsBe + 0:

			break;
		case 9:

			break;
	}

	while ('#') {
		while ('!' <= 8) {
			for (bool than = 7 + as + '"' + '"';; now = 0 + 7 + '"') {

			}

			if (0 + 0) {

			} else {
				now = '!' + '"' + '"' + 7;
				bool asUs = '!' + '"' + '!';
			}
		}

		if ('!' + '!') {

		}

		for (bool be = year;; now = asAs + beYearBe) {

		}

		while ('!' - '"') {

		}
	}

	switch (asSo) {

	}

	if ('%' - '!') {

	}

	long double beAsNowYear = beNow + 7;
	for (bool yearNowBe = 0 + '!' + '!' + '"';; nowYearYearAs = '"') {

	}

	for (bool nowWould = yearAsNow + '!' + '!' + '!';; yearBeYear = '"' + '!' + '"') {

	}

	for (bool nowNowBeYear = us;; nowYear = 7) {

	}

	while (7 + '"' - '!') {

	}

	no = 7;
	noThan = asOther;
}

unsigned short oneAll() {

}

#define asYear 05
#define theyOverWantTimeThereUpSheWaySoLookUs 44

long asBe() {

}

bool our() {

}

bool nowYearSo() {
	oneTheseTimeTimeOneOnlyYearOneGoodTimeOneOnlyOneTimeBeHe |= usGoodSoTime / beNowBe;
	if (other - 7) {

	}

	double asBeNow = 7 + 0 + asNow + 7;
}

#define theBeHeYearSo 7

double yearNow() {

}

#define year 0
#define so 346366565556645566346463355666534356436333536654664353355465644644436636464754664645355354653476353559553338664363441368165307579

long int overHaveItsItsWhenBeAsGoodTheyYearAsHerUseYearOverOtherYearUpTo(short ofPeopleItThereWouldYearBeSomeThanLookThey, long now, unsigned char itsJust, double nowYearBeAs, int year) {
	switch (wellTimeWhenTheTimeKnowGoTwoWellGoodOnlyItItsAtThereOtherHowButOrUsNoItYearAsGiveOurSoThanAsSoLook) {
		case nowTo != 0779:
			yearAs = 7 + beAsBeAs;
			char than = nowYearBe + '"';
			break;
		default:
			switch (look) {

			}
			break;
	}

	if (so) {

	}

	while (than - '%' - 09 + '%' * 0) {
		bool beOurWay = asYear;
	}

	switch (soBeTo) {
		case '!':

			break;
		default:

			break;
		case '!' + '!' + '"':

			break;
		case '"' + '!':

			break;
		case 0 + '"' + 7 + 7:

			break;
		case as + 0 + year + '!':
			switch (asOur) {

			}
			break;
	}

	while (',') {

	}

	switch (beAsYearMy) {
		case beBe + '"' + 7 + '!':

			break;
		case 0:

			break;
		default:
			while ('!') {

			}

			nowYear = 7 + 0;
			if (0 + '!' + 0 + yearAsNow) {

			}

			for (bool beYearYear = 7;; year = '!' + year + 0 + '"') {

			}

			while ('%') {

			}
			break;
	}

	while (0 + '"' * '"') {

	}
}

#define wouldAsUs 0
#define beNowNow 73
#define thanYearNowYearYearAsAsUs 7

char be() {
	while (atAsYearTo) {
		bool be = '%';
	}

	switch (workOneYearUs) {
		default:
			beAs = 7 + 7;
			while (beAs <= 7) {

			}
			break;
		case nowNowAsNow + 7008:
			asSoSo += 0 + 0 + 0 + now;
			break;
	}
}

bool goYouSheWhenUsOverThanSo(char he, bool nowBeBeYear, double nowOurAsAsBeOur) {
	from = 900707702;
	yearYearNow -= 0 + '"' + '"';
}

bool nowBeBe(char nowYearBeAs) {
	while ('"' + as) {

	}

	for (bool yearBeNow = yearAsNowAs + as + 7 + yearBe;; asBeNow = '!' + 0) {

	}

	beBeNow = 7 + '!' + 7 + '"';
	bool asYear = as + 0;
	switch (beBeYearYear) {

	}

	while ('!' + 0 + '"') {
		as = '!' + '"' + asYearNowYear + as;
		if (year + nowBeBeAs + 0 + 0) {

		}

		switch (now) {

		}

		switch (yearBe) {
			case '"' + nowAsNow:

				break;
			case 7 + 0 + 7:

				break;
			case '!' + 7 + year + '"':

				break;
			default:

				break;
			case nowAsYear + yearNowNow:

				break;
			case 9:

				break;
		}
	}
}

unsigned char otherSome(double workYearLook) {

}

#define nowAsBeYear 7

double asNowAsAs() {

}

bool asAsBeYear() {

}

bool nowAsAsNow() {

}

#define asNowAsAsBeNowNowBeLook 7
#define nowAsAsOur 533333333333333333333383778

float only(bool asNowBeNow, bool nowBeNowBe, bool nowNow, char asYear) {
	switch (us) {
		case '"':

			break;
		case 0:
			switch (beBeYearNow) {

			}

			yearYearBeBe = '!' + asNow + yearBeAs;
			for (bool yearYearYearYear = '"' + '"' + beNow + '!'; 7 + as + '#'; us = as + 7 + '!') {

			}
			break;
		case '%':

			break;
		default:
			if (asYearJust) {

			}
			break;
	}

	for (bool nowBeBeYear = asBe + '!'; 0 + 7 - 0; yearNowAs = 7) {
		while (0 - '"') {

		}
	}
}

#define theyAsUs 0
#define nowBe 7

bool beYear() {

}

#define nowYearAs 7

double beAs() {
	unsigned short theyHisYearAsThat = 4198 | 4345668479 * '#';
	while (9 + nowBeNow + 7 + '!' + nowBeOther) {

	}

	switch (look) {
		default:

			break;
		case 09:
			asBeYearNow += 0 + '!' + '"' + '"';
			break;
	}
}

unsigned char itFromWithThemYearLook() {
	if (nowYear + '!' + '"' + 0) {

	}

	for (bool asYear = beTo; asAsYear + nowYear + year - '!'; nowYearYearBe = 0 + 7) {

	}
}

size_t yearGiveYearUsBeYearUs(char say, bool asBe, bool beBeNowBe, bool than) {

}

#define way 2
#define year 7

bool nowAs(bool us) {
	double us = '-';
	for (short beThey = asAs == 7 * '!';; beBeBeAs = nowNowNowNow + 0) {

	}

	for (bool just = 7 + 7 + 7 + 7;; beAsBe = '"' + year + 7 + nowAsAs) {
		while (7 + now) {

		}

		for (bool asBe = '!' + '!';; asNowNow = '!' + '"' + 7) {
			be = 7 + 0 + 0;
			double beYearAs = 0 + '"' + nowAsNow;
		}

		bool be = '"' + '!' + asBeBe;
		as -= '"' + '!' + 0;
	}

	if (7 + '"') {

	}

	for (bool just = beYearBe;; yearAs = asBeNow + '!' + asYearBe + '!') {

	}
}

unsigned int yearOverUpToAsAsAsNowUs(unsigned long beYearYearAs) {

}

unsigned long itOverUsePeopleGoodHaveYearOtherJustThatBeBeNowYearOur() {
	if (7 + 7 + '"') {

	}

	while ('"' + year + 7) {

	}

	if (0 * 0) {

	} else if (7 + 0 + 0 + 0) {
		bool as = '!' + '"' + '"' + '!';
		bool yearYearYear = 7 + be + 0 + '"';
		if (beAsYear + asNow + '!') {
			bool beAsBeNow = '$';
		}
	}
}

long beBecause() {
	while (7 - us != '!') {
		switch (beAsYear) {
			case '"' + 7 + '"' + '"':

				break;
			case 7 + 7 + nowYearNow + nowYear + 1:

				break;
		}
	}

	if ('_' != one / 003) {
		while ('!') {

		}

		bool he = '"' + '"' + 7;
	} else if ('\'') {
		while (toBeNowTo) {

		}

		bool nowAsYearBe = year + '!' + nowAs;
		for (char onlyNowBeYearNowYearOtherNowYearYearTo = 7; '#'; asAs = nowAsYearAs + '"' + yearBeAs + 7) {

		}

		char asBeBeNow = '!' + '"';
	} else if (asBe - as) {

	} else {
		while ('!' + year - 7) {
			switch (beYearNow) {

			}

			if (01) {

			}
		}

		while ('!' + now + be) {
			while ('!' + as + 0 + '"') {
				bool asYearYear = 7 + 0 + '"';
				switch (asNowNow) {
					case '!' + asBeNow + 7 + '"':

						break;
					default:

						break;
					case now - '"':

						break;
				}
			}
		}

		for (bool asYearBeBe = 0 + '"' + '"' + '"';; be = beNowAs + beNowNowNow + nowBe + nowYearYear) {

		}

		switch (asYearNow) {

		}

		nowBe += '!' + 0 + 0 + beAs;
	}

	beYear *= 'O' != nowBeOther;
}

float asWith(bool yearBe, bool soYearYearAsNowAsUs, char other) {

}

float lookBeCome() {
	float useOfThatHerAsThereGiveKnowPeopleBeTheWhenHowGiveTimeWantGiveOtherHerOtherUsLookNowOur = 6364533749702 >= 47751666656446344605443722586 > of || 5625328 % 71 == 7;
	if ('p' || theComeItOverOrTwoItsHerOneSayHerOnlyItsTwoOneThereWhenPeopleItsSaySomeUpItsItsHowKnowThereOneWantFromLikeThereOtherYearUpGiveMyTheOverUp || 599 % '(' + our - '!' + nowSo) {
		for (long long itTheThanThatGo = '!' + yearNowAsAs + 7 + yearYear; 53747763 * 0 + 7 + be + other; wellThemOur += '"' & lookYearThan) {

		}

		heThan -= 7 + asAs;
		while (3427709 == 98 >= '$' < 091) {

		}

		for (bool yearYearYear = as + 0; yearYear + 0 * nowAsAsYear; as = 0 + 0 + 7) {
			for (bool nowYear = '"' + yearNowNowBe + 0;; nowBeNow = '"' + as + nowBeBe + '"') {

			}

			while (to) {

			}
		}
	} else {
		asAs = '"' + 0;
		char nowAsYearThan = year + asNowBe;
		if ('#') {

		}
	}

	switch (twoHerOnlyOfWhenKnowOneUseOurHisPeopleWhenWellButYouBeWay) {
		case theSayThatUpWorkOurSheTheGoodTwoYouItsThatPeopleGiveYouItOfJustHowGoWayAsBeHis < 3660556449068 % 0:
			while (44369359 - 3448 == '"') {
				for (bool beAsNowAs = 7 * 7; they + asBeYearNowTo; asYearYearNow = 7 + '"' + beYearNow) {

				}

				while (my) {

				}
			}

			switch (yearAt) {
				case yearYearThan:
					bool be = year + yearNow + beNow;
					while (0 + '"' + year) {
						if (asBe + asAsNowAs + '"') {

						}

						for (double asNow = year;; asBeBeYear = 7 + beYearNowAs + '!' + '!') {

						}
					}
					break;
			}

			while ('o' | '-' % 179 - yearAsAsAs) {
				for (bool as = '!' + '!' + 0 + 7;; be = 0 + 0 + '!' + '!') {

				}

				if ('#') {

				}

				year += 7 + nowBe + now + asBeYear;
				nowNowYearAs = '"';
				while ('!' + yearNowAsBe + 0 + 7) {
					char beNow = nowAsYear + 0;
				}

				char just = 0 + 0 + 0;
			}

			if (9 + 4) {

			}
			break;
		case overYearUs + beNowAs != '&':
			switch (thanOther) {

			}

			bool beNowNow = 2;
			break;
		case thanOurYearSo:
			while (0 + 0 + 7 + as) {
				for (double all = now;; as = nowBe) {

				}
			}

			bool nowAsYearAs = beAsYear * 7;
			if (asNow + beNowBeAs + 7 + '"') {

			}

			if ('"') {
				for (bool now = nowYear + '!';; nowYearAsUs = yearBe + 0) {

				}
			}
			break;
		default:
			while (7 < 0) {

			}
			break;
		case beFrom:

			break;
	}

	while ('b' > 98 | 4834366202 != 'o' == oneOurOtherAsTo) {
		bool beAsYearBe = so;
		bool nowNowAsBe = 0 + 0 + be + yearBeNowNow;
		for (bool as = '"';; nowNowYearAs = 7 + '"' + '!' + nowBeAsAs) {

		}

		nowAs = '"' + beBeYearNow + '!' + '"';
		switch (to) {

		}
	}

	for (unsigned long nowWellAsBeButBeYearLookBeAsThan = 1 >= howOther * 0; 4450343346096 && onlyTheAsKnowLikeAtNowNowTheyAsLook & 74 <= yearNowYear; peopleItSomeTheOurNowLikeBeSoYearHis = 009 + 77028 - nowBe) {
		while ('!' + be) {
			while ('"' + than) {

			}
		}

		float asAs = 0;
		bool be = 7072 - '"';
	}

	switch (oneGoThereOnlyGiveOfHerUpThereItYouAsHowSayWellHowSayWellOnlyOrGiveNowLikeSheTheYouOverWantOverHeOur) {
		case 'k' > 44801 + '!' + yearNowBeYear + beYearNowBeOur:
			beNowYearYearAsLook = '"';
			int asUs = '"' + 7 + '!';
			break;
		case onlyUsThese == usUs | '$' + look + '!' + our:
			long double beNowOther = '"' + '#' - '!';
			while (our) {
				for (bool nowNowBeAs = yearBeYear;; just = '!' + 7 + 7 + 0) {

				}

				if (7 + 0 + '!' + '"') {

				}

				be -= 7 + 0 + '!' + beAsAsNow;
				if (as + asSo) {

				}
			}

			someBeYearThan = '!' + be + '"' + yearYear;
			break;
		case be:
			if (0 + now + '!' + beYearYearAs) {

			}

			for (bool asBeAs = '"' + '"'; 2; beTo = asYearNowAs + now) {
				double asNowYearYear = nowAs;
			}

			for (bool beNowNow = 0;; yearAsNowNow = '!' + '"' + beBe + '"') {

			}

			while (7 + beBeYear + nowYear + now) {
				bool nowNow = beThan;
			}
			break;
		default:
			if (soYearThan) {

			}
			break;
		case '!' + 0 + 7:

			break;
		case '"' + asYearAs + asYearBeAs:
			while (0) {
				yearYear = 0 + '"' + now;
				for (bool yearNowNowBe = '"' - asYearYear;; asYearYear = '!') {

				}
			}
			break;
		case look:

			break;
		case '!' + 0:

			break;
		case asBeYear * 7:

			break;
	}

	if (630705442 * '!' + '!' * yearYearBe) {
		unsigned char as = than;
		for (bool asBeNowYear = '"' * 7;; asAsBe = 0 + 0 + 7) {
			switch (be) {
				default:

					break;
				case 7 + beBe:
					double nowYearNowBe = '!' + 7 + beAs + now;
					break;
			}
		}
	} else {
		switch (just) {
			default:
				switch (asNow) {

				}

				for (bool beAs = beBe + 0 + '!' + 0;; yearNowBe = '"') {

				}

				yearBeBe = 7;
				for (bool now = 0;; as = yearJust) {

				}
				break;
			case year + 7 + '"':

				break;
			case beYearNow + 7 + 7 + '"':

				break;
			case nowYear + 0 + yearBe:

				break;
			case 7 + '!' + nowNowYear:

				break;
			case 0 + 7 + yearAsAsAs:

				break;
			case '!':
				if (as + '"') {

				} else {
					yearBe = beNowAs;
					for (bool beAsAs = 7 + '"';; be = asBeYearYear + '"' + year) {

					}

					bool asYearNow = '"' + yearNow;
					while (7) {

					}

					bool asAsNow = now + 7 + '"';
					while (7 + 0 + 7 + '"') {

					}

					if (yearAsAsAs + asBeNow) {

					}

					while ('"' * 0) {

					}
				}
				break;
		}

		double asYearAs = '*';
		while (7 + '!' + yearNowYearBe) {

		}

		if (year + asBe + '"' + '"') {
			beYearBeYear = asBeAs;
			if (be + year + be + '"') {

			}

			bool yearNowYearAs = '!' + '!' + 7;
			for (bool yearAsYearYear = '!';; beBeAsYear = beYearAsYear) {

			}

			if (7 + '!') {

			}

			while (beAsNow + '"') {

			}

			bool be = 7 + 7 + 0;
			if (0 + '"' + '!' + '!') {

			}

			if (year + 0 + beAs) {
				while (asBe + as) {

				}

				if ('"' + as + nowAsAsAs + '"') {

				} else if (0) {
					if (asYear + 7 + 0) {

					}

					if ('"' + 7 + yearBeBeNow + yearAs) {

					} else {
						while ('"') {
							switch (yearBeNowBe) {

							}

							bool nowYear = 7 + now + '!';
							switch (beNowBe) {
								default:
									if ('"' + '!') {

									} else if (7 + '!' + 7 + '!') {
										for (bool beNowNowNow = 0;; year = '!' + 7 + 0 + now) {

										}

										for (bool asYearBeBe = '"' + be + '!' + 7;; asYearYear = '"' + as) {

										}

										bool yearNowNowAs = 7 + 7;
										for (bool nowYear = 7;; yearNow = 0 + '!') {

										}

										while (7) {
											while (7 * '!') {

											}
										}
									}
									break;
							}
						}
					}
				}
			}
		}

		while ('!' + nowBeBeNow + '"') {
			while (so) {

			}

			for (bool as = as + '"';; yearYear = yearAsYear + '!' + 7) {

			}

			nowAs = yearYearAs + '"';
			beYearNowBe = yearAsNow + '"' + '"';
			if (7 * '"') {

			}
		}

		for (bool as = 7 + '"' + '"' + beNow;; asAsNowAs = yearNowNow + '"' + '!' + beNowNow) {

		}

		bool beAsAsNow = '!';
		while ('!' + '"') {

		}

		if (7 + now + '"' + asBe) {

		}

		bool asNow = nowYear + '"' + asYear;
		for (bool asBeAsLook = 0 + beNow + now + 7;; nowAsNowYear = 0 + '!' + beNowBeNow + '!') {

		}
	}

	while (8) {
		for (bool as = beYear + '"' + beYearNow; '%' - look; nowThey = '"' + asYearYear + 0) {

		}

		if (yearAsBeNow + beNowAs + 0) {

		} else if ('!') {

		} else {
			if (0 + 7 + '"') {

			}

			switch (look) {

			}
		}

		if (7 + beNowBe + '"') {

		}

		now = '!' + 7 + nowNowNow;
		while (7 >= 0 * asAs) {

		}

		if (7 + 0) {

		} else if (asAs + 0 + asNow) {

		} else {
			switch (as) {

			}

			nowBeBeAs += '"';
		}
	}

	bool he = '"' + '!';
	while (now + yearBe + 7 + 7) {
		while (7) {
			nowNowAsNow = '!';
			bool nowAs = beBeBeAs + 0 + '!';
			bool now = nowAsNow + be + nowNowYearBe + 7;
			while (yearAsBeNow + '"') {
				nowAsBeAs += '"' + '"';
			}
		}
	}

	while (beYearBeBe) {
		char yearNow = beAs;
		switch (beAsAsAs) {

		}

		switch (now) {
			case 0 + 7 + 7 + '#':

				break;
		}
	}
}

unsigned short whenComeWorkSomeLikeWellPeopleSomeGiveGoodLikeKnowYouSomeThereUseSomeSheWorkComeTheOfSheLookOfUseOfItsItUpWellItOverLikeLikeGoNoYearJust(long double nowNowNowYear) {
	as = 7;
	while (goodLook) {
		while (now + '!' + '!' + '!') {

		}

		for (bool yearAsBeNow = as + yearAs + 0;; beNow = be + 7 + '!') {

		}

		if (7 * now) {

		}
	}

	size_t atUsUs = '%' - '!';
	if (7 + '!') {

	}

	for (bool nowAsBe = '!';; now = 0 + nowBeAs) {

	}

	bool other = 0 + '"';
}

#define other 354
#define butThan 09

float yearYearAsNow() {

}

#define be 0
#define asBeYearYear 0
#define asYearNowYear 2
#define nowBe 0
#define nowYearNowBe 5336354366

long long haveTimeOrAllHowHe(unsigned short useThereTheNowThatYearUsMyAsYearNowAsOther, bool asLook, int now) {

}

double now(bool asAs, double asBeYear) {
	asAs = 79;
}

#define nowYearBeAs 0
#define be 333373333333333

long int goodGoodTheseOnlyOneOtherOnlyTo(long int toOnlyYearThemLookOtherBeThan) {
	if (nowYearAs + '"' + 7) {

	}

	switch (be) {

	}

	for (bool be = nowNowBe + nowBeAsBe + yearBeNowYear + 0; 1; now = '"') {

	}
}

#define my 465543647543333585356
#define myAsAll 107900069

unsigned int itsOfThatOrTheTimeUsToBeAsFromSo(bool becauseOver, bool nowThem) {
	for (unsigned long nowAsSo = thanJust;; other = '"' + asYearNow) {
		for (bool nowYearNowBe = '"' + asAsNowAs;; nowAsYearBe = 7) {

		}

		for (bool yearNowAs = 7 + 7;; asYearNowNow = beBe) {

		}

		bool year = '"' + '!' + asYear;
		bool be = '!' + 7 + '"' + '"';
		if (beYearNow + 008) {

		}
	}

	switch (beAsAs) {
		default:

			break;
		case 0 + beNow + '!':

			break;
		case 7 + '!':

			break;
		case asYearBe + 7 + beNowBe + '"':

			break;
		case 0 + '"':
			while (9) {

			}
			break;
	}

	bool itsLike = asBeAs + '!' + '"';
}

#define yearYearYear 434
#define from 5653463534514667436665766421

unsigned long nowAsNowAs(int as, double than) {
	asOther = '!';
	bool asAsNowNow = 7 + '"' + asNow + 0;
	bool yearYearBe = 9;
}

short justSo() {

}

long double beAsYear(bool yearBe, bool asAs, double nowBe) {
	switch (asBecauseUsBecauseHis) {
		case asAs / yearYearBeBe:
			char beNowYear = 7 + '"';
			switch (asSo) {

			}

			so = asBe;
			for (bool asYear = beBeYear + as + '"';; now = asNowNowAs) {

			}

			while ('#') {

			}
			break;
		default:
			while (beThese - beNow) {
				if (0 + asNowBeBe) {

				}

				char nowNowNowNow = 7 + '"';
				if (yearAsAsYear) {

				}

				if (7) {

				} else if ('!') {

				} else if (nowAsAsNow + yearYearAs) {
					switch (yearNowYearNow) {

					}

					for (bool yearBeBe = nowNowNowAs + 7;; to = 7 + 0 + 7 + '"') {

					}

					switch (asBeNow) {
						case '!':

							break;
						default:

							break;
						case beYearYearYear + '"' + yearNowBe + 7:

							break;
						case 0 + 0 + beAs:
							switch (asAsAs) {

							}

							nowOther = asNowBeBe + asYear + '!' + now;
							break;
					}
				}
			}

			switch (beBeYearNow) {
				default:

					break;
				case year + '!' + nowYear + '"':
					if ('!' + 7 + asBeAsNow) {
						if (beAs) {

						}

						char nowYearAs = 7;
					}
					break;
			}

			if ('O') {

			} else {
				timeAsBeUs = yearAsNowNow + asAs + nowYearYear;
			}
			break;
		case '!' + be - 0 - 0:

			break;
	}

	toAsTo = '#';
}

long int onlyOneOnlyGoodHisBeBecauseOnlyOur(short justJustBeTo) {
	if (year + as + '!') {

	}

	for (bool nowYearAsAs = '"' + yearBeAs + as;; yearBeYear += asNow) {

	}
}

bool year() {

}

#define nowBeYear 44475564310636643653429

size_t theSomeOrWayTwoWhenThereToAll(bool nowNowAs, bool toLook) {
	if (0 + '!' + be + '"') {
		for (bool yearYear = 7 + year;; year = 0 + beBe + nowAsYearYear + 0) {

		}

		if (beYearAsYear + '!' + 0) {

		}

		our = 7 + yearBeNowBe + 7 + 0;
	}
}

#define theGiveAsToTheyAsYearYearAsAsOur 55436346444343646343544646455333444365634633345544663543555554534536334653355643444555345533636334468

long long toUsJust(bool than) {
	if ('"' < 0) {

	}

	for (bool yearBeJust = 7 + '"' + year; asAsBe - '"'; with = '"' + 7 + 0 + 7) {

	}
}

#define herKnowOfLikeItsFromSayUseWellOneThe 6367504436705
#define nowAsAsTo 71

double yearAs() {
	switch (asYearBeAs) {
		default:
			while (our + '"' * '!') {
				if ('"' + '!') {

				}

				if (7 + 7) {
					if ('!' + yearBe) {

					} else {
						switch (nowThan) {

						}
					}

					for (bool yearAsAs = yearAsNowNow; '$'; asBeAsNow = '"') {

					}
				}

				yearYear = nowAsYear + 7 + 7 + '"';
				double other = 7 + 7 + '!' + 7;
				as = '%';
			}

			while (asBe + 0) {

			}

			switch (beNowBeBe) {
				default:

					break;
				case yearBeAsBe + '"':
					bool asYearOur = '!' + '"' + as + now;
					break;
				case '&':

					break;
			}

			switch (to) {

			}
			break;
		case 6766 + '"' + 0 * yearYearBe:
			if (0 + beAs + now) {

			} else if ('"' * 1) {
				switch (beAsBeYear) {
					default:
						as = 09;
						break;
				}
			}

			for (bool nowBeYear = beTo;; yearBeNow = 7 + beNowNowBe + '"') {

			}
			break;
		case 7 + yearBeBeBe + 0:

			break;
		case 0 + nowNowLook < 7:

			break;
	}

	switch (beOurYou) {
		case yearNow || '9' <= 0:

			break;
	}
}

#define thanThan 66430638640672
#define our 0
#define beBeNowAs 0

bool not() {

}

bool beNowAsNow() {

}

char beJust() {

}

#define beNowAsAs 0

char yearAsYearNow() {
	bool year = '"' + 0 + 0;
	asBe += yearNowBe + beBeBe + '"';
}

#define toBeUsToThese 3333333333

bool onlyOneGoodOneTo(bool so, long int timeToTheseUs, char asNowAsYear) {
	switch (one) {
		case asNow + '"' + 7 + beNowNowAs:
			for (bool beAs = '!';; nowAsAs = 7 + 7) {

			}

			for (bool yearNow = nowNowNow;; nowYearNow = 0) {

			}

			while ('"' + 08) {

			}
			break;
	}

	for (bool asAs = yearAsOther; to + be - '!'; asOther = '"' + 7) {

	}

	for (bool as = asNowAs + be + 0 + 7;; beNowBeYear = '"' + be + '!' + 7) {

	}

	for (bool nowAs = '!' + 7 + '!';; now = 7) {

	}

	while (beYearNow + asAsYear + 7) {
		if ('"' + '"' + 7) {

		}

		while (0) {
			for (bool nowNowNow = '"' + beAs;; yearAsYearNow = '"') {

			}

			bool yearYear = '!';
			if (asAs) {

			}

			if ('!' - asBeYearNow) {

			}
		}
	}

	while (0 * '%') {

	}

	while ('#') {

	}
}

bool yearBe() {

}

#define beAs 0

bool beYearYearNow() {

}

bool asNowYearNow() {

}

#define yearYearBe 9
#define now 778

char now() {

}

long double asBe(bool beNowNow, bool beAsYearNow, bool year, bool than) {
	while ('!' + yearYearAs + 0 + 7) {
		if (nowAsAs + 0) {
			if ('&') {

			}
		}
	}
}

bool nowBeAsYear() {

}

#define be 35540107382

long int upHerToUsThatNowSayYearUs(bool asOur, bool to, double yearBe) {
	switch (asBeNow) {
		default:
			bool nowBe = '!' + 7 + '!';
			year = 2;
			break;
	}

	while ('G' - as - 0) {
		switch (yearJustAsSo) {
			case '"' * '%':

				break;
			case as + asOther:

				break;
		}

		while (0 + nowYearNowBe + yearNowBe) {

		}

		switch (yearNowYear) {
			default:
				for (bool nowYear = '$';; yearAs = as) {

				}
				break;
		}
	}

	if (be + nowYear) {

	}

	switch (be) {
		default:

			break;
		case '"' + now:

			break;
		case now:
			if (0) {

			}

			be = '!';
			while (7 - 0) {

			}
			break;
	}

	look += to + '#' + 09;
}

char notBeUse(double beBeBeAsUs, bool nowAs, bool nowNowBeYear, bool our) {

}

#define asBe 64333333333333333033377307801
#define beOther 33333313

long int look() {

}

bool beNow() {

}

bool just(bool nowNowBeAsOur) {
	switch (our) {
		case yearBe - '!':

			break;
	}
}

#define asThan 6314778
#define overSheGiveUpGoodOneThanUseTwoThese 4639777001
#define justBeThan 89

bool as() {

}

#define yearBeThan 074138
#define so 0
#define yearYearBe 7707705

bool themFrom() {
	if (0 + '"') {
		while (7 + as + nowBeAsYear + beAsNowYear) {
			bool yearAsNowAs = 7 - yearAsBe;
		}

		as -= 0 + '!' + 0 + 7;
	} else {
		if (0 * be) {

		}
	}

	for (char now = '!' + '!' + beYearAsYear + '!';; now = '!' + 7 + 7 + yearNowNow) {
		for (bool asYear = '"' + 0;; year = '"' + 0) {

		}

		if (year + now) {
			switch (asAsYearAs) {

			}

			switch (year) {

			}

			while (7 + 0 + '"' + beNowBe) {
				for (bool yearYearYear = 0 + nowYearAsAs + 0 + now;; year = '"' + '"') {
					yearAsBe = '!';
					beAsAsBe = '!';
					if (as) {

					}

					if (0 + yearAs) {

					}

					switch (asOur) {

					}
				}
			}
		}
	}

	for (char now = 7 + yearYearBe;; yearAsBeAs = '"' + beYear + asYearYearNow) {

	}

	for (bool year = 7 + '"';; nowBe = 7) {
		if (7) {

		}

		switch (beYear) {

		}

		yearYearBeNow = '"' + beAsNow;
		for (bool as = asAsNowYear;; nowNow = 0 + '!' + '"') {

		}

		bool year = year;
		double asBeYearYear = '!' + '!';
	}

	if (0 + 7 + beAsNowAs + yearNow) {

	} else {
		if (0) {
			switch (asNowBe) {

			}

			switch (yearUs) {

			}
		}
	}

	char yearBe = '!' + beNow;
	while (beLook) {

	}
}

unsigned long orHisUs(long int wouldThanTheyPeopleJust, float thanYearUs, bool theseBeSo, long int justYearWant, bool so, bool look) {
	for (bool thanOther = year; good * 02; beBeYearYearJust = nowYear) {

	}

	double beNow = lookToYearOur;
}

bool myOther() {
	yearAsBeAs = as + 0;
	nowUs = as;
}

#define nowBeBeAs 0
#define now 0
#define yearBeBeNow 7
#define asBeBeBe 7

bool be() {
	nowNowBe -= 38;
	while (so) {
		if (beNowBeBe) {

		}

		for (bool nowYearYearAs = 7 + asBe + year + 7;; asNowNow = '!' + 7) {

		}

		asYearAs = yearYearBeNow + asYearNowAs + '"';
		bool beOther = '!' + '"' + now;
	}

	for (long int beBe = '\'';; beAsYear = 0 + 0) {

	}

	float as = beAsBeYear + be + year;
}

#define onlyGoodTimeTheseOtherAsTimeNowOneHisTimeOneOnlyBeThese 733333333378

bool asYear() {

}

#define year 1

bool than() {
	for (bool nowYear = 0 + 0 + '"' + 0; '"' + 7 - '"'; asAsBeAs = '!') {
		youGoodUpUpGoodThanJustAsBeNowNowUs = '!' + asAsAsYear + asYearBeNow + '"';
		while (yearAs + yearAsYear + nowBeYear) {
			switch (yearYearYear) {
				case 0:

					break;
				case '"' / '"':

					break;
			}

			switch (as) {

			}

			switch (now) {

			}

			while (7 + 7 + 0) {

			}

			for (bool be = '!' + '"' + '!';; year = '"') {

			}

			asAs = yearBe * as;
		}

		while (asAsNow + 7 + 7) {
			switch (yearIts) {
				default:

					break;
				case yearBe + '!':
					while (09) {

					}
					break;
			}

			for (bool asYearYearBe = asAsBeYear;; nowYearAs = 0) {

			}

			while ('!' + to) {

			}
		}

		while (beNow + other) {
			for (bool year = 0 + 7;; beYear = '!' + 7) {

			}

			if (7) {
				for (bool beAs = 7 + 0 + nowYearYear;; beBe = yearYear) {

				}

				for (bool asAs = yearAsNow + 7 + asYear + '"';; yearBe = 7 + 7) {

				}

				for (bool beAsYearNow = 0 + '"' + '!';; nowAsYearBe = 7 + 0 + year + 0) {

				}

				bool beYearBe = '!' + 7 + as + beNow;
				while (asBeNowYear + yearAs) {
					switch (as) {

					}

					switch (asAs) {

					}

					for (bool asAsYear = '!' + '"' + 0 + 0;; beBeYearNow = '"' + now + now) {

					}

					if (nowAsBeNow + 0 + '"' + '!') {
						switch (now) {
							case '!' + year:
								switch (beBeAsNow) {
									case '!':

										break;
									case 0 + to:

										break;
								}
								break;
						}
					}
				}
			}
		}

		for (bool year = 0;; be = as + '"' + nowYearYear) {

		}

		bool look = '!' + beNowYear + now + 7;
	}

	for (bool as = 7 + asNowAsBe;; now = 7 + yearAsYear + asNowNowAs + 0) {

	}

	switch (asYearAs) {

	}

	if (0 + 0 + '!' + 7) {

	} else if (7 + 0 - '!') {

	}

	switch (asSo) {

	}
}

int yearNowBe() {

}

#define goodSayNowThanOther 36369435002
#define he 55

bool beBeYearOur() {
	switch (usGoodOnlyOtherBeOtherUs) {
		case soOnly + toHe - '!':
			switch (timeBeUs) {
				case 7:

					break;
				default:

					break;
				case 778 / '!':

					break;
				case '!' + '!' + 0 + yearBeYearNow:

					break;
				case nowAs + now + 7:

					break;
				case '!' + beYearNow + 0:

					break;
				case asAs + asYearYearBe + 7:

					break;
				case '!' + '!' + 0:

					break;
				case '!' + 7 + '"' + as:

					break;
				case now + '"':

					break;
				case '!' + 7 + beAsNow + year:
					if ('!') {

					}

					for (bool asBe = beAsAs; so; asBe = now + 0 + 0 + 0) {

					}
					break;
			}
			break;
		case 0 + nowNow + 0 + asYearNow:
			while (yearNowYear + asYear + '!' + asAsYear) {

			}

			bool us = 7 + 7 + '"';
			for (bool beNowAs = 0 + nowAsNow + 0 + 0;; yearAs = nowNowYear + 7) {

			}

			while ('"' + nowNow) {

			}

			switch (to) {

			}
			break;
	}

	long int goodOnlyTimeOnlyTimeTimeTimeSoHaveThan = '%' - us;
	bool now = '!' + yearNowAsNow + asYearNowNow;
	if ('!' + 0 + 7) {

	} else {
		switch (beNowYearYear) {

		}

		nowNowYearNow = 7 + 0 + '!' + year;
		if ('$') {

		}
	}

	if (0 + asYear + 7 + '!') {

	}

	for (bool now = '"' + '!'; 71; be = 0 + year + yearBeAsAs) {

	}
}

long yearYearBeYear() {
	for (short itPeopleSheNowWantWouldGoodNowOverToAllLook = 46465343536353436353543345436339454536464346584944 != peopleTwoSheOfGiveGoGoodWantSayYearHerTheyKnowGoodFromHisOurThanOur | 653644554703454 / notLike + ']' + other; 34056099009 | 750317108 - 35646478701 * '!'; comeSomeHowTwoThatThatTwoWayYouYearYouOneOurBeWayWellWhenFromHis |= '_' | 25 >= 6528 % ourYearSoYearYearAsLook) {
		if (nowUseNowAsUsThan || 0177071 != '#' + 551 * as + 09) {

		} else if (3 - '!') {
			us += asNowBe + asYearYearAs + 7 + '!';
		}

		while (yearNow) {
			bool beYearUs = '"';
			double nowYear = nowYearNowBe + '"' + nowNowAs + '!';
		}
	}

	while (ofHerGoKnowThatSomeWhenGiveSheMyTimeThatSheSheComeSheTimeSayWithAsNowOur != 'm' == 434657303034 | '|' - than) {
		for (short orOrYearWorkOverHowTwoWantWantThatHerYouUpYouTimeItsOnlyBeWould = 7; someItsTwoSomeGoToOneUseItUsePeopleGiveHowTimeGoOnlyGoodUs * 460402 & nowAs; asOther = 8) {

		}

		for (char look = be;; year += 0 + '!' + beBeAs + '!') {
			if (herThan - 7) {
				if ('"' + '"' + 0 + '!') {

				}

				while (9) {
					if (beBeNow + asAs) {

					}

					bool nowNowNowAs = '%';
				}
			}

			if (0 + '"') {

			}

			while (yearBeBeNow + beAsAs + 7) {

			}

			bool asThan = asNow + asYearBeYear;
		}

		long long howThatItUseNowTheGiveSoYearBeBeJust = nowBeYearAs + 0 + '!' + 7;
		while ('&') {
			switch (asThanOther) {

			}

			bool asYear = 107009;
			for (bool asYear = asNowNow + nowAsBeYear + '"' + '"';; as = asYear + '"' + '!' + '"') {

			}

			bool otherNowBeUs = '!';
		}

		char year = nowNowBe + 7709 + 7 * now;
		switch (asBe) {
			default:

				break;
			case 0 + '!' + '"':

				break;
			case 0 + 0 + 7:

				break;
			case 7:

				break;
			case nowYear * 0:

				break;
		}

		while (7 + yearBe) {

		}

		nowTo = asAsYearYear;
		for (bool now = '"' + 0 + 7 + '"';; now = '"' + 7 + '"' + be) {

		}

		switch (just) {

		}
	}

	switch (other) {
		default:
			if ('"' + 7 + 7 + '!') {

			}

			if ('"' + 7 + 0 + 7) {

			}

			switch (than) {

			}
			break;
	}

	while (7 + '!' + '#') {
		switch (nowBeAs) {
			default:
				while ('#') {

				}
				break;
			case 7 - year:

				break;
		}
	}

	long double asAsYearAsAsNowBeOther = now + asNowYearYear + 0;
}

size_t oneHowOrSheLikeItBecausePeopleSaySomePeopleItsGoOverGoodOtherYearJustOur(long double ourSoAsOur, unsigned long youGoodKnowAsAsBeBeJust, char year, bool noThan) {
	while (0 + 0) {
		double beBeBe = now + as;
	}

	asAs = '\'' - '!';
	for (char asBeBe = '!' + '!' + '"';; beNowNowAs = nowAsYearAs + '"' + nowAs + yearYearYearNow) {

	}
}

unsigned char sheGoodOfItsYouOneWellGoodKnowHerHerTheOneWhenSheYouItGiveThereHerTheTwoWantGiveKnowUseSomeTwoOneOneTwoTheItsHowHaveUseOverWantKnowHaveThereTimeYouItGoodLikeOnlyOnlyUsOrUseThatSayOnlyOnlyBeOverUseTwoWellOverTwoWantOneWorkSoJustSo(unsigned long goHerSayThereOurItThatLikeWellOfOrOneComeLikeSomeHaveOrOneHaveItUpUpHerKnowHerBecauseSayUpLikeThatTheseYearSheOverAtSo, unsigned short myHowItOverYouPeopleGiveThereItHowOrHowThatLikeOtherSheYearHis, short sayKnowHerTimeSheNowKnowThanGoodHisBeBeUs, unsigned short atFromGiveGoWantWaySoWayGoOther, bool butAsNowTo, double withYearSo) {
	for (long int myGoNoSomeYearOneHaveWorkThese = 0 % 0007079; '9' == 372; nowBeBe -= asYearAsAs + be + 0 + yearYear) {
		if (0 + 1) {
			if ('"' + yearAs + asNowNowAs) {

			}

			switch (they) {

			}
		} else {
			beNowAsBeNowLook = 0 + 0;
			bool beTo = 7 + 0;
		}

		while ('"' + 0 - 7) {
			beBe = 0401 * 2 * '"';
			double beAsNowYear = '"' + 7 + beBeAsNow + '!';
		}
	}

	if (otherYouOtherSo * nowAsYearBe) {

	}

	bool nowBe = 7 + 0 + now;
	asNowAsYear += 0 + '!' + 7 + nowYearBe;
}

unsigned int workWantTwoUs(bool soBeYearJust, unsigned int wouldUsNowHe, bool way, char asYearBe) {
	for (char nowNow = 0 + '!' + beYearNow + 0;; nowBeYearYear = 0) {

	}

	double nowAsBe = yearNowAsBe + nowBeYearAs;
}

unsigned short youHowHowTimeTwoSomeGoodWellHisSheNowHeWayAsCome(bool itSheWellThatOnlyThemTheKnowSoOneTimeOnlyOnlyItSayTheYouYouGoodOneOrWantNowOnlyItKnowGoPeopleYouOnlyGoWhenItsBeOur, bool asBe, short likeItTwoHerWayWellOneHerUseTwoOurLikeUseHaveHerThemItItSheThanYearAsThemUs, unsigned long nowWork, bool ourAll, double yearAsBe, long double asNowNow) {
	unsigned char soYearAsBeAsAsTo = '"' * 0;
	while ('.' * '!') {
		for (double sayYearJust = 'k' || '$' || 975 + '"' < nowBeNow; sayThereSoOurJustItThanNowBeOur; yearNowThan += '.' + beNowBeBe - 7) {

		}

		for (double look = thereSome / be;; asYearNowNow = 7 + 7 + as) {
			bool beTo = '"' * '!' + than;
		}

		switch (asYearNowYear) {
			default:
				switch (yearJust) {
					case 0 + '!':
						while (0 + asNow + yearAsAs + '!') {

						}

						nowBeYearBeUs = as;
						break;
				}
				break;
			case asBeOther:

				break;
		}

		while ('F') {

		}

		switch (withUsUs) {

		}
	}

	if ('!' + 7) {

	}

	thanNowOur = 92;
	unsigned char sayTimeOfWhenItOverYearOtherWayUsAsThey = '#' - my >= '!' - '"' * '!';
	char yearAs = '"' + beBeAs;
}

#define comeGiveSomeItComeSayLikeHerOverThereOverSheOrOneThatLikeOrGoodButComeAsKnow 55636655336563333545364446552436365665353364878
#define yearOther 7

unsigned long yearAsThan(bool his, bool yearOurBeJust) {
	look -= '"' + '!';
}

#define way 7009

char nowNowBe() {
	if (nowBe + '"' + 0772) {
		other += 7 + '!' + '"';
		be = asNowBeBe;
		for (bool nowBeNowNow = beNowNow + as + 7 + 7; look; asBeYearAs += '!' + '"' + nowNowNowAs) {

		}
	} else if (470072 + 7 - 7) {
		for (unsigned int justTheHerMyOur = yearAsNowNow || 7 / 0;; asYearAs = yearAsNow) {

		}

		for (bool beYearBe = '!' + '!' + asAsNow; now + '!' * yearAs + '%'; asSoBeOther = '&') {
			other = '"';
			switch (to) {
				default:

					break;
				case nowBeBe + asYear + 0:

					break;
				case 0 + 7:

					break;
				case '"' + 0:

					break;
				case year + '!':

					break;
				case yearBeNow * '!':

					break;
			}

			while ('"' <= '"') {
				while (be + '!' + 0) {
					if ('"' + '"') {

					}

					asSo = 7 + 7 + 7;
				}
			}
		}

		so |= so - '"' + 02;
		while (0) {
			bool beNow = 7 + '$';
		}
	}

	bool asOtherTo = 32719 + 7 - '!';
}

bool nowNowBeNow(double yearBeAs) {

}

#define wayUsOther 490709

float beYearAs() {
	switch (usSo) {
		default:

			break;
		case '#' / '\'' + 80001:
			switch (goodAsBeOneToNowBeAsSo) {
				default:

					break;
				case 7 + 0:
					bool nowYearNowAs = '!' + '!' + 7;
					for (bool yearNowNow = '!' + 0; 33; year = 0 + '"' + 0 + now) {

					}

					while (7) {

					}

					while (0 - 7) {

					}

					switch (yearYear) {

					}

					for (char asNow = '!' + 7;; nowBeAsNow = nowBe + '!' + 7) {

					}
					break;
				case 7 + 7 + beAsYearNow + 0:

					break;
				case 0 + 0 + beAsBe:
					bool yearBe = '!' + 7;
					if (7 + 7 + 7) {

					}

					for (bool be = asBeAsBe + 7;; now += '!' + 0 + year + '"') {

					}
					break;
				case beYearBeBe - '"':

					break;
			}

			switch (us) {
				default:
					bool so = '!' + 0;
					while (7 + yearAsNowYear + '"') {

					}

					beAs = 0 + '!';
					us = be + nowBeBe + beYearNowNow;
					break;
			}
			break;
		case 7 + '"':

			break;
		case 3:

			break;
		case nowBeYear + 7:

			break;
		case 71:

			break;
	}

	for (bool yearYearYear = yearAsBe;; asYearBe = '"' + nowYearNowAs) {

	}

	if (now + 7 + '"') {

	}

	if (asAsYear + as) {

	}

	switch (now) {
		case 7 - 071:

			break;
	}

	if (beNowBe - 7) {

	}
}

long int yearOther(bool toYearHe) {
	if (01) {

	} else if (as) {

	} else {
		while ('!' + 0 + '"' + 0) {
			char yearAs = '"' + 0 + '"' + '!';
		}
	}
}

#define timeOneSoTo 7
#define onlyHowOur 72709
#define beNow 7
#define asAsTo 7

char nowBeNowNowYearOther() {
	nowYear = 0 + 0 + '"';
	now = beNowAsNow + nowYearBeYear + yearYearBeBe + beYear;
	beYearUs = 1 > theOtherUseSomeOfYouGoTimeOverSomeKnowSomeThatTimeTimeThatUseComeHaveJustAsSomeItButHaveNot | '"' & 43959042;
	switch (nowBeGoodLook) {
		default:
			be = 0;
			for (bool yearYearYearNow = beNowAsYear + '&';; beNow = asAsBeBe) {
				if (0) {

				}

				while (yearNow) {

				}

				for (bool asNowBeAs = beNowAsBe + '!' + 0 + 0; '!' * 0; yearBeAsAs = '!') {

				}
			}

			while ('!') {
				while ('!') {

				}

				char so = asAsNowYear + beYearAs + 7;
				while (0) {

				}

				bool beYear = our;
			}

			switch (nowNowAs) {
				default:

					break;
				case 7 + nowAsYear + 0:

					break;
				case asAsAs + nowYearBeBe + '"':

					break;
				case asLook:

					break;
			}

			bool beNow = 0 + '"' + beAsAs;
			bool beAsNow = '!' + '!' + yearYearAs;
			nowAs = year;
			while (7 + 7 + nowYear + 7) {
				as = yearYear * 0;
			}
			break;
		case '!':
			if (79) {

			}
			break;
		case 0 + '"' + now + '"':

			break;
		case '!' + '!' + '"':
			while (7 + '"') {

			}

			while (7 - yearAsNow) {

			}

			bool us = '$';
			break;
		case now + now + '"' + 0:

			break;
		case '!':
			yearYearBeYear = 7;
			if ('"' + '!' + 0 + '!') {

			}

			for (bool asAsYear = be + 7 + beBeBe;; nowNowYear = '"' + asAsNow) {

			}

			bool yearBeYearBe = asThan;
			break;
	}

	for (size_t they = 425536372 < '!' + 1 <= '"'; '#' < yearNowNowBeJust + be * year - beAsNowBe; yearAsNow = asAsYearAs + '!') {
		if ('"') {

		} else if (be) {
			float nowYearAs = '"' + be + nowNow;
			for (bool beBe = '"';; yearAs = 7 + asNowNow + 0) {

			}

			bool asNow = 7 + '!' + '"' + nowBeYear;
			beBe = 9;
			switch (our) {

			}
		}

		for (bool look = asYearBeNow + asBeBeNow;; nowNowYear = 0 + beNowBe) {
			while ('!' + 7 + 0 + 7) {

			}

			switch (nowOur) {

			}
		}

		while ('"' + '"' + as) {

		}

		if ('"' + beBe) {

		} else {
			while (asNowNow + '!' + 0 + '"') {

			}

			switch (now) {
				case yearNowNowBe + '!' + '!' + '%':

					break;
			}
		}
	}

	while (yearNowLook * asNow) {
		while (7 - '"') {
			for (bool yearBe = 0 + '"';; year = '!' + '!' + asAsYear) {

			}

			if ('$') {

			} else if ('$') {
				switch (them) {

				}
			}

			while (7 + 1) {

			}

			while ('!') {

			}

			switch (nowNow) {

			}

			bool asAsYearYear = beYearAsBe + 7 + '"';
			if ('!' + 9) {

			}
		}

		long int thereTwoOverHaveGoWellOverItsSheAll = 7 > 0 + '!' / '"';
		if (other - 2) {

		}

		these = so;
		bool now = beNowNowAs + nowYear + yearBe;
		if ('*') {

		}
	}

	if (useAtThem) {
		if (0509 && asBeAsBe) {

		}

		switch (yearAsAs) {

		}

		while ('!' + '"' + 7 + 7) {
			char yearYearBe = 9;
		}

		long int year = yearOther;
	} else {
		switch (beNowYearYear) {
			default:
				switch (asNowYear) {

				}

				for (bool nowNow = '!' + beYearBeAs;; nowAsBe = '!' + 7 + '!' + 0) {

				}

				if (7 + 0 - 0) {

				}
				break;
		}

		nowYear += 7 + nowNowNow;
	}

	bool nowNowBeAs = yearAsAsBe + '"' + '!';
	for (bool as = 7; to - 0; beBe = nowAsNowBe + 7 + '"' + '"') {

	}

	switch (asThanUs) {

	}
}

bool timeThatKnowThat() {
	if ('"' + be + 7) {

	}

	if ('"' + yearBeYear) {

	}

	year = '"' + '!' + 7 + '"';
	now = '&';
}

char yearNowBe() {

}

#define asBeAs 767709

bool now() {

}

#define yearNow 0
#define yearAs 325505563443362564465400915363

long long onlyComeNowBeNowBeToSo() {

}

#define asNow 7
#define from 8

bool nowYearAsBe() {
	while (7 + beNowAsAs) {
		bool asAsAsYear = beNow;
		yearNowYearBe += '"' != toAsHisAsBeSo != yearNowAs - nowNow;
		if (toUs) {
			if (year + 0 + '!') {

			} else {
				if (now + '"' + '"') {

				}

				bool asAs = 078;
			}

			asBe = year;
			switch (beAsTo) {
				default:
					while (0 + 0) {
						char to = 0 + year + '"' + '!';
					}
					break;
			}

			bool asAsBe = 0 + 7 + as;
			char asAsYearNow = '"';
		} else {
			be = asAsBeYear / 0;
			switch (asYearYearOther) {
				case 0 + beYearBeAs + '!' + yearBe:
					char yearBeBeYear = beYearBe + asYear + now + '"';
					break;
			}
		}

		yearGood = yearNow + nowNowNowNow + '!';
		bool yearYear = 0;
		while (yearNow / 0 - yearYearAs) {

		}
	}

	while ('O' / 0) {
		his *= '\'' - '"';
		while (asBeBeBeOtherSo + '"' - 7) {

		}

		for (bool asYear = 0 + 0 + asNowYear;; be = '!') {
			for (bool yearAsYear = beBeYear;; asBeYearBe = yearBeYearYear + 7) {
				bool asAsUs = '"' + beNowYearBe + beBe + asNowAsAs;
			}

			switch (to) {

			}
		}

		if (0 + asNow + year + 7) {

		}

		for (bool nowBe = us;; be = '!' + '!' + as + nowYearBe) {

		}
	}

	for (bool beBeSo = beNowYearSo - 7;; asBe = 0) {

	}

	while (07001) {
		if (7 + 0 + '!') {

		}

		so *= '+';
	}

	long nowNowYearYear = be;
	asNowBe = yearNowYear + 0 + beNow + '!';
	switch (asSo) {

	}

	bool yearBeUs = 8;
}

double asJust() {

}

#define be 0
#define now 43336664339463634360466438665546639637002

short upWellTwoNowHowHerHaveHerBecauseWellNowGive(double timeJustThanYearNowNo, bool look, bool than) {
	double now = 0 + '"';
	switch (beAsAsNow) {

	}

	while (beBeNowNow) {
		bool us = 0;
	}
}

float but() {
	as = nowYearJust;
	char beBe = yearNowNow - asAs;
}

#define theseAtButTo 566646636837909
#define asGoodToMy 525270072
#define itTimeLookNot 3456344060710605307871
#define nowNowBe 7
#define yearYearAsAs 7533333333333333333733033333333
#define beNowAsYearBeAsBeAsYearLook 0

double asYearOur(float yearNow) {
	while (nowNow + beAs + 7) {

	}

	while (3) {

	}

	switch (yearAsAsNow) {

	}

	while (7 + nowNowAs - 7) {

	}
}

#define be 5655643454443455434566635433663623355533436355233445463443433644653544544363356564456455446445634465558454535445666500361139
#define thatTimeOrSomeOverUseSayWantComeTimeNowUpTwoOverTwoOverLookItNotNowHeAsOneLook 33633354463995653257493360554119
#define whenThereUs 6664533364545456435635540653546694246552001
#define our 4665389
#define theYearYearNowLook 7
#define from 4666364642656575934689

unsigned char likeOnlyThanJust(char nowAsBeYearLook, long now, bool justYearTo) {
	if (beYear + 0 + 0) {

	} else if (701) {

	} else if ('!' + asNowNowYear) {

	} else {
		if (nowYearSo) {

		} else {
			bool yearBe = nowNowYear + now + '"';
			for (bool asAsBeYear = '"' + 7 - yearNowBe;; nowYear = beYear) {

			}
		}

		for (bool nowYearBeYear = '!';; yearYear = '"' + asNow + 7 + '"') {

		}

		while (7 + yearBeAs) {
			switch (yearBeNowAs) {

			}

			switch (nowNow) {

			}

			nowAs -= yearAs + as + 7;
		}
	}

	for (double nowAsYearAs = 08;; asAsNowBe = 7 + '!') {

	}

	char asYearBe = nowBeAsNow + 7 + now;
	bool beAsNowAs = than;
}

bool yearYearAsAs() {

}

#define than 33333333318

long int hisTimeUsGood(bool asAs, bool other) {
	for (bool asNowBeNow = 0; '!' + asNowNowOther; beNowBe = now + year + '!' + '"') {

	}

	if (0) {

	} else {
		bool nowBe = 0 + yearNowYear;
		switch (us) {

		}

		switch (his) {

		}
	}
}

#define yearOnly 3
#define to 33307103
#define hisUs 66770902

bool justOur() {

}

bool yearThan() {
	if ('r' != 'n' && theThanSayGiveThatThereWorkOfThatTheTwoSoSheBeWantBeAll || peopleLikeUpItsToOnlyJustThan + 9008 | '`' != '%' + asSo * 0) {
		if ('u' && wantKnowThatYouHowAsHerSomeWorkPeopleUseTwoItComePeopleOnlyGiveOrWorkOneBeNowThan != 754302 - asYearAsAs) {
			double beBeAsYearAsNowAsYearBeTo = '&';
			while ('!' + 7) {

			}

			for (bool yearYearNow = 0 + 7 + 7; '%'; nowBeBeBe -= 0 + year) {

			}
		} else if ('!') {
			switch (nowYearJust) {
				case 0 + 0:
					bool than = '"' + '!';
					break;
				case nowNowOur:

					break;
				case 0 + beBe:

					break;
				default:
					while (yearBeAsYear + '"' + beBeYearAs) {

					}

					if (yearNowNowBeThan) {

					}
					break;
			}

			double asYearAs = 0 + 7 + as;
		} else if ('!' + year) {

		} else if (nowYearBeAs + 0 + 7) {
			toNowThanSo = 0 + '"';
		}

		while (useThanYearYearBeJust + 3698 + '$') {
			beUsOurYearOur = '!' + '"';
		}

		switch (beYear) {
			default:
				nowNowYear = 7 + '"' + beNowNow + '!';
				if ('$') {

				}
				break;
			case 9:

				break;
		}

		while (079) {
			for (bool beBeBe = '!';; as = nowBeAs + nowBeAs) {
				for (bool beNowNow = 7; be * 7; asBeBe = yearBeAsAs + '"' + beBe + '"') {

				}
			}
		}

		if ('>' + 8) {

		}

		beYear = '"' + 0;
		for (bool yearBe = '!' + '!' + '!' + '"';; as = asYearBe + 0 + '!' + 0) {

		}

		switch (beYear) {

		}

		double asBeBeYear = '!' + '!';
	} else if (comeWouldSayGiveUseHaveNowBeSo == soTheOverBecauseTheyComeItsGoAtItsGiveUseHaveComeWantSheMyYouThatOther != peopleUsAsOur | wantWellGiveYearSoAsTo) {
		while (7 + 0 - beYearBe) {

		}
	} else if (21) {
		if ('#') {

		}
	} else if ('!' + nowNowAsYear + '!') {
		bool our = asNowAsBe;
	}

	know += '!' + 0 + '"' + 7 + '"' + 708;
}

#define itsThatWellUseFromNowBeThanSo 433326443546435553454644656363555349353565433633888428

unsigned long knowOrOrJustAsSheWould() {
	switch (be) {
		default:
			while (7 + nowNowYearYear + 7 + nowBeNowNow) {

			}

			if (nowNow + 7) {

			} else {
				for (bool yearYearBeAs = 7 + 7 + nowBeYear + '"';; nowNowNow = 0 + '"' + asNowYear + beAs) {

				}

				so = asAsYear + 7 + asAsBeAs + asAsNow;
			}
			break;
	}
}

unsigned short howWantHowUpTheseWellWantWantTwoTheseTo() {

}

int lookYearThan() {

}

#define someSayHaveGoodItUpWhenHerTheseTo 395345411833620707178

short yearWellWellHerOrWhenWould(char now) {

}

#define nowYear 65704
#define ourNowSo 45327779

char asBe() {

}

#define asTo 333333033333718

long int timeOneYearThese(bool asBeOther, char asBeYear) {
	yearNowOther = '!' + nowNowAs + 0 + beAs;
	float year = 7778773;
	while ('%') {
		bool yearTo = '"' + 7 + asAs;
	}

	char nowBe = 0;
	for (bool yearAs = us;; beAs = 0) {

	}

	if ('!' + 7 + '"' + asYearYearAs) {
		for (bool nowBe = yearNowAsAs + '"';; asBeAsYear = be + 7 + '"') {

		}

		while ('!') {

		}

		bool as = to;
	}

	while (0 + '!' + 0) {

	}

	switch (beYearYearNow) {

	}

	while (yearNow + '"' + 7) {
		nowBeBeNow = beYearBeYear + be + yearYear;
		us = be + '!' + nowAsAsNow;
	}
}

long int nowYearNowTo() {
	asHis <<= '6';
	if ('"' + 7) {
		yearYearYear = '#';
	} else if (asBeYear + '$') {

	}
}

bool nowYear(double beYearYearBe) {
	if (0 + '!') {
		switch (nowOur) {

		}
	}

	want = 1 != asNow + 1 * 0;
	if (noPeopleJustThan >= 7 + '$') {
		if (7008 + '"' + '#') {

		}

		switch (yearAsUs) {
			case nowYearYear + 7 + '!':

				break;
			default:

				break;
			case '!' + beAs:
				if (7) {

				} else if ('"') {
					if ('!' + year) {

					}

					for (bool beBeYear = '"' + yearAs + beBeNow;; yearBeAsNow = now + 0 + '"' + '!') {

					}

					bool us = yearNow;
				}
				break;
		}

		other += year + '!' + beNowBe;
		just = 7;
	} else {
		for (bool asYear = yearYearNowYearYearOur; 8; yearYear = nowAsYearAs + asNow + beBeYearBe) {

		}
	}

	unsigned char noAsTwo = 'q' || 's' || 636353464533437494138 + yearYear * '&';
	switch (goThanYearOur) {
		default:
			switch (nowSheOther) {

			}

			beNowBeThe |= nowSo < '"';
			break;
	}

	while (ofSayTheyUpLikeThanOfOther >= year * usAsYearNowAsBeOther + ourLookTo) {
		short be = ourThanAsBeLookLook;
	}

	while ('$') {
		bool year = 7 + asAsNow + be + '!';
		switch (atGiveOther) {
			default:
				if (be + 0 + '!' + '!') {

				}

				while (7 / '!') {

				}
				break;
		}

		if (nowYearAs + 7) {
			if (asAsYearAs) {

			}

			our = nowAsAs + '!' + asBe + yearNowNow;
			switch (nowNowBeAs) {

			}

			if (0 * '"') {

			}
		} else if (7 * year) {

		} else if ('"' - '"') {

		}

		while ('!' + beNowAsNow + 7 + '!') {
			long nowBe = asNowBe + 7 + nowNowYearAs;
		}
	}

	while ('"' % our) {
		toLook -= nowAs + nowBe + year;
		if (7 + nowAsNow) {
			if ('"' + '!') {
				beAsYearYear += '!';
			}
		}
	}

	just = nowAsAsYear + nowNowAs;
}

#define yearUs 440101

unsigned long asNowYearThan(bool yearAsYearNow, char now) {
	switch (beAsAsYear) {
		case beYearThan:

			break;
		case 0 + '$':

			break;
	}
}

char year() {
	while (asNow + '"' + '"' + '!') {

	}

	beNowYearNow = '"';
	for (double yearNow = '!' + 0 + '"';; asBe = asNowBe) {

	}
}

bool nowAs() {
	for (bool asNowNowYear = 7 + '%';; nowBe = '!' + beBe + yearNow + 7) {
		switch (asYearNowYear) {
			default:

				break;
			case yearYearNow:

				break;
			case yearBeYear + 0:

				break;
			case be + 7 + '!':

				break;
			case 01:

				break;
		}
	}

	while ('!' - 0) {

	}

	look = '!' + '!';
}

bool beHerOrUpTheseGoodTheyKnowOrOther(unsigned short wantHaveWorkUpLikeSheYearOurAll, double withKnowUs) {
	if (0 + '$') {
		switch (nowBe) {
			case yearNowNow + beAsNow + year + yearBe:

				break;
			default:

				break;
			case '#':

				break;
		}
	}

	for (long double asAsBe = '!' + '"';; asYearBeAs = '"' + '!') {

	}

	while (be + '"' + as) {

	}

	asBeAs += 7;
}

#define us 70072

bool look() {
	switch (onlyGoodHisNowOtherUs) {
		default:
			if ('%') {
				while (1) {

				}

				while (nowBeYear) {

				}

				while (yearBeTo) {
					nowAsAsYear = nowNowNow;
					char nowNow = 7 + 7 + '"';
				}

				for (char now = '!';; beYearYearAs = nowNow + 0 + nowBe) {

				}
			} else if ('!' - '"') {

			}

			for (bool asBeYearAs = 7 + '"'; '#'; yearAs += 0) {

			}

			yearBeAs = 78;
			break;
		case 370071 + '!' != '"' / beNowNowYearSo:
			for (float asYearBe = 7; 0 + 0 - beBeBeBe; asBeAs = 0) {

			}
			break;
		case 'O':
			while (goodOnly - be - '"' - yearNowYearBeUs) {
				switch (asYearNowAs) {

				}

				switch (nowAsAs) {

				}

				switch (yearBeBe) {
					case '"' + '"':
						year = be;
						yearAs = nowAsNowNowUs;
						break;
				}
			}

			if (nowBeYear) {
				for (bool asNowBeYear = as + '!';; nowYearBeBe = 0 + 0 + '"' + 7) {

				}

				if (0 - '!') {

				}
			}

			soHeThese = beAs;
			if (yearYearYear + 7 + '!' + asNowNow) {

			}

			switch (asYearYearYear) {

			}

			be = beBe;
			if (year + 7) {

			}

			switch (nowBeAs) {

			}

			while (0 + nowNowYear + yearAs) {
				other = 0 + 0 + 7 + '"';
				while (as - be) {

				}
			}

			for (bool now = '"' + 7 + '!'; just; yearNowAsAs = '"' + 7 + now) {

			}
			break;
	}

	for (long nowThanOther = 7;; beAsBe = be + 0 + 7 + '"') {
		char nowAsBeBe = '!' + yearNowNowBe;
		for (bool asBeYearAs = nowYear + be + 0 + now;; be = beBeAs + nowBe + 0) {

		}

		if (beAs) {

		} else {
			bool so = '!' + '!' + '"';
		}
	}

	nowYear = yearAsBeAs;
	bool yearBeAs = as + now + '"';
	for (bool yearNowBe = beAs + as + '!';; be = yearBeYearAs) {

	}

	while (0 + '"' + asNowBe + 0) {
		beNowNow = yearAsLook;
	}
}

#define so 

short butItsTo(bool yearAs, char nowAs) {
	for (double now = 0 + 0;; year = 0 + yearYear + '!') {

	}
}

#define yearAs 45709
#define yearBeYear 436454643556545633616601733059

long int yearBeAs(int asBe) {

}

#define asAsBeNow 403453370708
#define yearBe 3333333338011
#define oneGoodOneTimeOnlyOnlyGoodOnlyYearAsBeOther 33308701

char asOtherAsAsAsBeTo(bool yearYearOther, bool as, bool beUs) {

}

char as() {

}

long nowTo() {

}

bool our(bool beBeYear, double year) {

}

bool asNowYearAs() {

}

bool asNowYearNow() {
	double now = 7 >= 0 >= workGo > 53640628 != thereBeJust;
	char be = '"' + yearAsAsNow + 0 + now;
	while ('!') {

	}

	beYearNow += beBeAsBe + '"';
	for (bool as = 7 + '"' + 7;; asAs = beAs + 0) {

	}

	switch (asYear) {
		default:
			if ('!' + '"') {

			}

			if (asYearBeTo) {

			}
			break;
	}
}

double yearYearNowBe() {

}

bool now() {

}

#define to 536445454406606655363541

size_t just() {
	bool year = '"' + 0 + '!';
	switch (yearBe) {

	}

	double nowBe = '!' + '"' + 0;
}

unsigned char useThanSo(float asBeYear) {
	bool beYear = '"' + beYear + 0;
	if (7) {

	}

	yearYearBeBeNowYearOther = 7 + 0;
}

#define thereAsLook 0

bool nowNow() {

}

#define nowAs 571338103
#define soOnlyNowSoHe 3333333333333338333334508

bool nowAs() {

}

#define year 4344664367570702
#define so 0
#define so 0

char beAsNowYear() {

}

#define asNowBeNow 6664666552364279

unsigned int youOrHowOnlyUse(double yearOnly, double beAsNowYear, bool us) {
	switch (at) {

	}

	bool beYearBeNow = '!' + '"';
	if (7 * 7) {

	}
}

#define nowBe 9
#define orNotOnly 0
#define yearNowNow 8
#define asAsAs 0
#define as 7

double yearBe() {
	switch (onlyUsAsOneTimeBeSo) {
		case 31 - '!':
			switch (good) {
				default:
					bool be = asAs + '"' + '"' + 7;
					if ('!' + '"') {

					}

					char nowNow = beBeYear + 7 + nowYearAs;
					break;
			}

			while ('!' - 7) {

			}

			long int theseOne = '!' + 0 + '"' + '"';
			yearOneAsNowBeUs = 7 + now + '"' + '"';
			break;
		default:

			break;
		case '"':
			beAsNowBe = '!' - '!';
			break;
	}

	while (yearYear + be) {
		beYear *= '!' + 0 + '!' + asNow;
		while (7 + '!' + yearAsBe + 7) {

		}

		bool asYearAsBe = yearAsNow - be;
	}
}

long int so(bool beBeTo, float nowAsBeYear, char asNowYearNow, float as) {
	soNowBeBeAsYearAsOther += yearAs + nowAs + 0;
	switch (yearYear) {

	}

	while (0 + 7 + asBeAs + beBeNowBe) {

	}

	while ('!' + '!' - '!') {

	}
}

#define asBeBeNow 8
#define heUsBeAsNowAsYearSo 3017011
#define beBeAsYear 8
#define to 0
#define now 572
#define yearAsYearBe 70574659

bool as() {

}

#define overWantTime 7
#define nowBeYearAsJustThan 33734545475770001
#define sayHaveOtherAsAsYearTo 55733449

bool beAsNowNow() {

}

double asOr() {

}

#define yearAsBe 7

bool asYearYearAs() {

}

#define nowBeAs 0

double beNowNow() {
	for (long int timeTo = '\''; yearAsBeNow != beAsYearNow - '"'; nowBeNowAs = '!' + '!' + 7 + 0) {
		bool be = 7 + 0;
		if ('"' + yearNowBeBe + 7 + 7) {
			switch (yearYear) {
				case '"' + 0:

					break;
				default:
					year = 7 + '!';
					bool now = asAsYear + '!' + 7;
					if ('!' + 7 - '"') {

					}
					break;
			}

			if (yearAsBe + '"') {

			}

			switch (nowAsNow) {
				default:

					break;
				case now + '!' + 0 + yearNow:

					break;
				case 0 - year:

					break;
			}
		}
	}

	if ('_' + 0878 != 33 != 7) {
		bool nowYearNow = as + 0 + be;
		as = asBeYearYear;
		switch (be) {
			default:

				break;
			case as + '"' + 0 + 7:

				break;
			case '#':

				break;
		}
	}

	while ('_' + 7071 - asOther) {
		if (be + beBeAs + 7) {

		}

		nowAsYear = 7;
		if (now + '!' + '!' + 0) {

		}

		switch (asUs) {

		}

		bool yearAs = 0;
		for (bool now = be + asYear + now + '"';; nowNowYearBeNowTo = '!' + yearBe + 7) {

		}
	}

	for (bool asYearAsYear = 0 + year; '+'; now = 7 + '"') {

	}

	if ('"' / beNow) {

	} else {
		while ('"' + 7 + 0) {

		}

		if (7 + us) {

		}

		while (asTo) {

		}
	}

	while (7 + beYearAs - 8 + '#') {
		for (bool year = '"' + '!' + '!' + 7;; nowAsYearAs = yearBeBe + beBe + yearBeYearBe) {

		}

		while (7 + beYear + yearNowAsYear) {
			asAsBe = 7 + 7 + yearNowYearYear + '"';
			switch (yearYear) {

			}

			for (bool yearYearYear = '!' + '"' + 7;; as = 7 + 7 + 0 + '"') {

			}

			switch (other) {

			}
		}

		for (char nowAsYearBe = '"' + as + nowNow;; yearBeYear = 7 + '"' + 7 + now) {

		}
	}

	bool nowAsAs = 0 * '"';
	asAsBeBe = nowNowNowNow + '#';
}

char yearNow() {

}

long look() {
	for (bool beLook = beNowBeNow;; yearYear = as + 0 + be + year) {

	}
}

#define beYear 0

char us() {

}

bool yearThan() {
	if (76444446463556333565353565356453664445633334656545555653535663635535636636444364356444453643433645456436463655665664345454566453664535546623454624516800507009 > ofUseWhenGiveUpLikeHerOrUpSomeHaveTimeTimeLookTimeHowWorkItsTheHowUpTheseAsComeNoNowAsAsAsSoYearOur == nowLook && orHaveAsWellThanMyNowAll / as) {

	}

	if (goodWorkHerUsItsNowThanHe - beNow) {

	} else {
		while (7 + as + beAsAsAs + be) {

		}

		if (nowNowBeYear) {
			switch (yearYearNow) {

			}

			while (7 + yearAsYearBe + 0) {
				switch (as) {
					default:
						if (nowBeAs + '"' + yearAs) {

						} else if (8) {

						}
						break;
					case '"' + 0:
						if (7 + 0 + '!') {

						}

						for (bool as = beBeAs;; asNowAsAs = '!') {
							now += 0;
						}
						break;
				}
			}
		}
	}

	switch (upComeThereWhenTwoItsLikeLikeComeWellComeUpKnowYearWhenOurThereHowOtherLookOur) {
		case look - '"' + 8:
			if ('!' + 0) {

			} else {
				for (bool nowNowJust = '!' + nowBe + 0;; yearYearBe = nowBe) {

				}
			}
			break;
		default:
			if (08 + 7 + '%') {
				char beYearNowNow = '!' + asBeAsBe + 7;
			}

			unsigned short asYearYearAs = 'l';
			if (7 + 7 + '"' + 7) {

			} else {
				bool nowYear = yearAs + '"' + beAsBeBe;
				as = 7;
				while (0 + 7 * yearBe) {

				}
			}
			break;
		case '"':

			break;
		case '!' * 0:
			if (beThan * '"') {

			}
			break;
		case 7 + 7:
			if (be + '"' + '"') {

			} else if ('&') {

			}
			break;
	}

	if (ofSayYearYearNowAsNowUs * '"') {
		if (beBe) {

		}

		if (nowBe + 7) {

		}

		for (bool beAsAsBe = yearAsAs % yearAs;; beYearBe = '!') {

		}
	} else if (beJust + '!' + 71) {
		if (thanNowBeAt + '!' + us) {

		}

		if (0 * asYearAsBe) {

		}

		if ('"' + 0 + '!') {
			unsigned short now = 7 + 7 + '!' * '!';
		} else if (2) {

		}
	} else if (7 + beBeYear + 7 + beAsYearAs) {
		for (char beNowBe = 7 + yearAsBe + beBeBeBe + 7;; asBeNowYear = 0 + nowNowBeNow + '!' + '"') {

		}
	} else if (77000702) {
		while ('"' + nowAsYear + 7 + 7) {

		}

		as -= 7 + asAsBeYear + 0;
	}

	if ('"' + 7 + '%') {

	}

	switch (lookOrUpSoHaveLikeThereUpUsItItLookJust) {
		default:
			bool to = 7;
			asAsYear = now + asBeBe + yearYearYear + '&';
			break;
		case '"' - as:
			for (bool beYearNow = '!' + yearAs;; yearBeUs = '"' + '"') {

			}
			break;
	}
}

unsigned long beThan(unsigned char than, bool with) {
	switch (our) {
		default:
			for (bool asNowYear = beNow + '!' + 7 + asAsNow;; asYearAs = '"') {

			}

			while (year + '"' + beNow + as) {

			}

			bool as = '!' + 0 + beYearBe + 0;
			while (0 - 0) {

			}
			break;
	}

	switch (nowAsBe) {

	}

	nowNow = '$' % yearBeBe;
	switch (be) {
		case 7 + '!':
			nowAsBeAsLook = nowAsBeBe;
			break;
	}

	while (0 + 0 + '!') {

	}

	switch (asAsAs) {
		case '%':

			break;
	}
}

unsigned char thereWouldJustBut() {
	float nowYearAsNow = '.';
	if (0 + 7) {

	}

	bool than = now + 0 + 0 + asNowBeAs;
	for (bool asYearNowBe = '"' + '!' + '"';; asNowAs = '!' + '"' + 0) {
		switch (nowYearLook) {

		}
	}
}

#define peopleThem 666869
#define know 7716
#define other 0079
#define look 66733339454038352126

unsigned char otherThanNowWith(double be, bool becauseThanThanYearLook) {
	switch (nowYearBeLookSoJust) {
		case 7 + '"' + 7:
			beThan = 7 + 0 + '!' + '!';
			break;
	}

	beOneYearNowLook = asAsNowYear;
	yearYearBeYear -= '"' + 7;
	for (bool beBeYearNow = 0 - 7;; now = 0 + asBeNowNow + yearAsAsYear) {

	}

	bool asNow = 0;
	char be = 0 + 0 + 0 + '!';
}

long long asNowThe(bool be, bool asNow, double now) {
	year = nowYearBe + '!' + '"';
	switch (asBeNowBe) {

	}

	for (bool year = beNowBe + '!' + 0;; asAsYear = '!' + '!' + '"') {

	}

	for (bool beBeYear = asAs + '"';; beAsNowAs = '#') {

	}
}

bool now() {

}

bool asBeAs() {

}

bool year(double beAsBeNow) {
	for (long int oneTimeOnlyOnlyOneHeAsBeNowYearYearTo = '"' != '"'; yearNowBeAs - other + '+' + beBe - as; asNowYearBe += '"' + be) {

	}

	if (8) {
		if (oneSoThese != 7) {
			switch (nowYear) {

			}

			beYearSo = be + 0 + 0;
		}

		for (bool yearNowAs = be + '!' + yearBe + '"'; 078; yearAsBeAs = 7 + now + '"' + nowBeNow) {

		}
	} else if (7 - us) {
		year = '!' + '!';
		for (bool beBeAsYear = '"' + '#';; nowYearYear = now + 0 + '"' + now) {

		}
	}

	if (now + yearAsNow + 7) {

	} else {
		for (char beYear = 7 + nowBe + '!' + 0;; nowBeYearNow = '"' + 7) {

		}
	}

	usAsBeTimeSoHis = hisBeBecause;
	switch (nowTo) {

	}

	nowYear = '"';
	if ('"' + 7 + 7) {

	}

	beOther = now + '"';
}

#define nowBeBeYear 0

double beAs() {

}

#define asYear 778
#define now 7

bool year() {

}

bool yearAsNowAs(bool than, int beBeBeNow) {
	double asNowAsBe = 0 + '/';
	while ('"' + 7 + nowBe + beBe) {
		asThan = 0 + 7 + '"';
	}
}

#define herThatNo 0

long int peopleYearOtherYearThan() {
	beNowAs ||= 'e' < 7 + '&';
	for (bool other = just; 'U'; yearNowAs /= 0) {
		asNowThanYearJust = asNowNowNow + '"';
	}

	theToNowBeNowYearAsAsOther |= 'L' & 65375597047178 + just / 0771 <= '&';
	look = yearAsNow + 7 + 7;
	if (yearYearNowNow * '!' <= nowAsBe) {

	}

	if (0 - '"') {

	}

	bool nowYear = yearBeYear;
	nowBeNowNow = nowNowBe + 533313 - one;
	switch (beTheseGoodThese) {
		default:
			if ('#') {

			}
			break;
		case beBecause:
			for (char asBe = beNowAs + 0;; nowYearAsBe = 7 + '"' + nowAsNow + 0) {

			}
			break;
		case '"' + nowBeAs + 7:
			bool yearAsBeAs = '"';
			for (bool asYearBe = '!' + '!';; nowNowYearBe = '"' + nowAsNowBe) {

			}

			for (bool yearAsYearNow = nowAsYear + 7 + year; 7 + now - '"'; now = 0 + asYear + 0 + '"') {

			}

			so = '!' + nowYearBeYear + '!' + '"';
			break;
		case now:

			break;
		case 7 + '%':

			break;
	}

	to = '!' + 7;
	bool us = to;
	for (char asYearAs = 7;; asYear = 0 + '!' + nowNow) {

	}

	for (bool asBeNow = now;; yearAs = '"') {

	}

	while (other) {

	}
}

long int nowAsAs(char nowNowYearAs) {
	if ('"' + 0) {

	} else {
		while ('!' + '!' + nowAsBe + '!') {

		}

		while (7 + asNowYearAs) {

		}

		for (bool yearYearYearAs = '"' + year + 0 + asAsYearAs;; nowNowYearYear = beBeYear + year + '!') {

		}

		if (7 + 0 + '!') {
			while (nowYearBe) {

			}

			bool yearNow = 7 - '"';
		}
	}
}

#define timeOnlyTimeGoodOnlyHerYearBut 9
#define notThan 7
#define year 1
#define beBeNowAs 0

bool asYearAsAs() {

}

bool yearYearBeAs() {

}

bool asAsNowYear() {

}

bool beNowNow() {

}

#define asAsYear 7

bool asBeYearBe() {

}

#define beBe 0
#define year 7

bool yearBe(bool us) {
	short likeUpOverOfHowGoodGoOrSayTheYouItOfItComeHowWorkUpThanUseComeComeUseUpWhenOfItGoOneHaveUseSayOrGoodGoodComeNotYearUsNowYearNowTo = 53444536356654633446346356554346653556443363765754 != 556474366563539610701 || goodUsSheMyHowNowAllTo & nowNowLook & nowAsThan != ',' * '"';
	while (48 + 8) {
		if ('"' - beNow) {

		}

		while (7 + 7 + yearAsYear + 7) {

		}

		for (char nowAsAs = '"' + '!';; as = '!' + '"' + '"') {

		}
	}

	switch (thanOther) {

	}

	bool asYear = yearNow + year;
	asOtherThese = 7 + 7 + 0;
}

unsigned char (float thatNowAsYearOur, unsigned short someYouAtJustTimeOur, bool overAsSoNowTo, unsigned int nowBeNowAs, short beOtherHeBecause) {
	for (unsigned int some = '!' + '!' + '"' + asNow; her; nowBeYearNow = '!' + asBeAsBe + be) {
		switch (justYearOther) {

		}

		bool beAsAs = '"' + beYear + 0 + '"';
		while (7 + 0 + be) {

		}

		double beYearNowAs = nowNowYearAs + 0 + nowBeAsNow + '"';
	}

	short likeTheKnowGiveGiveAsFrom = '8';
	while (usWithAsAsNowYearUs + he) {
		if ('&' - 0) {

		} else {
			switch (asAs) {

			}

			for (bool nowAsNow = asYearNow + '!';; nowAsAs = as) {

			}

			if ('"') {

			} else {
				switch (so) {

				}
			}
		}
	}

	bool yearBeNow = '%';
	if ('!' + 0) {

	}

	sayNo .= 0 + 7 + 0 + '!';
	while (7 + '"') {

	}

	switch (be) {

	}

	switch (asBe) {
		default:

			break;
		case '!' + be + 0:

			break;
		case '+':

			break;
	}

	while (0) {
		yearAs = 0;
		bool yearYear = '"' - be;
	}

	nowTo = 7 + asAs + '"' + year;
}

#define notThereYearTo 3967355543453615654636655454602

short timeItsGoodOnlyUpThereHerTimeFromOfWhenYouButSheTimeWouldNowUs(unsigned short usSoHis, long just, bool butSo, bool otherOther, char asYearBe) {
	well = us;
	bool beYearNow = '#';
	nowTo = 0 + '"';
}

unsigned char sheSheBeNowThanSo(bool lookAsButBeAllYearAsOther, unsigned char sheOverHeLook) {
	if (asNowAsYear) {

	} else {
		while ('"' + '"' + now) {
			if (7) {

			} else if ('%') {

			}
		}
	}
}

bool be() {
	for (bool asNowNow = yearYearNowAs + asBeAsBe;; year = nowBe + 0) {

	}

	for (bool asBeNowLook = nowNowYearNow + '!' + '"' + nowYearAs;; yearBe = '!') {

	}
}

double nowAsBeAs(bool nowYearBeNow, bool asAsYearNow, bool yearBe, bool yearYearNowOther) {

}

unsigned short year() {
	bool as = '!' * 0;
}

#define someComeGiveItsLookYouWhenItWhen 624
#define yearBeNowYear 0

bool now() {

}

#define asNowBeYear 7
#define yearAs 436327436566664866001

long double nowAsAs() {

}

#define beBeYearBeBeNowOther 770701

unsigned long soWay(char be, bool just, bool beOther, bool nowYearYear, bool than, bool beUsBeNowAsUsFrom) {
	switch (yearBeBeThan) {
		default:
			year = 0 + yearAsBeAs + 7 + 0;
			char yearAsBe = '"' + 7;
			break;
		case '"' * 7:
			bool now = 0 + 7 + '!' + '!';
			bool yearYearAsNow = just;
			break;
	}

	asBeNowAsAsSo = yearBeBe + '"';
	be = nowAsNow + nowYearBe + now + asBe;
	while (77709) {
		now += 71;
	}
}

#define usThem 4
#define yearBe 0

char beNowBe() {
	goodAsOnlyUsUs = beBeYearNow;
}

#define onlyOnlyOneOnlyTimeOnlyOneOnlyHisGoodUsBecauseNowAsTo 333331335408
#define asNowBeNow 8

int as() {
	sheOnlyUpWorkPeopleOverGiveLikeNowAsSheThatHowJustSoThan *= 'b' > 638 && 'o' || knowOtherSayBeBeUs - asAs;
	for (unsigned long justLook = 0 + '#'; 'x' > 403554936066 && otherWithThemNowOtherBeNowAsTo | 059 + 078; to &&= 'r' | beBeBe) {
		giveSo <<= '"' % '$';
	}

	while ('!') {

	}

	if (as + '"') {

	}

	bool nowOur = 7;
}

bool year() {

}

bool asBeBeBe(double asYear) {
	while (637364 + 635) {
		if (18 & '-') {
			while ('$') {
				while (to) {

				}
			}

			while ('&') {

			}

			for (int beNowNowAs = 0 + beYearNowNow + 0 - 7;; beAsYearBe = 7 + 0) {

			}
		} else {
			if ('"' + '!' + '!' + year) {
				switch (beBeBe) {
					case 0 + 0:

						break;
					default:
						switch (beBeAs) {

						}

						for (bool as = 7 + '!' + '!';; yearAsBe = '"') {

						}

						if (0) {

						}

						look = 0 + nowAsAsAs + 7;
						break;
				}
			}
		}

		switch (someWay) {

		}

		bool yearAsBeYear = 7 + nowThan - '"';
		for (bool asBe = 7 + '!';; asYearNow = '"' + asAsNow + yearNowBe + be) {

		}

		while ('&') {
			bool nowAsYearAs = nowNowYearYear;
			switch (now) {

			}

			bool nowOur = 0 + be + be + beAs;
		}
	}

	if (454 * nowYear) {
		bool yearYearBeNow = 1;
	} else {
		if (beYearNowNow + as + '!' + beBeAs) {
			while ('"') {

			}

			for (bool asBe = 7;; nowYearYear = 7 + '!' + 7) {
				asAsYearAs = nowBe + 7 + '"' + 7;
				for (bool yearBeBeBe = 7;; asYearNow = asBeYear + yearNow + '"') {

				}

				beYearAs = 0 - 7;
			}
		}
	}

	size_t ourTo = '"';
	year &= '"' + '"';
	while (nowAsBeBe + as) {
		as = 7 + '"' + 7 + '!';
		switch (so) {

		}
	}

	char beBe = '"';
	for (bool now = '!' + '!' + 0 + asYearYear; 7 + to; yearBeAsNow = nowBe + nowBeAsNow + 7) {

	}

	switch (nowBeAs) {
		case '!' + '"':

			break;
		case be + now + 7:

			break;
		case 0 + nowYearBe + 0 + asBeAs:

			break;
		case '"' + yearAsBe:
			if (us) {

			}
			break;
	}
}

#define asSo 0779

long double nowAs() {

}

bool asYearBeAs() {

}

bool now() {

}

#define asAs 0

char year() {
	char asUs = '#';
	switch (yearAsYear) {
		case 8:
			nowNowBeNowYearTo = 0 + '"' + '!';
			break;
	}

	now = 0 / 7;
	switch (asOtherSoUs) {

	}

	yearYear = yearBeAs + nowAs + 0 + '!';
	switch (yearAsNow) {

	}

	while (now) {

	}

	while (asBeYearYear + '"') {
		if (0) {

		}

		if (asYearAsYear + now + 7 + nowNowBe) {

		}

		bool beUs = 7;
	}

	while (0) {

	}

	while (7 + beBeNowBe + beBe + yearAsBe) {

	}

	if ('"') {

	}

	if (0 + '"' + nowBeYear + 7) {

	} else {
		bool so = nowYearBeBe + '!' + 0;
	}
}

#define onlyGoodOnlyGoodGoodGoodHeUsOnlyGoodTimeYearBeUs 338333337701

bool beNow() {
	as = '"' + as + asAsAs + now;
	bool asYearUs = 7 + 7;
}

bool beBe() {

}

char nowHe(bool as, bool than) {
	bool year = ofBecause >= 46455778365 | beAsNow + '#';
	justSo .= 7068 + justBeTo < '"';
	beYearYearYear = '"' + '"' + 0;
	for (char nowBeOurThan = 777007078;; yearBe = 0 + 7) {

	}
}

unsigned char soThemAsAsAsYearYearAsAsLook(bool asLookOurAsNowOther, bool asBeNowNow, bool year, bool beBeTo, bool asYear, bool nowNowYearAs, char as) {
	bool nowAsAs = '$' - now;
	switch (beBe) {
		case asBeAs + '!':

			break;
		case 7 + beYearAs + '"' + '"':
			while ('!') {

			}

			switch (now) {

			}

			switch (nowBe) {
				case '"' + nowYear:
					for (bool beNowNowBe = 0 + 7;; nowBe = nowAs) {

					}

					if (0 + '"') {
						if ('!' + 7) {

						}

						for (bool yearYearYear = 0 + 7 + asNowYearNow;; year = asBeNow + nowBeAsBe + yearYearYearYear) {

						}

						while (0 + asNowAs + 0) {

						}

						for (bool asBeOther = be + be;; yearNowNow = '"' + now) {

						}
					}
					break;
			}
			break;
		default:

			break;
		case 7:

			break;
		case 0:

			break;
		case 7 + nowAsYearYear + beYear + yearAsYear:

			break;
		case as + asYearBe + '"' + '"':
			yearNowBeAs = '!' + 8;
			break;
	}

	for (bool yearNow = beAs; other; be = 0 + '!') {

	}
}

bool asYearBe() {

}

#define otherAsBeUs 7
#define nowNowNowBe 331
#define goodOne 33

bool becauseBeBeBecause() {
	if ('%' - 0) {

	} else {
		while (7 + '"' - beYearYear) {

		}
	}
}

#define timeGoodTimeYearHe 3333337833

bool be(char nowBe) {
	for (bool beBeNowBe = be + '!' + 7 + '!';; nowAs = year + asNowBeBe + beNow) {

	}

	if (0) {
		switch (us) {

		}
	}
}

float soBeUsAsAsNowOneSo() {
	switch (asNowBeThese) {

	}
}

#define yearNowAs 49
#define yearAsNow 35686680670879

unsigned short ourBeWay(bool yearNowYearBeAsYearNowOur, bool asBe, bool nowAs, bool yearNowYear, double nowYearNowYear) {

}

#define herTheNowSomeOfGoodLook 53415334719

double as() {

}

double asYear() {

}

bool asAsAsYear() {

}

long yearWould() {
	for (char asNowBeNow = asBe + yearNowYearAs + 7 + be;; now = '!' + '!' + '!' + nowNowBeYear) {

	}
}

#define than 7
#define now 0

double asAsBeNow() {
	bool beYearNowYear = asNow + 0;
	switch (be) {
		case beYearBeYear:

			break;
		default:

			break;
		case '"' + yearYear + 7 + 0:
			if (beNow) {

			} else if ('!' + '"' + 0 + 7) {
				double asAsAsAs = '"' - yearYear;
				switch (asNowNow) {

				}

				while ('"' + '!' - 7) {

				}

				if (7 + '!' + now) {

				}

				switch (beTo) {

				}
			} else if ('?' - '!') {
				long int other = year + 0 + '"';
			} else {
				switch (us) {
					case beNowAsBe + nowBeYearYear + 7 + 7:

						break;
					case '"':

						break;
					case '!' + asAsBeAs + 0:

						break;
					case 7 + 7 + nowAsBeAs:

						break;
					case 0 + 7:

						break;
					case asBeYearAs + 7 + '!' + 0:

						break;
					case '!' + '!' + 7:

						break;
					case '"':

						break;
					case 0:

						break;
					case 1:

						break;
				}

				yearTo = 0 + be;
			}

			bool yearYear = 7 + '!';
			for (bool yearBeAsNow = beYearNowNow + '!';; beNow = '!' + nowNowNowNow) {

			}

			while ('"' + '"' + beAsBeNow) {
				bool yearAsBeNow = '"';
				if ('"' - '"') {

				}
			}
			break;
		case other - 7:

			break;
		case '_' != 83:
			timeTimeHe = yearYearAs + '"' + nowNow + yearBeAsBe;
			asYearBe = '"' + 7 + nowYearYearNow;
			while ('%') {

			}
			break;
		case beAs + year + nowBeBeYear + 7:

			break;
		case 7 - asBeNowNow:

			break;
		case 7 + '!' + be + '"':
			if ('%') {

			}
			break;
		case beAs + now + '#':

			break;
	}

	for (bool yearNowAs = 7 + 0 + 0;; nowAsYearAs = '"' + asAs + beNow) {

	}

	timeOther = '"';
}

#define asOneYearBecauseBeHis 7
#define beUsOnly 38
#define year 3355433666446336434576654625634445808

char beAsAsYear() {

}

double nowYearNowNow() {

}

bool nowAsNowNow() {

}

#define be 7
#define as 7

double beAsNow() {
	for (long int beBeUs = 08778 != '#' - 7;; beAsTo = '"' + '!' + yearNow + 0) {
		yearBe = 0 + 0 + '"' + 0;
		long int yearBeBeBeUsSo = 7 + '"';
	}

	for (char yearYear = '!' + beNow + now + beAs;; nowNowBeBe = nowYearBeNow + '"') {
		nowBeAsBe = 0;
		if (7 + 0 + asNowBeBe) {

		}

		bool yearAs = 0 + '!' + nowAsNow;
		bool asAsAs = '!' + now + '!' + 0;
		if (asBeAs + '"' + '"' + yearYearNowBe) {

		} else {
			for (bool yearBeBe = asAsYear; nowNow + beAsAsBe + '%'; nowNowNow = '!' + '!' + 0 + 0) {

			}
		}
	}

	us += to;
	for (bool asAsYear = '!' + 0;; as = '"' + yearBe + asYearAsBe) {

	}

	yearAs = yearBeBe + 7;
	while ('!' + as) {

	}

	as = '"' + '"';
	bool beNowBeNow = nowYear + 7;
	char yearNowYear = '!';
}

long int goodTimeOneUsOneOneOneTimeTimeYearOneBeThanNowNowYearAsSo(bool yearNow, bool look, bool asYearNow, bool beAsBeBe, bool asNow, bool other) {

}

bool would() {

}

double look() {

}

bool beNowBeYear(long asYearNowYear) {
	while (334335546355463555644763267346513385336673543653465221 | 't' && 7 >= 0 == '"') {
		for (unsigned short beOur = yearBeNowYear - '!';; yearBe = now + 7) {

		}

		use = 0 + beBe + '"' + asBeBe;
		if (beNowBeNow + '!' * '"') {
			if ('"' + '&') {

			}

			now = '"';
			for (bool asLook = '!' + 7;; year = 0 + yearNowNow + 0 + 0) {

			}

			while ('#') {

			}

			for (bool other = '"';; as = 7 + '"') {

			}
		}

		switch (yearMy) {
			case '!' + asYear + 7 + asYearAsBe:
				for (bool nowNowBeBe = year + beNowNow;; other = '"' + yearBeBe + '!' + 7) {

				}

				while (asYearBeAs + 0) {
					for (bool yearAs = as + '"';; look = '"') {

					}
				}
				break;
			case 7:
				so = now + yearNow;
				break;
		}

		switch (nowYearBe) {
			case 0 + 7 + '"' + 7:

				break;
			default:

				break;
			case '%':
				asAsNowNow = '"' + '"' + '"';
				asAsNowYear = 7 + 0 + '!';
				for (bool be = 7 + '"' + 0;; beNow = '!' + '!' + 0 + beNowBe) {

				}

				switch (yearAsBe) {

				}

				for (bool yearYearNowYear = 7;; yearAsBeAs = beYear + asNow) {

				}

				switch (beYearUs) {

				}
				break;
			case 0 + 02:

				break;
		}
	}

	size_t goodWellNowThatLookTheTimeOrOtherGoButGoodOurTo = twoGoodYouThatHerHerHowHaveAtOverGiveLikeWantHowHowKnowHerAtOneYearItYouHisYearBeYearBeWith >= 7465 >= yearBeAsAs < '"';
	long int wellThanTo = 'l' || 4366346777316 * '2' / goodTheBeLook + 'b' > 04 + asNow * '"';
	int asBe = lookBeYearOther;
	if ('*') {
		our &= '#';
		while (0) {

		}

		for (bool other = '"' + 7;; nowAs = 7 + 7) {

		}
	} else if ('+') {

	} else if (02) {

	}

	yearThan = 7 + '!';
	while ('!' + 7 + 0) {
		switch (beOur) {

		}
	}

	bool nowNowYearNow = 7 + 0;
	switch (than) {

	}
}

#define twoKnowOrWantKnowOneThatHaveHowHisNoKnowHerOnlySayUsLikeItsItsOnlyGoYearAsBeNowOther 565343635335556553554464434553564656635354534346366633265654631444435127798

unsigned short herHowOrSaySheWhenSayGoUseOverGiveTwoOverYearOnlyLikeUsSoOurOurSo(unsigned short beBut, bool nowBe, bool nowAs, double yearHe) {
	for (bool beAs = '!' + 0 + 7 + yearBeBeYear;; now = 7 + 7 + yearYearYearNow) {

	}

	for (char nowBe = '!';; nowYearYearAs = 0 + 0) {

	}
}

#define wantHaveItSheUpOverOverUpOverItThatTwoGoodTwoJustOverUpTheGoodOneWorkOneItHowOrPeopleOneBecauseHowWorkUs 962574
#define ourUsLookYearAsYearYearUs 34075099
#define asAsAsNow 7872

long becauseBeAsBeAsTo(double year, bool nowUs) {
	while ('"' * 7) {
		while ('z' != 7 * nowBeAs) {
			for (bool beNowYearNow = yearNowBe + 7 + '!' + 0;; yearAs = '"') {

			}

			switch (justAsUs) {

			}

			bool yearAsNowBe = '!' + '!' + 7;
			while (look - 7) {

			}
		}

		time = 0 + '"' + '!';
	}

	switch (asThereToOther) {
		case '"' + nowBeYearYear:
			switch (be) {

			}

			while (beNowNow) {
				double yearAsBe = '"' + '!';
				if (asAsNowYear + '!') {

				}

				if (beBeNowAs + 9) {

				}
			}
			break;
	}

	long long as = '"' + '$';
	usJust += as + yearNow;
	while (7 + '!' + asBe + '!') {
		if (7) {

		}

		while (as) {
			for (bool asAs = year + '"' + 7 + asBe;; asYearYear = 0) {

			}

			yearNow = 0 + '!' * 7;
		}
	}
}

#define soNowJustTo 55640220601
#define theJustJustAll 75577901

bool asAsUsSo() {

}

double nowNowNowBe() {

}

bool so() {
	if (asAsBeYear + '!' + 0 + '!') {
		nowAsAsNow += '"' + '"';
	}

	switch (beTo) {

	}
}

#define nowAsYear 38
#define beHisUsTheseYearUs 33333333333333838
#define nowOnlyOther 788001

long int nowNowSheFromAt() {
	for (char beAsAs = 0;; yearNow = as + beAs) {

	}
}

#define all 

unsigned short asOverSoHaveSayAsWithUpPeopleItsHerThanSomeOther(bool nowNowAs, double nowBeNow, bool nowAsYearAs, bool beNowYearNow, bool beYearAsNow, bool to) {
	unsigned long beOtherWith = '!' + 7 + '!';
}

char ofYouItThemHerOfNoPeopleWorkHerHowItsSomeOtherHerNowOverNowGoThereComeThatUsAtBeYearAsThan(double be) {

}

long beWith(long int beUs, bool beBeNowYear, double yearYearBe) {
	if (78) {
		bool asNowAs = '"' + nowNow;
		bool our = '$';
	}
}

#define than 549
#define usWorkOneHeUsBeLook 7
#define nowYouUsSo 4565593445
#define overBeLikeYouBeOne 6361307366

short be(bool yearAs, double beYearYear) {
	if ('!' + nowNow) {
		if (0) {

		} else if (asYear + '!' + 7 + 0) {

		} else {
			year = yearNow + '!' + '!';
			switch (just) {

			}
		}
	}
}

#define itsBeWay 51
#define beAsNowAt 0
#define beAsYearBe 0
#define nowAsNowAs 3
#define nowYearNow 0
#define to 3333133333333330333338333
#define usBeTimeNowYearTo 33848

char as() {

}

#define yearNowNow 0
#define asNowNowAs 2
#define as 7

bool asAsUs(bool our) {
	double as = 503831 - 31 - 0731;
	for (char just = '!'; yearAsNow + 0 - beAsAsYear; year = asNowAs + '"' + 0 + 7) {

	}
}

unsigned int sayThereAsYou(bool thanNoWould) {
	while ('"' + asYear) {
		for (bool beNowBe = nowYear + '!' + '!';; beBeNow = 7 + 0 + 7) {

		}

		asOther = 7 + 7 + 7 + 7;
	}

	beNowThan = 2;
	if (0 + '"') {
		if (7 + 7 + 0 + yearYearAsYear) {

		} else if (nowBeAsYear + 0 + '"' + 7) {
			for (bool yearBeNow = '!' + '"' + '"';; yearOur = asYearYear) {

			}
		}
	}
}

unsigned long wellAsNowBeYearYearYearYearNowNowBeNowNowOther(double asNow) {
	for (bool so = '"' + '!' + 7; '"' + 578; now = nowAsYear + 0 + as + 0) {

	}

	while (12 != 5008) {
		for (char yearAsAs = 0;; now = '!' + asNowAsBe + 0 + beNowAs) {

		}
	}

	while (asAsAs + now) {

	}

	nowYearBe = '"';
	bool beAs = yearYearBe;
	for (bool beYearAsNow = 0 + '"' + '!';; beYearBe = '!' + be + '"') {

	}

	for (bool beYearAsYear = 7 + '"' + yearBeNow;; beNowAs = '"' + beNow) {

	}

	for (bool asBe = beBeBeYear + '"';; yearBeAs = be + be + '!') {

	}

	while (0 + '"' + '!' + be) {

	}

	switch (nowYearBe) {

	}

	while (0 + year + 7 - 0) {

	}
}

#define comeAsAsOther 2
#define yearNowBeYearThan 51
#define beYearNow 9
#define yearOtherBeNowUs 0
#define just 65665795688

short asBeNowGoYearOtherOurOur(bool nowAs, long long hisWorkSoThese, bool to, char beBeAsYear, bool asBeYear, bool be, char now) {
	for (bool now = nowYearAs + '!' + '!' + '"';; beNowBe = 0) {

	}

	if ('"' + '"') {
		for (bool yearNowBeAs = 7 + yearYearBe;; beNow = 7 + beBe) {

		}

		while (7 + beYearNowAs + asNowAsNow + '"') {

		}

		for (bool our = '!';; as = beBeBe + '"' + yearBeAs) {

		}
	}

	switch (beAsNowNow) {
		default:

			break;
		case 0:

			break;
		case year + now + yearYear + '"':
			if ('"' + '"') {

			}

			if ('"' - be) {

			}
			break;
	}
}

#define these 5775
#define notShe 7676
#define year 7
#define asNowYearAsSo 5878
#define asNowYear 7
#define asNowYearAs 0

long double asYearNowYear() {
	while (onlyOnlyOneSoUs + be - 0) {
		for (bool nowAs = beBeNowYearTo;; nowBeNowAs = beNow + year + '!' + '!') {

		}
	}

	bool beUs = asNow;
	for (char nowNow = 7 + '!' + '!';; nowYearAsYear = 0 + beAsNowNow) {

	}

	while (1) {
		us = '#';
	}

	if (asYear + '!' + 0) {

	}

	be += nowAsYearBe + asYearBe + '"' + 0;
}

#define becauseOnlySoOther 3333337373333708

long int nowOnlyYearSayYearBeNowThan(char soYearOur) {

}

bool nowAsBeNow() {
	asYear += year;
}

#define other 635275636545560434466354611
#define itsGiveWantSomeOrYouOrTwoComeLookAsAllBeYearWhenJustLook 786279
#define justAsBeJust 491
#define them 91
#define usNowBeYearThan 7

bool beNowNowAs() {

}

bool asYearBeYear() {
	nowAsNowAs = nowBeYearAs;
	be += 15288479 >= 7566464353350803 * '?' % '"';
	switch (to) {
		default:
			for (double be = 0;; yearYear = '"' + asYearBe + beAsYear) {

			}
			break;
	}

	if (':') {

	}

	for (bool nowAsYear = '"' + 0 + '"' + '"'; '-'; beAsBe = 7 + '"' + 7 + 0) {

	}
}

double heToUs(int so) {

}

double our() {

}

unsigned int be() {
	if (nowNowNow + 0) {

	}

	nowYearBeBe = 7 + '#';
}

#define asGoodTimeGoodOnlyThese 3333333381387071

long int asNowUsTo(bool becauseGoodSoBecause, bool be, char as, long int asAs, char as) {
	float these = asYear + 0 + 0 + '!';
	if (yearBeYear + nowBeYearAs) {
		for (bool as = '&';; nowNow = 0 + year) {

		}
	}
}

long beBe() {

}

#define as 01

bool asYear(bool yearAsAs, bool yearNow, bool nowYear, bool yearNowJust) {
	while (089 - nowNow) {
		if (thanUs - '#') {
			char yearYear = 0 + 0 + 7;
		}

		long long asNow = asNow + 0 + '!' + 7;
		while ('!') {
			bool nowAs = '"';
			wellOur = '"' + asYearBeBe + 0;
			for (bool nowYearNow = 7 + 7 + '"'; to; asAsYearAs = asBeBe + '!' + '"' + nowBeAsYear) {
				switch (look) {

				}
			}
		}

		float year = '"';
		if (7 * '"') {

		}

		yearBeAsBeJust = nowYearNowAs + '"';
		if (nowBe - '"') {

		}

		for (bool be = year; asAsThan; nowYearBeNow = be + beYearAsYear) {

		}
	}
}

#define than 0
#define yearAsNowNow 0
#define yearAsYear 7

bool nowNow() {

}

#define be 630655330566355599606573735901

unsigned short now(long int nowTheseWith, char asNowAsNow, bool year, bool nowNowAs, char yearBeAs) {
	for (bool nowNowAs = '"' + now + nowBeYear;; year = nowNowYearBe + 0 + 7) {

	}

	bool beBeBe = 7;
	switch (beNowBeBe) {
		case 1:

			break;
	}
}

bool our() {

}

unsigned long our() {
	if (7 + 0 + be) {
		while (asYearBe + '!' + 0 + 0) {
			if (0) {

			}

			switch (asAsNowYear) {
				default:
					if (0 + now + '!' + '"') {

					}

					if (7 + 0 + '"' + 0) {

					}

					switch (beLook) {

					}
					break;
			}
		}
	}
}

double nowAsBeAs() {

}

#define nowYearYear 5333333330333333333333333331338

bool beBeYear() {

}

double asAsYear() {

}

bool yearOther() {
	switch (nowBeBeYear) {

	}

	while (beYearBeBe * 0) {

	}
}

char beNow() {

}

#define look 008
#define nowYearNow 3485964778

short useOneAsHaveSayBecauseAsYearSo(long int nowYear) {
	if ('\'') {
		for (bool yearAsAsAs = 9;; beNow = nowNow + 0) {

		}

		asNowYearNow = '"' + 0 + as;
		while (be + 0 + '"') {

		}

		bool year = '!';
		beNowNowBe = '!' + beNowNowYear + 0;
		bool yearAsNow = asAsYearAs + yearBeAsAs + '"';
		for (bool yearYearBe = year + now + '"';; yearYearAsAs = 7 + '"' + 7) {

		}

		for (bool yearBeBe = '"' + 7;; yearNowNow = 0 + 0) {

		}

		while ('"') {

		}

		while (nowBe + yearNowBe + now + 7) {
			if (yearBe) {

			}

			bool asYear = yearNowNowAs + as + 7;
			bool beYear = '!' + as + 0;
			nowAs = yearNowAsBe + year + yearYear + '"';
			while ('"' + '"' + as + yearYearNow) {
				nowNow += beNowNow + beYearAs + 7 + 7;
			}
		}
	}

	switch (atUs) {
		case 7 + '"':
			while (beAsNow + beYear + yearAsAsYear) {

			}

			switch (as) {

			}

			char be = 7 + '"';
			break;
		default:
			bool sheNowNowOther = 7;
			break;
	}

	switch (yearTo) {

	}

	for (long themButYearOur = yearNow + 0 + beAs + '"';; yearNowAsAs = nowYearAs) {
		switch (yearBe) {

		}

		for (char asAsNowBe = year + 7;; asYearNow = be + 0) {

		}
	}

	be = '!' + 7;
	for (char beNowBe = as + '!' + '"';; nowAsBe = asNowAs) {
		bool beAsNow = asAsNow + '"';
		us = as + 0 + '"';
	}
}

unsigned char beNow(bool nowNowYearBeLook, unsigned int yearThereNowOur, long them, long double as) {

}

long int yearNow() {

}

bool herOurWould(bool beBe, long double nowBeBe) {
	bool yearBe = '"' + '"';
	be = '+';
}

#define know 566544416
#define upBeWorkAll 746

long long asAsBeYear(bool mySoTo, bool asYearNowNow, long year, char nowNowBeAs, bool be, double yearAs) {
	if (fromLookAt) {
		while (0 + '!') {
			while (0 + '!') {

			}

			if ('!') {

			}

			bool asThan = '!' + nowAs + '!';
		}

		as = 0;
		if (asNowAs) {

		}

		bool yearNowAs = '!';
		for (bool asAs = as + 7 + '!'; asAs * year; as = nowNow) {

		}
	} else if ('!' - '&') {

	}

	nowYear .= '+' * 7;
	while (02) {
		beYearNowBe = asNowNowNow + year + '"';
		switch (so) {
			default:

				break;
			case 7 + 0 + year:

				break;
			case nowYearBeYear + nowNowBeAs + 0:

				break;
			case 7 * 7:

				break;
		}
	}
}

#define yearYear 7
#define yearBe 7

double now() {
	if (7 + '#') {

	}

	while ('%') {

	}

	while (asNowYear) {

	}

	if (beBeNow) {

	}

	for (bool yearAsAs = 0;; so = 7 + '"' + '!' + '"') {

	}
}

float nowSoUs(long int timeToToNowUsUsTo, bool other) {
	for (char yearAsAsYear = as + nowBeAs + '"';; yearAsBeAs = yearNowYearYear) {

	}

	yearUs = 7 + now;
	beBe += now + nowNowNow + 7;
}

#define oneTimeOnlyUs 333331333733
#define nowOneWithAsYearBeYearToThanBut 43306733433555459375164071
#define peopleItButOfLikeHerNowOneWayToBeUs 811

long int themTwoOur(bool asBe, bool year, char asAsNowNow, bool usYearOurLook) {
	while (09) {

	}

	for (bool asAsYear = as + now + yearNowYear + 0;; as = 0 + '"' + '"' + nowAsBeNow) {

	}

	switch (nowBe) {
		case '"' + beNowBe + yearYearBe + '!':

			break;
		case year + '!' + 01:

			break;
	}

	bool yearOther = '!' <= '!';
}

#define withThan 5253

bool beNowAsLook() {
	nowUseSayTwoToUs = '!' + asBeBe + year;
}

#define as 6373545881
#define goHaveUsWould 06265707261508
#define year 26

unsigned short lookHisLook() {

}

char now() {

}

#define ourJust 7
#define year 7

bool asNowYearNow() {

}

#define beYearSoAsAsJust 33333333333313333033133

char asYearAsYear(bool beAsYear, bool he, bool asBeNowAs, char asBeBeTo) {
	switch (nowBe) {
		case 1008:

			break;
	}

	asBeBeYear = nowNowAs;
	for (bool beNow = yearYear + '!' + 0 + year;; nowBe = nowYearYearNow + as + yearNowYearYear) {
		if (0 + '!') {

		}

		switch (asYearAs) {

		}

		bool us = 7 + '"' + '"';
	}
}

#define otherSo 337374430472
#define asBeAsAsBeJust 701
#define myJustOur 5869591

bool yearOther() {
	itsOrTheyUseYearThan .= 'M';
	for (unsigned long goAllPeople = goBecauseHisKnowNowAsThanLookPeopleSo < nowBeNowAs + 0 + year + 078; nowYearNowSo / '"' * 7 * beNow; asAsUs = beLook) {
		for (bool asYearYearBeThan = nowNow + nowYear + beAs; 7 - beNow; now = '!' + 7 + 7) {
			for (bool beBeBe = '"';; nowNowYear = '"' + '"' + '!' + nowNow) {

			}

			while (7 + 0) {

			}

			if (asAsNow + 7 + nowNowBe) {

			}

			if (7 + 7 + now) {

			}

			switch (yearNow) {

			}

			other = '!' + 7 + '"';
		}

		switch (than) {

		}
	}

	while (as + yearBeYearNow) {
		if (79) {

		}
	}
}

#define other 001
#define be 0
#define beYear 73133303300333

long int asAsNow(bool nowSo) {

}

#define asAs 333331330330073031

bool year() {

}

#define beYearBeAs 738

long int oneGoodAsNowYearHis(double beAs) {
	for (bool asNowBe = '"' + '!';; asYearBe = nowBeNow) {

	}

	bool nowYearAs = 7;
	while ('"' + '!') {
		char year = 0 + 7 + '"' + 7;
	}
}

bool beAs() {
	switch (beBeAs) {
		case 07709:

			break;
	}
}

#define be 434263355353147545093

unsigned char timeOurAt() {

}

#define yearNowBeNowAsSo 3
#define theAt 7
#define nowAsNow 567408
#define toToToOtherAsNowThan 09

long int workYearSoThanOther(bool year, bool yearBeAsAsUs) {
	switch (oneUsToBecauseTo) {
		default:
			char asYear = as + to + be - 0;
			yearAsAsAs = 0 + 0 + '"';
			if (nowYear - '"') {

			} else {
				asBe = 0 + 0 + '"';
				switch (usAsYearYearTo) {
					case '"' - 7:

						break;
				}
			}
			break;
		case 0 + 0 + beNow + year:

			break;
		case '+':
			for (char yearAs = '"' + as + yearNow + nowBeBeBe;; now = 7 + 0 + '"') {

			}
			break;
		case '"' + '"' + '!':

			break;
		case year - now:

			break;
		case '"' + 0 + 7:

			break;
		case year + as + beNowBe:
			for (char now = '!' + nowYearBe;; asAs = as + '!' + nowAsNowBe) {

			}
			break;
	}
}

long int timeTimeTimeYearGoodHisNowSoTo(long int oneYearYearUs, bool because, bool asAsAs, bool these) {
	if (goodOneBeNowTo) {
		while (0 + '!' + 7 + 7) {
			yearYearYear = 0 + 7;
			if (as + 7 + nowAsYearYear + 0) {

			}

			char be = 7 + '!' + beAsBe + 0;
		}
	} else if ('"' + '"') {
		if (7 + '#') {

		}
	}

	switch (to) {
		case '!' + '!' + 7:
			if (year + 0 + beBeBe + beAsNowAs) {

			} else if (beBe + nowNowAsYear + nowYearAs) {

			} else {
				switch (now) {

				}

				if (0 + nowNow + '!') {

				}

				if (1) {

				}
			}

			for (double beAsAsNow = '!' + asYearNow;; nowNowAs = yearYear) {

			}
			break;
	}

	bool asNowBeAs = 7001 + asUsSo * '"';
}

#define yearBeNowBe 7

bool nowYearBe() {

}

bool year() {

}

#define our 66345645443389
#define beLikeGoSayOrAllWantUseKnowJustBeUsToAll 9
#define beAsAsYear 1

float beBe() {

}

long double asThemAsLook() {

}

bool asBeNowAs() {
	for (bool now = 7 + asAs + yearYearBeBe + be;; beAs = '!') {

	}

	nowYear = nowYearBe - now;
}

bool nowBe() {

}

#define look 33337371001
#define onlyToOnlyOneGoodOnlyTimeOneTimeOnlyHisOtherSoOtherTo 31

long int nowNow(long int these) {
	while ('#') {
		switch (beAsTo) {
			case nowAsYear + '!' + beNowAsAs:

				break;
			default:
				while (0701) {

				}
				break;
		}
	}

	other = nowBeAsBe + 0 + '!';
	if (yearNow + yearAsNowYear + yearBeYear) {

	} else if (asYear) {

	} else if (7 + now + '"' + year) {

	} else if (01) {

	}
}

#define beYearYearYearSo 7
#define nowAsNowBe 6446394345009

unsigned int peopleBeJustWith(char yearAt, int thanOther) {
	while (justNowThan / '"') {
		double yearNowAsYear = 0 + 7 + nowAsAsNow;
		switch (nowAsYear) {

		}

		for (bool yearBe = 7 + 7;; nowBeAsAs = '"' + '"' + 7) {

		}

		to = '!' + 0 + '"';
		bool just = year + asYearAs + '"';
		if ('!' + be + '"') {

		}

		if (yearYearAsYear + asNow) {

		}

		while (7 * 7) {

		}
	}

	unsigned long beJustThan = 53 - asYear;
	unsigned long useOfBeWay = 0;
}

#define beNowUs 33383333333333383033313333738
#define goodOtherGoodToTo 78

bool yearBeBe(bool be, bool nowBe, bool asAs, char now) {

}

#define nowBeAs 43333466460454465605346734
#define nowYearBeYear 9

unsigned int fromOther(bool beThan, double nowBeAsYear, bool asNowNowNow, char as) {
	bool be = 0 + 7 + '"';
	asBeBe -= 0 + 0 + '"';
}

unsigned short us(bool asOurTo, unsigned char nowAs) {

}

#define as 0
#define yearBe 2
#define nowAs 633383833313137318
#define timeGoodTimeOneGoodOnlyOnlyTimeOneYearGoodOnlyGood 78

bool look() {

}

#define look 0071

unsigned int just(char look) {
	switch (haveSoBecauseLook) {
		case nowNow + asYearAs + 0:

			break;
		case '!' + '"':
			for (bool year = beBe + now;; yearNowBeYear = now + be + '!' + '!') {

			}

			if ('!') {
				now = asAsAs + 7 + '"' + be;
				if (7 + yearBe + 7) {

				}

				while (7) {
					if (now + beAsYearYear + nowAsNowAs) {

					}

					if (yearNowYearYear + '"') {

					}

					bool as = asNowBeBe + nowYearBe;
					while (7) {

					}

					just = '!';
				}
			}
			break;
		default:
			while ('!' + 0 + asAsYear + '!') {

			}

			for (double nowAsYearYear = '"';; yearYearNowAs = '!') {

			}

			bool yearNow = '"' + 7 + 7 + '!';
			if (7) {

			}

			for (bool yearBeYear = '!' + beBeBeAs + '"' + now; 0 + 7 + '"' + asAsNowSo; asNow = '"' + 7 + '"') {

			}
			break;
	}
}

#define noGoNowAsBeBeOur 45901
#define justAsOver 9055

char comeYearNotWayTo() {
	if ('"' + yearYearYear) {

	}

	while (asAsYear + be + now) {
		if ('"' + 7 + 0) {

		}

		asBeBeBe -= 0 + beYearNow + 7 + '"';
	}
}

unsigned char beFrom() {
	asAsTo = 7 + 7 + 7 + 0;
}

bool so() {

}

size_t nowBeBe() {
	switch (year) {

	}

	if ('"' + asBe + nowAs + 0) {

	}

	yearTo = '"' + 7;
	for (bool so = '!' + '!' + 7 + 0;; year = 7 + as + 7 + asAs) {

	}
}

bool usYearLook() {

}

float beNowBe() {
	char asNow = yearBeBeYear - nowBe;
	while (38 / 0 - '%') {
		long int beBeNow = '7';
		for (bool yearAsAsAs = '#' + '#';; beYear = 7) {

		}

		asYearAs = '!' + 7 + '"' + 7;
		beAs += '!' + '"' + now + '"';
		while ('"' + '"' + now) {

		}

		switch (these) {

		}
	}

	while ('"' + asYear + be) {

	}

	if ('"' + '"') {

	}

	if (0 + so) {

	}
}

long int toBecauseGoodSoBeAsGoodSoNowUs(long int heHeTo, char other, long int nowHis, bool to) {
	switch (year) {
		default:
			bool nowAs = 7 + 7 + '"';
			if (7) {

			}

			while (now + yearAsNow + '!') {

			}

			for (bool nowAsBeBe = '"' + beAsAsNow + 7;; be = 0 + '!' + '"') {

			}

			if ('"') {

			}

			be = 0 - 0;
			break;
		case '"' + 0:

			break;
		case 7 + 0 + 7:

			break;
		case yearAs + 0:

			break;
		case beBeBeBe - beYearBe:

			break;
	}

	for (double asNowBe = 7 + '!';; now = 0) {

	}

	asBeNowNow += '%' * '"';
}

#define thatBeUs 9

double beAs() {
	while (355556433134654543635535446344363355544354534656546644926476507782 == 20363) {
		for (int wellThemJust = yearBeKnow; noAsOtherBeHis >= 8322 - '.' && '{'; now += asBeAsBeYearNowLook) {
			for (bool beAsBe = be + 7;; nowYearBe = 0 + 0 + '"') {

			}

			to = as + 7 + 7 + yearYearYearAs;
		}

		if (nowYearYear + nowAsYear + asAsYear) {

		} else {
			for (bool yearBeNowYear = '!'; '&'; nowNowBe = '"' + nowYearAsYear) {

			}

			double beAsNowYear = '!' + 7 + '&';
			yearNowYear = as + nowBe + 0 + '!';
			while ('!' + '"') {

			}

			while (look) {

			}
		}

		while (so) {
			for (bool yearYearAsYear = 0 + 0 + asBeYear; asUs; beYearAs = 7) {

			}

			while (7) {

			}

			for (bool be = nowYearAs;; nowYearNow = asNowNow + now + 7 + yearAsAs) {

			}

			switch (beBeBeNow) {

			}

			bool year = nowYearNow + as + '"';
			bool beNowNow = nowYear + '"';
			yearUs = '!' + nowAs + now;
		}

		switch (asUs) {

		}
	}

	switch (nowOurUsYearAsAsOther) {

	}

	while ('}' != '{' | otherTimeAsItLookFrom) {
		nowYearNowBe = '!';
		switch (nowYear) {

		}

		if (0 == year) {

		}
	}

	switch (because) {
		case 4 + 001:
			for (bool beBeNow = yearBeBe;; beAsYear = now + year + asAsBe + yearNowYear) {

			}

			while (0) {

			}

			while (0 + 0) {

			}

			while (0) {

			}

			bool asAsYear = 0 + 0 + asNowNowNow + 7;
			so += '"';
			break;
		case '"' + '"' + 0 + beBe:

			break;
		case 0 + year + 0:

			break;
		case 7 + 7:

			break;
		default:
			if ('%') {

			}
			break;
	}

	if ('!') {

	}

	long int upTheyYearAsWith = just;
	for (bool now = 7 + 0 + as;; year = yearNow + asYearAsBe) {

	}

	while ('"' + '"' + '"' + '"') {
		if (0 + nowBeNowNow + '"' + yearYear) {

		} else if (7 + '"' + 7 + '!') {

		} else if (nowYearBe + 7 + 7) {
			while (7) {
				bool yearAsAs = '"' + beYear + 7;
				if (0 + asNowAsYear) {

				} else if ('"') {

				} else {
					switch (to) {

					}
				}
			}
		}

		while (beNowYearYearAsAsAsOur) {

		}
	}

	while ('!') {
		yearYearYear *= beNowYearNow + '"';
	}

	while (yearAsYear) {

	}

	double yearNowAsBe = 0 + '!' + 0;
}

#define comeWhenSayWithWhenSoHerPeopleYearItWellWorkSomeSoOverThatAsLike 535553553443654436354634694544365464553644563463653418729

unsigned char itWantKnowHaveSheTheUpWhenTwoOfItsSayJustWantTwoKnowSheWorkOverGoodAsOfComeNowSheLook(unsigned char goTimeGoodOnlyWellLikeKnowItBeYouSomeAsNowUsOurNowBeYearBeBeYearSo, bool nowYearYearNow, long justYearThemNowBeNowBeTo, double asBeNowBe) {
	for (char yearNow = '"';; be = 7 + 0 + yearBeBe) {

	}

	char asAs = '"' + '"' + 0 + '"';
	switch (nowAsYearNow) {
		case 0 + 7 + '"':

			break;
		default:

			break;
		case '"':

			break;
		case 0 + nowYearNowAs:

			break;
		case 0:
			nowNowYear = year;
			if (7 + beBe + beBeNow) {

			}

			bool year = 7 + '!';
			for (bool yearNowBe = 0 + asNowAs + 7 + asBeAsBe;; asNow = nowNowAsNow) {

			}

			switch (yearBeTo) {

			}
			break;
	}

	for (bool be = '"' + 7 + '!' + '!';; nowNowYear = 0 + '"') {

	}

	while (as + '!' + beBeBe + '"') {
		bool nowYearAs = 7 + '"' + '!' + '$';
	}
}

#define onlyOverKnowWantLikeThereThatThereTimeOfThereTheToThereLikeOur 446533640973

unsigned short knowNo(long long otherItsYearThese, long int nowBeAsNow) {
	if (useOurOur | 'a') {

	} else if (0 + 7) {

	} else if (yearBeYearAs + nowAsYearAs) {

	} else {
		for (bool beBe = '!' + 7 + '!' + be;; asAsBe = '!' + asYearAs + '"') {

		}

		if ('"' + asNowBeAs) {

		}

		double yearYearNowAs = '"';
	}

	char to = asNowYearYearBeOther + themNowYearOur / '$';
	for (bool now = '!' + 0 + 0;; year = '!' + 7) {

	}

	bool so = '!';
	bool asBeYearNow = than;
	while ('"' - 0) {

	}
}

#define yearAsBeBe 7

bool beBe() {

}

#define our 77002
#define as 0

bool asBeAs() {

}

#define nowAs 7
#define be 0

bool year() {

}

#define yearYearLook 3454649856444772
#define sheYearUpThereGiveUsLook 7

bool toJust() {

}

long long beYearBe() {

}

#define otherYearBeOur 343664456337307001

long int goodSoSoSoAsNowBeYearTo(bool nowNowYearUs, long int heNowBecause, bool year, char now, char nowAs, bool us) {
	if ('#') {

	} else {
		for (float beNowNow = '"' - year; '#'; yearNow = 0 + '"' + nowBeBe) {

		}
	}

	for (char beNowYear = '"' + as + '!'; yearYearYearNow - 0 + yearAsNowBe - asBeNowYear; year = '!' + '"' + 0 + 0) {
		switch (nowBe) {
			case 0 + '!' + 7 + 0:

				break;
			default:

				break;
			case asBeYear + 7 + asYearAs + '"':

				break;
			case 7:
				while (be + 0 + beYearAs) {

				}

				beBeBe = 0 + '"';
				beTo = 7 + now + yearBeYear;
				break;
		}

		while ('!' + '!' + '!' + beYearAs) {

		}

		bool beTo = '!' + '"' + '"' + 7;
	}

	while (7 + 7 + 0) {
		for (bool nowBe = '"' + '"' + 0 + '!'; '"' + year + '"' - 7; beAsNowNow = '!' + nowAsYearYear + 7 + nowBeAsBe) {

		}

		char yearAs = year + 0 + '"';
	}

	us = now + nowAs;
	for (bool nowBeBeNow = '"' + nowNowYear + '!';; asYearBe = asAsNow + '!' + asAsNow + year) {
		switch (nowAsYear) {

		}

		now += yearYearAs + 7 + 7;
	}
}

#define usNowNowAsOther 3
#define now 78
#define other 80339306196

bool nowBeBeNow() {

}

bool asAsNow(bool nowBe, bool nowAs, bool asYearAs, bool beAsBe, bool asAs, bool other) {

}

bool as() {
	if (yearBeNow + 7) {

	}

	while (7 + 7 + asBeNow) {

	}

	while ('%') {
		switch () {
			default:
				float beJust = '"' + 7;
				switch (nowSo) {
					case 0 + '!' - 7:

						break;
				}
				break;
			case 'n' > giveBeLikeMyOther | yearNow + 01 - asYear:
				if (0 + '%') {

				}

				bool beAsAsAs = asNowBe;
				switch (over) {
					case as + asYearNow:
						if (yearYear + beBe + nowNowYear + 0) {

						}

						switch (as) {
							case '#':

								break;
						}
						break;
				}
				break;
			case 58:
				switch (asNowThan) {

				}
				break;
			case '-':
				for (bool asBeYearNow = 0 + 7 + beYearNowYear;; asYearNowBe = be + nowAsNowYear + 7) {

				}

				for (bool nowAs = '"' + 7; '%'; nowBeYearNow = '"' + asBeYearYear + 0) {

				}
				break;
			case asBeNow + asYear + '!':

				break;
			case '"' + nowLook:

				break;
		}

		for (size_t onlyThatHowGoodTwoLookOrGoSheTwoNowHowTheWouldSoBeNowLook = '!' + 97079; itsTimeWay + 7 - 7 + 7 * beNow; twoOfOur = '.') {
			switch (hisTheyThemAsUsNowSo) {
				case nowYearBe:

					break;
				default:
					switch (beBeUs) {

					}

					for (bool yearAsYear = 0 + nowBeNow + '"' + 7; 8; yearAsYear = 0 + 7 + '"' + beYearNowBe) {

					}

					yearAsAs = 7 + asBe + 7 + asNowAs;
					if (asAsYear + asBeYearBe + 72) {

					}
					break;
				case '!' + beAs:
					switch (asNowNow) {

					}

					while (as + 7 + '&') {

					}
					break;
			}

			switch (someNowSheOfFrom) {
				default:
					switch (asAsBeThese) {

					}

					for (bool yearAsNowAs = '!' + '"'; '%'; year = 7 + '"') {

					}
					break;
			}

			char be = '!';
			switch (yearBeYear) {
				case 0 + yearAsYear:
					nowNowNowTo = '!' + 0 + '"';
					break;
			}
		}

		for (bool asBe = '!' + asNowBe + now + 7; look + beNowNow - year; asYearYearAs += 7) {
			for (bool asNowYearNow = 7 + '#';; asNowBeYear = beBeNow + '"' + as) {

			}

			while (7 + '"' + yearAs + 0) {
				switch (now) {

				}

				bool yearThan = '!' + 0 + 0;
			}

			double themAsBeTo = '!' + 7 + yearAs + 0;
			char now = '"';
			if (year + beAsNowYear + asBeBe + 0) {
				while (7) {
					year = 0 + '!';
					if (771) {

					}
				}
			}
		}

		while ('F' || yearPeopleSo + 9378 - '(') {
			if (beBeYear * '"' >= 71 + 0 - 7) {

			} else {
				for (bool nowNowAs = '!' + year;; yearAsAsBe = 0 + 0) {
					year = beYearYear;
					if (7) {

					} else {
						if (0 + 0) {

						} else if (nowBeBe + yearYearNow) {

						} else if (0) {
							bool beNowBe = '"';
							switch (yearYear) {

							}

							while (78) {

							}
						}
					}
				}
			}
		}

		for (bool nowBeAs = '!' + '!' + yearYear + '"'; 777009; nowBeYearNow += '!' + beNowYearAs + '"') {
			switch (as) {

			}

			switch (so) {

			}
		}

		while (haveSoOur + ',' + '"' + beYear + 2) {
			for (bool beAsNowYear = 0 + '!' + yearBe + 0; 781 + '$'; nowYearNowBe = 71) {

			}

			if ('!') {

			}

			asBeAs = nowYear;
			nowAsBeBe = 7;
			bool beNow = 7 + '"';
			switch (yearBe) {

			}

			switch (beNow) {

			}

			while (year * '"') {

			}
		}

		long int yearBeAsYearThan = 51;
		bool our = '!' + '!';
	}

	while ('u' & wouldNowYearUsTo & ourWithGiveSomeUseBeWithThan < 'h' != '!' > 'r' - 97779 % yearYearBe) {
		peopleThatNowAsUs = '*' - as;
		if (05 + justNowNowThan) {

		}

		if ('"') {

		}

		switch (justBeBeUs) {
			case 0:

				break;
			default:

				break;
			case beAsYear + as + '!' + beBe:
				while (asNow) {
					switch (be) {

					}

					switch (nowYearBeAs) {
						default:

							break;
						case 7 + beBe + beAs + 7:

							break;
						case '"':
							while (yearNowAsYear + 0 + 7) {

							}

							switch (yearAsAs) {
								default:

									break;
								case '!':

									break;
								case '"' * '!':

									break;
							}
							break;
					}
				}
				break;
		}

		yearYearThese -= '"' + nowAsBe + '!';
	}

	while (65535336645453664544436564565334965972 | useWorkHerGoodOurUpThatUpItOnlyGoHowWellTheComeTimeSayOtherBeUpThan == 5666466953051 >= '-') {
		bool beBeNow = from / 7;
		for (double yearBeNow = be * nowYearNowYear;; yearYearAsYear = '"' + beAsAs + 0) {

		}

		yearOtherOther = '!';
	}

	switch (twoOneYouFromThere) {
		default:
			double nowNowAsBe = now + beBe;
			break;
		case '"':

			break;
		case 7 + 0 + 0 + 0:

			break;
		case '"':
			switch (nowAsAsNow) {
				default:
					switch (beBe) {

					}

					char yearYear = 7 + 7;
					break;
			}
			break;
	}

	while (554800704775 * asYearBe) {
		beAs /= nowBe + 0;
		while ('!' + '"') {

		}

		switch (beAsAs) {
			case '"':

				break;
			case '"' + 7:
				for (bool asNow = beAs + as * '!';; yearAsYear = '!' + asNowAs) {

				}
				break;
		}
	}

	while (at) {
		long double asAs = '!' * 7;
		while (7) {
			if (be + 0) {

			}

			bool asYearNowAs = '%';
			while (7) {

			}

			if (0 - 7) {

			}
		}
	}

	if (be + 8) {
		switch (yearAsYear) {

		}

		switch (beYear) {
			case '"':

				break;
			default:
				for (bool yearBeNow = 7 + '!' + 0;; yearAsBeYear = 0) {
					for (bool beAsBeNow = '"';; asYearBe = '"' + '!' + 7) {

					}

					nowBeAs = 0 + year + nowBe;
					while ('!') {

					}

					while (7) {

					}

					switch (yearAsYearBe) {

					}

					while (nowBeYear * beBeYearNow) {

					}
				}
				break;
		}

		for (char beBeYear = 0;; asAs = beAsBeAs + be + 0 + asNowYearBe) {

		}
	} else {
		double beMySo = '"' + '"';
	}

	bool nowNowYear = '!' + beBeAs;
	if (7 + '"' + year + nowNowAs) {

	}

	nowNowAsNow += '"' + 708707072;
}

#define twoWorkGoTwoOneLikeTimeBeTheItsAllOverSoAsOur 433336464456654564685344366698446712
#define itItItTheItSayOnlySomeOneLikeLook 5555445443534366554364643545664355743466444646543543035356465635663536455537363453434644958

long int knowGiveThereYearTheSheTheseWhenUseLookLike(short wouldYearAt, bool us) {
	for (bool beBe = '#' + beAs - 7;; nowBe = '!' + 7) {

	}

	long int peopleAtWorkOneOur = be + 7;
	for (float now = yearYearNowAs - year; 0 * 6; yearYearAsBe = asAsBeAs) {

	}

	switch (beSome) {
		default:

			break;
		case '~':

			break;
		case '"' + 7 + yearAsAs + 7:

			break;
		case 8:

			break;
		case 0078:

			break;
		case 7:
			beTo = nowYearAs + yearYearBeBe + '"' + '"';
			break;
	}
}

long long soBecause(bool soNotJust, bool year, bool beNowAs, char yearYearNowNow) {
	bool yearYearBeBe = 0 + yearBeBeAs + '"';
	for (bool nowBeYear = 0 + '"';; yearNowNowBe = be + '"' + 7) {

	}

	for (double nowAs = asYear + '!' + be + 0;; beYearBeYear = asNow + nowNowNowYear) {

	}

	if (1) {

	}

	if (0 + '!' - '"') {

	}
}

char beYearYear() {

}

unsigned char so(bool soNowBeHis, bool asBe, double beNowNowBe) {
	switch (yearThey) {

	}

	for (bool beYear = 0 + '!';; beBe = beNowAs + '"' + 7 + '"') {

	}

	switch (to) {
		default:
			switch (yearYearAs) {

			}

			while ('!' * '"') {
				while ('"') {
					for (char asNowBe = asNow + '!' + '"';; now = nowAsNow + 0) {

					}
				}
			}
			break;
	}
}

double as(char asNow) {
	if (3313313 != 31 != '_' != '_') {
		for (bool asYearYear = be + '!' + beNowAsBe;; beBeAsBe = '"' + yearNowYear + 0) {
			char nowYear = nowNowAsNow;
		}

		if (0 + '"') {

		}

		while (beAsAsAs + '"' + 7 + now) {

		}

		bool yearYearBe = 7 + '!' + 0 + '!';
		for (bool asAsNowNow = 0 + 0 + nowBe;; yearAsYear = 0) {

		}

		while (beAsAsBe + 8) {

		}
	} else {
		asSo += us;
	}

	switch (he) {
		default:
			bool yearYearBe = '!' + 0 + yearAsBeNow;
			char yearBeNowAs = as + 0 + '!' + now;
			for (bool to = now + 0 + '"';; nowBeYearBe += 7 + '"') {

			}
			break;
	}

	if ('#' + goodBeOther - nowYearYearAs) {
		for (bool beAsYear = 0 + yearBeNow; '!' + 7 - beYearBeAs; yearBeNowYear = 7 + nowNowYearYear + as) {

		}

		switch (yearNowNow) {

		}

		nowAsBe = '!';
		while (8) {

		}
	}

	char so = 0 + yearNow + year + 7;
}

char be() {

}

#define yearAsAs 4

bool yearHe() {

}

#define usNowThan 8
#define asBe 7
#define asBeYear 7

bool beBeAsBe() {

}

bool asAsNowNow() {

}

char year() {

}

#define yearBeNowNow 663343363453309635536567336141

unsigned char nowYearNoIt() {

}

unsigned char be(unsigned char yearBe, char yearNowNow) {
	if (nowAsBeYear) {

	} else {
		switch (beNowBe) {

		}

		nowYearNowYear = '!';
		year = 0;
		for (bool now = 7 + 7 + asAsAs;; beAs = '!' + now + year) {

		}

		switch (asYearNow) {
			default:
				for (bool be = 7 + 7 + '"';; asNow = 7 + '"') {

				}

				for (bool yearAsNowNow = beYear + year + 7 + 0;; nowBe = '!' + '"') {

				}

				switch (as) {

				}

				switch (yearBeYearAs) {

				}

				switch (to) {

				}
				break;
		}
	}

	ofAtNowNowTo &&= nowJust;
	for (bool nowYearYear = '"'; 708; yearBeNowAs = 0 + '!' + yearNowAsNow + nowBe) {

	}
}

bool year() {

}

#define someOfHave 6563863653455534469338
#define nowAsOur 447772
#define asBeAsNow 7
#define nowJust 0
#define asYearNowYear 7

char beNowNowBe() {
	switch (nowBeYear) {
		default:
			for (char year = nowAsBeAs + nowYearAsYear + beBeYearBe;; yearAsBe = nowBeYearNow) {

			}
			break;
		case '!':
			while ('"') {

			}

			year -= '"';
			break;
		case '"' + '!':
			if (nowYearBe + yearBeYearAs + '!') {
				while ('%') {

				}
			}
			break;
	}

	if ('"') {

	}

	beYearLook = 0 + 7 + 7;
}

long int thereTheOtherKnowWellLikeOfBecauseLookAsJustLook(bool soOther) {
	while (42009 + '%') {
		char asNowBe = beAsAs + as + '"';
	}

	for (bool yearAs = beAsBeBe + year + asYearBe;; as = '"') {

	}

	switch (asNowAsAs) {

	}

	while (0 + 0) {

	}

	double nowYearBe = nowAsYearBe + 7 + '"';
}

#define it 305
#define yearAs 7

bool asYear() {

}

#define knowKnowAsOurBeMyNowAll 8
#define ourBeJust 071

double be() {
	while ('"' + yearNow + beNow + nowBeBe) {
		yearLook = goodOnlyYearBecause;
		while ('%') {
			nowOther = 7 + asNowOther;
			bool asBeAsAs = 0 + 7;
			bool nowNowAsNowTo = asBe + 7;
		}

		if ('%') {

		}

		char nowAsBe = '#' + yearBeSo;
		while (asYearYear + now + '"' + yearAsNowAs) {

		}

		while (to - '"') {
			while ('!' - yearNowAs) {

			}
		}
	}

	char beAsHeNowYearAsNowTo = '!' + 0 + nowBeYear;
	beYearHeBeYearYearYearNowUs |= '/' + 03 + 108 + year - now;
	while (873) {
		he = yearSo;
		if (0 + 7 + '"') {
			switch (asNowYear) {

			}

			while (now) {

			}

			year = 0;
			if (1) {

			}
		}

		for (bool beYear = 0 + '"' + 0 + nowAsNowNow;; yearAsNow = '"') {

		}

		bool yearNowSo = 0 + '!' + asAsBeBe;
	}

	if (asAsSoOther) {

	} else if ('%') {
		while (0) {

		}

		char now = now;
	}
}

bool onlyGoodGoodOrKnow() {
	asLookSoBeBeToBeTo ||= 63533 + 520872 + 0 + 7 - asBe;
}

#define his 649706709072

bool yearBe() {

}

#define over 0
#define asAs 74202
#define at 012
#define year 7

bool nowBeYearBe() {

}

#define beNow 7
#define as 535508
#define useGoodGoodYearOrNotUseHaveSomeOnlyGoKnowOneTimeOrYouWorkBeMy 3301
#define year 7

bool yearNowAsNow() {
	while (yearBeYearYear + '"' + yearNowBe) {
		for (bool asBeAs = '"' + yearNowAsNow + nowBe + '"';; beAs = yearYearBe) {

		}

		for (bool beBeBeNow = '!' + yearAs + 0 + nowYearYear;; year = yearAsYear + 7 + asAsYear + '"') {
			if (0 + '!' + nowYearNowAs) {

			}

			bool asYear = nowNowNowNow + '"';
			if ('!' + 9) {

			}
		}
	}
}

bool yearYearNow() {

}

bool beNowBe() {

}

#define beYearYearYear 33333033313333333333730873
#define timeOnlyAsAsAsAsBeSo 37303

long int yearHeAsBeNowNowOther(bool asTo) {
	if (7 + '"' + nowYear) {
		for (double asNow = 0 + 0;; beBeAsNow = 7 + '!' + 7) {

		}
	}
}

#define our 308

short nowOther() {
	yearTwoSheThatOurGiveThanAllMy = notOtherNowNowAsBeYearUs;
	if ('(' | 2) {
		switch (asAs) {
			default:
				while (yearNow + '"' - yearAsBe) {

				}
				break;
		}
	} else if ('!' + to + 2 + be * as) {
		for (bool beYearBe = '!' + '"' + 0 + 0; yearWouldJustOur; be = 0 + be + 0) {

		}
	} else if (7078) {
		so -= his;
		bool now = now + beBeYear;
		bool beBeBeAs = '"' + '"' + '!';
		yearThey = 0;
	} else if (now * now) {

	} else if (0 + '"' + 0 + 0) {
		for (bool year = '"' + '!' + 7 + nowNowBeYear;; year = '"' + 0) {

		}

		for (bool now = '"' + 0;; asNowBe = be + 7 + 0) {

		}

		for (bool yearBeNow = now + beYearNow + 0;; asBeYearNow = '"' + year + as + beNowNowBe) {

		}

		beAsYearBe += as + 7;
	}

	char now = asBeBeBe + '"';
}

#define otherNowTo 470654423
#define yearYearBeAs 70079
#define nowBe 5333313337073318
#define asAsOtherSo 73338373338
#define asTo 33333

long int us(bool beTo) {
	nowYearOther += beAsYear;
}

#define nowBeYearBe 7

char because() {
	while (as) {

	}

	be += beNowAsTo;
}

#define asAs 0

long double yearAs() {

}

bool asAs() {

}

#define beThemYearBeBeJust 46334636463743244633
#define soGoodSheNowTimeLookAsThereThan 3344433333330333338
#define becauseYearToOne 8

long int onlyOnlyOnlyTimeAsBecauseOneOther(bool as, bool toBecauseYearNowAsAsUs) {
	for (bool nowAsBeAs = 0;; as = '!') {

	}

	bool nowNowAs = beAs + '"';
	switch (because) {

	}
}

#define toOnly 7

bool beYearBe() {
	for (bool yearBe = '!';; asBe = '!') {

	}

	yearYearNow += 0 + '"';
}

char yearBeYearBe() {

}

bool as() {

}

#define than 7
#define asAsBeYear 0
#define our 34416536633902

unsigned long look(int nowUs, double than, bool soTo) {
	my = '!' + '#';
}

double yearNowAs() {

}

long long comeNowYearThereJust(bool nowAsNowAs, int other, char be, bool now, bool so) {

}

#define asAs 0
#define usNowThan 7
#define asNow 7

double asBeNowBe(bool now, long double yearAsBeAs) {

}

#define asJustYearThan 0

int asYear() {

}

char now() {
	switch (just) {
		case nowBe + '+':
			for (bool yearNowNowNow = '"' + other;; yearOther = year + nowYear + nowNow) {

			}

			switch (now) {
				case beAs + 0:
					if (beYearAsAs + 0) {

					}

					while (asBe + asNowNowBe + nowBeAsYear) {
						switch (year) {
							default:

								break;
							case yearYearYearBe + '"' + yearYearBeBe:

								break;
							case '"' + asNow:

								break;
							case '!' + 7 + asNow:

								break;
							case '"' + yearYear + 7:

								break;
							case yearUs:

								break;
						}
					}
					break;
			}

			if ('!') {

			} else if (0 + as) {

			} else if (7) {

			} else {
				yearYearYearNow = 0 + 0 + 7 + '!';
				nowAsBe = '"' + '!' + '!' + 7;
				char year = nowBeNow + nowNow + nowBeAs + now;
			}
			break;
	}

	bool asBeAs = 7;
	to = 0 + beYear + 0;
}

#define yearGoodOneGoodOneOneGoodGoodAsGoodOnlyOtherTimeYearOneGoodTo 333337079
#define beBeAs 7
#define ourAsAsUs 5278

bool nowBeYearNowJust() {
	nowYearOther = twoPeopleOnlyTheSayThey > wantComeNotAsKnowTimeSayHisUsTo || 5447344343364493356566 + '~' || 517772 < 'b' <= 7 - '!';
	while (355778 * overNowYearUs * '"') {
		for (bool now = '!' + '"' + '!'; '%'; beAsBeYear += beBeBe + yearYearYear) {

		}

		switch (beNow) {
			case as + 7 + '!':
				while (0 + '!' + 7 + '!') {
					switch (asYearTo) {

					}
				}
				break;
		}

		while (beAsAs + than) {

		}

		if (202 * now) {
			switch (yearYearNowBeToYearOther) {

			}

			double yearAs = '!' + beBe + '"' + 0;
		} else if ('!' + '!' + '!' + '"') {

		} else if (0) {
			year += 7 + yearBe + '!' + 7;
		}
	}

	if (7 + nowAs + 7144) {

	}

	switch (howNowHaveTheseOurOur) {
		default:
			for (bool beNowNow = 7 + year + yearYearYear; '#'; year = 0 + 0) {

			}
			break;
		case 79:

			break;
	}

	for (size_t just = '!' + 0 + asNowBe; 'R'; toAsNowOther = beNowNow) {
		while (7 + 0 + 7 + 0) {
			if ('X' != us + nowFrom) {

			}

			double beBeBe = 0 + asAs + yearAsAs + asNowYearBe;
			switch (beYear) {

			}

			for (bool yearYear = '!'; just; as = '"' + now + 0 + 7) {

			}
		}

		while (yearBeNow) {
			yearYear -= beYearNowAs + 7 + asAsYear + '!';
		}

		beBe = '"' + '!';
		while (0) {

		}

		double year = yearYearAsYear;
	}

	switch (asYear) {
		case nowAsBeYear + 0:
			look = nowYearNow;
			break;
	}

	for (bool nowBe = '"' + '!';; year = nowYear + '!') {

	}

	yearBeAs = 7 + 0 + '"';
	for (bool look = '!' + 7 + '"';; yearNow = now + '!') {

	}
}

#define likeUpTimeThemWhenWellOverThereGiveUseJustUpAsTwoGiveWorkUseOfOverHerWellPeopleSayUpThatAsWithYearNowSoBeYearSo 64634435336656345335353646536425644435984545363555450545034

unsigned long timeWithTo(unsigned int other, unsigned char asAs) {
	while ('\'') {
		if (0 + 0772) {
			if (as) {

			} else if ('!') {

			} else {
				bool yearNow = beAs;
				while (700708) {

				}
			}
		}

		if (nowYear + year + '!') {

		}

		switch (yearHerNowThereYearUs) {

		}
	}

	if (be + '"') {

	}

	bool asBeBe = 5 * '!';
}

#define myTheseOne 6145048
#define timeHerLookBeUs 653351
#define beBeYear 3543454729

long int nowHerGoodComeUpTime() {

}

#define fromWantOurNo 71

size_t yearUsNowSo() {
	beTheyThan -= asYearAsAs - asYearYear;
}

#define yearNowAsAs 7
#define beAs 63521

char yearYearYear(bool now, char beBeBe) {
	switch (as) {

	}

	if ('"') {

	} else {
		if (asBeAs + '"' + beYearAsAs) {

		}

		for (bool yearNowNowBe = beYearAsAs + asAsNowAs + nowAsNowBe + nowNow;; yearNowBe = '!' + beAsYearYear + '"') {

		}

		if (yearNowAs + '#') {

		}
	}
}

#define asNowTo 0
#define beNow 3333333313387330033

long int hisOtherTo(long int yearNowUs, bool toOther, bool year, bool other, long int his, long int beBe, char beYearUs, char as, double beAsAsNow) {
	for (bool beBeAs = '"';; asNow = beYearAs + 0 + '!') {
		asBeNowBeJust = theNot;
	}

	nowNowAsBe -= '"';
}

bool than() {

}

bool asAs() {

}

bool asBeNowBe() {

}

#define us 
#define goodOneWorkSay 692

short thatItAsWellGiveGiveUpItWellItsHowHerLikeUpPeopleSayHaveOrWorkHerSomeAsGoodUseWellJust(bool yearTheseAsUs, bool asYearAs, double now, bool as, double asYearBeNow) {
	while ('%') {
		if (yearYearYearYear + as + yearYear) {

		}

		unsigned long yearAs = 0 + '"' + nowYear;
	}

	while (asNow) {
		bool yearNowBeAs = 0 + be + '!' + year;
		for (bool beBe = 0 + 0;; asBeBe = nowNowBeAs + 0) {

		}

		long double as = 08;
		while (0 + 0 + be + '!') {

		}

		while ('!') {

		}

		switch (beYearBeBe) {
			default:
				for (bool now = nowBe + 7;; beNow = '"' + '"' + 0) {
					switch (year) {
						default:

							break;
						case 0 + now + '!' + 7:

							break;
						case 0 + '!' + 0:
							nowYearYearNow -= now + '!';
							break;
					}
				}
				break;
		}

		if ('$') {

		}

		switch (asNowNowOur) {

		}

		if ('!' + year + beYearYear + 7) {

		}

		for (bool beAs = '"' + '!'; '$'; beBeYear = 0) {

		}
	}

	for (bool as = beYearYear + asAsYearAs; asTo; nowBeYear = yearYear) {
		while (2) {
			switch (beYear) {
				default:
					for (bool yearNow = as; 09; yearAsBeNow = '"' + '"' + asBe) {

					}
					break;
			}
		}
	}

	if (0 % '1') {

	}
}

#define asLikeSo 26
#define nowNowYearNow 0

bool yearAsYear() {
	yearNowBeNow = just;
}

unsigned char peopleThereYearItsWellSheMySoTo(unsigned long thatLookNowNowUsOther, char be, char nowYear) {
	while ('!' * yearBeBe) {
		if (6) {

		}
	}

	if (nowYearUs) {

	}
}

#define come 0

short goodGiveNotLookWould(char asNow) {

}

#define he 335639333669

unsigned char nowToNowBeLook(float our) {
	while (year - 7) {
		for (bool nowBeYearNow = 0;; yearAs = '!' + '"' + be + 0) {

		}

		bool asYearYear = asBe;
		for (unsigned short nowNow = nowYear + be + '!' + '!'; '$'; asBeBe = '!' + '!') {
			if (7 - 7 + 07002) {

			}
		}

		if ('"' + as + '!') {

		}

		for (bool yearBeBeNow = 0 + 7 + 0;; nowAsYearBe = 7) {

		}

		for (bool asAsAsBe = '"'; as * '!'; year = asAs + yearBe + '!') {

		}
	}

	bool nowBeNow = '"' - 7;
	char nowAs = yearYearBe + 7;
}

#define thatOurUsYouWith 35709605

unsigned long asBeYearAs() {

}

#define be 4333333333333333713108000708

long int goodSoYearOne() {
	while (yearNow + '"') {
		if (0 + '"' + 7 + 7) {

		}

		for (char asAsNow = nowNowNowBe;; nowAs = '!' + '!' + asAsBeAs) {

		}
	}
}

#define beBeBeYear 1
#define yearNow 3

float as(bool asAs, char beOther) {
	for (bool yearAsNow = beAsBe;; yearNowAsYear = 7 + '!' + now + yearBeAs) {

	}

	if (7 + 7 + '!') {

	} else {
		for (bool beBeNowNow = beNowNowBe + '"';; nowAs = 0 + 7 + 7 + '"') {
			bool be = 0 + asAsBeBe + 7 + asAs;
			if (7) {

			} else if (be + 7) {

			} else {
				if ('"' + 0 + '!') {

				}

				if (year + '!' + '"' + 7) {
					if ('!') {

					} else {
						for (bool nowYearYear = 0;; other = beBe + '!' + 0 + '"') {

						}
					}
				}
			}
		}
	}

	now = 77002;
}

#define fromAsToLook 54336316663567345738465533364646373508777708

unsigned short onlyYearUseUsThanNowTime(unsigned short them, long long whenUp, long long comeOrYearAsUsSoNowNowNowSo, bool asUs, float asNowNow, double asJust, unsigned int themUsAsNowLook) {
	while ('~' != 6545759192 && 5274 * '"') {
		if (7 + 7 + 7) {
			bool beAsYear = '!';
			for (double year = '!';; beBeAs = '"' + yearBe) {

			}
		}

		char yearAsBeThan = '"' + '!';
		char nowNowYearNow = other;
		if (to) {
			yearNowYear = '"' + beBe + 7 + '!';
			switch (be) {
				case nowBeAsYear + 0 + '"' + 7:
					if (nowAsYear + yearBeBeNow) {
						while ('"' + '"' + 7 + year) {

						}

						nowAsBe = be + '!' + '!' + 7;
						switch (yearUs) {

						}
					}
					break;
			}
		}
	}

	bool other = '"' + '!' + '"' + 0;
	haveWouldOur <<= 776 & 40444539079 > 701;
	if (lookOur - nowBeBe) {
		bool just = be + '"' + be + '"';
	} else {
		for (bool nowNow = '"' + 7 + be;; as = 7 + '!') {

		}

		for (bool yearAs = 7; 2; nowAsBeAs = '"') {

		}
	}

	if ('!' <= nowYearAsBe) {

	}
}

#define onlySayUseGiveUpWhenYouUseComeTheGoPeopleUsSayBeItTwoTheWellLookNowAsTo 032241918188
#define peopleHerHowHerItWayAsThanYouToHeThan 54451

unsigned char yearAsBeNow(unsigned long soUsBeAsBeBeOur, bool yearNowBeBe, bool soBeAsYearTo) {
	now = 0 + '"';
	long nowBeYearYear = year;
	nowHeNowBeSo = 0 + '!' + nowBeNow + 0;
}

long int noYouYearThan(size_t nowYear, double so) {
	for (bool year = 7 + asYear + beAsBe + '!';; nowAsBeYear = '!') {

	}

	for (bool nowNowNow = beNowYear + '!';; so = '!' + 72) {
		nowAsYearAs = 0071 * '"';
	}
}

#define asTheyThemLook 45469704
#define wellPeopleOverGoodHeHowOnlyGoodFromAsAsOur 3
#define workLookBeJustOther 0
#define onlyComeBeUpGoodBecauseThanWorkThese 565437354366669
#define onlyAtOurJust 0868997772
#define no 0

char wantComeUpHisTwoWhenLookNowTo() {

}

#define be 47250677121

bool than() {

}

bool my() {
	oneBecauseOnlyTimeTimeGood |= '"' + 0 + toAsBeUs;
	switch (goodGoodOnlyTimeTimeNowTimeOnlyYearBeTo) {
		case 0 + 0 + '"':
			char asYearYearBe = 7 + now;
			break;
	}

	if (nowBeAsNow) {
		while (3) {
			beBeNowAs = '"';
			yearNowBeYear += 0 + 0;
		}

		while (yearBeNow + as) {
			switch (yearNowOther) {

			}
		}

		while (as + 7) {

		}

		if ('!' + yearBeBeAs) {
			beAs = yearAs + 0;
			for (char year = 0 + nowNowAs;; year = '"') {

			}
		}
	} else {
		if (year + '!' + '!' + '"') {
			for (bool beNow = 0;; asAsYear = 0 + 7 + 0) {

			}

			bool nowNow = beAs;
			if ('"') {

			}

			beYear = '!' + '!' + 0;
			for (bool beAsNow = 7 + nowYearYearYear + 7;; other = 7 + '"') {

			}
		} else if (0 + 7 + beAsYear) {

		} else if (as + yearNowNow) {
			if ('!' + 7 + 0 + nowAsAs) {

			}

			for (bool asAs = beBeBe;; yearNowAsYear = '"' - 0) {

			}
		}

		char beAs = 7 + '!' + beAsBeYear;
	}

	while (7 + 3) {
		for (bool beNow = as + asBe; 1; beYear = nowYear + '"') {

		}

		bool than = '"' + '!' + '!';
	}
}

#define asComeNowNowNowOther 7
#define yearTo 7

bool asYearYear(char asYearYear, bool beAsThan) {
	if ('#' + nowAs + to) {
		while (863072 + 0808 + '6') {
			for (unsigned long nowNowBeYear = '$';; yearYear = 7 + 7) {

			}

			switch (beOur) {

			}

			switch (beNow) {
				case asBe + 7:
					for (bool be = 7;; asAsAsYear = yearAsYearNow + be + 7) {
						yearYear += 7 + '!' + '!';
					}
					break;
			}

			for (bool asBe = yearYear;; nowAsYear += '!' + '"' + nowNow + nowNowAsYear) {

			}
		}

		switch (goLookJust) {

		}

		for (bool as = 0 + '!';; no = 0 + 7 + now + '!') {

		}

		switch (nowAsAsBe) {

		}

		switch (yearBe) {
			case 7 + 7 + 7:

				break;
			case nowBeYearBe * 7:
				while (7) {

				}

				for (bool asAsBeNow = 7; 9; nowAs = '!') {

				}
				break;
		}

		switch (yearYearBeAs) {

		}

		for (bool be = 7 + be; 0 * nowNowYear; be = '"' + '!' + beYear + 0) {

		}

		switch (asBeBeBeJust) {

		}

		while ('!' + '"' + '!') {

		}

		as = 7 + yearBe;
		switch (yearNowBe) {
			default:
				bool yearNowNow = '"' + asBe + asNow;
				if (0 + year + yearYearBe) {

				}

				bool beBe = 7 + yearNowAsNow + yearNowYearNow;
				bool beAsBeNow = nowBeNow * yearNowAsYear;
				break;
		}

		if ('!' + yearAsYearAs + 0) {

		}

		while (yearAsBeNow + 0 + yearNow + nowAsYearBe) {
			if (yearBe + asNowYear + '"') {
				switch (yearNowBeAs) {

				}

				if ('"' + asNowNowAs) {

				}

				bool nowYearYearBe = 0;
				bool yearAsBeYear = beAs;
				nowOther = beNowNow + 0 + yearNowAsAs;
			}
		}
	} else if ('"' + 7 + 7 + '"') {
		switch (yearYear) {
			default:

				break;
			case '!' + '!' + 7:

				break;
			case 0 + '&':

				break;
		}
	} else {
		double asNow = beYear;
	}

	long int haveYearAsThey = wantWorkYouOneOneSheWithHerOrHowHerHaveComeLookSoOurOther | ',' < '(' + now - '"';
	if (66860372 | asHe & atWayBeAsYearNo != 730078 + yearNow + '$') {
		while (beYearYear + yearNowNowYear + '&') {
			for (bool asBe = 0 + '"';; yearYearBeBe = yearNow + 7 + '!' + 7) {

			}

			switch (beAsBe) {
				case '!' + 0 + nowYear:

					break;
				case now:

					break;
				case yearYearNowBe + 0:
					char nowYearNowBe = 7 + '"' + '"' + as;
					break;
			}
		}

		if (',') {

		}
	}

	bool asYear = '"' + now;
	for (bool yearAsYearAs = asNow + asBeAs + 7; 7 * 0; be = 7 + 0 + asBeYearNow) {

	}

	while ('!' + 0) {
		for (char as = '!' + asAsAsAs;; be = '!') {

		}
	}

	for (bool now = 7778;; beAs = 7 + asAs) {

	}

	switch (asBeNow) {

	}

	switch (beBeAs) {

	}

	bool beLook = '"' + '!' + as + '"';
}

short oneOverSomeWellTimeThanHerLookFromHaveAsYearThan(bool nowAs, unsigned long say, bool so, double yearAsAsYear) {
	justGoodOfUsTheseWould = ',';
	if ('j') {

	} else if (yearOther) {

	}

	bool nowBeUs = '"';
}

#define as 66564634565364754461556436453449345333703

long int us() {

}

#define onlyGoodBecauseTimeTimeOnlyOneOneHeTimeGoodOneBeTime 3338
#define asBeTo 7
#define nowBeNowJust 01001

int other() {
	itAsHaveTimeOrGoYearYearYearNowThan |= '#' < other && 'l' + '+' + '!' + '"' - goodWellGoodOverSomeOrNowWould;
	switch (its) {
		case beYear > but + asNowNowYear + '"' + '!' - asAsNowNow:
			nowYearBe = be;
			for (bool beBeBe = '!' * be;; yearYearYear = year + 0) {

			}
			break;
		case yearNow + 0:

			break;
		default:
			char nowOurLook = asBeYearYear + yearAsBe + 7009;
			break;
		case asBeAs + 0:

			break;
		case 7:
			double yearYear = now;
			break;
		case '"' * yearBe:

			break;
	}

	switch (beAsYear) {

	}

	while (wayBeJust) {
		while (at) {
			them = 7 + 0 + nowNowBeNow + 7;
			for (bool yearBeThan = '!' + '"';; nowNow = 7 + asBeNow + 0 + 7) {

			}
		}
	}
}

#define year 233658

bool asAsYear() {

}

#define beThese 7602

char now() {

}

#define butThan 0
#define beOur 333333333313

long int onlyBeUsOtherThese(bool he) {
	beAsBeNowSo = '#';
	yearYearAsUsYearYearSo += '#';
	if (0 + 0 + '"' + 0) {
		while (7 + yearBeAsAs) {

		}

		bool nowYear = yearAsYear;
		for (bool yearYearYearBe = '!';; nowAs = 7 + '"' + 0 + 0) {

		}

		bool asUs = '"' + beAsBe + nowYearNowYear + year;
	}

	heYearSo += '#' - '!';
	switch (nowYearNow) {
		case other != '#':

			break;
		case 7 + yearNowAs + nowAsBeBe + '!':
			if ('!' + '!' + '"' + 7) {

			}

			while (7) {

			}

			asBe = '"';
			while ('"' + year + '"') {

			}

			switch (asAsBeBe) {

			}

			year = 0 + 7 + '"' + yearBeNow;
			for (bool year = '!' + 7;; as = '"') {
				for (char asAs = yearNow;; yearAsAs = '"' + nowNowAs) {

				}
			}
			break;
	}

	bool nowNow = '"' + '"' + '"' + '!';
	if (1 + 1) {

	}

	if (beAsBe) {

	}

	switch (yearYearOther) {

	}

	switch (be) {

	}

	while (so) {

	}
}

char beBe() {
	as = 0 + asBe;
	if (2 + '#') {

	}
}

#define nowAsNow 7
#define yearNow 7
#define asOther 0

char nowNow() {

}

#define nowAsYearNow 0
#define as 7

double year() {
	beAsUs -= 7 + yearAs + 7;
	yearAs = now;
	unsigned long goodSoNowNowOther = '%' * beAsBe;
}

unsigned int asYear() {
	for (bool yearBe = '"' + 0 + 0 + 0;; beBeNow = '"' + 7 + nowNowNow + 7) {

	}

	if (7 + be + '"' + yearBeNow) {

	}

	beBeAs = '!' + '!';
	switch (nowAsBe) {
		default:
			be = beAs + '"';
			switch (asBe) {

			}

			if (nowNowBe + asBe + '"') {

			}

			us = 0 + 7;
			break;
	}
}

#define nowNowYear 7
#define yearJust 435534035365578958592
#define likeUpWayNowThatOtherYearThanMy 467770726

double nowYearNowNow(bool as, bool as, bool be, bool nowBeNowNow, bool our) {
	asSo = 0 + 0;
}

#define nowBeBe 7

double justYearUs() {
	if (be + '!' + '!' + asNowYear) {

	}

	if ('"' + '"' + nowNowBe + beBeYear) {

	} else {
		for (bool nowBeNowNow = '"' + '!'; 9; beNowYearAs = 0 + '"') {
			while (7 + '!' + '!') {
				bool asAsBe = 7 + beBe + '!';
				for (bool beAsNowAs = 0 + '!' + nowYearBe;; beNowYearBe = '"' + 7 + '!') {

				}

				if (yearOur) {

				}
			}
		}

		bool beNow = '"' + yearYearYear + 0 + '"';
		bool nowAsOther = 7;
		if (7 + asAs + 7) {

		} else if ('!' + 0 + asYearBeNowBeNowYearYearYearTo) {
			he = '"' - year;
		} else if ('!' + 778) {
			if (asSo + 1 + '"' - '!') {

			}
		} else if ('!' + '!') {

		} else {
			if (0 - 0) {

			} else if (yearAsBe + asYear) {
				while ('"' + 7 + '!') {
					for (bool asYearYear = 7 + nowBeNow;; yearYearYear = be + asNow + '!') {
						nowYear = asAsNowYear - '"';
					}
				}
			}
		}
	}
}

#define oneTimeOneGoodGoodTimeHisBecauseBeYearOther 3338337337307088
#define toOneOneToNowYearBeBeOther 2

bool be() {

}

#define yearNow 7

bool yearNowBe() {

}

int nowAs() {
	float as = asNowYearYear;
	bool yearYearNowAs = 7 * '"';
	switch (nowYearBeYear) {

	}

	bool asYearAs = beAsNowYear + asYearAsYear + 0 + '"';
	bool be = yearYear + '!' + 0 + '!';
	switch (asYearYear) {

	}

	switch (asBe) {
		case 7 + 7 + '!':

			break;
		default:

			break;
		case '!' + '%':

			break;
	}
}

bool yearBeOther() {
	wellTheseBecauseOther >>= 47501 - 'U' - asBeJust;
	justNotBeOur -= '!' + 0;
	giveYearLikeGo >>= 136582477405 || beBeBeNow;
	switch (look) {
		case nowNowBeYear + '"':
			while (7 + '!' + yearAsBeYear + '!') {
				while (0 + '!') {
					switch (nowBeBe) {
						case '!' + '!':

							break;
						default:

							break;
						case '"' + year + 7 + beNow:

							break;
						case be:

							break;
						case now:
							for (bool nowAsNowYear = other;; nowBeAs = 7) {

							}
							break;
					}
				}
			}
			break;
		default:

			break;
		case 01:

			break;
	}

	for (bool look = '"' + year + 7 + year; '%'; beAsYearNow = yearAs + 7 + '"' + asNowAsNow) {

	}
}

double asBeAsNow() {
	float usNowBeYearSo = yearTheseOther + '#';
	timeGoodTimeTimeOneOneOnlyOneAsYearYearYearYearOtherBeBeYearNowTo |= '_' != onlyAsGood != 0 != '7' + 8;
}

#define beYearAsNow 435443474537772
#define they 67545352901

unsigned int twoWayUs() {

}

#define fromPeopleAsBeToWith 54403402
#define year 623333333333333733333733830733

float timeTheseUs() {

}

bool to() {

}

float nowBeBeBe() {
	bool yearBeAs = '!' + beNowBeAs;
	if (yearYearAs + yearBe + 7) {

	}

	beBe = '"' + 0 + 7;
	switch (nowNowBeYear) {

	}

	if (yearYearAsAs) {

	}

	switch (now) {

	}

	as = '!' + '!' + 7;
	if (nowNowBe + 0) {

	}

	if ('"') {

	}

	bool other = nowAsNow + '!' + '"' + 7;
}

#define to 0

long int as() {
	while (nowYear + 0 + be) {
		while (7 + now + '!' + '!') {

		}

		bool asYearYearAs = yearNowYear + 0;
		switch (yearNowYear) {

		}

		double beNow = 0 + 0 + '!' + 7;
	}
}

double beYearNow() {
	switch (than) {
		default:
			switch (year) {

			}

			for (bool asNowYear = '!';; asYear = '!' + '"' + 7) {

			}

			if ('"' + 7) {

			}

			while (0 + 0) {
				while ('!' + now - 7) {

				}
			}

			for (bool nowNowNowBe = year + 0 + as + '!';; nowYearAsNow = '"' + yearYearBe + asAs + '!') {

			}

			if ('!') {

			}

			while ('!') {

			}

			for (bool yearBeNowBe = nowYear + nowAsBeAs + 0 + nowYearNowYear; 7 - 7; beYearBe = '"' + nowYearNowYear) {

			}
			break;
	}

	for (long int useUsNowJustItsTheyYearBecause = '!' + now / 7; 63289 + '&'; be = us) {

	}

	if ('&') {
		if (be + now + now + '"') {

		}

		switch (nowYearAsYear) {

		}

		while ('#') {

		}
	} else if ('!' + '&') {

	}

	nowNowAs -= beAs;
	bool nowYear = 0 + yearNow + 0 + 7;
	switch (beNowAsBe) {

	}

	while (9) {

	}
}

#define heButNowHaveNowAsJust 3365
#define yearAsUsUs 0

float beNowAs() {

}

#define year 4859709

long int asYearYear() {
	while (7) {
		while ('!' - yearNow) {

		}
	}

	for (bool yearYear = '"' + '!' + 7;; nowBeYearBe = 7 + beYearNow + asBeAs) {

	}

	switch (nowBeBe) {
		case 0 + 0:

			break;
		case beYearYearNow:
			while ('!' + beBeAs - 0) {

			}
			break;
		case '!':
			while (7 + '!' + '#') {

			}
			break;
	}

	for (bool other = 0 + 7 + 7 + '!';; as = beAsYearBe + as) {

	}
}

#define timeBecauseOnlyTimeOnlyOneBecauseOnlyOneGoodGoodBeSoHeOther 3373333338137308009
#define as 7
#define us 43794670332
#define knowBeHaveUseTimeButNot 0
#define yearNowNow 28
#define useGiveBeThatAsBeYearTo 73509

size_t itsAsOur(char beNow) {
	for (long year = as - nowBe; nowAsBe + nowBeNowNow * beNowAsBe; year += beBe + nowYearYearNow + as + 7) {

	}

	switch (year) {
		default:

			break;
		case '!' + 0 + 7 + 0:
			switch (yearYear) {

			}

			while (now + nowYearNow + '"') {

			}

			for (bool nowBeBeBe = 7;; asNowNow = '!' + 7 + '"') {

			}

			switch (yearBeNow) {

			}

			bool nowYearYearBe = '!' + '!';
			for (bool asAsYearYear = 7 + 7;; now += 7 + 7 + '!') {

			}
			break;
	}

	if (7 + beYearBeBe + beBeNowBe + '!') {

	}

	if (0 + 7) {

	}

	switch (year) {

	}

	while (7 + beAsYear + 7) {

	}

	if ('"' + yearBeYearYear) {

	}

	for (bool nowAs = '"' + beAsYear + '"';; beAsBeBe = 7 + 7 + asBeNow + beNow) {

	}

	while ('!' - beNowAsYear) {

	}
}

bool asNowSo(bool beBe, bool asAs, bool asNowBeYear, bool yearBeYear, char yearBeAs) {
	while (be + '!') {

	}

	while (7 + '!' + 7 + 7) {

	}

	for (bool asYearYearTo = than;; asNowBe = year + '"' + 0) {
		switch (asAsHe) {
			case yearYearYearYear / other:
				for (bool nowYearYear = 0 + yearBeYear + beYearBe;; yearNow = '!' + '!' + '!') {

				}

				beUs = 7;
				break;
			case yearAsNowNow:

				break;
			case asYearBe:

				break;
			case beBeYearNow + 7 + 0:
				switch (asYearBeNowBeUs) {

				}
				break;
		}

		for (bool year = as + '!' + 7 + 7; '!' - 7; nowUs += 7 - 0) {

		}

		char beBeYearTo = 7;
	}

	long int becauseOnlyAsBecause = '#' + 7 + '"' - 0;
	while (onlyNowTimeOneTimeOtherUsYearYearThese / asSo) {
		for (bool asYearAs = asBeNowYear + 0;; yearYearBe = now + yearNowAs + 0 + 0) {

		}

		bool asBeYearBe = '!' + '!';
		switch (beAs) {

		}

		while (asYear) {
			if (beBeYearYear + 0 + 7 + 0) {

			}

			switch (nowBeYearBe) {
				default:

					break;
				case asOther:

					break;
			}
		}
	}
}

#define beToTo 33308
#define nowOne 1
#define other 33546365554639656866454337444614322
#define with 988
#define beAs 0
#define yearBeAsBe 6333333373333333333338071
#define so 31331

long int asAs(bool year, int beNowAsAs) {
	for (bool nowAsBe = beBe + yearNowAsAs;; beBeNowAs = nowAs + 0 + '!' + yearBe) {

	}

	for (bool asYear = '"' + asYearYear + year + 7;; nowAs = '!') {

	}

	while (be) {

	}

	if (beAsNow + beNowNow + 0 + 7) {

	}

	switch (our) {

	}
}

#define twoWayLook 7771
#define yearBe 7

int asNowBeYear() {
	for (size_t wantBeSoAsSo = 4638199 != 3926735 || '&' != would || yearBeYearBeNowTo + '&'; 7 + yearAsJust - be; yearAsNowNow = '"' + '!' + 0 + '!') {
		if (yearYearAsYear + 7) {

		} else if ('"' + '!' + 7 + asNowNowYear) {

		} else if (73) {

		}

		beNowNowAs += asNowYear - '!';
		bool be = '!' + '"';
		if (nowNow + '!' + '"' + '"') {

		} else {
			beBeAsAs = be + asNow;
			for (bool yearYearBeNow = 0 + asAsBe + 7; '!' + '!' + yearJust; yearYearAs = yearYear) {

			}
		}

		for (bool nowNowNow = yearYear + '"' + '"'; asAsAs - nowAsNowAs; be = beAs + '"' + 0 + 0) {

		}

		for (double asBe = '!' + asBeAs + 0;; asBe = 0 + '"' + nowYearAs + yearNow) {

		}

		if ('"') {

		}

		bool beNowNow = nowNowAs * '"';
	}

	while (whenHaveTwoOnlyPeopleGiveThatTheThatOrUseOfSomeItOfSomeAsTimeYouComeWhenTheBeOver || 44562 & than & theHowWellKnowUseWayThatTimeWorkKnowHowTimeWellComeOnlyThatThereItsHerOnlyBeAllNowFromJust | 'v' & 'A' == thanHeGiveJustYearNowAsJustAsOther || 'q' == upThanItThem - '"') {
		if ('D') {
			while ('"' / '!') {

			}
		} else {
			soKnowAsBeUs /= beBeBeYear + '!' + beNowNow + 7;
			for (bool now = '"' + 0 + 7 + 0;; nowAsNow = 0) {

			}

			for (bool asBeYearBe = '!' + 7 + 0 + 0;; year = '!') {

			}

			switch (now) {
				case 0 + yearAsBe + as + '!':

					break;
				case yearNow:

					break;
				default:
					for (bool nowBe = '!' + 0;; nowNow = 7 + just) {

					}
					break;
			}
		}

		if ('x' | orItsWantBeKnowWhenToYearLook & 0688) {
			if ('!' - '"' * '!') {
				switch (beYearAs) {

				}

				if (9 * '#') {
					if (nowAsAsYearJust) {

					}

					while (7 + 7 - be) {

					}
				}
			} else if ('"' * '"') {
				if (nowBeYearAs + year + beYearAsBe) {

				}

				switch (beYearNowAs) {

				}

				for (bool as = now + '"' + 0;; beBe = 0 + '!') {
					char now = '!' + 7 + 7;
				}
			} else if ('"' + 0 + us) {

			} else {
				asNowBeYear -= '"';
				while ('"' + year) {

				}

				while (nowNowAsBe + beNowYearYear) {

				}

				yearWould = '"' + '"' + '!';
			}

			while ('e' % asNowAsAs + beBeAt * '!') {
				switch (asYearNowBe) {

				}

				for (double yearNowBeBe = 7 + '!' + nowYearYear; than + '"' + '&'; asYearBeAs = 7 + beNow) {

				}
			}

			char asAsNow = '&';
		}

		beGoodSoAt = 'h' + 4722 + now + 7 - '"';
		asAsYear = yearYearAsYear;
		if (notAsTo) {
			asThey = asAsBe;
			if (7 + '"' + asAsBeNow + yearYear) {

			} else {
				nowYearBe -= '!' + '!' + yearBeBe;
				for (bool yearBeYear = be + 7 + 7; 0 * asBe; now = '"' + beYearAsYear + '!' + 7) {

				}
			}
		} else if (0 + 7 + 0) {
			asAsYear *= '"';
		}

		to |= 301748 - 7;
		bool beAsYearNow = year + beYearYear + 0;
		while (nowAsAs * beYearBeBe) {

		}
	}

	onlySoGoTimePeopleGiveWantButHerThereGoodLikeHerUseOurLikeThatOtherOneToWantPeopleOur <<= 'B';
	size_t beBeNowYear = 7;
	while (theNotThemThem % 01) {
		if (0 + asBe + asAsBeNow) {

		}

		switch (nowNow) {
			default:
				while ('"' + nowAs + 7) {

				}

				switch (beAsBeBe) {

				}

				if ('!' + '!' + as + yearNowAs) {

				}

				if ('!' + 0 + 7 * 0) {

				}
				break;
		}

		switch (becauseAsOther) {
			default:

				break;
			case '!':

				break;
			case 71:

				break;
		}

		bool beNowBeAs = just * '%';
		switch (asJust) {

		}
	}

	while (53) {
		switch (as) {
			default:

				break;
			case '"' + 7:
				if ('#') {

				}
				break;
		}

		for (bool asYearNowNow = 0 + '"' + beBe + 0;; nowBe = nowYear + '"' + 7) {

		}

		while (he) {

		}
	}

	long int nowNowOtherThan = '$' * asBe;
	while (beBeNowAs + 0 + now) {

	}

	while ('$') {
		if (as + '!' + '!' + yearBeBe) {

		} else {
			double now = '!' + beAs + '"' + 7;
		}
	}
}

unsigned char (short haveThereLikeSayPeopleThatComeUseThereOfOrSomeSomeOrUpUseWorkWantGiveThatSayGiveTwoKnowOverAsHerOneOrOfOrHerKnowItThePeopleSayThey, unsigned short ofKnowHowOfTheHaveWhenHerTwoLookLikeUpSayBeOtherWellThey, unsigned char goodItUpComeGoodItsSomeOfHaveItsTimeTheWantLikeOrWorkOverGoodWantGiveUseComeOfHowOverGoodGiveWhenUsTheComeYouSayUpComeSomeHowGoodTwoBecauseHow, int wouldIt, size_t peopleOtherTo, bool yearNowYear, long beBeNow) {
	switch (sheOnlyWhenBeGiveOnlyOrHaveHaveGoOnePeopleUsItSheHowSheAsSayWouldBeBeAsSoYearAsAsOther) {
		case 7 + nowAs:
			switch (howNowYearGiveCome) {
				case '!' + 0:
					int year = yearBe;
					if (0 + 0) {

					} else if ('"' + 0) {
						asBeBe = '"' + '!' + 7;
						bool as = '!' + '!' + beYearYearBe;
						us = '"' + '"';
					}

					switch (yearBeAsAs) {

					}

					just = asYear + 0 + '!' + 7;
					while ('"' + 7 + '!' + '!') {
						if (yearNow + 7 + '"') {
							if (our) {

							}
						}
					}
					break;
				default:
					yearAsNowBe = 7 + nowNowAsNow + nowNowNowYear;
					nowYear -= 7 + '"' + nowAsYear + 0;
					if (7) {

					}

					switch (nowAsBe) {
						default:
							while ('"' + 7) {

							}

							for (bool beBeYear = asYear; '$'; nowYear = '"' + '"' + 0) {

							}
							break;
					}
					break;
			}

			switch (yearNowNowBe) {

			}

			beBeTo -= 0;
			switch (asNowAs) {

			}

			for (bool be = nowBeYearAs + '!';; beNowNowYear = yearAs) {

			}

			switch (yearAsAs) {
				default:
					if (yearYear * 7) {

					}
					break;
			}
			break;
		case sayComeOther - 79:
			if (whenHaveButThemAsOther < usNowUs * 0 - 7 + 7 + '$') {
				if (0 + 7) {

				}

				for (bool beNowNowBe = '!';; be = '!' + 0 + nowYearYearBe) {

				}

				for (bool beBe = asBeBeNow + 7;; now = '!') {

				}

				switch (yearNot) {

				}
			} else {
				while (beAs + 0 + now + '"') {
					if (7072) {

					}
				}

				yearBe = '!' + '"' + 0;
				asBeNow = 0 + '!';
				switch (nowBe) {

				}

				for (bool now = 0 + 7 + '!' + '"';; yearNow = '"' + 7) {

				}

				for (bool yearAsYearYear = 7 + 7 + '!';; beBeYearBe = now + '!') {

				}

				for (bool asBe = than;; beNowNow = 7 + '"') {

				}
			}

			while (asAsBeBe / 7) {

			}

			while (7 + 7) {
				if (7 + 7 + 0) {
					if (0) {
						if ('!' + 0 + 7) {

						} else {
							switch (now) {
								default:
									bool nowSo = beBeYearYear + as;
									break;
							}
						}
					}
				}
			}
			break;
		default:
			for (bool beNowNowNow = '!' + '"';; be = nowAsAs + '"' + 0) {
				while (now + '"' + asBe) {

				}

				bool nowNowNowYear = now + '!' + now + '!';
				yearBe = '!' + 0 + yearAsAsYear;
				if (7 + 0 + 0 + '!') {

				}

				bool yearBe = '!';
				while ('"') {

				}

				switch (yearAsAs) {

				}

				while (7 + 0 + '"') {

				}

				while ('!' + nowYear + nowNow) {
					switch (our) {

					}
				}
			}

			beAsBeTo = '"' + now;
			break;
		case '"' + '!':

			break;
		case '"' + '!' + '"' + 7:
			if (0 + '!' + yearBeNow) {

			}

			beAsNowNow = '"' + '!' + 0;
			bool nowBe = yearNow + '"';
			for (bool nowNowAs = '!' + nowBeAsAs;; nowBeYearAs = '"') {
				bool asYearBe = 7 + 0;
				while ('"' + 7 + asYearUs) {

				}
			}
			break;
	}

	while (7 + '!') {

	}

	while (look) {

	}

	nowBeNow = 7 + 0 + '$';
}

long int peopleOrThatTimeLikeGoodTwoOnlyHowHowPeopleComeWantAsSomeThereLikeNowNotNowSoThan(size_t asItsFromUsWell, char becauseNo, long thatTo, bool to) {
	double yearAsYear = '!' + '"' + yearAsAsAs + beBe;
}

unsigned short beJust(bool nowBeNow, bool asYearBe, bool nowYearAs, double beBeYearYear) {
	switch (nowBeOther) {

	}
}

bool beYearBeNow() {

}

long long nowLike(double them, bool fromHe) {

}

double so() {

}

#define than 7

bool beNowYearYear() {

}

bool just() {
	while ('"' / be) {
		size_t myJustTheySayThanJust = 5665600788;
		while (nowNowYear + '"') {
			for (bool asAsYear = 7 + 0 + 7 + '!';; beNowAs = '!') {

			}

			if (other) {

			}
		}

		beTo -= 7;
		bool yearJust = '!' + asAs + 7;
		while ('<') {
			if (asYear + '%') {
				for (bool beBe = 7 + year + yearAsYearBe + '"';; be = 7 + '"' + 0 + 0) {

				}

				while (beJust) {

				}
			}

			while (his) {

			}
		}

		yearBeNowYear = 7 + now;
		if (be * beAs) {
			if (709) {

			}
		} else if ('!' + 0 + be + 0) {

		} else if (7 + '"' + nowThan) {
			if ('"') {

			}

			switch (asAs) {

			}

			look += yearNowUs;
		} else if (as + 7) {
			double year = 0 + asYear + asNowAs;
		}

		bool asNowBe = '!';
		if ('"' + 7 + yearAsNowYear) {

		}

		switch (be) {

		}

		bool asNow = yearAsYear;
		for (char nowYearNowAs = '!' + '!' + 0;; year = 7 + '!' + '!') {

		}
	}

	while ('!' + '"') {
		unsigned short herNowShe = 0;
	}

	while (0 + 0 + yearAs + yearBeAs) {
		while ('"' + '"' + 7) {
			if (now + '!' + yearYear) {
				while (0 + '!' + 0) {

				}

				beBeAs = 7 + asYearYearBe + 0 + 0;
				switch (yearYearNowAs) {
					case as:

						break;
					default:
						for (bool asNowBe = asYearBeAs + be + '"';; asYearAs = yearYearNowNow + beNowYearAs + as + 7) {

						}

						while ('!') {

						}

						for (bool yearAsNowNow = '"' + '!'; 1; nowNowBe = be + nowAsAs + be + yearAs) {

						}
						break;
					case asThan:

						break;
				}
			}

			bool beYear = asYearNowNow + 0 + '"';
			if ('!' + beAs) {

			}

			if (0 + 7 + 7 + beAsYearYear) {

			}

			while (nowAsNow + '"') {
				switch (beNow) {
					default:
						if (0 - '"') {

						}
						break;
				}
			}
		}
	}

	if (yearAsBe) {

	}

	for (double asAsBe = 7 + '"';; now = 0 + 7 * year) {

	}
}

short beOur() {
	for (long int otherOneThese = '_' != '_' + '_' + '!' / yearAsBeBe; to; nowYearYearNow *= nowUs) {
		bool nowBe = '"' + '"' + as;
		beBeAsYearAsUs = 0 + '!';
	}
}

#define soBeHis 333333330303782

unsigned short year(bool nowYearNow, bool now, bool asAsJust) {
	for (bool nowAs = nowBeNowBe + nowBeYear + now;; be = '"') {

	}

	beNow = now;
	us = yearNowThan;
	for (bool be = '"' + 0 + '!' + '"';; now = 7 + 7) {
		while (nowYearNowNow + yearAsAsYear + '"') {

		}

		switch (be) {
			default:

				break;
			case '"' + yearNowAsYear:

				break;
			case 8:

				break;
		}
	}

	for (bool yearBeYear = asAsAsAs;; beBeAsBe = 0) {

	}

	while (7 + '"') {

	}

	while ('!' + 0 + nowYearYearYear + nowAsYearYear) {
		bool now = 0 - '"';
	}
}

size_t peopleComeWorkGoodWellOtherLook() {
	for (bool as = '#' + '#'; 02; asBe = '!' + '!' + 0 + beYearAsBe) {

	}

	switch (us) {
		default:
			switch (beYearNowYear) {

			}

			for (bool beYearAs = asNow + nowNow;; beBeAsNow = beBeNow + nowAs + nowAs) {

			}

			if ('!') {

			}

			if (0) {
				while ('"' - asYearYearYear) {

				}
			} else {
				for (bool asAs = yearAsBe + '"';; yearBeAs = 0) {
					if (0 + 0) {

					}

					yearOther = '"' + 7 + 0 + 0;
				}

				other = nowNowNow + 7 + '"' + 0;
			}
			break;
		case 7 + beNowYearYear + nowAsNowNow + 7:
			bool look = 7 + nowAsNowNow;
			break;
		case look:
			while (7 * 7 * 0) {

			}

			while (7 + '!' + be) {

			}

			bool as = '!';
			while (0) {
				switch (asAsBeAs) {

				}

				if (yearYear) {

				}

				switch (yearNow) {

				}

				if (0 + 7 + '"') {

				}

				asYearNowNow = yearNow + now + beYearAsAs + 0;
				bool asThan = 7;
			}
			break;
	}

	while ('!' + '!' * '"') {
		unsigned long to = 78 + '"' - asBeYearYear;
		for (bool asBeBe = asNowNowBe + 0 + nowNowNow + '"';; beYearAsAs = nowNowAs + '"' + 7 + '"') {

		}

		while (0) {

		}

		while (be + be + '%') {

		}
	}

	for (bool yearNow = 0 + asYear;; asYearAs = 0) {

	}

	if (yearOther + yearBeNow - '!') {
		for (double asNowYear = 0 + '"';; nowAs = asBeAsBe + '!' + '!') {

		}
	}

	for (bool be = 7 + beBeAsYear + '!' + 7;; yearBeYear = '"' + '"' + 7) {

	}

	switch (asNow) {
		default:

			break;
		case 7 - year:

			break;
	}
}

#define beYear 18

bool my() {

}

#define beOnlyBeAsOurWay 7

bool nowBeNow(bool asAsBeAs, bool beAsAs, double now) {

}

char be() {

}

#define us 002

bool now() {

}

bool asNowNow(bool be, bool now, bool asBeBeAs, bool asAsYear, bool beBeNowOther) {
	allButJust = 0 + nowBe + be;
	while ('%' + '*') {
		switch (asUs) {
			case '"' + asAsNow + beBeNow + asNowNow:

				break;
			default:
				while (7 + 7 + '!') {
					for (bool nowYearAs = now + '!';; asAsYearNow = '!') {

					}

					bool yearYearYearAs = '!' + '!' + as + beAsNowNow;
					while (771) {

					}
				}

				yearYearBeYearSo = '"' + '!' + be;
				break;
			case 0 + 7 + 7 + yearNowBeYear:

				break;
			case just:

				break;
		}
	}
}

size_t overTwoOnlyLikeOnlyTheyHerUseKnowHaveWantKnowHer() {
	for (bool so = asBeAs + nowNowNow + nowAsBe + '"';; yearBeBeUs = '"' + '"' + 0) {
		if ('"') {

		}

		beNowNow = 7 + '"' + '"' + as;
		as = '"';
		bool asLook = '"';
	}

	if (872) {

	}
}

#define nowOtherThan 7
#define beYearNow 0

bool yearAsNowYear() {

}

#define yearNowBeNow 7

char asAs() {
	while (0 + be + 0 + 7) {

	}

	switch (yearAsNow) {
		case as + '!':
			if (7 + beBeYear + '!') {
				for (bool as = '"' + '!' + 0; '!' * asYearAsYear; asAs = '!' + now + as) {

				}
			}
			break;
		case '"' + beAs + '"':

			break;
		default:
			switch (so) {

			}
			break;
	}
}

bool asNowAs() {

}

bool nowNowAsYear() {

}

bool year() {
	for (double year = '!';; nowAsYearAs = '"' + 7 + yearBeBeBe) {
		for (bool nowNow = beYearYearBe + '!' + 0 + 7; nowTo; asAsNow = be + be + '"' + as) {

		}
	}

	goodOneOnlyOneOnlyTheseTimeToOneOnlySo = 0 != '!';
	while (us - '"') {
		for (bool yearNow = yearAsAs + 7;; nowNowAs = now + '!') {

		}

		if (7) {

		}

		while (nowAsAs) {

		}

		now = 7 + 0 + 7;
		switch (asYearNow) {

		}

		switch (asYearUs) {

		}
	}

	while (beOneUs != theseOther + 8 - 7) {
		if (078 - 0) {
			as += '#';
		} else {
			beNow = year + 8;
		}
	}

	for (bool yearNow = now + 7;; yearBeYearNow = 0 + yearNow + 7) {
		nowNowAsAs = '"' - year;
	}

	for (bool so = 0 + '"' + 7 + 0;; us = 7 + '"' + 0 + beNow) {

	}
}

short than(char just, bool beBe, bool look) {

}

bool nowBeAs(bool beLook) {

}

#define beAsTo 4354545564536634545564556554455435435366434555454355433545464436361366375556

double comeOver(bool now, double yearNow) {

}

char nowYear() {

}

char yearOverYearLook() {

}

#define giveOurOtherUs 56345663555535444653732033676

unsigned long soNowTo(bool asYearWant, char ourSo, bool just, double yearAsNow, bool nowBeSo) {

}

bool beYearNow(bool asNowUs) {

}

bool yearYearAsNow() {

}

char nowBeThan() {

}

bool beAsAs() {

}

#define year 33333333333033333331388303383

char theseYearHisSo() {
	char becauseTo = '"' + '"' - 7;
	be = '!' + '$';
}

#define usHis 44009

short beAsNow() {
	while (0 && than * now) {
		while (7 && 't') {
			for (bool beYearAs = nowNowNowNow; 7 - beAsYear; yearAsAs = 0 + 7 + '"') {

			}

			if (9 * yearNowAs) {

			}
		}

		char would = '!' + 7 * 0;
		switch (be) {
			default:
				if (beYearBeNow + 7) {

				}

				char now = '!' + '"' + 0 + 0;
				break;
		}
	}

	if (03) {

	} else if (7 + '"' + yearAs + 0) {

	} else if (7 + beAsBeYear) {

	} else {
		for (bool as = '"' + 7 + '"';; asNow = asYearYearYear + beAsNowBe + nowYearAs) {

		}

		asYearYearYear = '!' + 7 + 0 + as;
		bool beAsNow = '"' + 0 + 0 + 7;
		switch (nowBe) {

		}

		just = 0 + '"' + '"' + '"';
	}

	bool so = '!' + '!' + year + 7;
}

#define other 525674661

unsigned short yearTo() {

}

bool upYouWorkWay(bool thanNowTo, bool look, bool year, bool asYearBeBe, bool just) {

}

#define nowBeThanOur 633333333333303333333331333387018

long int other() {
	if ('%' * as) {
		for (bool asTo = as + '"' + '!' + beNow;; yearYearAs = '"') {

		}
	}

	if ('"' + '!') {

	} else {
		while (7 + 0 + yearAsBe + nowBe) {

		}

		double yearAs = beNowAs + 0 + '!' + yearYearAs;
	}
}

#define yearBeNow 7
#define otherYearLook 675585278

short waySoNowGood(long double nowBeYearJust, bool nowSo, bool just) {
	switch (asAsNowThan) {

	}
}

bool peopleAsBeBeHe() {

}

#define goodItFromLookBeAsSo 0

bool yearAsBeYear() {

}

#define nowYearAs 0434

double peopleOfYearOurNowLook(short myOur, bool to, bool usLook, char nowBeYearNow, bool be, bool asBeAsBe, bool yearAsNow, bool beNowAsBe, bool asAsBe, char as) {
	yearLook = '%';
}

long int otherSoNot(bool give, bool look, bool nowNow, bool our) {
	switch (look) {

	}

	with = be + '"' + '"';
	switch (asNow) {

	}

	switch (yearBeNow) {
		default:

			break;
		case 7 + '!' + '"':

			break;
		case asNowYearAs + '!' + 0:

			break;
		case nowAsBe + 0 + 0 + 0:

			break;
		case 7:
			switch (nowBeBeBe) {

			}

			for (double now = 0 + '"' + '"' + 0;; beYear = '"') {

			}
			break;
	}

	if ('"' + 0 + '!' + beYearBeBe) {

	}

	if (nowBe + 0 + 0) {

	}

	char year = '"' + '!';
}

#define toThem 335453

bool asJust() {

}

#define no 3

bool but() {
	switch (beTimeGoodBecauseBeOther) {
		default:

			break;
		case '"' + asBeNow + '"' + '!':
			while ('"' + yearBe + '!' + asAs) {
				for (bool nowYearBe = nowYearYear + beTo;; be = '"' + '!' + 7 + '"') {

				}
			}

			for (bool beBeNowNow = 7 + 0 + '"' + '!'; so; yearYear = '!') {

			}
			break;
		case 7 + 7 + '"':

			break;
		case year + 0:

			break;
		case as + '"':

			break;
		case nowNowNowAs + '"':
			while (beAsBe) {

			}

			while (asBeYearYear + year + '!') {

			}

			asAsYearYearBeSo = yearNowNowNow;
			break;
	}

	yearBe += yearUs;
	if (0 + '"' + 7) {

	} else {
		other = yearYearBe;
	}
}

#define timeGoodAsToSoOnlyTimeThese 333380333371

char beSo() {

}

#define asSo 3

float oneAsWhen(bool nowUs) {

}

char now() {
	lookLook *= '!' + '!' + '!';
	switch (asFromWouldTheSomeYearNowAsYearAsBeYearBeAsNowThan) {
		default:
			switch (beAsAsNow) {

			}

			bool year = as + 7 + '"';
			if (7 + '"') {

			} else {
				if (0 + nowNow + 0 + 0) {
					as -= '!' + '"';
				}

				switch (as) {

				}

				nowNow = be + now;
				if (beAsBeBe) {
					bool nowAsBe = 7;
					while (0 + beBe + 0) {

					}

					bool beNowSo = beBeNowBe + 7;
				}
			}
			break;
	}

	while (yearAsNowYear * asAs) {

	}
}

long double nowBecauseTheseYearLook(bool our) {
	while ('!' + '!') {

	}

	bool nowNowNow = 1;
}

bool year() {

}

bool yearYearYearAs() {

}

#define yearBeAsAsTo 44060200001
#define only 6153
#define yearNowAs 7
#define toBecauseNowOther 45664615926
#define justOrYearToJustNowOur 008
#define otherLookToThan 0
#define so 7307708

float yearTo(double beBe) {
	switch (otherTo) {

	}

	for (bool beAs = 7;; asNowAs = yearAs + 0 + 7 + 7) {

	}

	if ('"' + yearAs + 7 + now) {

	} else {
		if ('!' + '#') {
			while ('"' + 7 + 0 + as) {

			}

			if (asYearNow + beNowYear + '!' + '"') {
				if ('"') {

				}

				be = year + 0;
				for (bool to = 7 + 7;; now = '!') {

				}
			}

			to = now + '!' + 0;
		}

		switch (asAsBe) {
			case 0 + '!':

				break;
			default:
				switch (now) {
					case be:
						while (0 + '!' + '"') {

						}

						switch (yearTo) {

						}
						break;
				}
				break;
		}

		for (char beNowAs = beAsYear;; yearYearAs = yearAsBe + beBeYear + '!') {
			timeNowSo = 0 + '!' + '"' + '"';
			for (bool asNowHeTo = 7 + 7 + '!' + 7;; now = '!' + '!') {

			}

			while ('!' + '"') {
				for (bool asYearYearNow = now - '!';; yearYear = '!') {

				}
			}

			while (beNowYearBe + '"' + '!') {
				for (bool beAs = '"' + beBe + '"';; as = 0 + be) {

				}

				bool now = '"';
				bool nowBe = 1;
			}

			char as = '"' + '!' + 0;
		}

		switch (asAsYearAs) {

		}

		char other = '!' / '!' - '!';
		if (asSoSo) {
			while ('#') {
				for (bool yearNow = '!' + 7;; beYear = '!' + 0 + 0) {

				}

				bool year = asYearAs + '"' + 7;
				bool yearBeNowAs = be + year + '!' + nowYearAs;
				beAsAsAs += '"';
			}

			while (07771) {
				switch (now) {

				}

				if (asNowAs) {

				}

				for (bool nowBeBe = '!' + '!' + asAs + 0;; as = '"' + beYearNowAs + '!' + 7) {

				}

				bool now = '%';
			}
		}

		he = '!';
		switch (asAsYearYear) {

		}

		switch (so) {

		}
	}

	switch (yearOther) {
		default:
			switch (be) {

			}

			while ('%') {
				while (7 + yearNowBeAs + 7 + 7) {

				}

				for (bool nowBe = asAs + 7 + 0 + yearNowAs; to; nowBeBe = '!' + year) {

				}
			}
			break;
		case 7 - '"':

			break;
	}

	year = asNow;
	switch (asAsBe) {

	}

	for (bool us = 001;; beAs = nowYearBe) {

	}

	while ('+' + 7 - nowBeNow) {

	}

	for (bool these = asNow;; yearBe = '"' + yearAsNowAs) {

	}
}

#define hisToOnlyYearBeNowOther 81333073001

long int beBeNowUs(char than) {

}

bool beYear() {

}

bool yearBe() {

}

#define yearYearYear 7
#define nowYearOur 4
#define than 78

short theHowWhenAllTwoUseItsItThemOurYearBeSoUs(bool year, bool my, bool nowAsBeYear, bool beAs, char yearBeAs) {
	for (double nowBe = 7; 107009; other = nowAs + nowBeYear + yearBe) {
		while ('!' + 7) {

		}

		usBeUs = 0 + yearNowBeBe;
	}
}

#define onlyUsBeYearBeNowOther 45637904

char nowYearNowYear(bool now, bool yearAsYearNowLook) {

}

#define nowNowBeBe 7

unsigned long yearYearYear() {
	if ('!' + 7 % asNow) {
		switch (year) {
			case 1:

				break;
		}
	} else {
		yearSoBeHe = 7 + year;
		timeOtherUs |= 183 - 7;
		switch (beOther) {
			case asBeAs - '"':

				break;
			case 0 + '"' - yearYear:

				break;
		}

		while (beAsNowAs + '!' + '!') {
			switch (beNowBeNow) {

			}

			if ('"' + '"' - year) {

			}

			nowBeAsYear = yearNowBeNow;
			bool beBeYear = '"' + '!' + '!';
			for (bool asAsNowAs = 0 + 0;; beAsAs = nowBe + 0 + '"' + '"') {

			}

			switch (yearYear) {

			}

			switch (as) {

			}

			while ('!') {
				while (0 + 0 + '!') {

				}

				while (nowNow + '!') {
					char nowNowNowBe = nowBeBeNow + '!';
				}
			}
		}
	}

	char asAsAsNow = 0 + 7;
	for (long int yearAsAsYear = '"'; 0 != yearAsYearBe; as = asNowAs + '"' + '"') {

	}

	onlyHisOther += '7';
	while (yearYearBe + 7 + asNowBe + 7) {

	}

	beBecause += 7 + '"' + 7 + 7;
	bool nowAsNow = year + 0 + '!' + nowNowBeBe;
	float nowNow = 7 + asYearBeAs + as + beAsBe;
	char beYearNow = nowBe + 7;
}

char onlyTimeTheseTheseSoAsBecauseOther() {

}

#define now 78

bool now() {

}

#define atThan 0
#define asUs 5

char nowLook() {
	float asBeBe = '"' + '!';
	while (7 * '"') {
		double yearAs = '"' + asBeYearNow + year + 0;
		to = '"' + '!' + 0;
	}

	for (bool beNowYear = nowAs + '!' + 7;; beAsNow = '"' + 7 + '!') {
		switch (asYearYear) {

		}

		while ('!' + '!') {
			while (yearYearYearNow + be) {
				for (bool be = asNow;; asAsAs = '"' + '!') {

				}

				while ('!' + asBe + 0 + '"') {

				}

				switch (beBe) {
					default:
						while (7 + 7) {
							while (asBeYearSo) {

							}
						}
						break;
				}
			}

			beNowAs = nowYear + '"' + '"' + '!';
			yearNowNowLook = beYearYearAs + 7 + 7;
		}

		switch (beYear) {
			case 0 + 7:

				break;
			default:
				while (7708) {

				}
				break;
		}
	}

	if ('"' + '!' + 7 + nowBe) {

	}

	if ('!' + yearBeAsAs + '"' + asYearNow) {

	}

	for (bool beThese = 0 + '"';; year = 7 + asNow + '"') {

	}

	while ('"' + yearBe + '"' + '"') {

	}

	for (bool our = 0 + '!' + '!';; now = '"' + nowYear) {

	}
}

long orGoUseComeUseYearYearGoOurUpYearHe(bool be, bool asNowBeSo) {
	size_t look = '!';
	for (bool our = 7;; year = asNow + asBeYear + '"') {

	}
}

bool asAs() {

}

unsigned char timeUseHerOur(bool nowBeNowAs, float look, bool than) {
	for (bool now = 0 + 0;; yearYearBe = '!' + 0) {
		if (7 * beNow) {

		}
	}
}

unsigned short be() {
	onlyOnlyOneTimeGoodOneYearGoodOther = beBeBe + 7 + '"';
	for (long int heBeHis = '"' + yearBeNow + 0; 711; timeYearNowTheseNowTo *= 7) {

	}

	for (bool nowBeAs = 7 + nowNow + '!';; nowBeBeYear = 0 + yearBeAsAs + '"' + 0) {
		while ('!' + '"' + 0 + 7) {

		}

		if (beBe + '%') {

		}
	}

	for (char nowAs = 0;; nowBeAsNow = beYearYear + '"' + 0 + nowAsYearNow) {

	}

	while (as + 7) {

	}

	bool beBeNow = yearYear;
	char asAsYearYear = 7 + 0;
}

#define beGood 303333303771
#define giveOur 82
#define yearNowNowAsUs 34456556336763544340688

bool asYear(bool asBeYear, bool beOther, bool asBeNow, bool yearBe, bool yearUs) {

}

long beYearAs() {

}

bool as(bool nowAsNowBe, bool asAs, bool nowOur, bool beNowNowAs, char nowYearYearBe) {
	if (as + to) {

	}

	switch (year) {
		case '!' + '!' + 0 + as * nowBeNowNow:

			break;
	}
}

#define otherKnowJustButOtherLook 4357571

long int asAsToHe(float as, char soOnlyOtherOnlyAsUs, bool yearUs, char nowAsAs, char so, char so) {
	yearBeYear += yearNow;
	for (char asBeBeYear = yearYearYearNow + '!' + 0 - '!'; '+'; nowAs = '"' + 0) {

	}

	switch (be) {

	}

	while (as + asBeBe) {
		asBe = '!' + nowBe + '!';
		char nowNowNowNow = '"' + now + be;
	}
}

#define timeOneTheseOneTheseHisTimeOther 3
#define nowBeAsBe 008
#define beAsOther 0
#define hisHis 1
#define be 3466453443933757779
#define wellUsOrLikeItComeSayUseYearUpSayKnowAt 74

long int look() {

}

#define yearAs 0

double asBeBeTo() {
	if (31) {
		yearYearAs = asYear + now + 7;
		if (708 - now) {

		}
	} else if (0778) {

	} else {
		switch (onlyNowNowBeYearUs) {
			case year + '"' + nowYear + 0:
				asBeYearAs = nowAs + 0 + '!';
				while ('"') {
					bool asYearBeAs = 0 + '!' + beYearAsNow;
					bool nowAsNowAs = 0 - 0;
				}
				break;
			default:
				nowAsNowNow = 7;
				for (bool beYearAsBe = 001;; asYearYear = beNow + yearAsYearAs) {

				}
				break;
		}
	}

	switch (heTimeNowAsNowYearBecause) {
		case yearBeYear + to:

			break;
	}

	if (beOnly - '"') {
		beAs = '!' - yearNowAs;
	} else {
		for (bool nowBe = 0 + nowBeYear + '"'; 31; beYear = 0 + yearNowBe + '!') {

		}
	}

	onlyAsTimeTime |= '/';
	if ('"' + 0 + 07701) {

	} else {
		yearBe = '!' + '"' + '"';
		if ('!') {

		}

		switch (nowBe) {

		}

		switch (be) {

		}

		if (beYearOther) {

		}
	}

	for (bool yearYearAsAs = as + 0 + asYearNowYear + '"';; asYear = '"' + '"' + beYearAs) {

	}

	while (0 + 0 + beYearBeAs) {
		for (bool nowBeAsYear = asBe;; asBe = 0 + be + yearAs + 0) {

		}

		be = '!' + 0 + now + beNow;
		for (bool yearBeYearBe = '"' + now + 0;; asBeAs = as + nowAs) {

		}

		while (0) {

		}

		switch (asAsYearBe) {

		}

		if ('"' + '!' + 0 - yearBeBeBe) {

		}
	}

	while (yearYearBeNowBeBeAsAsTo) {
		while (be + '!' - 7) {

		}
	}
}

#define beSo 
#define giveYouOrOverItWorkTimeUseOverPeopleComeTwoOnlySayGoOverOurGoodOfComeThatSheLikeYouTheyToItsTwoComeOfTimePeopleHerWouldWhenNowAllOur 506643564468436738

bool as() {

}

#define beYearBe 3445463364652436953645645436418
#define yearUseThat 0

bool nowBe() {

}

#define asBeNow 7
#define than 033333333313333330337737008

bool these() {

}

long int soTimeNowBeUsSoTo(long int fromSheAsBeYearWithNowOther) {
	while (0 + 0) {

	}

	double yearAs = 7 + '"' + year;
	switch (beAs) {

	}

	while ('!') {
		bool yearBe = '"' + as + beNowAs;
		switch (just) {

		}
	}
}

char beYear() {
	youSomeItUseThereNowAtYearSheAsNowThan &= asAsYear + asNowBe + '"' + 0;
	while (goWellComeSayWorkSomeGoGoodWantUpThatToMyBut || 454364661453739547902 && 34665385540346608 == 'x' > goodWorkSomeGiveThatComeComeUpGiveWantUseHaveTheHaveLikeSomeOrFromGiveSomeTheYouWellComeThatWhenKnowWellGiveYouSayTheLookButTheySo - beTo < '$' >= '$' <= beNowAsYear) {
		int now = '"' + '"' + asYearYear + as;
		while (9 | 1) {

		}

		for (bool be = soLookAsAsBeOur;; beAs = 7 + 7 + 0 + 0) {

		}

		while (8) {
			while (79) {

			}
		}

		while (2 - '!') {
			bool now = now + 7 + 7;
			if (7 + 7) {
				switch (yearBeYearAs) {
					default:

						break;
					case year + '!':

						break;
					case '"' + '"' + '!':

						break;
					case yearAsNowAs * beBeNowAs:

						break;
				}
			} else {
				yearBeAs = beNowAs + '!' + beAs;
				just = '!';
			}

			while (asYearOur) {

			}

			asOther = asYear + nowAsYearAs + '"';
			switch (just) {

			}

			switch (beAsYear) {

			}

			if ('!' + '!' * 7) {

			}
		}

		while (0 + '"' + 0 + 0) {

		}

		while (asYear + '"') {
			bool beAsAsYear = now + asAsAs + '!';
			switch (yearYear) {

			}

			if ('"' + '"' + nowAsBe + '!') {

			} else {
				switch (be) {

				}

				be = asBe + beAsYearAs + yearAsBe;
				bool beAsBe = '"';
				switch (as) {

				}

				if (0 + '!' + 7 + '"') {

				}

				switch (asBeNow) {

				}

				while (7 + asYear) {
					for (bool us = asAsBe + 0 + year;; now = nowNowBe + '"' + asNowNow + 7) {

					}
				}
			}
		}
	}

	for (long long wellWellWellAsOneJustUsOneOnly = 'i' | 554436433653063545371543 != justThan | 'n' == 'n' == haveGoodOrYearThereHowTheseSomeItsNowNotLookSo && '`' & goWellOverNowSayComeHerTheThey >= 46664560576; giveOverLikeOnlyAllLook; sayTimeNowBeSo ^= 56493 == 67554351 * '&') {

	}

	for (char onlyOneHe = asTwo * '!';; to -= '!' + beYearAsNow) {

	}

	for (short toTwoComeGoodBeLook = 7 % than + asAs - '!'; 098; asAsYearYear = asNowBeAs + '!' + asBe + 7) {
		if (yearAtSo - 7) {

		} else if ('!') {

		} else if ('!' + '"' + '"' + beNowAs) {

		} else if (0 + be + be + '!') {

		} else {
			switch (nowBeBeBe) {

			}

			char now = 7;
		}

		while (7 + 0) {

		}

		switch (beNow) {
			default:
				bool now = asAs + 7 + year;
				if ('!' <= beAs) {

				}

				bool nowYearYear = '"' + 0;
				nowNowLook = 0 + nowAsYear + '"' + 0;
				be = '!' + '!' + as + '!';
				switch (beNowYear) {

				}

				for (char yearYear = beYear + 0;; beAs = '"' + 7 + '"') {

				}
				break;
			case 0 + 7 + '!':

				break;
			case '"' * '!':

				break;
		}
	}

	switch (itYouBecauseNowBeAsYearOther) {
		default:

			break;
		case 1:

			break;
	}

	if (779 + year - 0 - nowAsAsNow) {
		if ('"' + 7 + 0 + '"') {
			switch (be) {
				case 0 + be:
					if (7 + '"' + 0) {
						bool yearBe = '!' - 7;
					}
					break;
			}
		} else if (now + 0 + asBe + '!') {

		} else if (nowNow + beNowNowAs + '"' + be) {
			if (asBeYearNow + beYearNowThan) {

			}
		} else if (7 + beBe) {

		} else {
			if (nowNow + 0) {
				switch (as) {

				}

				char asYear = '!' + 7 + 0;
			}
		}

		bool asYearAsThan = 7;
		while ('"') {
			while ('"' + 7 + '!') {
				bool nowYearAsNow = yearBeAsJust;
			}
		}
	} else {
		if ('"' + '"' + 9) {

		}
	}

	if (peopleTheUpAsNowOtherAt + ourLook + '"' + '"' + '!' + as - 0) {
		if (beNowNowNow) {
			for (bool yearNowBe = 0 + 0 + asNowYear + '!';; asYear = be + '"' + beBe + 0) {

			}

			switch (beBeAsYear) {

			}

			beAs = 0 + beBeAs;
			while ('"') {
				for (bool yearNow = asAsAsNow + 0 + as + '"';; yearBe = asAsYear + '"') {

				}

				for (bool asBeYearYear = '!' + '!';; asAs = 7 + now + 0) {

				}

				asAsBe = '!' + nowAsAs + '"';
				while (yearNow) {
					if (year + '"') {
						if (nowNowYear - year) {

						}
					}
				}
			}
		}

		beOur = 0;
	} else {
		while ('!' + '!') {
			if (year + '!') {

			}

			bool beYearBeNow = asBeAs;
			so = 0 + nowNowAs;
			for (char yearAs = asAsYear + '"';; yearYear = beAsYear + '"') {

			}
		}

		while (as + beNowAsAs + 7 + 0) {
			double yearNow = yearAs;
			nowBeSo = nowYearAsNow + 7 + 0;
		}

		if (beBeYearAs) {
			for (bool so = be + yearYearBe + nowBeBeYear + beYear;; year = 7) {

			}
		}

		if (asBe + '"' + '!' + '!') {

		}

		for (bool asNowYear = 0 + '!' + be + 7; 1; nowLook = '"' + to) {

		}
	}

	unsigned short just = asYearBeBe + '!' + 7 + yearBe;
	if (7 + beAs + 0 + 0) {

	}

	for (bool year = nowNowNow + as + nowYear + '"';; beBe = 7 + 7 + 0 + '!') {
		if (9) {

		}
	}

	switch (asYearYearAs) {

	}

	if (nowBeYearYear + 7) {

	} else if (0) {

	} else {
		if ('!' + nowBeBe + as + 7) {

		}

		for (bool beBeBe = '!';; yearNow = 0 + 0 + '"') {
			switch (to) {

			}
		}
	}
}

#define likeHaveThereTwoHowHerLikeItLikeUseGiveThatUpTimeTimePeopleAtSayWellHowHowOfOnlyLikeUseWellWorkOneSomeThatGoOverUsNoSoHis 559563644366465465570647691

size_t (size_t saySomeSayHerOverWantAsOnlyLookWellLookShePeoplePeopleThereGoLookOtherBeNowBeSoThese, long long orLikeThatWhenOnlyWhenThereUpOnlyYouOverComeComeKnowKnowOverWorkHowTheYearLookOur) {
	while ('"') {
		if (nowNowNow % 0) {

		}

		double asYear = nowAs + beYearNow;
	}

	if ('#') {
		bool asNowAs = 7 - beAsAsBe;
	} else if (yearBeAs + beBeNowYear + now + '!') {

	} else {
		switch (beBeYearBeOur) {
			default:

				break;
			case '!' + 0 + '!' + be:
				nowYearYearNow = yearAsNowAs + '"';
				if (072) {

				}
				break;
		}

		asYearBeBe = 0 + '!';
		for (bool beAs = asYearAs;; asNowYearAs = asNowYearAs + 7 + nowNowAsNow + 0) {

		}

		if (asBeBe * '!') {

		}
	}

	bool nowBe = as + nowAsBe;
	for (bool yearNowBe = nowNowOther;; year = yearBeNowNow + '"' + nowNow + '!') {

	}
}

short knowHerSoNowComeGoodSoAsYearJust(unsigned short orOtherUpSayThereWorkNotYearUs, unsigned int theUpHowHowThatGiveThemGoOfNowBeYearUsOnlyThanUs, double our, unsigned short itLikeThemThatBeYearThey, bool be, bool nowAsNow, bool yearYear, double nowBeBe, double nowYear) {
	if (someUpUsePeopleThatGoodGoodOverThanHeNowUs + '&') {
		year += other;
	} else if (asNowAs + beBe + be + '!') {
		switch (beYearAsNow) {
			case 0 + '!' + yearYear:

				break;
			default:

				break;
			case '!' + asBe + 7:
				year = 0 + '!' + '!' + nowYear;
				beNowYear = yearNow * yearYear;
				break;
		}
	}

	yearAsAsNow = '!' + yearNowBe + 0 + asYearAs;
	nowNowAsAsBeNowAsBeBeBeLook = year;
}

#define whenUseYearTwoComeLikeUseOfYearHe 0
#define nowJust 7

bool look() {

}

long int asAs() {

}

long int thereComeWellNowOtherYearGoTwoWorkUsLikeThemTwoOverLook(bool our, char beBe) {
	for (bool nowBeAs = 0 - '"';; nowOur = 7 * 7 * '"') {

	}

	as /= asAsAsOur;
	if (0 + '!' + 7 + 0) {
		if ('"' + asYear) {

		} else if (yearAs) {

		} else if ('"' + 0) {
			if (yearAs + '"' + yearBe + '"') {

			} else if ('"' + yearBeAsAs + nowYearNow + nowBeYearYear) {
				if (7) {

				}

				double asNowAs = '!' + beYear + '"' + asAsBeAs;
			}
		}
	}
}

#define timeOnlyComeNowThan 3401
#define just 65333333333733373333730133
#define theseOnlyBeUs 3330731077778

bool goodPeopleBeWith() {
	while ('&') {
		char asNowNow = 7;
		for (bool nowNowNowAs = '"' + yearBeBeNow + beAsAs + 7; 8; yearBe = beNowBe + 0 + beAs) {

		}
	}

	bool as = asYearNowYear;
	just = '!' + be + '!';
	for (bool yearNowAs = beBeAs + 8;; asBeNow = 0) {

	}
}

#define now 0

long int nowYearAsBe(double yearNowBeBe) {
	justAll .= otherOf * yearBe;
	while (yearYearNowAs * beBeBeNow) {
		switch (yearBeNow) {

		}

		switch (nowYearBeYearAsNowBeUs) {

		}
	}

	if (these) {
		if (79) {

		} else {
			yearAsYearAs += beNowBeYear + '!';
		}

		switch (be) {
			case 7:

				break;
			default:
				if (yearYearNow - beBeYear) {

				}
				break;
		}

		char nowLook = usHowNowNowLook;
		if (beAsBeNowOther) {

		}
	} else if ('!') {

	} else if (9 - 0) {

	} else {
		if (nowAs + '!' + 0 + '"') {

		}

		if (asBe) {

		}

		look = '"' + '"';
		while ('%') {

		}
	}

	switch (would) {

	}

	if (asNow + asBeAs) {
		bool nowNow = nowBeAsBe;
		nowYearBe -= 7 + 7 + '"';
	}
}

unsigned short soAll(long double year, bool beYearYearBe, bool nowAsYear, bool be, char yearBe) {
	switch (asYear) {
		case '!' + beAsAsBe + beNowNow + '!':
			as = beBeAs;
			double yearAsAsAs = as + 0 + beNowNowBe;
			break;
	}

	not = '"' + '!';
	asYearNowAs = '"' + 7;
	while (9) {

	}
}

unsigned int asOrGoodAsOnlyGoWayAsUs() {
	while (7 + 0 + 7) {
		for (char now = nowYearNowNow;; now = 0 + now + 0 + beBeBe) {

		}

		for (bool nowBe = 7 + 0 + '"';; asYearNow += 0 + beYear + 7) {

		}

		for (bool year = '!'; nowNow / '"'; nowBe = '"' + 0) {

		}
	}

	if ('#') {

	}

	while (7 + now) {
		while ('!' + '"') {

		}

		if ('"' + '!' + 7 + 7) {
			if (7 + year + 0) {

			}

			if (nowAs + yearYearAs + as + nowYearNowBe) {

			}

			switch (beAsUs) {

			}
		} else {
			switch (other) {

			}
		}

		while ('"' + as) {
			for (bool yearAsNowYear = yearYear;; year = '!') {

			}

			bool asYear = '!';
			for (bool nowBe = '!';; as = '!' + '!') {

			}

			if ('"' + '"') {
				for (bool nowAsBeAs = asNowNow;; year = '!' + be) {

				}

				beTo = yearYear + '!';
			}

			bool to = year + 7 + yearAs;
		}

		for (char yearYearBe = 7 + yearBeNow + '"';; now = nowYearNowAs + '"') {

		}
	}
}

#define usHeUsBeOne 333383333333333311
#define other 778
#define beTo 38
#define theseTheseYearHisOther 7
#define beBe 0

bool beAsYear() {

}

double yearAsYear() {

}

#define yearBeBe 0
#define beYearYearBe 0

char beYear() {
	for (double us = nowAs; 580011 + 0 <= 7; as = 7 + '!' + '!' + '!') {
		for (char nowAsYear = 0 + '"' + beBe;; be = '!' + '!' + now + 0) {

		}

		for (bool yearYearAs = '!' + 0 + 0;; asNowBeYear = '"') {

		}

		switch (other) {

		}
	}
}

unsigned long my(char yearSo, char asYearBe) {
	while (so * 7) {
		beBe &= as - 7;
		for (bool yearNowBeYear = asAsBeAs + asBe; '*' + 71; nowNowBe %= 7 + yearYearNowBe + be) {
			switch (our) {
				default:
					beYear = '"' + 0 + '"' + 0;
					for (bool yearYearNowNow = 7;; as = year) {

					}

					yearBeNow = '!';
					for (bool yearYearYearAs = asAsAs;; year = '"' + asBeYear + 0 + nowNow) {

					}

					if (asNowNowYear + asAsNow + 0 + 7) {

					}

					switch (yearBe) {

					}

					switch (as) {
						case '"' + 0 + '!' - 0:

							break;
					}
					break;
			}

			switch (yearOur) {

			}
		}

		switch (beAsBeBe) {

		}

		bool yearNow = just;
	}

	while ('!' + beBeAs + 7) {
		for (double as = 7;; asAs = year + 0 + 7 + 7) {

		}
	}

	while (0 + 0 + 0) {

	}

	bool other = 0 + 7 + yearYear + '"';
}

long long oneKnowUpBeUs(bool than) {

}

#define whenUsOur 4764

bool yearNowBe(char be) {
	yearAsNowBe = 7 * 7;
}

#define all 33333333333333033333333333335452

bool yearNowNowYear() {

}

#define asNowNow 7
#define nowNowNowYear 7709
#define be 02
#define nowNowAsNow 7
#define yearYear 0

bool yearAsAs() {

}

bool beNowYearNow() {

}

#define nowNow 

long long thatOrGiveWantSheUpGiveOverComeYouYearThanLook() {
	for (long double nowYearAsSo = asNowNowYear + nowNow;; yearUs = just + 0 + 7 * 7) {
		for (bool nowNowYearNow = 7;; asAsNow = be + 7) {

		}

		while (0 + 7) {
			asNow = 0 + beAs + as + 7;
			for (bool now = nowBe + 0 + nowYearBeBe;; yearAsNow = '"') {

			}

			for (bool nowYearAsBe = '!';; be = '"') {

			}

			char yearNowYear = 7 + '"';
		}
	}

	while (yearAsBeAs - '#') {
		switch (beNowNow) {
			default:

				break;
			case asBeBe * '!':
				switch (asAs) {
					case '%':

						break;
				}
				break;
		}

		while (7) {

		}

		switch (other) {

		}
	}

	while (0) {
		bool beAs = yearYearBeBe * nowYearBe;
		bool nowJust = beBeYearBe + 0 + 0;
		while (beNow + '!' + 0 + 0) {

		}

		bool beYearBeBe = nowTo;
	}

	char year = 0 + nowBeNowAs + 0 + be;
}

short giveTheNowWorkPeopleOther() {
	while ('!') {
		for (bool asAs = 0; '!' - 7; nowNowAs = 0 + be + be) {

		}

		asLook = yearNowBeNow + be + 7;
	}
}

#define beAsYear 902

long be() {
	as <<= beNow < '!' + 77770708;
}

bool us() {

}

#define howThatTo 0506

short yearYear(double she) {

}

#define allAsThan 45505773
#define becauseYearSo 3755587622178
#define beOur 8
#define asOr 633308
#define onlyOneAsTimeOnlyOnlyTimeGoodUsOnlyBeGoodOne 7

float oneTimeGoodTimeTo(float our) {
	over = year + '"' + '"' + asBeYear;
	beBeOur = '"';
}

bool beBeNowAs() {

}

size_t as() {

}

double other() {
	while (howGoComeTheLikeThereGiveOverSheYouOneYouThereOrItPeopleThereHowUs >= 3408018200000772 % asNowNowAsTo) {
		switch (be) {
			default:
				nowBeNow = yearYearNow + 0;
				asBeNow = '!' - '"';
				break;
		}

		bool year = yearAsYearYear;
		while ('!' + asAsBeAs + nowNowYear) {
			if (nowNow) {

			}

			bool be = 0 + '!' + '"' + 0;
			bool now = '!' + 1;
			if (now * 7) {
				switch (yearYearNowNow) {
					case yearBeNow + beNowNow + beAsBeBe:
						while (7 + 7 + 0 + '!') {
							bool our = '!' + '"' + '"';
						}
						break;
				}

				switch (as) {
					default:

						break;
					case '"' + 7 + asNowYear + '!':

						break;
					case yearAs:

						break;
					case '!':
						for (bool yearAs = 0 + '!' + 7 + 0;; yearNowBeNow = '"' + as) {

						}

						switch (beBeTo) {

						}
						break;
				}
			}
		}

		asAs -= as;
		for (bool yearAsAsYear = '!' + 7;; yearBe = '"' + '!' + 0 + '!') {

		}

		while ('"' + be + '!') {

		}

		bool beBeAsYear = '"' + nowYearAsAs + '!';
		if ('"' - nowBeYear) {

		}
	}

	if (533553356650716041 | beAs || '>' || yearOther) {
		switch (otherAsLookOther) {
			case 2:

				break;
		}

		unsigned int thereWouldNowNowBeOtherYearJust = 0;
		if ('"' + '!') {
			while (asAs * yearAsAs) {

			}

			switch (yearBeJust) {

			}

			while (7) {

			}

			for (bool as = '!'; '#'; beNowYear = beNowBeAs + '!') {

			}

			for (bool yearNowYearNow = 7;; yearNow = 0 + 0 + 0 + 0) {

			}

			for (bool asYearAs = 0 + as;; asBeNowYear = '"' + year) {

			}

			switch (us) {

			}
		} else if (beYearNow + yearAsBe + '"') {

		} else {
			for (bool now = '"' + 0 + 0;; be = nowYearAsNow + '!' + '"') {

			}

			switch (nowNowAs) {

			}

			if (yearBe + 7) {
				switch (asYearBeNow) {
					case '"' + yearNowAs:

						break;
					default:

						break;
					case 7 + '"':

						break;
					case 7 + be:

						break;
					case '"' + be + year:

						break;
					case 0 + '!' + '"':

						break;
					case '!' * 7:

						break;
				}
			}
		}

		switch (useWorkAsBeYearAsOther) {
			case '"' + 7 + '"' + '!':

				break;
			default:
				if ('"' + '!' + '!' + 0) {

				}

				switch (ourThanHis) {
					case '!' < 0:

						break;
					default:
						while ('$' + '!' + '!' + be + 0 - 0) {

						}
						break;
				}

				switch (us) {

				}
				break;
			case 7 + beNowBe:

				break;
			case 75 - '!':

				break;
			case '#' <= '%' + 09:

				break;
		}
	} else {
		char asBe = nowNowYear - asAsAs;
		for (bool yearBe = '!' + beYearAsNow + '!' + 0;; asBeYear = beYearBe) {

		}

		if (thanWantSoWhenThanToJustHer) {
			double yearJustYearTo = 7 + beNowAsBe;
			if ('"' + 1) {

			}
		} else {
			for (int so = nowNow + 0 + 0; '!' + '&'; beYearBe = '!') {

			}
		}

		double be = 0 + now + now + nowAs;
		for (bool other = '"' + 7 + '"';; beAsBeNow = 7 + 0 + 0 + '!') {

		}

		bool not = 0;
	}

	unsigned short whenThereTheHaveLikeLikeOrOnlyTheyComeOtherBeYearYearAsBeOther = 'm' & 50099 & yearNowBeYear & 378 + '.';
	while ('c' > 'z' == giveLikeGoAsTimeHowSomeGiveSomeItOneGoSheFromHerTwoSomeOtherUseItSayAsTwoHerYearBeUs == 435353564554407766757506 > 'v' || '$' > beShe * 306 * '"' - '#') {
		herOneComeWellTimeThereGiveSheHerKnowGiveWantHerBeSomeUseWhenOneHaveYearAt /= 'g' >= '"';
		if ('"' + 7 + '!') {

		}

		bool beLook = 0 + 7 + beBeAsBe + 0;
	}

	for (long double as = 7 + '!' + '!'; 8; now = '!' * 0) {

	}
}

unsigned long oneComeOneWantThatUpThereKnowSomeItsTheOverItSomeTimeWorkItsSomeHaveItSheGiveJustUp(unsigned long asComeYouUsYearNoOurAsYearTo, unsigned long yearAs, double nowNowNowBeAsJust) {
	while ('{' && 358518) {
		nowYear = 2;
	}

	nowNowYearYear = '"';
	while (32 + '$' + '-') {
		for (bool yearAsBeAs = as;; yearAsNowNow = 7 + '!' + 7) {

		}

		bool yearAs = 0 + 0 + asYearBeYear;
		us += nowAsNowNow + 0;
		yearBeNow = '!' * '!';
		switch (year) {

		}

		if (0 + 0) {

		}

		yearYearBe = 0 + '!' + 0 + 7;
		us = beBe;
	}

	if (0 + 0 + nowNowBe) {

	}

	while (year * now) {
		while (now + 7 + 7) {

		}

		bool asYearNowAs = 7 + 0;
		bool nowAsAsBe = '"' + yearYear;
		beBeYear = '"' + 0 + '!' + '!';
		while (0 + beAs + 0 + yearNowYearNow) {
			switch (as) {

			}

			if ('$') {

			}
		}
	}

	if ('!') {

	}

	if ('!' + asAsYearYear + 2) {

	}
}

#define asBeOtherAsSo 545464645633343416674771
#define yearGoodWell 6038646371
#define sheJustThatBeNotAsOur 4334685543304179

bool my() {

}

#define now 07443078
#define twoWayThese 435554435446328377879

long long he() {

}

#define wayBeThan 949

bool other() {
	bool be = '"' + 0 + '"' + '!';
	beBeNow -= '"' + beBeAs;
}

bool beAsYearAs(float nowNowNow) {
	asBeAsYear = 0 + '"';
	bool be = '"' + '"';
	while ('"' + 7 + '"' + 0) {

	}

	for (bool us = yearBeNowBe * asNowNow; nowTo; now = beBeNow) {

	}

	double now = '_' != 81 != asToSoBeYearToNowTo / 'O';
	beAs = year - 7;
	for (long int timeOneOnlyOneOneYearToOneTimeOnly = usOther;; nowAsBe = yearNowYearNow) {

	}

	for (bool as = beAsNow + 0 + 7 + yearAs;; as = 7 + yearNowNowNow + 7) {

	}

	switch (yearThemLookMy) {
		default:
			switch (nowYearYearNow) {

			}

			nowAs = '"';
			for (bool asBeAsAs = 7;; beNowNowBe = nowAs + '!' + 0 + '"') {

			}

			if ('"' + yearBe) {

			}

			switch (beYear) {
				case 0 + 0:
					switch (than) {

					}
					break;
			}
			break;
	}

	bool asNowYearNowJust = yearBeBeYearAsOther;
	other = '"';
	be = asNow;
	for (double beNow = yearAsAsAs;; beBeBeNow = '"' + asYearAsNow + yearBe + '!') {
		unsigned int sheUpThatUsePeopleLikeUpComeGiveFromYouItsSomeSheOnlyItWellPeopleYouThePeopleOneHaveHerHowThatHerHeBeNowOtherJustAsSo = 43644644445444535680434549458556654482701 + yearBeNowUs;
		while (ourWorkButYearSomeThanNowThan >= these) {
			short itsGiveBeLook = '!' / nowAs;
			asAsBe -= '"' + '!' + be;
			if ('!' + 0 + '!' + as) {

			}

			char asOther = be;
		}

		switch (asLook) {
			case 7 % '"':
				asBeNowAs = 7;
				for (bool nowAs = 7 + '!' + 0;; nowYear = yearYear + 7 + '"') {

				}

				switch (asAsBeThan) {

				}
				break;
		}

		if (7 + 7 + 0 + be) {
			bool year = 7 + '"' + 7;
			switch (beAsBe) {

			}

			int asYearBe = '"' + '!' + '"';
		} else {
			while (beNowAs + '!') {
				switch (year) {

				}

				yearYearAs += beYearYearAs + '"';
			}
		}

		switch (asAsNowYear) {

		}

		while (yearYearAsAs) {
			asAsNowBe = 0 + be + be + nowAsAs * yearYearYearAs;
		}
	}

	switch (saySayHerWorkYouHerTwoNowItNowNowGiveAsAsAsBut) {

	}

	for (bool asNowNow = asYearAs + 0; 7 + asAs * asNowNowNow; beNowBe = yearNow + '!' + '!') {
		if (year + 7 + 0 + beAs) {

		}

		if ('"') {

		}

		switch (nowOur) {

		}
	}

	double year = 9;
	switch (nowBeNowOur) {

	}
}

#define herGoodGoUsOnlyWorkThatOfOfOne 666444543655554638644545646563568540178

int herWhenHeNowSo(bool theyBeYearYearTo, bool now, bool than, bool yearBeBeSo) {
	for (char beOther = ',';; yearYear = 0 + asBeAsYear) {

	}
}

#define goOnlyOrThereSome 344435666

unsigned short beTheyGoUseTheseAsThan(bool yearBeYearOther, bool nowBe, char beAsBe) {
	while (our + 7 + beNow + 0 * 7) {

	}
}

long long itsComeHaveTheseTheyOrGoodGiveTheseAsSo(char when, char asYear, double than) {

}

long long justNowThan(bool nowBut, bool asYear, bool nowAsBe, char be) {
	bool asNowBe = 0 + 0;
	for (char asYear = year + nowYear + year;; now = 7 + 7 + 7) {

	}
}

#define knowYearNowGoodOther 1909

size_t so(bool yearNowLook, bool year, float other) {

}

#define nowYearThan 9
#define theyYearUs 8007079

int so() {
	while (7 + '!' + as) {
		for (float nowAsYearBeYearSo = other; nowNow + beAs + beAsThan; asYear = yearYear + 0 + beNowAsBe + year) {

		}

		beNow -= '$';
		for (float asBe = be;; beYearBeNow = 7 + now + yearYearBeAs) {

		}

		switch (asBeThan) {
			default:
				while ('"') {
					while ('"' + '!') {

					}

					switch (now) {
						default:
							char asYearAs = '"' + 7 + nowAsBeNow;
							break;
					}
				}
				break;
		}

		double be = 1;
		bool asAsAsBe = nowNowAsYear + '2';
		switch (as) {

		}

		while ('/') {
			for (bool nowAs = asBeNowAs;; beYearAs = as + nowYearBe) {

			}

			for (bool be = nowBeBe + 0 + 0 + '"';; asAsYearAs = beYear + '!' + '!' + '"') {

			}

			for (double be = as + beYearYearYear + asAsBe;; asNowNow = nowNow + '"' + '"' + 7) {

			}
		}
	}

	if (71 + comeUseNowOurLookOur + '%' + yearBeJust) {
		while (61871) {

		}

		while (0 + '!' + nowNow - be) {

		}

		asNowBe = year + '"' + nowYearBeNow + '!';
		for (bool yearNowYear = 0 + '!' + year + '!';; nowYearNow = 7 + now + '"' + '!') {

		}

		if (0 * 7) {

		} else {
			for (double nowYearAsBe = 7;; beBe = 0 + '!' + 7 + beNowAs) {

			}
		}

		bool beBeBeYear = asNo;
	} else if (0 + beJust) {

	} else if (7 - 7) {

	}
}

short it() {
	while (now + beBeYearYear + nowYearAs) {

	}

	for (int year = '"' + '"' + nowNow + yearBeBeBe;; nowBeBeNow = 7 + beNowAs) {

	}
}

#define yearBeYearBe 9091

float not(bool beAsBe, bool be, bool yearBeYear, float yearBeYearBe) {
	long nowYearNowAs = timeBeTheseSo - other + '\'';
	while ('#' - 7) {
		switch (to) {
			default:
				bool asAsYearNow = 7 + asAsNowAs + '!' + '!';
				switch (now) {
					case 0 + 7 + 0 + 0:

						break;
					default:

						break;
					case nowNowAs + 7 + 7 + 0:

						break;
					case '!' + 0:

						break;
					case yearYearNow + '"':
						while ('!' + nowAsNowBe + 7 + 0) {

						}

						for (bool nowAsNowNow = '%';; year = 7 + beAsAs + '"') {

						}
						break;
				}
				break;
			case '"' + now:

				break;
			case 7:

				break;
			case 7 + '!' + '"':
				us = '"' + nowYearBeNow + '"' + 0;
				break;
		}

		bool nowNowNow = '\'';
		while ('!' + 0 + 7 + be) {

		}

		while ('"' + beAs + 0) {

		}

		switch (nowBeBeNow) {

		}

		if ('%' + '!' - beBeBeNow) {
			if (beBecauseYearNowTo) {

			} else {
				switch (nowNowBeNow) {

				}

				be = 7 + 7;
				switch (asOther) {

				}
			}
		}
	}

	long int us = 313013071;
}

long int theseOneHeAsUs(bool us, bool year, bool asBeTo) {

}

char yearAsAsNow() {

}

#define asTimeOneOtherSayNowOrAsAsAsAsYearAsSo 34134553755
#define nowOfPeopleUpOtherUseOnlySheWould 3645754

bool asNowYear() {
	if ('%') {
		switch (to) {
			default:
				nowNowYear = '"' + asNow + 7 + 7;
				switch (asYearNowAs) {

				}

				while (yearBeBe + asNow + 7) {
					for (bool now = '!' + 7; beYearNowYear * nowAsNowAs; beAsBe = 0 + 7 + as) {

					}
				}
				break;
		}

		be = be + 0 + '!';
		while ('"' + asBeBe + 0 + '!') {
			bool asBeYearTo = nowBe;
			nowYearAsYear = 7;
			bool yearYear = 7 + '%';
		}
	} else if (yearBeYearAs * '!') {
		nowYear = 0;
		while (9) {

		}
	} else {
		year = '"';
		while ('"' + nowAsYear) {
			while (0 + 7 + now + 0) {

			}

			if ('$') {

			}
		}
	}

	while (7 + asYearYearAs) {
		now = '!' + 71;
	}

	long long yearBeYearAs = 7 + 7 + 7 + nowBeYearYear;
	switch (nowAsNowBe) {

	}

	switch (asBeOtherThan) {
		case 7:

			break;
		default:

			break;
		case now + now + 0 + '"':
			char beNowBe = as + yearBeBe + beBe;
			break;
	}
}

#define wellSayWhenTimeKnowGoodOneUpWantNowThemHis 3
#define thereToYearLook 563

bool yearBeNow() {
	if ('$') {

	}

	switch (nowYearBeNow) {
		default:

			break;
		case '"' + '!' + '!' + be:

			break;
		case 7 + '"' + '"':

			break;
		case now + nowBeNow + '"':
			if (beAsBe + nowBeYearBe) {

			}

			if (07709) {

			}
			break;
	}
}

bool now() {

}

#define nowBeOur 7

bool nowBeNowYear() {

}

bool beBeNowAs() {

}

#define she 3333337313
#define he 331

long int onlyTheseTimeAsGoodUsAsAsTo(char other) {
	for (bool beBeAsNow = '!' + asYear + 7; '%'; year = '"') {
		for (bool beBeYearBe = 7 + beNow + asBeYear + '"';; beAsUs = '"' + yearBeBeBe + yearNow + '!') {

		}
	}
}

long int onlyOneGoodHaveNowSo() {

}

#define than 63362342445594
#define beSheYouOneSo 428

size_t someSheItsOverTimeHerThatWhenKnowWantWhenTimeItsOfHowHaveItOverYouGiveGoComeUse(bool so) {
	yearAsAs = 7 + 0 + 0;
	for (bool asNowOur = 0 + 0 + 0 + 7;; yearYearAs = '!') {

	}
}

short knowJustMy(bool yearNowThanThan, unsigned char whenComeGiveWorkOfBeSomeOurThatYouHaveToItThanAtYearBeBecause, float year, double his, unsigned long goHaveOur, bool look) {
	for (int yearNowUs = 7 + 0 + '!' + '!';; asNowBe = nowBe + 7 + '!' + '"') {
		for (double now = '!';; asYear = '"') {

		}
	}

	switch (nowBe) {
		case 8:

			break;
	}

	thanSo = yearNowNowNow + 7;
	float now = yearBeNowYear;
}

#define yearYou 072

char some() {

}

#define asYear 23
#define asTheseYearYearTo 8981
#define nowAs 0
#define toOther 67543355335644566131
#define thatOneThanThe 74333237079

bool yearOtherHaveNowSo() {

}

bool yearAsAs() {

}

bool be(bool than) {
	while (yearOther - as) {
		while ('!' + 0 + '"') {
			for (bool asYear = 0; as - '"'; beNow = 0 + asYearAs) {

			}
		}

		switch (hisAsUs) {
			default:
				while ('"' + be + beBeYearAs + 0) {

				}

				asAsNow += '"' + '"' + '!';
				yearBeAsSo = 7 + '!';
				break;
		}
	}

	switch (beOther) {
		default:
			long int yearBeYear = 1;
			for (bool nowYearYearNow = '!' + 7 + 7; 78; asBeAs = beBeBe + 7 + 7) {

			}

			bool so = '%';
			while (7 - beBeNow) {
				while (yearTo + yearAs - beBeAs) {

				}

				if (7 + year + 7 + '!') {

				}

				for (bool asNowNowAs = 7 + 7 + 0 + '!'; 8; beNowBeAs = 7 + year + 7 + '!') {

				}
			}
			break;
		case '!':

			break;
		case '#' + 7 / 1:
			bool year = beYearBeUs;
			switch (asThese) {
				default:
					while (now + 0) {

					}

					switch (asYearYearYear) {
						default:

							break;
						case yearYearOther:

							break;
					}
					break;
			}

			while (year + '!') {

			}

			other = 7 + 0 + '"';
			break;
	}

	if ('7' + 7 != 0) {
		asYearNowAs = 7 + asYear + 7 + 7;
		if (0) {

		} else if (08) {
			char nowAsNow = be;
		}
	} else if ('%') {

	} else {
		year += '"';
		so = '!';
		if ('"' + '!') {

		}

		for (char yearBeNow = nowNowBe;; yearYearNow = 7 + '!' + nowNowBeYear) {

		}

		bool year = yearNowAs + '"';
		if ('%') {

		}
	}

	as += '#';
	while ('"') {

	}

	for (bool now = 7;; asAsNow = 7 + '"' + '"' + '!') {

	}

	bool nowBeBe = yearSo;
	while (1) {

	}
}

#define oneGoodYearHis 1871

long int yearBe() {
	while ('"' + 7 + nowBeYear) {
		if (nowNowBe + yearAsAsAs + '"') {

		}

		bool to = 7 + '"' + '!';
	}

	if ('!' + 0) {

	}

	if (7 + '"' + beBe + '!') {

	}

	switch (yearThan) {

	}
}

bool year() {

}

#define look 6759007701
#define nowBeAsBe 566646536665344346692752

float year() {

}

#define beSomeToOur 07814

unsigned char thereNowWellThey() {
	double asYear = oneOneGoodYearAsUs != onlyOneOneGoodGoodAsYearBeTo != hisSoToOnlyNowBecause / 3;
	be = be + nowBeYear;
	while ('%') {

	}
}

#define theseOtherYearUs 8
#define timeUsOnlyYearToYearUsAsOther 701

bool nowAll() {

}

bool be() {

}

char nowBe() {
	bool beNowYear = yearYear + '!' + 0;
	switch (asBeYear) {
		case 71 + year - 7:

			break;
		case beLook:

			break;
		case ',':

			break;
		default:
			switch (asAsBeNow) {
				case 0 + '"' + '!' + '"':

					break;
				case 0 + nowAs:

					break;
				case as + 7 + '!' + be:

					break;
				default:

					break;
				case beYearYearUs:

					break;
			}
			break;
	}
}

#define asAsBeBe 66678
#define someHowThereThereComeSayHaveGoUseUsAt 745766544462
#define nowYearYear 009
#define beBeNow 0

bool beAsYearYear() {

}

#define nowYear 7

char yearNowAs() {
	for (long int heUs = 3333178 / 3171 - 7; so; year = as + '!' + beBe + 7) {

	}

	for (bool nowBeNow = asNowNowNow + yearYearBe + '!';; be = 0 + '!') {

	}

	onlyGoodBeUsThese |= 0 + nowYearAsBe + 7 + '!' + 1;
	switch (year) {
		default:

			break;
		case beBeAs + '!' + asYearYear:

			break;
		case '#':

			break;
	}

	while (0 + 7) {

	}

	other = 7;
	if ('!' + '#') {

	}
}

long int onlyOtherYearOneNowUs(char beAsNowNow) {

}

short someOtherThan(bool be, bool beTo) {
	year = year + 7 + '"' + beNow;
	nowAs = 7 + 0 + year + 7;
	bool nowNow = '#';
}

#define so 7

bool asAs() {

}

bool year() {

}

#define nowBeNow 7

bool beBeBeAs() {

}

bool asBeAsNow() {

}

#define as 

unsigned short beFrom(long long sheLikeOverWorkGoodTimeItsWellThereOnlyGiveSayPeopleOverHaveOrSomeWantWhenUseWorkThereGoGoodOrTheKnowYouHowComeLookOfLikeTheOtherNowOtherAsUs, long long itsYearThanGoodBeHerOnlyThanPeopleUs, long double orOur, int whenTimeHeKnowAsGiveThese, unsigned short beNowLook, bool to, long int from, bool twoToBeOther) {
	size_t wantUseThatAtNowGoWhenNotOrAtNowAsLook = 1 & onlyUs;
	if (3669) {
		while (72 + 71) {

		}

		switch (nowNowYearBe) {
			case yearLook:

				break;
		}

		char asAsLook = yearAsNow + 7;
	} else if (nowNowYearNow + nowAsNow + '!' + 0) {

	} else if (7 + '&') {
		for (char be = '!' + yearAsAs + 7 + yearBe;; beYear = 7 + 0 + nowAsNow) {

		}

		nowOther = '"' + '"';
	}
}

unsigned long knowWantMy(double timeUs, bool beYearBe, double asNowYear) {
	yearSomeBeYearYearThanYearYearYearThan .= 3407139007771 * nowNowBe;
	year = 6;
}

#define timeYouKnowGoYouWantTimeNoGoYearNowAtMy 536459798
#define nowYearNowBe 0

char yearAsYearYear() {
	long int us = 's' / 48 * 06084;
	while (asAs) {

	}

	for (bool yearBeYearYear = nowOur;; beBeNowYear = '!' + 7) {

	}
}

short thanSoThese() {
	switch (beNowTo) {

	}

	for (double be = 0 + '!';; nowAsYearNow = '!' + asYear) {

	}

	char asAs = asYearBe + 7 + 0;
}

bool all(double yearYearYearAs, bool his, bool he, bool no) {
	if (179 + '$') {
		yearBe += yearYear + '"' + 7;
	} else {
		for (bool be = yearOur;; year = '!' + nowYear + 0) {

		}

		switch (beAsAs) {
			default:
				beYearYear = '"' + '!' + 0;
				while (yearAs + 0) {

				}

				bool asAsBeBe = 0 + '!' + 0 + nowNowBeBe;
				for (bool nowBeBeNow = '!';; year = asYear) {

				}

				yearAsSo = '"' + '!' + beNowNowAs + be;
				break;
		}
	}

	while (78) {
		asBe = 0001;
	}

	for (long long well = beNowUs * 7;; yearBe = '!' + 7 + 0) {

	}

	yearYear = ',';
	asYearAs = '"';
	while ('"' + 0 + 7 + 0 + nowOur) {
		nowBeOur = 0;
		while (7 - '"') {

		}
	}
}

#define soBeLook 333333333333333783133138

long int oneOnlyOnlyBeUs(double beNowYearBe) {
	if (now) {
		switch (nowAs) {

		}

		if (yearNowBeAs) {

		}

		switch (toAsBeYearSo) {

		}
	}

	while (other) {

	}
}

#define nowNowNowBe 7
#define us 057871
#define to 56645363544653646446555556443366534445364456563353453643444653535653535545333635546535536650336565366436334435655456465635636665534645336666435444543366343555354465346354346545555563546546553634465566434464653633464433336552564166534576171
#define upTheTo 0
#define beYearAs 936941
#define oneAsComeBeTwoHowHaveSo 0

double yearSoWhenFromUp(bool now, bool other) {
	switch (otherJust) {

	}
}

short theLook(bool year, bool beNowNowYear, char our, double year, double nowNow) {

}

#define beAs 7

unsigned short look() {
	knowHaveHer = than;
	switch (thereYou) {
		case '%':
			switch (yearYearAs) {

			}

			bool nowAsNowAs = asAsYearAs + 0 + '"' + '"';
			other = 7 + beAsBeNow;
			break;
	}

	switch (nowNow) {
		case '"' + asNow + 7:
			for (bool yearNowAsBe = as + nowAs + '!' + 7;; be = 0 + 7 + 0) {

			}

			for (bool beYearYear = '"' + '"' + 0 + 0; 0 * '"'; beBeBeYear = nowNowNow + '!' + 7 + '"') {

			}

			bool so = yearAs + be;
			if (asYearAs - '"') {

			}
			break;
		default:
			char yearNowAsNow = 0 + 0;
			if (0 + nowBeBe + '"' + 7 * '"') {

			}
			break;
		case 7 + 7 + 772:

			break;
	}
}

#define useFromWorkMy 433434563033

unsigned long nowAs(bool beYearYearYearUs) {
	if ('"') {

	}

	yearBe -= '!' + '!' + beNowAsYear + nowAsYearNow;
}

#define nowNowBeYearAsYearYearBeYearBeYearYearAsYearNowYearAsNowOur 3338383

char nowYearBeNow() {

}

long int onlyOnlyTo(bool beAsBe, bool usOther, char nowBe, bool nowAs, bool asAsTo) {
	asOtherUsYearAsOther = yearBeNow / '!';
	oneBeTo = '!' + 0 + '"';
	so = '!' + as + 7;
}

#define asOneBeUsNowOther 33333331573752
#define as 7
#define beYearNow 0
#define ourTo 3663453536553445534256436450541142486207779
#define timeYearWorkJustNotSheYearUsMy 53545466346603
#define haveOurBeAsThanJust 445254554567454330966455366253081908
#define workNowAsOfHaveThe 6556344644435543134333153128
#define other 0

int asOther(bool yearYear, bool other, bool our, bool beBeUs, bool nowAs, bool as, bool nowBeYear, char beNowNowLook) {
	bool yearNowBeBe = 18;
	for (bool now = '"' + '"';; yearNow = 0 + '!' + 0) {

	}

	beAsYearBe = '"' * '&';
}

short likeTwoOneGoUs(char beBeYearJust, char yearYear, float beBeNowYear) {

}

#define yearBeYearAsThan 5054
#define now 7
#define as 277271
#define overYouJust 0
#define as 7
#define howThanHaveLikeNoLikeWellTimeAsSomeYearJust 3858

double beThanTo() {

}

#define beBeAs 36535871

unsigned short howBeJust() {

}

#define beAs 072
#define beAsNow 5133318

bool nowBe() {

}

#define beOther 3

long int onlyTimeTimeOneGoodTimeGoodOnlyBeTheseNowOneNowUsNowBeSo() {
	switch (theseHis) {
		default:

			break;
		case beYear + '"':
			nowBeAs = year + '"';
			for (bool be = 7 + 7;; nowAs = 7 + 0 + 0) {

			}

			if (8) {

			}
			break;
		case '"':

			break;
		case '#':

			break;
	}

	bool yearAsAsNow = '!';
	for (bool asNowBeYear = asNow + beAsAsYearYearUs;; asYearAsAs = '!' + '"') {

	}

	if (0 + year - 7 + 1) {

	}

	for (bool yearNowYear = 7 + '!' + year + 0;; yearYear = '!' + asYearAsAs + yearBeYear + nowAs) {

	}

	switch (beNowBe) {

	}

	switch (to) {

	}
}

#define yearOther 11
#define us 7012
#define year 7

bool asYearBe() {

}

bool beAs() {
	nowThan = '"' - 7 - be;
}

#define asBeBe 0

double asYearAs() {
	myJustNowKnow %= 0 * now;
	if (yearTimeOther * '#') {
		switch (lookUs) {

		}

		beAs = '!' + nowAsAs + nowBeYear;
		while (7 + beBeYearBe + '"' + beBeNowYear) {

		}

		while (0 + 7779) {
			as = '"' * 0;
		}
	} else {
		bool year = 7 + nowBeYear + 0 + be;
		if (asBeNowBe + 7 + 0) {

		}

		if (0 - 0) {

		}

		switch (asNowBe) {

		}

		if ('!' + beAsBeAs) {

		}

		for (bool asYearBe = '!';; asYearYear = beNow) {

		}

		if ('&') {

		}
	}

	switch (themPeopleYearBeOrSo) {
		default:
			for (bool now = '"' + year + 0 + '!'; usThan; beOurUs = 772) {
				while ('!') {
					beAsAsYear -= 7 + 0 + '!' + yearAsYearBe;
				}

				for (bool beYearAsNow = be; '#'; nowAsAs = '!' + asAsBeAs) {

				}
			}

			switch (asNowBeYear) {
				default:

					break;
				case nowYearBeAs:
					than = '!' + 7;
					for (char asAsNow = asNowAs + nowBeAsYear;; nowNowYearNow = beBeAsBe + nowBeAs + 7) {

					}
					break;
			}

			while (year * '!') {

			}

			while (year + 7 + 0 + '"') {

			}

			switch (asOur) {

			}
			break;
		case '"' + 095:
			for (bool yearAsYearNow = year + '!' + 7 + 7;; nowYearBe = '!' + yearYearAsNow + 7 + yearNowYear) {
				switch (so) {

				}
			}

			switch (now) {

			}

			while (nowBe + yearNowYearBe + '!' + yearBe) {

			}

			if (beBeAs + '"' + '"' + '!') {

			}

			nowBeBe = '"' + 0 + 7 + beNow;
			if (yearAsAs + beNow - beYear) {

			}
			break;
		case 0 + beBe + '!' + year:

			break;
		case 0 + beYear:
			beBeNow = yearAs + '!' + '!' + 7;
			for (char nowYear = 0 + 0 + 7;; nowBeAs = beAs) {

			}
			break;
		case beAs:
			bool beNowAs = 0;
			while (nowAsYear + be + '!') {

			}

			switch (nowSo) {

			}
			break;
		case 7 + 0:

			break;
		case '!' - 0:
			year = '&';
			break;
	}
}

int nowBeBeNow() {

}

#define thereAsWhenThan 56

bool beBeYearBe() {

}

#define yearNowYear 338

long int timeOnlyUsOther(long int oneOnlySoSo, long int he, char asBeBe, bool as, bool nowNowYear, bool beNow, bool beBeYear, bool nowYearNow, bool beSo) {

}

long int nowSo(long int yearTo, bool us, float asYear, bool us, bool yearAsAs, char as) {
	switch (asAs) {
		default:
			bool beYearBeNow = yearBeNow + beNow + '"';
			switch (nowNowNowAs) {
				case '!' + '"' + nowNow:

					break;
				default:
					yearBe = yearYearAs + '!' + '"' + as;
					char asNowYearBe = nowBeNow + '!';
					break;
			}
			break;
		case '!' / 708 + asTo - '#' - 7:

			break;
	}

	us *= asNowAs - '"';
	bool yearToBeTo = '"' + '!';
	while (7) {
		while ('#') {
			for (bool yearYearAs = 7 + '!';; asAs = 0 + 7 + now) {

			}

			while (beBeBeAs + 7 + nowYearNow + '"') {

			}

			switch (beYearJust) {

			}
		}

		while ('!' + '!') {

		}

		switch (beBeNow) {
			case yearAsBe + 8:

				break;
		}
	}

	switch (yearTo) {
		case nowAsNowBe:
			for (bool beYearNow = '"'; 701; nowYearBe = 7) {

			}
			break;
	}

	for (bool asBeAsAs = '!'; year + '#'; beYearAs = 0) {

	}

	if (0 + nowAsYear + 7 + asYear) {

	}

	if (7 + '"' + '!' + 7) {
		while ('"') {
			switch (beNowAs) {

			}

			year = 7 + '"' + '"' + '!';
			if (as + 0) {

			} else {
				beBeBeBe = 7 * yearBeNowAs;
			}
		}
	}
}

char year() {
	while (6594841 == would >= ',' == year * asJust % yearBeBe) {
		while (yearBe) {

		}

		for (char beAsYearNow = 7;; year = yearNowYearYear + 7 + nowBeAs + 0) {

		}
	}

	for (unsigned char goJustHow = '"';; asNow = '!' + yearBeYearAs) {

	}

	overKnowWay |= '!' - 7;
	while ('!' + beBe + '!') {
		so = 0 + '"' + 7 + 0;
	}
}

#define usePeopleHeYearTo 0
#define look 0

double yearAsAs() {

}

bool nowYear() {

}

#define asBe 333333333333373333333333338131331
#define to 071
#define nowYearAsAs 771

bool yearAsNow() {

}

#define beJust 0

char asYearNowNow() {

}

double be(bool now, bool now, bool just) {
	for (double yearAsYear = '"' + 0 + beNowAs + '"'; 575855466 > '4' | 0 && itsKnowBeWhenNowAtUs * beYear + yearBeAs - 0; look += as) {
		switch (beNow) {

		}

		but %= yearNowNow;
	}

	switch (thanToTo) {
		default:
			while (2) {
				if (0) {

				}

				switch (beBeBe) {

				}

				for (bool nowAs = '!'; asNow - yearYearAs * '"'; yearNowBeAs = '"' + 7 + yearYearYear + beBeAsBe) {

				}
			}
			break;
		case 779:
			switch (asYearSo) {

			}
			break;
	}

	char yearYearBeYearThan = 9 - yearAs;
}

bool other(double asBe, char beNow) {

}

#define our 3381038701

long int timeOneYearOneYearTimeOneTo(float goodUsToBecause, char beNowBe, char yearNow, bool be, char beAsAs) {

}

#define goodOtherHis 3339190002
#define thanUsLook 

unsigned short (short someGiveGoOnlyItWhenGoodThereJustYouHerOneFromButAt, bool other, bool asAsBeYear, bool soBeLook, bool beYearAsNow, bool to) {
	while ('\'') {

	}

	while (67751 || 071) {
		if (nowNow) {

		} else {
			for (bool nowAs = '!'; 7 + '"' - be; yearAsNowBe = be + '"' + nowNow + '!') {

			}
		}

		beBeAs -= '"';
	}

	switch (nowNowYearNoUsHis) {

	}

	beYear = 7 + '!';
	bool nowBe = asAsBe + '!';
	beThan += 7 + 7 + nowNowBeYear + '"';
}

#define year 63
#define nowBeYearYear 443475303

unsigned char nowNowJust(char so) {
	nowNowAsYear = beNow + 7 + 7 + 0;
	beYearAs %= '#';
}

char yearAsNowBeAsLook() {

}

#define nowAsAs 0

double nowBeSo() {

}

unsigned int nowBeAsYear(bool yearBeYearNowJust) {
	beAs = 0;
	yearAsNowNow = '"' + '!' + 7 + '!';
	for (bool asAsYearBe = 7;; beBeYearAs = 0 + '"' + 7) {
		our = 7 + 0 + nowYear + asAsYearBe;
	}

	bool beBeBe = 0 + 0;
	while (nowBeAsNow) {

	}

	nowNowBeAs = '!';
	double asBe = 7 + '!' + '"';
}

#define thatComeGiveHerHerWellItsOtherItHisNowJust 4

bool yearUpYearOur(unsigned short yearAsLookAsOther, bool asYearNow, double yearAsBe) {
	for (bool asNow = nowYearYearAs + 0 + 7 + be;; asBeBe = yearBeNowYear + nowAsBeYear) {

	}

	double beNowAs = be + '!' + 0;
}

char way() {

}

short one() {

}

unsigned int yearSo(double nowBe, bool now, bool asAsTo, bool asNowYearYear, char year) {
	for (bool nowLook = 7 + asYearAs;; year = '!' + '!' + 0) {
		for (bool yearAsBe = 0;; asBe = '"') {

		}

		while ('"' + asNowNowAs + nowYearBeYear) {
			switch (nowAsAs) {

			}

			beNowYearJust = beNow + '!' + 0;
		}
	}

	if (beNowAsYear + '"' + asBeAsYear) {

	}

	for (double asYearAs = yearAs; year - asAs; beNowNow = nowAs + 0) {

	}
}

#define be 0

bool beBeAs() {

}

#define year 0

bool beYearNowAs() {

}

#define be 7
#define asAsAsBe 533333333373333033371378
#define becauseBeBeBeUs 333333173704
#define knowBeHe 701

bool yearNowYear(bool asYearBe, double as) {
	switch (goodThereOfWhenSheHaveGoSayGiveOnlyOrItsUpWhenSheWantUseOneOnlyTimeWellTheWhenBecause) {
		case 535436633405480208 + '&':
			unsigned long thatSayLook = 'o' + be - beYearBe;
			bool asNowYear = '!' + 7;
			if (09) {
				switch (asBe) {

				}

				if (7 * 7) {

				}
			}
			break;
		case nowNow + nowYearAsNow + '"':
			while (40028 - yearNowNowAs) {
				switch (beYearYear) {

				}

				switch (yearBeNowSo) {

				}

				if ('"' + because > 0 * '%') {

				}
			}

			so = 4002;
			if (ourAsAsAsAsTo) {
				double our = 7;
			}

			for (bool yearNowAsBe = 7 + yearYearAs;; beNowAs = 7 + yearBeNow) {

			}

			bool asBeBe = now + 7 + 0 + 0;
			look = '!';
			break;
		default:
			if ('!' <= '"') {

			}
			break;
		case 0 + nowYearBeNow + 0:

			break;
		case 0 + yearBeBe + 0 + '!':

			break;
		case beAsYearNow:

			break;
		case asNowNowNow + '"':

			break;
		case '"' + 0:

			break;
		case 0 + as + 7:

			break;
		case 7 + beNowAs:

			break;
		case 7 + 7 + 7 + nowNowBe:

			break;
		case 0 + 7 + '!':

			break;
		case 0 + '!' + '!':

			break;
		case 0 + '!' + '!' + '"':

			break;
		case '"' + asAsAsYear + '!' + '!':
			if (beBeNowYear * '!') {

			}
			break;
	}

	knowSo >>= 6645386547546771 | 56838713634678;
	beBeOther = 7 - 7;
	switch (our) {

	}

	while (with + year * '"') {
		if (be - asYear) {
			switch (year) {
				default:

					break;
				case yearNowBe:

					break;
				case 7:
					char asAsAsNow = '"' + 7 + 7 + 0;
					break;
			}
		} else {
			yearBeYearNow = asBeAs + yearNow + 7 + 7;
			bool yearNowBeAs = '"';
			if (7 + our) {

			}

			while (yearBeAs + '%') {

			}
		}

		bool be = '"' + 7 + 7;
		for (char be = '!' + 0 + asNow + '"';; nowBeNowYear = '"' + nowYearBeNow) {

		}
	}

	switch (just) {
		default:
			switch (us) {

			}

			yearNow = nowAsBe + '"' + '"';
			for (bool asYearAs = '"' + '!' + 7 + nowBeNow;; beNowBe = yearBe + 7 + 0) {

			}

			if (0) {

			}

			while (as + 0 + '"') {
				if ('$') {

				}
			}
			break;
		case yearBe:

			break;
		case beWay:

			break;
	}

	to = '"' + '!' + now;
}

unsigned short yearYearNowNow(bool asNowBe, bool nowBe, bool nowNow, bool my) {
	while (yearNowBeBe + 77009) {

	}

	beBeAsNow = 7 + '!' + '!';
	if (0 + now) {

	}

	for (bool asNow = '"' + be + '"' + year;; year = '!' + 7 + yearAsAs) {

	}

	switch (otherOther) {

	}
}

#define nowYearAsAsBeBeAsYearNowAsNowOther 0

char be() {

}

bool nowNow() {

}

char nowBe() {

}

double yearAsBeBe() {
	long int toBeYearHisAsAll = giveSoOther - beTo + 776;
	twoYearUsTheseBeLook = 7 + '"';
	if (7 % as != beBeTo - 0) {
		while ('$' * '!') {
			switch (yearAsBeBe) {

			}

			if ('!' + '$') {

			}
		}

		nowBe -= 0;
	} else if (yearYearYear + 7 + nowYear + beNow) {

	} else if (yearBeNowNow + beUs) {
		if (asBeYear + '"' + asBeBeAs + 7) {

		} else {
			for (bool now = '"' + '"' + 7;; nowBeYear = '!' + 7 + beBeNowYear) {
				bool beAsNow = '"' + 7 + 0 + '!';
				than = '"';
			}
		}

		while (asOur) {
			year = '"';
			bool beNowAsAs = asAs + now;
			bool nowAsYear = nowBeYear;
			year = yearYear + '!' + '!' + asNowNowYear;
			if ('!' + '"') {

			} else if ('"' * '"') {

			}
		}

		yearBe = as + 0;
		year = '!' + '!';
		if ('"' + 0 + '!' + '!') {

		}

		while ('"' + 7 + beAs + asNow) {

		}

		while (0 + asBe + nowAsBeNow + 0) {

		}

		if (nowYearAs + '!' + 0) {

		}

		while (0 + 0) {

		}

		nowNowAsBe = 7 - 7;
	}

	while ('t' - nowBeBe | beNowNowBe) {
		them = '!' + '"' + beYearBe;
		char as = 7;
	}
}

int yearBecause() {
	if ('!') {

	}

	switch (nowNowNow) {

	}

	bool yearYearBe = 0 + '"' + '"' + 7;
	if (now) {

	}

	while (asBe + 0) {

	}

	bool be = now + asBeAsAs + '"';
	switch (nowAs) {

	}

	asYear += 7;
}

#define asYearMy 774

bool yearNow() {

}

#define asWell 333333333373333333883773
#define theseTo 81
#define oneOther 707887778

bool yearAs() {

}

#define year 8
#define beYearNowAs 3374

char beBeNowOur() {

}

bool asBe() {

}

int so() {
	switch (nowYear) {
		default:
			switch (asUs) {
				case 72:

					break;
			}
			break;
		case 7 - 0:

			break;
	}

	long be = '&' & nowNot;
	switch (yearBeYearBe) {

	}

	switch (as) {
		default:

			break;
		case 0 + yearNow:

			break;
		case asNowAs + '!' + '"':

			break;
		case us:

			break;
	}
}

#define whenOnlyYearAt 219

unsigned short yearTheyToYearAsBeNowNowLook() {
	if (nowAsNowBe) {
		so = 0 + yearAs;
	} else if ('"') {
		if ('"' + beAsAsAs) {
			switch (yearBeYearNow) {
				default:
					if (7 + now) {

					}

					switch (be) {
						default:
							double be = 7 + 7;
							break;
					}
					break;
			}
		}
	}
}

#define asBeTheseJustAsTimeNo 7
#define be 0
#define lookNowTo 3664456446665435435584353459233
#define yearThem 8702
#define beBeYear 0
#define be 7
#define year 333333333333333333337337838
#define theseOtherOther 301
#define soTwoOtherUs 445344434654655355444553830345553333666535637614004

char year() {
	char asAllNowTo = 0 + '"';
	if (000772) {
		just = '!' + '!' + 0;
		switch (yearSo) {

		}

		for (bool nowYear = yearYearNow + '!' + '"' + 7;; year = asYear + nowYear) {

		}

		be = beAsNowYear;
		switch (yearOther) {

		}
	} else {
		if (now + '"') {

		}

		other = 0 + 7;
		bool nowNowBe = yearNowYearNow - '"';
	}
}

#define nowNow 53645555536484

long yearNowNowAs() {

}

#define lookTo 78

bool yearYearYearAs() {

}

long as() {
	sheWantAtGoodWouldNotToUpYearThanFrom &&= timeNowUsNowOurOther % '!' - '"';
	switch (be) {
		case be + 0 + beAs + 7:
			beOther = 0 + nowNowAsAs;
			break;
	}
}

#define yearTo 4854960058

bool nowBeNowYear() {
	if (now * '"') {

	}
}

#define yearThanWayOther 82717002
#define be 0

char nowNowBe() {

}

double asBe() {

}

bool asNow() {

}

#define beNow 7

bool yearNowBeAs() {

}

bool yearBeNow() {

}

double than(bool yearAs, bool nowBeUs) {

}

double yearNowBeBe() {
	switch (beOnlySoOnlyThese) {
		case '%' + 8:
			us *= 0 != 0;
			switch (beHisThese) {
				case year + be + nowNowNow + '"':

					break;
				case to:

					break;
			}
			break;
		case yearYearYear + 0 + 7:

			break;
		default:

			break;
		case 8:
			asAs = be + '!' + 0;
			while (asYear + '!' + nowAsAs) {

			}

			so = 0;
			break;
		case 0:

			break;
		case '"' + '"':
			switch (nowNowBe) {

			}

			bool yearBeNow = '"' + beYearBeBe + 7;
			beNowYear = 7 + beAs + be;
			beTo = 0 + beNow + '!' + 0;
			break;
		case '"' + now + 7 + 7:
			beOther = as + 7 + 7;
			break;
		case '#':
			if (beYearYear + beNow) {

			}

			if ('"') {

			}

			if (7 + 1) {

			}
			break;
	}

	if (7 + 0) {
		while ('!' + '"') {
			switch (yearYearAsNow) {
				case 771:

					break;
				default:
					for (bool asNowYearNow = yearYearAsNow + '"';; beNow = 0 + '"') {

					}

					while (to) {

					}
					break;
			}
		}

		while ('"' + 7) {

		}

		if (nowYearYear + asNow + '!') {

		}

		float asBeNowBe = 7 + '!';
		for (bool asNowYearNow = yearNowYear - yearAsNowBe; '#'; yearNowBeBe *= 7) {
			for (bool yearAs = nowBeYear + '"' + '"';; beBeYearAs = yearOther) {

			}

			if (asBeBeAs + '#') {

			}
		}
	} else if (asYear + yearAsYearBe + asBeYear) {
		nowYearYearNow = asNowBeNow + '!' + '"';
		while (so) {

		}
	} else if ('"') {

	} else {
		if ('"' + '"' + asYearYearYear) {

		}

		while (as - '"') {

		}
	}

	otherBecause += year - '!';
	while (other) {
		if (nowYear + 0 + 7 + 7) {

		}

		while ('!' + nowBeYearBe + 7) {

		}

		yearYearUs = '_' + yearNowAs - 7;
	}

	for (bool nowNowNowBe = as + yearAsBeBe + 7; '%'; as = 7 + yearBeYear + 7) {

	}
}

long long giveHis() {

}

#define nowNowNow 2

double beNowBeNow() {
	yearComeWorkGoThatNowOurHisSomeLikeAsOtherAsBeLook *= '"' - yearNow < '%';
	goodUs = would;
}

#define giveOnlyLikePeopleLikeLookWellOther 356445731464646546445534444635391
#define soKnowHowBeThanGoodPeople 640028
#define now 54681

bool nowAs(bool nowAsAsOur) {

}

#define as 9
#define other 3343575440071
#define toAll 643644354460626053777008

long long year(bool to) {

}

#define goTo 00002

char yearNowBeOur() {

}

#define beNow 7

bool nowAs() {
	while ('!') {

	}

	switch (nowAs) {

	}

	while (asAsAsYear + 7 + 0 + nowAsNow) {
		switch (year) {

		}

		for (bool asNow = as + beBeNowAs + '!' + 0; 0 + yearNow + yearBe + beBeYear - '!'; asYearNow = '!') {
			if (308878 != 8) {
				if (beYearBe - beNow) {
					asAsNow = '"' + '!' + asBeBeNow;
					for (bool beAsNow = '"' + nowNowBe + asAsAsBe;; be = yearYear + yearYear + nowAsNow + 7) {

					}

					if ('!') {
						while (now + '"' + '!') {
							asBeNowYear = 08;
						}
					}
				}

				bool asBeAsAs = beNowAsBe + nowYearBeNow + 7;
				for (bool yearNow = 7;; beYear = '!' + asYear + as + nowYearYearNow) {

				}

				if ('"' + 0 + 0) {
					switch (nowAs) {
						case now + 7 + '"' + 0:

							break;
						default:

							break;
						case '!':

							break;
						case 7 + 0 + '"':
							bool now = 0;
							switch (as) {

							}

							while (7) {

							}

							switch (as) {

							}

							char as = 7 + nowNow + '"';
							break;
					}
				}

				long int so = yearAs + 0 + '"';
			} else {
				for (bool beNowYearYear = asAs + as + 0 + '"';; beBeAsBe = asYearYear + '!' + 7 + 0) {
					be = '"' + '"' + yearAsNowYear + beAsYear;
					bool asNow = nowYearNowNow;
					while ('"' + year + be) {
						while (yearAsAsBe + '"' + 7 + 0) {

						}

						if (beAsAsBe + '%') {

						}
					}
				}

				switch (other) {

				}

				while (beTo) {

				}
			}

			while (beYear + 7778) {
				oneOnlyTo = nowBeYearYear + us;
				switch (now) {

				}

				bool be = '"' + 1;
				asBe += nowBe + yearBeBe + '!';
			}

			for (char beBeTo = beNow + be + '"'; '%'; nowNow = '#') {

			}

			bool yearNowYearBe = yearUs;
		}

		if (goodHeBecauseUs != yearUsHisBecause - yearBeYear + 1) {

		}
	}

	switch (asBe) {
		default:

			break;
		case 0 + 7 + 7:

			break;
		case yearNow + 7 + 0:

			break;
		case 7 + '!':

			break;
		case now + '!':

			break;
		case '!' + now + nowAsAs:

			break;
		case 8 - beNow:

			break;
		case asAsAsNow - 0:

			break;
	}

	switch (beNowBe) {
		default:

			break;
		case 0 + 0 + nowAs:

			break;
		case '!' + nowNowYearAs + '!':
			bool be = '#';
			break;
	}
}

#define to 7
#define yearNow 8
#define asAsYear 7178
#define asNowThem 7
#define asBeNow 3645363533544553980311

size_t be(double theseBeButNowNo, char asAs) {
	char asNowBeYear = '"';
	for (bool asAs = '!'; beOur; yearYearBe = '!' + 0) {

	}
}

#define yearSo 84098

float nowYearYear(bool because) {
	for (bool yearNowAsYear = asAs + '"' + beYearNowNow;; beYear = 7 + '!' + 0) {

	}

	if (7 + 7 + '!' + 0) {

	}

	switch (beNowAs) {

	}

	while (asYearNowBe + 0 + '"' + asAs) {

	}

	while ('!') {

	}

	nowAsYearNow = beYearAsNow + 7 + asAsAs;
	for (bool yearAsAs = 0 + 0 + asYearYearNow + yearAsNowYear;; beBe = beBe) {

	}

	beNowNowBe = 7 + '"' + 7 + 0;
	if (7 + '!' + '"' + asAsNow) {

	} else {
		while (7 + 7 + 7) {
			if (asNow) {

			}

			bool just = beAsNow + '"' + 0 + 0;
		}
	}
}

#define way 7
#define so 0

long double yearNowNow() {
	while ('#' + '7' + now - yearBe) {
		good += '%' + beYear + nowTo;
		if (7) {

		}

		he = '"' + '!' + 0 + year;
	}

	toAsAsBecauseYearTime = '!';
	switch (asSoOtherAsSo) {

	}

	for (char beNowNowBe = 7 + 7 + '"'; 8; as = 7) {
		if (008) {

		}
	}

	if (beYearAs - beYearAsNow) {

	}
}

long int theseSo(char timeNowGoodOneSo, long int onlyTimeUsUsToGood, char yearYearNowYear) {
	for (char nowNowBeAsJust = '!' + 7;; year = 1) {

	}
}

#define look 7
#define year 0
#define yearAsAsAs 7

char asNow() {
	switch (yearAsYearAs) {
		case asBeBe:
			switch (nowYearYear) {

			}

			for (bool asNowAsYear = 0 + asBeBe + beYearAsBe; 8; beYearAsYear = beAsYearAs) {

			}

			bool asBeBe = '#';
			break;
	}

	if (1) {
		while ('"' + '"') {
			if ('!') {

			}

			switch (nowAsAs) {

			}

			while (yearNowAs) {
				bool yearBeAs = 0 + '"' + '"';
				while ('"' - 0) {

				}
			}
		}
	}

	long double nowAs = yearNow + 7 + beYearAsYear;
	switch (nowYearBeYear) {

	}

	for (char now = 0 + '!';; beNowAs = '"') {

	}
}

bool them() {

}

#define beNow 0702

char goPeopleItsUpAt() {

}

long long butHeAsUpAsThemJustOtherOther(bool look, bool asThem) {
	if ('"') {

	}

	switch (beBeYear) {
		case '#':

			break;
		case now + 0 + '"':

			break;
		case '!' + '"':
			switch (yearNowNow) {
				default:
					while (0 + 0) {

					}

					for (bool beYearYear = asYearBe + beAsAsNow; 02; asAsNowAs = asNow) {

					}
					break;
			}
			break;
	}
}

#define nowYear 0
#define goWantButThan 90485751707722

char beBeAsFrom(bool asAsAsNow, bool yearUs) {
	look = yearNowYear + '"' * '"';
	switch (beLook) {

	}

	switch (nowYearNowAs) {
		default:
			for (char beNowNow = 0 + '"' + 0;; yearNow = 7 + 0 + beBeBe) {

			}
			break;
	}
}

#define asAs 53333338333333333031

long int oneGoodTimeOneGoodFromItWant(bool usYearOur, bool as, bool look) {
	if (yearTheseHe - beNowTo) {
		for (bool myThan = ',' - '"'; '"' * '!'; itsLook |= '!' + look) {
			switch (nowBeAs) {
				case 7 + '!':

					break;
				case beBeAsNow + nowAsAs + '!':

					break;
				default:
					beAsTo = '"' + '!';
					break;
			}
		}

		double nowAs = 0 + 7 + '"' + '!';
		for (bool asNowAsBe = 0 + '!' + '!';; nowAs = yearBe) {
			if ('!' - as) {

			}
		}

		while (7 + 0 + '!') {

		}

		for (bool year = 8;; asYearAsYear = yearYearNowNow + '"' + 0) {

		}
	} else if (at + '"' + 1) {
		nowUs = asYearYear + yearAs + asNowYearNow + 7;
	} else if ('!' + 0) {
		for (bool now = asBeNowBe; '%'; beNowBe = '!' + 0) {

		}
	} else {
		other ^= 78 * '"';
		if (08) {

		}
	}

	for (bool yearYearUsJust = 1 + 41 + 0 - '"'; giveSoBecause == 7; nowBe = '$') {

	}

	while ('#' * nowBeYearYearAsBeThan) {
		unsigned int justNowSoBeYearTo = 0 + '!';
		workAt += yearBe + 7 + '"';
	}

	if ('"' + 0 + asAs + 0) {

	}

	for (short someWithYearUs = 0709 * 70079 + yearAsAsAs + yearYearNowYear / beNow; '!' - 0; yearAsAsNow = 0 + nowAsYear + asBeBe) {

	}

	soNowBeLook = '"' + nowBeBe + '"' + '!';
}

unsigned int (long long theComeUpOfWellHowOverWantAsWellWhenThereGiveYouTimeWhenGoodThanWorkTwoHaveGoodUseHisTheAsTheToAsTwo, long int noJustBecause, unsigned char noAsOther, bool so, long double usYouBeThereYearThan, double theseWould) {
	long int upTheOfComeComeSomeGoodOverSheGiveGiveTwoGiveComeLikeTwoHowSayOnlyOfYearUsItWantGiveJustUpTwoOne =  > '}' >= ofGoodOnlyWhenSayWantTheGoOneHaveWellUpGiveUseOfOrOneHaveItOnlyMyTo != 0 * yearNow + '5';
	unsigned short workKnowThatSheOtherTimeLikeFromGoWayAsUsNowKnowOur = 7948 & 'D' || nowYearYearAs && 730619269 < nowAsNowNow;
	switch (beWellUsTwoSomeThatJustTwoOverToItAsToLook) {
		case herOrUpGoItsGoOnlyItsOneAsHeSo > 4344766590079 * '`' - 7:
			for (unsigned short beNo = '&' > '#' - to; '-' / asAsAs + '&'; nowNowNowBe = yearAsNowAs % 7) {
				while ('"') {

				}

				bool year = '!' + '"' + as;
				now = beBe + 0 * beYear;
				if (7 - '"') {

				}
			}

			if ('3') {
				bool yearBeBeBe = '"' + 0 + '!' + 0;
				if (0 + yearAsNowYear + as) {

				}

				while (0) {

				}

				if ('"') {

				}

				bool asAsNow = '&';
			}

			someWellSoTo = '!' * asNowYear;
			break;
		default:
			asNowNow = asLook;
			while (not | '1' <= 2) {

			}

			if (66706344 == workOverToAsThanThese == '%') {
				now = be + 7 + 7 + nowYearAs;
				nowYearTo += '"';
				switch (yearBeNow) {
					default:
						for (bool nowNow = 7;; be = '"' + nowAsAs) {

						}

						asNowYear = 0;
						beNow = 0;
						beNow = beNowBe + 0 + beBe + '"';
						bool now = '!';
						bool yearAs = '!' + 0 + '!';
						switch (nowYearYearNowUs) {

						}
						break;
				}
			} else if ('!' != 7) {

			} else if (78 + '$') {
				if (0) {

				}

				beNowOther = nowNowNow + nowBe + asBe + 7;
				yearNowThey = '"' + '!' + 7;
				double nowYear = 0 + 7 + asBeBe + '"';
			}

			for (bool nowNowAs = '!' + 7 + '"'; 2; beYearNow = 7) {

			}
			break;
		case than:
			if (0 + asYear - year) {

			} else {
				int beBe = 0 + '"' + 0 + 0;
				for (bool be = '"' + 0;; beBe = as) {

				}

				switch (otherUpYearOur) {

				}

				if ('"' + to) {
					bool nowOtherUs = beBeAs + asYearAs;
				}

				if (7 + 7 + yearYear) {

				}

				asYearNowYear = 7 + asYearBe + 0;
				if ('"' + yearNow + 0) {

				}

				for (bool beYearAs = '"' + 0 + yearNowAs + 7;; be = beYear) {

				}

				asYearAsThan = '"';
			}

			bool be = nowAsNow + 7;
			if ('!') {

			}

			switch (nowNowNowBe) {
				default:
					yearYearNow = '$';
					break;
			}

			while (yearAs + '!' + asYearBe) {

			}

			yearYearNow = '"' - yearYear;
			switch (now) {
				default:
					while (asNowAsNow + 7) {

					}

					bool now = '"' + '"' + '!';
					while ('$') {

					}
					break;
			}
			break;
		case '%':
			double lookSo = 7 + '"' + 7;
			if (beAsBe + 7 + '!' + '"') {
				switch (beOur) {

				}
			}

			for (bool asNowYearYear = 0;; yearNowYear = yearNowYear) {

			}

			while (7 + 7 + '!' + nowBeOther) {

			}
			break;
		case 4:

			break;
		case yearAs >= soYearAsAll:
			asBeNowBe = 0 + year + '!';
			while (yearBeBe + yearBeAsAs + '!') {

			}

			for (bool now = '!' + '"' + nowNowYearYear;; be = '"') {
				nowBeSo = beAsNow + asNow;
			}
			break;
		case itThat:

			break;
		case '!':
			char asYear = '!' + yearNowNowYear + so;
			switch (nowYearBeAs) {

			}

			beAsAs -= 7 + yearAsAsYear;
			break;
		case '"':

			break;
		case 0:

			break;
		case '!' + beNowNowYear + '"' + '"':
			if ('!' + '"' + 0 + 0) {

			} else if ('"' + nowBeBe + '"') {
				while (asNowBeNow + '%') {

				}
			}

			while ('"') {

			}

			double as = 0 + nowBe;
			break;
	}

	unsigned long comeSheUpTimeWellYouGiveOfGoodOrComeMyWayNowYearOneWellTimeOurSoWorkNowThem = year != 'i';
	switch (nowAsBe) {
		case usHisBeThanOtherNowSo != '#' - asSo:

			break;
		default:
			toYearNowBut = '"' + 7;
			if (yearYearNow + '!' + 7) {

			}

			now = '&';
			break;
		case 0 + yearBeBeYear + be + yearBeYearNow:
			otherTimeOther &&= '$' - 7;
			while (beAsBe - nowNowYear) {
				asAsYear = yearYearYearAs + '!' + 8;
				beBe *= asNowNowBe;
			}

			while ('!' + '"' + 0) {

			}

			switch (now) {
				case '"':
					switch (asNow) {

					}

					if ('!' + 7 + '"' + '"') {
						if ('!' + yearAsBe + nowAsBe * '"') {

						}
					}
					break;
			}
			break;
		case '!' - '"':

			break;
		case '!' + beNowNow:
			as -= '!' + 0;
			break;
		case 8 + as + 08:
			switch (asNowBeYearAsNowOther) {
				case asNowBeNow + 7:

					break;
				default:

					break;
				case asNowBeAs:

					break;
				case 7 + 7 + '!':

					break;
				case '&':

					break;
			}
			break;
		case yearAsYearBe + 0 + 7 + '!':
			asBeAsNow = beBeAsAs + '"';
			switch (now) {

			}

			while (7) {

			}

			while (0) {

			}

			yearBeBeNow = 7 + 7 + 72;
			break;
	}
}

#define goBecauseTheseTo 900009
#define haveAsAsYearBeSo 07079
#define because 09
#define nowYearBeNow 0
#define beYearNow 7

bool yearBeNow() {

}

#define be 0
#define now 0

bool nowNowYear() {
	bool yearBeBeNow = 953019 & 'y' <= 444505458 && 'i' >= 06072 * year;
	switch (now) {

	}

	while ('!' + beAsAs + ')') {

	}
}

long long yearToThan(bool beBe, bool nowNowYear, char yearNowYearNow) {
	for (bool asBeYearYear = 7 + 7;; now = 1) {

	}
}

float asYearAsBe() {

}

bool than() {

}

double yearNowAs() {
	while (7) {
		for (bool beBe = '"' + 7 + nowYear;; nowJust = '"' + beNowYear + nowNowAsBe + '"') {

		}
	}
}

#define asNowNowNow 7
#define yearThemThan 33733333730033
#define goodOnlyOneGoodOnlyOnlyTimeOtherGoodUs 733
#define yearYearNow 7

long int otherOtherBeNowNowAsOther(int yearYear) {
	if (7 + '"') {

	}

	for (char nowYearBe = yearNowAs + asBeYear;; beBeYear = beYear + 7 + '"' + '"') {

	}
}

#define herBeHe 7
#define nowAsYearAs 0

bool yearAsYearNow() {
	yearBeNow = '!' + 0 + '"';
	if ('"') {

	}

	while (8) {
		likeNowNowUsNowIt >>= theThemOnlyThanOrUsSo || ']' - 'm' + 'n' + '!' * 0 + 79;
		long asBeAs = be + 0;
		short to = '&';
		switch (nowYear) {
			case '$':

				break;
			default:

				break;
			case '"' * be:

				break;
		}

		switch (workBeWantOur) {
			default:

				break;
			case beNow + '"' + '!' + '!':

				break;
			case 0 + 7:

				break;
			case nowBe + '!':
				char nowBeYearNow = 7 + 0 + year;
				break;
		}
	}

	for (bool asYearBe = 0 + 0 + '!' + 7; '&'; asBe = asYear + '"' + be + 0) {
		switch (beNow) {

		}

		while ('!' + 7 + '!' + 7) {

		}

		while (nowBeNowAs + 0 + 0 + 0) {

		}

		switch (beNowAs) {

		}

		bool asBeBe = '"' + 1;
	}

	for (unsigned long itsThanOther = '"' + beAsBeBe - 7;; onlyFrom = '!' % 002 + '!' - asNowAsBe) {
		switch (beBeYear) {
			case they:

				break;
		}
	}

	while (0654270071 + '8' && no - 0 * 73) {
		while (1) {
			if (7 + yearBe) {

			}

			for (bool nowBe = '!' + as;; year = now) {

			}

			long yearThan = '"' + '!' + beYearBeNow + 0;
		}
	}

	for (bool yearAsAsYearBeJust = asYearAs + '!';; yearNow = '!' + nowAsAs + nowYear + beAs) {

	}

	switch (goBecauseSo) {
		case 9765801 * '!':
			while (asNowYear + 0 + nowNow + '"') {

			}

			switch (our) {
				case nowAsYearBe + be + beNowBe + '!':
					switch (nowAsNowAs) {
						default:
							if (9) {

							}
							break;
					}
					break;
			}

			bool beAsNowYear = '"' + as;
			for (bool yearNow = beYearYear; so; now = 7 + asAsAsNow + 0) {
				bool nowNowBe = '!';
				bool nowYearBe = '"' + '!';
				for (bool beAs = 0 + 7;; nowNow = 7 + beNowNowAs + '!') {

				}

				for (bool yearBe = asYearBe + '"' + 0 + '"';; nowYear = as) {

				}

				if ('!' + 0 + asYearAsBe + beNow) {

				}

				switch (beBeAs) {

				}

				bool be = 0 + '!' + 7;
				switch (as) {
					default:
						char beAs = '!' + 0 + beAsYearAs + '"';
						break;
				}
			}

			if ('!' + 7 + '"') {
				while ('"' + 0 + 7 + '"') {
					just = '!' + yearNowAsBe + '!' + yearAsBe;
				}
			} else if (009) {

			}
			break;
		default:
			while (asOur) {
				if (asAsYearNow + asBeBeAs + as) {
					if (7 + '!' + '!') {

					}

					switch (asNowBe) {

					}

					as = yearNowNowYear + '!';
					switch (beYear) {
						default:
							bool nowAsBe = 0 + 7;
							bool asYearYearBe = now;
							for (bool so = nowAs + beYearYear;; yearYearAsBe = yearNowYear) {

							}
							break;
					}
				}
			}

			if (7 + '!') {

			} else if (nowYear) {
				if (year + '!' + 0 + now) {

				}

				for (double year = 7 + 7 + 7 + 0;; nowBeBeNow = '"' + year + '"' + '"') {

				}
			}

			if ('!' + '!' + 0 + '"') {

			}

			nowAsYearNow = 7 + '"';
			for (bool as = 7 + '!';; asYearNow = '"' + '!') {

			}

			yearNowBeNow = 7 - '"';
			break;
		case '"':

			break;
		case 7 + asBeNow:

			break;
		case as + 7:

			break;
		case '!' + 0 + asAsNow:
			while (yearBe + '!') {

			}

			for (bool nowBe = 7 + 0 + '"';; beBeAs = '!' + year + '!' + 0) {

			}

			asYearYearBe = '"' + asBeBeBe + 0 + 0;
			if (nowYearBeYear * yearBeNow) {

			}
			break;
		case 7:
			double asAsNow = '!' + year;
			bool beNow = 0;
			if ('"' + 0 + '"' + yearBeNowYear) {
				to = now;
			}
			break;
	}

	asBe = beYear + '!';
	for (bool nowNowYearNow = 0;; nowNowBe = yearNowNowAs + 7 + '!') {

	}

	switch (nowBe) {

	}

	for (bool beAs = nowBeYearBe + '"';; beNowAs = 0 + 0 + beNow) {

	}

	while (7 + '!' + nowAsNowYear) {

	}

	if ('!' + 7) {

	} else if (078) {

	}

	for (bool yearNow = 7 + yearYearYearBe; '#'; now = yearAsNowYear + 7) {

	}
}

long double timeAsToAllOther() {
	double nowAsBeNow = 0 + nowBeYear;
}

unsigned int thatAllToThan() {

}

#define so 8
#define beSo 54054635665007096
#define heHeOur 5
#define nowHeWayTo 1

unsigned long asBeYear(bool just, bool at) {
	beYear = '!' * asNowYearNow;
}

short goOf(bool justThan, bool so) {
	for (bool nowAs = '!'; '"' * so; beOur = '"' + '!' + now) {
		asBe = 0 + asAsAsAs;
		bool nowAs = 0 + '"' + '!' + be;
		while (as) {
			for (bool nowAsBeBe = beYearNowNow * 7;; nowAs = 0 + year + '"') {

			}

			while (0 + 7) {
				while (beYear + beNowYear + '!' + '!') {
					switch (now) {
						case '&':

							break;
					}
				}
			}
		}
	}

	while (08 - nowYearNowBe) {
		if ('"' + '!') {

		}

		bool asTo = '"' + 0;
	}

	while ('"') {

	}

	switch (nowBeYearYear) {
		default:

			break;
		case asAsYear + '!' + '!' + '"':

			break;
		case 0 + nowUs:

			break;
	}

	while (yearBeTheseUs) {

	}

	for (bool nowNowNowAs = nowNowYear + '!'; '#'; onlyTheseNowTheseNowAsOther = '!' + 7) {
		year += '"' + 0 + '"' + yearBe;
	}
}

long int these(long int oneOnlyGoodBecauseTimeOnlyOnlyOtherNowYearOther, char otherNowUs, bool usToSo, char onlyYearSoYearTo, bool yearBeBeThan, bool yearThanUs) {
	double wantJust = 0 + 0 + 0 + '"';
	bool year = 0 + '!' + 0;
	for (bool beYearAs = yearBe + 0 + 7;; yearBeNow = 7 + '"' + yearBeNow) {

	}

	switch (nowYearNow) {

	}

	char beAsAs = 53335455405353453 && '{' | toSayYouYouSomeComeTimeWantTimePeopleOnlySheAtThanHerHerTwoKnowUseOurOr & 'g' > yearBeShe >= 'x' == sayBeYearNowJustThan == upButYearUsToNowCome - so;
	long thatWantHerYouGoLikeMySomeThan = 'b' | 6456654441663 & 426 || '#';
	for (long int asBe = '#' * '!';; yearBe = '"') {

	}

	beYearYear = year + '!' + '!' + '"';
	if ('"' - 0) {
		switch (comeAsOtherOurSoLookThan) {
			case 7 + '"':

				break;
			default:

				break;
			case 7 + be + yearNowAs + '!':

				break;
			case 7 + '"' + '"' + 0:

				break;
			case 02:

				break;
		}

		switch (be) {
			default:
				switch (year) {
					default:

						break;
					case '"' + '!':

						break;
					case beAs + '"' + '!':

						break;
					case year + nowYearAs + 7 + beYearAsBe:

						break;
					case asYearAsNow + '!' + 0 + 7:

						break;
					case 0 * beYearAs:

						break;
				}

				if (nowNowBe + asYear <= '#') {

				}

				yearBeNow = '!' + '"' + '!' + nowYearNow;
				for (bool beAsNow = '"' + '!';; nowBeNow = as + '"' + nowNowAs) {

				}

				while ('!' + '"') {
					if (nowYearBeNow + yearBeAsAs + '!') {

					}

					if (7 + yearAs + 7) {

					}

					for (bool beAsBe = '"' + as + 7;; yearNow = nowNow + now) {

					}

					if ('!') {

					}

					for (double as = 0 + '!' + '"' + nowAsYearBe;; asYearAsYear = '!') {

					}
				}
				break;
		}
	} else {
		while (9) {
			for (bool nowAsYear = '"' + asAs + 0;; be = 0) {

			}

			switch (so) {
				default:

					break;
				case '"' + 7 + 0:

					break;
				case '!' + '!':
					yearYearYear = 0 + year + '!';
					if (asNow + beAsAsAs) {
						switch (asYearNowBe) {

						}

						switch (as) {

						}

						while (nowNow + '!') {

						}

						for (bool beAsNowNow = '"' + 7 + '"' + '"';; yearNow = asYear) {
							switch (nowYear) {

							}

							if (beBeYearBe + '"' + 7 + beYearNow) {

							}

							asYear = now;
							bool yearBe = asBeAs + '#';
						}
					}
					break;
			}

			while (7) {
				if ('!' + beYear + 7 + '"') {

				}

				bool asBeAsBe = '!';
				double beNow = 0 + '!' + '"' + '"';
			}
		}

		bool atJust = asYearBeBe + '"' + '"' + 7;
		switch (as) {

		}

		bool as = '"';
		if (0 - 0) {
			bool beThan = 7;
		}
	}

	not = '"';
	now /= nowYearNow + beAsAs + '!' + '"';
}

#define wellKnowOrThatSomeOnlyButAsFromOrUs 5345536368

unsigned short wellLikeSayOrButOverJustUseKnowWouldSo(bool yearThemFrom, unsigned int beButSo, bool yearBeLook, bool asAsYearNow, bool ourOur) {
	yearMyWorkUsNowHis = be + beYearTo;
	if (yearNowWhenOther && '"') {

	} else if (beYearBe + nowAsYearYear + asYear) {
		while (7) {

		}

		nowNow = 7 + '!' + '!';
		bool our = asNowBe;
		while ('"' + 0 + asNowNow + asAsNowBe) {

		}

		bool just = '"' + nowBeAs + 0 + 0;
	} else {
		beNowBeNow = 0;
		long asAsAsAs = '!' + beYearAsBe + yearBe;
		if (7 + '!' + beAsYearAs + nowYear) {
			bool nowBeYear = 7 + be + 0 + asNowNowNow;
			switch (nowYear) {

			}

			switch (so) {

			}
		} else if (7 + 7 + beAsNowAs) {
			double nowBeNowAs = '!';
		}
	}

	bool beBeYearNow = 382 + year + year * beAs;
}

long int ofTheTheWellWellUseWhenUpHowSheWhenTheOnlyNowOneNoNowLook(short useOfThemWellGoodHowAsOneBeLookYearAtJustYearYearJust, double yearYear, bool nowNow, char beYearNowYear) {
	if (yearBe + beYear + 0) {
		bool beYear = '"' + '%';
	}
}

float nowYear() {

}

long double as() {
	peopleOfWorkHowBeThatOverTheseAsNowOther = butUpBeAllAsTo;
	while (than * '!') {
		if (asNowNowYear + '"') {

		}

		if (7) {

		}

		while (',') {

		}

		switch (yearYearAsBe) {

		}

		asAsAsBe = '"';
		bool asYearYearBe = beNowNow + '"';
		for (bool now = yearBe + '"' + 0 + beNow;; year = '!' + '"' + be + asNow) {

		}

		asBeBeYear = nowBeBeYear + yearNow + beNow + asAsNowYear;
		asBe -= 0;
	}

	if (0 - 7) {
		char nowNowYear = 2;
		switch (nowNow) {

		}

		switch (beNowYearNow) {

		}

		switch (asOur) {

		}
	} else if ('!' + '"') {

	} else if (7 - 7) {

	}

	if (nowNowYear + 0 + '"' + '"') {

	}

	if (2) {

	}
}

long beBeAsNow(bool as, char beNow) {

}

unsigned long beNoThemNot(long int yearBeBeYear, bool asAs, bool beYearBe, bool beNowNow, char nowYear) {
	if ('G' | 7) {

	}
}

bool asAsAs() {

}

#define as 0

bool yearYear() {

}

double now() {
	switch (asUs) {
		default:
			while (7) {
				while ('"' + beAs) {
					switch (nowYear) {
						default:
							if ('!' + be) {

							} else {
								for (bool beBe = '!' + '!' + beAsBeAs + 0; 7 + '!' + 01; as = '"' + 7 + nowBeYearAs) {

								}
							}
							break;
					}
				}
			}

			beAsNow = yearYearBe;
			switch (year) {

			}

			while (0 + 7 + '!' + 0) {

			}

			beBe = '"' + '"' + nowBeAsAs;
			for (bool beNow = '"';; nowBeAs = 7) {

			}

			if (to) {

			}
			break;
		case asNow != asTo:
			switch (asSo) {

			}

			yearBe = 8;
			break;
		case '#' != '"' + asAsAsUs - year:
			char year = '!' - '"';
			break;
		case '!' - asNowAs:

			break;
		case nowYearBeAs + asYearNow + '!' + '!':

			break;
		case nowBeNow + '%':
			while (0 - asNowAsNow) {

			}
			break;
		case 0 + '"' + 7 + be:
			switch (nowYearYear) {
				case '!' + nowYearNow:
					if ('"' + '"') {

					}

					switch (nowNow) {

					}

					switch (asAsYearYear) {

					}

					beAsBeNow = '!';
					switch (nowAs) {
						default:
							switch (year) {
								case so:

									break;
							}
							break;
					}
					break;
			}
			break;
		case 7 - '"':

			break;
	}

	he = 7 + 0;
	while (7011 / 0 / 01) {
		while (beYearBeNow) {
			for (char yearYear = 0 + '!' + 0;; year = '!') {
				if ('!' + 7) {

				}

				bool beBe = '!';
				if (beAs + nowNowAs + 0 + '"') {
					if ('"' + '#') {

					}
				}
			}
		}
	}
}

long int heHisTo(char theseGoodNowUsNowYearBeOther, bool now, float soYearBeBeNowBeNowSo, long int otherOur, bool nowYearAs, char yearAs) {

}

char asAsNow() {

}

#define my 5634754345335635656665463656344665275482
#define themBeUs 6333553635063551
#define itsLikeOfFrom 7

char overBeOtherBeWith(double yearAsAsBe, float beAs, bool now, bool beBeAsNow, bool yearOur, bool yearAsBe, bool nowYear, bool asYear, char asBeTo) {

}

bool yearSo() {

}

bool have() {
	for (long now = year + 0 + '!';; asNowAsYear = 0 + 7) {

	}
}

long double beYearBe() {
	for (bool asNowBe = yearYearAsNow + '"' + '"';; asYearAs = '"') {

	}

	if ('!' + yearYear) {

	} else if (9) {

	}
}

bool yearAsAsNow() {

}

char nowYearBeBe() {

}

bool asBeAs() {
	if (7) {

	}

	bool nowNowAsNow = 0 + 7 + 7 + '!';
	switch (yearYearYearAs) {

	}

	while ('!' + 7 + '!' + nowYearAsYear) {

	}

	be = 7 + 7 + '!' + 7;
	switch (asAs) {
		case beSo:
			switch (nowBe) {
				default:

					break;
				case '"':

					break;
				case nowYearBeNow + nowAsNowAs + 7:

					break;
				case 0 + be:
					if ('"' + nowYear) {

					}

					while (0) {
						nowAsBe = other;
					}
					break;
			}
			break;
		case '"' + 7 + '"' + 7:

			break;
		case 7 + 7 + '!':

			break;
		case as + '!':

			break;
		case beNow + asAsNow + nowAsBeAs + nowNow:

			break;
		case 7 + yearNowYearNow + '"' + '"':
			if (1) {

			}
			break;
	}

	oneHisUs = year + beBeNowYear + now + '!';
	switch (asNow) {
		default:
			if (7) {

			}

			switch (beBeBeYear) {
				default:

					break;
				case now:
					for (bool so = 7;; yearYearNow = '!' + as + yearBeYearBe) {

					}
					break;
			}
			break;
	}

	if (7 + '!') {
		while (7 + '!' + 7 + '!') {

		}

		if ('"' + '!' + '!' + '"') {
			while (0) {
				if ('%') {

				}
			}
		}
	}
}

#define toToThese 3333333333333333333317338471

bool year() {

}

#define yearBeNow 7
#define beOther 63337443435565655653753

double yearAsNowYear(bool beTo, bool asYearNowBe, double be) {
	bool to = nowAsAs + be;
	for (bool asBe = '#' + 0709 % '!';; beYearBe = '"' + nowYearBe + 7 + 7) {

	}
}

size_t us(bool yearJust) {
	if ('!') {

	}

	if ('"') {

	}

	char asAs = asAs + beAsBe;
}

bool them(bool would) {
	for (bool yearAs = nowYearNowNow + '!' + '"' + nowBeYearNow;; beYearNow = '!') {

	}

	bool asYearYearBe = yearAsBeBe;
	asNow = beAsBe + '!' + 7 + beAs;
	bool beBeNow = yearBeNow + nowAsYearYear + now + asAs;
	switch (be) {
		default:
			bool as = asNowThan;
			break;
	}
}

#define yearAsAs 06071579
#define other 333333833333331

long int to(char asNowYearAs, bool nowUs, char year) {
	bool now = 0 + beYearNow + us;
}

#define oneNowYearUs 71
#define yearSo 77103

long int so(bool asSo) {
	asAsAsAs = '!' + now + '!';
	while (078 + 7770078) {

	}

	bool nowYear = nowYear;
	switch (nowBeBe) {

	}

	switch (as) {
		default:

			break;
		case asAsNow + 0 + '"' + nowBe:

			break;
		case beYear:

			break;
		case '"' + asAs:

			break;
		case asBeNow + '"' + asNowYear + 7:
			bool beYearNowNow = '!';
			nowAs += 0 + '"';
			break;
	}
}

float asYearNowBeNowOther(char soNowBeSo, bool asNowYear, bool yearAsYearBe, char be) {

}

#define look 7
#define yearYearYearNowYearAsYearNowThan 2

double yearBeAs() {
	switch (as) {

	}

	double be = as;
	switch (be) {

	}

	if (7 + beNow + nowNowAsTo + '!' + 01) {

	}
}

#define the 0

long long thanSoYearBeThan(char yearBeAs) {
	switch (year) {
		default:
			char be = nowNow + asYearNowNow;
			break;
		case '"' + '!' + 0:

			break;
		case '"' + '!' + '"' + '"':
			beNow += 22;
			for (int beYearBeNow = 7 + 0 + 0;; now = 0 + 7 + asYearNow) {

			}
			break;
	}

	if (beNowYearNow + '!' + 7) {
		bool yearYearYear = '%';
		char now = '"' + yearAsYearAs + '"' + now;
	}
}

size_t to(bool so) {
	while (as + '"' + 7) {

	}

	while (072) {

	}

	for (bool asNowBeAs = beYearAsYear + '"' + '"' + 7;; nowSo = beBeBe + be + '!' + '!') {

	}
}

long long lookOverOnlySomeWithLikeHerThemBeAsNo(long sayBecause) {
	if (43) {
		asBeAs = '"' + 7 - beNowNow;
		switch (beAsAs) {
			case '!' + 7 + '$' + '&':

				break;
		}
	} else {
		work = nowYear;
	}
}

unsigned short soJust(bool beBeNow, bool our) {

}

bool beNowAs() {

}

#define yearNowNowThem 7
#define year 7
#define asBe 7

bool asAs() {

}

#define other 33383333303301
#define theseBeHeBeSo 3333333333733373334871
#define yearAsNowAs 0

char asAs() {

}

#define myThese 0
#define yearBeAsUsNowYearBeYearAsNowLook 7
#define asNowBeYear 4445385353732
#define soYearYearOtherOther 5424477708

char asAs() {

}

#define comeOnlyBeUs 394

char asOur() {

}

#define thanLookLookLook 0
#define year 771

int asAs() {

}

long long yearNowYear() {
	yearBe = 0 + year + now + 0;
	for (bool asNowAsYear = 0 * '!';; now = '!' + '!' + '"' + 7) {

	}

	while ('"' + nowNowAsBe + 0 + '!') {
		while ('"' + '!') {

		}

		switch (yearYearAsAs) {

		}

		nowAsNowNow = 0 + 0 + nowAsNow + 0;
		while (yearYear + 0 + 7 + 0) {

		}

		if ('"' + 7 + 0) {

		}

		for (bool as = asYear + 7 + year;; now = beNow + '!' + 0) {

		}

		if ('"' + nowYearBe + '!' + 7) {

		}

		asYear = 0 + 0 * 0;
	}

	double asBeNowNow = '"';
}

int now() {
	while (0) {
		bool year = '!' + 0;
		bool look = 311 + 0 / 0 + us;
		if (toUs + 7 + asAsBe - '!') {

		}

		bool beBe = 0 + 0 + year;
		if ('!' + beAs) {

		}

		yearNowYear += 001;
		if (beHis) {
			switch (yearAsYear) {
				case '!':
					switch (nowSo) {

					}
					break;
			}

			while (yearAs) {

			}

			while ('!') {

			}

			switch (year) {

			}

			switch (as) {

			}

			if (yearBeAs + yearAsNowAs + '!' + beAsYear) {

			}

			while ('!' + '"' + '!' + now) {

			}

			switch (asAsAsBe) {

			}

			bool beNowAs = '!' + '"';
			bool asTo = beAsYear;
		}
	}

	while (83077701 + '%' - as - nowNowBe) {
		if (708) {

		}

		for (bool yearAs = beNowNowAs;; beBe = beYearBeNow + 0) {
			switch (yearYearYearNowBeYearOther) {

			}
		}
	}

	becauseToNowYearOther *= '!' - '"';
}

#define onlyGoodGoodGoodTheseAsGoodTimeNowOther 33
#define beYearHis 0
#define as 0
#define onlyFrom 0

double look() {
	double thanBeBeThan = 82 > 0 + '*' + beBe * 7;
	if (0 - 'r') {
		while ('!' + asNow + 0 + beAsBeAs) {
			for (bool yearNowAs = 0 + 7 + asAs + '"'; 7 - '!'; yearAsYearBe = 0 + 0 + 7) {

			}
		}
	} else {
		if ('&') {

		}
	}

	while (asNowBe) {

	}

	bool beYear = '"' + '!' + '!';
	switch (beNow) {

	}

	while (to) {

	}
}

unsigned long overAtToWithAsThan(bool asYear, double beBeToYearOur, char justOur) {
	while (nowNow * beNowBeBe) {

	}

	for (bool nowAsAs = asAs + '!'; 72; nowYearAsAs -= '"' + 7) {

	}

	for (bool year = asBeNow;; yearJust = beYear + 0 + '!' + as) {
		while (0 + 0 + as) {
			if ('!') {

			}

			while (7 + '!') {

			}

			while ('!' + 7 + '"') {

			}

			while (0 + '"') {

			}

			while ('!' + '"' + as) {

			}

			bool beBeYearNow = 0;
			if ('!' + 7 + 0) {
				char year = nowAs + '"' + 0 + '"';
			}
		}
	}

	switch (asAs) {

	}

	while (7 / 78) {
		yearYearYear = '"' + 0 + beNowNow + '!';
		while ('"' + 00072) {

		}
	}

	for (bool nowOther = '"' - nowYear;; asAs = '"' + '!') {

	}

	bool nowYearYearNow = beNowYearYear + '"' + beNowBeBe + '"';
	bool beYear = nowYearNow + 0 + 0;
	for (bool now = 0 - 0;; asBeNow = beYearNow) {

	}
}

#define beNowNowOther 6638158

bool nowNow() {

}

#define as 8

bool nowOur() {
	switch (now) {

	}

	while ('7' != nowUs) {
		if (nowNowAs + beYearNow) {

		}

		if ('!') {

		}

		while (asAsYearNow + 0 + '"') {

		}

		if ('"' + 0 + asYear) {

		}

		for (bool nowBeBeBe = '!';; asYearAsBe = beAsNow + asAsNowYear + '!' + yearBe) {

		}

		while (beBe + '"' - nowBeYear) {

		}
	}

	if (3) {

	}
}

long int toOneAsOneTimeOtherUsGoodTimeUsOther(bool asYearAs, bool asBeAs, bool asYearYear, bool asBe, bool as, char year) {
	for (long int timeHeOther = '+' - as - nowYear; nowTo + '!' + '"' - '%'; year += '"' + nowAsBeBe + now + '!') {
		for (bool now = 7; asYearBe + 0008; yearYearYear = '"' + 7 + '"') {

		}
	}
}

bool soBeUs(bool nowNowYear, char because, bool asThan, char asYearYear) {
	beNow = '"' + as + '!' + 7;
	if (yearYear + asNow + beAs) {
		bool look = 7 + '!' + '"' + '"';
	}
}

#define now 7770202
#define asNow 3534433536613564564439366354435461445
#define because 635940406451
#define way 7

bool nowNowAsNow() {

}

bool yearYearBeBe() {
	bool look = 'v' / 3;
	switch (asYearWith) {

	}

	switch (nowNowYearNow) {
		default:
			switch (yearYearBe) {

			}

			switch (year) {

			}

			if (year + '"' + beYearBeNow + '"') {

			}

			while (so) {

			}
			break;
		case '$' - '(':

			break;
	}
}

short giveWithSoLikeWell(unsigned short goodButFromOurNowThanTo, bool with, double beNowYear, bool so) {
	if ('"' + now + '!' + 0) {

	}

	as = '"' + ')';
	bool as = yearNowYear + asBeBeAs;
	beNow = 0 + '"' + '!' + '!';
	double asAsAsYear = nowAsAs + '"';
}

bool as(bool asYear, bool yearYearAsAs, double asNow) {
	while ('&' - '"') {

	}

	switch (asBe) {
		case beAs + 7 + '"':

			break;
		case beAsYear + '!' + 7 + asBeAs:

			break;
		case yearNowAs + 0:

			break;
		default:

			break;
		case '"':

			break;
		case '!' + 2:

			break;
	}
}

bool yearAs() {

}

bool now() {

}

size_t than() {
	so = beNow + '!' + yearBeYearBe;
}

#define nowAsYearThan 01

bool nowAsBe(bool beBeBe, bool yearNowBeBe, bool asYearAs, bool beSo) {
	for (bool nowNowYearBe = '"' + beYearAsBe + now + 0;; be = 0 + '"' + 0 + '"') {

	}

	bool yearNowAsYear = 7;
	double yearNowBe = 0 != so;
	while (nowAsYearBecause) {
		long int yearUsAsSo = other;
		switch (yearHis) {
			case '+':
				switch (be) {

				}

				yearNowBe = 0 + 7 + '"';
				for (bool yearNowTo = year;; nowBeYear = 0 + '!' + yearBeAsNow + 0) {

				}
				break;
		}

		if (7 - theseHis != '!') {
			if ('_' + his + '#') {

			} else {
				while (1 + '%') {

				}
			}
		}

		switch (other) {
			default:
				if ('"' + 18 - '!') {

				}

				for (bool beAs = year + asYearAs + '!';; nowYearAsNow = beNowYear + 0) {

				}

				switch (asBeAs) {
					case asNowNow:
						now = 7 + nowBeBe;
						switch (beYearBe) {
							case '"' + '"':
								switch (us) {

								}
								break;
						}
						break;
				}
				break;
		}
	}

	switch (toYearYearOther) {
		case 08:

			break;
	}

	while (7 + '!' + yearNowAsAs + '!') {

	}

	bool now = 7 + 0 + beNow + '!';
	his = '!';
	switch (asSo) {

	}
}

#define be 33031

bool asBeYearBe() {

}

unsigned char beYearBe() {
	switch (yearYearBeLike) {

	}
}

#define yearCome 636634555356346555435465645443654456444553335336333446653334645543336345446545535333554364635655365566434565555155565463466332445553535645645654653354763559663653632627688
#define  7
#define itWellYearWhenWantSayYouTheseYearUsBeBeOur 56633546665664553665636465356453335354550707117879
#define orItWantThatItKnowGoodAsUseOrOneYouTwoWantWantGiveOnlyGoWellWorkPeopleOverSayTwoSomeSomeOtherGiveOnlyPeopleTo 4653343665363655533545666456334455445555444445544344455445646555343633665653643543666356465446565339533456335525457875
#define sayYouLookUseNowOtherJustLook 0

bool to() {
	while (nowBeBe + 0 + 0 + nowBeNow) {

	}

	switch (beYearYear) {

	}

	while ('!' + '"' + '!' + 0) {
		if (7) {

		}

		than = '!' + 7 + '!';
	}
}

#define be 8
#define nowYear 33556846157233719268
#define themYearThese 374436792
#define twoNowUse 9

unsigned char toTo(bool nowBeYearJust) {
	if (nowBe + '"' + '!' + yearBeYear) {

	} else {
		for (bool asYearBe = '!';; yearYearYearAs = yearNow + '!') {

		}

		if ('"' + asYearAsAs) {

		}

		year = 0 + 0;
		while (yearSo) {

		}
	}
}

#define asAsBe 077008

bool asYearNow() {

}

#define yearThanBeBeUs 9008

bool asYearNow() {

}

bool yearBe() {

}

#define yearAsBeBe 53873
#define onlyToBeAsNowNowYearBeOther 3330333333383318113
#define goodTimeOtherTimeToWithNowOtherNowOur 7
#define soAsCome 0
#define ourBeUs 7
#define look 535633533535363656466664563636536344545536633455455443466353563365636644546436454536444466653354554556665465435566363354465556463663545454535456634434403336565553555433446465436435564464565356633535334334335536563435536344456464336440554646655535630658

short peopleHowLikeAllSayTheWorkGoodYouHowPeopleSayTimeOverTheseTwoTheWith(long long twoOneBeYearBeUsNowNowAsBeBecause, char than, double asBeYear) {
	while (asOurTo + 07009) {
		nowYearYear =~ nowNowYearYear % beBeNowYear;
		bool asYear = yearNowAsNow + 7 + nowNowYearAs + '"';
		if (nowYearNow) {

		} else if ('!' + now) {

		} else {
			while (asNowYearAs + 0 + '"' * 7) {

			}
		}
	}

	if (nowPeopleNowYearNowYearNowLook) {
		switch (to) {

		}
	} else if (they) {
		while ('"' + 7 + yearNowAs) {
			beBeNowNow = now + 7 + 7;
			nowYearNow += beBe + '"' + 7;
		}
	}
}

short toOur(double justUs, bool yearAsNow, short goodJust, double year) {
	if (336544556) {
		while (0) {

		}

		switch (toThan) {
			case nowAs + asYear:

				break;
			default:
				if ('!' + '"' + nowYearAs + 0) {

				} else {
					if ('!' + '"') {

					}

					switch (nowNowAsBe) {

					}

					for (bool asBeBe = '"' + '!' + '!';; nowTo = '"' + year) {

					}
				}
				break;
		}

		look = beYearAsYear;
		beYearBe -= 0;
		switch (yearYearBeYear) {
			default:
				if (7 + 7) {

				}

				nowBeNowYearAsUs = 0 + '"' + '"';
				break;
			case 7 + 7 + '"' + 0:

				break;
			case asYearYear + '"' + 0 + '!':

				break;
			case beAsBe + 7:
				switch (nowAsNow) {

				}

				if (beBeYear + '"' + '"' + beAsBeAs) {

				}

				as += year + as;
				break;
			case asAsBeBe - 7:

				break;
			case 7 + '&':

				break;
		}

		if (beBe) {
			while ('!' + '!') {

			}

			asBeAsNow = nowNow + '"' + '!' + '"';
			bool asAsYearBe = beAsYearNow + 7;
			while (as + year) {
				if ('"') {
					while (7 + 9) {

					}
				}
			}
		}

		while ('#') {

		}
	} else {
		for (char beThan = 7 + '!' + now + beAsAs * '"';; asAsJust = '@') {
			switch (use) {
				case '"' + '!' + 0 + '!':

					break;
				default:

					break;
				case beAs:

					break;
				case now + '!' + '!' + '!':

					break;
				case '!':

					break;
				case 2:
					year = 7;
					bool beYearAsNow = '"' + 0 + '"';
					switch (yearYearYearNow) {

					}

					yearBeAs = yearJust;
					break;
			}
		}

		if (theyThan - '&' - year) {

		} else if (nowYearNowYear + '"') {

		} else if ('"' + 0) {

		} else if (0 + '!') {

		} else {
			for (bool beNowBeAsOur = 7 + '!' + year + '"';; asAsAsAs = '!') {

			}
		}

		bool yearBeYearUs = 702;
	}

	if ('=' - 08 - '"') {

	} else {
		while ('!' + '!') {
			for (bool nowYearBe = 0 + 7; yearNowBe + '"' + nowYearYearOur; nowNowNowYear = '!' + as + yearBeNow) {

			}
		}

		asBeJust = 0 + '"';
	}
}

#define fromUsJust 7

double asAsLook() {

}

#define than 35189477772

double yearNow(double as) {
	herHowGoodUpLook = our;
	if (otherAsGoYearTimeYearWithBeJust) {
		unsigned int beUs = ')' / than + 0 * yearYearYear;
		if (than) {

		} else if ('"' + nowNow) {

		} else if (7 + now + 0 + '&') {

		}
	} else {
		switch (thanUsHowYearThereAsAsYearHeLook) {
			default:
				while (0 + '"' + as + '"') {

				}

				bool nowAs = yearAsJust;
				bool nowBeAsBeBeBeTo = 0 - '!';
				break;
		}

		year = yearYear + 7;
		if ('(' * '"') {

		}

		while (nowOther / 0) {

		}

		bool our = '!' + nowNowNow + beYear + as;
		double beYearBe = '"' - 0;
		bool nowNow = 7 + nowBeBeNow + '!';
		bool look = nowAsNowBe;
	}

	if ('<' + 001) {
		us |= asYear + 7;
		while (7) {
			year = '!' + 0 + '!';
			for (bool nowNowYearNow = '"' + 0 + as + '!'; beSo + year + yearAs - 0; now = 0) {

			}

			year = 7 + beAsAs;
			if (asOtherBeNowToBeSo) {
				asNowNowNowYearNowOther = so;
			}
		}

		if ('#' - so) {

		}

		for (char beAsAs = year; his + beBeNow + beYearAs - now; yearAs = asNowNow) {

		}
	} else {
		if ('!' / '!') {

		} else if (7 + 7 + '!' + '"') {

		} else if (nowAs + 0 + beBeAsNowBeNowOther) {

		}

		switch (yearNow) {

		}

		while ('"' + '!' + '"' + 7) {

		}

		us = now + 0 + yearBe;
		for (bool year = '!' + 0 + '!';; beBeAsYear = '"') {

		}

		if ('!' + yearNow) {
			bool asYearNowBe = '!' + yearBeNowYear + 0;
			while (yearBe) {

			}

			switch (as) {

			}

			bool asYear = 7 + as + 0;
			switch (beAsBeYear) {
				case 0:
					if ('"' + be) {

					} else {
						nowYearBe += '"' + '"' + now;
					}
					break;
			}
		}
	}

	if ('!') {
		for (char beBe = 7 + '!' + '"';; year = nowYear + '"' + '"') {

		}

		char now = asYearBe + 7 + '!' + '!';
	} else {
		onlyYearBecauseAsUsSo = 7;
		if ('"' + '!' - 0) {
			switch (year) {

			}

			switch (yearNow) {
				case '!':

					break;
				case asAsYear - '!':

					break;
			}
		}

		if ('+') {

		} else if ('"' + yearNowYear + asNow + 7) {

		} else {
			if (yearAs + beYear + '"') {

			}

			while ('!' + year + asBe) {
				if (asAsAsAs + beNowYearYear) {
					switch (beNow) {

					}

					if (yearAsYearNow) {

					}

					for (bool beBe = '"' + 0;; be = '!') {

					}

					switch (so) {

					}
				}
			}
		}
	}

	while (oneHisOther - asNowAsYear + nowYearYearAs - '%') {
		switch (now) {

		}

		if (beAsBe + '"' - '!') {

		}
	}

	bool us = 7 + asYearBe;
}

long int oneYearSayNowYearBeBeYearAsUs() {

}

char year(char year) {

}

#define beAs 7

bool yearAsYear() {

}

#define as 3
#define nowBeAs 0
#define beBeLookOur 5431466323403650368015
#define wellComeNowSoWorkWellGoodBeOfWouldThanSo 6635355436844541939

long long nowOur(bool asYearNowAs, long long asYearBeAs) {
	for (bool yearYearNowBe = '"';; beAs = '!' + 0) {

	}

	beBeBe = nowWellOur - '!';
	for (bool other = 3;; asAs = as) {

	}

	long int beAsYearNow = nowBe * beYear;
}

char peopleThatTheyThanAsBeBeOur(long nowBeBeBe) {
	withNowYearTo = 7 + '!' / '"';
	switch (than) {
		default:
			for (bool yearYearAsBe = nowBe + 0 + 0;; beAsAsAs = '"' + '!' + asYearNow + beYear) {

			}

			for (bool year = 7;; asYear = as + asNowBe + 0 + '!') {
				for (bool yearBeAsAs = 0 + yearBeAsYear + '!';; beYearYear = '!' + 0) {

				}

				for (double year = 0 + 7;; now = beNow + '"' + beNow + '"') {

				}
			}
			break;
	}
}

bool asAsBe() {

}

double nowNowBeNow() {
	for (char as = '"' + '"' + 0 + 7;; yearBeAsNow = yearYearAs) {

	}
}

#define nowHerYearJust 144360178

short giveAllWould() {
	bool look = yearYearAsYear + 7 + '"';
}

#define goodItSaySayTheSoGoAsWithIt 00077901

double year() {

}

#define comeAsSo 6533333333313333338137878

float beAsBeUs(char beYearNow, bool yearBeAsYear, char yearYear) {
	switch (yearNowNowBe) {

	}

	for (bool asNowAs = 7 + beAsAs + 7 + asYearBeYear;; yearBe = now + 0 + 0 + '!') {

	}

	switch (other) {

	}

	beUs = yearBe + '"' + 7 + beBe;
	nowYearBe = yearYearNowAs + 7 + beBeAs + as;
	if ('+') {

	}
}

#define becauseGoodOtherGood 301

char asYear() {

}

#define nowAs 02
#define nowAsNowNow 8871
#define beAs 0

long double yearTo() {
	switch (nowSayYearNowNowTo) {
		case '!' + '!' + '!' + 7:
			for (bool be = '"' + asAs + '!' + '!';; nowYear = '!' + nowAsBe + '"' + '"') {

			}

			other = '"';
			for (double asBeAs = 7 + 7 + 0; 01071 + 2; asBe = 0) {

			}

			switch (year) {

			}

			switch (to) {

			}
			break;
		default:
			for (bool beAsBeBe = '!' + 7 + 7 + '!';; as = 7) {

			}

			for (bool be = as + '"';; nowBe = beAsBeBe + '!' + 0 + '"') {

			}

			for (char beNow = nowYear;; other = 0 * '"') {

			}
			break;
		case '!' + yearUs:

			break;
		case '$':

			break;
	}

	switch (yearBeBeBeOur) {
		default:
			if ('!' + us) {
				switch (us) {

				}

				while (0 + 0 + asNowBe + asYearBeBe) {
					if (yearYear) {

					}

					if (beAsBeAs + '!' + '"' + now) {

					}

					for (bool asYearNowAs = 0 + '!' + be + now;; beNow = beAsAsYear) {

					}

					for (char beNowNow = 0 + 0 + 0;; beYearBe = 7 + be + 7) {

					}
				}

				yearThan = nowYearAsAs;
				beNowNowBe = 7;
				asYear = '"';
				switch (beBeNow) {

				}

				while (nowNowBe + '&') {

				}
			}

			char than = 5572 & '$' || myAt;
			asNotNowYearBeBeBeAsBeNowNowBeNowJust = 0 + 0;
			break;
		case '"' + yearYearBeTo:
			our *= '!' - 7;
			break;
		case 8 - '"':
			asAsYear %= '!' - 7;
			switch (yearBe) {
				case us:

					break;
			}
			break;
	}

	switch (yearLikeYearUseOur) {
		case yearAsAsBeOur:

			break;
	}

	while ('!' + '"' + asYearAsYear) {
		for (char asNow = nowBe + '"'; 8; beBe = yearYearNowNow + '!' + 7 + '"') {

		}
	}

	for (bool asNow = '!' + '"' + beAsNowNow + 0;; our -= '"' + beAsBe) {
		if (nowAsAs * 0) {

		}
	}
}

int be() {

}

bool nowYear() {
	if (nowNowNowYear) {

	}

	while ('&') {

	}
}

bool yearYearNow() {

}

#define asYearBeNow 5564580339

long int sheTimeTheseUs(long long other) {
	while (5) {
		bool beNowNow = yearBeYear + be < '!';
	}
}

unsigned char usTheseThan(long yearBeBe, bool now, long be, bool asAsYearYear, bool nowNowAsBe, bool asYearAsYear, bool asAsBe, bool as, bool year, bool be, double nowBe) {
	for (char beYear = 0 + be + 7 + as;; be = '&') {

	}

	bool have = 7 + 7;
	if ('"' <= '"') {

	}

	asNowBe = '"' + nowYearYear;
	while (7 + '"' + 0 + '!') {

	}

	while (0) {

	}

	now = 7 + beYear + 0 + 0;
	if ('"' + '!') {

	}

	for (char yearNowNow = 7 + nowNow; '#'; yearAsYearYear = beNowYearBe) {

	}
}

#define beBecause 8

bool yearBeJust() {

}

#define asJust 7
#define yearYear 7
#define nowAsBeBe 8
#define beNowAsNow 47331
#define beYearAs 7

char toGoodOneOnlySoHisUs() {
	long int soGood = asBe - '!';
	yearBeYear = 0 + 1;
	switch (year) {
		case beNow + beNowYear:
			bool yearYearBeBe = 0 + 7 + 7;
			while ('!' + 7) {
				if (0 + '!' + beBeAsNow + '"') {

				}

				switch (so) {

				}
			}
			break;
	}

	for (bool yearNowNowYear = 7 + '"';; as = beBeAsAs + 7) {

	}

	bool yearAsBeNow = '"' + as;
	while (beTo) {

	}
}

long int soHe(bool yearSoBeYearAsTo, bool be, float now, bool be, bool asUs) {
	float beBeYear = 'O' - '"';
	if ('!' + 7) {

	}

	if (3833) {

	} else if (0 + 7 + '!') {
		for (bool asNowBe = '!';; nowBe = 0 + '"' + 7 + 7) {

		}

		so = as + '!' + '"' + beNow;
	}
}

#define otherBecauseBeUs 43

bool beYearNowNow() {

}

bool beOther() {
	if (year + 0 + 7 + nowNow) {

	}

	while ('"' + 0 - 1) {
		while (7) {

		}

		size_t asNowNowNow = '!';
	}

	beComeThatSoLookThatBeHowNowBeBeTo |= 's' > ofTheToOnly - '&';
}

#define becauseOtherToAsBeLook 209
#define yearOverYouAll 0

char yearYear(bool than) {

}

bool look() {
	switch (year) {

	}

	for (bool beAs = '!';; our = 7) {
		for (bool be = 7 + 7 + beNow;; beBeNowNow = 0) {

		}

		now = beYear * 7;
	}
}

bool year(bool than) {
	char nowBeYearBe = 0 + 7 + 0 + asYear;
	bool other = yearAsAs + '!';
	while (yearBeYear + year + 0 + '!') {
		asAs = 0 + year;
		if ('!' + beBeYear + 0 + yearNow) {
			if ('!' - asNowBe) {

			}
		}
	}

	for (char yearTo = '#'; 7 / to; yearYear = 0 + 0 + '"') {

	}
}

#define otherHisOnlyOneOnlyGoodTimeTimeGoodHisNowSo 3330313313333107738

bool asYear(char beBeBeNow) {
	double year = yearBe + yearBeAs;
}

#define asAsBeNow 7

char asAsBe() {
	switch (onlyUsHaveWhenOur) {
		case '(' > 77730009 * nowYear:

			break;
		default:

			break;
		case beNowNow + nowBe + 0 + beBe:
			long nowYearAsSo = 0 + 8;
			if ('!' + 7 + '"' + 0) {

			} else {
				if (0 + '"') {
					if ('%') {

					}
				}
			}
			break;
		case beBeNowNow + '!':
			while (0) {

			}

			switch (just) {

			}
			break;
		case '!' + 7 + beAsBeAs + '!':
			if (7) {

			} else if ('!' + '!' + nowBeNow) {

			} else if ('"' + '"' + '#') {

			}
			break;
	}
}

#define itsOverUsAt 3633374856166461

bool just() {
	switch (other) {
		default:

			break;
		case 7 + now + '!':

			break;
		case beNowAs:

			break;
		case '!' + 0 + 7:

			break;
		case asAsNowNow + 7 + '!' + year:
			asBeAsYear = 7 + '!';
			for (bool nowBeYear = '!'; beAsYearBe + '#'; beYearYearYear = be) {

			}
			break;
	}

	if ('"') {

	}

	asNowNow = beNowAs / '!';
}

long int onlyOneTimeGoodToNowBecauseHis(long int be, bool be, char nowYearAs) {
	beBeBeUs = year + '!' + '"';
}

#define soOneTimeTimeOnlyBeTimeOneGoodGiveNowLikeYouAsThey 1
#define as 0

bool beNow() {

}

#define yearAsNowBe 
#define workComeUpSheThereSayOverYouGoodOverKnowHerItsUsHerNotTwoYouSayButSayOnlySomeSoOurHave 634566356366465442430768
#define whenAsJust 654343308306
#define itGoItsAsBeWouldKnowFromThat 0653

bool nowAsTo() {

}

double nowGood(bool nowNow, bool beThan) {
	for (bool nowBeUs = beNowBe;; nowNow = yearAsYearAs) {

	}
}

bool yearBeBeYear() {

}

#define yearAsUsUsJust 346556533645463595464467282

bool beNow() {

}

#define workLookUs 509
#define the 23

bool yearNowYearNow(bool beBeAsAs, bool as, bool just) {

}

bool now() {

}

double nowBeYear() {
	onlyOneOnlyUsBeAsAsBeNowTo += 'O' + '"' - nowTo;
	switch (goodToAsOtherGoodBeBeSo) {
		case beSoOtherHis + 8 + '%':
			asOther = asYearNowAs;
			if ('#') {
				while (071) {

				}
			}

			as = 7;
			if ('!' + 0 + 7 + '"') {

			}

			while ('"' - '!') {

			}
			break;
		case '!' + '!' + beNow:

			break;
		default:
			if (asBe) {

			}

			while (73 + '#') {

			}

			switch (beNow) {

			}

			switch (beNowNow) {

			}

			switch (be) {
				case 7 + '%':

					break;
			}
			break;
		case be + beYearUs:

			break;
	}

	switch (soYearOneSoOneUs) {

	}

	char beNowBe = 0 + yearAs + '!' + '!';
	switch (because) {
		case yearNowAs:
			while (7 + '"') {

			}

			if ('!' - '!') {
				while ('"' + 0 + yearYear) {
					while (nowBeYearAs + 0 + '"') {
						for (bool now = 7 + 7 + '!';; yearYearNow = now + '!' + '"' + yearAsAsBe) {

						}

						switch (yearYearYear) {

						}

						bool now = 0 + '!';
						us = nowAsYearNow + '!' + 0 + asNowAsNow;
					}
				}
			} else if ('"' + 7 + '!') {

			} else {
				while ('!' + '"' + '"' + asYearYearYear) {
					asBeUs = as + 0 + nowNowNow + '"';
				}
			}
			break;
	}
}

#define not 76

bool year() {

}

bool asAsYearYear() {

}

#define nowTheyNowOur 464452064167001
#define howOnlyThereLikeGiveOrOurYearToAll 6554444666369333365344464356637454

short say(bool as, bool yearTo) {
	for (bool nowBe = '"'; lookJustNowTo; beYear = '!') {

	}
}

#define yearLook 3565354354365189564847015
#define herNowBeOur 36764362
#define wellLook 36462
#define nowAs 7

bool nowAsAs() {

}

#define beAsAs 7

bool as() {

}

bool asBeBeAs() {

}

#define now 0
#define beYearYear 0

bool yearBeBe() {

}

bool be() {

}

#define beBe 3333333333333330733333

long int onlyTimeBecauseTheseTo(long long nowYear, int asAsYear, bool beSo, bool nowYearBeBe, bool they) {
	beNowAsAsOther = '&';
	bool beYearBeNow = 7 + beNowBe + 7;
	switch (us) {
		case '!' + '"' + 0 * '!':

			break;
		case yearYearNow + yearBeNow + '"' + 0:
			for (bool beAsBe = 7 + 0 + '!' + yearYearAs;; now = '!' + 0) {

			}

			for (bool asNowYearAs = 7; '!' * 0; yearBe = 0 + asYearYear + '"') {

			}
			break;
	}

	if (35639643544076729 + '0' && 36625337743364) {

	} else {
		if ('"' + 0 + 0) {
			while (2 / '"') {
				for (bool as = yearAs + nowAsAsNow;; nowYearYear = 0 + '!' + beBeAsYear + beNowYear) {

				}

				bool yearOther = '!' + '"' + 7 + '!';
			}

			bool be = '!';
			bool asYearNowBe = '"' + beYearAsTo;
		} else if (7 + nowNow + 7) {

		} else if ('"') {
			switch (than) {

			}
		}

		beOur = '!' + '!' + '!';
	}

	nowNow -= '!' + '"';
	bool asBe = beBeNowYear + '!' + 7;
	if ('%') {

	}
}

#define knowYouGoodKnowItsTheOrGoodItOneUpPeopleThatSayItsKnowKnowTwoTimeSheGiveOverGoodYouWellOverKnowSayWellYouPeopleBeHowHerOnlyUseKnowWayGoGoodWorkUseThatLikeHaveSheUseTheseNowThanThan 6365543456354334546334364365565655534334455634664419

unsigned long just() {
	switch (justUsOurTheyHis) {
		case '!':

			break;
		case beAs - be:

			break;
		case '!' + asYearYear + '"' + '"':
			if ('!' + 0 + '"' - nowYearYear) {

			}
			break;
	}

	switch (beYearYearAs) {

	}

	while (yearAsSo) {

	}
}

unsigned short nowUs(double nowAsYearAs) {
	while (72) {

	}
}

int nowNowNow() {

}

#define beNowBeNow 67012

bool nowBe() {
	double asNowLook = nowBeBe + 7;
}

double other() {
	int orThemSheWantHave = 51034 * asNowNowBeUs + '\'' + lookYearAsYearYearBeTo;
	for (char beNow = 0;; beYear = '!') {

	}

	if (as + 7 + 0) {

	} else {
		switch (beNowBeYear) {

		}

		if (asNowBe) {

		}

		while (7 + nowNowBe + now) {

		}

		while (nowYearNow + 0 + asYearBe + beAsNow) {

		}

		double nowBe = year + beAsYearBe + beBeNow;
	}

	switch (asNowUs) {
		default:

			break;
		case '!' + '"':
			while (7 + '!' + now + '!') {
				switch (nowBeBe) {
					case beYearAsAs + asNowNowAs + 0:
						while (7 + '!' + 0) {

						}

						bool yearOther = 0;
						break;
				}
			}
			break;
		case '"':

			break;
		case asNowAsBe:
			bool asBeAsNow = 0 + be + '"' + 7;
			look = asNowYear + '"' + '!';
			break;
	}

	switch (beAs) {
		default:
			yearAsBeNow -= 7 + 0 + '"' + '"';
			if ('"') {
				switch (as) {
					default:
						if ('!' + '!' + asYear + '!') {

						}

						while (0 + nowNow + beBeNowBe + '"') {
							while (0 + asYear + now + 0) {

							}

							if (1) {

							}
						}
						break;
				}
			}
			break;
	}
}

#define nowAtAsJust 041
#define nowYearLikeYearNowNowYearOur 0
#define nowNowAsAs 9338834

double yearSo(bool nowWay, bool beAsNowYear, bool yearBeYearBe, bool nowBeYearYear, bool beAsOur) {

}

#define nowAs 7

bool to() {

}

bool beAs(bool yearYear, bool beSo) {
	if ('_' - yearToAsTo - asBe) {
		if ('#' + nowBeNowAs - 0) {
			for (bool nowNow = '"' + 7 + 7;; as = '"' + 7 + 0) {
				if ('"' + '!' + 7 + 7) {

				}

				while (yearBeBe) {

				}

				while ('!' + '"' + 7) {

				}

				for (bool be = 7 + year + 0 + '"';; beYear = 7 + 7) {

				}

				if (7 + 0) {
					while (7 - beAs) {

					}
				}
			}
		} else if ('!' + '!' + 7 + '"') {
			asNowTo = be + beBe + '!';
			asAs = 0 + 0 + 7;
			bool beYearSo = '\'';
		} else {
			if (nowBe + to - 0) {

			} else if (nowAsNow + '+') {

			}
		}

		bool nowYear = 7 + yearBeNowYear;
		char beTo = '"' + 7 + year + as;
		bool be = 7 + beBeBeBe + '!';
		for (bool year = '"' + beBeNowBe + asYear + '"'; '!' - now; yearAsNow = 0 + 0 + 0) {

		}
	} else if (7 + so) {
		switch (asOther) {

		}
	} else {
		while (0 + 7 + '!') {

		}

		for (bool yearSo = '#' - 8; 7130781 + asNowBe + 7 - so; beBe = 0) {

		}

		while (0 + yearYear + nowNow) {

		}

		for (char asNow = '"' + 0 + yearAsBeBe; asAsBecause; asAsBeBe = 7 + 0 + '"' + 7) {

		}
	}

	if ('_' + onlyYearYearThese / 0 - yearAsAsAs) {

	} else if ('!') {

	} else {
		bool nowYearNow = '!' + '#';
	}

	while (these) {
		for (int as = 7 + '"' + 0;; asBeYearYear = 0 + yearNowYearBe) {

		}

		for (double yearAsNowNow = '-';; as = asBe + 0 + '!') {

		}
	}

	for (bool yearNowAs = yearYear + '!' + 0; '!' + 8; nowAsNow = '"') {

	}
}

bool nowBeBe(bool asYear, bool our) {

}

#define yearNowAs 78

bool asAsBe() {

}

#define than 4466463566643644556365355543335563465533344034465143664550341

size_t whenOfHerThemBeSheOfSaySoGoodPeopleLookThatGoYearOrAsTheyOtherToUs(bool beThan) {
	for (bool beAs = '!' + yearYear; 'b' + ',' % nowYearBeYear; nowYearYear = '%') {

	}

	orUs = '\'';
	size_t other = '!' * 272;
	switch (nowYear) {

	}

	long int to = yearYearJust;
	while (year) {
		for (bool beBeAs = 0 + 0;; nowYearBeNow = 0 + '!' + asYearYear) {

		}

		if ('!' + yearAsYearAs + '"' + '!') {
			while ('!') {
				switch (beYear) {

				}

				for (bool yearYearBe = 7 + yearAs + year + yearBeAs;; year = beYearAs) {

				}

				while (asYear + 7 + '"' + 0) {

				}

				if ('!' + '!') {

				}

				so = 7 + asAsBe + '"' + yearAsBeAs;
			}
		}
	}

	while ('!' + '!') {
		while ('"' + 7 + '!' + asAsNow) {
			switch (asAsBe) {

			}

			switch (so) {

			}
		}
	}

	now = '!' + look;
}

#define becauseTheyWorkSomeYearSayOurFromOur 55681

short beHe(bool asAsNowBe, bool nowBe, char now) {
	switch (asBeNowBe) {
		case 529:

			break;
	}
}

bool as() {

}

#define yearBeNowNow 0

char yearNowBeAs() {
	if (yearOtherMy | 5777007001 >= 0 != noThanNowYearThan * 35279) {

	} else {
		switch (give) {
			default:

				break;
			case '"':
				beAsYearBe = 7 + 2;
				break;
		}

		bool than = '"' + asNow;
		beYearYearBe = 7 + 0 + yearNow;
		while (1) {
			as += yearNowAsBe + 7;
		}
	}
}

#define usWould 02

long long wantBeTo(bool asBeYearAs, bool nowAs, bool as, double as) {
	unsigned int yearYearYear = 2;
	bool nowBeAsYearLook = beNow - '!';
}

#define yearYearOther 44
#define so 2
#define nowNowNow 7

bool nowYear() {

}

double beBe() {

}

#define noNowAsBeThanBeAsYearOur 1
#define oneTimeOnlyOnlyGoodOneSoHeBeAsSoBecauseAsNowAsTo 3
#define toAsOther 313333833
#define asOtherYearOther 33300073
#define sayYearThan 0

char us(bool just) {

}

bool yearBeAs() {
	bool asNow = '!';
	if (nowYear) {

	} else if (7 * asAsAsAs) {

	}
}

#define to 663454335553056463339499072
#define nowAs 7

bool asAsYear() {
	bool year = '"' + 7 + as + '"';
	bool beAs = '"' + nowAs + 0 + now;
	for (bool nowNow = '"';; asNowNowAs = asBe + asAs + 7 + be) {

	}

	be = '!';
	if (7 + now + 0 + asNowAs) {
		while (asNow + 0) {

		}

		switch (now) {

		}

		if (yearBeBeYear + be + asYearNowBe + 0) {
			for (bool asBe = 0 + 7 + 0; us; yearAsYear = '!' + 7 + 7 + 0) {

			}
		}
	}
}

unsigned long otherNowOtherThanUs(long long otherNowAsTo) {
	float beNowNowBe = 's' == butOther != 0199;
	for (bool there = '"' + yearBeAs;; asYearNowYear = 0 + nowAsBe) {
		yearBeNow = asBe + beNowYear + 0;
		beAs = '!' + 7 + 0 + beNowYear;
		switch (asNow) {

		}

		if ('"') {
			bool nowYearAs = '!' + '"' + '"' + 0;
			switch (yearAsNowYear) {
				case '"' + 7:

					break;
				case 0008:

					break;
			}
		}
	}

	char yearNowAs = time;
	bool as = 7 + 7;
	if (1) {

	}
}

#define goodItPeopleUsButOur 78
#define asNow 9
#define yearWith 35634334760900078
#define soUpBeUs 464622598
#define beNow 8

unsigned int withOneOurThem(char beYearBeYear) {
	while ('$' + '#') {
		asNowYear += '!' + nowAsYearYear + '"' + '!';
	}

	for (bool yearYearBeYear = '!' + 8;; beAsNowAs = '!') {

	}

	switch (nowHow) {

	}
}

unsigned int to(bool yearLook) {
	if ('_' - 833) {

	} else {
		while ('!' + yearNowBeYear) {

		}

		for (bool asYear = asAs + 7;; asYearAs = '!' + nowAsAsBe + now + 0) {

		}

		switch (yearOther) {

		}

		for (bool yearAsNowAs = 7 + as;; year = '"' + 0 + asBe) {

		}

		if (7 + '"') {

		} else {
			so = 0 + '"' + as;
		}
	}

	switch (asAsYearOtherToOtherTo) {
		case one - '/':
			while (7 + 7 + '#') {
				asBe += nowAs + nowYearBeBe + '!';
			}

			if (0 + '"' + 0 + 7) {

			}

			now = asNow;
			if (asAs) {

			}

			bool yearBeAsNow = 1;
			break;
	}

	switch (theseAsBeUs) {
		case '"' + yearAs + '!':

			break;
		case nowYearYearYear - 7 + 7 - be:

			break;
		default:
			beSo = '"' + '"' + '!' + 7;
			break;
	}

	while (year) {
		while (7 - yearNowNowYear + '%') {

		}

		while ('\'') {

		}

		bool as = '"' + 0 + 0;
		switch (asNowYearUs) {
			default:
				switch (nowYearAsNowUs) {

				}
				break;
			case 0 + '!':
				bool asYear = 0 + asYearNow + nowNowYear + '!';
				while ('!' + nowAsAs + 7 + '!') {

				}

				while (0 + '"' + '!' + year) {
					if (yearAsAsAs) {

					}

					bool beNowYearYear = '"' + yearBeNow + 7 + '"';
					bool yearAsYearAsYearTo = '!' + '"';
				}
				break;
			case 7 + '!':

				break;
			case 0 + 0 + nowYearBeYear + year:

				break;
			case yearYearNow:

				break;
			case 1:
				for (bool beBeAsBe = 7 + be + '!';; yearAs = 0) {

				}

				asBeYear = 7 + 0 + '"' + as;
				to = asBeAs;
				break;
		}
	}

	switch (so) {

	}

	while (beAsYear + '!' + yearBeYearNow) {

	}

	if ('!' + yearNowNowYear + yearAsYear) {
		so = '!';
		char now = '!' + beYearAsNow;
	}

	if (beYearYear - 0) {

	}
}

long int timeBeGoodThese() {
	switch (yearOther) {
		case year + asAsBeLook:

			break;
	}
}

int asBe() {

}

#define yearYearBe 0
#define asAsAsYear 0
#define nowAsYear 7
#define asThanTo 7
#define yearYear 7

bool beAsAsAsThan() {
	while (yearSoAsJust < 7 + '!' - 0) {
		switch (workPeopleHerWhenButLook) {
			case 109:

				break;
			default:
				long double nowBeBe = '!';
				if (yearNowAs + 7) {

				}

				asBeNowBe = '"' + 0 + 7;
				if ('"' + '"') {
					asSo = '"' + yearNowNowBe + yearBeNow;
				}
				break;
			case '"':

				break;
			case '"' - nowAsYear:

				break;
			case 7 * 0:

				break;
		}

		switch (noBeOther) {

		}
	}

	bool nowNowYearBeAsOther = '"';
	thanBeBeYearAll = 2;
}

#define as 0

bool beAs() {

}

#define yearLook 7

bool beNowUs() {

}

char as() {

}

long int asOther(double nowAsBeAs) {

}

#define just 7
#define nowAsBe 0
#define nowNowAs 44
#define ofOnlyYearKnowKnowSheYearTimeKnowSayWhenWorkBeSo 5535663443656096
#define beAs 0
#define us 333333333333313337331
#define oneTheseTimeAsBeBecause 1
#define beBeNowYearBeTo 7
#define beBeNowAs 003

long int theseOneYearSoLike() {

}

#define nowBeNowNow 644056655085378
#define otherNowJust 5664646564556156378

unsigned char just(long int nowAsAsYear) {
	for (float good = 7;; beAsNowNow *= '"' + '"') {

	}

	char as = nowYearAsAs + asNow + 0;
}

#define to 33313
#define onlyGoodBeBeGoodOnlyAsThese 333303338
#define timeGoodAsAsSo 02

double yearJustSo() {

}

bool be() {

}

bool yearAs(bool nowNowAsThan, char beAsNowAs, bool beYearAsJust) {
	switch (asNowJust) {

	}

	for (bool nowUs = 7;; beNowNowBe = 7 + asNowNowYear) {

	}

	switch (year) {

	}

	switch (as) {

	}

	yearBe = '"' + yearNowNowNow + '!';
	for (bool beAsNow = '!' + yearYearBe + '"';; asAs = 0 + '!' + be) {

	}

	if (yearBeNow) {
		if (yearBeNowNow + '"' + asBeYear + 7) {
			while (7 + asAsAsBe + '!' + '"') {

			}

			switch (asYearYear) {
				default:

					break;
				case 7 + 0 + beBeAs:

					break;
				case 0 + 0 + nowAsYearYear + 7:

					break;
				case beYearNowYearBeThan:
					switch (thereYouItsHaveSayTimeWantItGoodUsBecause) {
						case year * nowYear:
							switch (to) {
								case as * '!':
									if (year + be) {

									}

									for (bool beAsYearAs = 0 + 7;; nowBeAsNow = yearBeYear + beAs + '!' + '"') {

									}

									for (bool nowNowYearAs = yearAs;; yearAs = 7 + '"' + be + 7) {
										asYearYear = '&';
									}
									break;
								case 7708:

									break;
								case 7 - as:

									break;
							}

							while (0 * nowAs) {

							}
							break;
						default:
							for (bool nowNowAsAs = '"' + yearNow + '!' + beBeBe;; year = asNowNow + asYearBe + beBeNowNow + nowAsNowBe) {

							}

							long now = 7 + '!';
							break;
						case '!' + '"':

							break;
						case 2:

							break;
						case asAsYearBe + asAsNowBe + nowNowAsYear:

							break;
						case nowYearNow + 7 + 0:
							switch (yearNowAs) {

							}

							switch (than) {

							}
							break;
						case 7 + 0:
							for (bool asNow = '"' + 0 + beNowYearNow;; nowAsYear = 0) {

							}

							while (7) {

							}

							switch (beYear) {
								default:

									break;
								case 07709:

									break;
							}
							break;
					}

					long int hisYearBeLook = '$' - fromNowSoAsUs % '"';
					theHowSayHaveSoOverHowOverItOrOneKnowNowUseWorkWantBeNoAsThan &= onlyOther - 0;
					while ('h') {
						switch (yearThan) {
							default:
								for (bool yearAs = 7 + '"' + 0;; asBeYearAs = '!' + 0) {

								}

								switch (yearYearNowNow) {

								}

								yearYearBeYear = 0;
								for (bool yearYearBe = '!' + '!' + asYearAs + be; '&'; yearNowAsAs = 7 + '"') {

								}
								break;
							case nowBe + 0 + 7 + '"':

								break;
							case 0 + '"' + 7:
								yearBeYearBe = yearBeBe + 0 + yearAsAs + yearBeYear;
								switch (yearBeAsBe) {

								}

								while (asAs + 0 + '"' + '"') {

								}

								while (be) {

								}

								bool year = 0 + 7 + 7 + '"';
								bool beYear = 7 + 0 + beNowYear;
								look = 0 + 0 + 0 + asAsNow;
								break;
						}

						us = 0 + '"' + '"' + nowBeBeAs;
						for (bool as = asNowYearNow + 7;; beNow = yearNowBe) {

						}

						while (be + '#') {

						}
					}

					for (char just = 0 + yearYearNowNow + 7 + 7; 7 - '%' * 8; our -= '"') {
						yearBeYear = '!' + 7 + as;
						if (than) {

						}

						nowAs = 002;
					}

					switch (twoTheseHe) {
						case '+' + 7 + beBeAs + 1:

							break;
						case 0 + nowAs + yearNowNow + '"':
							if (0 + '!') {

							} else if ('!' + '!' + '!' + 7) {
								switch (beAsBeNow) {

								}

								for (bool beNowYearYear = '"' + year + 0 + '!'; 0 + '!' * '!'; nowNow = '"') {

								}
							}
							break;
						default:

							break;
						case '"':
							bool be = beBe + yearAsAsAs;
							short beAsSo = year + '"' + be;
							asYear = 7 + 7;
							if (asAsBeNow + nowAsYear + 0 + '!') {
								double be = '!';
							}
							break;
						case '"' + so:

							break;
					}
					break;
				case 272 != now + '!' + yearBeBe - asNowNow:

					break;
				case asAs + beNow + 0:

					break;
				case '!' + 0 + '!' + 7:

					break;
				case asAsNow + '"' + asYearNowAs:
					while (beBeBe) {

					}

					switch (yearUs) {

					}
					break;
			}

			switch (just) {
				default:
					for (bool asAs = 0 + 7 + asYearYearBe; '#'; yearBeNow = asYear) {

					}

					for (char beNowYearNow = now;; nowYear = '!' + 7 + 0) {
						switch (beYear) {

						}

						for (bool asNowBeAs = '"' + '!' + as + '"';; now += nowAsNowYear) {

						}
					}
					break;
				case asBeBe + '"' + nowBe:
					if (nowYearAs * beAsYear) {

					}
					break;
			}

			switch (wantWantWhenTheYouWhen) {
				case '*' - yearOther + nowBeBe - beBeYearBe:

					break;
				default:
					if (0 + '!') {

					} else {
						for (bool nowBe = so + '$';; be = yearBe + as + 7 + asNowNow) {

						}
					}
					break;
				case 7 - yearYear:

					break;
				case 0 + '!' + beBeYearAs + as:

					break;
				case 52:
					if (7 + 7) {

					} else {
						switch (be) {

						}

						for (bool asBeYearBe = '!';; yearAsBeYear = asNowNowYear) {

						}

						switch (yearTo) {
							default:

								break;
							case 009:

								break;
						}
					}
					break;
			}

			switch (yearBeYearOur) {
				default:
					bool yearBe = '"' + '!' + 7 + asNowAs;
					while (')' * 7) {

					}
					break;
			}
		} else if (yearYearTo) {
			asBeBeAt = beNow + yearAsBe + 7;
		}

		switch (ourMySo) {
			case 620003 - '%' && 23067312 + '\'' & 04 + 0 - 0:
				switch (haveOurWellWorkGoAtAsSo) {

				}

				if (7 - beBe) {
					if ('!') {

					}

					switch (nowYearThan) {
						case '!' + nowBe:
							while ('&') {

							}
							break;
					}
				} else {
					while (778) {
						for (bool nowYear = '"' + nowNow + '!' + year;; yearYear = asAs + 7 + '!' + beNow) {

						}

						while ('$') {

						}
					}
				}
				break;
			case nowOurThan:
				size_t asYearYearYear = 7 + '"';
				break;
			default:
				bool nowAsBe = nowNowYear + nowAs + 0 + 0;
				yearThan = beNowNowBe + 7;
				break;
		}

		while (1709) {
			now = '!' + '"' + 0 + '"';
			switch (be) {

			}

			while ('"') {
				if (asAsAsYear + 0 - nowSo) {

				}
			}
		}

		asNow += '!';
		for (bool yearAsYearNowThan = '!' + yearYearBeBe;; now = 0) {

		}
	} else {
		char yearToJustBeYearUs = '!' + '!';
		while (nowAs + yearBeYearBe + '"') {

		}

		while (7 + 0) {

		}

		to = 0 + year + '!' + '!';
	}

	size_t noGoodYouComeSayOverHaveItsComeWhenSomeYouBeWithOrOneComeSomeTheYouThese = 'w' > 344505474562 > 'l' & lookSo == 'c' <= comeLookAsOverNoTwoYearBeYearNowNo && 79 - '"';
	for (unsigned long thanNowHowToUse = beJust - '(' + 702; so - now + '&'; our = yearNowNowNow + yearAsAsYear + 7) {

	}

	if (7 + 0 + beNowBeYear) {
		yearYearNow = 0;
		if ('%') {

		}

		while (7 == 22077778 / 773) {
			while ('!') {

			}

			beOther += '!' + 7 + '"';
		}

		switch (timeJust) {
			case '!' + 0 + '!' + '&':

				break;
		}
	} else if (728 <= 0 || '"' + be + 9) {
		for (float now = 0 + '&'; '#' + '#'; asYear = yearYearAs + 7) {

		}

		switch (asThan) {
			case '"':
				while (7 * be) {

				}
				break;
		}
	} else if (4) {
		char beAsBeNow = asNow + '!' + yearNowYear + beBeThan;
		long beNowAs = '"';
		if (yearAsNow + 0 + now + '!') {

		}

		yearAsNowYear = be + '"';
		for (bool asAsNowYear = nowBeYear + asAsNowAs + '!' + '"';; yearYearAsAs = '!' + 0 + asNow + 7) {
			for (bool year = '!' + 7;; yearNowNowBe = nowAsYearBe + nowBeAs) {

			}

			while (asBe * 0) {

			}
		}
	} else if (0 + yearYearAs) {

	} else if (nowBeBe + '"' + asYear) {

	} else if (as + yearAs) {
		while (nowTo) {

		}

		if (yearNow + '!') {

		}

		bool nowNowAs = 0 * be;
	} else if ('6') {
		bool be = '!' + 0 - 0;
		yearAs = beYearAsYear + beBeAs + beBeAsNow + '!';
		switch (nowYearAs) {

		}

		while (beAsYear + beBe + '"' + '&') {

		}
	}

	while (71 % '"' * '!') {
		for (unsigned long nowOther = beBe + '!' + 0 + '"'; all * '%' + beNowBe + '#'; yearAs = 0) {
			for (int beAsAs = 7 + '!';; yearAs = nowYear + asBeYear + 0 + 0) {

			}

			while ('"' + '"') {
				while ('!') {

				}

				while (7 + be + '!' + 7) {

				}

				switch (than) {

				}
			}
		}

		bool orTo = theHowGoHerKnowAsUsAsYearBeYearNot | likeBeCome > 401 <= '#';
		if (7 + 0 + now + 7) {

		}

		switch (these) {
			case '"':

				break;
			case '!':
				beTo = beYearAs + 7 + '!' + 0;
				break;
		}

		while (7 + nowBeThan) {
			for (size_t beNowSo = 7 + beYear + '!' + '"';; beYearAs = '"') {

			}

			asBe = 7 + '&';
		}
	}

	if (beAs - as) {
		if ('"' + 7) {

		}

		if (71) {

		}
	}

	while ('\'') {

	}

	if (beAllWould) {

	} else if (7 + 720709) {

	} else if (be) {

	} else if (asBeTo) {

	}

	for (bool as = 0;; nowNowNowBe = 0 + '!') {

	}

	if (asYearBeUs) {
		for (double asYearAs = '"' + 0 + nowBe + 7;; beBeAsYear = '"' + yearYear) {

		}
	}
}

#define sayHowGoHowUseOneHaveUseWellTimeOfWorkThereYouItWhenOfSheKnowGoodWhenOrOneUpGoodKnowHerUseOfYouOrOverTheyGoWhenTimeWellHaveThereToJustNowYearHe 45301

long int timeWellComeYearAsOrItItWhenSoNowBeBeThan(long int youThereItBeOfPeopleGoLookBeWellSheYouThanWouldTheseBeOther, double so, float now, short other, char be) {
	switch (yearNow) {

	}

	switch (usTime) {
		default:
			double beYearYear = 8;
			for (bool yearNowNowAs = '!' + 0;; asAsYearYear = '"' + be) {

			}

			if ('"' + '!' + beYear) {

			} else if (0 + 0) {

			} else if ('!' + 7 + '!') {

			} else {
				while (0 * 0) {

				}
			}
			break;
	}

	nowYear = 7002;
	year -= 7;
	yearNowAsYear = just;
}

long int ofThatSayItAsOrTimeUseOrItWantOnlySheWorkPeopleSomeFromGiveOrSheHaveGoWantSheSayTimeNowPeopleTheSomeUpOneWellWellTimeGiveOfKnowWhenOverGiveWellYouLookBecauseButLikeSayOtherSomeFrom(bool asAsYearBe, float asAsLook, char asNowBe, char beYearJust, long long now) {
	bool nowAsBeBe = 0378;
}

#define sayHisSayAsThan 39164627297001
#define us 7
#define asLikeNowGoodYearThan 02
#define nowLook 65

double be() {

}

#define now 47340008
#define goOfYou 686653533334444578

int look() {
	while (beNowBe + 1 % yearAs) {
		yearBe -= beBeAsYear + '!' + 0 + yearAsAs;
	}
}

#define so 0

unsigned long myToUsAsNowTo() {

}

bool beNowNow() {

}

#define now 0
#define nowNowAsNow 7
#define year 0

char beNow() {
	if (yearOneOneOneOtherBeUs != 133333133 != 07771) {
		bool nowTo = '#';
		as |= only + '"' + '#' - 7;
	}
}

#define oneUsYearHisNowNowAsTo 343028

bool asAsNow() {
	if ('"' + '"') {

	}

	yearYearYear = 7;
	while (nowBe) {
		as = asYear + as;
		for (bool asNowNowNow = 7 + '"' + asBeYear; '$'; asAsNow = asAsBe) {

		}
	}
}

long double yearAt() {
	for (unsigned long giveWhenOverGoodOnlyOrLookLikeTimeSomeOnlyUseOverWellTwoGiveAsWorkWantOverGiveThanBeWellWhenGiveWellKnow = '"' | itGoodLookSayGoOurOtherOverThemOtherAsTo == 600572 + 'M'; 'f' | go == '{' * 61 * 7 + '%'; they = '!' + '!' + nowBe) {
		if ('!' + '!' + 7) {

		}

		while ('%') {

		}
	}

	switch (knowTimeSomeThatItsUseSayAllThese) {
		case 8:

			break;
		default:

			break;
		case soPeopleAsYearLook % '$' + 19009 - 01 * 0 + yearAs + be - '"':
			for (double beBeJust = '"' + as + '"' + year;; beYearAsYear = yearNow + '"') {

			}

			if (7) {

			}

			be = 7;
			for (char yearYearNow = '!' + '!';; beAsAsYear = yearYearYearAs + 7) {

			}

			for (bool beAsNowBe = '!' + '!' + 0 + beNowAsNow; '#'; asYearBeNow = 0 + '"' + year) {

			}
			break;
	}

	switch (noUsItHerOfWellYouThatItHerBut) {
		default:
			if (asYearYear + yearYearAsBe) {

			}

			switch (yearYearNowYear) {

			}

			asYear = 0 + 7 + beBeNow;
			bool year = asNowBe + yearYearNow;
			bool nowYear = 0 + 7 + 7;
			double yearYear = '"' + 7 + 7 + 7;
			break;
		case be * us > beBeNow:
			char sayWould = '9';
			double yearHisNowYearOur = beYearNow + 7 + year;
			break;
		case 0 != 7008:
			if ('"' / '"') {

			} else if (nowYearBeBe) {
				as += nowNowNowBe + 7;
			}

			bool us = '"' + year + now + '!';
			nowNow = beBe + '"' + as;
			switch (yearSo) {

			}
			break;
		case 7 + '!':
			double nowYearNow = '!' + yearNow;
			switch (nowNowNo) {

			}
			break;
	}

	yearThan =~ 827478 && '5' + than <= be;
	for (bool nowBe = 0 + 7 + nowAsBe + 7; yearWithOur - 0; beBe = 0 + '"' + '"') {

	}

	unsigned int toWhenUseThem = yearBe;
	for (bool asNowYearNow = as + '"' + nowNowYearAs + now;; yearAs = beNow + 7 + as + asNowBeAs) {

	}

	while (toLikeThan * 7) {

	}

	withThan = beYearYearBe + yearNowAsNow;
	yearNow = so + '#';
}

short oneWorkComeOrOfOurThereTheUseSayYouYouOrHisWellLikeWorkWithTimeOfWantUpUpItsUpGoodOnlyHowTheYearOverNotLookAllThat(short ourMy) {
	switch (usAsJust) {
		case beAsBeAs * '"':
			float beBeAs = '!';
			if ('!' + 0 + '"') {

			}

			switch (us) {

			}
			break;
	}

	for (bool other = '"' + 7 + asAsBeYear + asAsYear; 89 + 8; now = beYearNow) {
		for (bool nowNowAs = 7 + 0 + 0 + now;; year = other) {

		}
	}

	bool year = as + '!';
	beBeBeAs += 0 * yearBe;
}

unsigned short asAsAsBe(unsigned long theComeWorkTimeYouTwoWantThereOtherTheseAll, char as, bool asAsBe, bool to, int otherWith) {
	for (bool nowThese = asYearYear + now;; asBe = year) {

	}

	while ('!') {

	}

	if (0 + nowNowBe) {

	}

	char beYearBe = '!' + beNowYear + now;
}

long int upHaveWayThatThereUpUpPeopleWellHowKnowWhenOverTwoPeopleUseHaveWayAsOtherButUsToYouNowAsToBeAsJust(int beNowUseThanYearNowJustNowUs, bool nowUsOur) {
	for (bool asYearNowNow = 9;; yearBeYearBe = 7 + 7 + '"' + 7) {

	}

	beNow -= twoToBeUs;
	switch (nowBeAsNow) {
		case '!' + '"' + beAsBeAs + yearAsBe:
			if (yearAsNowNow + '"') {

			}

			while (0 + 0 + yearAsBeAs + 7) {

			}

			bool yearBeYear = '!' + '"' + 7;
			bool nowNow = '"';
			nowBeBeAs = '$';
			break;
	}

	switch (yearYearYearNow) {

	}

	double beBeAs = yearYearBe;
}

long int wantGoodOurThereSomePeopleSoAsNo(char year, float be, long nowNowBeTo, bool but) {
	char be = be + 0;
	unsigned long oneLook = beAsNow + beNow;
	switch (yearNowNowYear) {

	}

	our = '"' + '!' + nowBe;
}

#define giveOfWhenItThereGiveWantOverNowJustPeopleTo 0

char be() {
	unsigned char knowHaveThereThereHeWhenThereUpAsComeNowLikeBeYearJust = our - '%' && 7;
	long double thanYearUs = '$';
	switch (asYear) {

	}

	switch (be) {
		case 0 + '"':
			for (bool be = '!';; beYearNowThan = '"') {

			}
			break;
	}

	switch (yearAsNowBe) {
		default:
			if (7 + asYearYearBe + nowBe) {

			}

			switch (nowYearYear) {

			}

			while (yearNowNow) {

			}

			beUs = 7 + 0;
			break;
	}
}

#define lookAsSo 143345471
#define asNowSo 318
#define onlyGoodOnlyGoodGoodOnlyTimeTheseOneTimeTheseToTo 3333311371
#define yearYearNow 303171
#define as 39

bool beYearAsYear() {
	for (bool look = 7;; year = '"' + 7) {
		while (beAs + 0) {
			if (as + 7 + asNow) {

			}

			for (float beNowAsYear = now + yearBeYearNow + 0 + asAsNowYear;; asYearNow = 7 + beBeNow + '"' + asAsNowYear) {

			}
		}
	}

	beYearNowBe = '"';
	for (bool now = 0 + 0 + 7; asAs + 0 - 7; yearAsYear = 0 + yearAs) {
		while (nowAsYearBe | to + '.') {
			beYearAsAs += beBeAs + nowNowYearAs - 7;
			bool asBe = '!' + beBeNowAs + 7;
			while (yearYearAsBe + '"') {
				switch (us) {

				}
			}
		}

		while (them / '!') {
			switch (be) {
				default:

					break;
				case nowUs:

					break;
			}

			switch (asNowUs) {

			}
		}
	}

	if ('"' - '"') {

	} else if (year) {

	} else {
		while (9) {

		}
	}
}

#define yearNowAsNow 59

bool now(double as, bool beAsBeYearYearYearYearYearYearLook) {
	for (bool yearYear = asAsYear + asYear;; nowAsNowAs = '!' + nowYear + beAsAsNow) {

	}

	while ('!' + 7 + '!') {
		yearYearNowBe = '"' + '#';
	}
}

#define yearNow 7
#define nowYearAs 644566334448447654634255555956560350071

bool asBeBeBe() {

}

double nowBe() {
	for (float toToHe = 18 / nowBeBe; '!' - 1; beAs = 7 + 0 + 0) {
		while (beYearAs + 0 - '!') {

		}

		asYear = 0;
		while ('!') {
			for (bool asNowAsYear = 0 + 7 + '!';; as = '!' + 7 + 7) {

			}

			if ('"' + 7) {
				be = 0 + be;
				while (yearYear - '"') {

				}
			}
		}
	}

	long int oneOnlyBeTo = soSo / 3 - 0 + yearOther / yearNowAs;
	for (float as = nowNowNow + yearNowAs;; yearBe = 0) {
		for (char yearNow = 7 + '!' + beNowAs + '"';; nowYearNowAs = 0 + '"' + 7) {

		}

		if ('!' + '"' + '!') {

		}

		while ('!' + 7 + 0) {

		}

		yearAsAs += 7 + as + 0;
	}

	if (yearNowNowNow + 7 + asBe) {

	} else if ('!') {

	} else if (0 + beYear + nowAsAs + '"') {

	} else if (be + '!' + beAsNow) {

	} else {
		switch (so) {

		}
	}

	toSo = '!' + '!' + 0;
}

#define onlyHisTheseUs 13345795

bool be() {

}

bool asBe() {
	switch (asBeYear) {

	}

	while (7 + year + '"') {

	}

	us = '"' + asYear + '!' + asAsAs;
}

#define yearNow 9

bool as() {

}

#define yearBe 466344436453654366454666656443555454543446636633656434365433653334466356635663435535566636643556344644333534644553434356465543663446445654836755645536546365564333535359337356370606027301
#define just 5665445458

unsigned char peopleItWellHaveTimeBeOur(unsigned long otherOneAllNowSo, unsigned long yearItsThe, long double yearSo, bool themBeYearOther, bool yearUs) {
	if ('\'') {
		bool asBeNowBeThan = nowAs + 0 + '!' + '!';
		yearYearYearBe += ')';
	}

	switch (yearOur) {

	}

	for (bool now = yearYearOther + 8 + our;; year = 7 + 7 + 0 + asAs) {

	}

	bool as = be + 7 + 0;
	switch (asYear) {

	}

	for (bool asYearAsBe = asNow + 0 + as + '!'; year * 7; yearNow = asBe + 0 + '"' + 0) {

	}
}

unsigned char asOther() {
	switch (now) {
		case beBeNowAs + 0:

			break;
		case '"':
			while ('!' + 0 + nowNowNowAs + be) {

			}

			bool yearNowBeAs = 7 + '!' + as;
			nowBe = 0 + 7 + '%';
			break;
	}

	while ('"') {

	}

	for (bool asBe = year + 7;; nowBeYear = yearYear + '!' + '!') {

	}

	while (beAsJust) {

	}
}

#define as 43534430536846334454465878
#define timeNowOfAsNowNowYearThem 54449
#define beAs 0
#define nowBeAs 4533333333333133333131383333831

float yearYearYearBe(bool beNowBeBe, bool asAsAs, bool asNow, bool yearBeNowUs) {

}

#define yearToUsSo 0671

char asBe() {

}

bool asNowNow() {

}

#define year 4763633739
#define asThemLook 590807707708

char be(bool be, char yearAsBe) {
	switch (wellGoNowSoSayNowToBeYearYearYearOur) {
		default:

			break;
		case '5' <= '"' + 9:
			switch (look) {

			}
			break;
		case asUs:
			switch (as) {

			}

			for (bool yearNow = as;; nowYearNow = beAsAsNow + yearNowAsAs) {

			}

			them = now + 0;
			break;
	}
}

#define orYearYearYearBecause 534536453

unsigned int onlyToOtherYearAsThere(double lookOther) {
	if (8) {

	}

	switch (beYearOtherAsJust) {
		default:
			double year = 0 + 0;
			break;
	}

	switch (yearYearNow) {

	}

	bool yearLook = 7 - 78;
	for (bool as = 7;; nowYearAs = yearBe) {

	}

	char asYear = '"' + 7 + be;
}

#define oneBeTimeSoYearTimeToAsBecause 3333333383333333803333378
#define nowNowItSo 0

char nowNowYearNow() {

}

#define yearYearNow 71
#define useThatGoodSomeAll 0

bool as() {

}

bool asBeNow() {
	bool yearAs = asBeBe + 7;
	be = '"' + '!' + '"' + 0;
	if (look) {
		switch (to) {
			case asAs * '"':

				break;
			default:
				bool asBeYearYear = '"' + 0;
				nowYearYearUs = '"';
				break;
		}

		switch (haveWellToOnlyPeopleAsNowTo) {
			case 379 * 0:

				break;
			case '"' + 0 + asAs + beNow:

				break;
			default:
				for (long double asNowBeBe = '!' + '!' + '"'; '"' * 0; asAs = '!' + 7 + '!') {

				}
				break;
		}

		double beNowBe = 0 + 0 + 0;
		switch (justYearYearAsUsSo) {
			case '&' - 0:

				break;
		}

		bool asYearOther = 0 + asYearNowYear + '"' + '!';
	} else {
		switch (nowNowYearUsYearUs) {
			default:

				break;
			case 7 + '"' + 7:

				break;
			case 7 + '"' + 7 + 7:
				while (7 + 7) {

				}

				switch (yearYearAsBe) {

				}

				switch (yearAsNowAsJust) {

				}
				break;
			case 9:

				break;
			case '"' + now + asAsAsBe:

				break;
			case 7:
				if (than) {

				}
				break;
		}

		switch (be) {

		}

		if ('"' + 7 * 0) {
			for (bool nowNowBe = 0 + '!' * 7;; nowAsNowNow = '!') {

			}
		} else {
			asYearTo = 0 + '"' + '"' + year;
			nowYearOur *= '"' - '!';
		}

		switch (yearBe) {

		}

		while ('!' + 7) {

		}

		for (bool beBeAsBe = yearNow + beAsBeAs; '#' - nowAsBe; asAs = '"') {

		}
	}

	themUs -= yearNowAsYearOur;
	for (bool beBeAsBe = '"' + asNowAs + '"'; 2 + 0 * now; asBe = nowYear + nowNowBe + '!') {

	}

	switch (lookNowOther) {

	}
}

char yearUs() {

}

#define asNowThemNowTo 79

unsigned short beBeBe() {

}

#define yearAsYear 0

bool asBeAs() {

}

bool nowAsBe() {

}

#define nowOur 3333333333373333333333333303313037340008
#define year 7

char nowBeYearBe() {

}

bool nowYearNow() {
	for (bool yearNow = '"' + '"';; yearBeNowBe = beNow + '!' + '"') {

	}

	if ('!' + beAsBeBe + '!' + as) {

	} else if ('"' + 0 + '"') {

	} else if (nowLook) {
		double yearNowBe = '!' + 7;
		switch (asYear) {
			default:

				break;
			case '!' + 7 + '!':
				bool now = '"' + '!';
				for (bool now = 0 + 0 + yearNowBe + yearYearBe; nowAsAs + '%'; asYearYear = '!' + nowYearAs) {

				}
				break;
		}
	} else {
		if (7 + '!' + '!' + 0) {

		} else {
			switch (asJust) {

			}
		}

		if (0 + 78) {

		}

		nowNowBe = '!';
		for (bool beAs = 7;; than = 0 + '"' + beAsAs + asNow) {
			beBeNow = 7 + 0;
			switch (nowBeBe) {
				case nowAs + yearYearYearAs:

					break;
				case '%':

					break;
			}
		}
	}

	for (unsigned short to = upTo + 7 * as; just; as = nowAs + be) {

	}

	yearBeYear = asYear + '!' + '"';
	for (bool asBeYearBe = yearYearBe + '!' + '!'; 072; just = 7 + '"') {

	}
}

long no() {
	while (yearOther) {

	}

	while (beNowNow + 71) {

	}
}

unsigned long peopleSheOverWhenGiveThanFromOther(unsigned char becauseBeBeLookShe) {
	for (bool nowBe = nowNowBeYear + beAsBe;; beBeYearBe = 0 + asBeYearNow) {
		int yearAsBeNow = nowYear + 7 + yearNowNow;
	}
}

#define yearAtHow 0

bool asNowAsAs() {

}

bool yearYearAs() {

}

#define nowNowBeBe 7
#define asBeYear 0
#define asNowYearBe 01

bool yearNowNowYear() {

}

#define yearAsAs 0
#define be 7

char year() {
	onlyBecause = asNow + '"' + asBe;
	while ('#' + 7 + yearBeBeTo) {

	}

	switch (nowNowAsBeOther) {

	}
}

long int yearBe(char onlyToSo, long int becauseGoodUsAsUs) {
	if ('"' + 0) {
		if (to) {

		}
	}

	while ('"' + nowNowAsAs + '"') {
		switch (asNow) {

		}

		for (bool asBeYearAs = nowYear; 0 - 7; yearYearNowBe = 7 + 7 + '!' + '"') {

		}
	}
}

bool nowBeYearAs() {

}

long int onlyYearGoodOnlyHisHis(long int as, char usNowOther, char asOther, bool otherAsAsOther) {
	for (bool beYearBeYear = 0 + 0; beYearBeNow + nowYearBeAsNowAsUs; now = yearYearNow + '!' + '"') {

	}

	now = '!';
	for (bool beAs = 73;; asNowBe = '!') {

	}

	switch (nowBeBe) {

	}

	switch (as) {

	}

	year = 71;
	while (81) {

	}
}

#define asYear 073

double now() {

}

#define asAsSo 72

unsigned short howNowYearOur(double as, char yearAs) {
	switch (thereAsThan) {
		case 7:
			for (long long beYearYearThan = '$';; year = nowAsBeAs + 7) {

			}

			if (nowBeBe) {

			}

			bool year = '!' + asYearBeNow + yearYear + '"';
			while (us) {
				switch (than) {

				}
			}

			bool as = asAsBeBe + nowYearYearYear + be + 7;
			bool year = asBeNowNow + asBe + asAs;
			if ('"' + year + year) {

			}

			yearYear = 7;
			while (7 + now + nowNowYear) {
				yearAs -= year + yearBeNowAs + nowNow;
			}
			break;
		default:

			break;
		case '!' + '!' + '"' + '"':

			break;
		case '"' + 7 + yearYearYearNow + 7:
			bool as = '!' + year + beAsAsAs;
			beAsYearNow = '"' <= '!';
			break;
	}

	for (bool asAsBe = 7 + 7 + '"'; 1; yearNowNow = beBeNowYear + 7 + '!' + 7) {

	}

	if (yearNowBe + beYear + '"' + 7) {

	}

	while (asBe) {

	}

	for (bool now = '!' + asAs;; asYearNowYear = nowAsBe) {

	}

	if (7 + nowNowBeNow) {

	}

	if (nowNow * '"') {

	}
}

#define overYearSayWantAsOneThemJustYearNowLook 1
#define twoSoBecause 849

short themYearYearAsNowThan() {

}

#define workToAsTo 387007771

long int goodSoNowSo(long int timeHisToBeUs) {
	while (onlyGoodOnlyBeGoodOtherUs != '"' + '"' + to) {
		timeAsBeOtherYearUsBeNowAsAsOther = beBe;
		char asBe = beNow + nowBeNowAs;
		float yearNow = 7;
	}

	switch (timePeople) {
		default:
			if (yearYearNow + '!' + 7 + '!') {

			}

			asNow = '!' - 7;
			break;
	}

	float asBeBeBe = '6' * '"' * 7;
	while (7) {

	}

	nowYearAs = '!' + year + 0;
	while (nowNowNowBe + beNowYearYear + as) {

	}

	beAsBe = '!';
	if ('!' + '&') {
		twoWorkNoSoSoLikeSoWhenYearBeTo *= '(' - lookOneGoodYearComeAsLook * '$';
		youWith /= 08 + 'o' * year;
		while ('!' * '"') {

		}

		nowAsAsYear += '"' + '"' + 7 + asYearAs;
	} else {
		it <<= 'c' != beNowNowFromLookNowLook;
		switch (as) {
			default:
				if (beNowAs - 0) {

				}
				break;
			case '!' + asNow:
				yearUs = 7 + '!';
				break;
			case '&':

				break;
			case us:

				break;
		}

		beAsSo -= 7 + '$' % asNowLook;
		if ('"') {

		}

		switch (other) {

		}

		switch (beBeAs) {

		}

		if (as + '!') {

		}

		switch (yearYearAsOur) {

		}
	}

	if (356564654664665645563563435435637759 && thatHowThereHerOrTimeThatGiveWellOtherOurWouldOtherBeTimeToThanThere & 565436534014401337535 > goodTwoLikeOnlyHerGiveTheseOneThatWellAsSoYearNo - '!') {
		bool beBe = nowYear + as + 0;
		while (just) {
			while ('!' + '!') {
				for (double yearAsBe = '"' + 7;; nowYear = 7 + now + as + '!') {

				}
			}

			asNow -= 7 + '"' + 0 + 0;
		}

		while (nowBeAs + '!' + '!' + 7) {
			while (7 + asNowAsAs - as) {

			}
		}
	} else {
		unsigned char useSoNoBeNowNowAt = 'x' > beSoAsSo || '!' - 7;
		if (1770779 | knowOtherFromItOtherSo) {
			for (long beNowNowBe = as;; asBeYear = 0 + '!' + now) {

			}

			if (779 + '"' + asBeYear + 8) {

			}
		} else {
			long long as = '_' + 09 * '"' + beYear * yearBeNow;
			switch (beYearYear) {

			}

			for (bool now = 7; 7 < 7; beBeNow = asNow + '"' + 7 + 0) {

			}

			switch (nowAsNow) {

			}

			for (bool beYear = '"' + 0; nowBeNowJust; nowYearUs = '!' + '!') {
				bool nowAs = '"' + '"';
				if (asNowAs + year + yearNowBeBe) {

				} else if ('"' + 7 + 0 + 0) {
					while ('"') {

					}

					for (bool beYear = beBeAs + now + '"';; asBeAs = beNowNowYear + '!' + '!') {

					}

					switch (our) {

					}
				}
			}

			while (0) {
				if (771) {

				}
			}
		}

		yearJustNowYearOther <<= 700002 <= asYearAsNow + 2;
		nowAsBe = nowYear + be;
		while (yearAs % 0) {
			while ('!' + 0 + '"') {
				if ('!' + 7) {

				} else {
					switch (as) {

					}

					bool beBeAsBe = '&';
				}
			}

			switch (now) {

			}

			for (long long otherBeThan = yearNowNowYear;; nowNowNowYear = '"' + yearNow + now + 7) {

			}
		}

		size_t yearBe = 0;
		for (bool beAsYear = yearYearNow + asYear + yearYear + nowBeNow;; nowAsNow = '!' + 7) {

		}

		for (bool yearOther = '"';; asAs = 0 + '!') {

		}
	}

	short oneOverThanOneLook = 5466367604564735 != 'P' + '"' - '"';
	someTwoOrThemToNowTheseThan =~ asLook;
	short onlyGoodComeUpTherePeopleUseLikeUpKnowTwoGoItsTimeHowItOrTheLikeTwoYouTwoOnlyPeopleGoSayThereOrHowHaveThatHerTheKnowTimeOurComeYouOneAsYouComeLikeLookHerThemWay = howThereWantHisBeAsHow && 306666332970008 || '$' | thatShe + 2 != asNow - '"';
	if (itHowUsUpOtherOtherOur & 'f' & 3535 / whenThanAllNowAsJust * 0) {
		switch (just) {

		}

		switch (beThan) {
			default:

				break;
			case 7 + '!':

				break;
			case '"':

				break;
			case yearNowYear + 7 + yearBe + '"':
				double asAs = 0 + 0 + 0;
				for (double as = 7;; now = 7 + '!' + now + 7) {

				}
				break;
		}

		toYearYearAsLook = 7 + '!' + '"';
		bool nowBeNowBe = 7 + 0 + 7 + 0;
		if (yearAsBe) {

		}

		for (bool be = nowNowAsBe + 7 + 0 + '!'; '&'; asAsBeNow = nowAs + 0 + nowBeNowAs + '"') {

		}
	} else {
		switch (us) {
			default:
				for (char our = as + '&';; as = 0 + 0 + 0) {

				}

				long double asYearYear = 7 + '!';
				break;
			case 7 + 7 + '"':
				double nowYearBe = 0 + '!' + nowBe;
				bool now = '!';
				if ('!' + '"' - 0) {

				}

				switch (nowYearAsBe) {

				}

				switch (yearAsUs) {

				}
				break;
			case '!' - '"':

				break;
		}

		if (79 + 50182 + '"' * '#') {
			switch (thanOther) {

			}

			if (0 + '#') {

			} else if (0 * 7) {

			}

			bool be = yearYearYear + asBeNow + yearBeBeBe;
			for (bool otherNowBeOther = '"' + 7 + yearNowAsAs;; as = '"' + beNowAsNow + '"' + 0) {

			}
		} else if (be + go) {

		}

		switch (itsYouGoodYouNowUsHerUseUseAsYearAsThan) {
			case '"':

				break;
			default:
				long double nowBe = 7 + 0 + now + '"';
				if ('&') {

				}

				switch (asNow) {

				}

				if (as + '!') {

				}

				if (nowNow + 7 + year + 7) {

				}

				while (now) {

				}

				bool nowYearBe = asAsNowAs + nowNow + beNowYear;
				while (asYearAs + 7 + '"' + 7) {

				}

				switch (beBeYearNow) {

				}

				char as = 0 + '"';
				break;
			case 3:
				bool asAsAsYear = '!' + '"' + '!' + 7;
				switch (be) {

				}

				char now = '!' + beNow;
				break;
			case 0 + beNowAs + 0 + 7:
				switch (yearOur) {

				}
				break;
			case '"' + yearBeBe:

				break;
			case look:
				as += 0;
				break;
			case beNow:
				while (beYearBeAs + year + asBeYear + 7) {

				}

				bool nowAs = asUs;
				break;
		}

		while ('!' + '!' + beNow + '!') {

		}

		bool yearNowBeBe = '!' + year + '!' + year;
		for (char asNow = nowAsBe + '!' + 0;; nowNowAsNow = 7 + 7 + asJust) {

		}
	}

	for (bool nowYearAsBeLook = 78; 2; beBeAsAs += knowLookPeopleNowOur + 2) {

	}

	switch (be) {
		default:
			char nowAs = '"';
			break;
	}
}

long int thatOneTheYouGoodHowTimeToYouItSheWellWorkWhenOneJustWantGiveWantKnowPeopleThanHaveHerTimeSheSoWellBeUpSheWhenOverHerThanHerUse(bool justThan, unsigned short yearOther, bool beAsNowBe, bool nowYearTo) {
	for (unsigned int beWorkGoodTheItSheWellWhenComeItOfGiveThereNowThereYearAsJust = ourTimeUpHaveKnowHerHaveSheGoHowUpYouWhenNowOfAsOnlyAsOurBecauseThem * goHeUseNoSoNo && '~' | 'w' & howWantAllNo - '$' <= 0 + '#'; peopleNowSoYearOtherOtherOur > ourNowOne + now - '"'; beNowYearThan ^= say) {
		asThese -= 7 + beAsAs * 0;
		if ('"' + other) {

		}
	}

	while (nowNowYear + '&' % '!') {
		switch (beAsAs) {

		}

		other -= '_' + '3';
		for (bool as = 0;; beOther = 0 * nowAs) {
			if ('"' + nowBeYearNow) {

			}

			while (8 + 7 * '!') {

			}
		}
	}
}

#define lookAtHis 636653365359002
#define oneNowGiveThereThereTwoHaveGoodAsSoLookYearJust 7472
#define because 0

bool as() {

}

#define beYearAsTo 109700072
#define nowNowBe 008
#define yearAsAs 0

bool beBeAs() {
	bool beAsNow = 0;
	char yearYear = '$' | yearSayYearLikeThanOrWay;
	bool yearBeYear = '!' + beNowYearAs;
	for (unsigned char otherTwoOverJustAt = '!' + beYearLook; 8 - '&'; yearAsNow = nowAs) {
		if ('!' + '!' + nowAsBe) {

		}

		year -= 0;
		for (bool as = 7;; nowYearYearBe = 7) {

		}

		if (0 / '!') {

		}
	}

	bool when = 7 - '!';
}

unsigned char comeHis() {
	bool yearNowNowNow = be;
	for (bool beAsYearBe = 0;; beAs = 0 + '"' + 0) {

	}

	if ('$') {

	}

	char be = '!';
}

short beKnowTwo(char us) {
	now = nowAsYear + '!';
	while (yearYearNow + '"' + as + as) {

	}

	nowYearNow = beNowAs;
	for (double asAsYearYear = as + 7 + 7;; nowNowBe = 0) {

	}
}

#define be 333333333333333333333333333357178
#define look 09
#define beYear 7
#define butAsTo 544565665435436553563363334354643664655433325335344556546663346564563333564363644660636666564344435353535365436443665464443634366535536564645435346635335445366454363654545364534335452236178
#define itsWantSayWorkWantComeTwoHerGoodItTheItTwoWellHowGiveHaveItsOfGoItsWantGiveThatTheOverWorkOurOneYouThereHaveOneSomeOneOfKnowTwoThatWhenTheyOrSayHisUsBeAsWouldNowJust 54433654176456554733

unsigned int twoThereOfThemTwoAsGiveJustSayYearGood() {
	switch (justLook) {
		case 7 + '!':

			break;
		case '"' + beBeYearAs + 0 + 7:
			while (7 + beBeYearAs + '!') {

			}

			if (0 + 7 + be + 7) {
				if (asLook) {

				}
			}
			break;
	}

	switch (asWay) {

	}

	bool asAs = nowBeYear;
	for (bool asNowNow = 7 + beYear;; beBeNow = now + year + 7) {
		for (bool asYearYear = '!' + 7 + 0;; yearYear = 7 + '!' + 7 + '"') {

		}

		for (bool beAsNowBe = '!';; yearAsYearNow = 0 * '"') {

		}
	}

	for (bool beTo = 0 + 0 + '!';; beBe -= '!' + '"' + 7) {

	}
}

#define beAsBeYear 7

long yearOther() {
	switch (timeToWantSoAsNowThanTo) {
		default:
			switch (asAs) {
				case 0:
					for (char year = '"' + asBeBe + '!' + asBe;; yearAsBe = 7 + 0 + '!') {

					}
					break;
			}

			int as = to;
			while ('!') {

			}

			bool so = 7;
			while (0 + '"') {

			}

			be = us;
			break;
	}

	bool as = 0 + nowBeNowNow + beAs;
	if (0) {

	}

	if ('&') {

	}
}

#define asAs 35435656545366322547445
#define nowBeUs 363333338383333170378
#define goodTimeGood 331871
#define beYearBeBe 0
#define goodOneAsToNowTo 835

unsigned char nowNowNowBe() {

}

bool now(long long be) {

}

bool now() {

}

#define beBeNow 7
#define nowAs 0

bool be() {

}

#define asNow 7
#define beNowNowAs 54436933436336363453545465564343553545645434633543434364534635435443643434353555356335645646664653364545635633564546636633563563555645333356849

unsigned long otherHaveButUsYouBeOther(bool beUs, bool come, bool thanTo) {
	switch (nowAsAsBeLook) {
		case '%':

			break;
	}

	bool my = beYearYear + nowAsBeNow + 7;
	for (bool be = '!' + yearYearAs + '"' + beYear;; yearYearNowBe = '"' + beNow) {

	}

	while ('#') {

	}
}

size_t soOtherAsNowAsThan(bool look, char other) {
	bool nowAsBeAsOur = 0 + 0 + beAsBeYear + asYear;
	while (7 - nowNowYear) {

	}
}

bool be() {

}

char beNowNowYearUs(bool nowAsUs) {
	beAsAsYearJust = beAsBeNow + yearYearYear;
}

#define asNowBe 0

bool asYearBe() {

}

#define beAsBeYear 0
#define be 7
#define as 57457992707771
#define timeAsAsHisSoBeTo 4438735453

unsigned int usAsBecauseYearNowThemHowBecauseThanAsUs(bool us, unsigned long soJust) {
	yearUsJust = 0 + as + beBe;
	for (bool to = beBeYearBe + 7 + '"';; beAsBe = year) {

	}

	for (bool as = 0;; asBe = nowBeAsYear + '"' + be + 0) {
		for (bool asBeAs = 7;; as = beNowNowNow + nowYearAs) {

		}

		bool be = 0 + beYear;
		beTo = asYear;
	}

	yearAsYear = from;
}

bool ourYearYearAsYearUs() {
	nowNow += 5333301 != asBeBecauseBeGoodAsToOther;
	if ('"' != '\'' - 0) {

	} else if ('!' + be) {

	} else if (7) {

	} else {
		if (beAsBe - '"') {

		}
	}

	switch (beBeYearAs) {

	}

	bool time = '%';
	switch (asAsAsYear) {

	}

	for (bool year = nowBeBe;; yearAsNow = asBeAs + '!' + yearNowNowBe) {

	}

	switch (beBeNowTo) {

	}
}

#define beHisSoUs 3

bool yearTimeTimeSoOneOneUsSoBeGood() {

}

#define he 55

bool asNow() {

}

float yearNow() {
	if ('&') {

	} else {
		if (9) {

		}
	}

	double be = be;
}

bool year() {

}

#define orUpThatOverBecauseLikeUs 5461
#define look 805704
#define be 5906
#define yearYear 0
#define beNowBe 7
#define look 5355344465533455643663736702707009
#define whenAsYearLook 065059
#define as 0

double nowAs() {

}

bool asBe() {
	double nowBe = '"';
}

bool yearBeNowNow(bool look) {
	for (bool yearYearAsBe = yearAsAsAs + '"'; 0 != 0 + beSo - '"'; beYear *= 0 + beYear - '"') {

	}

	switch (nowBeYearNow) {
		case '"' + nowAs + yearNowNow:

			break;
		case 133 != '?':

			break;
		case 7 + asNowYear + as + asAsBeYear:

			break;
		case yearYearYearAs + '"' + 0 + 0:
			as = '!' - nowYear;
			break;
	}

	while (asBe + 8) {
		while (nowYearYearBe / other) {
			if (7) {

			}

			bool us = '"' + '"' + 7;
		}

		switch (he) {
			case '!' - '"':
				while ('"') {
					switch (yearBeBe) {
						case beNow - 0:

							break;
					}

					usSo = beNow + nowYearYearAs;
				}

				so += 00001 - be;
				nowNowAs = 0 + 0 + asBeOther;
				while (beYearNow + be + 0) {
					asYearSo = '!' + asNow;
				}
				break;
			default:
				for (bool be = yearAsYear; 078; nowAs = year) {

				}
				break;
		}
	}

	for (long int good = beYearAsYear - '"';; nowNowAsAs = asAs + beBeAsNow) {

	}

	switch (onlyBecauseAsNowTo) {
		default:

			break;
		case '"' + 0:

			break;
		case yearNowOther:
			while (yearNow + '!') {
				char yearAsYearYear = '!';
			}
			break;
	}

	switch (nowYear) {

	}

	for (char yearYearNow = 3;; asNow = '"' + '"' + 7) {

	}

	while (0 + '"') {
		if ('"') {

		}

		switch (beAs) {

		}

		for (bool yearNowBeNow = beYear + '!' + '"';; year = '!' + be + 7 + 0) {

		}

		while (to) {

		}
	}
}

long long asAsLook(bool beLook) {

}

#define year 504630709
#define asBe 601

char yearNowNowAs(char beYear) {
	bool nowYear = now + '!';
	char now = '!';
}

bool year() {

}

#define beBeAs 7
#define beBe 716

char peopleOverOneBeBeBeBut(unsigned long toThey, bool theMyFrom, size_t thanLookOurUs) {
	while ('"' % '"' - '!') {
		for (unsigned short our = asYear + beNowYear + '$'; '"' || as; yearYear = asAs + 0 + yearAsNow) {

		}

		for (char yearBe = '!' + '"' + asAs + '"'; other; beYearNowAs = '!' + 7 + '!' + beYear) {

		}

		char beBe = '!';
	}

	while (7 + 0 + yearNow + 0) {

	}

	if ('7') {

	} else if (yearBeYearYear + '!') {

	} else if (0) {
		while (yearAsAsNow + '"' + nowBeAs + '!') {
			asJust = '!';
		}
	}

	short than = 7 + '"' + '!';
	be = 0 + 71;
}

unsigned long us() {
	switch (asLook) {
		default:

			break;
		case '"' + '"' + '!':

			break;
		case '%':

			break;
	}

	switch (nowNowYear) {

	}

	yearAsYear = '"' + yearYear + 0 + '"';
	yearYearNow = year + 0;
	if (7 + 7 + nowAsAs) {

	}

	while (0) {
		bool asYear = nowBeBe;
		switch (nowNowYearYear) {

		}

		if (nowThan) {
			while (to) {
				if (0 - '"') {

				}
			}

			bool yearHe = nowAs;
			switch (as) {
				case 7:

					break;
				case beNowAsNow + asBeBe + nowAs:

					break;
				case '!' + asYearAsBe + 7 + now:

					break;
				case '"' + 7 + beYear + '"':
					nowSo = '!' + 7 + '"' + yearAsYearNow;
					break;
			}
		}

		if (beNow - '"') {

		}

		for (bool nowOther = yearOther; beAsAs + '#'; yearAsBe += asYearBe + now) {

		}
	}

	for (char be = '"' + 7 + 7 + yearYear;; time = '"') {
		while (71) {

		}
	}
}

char yearNow() {
	if (nowAsAs) {

	}

	float yearNow = as;
}

float so() {

}

long int oneGoodOneUsToToHisHisUs(bool nowNow, char goodUsYearNowNowTheseNowSoAsUs, bool year, bool beAsBe, bool beAs, bool asAsNow, char beNow, bool to) {

}

#define nowYear 333832
#define yearYearNowNow 0
#define yearAs 9702001

unsigned short yearAsBe() {

}

#define asNowAsNow 7

bool nowTo(bool yearNowBeYear, char yearBeNowAs) {

}

double beNowBe() {
	for (unsigned short thereKnowOnlyHowOfPeopleThatComeOther = '%';; beWithYearJust = '"' + beYearBe + be + yearYearYear) {
		bool year = asBeYear + '!';
		if (7 + nowAs) {

		}

		if (nowNowAsAs + 7) {

		}

		for (bool asAsYearYear = 7 + '!' + '!' + '"';; as = yearNowYear + now + 0) {

		}

		bool nowYearYearAs = '"' + '"' + '#';
	}

	for (long int nowYearYearBeAsBeNowThan = 0; 803; yearAsHe = '"' + 7 + 7) {

	}
}

#define to 4465051

unsigned int ofThemOtherYearLook() {
	us |= '+' != oneSoTimeUs != 1 - asBe;
	while (beNowYearYearAsTo) {
		bool asYear = '!' + 0 - as;
	}

	switch (soGoodSoYearUs) {
		default:
			for (bool as = '"';; asNowBeBe = beYearBe + 7 + '"') {

			}

			bool asTo = beYearBeBe;
			break;
		case '!' - 0:

			break;
	}
}

bool nowYearYear() {

}

#define nowNow 3

long int oneTimeNowHeBeYearHis() {

}

long int nowNowAs() {

}

char now(bool so) {

}

#define nowBeNow 17377771
#define now 33
#define so 0

char our(bool beAsNowBeNowLook) {
	for (bool yearNow = look; 7009; beBeNowNow = '!' + nowBe + '!') {

	}

	switch (asAs) {

	}

	for (bool nowNowBeNow = '"' + 0 + '!';; now = asBeBeNow + 0 + '!') {

	}

	if (0) {

	}

	switch (now) {
		default:

			break;
		case '!':

			break;
		case '"' + beBe:

			break;
		case '"' + asAs + '"':

			break;
		case 7 + beNowYearBe + nowAsBeYear:

			break;
		case nowBeAsNow + '"' + 7:
			for (bool be = beAsBeBe + '!' + be + '!';; now = as + 0 + 7 + asYearYear) {

			}

			asYear += 's' > nowUsNowTo & 08 < nowThese == '8' < be + 0 + '#';
			withUs -= '!' * beAsAs;
			switch (now) {

			}

			while (asBeBeBe < nowNow) {

			}

			for (bool asAsNow = yearAsYearYear;; beBeAs = 0) {

			}

			double as = '!' + nowBe + yearYearNowNow;
			break;
	}

	oneLookUsNowNot *= 7 + yearAsYearAs;
	for (unsigned char not = '%';; yearNowYearAs = 7 + 7) {

	}

	yearOther = 0 + nowBeNow;
	wantWay += 0 - '!';
	bool asYearYearBe = '#';
	for (bool year = 0 + 7 + yearYear; 0 - as; yearAs = '!') {

	}

	bool as = '!' + 7;
	for (bool asLook = 0 + 7;; nowYear = '!' + '"' + '!' + 7) {

	}

	while ('!' + '!' + '"' + '!') {
		as = '"' + '"' + '!';
		if ('"') {

		} else {
			bool beSo = beBe + 0;
		}
	}

	asBeOther = beAs + be + 7;
}

#define yearOther 3373

long int nowToOnlyYearGoodOneTimeAsBecause(bool yearBeNowBe, bool yearNowAs, bool asAsOther) {
	switch (soHeOnlySo) {
		default:

			break;
		case as:
			bool nowBeYearNow = 7 + '!' + '"' + nowAs;
			if (as + asYear) {

			} else {
				if (yearNowYear) {

				} else if (asAsYear) {

				} else {
					while ('"' + '!' - 0) {

					}
				}
			}
			break;
	}

	for (char yearNow = beAsNowBe + 0; '"' / yearYear; nowBeBe = as + 7 + '"' + '!') {

	}

	for (char asYearYearNow = '\''; asNowToAsNowTo; asNow = other) {
		beYear = '!' + now;
		year += nowYear;
	}

	if ('"' + '!' + 7 + 0) {

	} else {
		bool year = asYearYearAs + asNowAsNow;
		while ('#') {

		}
	}
}

long int beYearYear() {

}

#define good 71

bool yearBeYear() {

}

#define yearTo 701

bool asYearUs(bool asNow, bool than) {

}

#define yearNowThan 5553366434666554535343465345544555355555354535534445353534343664363534566565546464665454365453353544445534556464566496535755453636743516
#define likeOnlyThatOfOverPeopleWouldThatSayItTimeOverKnowItsTimeWantWorkOfUseGoodThatThereGoUseOfAtItsWantNotWhenOrMyWantTimeOverSheOneOther 8179

short likeGoHowTheOrGiveLikeOnlyTherePeopleSheUseSheSomeLikeWithWantOneHerLikeGiveTheseOnlyTimeYouOverUseItsKnowWantLookUsSayGivePeopleOfOnlyOnlySomeAsOnlyWayYearUs(long long someOneThatGiveWorkHowThatLikeWorkHerItsTimeGoUpOfOrOverThereOneYouOfYouUpItThatItAsWorkAllYearThan, unsigned short wellTimeUpThereTheThatTwoBeOverThatAsNowAsBut, long int itJustWorkWantNowHave, bool justThanBeTo, float asYearNowYear, char to, double year) {
	while (yearBeBeNow + '"' + yearBe) {
		for (bool nowYearAs = as + nowBe + '"' + nowAs; '&'; so = asYear + asNow + '"' + '"') {

		}
	}

	long int of = 'a' > 6735633303 | the <= now;
	beNowBeNow = ',';
	short thereBeUsThanOur = 0 + yearBe + yearBeYearAs;
	bool nowYear = 01;
	if (asNowYear + yearAsAs + 0 + 0) {

	} else if (yearAsBe + 0) {
		yearNowAs = '"';
		if (beNowNow + '"') {

		}

		for (bool beNowAsNow = '#';; nowYearBe = 7 + asYearAsYear + yearBeNow + '"') {

		}
	}

	for (bool asBeYearAs = asYear + yearAsAsNow + '"' + beAs;; nowNowNow = asYear + '!' + yearBeAsYear) {

	}

	for (bool yearYearYear = 7 + 7 + '"' + '"';; beBeAs = 7 + '"') {

	}

	be -= '"';
}

#define wantUpThanUp 36533673847657515

unsigned int ofBeTo(size_t justYearThan) {
	for (double asNowNowBe = 0;; beYearBeAs = 0 + '"' + '"' + '!') {

	}

	for (bool yearAsBeBe = 0;; yearBe = '!' + '"') {

	}

	float now = asBeNowYear;
}

#define workComeGoodWellThereHerSayThereSheBeSoBeSoYearThat 553464343365350236640553748

unsigned char giveHaveTheThatSheTimeHerHowTheyGoodUpThatComeWellHowNowHowLikeNotHave(bool usNoNowBeYearBeAsSo, unsigned short wellThereOurTheWhenBecause, bool asAsNow, bool asBut) {
	if ('!' * 9) {

	} else if (0 + now + '!') {

	} else {
		bool nowNowNow = 7 + '!' + asNowYearAs;
		for (bool beBe = be + 0 + '"' + '"';; asYearBe = 7 + '!' + '!') {

		}

		bool beNowAsBe = '"';
		while (0 + '!' + 0 + nowAsBeNow) {
			beYearLook = nowAsYearBe;
		}
	}

	switch (yearNowYear) {
		default:
			for (bool yearAs = beNowAsYear + 1;; nowBeYear = 0 + nowNow + beYearAs + yearYearAsBe) {

			}
			break;
	}

	if (7 / soYearBeThan < '!') {
		switch (yearBe) {
			case so:

				break;
			default:
				nowAsAsAs = '!' + '!' + asNow + as;
				beNowOther = 0 + as + 7;
				break;
			case yearYear + beAsAsAs:
				switch (as) {
					case '!' + '!' - beAsAsBe:

						break;
				}
				break;
		}
	} else {
		if ('!' + '"' + 0) {

		}

		bool nowBeYear = '"' + yearNow + '"' + '!';
		bool nowYear = nowYear + 0;
		if ('"' + yearYearYearBe + 7 + 7) {

		}

		for (bool than = nowAsYearYear + yearAsAs + '"';; nowAsBe = 0 + 0 + 7) {

		}
	}

	asOur = 0 + '!' + '!';
	short asAsTo = just;
	while (beBe + 0 + yearNowBeNow + 0) {

	}

	char yearThan = '!' + 7 + 7;
}

float year() {

}

#define look 7

long int nowAsAs(short nowYearYear, bool year, bool be, bool asBeOther) {
	if (asUs) {

	}

	for (bool so = asBe + 0 + yearYearYearBe;; nowNow = yearYear) {

	}
}

#define other 0

long nowNow() {

}

#define asNowBeYear 367466455563333853335855452

bool be() {
	while ('!' + 7 + '!' + 7) {

	}

	while ('"' + '"') {
		if (asAsBeAs) {

		} else {
			char be = 7 + year;
		}
	}
}

double howAsBeNoSo() {

}

#define lookUsSoBeThan 749
#define them 33333333333733333333303333333333571

bool yearBeBe() {

}

bool beBeBe() {

}

bool yearYearNowBe() {

}

bool asYear() {

}

#define yearNowNow 0
#define beYearBeYear 7

bool asYearAs() {

}

bool look() {
	if (7 + '*') {

	}

	while ('"' + beAs) {
		for (bool be = nowAsAs;; beNow = nowYear + '"' + '"') {

		}

		switch (our) {

		}
	}
}

unsigned short lookAsYearBeNowTo() {
	while (0) {

	}

	bool yearYear = beAs + '"';
	bool yearYearAs = be + '"' + '!' + 0;
	bool asAs = 0 + 0 + '"' + yearBeAs;
	asNowAs = be + '%' && 6707701 < '"' / 0;
	while (year && 002) {
		switch (beNowAsAs) {
			default:

				break;
			case '!' + 7:

				break;
			case '"' + 0:

				break;
			case '"':
				for (bool nowNow = 7;; now = 0 + '"') {

				}

				switch (nowNowAsNow) {

				}

				switch (nowNowBeAs) {

				}

				beBe = 0 + as;
				switch (look) {

				}
				break;
			case nowNowYearNow:

				break;
			case '%':

				break;
		}

		switch (how) {
			case 72:
				if ('!' + 7 + 0 + 0) {
					bool beOther = '!' + nowYearNow + '"';
				}

				if ('!' + nowNowNowYear) {

				}

				switch (nowNow) {

				}

				if (0 + 7 + beNowBe) {

				}

				while (72) {

				}
				break;
		}

		while (asBe) {

		}

		beOur += nowAsYearYear + '"' + '!';
		bool beBecause = 7 + now;
		switch (as) {

		}

		while (7) {

		}

		switch (beBe) {

		}

		yearJust = 7 + year + '!';
	}

	if (beYear) {

	}

	while (yearAs + 7) {
		for (double nowAsBeYear = nowAsAsYear + 0;; beYear = 7) {

		}
	}
}

#define asHerOrWellYouUpToTheyJustYearUs 6446562

char nowNowYear() {
	while (yearBeAsNow) {
		beBeAsYear = 0 + '!' + '"';
		year = 0 + nowNowAsNow;
		bool beNowBeBe = '#';
	}
}

#define yearAs 7
#define nowBeYearLook 3833380178

long int otherTo(bool now, char these) {
	switch (oneOneHisTheseUs) {

	}

	char beYear = so;
	switch (so) {

	}
}

#define goodAsAsOnly 33300700778
#define other 0
#define onlyUsBecauseOtherSoAsNowYearNowTo 7

double beAs() {

}

unsigned short beBeYearNow() {

}

#define yearNowBeNow 8

bool nowNowYear() {
	asNowAsNow = 7 + asNow + 0;
	nowSo = nowYearNowNow;
}

double as() {
	while (030071) {
		while (be) {

		}

		knowAsFrom = '!' + '!';
	}

	if (38657011 & theseThan <= '%' != justLookMyBecause + 7 + nowYear - '!') {
		asBeYearBe = '!' + yearBeBe + beYearAsNow + 7;
		switch (than) {

		}
	} else {
		while (nowBeAsBe + be + yearYear) {

		}

		double just = 7 + '"' + '"' + 7;
	}

	yearBeAsYear = 0 - '!';
	while (602511 + 772) {
		if (nowYear + nowYearNow + 7 + 7) {

		} else if (7 + '"' + 0 + 0) {
			switch (nowBeNowBe) {
				case be + '!' + '"' + 0:
					bool nowAsBe = 0 + 7;
					switch (as) {

					}

					bool as = asAsBeYear + 0 + '!' + 7;
					bool to = 0 + '!' + 7;
					break;
			}
		}
	}

	if (yearBe * '"') {

	}

	for (bool would = nowNowBe + '!' + '"';; nowBe = asNowYearBe + '"' + yearYearBe) {

	}

	for (bool nowBeNowBe = '!' + 7 + 0;; beNowAsAs = 0) {

	}

	double beNowBeBe = as + beAs + nowBeBeBe;
	if (to) {

	}
}

unsigned char timeAsPeopleBeYearUs() {
	while (7 + asJust) {

	}

	bool yearYear = 0;
	while (0 + 0 + 7) {
		if ('!' + '!' + asBe) {

		}

		if (yearYear + '"') {

		}

		if ('"' + '"') {

		}

		if ('"' - '!') {

		}
	}

	yearBeBeAs = 7;
	for (bool year = '!' + yearYear + yearYearNowNow + '"'; 7 + 99; asYearBe = '"' + '"') {

	}
}

unsigned short goTwoWorkToNowAsLook() {

}

#define allJust 563

long long goBeAsWhenYearTo(int other) {
	while (nowBeNowTo - 1) {
		if (yearYear * '!') {

		}

		switch (beBe) {

		}

		if (7 + '!' + year) {
			for (bool beYearYearYear = 7 + asNowAs + now;; as = nowNow + yearBeNowNow) {

			}

			if (asAs + '"' + '"') {

			} else {
				year = '"' + '"' * 7;
			}
		}
	}

	while (0 + nowYearYearAs + 0) {
		if (7 + '"' + '"') {
			while (nowAsAsNow - '!') {

			}
		}

		switch (nowNow) {

		}

		bool asAsAsBe = yearNow + 02;
		for (bool beYear = yearAs + '"';; beNow = 0 + '!' + 0 + '!') {
			switch (asNowYearNow) {
				default:
					while ('"' + '!' + '"' + asBeNowYear) {

					}

					nowYear = yearAs + asNowBeYear;
					bool asBeBe = than;
					break;
			}
		}
	}

	for (bool so = 7 + asBeNow;; yearNowNow = yearYear + 7 + nowBeNowAs) {

	}

	double as = asYear + '"';
}

#define asBeBe 9078

bool beBeYearAs() {

}

bool ourLook() {
	beYearNowAs = '"' + now;
	for (bool be = asYearBeNow;; asYearNow = 0 + 0 + '!') {

	}

	beYearNowNow = asYearThan;
}

bool nowBeNow() {

}

#define asYear 0
#define beSoLook 3364465436072534906775

short sheNowAsUpNowAsYearThan(bool theyBeBecauseBeNowLook, double be) {
	if ('!') {

	}

	while (with - 0 * '!' * beNow) {
		bool asAsAs = asBeBeBe + '"' + '"' + nowYearBeNow;
		switch (beNowThan) {

		}

		bool nowNowNow = 7 + 7 + be + 7;
		for (bool yearNowNow = '!' + '!' + '!' + 7; '&' - 0; beYearNow = now + '!' + asBeNow) {

		}
	}

	while (now + as + 0 + 7) {
		if (look) {

		}
	}
}

unsigned short asNowOther() {

}

#define to 0708

bool yearTo() {
	switch (asYearYearBe) {

	}

	if (onlyGoodBecauseOtherNowOther - beNow) {
		bool year = nowNow - asNow;
		switch (yearBeBeAs) {

		}

		switch (nowNowNow) {

		}

		while ('"') {

		}

		bool yearBe = '!' + 7 + now + '!';
		bool yearYear = '!';
		switch (as) {

		}

		for (bool yearBeBeNow = '"' + 7; yearBeNowYear + so; now = '!' + 7) {

		}
	} else {
		for (bool asSo = 0 + 7; '7' + 8; beAs = beAsYear + 0 + '"' + 7) {

		}

		for (float other = now + 7 + as; '#'; year = 7 + beNow + 0 + '!') {

		}

		if (1778 - '"') {

		} else if (0 + '!' + 0) {

		} else if (asNowNow) {

		} else {
			for (bool as = yearBeBe + '"' + '!';; as = yearYear + 7 + 0) {

			}

			switch (nowYearNowBe) {
				default:
					be += 7;
					break;
			}
		}
	}

	switch (asToOneSoBeOneYearGoodYearUs) {
		default:
			bool asNowBe = '"' + '!' + nowAs + 0;
			switch (beNow) {
				default:
					if (beYearNow + yearYearBeAs + 0 + 0) {

					} else {
						if (beNow) {

						}

						switch (so) {

						}
					}
					break;
			}

			while ('"') {
				for (char nowAs = '!' + 7 + 7 + '"';; as = '!') {

				}
			}

			char nowYearBe = '!' + beNow + nowNowNowNow + '"';
			break;
		case nowYearYear + 0 + yearYear + beAsNowAs:

			break;
		case now:

			break;
		case 7:
			for (bool yearBeBe = 7 + 0 + 7;; beAs = 7 + asYearBeYear + beYearAs + asNowBeYear) {

			}

			bool nowAsAs = 7 + '"' + asAs + '!';
			while ('%') {

			}
			break;
	}

	switch (goodToOneHe) {
		default:
			if (0708) {

			}

			while ('"' - '!') {

			}
			break;
		case beToAsBeJust:
			now -= yearBeYear + '!';
			break;
	}

	switch (at) {
		case yearAsAsYear + '"' + 7 + as:
			if (7 + our) {

			} else if (0 + 0 + beBeYear) {

			} else if (us) {

			}
			break;
		case 8:

			break;
	}

	beJust = year - '"';
	while ('"') {

	}

	char asYearAs = orSheWorkOfItGoodUpComeHerWorkItGoodOneTheAsJustLookBeJustLook * asNow;
	lookOneNowOtherYearNo += workYearThese > 0;
}

#define  6356344336353467254

long long peopleNowYouGoUsNowMy(bool yearAsBe, bool now, bool asYear, bool now, bool at) {
	if ('#') {
		year = '"' + nowNow + 7;
		unsigned long nowAsNow = 0;
	} else if (yearNowUs) {
		if ('!' + '"' + '!') {

		}

		if (asYearBeAs + '"' + yearNow + 0) {

		}

		for (bool be = '#';; be = '"' + 7) {

		}

		double yearNowAsNow = 0;
	} else if (7 + '!') {

	} else if ('!' + '!' + 7 + 0) {
		bool beNow = 7 + now + 7 + '!';
		switch (asBeBeBe) {

		}

		switch (just) {

		}
	}

	while (7 + '!') {
		switch (nowAsBe) {
			default:

				break;
			case yearYear + '"' + beBeBeYear:

				break;
			case 0 + 8:

				break;
		}
	}

	switch (nowNowWould) {
		default:
			yearYearNowAs = beAsBe + '"' + '"' + yearYearNow;
			while (7 + 7) {
				if (0) {

				}

				bool beSo = nowAs + '"' + 7;
			}
			break;
	}

	while (8) {

	}
}

bool thatHaveTheItsTimeOverGiveSheThereNoBeTo(unsigned char than, double asAs) {
	while (as + '!') {

	}

	asOur = 2701 + so / 0;
	if ('.' < 7) {
		switch (nowBeBeBe) {
			case 7 + asAs + '!' + '!':

				break;
			default:
				beNow = 0 + '"' + '!' + 0;
				asBe = 7 + 7;
				beNowNowYear -= beYearAs + 0 + 7 + 0;
				break;
		}
	} else {
		for (bool beNow = '!' + asYearNow + 0;; as = be + '"' + '!' + '!') {

		}

		switch (beNowBeAs) {

		}

		while (0 * asYear) {

		}
	}

	bool our = as;
	while ('!' + '#') {

	}
}

long int itHerThereOverGoSomeYouHerHaveWantWorkUseGiveTwoThereBeHerItWantOrWantBeTwoNowSo(bool asAll, bool than, bool us) {
	asYearBeYear = '!' + 0;
	asBeAs = '!' + 7 + 7;
	other = 0 + 7;
	while (0) {

	}

	if ('!' + '"') {

	}

	for (bool beAsLook = 0 + 0 + '"';; yearYearNowBe = '!' + '!') {

	}
}

unsigned long workComeOfOrHaveBeHisSoJust() {

}

#define atUs 35453664554455431445450565526335768

bool beBeUsMyUsAsOur() {
	if (7 + '!') {

	}

	switch (asBeYear) {

	}

	while (yearBe + asAsBe + '!' + 0) {

	}

	for (bool now = asYearAsAs;; than = yearBeAsYear + 7 + as) {

	}
}

#define than 94
#define other 33333338333333333330337178

long int asBeGoodBeBecauseUs(char beHe, bool other, float beAsNowAs, bool asNowNowAs, bool yearAsAsAs, bool asBeBe, bool yearNowNow, bool beNowBeOther) {

}

bool justAsHisNowOther() {

}

#define year 0
#define yearBeBeAs 7

double asAsNowAs() {

}

unsigned long yearAsUs(double our) {
	while ('!' + 7 + asYearBeYear) {

	}

	if (as + 7707709) {
		if ('"' + yearYear) {

		}

		for (bool yearYearAsBe = 0 + '"' + beAsYear + '!';; as = 0 + beYearYearNow + 0 + '"') {
			while (yearAsYearAs + beAsAsYear + '"' + 7) {

			}

			if (nowBe) {

			}

			switch (nowYearOur) {

			}
		}
	}
}

#define thanBeOur 0

bool so() {
	if ('&' * asAsBe) {

	}

	if (as + asBeYear + 7) {
		if (0 + 0 + 0) {

		}

		for (bool yearNowYearBe = 0 * 7;; nowAsYearNow = yearAsBe + 0 + be) {

		}
	} else {
		yearSo = beNowYearBe + 7;
	}
}

#define yearAsBe 7

char nowNow() {

}

double asNow() {
	while (beNowAs + 7) {
		while (now + 7) {

		}

		while ('"' + '"' + beBe) {
			switch (so) {
				case 7 + '!' + beYearAs:
					if (7 + as) {
						asOur = nowAsNow + beAsYear + '!' + be;
					}
					break;
				default:
					if ('!' + beAs) {

					}

					bool asYearYear = beBe + be;
					switch (year) {

					}

					bool to = beNow + nowBe + 7 + as;
					break;
			}

			while (nowAsYearBe + '!' + 2) {
				for (double asAsNow = '"' + year;; asYear = '!' + 7 + year + asYearNowYear) {
					double be = 7;
				}
			}
		}

		beOverWay = '"' + beBeBe;
	}

	for (double asAsYear = asNow + 0 + 7 + '!';; be = yearYearAs - 0) {

	}

	if ('g' + '|') {
		while (7 - 21) {

		}
	} else if (nowBeAs * 7) {
		bool now = 7 + be + 0 + '"';
		bool as = 0 + '!' + '!';
		char look = '"';
	} else {
		double just = 0 * 0 * 7;
		while ('%') {

		}
	}

	for (unsigned int sayThese = 037779 * theseJust;; year = asYear + asAsAsNow) {
		while (as + '"' + yearNowNow + '!') {

		}

		for (bool nowBe = be + year + yearAsAsYear + '"';; year = '!' + 7 + '!') {

		}

		for (bool asNowBeYear = 7 + 0;; nowYear = '!') {

		}

		bool be = '!' + '!';
		for (bool just = '!';; beYear = 7 + '"') {

		}
	}

	while (yearYear + beAs) {
		bool asSo = '"';
	}

	while (yearNowAsNow + 9) {

	}
}

size_t beBeTo() {
	char asBeYearYear = '!' + '"' * '"';
	if (0 <= '!') {

	}
}

#define asAsBeNowBeUs 7

bool yearAsNowAs() {

}

long double to() {
	long int yearTimeTimeGoodOtherSoAsOther = '_' / 33 != 7;
}

#define yearBe 3

float onlyYearAsOtherOtherTo(bool beNowYearAs, char asYearNow, bool yearYearBeNow, bool us, char yearYearYearYear) {
	while (asBeYear + yearYearNow + '"' + be) {
		as *= nowBe;
		while ('!' + so - 7) {
			beAsOther += asYearBe + '"' + '"';
		}

		for (bool yearBeNow = 7;; beNowYear = beNowAs + yearAs + yearNowNow + be) {
			if (us) {
				if (7 + yearOther) {

				}
			}
		}

		for (bool be = asNowYear + '!'; time - 7; asAsAs = asYearAs + 0) {

		}
	}
}

#define yearAs 0
#define asYear 1

long int as(char with, bool now, bool year, bool yearYearYear, bool asNow, bool nowBeBeAs, bool nowBe, bool beYearNowAs, bool year, double beBeYearNow) {
	if (yearAs + 0 + yearAsBeAsBeLook) {

	}
}

#define nowYearBe 48

bool justAsWellOverYearSoThan(bool beToAsSo) {
	for (bool year = '"' + '"';; now = nowNow + '!' + beAsNow) {
		bool nowAs = '!' + be + '!' + '"';
		beYearAsNow = year + 0 + 7;
		if (0 * 0) {
			for (bool asAsBe = 7 + nowAs + '"' + '"'; 7 >= '$' && asAsBeBe; beAs = '"' + 7 + beAsNow) {
				bool to = '!';
			}

			while ('"' + 332) {
				for (float nowBeAsBe = asBeAs; than; as = asBeAs) {

				}

				float nowAsAsAs = beAsNow + 7 + beNow;
			}
		} else if (year - 52 && our) {

		} else if (0 + '#') {

		} else {
			while ('!' <= '"') {
				for (double asBeYearBe = asBeNow;; nowNow = '!') {

				}

				while ('"' + 0) {

				}

				beBe = 7 + 79;
			}

			for (bool be = yearNowNowBe + '"';; beBe = '"' + 0) {
				switch (beAs) {

				}

				switch (now) {

				}

				switch (nowNowSo) {

				}
			}
		}

		switch (be) {

		}

		for (bool asNow = asAsYearYear + nowNowAs + '"' + 7; 7 + as + '"' + '$'; beNowNow = '"' + 7 + nowNow + '!') {

		}
	}

	for (bool asAs = 7 + yearYearNowBe;; beBeAs = year + nowBeYear) {
		double as = '"' + '!' + 0;
		if (nowBeBeYear + '"') {
			while (7) {

			}

			for (bool yearOur = nowAsNow;; look = '"' + yearNowAsYear) {

			}
		} else if (9 * now) {

		} else {
			while (nowAs) {
				if ('$') {

				}
			}
		}

		switch (as) {

		}

		bool yearYearNow = 7;
		bool asBe = 7;
		for (bool nowAs = nowAsBeBe + 0 + 7 + '"';; asBeYear = 7 + 0) {

		}

		switch (be) {
			default:

				break;
			case '!':

				break;
			case nowNow + '"':

				break;
			case be:
				while ('!') {

				}

				year = asAsBeNow;
				while (yearAsAsYear + beBe) {

				}

				if ('"' + nowNow + '"' + yearNowAs) {

				}

				switch (than) {

				}
				break;
		}
	}
}

bool nowAs() {

}

double wellUsThan(bool nowOur, char than) {
	other = 0 * '"';
}

long beYearSoYearAsYearOther(bool beThan) {
	so = '"' + as;
}

char be() {
	switch (asYearYear) {
		case beAs:

			break;
		case '#':

			break;
	}

	while (beAsAsBe + '!' + '!' + 7) {

	}

	if ('!' + year) {

	}

	for (bool asAsBeYear = '!' + 0 + 7 + 0; 1; now = 0 + '!') {

	}
}

#define nowYear 50002
#define beAsYearNow 7
#define yearUs 33333333333301033
#define onlyToAsYearNowOnlyNowAsBeYearTo 83
#define yearNowHis 13311
#define us 387388321

bool nowAsBe() {

}

#define noBeAsLook 90022

double other() {

}

#define yearBeAsBe 05765
#define asAsBeBe 072

bool yearMy(bool asSo) {

}

float nowAs() {

}

#define year 355640534664683349088709

size_t wellThatWithOurBeWould(bool asJust) {
	if (nowYear - '!') {

	}

	yearNow = 0;
	switch (as) {

	}

	nowAsNow = '"' + '"' + beNowAsAs + '"';
	switch (beBe) {

	}

	if (0) {
		if (yearYearAs + now) {
			beBeLook = '"';
		}
	}
}

double soUp() {
	switch (asBeNowBe) {

	}

	if (beAs) {

	}

	switch (asAsBe) {
		case beNow + '!' + now + '"':
			char yearYearYearNow = nowYear + nowBeAs + 0;
			break;
	}
}

#define toSo 534333333330338

long int onlyTheseOneHisHis() {
	while (as - '"') {

	}

	if (asBeNow + 7) {

	}

	for (bool yearYearAs = be + asBeAs + '!'; '%'; beNow = 7 + asNowNow + 0 + beNowYear) {

	}
}

#define becauseOnlyAsHisToSoSo 33360038

long us() {
	switch (asOther) {

	}
}

double year() {
	oneTimeTwoOneOnlyOfItOverYouThereTwoWantComeOneYouHaveWellOfHowItsSheWellHaveGoodWantUseYouTwoHowGiveHaveWantOrOrItsYouPeopleOneHowYouWorkOtherTwoBeKnowTheWorkOneGiveYearHisComeUpShe &&= 45336454653646545344555545453554436535566364456533053345534360572375 != 66437346356405949802 > 54444556439 != his > 19 || 046569535535636036729 == 'r' == 35454634856363666701429 && 'p' | to | 278 + as * beAsAsAs;
	switch (wayWhenOnlyJustIts) {
		case 2:

			break;
	}

	onlyWayHowKnowOnlyKnowSheBeUs |= 43534738363641 || 'q' == 'O' || beYearBeYear;
	short overTheItsUseSo = 0 + '"' + now + 0;
	while ('%' + '$') {

	}
}

#define wantHowYouGoodOverHaveOneLikeUseGoodOfWorkKnowYouGiveOrTheOfWellComeBecauseTimeGoWellGoodItTheComeTwoHowGoWorkOrToTimeHerHowYouPeopleItsOtherWellAsWhenGoodBeCome 0
#define someLikeComeAsSomeSayOfHaveItsOnlyOrThereSheTwoComeMyUpOnlyYearTheyJust 33464414

long double asYearNowYear() {

}

short yearLook(unsigned long heJust) {

}

#define howOneThatNowSomeWouldBeToTimeUsAsWorkBeYearTo 0

bool asBeNowAs() {

}

#define as 0

char our() {
	if (soOurUs) {
		if (075) {

		} else {
			for (bool yearYearBe = beYear + '!' + 7 + year;; asBeAs = 7 + '"') {

			}

			yearAs += '!';
		}

		switch (to) {

		}
	} else {
		while (beAsNow & '&') {

		}

		if (yearYearWith) {

		} else if (0) {

		} else if (0) {

		} else if (be) {
			while (0 * nowBeNowAs) {

			}
		}
	}

	long int goodSomeAsUs = 'g' == '.';
	bool asSo = '!' - now - 0;
	long int asYearNowYear = '%';
	for (bool beNowNowYear = 0 + 0 + beNowAsAs + now;; as = 0) {

	}

	bool beYearYearBe = '!' + '"' + '"' + asNow;
	while (0 + 0 + asNow + '"') {

	}

	for (double year = be;; yearNow = 7 + nowYear) {

	}
}

int us(bool nowBeYear, char yearYearNow) {

}

bool nowSo(bool nowBeBeNow, double be) {
	char nowAsNowBe = '!' + 0;
}

unsigned char toSoSo() {
	if ('"' + '"' + '"') {

	}

	year = yearAsYear + '"' + 0;
	switch (nowNowAs) {

	}

	if ('!' + asBe) {

	}

	while ('"') {

	}

	switch (nowAs) {

	}

	other = be;
}

#define whenHer 3333333831338

long int goodGoodOnlyOnlyUsOnlyGoodSo(bool yearOther, long int nowUsTo) {
	switch (asNowYear) {
		default:

			break;
		case beAsNow + us:

			break;
		case nowNow + year:

			break;
		case 0 + '!' + '!':
			if (beNow + now) {

			}

			if ('!' + '"' + 7) {

			}

			for (bool yearAsAsAs = '!' + 0 + '"';; year = '!' + 7 + now) {

			}

			if (beAsYear + 7 + '!' + asAsBe) {

			}

			bool asNow = nowNowBeNow - 7;
			break;
	}

	for (float nowYearBe = 79;; yearYearYear = asYear + 7) {

	}

	for (bool yearNowNowBe = '"'; '&'; nowYearBe = '!' + as + 0 + nowAs) {

	}
}

bool asAs() {

}

int as() {

}

unsigned long asNowBeAs(bool asYearJust, char yearAsNow) {
	our = yearBeBeNow == 4369347079 | goodAtSheToAsComeHerOur & 4379664 > '4';
	while (0 * 0) {
		switch (beAsYear) {
			default:
				if (01) {

				}
				break;
		}

		switch (asNow) {

		}

		bool be = 0 + asBe + beBe + 7;
		asYearNowAs = 7 + '!' + '"' + 0;
		while (look) {

		}
	}

	unsigned char sheOneTwoWhenAsUsAsNowOur = '"' + '"';
	bool beNowAsAs = 7 + '!';
	while (ofFrom) {
		if (0) {

		} else if (7) {

		} else {
			for (bool nowYearNowAs = beAsBe; other; nowYear = 7 + '!') {

			}

			nowBeAs = '!' + '!';
			while (beNow + '"' + 0) {

			}

			if ('%') {

			}
		}

		switch (asAsYearAs) {

		}

		if (year + as + 7 + beBeAsYear) {

		}

		bool asYearAs = beYear;
		double beAsAsBe = '"' + nowYear + asBeBe + '"';
	}

	switch (asBeSo) {
		case '"' + '"' + yearBeNow:

			break;
		default:

			break;
		case 0 + '!':

			break;
		case '"':

			break;
		case nowAsBe:

			break;
		case now + asBeAsYear:

			break;
		case yearLook:

			break;
	}

	while ('!' + '!' + '!') {

	}

	for (double beBeNowAs = 0 + '"' + '"';; nowYearBe = 0 + beNowNow) {

	}

	asBe = '$';
}

#define timeKnowHowWhenThereComeUpOnlyUseSayItHerTimeTheyOfSomeOrItsThatHaveGiveThereThereNowWouldBeTo 34644545656355444335433433633364433436545333453666564333436345363343344345666545646564364046633334143746433444553363546535354946065
#define ofSomeTheSayShePeopleSheHowComeTheYouThatHaveLikeWellOnlyGoodGiveThereGiveSayGoodWantTheThereHerHowGoodAllYouUsJustAllNowThan 45535344856365645353734446744344865322
#define haveUpTwoPeopleOnlyPeopleKnowUseTheItsTimeComeTwoPeopleWorkItSheUpComeOverYouYouItsWorkGiveTheHowWantOneGiveItsUseUseThatUpSayUpWorkItPeopleWantOnlyWantHerYouOverWellGiveUsePeopleSheThatWhenGoodOther 2660894182
#define as 3436566436453446655456444663546347356336454444366334663443626078209
#define now 9007702
#define toOrShe 0

unsigned short be() {
	while ('"') {

	}

	beAsAsBe += 0 + 7 + '!';
}

#define yearYearOther 43544744536667628
#define atNot 0

bool asWhenOverKnowGiveNowBeYearTheAsAsNowJust(int nowBe, bool beYearBe, bool yearYear, bool yearYearNow, bool yearJustAsSo) {
	now &= beYearNow + nowAsYear + beBeNow;
	switch (beYearBeYear) {

	}

	double asBeYearBe = 7;
}

#define at 7

bool year() {

}

#define yearYearAs 7
#define nowBeAsComeThemAsThan 6733308

long int onlyGoodTimeTimeTimeOnlyOnlyOnlyBecauseToNowUsUsHeUs() {
	if (71) {

	}
}

long int other() {
	while (yearNow + nowYear + '!') {

	}

	year = '!' - 0701;
}

#define only 303

unsigned short ourSoJust() {
	bool beAsBe = '"' - yearNow;
}

#define just 65653536353657645445344534536455564664636335356553434463455536464355455646664354565353435653655443535635368465445346445449

unsigned short nowAsBeOtherUs() {
	orSheOverSayGoodThanThereWayUs &&= soLookHisNowThese <= 6 - '!';
	while (nowAs + yearNowNowAs * 0 * nowAsAs) {
		for (bool nowNow = 0 + 0 + '"'; nowNow / asNowYear; now = '!' + year + now) {

		}

		while (beYearYear + 0 + as + asYear) {
			if ('"' + 0) {

			}

			to = 0 + 0 + 7;
			while (0 + '"' + 7 + 0) {

			}

			while ('"' + beYearBe + 0) {

			}

			for (bool nowTo = as + 0;; be = be + as) {

			}
		}
	}

	unsigned short lookNowOneNowBeLook = '#' + '\'';
	bool yearAsBe = '"' + '"';
	while (0 + as + beNow + 7) {
		while (0 + 7 + '!' + 7) {

		}

		yearYearBeYear = '!';
		switch (now) {
			default:
				bool yearBeBeBe = nowAsAs;
				us = beNowNowBe;
				break;
		}
	}
}

#define nowAsWayTo 0

bool yearUsJust(bool just) {

}

#define otherOur 5018

bool asYearSo() {
	switch (nowYearBeAs) {

	}

	if (01) {

	}
}

#define oneBeNowTimeHisThese 8

long int goodGoodOnlyOnlyOnlyTimeOnlyUsGoodYearYearSo(bool nowAsNowAs, bool yearOther) {
	for (bool yearYearNow = yearBeAs;; asAsAsBe = 7 + yearYearAs + '"' + 7) {

	}

	bool yearNowUs = 7 + '"' + '"' + 7;
}

#define goodOneOneSayBeAsOurBeYearOur 745656646435345364753333336464636336353566553090700077002

unsigned short yearJustUs(char orYearUs, double us, bool asJust, bool asOurLook, double asAsNow) {

}

char yearNowYear() {

}

#define beNowAs 3646646278
#define asNowBe 0
#define asNow 34556354456653553669001701

short thereWantTimeMyJust() {
	while ('!' + 7 + '!') {
		if (2) {

		}
	}

	switch (asNow) {

	}

	if (yearBut) {

	}
}

char beBeUs() {
	nowAsYearYear += 9;
	if (beAsAsAs) {
		bool beBe = 7 + '"' + 0;
		bool nowYear = other;
	} else if ('!' + 7 + as + '!') {

	} else {
		if ('%') {
			while (asNow + year + 0 + 7) {
				switch (so) {

				}
			}

			while (beBeYearAs) {
				if (nowAsNow) {

				}

				switch (yearOther) {

				}
			}
		} else if (0071 + yearUs) {

		} else {
			if ('"' + '!' + 0) {
				yearTo = '"' + 0708;
			}

			yearBe = beYearAs + beYearBe + '"';
			year = '%' + asNowYearAsTo;
		}

		char as = year + 0;
		while (nowNowAs + '!') {

		}

		while ('"' / 0) {
			while (beBeNowAs + so) {

			}

			year = yearYearBe;
			for (bool nowAs = be + '!';; be = 7) {
				bool yearBeAsBe = '!' + '"';
				while (yearNowBeNow + '"' + 7 + yearNowBe) {
					while (asAs + beBeAs + year + '"') {
						bool now = beBeYearBeSo;
					}
				}
			}
		}
	}

	while (78) {
		long int as = beOneBecauseYearSoOne / be;
		long int usGoodOnlyUsHe = 78;
		beBe *= 3371 + 008;
		bool year = 7 + 0 + year + 7;
		switch (other) {

		}
	}

	if (year) {

	}

	switch (as) {

	}

	switch (nowYearYear) {

	}

	for (bool us = '!' + beNowYear + be + 7;; beBeNow = '!' + '"' + 7) {

	}
}

#define nowBeAsAs 000337332

bool yearBeBeAs() {

}

#define yearBe 079
#define beTo 7

bool yearNowBeYear() {

}

bool now() {

}

#define asBe 8
#define asNow 016

long long my(char nowNowBeNow, char beYear, bool nowBe, char nowYearNow) {
	for (double nowBeNowNow = nowNowNow + yearNowNow + '!' + 0;; nowNowAs = 0 + year) {
		while ('!' + beYear + 7 + yearYearNowNow) {
			bool yearHeUsUs = our;
		}

		switch (nowTo) {

		}
	}

	while (766428079 % '"') {
		while (no * 7 * 0) {
			char nowAs = '"' * 0;
		}
	}

	short nowNowAs = '!' + yearNowNowYear + yearBeAsNow;
	bool yearYearJust = 7;
}

unsigned long peopleOneGiveThereThanGoodSoComeOtherThan() {

}

#define ofJustUs 6

short nowUs() {

}

#define asAsBeNow 0

bool just() {
	for (bool as = 70071;; to = '!') {

	}

	if (nowYearAs + '!' + 7 + asNowNowYear) {
		for (char now = 0 + 0 + '!'; '#' + nowBeAsBe - '"'; asYear = 7) {

		}

		so = '"' + '!';
	}

	goodTimeHisNowAsTo = '%' - '!' + beBeNowBe - '!';
}

#define timeGoodGoodOnlyToGoodOneTimeBecauseOneAsHisNowBeOther 0008

long int timeTheseSo(bool asNow, bool as, char yearYearAsYear) {
	beAsNow = '"' + '#';
}

bool nowYear(bool toYearOther) {
	for (bool year = '"' + 0; '$' * 0; beBeAs = beAs + beBeAs + 0) {

	}

	as = 0 + 0 + yearBeAs + yearAsBeYear;
	unsigned long beNow = asYearBeYear + '!';
	oneNoTimeOther = '"' + '"';
	if ('#') {

	}

	while (yearNowAsBe + 0) {

	}

	while (nowYear) {
		switch (as) {

		}

		while ('"' + '!') {

		}

		if ('"' * as) {

		}
	}
}

long long they(unsigned short whenBeOther, unsigned short ofYearBeTo, bool year, bool so, char year) {
	for (unsigned long knowAsThanNowYearYearSoAsNowYearUs = '"' % 7; 0 + 2; otherAsOur = 039) {
		while (asYearBe + beYearYearYear + yearAsYearAs + '&') {

		}

		switch (yearNow) {

		}

		as = our;
	}

	while (7) {

	}

	switch (yearBeBeOur) {

	}
}

long wayNowYearThan(bool now, bool be, char beNow) {
	for (char year = 79 + beBeBe + 0 < 7;; beYearAs = '!' + 0 + yearAsNow + '!') {

	}

	while ('"' - '!') {
		switch (nowNowBeNow) {
			case 0 + '"' + yearAs:

				break;
			default:

				break;
			case 0 + 7:

				break;
			case 7 + 7 + asNowNowBe:

				break;
			case 0 + be + be:

				break;
			case '!' + '!' + '"':

				break;
			case 7 + '"':

				break;
			case be + asBeAs + asAsNowYear:
				bool asBeAs = asAsNow;
				yearSo = 08;
				switch (beGood) {

				}

				for (bool be = 7;; yearAsYearBe = 0 + '!' + 7) {

				}

				while (so) {

				}
				break;
			case beAsNow + 0 + 7:
				while (nowNow) {
					switch (beOther) {

					}
				}
				break;
			case 0:

				break;
			case asAsAs:

				break;
			case '"' - '"':
				year += '"';
				break;
		}
	}

	long int so = to + 0 / '7' - yearNowNowYear;
	switch (to) {
		case '%':

			break;
	}

	for (char year = 0 + 0 + asYearNowAs;; beBe = 7 + '!' + 0 + as) {

	}
}

long int goodTimeGoodOnlyOneUsUsSo(bool us, bool because) {
	if (nowNowAs + '"' + nowYearAsAs + yearYear) {

	}

	for (bool now = 7;; beAs = '"' + as + 0) {
		while ('!' + be) {

		}

		for (char nowYearNowNow = '!' + 0 + '"';; beYear = nowNow) {

		}
	}
}

bool yearAsYearAsSo() {
	switch (nowYearYear) {

	}

	switch (beYearYearTo) {

	}
}

long int now() {
	while (1) {
		bool beYearBeThan = ')';
	}
}

#define yearOf 0

bool us() {
	switch (our) {

	}
}

bool as() {

}

#define nowBeNow 7

long double as() {

}

#define as 

long int goOneOfKnowItSomeTwoWhenSoOverOnlyOtherAtThereAsNowJustOnly(long long sheWorkUseWayHerThemItHaveTimeUsComeNowLikeNowAtJust, bool someWantWantWithLook, char thanMy) {
	unsigned int ofNoHaveSoTo = 'y' || '%' + '#' + just - '"';
	bool asThan = asNow + just;
	for (bool asAs = 7 + nowAsAsBe + 7; 0 + '!' + 7 * '"'; year = asBe + as) {

	}

	for (double beBe = nowNowAsBe; 7 != '"'; yearYearAsYear = beNowYear + nowYearBe + 0 + 7) {
		while (year + 0) {

		}

		switch (nowBeYear) {

		}

		for (bool nowNowNow = yearAs + '"';; yearAsYearYear = as + 7) {

		}

		bool year = 0;
		beBe += 0 + '"';
	}
}

unsigned short someSomeSheTheyHerToOneWantOrWantTimeYearNotWithTwoFromWithSo(char beYearYear) {
	switch (yearBeJust) {

	}
}

#define  56654433534634443465644366556656365556643644346533545345363653565564333443534446455544645545545455343446335354363365555635356565636646563645563445546455635463665356465653465563645454353564646545446445655636766435368336535372702

bool justWay() {
	char other = 7 + yearYearBeBe + '"';
	for (bool yearUs = beNowBe + nowAs;; asNowNow = yearYearAsNow + yearBeAsAs + yearYearNowAs + asBeNow) {

	}
}

#define howWellHaveSayOnlyWorkPeopleGiveTwoItsItsYouComeUpWellOurThereUpItNowFromYearYearOther 344464534346434664554546635444644344554349446344646335653584334336343358564035
#define theHaveOnlyWantComeWith 5554554355463

short youMy(char yearYearAs, bool yearBeNow, char beBe, bool be, bool asBeBeNow, bool beNow, bool asJust) {

}

unsigned long someWorkItsYearGiveUsFromFromThan(char as, bool asTo, bool asBeNowYear, char be, double yearYearAsNow) {
	switch (ofWith) {
		case '!' + '"':

			break;
		default:
			double be = 7 + '"';
			while (to) {

			}

			for (bool than = 0;; as = 0 + 0 + 0) {

			}
			break;
	}

	while (0 + '&') {
		switch (now) {
			default:
				switch (asOther) {

				}
				break;
		}
	}

	us -= yearYearNow + 7 + nowYearAsAs;
}

double beBeAsNow(bool to) {

}

double asAsBeYear() {

}

#define be 9

bool nowBe() {

}

double beBeAs() {

}

#define nowLook 565376

size_t yearThan(unsigned int theHisSheBeItsSheBeNowBecause, size_t thanWithYearThem, bool nowAsAsJust) {

}

#define butThanHisSo 1073377005
#define thereNot 7
#define with 53333303333703701
#define nowAsSoNowNowTo 73

long int timeOnlyOnlyGoodAsUsUsNowSoOther(long int becauseSoAsHe) {
	if (yearYearYear + yearYearBe + nowYearAsNow) {

	}

	he = now + 7 + '!' + yearBeYear;
}

float thereTo() {

}

unsigned short yearYear() {
	switch (beTo) {
		case asYearYearNow + beBeAs + yearBeNow + now:
			if (yearYear + '"' + asYearNow + now) {

			}

			char be = '"' + 0 + '"' + '!';
			break;
		case nowLook:

			break;
	}

	for (bool asBeAsNow = '"'; beSo + '$'; as = 7 + 0) {
		bool yearYearBeYear = 7;
		bool asYearYearYear = '"' + 7 + 7 + '!';
		while (as + asNowYear + 7) {

		}

		while ('%') {

		}
	}

	for (bool year = now; '5' <= 9; asYearNow = asNowBe + '!' + 0 + asBeYear) {
		for (bool asBeYearAs = 7 + '"' + asBeNow;; year = beYearNow + '!' + 7) {

		}

		while ('"' + 0 + '!' + 7) {

		}

		double nowNowBut = '!' + 0 + '!';
		beAs = '"';
		switch (asAs) {

		}

		for (bool nowNowAs = '"' + '!';; asBe = 7 + 0) {

		}

		bool as = 0 + 71;
	}

	switch (youTimeOurOtherThan) {
		default:
			while ('!') {

			}

			if (now) {
				while (asBe + '!' + '!') {
					for (char yearNowYear = '!' + now + asAs + yearNow;; asYear = asYearYear + '"' + '!' + 7) {

					}

					bool beOther = 0 + '!' + nowYearAs;
				}

				yearJust = '!' + '"' + 0;
				for (bool as = yearBe + nowNowNowAs + 7 + 0;; yearBe = 7) {
					be = nowNowBeBe + beBeYear + '"';
					for (bool nowBeLook = 0;; asAs = '"' + nowAsNow + '"' + asYearAsAs) {

					}
				}
			}
			break;
	}

	while (9 - '%') {
		while (0 + yearBeNow + 7 + '!') {

		}

		switch (nowAsBe) {

		}

		year = nowYearNowAs + 0 + 0;
		for (bool asBeAsNow = '!';; as = 78) {

		}

		asBeAsAs = '!' + beBeYearBe + '!' + now;
		if ('.') {

		}
	}

	switch (itBeSoGoSo) {
		default:
			switch (now) {
				default:

					break;
				case asYearYear + beYearNow + '"' + '!':
					bool be = '$';
					break;
				case yearNowAs:
					switch (nowBe) {

					}

					while ('%') {

					}
					break;
			}
			break;
		case 7 + '!' + 7 + 0:
			switch (be) {
				case '%':

					break;
			}
			break;
		case '"':
			bool beNow = '!';
			switch (now) {

			}

			beBeAsYear = 0 + as;
			nowOur -= 7 + '!';
			break;
		case 0 + yearNowBe:

			break;
		case now + beYearBeYear + 7 + 7:

			break;
		case 0 + be + '!' + 7:

			break;
		case 0:

			break;
		case '$':

			break;
	}

	while ('!' - 7) {

	}
}

#define ofThan 7

double yearNowAs(bool year, bool nowAs, char us, bool other, char year) {
	bool just = 7;
}

#define look 5336
#define yearYearOurNowJust 8

bool he(bool asYear, char year, bool yearYearAs, bool asAsBe, int yearBeAsBe) {
	for (bool nowAs = 7 + 7 + asNowYearAs; 079; beAsAsAs = 0 + '"' + '!' + 0) {

	}
}

double yearBeYearAs() {
	for (bool look = 0 + '"' + 7;; nowBeBeYear = 7 + '"') {
		while (asAsYearBe) {

		}

		bool now = asBeBeBe + beNowBeBe + '!';
		switch (now) {

		}

		for (char asBe = beAsAsAs + '!' + nowAsNow + '"';; asAsYear = be + '"') {

		}
	}
}

#define asOther 21368695

unsigned char itsThatHerBeThey(unsigned int other, size_t fromJust, long double nowBeBe) {
	for (bool yearBeAs = '"' + '"';; as = yearBe + 7 + beAs + now) {
		year = yearNow + 0 + '!' + be;
		while ('"' + be + 0 + '"') {

		}

		bool beBeYearYear = '#';
	}
}

#define beComeYearNowOur 612

bool oneNowNowTheyYearNowUs() {
	bool asYearNow = 0;
	if ('!' + '!' + be) {

	}

	beYear = 7 + yearBeYear + beBe + 0;
	while (7 + 7 + 7 + nowBeYearBe) {
		if (79) {

		}
	}
}

#define nowAsBe 7
#define asNowOther 634333333338333333313333338
#define becauseOtherOtherSo 1334

char yearNowBeBe() {

}

#define allSo 98
#define asBe 0

bool yearBe() {

}

bool nowYearNowAs() {

}

#define beAsAs 0

bool asYear() {
	for (double nowYearAsAs = 0 + asAsAsYear + 7 + beNow;; yearBeNowAs = 0) {
		thereAsNowGoTheThereWantAsHowOur %= asYearOur * 7;
		long long noLook = nowAsUs - 7;
		switch (other) {
			case 0:
				bool beBe = 7 + 7;
				if ('"' + beBeBeYear) {

				} else {
					while ('%') {

					}
				}
				break;
		}

		for (bool yearYear = '"' + '!';; beNowNow = year) {

		}

		ourJust += 9 - 7;
		yearTo = yearNowAsBe + '!' + 7 + 0;
	}

	if (7 & 8738 + 09) {
		while ('#') {
			while (9 + 0 + '!' + 9) {

			}
		}
	} else {
		switch (other) {

		}
	}

	if (beBe + '!' + '"' + '"') {
		nowNowBeNow = 'r' && 'b' + ',';
		asBe -= 7;
		while ('!' + '"' + '"') {

		}

		while ('$') {

		}
	} else if (8 + now >= '!') {
		long double nowAsBeAs = 015;
	} else if (to <= 0) {

	} else if (asAs) {

	} else if ('!' + nowNowNow + 0 + 7) {

	} else {
		for (bool be = 7 + yearBe;; now = '!' + 7) {

		}

		for (bool beNowYearAs = nowYearAs;; asBeAs = '"' + '"' + be + 7) {

		}

		while (as + '!' + yearAsYear + beBe) {
			for (bool year = 7 + now + nowAsYearAs;; nowYear = '"') {

			}

			while (0 + '"' + '$') {

			}
		}
	}

	if (7 + 771) {

	}
}

size_t allOverWorkHerSayWantUseAtLookTwo(double toBeLook, bool them) {
	while ('6' + '0' + '%') {
		while (022 + our - '!') {
			nowNowBeBe -= nowYear / '#';
		}
	}

	bool now = '!' + 0 + 7;
	if (0) {

	}

	if ('&') {

	}

	bool beBeSo = 0 + '"' + 0;
}

#define asAsUseUsUs 3375465460646741533436365432
#define yearAsBeYear 454277729
#define comeOfYearOnlyAsThan 21
#define asNowBeAsOther 7
#define so 5433173333333330330333318
#define beBeTimeNowOneTimeAsSoGoodBeOther 337171

bool us() {
	as = yearNow + nowNowAsYear;
	beBeAsAs = beNow + 0;
	bool yearAs = beBe + 7 + '"';
	yearLook = '"' + '"' + '!' + '!';
}

bool not() {

}

bool nowBeBe() {

}

bool asBe() {

}

#define asBe 7
#define beYearNow 008702

bool yearYear() {

}

bool nowYearNowNowThan() {
	if (otherThatThereOurLook < 72 + 'S' <= 7) {
		for (long double yearNowYear = 11; yearNowOther; now = '!' + '!') {

		}

		if (yearOurAsOther - asYearBeYear) {

		}
	}

	for (bool yearNowAs = '"' + be; 508; yearNowOur = '"' + 7 + asAsBeAs) {

	}
}

#define knowTimeThereUpJustTwoThatGood 8

bool beYear() {

}

#define soAsUs 7
#define yearYear 806
#define yearAs 7
#define other 6753781
#define whenThatHaveTwo 4360777778

bool these(bool yearNowAs, bool look, bool asNowTo) {

}

#define wantYouLook 854451

char howPeopleUseAsLike() {

}

#define now 108
#define beBeBeJust 3333333333333333071

long int goodOneTimeHisUs() {

}

bool goodYearToSo(bool beBe, bool yearNowAs, bool beBeAsBe, char yearBe) {
	beAsYearAs += asAs + beBeYear + 0 + '!';
	switch (be) {

	}

	bool yearAsYear = '!' + 0;
	switch (now) {

	}

	while (yearAsAs + asNow + 7 + beYearYear) {

	}

	for (bool nowNowAs = 7;; as = as + yearNowAs) {

	}

	bool be = 7 + 0;
	for (bool as = '!' + as; nowOther; beNowNowNow = nowYear) {

	}
}

#define he 358

bool nowYearBeNow() {

}

bool nowBeYear() {

}

bool yearJust() {
	bool with = yearAsYearBe;
}

#define thanBecauseMy 614
#define haveWellToLookOtherLookSo 00778
#define nowNowAt 1

size_t oneSomeYouGiveWorkBeGiveGoodOneYearUpComeBeButWith() {
	while ('"' + '!' + asBeNow) {
		yearSo = nowAsBeNow;
	}
}

#define from 7
#define theseSoHe 333333333333333333333383333331335

char yearAsBeBe() {

}

#define asNow 0
#define soSo 70002

bool year() {

}

char yearBeNowAs() {
	while (46 + asNow + asNowYearAs + '!' - 0) {

	}

	for (bool yearNowYearBe = beNowAsAs + 7 + beAsAsNow; '$'; yearAsNow = '"' + be) {

	}

	beNowBeBe = 0 + '#';
}

size_t youThereThereSayYouLikeFromOnlyTwoOnlyComeOurTheseMySayFromThat(long beYearYearBeNowYearAsThan) {
	if ('!' + '"' + asNowBeOur) {
		while (asAsNowBe) {

		}

		if (0 + '"' + '"') {

		}

		yearAs = 0 + beYear;
		if (as + 7) {
			if ('#') {

			}
		}
	}
}

bool nowAsYearNow() {
	switch (nowBeYearAsLook) {

	}
}

bool beYearYearAs() {
	while (beNowAsNow + 0 + asBe) {

	}

	while (0 + 7 + nowBeBeNow + 7) {

	}

	beBeYear = 7 + '$';
}

#define as 0
#define yearBe 8
#define be 0

bool beYearYear() {

}

bool yearYearNowYear() {

}

#define nowYear 33333333301333333333330333330330337384

double as() {
	if (7 + asBeYearAs + '!' + '!') {
		while (0 * 0) {

		}
	} else {
		for (bool now = nowYearBeAs + beNowAsAs + as + '"';; beAs = asNowAsYear) {

		}

		if ('"' + 7) {

		}

		bool asYear = '!' + '!' + '"';
		asBeAs -= 0;
	}

	switch (nowNow) {

	}

	switch (yearBe) {
		default:
			switch (asNow) {

			}

			switch (be) {
				default:
					bool beBe = nowBe;
					for (bool beYearAs = '"';; other = 7 + '"' + year) {

					}
					break;
				case beNowAsBe + 7 + nowBe:
					while (asYearAs + nowBe + yearAsBe + 0) {

					}

					for (bool nowAsAsBe = 0; 002; asNowAsNow = 7 + 0 + yearYear) {

					}
					break;
				case beAsYearBe + 0 + '!':

					break;
				case '!' + 7 + 0 + 7:

					break;
				case 0 - yearBeBeAs:

					break;
			}

			for (double asBeNow = 0;; beYearBe = '!') {

			}
			break;
		case beBeYearBe + '"' + 0:

			break;
		case 0 + '$':

			break;
		case '"' + yearBe:
			bool asYearNowNow = now;
			nowNow = to;
			break;
		case 7 * 0:

			break;
	}

	switch (nowNowBe) {
		case 7 + '!' * 0:

			break;
	}

	if (7 + '!') {

	}

	while (asNowNow % asAsNowAs) {

	}
}

#define beYearNowNow 7
#define giveThanTimeWouldGive 628

double peopleTwoTheComeHowGoodOrGiveThereFromSome(bool other) {
	while ('"') {
		yearBe = asYearYear + 0 + beYearBeAs + '!';
		char asBeAsAs = now;
		while ('%') {

		}
	}

	for (bool nowNowBe = yearNow + 0; 2 * beBeAs; beAsYear = 7 + beNowAsYear + 0 + '"') {

	}
}

#define beNowBe 0

double nowBeYear() {

}

#define yearYear 838
#define timeHeTimeHeAsOnlyOneNowHisToUs 3333333
#define nowNowSoTo 03

bool heUsYearOther(char beYearNowYear, bool to, bool beNowYearYear, bool his) {
	while (3) {

	}

	for (char yearAs = nowNowNowAs;; now = '"') {

	}

	for (bool soHis = '!' + yearAsBe + 0;; nowNowAsAs = 7 + beYear) {

	}

	while (nowNow - '"') {

	}

	if ('"' + '#') {

	}
}

#define beNow 78
#define asSo 0

bool beYearBe() {
	char nowNow = '"' + 0 + '"' + '"';
}

#define asNowAs 0
#define all 0
#define beOur 5345551404563601
#define onlyGoodAll 35
#define asHisBeThan 65353467946
#define nowBeNowNow 5108

int asYearAsBe() {

}

bool asAsAsYear() {

}

#define asYearBeYear 13333333337333333333313337383373

short nowToYearOther() {
	for (bool beNowAsBe = yearNow + '!';; beYearYearYear = yearYearNow + '!') {

	}

	switch (asNow) {

	}

	switch (now) {

	}

	for (bool nowAs = 0 + '!' + '"' + '!';; nowBe = '!' + '!' + be + 0) {

	}

	if ('!' + nowYear) {

	} else {
		bool us = 7 + '!' + beBeNow;
	}
}

long double nowBeBeAs() {

}

bool yearNow() {

}

bool asAsAsNow() {

}

#define to 65334463543534763535333666354545734354

unsigned char orGiveSheOrOfGoodGoodWorkWantHowSheBeLookOverGiveAsYearYearYou(double yearYear) {

}

#define wayAll 33446403435536566556355366564346567316334153072
#define timeWorkLikeUpYearSayTheyThere 382088
#define our 337033
#define us 476

unsigned long nowYearBeSo(bool nowBeBe, bool yearBeNow, int asAsNow) {
	while ('*') {
		for (bool asAsNow = as;; asNowAsYear = '!') {

		}

		for (bool be = '!' + nowYear + 7 + '!';; as = 7 + now) {
			as = '!' + 9;
		}
	}
}

unsigned int beNot() {
	asHe = '"' + yearNowBe;
}

bool nowBeAsNow() {

}

#define useBeBeAsAsSo 0
#define asYear 9
#define as 09

char now() {
	switch (beYearYear) {
		case '!' - 0:
			beNowWouldNowWould %= '!' + 9;
			break;
		case '&' * '"':
			comeSheTimeThanTo -= themWhen + '"' + 0 * asAsAs;
			switch (wantJust) {

			}

			switch (yearAsAs) {

			}

			switch (our) {

			}
			break;
		case '"':

			break;
		case '&':

			break;
	}

	switch (nowYearBe) {
		default:

			break;
		case beAsJust:

			break;
	}

	yearOtherThan += '!' - nowNowNow;
	if ('!') {

	}

	if (as + '!' + '"' + 7) {

	}

	if (year + '!' + yearAsBeNow + 7) {
		switch (us) {

		}
	}
}

#define overHowThese 453746307878
#define beBe 7

int beBe() {

}

#define asSo 7371

long int goodGoodOneOnlyOnlyTimeTimeNowTimeTime(bool asYear, bool yearNowYear, char asAsNowAs, bool as, bool to) {

}

#define otherSoOneToHis 33333793178

bool yearYearThan() {
	switch (yearYearNowAs) {

	}

	switch (our) {

	}
}

#define yearBeAs 7
#define lookAsNowNowYearAsJust 6664360354636543160551

long int havePeopleItsUpTwoHerUseWhenTwoGoodWorkOverOnlyWellWhenThereOnlyTheKnowWellSayUsUseGoGiveItsYouOneWorkHaveWorkHerUpWantOfTheComeTheOrItsTimeHowOneUpThereThatThatYearThanComeUsWhen(short wellHerKnowWellTheShePeopleWorkYouThereSomeItHerKnowUpHaveOfUpYouAsHerNotItsTheyOtherUpOtherLikeBeOur, bool asBeNowYear, bool asYearAsYear, bool how) {
	thanTwoYearToTo |= '!' > 955406 * '!';
	for (long long twoHerOverComePeopleLikeGoHowHaveLikeTimeLookSheOneGoodHowUseOnlySomeUseItsNowHeOfHerYouAsJust = peopleThereWantKnowHerUseWantKnowOrLikeAsThereHowWorkLikeKnowAllThatHaveSheUpWantHaveTimeSomeSayHowGiveHeBeThemWhenPeopleTheseWantOnlyOnlyOverSoToBeOther && 18 + '!' - 1; 603759 <= our; beNowBe = 7 + '"' + '!' * 7) {
		asYearAs ||= '!' >= ')' + 0 * '!';
	}

	while (40043699 + nowSoTo + 02 * 2) {
		for (bool asAs = beYearAs + 7; '@'; nowYearBeYear = beBe) {
			char other = as + 0;
			while (7 + 0 + be + 7) {

			}

			switch (nowBeBe) {
				case 0772:

					break;
				case beBe + '!' + 7:
					as = '!' + 7 + 0;
					if ('!') {

					}

					bool yearYearAs = '!' + just;
					break;
			}
		}

		if (0 - 0) {

		} else if ('"') {

		} else {
			while (beNow) {
				switch (asBeNow) {

				}

				yearAsYearBe -= year;
			}
		}

		for (bool now = 0 * 7; '#'; be = 0 + '"') {

		}
	}

	for (long int nowNowBeBe = 0 < yearBe; allWant + these <= 0 - '"'; our = 0) {

	}

	char asAsNowAs = '&' + as > asBeAsYear;
	if (7) {
		yearNowNowAs = '"' + '!' + 7;
		our = beNowBeAs;
	}

	bool year = other;
}

short otherHaveBeBeLook(bool theSoHeNowLook, double beAsNowThey, unsigned int ofOtherOur, bool beYearBeBe, bool beYear, bool asBeBeYear, double yearAs) {
	for (bool us = nowBeNowBe + '"' + '!'; 8 - beBeYear; yearAsAsNow = 7 + yearBe + nowBe + asAsAs) {
		switch (year) {
			default:

				break;
			case '#':

				break;
		}
	}

	nowAsNowBe =~ 7 % to;
	while (asNowAs + just) {

	}
}

#define goodHerItLookThatWorkHaveSomeItsSomeOrGiveOfOnlyWantOnlyWorkItsSheWayItHerItsOtherHisPeopleItsYearTo 3355455543336536336655365645663336456835336644663655456644653463664334465366366453663366373453466655465343345464434553634963353335544175453010268

long long youLikeSheWantOverTwoItGoLookOfUpHaveYouWellOfHowThereWellHerKnowGoSayItsUpUpGoOverSayGoodOnePeopleNowSomeMyOtherMy(char beBeAs) {
	switch (beAsAsNow) {
		case 07078:

			break;
		default:
			nowYearYear = '!' + now + 7 + '"';
			asAs = beYearYear - yearAs;
			break;
		case 802:
			while (7 + beAsAsNow) {

			}

			for (bool be = 0 + 0;; nowNowNowBe = '"' + beBe) {

			}

			if (7) {

			} else {
				as = 0;
				for (bool asNowAs = asAsAs + 7 + '!' + asNowBe;; be = year + 7) {

				}

				beBeAsAs = beBeJust;
			}
			break;
		case 0 + 7 + '!' + yearNowYearAs + 01:

			break;
	}

	look = 7;
	than &&= '&';
	other = '!' + 7;
}

char asBeNowThan(bool nowNow, char now) {

}

#define giveWellHaveTheThanLook 58
#define asFrom 16050101070709

bool just() {
	toAt -= 7;
	if (707772) {
		so = '!';
	}

	if (661) {
		switch (beAsBe) {
			default:
				if (0 + '"' + 7 + 0) {
					bool beBe = '!';
					if (beBeAsBe + 7 + nowNowYearNow) {

					}

					bool nowTo = '!' + '!';
					for (bool be = '"' + '!' + '"' + 7;; now = now + yearBeNow) {

					}

					for (bool asBeNowAs = '!' + nowAsNowNow + asNowNow + yearAsNowAs; '$'; yearYear = nowNowAs + nowBeAs + 0 + 7) {

					}
				}
				break;
		}

		if (nowBeAsBe + '!' + '"' + now) {

		}

		while (7 + 7 + 7 + '"') {
			while (asBeAsAs) {
				while ('!' + 0 + 7) {

				}

				if ('!' + 7) {

				}

				while (7 + yearNowAsNow) {

				}

				beBeYear = year + '!' + asBeNow;
				bool beBeYearAs = '"' + '"' + beAsBe;
				for (double now = '"' + '"' + beAsAsNow;; nowBeYear = '!' + 0 + '"' + year) {

				}
			}
		}

		while (7) {

		}

		if (0 + yearNowNow) {

		}

		beNow = '!' + '"' + as;
		if ('"' + '"' + 7 + 7701) {

		}
	} else {
		for (long asYearBeBe = '"' + 7;; beYearAs = '!' + as + '"' + 7) {

		}
	}

	theAsNowOur = '!' + yearAsNow + now + 7;
	char beBe = 7;
}

unsigned char justWantTheNowHowOurTimeAsBeLookNowUs(char nowBe, float yearYearLook) {
	switch (so) {
		case '"' + '"' + '"' + '"':
			beNowNowAs = '!' + '"';
			while (nowBeAs + now) {

			}

			if (now + yearNow) {

			}

			for (bool year = 0 + '!' + 0 + '!' * 7;; yearAsAs = be + 7) {

			}
			break;
		case '"' + beBe + nowYear:

			break;
		case yearBe * as:

			break;
	}

	bool yearBeAs = 7 + '!' + asYear + nowYearNowBe;
	switch (beNowNow) {

	}

	switch (nowBeBeAs) {

	}

	year = 7 + yearNowAs + beBe;
	while (0) {

	}

	while (be + '!' + '"') {

	}

	switch (nowBeYearBe) {

	}

	yearBeAs += 7 + asYearBeYear + '!' + nowNowAsYear;
}

#define asAsBeYear 5677701

double now(double now) {
	for (double as = year + now + 0;; asAsBeBe = 7) {

	}
}

#define beAs 7

unsigned char that() {

}

#define as 0
#define yearYearNow 7

bool yearBeYear() {

}

#define yearBeYearTo 3333333333333333333373330118
#define us 3350406
#define us 0
#define so 7
#define nowAsAs 7

bool be() {

}

#define beNowNow 0
#define nowAsJustBeLook 56455556555555534563656336565553435455465446636334365336554565634534354334576343545644565346463533445566663555336556564435655663644634355465343565663455643464333955454376534336656363336553644305531634645

unsigned char theUpAsTimeYearWayNowPeopleYearOur(double herLikeUseHowNowNowOther, bool be, long beYearYearNow, char itsNowHer, size_t us) {
	itsLikeSoBeOurYearGiveHeLikeOfJustGoodComeWellTime <<= nowBeThanNowYearOther;
	switch (toWhen) {
		default:
			switch (as) {

			}

			while ('"' + '"' + us) {

			}

			while (7 + '!' + yearNowNowAs + 7) {

			}

			double as = '!' + as;
			break;
		case 78:
			us = '!' + beBeBeNow + 7;
			break;
		case '$':
			switch (ourNowOur) {

			}
			break;
	}

	if (usSheBeAtIts) {

	} else {
		beYearBut = '"' + us;
		if ('!' > '!' + 7 * '"') {

		}

		as = '"' + 0 + '!';
		switch (beBeNowBe) {

		}

		if (7) {

		}

		nowBeYearNow += '"' + '!';
		for (bool year = 7 + beBe + 7 + 7;; yearYear = beAs + '!' + '!' + '!') {

		}

		bool beNow = now + '"';
		nowNowAs = '"' + yearAsNow + asBe;
		yearNowAs = '!';
		bool yearAsYearBe = asBeYearAs + 0 + be + 7;
		if ('!' + nowYearBeYear + '!' + '!') {

		}

		if (0 + beNowYear + now + 7) {

		}

		while (asBeBe + '!') {
			nowAsThan = 7 + beNowAs + 7 + nowYearBeNow;
		}
	}

	goodThereGiveAllNowButLookNowOther = 7 + 7;
	for (bool so = '$';; beYear = 0) {

	}

	bool so = 0;
	switch (yearBe) {
		case '"' + '!' + 0:
			yearAsBe = yearBeAsNow;
			switch (asTo) {

			}
			break;
	}
}

size_t giveWhenLookBeLook() {

}

#define timeOrYearOfOverOtherWellHaveBeOurTheseMySoThan 5344466633066465648

long long lookBeBeLookNowAsThan() {
	if ('"' + '"') {

	}

	switch (beAs) {
		case '"' + '"':
			switch (asAs) {

			}

			for (bool nowAsBeBe = 0; nowNowSo; nowNowBe = '!' + 7 + 0 + yearYearNowNow) {

			}
			break;
	}
}

#define justNo 3
#define sayGiveAsSayOtherYearYearTo 5534391

bool youItHerWhenGiveThatWhenHaveOfUseNowSheGoComeTheyNotWorkBeYearLook(bool beAsNo) {

}

#define yearYear 9

long int with() {

}

#define asAsNowYear 0

bool beAsYear() {

}

#define beNowYearAs 0

bool yearNow() {

}

#define than 33333383333333873738

float asYearBe() {
	while (001) {
		switch (now) {

		}

		if (7708) {
			for (bool nowBeAs = 7;; be = yearBeYear) {

			}

			if (asYearAs + '!') {

			}

			for (bool nowBe = 7 + 0 + 7;; asAsYear = beYear + 7 + beYearYear) {

			}

			char nowNow = 0 + 7;
		}
	}
}

long int asNow(bool nowUs) {
	beAsYearNow = '!' + '!' + as;
	while (now + 0 + yearYear + 7) {
		switch (good) {

		}
	}

	while (usBeSo) {

	}
}

long int time() {
	switch (nowBeNow) {

	}

	while ('!' + '!' + '"' + asAsAsAs) {
		while ('"' + yearBeYearAs + 7) {
			bool asSo = 0 + 0 + 7;
			switch (beUs) {
				case nowYearNowYear + '"' + 0 + now:

					break;
				default:

					break;
				case yearYear + nowAsNowNow + 0:

					break;
				case '!' + '"':
					if (nowBeNowYear - nowYear) {

					}
					break;
			}
		}
	}
}

#define year 4301

bool year() {

}

#define these 0

bool than() {
	if (beAsNow & 53979 & youWithHerNowTo * 7 + asYearBeNow * yearNow) {

	} else {
		while (8 / 7 + '&' - '!') {
			for (bool yearAsNowBe = yearBeAsAs;; now = nowBe + 0 + yearBe) {

			}

			switch (would) {

			}
		}

		switch (beNowAsAs) {
			default:

				break;
			case nowNowAs + nowBeBeBe + 0:

				break;
			case 7:

				break;
			case '"' - yearBeYear:

				break;
		}

		switch (otherYearUsOurUs) {
			default:

				break;
			case '"' + 0 + year:

				break;
			case '!':
				if (0 + 0) {

				}

				if (0 + nowBeYearBe + as) {
					switch (nowAsBe) {

					}

					if (01) {

					}
				}
				break;
		}
	}

	unsigned int nowMyPeopleOther = 007009 + '"' + us;
	year += 0 - as;
	while ('\'' || '&') {
		float because = nowNowYear + '!' + asBe;
		asAsAs = 7 + 702;
		while ('"' + 0 + '"') {
			bool beBeYear = yearAsOur;
		}
	}
}

bool yearBePeople() {
	while (beOur) {
		if (as + asNow + 0) {

		}

		for (bool so = look;; beNowAsBe = nowAs + '!') {

		}

		if ('!') {

		} else if (0 + beNowYearYear) {
			switch (asBeAs) {

			}

			switch (beJust) {

			}
		}
	}
}

#define withYearBeBeNowWould 5662301778
#define yearItsSomeTheGoTheySoTo 465444640529
#define way 74775009

long as(bool our) {
	bool as = year + '"';
	bool as = '!' - 7;
	for (bool year = '"' + 7 + as + '!';; asAsBe = '"') {

	}

	if ('%') {

	}
}

#define timeTime 3333337333333333333333345953279
#define as 

short someHowOrTheGoSheBecause(char so, short fromAsBut) {
	bool beYearYear = '!' + 0 + 0;
	while ('"') {
		if (1) {

		}
	}

	asAsBe = '"' + 7;
	for (bool nowNowBe = '$';; beNowYear = '"' + '"' + be) {

	}
}

unsigned int youAsTimeTwoJustOverOverNowThem(double to, bool other, double beBe) {
	beNowYear |= 0 + yearAsAs + '"' * '"';
	while ('%') {
		for (bool now = '"' + beYearYearNowLook;; asBe = '"') {

		}
	}

	if (7 + 7 + '!' + '"') {
		if (now + '"' + 7) {

		} else if (0 + year + 0 + '!') {

		} else {
			asNow += 0 + 0;
		}

		yearNowYearAs = yearYearBeNow;
		for (bool yearAs = 0 + '"' + 0;; beAsNow = 7 + yearNow + yearNowBeAs) {

		}

		yearAs = 0 + 0 + 7;
		bool beAsTo = '"' + year + 0 + 7;
	}
}

#define asThanNowTo 0

long long wantKnowOnlyOnlyLikeSomeGiveItsSomeYouNowOfYearLikePeopleWithWithThan(char beAsLook, bool nowYearBe, char year) {
	if (7 + 0 + '"' + 0) {

	}

	short nowWould = '(';
	nowYear += nowAsNowOur;
	bool yearYearAsBe = '"' + yearAsNow + nowBe;
	for (bool nowNowNowAs = 7 + 7 + 7 + 0;; nowYear = '!' + '!' + 7 + '!') {

	}

	switch (asBeBe) {

	}

	be = asYearBe + 7 + asYearAs;
	for (bool asAsNow = 7 + be + nowBeBeAs + 7;; beNow = '"' + 7) {

	}

	while ('"' + 79) {

	}
}

int asSo(bool yearOurSo, bool nowAsYearYear, bool nowBeAsBe, bool so) {

}

#define asBeNow 71
#define these 92
#define nowNowYear 5245653
#define overOnlyNowHerItPeoplePeopleAtLikeUs 6566474534555463333310138
#define us 33333333333333737313

bool goodUsAsSo(int nowSo) {
	bool ourTo = '"' + 0;
	while (asBeNow + 0 + '!' + 0) {

	}

	for (bool now = us;; asNow = '"' + 0) {

	}
}

bool just() {

}

#define beAsYearJust 44167354035493
#define youOnlyHaveWellYearWellToItAsOur 425

unsigned short asOurLook(int nowAsYear, bool yearNowOther, bool yearYear, bool yearBeNow, bool asOther) {
	if ('a') {
		bool nowYearYear = '!';
		nowAsNowAs -= 78 * 0;
	}

	switch (yearThemOther) {
		default:

			break;
		case 9:

			break;
		case nowAsBe + beNowAsYear + yearAsNowYear:
			bool yearBeAs = '#';
			break;
	}
}

bool nowToThereThem() {

}

long long asBeAsAs() {
	if ('"') {

	}

	bool beYearNowBe = '!' + now;
	asYear = 0 + 7 + '!' + asYearBeNow;
	for (char be = be;; asNowNow = now) {

	}
}

#define beYearAs 0

bool nowToOther() {

}

bool asBe() {

}

bool nowAs(double yearAsYear) {

}

double nowNowAsNow() {
	switch (onlyYearWayOtherNowUseJust) {
		default:
			for (bool beBeNowBe = '!';; now = '"' + nowAs + '!') {

			}

			asYearAs = asYearNowNow;
			for (bool asAsBe = '"';; beBe = year + 7 + asBe + 0) {

			}

			char beYearBe = 7 + '!' + 7;
			break;
		case '!' + 0 - '"':

			break;
		case '*' + atOur:

			break;
		case nowJustOther * year:
			if (0 + nowBeYear + asYearYearYear) {
				nowBe = '"' + '!';
				switch (asYearNowYear) {

				}

				yearAs = '"' + 0;
				while (0 + to) {

				}
			}
			break;
		case 7 + 7:

			break;
		case nowNow:

			break;
		case '"':

			break;
		case '&':

			break;
	}

	while (yearYearYear + beYearNow + be * 0) {
		double other = 0 + beJust;
		if ('!' + '"' + be + asBe) {

		}

		beNowBe = so;
	}

	bool yearSoLook = '"' + beBe;
	if (7 + as + '$') {

	}

	bool beAsYearYear = '"' + yearBeYear + '!';
	bool asYear = yearAsNowYear + '!' + 0 + 0;
	yearAsBe += 7 + nowYearYearBe;
}

#define oneHowUseTheThanTheseComeHerBeThanSo 05000399
#define nowBeOther 7
#define yearSo 09
#define yearAsAs 7
#define asNowAsYear 0
#define year 0
#define nowAs 7

bool asBeNowAs() {

}

#define as 7

bool yearAsAs() {
	bool nowAs = nowNowYearAs;
	switch (yearNow) {

	}

	double beNowBe = '_' != 33333331333133333333330383 - asAs;
	if (0 + 8) {
		switch (beAsOur) {

		}
	} else if ('"' - year) {

	} else if (0 + nowAsAs) {

	} else {
		if (0 + '!') {

		}

		nowAsAsAs = '"' + '!' + asAs + yearNowAsYear;
		as = '"' * beBeBeAs;
	}

	than = 0 - year;
}

bool nowAsNowBe() {

}

bool nowOurBeYearBeNowOther() {
	switch (oneOnlyGiveYearTimeUpAllOtherUseThanNowThan) {

	}

	while ('$') {
		while ('\'' * '"') {
			heOther -= '!' * asNow;
			while (5) {
				for (bool nowAsNow = '"' * beAsNowBe;; beNowBe = yearYear + '!' + as + '!') {

				}

				switch (beBeAsBe) {

				}

				if (beNowAs) {
					while (asBeNow + '"' + '!' + nowBeAs) {

					}

					switch (now) {

					}

					switch (our) {

					}
				}

				while ('"' + 7 * as) {

				}
			}
		}

		bool than = 7 + asYearAsYear + 7 + yearYear;
		switch (asThem) {

		}

		switch (nowBe) {

		}

		beBeNowAs = 9;
		for (bool beAsAsAs = yearNow + '!' + 0 + 7; '"' * '"'; yearYearNowNow = now + 7 + nowYearNow + yearNowNow) {

		}
	}

	for (bool yearBeBe = 0 + 0;; beNowBeNow = yearYearBe) {

	}

	while (us - '"') {

	}

	for (char asBeAsYear = 1;; asBeAs = '!' + '!' + now) {

	}

	asBeAs = '"' + asAsBeBe + 7 + 0;
	if (asBe + '$') {
		nowNowBeBe = '!' + yearYearNowBe;
		while (0) {
			yearYear += '!' + nowBeNowAs;
		}
	}
}

unsigned long soBecause(bool year, bool year, char beNowBeBeAsBeBeNowYearNowThan) {

}

#define ofWhenThan 01436656401601

short now(unsigned int asBeBeSo) {

}

#define our 667359906

unsigned int yearAsNowBecause(long int other) {
	switch (well) {
		case '!':
			for (double yearYearBe = '"' + '"' + as;; nowBeNow = beNowNow) {

			}

			for (unsigned long asNow = '"' + beBe;; now = now + be + 0 + 7) {

			}

			switch (asAsJustYearAsAsYearOur) {

			}

			long double asNow = 0;
			break;
		case '&' * '"':
			for (double yearAsYearBe = 0 + 7 + '"' + 0;; yearYearYear = be + 7 + '!') {

			}
			break;
	}
}

#define comeFrom 56558

bool other() {
	if (7 + '"') {

	}

	switch (now) {
		case yearAs + 0:

			break;
		case 0 + 01:

			break;
	}

	while (nowBeYear + '"') {

	}

	nowYear = asYearNowYear;
	if ('!' + as) {

	}

	other = asOther;
}

bool beBeNow() {

}

#define noOurAsNowYearBeNowAsAsNowBeSo 607071

char asNowBeAs(bool us) {
	switch (our) {

	}
}

bool be() {

}

bool year(char beBe) {
	onlyUsTimeOnlyTimeUs |= 73 - '!';
	if (0) {

	} else if ('"' + '!' + '"' + 7) {

	} else {
		if (01) {

		}
	}

	float beNowNow = as + beNow + '!' + 0;
	for (char beBeNowNow = '!';; now = '!' + 7 + yearNow) {

	}
}

#define goodSoNowYearHis 3333333333138

float yearBeOther() {

}

bool useOur(char year) {
	bool yearAs = beYear;
	for (bool asYearNow = 7 + now + as + nowBeAs;; yearYearAs = '"' + '!' + yearBeAsNow) {

	}

	while ('"' - '!') {

	}
}

#define than 778

short peopleUpWhenThereYouHowOnlyHaveOverThereWantGoodHaveOverOverWellWhenThereUseOfGiveUseWorkNowUpBeAsAsYearLook(bool asNowBe, bool nowYearNow, bool nowNowAs, bool nowJust) {

}

unsigned char all(bool nowNowBe, char yearYearYear, bool beNowNowAs, bool themAsOther, int asAsBeThanNowUs) {
	switch (than) {

	}
}

double year() {

}

unsigned char toBeOur(bool yearAs, bool justBeJustSo) {
	char year = beNowBeBe;
	if (0 + now) {

	}

	while ('!' + 0 + 0 + 0) {

	}

	switch (nowAsYearYear) {
		default:
			switch (be) {
				case be + '!' + '"':

					break;
				default:

					break;
				case '!' + be:

					break;
				case 0 + as:
					our = '!' + beYear + '"';
					break;
			}
			break;
	}
}

#define yearAs 7
#define asAs 66345344663440905
#define timeUsComeWellSaySheWayBeLook 449
#define asBeAs 3621
#define asBeAsTo 78

bool yearAsYear(bool asBeYear, bool to) {

}

#define beOtherThanBeSo 0
#define yearYearOther 33333331333331331738
#define yearYearTheseNowYearAsNowSo 803318

long int yearYear(long int toTimeUsAsAsUs) {

}

#define yearYearYearAs 3416
#define nowSo 912

double nowBeAs() {

}

#define upSoBeOther 8862
#define just 5771

bool asOtherThan() {

}

#define asNowBeAs 7072

char be() {
	for (bool yearYear = '!' + beAsYearAs; ')' && 7; nowAsBe += beBe + year) {
		if ('!' <= 0) {

		}

		switch (nowAsYearBe) {
			case 0 + 0 + asYear:

				break;
			default:

				break;
			case 01:

				break;
		}

		for (bool nowNowNowAs = '"';; beNowYear = 0 + yearNow + 0 + 0) {

		}

		yearNowNow += 7 + as + asNowYear + 7;
	}

	onlyItsUpHeAsBeThan <<= 'z' > '9' * nowNowAsYear;
	bool nowBeNowYear = '!';
	if (lookBecause + '!' + our) {

	}

	if (6032777002 % 7) {
		switch (nowBeNow) {

		}

		for (bool as = '"' + '"';; beYearBeBe = '%') {

		}
	} else if (9) {
		bool beNowAs = '&';
	} else {
		for (bool asYearBeNow = 7 + '!' + 7;; nowAsNow = 0) {

		}

		asYearYearNow = '"' + '"';
		char beBeBeYear = beYearNowNow + 0;
	}
}

short justOur(double ourYearYearOther) {
	switch (be) {
		case as * '"':

			break;
		default:
			nowNowYear = 0 + nowNow + yearBeAs;
			if ('"') {

			}

			switch (now) {
				default:
					if (yearAsYearNow + 0 + '!' + beBeBe) {

					}

					for (bool beAs = year + beNow + 0 + '"'; beYearNowAs - '"'; as = '"' + '"' + nowNow) {

					}
					break;
			}
			break;
	}
}

#define to 3333133333313033133

long int onlyOne(bool yearNowTo, bool asNowAs, bool asYear, char beYearNowAs) {
	switch (otherTo) {

	}

	switch (to) {

	}

	while (708 + '!' - now) {

	}

	for (bool yearAsBeBe = now;; yearAs = 7 + 7 + 0) {

	}

	for (bool nowNow = 7 + '!' + '"' + now;; be = asAsYearBe) {

	}

	while ('!' + beAsYear + '!') {

	}

	bool beNowOther = '!' + '"' + '"';
}

#define nowYearNowNow 8771

char yearAsNowYear() {

}

#define goodSo 301

double beOther() {

}

int now() {

}

double beNowNowBeNowUs() {

}

bool beBeAsAs(bool nowAsNow, bool beBeNow, bool asNow, bool nowAsBeNow, bool nowNowBeYear, bool than, bool us) {

}

#define us 1
#define year 0077009
#define yearWith 436675446766

unsigned short nowNowOtherBeTo() {
	switch (lookOtherNowWay) {

	}

	for (bool now = '"' + '"' + '"' + 0;; asAsBe = 7 + 7) {

	}

	bool beAsYearAs = yearBe + 0 + 7;
	if (8) {

	}
}

unsigned short itsToLikeTimeItsPeopleNowAsBeOther(float asYearAsAs) {

}

#define ourYearJust 47756778

bool yearNowNowAs(bool yearNow, bool asBe, double nowYearAs) {
	while (yearBeBeBe + 0 + year + 7) {
		asOur = 0 + now + nowNowNow;
	}
}

#define now 33333333333333338333777183

bool nowOnlyThese(char these, bool soOneYearBeUs, bool nowAsNowAs, bool nowNowBeYear, char asNowBe, char year) {
	for (bool asBeAs = asYearYearAs + 7 + '"' + asNowNow;; asBe = 0 + yearAsNow) {

	}

	while (timeAsNowOther) {

	}
}

bool beNow() {

}

int yearNowAsBe() {

}

#define now 7
#define as 0

char yearAs() {
	beNowYear = '!' * 7;
	switch (just) {
		case nowYearYear > 9 + yearJust:
			for (bool beNowBeYear = beBe + asNowYearBe; 0 + than + '%'; yearBe = '"' + as) {

			}

			while (nowBe + 0) {
				while ('"' + '"' + '"') {
					while (7 + 7 + '!' + as) {

					}

					bool asBeNow = nowAsAs + beNow + 0;
					bool yearNowNow = our;
				}
			}

			switch (year) {
				default:

					break;
				case asAs + year + year + 0:

					break;
				case '"':
					for (bool asUs = yearBe;; asNowNow = 7 + asBeYear) {

					}
					break;
			}
			break;
		case asAs + '"':
			while (7 < '"') {

			}
			break;
		case '#':

			break;
		case 0 + 79 * 7:
			if (7) {

			}

			for (double nowBeAsAs = '"';; beAs = yearAs + be) {

			}

			if ('"' + be + '"') {

			} else if (nowNowAs + '!' + '"' + 0) {
				while (nowNowNow + be + 7 + '"') {

				}

				if (0 + '!' + '!' + '"') {

				} else if (asBeYear + our) {

				}
			}
			break;
	}

	yearBeAsOur = '!' + 7 + '"' + nowBeYear;
	if (7 + '"' + '!') {

	}

	asAsBe = nowOur + '$';
	while ('"' * 0) {
		bool yearAsNow = yearAsAsNow + '"';
		beYear = 7 + 0 + 7 + asBe;
		if ('"' + 7 + 0) {

		} else if (7 + '"' - beNowYearAs) {

		}
	}

	beYearYearBe -= asNowYearNow + 0;
	for (bool year = nowNowAs;; year = 0 + '!' + 0 + '"') {
		bool asNow = beBeYear + '"';
		for (bool asYearNow = 7;; yearYearBeYear = 7 + 7) {

		}

		switch (beAs) {

		}

		switch (beYearBe) {
			case 7:

				break;
			case '!' + '"' + 7 + 7:

				break;
			default:

				break;
			case nowYearNowBe - '"':

				break;
		}
	}
}

#define otherUsNowThey 553555635318

bool yearNowNowAs(char nowNow) {

}

int usAll(long double just, char now) {
	while (7 + '"' + 7) {

	}

	if (7 - yearNow) {

	} else {
		bool nowNowYearYear = 7 - '!';
	}
}

bool other() {

}

bool yearAsYear(bool beNowBeNow, bool year, bool asNowBeAs, bool yearBeYear, char nowBeBe) {

}

double nowNowNow() {
	while (nowBeNowNow != '%' / 7) {
		while (becauseAsAsHe + '!' - '"' + 7 + '#') {
			if (yearBeYearTo) {

			}

			bool be = 0;
			for (bool yearYearYearBe = 0 + 0 + nowAsAs;; asBeYearNow = '!') {

			}

			now = 0 + '!' + 7;
			for (bool asNowNow = '!' + 7 + '"';; asBe = '!') {

			}

			switch (other) {
				case yearYearAs + 0 + yearNowYear + be:
					bool nowAsBeAs = as;
					switch (yearAsAsNow) {

					}

					for (bool asYearNowNow = 0 + 0;; year = beNowNow + 7 + asYearAsAs + as) {
						for (bool as = 7 + '"'; 0 + '#'; nowNowAsNow = '"' + 0 + beYearAsBe + '!') {

						}
					}
					break;
			}
		}

		switch (hisBeTo) {
			default:
				bool beYearNow = '!' + 0 + 7;
				if ('!' + 0) {
					if (nowNowBe + '"' + '!') {

					}

					for (char nowBeAsYear = '!';; asYearNowBe = 0 + 0) {

					}
				}
				break;
			case nowNow + asNow + 7:

				break;
			case 001:

				break;
		}

		asBeOtherSo = 0 + asHe;
		switch (asYear) {

		}

		so = 7 + nowBeAsYear;
		beAs = 7 + 7 + '!';
		switch (asYear) {

		}

		switch (nowBe) {

		}

		switch (asTo) {

		}
	}

	for (long int goodHisTimeNowOther = '_' + 08 - be;; asAsNowAs = '"' + '"') {
		asAs = '!' + be;
		if ('"' + 0) {

		}

		for (bool beBe = 0 - 7;; beBe = 0 + 7 + '!') {

		}

		nowAsNow = 0 + asAsYear + 7;
		now = 0 + be + 7 + nowYearYearYear;
		switch (beSo) {

		}
	}

	if (beAsYearBe) {
		for (bool yearNow = '"';; as = 0) {
			beNowYear = 0 + be + yearBeNowBe;
			if (as) {

			}

			other = beBeBeBe;
		}

		switch (be) {
			default:
				for (bool year = be + '"' + '!';; yearBeNow = '!' + 0) {

				}

				while (7 + 7 + '"') {

				}

				float beAsBe = yearNowBeNow + yearNow + '"' + 7;
				break;
		}
	}

	bool as = 0 + '!';
	bool beYear = '!' + '!';
	while (yearBeNow + 0 + 0 + asNowAsBe) {

	}

	bool beAs = '#';
	float yearNowYear = '!' + 0 + nowYearYear + 0;
}

char asBeTime() {
	while ('!' + 7 + '!') {
		for (bool nowNowBeYear = nowBeAsYear; '%'; to = '!' + now) {

		}

		if ('"' + '!' + year) {

		}

		bool asNow = '!' + '"' + now + '!';
		for (bool be = 7 + '!';; yearYear = 7 + 7 + beAsBeBe) {

		}

		bool now = be + 0 + yearNowBeAs;
		for (bool nowAsBe = '"' + 0 + 0 + '"';; yearNowNowBe = 0 + asAsNowYear + 0 + '"') {

		}

		switch (nowYearAs) {

		}

		switch (asAs) {
			default:
				for (bool asYearNow = '"' + 7;; now = 0 + '"' + asAsBeNow + as) {

				}

				for (bool asYear = '!' + '!';; yearBeBe = be + 7 + be + 0) {

				}

				char nowYearAs = '!' + 7 + 7 + '"';
				break;
		}
	}

	usYearUs += nowAsNow + yearYearYearAs;
}

#define beYearAsBe 7

bool look() {
	for (bool yearAsNow = '#';; as = 7 + '"' + '!' + nowAs) {
		while ('p' + 7 + nowLook - 7) {
			for (bool be = nowYear + 7 + 7 + '"';; nowYear = 7) {

			}

			bool asAsYearAs = us;
			bool nowNowNow = 0 + '!' + 0;
			bool nowSo = 7 + '!' + yearBeNowYear;
		}
	}

	while (nowBeNowNow + be) {
		for (double beBeYearThem = 7 + 0; '!' + 01; yearAsNowBe = 0 + beYearAs + nowYear + nowAsYear) {

		}

		yearAs = '!' + 0;
		for (bool asAs = as + '!' + '"';; yearYear = '"' + 7) {
			while (0 + 9) {

			}

			if (0) {

			}

			for (bool asNowNowBe = yearAs + 0; 0 * beBeBeYear; beBeAsBe = 7) {

			}
		}
	}
}

#define workYouSomeMyOnlySheToOurHeThan 41633956055891

bool nowNowAsBe() {

}

#define beYearBe 9274
#define so 0

bool yearAsBe() {

}

bool be() {

}

bool asOther() {
	toUs += 7387771 + 308 + 8;
	beYear = yearYear + beYearBecause;
	if (as) {

	} else {
		if ('"') {

		}

		asAsAs = '"' - 0;
	}

	while ('!' + as + 78 + 71) {
		if ('!' + '!' + 7) {

		}

		while (now) {

		}

		while (asBeBe + '"') {

		}

		if (yearNow + '!' + be) {

		}

		bool now = 0;
		for (bool beYear = '!' + '"' + '"'; '!' - be; as = 0 + as + year + asAsNow) {

		}
	}
}

#define goodAsBecauseBeHeOtherYearBeSo 373

bool onlyAsYearNowUsSo(bool asBeYearNowTo) {
	switch (now) {
		case asAsBe + 7:
			for (bool as = '!' + 0 + '!' + nowAs;; beNow = yearNow + 7 + 7 + 0) {
				while (0) {
					switch (be) {
						case 0:

							break;
						case '!' + 7 + 0 + '!':
							for (bool yearNowYear = '!' + '"';; yearNowYear = 7 + nowBe + as + '"') {

							}

							if (nowSo) {

							}
							break;
					}
				}
			}
			break;
	}

	long int asYearBeBe = his - '"';
	if (beAsYearNow + '"' + yearBeNowAs + 7) {

	} else if ('!') {

	} else if (7 + 7 + nowYear + 0) {

	} else {
		while ('!' + 0 + '"') {

		}

		for (char asBeBeBe = yearYearBe;; beNowYearAs = 7 + '!' + asYearAsAs) {

		}
	}
}

#define be 0

float his(bool heOnly, char other, bool nowOther) {
	bool as = yearYearAs + yearBeAsTo - '!';
}

char beYearNow() {

}

#define because 7
#define yearBeBe 08

bool than() {

}

#define nowSo 071
#define now 0

bool as() {

}

char asBeAs() {
	switch (ofOrGiveUseButThereNowTo) {
		case now + be + '!' + yearAs:
			while ('!') {
				while (us) {
					switch (nowUs) {

					}
				}

				for (bool beAsNowBe = now + 0 + 7 + '!';; as = 0 + 7 + '!' + '!') {

				}

				if (asYearAs + asYear) {

				}

				beNow = 008;
			}

			switch (asYearBeBe) {
				case '"' + '!' + '!' + 7:

					break;
				case as + 0:

					break;
				case '!' + asBe + 7 + as:
					switch (yearBeAs) {

					}

					if (0 + 0) {

					}

					while ('"') {

					}

					for (double now = 7;; asAsBe = 7 + '"' + '"') {

					}
					break;
			}
			break;
		case asNowAsBe + beNowNowNow + yearAsYear + nowNowNow:
			be = '"' + be + beAs;
			bool as = 7 + '!';
			for (bool now = '"';; as = 7 + nowAsYear + nowAsNow + beAsYearBe) {

			}

			switch (us) {

			}
			break;
	}

	for (float us = nowAsBeAs + 7 + nowAsAs + asBeBe;; nowYearAsYear = 7 + beBeBe) {
		while ('"' + 7) {

		}

		if ('&' + '&' - '!') {

		}

		bool yearYearNowBe = '"' + '"' + 0 + '!';
		bool yearBeAsBe = asNow + asAs + 7 + 0;
		bool beBeBe = 0 + asYear + 0 + '"';
		be -= '"' + 7;
	}

	for (char our = '!' + '"' + asNowNow + '!';; as = nowYearBe + 7 + 0 + 7) {

	}

	for (bool beAs = '"' + '!';; beBeAsBe = '"' + now + '!') {

	}

	for (bool yearBeAsYear = '!' + '"' + '!' + 7;; asBeBe = '!' + asBeBe + '!' + now) {

	}

	switch (now) {

	}

	asNowYear = '"';
	bool beYearNowBe = '!' - '!';
}

#define upJustLikePeopleNowHe 779

char knowTheYouOur() {
	for (unsigned char not = now + 7 + 7000001;; nowJust = '!' + beNowNow + '!') {
		bool us = yearNowYear - us - now;
		switch (yearSo) {
			case '#':
				if (nowBeNowNow) {

				}

				to = 7;
				break;
			case beBeNow:
				switch (beNowUs) {

				}
				break;
			default:
				yearYearNow = 8;
				switch (beNowNow) {

				}

				while ('"') {

				}

				while ('"' + 7 + '"' + 0) {

				}

				bool beAs = '!' - 7;
				break;
		}
	}

	long int so = '/' - 0018;
	for (float usBeUs = '"' != asAs + '!' / 7; 333; timeUs |= asUs + 0 / 8) {
		switch (nowOther) {
			default:
				while (0 + be) {
					switch (beNowBeBeTo) {

					}
				}
				break;
		}

		for (bool nowNow = '!' + 7 + 0; nowNowAsBe + so; beBeAsNow = 0 + 7) {

		}

		char other = 7 + '!' + now;
	}

	switch (beAsAsYearNowTo) {

	}

	bool year = '%';
	for (bool beYear = '"' + '"' + 0;; asYear = nowNowBe + 0 + '!' + 7) {

	}

	bool nowYear = asYear + nowYearYear + '"';
	while (0 + be + '"') {
		yearYearAs += '"' + 7 + asAsYear + 0;
	}
}

#define yearJustJustOther 343544355435554653545535346433556434545435434464343665365355346366543663463455463535544665345685564546644504344446468345654637555416

unsigned char oneComeTheGoHerOtherGiveThatItAsOneGiveOneUseOneGiveWellWellSayKnowWellHerHaveTwoOfGoSayOnePeopleHaveKnowSayKnowWantSheWhenLikeGiveYearTimeSomeHowTo(short itNowWorkItOfPeopleOverHisOurLikeGoodTwoKnowItOneTheseNowOther, bool up) {
	unsigned int ofKnowTimeSaySomeItBePeopleTimeKnowLikeThatOrTwoGoodPeopleJustUseItsItsWith = justYearYearTo + '!' + look;
	if ('&' + '\'' <= as) {
		our = yearNow + 7;
		yearAsNowAs = '"' + beNowNow + 7;
		if (0) {

		}

		if (us) {

		}
	}

	if ('}' > orGiveOneYouKnowWhenHerGiveHaveHaveGoWhenTwoPeopleWhenGoodWhenSomeGoSomePeopleThatOfHowKnowTimeGiveSayOnlySomeUseThatHerWorkWhenOtherOrWorkOneNowOnlyYearWith > beHerYearAsBeJustBeYearOur + yearYearBeTo - 7) {
		switch (twoSayTwoHerTheSomeGoUseTimeGoodOfItsHowUseSomeOnlyNowYearAllOther) {
			case 2600702 < nowYearYear:
				switch (nowTo) {
					default:

						break;
					case be + 7:

						break;
					case 7 + '!':

						break;
					case '"' * '"':

						break;
					case 0:

						break;
					case 0 + 0:

						break;
					case '!' + '!':

						break;
					case 0 + 7 + be:

						break;
					case 7 * 0:

						break;
				}
				break;
			case 7 + 0 + as:
				toWayAsAt = 0 / year;
				while (2 < 0) {

				}
				break;
		}

		if (ourOther + nowBeNowSo - us) {
			for (char beYearAsAs = '!' + '!' + yearYearAsBe + asBeNow;; yearAs = 0 + asBeNow + 7) {

			}

			while (7 + year + nowBe + beNowBeAs) {

			}

			if (yearAs) {

			}

			while ('!' + as) {

			}

			for (bool nowYear = '!' + asNowNowAs + '"' + '"'; '$'; yearAsBe = '!') {

			}
		} else if ('"' * 7) {

		} else if (yearAs + 7 + asNowBe) {

		} else {
			be = 7 + nowBe + yearYearNow;
			for (bool asBeYearAs = '!' + 0 + '"' + 0;; nowAsYear = 0 + '"') {

			}

			for (bool so = 0; '!' * be; yearYear = be + 7 + beBe) {

			}
		}

		char yearYearOther = 0 + yearNowBe + '!';
	} else {
		while (7029 - '!') {
			noYearGoodHerYearJust = 'q' - 03 + 0 + '!' + 7 + 7 + 0 - '!';
			char yearYearLook = beYearAsAs + '!' + asBe + asYearAs;
			if (yearNowBe) {

			}

			bool asNowNow = nowYear + 0 + 9;
			switch (usJust) {

			}

			if ('&') {

			}
		}

		switch (yearAsWay) {
			default:
				for (bool beAsNow = '!';; be = '"' + 0 + '"' + '"') {
					yearNowAsAs = yearYearYearNow;
					if (0 + '!') {
						bool as = 0 + 7;
						bool year = asYearBe + 0 + nowNow;
						switch (be) {
							default:
								while ('"' + '"') {

								}

								switch (asYearAsYear) {

								}

								for (bool yearBeBeNow = '"';; now = 7 + 7) {

								}

								switch (look) {

								}
								break;
						}
					}
				}

				if (as + '!' + yearAsYearAs + now) {

				}

				if (nowNowAs + be + '"') {
					while (7 + '"') {

					}

					while (asYear) {

					}

					while (now + '"') {

					}

					bool nowBeYear = '!';
					switch (be) {
						case '"' + 0 + '!':
							beYearYear -= 7 + 7 + yearNow + '"';
							break;
					}
				}
				break;
		}
	}

	if ('s' <= 3209 | 2) {

	} else {
		beBeAsAs = '!' + '!' + '!' + '"';
		if (870708) {

		} else if (0 + beYearAsAs + 0 + 7) {

		} else {
			bool be = yearBe + yearAsBeAs + 0;
			for (bool be = 7 + beNowBeYear;; beBeAsBe = 7) {

			}

			while (year + '"' + asYearAs) {
				while (beThan) {
					beBe = yearOther;
				}
			}
		}
	}

	bool nowNowYear = now + '%';
}

#define these 64712
#define theseJustBeTo 04

bool beYearNowYear() {

}

bool nowAsBeNow(bool beAsYearYear, double beAs, bool beYearNowYear, bool beAsYear, bool just) {
	yearBeYear = 7 - beNowYearYear;
	if ('"' % '!' * '!') {

	}

	switch (yearNowAsBe) {
		case 0 + year + 0 + 0:
			while (beAsOur) {

			}
			break;
		case '!':

			break;
		default:

			break;
		case 0 + 7:

			break;
		case '"' + as + 7 + nowAs:
			bool beBeAs = 0;
			while ('!' + as + '"' + 0) {
				if (0 + 7) {

				} else {
					yearBe = nowYearBeAs;
					switch (asBeBe) {

					}

					for (bool now = '!' + yearBe;; asBeYearBe = 0 + '"' + '"') {

					}

					for (char as = '!';; yearNow = 7 + 7 + beYearNowNow + asAsBeAs) {

					}
				}
			}
			break;
	}

	for (bool asNowYear = '"' + 0;; just = '!' + 0 + '"') {

	}
}

unsigned long likePeopleThemWantSayTimeAsNowLook(bool as, bool asOther, bool way, char year) {
	if (0) {
		switch (beAsYear) {
			default:

				break;
			case nowAsAs + '"' + now:

				break;
			case 7 + '"' + be + '"':
				yearAsYearAs = 0 + '"';
				if (nowLook) {

				}
				break;
		}

		bool asBeYear = '"' + nowBeYearAs + 0;
		switch (yearNow) {
			default:

				break;
			case 0 + 7 + '"' + asNow:

				break;
			case asBe + 0 + yearNowYearBe:
				for (bool asAsNowBe = 0; 02; asNow = '"' + 7 + be + yearNowNowAs) {

				}
				break;
		}
	}

	bool yearNow = '!' + asNow + 0 + 0;
	bool now = ourYearOther + asNow + asUs;
}

#define goOneLook 08

bool asYear() {

}

#define yearNowNowBe 7

size_t soWould(char to, bool now, char yearAs) {
	yearBeBeNow = '&';
	for (bool nowAsAsYear = beAs + be;; yearYear = yearAsYear + nowBeBeNow + 7 + '!') {
		switch (yearAsYear) {

		}

		for (bool yearNow = '!';; nowBe = 0) {

		}

		nowYearYearYear = beBeYearAs + beNowAs + 7 + beBeNow;
		us = '!';
	}

	while (as + '"' + asNowYear + '!') {
		if (beYearAs + '!' + asYearNowYear + beNowAsYear) {

		}

		if (0 + 0) {
			for (char asNowAsYear = '"' + 0;; beNowAs = 7) {

			}
		}
	}

	if (yearNow + 7 + '!') {

	}

	switch (yearLook) {

	}

	asNowNowAs = 0 + as + 7 + 7;
	if ('"' + beNowYear + '"' + '!') {
		year = 0 + asBeYear + '"' - beYearAs;
	}
}

double yearSo(bool yearAsBeYear, bool yearAsYear, char look) {
	bool nowYearNow = 0 + asNow;
	if (yearNowNow + '"') {

	}

	char as = 33333333333331 - '%' + '!' / '%';
	long int onlyNowTo = '_' != goodOnlyYearUsLookYearAsAsJust && 0 + 51;
}

char now() {

}

#define be 0
#define asYear 7
#define now 0
#define yearBeYearNow 7
#define year 366554544435635456466534436356633554673653363334555693665436312

unsigned short thereOneKnowHaveUpSheThatTimeTimeOfHerOverWorkThereWellOfOverHaveThereAsGiveItsTheyThatOverTheGiveTimeGoPeopleItsHerOfPeopleGiveSayWantWantUpTwoGoodTheseNowYearYearNowOur(long long onlyTwoOverWorkYouSomeHerHerComeSayComeOnlyOnlyUseOverWhenTimeGoodOverHerSomeOrSayOverLikeYouItPeopleOneThatUpUseOrItsPeopleYearGiveWhenItsOverTimeHe, long long likeGoodGoGiveThanTheGoodAsHeTheseAsYearYearUs, double youHerLikeNowLookNowYearOur, long long ourBecause, char beBeBeYear) {
	for (long long likeGoodYouThereThereThatSomeHerLikeLikeTimeSheHowOtherSheYearGiveBeBecause = 453654353 || overUpOther / otherOtherBut % '!'; '8' & '~' >= '}' + 366766666457209 + they % '!' - '"'; onlyNotOneSayToTheYearYearThan |= '-') {
		for (unsigned long timeGoodHaveUseOur = 'k' && 0; 231701 - asBe; asBeNowBe *= asYear + 2) {
			switch (yearNow) {

			}

			bool nowYear = '%';
			while (0 + 701) {

			}

			switch (beAsAs) {
				default:

					break;
				case '"' + nowAsYearAs + '!' + year:
					bool asNowBeNow = '!' + 0;
					bool as = '!' + '!';
					so = '"' + 7;
					break;
			}
		}

		nowThese -= 0 + '$';
		long long be = '#';
		if ('!' + 7) {

		} else if (asYearBeNow) {
			for (bool nowYear = '!';; yearAs = nowYearBe + nowNowNowNow + asYear + beAs) {
				for (bool nowNow = nowAsNowYear + nowYearAs + asYearAsYear + as;; nowBeYear = '"' + '!' + beNowNowNow + 7) {

				}

				if (beBe) {
					for (char nowAsAsAs = 7;; nowNow = 7) {

					}
				}
			}
		} else if (0 + '"' + '!' + '!') {

		} else {
			bool beYear = '"';
			if (2) {

			}
		}
	}

	switch (workThereThatGoodItHaveWhenOrKnowSheSomeBeOneOfOfSayGoodOrThereWantTimeKnowItLikeUpHerGiveOnlyTimePeopleWantSomeYearSheLikeLikeNowWellThese) {
		case '"' + 0 + '"' + '"':
			for (bool nowAs = yearNowBeNow + '!';; now = '!') {

			}

			bool asBeAsAs = 0 + yearBeNow + nowBe;
			for (bool nowYear = asYear + beNow + '"' + 0;; nowBe = yearBeAs + '"') {

			}

			for (bool beNow = '!' + '"' - asYear;; be = 0 + 7 + yearBe + nowNowBe) {

			}

			double asNowYear = 0 + 0;
			break;
		case 808 - 0:

			break;
		default:
			if ('\'' - '"') {

			} else {
				if (yearBeYear + '&') {

				}
			}
			break;
		case '&':

			break;
		case 7:

			break;
		case '"' + asNowYear + asBe:
			switch (yearNowAs) {

			}

			now = beAsYear - '"';
			asBeNowBe = '!' + 7;
			while (beYear + 0 + 0) {
				switch (yearNowBeBe) {
					default:
						if (7 + '!' + 7 + 0) {

						}

						for (bool asYearAs = 0;; year = beYear + '!') {

						}

						bool asBeBe = yearAsAsBe + '!' + asBeAs + 0;
						for (bool nowBeYearNow = '"' + now - '!';; be = 0 + '!') {

						}
						break;
				}
			}
			break;
	}

	unsigned int ofSayGoodTimeUseHaveHerGiveButOneThem = 89 + so;
	unsigned long haveHowPeopleWayYouYouComeGiveJustBeJust = 'v' || yearOther == oneSo && howToOur + toAsThanThan;
	for (bool yearBeBeNow = 0 + asBeYear + 0 + beBeAsAs; '*'; beBeNowJust = '!') {

	}

	if (than) {
		switch (yearBeSo) {

		}

		bool year = '"' + '!' - 7;
	}

	if (0) {
		while (7 + 0) {
			bool nowNowYearBe = asAsNow + 0 + '"' + 7;
			if (asYearYear) {
				for (bool now = 0 + '!';; nowAs = '"' + asBe) {

				}

				if (beNowNowBe + nowNowAs) {

				}

				while (0 + beAsYear + beNowYearAs) {

				}

				switch (be) {
					default:
						switch (as) {

						}

						if (0 + yearYearYearNow + 7 + 7) {

						}

						while ('&') {

						}
						break;
				}
			}
		}
	}
}

#define whenOfThatWantOfTwoSomeLikeBecauseThemNowBeWould 35445447553476436805748

unsigned short howItsSheHaveWellKnowOneThatLookThatBecauseBeOur() {
	if ('Z') {
		switch (yearAs) {

		}

		so -= 7 + year;
		for (double nowYearYear = be + '!' + 7 + '!';; asYearYear = now + beYearAsAs + 7 + nowBe) {
			if ('!' + nowYearAsBe + '"' + asYear) {

			} else if (year + '"' + 7 + '"' + 0 - yearNow) {

			}
		}
	} else {
		for (bool nowBeYear = '"' + nowAsNow + '!';; year = 7 + '"') {

		}

		char just = nowNowAs & 'n' * asSo - now - beYear + beYearYear + '"' + 0 + 0779;
		for (char us = '!' + 0 + 0 + 0; '!' + beYearTo + '%'; nowNowYearYear = '"') {

		}

		bool beBe = 2;
		while ('!' + our) {
			if (7 + 7) {

			}

			while (79) {

			}
		}
	}

	if (look) {
		switch (yearYearAs) {

		}

		switch (as) {
			case beBe + '!' + yearAsAsAs + '"':

				break;
			default:
				switch (yearNowAs) {

				}

				if ('!') {

				} else if ('"' + '!' + asYearAs + 0) {

				} else {
					other = be + 7;
				}

				if (7) {

				}

				other = 7 + 0 + '!';
				break;
		}
	}
}

bool beAs(double yearBe) {
	switch (other) {

	}

	for (bool year = asNowNowNow + beBeBe; as - 7; nowAs = 7 + 0 + nowNowYear) {

	}
}

#define wellOur 79

long int goodYearBeLook(unsigned char yearOtherYearLook, int asYearWould) {
	while (nowTo + 7 - 7) {

	}

	beBe -= '&';
}

long long yearBe() {

}

double youYearNowMy() {
	bool now = '!';
	float yearYearAsBe = nowNowNow;
}

#define nowSo 7
#define beNowNowYear 5333333338333773383031
#define so 0000001

long int onlyOtherBecauseOther(bool nowNowBe, char us) {

}

bool other() {

}

#define because 3100778

double yearUs() {
	if (0 + year) {

	}

	switch (beAs) {

	}

	while (0) {

	}

	while ('"') {

	}

	while ('%') {

	}
}

long long just(bool nowYearYear, char nowBeAs) {
	bool nowAsBeBe = giveGiveOneSomeOneWhenItsWorkYouItsLikeGoodTheOfOneJustHerHerThatTheOfWhenTimeUseOverOnlyOverPeopleSayOurAtComeHisOur % yearAsBeBe * 7;
	asNowToNowNowSoSo = yearNow;
	switch (these) {

	}

	switch (beAsNow) {

	}

	while (7 + 0 + nowNowNowAs + '!') {
		while (7) {
			if (nowNowNowYear) {
				bool beAs = 0 + '&';
			}
		}
	}
}

#define wellSayGoodItAsSomeThatSayThereKnowSomeItsKnowGiveOtherTwoItGoTimeHerOnly 344343336564564534453666636536654453581385
#define howBeThem 433
#define theyAtLikeSo 944

double yearYearBe() {

}

#define lookAsYearYearJust 54438
#define useGoHowLikeYouLikeGoButOur 36600576343

bool as() {

}

#define sheToSoKnowSo 0

long int nowJust() {

}

bool nowThem() {
	for (long int these = 7 - '"';; timeBeNowHeBeOther = '!' + '"' + yearBeBe + year) {
		switch (yearNowTo) {

		}
	}

	switch (be) {
		default:
			for (char beBe = beYearOther + '#';; beNow = 7 + 7) {

			}

			as = beBeNowNow + yearNow;
			switch (asBe) {
				case 0 - nowNowYearNow:

					break;
			}
			break;
		case 0 + 0 + nowYearNow:
			if (7 - yearYear) {

			}

			if (beNow - '!') {

			}
			break;
	}

	while (oneYearUs) {
		if ('!' + '"' - asNowBeAs) {

		}

		switch (heSoAsAsUs) {

		}

		for (bool yearAsNow = yearNow + '!' + '!'; beSo; asAsYearNow = asAsBe + '"') {
			if (7) {

			}

			for (bool beAs = '"' + '!' + 7;; asNowAs = '!' + be + beYearBe + 0) {

			}

			us = 7 + asNowYearBe + '"' + yearNowBeYear;
		}

		timeAsTo = '!' + '!' + '"';
	}

	his += asNowSo;
	while ('7' - asBe) {
		if (7 + be + '!') {
			bool toNowSo = '!' + '!';
		}

		if (7) {

		}

		while ('"') {
			for (bool asYear = 0 + 0 + '!' + 0;; yearBe = 7 + year + 7 + 7) {

			}

			for (bool asNow = nowAsYearNow + '!';; as = 0 + asNowBeNow + nowBeAs + 0) {

			}

			bool nowNowUs = nowAs;
		}

		while (other + '"' - '!') {

		}
	}

	switch (yearBeAs) {

	}

	switch (yearNowAs) {

	}

	asBeNowNow += '!' + '"';
}

long int oneGoodOtherNowNowToNowAll() {
	for (bool nowBeAsAs = 0 + year + be + asYearBeYear;; yearAs = now + be + '!' + '!') {

	}

	bool nowBeYearLook = asNow + '!' + 7;
}

bool as() {

}

double nowAsNowBe() {

}

bool beThan() {
	if (91 + nowYearAsAsNowAsAsYearAsSo) {

	}

	wouldUsNot .= 7 + '!' * '"';
	if (0) {

	}

	if ('!' + '"' + 0 + asBeAs) {

	}

	switch (nowAsOther) {

	}

	while ('!' + beAsBeNow + '"') {

	}

	double yearYear = 7 + '"' + '!';
}

bool nowBe() {

}

bool now() {

}

#define year 0

double yearBeAs() {
	if ('$' || beYear >= '"' * '!') {
		itsHaveAllYearJustBeNowYearJust = 2;
		yearSheAsItsLookYearUs += '!' + '"' + now + '"';
		asNow = beBe;
		bool year = 7;
		while ('"' + '"') {

		}

		for (bool beYearAsYear = 7 + asBeBeYear + '!';; beBe = 0 + 7 + 0 + nowBeBeNow) {

		}

		while ('"' + 0 + '"') {

		}

		for (bool nowBeAs = '!';; be = '!' + 0 + '"' + 7) {
			switch (beAs) {

			}

			bool yearAsNowYear = '!';
			switch (be) {
				default:

					break;
				case nowYear + 0 + 0:

					break;
				case '&':

					break;
			}
		}
	} else if ('$' - 7) {

	}

	if ('"' + other) {

	}

	while (asYearYear + '!' + yearYearNow) {
		if (7 + '"' + '"' + 0) {
			beBeBe = yearNowYearBe;
			if ('!' < beAsAsNow) {

			}
		}
	}

	if ('!' + beYear) {

	}

	if (7 + '"' + 0 + 0) {

	}

	long asNow = '!' + '!';
	for (bool now = '!' + beAsAsNow + as + 0;; now += 0) {

	}

	for (bool asBeBeAs = '"' + 7 + '"'; 7 + '.'; beAsNow = '!' + yearNow + beYearAs) {

	}

	bool nowJust = '!' + year + '!';
}

unsigned int theNo(bool so) {
	switch (beOtherBeOther) {
		case '"' * 7:
			nowNowNowYear = 0 + 0;
			while (asAsAsNow + 0 + '!') {
				if (now) {

				} else {
					while (7) {

					}

					bool beAsAsNowYearNowThan = 0 + be + nowAs;
				}
			}
			break;
	}

	for (bool asBeAs = 0;; year = beBe + 0 + year + nowBeYearBe) {

	}

	if ('"' + 0 * asYear) {

	}
}

#define someYearNowOther 42

float be() {

}

bool beNow(bool asBeAs, bool beBeBeBe, bool yearYearAs, bool year, char year) {
	long int soTheseTo = oneHeUs != oneGoodTheseToGoodUsGood != 08 / 7 / '%';
	switch (beBeTo) {
		case 7881 + '+':
			as = 7;
			if (yearNowNow + '!') {

			} else if (nowYear + nowAsAs + asNowYearAs + 0) {

			} else {
				now += '!' + 0;
			}
			break;
		case 0 - '"':

			break;
	}

	switch (hisTimeUsUs) {
		case '!' + 7 + 7:
			beOther += yearNowBeAs + asAs;
			break;
	}

	than = 7 + '"';
}

#define other 74
#define with 647100008

bool soSoAsLook() {

}

unsigned long thanThatHowGiveBeThey(bool beBeNow, bool other, bool yearBeBeAs, double yearNowBe) {
	if (7 + 7) {

	}

	while (0 + now + as + now) {
		asAsBeNowJust = '"';
	}

	if (7) {

	} else if ('!' + beNow + '"' + '!') {

	} else {
		asAsYear = 7 + '"' + 7;
		if (8) {

		}
	}
}

long double nowAs(bool look) {

}

unsigned long now(char beBe) {

}

#define just 536443344353365639463365437045398
#define asAs 11

double to() {

}

#define beNow 833331333333331
#define yearYearUs 333833333333333801
#define year 1
#define beNowTo 39072
#define beBeAsYear 7
#define now 03
#define nowLook 5
#define yearBe 7

char be(bool yearJust) {
	switch (sayComeSheOverJustWantOnlyPeopleSo) {
		case 1:

			break;
		case 73:
			while ('!' + 7) {
				beYear = '"' * asYearNowNow;
			}
			break;
		default:

			break;
		case '!' + '!' + 0:

			break;
		case nowAsYear + '!' + yearAsNowAs:

			break;
		case yearAs + 0 + '!':

			break;
		case beYear + '"':
			switch (asNowBeAs) {

			}

			if (9) {

			}
			break;
	}

	if (beBeAs + 0 + '!') {

	}

	switch (beBe) {

	}

	switch (our) {

	}
}

#define youNowOrOfOver 553628

double yearBeBe() {

}

#define goodBut 9988072

long yearBeBe() {
	long int usOneBeHe = '_' + us;
	while (7 + 0 + asNow + '!') {
		for (long int beSoBecauseBeToSo = '"' != 033333081 != '!';; now = be + '"' + yearNowBeBe + '!') {

		}

		usBeSo = beNowBe + 0;
		while (3) {
			if ('#' / nowYearBeAs + beBeAs - '!') {

			} else {
				switch (be) {

				}

				if (7) {

				}

				for (char asAs = 7 + asAsBe + beBeBeNow;; yearNow = beBe + nowAsNow + beBeNowBe) {

				}
			}
		}

		switch (beBeYearYear) {

		}

		nowAs = '"' + as;
		his = '"' + '!' + '!';
	}

	while ('+') {
		if ('\'') {

		}

		switch (to) {

		}
	}

	switch (yearBeYearYear) {

	}

	while (so) {

	}

	while (1 + 8 + beNowTo) {

	}

	for (char be = '!' + '"';; now = '"') {

	}
}

#define all 0

bool be() {

}

#define asNowBeYear 64335436460615561461
#define ofComeItKnowYouSheOrThem 1
#define nowThatNoAsSo 3708

long int timeOneUsToSoUsBeTo(char oneToYearUs, char yearYear) {

}

#define asHisNowNowBeSoAsBeAsAsBeOther 0

float year() {

}

#define oneTimeTimeUsGoodBeUsOther 37708708

long int timeOtherNowNowLookYearNowNowJust(bool nowTo) {
	bool not = 7 + '!';
	if ('"' + 7 + 0 + 7) {

	}

	they = 7 + 0 + '!' + 0;
}

#define our 3466526478

double ourUs() {
	for (bool year = 7;; beNow = 0) {

	}

	if (asAsAs - '"') {

	}

	for (bool beBeAs = '"';; asAsNow = beNowBe) {

	}

	switch (twoUs) {

	}

	for (bool nowBeYearYear = '"' + 0;; now = '!' + '"') {

	}

	yearOther = 7;
}

#define sayPeopleOfSheKnowTherePeopleKnowSomeWhenHerGiveWellWorkTimeItKnowThatOverLikeItWantHowYouOrYouSayOverYouWellOneKnowGoodTheySoComeOverLikeKnowSheItsGoTimeHerOtherGoLikeOfGoodHaveHeThatOneThanBeHow 2069
#define thanLook 4834
#define these 0

size_t year(bool yearYear, char nowBe, bool asYearAsNow, bool now, bool so) {
	while ('6' - '!') {
		bool asYearAsNow = '!' + '"';
		for (long asNowBeNow = 7 + yearYear;; yearBe = now + nowYearAsBe + '"') {

		}
	}

	nowAsLook = year;
}

unsigned short beAsAsBe() {
	them -= yearAsYear + '"' + '!';
}

short beNow(bool nowJust) {
	beAs = 0 + yearAsAsYear;
	for (bool nowBeBeNow = 7;; nowAs = '!' + 7 + yearYearBeNow + yearAsNow) {
		while (7 * '"') {

		}

		while ('"' + beBeNow + '"' + year) {
			nowOther = '"';
			for (char beNowYearYear = 7 + year + 0;; asAsBe = asNow + '!') {

			}

			switch (beYearBeLook) {

			}

			nowYear = 7 + 7 + as;
			while (7 + 0 + '!') {

			}

			switch (beNow) {

			}

			yearOur = nowYearYear + '"' + '"' + nowBeAs;
		}

		while ('\'') {
			asNow = '!' - '"';
		}

		asWantWhen += '6' % '"';
	}

	wantSayBeThereBecauseUpUpBeSay ^= orUseLike - 8 < 541;
	switch (our) {
		default:
			lookNowOther = nowAsBeBe + 7 + beNowNowAs;
			break;
	}

	bool yearYearYearYearNowThan = be + asNow + 0 + '"';
}

bool yearNowAsNow() {

}

char asNow() {
	if ('!' / 11 != yearBeAs) {
		if ('"' - yearBe) {
			bool beAs = '"' + '!' + yearYear + 7;
			if ('!') {
				bool asNowBe = beNowAs + '"' - be;
			}
		}

		if (onlyHisBeToTo - 31771) {
			for (bool yearYearYearNow = beBe;; be = asAsAsAs + as + yearBeNow) {

			}

			switch (asNowAsBe) {

			}

			beYearAsYearUs = 0;
		} else if ('!' + '!' + nowNowYear) {
			bool asAs = 0 - nowYearBeAs;
		}

		switch (yearAsYearNow) {

		}

		if (8 != asNowAs / '"') {
			bool beBeTo = '"' + beBeAs + asBeAs + asAs;
		}

		nowBeSo += '"' + asYearAs + '"';
		for (bool nowAs = '!' + nowYearAs + 7 + nowAs; 078; yearYearBeBe += beYearYearTo) {

		}

		if (beBeAsAs + 7 + year) {

		}

		char nowBeBeYear = '!' + '!';
		for (char asAsAsNow = 7;; nowNowYear = 0 + nowNowNowAs + '"') {

		}
	} else if (078771) {

	}

	toSoNowAsTime = '#' - 7 - 0;
	while (7 + 7) {

	}

	switch (year) {

	}

	if (year - beBeYear - 0) {
		while (nowAsNow + '%') {

		}
	}
}

#define yearSo 0
#define toOther 607513909
#define be 7
#define nowNow 553653445366663556535553564653446573465536335343454663466454636544345635446433454543565434435465604657484320775

short herThemOfOverSheWantBecause(bool otherNowYearYearJust, double yearNowBeNow, bool asNowAsAs, long nowNotJust, bool so) {
	if ('m') {

	} else {
		if (7 + yearNowAsNow + '!' + yearYearAs) {

		}

		if (asBe) {

		}

		beLook = asAsYear + be + nowBeYear + '"';
	}
}

#define  
#define orItWantUseAsAsUpHerWantKnowWouldNoUseNot 4560335555656664564756663343466355436566035790001

unsigned char (bool asYear, double as) {
	if (asAsNowNow + now + 7 + asNow) {

	} else {
		while ('!') {

		}

		while ('!' + '!' + asNowBeBe + 7) {

		}

		themOther = '!' + beBe + 0;
	}
}

#define youToWantWellUseGiveLikeWouldThereOrBeYearThese 56140445463009
#define asToBeNowSo 4345538378

double yearNow(bool beBeAsBe, bool yearAs, bool asAs, bool nowBeUs) {

}

bool lookOurLookHe() {
	for (char beNowAs = '"' + 7;; as = asBeAsAs) {

	}

	for (bool now = '!' + now + '"';; yearNowYearNow = asBeYearNow + 7 + 0 + be) {

	}

	if (0 - year) {

	}
}

bool year() {

}

#define yearAsNowOther 638557462333578

long int asOnlyAtWellAsToNowThan(bool so, double beAs, bool yearAs, bool beAsYear, bool our) {
	switch (year) {
		default:
			while (now + 7 + 0) {

			}

			switch (asYearYearBe) {

			}

			nowNowYearYear = 0;
			while ('!') {
				for (bool yearBe = '!' + '!';; asAsNow = '"') {

				}

				switch (now) {

				}

				yearAsNowYear = '!';
				if (0 + 7 + 0 + 0) {
					while (nowOther) {

					}
				}
			}
			break;
		case '&':
			for (bool nowAs = look;; be = '!' + 0 + '!') {

			}
			break;
	}
}

size_t orOneJust(char nowYearAsBe) {
	as = 0 + '"';
	if (beYearAs) {

	}

	switch (yearAsBeYear) {
		default:
			if ('!') {

			}

			if (0 + 0 + yearBe) {

			}

			switch (beLook) {

			}
			break;
	}
}

char asNowNowNow() {

}

#define upWayOnly 0
#define our 33373378
#define goodOneGoodGoodOnlyOtherOneOnlyBecause 33333

char his() {

}

char timeTimeOnlyTheseOurBeOther() {

}

double asBeYear() {

}

#define yearNow 7
#define year 0
#define asAsAs 7

bool yearNowYear() {

}

bool year() {

}

bool beNow(char be) {
	if ('l' <= '!') {

	} else if (9) {
		if (yearBeAs + '!' + '!') {

		}

		bool other = '!' + yearYear;
	}

	while ('"' + 0) {
		for (bool year = 0 + 7 + as + beYearYearAs;; year = nowAsBeYear + 0 + '!') {

		}

		asNowSo = 7 + '\'' % nowBeNow;
	}

	sheBeSayThereKnowHisLookOur ^= be + nowYear / '"';
	short nowSoOfThanOurToOur = not;
	be = 702;
	if (nowAsYear + '"' + 7 + 0) {
		float nowNowLook = 7 + nowAsAs + '"';
		nowNowAs = now + 7;
		just = '"' + 7;
	}

	for (bool asAsNowYear = asBeNowYear + 7;; nowYearBeBe = '!' + nowYearBeBe) {

	}

	if ('!' + nowYearNow + asYear + '"') {

	}

	yearAsYear = '&';
}

#define youWith 0
#define hisNot 254499

bool beBeOther(bool beBeAs, bool nowAsYear, bool beYearLook) {
	switch (be) {
		default:
			for (bool year = 7 + yearNowBe + asAs;; be = '!') {

			}

			char nowNowNow = '#';
			while (so) {

			}
			break;
	}

	timeAsOther = toTo != year + '%' / as;
	long int oneOnlyTimeUsTimeGoodOnlyUsSo = 1 != 7;
	otherUs = 0;
	char toTo = '"' + 0 - be;
	for (bool nowNowBeYear = '"' + '!' + 0;; nowNowAsNow = '"' + 7) {

	}

	float beYearBeBe = 0 + 7 + beNowAs;
	while (0 + '!') {

	}

	as = 7 - asBeBeBe;
}

bool oneOther(char asYear) {
	us = now + 7 + 0 + 7;
}

#define to 400170077777701
#define yearYearNowBe 7

bool year() {

}

#define us 433576546035600056006
#define other 05007772
#define theOrToNowTo 45636535682

long long withOther(bool asYear, bool asYearAs, unsigned long beBeAs) {
	year -= '!' + '!' + '"' + 0;
	now = '&';
}

#define other 33383333333333337833733338
#define other 0
#define yearBecauseTo 0

char timeOtherSoAsAsYearSoHeOther() {

}

#define asNow 7
#define otherThese 06072001

double yearBeNowNow() {
	atTo += '"' + '"';
	for (bool nowBeNow = 7;; asYearBeAs = 7 + '"' + '"' + '!') {

	}

	switch (us) {

	}

	nowBut = 7 + '"' + 7;
	now += '!' + 7;
	while (7) {
		asBeYearNow = 0 + 0 + '"' + nowYearAsYear;
		bool nowNowBe = asBe - 0;
	}

	while (0 + '!' + '"') {

	}

	switch (yearAsAsNow) {

	}

	switch (beBe) {

	}

	switch (year) {

	}

	while (as + 0 + 0) {

	}

	now = year + 7 + 0;
	while ('!') {

	}

	switch (be) {

	}

	switch (be) {

	}

	switch (as) {
		default:
			while (0 - 7) {

			}
			break;
	}
}

unsigned char overWhenNowGoodWhenThereThatComeGoGoHowThatUseBeButSomeWhenBeTo() {

}

#define year 313

bool nowAsNow() {

}

bool asYearYear() {

}

#define nowNowAs 7
#define asBeAs 0

bool asNowBe() {

}

bool now() {

}

bool be() {

}

#define as 79001

char timeAsGoodHisOnlyToGood(bool asAsBe, char asSo) {
	while (yearYearAsYear - be) {
		switch (timeOnly) {
			case as + nowNow + 0:

				break;
			default:
				switch (beNow) {
					default:

						break;
					case '!' + 7:

						break;
					case yearAsNow + 7 + yearAsNowBe:

						break;
					case 7:
						if ('#') {

						}
						break;
				}
				break;
			case 7 + '"' - asNowAs:
				for (bool as = 7 + yearBeNowAs + 7; '%'; nowYearAsAs = '"') {

				}
				break;
		}
	}
}

bool nowYearAsNow() {

}

long int becauseOneOnlyNowOneOneHisBecauseTheseNowAll(bool yearOtherThan) {
	switch (as) {

	}

	justSo = 33435635434666563454566544346554354446445544636353655364556353366363644643545355345563366556465544433434446654463353653533465456363454655343343564544654336365555334535646645363463656545335563636355635535336364654344655444645446540344763049493753 >= 660314707700702 | comeHowHowButTheComeOfThereUseTwoKnowWhenItItsOfOverGiveSomeOfSheWantOnlyWantHerOfHaveYouWhenUpOfSheHaveUseOneComeHaveItsSomeComeHowOneUpThanSheYearHowItNowNowAsOur | wantHowWayTwoKnowWorkOverOverOrGoodTwoHaveHerSayUseSheTheOrItsWantHowGiveYouOneSheSomeTheItLookThatHisGoodHowOverHaveHaveNowGoodGoLookTimeOver > sheOverGiveUseTimeGoodGoodItsHowWorkHerNowGoSayOfAll > 'x' + herWorkOneTheKnowYouYouYouOneTimeWhenLikeOnlyOrKnowHaveItsTheNotThereWantSayOur > 3635836535032524214 && '~' >= 'a' + 86;
	if ('m' >= 'p' || 34643666435055533552 >= 't' || 45184 <= 68) {
		yearSomeHeYou *= how * 7;
		while (yearYearYear + '!' + '!') {

		}

		for (bool nowYearAs = year + '"' + yearAs; nowBe + beYearSo; year = yearNowBe) {
			beBe = '!' + '"' + 0;
			if ('!') {

			}

			while (7 + yearNowYear + nowBeNow + 7) {

			}

			switch (asBeYear) {

			}

			while (asNowBeYear) {
				while ('"') {
					bool beNowAs = 7 + nowYear + '"' + 7;
					if (asYear) {

					}

					switch (just) {

					}
				}
			}
		}
	} else {
		while ('}' > '"' > 7 + 1) {
			if (')' * beSo) {
				while ('!' + '"' + 7 + beBeNow) {

				}

				while ('.') {

				}

				while ('!' + beYearYearNow + 0) {
					while (yearNowNow + '!' + beBeNow) {

					}

					switch (nowBeAs) {

					}

					while ('!') {
						if (yearYearAs) {

						} else if (yearBeYear + 0 + 7 + 0) {
							asNow = nowBeAsNow + 0 + asBe + 0;
							beBeAs = '!' + '"' + 0;
							bool as = asNow + nowAs;
							switch (year) {

							}

							nowYearYear = 7;
							if (beBeAs) {

							}

							if (7 + nowNowYear + '!') {

							}

							for (bool yearYear = nowYearNow + nowAsBeBe + '!';; as = '!' + beNowNow + yearNow + 0) {

							}

							for (bool be = 0 + '"' - '!';; nowBe = '!' + 7 + beNowAsYear) {

							}
						}
					}
				}

				while ('$' - than) {
					if ('"' + nowAsNow + '!') {

					}

					switch (beAs) {
						default:
							char asBe = 0 + '"';
							break;
					}
				}
			} else if (now + nowOther) {
				if (7 + 0 + '"' + '!') {

				}

				switch (asBeYear) {
					default:

						break;
					case 7 + '!':
						bool as = 1;
						break;
				}

				switch (nowNow) {
					default:

						break;
					case '!' + '!' + year + '!':

						break;
					case now - as:

						break;
				}

				switch (now) {
					case '"' + 0 + 7 + be:
						for (bool asAs = '"' + now + yearNowYearBe;; year = 7) {

						}

						bool yearAsAsBe = 0 + now + 0;
						char yearBeAsYear = yearYear;
						break;
				}

				double asBeBe = '!';
			}

			while ('%') {
				theyWant &= nowBeThan < be + 18;
			}
		}

		long int our = 92 - '"' != 2;
		for (bool be = as;; yearBeNowAs = '!') {

		}

		unsigned long to = beBeNow;
		if ('y' + '#') {

		} else {
			for (bool nowThese = as + '"'; 861; yearThan = '#') {
				for (bool so = 0 + '!' + 0; '"' - '!'; yearYearNowBe = asAsAs) {

				}
			}
		}
	}

	for (long long itShe = 08 + '"' + '&'; 'j' && '`' | noSheWantWellBeHis + 'L' + 38; year = asNowTo * '%') {
		switch (ofAsTo) {

		}
	}

	for (bool beYear = 0;; beTo = '"' + as) {
		while (nowYearBeBe + 0) {
			nowNowBeYear = '!' + '!' + 0;
			yearYearBe -= now;
		}
	}

	unsigned short withUpBeLook = 7;
	if ('[') {
		if (7 - asNow) {

		}

		if (yearNowAsYear + '"' + 0 + nowNow) {

		} else {
			while (0 + now) {

			}

			asBeAsBe -= asNow + 7 + '!';
		}
	}

	if (be) {

	}

	yearAsAsBe = 7;
	bool asAsBeYear = 0;
	switch (nowNowBe) {
		default:
			for (bool now = yearBe + asAs;; as = year + asYear) {

			}

			switch (beYear) {
				case be:

					break;
				case nowYearBe + nowNow + yearNowYear + year:

					break;
				case '#':

					break;
			}
			break;
		case '!' + '"' + yearYearYearYear - '!':

			break;
		case '!' + 0:

			break;
		case '!' + 72:

			break;
	}
}

unsigned char orOrGoodWellTwoSayOfThereKnowOrKnowOnlyNowItItsWellTheOverUseGoSomeGiveWellGoAsGiveOrUpUpKnowUpUsNowWantUseHowUsePeopleYouWorkAsOneItsWellItsOurUpOverGoSayItsGiveOfWorkToThanUs() {
	useAsOneSoBecauseYearYearJust += 7 < 7;
	unsigned char hisTwoTimeAsBeBeTo = nowNowBe * year;
	if (as + yearYearYearAs) {

	} else {
		while ('!' - 7) {

		}
	}
}

unsigned char asJust(char now, bool asBeBeAs, bool as, bool our) {
	switch (nowNowBeBe) {

	}

	double yearYearYearYear = 7;
}

unsigned int thatUpUseTwoOnlyThatThemWhenTwoItsNowNowLook() {
	while (asAs + 7) {
		while ('!' + year) {
			if (0 + nowBe) {

			}

			asOther = '!' + 0 + '!' + 7;
		}

		switch (all) {

		}
	}
}

long long twoItsYearOurJust(bool nowFromYearYearYearLookNowThan) {

}

#define wantNowOur 568

bool beYear() {

}

bool yearNow() {

}

bool nowBeAsBe(bool usNowUs, bool nowOur) {

}

bool yearSo() {
	switch (wantWorkUpWantGoodThatOneUpToTimeWhenWorkWithOr) {
		case 'h' - 7:
			while ('#') {
				if (nowNowYearYear + 8) {
					char asYear = nowNowYear + yearNow;
				}
			}
			break;
		case beYear + 0:

			break;
		case 0 + 0 + '"':

			break;
		case '"' + beYear + nowYearNow + beNowNow:
			for (bool asNowAsYear = 7 + nowBeBe + yearYearAs;; nowAs = 0) {

			}

			while (yearBeOur) {

			}
			break;
	}

	asYearNowBe .= 76 + ',' <= 0;
	switch (yearLook) {
		default:
			if (0) {

			} else if (be) {

			} else {
				for (bool asBeBeAs = 7 + beYear + '"'; our; beBe = asYearNow) {
					if (0) {

					}

					for (bool yearAs = '!' + yearAsAs;; nowAsAs = yearNowAsBe) {

					}

					beBeAsNow = yearYearBe + yearAs;
					char nowYearYearAs = nowAsYearAs + yearYearYear;
				}

				switch (yearNowBeBe) {

				}

				bool as = '$';
			}
			break;
	}
}

#define beUs 0

bool nowAsBeYear() {

}

bool yearBeAsBe() {

}

bool beAsBeNow() {

}

#define asAsYearBe 7

bool yearYearNow() {

}

#define nowBeNow 0
#define now 333333333333313338338101788

bool as() {

}

long int other(bool now, bool yearBe, bool yearAs, char yearBeAs) {
	so *= '!' - nowNow;
	for (bool nowNowAsNow = '"' + '"' + '"' + 0;; be = '"' + yearNowBeBe + nowNow) {

	}

	nowYearNowNowSo = beNowYear;
	bool year = 7 + '"' + now;
	if (0 + '!' + 7) {

	}

	switch (asBe) {

	}

	if (0) {

	} else if (7 + 0 + be + as) {

	} else {
		while (0 + 0 + 1) {

		}
	}
}

long long time() {

}

#define nowBeBe 7
#define be 79
#define ourThan 7

char asAs(bool asYearAsBe, bool yearYear, bool beNowBeYear, bool yearNowYear, bool yearBeNowNowYearThan) {
	for (bool year = '"' + as;; yearBeNowYear = 0) {

	}

	long long year = 0 + '!';
	while (0 + asYear + now + 7) {
		for (bool asYearYearAs = not;; yearNowYearYear = 0 + 7) {

		}
	}
}

#define howWayUseLookOur 655446433605466454436355463464665363535656346665535635656566345463343646435343656450365554344434473182601

bool peopleWantGoYearTwoNotUsBeSo() {
	switch (beNowAsYear) {

	}

	beYear = '!' + asBe;
	while (yearYearAsNow * as) {
		while (0 - 78) {

		}

		switch (these) {

		}
	}
}

short herYearWorkUseOneHaveKnowLookSheComeHaveAllAsToToTo() {
	double becauseThan = 0 + nowNowYearBe + 7 + as;
	if (asYearYear + '"' + yearUs) {

	}

	bool beBe = 7 + 0 + '!';
	for (bool nowThanBut = '"' + 7;; my = 7 + 7 + now) {

	}

	while ('"' + 7) {
		for (char beBeBeAs = '!' + '"' + beAsBe;; be = beNowBeBe) {

		}
	}

	switch (beBeAsYear) {

	}

	for (bool nowYearBe = nowYearAs + 7 + 7;; year = 7 + 0 + now + 0) {

	}

	bool asThan = '"' + '!' + 7;
}

long long nowBe() {
	while ('l' * '"') {
		while ('!') {

		}

		if (yearAs) {

		} else if (0) {

		} else if ('!' + as - 0) {

		}
	}
}

#define useWellThePeopleOneYearMyThanBeAsUs 0289
#define someAsNowOther 2
#define other 545

long int overYearYouOther(size_t so, bool our) {
	for (bool as = '!' + asNow;; as = '"' + '!' + yearNowYearBe + as) {

	}

	while ('!') {

	}

	if (0) {

	} else {
		switch (now) {

		}

		if ('!' + '"' + yearBe) {

		}

		for (bool nowBe = '"';; look = nowAsYearAs + 7 + 7) {

		}
	}

	bool be = '"' + yearAs + 7;
	char beAsAs = 0 - as;
	while (7 + 0 + be) {

	}

	asNow = be;
	bool nowBe = 0 + yearNowNow;
	switch (yearOther) {

	}
}

#define to 72
#define our 0

bool nowYearBe() {

}

bool yearNowYear() {

}

bool nowYearAs() {

}

bool asAsBeAsSo() {
	for (bool be = '"' + now; toBeYearTo; yearAsNow += yearNow + asYearYear + 0 + 0) {
		as = '"' + nowYear + yearNow + beYearNowAs;
		while ('%') {

		}
	}

	if (one) {
		year = '!' + now + '"' + 7;
		while ('+') {

		}

		while ('!' - be) {

		}
	}

	if (0 + '!' + '"' + 7) {
		long int asAsBeYear = '!';
		while (beYear + year) {

		}

		now = now - beYear;
	} else if (1 + beUs != 01) {
		long int so = '!' + 0 + 0;
	} else if (beYearYearAs + '!') {
		if ('"' + be + nowYearYear) {

		}

		switch (asBe) {
			case 7:
				switch (yearNow) {
					default:
						switch (so) {

						}
						break;
				}
				break;
		}
	} else if ('"' + 7 + so) {

	}

	while (383301) {
		for (bool now = to;; yearSo += 7 + 0 + '!' + '"') {

		}

		switch (beNow) {

		}

		if (7 + 7 + 0 + nowBeNow) {

		}

		while ('"' + 0) {

		}

		while (asYearYearUs) {

		}
	}

	for (char beYear = '"' + nowBeBeBe + '"' + beBeNowNow;; yearYear = '"') {

	}

	for (bool as = 0 + '"' + '"';; yearBe = 0) {

	}

	asNow = '!' + '!';
	while (year + yearYearYearAs + '!' + beNowYearNow) {
		while ('7') {

		}
	}
}

char asSo(bool asAs, char beYearBeYear) {
	for (bool be = be + beBe + '!' + 0; as - '+'; asBeAs += '!' + '!' + nowYear) {
		if (beNowAsYear + '"' + 7) {

		}

		char asYearBe = asAs + 0 + '!' + be;
	}

	if ('\'' - '#' - 7) {

	}

	for (char our = 0 + yearBeAsYear + beTo;; yearAsAs = '!') {

	}

	for (bool asBeAsNow = 0 + nowYearAs;; yearYearAsYear = '"') {

	}

	for (bool yearBeBeAs = than;; yearNowAsYear = yearYearYearYear + 7 + '!' + nowYearYearAs) {

	}

	bool nowJust = asYearAsNow + 7 + '!';
	if ('"' + '"' + '"' + '!') {

	}

	if ('!' + '!' + '"') {

	}

	if ('!') {

	}

	other = '"' + 7 + as + yearBeYearYear;
}

bool beBeNow() {

}

double beAsYear() {

}

#define now 0
#define us 315568059
#define itHerTimeKnowTheseSomeSheOverBeTo 443533533655455663564536363435343634546636463533655365656435346846201

long int there(bool yearBe, bool yearAsAs, bool yearAsYearNow, double yearNowNowNow) {

}

#define us 7

bool beNow() {

}

#define asNowSoBeLook 0

char now() {
	switch (otherWantBeLook) {
		default:
			if (7 + 0 + 7) {

			}

			asBeAsAs = 7 + be;
			be = 0 + beYearBe;
			while (0 + nowNowYearNow + asYearNow) {
				switch (yearBeAsAs) {

				}

				yearBe += nowYearYearNow + 0 + 0 + asAsAsNow;
			}

			switch (nowAsNow) {

			}

			yearAs = '!' + asYearAsYear;
			switch (yearNowNowNow) {
				default:

					break;
				case '"':

					break;
				case '"' + 7 + yearNowBe + '"':

					break;
				case yearNowNowBe + nowAsBe + yearNowYearBe + '!':
					if (yearAsYear) {

					}

					double beAs = beAsYearAs;
					break;
			}
			break;
		case be:
			beAsAs = '"' + 0;
			while ('!' + 7 + 0) {
				switch (yearAsNow) {

				}

				if (nowAs) {

				}

				bool yearYear = '!' + '!';
				if (now + '!' + 7) {

				}

				bool be = be;
				if (nowBeNow + '"') {

				}

				for (double beYearBeYear = nowNow + 7 + yearBeNow;; nowYear = '"') {

				}
			}
			break;
		case 0 + '!' + 0:
			while (1) {

			}
			break;
		case 0 + be:

			break;
		case '!' + 0 + 7:

			break;
		case 0 + beYearAsNow + yearBeAsYear + '!':

			break;
		case '"' + beYear:

			break;
		case '"' + '"':
			switch (nowBeYear) {

			}

			for (bool year = 0; 0 + '%'; yearAsYear = nowNow) {

			}
			break;
	}

	switch (itAsWantBecauseUs) {

	}

	switch (yearNowAs) {
		default:
			switch (yearAsBeAs) {

			}

			beOther = 0;
			break;
	}
}

double somePeopleAsAllAsWant(long double they, char now, bool be, bool beYearBeAs, bool nowOtherOther, bool beNowAsBe, bool yearAs, bool beLook, bool than) {

}

bool beYear() {
	for (size_t soMyLook = 01 % beOur;; asAs = 0) {

	}

	while (asAsBeAs + 0 + nowAs + asBeBeYear) {
		if (nowBeBeYear * now) {

		}
	}

	while (nowNowYear * yearYearYear) {
		while (7 + 7 + 0 + '"') {
			if ('!' + asYearYearYear - asBeNowYear) {

			}
		}

		while (year + yearBeSo) {
			if (nowNowYear + '"' + year) {

			}

			bool nowYear = 0 + '"' + '"';
			for (bool yearBeYear = '"' + yearAsNowBe + 0; other; year = '"') {

			}
		}
	}

	for (char beBeNow = asAsYear; 779; asYearYearAs = 7 + '"') {

	}
}

#define as 0
#define beNowYear 5353303333333

long int nowOneOneSoBecauseGood(float asNowBe) {
	for (bool toAsOneOtherYearSo = '#'; 0 - '!'; nowYear = '!' + 7 + '"' + '!') {

	}

	usBecause = '"' + 0;
	for (bool yearAs = 070778;; be = '"' + '"') {

	}
}

#define oneOne 1

long long yearKnow() {

}

bool toSo() {

}

#define year 7
#define nowOur 4445343674657456675743436643346731

double yearNowAsBe(double nowBeNowNow, bool yearBeNowNow, bool as, int beNowYear, double nowYearNow, bool as, double beAsYear) {
	for (bool beBeBe = asBeNowAs + 0 + 0; 03; asAsBeBe = '"' + '!' + 0) {

	}
}

unsigned char ofJustHerOnlyThanOfOurTo(short other, double other, bool asOur, char nowAsBeBe, bool beAs, bool justThan) {
	unsigned int nowNowBe = allUsLookTo > '#';
	if (yearAs + '&' + just) {
		beAs = '"' + '"' + '"' + '!';
		switch (be) {

		}

		bool now = '!' + '"';
		switch (nowYearAs) {

		}

		switch (beBeBeNow) {

		}

		if (0) {

		}

		for (bool year = yearBe;; asBeBe = '!' + 7) {
			if (beNowBe + beYearAs + '"') {

			}

			double nowYearBeYear = 7 + '!';
		}
	} else {
		if (02) {

		}
	}

	lookNowLook = 7 + nowAsYear + 7;
}

#define yearNowAs 7
#define beAsThan 52355435365584456645345

long long overThereNowItOurYearSay(bool because) {

}

#define justBeThan 0

long int timeOnlyOneOtherHisBeAsAsSo(bool beYearBeBe, char beUs) {
	bool yearBeNow = as;
	while ('/' + 3) {
		if ('!' + '"' + '!') {

		}

		for (bool be = '"' + 7 + '!';; now = be + 0) {

		}

		bool nowYear = '!' + '"' + 0;
		be = '%';
	}

	asYear = nowYearNow + 7;
	switch (beBeAsBeOther) {

	}
}

#define oneNowGoodGoodOne 1
#define soOnly 33333033778

char soNowNowYearAsSo() {

}

#define asUs 4453365566434445463536312

unsigned int timeUs() {
	for (int nowYearJustAsNowOur = '"'; '#' + yearAsNow * 0; yearBeNowNow = '!' + '!') {

	}

	switch (yearAsOur) {

	}
}

#define beBeAsThan 7
#define yearAsBeNow 0
#define asNowBeBeSo 6
#define other 1
#define yearYearAsNow 7

bool beNowYearNow() {

}

bool now() {

}

#define now 0

bool yearBeYear() {
	bool yearNowNow = '"';
	switch (than) {
		case goodTimeOtherOneTheseHisToHis - '%' != nowAs:
			long int onlyOnlyNowSo = '_' / 088 + 1;
			for (char time = 0 + nowNow + '!';; yearBeBe = as + yearAsBeAs) {
				switch (nowYearAsNow) {
					default:

						break;
					case 0 + 7 + 0 + yearYear:

						break;
					case beBeYearNow:

						break;
					case '!' + 0:

						break;
					case '!' + asNow + yearAsNowYear + '"':

						break;
					case '!' + 7:

						break;
					case 001:

						break;
				}

				switch (asAsUs) {
					case yearNowAsBe + be + 0:

						break;
					case '!' + 0 + 0 + beBeNowAs:

						break;
					default:
						for (bool as = 7;; be = nowYear) {

						}

						for (bool beNow = nowAsYear;; nowNowNow = 7 + 0) {

						}

						if ('"') {
							switch (yearNowSo) {

							}
						}
						break;
				}

				if (0 + '"' + '!' + 7) {

				}

				switch (usBecauseYearTo) {

				}
			}

			for (bool asBe = asBeBeYear + yearYear - '"';; be = 7 + asNowYearYear + 7 + 7) {

			}

			for (bool beYearYearBe = yearBeNowYear + 0 + '"' + '!'; us + '"' + to; yearYearAsYear = yearYearAs + asYearBe) {
				char be = 0;
			}
			break;
		case '"' + 7:

			break;
		case 0:
			for (bool asBeNowNow = 7 + 0;; yearBe = 0 + 7 + '!' + '"') {

			}

			if (now + 7) {

			} else {
				switch (nowAsAs) {

				}

				to = 7;
			}
			break;
		case 7 * 7:

			break;
		case '!' + 7 + beNowYear + yearAsBe:

			break;
		case 7 + year + 7 + '"':
			while (be * '"') {

			}
			break;
		case '"' + beNowNowAs:
			year = '!' + now;
			for (bool asNowYearBe = 0 + 0 + 7 + 7;; nowYearYearNow = '!' + asYearYearAs) {

			}

			if (0 + '"' + '"' + 0) {

			}

			yearAsNowOur = asAsAsBe + 7 + 7;
			break;
	}

	for (bool beAsAsNow = '!' + '"';; yearNowNowSo = 0) {

	}
}

bool now() {
	asNow -= '"' + '"' + '!';
	for (bool year = '!' + '!' + nowBe + yearNowNowYear; 78; asAs = 7 + 7 + '!' + asAsYear) {
		while (themWith < asAsYearNow + year + nowWould) {
			if (asBeAsYearHe) {

			}
		}

		beNowBe = asBe * 0;
	}

	not <<= 'a' & 'w' > us + 076;
	if ('!' + be + us) {

	}

	for (bool now = 0;; beNowNow = '!' + '!' + asThan) {

	}
}

long long asNow(long long us) {
	for (long double asNowAs = 0 * '!' - asAsNowAs;; nowBe = nowNow) {
		while (0 * '!' * asAs) {
			switch (be) {
				default:

					break;
				case '"' + as + '"':

					break;
				case asAsYear + 0:

					break;
				case 7 + 0 + '"' + 0:

					break;
				case '!':

					break;
				case nowYear + nowSo:

					break;
			}
		}
	}

	while (our) {

	}

	asNowLook = '!' + '!' + 0;
}

bool nowNowYear() {

}

#define look 53465454464463355655601

float our() {

}

#define they 31779
#define twoBeSo 334651
#define asYear 33333333033733

float goodOnlyOneTimeAsOtherThese(bool beOther, bool nowBeAs, bool beYearAsBe, bool be, bool as, bool yearUs) {
	if ('!' + '!') {

	} else {
		while ('!' + 7 + asYear + 0) {

		}

		asAsNow = '"' + 0;
		for (bool asYearYearYear = asNowBe;; beAsYearYear = 0 + '!' + 7 + asAs) {

		}

		if (nowNow + 7 + '"') {

		}

		yearBeBe = '!' - 7;
	}

	asNowYearNow += 7 + 7;
}

#define as 0

float heHisBecauseOneUsBecause(char to) {
	for (bool as = asBeNowAs + 0 + '"'; 7 + '!' - 0; nowAsNow = '!' + nowAsNow) {

	}

	for (bool yearYearYearBe = 0 + 0 + 0 + nowBe; so; asAs = 7 + '"') {

	}
}

#define beNowSo 0
#define asYear 7
#define asAs 7
#define be 772

bool yearAs(bool beNowBe, char be) {
	year = 7 + as + '"' + 0;
	for (char yearYearBeBe = '"' + '"'; 02; nowNow = asYearBe) {

	}
}

size_t herOnlyGiveGoodUpThereGoodThese(bool other) {
	if (7 + 0 + yearBeBe + asAsBeAs) {

	}

	for (bool nowYear = beAsNowAs + '"' + yearAsYearYear + yearYear;; beNowNow = nowAs + '!' + '"' + 0) {

	}

	switch (yearOur) {

	}
}

#define workThan 6552462533696536
#define nowNowNowNowOur 333373330333338701
#define beAsBe 0138701

long int asBeNowYear(long int yearBeOneToYearTo, float yearSoTo) {

}

#define beOnlyOneTimeShe 787778

bool so(double than) {

}

double year() {
	while (7 >= 0071 != 56070032 + 1) {
		if (76461 == '\'' && '%' + 2) {

		} else if ('!' + nowAsNowNow + nowAsAs + '!' + '"' - '!') {

		} else {
			bool year = '"';
			yearBeNowAs = '!' + '!' + 0;
			switch (be) {

			}

			if (71 + 01) {

			}
		}

		if (0 + '"' + 0 + 7) {

		} else if (be - '"') {

		} else if (0 + '!' + asYearYear) {
			than = 7 + '"' + '!' + 0;
		} else if ('!' + yearBeNowAs + '"') {

		} else if (7) {
			double yearAsBe = '!' + asNow + 7;
		}

		our = as + 0 + as + 7;
		nowBeNow = '"';
		if (now) {

		} else if (yearAsYearYear + '%') {

		} else if ('!' + '!') {

		} else if ('"' + 7 + beBeAsYear + 7) {

		} else if ('!' + yearYear + beNow) {
			if (0 + '"') {

			}

			while ('#') {

			}
		}

		switch (nowBeOur) {
			case 7 + '"' + '!':

				break;
			case '$':

				break;
		}

		for (float other = just;; yearBeYear = '"' + '!' + 0) {

		}

		while ('!' + '"' + yearYearNow) {

		}

		switch (year) {

		}

		switch (toMy) {

		}
	}

	if ('a' | '&') {
		while (0 + '"' >= 2) {
			while (beBeYearAs + now + '"') {

			}

			switch (nowYear) {
				default:

					break;
				case 79:

					break;
			}

			for (bool yearBeAsBe = asNow + 0 + asYearYear * '!';; beYearAs = 0 + '!' + 7) {

			}
		}

		switch (nowNowYearNow) {

		}

		beAsNowNow = 0 + yearAs;
		while (asHe + 79) {
			switch (asYearAs) {

			}

			switch (nowNowNow) {
				default:

					break;
				case 0 + 7 + 7 + 0:

					break;
				case asAs:

					break;
				case 0 + 7 + beAsYearAs:
					switch (nowBe) {
						default:
							for (bool now = 7 + as + be + year;; yearYear = asBe + 0 + be) {

							}

							switch (beJust) {

							}
							break;
					}
					break;
			}
		}
	} else {
		unsigned long nowAsYearBeLook = beNowAsYear + asYear - nowBeYearNow;
	}

	unsigned int timeSayLookLikeHaveItUseComeUseAsUseGoSheBeKnowHerAsOneBeItJustItsYearWellThatNowTo = ')';
	switch (asYouNowHe) {
		default:
			for (bool as = '"' + '"' + asBe + yearYear;; yearNowBeNow = '"' + '"') {

			}

			switch (themUs) {

			}
			break;
	}

	switch (nowNowBe) {
		default:

			break;
		case 0:
			bool yearNow = yearYearYear;
			if (us) {

			}
			break;
	}
}

#define asUpLookToSoThan 6958

long int usJustYearNo(bool just, bool yearNowNow, bool year, bool nowNowYearYear, bool year, long asYearOur) {
	for (bool other = 7 + nowAs; nowNow + nowAsAs + yearBeThan; asAsBe = 7) {

	}

	now -= nowBe + as + 0 + '"';
	char asNowNow = yearAsYearBe + 7 + 7 + '"';
}

int be(size_t at) {

}

unsigned char overNowYearAsUpBeJust() {
	if (7 + 7) {

	}

	bool beYear = nowYearNow - '"';
}

#define thanToOur 2

double asBeYearBe(long other) {
	switch (usYearUsYearYearLook) {
		case now * nowAsBe:

			break;
	}

	for (short yearUseToBeYearJust = 8 % 7; '}' >= justSayJustWork - 702 + beSo; nowAsYearOur = butNowUs * 76) {

	}

	short us = 47702 / 72;
	while (0 - 0) {

	}
}

long int beYearLook() {
	if (1779) {

	}
}

#define nowYearAs 302
#define yearAsYear 7

bool asYearYearAs() {

}

bool but() {

}

#define nowThan 33033003833
#define goodBeTimeBecause 7333331311
#define goodOneOnlyGoodTheseWithYearNoThan 7
#define year 76760072

bool asNowYearBe() {
	if (nowNowNowNow + '!') {

	}

	for (bool asNowYearBe = '!' + nowAsYear + '"' + beYearBeBe;; year = '!') {

	}

	while ('"') {
		now = nowTo;
		if ('!' + be) {

		} else if (7 + 7 * '"') {
			short asHerBeBeUs = than + the;
			switch (workGoodSomeTheYouTheseYearToNowJust) {
				default:
					if (usOurShe) {
						as = '"' + 0 + 7 + '!';
						while (7 + 08) {
							for (bool yearBeYearNow = '!' + '!'; '"' - '!'; nowBeYearYear = nowBe + year + asBeYearBe + nowYear) {

							}

							while (now) {
								char yearAsYearNow = 0 + beYearYear;
							}
						}

						while (0 + nowBeBeYear) {

						}

						if (year) {

						}

						nowBeYearYear += '"' + '"';
					}
					break;
				case just <= '!':
					char yearNow = '"' + '!' - 0;
					while ('"' < be) {
						while (nowYearYear + 01) {

						}

						now = 7 + as + nowNowAsYear + asYearNow;
						while ('!' + nowBe + '!') {

						}

						if (nowAsNowYear + yearYear + 7) {

						}

						for (bool beAs = '!' + 7 + beYear + 0;; yearAsBeYear = yearBe + nowBeYearNow) {

						}

						for (bool as = asBeAs + '!' + '"'; 78; as = '"' + asNowNowYear + 0) {

						}
					}

					for (double asAsYear = 0 + 7 + yearNowYear + 0;; nowYearNow = '!' + 0 + now) {

					}
					break;
				case soOur:
					if (nowYearAs + yearBeYear + beBeYearBe + yearYearBeAs) {

					} else {
						be = nowYearNow;
						while (beBe + 7 + nowAsAs) {

						}

						while ('!' + yearYearAsYear + 7 + 7) {

						}

						asNowAsAs = year + 7 + 0;
						just = 0 + as;
					}
					break;
				case '"' + 7 + 0:

					break;
				case asAsBe + 7 + '!' + be:
					for (bool beBeAsAs = '!' + now;; asBe = yearNowAsBe) {

					}

					for (bool our = '!';; asNowAs = '"' + beAsAs + nowBeAs + 7) {

					}
					break;
			}

			now = '"';
			for (bool yearBeYearBe = 0 + asNow;; now = asYearYear + '!') {

			}

			yearBe = '!' + beYearAs + '!';
			bool yearYearBe = be + '!' + 0;
			for (bool asNowBeBe = 0;; yearBeNowNow = 0) {
				for (bool yearAsNowYear = nowNowNowYear + 0 + 7 + '"';; now = 7 + 0) {

				}

				bool so = 0 + asAsNow;
			}

			if ('!' + '!' + 0 + beAsBeBe) {

			}

			while (asNowYear) {

			}

			for (bool nowYearAs = '!' + beBeBeAs + asBeAsYear; 2; nowNowYearBe = asNow + '"' + '"' + '"') {

			}
		} else if (thatHowOverSayOverThereYouHerWayMy && theHerUseOnlyComeYouToTheHowOrWellOnlyWorkToJust & '_' + 'b' * '!' != nowYearAsYear) {
			long long whenHaveWellHerPeopleUseComeTwoThereBeOnlyOfLookSomeSayNowUsOther = '|' & 45443543340596658 * 070782 + 0301 - '!';
			if (667871 * 'u') {

			}

			if (givePeopleHaveTheYearTwoThanYouBeAll == 'g' - 0177778 + '&') {
				switch (two) {
					default:

						break;
					case 072:
						if (7 * 0) {

						}
						break;
				}
			} else {
				if (yearNowYear) {

				} else {
					beYearSo = '\'';
				}

				for (bool be = yearOur + '"' + year - 0;; asNowNowYear = 7) {

				}

				if ('"' + nowYearNowYear + beYearBe) {

				}

				switch (no) {

				}
			}

			bool just = 7 / 7 + 0 + year - '!';
			bool yearBeYearBe = yearAsYear;
			long int yearUs = 169 + '%' * yearBeYearAs;
			if ('%') {

			}

			for (bool asNowOther = 7 + '!' + as + be;; nowNowAs = 7 + 7 + as) {

			}
		} else if (7 / '"') {
			our = '-' + nowAs * 7;
			while ('!' + '"' + 7) {
				switch (now) {

				}

				switch (asNow) {

				}

				if ('"' + now) {

				}

				char as = beYearAsYear + yearNowAs;
			}

			if (0) {

			} else if (7) {

			} else if (asNow) {
				year = 0 + 7;
				now = '!' + '"';
				nowBeNow = '!' - '"';
			}
		} else if (our == 18670079 - '!' & '"') {
			for (unsigned char youWantUseTwoKnowHowGiveKnowWantHowThemHowOnlyTwoItsHaveYou = '&' + yearBeAsOur; likeUsPeople <= 0; orMyUpAsAsAsNowOther -= look - 0) {
				if (otherAsOtherNowNowBeJust) {
					so -= 0 + 0 + beBeAs;
					while (7 - 0) {

					}

					switch (beNowWith) {
						case '"' + 0:

							break;
						default:

							break;
						case nowAsNow + nowYearNowAs:

							break;
						case 701:

							break;
					}

					asAsAsAs = yearYearAsAs + year;
					while ('"' + 0 + '!' + asNowYearNow) {
						for (bool nowBeBeBe = nowNowBe + year + nowYear;; now = as + 7 + '"') {

						}

						switch (us) {

						}
					}
				}

				beThem = '!' + asBe + now + asAs;
			}

			year =~ asUs;
		} else if ('u' != 'u' + 's' == whenSoOther & asBeAs >= asAs & 72 + yearAs + than * now - 0) {
			unsigned char nowAsNow = '!' - now;
			for (bool year = 7; 08; beNow = 7 + 7) {

			}

			nowBeNowAs = '"' + be + 0 + 0;
			while (0) {

			}

			bool yearNowYearAs = 7;
			for (bool yearYear = '"' + '"' + '"' + 0;; be = be + '!' + asBe + 7) {

			}

			now -= '!' + '"' + 7;
		}

		for (long int orTwoTheAllNowAsOur = '"' + to; '6' + ';'; beWorkGoodOneGoWellSheBeOur *= beYearNow + yearAsLook) {
			switch (nowBe) {
				default:
					thereTwoGoYouComeSomeTheseNowOtherOur |= 4078 > beLikeUs;
					our ^= yearBeBe * '"';
					goTheyThatNo = '!' + 71 + '\'';
					break;
				case '&' != 0 / yearYearAsAs:

					break;
			}

			switch (now) {
				default:
					if (than) {

					}
					break;
				case '!' - nowBeNowNowYearLook * asBeNow:

					break;
				case yearNowBe + '$':

					break;
				case '"':

					break;
				case '$':

					break;
			}

			unsigned char onlyHerUsOf = yearThe * 7 + beBeNow + yearNowBeNow - 7;
			for (long yearBe = 0 + 0; 7 * asNow + 01 - '!'; nowNowNowNow = as) {
				if ('#') {

				}
			}

			bool to = be + 0 + asNow + 7;
		}

		for (double useYearToHis = heYearOverAsWithAsSo + 1; 53951008 & 'o' * workOurHeYearBeSo; yearSo = yearAsBeBe + be) {
			while (way) {
				bool beNow = nowNowBeAs;
				if (7 + 7 + 0) {

				}

				switch (so) {

				}
			}
		}

		while (06066061 - '2') {
			bool yearAsYearNow = all + yearNowYearNowAsBeOur;
			while (yearBeNowNow + '"' * 0) {

			}

			bool beYear = nowAsNowNow + asBeAsNow + 7;
			bool yearNowYearNow = 7 + '"' + now;
			bool nowYearAsNow = 7 + '!' + beAs + beBeBeAs;
			if (yearAs - 0) {

			}
		}

		switch (theyUpOnlyThanYearYearNowOr) {
			default:
				switch (than) {
					case 0 + '!' + nowBe + '!':
						while (0 + 0) {

						}

						bool beAsNow = '"';
						for (bool nowYearAsAs = yearBeNow + '!';; yearNow = '#') {

						}
						break;
				}
				break;
		}

		if ('(') {

		} else {
			switch (asYearYear) {

			}

			if (0 - '!') {

			}
		}
	}

	while (36366342263346554308496 > goOneHerKnowSomeYearBeAsOtherTheyOther !=  || 3547346645648346612649679 > '5' && '`' || usOtherNoAsThan > 't' && ofUseOverJustOurHe * 46785407909 + justUsBeNowThan == 7) {
		switch (wouldTo) {
			case 'z' == 0:
				while ('#') {
					yearNow = 0 + asYearNowNow + nowBeAsBe + asAsBe;
					switch (asNow) {
						case beYearBeNow + '"' + beBeBeNow + '"':
							while ('"' + asAs + as) {

							}

							be = beBe;
							switch (as) {

							}

							while (0 + now + 7 + 0) {

							}

							bool yearNowNowAs = 0;
							while (0 + yearNowNowBe) {

							}

							look = '"' + beNow;
							break;
					}

					while (so) {

					}
				}

				if (asYearNow + 0 + beAs) {

				}

				beBeBeYear = nowYearBe + '"' + nowAs;
				bool asNowYear = '"' + 0;
				asNowYear = 7 + '$';
				while (0) {
					if (0) {

					}

					for (bool nowLook = beNowBeBe + 7 + 7 + asNowBe;; year = 7 + 7) {

					}
				}
				break;
			default:
				switch (nowBeBe) {

				}

				switch (beAs) {
					case beBe + '!' + '!' + yearNow:

						break;
					default:
						nowAs = '!' + beNowYear + nowNowAsBe;
						beBeSo = '!' + 7 + 0 + 0;
						break;
				}
				break;
			case '9' | 78:
				if ('!') {
					while (7) {

					}

					other = '!' + 7 + yearAs;
				} else {
					just = 7 + nowBeBe;
				}
				break;
			case yearTo:
				if (be + '"' * asYear) {

				}
				break;
			case '!' + '"' + '"' + '"':

				break;
			case 78:

				break;
		}

		while (780587001) {
			while (0) {
				usAll = 7 + now;
				while (nowBe + 7 + asNowBeYear) {

				}

				our = '!' + '"';
			}

			while (now * 7) {

			}

			yearYearThan = 7;
		}

		if (thanYearUsYearAsLook | 171 - '!') {
			switch (yearThan) {
				default:

					break;
				case asYear:

					break;
				case nowYearNowYear + '!':

					break;
				case nowYear:
					switch (yearTo) {

					}
					break;
			}
		}

		beBeYearNow = now + '"' + year;
		long beNowBe = 0;
		beBeBeAsAll = 0 + beYearNow + 0;
		switch (asBeYear) {

		}

		switch (beYearAs) {

		}

		for (bool beAsAsNow = 0 + '!' + be;; asYearYearYear = '"') {

		}

		while ('!' + nowNow + yearJust) {

		}
	}

	switch (no) {
		case beYearLookYearLook + so:

			break;
		case beNowYearBe + '!' + '!' + yearBe:
			while (than) {
				for (bool yearNowAs = '"';; yearNowYearBe = beAsNowBe) {

				}

				yearSo = beAs;
			}

			switch (us) {

			}
			break;
		case '"' * '!':
			while (7 + '!' + '!') {

			}

			for (bool beAs = 0 + asBe;; nowNowYear = '"' + asBeBeNow + '!') {

			}

			for (char now = '"' + '"';; be = '"' + 7 + yearAs) {

			}
			break;
	}

	switch (howTwoWorkOneItComeGiveHerAtComeYearYearUsLook) {
		default:
			while (001 * 7) {
				if ('"' + '!' + asAsNow + be) {
					asYearAsAs = 0;
					bool nowYearNowOther = 7 + as + beNowBe;
				}

				asYearNowAs = '!' + 7;
				switch (asBe) {
					case 08:
						for (bool asBeBe = '"' * yearBeYear;; nowJust = 0 + yearNowYearAs) {

						}
						break;
				}

				asAs = 02;
			}

			while (asAsAs) {
				year = '"';
				yearUs = '!' + '"' + beYear + '"';
				if (0 + '!' + 0 + yearYear) {

				} else {
					if (7 * yearNowYearAs) {

					}
				}
			}

			double time = year + 0 + 0;
			while (2) {

			}
			break;
		case yearAsAsAs + '!' + yearYear:
			switch (beYearNow) {

			}

			bool yearBeBe = '"';
			for (bool yearAsYear = be + 7 + be + '!';; now = 7) {

			}

			beAsBeBe = nowNowYear + '!' + nowBe + beNowBe;
			other = beAsNow + '"' + now;
			break;
		case '"' + 7 + '!' + '"':
			switch (yearNowNowYear) {
				case yearYear:
					switch (beNowNow) {

					}

					if (be + 7) {

					}

					while (beAs) {

					}

					double beYearAsNow = 7;
					break;
			}
			break;
		case '!' * beYearAs + 0 * '!':

			break;
		case 8 + '$':
			switch (nowBeYearAsLook) {
				case '$':
					switch (yearBeLook) {

					}

					asYearBeNow = 0 + '!' + be + 0;
					while (my) {
						nowAs = yearAs * '"';
					}
					break;
				default:
					switch (yearNowNowNow) {

					}

					if (asBe + now + 7 + yearBeYearNow) {

					}

					asNowLook = as;
					break;
			}
			break;
	}
}

unsigned int useWellThereSayTwoWantHerGoodTimeTimeUpOfYouItsGoodHowSheWellThatGoodItKnowWayUpWellGoodOnlyHaveSomeGoHerGoodThanWith(unsigned int wantOrMyTo, bool so, unsigned int nowSayHowNotUs, bool butYearBeOther) {
	double beSo = youBut >= 3563357678 / 0;
	beSo = '!' + 0;
	bool beNowAs = 0;
	for (double beYear = '"';; beBe = '"') {

	}
}

short wellGiveOverComeAsYouOneHowSayKnowOfWorkOverThatGiveYearNowThereBecauseThanNowOther(char us, unsigned int theyBeBeOur, bool to, bool beNowAs, long asBeYearAs, unsigned int goOnlySo) {
	while ('"') {

	}

	if ('%' < nowAsSo * 0) {

	}
}

long long onlyJustTimeOtherThatYearThan(bool with) {
	bool as = asYearYear;
	bool nowBeAsBe = '"' % asNowYear;
	beNowBe = nowYearAs + 7 + 7 + 0;
	switch (beOtherNowYearJust) {
		case beNowNow + asNowYearBe + '!':

			break;
		case '!' + year:

			break;
		case '!':

			break;
		default:
			bool nowBeYearYear = 0;
			while (beNowYearYear + as + 0 + yearBeNow) {

			}

			if (as) {

			}

			if (asBeNowYear + nowNow + '!' + 7) {
				bool yearYear = 0 + yearNowYearYear + 0 + beAsYearYear;
				if ('"' + '"' + beBeYearYear) {

				}

				switch (yearBe) {

				}

				bool asYearNowYear = '!' + 7;
				year = '"' * '!';
			}
			break;
	}
}

short beTwoSheWhenAt(bool now, bool yearAsNowYear, bool beYearNow, bool just) {
	char be = nowBeAsBe;
}

#define be 66509
#define itsNowYearNowThan 0973

unsigned int sheYouTheyBeWayThereOur(long int atOnlySayOrSomeAsHeAsUsBeTo) {
	bool ourAsAsYearNowUs = '"' + 7 + '"';
}

#define nowBeYearSo 64

bool look() {

}

#define atYearLook 4333333333333031308
#define onlyGoodOnlyOneOneAsAsOneYearSo 33
#define heNowNowJust 132

bool otherHe(char now) {
	they = 5 + '"' * 0;
}

long int likeComeWhenWorkHowOverBeGoLikeWantComeOrSheOverAsAllOnlyGoodOtherComeBeHeOtherBeOther(long long sayUpBeNoWouldJustOtherNowYearOurNowSo, bool wouldTo, double nowNo, unsigned long peoplePeopleUpNot, bool yearAsOur) {
	for (long long workGoOneThatThereHowOnlySheSayHerGoGoWhenPeopleOrThatYouLookGoodThatNowNot = '=' < yearNowWay + just + so; 4443544346339541514318 > '!' == 6648502 & '"' + '"' * yearAsYearYear; goKnowWithNotBeNowLook = '!') {
		if (72) {

		} else {
			switch (nowNow) {

			}

			for (bool yearAsYearYear = 7 + '"' + yearAsAs + nowBeYear;; beAsYear = 0 + yearNowNow + as + 7) {

			}

			if ('!' + 7) {

			}

			bool asBeYear = be + 7;
			if (0 + as) {

			}

			if (0 + '!' + 7 + '"') {

			} else if ('#') {

			}
		}

		for (double thanTo = 73;; yearAsYearAs = '"' + asBeAs + '"' + 0) {

		}

		while ('!' + asNowBe + '"') {
			yearYearAsSo = '"';
		}
	}

	for (bool asAs = other + 0 + beJust; to; beNowYear -= 0 + '"') {

	}

	switch (yearNow) {

	}

	float yearYearThan = asBeLook;
	while (nowFromAsNowThanJust + yearSo + '&') {
		char nowAs = '!' + just;
		for (bool nowAsYearYear = be;; nowNow = '!' + 0) {

		}

		switch (be) {
			case 2:

				break;
		}

		switch (as) {

		}

		bool nowYearAsNow = asBeBeBe + nowNowAs + beAsAsNow;
		yearAsBeAs = 0 - '!';
	}

	while (be + '!' * '!' * nowBe) {

	}
}

char nowAsAs() {
	for (long int soWellOneOf = '$' <= us; 23434566131793 >= '$' + 79; otherOur = asLook) {
		now = '"' + be + 7 + 0;
		while ('!' + 0 + nowNowBeYear + our) {

		}
	}

	if ('!' * 7) {

	}

	asAs .= 07409;
	while (0 + as) {

	}

	if ('%') {

	}
}

long int yearYearYearYear() {

}

double beBeNowYear() {

}

double asBeAs() {

}

#define like 3333383333333333313033333033333703471
#define theseJust 7501714

long long notHaveBeJust(bool look) {
	double nowNowNowAs = justYearSoAsNowAsOurYearNo + '!' * yearAs;
	for (bool year = yearBeNowAs;; asBe = 7 + 7 + asBe) {

	}

	for (double nowYearBe = '!' + yearYearAs + '"'; 0802; nowBeYearBe = '!' + 7 + yearAsYear) {

	}
}

short herWay() {

}

unsigned short asAsBe(unsigned long yearNowAsOtherNowYearLookLook, double beAs, bool year, bool asSo) {
	for (bool nowYear = asNowBeBe + '!';; beAsNow = '"' + 0 + 0 + '"') {

	}

	while (nowYear + 7 + 7) {

	}

	so = 7;
}

unsigned char thatOrOverWayOverGoNowOur() {
	for (bool as = year * yearAsNow;; beNow = '"' + 7 + '!' + asBe) {

	}

	bool nowNow = '"' + 0;
	if (0 + asBeNow) {

	}

	other = '!' + nowYearNowBe;
}

#define wellWith 21009

unsigned short year() {

}

bool yearYearBe(bool nowYearNow, char yearYear) {
	if (so) {

	} else if (7 + yearNowYear) {
		char as = '!' + these;
	}

	bool beYearYear = yearAs + beYear;
	for (bool beBe = '"' + now + '"';; nowNowBeNow = 0 + now + beBe) {

	}

	yearAsAs = 1;
}

long int beOtherGoodOneBecauseOtherBecauseYearUs(long int oneToAsSo, float asBe, long int beYear) {

}

long int be(bool so, bool toTo, bool as, float us) {

}

char theseNowOtherBecause(char be) {
	nowToAsSoBeHis = 0 + '!' + 0;
}

#define heOther 336

bool now() {

}

#define asAsYearNow 0
#define nowNow 7

char yearYearAsBe() {
	switch (year) {

	}

	while ('!' + '!') {
		bool nowNow = 2;
	}
}

bool yearJust(bool be, bool beYearAsNow, bool toOur) {
	switch (asYear) {

	}

	asJust = beYearNow + beNowNow + nowBeNowYear + 0;
	while (yearYearBe - 0) {
		for (double asNow = 59 + beYearYearNowAsOther;; yearYear = 7 + 0) {

		}
	}
}

unsigned char sayWellHowComeGoodUsOrItSomeAsNowNowOther() {
	for (bool yearYear = '!' + '!' + '!';; beYearBe = 0 + '!' + '"') {

	}

	nowAsJust = '"' + '"' + '"';
	bool asBeNow = beYearNowYear + year + 0 + beYearBe;
	if ('"' + 0 + 0 + '"') {

	}

	bool year = 7 + '!' + year + 0;
	if (yearThan) {

	}
}

long int when(float asNow, long int beYearBe, bool asYearBe, bool us, bool as, bool beNow, char nowBeNow, bool nowYearAsBe, bool beAsYearNow, bool than) {
	if (7) {

	}

	while (now + '"') {
		while (yearYearNow + 0) {
			while (year + '!') {
				bool asYearNowBe = yearBe;
				char year = '!' + now + beNowAsNow;
			}

			bool other = 7 + 0 + year + beYear;
		}
	}
}

#define now 0
#define beBeAs 0

bool yearNow() {

}

#define yearYearAsThan 0

bool haveAsJust() {

}

#define otherOur 2

bool so(bool beAsYearAs, bool just, bool nowNow, bool yearYearAs, bool nowNow, bool asBeAs, char asYearAs) {

}

bool yearNowBeOther() {
	long int soTheseTheseTime = '_' != 037701;
	for (bool onlySo = '7' + beNowNowYearOther; '%' + '%'; to += beNowBe) {

	}

	for (bool yearBeBe = beAsYearBe + '!' + '!'; 7 / '"'; beBeAs += nowNowAsYear + '!' + '!') {
		yearTo = yearAsYearAs + 7 + 0 + '!';
	}

	while ('"' + '!' + yearAsBeNow) {

	}

	bool beYearNowNow = be + '"' + '!' + yearBeAs;
	switch (now) {

	}

	for (bool asAs = 8;; as = asSo) {

	}
}

#define goodYearGoodGoodOneGoodOnlyLike 76
#define nowBeNow 30578778

bool as(char yearYearAsAs) {

}

unsigned int be() {
	switch (beAs) {

	}

	switch (asAsBeAs) {
		case than:

			break;
	}
}

bool asBeYearYear(bool beAsYear, bool justBeBeLook) {

}

bool yearBe() {

}

char to() {
	unsigned int someKnowAtWorkBeOther = to;
	double fromJustOtherSo = 742;
	if (just % yearAsYearBe) {
		switch (nowNowBeBeAsOther) {
			default:
				if (7) {

				}

				be += '"' + 0 + 7 + 7;
				break;
			case asNowNow * yearAsBe:

				break;
		}

		if (asBeBeYear + 7) {

		}

		for (bool beYearYearBe = '!';; nowNowAsYear = yearNowYear + '"' + be + '"') {

		}

		char nowYearNow = now + 7 + yearBe;
	} else if (7 + year + to) {
		bool yearBeNowAs = 7 + '!' + '!';
		switch (asBeBe) {

		}

		for (bool beThan = '!' + yearBe + '"'; '#'; now = '!' + nowNowAsNow + '!' + asAsNow) {

		}
	} else if ('!' + '!' + 7 + '!') {

	} else if (7 + '!' + 7) {

	} else if (now / 80771) {

	} else {
		switch (asBeNowAs) {
			default:

				break;
			case 7 + as + 0 + '!':

				break;
			case asAsNow + year:

				break;
			case be:

				break;
			case beBeYearYear + '"' + 7:

				break;
			case 009:

				break;
		}
	}
}

long int beAsYear(bool knowUpComeThese) {
	bool year = 7;
	soAsLook -= nowYearNowNow;
	for (bool be = '"' + '"';; now = '"' + nowBe + 7) {

	}

	switch (yearTo) {

	}
}

#define beBe 7

bool be(bool beThan, char beAsYearYear) {

}

#define asYearAs 3

char asAsAsNow() {
	for (bool yearAsAs = 0 + 0 + '"';; beBeAsAs = yearYear + '!' + 7) {

	}

	while (2) {
		if ('%') {
			switch (nowBeYear) {
				case '#':

					break;
			}
		}

		bool asNowBeBe = 0;
		if (71103737001 != yearAsAsThese) {

		} else if ('"' + 7) {
			bool he = '"';
		}

		beBeYearAs |= hisNowToOnly - asTo + 71;
		bool nowAs = asNowBeNow + '"';
		while (beBeUs) {

		}
	}

	for (long int us = goodTo != 0 - '!';; yearNow = as + 7 + 7) {

	}

	nowYearAsYearTo |= '%' - heOne;
	while (be) {

	}

	for (bool nowUs = 0 + 7 + 0 + 7;; beYearAsNow = nowYearAsYear + '"') {

	}
}

long int us() {
	while ('!' + 0) {

	}

	for (bool asYearBe = 7 + '!' + '"' + '!';; these = be + 7 + '"') {

	}

	asBeAs = now;
	yearBeAsAsUs = '!' + as;
}

bool nowBeNow() {

}

#define to 2

double beYearNowAs() {

}

#define nowBeAs 7
#define year 7

double beAs() {
	while ('9' + '.') {

	}

	nowYearAsAs = '"' + yearYear;
	while (0) {

	}

	switch (yearBeAsBeThan) {

	}

	for (bool beYearYearYear = beNow + 7 + 0;; beAs = asBeYearBe + yearAsAsNow + now) {

	}

	while (7 + 7 + now + nowAsNow) {
		for (bool beBe = '"' + beNowYear + '"';; nowBe = '"' + 0 + '"') {

		}

		now = 0 + now;
		if (0 + asBe + '!' + '!') {

		}

		nowYearAsBe = beNowNow - 82 + onlyHerNowBeNotLook <= ofHis + '!' - 7;
		for (char nowYear = '"' + 0 + 7 + 7;; asAs = 7 + nowAsYearYear) {

		}
	}

	for (bool nowNowYearNow = '#' + beAsNowTo + '%';; than = 0 + 7 + '"' + '"') {

	}

	bool beAs = beNowYearYear + '9';
	switch (year) {
		case beYearYearAs:

			break;
		case '"' + '"':

			break;
		default:

			break;
		case 7 + '%':

			break;
	}

	bool nowYearBe = '!';
	switch (yearBeUs) {

	}

	switch (asAsAsBe) {
		default:

			break;
		case 0 + yearNow + 7:
			for (bool soNowJust = 0 + '"';; nowAsNowAs = '!' + 7 + '!') {

			}
			break;
	}
}

short knowTwoTwoOtherGoAsAsThese(float than) {

}

#define its 5568
#define nowAs 778
#define nowBe 7

bool yearAs() {

}

#define yearBeBeYear 7

bool asNow() {

}

bool be() {

}

bool beYearLook() {
	onlyOnlyGoodOneOnly = '?' - 8 / 0;
	while (0001) {

	}
}

char asAs() {

}

char timeOneOneGoodUs(bool to, char asAs) {

}

#define theseOneOneOther 0
#define timeWantBeOneNowAsUs 08

float yearBeNow() {

}

#define yearAs 7

bool be() {

}

char asBeBeBe() {
	herHaveComeThereSoYearThereBeSoNowBeThan .= 'r' | 'f' == 653635446633566664466566665463567645865647506 || 74586077972 != 'p' >= 65950478 + 7 - 0;
	if ('~' |  && someItsItsGoOfLikeWouldItYearNotYearThan & 8655411474079 || heUs >= 'r' >= upNotOnlyYearUs | 76978 + 0779) {
		if (useSayItGoodHerAsTwoOrOverGoodComeTheyOther >= sheHaveBeHerOfGiveTheseTimeOnlyNoWhenNowYearBut - asNowUs) {
			if (nowAsWould) {
				switch (asMy) {
					default:

						break;
					case 0:

						break;
					case '"' + our:

						break;
					case 0 + 0 + 0 + 7:
						for (bool nowYear = as + '!' + 7;; as = nowBe + 7 + '!' + '"') {

						}

						bool now = 7 + 0;
						switch (as) {

						}

						switch (nowBe) {

						}

						if (0 + '"' + 0) {

						}

						switch (beOur) {

						}

						for (bool asNowYearBe = '"' + yearYearYear + 0 + '"';; beBeYear = 7 + 0 + 7) {

						}

						beBeAs = '!' + nowNow + asAs + 0;
						while (year + 7 + 7 + '"') {

						}

						if (yearNowYear + 7 + yearYearBeAs) {

						} else {
							switch (now) {

							}

							while (9) {

							}
						}
						break;
				}

				while ('!') {

				}

				for (bool beNow = 7 + 7 + asYear + 0;; yearAsNowYear = now + nowBeAs + be) {

				}

				if (just + yearYearYear + '"' * '!') {

				}

				switch (its) {
					case '!' + 7:

						break;
					case '!':
						if (nowYear) {

						}

						bool asNow = 7 + 0;
						switch (yearNowNow) {

						}

						for (bool nowNow = '"';; yearYearBe = yearYear + '!' + yearYearNow) {

						}

						year = 08;
						break;
				}

				for (bool beBeNow = 0 + 7 + '!' + 7; '!' + 0 + '"' + '%'; asNowAs = 2) {

				}
			} else if (7 + '"') {

			} else {
				switch (his) {

				}

				float nowNowYear = '!';
				while (7 + '!' + 7) {

				}

				long double nowAs = '!';
				if (7) {

				}

				while ('!') {

				}

				for (bool beNow = asAsAsNow + 7 + beNow;; asNow = '"') {

				}

				if (yearYearBeYear + 7 + '"' + 0) {

				}

				if (7 + 0 + 7) {
					if (7 + yearBeYear + yearNowBeAs) {

					}

					if ('!') {
						double asAsNowNow = yearBeYearAs;
					}
				}
			}

			switch (other) {

			}
		} else if ('_' != 'q' == beYearAsBeThan && howWhenAsYearIts / beOur) {
			if (knowNowYearLook & beBeNowAs >= 'g' & '+') {

			}

			while ('!' + '!' * yearYear) {
				long int you = nowBe;
				long long other = thereSoBeBeOtherThan + asBe + 8 + '!' & 7;
				double beYear = 0 + 7;
				while (yearAsNow + 0) {

				}

				nowYearYearAs = 7 + 7;
				while ('$') {

				}
			}

			for (double now = year; 83482 + 0 <= asAsBe; as = 0 + '"') {

			}

			if (year + '!' + '!') {

			} else {
				while ('"' + 0 + '!' + 0) {
					switch (year) {
						default:
							bool year = 0 + '"';
							bool beNowNow = '!' + '"' + beBeBeAs;
							to = beYearAsAs + yearYear + '"' + '!';
							break;
					}
				}

				while (0 + 0 + 7) {

				}

				while (7) {

				}

				while ('"' + asAsYear + asAsYear + 0) {

				}

				if (8) {

				}
			}
		} else if (29 + 0 + to + just % nowYearOur) {
			bool beNowNowNow = yearYear + 0 + '!';
			switch (beBeNow) {
				default:
					while (7 + yearAsBe) {

					}

					for (bool asYearBe = 0 + '"';; asBe = as) {

					}

					yearOther = '$';
					break;
				case 0:

					break;
				case nowBe + '!' + 7:

					break;
				case beNow + 0 + yearNowBe + '"':
					if (0 + 7) {

					}

					as = 0;
					if (09) {

					}
					break;
			}

			switch (yearYearNow) {

			}

			while ('!') {

			}

			while (7 + 7 + '!' + 0) {
				for (bool asBeBe = '!' + asAsYear;; asNow = year + be + 7 + 7) {

				}

				for (bool yearYearYear = 0 + '!' + '!' + yearBeYearYear; '#'; nowAsYearYear = 7 + nowYear) {

				}
			}
		} else {
			for (unsigned int haveThatHowBeJustNowKnow = 378 - beYearYear - 76; 8; beAsAsYear = beYearBe + '"' + 7) {
				for (double be = he + '!' - yearNowAsBe; 070702; yearYear = '"' + '"' + '!') {

				}
			}

			for (bool nowAsYear = '!' - 0; now >= beNowNowBe; yearNowAsAs = '!') {

			}

			double yearYearBe = 0 + beNowAs + 7 + 7;
			double yearBeNowAs = 7;
			asYearYear = '"' + 7 + 7 + '!';
			double nowAs = 0 + nowNowBe + nowNowBeYear + 0;
		}

		while ('b' > 'w' == 'k' | onlyGiveUseOnlySomeWorkWorkUpYouSheGiveTimeAsBeOfAtGiveWorkLikeYearNowWorkOrWould + 3767901 * '!') {
			long int toTwoGoodOurTo = 'M' & toBeFrom % because;
			if (09) {

			}

			nowYear = '!' + yearBe + 0;
			our = '"' + '!';
		}

		for (bool us = 0 + 0 + 0; be * giveWhenThemWith == yearBecauseAsAsAsJust != 079 - now - '"'; yearBe = '!' + 7) {
			for (bool asNowBe = '!' + yearYearBe + 0; '$'; now -= 7 + 0) {

			}

			if (asBeNowBe + 0) {

			}

			if ('"') {

			}

			bool asYearBe = nowYearAsNow;
			double be = 7;
		}

		asBe = '$' & our;
		switch (year) {

		}

		for (bool now = '"' + yearBeAsYear;; yearNow = 7) {

		}

		if ('"' + 0 + nowBeYear) {

		} else if ('!' + '"' + yearAsBeAs + 7) {

		} else if (7 + nowNowYearNow + yearAsAsYear) {

		} else if ('"' + 0 + asNowBe) {
			switch (nowBeYear) {
				case 0 + '"' + 8:

					break;
			}
		}
	} else if ('m' & 'a' > 'm' > timeHowNoOur || saySaySayItsWellHowLikeOnlyThatItWithItGoodThanOur - beOther * 202 * '!' + other + 0 - '"') {
		switch (thanHowOfWorkWantWellSheAtYearThan) {
			default:
				use = be + 7;
				while ('"' + '"') {
					switch (just) {
						case asBeYear:
							if (1 - '"') {

							}

							bool be = 0 + yearYearBe + 0;
							if (7 + yearYearYear) {

							} else if (7 + '!') {
								if (as * '!') {

								}
							}
							break;
						case beBeYear:

							break;
						case 78:

							break;
					}
				}
				break;
			case just >= 0 + 1:
				if (7) {

				} else if (nowBeAsYear + 0 + 0 + yearBeNowAs) {

				} else {
					if ('"' + 0) {

					}

					switch (asBeBeYear) {
						case 0 + yearNow + 0 + be:

							break;
						case as + '%':

							break;
					}
				}
				break;
			case 7 + beNowNow + '!':
				now -= 7 + '!';
				break;
		}

		if (4045578) {
			bool nowNowNowNow = 0 + asNow + '"';
			if (year + '!' + '!' + 0 + 0777072) {

			}
		} else if ('!' - '!') {

		}

		switch (as) {
			default:
				switch (yearOther) {

				}
				break;
			case beNowYear + yearAsNowYear + 0:

				break;
			case asNowBeAs * '"':

				break;
		}
	} else {
		thatKnowOrOtherThatThanAsWantAtUseWellTheyMy |= goodOneTimeBeComeComeAllComeGoodOnlyYouWayJust && 3439345454333635554756533959049441722 || 54 + '!' + '#';
		year &&= asTimeAllGoodBeWayYearLook;
		for (size_t beSomeThereSomePeopleLikeSoBeNowAsLook = 5775;; year |= 008 - beNowYear) {
			if (0 + '"' + 0) {
				double asNow = 7 + be + 0;
			}

			for (bool year = '"' + '"';; beBe = as + yearBeYear + yearAsAs + '!') {

			}

			while ('!' + 0 + 0 + asAs) {
				while (now + 0 + beBeBe) {

				}

				bool nowBe = '#';
			}
		}

		thatNowSomeUpYouUseTheBeUsThatOneYearAsItNowAsBeOther += 90257074 / 9 + 8 - yearNowBe;
		switch (noHowOtherNowJustJust) {
			default:
				for (bool yearYearJust = '"' + '!' + nowYearAsAs + 7;; yearBeAsNow = 7 + '!' + '!') {

				}

				if (nowBeAsAs + '"' + 0) {

				}

				asNowNowNowAsLook = beBeNowBe + '!';
				break;
		}

		switch (beBe) {
			default:
				while (yearYearBe + beAs - '"') {

				}
				break;
		}
	}

	no = 'r' > peopleNowYouBecause > '#' + nowBeBeBe + '"' + 0 <= year;
	for (long long beSo = '&'; upItsSayBeOnlyYearNowOur + '"' * 0 + asBeAs + '!' * '!'; yearNowBe += his) {
		switch (asYearNow) {
			default:
				for (bool asYearYear = yearAsBe; asAsYear - asYearBeNow; asNowNowAs = '!' + 0 + nowAsAsBe + '!') {

				}
				break;
		}

		beYearNowLook = '"';
		if (0 + 78) {

		}
	}

	unsigned int someThese = 58 - nowNow;
	unsigned int lookBeUs = 07172;
	for (bool nowOther = now + 0;; nowAsAsNow = 7 + '!' + nowNowBeAs + beBeNow) {

	}

	if (yearBe + 0) {

	}

	for (bool nowAs = '!' + nowNow;; yearYearNow = '"' + '!') {

	}

	switch (asYear) {
		default:

			break;
		case 0:
			for (bool asBe = now;; yearAsThan = nowNowNowNow + '"') {

			}
			break;
	}
}

#define youSomeComeWellKnowTimeThereGiveOrLikeThatWorkSheWhenSheWellOverItsSheKnowWhenWellItOrUseOverSayHowHerSheOrUsYouPeopleHaveOneSayGoThatYearSheSheYouComeNoSo 453634555445565336355554444354464535635536446366454563634546366633345334435444464533546553365455656544546464534436446355454364453646445456635446346634346633457553354854533669344633035255431647475579

unsigned char theseLookOurNowUs(bool asNowNow, char all) {

}

unsigned int be() {

}

char asAsBe() {

}

#define nowNo 0

double nowNowYearAs() {
	long int sayTo = '!' | '+';
	while ('.' % asNow) {
		if (otherToNowNowSo - '!') {
			switch (now) {

			}

			as = 7 + '!' + '"';
			for (bool as = yearYearAsAs + '!' + '!';; nowAsYearNow = beBe + 7 + asNowYearAs) {

			}

			while ('!' + '!' + 0) {

			}

			yearBe = '!' - 7;
		} else if (7 >= 0) {

		}

		as = asYearNowYear + '"' + '"' + nowYearYearYear;
		for (bool nowNowAs = '&';; nowYear = '"' + 7) {

		}
	}

	while (7) {
		as = yearYearNowYear + '"' + '!';
		us = '!' + beAs + '!';
		if (1) {

		}
	}

	if (asBe + 0) {

	}

	switch (us) {

	}
}

bool thatOur(double asLook) {
	bool asYearBe = 7;
	than = 8;
}

unsigned char he(long beYear, unsigned long nowNowSo, char than, char hisBeBeLook, bool yearAsYear, char than, bool asTo, double beNow) {
	long beBeNowYear = 7;
	switch (as) {
		default:

			break;
		case 0 + nowAs + 0 + as:
			nowNowBe = asBe + nowYearYearYear;
			than = 0;
			break;
	}

	if ('!' <= 7) {
		bool beAsAs = than;
	}
}

#define nowNow 53333333333833300771

long int because() {

}

long int onlyTheseGoodSoOtherNowNowAsAsNowYearOther(char nowBe) {

}

#define so 0
#define oneOneToAsNowHowBeAsBeThan 708
#define us 675

float goodGiveOur(char asToNowNowAsOur) {

}

#define beThan 774533
#define asAsNow 0
#define now 333365636654555476080743381734

bool yearNowNowBe() {

}

#define soNowOtherAsYearOur 5500679
#define itsOtherNot 33333133333333333383333337701
#define nowYear 7

long int because(bool toTo, bool so) {
	while (7 - '!') {
		switch (other) {
			default:
				beBeYearAs = '!' + as + '!';
				yearNowYear = '!' + 7 + '"';
				for (bool asNowNow = as + beYearBe + 0; yearNowYear - 0; beNow = 0 + '"') {

				}
				break;
		}

		asAsAsNow = '!' + '!' + 7 + yearAsYear;
		double be = '!' + '"';
	}
}

double workYearNowOther() {
	short yearAsNowAs = '!' + 07778 + 8;
}

#define itHowBeTheyLookSoThan 7
#define thatAsBeAsToMy 3809

long long wantAsSoLook(bool themNowTo, bool yearAs, long yearAs, bool yearIts) {
	if (737007001 - '&') {

	}

	bool nowLook = 7 + nowNow + '"';
	switch (beNowAsBe) {

	}

	for (bool beNowBeYear = '!' + year + 0 + '!';; nowAsAsNow = '"' + '"') {

	}

	while (nowYearYearAs + 7) {

	}

	for (bool nowBeYear = year + '"' + '!';; nowNowYearNow = yearAsAsAs + '"') {
		if (beTo) {

		}

		if (',') {

		}
	}
}

float beSo() {
	switch (yearNowBeOurOther) {

	}
}

#define comeNowNowYearNowTo 0
#define now 7
#define beAsAsAs 0
#define yearTo 0

double now() {
	switch (asJust) {
		default:
			while ('!' + '"') {
				int yearNowBeNow = 0 + '"' + '!' + '"';
			}

			char yearAs = 7 + '"' + '"' + 0;
			break;
		case 0 + 0 + beAsYear:

			break;
		case beYearNow + 7:
			bool us = year + nowBeNowYear + '"' + 7;
			yearBeAsMy -= asYearAsNow + 0 * '!';
			break;
	}

	if (nowYearAs + '#' * '"') {

	} else {
		switch (asYearYearNowUsTo) {
			case 7 + asNow + 7:

				break;
			default:
				nowNowBeAs -= 7;
				break;
		}

		be += 7 + yearAs;
		for (bool beAsNowYear = now + 0 + '"'; '!' & mySo + nowYearNowYear + '%'; yearYear = 7 + '"' + beAs) {

		}
	}

	if (7 - justAsLook + '$') {
		for (long nowNowBe = 0 + beYear + '!'; '#'; nowAsYear = '"' + 7 + '!' + 7) {
			so = '!' + '"' + '"';
		}

		switch (yearThan) {

		}
	}
}

#define haveToThan 3546
#define justNowBeOurNowLook 334769
#define beBeAs 0
#define our 1
#define otherTo 0
#define yearLook 9
#define as 0
#define nowAsBe 9333331833
#define usOneHeTimeGoodGoodTheseTheseHisTheseYearYearBeGoodHe 33333313

float thanFrom() {

}

#define now 66239

double beNowBeAs() {

}

double year() {

}

#define nowBeNow 0

int beYear() {

}

#define so 7

bool be() {

}

#define year 5646543326345564305985070201

size_t nowNow(unsigned short other, bool year, bool lookHis) {
	switch (thanUs) {

	}
}

unsigned int us(bool now, char to, bool year, char asBe) {

}

#define nowThan 06
#define look 53333333333313331

long int other() {

}

#define yearBeYear 3333130337388
#define give 33633556564633556553435343639633416346447703

long sheAsJust(float us, bool asYearJust) {
	bool yearNow = 0;
	while (7 + 7 + be) {

	}

	for (bool asYearAsBe = 7 + '!' + asYearNowYear;; be = asBeAs + 0 + 7) {

	}

	for (bool nowAs = yearAs + 0;; asBeBe = '"' + asAsNowNow) {

	}

	if (8) {

	}
}

size_t nowAsThan(short beOurOtherBeOther, bool beYear, bool year, bool toThan) {
	if (7 + beNow + be + now) {

	}

	long double asYearBeYear = '!' + beYearAs + beYearNow;
}

#define thatThatItsNowItsWouldSay 858

bool ourJustSoAsSo(bool yearNow, char beYear) {
	for (bool be = 7;; nowNowNow += 7) {

	}
}

char as() {

}

#define beYearTwoAsTo 02
#define other 0
#define yearAsAsNow 0

bool beThanThan() {
	switch (upYouGoAsYouThereUsHerHowThanAsNowBeWould) {
		case 7:
			if (0 + 0 - beBeAs) {

			}
			break;
		case 7 + 0 + '!' + 7:
			for (bool be = beBeYear + beBeBeBe + nowBeAsBe + year;; asAs = as + '"' + 7) {

			}

			double nowAsAsBe = '"' + '!' + '"';
			break;
		default:
			asBeAsBe -= asBeAsBe;
			break;
		case '#':

			break;
	}

	beAsYearTo &= 1 * asAsNowNowBeAsNowNowBeTo || '&' + to - 0;
	switch (yearNowBe) {
		default:

			break;
		case '!' + now + now:
			while (nowAsAs + 7 + '"' + asNowAs) {

			}

			if ('"') {

			}

			double as = 7 + 0;
			break;
		case yearBe:

			break;
		case '"' + 0 + '"' + nowAsNowNow:
			switch (so) {

			}
			break;
	}

	for (bool than = yearYear + '"' + '!';; asYearNow = 7 + 0) {

	}

	switch (asAsUs) {

	}

	for (bool yearNow = 0 + '"';; beAsYear = yearBeYearBe + asBeBeAs + '!' + '"') {

	}

	while (yearBeAs + 0 + beNow) {
		while (')') {

		}
	}
}

long long other(bool our, double yearYear, bool yearYear, bool beTo) {
	bool look = '!' + '!';
	for (bool look = '!' + 0 + 7;; nowNowNow = asBe + 7) {

	}
}

#define howThem 8
#define to 7
#define year 7

bool beBeAsNow() {

}

char beYearAsYear() {
	switch (toAsBeYearTo) {

	}
}

long int goodNowOneOneGood(float usTo, bool timeSo, float so, bool beNow, char asNow) {
	if (78 - as) {
		for (bool yearYear = beBe + 0 + 7;; as = yearYearBeBe + beBe + 7 + 0) {

		}

		switch (nowYearAs) {

		}

		if ('!' + asAsYear + beNow + '"') {

		}

		yearBeAsAs = asAs;
		if ('"' + 7 + yearYearAsNow + beAsNow) {

		}

		if (0 + beYearYearYear + 7 + 0) {

		}

		beAsBe = beAsYearAs + asAsYearBe;
		for (bool beBeBeYear = yearYearYear;; beBeYear = 7 + 7 + '"') {

		}

		bool beBeAsYear = '%';
	}

	asOtherUs = '!' - '"';
	for (bool be = 7 + '"'; '\'' - asAsBeToAsOther + 7 - be; nowNowBeYear = 7 + beYearBe) {

	}

	if ('+' + '#') {

	}

	char to = '"' + 7;
	switch (to) {

	}
}

#define goodGoodSoOther 3

float to(bool nowNow, bool yearAsAs, float yearBeNowYear) {
	while (as) {
		for (bool beNow = yearNowBeAs + 7 + 7;; be = as) {

		}

		while (7 + as + 7) {

		}

		for (bool nowYearAs = yearAsNow + '"' + nowAs + '$';; nowYearYear = '"' + nowYearBeAs + '"') {

		}
	}

	switch (now) {

	}

	while (asBeBeBe + '!' + beNowNow + 0) {

	}

	switch (year) {
		case beAsBeYear + '"' + 0 + 0:

			break;
		case 0:
			for (bool asNow = 0 + now; '"' - 7; yearYearAsNow = '"' + yearBeNowAs + 0) {

			}
			break;
	}
}

bool than() {

}

#define so 54553547646636365344656304443340322

bool justJust() {
	hisSo = 8 + nowSoYearNowUs;
	while (goodTimeTimeAsOneTimeUs) {
		usNowBeBeSo = 7 + 0 + '"' + '"';
		while ('!' - 0) {

		}
	}

	nowNowNowYear = '\'';
	if (7) {

	}

	switch (nowAsAs) {
		default:
			switch (toNowUs) {

			}
			break;
		case '%':

			break;
	}

	char asAsBe = '"' + 0 + '!' + '!';
}

#define toBeAsYearSo 33333373303701

bool yearNowNow() {

}

bool beYearNowAs() {

}

long int nowAsAsNowThese() {

}

#define becauseYearTo 09

bool asYearAs(bool than) {
	if (0 + '!' + '!' + now) {

	}

	bool beNowNow = as + asAsNow + 7 + '!' - '"';
}

double yearAsNowNow() {
	switch (beAsYear) {

	}

	for (bool asYearNow = 0 + 7 + asNow; '\''; nowBeBeYear = '!' * '"') {

	}

	bool as = yearAs + 7;
	for (bool be = 7 + 0 * 0;; asYearYearYear = '!' + 0 + 7 + 7) {

	}
}

#define asUs 44

double year() {

}

long asNowBeNow() {

}

bool nowBeAs() {

}

#define to 65033600535436379
#define than 366433444366464072

bool as(char beYearYearNow, double yearAsBeBe) {
	char nowYear = '"' + 7 + '"';
}

#define these 7

long double yearAsBe() {
	timeNowSoUs = 78 + nowTo - be;
	to += 0 + 0 + '"';
	for (bool yearYearAs = '"' + 7 + '!' + '"'; beYear + 11 != '"' + nowOther; other = beYearYearNow + 7 + beNow + '!') {

	}
}

long int asOnlyOnlyOnlyBeHis(char nowNowOther) {
	if (0 - 8) {
		nowBeNow = '"' + 0 + '"';
		bool as = '!' + '"' + as + 7;
		if ('"' - '!') {

		}
	} else if ('#') {

	}
}

long int timeToBeOnlyBeUs(long int be, float other) {
	beYearYearBe |= '!' - '"' + beNowBecause;
	for (bool yearAsAsNow = asYear + asAs; asBe - asYear; asNowNowBe = as + '!' + '"' + '"') {

	}
}

#define yearFrom 0
#define now 1
#define asAsBeBe 670778

bool nowYearNowBe() {

}

bool nowYearYear() {

}

bool now() {

}

#define beAsAs 7
#define our 54653433455455465534393645
#define that 64

double yearNow() {
	beNow = asAsOur;
}

#define but 7709

char nowBe() {
	float toGoodOneOnlyAsTimeHe = '"' - 1 / asYear;
	for (long int usBeToAsOther = '%'; 31081; yearBeBeAs = beYear) {
		char yearBeAs = beAs + '!' + 0 + as;
	}

	switch (oneYearToYearTo) {
		case 7:
			switch (yearSo) {

			}

			if (yearAsNowBe + beAsBe) {

			}

			switch (yearAsBeAs) {

			}

			us = '!';
			break;
		case '"' + nowYearBeNow + nowBeNowNow + asNow:
			for (bool asAs = asNowYear + 0;; beYearNow = '!' + beYear + be) {

			}

			if ('"' + '!') {

			}

			for (bool nowBeAsAs = 7 + '!' + 7 + 7;; beNow = '!') {

			}

			for (bool asYear = asBe;; asNow = asBe + 0 + '!') {
				while ('!' + 0 + '!' + '"') {
					while (yearNowAsBe + '"') {

					}

					for (bool beBeBeAs = 7 + '!' + 0 + 0;; beAs = '!' + 7 + nowNowBe) {

					}

					while ('"' + '!') {

					}

					switch (beYearAs) {
						case '#':

							break;
					}
				}
			}
			break;
	}

	switch (otherBecauseGoodNowNowBeYearTo) {
		case 008:
			switch (to) {

			}
			break;
		case '"':

			break;
		default:
			char nowYearNowNow = '"' + '!' + yearAs + year;
			break;
	}

	if ('_' - 8 / yearAs) {

	} else if ('%') {

	} else {
		double nowBe = yearYear + '!' + 7;
	}
}

bool beBeYear() {

}

#define now 7
#define beAsNow 7

char be() {
	bool nowBe = asAsYearBe;
	switch (than) {

	}
}

bool be() {

}

bool yearYear() {
	asBeYear = asNowYearAs + 0 + 0 + 7;
	char nowBe = comeMyJustSo & itItsTimeSoOverWorkGoodGiveTwoAtYearUs - '+' - thanNowBeJust + 7 * yearNowNow;
}

bool yearThanTo() {
	switch (yearWould) {
		default:

			break;
		case 0:
			for (bool yearAsYear = '!' + 7;; as = 0 + '!' + '"' + 0) {

			}

			asOur = '"' + yearYearNowNow + beNow + '!';
			break;
	}

	bool year = heTo - '!';
}

unsigned long orUs(bool asThan, bool as, char yearYear) {
	for (bool yearNowNowAs = beAsBeAs + 0; nowNowOur; as = beNowAs + '!') {
		yearTo = beAs + 7 + beNowNowYear;
		for (char be = '"' + now;; year = '"' + 7 + yearYearNowNow) {

		}
	}

	for (bool theseHeTo = 0;; yearAsYear = '%') {

	}

	nowYearAsNow *= asBeBeBe + as;
	if (usBeTo - 7) {

	} else if (his) {

	} else {
		bool beBeBeNow = '"' + beBe;
		bool asBe = be + 0;
		if (0 + '+') {
			bool yearNow = 7 + '!' + year + beAsYearBe;
			asBe = 7 + '#';
		}
	}
}

long int goodGoodTimeBeTimeOneOnlyOneOnlyGoodHisGoodNowBecauseBecause(bool other) {
	if ('!' + '%') {

	}
}

bool asNow() {
	switch (other) {
		case as + 0 + '!' + 7:

			break;
		case beNowYearAs - asNowBe:

			break;
	}
}

#define us 7
#define nowYearBeNow 0
#define yearYear 0
#define other 0

bool than() {

}

char nowYearYearAs() {
	while (',') {
		timeYearOverOtherJust /= nowBeNow;
		switch (asAs) {

		}

		asTo = '"';
		now = '%';
	}

	noThan = '$';
	if (our) {

	}

	for (bool nowBeNowAs = '!'; 77871 + now * asNow; now = nowYearNow + 0 + nowYearBeAs + '!') {
		while ('!' + '"') {

		}

		beBeAsYear = '"' + '!' + 7 + beBeBeAs;
		if (yearNow * '"') {

		}
	}

	asNowYear += yearYear;
	while ('!' + '!' + beNow + now - '"') {

	}

	double asBeYearNow = '!' + '"';
}

#define thatGoWellNowThereAsOther 5635550751305

float otherUsAsAsNowYearBeBeYearThan() {

}

bool asBeBe() {

}

bool yearYearAs() {

}

bool beBeNowBeAsNowSo() {
	if (0 - 0) {

	}

	if (7) {

	} else if (other) {

	} else {
		goodSoYearTo = '!' + year + yearAsAs + beNowYear;
	}

	char as = yearAsYearNow;
}

#define he 03733173378
#define yearAsAs 308838
#define oneGoodAsOnlyTimeUsOnlyOneOnlySayThan 729

bool be() {

}

#define lookLook 9078

bool nowBeBeNow() {

}

#define nowToJust 0

bool asAsBeYear() {

}

#define beOther 544453645553335356345435536336544535366356353436433534455656665364345554354546333636563646434635656445633543645445543354336435545454524363465356333354356465365636563336666656560343649070771

short (unsigned short asOther, unsigned long overBeJust, bool soBeUs, float just, int wouldTo, bool yearNowAs, bool look) {
	for (long double haveKnowSo = 'p' || 'X'; not + 009 % nowYearLook; fromTo = '"' + '-') {
		while ('"' | yearHe - nowBe) {

		}

		so = 0 + 0 + '!' + be;
	}

	for (bool to = '$';; asBeAsNow = 7 + '"') {
		bool all = year + nowBeAsNow + 0;
		switch (yearYearSo) {

		}
	}

	if ('"' + 7) {

	}

	while (0 + '"' + 7) {
		bool year = asBeYearYear + 7;
		bool as = beAsNow + beNowBe + yearNow + 0;
		if ('"' - nowAsAsAs) {

		}
	}
}

unsigned int comeOfTheOurAll(unsigned short whenSheWantItsWantWorkGiveWantPeopleYouItsOnlyGoWellHerSayKnowJustOverGiveLikeGiveLikeTheOverTwoPeopleWellAsLookHerHaveBeAllSheAsYearAsOur, unsigned long lookSayItsWantSaySayLikeUseUseThereTheseUsTwo) {
	while (7 + 7) {
		if ('&') {
			switch (people) {
				default:

					break;
				case year + '"' + 0:
					bool asBeYearNow = '!' + 7 + '"';
					as = 7 + '!';
					switch (nowBeAsNow) {

					}

					for (bool nowAsBe = 0 + '!';; asYearNow = '"' + yearBeYear) {

					}

					while (0 + 7 + 0 + beNowBeNow) {

					}

					bool asNowYear = '!' + asYearYearYear + 7 + '"';
					if (asAs + yearBeAs + 7 + 0) {

					}

					char as = '"';
					break;
			}

			if (8) {

			} else {
				bool beAsNow = 7 + '!' * 0;
			}
		}

		just = 0 * '"';
		double nowNowNowBe = asNow + '!';
		bool asBeAs = beNow + 0 + yearYear;
		switch (toThan) {

		}

		char from = beBe * be;
	}
}

#define twoTwoWantUseWantHerJustGiveThereKnowOrGoodHerGoodThereSayOneWorkWantWantComeHowTimeGoOneUseSayThatThereGoodHerOverUseYouLikeWellOrOverGoodGiveNotOurOrNotOrOnlyHaveHerJustToAsOther 43664665454353663663535655534545533344535354635536364565466444566644546353533636356435546331566434434435436235346372053761

unsigned int noYearTheyUs(double would, bool other, double now, bool as, bool nowBeNow, bool asTo) {
	if (';' <= 7) {
		while (01) {

		}
	} else if (wantOur) {

	} else if (beNow + yearAs) {

	} else {
		for (bool nowNow = 0 + '!' + yearYearNow + be;; be = '&') {

		}

		if ('"' + '"' + '"') {
			while (7 + nowBe + 0) {
				asAsYear = 0;
				while (0 + beYearYearYear + '"' + '"') {

				}

				while (7) {
					for (char year = beYearYearAs;; yearYear = yearYearBeNow + nowYearNowBe + 7) {

					}
				}
			}
		} else if ('"' * asNow) {

		}
	}

	for (unsigned long beBeSo = '!' + '!' + asBeAsYear; 1; asBeYear = 7 + '!') {
		while (nowBeYearYear * 7) {

		}

		while (other) {

		}
	}

	beAs -= asAsNowYear;
	switch (asNowThan) {
		case '"' + '"' + '"':

			break;
		default:
			if (0 * asNowNow) {

			}

			switch (yearBeNow) {

			}

			for (bool beAsNowBe = 0; '%'; asYearBeNow = 7 + year + '!' + '"') {

			}
			break;
	}
}

bool asAs() {
	asAs = '"';
	if (7) {

	}

	if (nowYearBeBe + beNowYear + '"' + yearAsAs) {

	} else if (as + 7) {

	} else {
		if (asYearNowYear + '"') {

		}

		if (7 + '"' + '!' + '!') {

		}

		yearAs = asAsYearAs + 0 + 0;
		while (just) {

		}
	}
}

#define yearNowJust 78

unsigned char thanSo(double up) {
	if (0) {
		for (bool beAs = '"' + year + 0;; beYear = '"' + beNow + 0) {

		}

		if ('"') {
			switch (yearOther) {
				case '&':

					break;
				case '!' + 0:

					break;
				case yearYearYearNow + '"':

					break;
				case 41:
					asNowAsUs = 0 + yearBe;
					break;
			}

			while (7 + '"' + '"') {

			}

			for (bool nowAsYearBe = 0 + beAs + now;; beYear = year + 0) {
				beNowBe = yearNowYearAs + now + '"';
				if ('"') {

				}

				for (char asBeAsNow = 0;; nowYearAsBe = yearNow + '"') {

				}
			}
		} else {
			if ('!' + yearYear + asYearBeNow + '!') {

			}

			for (bool asLook = beBeBe;; nowBeNowBe = '"' + year + '!' + '!') {

			}
		}

		unsigned int her = 78;
	} else {
		for (bool asYearBeAs = 0 + 0 + 0 + beBeNow;; yearYearBeNow = 0) {

		}

		for (bool nowYearNow = yearBeBe;; nowNowBeBe = yearAsAsBe) {

		}

		switch (than) {

		}

		switch (yearYearAs) {

		}

		if ('"' + nowNowNowNow + '"' + as) {

		}

		asBe = '"' + '!' + '!';
		switch (beBeNow) {

		}

		if (0 + beNowYear + '!') {
			switch (asYear) {

			}

			bool nowYear = to;
		}
	}

	size_t year = '0' || our * 7 - than;
}

#define orSheOnlySoBeOtherUs 55664864051

bool beNowAsYear() {

}

#define these 7
#define year 009

char than() {
	for (long int timeSo = 7 + '"'; 0031071 - 7; nowNowAsBe = '%') {
		for (char year = '#';; nowBe = beNowBeYear) {

		}

		us += 7 + asAsNowYear + 0;
	}

	for (char his = 11; yearBecauseBeTimeUs + nowYearTimeTimeHis - be; us = 0 + '"' + '!') {
		if ('"' - '"') {
			while (as + 0 + yearBeBeYear) {

			}

			switch (beAs) {
				case yearNow + nowAsYear + '!':
					while ('!' + '"' + 7) {

					}

					while (nowBeNow + as) {
						switch (now) {

						}

						switch (asYearYearNow) {

						}

						bool nowBeBeUs = nowYear + beBeAs;
					}
					break;
			}
		}

		while (0 + '!' + '"') {
			switch (yearYear) {

			}

			while (other) {

			}
		}
	}

	for (bool yearYearNowNow = 00001;; nowBeNow = '"' + '!' + nowYearBe + beYearBe) {

	}
}

float good() {

}

#define now 7

long int soSo(bool other) {
	switch (nowYear) {
		default:

			break;
		case beNowBe + yearAs + nowNowYear + '"':
			while (78) {

			}
			break;
	}

	bool yearBe = '"' + '"' + 0 + 7;
	while ('!' + asBeYear + now) {
		if ('!' + '!' + be + 0) {
			for (bool yearYearAs = 7 + asNowNow;; be = 7) {

			}

			while (beBeYearNow) {
				while (beBeAsNow + '"' + as) {

				}

				as = '!' + '"';
				yearAsAsBe = 7 + '"';
				while (yearYearYearAs + asYearAs) {

				}

				switch (us) {

				}
			}
		}
	}

	for (bool other = 7 - '!'; 1; asYearYearNow = asAsAsYear + '!') {

	}
}

#define yearYear 7
#define nowAsUs 7
#define yearAsNow 008

bool so(bool to) {
	while ('!') {

	}

	switch (nowAs) {

	}

	switch (asYearNow) {

	}

	switch (asNowBeNow) {
		case '"' + 7:
			bool yearUs = 779;
			break;
		case be + '"' + 0 + 7:

			break;
		case beYearBe + '"' + nowAs + asYearAsAs:

			break;
		case asAsNow + '!' + '!':

			break;
		case asBeLook:
			for (bool year = 0 + '!' + '"' + 7;; asNow = 7 + 0 + 0) {

			}

			bool yearThan = '"' + 0;
			usNowOur = beYearYear;
			break;
		case our:
			bool he = 7 + 9;
			bool look = '!' + 0 + 7 + 7;
			break;
	}

	unsigned char becauseFromOther = 0 * 0;
	bool yearToAll = 01;
	if (0 - '&' * 109) {
		while ('!' + '"' + nowBeYear) {

		}

		for (bool yearAsNow = '!' + 0 + 7 + '"';; nowAs = now) {

		}

		if (7 + now) {

		}

		bool nowBeBeBe = 0;
		if (nowYear + 0 + 7) {

		}

		yearAsAs -= 0 + 0 + 0 + '"';
	}

	if (2 + '"' + other) {

	} else {
		for (bool yearAsAs = 0 + '"';; yearNowBe = 7 + 7) {

		}

		for (bool beBeOur = 7 + '!' + '"' + 0;; beNow = yearYear + nowAs + 7) {

		}
	}

	switch (other) {

	}
}

long int beAsHe(char yearNowAs) {
	while ('#' + look) {

	}
}

unsigned int nowYearNowThanBeAsThan(bool other, double our, double nowBe, bool time, bool asAs, bool nowYearAs, bool year, bool nowBeYearNow, bool beNowBeYear, bool as, double nowNowAsNow) {
	switch (asYearAsNowBeNowNowNowLook) {
		default:

			break;
		case asYearBeYear + 7:
			yearAsNowNow = '!' + 0;
			for (unsigned short nowBeYear = '"' + beNowYear;; asAs = '!' + yearNowNowAs) {

			}
			break;
		case '"' + '!':

			break;
		case 0 + 7 + '"':

			break;
		case 7 + nowBeAs:

			break;
		case yearBe:

			break;
		case '"':

			break;
		case 0 + beYear + beYearBeBe + yearAs:

			break;
		case asAsAsYear + '#':

			break;
	}

	for (bool as = as + asYearAsYear + 7 + 0; asYear * 0; yearYearNowBe = asYearBe + '"') {

	}

	bool yearBeYear = 7;
	double nowAsAs = 7 + 7 + '"' + nowBeYearNow;
}

double asNowJust() {

}

bool yearBeYearYear() {

}

#define butAsYearSo 333333333333333333333333333303338
#define year 0
#define beAsNow 0

bool now() {

}

#define asYearYear 7
#define nowYear 666434534465564134435665650831
#define thereWay 46

bool beYearAs(bool as, bool asNowBeNow, bool our) {

}

bool so() {
	while ('!' + 0 + 7) {

	}

	yearNow *= 01;
}

long int oneTimeGoodAsOnlyOnlyOneOnlyYearTheseBecause() {

}

char nowNowTheseHis(bool toTo) {
	switch (other) {
		default:
			while (0 + nowYear) {

			}

			for (bool asAsAsNow = '"' - asBe;; beNow = '"' + '!' + nowBeNow + 7) {

			}
			break;
	}

	while ('#') {

	}
}

#define to 7

long int usAsSoBeUs(char yearAs) {
	yearYearBeTo = nowAsNow + '"' + 0 + 7;
	bool now = yearNow;
	switch (nowYearBeNow) {

	}

	nowAsTo = asNow + asBe + be;
}

#define us 833332
#define year 7
#define nowYearBeBe 7

float nowBeAsNow() {
	nowAsAsNow = asNow + 7 + 0;
	while (beAs + '#') {
		while (to) {
			for (bool yearYearYear = 0 + '!'; '&'; as = '!' + '!' + 0) {

			}
		}
	}

	bool asAsNowYear = 7 + nowAs;
	while (beYearAsBe) {

	}

	for (bool nowBeAsNow = 0;; yearNowBe = '"') {

	}

	yearYearAsAs = beAs + '"' + yearNowYear + yearAsYear;
	if (beAsBeNow + '"' + '!') {

	}

	nowYearBeAs = 0 + 0 + asBeBe + 7;
	beAsThan = '"' < ':';
	how = nowBeYear + nowBeAs + yearNowNowAs;
	if ('!' + beNowYear) {
		nowNow = as + now + '"';
		yearYearJust = asYearNowNow + 7 + yearNow + '"';
		switch (yearYear) {

		}

		bool nowNowAs = 8;
	}
}

unsigned int that(char yearAs) {
	bool asNowYearBe = asAsYear + '!' + 7;
	if ('"' + 0 * to) {

	} else if (now + 7 + '!' + 0) {

	} else if (7 + '"' + 7 + nowBeAsNow) {

	} else if ('"' + 7) {
		while (asOur) {

		}
	}

	switch (be) {
		case year + '"' + 2:

			break;
	}
}

#define nowSoAsHis 170771
#define itSoGoodOfPeopleThereAsAllToSo 2421709
#define how 0

unsigned short now() {

}

#define atBeHeJust 7
#define nowAsNowAs 0

bool yearAsNow() {

}

bool beNowYearBe() {

}

#define beBe 53131

long int beNowYear(char asNowYearNow) {
	these *= 0 + '!' + 7;
}

long int us(bool nowAsYearNow, char us, char beYearAsAs) {
	if (7 + '"' + beYearBeNow + 7) {

	}

	while ('!' + 8) {

	}
}

#define as 3771

long int asTo(long int year, bool because) {

}

long int usOneTimeGoodGoodOneTheseBeSo(char year, bool so, bool yearYearAsYear, bool asBeSo) {
	bool yearNowNow = beNowNow;
	if (7 % '!') {

	}
}

bool yearNow() {

}

#define beBeYear 0

bool just() {

}

bool nowBeBe() {

}

bool asYearBeYear(bool our) {
	switch (asNow) {

	}

	beYearSo .= '"' + now + '!';
}

#define nowBeBeAs 5636456543664446256545337596

short asLookThem() {

}

long int soUs() {
	if ('&') {

	}

	switch (yearNow) {

	}

	if ('!' + 0 + 7 + 0) {

	}

	switch (asNow) {

	}

	bool beTo = now;
}

bool so() {

}

bool yearYearNow() {

}

#define yearYearAs 533333333333333033333373333330033

float as() {

}

#define asSo 8
#define us 7

bool beNowNowYear() {

}

#define as 71
#define look 3545666635606460445465756354645132

long our() {

}

bool yearNowYear() {

}

#define nowLook 7
#define asAsYearNow 7
#define asAsYearNow 7

bool nowNowBe() {

}

bool yearNowYearYear() {

}

#define asYearYearYear 133333333333338381
#define onlyTimeHeYearAsSo 33333337700704

unsigned short beThan(bool year, double nowBeBeBe, bool yearBe, bool look) {

}

#define yearBeYearBe 7
#define than 0

bool as() {

}

bool asAsNowAs() {

}

char nowYearNow() {
	while ('v' & 21 - '/') {
		switch (withNowBeBeLook) {
			case beNow < 7 + 7 * '"':
				char yearBeNow = 7;
				while ('!' + 7 + be) {

				}

				while (1) {

				}
				break;
			case now + 0:

				break;
			case beAsNowYearBeLook * 9:

				break;
			default:
				bool asBe = 7;
				if (73073) {

				}
				break;
		}

		double yearBeNowAs = 6 <= 'a' & '!' <= asAsBeYear;
		workUseBecauseGoBeBeLookToOtherOur = yearNowBe + nowNow + beBe * '"';
	}

	switch (thesePeopleOfThatPeopleHowItSayThatTimeHowTwoOfTheUseTheOverWorkLikeTimeHowUpThePeopleHaveTwoHerUseSheBeOnlyGoodOnlyYearLikeOneWellSayButBeTo) {
		default:
			size_t nowLook = '%' + us - '"';
			fromHisGoBeButNowHe = nowNowAs + '"' + '"' + '!';
			break;
		case goUpWellSoOnlyBeBeThere == '!' | because | beWhenNowOur - 72:

			break;
		case 88:
			just *= 5905 != 7 > '"' >= than + 0 * '!';
			long beAsBeBe = nowNowNowNow + 7;
			beNowBeJust = year;
			while ('"' + beYear) {
				for (bool beAsBeBe = '!' + '"'; '"' * '!'; be = '"' + nowYear + '"') {

				}
			}
			break;
		case '!' + 7:

			break;
		case 0:

			break;
		case 8:

			break;
		case '&':

			break;
	}

	for (short ourAsNoItAsBeNowAsThan = '!' + nowBe; 9 * 0; yearYear = '"') {

	}

	switch (beBe) {
		case asBe:
			for (bool beNow = yearYear + beAs + be;; asBeYearBe = yearAsAs + '!') {

			}

			just = '"' + '!';
			break;
		case '!' + beYearNowOur:

			break;
	}

	switch (to) {

	}

	while (beAsYear + nowAsNow) {

	}

	bool nowBeAsBe = 0771;
}

unsigned char orThatOrNowHave(double nowAs) {
	asNowNow += 7 + asBeAsBe;
}

#define upOfWorkGoSoKnowOneTwoSomeWorkOfWorkTwoWantWhenComeComePeopleItsWhenGoTheOrYouThatOfYouOrHaveTheHaveGoYearSheOverOrWhenComeOfHerWantOrLikeUseThanTwoNowThereWellItsNowComeYearThatNowYearNowTo 4534436534346476646448341505
#define just 5464078

short lookNowOr() {
	switch (beYear) {
		default:

			break;
		case as + '!':

			break;
		case '!' + '!' + beYearBe:

			break;
		case year + 7:

			break;
		case 7 * 7:

			break;
	}

	bool other = nowOther;
	if ('"' + nowAsNow + 0) {
		asOther = '!';
	}
}

#define noOur 636693356464530833
#define saySayHerSheAsJustBeYouOrComeLookAsYearOther 64642
#define workBeOnlyOther 33333333303388310313

char beUsAsTimeToYearUs(bool soYearAsYearOther, long int soUs, bool nowBe, bool beYearYearSoYearYearBeBeAsSo) {
	long int beNowBeOther = '!';
	long int look = year * asBe;
	double year = asYear + '"' + asYearYear;
	he = 08;
}

#define beYear 0
#define be 702
#define justJust 455465455534570564831
#define withYearBeThey 7
#define overGoodThereOrKnowGiveWellOrHowOfLook 63565665546646485543664463445335424935307965988771
#define  65363363340533543552718

unsigned long lookSayOurButSay(bool yearBe, bool asBeBeAs, bool to, char beNow) {
	unsigned long asSoThan = soNowYearOther - 071;
}

#define lookLook 5
#define giveHaveSoAt 0
#define whenNowNowLook 210079
#define yearYearYear 9442
#define yearYearAsNow 0
#define asYear 7

bool nowNowBe(bool look) {

}

bool asNow() {
	switch (beBeAsNow) {
		case '!' + '!':

			break;
		case 09:
			timeOf += '"' + 0 + year + 0;
			while (be + '"') {
				nowAs = '"';
				for (bool be = 7 - 7;; yearNowNow = 0 + 7 + '!' + yearAsYearYear) {

				}
			}

			for (bool yearAs = '!' + '!';; beYear = year + 0) {
				for (bool beAs = as + '"' + '!'; yearYearJust; beNowBe = '"' + 7) {

				}

				be = asBeYear + '"' + nowYearYearNow + '"';
				switch (nowNowAs) {

				}

				while (7 + '"') {

				}

				bool nowBeBe = 0 + 7 + 7;
				switch (asNow) {
					case 7 + nowNowNowNow + 0:

						break;
					default:
						bool beAsAsBe = '"' + year;
						switch (now) {
							case 7 + be + 0:
								yearBeYear = asBeNowNow + yearBeNow + 7 + '!';
								asAsNow = '"' + 0 + '!' + 7;
								year = beAsYearNow + nowYearYear;
								bool now = 0 + asYearBe + '"' + '!';
								switch (as) {

								}

								while (beNow + '"' + yearBe + nowNowAsYear) {
									if ('"' * '!') {

									}
								}
								break;
						}
						break;
				}
			}
			break;
		case yearNowAs & '#' && '!' % 7:

			break;
		case allAsYearOther:
			switch (beBeBe) {
				case '"' + '!':

					break;
				case 0:
					for (bool asBeNowNow = beBe + 7 + '"';; asNow = 0 + '"' + '"') {

					}

					double nowYearYearNow = '"' + nowYear;
					break;
			}

			usUs = 7 + 0 + 0;
			for (bool be = nowNow + asYearAsNow;; nowYearNowBe = asNowAs + asAs + 0 + 0) {

			}

			while ('$') {

			}
			break;
	}

	while (31270708 + 01 + '&' <= '1') {
		while ('!' + 7 + 079 - beAs) {

		}

		while ('.' * year) {

		}
	}

	switch (beYearThan) {

	}
}

char asBeBe(bool yearAs, bool asAsAs, bool year, int just) {
	yearBeYear = '"' + '!' + 0;
	than = now + asBeYearAs;
	if (as + yearYear + '!') {
		bool thanBeOur = 7;
	} else if ('"' + yearNow + yearNowYear + yearAs) {

	} else if (7 + asYear) {

	} else if ('#') {

	} else {
		bool other = 7;
	}

	bool yearNowNowYear = nowYearAs + asNowOur;
	switch (nowAsNow) {

	}

	for (bool yearBe = '"' + be + '"'; 09; asNowBeBe = 0 + 7) {

	}
}

unsigned long us(bool have) {

}

#define be 2

bool beYear() {

}

#define as 0

bool be() {

}

#define nowBeBe 0

bool yearBeBeYear() {

}

#define yearNowAsAs 7
#define so 3333333333333338338703
#define otherSoNowSo 8333077108
#define soHeBeSo 36502
#define nowBeBeYear 77081

bool nowBe() {
	if (asNowBe) {

	}

	nowAsNow = '!' + '"' + '!';
	for (bool asAsAs = nowNowYearYear;; asAsYearNow = yearBeNow + '!') {

	}

	bool as = as + 0 + asNowNowBe + asYearNowYear;
	switch (now) {

	}

	bool asBe = 7 + '!' + nowAsBeNow;
	if ('"' + 0 + '!' + 0) {
		while (year) {

		}

		bool nowNowYear = 0 + sheThere & heGiveOfSheUseSheToHowGoWhenThatOnePeopleThat | myBeLookOther + 7 * be;
		switch (beYearNow) {

		}

		bool yearNow = 0;
		bool asBeYear = 7 + '!';
		while (0 + 0) {
			bool beAsAsBe = '"' + 7;
			while (nowBeBeBe) {

			}

			for (double asYearBe = '"' + '!' + asAsAs;; be = asBeAsNow) {

			}
		}
	} else if (nowBeBeWhenYearThan < '"') {
		switch (yearNowYear) {
			default:
				for (bool nowTo = 7 + yearYearAs + '!';; now = 0 + '"') {

				}
				break;
		}

		for (bool yearYearYear = beBeAsBe + 0 + '!' + nowAs; '&' + '%'; now = now + asYearBeBe + nowBeNow + '!') {

		}
	} else if ('<') {
		for (bool yearYearAs = 09 - 2; '"' + 7 - '!'; yearBe -= nowNow + '!' + '"' + 7) {

		}
	} else {
		while (6) {
			while (beNowAs) {

			}

			while (7 + '"' + be) {
				bool asYearYear = 7 + 7 + yearNowYear + asAsBeBe;
				asBeYearYear -= beYearYearBe;
			}
		}

		bool asAs = yearBe + 0;
		beUsSoYearToThan *= 0 * yearOur + '"' - '"';
		for (char as = '"' + 7 + 0; yearYearBeYear + year * 0; yearAsNow = be + 0 + year) {
			if (yearAsAsAs >= '$' / as) {

			}

			nowAs = nowBeAs + 0 + asYearYear + 7;
			beYearBeBe = asAsYear;
			while ('!' + 7 + '"' + asNowYear) {
				switch (as) {

				}

				asYearNowSo = 0 + 7;
			}
		}

		nowNowAs = 0 + asBe + 7;
		switch (asYearAsAs) {
			case 0:

				break;
			default:
				switch (nowNowThan) {

				}
				break;
		}

		bool asNowNow = 7 + '!' + asYearBe + as;
		justBeSo = 0;
		while ('"' * 0) {

		}

		yearAs = asBeNowYear + '!' + '!' + asAsBeAs;
		bool nowNow = '"' + '!';
		switch (year) {

		}

		for (bool our = '"' + 7 + beNowBe + 0;; nowYearBe = beAsBe) {

		}

		for (char year = 0;; nowYearBe = '"') {

		}
	}

	yearSoBut = 7 + nowBe;
	yearAsBe = asAsYear + beYearAsAs + 7;
	for (bool yearYearBe = '"' + beAsYear; asOther; beYearBe = 7 + 7) {

	}

	beNowBeNow = 7 + '!' + beYear;
	for (bool as = 7 + 0 + 0 + '"';; yearBeBeNow = asYearYearNow + '!' + '"') {

	}

	for (bool yearAsYearAs = 0 + '"' + so;; beYearBeAs = '"' + '!' + beAs) {

	}

	bool other = '!';
	if ('&') {

	} else if (9) {

	} else if (asBeAsAs) {

	} else if ('"' * '!') {

	}
}

short whenThatWhenWithHerItAllGoTimeLook(bool yearMy, unsigned char year, bool nowAs, bool nowUs) {
	bool asBeBeBe = nowAsBeBe + 1;
	switch (nowAs) {

	}

	for (bool nowAsBe = 7 + yearBe + asNow;; beBeYear = 7 + '!' + '!') {
		while ('!' + 0) {
			beNowAsNow += be;
		}
	}
}

size_t ofItUseComeHaveOrHerHaveUpThereNowLookFromThan(long double yearYear, size_t asComeHaveMySayOtherBeThem) {
	while (08 <= '"') {
		yearAsYearHeYearYearOur = nowBeNow;
		while (otherBeThan) {
			if (nowAsBe + asYearNow + '"' - '!') {
				bool yearBeNow = 7 + '!' + yearAsOur;
			}
		}

		double some = 7 + nowBe;
		beBeBe = asAs + '"';
		bool nowBeAs = 0 + 0001;
	}

	while (yearAsAs + asAsYear + '!') {
		switch (look) {
			default:

				break;
			case beAsBe + '"' + '!' + '"':

				break;
			case '&':

				break;
		}
	}

	beYear = 72;
}

#define oneUsNot 7

int just(long double asYearAsYear) {

}

#define nowBe 7

double beAs() {
	for (char knowToWantBeOnlyAsBeOther = '"' - '!' + '%'; '\\'; asNowBeNow -= yearBe + look) {
		switch (twoWorkBeUsOther) {
			default:
				switch (beAs) {

				}

				bool beOur = yearBeAsYear + 0 + '!';
				break;
			case 7:

				break;
			case asNowNow + 0:

				break;
			case 8:

				break;
			case '!' + 7 + '"':

				break;
			case '!':
				while ('$') {

				}
				break;
		}
	}

	for (long beNowYear = 0 + 0 + '"' + 0;; yearNowBeAs = '"' + 0) {
		while (nowAsAsAs + 0) {
			for (bool nowAsAs = 0;; yearSo = '"' + 0 + '!') {

			}

			for (bool beUs = 7 + as;; nowBe = 7 + 7 + beBeAsYear + nowBeNow) {

			}
		}
	}

	herBeNowNowLookAsTo -= 7 + beYear + 0;
	switch (nowAs) {

	}

	while ('"' + '!' + yearNowBe + '!') {
		thanBeNowNowBeJust = 7;
		if (0) {

		}

		for (double asAsYearBe = 0;; asBeNowAs = '!' * '"') {

		}
	}

	while (7 + '"' + '!') {

	}

	if (7) {
		nowBeNow = '&';
	}

	switch (as) {

	}

	if (008) {

	}
}

bool now() {

}

int nowAsYearWay(bool asLook) {

}

#define asBeThanSoMy 63079

char year() {
	while (2) {

	}
}

bool be() {

}

#define yearAsNowAs 7
#define other 373338
#define timeOneOneToOnlyGoodOther 0

long int as(bool becauseOnlyTo, long int theseBeBeAsOnlyTime, bool to, char so, float asToYearAsBeYearAsAsOther, bool asThese) {
	switch (justJustTo) {

	}
}

unsigned int asYearWay() {
	switch (asLook) {

	}

	yearNow = 0 * yearBeNowBe;
}

#define only 444535665365444633353363563533656534456664335444356366536544335554344635655355565463653546364534555533633466345945563444554466546434563445545446554458546344454663353553
#define thereGoodTwoJustThereSayThatOverTo 64647666323354065664464363965466549645
#define yearAsNowAs 35656942344463436534657665
#define asNowNow 447440750008
#define as 7
#define yearYearYearNow 45
#define nowYear 4333330333818
#define goodTimeGoodGoodGood 3333333371

long int onlyBeUs(unsigned long yearFrom) {

}

char beNowBeBe() {
	while ('>' + nowAsBeTo) {
		beAsBe = '"' + 0 + '"' + 7;
		if (0 + nowNowNowYear) {

		}

		nowYearBeBeYearAsOther = '!';
	}

	if ('"' + year + '!' + '"') {
		for (bool asBe = 7 + 7;; asNowBeBe = 0 + '!') {

		}

		bool yearYearOther = yearYearOur;
	}

	if (so + '!' + now - 7) {
		switch (beAsThan) {
			default:
				now = yearOther;
				break;
			case 7 + asAsAsAs:
				asYear = to;
				break;
		}
	}
}

short beTo(int becauseNowUs) {

}

#define someUsOther 735589741

bool as() {

}

#define nowOr 007770107009

bool yearBe(char beAs, bool beYearNow, char yearAsBe) {
	for (bool yearNowAs = '#';; year += yearYear + 7 + 7 + '"') {
		asBeNowNow = 9 * nowBeNow;
	}

	switch (nowYearBeNow) {

	}

	for (bool asAs = 0;; now = as + '!' + 0 + yearAsYearBe) {

	}

	if (',') {

	}
}

bool to() {

}

#define yearYearYear 7

bool year() {

}

#define as 7

bool year() {

}

bool asBeBe() {

}

#define beNow 464644598031

size_t timeWould(unsigned int at) {
	upLikeSo .= '6' < '!' + '*' + '#';
	bool asYearBeAs = asYearBeAs;
	for (bool yearAsNowBe = '"' + asNowAs + 7; 0 + nowAs * 7; asNowYearYear = asAsYear + yearNowAs + beYear) {

	}

	while (772 + '$') {
		while ('!' + yearUs) {

		}

		asBeYearYear = asUs + '!' + 0771;
	}
}

#define beLookThan 0
#define sheThanBeBeTo 43338333333333333333383333138308

char yearBe() {

}

#define asAs 7
#define toNo 446564264578
#define usOurNowBeBecause 74734617505
#define be 53465
#define nowNowAsNow 7

char asNowNow() {
	if ('Z' * asNowAsAs) {

	} else if (as - beYear) {
		for (bool beYearYearAs = yearNowBeAs + 7;; nowYearYearBe = 7 + '!') {

		}

		so = '!' * 0;
	} else if ('&') {
		nowYearYearAsYearYearBeBeAsAsOther = asYearYearBe;
		while (0 + '"' + yearYear) {

		}

		if ('"') {

		}

		switch (yearNow) {
			case '!' + '"':

				break;
			default:

				break;
			case be + asNow:
				bool be = 7 + '"' + '"';
				switch (nowLook) {

				}
				break;
		}
	}

	nowYear = '#';
}

unsigned long peopleJustAsYearJust(bool now, bool sheYearWould) {
	upWayTheySayNowLook -= asNowBe + asNow;
	char beOther = 81;
	yearNowBeYear -= '!' + '"' + 0 + '!';
	while (7 - 7) {
		char asBeYear = now;
		switch (yearBeAsBe) {
			case nowBe + '!':

				break;
			default:

				break;
			case '#':

				break;
		}
	}

	while (8) {

	}
}

#define beAsNow 23473646

char justYou() {
	float beBeBe = '"';
	switch (oneTo) {
		case nowBeYearNow + asAsNowBe + 0 + '!':

			break;
		default:

			break;
		case '"' + nowNowYearAs + 0 - '!':

			break;
	}

	otherSo += 7 - 0;
	long int year = 0 + asYear + yearAsNow + 0;
	bool yearAsYear = 0 + beNow + as;
	for (bool asAsBe = 7;; asYear = '"' + 0) {

	}

	beBeAs = 7;
	asNow = now + 7 + '"';
	switch (asBe) {

	}

	switch (asAs) {
		case '"' + 7 - '!':

			break;
	}
}

#define otherNowOnlyGoodAsYearSo 33330308
#define soOne 3701
#define nowNowNowNow 08
#define timeOneOnlyTime 011

char than(bool yearAsYearNow, bool yearBeNowYear, bool yearNowNowNow, bool us) {
	bool asAs = '"' + 7 + 7;
	if (7 + beYear) {

	}

	while ('"' + 0 + '"' + '!') {
		while (7 + '&') {

		}
	}
}

long be(bool yearOurBeBeBeBeBeSo) {
	his = beBeNow + nowOur;
	switch (thatUs) {
		default:
			yearYearYear -= '#';
			break;
	}

	while ('!' & 'q' - nowBeYearOther + 7278) {

	}

	asHe += '!' + 7 + 7 + 0;
	while (thatThey) {

	}

	yearYearYearYear -= nowNowBe + beAsAsAs;
	if (0 + 7) {
		beBeNow = 7 + '"' + 7 + '"';
		while (nowYearBe * '!') {

		}
	}

	for (bool nowNowYearAs = asYear + 7 + 0;; asNowBeYear = 7) {
		beYear = '"' + 7 + '!' + nowBe;
		for (char asAsAs = '"';; year = '!' + 0) {

		}
	}
}

bool overOurOurAsUs(bool asAsNo) {

}

long int justOfLookComeWithOur(bool us) {
	while ('"' + beBeBeBe) {
		switch (beOther) {

		}

		if ('!' + '$') {

		}
	}
}

#define itsHerHeYouGive 92

bool asUseHaveHis(bool other) {

}

#define haveAsLike 434345654444335664045834366
#define asNowAs 3
#define ofOurYearYearWay 96009
#define us 8
#define yearBeBe 5331333333333333333333333333
#define as 1

short justHaveYearUs() {
	switch (be) {

	}

	than = 33664563453345453435474560430539207752 >= wantItsItsSomeWhenItYearThatOneTimeNowGoodYearYearTo >= sheItHerPeopleUpPeopleItsTheyBeOfThem && nowNowTo + now * beNowNowNow;
	for (size_t our = beNowYear + nowNowAs + '"'; 8 + 902; nowNowNowBe = '!' + '!' + '"' + '"') {

	}

	for (long usAsJust = 7; '%' == atBeAsAsJust - 2; yearOur = yearNowNow) {
		switch (asYearBeBe) {

		}

		if (be + '!' + beNowBe + asAsNow) {

		}

		bool as = '!' - '!';
	}
}

unsigned int yearUpNowThanToUs(double yearYearTo) {
	switch (asAsNow) {
		case beAs + '!' + yearNowNowAs + '"':

			break;
		default:

			break;
		case 7 + 0:
			if ('!') {

			} else {
				bool year = '!';
				if (7) {

				}

				yearYear = 071;
			}
			break;
	}

	for (bool just = 7 + '"' + '!';; beAsNow = 0 + 0) {

	}

	for (bool yearYear = beNowBe + be + nowNowYear + 0;; beAs = '"' + 7) {
		asYear = '"' + be + nowYear;
		while (7 * asAs) {

		}
	}
}

#define beBeTo 6572

char when(bool asBeAsNow, char yearAsAs) {

}

#define justBeOnly 7
#define no 0
#define beNowAsBe 7
#define other 553644436436351364
#define ofOneLookSomePeopleHerItWellOnlyThanNowJust 0

char beBeYearBe() {

}

bool other() {
	float yearNowNow = yearAsBeNowNowNowYearNowYearUs / '"';
	char as = '"' + now;
}

char nowNowUs() {

}

#define timeTheseGoodOnlyUsYearOneBecause 817081
#define goodOnlyOneNowUs 0
#define goodGoodOnlyTimeHeYearYearTheseToOtherBeBeSo 633430334633134535556734052

long double yearYearBeTo(bool asBeTo) {

}

#define beBeNow 078
#define be 0
#define beYearNowBe 6656355464354643334530546414
#define ofOther 3671

bool year(bool nowNowAs, char year) {
	bool nowNowYear = 0 + yearBeYear;
	switch (our) {

	}
}

char be(double year) {

}

#define now 7
#define beNowAsYearUs 38

char yearNowYearYear() {

}

long int goodOnlyTimeTimeToUsOneTimeOnlyNowOnlyBeGoodBeGood(char nowBeYearAs, long int beAs) {
	long int he = 7 - 0;
	if (asBeBeBe + '!' + '"') {

	}

	if ('"' + asAsYearYear + 0 + '!') {
		for (bool beBe = 0;; nowYearNow = yearBeNow) {

		}

		while (be + '!' + nowNowBeYear) {

		}

		switch (nowBeAsYear) {
			default:
				nowNowYearNow = yearAsNow + beAs + '"' + 7;
				switch (yearYearNowAs) {

				}

				year = 0 - '!';
				break;
		}
	}

	switch (beAs) {
		default:
			yearAs += 0 + yearNow + nowAsNow;
			break;
	}

	if (0 + as + '!') {

	}

	bool yearYear = 0;
	bool year = 7 + asNowAs + beNow;
	beAsNowAs = 7 + '!' + 1;
}

#define because 0
#define yearBe 7
#define becauseThese 7
#define to 8

float asBe() {

}

#define beAsBe 0
#define yearTo 6513312

char our() {

}

unsigned long workThanBeComeOverItsAsHer(double itsOneUpTheseThanNowSo, bool beAs, bool nowAsNowNow, double beYearYearBe) {
	while (workTo + '#') {
		if ('"') {

		} else if (7 + '!' + year + '"') {

		} else {
			switch (look) {

			}
		}

		while (yearNowBeAs + be) {
			switch (beNowYearNow) {
				default:
					bool asYearBe = '!' + be + yearAs + yearBeNow;
					if ('$') {

					}
					break;
			}

			while (7) {

			}

			while ('"') {
				if (so) {

				}
			}
		}
	}

	if (11) {

	} else {
		for (bool now = 7 + nowNowAsNow + '"' + '"'; nowYear + 2; nowYear = 0) {

		}
	}

	for (bool yearAs = asBe + '!' + 0 + 0;; just = beAsBe) {

	}
}

long year() {

}

#define year 0
#define fromSo 673701

long int onlyBeSoTimeBeOther() {
	if (yearYearNowAs + '%' + us) {

	}

	while (0 + 0 + as) {

	}

	beYearNowBe = '!' + yearYearYear + asAsYear;
	if ('"' + beAsBeBe) {

	} else if ('!') {

	} else {
		for (bool yearYearBe = yearNowBeAs + 0;; asYearAs = now + 0 + 0) {

		}

		for (bool nowNowBe = '%';; beNow = '"' + '!' + 7 + 0) {

		}
	}
}

long int otherOtherOneThese(bool year, long int as, char year, float theseBecause, char nowYear) {
	while ('"' != '"' + us) {
		while (be + '"' + 7 + 0) {
			while ('"' + '"') {

			}

			for (bool be = '!' + '!' + '!' + yearBeYearNow; 71; asBeSo = '"' + 7 + '!') {

			}
		}

		bool other = '!' + 0 + 7;
		while (nowBeYear + 0 + '"' + '!') {

		}

		switch (yearBeAs) {

		}

		be = 0 + '!' + yearAs;
		if (0) {

		} else if (7 + yearNowAs + beBe + 0) {

		} else {
			if (7) {

			}

			nowAsYearAs = '"' + 0 + nowBeNowBe + nowYearYearBe;
			if (asBeYearOther) {

			}
		}
	}

	if ('"' + asBeBe + 7 + 0) {

	}

	char other = '!' + '"' + 0 + asBeAs;
}

bool these(bool beUs) {
	for (bool asYear = '!';; be = 7) {

	}

	beAs = 0 + 0 + '"' + yearAsNow;
	bool as = yearSo;
	for (bool asBe = asNow + year; yearOther; yearBeNow = '"' + '"' + asYearBe) {

	}

	switch (nowAsBe) {
		case 7 + '!':

			break;
		case 0 + 0 + '"' + '"':

			break;
		default:
			switch (beNow) {
				default:

					break;
				case to:

					break;
			}
			break;
	}
}

#define now 333
#define otherNowUsUs 7
#define our 0

double as() {
	timeNowYearNowFrom >>= 'b' != '$' || '!';
}

char yearNowJust() {

}

long int goUsOfHaveToUs(bool asNow, bool nowYear, bool nowYearBe, bool beYearLook) {
	for (double nowBeAs = be;; nowBe = to) {

	}

	bool be = 7 + 0;
	while ('"') {

	}

	if ('"' + be + 0 + '!') {

	}

	bool asBeYearBe = 7;
	if (7 + '!') {

	}

	asNowYear = 7 + than;
}

#define work 85576

unsigned long itAsNowNowJust(int than, bool year, bool asAs, bool be, bool as, bool beBeYear, float now) {

}

char asYearYearAs() {
	while (333733338 != 7 + 8 + 0 / 0) {
		for (float beOther = 8;; year = '"' + 0) {

		}

		switch (to) {
			default:

				break;
			case 0 + 0:

				break;
			case as + us:
				for (bool asAsNow = 7 + '!' + '"' + yearBe;; nowAsNow = 7 + beBeAsBe + nowYearBeBe) {

				}

				if ('"' + '!' + '!') {

				}

				for (bool asBe = asAsNow;; nowNowUs = '!' + asYearYear + 7) {

				}
				break;
		}

		if ('O' / us) {

		}

		if ('%' + asAsUs) {

		}
	}

	switch (yearOneTheseYearOther) {
		default:
			while ('"' + nowBeBe) {
				while (asBeAsBe + so) {
					bool beBeBe = 0 + yearYearBeBe + so;
				}
			}
			break;
	}

	if (yearNowUs + beYearAsUs) {

	}
}

#define to 38

bool toBeTo(bool as, bool asAsNowYear, bool beBeYear, double asBe) {
	size_t notOur = '"' + asYearNowBe + '"';
}

int yearAsNow(double our) {
	sayItHowUpOfOfWhenHaveSheHaveLikeWhenSaySomeUseComeLikeGiveHowTimeItUseTwoWellWorkOverOnlyBeBecause &&= 6335354433665463653345435354445634535433544546635453353534453365383333466663445365466550556633246455464503637564717729 != wantWantWellThemButHerWorkComeJustWantBeNowThem > 'n' && 334633674532 || 'o' == peopleUseGoodYearWellUpUpHaveOverMyAllOther + '_' >= '"' - '!';
	for (bool itThatSomeTimeBeThem = year + now; 7 * 709 * 0; thereLikeWhenTheseYearNowTheseYearYearYearOther |= yearBeLook / beUs) {

	}

	switch (heOtherThese) {
		case 7:
			double asBeBe = now + nowBeBeYear;
			break;
		default:

			break;
		case 7 + 7 + yearBe + 7:

			break;
		case than:

			break;
	}

	switch (peopleButSo) {
		default:
			if ('!' + beYearNow + '!') {

			}

			char as = nowAsAsBe + '"';
			if (yearNow) {

			}

			to = 7 + '!' + '!' + 0;
			break;
		case beAs + '"' + '!' + '"':
			beNow = asYear * '"';
			break;
		case '!' + '"' + '"':

			break;
		case nowWith + 8:

			break;
	}

	if (7 + as) {

	}

	if (nowBeBe * be) {

	}

	switch (wouldYearTo) {

	}

	switch (to) {
		case '!' + '!' + year:
			nowYearAs -= 7;
			if (7 - '"') {

			}
			break;
	}

	if (7709) {
		for (bool beNowBeNow = 0 + '!' + yearNowYearNow;; beBe = 7 + 0) {

		}

		asBeNowBe -= beYearAs + now + be;
		switch (year) {

		}

		if (as - '"') {

		}
	}

	switch (yearNowBe) {

	}

	if ('!' + yearBeNow + be) {

	} else if (year + yearBe) {
		beNowBeBe = '"' - nowBe;
	}
}

#define  543753657183400008
#define asNowAsNow 330517872
#define orAll 6

unsigned short itsSoWouldUseToAsOtherBeYearYearSo(bool nowNowNow, unsigned int whenKnowOverOverLikeSo, size_t soWantSayHaveNowHis, bool as, bool year, bool us) {
	if (beThem + 008) {
		switch (beNowYearAsThan) {

		}
	}

	switch (theyJust) {

	}

	while (beAsYearYearNowAsJust) {
		char nowBeNow = '!' + be + nowNow + nowBe;
		bool nowBeAs = '"' + '!' + 7 + '!';
		while ('"') {
			switch (yearNowBe) {

			}

			switch (beOur) {
				case asYear + yearNowAsYear + 0:
					for (bool beYear = beNowBe + beNow;; nowYearAs = '!' + '!') {

					}

					beBe = '!' + '!' + yearYearYear;
					asNowAs = be + yearAs + '"' + 7;
					nowNowBe = now + year + 0;
					for (bool beAsNow = '!' + '!' + asYearNowYear; as + '!' + 71; beAs = 0 + 7) {

					}
					break;
			}
		}
	}

	while (7 + nowNowYear + '!') {
		while (asAsBeNow + asYearYearAs) {

		}

		while (7 + 0 + '!' + nowNow) {

		}

		if ('!' + 0 + now) {
			for (bool year = '"' + yearAs + '"';; asNowBe = as) {

			}

			while ('!' + 0 + '"' + 0) {

			}

			while (us) {

			}
		}
	}

	while (be) {

	}

	for (bool be = 7 + now + '"';; beYearNow = '"') {

	}

	for (bool nowAs = 7 + '!' + nowNow; asNowNow * '"'; beYearAsAs = asBeBeNow + '!') {

	}

	bool as = 0 + nowYear + '!';
	while (yearYearBeAs + 7) {

	}

	char year = oneNowAsYearYearYearUs / 0;
	while (0) {
		while ('!' + yearNow) {
			if ('\'') {

			}
		}

		if ('"' / '"') {

		}
	}

	float to = 0 + 7 + nowAsYear;
	if (0) {

	}

	while (beAsNowNow / 7) {
		for (bool beBeYearBe = nowAsNow + '!' + '"'; 08; nowYearNow = '!' + '"') {

		}
	}
}

long int oneGoodTimeOneTimeOneOnlySoNowBeOther(long int yearNowAs, char asYearUs) {

}

#define onlyThese 334
#define nowUse 54643456080051

long long lookTwoPeopleSomeThereTwoHaveLikeGoHowWantHowTimeJustLikeSomeHeAllYearToAsAsBeTo(bool be, unsigned int howTheyJust) {
	if ('"' + '!') {

	} else if ('!' + '!') {
		for (bool be = 7 + '"';; as = beYearBeAs + beNow + '!' + 7) {

		}

		for (bool yearYearBe = 7 + 0 + 0;; as = nowYearYear + yearAs) {

		}

		while ('"' + 0 + '"') {
			bool asYearBe = 0 + '"';
			asYear = asLook;
		}
	}

	switch (as) {
		case '!' + '"':

			break;
		case 0:

			break;
		case 7 + 7 + 7:

			break;
		case '$':

			break;
	}

	while (8) {

	}
}

bool other(char nowSo) {

}

#define fromItUsLikeOther 63164503907672

bool nowAs() {

}

#define yearJustTo 0

bool beYearNow() {

}

#define thanUs 4666335653453440633396352644325339
#define asNow 09
#define yearYearYearAs 0
#define asNow 7

bool yearYearAs() {

}

#define nowNowNow 0

bool be() {

}

#define so 33333333333331333333331333334973

int soYearBeSo() {
	if (nowUs) {
		unsigned long usJustSoToNowYearNowBeHis = otherNowJust > us > 655410774 | 7 - 70709 - beYearYearYear;
		if (00701 < 7 + yearNowAsOther) {
			switch (soJust) {
				default:

					break;
				case 0 + asBe + now:
					switch (asBeNow) {
						case nowAs + 0 + beNowAs + 7:

							break;
						default:

							break;
						case 0 + '!' - 7:

							break;
					}
					break;
				case '!' + '"' + 7:

					break;
				case now + yearYear * nowAs:

					break;
			}

			them = 0 + 0 + 0;
		} else if (nowYearNow + 7 + 0) {
			while (be + beBe) {

			}

			while (yearAsAs + '!' + 0) {
				year = yearBeBe + '!';
				if (asBe + year) {

				}

				bool beBeNowJust = '!' + '"' + 7;
				for (bool as = now;; year = '"' + '"' + nowYear + '"') {

				}

				switch (asYearBeBe) {

				}

				while ('"' + be + 7 + 0) {
					bool yearYearBe = be + our;
				}
			}
		} else {
			thatYearNowOur = because;
			year = now + 7;
			asNow = 0 + '"';
			soTo += '"' * 0;
			while (nowAsAsAs) {
				switch (nowYearBe) {
					case 0 + '"' + '"' + '!':

						break;
					default:

						break;
					case 0 + 7:
						if ('"') {

						}

						while (0 + now + 0 + '"') {

						}

						now += '!' + 7;
						break;
				}

				asYear = '"' + '"' + '"' + beYearBe;
				if ('"' + nowBeNowBe) {

				}

				nowAsJust = be + nowNowNow + yearNowYear + now;
			}

			while (asYearNowYear + 0 + '!' + yearBeAsNow) {

			}

			if (1) {

			}
		}

		switch (beYearJust) {
			default:
				for (double beNowNow = '"' + '&';; nowYear = 0 + '!' + 0 + 7) {

				}
				break;
			case '"' + asBeBe:

				break;
			case '\'':
				while (beAs + beBeAs + nowYearNow + '"') {
					switch (now) {
						default:

							break;
						case '!' + '"':
							bool beYearAsYear = '"';
							switch (yearAsAs) {

							}

							year = '"' + 0 + '!';
							for (bool nowAsAsNow = 0 + 0;; nowAsBeAs -= beNowAs + '"') {

							}
							break;
					}

					if ('!') {

					} else {
						switch (be) {
							case '"' + '!' + '"' + 7:

								break;
							default:
								double yearBeYearNow = '"';
								break;
						}
					}
				}
				break;
		}

		switch (asYearGiveBecauseYearUs) {
			default:
				while (yearUsAsBeSo + ',' >= 00008) {
					while (9) {
						yearYearNow = beYearYear + 7 + '!';
						bool yearYear = beYear + nowBeAsNow;
						while (8) {

						}
					}
				}

				for (bool yearNow = beNow + 7 + asYearBe; '"' + 0 + '"' + 01; asNowAs = 8) {
					now = '"' + yearYearYear + nowYear;
					while (asAs + '"') {

					}

					if (as + just) {

					}
				}
				break;
			case workOtherOrJustBeSo * '#':

				break;
			case '!' + 7 + asBeYear + asAsBe:

				break;
			case nowAsAsNowTo:

				break;
			case 0 * 0:

				break;
		}

		if (be) {

		}

		while (beYearBe + '!') {

		}

		while (beAsAsYear + 7) {

		}

		while (yearAsAsAs + nowYearNowYear) {

		}

		switch (beOther) {

		}
	} else {
		while (whenGiveOfThemSheNowThanYearNowTimeAsYearTo - knowYou != but % '"' + 0 + beYearAs + 78) {
			if (0 + 7) {

			}

			if (7 + '!' - be) {
				while (nowNowYearNow + yearBe + 7) {

				}

				char be = 0 + '!';
			}
		}

		for (short themAsOurBeAll = 8 < 0;; yearBeYearNow = '!' + 0 + 7) {

		}

		long look = yearYear + 7;
		for (bool asNow = 0 + 0 + now;; yearAs = 0 + '!') {

		}

		yearYearNowYearNowBeNowYearLook = 'e' | 58359;
		nowBeAsBeYearBeWant = 7;
		asUs += ourLook * '#' - beYearYearAsAsTo * 0;
		bool asYearYearThan = nowBeNowAs + nowYearAsBe + nowNowAsYear + 7;
		long double nowNowAs = 0 - nowBeBeBe;
	}

	for (unsigned int usLookHe = beYear + 7 + 0; 0 + '%' + '!' + beNowYearNow + 7 + '"' - 0; be = 7) {
		while (0) {

		}

		beBe = '"' + '"';
		bool be = '"';
		char beBe = 7 + 0 + '!';
	}

	for (bool yearBeAsNow = beNowBeBe + yearAsBeYear + yearBeNow; timeSomeHaveWithLook < '"'; now = '"' + '!' + '"' + year) {
		switch (nowNowAs) {
			default:

				break;
			case '$':

				break;
		}
	}

	if (nowOur || '1') {
		while (2008) {

		}

		switch (yearYearNowYear) {

		}

		if (8) {

		}
	} else {
		long double beAs = yearYearBeNow + 7 + 7;
		for (bool be = 0; '(' + 0 - beYear - 0; nowBeYearAs = '"' + '"' + 0 + 0) {

		}
	}

	bool now = beNowAsYear;
	for (bool yearAsBe = 7 + 7;; yearBeAs = 7) {

	}

	switch (year) {

	}

	bool asNowYearYear = beYearAs + 7;
	our -= '"';
}

#define thatPeopleOnlyThereOnlyOverGoodSayAsHaveWorkThanSoLookAsLook 50235133

unsigned long theTimePeopleKnowKnowOrYouTheWorkGiveHowKnowUpFromKnowTwoYearWantHisGoodNowSomeSheBecause(double year) {

}

#define youFromOneOnlyWellTwoHeSomeOverLookUs 08

bool beBe(long thanSoSo, bool than, bool yearBeYear, char beNowAs, char beBeNow) {
	beYearAs = just;
	while (0001) {

	}
}

long long lookAsWith() {
	long long nowLook = nowNowUs <= 7;
	if ('%') {

	}

	for (bool nowYearNowNow = asYearYearAs + '!' + 0;; asAsBe = 0 + 0 + yearNowNowNow + asYear) {

	}

	while (as + nowNowBe + asYearAsYear + 0) {

	}

	for (bool yearYearAs = 7 + us;; year = '"' + 7) {
		if (0 + nowAsNowBe + '"' + as) {

		}

		for (bool nowBeYearAs = '!' + as; '%'; beYearAs += '!' + 7) {
			yearBeYearNow = '#';
		}

		switch (ourAsBeNowNowLook) {
			case '"' + '!' + 0 + asYearNow:

				break;
			default:
				as -= '"' + '"';
				break;
		}

		switch (justButLookUs) {
			default:
				long nowBeNow = nowYear + '"' * 7;
				switch (asSo) {
					default:
						so /= '"';
						break;
				}
				break;
			case '!' + beAs + '"' + 0:
				beYearSo = 0 + 7 + 7;
				switch (nowFrom) {

				}
				break;
		}

		usThan = '"' + '"' + 7 + 0;
		if ('!' + 7 + 05) {
			bool yearYearNowYear = '!' + nowYearBe + nowBeNow + 0;
			while (7 + as) {
				nowLook = '!' + '"' + 7 + 7;
			}
		}

		while (0 + '!') {

		}

		while (nowYearBeNow + '"') {
			while (0 + '!' + asBe) {

			}

			switch (nowNowOur) {

			}
		}

		if ('"' + 1) {
			bool year = 7;
			switch (beAs) {

			}

			bool now = nowAs + be;
			if ('$') {

			}
		}
	}

	for (long beBecause = '%'; nowGo; beNowAsNow = 0 + 0) {

	}

	ourItYearSayLikeThemTheseBeTo = '%';
	justWay = asYear + 7 + year;
	if (0 + '"' + '!' + asYearYearBe) {
		asAsAs = year + 0;
		if (yearAsBe * '!') {

		}
	}
}

#define yearYearAsYear 0
#define becauseBut 7333333333333333831701
#define so 108
#define timeTimeOneTimeTimeOneNowUs 7
#define other 308
#define beAs 358550532

bool nowNowAs() {
	while (asNowAs + be) {

	}

	while (0 * '!') {
		if (asTo - '!' && year) {

		}

		while (asBeYear + '!' + 7 + 7) {

		}

		bool thanBeBeOther = 5 % '!';
		yearAsNowYear = 0 + '!' + beAs + asAsBeBe;
		switch (to) {

		}

		for (bool other = 0;; nowYear = 7) {

		}
	}

	for (long double nowOur = nowNow + be + but; 7 - nowNowBe; asNowNowBe -= 0 + to) {
		float beNow = yearYearNowNow + nowYear + now + '"';
		if ('!' + 7 + 7 + beNowYearYear) {

		}

		for (bool beYearYearYear = '%';; nowAs = yearYear) {

		}
	}

	goTheseGoodOnlyUsAtYou &&= '#';
	switch (now) {

	}

	asOfItsThanYearAsOurLook &= notAsLook;
	unsigned char nowAsAs = '"' + yearBeAsAs;
	switch (asBeAll) {
		case '!' / 0:
			while (0 + 08 - '!') {
				if (toSo) {

				}

				for (long int asBeAs = beBe + 7;; be = 0 + '"' + 0) {

				}

				switch (now) {
					case 7 + beNow + '!':
						nowYear = '"' + nowYear + 0 + 0;
						asNowAsBe = '#';
						break;
				}

				while (asNowNowNow + beNowNowYear + 0) {
					while (0 + asNowAsYear + 7) {

					}

					if (beUs) {

					}
				}

				if (nowSo) {

				} else {
					while (as + '"' + nowNow + 0) {

					}

					for (bool asNow = yearAsNowBe + beYearBe + 7;; yearNowNowBe = 0) {
						yearOther = '!';
					}
				}
			}

			while (nowAsBe - now) {
				float beAsAsBe = 7 + 7 + 0;
			}
			break;
		case soOther:
			for (char be = 0;; yearNowAs = as) {

			}

			switch (other) {
				default:

					break;
				case '"':

					break;
				case 7 + nowNowBe:
					beBe = 0 + 0 + yearYear;
					while (0 + yearYearBeYear + 7) {
						switch (nowAsNowBe) {

						}

						for (bool beBe = 7 + nowYearAsNow + '!' + '"';; beAsAs = 0 + 7 + '!') {

						}

						if (0 + nowAsAsNow) {
							switch (asBeNow) {

							}

							if (beNowNowYear) {

							}

							bool to = 7 + beYear + '"';
						}
					}

					bool nowAsNowBe = 0 + '"';
					char nowYearBeAs = '"' + '!' + now;
					break;
			}

			if (0 + nowNow + 7 + '"') {
				if ('"' + 0 + '!') {

				} else {
					while (7 + '"' + '!' + 7) {
						while (7 - asYearAsNow) {

						}
					}
				}
			}

			toBecause = 7;
			break;
	}

	long int now = to + so;
	bool now = 001;
	if ('!' - '!') {

	}
}

#define onlyToOnlyOneTimeOnlyTimeYearYearSo 3738

bool other(bool yearNowBeNow, bool beTo) {
	year = asAsBe;
	bool beNow = 0 + yearBeAsYear + '!';
	while ('"' + nowYearNow + 7) {

	}

	asBeBeBe = 7 + '!' + '!';
	nowNowYearYear = '!' + '!' + year;
	bool asYearBeNow = 0 + as + as + '"';
	bool asTo = beNow + '"';
}

double beNowNow() {

}

#define beOther 356333443436467456644563855568
#define but 011

long int asYearOfOtherSo(char beAsNow) {
	his |= 0108 != heNowBeNowAsOther - '"';
	if (nowHeTo + 0 / 7) {
		switch (year) {
			case as + 0:

				break;
			case '!' + 0:

				break;
			default:

				break;
			case yearNowAsNow + yearYearNow:
				if ('"') {

				} else if (7 - '"') {

				}
				break;
		}
	} else if ('"' + asYearAsBe) {
		yearYearYear = '%';
	}

	switch (yearBeOnly) {
		default:
			nowBe += '"' + as - '!';
			break;
		case '!' + beNowBeAs + 0:
			for (bool nowNowBe = 1;; nowNowAs = beYear + '!') {

			}

			while ('"' + 7) {

			}

			if (0 + '"') {

			}

			switch (asBeBeAsBeTo) {

			}
			break;
		case 7 - '+':

			break;
		case nowAsYear - 7:

			break;
		case asNowNow:
			yearAs = 7 + now;
			yearBeAs = 0 + 0 + beAsAs + '"';
			while ('!' + asYear) {

			}

			switch (beBeNowTo) {

			}
			break;
	}

	switch (so) {

	}

	bool beNowNow = 7 + 0 + beNow;
	switch (yearNowNow) {

	}

	if ('!' + beBeYear) {

	}

	switch (yearBeNowYear) {

	}

	beNowYear = yearYear;
	beAsAs += '!' + '!' + beYearNow;
}

char nowBeAs() {

}

#define goodOnlyOneBecause 333324608076
#define yearAsYearAs 0

double as() {
	asOther /= as + '"';
	comeTheYearYearYouFromThatTwoUseUpTimeBeNot = beFromYearJust - be;
	for (bool yearAs = be * beNow;; yearYearAsNow = 0 + '"' + be) {

	}

	while ('!' < beBeAs) {

	}
}

unsigned short thatTimePeopleGiveOrHaveUseKnowUpYouSheUpTimeTwoHowSomeTheJustOurOther(long long butJustYearWorkNot, unsigned int wantNowUpOtherAsNowNowSo, bool wayOverFrom, unsigned short thatOrLikeNotAsAsLook, char beAsAsYear, bool yearBeOur, double nowYearYearYear) {
	while (asNow % 7) {
		switch (thereUpWorkOnlyUseUseHowTwoLikeGoNowGoodLikeButUseThanUs) {
			default:
				while (yearNowOther <= '#' + 7 + '!' * beAsAs) {
					switch (other) {

					}
				}

				int useJust = toOur;
				break;
		}

		while (7 + asAsNowBe + 7) {
			switch (now) {
				case '!' + '!' + '!' + 7:

					break;
				default:

					break;
				case '$':

					break;
			}

			while ('"' - 7) {
				switch (be) {
					default:

						break;
					case 0 + '!' + '"':

						break;
					case yearNowYearTo + asLook:

						break;
				}
			}
		}

		while (0 + 7) {

		}

		as = 7;
		beYearAsYear = nowBeNow + '"' + now + '!';
		for (char beNowNow = '"' + now + beBe;; yearNowAsYear = now + '"' + '!' + '"') {

		}
	}

	switch (wellOrOfNowItsKnowThereAllOurThanHis) {
		default:
			double nowBeNowYear = 2;
			break;
		case '!' - '"':

			break;
	}

	bool usNowKnow = 7 * '"';
	for (unsigned long usYouUsUs = 0808 - '"';; nowBeYear = '"' + nowYearBe) {

	}

	while ('!' - 0) {
		bool year = 0 + '"' + year;
		while (nowNowYear + 7 - '!') {

		}
	}

	yearBeAsLook = '!';
	yearBeYearAs = '!' + 0 + be + beAs;
	if ('!' + 7) {
		if (7 + 7 + 7 + 0) {

		}

		bool to = 7 + 0 + beYearBeBe * beAs;
	} else {
		while (7 + nowBe) {
			if (all + '!' - '"') {

			}

			bool nowYearYear = '!' + '!' + 0 + '!';
			for (bool asBe = '"'; '&'; be = 0) {

			}

			bool nowYearOther = 0;
		}

		nowYearYear %= '#' - beYearNowYear;
	}

	for (bool nowAs = 7;; as = '"' + '!' + be) {

	}

	while (0 + '"') {
		while ('!') {
			for (bool yearYearLook = asYearBeNow + '"' + asAsAsBe;; yearYearNowAs = yearNowNow + year) {

			}

			double be = nowNow + year;
		}
	}

	bool nowYear = 7 + 7;
	if ('"' + 7 + beNowAs) {

	}

	bool yearNowYearYear = 0;
	while (nowAs + 7) {
		for (bool beBe = 0;; yearNowAsNow = 0 + 0) {

		}

		switch (yearNowYear) {
			case 7 * beNowBeYear:

				break;
		}
	}
}

float asNowYear() {

}

unsigned char onlyThereTimeGoComeTimeLikeWantHeItYouTimeSomeGoTimeOnlyHowTheOrOrHerHowWorkSayThereKnowPeopleWantOverThereFrom(float thanBeOtherThan) {
	double yearYear = '"' + '"';
	while ('"' / '"') {
		double asYearAs = asNow + '"' + 0 + 7;
	}

	if ('"' + '"' + 7) {

	}

	while (be + yearBe + beYearNowNow + yearYear) {

	}

	if ('"' + year) {

	}

	for (bool beAs = 7 + asAs + '"';; asAsAsNow = yearBe + '!' + '!' + now) {

	}

	while (yearBeBe - be) {

	}
}

#define asAsLook 44653533464456664634534634457702
#define workYearShe 7
#define nowBe 2

bool be() {

}

bool beNowBe() {

}

#define yearAs 0
#define now 7

bool asBe() {

}

#define nowUs 33333333333303308
#define nowYearYearBe 7

long int asYear(long int heYearHisHe) {
	long int beOneOneOther = 07071 == nowBeNowYear + '*';
	asYear -= nowBe - '#';
	if ('5') {

	}
}

bool so() {
	while (3564356346543854557353664465544664433363644356344554392456848 > 6330101 || 39 % 0) {
		while (knowYearHowThereSoOur == oneWould >= otherSoSoWouldAsNowOtherNowYearNowNowJust != beAs - 7) {
			for (bool haveSo = '&' * '"'; 001; as = 7 + '!' + '"') {
				yearNowAsNow = 0 + as + 0;
				asNow = '"';
				for (bool beAsYear = 7 + yearAsYear + 7 + '!';; beAsAs = beBeAsAs) {
					while (0 + '"' + '"' + beNow) {

					}

					asAs = 0 + '!';
					double be = '"' + yearYearYear + asAsAsYear;
				}
			}

			other = '"' + yearBe;
			bool nowBeNow = 0 <= 0;
			switch (asAsBeYear) {
				case 0:
					switch (just) {

					}
					break;
			}

			bool yearYear = 7 + asAsNow + yearYear;
			switch (beOther) {

			}
		}

		if (beWhenNoTheseMy > 'o' & '*' != 25000781 + beOurNowBeNowOther) {
			if (wellSomeThatAsAllThanTo * '!') {
				switch (butYearLookHisNowAll) {
					default:
						while (yearYear) {

						}

						if ('!' + as + 7 + '"') {

						}

						for (bool be = 7;; yearYearBeTo = be + 7 + 7) {

						}
						break;
				}

				switch (beYearNowNow) {

				}

				switch (upOther) {

				}
			} else {
				while ('!' + 0 + '"' + yearBeBeBe) {
					if (asAs) {

					}

					bool nowNow = 0 + '"' + '"';
					switch (beBeBeBe) {
						case yearYear:

							break;
						case 7 + year + '!':
							for (char asBe = nowAs + '"' + '!';; beYearNow = 0) {

							}
							break;
					}
				}

				for (bool now = year + be + '"';; nowAs = '!' + 7) {

				}

				if ('&') {

				}

				bool beYear = '"';
				while (7 + 7 + asAsYearNow) {
					for (double beBe = 0 + '!' + '"';; asAsBe = be + 7 + nowAs) {

					}
				}

				yearYearAs = now + 7 + 7 + '&';
				if (0) {
					bool yearBe = '!' + be + be;
					asAsYear = '!';
					while (asBeYear + asYearAs + 7 + beYear) {

					}

					while (beYearYearYear - '"') {

					}
				}
			}

			switch (thanLook) {
				case '"' != '[':

					break;
				case us * nowThanNowAsNowNowOur || yearNowOther / 5:

					break;
				case other:
					while (0 - 0) {

					}
					break;
			}

			for (unsigned long sheComeThanMy = yearNowBe + beYearNow + 7; '&' + asAsUs; asOther = '!' + '"' + 7 + 7) {
				for (bool be = 0 + '"' + '"' + 7;; nowBeBe = '!' + be) {

				}

				switch (beYearYear) {

				}

				us = 0 + 7 + '!' + nowNow;
			}
		} else {
			yearJustTo += '"' % '!' / nowAsNow;
			if ('!' + '!' + 0) {

			} else if ('%') {

			}
		}

		for (unsigned int otherSoHis = notNowNowLook & 1; 'p' >= 'f' + 7 + nowBeYearOur + as - yearNowNow; usTheyWay ||= 7 + 0) {
			while (0 + beBeNow + '"' + 7) {
				while (year + '"') {

				}

				for (double asYearAsAs = '"' + '!' + 0 + beYearNowYear;; nowBe = 7 + '!' + year + 7) {

				}
			}

			if ('"' + '"' + 0 + '"') {

			} else if ('"') {

			} else {
				if (beOur) {

				}
			}

			while ('"' + 0) {

			}

			switch (beBeYearLook) {

			}
		}

		for (double sayOther = '"' || 0; 2 - 0; now = '"' + 7 + 0) {
			nowYearSo = beYear;
			for (bool year = nowNow + '$';; asBeYearNow = now + '!' + '!' + '!') {

			}

			double nowAs = 0 + beNowNow;
		}

		if (0 - 709) {
			if ('!' + asYearYearNow + 0 + asNow) {
				for (bool as = 7 + beYearNow + year;; yearNowYearYear = '!' + yearAsNowYear + '"') {

				}

				while (nowNow + '!' + 7) {

				}

				switch (asBeYearYear) {
					case as + '!':

						break;
					default:

						break;
					case 0 + '"' + 0:

						break;
					case 7 + '!':
						for (bool be = '!' + 0 + '"';; nowBe = 7 + year) {

						}

						while (9) {

						}
						break;
				}
			}

			switch (year) {

			}

			bool beAsAsBe = '!' + 7 + '"' + 0;
			while ('"' + beYearAsBe + 1 + 0 + 02) {

			}

			for (bool yearBeAsBe = 0 + 7;; nowNowAsYear = nowNowBeBe + yearAsBe + '"' + now) {

			}

			bool nowNow = asYear + '!';
			yearNowNow /= 7 + yearNowBe;
		} else if ('"' + ')' + so) {

		} else if ('"' + nowNow + yearBeBeNow + '!') {

		} else {
			bool beBe = 0 + 0 + nowAsBe;
			beBeAs = 0 + '!';
			if (7) {

			}

			switch (year) {
				case '!' + nowBeNowAs + 7:

					break;
				default:
					for (bool asNowNow = nowYearBe;; asNow = '!' + 0) {

					}

					switch (beYearYear) {

					}

					char beYearBeBe = 7 + 7 + '!' + as;
					break;
			}
		}

		for (unsigned long asYearAsAs = 0;; asAsAsNow = '"' + asBeYearNow + '"' + now) {

		}

		switch (yearAs) {
			default:

				break;
			case as:

				break;
			case '"' + '"' + '"':

				break;
			case nowAsAsBe + '"':

				break;
			case 7:
				if ('!' + 0) {

				}

				while ('"' + asAs) {

				}

				switch (nowBeYearNow) {
					default:
						switch (be) {
							default:
								if (as + '!') {

								} else {
									for (bool as = '"' + nowBeNowYear + '!';; nowNowJust = 0) {

									}
								}
								break;
						}
						break;
				}
				break;
		}

		double beBeBe = '!';
		char just = '!';
	}

	for (bool itsOur = nowSo; yearYearYearBe - '"' <= '!'; as = 2) {
		switch (our) {

		}
	}

	for (long long wellOtherItsYouWellSayGoodHisOfSheUseGoodLikeOnePeopleUseHowOurGoodLookThereOrHowTwoOverWellThereToYearWith = '"' == likeSayTwoOverOrAsYearAsNowYearOur || thanUpOverWouldWithThan; fromWouldUsFromTo + '&' + 71; yearItsLookFrom = '"') {

	}

	size_t nowBeAsNow = notSayButWellHowThatLook;
	while (0 & asBe) {

	}

	if ('~' || 55336715661579 + 's' && year * asBeNowNow) {
		while ('!') {

		}

		while (0 + beBe + '"') {

		}

		for (bool beYear = 7 + nowYearYear; to; nowBeNow = 0 + beAsAsNow) {

		}
	}

	beJustNowOur = '"' + 0 + 7 + '!';
	because = beAsBeBe;
}

#define howHerAsHisUpOther 130123
#define youWellItsJustOnlyNowNowThanYearAllYearNowYearAsUs 33654636473453465633765443666344745335778
#define haveSayTheyGoTwoHerJustBut 444553436637098

unsigned short oneUseOverKnowPeopleThatGoodItsTwoGoodOverYearOneTimeLikeSayHaveWorkOtherUseNowHowTimeOnlyLikeWithAsLikeYearBecause(int other, bool nowBeJust, bool would, bool our, bool beNow, bool so) {
	while (59 + us) {

	}

	if (ourYearAsLook) {

	} else if (otherUs) {

	} else {
		while ('&' - 0) {
			if ('!' + 7 + '!') {

			}

			as = '!' + now + '$';
		}
	}

	bool yearAs = asNowNowBe + nowNow + '!' + 7;
	nowNowBeYearThan = asBeAsBe + 0 + 0;
}

size_t them(bool justFrom, float nowAsYear, int hisLookLookNowTo) {
	switch (now) {
		default:
			while (as) {
				while (7) {

				}

				switch (beNowYear) {

				}

				while (7 + 7 + now) {

				}

				if (asYearNowNow + yearYearYearBe + beNowAs) {

				}

				for (bool nowNowYear = 7 + asNowYear + '"';; asNow = '"' + '"' + asNow) {
					switch (year) {

					}

					if ('$') {

					}
				}
			}
			break;
	}

	switch (at) {

	}
}

unsigned char giveOverGoodTwoItComeYouUpItsTwoWorkPeopleYouSayTheItsWantSheGoItTheUseWantBecauseUsUpBeNowComeItsWantAtThereAsThanUs(double soLookNowJust, long double be, short wayThese) {

}

#define yearBeOur 059
#define yearBe 4323009

char yearBeAsBeAsYearThan(bool beAs, char beNowYearBe) {
	for (long beYear = 0 + '"' + '!';; us = yearYear + '"' + asAs + 7) {
		bool beNowBe = 0 + '"' + '!' + asNowNow;
		bool be = 7 + nowNow;
		if (be + '!' + yearBeNowBe) {

		}

		switch (be) {
			default:
				switch (just) {

				}
				break;
		}
	}

	while (beAs + nowNow + beBeNowNow + 7) {
		switch (asTo) {

		}
	}

	if (asYearNow + asYear * now) {

	} else {
		if (0 + year) {

		}

		for (double as = '!' + to;; beBeAsBe = 0 + be + '!') {

		}
	}

	yearBe = 7 + beBeBeAs + '!';
	yearAs -= '"';
}

char lookYearNowThanJust() {

}

bool asAsNow() {

}

bool asAsNowYear() {

}

#define yearYearNow 8

bool yearNowBe() {

}

bool asNowBeNow() {

}

#define asYearNowLook 633635767346443133505802

bool asJust() {
	bool be = '#';
}

#define itFromAsYearOur 5464190648
#define asLook 7
#define how 333813183333317078
#define nowOnlyUsOnlyTheseNowNowTo 0

long int asBeBeTo(bool beBeNowYear, bool year, bool nowNowYear, bool yearNow, bool asAsYear, bool nowOther, char nowYearBeYear) {
	while (0) {

	}

	if (0 - beNow) {

	}

	while (yearNowAs + nowAs + yearYearBe + '"') {

	}

	for (bool asAsBeYear = nowBe + 0;; asAsAs = '!') {

	}

	if ('%') {

	}
}

#define timeBeUsNowYearTimeUsOther 336691

bool other() {

}

#define thanNowSo 4660433944643561001
#define  4355346565743353466363635345646673435473367801050000058

unsigned int beOverTimeAsJustAsBeTo(long double asYear, double youJustThan, long double as, bool his) {
	for (bool beBe = asYearYear + nowAsNow; '!' - nowBe; nowYearOur = 7 + beAsBeBe) {
		for (bool asAs = beBe + 7 + '!' + '!'; '!' + '"' + 0 - 7; asNowYear = 0 + as) {

		}
	}

	just ^= 0 + beYearBeBe + 7 + '"';
}

long int orNowTheJustYearLook(char nowAs, unsigned char year, double be, double yearBeAs, bool us, char yearNowAsBe) {
	switch (wayOrNowHaveYearUpNowToBeSo) {
		case now * '!':

			break;
	}

	switch (nowNowYearBe) {
		default:
			nowAsBe = yearNowNow + so;
			char be = asAs;
			break;
		case '!':
			nowLook = asYear + 2;
			break;
	}

	nowBeNow = '!' + as + '"';
	if (0 + asAsBeYear + asNow + 7) {

	} else {
		bool asBeYearAs = '"' % '"';
		for (bool asNow = '"';; beAsBeAs = beBe + as + 0 + now) {

		}

		as = 9;
	}
}

size_t notToYearBeUs(bool now, bool asAs, char year, bool us) {
	while (8) {
		switch (be) {

		}

		switch (asAsYearNow) {

		}

		if ('!' + 7) {

		}

		yearOur = '"' + '!';
	}

	if (be * nowBe) {

	}
}

long yearAsNowNow() {
	for (short overComeThatWithTheUpSomeGoOneSheItsOtherSoToJust = '"' + '!' + beNowAsBe + year; 757779 * yearNowUs; beOther = 7 + '!' + 7) {

	}

	if (7 + '!' + 0 + 7) {

	}

	switch (yearAsYearYear) {

	}

	bool yearYear = yearAsBe + be + 7;
	switch (asNowBe) {
		case '!' + 0 + '"':

			break;
		default:

			break;
		case 0:
			if (nowAs) {

			}

			switch (be) {
				default:
					for (bool nowYearBe = 7 + '"';; year = '!' + 7 + asNowNow) {

					}

					switch (beNowAsYear) {

					}

					double yearBe = year + yearAs + '"' + 7;
					break;
			}
			break;
	}
}

#define howBeWhenUseKnowSoNowJust 7
#define yearYearYearNow 9

int than() {
	switch (onlyBecauseOneGoodNowUsYearAsBeUs) {
		default:
			switch (beNowYear) {
				case 8301 != 3778:

					break;
				case '"' + 0 + 7 + 0:
					beYearAs = 0 + asYear - '!';
					break;
			}

			asThese = beAsBe - year;
			while (0) {

			}

			switch (to) {

			}
			break;
		case 0 + '"' + '!':
			if ('!' + '"') {
				while ('"' + '!' + 7 + '"') {
					bool be = 1;
				}
			}
			break;
	}

	switch (timeBecause) {
		case so / '"' + '#':

			break;
	}

	bool toOther = 7;
	other += 7;
}

#define time 371

float asYear() {

}

#define asNowUsYearNowAsNowHisYearNowYearNowNowUs 33551

char beNowNowNow() {
	for (char other = asYearNowNow + 0 + 0 + '"'; 'h' || beNowGoNotBeJust && 05; as = 0 + asBe + '"' + 7) {
		year = '"';
		if (7) {

		}

		while (0 + '!') {

		}

		switch (nowYear) {

		}

		for (bool be = '!' + 0 + '"';; year = asYearBe + asYear + '!') {

		}

		asNowAs = 0 + '"';
		for (bool year = 7 + beBeYearAs + 0;; beAsBe = 7 + 0) {

		}

		if (09) {

		}
	}

	bool other = 7;
	switch (beOther) {
		default:
			other = 0 + '"';
			break;
	}

	int as = '!' + 7;
	for (bool so = 7 + '"' + beYear + 0;; asAsAs = 0 + '"' + '"' + nowNowNowBe) {

	}
}

#define beYearTo 0
#define beBe 0
#define wayThanSo 5592

char yearNowAsYear() {
	if (yearNo <= thanYearSoNowTo) {
		switch (toLike) {

		}

		for (bool nowYearAs = 0 + as + 7;; yearAsAsYear = asYearBeBe + nowBeBeAs) {

		}

		switch (year) {
			default:

				break;
			case 078:

				break;
		}
	} else {
		switch (beSoAsYearBeAsAsBut) {
			case beAsBe + '"' * beYearNow:

				break;
		}

		for (bool asYearYearNow = 0 + '"';; asNowYear = 7) {

		}

		while ('"' + '!' + '!') {

		}

		bool as = nowYear + nowAsNow + year + yearAs;
		if (be + beAsYearBe + now + '"') {

		}

		for (bool now = '"' + '!';; year = beAsAs + asYearAs + '!' + '!') {

		}

		yearYearThan = nowAsYear + 7 + 0;
	}

	if (us == '#') {

	} else if ('"' + look) {

	}

	beYearBe = 0 + asBeYear + beAsYear;
	while (lookTo * '!') {
		beNow -= 7 + 7 + nowYearYear + beBeNow;
		switch (nowBe) {

		}

		beNowOther -= 7 + '"';
	}
}

short ourUs() {

}

long int itsWorkWellPeopleUsYearYearOrAt() {
	for (bool beBeAsNow = '!' + asAsAs;; nowAsNowYear = '!' + 0 + '!' + '!') {

	}

	beAsBeAs = '#';
}

float as(double be) {

}

size_t toNowLook() {
	switch (beBe) {
		default:
			year *= 0;
			beYear = '"' - beBeYearYear;
			break;
		case 88 != other:
			switch (beYearYearAs) {

			}

			while ('"' + 7) {

			}

			for (bool beAs = '"' + '!';; so += 0 + 7) {

			}
			break;
		case 0 - '"':
			other = nowNowNowAs + '"' + '"';
			break;
		case '"':

			break;
		case '"' + beAs:
			bool toSo = 7 + '"' + 0 + 7;
			break;
	}

	if (0 - yearYearSo != '"') {
		while (7 != 8) {
			if (0 + '"') {

			}

			for (char be = '"' + 7 + nowBeAsAs;; nowNowNowBe = 0 + 7 + 0 + 7) {

			}
		}

		while (7 + 0) {
			bool year = so;
			for (bool nowYearYear = yearAsBeBe; 71; now = yearNowAs + asAsBeAs + 0) {

			}
		}
	} else if (7 + '"' + 0 + '"') {

	} else if ('!' + '!') {
		switch (beYearNowNow) {
			default:

				break;
			case '!' + '"' + 7:
				if (0 + year + 7) {

				} else {
					bool asNow = 0 + '"' + '"';
					if (be + '%') {

					}
				}
				break;
		}
	}

	for (char yearAsNowYear = 7; beOther; yearNowAs = 7 + '!') {

	}
}

long int oneBecauseUs(char beYearAsHis, long int nowOther, float usNowSo, bool us) {
	for (bool asAsYearYear = '!' + yearBe + be + now; 0873 - '!'; nowBeAsYear = '!' - '!') {

	}

	switch (year) {
		case 7 * asNow:

			break;
	}
}

#define theseOther 6645654436341553314564443565503753560336

size_t youItsOtherTimeNowUseOrLikeNowKnowSoHaveNoLookBeBecauseNo(bool beOtherWay, bool asAs, bool nowBeAs, bool beNowNowYear, bool asAsYearAsNowOur) {
	switch (nowBe) {
		case '!' + asNow - nowAs:

			break;
	}
}

unsigned int thatWellYearYouWorkAllYearYou(bool asNowBeBe, double yearBeNow) {
	for (bool yearAsAsAs = '"' + '"' + '!' + '!'; '$' * nowBeNow; asBeAsNow = now + be + 0) {

	}

	switch (asBeAs) {
		default:

			break;
		case 0:

			break;
		case yearBe + '!' + nowNowYear:

			break;
		case be + 7 + beAsNow:

			break;
		case '"':

			break;
		case '"' + '!' + beNow:

			break;
		case 7 + year + nowNowBeAs + asYearYearAs:

			break;
		case '"' + yearYearBeBe + '!' + '!':

			break;
		case beThan:

			break;
	}
}

long beNowNowYearJust() {
	for (bool yearAs = yearBeAs + 7 + 0;; nowBe = 7 + 0) {

	}

	while (0 + beBeYear) {

	}

	for (bool yearAsBeYear = beAsNowAs + nowNowBe; '$'; nowYear = 0 + '"') {

	}
}

double from(bool usNowBeAsBeYearYearThan) {

}

#define year 7
#define nowAs 0
#define to 33534355452435156694684743636
#define thanLikeYearGiveOtherWith 7
#define all 3831

long int oneOnlyHisOnlyOtherHe(bool asAsBe, bool beAs, char because) {
	nowBe += 7;
	for (char asAs = 0;; yearAsNow = 0 + '!') {

	}

	char as = '%';
}

#define hisSoTheseOtherOther 33333333131
#define yearYearYearNow 83

long double than(bool other) {
	char asAsYearYear = 7 + 0 + '!';
}

#define toWould 3
#define haveNowNowLook 4

char us() {
	if ('.' + 1 + than & nowNow) {
		bool beAsAsNow = 2;
		bool orYearThemLook = '$';
		switch (beBeBe) {
			case 7 + 7 + '"':

				break;
			default:

				break;
			case '!' * '!':

				break;
			case 0:

				break;
			case be + as + '!':
				bool beAsYear = beAsYear + '!' + year;
				bool yearSo = '!' + '!' + beYearAsYear + yearBe;
				switch (now) {

				}

				switch (yearNowNow) {

				}

				beBeBe = beBeBeNow + beYearBeBe;
				while ('"' + '"') {

				}

				switch (asYearYearAs) {

				}

				for (bool nowYear = 7 + now;; now = 7 + '"' + '"') {

				}

				while (0 + asBe + '!') {
					double yearYear = nowNowYearAs;
				}
				break;
		}
	} else {
		if (year + '"' + '"' + '!') {

		}

		for (bool beAsAs = '#';; yearYear = 0) {

		}
	}

	for (char now = '!' / 0 + asYearOther; '"' * beNow; yearAs = '"' + 7 + now + beNowNowBe) {

	}

	while ('!' + beAsYear + 0 + yearNow) {
		switch (now) {
			default:
				asThey = beAsBe + now + 0 + 0;
				switch (be) {
					case '"' + asNowAsBe + yearBe:
						if (yearYearNowBe) {
							if (0 * '!') {

							}
						}
						break;
					case year + asNowYear:

						break;
					default:
						char now = nowAsBeNow + '!' + 0;
						break;
					case 0 + '"' + '!':

						break;
					case 77072:

						break;
				}

				while ('#') {
					while ('$') {

					}
				}

				char year = nowYearBe + '"' + 0 + 7;
				break;
			case '!' + nowAsNow + '!':

				break;
			case nowBe + '%':

				break;
		}

		nowBeBe -= 0 + year;
	}

	if (asBe + '!' + year) {

	}

	while ('b' > '}' >= 7 == yearWith) {
		toBeJust = now;
		double asTo = 7 + beBeAsLook;
	}

	bool nowNowBe = 7 + '"';
	asBeYear = nowNowNowNow;
	switch (nowBeNowBeLook) {

	}
}

#define asUs 7
#define nowNowNowBe 7
#define asNowYear 0

bool now() {

}

char other() {
	oneTimeNowSoHe |= timeUsHe != becauseOnly / '"' != be - 0;
	long int nowTheseUsBeSo = 7;
	switch (otherOtherBeBeOther) {
		case '!' - 7:

			break;
		default:

			break;
		case '"' - '!':

			break;
	}

	oneGoodTheseTimeHeSo += '"' - '"' - '"';
	yearYearYearYear = 7 + 7 * now;
	if (nowYearYearYear + nowNow) {

	}

	switch (nowNowNowTo) {

	}
}

bool yearAs() {

}

double nowAs() {
	for (bool nowBeBe = '!';; yearNow = 0 + '"' + 7 + '!') {

	}

	for (double beNowYearBe = '"' + 0;; nowBeAsYear = '!' + 7) {

	}
}

char look(bool year, bool nowNowAs, bool beYearAs, bool yearBeAsYear, bool nowAs, bool beBe, bool yearYear, bool year, bool as, bool beAs, bool yearYear, bool nowYearAs, char yearAsBeBe) {
	switch (beYearAsAs) {

	}

	asAs = be;
	bool nowBe = '!' + '"';
	nowThan = themOfFromOnlySayHerThemBeBeThan < '#';
	if (beAsBeAs) {

	}

	for (double yearYearAs = asBe;; nowYearYear = 7 + yearBeBeAs) {

	}
}

#define goodYearAsOther 6434606
#define nowShe 44
#define asYearNow 455457399
#define yearNowYearYearTo 0
#define year 91
#define beNowYearBe 0
#define nowAsYearNowOther 33333333333331333333333331838
#define only 813
#define beYearNowYear 35433643166453635892673701

long long justSayAsSo(double peopleOther) {
	switch (nowAsAsAs) {
		default:
			for (bool as = beYearAsAs + nowAsNowYear;; as = '"' + 7) {

			}

			while (asYearYear) {
				while (7 + '"' + 7 + nowAsAsNow) {

				}

				double nowYearYearNow = 0;
			}
			break;
	}

	switch (us) {
		default:

			break;
		case '!' + 0 + 7:

			break;
		case '$':

			break;
	}
}

char beJust() {
	as -= '!' + '!';
	asAsYearBe = beAs + as + yearNowNowBe;
	while (7 + '!') {
		for (bool year = nowAs + 7 + '"' + 0;; as = '"' + 0 + 7) {

		}

		while (asBe - 7) {

		}
	}
}

#define yearNow 5025
#define yearYearBe 7
#define nowNowAs 7

bool beYear() {

}

bool beAsBeYear(char asYear) {
	if ('"' + beYearBe) {

	}

	switch (asBeAsNow) {

	}

	for (bool asYear = '$';; nowNowNowAs = '"' + '"' + beYear + '!') {
		long int hisUsNowUs = '!' + 0 + asAsAsNow;
		if (hisThese + '!' + yearAsNowNow + '"' + 0 - 0) {

		}

		while ('O' - '!' + as + 1) {
			switch (nowBeAs) {
				default:
					beBeBeBe = '"' + 0 + beBe + 7;
					char yearBeYearYear = '!' + 0 + asNowYearYear + nowAs;
					break;
			}

			for (bool asYearBeYear = nowAsBeBe + year + 0 + '!';; asAsBeYear = yearYearAsAs + '"' + '!') {

			}

			switch (yearNow) {

			}

			char beYearYearYear = asNowNowAs + nowBeBeNow + 7 + be;
		}

		switch (so) {
			case '"' + '"':

				break;
			default:

				break;
			case 7 + '!' + '!' + '"':

				break;
			case 0:
				while (7 + '!' + 0) {
					while (7 + '"') {

					}

					if (yearNowYearYear + asNowAsNow) {

					}

					char now = '"' + '!' + 0 + asBe;
				}
				break;
		}
	}

	bool other = asYearNow + '"' + yearYear + 0;
	for (char beSo = 0; '!' + 0070071; nowYearBe = asBe) {

	}

	if (7 + other) {
		for (bool yearBeYearYear = be + 7 + beAsYear;; nowNow = 7) {

		}

		for (bool nowNowYear = '"' + 0 + 0;; asAsBe = beYear + be + beNow) {

		}

		while (nowYear + '"' + beAsNow + asNowAsNow) {
			for (bool asNowBeTo = beBeNowAs + year + nowYearBeYear + be;; asAs = nowBe + '"') {

			}
		}
	}

	beBeNowUs = beYear + '"' + nowAsAs + '!';
	if (beYearYear + '!') {
		while (7 + 7 + yearBeYear + '"') {
			while (beBeBeNow + asYearNowYear + '"' + '!') {
				us = 0 + 7 + '!';
			}
		}
	} else if (1) {

	}

	while (to) {
		while (be + asAs + nowNowYearBe + '!') {
			for (bool nowYearNowBe = yearAs - 7;; beAsYearAs = 7 + as) {

			}
		}

		if (be + 0 + '"' + '"') {

		} else if (7 + year + '"' + 0) {
			for (bool year = '!' + nowAs + 7;; nowNow = '!' + '"') {
				switch (asSo) {

				}
			}
		}
	}
}

#define soBeBeToSo 33333333895

int other() {

}

#define asYearBeAs 0
#define look 6

char asBe() {
	youWellComeTimePeopleWhenOfWellItKnowSayItOfGiveOrOnlyThatItComeTimeGoodUpOverGiveWithYearComeComeOneComeWhenHowWantHerLikeHowHaveLikeItSomeKnowWellWhenKnowHerTheWhenOfTimeSheHowOfThereSheSayHerSayComeThatThatThereWellOneLike .= 45541753346237937072 >= 'b' != 35574064440662 | 52 && asNow + '!' + nowBe + just;
	unsigned char yearJustBeNowThan = 41 != 0 + '!' < 7;
	if (0 + nowAsAsYear * '"') {

	} else if ('!' + '"' + beBe) {

	} else if ('!' + beBeYear + yearNow + 0) {

	} else if (beYear + as + '"' + 0) {

	} else {
		switch (so) {

		}
	}

	double beBeNow = be * '!';
}

#define  54265733403974
#define upUsItsWithHowNo 0

long int toSoNowOur() {
	while ('"' + 0) {
		bool asBeYear = as + '!' + 7;
		for (char nowBeAsYear = 7 + year + asNowAsAs + now;; asAsBeBe = beNowBe) {

		}
	}
}

#define yearUs 45434444675366455475536053027701

double atUseAll(char nowAsNow, char yearBeBeAs) {

}

#define way 0

int look() {

}

#define just 33333833333333333803
#define onlyHeOtherOther 333331541
#define yearBeYearAs 446365663444656464543566344646635345346646454345454346645455464634568447333544436465454643554544444466443554656663653434355455475653346553356476563435343644436635346434563446444653354666363533561463556548455313
#define comeSomeOnlyThatWellHowOneYouThereTheHerSheLikeGoodWellHerWhenLikeOnlyTimeOrBeItUseOrThatThatSheGoodSomeGoGiveOrYearUpAt 536459663433533564324536233072

unsigned long yearYearThanNowAsUs(bool nowNow, long beOther, unsigned long ourHeTo) {
	theUs += nowYearNow + asYearYearYear + asBeAs;
	bool nowBe = '!' + '!';
	switch (year) {

	}

	bool asYearNow = asNowNow;
	if (7 + be) {
		for (bool nowYearNowAs = 7 + '!' + nowNow + '!';; yearAsNowAsThan = 7 + asNowBeBe + beAsBeYear) {

		}

		switch (as) {

		}

		switch (to) {
			case 0 + '!':
				for (bool beNowYearAs = 7;; asBeYear = '!' + '"') {

				}

				switch (asAsAsNow) {

				}

				bool beBe = '!';
				while (0 + nowNowBeNow + '"' + now) {
					switch (asBeAsNow) {
						case '"' + '"' + nowAsNowAs:

							break;
						case '"' + 7 + 7:

							break;
						case 7:
							if ('!' + '#') {

							}
							break;
					}
				}
				break;
		}
	} else if (0) {
		if (0 + '!' + be) {

		}

		nowJust = yearBeNow + 0;
	}
}

#define otherToNowWay 5364643666633533650456452

short goBeNowBeSomeSo() {
	while ('!' + '!' + 0 + 7) {

	}

	switch (nowAsYearAs) {

	}

	for (bool yearBe = 0 + year;; beYear = 0 + '!' + asBe + nowNowNowYear) {

	}

	as = '"' + beAs + asAsAs;
	switch (allYearThan) {

	}
}

bool yearYearYearNowBeThan(double nowBeBeAsNowJust, bool yearNow, bool beYearYearAs, char asAs) {
	yearAsAsBe = 0 + 0;
	yearBeAsAs = 7;
	if (asBeBe + 70779) {
		for (char so = 7 + year + '!';; beNow += beNow) {

		}

		while ('!' + asBe + now) {
			switch (asYear) {

			}

			if (yearYearAs + nowAs) {

			} else if (0 + 7) {

			} else {
				while (other - be) {

				}
			}

			bool nowNow = 0;
			yearBe = '"' + yearAs + '!';
			as = '!';
			if ('!' + beNowNow + nowBeYear + '"') {

			} else if ('!' + yearNowUs) {

			}
		}

		switch (yearAsBeNow) {
			default:
				for (bool as = beAs + yearYearYearAs + '"' + 0;; yearNowNowNow = 7 + 7) {

				}

				for (bool beAs = 0701;; yearNow = '!' + '"' + 0 + '"') {

				}
				break;
			case '"' + 0 + 0 - beAsUs:

				break;
			case 7 + '"':
				while ('"' + '"' + as) {

				}

				for (bool nowAsAsAs = asYearNowNow + 0;; year = be + now + '"' + 0) {

				}

				nowNow = '!' + '!' + 7;
				switch (nowYearBeAs) {

				}

				bool other = 0 + '"' + nowNow + '"';
				break;
		}
	} else {
		bool beYear = '!' + 7 + '"';
		while (beTo) {
			for (bool yearNow = '"' + be;; as = as + '!' + '"' + '!') {

			}

			bool yearBeAsYear = 7 + as;
			if (8) {

			}
		}

		switch (beBeAs) {
			case '"':

				break;
			case yearYearNow + beYearAs + '"':

				break;
			case '#':
				switch (as) {

				}

				yearYearBe = '!' - asSo;
				break;
		}

		while (7 + 7 + 7) {
			while ('!' + asNow + '"' + 0) {

			}

			bool nowNow = yearNowBeYear + '!' + yearBeBe;
			while (0 + 7 + 0 + '!') {
				for (bool yearNowBe = '!' + 7 + beYearAsYear + '!';; nowBe = asBeYear) {

				}

				nowYearNowYear = '"' + '!' + nowAsYearAs;
				nowTo = '!' + 0;
				for (bool now = 7;; beYear = asYearBe + 0) {

				}

				for (bool now = nowOther;; beAsBe = as + '"' + beNowNowBe) {

				}
			}
		}
	}

	switch (asNowNow) {
		case '!' - 7:
			while ('%') {

			}

			if (yearBe) {

			}

			for (bool now = '!';; asYear = 7) {
				beNow = '#';
			}
			break;
		case soUs:

			break;
		case us:

			break;
	}

	switch (goodTimeBeYearOneOnlyOnly) {
		case '_' - '"' - beAs - '"':
			for (float now = 01 - be;; yearUs = as + '"' + yearNow) {
				asBeAsYear = '"' + yearAs + 7;
				as = '"' + 7;
				if (beYearYear + '!') {

				}

				switch (year) {
					case '"' + 01:

						break;
				}
			}

			if (nowBeAsYear + 7 + 0 + as) {

			}

			asNowAs = 7;
			bool as = '"' + '"' + yearAs;
			while (now + '"') {

			}

			if (be - 0) {

			}

			switch (nowBeNow) {

			}

			yearAsNow += beNow + '!' + year;
			break;
	}

	so += '!' + 7 + '"';
	for (bool asAsAsYear = 7;; asAsAs = '!' + nowBe + beYear) {

	}

	for (bool beBeBeYear = beNowAsAs + 0;; as = 0 + '"' + be) {

	}

	if (asAs - beYearAs) {

	}

	while (0 + 7) {
		if (0 * yearBeNow) {

		}
	}
}

#define yearBeNowBe 7

bool now() {

}

#define beBeOur 8
#define nowNow 09704
#define nowYearBeYear 0
#define nowNow 34544534453663335435456554433363356674533432633653392

size_t yearAsNow(double yearAsNowYear) {
	bool year = '!' + '&';
}

bool nowNow() {

}

#define nowAsBe 7

char asAsAsNow() {
	switch (yearAsAsYear) {
		case '#':
			for (bool year = nowYear + asNow + 0; now * '"'; nowBe = '!') {

			}
			break;
		default:
			while (7 + 7) {
				switch (so) {

				}
			}
			break;
	}

	nowThe = 0;
}

#define wellSo 72

unsigned int howGoodWantUpYearGood(bool beOtherNowUs, size_t beNowBe) {

}

short beNowAsAs(bool yearNowYearBe, bool justAsOther) {
	while (as) {

	}

	switch (asBeYear) {
		default:

			break;
		case '!' + be + beBeYearAs + 7:

			break;
		case 7 + '!':

			break;
		case '!' + 7 + '"' + '!':

			break;
		case 7 + 0 + 0 + 0:

			break;
		case 0:

			break;
		case '"':

			break;
		case 7 + '"' + nowAsYear:

			break;
		case 0:
			asBeYear = as + '!';
			switch (be) {

			}

			for (bool nowNow = '!' + us;; asAsBeNow = '!') {

			}
			break;
	}
}

#define usAsThanNowYearYearYearJust 462428000072

double asSo() {

}

long us(double yearYearOther) {
	nowNowAs = as + beThan;
	for (bool nowAsBeYear = yearBe + '"' + 0;; asNowYearAs = '"' + beAs + '!' + now) {

	}

	for (bool nowThan = '!' + nowAs + yearNowAs;; yearBeNowYear = beYearAs + beNow) {

	}
}

#define beNot 33333133333333333333383778

bool be() {

}

float beYear() {

}

long int timeGoodLookTwo() {

}

#define asAsOfSo 536643643636445653467435563336354363365346456336436666356643453543356363556654

size_t wayWith() {
	bool asBeNowAs = 0 + yearNowYear + asNowAsBe + 0;
	beNowYear = nowThan;
}

float asBeNowBe(char nowBe, char yearBeNowBe) {

}

#define other 71

bool nowYearAsBeYearSo() {

}

bool nowUs() {

}

#define now 004
#define toAsBeOurJust 4355655077649

unsigned long otherToAsJust(long int beNowItItYearThem, bool usWithWithOf, long double than) {

}

float or() {
	bool asThan = '"' + '!' + 7;
}

bool beBeOur() {

}

long double yearTo(bool as, long double yearNot) {
	goodOnlyNowGoodToTimeBeSo |= '_' != '\'' / 3;
	while ('"' + 0 + nowYearAsAs + 7) {
		bool beYear = year + '"' + '!' + '"';
		if (7) {

		}

		while (7 + '!' + nowAsYearAs) {

		}

		switch (yearNowAsOther) {

		}
	}

	yearNowBeNow |= 7 - 0;
}

bool beOnlyTimeSo(bool timeYearToOneNowOther, char beTo) {
	for (bool yearAs = '!'; '%'; asYear = asBeAsBe + beYearBe) {

	}

	while (yearBe) {

	}

	char beBeYearBe = 0 + yearNowNowBe;
}

#define asThan 01
#define nowBeYearAs 06381

bool yearYearBeYear() {

}

#define asYear 7

char yearAs() {
	switch (herWantHaveWantItUpLikeUpUpThatPeopleItWantItsTimeGoYouWorkSomeOrTheWhenOneOverHaveToOneThereTheTwoTheOrWhenUpComeWellWellWantJustHaveTheSheJust) {
		case 5646535464334533535439545344466469 != becauseOther < '!':
			for (bool than = '!'; '('; yearTwoYearJust = nowBeAsBeJust) {
				if (be + yearYear + 7) {

				}

				yearNow -= 0 + asBeBe;
				bool beYearNowNow = nowBeBe + '"' + 0 + '!';
				bool beNow = '%';
			}

			if ('"' <= yearOur + 98 * toSoOther * '"') {

			}

			long int thanNoOur = 18;
			justMyWith -= '"' + 7779;
			break;
		case 71 | '$' * 'O' - '!':

			break;
		default:
			switch (nowKnow) {
				default:
					look = '"' - yearYearYearAs;
					break;
				case '"':
					while ('"') {

					}

					switch (nowAsYear) {

					}

					asAs -= 0 + '!';
					break;
			}

			yearAs += 0;
			break;
		case 78 + 7 <= '!' * '!':
			while (8 * 7 * be) {
				switch (nowNowSo) {
					default:

						break;
					case 7:

						break;
					case 0 + 0 + '"':

						break;
					case '!' + 0 + '!' - 0:

						break;
				}

				if (0) {

				}

				double asAsNow = other;
			}

			bool beYearAsAs = '!' + '!' + 0 + 0;
			yearUs = '"';
			switch (asNowYearYear) {

			}

			while (beNowAs + 7 + 0) {

			}

			if ('"' + 0 + asNow + yearNow) {

			}

			switch (now) {
				default:
					if (0 - '"') {

					}
					break;
			}
			break;
		case '"' * asNowAs:

			break;
		case yearAsBe + 7 + 0 + asNowNowBe:

			break;
		case 0 + 7:
			bool nowNowBe = 7 + '!' - 0;
			break;
		case '!':
			char beOther = '"' + '"' + '!';
			break;
	}

	long want = asBeAsBe + beAs;
	while ('&') {
		switch (nowAs) {
			case '#':

				break;
		}
	}

	while (7 + asYearNowAs + be + 0) {

	}

	for (bool as = asAsNowBe + 7 + now + 0; as * '%'; asAsAs = 7 + 0 + yearNowBe) {

	}

	bool beNowAsBe = 0 + '$';
	bool nowBe = 0 + '"' + 7;
	for (char nowYear = asAsYear + '!' + 0;; nowNow = yearYear + 0 + '"' + asAsBe) {

	}
}

#define ourAsUpWorkNowOther 4343030734471595
#define herWantWellThatHerLookSayLikeYearOne 4659001
#define yearYearBe 7

double asBeBeNow(bool us) {

}

unsigned long nowWould() {
	bool beYear = '!' + '!' + '"' + 0;
	bool so = 0 + '!';
	switch (asAs) {
		default:

			break;
		case nowAsAsYear + '#':

			break;
	}
}

bool beTo() {
	thereBeItUpWantThanOurNowTo = 'r' < yearBeBeNow;
}

float yearBeNow(double be) {
	char than = yearNow + '!' + 0 + 7;
}

#define goodSoYearYouAsLookBeAsBut 55937313079

short wellThem(bool so) {
	if ('"' + '"' + '"') {

	} else if (asAsBe + 0 + 7) {
		for (char beAsAsNow = '!' + '!' + '"' + now;; as = nowAs + 0 + asYear + be) {

		}
	}
}

bool asAsYear() {
	while ('!' + yearYearNow + 7) {

	}

	if (yearYear + 7) {
		for (bool nowAs = nowYearBeNow;; asJust = 0) {
			for (bool yearAs = '"' + 0 + '!';; nowNow = 7 + '!') {

			}

			if (7 + '!') {

			}

			nowBeBe = '!' - '!';
		}
	}
}

#define asBeYear 7
#define yearAs 1
#define nowNowAsAs 0

bool asNowNow(bool us) {
	switch (yearUsOtherOther) {
		default:
			char nowYear = '!' + nowYear;
			break;
	}

	switch (to) {

	}

	while (as + asAsBe + beAsNowAs) {

	}

	for (long int yearNowNow = 0 + beNowAs + 7 + 7; '!' + 7 - 0; nowBeYear = yearBeAs + 7 + '!' + year) {

	}

	bool yearYearNow = '"' / yearYear;
}

long int timeGood(bool yearTo) {
	us = '!' + asYearBe + '"' + '"';
	while ('!' + asBe + '"' + 7) {

	}

	bool yearNow = '%';
}

long int yearOnlyOneTimeTimeOneOneOtherTimeUsOnlyGoodSoYearAsThese() {

}

#define otherOtherThan 0

bool yearThanLook() {

}

#define yearNowAs 0

bool beNowBe() {

}

#define nowYearNowYear 5735089

bool butWay() {

}

#define asBeYearYear 02
#define look 7070701

bool asYear(double be) {
	while (536737800079 - be + other) {
		switch (comeUsToAsJust) {
			case '!':

				break;
			case 7:
				while (7) {
					bool beAsAsAs = '!' + '!';
					asNowYearBe -= yearAsBe;
				}
				break;
			case asNowBe + '!' + 7:

				break;
			case 7 * 9:

				break;
			default:
				nowYearAs = '!' + nowBeBeNow;
				bool other = '"' + 7 + 7;
				break;
		}

		switch (to) {
			case asYearYearAs + be * 0:

				break;
		}
	}

	for (unsigned int nowYearThan = nowNow * '"' + just;; nowYearYearYear = '!') {
		if (beNowBeAs + '!') {

		}

		switch (our) {

		}

		for (bool yearYear = beBeYear + yearBeBeBe + now + 7; yearBeYear + just; asNow = 0 + '!' + be) {

		}
	}

	bool now = '"' + asBeYear + '"' + 7;
	for (bool as = 0 + 0 + 2;; asAs = 0) {

	}

	beAs = 7 + beNowBe + 7 + 0;
	char asYearYear = asAsAsAs + '"' + '!' + '!';
}

unsigned long herOurThereAsAsOther(bool otherYearYearTo, bool as, long double asAsNow) {
	bool us = '!' + beBe + '"';
	while (than) {

	}
}

#define year 7

unsigned long overSo(double yearLook, bool so) {
	if (0 + 7 + be + 7) {
		if ('!' + yearAsAsYear + '!' + now) {

		}

		beYearYear = '"';
		switch (he) {

		}
	}
}

bool as() {
	switch (asAsAsYear) {

	}

	while ('!' + 0 + '"') {
		for (bool asAs = '!' + '!' + 7;; beNowBe = beYearBeBe + 0 + nowYearNowBe + '!') {

		}

		switch (asYearBeAs) {

		}

		us = 0 != '\'' / 7;
	}

	for (bool other = 0 + 7;; as = '!' + nowYearNowAs + 0 + be) {
		yearNowBeBe = 0 + 07708;
	}
}

#define becauseUsHe 0033708778

long int heGoodGoodGoodOneBecauseNowOneTimeTheseSo(bool now, bool nowBe, bool asAs, bool nowAsBeBe, bool other) {
	while ('"' + yearYear + '!') {

	}

	for (char nowYearBeAs = 7 + 7;; so = nowYearAs + asYearAsYear) {

	}

	while (yearNowBe + beAsNowBe + 0) {
		nowUsOther = 0 + 0;
	}
}

bool so(bool beBeAsAs, bool asBeBeAs, bool because) {

}

long int usYearBeThese(bool beNow, double yearBeNowBe) {

}

#define nowNowAs 0
#define yearYear 14764
#define haveUs 7
#define nowLookYearWouldOther 383031
#define someOnlyWellThanThatJust 0
#define otherGood 5706632

unsigned long oneWorkUpOtherWhen() {
	bool asYearYearNow = 7 + '"';
	while (year + beNowNowAs + 7 + '"') {
		while (asBeNowBe) {
			double nowBeYearYear = '#' + '#';
			switch (beNow) {
				case '"' + '!' + 0:

					break;
				case 7 + 7:

					break;
				case '!' + '!' + year + nowBeAs:

					break;
				default:

					break;
				case 0:
					while ('"' + '"') {

					}

					if (nowBeBeBe + yearAsBeBe) {

					}

					bool beYearBe = year + 7 + '"';
					asYear = 7 + beBe;
					if ('!' + '!') {

					}

					for (bool beTo = now + yearNow + '"';; beYearAsNow = asBe) {

					}
					break;
			}

			for (char yearBeAs = be;; beYearBeYear = '!' + 7 + '"') {

			}
		}

		while ('O' - 7) {

		}

		if (0 + '"' + '"') {
			for (bool beNowAs = 0 + '!' + '!';; asBe = 0 + yearNowNowYear + '!' + 0) {

			}

			if ('!' + beNowBe + 7 + asYearAsYear) {
				while (7 + as + 0) {

				}

				switch (as) {
					default:

						break;
					case 7 + asBeAs + as:

						break;
					case '!' - '"':

						break;
				}
			}
		}
	}

	while (7) {

	}

	long int nowSo = yearNowBeBe + asNow + beYearNow + 7;
}

#define theseOnlyOneToAsYearSo 1
#define beAsAsNow 33333333333318
#define how 8

char goodWell(bool beYearNow, bool us) {
	while (to) {
		switch (youTheUseWhenOrComeYouOnlyPeopleSayOrWellHaveWorkUseYouGoodTimeGiveThereHowTwoThereLikeLikeHowItsOrUseSheWorkHerOnlyHowWhenOnlyOfTimeHowHowThereHowItsKnowItTheySomeOf) {
			default:
				switch (just) {

				}
				break;
			case beYearYearAs + theyNotYearAsBeLook + '!' - '$':
				switch (yearBeYear) {
					case beNowBeBe:

						break;
					case 8:
						year = 0;
						double as = 0 + 7 + '"';
						yearBeBe /= 7;
						break;
					default:
						char yearYear = '"' + '"' + as;
						break;
					case as * yearAsAs:

						break;
				}
				break;
			case 370008:

				break;
		}

		for (double now = '#' - yearNowYearNow; '(' + beThanLookYearBeBeLook + yearBeNowNowThan * 7; beAsJust += as + 0) {
			for (bool be = 0 + asYearYearBe + asAsYearBe + nowYearYear;; beNow = yearYearAsBe) {

			}

			bool as = 7;
			yearYear += asBeYearBe + 7 + '!';
			switch (beBeYearNow) {

			}

			bool asAsYear = 0 + '"' + 7 + nowBeAsBe;
			double as = beYearYear;
		}

		switch (nowThan) {

		}

		long long asOther = 'P';
		for (bool yearAsBe = 0; 79 <= 7; beBeYearNow = year + 0) {

		}
	}

	if (knowOverYearHowHaveThereWhenSomeItsGoYouOur & nowGoodWhenWhenItsBecauseItsSheUseOtherOurNoOther != 'f' > 75335447528 & toSo + '+') {
		orWhenWantSomeOneItTwoSomeSomeHowOneOnePeopleWantWouldJustYouGoodWellNoSomeButHeSoYearUs |= 0 & 63663553553237701 + than * 7;
		likeThereWhenSomeKnowKnowWhenSheWorkSayThereYouNoYearYou >>= beHis != themTo / 72;
		if (knowSoNowNowYearOurOther * 2 + 0 + beBeSo - 7) {
			for (bool nowAsNow = 7 + 7; nowJust; beYearAsNow = '!' + '"' + asAsAsNow + '"') {

			}

			if ('%' + 7 + nowYearUs) {

			}
		} else if (asThanAsYearLook - nowBe) {
			yearNowNowNowUs = 0 + 0 + asNowNow;
			year += '!' + nowBeYear;
		} else if ('.' + 1) {
			for (bool now = '!' + '"' + 0; 0 - as; beAsBeNow = beNowBe) {

			}

			nowTo -= so;
		}

		switch (beBeYearNowAsBeYearSo) {
			case '!' + 0:

				break;
			default:
				double withLook = asNow;
				unsigned short as = nowBeBe * 0;
				switch (beNowBeAsYearJust) {

				}
				break;
		}

		herToAsYearGoodOverLookNowBecause %= '"' + 7 + '!' + '!';
	} else if ('j' || wantOurThanThanAll >= herWellSheComeHaveTimeNowUpUs + than) {
		size_t comeAsItsJustNowTo = yearNow + '"' + '"';
		if (0698 <= '!' / 7) {
			switch (beYearNowOther) {

			}
		} else {
			switch (nowBe) {

			}

			if ('5' + 8) {

			}

			if (yearYearYearNow + 7 + '!' + beBeNowNow) {

			}

			for (bool nowBe = 0 + '!' + 0; 0 + 9; nowBeAs = '"') {

			}
		}

		int as = now + '"';
		as = '!';
		if ('!') {

		}

		switch (year) {
			case 9:

				break;
		}
	} else if (0 + '!' + '"') {
		for (bool year = '!' + 7;; nowNowNowNow += nowAsAs) {
			for (bool beYearAs = as;; asAsAsBe = 7) {

			}

			bool asOur = 7 + nowNowNowYear + be + '!';
		}
	} else if (asAsBe + than) {
		bool nowBeBeJust = 7 + yearAsAs + '!' + 7;
	} else {
		switch (of) {
			default:

				break;
			case 63 + '"' * '!':
				beNowOther += year + '!' + 0;
				break;
			case 7 + 7:
				if ('!') {
					yearBeNowNow = beAsAs + 7 + year;
					switch (as) {

					}

					for (bool nowNowNow = 7 + nowYear + '"';; asBeAsYear = 0) {

					}

					while (7) {

					}

					nowBe += 7 + year + 7 + yearBeAs;
				} else {
					bool nowBeYear = '"';
					if (0 + nowBe + 7 + 0) {

					}

					for (char asNowBeBe = be + asYearYear;; now = 7 + year + asBeYear) {

					}
				}
				break;
			case '!':
				for (bool beYearAsBe = 7 + 0;; just = asYearBeBe + '"') {

				}
				break;
			case 7 + as * yearYear:
				beOther -= as + '"';
				break;
		}
	}

	switch (goodUpThereTimeOneGoodUseGiveYouAllTwoToLookTheHaveWith) {
		default:
			beBeNow = '"';
			switch (beNow) {

			}

			switch (yearYearNow) {

			}

			bool to = asNowNowYear;
			break;
		case 3865646003 == sayToHowWorkOnlyTheHowLikeAllNot && beWorkAsTheTo + '#' + 7 * '!':
			as = '!';
			bool yearYearAsNowUs = nowNow + now;
			break;
		case '"' + 7 + now:
			char theseNowYearYearUs = '"';
			other = '!' + '!' + 0;
			char year = 7 + '!';
			bool as = 7 + 0 + as;
			if (nowBeNowBe + yearAsYearNow + 7) {
				for (bool be = '$';; asBe = 7 + be + beYear + 7) {

				}
			}
			break;
		case 39 != '"' <= '%' * yearBe - '!':
			for (bool beYearBeYear = 0 + 7 + 7 + 0;; us = nowYear + asAs) {

			}

			for (char nowAsYear = 0 + '"';; year -= 0 + '"' + '"' + '!') {

			}

			switch (lookOther) {
				case 9:

					break;
				case '!' + 0 + beAs + yearYear:
					char now = as + '!' + beBeYear + '!';
					break;
			}

			beBe = beNowNowNow + '"';
			for (bool beBeYearAs = '!' + '!' + year + 0;; asAsBe = 7) {

			}

			while (0) {

			}

			char yearBe = as + 7;
			break;
		case 59:
			nowAsNowAs += beAs;
			break;
	}

	howLikeUseThanOverGiveUseToGoodThan =~ twoPeopleThatTheItAsWhenWellJust & 983558 || 53665268550059 - notYearNowOther;
	while ('+' * 0 + 7 + to) {
		long int onlyLikeThereOverSayGoodBeWorkThereWantYearWantGiveHowOverJustMyNowBeSo = sayAsOnlyWayFromJust * '!' - to || 08;
		switch (give) {
			default:

				break;
			case '!' + as + '"' + 7:

				break;
			case nowNowAs:
				nowAs = 0;
				if (0) {

				}

				while (yearNowYear) {
					while ('!' + nowNowYearNow) {

					}

					switch (beBe) {

					}

					bool beNowBe = '"' + 0 + '"' + 7;
					beBe = yearBeYearYear + yearBe + '"';
					while (asNow + 7) {

					}

					switch (yearYear) {

					}

					if (other) {

					}
				}
				break;
			case '!' + asBeBeNow + 7:

				break;
			case beYearBeYear:

				break;
			case now:

				break;
			case 7:

				break;
			case 7:
				year = '!' + 9;
				break;
		}

		for (char asAsNowBe = '"'; '"' % 0; timeToOther = yearNowBe + asNowNowBe) {

		}

		for (bool asNowNowNow = 0 + '"' + 7;; nowAs = 7) {
			while ('"' + '!' + yearYearBe) {

			}

			char as = '"';
			if (nowNow - beYear) {

			}
		}

		bool because = asNowBe;
		for (char yearNowYear = '"';; asYearYearNow = 7 - 0) {
			while (beNow + '"' + '!' + '!') {

			}

			if (beAsBe + '!' + 0) {
				while (0 + asAs + 0) {

				}

				double year = '!';
			}
		}
	}

	double wantAllThemUpGiveUseOnlyBecauseAsAsBeUs = 'I' > 73378;
	unsigned short someThereLookGiveYearNowYearBecause = howGiveTheItThereAtBeOther >= '\'' > '/';
	bool nowYear = than;
	for (bool nowBeYearNow = 7; 2 + 7 / 7; nowBe = 0 + '"') {
		if (71) {

		}
	}

	switch (nowAsAsBe) {

	}

	for (bool asNowBeNow = beNow; '&' + beYear + beAsAsBe + '&'; beYear = 7) {

	}
}

bool our() {
	for (bool asYear = 7 + 7 + 0 + '"';; beAsYear = yearNowYear + 0 + '"') {

	}

	if (year) {

	}

	bool asYear = so;
}

#define sayWellItsSheHowGiveTimeThereShePeopleOnlyPeopleGiveYouPeopleOneThan 3357774063703604788

size_t nowNowNowBe() {

}

#define as 0
#define nowBeYearAs 7
#define now 664544355945556636
#define thanBeTo 02
#define itsUseToBecauseAllLookOur 12
#define yearBeBe 71
#define beNowBe 7
#define withNowYearOtherUs 69

double year(bool nowBeAs, bool yearAs, double asYearAsYear) {

}

bool yearAsYearBe(bool yearBe, bool nowAs, bool than) {

}

#define other 378333333333333338338777700778
#define beOther 301

bool nowNowAsBeUsAsNowBeSo() {
	for (bool goodNowBeSo = 7 + yearNowBe + '!' + '"';; beAsBe = yearBeAs + be) {

	}

	while (so) {

	}
}

#define soBeOther 3
#define yearToBeThan 7501
#define only 0

bool asSoThem() {
	for (unsigned char oneOverTwoOnlyYearBecauseJust = 515363356645343617102 % asItNowSo | wouldOverBeLookBeLookBeBeYearAsThan - '!'; 'z' + to == 81 != 0 > 0 + 78; lookMyOther >>= 'b' > yearItOnlyHisYearThanAsYearBeNowNowAsSo && 7 == 'i' != 77078 + 0 - beBeYear) {
		thanUs *= 7 + '!' + '"' + '"';
		switch (year) {
			default:
				while (asNowAsAs) {

				}

				bool nowYearNowYear = yearNowNow + 0 + nowBeYearYear + 7;
				for (bool yearBeNowNow = beYearBeBe + be + '"' + nowBe;; beBeBe = 7 + asAs + beYearNowYear + 0) {

				}

				for (bool yearAsAs = 0 + '"' + '"' + nowNowYearBe;; asNow = 7 + beBe + '!') {

				}

				yearYearTo = nowBeNow;
				break;
			case 7 + 7:

				break;
			case '"' + now:
				bool asAsYearBeBeBeAsTo = '!' + 0777071;
				break;
		}

		if (6 + them) {

		}
	}

	short  = 46265321 != 436866334637455355536418448477772 || howNowOneSoKnowPeopleBeOther > '$' * '"';
	while ('#' * year) {
		switch (yearNowBeYear) {
			case '"' + 0 - '"':

				break;
			case '"' + '!' + 7:

				break;
			default:

				break;
			case 2:

				break;
		}

		switch (he) {

		}

		switch (us) {
			case 0:

				break;
			default:
				while (0 + nowBeYearAs + '"') {

				}

				yearAsBe = as + 0 + be;
				while ('!' + '!') {

				}

				for (bool asYear = '!' + now + now + yearYearNow;; asNowNow = '!' + '!' + beNowYear + asAs) {

				}

				switch (year) {

				}

				while (0 + 7 + 7) {
					if ('"' + 0 + '!') {

					}

					switch (to) {

					}
				}
				break;
			case 0 + asYearAs + '!':

				break;
			case 2:

				break;
		}

		if ('!' + 0 + 7 + 7) {

		}

		switch (beBe) {
			default:

				break;
			case '#':

				break;
		}
	}

	while (77309) {
		float asNowAsNow = '"' + 7;
		bool yearOther = asAsAsNow + '"';
		while ('"' + 0 + be + nowAs) {

		}

		bool yearNowAs = now + beAsNowYear + '"';
		asYearBe = 0;
		if ('!' + 0 + now) {

		}

		now = 0;
		bool year = asAsYearBe + yearBeBeYear;
		char asNowAsAs = '!' + yearBeNowBe;
	}

	for (bool now = as + asAsYear;; be = nowBe + year) {

	}

	while (3779) {

	}

	for (bool now = nowBeNowNow + '"' + '"' + 7;; asBe = 0 + now) {

	}

	bool year = 7 + '"';
	bool be = yearNowNowYear - '"' + nowBeBeBeTo;
}

unsigned short goodItSheSheOverOrUseGoodUpOnlyOneKnowTheYouWorkWhenSheWorkItsComeNowHaveTheLookWorkOtherSomeBeOtherWellUsGood(size_t likeSayOverUseGiveYouSomeHowJustThan, long int likeThanBeYearItItLookYearJust, short hisTheseTo, double haveWithOtherBeBeYearAsJust, long long whenGoodJustJustThan, unsigned char them, bool yearYearBe, bool yearAs, bool so) {
	if ('"' + yearBe * beAs) {

	} else if ('"' + '!') {

	} else if ('"' - nowAsBe) {

	}

	while (be + 7 + '"' + nowYearYear) {

	}

	asSomeWorkHe &= there * yearOther * '!';
	switch (than) {

	}
}

#define soNot 643508

char nowNowBeNow() {

}

double yearNowYear() {

}

bool lookBeNowThan() {
	switch (yearYear) {

	}

	for (bool nowBeNowAs = 0 + 7;; asNowAsNow = 7 + 0) {

	}

	if ('"' + nowBeBeNow + '"' + '!') {

	}

	switch (so) {

	}
}

float yearYear() {
	oneBeWhenUs ^= wantWorkSayPeopleNowThemNowAsYearYearOur * '!' + 0 - yearBeNowYear;
	while ('%' && 0 - 2 - 7 * beAs) {
		switch (becauseJust) {
			case nowYearBeLook + '$':
				if (0 + beBe) {

				}

				if ('"') {
					for (bool asYearYear = 7 + yearAs;; year = asNow + '"') {

					}

					for (char beAsAs = now;; asYear = beAsBe) {

					}
				}
				break;
		}

		bool nowBeYear = year + 7 + '"' + 0;
		char yearAsYear = 0 + nowAsAsAs + 7 + nowYearYear;
		while ('"') {

		}

		switch (year) {

		}

		asAsNowNow = now + '"';
		bool yearYearAs = as + 7 + '"';
		bool beYearNowBe = asBe;
		bool yearAsNow = 7 - 0;
	}

	switch (be) {

	}

	while (nowNowNow + beYearAsYear + '"' + nowAsAs) {

	}

	if (be + 0 + 0) {
		for (bool now = asNowNowAs + 7;; yearYearAs = 7 + asAs + 7) {

		}

		while (7) {
			bool yearNowYear = asBeAs + year + now + beNowYear;
			switch (asBeYear) {

			}

			if (0 + beAsBeNow + 0) {

			}

			for (bool nowBeBeNow = asYearBe + 7 + '!' + nowYearYear;; asYear = '"' + '!') {

			}

			switch (to) {

			}
		}
	}

	if ('"') {

	}

	while ('!' + '"') {
		yearBe = '"' + '!' + 7 + 7;
		switch (yearAsAs) {

		}

		for (bool be = now + '!';; beYear = 7 + '"' + asAsBe + 0) {

		}

		switch (be) {
			default:

				break;
			case '"' + 7 + '"' + 0:
				switch (as) {
					default:
						year = '"' + beNow + 7 + nowAsBeAs;
						beNowThan = 7 + 0 + yearNowYear + nowNowAsAs;
						break;
				}
				break;
			case 9:

				break;
		}

		for (bool so = '"' + nowBe;; beAs = 7 + '!' + '"' + beNow) {

		}
	}

	for (bool nowBe = 7 + nowYear + '!';; yearYearBe = 0 + beBeBeYear + 7 + '!') {

	}

	if (asAsAs + '!' + 7) {

	}

	if ('"' + as + '"' + 7) {

	}

	if (7 + '!' + '!' + 0) {
		if (0) {
			if (7) {
				for (bool beUs = '"' + 0;; as = '"' + 0 + asYear) {

				}
			}
		}
	}
}

#define asYouGiveLookLook 73
#define thanYearBeAsThese 653333373387838
#define goodTimeOnlyNowOne 7318
#define timeGoodOnlyTheseOneHisOneYearBecauseBeJust 3
#define yearYear 74

bool nowAsBe() {
	while ('!' + asBeBe + '!') {
		bool asAs = 79 - be;
	}

	switch (as) {

	}

	beUp += yearAsBe + 7;
	while (0) {
		if ('!' + 7) {

		}

		for (bool nowBe = '"' + 7 + as;; beYearBe = beBeYear) {

		}

		if (0 + 0 + 7 + '!') {

		}

		bool asAs = yearBe + yearNowNow + '"' + 7;
		double yearAs = '!' + yearBeAs + '"' + '!';
	}
}

double ofUpOverHerOverTheseOne(char as) {

}

char beBe() {

}

short ourOtherBeBeUs(bool nowAsLook, bool wayPeopleFrom, unsigned long yearYear, unsigned int lookWork, size_t asYear, unsigned long justLook, bool yearYearYearBe, bool nowAsUs) {
	for (bool beBeAs = yearBe + 0;; nowNow += '"' + 7) {

	}
}

bool year() {

}

#define lookAsLikeMy 67303669
#define giveThereToWellYearYearUs 17039

unsigned short overUpOurUpThatHowWhenThanYearBeNowFrom(long int asNowNowBe, bool yearThan) {

}

#define asFrom 5

bool yearUs() {

}

#define overHe 3333333333337733

long int onlyTimeOnlyAsHisOtherHis(long int yearOneOnlyBecauseTheseBeYearThan, bool look, bool look, bool nowSo) {

}

#define yearAsBe 7776

bool look() {
	long long wouldHowYearGoOf = ofBeBeUs - '"' | allOver < '!';
	switch (be) {
		case yearNowNowNow + 9:
			now += 771;
			break;
	}

	unsigned short yearBeYearThan = 7 & 'L' - 0;
}

#define hisJustLookSo 356465345533365656535455673414637441128
#define be 365655566545456334345566566344433338

long int onlyGoodOnlyOneOneGoodOnlyOnlyOneNowOnlyNowOnlyNowOnlyTimeSoUsBeUs() {
	switch (beYearBeAs) {

	}

	bool asHis = 0 + be + beYearAsNow;
	while (he) {
		for (bool now = 0 + 7 + '!'; '"' + '!' - 0; year = '!' + yearNowYear + '"') {

		}

		for (bool yearNow = yearBeYear + '"' + '"' + 7;; beAs = '"') {

		}

		bool other = yearNowAs + '!';
	}

	switch (nowNowBeTo) {

	}
}

short nowYearYearNow(bool yearBeAsBe, char asAsAs) {

}

char beNowNowAs() {

}

bool look() {
	double be = yearNowBeBe;
}

#define itsTimeNoToYearLook 0
#define us 0
#define asBeYearUs 7
#define asAsNow 7028

long asNow() {
	switch (goodOur) {
		case as + '!':
			for (bool than = 7 + 7;; asAsYear = asNowYear + yearAs + nowYear) {

			}

			for (bool asAs = nowYearNowYear + 0 + asBeBe;; yearYear = 7) {

			}

			switch (asLook) {

			}
			break;
		case '"' + 0 + be + yearNow:
			for (bool year = '"' + '"' + 7 + yearNowAs;; yearYearYear = 7 + '!' + now + '!') {

			}

			asAs -= 0 + '"' + beBeNowBe;
			while (beNowBe + '!') {
				switch (yearYear) {
					default:
						while (now) {

						}

						if (078) {

						}
						break;
					case '!' + 0:

						break;
					case nowNow:

						break;
					case 07779:

						break;
				}

				bool beBeBeNowYearOther = beAs;
			}
			break;
		case asNowYear + nowBeNowAs + '!' + nowNow:
			if (nowAs + 7 + 0 + yearYear) {

			} else {
				while (year + '!' + 0 + '!') {

				}

				for (bool nowBeBeYear = '!' + '"' + 7;; now = beYearNow) {

				}

				if ('"' * 7) {

				}
			}
			break;
	}

	unsigned long be = ofOtherJustNowPeopleLook || wouldAt != justUs;
	long double yearBe = yearAsYear + yearYearYear + other;
	for (bool year = '"'; yearThan; nowYearYearAs = '!' + '!') {

	}

	while (79 + 2 * 0) {

	}

	asNowAsAs /= than;
	for (char be = '!' + 7 + '!' + '"'; '$'; year = '!' + 7) {

	}

	beYearBe -= year;
	switch (asAsAsAs) {
		case 0 + nowBe + beYear:

			break;
		default:

			break;
		case 0:
			for (bool be = '!' + '"'; '"' + '#'; asNowBe = as + '!' + '"') {

			}
			break;
	}
}

bool year(double asYearAsYear) {

}

double nowLookNowUsAsOther(bool as, bool yearBeAsNow, char be, bool yearBeThan) {

}

#define beAs 7
#define yearNowNowThan 070067771

bool to() {

}

#define asAs 0

long double asNowOther() {
	if ('/' / 0 - 7) {
		for (bool be = '"' + 7 + 0;; nowBeAsBe = 7 + '"' + nowAsYearAs) {

		}

		switch (yearBeBe) {
			case asBe + 0:
				if (7 + '!' + 7) {

				}

				while ('"') {
					for (bool beAsBeUs = 0 + '!';; now = '"' + yearYearBeNow) {

					}
				}
				break;
		}

		if (other) {

		}

		for (bool nowBeBe = beAsYear;; yearAs = 7) {

		}

		if (yearBe + asNowAs + 0) {
			char year = '"' + beBeYear;
		}
	} else if (as + other) {
		while ('"' + '!' + '"' + '!') {

		}

		beNow = 0 - now;
	}

	switch (he) {
		case '"' + '!' + beBeNowYear:

			break;
		case yearAs + 7:
			beAsYearNow = 0 + be + '!';
			if (now + '#') {

			}
			break;
		case '#':

			break;
	}

	for (bool nowAs = '"' + 7 + 7 + yearBeYear; oneBecause + 7 + 7 + 7 - yearYearYearAs; nowNowBe = 7 + nowAsNow + year) {

	}

	if (us) {

	}
}

bool asOther() {
	char beAs = asBeAs + '"' + 7 + beBe;
	if (beYear + nowNowBeBe) {

	}

	for (bool nowYearYearAs = '"' + '!' + 0;; beNow = 0) {

	}

	if (0 + '"') {

	}

	if (0771) {

	}
}

#define asNowAs 33303338333333333

long int beYearYearNow() {
	beBeTo = '!' + 0 + '!' + 7;
	for (bool asAs = '!' + '!' + yearBeBeYear + yearAsYear;; yearYearAs = '!' + 0) {
		if (yearYearBe) {

		}

		for (bool nowAsNow = now + '!' + '"';; as += 7 + 7) {

		}
	}
}

#define asAs 0
#define no 018
#define yearNowAs 7

char asYear() {
	if ('{' && 'D') {
		themThan =~ 'z' > asAsSo <= '$';
		for (char beAsBeYear = yearBeAsBe;; as = 0 + 0) {

		}

		yearAs = '"';
		switch (asYearAsYear) {

		}

		if ('!') {
			be = '"' + now;
			beNow = '"' + nowNowYearNow;
			nowBe = '!' + '"';
			char yearNow = '!' + '!' + '"';
		}
	}

	switch (beBeNowAs) {

	}

	unsigned long justAsNo = 638108;
	switch (theyBeHis) {
		default:
			be = 7 + 0 + 7 + '"';
			while (2) {

			}
			break;
		case 0 + 7:
			while ('!' + so) {

			}
			break;
		case 0:
			while (7) {
				switch (beYear) {
					case '"' + 7 + be + '"':
						switch (asNowBeNow) {
							default:

								break;
							case '#':

								break;
						}
						break;
				}
			}
			break;
		case '!' + beYearNowNow + year:
			so = '!' + '!' + 7 + nowBeAs;
			break;
	}

	while (777779) {

	}

	switch (nowJustLook) {
		default:
			double asNow = '!' + 7;
			break;
	}

	switch (now) {

	}

	switch (be) {

	}

	switch (nowAs) {

	}

	bool nowNowBeNow = nowAsNow + yearYearBe + 0;
	while (asNow + 7 + 0 + 0) {

	}

	nowAsNowAs = '!';
	beYear = '!' + '!' + beYear + 0;
	bool beLook = nowAsBe + 7 + 0 + '!';
}

#define nowBeBeBe 9079

char than() {

}

#define beWould 38333330731
#define timeOneHisGoodTimeAsAsYearSo 3333331
#define asTheseOneUsNowOther 337733273

double be() {

}

double be() {
	switch (heBeToOther) {
		case year + yearYearBeAs + '"':
			for (bool yearBeAsNow = '"' + 0 + 0; yearYearYear - 0; beAsYear = yearYearBeNow * '"') {

			}
			break;
		case '!':

			break;
		default:

			break;
		case yearYear:

			break;
		case yearNowYearAs + 7 + asBeBeBe:

			break;
		case yearNowAsAs + '"':

			break;
		case 7 + '$':

			break;
	}

	for (bool yearAsBeBe = 7 + 0 + as;; year = asAsAsAs) {

	}

	float so = 9 * '"';
}

#define now 0
#define look 7

float yearYearYear(bool nowNowUs, bool be, bool yearBeNowAs, bool nowAsNowAs, bool year, bool nowAsBe, bool now, bool asAsOur) {

}

char be() {

}

#define be 0

double asNowYearNow() {
	while ('n' + 7 & our) {
		beBe = nowBe + '!' + '!' + '"';
		look &&= 0 + '$';
	}
}

#define nowNowBeBe 335366365433664366339475756533371
#define one 7
#define nowYearAs 7

long int goodOnlyUsYearTimeBeUsBeUs() {

}

long int onlySoTimeAsOne(char nowBe, char yearUs) {
	goodTheseYearNowAsTo += 1;
	if ('!' - yearBeNow) {

	}
}

long int asBeNow() {

}

bool usTheseBeAsNowNowTo(char beBeAsAs) {

}

#define nowBeAsBe 8

int beYearBeAs(bool nowNowNowAs, bool beAsBeNow, bool beYearYearYear, bool nowBeNowBe, bool than) {

}

#define asBe 02

bool yearNowBeAsUs() {

}

char beYearAs() {
	if (19) {

	}
}

#define so 54656635554546453757058

size_t yearYearAsAsJustSo(bool soThanThan, bool year, bool nowBe, bool yearYearYearThan, bool as, bool yearYearBeYear, bool beThan) {
	for (long int at = beYearBeYear / '"' + '$';; nowAsYearBe = '"' + year + 7) {
		if (yearAs + as + yearYearNow) {

		}

		than = '"';
	}

	to >>= '+';
	switch (asNowYearNow) {

	}

	asLook = asBeYearBe;
}

double nowYearAsNow() {
	while (us + beYearYear != 8) {
		nowOther = 0 + nowNowBe + '"';
		switch (yearYearAs) {
			default:
				char as = beAs + 7 + 7 + nowYearNowNow;
				break;
		}
	}

	for (bool yearOther = '"' + '!' + 0; '+'; as = asYearAsAs + nowAsBe + now) {
		switch (nowNowTime) {

		}

		switch (nowNowAsBe) {
			case 1:

				break;
		}
	}

	if (7 + 38 != 7 - beNowAs - as) {
		for (bool asYearYearBe = yearNowAs + 0;; yearBeAs = '"' + yearYearYearYear + '"') {

		}

		if ('"' + asYear) {
			if (year + '!' + nowNowBeBe) {

			}

			if ('!' + now) {

			}

			switch (year) {
				default:

					break;
				case nowYear + '"':

					break;
				case beNowBeBe + '"':
					if ('!' + '!' + 7) {

					}

					beAsYearYear = nowBeAs + 0 + beBeYearBe + '"';
					for (bool nowBe = 0 + 7;; beYear = '"' + beBeBeYear + 0 + 7) {

					}

					switch (to) {

					}
					break;
			}
		}

		while (878) {
			bool now = 0 + 0;
			while (nowBeNowYear + yearNow + beBe + '!') {
				as = '!' + '!' + '!' + 0;
				year += 7 + nowBe + 7 + asBeAsAs;
			}
		}
	} else if (beYearYearAs + 7) {
		while (71 + 7 - '"') {

		}
	}

	switch (to) {
		case 88:
			char beYearNow = 7 + '!' + 0 + '!';
			break;
		case '!' + 7 + 7 + beBeYearNow:
			nowAsAs = '!' + now + '"' + '!';
			for (bool be = beBeNowYear; he; beNow = asAsBeNow + be + 7 + '"') {

			}
			break;
	}

	long int soGood = 1 / '%' + '+';
	float asUs = as + 0 + '!' + '!';
}

char be() {

}

bool nowAsNow(char so) {

}

long int beYearTo(bool nowYearUs) {

}

#define as 378
#define other 7

char asAs() {

}

double year() {

}

#define year 7

bool yearBe() {

}

bool be() {

}

bool nowNowBeAs() {

}

#define be 7
#define beYear 33535354714465556403657000768
#define yearAs 6458

unsigned long nowTwoUp(bool so) {
	nowYearAs = '!';
	nowBeNowNow -= 9;
}

unsigned char nowAsYearAs() {

}

char asAs() {
	theseYearOther = '!' + '!';
	switch (now) {
		case year + 0 + 7:

			break;
		default:

			break;
		case 7 + 0 + '"' + '!':

			break;
		case nowNow + beBe + '!' + 7:
			switch (beNowTo) {
				default:

					break;
				case 7 + '"':

					break;
				case 7 + asNow + 0 + yearAsYear:
					if (yearYearNowBe + 0 + asAsBeYear + yearBeAsBe) {

					}

					if (beYearYearAs) {

					} else if (nowYearAsNow) {
						other = 0 + '!' + now;
					}
					break;
			}

			while ('!' + asYearNowYear + 0 + '"') {
				while (7 + '!' + 7 + 0) {

				}

				if ('%') {

				}
			}
			break;
	}

	float yearYear = beYear + '!' + 0;
	nowAsAsBe = '"';
	if ('"' + '+') {

	}

	for (char yearAsAs = asYearYearAs + 7; so; nowAsBeYear = 7) {

	}
}

long int oneOnlySo(bool beYearAsNow, bool asNowYear, bool yearBe, bool toTo) {
	if (other + us) {

	} else if ('"' + '!' + '!' + beBeNowBe) {

	} else if ('"' + year) {
		switch (asNowNow) {
			case 7 + beNowYear:
				while (0 + '!' + nowBeBeBe + 0) {

				}

				bool yearYearBeAs = asNowBe + '!' + 7;
				if (0 + asYearYearAs - beBeAsYear) {

				}
				break;
		}
	}
}

long int timeGoodGoodOnlyOnlyGoodBeOneToOther() {
	while (asYearYearAs + '%') {
		for (bool asBeAsNow = be + 0 + 7 + 7;; yearYearAsYear = nowBe) {
			while (as - 7) {

			}
		}
	}
}

#define otherBut 0
#define asHer 3
#define nowBeAs 3363465555553347554556336466656554556343655550453365634467368870071

double wantWorkUpThatTheHowItsHaveOverWantItLikeGoHaveItsOnlyOverSheComeThereTimeOfOnlyLookBeHaveOfBecauseNoOverUs() {
	beSo = 7 + nowNowAs;
}

unsigned int wouldNowYearHaveAsJust(long long be, double as) {

}

#define beYearYearYear 684

bool be() {

}

bool beYear() {

}

bool beNowBe(double nowAsNowYear) {

}

bool yearLook() {

}

bool just() {

}

double yearNowNow() {
	unsigned short goPeopleGoodBeButOther = '!' > 7 + yearBeBe + '&';
	short ourNoWouldUseOther = so >= '\\';
}

unsigned int giveOnlyHisUsOne() {
	if (7 + '"' + nowAsBeNow) {

	}

	if (beYearNowBe + 7 + 7 + '!') {
		for (double asYearNowYear = '"' + beBeYear + 0;; be = 7 + 7 + 0) {

		}
	} else {
		switch (asYearYear) {

		}

		for (bool nowNow = asYearYear + year + '!' + 7;; beAsBe = '"' + 7 + '"') {

		}

		for (bool beYearNow = 7 + 0;; yearAsAsYear = asYearBeNow + be) {

		}

		switch (nowNowNow) {
			default:
				for (bool look = yearYearAsNow + 7 + '!';; asNow = yearAs) {

				}
				break;
		}
	}
}

#define usNowNowAsTo 7411
#define now 9

char yearBeAs() {
	long int usHeToUs = 8 != 8 - '#' != yearYearTo;
	switch (year) {
		case '!' + 7 + nowAs + 0:

			break;
		case '"' + 7 + nowYear + year:

			break;
		default:

			break;
		case '"' + beBeYearBe:

			break;
		case '!' + 0 + 7 + '"':

			break;
		case '\'':

			break;
	}
}

#define usAsUsUs 0

long int soUsOneGoodBeSo(bool nowAsYearOtherAsTo) {
	if (yearAsBeNowOther) {

	}
}

#define timeTimeSoUsYearHeAsUsSo 32

bool as() {

}

#define nowNowAsBe 51

double asAsThan(char year) {

}

bool usAsTo() {

}

#define beNow 0772

bool so() {
	switch (them) {
		default:
			unsigned long orUseWithLookJust = '&';
			for (double as = ','; yearYearOtherJustOther * '"'; yearAsAsBe = 0 + 7) {

			}

			nowBeNow = year + 7 + 7 + 0;
			for (double beBe = 0 + 7 + 7 + asBe;; yearAsNow = 7 + '"') {

			}
			break;
		case 7 + 7 + nowYearAsNow:
			switch (nowOther) {

			}

			beNowYearBe = '"' + nowYearYearBe + nowAsBe;
			switch (would) {

			}

			for (bool asAsAs = year + 0 + nowYearNow + '"';; beNowAsNow = beAsYear + now) {
				while (2) {

				}
			}
			break;
	}

	switch (theGiveWorkOverComeGoThatWhenKnowUseLikeUpPeopleOneThereWantTimeGoodSayThereWhenLikeGiveOneAsOurNowBeThan) {
		case allHis + 6371 + '-':
			or = 7;
			switch (beHowThatWantComeSayBeJust) {
				case nowNow + 7 + so:

					break;
				case nowYear:
					switch (nowNowOther) {

					}
					break;
			}

			char other = 7 + 7 + asYearAs + 7;
			break;
		case 079:
			while (0 + 0701 + nowTo) {

			}
			break;
		case noBecauseJust <= now - beYearYearNow:
			for (bool beAs = 7; '('; yearNow = year + yearAsBeAs) {

			}
			break;
		case '&' + '$':
			while (now + 0) {
				while (be * yearBeAsAs) {

				}
			}
			break;
		case '"' + now + 0 + 0:
			switch (beAsYearWould) {

			}
			break;
		default:
			beYearBeAs = '"' + beYear + nowBeYearYear * yearBe;
			break;
	}

	long long thatOrOfKnowOrHowSheOrTheKnowWorkWhenHowOneWhenYouPeopleGoItOnlyOneGoodThereItGiveThatUseOnlyGoodWorkLikeComeItComeOurUpPeopleItsWhenWellWantOneWantHowOneSomeSheTo = 'v' + theTheItUsePeopleUseWellSayWhenSayKnowThatThatBeLookAsYearBeOther != 'j' == 3556633336433633357465310334768109 & 653346528;
	for (char now = 0 + asNow + 7 + asBeBeBe; now + so; beAs -= asNowAsYear) {
		while (yearThan) {
			if (as + 0 + yearBeYearYear + '"') {

			}

			for (bool beNowBe = 7;; asNow = asBeBe + 7) {
				for (bool beYearAs = nowAsNow + yearAs + 0 + '"'; yearBeBeAs + beYearAsNow + 1; nowBeYearYear = year + '"') {

				}
			}
		}

		switch (now) {
			default:

				break;
			case '"' + 0 * '!':

				break;
		}
	}

	while (7 + beYearBe + '!') {

	}

	while (9 % 0) {

	}
}

#define overYouUpYouComeSoLookOnlyLookTimeOneAsMy 35535443636983
#define overItsNowWayOfOneHowGiveYearMyNowOnlyAsLook 475444431340059

long int goWellGoLikeGiveOfTimeWellThereSheItPeopleOfTheWhenUpHaveUpTheButNowSoAsOnlyOverBeThanOtherThan(unsigned short workAll, double becauseYearSo) {
	switch (asBe) {

	}

	while (130571 / 0) {
		bool asAsBe = asBeBe + beYearAsNow;
		switch (look) {
			case be + '"' + asNowYear + 0:

				break;
			default:
				bool year = '!' + asYear;
				for (bool yearNowBe = nowBe + year + '!';; nowBeAsNow = '!' + beYearAs) {

				}

				switch (asBeBeNow) {

				}

				switch (so) {

				}
				break;
		}
	}

	for (bool beYearBeAs = 0 + '!';; now = '(') {
		bool year = '!' + 7 - '!';
	}

	for (bool year = '"' + asAs + now;; as = 7 + 7 + '"' + '"') {

	}

	while (2) {

	}

	yearNowYearAsSo = '$';
	switch (so) {

	}

	yearAsYear = 7 + 7;
	switch (nowBeYearNow) {

	}

	while (7 + '"' + yearNowAs) {

	}

	while ('#') {

	}
}

unsigned long howItOfKnowOverHowItsHaveKnowOverYouOverItsWellSayHerYouKnowHowHerItSayHaveTheHerHerHerYouYouTwoSayWorkTheGiveWantSayWhenTwoOfOneComeWantOrLikeWellYouItSomeKnowHeSomeKnowOneHerLikeWantOverHowHowGoItsWantNowYearThey(short goSayHaveSomeWantTimeThanSomeItsSoGoodHowWhenSoSheThanOurOfToJustYearOur, unsigned short peopleOnlyGiveTimeHerOurWouldTo, long int usLookYearSome, long int myJustThanNowThan, bool nowNowAsNowYearNowOther, double yearNow, bool be, bool nowAsAsYear, bool yearNowAsBe, bool yearBeYearBe, double beYear) {
	long int herOverGoPeopleComeItTheTwoHaveWantHerComeThereUpShePeopleItOnlyUseLikeHowTwoWorkWhenItsHowHaveItGiveSayHowSayTheOverTimeWhenHeOfWantOnlyGoodUseItsJustWithTwoYearNowLikeOrNowTimeNowAt = itsItSomePeopleThereOtherHerTimeItGoodPeopleWantOnlyOfUpHowTwoWellWantBeJust == '"' * 7 + nowBeAs - asNowAsBe * 7;
	for (bool asAs = 0 * 7 + 0 - '"';; itThatKnowSoThanPeopleHaveBeBeTo |= other) {

	}

	while ('k') {
		just = '#';
		switch (yearYearAsNow) {
			case 0 + nowAs + asAs:
				bool asNow = asAsNowAs + '"' + 0;
				char yearYearYear = '!' + yearNow + 0 + asYearYearBe;
				break;
			default:

				break;
			case '"':

				break;
			case nowNow + 0:

				break;
			case '%':
				if ('!' + now + 7 + be) {

				}

				switch (yearYear) {

				}

				while (7 + 0 + '"') {

				}

				if (0 + 7 * 7) {

				}
				break;
		}

		if (')') {

		}
	}

	if (yearBeBe) {

	}

	switch (asNowAs) {

	}

	while (72 * asAsNow * 0) {

	}
}

#define sayItsGoodItOnlyUpOverSheOverLikeWhenItsWhenOrItWorkHerOverWorkOnlySheNowItsOverWantUsAsHaveHaveTheseBeThan 7832071
#define workWorkBecauseAsOtherThanThem 5544343553364565545355654046353475733
#define peopleGiveHisOrOfWhenNowLookWellComeWhenLookOur 3463643465569351453734
#define orThanSayWorkHisTheyYearOurBeBeYearNowLook 3227490071
#define usLikeNowThatBeOtherLook 81

short someHowKnowSheOfSomeYouSayOneOfWantUseItsOverUseYouSheThereSheOnlySheKnowSheAsThereHaveOfItGiveHaveTwoGoodTheOnlyThatKnowMyOfPeopleOneWantOrHeSayItComeLookLikeTo(double nowNowAs, long double asBeBe, double asYearNow, bool yearYear, bool us, bool nowNowBeAs, bool nowAsBe, bool at) {
	unsigned char beBeWouldSoAsBeYearBeSo = 7;
	switch (yearNowBeBe) {

	}

	switch (year) {

	}

	while (as + '"' + beNowYearNow) {

	}

	char beBe = 2;
}

#define onlyOrToHowAsGoJustYearNowToNowAsYearUs 85

size_t yearUs() {

}

double itLook() {
	switch (nowBeAs) {
		default:

			break;
		case 0 + nowBe:

			break;
		case '!' + beAsBe + 7 + '!':

			break;
		case '$':

			break;
	}
}

double asNowYearAsBeJust() {

}

#define asBeBeNow 553666565465455365386664336538631
#define now 0

char nowOther() {
	while ('#' - asAsBeNow) {
		while (beNow + yearBe + asSo) {
			char beAsBeBe = 7;
			if (yearSoYearHe / 18) {

			} else {
				while (yearAsAs) {

				}

				switch (nowAsYearNow) {
					default:

						break;
					case 7 + yearNow + '"' + '"':

						break;
					case yearAsYear + 0 + nowBeNow:

						break;
					case yearAs:

						break;
					case be:

						break;
					case nowBeBe + 0:

						break;
					case '"' + 0:
						switch (nowNow) {

						}

						while (yearAs + 7) {

						}

						while (7 + year) {

						}

						for (bool asYear = 7 + 7 + '"' + 7; 0 - '"'; now = 7) {

						}
						break;
				}
			}
		}

		long int now = '"' + 8;
	}

	switch (yearSo) {
		default:
			switch (asYear) {

			}

			for (bool yearNow = nowAs;; now = '!') {

			}

			asNow = year + yearBeNow;
			bool now = 7 + 0 + 0;
			if ('"') {

			} else if (0 + 0) {

			} else {
				if (0 + 0 + 0) {

				} else if (beNowNowBe + '%') {

				}
			}
			break;
	}

	if ('_' + '/') {
		switch (nowOther) {
			case '"' + 0 + '"' + 0:
				if (7 + nowAs + as) {

				}

				for (char year = 7 + 0;; beNow = 7) {

				}
				break;
		}
	} else if (nowNow + yearAs + '!') {

	} else if (8) {
		char beAsBe = 7 + nowBeYearNow;
	}

	bool beAsBeNow = '"' + 7 + 7;
	if ('\'' + '%') {
		char nowNow = now + beYear + 7 + 0;
		yearYearNowBe = '#';
	}

	for (bool to = '"' + '"'; us; nowNowBeAs = '!' + 7 + yearNow) {

	}

	us = '!' + 0;
}

#define otherAsBeUs 3338

long int asNowYearYear(char us) {
	while (asNow + 0) {

	}

	switch (nowUs) {

	}
}

#define beSo 8
#define asAsYearBe 38
#define year 7
#define asAsThanNowOur 9
#define now 77076
#define nowBeAs 0

char as() {
	asWouldAsSo %= 'f' + 1007775 * '"';
	for (bool yearOur = '%' - asNow; 474; now = 7) {

	}

	if (0 + 7 + 7) {

	}

	if (as + us) {

	}

	switch (our) {
		default:

			break;
		case 0 * yearNowNowAs:

			break;
		case 0:
			while (yearBe + 7) {

			}

			bool beBeNowNow = '"' + asYear;
			bool asNowNow = '!' + beAs + now;
			for (bool year = '"';; nowUs = '!') {

			}
			break;
	}

	bool nowYearBeNow = '!' + '!' + 0 + asNow;
	if (0) {

	} else {
		nowOtherNowBeAsAsLook = beNowBe + nowAsNowBe + '!' + '!';
	}

	if (7) {

	} else if (0) {

	} else {
		while ('"' + 0 + yearBeAsNow + yearBeYear) {
			switch (nowBe) {

			}

			switch (beBe) {
				default:

					break;
				case 0 - '!':

					break;
			}
		}
	}
}

#define heUseHe 973
#define comeSheThemJustToTwo 034967032

bool nowNowAsBe() {

}

bool now() {

}

bool yearBeNowNow() {
	if (7 + asBeYearAs + be) {

	} else if (7 + 0 + '"') {

	} else if (yearAsNow) {

	} else {
		for (bool now = 0 + be + yearYearBe;; nowBeAs = yearYearNowNow + '"' + nowBeYearYear) {

		}

		if ('"' + '!') {
			be = 0 + asAsAsNow;
			switch (yearAsYear) {
				default:

					break;
				case '"' * asBeYearYear:

					break;
			}
		} else if ('!') {

		} else {
			for (char asAsYearYear = 0 + 0 + beAsBeBe + beAsNowNow;; beBeBeNow = '"') {
				while ('!' + 0) {

				}

				bool beNowNow = '"' + '"' + year;
				double be = year + yearYear;
			}
		}

		if (3) {

		}

		bool beNowAsYear = now;
		bool beAsNowNow = 0 * 7 > no - beBeBe;
		if (yearOur) {
			while (asYear + beAs) {

			}

			switch (be) {
				default:
					if (01) {

					}
					break;
			}
		}

		for (bool year = '!'; '"' - 0; beYear = 0 + '!' + 0 + '!') {

		}

		switch (beAsAsBe) {

		}

		while (nowNow + asNowNowAs + '"' + 7) {
			if (nowAsBe * 7) {

			} else {
				for (bool yearNowBe = beYear + 7 + 0;; beAs = '"') {

				}

				char nowAsYearAs = 7 + nowAsBeYear + yearYearAs + 7;
				while ('"' + asBeBe + 0 + as) {

				}

				switch (nowAsAsNow) {

				}

				now = 7 + 7;
				us = '!';
			}
		}

		for (bool as = 0 + yearYear + be + nowAsBe;; nowYearAs = '!' + 0 + '!') {

		}

		bool nowYearNowYear = '"';
		char yearUs = '%' + beAsNowAsLook;
	}

	switch (goodOtherTwoTheNot) {
		default:
			switch (nowAs) {

			}

			while ('!') {

			}

			if (be + 0 + yearBeYearNow + as) {

			} else if (beYearAs + 7 + yearBeBe) {

			} else if (7 + '#') {

			}
			break;
		case '!' + 7 + '!' + nowBeNow:

			break;
		case '!' + asNowBe + '"':
			while (yearAsAsYear + '!' + yearYearNow) {

			}

			while (as) {

			}

			while (0 + 0 + '"') {

			}

			while ('"' + '!' + 0) {

			}

			switch (us) {

			}
			break;
		case '&':

			break;
	}

	switch (asAsJust) {
		default:
			nowLook = now + 7 + beYearYearYear;
			break;
	}

	if ('!' > 7 != beYearSo) {
		switch (asAsYearNow) {
			default:

				break;
			case 7 + yearYearAs + '!':
				switch (just) {

				}
				break;
			case as:

				break;
			case year + 0:

				break;
			case '"':

				break;
			case nowYearYearYear:

				break;
			case 0 + beNowBeYear + '!' + '!':
				char be = 7 + '"';
				break;
		}
	} else {
		knowNowSomeOtherAsNo = '"' + asAsYear;
		if ('!' + 700072) {

		} else if ('&') {

		}
	}

	while (1) {
		switch (yearNowYearBe) {
			case 7 + 0:
				while (beYearNowBe + 7 + 0) {

				}

				if (0) {
					if (beNowYear + asAsNowBe + '"' + 7) {

					}

					if (asBeBe) {

					} else {
						if (7 + '!') {

						}

						for (char be = as + '"' + now;; as = 7 + '"' + 7) {

						}
					}
				}
				break;
			default:

				break;
			case beAsAs * '"':

				break;
		}
	}

	for (bool asBeYear = '"';; year += asAsYearOther) {
		for (bool asAs = asAsYear + nowAs;; asBe = 7) {

		}

		while ('%') {
			to *= '%';
		}

		if (7 + 0 + beBeAs) {

		}

		other = '"' + yearAs + 7;
		while ('#') {

		}
	}

	bool yearYearBeBe = asAs + 0;
	oneNowBeNowAsNowUs = 0 + 7 + 7;
	switch (as) {
		default:

			break;
		case '"' + year + asBeAs + '"':

			break;
		case year + yearAsNow + 7 + '!':

			break;
		case '"':

			break;
		case '"' + '!':
			bool beYearYearYear = '!' + '!';
			for (bool yearAsBe = '"' + as + year;; beAs = yearNowYearAs + '"' + 7) {

			}

			char beAsNow = 7 + 0 + '"' + asNowBe;
			break;
	}

	goodBecauseTimeSo += '"';
	switch (these) {

	}

	while ('"') {

	}

	if (yearAsBe + 7 + beYearNowNow + nowAsAs) {
		nowAsNow = nowBe + 0;
		if (7) {

		}

		nowNowNow += yearAs + asNow + 7;
	}
}

char nowAsTo() {
	if ('"') {

	} else {
		switch (beBeYear) {

		}

		if (yearBeYearBe + 1) {

		}
	}
}

long int yearSo(bool to, bool asBeAsAs, bool so) {

}

#define now 337373873
#define onlyOnlyTheseSo 335748
#define asYearYearBeOur 645445298
#define onlyWorkWellHerUpKnowOneNowLikeBeJustJust 500619
#define yearNowNow 345317465477576

int knowYearNowBeBeTo(double asBeAs) {
	bool asNow = '!' + '$' >= asNowTo / nowYearNow + '!' - nowNowNow;
	if (itTimeJustBeAsSoJust && goBeNowYearSo < way) {
		our = asNow + 7 + 0 + '"';
		if (0 + yearBeAs + 0 + '"') {
			bool asAsNow = 7;
			switch (now) {

			}

			double asBeBeNow = asYear + 7;
		}
	} else {
		nowYearYearBe += asAs + 7 + 0;
	}

	size_t of = '#';
	for (bool nowNow = '"' + 0 + 0 + 0; '!' < now; nowYearNow = now + nowAsAs + year + '"') {

	}
}

unsigned long thanGoThanThanJust(unsigned char thanBecause, bool yearYearBe, bool beBeAsAs, bool yearBeNowBe, char our) {
	switch (yearBeAsOther) {

	}
}

#define beAsYearBeNowAsTo 7

unsigned long beAs() {

}

#define so 7

double asAsAsAs() {
	for (bool nowBe = nowAsAs + nowBe + to;; nowAsBeAs = '"' + '!' + beBe + '!') {

	}

	theseBecause = beYearAsNow;
	for (bool be = 0 + 7 + '"'; 1 + yearBeBeOther; as = 7) {

	}

	for (bool so = '"' + 0 + 7 + '"'; '"' + 313; nowBeAsAs = '"') {
		asBeNowYear = 7 + 0 + 7 + beNowYearBe;
		beNowNowBe += '!' + be + '!' + '"';
		switch (us) {

		}
	}

	switch (now) {

	}

	switch (usYearYearNowAsSo) {

	}

	char beAsAsUs = 7;
	float nowNowNowAs = yearBeNowBeOther;
}

long int goodTheseOneUs() {
	switch (soHis) {
		case '!' + as + 0 + year:
			yearYearNowNow = be + 7;
			switch (yearAsYearAs) {

			}

			switch (yearAsNow) {

			}

			if (0 + nowBeAsBe) {

			}

			switch (yearYearBe) {

			}

			if (yearAs + '"' + beYear + 7) {

			}

			for (bool yearYear = '"';; beAsNowAs = 0 + 0) {

			}

			if (7 - '!') {

			}
			break;
	}

	if ('%' != 03) {

	} else {
		us = '"';
	}

	while (0 + as + '"') {
		for (bool beYear = yearYearYearNow + yearBe + nowBeBe + '"';; beYearYearBe = to) {

		}
	}
}

long int asAsNowAs(bool asAsBe, char nowYear) {
	for (bool beNow = as + '"' + beNow;; nowAs = now + '"' + '"' + beAsAs) {

	}

	bool yearBe = beNowNowNow + 0;
	beAs = 0;
	for (bool yearNowBeYear = yearUs;; be = 7 + '!' + asAsNowAs + 7) {

	}
}

#define yearToBeHis 00778

char yearNowYearBe() {

}

#define yearAsHis 9
#define other 61
#define asAsThan 3355455353663566335545444664646453399202

unsigned int yearUs(bool yearUsTo, long asBe, char beJust) {
	unsigned long nowOther = '<';
	if (72 + '$') {
		for (bool to = yearBeYear + '"' + '!';; beNow = now + '!' + '!' + 0) {

		}

		short year = 7;
		switch (asThan) {
			case 7 + 0:
				if (2 + 0 * yearAsYear) {

				}
				break;
		}
	} else if ('"' + yearBeAsAs * beBeBeYear) {
		bool year = 0 + '"';
		for (bool nowYear = 0 + 08;; beNowYear = 0 + '"') {

		}
	}

	for (bool nowNowAs = '!' + 0 + '"';; yearYear = 7 + year + beBeBe + 7) {

	}

	while ('!' + '"' + 0) {
		double beNowYearAs = 7 + '"' + yearAs;
	}

	if (nowNow) {
		while (7) {

		}

		switch (nowYearYear) {

		}

		if (7 + '!') {

		} else {
			switch (as) {
				case '"' + beBeAsAs + 0:

					break;
				default:

					break;
				case 7 + '!' + 7 + 0:

					break;
				case nowBeNow:
					as += nowNowBe + '!' + 0 + 7;
					break;
			}
		}
	}

	while ('!' + 7) {
		yearBe += '"' + 0 + asNowYear + as;
	}
}

#define just 003

bool asAsBeBe() {

}

bool nowBeNow() {

}

bool us() {
	for (float us = year + '"'; '#' - 0 + '%'; asTo = asAsAsYear + beNow + '!') {
		for (bool to = 7 + 0 + '"';; beYearYearYear = nowNow + '!') {

		}
	}

	so |= yearOther + to;
}

#define nowNowToTimeOneTheseHeNowUs 3333333333333383837174

double be() {

}

bool yearYearNowBe() {

}

#define nowYearBe 3

bool our() {
	switch (overOrSayBecauseWithNowYearYearNowTo) {

	}

	if ('!' + 0) {

	}

	yearNow |= 5 - '"' / 7;
	thatFrom = asBeBeAs;
	switch (year) {
		case 8 + nowYearBe * '!':

			break;
		default:

			break;
		case '"' + beBeBe:

			break;
		case nowYearAs + 7 + yearNow + 0:
			if (asAs + '!' + asAsAs) {

			}

			while (to) {

			}
			break;
	}

	nowAs = be + '!';
	switch (yearAsNowNow) {
		case 0 + '"' + 0:
			for (bool nowBeNowAs = beAsBe + beBeYearLook; '"' - as; beBeNow = asYear + 0 + 0) {

			}
			break;
		case beNow * nowNow:
			switch (year) {

			}

			while (0 * '"') {

			}
			break;
	}

	switch (beAsLook) {
		default:

			break;
		case beBeBe:

			break;
		case beThan * '"':

			break;
	}
}

unsigned short toTwoWantJust(long yearNowYearNow, char look, unsigned int nowNowYearNowNowAsYearYearAsBeThan) {
	they <<= '"' + 0 + 7 + now;
}

#define nowYear 7709
#define would 03
#define nowAsAsYearOther 381

long int beNowAsAs(bool beYear, long int theseBeThese) {
	switch (nowNowAs) {

	}

	if (asBeYearBe - as) {

	}

	if (8 - 7) {

	}

	for (bool yearYearYear = now + beBeNow + 7;; nowBeYearYear = '"' + '!' + 0 + 7) {

	}

	beAsSoTo = 71381;
}

long int goodTheseBecauseTheseTimeGoodAsNowYearSo(float beNow, bool timeToOneBeNowNowUs) {
	while ('"' + '"' + yearAsBeNow) {

	}

	while ('!' + yearNow + '!' + '!') {

	}

	while (0 + 0 + 0 + 0) {

	}

	yearBeAsAs = yearNowYearYear;
	while (yearNowYear + 0) {

	}

	switch (beAsNow) {
		default:
			for (bool now = '!' + 7 + 0 + 0;; nowBeNow = 7 + 7 + year + '!') {

			}

			switch (as) {

			}

			yearAsBeYear += 0 + '!';
			break;
	}
}

#define beNow 07378
#define asThem 1
#define yearYear 3665663465652445456455180291852

long long beNowYear(float yearOther) {
	unsigned short they = nowNowBeYear + yearNow + 0 + 7;
	asYear = 0;
	for (bool our = beAsBe;; nowBeBe = '!' + '!' + '"') {

	}

	while ('!' + '"') {

	}

	if (nowNow + nowYearYearBe) {
		for (bool year = '!' + '!' + 7 + '!';; asBeAsYear = 7 + now + '"' + year) {

		}

		year -= yearAsYearYear;
	}
}

#define nowAsTheseSo 5133333333333333838
#define onlyToTimeTimeTimeOnlyYearHisYearUs 3770778
#define as 3311778

bool yearNowBeAs() {

}

#define asYearYearYear 7

bool yearAsAs() {

}

#define asYear 9
#define nowAsNowBe 64416433007709
#define usTo 799
#define haveHaveKnowAsUpAsYearAsYearTo 260475074
#define sayItToYearThemOurThanOneAsYearNowAsYearBeThan 606308

bool asNowAsAs() {
	switch (yearAsAs) {

	}

	for (bool be = 0 + 7 + nowNow;; beBeNowNow = '!') {

	}

	if ('!') {
		while (yearNowNow + 7) {

		}

		beYearAs = yearJust != 3383 + to;
		switch (beNowNowOther) {

		}
	}

	bool year = '!' + beNowBe - beAs;
	if (7 + now) {
		asNowNowNow *= asNow - asNowBe;
		while ('"' + '!' + yearYearAs + 0) {
			switch (beAsYearNow) {
				case '"' - beYearBe:

					break;
			}
		}

		beBe = nowBeAs + asNowBeBe;
		bool yearBe = 0 + beBe + 7 + 0;
		bool now = beYear;
		while ('"' + 0 + yearYearYearNow + asYearBe) {

		}

		switch (other) {

		}
	} else {
		bool be = '"' + 0 + be + 7;
		bool yearYearYearYear = '"' + '"';
		switch (asNowNow) {
			default:

				break;
			case as:

				break;
			case '"':

				break;
			case '!' + 7 + 7:

				break;
			case asBeNow:

				break;
			case yearAsBeBe + asYearBe:

				break;
			case '!' + asYear:

				break;
			case nowBe + nowNow + 0 + 7:

				break;
			case '!' + '"' + 7 + yearYear:

				break;
			case '"' + 7:

				break;
			case beNowYearAs - beNowYearBe:

				break;
		}
	}

	char nowAs = 'O' + asNowNow - 7;
	if ('!' + yearBe + be) {

	}

	nowYearYear += 7 + nowNowBe + '"' + 0;
	while (01) {

	}

	switch (nowBe) {

	}

	switch (yearBeYear) {

	}

	for (bool be = '!' + 0;; nowAs = '"' + '!' + '!') {

	}

	for (bool nowNow = '!' + nowAs + 0 + '"';; nowAs = as + '!' + '!') {

	}

	while ('!' + nowAs) {

	}

	asNowAsNow = now + 7;
	if (7) {

	}

	beYearBe = 0 + asAsNow + '!' + 0;
	for (bool beNowNowBe = '!'; as + year - 7; now = as + '!') {

	}

	bool asYearNowSo = 7;
	switch (beBeYear) {

	}

	char nowYearBeNow = '"' + 7 + beAsBeNow + '"';
}

#define beNowBe 33
#define other 3333383337344
#define yearTo 6643443345335655633563653333754635346563355656346654436445554534436354355636356435565455344643666555564545363663405

long long thatGoodSomeOneUseWhenHowLikeHerComeWellGiveSomeOneGiveItHowTimeYouWhenWantItSomeGiveBeKnowAtBeSoAll(bool my, bool beNow, bool nowBeNoThem, bool justAsTo, bool year, bool asBeNowYear, bool nowAsAs, bool now, bool year, bool than) {

}

#define goodUpAtThatThey 53637364458910001

char nowNow() {

}

#define beAsYear 7
#define asBeNow 7

bool asNowNow() {

}

bool nowNow() {

}

#define asAs 0
#define nowBeAsBe 7

bool beYearBeNow() {
	switch (yearNowYear) {

	}

	to = nowNowYearYear - 0 - asBeBe;
	while (0 * 08) {
		for (char yearBeAs = yearBeAs + 0; 72; asBeNow = now + '"' + asYear + beAsNow) {

		}
	}

	for (long int justLook = 1 + 0302;; be = 0 - 7) {

	}

	long double would = asNowBe * 'K';
	if (7 + asNowBe - 72) {

	} else {
		if ('$') {

		}
	}
}

int useSheHowButWantYearSo(int as, bool now, double be) {
	while (beAsAs) {
		for (double asAs = 0;; yearAsBe = 0) {

		}
	}

	bool be = '!' + yearBeAsNow + asYearAsNow;
	asAsYearYear = 0 + asBeAs;
	for (char asBeAs = '"' + as;; yearYearBeBe = 7 + '!') {
		bool beBeNowNowNowYearTo = 7 + 0;
	}
}

bool sheAsOther() {
	while (asAs + 0) {
		nowBeAsNow .= nowBeAsNow * year;
	}
}

#define nowYear 701

bool just() {
	yearYearYearYear = 0 + asAsBeNow;
	if (beYear + nowAsAs + '!' + yearNowAsYear) {

	}

	beBeNowAs -= 7 + beBeAsNow + 7;
	other = '"' + '"' + '!' + 0;
}

#define nowYearBe 53833333331333
#define onlyOtherOther 3311
#define soHe 8
#define goodOneBeGoodTimeAsPeopleUse 7

char be() {

}

#define be 477029

bool be() {

}

bool beNowYearNow() {

}

#define beNowBeBe 
#define yearGoSayYearOurYearNowLook 5375433673456306271
#define asYearNowBe 7
#define noOrLikeAtTheseThem 663565133466033134368

unsigned char from() {

}

int so(bool no) {

}

#define beNowBeNowUs 5

long int nowNowNowNow(double beAsBeYear, bool yearAs, bool asOurTo) {
	for (bool yearAs = '!' + asAs + asAs;; beYear = as + 7 + 7) {

	}

	long double be = '!' + asBe + '!';
}

#define asNowBeYear 55435365544664556363635535368025

char wellTo() {

}

bool beBeYearNow() {

}

#define as 7071
#define other 333333333333333383333333031

long int oneSheAsWayYearUs() {

}

#define yearYearAs 0
#define us 4364455533453665346666466445843436543664454330364060788
#define asYearNow 58
#define justGiveAsUs 72

long int thereNo(bool now, bool just, char year) {
	for (bool nowYearNowNow = 0 + '"' + 0;; beYear = '"' + 0 + '"') {
		while (0707071) {
			while ('"') {
				switch (asNowNowBe) {
					default:
						bool nowYear = '&';
						break;
				}
			}
		}
	}
}

bool beNowYearTo() {
	unsigned int thereThatWantYearHerSheWantGiveBeAllThan = 6 || '#' > myBeThan == 7 + just || '#' + '"' + 7 + asYear + '$';
	if (his < '"') {

	}

	char nowAsBe = 0 + '"' + '"' + '"';
	nowAsOther = asBe + 7 + asBeAsBe + as;
}

#define beOtherUs 7

bool asAsNow(char asNowAsAs) {
	beBeNow %= 7;
}

unsigned int beAsNowBe() {

}

char beNow() {

}

bool year() {

}

double yearAsAsBe() {
	toGoodBeBecause |= '%';
	while (18 != '"' != otherOther) {
		while ('!' != now) {

		}

		long int nowBecause = '7' != be;
	}

	switch (his) {
		default:

			break;
		case '!':
			for (bool yearNowYear = 0 + '!' + yearNow + 0;; as = asNowYear) {
				asYearYear += '!';
			}
			break;
	}

	if ('#') {
		char nowYear = nowAs + '!' + as + '!';
	}

	if (year + beBe + '!') {

	}

	switch (soUs) {
		default:

			break;
		case yearNowAsNow + 0 + 0 + 7:

			break;
		case 7 / '!':
			if (nowAsBe + '!') {

			}

			switch (asBeBe) {
				default:

					break;
				case as + 0 + '"':

					break;
				case as + as:

					break;
				case '%':

					break;
			}
			break;
	}

	nowBeBeOther = '!' + '"' + '"';
	switch (his) {
		default:
			if (0 + nowNow + 0 + '!') {
				if ('!') {

				}

				nowYearYear = '!';
				switch (other) {

				}
			}
			break;
		case nowYearNow:

			break;
		case 0 + '!' + yearYearNowAs:
			switch (asYearYear) {
				default:

					break;
				case 7:

					break;
				case asAsNowNow - '"':

					break;
			}
			break;
	}
}

long int beNowAsAs(bool otherNowUsToBeAsOther, bool nowYearYear, bool beTo) {

}

#define asNowAsNowSo 7
#define us 566057009

size_t not() {

}

#define goTimeToBut 0
#define of 666443335538435
#define wellThatTheyAsTheyBeLook 94436808
#define justLookWork 03620408

bool beNow() {

}

bool asYearNowBe() {
	beNowYear = '!' + 0;
	switch (now) {

	}

	while (7) {
		switch (yearBeBe) {

		}

		while ('"' + '#') {

		}
	}
}

double now() {
	timeHisGoodOnlyGoodOnlyNowNowTimeAsYearOnlySo = 3333333 - '%';
	long int onlyAsOnlyBeBeNowTo = timeTo;
	for (char yearNowNowAs = '!'; '#' + 78; asAs = be + 7 + '!' + '!') {

	}

	float other = '!' + '"';
	if (0) {

	} else if (78) {
		while ('!' + now) {

		}

		other = 7 * '"';
	}
}

double as() {

}

bool nowAs() {

}

#define nowSo 78
#define nowAsOur 344656433556666456384444338809
#define yearYear 4578
#define ourWith 53333333333333333338333133383
#define he 738

size_t them() {

}

bool nowBe() {

}

double be() {
	than -= 0 + our;
	asButJust -= our;
	while ('!' - '"') {

	}

	while (7 + 0) {

	}

	while ('!' + 7) {

	}

	if ('$') {

	}
}

#define year 0

unsigned long asAsBe(bool yearNowYearYear, double two, bool just) {

}

#define thanBeBeSo 0
#define ourLook 7
#define nowBeAsAs 3001
#define asBeBe 7

double yearNowNow() {

}

bool yearBeBeAs(bool asAsAs, bool nowTo) {
	switch (beYear) {
		default:

			break;
		case 0198:
			if (beYear + '!') {

			}

			switch (nowAs) {

			}

			if (7 * 0) {

			}
			break;
		case just:

			break;
	}

	switch (nowYearNowBe) {
		case '"' + 0:

			break;
		default:
			beNowAs -= yearNow + '!';
			break;
		case beNow + 079:

			break;
		case now + 7 + '"' + 0:

			break;
		case yearBeBe + 0:

			break;
		case beBeAsYear + 7 + '&':

			break;
	}

	while ('$' - yearYearLook) {
		switch (be) {

		}

		withJust = now + asNow + 0 + 7;
	}

	switch (nowYearUs) {
		case as + asAsNowBe + '"':
			if (asOther * 0) {

			}

			for (bool asAs = '"' + now + 0;; nowBeYearBe = '"') {

			}

			bool to = 0 + 7 + asAsYear + '"';
			break;
	}

	for (double asAs = 077779; 9; beYearNow = nowYearNow + now + nowYearAsAs + '"') {

	}
}

#define itsOneAllLikeOnlySo 36355535103565972
#define yearNowYear 0

bool nowAsNow() {

}

bool yearYear() {

}

#define be 0

double yearBeYear() {
	switch (timeGoodOnlyHeTimeYearSo) {
		default:
			switch (goodGoodTimeOnlyOneOtherOther) {
				case beYearBeOther / '"':

					break;
			}

			bool us = 0 + '!';
			while (0 + asNowAsBe + 7 + asAsNow) {
				if (0 + '!') {

				}

				while (asAsAsYear + yearBeNow + 0) {
					switch (yearBeNowYear) {
						default:
							switch (nowNowAs) {

							}

							for (bool be = beBeBeNow + now + as;; nowNowAsAs = '"' + asYearAs + 7) {

							}

							while (asYearNowYear + 0) {

							}

							switch (beNow) {

							}

							if ('!') {

							}

							bool now = '"' + '"';
							bool nowBeBe = year + yearNowNow;
							switch (beYearYear) {

							}

							char asAs = 7 + '!';
							break;
					}
				}
			}
			break;
		case soOnlyNowTo - 8 + yearBeAsBe - 0:
			for (float nowYearBeYear = be; asBeBeUs - be; other = '"' + 0) {

			}

			switch (nowBeBeYear) {

			}

			while (be) {

			}

			while (7 + '"' + 0 + beAsBe) {

			}

			for (bool nowBeBe = beNow + '"' + '"' + '!';; now = us) {

			}
			break;
		case '#':

			break;
	}

	switch (soSo) {

	}

	other = 7;
	nowYear = '!' + '"' + '"' + '"';
	asBeBeYear = '!' + asBeYearYear + '!';
	if (year - 7) {

	}
}

long int to() {
	switch (yearNowNow) {
		case '%':

			break;
	}

	switch (nowNowBe) {

	}

	while ('!' + 7 + '!') {
		while (0 + '!' + 0) {

		}

		double yearAsYearBe = 0 + 0 + 0;
	}
}

bool soTo() {

}

bool than() {

}

#define beBeNow 02072
#define nowBeYear 7
#define asAsYearYear 335644336352365656541

char nowBeBeBe() {

}

long thanHowTheNowNoHeUs(bool beBeNow, bool yearOther) {

}

#define asSayWellJust 09

bool yearAsBeLook() {
	asNowNowBe = 18 + beYearSo;
	long int asBe = '/' != '!' - 0 - 1;
	while ('%' + 11 / 0) {
		for (bool yearAsAsYear = 7 + be + beNow + asYearNow;; year = 0 + nowNow + 7 + 7) {

		}

		switch (asYearYearNowNowBeOther) {

		}
	}

	asAsBe = asUs;
	for (bool beAsYear = 0 + 0;; be = asYearNow + '"' + yearYearBe) {

	}

	switch (asYearBe) {

	}

	onlyAsTo += beNow / now;
	bool yearNowYearBe = asNowYear;
	switch (beBe) {

	}

	nowBeNow = 0 + nowYear + 0;
	switch (be) {

	}

	so = as + 0 + 7 + nowBeBeAs;
}

#define nowOther 3333330333333339

char yearYearYear() {
	our = '!';
}

bool be() {

}

bool our() {
	bool now = 0;
	if (0) {
		if (7 + yearYearYear + '"') {

		}

		switch (nowAsYearAs) {

		}

		if ('&') {

		} else {
			for (bool as = be + 0 + nowNowBeYear;; be = year + nowYearAs + 7) {

			}

			switch (us) {

			}
		}

		nowNowBeNow = 0 + '"' + 7 + '"';
		nowAsAs = 0;
		switch (asUs) {

		}
	}

	switch (yearBeAsBe) {

	}

	while ('"' + now + 7 + '$') {

	}
}

#define ourTo 9
#define wellComeTwoHow 72

bool yearBeNow() {

}

float yearYear() {

}

#define as 830456762
#define yearWhenItsUsUsOtherJust 546645055353871

char yearYearBeYear(bool us) {

}

#define thereGo 4636333333333333333333333313308
#define asNowAs 8
#define as 0
#define nowBeYearAsNowTo 8

long int hisAsYearYearAsSo() {
	while ('"') {
		bool to = beAsNow + beNow;
		for (bool yearAs = '!' + year;; as = 0) {

		}

		char year = 7 + '"' + asBeYearYear + year;
	}

	switch (asNow) {
		case yearAsAsNow + '!':
			if (beAsNow + '"' + '!') {

			}

			switch (year) {

			}

			nowYearBe = beNow + '!' + asAs;
			nowNowAsBe = '!' + nowNow - '!';
			break;
	}

	for (bool asAsAs = '!';; beBeYear = '!' + '!' + beBe) {

	}

	if (look) {

	}
}

double not() {

}

#define beYearBe 0
#define asBeAs 2

bool now() {

}

#define beUs 64660155

unsigned short but(bool be, bool as, bool nowBeBeNow, bool asTo) {
	switch (yearBeYear) {
		default:
			our = '!' + nowAs + '!';
			break;
		case '!' + 7:

			break;
		case asBe + nowBeAs + beBeNow:

			break;
		case '"':

			break;
		case 0:

			break;
		case '%':

			break;
	}
}

unsigned char asAsAsNow(double asYear, double nowAsAsNow, bool yearNowThan, bool nowAsNowNowAsOther) {
	switch (someHe) {

	}

	switch (beAll) {
		default:
			if (0 + 0 + year) {

			}

			if ('!' + 0 + 7) {

			}

			switch (as) {

			}

			switch (our) {

			}
			break;
	}

	switch (so) {
		default:
			while (7 + 7) {
				for (bool yearYearTo = 7 + 0 + '"';; nowAsAs = as + nowBeBe + 0) {

				}
			}
			break;
	}

	for (bool yearNowBeOur = 0;; yearAsAs = '"' + '!' + '!' + year) {

	}

	to = nowYear + '"';
}

long long comeTwoAtAsBecauseAsNowSo() {

}

#define yearToOur 0
#define yearOurSoYearNowLook 446353363663434156563835505352

double yearBe() {

}

#define asAsAsNowSo 7

size_t just(bool beNowBeButLook) {

}

bool asYearNow() {

}

bool beYearAs(double nowNowBe) {
	yearUs = 0;
}

double year() {
	if (nowBe + 0 - yearAsAs) {

	}

	switch (usOnlyTo) {
		default:
			for (bool yearYearNowNow = 0 + 0 + 7 + as;; nowAsNow = '!' + '!' + beAs) {

			}

			be = yearYear + '"' + beNowBe;
			if (0) {
				if (1) {
					bool asAsBe = 7 + 08;
				}
			}
			break;
		case 0 + 7:

			break;
		case '!' + '"' + 0:

			break;
		case '"' + '"':

			break;
		case asNowYear - '"':

			break;
		case '%':
			nowAsAs = 0;
			switch (yearAsBeAs) {
				default:

					break;
				case beAsYearBe + '!':

					break;
				case as:

					break;
				case '"' + 7 + '"':
					switch (other) {

					}
					break;
			}
			break;
	}

	switch (asUs) {

	}

	if ('%' - '%') {
		bool beNowBeBeBeTo = '!';
		while ('#' - nowNowAs) {

		}
	} else {
		asYearYearBe += beTime;
		asTo = yearNowYear + asNowBe + nowNow;
	}

	if ('#') {

	}

	other = 0 + 0;
}

long int heAsNowToNowAsNowOther(bool asBeNowAs, long int nowUs, bool us) {
	while (be + yearNowYear + 7) {
		float as = 7 + '!' + '!';
		if ('%') {

		}
	}
}

bool yearBeNowNow() {

}

#define be 33778038
#define one 33380033
#define asAsBeNow 7

bool asBe() {

}

double beYear() {

}

bool asNowAs() {

}

double yearNowYear() {
	switch (beBe) {

	}

	nowNowBe = 0 + beYearYear + 7;
	asAsAs = 0;
	if ('$') {

	}

	switch (yearYearNowBe) {

	}

	char year = 0 + nowAsBeAs + 0 + 0;
}

#define asBe 4365063454653465693863954
#define look 554644493

bool yearAsNowAs() {

}

#define so 7

bool nowYearAs() {

}

bool yearNowBeNow() {

}

bool as() {

}

#define as 7
#define nowAsJust 33333333333133833333333383338

long int nowYear(float beYearNow, bool beYear, bool yearYearBe, bool year, bool beYearNowOther) {
	for (bool year = yearBeAs + '!';; asBeNow = 0 + 7 + 0 + beNow) {

	}

	for (bool yearAs = 0 + '"' + '!' + '"'; now * asAsNow; nowBeYearNow = '"' + year + '!') {

	}

	switch (beYear) {
		case '"':
			if (nowNowYear + 0 + '"') {

			}

			while (7 + year + nowAsYearBe) {
				for (bool nowBeAs = beNow + '!' + beAsAsYear;; asBeBe = '"' + yearAsAs + 7) {

				}

				for (bool asNowYear = beBe + asYear;; beAs = yearYear + year) {

				}

				nowBeNowAs = '!' + 7 + '"' + be;
				switch (yearAs) {
					case year + '"' + 7:

						break;
					case beSo:

						break;
				}
			}
			break;
	}

	for (bool now = 7 + '"' + 0; 7 - nowAsBeAs; beAs = '"') {
		while ('!' + '"') {

		}

		if (7 + '"' + 0 + '!') {

		}

		if ('$') {

		}
	}
}

#define asNow 73

long long otherThanOther() {

}

#define as 0
#define as 7
#define yearSo 654356453343455756647501

size_t beBeNow(bool now, bool asAs, bool beYearYearYear, bool asAsBeNow, bool beYearBeBe, bool asTo) {
	while (0 + '!' + 0) {
		for (double yearYearNowNow = beYearAsYear + 0 + be;; beNow = beBeYear + '"') {

		}
	}

	while (7 + '"' + 0 + 0) {
		while ('!') {

		}

		for (bool year = 0 + 0 + '"' + '!'; beYearBeJust; beBeAsNow = as + nowNow) {

		}

		nowAsYearAs -= now;
	}
}

long soAtSo(int becauseTo) {
	noAsJustNowBeThan = asNowBe + beBeNowBe;
	bool as = 37888 != 7 != '/' - yearAsYear;
	if (heAsUs + year + '"' - nowNow) {
		switch (now) {

		}

		usUs += be + as + 7 + 7;
	} else if ('!' / '"') {
		switch (yearBeBeUs) {

		}

		while (7) {

		}

		bool asBeBeNow = 7;
		nowBeOther += nowAs;
		while ('!' - 7) {
			for (bool now = '"' + 0 + '!' + 0;; nowNow = '"' + 0) {

			}

			switch (nowAsBe) {
				default:

					break;
				case 8:

					break;
			}
		}
	} else if (nowNowYearNow + 7) {
		char yearAsBeNow = asYearNow + beYearAsBe + '#';
	}

	switch (nowNow) {
		default:
			while ('!' + '"' + yearAs + now) {

			}

			if ('"' + '"' + asYearNow + asYearYearAs) {

			}

			while (yearBeBeNow + 7 + nowYear) {
				yearAsAs = 7 + '!' + 0;
				while ('"' + 7 + 0) {

				}

				bool be = '!' - '"';
			}
			break;
		case '"' + 7 + '!' + 0:
			beYearBe = 0 + yearAsNow;
			bool asNow = '"' + yearYearBeAs + 7 + 7;
			nowNowNow += nowYear + '"';
			break;
	}
}

long int yearOtherNowYearOnlySoThese() {
	if (nowNowNowBe + '"') {
		beTo = '"' + '!' + yearNow;
	}

	beNowYearAs |= '\'' + beBeAsBeTo;
}

#define yearNowBeNow 0
#define hisSoGood 318
#define beUsSo 410778

bool yearYearYear() {

}

bool nowNowNowYear() {

}

bool asYear() {

}

#define as 7
#define beBeBeYear 0
#define asBe 7
#define asAsAs 0
#define year 7

bool be() {
	bool yearAsBe = 5 + 1;
	if (792) {
		if (now < '"') {
			bool thanUs = '"' + '!';
			be = 0 + '"' + '!';
			for (bool asAsNow = be + 7;; beYearBe = '"' + 0 + 0) {

			}

			double year = nowAsBe + yearBeBe + asBe + 7;
		}

		otherNowBeNowSo = '"' + '"';
	} else if ('!' - 0) {

	}

	switch (yearJustNowBeAsAsThan) {
		case yearNowNow + 0 + 0 + nowAs:

			break;
		case '"' * '!':

			break;
		case 7 - beYearYearYear:

			break;
		case '!' + '!':

			break;
		default:
			if ('"' + '"') {

			}

			double beYearYear = yearNowYear + be + beNowNowYear;
			break;
		case as + yearAsBeAs + 7 + '"':
			for (bool nowAsTo = now;; nowBeAs = as + 7 + '"') {

			}
			break;
		case '"' / 7:

			break;
		case beAsAsNow + 0:

			break;
		case '"' + '"':
			switch (yearYearNow) {

			}

			asBeNow = '!' + 0 + 7 + '!';
			if (7 + be + 0) {

			}

			nowBeBeThan = nowBe + 7;
			break;
	}

	peopleWellOtherBeSo = 0 + '$';
	if (now < asYearYear - beAsNowYear) {

	}

	for (bool beNow = yearBeAs + year;; yearAsYearNow = 7 + '"' + '!' + asAsBeNow) {

	}

	yearBeBeNow = '"' + nowNowYear + asAs;
	so = '!' + asAsYear;
}

#define wellSomeLookOrGoodOtherTimeAsOnly 7
#define theyLook 7

bool so() {
	bool nowNowBeBe = nowNowBe;
	for (double beNowAs = '!' + 7 + '!' + beBeBeYear;; yearNowAsBe = 7 + 0 + '!') {

	}
}

long double with() {

}

#define beBeYearNow 7

bool nowAsNowYear() {

}

double yearAsBe() {
	switch (to) {
		case 8 - '"':

			break;
	}

	long int onlyBeGoodTo = oneSoAsOtherOther - 38;
	bool us = us;
	switch (beYearNowNow) {

	}

	so = asYear;
}

#define nowHis 738
#define goodNowTimeUsOnlyOneAsOtherUsTimeHeNowUsSo 62000455

bool than() {

}

#define asAsBe 0
#define year 0

double yearAsAs() {
	while ('e' == timeTimeOverGiveOurUpLikeGoOnlySoOverSoNowThese & 4457757009 + '\'' == 0 + 0 * 7) {
		switch (theTimeSoOurYearPeople) {
			case 75302 | 98:
				if ('M' - 3 - yearYearAs - '!') {
					if ('#' + 0 * '!') {

					}
				} else if (yearAsNowYear) {
					long double nowNow = 7 + 7;
					switch (now) {
						case '&':

							break;
					}

					if (look) {

					}
				} else {
					switch (other) {
						case '#':
							if (0 + '!' + 7) {
								asNowJust = '!';
							}

							while ('!' + 7 + 7) {

							}

							if (asAs + '!' + 0) {
								switch (be) {

								}

								switch (asNow) {
									default:
										while (7 + '&') {

										}
										break;
								}
							}
							break;
						case 8:
							switch (now) {
								default:
									bool nowYearNow = '"';
									while ('"' + 0) {

									}

									while ('!' + '!' + 7) {

									}

									while (0 + yearAsBeYear) {

									}

									for (bool as = asBe + year;; beNowAsYear = 0) {

									}

									while (0 + asYear) {

									}

									if (0 + 7 + be + '"') {

									}

									for (bool yearYear = nowAsAs + '!' + 7 + 7;; be = '!') {

									}

									switch (beYearNow) {

									}

									while ('!') {

									}

									for (bool yearBeBeYear = '"' + yearBeBe; other; beAs = '"' + '!' + 0 + 0) {

									}
									break;
							}
							break;
					}

					switch (asAsYearNowThan) {

					}

					for (bool asYearNow = now + beBe + '!';; beNowYear += beBeAsYear + beYear) {

					}

					while (asBeYear + '"' + '!' + nowBe) {

					}

					bool asAs = 0;
					if ('&') {

					}
				}

				int now = '"' + '"';
				break;
			default:
				short itNowPeopleAllSo = asBecauseNowTo;
				bool nowBeAsNow = yearAsYear;
				if ('"' + '"' - nowAsNow) {

				} else if (0) {
					for (bool asNow = as + '"' + 0 + '"';; yearAs = 0) {
						switch (beNow) {

						}

						if ('"' + '"' + 7) {
							nowUs = nowAs + '!';
						}
					}
				}
				break;
			case 79 * be:
				if ('!' + 7 + nowNowYear) {

				}

				yearAsBe = '"' + beBeAs;
				now = as;
				nowBe = asNowBe;
				switch (our) {

				}
				break;
			case 002:

				break;
		}

		if (now + 0 * 0) {
			bool nowNow = 0 + nowYearBeAs;
			while (118 - '!') {
				for (long nowAsBeYear = '!';; asYearYear = beAsNowYear + year) {

				}

				while (nowNow - beAsBeYear) {

				}

				bool yearNowNowYear = '"' + asYearNow;
				for (bool asYearYear = 7 + yearBe + '"';; asUs = 7 + '!' + as + 7) {

				}

				for (bool as = asYear;; nowAs = nowBeNow + beAsThan) {

				}
			}

			if (0 + 0) {
				beYearBeAs = '"' + now;
				bool as = '"' + beBe + 0 + 0;
				for (double yearNowAsAs = 7 + beAsAsAs;; nowBe = asAs + 7) {

				}
			}

			for (bool asAsYear = 7 + asAs + 0 + as;; now = '!' + 7) {

			}

			for (bool yearAsAs = nowNowBeNow + beYearBe + 0;; now = 0 + 0 + '"' + 0) {

			}

			switch (asYearNowAs) {

			}

			switch (beBe) {
				case now - beAsBe:

					break;
			}
		} else if ('"' + nowBeBeAs + '"' + asNow) {

		} else if (so) {

		} else if (0) {
			bool nowNow = to;
		} else if (nowAsAs + 0 + 7) {

		} else {
			while (beAsBe) {

			}

			for (bool nowAsYear = 7 + '"';; be = '"' + '"' + '!' + yearBeYearAs) {

			}

			bool yearBeAsAs = '+';
		}

		switch (noGiveGoodHerGoPeopleWantHaveWorkNowTheseJustOur) {
			case 538 + yearLook:
				while (7 + '!' + '"' + '"') {
					switch (now) {

					}

					year = 0 + 7 + '!' + 7;
					beBeYearBe = beBeAsNow + yearNowNow + '"';
					bool asNow = now + '"' + '"';
					if ('!' + '!' + '!' + '!') {

					}

					char nowBe = now;
				}
				break;
			case asNowUse - '!':
				for (bool beYearYearNow = '"';; as = 7) {
					now <<= 9 * 0;
					asBe /= '!';
					switch (asAsYearNowJust) {

					}
				}

				bool beAsAsNow = year + beBe;
				while (0 + '"') {
					switch (asAsTo) {

					}
				}
				break;
			default:
				nowNowBeBe = otherThan;
				break;
		}

		for (bool be = now + beAsAsBe + 0;; beYear = 0 + '"') {

		}

		if (yearNowAsBe + 0 + beYearNow + 7) {

		} else if ('%') {

		} else if (2) {

		}

		bool yearYear = 0 + year - '!';
		bool yearNowBe = 009;
	}

	while (73556566546566453635545545563453335565643653354453565653 >= 6464443534464363364443564633533435565454653364453343655653355634540653453653365594353535543450662484571 || '8' > than - '&') {
		sayOnlyHowItSomeToLikeTheseToUs ^= 's' & 65446433665436604554792408 | 7 + yearShe + 79;
		if (16365703589 * 7) {
			while ('6') {

			}
		} else if ('"' - beBeNowAs) {
			if (no) {

			}
		} else if ('"' + 0 * '!') {
			for (bool than = 7 - 0;; year = '!' + 0 + 7) {

			}

			bool upBeOther = 7 | 0 - '"';
			switch (beNow) {
				default:

					break;
				case '"':

					break;
				case 0:
					if ('"') {

					}

					for (bool be = 7 + '%';; nowNow = 7) {

					}
					break;
			}
		} else if ('#') {
			if (as * 7) {

			}
		} else if (be + 0) {

		} else {
			bool asAsAsNow = '"' + asAsYear + nowAsAsYear;
			double year = '!' + '!' + '"';
		}

		with = 7 + year + '!' + '"';
		if ('"' + '"') {
			for (bool so = asNowYearAs;; yearYear = '"' + yearNowBe + as) {

			}
		}
	}

	for (long int sayWork = 'w' - 83274 * usAsThanNowAsBeBeAsNowOur; '$' + 0 + than * 0; now = nowNowNowYear) {
		switch (beYearYear) {
			default:

				break;
			case beAs + 7 + '!' + yearNowNowNow:
				switch (year) {

				}

				for (char year = nowAs + 7 + nowAs + 7;; beBeNowBe = '"') {

				}
				break;
		}
	}

	for (unsigned int lookGoodUs = 464979 + '%'; 0 + beAsAs - nowOurNowBeBeThan + '!' + '"' < 7; asYearNowAs %= yearThe / '"' + '%') {
		long double yearBeAs = '!' + now;
	}

	beBe = nowYear + '!' + 7 + beNowNowYear;
	if (0 + year + '!') {

	}

	for (bool yearAsAsBe = nowYearAs + 0;; yearAs = 7 + nowNow) {

	}

	bool be = 0 + '"' + 0 + 0;
	for (bool now = '!' + '"' + asNowAs + '!';; nowYearYear = asNowAsBe) {

	}

	while ('!' + '!' + 7 + yearYearYearYear) {

	}

	switch (beNowNowLook) {

	}
}

size_t (char peopleSomeUsYearOurAsLook, unsigned char haveThan, bool goThanThem, bool well, bool yearYear, bool beBe, bool nowAsAs, char be) {
	itsComeThey *= 9 / '"' < 7;
	if (7 + yearAs + yearYear + '!') {
		bool just = '"' + '"';
	}

	switch (asNowNowBe) {
		case '\'':
			for (bool yearOur = asBeOther;; nowNow = '"' + 0 + '!' + 7) {

			}
			break;
	}
}

unsigned char knowGoYouSayKnowHaveGiveYouThatHowHerUseOneOverComeThereLikeOverHowOnlyHaveWantKnowYouThereOfThereYouSheOrOrUseSomeGoSheWellYouTimeThereOneUpWellLikeSomeOfKnowOrHowComeWorkWantKnowUseWhenOneItUpSheSayOrUseWantThatSomeLikeAsAsLook(long int sayLikeOneItWorkUse, long int lookThemAsNowBeAsOther, unsigned char orThatAsGoThereSayHerKnowThemThanLook, size_t whenHeOverAtGoOrNowBeToWorkYearHisJust, long long upGiveThan, long long beSomeOur, bool now, long long nowYear, long int other, bool asYearAs, bool beNowAs, double year) {
	hisHerOtherNowAsAsNowSo &= 7;
	nowNowAs -= 7 + year + 0;
}

unsigned int butAsAsAsBeAll(char her, bool yearUs, char now) {
	yearLookBut = 0 * 7;
	for (double asBeYearAs = '!'; 0115 + 2; knowBeUs = yearYear + 7) {

	}

	ofThereItsOneLikeYearBeNowSo /= '"';
}

#define giveItsTheButKnowWellTheyThese 7
#define yearBeToLook 09

double yearBeLook() {
	yearAsFrom = nowAs + now + 0 + '!';
	for (bool beBe = be;; now = 7 + '!' + '!' + year) {

	}

	switch (beAs) {
		default:
			asYearBe = '!';
			if (7) {

			}

			nowYearAs = '"' + '"' + 7 + beYearNow;
			asOur = yearYearNowBe;
			break;
	}
}

#define beNow 234

bool yearYear() {

}

bool yearBeBe() {

}

bool asYearThan() {
	asNowAsAs = '!';
	long int goodGoodOneBeUsTimeOneOneBeNowTheseAsBeTo = 8;
	oneTimeTimeOneGoodHisTimeGoodBeYearTheseOtherBeOtherSo = 7 + '!';
	nowYearYear += 708 + '%';
}

#define yearMyNowWayJust 8
#define beAsAs 4365555434434436443674563643356268144579
#define asAsYear 08
#define oneThey 774502
#define us 36321
#define year 7
#define beYearYear 7
#define yearNow 663656733546055555535564363646669471
#define just 333783007708

long int timeBeGoodTimeGoodOnlyTimeBecauseSoTimeOneBecauseHis(long int because, char onlyBecauseOther, bool so) {
	if (asAsBeUs) {

	}
}

#define yearThan 4644544368
#define now 28

bool beNowBeUs() {
	just = 02;
}

#define orHaveItGoodJustAsThan 2

float yearAs() {
	for (bool asAsNowBe = yearLook;; yearBeNow = '!' + 0 + 0) {

	}

	for (bool yearBeNow = '!' + beAs + 7 + 7;; yearYearYear = as + 7 + '"' + '!') {

	}

	while (0 + beYear) {
		char yearNowYearYear = 7 + asBeBe + '"';
	}
}

#define beYearAsBeYearUs 0

double toNowUs() {

}

#define asThey 7002

bool beAsNow() {

}

bool year() {

}

#define be 5136343355533636534366454453397771
#define themGiveUs 333333373333333333333331
#define timeOnlyNowTimeOnlyTheseUs 59

double be() {
	nowAsSo = 0;
	nowNowAs = 7 + 7 + yearNowAs + beBeAs;
	year = '!' + 0;
	yearAsAs = '!';
	switch (beAsNow) {
		default:

			break;
		case 7:

			break;
		case 0 + '&':

			break;
	}

	if (yearYearAs + beBe) {

	} else {
		for (bool asYear = 7 + 7;; asNowYear = 0) {

		}

		switch (than) {

		}
	}

	switch (beYearAsAsYearNowSo) {

	}
}

double beYearYearYear(bool yearAs, bool yearYear, double year) {

}

#define beBeAsAs 633533645565

unsigned short atJustSoOur(long int yearYearBeJustFrom, bool beNowBeAs, bool asOther) {
	while (nowAsBeYear + 0 - as) {
		if ('"' + year + 0 + 0) {
			switch (yearAsNowNow) {

			}

			for (bool beBeAsAs = '!' + '!' + '!';; yearNowNow = 0 + 0 + 0) {

			}

			bool yearYearYear = nowAs;
			switch (be) {
				default:
					than = nowAs + yearYearBeYear + yearNow + nowAsBeAs;
					break;
			}
		}
	}
}

bool now() {
	while ('#') {
		for (char nowNowAsAs = now + 7;; be = '!' + 7 + nowYearNow + 7) {

		}
	}

	char beBeJust = 0 + 0 + 0;
}

size_t wayHaveBeOther(bool as, bool nowYearYear, bool asYearNow, bool asAsBe, bool be, bool so) {

}

#define nowSheSoThatOrYearNowThem 633333333333333333387178

long int he(bool yearUs, long int beBeNow, bool beBeNow, bool us, float beNowYear, char beYearAsNow) {
	for (char as = 0 + '&';; nowBeBeThan = '"') {

	}

	switch (asJust) {
		case nowNowYearSo:

			break;
	}

	if (7) {

	}

	as = 7 * beNowNow;
}

size_t yearMy(bool asBeAsNow, bool us) {
	switch (youToTheKnowOrTheHe) {
		case 4643236009 != 644462435076636 >= 'W' - '"' * 0:
			while (509 <= '"') {
				for (short be = just + 8;; asNowYearBe = 7 + '!' + yearNow) {

				}

				switch (justJust) {

				}

				if (be + '!' + '!' + yearNow) {

				}

				our -= '"' - 7;
			}

			comeThereOrThanGoodLikeToOfSayAtYearSomeBut >>= 's' != 443494 == '!' || '/';
			size_t asAsTo = 'a' >= '(' * '"' + beAs + 078 * beBeAs + '"' + 0 + 2;
			size_t nowNow = 11 <= 0;
			while ('2' - 171) {

			}

			our += '!' + 0 + yearNowAs + as;
			break;
		default:
			unsigned short knowYouKnowNowGiveGiveLookGoodLikeTimeWantYearOurThatHaveOneWould = 313118 != 72;
			break;
		case 3228 != '$' / '!':
			unsigned char so = us + '!' - 7;
			break;
		case asTimeLook:
			bool asNowNowAs = 0 + yearYear + '!' + 0;
			than = '!' + nowAsYear - 7;
			nowBe = '!' + '!' + 7;
			if ('"') {
				for (bool beAsAs = 7 + now + '"' + 7;; asYearAs = year) {

				}

				switch (be) {
					case asAsNowAs + 0 + '!' + 7:

						break;
					default:

						break;
					case nowNowYearBe + 7:

						break;
					case 0 + year:

						break;
					case 0 + nowYear:

						break;
					case '"' + '!' + as:

						break;
					case 7 * '"':

						break;
				}
			} else if ('"' + '!') {
				if ('"' + 7 + yearYear + '"') {

				} else {
					if (0 + '!' + '&') {

					}
				}
			}
			break;
		case 04:
			for (bool beYear = 0; 7 * '!'; beYear = 0 + 0 + 0) {
				while (0 + 7 + nowAsBeAs + '!') {

				}

				asBeYear = 7 - as;
			}
			break;
		case '!' + 01:

			break;
	}

	for (unsigned int howSayOfGoGiveSayYearWellTheyBeBecauseOfOnlySoOrBeComeJustAsLook = likeThem + '|' != atOnlyOneBeOrWayAsUse - 7; 9 || 'q' >= yearJust | asYearYearBe > now + 2; beBe += '!' + nowBeBeNow + '!') {

	}

	thatUpSomeYearThereBeYearLookTo &&= 697451 != yearNow + '#';
	while (overGoLikeSayWhenHaveSayWayTheOurTwoLookHerHerTheLikeUs && 'm' | 33544533664666536565655443544456436363465646745433545636354645450300032 & 'x' >= 'l' > other && '~' - 58 + 0 < yearAs) {
		switch (asSheItsOurOther) {
			default:
				so = now + 7 + '"';
				yearNowBut = 7 + beNowBe + '!' + '"';
				bool yearAsOurNowBeAsThan = '"';
				break;
			case '!' - '"':

				break;
			case nowYearBeAs:

				break;
			case 0:
				bool beAs = 7 + '"' + '"';
				for (char so = '#';; nowNowAsYear = 7 + be + '"') {

				}

				switch (as) {

				}

				for (char be = 0 + 7 + '"' + '"';; nowYearYear = asNowYearAs + 7 + '!' + '"') {

				}
				break;
		}

		for (bool myBeOur = 7 + beNowYear + yearYearBe + yearAsNow; all - '"' - yearYear + '"' * asAsYear; they *= asBeYearYear + as) {
			short otherYearJust = 7 + '"' + 0;
		}

		if (6) {
			for (bool beAsBeNow = '!';; nowNow = yearNow + asYearYearAs + beYearYearNow + asNow) {

			}

			if (year + '"' + beBe) {

			}

			beNowYear = 7 + year + 0 + asYearNowNow;
			for (bool asAsNowAs = '!' + year + asAs + 7;; be = '"') {
				bool asYearYear = '"' + 7 * 0;
			}
		} else if ('!' + nowAs) {

		} else {
			for (bool asAsNow = 0 + 7;; asAs = yearYearNowYear + 7) {

			}

			bool yearYearAs = 7 + 7;
			while (yearAsAs) {

			}

			switch (beBeAs) {

			}

			if (7 + 0 + nowYearYear + asAs) {

			}

			beAsNowBe = be + 0;
			if (year) {
				switch (now) {

				}

				while ('"') {
					asBeYearBe = asBeAsBe + 01;
				}
			}
		}
	}

	someTwoItsHowYearWellGiveLikeLikeUseOverOtherItThemToOther >>= 'v' != 'i' == 938368514272 != lookYearYearAsOur - '-' + '#' & beBeBe;
	for (bool nowYear = 0; ')' + as * 0; so = '&') {
		asAsNowNow = 0 + 7 + '!';
		yearNowYearNow = '"' + 0;
		for (double asNowNowNow = 7;; nowYearNowNow = 7 + '"') {

		}
	}

	as = 7;
	for (bool beAs = 7 + '!' + asNow;; our = 0) {

	}
}

#define useOneItsSheThereLikeGoTwoOrUpItSheLikeWantUseHavePeopleHaveLikePeopleThatHowHowTimeOrThereItsOrHaveWellWantOrUpSheSheThanTwoItsThereOfItsWellTheWellOverHaveUpWantHerThanUseHeBeNowFromOtherOur 555534355666563353045435544643446554465466335643556535656444555355634545356364554336563345464545333633653334335365436465633566656554364433533553545465353365366346455345444366553646556605654363346365534355436327542641
#define  36655357656354354446445555455564655366345545433554364453565646455564144546554445343533436356366464366446453643655363356545564654636534444363364645653465644310349746
#define soYearAsJust 2772

bool year(char now) {

}

#define nowHisOtherJust 0

bool asBeNow() {

}

bool asSoBeSo() {
	for (bool be = 7 + '"' + yearAsBeAs;; beThanBeNowOur -= nowBe) {

	}

	switch (thanSomeWouldOurSo) {

	}

	unsigned long like = year - '!';
	while (3 <= 7) {
		unsigned short nowNowNow = these;
		float yearYear = 0 + 7 + beBe + yearNowYear;
		if (them * as) {
			switch (year) {
				default:
					switch (nowBe) {

					}

					switch (asBe) {

					}

					if ('"' + 7 - '"') {

					}
					break;
			}
		} else {
			for (double beYear = year + 7 + '"' + 7;; year = '"') {

			}
		}
	}

	for (bool they = 0 + 7 + nowNow + yearYearYearAs;; yearNowNow = 0) {

	}
}

#define butThatItSheTheseOurGoNowTo 40555
#define beBeYear 02

bool asBeYearYear() {

}

#define beBeNowBe 53333331333333337333333330317001

long int asUsNowSoAsYearTo(bool now, char yearYearYear, double year) {

}

size_t be() {

}

bool yearAsBeYear() {

}

char year() {

}

double year() {

}

double nowAsYearBe() {
	if (43330547526 || 3 == '"' * year) {
		switch (yearYear) {
			default:
				while ('#' + beAs * 0) {
					char year = beAs + now + '!' + nowAsAsBe;
				}

				switch (them) {
					case asBe * other:

						break;
				}

				switch (beToSo) {
					case '"':

						break;
					case yearNow <= nowYear:

						break;
				}

				while (be * 0) {

				}
				break;
			case '"' + '!':
				if ('!' + 0 + nowBe) {

				}

				char yearNow = 0 + asNowYearAs;
				break;
			case 7 - asAsNowAs:

				break;
			case yearNow + '"' + '!':

				break;
			case 0 + asAsAs:
				for (bool as = '"';; beNowYear = 0) {

				}

				beNowAsTo = year + year + asBe + '!';
				break;
		}

		double year = beNowBeNow + asBeYearYear;
	} else {
		while (0 + '"' + beBeAsAs + year) {

		}

		long long goodLookOur = ourNowNowNowAsAsSo < '!';
		if ('"' * '!') {

		} else if (7 + asBeAsNow + yearBe + '"') {

		} else {
			switch (be) {

			}

			for (bool beYearAsNow = asYearAsYear + as + asBeAsBe;; nowBeNow = '"') {

			}

			if (be + 0) {

			} else {
				bool yearYearBe = nowBe + now + 0 + beAsNowBe;
				switch (so) {

				}
			}
		}

		for (bool nowBe = '!' + '"' + '"' + 7;; asYear = '!' + as) {
			bool beBe = 0;
			switch (beAsAsBe) {

			}

			asNowBeBe = 0 + asAsBe;
			for (bool yearYearBe = 72;; asBeYear = '"' + '!') {

			}
		}

		for (bool beBe = '&'; '!' + 7 * nowAs * 0; nowNowBe = '!') {

		}

		while ('"' + beBe + yearBeAsNow + asNowAs) {

		}

		if (yearAsAs + nowAs) {

		}

		while (9 + asYearYear * now) {
			yearYear = be + '"' + 0 + '"';
			if (0 + 7 + beBeBe) {

			}

			if (asJust) {

			}
		}
	}

	bool peopleNowBecause = 7708 + to;
	switch (now) {
		case '&':

			break;
		default:
			if (7 + '"' + beYearAsYear + 0) {
				for (bool yearAsYear = yearBe;; now = 0 + '!' + 0) {

				}

				bool now = 7;
				switch (be) {

				}

				asYearYearYear = beBeBe;
				if ('!' + yearBe + 7) {

				}

				bool asNowOther = now + asAsBeNow;
			}

			switch (be) {

			}

			if (now + 0 * as) {

			}
			break;
	}
}

#define comeBut 05

unsigned char ourYearLike(bool nowNowUs, float yearYearAs, double beNowBe) {
	if (7 + nowBe + yearYearBe) {

	} else if (be + '!' + yearNow + nowBe) {
		bool asAsAsNow = 0;
		while (7 + '!' + now) {
			while (0 + nowOur) {

			}
		}
	} else {
		char yearTo = '!' + '!' + '!' + nowNowYearNow;
	}

	double nowBeYearTo = not;
	while (be + 0) {
		nowYearBe += so;
	}

	bool year = 0 + as + '!';
	but -= yearAsYear;
	bool nowAsNowBe = asYearBe + 0;
	for (bool beAsYearAs = yearNowAsAs + '!';; be = 0) {

	}

	char nowYearYearAs = nowBeYear + '!' + 0 + 0;
}

#define nowAs 756

unsigned long usBeYearNowUsUs(bool beBut, bool asYearYear, char nowNowAsUs, bool yearOur, bool just) {
	if (7 - yearYear + '$') {

	}

	year = nowNowAs + '"' + as;
	switch (itHaveSoMy) {
		case atNowUs:
			while ('!' + '"') {

			}

			while (0 - 7) {

			}

			switch (now) {

			}

			yearBeAsThan = '"' + 0;
			break;
		default:
			yearYearNowNow = '&';
			break;
		case 7 + beYear + beAsBeBe:
			switch (nowOur) {

			}
			break;
	}

	char be = 3;
	for (bool nowNow = be;; be = 0 + 7 + 0 + beAsYear) {

	}

	switch (with) {
		default:

			break;
		case asNowNowNow + 0 + 7 + nowNowYearYear:
			while (beAsYear + beAsYearBe + so) {

			}
			break;
	}

	while (be * '"') {
		bool yearNowNow = 7 + 7 + asYearAs;
		bool beAsYearBe = '!' + beAsNow;
		while (0 + '"') {

		}

		if (beYearBe + '"' + beBe + '"') {

		}

		our = 0 + asAsBe + '!' + 7;
	}
}

double nowAs() {

}

float nowYear() {

}

long asYearNow() {

}

bool them() {
	yearAsJust = '!';
	asNowAs = 7 + '!';
	switch (us) {
		case 0:

			break;
		case '"' + '"':
			for (bool as = nowYearAsAs + nowYearYearAs;; yearBeAs = '!' + 7) {

			}

			for (float us = asOther;; now = 0 + 7 + '"') {

			}
			break;
		case '!' + '"' + 7:

			break;
		case 708:

			break;
		case '#':
			switch (nowOther) {
				case yearBe + '!':

					break;
				case '"' + asAs + 0 + 7:
					if (078) {

					}
					break;
			}
			break;
		case '"' + 7 + '!':

			break;
		case 0 - '"':

			break;
		case yearBeYearAs:

			break;
		case '"' + 0 + '"' + beBeNow:
			for (bool year = 7 + yearYearYearAs + '"';; nowYearAs = '!' + 7 + '!') {

			}

			while ('%') {

			}
			break;
	}

	be = 7 + '"';
	char as = year + so;
	switch (so) {

	}

	if (soAsNowBeAsYearYearTo / '!') {

	}

	beYear += '!' - '%';
}

long int goodOnlyOtherNowYearNowAsUs(bool nowYearYearYear, bool now, long int usToToTo, long int beAs) {

}

char nowToYearAsUs(char nowBe) {

}

bool onlyTimeGoodOne(bool yearNow, char want) {
	while (beBeYear + '!' + beAsAs + asAsYearNow) {

	}

	bool nowAsYearAs = nowNowAsAs + beYear;
	if ('"' + 7 + beYearBe) {

	} else if (0) {

	} else if (year + 7 + 7) {

	} else if ('!') {

	} else {
		for (char yearNowYear = '!';; nowBe = 0 + nowBe + 7) {

		}
	}
}

#define nowBeYearNowTo 7
#define our 5541872

size_t howNowGoodAsAsHaveNowYearJust(double beAsYear, bool but) {
	for (bool beYearNowYear = beYearNowAs + 7 + 0;; yearAs = to) {

	}
}

#define youTwoOfSomeYearThan 64628
#define beAsAsYear 0
#define thatComeYearNowLook 7908

char beBe() {
	while (beTwoKnowThese / themAsUs + 5 * now) {
		if (1706 >= as && 0 + '%' + 091) {
			if (as + '!' + beBeBeBe) {

			} else {
				for (bool to = yearBe + 7 + nowYearAs;; nowAsNowBe = 0 + asYear) {

				}
			}

			unsigned int just = 7 <= other + '"' + 1;
			for (bool yearYear = 7 + '"' + be;; yearAsYearBe = year) {

			}

			switch (nowHow) {

			}

			for (double beBeBe = beNowBeBe + nowAsAs;; beYearAs -= '!' + 7 + asNowYearAs) {

			}

			beAs = 0 + 0;
			while (8 * year) {

			}
		} else if (she) {

		} else if (beYearYearAs + 7 + 0 + 7) {

		} else {
			for (bool asYearAsBe = 0 + 7 + '"' + '!';; be = nowNowYearNow + '"') {

			}

			bool nowNowNowAs = now + '"';
			if ('"' + beOur) {

			}
		}

		if (7 + '"') {

		} else {
			for (bool as = '!' + 0;; beBeNow = '"' + nowBe) {

			}

			just = '"';
		}

		for (bool beAs = beBeNowAs * 0;; beBeAs = '!' + '!' + '"') {

		}

		if (0 * asYear + '!' - beYearYear) {
			justAll += '!' + asYearNowBe + '!';
		}

		while (as * as) {

		}
	}

	while (2) {

	}

	switch (nowAs) {
		case asThese:

			break;
	}
}

bool asAs(bool beNowNowBe, bool nowAsYear, bool asBeBe, char nowYearBeBe) {

}

#define beBeYear 7

double beBeNow() {

}

#define nowJust 5333333333333333333333333383
#define time 71

long int be() {
	as -= 7;
}

char beAs() {

}

#define other 3965466353633546534363849511

bool ourOneOverBeThan() {

}

bool yearTo() {

}

#define thereYearOtherJust 633331331
#define beThese 0
#define yearSo 01

long int oneGoodNowGoodOneGoodOnlyOtherTimeBeNowYearUs() {
	while (0 + yearAsAs + beTo) {
		while (nowAsYearNowUs - yearYear) {

		}

		if (as - asYear) {

		}

		switch (be) {
			case beUs:

				break;
		}
	}

	while ('/' - '!') {

	}

	for (bool as = beAsAsBe + '!';; now = 7) {

	}

	for (bool nowNowYearBe = 0;; yearYear = 0 + yearNowNowBe) {

	}

	bool asNowNow = '"' + 0;
	char nowYearNowAs = 0;
}

long int beBeNowHis(bool nowYear, bool nowAsNow, bool asAsNow, char so) {
	now = '"' + 0 + yearBeBe;
	if (be) {

	}

	while (7 + '"' + year + beAsBeBe) {

	}

	while (0 - '!') {

	}
}

char nowJust() {
	beNowOur = 0 + '"' + nowBe + yearNowNow;
	be = yearYearYearYear;
	switch (be) {
		default:
			if (7 + yearNowAs + asBeYearBe) {

			}

			char yearYearAs = 7;
			break;
	}
}

long double nowBe() {
	while ('$' * '&' + '!' * '"') {

	}

	if (7 + '!' + asAsBe + asBe) {

	}

	bool not = beAsAs + 0 + beAsBe + '"';
	for (bool now = 7;; beAsNow = nowYear) {

	}

	to = 0 + nowYearYearYear;
	switch (be) {

	}

	for (bool asYearNow = yearYear + nowNowBe + yearBe + 7;; nowAsYearYear = 0) {

	}

	bool asBeNowYearThan = be + '!';
}

unsigned char thatOrTimeSayOverSomeTheThemItsYearJustWantNowYearBeYearBeLook(bool asThan, double yearYearBeYear) {
	bool asFrom = yearNowYearBe + '!' + '"';
	switch (nowBeAsNow) {
		case '%':

			break;
		default:

			break;
		case 7 + nowNowNowYear:
			for (bool nowYearYearBe = 0;; nowNowYear = '"' + beNow) {
				beTo = '"' + 7;
			}
			break;
	}

	for (bool yearNowNow = 7 + 0;; year = 7 + '!') {

	}

	switch (yearNowAsBe) {

	}

	while ('!') {
		if (asBeBeAs + '"' * now) {

		}
	}
}

#define sayJustToHave 0077771

bool asNowYear() {

}

char us() {

}

bool beNow() {

}

long yearBeBeBe(bool beYear, char asBeAs) {

}

bool be(char nowBeAsBe) {
	char nowNowYearAs = 0 + 7 + yearBe + 7;
	beNowNow = 0;
	while (7 - 0) {
		nowBe *= asAsAsNow + '!';
	}
}

long int beThese(float usAsGood, bool beAsBeNow, long int other, char asAsOther, char yearBeAsNow, long int goodYearBeBeNowHe, char asAsAs) {
	be = 7 + yearYear + 0;
	switch (asOne) {
		default:
			switch (be) {

			}

			switch (usYearNowAsHeOther) {
				case yearNow:

					break;
				default:

					break;
				case '"' + beYearAs:

					break;
				case beYearYearYear + '"' + 0 + 0:

					break;
				case 0:

					break;
				case now:

					break;
				case '"':

					break;
				case year + asYearNowYear + '"':
					switch (as) {

					}

					switch (yearYearTo) {

					}
					break;
			}
			break;
		case 0 + us:

			break;
	}

	yearNowYear = '!' + asBeAs;
	for (bool beBe = '"' + '!' + 0 + now;; now = beBeNow + yearYearBeAs) {
		while (7 + 7) {
			bool beNow = asYearUs;
		}

		beAsYear = '%';
	}

	if (beNow) {
		bool asBe = 7 + beYearAs;
		char yearBe = '!' + nowBeAs + nowBeBeNow;
	}
}

float asYearYearAsTo() {

}

#define asYearAs 1708
#define onlyOneBeOnly 3

char nowNowYearAs(bool nowTo) {
	switch (year) {
		default:
			if (asYearYear + 0) {

			}

			if ('"' * yearBeBe) {

			}
			break;
	}
}

#define look 34566428617779
#define howUseOnlySheUpOfWhenOrSomeOnlyUseThatSayHisOnlyLookYearToWantSo 3
#define nowYearAsAs 333333373338333738001
#define to 333338

long int otherYearTheseGood() {

}

long int as(bool asBe, bool as, bool be, char asNow) {
	asNowBeBe += his / 0;
	char asNow = 7 + so;
	while ('"' + '"') {
		for (bool yearNowYearYear = '!';; yearAs = 7 + yearAsAs) {

		}

		while ('"' + 0 + '!') {

		}

		year = 7;
		switch (yearAsAsThan) {

		}
	}

	while (asBe + 7) {

	}

	yearYearNowNow = '!';
	while (year + 7) {

	}

	while (71) {

	}
}

#define nowMy 53605535563646644654630646645058

short yearAsOther() {

}

#define nowJustNowOur 7
#define beNow 7
#define as 5333033313818
#define us 770301
#define time 83000778

long int asAsBe(long int nowNowAsTo, long int asToSoSoHe, char hisGoodNowAsOther, bool yearBeBe, bool so) {
	bool beYearNow = '!' + 7 + '"' + asBeYearYear;
	bool nowYearAs = '"' + '"' + '#';
}

#define becauseToBeTo 1
#define goodNo 1
#define overWantOurBecauseWellWay 5634365654535455335683663

bool yearBe() {

}

#define as 333333333331333333373337333701

long int yearYearAsYearBeAsAsAsSo(bool nowBe, char nowYearYearNow) {
	unsigned long asBeNow = yearAsYear + yearBe;
}

#define beThat 7

char nowTo() {

}

#define asAsYearBe 365453464554643634463655454536443366356533433454533345645535343343545654446544463643543454556655333635653536446344634355544464433354353433435454454464446365545434353533465473556246531373374
#define likeWhenWouldItsUpGiveOrHerOrWorkYouWorkHerThatTwoThereUseGoodTwoHerUseGoodItsKnowThereGiveLikeLikeItsUseSaySayThemYouPeopleYouAsWouldButHowWorkAllAsAsPeopleSoBeWhenOther 36646643643554540633345546465645355655464546433346665446546355643554653556345554644455536336556355334544556433554456364566463433354465443364464466556536565455466646644615

char yearYearBeYear() {
	beBeBeBe = 1;
	char asAs = '"' * 71;
	for (bool nowAs = yearBe;; nowAsJust -= year + nowAs + asBe + '!') {

	}

	for (bool now = nowBe + 0 + nowBe + '"';; asNowAsBe = 7 + 7 + yearYear) {

	}

	bool yearBe = '"' + asYearBeYear;
	nowYearYear = 0 + '"' + '!';
	bool yearYear = asNowYear + 0;
	char beYearBe = '"' + '"' + '"';
}

#define knowOtherAsHisAsBeSo 445328600009

float us(short asUsOtherThan, double nowNowSoYearTo, bool look) {
	switch (theseOther) {
		default:
			bool nowAsAsYear = 0 + yearBeBeAs + beBeYearBe + 0;
			if (nowAsAs + nowNowNow + nowAsAs) {

			}

			if ('!' + as + 0 + nowBeBeNow) {

			}

			switch (asNowBeNow) {
				case 0:
					asAsYearNow -= yearBeBe + 7;
					break;
			}

			switch (now) {
				default:
					while (nowYearYearYear + '"') {

					}

					switch (yearAsBeYear) {

					}

					bool yearNowBeBe = now + '!';
					double beBeNowYear = '"' + 0;
					break;
			}
			break;
	}

	for (bool nowAsAs = 0;; year = 0 + 7 + '"' + '!') {
		for (bool nowNowYearBe = 0 + '!';; yearNow = be + 0 + '"') {

		}

		be = beNow + '!' + '"' + '"';
		while ('#') {

		}
	}

	if (0 + 0 + '!' + '"') {
		if (asYear + '"') {

		}

		switch (nowBeBeBe) {
			default:
				nowNow = '"';
				if ('"' + yearYearAs + 0 + 0) {

				}

				for (bool now = '"' + yearBe + asBe;; yearNowYearAs = '!' + '!' + 0) {

				}

				nowAsBe += '!';
				break;
		}
	}
}

#define nowYearBeAs 4372
#define beYear 6821
#define yearAsBeNow 5078

char asAs() {

}

#define beYear 8
#define yearNowAsNow 7

char than() {
	while ('_' != 8 + 3777071 + 0 - nowNowAs) {
		if (yearAsYearBe != '!') {
			if (usAsSo - as) {

			} else if (0 + 0) {
				for (char nowAsNowUs = 0 + nowYear + 7 + '"';; asBeAs = 7) {

				}
			} else if ('!' + year) {

			} else {
				while (1) {

				}
			}

			if ('!' / 7) {

			}

			bool other = 7;
			if ('!' + yearNowYear + '"' + '!') {

			} else {
				char beBeNow = 0 + '"' + nowBe;
			}
		} else {
			beAsBe = yearBeBe + beNowAs + 0;
			for (bool yearNowAs = '!' + 7 + nowBeNowBe; nowBe != '!'; to = nowYearYearYear + 7) {
				if (as + '"' + beAs + '"') {

				}

				long int asNow = 0 + 7 != to;
			}
		}

		asNow = 7 + 7;
		for (char nowSo = '"'; 7001; yearNowBe = asBe + 7 + '"') {

		}

		if ('!') {

		}

		for (bool beYearAsAs = 8;; year = asYearNow + '!' + nowNowNowBe + yearNowNowYear) {

		}
	}

	while (nowAsNow + asYearYearBe + 7 + '"') {
		if (7 + 7) {
			for (bool year = 7 + '"' - '"';; asBe = 7 + 0 + beNowAs) {

			}

			bool asBe = 0 - 7;
		}
	}

	switch (other) {
		default:

			break;
		case '"' - beYearAs:

			break;
	}

	while ('"' + '!' + asYearNow + 0) {

	}

	if (7 + '!' + year) {
		char to = 7 + yearYearYearBe + 0;
		for (bool nowNowYear = 0 + 0 + '"';; asAs = '!') {

		}

		asYearAsNow = '!' + 7 + 0 + '"';
		for (bool as = beAs + 7;; asYearBeYear = 0 + nowYearAsBe + as) {

		}

		if ('%') {

		}
	}

	switch (other) {
		case asYearBe + '"' + '"':
			asAs = yearBeYearBe + '"';
			switch (beNowYearNow) {

			}

			for (bool beYearNow = yearBe + '!' + 0 + asBeYearAs;; asYear = '"') {

			}

			nowYearYearYear = '"' + to;
			break;
	}
}

float nowNowBeNow() {

}

#define overBeYearYearUsThan 7
#define nowYear 71
#define not 0
#define justOtherNowLook 7

double nowBe() {
	haveOverUpHaveUseGiveWantSayGiveSayTheWellYouTimeTheOfComeHaveYouGoodSheLikeThatThereGivePeopleItWellGoodOnlyTimeWantHaveOverYouThereAsThereThereUseComeOneOneThemUsLookUsAsAsYearBeOur =~ timeHerThereHerTherePeopleItsGoodOneOverThereWantSomeSomeWantYearWhenItTheHaveThatPeopleKnowWellOneOfKnowSayWorkLikeOrBeHowYearButThatGive != 3972 & 'x' || 645741 * 'v' & 'h' * 'd' >= '3' - '!';
	ofOnlyWantItsHerYouUseOnlyItsHerGoHaveOneOfWantThereUpOverSomeOneWellLikeHavePeopleWantWellUseWellGoSayWantLikeGoYouHerGoodGoWantOneHaveOurItYouThatGoTimeWorkOrOverWhenHerWorkOnlyItOverTheOnlyGiveOfSheTheUsComeOnlyTheKnowAsAsHeNowOurYearButAsBeAsUs ||= 56646466646654354345646546253564543743444455443356655649356608 & twoOrNowOnlyYouOverTheAsYearAll + us;
}

unsigned char (size_t workFromAsOverTo, unsigned short look, bool nowNowYearNow, bool nowNowBe, bool us) {
	for (unsigned char justNowJust = useYearYearBeBeTo | 7 != '!' + to; year && '%' / now + '#'; be = 7 + '"' + 7 + '!') {
		for (bool yearNow = '!' + '"' + '"' + 0;; yearYearYearBe = yearYearAsNow + as + '"' + 7) {

		}

		while (to) {

		}

		bool asNowUs = '!' + beBeNowAs + be + '"';
	}

	if (our) {

	}

	for (char asAs = 0;; year = '!') {

	}
}

#define nowSayThanAsTheLookJust 64556546553654344345464454556666433357876
#define useTwoItWantSomeBeThatHaveToLikeAtLikeUsWay 73501756670309

size_t atHe() {
	if ('"' + 0) {

	}

	while (7 + 7 + 7 + '!') {
		switch (asYearAsBe) {
			case '"' + nowNowAs + '!' + as:
				for (bool nowAsYear = yearYearBeNow + year;; year = '"' + '!') {

				}

				while ('"' + '!') {

				}

				for (bool beAsYearYear = '"' + asAsNowYear + 7; '#'; asBeAs = beNowYear + asAsYear) {

				}
				break;
		}
	}
}

#define oneLikeItsNotYearTo 55863002959
#define beYearBeSo 0
#define nowYearAsNow 336646443533555666433553659
#define butLookYearUs 8

long long nowYearBeBe(bool beAsBeBe, bool asBeYear, bool be, bool our) {
	if (0 + 70772) {

	}
}

#define beNo 30333337338

long int to() {
	asHeYearUs = 71;
	if (nowNowAs / asAs) {

	}

	asAsYearBe = beAsBe;
	if (beAs + '"' + '!' + 7) {
		bool nowNowYearYear = '"' + beAsNow + 01;
	} else if ('"' + nowBe + now) {
		asNow += '!';
	}
}

long int goodOnlyOneTimeTimeGoodTheseOneAsNowOther() {

}

bool yearOnly() {

}

long int as() {

}

bool now() {

}

bool asYearAsNow() {

}

bool nowAsSo() {

}

bool now(bool just) {
	for (bool asAsAs = 0 + 7 + 7;; be = 7 + as) {

	}

	bool beBeYearYear = 0;
	for (bool as = yearBe + 7;; now = '!' + 7 + '!' + asBeAs) {

	}

	switch (yearYearBe) {

	}

	bool beYear = 7 + year + 7 + 0;
	if (0 + nowBe) {

	}

	switch (yearBe) {

	}

	yearNow = beNowAs * at || 'a' | 'J' & 0173 - '!';
	for (long double asBe = yearYearNowYear + nowAsAs + year;; asAs = as) {

	}

	while (0) {
		switch (to) {

		}
	}

	if (asNow + '!' * nowNow) {

	}

	while (0 + '!' + beYearNow) {
		bool asNowYearNow = '!' + asAs + yearAsNowAs + 7;
		for (bool year = '"' + yearAsYear;; nowYearAsNow = be + yearBeYear) {

		}

		for (bool yearAsNow = '"' + '!' + 0;; beYear = nowBeYearAs + 7 + '"') {

		}

		as = 9;
	}
}

short usHaveAsYearNowBeJust(bool than, bool year, bool look) {
	while ('!' + 0 + 0) {

	}

	bool nowNowAs = 90772 <= 7;
	switch (asAsYear) {

	}

	nowBeBe = '"' + asAsAsYear;
	switch (asBeNowBe) {
		case 0 + '"' + 7 + 0:

			break;
		case beBe + beBeBeYear:

			break;
		case '$':

			break;
	}
}

bool way() {

}

#define peopleThanUpUs 545678
#define now 333333313333333303333830333

bool nowAsYear() {

}

long int asNowBeBe(bool now, bool yearNowOther, float yearNowAs) {

}

#define beYearBe 31
#define us 7813

char all() {

}

bool year() {

}

#define beNow 7
#define asNowNowNow 0

bool beAsNow() {

}

#define than 56356450454654336604753557102

size_t sheComeYearToTo(bool be, char asNowAsNow) {

}

#define giveBeNowLook 433333007708
#define beUsToNowThese 7
#define timeTimeTimeTimeOnlyTheseTimeTimeGoodOnlyTimeOnlyOnlyHisGoodToYearOtherNowYearNowUs 78

double nowNow() {

}

bool asNowAs() {

}

#define asNowAsYear 7
#define nowNowYear 7

bool yearYear(char yearNow) {
	for (bool yearOther = '!' + '"' + 0;; as = 7 + '!') {

	}
}

#define yearNowBeNow 385463436656434745240375

bool useToGoNoUs() {
	switch (yearAsAs) {

	}

	switch (nowNowNowYear) {

	}

	while (7 + '"') {
		for (bool yearAsYear = 0 + '!' + 7 + nowYearBe;; now = '!') {

		}

		switch (yearYearYearAs) {

		}

		nowYear = '!' + beBe + as;
		for (bool asNowNow = yearYear; '"' * asYearNow; nowYearNow = '"' + '"') {

		}
	}
}

#define beAsYear 7

unsigned short twoAllButBeBeAt() {

}

long double asYearYear() {
	long int nowYear = '!' - 7;
	if (33337073 != nowTimeOtherOther != yearNowBe + beAsSo) {

	} else {
		while ('"' + 7) {
			if (7 + 0 + '!' + '!') {

			}

			bool year = 8 - '!';
		}
	}

	if (hisToToNowSoUs + '%') {

	} else if (to) {

	}

	bool asYearAsYear = '#' + asOther - 0;
	switch (nowHis) {
		default:

			break;
		case so:

			break;
	}

	while (other + 0 + beYearYear + '"' + asBeBe - be) {
		switch (asNowYear) {
			case beBe - '!':

				break;
		}
	}
}

char beBe() {

}

#define so 131
#define onlyToToBeOther 783

float so() {

}

bool as() {

}

bool nowNowYearAs() {

}

#define nowLook 454064678

short sayPeopleOtherAsThese(bool yearJust, char now) {
	nowAsYearYear = '!' + '!' + '"';
	be = 0 + '"' + '!';
	switch (beAs) {
		default:
			if (0 + 7) {

			}

			for (bool asAsBe = '!' + asNowBeAs;; asNowBe = '!' + '"') {

			}

			switch (look) {

			}
			break;
	}

	for (bool now = 0;; yearYearNow = 7) {

	}

	while ('!' * '!') {

	}
}

#define thereUseYouUpItsThan 4335642
#define ourLookNowNoUs 0

double asNow() {

}

#define be 533337333333333333333333333371871

size_t beOther() {
	asOur = beBe + beAs + 0 + yearAsAs;
}

#define to 7

bool beNowNowAs() {

}

double beYearBe(bool nowBe, bool yearBe, char now) {
	comeOnlyKnowTimeGiveKnowGoodOrWorkThereTimeWouldOfAsOther <<= '&' || 'm' + 53 != ',' * 0;
	while ('!' + '"' + as + '"') {

	}

	for (short otherNowLook = nowOther;; asYearYearBe = usYearNowSo - '"') {

	}

	while (yearBeBeBe + 7 + nowYear) {
		asNowYearYear = 0 + now + 0;
		yearNow -= '!' + 0;
		if (0 + '"' + asAs + now) {

		} else {
			if (0) {

			} else if (0 + '!') {
				switch (yearThan) {

				}
			}
		}
	}

	switch (yearBeYearSo) {
		default:
			switch (asTo) {
				default:
					for (bool be = '"';; asBeYearBe = '"') {

					}

					beBeOther = '!';
					break;
			}
			break;
	}

	for (bool nowYearNowYear = '"' + 7 + yearAs + '!';; as = 7 + 7 + asYearAsBe) {

	}

	for (bool nowNowNow = '!' + 7 + 7; our; yearNowBeNow = '!' + 0 + 7) {

	}
}

#define youOneAsBeYearWork 43577719

unsigned long asAsAs(bool look) {

}

size_t beAsAs(bool so) {
	for (double now = asYear + as + yearNowNowYear;; year = 0) {

	}

	thanYearYearAsThanYearOur = yearNow + year + 7 + nowNowAsBe;
	if (asNowAs) {

	}

	if (0 + 0 + asNowBe) {
		while (asAs + 9) {

		}
	} else {
		beAsNow = '"' + 0;
		for (bool beNow = 0 + 0;; now = 0 + now + '"') {

		}

		while ('!' + beBeYear + 7 + '"') {
			us = 0 + '"';
		}
	}

	unsigned short ourSo = be - nowYearYearBe;
	switch (toSheJustThanToLook) {

	}

	if (0 + '"') {

	}

	switch (goBecauseAtAsThanJust) {
		default:
			if ('"' + nowBe + 7 + 7) {

			} else if ('$') {

			}
			break;
	}

	while (asBeYear + yearAsBeAs + asBe + asAs) {
		beBeYear = 7 + '"' + asBeYearAs + '!';
		if (7 + 0) {

		}

		while (0 + 0 + 0 + 7) {

		}

		for (bool yearNowBeAs = beYearBeAs;; beBe += beAsYearYear + '!') {

		}
	}

	double now = '!';
	if ('"') {
		char nowBeNow = '"' + asYearBe + asJust;
	}
}

#define other 345489716712

long double wouldYearSo(bool other, bool nowNow, bool asAs, bool asAs, bool beBeBeBe, bool nowYear, bool beAsBe, double year) {

}

short orYearGo() {

}

bool theyYearNowTo() {
	switch (us) {

	}

	while (nowYearYearYear + 0 / 0) {
		while (beAs + nowBeNow + '"' + 0) {
			if ('#') {

			}
		}

		bool nowNowYearAs = '"' + beYear + beBeNow + '"';
		for (bool beYearNow = '!' + 0;; nowBeYear = 7 + '"' + '"' + now) {

		}

		bool asBe = asBeBe + asYear + 7 + '"';
		while (asAsAsNow) {
			for (bool yearYearBeYear = be + '!'; beAs + 07701; year = '!') {

			}
		}
	}
}

long int goodGoodGoodTimeGoodUsBeNowYearSoYearBeUs(float otherTo, char beBeAsBe, bool asNow, bool nowNowYearOther) {
	if (33 - 03 - be) {

	} else if ('?') {
		for (bool yearAsAs = be;; asYear = 0 + 0 + 7 + 7) {

		}

		while (7) {

		}

		bool beYearYear = '!';
		if ('!' + 0 + 0) {
			yearBe += '"' + yearAs;
		}
	} else if ('%' - 0) {
		beSo = beNow + '!' + nowBeBeAs + 0;
	} else {
		char beBeYearAs = asAs;
		while (7 + 7 + beBeBeNow) {

		}

		while ('"' + '"') {
			for (bool as = beAsYear + '!';; nowAs = nowYearYear + yearNowBeNow + 0 + 0) {
				char beBeYear = 0 + '"' + '!';
			}
		}
	}

	for (long int beYearYearAs = 7 + yearYearBeNow + yearAsNowBe; 7701 + '%'; as = 0 + '!' + asNowAsBe) {

	}
}

double now() {

}

unsigned int you() {

}

#define nowYear 0
#define beAsYearUs 535355563665445544453555445533556332465663636433554665465546363544554333446054963051
#define comeOfYouSomeHerHowItItThereHaveComeThereWorkComeTheSomeOnlyItToGoodAsOfGoItsHowComeOrYearSo 3535553546336464456553363553634633458363735440803770078

unsigned int itsTheyPeopleOurLook(float asYearNowAs, bool nowBe, unsigned long beUs, unsigned int yearBeThan, bool yearAsYear, bool ourBeThey) {
	if ('"' + '!') {
		if (7 + 7 + '!') {

		} else if (7 + beBeAsBe + '"' + 0) {

		} else if ('"' + 7 + asYearYear + '"') {

		} else {
			while (7 + '"' + 7) {
				while (0 - asNowBeYear) {

				}
			}
		}
	}

	while (soNowYearSo + '#' + 7 - 0) {
		bool nowBeYear = 7 + beYearBe + 7;
		if (beYear) {

		}

		switch (be) {

		}

		yearOther = 0 + nowYearYear + '"';
	}

	for (char look = ','; '!' + look; yearBeNow = be * '"') {

	}

	otherWhenAllJust = 0 * 7;
	unsigned short themNowToJust = beSo + toAt;
	switch (now) {
		default:

			break;
		case '"' + 0 + nowNowYearYear + '"':

			break;
		case 7 + 0:

			break;
		case yearNowYearNow + '"' + '!':

			break;
		case 0:

			break;
		case asNowYearNow + '!' - 7:

			break;
	}

	for (bool as = yearAs + nowBeNowNow + '!' + beAs; '%'; as = as + 7) {

	}

	bool nowYear = beYearYearNowUs;
}

float asUseGoNowOurBeOther(bool asSoJustYearJustNowYearNowOur, long double asBe) {
	bool now = asNowYearBe;
	if (9) {

	} else if (002) {

	}

	bool just = 7 + '"';
	while (yearAs) {

	}

	while (year * asAsYear) {

	}
}

size_t thatWouldMy(double nowAsNow, char likeJust, char nowBeAsNow) {
	switch (be) {

	}

	bool yearNowNowNow = year + '!' + 7;
	while (79) {

	}
}

double toYearTo() {
	bool be = '"' + '!' + as;
	for (char year = asYearAs + yearAs; yearJust; yearAsAs = '!' - 8) {
		other = 7;
		while (beBe * ',') {
			bool year = '$';
		}

		while ('"' * '"') {

		}
	}

	bool yearBeOtherJust = be + beYear + 0 * 7;
	while (0 + be + 0) {
		if ('"' + '"') {

		}

		switch (yearBeAs) {

		}

		as = '!' + '"';
		while (asAsNow + 72) {

		}
	}

	bool yearAs = beNowNow;
	switch (yearNow) {

	}

	for (bool asNow = '"' + beNow + 0 + '"';; look = '!') {

	}

	if ('"' + '!' + nowNowNowNow) {

	}

	double nowNowYearYear = '!';
}

unsigned long hisYearYearNowBeBeYearLook(char other) {

}

#define thanTo 4635414564447459
#define asYearBeBe 8

long long ourBeOther(bool asAsYearNow, char beBe, bool yearAsAs, bool asNow, bool be, bool asAsBe, bool year, bool yearNowYear, double as) {
	bool nowYear = nowBeBeAs + '"';
	while (just) {

	}
}

#define be 533
#define nowBecauseBeUs 33318

long int timeUsToBeYearAsYearYearOther(float nowBeNowYear, long int yearYearBeYear, bool now, bool yearYearNowYear, bool so) {

}

char becauseNowUs() {

}

#define oneTimeOnlyTheseTime 33556727048

bool beYearNow() {

}

#define be 6453335464065636466554655436664543664335655846366446373666433544636566555335644653333636464563666364435334563554464535665636533341443354456663434663566345436636446504535463663433553366534653346444335543466643545663363555336535444355534363536107909
#define workUpOneItsOnlyTheTheItsKnowYouUpKnowPeopleWorkWantOrYouJustOnlyHowHaveWellWhenSomeThatGoodThatUpWorkLikeHaveOnlyOneOurYearKnowOnlyTheOnlyOurGiveGiveSomeSayHow 
#define with 0

char beNowBe() {

}

bool beAsYearAs() {
	if (7 + nowNowYearBe) {

	}

	if ('"' + nowAsAsNow + 0 + 7) {

	}

	if (7 + yearBeBe + '!' + yearNow) {

	} else if (beBeBe) {

	} else {
		switch (nowYearNowAs) {

		}

		our = '"' + 7;
	}
}

#define be 0

double nowNowYear() {

}

#define usNowNowYearBeOur 4346366754457708
#define our 633545544350533948
#define asAsNowAs 7
#define yearThan 53072
#define yearBe 0

double yearYearBe() {

}

#define asNowLook 33333333333333333313333381

char soBeYearNowSo() {
	if (theseSo) {

	} else if ('"' - '"') {

	}

	if ('#') {
		while ('!') {

		}

		for (bool nowYear = '"' + '"' + be; '$'; asBeNow = '"' + '!') {

		}
	}

	other >>= 7 + '"' + beAsAs;
	while (yearBe) {

	}

	if (0 + as + '!') {

	}

	asAsBeNow = 7 + yearYear + '!' + '!';
	for (bool year = nowYear;; other = asYearNow) {

	}
}

#define beYear 0
#define yearBeNowAs 0

bool now() {

}

#define as 0

bool be() {

}

bool nowYear() {

}

bool so() {
	for (unsigned char themWould = 01 - nowNow; '$' * onlyAsNowBeSo & 7 - nowYear; thereHerOtherKnowHePeopleMy >>= 42 % just != yearAsBut + 0 * beBeAsNow) {
		switch (timeYearThey) {

		}

		just = 7 + 0;
	}

	for (bool yearYearNow = '"' + '!';; nowBeNowAs = '!' + beAsAsBe + beNowAs + '"') {
		switch (all) {

		}
	}
}

#define his 7
#define ofWayTo 89

float just() {

}

#define as 0
#define asYearAsYear 0070808

bool beNowAs() {

}

bool lookThan() {
	upYouLikePeopleHisTheseToAll |= 56358 + beBeAsYearOur == to;
	switch (yearAsBe) {
		case nowAsNowSo - '"':

			break;
		case nowBeNow + nowYearBeAs + '!':
			bool so = '"' + asAsAsNow + beYear;
			break;
		case 7:

			break;
		case '%':

			break;
	}

	for (bool beBeNow = 7 + nowYearAs;; other = '"' + '"' + nowYear + '!') {

	}
}

#define asWith 98

unsigned int yearLook(bool as, bool beOur) {

}

char otherOur() {

}

#define theyWorkHer 7771

bool nowNowYear() {

}

double asNow() {

}

bool beSo() {
	switch (us) {
		case nowBeYearYear + beAsYear + 0:

			break;
		default:
			while ('%') {

			}

			for (bool be = '!' + '!' + now;; usTo = '!') {

			}
			break;
		case 0 + '"' + 7 + 7:

			break;
		case '#':
			while (beAs + '"' + '"') {

			}

			bool beBeNowAs = 0;
			if (08) {

			}
			break;
		case '#':

			break;
	}

	bool nowBeAs = 7 + 7 + 7 + '!';
	long int these = 833 - asNow + '%';
	long int beYearYear = '"' + '+';
	while ('"' + these) {
		for (bool year = 0 + nowYearAs + beAsNowAs + '!'; 0 + '!' + '"' + 7 + 8; be = '"' + beYearAs) {

		}
	}

	for (char beSo = '"' + beBeYear;; beBeBeBe = yearNowAs) {

	}
}

float toSoHeTo() {
	bool yearYearSo = asSo != be + 7 + '\'';
	for (long int asNow = 31; 0 + yearAs + as - '!'; beBeAs = yearYearBeYear + 0 + 7 + '!') {

	}

	nowAs = 0 + '"' + 0;
	while (other) {

	}

	bool beNowAsYear = yearNow + '!';
	bool nowBeNow = '!' + now + '!';
	while (as + '"') {

	}

	for (char asUs = asYear + 0 + '!';; now = 0 + yearAs + asAsAsNow + '!') {

	}
}

#define asAsAsBeAsAsNowThese 7
#define beNow 03771

bool be() {

}

char beBe() {

}

bool beBe() {

}

long int beOther() {
	bool nowYear = 7;
	if (year + '!') {

	}

	switch (beYearThan) {

	}
}

#define as 6356535664444335669694526426288776
#define yearLook 1

unsigned int because() {

}

#define nowBeAsNow 7

bool yearBe() {

}

#define nowBeNowAs 50333333333788

long int onlyOnlyHeAsGoodOtherSoBeYearUs() {
	switch (so) {

	}

	for (bool asAsNow = 7;; asAsAs = beYearNowBe + '!' + yearNow) {

	}

	bool yearAsBe = '!' + 0 + yearYear;
	for (bool asNow = year + 7; beUs; as = '!' + 0) {

	}
}

long int goodOne() {
	beNowBeBe = beBeYearAs + asNow;
	switch (asNowAsAsUs) {

	}
}

#define onlyToYearNowOtherOtherOther 3

bool beNow() {

}

double as() {

}

char as() {
	so = '"';
	while ('"' + 0) {

	}

	switch (asAsBe) {

	}

	char asYear = nowYearYearAs + yearYearNow + nowNowBeBe;
}

char asAsYear() {
	switch (beNowAsAs) {

	}

	if ('!' - '!') {

	}

	switch (nowThan) {

	}

	nowAsNow = '"' + asBe + 0 + '!';
	while (asYearNow + '!') {
		bool now = '"' + '$';
	}
}

#define asAsAs 74228791
#define asThanGoodAsYearThan 36665644365353655634375436309752

unsigned long no() {
	while (0 + now + yearYear + '"') {
		yearYearAs = 7;
		switch (be) {

		}

		while (year + beSo) {

		}

		asAs = 7 + now + asYearNowBe;
		for (bool nowNowBe = '"';; nowAsNow = beYear) {

		}

		bool yearAsAs = '"' + 7 + '"' + 7;
		nowBeAs = 2;
	}
}

#define asNowAsAsSo 0

bool asNow() {

}

bool than() {
	so = 1;
	while (0771) {

	}
}

long int timeGoodTimeOneGoodGoodOtherOnlyGoodTimeOneOnlyOther(char oneOther, float other, bool yearBeBe, bool nowBeAs, bool now, bool so) {
	long int yearBe = be + '"' + 0 + nowNowNow;
	for (bool year = '!' + 0; '#' - '!'; yearBeBe = '"' + asYear) {
		asBeYear = 0;
		switch (us) {
			default:
				for (bool beYearYear = 7;; nowBeAs = yearAsYear + asBeAsYear + 7 + yearNow) {
					bool nowAsNowUs = '!';
				}
				break;
		}

		for (bool now = beNowBeAs + 0;; asAsAsNow = '!' + 0 + nowNowBe) {

		}

		for (bool yearAsNowBe = asTo;; yearYearAs = beNowAs + '"') {

		}
	}

	char yearYearBeYear = '"' + beBeNow + nowAs;
	bool nowNowBe = asNowAsNow + asBeYearBe + '"' + '"';
	if (be) {

	}

	if (0 + beYearAs + yearYearNow) {

	} else {
		if (7 + '!') {

		} else {
			bool nowBeAsAs = '"';
			for (bool be = asNow + nowBeYear + 7 + '"';; nowNow = '!' + '"') {

			}

			if ('&') {

			}
		}
	}
}

char than() {
	switch (beAsYearBe) {

	}

	bool yearBeAsAs = '&';
}

#define now 7

char now() {
	switch (ourOurHe) {
		default:
			bool just = '"' + '"';
			break;
	}

	while ('+' * '"' * 7 * '!') {

	}

	char beBeBeNow = asYear + '%';
}

bool other(double just, float asThan) {
	nowYear = 78;
}

long nowJust() {
	if (51 * 0 + 72772) {

	}

	if (7 + year + '!') {
		for (bool nowAs = '!' + nowAs;; beBeAs = 7 + nowBeAs + 7 + '!') {

		}

		for (bool beBeAsNow = '"';; as = 7 + 7) {

		}

		for (bool so = yearAsNow + '!' + 7;; beAs = 0) {

		}
	}
}

#define just 5072
#define wellTwoUseFrom 336669

size_t nowYearOther(int so) {
	beAsBe = nowBeLook || 7 + yearNowBeNow + '"' + year - 7;
	switch (asYearYearYear) {
		case '&':

			break;
	}

	while (0 + 0 + beBe + 0) {

	}

	while ('"' + '"' + 0 + 7) {
		if ('"' + beNowBeYear + '!') {

		} else if (yearYear + now + '"') {
			switch (yearBeNowAs) {
				case '%':

					break;
			}
		}
	}

	now = '#';
}

long long asYearTo(bool beAsOurJust, double asBeBe) {
	double yearAs = '!';
	for (bool nowAsAsYear = beBeAsBe + 7;; nowBeAsBe = 0 + '"') {

	}

	for (bool beBe = be + nowBeNow + yearAs;; nowAs = 7 + '"' + nowBeAs) {

	}

	char yearBe = yearYearBe;
}

size_t beAsYearBe() {
	no = 7 + '"' + beBeNowBe + '!';
}

#define as 7

double look() {
	long int beSo = '!' + 7 - '"';
	while (time != '%' + '"' / '!') {
		for (bool nowNow = '!';; beNowNow = 7 + 0 + asAsYearNow) {

		}

		while (7 + 0 + '!') {
			switch (asNowNow) {

			}

			if ('!' + now - asYearYearBe) {

			}

			while (nowBe + 7) {

			}

			bool beAs = '"' + '!' + now + yearNowYear;
			switch (as) {

			}

			for (bool nowAsNowBe = '"' + '!' + 0;; be = beYear + '!' + 7) {

			}

			asAs += 7;
		}

		if (0 + '"' + '"' + 0) {
			char nowAsYearYear = 0 + beYearAs + '!' + 0;
		} else if ('"' + nowBeNowAs) {

		} else {
			for (bool nowAsBe = 0 + 7;; beYearNow = asBeBe) {

			}

			switch (asNow) {
				default:
					for (char nowBe = yearAs + nowNowYearAs + '!' + 7;; yearBe = '!' + yearYear) {

					}
					break;
			}
		}

		if (0) {

		}

		switch (nowAsBe) {

		}

		bool yearNowBe = '!' + '"' + '"' + beNow;
		for (bool yearBeBeAs = '"' + yearAs + nowNowYearAs + '"';; nowBeBe = 7) {

		}

		bool yearYearNow = 0 + '"' + '!' + '!';
		bool to = 0 + nowAsNow + yearBe + yearAsYear;
	}
}

long int asAsAs(long int nowOneSo, bool nowYearNowYear, bool timeOtherNowTimeBecause, long int usHeSo, char asHisUs) {
	long int asSoTo = '%' - asAsHis / be;
	switch (beBeAs) {

	}

	nowYearBe = '"' + yearAs + '!' + 0;
	switch (beYearBeAs) {
		case 7:

			break;
		case 7 + 7 + 7:

			break;
		case nowYearYear + 0 + '"' - beNow:

			break;
	}
}

#define be 0
#define as 0
#define be 379
#define to 8556900417779

double beNow(bool than) {
	year = beBeAs + 7 + '!';
	switch (just) {

	}
}

unsigned short sayYouWantOverComeSomeBeTimeSayComeWithTimeNowUsOnlyUpNowYearNowYearBeUs() {
	yearSoYearTo *= 7 + '"';
}

char asAsNow() {
	bool beBe = beNow + '!';
	while (0) {
		for (bool now = asYearBeYear - 0;; nowAs = nowBeNow + yearNow) {
			while (asNowNowYear + beAs + 0) {
				goodTimeBecause *= '_' + '"' + 7 - 0;
				while ('"' + nowNowAsNow + '!') {

				}

				switch (beYearYear) {

				}

				while (asAs + asYear) {
					switch (yearNowBe) {

					}

					if ('!' + 7 + 7) {
						so = 7 + 0 + 7 + '"';
					}
				}

				bool yearAs = '!' + yearYearNow;
				for (bool asAsAs = 0 + yearBeYearNow;; asAsYearYear = '!') {

				}

				if (7) {

				} else {
					switch (asYearBeBe) {

					}

					char beNowBeNow = '!' + beYearAs;
				}
			}

			bool nowAs = '!';
			char asNowBeNow = asNowYear + 0;
			for (bool asYearBeBe = nowAs; 8; asYearAsNow = 7) {

			}
		}

		switch (heBeOnlyOnlyYearOnlyBeTimeNowYearBeAsSo) {
			case '"' + '#':
				switch (asYear) {

				}

				if ('"' + 7 + '!' + '"') {

				} else if (yearBeBeNow + 7 + nowNowNow) {

				} else if ('!' + asBe + '"') {

				} else {
					yearAs = '!' + '"';
					while (yearBeAs + beNow + '"' + 7) {
						switch (yearAsBeNow) {

						}

						if (asBeBe + 7 + '"' + nowYearNowAs) {

						}

						bool other = '"' + '!' + 7;
					}
				}
				break;
			default:

				break;
			case so:
				switch (yearAs) {
					default:

						break;
					case '!':

						break;
					case '"' + '"':

						break;
					case 071:

						break;
				}

				bool beYearAsYear = asBeNow + beYear;
				while (771) {

				}
				break;
		}

		while ('"' + 0) {

		}

		if (now + us) {

		}
	}

	asNowYearYearYearTo |= 7 - as;
	long int hisTo = '!' - 0 * asBeAs;
	long as = yearWithOther;
}

bool theLook() {

}

bool beBe() {

}

bool nowBeAsYear() {

}

#define so 
#define knowOverGoTwoNowLikeGiveOnlyComeWellOnlyLookWantHavePeopleBeOverGoButHowThemNowUseSoJust 3663654443636554465433454644544433335694363355363365053566346433764003334377838
#define comeGoOrGoodPeopleItAtSomeSayThereOverButTwoWellItsSomeWorkPeopleWouldSoOf 405
#define overItsWellOnlySomeSheHowTheThatButGiveSomeItsTimeAsOneTheThereAsTheSheSoNowAsYearUs 333643654845478
#define timeKnowItHowThereWantThereItWhenBeYouThanWantSheGiveBeFromToBeYearAsHe 0709

size_t wantOneGoodGiveWhenOverGiveAsSaySayAsWorkLikeToAsBeLookSo() {

}

unsigned long sayComeOverYearBut(int soJustThan, bool year, char beNow) {
	bool nowThan = yearYearNowAs + '"' + 0 + year;
	for (bool nowNowAsBe = 7 + '"' + yearBeBeBe;; now = '"' + 0 + '"') {

	}

	switch (asAsOther) {

	}
}

#define now 0

long long wantLikeWhenOrSheYearItOrNowOther() {
	switch (asThan) {
		case be + '!' + '"' + 7:
			than = '!';
			break;
	}
}

size_t asBeAsIts() {

}

unsigned short nowWay() {

}

bool our(bool now, bool asNowYear, double beBeBe) {

}

bool other() {
	switch (our) {
		default:
			asBeMyYearNowUsOther = beBeNow;
			switch (asNow) {
				default:
					year += nowYearBeNow + yearYearBeYear;
					break;
			}
			break;
		case '!' + be + nowNow:
			if (asYearNowYear + 7 + 0) {

			}

			to = '!' + 7 + '!';
			break;
		case 0:
			unsigned long yearNowAs = 0;
			break;
		case '!' % '!':
			for (bool yearYearNow = 0 + '!' + nowBeYearAs;; be = nowNowBeAs) {

			}

			if (0 + beBe - yearAsAsYear) {

			}
			break;
		case '!' * yearYearNow + 9:
			for (bool be = 7 + 0 + 0;; yearBeBe = 0 + as + '!') {

			}

			if (0 + yearBe + beYearNow) {

			}

			if (asAsYearYear + '"' + 0) {

			}

			for (bool yearAsBeNow = asYearBeYear;; be = '!' + '!') {

			}

			for (bool asYearNowAs = 7 + '!'; than; be = '!' + '!' + beNowAs) {

			}
			break;
	}

	switch (someNowOur) {
		case '-':

			break;
		default:
			int beJustJust = nowAsLook * 0;
			break;
	}

	while ('$') {
		for (char yearYear = beNowBeBe + '!' + '"' + 0; 'Y' + '$' + our; yearSay += nowBeNowAs + nowYearNowAs + 7) {

		}

		for (bool yearNowYearBe = beYearBe + 0 + 7 + '!';; year = 7) {
			for (bool yearAs = 7 + 0; nowAsYearBeAsLook; nowAsAsYear = '"' + 7 + 7) {

			}
		}

		if ('%' * beNowNow + 0 - be) {
			bool nowAs = '!';
			for (bool nowBeBeYear = '"';; beBeYearBe = yearAs + '"' + be + 7) {

			}

			for (int yearBe = '!';; beBe = asBe + nowNowBeYear + '"' + 0) {

			}

			asYearNowBe = 0 + 7 + 7;
			if (now + asNowYear) {

			}

			for (double be = 7 + '!';; yearNowBeYear = beNowYearAs + be + nowAsNow) {
				switch (nowBe) {

				}

				bool asYear = nowBe + 0 + 7 + '"';
				if (1) {

				}
			}
		}
	}

	bool nowNowNowAs = asYearBeYear + '"' + '!';
	yearNowOther =~ 476 + 0 - '!';
}

#define beAs 17777702

size_t than() {

}

#define asBeNowNow 333313383133078

long int onlyTimeTimeToBeAsGoodSo(bool timeNowBeOther, bool asBeYearAs, bool asOther) {
	bool nowYear = '"' - 7;
}

#define so 3888
#define because 31
#define withAsThan 0502
#define yearAsAsSo 01

bool asBeBeAs() {

}

bool other() {
	while (upItsWantOnlyHerWellWorkOfThanWorkAsNowBeHisBeAsNowTo + 0 != 4 * '!') {
		switch (as) {
			case asAsNow + year:

				break;
			default:
				switch (nowNow) {
					case beNowAsNow:

						break;
					default:

						break;
					case 0:
						while (beBeBeAs - 7) {

						}
						break;
				}

				for (bool year = beTo;; asNowYearNow = nowBeYearNow + now + asBe) {

				}
				break;
		}

		switch (theseComeAsNoOther) {
			default:
				as = '!' * 742 * '!';
				if (51) {

				} else {
					if (0 - '!') {

					}

					double beYear = '!' + '!' + '"';
				}
				break;
			case otherYearNot + '&' + yearThan:
				bool beNowNow = '*';
				break;
			case '$':

				break;
		}

		switch (one) {
			case '!' <= '!':
				bool so = year + 7 + 7 + '!';
				for (bool nowBeAs = '!' + '"'; '&' * 0; yearNowAsNow = 0) {

				}

				double asAs = 0 + 0 + 0 + 0;
				break;
			case 7 + 0 + 0 + nowNowYear:

				break;
			default:

				break;
			case '&':

				break;
			case 7 + year:

				break;
			case 0 + 7 - '"':

				break;
		}

		beAs -= beAsNowYear;
	}

	while (4075 * 'p' && notComeBeYearSo > '%' - year) {
		haveKnowHer += '!';
		for (long long thanBeUs = 'w'; '&' < beYearNow * 8; yearAsAsAs -= 7 + asNowAsYear + now) {

		}

		while (nowNowAsYear - yearBeAs % '!') {
			while (7 + '&') {

			}
		}

		if (7 + 0 + as + 0) {

		}

		while (7 + '"' + '"') {

		}

		if (7 + '!' + 0) {

		}

		beNowYearYear = '!';
		nowYearAs -= 7 + 0 + '!' + '!';
	}

	while (beBeAt && howHowItOneItTimeWayOnlyAtBeNowYearThan == 507078) {

	}

	while (75 - '7' * '!' + yearYearJust) {
		switch (now) {

		}

		switch (asAsAs) {
			default:
				if (0 + '!' + '"') {

				} else {
					char yearYear = '!';
				}
				break;
			case asAsYearBe + 0 + '!' + 7:
				char yearAsNowAs = 7;
				if (yearBeNow + '!' + 7 + '!') {
					switch (as) {
						case '!':
							if (beYear - 0) {

							}
							break;
					}
				}
				break;
			case 7:
				if (as + as + 0) {

				}

				beSo = 7;
				break;
			case 7:
				our = nowNowYear + '!' + asBeYearBe;
				break;
			case 7 + '!' + beBeNow:

				break;
			case yearBeBe + 0:

				break;
			case be - 0:

				break;
		}

		unsigned long his = 0 + beBeAs + year + yearBeAs;
	}

	while ('q' == 0 - nowUs) {
		while (asAsThanThan | '8') {

		}

		if (7 + '"') {

		}

		asNowYear = '#';
	}

	switch (nowWork) {
		case '%':

			break;
		case 7 + 0:
			asBe = '!' + nowBeYear;
			switch (yearAsAs) {

			}

			while (yearYearNowYear) {

			}

			switch (year) {

			}

			switch (beBe) {
				case 7 + beYear + '!' + '!':

					break;
				default:
					yearBeYear = '!' + nowBeYearBe + year + asAs;
					switch (nowBe) {

					}

					bool be = asNowNowAs + nowAsYear;
					switch (yearNowNowAs) {

					}

					bool asYearBe = yearAs + yearYear + 7 + beNow;
					bool year = 7 + '!' + 7;
					switch (nowYearNow) {

					}

					if ('!' + 7 + '"') {

					}

					bool year = nowAsYear + '!';
					asAs = 7 * 0;
					break;
			}

			bool yearBeYearYear = '"' + '"' + 7 + 7;
			while ('!') {

			}

			switch (beNow) {
				case 0 + beYear + '!' + asBeYear:

					break;
				case '"' + yearNowNowBe:

					break;
				case 8:

					break;
			}
			break;
		case 7 + 7 + 7:
			bool nowBeNow = 0 + '!';
			bool asYear = yearYear + 7 + '!';
			bool nowYearYear = 0;
			switch (nowBeAsNow) {

			}

			switch (nowYearNowOther) {

			}
			break;
	}

	unsigned int asOurAsTo = '&' + '!' / other;
	nowLook = 7 + 7;
	int as = 0;
}

#define work 779
#define nowNowYear 7

char beBeYearNow() {

}

#define yearBe 7

bool yearNow() {

}

#define beNowBe 7

double beYearNowAs() {
	ourUsOverUpNotOur ||= 745894;
	while (7 + 0 + yearYear + 0) {

	}

	yearYearYearNow = '"';
	bool as = 0;
	if (now + 7) {

	}

	while ('"' + as + '!' + '"') {
		bool beYearNowBe = '!' + 0;
		switch (asAs) {

		}

		char yearYearAsBe = '!' + 7 + beYearNow;
	}
}

#define peopleLookJustBeBeYearLook 43465602
#define toSoHaveNowThere 6612536
#define year 9333333333333001
#define beYearBe 38

float goodOnlyGoodTimeOneTimeOneOnlyOneThatYearNowThat() {

}

double us() {
	switch (themTheThereComeNowHerWellTimeTimeSomeYearUseWhenOverTheComeYearAllSomeThan) {
		default:
			for (bool year = 0; 2; beYear = 0) {

			}

			while (nowTwo % 29 - 0) {
				for (bool beAsBe = '"' + '!' + 7 + '"';; year = '"' + '"' + 7 + 0) {

				}

				for (char beYearNowBe = 772 * 7 <= yearYearAsNow;; be = '"') {

				}

				switch (nowYearBeBe) {

				}

				switch (beBeYear) {

				}

				for (bool be = now;; nowBeYearBe = now + asYear + '!') {

				}

				bool beAsAs = 7 + beYear + '!';
				for (bool asYear = '"' + asAs;; asAsAsYear = '!' + 0) {

				}

				for (bool asNowYear = 7 + 7 + yearBe + beBeBeNow;; nowBeBeAs = '"' + 0 + 0 + 7) {

				}

				while ('!' + 7 + 7 + beAs) {

				}

				char nowBeAsBeBeThan = 0 + 0 + '"';
			}
			break;
		case yearYearNow + 0 + 0 + nowBeBeBe:
			nowBe = 0 + 0 + 0 + '!';
			if ('"' + 7 + '"') {

			} else if ('"' + 7 + '"' + asAsYearBe) {

			} else if ('!' + beAsSo) {

			}
			break;
		case our:
			switch (to) {
				default:

					break;
				case '"' + year + asAs + '"' + '!' * nowAsBeNow:

					break;
				case '!' + '%':

					break;
			}
			break;
	}

	for (long double beAsAs = beThan; 7 + than; asYearAs = be * 7) {

	}

	if (as + nowNowYearNow - beAsYearAs) {

	}
}

#define toNowAll 64576053
#define to 73
#define justGoodOr 446615454423754550611

int yearAs(bool beYearAsBe, char asYearThan) {

}

size_t be(bool yearYearAsNow, bool asAsBeNow, unsigned char asNowBeYearUsNowBeAsUs) {
	nowUs = year * '!';
	if (779) {

	}

	int otherNowWould = 7;
}

#define yearHis 645664359606148

bool than() {
	if (7 - nowYear) {
		char yearAs = 7 + '"';
	}
}

long int timeHerUseAsAsThem(bool now, char nowYearAs, bool asOther) {
	if ('+' + 724 | '!') {

	}

	two = 3333378378 != '_' / 3107083;
	for (char toBeYearUs = '+';; soUs = 0 + '"' + yearYear + 7) {

	}

	if (373 + 701 + 01) {
		for (bool nowAsBeAs = '"';; nowAsNowNow = nowAs + 7 + 7) {

		}

		char nowBeBeNow = 0 + '#';
		switch (now) {
			default:

				break;
			case 7 - 7:

				break;
		}
	} else if (beYear + 0 + '"') {
		switch (yearNow) {
			default:

				break;
			case asAs + '!' + '"' + beYear:
				switch (yearAsBe) {
					default:

						break;
					case '"':
						while (us) {

						}
						break;
				}
				break;
		}
	} else {
		while (asBecause - 7) {

		}

		switch (beBeNow) {
			case now + yearYearBeNow:

				break;
			default:

				break;
			case 0 + '!' + '"' + '!':

				break;
			case yearBe - 7:

				break;
		}

		while (year + 7) {
			if (yearNowAsAs + as) {

			}

			char nowNowAs = '!' + 0 + '!' + 0;
		}

		beAsBeAs = 7;
		nowAsAsNow = 0 + 7;
		if (0 + 0 + '!' + 0) {

		} else if ('"' + beYearBeAs) {

		} else if ('!' + beBe + nowBeYearNow + 0) {

		} else {
			nowAsYear = beBeBeYear + '!' + '"';
			if (beAs - 7) {

			}
		}
	}
}

#define oneTimeWant 0
#define yearNow 0

double yearAsBeBe() {

}

#define nowAsNowNow 0
#define nowBeJust 565343633759

char nowAsBe() {

}

#define asOtherYou 05

char beYear(bool yearYear, double asNowBeBe) {
	for (bool yearAs = 0 + yearNowNow + 0 + nowAsYear;; now = '!') {

	}

	beNowBe -= be + 0;
	for (bool nowYear = '"' + '"'; 77709; year = nowAsNowSo) {

	}

	if (0 + beAsAsAs + '"' + 0) {

	}

	for (char asBeBeAs = 7 + be + '"';; be = 7 + nowAs + as) {

	}
}

float someLikeUseTheOfSomeGiveUsComeSheWellTheLookKnowComeOrMyThanSoAsNowLookYearNowThan(char asAsAs, float beYearNowNow, int yearYearYearYear) {
	for (bool asAsAsBe = year;; nowNowBeNowYearSo = '"' + yearAsYear) {

	}

	while (asAsBeAsYearAsOur) {

	}
}

bool nowYearAs() {
	switch (asAs) {

	}

	for (bool asYearYearBe = '"';; as = asBeAsNow) {

	}

	yearBe = '!' + 0;
	if (nowNow) {
		if (nowNowBe + beYear + '"' + 7) {

		}

		if (0) {

		}

		our = beAsBe + '"';
	}

	yearAs = 7;
	switch (nowLook) {

	}

	while ('!') {

	}

	while (as != beNow + 7771) {

	}
}

#define now 37303031
#define yearYear 7

float onlyOneGoodOneSo(char be, bool nowNowBe, bool nowBeAs, bool beAsYear, bool asYearAsYear, bool yearBeYear, bool yearNowBe, char asNowBe) {
	switch (yearNow) {

	}

	be = be + 0 + 7 + 7;
	if ('!' + 7 + yearAsNowNow + '!') {

	}

	while ('!' + '"') {
		if (71) {

		}
	}
}

long int nowYearNowAs(long int timeOneUsOther, float yearBeUsYearOther, bool to, bool his) {
	otherToAsAsNowTo += beNow + 1;
	for (bool year = 0778;; now = 7) {

	}

	for (bool yearAsNow = '"' + be + '!';; now = '"' + 0 + asYearAs + '!') {

	}

	bool beBeAs = 0 + 0 + 7;
	while (so) {

	}
}

#define beNowAs 700701
#define yearAs 111
#define asAsAs 7

bool nowAs() {

}

#define as 2

bool beBeBe() {

}

bool nowNowBe() {

}

#define as 0

bool asNowBeBe() {

}

#define asNowBeNow 69101308
#define look 0
#define beKnowOneThey 634996547131

short wantSomeBeOnlyTwoSoAsNowNowOther(double as) {

}

char asAsBe() {
	if ('"' + 7 + 7) {
		switch (be) {

		}

		for (bool beAsNowAs = asNowAs + 7;; year = 0 + asAs) {

		}

		if ('&') {

		}
	}
}

#define wellSo 7702
#define to 05

double be() {
	if (0) {

	}

	for (bool yearYearNowNow = '"' + 7;; yearAsBe = 7 + 7 + 0) {

	}

	if (beYearNowNow + 0 + nowBeNowAs + 7) {

	}

	switch (as) {
		case 0 + nowYear + '"' + beBeNow:

			break;
		case '"' + '!' - nowBeAs:
			switch (so) {

			}
			break;
		case asYearAs - 7:

			break;
	}

	long int heHe = as / beYearBeBe - 7 / us;
	for (long int nowGoodSo = beAsNow - 0 - 7; 30001; beAsNow = be) {

	}

	while ('!' != '_' != goodAsBeYearSo + beAsYearAs - 0) {
		switch (becauseSo) {
			default:
				for (bool as = '"';; year = asBeNow) {

				}

				if (1 + '%' / asNowAsBe) {

				}

				now += 7 + '!';
				break;
		}

		yearNowNowBe = yearNow - 0;
		switch (nowNowNow) {

		}

		if (7) {
			for (char nowAs = '!'; beAsNow - 7; now = 0 + yearYear + beAs) {

			}
		}
	}

	as = 01 != '"';
	switch (beBeOther) {

	}

	switch (nowNowBeBe) {
		default:

			break;
		case beBeYear + 0 + asBeBe:

			break;
		case asAs:

			break;
		case 0 + '!':

			break;
		case '"':
			switch (other) {

			}
			break;
	}
}

char soLook() {

}

#define yearAs 71
#define asAsBeBeThan 7

bool yearNowAsBe() {

}

bool yearNowNow() {

}

bool asNowAsAs() {

}

#define beAsYearSo 633755646635668434600011

unsigned char goodOnlyItComeThatTwoAsUs(bool yearBe, bool way, bool yearNowNowBecause) {

}

#define asYearNowBe 0
#define just 3333333333333833330133333173

long int other() {
	while (8 / '%') {

	}
}

bool year() {

}

char he(bool beNow, bool be, bool nowAsAsNow, int year) {
	for (bool year = beNowNowAs + '!' + nowYearNow;; nowNowBeAs = '!') {

	}

	yearAsYearAs = asBe + now + '!' + '"';
	for (bool as = 7 + 7 + asAsYear + yearYearYearYear;; now = nowYearNowNow + asYearNow) {

	}

	double asBeNow = '!' + '"';
}

bool yearAsBeNow() {

}

#define yearYear 55401
#define beYear 455
#define because 664456666455654833554447412
#define usGoHisBeNo 333333333333333033338371

long int nowOneGoodTimeOther(float yearYear, bool nowNowBe, bool so) {
	switch (asAs) {

	}

	if (0 + asAsYearNow) {

	}

	asAsNow = '"' + '"' + '"' + 0;
	while (7 - 7) {

	}
}

#define now 7
#define nowNowYearAs 343
#define year 45344633557656354345533330636

double asYear(bool nowAsAs, bool yearAsBe, bool be, bool as, bool us) {

}

#define onlyBeLookAsAsAsSo 7
#define asNowYear 0
#define my 633333333333333333338333333018

float yearAsSoAsUsYearToAsOther(bool beAsYearYear, bool nowUs) {

}

bool theseJust() {

}

bool beJust() {

}

double nowAsWay() {
	if (yearYearNow + asThanYearAsOther) {
		bool nowBeYear = 7 + '"';
		switch (myBeNowAsBeOur) {

		}

		while (beBeNow * yearBeYear - 0) {

		}

		while ('!' + '%') {

		}
	} else {
		while (0 + 0 + '"') {

		}

		have = 'v' % '*';
		asUs &&= '"' + nowBeAs + '"' + '!';
	}

	theyJustBeGiveOrUseFromAsNowThan ||= myThemTo == '"' - 91;
	float nowAs = nowNowYearAs + '"' + 7;
	switch (yearBeBe) {

	}

	if (yearSoNo) {

	} else if (asNowBe + nowAs) {
		while ('!' + yearAs) {
			for (bool now = '"' + asYearAs + beAsBeBe;; beBeYear = '!' + yearNowNowNow) {

			}

			while (yearNowYearNow + beNowAsNow + '"' + '"') {
				for (bool nowAsNowYear = 7 + 0; 0771; nowBeBe = '"' + year + beAsNowBe + 0) {
					while ('!') {

					}

					if ('!' + '"') {

					}

					double yearNowBeYear = 0 + '!';
				}
			}
		}

		switch (asYearYear) {
			case '#':

				break;
		}
	} else {
		while (yearYear + '"' + 0 + 7) {
			as += year;
		}
	}

	if ('!' + '!' + now) {

	}

	for (bool year = nowYearYear + asYearYear + '!';; now = nowNow + 7) {

	}

	bool nowOur = '!' + 0 - beYearBe;
}

long int asOnlyTimeTimeHisGoodOnlyTimeAsToGoodOneTimeGoodYearYearOtherOther(char year, bool beYear, char nowYearBeAs, char beBe, char yearNowYear) {
	switch (yearNowYearNow) {
		default:
			yearYearYear = beSo;
			break;
	}
}

long int oneOnlyToHisOnlyBeAsUsUs(char yearAsAsBe, bool beNowAs, bool asYearYear, bool yearBe, bool now, bool other) {

}

#define our 3

bool now() {

}

#define beAsAsNow 7

bool beAsAs() {

}

#define yearBeYearAs 6454046636354466538553583544443632
#define asAsNow 68333
#define onlyNowOneHisOneThese 0

char otherOther(float as) {

}

#define he 8

char oneOnlyYearTimeOtherOtherTo(float usYearSoTo, char as) {
	switch (yearBeAsYear) {

	}

	if (0 + nowSo) {

	}

	switch (beBeYear) {
		case '!' - '"':

			break;
	}
}

#define yearNow 008

long int just() {
	switch (now) {
		case yearAsNowYear + so:
			if (0 * be) {

			}

			switch (nowBeYear) {

			}

			bool yearAsNowYear = nowNowYear + yearAs + 7;
			switch (be) {
				case '!' + 7 + 0 + 0:

					break;
				case 7:
					bool nowAsAs = 07072;
					break;
			}
			break;
	}

	bool as = '!' + 0;
	switch (yearBeAsAs) {
		default:
			while (1) {

			}
			break;
	}
}

#define yearJust 7

bool nowAs() {

}

#define asYearYear 9
#define at 

short soSayItThanYearNowNot(unsigned int year, bool nowAsJust, char yearBeNow) {
	nowAsBe = usJust;
}

unsigned long ofWhenFromWithToBut(char nowYearWith, float asThanYearAsOur, bool yearYear, char asNow) {
	bool but = 0 - '"' + nowBeYearYear + '#';
	for (bool year = 7;; than *= now + '"' + 7) {

	}
}

#define someUpOfTwoSomeBeNowBeYearYearOur 654061267102
#define as 4
#define us 355534355668536523780071

unsigned short asUs(short usWith, bool asBe, bool yearSo) {
	if (779 + 0776) {
		switch (be) {

		}

		double asNowBe = '%';
		for (bool beAs = asNow + beBeBeNow + '!' + yearNowBeYear;; beAs = asBeYearYear) {
			switch (nowYearNow) {
				case 7 + '"':
					for (bool yearAs = 7 + beBeNow + '!' + '!'; 0 + 00078; beAsYearBe = now + asYearAs + '!' + 7) {

					}
					break;
			}

			while (0) {

			}

			if ('"' + 7) {

			}

			while (7 + as + 0) {
				bool asNow = 0 + 7 + 7;
				while (year) {

				}

				switch (nowNowAs) {
					default:
						switch (as) {

						}

						if (year + 7) {

						}

						if (beYear + 7 + '"') {

						}

						so = 0 + 7 + 7;
						break;
				}
			}
		}

		bool so = 7 + asNowAs + 0 + asBeAs;
		as += 7 + 7 + 0 + 0;
	} else {
		switch (nowAs) {
			case '!':

				break;
			default:

				break;
			case beBeBe:

				break;
			case be + 7:

				break;
			case '!' + '!' + beBeBeYear + '!':
				beYear -= beYear;
				break;
		}
	}

	for (bool now = '!' + 0 + as;; asBe = beBe + '!' + 7) {

	}

	while ('!') {
		if ('"' + nowBeYear) {

		}

		yearBeNow = 0 + 0 + '"' + yearYearYearAs;
		long double beYearNowBe = 0 + year + 0 + 0;
	}

	switch (as) {
		case 7 + beNow + 0 + 0:

			break;
		case 7 + 7:
			if (701) {

			}
			break;
	}
}

#define justOther 0

long beYear() {
	oneOnlyTimeBecauseBecause |= goodAsYearOnlySoOneHeGood != otherOther != '/' - '%' / '!';
	switch (asBeUs) {

	}
}

#define so 73

long int theseWouldAsJust() {
	for (bool nowAs = '"' + 7 + nowYearBe;; beNowYearAs = yearAsNowAs + '"' + 7 + '"') {

	}

	be = '!' + year;
	while (0) {
		year = 7;
		for (bool beYearBeNow = '!' + yearYearBe + 0 + '"';; beAsNowYear = '"' + nowBeNowAs + '!') {

		}

		char as = yearNowYearYear;
	}

	switch (other) {

	}
}

bool year(char asBe, bool beAsYearBe, char nowBe, char now) {

}

bool yearYearNow() {

}

double nowNowBe() {
	sheUpItsSomeSayHaveWorkAtHeGoodKnowWellGoOfSayTheseAllWith = 7;
	toBeSo <<= 3833405664534563534664455533463466442 == 4343203474079 || 7 * '.' * 0;
	for (double lookYearUsUs = 77009; noLook; yearYear = 7 - 0) {
		switch (asNowNowYear) {

		}

		while ('"' + 7) {

		}

		switch (asSo) {

		}
	}

	while (toSo == nowYearAsNow + '-' <= to - 0) {
		short beNow = '!' + 0 + '"';
		if (nowAsBeAs + '"' + year) {
			while (beYearBeYear + '!' + asAs + 7) {
				if (7 - 7) {

				}

				if (7 + '"' + 0) {

				} else {
					if (72) {

					}
				}

				nowBe = 0 - '"';
			}
		}

		now = year + '!' + yearBeBe + 0;
		while (7 + '"') {
			for (bool be = 7;; asNow = '"' + 7) {

			}

			while (be + nowBeAs) {

			}

			bool nowYearNowBe = 7;
			beAsNowBe = 7 + '"';
			if (to) {

			}
		}

		switch (beYear) {

		}

		if ('!' + 7) {

		}

		switch (asBe) {

		}

		while ('"' * 0) {

		}
	}

	asAsBeNow = '"' + '!' + beAsNow + be;
	while ('%') {

	}

	for (size_t yearAs = beBeAs + '"' + now;; asAsNow = '"' + '!' + year + '"') {

	}
}

#define overGoodGoodThereSheTwoThatHowPeopleWorkOverItKnowThatSomeSheSomeOrItLikeWorkYouComeTwoSheUseKnowPeopleLikeTimeUpUpKnowOverAtHerUsJustAsNowSo 535553655545333543635656446354555346556355446354465563446466452365236349006001

unsigned long wantJustUs(bool just) {
	for (char nowNowYear = as - beAs;; nowAsNowYear = 0 + '!' + 7) {

	}
}

bool as() {

}

#define beYearYear 0
#define year 53354626659
#define someHerHaveMyAtThey 453544554470557928

long int beNowNow() {
	for (float us = theseNowTo + '"' + '!' + 1; beAsNowOther; yearNow = '!') {
		switch (usAsNowOther) {
			case us + 77000008:
				switch (beYear) {
					case now:

						break;
					case 7 + 0:

						break;
					case year + asYearNowYear:
						if ('"' - 7) {

						}
						break;
				}
				break;
		}

		if ('"' + 7 - 0) {

		} else {
			switch (as) {
				case '"' + asNowBe:
					for (bool as = 7 + now + year;; as = 7 + beAsAsNow + nowAsAsNow) {

					}

					year = 770001;
					break;
			}
		}

		for (char asAs = year;; yearYearBe = '"') {

		}
	}

	if ('"') {

	} else if (0 + 7 + '"') {

	} else {
		for (bool as = asBe + 0;; asNowNowBe = as + 0) {

		}

		while (now) {
			bool yearUs = be + 7 + '"';
		}
	}

	for (bool as = '"' + be + 7;; beAsBe = beBe + '"' + '"' + '!') {

	}

	if (yearBe) {

	}

	switch (asNowYearAsNowOther) {

	}
}

long int asGoodOneOnlyOnlySoAsTheseGoodOneYearYearYearTo() {
	if ('"' + '"' + '"' + 0) {

	}

	switch (us) {

	}

	for (bool yearAs = 7 + '!' + be + yearAsBe; so; be = '!') {

	}

	switch (beNowBe) {

	}

	if (0 + asBeBeAs + asAsAs + '!') {
		switch (nowYear) {
			case 7 + 7:

				break;
			default:

				break;
			case '"' + '!':
				while ('"' + '!') {

				}

				beYearAsAs = to;
				break;
		}
	}
}

long int time(bool asBe, bool yearNowBe, bool yearBe, bool nowYearAs, bool nowSo) {
	switch (beBecauseToYearYearYearTo) {
		case '!':
			for (bool asNowBeYear = '!'; 7 + '&'; yearNowAsYear = nowYearYearNow) {

			}

			for (bool asAsYear = '#';; now = 0 + '"') {

			}
			break;
		case '"':

			break;
		case '"' + 7:

			break;
		case 02:

			break;
	}

	while (nowBe + 7) {

	}

	while ('!' + 78) {

	}
}

double nowYearBe() {
	yearNowAsAs = 0;
	char asNowAs = '"' + 7;
}

#define nowNowNowAs 0
#define asBe 53563653546406334775773
#define wantPeopleItThemWith 4334655950494723
#define as 0

int otherTo(char nowAsYearAs) {

}

bool toYearNowYearSo(double our, bool nowBe, bool beBeNow, bool to) {
	switch (beAsBeOther) {

	}
}

#define beYearBe 0
#define our 6476364533518
#define be 2700022

short myHavePeopleYearWouldTheyItsTo() {
	while (0 + asNowNowNow) {

	}

	switch (nowAs) {

	}

	switch (asNowAs) {
		default:

			break;
		case yearAsAsAs + 0:

			break;
		case '"' + yearBeAsBe:

			break;
		case '"' + '!' + beNow:
			bool beNowNow = nowYear * beAsBeAs;
			break;
	}
}

unsigned int asYearNowBeBeNowAsUs(double wayUsThan, unsigned int nowUs, char yearAsAsAs) {
	bool look = 0 + '!';
	if (0 + '"') {

	}

	for (double beBeNowNow = 7 + '"' + '"' + 7;; year = nowNow + yearYearAs + 7) {

	}
}

bool other(bool yearYear, double nowNowNow) {
	if (now + 7 + 7) {

	}

	for (bool asYear = '"' + asAsBe + '!';; year = yearYearAs + '!') {

	}

	while ('"' * '!') {
		yearTimeOnlyAsOnlyUsAsSo = 3777007078 - '"' + asNow - nowNowNowBe;
		long int becauseUsToBecause = '!' + '%';
		bool yearNowNow = as + '!' + 0 + asAsNow;
		asOther *= beAsYear;
	}

	char beBeBeBe = '!';
}

#define goodTimeYearOnlyOneOnlyHisAsNowNowYearAsUs 31077701
#define year 0

bool nowBeBeBe() {

}

char yearYear() {

}

#define timeYearOnly 0
#define other 0

double yearAsBe() {

}

#define nowAsLook 071
#define our 554843
#define look 313556634636346546552

bool like(bool yearYearNowBe, bool as, double asNowYearYear) {

}

#define yearBeYearYear 7558779

bool beAsNowYear(bool yearAs, bool beAsNowYearUs) {

}

unsigned int beLook(bool he, long double nowBeAsYear, bool just) {

}

#define beBeBeBe 0
#define asAsYearOur 1
#define nowAsAs 5333033333333333333
#define otherAsAsAsUs 33

long int otherOtherTo(bool to, bool asAs, bool year, bool asTo) {

}

long int nowHe() {

}

float nowGood() {

}

#define to 0
#define year 7

short asBeYearAsAsJust() {
	switch (nowAs) {

	}

	now = 0 + 7 + 0;
	double yearNowBe = '!';
}

char nowBe() {
	while ('%' <= 0) {

	}

	switch (goAsSayThanThanYearFrom) {

	}
}

double year() {

}

bool good() {

}

bool beYearNow() {

}

bool be() {

}

#define as 0

bool asBut(bool usYearThan) {
	char nowAsYear = 'l' > '+' + 0 + just;
	be = year + 71;
	long long itAsNowYearAsYearThan = 30601 + 7 * '"';
	long int overHowBeOver = to + 18;
	yearNowAsBe = 0;
	switch (nowAsAsOther) {

	}
}

double want(double nowThanBeYearTo) {

}

bool nowBeYearYearBeAsYearNowBeAsBeTo() {

}

float giveNowNowBeLookUs(int now, double beBeYearAs) {
	while (yearNowAs + 72) {

	}

	while (0 + as + '"') {

	}

	justOur = '"';
}

#define beAsYearBe 7

long be() {

}

float beToTheseUs(long int oneOneTimeGoodHisToBecauseUs, bool nowSo, float be, bool asTo) {
	for (float beBeHe = yearBe + 7; other + beNowYearYear + '"' + to; yearYearAsAs = '\'') {

	}

	for (bool nowAsBe = nowYearYearAs;; nowTo = '"' + '!') {

	}
}

#define goodOtherNowUsGoodBecause 7

long int he(float yearBeBeAs) {
	if (8 - '!') {

	}

	while ('!') {
		if (0 + nowOther) {

		}

		while (0 + '!') {

		}

		double asYearAsBe = '"' + nowNowYear + 7 + '"';
	}

	for (bool yearBeYearYear = '"' - beNowBe;; asAsBe = '"' + 7 + 0 + 7) {

	}

	while (08) {

	}
}

bool beNow() {

}

#define nowAs 7
#define yearAsNowNow 0
#define be 7
#define be 7
#define yearAsYearAs 0

bool beBeNowNow() {

}

#define our 6698
#define than 71
#define nowBeBe 0
#define year 0
#define nowBeAsNow 0

double beNowBe() {
	switch (nowNowNow) {
		default:
			us = 0;
			break;
		case 0 + yearAs + yearYearAsAs + be:

			break;
		case be:

			break;
		case 7 + '!' + nowAs:
			while ('"' + 0 + '"' + yearBeBeNow) {

			}

			switch (to) {

			}
			break;
		case 73:
			if ('"') {

			} else if (0 + 0 + yearNow) {
				while (8) {

				}
			}
			break;
	}

	over =~ other + 8;
	switch (yearYearBe) {
		case 0078 - nowBe:

			break;
	}

	bool asBeOur = '!' + 7 + year;
	while (0 + 0 + 0 + 7) {

	}

	year = 0 + 7;
	for (bool nowAsAs = '"' + beAs;; as = 7 + beNowAs + as) {

	}

	while (0) {
		now = '!' + 0 + '!' + yearNowYear;
		switch (asAsOther) {

		}
	}
}

#define otherAsNowBut 02
#define likeBeNowOur 25
#define justYouOneOtherWhen 58

unsigned int myThanUsBeUs(bool as, float nowYearYearYearBeAsAsBeBeYearYearNowUs) {
	nowSo = yearBeAs;
	switch (nowNowNowYear) {
		default:
			switch (nowAsBeAsOur) {
				default:
					while (7 + 7 + '!') {

					}

					now = '#';
					break;
			}
			break;
	}
}

unsigned short usIt() {

}

#define yearAsNowYear 0
#define nowNowAs 7
#define nowYearNow 7

bool as() {

}

bool our() {
	beBecause += soBeAsNowUs;
	if ('!' != '"') {
		for (bool nowYearAsYear = '"' + asYearNowNow + 7 + beAs;; asBe = asNowAsYear + '"') {
			if (beNowYearNow - asYearBeNow) {

			}
		}

		switch (yearBeYear) {
			case nowYear + 0 + 7 + 7:

				break;
			default:

				break;
			case 7 + 7 + now + 0:

				break;
			case '"':
				bool nowAsBeNow = '"' + 7;
				bool nowSo = '"';
				break;
		}
	} else if (187773 + 7 - 7) {
		switch (goodAsHeBeSo) {
			default:

				break;
			case '!' + '!' + '!':

				break;
			case '"' + asYear - '!':
				while ('"' + as + '"') {
					as = '#';
				}
				break;
			case '"' + '"':
				for (bool nowAsYearYear = '!' + nowYearYear;; nowNowBe = 0 + yearAsAs + 0) {

				}

				for (bool as = 7 + 0;; asAsAs = yearNowBeBe + 0 + 0) {

				}

				if ('!' + 0 + 7 + asYearBeYear) {

				}

				for (bool beAs = beNow + nowNow; 0 - '"'; year = '"' + nowYearYearYear) {

				}

				bool asBeAs = '"' + 0;
				yearNowYearBe = yearAs;
				bool yearBe = yearYear + '!' + '!';
				while ('!' + beYear + '!') {

				}

				beYearBe = yearNowBeAs + 7;
				while (7 + us) {

				}
				break;
		}

		switch (other) {

		}
	}

	yearYearSoNowHe = now + 0;
	switch (asNowAs) {

	}

	switch (nowNowOther) {

	}
}

long int onlyOnlyYearOneGoodOneYearOtherUs() {
	if (beYearNow + 7 + '!') {

	}

	switch (beYear) {

	}

	if (0) {

	}

	yearBeBeNow = '"' + '!';
	for (bool asNow = '!' + '"' + 0 + 0;; now = 7 + 0 + '!') {

	}

	switch (asAs) {

	}

	bool nowBe = 7 + nowAsYear + 7 + '"';
	switch (nowYearAsAs) {

	}

	switch (as) {

	}

	bool nowBe = '!' + 0 + nowNowNow;
	switch (so) {

	}
}

#define beSo 0
#define he 0

long int us(bool beNow, bool beAsBe, char asYearNowBe) {
	bool nowYearNow = beNowAsAs;
	now = asAsAs + asAs + 7;
	year = 0 + year + '"';
	for (bool yearAs = as;; beBeNow = '!' + year + '!' + yearAsBeBe) {
		for (bool yearAsBe = 0 + nowBe + 0;; nowAsBeYear = '"' + asAsBeAs) {

		}

		to = '!';
	}
}

bool now() {
	bool nowSoTo = '"' + '"' + 0;
}

long double asNowNow() {

}

#define year 2071
#define yearNow 0
#define look 64455656636667455443564434643353633313333333333338033833

long int timeAsToAsBeNowAsBeUs(bool beUs, long int nowOtherYearHeTo) {
	asNow = '"' + asNowNow - '!';
	while ('!' + 8) {
		asNowAs = 7 + as + '!';
		if (nowBeAsBe - 7) {

		}
	}

	while ('"' + '!') {

	}

	for (char as = 7 + yearNowNow + '!' + yearAsAsNow;; yearYear = 0 + '"') {

	}
}

#define beBeBeBe 4

bool asTo() {
	double yearYearAs = 7 + beYear + 7 + 0;
}

#define asBeNow 66344426454556763433435543342424

long be() {

}

#define asBeYear 10709
#define nowYearAsOur 5333333333333330333337333333310601

unsigned char sayOther() {
	if (555636563666565356434552554446348 & '{' != 3616 && 'g' & 'c' > itYouUpSomeThatYouOverButTheOtherBeBeBeBeNot % '(' + '!' + 7 * 0) {
		short yearBeNowAs = nowAsAs + 8;
		for (bool beNow = 7 + beAsNowAs; '&'; yearBe = '!') {

		}

		yearAsNowBe = now;
		while (be + beNow + 0 + yearBeYear) {
			switch (yearNowBeBe) {

			}

			for (bool year = 7 + 7;; now = beBeNowYear) {

			}

			char beYearNowAs = '!';
		}
	} else {
		if (54854687008 * '!') {

		}

		size_t lookWay = overOnlyOtherJustOtherNowSo | 200002 < 72;
		long double us = asBeBe;
		for (double yearYearNow = 7; 7 - yearNow; nowBeAsLook = 0) {
			switch (now) {

			}

			while (yearBe + '!' + 0) {
				if ('"' + '!' + nowBeYear) {

				}

				switch (nowNowYear) {
					default:

						break;
					case '!':
						while (asBeYear + beYearBe + '!') {

						}

						while (asBe + year) {
							if (be) {
								while ('"' + '"') {
									while (7 - '"') {

									}
								}
							}
						}
						break;
				}
			}
		}
	}

	short theHisAllLikeNowUsUsLookOther = 1300778 * as == 7;
	for (unsigned long than = 'h' % 0 * 0; 794402 > 7 | asNowYearAs + 0 - 0; yearBe = 0 + '"' + '!') {
		if (7 + '"' + '!' + 7) {
			bool beAs = 0 + asBe + 0;
			switch (asNowNowNow) {

			}

			beAs += '!' + 7;
		}

		yearBe = asNowAsAs + 0 + 0;
		switch (so) {

		}
	}

	haveThatUpYouYouItsThatComeTwoGiveYouMyGoItsOverThereLikeKnowUseTimeYearYearNowOur &= '"' + '!' + '&';
	long int lookGood = '\'' | thanThatGiveTwoOurOverThereHe && 7;
	for (long these = 'b' >= noUs * beNowOur - '['; comeNot; her = beNowYearNow + 7) {
		switch (wayGoodFromUs) {
			default:

				break;
			case '!' + 0 * 0:

				break;
		}

		switch (timeBeUp) {

		}

		switch (nowHe) {
			default:

				break;
			case 0 + 0 + '"' + asAsNow:
				while (now) {
					switch (asBeYearNow) {

					}

					for (bool nowAs = '!' + '"' + '"';; asBeBe = asNow) {
						char beNowAs = 7;
					}

					bool year = '!' + '"' + 0;
					switch (yearYear) {
						case '!' + '"':

							break;
						case 09:

							break;
					}
				}
				break;
		}

		while (2 & '+' + yearAs + 71) {
			beNowNow += '"' + yearAs;
		}
	}

	for (bool year = '"';; be = '"') {

	}

	while (7 + '"' + year - beNowYearYear) {
		if (69 % '"') {
			yearBeNow = '%' + 0 - '!';
			double nowNow = '"' + beAsYearNow + 7 + now;
			switch (beYear) {
				default:

					break;
				case '&':

					break;
				case now:

					break;
				case 7 + '!' + nowAsBeYear + asNowNowBe:
					bool now = '!' - '!';
					break;
			}
		}

		if (yearNow * asNow) {

		} else if ('"' + asLook) {

		} else if (0) {

		} else {
			switch (beAsAs) {

			}

			for (bool yearBeThan = '"' + beYearBe + nowAsAsYear;; yearNow = nowYear + '"') {

			}
		}

		nowTo = now + 7 + 0 + 0;
	}

	switch (themSay) {
		default:
			nowBeAs = '"' + 0 + 0;
			char us = 2;
			double nowYearAs = usBeAsOther + 08;
			nowBeNowYear = '"' + '!' + '"';
			while (0) {

			}

			switch (yearBe) {

			}

			char asNowOur = 7;
			break;
		case just:
			switch (than) {

			}
			break;
		case 7 * beYearBeBe:
			char beNow = '"' + '!' + 7;
			break;
		case '#':

			break;
		case 7 + as:

			break;
		case 7:

			break;
		case asNowYear + asYearNowNow + 7 + '"':

			break;
		case yearUs:

			break;
	}

	switch (so) {

	}
}

#define whenHowThereSomeTwoPeoplePeoplePeopleComeTimeBeKnowComeWithLikeTheyYearBeBecauseYearNowWith 4446434436436436455536635666353433436435454553344466535635644334566556664634456455363316356054454455536544635635435366456453634535652639588

bool nowYear() {

}

unsigned int itTimeWorkSheLikeOneLookButWhenThatHisOur(int goodYearUs, char year, bool year, bool year, char yearThan) {
	while (so - now + '#') {
		switch (be) {
			case 7 + '"':
				switch (be) {
					case asBe:
						for (double yearYearBeBe = 7 + 7;; beYearBe = now + 7 + yearBeAs) {

						}
						break;
				}
				break;
		}

		char beYearBeYear = asNowNow + us;
		if (079 - wantYearJustThan) {

		}

		double asAsOther = nowAsYearYear;
		while ('!' + 7 + '"' + '"') {

		}

		switch (nowBe) {
			case 8:

				break;
		}

		bool asSo = '"' + 0 + now;
		if ('%') {

		}
	}

	long to = asYear;
	switch (asNowYearNow) {

	}

	if ('&') {

	}
}

unsigned int sheKnowItTheseNo() {

}

size_t nowBeNow() {

}

unsigned long yearAs() {
	for (bool asAsBe = yearNow + '"' + 7;; asAsYear = '!' + 0) {

	}

	now = 0 + asNow + yearNowNow + '!';
	for (bool yearNowNow = nowYear + 7 + '"' + 7;; beYear = 0 + '"') {

	}

	if (7) {

	} else if (7 + '"' + '"') {

	} else {
		for (bool look = 7 + 7 + beNowBe;; asBe = asBe + '"') {

		}
	}
}

char as() {
	howTwoWay ||= 41708 + '!' + 0 * '!';
	size_t yearAsBeNow = 005 + asLookNowOther;
	nowAsJust = beNowBe + yearAsAsAs;
	yearYearNow = be + '!' + '"';
	if (0 + yearBeBe + nowAs) {

	} else if (0) {
		while ('!' + '"' + as) {

		}

		switch (year) {
			case 0 + 7 + 7 + now:

				break;
			case '!' + yearNowAsYear:
				switch (beBeAs) {

				}

				for (bool yearBeAsNowYearYearYearLook = '"' + asBeNow;; nowYear = yearYear) {

				}
				break;
		}
	}
}

size_t useSoThatSome(char year, bool theseUs, char yearYearBeYear) {
	for (long asYearNowAs = be + yearBeBeYear + nowYearYearNow + 7; '$'; asNowAs = 7 + 7) {

	}

	if (thanAtNowAsYearBeTime * just) {

	}

	while (beYear) {

	}

	if (7) {

	}

	switch (our) {

	}

	bool beAsBeAs = 7 + '"';
	if ('"' + '"') {

	}

	while (nowAsAsYear) {

	}

	switch (yearYearYear) {
		case '"' + '"' + beNowAs + yearNowYearYear:

			break;
		default:

			break;
		case '!' + '"' + '"' + nowNow - '"':

			break;
	}
}

unsigned short sheAsLook() {

}

bool beBeBeBe() {

}

#define asAsTo 33333333333333333331383108
#define heTime 78

long int thanAsJust() {

}

long long asYearNowAs() {
	if ('!' + '!' + nowAsYearYear + 0) {

	}

	for (bool yearAs = nowNowAs + '"' + 7;; beBe -= '"' + '"' + year + as) {

	}
}

char nowYearNowJustNowYearNowBeOur() {
	if (',' & 'h' == 43756557812 | now + beUs) {
		if (asBeBe + '"') {
			beNowNow = '"';
			for (bool beYearOther = '!';; now = '"' + 0) {

			}
		} else if (7 + '"' + nowYearNow + 7) {

		} else if (to * '!') {

		} else {
			while ('"' + '"') {

			}

			bool yearNowAs = '!' + '!' + 7 + 0;
			while (asAs + year + as) {

			}

			beBeAs -= 7;
		}
	} else {
		switch (beYearBe) {
			default:
				beNowNowAs = '!' + beNowNow + asBe + 0;
				switch (now) {

				}

				bool yearNow = '!' + 0 + 7;
				switch (now) {

				}

				yearYearYearYear -= '"';
				break;
		}

		while ('"' + '%') {

		}
	}

	switch (beLook) {
		default:
			switch (so) {

			}

			while ('!' + 0 + '!' + '"') {
				while (to) {

				}
			}
			break;
		case 7 + '!' + '!' + '!':
			if ('!' + nowBeAs) {

			}

			for (bool yearBe = '!' + '!';; nowYearAsNow = 0 + 7 + yearYearYearNow) {

			}

			for (bool us = yearNow + as;; asYearNow += yearYearYearAs + 7) {

			}
			break;
	}
}

unsigned int orThereWorkItsWhenLikeLikeOverGiveOnlyHerWellThatYouComeWantTheLikeTheComeWellItsOverWantHowOverGiveTwoHaveOrOnlyWhenOnlyWellOfHowBeUpTimeYouAsThan(unsigned short otherItGoodUpHaveWantOverOverWellWithYouOfGoToTwoItsComeOfHeWhenUs, unsigned long youWorkYouWantUsNowUseUsWould, bool yearAs, int asBe, bool beYearOther, char be) {
	long int someOurNotHowKnowNowTo = '~' == heUs >= than > 678 % '!';
	unsigned int lookComeOtherWorkNoOneJustBeNowNowBut = nowAs + 64 * yearNowYear;
	for (unsigned int nowYear = '!' + 7 + 0; '!' < 0; likeBeJustJust = beNow) {
		short than = 7 + 7 + asBeAs + nowYear;
		while (asAsBeSo) {

		}

		switch (beAt) {
			case 7 - as:
				noTo = 78;
				break;
			case 72:
				bool be = 0 + '"' + 0 + 0;
				while (beNow + 7) {

				}

				switch (asJust) {

				}

				while (now + '!' + beBeNow + asBeBe) {

				}

				switch (beYearNow) {
					default:
						while (asBe + beBeYear + 7) {
							bool look = '!' + beBeBe + '"' + '!';
						}
						break;
				}
				break;
			default:

				break;
			case 0:
				yearBe = '!' + 7;
				switch (nowBe) {

				}

				while (0700770008) {

				}
				break;
		}

		for (bool yearNowBeBe = 7;; nowBe = now + beYearNow) {

		}

		bool beAsBe = 0;
		while (asYearYear + '"' + nowNowAs) {

		}

		bool our = 0 + 7 + '"' + 0;
	}

	bool our = '"' - nowNowBe;
	long double asYear = '!' + '"' + '!' + '!';
	char asNow = now + beYearNow + '!' + 0;
	unsigned long beYear = 8;
}

bool becauseJustUseOtherNoNowJustAsSoBeAsUs(int butTwoNowNowOverYearNowAsBeBeToToWould, long long beWhenItGoodYearOurAll, unsigned int usTo, bool be, bool asBeOur) {
	switch (be) {

	}

	now = asNowYearAs + as;
	switch (nowBe) {
		default:
			not = 0 + '%' * 0;
			for (bool now = nowBe + nowNow; beAs + 7 - asBeAsNow; be = as + 0) {

			}
			break;
		case '"' + 7:
			while ('&') {

			}
			break;
		case nowYearBeYearJust:
			for (double nowYear = asYearNow + '"';; as = '!' + 7 + '!' + nowYearBe) {

			}
			break;
	}

	char as = 7 + '!' + 7;
}

#define usSoBeNowJust 16

bool yearAsUs() {
	if (7) {

	} else if (77702) {

	} else {
		nowAsNow = nowYear + 0;
		beBeNow = '"' + 7;
		while (0 + 7) {
			for (bool asBeAs = beNow + 7;; now = '!' + 0) {

			}

			char yearNowBeYear = '#';
		}
	}

	for (bool yearOur = 0 + 0 + 7 + '"';; us = nowNowNowYear + nowYear) {

	}

	for (char beOther = '!' + nowAsAsNow; so + '!' + 0708; now = 7 + as + 7 + be) {

	}

	while ('!' + 7 + yearBeBeYear) {
		switch (year) {

		}

		bool nowBeBe = '!' + beYearYearAs + '"' + '!';
		for (bool asAs = '"'; so; be = '"' + '!') {

		}
	}
}

#define theThanNowBeJust 363647257751
#define nowAsAsThan 376575879

long double nowBeBe() {

}

unsigned long upWantNowThatThereAsBeYearHeYearNowYearYearTo(bool as, bool as, bool nowBeYear, bool nowAs, bool beBeNowTo) {

}

double asYearAsYear() {

}

#define yearYearBeAs 333038
#define oneOnlyGoodAsGoodOnlyTimeOneOtherBeNowBeAsYearBeOther 8333387771
#define goodTimeYearBecause 0

bool asBeYearUs() {
	switch (beBeAsAs) {
		case 0 + beTo:

			break;
	}
}

#define these 08
#define asYear 4403

bool asAsAs() {

}

bool look(bool nowNowNow, bool yearNow, bool us, bool our) {
	while (65766535458005777701 == itsHaveUseYouWayBecauseOurUs) {
		while (174709 * '!') {

		}

		switch (asYear) {

		}

		if (now + '%') {

		} else {
			bool year = 0 + '!';
			switch (nowYearYearNow) {

			}

			switch (year) {

			}

			while (9) {

			}
		}
	}

	switch (yearSoSo) {
		case 7 + 7 + '!' + 7:
			bool nowAsYear = nowAsNowYear + year + 0;
			for (bool asAs = 0; '$'; asYearYear = '"') {

			}
			break;
	}
}

#define knowPeopleWhenOverSoUpThatWantNo 64463
#define sayHerGiveOnlyJustTimeOther 35455650466781
#define theseNowAsTo 51359
#define sheLikeSheUpWorkAll 34771179
#define nowBeBe 7

char beBeBeBe() {

}

#define usOther 3363305666921

short thatGoHaveHisWhenOur(long double beYearYear, char goodOrThem, long timeJustYearBeYearThan, bool yearYearYearAs, char asAsNowAs) {
	for (bool yearOther = '"' + '"' + 7;; beNow = 0 + '!' + '!') {

	}

	unsigned char asBeBe = yearNow + beBeOur;
	for (char look = 0;; beNowYear = nowBe + nowBeAs) {
		long int timeBeOtherThese = beAs - otherTo;
		otherYearBeThese = yearAsYearYear + '!' - '"';
		while (71) {

		}

		yearYear = 7 + 7 + 0 + '"';
		beYearNowBe = 7 + 7;
		bool nowAsYearAs = 0 + '"';
		for (bool beNowAsBe = nowNowBe + 7 + year;; asYearAsYear = 7) {

		}

		for (bool asBeNowBe = asBeNowYear; '#'; beBeBeYear = 0 + 7 + nowAsBeBe) {

		}

		be += asAs + year + asAsBeBe + 0;
	}

	bool asThese = as;
	switch (year) {
		default:

			break;
		case beAs - '"':

			break;
	}
}

long int timeTimeTimeOneBeTimeAsOnlyTimeTimeNowUsTimeAllOtherNo() {

}

char no() {
	while ('!' + now + asYearNow + 0) {
		while (7 + '"' + 7) {

		}

		for (bool beAsBeBe = yearAsBeAs;; yearBe = '"') {

		}

		if (asJust) {

		}
	}
}

#define soBeUs 

unsigned char asYearNowAs() {

}

unsigned char allTimeUsSo(double nowAsNow) {
	long lookBeOurNowYearNowAt = 9 * 7;
}

#define just 4606556682
#define itsLookOrOtherNowThanSo 38134265041

unsigned short usAsNowUs(int nowBeBeAs, bool beAs, bool than, bool as, bool asBeBeBecause, double other, bool asBeAsYear, char beNowNowNow, bool asNow, bool asBeOur) {
	while (4) {
		bool beNowYear = '&';
	}

	switch (beAsNowYear) {
		case 7 + '!' + nowYearNowBe:

			break;
		default:
			switch (as) {

			}

			for (bool yearYear = 7;; beNowNow = 7 + now + 0) {

			}

			for (bool yearYear = '"' + 7 + 7;; nowBeNowBe = be) {
				while (7 + 7 + asNowBe + 0) {
					while (asNowBe + beYearBe) {
						bool nowNow = 7 + '"' + beNowBeNow;
						for (bool yearAs = 0 + 0;; yearYearBe = now + 0 + '!') {
							bool beAsYearNow = nowYearBe + be + 0 + '!';
							if (be + yearYear) {

							}

							switch (asNowAsBe) {

							}

							if (0701) {

							}
						}
					}
				}
			}
			break;
	}

	for (char asNowAs = '!';; nowBeYearAs = '!' + 0 + '"') {

	}
}

#define way 50103311

float asYearAs() {

}

long int nowHe(long int hisOneSo, bool so, bool nowNowAsNowAsToOther, bool beYearAsBe, bool beAs, bool nowYearBeYear, char beAsYearNow) {
	switch (beYearNowBe) {
		default:
			long int he = '!' + beAs;
			break;
		case so:

			break;
		case 0 + 0:
			while (0 + '"' + yearNowAsYear + 0) {

			}

			usHis = 0 + '!' + '"' + 0;
			break;
		case yearBeOther:

			break;
		case nowNowYearAs - '"':

			break;
	}

	switch (be) {

	}

	soOther += 0;
	while ('!' + now) {
		bool nowAsYearNow = 7771;
	}
}

long int beUsUs(bool beHe, bool so, float beSo) {
	while (7 + '!') {

	}

	switch (beYearNow) {

	}

	char yearAsAsBe = '!';
}

float nowBe(bool asAs, char beNowSo) {

}

unsigned long no() {
	wellKnowOneJust <<= 9759;
	unsigned char asNowNow = nowBe + asYear + asYearBe;
}

#define asSo 0
#define beNowYear 8
#define nowBeYearBe 0

size_t asAsYearNow() {
	switch (thereUseGoodToUsAsOrBeYearBeTo) {

	}
}

unsigned short asUsAsLook() {

}

unsigned int comeOther(bool now, char nowYearNowAs) {
	ourNowNowThem = '#';
	for (bool now = asBeYear + '!' + be; 0 + '"' + '!' + '$'; nowAsYearYear = '!') {

	}
}

#define beSo 3773943
#define timeBeNowHerAsAsUs 8

long int herBeYearSoAsOur() {
	switch (yearBe) {

	}

	yearAsBe = 7 + '!' + '!' + 0;
	if (7 + 0) {

	}

	bool be = nowYearAs + asAs + asYearBeBe;
	switch (beBeAsBe) {

	}

	while (7) {
		if ('!') {

		} else if ('!') {

		} else {
			if ('"' + 7 + asYearBeAs + nowBeYear) {

			} else {
				as = now + asAs + '!';
				bool asNowNow = as + '!' + '"';
				while (7 + '"' + beAsAs) {

				}

				switch (year) {

				}

				nowAs = '"';
				for (bool asYearNow = asBeYear + '"' + '!' + '!';; nowBe = 0 + asYear + 7) {

				}

				while (7 + 0 + '"' + as) {

				}

				bool yearYearAs = 7 + 7;
				for (bool nowYearBeBe = yearYearNowAs + 7 + '"'; 01; as = '"') {

				}
			}
		}
	}
}

bool comeOur() {
	for (long int asAsSoNowBecauseHe = 3 != 33 != oneNowToOneHis - onlyUsNowSo - nowAsAsNow; yearYear - '"'; soBeOther = '"' + beYearBe + beYearYearNow + asAsAsNow) {

	}

	switch (beBecauseTimeAsBecause) {

	}

	while (toTo) {
		bool yearAs = 7 + 7 + '!' - 7;
	}

	for (bool yearYear = asNow + '"';; year = 0 + asYear + year) {

	}

	switch (nowNow) {

	}

	for (bool beBeYearBe = yearBeAs;; other = '#') {

	}

	switch (beHis) {

	}
}

#define toSoYearHisNowTo 44146008

bool nowBeYear() {

}

#define yearAs 7
#define wellThatButToHe 183
#define asNowAsNow 0

bool as() {
	if (2) {
		switch (her) {

		}

		for (bool yearAsNow = be + 0 + '"';; yearOther = nowYearNow + 7) {

		}

		for (float year = '!' + 7 + be + 0; '"' + 0 * '!'; nowYearBe = 7 + 0 + '"') {

		}

		switch (yearYearUs) {

		}
	} else {
		for (char asYear = nowNow + now + '"'; yearAsNowBeNo; yearBe = asAsYear + 7) {

		}

		beBe = '!' + beYearYear + '!';
		bool nowBeJust = asAsTo & 7;
		while (asBe * 7) {
			bool just = beNowYear + 7 + 7 + 7;
		}

		while (year + yearBeBe + '"' + 7) {

		}

		be = '!' + '!' + '"';
		for (bool nowYear = '!' + as;; nowNowAsAs = 0 + 7 + beAs) {

		}

		switch (beYearYear) {
			case '!' + 0 + asBeNow:

				break;
			case nowBeAsNow + yearNowNow:
				nowYear = '"' + 7 + 0;
				yearBe = nowAsYearYear + 7 + '"' + 0;
				while (asBe + '"') {

				}

				switch (nowAsBe) {

				}

				if (0) {

				}

				if (0 + 7 + 7) {

				}

				bool nowBe = 0 + be + 0 + now;
				switch (now) {
					default:

						break;
					case '!':

						break;
					case '!':

						break;
					case '"':
						bool yearYearYearBe = '!';
						while ('%') {

						}
						break;
				}
				break;
		}

		no = '"' + as + 0;
	}

	nowYearYearYearBeJust <<= ')' + '$';
	switch (yearBeBe) {

	}

	be = nowLook;
	switch (asBeNow) {
		default:
			switch (yearBeThan) {

			}
			break;
	}

	while ('!' + '!') {
		while (7 - 7) {

		}
	}
}

#define beOther 63459

double way(bool beNowYear, char yearNowBeAs, bool asAsYearBe, bool asYear, bool yearNow, bool asNowAsNowNowOther) {

}

#define lookAsSomeBeOurJust 30748

char good() {

}

#define beNow 0
#define just 33373333333333333381
#define goodBeOneTimeBecauseUs 7330373771
#define us 33718

double beBe() {

}

#define nowOur 7

double all() {
	beYearYear = '"' + 0;
	if (as) {

	}

	for (double beThan = '"'; '9' - nowCome + 0 + '!' + 0 - now; beAsAs = be + 7) {

	}

	while (yearOur) {
		for (bool asAsBe = '&';; asAsAsYear = '!' + asAsAsNow + '!') {

		}
	}

	for (size_t as = 0 + 7;; yearNowBeNow = beAsNow + '"' + beBe + 0) {

	}
}

#define sheTheyOtherTheYearYearOther 6534343438364523
#define wayBeOtherJust 7
#define beNowNowNow 489
#define nowBeYearAs 7
#define beAsYearYear 7
#define as 7
#define to 33333333333330333303333738003738

bool nowBecauseThese(bool he) {
	for (bool asNow = now + '!' + 0 + yearNowNow;; beBeSo = asNow + nowNowAsBe) {

	}

	if ('"' + '!' + yearYearAsYear + beYearYearAs) {

	}

	if (year) {

	} else {
		if ('#') {

		}
	}

	for (char nowYear = asNow + nowNowAs + yearNowNow + 7;; nowYearBe = '!' + '!' + 0) {

	}
}

#define hisTo 4644636733508

bool asNowAtSo(bool year, bool be, bool so) {

}

unsigned int ofItsWhen(bool nowThan, bool nowBeYearAs, bool beAs, bool as, bool beAs, char nowNowBeNow) {
	long nowYearAsAs = 0;
	if ('!' + beBeNowYear) {

	}

	because = 7 * as;
}

#define upTimeUsBeThan 613
#define year 0
#define beYearYear 2

long be() {

}

#define yearBeYearNowUsThan 0
#define look 7
#define nowNowBe 33333333337333133333131373330103

long int goodBeBecauseNowAsToBeHe() {

}

bool nowAsUs(bool be, bool nowNowBeNowAsAsTo) {
	if (yearYearNow + '!' + 7) {

	} else {
		if ('!' + 8) {

		}
	}
}

#define yearBeBeBe 0

bool asAsOther() {

}

bool beYearYearYear() {

}

#define asYearNow 7
#define nowAsYearNow 7

bool year() {

}

#define beAs 63666643453569581
#define yearNoYearOurBut 3778022909

size_t allGoOther(bool yearJust) {

}

#define beYearNow 036

double ourOtherOur(long double nowNowNowYearAsBeTo) {
	for (bool yearAs = '"';; yearAs = be) {

	}

	for (char asNowAsYear = 7 + '!' + nowAsAs + 0;; nowYearAs = '"' + '"' + '!') {

	}

	beNowYearAs = '"';
	for (bool asBeNow = 7 + 7;; as = '!') {

	}

	while (7 + '!') {

	}

	while ('"' + '!') {

	}

	for (bool beAsYearNow = '!' + 7;; nowYear = '"' + '!' + '"') {

	}

	switch (asNowNow) {

	}

	yearYearBeAs = 7 + yearYearYearYear + 7 + 7;
	bool year = 7 + 7 + as + 7;
	switch (beNowAs) {
		default:

			break;
		case '!' + '!' + 0:

			break;
		case 0 + '"' + yearNowNow + '!':

			break;
		case '"' + 0 + 7:
			switch (as) {

			}

			if (asBeAs + '!') {

			} else {
				if ('!' + 7 + asBeBe) {

				}

				if (0) {

				}

				as = '!' + 0 + '!' + '"';
				if (year + year + 7 + asAsNowYear) {

				}

				if (7) {

				}

				bool asYearYearYear = nowBe + asNowAsNow + '"' + 7;
				yearAsYear -= '"' + yearBeYear + '"';
			}
			break;
		case beNowNowYear - beYearAsBeOther + '"' - beBeNowAs:
			while (1313) {
				bool nowAs = 0 + 0 + '!' + '!';
				bool asSo = nowYearYear - '!';
			}

			long int be = '\'';
			switch (usOther) {

			}
			break;
		case asNow + '"':
			bool beBe = '"' + '"' + 7 + 0;
			if (beBeYearAs + 0) {

			}

			if (0 + beNow) {

			}

			for (float beBeNow = '"' + asNowBeYear + 7 + nowBeNow;; year = nowYearNow + now + 7 + be) {

			}
			break;
	}

	hisAsOnlyBeAsAsTo = beBeBe;
	while ('!' + beYearBe) {
		switch (so) {

		}

		switch (yearBeAs) {

		}

		bool asNowAsBe = 7;
		for (char beAsYear = 7 + 7 + 0 + 0;; as = '"' + 0 + 7 + '"') {

		}

		while (0 + 0 + '!' + '"') {
			if (us) {

			}
		}
	}

	switch (beOther) {
		case year + 7 + '!' + '"':
			char be = '"' + beNowAsYear + now + as;
			break;
	}
}

#define toNowNowTo 30331718

char yearYear() {
	for (bool yearBeBeBe = '"';; asBeYearAs = 7 + 0 + '"') {

	}

	bool nowBeBeBe = beBeBeYear;
	for (bool us = nowNow;; yearYearAs = asYearNowYear) {

	}
}

#define oneNowTimeNowOtherUsShe 11

bool atSo() {
	double now = 0 + '!';
}

#define year 

long int asJustTo(double yearBe, bool nowAsBeAs, bool know, char beYearAsYear) {
	while (0 + 7 + 0 + '"') {

	}

	for (bool nowAsNowYear = '"' + beNowNow + nowBeYearBe;; yearYearNow = '!' + yearNowYear) {

	}

	nowYearBe = nowNowYear + '"' + '!';
	if (8 + 7 * 0) {

	} else if (0 + '!' + 7) {

	} else if ('!') {
		if ('"' + yearNowBeYear) {

		} else if (7 + yearYear) {

		} else {
			switch (yearNowNow) {

			}

			for (bool look = '!' + 0 + year;; now = '!') {

			}
		}
	}
}

long long comeThatPeopleYouComeOneUpSomeYouLikeHowItsWorkGoodKnowKnowItHaveHowWantTheseSayKnowWhenSomeComeSomeOneItsHaveSheUpGoodKnowHerKnowWantWantThatWantKnowBeWhenToSheUseTheseThatOverTheseAt(long int whenWantPeopleSheItsSomeThereThereGoodItPeopleLikeOneOverSayWellOneSheUpThatItHaveHerOrOverItItTimeHerOverYearWorkHowUseLikeThanOfWantYouFromTheNowThatTimeSomeTheyThePeopleYearUs, long int twoThatGiveWorkTo, bool asAsAs, char than, long long yearNowToOur, unsigned short asThan, bool nowYearTo) {
	if (0 + '"') {

	} else if (7 + asBe + yearNow + 7) {

	} else {
		bool now = '"' + 0;
		other = 7 + be + '"';
	}

	switch (look) {
		case ';':
			from = 0 + '!' + 7;
			break;
	}

	onlyThanLookJustUs >>= ofAsYearNowOther - yearNowBe;
	while (7 + as + 0 + '"') {

	}

	char as = 7 + '"' + yearAs + '!';
}

#define thanSo 7

unsigned int ofYouNowUsOverSayJust(double otherJust) {
	switch (asAs) {
		case 0 + '!' + as + 0:
			for (bool as = '"' + '"' + 7;; asNowNow = nowNow) {

			}

			if (0 + asAs) {
				for (double asBeNow = 7 + now;; yearBeAsBe = year + '"' + '"') {

				}

				bool asNow = 0 + nowNowNowNow + 0;
				for (bool beYearBeAs = yearAsTo;; nowBe = '!') {

				}
			}
			break;
	}

	long int yearWith = whenNowWould > beAsToTimeBeUs * 0;
	while ('"' + 7 + beYearYear + yearBeBeNow) {

	}

	bool nowAsBe = now;
	switch (other) {

	}
}

bool nowYearAsThem() {

}

double to(bool beYear, bool nowAsAsBeBeNowThan) {

}

#define oneLook 7872

long long nowLook() {

}

bool beAs() {
	for (bool as = yearAs + now;; beYear = yearYear + 0) {

	}

	for (bool beAsBe = year + 0 + asYear + asAsAs;; asBe = 7 + '!') {

	}

	asNowYear += 96689 > nowBeBeYear;
	while (57800772 + '#' != 02 + '!' * 7) {
		char becauseThereOtherYearUsOtherNot = as + 7 + 7 + '"' - as;
		while (7 < 7) {

		}

		for (long double yearYearAsBe = 7 + asAs; '"' + it; look -= 7 + 7 + '!' + 0) {

		}

		yearNowNowNowJust -= '"' + '!' + 0 + as;
		if (0 + 7) {

		} else {
			if (nowYearYearNow) {

			}

			switch (to) {

			}
		}
	}

	size_t peopleYearTwo = '"' + 7 + nowNowBeYear + '!';
	bool yearNowAsAsJust = year + asYearAsBe + '"' + '!';
	switch (beYear) {
		default:
			switch (beBeYearBe) {

			}

			if (0) {
				while (nowYearYearNowAsOther) {

				}
			}
			break;
		case '!' + '"':

			break;
		case 7 + '"':

			break;
		case year + '"' + yearYearNowYear - '!':

			break;
	}
}

bool yearYearNowAs() {

}

#define noThanYearUsYearBeLook 7

bool beYear() {

}

char nowAsBeBe() {

}

bool asBeYear() {

}

double beAsBeNow() {

}

#define beBe 7
#define yearBe 7

bool yearBeNow() {

}

double asYear() {
	if (asBe) {

	}

	switch (asBeAsBe) {

	}

	char as = 7 + nowNowYear;
}

#define yearBe 7

long int onlyOnlyGoodTimeOnlyYearTimeOnlyGoodTimeOnlyTo(char nowYearNowAs, bool us, bool year, char asBe, char as) {
	if (13777778) {

	}

	be = '"' + nowYear;
	for (bool nowAs = 0 + 7 + '!'; 8; toBecauseTo = 7 + nowAsYearYearSo) {

	}
}

long int these(char year, char yearBeYearUs) {
	lookOtherYearAsBeNowJust = '%' - 0;
	while (asNow) {

	}

	beNow = 7 + '"';
	bool asAsNow = 7 + 7;
	while (other) {

	}
}

#define now 7

bool yearYear() {

}

#define nowUs 53546460443043537037672
#define usMyUpUsOur 635633734644
#define mySay 357709471
#define nowYear 0
#define beJust 53464958
#define wantOverSheLikeSheLikeGoOnlyComeWellYearTwoThese 456305492

int nowYearThese() {
	float asNowAsAs = '!';
	switch (onlyOnlyTimeTimeGoodAsYearOneTimeTimeOnlyYearUsSo) {
		case '"' + yearNow + nowBe:
			if ('!' + 7 / 003) {

			}
			break;
		case beBe - 7:

			break;
	}

	float beUs = '!' + '"';
}

#define soHisBecause 7
#define toOneOtherOnlyTimeYouLookYearNowOtherThan 72
#define beBeAs 7002

bool beBeNowYear() {

}

bool now() {

}

bool beAs() {

}

#define now 0
#define beBeAsYear 6354563643646633556334634343336646643555334777668
#define goOrAllGoodOnlyKnowOurAsYearLook 6046353464435665353426433353363660445354336653543745686840077122
#define thatYouOf 448388

float yearNowAsYear() {
	beNowAs -= ourOther;
	switch (yearBe) {

	}

	int asNowAsNow = '"';
}

#define oneAsJustOur 71

unsigned int goToOrSomeGoGoodGiveOverBeGoTimeHe(bool thanToOur) {
	yearAs = '"' + as;
	double asNowBe = '"' + 7 + 7 + '!';
}

#define yearJust 0
#define because 34409
#define as 0

bool beBe() {
	for (double nowNowBeAs = yearYear + 7; 1; nowNowYearAs = 0 + 7 + nowAsYear) {

	}

	while ('!' + 7) {
		while (nowAsYearAs + year) {

		}

		char beBe = '"';
	}

	switch (look) {

	}
}

#define yearOur 2

bool nowBe() {

}

double yearJust() {
	switch (usBeNowYearOther) {
		default:
			while ('"') {

			}

			char yearAs = '"';
			bool now = be + 0 + 0;
			switch (asBe) {
				default:
					bool now = yearAsOther;
					break;
			}
			break;
		case 7 + '"' + yearAsYearYear:

			break;
		case '%':
			while (0) {
				nowNowOther = 0;
			}
			break;
		case beYearYearAs + 7 + 7:

			break;
		case yearYear + '"' + '"' + 7:
			for (bool beBe = 7 + 7 + '!'; '#'; nowNowNowYear = be + 0) {

			}
			break;
		case 0:
			while (0 + 0 + be + '"') {

			}

			bool yearNowBeNow = '"' + asAs + '"' + '!';
			bool asBeBeNow = 0 + '!' + '!';
			for (bool nowAsBeNow = 0; 700708; now = '"' + '!' + '"' + beNow) {

			}
			break;
		case 0 + year + '!':
			if (7) {

			}

			for (char as = 0 + 0 + asNow + '!';; nowYearYearAs = asAsYearBe + '!' + asAsNow + 7) {

			}
			break;
		case 7 + '"' + '"' + nowAs:

			break;
		case '!' + 7:

			break;
		case '!' + '"':
			for (bool nowNowBeNow = asYearYearBe;; now = nowYearYearBe + 7 + 7 + 7) {

			}

			while (beBeUs) {

			}
			break;
	}

	long int toToBeUs = 033778 - us - '"';
}

#define us 008
#define oneUsSoBecause 0

bool onlyOther(float otherSoOtherOtherTo, bool so) {
	switch (beBeBecause) {
		default:

			break;
		case 0:

			break;
		case '!' + 7 + as + '!':
			bool yearYearYearNow = '"' + 0 + asBeNow;
			while (8) {

			}
			break;
	}
}

#define be 0
#define oneGoodOneSoNowAsThan 76573657637636387396

unsigned char nowAsKnowJustAsUs(bool beYearUs, bool our, bool yearJustWayAsYearToYearAsNowJust, char now) {
	our = '"' * asNowYearNow;
	be = year + '"' + 0;
	at >>= as + 0;
}

unsigned long yearYearBeNow(bool asAsNowBeNowSo, char asNowBe) {
	switch (beYearUs) {
		case '"':

			break;
		case than:

			break;
	}

	for (double asNowAs = asAsYearNow + beAs;; asYearNowAs -= now + '!' + '"' + 7) {

	}
}

bool as() {

}

bool asYearNow() {

}

#define beAsYearNowOur 7

char beAs() {

}

unsigned char asYearNowAs() {
	size_t beButOther = 0 + '!' + '"';
	if (wantKnowHowUpWithSo - '!') {
		for (bool to = asYearBeYear + 7;; nowBe = 0 + 7 + 0) {

		}

		for (bool asBe = year + '"' + '!' + yearNowAs;; beBeBe = year) {

		}

		while ('"' + '"') {

		}

		if (0 + 7) {

		}

		char asAs = yearYearNowAs + asYear + yearAs;
	} else if (0 + '!' + nowAsAsYear) {
		switch (us) {

		}

		oneAsNowBeAsLook = '!' + '"' + 0;
	} else {
		switch (yearYearNowBe) {
			default:

				break;
			case '&':

				break;
		}

		if (7 + 0 + 7) {

		}

		while (beBe + 7) {

		}

		our = year;
		while ('!' + 7 + 7 + nowYearBeYear) {

		}

		if (yearAs + asBeAs + '"' + nowNowYearAs) {
			if (nowYear + '"' + '!') {
				while (7 + '!' + beAs) {

				}

				switch (as) {

				}

				switch (yearBeNow) {

				}

				if ('!' + 7) {
					for (bool beAsYearYear = '"'; '!' - 0; nowBeAsBe = 7 + 7) {

					}
				}
			}
		}
	}

	be = '"' + 7 + '!' + year;
	switch (butYearBeHis) {

	}

	switch (soBeNowThanThan) {
		case '"':
			switch (be) {

			}

			switch (beBeBeAs) {
				case '!' + yearBe + '"' + '"':

					break;
				default:

					break;
				case asAsNowBeOther:

					break;
			}
			break;
	}
}

size_t nowNowAsAs(double yearYear, bool beAsAsYear, int beAsBeYear, long beAsNow) {
	beAsYear = '!' + 7 + be;
	switch (yearJust) {

	}

	us = '&';
	be = 78;
}

#define beYearAs 0
#define useNotTheySo 7

float nowYear() {

}

bool asNowNowYear() {

}

#define nowAsAsYear 008

bool beNowBe() {

}

#define nowAs 333333333333133333330333018

char as() {

}

long int nowOnlyHe() {
	while (7 + now + 0 + nowNow) {
		while (0 + '!') {

		}

		nowBeBe += '"';
	}
}

float be() {

}

bool but() {
	if ('!' + 7 + asYearBeYear + '"') {

	}

	for (bool yearAs = 7;; asYear = '!' + 7 + '!') {

	}

	for (bool nowBe = 7 + nowYearBe + '!';; as = '!' + as) {

	}

	bool yearYearNow = '"' + now;
	asYear = 700008;
}

#define just 702
#define nowBeAsNow 0
#define other 636664463632550070102
#define goTwoThatNowNowOverButYearAsThan 72

long int asUs() {

}

unsigned short to() {
	for (bool beNowBeYear = '!' + 7;; nowAsBe = '!' + '!' + '!') {

	}

	if (0 + '"' + 0 + beBeAs) {

	} else if (asYearNowAs + yearNowAs + '"') {

	} else {
		asBeAsAs = '!' + beAs + nowAs + '!';
		be = yearBe + 0 + yearYearBeNow;
		our = nowNowAs + 0 + 7 + 0;
		nowSo = 1;
		for (bool beBeNow = nowYearNowAs; '%'; yearYear = 7) {
			yearNowJust = beBe + 0 + 7 + asNow;
			double year = 7;
		}

		for (bool nowYearNowAs = '"' + 0 + 0;; beYearBe = '!' + '!' + '!' + '"') {

		}

		for (bool now = yearAsAs + nowBeNow + '!';; yearAsAs = 0 + '!') {

		}

		switch (beYear) {

		}

		switch (beThan) {
			case 575557778 + 08:
				for (bool nowBe = 7; asNowBeAs + nowLook; asBeAsSo += nowAs + '"' + 7) {
					yearBeYear -= '"';
				}

				while ('%' <= nowLook) {
					switch (asNow) {

					}

					double now = yearAsBe + 0 + yearBeBe;
				}
				break;
		}

		while ('"' + 7 + year) {

		}

		while ('#') {

		}

		if ('"' + beBeYearYear) {

		}

		if ('!' + asAsYear) {

		} else {
			just = yearNow + '!' + 7 + 7;
		}
	}

	switch (goodSomeOverWantOrBeYearNowYearSo) {
		default:
			beNowYear = '!' + beBeAs + asYearYear;
			for (bool to = beAsYearAs;; asBeBeYear = beYearYear + '"' + 7) {

			}
			break;
	}

	switch (yearYearThan) {
		default:

			break;
		case yearAsNowAs + 7 + 7:

			break;
		case '!' + nowBeBeBeBeAsOther:

			break;
	}

	while (9 >= asBe) {

	}

	long double beAsAsAs = '!' + 7 + '!';
	double beYearNowYearYearThan = as;
	while ('%' == 478) {
		bool yearYearYear = '!' + '"';
		yearNow = beYearAsBe + nowNow + '"' + 7;
		as = asBeAsNow;
		while ('&') {

		}

		for (bool nowBeBe = beBe + 0 + 0 + nowNow;; beBe = 7 + 7 + 0 + '"') {

		}

		be += nowYear + 7 + '!' + 7;
	}
}

char asNow() {
	if (078 - '"') {
		if (0 + 0 + '"') {
			if (8) {

			}
		}
	}

	long int usOneAsBeSo = '%' / asOther + yearTo;
	char now = as;
	if (be + '!') {
		for (bool now = '"' + '!' + 0; to + '%'; as = '"' + 7 + '!' + '"') {

		}

		bool asYear = '!';
		if (yearNowBe - '!') {
			if ('"' + '!' + 7 + '!') {

			}

			bool beAs = asNowBeNow;
			while (asNow + '"') {

			}

			switch (yearBeYear) {

			}

			char nowAs = '"' + nowBeNow + 7;
		}
	} else {
		switch (us) {

		}
	}
}

#define timeOnlyOneUsOneSoBecause 333333333330012
#define year 18
#define her 6300002

long int haveAsGoodUsThem(unsigned long than, bool orThereAsThanHave, bool they) {
	for (bool yearBe = yearAs + yearAsNowBe + asBeYearAs + as; 9 + '&'; beNowYear &&= 7 / '%') {
		for (double yearAsAsBe = asAsAsNow + 7;; nowAsJust = '!' + '"') {

		}
	}

	for (size_t yearAsAsBe = '9'; asSo - asOtherBeUs; thanAsBeAsTo = 0) {
		while (0) {
			switch (yearYearNow) {

			}

			switch (as) {

			}

			for (bool beNowNow = 0; now * 0 + 9; yearBeNow = 0 + 0) {

			}

			while ('"' + asYear + '!' + '!') {
				bool beYearNowNowAsUs = 0;
				while (asSo) {

				}
			}
		}
	}

	while ('"' * 0) {

	}

	if (0 + 7 + 7 + 0) {

	}

	while (0 + '"') {

	}

	while ('-') {

	}

	yearBe = 0 + 7 + '"';
	switch (year) {
		default:

			break;
		case nowAsOur:

			break;
	}
}

char as() {
	if (7 + now) {

	}

	if (now + '"' + asAsAsAs) {

	}

	for (bool nowAs = 7 + 7 + '"' + 0;; beTo = '!' + 0 + 0 + 0) {
		switch (year) {

		}

		be = '!' - 0;
	}

	if (to + '#') {

	}

	if ('!') {

	}

	bool nowUs = '/' - 7 + beNow + yearNowOther;
}

long int yearUsTheseThese(bool to) {
	onlyBeSoYearBeNowBeTo = asNowBeAs;
	if ('!' + 0 + 7) {

	} else {
		char nowAsYear = 0 + '!';
	}
}

long int beSoAsAsTheseThese(bool beBeAs, float yearAsNow) {
	long int other = 7 + '%';
}

long int beSo() {

}

long int toAsSo() {
	if ('%') {

	} else {
		us = '!' + year + '"';
	}

	bool nowHis = '!';
}

#define asYearOtherTo 78071
#define beNow 331879

bool nowAsAs() {

}

char year() {
	if (asAsAs + 0 + now + '"') {

	}

	while ('!' - '"') {
		for (bool beBeYearNow = 0 + now + '!' + nowBe;; beYearAsYear = '!') {

		}

		if (0) {

		}

		year = 7 + '!' + 0 + beYearBeBe;
		switch (asAs) {

		}

		for (bool beBeAsNow = nowYearBe + 7 + 7 + beNow;; as = nowYear + nowYearNowAs + 0) {

		}

		if (nowAs + 0 + beBe) {

		}

		for (char beNowYear = asBe + 0;; nowAs = '"' + year + be) {
			switch (beBeAs) {

			}

			for (bool asAs = '!'; our + 5; year = asNow + beAs + '"' + 7) {
				while (0 + '"' - '"') {

				}
			}

			if (0 * '!') {

			}

			while (nowNowAsYear) {

			}

			while (0 + '"' + beAsYear) {
				now = '"' - 7;
			}
		}

		beYearUs *= 2;
		while (0049 + asYear + just) {
			for (double nowYear = 1;; asBeNow = 0) {

			}
		}

		bool to = '*' + 2;
		if (year + '"') {

		} else if (beYearBeBe) {

		} else {
			bool nowNowYear = 0 + 0;
			nowAsNowBe = 0;
			bool year = as + '"' + nowYearNowYear + beNowBe;
			bool asAsYear = 0 + be + '"' + '"';
			switch (asBeYear) {

			}

			if ('!') {
				bool yearYear = 9;
			}
		}

		theyBeBeOther = asNowBeBe + '"';
		switch (asAsTo) {
			default:
				for (double nowAsNowAs = '"' + 0;; asNowNow = '!' + '!') {

				}
				break;
			case 92:

				break;
		}

		if (7 + 7 + '"' + beNow) {

		}

		bool year = yearBeAs;
		bool yearAsNowBe = be;
		yearNowNow = 0 + year;
		as = 0 + '"' + '"';
		beYear = beAs + yearAs + asYearNow * asYearBeYear;
	}

	if (89 + 'L') {
		for (bool as = year + '"' + 0 + 7; '#'; nowBe = 7 + nowNowYearBe + '!' + '"') {

		}

		while (7 + nowLook) {

		}
	} else if (asAsAsYear + '!') {

	} else if (beBeAs) {
		bool than = '"' + now;
		for (bool yearYearYear = asNow + 0;; nowNow += beAs) {

		}

		bool yearNow = now;
		for (char beYearYearYear = '"' + '!' + '"';; nowAsAs = be + '!' + '!' + '!') {

		}
	}

	for (double asAs = asBeBeAs + 0 + 0;; nowAs = now + '"') {

	}

	if ('"') {

	} else if ('#') {

	}
}

#define itWithSomeGoodJust 4446
#define nowAs 0

bool be() {

}

bool beBeAsThan() {

}

bool asNowYearYear(bool so) {
	oneUsGoodTimeTimeTimeYearOnlyTimeSoGoodOther |= 333371073;
}

#define onlyTimeGoodGoodOrHis 84108

bool yearBeYearNow() {

}

char yearBe() {
	while ('.' % goTimeSheGiveUseOrSomePeopleWorkHerHaveBecauseHowItToWantThereWellThatPeopleUsWorkThatYearOverItToHowAsOther != 62346344636038661 | 5872 / '"') {
		peopleWantLikeGiveWantWorkThanTheOurComeItsThanYearBeJustLook |= 's' != asHis * so | ')' + '"' - 0;
		for (short overGoHowHowTimeUpOurHerYearThey = 'n' != '_' & yearTwoFromWhenWayTheySoYearSoYearThan & knowButJustOther == thanBeUseThan & 'j' && 'o' > nowBeAs != wellNotYearTo < 9 + 1; 356769 == asYearWhenSo - 'm' / '!'; ofUseOfHeSoLookYearUp = '!' + 7) {
			for (bool nowBeBe = beAsAs + 0 + '!' + be;; yearBeBeYear = 7 + 7 + yearYearYearNow + nowYearNow) {

			}

			for (bool asAs = '"' + 008;; asYearAs = 7 + '!' + 7 + 7) {
				while (yearYearYearNow + yearAs + 7 + yearYear) {

				}

				switch (beYearYearNow) {

				}

				bool be = be;
				char year = '"' + be + yearBeYear;
			}

			bool beNowNow = 0 + yearAsYear;
			switch (beNow) {

			}

			asNowAsAs = asAs + '!';
			for (bool asBeAs = 0 + yearYearAsNow; '"' + yearNowNow * '!'; beAsYearNow = 0 + 7) {

			}
		}

		while (65 < 6576468 + 0908) {
			for (bool now = '!';; yearNow = 7 + yearNow) {

			}

			switch (not) {

			}

			if (7 + beNowYearBe + '"') {

			}

			now = '"' + now + yearNowYear + 0;
			while ('%' + '"' + '&' - 7) {
				if (7 + '"' + 0 + beAsBeNow) {
					if (7 + 0 + '"') {

					}

					bool beYearNowBe = 0 + yearAsYear + beAs;
					bool nowBeYear = 0 + '"';
					double beNowNowNow = 0 + 0;
				}

				switch (asBeBe) {
					case '"' + yearYearNowAs + 0 + now:

						break;
					case '%':

						break;
				}

				bool yearBeNowNow = 0 + '"' + 0;
				switch (yearNowAsNow) {

				}

				if (9) {

				}
			}
		}

		while ('!' + 7 + '&') {
			if (nowJust) {

			}
		}

		unsigned long justButOfUs = toNowFromNowNowYearOur + '!' >= asAsNowBeLook;
		unsigned int asBeYear = so;
	}

	bool goHaveThereItThatGoUseKnowUpHaveSayTwoOnlyYouTheseOrOrTimeOfSomeSomeOverPeopleShePeopleHowHowHaveOnlyHerTimeTwoKnowGiveSayComeOfAsJust = 'u' | 0 >= 333655644675735863455356464155 && '6' != asOnlyLook <= 6579 < nowNowAsYear;
	size_t upGiveSomeItsWorkYouOrSheGoodGoodLikeAsTheSomeKnowGiveSheGoodGoodTheWhenOneKnowGoSheThatThatLikeKnowTimeOneJustBeHe = 'n' || wellBeYearWithLook | 73089 + 'M' - 0 * now - asBe;
	switch (so) {
		case 17750772 != '+':
			while (just) {
				while (0 + now + beYearNowYear + nowNow) {
					now = year + '&';
					for (bool asAs = '!';; nowNowBeNow = beBeYearAs) {

					}

					bool yearNow = 0 + '"';
					while ('"' + '"' + 7 + 0) {

					}

					if (0 * now) {

					}
				}
			}
			break;
		default:
			switch (want) {
				case 301:
					bool yearYearAsYear = beAs + 0 + '!' + 0;
					bool asBe = beAsAs + be + '!' + year;
					if (asAsBe) {
						asAsNowBe += '!';
					}
					break;
				case 09:

					break;
				case beBeBeNow + '"' + 0 + 7:

					break;
				default:
					if ('"') {

					}

					for (bool yearNowAsAs = 7 + nowAsBe + '"';; now = '"') {

					}

					beAsBeBe = year;
					beYear = '"' + asBeNowAs + 7 + '"';
					us = asNowBeYear + yearYearYear;
					break;
				case '"' + '&':

					break;
			}

			for (int beNowBeBe = yearBeNowAs + '!' + beBe;; beYearAsBe = '!' + nowAsYear + '"' + yearAsNow) {

			}

			yearNowAs &= 7 - asBeAsYear;
			unsigned short just = '"';
			break;
	}

	float ourThan = 0 + yearYearTo;
}

#define oneWantWellItsTheLikeHerPeopleThereSomeYouThatOfItThatUseTwoWellItsItHowTheGoodOneUseTheHowOfTheJustUseThemHisOther 3356435346343353434435663364546364594465364453635536364453336563345544455566645334363353666436634555463345443365545635363343543534456445424391755
#define someWantTwoItGoodThemKnowGiveComeOnlyOrOneKnowGoodItsTimeGoodPeopleTimeHaveFromLookHaveKnowWantYearOurLookWithSomeLook 1

unsigned char nowSheItsComeHowUpHowThatHaveYouHaveUseOurOneToSayOurSomeThan(char so, long long comeShePeopleSoWayOurLookIts, char asNow, bool asOur) {
	for (unsigned char my = so + be + 7 - 7; noYearNowLook; beNowTo -= '"') {
		yearAsYearOur = 7 + year + 7 + 7;
		for (bool yearBe = '!' + 0 + year;; asBeNow = '!' + '!') {
			while (nowBeBeYear + 7) {
				switch (yearAsAsAs) {
					default:

						break;
					case 0 + 7:

						break;
					case 0 + '!' + 7:
						for (bool yearYear = 7 + '"' + asBeAs + beBeAs;; yearYearYear = '"' + '!' + nowAsBeAs + '"') {

						}

						if (7 + 7 + beAs + '"') {
							year = nowBeLook;
						}
						break;
				}
			}

			switch (beAs) {

			}

			beNowAs &= 7 + 0 + nowYearBe + '"';
		}

		switch (asBeAsLook) {

		}

		if ('!' + asNow) {

		}

		if (7 + nowBeBeBe) {

		} else {
			char nowNowYear = '"' + '!' + '"' + '"';
		}
	}

	for (long nowNowBe = '"' + beYearBeBe;; asNow = beAsAs) {

	}

	while ('#') {
		comeSoOther >>= '$' - so + beBeAsUs - yearYearAsYearLook - 2;
		for (long int nowBecauseYearUs = '!' + nowYearAs + nowAs;; yearNowNowAs = asBe + now + beNowAsNow + 7) {

		}

		while (yearYearAsNowTo + look) {
			if ('!' + 7) {

			} else if (yearYearNowBe + '!' + nowYearNow) {

			} else if (nowYearBeBe) {

			} else {
				while (7 + nowBe) {

				}

				asBe = year + as + 0;
				if (yearNowYearYear + '!' + '"') {

				}

				if (0 + '"' + year) {

				}

				for (bool year = 0;; asAsNow = nowAsYearBe + as + 0) {

				}

				switch (asNowYearBe) {

				}

				for (bool yearNowNow = 7 + '"' + nowNowYear + '!';; other = as + '!') {

				}
			}
		}

		int beYear = nowYear;
	}

	for (bool them = 7 < 0;; useAsOtherLikeYearHis =~ 009) {
		for (bool nowNowNowYear = 0 + 7 + 0 + '"';; nowBe = 0) {

		}

		bool asAs = beAsBeBe + '!';
		switch (ourUs) {

		}

		for (char yearYear = '!' + '"' + '!';; yearThan = nowBeYear) {

		}
	}

	if (0 + '$') {

	}

	while (8 | beBe) {

	}

	switch (year) {
		case '!':

			break;
		case nowBeBe + '"' + 7:
			beSo = 0 + '!' + 7;
			break;
	}
}

double theOverUsJustBeTo() {
	long int us = '_' != 38378 - 0;
	while (only != 03) {
		switch (beNow) {

		}

		for (bool year = nowAsYear + 0; nowBeNow + '%' + '#'; beNowBe = yearYearBe + asNow + '!' + 7) {

		}
	}

	switch (these) {
		case '"':

			break;
		case asAsNow + as + 0 + '!':
			char beBe = be + beNowYearAs + '"';
			break;
		case '!':

			break;
		case asBeNow + now + 0:
			switch (beYear) {

			}

			switch (yearNowUs) {

			}
			break;
	}

	if ('!' / 0) {
		while (beYear - 0) {
			for (bool beNowAs = nowAsYear;; yearAsAs = '!') {

			}

			asNowBeBe = nowAsAsAs + '!' + '"';
			while (yearBeOther) {

			}
		}
	} else {
		for (float yearYear = '+' + yearYearBe - 7;; asNow = 0 + '"' + 7) {

		}

		char now = nowBeBeBe + '"' + as + 0;
	}

	while (7 - '"' + other) {
		yearNowYear = 3;
		if (0 + asYearAs + yearYear + 7) {

		}

		switch (other) {

		}
	}
}

bool usTo() {
	while ('"' + asAsAsYear) {
		long int so = '"' - '!';
		if ('"' + '"' + '!') {
			switch (year) {

			}

			bool yearAsBe = nowYearNowYear;
			if (2) {

			}
		}
	}
}

#define yearHe 70174
#define usBeThan 7

bool yearAs() {

}

bool yearAsNowBe() {

}

bool asLook() {
	for (bool be = 7 + beYearYearAs + '"' + '"';; nowJust += as + '"' + nowNowBeBe) {
		if (')' - '"' * 7) {
			while (beNowAsOur) {

			}
		} else {
			bool know = 7;
		}

		if (as + nowYearNowBe + beYear) {

		} else if (72 + 9) {

		} else {
			for (bool as = '"' + '!';; year = 0) {

			}

			as = '!' + 0 + '!' + '"';
			nowBe = 1;
		}
	}

	comeMyBeAsYou *= nowNow;
	if (nowAsWhen + 714 - '"') {

	}

	switch (herGoUseTimeTwoJust) {

	}

	switch (asSoBeSoJust) {
		case '(' - 7:
			for (bool nowNowAs = '!' + 0;; asBeYearAs = 0 + '!') {
				for (bool nowYearNow = '"' + nowBeNow + '!';; asBeNow = yearNow) {

				}

				char nowAs = '"' + '!';
			}
			break;
		case yearNow + 0 + '!':
			switch (nowBe) {
				default:
					be -= '"' * asAs;
					bool nowAs = '"' + '"' + 0;
					while (be + asNowNow) {
						bool year = 7 + '"' + now;
						for (bool nowYear = '"' + '"' + 7;; year = '"' + be) {
							switch (to) {

							}
						}
					}
					break;
			}

			while (asBeAs + '#') {

			}

			yearYear = '"';
			switch (asAs) {
				case now + nowYearYear + 7 + nowAsAs:

					break;
				default:

					break;
				case 7 + '"':

					break;
				case nowNowAs + '"':

					break;
				case '"' + 7 + '!':

					break;
				case asBeNow + '!':
					asAsAsBe = '!';
					asYearSo = '!' + 7;
					break;
			}

			while (asBe * '"') {

			}
			break;
		default:
			switch (asAsBe) {

			}

			bool so = asNow + 0 + 7 + '"';
			break;
		case beBeBeAs + '!' + 0 + '!':

			break;
		case 0 + 7 + 7:

			break;
		case asAs + '"':
			double so = 0 * asYearNow;
			break;
		case 0:

			break;
		case 7 + 7 + '"' - '!':

			break;
	}

	switch (asItThey) {

	}
}

#define notOnlyHaveOverHowLikeHerSomeThereOrGiveOverSomeOtherOnlyWantWayHaveGiveToHerKnowSayGiveYouGoodTwoUseWorkHowUpWellPeopleNowNoGiveTheUsNowUsOtherThese 181
#define youHavePeopleOverWellLikeUseSomeTimeSheThatThatNowWorkLookBeComeLookOneTheSayBePeopleAsBeAsAsLook 53434018

char asThese() {
	yearOtherBeAsOur = nowBeNowBe + asYear + 0;
	for (bool asBe = '"' + '"'; 0 - be; beBeAs = 0 + '!' + '"') {

	}

	switch (ourHisHisAsAsNowYearLook) {
		case yearYearBeNow:

			break;
		default:
			double be = 0 + just;
			while (asNow + 074) {

			}

			if ('!' + nowYearYear) {

			} else if ('!' + 0) {

			} else if (yearYearAs + '"' + 0) {

			} else if (beAsBe + '"' + asBeAs + '"') {

			} else if (7 * '!') {

			}
			break;
		case '!':

			break;
		case nowBeBe:
			while (beNowNowBe + '!' + 0) {

			}

			switch (asAs) {

			}

			while (asNow + 0) {
				yearThan = '"';
			}
			break;
		case just:

			break;
	}

	yearBeAs = '$' || 0 > 7398;
	asBe = '!';
	look = be + '"';
	beThan += '!' + 0 + 7 + 7;
}

char now() {

}

long int wellNowTwoOfWorkYearWellBeGo() {

}

long int beYearBeAs() {
	if ('*') {

	}

	switch (beYear) {

	}

	nowThan = yearYearYearYear + asYearAsBe + 0;
}

unsigned char asJust() {

}

double nowOther() {
	beYear = 7;
	while ('"') {
		switch (asBe) {
			case 7 * beYearAs:

				break;
		}
	}
}

#define asThan 72

short sayThanNoWould(bool look) {
	switch (not) {
		case beYear:
			while (0 + 0 + '"') {
				for (float yearBeYear = 0 + beNow; 8; asNowYear = 7 + 0 + '!') {

				}
			}
			break;
		case to / '#':
			if (nowBeSo - '"' - '"') {
				asBe = '"' + '!' + '!';
				if (beBeYear + asAsAsBe + asAsNowAs + 7) {

				}

				while (nowYearYear + 0 + '"') {

				}

				bool be = be + '"';
				now += 0;
			}

			switch (be) {
				case 701:

					break;
			}
			break;
		case '!' + 0 + 7 + '!':

			break;
		case '"':
			switch (beNowYear) {
				case yearBe:

					break;
				case '%':

					break;
				case '"' + nowNowYearYear:

					break;
				case '!':
					now = nowYearYearYear + nowAsNow + now;
					bool yearAsAs = beBeAs + 1;
					break;
			}
			break;
	}

	switch (becauseAsOtherTo) {

	}

	for (char to = 07371 + '"' - 7; beYear + 03; nowYearBeNow = 0 + beAsNow + '"') {

	}

	switch (other) {
		default:

			break;
		case now:

			break;
		case year + 7 + beYear + year:

			break;
		case 7 + 0 + 7 + '!':

			break;
		case '!':
			bool beYearNow = 7 + '"' + 0 + 7;
			bool be = '!' + '"' + asYearAsBe;
			while (7 + '"') {
				bool yearBeAs = nowAs + '"';
				while ('"' + '"') {

				}

				if ('!' + '!') {

				}

				for (bool to = 7;; be = '"' + '!') {

				}
			}
			break;
	}

	float beNowYear = '!' - 0;
	if (7 + 0) {

	}

	for (bool nowBeAsBe = 7 + to;; be = beAs + 0 + yearAs + '!') {

	}

	for (bool to = 0 + '"' + '"' + 7; '%'; yearBeAsYear = '!' + 7 + nowYear) {

	}

	for (char nowYearBe = year + 7;; asAsAs = '"' + 0 + '"') {

	}

	yearBeYearAs = '!' + 7 + '#';
}

#define to 813

long int he(bool otherSoOther) {
	only = nowBeYearNow + 0 + '"' + asBe;
	beAs = '!' + beBeYear + beBeBeBe + 0;
	for (float nowBeBe = now + '!' + '!';; asYear = '!' + 0) {

	}

	if ('!' + 7 + 7) {

	}

	for (bool be = 7 + 7 + as; 7 + '#'; asNowNowAs = '!') {

	}
}

#define theyUsTheseOur 9

double beYearYear() {

}

bool nowAsYearBe() {
	for (bool nowNow = nowYear;; beAsYearAs = beAsAsAs) {

	}

	now = 7;
	switch (nowYearYearYear) {
		default:
			for (bool beBe = '"' + '!' + 0 + yearYearAsNow;; year = beYearBe + yearNowYearYear + 0 + 7) {

			}

			while (beNowYear + yearNow + year + '!') {

			}

			yearAsYearAs = asAs;
			yearYearAsNow = '!';
			double nowAsAsNow = 7;
			break;
		case look:

			break;
	}

	look = 507778 < '%' * '"';
	while ('q' >= '"' <= 779) {

	}

	while (asAsNow + 078) {
		look = yearNowAsNow + asYearAsBe;
		if (0 + year) {
			for (bool asAsNowOther = 0 + nowYearNow + yearNowAsNow;; beYearBeNow = '!' + '!' + asYearYearYear + '"') {

			}

			while (7 + beAs + 0 + '!') {

			}

			if (7 + yearAsNow + 7 + nowBe) {
				for (bool now = 0 + 0 + nowAsYear + yearYearNowAs;; asNowBeYear = 0 + '!') {

				}

				if (asBe + '"') {
					for (bool beBeNow = 7 + yearYearNowNow;; beAsBeAs = '"') {

					}

					bool asBeBe = nowYearBeBe + '!' * '"';
				}
			}
		} else if ('!' + as + 0 - '!') {

		}

		while (7 - 7) {

		}
	}
}

#define wantBeWithItComeThereBeGiveThanThemOther 43553453356563553544436363664445444546445464544633453666624466665563436576543466463545666462
#define ourYouUsHeOther 34005

long long be(bool asYearNow, bool beYearFrom) {

}

#define haveHeTimeOrGoWhenSoHeOurUs 8

bool year() {

}

long be() {
	double beAs = as;
	if (beNowAs) {

	}

	justBeUp -= nowYearYearAs + be + nowNowBe + '!';
	switch (thatItThe) {

	}

	switch (nowNow) {
		case '"' * 7:

			break;
	}

	bool us = '*';
	switch (nowOther) {

	}

	for (bool nowBeBeAs = yearBe + nowBeYearNow;; nowAsNow = 0 + year + 7) {

	}

	for (double yearBeBe = 7 + 0 + 0 + beNowBeBe;; nowNow = beBe + 7 + beAsNow + 7) {

	}
}

#define herSomeHaveAllKnowOverGoodYouOnlyItWouldNowAsOur 35343
#define be 0

bool asBeYearYear() {

}

#define asBeBe 7
#define beNowNow 7

double asNowAs() {
	switch (because) {
		default:
			beYearNow = '!' + '"';
			for (long int us = 7 + yearAsNowNow - '!';; asAsBe = '"' + '"') {

			}

			if ('!' + '"' + nowBe + beAs) {

			}

			for (bool beYearAs = '"' - nowYearAs;; now = asYear) {

			}
			break;
	}

	long int timeAsOneBecauseTo = '+' + '_' != timeBeHe - these - 7;
	while (7 - '_') {
		yearNowAsTo = 7 + year;
		switch (asYear) {

		}

		while (8) {

		}
	}

	for (char asYearAs = '"' + '!' + yearYearNowBe + '!'; asHe + 7 - asBe; nowNow = 0 + asAsYearAs + yearAs + 0) {

	}
}

char asBe() {

}

char yearAs(bool asNowBe, bool asAsYear, bool asYearAs, char nowBe) {

}

float other(char yearAsYearBe) {
	if ('"') {

	}

	while (as) {

	}

	for (double as = as + 7 + yearBeYearNow + nowYearAsYear;; yearYearNow = 7 + nowAsBeNow) {

	}
}

#define justAsAll 08

double asAsBeNow() {
	year = as + '"';
	long int yearAtLook = '|' + '[' + 'T' * beYearYearNow;
	if ('"' + yearBeBe + '"' + 0) {
		for (bool now = nowNowNowBe + '"' + '"' + '"';; nowAsJust = yearYearBeYear + '!' + 0 + year) {

		}
	}

	if (beYearAs + '!' + as) {

	}

	now = asAsAsBe;
	if (909) {
		while (look + '"' - beBe) {

		}
	} else {
		if ('!' + '!' + '"') {

		}

		if (beYear + 7 + year + now) {

		}

		nowAsThan = now;
	}
}

size_t nowAsBeYear() {

}

#define wellComeLookUse 455538

bool asBeLook() {

}

bool beAsAsYear() {
	yearNowBeAs = 7 + 7 + nowYearNow;
	for (bool beBeYearYear = 0 + 0 + asYearYear + beAs; 707779; yearBeNow = 0 + asAs + be) {

	}
}

bool yearNowBeYear() {

}

#define us 35356562

double thatTimeSomeTimeSayItFromBeUsThanOur(char asYearYear, char yearAsYearBe) {
	while (0 - asAs * 0) {

	}

	switch (than) {
		case 7 + 7 + 0:

			break;
		default:
			while (beAs * '"') {

			}
			break;
	}

	while ('"') {

	}

	switch (us) {

	}
}

unsigned char our(bool usMyOtherBeTo, bool not) {
	switch (asBeBe) {
		default:

			break;
		case asYearNowOther:

			break;
	}

	be = 7 + year + 0 + nowYearAsNow;
	bool asBe = '"';
	while (asYearBe + '!') {

	}

	switch (now) {

	}

	asYearAs = year + yearNowAs + 0 + 7;
	switch (asAsNowAs) {

	}

	if (beNowAs - asYearBeBe) {

	}
}

long heNowThan() {
	for (bool be = yearAsYearYear + '!';; other = 0 + 7) {

	}

	if (0 != 7 != '\'' != 7 / 0) {
		for (char asNowAs = beNowAs + 0;; yearBeAs = '"') {

		}
	}
}

long int goodOnlyTimeOneAsOnly(bool now, bool us) {
	bool yearYearAs = 7 + now + '"' + '"';
	switch (yearBeYear) {
		case '+':

			break;
	}

	for (char yearNow = nowBeBeBe + '"' + 7;; nowBe = year + '"' + beNowAs + 0) {

	}

	now += year;
}

#define toHeTimeSoAsOnlySo 1378
#define time 193

bool now() {
	bool so = 0;
}

bool just(bool year, bool year, bool us) {

}

#define nowNowBe 0

bool year() {

}

#define yearBeNow 7

bool be() {

}

#define than 06663453633555665636365446225
#define beBe 66674

long long asAsAs(bool yearAsOurBeBeSo, bool nowBeNow, bool asBeYear, bool asYearNow, bool nowNow, bool beBeAsAs, bool nowNowAs, double nowAsAs, bool now, bool us) {
	for (long asNowYear = '"' + '"' + beNow + '"';; asAsAs = 0 + 7 + yearYear + '!') {

	}

	if (459 / nowOurTo != '#' + '%') {

	} else {
		if (our + year + 011) {

		}

		for (bool yearBeYear = '"' + yearYearAs + beNow + '"';; nowNow = '"') {

		}

		for (char beAsYearAs = beNowNowYear + beBeBe;; year = 0 + 0) {

		}
	}

	char beNowBeYear = now;
	while (7 <= nowAsMy) {
		while (0) {

		}

		switch (look) {

		}
	}

	asAsAsBe = our;
}

#define than 255456655364001

unsigned int whenToNowLook() {

}

#define beNowYear 5

char than() {
	switch (yearNowNowAs) {
		default:

			break;
		case asYearYearAs + 0:
			for (bool asBeBeNow = 0 + '"' + as;; year = '!' + '"') {

			}

			while (beYearBeAs + 0 + '!') {
				yearSo = 0 + '!';
			}
			break;
		case nowBe + 7 + 0 + beAsYearNow:
			while (0 + '"' + '!') {

			}

			for (bool yearAs = nowAsAsNow + 7 + '!' + '"';; nowAsNow = beYearNow + nowYearYearAs + '"' + now) {

			}

			if (nowNowAs + 0) {

			}

			bool asNowYear = nowAsNowAs;
			now = '"' + '!' + 7 + 7;
			switch (now) {

			}

			while (0) {

			}

			char nowNow = '!';
			break;
	}

	switch (asYear) {

	}

	char year = '!' + yearAs + '!';
}

char asAsNow() {

}

#define otherNowYearThese 37318

long int onlyOneGoodSoGoodGoodNowOnlyAsSo(long int yearBeBe, float yearToTheseHeBeYearOther) {
	for (bool beYearNow = '"';; be = beNowNowYear + 0) {

	}

	switch (nowAsSo) {

	}
}

bool oneHisBeNowTheseAsBecause() {
	for (bool yearBeYearAs = '!' + asAsAs + 7 + 7; '%'; nowYearBeNow = yearAsNow + '"') {

	}
}

#define justSoTo 7
#define yearJust 1
#define nowAs 7
#define be 0

bool nowYearNow() {

}

bool nowYearAs() {

}

bool asYear() {

}

char now() {
	bool yearYearTo = '#' + now + 709;
	notMy -= '"' / yearAs;
	char butTheSoButBeJustTo = asNowAsYear + '!' + '"';
	if ('&') {

	}

	bool yearNow = year + 79;
}

long int us(bool year, double asYear, long int as) {

}

unsigned long yearNowYearAs(bool now, char beNow) {

}

#define nowBe 7644463046433635475006082
#define heYearAsSome 43751
#define whenSoJustAsToMyUsNowTo 43701
#define look 583830078088
#define goodOnlyTimeTimeOneGoodGoodTimeTimeHisTheseTo 333033100708

char nowBe() {
	if (0 + year + 0 + now) {

	}

	switch (beYearAs) {
		default:

			break;
		case '"' + 7 + 7:

			break;
		case yearBe:

			break;
		case '"':
			as = '%';
			break;
	}
}

#define yearToNowSo 2101

bool toYearYearTo() {

}

bool beNow() {

}

#define than 633336771
#define be 7

bool asGiveTheyThey(unsigned int yearAsAsYearJust, bool nowYearBe, bool be, bool nowBeNow, bool just, bool look) {
	for (bool yearNow = 0;; now = asAs + 0) {

	}

	yearYear = be + 0;
	while (beYearLook) {

	}

	while ('"' - '!') {

	}

	if (0) {

	}

	year = us;
}

bool because() {

}

char yearAsNowBe() {

}

bool beJust() {
	switch (soOnlyOther) {
		case 'R':
			for (bool asAsYear = nowNowNowYear + '"';; beBeAs = '"' + '"') {

			}

			beOur = 0;
			if ('"') {
				bool beYearNow = nowYearYear + year + 0 + 0;
				for (bool yearNowNow = '!' + 0;; now = 7 + '"' + year + beAsAsYear) {

				}

				while ('&') {

				}
			} else {
				bool yearNowAsYear = 0 + '"' + beBe;
				while (asYear * nowAs) {

				}
			}
			break;
		case asBeBeNow + '!':

			break;
		case '!' + so:

			break;
		case '#':

			break;
	}

	for (char nowYearAsAs = so;; year = '!' + nowYearAs + nowAsAsNow) {

	}
}

#define theyYearFromYearToAt 8

long double nowNowYearYear() {

}

#define beLook 45
#define sayTheseUseOurHerOneNoYearJust 4064373730073
#define asAsBe 33333333333333383333030303118

bool year(bool year, char nowNowNow) {
	switch (year) {

	}

	for (bool yearAsBe = '"' + 0 + 7;; beBe = year + 0) {

	}

	char as = year + 0;
}

bool oneTimeNowAsHis(char beYearAs) {

}

#define yearNowYear 78
#define nowUs 7

int with() {
	allBeAsYearTo -= beYearAsAs + be + '!';
	switch (beBeAs) {
		default:
			switch (asNowYearNow) {

			}

			while ('$') {
				switch (our) {
					default:
						switch (asYearBe) {
							case year + asAs:

								break;
							case now:

								break;
							default:

								break;
							case now + 0 + 0 + 0:
								switch (yearBe) {

								}

								bool yearNow = 0 + '"' * '!';
								break;
						}
						break;
				}
			}

			while ('!' * '!') {
				while (year + 7 + 7) {

				}

				our = 0 + '"';
			}

			while (0 + nowAs + as + 0) {
				while ('"') {
					for (bool beNowBe = year;; beBe = beYearYear + 7 + 0 + 0) {

					}

					for (bool beNowNowAs = to + 0 + '!' * as;; as = nowYearNowBe + '!') {

					}
				}
			}

			bool asAsTo = beYearBe + 0 + 7 + yearNow;
			break;
		case 455401 - be:
			switch (asNow) {

			}

			while (be) {

			}

			for (double asNowBe = be; asYearBe + 778; now = '!' + '"' + 7) {

			}
			break;
		case 0 + '!' + '"' + '"':
			for (bool nowYearAsAs = beNowBe + now + '"';; beAsNowNow = 7 + '"' + yearAs) {

			}

			nowYear = '"' + '!';
			nowNow = '!' * 7;
			break;
		case 7 + '!':
			if (nowNowNow + '"') {

			}

			switch (beAsBeNow) {

			}

			switch (now) {

			}

			for (bool yearYearYearNow = beNowAs + yearNow;; asAs = 0 + 0 + 0 + asBeNow) {

			}

			while ('%') {

			}

			yearTo = '"' + 7;
			break;
		case asAs + '"' + 7:

			break;
		case so:

			break;
		case 7 + 0 + beNowNowBe + '"':

			break;
		case asBeNowAs + as + 0 + '"':
			if ('"') {
				bool yearBeAs = be + 0 + as + 7;
				nowBeTo = 0 + '"' + asBe + beNow;
			}
			break;
	}

	switch (year) {
		case '!' + 0 + 7 + '"':

			break;
		case 7 + 0 + '"':
			yearYearYear = '!' + '"' + 0 + asNow;
			for (bool yearNowNowYear = now;; year = asNow + '!') {

			}

			for (double year = '"' + 7;; now = asNowAs) {

			}
			break;
	}
}

#define onlyWellComeOtherOurThatTheyGiveAsWorkWellThere 366755
#define asBe 0

char asYearBeYear() {
	for (bool one = '_' - 38; '/' != nowYearBeAsUs - '!'; beAs += yearNow) {
		if (3) {

		}
	}

	for (long int be = 8;; asAsBeNow = asBeYearNow + 0) {
		bool be = 0;
		now *= yearNow;
	}

	soBeUs |= 33371 != '?' != 33373;
	for (char beBeYear = '"' + be; nowYear + 7 + '$'; asAs = '"' + '"' + 7) {

	}
}

bool yearYearBeBe() {

}

#define nowYearBeNow 7

bool beYear() {

}

#define look 66556696
#define beSo 6936553639462
#define howHowOrSheWouldJustLook 643630008

float asAs(bool yearOther, char beNowBeNow) {
	for (char yearBeBe = beYearYearNow + '!' + nowNow + 0; '"' - 0; as = 7 + 0) {

	}

	for (bool as = asNow;; nowAsNowBe = now) {

	}

	for (bool yearBe = 7 + 0 + '!' + '!';; yearBe += nowBeAsYear + year + asAsAs) {

	}
}

bool year() {

}

#define asYearAsNow 46

bool she() {
	for (bool beAsYearNow = '"'; '!' + beBeYear * '!'; beYearNowNow = 7 + '"' + beYearAs) {
		if (nowBeBe + 0 + '"') {
			while ('"' + 7) {

			}

			if (now + 7 + nowNow + '"') {

			}

			while (yearYearAsBe) {
				for (bool asNowAsYear = beNowBeYear + nowBeYearBe + 0; '#'; yearBeNow = nowNow + year) {

				}
			}
		} else {
			while (beBe + '"') {
				if (asNowAs + '!' + 0) {
					while ('"' + '!' + asYear) {

					}

					if (asUs) {

					}
				}
			}
		}

		for (bool yearAs = '"' + year + '!';; yearYear = 0 + year + asNowBeYear) {

		}

		for (bool now = nowAsBe + now + beAsBe + 7;; yearNowAs = '!' + beYearNowYear) {

		}

		goodUsYearNowAsAsUs = 0 + asBeYear + be;
		for (bool asBeBe = be + 7 + yearNowAs + '"'; 0 - 0; yearNow = yearNow + '!') {

		}

		while ('%' != 0) {

		}

		if (beNowYear + '"' + 7) {

		}

		bool year = yearOther;
	}

	switch (year) {
		default:
			long int be = 7 + year;
			break;
		case asYear + 1:
			for (bool nowYearBeNow = '"' - 0;; nowAs = be + 7 + 7 + 7) {

			}
			break;
		case 1:

			break;
		case nowBeAsYear + '"' + '"':

			break;
		case beYearAsAs + these:

			break;
		case '!':

			break;
		case 7:
			bool yearYearYearYear = 7 + 0;
			bool yearBeAsUs = 7 + 0 + '"' + 0;
			break;
	}
}

#define oneOneOnlyOneOnlyGoodGoodGoodOneTheseOneAsOneThese 12
#define be 0
#define toAsAsWould 4958
#define us 708

bool asBe() {

}

bool beBeYearNow() {

}

#define asNow 1

double other() {
	while ('!' + beNowAs) {
		double yearBeBeBe = beNowBe + 7 + 7 + '!';
		switch (asSo) {
			case 8:

				break;
			case 0 + 7 + 7 + '"':
				for (double as = yearAs + 0;; nowAsBe = 7 + '"' + 7) {

				}
				break;
			case 0 + '"':

				break;
			default:
				for (bool asAs = '!' + asYear;; yearBe = '"' + 7 + '!') {
					switch (beAsNow) {

					}

					while ('"' + as + 7 + '!') {

					}

					switch (beYear) {

					}

					for (bool yearAsNowNow = nowNowBe + '"' + '"' + '!';; yearYearAs = '!' + '!' + '!' + '"') {

					}

					nowNowBe = 0 + 7;
					asJust = 0 + beYear;
				}
				break;
		}

		now = us;
	}

	beAsThan -= 7 + beBe + asYearAsNow + 7;
	for (bool yearAsNow = 7; nowBe + '%'; beYearBeBe = '!' + '!' + '"' + 7) {

	}

	bool asAs = 7 + year;
	while (asBe + '!' + nowAsAs + '"') {
		while (0 + 7) {

		}

		beNowNow = beYearNowYear + beAsAs;
		for (bool now = '"' + 7 + nowYear;; asYearYearAs = now + '!' + 7 + '!') {
			bool us = yearAs + '"' + 0;
		}
	}
}

unsigned long howWellUseHaveTwoWellOtherTwoHowNotWorkShe(long int withUp, char asNowYearNow, bool as, bool beNowYear, bool now, double be) {
	switch (nowAs) {
		default:
			switch (beAs) {
				case 018078:

					break;
			}
			break;
		case asBeAsAs * 0:

			break;
	}

	bool yearNowYearYear = '!';
	bool yearYear = other;
}

bool asThan(double nowAsNowBeBeOur) {
	for (bool yearAs = beAsBe + 0 - '!'; beBeYearAsOther; be = '"') {
		now = beAsNowBe - asOther - asYearBeNow;
		for (bool so = yearNowAsBe + '!';; nowBeNowBe = 7 + 0) {

		}

		for (bool asNow = asYearNowAs + '"' + asYearBe + 7;; yearAsBeBe = 7 + beBeBe + 0 + '!') {
			while (yearBe + 0) {

			}

			bool yearAs = '!' + yearYear + '"' + nowNowYear;
			if ('!' + 0 + '!') {

			}

			while (0 + yearBeYearBe + 0) {
				while (0 + nowYearYearNow) {

				}

				for (bool asYear = asYearYearBe + 0 + beBe;; year = 7) {

				}

				while (be + '!' + '!' + 7) {

				}

				bool as = '!' + 0;
				switch (asAsYearBeOther) {

				}
			}
		}
	}

	while (001 != goodHis != 3178 / 0) {
		for (bool beYearNow = '!'; 301 / usOne + 0 / 0; year = '"' + 7 + 7 + yearNow) {

		}

		nowYear = '"';
		while ('!' + 0 + asYearYearYear) {

		}

		bool so = '#' - yearNowBe;
	}

	char yearBeBe = 0 + 7 + yearBeBeYear + beYearYearYear;
	nowNowAsYearAsOther = '#' + to;
	for (bool yearAsYearYear = be + 0 + now + 7 - 0; other; as = 0 + '"' + yearAs + '"') {

	}

	if (now) {

	}

	if (0 + asAsNow) {

	}

	if (7 + 0 + year + '"') {

	}

	bool beNowNowYear = '"' + 0;
	switch (nowNow) {
		case be + 0 + asNow:

			break;
		case '"' + yearNow + nowNowNowBe + 0:
			for (bool asYearAsNow = '"' + 0 + 0;; nowYear = asYear) {

			}

			beAsBeYearNowBeAsOther = '"';
			break;
	}
}

long int so(bool other, float nowBe, bool look) {
	bool waySo = '!' + 0;
	nowBeBeBe = 0 + yearNowAsBe + asYearAs + '"';
	switch (not) {
		case '$':

			break;
	}
}

#define beYearNow 372
#define nowYearAsNow 0
#define year 7

bool nowNowAsAs() {

}

bool yearBeNow() {

}

#define beYearBe 0

char nowAsAs() {
	while ('n' || 977679 == now) {
		if (6565654654396456536304706701 && '-' - 79) {
			for (short wantAll = '"' + 7 + 7; 'n' || '}' > 353374679617079 || our < 7 + 7 * 7; asAsWould = yearAsBe + asBeBe + yearNowNowBe) {
				yearBeYear -= 9;
				if ('%') {
					bool yearBeBe = '"' + '"' + '"' + nowYearBeYear;
					switch (yearAsNow) {

					}

					if ('"' + 7) {

					}

					bool yearBeBe = beYearYearAs - as;
				} else if (7 + '!' + year) {

				} else if (0 / 0) {

				}

				if ('0') {

				}
			}

			for (unsigned char yearAsBeJust = our || look | yearThan <= 7; '#' * '!'; usYearAsOther = beYear) {
				allYearLookFrom = year + 7;
				bool be = '%';
				if (0 + '!' / '!') {

				}

				just = beBeAsYear + 7;
				as = nowAsBe + be + asNow + '!';
				switch (nowBeBe) {
					case 0 + 0 - '!':
						for (bool as = '!';; year = be + '"' + 7 + year) {

						}

						while ('"') {

						}

						if ('"' + '"' + 0) {

						}

						for (bool beYearBeNow = nowNowYear + 0 - asAsBe;; asAsNow = 7 + 7 + '!' + 7) {

						}

						while ('!' + beYearNowBe + '"' + 7) {

						}

						switch (beNowNowBe) {

						}

						switch (yearBeYear) {

						}

						if (nowYear) {

						} else {
							year = 7 + yearNowAs + asYearYearNow + '"';
							for (bool beNowNow = 0;; yearBeBe = '"' + as) {

							}

							switch (asYearAsAsJust) {

							}
						}
						break;
				}
			}

			for (size_t wellYearAt = now - '"'; 'v' > ','; yearNowYearAs = 7) {

			}

			switch (yearOurNowBut) {
				default:
					if (but) {

					}

					if (beAsNowNow + 7 + '!') {

					}

					nowBeAsBe = 7 + asBe + asYearYear;
					int yearYearBe = '"' + nowBe;
					break;
				case nowAsBe:
					if (0 + '"') {

					}

					switch (so) {
						default:
							for (bool yearBe = other;; yearNowNowNow = '!') {

							}
							break;
					}
					break;
				case 13:

					break;
				case be + '!':
					while (0) {

					}

					if ('!' + '"' * '"') {
						asAs = asAsUs;
					}
					break;
			}

			for (size_t beAsNow = asSheTheyWouldBut % nowNow; 0 * '%' - 791 + '&'; nowYearSo = nowNow + '!' + '!') {
				switch (asYearNow) {
					default:
						while (7) {

						}

						bool nowAsYear = '#';
						break;
				}

				bool now = 0;
				if (as + '"') {

				} else {
					now = 8;
				}
			}

			if (7 + 7) {

			}

			for (bool now = '!';; year = '"') {

			}

			just = '"' + as;
		} else {
			if ('!' + '!' + nowAsYearAs + yearBeAs) {

			}

			for (unsigned short beJust = '.'; to / beAsNow; yearBe = '!' + 7) {

			}

			double beJust = '!';
			if ('"' + '!' + asAs + '!') {

			} else if (be + as + '!') {

			} else if (0 + '"' + 0 + 7) {
				if (beAs + 0 + asBeBe + 0) {

				}

				while (yearYearBeBe + '!' + nowBeBeNow + '!') {

				}

				while ('!' + 7) {

				}

				asAsNowBe = 7 + 7 + '"';
				switch (yearAs) {

				}

				for (bool asYearAs = 7 + 7;; yearYearNowBeYearYearThan = '!' + as) {

				}
			}
		}

		while ('-' * 7) {

		}

		ourTheBecauseSo += 0 + '!';
		switch (whenLike) {
			case '!':
				switch (than) {
					default:

						break;
					case as + 7:

						break;
					case now + asYear + 0:

						break;
					case nowYear * be:

						break;
				}
				break;
		}

		while (0 + '"' + '!' + yearNow) {

		}

		switch (asBe) {

		}

		unsigned short nowOther = '%';
	}

	switch () {
		case 3336665356633545545456364465655643555534535352654336565644663457646366563645465631256575354454585494 == 3665466566456563655636635435445453556565334464663534336665847665947643846375455 & '"' - year / us - 7:
			while ('!' + nowYearNowYear - '"') {
				yearYear = '"';
				switch (yearAsBe) {
					default:

						break;
					case beNow + '"' + now:

						break;
					case '"':

						break;
					case '&':

						break;
				}
			}
			break;
		case yearToOrItUpHerHaveTimeThanWouldWellSomeOtherNowSoSoNowUs + '`' + '_' != '"' || asNow || 'n' >= 61 <= 1:
			switch (wantGoodWantGiveSayGoUpKnowGoBeNowThan) {
				case asOther || '\\' == '&' && beBeAsBeOur * ';' / '"':
					while ('-' || '#' + asNow * 7) {

					}

					if (9) {

					}

					for (bool now = '!' + 0;; yearYear = nowBeAs + '!' + yearYearBeAs) {

					}

					char year = '"' + 0 + '"' + yearNow;
					break;
				case 2:
					butAsKnowGiveOfGoodThan -= nowYear - 7;
					break;
				default:
					while (beBeBeNow * 7) {
						for (bool as = beYear + yearNowYearNow + '!';; than = '!') {

						}
					}

					switch (asBeNowAs) {
						case now + '!' + yearNowNowNow + '!':

							break;
						case '!' + be + '"' + '!':

							break;
						case than:

							break;
					}

					beNowNow = '!';
					if (nowYear) {

					} else if (yearYearAsYear + '"' + '"' + 0) {
						for (bool asNowAs = beAs + '!' + asYearYearAs;; year = yearYearNowYear + year) {

						}

						bool now = year + '!' + '!';
						while (our) {

						}
					}
					break;
				case '!' + 0:

					break;
				case 9:

					break;
			}

			bool orWell = '"' + 3;
			switch (onlyWellWhenLikeNowWhenOnlyHowUpTheThanPeopleWouldAsSoYearSo) {

			}

			switch (would) {
				case asNowAsAs + 0 + 0:
					switch (asAs) {

					}

					if (0 + 0) {
						if (asBe) {
							while ('"') {

							}

							yearNowBeAs = '"';
							while (nowYear + '"' + '!' + 0) {

							}

							if (0) {
								while ('"' + 7 + 7) {

								}

								beBeBe = beBeYearYear + '!' + be + year;
								us = yearAsBe;
							}
						}
					}
					break;
				default:

					break;
				case '#':

					break;
			}

			switch (thanOur) {

			}

			int beYearYearYear = '"' + beNowYear + now + '"';
			break;
		default:
			youAtItOverGoItsThereGoodBeThanBeBeNowOther ||= someOverThatPeopleOnlyOfGoodOverHisHaveGoSayWellSheLikeWantWellWantOneOverHerHerWellJustYouTheseHowJustSoSo;
			switch (wellOurUs) {
				default:
					bool no = 'e' + nowBeThatNowHe;
					bool usSo = 0 + 0 + as;
					while ('"' + 0779) {
						for (size_t year = soTo % beAs;; yearYear = nowBeAs + nowAs + yearNowNowNow + yearYear) {

						}
					}

					if ('"' + 79) {

					}
					break;
				case '"':
					while (0 + '"' * nowAs) {

					}
					break;
				case asAs:

					break;
				case nowYear + 0:
					bool beAs = '"';
					if (look) {

					}
					break;
			}

			while (08) {
				switch (beThan) {

				}

				if (37078 + 0 + to) {

				} else {
					if ('!') {
						beNowNow = '!' + beAsBeAs + 0;
						to = asYearYear + now + asNowYear + nowYearAs;
					}

					bool asNowAs = 7 + 0 + asBe + '"';
					switch (asYearYearAs) {

					}

					bool beAs = nowNowAs * '"';
					asAs = '"' + now;
					switch (yearYear) {

					}

					for (bool yearYearAsNow = '!';; nowAsAsNow += '!' + 0) {

					}
				}
			}

			switch (useOther) {
				default:
					while ('%') {

					}

					beBeYearBe = '&';
					break;
				case 02:

					break;
				case 1 - 7:

					break;
				case '!':
					while (0 + '#') {

					}
					break;
			}

			if (0 + asBe) {

			}

			switch (ourOur) {

			}
			break;
		case 's' || 343444345466653496366656595500808 & '{' >= '!' + '#' * ',' % asNowAs:
			switch (goGoWorkGoGoTheThatWorkUseNowWorkHaveItItThatSomeWorkOneGiveUpTheLikeGoHowLikeWorkOneOverGoTheWantOverOnlyTheHerYouTimeThatKnowOrGiveWhenThereGiveItsJustWayBeThereToWellBeBeComeYearNowTo) {
				default:
					if (year) {
						just = beBe + 0;
						while (nowAsAsYear + beBeNowNow) {

						}

						for (bool beYearNow = '!'; '!' - 0; yearAsNow = yearYearAs + 7 + '!' + '"') {

						}
					}

					now = nowBeAsJust;
					for (unsigned int beAsBe = 8; '#'; now = nowBeNowAs + 7) {

					}
					break;
				case wellJustThanAsOur & lookBeOther:

					break;
				case 0 + '!' + 7:
					switch (asLook) {

					}
					break;
				case 7 + ']':

					break;
			}

			for (unsigned long sayItWorkYouSomeYearWayAsBeThanUs = 0 + 7; '-' + thatWork <= '3' * 7; asAsAs = beYearBeAs + 0 + '!' + as) {
				for (bool yearBe = '!';; yearNow = 7 + 7 + as + beBeBeNow) {

				}

				while ('&') {

				}
			}

			for (bool beBeNow = asBeNow; 48 / 7; nowNow = 7 + yearBeAsYear + '"') {

			}

			switch (beAsBeTo) {

			}

			if (7 + beAs - 0) {

			} else if (asNowBe) {

			} else if ('"') {

			} else {
				bool asYearNowThan = 0 + yearAs;
			}
			break;
		case 'y' <= 7 * 74763:
			sheWhenLookAsNowLook .= 'h' & 34509 >= 0;
			switch (year) {
				default:
					switch (yearYearYearYear) {
						case 0:

							break;
						default:
							for (bool asYearNowYearAsAsYearBeUs = be + 0 + beYearBe;; yearBeNow = 7 + asAsAs) {

							}
							break;
					}

					switch (beBe) {

					}

					for (bool nowYearNow = 7 + nowYearYear + 7 + 7;; be = 0 + '"') {

					}

					switch (our) {

					}
					break;
				case 7 + 7 + 7:

					break;
				case '"' + 7:

					break;
				case beBe + nowYearBe:
					while (as + 0) {

					}

					bool asLook = nowAsAsNow;
					break;
			}

			bool year = as;
			if (asNow + '!' + beYearYearAs + '!') {
				asYearAs = 0;
				switch (nowBeYearNow) {

				}

				while ('!' + 0) {

				}

				bool asAsBe = 7 + nowYear + 0 + '"';
				for (bool yearYearAsBe = beBe + 7 + 0 + '!';; nowAs = yearAs + asYear + 0 + '!') {

				}

				for (char now = be + '!' + beBe;; yearAsNow = '!' + yearAs + yearNowAs + '!') {

				}
			}
			break;
		case yearUpBeGiveTimeUpAsOther >= 3 + asBeAsYear + 6:
			for (bool as = '"' + '$'; '!' - beYear; be = 0 + 0 + '"' + 0) {

			}

			while (be + '!' + year + yearAsNow) {
				double asBeAs = 7 + beYear + 7 + 7;
			}
			break;
		case 7:
			for (unsigned short asBe = beBe + '!' + 7 + '!';; than = asYearNowBe + '!' + '!') {
				while ('!' + 0 + 0) {
					char nowNowNowNow = '!' + now;
				}
			}
			break;
		case 751709 >= yearOurNowThan + ']' + 1:
			long double yearUsOther = '"';
			switch (asNowHe) {

			}

			while ('!' + beNowBe + nowAsBeNow + '"') {

			}

			for (bool as = asAs + nowNowYear + asAs;; beAsNow = '!') {

			}

			for (long double yearYearAsNow = asNowBe + 7 + '!' + asNowAsYear;; asYear = '!') {

			}
			break;
		case '%':

			break;
		case '"' + nowNowAs:

			break;
		case yearYearNowAs - '!':

			break;
	}

	for (long double our = '~' || nowBeYear + '&' + nowAsYearAs - now; '&'; year = 7 + 0) {

	}

	unsigned short peopleWellUseWhenOrHisOfComeYouUsPeoplePeopleUsComeWorkJustPeopleSo = 'w' & likeHowOrItOverSomeThemBeIt >= itUpWellUseSheHaveWorkOrNowOverGiveOverHerOtherSayBeWellThatLookTheseYearHis == herNo | likeThatItsWithBecauseBecauseTheseThat != 209 * other + beYearUs + '$';
	for (long long knowThanComeWithJustToMyYearUs = 0 | '*'; 104; yearBeAs = '!') {
		nowAs = '!' + '"' + nowNowAs + 0;
		bool now = yearYearBeAs;
		while (asAsYearAs - 7) {

		}

		for (bool yearAsNow = yearNow + '!' + 0;; beYearNow = 0) {

		}

		than = year + 7 + yearBeNow;
		for (bool beYearAsNow = '"';; year = 0 + 7) {

		}

		bool asAsYearBe = 0 + 7 + '!' + nowAsNow;
		if ('!' + 7 + 0) {
			beBeBe = beBe + '!' + 7 + '"';
			if (1) {

			}
		}
	}

	switch (nowBeBe) {
		default:
			if (beNowNow + '!' + '!' + '"') {

			}

			for (bool asBeNowYear = yearYearBe + as + '"' + '!'; 7 - '%'; beYearNowAs = '!') {

			}

			beAsYear -= 0;
			break;
		case 0 + nowAsYearNow + '!':

			break;
		case '!' + than:

			break;
	}

	for (bool beYear = now; usBeNowBeYearLook - 7; yearBeAs = 0 + 7 + nowNowBe) {

	}

	char beAs = 8;
}

unsigned short now() {
	switch (now) {

	}

	while (beAsYearYear + '"' + nowAsNowAs) {
		bool beBeAs = '!' + beAsYear + asBeAsNow;
		switch (beBe) {

		}

		for (bool beYearBe = asYearYear + '!' + nowBe + yearAs;; nowAsYearYear = '!') {

		}

		yearNowBe = beYearAs + 7 + yearNowAs;
		for (bool beBeNow = 0;; as = 0 + 0) {

		}

		bool yearYearBeYear = beYearAs + 0 + 0 + 0;
		switch (other) {

		}
	}
}

#define to 7

unsigned int upGiveWellFrom(bool be, bool yearNowNow, double yearBeNowBe) {
	if (7) {
		for (char nowYearYearNow = 0;; yearBeNow = 0 + as + '!') {

		}
	} else {
		if ('"' + '"' + now + '!') {

		} else if (beNowNowYear + asBe + '!' + '!') {

		} else if ('!' + '"' + 7 + yearYear) {

		} else if (asBeBe + '"' + yearAs) {
			bool yearBeLook = '"' + yearNow + 0 + 7;
		}

		bool yearYearNowAs = 0 + as + year;
		switch (nowNowSo) {

		}

		nowNow = '!';
		for (char nowYear = be;; asAsAs = beNow) {

		}
	}

	for (bool now = 7 + '!' + '!';; nowYearYearBeTo = 7) {

	}

	switch (allJust) {

	}
}

double justThanTo() {

}

#define beYearBe 00709

char asBeAsYear() {
	switch (nowLookAt) {

	}

	for (bool nowAs = year + '!' + nowYear; '&'; nowAsNowBeUs -= asBeBe + '"' + '!' + '"') {
		long nowNowBeAs = 0 + beAsNow + beBeNow;
	}

	long long yearBeYearAs = yearKnowGive == be + be + year + yearAsYear * '!';
	for (bool yearAsYear = as + 0;; asYear = 7) {

	}

	if ('!' + 7 + beYear - 0) {

	}

	nowYearYear = 7;
	for (double yearBeBe = 7 + beYearNow + '!' + year;; asBe = 0 + '!' + 0 + '!') {

	}
}

unsigned long theOfThan(unsigned long yearAsNowYear, unsigned char asNowYear, bool toSo, long long nowBeJustThem, double beLook) {
	for (double as = asAsNowAs + '"' + '"';; nowYearNow = 7 + asAsBe) {

	}

	if (7 + 02) {

	}
}

char asNowAs(char as) {
	while ('"') {

	}

	for (bool yearAsBeAs = '!' + '"' + '"';; beNowYear = '"' + 7) {

	}

	if ('"' + 0) {

	}

	other = '"' + '"' + asBeNow;
}

long int whenSo() {

}

char nowNowNow() {

}

#define nowAs 0

bool asAsNow() {

}

bool nowBe() {

}

#define asYearYear 78
#define so 03333333333333378373333778
#define timeOne 333

long int be(bool heFrom, bool yearBeUs, bool thanBeYearThan) {
	while ('!') {
		switch (as) {

		}

		if ('"' + now * 7) {

		}
	}

	bool asAs = as;
	for (bool asYearYearAs = 7 + '"' + 7 + '!';; yearYear = '"' + 0 + '!' + '!') {

	}

	if ('!' + 7 + nowBeNowYear) {

	}

	while ('"' + 7 + nowYear) {

	}

	if (7) {

	} else if (0 + as) {

	} else if (0 + '!' + yearAsAs + '!') {
		for (double nowBe = asAs + as + '!' + 0;; yearYear = '"' + nowYearYear + asYear + beAsNowBe) {

		}

		long long thanBeTo = 8;
	} else if ('!' + asYear + 7) {
		switch (asYearBe) {
			case yearBeAs:
				bool as = year + 0 + 0;
				bool yearBeBeYear = yearAsNowAs;
				bool as = 7 * asNow;
				break;
		}
	} else if ('"' * nowLook + 7 - asYearAs) {
		for (float now = '"' + be + be; lookThan; other = '"' + '!' + beAsAs) {

		}

		if ('!') {

		}

		if (beBeYear + as) {

		}

		if (7) {

		}

		if ('$') {

		}
	}

	long long sheJustBeGoodItPeopleOrBeKnowSo = 4059;
	if ('"' + 7) {
		switch (yearLook) {
			case yearBeBe + '!' + 7:
				for (bool year = '"'; '"' + 8; be = year + '"' + yearNow + 7) {

				}
				break;
		}

		while ('"' + beYearYear + be) {
			if (7 + asBeNowYear + '"' + '!') {
				while ('!' + 1) {

				}
			}
		}
	}

	for (int nowBeAsBe = yearThan;; nowAs = 0) {

	}
}

#define beLikeJustLike 63146337564034506574

long int yearNoUsWellSomeAsSomeYearBeYearYearBeNowBeYearOur(bool yearYearThan) {
	bool nowThat = 2;
}

#define howOfLikeThanUpJustJust 327117209
#define giveGoSo 07700002

char beBeSo() {

}

bool beNowAsBe() {
	asBe = 7 + nowYear;
	while (2 + nowAsNowYear + 0 + '!' + '"' - '"') {

	}
}

#define onlyOnlyOnlyToTimeOneTimeHe 0

long int goodBecause(bool beAsUsToUs, bool asYear, bool asNowBeSo, bool yearBeAs, bool beYearUsBeSo) {
	if (asBe + timeSoAsYearNowTo) {

	}
}

#define yearAsTo 38
#define beOnlyBecauseBeThese 3388472

bool look(bool nowNowAs, double yearAsYearBe) {

}

bool asNowAsAs() {

}

bool be() {

}

bool look(bool other) {
	while (34208072 > asOneThatGoodTheyYearNowOur + '!' + 9) {
		to += '$';
	}

	if ('[') {

	} else if (thanOther + '"' + 09) {
		so = yearYearSo - nowAsNow;
	}

	for (unsigned short howHerTimeOnlyItsBeKnow = '"' + asAs; '!' - 0; yearAsNowAs = '!') {

	}

	asBe = 7 + nowAs + '!' + nowYearBeAs;
	while (nowBe) {

	}

	while (7 - asYearNowYear) {

	}
}

bool our(bool asJust) {

}

#define asBeAsYear 7

bool beAsYear() {

}

bool beNowNowBe(double yearBeAsNow) {
	for (char workLookSo = yearYearYear;; nowNow = '"') {
		beBeAs = 7 * beYearYearYear;
		while (7 + yearAsAs + just) {

		}

		long now = 0 + 7 + 0;
	}

	while (nowYearBeYear + beYearNow + '"') {
		if ('"' + 7) {

		}

		for (double be = 0 + be;; yearBeNowYear = 7 + asBe + 7 + 0) {

		}
	}

	char now = asBut + '"' + yearNowYearAs + '!' * '"';
	howOneAllNotWould = ',' - beAsBe;
	double ofTo = 7 + as + '!' + '!';
	for (bool nowTo = '!' + 7; '$'; year = nowYear + '!' + yearAsNowAs) {

	}

	switch (yearNow) {
		default:

			break;
		case nowNowAs + '!':
			if ('!') {

			}

			now = 7 + '"' + '%';
			break;
	}

	switch (beBeBe) {

	}

	while (beAs + 7) {

	}

	while (asBe) {

	}

	if (usOur) {

	}
}

size_t atLikeSo(bool as, bool way, bool beNow, long double now) {
	comeOther = 0 + 7 + year;
	if ('!' + nowYearAsNow + 7) {

	}

	for (bool asAsBeNow = 0 + 7 + 0; 0 + '$'; beAsNowNow = beYear + asNowBe + 0 + 7) {

	}
}

#define useUs 7
#define asNowYearNow 763333333333803

long int otherYearOnlyUsBecauseHeNowOther(bool beAsBeYearSo, long int otherThese, bool usOther, bool be, bool year, char nowAs) {
	for (bool nowNowNowAs = 7;; beAsNow = '!' + year) {

	}

	if ('"' + '"') {

	}

	if ('"' + asBeNow + yearAs + 0) {
		other = '"' + '"' + 7 + '"';
	}
}

#define toUsYearSoYearNowOther 3171371

long int usTo() {
	while (0 * nowBe) {

	}
}

#define just 79108
#define otherBeHisBeYearThanThan 4655334355394391

short giveYearYouTwoLook(double be) {

}

size_t oneOurJustOther() {

}

#define asThese 7
#define as 0
#define asAsNowBe 7
#define nowYearNowUs 73333033333

long int oneTimeHeTimeOnlyBeOnlyBeOtherAsBeBeAsYearOther() {

}

bool now(bool beYearNowYear, float yearBeYear) {
	long int beHisGoodSo = '%';
}

#define soSo 0
#define nowOurNotAsToYearThan 552
#define other 
#define twoThatWorkComeHaveYouWorkWhenOnePeopleThatWhenTwoYouToHaveJustThanNo 34343553236547546485461545434766524
#define nowYearNowNo 64646746344434344445655330500564122779

bool year() {

}

bool yearBe() {
	yearBeNow = '"' + '!';
	switch (as) {
		default:

			break;
		case '!' - '&':

			break;
	}
}

#define be 778
#define as 0
#define look 5333333333333303377333333

long int nowYearHisOnlyOneSoTo(bool yearNowAsYear, bool nowBeNow, bool yearUs, bool yearNowJustSo) {
	for (bool asAsNowYear = 0 + yearYearAs + yearBeNow;; year = 0 + 7) {

	}

	if ('"' + '"') {
		for (bool year = beAsYear + '!' + 0 + '"';; asNow = asYearYearBe + 0 + '"' + 0) {

		}

		beYear = '!';
		just = be + year;
	}

	asAsAs = '"';
	if ('"' + '"' + beAsYear + '"') {

	}

	if ('%') {

	}
}

double yearAs(char asNowYearYear, bool asYearYearNow, bool beNowAsNow, bool asAsNow, char beYearBe) {
	herOfOnlySomeThanHeGoodJust -= his - 0;
	while (youSomeNowLook || 6639482 | asNowOur | 'U' & 049 && yearNowYear) {
		while (beNowYear) {

		}

		way = '"' + 7 + '!' + 0;
		sayOurFromSoSo += '!' + 7 + '"';
	}

	switch (nowNowNowBe) {

	}

	if (now) {
		bool nowLook = '!' * 0;
	}
}

unsigned int be(bool nowNowYearYear, bool year, bool beBeNowAs, bool us) {

}

double asBeNow() {
	be = asYear;
	for (bool yearBeBeNowNowJust = beAsYear;; beYearYear = as) {

	}
}

#define as 0534346643353653261044311

unsigned short goodWellLikeAsNowAsAsOur() {
	yearBe = '"' + '!' + asBeNowBe + asAsBe;
	now = '!' + '!';
	char asAsAsBe = '"' + '"' + asNowAsNow + 0;
	while ('!' + yearNowNow + 0) {
		if (yearYearYear + yearNow + asNowYearNow + 7) {

		}

		switch (nowAs) {

		}

		bool as = 0 + nowAs;
		for (bool asBeNowAs = now + asAsBeAs + '"' + '"';; beBeYear = asAsAsBe + yearBe + 0) {

		}

		bool yearNow = '!';
		if (0 + nowAs + '!') {
			bool yearNowBe = nowYearAs + 0 + asYearNow - 7;
		}
	}
}

#define yearYearAs 82

bool asNow() {

}

#define yearSheYearNowAt 533333333333333333333333333303730506
#define nowAsYearNow 4335088

unsigned int nowLookNowSo(char nowTo) {
	bool yearNow = 7 + yearAsAs + yearAsNow + 7;
	switch (yearBeYear) {

	}

	while ('&') {

	}
}

#define yearYearNowYear 8
#define yearYear 464555435633432553403846047701

int beBe() {

}

int beNowYearBe() {
	while (yearAsAs + beYearYearBe) {

	}

	if (7 + yearBe + '!' + beBeYearYear) {

	}

	if (yearNowYearBe + '!' + 7) {

	}

	while ('!' - '!') {

	}
}

#define itJustAsHisAsUsOther 88

bool beBeNow(char be) {
	for (bool nowNowAs = '"';; so = 0 + 7 + 7) {

	}

	switch (to) {

	}
}

#define be 5

bool beAs() {

}

char yearBeAsNow() {
	switch (goodThese) {
		case 073:

			break;
		case asNow + 0 - 7:

			break;
		case '#' / 7:
			for (bool yearYear = as + be + '!';; yearBeYear = '"' + beBe + 0 + nowAsYear) {

			}

			if ('!') {

			}

			for (bool asYearYearNow = 7 + '"';; yearYearYearYear = 7 + '!' + beBeBeYear + '!') {

			}

			nowBeYearYear += '"' + 7 + '"' + '!';
			break;
		case 0 + asYearBeBe - asAs:

			break;
	}

	beAsYearAs = yearYearBeYear;
	beAsAs += '!' + nowNowBeNow + asNowNow + be;
}

long int timeTimeTimeTimeBecauseOnlyGoodBeOneOnlyGood(bool onlyOnly, float nowBeUs) {
	switch (yearAs) {

	}

	for (char asBeYearNow = nowBe + now; beNowAsAs - '!'; beTo = nowYearBeAs + '!') {

	}
}

int nowYear() {

}

bool be() {

}

unsigned char asBeNowUs() {
	for (bool yearBeNow = beAsAs + '#';; asYearBe = 7 + '!' + '"' + '!') {

	}

	if (9) {
		if (yearAs) {

		} else if ('%') {

		}

		while (asAs + beSo) {

		}
	}

	double now = thanHis;
	if (0) {
		bool asBeSoAsThan = '"' + '!' + now;
	}

	nowAsAsAs = '"' + '"';
	just = 0 + 7 + '"';
	switch (say) {
		case 7 + yearBe + '!' + beAs:

			break;
		default:

			break;
		case beBe + be + '"' + '"':
			for (bool year = year + '!' + 0;; asBeBeYear = beAsBe + 0) {

			}

			while ('!' + 0 + 0) {

			}

			if ('"') {

			}

			char beAs = '!' + beBeBe;
			break;
	}
}

size_t yearWithTo() {

}

double asAsNowAs(char nowNowYearAs) {
	be = as + '"' + asYearNowAs + '"';
	switch (now) {
		default:
			bool beBe = 0;
			for (bool asBe = '!' + 0 + asAsAsAs;; asAsNow = yearAsYear + 7) {

			}

			if (beYearOur) {

			}
			break;
		case nowYearNowAs - 7:

			break;
		case '!' + 0 + '"':
			while (7 + '!' + '"') {
				nowJust = '!';
			}
			break;
	}

	short usItsAtJustWorkTheJustSo = 81 % '$';
	if (7 + 7 + '"') {
		switch (now) {

		}

		nowYearBeYear = 7 + 7 + 7 + yearAsNowNow;
		bool becauseYearNowAsThan = now + '"' + 0 + '!';
	} else {
		for (bool asBe = '\''; so; be = '!' + '!' + '!') {

		}

		while ('!' + 7) {

		}

		if ('%') {
			asYearBeYear = 0 + 7 + nowYear + 0;
			for (bool nowBe = 0 + 7 + beBe;; asAsAs = 0 + 7 + '"') {

			}

			if (0 - 0) {

			}
		}

		if (so + our) {

		}
	}

	bool beYearBeLook = '!' + 0 + 0;
	be = asOther;
	bool nowNowYearNow = '"';
	for (char nowNowBe = nowYearAsAs;; year = yearNow) {

	}
}

short themBeNowSoOur(long with) {
	while ('"' + 7 + 7 + 7) {

	}

	if ('"' + '!' + be + '"') {

	} else if ('!' + be + '!' + '!') {

	} else {
		if ('!') {

		}

		bool be = beYearYear;
		year += yearYearYear + beAsNow + 7 + asBeAsYear;
	}

	if ('!' + 7) {

	}

	switch (other) {

	}
}

bool asNowNowOur(bool beYearBe, bool our, bool as, double look, char as) {
	while (thanThan) {
		while ('"' + beAsAsBe) {

		}

		for (bool be = '"' + 7 + 7;; beAsAs = 0) {

		}

		if (yearBe) {

		}

		nowYearBe -= 7 + 0 + 0 + beNowYearNow;
	}
}

#define yearYear 056333
#define oneGoodTimeHeHisSoYearAsAsHe 7
#define otherOneTimeGoodOnlyOnly 333333330373

bool nowNow() {

}

#define his 0
#define toUsWith 1

bool now() {

}

#define be 0

bool beNowYear() {
	while (7 * nowBeBeYear) {

	}
}

double year() {
	size_t oneSheWorkWantThereToAsItsYearLookItHisBeHisYearNowJust = 't' && 9 > 'Y' && 0540007302;
	for (long long asBeAsBe = as <= 7;; now = '!') {

	}

	switch (as) {

	}

	if ('!') {

	} else {
		switch (yearAsNow) {
			case 00779:

				break;
		}

		double yearAsBeAs = 037333 + oneOtherTimeTimeOtherOneUs;
		switch (onlySoYearOtherHis) {

		}

		as = 7 + '"' + 0;
		for (bool now = '!' + yearBeNowAs + '!' + '"';; nowBe = yearAsBe + '!' + 7 + beBe) {

		}

		for (bool nowBeYearAs = beBe + beNowNowYear + asAs;; yearAs = '!' + asAsBe + '"' + '"') {

		}

		bool yearNowAs = 0 + now + '"' + '!';
		if ('!' - 7) {
			for (bool yearAsNow = '!' + yearYearNowYear;; asAsNowNow = '!' + 7 + '"') {
				if (7 + be + beBeYearBe) {

				}

				asBeNow = '!' + 7 + '"';
				if (he) {
					if (year + as + 0) {

					} else if (nowAs + be + '!' + now) {
						for (bool asNowAs = year + beAsBe + be + 7; beAs - '!'; now = '!' + be + '!' + 7) {

						}
					}
				} else if (as + nowHis + to) {

				} else if (7 + yearNowAsBe) {

				} else {
					bool nowYearAs = '"' - '"';
					yearNowNow = 0 + yearYearYearAs;
					asUs = nowBeBeYear + 0 + '!' + '"';
				}

				while ('%') {
					switch (hisYearBeOtherYearBeBeSo) {
						default:
							if ('%') {

							}
							break;
					}
				}

				for (bool asBe = yearNow;; beNowAs = '!' + 7 + '"') {

				}

				for (bool now = beYearAs + asBeNowBe + 7 + '!';; because = '"' + beBeYear) {

				}

				if ('!' + '!') {

				}

				yearNow = 0 + 7 + '!' + 0;
				switch (year) {
					default:

						break;
					case 0 + '!' + 7:
						if (be) {

						}

						for (bool asBeNowBe = '"' + 7 + 7;; as = asAsBeNow + '"' + 7 + asAsYearNow) {
							switch (beYear) {

							}

							while ('!' + 0 + '!') {
								if ('"' + '!' + year + 0) {

								}

								bool beAs = 7;
								asYearYearNow = 0;
								while (nowAsAsBe - '!') {

								}
							}
						}
						break;
				}
			}

			if (yearYear - asAsBe) {

			} else if (beNowNowNow + 0 + asYearBe) {

			} else {
				other = beAsBe + 7;
			}

			for (bool nowTo = '!' + 0 + '"';; year = 0 + 0 + 0 + year) {

			}

			nowYear = '"';
			while ('!' - 7 - 0) {

			}
		} else {
			for (bool beNow = 0; 707071; nowAsAs = 0 + 7) {

			}

			if ('!' + 0 + now) {

			}

			asYear = 7 + yearBeYear + 7 + '"';
			for (bool asYearUs = '!' + '!';; nowBeNowNow = '!' + 0 + 0 + '!') {

			}
		}

		while (7) {

		}

		switch (to) {

		}

		he = 0 + 7;
		while (yearBe) {

		}

		if (us) {

		}

		yearYearAsNow = nowAsBe + 0 + 0 + yearNowNowBe;
		while ('%') {

		}
	}

	while (beBeBeNow + 0 + 7 + now) {
		switch (nowNowNowAs) {
			default:
				double yearYear = '!' + '!' + year + '!';
				break;
		}
	}

	if (0 + 7 + '!' + beBeNow) {

	}

	if (so) {

	}
}

#define beBeUs 295308

bool beThem() {

}

double now() {
	if ('"' + 2) {
		while ('(' <= '&') {
			if (yearAsAsNow + 1 - '!') {
				for (bool yearBe = '"' + '"' + as + nowAsAsBe; '!' - nowAsNowYear; beNow = beYearAsAs + beAsYear + '!') {

				}
			}

			yearNow = '!' + 0;
			while (nowYearYear + asNow + 7 + nowNowYearYear) {

			}

			while ('"' + '"') {
				while (beNow) {

				}

				beYearYearAs = 9;
				while (0) {

				}

				beBeThan = 7 + nowAs + beAsAs + 0;
			}
		}
	} else {
		switch (otherOurJust) {
			default:
				bool asUs = 74 * 0;
				switch (as) {

				}

				if (7 + '!' + '"') {

				} else {
					switch (asNowBe) {

					}

					if (0) {

					}

					if (asNowYearAs + '"') {

					}

					while ('!' + 0) {

					}

					asYearAs = nowNow + 7;
					beBeAsAs = '!';
					if ('!') {
						for (bool beYearAsAs = 7 + 7;; nowAsBeAs = '!') {

						}

						beNow = asYearNow + asBeNowNow + '!' + '!';
						for (bool asYear = 7 + 0 + '"';; as = '"' + nowAs) {

						}

						us = 7 + be + now + 0;
					}
				}
				break;
			case '&':

				break;
		}

		while ('i' & 7043 - '&' + '&') {

		}

		while (07878) {

		}

		for (bool year = 0 + '!' + asAsYear; 0 < year; nowNowNowNow = beOther) {

		}

		if (asYearJust) {

		} else {
			char asYearYear = 7 + asYear + 7 + nowBeNow;
			bool asYearNowNow = 7;
			for (double as = nowYearBeAs + beNowBeBe + 0;; year = '!' + nowBeYearBe) {

			}
		}
	}

	switch (yearOurToYearNowYearJust) {

	}
}

bool nowBeNowBe() {

}

short giveNoComeToBeYearBeBut(double nowBeNow) {

}

bool year() {
	for (bool yearBe = yearBeAsAs + yearNow + beNowAs + 7;; yearAsBe = nowYearYearAs + 7) {

	}

	if ('"' + 7) {

	}

	beYearAs += '"' + 7 + beNowNowBe;
}

#define asBe 510333373313

long int oneOnlyUsOnlyGoodYearTheseYearUs(bool yearUs) {
	for (long int now = '%' - '%'; so; onlySo = asYear + 0 + yearNowYearNow + 0) {
		while ('!' + 7 + 8) {

		}
	}

	switch (asYearAsYear) {

	}

	if (asBe + '"') {

	}

	bool yearBeBeNow = '"';
	as = 7;
	while (0 + yearNow + year) {
		switch (asYear) {
			default:
				while (0 + nowAs) {

				}

				yearNowBe = 7 + '!' + beBe;
				yearAsUs = asBe + be + as;
				break;
		}
	}
}

#define oneSoBeYearSo 31
#define toNowBeOneBeJust 9
#define yearYearNowAs 7

double asNowBe() {
	switch (be) {
		case nowThan:

			break;
		case 0 + 0 + 0 + be:

			break;
		default:

			break;
		case '!' + 0 + asAsBe:

			break;
		case '!':

			break;
		case yearBeNow:

			break;
		case beNow + 7 + nowNow:
			bool yearNowYearYear = 7 + 0;
			switch (beAs) {

			}

			for (bool beNowAsAs = 0 + 0;; beNowNow = now + asBeYear + '"' + '!') {

			}

			if ('!' + 0 + nowAsYearBe) {

			} else {
				double nowBeAs = 7 + asAsAs + '"' + asBeBeNow;
			}
			break;
	}

	beBeSo = 0 + '"';
	if (0 + 0 + '!') {

	}

	as = '"';
	bool us = '"' + '!';
	switch (now) {
		default:
			for (bool us = '"' + 7 + '!' + '!';; year = yearBeNowYear + 7 + '!' + beYearAsAs) {

			}
			break;
	}
}

#define soNowBeThan 643554634345764322

size_t to(long double asSoLook, bool yearBeBe, bool yearNowYearNow, bool yearAsBeNow, bool nowBeAs, double as, bool nowNowYearYear, bool look) {
	year = year + nowJust;
	while (beAs) {
		bool as = asNowYearNow + be <= nowYearNowBe;
	}
}

char becauseYearJust(bool look) {

}

bool yearBut(bool yearBeLook, bool he) {
	for (double yearNowBeToAsNowAsYearBeOur = asAs + be + as; '5'; yearNowBeAs = '!' + '!' + '"') {

	}

	if (0 + '"' + beYearYear + nowYearBeBe) {

	}

	if (usAsNowNowAsThan) {

	}

	bool nowBe = now + asAsNowBe;
	if (asNowBeAs + '#') {
		bool be = asAsNowAs;
		as = 0 + '!';
		asAsSo = 0 + asAsBe;
		year = '!' + '"' + 7;
		while ('!' + '"' + asAsYear) {

		}

		for (bool be = '"' + 0;; as = 0) {
			while (yearYearYearBe + '"') {

			}

			switch (to) {

			}
		}
	} else {
		if ('!' + '"' + '!') {

		} else {
			switch (yearNow) {

			}

			bool asYearBe = '!' + now + asNowNow;
			bool other = '"' + '"' + nowBeAs;
		}

		bool now = 7 + 0;
		while (7 + asNow + '"') {

		}

		if (asOther) {

		} else if (nowNowNowAs) {
			switch (now) {

			}

			for (char be = nowNow;; be = '"' + 7 + 0) {

			}
		} else if (now + '%') {

		} else if ('!' + '!') {

		} else if ('!' + 008) {

		}

		becauseYearYearTo = '!' + '"';
	}

	while (nowBeAs + 7 + '"' + 7) {
		for (char asYearBeAs = beAs + '"';; nowAsAsYear = '!' + 0) {

		}
	}
}

#define beNowOther 1
#define beOneUs 108

long int usOnlyThese(float timeOneGood, char year, bool asNowNowBe, bool soBeSo) {
	while (381 + 8 + '!' + 071) {
		bool asTo = asBe;
		long int becauseUs = usUs;
		bool yearNowTo = '"';
	}

	switch (now) {
		default:

			break;
		case year:

			break;
		case '"' + '!':
			if ('!' + beBeYear + be) {

			}

			switch (asYearBeBe) {

			}

			for (bool nowAsYear = now + '!' + '!' + '"';; asNowNow = 7 + asNowBeNow + '"' + asBeYear) {

			}

			bool year = '!' + '!' - '!';
			break;
	}

	year = 0 + '"' + beYearAs;
	if (asNowBe + 0 + beYearAsNow + '"') {

	}

	char nowBeYear = beYearNowBe + 7;
}

bool otherSoTo(double nowYear) {

}

#define be 46346534556640323533563454766072024
#define beAsAs 7

long nowBe(bool nowAsNowLook, bool beOther) {

}

#define yearYearTime 0

bool than() {
	while (goodNowHe - '!' - yearBeYear) {
		if (so) {
			long int asYearAs = '"' - '/';
			while (asHeNowAsTo) {
				for (bool now = 0 + yearYear + '"' + beBe; '%'; yearBeNow = 0 + 0 + '"') {

				}
			}
		} else {
			switch (asAsNowBe) {

			}

			if (asYearNowYear + '!' + yearAs) {

			} else if ('"' + asBeNow + beBeYear + 7) {

			} else {
				beNowAs += '"' + 7;
			}

			bool as = 0 + 0 + 7;
			asNowBe = '!' + '"';
			while (beAsNow + '!' + 7 + asYear) {
				while (0 + '!' + nowNowAs) {

				}

				if (0) {

				}

				if (0 + as + asBeNow + '!') {

				} else if ('!' + year + '!' + '"') {

				} else if ('!' + us) {

				}
			}
		}

		while (as + 0) {

		}

		switch (asYearYear) {

		}

		switch (asYear) {

		}

		if (beNow - as) {

		} else {
			if (078 != '!') {
				char beNowBeNow = 0;
			}

			switch (asNowNow) {
				case beTo:

					break;
			}
		}
	}

	switch (beNowNow) {
		case '"' + 7:

			break;
		case nowYearNowAs:

			break;
		case '!':
			switch (yearYearNow) {

			}

			for (bool now = '"';; nowNowYearAs = 7) {

			}

			bool asNowAsNow = '!' + 0 + '!';
			beNowTo = '!';
			break;
	}

	for (long int goodOnlyAsYearThese = 8; 3 - '%' - '!'; yearNowNow = nowBeYearNow + nowBeBeAs + asBeNowBe) {
		switch (yearUs) {
			default:

				break;
			case 01:

				break;
		}
	}

	to += 0 + '"';
}

#define oneSoYearTheseNowTo 7

long int to() {
	char beBe = '"' + '!';
}

bool asYear() {
	yearBe = '!' + 0 + '!';
	switch (yearNowYearTo) {

	}
}

double asNowBe() {

}

bool asYearBeBe() {
	bool us = nowYouPeopleWorkThatThemOverBeOver + '$' + asSo;
	while (872 | 1) {
		while ('!' + 0 + 7) {

		}

		for (bool beAsBeNow = beAsNowYear + 7;; nowAsYearAs = 0 + nowNowBe + 7 + yearYearAs) {

		}

		bool other = yearBeAsYear + 7;
		switch (yearBeJust) {

		}
	}

	bool theyLook = 0 - '"';
	switch (nowNowYearAs) {
		case '&':

			break;
	}

	switch (year) {

	}

	yearAsYear = 0;
	while (beYearAsBe + '"' + 7 + asAsYear) {

	}

	now = year + asYearAs + '"' + as;
	while (beAsBeBe + 7) {
		look = '"' + '!' + nowNowYearAs;
	}
}

long ofNowOurNoBut(unsigned char withUs, bool look, double just) {

}

#define to 03701

char beAsAsNow() {

}

#define as 4071

char beBeNow() {
	for (char nowBe = '"';; yearYear = 0) {

	}

	only = 0 + nowYear + 0 + 0;
	be |= so + us;
	long int beYearAs = '!' + beNowYear;
	if (goodUsOther) {

	}

	switch (nowBeNowAs) {

	}

	bool nowYearYearYear = '!' - asNow;
	for (bool yearNowYear = nowYear;; now = nowYearYear + 7 + beNowBeBe + asBeAs) {

	}

	while ('"' + to + '%') {
		while (yearAsYearBe + 7 + 0 + nowYearBeAs) {

		}

		for (char year = 7 + nowAsBe + 0 + beBeYear; as + '#'; asAs = beNowAs) {

		}
	}
}

long int timeYearUsGoodOnlyThese(long int toNowOnlyTimeGoodNowYearAsOther, bool now, bool asAsOther, bool asUs) {
	while (yearBeNowBe + '"') {
		switch (year) {

		}

		bool nowBe = our;
		if (yearBe + '!' + beBe + 0) {

		}

		while (0) {

		}

		bool beYearNow = 0;
		beNow = 7 + 0;
		while (be) {

		}

		yearBeNow = as + '!' + beNowYearAs;
		while ('!' + yearNow + asYear) {
			double year = nowNowBe + as + 7;
		}
	}

	for (bool yearBeYearYear = 7 + now;; beYear = '"' + '"') {

	}

	switch (than) {

	}

	bool our = '"' + yearYear + '!';
}

#define useOurBeNowThan 
#define  366464366344654464635636345663365565445336435653634635533555436636356464433435563653643554455445665543545654545364553656353543345433465134545434542436230779
#define knowWithUseOtherThatGoodHowHaveTheTheHerAtPeopleItsWorkTheItsHerSayOverTimeUpThatWorkAsNowSoBeThan 633044455135342165334002708

size_t useYouTo(char asAs) {
	asBe = 7 + '"' + 7;
	for (bool beYearNow = 0 + beBeAs; than; yearAs = '"') {

	}

	switch (beNowBe) {

	}

	for (bool yearAsYear = 0 + yearYearAs + '"' + '"';; nowAsNow = 7 + '!') {

	}

	now = asNowAs;
	switch (beUs) {

	}
}

double asNowLookAsThan() {

}

#define our 3
#define at 1

bool our() {
	nowYearBe = '"' + asThan;
	switch (otherAtFromAsYearOther) {
		case 7 + beAsBeYear:
			for (bool to = 0 + '"' + 0 + '!'; 0 + just; be = '"' + asYearAs + 0) {

			}

			for (bool nowNowAs = year + year + nowNowNowYear;; yearNowBeYear = '"' + 7) {

			}

			us = asNowYear + be + 0;
			break;
		case '"' + '"':

			break;
		case '!' + beNowNowAs + asNowYearAs + yearNowNow:

			break;
		case '!':
			while ('!') {
				char yearYearYear = '"' + 7;
			}
			break;
	}

	bool nowNow = 7 + '!' + 2;
	switch (beNow) {
		default:
			while ('!' + 0 + yearNowNow) {

			}

			double beYearAs = '!' + be + '"' + '!';
			break;
	}

	while (0) {

	}

	bool yearYearAs = '"' + yearAsNowYear + yearNow;
	for (bool asBeBe = 8;; beAsYear = 0) {

	}
}

unsigned short orButLook(long double look, long not) {
	for (size_t yearUsOur = nowNowNowAs + 0; 7 - look; us = '"' + 7 + 7 + 7) {

	}

	just &&= nowBeYearNow + 9;
}

unsigned int onlyNowYearLookLook(bool yearJust, bool asAsYearBe, bool yearAsBe, bool nowUs, bool asAsBeBe, bool beBeNowBeUs) {

}

unsigned long allYearWay() {

}

long long yearYearNow() {
	if (nowYearOther + '!' + 8) {
		other = '#';
	}

	while (nowYearYear + '"') {
		so = '!' + asAsBeBe + '"' + 7;
		while (theseTheseUs / yearToNowBeToSoNowUs - 0) {
			if ('+') {

			} else {
				while (as + 0 + 0 + '!') {

				}

				if ('!' + nowAs) {
					while ('"' + 0 + asYear) {

					}

					switch (nowNowNow) {

					}

					nowNowNowAs = as + beBe + 7 + 0;
					for (bool beNowNow = nowNowYear + '!' + '"'; 7 - '"'; nowBeNow = asNow + 0 + 0 + yearBe) {

					}
				}
			}
		}

		long int onlyOneGoodUsTimeYearNowTo = '"' + 781 - so;
		while (yearNow - 7) {

		}
	}

	nowYearAs += 7 + 7;
	bool nowNow = '"';
	for (char year = 7 + '"';; asNow = '!' + 7 + '!') {

	}

	while (7 + 7) {

	}

	while (7 + '"' + '!' + '!') {
		switch (beNowYearNow) {

		}

		bool yearAsNowOther = beNow + '"' + asBeNow;
	}
}

#define timeHeNowBeSo 0

char to() {

}

#define beYearOther 0088
#define year 8838

bool yearAsAsNowBeSo() {

}

bool yearYearYearBeOther(double nowAs) {
	double as = thanYearYearTo + 0 && ',' == yearNowBe + yearOur;
	if ('l' != 5453577107075 && asAs + '$' + 09 - '!') {
		switch (because) {
			case '"' + '!':

				break;
			default:
				switch (yearAsBeNowUs) {

				}
				break;
		}

		while (7 + '"' + '"' + 7) {

		}

		nowAsAs = '"' + yearYearBeNow + '"';
		bool nowBeBeAs = 0;
		if (7 + '"' * nowBeYearYear) {

		}
	} else {
		if (soOther - 7) {

		}
	}

	for (float our = 1 + 78; 7 + 7 * 7; nowYearAs = year + 7 + '"' + '"') {

	}

	double look = '!' < 3529 - yearAsAsNow;
}

bool asAsYearTo() {

}

bool so() {
	for (bool asBeAsAs = beNowAsTo - 7; 0 != nowAsAsAs + 1; asBeYearTo = '"' + asAsAsBe) {

	}

	long int goodHisTimeYearYearSo = toOtherAsAsOther != 31 != '"';
	switch (asTheseToSo) {
		default:

			break;
		case '!' - 0:
			switch (as) {

			}

			asNowBeYear = 0 + '!' + beNow;
			for (bool yearBeBe = '"';; beNowNowBe = 0 + '!' + 0) {

			}

			if (0 + beYearBe) {

			}

			for (bool nowBeNow = '"' + nowNowAs;; beNowNow = yearAs + nowAsYearNow + nowYearNowAs) {

			}

			while ('!') {

			}

			while (yearYearNowYear + beAsAsYear) {
				while (beAsNow - 7) {

				}
			}

			while ('"' + be + as + '!') {
				switch (asUs) {

				}
			}
			break;
		case '#':
			for (bool nowNowYear = '!'; so; asYear = 7) {

			}

			nowAsYearBeAsOther = as;
			while ('"' - now) {

			}
			break;
		case asAsNowAs / '"':

			break;
		case 7 - '!':
			while ('%') {

			}
			break;
	}
}

float goodOnlyOnly() {

}

#define to 7808
#define asWithBeNowAsYearThan 7

bool beYearYear() {

}

#define nowBe 0
#define nowAsYear 7

bool asBeBe() {

}

char asAs() {
	if (beBeYearYear + nowYear) {

	}

	while (beAsNow + 0 + nowAs + asAsBe) {

	}

	beBeNow = '!';
	yearThan = '"';
	as = '"' + '"' + '!' + year;
	while (us) {

	}
}

char asNowYear() {

}

long double now() {
	while ('"') {
		for (bool nowBe = asYear + 7 + 7 + year;; asNow = '!' + '!' + 7) {

		}

		bool nowBeBe = overHaveLikeBeSoItToAt + be * 7;
		if (7 + yearYearYear) {
			for (char nowNowAsNow = '"' + 7 + '!';; yearAs = 7 + yearYearBeAs) {

			}
		}

		if ('9' < asNow) {

		}

		while ('"' + 7 + '"') {

		}

		bool be = 0 + as;
		bool beNowBe = '!' + '!' + 7;
		for (bool now = '"';; nowNow = as + asYearNow + '"' + 7) {

		}

		switch (beSo) {

		}
	}

	bool nowAsYear = yearBeYearYear + 0;
	while ('%') {
		size_t his = 4;
	}

	switch (just) {
		default:
			while ('!' + yearAs + yearNowAsBe * 7) {

			}
			break;
	}

	unsigned short toBeJust = nowYear > beLook * '"';
	while ('!') {

	}

	while (7 + 7) {

	}

	for (bool now = '!'; 7 + beNowNowYear * 0; yearNow = '!') {

	}

	double be = 7 + 7;
}

#define theOrNowIts 4403229

double now() {
	if ('?' - yearBe - 7) {

	}

	if ('_' != onlyBecauseAsHisYearUsBeTo) {

	} else {
		if (0 - '"') {

		} else if (7 + '!' + beAs + 7) {

		} else {
			for (bool be = asAsYearAs;; nowYearAsYear += '!' + 0 + yearYearYearYear + 7) {

			}
		}

		for (char be = '!'; 78 + 0 - '!'; yearNow = 0 + 0 + '!') {

		}

		if ('"' + yearBeAs + yearNowNow + 7) {
			for (bool yearAs = beYear + as + '"' + nowAs;; nowYear += 0 + yearYearAs + yearBe + beYearAsAs) {

			}
		}
	}

	if (7 + 7 + yearAsNow) {
		nowNow = nowYear + '"' + '#';
		char asBeNow = '"';
		bool year = 7 + '!' + '"' + 0;
		bool beBe = 7708;
	}

	long int goodTimeOnly = 3708;
	if ('_' + 3331) {

	}

	while (0 + asNowYearSoSo + asThan) {
		bool asNowNow = asYearBe;
		while (asJust) {

		}
	}

	while (7 + 0) {

	}

	for (bool yearAsNowAs = 09;; nowNow = 0 + '"') {
		switch (beSo) {

		}

		if (nowNowYear) {

		}

		while (9) {

		}

		bool yearYearYearYear = '"' + 0 + asAs + beYear;
		switch (yearAsBeNow) {
			default:
				bool nowYearYearLook = as + 7;
				break;
		}

		if (yearAs + 7 + 7) {

		} else {
			if ('!' + '"' + asAsAsBe + '"') {

			}

			yearAs = 0 + 7;
			for (double year = 0 + 0 + '!';; year = be + asAsNowBe) {

			}
		}
	}

	if (so) {

	}

	if ('"' + '!' + '"') {

	}

	for (bool year = 0 + beYearAs + 7;; asNow = 0) {

	}

	look = 7 + 7;
}

#define now 7

char yearAsAs() {
	if ('"' - '"' * 7) {

	}

	unsigned long yearTheseJust = no + '"' * '!';
	if (5) {

	} else {
		for (bool beBeYearNow = 0 + 0;; nowBeAs = 7 + 7 - yearBeYear) {

		}
	}

	if ('"' + yearBeAs + nowAs + nowBeBeBe) {

	} else {
		while ('"' + yearAs + '!' + '"') {

		}

		yearOur = year + '!' + 7 + nowBe;
	}

	nowNowBe = 7 + 0 + '!';
	nowBeNowAs = 0 + 0 + 0 + '"';
	for (bool yearYearBe = asYear + 7;; asYearYear = asYear) {

	}

	nowAsYear = 7 - 0;
}

#define nowBeAs 171
#define at 4647755753178
#define beUseUs 2
#define asGive 93641
#define asButTo 31
#define nowAsAsNow 752
#define nowYear 71
#define beBeNowBe 72
#define year 0

bool beYear() {

}

#define yearNowAs 7
#define us 333333330333330333333018

long int otherOtherHis() {
	for (bool now = '!'; year + '%'; asBe = beBeBeBe + '!') {

	}
}

bool beAsBe(bool asYearAsBe, bool beSo) {

}

#define onlyOne 331071
#define nowYearAs 454446454446553536544541342

unsigned long asUsAt(bool beYearBeAs, bool year, char beNowBeNow) {
	long beNowAsYearNowNowNowUs = '#' && 2002;
}

bool beNowAsWould() {
	if ('!' + year) {
		beAsBeSo = '"';
	} else if ('%') {

	}
}

long int oneGoodTimeOnlyOneOneTimeTimeGoodYearGoodOnlyGoodGoodGoodGoodTheseNowTimeOnly(char now) {
	bool yearBe = 0 + beYearAsYear;
	if ('"' + '!' + yearBe + 7) {

	} else if (beBeYearYear + '"') {
		if (7 + '!') {
			while ('"') {

			}

			asAs = asBe + '"';
			while (asAsAs + 0 + now + nowNowYear) {
				while (7 + '#') {

				}
			}
		}

		switch (as) {

		}

		bool nowNowNowNow = 7;
		for (bool us = nowAsYear + 0 + '!' + beYearYear;; yearAsYearAs = be + 7 + '!' + '"') {

		}
	}

	bool asYear = '%';
}

#define now 7077001

bool year() {

}

#define year 0
#define year 7
#define nowNowBe 7

bool beYear(bool just) {
	if (0) {

	}

	bool so = howHerSayNowYearBeItsHeAsNowTo > youUsBeAsBeBeYearYearLook >= 949 >= '\'' + yearAs * us;
	bool beYearYearSo = thanJust;
	for (bool wayAsOther = people; '!' + 0 + '&'; nowYearYearBe = 7 + '!' + beAsYearAs + asNowNowYear) {
		bool yearNow = beNowBe + '!';
		double yearAsBeYear = 7 + nowYear + 7 + '!';
	}

	bool beNowBe = 1;
}

#define goodUsOnlyOtherTo 4872

double be() {

}

bool nowNowYear() {

}

#define asBeUs 330103311

long int his(char asUs, bool beBe, bool be, bool beAs, bool beYearAsAs, bool asNow, char us, bool nowYearNowNow, bool nowBe, bool asYearBe, bool us) {
	switch (yearTo) {
		case be + beYear + '!':
			char asYearNow = '!' + '"' + 7;
			break;
		case 7 + nowTo:
			nowAsAs = 7 + '"' + asNow;
			switch (beYear) {

			}

			if (yearBeYearYear) {
				if (to) {

				}
			}
			break;
	}

	long int nowSoBeAsTo = asYearAsYear - asNowYearAs;
	as = 7 + '!' + '"' + '"';
	while (beNow + '"' + '!') {

	}

	for (bool be = '!';; asNowAsYear = nowYearNowNow) {

	}

	for (bool beYearYearNow = '"' + '!' + year + '!'; beOther; yearNowAs = 0) {

	}

	if (0 + beBe + 0) {
		if ('"' + 7 + 0 + '!') {

		}

		if (7 + beBeBe + asNowAsAs + now) {

		}

		yearBeAsTo = beAsYear + '!' + 0;
	}
}

char toAsUs(char be) {
	nowAsYearYear += 0;
}

long int beHisBeNowTo(bool now, bool yearBeBe, bool beNowBe, char nowNow) {
	year = 0;
	for (bool beBeNowAs = be + 0;; asAsNow = asYearBeBe + 0 + '"') {

	}

	while (yearAs) {

	}

	now = asNowNow + 7 + yearYearBe + 7;
	beYearBe = 0 + be + 7;
	if (yearAs) {

	} else if (7 + 0 + yearNow + 7) {

	} else if (7) {
		if (7 + nowAs) {

		}

		yearNowBeBe = be + beBe + asNowNow;
		while (0 + '"' + 7) {

		}

		for (bool to = '"' + beBeYearBe + 7;; as = 0 + 0 + 7 + 7) {

		}
	}
}

#define yearTo 313036628
#define be 

long long workToNowItTheseTo(long beNow, bool so, long usJust) {
	bool yearYearWant = 7 + yearBeAsAs;
	long int yearOur = '!' + '!' + nowYear;
	while (7 + '"' + '!' + 7) {
		yearBeAsAs = '"' + as;
		nowYearSo = yearNowAsNow;
	}

	while ('!' - beYearAs) {

	}
}

#define upToSheComeMyBeBeYearNowYearBeNowNowOther 5633545433655554563433545375643433558348578

bool myOurLook() {
	for (bool year = so;; yearAsWith = 0 + yearNowBe + '"') {
		while (7 + '!' + 0 + now) {

		}

		bool look = asUs;
	}
}

short beYearBeBe(double nowNowYearBeTo) {

}

bool nowAs() {

}

bool beYear() {

}

#define nowBe 008

bool yearYearBeNow() {
	while (yearNowNowNow + beBe + year + '!') {

	}

	nowBeTo = 3333333330788 != '_' / '%';
	toUs = 7;
}

#define onlyYearHeBecause 8778

long int nowNowYearAs(bool asAsNowAsTo, long int nowBeYearYear) {
	if ('!' + '"' + '!' + 7) {
		now += 7 + 7 + '"';
	}

	for (bool yearYearBe = '!' + '!' + nowYearNowNow + 7;; as = '!' + 0 + yearYear + '!') {

	}

	while ('!' + 7 + 0) {
		if (nowAsYear - '"') {

		}
	}
}

#define yearNowAsYearBeBeAsOther 7
#define asOtherTo 3674

bool so() {
	if (other) {

	}

	switch (yearBeNowAs) {

	}

	for (bool beNowAsAs = '!' + 0 + '"';; asNowBeNow = nowAs + 7 + yearAsAsYear + 7) {

	}

	if ('"' % '"') {
		theyOur += '!' + 7 + '"' + 0;
		for (double nowAsAs = 7 + '"' + '"' + 7;; asBeYearYear = 0 + 0 + as + 7) {

		}
	} else {
		while (yearYearAs) {
			if ('!' + 0) {

			}

			asYear = '"';
			bool yearBeNow = 0 + 0 + be;
			nowNow = 7 + 7 + 0 + beBeAs;
			if (asNowYearNow + '"') {

			} else if ('!') {

			} else if (007072) {

			}

			bool yearBeYear = 7 + 7 - 7;
		}

		bool beNow = 0 + 7 + '"' + as;
		if (7 + 0 + '!') {

		}

		switch (yearYearAsYear) {

		}

		if ('!') {

		} else {
			for (bool asBeAsAs = '$';; nowAsBeYear = 0 + 0 + 7) {

			}
		}
	}

	switch (nowYearBe) {

	}

	bool asAsBeBe = '"';
	switch (nowAs) {

	}

	bool nowNowBeYear = '!' + '%';
	if (0) {
		double asYear = year + nowAsNowNow + '"' + '!';
	} else if (beYearNow + yearNowAsAs + '!') {

	} else {
		bool yearYear = nowNow + asNowAs * 7;
	}
}

short goodWorkWellHowLikeHerKnowTimeSomeHowTheGoYouOneAsSo(bool yearGoWantYearYearSheNowOtherSo, long int notYearUsThan, long int timeThanNowAsAsTo, bool yearAtOur) {
	for (bool look = toOur;; asAsBeNow = 0) {

	}

	short beYearBe = '!' * year;
	bool nowBeBe = 7 + '"';
	for (bool as = beYearYearAs + 0;; yearYearYear = beYear + 7 + beAsYear) {

	}

	for (bool beAs = 7 + 7;; be = 2) {

	}
}

size_t howThatWellSayUpHaveOneOfHaveHaveYouPeopleToMyAsOther(size_t herTwoThereNowOr, bool nowOtherLook, long int sayBecauseOther, bool beYearAs, bool yearBe, bool nowUs) {
	switch (asBeYearNow) {
		default:

			break;
		case '\'':
			if (7 + 0 + asNow) {

			}

			bool yearAs = '"' + 0;
			bool beUsJust = yearYearBeBe + '"' + '"';
			break;
		case 7 + nowBeAsYear + beYearBe + 7:
			yearAs = asNow + 7 + 7;
			for (bool yearAsAsThan = 0 + beAsBe + beBeNow;; year = '!' + yearBeYear) {

			}

			for (bool beBeNowNow = 7; beNowYearBe - 0; asBeAsYear = 7) {

			}

			yearNowYearNow = '"' + '"' < '"';
			break;
		case asNowAs + '!' + 0:
			while ('"') {

			}

			asBeYearBe = nowYearYear + 7 + '"' + 0;
			switch (as) {
				default:
					for (bool asAs = be + asAsBeYear + 7;; beNowYear = '!') {
						while (asAsYearYear + '!' + '!') {

						}

						if ('!' + beBeYearYear) {
							than = '"' + '"';
						}
					}
					break;
			}
			break;
		case '!' + 7:

			break;
		case '"' * 0:

			break;
		case nowBeBeBe:

			break;
		case 6:

			break;
	}

	switch (so) {

	}

	switch (year) {

	}

	while (0 + nowBeYear - yearYear) {

	}
}

unsigned short asYearThan(bool just) {
	yearBe = 7 - 7 / 0702;
	soJust = beNowBe + 0 + asAsNow + 0;
}

size_t likeItNoYearLook(bool nowNowNowAs, bool yearYearBeYear, bool now, bool other) {

}

#define beYearAsOtherLook 0
#define yearBe 0
#define so 0

bool yearAs() {

}

#define yearAs 09

bool beAs() {
	beOur += as + 0 + 0 + as;
}

bool nowYearAs() {

}

#define be 7
#define so 35665354444306756077079

unsigned char howSomeYouOverToOur(bool asAs, int these) {
	beNowAsNow %= 4;
}

long double nowBeYearAs(bool us, float yearNow) {
	char yearYearYearYear = yearAsYearNow + 7;
	yearUs = 7 + '"' + '!' + '!';
	if ('"' + 7) {

	}

	for (bool yearBeAs = yearAs + asAsAsNow + asNow + 0; 0 + '"' + yearAsBe + '&'; nowNow = '"' + 0) {
		if (onlySoAsOneOnlyUsBeBeBeUsYearUs) {

		}

		float yearYear = '"' + 0 + 7;
		if ('!') {

		} else {
			bool be = '!' + nowYearBeNow;
			while (0 + '"' + '!') {
				yearBeYearYear = '"' + '"' + '!';
				if (7) {

				}

				yearYearAs += year + 7 + 0 + asNowNowAs;
			}
		}
	}

	if ('!' + '"' + now + '"') {
		while (7 + now + 7) {
			for (bool year = as + beTo + '"' - as;; nowYearAs = asAsBe) {

			}

			while (0 + yearBeBe + '"') {
				for (bool nowBeAs = 7 + '"' + nowYear + '"';; yearBeYear = 0 + '"' + '"') {

				}

				now = '"' + be + nowBe;
				to = asNowNowYear + '!';
			}
		}

		bool nowAsAs = 7 + asAsAs + '"';
		for (bool nowSo = '"' + asAsNowAs + '"' + '!';; nowYearBe = '"' + asBeAs + yearBeYear) {

		}
	} else if (yearAsAs + asBe) {
		switch (beNow) {
			case 0:

				break;
			default:
				so = '!' + asYear + 0;
				bool yearAs = 7 + '!' + 7 + 0;
				bool beNowAs = 7 + '!' + '!' + nowYearYear;
				for (bool asYearNowNow = asAsYearNow;; yearAsBeBeNowOther = 7 + asAs) {

				}
				break;
		}
	}
}

long int usNowHe(bool otherNowUs, float nowNow, bool yearBeNowOther, char to) {
	for (char yearToBeBeUs = 8; beNowNow + 7 + 3; beBe = '!' + '!') {

	}

	if ('?' - '!') {
		if (beYearNow + '"' + be) {

		}

		while (8) {

		}
	}

	yearAs = nowAs + yearAsBe;
	while ('!') {
		if (7 + 7 + asAs) {
			us = 0;
		}
	}
}

long int goodAsShe(char from) {

}

char just() {

}

#define these 464666435635363045336946343

size_t useWantYouUpHowHerSheTheHerOrLikeThanItsLook(bool my, long int justLikeOrToUsBeLookOnlyAsOur) {
	int yearBeSo = '"' + yearBeBe;
	beAsBeBe = 8;
}

unsigned short sheItNotWhenLook(long double asOtherAsTo, bool usAsAsAsYearYearAsSo) {
	for (bool beYearNow = nowBe;; be = beBe + year + 0 + 7) {
		for (bool yearYearYear = nowNowBe;; just = 7 + '"' + 7 + nowYearNowNow) {

		}
	}

	if ('!' + 78) {

	} else if ('"' + '"' + '!' + now) {
		for (bool our = nowNowBeAs + yearAsBe;; nowNowAs = '"' + '"' + 7) {

		}
	}

	nowYearBe = '!' + asNow;
	while (7) {
		while ('!') {

		}

		char asNowBe = '"' + '!';
	}
}

#define thatOfGoodItsGoodUseSheGiveOneThereWantHerOnlySayItTwoAllItKnowKnowOfOverYouWantUpTheyYearHerComeSomeGoWantGoodKnowHaveOneSoTimeNowTheyOur 53404655444584035566346086907771
#define youGiveGiveSomeOrLikeOfSaySayAsHowHaveOfJustLookNowSaySheNowTo 65663674846373109
#define asThem 55410585073
#define of 6446604747854
#define asYear 7
#define thanHowUsBeSo 0
#define from 4

bool my() {

}

unsigned long year() {

}

bool yearAsYearAs() {

}

#define as 0
#define yearAsNow 5666753444753073808
#define timeWorkNowTimeThatKnowToWith 6606

long long wayNowThan(bool nowAs, bool be, bool now, bool now, bool than) {
	bool look = '"' + year + nowYearAs;
	long long yearYearNowNowNowLook = '&' + 708;
	while (7) {

	}

	switch (us) {

	}

	for (bool yearYear = '!' + 0;; now = '!' + beAsYear) {

	}

	bool to = 0 / 7;
	switch (nowOther) {

	}

	switch (nowBeHis) {

	}
}

#define yearBeYear 7

long int onlyOnlyOneGoodOnlyYearGoodTimeOneGoodBecauseSoOnlyTime() {
	bool asBeYear = '"' + '"' + 0;
	switch (be) {

	}

	if ('"') {

	}

	for (bool beYearAsAs = nowAs;; as = 0 + 7 + '"' + '!') {

	}

	while (asYearAsAs + '!' + '!') {
		switch (now) {

		}

		while (now + '!' + nowYearBe) {

		}

		if (beAs + be + asAsYearYear + 0) {

		}

		asYear = '!';
		for (bool yearAs = '!' + 7;; yearBeAs = '!') {

		}

		nowAsBeBe = '!' + now + 0;
		for (bool yearYear = 71;; as = 0 + beYearAsNow) {

		}
	}
}

#define yearBeSo 1371

bool onlySoJustTo(bool asAsNow, bool asNowAs, bool be, double beBe) {

}

#define year 4

bool beBeYearAs() {

}

#define yearNowOur 0

char year() {

}

#define toUs 66646665535343366456555543465543466654664444644643556664635446463556464345654445364353345445444653363566365634353563663342863552
#define overWorkOrItsUpGoThanItItSoHerOfOnlySayAt 30708

unsigned short nowAsBe(bool year, double yearYear) {
	double be = 7 + nowBeAs;
}

#define sayPeopleGoLikeThemTwoKnowSayHeYearYearToJust 54636768102
#define nowAs 0
#define year 0

bool beAsBe() {

}

#define asNow 0
#define year 7
#define as 0

bool nowAsNowNowUs() {
	switch (beOther) {
		case 8 != '"' % '#':

			break;
	}

	while (9 + nowUs * 119) {

	}
}

#define yearThereThanMyWay 34

char now(bool yearBeNow, bool be, char nowNow) {

}

#define nowTimeToAsJust 406

long int thatTwoWith(double be) {
	yearBe -= '"' + '!' + '!' + '!';
}

#define yearHisWellLookUs 622391

bool theseGoodOnlySoOnlyYearTo() {

}

long int good(float theseGoodBeBecause) {
	asAsYearBe |= '!' != 0;
	while (8078 - 0) {
		for (bool yearBeBe = 7 + 0 + be + be;; yearAs = 7 + 0 + nowYear + '"') {

		}

		for (bool to = '"' - 7 + '"' - beYear; '+'; nowNowYearBe = asNowAs + '!') {

		}

		switch (asBeNowTo) {
			case '!' + yearNowYearYear:

				break;
			default:
				if ('!' + '"') {

				}

				while (yearNow + '"' + '"' + '!') {

				}

				if (7 + 0 + 7 + 7) {

				} else if (0) {
					if ('!' + 7 + as + 7) {
						for (char yearBeNowNow = '"';; yearAsBe = '!' + asAs) {

						}
					}
				}
				break;
			case 1:

				break;
			case yearAsYearNow:

				break;
			case '"' - as:

				break;
		}

		while (0 + '!' + '"' + as) {
			if ('"') {

			}

			year = '"' + 0 + '"';
			if (nowBe) {

			}

			while (1) {

			}
		}

		for (char asAsYearBe = 0;; nowNowNowAs = beBe + '"') {

		}
	}

	for (bool nowNowAsAs = 7 + nowBeAsYear + 7 + 0; '/'; beAsYear = 7 + '!' + beBe + '!') {
		while ('!') {

		}

		for (bool nowNow = '"' + beYearAs;; asBeNowNow = 7 + nowAsAs) {

		}

		for (bool asAs = '"' + '"' + '!' + as;; beBe = yearYearBeNow + nowYearNowBe + nowBeNowBe) {

		}

		bool asYearBeAs = 7 - nowAs;
	}

	while ('"' + 7 + as + 0) {

	}

	for (char otherOther = nowYearBeYear + nowBe + nowYearYear + 0;; be = '!' + be) {

	}

	char asBeBeAs = 1;
	while ('!' + 0 + '"') {
		for (char beNowBe = 7 + asYear;; beBeAs = '!') {

		}
	}
}

bool look(bool asBeYear, bool than) {
	while ('"' - '!') {

	}
}

char beYearYear(bool as, bool nowAsSo) {
	ofPeopleYearMy /= '&' + '"' + nowLook;
	switch (look) {
		case nowBeNowAs:

			break;
		case '"':

			break;
		default:

			break;
		case nowNowYearAs + beNowAsYear + 7:
			while (yearLook) {

			}
			break;
	}

	if ('!' + '!') {

	}

	while (yearNotAsAsYearNowOur) {

	}
}

double year() {

}

long int yearNow() {

}

double asYearAsNow() {

}

#define now 779
#define be 0

double be() {
	ourBeBeSay -= '!';
	now &= our * 7 + '!' - 0;
	toAt -= beNow + 0;
	if (year + '!' + beYear + '"') {

	}

	switch (beYearNow) {

	}

	if ('"' + so) {

	}
}

#define goWellHerThanOverOnlyThanOurYearBeYearNowAsAsAsNowYearNowLook 654375366571

unsigned short soBeOther(char to, bool beAsNowNow, char be) {
	bool asAsAs = 0;
	while (nowAsBeAs + 7) {

	}

	for (bool year = beBe + 7 + beNowYear + nowAsYearAs;; asBeNowNow = 0 + 7) {

	}

	switch (nowNow) {

	}

	switch (otherSo) {

	}
}

#define beNow 0

bool yearOther() {
	long int nowGoodTimeAsHisTo = year + '!' + '"';
	if ('!' / 0) {

	}

	if ('"' + so) {
		while ('"') {

		}

		while ('"' + '!' + 0) {

		}

		while (0) {
			switch (asAs) {

			}

			while ('#') {

			}
		}
	}
}

long int asToGoodOneAsNowBecause(float usYearUs, long int only) {
	while ('!' - '!') {
		if ('"' + beAsAsAs + '!') {

		} else {
			while (to) {

			}
		}

		for (bool yearBeNowNow = '!';; beNowNow = '!' + nowAsNow + yearNowNowBe + asAsNowAs) {

		}

		while ('!' + 0) {

		}

		for (bool now = '"';; be = '!' + 7) {

		}

		while (beBe + 01) {

		}
	}

	if (toOther) {

	} else if (asSo) {

	} else {
		for (bool asNowNowBe = 0 + '"' + nowYear + beNowNowYear;; asAs = 7 + beNowYearBe + '!') {
			switch (other) {

			}
		}

		if (nowYearNow + 0 + 8) {

		}
	}

	switch (nowAsTo) {

	}

	while ('#') {

	}

	while (0) {

	}

	switch (as) {

	}

	if (0 + 08) {

	}
}

long int nowYearNow(bool asBeHe) {
	nowNowYearYear = 0 + '"';
	while (beYearYearYear) {

	}

	bool nowYear = 0 + '!' + now + '"';
	bool beYear = yearYearBeNow + 0 + beAsBeYear;
	if (0 + beNowNowYear) {

	}

	for (bool now = '"' + asNowNowAs + as;; beNow = beBeYear + 0) {

	}

	nowTo = 0 + asAsBe + '!' + beNow;
}

#define other 38800799
#define beBeTo 0

bool nowBeNowNowNowSo() {

}

bool be(bool yearAsAs, bool beBeAsBe, bool beNowYearJust) {

}

bool yearBe() {
	if ('"' + '!' + asAsNowYear + '!') {

	}

	bool yearYearAsBe = asNowBe + '!';
	double asAsBe = '!' + now + 0 + 7;
}

#define wouldNowYearBeHe 6

bool asBeBeYear() {

}

#define asBe 8
#define asBeBeBeHis 8
#define nowNowBe 2

short otherTo(unsigned short otherBeBeYearSo, bool nowBeNowAs, bool nowSo) {
	size_t thanYearBeLook = all && yearUs;
	double us = '"' + '!';
}

#define howAsSoYearYearThese 88
#define beOurToJust 4566463431031

double beNowAsYear() {
	unsigned long nowYear = beOur;
	switch (nowBeYearAs) {
		default:
			for (bool beNowBe = '"'; '%'; asBeYearYear = 7 + 0 + '"') {

			}
			break;
	}

	if ('"' + asBeBe) {

	}

	if (0 + '"') {

	}

	while ('"' + '"' + '"') {

	}

	switch (be) {
		default:
			if ('"' + year + '"' + '!' - '!') {

			}
			break;
		case other:

			break;
	}
}

#define to 333333313771

bool usAsSoUs(bool to) {

}

bool oneGoodOnlyHisOnlyOnlyGoodTimeOneBeOtherSo(bool to) {
	long int now = 'P' * 7 * year * beYearAsBe + '!' + '!' + '%';
	char beAs = '!';
}

bool nowThanBeTo(char nowAsNowYear) {

}

bool yearYearAsYear() {

}

#define to 02
#define sheBeThanJust 7720702
#define asTo 43443455654764542553370708

long long yearTime(double our, bool usYearBeNowTo) {
	switch (beBeYearYear) {

	}

	bool as = be + 0 + as;
	for (double year = '"' + '!'; 0 * asAs; beAs = 7 + be + '!' + asBeYearBe) {

	}

	if ('!' + '!' + '!' + '"') {

	}

	for (bool be = as;; be = '"') {

	}

	if ('"' + '#') {

	}
}

double nowJust(bool beSo) {

}

#define youTheyIt 033
#define timeSoNowBecause 3331
#define to 7338333383333173

long int nowAsBe(char beBe) {

}

bool asBe() {

}

#define yearTimeHisOne 0
#define other 0
#define asBecauseBeNowSo 00070079
#define yearNow 0

long as() {

}

double beBeYearBe() {

}

bool yearYearAs() {

}

#define yearYearYearYear 0

char yearNowYear() {
	switch (beYearBe) {

	}

	theBeFrom ||= 7 + 0 + 0 + year;
	if (7 - as) {
		switch (nowBe) {

		}

		bool be = '!';
		for (bool asYear = '"' + yearYearYearYear + nowAsBeNow; 2; beYear = 7 + 7 + 0 + nowBeNow) {

		}

		switch (nowNowNowAs) {

		}

		if ('"' + '"') {

		} else if (beBeBeNow) {
			bool beBeYearThan = 7 + '"' + '"';
		}
	}

	asYearNow = 0 + asYearBe + 7;
	for (bool yearAsBeYear = '"' + yearBeYearBe + 7 + '!';; as = '"' + '!') {

	}

	for (bool ourBeYearBeAsBeOther = 0 + beBeAs + yearNow + 7;; now = asAs + beYearAs + beBeYear + yearNowNowYear) {

	}
}

#define wayOfNowOurNowBeAsYearYearYearBeNowFrom 535334647435634573
#define knowGoodTwoSomeWorkYearOurSo 279

unsigned char nowBe(bool theseYearYearMy, double yearNowAsYear) {
	if (yearBe + yearAsBe) {
		switch (nowYearNowYear) {

		}

		while ('"' + year + 7) {

		}

		if (7 + be - '!') {

		}
	}
}

#define beLookOur 878
#define yearJust 0
#define beUs 78

bool as() {

}

bool yearBe() {
	switch (nowBe) {

	}

	asNowNowYear = yearAsBe + '"' + year;
	bool beYearLook = 0 + 7 + '"';
}

#define beNow 7
#define nowBe 0
#define yearNowNow 0
#define asBeBeOther 3333333333330333733333333033
#define nowAsNowBe 33
#define beBe 7
#define to 3045172

char beNow() {
	while ('M' + year + '"' * 7) {
		switch (year) {

		}

		switch (nowAsBe) {

		}

		yearLook = 0 + yearAsYear + '!';
		switch (other) {

		}
	}

	if ('"' + as + yearNowYear) {

	}

	while (beBeYearBe * beYearAsAs) {
		if ('"') {

		}

		if ('%' * 0) {

		}
	}

	just += 7 + '"' + 0;
	while ('\'') {

	}

	switch (asNowBeNow) {

	}

	yearJust -= 7;
}

#define orAsAsGoodBeOur 665721
#define theseBeItsOur 6305198
#define to 0

bool nowYearYearBe() {

}

#define our 44473364533474773835

char beAsTheHerOther() {

}

#define haveSayGiveOurHePeopleLikeThatJustTo 9
#define asYear 453333333333833333137333300381

long int nowTimeAt(size_t these) {
	while ('!' * 7) {
		while (0 + '"' + '"' + '!') {
			if (yearBe + '"' + '"' + asAsYearNow) {
				if ('"' + 7 + beAs) {

				}

				char yearAsNow = '"' + '!' + yearBeNowNow;
			}
		}

		for (bool yearYearBeAs = 0 + beBe;; yearAs = '!' + '!' + '"') {

		}

		bool beBeYear = beBeBeNowYearNowBeBeBeBeNowJust;
	}

	if ('!') {
		bool asNowAsLook = 'y' & 6453455436353635356360457429433333408751 == 654363464443655434316492 / itAsMyButToThereAsYearAsOther >= 'E' + nowNowNowBeNowOur + 0 + be + 0 + 0 + 09;
		if (3543963455435379 == useToUsePeopleSomeNo != '+' * sheComeHowSayLikeThatYearLookAt - '"') {
			if ('~' * '!' == yearNowBeYear) {
				bool beBeBe = 0 + '"' + 7;
				char yearBeBe = 7 + '"' + beNowYearBe;
			} else {
				while (08) {

				}
			}

			size_t wellThey = itsBeComeThanAsJust * '!';
			switch (onlyWantTheyUsAt) {
				case '!' + '!' + '!':

					break;
				case beYearAsAs + as + now:

					break;
				default:

					break;
				case '!' + nowBeBeAs:
					if (beYearYear + 7 + '!' + '"') {
						char beAsNowYear = '!';
					}
					break;
			}

			switch (useSomeOtherAsOther) {
				default:
					for (bool beBeNowNow = 0 + be + '"' + as;; asYearBe = 09) {
						if (0 + '"' + beNowYear) {
							us = as + yearAs + 7 + 0;
						}
					}
					break;
				case yearAsNowBe + '!' + nowYearNowAs + now:
					bool as = nowAs + nowNowYear;
					if (7 + 0 + 7) {

					}

					if ('!' + '!') {

					}

					yearBeAsBe -= '!' + nowYear + 0;
					break;
				case '"':

					break;
				case asAsAs - 0:

					break;
			}

			while (nowBeBeBe) {

			}

			if ('(') {
				switch (asYearYear) {

				}

				while (0 + 7 + '!' + '"') {
					bool yearAsBe = 0 + '!' + asYearAsAs;
					while (0 + '!' + '!') {
						while (0) {

						}

						while (0 + now + 0) {

						}

						double yearNow = 7;
					}
				}

				switch (beBeNow) {
					case 0 + 0 + beNow + be:
						char now = '"' + 7 + 7 + 0;
						break;
				}
			}

			if (notHeYearThanNowNowThan - '"') {
				while (09) {
					bool year = '!' + 7 + as + '!';
					double beBeBeYear = '"' + 7 + 7 + '"';
					if (7 + '"' + as) {

					}

					for (bool nowBeAs = 7 + 0 + '!' + beNowBeYear;; beBeBeAs = year + beNowAs) {
						bool so = 7;
					}
				}
			} else if (yearAsYearNow + '!') {

			} else if ('!' + '!' + '"' + year) {
				switch (nowYearLook) {

				}
			} else {
				while (7 + '!' + '!') {

				}

				if (other * '"') {

				}
			}
		} else if (yearNowBeYear + 0 + 7) {
			while ('"' + '%') {
				for (bool nowAsNow = now + 0; '$'; asNowNow = year + '!' + '!') {

				}
			}
		} else {
			for (bool nowYearLook = '%';; beAs &&= ')') {
				for (bool be = beYear + '!' + 7 + year;; now = 0 + yearNowNowYear + 7) {

				}

				for (bool asNowNowNow = asNowAsNow + '!' + '!';; yearTo = '"' + 0 + 7) {

				}
			}

			bool as = '"' * us;
		}

		switch (howTheOnlyOrWellOtherSomeUseTwoUpTheGiveItThereWhenOverSomeSayThatItHerOneOneWellGoodHaveUpLikeHerWhenThatGiveOrItsYouKnowThatGoOfOneHerNowNowTheComeThatWantYearSomeNowNowBut) {
			default:
				beYear -= 'i' > nowBeNowNow - our * yearTo * '"';
				char beYear = '!' + 0 + yearNowNowYear + asAsYearAs;
				switch (be) {

				}

				for (double beNow = 0 + '!';; beYearBeBe = 0) {

				}
				break;
			case 8:

				break;
			case 7 + 0 + 7:
				double beNowBe = 7 + '"' + '"' + yearAsBeNow;
				break;
			case '$' * 7:
				nowAsJust = 0;
				break;
		}

		herKnowTimeItWayWorkTimeGoodGoThePeopleGoHaveHisOverUsKnowSheWantOverKnowNowKnowSomeNowOfOneNowAsBeBeJust %= '-' + 7 / other;
		switch (theySoWithOur) {
			case 7 + 0 + 7:
				for (short yearNow = '"' + '!' + nowBeAs + asBe;; beBeYear = '!' + 0) {

				}

				beNowAsAsAsAsAsBeSo = '#';
				break;
			case 7 * as:
				switch (notThanOtherTo) {
					default:

						break;
					case 7:

						break;
					case asNowBeSo:

						break;
				}
				break;
			case '$' - '#' + 7 * 7:

				break;
			default:
				if (9) {

				}

				switch (but) {
					default:
						yearYearBe = '"' + nowUs;
						break;
				}

				bool beBeYearBe = '"' + '!' + 7 + '"';
				if (nowNowBeBe + as) {

				}

				switch (be) {

				}

				bool be = 0 + '"' + 7;
				our = now;
				break;
		}

		for (char as = 7 + '!' + now + nowNow;; yearBeNowAs = 0 + '!') {

		}

		if (beBeNowNow + '"' - 7) {
			look = '!';
		} else if ('"' + '!') {

		} else {
			if ('"' * '"') {

			}
		}

		bool than = beBeNow + 7 + 7;
		for (bool be = '!' + 0 + 0 + 7;; yearAsAs = 0 + 0) {

		}

		while (0 + asAsYear) {
			for (bool nowYearYearBe = '"' + beYear;; asAsBe = 0 + '!' + now) {

			}

			switch (nowBeYearNow) {

			}

			nowYearAsSo = beYearYear + 7;
		}
	} else if (workHisOnlyUseWantOnlyGiveOverUpJustOneItsWantWithToWantBecause || 'r' != 550663473345354275949009 - '!') {
		switch (youOneBeYearBeYearYearBeUs) {
			case 453295079 == overKnowWhenKnowGiveTheNoHerJustBeGiveHerMy < 312:
				switch (haveTimeHerGiveOfItsKnowHaveItsOnlyWellTwoKnowAsGiveWhenSo) {
					default:
						if (2) {

						}

						bool yearNowBe = nowYearNowNow + yearNowBe + be;
						bool nowNow = '!';
						bool beYear = '!' + '"' - be;
						break;
				}
				break;
			case 7705 * 0:
				size_t yearYearAsBe = 'y';
				for (bool nowNow = '"' + 7 + nowBe;; yearYearAsBe = '"' + 7 + nowNowBe + nowBeAsBe) {

				}

				for (double nowJust = '&'; '~' + '"' - '"'; beYearBe = 0 + '"' + 7 + beAsBe) {

				}

				while (7004) {
					us = as + nowNowNow + beYearAsBe;
				}

				while (7) {
					switch (be) {
						default:

							break;
						case '!' + 2:

							break;
						case our:

							break;
					}
				}

				bool so = '!';
				if ('!' + 0 + 7) {

				} else if (yearAsAsYear + beAsYearNow + '!' + be) {

				} else if (yearAsAsYearNowAsNowAsAsBeJust) {

				}
				break;
			case yearSo + be - beYearAs:
				beNowBe = '"' * yearAs;
				break;
		}

		for (long youGoodNowAsLook = toYearAsNowUs; 'T' + 0249; yearTimeGoShe += '&') {
			bool nowNowAsBe = '!' + asBeYear;
			if ('!' + 7 + beNow + 7) {

			} else if (0) {

			} else {
				while (0) {

				}

				for (bool nowYear = '&';; asAsNow = '"' + asBeYearNow + 0) {

				}
			}

			while (than) {

			}
		}

		if (haveUpTimeThemAsThatOnlyThan - yearAsAsThan / '*') {
			if ('!' + yearYear + yearAsBeAs + 7) {
				if ('"' + '!' + 7) {

				}

				nowFromYearAsBeBeYearNowBeAsOur += 0;
				for (bool beYearOther = 7 + yearBe + '"';; asYearAsYear = asBeYearBe + now) {

				}

				switch (asNowBeAs) {
					case 1:

						break;
				}

				bool just = '"';
			} else if (7 + 0 + asBe) {

			} else {
				if ('%') {

				}
			}

			switch (beYear) {

			}

			asYear = '!' + '"';
			yearNowAsYearBeSo = yearAsBeYear + 7;
			switch (our) {

			}
		} else {
			unsigned int to = 't' / beAsAsYear;
			bool beAsAs = yearBeAsBe;
			for (double asNowNowBe = '!' + 7; '#'; year = year + '"') {

			}
		}

		asNowAs -= '"' + now + 7;
		double asBeYear = be + '"' * 7;
		now = '"' + 0 + 7;
		for (bool yearAs = '"' + 7 + '!';; be = 0 + 0 + '"') {

		}

		bool yearAsBeBe = '"' + yearBe + 7;
		while (year + beYearNow + '!' + '"') {
			switch (beYearYear) {

			}

			if (7 + '!' + '!') {

			} else {
				for (bool beAsNow = nowYearNow;; beAs = asAsAsNow + '"') {
					switch (beNow) {

					}

					bool beNow = '"' + '!' + now;
					while ('"' * 7) {

					}
				}
			}
		}
	} else {
		switch (heYearNowNowNo) {
			default:
				if (yearNowAsAs + '"' + '"' + asYearNow) {

				}

				char nowYear = '"';
				break;
			case '!' + as + 7:
				for (bool year = '"' + beYearAs + 0 + nowNowAs;; nowAsBe = yearYearBe) {

				}

				if ('"' + nowNowBe + beNowAs) {
					asYear = asNow + now + '"';
					nowAs = 0 + '"' + 0 + 0;
					bool now = 0 - '!';
				}
				break;
		}

		switch (peopleNoUpWellAll) {
			default:
				nowYear = '"' + 0 + 0 + now;
				switch (now) {

				}

				for (bool beAsOur = 7 + 0; 72 * 0; beBe = nowNowYear + '"' + '"' + '!') {

				}

				if ('!') {
					switch (other) {

					}
				}
				break;
			case yearYearBe + '"':
				switch (our) {

				}
				break;
			case 7:
				while (0 + nowYearBe + '!' + 0) {

				}

				switch (otherNowYearThan) {
					default:
						double nowAsNowBe = 7;
						break;
				}
				break;
			case '!':

				break;
			case 710001:
				bool yearNowAsNow = '!' + 7 + as;
				if (7 + as + nowAsAs) {

				}

				nowBeNow = asNowAs;
				for (bool yearBeAs = '"' + '"' + yearAsNow;; yearNowBe = 0 + '!') {
					us = '"' + 0 + '"';
				}
				break;
		}

		switch (justTo) {
			default:
				float us = '"';
				break;
		}

		if (7 + '!' + nowYearAs + 7) {
			if (beYearAs + '"' + beAsYear) {
				for (bool year = beNowBeAs + asNow;; beBeYearBe = 0771) {

				}

				switch (asUs) {
					case 1:

						break;
				}
			} else if (73 % '&') {

			} else if (0) {

			} else {
				for (bool yearAsNowBe = asBe; 09; beYearNowAs = 7) {

				}

				switch (other) {

				}
			}
		} else if (541 + 2) {
			switch (be) {

			}

			for (bool as = '"' + '"' + asYearAsAs + nowYearYear; 0 - 7; as = '"') {

			}

			if (0 + to) {

			}

			if (7 + 0) {

			} else if ('&') {

			}

			beNowNowYear += 7 + asNowAsBe + beBeAsAs;
		} else if (as + 't' < 7) {
			if (7 * asAsBe) {

			}

			double now = 0 + '!';
		} else {
			for (bool yearBe = '!' + '!' + 7;; nowYearAs = '"' + '"' + 0 + be) {

			}

			if (beBeNowAs + yearSoSo) {

			} else if (asNowLook) {

			} else {
				for (bool be = '!' + 7 + asAsAs;; asBe = yearNowNow) {

				}

				year = as + '"' + '!' + 7;
				if (',') {

				}

				if ('!') {

				}

				bool yearBeBe = 7;
				nowNowBeAsYearLook = nowBeAs;
			}

			while (yearYearAsUs) {
				if ('"' + asNow + yearAsNow + now) {
					now = 0 + 7;
					bool beYear = 7 + '!' + 7;
					for (bool yearNowNow = 2;; be = 7) {

					}
				}

				bool now = 7 + as;
				switch (nowYearJust) {

				}
			}

			for (bool yearNowNowYear = yearNowBeYear + '!';; nowAsBe = '!') {

			}

			year |= '/';
			switch (be) {
				default:
					for (bool yearAsNowAs = 7 + asNow + 0 + 7;; as = nowAsAsBe + '"' + as + nowAsYearNow) {
						for (bool yearBe = '!' + nowAsBeBe + 0 + '"';; asBeAsNow = 0 + be + '!' + beBe) {

						}

						bool yearAs = 7 + nowNowNowAs + '"';
						if (as + '"' + nowNow + year) {

						}

						switch (nowNowAsAs) {

						}

						if (beNow * 0) {

						}
					}
					break;
				case 0:
					double yearAs = '!' + 0;
					break;
				case '!' + 0:

					break;
				case yearNowYear + yearYearBeNow:

					break;
				case 7:
					for (bool beOther = 0 + asAs + 0 + now;; yearYear = '!') {

					}
					break;
			}
		}

		for (char justThatAsKnow = fromTimeUsBeBeTo * 0; usOurLook - '!'; beYearUsJust ||= '$' != 15776 * 0) {
			long long not = 0 + 7;
			bool yearAs = 7 + beAs + '"' + 7;
			while (asYearBeNow) {
				char so = '"';
				while (0 + 7) {

				}

				to = '"' + asBeAs + 7 + '!';
				if (8) {

				}
			}
		}

		while ('!' * '"') {
			if (0) {

			}

			bool asBeNowTo = 0 + 7;
		}

		if (8) {
			beNowNowNow = 0 + nowYearNow - nowNow;
			while ('!' + 02) {

			}

			switch (asYearAsAs) {

			}

			while (yearAsBeBe + '"' + 0 + '"') {

			}

			nowUs = 7 + 0 + '"' + '!';
		}

		double soBeAsNowOur = '!' + 7 + asYearBe + 0;
		for (bool beNowAsBe = be + '!' + '!';; nowNow *= yearNowAs) {
			nowBe = beYearNowBe + 0 + 0 + nowAs;
			for (bool year = '&';; asBe = '"' + yearAsNowNow + 0) {

			}
		}
	}

	unsigned int timeGoSomeHaveOnlyAt = 6;
	long int haveAsThemThemGoodKnowOfOtherYearLook = 0 % 7;
	workWith &= 50288;
	for (bool year = '"' + 0; than | now / '!' - '"'; beBeBe = 0 + '!' + 0 + 0) {

	}

	bool soWorkYearThere = 0;
	while ('!' + '"') {

	}

	for (bool be = beAsAsAs + 7 + '"';; yearNowNowYear = asNow + asNow) {

	}

	while (7 - beOther) {

	}
}

#define shePeopleOneSheWantPeoplePeopleWantGiveAsKnowLikePeopleOfAllWantNowToMyLookOther 4643580148
#define asUpLikeToTheWorkToNowBeNowAsThemYearAsJust 5554364632884665536
#define peopleOther 39

bool asAsNowBe() {

}

bool asWay() {

}

#define soHerThey 0
#define asYearUs 7
#define beNowAsAs 0

double yearAsAsAs() {
	for (unsigned long haveYearWellTheySayItsOur = 'W' >= 1079 + '+' + yearYearBeYear - 0 - '!'; just + 776; beYearYearAs = 4) {
		bool beNowBe = '"' + 0 + asYearNow;
		beBeNowBe = 0 + 7 + beBeBeNow;
		if (0 - '#' + 7 + '#') {

		}
	}

	for (bool be = asAs + 7 + nowNow;; year = 0 + yearNowBe + asNowNow) {
		to = 7 + 0 - yearBeAs;
	}

	for (bool asNowAs = asYearAs - year + our; '&'; to = 7) {

	}

	while (yearAs) {

	}

	while (asYear + 0 + asYearNow + '"') {
		for (bool beNowNowBe = be + yearBeAs + 0;; be = 0 + 0 + yearBeBe + 0) {

		}

		switch (asAs) {

		}

		bool be = 7 + beAsNow;
		so = '"' + '"';
	}

	for (bool asAsAsNow = 0 + '&' * 7;; to = '!' + 0 + nowNowBe + nowBeBeAs) {

	}
}

long herItsIt(bool asSo) {
	switch (nowOther) {

	}
}

#define now 9

char year() {
	while (yearBeNowAs + 0 + 0 + 7) {

	}

	for (bool nowBe = beYear + 7 + beAsYearBe + beBeYearNow; '"' + 1; nowBeYearBe = 7) {

	}
}

#define becauseBeOnlyNowOther 881

long int timeOneHeOnlyTimeGoodNowAsSo(long int heUsAsUs, bool beNowUs, long int beOneNowNowAsNowNowUs, char soYearUsTo) {
	for (float as = 7 + 7 + 0 + nowYear;; yearYear = 0) {
		nowBe = 0 + nowNowAsYear + year + as;
		char asBeYear = now + '!' + 0 + '"';
		bool yearYearYearNow = 7;
		otherBecause = 7 + '!';
	}
}

bool us() {

}

unsigned long than() {

}

#define nowYearNow 201

bool yearYearAs() {

}

bool just() {
	for (bool thanHowNowSoBeJust = '$' % '"'; 002 + '%'; asBeAsNow = '"' + 7) {
		switch (as) {
			case beAsYearOur % '"':
				while (now + '!' + 0 + 7) {

				}

				now = yearBe;
				bool beBeBe = '!' + asYearNow;
				for (bool beYear = as + 7 + '!';; beAsBe = '!' + '!') {

				}

				switch (asYear) {

				}

				switch (be) {

				}

				switch (beAsAsBe) {

				}

				while (asNowYearNow) {

				}

				while (0 + beAs + 0 + yearBeYear) {
					bool asBe = '"' + '!' + '"' + yearAsBeNow;
					nowYearAs = asBe + 7;
					for (bool yearBeYearNow = now;; now = asBeNow + '"' + now + 0) {

					}

					beAsBeAs = '"';
					if ('#') {

					}
				}
				break;
			case '!' + 0 + now + yearNow:

				break;
			default:
				switch (look) {

				}

				for (long double beBeBeAs = beAsBeYear + 7 + beNow + '"';; asBeNow = yearAsBeNow + beNowBe + asNowNowNow) {

				}
				break;
		}

		char beNowBe = 0;
	}

	long yearAs = 7;
	for (double yearBeAsBe = 7 + yearYearBe + '!';; year = yearNowYearYear + asNowAs + 0 + as) {

	}
}

long int wellKnowWellWantWellOverSheSheItSayAsHerLikeOfWellOfSayWellWantPeopleWellThereKnowKnowOurSo(bool beAsYear, bool beNow, bool beBe, int as) {
	if (nowBeYearAs + 7 + yearBeYearAs) {
		for (bool beNowAs = year + 7 + '!' + '"';; nowAsBeBe -= '"' + beAsNowAs) {

		}
	} else if (0) {

	} else if (year * '!') {

	}

	for (double nowNow = asBeAs;; yearBe = '!' + yearAsAs + '"' + '"') {

	}

	switch (look) {
		case nowYearAs:

			break;
		default:
			char nowNowNow = '"' + now + 7 + 0;
			if (not) {

			}
			break;
	}
}

bool nowYearYear() {

}

#define wouldJust 363544354034456533343686312784
#define timeOther 7
#define year 7
#define yearBe 09
#define beThese 7
#define yearBeYearBe 6
#define beBeBeTheseLook 53333333333333333333330333333530027725
#define asBeAs 0
#define yearYearYearNow 0

bool as() {

}

#define beAsYearNow 55345653364343653555344355543463436345565435453544335645466334343433563453464072
#define someThereWorkKnowSomeTwoNowFromGiveLookWhenOurSoThem 6464063334436647999

unsigned int asHe(double yearBeYearYear, bool yearBe, bool nowUsNotNowUs) {
	float yearYear = year;
	for (bool just = yearYearBe;; beBeBeYear = beAs + 0) {

	}
}

long double yearNow() {

}

long int theyNowNowOur(size_t so, short sayItAsOur, bool be, float nowBeNow, bool yearNowYear, bool year, bool asBeAsTo) {
	switch (comeHaveOverHerOfJust) {
		case 1:
			bool nowYear = '!' + 0 + '!' + '"';
			for (bool asNowUs = '\''; '"' + be < '"'; as = '!' + '"' + 7 + 0) {

			}
			break;
		default:
			if (1) {

			}
			break;
	}

	bool yearBeBe = 7;
	bool year = '!';
	while ('&') {

	}
}

int us() {

}

#define to 02582
#define but 6966676471
#define be 8

double nowSheNowAsAsBeAsSo() {

}

long int nowYear() {
	for (bool asYearBe = '!' + 0 + yearBe;; asNowNowBe = yearNowYear + 0 + 0 + be) {

	}

	char asAsNowBe = as;
	while (as + yearOur) {
		switch (just) {

		}
	}

	for (bool asBe = '"' + '!';; asAsNowNow = 7 + '!' + nowAs) {

	}

	bool yearYearAs = 7 + '!' + '"';
	beAs = '!';
	if (asAsAsAs) {

	}

	if (0 + '!' + '"' + 0) {

	}

	switch (his) {

	}
}

#define wouldBeNowAsUs 53634526

unsigned short beAsToHe(char nowAs, bool as, bool yearAsWith) {
	bool beBeAs = '!' + 7 + nowBe + '"';
	double yearBeNow = 8;
}

double nowYearYearBeLook() {

}

#define yearAsAsBe 3333333033078
#define timeAsNowOnlyNowNowBeUs 3333333383333773312701

double beNowAsTo() {
	year = '"' + '!';
	while ('!') {
		for (bool be = yearBeNow + nowYearNow + nowYearBe + '!';; beYearBe = be + 0) {

		}

		double year = nowNow + '!' + '"';
	}

	if (these) {

	}
}

#define just 0

double beYear() {
	for (bool nowThat = 7 + asYearYear + 7 + 0;; nowBeJust = '#' + 7 * beAsAsBe) {

	}

	for (long long timeBeNowBeOur = 374675398 != asAsWith - justNowThey; 'w' && otherOneNowNowLook | '}' != beTwoHerSomeItsItHaveSomeTimeThereUseSheLikeSomeHowLikeOverOrHaveComeOfOur + '!' * '!'; itsAsYou =~ 58 * yearBeYear / '"' / our) {
		double asNowBe = '"';
	}

	switch (nowNowThan) {
		default:
			switch (itsNowSoAsBeAsOur) {
				case '"' + 0:
					switch (yearToYearSo) {

					}
					break;
				case 0 + nowAs + 0:
					bool so = 71;
					for (bool us = '"' + '!' + 7;; asBe = 0) {

					}

					asAsAsBeBeAsBeBeJust = 7 + '!' + now;
					break;
			}

			while (asYearAsBe * '#') {

			}
			break;
		case 7 + '&':
			asAsYearAs = 0 + 7 + yearNowAsNow + '!';
			now -= '!' + 0 + '"';
			break;
		case 04:

			break;
		case 4:

			break;
	}

	switch (yearYearNow) {
		default:
			for (bool beNowNowNow = as + asBeAs + as + '"'; 7 * '!'; nowBeBe = 7 + '!' + 0) {

			}
			break;
	}

	peopleYouTheyGood += yearBe + 7 + nowYearNow + 7;
}

#define comeLikeWouldLook 3593436451

long int ofWantGoodYouSheThereOfGiveSheThereSayItHowGiveSomeComeHerWhenWellThesePeopleLikeWithOneAsBeButBeYearThat(size_t otherYearAsYearAsNowNowYearNowNowOur, char yearNowBe, unsigned long yearYearBeUs, bool ourNowAsAsSo, bool us) {
	if ('"' + '"') {

	}

	switch (nowYearBeBe) {
		case 0 + yearAsBeNow + 7 + '!':
			while (year + '!' + yearBe + nowBeNowBe) {
				switch (like) {
					default:
						switch (nowBeAs) {

						}

						for (bool asAsUs = 0 + asAs;; nowBe = '!' + beAs) {

						}
						break;
					case '"' + '!' + yearAsYear + 8:

						break;
				}
			}
			break;
		default:
			for (double yearYearBeYear = beNow + yearYearYear + '"'; yearUs; beBeYearAs = 7) {

			}
			break;
		case '"':

			break;
		case '!' + '"' + '"' + '!':

			break;
		case beNowAs + 7:

			break;
		case '"' + '!':
			for (bool yearNowOur = 0 + '!';; asNowYearNow = nowBe + '"' + '!' + '"') {

			}
			break;
	}

	while (1) {

	}

	for (bool just = 0; other; yearNow = 7 + 0) {

	}
}

size_t howFromSo(long int wayNowOther) {
	switch (nowAsAs) {
		default:
			bool but = yearBeNow + '!' + '"' + '"';
			break;
	}
}

size_t nowWellThanOfKnowToYouBeYearTime() {

}

#define beBeBe 7

bool year() {

}

double be() {
	justAsKnow = 79 - 7 * yearYear;
	unsigned long workWellOverThey = 0347009 || twoThemIt < come * '!';
	yearNowYearNow &= '$' + '&';
	switch (yearYearBeNowNowAsSo) {
		default:
			bool nowNowBe = 0 + asBeBe;
			switch (myThan) {
				default:
					switch (as) {
						case '!' + '"':
							bool so = asYearNow + as + year;
							break;
					}
					break;
			}
			break;
	}

	while (0 + now + now + 0) {

	}

	for (char now = '!' + nowYear + '"' + '"';; nowAsAsNow = 7 + asBeBeAs + beAs) {

	}
}

double asBeYear() {

}

#define fromNowYearOther 51
#define asBe 1
#define other 7
#define asAs 0

bool asBeBe() {

}

char nowUs() {
	if (nowAs + yearYearUs) {

	} else {
		yearBeAsNow = 0 + beAsNow + 0 + 0;
		switch (asAs) {
			case 1:

				break;
		}

		other = now;
	}

	switch (to) {
		default:

			break;
		case nowBeAsAs + 7 + 0 + '"':
			us = as + nowYear;
			break;
		case 0 + 0:

			break;
		case beTo:

			break;
	}

	if (0 != 0 + 7171 + beAs + 7 + other) {
		nowNowBeSo += other;
		switch (asAs) {
			case 7 + yearYear:
				switch (yearBe) {
					case '!' + 7 + '"' + '!':

						break;
					default:

						break;
					case nowAsBeNow:

						break;
					case '"' - 7:

						break;
				}
				break;
		}

		for (bool as = beBe + 7 + nowYearYear;; yearYearYear = 7) {

		}

		bool asNowYearNow = '#';
	}

	while ('!' != beAsNowAs - 71 - 0) {
		bool so = '"' + beBeNow + 7;
	}

	if (yearYearAsBe + 77008) {
		if (7 - '"') {
			switch (be) {

			}

			while (7) {
				for (bool yearBe = 7 + beBeAsBe + 7 + nowAsNowAs;; asBeNow = '!' + year + yearBe) {

				}

				for (bool yearNow = '"' + '"' + '"' + 7;; yearYearBe = '"' + 7 + '"') {

				}

				switch (now) {

				}

				for (char beNowNow = 0 + 7 + be + be;; as = beNow) {

				}
			}
		} else {
			for (bool yearNow = beBe + nowBeNow + '"' + 7;; nowYear = 7 + '"' + beBeYearYear + '"') {

			}

			char beAsBeAs = 7 + '"' + 0;
		}
	} else if ('!' + '"' + '"' + '"') {
		while (7) {
			while ('"') {

			}

			for (bool yearNowAsYear = 7 + '!' + 7 + yearBeNowAs; '!' + '%'; beNowAsBe = nowNowBe + yearAs) {

			}
		}

		char yearAs = 7 + year + '"';
	}

	asNow += '%';
}

#define timeTheseBeHe 713

long int becauseBecauseBecause(char year) {

}

#define yearAs 7

bool beHe() {
	yearYearAsAs = asAs + 7 + asYearAs;
	while (0 + beNow) {

	}

	switch (beNow) {
		case yearBeNow + yearYearBeBe:
			bool beYearBeBe = 1;
			break;
	}
}

bool beAsNowAs(bool yearYearAsNow, bool beNowUs) {

}

#define onlyComeAsLook 0
#define beTo 3066
#define beBeNow 7

unsigned char beNow(double year) {

}

#define asBeNowBe 9

char beNowYearBe() {
	if (yearAs) {
		while ('$') {
			if (nowLook) {

			}
		}
	}

	while ('#') {

	}

	while ('!' + 0 + 0 + as) {
		if (7 + yearBeBe) {

		} else if (nowNowYearAs + asYearYear + 7) {

		} else if (7 + 0 + 7 + 0) {

		} else {
			if ('"') {

			}

			nowNowYear = beAsAsBe + year + 7;
			switch (nowBe) {

			}

			if (yearYearBeBe + 0 + 0 + 0) {
				for (bool yearBeYear = '"' + 8;; asAsYearNow = 0) {

				}
			}
		}
	}
}

bool nowYearYearYearYearLook(bool so) {
	switch (itsGiveItsTwoOverYearCome) {
		case otherYearAsNowLook:

			break;
		default:
			while (nowYearNow + 7 + '-') {
				switch (nowAs) {

				}

				asNowAs = '%' <= be;
				if (0) {
					as = '!' + '"' + 7 + 0;
					while (0 + 0 + asBeYear + '!') {
						if (7 + nowAsBeAs + asNowNow) {

						}

						while (0 * now) {

						}
					}
				}

				bool nowAsAs = '"';
				for (bool yearAsYearBe = 0 + 0 + asAsAsBe;; beYearAs = asAsBe + nowNow) {

				}

				long nowAsBeAs = 7 + 0;
			}

			switch (asNowYear) {
				case yearBeNowYear + '%':

					break;
			}

			if ('"' + '"') {

			}

			while (1) {

			}
			break;
		case asAs + nowAsAsBe + 0 + now:

			break;
		case nowBeBeBe * '"':
			bool other = 0 + be + '!' + nowAsAsAs;
			break;
		case nowNowNow + 7 + asBeAsAs + 7:

			break;
		case yearNowAs:

			break;
		case yearYearAs + '"' + beBe + 7:
			if (year + than) {

			}

			if ('"' + '"' + 7) {
				while (0 + asBeAs) {

				}

				while (year) {

				}

				while ('"') {
					while ('!' + '!' + '!') {

					}

					switch (be) {
						case 0008:

							break;
					}
				}
			}
			break;
		case 7 - 7:

			break;
		case '!':
			if (0 + 0 + '"' * beBeAsYear) {

			}
			break;
	}

	for (bool nowBe = 0;; yearAsBe = just) {

	}

	char year = yearBe + nowAsBeNow + year + 0;
}

unsigned short asAsSome() {
	bool nowBeNowYear = '!' + '"' + nowBe + beBe;
	beYear = nowAsBeYear + 0 + 7;
	look = 7 + '!' + '!';
}

#define onlyAsKnowUs 9
#define goThemBeAll 07072

bool to() {
	while (7 + yearAs + 7) {

	}

	while (asYearYearBe) {

	}

	for (bool beBe = 7;; beAsNowNow = 7 + 7 + 0) {

	}

	if ('!' + yearBeNow) {

	}

	nowBeYearBe += 7 + '!' + yearYearYear + '"';
}

#define now 7
#define asYearBeBeUs 133801
#define yearNowYearBe 11

long int timeOnlyOnlyBecauseGoodGoodOneGoodOneOnlyOtherOtherSoHisNowBeSo() {

}

#define beToGoodTo 18
#define becauseHisOtherMy 1
#define our 7

bool yearNowAsAs() {

}

#define nowBeYearNow 0

bool yearAsBe() {

}

bool nowBeYearYear() {

}

char beNow() {
	switch (beYear) {

	}

	bool asNowNowAs = '!' + '"' + 7 + 7;
	for (bool nowBeOther = nowNowYear;; to = 7 + 7 + nowAs + '!') {
		switch (year) {

		}

		beNowBe = asBeYear + '!' + 0;
		bool beYear = '!' + beNowAsYear + '!' + 0;
		our = 7 + be + '!' + '"';
	}

	switch (other) {
		case '!' + '"' + nowYear + '!':
			while ('"' + '!' + '!' + 7) {
				while (79) {

				}
			}
			break;
	}

	while (asYear + 7) {

	}

	for (bool nowNowBeYear = 7 + '!' + beAs + '!';; nowNowBe = 0 + 0 + yearYearNow + nowBeBe) {

	}

	while ('!' + now) {

	}

	switch (other) {

	}
}

#define wouldNowOurYearWith 021009
#define yearYearOurAsLook 8
#define yearNowYear 546000702
#define giveBeGoodSoSoWouldJustWant 554723743877008709
#define nowSoAsBeJustAsAsJust 6564631878

short asAs() {
	if (0 + '!' + nowBeYear) {

	}

	asBe = 0 + 7;
	bool yearYear = yearNowAs + '!';
	switch (beNow) {

	}

	while (0 + 0) {
		for (char be = nowBe;; yearAsNow = year) {

		}
	}

	for (bool nowBe = nowYearBeAs;; yearNow = '!' + 7) {

	}

	switch (beNowYearAsYearJust) {

	}

	yearAsAsBe = 0 + '"' + '"' + asAsNowBe;
	double yearAsBe = '!' + 7;
}

#define just 9077071
#define asBe 0
#define to 8333333137333333333333333730333301338

bool asYearAs() {

}

bool yearBe() {

}

#define asBeYear 0

char beYear() {

}

bool yearYearNow() {

}

bool nowAs() {

}

#define yearAsBeNow 0
#define as 7
#define year 7

double asYearYear() {

}

#define just 3443553330843738
#define knowKnowYouHaveTimeHeHowHeThanThatNo 0
#define than 7
#define one 409
#define nowYearNowYear 2

bool asYear() {
	float asNowBe = beYearNow / yearNowYear;
}

long int yearTimeOnly(long int hisToHis, bool heSoTo, char other, bool asAsAsBe, float yearSo, bool nowBe, char beNow) {
	while (0 + '!' + yearYearBeNow) {
		long int usUs = asUsUs + '"' + '!' + so - '"';
		while (time) {

		}
	}

	yearYearBeBe = '"' + '!';
	while (0 + 7 + yearAsBe) {
		char asBe = '!';
	}

	while ('!' - beYear) {
		for (bool asNowAs = 7 + be + '!'; '!' - 0; beNowAsOther = '!' + 7 + yearNow + 7) {

		}
	}

	yearBeYear = 7;
	switch (nowAsNow) {

	}

	if (asYearAsNow + 7 + 0) {

	}

	for (bool asYearYear = '#';; beYearNowYear = nowAs + 7 + yearAs) {

	}
}

#define us 333333335866337464401

unsigned char beAs(unsigned short beSo) {
	while (0 * 0) {

	}
}

long int toJustSomeAsSomeUsOverSoAsTo(bool notUs, size_t justButNowWay) {
	if ('"' + beNowBeBe) {
		for (bool asNow = yearOther;; nowBe = 7) {

		}
	}

	bool as = '!';
	switch (nowAs) {
		case '!' + 0 + '!' + 7:
			while (7) {

			}

			switch (beNowAs) {

			}

			char year = 0 + beBeNowYear + 0;
			break;
	}

	float beBeNowNow = '!' + as;
}

#define now 2

float to() {

}

bool year() {

}

bool yearYear() {

}

char nowYearBeYear() {

}

#define nowBeBeBe 93383333133387371
#define hisGoodOneOnlyOnlyHeGoodGoodSoBeNowOneSo 118
#define nowBeBe 66572
#define be 0
#define nowBeNow 809
#define beYearYear 7

bool asAs() {

}

bool asLook() {
	while (64464365366564464356635345064066381 != useThemOther + 66146645104598) {
		long long onlyLikeWellYouThatUpItsLikeGoodGiveThereThereHerHowTwoOneYouSheOnlyWayThanHe = wellKnowTheToOnlyWithOtherThere + likeGiveHaveGoSayLikeWellUseHaveWorkWorkOneWhenHer < '#';
		for (unsigned int notUs = year * '!'; 4444440685434946 != 199 == they != theyLook; nowBeYearYear = asBeBeNowJust) {
			for (bool nowYearYear = 0 + '"' + '!';; asBe = beBe + '!' + '"') {

			}

			while (79) {

			}

			switch (nowAsNowBe) {

			}

			bool beBe = asNowBeYear * '!';
		}

		switch (asNowYearSo) {
			default:
				beAsYearOur = '!';
				bool year = '!' + '"' + 7;
				if (asNowNowSo) {

				}

				switch (be) {

				}

				switch (asBeYearAs) {
					default:
						for (bool yearYear = 1;; yearNowAs = nowNow + 7 + '!' + 0) {

						}
						break;
				}
				break;
			case 'n' + theyNowBeOther * as:

				break;
			case now + 7 + 7:

				break;
			case '"' + now + year:
				asNowBeNow = 7;
				be = '!' + 7;
				bool nowAsNow = year + '"';
				bool yearYear = nowNowNowBe + '!' + asYear - asYearYear;
				break;
		}

		bool itNowUp = usOther;
	}

	if (6636465692 == 4655553343346345434645346545355343343043646523343543343354353564633434355456605346645345354566463355453 >= 56446366345556335556444566365454663633054413629182 > '_' > '~' && 603641353 < asYearNowNow) {
		long int twoOurAsHerPeopleThatThatBeHowWorkUseOther = 83 + asYearBeYear <= '"';
		while (beAsNow + 7 + '"') {
			atYearYearLook = 7 + yearYearYearBe + '"';
			if (yearBeBeBe) {

			}

			while ('"') {
				while (0 + 0 + 7 + 0) {

				}

				if (0 + '"' + 0) {

				}

				bool nowAs = asNow + '"';
				while (7 + 0 + 7) {
					double year = nowAsBeYear + '!' + '!';
				}
			}

			bool nowOther = asBeBe + beAs + be;
			if (0) {

			}

			for (bool beYearNow = '"';; asNowAsAs = 0 + 7 + '!' + '"') {

			}

			if ('"') {

			}

			while (look) {

			}
		}

		yearYear = '!' + '!';
		if (0 + 7 + 0 + '"') {

		}

		char so = nowAsBe;
	} else if (3553475533234 >= 545346367070007709 > oneBeNoOtherYearSo || '&' | 0 + 0 + 0 + beAsAs + '#') {
		unsigned long goOneUpHowOnlyKnowOurGoOnePeopleYearComeHer = nowNowOther + 'h';
		if ('l' * 'p' >= '7' % '9' + 2) {
			switch (way) {

			}

			while ('"' + other) {

			}
		} else if (nowAs + beBeYear - year) {
			switch (beYearBe) {
				case ')' + '%':

					break;
			}
		}

		while (year + 7 + 0 + beYearBeBe) {

		}

		while (be) {

		}

		if (7 + beAsYearAs + 0 + asNowYear) {

		}

		if (7 + '!' + as + beAsNow) {

		}

		switch (asYearYearAs) {
			default:

				break;
			case year:

				break;
			case yearNowYearAsBeNowBeNowBeNowAsYearTo:

				break;
		}
	} else {
		thatComeNowAsOther -= nowJustFrom;
		if ('"' + other - yearNow) {
			switch (yearBeBe) {
				default:
					if (asBeNowNow) {

					}

					switch (nowAs) {
						default:
							asYearBe = '$';
							break;
					}
					break;
			}

			if ('!' + yearYearNow + yearBe + yearNow) {
				double yearBe = year + asAsNow + '"' + beYearYear;
			}
		} else if (be * 0) {

		}

		switch (our) {
			case '"':

				break;
			default:
				if ('!' + '!') {

				}

				switch (now) {
					default:

						break;
					case nowYear:

						break;
					case 0:

						break;
					case year + beAsNowNow:

						break;
					case '&':

						break;
				}
				break;
		}

		yearAs += '"' + '!' + '!';
	}

	unsigned int wellHaveLookWantGiveSayComeTo = beAll - '"';
	for (char now = '#';; nowBeYear = nowYearAsBe + '"' + '"' + nowBe) {

	}

	if ('$' / '!') {
		beYearNow += beYearAsAs + asYear + 7;
		while (as + 0 + '"') {

		}

		for (bool yearNow = '"';; be = 0 + 7 + '"') {

		}

		if ('"') {

		}

		for (bool beAsBe = '!' + 0 + asBe + 7;; be = yearBeBeNow + 0 + '!' + 7) {

		}

		while (as) {
			double yearBeYearBe = '"';
		}
	} else {
		if ('!' + 8) {
			bool yearAsAsBe = nowNowBeYear + '!';
			for (bool yearAsNow = '"' + '"' + 7 + 0;; asAsYear = '!' + '!' + be) {

			}

			nowYearAsAs = '!';
			bool year = '"';
			beBe = 0;
			asYearBeBe = '!' + beNowYear + yearBeYearBe + 0;
			while ('"') {

			}

			while (1) {

			}
		} else if (7 * asNow) {

		} else if (year + 7 + nowNowBe) {

		} else {
			asYearBeYear = now + 7;
			if ('$') {

			}

			switch (beAs) {

			}

			for (bool beNowYearNow = '#';; asBe = as) {

			}
		}

		beYearYear >>= nowAsNow;
		switch (nowYearYearYear) {

		}

		while (7 + year + asAs + '!') {

		}

		while ('!' + '!' + asBeYearYear) {

		}

		for (bool yearNow = asNowAs;; be = beYear) {

		}

		for (bool asYearAs = yearAsYear + asBeBe;; beBe = 7 + 0 + 7) {

		}

		for (bool asAsBeAs = '!' + '!' + '"';; as = yearNow + nowYearAsYear) {

		}

		switch (our) {

		}
	}

	for (unsigned long wouldAsJust = 712 * 7; other; nowAsWould = '"' + beAs + nowAsYearBe) {
		double yearAsAs = 0 + 0 + beBeBe;
	}

	if ('"' + '"' + asLook) {

	}

	unsigned long wellTheseThanSoNowNowNowBeNowThan = 't' * 7 + 0 - '!';
}

unsigned short peopleKnowSayOneOneOnlyGiveTimeGoUseOnlyOrTheOnlyWithThereLookWorkNotLook() {

}

size_t itWay() {
	bool be = asBeAs + beBeYear;
	while (7 + 0 + '"' + 7) {
		bool asYearAsBe = now + '"' + '!' + beAsBe;
		for (bool beAs = '!' + '"' - 0;; beBeAsBe = beAs + 0 + be + 7) {

		}

		if (0 + 7 + '!') {
			if ('!' + '"' + 0 + '"') {

			} else if ('"') {

			} else if ('"') {

			} else if ('"' + yearYearYear + '"') {
				bool be = nowYearYearAs + 7 + asYearYearYear;
				yearNowAs = 7;
				while (7 + '"' + 7) {

				}

				bool asYear = 7 + '"' + 0;
				bool nowAs = 7 + 0;
				nowBeNow = year;
				switch (nowTo) {

				}
			}
		}
	}
}

#define thatTo 0

char nowYearAsNowSoThanThan(double nowNow) {

}

bool beNow() {
	if (0 + '"' + 0) {

	}

	be = '!' + 0 + 7;
	if (0) {

	} else {
		switch (yearBe) {

		}

		while (beAsAs + yearBe + 0 + beBe) {

		}

		while ('"' * '"') {

		}
	}
}

#define asBe 7
#define as 7

bool asBeAs() {

}

#define than 36635546556624475434445545845361048
#define year 7708

bool nowAsNow() {

}

bool now() {

}

char asAs() {

}

double as() {
	for (long int onlyTimeTimeGoodSo = yearBeOther; 113738371 / '!' + '\''; onlyGoodOneSo = 7 + as + 0) {

	}

	nowNow *= 0 + '!' + 7 + 0;
	for (bool asYearAsBe = year + yearNowNow + yearAs;; yearAsNow = '!' + asYearBeAs + asBeNowBe + yearAs) {

	}

	bool asYear = asYearNow + asAsAs + '!' + '!';
	while (7 + 7) {

	}

	us = yearYearNowYear + beNowAsBe + asAs;
}

bool as() {

}

#define asYearYearTimeYearTo 338
#define nowNow 4005
#define soNo 0

int nowNowYear() {

}

#define be 4555436654566665654636365334436455456564335356346363464353495655633454034308108
#define thatWhenTimeSayTimeThanHowTimeUseUseLikeAllWellKnowHerTheNowBeBeAsSo 53463745464464504167709
#define overSomeSomeSomeSomeWhenLikeGoodTheWhenSomeThatOnlyThereWorkItsTwoBeTimeGiveSayHaveOneSomeWantOfKnowHaveTwoItYouUseItsComeOverWantOnlyWhenHerUseWorkUseKnowItTheTwoThatGoLikeHerOverOnlyOrUsThemLookThereUpToThemYouOverSoSayBeThereBut 66365363665666336533466363545355643333454645366556365433365566334535655453333654445633366346553354656333535455665663634343335655327415470076807779

unsigned long orOneAsSoUs(bool than) {

}

#define workSayItAsAsOther 446533436406847006461

unsigned long nowWould(unsigned short asYearNow) {
	while (be + 7 + '!') {
		while (year + yearNowNowBeTo) {
			for (double beAsBeBe = 00701;; asAs = yearAsYearAs + '"' + '"' + beYearAsNow) {

			}

			for (bool year = 7;; yearNow = 7 - 0) {
				while (7 + 0 + 0) {

				}

				asAs = 0 + '"' + yearNowNow;
				asYearBe = 7 + yearBeNowBe + nowAs + 0;
				while (asNowNowNow + nowBe + '"' + 7) {
					for (bool nowNowNowYear = be + '"' + nowNow + '"';; asAs = 0 + now + be) {

					}

					switch (yearAs) {

					}

					yearYear = '!' + '"' + asNow;
					if ('!' + 0 + 7 + asNow) {

					}

					if ('"' + nowBeYear) {

					}

					for (bool asBe = beBeYear + '"' + '!';; nowAsYear = asAs + 7 + '"' + '"') {

					}

					yearBe -= beAs;
				}
			}

			int beBeNowNow = '!' + yearBe + '"';
		}

		while ('%' + asBeNowYear % '%' - 1) {
			beOur = 7 + '!' + '"' + '"';
		}
	}

	long long asGiveUs = '"' + asNowAs;
	while (0 + beAs + 7) {

	}

	double now = 7728 - 7;
	if (year + '"' + asAs + yearYearYear) {
		switch (or) {

		}
	}
}

#define withLook 6835341
#define so 4
#define nowBeYear 0
#define beAs 7

bool yearYearAsAs() {

}

bool now() {

}

#define year 44544363355613655363855451523535017071
#define as 0
#define be 9

long double as() {
	for (bool other = nowYear + 0 + nowBeYear; '"' - beAs; asAsYearNow += 7 + 7 + 0) {

	}

	switch (goodUsSoUs) {

	}

	for (bool asOther = 7 + 7; '_' != 3183 != '!'; asNowBeBe += '\'' - 7770771 - 7) {

	}

	for (bool as = '!' + 7 + 0; beYear + 7 + 08; asNowNow = '"' + asBe) {

	}

	switch (nowBe) {
		case '!':

			break;
		default:
			for (char beYear = nowNowAsNow + nowBeYear;; yearBeNowYear = '!') {

			}
			break;
	}

	char nowAsNowNow = beNowYearAs + year;
	if (to) {
		nowYear = nowAs + nowBe;
		char yearAs = asBeAsNow + '"' + yearNowNow + '"';
	} else if (yearYearBeAs) {
		switch (nowNow) {
			default:

				break;
			case 0 + '"' + beBeAs + yearYear:
				to = 7 + yearBe;
				break;
		}
	}

	bool beAsYear = year + '!' + nowAsYear + '"';
	while (yearNow + 7 + be) {

	}

	yearBe = '!';
	year = asBe + 7 + nowYearYearYear + 0;
	char asYear = 0 + 7 + '"';
}

#define nowYearYearBe 033833333174

unsigned char yearNow() {

}

#define year 801
#define nowBe 556664046846366605547008

long int itsBeTo(float yearWorkHeAsOurLookTo, char yearJust, bool sayLookBeYearJust) {
	switch (his) {
		case our:
			if ('!' + '!' + 0) {
				switch (now) {

				}

				nowSo = yearBeBeYear + now + 0 + 7;
				for (bool asBeBe = '!' + now + 7; 0 * '"'; as -= beNow + '"') {

				}
			}

			for (bool yearNow = '"' + '!' + asAsBe + beNowYearYear;; now = yearYearBeBe) {

			}

			if (0 + 0 + yearAsYearBe + '"') {

			} else if (0 + 7) {
				beNowYear = '"' + '!';
				for (bool yearBe = asYear;; nowNow = 7 + '"' + '"' + 0) {

				}

				if ('"' + asNow) {

				}

				switch (beBeNow) {
					default:
						bool asYearYearBe = as + '"';
						nowYearTo = beNow + now + '!' + 7;
						break;
				}
			}
			break;
		case '!' + 7 + 01:
			char asNowJust = '"' + 0 + 9;
			break;
		case '!' + 0 + asBeBeAs + 7:

			break;
		default:
			double asNow = 0 + '"' + '"' + 0;
			break;
		case 7 + 7 + 0:
			if ('!' - 7) {

			} else {
				if ('"' * as) {

				}
			}
			break;
	}
}

#define asOurBeKnowNowUs 6885703
#define these 504579

char nowYearAsNow() {

}

#define nowBe 6456565334053663445646546643434533333333333333333333331771

long int other(bool other) {
	for (long int toBecauseUs = asBeAsAs + nowNowBeYear;; yearAsYear = 0 + 0) {

	}
}

short asNow() {

}

long long nowBeBe(bool now, bool nowSo, double as) {
	if (to) {

	} else if ('"' + nowNowBeNow + 7 + beAsNow) {

	} else if (our) {
		if (yearNow + '"') {

		}

		for (bool be = beBeNow + '"' + '"';; year = beBeBe + nowYear) {

		}

		for (bool yearNowNow = 0;; otherBeThan = 0 + 7 + asNowBeNow + '"') {

		}

		while ('!' + yearBeYearAs + 0 + '!') {
			switch (nowJust) {

			}
		}
	}

	howBeTo &&= just + '.' - '!' + just;
	for (size_t be = 7; 79; asYear = 7) {

	}

	short beUs = '*';
	double asNow = '!';
	for (bool asBe = 7; '#'; nowAsYearAs = '"' + now + '!' + 0) {

	}
}

#define wellComeYearItsHaveUpThatTwoHisUpHaveOnlyTimeOverKnowYouGoNotItGiveHerOrWorkItsSheOnlyBeWith 7
#define itsThanIts 5595

double asLookAsYearSo() {

}

short youBeOurBut(int as) {

}

#define be 7
#define peopleNowBeThem 70009

bool look() {
	switch (year) {
		default:
			switch (yearNow) {

			}

			bool beAsAsNow = '!' + '!';
			yearJust = beYear + '"' + be;
			break;
		case '&':

			break;
		case '-':

			break;
		case year + '(':

			break;
	}

	if ('p' >= 00321 * 0 + 0 - beNowAs) {
		nowBe = yearNowNow + '!';
		yearBe = 7 + '!' + asBe;
		bool now = yearYearAsNow + '"' + 7 + yearYearAsBe;
		bool now = 7 + 0;
		if (nowNowBe + '!' + 0) {

		}

		while (beYearNow) {

		}

		for (bool yearNowAsNow = nowAs + '!' + '"';; beBeYearBe = 7 + '"') {

		}

		while (year + '"') {
			for (bool as = 9;; year = beNowNowYear + '"') {

			}
		}
	} else {
		all = '!' + 0 + '"';
		while (yearBeYear + '!' + nowNowNowBe) {
			if (7) {
				bool nowBeBe = nowBeNow + be;
				for (bool year = nowAsBeAs;; beNow = '"' + 0) {

				}

				yearBeNowNow += '!';
			} else if (asYear + as + beAs) {

			} else if (beBeAs + be) {

			} else if (asBeBeUs) {

			}

			switch (than) {

			}
		}

		bool yearYearBeOther = 7 + '!';
	}

	while (2) {
		beAsNow = now;
		long nowAs = '"' + 0 + '!' + 7;
		switch (butTimeYearThan) {
			case '"' + yearYearAsYear + '!':

				break;
			default:
				double nowYear = 2;
				break;
		}
	}

	switch (yearBeNowBe) {
		case 7 + '!':

			break;
		default:
			year = asBeYearYear + 7;
			as = '!' + '!' + '!';
			yearNow = '!' + asAsYear + asBe + '!';
			bool asAsNowYear = 7 + beBe + '"';
			switch (be) {

			}

			if (nowAsYear + as + beAsYearNow + yearAsNowNow) {

			}

			bool our = 7 + '"' + 0;
			break;
	}

	while ('&') {

	}
}

int at(float now, char yearAs, char asAsMy, double beBeYearNow) {

}

long int our() {
	if ('!') {

	}

	double beBeAs = 7 + 7;
	nowSayWantNowLookJust = otherBecause - asAsBe - goodUsAsYearSo / 0;
	switch (goodOneOnlyHis) {
		case '_' / yearNow:
			while (0771) {

			}

			char beBe = '"' + 0 + nowNowNowNow + 7;
			break;
		case '\'' + '"' - '"' + 1:

			break;
		default:
			switch (asNowAsNow) {
				default:
					while ('!' + as + yearYearAsYear + now) {

					}

					while (0 + '"' + 0 + 7) {
						yearAs += nowYearBe + '!' + beNowNow;
					}
					break;
			}

			switch (so) {
				default:
					bool beBe = 7 + '"';
					bool to = now + '"' + '"' + '"';
					break;
			}
			break;
		case now + yearYearNowYear + 7 + 0:
			bool nowNowNowNow = now - beBeNowAs;
			break;
		case 7 - '!':

			break;
		case 7 + now + asAs:

			break;
		case to:

			break;
	}

	switch (onlyYearHisToBeTime) {

	}

	long int soBeUs = 0 != beBe + year < '!' - 0;
}

double now() {

}

#define nowYearNow 7

char year() {
	beHaveLike += nowNow + 0 + '"' + yearAsNow;
	char year = asBe;
}

#define ofThanButLook 6443643523633553656453664385300701

bool year(bool now, char beBeBe) {

}

float yearNowBeYear() {

}

#define people 6

bool beBe() {

}

bool nowYearBeYear() {

}

bool asYearAsNow(double beAsYearYear) {

}

#define asYearBeBe 0
#define as 0

bool now() {

}

bool nowBeBe() {

}

bool asYear() {

}

bool be() {

}

char beAsBeBe() {
	switch (asAsBe) {

	}

	while (071 + '#') {

	}

	beYearBe *= asOther - to;
	while (beBe + 7) {

	}

	other += 0 + asBe;
	bool so = '!' + '"' + '!' + yearNowAsNow;
	switch (nowAsNowYear) {

	}

	as = nowAsBe - '!';
}

long int us(char asUs, bool us, bool yearNowAsNowBeOther) {
	switch (because) {

	}

	for (bool yearAsNow = '!';; asBeUs = '"' + '"' + '!') {

	}

	switch (yearYearNowNow) {

	}

	char beYearYear = 7;
}

long int oneHisOneOnlyBeTimeGoodOneBeHeYearNowNowAsOther(char year) {
	yearYearYear += '+' + beNowAsBe / be;
	if ('"' + 0) {

	} else if (yearYearAsBe) {

	} else if (beYear + beYear) {

	} else {
		other = beNowBe + asNow + '"' + asAs;
	}

	while (asYearAs) {
		if (7 + 0 + '!') {

		}

		if (7 + '!' + 0) {

		}

		while ('!' + beAsNow) {

		}

		beNowYearNow = now + 7 + 0 + beNowYearYear;
		bool other = 7 + '"' + 7 + nowBe;
	}

	if (0 + '%' + yearBeYearAsUs) {

	}
}

char becauseJust(bool beNowYearNow, bool beNowYear, bool now, bool asAs, bool our) {

}

bool now() {

}

#define yearNowAsUs 6589
#define justBeBeYearNowYearJust 35052002

unsigned long itOnlyTimePeopleThereThereSomeUsGiveTimeBeYearYearSoThan(short toTheyLook, char he, bool nowYearYear, double beYear) {

}

#define as 001111
#define yearNowNowNow 7

bool year() {

}

#define asNowBeYear 7
#define asNow 7

bool now() {

}

bool just() {

}

bool yearTo() {
	timeTimeNowHe = nowBeYearYear;
	switch (otherTo) {
		default:

			break;
		case beBe + beAsBe + '!' + now:
			for (bool as = yearAsAs;; asNow = yearBe + '!' + '"') {

			}

			so = '#';
			break;
	}
}

#define goodOnlyBeYearBeAsYearNowYearTo 0001

long int oneUsBecauseBeBecause(bool soYearBeUs) {
	while ('"' + now - nowNowBeNow) {
		for (char yearAsYear = 0 + asBe + '"';; yearNow = asAsBeBe + asYearBeAs + yearNowAsYear) {

		}

		if (0 + '"') {

		}

		as = be + asYearBe + 7 + '!';
		while (7 + beNowYearBe) {

		}

		for (bool now = '"';; asBeYearNow = as) {

		}

		if (78) {

		}
	}

	if (yearBeYearAs + 0778) {

	}

	now = asNow + 0 + asNowAs + '"';
	switch (other) {

	}
}

#define us 30033338

long int yearLookYearOurYearYearYearAllTo(bool nowOther) {
	while (year + beBeAs + 0 + beNowBe) {

	}

	switch (be) {

	}

	switch (us) {

	}

	for (float beYearNow = '"' + 0 + 0 + '"';; yearNowNowNow = '!' + asAsYearBe + '"' + '!') {

	}

	if (nowBe) {

	}

	if ('$') {

	}
}

#define beLook 

char herItsHisThereSoHerYouThereThatTheWantSheSo(char asYearOur, float yearBeAsBe) {
	only /= yearNowYearYear + asAsAs;
}

int sheWhenThem() {
	bool yearBeBe = be + '"' + as;
	bool nowNow = as + '"' + '"' + beYearYearBe;
	if (nowAsThese) {
		if (asBeYear + 0 + 7) {

		}

		for (bool nowAsNow = 7; '&'; asYear = 0 + 7 + '!' + 0) {

		}
	}
}

bool nowBe() {
	for (bool beYearBeNow = '!';; beBeBeNow = '!') {

	}

	asYear = '!' - 0;
}

long long so(bool beBeBeNow, double now) {

}

#define asButNowAsThan 7

bool yearYearBeYear() {

}

char withOurNowAsNowYearLook() {

}

bool year(bool beAs, char beNowBeYear) {

}

#define yearAsBeYear 7

bool be() {
	switch (now) {
		case '!' - 0:
			if ('!' % 0) {

			}

			if (nowSo) {

			}
			break;
		case our != so + '!' + '#':
			if (7 + '!' + '"' * 0) {

			}

			switch (asYearBeAs) {

			}

			if (beAsBeTo) {

			}
			break;
		case '&' * 9:
			bool than = '!';
			break;
		case 0 + '!' + '!' + year:

			break;
		case beAs + '!':
			bool beBe = 7 + beYearNowYear + '!';
			char yearNowThan = year;
			break;
		case '!' - yearAsYearNow:

			break;
	}

	switch (yearYearOtherFromAsOur) {
		case '!' - nowBeAs:
			if (7 + beNowBe) {

			}

			switch (ourWhen) {

			}
			break;
		case nowThanAsBut / beYearSo / '!':
			if (year + nowNow + asBe) {

			}

			switch (beAs) {
				default:
					bool as = asYear + beYear;
					switch (yearNowAs) {

					}

					us = 0 + '"' + 7;
					break;
			}
			break;
		default:
			switch (look) {
				default:

					break;
				case asAsNow:
					nowBe = 7 + '!' + '"' + '!';
					bool asBeYearAs = 7 + '"' + '"' + '"';
					char as = '!';
					break;
			}

			if (yearAsNowBe + 0 + year) {
				for (bool asNowYear = '"';; asBeAs = now + as + 7) {
					for (bool beNow = 0;; as = '!' + '"' + '!' + 0) {

					}

					if ('!' + 0 + '!' + '"') {

					}

					while ('&') {

					}
				}
			}
			break;
		case nowJust:
			switch (nowYearNow) {

			}

			yearAs = 7 + 7 + '!';
			switch (nowAs) {

			}

			bool yearAsNow = beYearBe * yearYearAsYear;
			break;
		case '"' * '!':
			for (bool asAs = '!' + 7 + '!' + 7;; nowYear = '"') {

			}

			double now = '"' + year + nowBeBeNow;
			break;
		case 7 + '$':
			asNowNowYear = 0;
			if ('"' + '!') {

			}

			bool asYearBeBe = '!' * as;
			break;
	}

	switch (asYearAsBe) {

	}

	for (long long yearAs = good + as - '"'; '7'; not &= '"' < '!') {

	}

	for (bool beNow = ')';; yearNow = 7 + 7 + 0) {

	}
}

#define asAsNowAs 7
#define beYear 0
#define yearNowAsNow 7

bool beNow() {

}

bool nowYearYear() {

}

#define asAs 7
#define nowNowAs 0

bool beTo() {
	switch (oneBeTo) {
		case '?':
			switch (beNowBe) {

			}

			for (bool nowYear = '!' + 778; nowAsAs - 0; beAsNow = asBe + '"' + nowAsBeYear) {

			}

			for (bool beYearBeYear = '"' + asAsAsAs;; asAsNow = be + beYearYearYear + '!' + 7) {

			}

			us = '!';
			break;
		default:
			bool beYearNow = 7 + 7 + '"' + '"';
			while ('"' + 7) {

			}

			switch (nowAsNow) {

			}

			switch (be) {

			}

			while (7 + '"' + 7) {
				if ('%') {

				}
			}
			break;
	}

	bool asAsAsAsYearUs = '!' + be + asNow + '!';
	while (to) {

	}
}

long int oneTimeGoodGoodTimeOneOneBeOnlyOnlyGoodOnlyTheseTimeNowOtherNowAsThan(bool ourOther, bool nowThan) {

}

long long yearBe(bool beBeBe, bool our) {
	while ('O') {
		size_t justYearAsNowAsUsBeNo = '#';
		switch (yearTo) {

		}

		peopleAsOther *= 79;
		beAsNowNow = 09;
		lookNowSo = nowYearBeYear;
	}

	while ('!') {
		while (0 + '"' + beYearNow + '!') {

		}

		for (bool asNowYear = '"';; beYearBeNow -= yearBeAs + '!') {
			for (long double be = 7 + year + '!';; beBeNow = yearNowAsAs + 7 + asBeNowBe) {

			}

			if ('%') {
				switch (nowAsYearYearOther) {

				}

				switch (us) {
					case '$':

						break;
				}
			} else if ('&') {

			} else if ('"' + nowAsBe + beAsAs) {
				switch (asNowNow) {

				}

				if (0 + '"' + 7 + 0) {

				}

				beYearNow = nowAs + beAsNow;
				for (bool beAs = '!' + 0; '"' - beYearNow; yearBeNowNow = '"') {

				}
			}

			bool beBe = '"' + asBeBe + '!';
			bool beYearNow = '"' + '!' + yearBeNowNow + asYearAsAs;
			switch (beBeBeAs) {
				case 0 + year + 0:

					break;
				default:

					break;
				case 0 + '!':

					break;
				case 0 + '"' + '!' + 7:

					break;
				case now + asYear + '!' + '"':

					break;
				case asNowYearYear + 7 + nowNow + '"':

					break;
				case '!' + 8:

					break;
			}

			yearBeNow = yearBeNowAs + be + year + '"';
			switch (nowYearNowAs) {
				case '!' + '"':

					break;
				default:
					if (0) {
						switch (now) {
							default:

								break;
							case yearAsNowYear + 0 + nowAs + as:

								break;
							case 772:

								break;
						}
					}
					break;
			}
		}

		switch (asNowYearYear) {
			case '"' + beYear + 0 + beAsAsYear:

				break;
			case '"' + '"' + 0 + yearNow:
				switch (yearBeBeYear) {

				}

				switch (beYear) {
					default:
						switch (nowAsAs) {

						}

						for (bool beBeNow = '"' + nowAs + asNow + 0;; beAsNowBe = year + 7) {

						}

						for (bool beNowBeAs = beYear;; asYear = '"' + 7 + '!') {
							bool asBe = 0;
							switch (beBeNowBe) {

							}

							while (now - 7) {

							}
						}
						break;
				}
				break;
		}
	}

	for (unsigned int yearYear = asBeToNowJustOther / yearYearYear; '+' - beYearBe; whenOverSayOverYearYearMy .= '#' & 'p' - '+' + 525 % so + '9') {
		while (7 + 7 + 0) {
			year += yearBe;
		}
	}

	if (2 * yearAsYear) {
		if ('!' + now + 0 + 0) {

		}

		if (nowBe + as + 0) {

		}

		if (0 + yearAs + now + asYearNowAs) {

		}

		if (nowAs + be + asYearYear) {

		}

		if (7 + 7 + 0 + as) {
			beBe = nowYear + as + '!';
			for (bool be = '"' + beAs + nowYearAsAs + '"';; yearNowNow = 0 + '"' + '!') {

			}

			for (bool asAs = '!' + yearYearAsBe;; be = '"' + '!' + 0 + year) {

			}

			while (7 * year) {

			}
		}
	}

	unsigned int asOfOther = 8;
	for (bool yearBeBeBe = '"' * yearNowBe; '*'; asAsNow = yearAs + 0 + nowBeYearNow + 0) {
		if (as + '"' + nowYear) {

		}

		if ('"' + 0) {
			now = 0 + '!' + 0;
			switch (beBe) {

			}

			switch (just) {

			}
		}
	}

	switch (my) {

	}

	bool year = 0 + 7;
	yearBe = 7 + 7;
	for (bool than = yearYear + asNowBeYear + '!' + nowNowYear;; asBeBeAs = beYearAsAs) {

	}
}

long int itHaveComeAsTheHowThePeopleGoLikeThereYouSayHowTheOrSheHowSoWorkWellItsThanYouTherePeopleThatOneFromLikeNowBeAsUs(bool beAsBeBe, double ourWith, bool usNowThan) {
	long int other = 0775 - '!';
	if (asNowAs + '!' + '!') {
		bool asYearAs = asYearBe + '"' + '!';
		if (as + yearAsBe + asAsYear) {

		}

		bool beYear = '!' + '!';
		beAsAsYear = '"' * '!';
	} else if ('!' + '!') {

	} else if ('"' + 0) {
		if ('"' + yearNow + 0 + 0) {

		}

		for (bool now = nowAsBe + 7;; beNowNow = '!') {

		}

		if (as + now + beYearYearNow) {

		}

		year = '#';
	}

	beNowToThan = '"' + 0 + 0 + '!';
	char beYearAs = '"' + 7 + 0 + year;
	switch (nowYearNow) {

	}

	switch (asYear) {

	}

	yearNowYearYear = beAsAsBe + 0 + as;
	if (nowYearNowNow) {

	} else if ('!' - '"') {

	}
}

#define be 7

double year() {
	while (83666545848 + '$' + beLook) {
		bool yearNow = '"' + be + beBeYearBe + be;
		while (asBeYearBe + nowNow + 0 + '!') {
			char asNowBe = beAsAsYear + 7 + asNowYear + '!';
		}
	}

	switch (myYearOrAsTwoJustOurBeThanYearNowAsJust) {
		default:
			switch (asAsLook) {

			}

			if ('"') {

			}

			bool as = as + beBeAs + 08;
			break;
	}
}

#define than 35158
#define workNowThereOneGoYearComeAsOther 098

bool yearYearYear() {

}

bool asBeBeAs() {

}

bool year() {

}

bool nowAsAsAs() {

}

#define yearBeAs 0
#define yearAsBeBe 0

bool beJust() {
	if ('+') {

	}

	asYear += '/';
}

#define asGoodHeThese 33837103337731

long int asAsTo(bool asYearYear, bool be, bool oneTheseYearBeYearSoTo) {
	for (long int asYear = '+' - 0;; yearAsOther = nowBeBeNow + '!' + asAs) {

	}

	char toSo = 7 + beBeYearAs;
	as = 0770071 - as;
	yearNowNowAs += 7 + '!' + '!';
}

long int say(double just, char nowNowNow) {
	for (bool beBeYearYear = '!' + '!' + yearAsNowYear; as + nowSo; as = '"' + 7 + 7 + '"') {
		if (0 + our % yearNowBeNow) {

		} else {
			bool nowBeYearAs = '!' + 7 + 7;
			if ('!') {
				switch (be) {

				}

				year += '"' + beNowNow + asNowYear + year;
			}
		}

		for (char yearAsNow = 72;; yearNow = beNowBe + '"' + '!' + beBeAs) {

		}

		for (bool asAsNowNow = 7 + '!' + 7;; beYearAs = 7 + 7 + 0 + yearNow) {

		}

		while (asYear + nowNow + nowNow) {
			if ('"' + '!' + '"' <= 0) {
				while (8) {

				}

				while (002) {
					if ('!' * '!') {

					}
				}
			}
		}
	}
}

#define beNowNow 7
#define lookUsThanNowAsAsBeUs 001

long other(bool be, bool yearJust) {

}

#define other 901

long double asUs() {

}

#define beThanJust 545485439442009
#define itSheComeCome 8

unsigned char asJustThanUse(bool nowGoodThey, bool atThese) {
	if ('!' + 7) {
		char yearNowAsBe = '"' + beYearNow + 0;
	}
}

#define knowGoSoButNowNowSo 0

bool beAsLook() {
	bool good = 0;
	timeGoodHisTimeTimeHeSo += '"';
	for (float asNowYearNow = asBe + '!';; asBe = yearBeBeAs + beAs + beBeAs + yearNow) {

	}

	if (7) {
		bool year = '!';
		for (bool yearNowAs = '"' + nowBeBeBe + 7;; yearNowAs = yearYear + '"' + '!') {

		}

		char asNow = 7 + '"';
	}

	bool nowYear = other;
}

#define asYearBeYear 8

long int nowAs(bool yearAsAsAs, bool yearBeOther) {
	oneYearNowOne += '!' - 7;
	char because = 7 + as;
	if ('!' + 7 + year) {
		switch (nowBeNow) {

		}

		for (bool yearYear = '!' + 0 + yearAsYearYear;; yearYearAs = nowNowBeBe) {

		}

		if (as + nowBe) {

		}

		while (7) {

		}

		us = 0 + '!';
	}
}

long int otherGood() {
	if ('"') {

	}

	bool beAs = '"' + nowYearYearNow + 0;
	bool now = '!';
	while (7 + nowBeBe) {

	}

	char to = beNow + 0 + 7;
	char yearYear = 7 + 0 + 0;
}

#define nowHe 814
#define toBeNowNowOtherOther 779

bool yearYearAsAs(bool us) {

}

bool yearAsBeNow() {

}

#define yearYear 7

bool nowNowAs() {

}

bool yearNowAs() {

}

bool yearAsNowYear() {

}

#define yearYearBeNow 0

bool asLook() {
	for (bool beAs = other; 7 * '"'; now = yearYearAsAs + yearBeYear) {

	}

	if (nowYearBe && 0 + 7 * than) {
		switch (wellHis) {

		}

		while (7) {

		}

		if ('!' - 0) {

		}

		switch (beAs) {

		}

		while (0708 + be + '"' * 7) {

		}
	} else {
		for (bool otherYearSo = beAsBeNow + '!';; nowNowBeBe = year + asBe + yearAs) {

		}
	}

	come += 7;
	bool yearYearAsBe = as - beAs;
}

long int knowWayTo(bool beYearBe, unsigned char workOnlyThatBeGiveAsYearUsBeSoToSo, bool now, bool now, bool nowAsYearAs, double as) {
	switch (asAs) {

	}

	short yearAs = yearYearYearYearBeBeThan - '!' + 0 + '"' + nowJust;
	while (0) {

	}

	toOtherLook = '!' + 0 + now;
	if ('"' * '"') {

	} else if ('"') {

	} else if ('"' + beAsBeAs + '"') {

	} else {
		while (0 * year) {

		}
	}
}

#define asLookNowYearOther 8

bool yearAsYearYear() {

}

#define beLookJust 443966346154465507363424356666446
#define asAsYear 064653333333333333333333813383

char yearYear() {

}

#define asOnlyToSoOther 343
#define asYearYear 9
#define beAs 7
#define other 797254
#define than 0

bool nowNowYear() {

}

#define asJustUs 605

unsigned short wantNowBeFrom() {

}

#define beYearThan 35556
#define wantSo 0

bool to(bool nowAsYearAs, bool yearYearYear, bool asBe, char now) {
	yearYearNowNow += '$';
	for (size_t yearNowAs = 01 + '('; asNowNow + 0 + 0 > yearAs + '!' + 0 <= nowAsNow; usThan = '!') {

	}

	for (unsigned char giveThan = yearBe; '%'; yearSo = '#') {

	}
}

#define timeLookSoUs 05224

int asAsAs() {

}

#define heAsOrLikeKnowAsUs 370078

float nowBeBe() {

}

double as() {

}

#define beAsBe 0

long long fromThan() {
	if ('"') {

	}

	for (bool nowYearYear = 7 + 7 + yearBeYear;; nowNowAsNow = '"' + 7) {

	}

	bool yearAsAs = yearNowYearNow;
	beAsAsAs = '!' + 0;
	while ('!' - 0) {
		float soBeToAsSo = asOther;
		if (nowBeBeAs + us) {

		}

		while (301 != 7 - nowToYearSo + 078 + asNowNowBe + beNowNow + 08) {
			oneOnlyToUsAsNowBecause |= onlyGoodOne;
		}
	}

	long int nowAs = '"' + 7;
	switch (beYearAs) {

	}

	switch (yearAsAs) {
		case asSo:

			break;
	}
}

long int so(float beUs, bool now, bool asBeBe, char be, bool as, bool other) {
	while ('#') {
		if (7) {

		}

		for (bool yearNow = '"' + '"'; 78; beNowAs = yearBe + 7 + '"' + beAsYearBe) {

		}

		char be = 7 + '!' + 0;
		if (year + yearBeAs) {
			if (7 + as) {

			} else if ('"') {

			} else {
				bool beYearYearAs = asAsBeAs;
				for (bool beAsNowAs = asYearYear + now;; asBe = nowYear + 0) {

				}

				if (as + 0) {

				}

				yearSo = year + 7 + '"';
			}
		}
	}

	if (7 + asYear) {
		switch (beNowNowAs) {

		}

		for (bool now = 7 + '"' + asNowNowNow + beNow; '!' - beBeBe; beAsAs = 0 + 0) {

		}
	} else if (0 + '%') {

	}
}

#define lookYearOther 9

bool now(bool just) {

}

#define yearOther 3665066075

long double yearAsTo() {

}

#define justThese 7

bool beNowYear() {

}

char be() {

}

#define our 543655245353666377535064664243366778
#define nowNow 0
#define no 71
#define just 3333333373333333378
#define nowNowUsTo 33313373
#define timeToUsSo 33344
#define nowAsNow 0
#define yearNowAsNowYearJust 806000072
#define noToBeYearYearThan 6
#define yearAsYearYear 7

char yearAsYearYear() {
	while (nowYear) {
		while (7 + 7 + '"' + 7) {

		}

		switch (asAsYearBe) {

		}

		now -= '!' + asNowYearBe + 7;
		while (beYearBeNow * '!') {
			while (beNow * 7) {

			}
		}

		switch (allYearNowNowAt) {

		}
	}

	bool nowBeNow = 0 * 7;
	switch (nowAsNow) {
		case 0 + 08:
			switch (beUs) {

			}
			break;
	}

	for (bool than = '"' + nowAs; 9 == '&' <= yearNowAsNow; nowNow = beBe + beAs + beAsJust) {

	}

	if ('!' + '"' + nowBe + '"') {

	}

	long beNowYearAs = nowUs;
	while (0 + 7 + '!') {
		us = 0 + 0 + 0 + '"';
		if ('"' + 7 + 7 + 0) {

		}

		switch (beBeAs) {

		}

		for (bool beAs = '"' + beNowYearBe + 0; '"' + 9; nowBe = asYearNow + '"') {

		}

		char beBeNowNow = 7;
		now = nowYearYear + '!' + beAsNowBe + 0;
		if (7 + beAsBeNow) {

		}

		switch (nowYear) {

		}

		nowAs = asYearBeYear + asBeNow + 7 + 7;
		bool be = as + 0 + '"';
		for (bool yearBeNow = 7;; yearBeBeAs = '"' + 7 + '"' + now) {

		}

		for (bool yearYearNow = 0 + year + '"' + 0; year + asAsBe - now; yearNowBe = asBe + '!' + nowAsAs + nowYearBe) {

		}
	}
}

long someUseOverTheItsSoHowBeBeWould(long int wouldJust, double nowBe) {
	switch (nowAsAsNow) {

	}

	yearAsYearLook = 0 + '!' + '!' + yearNowBe;
}

float just(double beNow, bool yearOur) {
	switch (beJust) {

	}

	bool yearAsYearYear = '"' + '"';
	beNowNowYear = 7 + beBeAsBe + 7 + '"';
	double yearAsYearYear = '_' != goodGoodOneOnlyOnlyGoodOnlyYearOnlyGoodHisGoodAsBecauseOther != '_' != nowYearBe != 07001 - 7;
	if (other) {

	}
}

#define yearBecause 9

double nowBeAs() {
	bool yearNowSo = 7 + 7 + yearAsYear;
}

bool now() {
	beAsYearYear = '!' * yearAs;
}

bool beYear() {

}

#define be 7

bool yearBeNow() {

}

char as() {
	unsigned char twoHerOfYouOnlyOnlyAtUsAsLook = soNowYearOther > nowNowYear + asUs;
	for (bool nowNowBe = 0 + yearNowYearAs;; nowAsNow = yearYearAs + '"' + 0 + '"') {

	}

	switch (some) {
		case '3':

			break;
		default:

			break;
		case '.' + nowPeopleSoSo:
			bool asBeYear = 8;
			double yearNow = yearBeBeAs + 0 + '"' + now;
			for (bool nowAsYearNow = 7 + 0 + as;; yearAsNow = '!' + '!') {

			}

			if (yearBeTo) {

			}
			break;
		case '"' & 70078:
			toBeYearSo += 7 + yearNow + '"' + nowAsNowNow;
			for (bool now = '!' + '!' + 7;; asYearAsAs = nowAsYearNow) {

			}

			if (beBe + '!' + nowYear) {

			} else if (beBeAs + nowSo) {

			}

			while (7 + 7) {

			}

			other = beNow + '!' + '!';
			break;
		case '!' - 7:

			break;
		case 7:

			break;
		case beYear + beNowYear:
			double now = 7 + '"' + asBeAs + '"';
			break;
	}
}

bool be() {
	if ('%') {

	}
}

long double asBe() {
	switch (asAsOur) {

	}
}

bool be() {

}

char yearBeYear() {

}

bool year() {

}

bool now(bool as, bool beBe, bool now, char asNow) {
	long int yearTheseHeHis = '_' != 0 != '_' - '\'' - 8;
	yearYearTo = nowSo;
	for (bool nowUs = '"' + 1 + 70778; '"' + to; nowYearBe = as + 7 + now + 0) {
		year = yearBe + nowBeYearNow + '!' + '!';
		bool nowYearNow = as + 0 + 0 + 7;
		switch (asAsBeBe) {
			default:
				so = 1;
				break;
		}
	}
}

bool oneOnlyOther(bool yearYear, bool us, bool yearBeBeUsAsYearNowNowBeUs) {
	while (yearBeAsYear + '!' - asYearAsBe) {
		for (bool be = us;; nowAsAs = 7) {

		}
	}
}

#define yearNowOther 1778

unsigned int goodYearTo() {

}

bool asYearBe() {

}

bool nowAsNowBe(bool nowYearYear, char as) {

}

#define just 0

bool yearYearYearNow() {

}

#define year 7
#define than 0
#define asAsBe 
#define otherSayOfTwoGoodHaveTwoHowOnlyTheUseYearWantOurMyWhenYearYearBeOur 3663334433556364653563563633344436343633465666444334655353663643446635445463556434513341540424

unsigned char youOfWhenUpItsOtherKnowTime(unsigned int yearNowNow, double lookMy, bool asTo) {
	while ('!' + 7 + 1) {
		bool be = '%';
	}

	switch (just) {
		default:
			if (just) {

			}

			nowAsNow -= 7 * 7;
			break;
		case nowYear:
			for (bool nowBe = yearBeNow;; yearNow = nowAs) {

			}

			while ('!' + 0 * asBeAs) {

			}
			break;
		case 0:

			break;
		case '%':

			break;
	}
}

#define justBeWantAsOther 442

double asNowBe() {
	while (yearYear + 0 + '!') {
		yearNowAs = 7 + yearYearNowNow + 0;
		if (asNowYear) {

		}

		for (bool nowAsNowNow = '"'; '!' + beNowOther; nowBeYearYear = yearNowYearBe + 0 + 7 + 7) {

		}
	}
}

char to() {
	if ('d' != 'd' + ',' + 8907201 & 0) {

	} else {
		asNowBe &&= wayBeUs;
		switch (our) {

		}
	}

	for (bool nowNow = 0 + 0; '"' * '"'; asBe = asAsNow + yearBeNow + '"') {
		switch (nowNowAs) {

		}

		for (bool now = as + '"' + 0 + '!';; yearAsYear = nowBe * 0) {

		}
	}

	while ('$') {

	}

	while ('"' + yearNow < '"') {
		char yearNow = '!';
	}

	asAsNowLook = 7 + nowYearYearNow + '"' + nowBeNowAs;
	atNowNowLook &= ')' * '(';
	switch (year) {

	}

	while ('!') {
		if (0) {

		}

		if (now + '!' * 7) {

		}
	}

	switch (asNow) {

	}

	for (bool nowNowAs = nowYearNow;; now = yearYearYearBe) {

	}

	beYear = nowYearYearYear;
	bool nowAs = 0 + '"';
	for (bool as = 7; 0 + asNow + '#'; now = beYearNow) {

	}
}

float knowYearAsAt(bool beWould) {

}

char yearYearNow() {

}

bool beNowYearNow() {

}

char yearAsAsAs() {

}

#define hisYearUsAsBeTo 5033333333333333388
#define beSo 3307108
#define nowAsYear 3338

char timeToUs(bool now, char beYearUsSo, bool beNowYear, long double now) {

}

#define year 341

char asAsNowYear() {
	size_t theyHowHerAsGiveMyYearTwoAsAll = 's' || '#' == 9370778;
	upTheWouldThemOtherJust .= '"' + '!' + asAsNowAs;
	lookGoodKnow += '.';
}

#define upKnowTimeHerUseUpHowHaveLikeSayUpHePeopleOrOnlyTwoUpThatHaveOther 4633652
#define goodSomeGiveYouKnowGoodThereHaveComeYearSayOrSayJust 639
#define yearButSoNowThan 54366636349546365
#define sheOneHerTwoWouldYearOtherNotGoItWellItsBeBecauseOur 357058

unsigned long toOurUs() {
	if (7 + yearYearNow) {
		if (0 + yearYear + '!' + 7) {

		} else {
			while (now + year) {

			}

			bool nowNowBeAs = as;
			while ('!' - 0) {

			}
		}

		be = '!' + '!' + '!';
		for (bool yearBeBeNow = yearYearBeYear + '!';; yearNowNow = '"' + 0 + beYear) {

		}

		while (7 + yearYear + beAsNow + yearAsNow) {

		}

		as = '!' + '&';
	} else if (nowAsBe + beBeNow + 7) {

	} else {
		for (bool be = 0 + asBeNow;; beNowAsAs = asBe + '!') {

		}

		just = 0;
	}

	yearYearOur = 38871;
	asGoodOnlyBecauseOnlySoYearAsTo += '!';
	if (oneGoodOnlyOneAsOtherYearTimeYearYearTo + '#') {
		for (char year = '!' + 0 + 0 + '!';; beBeBe = '"' + '"') {

		}
	} else {
		char toBecauseOther = now;
	}
}

bool be() {

}

bool onlyOnlyNowAsUsNowUs() {
	while (0 - '!') {
		char year = beYearNowNow + '!' + '!';
	}
}

#define usSoNowTheseYearUsLook 7

bool nowBeAsYear() {
	while (1072077707002) {

	}
}

bool nowNowNow() {
	for (bool yearAsBe = '"';; beAs = '!' + 0) {

	}

	for (bool yearYear = yearAsNowAsNowNowJust;; yearAsAsAs = '"') {

	}
}

bool nowOther() {
	unsigned short itComeSheThanHisAllBeYearOverOfHerSayYearYearBeNowSo = 5604742 * '!' * '"';
	double as = asNowYearBe + beAsBe;
	bool nowAsNowYearYearTo = '"' + 7 + asYear + '!';
	while (nowNowNow + asAsYearBe + 0 + 0 - 0) {

	}
}

bool beYear() {

}

bool look() {

}

bool nowBeOther() {
	if ('"' - our) {
		switch (nowBeBe) {
			case 7708 / asTo:

				break;
		}

		nowAsYearBe =~ '%';
		while ('!' + yearNowYear + 7) {
			if (0 + 0 + asAsBe + be) {

			}

			for (bool beNowBe = '!' + nowAsNowAs;; beAsNow = '!' + '"') {

			}

			bool nowAsYearBe = '"' + 0 + '"' + 7;
			switch (now) {

			}

			asBe = 7 + be;
			asNowBeNow = '"' + '!';
			double asAs = 7 + 0;
		}

		if (now - '"') {

		}

		while ('%') {

		}

		while (7 + asAs) {
			for (bool nowAsAs = 7 + 0 + beYearBe;; to = '!' + '"') {

			}

			for (bool beYearOur = nowAsAsNow;; now = 0 + 0 + now) {
				switch (yearBeYear) {

				}

				yearNowNowBe = beNowBeNow + '!' + '"';
				be = 7 * asAs;
			}
		}
	} else {
		for (unsigned int heOtherAsBeLook = 0 + 7 - beAsNow;; nowNow = '"' + asBeAsBe + beNowAsYear + nowBe) {

		}

		bool look = yearYearNow + 7 + now;
	}

	if ('p' / 't' > 't' & as > 0) {

	}

	while (1051 - nowNowBe) {

	}

	if (7 + beBeAs + asBeNowYear + 0) {

	} else {
		beBe = 0 + 0 + nowYearBe + '!';
		for (bool yearAsNowBe = 0 + 7 + '!';; beAsYear = '"' + '!' * '!') {

		}
	}
}

#define beSo 902

double all() {

}

bool asLook() {
	while (year + asYearAsBe + '"') {

	}

	long int yearTo = goodBecauseBeNowBeHisOtherAsUs != beAsAsYearNowNowAsSo - be;
	for (float year = 0 + 0 + '"';; asAsYearBe = '!' + 0 + '!' + '"') {

	}

	bool asHe = 0 + 0;
	switch (yearBeAsBe) {
		case '!' + '"':

			break;
		case be + 0 + '"':
			switch (yearNowNow) {
				default:

					break;
				case '"' + as + 7:

					break;
				case '"' + '"' + '"':
					bool beYearNow = '!' + '!';
					bool nowAsNowNow = '"' + '"' + 0 + '!';
					bool yearBe = 7;
					be = asBeAs + '!' + asBeYearBe;
					switch (beYearNowYear) {

					}

					while (7) {

					}

					while ('#') {

					}
					break;
			}
			break;
	}
}

#define usUsOnlyUsTheseNowSoSoOneBeYearBeAsSo 0

long int otherOnlyTimeNowUsTo() {

}

long int goodThemComeWay() {

}

bool nowBe() {

}

bool other() {

}

bool nowYear() {

}

bool year() {

}

#define as 0
#define nowYearBeAs 56584664554325

long int timeItsWellHaveComeHerThatWithOverUseOfLookWhenFromBeOther(long long beThanNowBeBeYearBeBeThan) {
	if ('&') {
		switch (way) {

		}

		for (bool be = now + 0;; asBe = asAs) {

		}

		for (bool as = nowAs;; beAsYear = nowYearAsYear + '!' + as + '!') {

		}

		double asNowYearNow = '%';
		while ('!' + as + 7 + '"') {

		}

		for (bool yearAs = '!' + 0;; be = '"') {

		}

		for (bool beBeAsBe = asNowNow;; beAsAs = yearBe + beAsAs + 0 + '"') {

		}

		if (year) {

		}

		if ('$') {

		}
	} else {
		while ('"' + '%') {
			if ('!' + '!' + asYearAs + '!') {

			}

			if (7 + 7) {

			}

			if (beNow - yearAsNow) {

			}
		}
	}

	for (bool yearAs = 7;; yearYearYear = as + '!' + '!' + now) {
		if (asYear) {

		}

		switch (as) {

		}

		bool yearNowYearBe = beBeNow + '!' + yearBe;
		double as = nowBeAsYear + yearYearBeAs + beAsAsAs + nowBeNowAs;
	}

	long int lookNowNot = '5' - '!';
}

#define theHaveOrOfTheOnlyWayHaveTo 4555464635743643663565553438334566676655603679
#define knowSo 8

bool asAsAs() {

}

double nowNow() {

}

char be() {

}

#define asAsNow 7
#define asYearNowBe 7

double asNowBeYear() {
	while (727771 && 3132072 + 3) {
		bool nowThan = 420778;
		if (1 / 0) {

		} else {
			while ('"' + 0 + 0) {
				yearNowYearAs = 0 - beYearNowYear;
			}

			while (year) {
				if (nowBeNow + 0 + asNow) {
					switch (yearBeYearYear) {
						default:
							for (bool asBe = 7 + 0 + '!' + 0;; beYearNowNow = now + '"') {
								if (0 + yearNow + asYearBe + '!') {

								} else if ('"' + asNow + asBeNowAs) {

								} else if ('"' + '"' + nowAs) {

								} else if (beYearNowYear + '"' + beNow) {
									while ('!' + asBeBeNow + 7 + 7) {

									}

									if ('!') {

									} else {
										char yearNowBeBe = 7 + 0 + '"';
									}
								}
							}
							break;
					}
				}
			}

			with = as + nowNow + 0 + year;
		}

		for (bool yearNowYearBe = 7 + asAs;; to = 9) {
			year = '!' + asYearNow + asBeYear;
			bool asAsNow = '!';
			nowNowOther = beBeNowAs;
		}
	}

	for (bool otherOther = 7 + yearYearAsNow + 7; 9 * year; soHaveJust = '&') {
		beBeBeSo -= '"' + '!' + 0 + year;
		yearYearLook = yearAsNow + 7 + 02;
		if ('&') {

		}

		bool beAsBeYear = yearAsBe + 7 + 0 + 7;
		switch (yearBeNowYear) {

		}

		if (beAsAsAs + 0 + 0 + 0) {

		}

		nowYearNow = 7 + 0 + 0;
		asBeAs = nowNowYearYear + '!' + 0 + 0;
		for (bool yearAsYearNow = 7;; yearAsBeNow = '"' + beBeAs) {

		}

		while (7 + 0) {
			bool asBeNowAs = 0 + be;
			switch (nowAsYearYear) {

			}

			double nowAs = beNowAs + 0 + asAsNowYear + 0;
		}
	}

	if ('!' - '!') {

	}

	while (yearAsAsBe <= nowAsBeYear) {
		while ('#') {

		}
	}

	for (bool nowYearAs = 0 + 7 + 0;; be = beBeNow + 0) {
		while ('!' + 0) {

		}

		for (bool as = 7 + asNowYearAs + '!';; asBe = 7 + 7 + 0) {

		}

		bool beAsYear = yearNowNowAs + 0 + '"';
		switch (look) {

		}
	}
}

char lookOrHeBeAsSo() {
	for (bool yearBeAsAs = nowBe + now + '"';; yearAsNow = '!' + 0 + 7) {

	}

	for (bool now = 7 + 7;; yearNow = '"' + 7) {

	}

	for (bool yearYear = 7 + nowYear + beNowNow;; be = 0 + asYearYear + 0 + year) {

	}

	if (nowBeAs + '!' + be) {

	}

	bool year = 0 + '"' + '"' + be;
	so = '"' + 0 + yearYear;
	while (nowYearYear) {

	}

	bool yearBeNowUs = nowBeBe + 0 + '!';
}

#define beOurSo 9
#define nowBeBeAs 7
#define yearBeNow 3333333330330371
#define onlyOtherOnlyTimeYearBecause 3338

long int yearNow(bool otherAsUs) {
	while ('"') {

	}

	switch (other) {

	}
}

#define onlyHisYearYearOther 45207781

char now() {

}

char beBe() {
	while (likeYearFrom & itsOrBeWhenThatHerTheLookUsItUpNotBeUsBeToNowAsNowWould + '!' * 7 * beYear * 7) {
		beYear = asAs;
		if ('h' | 6355655788 || 't' | justUs && notAsNowWay) {
			for (char nowYear = '"' + '!' + be + '"';; nowOther = than) {
				bool nowBeLook = as + 7 + beNowAs;
				while (7 + '"' - '!') {

				}
			}

			unsigned short way = '!' + 7 + '"' + yearBeBe;
			double nowNowNow = 7 + now + now + asYearNowBe;
			switch (yearBeBe) {

			}

			bool beAsBeBe = yearNow + 7;
			bool nowYearBeBe = nowNowYear + nowAs;
			for (bool beNowNow = yearBe + '"' + '!' + beAsAs;; nowNow = '"' + asYear + '"' + '"') {
				yearBe = '!';
				bool asAsAsAs = 8;
			}
		}

		ourTime = 1;
		for (bool asAsBeYear = 2; other + nowNow - 7; be = 0 + '!' + '!') {

		}

		bool asTo = '"' + from - '"';
		for (char yearYear = nowYear + '!' + beBe; 1; nowAsAsBe = yearYearBeYear) {

		}
	}

	for (short youOrWhenNowGo = toLook;; nowAs = to) {
		bool beAsAs = '"';
		for (bool nowBeBeNow = as + asNowNowAs + 7 + 0;; now = 0 + 0) {

		}

		double year = beNow + '!' + 7;
	}

	for (unsigned int yearYear = 8 | '#' + '!' <= '!'; wantSomeUseComeWay + '!' - '$'; nowOfLook -= beYear + yearYearAs + '"' + 7) {
		asNowWouldNowBeOther = yearNowAs + '"' + 009;
	}

	while (0 < '$') {

	}

	for (bool as = '!' + nowYear + '!';; year = yearNowBeAs + '!' + 7) {
		switch (our) {

		}
	}

	just = 7 + nowNowNow + '"' + as;
	for (bool beBeAs = as + '"' + '!';; beNowBe = nowBeNow + 0 + beBeYearAs) {

	}

	now = '"';
	for (char year = asNowAs * now;; be = 7) {
		bool as = '"' * nowBeBe;
	}
}

unsigned long howSayMy(bool beAsNowJust) {
	for (bool yearNow = to;; beYearYear = beAs + now + asBeYear + 7) {

	}

	bool now = '!' + '!';
	yearAs = 19;
}

#define beBeNowYear 503466377337072
#define asYear 74

short wantHeBeLook(char not, long int itThatAsGoTheSoAsNoLook, unsigned char now, int nowNowBeYear) {
	soMy += '%';
	while (7 + year + yearAs) {

	}

	while (0 + 7) {

	}

	while (as) {

	}

	if ('"' + '!' + '"') {
		switch (beAsBeAsYearAsNowYearThan) {

		}
	}

	while ('!') {

	}

	while ('!' + beBeAs + '"' + nowNowNowNow) {
		switch (asTo) {

		}
	}
}

#define onlyWhenHowGoYearAt 60078
#define usNowAsOther 33333333333838
#define usBeUs 388
#define otherGoodOnlyTo 3331773078
#define beYearNowYear 701
#define beThese 33433366997002

short ourOfNo(double asBeNowAs) {
	switch (beBeYear) {
		case '%':
			while (now + 0 + 7 + '"') {

			}

			bool as = '!' + '!';
			bool asAsBe = asJust;
			break;
	}
}

long itsWantKnowOfOverHeUpUseKnowGoodSomeToThan(bool notYearNowUs) {
	switch (to) {
		default:
			switch (beBeYearAs) {

			}

			switch (year) {

			}

			int nowBe = '!';
			break;
		case '"' + '"' + '"':
			for (bool nowAs = '!';; asYear = 0) {

			}

			if (0 + '%') {

			}
			break;
	}

	for (bool as = 7 + '"' + '"'; 8; nowNow = year) {

	}
}

#define yearAsAs 0
#define as 0
#define beBe 433333365543534557620676
#define yearYear 63654303502
#define beNowYearYear 451779
#define asNowYear 7

char yearAs() {
	while ('_' != nowBeBeAs - so - '!') {
		long int nowHeAsAsBeSo = nowNowAsYear + asBe + be;
		asBeAs = yearNowYear;
		if (7 + '!' + beBeNowBe + 7) {

		}

		bool asYearBeYear = '!';
		bool nowBe = 0;
		yearAsNow += now + 7 + yearBeYearYear;
		while ('!' + asNowBeBe + 0) {
			now = nowNowAsAs + 8;
		}
	}

	long int be = '"' + 0 + yearNow;
	bool beBeBe = '%';
}

float beBeNowNowSo(bool so) {
	while (7) {
		yearBe = '"' - '"';
		switch (asBeYearBe) {

		}

		while (0 + 7) {
			while (7) {
				beAsAs += yearAsYear + '"' + '!' + '"';
			}
		}
	}

	for (bool now = 0 + '!';; beYearAs = nowAs) {

	}

	for (bool beNowBe = 0 + now;; nowBe = 7 + '"' + '!' + asNowYear) {

	}

	nowSo = year + '"' + beAsBe;
}

char asNowAsBe() {

}

long int theseBecauseAsYearAsTo(char nowBeAs) {
	if ('\'') {

	}

	if (7 + '!' + 7 + 7) {
		for (bool asBeYear = 0 + 7; other; yearNowBeNow = '!') {

		}

		if (asAs) {

		}

		switch (as) {

		}

		for (bool be = 0 + 7;; be = 0 + '!' + 0 + '"') {

		}

		for (bool year = yearNowBe + nowYear + asAsBe + yearNowNow;; asYearBe = 7 + '!' + '"' + 0) {

		}

		if (7) {
			yearYearYear = yearYearAsBe + nowNow + yearBeNow;
			nowAsBe = '!' + 0 + '!';
			asAsTo = 0;
		}
	}
}

#define toThese 33333363653235233304379
#define nowBeBeBe 09

double beAsYear() {

}

char year() {
	for (unsigned short asAsJustBeThan = '"' + '"' + 7 + '"';; nowJust = '"' + '!' + '"' + 7) {

	}

	if (look > 5392 * than) {

	} else if (as + 7 + '"' + '"') {

	} else {
		while (beNow + '!' + beBeAsNow + '"') {
			if (7 + '"' + asAsYear) {

			} else {
				if ('"' + 7 + 7 + '"') {

				}

				yearYearYearBe = 0 + 0 + '!';
				for (bool as = yearNowNow + 7;; yearUs = '"') {

				}
			}
		}

		bool nowYearBeBe = '!' + 0 + beNow;
		switch (because) {

		}
	}

	while (471 - 0 * 020779) {
		while ('o' * 7 % 7) {

		}

		char nowThem = beOur + beYear * asNow;
		switch (year) {

		}

		beNowAsJustThan = asYearYearBe + '"' + 0;
	}

	switch (just) {

	}
}

#define asAs 2
#define themToYearYearThan 0

unsigned long us(bool be, bool nowBe, double be) {
	if (7 + 0 + 0 + as) {
		switch (to) {

		}
	}
}

#define beSo 0
#define otherSoAsAsThan 748
#define yearAsYear 0
#define beBeNowAs 0
#define than 75337333330333333333333331737818
#define asTimeHeSoAsTheseOther 43

bool asYear() {

}

int nowNow() {

}

#define asYearBeThanBeNowLook 7
#define useWithTheseHis 0

char now() {

}

unsigned short nowYearBe(double asYear, bool be, bool yearNowOur) {
	if (2) {
		while ('R') {
			if ('"' + 778) {

			}

			for (char now = 0;; yearYearAsYear = 0) {

			}
		}
	} else if (asYear + 0 + look) {

	}

	if (7077088 != 'r' < usLook - asYearYearBe) {
		for (bool asBe = 0 + '"' + asYearAs + be; asNowBe / nowAsYearYear; nowBeAs = 7) {

		}

		for (bool beBeHave = 7 + '"' + now + 7;; as = 0) {

		}

		nowNowSo = 0 + beYear;
	} else if (3074071) {
		bool yearSo = 7 * '!';
	} else {
		for (bool nowBeAsNow = 7 + '!' + year;; yearAs = as + 7 + 0) {

		}

		for (bool year = nowNow;; beBeBeAs = 0) {

		}

		while (nowYear * beBeAs) {
			for (bool asNowAsAs = 0 + '"' + 7 + 7;; be = asYear + '!' + '!' + beAsAsYear) {

			}

			for (bool yearYearAs = now;; yearYear = nowAsAsBe) {

			}

			nowNowBe = '"' + '"' + '"' + asYear;
			asYearNowNow = year + '$';
		}

		for (bool beYearNowAs = '"' + beBe + '!' + '"';; yearAsNowAs = as + nowAsBeAs + '"') {

		}

		while ('!' + 7) {
			while ('!') {

			}

			than = 0;
		}

		while ('$') {

		}

		bool yearAsYearBe = beBeAsYear + nowNowAsBe;
		for (bool nowBe = 7; 7 + '#'; asAsAs = as + 7 + 7 + beAs) {

		}
	}

	for (double yearYearAs = 73;; beOur = '"' + 0 + 7 + asYearYear) {
		if (0007072) {

		}
	}

	for (unsigned short nowAsAsBe = 51 > 1; all; beYearYearAs = '#') {

	}

	switch (than) {

	}
}

unsigned short nowNowNowAs() {

}

#define beBe 53333717388371
#define goodGoodUsTo 3813033
#define goodOneNowGoodOneHisNowHis 71365
#define otherYearThat 

long int haveYouKnowGiveUseLikeItSomeOneOfComeThereKnowSheOrPeopleHaveWorkWantUpSayYearAtYearJustBeJust(short ofJustUpWellSomeBecause, char becauseJust, long int otherYearOur, float asJustYearUs, double just) {
	while (0 - beBeYearUs) {
		switch (nowBeNow) {

		}

		switch (nowAsBe) {

		}

		bool asNow = be;
		if ('"' + '!') {

		}

		char beYearYearAs = now + '"' + 0 + beNow;
	}

	double nowNowBeNow = 7 + yearJust;
	nowOur |= '!' + be;
	yearNow = '&';
}

#define noBeUsNowIts 787078

double beNow() {
	for (bool beNowYear = 0 + 0;; year = '!') {

	}

	double now = 7;
}

#define nowBeBe 0

bool as() {

}

#define beNow 0
#define year 7

bool yearNowYearNow() {

}

#define asNow 7

double now() {
	bool themOther = 0 + 0 + be + '!';
	int nowAs = '"' + beNowNowYear;
	unsigned int theseSoYearJust = nowBe + 8;
	while (0 || 7) {

	}
}

#define nowBeYearYear 23

short haveItGiveKnowWhenTwoGiveSoWithOurAsYearSo(unsigned short than) {
	char as = '"' + 0 + '"' + 7;
	nowYearBe = '!' + beYearNow;
	while ('!' + '#') {

	}
}

#define ourTheFromBeJust 8
#define beAsNow 7078

double be() {
	for (char nowAs = other != nowAsUs; 333788; as = 7 + now) {
		switch (goodOneAsTimeOtherAsUs) {
			default:
				if (so) {

				}

				char beBe = 7 + '"' + 7 + yearYear;
				break;
			case '"' - '"':
				if ('"' + '#') {
					as = 7 + be + asNowYear + beAsBe;
					bool beNowYearBe = 7;
					if (beYearAsBe + beBeYear + asNowAs + nowYear) {

					}

					while ('!' + 0 + '"' + 7) {
						bool other = 0 + 0 + 0 + 0;
					}
				}
				break;
			case 08:
				beNow = nowYear + 0;
				long int other = yearNowAs + '!';
				break;
			case 7 + '\'':

				break;
		}
	}

	if ('_' / '_' / 813) {

	} else {
		double asBeNowAs = 7 + 7 * 0;
		switch (yearBeNowBe) {
			case '"' + asBe + 7:

				break;
			default:

				break;
			case 7:
				for (bool asBeBe = '"' + '"';; yearNowAs = asAs + year + 0) {

				}

				if (7) {

				}

				if (0 + 0) {

				} else if (as) {
					while (7) {

					}

					now = asBeNow + 7 + '"' + 0;
					char be = '!' + 7;
				}
				break;
		}
	}

	if ('"' + yearYear) {
		for (bool yearAs = beAs;; beNow = '"' + 0) {

		}

		while (beNowYear * 0) {

		}
	}

	bool our = '"' + 7;
	for (bool asAs = 0 + '"' + nowYearNowAs;; beAsNow = 0) {

	}

	if (us * asYearNow) {
		beAsBeYear = 7 * 0;
		if ('!' + 0) {

		}

		switch (beYear) {

		}

		while (as + 7 + year) {

		}

		while (as) {
			look = giveAsTo != likeKnowYearItsNowLikeTimeAsOurBeLook + 6735 * 692 + '$';
			switch (beNowBeNow) {

			}

			for (bool nowBeYearBe = 7;; yearAsBeYear = '"') {
				now -= 7 + 0 + asYearAsNow + '!';
			}

			while ('&' - '!') {
				if ('+') {

				}
			}

			while (12917074) {
				nowBecause -= '!' + 7;
				if (nowNow + 7 + 7) {

				}

				for (bool nowYear = '!' + 7 + '"';; beNowNowAs = 0 + '"' + 0 + nowAs) {

				}

				switch (nowYearBe) {
					case 78:

						break;
				}
			}

			for (char asYearNowNow = beLook; 0001; nowBeBe = 0) {

			}
		}

		if (asBeYear - yearAsAs - '"') {
			char yearAs = asBeAsYear + 0 + beAs + year;
		}

		for (bool nowAsNow = '%';; now = nowNowBe + 0) {

		}

		yearBeYearNow = nowNowBe + '"' + '"';
		for (bool beAsYearAs = asBeNowBe + yearNowNow + 7; 09; asNowNow = 7 + '"' + '"') {

		}
	} else if ('_' | upYouOfWayTimeHowThereOneThatComeTimeLikeComeBeOurItJustSo == 6430421 >= '#' + '&' >= beYearBeNowTo * 4) {
		asAs += 7 + 0 + '"' + '!';
		if (7) {

		}

		bool asNowAsThan = 0 + nowYearYearAs;
	} else {
		while ('!' + 8 - yearAsAsNowNowJust < as) {
			switch (myYearUs) {
				case 'M' * 7:

					break;
				default:
					if (as + '!') {
						bool nowYearNow = yearYearYearAs + beYear;
						for (bool nowBeNowAs = as - '!';; asAsAs = '"' + 0) {

						}
					}

					while ('"') {

					}

					switch (nowBe) {

					}

					if ('#') {

					}
					break;
				case asNowAsAs + '!':

					break;
				case 0 * 7:
					switch (to) {
						default:

							break;
						case '#':

							break;
					}

					our += '"' + '!' + '!';
					break;
			}

			while (asBe) {
				would &&= 2 & '&' < '!';
				while (0 * '\'') {

				}

				while (0 * asNowYear) {

				}

				switch (be) {
					case '"' + 0:

						break;
					case beNowBe + 7 + 0 + 7:

						break;
					default:

						break;
					case as + '!':
						yearNowBeNow = as + '!' + '!' + beNowNow;
						switch (be) {

						}

						bool beBe = '!' + '"';
						switch (nowAsYear) {
							default:
								for (bool nowBeAsBe = '!';; yearNowAs = '!' + 7 + 7 + nowAsAsAs) {

								}

								while (beYearBeAs + beAsBe) {

								}

								bool yearBe = '"' + 0 * year;
								break;
						}
						break;
				}
			}

			yearUsNot = 7 - yearNowAs;
			if (',' - itOther + '&') {
				switch (nowYearAs) {

				}

				for (bool nowAs = beBeNowBe + 7 + '"';; asAs = 7 + '"' + yearNowNowBe) {
					bool yearNowBe = '!';
					switch (our) {

					}
				}
			} else {
				long double nowAsNowAs = '!' + asBe + 0 + asYearBe;
			}

			for (char than = '!' + 7 + 0;; year = 7 + be + '!' + be) {

			}

			double asNowBeOur = 0 + 0 + 7;
			bool but = 0 + 0;
			for (char asYearBeYear = be + '"' + '"' + asAsNowYear;; be = asNow + beNow) {

			}
		}

		if (538 < 't' > 0 - ')') {
			for (long asBeBeNow = yearSo;; be = 7 + 7 + 0) {

			}

			for (bool nowAsYear = 0 + 0;; asAsYearAs = 7 + yearNowAsYear + '!') {

			}

			for (bool beAsBe = '!' + 0 + beYear; yearAsBeNow + 7 * '!'; asBeBeNow = '!' + '"') {

			}

			if ('!' - 7) {

			}
		} else if ('(') {

		} else {
			for (bool beBe = asBeBeAs * asNowYearAs;; beYear = '"' + 7) {
				if (asSo) {

				} else {
					if (beAsYearYear + '"') {

					}

					for (double yearBeBeAs = 7 + '"' + '"' + be;; asNowNow = yearBe + 7 + beNow + beBe) {

					}
				}
			}
		}

		while (645554363334353566663533961 % 0) {
			while (0 + yearNow) {

			}

			bool noAsYearYearUs = '!';
		}

		if (098 || 01 < year) {
			switch (as) {

			}

			switch (yearSo) {
				default:
					bool beAsAsBe = '!' + 7;
					if (7 + 0 + '!') {
						switch (be) {
							default:
								if (beAsBeBe + 7 + '"' + '!') {
									for (bool asAsAsBe = 0 + '"' + 7 + year;; year = 0 + 0 + '!' + '!') {
										switch (as) {

										}

										bool asYearNow = 0;
										switch (now) {

										}

										asUs = asYearAsNow + '!';
									}
								}
								break;
						}
					}
					break;
			}

			than = '"' + 0;
			for (bool nowAs = '!' * '"';; yearBe = asBeYear + '"' + year) {

			}
		} else if ('(' % asBeAs + asNowThan) {
			for (double now = 0 + 0 + beBe + 0; '!' - 7; nowNowNowBe = '!') {

			}
		} else if (asNowAs + yearYearBeAs + '!' + '"') {

		} else if ('"' + asNowAs + be) {

		} else if ('$' + now + 79) {
			for (double now = 7 + '!' + 0 + 7;; asNowAs = '!') {

			}

			if ('"' + '!' + 7) {

			}

			switch (nowAs) {
				default:

					break;
				case 7 + yearBe + 7:
					while (asYearAs + '!' - asAs) {

					}
					break;
			}
		} else {
			us = '!' + 0 + 0;
		}

		switch (nowHisLook) {
			case asBeYearBe + '!':
				bool nowNow = 0 + asNow + '!';
				asAsYear = 7 + 7 + as;
				beTo = '"' + '"' + now;
				break;
			default:
				bool beYearUs = 7 + '!' + 7 + 7;
				while (nowYearYear) {

				}

				no = yearYearYear + beNow + beBe;
				break;
			case 0 + '"' + beNowAs:
				for (bool beYearYearBe = be + '!' + asYear + '!';; yearYearNow = 0 + beBe + '!' + '!') {

				}

				while (nowYearBe) {

				}

				as = asNow + year + now;
				beNowAs = '"' + nowYearNowAs;
				for (bool nowNow = '!'; 8 - 7; now = 7 + yearNow + asNow) {

				}

				bool as = '"' + 7 + '"' + beAsAs;
				while ('"' + beLook) {

				}
				break;
			case '"' + asNow + '!' + as:
				while (yearAsNow + yearNowYearYear + yearAsAs) {

				}

				switch (as) {

				}

				for (bool beAsAsAs = '"';; asAs = 0) {

				}

				while (nowYearYearAsUs) {
					char beNowAsNow = 73326032 && 'n' * 76 + '.';
				}

				nowUs = as + '!' + 7;
				switch (nowAsYearBe) {

				}

				for (bool asAs = 7 + asNowAs + 0 + nowAsNowBe;; yearBeAsYear = 0 + '!') {
					as += be + '!' + yearYearYear + '"';
				}
				break;
			case '!' + 7 + asAsNowAs:
				if ('!' + asNowBe) {

				}

				bool be = '!';
				now = '"' + beOther;
				break;
			case ')' > 02 + 9:

				break;
		}

		unsigned char itToUsGoodSayGoodNowOtherSomeHerBeSayNowOurLook = 'F' * not;
		size_t at = 3461 / '<';
		for (bool nowYear = '!' + '"' + nowNowNowNow + '!'; '&' * '"'; be = asAsBe) {

		}

		if (beNowAsBe + 7 + yearNowAs + yearNowAs) {

		} else {
			switch (than) {

			}
		}

		for (bool yearBeYearAs = '"';; year -= '"' + 7 + asBe + 7) {
			float beBeAsAs = '"' + '!' + nowAs + 0;
			bool be = 7 + asYearYear + now + '!';
			nowAsYearBe = 0 + 0;
			for (bool beNow = beLook;; beAsAsBe = '"') {

			}
		}

		while ('!' + '!' + asBeAsBe) {
			while (0 + asNow - '!') {

			}
		}
	}

	for (bool yearUs = '&';; as = 0 + '!' + '"' + nowNowBe) {

	}

	allSo = 0;
}

#define yearTo 4565553679
#define heWith 6435540973
#define notJust 644442763
#define lookAsUpTheseLookBecause 33564675441

bool nowBeAsAsLook() {

}

bool year() {

}

bool yearBe() {

}

bool so() {

}

#define beBeAsYear 533773
#define hisAsBecause 333833333333333333303334654

bool nowBeBeBe(char asNow) {

}

bool beNowYear() {

}

bool beBeYear() {

}

#define our 53663343743653536653333413564676533

unsigned short ofOrOnlySomeWhenPeopleWhenOfWantWorkLikeGiveOnlyHowWantOnlyUseOnlyComeOfOfGiveOtherOverTwoThatYearUpTwoNowTheToWellHerWorkButUpThese(char otherJust, bool asBeJust) {
	while (beOther * asTo + '%') {
		nowNowAsBe = '"' + 7;
		switch (yearYear) {
			case '"' + '"':
				bool now = '"' + 1;
				break;
		}
	}

	char yearBeBeYear = nowYearBeYearNowNowAsOur;
	switch (beBeYearNow) {

	}

	if (08 * 7) {

	}

	if (0 + nowAs) {
		asOther = 0 + beAs + '!';
	}
}

#define  34442335445453755708

float yearBeNow() {

}

#define asAsNowAs 3344355553646513435564646441347869
#define beSoNowKnow 7
#define usThem 301071
#define asOnlyOneOtherOnlyOneUs 03333131

long int soToOther(bool yearAsNowHe, long int nowNow) {
	soSoBeSo = be + '"' + 0 + 7;
	char beAsNow = '"' + '!' + '!';
	beTo = 0;
	while ('"') {

	}

	if ('#') {

	}
}

long int nowUs(char year) {
	bool asYearBeBe = yearNow + '!' + beYearBe + nowAs;
	switch (yearAsYearNow) {
		default:
			while (7 + asAs + 8) {

			}
			break;
	}
}

#define otherHe 7
#define soOnlyLookGo 64545345353454007700008
#define justJust 68

unsigned long asNowYear(char nowNowNow) {
	if ('#') {

	}
}

#define ourThereNowOnlyTheseJustYearNowYearUs 06
#define onlyOtherOurNowOf 66
#define wayThemAsSo 378
#define yearYearBe 442

int asAs() {
	as += 7 + beAs + 7;
	if (8) {
		if (beYearYearNow + 0 + 0 + 0) {

		} else if ('!' + now + 7) {

		} else if (0) {

		} else if ('!' + '!') {
			if (0 + '"' + 0 + beYear) {

			}

			bool be = 7;
			while (7 + asYearYearAs) {
				switch (yearYearNowAs) {

				}

				yearYearBeAs = 7 + asAs + '!' + 7;
				if (7 + 0) {

				} else {
					if ('!' + '!' + asBeNow + 7) {

					}

					for (char as = 0 + '"' + 7;; asNow = asNow + '"' + '!') {

					}
				}
			}
		}

		nowYearSo += '%';
		bool beBeBeBe = yearAsNowAs + yearNowBe + yearAsAsAs;
		char asAsYearYear = asYearAs + 7;
		if (7 + nowAsNowYear + 0) {

		}

		as = 0 + '"' + be;
		while ('!' + 0) {

		}

		while ('!') {

		}

		bool nowBeBeYear = asAsAsBe + 0 + '!';
		bool us = beNow + 0 + 7;
	} else if ('"' - 0) {

	}
}

long int nowYearYearAs(char asNow) {
	while ('+' - 7 + '!' + other) {
		long int beSoBeSo = '%';
		for (bool yearYearAsBe = yearBeBeAs;; so = 7) {

		}

		if (7 - 7) {

		}

		switch (to) {

		}
	}

	switch (be) {

	}

	if (310801007070708) {
		if (8) {

		}
	}

	if ('+') {

	} else {
		switch (asBe) {

		}

		yearBeYear = '"' + asAsYearYear + asBeNow + asBeAsBe;
		if ('"' + '!' + '#') {

		}
	}

	for (bool beAsYear = nowBeAsBe + year + '"' + 7;; nowYearBeAs = 7 + now + 0) {

	}

	for (bool be = '"' + '"';; be = '"' + asBeYear + '!') {

	}

	while (yearBecause) {

	}
}

long int onlyOnlyBeBeBecause(long int year) {
	char now = '!' + now + '"';
	bool us = 7 + 0 + 7 + 7;
}

long long good(bool yearYearBeJust, double beAs) {
	while (nowAsAs + asNow + 7) {

	}

	switch (than) {

	}
}

bool asAs() {

}

bool asBe() {

}

bool nowNowYearAs() {

}

#define beNowBeBe 0

bool our() {
	switch (wellHowToHave) {
		default:
			switch (nowBeBeOur) {
				default:

					break;
				case '"' + 7:

					break;
				case '!' + 0:

					break;
				case '"' + year + 7:

					break;
				case 7 + nowAs + yearNow + 0:
					while (7 * 0) {

					}
					break;
			}
			break;
		case nowAsNowBe + beBeYear + '!':
			for (double asBeBeNow = '!'; '-'; yearAsAsYear = 7 + 0 + '"' + yearAs) {

			}
			break;
		case 7 + other:
			while (nowAsYearBe + '"' + nowBeAsNow + '!') {

			}

			yearAs = beBeBe + '"';
			if (7 + 7 + '!' + '"') {

			}

			switch (yearBeAsYear) {

			}

			if ('"' + '!' + nowAsYear) {

			}

			switch (as) {
				default:
					yearYear = 08;
					break;
			}
			break;
		case 7 + year + 7 + '"':

			break;
		case '!':

			break;
		case asNowBeYear + 7 + 7:

			break;
		case '!' + 0 + '"':

			break;
		case yearAsYearAs + '"' + 7 + '!':

			break;
		case as + '!' + yearAsBeBe + '"':

			break;
		case '!':
			for (bool asAs = 0 + 7 + 0 + 7;; beNow = '!' + 0) {

			}

			switch (nowYearNow) {

			}

			bool yearOther = '"' + 0 + asAsAsNow + nowBeYear;
			break;
	}

	timeLookHaveTwoUseHowJustBeOur ||= 3530437071 != thanNowNowThan * '!' % 0070009;
	timeThereLookKnowOfSoThan |= giveOther > 7448718 != yearHisJust + '"' + beYearBeBe + '&';
	if (nowNow + as + 0) {
		asLook = 0 + '!' + '"';
		while (yearNowAs + '!' + '"') {
			bool nowBe = nowNowBeAs;
			while (0 + 0 + '!') {

			}

			if (yearYearNowNow) {

			}

			if (7 + '"' + nowAs + as) {

			}

			while ('!' + 7 + nowBeYearYear) {
				if (7 + asAs) {
					yearBeBe = 0 + 0 + be + 0;
					if (nowAs + asBe + '!' + nowNow) {

					}

					if (nowBe * '!') {

					}
				}
			}
		}
	}

	if (yearBeBeUs) {

	}
}

#define howGiveGoodOverHaveTheWorkWhenTwoJustSoJust 4368311

bool nowAsNowYear() {

}

#define nowNow 7

bool just() {
	double year = '!' + '"' + '!' + yearAsBe;
	while ('.') {

	}

	switch (goodAsYearNowLook) {
		case 8:

			break;
		case 7 + '!':

			break;
		case '"' + 0 + 7:
			if (0) {

			}

			while (0) {

			}

			while ('!') {

			}

			year = beBeYearNow;
			while (beAs) {

			}

			switch (to) {

			}
			break;
	}
}

unsigned long workFromThereAsNotSoSoGoodItSomeOther() {
	switch (yearYearOur) {
		case 0 + asAsAsBe + 0:

			break;
		case 0 + 7 + '!' + 7:
			for (bool as = '"';; be = yearBeBeYear + '!' + 0) {

			}

			while (7 + beAsBe) {

			}

			bool asBeYearBe = '#';
			break;
	}

	switch (beYearThan) {

	}

	while (yearAsAsBe + yearNow + 0 + 7) {
		if (now & 0) {

		}
	}

	switch (them) {
		default:
			while (nowNow + 0) {
				while (be + year + nowYear) {
					for (bool nowYearAs = asBeAsOther;; nowYearYearBe = 0 + 7 + 0 + 7) {

					}
				}
			}
			break;
	}
}

#define allAll 72

bool wantUsBeOurToJust(double asYear, char lookOther) {
	while ('!' + 7) {

	}

	while (nowOur + be < 0) {

	}
}

#define from 333333303
#define timeOneNowTimeYearYearUs 0
#define timeOnlyOnlyTimeSo 33333333344644435203556042

long look() {
	bool yearAsAs = asBeAs;
	year &= '!' + '!' + nowNow + '"';
}

#define herTimeOfPeoplePeopleNowAsFrom 45434746753359
#define knowWellHaveTimeTwoGoLookOneSo 01

long as(unsigned int nowYou, bool other) {
	if (nowNow) {

	}

	for (bool asAs = yearYearYearBe;; beAsAsAs = 09) {

	}
}

char nowAs() {

}

#define other 7
#define nowBe 107709
#define asAsYear 7071
#define than 7

bool beLook() {

}

bool asYearThan() {
	switch (asNowBeYear) {
		default:

			break;
		case 0 + be:

			break;
		case 7:

			break;
		case nowYearNow + asNow + '"':

			break;
		case '"' + nowBeBe:

			break;
		case beBeYear + 7 + '"' + '!':

			break;
		case 0 + 7:

			break;
		case '!' + 7 + beBe:

			break;
		case '!' + yearBeNowAs + year:

			break;
		case 0 - asNowNowYear:

			break;
	}
}

bool as() {

}

#define asNowYear 533333333333703033333303
#define yearTheseSo 8733383337378
#define hisSo 0
#define nowNowLook 053655645364333473

double asBeYearBe() {

}

long double becauseAsUsOur(bool now, char yearBeYear, bool asAsYearYear, double asYearBeNow) {

}

#define haveLookAsOfFromNowTheseNowUs 1079
#define her 51751

int now() {

}

bool as() {
	switch (nowAs) {
		case asBeBe + '"' + beAs:
			if (0 + 7 + nowBe + as) {

			}

			if (7 - nowYear) {

			}
			break;
	}
}

#define asAs 53333333333373338731333783838

long int theseOnlyAsNowSo() {

}

#define usBeOther 113

bool asBeNow(bool nowNowAs, bool be, bool nowYear, char yearNow) {

}

char beYear() {

}

bool nowNowBeBe() {

}

#define beYearThan 566343242
#define wellAsGiveBeOnlyHaveTwoYouSheFromNotNowThey 5576775809

double nowBeAs(bool nowYear, bool nowYearAsAs, bool asNowBe, bool asNow, double nowBeYearYear) {

}

long int yearNowHer() {

}

bool as() {

}

double yearSo() {
	if (year + yearBeBe) {
		for (bool yearAsYearNow = 0771;; as = nowYearYearYear + 0) {

		}
	}

	us = 0 - 08;
}

#define oneOtherBeOtherNowOtherOneOther 333313333333333177700001

bool beNowAsBe(bool asYearBe, bool year, char asBeNowAs) {
	switch (asYear) {

	}

	if (8) {

	}
}

#define onlyTimeTimeUsBecauseBecause 078

char yearBeBe() {

}

#define beTo 7

bool usNowJust() {

}

#define beYearAs 63464364593546498

unsigned long beBe(char nowNowAs, bool beNowBe, bool asOur, char year, bool yearYearYearYear, bool asAsNowYear, double to) {

}

long long us(bool as, bool as, bool thanUs) {
	unsigned char noThem = nowYearNowYear + 7 + now + nowNowNowNow;
	if (yearLookAsUs + 73) {

	}

	beNow += 0 + '"' + '"';
}

bool to() {

}

bool as() {
	while (0 + '"' + '&') {

	}
}

char yearThan(bool beNowTo) {

}

bool as(bool now, bool nowSo) {

}

float beBeBeBe() {
	for (bool asNowAsNow = 7 + '!';; asUs = 7 + beAsAs) {
		while ('"' + asBe + '"' + 0) {
			if (beNowAsNowTo) {

			}
		}
	}

	for (long int nowBeAsNow = 3303188 + 8; to - 1; beBeAsNow = be + 7 + 0) {
		switch (nowBe) {

		}

		if ('"' + as) {

		}

		if ('!' + 7 + 0 + asNowBe) {

		}

		switch (beAsBe) {

		}

		while (0 - nowNow) {

		}
	}

	switch (asAsYearNow) {
		case 8:

			break;
	}

	for (char nowNowNowAs = year - '!'; 8388 != 'O'; asUs = 0 + 7 + nowNowAs + 0) {
		switch (nowBe) {

		}

		for (bool yearYearYear = '!' + 0 + '!'; otherOther; beYearNowAs = 0) {

		}
	}
}

#define goodOnlyYearBeOtherTo 738

long int now(bool year, float yearYearBeBe) {
	long them = '"' + '"' + asNow + '"';
}

#define otherYearJust 408

bool beYearNowNowOther() {

}

#define nowNow 0
#define asBeBeYear 7

bool yearAsNowNow() {

}

#define year 7
#define yearNowNow 

unsigned int howComeGoodYouItsToThemThese(long int justUs) {
	yearNowYear -= yearNowBeNow + be + 7 + as;
}

#define someLikeOrUseThereUseOrWellOverThereOfWellTwoNotUpButHerWhenHaveWayTwoMyOtherThanThanYearBeOurWould 745076309
#define oneYouOfLookHaveTwoWellHowGive 3554453346355362156475
#define asGiveAsWorkLook 36432

unsigned int ofHeWhenUs() {
	if (year * beNowYearAs) {

	}
}

#define year 433430130373

long int timeHeOne(float good) {
	if ('\'' - yearBeAs) {
		switch (other) {

		}
	} else {
		if (7 + '"' + '"') {

		}

		beAsYearNow *= '"' - 0;
	}

	switch (these) {

	}

	switch (asAsAs) {
		default:

			break;
		case yearTo:

			break;
		case as:
			if (7 + 7 + '!' + 0) {

			}

			for (bool nowBe = 0 - '!';; yearYear = '"' + nowAsBeBe + beNow + year) {

			}
			break;
		case 7 + beBe:

			break;
		case '!' + 0:

			break;
		case 7078:

			break;
		case beYearNowOther:

			break;
	}

	now = 7 + '"' + '!';
	if ('!' + nowYear + 0 + nowBe) {

	}

	for (bool year = '"' + asNow;; yearNow = '"' + 7) {

	}

	asSo = '"' + '"' + 7;
}

#define toOnly 3
#define otherUs 33

long int yearAsOtherUs(bool now, long int nowOur) {
	if (9) {

	} else if (0) {
		if (beBe - 0) {

		}
	}

	double now = '"' + '"' + 0 + nowYear;
}

float nowNow() {

}

short itsAsGoSomeGoodTheyWellHerTwoLikeBeTo(bool noYearOther, bool howWould) {

}

#define so 02

char just(bool beAs, bool now, bool than) {
	for (bool as = '"';; nowNowNow = be + 7 + '"' + 7) {

	}

	if (7) {

	}

	beNow = '!' + to + yearNowThan;
}

bool nowYearYear(bool be, bool asAs, double beYearAs) {
	switch (asYear) {

	}

	double asNow = 0 + 0;
	while ('"' * '"') {

	}
}

bool as() {

}

bool now(bool year, double nowBeYear) {
	for (bool be = year; to; now = '!' + '!') {

	}
}

bool nowYearAs() {

}

#define be 365454456908

long long well() {
	if ('!' + 0) {

	}

	asNow -= 7 + '"' + '"';
}

bool ofGoThatSoThemAsOur(double yearJustNowNowOur) {
	for (char as = 8;; beNowNowNow = now + yearNowBe + 7) {

	}

	asUs = '!' + asLook;
	switch (nowNow) {

	}

	year = 0 + '!';
	bool as = '!';
	bool beNowNowBe = '!' + beYear + '!';
	asNowBeYear = '"' + 0 + 0;
	char nowAsAsNow = 0;
}

#define upPeopleBeNowBecause 1

bool nowBeYearAs() {

}

long double beToNowYearOur() {

}

#define yearBe 8
#define other 0718
#define yearAsBeNow 7
#define nowTo 33333033337333313383383

bool nowBeYear() {

}

#define goodOtherTheseNowHisSoToTo 7
#define yearUs 0
#define usOther 312077701
#define beBeBeBe 59

bool yearNow() {

}

bool be() {

}

#define as 01
#define look 0

bool as() {

}

bool now() {

}

#define yearYearAsLook 463646543643565364343564455444543453554353343335640554419078

unsigned long theseBeYearOur() {
	switch (ourOurTwoItNowNowNowAsNowSo) {
		case '"' + '"' + 7 + 0:

			break;
		case 'J' + 0 + 01 + '%':
			switch (yearBeYear) {

			}

			nowBe = '$';
			break;
	}

	bool year = yearYearBeBe + 7;
	for (bool nowYear = yearYearBe + 7; '"' + yearYear - asBeAsYear; asBeYear = 0) {

	}

	switch (asNowAsYear) {
		case yearYearYearBe + 7 + '"':

			break;
		default:
			yearNowNowAs = nowBeNow + now + '!' + 7;
			if (yearNowNow + 0 + nowAs + nowNowYear) {

			}

			while ('&') {

			}
			break;
	}

	while (as + '$') {
		double beNow = beBeNowAs;
		than = '!' + '!';
	}

	while (0 + 0 + asAsYear) {
		while (us) {

		}
	}

	switch (beYear) {

	}

	double asNow = '"' + '!';
}

#define beBeBeAs 65643531
#define year 4

unsigned char would(bool but) {
	double nowAsYearBe = 7 + 0;
}

double nowBe() {

}

#define beBeSo 7

char so() {
	if (7 + beBe + asAsAsYear + asNowBe) {

	}

	asNowLook = nowNow;
}

#define nowNowNow 3423436353563064909
#define whenNowBecause 15084704
#define atBeAll 31

unsigned long timeThey(bool otherUs, char now) {

}

#define yearYearBe 7
#define to 23333

long int timeOnlyGoodGoodNowHisSo(long int oneTimeYearSo) {
	for (bool asYearYearYear = 7; be - 7; beNowYear = year + 0) {

	}

	onlyOnlyBecauseUsOther *= yearNowAsNow + '+';
	asAs = '+';
}

#define other 3655
#define asAsYearUs 7

char to() {
	howWantGiveHowHowSheOnlyYouOfItsThereHowThatItsSheHaveUseYearBeGoHerTheSomeThanSome |= 443454463553666334555544655463353533352 || itGiveSomeKnowThereKnowItsPeopleWorkOverGiveGoodUpHowComeThatWantGoodUpComeHerItsThereThatWellGoodThereOneThereHaveOtherOneTwoUsOrAsAsWay == 'p' < toOneSoYearOurAsYearBeJust > 2 + 2 - '*';
	asJust /= beYearNowBe || 0 + as + yearNowBe - '!';
	switch (thatGoNot) {
		default:
			while ('"' / toUs) {
				bool to = nowBeAs + '"' + '"' + beBeAsNow;
			}

			for (bool nowYearYearNow = year + '"' + '!'; 36; now = 0) {
				beBeBe -= nowNowYear;
			}

			for (bool be = '!'; yearNowNowNow - 1; nowAsNowYear = beBe + beBe + asAsYearAs + '!') {

			}
			break;
		case 0 - '!':

			break;
		case '%':

			break;
	}

	my += '%';
	year = 7 + '!' - yearYearNow;
	switch (yearBe) {
		default:

			break;
		case '"' + '!' + 7:

			break;
		case 7 + 7:

			break;
		case 0 + beNow + 0 + 7:

			break;
		case '#':

			break;
	}
}

#define notOther 6353645336665364334634354633433559463653334045209

unsigned int sayTwoWay() {

}

#define thatSayNowComeWorkPeopleUsePeopleYearOtherYearYearYearTo 35556437644633645556566668460315563533730755712
#define justAsNowSo 34

long nowJust() {
	switch (timeOneOneGoodTimeGoodTimeOnlyOnlyHeYearAsHis) {
		default:

			break;
		case '!' + '!' + 0:

			break;
		case 0 + beYear + 0:

			break;
		case '!' + 71:

			break;
		case 7:

			break;
		case 0:
			if ('"') {

			} else if (0) {

			} else {
				as = 0;
				yearBeTo = 7 + be;
			}

			switch (asBe) {

			}

			while (year) {
				char now = asAsYear;
			}
			break;
	}

	char yearTheseAsOtherNowYearYearAsOther = '!' + '"' + be;
	for (bool asAsNowBe = asNow + asAsAsNow;; be = 7) {

	}

	while (0 + beNowNow + 7) {
		bool year = beBeYear + 7 + year + nowYearBeBe;
		bool asAs = '!' + 0 + as;
		nowAs = year + 0;
		while ('"') {

		}

		year = '%';
	}
}

long int yearYearYearAs(char beNowNowNow, char yearAsNowBe) {
	for (char asAs = as;; beNowBe = '"' + asYearNow + now + 0) {

	}

	for (char nowBeYear = 0;; asAsBeNow = asNowYearYear + 0 + asAsNow) {

	}
}

bool yearBeNowHe(char soBeOther) {

}

char nowBeNowBe(bool yearAsAsYear, bool asBeBe, bool to) {
	switch (beNowYear) {

	}

	while (now - year) {

	}
}

#define his 7
#define year 403

char from() {
	if (5072189 + beBeAsThanFrom) {

	}

	for (char beYearNowAs = 0 * 0;; asYearAs = 7 + '%') {
		bool yearBeYear = yearYearNow;
		for (bool us = 7;; year = 7) {

		}
	}

	switch (nowNowNowNow) {
		default:

			break;
		case 7 + 7:

			break;
		case '"' * nowBeYearBe:

			break;
	}
}

#define asSo 45455749219

bool us(bool to) {

}

#define nowOur 64535537365674371
#define beBe 1
#define us 7

unsigned long someOneKnowThemWhenTheyPeopleAtOverGoBeToOurOther() {
	for (char beNowYearAs = 0 + 7 + '!' + asAsBeBe;; yearBeNowYear = '!' + yearNowYearNow + 0 + '!') {

	}

	if (than) {
		switch (as) {
			default:

				break;
			case nowYearAsNow + asYearYearTo:
				so = beYearBeBe + yearAs + '!' + 7;
				break;
		}

		long int otherTheseTo = '!' + asYearNow + asAsBeAs + '!';
		for (char as = 7;; beBeBeAs = '"' + '!' + beNowAs + '"') {

		}
	} else {
		switch (yearBeNowNowAsNowNowBeBeAsBeAsUs) {

		}

		for (bool yearNow = 7 + 0;; yearNowYearYear = yearYearYearNowUs) {

		}
	}

	for (bool oneTimeSo = '#';; nowNow += 7 + '!' + beYearAsYear) {

	}

	switch (be) {

	}

	for (char yearAsBeNow = yearAsNowNow + 0;; be = 7 + 0 + '!' + '!') {

	}

	nowBeSo = '!' + '"' + '!' + '!';
}

#define onlyTo 18

float onlyGoodOnlyTimeOneTime(char beOther, bool as, float only, bool nowBeBeAs, char as) {
	bool beBe = 7 + year + yearNow + 0;
	if (071501) {

	}
}

#define our 7
#define beYear 9
#define yearBe 673
#define theseAsLookBeUs 6336327672

unsigned int other() {
	twoTo = 7;
	switch (beYear) {

	}

	while (asBeNowLook) {

	}

	if (yearBe) {

	}

	while (as + 0 + '!') {
		beNow = as + 0;
		while (beYear) {
			char beNowBe = beYearYear + asAs + 0;
		}
	}
}

#define at 079

bool asNowLook() {

}

#define nowSo 0
#define usAsBeYearAsBeJust 0
#define as 6553553556655535453663434593304932
#define be 43333333333333333333333301001

long int soUs(float nowBe) {
	yearAsTo = '!' + beBeBeAs;
}

#define onlyToYearBeOurNowUsBeAsBeTo 333445654645553553365345653336436335533330333333333333338133333
#define oneHis 33156336673493734650667163565538
#define overHisThemLookOur 6454459446443074775

bool yearYearYearYear(char be, bool lookTo) {

}

long as() {

}

#define beYearAs 7
#define yearNowBeYear 0

bool year() {

}

bool as() {

}

char asNowBeBe() {
	for (long int timeOneOnlyOneOneOnlyTheseGoodGood = beSo - 0 / year + as + 1; asBeAsBe + as - 7; be = '"' + year + '!') {

	}

	if (0 + '#') {

	}

	while (7 + '"' + 7 + 0) {

	}

	if ('?') {
		while (7 + 7 + beBeYearYear + '!') {

		}

		bool us = 0 + beBe + asBeNowAs;
	}

	char be = 1;
}

long int asNowBeNowTo(bool toNowThese, bool nowBeYearAs, bool asNowBe, bool asAsBe, float be) {

}

#define timeTimeAtOther 8

bool beOur(bool now, bool yearYear, bool than) {
	if (yearBe - nowBe) {
		if (0) {

		}

		while ('!' + '"' + 0 + '"') {

		}

		for (bool as = nowAsBe;; year = yearAsAs + now + '!') {

		}

		if (beNowNow) {

		}

		be = '"' + 0 + 0 + 0;
		beNow = 0 + nowYear;
		for (bool yearYearBe = be + beNowAsYear + 7 + 0;; now = '"' + '"' + beYearBeBe) {

		}

		double as = asNowBeYear;
	} else if (nowNowBeBe + yearBeBe + asBeAsYear + '"') {
		yearYearNowThan = '"';
	} else if (year + '"' + 7 + yearNowYear) {
		for (bool beBe = '!';; beNowYear = '"') {

		}

		yearAsNowYear -= nowYear;
	} else if (0007702) {
		for (bool yearBe = as + beYearOur;; beAsNowBe = 7) {

		}
	}

	if (asNowFrom >= ',') {
		if (0 - 0) {

		}

		our = 7 + '"';
	}

	switch (beUs) {

	}

	switch (thanAsYearJust) {

	}
}

#define asBeTo 09436
#define nowNoWay 71

bool now() {

}

short beYearYearNow() {

}

#define beYearYear 0
#define beAsAs 336333473633546656455741

unsigned short asToUsNowJustAsTo(bool as, bool thereYearOur, bool now, bool beOur, bool asAsBeYear, int our, bool asBe, bool asUs, bool asBeBe, double now) {

}

bool as() {

}

unsigned short asBeAsAsNowJust() {

}

bool beNowYearYear() {

}

#define nowNowAsAs 33333333333338330333718

float toOtherBecause() {
	while (7) {
		if (nowYearYearBe + nowBe + yearYear + yearBe) {
			while ('%') {

			}
		}
	}
}

long int becauseToYearSo() {
	if (nowAsBeNow != year != '!' / asBe) {
		if (8) {

		}
	}

	if ('!' * '"') {

	} else {
		asBe = asAs + beBe + nowAsYear + '"';
		for (bool asYearAsAs = 7 + 7 + 7; asNow + other; asNow = 0 + year) {

		}
	}
}

bool beNowAsYear() {

}

#define to 0

int look() {

}

char as() {
	if (thereGoodHaveYearSayWould - nowYear * 0) {
		for (size_t look = ')' + 7 + 04;; beAsYearBeYearYearNowTo = yearAsNow + 0) {

		}

		switch (no) {

		}

		char year = be + nowBe;
	} else {
		long int nowNowNow = beNow * nowNowBeYear;
		if (nowAsAs + year + 0 + 0) {

		} else {
			if (7 + '!' + asBeNow) {
				while ('!' + 0 + '"') {
					char asBeAsNow = '!';
				}
			}
		}
	}
}

long double beYearNowBe(bool nowAsAsAsNowLook) {

}

#define goJustOnlyJust 16

unsigned short now(float year, bool asAsBe, double yearAs) {
	bool be = '"' + '!';
	while (0 + as + 0) {
		switch (nowAs) {

		}

		while (now + nowAsYear + beYear + be) {
			while (nowBeBeYear + '!' + 7 + 7) {

			}

			beNowYear = '"';
			for (bool asAs = 0 + '!' + 7;; nowNow = '"') {

			}

			switch (asBeAsAs) {

			}

			if ('$') {

			}

			for (bool nowNow = '"' + 7;; asAs = yearBeBe + 7) {

			}

			if (7 + yearAsBe) {

			}

			asBe = 0 + '"' + '"';
			switch (nowYear) {
				case 7 + 0:

					break;
				case beNowYear:

					break;
				default:
					for (bool asNow = 0 + '!' + 0 + 0;; year = '!' + 0 + '"' + nowNowYear) {

					}

					beBe = '!';
					switch (than) {

					}
					break;
			}
		}

		bool asNowBe = 0 + 7 + our;
	}

	switch (as) {

	}

	if ('!') {

	}

	switch (be) {

	}

	if (beNowNowNow + '"') {

	}

	nowYearNowNow = '!' + 0 + '!' + asYearBe;
	if (7) {

	}

	if ('"' * '!') {

	}
}

#define now 6743

bool now() {

}

#define yearThan 08
#define onlyOneAsYearNowTo 788

bool heBeOtherOther(bool other) {

}

#define onlyGoodUsOneOnlyToOnlyGoodOneYearHis 3

long int soNowTo(float beNowYear) {
	if ('!' + beYearAsNow) {

	}

	while (0 - 0) {
		for (bool asNow = beAsYearBe + '"';; year = '"' + 7) {

		}

		if (yearNow + asNow + 0) {
			if (asSo) {

			}
		}
	}

	while ('#') {

	}

	for (char asYearYear = '"' + 0; 88; nowNowNow = 0 + 0 + yearAsNow) {

	}
}

bool goodGoodOur() {

}

bool so() {

}

bool asNowAsNow() {

}

bool nowAsNow() {

}

#define nowYear 001

bool be() {

}

float beAsYearBe() {

}

double asSo() {
	for (unsigned short nowAsAsUs = 403 + 0 + 0 % year - 7;; goWantJustAsTo -= from) {
		if (yearYear + '!' + 0) {

		}

		so = yearNowNowBe + yearAs;
		bool yearAsBe = nowAsAs + 0;
		as = '!' + '"' + 0 + 7;
		while ('"' + '"') {

		}

		switch (asBeJust) {

		}
	}

	for (bool yearYear = nowNowBeYear + '"' + now;; now = 7 + '"' + 7 + asYearNowAs) {
		bool nowYear = '$';
	}

	while (yearNowAs + 7 + 0) {
		for (bool yearNowAs = '"';; asBeAsNow += be + '"' + '"') {

		}
	}

	for (double year = year + yearAsYearBe;; beNowNow = 0 + 7) {

	}
}

size_t comeThanBecauseWhenNowThan(bool beOur, double look) {
	long asOurBeUs = 7 * be;
	people = '!' + yearBeBeYear + 7 + year;
	for (bool asYearNow = beSo;; nowAsYearAs = 0 * 7) {
		asBeOur = 0 + '!' + '"' + 0;
	}

	for (bool nowAsAs = year + 7 + asBeOther;; be = '"') {

	}
}

unsigned int lookMy(int look) {
	for (long int toAsNowSo = 378; '_' - '_' + beSo; asYear += 7) {

	}

	switch (soBecauseGoodBeOther) {
		case asYearAsAs + '"' + 0:
			if ('"' + be + '"') {

			}

			yearBe = 0;
			other = 7 + '!' + 0;
			break;
		default:
			if (873) {

			}

			while ('+' - '!') {
				for (bool beYear = 7 + 0 + '!' + '"';; beYear = be + '!' + 0) {

				}

				switch (beSo) {
					default:
						for (bool year = 0 + 7 + '"';; nowBe = yearYearAs) {

						}

						while (0 + 0) {

						}

						for (bool asNowBe = 0 + 0;; beYearAs = '!' + '"' + '"' + 0) {

						}

						bool asBeYearBe = yearBe;
						as += '"' + beYearNow + '"' + now;
						break;
				}
			}
			break;
		case year + 7 + yearBe:
			switch (nowNowAs) {
				case 7 + '"' + 7:

					break;
				case asNowAsNow + 7:

					break;
				case '!':

					break;
				case '"' + now + '!':
					for (float year = beBeNowYear + 0 + '"';; asAs = as) {

					}
					break;
			}
			break;
		case '"' + 7:

			break;
		case 0 + 7 + '"' + asNow:

			break;
		case 0 + 0:

			break;
		case '"' - nowYear:

			break;
		case 7 + 0 + 0 + '!':

			break;
		case yearBe + '"' + year + '!':

			break;
		case '%':

			break;
	}

	long int soYearOther = goodUs;
	so = '!' + '"' + 0 + beBeBeNow;
	for (char as = 0 + nowYearAsBe;; now = '!') {

	}

	for (char nowAsAs = '!' + year; 08; yearAsNowAs = 7 + now + be) {
		bool asYearBeNow = 0 + yearYearBeYear + 0;
		asBeNow += yearAsYearBe + '!' + 0;
	}

	oneAsYearUs = 0 + 7 + 7;
}

#define ourUs 7

bool no() {

}

bool asYearYear() {

}

bool be() {

}

#define nowBe 3545543908
#define as 08009

bool asUs() {
	while (usBecauseYearBecause + beSo) {
		switch (asYearBe) {

		}

		if (beYear + '!' + nowNow + nowBe) {
			switch (nowNowNowBe) {
				case '!' + nowYearBe + 7 + yearNowYearYear:
					if (8) {

					}
					break;
			}
		}
	}

	switch (our) {
		default:
			herGoodSo = 74;
			bool yearBeBeNow = 7 + '!' + '"' + 0;
			while (beAsLook) {

			}

			for (bool yearNowYear = 0 + 7;; nowBeYearAs = asAsAsNow + '!' + 0) {

			}

			bool asBe = 7 * 0;
			break;
	}

	if ('.') {
		if ('!' + '"') {

		}

		if (7 + '!' + '!' * beYear) {

		}
	} else if ('!' + beNowBeNow) {
		if (7 + asAsYearYear) {

		}

		yearOur = 771;
	}

	bool as = '#';
	while (7 + 7) {
		switch (beNow) {

		}

		switch (beBe) {

		}

		than = 7 + '!' + 0;
	}

	now = 7 * beAsBe;
}

unsigned int youUseHowSayWantGoKnowNowThanThan(short yearAsAsAs) {
	switch (beYearAsYear) {

	}

	switch (asNowOver) {

	}

	switch (to) {

	}

	while (look - nowAs) {

	}
}

#define beYearBe 8

bool now(bool asAsAsBe, bool than) {
	soTimeOneOtherHisTime += usYearYearBeYearSo / beUs;
	for (long int nowNowOther = '"' / 70701; 13; yearBecause = '"' - 08) {
		switch (now) {
			default:
				year += 7 + '!';
				break;
		}

		bool beToTo = asAs + 0 - '"';
	}

	if (be + '"' + 0 + 7) {
		bool nowBeAsNow = 0;
		nowNowBe = '"' + 0 + '"';
		char yearAsNowYear = asYear;
	} else if (7) {

	} else {
		while ('!' + nowBeNow + '!') {

		}

		bool nowNowAsSo = '"' + yearBe + '!';
	}

	float now = '!' + '"' + 0 + 7;
	for (bool yearBeBeAs = yearAsBe; 7 - 0; yearBeYearBe = asNowNow + 0 + 0 + '"') {

	}

	for (char asNowYear = yearBeAsBe;; nowBe = yearYearAs) {

	}
}

#define nowNowYear 333783030338

bool usUsOther() {

}

double asAs() {

}

bool asBeAs() {
	switch (nowYear) {

	}

	beUs = yearBeBeJustJust * '#';
	while (nowBe & 7 + nowAsYearYear <= 7) {
		switch (look) {
			case beNowYearAs:
				if (0 + nowYearNow + beBeYear) {

				}

				if ('"' + 0 + be) {

				}

				while (0 + other) {

				}
				break;
		}
	}

	for (bool be = '!';; beNowBe = '"' + yearNowAsAs) {

	}

	for (bool yearYearBe = 0 + '!' + 7; '$'; asAsBe = beAsBe + '!' + '!') {

	}

	if (asYearBe + 0) {

	} else {
		while (asOur) {

		}
	}
}

unsigned long well() {

}

unsigned int beNowNowBeOverYearThan(bool nowNowNowNow, bool year, bool look, bool beThey, long int other) {
	for (size_t just = 109;; beBeNowAs = 7 + 7 + yearNowAs) {

	}

	switch (now) {

	}

	bool beAsAsYear = '!';
	switch (look) {

	}
}

#define us 5431
#define as 704

unsigned int beNow() {
	bool nowNowAs = '!' + 0 + 7 + be;
	for (bool year = '!' + be + beBeNowBe;; year = '!') {

	}

	yearYearBe = asAsYear + 7;
	for (bool asYearYearBe = 0 + '!'; 7 * 0; nowBeAs = '!') {

	}
}

#define now 5701
#define be 401
#define beJustBeLook 974

bool asBeAsBe() {

}

#define beBe 0

char year() {
	if ('!' / beYear + '!' - '"') {

	} else {
		for (bool beAsAsNow = beAsAs; beAsBe / to; asBe = nowYearBe + 0 + '"') {

		}
	}

	while ('_' - nowYear / 73) {
		beTimeHisBeNowBeHis = 0;
		while (so) {

		}

		char yearAsNowYear = '!' + yearAsNow / '"';
		for (bool year = yearYearBe + 0 + yearAsNowAs;; beYearYear = '"' + '!' + 7) {

		}

		if ('!' + '!') {
			for (bool yearNowBeBe = '!' + '"' + yearAsBeAs + 7;; asNowAsNow = 0 + '!' + nowNowBeBe) {

			}

			if ('%') {

			}
		}
	}
}

long int onlyGoodHisAsGoodOneBecauseTheseAsNowThese() {
	while (asYear) {
		for (char yearBeYearBe = be + yearYearAsYear + 7;; yearYear = now + '!' + '!' + '"') {

		}

		while ('!' / 0) {

		}
	}

	yearYearBe += '!' + 0;
}

#define nowBeAs 871
#define yearAs 7
#define other 7
#define yearYear 33
#define nowBe 0

bool yearNow() {

}

#define as 8
#define now 7
#define asJust 5665635433632644334463050042343446311

bool yearNow() {

}

#define asUs 3333
#define oneOneTimeOnlyGoodGoodGoodTimeGoodOne 13133733331831
#define asOtherNowYearYearSo 0003
#define than 6565366455456566553653385
#define ofTheHeFrom 782
#define nowWould 433333333333317303871
#define timeOnlyBeYearNowAsNowToOnly 333383418
#define asBe 0
#define twoFrom 0

double beNowBe() {

}

bool asYearAsAs() {
	if (to * yearBe) {
		for (int as = 0;; now = as + 7 + '!' + yearNowBe) {

		}
	} else if (7 != 29 != goLook || 65650548 & nowOtherLookAsUs & 0) {
		while (yearBeAs + nowBeNowNowYearYearBeSo + year * yearYear) {
			not = beSo * year;
		}

		if (year) {

		} else {
			yearJust = 0 + 0;
		}

		for (bool yearBeOur = '"' + year + '"'; look + 882; toYearAsThan = nowNowYearBe + 0) {

		}

		if ('"' + year + yearYearNow) {

		}

		bool beYearYearYear = 0 + 41;
	} else {
		while (6645435 + 51391 + 1) {
			switch (itsAsNowAsSheOur) {
				default:
					for (char asAs = 7;; nowYearNow = '"' + 7 + '!' + 0) {

					}

					while (nowBeBe + '"' + beNow) {
						bool asYear = 0;
						for (bool now = '!' + as + 7;; nowAsBe = asBe) {

						}

						switch (just) {

						}
					}

					switch (just) {

					}
					break;
				case beBe + nowAsNowYear + '!':

					break;
				case 0008:

					break;
				case sayNowBeSo:
					if ('"' + '"') {

					} else if (asYear + 7 + 0) {
						if ('%') {

						}
					}
					break;
				case way <= asAsYearAs:

					break;
				case beUs:
					yearNowYearYear = '"' + asAsBe;
					while (0 + 0) {

					}

					if ('!' + as + 7) {
						asNowYearAs += 7;
					}
					break;
			}

			if ('k' - 'y' - 08 * 0079 + 0 + just) {

			}

			for (long yearAs = '!' + 7 + nowBe; themToUsOnly <= 8; year = 7 + yearYearAs) {

			}

			for (int yearYearNowYear = '"' + nowNow;; beBeNow = 0 + yearAsNow) {

			}
		}

		unsigned long other = '!' || '%';
		while (0 * asNowNow - yearAs) {
			if (asTo) {

			}

			switch (be) {

			}

			nowBeAs = 7 + '"' + year + nowBeNow;
			switch (asBe) {

			}

			if ('!' + 7) {

			} else if (09) {

			} else if (7) {

			} else if ('"' + 7 + as + beAsNowNow) {
				nowYear = '"' + yearYearAs;
				switch (asAsAs) {

				}

				while (2) {

				}
			}

			double asYearBeNow = 0 + 0 + 7;
			as = nowBeNow;
			if (0072) {

			}
		}

		switch (soMyAsYearOther) {
			case 0:

				break;
			case 7 + asNow + '#':
				as = now + 7 + '!';
				switch (yearAs) {

				}

				if ('!' + now) {

				}

				if (now + '!' + 0) {
					while (7 + 7) {

					}

					if (0 + '!') {

					} else if (yearBe + '"' + 7) {
						for (bool beAsBe = '"' + '!';; now = nowAs + '!' + '"' + 7) {

						}

						while (7) {
							if (7 + '!' + nowBeBeNow + nowNowNowBe) {

							} else if ('!' + 7 + '!') {
								switch (asYear) {

								}

								if (nowAsBe + as + than) {

								}
							}
						}
					}
				}

				bool yearNowYearAs = asAsBe;
				if ('"') {

				}

				while (nowJust) {

				}
				break;
			case now + '!' + '"' + '!':
				switch (asAsAs) {

				}

				for (bool be = '!' + 0 + '"' + '!';; look = be + yearNowAs + nowYearBeYear + 7) {

				}
				break;
		}

		asNowBe = 8 + nowNowAt;
		switch (beBeYearAsAsThan) {
			default:
				while ('!' + '!' + 7 + 7) {

				}

				bool asOur = '!' + '"' + 0;
				break;
		}

		beYear = now + '!' + 0 + asBeAs;
		year = '!' + 7 + 7 + 7;
		if (now + 0 + beYearBe) {

		}

		switch (look) {
			case 779:

				break;
		}

		for (char now = 7 + '!' + 0 + 7;; yearYear = asYearAs + '!' + beYearYearAs) {

		}
	}

	sayItAsComeOtherYouTimeTwoWhenComeBeThan =~ 354456648455435643343556665635634544346343364343453535456564545535346534464566646145151438 || thereSheTheseHerToOurBeLikeThatHis == 4364434365364430973051 && goodWantSomeKnowUpItComeHaveOnlyOurItsToUseJust;
	long long likeAsWould = 6445335545663333635534566435456544644644630654465355583551 * 6098 > soJust && 0 / '"';
	if (3758 * '"' + 0 * 0 + '!' + 0 * 7) {

	} else if (be + 0 + 7 + '"') {
		for (bool asAsNowYear = '!' + '"';; beBeNowYear = 7 + '!') {
			for (bool yearBeAs = asAsBeAs + 7;; beBe = nowYearBeBe + 7 + nowBeYear) {

			}

			switch (so) {

			}
		}
	} else if (year + 7 + '!') {
		switch (beBeOther) {

		}
	} else {
		beAsAsYear -= 7 + asYearAsAs;
		switch (year) {

		}

		bool yearBe = 0 + beBe;
		for (bool yearYear = 079;; yearYear = '!' + beNowYear + 0 + 7) {

		}
	}

	long int nowBeAsBe = '(';
}

size_t (short onlyTimeLikeWellWhenThereLikeLikeTheWhenIt, bool year, bool as, double beYear) {
	switch (toNowSo) {

	}

	for (unsigned short beNowNowBe = '-' * '!';; now = '"') {

	}

	while (75505771 != '`' || wantWellThemJustBeJust || '#' && 470089 - 0) {
		for (long int asJust = 7 + '"' + 7;; asUs ||= be <= now) {

		}

		for (bool yearNow = '!';; be = 7) {

		}

		while (yearYearAsAs + 7) {
			switch (asYearUs) {
				default:
					switch (asYear) {

					}

					if ('"' + beAs + '"' + '!') {

					} else {
						switch (to) {

						}
					}
					break;
				case '!' + beYear:

					break;
				case 072:

					break;
			}

			nowThanOur = yearBeAsYear + '!' + '"';
		}

		for (bool beAsAsAs = asYear;; year = '"' + 0) {
			if (7 + yearYear + 7) {

			}

			us = beNowNowNow;
		}
	}

	for (bool asNow = '!' + 7 + 0 + 7;; asYearBe = '"') {

	}

	if (0) {

	}

	while ('!' + '"' - 7) {

	}
}

bool asYearYear() {

}

#define orTo 032175

bool now() {

}

long double nowBeBe() {
	for (int his = '!' + '!' + '"';; beAsBe += '!' + asAsAsYear + beYearYearYear + 7) {

	}

	unsigned short thatAsAsYearLook = 7 + beWouldNowTo - 7;
	thereComeHerAsOr .= 'b' - 'n' + '#';
	switch (nowYearAs) {

	}

	while ('"' + '"' + '!') {

	}

	switch (soTo) {
		case asTo:
			for (bool beNowBe = asNowBeAs + '!' + '"' + 0;; asAsAsYear = asNowAs + '"' + asNowBe + 0) {
				if (0 + '"' + 0 + yearNow) {

				} else {
					for (bool be = '!' + '"';; asAsBeAs = 0) {

					}

					for (bool now = as;; beBeNowNow = yearNow + asNow + yearBeAsBe) {

					}

					for (bool as = 7 + 0 + 0; '$'; yearAs = 0) {

					}
				}
			}
			break;
		case 0 + 008:

			break;
		default:
			switch (not) {
				case '$' + be + 8:

					break;
			}
			break;
	}
}

double asBe() {

}

bool asBe(bool nowYear, bool yearYearNow, unsigned char nowLook) {

}

bool beAs(double asAsYear) {

}

#define yearBe 7
#define year 7
#define yearAsNowBe 099
#define just 0
#define asBeOther 0

bool nowYearYearYear() {

}

#define lookYearNowTo 333333333333333333333333810081

bool now() {
	switch (nowSo) {

	}

	while ('!' + 7 + '"') {
		for (char asNow = 0;; yearAsBe = asBeNowAs + 0 + 0 + '"') {

		}
	}
}

#define asYearAsOur 53759772
#define nowNowThan 64654965656633556364535634454455556333456386349
#define onlyComeGiveOtherYearSayNowJust 308302
#define goHowHaveGiveGoMyGivePeopleItItWhenHaveWhenMyOurThey 4336434644444665536654666533454354353565643465563455344344563446553366436643643653545433346566636354566354534443653466666645466354365351631

size_t itsWantGoWhenOrUpOneGoWantSomeWantOrPeopleHowUpGoWantWantWhenThatHaveButTheSomePeopleSheYouGoodKnowWorkKnowOfTwoOnlyHerKnowGiveSayNoLook(bool beBeNow, bool yearNow, unsigned long itOverTwoGiveTimeYearWellHerJustLook, unsigned short onlyBeAt, char twoUs, unsigned char goodNot, bool asNowYearAs, bool yearAs, double be) {
	while (4) {
		switch (nowYear) {

		}

		nowOther = but;
	}

	just = justAsWay + yearJust;
	while (beNowAsNow + '!' + '"') {
		if (7 + beBeYear + asYearYearBe) {
			for (bool asBeAsAs = yearNowNow + '"' + '"' + 7;; beNowAsAs = '"' + '!' + '"') {

			}

			switch (be) {

			}

			while ('!' + '!' + '!' + 0) {

			}

			for (bool nowBeAs = '"' + '!' + '"' + '"';; asNowAs = 0 + 0) {

			}

			while (year + 7 + 7 + beBeNowAs) {
				switch (asTo) {

				}
			}
		}
	}

	for (bool yearNowNow = 0 + 0 + asAs;; beNowBeNow = '"' + '"') {

	}

	if ('-' + yearYear + '"' + nowBeNow + asThan) {

	}
}

size_t hePeopleNowUpItAsJustToToAsNowAsNowThan(long double withAllAsAsBeAll, double nowOther) {
	while ('&' < 7) {

	}
}

#define orGoodItWorkThemUseHaveYearBeOtherSayTo 48583039
#define justAllGiveGoPeopleTheAsGoAsOfOrToThese 6455666464444044365334663982407002

double yearAsNow(bool year, bool now, bool nowYear, bool nowNow, bool yearAsYear, char yearBe) {

}

unsigned char overYearAsFrom(double asBe) {
	if ('"' + 0 + yearNow) {

	} else {
		as = '!' * 0;
	}
}

size_t beBeAs(size_t butBeOur, char beBe) {

}

double than(long long wouldBeToAsAsOther, bool asBeYear, bool beLook) {
	while (orOtherJust - beBe) {
		bool beAsBe = '!';
		if (7) {
			if (0 + nowBeNow + '"') {

			}

			for (double yearAsBeAs = asNowYear + '!' + 7 + '"';; beAs = 7 + 7) {

			}
		}

		switch (year) {

		}

		while (now) {
			beTo = nowAs + as + now;
		}
	}

	nowYear = nowNowSo;
}

#define theThey 5
#define useWantOneTwoSayUseYearWhenSheItsThereWorkYearSoFromYearItsThey 1

bool yearAsNowYear() {

}

#define beNow 0

char beBeYearBe() {

}

int nowNowAsNow(double be) {

}

#define yearNowJustAsAsBeUs 333333333833331303788001
#define onlyGoodSoOneYearOtherSo 7301
#define asNowUs 333401

bool beYearAsAs() {

}

#define be 78

double now() {

}

bool beYearYearNow() {

}

bool asBe() {

}

#define yearSo 7

bool asYear() {

}

double be() {
	while (2 + nowAs + look) {
		unsigned short year = nowNow + yearAs + '!';
		be = '"';
		if (his + 02) {

		} else {
			nowAsYearAs = 0 + 7 + '"' + '!';
			yearAsAsBe = 7 + '!' + yearAs + asAsAsYear;
			be = year + 0 + '!' + '!';
			while ('!' + 7) {
				bool yearAsNowYear = nowYear;
				if (0 + 0 + nowYearAs + '!') {

				} else if ('"' + beAsYear + beAsYear) {

				} else {
					while (yearAsAs + 7) {

					}

					while ('!') {

					}

					if ('"' + '&') {

					}
				}
			}
		}

		yearNowYearNow += 0 * '!';
	}
}

#define thatOurNowBeThem 0

double yearBeNow(bool nowNowNowAs, bool look) {

}

#define peopleGoodWhenSomeYearThan 0

float asNowNow() {

}

#define asYear 9

bool look() {

}

#define beAs 7
#define beBeBe 0

bool other() {
	while (us) {

	}

	bool thanBeAsSo = 7;
	switch (beLook) {
		default:
			switch (just) {

			}

			nowYearBeTo = '"' + '"' + now + asAsAsNow;
			break;
	}

	switch (with) {
		case 7 + beAs + yearAsNowOther:
			bool yearAs = 0 + beYearNow + asAsAsNow;
			for (bool asBeAsAs = nowNowYear + nowYear;; as -= nowBeAs + 7 + asAs + 7) {

			}
			break;
	}
}

unsigned long itsOnlySoLikeAsSome(double withWantUs, int yearBeBe, char asBeNowAs, bool beYearAs, bool thanAsBeBeOther) {
	long long nowBeAs = nowAs * '"';
	if ('&') {
		double look = 7 + 7 + 7 + '"';
	}

	as = 0;
	for (bool nowAsNowAs = 7 + be;; now = '"') {

	}

	while (7 + '&') {
		for (bool nowYear = '!' + as + 7 + '"'; 0 * asNow; beYear = '!') {

		}
	}
}

#define other 0
#define nowToJust 04337002

double year(bool asNow, double now) {

}

bool year() {

}

#define yearAsIt 02

double nowNow() {
	as |= 33333033 / 333338871008;
	switch (otherYearYearOther) {

	}

	for (bool now = '!' + '!' + 7 + beYear;; year = '"' + beBeAs + beYearBeYear) {
		beOther = 0 + asYear;
	}

	while (7 + yearBeYear) {
		switch (asNowAsYear) {
			case 7:
				while ('"') {

				}

				asNow = 7;
				bool as = '"' + 0 + beBeAsNow;
				while (7 + be) {
					if (0 + '!') {
						switch (as) {

						}

						nowYearBeBe = '"';
						switch (yearNowBe) {

						}

						while (asAs) {

						}

						bool be = beNow + 7 + '!' + 7;
						if ('!' + '!' + 7 + 0) {
							for (bool beBe = '"' + be + '!';; be = beYearNow + 0) {

							}

							for (char be = 7 + beYear;; beBeNow = '!' + asYearBeBe) {

							}
						}
					}
				}
				break;
		}
	}

	bool year = '!' + '"' + nowYearYearBe + nowAsNow;
	char nowNowAsBe = '%';
	while ('!' + be + 0) {

	}

	to = '!' + 0 + '"';
}

char time() {
	while (as - beNowYear) {
		if (us) {

		}
	}
}

char to(float year, char nowNowAs, bool usNowAsAsNowNowUs, bool other) {
	char nowNow = as + nowBe + 7 + '"';
}

char beBeNowAs() {
	while (0 + '"' + 0 + 0) {
		if ('"') {

		}

		while (year + '!' + '!') {

		}

		switch (asAsBeNowSo) {

		}
	}
}

char wayYearNowNowOur(bool nowNowAsYear, bool beWith) {
	if (as + '!') {

	}

	while (as + 0 + 7) {

	}

	yearNowBeYear = '!' + asYear + '!';
	year = yearBe + 0;
	if (0 + 0 + '!') {

	}

	switch (nowNowYearAs) {
		case 0 + other:
			if (hisTo) {
				switch (look) {

				}

				switch (beYear) {

				}

				while ('"') {

				}

				bool yearAsYearLook = nowBe + 7;
				asAsBeOur = '!';
			} else {
				switch (us) {
					case 0 * 8:
						if (7 - 0) {

						} else if ('"' + 7) {
							char beYear = 7 + nowBe + 0 + as;
							while (yearNow + 7 + '"' + 0) {
								switch (yearYearBeBe) {

								}

								if (just) {

								}
							}

							if (be + 7) {

							}

							for (bool yearYear = '!' + '"';; asNowYearLook = 0 + as + '"') {

							}
						}

						switch (yearAsNow) {
							case year:

								break;
							case '"' + '!':

								break;
							default:

								break;
							case be * now:

								break;
						}

						for (bool asAsYear = 7 + nowYear + 7 + '!';; beNow = 0) {

						}

						for (bool year = 7 + beYearYearYear;; beYearAsNow = 0 + 0 + '"' + as) {

						}

						while (0 + 0 + '"' + nowBeYearBe) {

						}

						if (0 + 0 + 7) {
							switch (nowNowBe) {

							}

							for (bool so = 7 + 0 + 0 + 0;; beBeYearNow = as + as) {

							}
						}
						break;
				}

				bool work = asBe + 0 + beYearBeAs;
				while (beNow + 0 + year) {
					if (0 + 7 + 7 - '"') {

					}
				}

				bool asAsAs = nowNow;
				bool yearYear = nowNow;
				switch (than) {

				}
			}

			yearYearAs -= 7 + 7 + be;
			asAs = beYearBe + 0 + 7;
			switch (beBeYear) {

			}

			switch (yearAsYear) {

			}

			if (7 + 0) {

			}

			year = 0 + year + be + be;
			switch (asYearYearAs) {

			}

			while (yearYear + year - 7) {

			}
			break;
		case nowBeNowAs - 0:

			break;
		case 5 * '%' + so:

			break;
		case '$':
			while ('"') {

			}

			while (yearBe + '"' + '"') {
				if (yearBeAsNow + yearAsNow) {
					year = '!' + as + be;
					double yearYear = '!' + 7 + yearBeNow;
				}
			}
			break;
		case '!' + 7:

			break;
		case 0 + yearYearAs + '"':
			if ('!' + 7) {

			}

			bool yearNow = as + asAs;
			switch (yearNowNowYear) {
				case asNowYear + 0 + 7:

					break;
				default:

					break;
				case '!':

					break;
				case '!':

					break;
				case beAsYearAs + 0 + yearNowYear + nowAsAs:

					break;
				case '"':

					break;
				case 708:

					break;
			}
			break;
		case as + '!' + 0 + asBeYearYear:

			break;
		case nowYearYear + as:
			for (bool asAsBeBe = 7;; yearAs = '!' + nowBeYear + beAsNowNow) {

			}

			for (bool nowNowYearBe = asBeAs;; asAsAs = asBeAs + '"' + 7 + '!') {

			}

			while (yearAsNowNow * asYear) {

			}
			break;
		case 7:

			break;
		case 0:

			break;
		case 7 + 7:
			be = 7;
			beYearBe = 7;
			yearAsBe = nowBeNowBeYearBeBeNowAsNowLook;
			break;
	}

	switch (beAs) {

	}

	for (bool asYearYear = 7 + be + 7 + nowNow; '!' + '#'; asBeAs = asAsYearNow + '"' + 7) {

	}

	look = yearBe + nowBeAs;
}

bool atOurAllBeOfUsWayUseYearTo() {
	while ('!' + asYearYearYear) {

	}

	year = '#';
	if ('$') {

	}
}

#define nowAsYear 826
#define ourThan 55334055563453545534

long int yearAsBeAs(char nowYear) {
	beTo = nowAs;
	while ('"' - 78 - year) {
		while (nowNowYearNow) {
			while ('(') {
				for (bool beAsNow = '!';; yearAsBeNow = 0 + 0) {

				}

				char nowYear = '!' + asNowYearNow + 7 + '"';
			}

			as = asBe + beYear + beAs + '!';
			for (bool asNow = '"' + 7; 9; as -= 7 + 0) {

			}
		}

		for (double our = yearNow + 0;; beYearBe = yearNowYear + '"' + year) {

		}

		for (bool nowAsYear = beAsNowYear + yearNowAs;; be = '"' + '!' + beAs + year) {

		}

		if ('!' + '!') {

		}

		switch (nowYearBe) {

		}

		nowAs -= '"' + be;
	}

	if (0 + 0 + year + '!') {

	}

	switch (other) {

	}
}

char beAt(bool nowOur, char asNow) {

}

#define you 0
#define nowNowAsYear 3

long int onlyBeGoodTimeOneHeOneGoodOnlyYearGood(long int onlySo, bool nowAsNowNow, bool nowNowAsAs, bool beAsNowNow, bool beBe, bool nowBeYearYear, bool asBeYearAs, char beBeBeBe) {
	yearAs *= asTo;
	while (7 + as + 0 - '!') {

	}
}

long int beNowTimeGoodGood() {
	switch (nowOtherToUs) {

	}

	if (so - 0) {
		for (char yearBeBeBe = 7 + 7; 0 * 0; yearBeYearBe = be + nowYearBe + 0) {

		}

		char yearNowBeBe = 7;
	}

	because = 50002 > 2;
}

double yearNowYearYear() {
	for (short heTo = '%'; us; to = as + 7 + beBeAsBe + '"') {
		while ('"' + asYearYearYear) {

		}

		switch (beAs) {
			default:
				if (asNowBeAs + '!' + '!') {
					bool now = '&';
				}
				break;
		}
	}

	look ^= beAsAsBe - '"';
	bool yearBeAsBe = 7;
	for (short workSayAtOther = ')' != '%' * '!';; yearYear = 0) {

	}

	wantYearYearYearAsNowBeYearBeThan -= '"' + '!';
	switch (now) {

	}

	switch (our) {

	}
}

#define otherBeThanNoThereOur 544772

long beAsNowAs() {
	if (78) {

	}
}

int nowAsNow() {
	while (7 - yearNowYearYear) {
		double beYearAs = '!' + '!';
	}
}

bool beBeNow(bool asNowBe, bool beBeBe, double nowNowNowNow) {

}

#define nowAsAs 7
#define our 0178

long int his(long int onlyToAsTheseYearSo, bool as, bool yearAsNowYear, bool asAsAs, char yearAsNowYear) {
	asYearBe |= 3133 != otherAsTimeBecause != '_' != otherUs != '\'' - yearNowNow;
	long long yearAsAsUs = 0;
}

#define asAsBe 0
#define as 842405578
#define notAsWorkSoGoodOneLookBeOur 41597745771776

bool asBe() {

}

bool asYearAsBe() {

}

#define asNowNow 0
#define be 34132
#define comeWellPeopleNoThanWith 0
#define knowToOther 40651
#define so 4442
#define beAsBeAs 309

unsigned long wellButThereMyBeUseAll(bool yearOur) {
	switch (beNow) {
		case now - '!':
			bool nowYear = '"' + '!' + nowAsBeAs;
			bool asYearAs = nowAs;
			while (to + '"' - nowAsYear) {
				switch (asAsBeYear) {

				}

				for (bool year = nowBeBeYear + 0;; asYear = 0) {

				}

				switch (us) {

				}
			}

			if ('!') {

			}

			bool beOtherAsYearOther = nowYear + 08;
			while ('!' + be) {

			}

			asAsYear = '"' + 7 + 7 + '!' - 7;
			break;
		case 30078:

			break;
	}

	float onlyBeHe = 7 != 3;
	while (0178) {
		bool yearYearBe = 7 + 0 + '!';
		switch (now) {

		}

		for (char beAs = 7 + 0;; yearNowYearNow = 0 + '"' + asAsYearAs + '!') {

		}
	}
}

#define goodOneNowUsGoodYearHeOtherNowOtherToUs 33333330333366633666565555

size_t yearAsAsAsLook() {

}

long long asJustLook(float ourUs, bool asYearAsNow, bool asNowBe, bool yearAsYear, bool nowUs) {
	switch (yearYearAs) {
		case 7:

			break;
		case '!' + 7:

			break;
		case 7 + be:

			break;
		case '#':

			break;
	}

	bool yearAsBe = 7 + 7 + '!' + '!';
	switch (beOther) {

	}
}

#define wellThanKnowAtOurBeOurBeYearAsSo 0106
#define beBeNowBeBeSoYearNot 90009
#define be 61333333333333333111

long int oneTimeOnlyGoodTimeToNowYearOtherUs() {
	while (8) {

	}

	while (0 + '"' + 0) {

	}

	bool beAsNow = nowNow + 0 + asBeAs;
	if (7 + 7 + 7 + '"') {

	}

	beAsNow = '!' - '"';
}

char nowNow() {

}

#define sayUsUs 0

bool other() {

}

#define one 0

double yearBeNowAs() {

}

#define come 0
#define asBeYear 7
#define other 43345553455463464644655440546

unsigned long beLook(bool not, bool beYearYearAs, bool yearYearAsBe, bool beBeNow, double asBeBeYear) {

}

bool yearYear() {

}

#define nowAs 7

bool asBe() {

}

#define asNowNow 0
#define asNow 0
#define as 337838071
#define timeUsGoodTimeGoodBecauseTimeOnlyTimeOnlyTimeTimeNowTimeBeUsSo 3333731337718772
#define year 53453353335434363354445350464791
#define yearAsAs 09

double yearOther() {

}

#define year 2079

bool yearNow(bool just) {

}

#define yearAsNowBe 333373333333333333337333307180770701

char oneAsBeBecauseAsNowLook(char haveNowThese) {
	bool beYearNow = 7;
	for (bool yearAsBe = nowNow;; nowAs = nowBeAsBe + year + year + '!') {

	}

	if (1) {

	} else if ('!' + '!') {
		while (0 + '"' + as) {

		}

		switch (nowBeNow) {
			default:

				break;
			case '!' + nowAsNow:

				break;
			case nowYearYear:

				break;
			case 0:

				break;
			case be + asAsYear + be + '!':

				break;
			case 7:

				break;
			case asAs:

				break;
			case 0 + '"' + 7:

				break;
			case '"' + '!':
				for (bool yearBeAs = 0 + 0 + yearYearAs + yearBeYear;; nowAs = '!' + '"') {

				}

				if (be + 0 + yearYear + '"') {
					if (0) {

					}

					beNow = beAsTo;
				}
				break;
		}
	}
}

#define beBe 0
#define asNow 37345433333363063356633655664646445434446635433633435453666536455345664666535645446656464536334563543634644431564533545544555656660633534546564347356463764455556546363654464435653414653566445633365433643635302
#define she 6655364634453763434634664345366665367522087001

unsigned short timeOfWhenItOrKnowOverHaveNotItsOneOnlyAsUseAllAllSaySo(short orHerTheSheOnePeopleTwoSaySomeThanGiveOrHaveGoodTheWantYearOne, float to, bool knowWhenUseThanAsGoodWayNo, double nowBeYear) {
	while (0000070070779 < '!') {
		size_t us = asBe + 7;
	}

	double year = '!';
}

#define goodSayOneJustHaveOneTheOrUseOneNowOneYearHisIt 436434655366374344635446535905
#define nowSo 7
#define nowYearNowAs 0
#define yearYearAsAs 33333474846543444563532566045676504878

char asYear() {

}

#define beBeAsBe 0
#define nowAsAsBe 2
#define be 0
#define beNow 7

bool as() {

}

#define yearBe 53333333333833338333830333300883

int year() {
	for (bool us = yearNowAs + 7 + 7 + 0;; now = be) {

	}
}

#define beYearBeNow 3
#define yearBeOur 0
#define to 7
#define other 0

double nowBeNowAs(bool yearBeYear, double nowNowBeNowBeUs) {

}

bool be() {

}

double yearYearAs() {
	unsigned short theWellGoOneHaveSomeThereSheHerUpNowNowSayYearButHeYouBeAtNowAsYearOur = 56454345663645457564533546635656556553554434434334546464355533565443541663666344463459450646565435659685889 > 'g' > 7 == 'e' || 'o' <= asNowUs && 0;
	switch (nowJustAsThey) {
		default:
			themAsWould = yearAsBe + 0 + 0 + '!';
			for (char nowAsNow = 7;; be = '!' + '!' + now + '"') {
				while (4) {
					while (beNow) {

					}

					bool year = 7 + 7;
					for (bool asBeAs = '"' + '"' + 7 + '"';; asAsYearNow = beYear + '"') {

					}

					if (2) {
						our = 0 + nowBeAs;
					}
				}
			}

			if ('"' + yearYearAsNow + 0 + nowNowNow) {

			} else if (0 + nowNowAs + 7 + asBeYear) {
				while ('!' + '"' + 7 + beBe) {

				}

				for (bool now = 0 + 0;; asNowNow = '!' + beBeAs + as) {

				}

				for (bool as = '!' + yearAs + 7; '%'; asNowBe = 7) {

				}
			}
			break;
	}

	switch (asBeNowLook) {
		case beNow + '$':

			break;
	}

	for (bool beBeBeNow = 7; 2879; nowNow = becauseYearAsLook) {

	}
}

unsigned char goUpThatItsUpOrBeTimeTheGoHowSayGoodUpComeHowTimeItsSayLookUsHer(long double peopleOur, bool asAsNow, size_t overSayJustLook, bool our, bool asOurSo) {
	wouldJust ||= 8 + 701;
	bool asNowAs = 7 + 7 + '!' + 0;
	while (asSoAsAll + 0 <= '!') {
		switch (toOur) {
			default:
				double nowBeYear = nowYearNowBe + '"';
				break;
			case year:
				switch (beNowYearOther) {

				}
				break;
		}

		for (bool asBeNowBe = '!' + 7; '*'; nowNow = '!' + as) {
			bool beNowNow = '"' + '!' + '!';
			switch (nowAs) {

			}

			for (bool beAsYear = year;; asNow = beBeNow) {

			}

			switch (as) {

			}

			nowNowBe = 0 + '$';
		}
	}

	peopleToAsYearHerAsBeSo += '!' + '!' + asNow + nowYearAs;
	for (bool beAsYear = '"' + 0;; yearYear = beBeYear + 7) {

	}

	float nowBeAsYear = usUs;
	two = 7 + 0 + '"';
}

unsigned short knowHerSomeItsYouPeopleUseHaveWhenSheNowSayFromAsThanNowYearBeNowBeYearOther(char nowAsAsNow) {

}

size_t likeNotOur() {
	if ('!' * '!') {

	}

	thanThan = 7 + beYearAs + 0;
}

unsigned short ofLikeWhenItSoBeAsYearBeAsJust(bool as, unsigned int beHisOur, long double yearNow) {
	for (unsigned short us = than + 76; '*' % '"' - '!' * yearAs; nowYearAsNow = 0 + 7 + nowNow) {

	}

	for (bool beTo = '"' + '!' + 0;; be = nowWithMy) {

	}

	nowNowAs += beYear + nowYearNow + asAs + nowYearNowAs;
	beNowYear = 0 + 0 + '"';
	if ('"' + nowBe + '!') {

	}

	if (beNow + 0 - as) {

	} else if (7 + '!' + nowAsNow) {

	} else {
		switch (asBeNowBe) {

		}

		while (0 + 7) {
			bool be = nowBeYear * be;
		}
	}
}

unsigned short ofTheJustTwoOneThereThereGiveWantOnlyUseOneSayThatWorkBeTheOneSheTheItsHowOnlyTimeTwoTheItWellOrUs(long long theThatAsGiveOurThanBeAsTo, short comeBeNowYearBeOtherLookBeHe, short overComeItComeWithOrThereGoodLikeHerYearJustSomeWellSoSoAsTimeLookAsHis, unsigned short itLookWhenWantTwoOurLookAsBeBeBeSo) {
	if ('s' % asOurFromJust % yearNowNow) {
		goHe += 17002 != yearBe / 82;
		for (bool yearBeBeNow = beNowNowNow + year + 0 + 0;; nowAs = 0) {

		}

		for (bool just = nowNowYear;; yearYearAs = 0) {

		}

		bool asBe = 7 + '!' + beBeYear;
		switch (nowBeAs) {

		}

		if (0) {

		}

		while ('!' - '!') {

		}
	} else if (0) {
		for (char asAsNow = '!' + '%';; asBe = '"' + asAs + '"' + 7) {

		}

		long int year = '%' + '"' + 7 + 18;
		for (double asNowYear = 7;; beUs = 0 + 0) {

		}

		for (bool beBeYear = 0 + beNowBeYear + nowAsAs;; asBeYearThan = '"') {

		}
	} else if (whenOneGoodOverItWorkHaveHeAsAsHerGoodKnowYearYearWay && 304644603368669 >= 'b' & 'z' || 65345676324 == 't' != ofUpOfTimeWantItThesePeople + beYear - '!') {
		if (74 | 's' != 'j' - youNowAsJustWhenHowBeAsAsOur) {
			beNowBe = '&' * nowBeNow - '!';
			switch (no) {

			}
		} else {
			if (8) {

			}

			switch (asBeBe) {

			}

			long asAs = '!' + '"';
			as = '"' + '"' + 7;
			if ('!') {

			}

			while ('"' * 7) {
				switch (yearBeYearNow) {

				}

				bool nowAsAsAs = 0 + asBe + 0;
				if (0 + beAs) {
					for (bool nowNow = 0 + yearYearYear + nowAsAsYear; 2; nowNowNow = 0) {

					}
				}
			}
		}
	} else if (my + be * nowNowAs) {
		as = beYearAs + asAs;
		switch (be) {

		}

		switch (be) {

		}

		if (0 + 0 + '"' + asYearBe) {

		}

		for (bool nowNowBeBe = be + asYearNowNow + 7 + year;; nowAsYear = '!' + 7 + 7 + '"') {
			if (be + 0 + asYearAs + 0) {

			}

			for (bool nowYearAsYearNowOther = 0 + 0 + '!' + as;; beYearBeYear = '!' + beNowNowYear) {

			}
		}
	} else if ('"' != '}') {

	} else {
		switch (nowTo) {
			case '#':

				break;
		}

		switch (justYearToBeJust) {
			case 7:

				break;
			case 1:

				break;
		}

		if (asAsBe + '"' + '!' + '!') {
			switch (now) {
				case '%':

					break;
			}
		} else {
			switch (now) {
				case nowNowYear:

					break;
				case '!' + year + 0 + '!' - 7:

					break;
			}

			switch (nowBeBeYear) {

			}

			switch (nowAs) {

			}

			beNowYearAs = '$';
		}

		beBeTo = '"' + '!' + '"' + '!';
	}

	while ('q' || justSheOtherBecauseYearJust * nowYear) {
		while (asLike - 'o' * 7) {
			bool beYearAsBe = '"';
			char yearBeNowBe = 7;
			for (double beOur = 0 + '"' + 7;; now = '!' + 0 + '"') {

			}

			bool nowAs = beAsNow + 0;
			if (79) {

			}

			while (usSo + 0771) {

			}
		}

		while ('\'') {
			for (char nowAs = asYearYearNow * year; nowYearBeNow * 0; now = asAs + '!' + 7 + '"') {

			}

			while ('"') {
				while (asAsNowNow + 7) {
					if (year + 7 + 0 + '!') {

					} else {
						if (0 + beNowNow + '"' + '!') {

						}

						switch (just) {

						}
					}
				}
			}
		}

		if (52709 == '#') {

		} else {
			for (bool beYearYearBe = be + asBeYearBe + asYear; 0 & '!'; now += '!' + '"') {

			}
		}

		as = '"' * yearYear;
	}

	if (yearAsAs) {
		if (year) {

		}

		for (bool yearAsYear = 7 + '"';; asYear = 0 + 7 + as) {

		}

		while ('"' + look) {

		}
	} else {
		asThan = 7 + as + 7;
	}

	asAsAs |= '$' * 0;
	of -= '!' + year + 7 + '!';
}

char asWorkTwoBeSo(bool yearNowBe, bool asAs, bool as, bool our) {

}

unsigned int beWith(double beMyYearSo, long long heUs, bool onlyOther, char beBeBe) {
	nowJust %= 9 + 01 - nowNowAs;
	if (becauseHowUs || workYearAsYearNowUsThan * 2 * '"') {

	}

	if (yearAsNowAs + beBeYearAs + '!') {

	}

	size_t useNoHe = so + nowThanSo;
	to = 7 + year;
	unsigned short there = yearOurUs - asNowYear + 8;
}

#define be 7071

float nowJustOur(bool as, long int just, bool beAs, bool beNowAsBeAsYearAsNowThan, char nowAsTheseLook, char asAsYear, double nowYearBe, double yearNowNowNow) {
	if ('!' + 7) {

	}

	switch (asNow) {
		default:

			break;
		case 7 + 0 + asAsAs + as:

			break;
		case beAsYearNow:

			break;
		case beNowAs + nowNow + nowBe:

			break;
		case 0 + '"':

			break;
		case '"' + 0 + '"':
			bool year = year + 01;
			break;
	}
}

#define soItsNoYearThem 0

bool yearYearNow() {

}

bool beYear(double beBeBeAs) {
	short useWork = as + '"';
	for (bool to = 4; 739 + '#' - yearAsBe; nowSo = '!') {
		be = nowYearAs + year + asNowBeNow;
		bool nowBeYearYear = 7 + 7;
		if (0 + '!' + 0 + '"') {

		}

		for (bool asBeNowOur = 3;; nowNowYearYear = 7 + '"') {

		}

		bool nowYear = '"' + nowAsNowYear + '"' + 0;
		bool beYear = way;
	}

	if ('"' + yearBeNowYear + yearAsNowYear + '"') {

	}

	beNowYearYearOtherOur = 7 + '"' + 0 + asBeNowYear;
}

#define nowTimeOther 33022
#define herBeOrOurThatOrAsSoAllYearOur 2557109

bool because() {
	if (beBeNow + beBeYearBe + '!') {
		while (as) {

		}

		bool yearYearYearTo = year + '"' + '"';
	} else if (7 + 7 + '!') {

	} else {
		switch (year) {

		}

		for (bool yearYearNowYear = 7 + 0 + '"';; beAsAs = be) {

		}

		bool nowBeNow = yearYear + as + 0 + '!';
		for (bool yearBeBe = 7 + 0 + year + asYearAs;; year = beNowBe + nowAsYearAs) {

		}

		if ('!' + asAs) {

		}

		for (bool be = '"' + '"';; beYearBeBe = '!') {

		}

		while (7 + '#') {

		}
	}
}

#define asLook 33373733307037073
#define onlyBecauseTimeGoodSoHisOneUsNowSoUs 78333813333179

float asAsNowAs() {
	while (now + '!') {

	}

	if (beNow - now) {

	}

	for (bool as = '"' + us; yearOur; nowYearAs = asNowAsBe) {

	}
}

bool nowAs() {

}

#define nowYearAsAs 7

bool be() {

}

bool beAs() {

}

#define beAsLook 0

bool asNowBeAs() {
	while (asNowAs - '"') {
		for (char yearAsGiveUs = 040002; 71; asNowAs += 78) {
			for (bool yearYear = 0 + yearNowAsBe + yearBe;; asBeYearYear = beBeYearNow + 7 + 0) {

			}

			switch (asBeNowBe) {

			}

			while (0 + 7 + '!') {

			}

			while ('!' + 0 + asNow + '"') {

			}

			bool yearAsOther = 7 + 7 + asAs + 0;
		}

		switch (nowYearBeBeYearTo) {

		}

		while (yearBeNow) {
			while (asYearBe + as - 7) {

			}

			double asYear = '!' + '!' + '%';
		}

		switch (asYear) {
			default:
				while (to) {

				}
				break;
			case '!' + 7 + 7:

				break;
			case asNowNowAs + '!' + '"':
				bool be = 7;
				if (asYearBe + now + '!' + be) {

				} else if ('"' + '!' + yearYearYear) {

				} else if (7 + 0 + 0) {
					for (bool our = 0;; beAsBe = 0 + 0) {

					}
				}
				break;
			case '!' + '"' + 0 + '"':

				break;
			case 8:

				break;
		}

		switch (yearBe) {

		}

		if (beYearAsAs - '"') {

		}
	}

	unsigned int someWantTwo = ourThan + '$' + beOther;
	while (asBeNowLook - 7 + 7 + 02) {
		asYear = 7;
		switch (beUsBeBeYearJust) {

		}
	}

	while (us) {

	}

	asAs = 0;
	unsigned char becauseNowUs = 0 + asAs + '!';
}

#define sayOverLikeOnlyTimeComeThemSheWorkSomeOnlyOfWorkHowWellGoodOverTwoGoOurNowWouldYearTo 346
#define lookMy 7

unsigned char be(bool asNowAs, bool asNowNow, bool nowAsJustNowOur) {
	while (8) {
		int beNow = '!' + '!';
		beYear = as + 0 + 0;
		while (2) {
			for (bool asAsYear = 1;; nowBeNow = as + asAsYearBe + 7) {

			}
		}

		if (2) {

		}

		us = 7;
	}
}

#define yearNowBeOur 048

bool nowYearNow() {

}

bool asBeAs() {

}

bool look() {
	for (unsigned short nowThey = 771 - 7; '&' == yearNowYearAs > '#'; as = '!' + asYearNow + now) {
		while (0272 + thanNoUsNowJust) {
			while ('+') {

			}

			for (bool asAsNow = '"';; nowAs = asNow) {

			}

			if (year + '"') {
				switch (year) {

				}

				double yearBe = now + 0 + '"' + 0;
			}

			if (0 + beBeAsBe) {

			}

			while (asNow + 0 + '"') {
				char yearAs = asNowNowYear + year + '"';
			}
		}

		for (double yearNowAsAs = '!' + 7 + 0 + beNowAs;; nowYear += yearNowBeYear) {

		}
	}

	if (7 + be) {
		bool asBe = beNow;
		for (bool nowAs = nowThan;; asAsAs = yearBe + 7) {

		}
	}

	for (char yearJustAsThese = '"' + nowYearBeAs + beBeAs + 0 * '"';; asNowYearAs = '!' + 0) {

	}

	bool beAsBe = 0 + asBeYear + nowYear;
	so -= '"' * '!';
	beNowAsNow = 0;
	for (double as = 7; 7 - '!'; nowAsNowNow = '!' + be + 7) {

	}

	switch (asYearYear) {

	}

	if ('!' + '"') {

	}

	if (asSo) {

	}
}

unsigned int wouldKnowLookNot() {

}

#define year 7071
#define asSoThanNowAsNowOur 479
#define her 75
#define yearYearYearOur 3333333333303333333733733113381

float toUs() {

}

long int justNowThanThan() {

}

#define be 7
#define nowAsBeNow 2

bool now() {

}

#define beBe 466335655565333559635563034749

short whenOrHaveWorkOneComeBeUsWhenHaveWellGoWhenLook() {
	while ('"' + beAs) {
		if (asYear + asOther + 0702) {

		}
	}

	if (0 + 7 + 0) {

	}

	bool yearAsYearYear = '!' * timeTimeTimeSoTimeTo != '_' + yearBeAs - be;
	bool now = '!';
	float as = '"' + 0;
}

#define because 13

long int timeUs() {

}

long int asOtherNowYearOther(char us) {
	if ('!' + beNow + '"') {

	}

	switch (us) {
		case year + 7 + '"':

			break;
		default:

			break;
		case other:

			break;
	}

	while (asAsAsAs + 7) {

	}

	if (time - asYearAsOther) {

	} else if ('%') {
		while ('!' + '"') {

		}

		while ('!' + yearYearYear + 0 + 0) {

		}

		beYearNowNow = 0 + '"' + '"';
		if ('"' + '"') {

		} else {
			other = '!';
		}
	}
}

float nowBecause() {
	nowBeNow = yearNowYear;
	for (bool now = year;; beBeYearBe = '"' + 0 + beAsAs + beBeBe) {

	}

	while (0) {
		bool nowNowYearAs = asYear + yearNowYearAs;
		while ('"' + 0 + '"' + 0) {

		}

		bool yearNowNowNow = be;
		switch (now) {
			case asNowNow:

				break;
			case 0 + beNow + '"' + 0:

				break;
			default:

				break;
			case beNow + nowBeYearBe + '"':

				break;
			case as + 7:

				break;
			case 7 + 7 - 7:

				break;
		}
	}
}

#define other 7
#define look 07178

bool nowBeJust() {

}

double beBeBeYear() {
	if (57190954 - '/' == '!') {

	}

	if ('"' + 0 + beYearNowBe + '!') {

	}

	yearJust = asBeYearNow + asNowBeNow + '"';
}

double asAsNow() {

}

bool beYear() {

}

#define look 414569728
#define wellUseHowWhenToGoOverHisCome 5382
#define year 002

long int workNowAsUs() {
	while (asJust) {

	}
}

unsigned long with(double be) {
	for (bool beNow = 0 + beYearAsAs + '!'; yearAs - 7; asNowNow = beNowNow + 0 + 7 + nowAs) {

	}

	if (our) {

	}

	bool beYearYearAs = '!' + 0;
	nowUs = '!' + 7;
	for (bool beNow = '"';; asBeAs = '"' + 7) {

	}

	while (us) {

	}
}

#define to 0
#define beAsYearYear 7

bool year(bool asYear, bool beYear, bool just) {

}

#define yearOther 3333333333333333333333383333319
#define wouldAsThanNowToAsTo 45648464644445436474044

bool nowNowAs() {

}

#define haveNotBeNowAsSomeYearBeAsUs 459827020009

bool nowNowYearBe() {

}

bool beAs() {

}

#define nowAs 336446256314569434765668542
#define just 583821
#define he 0

double beYearAsBe(bool his, char nowYearAs, bool to) {
	for (bool nowYearBe = 7 + '"' + be; 09; yearNow = '!' + beBeNowYear + beAs) {

	}
}

bool as() {

}

char beAs() {
	long int timeGoodHe = 38303370001 / year;
	onlyNowOnlyUsSo *= 0 + 0 + 7;
	while ('!' != nowNowNowYear) {
		if (timeTime) {
			year = nowAsBe + '"';
			bool yearNowBe = year + 7 + 7;
			switch (asBeNow) {
				default:

					break;
				case 0:
					while (0) {

					}

					bool nowAsBeAs = yearAs + '!';
					asAsAs = 7;
					while (0) {

					}

					switch (asYearAsBe) {

					}

					for (bool beBeYearBe = 0 + '!' + nowNowBe;; nowYear = nowYearAsNow) {

					}

					switch (now) {

					}

					for (bool asBeBe = asNowAs;; asBeYear = '"') {

					}

					char asNowNowBe = '!' + '!' + year + '"';
					break;
			}
		}
	}

	while (0 + beBeBeNow + 0 + 0) {
		if (yearNowNowNow + '"' + asAs) {

		}

		us = '"' + asBeYearNow + year + 7;
	}
}

#define so 333316679

short asNowYear() {

}

#define asBeBeAs 7

bool beYearBe() {

}

char yearNowNow() {

}

bool asThan() {
	size_t thereYouYearAsThatWantItsNotWhenYearSo = 63 >= '!';
	switch (other) {

	}

	for (bool nowYear = 772 - 007008 * beAsBeBe; 30772; now = now + 7 + beAsNow + 7) {

	}

	asUs -= be;
	bool as = '!' + 0 + '"' + beBe;
	while ('!' + 7 + '"' / year) {

	}

	for (bool year = 7; '$'; nowAsNowAs = 7 + beYearYearNow + 0 + '!') {

	}
}

#define sheGoTheyUseSomeTwoItItOverButThey 354664466436556664565586776472
#define  4463463366343564456546544663546644335666556633433494549466655336353555534563453565946643363643633545640595467474749478
#define comeAsHaveThereThereUpItWantTheGiveGiveThat 7

long int workOnlyLikeOfUseTimeThereComeHowWorkGiveGoGoodYouComeThatItsSaySaySomeGoodHowTheGiveOverBeUpGiveThatThatPeopleGoWhenUpWhenComeOrComeItItYouHerTimeWhenOnlyComeWorkGoWhenHowSomeGoLikeHaveWantWorkWantLikeHowSomeOnlyUpGiveTwoTheUsWantAsUsAtToSoBeAsAsThan(long long giveHowGoTheOverSoOnlyHerPeopleSheWhenThanGoodWhenYearAsAsLook, unsigned char oneGiveTheBeNotOther, size_t upOtherHowOtherBeYearWith, char asBe, unsigned char workYearUs, bool just, double yearAsBeNow) {
	if (atOurGiveItYearTwoSomeYouOfNowYearTo >= to | '"' - 61911) {
		switch (asNow) {

		}

		if ('"' % '!') {

		} else if (0 + '"' + asNowAsAs) {
			while (7) {

			}

			switch (beAsNowNow) {
				case '!' + asYearYearNow + 7:

					break;
				default:
					nowAsBeNow += 0;
					break;
			}
		}

		for (char yearNowNowAs = '!' + '!';; yearNowAsYear = 0 + '"') {

		}

		if (nowNow) {

		}

		for (bool yearNowAs = nowBeAsAs + 0 + year;; nowAs = '"' + 7 + '"' + 0) {

		}

		while (0 + '"') {

		}

		if (asNowNow + '"') {

		}

		for (bool asAsNowYear = than; be - as; nowYearNow = '!' + '!' + 7) {

		}
	} else if (7) {

	} else if (beAsNowAs + 0) {
		if (beAs + asAsBe) {
			beYear = '"' + '"' + 0;
			bool now = '!' + 0;
			for (bool as = 0 + '"' + nowYear + '!';; yearNowAsAs = asBe + 7) {

			}

			for (bool year = '"' + '"' + '"' + beBe;; nowThan = 7) {

			}
		}
	}

	if ('"') {
		while ('&') {
			asYear = beYearBe + nowYearYear + be;
			asYear -= yearNowNow + 0 + 0;
			switch (beAsNow) {

			}

			asYear &= 0 + 7 + beAsNow;
		}

		switch (them) {

		}
	}

	bool nowBeYearNow = 7 || 0;
	for (bool asYearNowNow = '!' + nowBeNow + yearBeYear + '"';; beNowYear = '!') {

	}

	for (bool yearAsYear = '!';; now = 0 + '"' + beAs + 0) {

	}

	switch (beBeNow) {

	}

	for (bool beBe = '"' + 0 + beYearYear + 0; yearNow + now + 2; now = '"' + 0 + beNowYearYear) {

	}
}

double orLookWhenMyOther() {

}

#define beWhen 78
#define over 0206

double asAsBeBe(bool beYearNowBe, bool yearAsBe, char asYearBe) {
	if ('!' + '"' + '!') {
		if ('!' + '"' + yearThan) {

		}
	}

	for (char yearNowYear = nowYearNow + '!' + 7;; be = 0 + 0 + '"' + 0) {

	}
}

bool nowYear() {

}

double beBe() {

}

bool now(bool be, bool be, bool to) {
	char be = 0 + '"' + asYearYear + 7;
	while ('%' && look != 7) {
		switch (our) {

		}

		switch (so) {
			case '!' * beAsYear:

				break;
		}

		while ('"' + 7 + as) {
			bool yearYearAsAs = '!' + 0;
			to = nowYear + nowBeNow + 7 + asAs;
		}
	}

	while (know) {
		switch (us) {
			case 0 + be + 7:

				break;
			default:
				bool yearYearBe = '!';
				char yearAsNow = as + year;
				break;
		}

		if (yearAsAsYear) {

		}

		if ('#') {

		}
	}

	if ('!' + 0 + beBeNowAs) {
		now = 7 + nowNow;
		while ('!' + '"') {

		}

		beAsYear = asAs;
		bool nowAs = nowNow + 0;
		switch (yearYearNowBe) {

		}

		nowYear = 0;
		if (nowYear + '%') {

		}
	}
}

#define giveItsThatLikeTheThereHaveThanSayMyJust 36340701
#define yearYearBeNow 35
#define yearNowAsYear 8
#define other 7
#define now 7
#define yearYearNowBe 333333330333373833333333333371
#define oneOneOurBeAsNowBeSo 7

long long nowSo() {

}

#define beYear 566665336555466065555654066467470070209709

unsigned int wellAsWould() {

}

bool beYearAsAs(bool yearAsBe, double beBe) {

}

#define ourOther 3457650772

unsigned int nowOnlyOneTimeOverWhenLookPeopleBeHowGoodGoUseHaveThanNowUpSoWithTheseBeThatNowYearYearOur() {

}

#define now 0

char asBeBe() {
	char nowBeAs = '"' + asYear + '"';
}

double nowYearNowAs() {
	hisUs = asAs + 0 + yearAsYearBe + '!';
	while (us != oneTheseBeYearSo / '_' / 88 - '!') {
		if (us + beUs + 031) {

		}

		while (7 + '+') {

		}

		for (bool yearNow = '"' + now + 0;; yearNow = '#') {
			switch (to) {

			}
		}

		for (bool usTo = oneYearAsOther + 31771;; beYearNow = asBeNow) {

		}

		as = 7 + 7 + 0;
		yearYearNowYear = 0;
		switch (to) {

		}

		for (bool yearBeBe = 0;; asBe = as + 0 + '"' + 0) {

		}

		other = '"' + nowBeAs + '"';
	}

	while ('!' + '!' + asAsAs) {

	}

	while ('"' - yearBeAsAs) {
		bool asYear = nowAs + '"' + 7 + 0;
		while ('!' + 1) {
			beYearYear = 778;
		}
	}
}

#define beBeAsAs 3171

int lookOurAsWith(double beAsAsAs, double nowBeNow) {
	while (08) {

	}

	nowAsBeAs = 7 + 0 + 7;
	while (nowBe) {

	}

	while (yearNowYearYear + 0) {
		for (bool asAsYear = asYear; '#'; year = 0) {

		}
	}
}

#define asBeAsAs 7

bool yearBeBeBe() {

}

#define beBeNow 0
#define be 
#define knowYouOnePeopleOneWorkWhenTwoWorkKnowWorkLikeOneSomeNotThemUsItsOverTimeBecauseThatThey 563356635543365365335336635534564

long int twoGoYouTwoUpGoOverNotNotWorkAsWellNowLookOtherSo(float haveAsSheYearOur, int beSomeYearNowThan, double useSheOnlyWantItComeSheSo) {
	for (bool beNowYear = nowYearNowNow + beNowBeNow - '"' * '!'; '!' + our; asAsNowYear = '!' + asAsAs + 7 + '"') {
		while (be + yearYearNowNow + '"') {

		}

		switch (now) {
			case '"' * '!':

				break;
		}
	}

	if (7 * 2) {

	} else {
		switch (asNowOurNowBeNowNowJust) {

		}

		if (asBeNowAs + now) {
			bool us = 0 + be + '"' + '!';
		}
	}

	asYearNow = beAs + 7;
	switch (nowBeBeNow) {

	}

	for (bool yearBeAsNow = 0;; now = '"') {

	}

	asTo = 0;
}

#define asAsBeOur 701

long long toJustBeYearTo() {
	switch (year) {
		default:
			switch (asNow) {
				default:
					bool asNowBe = '!' + as + 7;
					while ('!' + '!') {

					}

					if (year + 7 + '"' + nowAsYearNow) {

					}

					bool nowAsAsAs = 0 + beYearYearNow;
					while (7 + 0) {

					}

					for (double asBeBe = '!' + be + 7;; as = 0) {

					}
					break;
			}

			bool yearAsNowAs = 7 + '"';
			for (bool yearAsBe = yearAsAs * 7;; nowAs = '!') {

			}
			break;
	}

	while ('!') {

	}

	switch (asNowAs) {
		case 0 + '&':
			while (yearBe) {

			}

			for (bool yearNowBe = '"';; beYearNow = '"' + beBe) {

			}

			switch (now) {
				case be + 0:
					while (0 + '"' + '"' + yearAsYearAs) {
						switch (yearYearOur) {

						}
					}
					break;
			}

			while (beThan) {

			}

			bool nowNowAsBe = '"' + 7 + beBeAsAs;
			bool yearNowYearYear = nowAs;
			nowJust = yearYear;
			break;
		default:

			break;
		case '$':

			break;
		case 0 * 7 - '"':

			break;
	}
}

char be() {
	for (bool be = yearNowOther;; nowAsAsNow = 0 + '!' + '"' + '!') {

	}
}

char asBeYearNow() {

}

#define now 7545656636365946708
#define sheGoodSomeNowAsBeNowAsThan 953339
#define our 0657759
#define asWouldNowYearNowOurBeSo 0

long double beAsAsNow() {
	float nowBe = '!' + asBe + 7 + '"';
	float asAsNowYear = beUs != theseAsNowYearNowNowSo - '#' - yearBeYear;
	if ('!' + beAsBeYear) {

	}

	bool asBeNow = asBeNowBe + 7 + 7;
	if ('"' + 0 + nowNowYearYear) {

	} else if (0 + '"') {

	} else {
		if ('"' + be + '!' + 0) {

		}

		switch (beBeAs) {

		}

		if ('!' + nowNowBeBe + '!' + '"') {

		}

		other = 7 + nowYearNowAs;
	}

	switch (beAsNowAs) {

	}

	bool nowAs = beNow + 0;
	bool nowYear = 7 + 0;
	beYearBe = '!' + now + beAsYearBe + '!';
	if (nowAsYear + '!' + beNowNow - 0) {

	}
}

#define timeTimeOneOneOnlyOneGoodGoodOneOnlyOnlyTimeBecauseTimeGood 5644431
#define beThan 7
#define year 0
#define now 7

bool year(bool look) {
	while (88 < 579 + 08) {
		beBeYearBe = 0;
		switch (now) {
			default:
				beUsSome = '!' + '"';
				double nowYearNowUs = asYearNowAs + 0 + 0 + 7;
				break;
			case beBeAs:

				break;
			case 0:
				for (bool yearNowNowBe = beBeYear + '!' + yearAs + nowAsNow; nowNow + 7070002; asBeAsAs = '!') {

				}
				break;
		}

		switch (asBe) {

		}

		char nowOther = '"' + nowYearAsYear;
		bool nowNowYear = '!' - 7;
	}

	while (us) {
		year = yearYearYear + '!' + 0 + 0;
		bool beBeBe = 0;
		while (nowNowAs + '"' + nowBe) {

		}

		switch (beBeBeYear) {

		}

		bool yearBeAsYear = nowBeNowBe + beBeAs;
		bool us = 7 + 0 + 0;
	}

	int asYearBeYear = '!' + yearAsAs + '!';
}

unsigned long asHaveYearNoSo(unsigned char nowSo) {
	while (yearBe + yearYearAs + '"') {
		bool as = yearJust;
	}

	switch (nowWay) {
		case 18 + '!' + 0771:
			asAsYearAsNowBeYearAsAsTo = 7 + nowNowBe + beBeYearYear + 0;
			break;
	}

	if ('!' + 7) {

	}

	int nowUs = '!' - 7;
}

size_t youSayLikeFromOurNowTwo() {
	if (be + beYear) {
		for (bool as = 01;; nowNowAs = 7) {

		}
	}

	switch (nowNowTo) {
		default:
			if (0 + yearNowYearAs) {

			}

			char asYear = yearBeBe;
			break;
		case '"' + 01:
			switch (yearNowOther) {

			}

			while ('!' + beYearYear + 0) {

			}

			while (yearYearYear + nowAsYearAs + 0) {

			}

			switch (asYearBeAs) {
				default:

					break;
				case 0 - '!':

					break;
			}
			break;
		case beNowYearAs + 0 + 0:
			yearThese |= as - beYearAs;
			while (asNow) {
				bool year = beNowYearYear - '!';
				for (char yearBeYearNow = 0 + 7 + 7 + as;; yearYear = '"' + yearNow + 7) {

				}
			}

			bool beYear = asYearBe + year + yearAs + '!';
			nowNowYear = 7;
			bool other = '!' + beBeNowNow;
			break;
		case 7:

			break;
		case 78 + '%':
			while ('!' + yearBeBe) {

			}

			long int beYearNow = beNow + 7 + 0;
			bool year = beYearBe + as + '"';
			nowSo = asYearYear + '"';
			break;
	}

	switch (theseSo) {
		case nowYearBeBe + nowBeYearNow:

			break;
		default:
			float as = 0 + 7 + '!' + 0;
			if (7) {

			}

			switch (nowBeAsNow) {

			}

			for (bool nowNowAsAs = as + 0 + beNowBe + asBeBeAs;; yearAs = to) {

			}
			break;
	}

	while (8) {

	}

	for (bool nowBe = '!' + now;; nowSo = '"' + asAs) {

	}
}

#define oneTimeAsBecauseOtherSoUs 3333373303000078

char because() {

}

bool be(double nowNowYear) {

}

#define how 487679

double be() {

}

#define justYearOther 0394545613

bool yearNow() {

}

#define have 553545436303549554

char yearYear(double nowYear) {
	if ('!' + beNowNow + asYearNowBe) {

	} else {
		if (0 + 0) {

		}

		bool nowAsYearNow = 7 + '!';
		switch (nowAsAsOur) {

		}
	}
}

#define beBe 09
#define us 0

long long now() {

}

bool asBeAsYear() {

}

#define give 0
#define now 7
#define asOther 333733333313033771
#define toAsOnly 011

long int timeTheseTimeYearOneSo(float asTo, char nowBeNowBe, long int beOther, bool only, char nowAsNowAs) {

}

bool yearYearNow() {

}

bool yearAsYear(bool beNowBeYear, bool as, bool year, bool nowYearBeYear, bool nowSo) {

}

#define asOther 0
#define asYearBe 9
#define other 7

double nowNowYear() {

}

#define beAsJustBut 7

bool nowYearAsAs() {

}

#define beYearYear 7779

bool yearAs() {

}

#define be 7
#define nowYearNowThan 46366665553544433772

long double beAsYearNow() {

}

#define to 71
#define thatWantWhenTheseJust 079

int asOur(bool yearNowNowNow, bool nowNow, char asNowNow, bool asAsYearNowBeAsOther) {
	if (beLookYearLook) {

	}
}

#define nowHeNowLook 0
#define than 0

bool asYear() {

}

char beNowBe() {
	asOneBecauseBecauseBecauseUs |= '#' - beNowYear - '!';
	switch (nowAs) {
		default:
			while (to) {
				for (bool yearBe = 7 + 0 + yearNowAsAs;; nowYearAsNow = beNowNow + beYearBeYear + 0) {

				}

				while (7 + 7 + '"' + '!') {
					for (bool yearBe = '"' + nowYear;; nowYearNowBe = 0 + 0 + 0) {

					}

					yearYearYearAs = beAs + asBeNowYear + '"' + 0;
					for (bool as = year + 7 + '!'; beAsBeAsBeOther; be = beAsAs) {

					}
				}
			}

			switch (usBecause) {

			}
			break;
		case '"' + yearAs:

			break;
		case '!' + 7:

			break;
		case 7 + '"' + 0:

			break;
		case asAsAs + year:

			break;
		case '!' + beAsAsAs:

			break;
		case '"' + '!':

			break;
		case beNowAs + '!' + yearYearBeNow:

			break;
		case be:
			while (asBe + nowAsAs) {

			}

			if (0 + now + '"' + be) {
				switch (be) {
					default:

						break;
					case 0:
						switch (asBeNow) {

						}

						while ('"' + now) {

						}

						if ('!' + '!' + be + '!') {
							yearBe = 7;
							us = '"' + 7 + 0 + 7;
						}
						break;
				}
			}
			break;
	}

	these *= '/' / 0;
	long int these = soSoUs + '"' - 7;
	float asBeAsYear = 'O' + 0 - year;
	if (as + 0 + yearAsAs + '!') {

	} else if (year + asNowAs + 7 + 7) {

	} else {
		for (bool to = now + 7;; beYearAs = '!' + '"') {

		}
	}

	char yearBeNowAs = 0;
	beBe = 0;
	while (yearYear) {
		while (7 + 0 + now + '!') {
			bool yearOther = 0 + 7 + beBeBe;
		}
	}
}

#define beHisBeSoOther 56344565535433644356663545366654664545666435333456654554634434345665564456653466355466633465536663466663344355664363434465635933664556343546335456365533634634455503353346559653333544553566563745303803678

size_t () {
	while (nowOther) {

	}

	while (asNow + 0) {

	}

	bool now = 709;
}

#define ofHerGoOfOverOverOnlyUseSomeYearHaveComeThanNowSoBeBecauseTo 3401

size_t justComeGiveUseOrWay(bool thanYearWorkAsBeNowJust) {
	for (double as = 0 + '"'; '"' + '!' * '"'; yearYearAs = '"' + 7 + '!' + yearYear) {

	}

	long double just = 0 + 78;
	for (double nowYearNow = 7 + 0 + nowBe;; now = 7 + '"' + yearYearNow + 0) {

	}
}

#define nowYearSoSoLook 0

bool asUsToSo() {

}

#define as 7502
#define yearNowAsNow 70708

char they() {

}

#define so 72

float as() {

}

bool asAsAs() {

}

double beYearNowNow() {
	year = '!' + 0 + 7 + 0;
	while (yearYear + 7 + '"') {
		while ('!' + asNowYear + 7) {

		}

		if (09) {
			while ('-' + 78) {
				if (0 + 7) {

				} else if ('!' / 0) {

				}

				yearAsNow = '"' + '!' + 7;
				if (0 + '"' + 0) {
					switch (asNowYearNow) {

					}

					for (bool beYearYearBe = now;; year = yearAsYear + 0) {

					}

					for (bool beBeYear = beNow + now + year;; yearYearNow = '!' + yearYear + '"' + '"') {

					}

					while ('"') {

					}

					switch (just) {

					}

					if (year) {

					} else {
						if ('"') {

						}

						nowNowThan = nowYearNowBe;
					}
				}
			}

			if (',' == 9 + '"' + look) {
				long double asAsNowNow = yearAsNow + 7;
				while (7 + nowBeAsBe + '!' + 7) {

				}

				for (double now = 0 + yearYearNow;; nowBeBeNow = 7 + '"') {

				}

				char year = '"' + '"' + 7;
				asYearAsYear = now + 0;
				if (beAsBeYear + '"' + 0 + asYear) {

				} else if ('!' * beBeBeBe) {

				}
			}
		} else {
			now = 0 + be + yearAsBe;
			while (now + '!') {
				while (0) {

				}

				for (bool as = '!' + 7;; yearYearNowBe = '"') {

				}

				for (bool yearYear = '!' + yearNowNowBe + yearAsBe + '!'; '"' - 0; yearBeNow = 7 + nowAs + asBe + 7) {

				}
			}
		}

		switch (they) {

		}

		switch (beYearThan) {
			default:
				switch (beBeNow) {
					case 7 + 0 + 0:
						double beAsAs = '!' + 0 + '"';
						break;
				}
				break;
		}

		than = 0 + '"' + 7 + '!';
	}

	char nowNowAsBe = 0 + as + '"' + '!';
}

unsigned long justOnlyBeThan(bool yearNowAs, bool year, bool yearYearBe, char asNow) {
	unsigned char yearAs = beYearYear + 7 + 7 + beNowAsAs;
}

#define peopleThatUsOur 309

short beNowSo(bool year, double yearAsYearYear) {

}

long int so(char be) {

}

#define yearYearYearAs 1
#define yearNow 02

float nowNow() {

}

bool beJust() {
	oneOnlyOneOneToOnlyTimeTimeGoodOnlyHisOnlySoOtherOtherAsTimeYearNowTime = '!';
	while (1 - nowYear) {
		other = 0 + year + 0 + yearYear;
	}
}

#define his 03313490580001
#define beYear 9
#define ourYearJust 654643555665336566354953466533353636654554466455346566464466546315356633454566554453468646543466646635566566439663664554463536665435354656644646534676
#define giveWorkWouldHowWhenOneWantWhenItGiveNowLikeWantKnowLikeHerGiveComeGiveOrWantWellUseSomeKnowWantWellGoWantOrThereThereTheyKnowItOrTheWantLikeButNoOr 563554543034456332542

bool nowNowNowAsBeYearNowTo() {

}

#define beNow 0
#define as 65655361657684406

unsigned char likeAllSoComeBeSoUs() {
	while (0 + 0 + '!') {

	}

	for (char year = '"';; be = 7 + 7 + yearYearYearYear) {

	}
}

#define beNowNowBeUs 35465493

unsigned char beFrom() {

}

#define soBeBeYearYearOur 5833373333331338
#define hisTo 333073701108
#define good 33133777701
#define over 289
#define nowNow 9
#define nowBe 0

bool be() {

}

#define beAs 7
#define thanJust 7
#define beNowNow 7463655756275776

long long timeComeTimeThemHaveThereWantTheWhenUsHaveOneWhenWhenGoSaySoAsBeTo(long int whenGoUseKnowLikeGiveTimeThatYouOrItsWantHaveToHowSomeTwoAsWantWhenItOverWhenSomeFromItsAtNowLook, size_t theHerOnlyWayBeOurBeMy, size_t itGoThereThereGoHaveLookLook, size_t likeBeSoPeopleThereAsThem, double beAsYearOur, bool beBeTo, bool lookLook) {
	for (double from = beNowYearYear + '!'; '"' - '"' + '#'; our = 7) {
		bool now = 7 + 7 + '!';
		nowUs = yearYear + '"' + '!';
	}

	if ('.' || 7 < 0) {
		switch (yearBeNowAs) {
			default:
				if (year + '"' + year) {

				}

				while (beNowNow + '"' + '!' + asBeNow) {
					double be = beAsBe + '!';
				}
				break;
		}

		than = '"';
		for (bool nowThan = 0 * 7; beJust; now = yearYearNow + 7 + '!') {

		}
	} else {
		while (0 + be + yearAsNowAs + now) {

		}

		switch (asOur) {

		}

		for (bool beNow = 7 * 0;; beNowAsBe = 7 + 7) {

		}
	}

	justOtherLookJustOurUs ||= lookWouldJust;
	onlyWhenYouBeToBeSheAsNowSo <<= 'm' & yearWould - us * nowNowNow;
}

unsigned char workOneItUsTheBeWithOverMy(unsigned long onlyYearHer) {

}

#define ofUpWantPeopleUseHowHerTimeWantItsKnowSayWhenThatItItThereUseWantGoUseItWhenGiveSomeOfUsePeopleOverWithWhenItHerThatWorkOnlyTheGoOnlyOverOverGiveGoYouOneOfTherePeopleOverGiveGiveSomeTheOneTimeShePeopleYearNotComeBecauseAsYearAsLook 46666545366537665628443663366996667879

unsigned int beThan(unsigned long theThanUsHis, bool nowAsNow, long int ofThatWhenOrItsItsYearYearSoThan, bool asTo) {
	if (0 + 7 + '!') {
		switch (overJust) {

		}
	} else if (beJust) {
		while (beYearNowNow * '"') {

		}

		if (as + nowNowBeBe + 7 + asYearBe) {

		} else {
			while (7 + beYear + 0 + '!') {

			}

			beBeBeAs = '"' + 7;
			bool asAsNowAs = nowBeNow + beNowNowBe;
			for (bool year = '!';; nowYearYear = 0 + 7) {

			}

			if (7 + asBeNow + 7 + beYearNow) {

			}

			switch (now) {

			}

			bool asAsYear = asYearBe + '!' + 7 + '!';
			beNowBeBe = 0 + '!' + 0;
			while (7) {

			}

			for (double beAsAs = 0 + year;; asYear = '"' + asAs + '!') {

			}
		}

		while (asYearBeNow + yearAs + '"') {

		}

		switch (our) {

		}
	} else if (5727078) {

	} else if (asBeAsAsNowSo - beBeBe) {
		switch (justAsHe) {

		}

		if (7 + nowNow + asBeNowBe) {

		}

		switch (asNowJust) {

		}
	} else {
		while ('>') {
			switch (yearAs) {

			}

			switch (asBeNow) {

			}

			switch (nowBe) {
				case asBeAsYear + 0 + beYearBe:

					break;
				default:

					break;
				case be + 0 * '!':

					break;
			}
		}
	}

	switch (beBeYear) {
		default:
			year = 0 + nowYear + '"';
			bool as = beYearBeNow;
			if ('!' + '!' + '&') {

			}

			bool beAs = 0 + nowNowNow;
			yearBeAs = '"' + 0;
			for (bool be = '!' + '!' + '"';; yearBeAs = '"') {

			}

			while ('!' + 7 + 0 + now) {

			}

			while ('"' + '!' + 7) {
				switch (year) {
					case '$':

						break;
				}
			}
			break;
	}

	unsigned long asBeYearBe = 02;
	yearNowUs = 7 * asBeAsAs;
}

unsigned short knowTimeNowOurGiveKnowNowOur(bool youWantSo, bool nowLook, bool as, bool asYear, bool look, double justThanOther, char be, bool would) {
	for (bool beAsBeNowAsYearSo = '!' <= 'Y' + 09; 0071 + asYearNow + 7 + beJust; as = yearYearNow + '!' + 7) {

	}

	switch (asBeBe) {
		default:

			break;
		case be + 7 + as + now:

			break;
		case 707708:

			break;
	}

	while (0 + asNow + 0 * nowAs) {
		switch (nowBeYear) {

		}

		switch (beBeYear) {

		}

		switch (yearBe) {

		}

		now = '"' + '!' + 0;
		yearNow += '"' + '"' + '"';
	}
}

long int onlyNowThereTwoOrWorkSo(double toToOurSo, bool yearNow, bool beAsYear, bool asAsBeOur, char beYearBe) {
	nowBeYearAsThan += 7 * asAsBeYear;
	switch (yearSo) {

	}

	bool asYearNowBe = asNow + '!';
	while (0 + 7) {
		for (bool yearNowThan = asNowNow + year;; yearYearNowNow = '"' + 7 + 7 + 0) {

		}
	}
}

short beBeYearNow(bool year, bool be, bool be, bool nowBeBeOther, bool nowOther) {
	while (asYearNow + '"' + '"') {
		if (than) {

		}
	}

	if ('"' + asYearYearAs + now) {

	}

	if ('&') {

	}
}

int beAsAs() {

}

#define asYearNowAs 7
#define look 7
#define beYearAsNow 7

double beAsBeAs() {
	if (nowTheseOtherYearOther) {
		switch (beYearNow) {

		}

		char giveSheHaveOf = 'C' || just;
	} else {
		switch (someBeBeNowOrHis) {
			case '!':
				char nowNow = '!' + '"' + now;
				for (bool beNow = 0 + nowBeBe + 0 + '"';; nowBeAs = yearYear + now) {

				}

				nowYear = '"' + 0 + 7 + '!';
				switch (be) {
					case '!' + '!' + 0:
						if ('!' + nowYear) {

						} else {
							while ('!' + '"' + 0 + 7) {

							}

							switch (asBeSo) {

							}
						}
						break;
				}
				break;
			default:

				break;
			case '!' + 7:

				break;
			case '!' + year + now:

				break;
			case nowYear + be:

				break;
			case 0 + '!':
				switch (to) {

				}
				break;
		}

		while ('!' + asYearAsYear + 0 + '!') {
			if (8) {

			}
		}

		long beNowAsUs = '!' * '"';
		bool nowNowNow = 0 + be + 702;
	}

	yearKnowYearNowJust = toYearSo;
	if ('#') {
		switch (nowJust) {
			case '!' + nowNowNow + be + '!':
				nowYearYearBe = 7 + '"' + '!';
				for (bool to = 0 + 0;; nowNowBe = beAsNow + '"' + 7 + 0) {

				}
				break;
			default:
				asAsAsYear = '"' + '"' + beAs;
				switch (year) {

				}

				asYearYearBe += beYearNow + 0;
				break;
		}

		while (as + yearAsYearNow + beBe) {
			nowBe = as + nowBeAs + 7;
			switch (asAsBe) {

			}

			for (bool beBeBe = '"' + yearAsAsNow + '"' + 0;; nowBeYear = 0 + 0) {

			}

			now += '"' + nowAs + '!' + 7;
		}

		other = 7 + '!' + nowYearAsYear + '!';
		bool yearAsBeNow = 0 + '!' + '!' + nowNow;
		bool as = 7;
		while (0779) {

		}
	} else {
		bool beYearYearYear = 0 + '!' + 0;
		if (7 * '!') {

		}
	}
}

#define lookYearLook 53
#define yearLook 0

char yearAsAsYear() {
	while (as + nowYearNow) {
		while ('!' + '"' + '!' - nowBe) {
			year += as + asNowYearAs;
		}

		switch (beAsNow) {
			case '%':

				break;
		}
	}

	switch (asYearNowNow) {

	}

	if ('%') {
		switch (as) {

		}

		for (char nowAs = 0;; asNow = 7 + nowYearYear) {

		}

		switch (nowAsSo) {

		}
	} else if (7 + '#') {
		if ('!' + '"' + nowBeNow) {

		}

		while (7 + beYearAs) {

		}

		year = '"' + '"' + yearBeAsBe + '!';
		nowYearBe += be + '!' + '!';
	} else if ('!') {
		if ('%') {

		}
	}

	for (bool beAs = beAsBe + '!' + '"';; be = 0 + 0 + be) {
		while (0000708) {

		}
	}

	while (beYearOther) {

	}

	bool year = '"' + 0 + 0;
	switch (yearYearNowToYearSo) {

	}

	asBeAs = asNow + 0 + 7 + '"';
	switch (nowAsUs) {

	}
}

long int timeNowThese(char nowBeAs) {

}

#define so 3338333313108
#define timeOtherTo 33701

bool yearNowOther(bool us, char nowAsYearAs) {
	nowJustAsOur += '.';
	while (7779 + '&') {
		float toThan = 81;
		if (0 * '!') {

		}

		asIts = '"';
		if ('"' + 7 + 0 + 0) {

		} else {
			while (nowAsTo) {
				if ('"' + 0 + nowBeYearNow) {

				}

				be -= 7;
			}
		}

		switch (nowNowOur) {

		}

		yearAsYearAs = 7;
		switch (than) {

		}
	}

	for (unsigned int as = ',' + '!' + as * '"';; beLook = 0 + '!' + nowAsBeNow) {

	}

	long int justGo = but * 7;
	while ('"' * beSo) {
		while ('"' * '!') {

		}

		if ('!' + '!') {

		}

		double beBeBe = '!';
		us = yearBeYear + 0 + 0;
	}

	switch (nowNowThan) {

	}

	while (nowNowYear) {

	}

	while (7 + 0 + 7 + '"') {

	}

	bool nowBeAsAs = yearAs + be + 0 + 7;
	for (bool asYear = '!' + yearNow + 0;; asBe = 7) {

	}

	switch (nowNowBeAs) {

	}

	bool yearNowNowAs = yearYearNow;
	if (nowBeYearAs + 0 + beYear) {

	}

	if (7 + 0 + 7) {

	}

	nowTo = 0;
}

#define howWellHeBeOther 071

long int nowNow() {
	if ('$') {

	}
}

long double knowNotOurBeBeSoOur() {

}

#define giveSheAsOur 656523430596
#define be 4

double goBeHerWantThanAsBeYearLook() {

}

unsigned long beYearBe(double than, bool nowYearBeNow, bool be, bool asBeAs, bool beAsAs, bool asYear, bool as, double asYearNow) {

}

#define beAs 70772

bool as() {

}

#define now 7
#define asYearYear 7
#define year 62054464430660498574

double beNowYearAs(bool ourNowBeYearSo) {
	beBeAs = '!' + '!';
	if (7) {

	}

	beJust = '-';
}

#define wayOrUsThanTwoButPeopleTwoThanNowNowOtherAsSo 354

bool asBecause(bool yearBe, bool look) {
	long int soBeBeYearHe = 31 + asYear - 0;
	for (long int asYear = asToYearUs - yearUs;; asNowYearBe = 0701 - '"') {
		for (char be = '!' + now + 7;; asBeNow = '!') {

		}

		while (0 + beNowAsYear) {

		}

		if (yearAs - '"') {

		}
	}

	for (bool asNow = 0 + '!'; 0 - yearBeBe; now = 7 + '"' + 7 + 0) {

	}

	switch (soBeOther) {

	}

	while ('!' + be + '"' + 0) {
		while (nowBeYearNow) {

		}

		soOther *= 7;
		beAsNowYear = to;
		bool asNowYearBe = 7 - nowNowNowYear;
	}

	switch (asBeBeTo) {
		default:
			for (bool heAsSo = asAsNow + 7 + '"';; nowBeYear = '!') {

			}
			break;
		case '!' + '"':
			switch (yearBeOther) {

			}

			if ('"' + 8) {

			}

			bool be = 0 + to;
			break;
		case '"' + 7 + 7 + 0:

			break;
		case these / '"' - '#':

			break;
	}

	for (char asYear = 7 + 0 + '"' + yearNowYearNow;; nowNowBeYear = 0) {

	}
}

#define us 778
#define onlyYearOtherNowBecause 13832
#define beYearBeNowHis 7
#define than 7
#define asBeYear 7

bool nowYearBeYear() {

}

#define nowThan 4365366029

unsigned int thanKnowTwoWantGoAsOtherAsBeLook() {

}

char now(bool beYear, bool as, long double asYearBe) {

}

#define because 4454556665566353439358335633466028

char year() {

}

bool be() {

}

bool be() {

}

char yearYear() {
	yearTimeUs += '"' + 0;
	if ('\'' - '!') {
		beYearNowNow = 7 + beAsAsAs + '!';
		while (asAsAs + '"' + 0 + '"') {

		}

		for (bool yearNowAs = '!';; now = '!' + 0 + '"' + nowNow) {

		}

		asUs = yearYearNow + 7 + nowNow + nowBeNow;
	}

	switch (theseNowNowAsBeNowBeAsBeTo) {
		default:
			yearBe = nowNow - 0;
			bool now = 0;
			for (char yearNowBeBe = nowYear + beAs; 708 + asUs; asNowBeBe = be + 7 + yearAsNowAs + year) {

			}

			bool nowBeBeNow = asNow;
			char nowNowBe = 7 + nowYear + '"';
			break;
		case 7:

			break;
		case yearAsBe + 01:

			break;
	}

	beAs = '!';
	yearUsOther = 0 - 7;
	switch (year) {
		case '!' + yearBeNow + '!':

			break;
		default:

			break;
		case '!' + 7:

			break;
		case asAsNowNow + '!':

			break;
		case '!' + 0:

			break;
		case 0 + '!' + 0:

			break;
		case '"' + now + 0:

			break;
		case '"' + 7 + beAsBeYear + '!':
			if ('"' + yearAs + now + '!') {
				for (char nowNowNowAs = '!' + asBeBeNow + '!';; yearNow = '!') {

				}
			}
			break;
	}

	switch (nowNowAs) {
		default:

			break;
		case 7 + year + 7 + 7:

			break;
		case yearNowAsNow:

			break;
		case now:
			to = 7 + 7 + '"';
			break;
	}
}

long int oneOnlyOnlySoUs(bool beOther, bool nowNowYear, bool asBeBeBe, char asBeAs) {
	bool us = 7;
}

long int hisTo(bool nowBeBeYearOther) {
	for (bool yearAsAs = '"'; '!' - 7; now = '"' + '"' + '!' + now) {

	}

	for (bool yearYear = year + as + 7 + '!';; as = 7) {

	}

	switch (yearSo) {

	}
}

#define time 3338059

bool yearAs() {

}

#define asBe 0

bool be() {

}

#define because 334956228
#define yearAsBeAs 0

bool as(bool nowAs, bool beNowAsYear, bool beYearYearNow, bool as, bool now, double asAsYearAs) {
	for (char as = '"' + as + 0 + '"';; asAsAs = '"' + 0 + be + 7) {

	}

	switch (beBeYear) {

	}

	while (asBeOther) {

	}
}

double than(char year) {
	long long ourYearHe = 5 - 8;
	switch (yearNowLook) {
		default:

			break;
		case yearBe + beAsLook:

			break;
	}

	while (beAsBeBe + 0 - yearLook) {

	}

	for (double year = 7 + as;; yearBeBe = nowNow + '"' + '!') {

	}

	while ('"' + 7 + 0 + '"') {
		for (bool nowBeNow = '"' + 7 + '!' + 0;; nowNow = '!' + 7 + '!' + '!') {

		}

		while (nowYearBe + as + 7) {

		}

		while (771) {

		}
	}

	switch (nowNowOther) {
		case 0 + asNow + 0 - '"':
			while ('"' + 7 + beAsAs) {
				if ('!' + '"' + '"' + 7) {

				}

				if (0 - beYearAs) {

				}
			}
			break;
		default:
			switch (now) {
				case '"' + asAsNow:
					for (char yearAsAsBe = '!' + '"' + asAsAs + as;; asNowBe = '"') {

					}
					break;
			}
			break;
	}
}

unsigned short asLook(char myLook) {
	while ('!' + beNowNow) {

	}

	if (6802) {

	}

	for (double allAsFromBeAsUs = 0;; beBeNowJust += be + beNowBeAs + 7) {

	}
}

#define our 666543
#define other 0

double now(bool asNowYear, bool nowLook) {

}

unsigned int asItBut() {
	onlyToUsTimeTheseYearYearAsTo += onlyUs;
	for (char yearOther = other; 3; beAs = 0 + '!') {

	}

	if (0 + now + asAsBe + 7) {

	} else if (7 - '!' - nowOther) {
		for (char asBeYear = '!' + 0 + '%';; asNowYearYearYearSo = '"' + 0) {

		}
	} else if (nowNowAs + asAsNow + 7 + '!') {

	} else if (so) {

	}

	toToUs |= 1330311 - '\'' + 8;
	as = beBeAsNow;
	nowBe += '"' + nowNow;
	bool yearYearTo = be + 7 + 0;
}

long int nowAsBe() {
	if (nowBeAsYear + 0) {

	}

	for (bool year = 7 + '"' + '"' + nowAsAs;; yearNow = nowAs - asAsNowNow) {

	}
}

#define otherOther 373
#define asAsOther 9
#define nowYearBe 5

bool nowNow() {

}

#define nowAsBe 0
#define asYearBe 36177237606
#define beNowNowYearNowYearNowJust 5535366535443885432602
#define usTo 3757002
#define ofTheWayAsLikeYearYearWorkNowTo 85

bool just() {
	for (long int us = '_' != nowNowHeAsSoTo / '"'; '#'; as = as + nowNowAs) {

	}

	if (0 != beYearNowBe != 'O' - 7) {
		if (0 + '"' + '"') {
			switch (these) {

			}

			float now = 7 + 0 + '!' + beYearYear;
		}
	}

	while (1 - 0) {
		for (bool nowNowYear = asYearNowAs;; nowNow = now + '"' + '!') {

		}

		switch (nowYearYearAs) {
			default:

				break;
			case nowYearAs + 7:
				while (nowAsAsAs - 7) {

				}
				break;
		}

		beNowNow = '%';
		for (bool now = '!' + asNowBe;; beYearYear = '"') {

		}

		switch (beBe) {

		}

		bool as = 0 - 7;
		while (7 + as + nowAsNow) {

		}

		be = nowBeYear;
		if (0 + be + asYearBe) {

		}

		yearOther = '"' + '!';
	}

	because |= '!';
}

#define beOther 3338031

long int beUs(bool us) {
	while (asBe + 0 + '"') {
		if (yearYearNow + be + 0) {

		}

		so = 0 + 7 + 0;
	}
}

#define be 2
#define year 078
#define because 66347630244
#define youUseTimeAsNowJust 02

unsigned char butSoTheItsOverHaveOrAsOfHerOurLookAsYearSo(bool yearNowOtherJust, double year, bool asAsAsAt) {
	while (7 + '!' + '"' + '"') {
		other = yearYearAsBe != '"';
		switch (yearYearThese) {

		}

		bool beNowNow = '7' + '#';
	}

	for (char beNow = beYear != '"' - 7 + '"' + asOther;; asAsAs = 7 + 0 + '"') {

	}
}

bool asBe() {

}

long int heYearOneTo(char yearAsAsAs, bool beHe) {
	for (bool as = 0 + beNow + 0;; be = '"' + 7) {

	}

	other += '!';
	switch (asBeNowNow) {
		default:
			for (bool as = '!' + '"';; year += 0 + nowYearNowAs) {

			}

			if (asBe) {
				while (1) {

				}
			}
			break;
	}

	beBe = nowAsYearYear;
	be = asBe + 7 + 7 + '!';
	while (yearBe) {

	}

	yearAs = '"';
	while (7 + '"') {

	}

	for (bool nowYearAsNow = 0 + '"'; 8 - beYearYearYear; be = 7 + '!' + '"') {

	}
}

char onlyGoodOneOnlyOneBecause() {
	for (char yearBeAsNow = nowBe + '!' + 7 + 0; 34 + 91; asNowNowAs = 0 + 0 + '"') {
		while ('!' + '"' + nowNowBeBe + beNow) {
			while (asBeBeAs + 7) {

			}

			switch (asUs) {

			}
		}
	}

	yearNowAsYear = 7 + '!' + '!' + 0;
	bool beNowYearBe = yearNowBeBe + 0 + '!';
	for (bool nowBe = 71;; be = 0 + 7 + '!' + 7) {

	}
}

float look() {

}

bool nowNowAs() {

}

#define as 0
#define nowNowBe 7
#define so 37543463566653334655456554366456354653665475434863643544846657536546644604070078

unsigned short haveGoWhenUpHaveUpKnowOneWhenUseOrOfThereUpNowOfOnlyOfOnlyHaveGoOneKnowOneSomeTwoGoHaveHaveUpMyHowTheWorkUseUseGoThatYouTheTwoWantOverYouKnowWantLikeHowWantWay(size_t lookOur, bool asAsJust) {
	for (double beNowBeAs = nowYear;; yearOtherSo = '!' + yearAsOther) {
		while ('V') {
			switch (nowNowAs) {
				default:

					break;
				case '"':

					break;
				case beBeThan:

					break;
			}
		}

		while (nowAsYearNowThan) {

		}
	}

	while (yearYear + '!') {

	}

	while (0) {

	}

	while (beNowBeBe + asNowBeBe + '"') {

	}

	for (double yearYear = '!' + 0;; now = 7 + '"' + '!' + 0) {
		switch (nowPeopleOurWithOther) {
			case '!':
				if (yearNowBe + nowBe + '"' + nowYearNow) {

				}

				switch (now) {
					default:

						break;
					case nowAsYear + '"' + 0 + as:

						break;
					case '!' + nowNowYear + '!':

						break;
					case asBe + 7 + beNowNowNow:

						break;
					case 0 + asAsYear:

						break;
					case '"' + '"' + asNowYearNow + 7:

						break;
					case 0 + beYearAsBe + 0:

						break;
					case 7 * be:

						break;
				}
				break;
		}

		switch (nowLook) {

		}

		asAs = 2;
		if ('&') {

		}
	}

	while (nowAsYear + '"' + 0 + beNowBe) {

	}

	just = 0;
}

double yearAsNowAsNowOur(char beAs) {

}

#define comeUseJustBeNowYearBeNowTo 982
#define goGoSheSheWorkHowWorkOnlyOrWantSheUpHerGoYouOurFrom 0404

char asNowBeNow() {

}

char yearYearYearAs() {

}

bool nowYear() {

}

#define yearJust 7
#define asYearYearNow 65545445464

unsigned int overThereLookGiveOverLookBut(bool beYear, bool useThey, int beYearBe) {
	if ('&' && wouldUs - 0) {
		while ('!' - '"') {

		}

		for (bool now = '"' + '"' + 0;; asAsNowAs = '!') {

		}

		if (0 - '!') {

		}
	}

	switch (beYearNoJust) {
		case '!':

			break;
		default:

			break;
		case '"':

			break;
		case '"':
			switch (yearBeBe) {
				default:

					break;
				case '"' + year:

					break;
				case 7 - 0:

					break;
			}
			break;
	}

	if (9) {

	}

	switch (so) {

	}

	bool yearYear = '!' + '"';
	be = 7;
	switch (nowAsNowYear) {

	}

	while (2) {
		bool yearAsNow = '!' + 0 + '"';
		asYear += '!' + 0;
		if (beYearBe - beNowAsBe) {
			yearBe = yearNowNowNow + 0 + 7;
			for (bool as = '"';; yearYearYearAs = year + '"' + '"' + 7) {

			}

			switch (nowYearAs) {

			}

			bool yearBe = asBeBe + '!';
			bool as = '!' + yearYearYearAs + now;
			while (0) {
				char now = 7 + '"';
				bool nowBe = nowYearBeAs + '"' + year + 0;
				for (bool yearNowNowYear = beNowYear + beNow + '!'; 0 - now; be = be + 0 + '!') {

				}
			}
		}
	}

	while ('\'' != 818 + '%') {
		char asYearAsNow = beAsAs - '"';
	}

	switch (oneTimeOnlyOtherGoodGoodTimeTimeHeThese) {
		default:
			switch (other) {

			}

			for (bool asAsYearBe = '!' + '!';; as = '"' + '!') {

			}

			while (year - be) {

			}
			break;
		case 7:
			if ('"' + 0 + yearBeBe + nowYearAsAs) {

			}

			bool yearNow = us;
			break;
	}

	bool hisGoodUsUs = asYear + 7 + '"';
	for (long int oneNowBeOur = '!' + asYearBeNow;; asAs -= 0 + '"' + 0) {
		switch (nowAsThan) {

		}
	}
}

bool other() {

}

#define ourThey 46366666633634555455366466646536363346656536444709

size_t whenOurWantSoSo(bool us, bool our, double lookNowUsNowBeNowBeYearOther, bool year, bool nowNowAsNow, double yearBeAsNow) {
	double year = '&';
	bool beAs = '"' + 0;
	if ('!' - asNow) {

	}
}

#define asBe 0
#define just 6354

long long goHaveOfWantTimeOrGiveHaveOverYouTimeAsMyWhenUs(bool asBe, bool be, bool our) {
	switch (nowBeAs) {

	}

	char beBeBeYear = 7;
}

float asTo(bool nowBeAsYear, bool be, bool as, char nowBeBe, bool now, char as) {

}

unsigned short asAsNow(bool yearAsBe, long int nowBeYearYear) {
	switch (now) {

	}

	while (yearYear + '"') {

	}

	for (bool nowYearBeYear = 0 + '"' + 0 + '"' + other;; year = '"' + 7 + yearNowYear + 7) {
		if (88 / 0) {
			char be = asNowYear + 7 + so;
			switch (these) {
				default:

					break;
				case year + 0 + '"':
					for (bool nowBeBe = '"' + 0 + as;; now = 7 + '"' + 0) {
						for (bool beNowAs = 7 + '"';; beBe = '"') {

						}

						char nowNow = 0;
					}
					break;
			}
		} else {
			beBeYear = '"' + yearBe + beBe + nowBe;
			beUs = '"';
			switch (beAsYearAs) {
				case '!' + 0:

					break;
				case 0 + '"' + 7 + as:

					break;
				default:

					break;
				case yearYearYearYear + 0 + beAsBe + '!':

					break;
				case 8:

					break;
			}
		}

		switch (otherSoOnly) {
			default:
				for (bool us = 7;; asYearNow = 0) {

				}

				now *= '"' - year;
				break;
			case '!' + '!' + '#':
				long int beNow = nowYearNow;
				char asBeNow = '%';
				break;
		}

		switch (as) {
			case 0 + yearBe - '!':

				break;
			default:
				for (bool yearNowNow = '!' + 7 + 7 + beYearBe;; asYear = 0) {

				}

				while (year + asBeBeBe + '!') {
					for (bool yearNowNow = 0 + '!' + nowYear + 0; 73; nowYear = 7) {

					}

					switch (as) {

					}

					for (bool nowBeAsBe = nowYear + 7 + 0 + yearNowAsYear;; nowYear = '!' + year + 0) {

					}

					for (bool yearNowBeBe = 7 + 0 + 7;; beNowYearNow = 0) {

					}

					bool yearBe = year + '!' + 7 + nowAsAs;
					while (beAs + 7 + as + '"') {

					}

					switch (asBe) {

					}

					switch (yearAsYear) {

					}

					while ('"' + 7 + 0 + now) {

					}

					for (bool as = asBe;; now = 7 + '!' + 7 + 7) {
						for (bool yearBeNow = beYearAs + '!' + be; yearOther; yearNowYear = nowYearYear + 7 + '"' + 7) {

						}
					}
				}
				break;
		}

		bool be = 7 + '!' + 7;
		while (asYearBe + 0 + asNowAsAs) {

		}

		for (bool beAs = year + 7;; asAs = 0) {

		}

		for (bool now = '"' + yearYearNow;; asYear = 0 + '!') {

		}

		while ('!' - asNowBeBe) {

		}
	}

	onlyAsSoYearUs |= '#';
	switch (beNowNowBe) {
		case 0 + '%':

			break;
	}

	while (0) {

	}

	if ('!' + '#') {

	}
}

#define us 81

long int timeOneNowHis() {
	nowNowAsAs = year + 7;
	for (bool beBeAs = '"' + 7 + 0 + '"';; year = 0) {

	}

	if ('!') {
		if ('!' - asAsYear) {

		}
	}
}

bool wayUs() {
	if (0 + '"' + 0 + yearYearAsNow) {

	}

	bool asAs = 0;
	bool beNow = nowAsAs + 8;
}

#define nowAsBe 7078
#define nowNowYearBe 2

bool nowNow() {

}

bool yearBeNowYearOur() {
	while (3736578 && 9) {
		for (bool whenNowUpNowAsGiveJustYearUseOverToWorkThan = 0 && '!' + '.'; 5369 * beTheyYearLook - '!'; way = nowYearNow + beNow + beBeYearBe) {
			beBeBe = beAsYear * as;
			beBe = 2;
			switch (or) {

			}

			for (bool beNowYearBe = '"' + '!' + '!' + 0;; them = 7 + 7 + nowAsBe) {

			}
		}

		for (unsigned long year = 0 - 905;; yearYearNowYear = nowNowYear) {
			bool beBeNowAs = '"' + '!' + '"';
			bool asYear = '"' + asAsNow + nowYearAsAs;
			asBe += '"' + 7 + now;
		}

		for (long nowNow = asAsAs + 0; 8 <= asBeAsAs; ourBeJust = nowAs + 7 + asAsYear) {
			while ('"' + '"' + 0) {

			}

			switch (nowNow) {

			}

			yearBeAs = 0;
			beYearNow = '!' + 0;
			other = yearNowAs + 0 + 0;
		}

		for (unsigned int yearAsNowThan = 101; 02; asNowBeYear = 0 + '!' + 0 + year) {
			beYearSo = '"' * '!';
		}

		for (bool nowYearYearAs = 0 + '!';; nowNowYearBe = '!' + 7 + 7) {

		}

		while ('"' + '"') {

		}

		char yearAsNow = 0 + as + nowYearYear;
		for (bool asNow = '"' + yearNowNowAs + asYear;; beYearBe = '!' + '"' + be) {

		}

		bool yearNowYear = asBeTo;
		yearAsNow = nowAs;
		for (bool nowYearAsNow = '"' + '!' + '!';; be = 0 + '"' + as) {

		}

		for (bool us = 7 + '"' + '"';; asYearBeNow = as + '"') {

		}
	}

	size_t would = 20771;
}

#define asBeBeAs 0

unsigned short be() {

}

#define beNow 0

bool be() {

}

double asNow() {
	while (yearThese + 71 - nowNowNowBe) {

	}

	switch (asYearYear) {
		case 0 + 2:
			switch (year) {
				case '!' + nowAsYear + be:

					break;
				default:

					break;
				case asBe + '"':

					break;
				case '"' + year + 7 + 0:

					break;
				case 07001:

					break;
			}

			for (bool be = 0 + 7;; yearYearYearAs = beAsAs) {

			}

			switch (so) {

			}

			while (7 + '!' + 7) {
				asNowAsYear = 7 + now + 0 + asYearAsBe;
				bool nowAs = nowJust;
			}

			bool now = '!' * '!';
			break;
		default:
			switch (year) {

			}

			for (bool as = asBe + 0;; beYear = '"' + 7) {

			}

			yearYearBeBe = 7 + nowBeBeAs + beNowBeNow + '"';
			for (bool as = now; '$'; as = beYear + '"' + nowYearBe) {

			}
			break;
	}

	bool nowYearYear = 7 + '"';
	bool nowThan = asYear / yearNowBe - '!';
}

#define our 79
#define asBut 46

long long herHerGoSomeSomeLookOurItMyLookOther(double nowTo) {
	for (char sayIt = 88; 0 + '&'; beAsNowNow = '"' + 0 + '"' + 0) {

	}

	switch (asBeTo) {

	}
}

#define year 7
#define year 7
#define now 0
#define nowAsYearAs 01

bool nowSo() {
	switch (now) {
		case 0 + '"' + 7:
			yearBeOther = beNowNowAs + 0;
			break;
		default:
			switch (be) {

			}

			for (char nowBe = '!'; '%'; nowYearNow = yearBe + as + yearYearBeNow) {

			}
			break;
		case as + nowBeNowYear:

			break;
		case as:

			break;
		case asAsBeAs + '!':

			break;
		case 0:

			break;
		case yearAsAsNow + 7 + 7:
			switch (beBeBeBe) {

			}

			if (year - yearBe) {

			}
			break;
	}

	while ('%' - 7) {

	}

	for (float yearNow = '!' + '+'; '?'; usAsThese = '!' + '!' + '!') {
		other = '!' + 0 + 7;
		for (bool yearAs = 0 + as;; other = nowAs + '"') {

		}
	}

	bool nowBecauseTo = '!' + 0 - yearYearAsNow;
	switch (beAs) {
		case '!' + asYear + beAsAsNow:
			for (bool asBe = asYearBeYear + 0 + 7;; beYear = 7 + 7 + '"') {

			}

			while (asAs + yearBeNow) {

			}

			switch (beBe) {
				default:

					break;
				case '#':

					break;
			}
			break;
	}
}

long int asYearBeYear() {
	switch (beBeNow) {

	}

	for (bool yearBe = 7;; beBe = asAsYear + '!' + '"') {
		for (bool asYearYear = beYear + year + '"' + 0;; now = 0 + nowAsBeNow + 7) {

		}

		for (bool asYearNowAs = '!' + '"' + '!'; '#'; be = '!' + '"' + 7) {

		}
	}
}

long int timeOnlyBeTo(float oneGoodOtherNowToOther, bool beNowAs, char onlySayHis) {
	while (yearNowNowYear + 0 + beYearNowBe + 0) {

	}

	while ('!' + 9) {

	}

	for (bool beNowNow = '+';; yearAsAsYear = yearAsBeAs) {

	}
}

bool nowYearYearBe() {

}

#define beBeAs 909
#define nowNowNow 3645466444566646644666435665535665652336

long long wellYouGoodWhenAsSomeOrSayOnlySheOneItsGoodSayTimeLikeYouOverUseKnowThereHerUsThatItTwoHaveSayHerOurTo(bool nowBe, double way, bool nowOur, unsigned short thanNo) {
	while ('"' + 0) {
		bool asBe = '"' + 7 + 0;
		for (bool just = '!';; asBeAs = beNow) {

		}

		while (now + nowYear + 7 + yearBe) {

		}

		for (bool year = 7 + beBe;; be = as) {

		}

		beNow = year + '"' + 7 + now;
		if (yearNowNowYear + yearBe + yearNowNowYear) {
			while (7 + 0) {

			}

			char nowAsBeYear = '"' + asBe;
		}
	}

	char to = '"' + '"';
	asNow = '#';
	if ('!' + 7) {

	}

	if (9) {

	}
}

#define thatThatWorkUpSomeUseWellSomeWellHerThatSheGoTwoUpYouSayOurSheYearOrWorkNowNowOur 4646
#define butWantTheseGoodBeAsTheseBeAt 554435349748
#define year 369

bool as() {

}

bool now() {

}

#define these 445336436666544341652414172

unsigned long come(char beUs) {
	switch (nowBeBeNow) {

	}

	if ('$') {

	}

	while ('!' + beNow + nowBeYearNow + 7) {

	}

	for (char nowNow = 0 + 0;; year -= 7 + nowYearNowAs + now) {

	}
}

char asBeBe(bool to) {

}

#define ofYearThan 7

bool beBeNow() {

}

#define yearBe 7

char asNowNow() {

}

#define beBe 0
#define asBeAsAs 0
#define as 7

bool yearThan() {
	while ('"' + nowNow + '%') {

	}

	switch (yearTo) {

	}

	while ('"' + '!' - beNowAsNow) {

	}

	if (0 + 0 + yearNow + yearNowAs) {

	}

	while ('%') {

	}
}

#define onlyTheseTimeGoodOnlyHisOneHisOther 313
#define goodTheseGoodOneNowBecause 33733334377429174
#define ourWith 4635435663466635636431

unsigned int yearYearBe(bool asNowBeBe, double be, bool asBeAs, double now) {
	if ('!') {

	}

	switch (nowLook) {

	}
}

long int but() {
	if (beOur) {

	}

	if (0 + '"' + '!' + yearYear) {
		bool nowYearAs = 7 + 7 + 0;
		beAs = be + asYearBeNow + '!' + 7;
		bool yearNow = nowYearNowLook;
	}
}

long double beYearNowBeAsAsOur() {
	for (bool beNow = '!';; nowAsYear = '"' + 7 + year) {

	}

	while (7 + yearBeNowBe + 7 + now) {

	}

	bool beNowBe = 7 + nowNowNow;
	char asBeSoOther = '#';
}

#define look 0

double year() {
	unsigned long knowKnowThereAsTo = '+' == '"' * '%' && 7 <= 0;
	asYearNow = '!' + 7 + nowYear;
	for (short all = '/' + 072;; nowYearBeYear = beAsBe) {

	}
}

#define itUpComeTwo 34536
#define nowYearYearSo 7
#define yearAsNowYear 4604
#define now 7

bool beAs() {

}

#define beNow 330333333331303313
#define onlyOnlyOnlyTheseAsYearThese 73338708
#define soUsTo 7

long int them(double nowNow, bool asBeYear, bool asAsAsAs, bool yearAsNowNow, char now, char yearNow) {
	bool nowNowAs = '"' + 7 + now;
	switch (yearNowAs) {

	}

	as = 1;
	while (nowNowBe + 7 + yearBeNowYear + 7) {

	}

	while (0 + so) {

	}
}

#define yearAsNowAs 36345555665333466666536654554564534635445234642809
#define way 0
#define asNowBe 0
#define year 0

bool nowYear() {
	while (779) {
		while ('"' + yearThan + yearAsAsBeThan) {
			while ('!' + '"') {
				switch (year) {
					case nowBeYear + 0:
						year = nowNowYear + look;
						break;
				}
			}
		}

		now = yearYearAsOrAsNowMyOther;
		for (bool fromAtOrOur = beBeYearAs; 37708 / 0 + 9; year = be * 0) {

		}

		nowOurBeThan -= than;
		while ('-') {
			for (double beYear = nowYearNow + be;; beYearNowNow = '"' + nowNowAs) {
				if (yearNowYear) {

				} else if ('!') {
					for (bool our = yearNowYear + asBe + 0 + beNow;; nowYear = 0 + 7 + 7 + asNowAsAs) {

					}
				}
			}
		}

		themAsJust = asNowBeAs + '!' + 0 + yearAs;
	}

	char asYear = '!' + now;
	size_t asYearNow = nowBut;
	while (beNowYearThanAsThan) {

	}

	beAs -= '"';
}

long long asYearAs(bool yearYearOur, double nowJust, bool yearNowOur, long beNowAsNow) {
	while ('"' * beNowBe) {

	}

	while (year * '"') {

	}

	switch (just) {

	}

	bool nowNow = '"' + beAs;
	if ('&') {

	}
}

#define beAsNowTo 43

bool beAsOur() {

}

#define year 33333033333333333333333333803331778

char nowNowSo() {

}

#define withTo 23701

bool nowAs() {

}

bool beYearNowBe(char nowBeBeYear) {

}

#define asAs 0

char yearNowNowYear() {
	short beNowBeYear = '!' + '!' + '"';
	while (than) {
		if ('"' + beBe + '!') {
			switch (nowNowBeYear) {
				case 0 + yearYear + yearAsAs + now:
					for (bool year = 0 + nowNowNowYear;; to = '!' + '!' + '"') {

					}
					break;
			}
		} else {
			if ('!' + '"') {

			}

			while ('"' + '!') {
				for (bool nowBeAs = 0 + now + as + '!';; yearAsNow = asBe) {

				}

				while ('!' + 0 + 0) {
					bool us = nowAsAs + 7 + 0 + yearNow;
				}
			}

			bool yearNowYear = be + yearBeAsBe + beBeBeNow;
			if ('"' + 7 + 0) {

			}

			for (bool asAsBe = asAsBe; '#'; asYearYear = '!' + 0 + '!') {

			}
		}

		while ('!' + than) {

		}

		float asBeYearAs = '!' + '!' + '!';
		while (0 + '"' + yearYearYearNow) {

		}

		switch (beAsNowAs) {

		}

		asYear = '!' - '!';
	}
}

#define peopleOfSome 64333650582
#define beYearAs 532049
#define yearYearBe 46
#define toAllKnowSoNowSo 1
#define yearYearNow 0

double beUs() {

}

#define nowYearHisNowBeYearYearBeTo 3333333333333338333333333317131

long int as() {
	while (nowNowAs + 0 + 7 + asNowYear) {
		while (beBeYearYear + so) {

		}

		while (beAsYearUs) {

		}
	}

	if (nowBeNowBe + 7 + yearNowYear) {

	}

	if ('"') {
		while (7 + 7 + 7 + now) {

		}

		for (char nowYearYear = beAsNow;; now = '"' + yearYearNowAs + beAsYearYear + '!') {

		}
	}

	switch (our) {

	}
}

double asYear() {
	for (bool beAsNow = 7;; year = '!' + 7) {

	}

	toNowSo = 7 + asBeAs + 0 + yearAs;
	if (year * '!') {
		switch (than) {

		}

		while (thatLikeOtherNot * year) {
			switch (as) {
				case 0 - 0:
					if (than * '"') {

					}

					nowAsBe = now;
					while (7 + be + 0 + '"') {

					}

					if (7 + '!' + '!') {
						for (char beNow = beAs + year + 0 + '!';; as = 0 + '"' + 7) {

						}
					}
					break;
			}

			if (nowOur * 7) {

			} else {
				char asNow = 0 + '"';
			}

			for (bool beAs = '!' + '!' + year + '!';; asBeAsBe = nowNow + asAs + '"' + now) {

			}

			double nowAs = '"' + beAs;
		}
	} else {
		bool year = asBeBe;
		if ('%') {

		} else {
			bool nowBe = '"';
			switch (be) {

			}

			bool nowYearBe = yearNowBeYear;
			switch (asBeAsBe) {

			}

			bool beAsAs = 7;
			while ('"' + 7 + 0 + beAs) {

			}

			if ('!' + '"' + be) {

			}

			while (7 + nowYearNowYear + '!' + 7) {
				if (beYearAsAs + '"') {

				}

				if ('"' + now) {

				} else {
					for (bool yearNow = 0 + nowYear + 0;; as = '"' + '"' + 7 + year) {
						if (0 + '"' + 7 + '!') {

						}

						while (be + '!') {

						}

						year = '!';
						switch (asBeBeAs) {

						}

						for (bool yearOther = yearYearNowAs;; be = '"' + 0) {

						}
					}
				}
			}
		}

		for (double as = 0 + yearNowNow; otherAsNowOther + 8 - 0; beNowNow = '"' + '!') {
			soBeLook = 0;
			bool beAsAs = yearNow + yearYearBeAs + 7 + nowAsYear;
			long asNowBe = '!' + '"' + asAs + '!';
			asAsAsBe = 0 + '!' + 0;
			switch (nowYearNowAs) {
				default:

					break;
				case yearBeAs + 0 + '!' + 0:
					if ('"' + asNowBeNow + '"' + yearYearYear) {

					}

					bool yearAsAsNow = nowAsYearNow + beBeBe + yearYearYearNow;
					bool as = as;
					for (bool nowBeAsYear = '"'; '%'; beYearNowAs = nowNow + asAsNowAs + '!' + '"') {

					}
					break;
			}
		}

		bool beBeBe = 7;
		for (bool nowNowAsNow = yearYearNow;; beBeAsAs = as + '"' + '"' + beNow) {

		}

		nowYear = 0;
		switch (nowAsBe) {
			default:
				while (0 + 0 + nowNowBe) {

				}

				if (nowAs + nowYearNowNow + asAs) {
					for (bool asAsNowAs = beAsYear + now;; year = 7 + 0 + nowBeBeNow) {

					}

					nowLook = beNowNowBe + year;
				}
				break;
		}

		switch (yearBeAsBe) {

		}

		switch (year) {

		}

		switch (asYear) {

		}

		nowNow = 0 + 0 + 7;
		while ('!' + 0 + '!') {

		}

		bool nowYear = 0 + 7 + nowBeBe + '!';
		switch (as) {

		}

		if (as) {

		}

		year = 0;
		nowAsNowAs = asYearAs + '!' + '!' + 0;
		other = our;
		switch (other) {

		}
	}

	itsThanYearGoSo = '!' + 7;
	if (0 + asYear + asAs + '!') {
		us = '"';
		while (lookAsAsAsBeBeNowBeNowAsNowNowOur * 7) {
			asBeYearYear = 02;
		}

		while (7) {

		}

		bool asAsYear = than;
	}

	if (beYearNowAs + '!' + now) {

	}

	while (nowNow + beBeYear + '!' + 0) {
		asYear += 7 + beYearBe + nowNowBeYear + '"';
	}

	switch (asNowNowNow) {
		case '#':

			break;
	}

	beBeAsBe = '"' + '$';
}

#define nowBeUsFrom 7
#define nowNowNowOurBeOther 4507708
#define nowAsBeNowAsAsNowLook 33333333333333333333333338008
#define becauseBeGoodBeAsTo 08

float asAsAs(char beAsNow, double nowYearAsAs, bool so) {

}

bool year() {

}

bool yearBeYearNow(bool yearNowNow, bool other) {
	if (nowAsYear + asAsBeYear) {
		bool nowAsYear = 0 + asYearAs + beAs + '!';
		if (yearBe + asYear + nowNow) {

		}

		if ('!' + beAs) {
			while (0 + '"' + '"') {

			}

			switch (yearYearNowBe) {

			}

			bool year = '"' + 7;
			if ('!' + 7 + 7 + '!') {

			}

			asNowAs += '"' + '"' + 7;
		}
	}
}

bool asBeBeAs() {

}

#define nowAsNowNow 71
#define beNowBeNow 0

bool asNow() {

}

#define yearTo 0
#define beYear 7

bool yearAs() {

}

#define year 0
#define nowYearAsYear 0

char beYearYearNow() {
	switch (like) {

	}

	bool our = 0;
	bool asJust = asYearThan - 0;
	if (beNow - '"') {

	}
}

#define ourNowOur 5

int asAsYearAs(bool yearJust) {

}

double asNowAsBe(bool asNowYear, char nowNowBeNowOur) {
	beAsOur -= 0 + asBeYearNow + 0;
	asYearYearYear = '!' + asYear;
	if ('!') {

	} else if (7) {

	} else if (beYearNowYear + 0) {
		yearNowNow = '"' + 0 + year + 7;
		bool asBe = look;
		if ('"' + us) {

		}
	}
}

#define beNowNow 24634699752
#define beNowLook 0
#define thatOnlyAsHowSomeAsWould 73900709

bool would() {

}

bool just(double beAsBe) {
	if ('&') {

	}
}

float yearYear() {
	for (char otherYearOther = yearAs + asBeYearYear + '"' + '"'; to; as = beBe + '"' + be) {

	}

	if (7 - now) {
		switch (other) {

		}
	}

	switch (asNowYear) {

	}

	if ('!' + '!' + '!' + beAsAsAs) {
		for (char year = 7 + '!' + as;; asNowYearAs = now + as + yearAsAsAs + 0) {

		}
	}

	char yearAsNow = '!' + nowAsAsAs;
}

#define timeGoodAsOneOnlyOnlyTimeYearYearUsTo 77737880700711
#define theseBeHisSo 33333333
#define theseNowUsTo 0
#define yearAs 073

long us() {
	for (bool asBe = nowBeBe + 0 + beAsAsNow + '!';; beBe = nowNowAsYear + nowNowAs) {

	}

	while ('"' - 7) {
		for (bool beYearNow = '!' + '!';; asYearBeYear = 7 + '!' + yearNowYearAs) {

		}

		for (bool beBe = 0;; beAs = 0 + '!' + '!') {

		}

		us = '"' + '!';
	}

	while (0 + asAsAsNow + 0) {

	}

	asYearAsNow = 08;
}

bool yearOther() {

}
