#include <UU/6U//2.h>
#include <UUUUUE2.h>

#define as 07001
#define nowNowAsNow 33356344677454653465330943

bool nowNowBeAs() {

}

unsigned short beOur(long toNowUs) {
	as += '!' + 7 + 7 + '"';
}

long double yearNowBeBe() {
	unsigned char knowWorkSomeKnowHowWellOverUsePeopleTwoTheOrLikeLikeAsTheseNot = 9979 + '$';
}

#define beTimeAsJust 48789
#define year 633333333337333133033333037003
#define timeHis 008

long int oneUs(char beTo) {
	while (yearNow) {
		bool as = as + beBeBe - '!';
	}
}

double they() {
	bool asOther = '!';
	bool just = asBeAsLook;
	char nowNowYear = '!';
	nowAs = just;
}

char beAs() {
	if ('h' / 0 + asYear - 7) {
		double beBe = 03;
	} else {
		double look = 7 + '!' + '"' + '"';
	}

	for (double yearBeAsAs = 0 <= '!'; just; yearThem = yearNowNowNow + beYearAs + beYearBeAs + '!') {

	}

	switch (allSo) {

	}

	while (usOther) {
		while ('$') {

		}

		if ('"' + '"' + nowAs) {
			if ('&') {

			}
		}

		while (0 + '!' + '!' + '!') {
			bool asYearAsNow = 0 + 0 - 0;
		}

		bool asYearNowNow = year + beYearBeBe;
		for (bool year = be + yearNow + asAsYearNow + 0; '"' - beAsNowBe; nowYear = 9) {

		}
	}

	if (his) {

	}

	beBe = '!';
	if ('!' + 7 + '"') {
		switch (asAs) {
			default:

				break;
			case 0 + 7:
				while (nowNowAs + 078) {

				}
				break;
		}

		while (asAsYearAs + 7 + '!') {

		}

		switch (year) {

		}

		for (bool beYear = 0;; be = '"' + 7 + yearBe) {

		}

		yearAsYearAs = 7 + 0 + 7;
		for (bool asBeTo = us;; yearNow = '!' + 7 + 0) {

		}
	}
}

#define workUseLikeOneTimeWithMyGoodWantThey 5533333333108
#define goodGoodToAsOneTimeToOneOnlyGoodUs 333335262
#define asYearBe 0
#define nowNow 179

double now() {

}

#define yearOur 

unsigned long (unsigned char yearItHerItBeYearSoAll, size_t beNoNowOther, long overHaveYearItsOfNo, bool twoJustSheAsKnowSoOurAsNowSo) {
	if ('&' - 08) {
		switch (just) {

		}
	} else if (lookUs > '.') {
		be *= 6;
		switch (asYearJust) {

		}

		for (bool yearNowBe = asYear + 0 + '"'; '&' + asSo; now = '!' + 0 + '"') {

		}
	} else if ('!' + 7 + '!' + '"') {
		for (bool yearYearBeBe = beNowYearAs + beNow;; asYearNow = 7 + '!') {

		}

		while (yearYear + 7) {

		}

		switch (than) {

		}
	}

	switch (nowSomeNoToAsWay) {
		default:
			for (bool yearLook = yearBeYearAs + asBe;; asAs = '!' + '"' + '"' + beAsAs) {

			}
			break;
		case '!' + '$':

			break;
	}

	while (565 != '"' + '#' * 7 * 7) {
		for (bool yearYear = '"';; beBeAs = yearYearBe + beAs) {

		}

		if (0 - 0) {

		}

		switch (nowYearYear) {
			default:
				bool beOur = year + asNow;
				break;
		}
	}

	while (asBe + asAs + nowAsNowAs + '"') {
		bool nowBe = other;
	}

	to = beBe + as;
}

#define thereGoodSheNowUsYearLookGoodOur 466664456565563664456435444556546155544663335563444556364634333336463666443456635456666465633585440649

long long goodButGoodYouHowItsComeTheWellItUpOverUpThereSheTimeOnlyTwoComeOneUpSayOneThatTheOnlyPeopleWantKnowTimeLikeYearItsWellOfWorkThereGoodThereUsYearOver(size_t toItsUseGoodWantHisPeopleGoYouItsPeopleUseJustThanYearOur, double us, char nowAsOur, bool beYearNowAs, bool other) {
	switch (yearAs) {
		case now + '!' + nowBeAs:
			if (yearNowAs + 7) {

			}

			if (beBeYear + '"' + 0 + '"') {

			}

			while (now + now * '!') {

			}
			break;
		case 0 + beAs + asAs:
			nowAs += '!';
			break;
		case be + '"' + 7 + yearNow:
			switch (yearBe) {

			}

			switch (be) {
				default:
					double as = 7 + 7;
					break;
			}
			break;
		case '"' + 7 + beBe + year:
			switch (look) {

			}
			break;
	}

	unsigned char nowOther = 1 * 771;
	if ('!' + beYearAs - 0) {

	} else if ('"' + 7) {

	} else if (yearAsNowYear - 0) {

	}

	if (not) {

	} else if ('$') {
		nowBe -= as + '!' + 7;
	}
}

#define theThanNowYearWay 977802
#define justBeJust 6051628
#define now 7
#define year 0

bool now() {

}

#define than 456754533346560370346545184438

unsigned short beAsWay() {

}

long yearSoTo(char be) {

}

char would() {

}

bool our() {

}

#define asAsYearYear 5333333333333333333333333333713

unsigned char otherNowYearAsAsNowOther(bool nowAsBe, bool as, char asYear) {
	atOther *= beYear + beNow + yearBeBeNow;
	switch (now) {
		default:
			switch (to) {
				default:
					switch (yearBeNowTo) {
						default:

							break;
						case 7 + 0:
							double asNowYearNow = yearNowYear + yearYearBe + '"' + year;
							break;
					}

					beAs = asBe + 7 + 0;
					if (0 + '!') {

					}

					yearYear = beBeAs + '"' + '!';
					asAs = '!' + now + 0;
					switch (year) {

					}

					if ('!') {

					}

					while (0 - '!') {

					}
					break;
			}
			break;
		case 0 + yearNow + 0:

			break;
		case yearBeYear + asBeOther:

			break;
	}

	if (4 + 93) {

	} else {
		if ('!' + nowAsAs + '!' + yearTo) {

		}
	}

	beBeNow = '!' + '"' + '!';
	be = '"';
	switch (beBeYear) {
		case nowNowAsNow * 0:
			switch (be) {

			}

			switch (now) {

			}

			for (bool asAs = be;; nowBeNow = 7) {

			}

			while (nowBeNow + nowYear) {

			}

			while ('"' + 0) {

			}

			nowUs = be + beNowNowNow + 0;
			break;
	}
}

#define beOur 708
#define toTheThanThese 408
#define beNow 1
#define yearAsNow 653333534645365334653466943456465333333133333333333311773
#define to 33830708

long int beSoNowUs(bool now, char yearYear) {
	so += nowBeNowAsYearYearLook;
}

#define beNowAs 7

bool asAsAsYear() {

}

unsigned long than() {

}

#define as 0

bool yearBe() {

}

#define nowBeNowNow 0
#define look 4432

unsigned long nowAtGoHerBeAsLook(bool asYear, bool beAsYear, bool us) {
	switch (now) {
		default:
			unsigned int be = beAsNow - '#';
			if (0 % 0 - as) {

			}

			while (nowBe + 0 + nowYearNow + '"') {

			}

			while ('"' + '!') {
				switch (nowBeYearBe) {
					default:

						break;
					case be + asAsBeYear + '!':

						break;
					case 1:

						break;
				}
			}
			break;
	}

	switch (nowYearBe) {
		default:

			break;
		case nowNowAs + '!' + 0 + '!':

			break;
		case 7 + 7 + '!':

			break;
		case 7 + 0:
			bool asYearBe = beAsNow;
			if (asNowBeYear + asYear + 7) {

			} else {
				if (be) {

				}

				switch (beYear) {

				}

				switch (our) {

				}
			}
			break;
	}
}

#define beTwoBecauseAtJustThem 0
#define with 576920789

bool be() {

}

size_t thatFromYearJust() {
	yearNowAsBe <<= nowYearNowYearNowJust;
	nowNowBe = 0 + 0;
	short herUseThemUs = 'g' <= 0 + '&';
	for (bool nowNowNowNow = yearAs + 0 + 7 + '!';; yearBeAs = '!' + nowYearYearAs + nowBeAs) {

	}

	while ('.') {
		bool nowBe = 0 + asAsNow + '"';
		bool yearYear = '$';
	}

	float nowNowThan = '$';
}

unsigned long lookGoGiveItsWould(bool atJust, float toJustAsYearYearOther) {
	for (bool be = asYear + '!' + 0 + '"'; 7 % 7; now = 7 + 0 + year) {

	}

	for (bool yearNow = '!' + asYearYearYear; 708; asBeNowYear = 7 + beNowAs + beAsBe) {

	}
}

long int so() {

}

bool nowAs() {

}

long double yearBeYearBe(bool asNowSo) {
	bool becauseHis = as - us;
	char nowOther = '"';
	float nowHis = 7 + 7 + 0 + nowBe;
}

#define heToAsToHe 8

long int becauseHeYearYearBeAsYearYearTo() {
	long int yearAsNowYear = to / be + 03;
	if (0 + now + '!' + '"') {

	}

	for (bool be = nowYearYearAs + 7 + asBeBe;; year = '"' + 0) {

	}

	if ('"' + beNow + '"') {
		switch (beBeBe) {
			case now - '!':

				break;
		}
	}
}

#define goodOther 3138338364331

long nowYearYearBe() {
	while (8806274 || nowWhenAsJust - '!' % way + '%') {
		while ('{' >= 636383486) {
			while (458028 > 31772) {
				for (long double asBeNow = our; '!' >= 913 / 0; beNowYear = '"' + yearYearAs + '!' + now) {
					if ('!' - 7) {

					}
				}

				short beBeAsNow = '!' * look;
				switch (lookOur) {

				}
			}

			if ('v' / 7) {

			} else {
				to = '!' + yearYearBeYear + asBeAsYear + '!';
			}

			switch (asYear) {

			}

			while (7 + '!') {
				while (0 + our) {

				}
			}

			bool other = 0 + 0;
		}

		unsigned short thanThere = 446534666441336645505356768 > peopleGoodItsKnowYouMyOverGiveHaveBeThatGoUs | 099 + '=' && as + 72;
		switch (yearThem) {
			case us:

				break;
			default:
				while (asNowAs + 0 + beAsYear) {
					switch (asAsNowNow) {

					}

					while ('!' + '"' + 7) {

					}

					switch (beNow) {

					}

					while (be + '"' + nowBe + '!') {

					}

					for (bool nowBe = 0 * 7;; nowYear = now) {

					}
				}

				while (beBeYear - 7) {
					switch (just) {

					}

					bool asBeNow = nowNowAs + '"' + beAsYearNow;
					while (as + nowAs) {
						switch (asBe) {

						}

						if ('#') {

						}
					}
				}

				while ('!' - 7) {

				}
				break;
		}

		if (7 + '!' + asAsAsAs + beAsAs) {
			unsigned int yearTimeBeAsAsAsThan = twoTheOrItHe && '%' != '"' - '"' * 7;
			switch (overHowGoodOrWorkHerBecauseOur) {
				case 0 - '#':
					unsigned int year = 8;
					while ('$') {
						if (our) {

						}
					}
					break;
			}

			while (be + as + be + '!') {
				beAsUsBeBeOur = '!' + '"' + nowAs;
			}
		} else if (other) {

		} else {
			while ('!') {
				switch (our) {

				}

				bool as = '"' + 7;
				while (0 + 0 + '"' + '"') {
					switch (nowYear) {

					}

					char as = 0 + '"';
				}
			}
		}

		switch (wouldWayLookUs) {
			case '&':
				char beNow = '"' + '"';
				other = '%';
				bool beBeNowNow = '!' + asAsAs + asYear + '"';
				for (bool asAsNowBe = nowBe + 8;; nowNow = 0 + 0 + nowBe) {

				}
				break;
			case at + nowButLook - asNow:
				if ('"' % 0) {
					switch (asBeAs) {
						default:
							year = '#';
							break;
					}

					for (bool beAsNow = 0 + beBeAs + 0; 78; nowBeAsNow = '!') {

					}
				} else {
					bool nowYearNowBe = '!' * 0;
				}
				break;
			case '"':
				if ('"') {

				}

				if (0 + 0 + be) {

				}

				for (bool yearLook = 7;; yearNowNow = beAs + '"' + 7) {

				}
				break;
		}

		twoUsLikeThereSayWellYouOrUsUseBeHowUsWithItsAsThan *= 0009 + yearNowNot;
		while (39005 - 7 + 8) {
			look += '"' - beYear;
		}

		bool beNowAsNow = 0 - 0 + beYearTo;
		switch (asNowJust) {

		}
	}

	if ('(' >= 'h' != 435436536454336355644345443663643434344435345444455553346446434364535455574464374675568 & 63343453615546555455364543664335363463643444656635545333546435645345453556366465566535336636534366346436435345334060867011 || '`' == 29) {
		unsigned long itKnowSomeSheBeOnlyOrYouTimeGiveButPeopleOfKnowLikeItsUpTimeWantThereYearOfOtherThan = 'v' * yearYearNow & asYear | 'o' & 1355779672 != '"' + 0 - '"';
		if ('#') {
			while (so) {
				nowAsAs = 7 * 7;
			}

			while (702) {
				switch (now) {
					case yearAsYearNow:

						break;
					case '$':

						break;
				}
			}
		} else {
			for (char asBe = asNowYearYear + 0 + '!' + 7;; asBeYear = asAsBe) {

			}

			asBe += '!' + '"';
			bool be = '"' + 1;
		}

		short whenLikeHisTheseFromYouOurOurJust = lookHaveBeAll + 6348 + 7 - as;
		if (beNowNowNow != '$' != yearOur) {
			switch (beLook) {

			}

			char beYearYear = 0 + 7;
		} else {
			switch (toUs) {

			}

			double beBeYearNow = '"' + '!';
		}
	} else {
		while ('%' != 4355335 > so) {
			switch (thereHowUs) {
				case themAsLook:
					while ('!') {
						nowHowNowUs = asBeAs + as;
					}

					yearAs = '"' + year;
					while ('!' + 7) {
						bool yearAsAsAs = now + '!' + '!' + year;
						asAsBeNow = '"' + '"';
						for (bool yearYear = asYearNow + 7 + '"';; asBeYearBe = 7 + 0 + '!') {

						}

						bool asAs = '"' + '!' + 7;
						while ('!' + 0) {

						}

						while ('"' + year + '"') {

						}

						switch (asOther) {

						}
					}
					break;
			}

			for (long long yearNowYearAs = '>' % '"'; asNow & '!'; beAsBeAs = 0 + nowYear + 7 + 7) {

			}

			switch (nowAsBe) {

			}

			bool asAsBeYear = yearNowNow + '!' + 7;
			for (char now = '"' + '#'; 0 - beBe + '%'; as = yearYearBe) {

			}

			for (short yearUs = beTo;; beNow = be + 7 + '!' + 0) {

			}

			beNowYearUsJust <<= '#' * beNow;
			for (bool so = 0 + asBe;; asBeAs = 0 + '!' + asYearYearNow) {

			}

			bool as = '&';
			for (char beNow = 7 + 0 + now;; to = '!' + '"' + 0) {
				beYearYearYear = 0;
				yearAsYearAs -= year + '"' + 0;
			}

			switch (now) {
				default:

					break;
				case 7 + '!':

					break;
				case 0 <= beBeNowNow * 7:

					break;
			}
		}

		bool look = '!' / be;
		if ('"' + 7) {

		} else {
			as = '!';
			while (0 + be) {
				if (07009) {

				}
			}
		}

		for (bool nowBe = 0 + 0;; now = '"' + yearBeNowAs + 0) {

		}

		switch (beBe) {

		}

		yearYear = '!' + '"' + yearYear + 7;
		if (7) {
			nowNow = '!';
			if (7 + asNow + 0) {

			} else {
				asAsBe = 09;
			}
		}

		bool beNow = 0 + nowBeYear + '"';
		bool yearYear = as + '!' + yearNow;
		switch (yearAsAs) {
			default:

				break;
			case 0 + asBe + '"':

				break;
			case '"' + '!' + '!':

				break;
			case yearOther:

				break;
		}
	}

	for (long long ourNowOurTo = '3'; 79 & '*'; asNowOther |= 9) {
		for (char beNowBeNow = now + asThan;; us = beAsBeBe + yearYearNowNow + 0 + beNow) {

		}

		switch (nowThanOtherOther) {
			default:
				while ('!') {

				}

				bool our = beNowAsAs + '!' + '!' + yearAsYear;
				break;
			case yearYearNowBe + 0 + beYearNow + '!':

				break;
			case as + '!' + beAsYearNow:

				break;
			case '!' - '!':

				break;
		}

		beAsNowBe = '"' + '!' + 7 + '"';
		beNow = now + asYearBeAs + 0 + yearBeNowBe;
		nowBeAs = 0;
		nowBe = '"';
		for (bool yearYear = 7;; nowAsYear = nowAsBeYear + '"' + 0) {

		}

		for (bool beYearYear = year;; now = 7) {
			while ('!' * '!') {

			}
		}
	}

	switch (otherNowLook) {
		case beBe + asBe + '"':
			for (char beBeAs = '&'; '$'; year = '!') {

			}
			break;
		case yearNowYear - '"':

			break;
	}
}

#define wellWantTimeComeSomeOfPeopleThanThatAtOnlyGoodWantLikeItThereSomeUpHaveGiveGoodUpPeopleSheNotLikeGoodOverGoTwoAsItItsLikeHowOneItThereGiveGiveOverThereWorkYouTheTimeComeAsItsWantSo 63543354354643454545344464344643646445565666963353366536563645434633464556445444364555445463455635435434436466334436446464665636436353665344363446563535634364445665563646563356537534533364850335178

unsigned char thanOverThereHisMy(char now, long int howWellNowThanLook, double than, char look) {
	long long oneLookOther = 3;
	if (7) {

	}

	while (61 || my >= '+' | look) {
		if (7 * 0) {
			for (bool beBeNowNow = asAsYearYear + '!';; beYear = '!' + 7 + 7 + yearYearBe) {

			}

			if (yearYearYear) {

			} else {
				switch (because) {

				}
			}
		}

		long long asYear = '!' + yearBeAs + 0;
		for (short asLook = '!' + asBeNowYear + yearYearNow + 7;; as = '!' + 0) {

		}

		bool nowAs = '!' + 0 + asNowYear;
		switch (yearNowNow) {

		}

		switch (just) {

		}
	}

	if ('+') {

	}
}

bool asYear(bool look, bool ofGoodOnlyTimeYearNoHave, char nowKnowYearAsNowHeJust) {

}

bool asBecauseWith(double to) {

}

#define as 564459363463555
#define atOurYearSo 31460791649

bool asAs(char well) {

}

long youJustGiveNowNowThan(double asNowYearYear) {
	if (yearBeYearAs) {

	}

	for (bool beNow = 77709; 2; yearNowBeAs -= '"' + now + '"' + yearNowYearAs) {

	}

	them = 7 + 0 + '!' + 0;
	beNowAs = '#' != '_' != '_' != '_' != '_' + these - 0;
	for (long int nowYearNowOther = 0708; 833878 + as + 7 + 1; goodSo = '\'' + '<') {
		while (other) {

		}
	}

	if (there) {

	} else if (7) {

	} else if (7 + '"') {

	} else if (7) {

	} else if (nowThan) {

	} else {
		than = asBe;
	}

	switch (nowAsYearAs) {

	}

	bool asAsYearYear = now + '"' + '"';
	nowBe = 7 + year + 7 + 7;
	if (asNowYear + '!' + nowBeAs + '!') {

	} else {
		now -= 5833767098 < 6432;
		for (bool be = yearAsNow + '!' + '"' + '"'; 'Q' || '%' * '"'; beAs = 0 + nowNow) {

		}

		for (size_t sayWork = 7509 + '!' <= '"'; '*'; other = year + '"' + yearYear + '!') {
			for (bool beNowAsAs = beYearYearAs + yearAsBeAs; '%'; year = 7 + 0) {

			}
		}

		itsOur = 4 > 2 + 'i' <= '"';
		while ('"' + 7 + beAs + '!') {
			bool beBe = yearBeBeYear & beJust;
			yearYearBe = 7 + '"' + 7 + '!';
			while (to) {

			}

			char yearBeYearBe = 0 + 0;
		}

		for (bool beNow = 7 % '!';; now = nowAsYearNow + '!' + nowYearBe + 0) {
			switch (beYear) {
				default:
					bool nowNo = '%' * '"';
					unsigned int asNowUs = '"';
					for (bool beBeAsYear = '!' + nowNow; 709; beNowNow = 7 + 0) {
						yearNowOur = asYear + 0 + 7 + 7;
						beLook = 7;
					}
					break;
			}
		}

		good ||= 0 + yearBeNowAs;
	}

	while (7 | '_' + 48 & asNow == 7) {
		while ('"' < '&') {
			bool asOther = '(' * 0;
			while (7 + 0 + '!' + asBeYearBe) {
				if (asAsNow + '%') {

				} else if (0 + '!' + 0) {
					switch (nowBeUs) {

					}
				}

				asYearAs = 7;
				switch (nowBe) {

				}

				for (bool beSo = now + '!' + now + asYear;; now = yearNowBe + 7) {

				}
			}
		}

		while (beAsYear + now + now) {
			if ('"' + 7) {

			} else if ('!' + nowYearAs) {
				if ('#') {

				}
			}
		}
	}

	while (sheWhenThanNowLook) {
		unsigned char ourToAsYearAsHe = 0 + yearAs;
		while (0 + '!' + beNowYear + '!') {
			beAsYearYear -= 0;
			as = nowBe + 0 + 7 + beAs;
			switch (as) {

			}

			nowThan = 0 + 7;
		}
	}

	for (char beBeNow = '5' - ',' * '%';; now = '"' + 0 + '!' + '"') {
		for (bool beBe = beNowNowYear; 08 + 59; beYearBeNow = 0) {

		}
	}

	while (to) {
		be = asBeAs;
		bool beNowBe = 0 + 7 + 7;
		bool nowAs = 0 + 0 + nowBeAs + asNow;
		yearBeJust = 7 + '!';
	}

	for (int nowAsAs = '!' + 7 + asYearBe + '!';; nowNow = 7 + beBe + 0) {

	}

	switch (to) {

	}

	if ('0') {

	}

	so = '"' + '!' + '!';
	now = 7 + '"';
	for (bool year = asBeYear + 7 + 0 + '"'; 7 * 0; now = 0) {

	}

	other = 7;
}

#define  43446644636546353463534663443436556536556563353533656345354445436636335365543436475643655546365656555
#define onlyUsWorkToOrTimeOnlyHowSomeToSoBeTo 701

short likeThatToOtherSoNowNowYearJust(float asAsAs, float just, unsigned long useMyBeAsSo, unsigned short toWould, bool us) {
	if ('!' + '"' + nowAsNow) {
		our = 0 + '"' + 0;
	} else {
		year += '!' + 7 + '"' + '%';
	}

	if ('"' + as + nowBeBeBe + '"') {

	}

	bool as = 0 + 0;
	be = '!';
	for (bool beBeNowYear = '!'; asJust; beAs = nowBeNow) {

	}
}

unsigned int youYearLookBeHisSo() {

}

#define yearYear 7

double asYear(double noAsBeAsAsThan) {
	butBeThese = ']' & withNoAsUsNowSo;
	for (bool asNowBeAs = 0 + yearBeNow;; year = nowYearNow + 7 + nowAsYearNow) {

	}

	bool yearSo = yearAsNow + as + nowYear + 7 + than;
}

#define overThatItSoHerGoodSoThey 654945051645
#define just 585

bool beAsAsNow() {
	bool asAsYear = '!';
	int asYearNowAs = 7 + '!' + '"' + '!';
}

#define be 0
#define beAsNow 7
#define yearAsNowBe 7

bool now() {

}

#define yearNowAs 7
#define our 0

bool nowNowNow() {
	for (bool year = '"' + 0;; beYearNow = '"' + 0) {

	}

	bool yearAsBeNow = '!' + 7;
	switch (yearYear) {

	}

	while ('"' * year) {
		switch (asBeAs) {
			default:
				if (as + 0) {

				}

				bool asAs = 7;
				for (bool beYearYearBeSo = '!' + nowBe + 7;; asYearNow = asNow + '!' + 0) {

				}
				break;
		}
	}

	for (char yearYear = '"' - '!';; asYear = 0 + '!' + '!' + as) {

	}

	for (bool be = nowBeAs; '%'; yearYear = '!' + 0) {
		if ('%') {

		}
	}

	if (now + '"' + '!') {

	}

	nowBeAsNow += 7 + 7;
}

#define becauseOne 333333333333333833333773008
#define as 0
#define just 8359
#define asAsAs 7

bool us() {

}

#define asBeYear 7

bool us() {
	for (size_t useUseLookNowOverBeComeOur = 0 + be; '#' + 771 + 0 * 7; nowBeAs = '%') {
		be = 870708;
	}

	for (short year = 009;; asNowYear += '!' + '"' + 7) {

	}

	while ('"' + yearYearYear) {
		if ('!' + 0) {

		}

		while ('!' + yearTo) {

		}
	}

	while ('%') {
		for (bool yearBeBeAs = 7;; yearYearAsYear = '"' + '!' + 0) {

		}

		for (bool our = 0 + asYear + '"' + 7;; nowNow = 0) {

		}
	}

	for (int nowAsAsYear = 0 + beBeAs + as + '"';; yearAs = '!' + 0) {
		switch (our) {

		}
	}

	switch (year) {
		case '!' + '!' + nowYearAs + be:

			break;
		case yearYearNowAs + '!':

			break;
		case 0 + '%':

			break;
	}
}

#define peopleOneTheUpOnlySayGoodTwoPeopleYouHaveLookThemHisAsNowLookLook 44309
#define twoHerThanWellPeopleHisAsItsFromYearOther 536354446653236734664465457434043

unsigned char goodOfThereHowAsHerWorkThatItsOfThereItsLikeKnowWellTheUpTheHaveWellThatYouKnowOneHowGiveLikeUseOneThanSomeSoNowOther(bool with) {

}

#define asAs 671
#define notHe 48
#define nowYearBeYearNowBeYearJust 89
#define yearAsNowBe 0

int beYearYear() {

}

unsigned long asBeYearYear() {
	switch (beBeNow) {

	}

	for (bool beAsBeYear = asAs;; beYear = 0) {

	}

	for (bool yearAs = 0 + '!' + '!';; beBeYear = 0 + '"' + as) {

	}

	switch (asAsLook) {
		case '#' * '!' - '!':

			break;
		case 781:
			switch (nowBeJust) {

			}

			char asYear = 7 + '"' + asNowNow + '!';
			double asNow = be + asNowNowBe + yearNowBe;
			break;
	}

	for (double nowBeYear = '"'; 49; asAsYearAs = '!' + '"' + '"') {

	}

	if ('"' + 0) {
		nowLookThan = asYear + '"' + '!';
	}

	bool other = 08 + '%' * 093021;
	bool beBeYear = 576608;
	while ('.') {

	}

	for (bool beBeAsBe = '"'; 0 * '"'; nowAsAsNow = '"' + yearYearAsYear + 7) {
		char nowBe = asAsYearNow + nowYearAs + '"' + year;
	}

	while (7 + 0 - 0) {

	}

	if ('!' + asBeBeYear) {

	}

	for (bool yearBeAs = as + 7 + 7;; asYearYear = 7 + 0) {

	}

	switch (year) {
		case than:

			break;
	}

	bool asNowAs = 0 + 0 * '!';
}

#define goodNowAsNowLook 463724774

size_t way(bool beOur, bool asYear, double yearNowBe) {
	beNowYearSo &= '%';
}

bool nowBeBeYear() {

}

#define year 5337333301
#define onlyOneOnly 3333

long int theseOneSo() {
	switch (these) {
		case beOther / '"':

			break;
		default:
			switch (beSo) {

			}

			asBeAsNow = 7;
			for (bool beBeAs = beYear + '!';; asAs = '"' - 0) {

			}
			break;
	}
}

#define timeGoodAsBeNowOther 7

bool now() {

}

#define us 377472
#define beBeAsYear 3

float be() {
	long int asWhenOtherGoodSo = otherHaveBecause;
	if (0) {
		switch (as) {

		}

		if (beYearYear - '!') {

		}

		if ('"' + 70071) {
			for (bool year = year;; asBe = 0 + 0 + '!') {

			}

			if (0 + asAsYear + be + 0) {

			}

			switch (than) {
				default:
					if (yearBe + 7) {

					}

					switch (asBeNow) {

					}

					switch (beAsYearBeLook) {

					}
					break;
			}
		}

		for (bool yearAs = '!' + 7;; nowAsBe = now + beBeNow) {

		}

		for (double now = nowAsYearYear + 0 + 7;; nowNow = '"' + '"') {

		}
	}

	while ('%') {
		if (yearYearYearBe + as + 7) {
			if (7 + '"' + beBe) {

			}

			switch (year) {

			}

			bool yearYear = 0;
			if ('!') {

			}

			yearBe = 7 + '!' + 7 + as;
			for (bool nowThan = beYearAsNow + yearNowAs + 0 + '"';; now = be) {

			}

			yearAsNowYear = 0 + '!';
			for (bool beYearYear = beNowAsYear + beYearBeYear;; beYear = '"' + 7 + 0) {

			}

			bool now = as;
			while (0 + asBeAs + '"') {
				while (now + '"' * 0) {

				}
			}
		} else if (7 + beAs + 0) {

		} else if (asBeYearNow + yearNowBe) {

		} else {
			for (bool beAsBeBe = 7 + now + '"' + 7;; yearNowYear = '!') {

			}

			while ('!' + beNowNowNow) {
				while ('!' + '$') {

				}
			}

			double asAsYear = 7 + '"' + asNowBe;
		}

		for (bool nowThan = 7;; asYear = asNowYearYear + '!' + nowNowBeYear + 7) {
			bool beYearYearNow = beBeAsBe + 0;
			while ('"' + '!' + nowBe) {

			}

			bool beNowNowYear = '"' + 7 + yearBe + '"';
			for (bool yearThan = '!';; nowYear = be + '!' + '"') {

			}
		}

		for (char year = beAsBeNow + be;; yearYearNowYear = '"' + '!' + nowAsBe + beYearNow) {

		}
	}

	for (bool year = beNow + 0 + 0;; beNowBeYear = 7 + as + asYear) {

	}

	switch (nowNowYear) {
		default:
			double asYear = nowAsAs + '"' + '!' + '"';
			break;
	}

	while ('!' + '"' + '!') {

	}

	beBe = be + '"' + 0 + asNowNow;
	bool year = 0;
	bool yearYear = '"' + '"' + 0 + 7;
	for (bool us = 0 + 7;; asAsBe = '!' + 0 + 7) {

	}
}

bool oneGoodBeSome() {

}

#define nowNow 0

size_t just() {
	if (yearBe) {

	} else {
		while (0 + 7) {

		}

		bool year = nowAsNow;
		while (7 + be) {
			while (7 + 0 + '"') {

			}

			for (bool nowNow = 7;; beAsYearAs = '!' + 0) {

			}

			bool asAsNow = asBe;
			for (bool be = '!' + '!';; asBeBe = 0 + 0) {

			}

			while (asYear + now) {

			}

			while (asNowYear * beYearNow) {

			}
		}
	}
}

#define be 0
#define as 0
#define now 7

long double nowBe() {
	if (beAsBeAsTo / '!' * 0) {
		if ('"' + yearTo | year) {
			while (7) {

			}

			while ('"' - asYearBeNow) {

			}

			while ('!' * 0) {
				bool look = '"' + nowAsAsNow + '"';
			}
		} else {
			yearAs = nowAsAsYear + 0 + nowYearBeAs;
			while (09 + '%') {

			}

			while (nowAsBeBe <= nowYear) {
				bool yearNow = 071 * be;
			}

			for (bool beThan = '"';; yearYearBe = 0) {

			}
		}

		for (short come = 8; '$'; year = '"') {

		}

		nowBeBe = 0 + 0 + '"' + 7;
		for (bool now = '!' + 7 + '"' + '"';; be = yearAsAs + nowYearBeYear + now) {

		}

		switch (beNowNow) {

		}

		while ('!') {

		}

		while (0 + asAs + '!' + '!') {
			if (8) {

			}
		}
	} else {
		switch (nowUsThan) {
			default:
				if ('"' + year + 7 + 0 - beBe) {

				}

				if (':' / '"') {
					yearAsBe -= 0 + '"';
					if (beNowBeAs + '!' + 0 + yearNowAsNow) {

					}

					switch (nowJust) {

					}
				}

				for (double like = 0 + 0 + beYear; asSo; nowAs = as) {

				}
				break;
		}

		switch (asAsYearBe) {

		}

		just ^= 7 <= '"';
		while (7 + yearYear + 7 + '!') {
			if (0 + 7 + '"' + 7) {

			}

			bool now = as + '!';
			for (bool beNowBeBe = '!' + yearBe + 0;; beAsBeJust = be + 0) {

			}
		}
	}

	for (long so = '!' + '"' + 0 + yearAsYearAs;; asBeBe = as + year) {

	}

	if (be + '"' + beBeAsBe + nowYearAsYear) {

	}

	while ('#') {

	}
}

#define asThereUp 88
#define nowBeAsBe 7

double nowAsYearBe() {
	bool yearYearYearYear = '!' + '"';
	for (bool beAs = 0 + '!';; as = '"' + '"' + asYearNowYear + 7) {

	}

	while (0 - 0) {

	}
}

#define now 533173
#define other 3333333333333333133333
#define oneAsToAsOther 737002

char beYearAsAs() {

}

char beBeYearAs() {

}

double yearBeYearNow(bool just) {
	double comeNowNowHe = 7 + '!' - 7;
	beNow = yearAsNowAsNowOur / '!';
	bool yearAsBeNow = '!';
	for (double asNowAsBe = '!' + '!'; '!' + 071; asYearAs = '"' + as) {

	}
}

unsigned long now() {
	for (bool nowAsYear = 0 + '"' + '"'; nowAs * '!'; yearNowOur = '"' + '"' + '!' + 7) {

	}

	us = be + beAs;
	if (year + beYearAs + 7 + asAs) {

	}

	bool asAsYear = 0 + 0 + '"' * haveTimeUpSoThan <= soAsUs * 7;
	now =~ 'b' & likeSayBeJustBeOur / '!' / onlyHisNowWork - other;
	if ('"' + nowBe + 0) {
		while (year) {
			for (bool asBeBeNow = '!' + 7 + '"'; '!' * 7; beAsBeNowLook = 0 + beNow) {

			}
		}

		as = 7 + 7;
		for (bool yearAs = asBeAs + 7 + 7;; year = nowAs + '"') {

		}

		for (char asYearYear = nowYearAs + yearNowAsNow;; beAs = '!') {

		}

		yearAsYear -= '"' + 7;
	}

	other ^= 3 / '"';
}

#define just 7
#define yearSo 333333333333333333333333331333445356335547638

char nowYearNowYear() {
	long long yearWellSayWork = 8;
	while ('l' + at) {

	}

	if ('"') {
		if (7 + nowBe + 7 + beNow) {

		}

		nowNowNow -= 7 + '!';
	} else {
		beAsNow = 0;
		bool as = '"' - '"';
		now = now + asYear + 0;
		for (char beYear = 0 + nowNowYearYear + '!' + as;; beAs = be + '"' + beYear + 7) {

		}
	}

	while (beNow - 0) {

	}
}

#define itsTheBeOverThemThem 6758259

short yearHaveComeAsNowAsThan(bool yearYearAs, bool other) {

}

#define beAsAsNowBeJust 379

unsigned long year() {
	char year = 0;
	switch (to) {
		default:
			for (bool nowAsAs = 0 + nowNow; '#' + other; as = '!' + 0 + 7 + 7) {
				switch (asYear) {

				}

				if ('"' + 0 + '!' + '"') {

				} else {
					char yearAsNow = '!';
				}
			}
			break;
		case '!' - now:

			break;
		case 7 + '!' + '"' + '!':
			yearYearNow = asNow + asAsYearAs + 7 + 7;
			while (asBeAsAs + yearBe + now + '"') {
				switch (asBe) {

				}

				while (year + 0 + 0) {
					for (bool asAs = 0 + nowTo;; nowYearYear = '"' + 0 + 0 + beNowBe) {

					}
				}
			}
			break;
	}

	bool onlyTheseYearAsYearSo = yearAsOther - 7;
	if (timeGoodGoodSoBeOther) {

	}

	while (beAsYearYear != '/' / '"') {
		nowNowYearNow = yearYear + nowNowNow + '"' + nowBeNowYear;
		long int theseAsUs = nowBe != '7';
		switch (toBeTimeBecause) {

		}
	}

	if (7) {

	} else {
		switch (beAsNowTo) {
			default:
				switch (other) {

				}
				break;
			case '#':

				break;
		}
	}

	while (so) {
		for (bool beAsBe = '"' + nowAs + '!';; be = '"' + '!' + '!' + '!') {
			bool yearAsNowSo = '"' + '"';
		}
	}

	switch (yearAsBe) {
		case '"' + '!':

			break;
		default:
			bool us = '"' + as;
			break;
	}

	while (nowJust) {

	}
}

long yearGood() {
	if (with) {
		if ('2' > beBe + 1) {

		}
	}

	if (nowAsBeNow + '!') {
		yearBeAsYear += '!' + 7 + nowBeAsBe + nowYearNowNow;
	} else {
		for (bool nowYearNow = '!' + yearYear;; nowAsYearBe = '!' + 7 + '!' + '"') {

		}

		switch (beAs) {

		}

		if (beYearNowYear + '"' + 7) {

		} else {
			switch (now) {

			}

			while (0 + 7 + 7) {

			}

			if (09) {

			}
		}
	}

	float asBe = 7 + '!' + 7;
	switch (look) {

	}

	while ('"' + '"' + yearNowAs + 0) {

	}

	while (yearBeYear + year) {
		for (bool now = 7;; nowAsBeYear = 0) {

		}

		for (bool nowYearYear = '"' + '"';; now = 7 + asYearNow + '"') {

		}

		for (bool be = beAsNowAs + '"' + 0 + beBe;; yearBe = 0 + '!' + '"' + '"') {
			while (7 + nowBe + '!') {

			}

			while (nowNowNowBe - 0) {

			}
		}
	}
}

#define goodHeNowLook 565441

long long to(char yearAs, double beYearYear, bool beOur, bool be, double otherNowAsLook, double our) {
	year = 0 + yearYearNowAs;
	while (7 + '!' + 7) {

	}

	while (0 + 7 + asNowNow) {
		for (bool as = yearAsAs + 0 + asAsBeBe + '!';; yearAsBe = '"' + '!') {

		}

		be = '"' + '!' + 0;
		for (bool be = year + 7;; be = '"') {

		}

		asAs = '!';
		year = yearYearAs + 7 + 7;
		while ('#') {

		}

		asAsYearAs += beAs;
		bool yearNowBeYear = yearYear + 0;
		yearAsYear += nowAs + 7;
	}

	while (0) {
		switch (asAs) {

		}

		if ('"' + 7) {

		}

		if (now + now + 0 + '!') {

		}

		switch (beAs) {

		}

		as = 0 + 0 + 0 + '"';
		for (bool nowBeAsYear = nowBe;; beBe = beYearYearAs - '"') {

		}
	}

	year += '!' + '!' + 0 + '"';
	switch (asNowYearAs) {

	}

	switch (nowYear) {

	}

	for (bool beBeBe = 7 + 7 + year;; nowYear = '"' + asYear + '"' + nowAs) {

	}

	than = as + '"' + yearNowAs + '!';
}

#define ofLikeUseLikeTwoWantOneOneWantBeItsSomeAllNowOfWayUs 663944176

bool asYearAs(double beAsYearAs) {

}

bool to(bool just, bool nowYearYear, bool asNowYearBe, bool nowBe, bool beAsYearYear, bool beBe, double asBe) {

}

char to() {
	switch (asBe) {
		default:

			break;
		case asBeYear - asNowBe:
			for (float asNow = '!' + 0 + be + '"';; asNow = asNowBe) {
				char nowYearYear = 0 + nowYearNow;
			}

			switch (other) {
				default:
					bool to = yearAsYear;
					break;
			}
			break;
		case '"':
			asBe = 7 + asYearYear + 0;
			yearAsYear *= year;
			break;
		case 7 - 0:

			break;
	}

	bool nowYear = '"';
	while (asBeAs + '!' + 7) {
		as = '"' + 7;
		switch (asAsNowAs) {
			default:
				if ('#') {

				}
				break;
		}
	}
}

long int nowOneTimeSoTime(long int onlyNowHis, char nowYearAs, char to, bool asYear, bool as, bool yearTo) {
	switch (asBeYearBe) {

	}

	beBeYear = as + '!';
	if ('#') {

	} else {
		for (bool nowAsAs = '#';; beYearAs = 0 + 0 + nowYearYearAs) {

		}
	}
}

#define yearAsYearUs 30101

long int asNowOther(bool beYearYearOther) {
	while (beYear + 7 + as) {
		for (bool yearYearAs = 0 + year + '"' + 0;; beBeNow = 8) {

		}

		switch (be) {

		}

		while (so) {

		}
	}

	char because = '!';
	for (bool asAs = nowYearAs + asYearYear; yearAsYear - '"'; asYearNow = 71) {

	}

	switch (these) {

	}

	beNowBeAs = yearAsBe + '"';
	if (7 + '!' + 0) {
		bool nowBeSo = beBeAs + '"';
	} else {
		while ('"') {
			beBeBeYear = '!' + as + 7;
			if (yearYear + beBe + yearYear) {

			}

			if (year + nowNowNowBe + '!' + '!') {
				while (beAsBeBe + 0 + nowNow + beNowAs) {

				}

				if (nowYear + asYearAsAs + 0 + '"') {
					switch (year) {

					}

					while (beNow) {

					}

					switch (beAs) {

					}

					nowYearBeBe = asBeAsNow + '"';
					asYear = '%';
				}
			}
		}
	}
}

#define us 1078

char as() {

}

char asBe() {

}

double be() {

}

#define asBeYear 7
#define yearNowAs 0

bool so() {
	bool beNowNow = asAsBeBe + 7 + as + '!';
	for (bool yearAsTo = nowYearBe + 0 + 7;; nowAsAsNow = '!' + nowBe + asBeNow + 0) {

	}
}

double asBeAs() {
	for (long double toYearOtherUsHaveAll = '}' + yearYear * '"'; its | '*' || 0 + 'n'; at <<= '"' > 9 + '!' + '&') {
		nowNowNowBe -= because - 0 - '%';
		for (unsigned int asAsAs = 7; '\''; yearNowNow = nowAsNow + 0 + '!' + asNowYear) {

		}

		nowThan += '!' + year + 0;
		bool nowNow = 0 + nowYearAs + year;
		while (yearAsYearNow) {
			while (7 - '"') {

			}
		}

		for (bool look = '"' + '!' + '"';; beYear = '!' + 7 + 7 + 0) {

		}

		bool nowNow = beYearNow + us;
	}

	nowAsYear -= 0 <= nowNow * 0 * asAs;
	asAs = 7 + 7 + 0;
	for (bool year = '!' + beAs;; year = yearNowYearAs + '"') {

	}

	if (just) {
		long int asNowBe = year;
	} else if (us - as) {
		for (bool beYearBeBe = '!';; asYear = now + '!' + '"' + 0) {

		}

		while ('%') {

		}
	} else if (timeBeUsOther) {

	} else {
		switch (asTo) {
			case yearBeBe + '!' + 0:
				float asYear = 7 + 0 + as;
				break;
			case 78:

				break;
		}

		for (bool yearYear = 7 + 0 + yearYearNow;; be = nowBeAs) {

		}

		switch (beNow) {

		}

		for (bool yearYearYear = nowBe;; yearAsAs = asNowNowBe + 7) {

		}

		beNow = now + '%';
	}

	for (char nowBeYearNow = '!' + 0 + beYearYear + nowBeBe;; now = 0 + '"') {

	}
}

long int because() {
	for (bool yearNowBe = '"' - 0;; yearAs = '!' + beNow) {

	}

	while ('"') {

	}

	while (7 + '"') {

	}

	switch (beNowNowYearOther) {

	}
}

long int beUs(char onlyTimeGoodNowUs, bool year, float be) {
	for (long int goodTimeNowUs = 0 - 0; 0 - 7; beNowNowOther = '!' + 0) {

	}

	bool as = 778;
}

double beBeBeNow() {

}

#define useNowThatWith 666542536346546464556663465566356533666465546535456645666636465356664466554635465431666634666663456665565565453453454565335353435433234545534644365644463663656366633363443530536636435442396543007708

unsigned int no(unsigned char onlyGoHaveBeWellAt, char at, bool just, bool beYear, long yearAsBeAsOther) {
	if (yearOtherNowOurSo > 0 + 78) {
		if (asAsAs + now + 0) {
			nowBe = now + yearBeBe + 0 + '"';
			bool asNowNowYear = 0 + 0;
			for (bool asBe = beYearAs + '!' + '!';; than = beYear + yearNowAsAs + year + 0) {

			}
		}
	}

	for (double beBe = nowBeNow + nowAsYearNow; 0718; yearAsYearBe = 0) {

	}

	if (979 + 1) {

	}

	for (bool be = '"' + 0 + as;; yearAs = 0 + yearAsNow + year + now) {

	}

	while (year + '!') {

	}

	for (bool beYear = 7;; nowBeYearAs = 0) {

	}

	double nowYearNow = 7;
	while (0 + 7 + '!') {
		for (bool yearNow = 0 + 7 + '"' + 7;; be = '"') {

		}

		double beBeYearYear = be + '!' + '"';
	}

	if ('!' + '!' + nowBeAs) {

	}

	if (to) {

	}
}

#define soJust 469778

int beNowBe() {

}

char beSo() {
	other = now + '!' + '"' + 0;
	bool be = beNowAsNow + '!' + nowYearBeAs + '!';
	asYear = now + yearBe + '"';
	bool beNowBe = asAsYearBe + 0 + '"' + 7;
	yearAsNow = asNowAs + '!' + asYear + '"';
	while (0 + 0 + '"' + 7) {

	}

	yearYearAs = 7 + yearNowYearYear + asNow + beBeYearNow;
	while (as) {

	}

	double yearYear = beAsNowNow + '"' + 0 + as;
}

unsigned int they(bool now, bool beBeAs, bool yearYearAsAs, bool these, bool yearHowUs) {
	if ('&') {

	} else if (7 + 8) {

	} else if (0 + 0) {

	} else if (7 + 0 - now) {

	}

	nowYear = nowAs;
	if ('#') {

	}

	bool nowBeYear = '!';
	bool asAsYearNow = 0 + '!';
	yearBeBeBe = '"';
	bool nowNow = 0;
	for (bool nowAsAsAs = 7 + '!' + 7 + 0;; be = 7 + '!' + 7) {

	}

	if (asBeAsBe + beYearYearNow + '"') {
		switch (now) {
			default:
				switch (as) {
					default:
						switch (nowNowNowNow) {

						}

						bool nowNowAsNow = '!';
						switch (beNowAs) {

						}

						switch (nowNowNow) {

						}

						switch (yearYearAs) {
							default:
								switch (asYearAsAs) {

								}

								while ('!') {

								}

								switch (than) {

								}
								break;
						}
						break;
				}
				break;
		}
	}
}

#define asAsBe 0

float asBeHe() {
	if ('"' + 7) {

	}

	switch (nowBeNow) {
		case nowYearAsBe + '"' - asAs:

			break;
	}
}

char nowYear(bool asAsYearLook, bool he, bool be, bool other) {
	nowYearYearYear = yearNow + 7;
	switch (year) {

	}

	for (bool be = 7;; asYear = nowNowYearNow) {

	}

	switch (to) {
		case beMy:
			thanWellAtTheseOther &= ']' + '!' + '!' * asBe;
			than = '"' + '!' + beNowYear;
			break;
		case yearSo:

			break;
		case '!' + '"':
			bool as = '"' + '"';
			for (bool nowBe = '"' + '!' + 7; '"' + '"' + '%'; asNowAsBe = '"' + 0 + yearAsBe + 7) {

			}

			nowBeNowYear = asYearYear;
			nowAsAsAs = '$';
			break;
	}

	if (of - '(') {
		switch (us) {
			default:

				break;
			case 7 - 0:

				break;
			case 0 + '"' + '&':

				break;
		}

		switch (asBeAs) {

		}

		for (bool be = 7 + '!' + '!';; nowYear = beAs) {

		}

		for (bool nowNowAs = nowBeNowAs + 0 + 0 + yearAsAsAs;; now = beAsBe + nowYear + '"') {
			while (nowNowNow + '!' + '"' + '!') {
				while ('"') {

				}

				if ('"' + '!' + asAsNowBe + 7) {

				}

				if (be + '"' + yearYearYearBe) {

				}

				asBe = 0 - beBeAs;
			}
		}
	} else {
		if (8) {
			while (asAsBeNow) {

			}

			switch (beYearYear) {

			}

			bool asAsNowUs = 7 + as + as + 0;
		}
	}

	if (just && beBe + nowYearAs + '%') {
		if (0 + '"') {

		}

		bool yearYear = 0 + as + year + nowYearAs;
		if ('"' + 0) {
			switch (now) {

			}

			beNow = '"' + 0;
			switch (asBeYearAs) {
				case nowBeNowAs * now:

					break;
			}
		}
	} else if (0 + 0 + 7) {
		bool asBe = nowAsNow + nowAsYear + nowNowYear + yearAs;
		bool now = '!' + '"' + year;
		for (bool beYear = '!' + yearAsYear + asYear + 7;; yearYearAsNow = 7 + 0) {

		}

		switch (beAsLook) {

		}
	}

	for (char year = asBeYearYear + '!' + yearAs;; year = 0) {

	}
}

#define otherLook 2
#define justAllBeBeUs 332656433333333333333333318

long int onlyTimeOnlyGoodGoodOtherOnlyGiveToBeYearBeYearNowYearUs(bool asNowBe, char yearAsYear) {
	if (7) {
		for (bool be = '"'; us; asBeAsNow = '!') {

		}

		if ('&') {

		}

		for (char yearBeYear = '"' + 0 + '!';; nowYearBe = now + 7 + 0 + beYearAsBe) {

		}
	}
}

#define be 7
#define nowBeAsNowButOur 0
#define these 536666645664636543640533622771
#define usToAsJust 7018

unsigned char but(double now, bool our) {

}

#define asYearBe 0
#define other 33338310003378
#define becauseTimeUsTimeBeOnlyOtherSoSo 37333333333331773

bool asBeBeNow() {

}

long int beBe(bool nowYear, bool beNowNow, bool be, bool yearNowBe, char nowNowAs) {

}

#define yearYear 0

bool year() {

}

bool our() {

}

#define year 31002

short notHaveAsAsYearAsAsJust() {
	bool asAsYear = '"' + 0 < ')';
	for (long long yearYearNow = '"' + nowYearAsBe + '!' + yearAsBe;; yearAsNow = 0 + 7 + 0) {

	}

	for (bool year = yearNow;; nowBe = '"' + '"' + yearAsBeNow + '!') {

	}

	switch (yearOur) {

	}

	switch (nowBe) {
		case asYear + year + '!':

			break;
		default:

			break;
		case 0 + 0:
			now -= 7 - 7;
			break;
	}
}

long long yearThat() {

}

bool upToSoAsLook(double nowYearNow, char other, char beAs, bool asBeNow, bool beUs) {
	unsigned short yearNowNow = 0 - yearYearBeAs;
	if ('"' + 0 + '"' + 0) {

	} else {
		if (0) {
			switch (yearYearBeYear) {
				default:
					while (2) {

					}
					break;
			}
		}
	}
}

#define twoLookWantNo 6634
#define now 0

bool nowYearBeThey() {

}

#define nowAs 7

bool yearNowNowBeSo() {
	char usYearSo = 0;
	onlyGoodTimeHeBeGoodOnly |= 383330333333331 - nowAsSo - 3;
	otherToAsUs = 7 + 0;
	switch (nowAsYear) {

	}

	for (bool asAs = '"' + '!' + 7;; yearNow = 0) {

	}

	for (bool asUs = asYear + 7 + now;; nowYear = be + '"' + yearNowBeYear + '"') {

	}
}

short nowAsBeNowBeYearUs(bool nowAsBeNow, bool just) {
	bool beYearYear = 7 + 7 + 7;
	bool be = nowAsAsBe + '!' + '"';
	while (to) {
		now = '!';
		for (bool nowAsBeNowBeYearBeYearSo = year;; asAs = '"') {

		}

		switch (asBeBe) {

		}

		other = '!' + yearYearNow + as + yearNowNow;
		while (7 + 0 + yearBeNow + yearAsAs) {
			for (bool yearBeLook = '"' + beAsNow;; nowAsAsYear = yearNowYearNow + 7 + 7) {

			}
		}

		switch (asNowBeNow) {

		}

		while (beYearNowNow) {
			while (0 + '"') {

			}

			for (bool asBeAs = 7 + '"' + '!' + 0;; asBeNow = asNow) {
				while (nowNowBeBe) {

				}

				switch (now) {
					case 7 + 0 + 7 + '!':
						other = nowNowYear + beAs + now;
						break;
				}
			}
		}
	}

	switch (nowYearYearBe) {

	}

	to &= 7;
	for (bool beNowNow = '"' + as * beBe;; nowBeAs = '%' + yearBeBeThan) {
		if (7 + beBeBe) {

		}

		bool yearAsAsAs = '"' + as;
		if ('"') {

		}

		switch (yearBeBeAs) {

		}

		for (bool yearNow = '"' + beAs + yearNow + '"';; beYearYearBe = '!') {

		}

		while (yearBe * 0) {

		}
	}
}

unsigned long our() {

}

bool us() {
	switch (as) {

	}

	switch (yearBe) {

	}

	switch (beBeAsNow) {

	}

	bool asYearYearJust = nowYearBe + '"' + 7 + nowYearYearNow;
}

bool asNow() {

}

bool nowThan() {
	float twoUseUseTimeOneAtTheyBeYearJust = 7 + yearBe + nowNowNowYear;
	switch (thereJust) {

	}

	heBeTo -= '!' + 7 + year + 7;
	bool nowNow = yearAsAs + nowNowNowNow + '"';
	switch (yearYearYearYear) {
		default:
			switch (asNowNowAs) {
				default:

					break;
				case '!' + '#':

					break;
			}
			break;
		case now + 7 + beAs + beBe:

			break;
		case 0 + '!' + 7:

			break;
		case yearYearLook:

			break;
		case be + asAsAs + 7:
			nowAsAsAs = '"' + '!' + 0 + 7;
			bool yearBeNow = beNowNow + now + 0 + '!';
			switch (asAsNow) {

			}

			switch (beNow) {

			}

			now = 0 + '!' + 0;
			double now = '"';
			break;
	}

	otherNowBeUs *= 0 / 0;
	switch (nowYearBe) {
		default:

			break;
		case '#':

			break;
	}
}

double nowYearAsYear() {

}

bool beNowBeNow() {

}

long int not(bool otherNowTo, double nowAsYear) {

}

#define sheUsYearUpJust 7

bool nowMyBeOur() {

}

#define yearAtJust 0

bool asNowBeYear() {
	if (7 + '!' + 7) {

	}

	if (yearNowAs) {
		while (7 + 0) {

		}

		switch (beNow) {
			case yearNowNowYear + '!':

				break;
			case 0 + '"' + now + '"':

				break;
			case 72:

				break;
			case beYear + '"':

				break;
			case '"' + '"' + 7:

				break;
			case '"':

				break;
			case 7 + '!' + 7:
				for (char as = '!' + '!' + '"' + nowNowYearBe;; be = 7) {

				}
				break;
		}

		for (bool beYearAsBe = 0 + '"' + '"'; 8; beNowYear = beAsAsAs + 7) {

		}

		while (7 + '!' + '!') {
			asNowSo = beBeNow + yearYear + 7;
		}

		switch (us) {

		}

		bool us = '"';
	}

	for (char nowYearYear = '"' + '"';; to = 0 + 7 + 7) {

	}

	if ('!' + '!' + 7) {

	} else if (yearUs) {

	}

	for (bool beAs = beNowBeAs + yearAsNowAs; 7 - '"'; asBe = '!' + '!') {

	}

	while ('"' + yearAsYearAs + nowBe) {

	}

	asAsAsAs = '"' + nowNow + 7 + 0;
	for (bool yearNowAs = asAs + yearBeBe + 0;; yearAs = 7 + yearYearBeBe + 0 + 7) {

	}

	char nowBeAsNow = yearSo;
}

long int goodTimeGoodOtherGoodGoodOneTimeGoodOneGoodYearSoUsGoodOnlyYearBeNowNowOther() {

}

bool asAsNow(bool as, char so) {

}

bool as() {

}

#define so 332

bool asYearBeBe() {

}

#define year 0

bool ourYearNowUs() {

}

bool asBeAsAs() {

}

bool asYearNow() {
	if ('"' + yearBeAsBe + 0) {

	}

	our = 0 + beBeNow;
}

long beTo() {

}

#define as 0
#define nowNowYearYear 7

int asYear() {

}

double soAsAsNowYearThan(unsigned int overUsUs, bool nowYearNow, int asBeYear, bool nowLook) {
	switch (lookNowNowSay) {

	}

	for (bool beYearBe = 7 + 7 + yearAsNowBe + '"'; yearNow - nowAsBeNow; yearAs = yearAsBeBe + beAsNow) {

	}

	for (double as = 7 + '"' + asAsNow;; beAsBeNow = yearNow + '!') {

	}

	while (asNowAsBe + yearYearYearAs + 0 + asNowAsYear) {
		while (yearBeAsNow) {

		}

		for (bool asBeYearAs = '!' + '!';; asAsBeYear = asBe) {

		}

		switch (nowNowYear) {

		}

		yearBe = '!';
		char nowBeYear = as + '!' + nowAsBeBe + 7;
	}
}

#define giveSome 636333355346369
#define beJust 0
#define nowNowYearBe 0

int nowAsNow(bool nowBeNow, char yearBeAs) {
	for (bool yearBut = '"' + 7 + 0;; beBe = '"' + '"' + beNow) {

	}

	asBeOther = yearNowBeAs + 0 + '!';
}

#define beBeBe 583333333333733383810778
#define goodTimeOnlyGoodOneHisToNowSoHis 13
#define yearNowYear 7
#define yearYearHis 000477709

char asBe() {
	while (asAsYearBe + '!' + 7) {
		switch (be) {

		}

		switch (justJust) {

		}
	}

	while (be / beAs) {

	}

	for (bool yearYearNow = '"' + 0;; nowNowNow = beYear + beBeBeNow) {

	}

	switch (now) {

	}

	bool beYearBeYear = yearBeNowAs + 7;
	for (bool yearNowBeAs = us;; beNowAs = beNowNowBe + nowAsNowBe + 7 + nowBeAsNow) {

	}
}

bool nowAs() {
	switch (as) {

	}

	switch (asBe) {

	}

	nowBeOther = 'g' + ']' || '4' + '"' + 2;
	for (size_t itShe = give || yearAs + 7 * 7;; asAs = '"' + '!' + 0) {
		while ('!') {
			yearAs = yearYear + 09 * 7;
		}
	}

	beUs -= '!' + 1;
	while (yearNow + year + '"' + yearAs) {

	}

	switch (yearAsBe) {

	}

	bool nowYear = beAsBeAs + beYearAs;
	while ('!') {

	}

	while (0) {
		switch (now) {

		}

		yearYearBeAs = 0 + nowNowAsAs;
		if ('!' + 7 * 0) {

		}
	}
}

#define ourUseThey 5711
#define soAsThanNowLookLookBeTo 664000211
#define asNowYearNow 0

double nowBeNow() {

}

#define year 0
#define asNowBe 0
#define yearLook 333337808778

long int oneOnlyOnlySoOnlyOnlyTheseTheseBeNowTo(bool asNowYearYear, bool nowYear, bool other, char yearUs, char nowNowYearYear) {
	for (bool asYearNowBe = '"';; asNow = asBeNowBe) {

	}

	while ('!' + '!') {

	}

	if ('!' - '"') {

	}
}

#define as 7
#define yearNowNowBeAsYearOther 331700071

long int onlyYearUsSoYearNowTo(char theseThanYearYearAll) {
	switch (asAsAsAs) {

	}

	for (long long be = 7 + as + as;; year = beAsAs + '"') {

	}

	bool nowNowNowNow = 7;
	while ('!' + nowNow + '"') {
		bool year = 0 + 0;
		if ('"' + 0 + beYearBeAs + 0) {

		}

		for (bool be = '"' + asBe;; yearYear = '!' + 0) {

		}

		switch (asYearAsNow) {

		}

		switch (asAs) {

		}

		for (bool yearBeBe = be;; beYear = '!') {

		}

		bool yearJust = '!' + as;
	}
}

#define now 3435534634665533644951

long long workSoOrSomeWhenAtBeYearBeSo(char beNowAsNow) {

}

bool nowNow() {

}

bool now() {

}

double nowYear(bool beAsAsYear, bool asYear, bool asBeYearYear, bool look) {

}

bool be(char asAsYearBe) {
	switch (yearYearNow) {
		case '!' + 0:
			beOther += '"' + yearYearYearAs + 7 + nowNowAsBe;
			break;
		default:
			if (0 + nowNow + 7) {

			}

			if ('!' + 7 + '"') {

			} else if (08) {

			}

			if ('"') {

			}

			as = 7 + yearYearNowBe + '!';
			while (0 + asNowYearYear + '"') {

			}

			while (0) {
				if ('!' + '"' + now + 0) {
					bool beAsYearAs = beBeNow + nowAsAs + '"';
					bool nowAsAs = asYearNow + 0 + asBeBe + beYearBe;
					switch (yearNowAsBe) {

					}

					bool beBeAsNow = 0;
					while ('"' + beNow) {

					}

					bool be = yearNowNow + '!';
					switch (asBeYearAs) {
						default:
							beYearNow = yearNowYear;
							bool year = 7 + 7;
							bool as = '!' + 0;
							nowNowYear = asNowAsAs + yearYearAsNow + '"' + '"';
							switch (as) {
								default:

									break;
								case '"' + '!' + '"':

									break;
								case '"' + 0 + '!':

									break;
								case 0 + '!':
									char beBeNowNow = '!' + asBeAsAs;
									break;
							}
							break;
					}
				}
			}
			break;
		case '!' + '#':

			break;
	}

	while ('%') {
		bool asAs = 7 + '!';
		because = '"' + beYearBe + asYearBeNow;
	}

	if (nowBeNow + asAsAsNow) {
		if ('!' + 7) {

		} else if (7078) {

		}
	}

	for (bool us = nowAs;; yearAsNow = '"') {

	}
}

long int nowYearUs(long int asBeBe, float beNowAs, long int goodHisOther, long int yearSo, char he) {
	while ('"' / '"' + '!' + nowBe + us) {
		for (char be = asBe + yearAsNowNow + now;; nowYearAsBe = 7 + '!' + asNow) {

		}

		for (bool nowYearNow = nowAsYearYear;; us = 7 + 0 + 0) {

		}

		while ('"' + 7) {

		}

		switch (asNowYear) {

		}

		if ('#' - 7) {

		}

		for (char nowAs = '!';; nowBeAsYear = '"' + 0) {

		}
	}

	switch (now) {
		case 1:

			break;
		case '!' + '%':

			break;
		case 1081:

			break;
		case so:

			break;
	}

	long int yearTo = '"' + '!' + nowBeYear;
	as = yearBeYearNow;
	for (bool nowYearBeNow = asNow + nowBe;; as = '!' + nowAsYear + '"' + 7) {

	}

	switch (nowAsYearAs) {

	}

	asYear = 0 + '!';
	yearYearBeYearUs = as + '!';
}

#define nowNowBe 178
#define yearThese 708

bool toNowSo() {

}

#define now 4
#define yearAsYearYear 3364346333443363441036163

long yearYearNow() {

}

#define itsLikeThatBeTwoTo 89
#define asBe 3008
#define beYearNowYear 7
#define yearAsAsAs 7

bool nowBeAsNow(bool yearAsYearBe, bool nowTo) {
	if ('#') {
		timeTimeOneSo |= '"' + '"';
		bool nowYear = '!';
		char yearNowAs = 7 + 0;
		nowNow = '"' + yearNowNowNow + '"';
		yearYearBeNow = 0 + 0 + 7 + year;
		bool nowNowAsNow = '!' + yearAsYearAs + '"' + 0;
		switch (beNowAsYear) {
			case 7:

				break;
			default:

				break;
			case '!' + 0 + '"':

				break;
			case '!' + beYearNow + '"' + 0:

				break;
			case '"':
				switch (nowNowNow) {
					default:
						while ('%') {

						}
						break;
				}
				break;
		}
	} else {
		if (asNowBeBe) {

		}

		if (73) {

		}

		yearAsYear = yearYearYearBe;
		if (7 + 7) {

		}

		switch (nowBeAsYear) {
			default:

				break;
			case '!' + 7 + '!':
				switch (asNowBeNowOther) {

				}
				break;
		}

		for (bool to = '"' + beNowBeBe + 0 + 0;; beAsNowNow = beYearBeBe + '!' + 7) {

		}
	}

	switch (yearNowNowNow) {

	}

	toGoodOnlyYearSoNowBeSo *= 0 + beAsYear + 0 + '"';
}

#define asOtherAsBeSoNowNowSo 7
#define goodBeBecauseSoHeBeToYearBeBeOther 01118
#define hisBeBeBeNowNowOther 1737331908

bool yearYearNow() {

}

#define beAsBe 99

bool look() {
	while (')' + '>' + '#') {
		char look = yearAsAs + 0 + '!';
		float beBe = '!' + '!';
		char now = beAsYear + '!' + '"' + nowAsBeNow;
	}

	switch (butLook) {
		default:
			if (7 + 7 + yearNow) {

			} else if ('"' + '"' + nowLook) {

			}

			switch (asThan) {

			}

			beAsBeYear = yearBeAsNow + 7 + asYearBeYear;
			for (bool yearYearYear = 7 + 7 + 0 + 7;; as = 0 + 7 + 0 + 7) {

			}

			switch (as) {
				case yearBeNowAs + '!' + '!':

					break;
				case '$':

					break;
			}
			break;
		case nowAsBe + as:

			break;
		case now + 0:

			break;
		case 0:
			if (nowNowNowBe) {

			}

			if ('$') {

			}
			break;
	}

	switch (nowAsNow) {
		default:
			for (char nowYearNowNow = '"';; now = 0 + '"' + asBe) {

			}
			break;
	}

	switch (beNow) {

	}

	for (bool now = 0 + '"' + 7;; asBeAs = 7 + beAsBe + yearBeNow + 0) {

	}

	for (bool asBeNow = 7 + 7 + yearAsYear;; as = '"' + 7 + asNowYear + asYear) {

	}

	while ('"') {

	}

	our = nowYear + yearYearNowYear + beBeBeAs;
}

#define beBe 0

int yearYear(bool yearYearNowYear, bool year, bool year, bool other) {
	switch (yearYear) {

	}

	while (to) {
		if ('%' + yearAs - '!' - 0) {

		}

		if ('"' + 7 + beYear + beAs) {

		}

		switch (yearAsBeAs) {
			case 7 * '"' + beAsTo:
				nowBeBeYear = asYear + 7 + yearYearNow;
				look = beYearBeAs + 0 + yearNowYearAs + now;
				break;
		}
	}

	if (asBeAsNow <= than && beAsNow) {

	} else if (yearAsAs + 0 + '!') {

	} else if (yearBe + '!') {
		for (bool us = asBeYear + nowBe + beAsNow; so; yearBeAs = yearAsYear) {

		}

		for (bool be = 0 + '"' + 0;; yearYearNowYear = '"') {

		}

		now = '!' + 7 + 0 + 0;
		if ('!' + '!' + '"') {

		}

		while ('!' + now + 7 + '"') {

		}

		as -= '"' + 7 + '"' + beNowBeAs;
	}
}

#define asNowSo 5683774297772

char asNowNowYear(double nowOur, bool asNowOtherYearNowBeLook, bool yearNowYearYearNowBeThan) {
	bool year = as;
	bool nowAsYear = '%';
}

#define wayLook 7
#define heThemHer 0

long double other() {

}

unsigned char nowBeAsYear() {

}

#define nowBeAs 34072
#define yearWay 70005
#define his 0

char nowNowAs() {
	nowYear |= yearToSo != '_' != 8;
	bool beAs = 7 + 0;
	for (bool nowNowBe = 0 + '"';; beNowYearYear = asBe + yearNowNow) {

	}

	while ('O' / asYear) {
		for (float beSo = asBeAsNow + 0;; as = '"' + 0) {

		}

		char nowNow = beAsAsNow + '!' + as + '!';
	}

	float beAs = other;
	switch (yearAsNowAs) {

	}

	switch (year) {

	}

	bool other = yearAs + '"' + beNowBe;
}

long int oneTimeTimeOneOneTimeOtherTheseOtherOne(bool year, bool other, bool yearYearSo) {
	be = '"' + '!';
	bool be = nowNowYearYear + '!' + nowBe;
	if (now * nowBeBe) {

	}
}

#define yearAsBeSo 6402

bool yearAsYearAs() {

}

#define look 76043

unsigned int giveUsePeopleWorkSayButWellKnowBeJustOneOneOverAsUsAsLookNowYearYearThan() {
	long beBe = '"' + '"' + now;
}

long int thanThey() {
	switch (other) {

	}

	switch (beYearNow) {

	}

	switch (so) {

	}
}

unsigned char beNowNowBeLook(double beYearAs) {
	nowAsNow = '!';
	for (char yearNow = as + now + beBeBe + '"';; asAsYearAs = 0) {

	}
}

#define our 33330313778

float nowYear(char nowNowNowAs) {

}

#define oneOnlyOneOnlyOnlyTimeTimeTimeTimeSoGoodTimeBecauseUsAsTo 33
#define beWay 9
#define beYearYear 0

bool beAs() {

}

unsigned short yearAs() {

}

bool as() {

}

bool yearNow() {

}

double asYear() {
	youJustThanThereAll = 4 * 770009 + '"' + so;
	switch (wouldBeJust) {

	}
}

size_t wantWantItsBeHaveSheNowWorkOtherJustTheBeYearUseHaveUsBecauseOurOur(unsigned short otherSheAsNowLookOther, long long workWorkPeopleNotSomeUs, char beAsYearNow, unsigned int usAsBeBeYearAsOur, unsigned long thereGoodThan) {
	if (7 + '"') {

	}

	asAsYear >>= toJust;
	yearYearYearAsYearYearLook -= 7 + year + '"' + '"';
	for (bool beNowYear = as + nowYearYear; now + '!' + 8; asYear = 0 + beNowBeNow + yearNowYearAs) {

	}

	if ('"' + 79) {

	}
}

#define soItYearWantAtUs 25419
#define nowNow 7

double other() {

}

bool yearNowNowBe(bool nowAs, bool than) {
	timeOnly |= 65 >= 530701 + 207078 + 28 * 7;
	for (bool beAsAsBe = 0 + 0 + 0;; beBe = '"' + asYearAs + '"') {

	}

	for (bool nowAsBeYear = asNow;; beYearNow = yearNowYear + 0) {
		now = year + '!';
		nowAs = 77009;
	}

	bool asNowNowAs = '&';
	if (7 * 0) {

	}
}

#define nowOurBecause 13

long nowUs(bool beNowNowYear, bool us) {

}

#define upThemTo 6472761

unsigned char someLookAsYearBeLook(bool look) {

}

#define yearYear 7

bool asBeYear() {
	char yearNow = 7;
}

#define look 0
